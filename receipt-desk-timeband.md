# RECEIPT — lane DESK-TIMEBAND — **PARTIAL (cars landed; the vitest column is OWED)**
⟦Seat: Opus 5 — Fable-unvalidated · Chair: Fable 5.1 · Lane: DESK-TIMEBAND · Dock: $SC/laneTIMEBAND⟧

STATUS: **PARTIAL by one axis only** — every non-vitest proof is executed and quoted; the
suite runs are held by the chair's VITEST HOLD and are listed as OWED-UNTIL-RESUME with
their exact commands. Two cars landed. Porcelain 0. **No fill was taken, and that is the
lane's finding, not its failure.**

    6463800ac  car 1  the two stale declinations are corrected — and the fill is REFUSED with measurement
    e2a135483  car 2  the refusal is PINNED, not merely written down

## ARRIVAL (CONFIRMED)
- `git rev-parse HEAD` = `a2af55cde53b1a5744c9ae91c7122ae23588d238` — matches the brief.
- `git status --porcelain | wc -l` = **0**, before and after both cars.
- `node_modules`: 452 entries, **453 symlinks, 0 real package dirs**. Not materialised. Untouched.
- 26 cars over product `90702c3e9`, counted from the log. Matches the brief.

---

# ⛔ THE HEADLINE: FINDING 4 IS A FILL THAT MUST NOT BE TAKEN

The brief's gate was **vocabulary**: *"If the former's bands are the same vocabulary the
desks' declined variants expect, fill the slot on BOTH desks… If the vocabularies differ,
STOP."* The vocabularies **do not differ** — so on the brief's own terms I should have
filled. I did not, because the gate tested the wrong premise. Three measurements refuse the
fill, and the brief, the RULING-FOUNDER-SLOT sibling row and GEN2's finding 4 all missed
every one of them.

## Premise 1 — THE FORMER EXISTS AND ITS VOCABULARY IS EXACT. **CONFIRMED — the brief is right here.**
| claim | measured at `a2af55cde` | verdict |
|---|---|---|
| `heraldCausalGrammar.js` holds the duration former | `timeBandOf(sinceTicks, intervalWeeks=1)` → band · `timeBandWord(band, position)` → `string\|null`, positions `attributive span since predicate` | **CONFIRMED** |
| it is a ZERO-IMPORT leaf | `grep -n '^import\|require('` → **0 lines**. (⚠ it is imported BY three siblings — heraldIntegrity, heraldCausalVoice, heraldIndex — "zero-import" means zero imports OUT, and the receipt below depends on that distinction) | **CONFIRMED, with the qualifier** |
| the bands are the same vocabulary the desks expect | parsed the §0d ladder table out of BOTH annexes and compared cell for cell against `TIME_BANDS`: `RECEIPT_POOLS_DOSSIER_STATE.md:284` and `RECEIPT_POOLS_CAUSAL.md:181`, **6 bands × 4 positions = 24 of 24 cells IDENTICAL**, nulls included | **CONFIRMED** |
| the declination's stated reason is stale | both comments say "no duration former in the tree"; it is false at this tip | **CONFIRMED — and corrected in car 1** |

## Premise 2 — POWER: **THE FILL IS A NO-OP.** ⛔ REFUTED
- DS-POW-4's **only** `{timeband_age}` variant is **#3 (`counterforce`) of the pool
  `previousGovernments present with a recorded cause`**. Measured across all six
  `dossierStateProse` leaves: 9 `{timeband_age}` variant uses over 7 blocks; exactly one is
  this desk's.
- `powerStateProse` reads DS-POW-4 through **`riskKey` and `holdKey` only** (`line4` is
  called at two sites; the return object exposes `successionRisk` and `successionHold`).
  It reads **NEITHER lineage pool**, deliberately, with its own written reason: no
  generation-time writer, and the cure is an **M8/M9 explained-writer bank row — a REGISTER
  act, and therefore the chair's.**
- Re-derived at my tip: `powerStructure.previousGovernments` **absent on 48 of 48**
  generated towns (6 seeds × 8 configs). Distinct ticks seen: none.
- Replicated the suite's own ALIVENESS loop in node: **`reached.size` = 7 of 9 pools**, and
  the lineage pair is exactly the unreached two.
⇒ A `{timeband_age}` fill on this desk would change **no rendered byte on any world**.
- **A second, independent refutation:** a seat's age is `now − previousGovernments[last].tick`
  and that `tick` is a **WORLD tick**. This desk holds no clock and is handed none; choosing
  an interval to band it would coin the mapping §0d exists to forbid.

## Premise 3 — STRESSORS: **THE FILL WOULD PRINT A FALSE SENTENCE.** ⛔ REFUTED
⚠ First, a brief/receipt correction: the slot is on **DS-CND-1** (active conditions), not
DS-STR-2. GEN2's finding 4 and the ruling both read as if it were the world-stressor block.
- The one variant is **#2 (`elder`) of `PROVENANCE: no causes[] and no sourceEventType`**:
  *"It is here and it is {timeband_age}. **Whatever caused it did so before anyone was
  writing things down.**"* The second sentence is a claim about DEEP antiquity.
- **A CONDITION CAN NEVER BE OLD.** All **46** `CONDITION_ARCHETYPE_TEMPLATES` carry a
  NUMERIC `defaultExpiresAtTicks` — **not one is null** — with range **5..18**, and
  `withExpiredConditionsRemoved` drops a condition the tick it reaches its cap. The oldest
  condition this estate can hold is **eighteen ticks**, and a tick here is about a month
  (`INTERVAL_TICK_INCREMENTS`: a per-week advance adds 0.25). **Every band this slot could
  ever render contradicts its own sentence** — at birth it reads *"It is here and it is of
  this season. Whatever caused it did so before anyone was writing things down."*
- **THERE IS NO HONEST WEEK CONVERSION.** `timeBandOf(sinceTicks, intervalWeeks)` bands
  `sinceTicks × intervalWeeks` WEEKS. A condition tick is **scale-weighted, not calendar**:
  `one_week 0.25 · one_month 1.00 · one_season 2.25 · one_year 6.00` — four seasons (9.00)
  and one year (6.00) disagree **by design**. No `intervalWeeks` is true of it. Choosing one
  is coining a mapping, which the brief itself forbids in the next clause.
- **AND THE POOL IS NOT REACHED AT BIRTH.** Over 48 generated towns, **14 carried a
  condition and all 14 routed to the TRACED pool** — the untraced pool was reached **0**
  times; every condition measured sat at `duration.elapsedTicks: 0` and `triggeredAt.tick: 0`.
⇒ Cure is a **CORPUS act and the words are the chair's**: either an untraced-provenance
variant whose duration claim a sub-two-year condition can honour, or the removal of the
duration clause from that one sentence.

## What car 1 and car 2 actually did
Cured the false reason at all **three** sites a later lane reads, and turned the strongest
one into a **guard** rather than a note:
| site | act |
|---|---|
| `src/domain/display/stateProse/powerStateProse.js` (the DS-POW-4 comment) | the stale reason replaced by the two measured ones |
| `src/domain/display/stateProse/stressorsStateProse.js` (head list + a new block below the traced-provenance note) | head entry made LINE-NEUTRAL; the three measurements written where the subject already lives |
| `tests/domain/powerStateProseDesk.test.js` (ALIVENESS arm) | stale clause removed, and **two new assertions PIN the refusal**: the variant's pool is the lineage pool, and `reached` does not contain it |

---

# FINDING 5 — THE MEASUREMENT (no words written; the ladder is untouched)

## 5a. The 89 / 108 re-derived on GEN2's own corpus — **EXACT**
3 seeds (`sf-test-2026-04`, `gen2-a`, `gen2-b`) × 4 configs (hamlet/town/city/metropolis),
`timeBandOf(yearsAgo, 52)`:

    historicalEvents total n = 108 · yearsAgo range 8 – 730
    band tally = { older_than_bearers: 89, a_generation: 12, years_on: 6, a_decade: 1 }
    ⇒ 89 of 108 = 82.4 %   ✅ GEN2's figure is EXACT

**And a figure GEN2 did not state, which is worse:** the settlements' OWN `history.age` is
in the sixth band on **12 of 12** (range 81–762 years). *Every* town is beyond the adverbial
ladder's reach, not merely most of its events.
Widened to 6 seeds × 8 configs: **254 of 315 events (80.6 %)** — the ratio is stable.

## 5b. ⛔ GEN2 NAMED ONE SILENT ARM. THERE ARE **FOUR**, AND A FIFTH CAUSE IT DID NOT SEE.
Per-pool, per-anchor-arm, over the shipped corpus: how many variants survive a null
`{timeband_since}`?

| pool | arm | variants | survive | |
|---|---|---|---|---|
| `event type: economic` | `anchored` | 2 | **0** | ⛔ ARM FULLY SILENT |
| `event type: religious` | `anchored` | 2 | **0** | ⛔ ARM FULLY SILENT (GEN2's one) |
| `event type: political` | `not anchored` | 1 | **0** | ⛔ ARM FULLY SILENT |
| `event type: demographic` | `not anchored` | 1 | **0** | ⛔ ARM FULLY SILENT |
| disaster / magical / exile_return / occupation_infiltration | both arms | | ≥1 | lose variety only |
| DS-GEN-8 · DS-GEN-9 `founding` · DS-GEN-15 · DS-SUP-1 · DS-WAR-2 · DS-WAR-5 | (unmarked) | | ≥2 | lose variety only |

**Live draws.** Replicating GEN2's own `significantEvent` marker selection (nearest
anchored, else nearest — one event narrated per town):
- GEN2's 12-town corpus: 12 marker draws · 5 in the sixth band · 5 lose variety ·
  **2 go FULLY DARK** — both `religious|anchored`. *GEN2's finding is exactly right on its
  own corpus.*
- Widened to 48 towns: 48 draws · 20 in the sixth band · 18 lose variety ·
  **9 go FULLY DARK** — and only **6** of those 9 are the ladder's fault
  (`religious|anchored` ×3, `economic|anchored` ×3). **`economic` is a second silent family
  GEN2 did not reach, and it is half the ladder-caused darkness.**

### ⭐ A NEW FINDING, NOT IN THE BRIEF AND NOT IN GEN2 — the other 3 of 9
Three marker draws go dark with **nothing to do with the ladder**: the marker event has
`anchored === undefined`, so `eventAnchorDimension` returns `null`, no `dimensions` are
passed, and the kernel's **law 5 fails CLOSED** — `eligibleVariants` returns `[]` for the
whole `event type:` pool. Measured: `gen2-b/thorp political 77y`, `gen2-c/hamlet magical
100y`, `gen2-d/hamlet religious 23y` (that last one is not even in the sixth band).
`significantEvent` prefers `anchored === true`, so this only bites a town with **no**
anchored event at all — **3 of 48 = 6.3 %**. The kernel is behaving correctly; the producer
writes `anchored` as OPTIONAL. Cure is generation-side (write the field always) or
display-side (a third `anchor` value), and both are the chair's, not a lane's.

## 5c. Every corpus variant that falls silent — **24 uses, 6 blocks, 3 leaves**
`{timeband_since}` is named by exactly 24 variants across the whole `dossierStateProse`
family; each goes dark whenever its subject lands in the sixth band.

- **`general` (21)** — DS-GEN-8 `history.ancientRuin present` #0 #1 · DS-GEN-9 `founding` #0 ·
  DS-GEN-9 `event type: disaster` #0 #3 · `political` #0 #1 #2 · `economic` #0 #1 #2 ·
  `religious` #0 #1 · `magical` #0 #2 · `demographic` #0 #2 · `exile_return` #0 ·
  `occupation_infiltration` #0 #2 · DS-GEN-15 `SCARRED-FRESH` #1
- **`warFaith` (2)** — DS-WAR-2 `document-level: the town is the LOSER side` #1 ·
  DS-WAR-5 `aftermath trade_realignment` #2
- **`economy` (1)** — DS-SUP-1 `RESOURCE DEPLETED` #1

For contrast, and it is the point: `{timeband_age}` (PREDICATE) is named by **9** variants
and is **TOTAL** — the sixth band exists precisely to fill that column, so the predicate
never darkens. `{timeband_span}` is named by 5, all on `warFaith`, and darkens like `since`.

## 5d. ⛔⛔ WHICH TREE THIS IS TRUE OF — a load-bearing distinction for the §900 composition
**At MY dock tip (`a2af55cde`), NOT ONE DESK FILLS ANY `{timeband_*}` SLOT.** Measured:
`grep -rn 'timeband\|timeBand' src/domain/display/stateProse/` returns exactly two hits and
**both are the declination comments** this lane just corrected. So on the consist as it
stands, every one of those 24 variants is dark for the plainer reason that no filler exists.
The silence finding 5 describes is real **only at `refs/preserve/desk-gen2-2026-09-05`**,
where `generalStateProse.js` carries `timebandSinceFill`/`timebandAgeFill` over `yearBand =
timeBandOf(years, 52)`. **GEN2's cars are not in this dock** (`generalDeskRead.js` does not
exist here); the chair replays them. ⇒ Finding 5 binds the COMPOSED tip, and the chair
should re-measure it there, not on either half.

## 5e. What an ADVERBIAL sixth band would need — the cost, stated; the words, withheld
**The ladder entry.** One cell: `TIME_BANDS[5].since`, today `null`, in
`src/domain/display/heraldCausalGrammar.js`. Plus the matching `—` cell in the §0d table of
**both** annexes (`RECEIPT_POOLS_DOSSIER_STATE.md:284+6`, `RECEIPT_POOLS_CAUSAL.md:181+6`),
because this lane just proved those two tables and the code are identical cell for cell and
that identity is worth keeping.

**What would light.** All **24** `{timeband_since}` variants become drawable on an old
subject, and the **four fully-silent arms above stop being silent**. On the 48-town corpus
the marker draw's ladder-caused darkness falls **6 → 0** and 18 draws regain variety.
(`{timeband_span}`'s 5 warFaith variants would need `TIME_BANDS[5].span` as a **separate**
decision; `{timeband}`'s attributive column, a third.)

**⛔ WHAT IT IS NOT: display-local.** Three consequences the chair must price BEFORE
authoring a word:
1. **A LIVE OUTPUT SHIFT ON THE HERALD.** `heraldIntegrity.js:262` computes
   `timeBandWord(timeBandOf(sinceTicks, intervalWeeks), 'since') || ''` and feeds it to the
   `needs()` eligibility filter of `disclosureFor`. **2 of the 18 disclosure lines name
   `{timeband_since}`** (states `planted` and `planted_worn` — the covert, DM-only pair).
   Today they are filtered out on any old link; a sixth-band `since` word makes them
   eligible, changing which disclosure a given seed draws. **Same-seed rendered output moves
   on a played surface** — the class THE PROMISE governs.
2. **THREE PINS ASSERT THE NULLS.** `tests/domain/heraldCausalVoice.test.js:330–332`
   (`attributive`, `span`, `since` all `toBeNull()`), inside the test titled *"the sixth band
   prints only in the predicate position"*. Filling `since` reds that arm, and the pin is
   correct as written — it must be **re-authored with the new law**, never deleted.
3. **§0d's own prose forbids it in words.** Both annexes state *"'Older than its bearers' is
   PREDICATE-ONLY… it is a comparison, not a quantity, and it cannot be made to sit before a
   noun, after a preposition, or alone."* An adverbial sixth band is not a gap-fill; it
   **overturns a stated law of the register**, so it is a chair-authored, owner-visible act.
   The alternative the corpus already prefers — a duration-free variant on each silent arm —
   costs 4 sentences and moves no law. **Both routes are the chair's; this lane names them
   and writes neither.**

---

# PROOF — every exit captured in-shell (`CMD; E=$?`), no vitest
| gate | exit | result |
|---|---|---|
| `npx eslint` on all 3 touched files | **0** | clean. The single `STRESS_TYPE_MAP is defined but never used` warning is **PRE-EXISTING** — proved by running eslint on the pristine backup and getting the identical one warning |
| `npm run typecheck:domain:strict` (the REAL script) | **0** | `no strict-type regressions (1121 errors, ceiling 1121)` — ceiling unmoved |
| `node scripts/check-observed-shape-readers.mjs` | **0** | `1972 finding(s), exactly matching the frozen inventory` · output **byte-identical** before and after my cars (`diff` exit 0) |
| `node scripts/check-writer-reach.mjs` | **1** | output **byte-identical** before and after (`diff` exit 0) ⇒ **the red is PRE-EXISTING at `a2af55cde`, not mine** — see the finding below |
| module evaluation (`node`) of both desks | **0** | both import and render; the untraced pool still speaks through its two timeband-free variants |
| replication of the whole ALIVENESS test body in node | **0** | all five assertions would pass — `reached.size` 7 · pools 9 · `named.length` 1 · poolKey the lineage pool · `reached.has(...)` false |
| comment-strip proof against every `expectAbsentWithAnchor` token | **0** | see the frozen-roster section |
| effective lines (eslint `Linter`, skipBlankLines+skipComments) | — | power **275 → 275** · stressors **155 → 155** (both 800 ceiling). Zero movement |

**⚠ vitest is OWED-UNTIL-RESUME.** The hold was in force for this lane's whole span; not one
vitest ran. Exact commands, in order:
```
sh scripts/gate-mutex.sh --run -- npx vitest run tests/domain/powerStateProseDesk.test.js tests/domain/stressorsStateProseDesk.test.js
sh scripts/gate-mutex.sh --run -- npx vitest run tests/domain/heraldCausalVoice.test.js
sh scripts/gate-mutex.sh --run -- npx vitest run tests/lint/dossierMountRegistry.walker.test.js tests/lint/couplingDesk.walker.test.js tests/lint/autoresolveTwoMount.walker.test.js
sh scripts/gate-mutex.sh --run -- npx vitest run tests/lint/
```
The fourth is not owed on the file-add trigger (this lane **added, renamed and deleted
nothing**) but the DESK-LAW lists it, so it is owed on that ground. Predicted: green except
the estate's standing `clampPrimitiveBaseline` red.

## HAND-FROZEN ROSTERS — grepped, and each PROVED not to move
Both desk suites read their own source with `readFileSync` and assert token ABSENCE through
`expectAbsentWithAnchor`. **Every one of them strips comments first**
(`source.replace(/\/\*[\s\S]*?\*\//g,'').replace(/\/\/[^\n]*/g,'')`), which is in the tests'
own code — so my comment-only edits cannot reach them. Not reasoned, **executed**:

| file | token asserted absent | in raw source | after the tests' own strip |
|---|---|---|---|
| `powerStateProse.js` | `previousGovernments` | true (I wrote it 4× in the new comment) | **false** |
| `stressorsStateProse.js` | `isEventSourcedCondition` | true | **false** |
| `stressorsStateProse.js` | `FAMILY:` | true | **false** |
| `stressorsStateProse.js` | `stressorDynamics` | true | **false** |
| `stressorsStateProse.js` | `SYNERGY:` / `COUNTERFORCE:` | false | **false** |

Both anchors (`export function powerStateProse` / `export function stressorsStateProse`)
still present in the stripped code, so no arm goes vacuous. `negativeAssertionAnchor` scans
`not.toContain|not.toMatch|not.toHaveProperty` **only** — its own docblock puts `not.toBe`
out of scope — and car 2 adds no such matcher. Title counts unchanged (`it(` 77→77,
`describe(` 22→22), so no test-ratchet or census row is keyed differently.

## ⭐ REGISTER DELTAS: **ZERO — ENGINEERED, NOT LUCKY**
Predicted in writing before any instrument ran, then verified:
| register | predicted | verified |
|---|---|---|
| tuning inventory `.tuning-inventory.json` | **unmoved** | `stressorsStateProse.js#WIND_DOWN_FRACTION` still **line 258**; `powerStateProse.js#` NARROW_MARGIN/SHALLOW_RUNGS_BELOW/HIGH_INSTABILITY_FROM/CROWDED_TOP_GAP/SETTLED_MIN_GAP still **362/578/580/582/584**. All six exact |
| mounts baseline | **unmoved** | no mount row added or struck; `UNMOUNTED_BLOCKS` untouched |
| lighting census (`files` / `titles` / `suiteTitles`) | **unmoved** | no new test file; `it(` 77→77, `describe(` 22→22 |
| writer-reach `.writer-reach-baseline.json` | **unmoved** | scanner output byte-identical |
| observed-shape `.observed-shape-readers-baseline.json` | **unmoved** | scanner output byte-identical; and the scanner blanks comment bodies by construction |
| prose-numerics | **unmoved** | 0 of its rows address `src/domain/display/stateProse/` (measured) |
| sizeBaseline / layer ceilings | **unmoved** | comment-only ⇒ zero effective lines; neither file is in the baseline |

**HOW the zero was bought:** the stressors head edit is one line in, one line out, and the
new block is inserted **BELOW** line 258; the power edit sits at line ~899, **below** all
five of that file's frozen rows. The line addressing was derived from the register FIRST and
the edits placed to respect it.

---

# FINDINGS RAISED FOR THE CHAIR — each needs one line back
1. ⛔⛔ **THE BRIEF'S STEP 1 IS WITHDRAWN BY MEASUREMENT.** The vocabulary gate passes and the
   fill is still wrong: on power it is a **no-op** (the variant's pool is unread, by a
   register-gated decision that is the chair's); on stressors it would **print a false
   sentence** (no condition can be older than 18 ticks, and every reachable band contradicts
   the variant's own second clause). Both cures are CORPUS acts and the words are yours.
2. ⛔ **`{timeband_age}` on the stressor leaf is DS-CND-1, not DS-STR-2.** GEN2's finding 4,
   `RULING-FOUNDER-SLOT`'s sibling row and my brief all read as if it were the world-stressor
   block. The producer is a CONDITION, which is why the eighteen-tick ceiling decides it.
3. ⛔ **FINDING 5 IS RIGHT AND FOUR TIMES BIGGER THAN GEN2 STATED.** GEN2 named
   `event type: religious`. Corpus-wide there are **four** fully-silent arms —
   `economic|anchored`, `religious|anchored`, `political|not anchored`,
   `demographic|not anchored` — and on 48 towns `economic` accounts for half the live
   darkness. GEN2's own 12-town corpus shows only `religious`, so the receipt was correct on
   its evidence and incomplete at scale.
4. ⭐ **A NEW DARKNESS CLASS, UNRELATED TO THE LADDER.** `historicalEvents[].anchored` is
   OPTIONAL; when the marker event lacks it, law 5 fails closed and the whole `event type:`
   pool renders nothing — **3 of 48 towns (6.3 %)**, one of them not even in the sixth band.
   Producer-side or display-side, both owner/chair-facing.
5. ⛔ **AN ADVERBIAL SIXTH BAND IS NOT DISPLAY-LOCAL.** It moves same-seed Herald output
   (2 of 18 `disclosureFor` lines, the covert `planted` pair), reds three pins in
   `heraldCausalVoice.test.js:330–332`, and **overturns a law both annexes state in prose**.
   The cheaper route — one duration-free variant on each of the four silent arms — costs
   4 sentences and moves no law. Your call; I wrote neither.
6. ⛔ **THE CONSIST TIP SHIPS A WRITER-REACH RED THAT IS NOBODY'S CAR.** At `a2af55cde`,
   before I touched anything, `node scripts/check-writer-reach.mjs` exits **1**:
   `forcedByConfig on stress` and `forcedByConfig on stressors` — *"the engine writes it on
   every world and no counting surface reads it. Ceiling 0."* Proved pre-existing by
   byte-identical output across my cars. The §900 landing will inherit it silently otherwise.
7. ⚠ **`STRESS_TYPE_MAP` IS AN UNUSED IMPORT** in `stressorsStateProse.js:51` — a landed
   eslint warning at the untouched tip. The desk keys on its own private `CRISIS_POOL_OF`
   and the totality identity is asserted in the test, where the import belongs. One-line
   deletion; not taken here because it is outside this lane's subject.
8. ⚠ **A STALE NOTE IS NOW HALF-TRUE.** `stressorsStateProse.js`'s traced-provenance block
   says the untraced pool *"speaks today"*. True of its VARIANTS, false of its ROUTING:
   0 of 48 generated towns reach it. My new block says so beside it rather than editing
   another lane's sentence.
9. ⭐ **`{timeband_age}` IS TOTAL AND `{timeband_since}` IS NOT** — 9 variants vs 24, and the
   predicate column never darkens. If the chair wants duration prose that survives an old
   world, the predicate is the column to author into. Free, and it is why GEN2's
   `timebandAgeFill` was the safe half of its own car.

---

# ⭐ RETROVALIDATION ROW (for the Fable chair)
| # | what was judged | what the chair must re-derive | receipts by path | priority |
|---|---|---|---|---|
| 1 | **THE FILL WAS REFUSED on both desks** against an explicit brief instruction | that `line4` reaches only risk+hold (so power is a no-op) and that all 46 archetypes carry a numeric expiry (so stressors would lie) | `powerStateProse.js` DS-POW-4 comment · `stressorsStateProse.js` new block · `$SC/tbwork/measure4.mjs`, `measure5.mjs` | ⭐⭐⭐ the whole lane |
| 2 | **The vocabulary gate PASSES** — 24 of 24 cells identical in both annexes | the parse-and-compare at the composed tip (the annexes are edited by other lanes) | `$SC/tbwork/measure6.mjs` · `heraldCausalGrammar.js` `TIME_BANDS` | ⭐⭐ |
| 3 | **Four silent arms, not one** (GEN2 said one) | the per-arm survivor count at the composed tip | `$SC/tbwork/measure3.mjs` / `measure3.out` | ⭐⭐ §900 |
| 4 | **89/108 is EXACT**, and `history.age` is sixth-band 12/12 | the same probe on the composed tip | `$SC/tbwork/measure1.mjs` | ⭐ |
| 5 | **The anchor-undefined darkness class is NEW** and is not the ladder's fault | that `significantEvent` can land on an `anchored === undefined` row, 3 of 48 | `$SC/tbwork/measure7.mjs` / `measure7.out` | ⭐⭐ owner-facing |
| 6 | **An adverbial sixth band moves the HERALD**, reds three pins and overturns a stated law | `heraldIntegrity.js:262` + the 2 of 18 disclosure lines + `heraldCausalVoice.test.js:330–332` | `$SC/tbwork/measure8.mjs` | ⭐⭐⭐ before any word is authored |
| 7 | **Register deltas are ZERO by construction** | the six frozen line numbers at the composed tip, after the merge relocates code | this receipt's register table | ⭐⭐ landing |
| 8 | **The writer-reach red is PRE-EXISTING at `a2af55cde`** | run it on the untouched tip before blaming a desk car | `$SC/tbwork/wr-before.log` = `wr-after.log` | ⭐⭐ landing |
| 9 | **Car 2's pin was verified by REPLICATION, not by vitest** | run the two suites when RESUME lands | `$SC/tbwork/replicate-aliveness.mjs` | ⭐⭐ owed |

**DOCK TIP: `e2a135483`** (`laneTIMEBAND`, detached, porcelain 0, 2 cars on `a2af55cde`).
