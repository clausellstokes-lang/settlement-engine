Desk composition (chair car): presentationFor keeps its parameter type — the lookup rewrite had dropped the docblock and re-opened the two strict errors

The significance-lookup car replaced presentationFor's body and lost its @param/@returns JSDoc, so the strict checker saw an implicit-any parameter indexing the partial record (TS7006 + TS7053, +2 on a baseline-0 file). The docblock returns; the lookup stays a lookup. Proof: typecheck:domain:strict exit 0 (1120/1120); the significance census and the writer's suite under the mutex; eslint 0.

Seat: Fable 5.1 — validated

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
