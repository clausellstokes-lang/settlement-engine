# FABLE VALIDATION — slice W20–W27 (the eight referent bars of ADDENDUM 13 part B)

**Seat.** Fable 5.1, sitting 2026-09-12 in retrospective review. Read-only on the repo. This file is the only thing written.
**Record read whole, in order:** `RETRO-VALIDATION.md` · `fidelity-W20-W27.md` · `items-W20-W27.md` · `evidence/convert-W20-W27.md` · `evidence/CONTRADICTION-TABLE.md` · `verbatim/BRIEF-pre-recut-ADDENDA-1-to-13.md` §A.1 line 166 and §B lines 172–180.
**Code resolved in the dock** `scratchpad/dock-f2da5a3ee` (git log -1 = `f2da5a3ee REWRITE car 8b-W-5-lighting …`). Every file:line below was read there, with its enclosing function.
**Protocol step 1 (re-run `measure-prose.py`) was NOT executed** — outside this seat's brief, which is the slice. Everything downstream of the warrant is validated on the slice's own grounds.

Labels: **CONFIRMED** = an executed check (a file read, a grep, a line count); **PLAUSIBLE** = reasoning only.

---

## 1. ROW-BY-ROW

Columns: (a) verbatim match · (b) ground — real field, line still reads so, engine behaves so · (c) permission safe? — worst sentence composed for every STRUCK item and struck half, hunted in the dock · VALIDATION.

| id | recorded | (a) verbatim | (b) ground in the dock | (c) permission / worst sentence | VALIDATION |
|---|---|---|---|---|---|
| **W20** | SPLIT | OK — mechanically checked by the fidelity packet; the `S-F1 …` ellipsis is the brief's own. CONFIRMED (re-read against §B line 173). | `priorityHelpers.js:45-60` CONFIRMED: `hasMilitaryInst` :45 · `hasGarrison` :46 · `hasMilitia` :47 · `hasWatch` :48 · `hasMercenary` :49 · `hasCharterHall` :51 · `hasWalls` :52 · `hasCourtSystem` :55 · `hasMarket` :56 (`hasGranary` is at :63, outside the cited span — trivial). `safetyProfile.js:281` `'Local enforcement'` · `:289` `'Locals watch over'` · `:300` `"There is no meaningful guard presence."` CONFIRMED, and the string is rendered beside the prose at `DefenseTab.jsx:378` (`sp.safetyDesc`). D-F2 "FALSE only at thorp/hamlet/village and on a ruined-watch town" CONFIRMED at `REFERENT-TABLE.as-ratified.md:522`. ⚠ `economicState.safetyProfile` is a generation-time snapshot never rebuilt (table §1.4) — the denial is frozen, the face may read live. | KEPT half safe and landed (F1-01 … F1-12, F1-20). STRUCK half safe: no field denies narrating a band through a body the roster holds. **The permission CELL is defective twice** (fidelity F3, unrepaired): (i) it refuses only six nouns while its own ground lists nine flags — worst sentence the cell licenses: *"The militia turns out for the badge"* on a no-militia town, denied by `inst.hasMilitia` false → `safetyProfile.js:300`; caught by the verdict's own kept half and by F1-03, not by the cell; (ii) *"the watch feels the badge before the town does"* is printed flatly lawful though NF-3's `:297` "The watch is stretched far beyond its capacity." can stand beside it (a soft collision — not a flat denial, so no reversal). | **OK** (verdict) · **CHALLENGE** (cell): restore the open residue "a body the roster denies / the nine flags" and qualify the example by band. |
| **W21a** | SPLIT | OK. CONFIRMED. | `rulingStructure.js:264-300` CONFIRMED — `'Town Mayor'` :284, `'Merchant Council'` :272, `"Headman's Authority"` :259, `'Elder Council'` :262/:265, `'Shadow Senate'` :293; `governingName` :787 / `government` :792 (cited :783-790 — spans the comment + `governingName`). `priorityHelpers.js:55` `hasCourtSystem` matches `'town hall'`/`'city hall'` CONFIRMED. | Both halves safe; landed as F1-20 (a hall as a place below town) + F1-53 (the seat's kind from `governingName`). **The permission note is FALSE:** "the `{seat}` slot is NAMED BUT NEVER FILLED (census W-2)". In the dock `defenseStateProse.js:961` fills it — `seat: properFill(text(power.government))` — and the ratified REFERENT-TABLE (OV-19, verbatim line 427) already records the census reporting it never filled "**while both are filled**" (the OW-4 census bug). The archive re-asserted a census bug as a fact about the record. CONFIRMED. | **OK** (verdict) · **CHALLENGE** (cell): delete the W-2 sentence or restate it as the census defect it is. |
| **W21b** | SPLIT | OK. CONFIRMED. | `cohesionWeave.js:133` `RULING_POWERS` CONFIRMED. **But the ground's causal claim is wrong:** "the seat label it was derived from" — `structuralLensOf` (`cohesionWeave.js:279-285`) takes the **MAX-POWER faction's category**, never `isGoverning`; under `under_siege` the `Military/Guard` row is doubled. The lens can therefore say `autocrat` on a town whose seat is `'Merchant Council'`. The table's own F1-53 corrects this ("take the seat's kind from the seat's own label"). CONFIRMED. | STRUCK half safe. **KEPT half, as grounded, manufactures FALSE FINDINGS:** a refuter failing "the council that meets" on a town whose LENS says autocrat while the Power tab prints `'Merchant Council'` fails a true sentence. The kept half is right only when re-grounded on `governingName` (as F1-53 does). | **OK** (verdict SPLIT) · **CHALLENGE** (ground): the kept half's field is the seat label, not the lens; say so in the row, or the row and F1-53 contradict each other. |
| **W21c** | SPLIT | OK. CONFIRMED. | `'Shadow Senate'` :242 · `'Corrupt City Council'` :244 · `'Corrupt Council'` :246 (cited :245-256 — off by three lines, substance holds); `:264-283` CONFIRMED (`'Corrupt Council'` at :273 and :281). | Both halves safe; landed F1-117 (NF-1). | **OK** |
| **W22a** | STRUCK | OK. CONFIRMED. | **The ground's first sentence is the archivist's invention and is false on two counts** (fidelity F1, unrepaired): (1) the ratified law bars PERSON "BY PRODUCT SCOPE, not by measurement" — a scope bar, not a permission regime; (2) the engine DOES carry a typed NPC→institution edge: `npcHomeInstitution` (`corruption.js:646-650`) reads `factionAffiliation ‖ factionLink ‖ institutionId`, exactly as the elided parenthetical of §A.1:166 says. CONFIRMED. The ADDENDUM 14 quotation ("an UNNAMED person may appear, act, keep a key, be avoided, be resented. A reeve who is slow to open the books …") — PLAUSIBLE: the post-recut brief is not in the working tree; the words recur verbatim at `CONTRADICTION-TABLE.md:43` and `evidence/exemplars-martin-hobb.md:48`. | Verdict safe on its true ground (the owner's relaxation of W22 by name). Worst sentences: *"the clerks record every cart at the toll-bar"* / *"the officers posted at the gate"* on a gateless town → F1-08 catches; *"the collectors take the long way round"* → nothing denies. **But the cell's own first example skirts the table's F3-06 rider:** *"Somebody keeps the key to the gate-house"* — at village tier and up the engine emits exactly ONE `Guard Captain` (`npcGenerator.js:1511-1519`) with a personality, disposition and secret, and F3-06's refused example is "The one who keeps the gate key is slow to answer at night". The archive's row carries no F3-06 cross-reference. CONFIRMED (table :278). The record's own challenge 4 (the person-PLURAL cases) resolves to the label trap, which the table carries — no reversal. | **OK** (verdict) · **CHALLENGE** (ground + cell): delete the invented sentence and re-ground on scope + floor 3; add the F3-06 rider to the cell; change the example. |
| **W22b** | STRUCK | OK. CONFIRMED. | `institutionTable.js:370-376` — the comment "THE COLUMN IS OPEN AND STAYS OPEN. The structural NPC roster is a SAMPLE … so a town has offices the roster does not name" CONFIRMED verbatim (line-counted: :366 opens the doc block, :370 is the ⚠ line, :377 `export function officesOf`). | Worst: *"the garrison commander"* on a no-garrison town → F1-02; *"the harbour master"* on a landlocked town → F1-18. Safe: the residue is the label trap, as the row says. | **OK** |
| **W22c** | SCOPE | **STILL ELIDED.** Row 21 still reads "BY PRODUCT SCOPE, not by measurement **…** A recorded NAME may be printed" — the parenthetical "(X-D3, OW-12: the engine DOES carry a typed NPC-to-institution edge; the institution table chooses not to read it; ADDENDUM 11's 'no typed edge exists' is corrected to 'the institution table reads no such edge')" is still cut. CONFIRMED by diff against §A.1 line 166. And it is the fact W22a's invented ground contradicts. | `ladderRead.js:95-107` `ladderRungsOf` returns recorded `{npcId, name, standing}` CONFIRMED. **`generalStateProse.js:1502` is `crossSettlementNpcPoolKey`** (keyed on the tie's recorded `npcConnections[]`) — the substance (recorded names) holds, but the quoted words "never minted, no fate resolved" do NOT occur at that line or anywhere in that file; "no fate resolved" is at `settlementLifecycleFirstClass.js:604` (a dispersal pin, unrelated). Misattributed quotation. CONFIRMED by grep. | Nothing released; landed F3-01 (which repeats the :1502 cite without the false quote). | **OK** (verdict) · **CHALLENGE** (column 2 + ground): restore the parenthetical verbatim; drop the misattributed quote. |
| **W22d** | STRUCK | OK. CONFIRMED. | `npcGenerator.js:1511-1532` CONFIRMED — `TIER_MANDATORY_ROLES` :1511, `STRESS_MANDATORY_ROLES` :1521, `occupied: ['Corrupt Official']` :1524, `insurgency: […, 'Corrupt Official']` :1532. | **Worst sentence on the PLAYER face:** *"Everyone here knows one of the officials is bought."* Hunted: (1) `publicSafe.js:204-212` `publicNpc` KEEPS `role` (:208) — the `'Corrupt Official'` string is on the player projection itself; (2) the mandated NPC is affiliated to the crime faction (`npcGenerator.js:1069`), is not `npc.corrupt` (set only by `applyWorldPulseBetrayal.js:49`), so it is NOT on the unexposed-stooge route of `corruption.js:683-689`; (3) its `secret` (the `occupied` pool, `stressInstitutionEffects.js:97-120`) is stripped from the projection and none of those five secrets denies that the corruption is known. **No field denies the sentence on either face; fidelity F4's audience objection conflates the mandated public role with rule 4's covert route.** CONFIRMED. The table agrees (F4-14: "a corruption the roster PUBLISHES called hidden"). Residual defect: the archive's "**on any town** where the roster prints one" — on a town WITHOUT the role, whose only corruption is a covert impairment, "the town knows an official is bought" on a player face collides with F4-13; the packet's "where the roster prints one" was the safe wording. | **OK** (verdict) · **CHALLENGE** (cell): revert to the packet's condition. |
| **W23** | STRUCK | OK. CONFIRMED. | `institutionStatusModel.js:118-120` `INSTITUTION_IMPAIRMENT_CAUSES = ['supply_shortage','corruption_exposed','damage','siege_occupation']` CONFIRMED. | Worst sentences: *"the hall moves against the operators and the operators move back"* — an episode with a before and after; floor 2 (elapsed course) already bars it without a row. *"the resistance is bleeding the garrison"* on an occupied town — "the garrison" resolves to the town's own row (F1-02) or the occupier's (F1-121). The record's challenge 1 (the cause set is a closed positive model): the causes are typed on an impairment RECORD, so a face asserting cause X where the page prints cause Y is a per-town floor-1 finding — **no table row carries "a cause the impairment record prints differently"** (grep: none). Where the record prints no impairment, silence is permission and the strike is right. PLAUSIBLE on the page-printing of `causeRef` (not traced to a component). | **OK** (verdict) · **CHALLENGE**: add the residue row (a relation naming a CAUSE the impairment record denies; floor 1) and note floor 2 catches the episode shape. |
| **W24a** | STRUCK | OK. CONFIRMED. | No field, correctly: the REFERENT-TABLE's own OV-33 records `HOLDER_KIND_NOUN_ROWS` "licenses nothing on its own". CONFIRMED (verbatim :441). | Worst: *"[ledger] the writ went out from the hall"* on a hamlet → F1-20 (no hall below town); *"the court's minutes"* on a hall-only town → the WORD court is free (R-4) and a record cited to a court kind with no holder → F1-24. The keeper-entailing words the record's challenge 2 names ARE caught — but only if the refuter reads them as CITATIONS under F1-24, which the cell does not say. The archive's rider "on any organ read whose keeper the town holds" is right (fidelity F7) and is now under `[archivist's reading]`. | **OK** (verdict) · **CHALLENGE** (cell): cross-reference F1-24 explicitly: a keeper-entailing record word is a citation and takes F1-24's holder test. |
| **W24b** | SPLIT | OK. CONFIRMED. | `composedWalker.js:1071-1073` "a holder with no institution … the holder kind resolves to no institution in this town's roster, so the cited record has nobody keeping it" CONFIRMED verbatim (line-counted). `priorityHelpers.js:46-47` = `hasGarrison`/`hasMilitia` CONFIRMED. | Both halves safe; kept half landed F1-24. | **OK** |
| **W24c** | STRUCK | OK. CONFIRMED. | `composedWalker.js:1051-1052` "the unit names the holder of a record, which is a PROVENANCE move and spends a budget the sitting has not yet set" CONFIRMED verbatim. | Worst: *"[ledger] The books show forty on the roll"* → F2-01 (a number); *"the books show a purse that does not stretch"* where the treasury holder is SOURCE-UNRESOLVED → F1-24. Safe given F1-24 — which is the record's own challenge 5: the residue attaches to W24b/F1-24 and the W24c row does not say so. | **OK** (verdict) · **CHALLENGE** (cell): state that a `[ledger]` citation is exactly the move F1-24 tests. |
| **W25a** | CONTRADICTION | OK. CONFIRMED. | `occupationStatus.js:74-96` — the record is `{occupierId, state, resistance}`, no garrison field CONFIRMED; `priorityHelpers.js:46` CONFIRMED. | Nothing released; landed F1-121. | **OK** |
| **W25b** | CONTRADICTION | OK. CONFIRMED. | `warFaithStateProse.js:930-933` `counterpartName(war) = occupierName ‖ war.counterpart` CONFIRMED; `:690-694` (the `counterpart` property doc: "the ONE opposing town … a coalition has no counterpart") CONFIRMED; `treatyDocument.js:359-361` `loserName`/`victorName` CONFIRMED. | Nothing released; landed F1-120. | **OK** |
| **W25c** | STRUCK | OK. CONFIRMED. | `stressFactions.js:105-107` — `faction: 'Unknown Faction (hidden)', power: 15, desc: '… Its presence is not known to the settlement.'` CONFIRMED (the engine's own positive covert model, in words). `rulingPowerCoup.js:51` `MIN_CONTENDER_POWER = 5` and `:104` the contender filter (criminal excluded, `power >= 5`) CONFIRMED — the hidden faction infers to `OTHER` through `factionArchetypes.js` NAME_RULES, so it PASSES with weight 13.5. `powerStateProse.js:178-186` `properFill` refuses dashes, sentence breaks, digits and snake_case — the string has none and begins with a capital, so it RENDERS; `:994` fills `{counterpart}` from `challengers[0].name`. `power.generated.js:1317-1762` (DS-POW-4) names `{counterpart}` in `riskLabel: Contested` (:1397) and `riskLabel: Critical` (:1436) and carries **ZERO `dm-only` marks** (nearest marks :394 and :1922). `PowerTab.jsx:198-199` computes contenders on `playerView`; only `publicDossier` nulls them. ALL CONFIRMED. | **THE FALSE SENTENCE:** *"Unknown Faction (hidden) now outweighs the Town Council at ⟨town⟩, and what keeps Town Council in the hall is the office rather than the strength behind it."* — on the PLAYER face of every `infiltrated` town where the hidden faction is the top challenger. Floor 4 (F4-13, the audience model; the faction's own `desc`). The archive's `[corrected]` cell already says the deferral stands; **the verdict still reads STRUCK, the table lists "W25c struck" (:332), and W-15 (:239) is a bare WIRING disposition** — so a refuter reading the instrument passes the sentence. Candour: `PowerTab.jsx:463` prints every `f.faction` with no `playerView` filter and `publicSafe.js:148` drops only objects flagged `covert:true`, which this stress faction is not — the page ALREADY leaks the name in the faction list on the player view; the prose compounds rather than originates the breach. That is a reason to keep the writer's deferral, not to drop it. | **REVERSE → SPLIT.** Wiring half: W-15 (register car). Deferral half: **KEPT, floor 4 / F4-13** — `{counterpart}` on DS-POW-4 stays DM-only until the register car filters `(hidden)`. Bring the verdict cell and the table's :332 / W-15 rows into line with the archive's own corrected cell. |
| **W26a** | MODEL | OK. CONFIRMED. | `corruption.js:667-691` (`compromisedSecurityInstitutions`; `covert !== true` → revealed at :679; the unexposed-stooge loop :683-689) CONFIRMED by line count. `settlementPolitics.js:423-436` `covert: true` on the compromise glue CONFIRMED. `mobilization.js:128` (`@property covert`) and `:147` (`covert: raw?.covert === true`) CONFIRMED. `brokeragePatronage.js:296` (`covert: house.legality === 'illegal'`) and `:326` (`patronName: 'unknown'`) CONFIRMED. `PowerTab.jsx:194-215` CONFIRMED. | Nothing released; landed F4-13. | **OK** |
| **W26b** | STRUCK | OK. CONFIRMED. | `composedWalker.js:1077-1079` "the holder of this record is a power with an interest in it, so the citing face is the DM's" CONFIRMED verbatim. "NO product caller resolves the arm at draw time" CONFIRMED: `armA13` is imported only by `tests/lint/proseComposed.walker.test.js`; `REFERENT-TABLE.as-ratified.md:496` says the same. | Worst: player face on a captured town — *"[ledger] the court's roll shows every case against the guild dismissed"* — names the capture → F4-13 (the capture at `corrupted`/`capture` is DM). A neutral citation (*"the court's roll runs long this season"*) — nothing denies. Safe: the audience model polices CONTENT, and the record's challenge 3 ("publicly credible") is a licence argument with no field. | **OK** |
| **W27a** | SPLIT | OK. CONFIRMED. | `priorityHelpers.js:45-60` CONFIRMED. | Both halves safe; kept half landed F1-22. | **OK** |
| **W27b** | SPLIT | OK on the quote. **The ground misreports the strike:** it lists "**see** / act / decide / be told / intervene / be resented" as STRUCK, but the old law says the stranger "**may see**" — seeing was the one affirmative licence and was never barred (fidelity F6, unrepaired). CONFIRMED (row 33 vs §B line 180). | Kept half: floor 3 + NF-4, landed F1-126. | Struck half safe; kept half right. | **OK** (verdict) · **CHALLENGE** (ground): strike "see" from the struck list. |
| **W27c** | STRUCK | OK. CONFIRMED. | Inherits W24a/W24c; no independent field. | Safe; W24b's residue (F1-24) binds. | **OK** |

### APPENDIX rows

| id | recorded | (a)/(b) | (c) | VALIDATION |
|---|---|---|---|---|
| **NF-1** | CONTRADICTION | Cites CONFIRMED (see W21c). | Landed F1-117. | **OK** |
| **NF-2** | CONTRADICTION | `npcGenerator.js:1511-1532` CONFIRMED; `corruption.js:654-658` (the revealed/covert comment) and `:679` CONFIRMED by line count. | Landed F1-111. Note for the refuter: the role is on the PLAYER projection too (`publicSafe.js:208`), so "clean hands" is denied on both faces. | **OK** |
| **NF-3** | CONTRADICTION | `safetyProfile.js:296-300` — "The garrison is overwhelmed or corrupt." :296 · "The watch is stretched far beyond its capacity." :297 · "The militia cannot maintain consistent coverage." :298 · "There is no meaningful guard presence." :300; the safe bands `:281-289`. CONFIRMED verbatim; rendered `DefenseTab.jsx:378`. | **NOT LANDED in `CONTRADICTION-TABLE.md`**: no row cites `:296-300`; the only condition string in the table is `:276` under F4-05 (grep for "overwhelmed", "stretched", "296-300": none). The archive appendix carries it; the instrument the refuters read does not. ⚠ any row added must carry §1.4's caveat — the panel is a generation-time SNAPSHOT never rebuilt, so a live-read face may lawfully diverge after a world run. | **CHALLENGE** (instrument gap): row it, with the frozen/live caveat. |
| **NF-4** | SCOPE | CONFIRMED (composition fact; `npcGenerator.js` roster + `{npc}` slots). | Landed F1-126. | **OK** |
| **NF-5** | CONTRADICTION | `safetyProfile.js:276` "The constant monster threat keeps the guard exceptionally well-drilled and alert." CONFIRMED. | Landed inside F4-05's ⚠ note. | **OK** |
| **NF-6** | WIRING (protective) | `occupationStatus.js:83-96` (`occupierName = nameFor(rec.occupierId)`, a town) CONFIRMED; `warStatus.js:318-335` (`governingName ‖ government ‖ 'occupation authority'`) CONFIRMED, the literal at :335; `pulseKernel.js:1150` (the occupations ledger is world-run) CONFIRMED. | **Landed INVERTED:** F1-123 (:214) rows it as a FINDING binding WF — "naming the OCCUPIER as one thing … Say which" — whereas the packet and the archive row it PROTECTIVELY ("recorded here so the refuters do not charge the writer for it"). A writer who follows one surface can now be charged for disagreeing with the other. | **CHALLENGE**: F1-123 should be a §1.6 wiring row with W-01's disposition ("no face may be charged on this town"), or F1-123 must say a face agreeing with EITHER surface passes. |

---

## 2. REVERSALS

| id | recorded | should be | field:line | the false sentence the permission would license |
|---|---|---|---|---|
| **W25c** | STRUCK | **SPLIT** — wiring half W-15 (register car); deferral half **KEPT, floor 4 (F4-13)**: `{counterpart}` on DS-POW-4 is DM-only until the register car filters `(hidden)` | `stressFactions.js:105-107` (`'Unknown Faction (hidden)'`, desc "Its presence is not known to the settlement") · `rulingPowerCoup.js:51`, `:104` (passes the contender filter as `OTHER`) · `powerStateProse.js:178-186` (`properFill` does not reject it), `:994` (fills `{counterpart}` from `challengers[0].name`) · `power.generated.js:1397`, `:1436` (DS-POW-4 Contested/Critical name the slot) with 0 `dm-only` marks in :1317-1762 · `PowerTab.jsx:198-199` (contenders computed on `playerView`) | *"Unknown Faction (hidden) now outweighs the Town Council at ⟨town⟩, and what keeps Town Council in the hall is the office rather than the strength behind it."* — on the player face of an `infiltrated` town. CONFIRMED end to end. The archive's `[corrected]` cell already agrees; the verdict cell, `CONTRADICTION-TABLE.md:332` ("W25c struck") and W-15's bare WIRING do not. |

No other item reverses. Every other STRUCK item and struck half survived the worst-sentence hunt: the false sentences each composed are caught by a surviving row (F1-02, F1-08, F1-18, F1-20, F1-24, F2-01, F3-06, F4-13) or by a floor, and the verdicts are right on the record's own test.

---

## 3. CHALLENGES (strongest first)

1. **W25c** — see REVERSALS; the verdict, :332 and W-15 must match the corrected cell, or the instrument passes a literal placeholder on a paid player surface.
2. **W21b (ground)** — the kept half is grounded on the LENS, which `cohesionWeave.js:279-285` derives from the max-power faction, not the seat; as written it fails true sentences on any town where lens and seat disagree (`under_siege` doubles Military/Guard). Re-ground on `governingName` as F1-53 does.
3. **NF-3 (instrument)** — the slice's "single richest untapped finding class" is rowed in the archive and absent from `CONTRADICTION-TABLE.md`; the refuters cannot cite `safetyProfile.js:296-300`. Row it with the §1.4 frozen/live caveat.
4. **W22c / W22a (F1 unrepaired)** — the parenthetical proving the engine carries a typed NPC→institution edge is still elided, and W22a's ground still asserts the opposite; the dock confirms the edge (`corruption.js:646-650`). The verdict survives on floor 3 alone; the record's stated reason does not.
5. **NF-6 (inverted)** — a protective wiring note became F1-123, a chargeable finding. Restore W-01's disposition.
6. **W20 (cell)** — the refused set is narrowed to six nouns against nine flags in its own ground; the worked example is unqualified by band though NF-3 (appendix) says it must be.
7. **W22a (cell)** — the first example, "Somebody keeps the key to the gate-house", is a near-instance of F3-06's refused sentence; the row carries no F3-06 rider.
8. **W21a (cell)** — "the `{seat}` slot is NAMED BUT NEVER FILLED" is a census bug restated as fact; `defenseStateProse.js:961` fills it and OV-19 says "while both are filled".
9. **W22d (cell)** — revert "on any town where" to the packet's "where the roster prints one"; on a town without the role the sentence can collide with F4-13. (The audience rider fidelity F4 asked for is NOT needed: `publicSafe.js:208` keeps `role` on the player projection and the mandated NPC is not on the covert route.)
10. **W22c (ground)** — "never minted, no fate resolved" is not at `generalStateProse.js:1502` nor in that file; F3-01 repeats the cite. Drop the quote.
11. **W23 (residue)** — no row carries "a relation naming a CAUSE the impairment record prints differently" (floor 1); the fused episode shape is already a floor-2 matter and the row should say so.
12. **W24a / W24c (cross-reference)** — keeper-entailing record words and `[ledger]` citations are caught only through F1-24; neither cell says so, and a refuter reading the rows in order sees W24c released and W24b kept with no bridge.
13. **W27b (ground)** — "see" listed as struck; it was never barred (F6 unrepaired).
14. **W20 / W24a `[archivist's reading]` prefix** — marks the cell as the archivist's without naming the two specific departures from the packet ("or a condition"; "on any organ read whose keeper the town holds"), so a reviewer still cannot tell which words are the fold's.
15. **Header** — "§B lines 172–180" should read 173–180 (trivial; F8).

---

## 4. FIDELITY-REPAIR CHECK

| finding | repaired? | evidence in `items-W20-W27.md` |
|---|---|---|
| F1 (W22c elision + W22a invented ground + framing paragraph) | **NO** | Row 21 still "not by measurement … A recorded NAME"; row 19 still "the institution table reads no NPC edge, so the card could not prove a person"; line 9 still "because the institution table reads no NPC edge … the whole family is a permission regime". |
| F2 (NF-3/5/6 dropped; NF-1/2/4 undefined) | **YES** | Lines 54–67: all six rowed in the packet's words with grounds and a MAY-NO-LONGER cell. |
| F3 (W20 cell: six-noun list; unqualified example) | **PARTIAL** | (a) NO — row 15 still "Refused: seating a watch, garrison, wall, granary, hall or market". (b) PARTIAL — the cell is untouched, but appendix NF-3 (line 62) and line 67 state that it "QUALIFIES W20's permission cell above". |
| F4 (W22d "on any town"; W22d missing from the closing) | **NO** | Row 22 still "on any town where the roster prints one", now under `[archivist's reading]`; the closing still names five. (On the code the audience rider is unnecessary — see W22d — but the broadening stands.) |
| F5 (W25c releases the player face; missing from the closing) | **YES at the cell / NO at the verdict** | Row 29 is `[corrected]`: "`{counterpart}` on DS-POW-4 stands as the old law left it, DM-only". The verdict cell still reads STRUCK; W25c is not a sixth challenge; the table still lists it struck. |
| F6 (W27b "see" inflated into the strike) | **NO** | Row 33 still "STRUCK — see / act / decide / be told / intervene / be resented". |
| F7 (undeclared re-conditioning of W24a, W20) | **PARTIAL** | Rows 24 and 15 now carry `[archivist's reading]`, which flags the cell but does not name the departures. |
| F8 (header line cite; empty VALIDATION column; four full-teeth items unenumerated) | **NO** | Line 5 still "lines 172–180"; VALIDATION "—" on all 20 (expected — reserved for this seat); W25a/W25b/W26a/W22c never listed as the four that keep full teeth. |

---

## 5. SLICE VERDICT

The chair's verdicts hold on nineteen of twenty items and on all six appendix rows; every citation I resolved in the dock names a real field at a line that still reads as recorded, with two harmless off-by-a-few spans and one misattributed quotation. The strikes were the right test of this slice — every worst sentence I composed for a struck rule is denied by a surviving row or a floor, and the seven splits each kept the half with a field behind it. The one reversal is W25c, where the archive's own `[corrected]` permission already knows the deferral must stand but the verdict and the refuters' instrument still say struck, and the dock proves the harm end to end: a player-face variant of DS-POW-4 renders the literal `Unknown Faction (hidden)` on every infiltrated town, with no `dm-only` mark on the block. The record's weakness is not its verdicts but its grounds and cells: the fidelity adversary's most serious finding (F1) is unrepaired and the archive still argues from a claim the ratified law and the code both contradict; W21b's kept half is grounded on the wrong field and would fail true sentences; and two of the slice's own new findings did not reach the instrument in the form the packet gave them (NF-3 absent, NF-6 inverted). Nothing here reopens the re-cut. Everything here is a repair to the record and to the table, executable without re-running the programme.

## 6. THE ONE THING

Fix W25c in three places at once — the verdict cell, `CONTRADICTION-TABLE.md:332`, and the W-15 disposition — so that `{counterpart}` on DS-POW-4 is DM-only until the register car filters `(hidden)`; the archive's corrected cell is right and everything the refuters actually read is wrong. Then re-refute every block landed since 09-12 against that one row, which the record's own §4 says is cheap.
