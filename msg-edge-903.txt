§903 lit-default landing (edge re-mint car): the edge-shared bundles re-mint because the preset table is one of their inputs

L-DEFAULT hunk 1 lit twenty-one virtual keys in simulationRules.js, which aiCharterBundle and aiOutputSchemaBundle
transitively import, so both freshness arms and the reproducibility arm went red at the §903 proof (whole-903.log:
sourceHash 855d65d7e73987a6 -> 150a3a3850539e85 expected by the builder over index content). The ritual: immer +
seedrandom materialised for the build only and restored to symlinks after (453 links before and after; a build under
the symlinks drops those packages' input rows and mints a different hash — measured first, reverted). The three edge
tests are the proof at the registers and again at the gate.

Seat: Fable 5.1 — validated

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
