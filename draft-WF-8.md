# WF / WF-8A — the settlement extinction obituary: the beat, its producer, the lawful resurrection of the refused Chronicle row, and the §321.2a minter-totality arm (stage 1 of the WF-8 train)

- **Status:** DRAFT
- Compiled by lane TC-WF8 under ODQ §291.5 / §347.1(2); chair-tier compile per the §343.2
  compile precedent; the chair reviews before TE-WF8 (queue slot 5) dispatches.
- **Verified base:** `claude/composite-r4` at `27c250f94bb7c5c799693a62985c5ddecd4b6c7c`
- **Preamble:** `docs/implementation/preambles/WF-PREAMBLE.md`, cited **BY SHA-256
  `f4cd39fee756c3a192710ab9aa3fc208e18ef1f08ea9e9073bcdc8bfb55d871c`** (780 lines, computed live
  at the compile base — NOT the `ca02c8a1…` of the WF-1D era, which is superseded; the preamble
  was amended by WF-1D's landing to carry the FOUR-member zero-headroom class). ⛔ The executor
  RECOMPUTES this hash at its own base and STOPS on divergence — the file has been re-stamped
  repeatedly this week and a quoted hash is never reused.
- **Substrate annex:** `docs/implementation/preverification/WF-SUBSTRATE.md`. ⛔ No absolute
  inherited; every figure below was re-executed at `27c250f9` and every figure is a **STOP:
  RE-DERIVE AT BASE** for the executor — WF-1F is in flight and will move the base first.
- **Design authority:** `docs/DESIGN_FP_ARCH_WF.md` at the LEDGER blob `169c75e7` (the §78-cured
  copy governs; the build copy is pre-cure — §P1 R-WF-1) · `docs/DESIGN_FP_FAITH.md`, THE
  EXTINCTION BEAT [CORRECTED 2026-08-02], obituary (a) — the settlement-scoped half ·
  `docs/DESIGN_FP_SPINE.md` §2 SP-6 and reqs 13/14 · GR-PREAMBLE §P2 (the registration-cost
  table; ⛔ AN OVERRIDE NEVER TRANSFERS) · `docs/implementation/packets/foreign-policy/IN-1C-A.md`
  (the one-row registry precedent AND the §85.4 two-obligation lesson this packet prices up
  front) · **compile seed: `draft-WF-1B-II.md`, PRESERVED by ODQ §309.3** — consumed, re-executed
  at this base, and corrected where the tree has moved (§1.4 below).
- **Authorizing decisions, named in-body per §299.4:** ODQ **§309.3** (the beat re-filed to WF-8;
  the seed preserved), **§321.2(a)** (the refused row's absence pin A6; the ordered
  minter-totality arm; the census-instrument-touch trigger this member fires), **§347.1(2)** (the
  dispatch). The `censusAuthorization` block in §9 cites these three.
- **Depends on:** WF-1B **LANDED** (the pruned-refs seam — `advanceShares` returns what
  `pruneSuppressed` deleted; measured live at this base, return DISCARDED at the seam site,
  which is the seam exactly as promised). WF-1A **LANDED** (`faithUnseatingEnabled`, the hoisted
  by-name read). No dependence on WF-1C/1D/1E beyond their landed state; **zero shared paths
  with WF-1F** (in flight: `realmEvents.js`, `subsystemRowsVirtual.js`, `pantheon.test.js`,
  `couplingInclusion.walker.test.js` — none named below).
- **Train:** `wf-8`, family **WF** (STAMPED §78.1 ⇒ engine cap EIGHT). This is the train's FIRST
  member; WF-8 (NARRATION) remains a multi-member train by arithmetic (preamble §P1 R-WF-8) and
  this member **does not touch `faithNarrationEnabled`** — the beat rides WF-1A's landed
  `faithUnseatingEnabled` exactly as the seed chartered.
- **Constitutional law:** the DEITY DOCTRINE — faith is CULTURAL, never theological; typed
  buckets under FINITE-SEMANTICS; AI is clerk, never writer. ⛔ The obituary records that **the
  people stopped keeping the rite**. It never says a god died, left, or failed.

---

## §0 · WHAT THIS MEMBER IS, IN FOUR ACTS

1. **THE PRODUCER.** The settlement extinction beat `faith_last_altar_dark` is minted at the one
   deletion path — the fold collects the refs `advanceShares` returns from `pruneSuppressed` and,
   behind the already-hoisted `unseating` local, mints ONE outcome per deleted creed. The bar is
   WF-1B's landed derivation (`supp.length > KEEP`); **a literal 4 appears nowhere** (§308.3
   RAISED-4, satisfied by construction — this member does not own the bar).
2. **THE LAWFUL RESURRECTION.** `chroniclersLetter.js`'s `KIND_SECTION` gains the row WF-1D
   refused dead — now with a LIVE MINTER in the same commit, which is what §321.2(a) ruled the
   row always needed. The row's key is `faith_last_altar_dark`; the WF-1D-refused keys
   (`war_termination_read`, `patron_fall*`) stay absent and A6's assertions DO NOT MOVE (§5).
3. **THE §321.2a MINTER-TOTALITY ARM** joins the walker estate at exactly the trigger the ruling
   named — this member touches the census instrument — with the "written ruling" escape the
   ruling ordered and the hygiene arms that keep the escape honest (§4).
4. **THE DECLARED A6 FLIP.** WF-1D's A6 absence pin is re-recorded — comment-only, assertions
   proven unmoved — citing §309/§321.2a in the test body and in this packet (§5). Never silent.

**Nothing else changes.** No flag is minted; dark is byte-identical twice over (deity-presence
gate at the fold's head, then the strict by-name flag read). No zero-headroom file is touched —
measured, not asserted (§2.1).

---

## §1 · COMPILE-EXECUTED SUBSTRATE (all CONFIRMED at `27c250f9`; every row a STOP: RE-DERIVE)

### 1.1 The four zero-headroom files — untouched, and re-measured anyway (§P1 R-WF-7)

eslint's own `Linter`, `max-lines {skipBlankLines, skipComments}` (`laneTCWF8-measure-base.log`):

| file | eff | frozen literal | this member |
|---|---:|---:|---|
| `src/domain/worldPulse/warTermination.js` | **818** | 818 EXACT | NOT TOUCHED |
| `src/domain/worldPulse/pulseKernel.js` | **1581** | 1581 EXACT | NOT TOUCHED |
| `src/domain/worldPulse/applyWorldPulse.js` | **941** | 941 EXACT | NOT TOUCHED |
| `src/domain/worldPulse/peaceTerms.js` | **797** | 800 layer ceiling, 3 under | NOT TOUCHED |

### 1.2 ⭐⭐ THE KERNEL-WIRING ACCOUNTING: ZERO — the transport is already built, end to end

The dispatch asked this compile to design any kernel wiring inside the net-zero law (the WF-1E
sixth-argument precedent). **Measured: none is needed.** The chain, every link verified live:

1. The fold `advanceReligionStates` (`religiousContest.js:538`) has in scope at the seam
   (`advanceShares(state, strengthByRef, { narrativePrune: unseating })`, `:795`): the `outcomes`
   array (`:576`, pushed at `:910`/`:942` in the same loop), `unseating` (`:686`), `tick`,
   `nameFor(cid)` (`:557`), `state.deities[ref].snapshot?.name`, and `stablePart` (the
   conversion-id idiom).
2. The kernel already forwards `religion.outcomes` → `religiousOutcomes` → `warOutcomes`
   (`pulseKernel.js:1237`, `:1249`) → admission → `outcomesToApply`.
3. `applyWorldPulse.js:1071` converts EVERY applied outcome generically:
   `newsEntryForOutcome(outcome, tick, 'applied')` (`worldPulseFeedCuration.js:74`) stamps
   **`impactKind: outcome.candidateType || outcome.type`** — the promoted-candidateType path its
   own walker documents.

⇒ **The mint is a fold-local `outcomes.push(...)` of an object built by the new leaf. Zero
edits to `pulseKernel.js`, `applyWorldPulse.js`, `warTermination.js`, `peaceTerms.js`.** The
WF-1E packing precedent is NOT invoked. (J-TCWF8-1, §8.)

### 1.3 The registration/census ground (each figure re-read from the live instrument)

| register | measured at `27c250f9` | after this member |
|---|---|---|
| lighting census tuple (`sovereigntyLightingContract.walker.test.js:5062`) | `2492/364/2128/20677/5780` | re-derived WHOLE by the placeholder method; predicted shape `+1/+0/+1/+T/+0` (§6) |
| `kindPoolFloors.walker.test.js` | `REGISTRIES` 10 · `REGISTERED_KIND_COUNT` **112** (`:184`) · `ROUTED_TOKENS` **378** (`:163`) · `LEGACY_UNVOICED_TOKENS` **274** (`:153`) · divergence `.toBe(8)` (`:350`) · small-family exact list `['INFORMATION']` (`:206`) | 11 · **113** · **379** · **274 unmoved** · **8 holds** (identity: routedAndRegistered 104→105) · ⛔ **list gains FAITH** (§3.2) |
| `PACKET_MANIFEST.json` | **137** packets, ZERO `WF-8*` rows, path free | 138 at DRAFT |
| `.prose-numerics-baseline.json` | 413 rows; **ZERO key ANY file this member touches** (executed over religiousContest/religionState/heraldRouting/chroniclersLetter/settlementRumors) | ⭐ the seed's six-row re-address obligation is **GONE at this base** — staleness finding S-3, §7. NOT INCURRED; re-derive |
| §104.4 edge bundles (all five metas under `supabase/functions/_shared/`) | 110/66/111/2/2 inputs, **ZERO hits** on every touched path | NOT INCURRED — re-derive at base |
| `chooserTotality.walker.test.js` | `CLASSIFIED toEqual DISCOVERED` (`:241`), `DEFER_CEILING` 30 (deferral only, shrink-law `:279`) | the new keyed pick is DISCOVERED ⇒ its `habitForkRegistry.js` row lands in the SAME commit; **no walker edit** — the exact-set holds both directions; DEFER untouched (the row lands classified) |
| `habitForkRegistry.js` | 245 eff, 35 `HBF-` rows | +1 row (§85.4a) |
| voice classifier (`newsVoice.js#newsVoiceCategory`) | EXACT-token matching; `faith_last_altar_dark` matches no classifier ⇒ null via the set-but-unclassified guard | the candidateType walker's voice arm holds with NO edit; `VOICED_CANDIDATE_TYPES` stays empty |
| `patronFall.test.js` | ONE `describe` (`:147`) | the 8 acceptance cases join it as straight-line `it`s |
| branch tip during compile | `claude/composite-r4` = `27c250f9` throughout | WF-1F lands first; executor re-derives at ITS base |

### 1.4 ⭐ WHERE THE SEED HAS GONE STALE (consumed, not inherited — the corrections)

The §309.3-preserved seed (`draft-WF-1B-II.md`, compiled at `9d851fae`) was re-executed row by
row. Five deltas: (a) WF-1B landed as a SINGLE member, so the seed's "depends on WF-1B-I /
serial promotion / reserved paths" clauses are DISCHARGED — no reservation collides today;
(b) the six `.prose-numerics-baseline.json` rows it orders re-addressed **no longer exist**
(§1.3); (c) its §6 census figures are three landings stale — every figure above re-measured;
(d) its N10 (`impactKindWalkers` `EXPECTED_VOICE`) is **NOT INCURRED** under the chosen mint
idiom — `MINT_RE` (`:46`) matches literal `impactKind:` only, and the file's own header routes
promoted candidateTypes to `tests/domain/candidateTypeVoicePhrasing.walker.test.js`, whose two
arms (phrase resolves underscore-free; voice resolves null) this member satisfies with ZERO
edits; (e) its §85.4 pricing named the two obligations but its manifest omitted their files —
IN-1C-A's recorded lesson ("the member actually cost FIFTEEN") is applied: both §85.4 homes are
manifest rows here (M7, M15).

---

## §2 · THE CURE — EXACT SHAPE

### 2.1 `faithNews.js` (CREATE, ≤80 eff) — the FAITH kind registry, the cured picker, the builder

- `FAITH_KIND_REGISTRY`: ONE row, `faith_last_altar_dark`, significance **`major`** (cadence
  class: an extinction is rare; the DERIVED pool floor is FOUR — `CHRONIC_FLOOR 8 −
  rank 2 × CADENCE_STEP 2`, from `tests/helpers/kindPoolWalker.js`, never authored), audience
  `public`, section `faith`. ⛔ Row-constructor parameter order is `grammarKindRow`'s
  `(kind, significance, audience, section, requiredSlots, …)` — LOAD-BEARING
  (`informationNews.js`'s own header law). `requiredSlots.length === pool.length`.
- **The picker copies `informationNews.js:164`'s CURED keyed pick VERBATIM** —
  `hash01(namespacedSeed)` (avalanched, `contestMath.js`) `* eligible.length` — never the
  `fnv % len` parity-aliasing spelling nine older sites still carry. Key:
  `faith_last_altar_dark::${cid}::${ref}` (deterministic, no rng threading, no Date).
- `faithReceipt({ cid, settlementName, ref, creedName, tick })` returns the OUTCOME:
  `id: 'faith.last_altar_dark.' + stablePart(cid) + '.' + ref` (the conversion-id idiom; entry
  ids gain the tick prefix downstream), **`candidateType: 'faith_last_altar_dark'`** (the
  literal that satisfies BOTH minter scans — C2's and the new arm's — and the
  candidateType walker), `curationClass: 'transition'` (existing vocabulary,
  `pulseHelpers.js:138`; forces `isDriftOnlyOutcome` false so the beat always reaches the feed),
  `targetSaveId: cid`, `affectedSettlementIds: [cid]` (scope stays `settlement`),
  `severity: FAITH_NEWS_TUNING.LAST_ALTAR_SEVERITY`, `headline` + `summary` from the picked
  variant, `reasons` carrying the address chain (§2.5). **No deltas, no condition, no patch** —
  `applyOutcomeToSettlement` no-ops on it by construction; it is a record, not a mutation.
- `FAITH_NEWS_TUNING = Object.freeze({ LAST_ALTAR_SEVERITY: 0.5 })` — ⛔ **the member's ONE
  authored value**; see RAISED R-3 (§7) for the derivation and the chair-signature request.
  0.5 sits deliberately BELOW `newsEntryForOutcome`'s 0.72 major-entry line: the obituary
  surfaces as a `notable` ENTRY (a record, not an alarm) while the KIND's cadence class stays
  `major` (the pool-floor axis). The two significance axes are distinct instruments; the
  executor proves no walker ties them (battery, §6) and STOPs if one does.
- ⛔ **The filename is load-bearing**: `LAYER_PATTERNS.FAITH`'s first regex
  (`couplingInclusion.walker.test.js:150`) claims `faith*` under `worldPulse/` — both new leaves
  are CLAIMED ALREADY ⇒ no `LAYER_PATTERNS` row, no `ARGUED_UNLAYERED` entry, both coupling
  ceilings stand still (re-verify both halves at base; §P2.8). A rename mints two unlayered
  modules against a zero-headroom baseline.

### 2.2 `faithReceiptPools.js` (CREATE, ≤40 eff) — the authored corpus

≥4 variants (the DERIVED `major` floor), **four different STRUCTURES AND ANGLES**, not four
rewordings (SP-6's own words). Slots available at the deletion and NOTHING else: creed display
name, settlement name, tick. ⛔ No slot the engine cannot supply — no clergy name, no realm-wide
count, no dormancy-length arithmetic (J-TCWF8-5). ⛔ DOCTRINE: the sentence's subject is the
altar and the congregation — the seed's worked line is the register: *"the last altar of
<creed> in <town> went dark; none there now keep the rite."* Two spellings REFUSED at compile,
not softened: any line naming a god's act (departed/abandoned/failed). No floats, no decimals,
comparisons in words (game-grade, L6).

### 2.3 The fold edit (`religiousContest.js`, MODIFY, ≤14 eff; base **470/800**)

Symbol-first, two anchors: inside `advanceReligionStates`, at the `// 3b. advance shares`
block's `advanceShares(state, strengthByRef, { narrativePrune: unseating });` call (`:795` at
this base).

1. Behind `unseating` (no new flag read — the hoisted local), capture the pre-prune names of
   currently-suppressed entries: `ref → state.deities[ref].snapshot?.name` (the entries are
   DELETED by the prune; the names must be read first). ~2 lines.
2. Capture the seam: `const prunedRefs = advanceShares(...)` — the discarded return, consumed
   at last. Net 0 on the call line.
3. `if (unseating) for (const ref of prunedRefs) outcomes.push(faithReceipt({...}))` — creed
   name = captured authored name, else `deityDisplayNameFromRef(ref)` (the estate's accepted
   resolver for uncarried refs — an extinct creed is definitionally uncarried; worldPulse →
   display imports have four landed precedents and `display/` sits outside `CENSUS_SCOPE_RE`,
   so no coupling row; re-verify with the walker at base). ~4-6 lines + the import.

⛔ Dark (`unseating` false): the name capture and the mint are both skipped; `prunedRefs` is
captured and unread — the fold's bytes on the dark path change only by the `const` binding,
and byte-identity is proven by the B5 fences, not assumed. Deity-free: the fold returned at its
head before any of this exists.

### 2.4 The three routing homes (+1 row each)

- `heraldRouting.js` `EXACT_SECTION`, the FAITH block (`:137-150`, beside
  `sovereignty_sale_judged`): `faith_last_altar_dark: 'faith'`. The `faith_` prefix rule
  (`:357`) would route it free — the exact row is what moves `ROUTED_TOKENS` and
  `REGISTERED_KIND_COUNT` together, holding the divergence identity at 8 (measured
  arithmetic: §1.3).
- `chroniclersLetter.js` `KIND_SECTION`: `faith_last_altar_dark: 'traditions'` — the letter's
  own vocabulary (its valid set is the frozen six; `traditions` is where the pantheon rows
  live), and the correspondence walker holds WITHOUT a divergence row because
  `KIND_SECTION_CORRESPONDENCE.traditions === 'split'` with deity/pantheon beats → `faith` —
  exactly this case. **This is the resurrection**: the row lands in the same commit as its
  live minter, discharging §321.2(a)'s condition.
- `settlementRumors.js` `WHAT_PHRASES` (`:116`): one phrase. Required for a desk-bearing kind;
  it is also what makes `whatPhrase()` resolve the candidateType canonically (underscore-free —
  the candidateType walker's phrasing arm).

### 2.5 The receipt (news address law, L6)

The entry as surfaced (via `newsEntryForOutcome`): id + `sourceEventId` (the outcome id, which
encodes settlement and creed) + `settlementIds: [cid]` + typed action (`impactKind:
'faith_last_altar_dark'`) + the settlement and creed NAMED in headline and reasons + the reason
chain (the creed lost its last altar; none there now keep the rite). One band per clause;
no numerals on composed output.

---

## §3 · REGISTRATION TOTALITY AND THE TWO REVIEWED ACTS

### 3.1 The §P4 template, at full strength (row · head · pin · test, per registration)

| row | head re-export | exact pin | registry test |
|---|---|---|---|
| `FAITH_KIND_REGISTRY` (faithNews.js) | none — kind registries are imported directly by their walkers (informationNews precedent) | the exact one-kind census, both directions | `tests/lint/faithKindPools.walker.test.js` (NEW) |
| `EXACT_SECTION` row | n/a (frozen map) | `ROUTED_TOKENS` 379 exact; the routed-arm | `kindPoolFloors.walker.test.js`, `heraldRouting.walker.test.js` |
| `KIND_SECTION` row | n/a | the minter-totality arm (§4); the correspondence arm | `heraldRouting.walker.test.js` |
| `WHAT_PHRASES` row | n/a | phrase resolves canonical + underscore-free | `candidateTypeVoicePhrasing.walker.test.js` (no edit), `heraldRouting.walker.test.js:128` arm |
| `HBF-` row (habitForkRegistry.js) | n/a | `CLASSIFIED toEqual DISCOVERED`, both directions | `chooserTotality.walker.test.js` (no edit) |

### 3.2 ⛔ THE SECOND SMALL FAMILY — the reviewed act §309.3 hoped to avoid, taken openly

`kindPoolFloors.walker.test.js:206`'s exact list `['INFORMATION']` exists "to make a SECOND
small family a visible, reviewed act." A one-row FAITH registry IS that family. §309.3's
economics assumed the beat would land beside WF-8's ~20 narration kinds; §347.1 sequenced the
obituary member FIRST, so the review falls HERE — priced at one exact-list edit (order derived
by RUNNING the arm, never predicted) with §309.3 + §347.1(2) as the authorizing decisions, and
**the shrink-back is a RECORDED OBLIGATION of the next WF-8 member that takes FAITH to ≥5
rows** (deferred-and-recorded, not dropped). RAISED R-1 (§7) gives the chair the veto with
alternatives priced.

---

## §4 · THE §321.2a MINTER-TOTALITY ARM — measured ground first, then the design

### 4.1 ⭐⭐ THE ORDERED GAP IS PART-BUILT, AND THE COMPILE PROVED IT BY PLANTING

Executed at `27c250f9` (plant → convict → `cmp`-exact restore → green control;
`laneTCWF8-c2-plant.log`, `laneTCWF8-herald-plant.log`, `laneTCWF8-c2-green.log`):

- A planted minter-less key (`zz_dead_row: 'sundry'`) reds
  `tests/domain/chroniclersLetter.test.js`'s C2 arm — `expected [ 'zz_dead_row' ] to deeply
  equal []`, 1 failed | 24 passed, TRUE_EXIT=1 — **C2 has required a literal
  `candidateType:`/`impactKind:` minter for every `KIND_SECTION` key since commit `1477c284`,
  long before the WF family existed.**
- The SAME plant also reds TWO arms of `tests/lint/heraldRouting.walker.test.js`
  (correspondence + routed drift-coverage): 2 failed | 17 passed, TRUE_EXIT=1.
- Restore proven `cmp`-exact against the git blob; green control 25/25, TRUE_EXIT=0.

⇒ TC-WF1D's §4.1 claim ("a dead row would have sat in a frozen table with every walker green")
is **REFUTED BY EXECUTION** — staleness finding S-1, §7. What §321.2(a) ordered that genuinely
does NOT exist: the **written-ruling escape** (C2 is absolute — a lawfully-refused-but-recorded
key has no way to stand), the **hygiene arms** that keep such an escape honest, and residence in
the **walker estate** (`tests/lint/`).

### 4.2 The design (the lawful §321.2a discharge; J-TCWF8-4)

- `chroniclersLetter.js` mints, beside `KIND_SECTION`:
  `export const KIND_SECTION_MINTER_RULINGS = Object.freeze({});` — key → the ODQ §-ref that
  lets a minter-less key stand. **Empty at mint, by measurement** (every current key is
  literal-minted — C2 green at base). Production precedent: `KIND_SECTION_DIVERGENCES`
  (heraldRouting.js), each entry a recorded judgment. ~+4 eff.
- **The arm RELOCATES C2's substance into `tests/lint/heraldRouting.walker.test.js`** — the
  KIND_SECTION walker's own home, satisfying §321.2a's "joins the walker estate" by the letter —
  as ONE `it` with a literal title, asserting: (i) every `KIND_SECTION` key is minted (the
  C2 scan mechanics: `candidateType:`/`impactKind:` literals over `src/domain`, non-vacuous
  floor >50) **or ruled** (`KIND_SECTION_MINTER_RULINGS`); (ii) no ruling names a MINTED key
  (a stale ruling reds); (iii) no ruling names a key `KIND_SECTION` lacks (an orphan ruling
  reds); (iv) every ruling value spells an ODQ §-ref; (v) the letter's section-value vocabulary
  check C2 carried (the frozen six) moves with it. **In-suite §75 controls, run every time:**
  the predicate is a local pure helper and two control calls prove both directions —
  `deadKeys({zz:1}, ∅, {})` convicts and `deadKeys({zz:1}, ∅, {zz:'§321.2a'})` clears.
- `tests/domain/chroniclersLetter.test.js`'s C2 `describe` (1 it) is REMOVED with a pointer
  comment — a **DECLARED re-record citing §321.2(a)**, stated in this packet and in the diff,
  never silent. Single-truth preserved: one arm, one home, one rulings map.
- **Executor plants (mutant hygiene, `cp`/`cmp`, never the `git checkout` family):** P1 the
  dead key (must red the new arm; the correspondence/routed arms red as attributed collateral —
  predicted above from the compile's own plant); P2 dead key + ruling row (the new arm's dead
  check must CLEAR — the escape's executed proof — while routed/correspondence still red,
  attributed); P3 a ruling for `pantheon_extinction` (the stale-ruling arm reds alone).

---

## §5 · THE A6 FLIP — a declared re-record, with the assertions proven still

`tests/domain/warTermination.test.js:455` ('WF-1d registration: the vocabularies match, the
pair is licensed once, and no Chronicle row exists'):

- **The assertions DO NOT MOVE** — measured at base and re-proven by the executor pre/post:
  `not.toContain('war_termination_read')` (`:466`, WF-1D's kind is a receipt kind and still
  earns no Chronicle row), the `patron_fall*` filter (`:467`), and the `pantheon_extinction`
  positive anchor all hold — `faith_last_altar_dark` collides with none of them.
- **The COMMENT is re-recorded** (`:463-464`): its clause "nothing would red on it" was refuted
  by execution (§4.1) and is now doubly false once the arm lands. Replacement states: the
  WF-1d-refusal stands for WF-1d's own kinds; the settlement obituary's row was lawfully
  resurrected at WF-8A under ODQ §309/§321.2a with its live minter; minter-less keys are
  redded by the §321.2a arm (heraldRouting.walker) and by nothing less.
- Title unchanged (census-stable); anchored-comment law re-checked (the `:466` anchor's
  justification still holds). ⚠ Comment prose must not transcribe any forbidden matcher or
  claim vocabulary — run the live `CLAIM_RE` before writing (§P5).
- §104.4 comment-only ratchet: test files appear in no edge bundle — NOT INCURRED (re-derive).

---

## §6 · PRICED OBLIGATIONS, BUDGET, ACCEPTANCE, CHECKS

### 6.1 Obligations table

| obligation | verdict |
|---|---|
| §49/§50/§148 flag-mint (FIVE obligations) | **NOT INCURRED** — no flag minted; rides the landed by-name read |
| §85.4 seeded chooser/pool | ⛔ **INCURRED, BOTH ROWS PRICED AS FILES** (IN-1C-A's lesson): (a) the `habitForkRegistry.js` classification row — by the registry's own taxonomy, no honest class is a STOP; (b) the `tests/fixtures/mechanism-lit-coverage-baseline.json` rows on the informationNews/informationReceiptPools idiom (the pools module takes the honest inventory row unless the battery genuinely lights it) |
| §102.2/§P3b mutation-coverage | ⛔ **INCURRED** — `faithKindPools.walker.test.js` is an invariant file at landing: ONE `rationale` entry in `scripts/mutation-coverage-manifest.json`'s `rationales` map beside its `*KindPools` siblings, surgical insert, promotion path named. NEVER `uncovered`; NEVER a re-serialize |
| §31 anchor preflight | ⛔ **INCURRED, ceiling zero, TWO new-negative surfaces** (the walker file; the acceptance negatives). `// anchored:` last-line law; preflight bare before member-green |
| test census | ⛔ **INCURRED — the named interior red.** New file ⇒ `files` reds by construction; tuple re-derived WHOLE by the placeholder-of-zeroes method; predicted shape `+1/+0/+1/+T/+0` — suiteTitles NET ZERO (the walker's +1 describe against C2's −1) and THAT prediction is itself proven by the separate `describe(`-grep law (§P5). `T` = the walker's its + 8 acceptance its + 1 relocated arm − 1 C2 it + the A6/kindPoolFloors title deltas (predicted 0) — derived at the tip, never inherited |
| declared shift | **NOT INCURRED** — no preset declares `faithUnseatingEnabled`; dark byte-identical by construction; ⛔ same-seed golden motion is a STOP (`worldpulseDeityGolden` runs both sides) |
| chair value signature (§42/§43) | ⛔ **INCURRED, ONE ROW** — `LAST_ALTAR_SEVERITY` (RAISED R-3). Everything else is derived (pool floor from the shared helper; the bar is WF-1B's) |
| §77 premise map | **INCURRED** — rows in §10 |
| naked-claim debt | **INCURRED, PER CLAIM** — live `CLAIM_RE` run BEFORE writing the packet and any comment prose; this compile pre-scanned its own outputs |
| coupling registry, BOTH halves | **NOT INCURRED, measured** — `faith*` names claimed by `LAYER_PATTERNS.FAITH`; display-import precedented and out of census scope; both ceilings still (§2.1, §2.3) |

### 6.2 Hard scope budget — and the TWO override rows this packet must have ruled before dispatch

| limit | budget | WF-8A | |
|---|---:|---:|---|
| behavior families | 1 | 1 | ✓ |
| new persisted record families | 1 | 0 | ✓ |
| feature flags | 1 | 0 new | ✓ |
| user-facing surfaces | 1 | 1 (the feed) | ✓ |
| direct production consumers | 2 | 1 (the fold) | ✓ |
| new logic leaves | 2 | 2 | AT CAP |
| existing logic files modified | 3 | 1 (`religiousContest.js`) | ✓ |
| **registration-only production files** | **3** | **4** — heraldRouting, chroniclersLetter, settlementRumors, **habitForkRegistry (§85.4a — estate-wide law, not a design act)** | ⛔ **OVERRIDE 3 → 4** |
| **handwritten files total** | **12** | **16** (enumerated §6.3; 17 if the packet doc is counted — both accountings stated, the ask covers the larger) | ⛔ **OVERRIDE 12 → 17** |
| new/changed effective production lines | 400 | ~143 projected (80+40+14+3+6) | ✓ |
| acceptance cases | 8 | 8 | AT CAP |

Grounds (the seed's own form): every file beyond twelve is a standing-law obligation, not a
design act — the family walker every registry family owes, its mandatory manifest row, the two
§85.4 estate rows, and the two §321.2a/§309-ordered re-records. ⛔ An override never transfers;
this is WF-8A's own ask, one enumeration, nothing inherited. Droppable rows named in §6.3
shrink the count and never grow it.

### 6.3 Exact change manifest (M-rows; the 16 counted are M1–M16)

| # | action | path | instruction |
|---|---|---|---|
| M1 | CREATE | `src/domain/worldPulse/faithNews.js` | §2.1. ≤80 eff, 250-leaf cap |
| M2 | CREATE | `src/domain/worldPulse/faithReceiptPools.js` | §2.2. ≤40 eff |
| M3 | MODIFY | `src/domain/worldPulse/religiousContest.js` | §2.3. ≤14 eff; base 470/800 |
| M4 | MODIFY | `src/domain/realm/heraldRouting.js` | +1 `EXACT_SECTION` row (§2.4); base 268/800 |
| M5 | MODIFY | `src/domain/display/chroniclersLetter.js` | +1 `KIND_SECTION` row + the RULINGS map (§2.4, §4.2); base 220/800 |
| M6 | MODIFY | `src/domain/display/settlementRumors.js` | +1 `WHAT_PHRASES` row; base 509/800 |
| M7 | MODIFY | `src/domain/worldPulse/habitForkRegistry.js` | +1 `HBF-` classification row (§85.4a); base 245/800 |
| M8 | TEST | `tests/lint/faithKindPools.walker.test.js` | NEW FILE — copy `informationKindPools.walker.test.js` (252 raw) shape: both-direction exact census, ≥2 in-suite §75 mutant-control arms proven to THROW (a rotted kind spelling; a duplicated row), ONE literal `describe`, straight-line `it`s, string titles, anchors per §P3 |
| M9 | TEST | `tests/lint/kindPoolFloors.walker.test.js` | `REGISTRIES` 10→11 (import `FAITH_KIND_REGISTRY`), 112→113, 378→379, divergence holds 8, small-family list gains FAITH (§3.2, order by execution). Count-titled arms take the rename door (census-cardinality-still) |
| M10 | TEST | `tests/lint/heraldRouting.walker.test.js` | the §321.2a arm (§4.2), ONE new `it` |
| M11 | TEST | `tests/domain/chroniclersLetter.test.js` | C2 block removed with pointer (§4.2) — declared re-record |
| M12 | TEST | `tests/domain/patronFall.test.js` | the 8 acceptance `it`s into the ONE existing `describe` (`:147`) |
| M13 | TEST | `tests/domain/warTermination.test.js` | the A6 comment re-record (§5) — declared |
| M14 | TEST | `tests/lint/sovereigntyLightingContract.walker.test.js` | the tuple, re-derived whole (§6.1) |
| M15 | TEST | `tests/fixtures/mechanism-lit-coverage-baseline.json` | §85.4b rows, surgical |
| M16 | GUARD | `scripts/mutation-coverage-manifest.json` | M8's `rationale` row, surgical |
| M17 | DOC | `docs/implementation/packets/fp/WF-8A.md` | this packet. `CLAIM_RE` first |
| — | DROPPED | `src/domain/worldPulse/religionState.js` | the landed bare-ref seam suffices (J-TCWF8-5); revive ≤4 eff ONLY if the executor's fixture-first run proves the name capture insufficient, and say so |
| — | NO EDIT | `impactKindWalkers.test.js`, `chooserTotality.walker.test.js`, `candidateTypeVoicePhrasing.walker.test.js`, `.prose-numerics-baseline.json` | §1.3/§1.4 — each verdict re-derived at base |

### 6.4 Acceptance (closed denominator — 8 cases, all in M12's describe)

⚠⚠ RUN THE FIXTURE FIRST AND PRINT WHAT THE ENGINE DID — the suppressed count, the deleted
refs, the composed outcome, `deriveDecisionTier(outcome)`, and the rendered entry — BEFORE any
pin. The seeding hazard is the family's canonical one (§P6): `PATRON_FLIP_TICKS` 3,
`SHARE_STEP_MAX` 6, an organic flip takes ~5 ticks, and `nicheOf` is COMPUTED — a hand-authored
`niche` string routes attemptEntry to open_slot and suppresses NOTHING (the §309.1(c) vacuity).
Deities enter fixtures ONLY via `SET_PRIMARY_DEITY` / `IMPOSE_CULT`.

| id | case |
|---|---|
| B1 | four suppressed → ONE beat, naming the deleted creed and the settlement, lit. The literal 4 lives in the FIXTURE only |
| B2 | three suppressed → NOTHING (the near-miss negative; positive control in-test; anchored) |
| B3 | five suppressed → TWO beats — per deleted creed, never per tick |
| B4 | stamps and codepoint order DISAGREE in the fixture (a fixture where they agree is vacuous — STOP); the beat names the lit-prune's deleted creed, and its NAME is the authored snapshot name, not the slug |
| B5 | dormancy: dark deletes silently — rendered feed carries no `faith_last_altar_dark` (anchored against the populated-feed positive); `{}` vs `{faithUnseatingEnabled:false}` byte-identical; the literal `faithUnseatingEnabled: true` drive moves a fenced byte (the lit-mutant control). Own-footprint golden captured BEFORE wiring on a deity-BEARING fixture (§P2.2 — the deity-free corpus golden is NOT this fence) |
| B6 | registration totality both directions: pool ≥ derived floor 4, `requiredSlots.length === pool.length`, SECTION_OF/`KIND_SECTION`/`WHAT_PHRASES` all carry it, registry census exact |
| B7 | the surfaced entry is game-grade + address-complete (§2.5): typed action, names, reason, no numerals; and it is `kind: 'applied'` — the beat is a record, never a proposal (print the applied path once through the real pulse) |
| B8 | the pick is deterministic (same key → same variant, pinned on a fixed fixture) AND the pool genuinely spreads (two different (cid,ref) keys reach ≥2 distinct variants — the non-vacuity of the chooser; derive the witness keys by execution) |

### 6.5 Mutants (plant by `cp`, convict, restore `cmp`-exact; a mutant convicts a BRANCH)

(e) hoist the mint out of the per-ref loop → **B3 alone** · (f) name the codepoint-last creed
→ **B4 alone** · (h) drop the `unseating` conjunct on the mint → **B5's lit fences** ·
(i) replace the keyed pick with `pool[0]` → **B8 alone** · P1/P2/P3 (§4.2) → the minter arm's
three directions · M8's two in-suite controls throw on every run. ⚠ If two convict the same
arms, one guard is vacuous — STOP and re-shape. The WF-1B-era mutant (g) (the off-by-one bar)
is NOT taken: the bar is WF-1B's landed branch, already under its own battery (recorded
non-goal, not a drop).

### 6.6 Checks

```
npx vitest run tests/lint/faithKindPools.walker.test.js \
  tests/lint/kindPoolFloors.walker.test.js tests/lint/heraldRouting.walker.test.js \
  tests/lint/phrasedKindPools.walker.test.js tests/lint/heraldContaminationFence.test.js \
  tests/lint/mutationCoverageManifest.test.js tests/lint/negativeAssertionAnchor.walker.test.js \
  tests/lint/couplingInclusion.walker.test.js tests/lint/sovereigntyLightingContract.walker.test.js \
  tests/lint/chooserTotality.walker.test.js tests/lint/wizardNewsAuthoring.walker.test.js \
  tests/lint/sizeBaseline.test.js \
  tests/domain/habitForkRegistry.test.js tests/domain/candidateTypeVoicePhrasing.walker.test.js \
  tests/domain/impactKindWalkers.test.js tests/domain/chroniclersLetter.test.js \
  tests/domain/heraldIntegrity.test.js tests/domain/patronFall.test.js \
  tests/domain/religionState.test.js tests/domain/religiousContest.test.js \
  tests/domain/warTermination.test.js tests/property/worldpulseDeityGolden.test.js
```

1. §31 anchor preflight bare, in-shell, BEFORE member-green (two new-negative surfaces).
2. ⛔ NEVER wrap `npm run check*` in `gate-mutex.sh --run`; `check:tail` bare, fresh shell,
   `; echo TRUE_EXIT=$?`, outlast it in your own turn; exit 3 is the mutex, not a red.
3. Trust no exit not captured in-shell; SELF-NAMED logs only. (This compile tripped the
   wrapper hazard once itself — a `tee` pipeline reported 0 over a crashed node; §11 of the
   receipt.)
4. Both typecheck configurations at their exact floors, config and window named.
5. The census is SEQUENCED; prove `suiteTitles` separately by the whole-diff `describe(` grep
   (predicted net 0 — named here before it exists, §P7.15).
6. Pre-existing reds only from `scripts/.test-ratchet-baseline.json` read WHOLE at the base.
7. Step-8 STOP: re-derive EVERY §1 figure at the executor's own base (WF-1F lands first).

---

## §7 · RAISED FOR THE CHAIR (rule before dispatch; each with the compile's recommendation)

- **R-1 · The second-small-family review is INCURRED here** (§3.2). Recommendation: take it —
  the arm exists to force exactly this review, both authorizing rulings exist, and the
  shrink-back is recorded against the next WF-8 member. Alternatives priced: hold TE-WF8 until
  a narration member co-lands ≥4 more kinds (inverts §347's queue; every narration kind needs
  its own producer and pool — a multiplied member, the WF-1 refusal shape); park the beat again
  (re-opens §309.3).
- **R-2 · §321.2(a)'s premise is corrected by execution** (§4.1): the minter half has existed
  since `1477c284` in `tests/domain/`; the compile discharges the ORDER by relocation + the
  ruling escape + hygiene (§4.2). Veto lands the arm as a tests/lint TWIN instead (C2 kept):
  −1 file edit, +1 duplicate truth with a foreclosed escape — priced and not recommended.
- **R-3 · The severity constant asks for its §42/§43 signature at this review.**
  `LAST_ALTAR_SEVERITY: 0.5` — derivation: the conversion outcome's no-piety-record default in
  the same fold; deliberately below the 0.72 major-entry line so the obituary surfaces as a
  notable RECORD while the kind's cadence class (major, pool floor 4) is untouched. Veto
  substitutes a signed value or re-classes the entry.
- **R-4 · Naming**: the packet lands as `WF-8A` (stage 1 of the multi-member WF-8 train), no
  flag, `faithNarrationEnabled` untouched and still charted to light LAST. Veto renames before
  promotion (J-TEWF1B-2: the path carries no suffix ambiguity either way).

## §8 · JUDGMENT CALLS (each vetoable)

- **J-TCWF8-1** — the beat travels as a fold-minted OUTCOME through the landed generic
  `newsEntryForOutcome` path, over a direct news-entry mint. The outcome path costs ZERO
  zero-headroom edits (measured end to end, §1.2); a direct mint needs an `ownsOutcomeNews`
  seam in `applyWorldPulse.js` (941/941). This is the compile's center of gravity.
- **J-TCWF8-2** — `candidateType` literal over `impactKind` literal at the mint: it is the
  promoted-candidateType path the estate already walks (phrasing + null-voice arms), it
  satisfies both minter scans, and it keeps `EXPECTED_VOICE` closed to event-shape labels.
- **J-TCWF8-3** — creed name = pre-prune authored snapshot name, `deityDisplayNameFromRef`
  fallback for the nameless (the estate's converged resolver for uncarried refs).
- **J-TCWF8-4** — relocation of C2 over amend-in-place (§4.2, R-2).
- **J-TCWF8-5** — no dormancy-length slot in the pool ⇒ `religionState.js` untouched; the
  member stays off WF-1B's files entirely.
- **J-TCWF8-6** — the picker copies `informationNews.js`'s cured `hash01` spelling VERBATIM and
  repairs none of the nine parity-class sites (`CR-IN1C-DRIFT` stands; not this member's).

## §9 · censusAuthorization

`{ movedInstruments: [sovereigntyLightingContract tuple, kindPoolFloors REGISTRIES/counts/
small-family list], authorizingDecisions: ['ODQ §309.3', 'ODQ §321.2a', 'ODQ §347.1(2)'],
newTestFiles: ['tests/lint/faithKindPools.walker.test.js'] }` — shaped to the §303.7 block at
promotion; the validator refusal for unauthorized census moves is satisfied in-body here.

## §10 · §77 PREMISE-MAP ROWS

| id | premise | grade at `27c250f9` | how it dies |
|---|---|---|---|
| Q-1 | ten registries, none FAITH; small-family list `['INFORMATION']` | EXECUTED | another family mints FAITH or a second small family first ⇒ §3.2 re-prices |
| Q-2 | 112/378/274/8 and the tuple `2492/364/2128/20677/5780` | EXECUTED | any landing moves them ⇒ re-read at base (WF-1F WILL land first; its own census prediction is stillness) |
| Q-3 | the seam: `advanceShares` returns the deleted refs and the return is DISCARDED at `:795` | EXECUTED | a landing consumes it first ⇒ re-derive the fold edit |
| Q-4 | C2 reds on a dead `KIND_SECTION` key; heraldRouting.walker reds twice | EXECUTED (plant) | the arms move ⇒ §4 re-prices |
| Q-5 | `newsEntryForOutcome` stamps `candidateType` as `impactKind`; the voice classifier is exact-token | EXECUTED | the curation seam changes ⇒ J-TCWF8-1 re-opens |
| Q-6 | `faith*` leaves claimed by `LAYER_PATTERNS.FAITH`; both coupling ceilings still | EXECUTED | the regex narrows ⇒ coupling rows owed |
| Q-7 | a chair ruling exists on BOTH §6.2 override rows and on R-3 | NOT YET TRUE | absent ⇒ ⛔ STOP at promotion (§P7.7) |

## §11 · REQUIRED SYMBOLS (present at THIS status; symbols and anchors, never figures)

`advanceReligionStates` · `advanceShares` · `isSubsystemActive` · `faithUnseatingEnabled` ·
`SECTION_OF` / `EXACT_SECTION` / `PREFIX_RULES` / `KIND_SECTION_CORRESPONDENCE`
(heraldRouting.js) · `KIND_SECTION` (chroniclersLetter.js) · `WHAT_PHRASES` / `whatPhrase`
(settlementRumors.js) · `newsEntryForOutcome` / `isDriftOnlyOutcome` (worldPulseFeedCuration.js)
· `newsVoiceCategory` (newsVoice.js) · `hash01` (contestMath.js) · `deityDisplayNameFromRef`
(deityNames.js) · `HABIT_FORK_REGISTRY` (habitForkRegistry.js) · `INFORMATION_KIND_REGISTRY`
(informationNews.js) · `FREQUENCY_FLOORS` / `floorFor` (tests/helpers/kindPoolWalker.js).
*Created at the flip to LANDED:* `FAITH_KIND_REGISTRY`, `FAITH_KINDS`, `faithReceipt`,
`FAITH_NEWS_TUNING` (faithNews.js) · `KIND_SECTION_MINTER_RULINGS` (chroniclersLetter.js).
`retiredSymbols`: none.

## §12 · PREFLIGHT (each a STOP)

0. The two §6.2 overrides and R-3 are RULED; this packet is READY (it is DRAFT as written).
1. `git rev-parse HEAD` is a descendant of `27c250f9`; the preamble SHA-256 recomputed; every
   §11 symbol resolves; discharge by an EXECUTED `git diff --name-only 27c250f9..HEAD`, quoted.
2. `npm run validate:packets` exits 0 at every status flip; no `CREATE` path exists at HEAD.
3. Re-read, never inherit: every §1.3 register, the tuple, the §85.4 taxonomies, the
   `.prose-numerics-baseline.json` verdict, the bundle metas.
4. Capture the own-footprint golden BEFORE wiring (deity-BEARING fixture, doctrine path only).
5. Run the seeding fixture and PRINT the suppressed count, deleted refs, outcome, and rendered
   entry before writing B1–B8.
6. Name the interior red (M8) in the train plan before it exists — done here (§6.1); the
   executor repeats it in the train plan it files.
