# §893 — THE ENC-4 RULINGS AND THE CHAIR'S DRAFTED WORDS
⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧ · every word below is the pen's to overwrite.

## 1. ⛔ HOLE 0 IS A PHANTOM BLOCKER — BOTH HALVES, ALREADY CURED, NEVER STRUCK
`ENC4-TYPED-HOLES-FOR-THE-VOICE-SITTING.md` calls HOLE 0 *"TWO NAMING RULINGS, AND BOTH ARE
BLOCKING."* **Both were settled before the sheet was last read, by the §892 banner at the top of that
same file** — which was written and then not carried down into the hole sections beneath it.

- **0a — which spelling of the exposed kind.** Settled: **`chance_meeting_exposed`**, per §889.1(b),
  which the banner already restates. §5.5's `chance_meeting_approach_exposed` is the loser; a ledger
  ruling outranks a design-doc section.
- **0b — the three-axis collision on the string `chance_meeting`.** **Dissolved**, because §889.1(a)
  renamed the first Herald kind to **`chance_meeting_recorded`**. The news kind never takes the
  contested string, so `WILLED_MEETING_CONSPIRACY = 'chance_meeting'` keeps its single meaning and
  `envoyChanceMeetingLedger.test.js:167`'s pin is untouched.

**MEASURED, not reasoned** (at train tip `62afd84d3`): `grep -rn` over `src` and `tests` returns
**zero** occurrences of `chance_meeting_recorded` and **zero** of `chance_meeting_exposed`. Both
tokens are free. The conviction risk the sheet describes required the news kind to resolve to the
live constant; it no longer can.

⭐ **THE LAW, AGAIN, ONE LAYER DOWN.** This is the third instance in three days of the same shape:
*an amendment declared in a banner is not an amendment — the clause text is the receipt* (§892's E1),
and *a blocker is a claim, and a claim decays*. A stale figure gets refused by a gate; **a stale
blocker is obeyed in silence.** HOLE 0 cost ENC-4 a full stop it did not owe.

## 2. THE DRAFTED WORDS — HOLE 3, owed on BOTH roads, mandatory
Destination: `WHAT_PHRASES` in `src/domain/display/settlementRumors.js`. A token with no row here
reaches the reader as the refusal phrase and is convicted by the §886 walker; there is no humanizer
to fall back on.

| token | ⟦CHAIR-DRAFTED — the pen's to overwrite⟧ |
|---|---|
| `chance_meeting_recorded` | `a meeting neither party arranged` |
| `chance_meeting_exposed` | `a refusal that did not stay private` |

**Checked against all six R1 refusals** (`newsSubjectVocabulary.walker.test.js:428-457`): each is a
bare lower-case noun phrase; no terminal `.`/`!`/`?`; no digit; no underscore; no `{`, `}` or `$`; no
opening connective (`and` in the second appears mid-phrase, which the rule does not touch); no em
dash; neither starts with a capital; neither equals `'a matter of some moment'`.

**Read aloud in all six frames**, which is the test that actually matters:
*Forge Town* **wakes to** / **braces for** / **has word of** / **is spared** / **pays no heed to** /
**has seen the last of** *a meeting neither party arranged* — and likewise *a refusal that did not
stay private*.

⭐ **The second phrase is chosen to end the §8.3-versus-§5.5 quarrel rather than pick a side.** §8.3
says *an approach was made and turned down, and the turning down reached the court*; §5.5 says *an
offer was refused and the refusal spoken of*. `a refusal that did not stay private` is true under
both readings, so the surviving reason no longer has to be adjudicated before the phrase can ship.

## 3. WHAT REMAINS GENUINELY OWED ON ENC-4
HOLE 1's six `chance_meeting_recorded` variants and HOLE 2's four `chance_meeting_exposed` variants,
plus the three band fills — **Road B only**. The floor is machine-enforced
(`FLOOR_BY_SIGNIFICANCE = { routine: 8, notable: 6, major: 4 }`) and the walker was built to refuse
floor-gaming stubs, so these cannot be placeholdered. The chair drafts them with the lane, in the
FAITH annex's exact format, once §5b's road measurement lands.
