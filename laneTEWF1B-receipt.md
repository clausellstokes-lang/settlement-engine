# lane TE-WF1B — WF-1B full packet lifecycle receipt

> **THE TIP FOR THE CHAIR'S CAS:**
> ## `4f25da23dcb72d77b9fe53a3b3a4318f14b3392e`
> Detached HEAD in `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/6298872d-53af-4c8a-99e5-139bfc5bfc1f/scratchpad/wf1b-tree`,
> **five** commits on top of `claude/composite-r4 @ 9d851fae`. Working tree CLEAN. **The worktree is LEFT IN PLACE for
> chair verification.** A second worktree, `…/scratchpad/wf1b-baseproof`, holds the pristine base
> `9d851fae` for identity comparison and is also left in place.

## OUTCOME, FIRST

**WF-1B is built, landed and terminalized. Sixteen of seventeen gate steps are GREEN, plus
`smoke:boot`. The seventeenth — `test:ratchet` — carries a red that EXISTS AT THE BASE and is not
this member's**, proven by a full committed-base run whose non-census failure set is IDENTICAL;
it is proved inherited on four executed legs in §7, and this lane deliberately does not repair it
because the standard forbids repairing unrelated rows, the file is outside the packet's closed
change manifest, and it is chair-signed law. **A green terminal is not reachable from this base
without a chair act**, and the one-line cure is written out for the chair in §7.4.

Everything the packet asked for is built and proven:

- ⭐ **The eight acceptance cases pass, and one of them carries the vacuity control as an
  assertion rather than a note.** `nicheOf` is a computed niche, so the obvious A1 fixture cannot
  execute its own defect — measured before any pin was written, and both arms are pinned.
- ⭐ **Dark is byte-identical, measured against a pre-wiring golden** on deity-BEARING fixtures.
  All four dark arms hash exactly as they did at the base. Only the lit eviction arm moves.
- ⭐ **Five mutants convict; both §13 STOP conditions are checked and neither fires.** A fifth,
  lane-added mutant is what makes the third one mean anything.
- ⭐ **Zero call sites edited.** `tests/domain/religionState.test.js` is green UNEDITED, which is
  the executed proof that the threading is additive rather than the claim that it is.
- **The census arithmetic closes exactly**: `+0/+0/+0/+8/+0`.

## 1 · WHAT LANDED, COMMIT BY COMMIT

| sha | what |
|---|---|
| `d6a932c1` | The packet lands at **DRAFT** at `docs/implementation/packets/fp/WF-1B.md`, with its `PACKET_MANIFEST.json` row spliced as a scoped text APPEND and its `INDEX.md` row inserted after WF-1A |
| `92217d2c` | **DRAFT → READY** after re-executing the whole §15 preflight at this tip |
| `39d0ad7c` | **The implementation** — M1, M2, M3, M4, M5; M6 verify-only and green unedited |
| `4acd323f` | **READY → LANDED**, `suppressDeity` joins `requiredSymbols`, and §16 (the landing record) is written into the packet itself |
| **`4f25da23`** | **The any-cast cure the FULL GATE found** — the sole writer's `state` param typed instead of `any`; see §13.3. **This is the tip.** |

Diffstat against the base: **8 files, ~1443 insertions, 26 deletions** — two production files, three
test/baseline files, and three packet-system files. No `package.json` / `package-lock.json` motion.
Final file digests at the tip: `religionState.js` `1ff2f3f9b637bba2c65bb4ceaf619d58`,
`religiousContest.js` `c3533b73dc24426da1216d00406b55d8`.

## 2 · PREFLIGHT (§15), EVERY FIGURE THIS LANE'S OWN

| § | check | result |
|---|---|---|
| 0 | `git rev-parse HEAD` | `9d851faeb7cac73217d508f621d1d9ee29f2c145` — **CONFIRMED** |
| 0 | `git diff --name-only f8d978df 9d851fae` | exactly three paths, all `docs/` — the window is DOCS-ONLY, **executed not asserted** |
| — | WF-PREAMBLE.md SHA-256 | `cd067ba1ce41232a99dee9a7247f4c82d47fbe2b4fd9bb8599784d4a33136211` — matches the packet's binding citation exactly |
| 1 | `BASE_STATE.json` admissibility | ⛔ **NOT CITABLE, measured independently.** Stamped at `b8946403`; its `consumptionLaw` admits a descendant only across a DOCS-ONLY window. `b8946403` IS an ancestor of `9d851fae`, but the window is **233 paths of which 192 are outside `docs/`**. WF-1A's verdict reproduces; nothing was inherited from it |
| 2 | packet status | DRAFT as written; promoted in-lane |
| 3 | target files clean | `git status --porcelain` empty in a fresh worktree; no foreign dirt |
| 4 | §14 symbols resolve | **20 of 20**, each checked as the exact substring the manifest would use |
| 5 | `validate:packets` at the tip | `valid: 129 packets (0 READY)`, TRUE_EXIT=**0**; collision check re-run: **zero** non-terminal rows, so no other packet reserves either target |
| 6 | eslint `Linter`, `max-lines {skipBlankLines, skipComments}` | `religionState.js` **356/800**, `religiousContest.js` **468/800**, and for the record `peaceTerms.js` **797**, `warTermination.js` **818/818**. All four match the packet exactly |
| 7 | lighting tuple, re-read live | `2484/364/2120/20598/5767` at `:4848` |
| 7 | `.prose-numerics-baseline.json` | six rows keyed to `religiousContest.js` at **894** and **926**, three categories each |
| 8 | own-footprint golden | captured on deity-BEARING fixtures built through `SET_PRIMARY_DEITY` / `IMPOSE_CULT` — see §5 |
| 9 | A1/A3/A5/A7 fixtures RUN and PRINTED | see §3 — and one refuted a pin reading alone would have written |

## 3 · THE FIXTURES, RUN AND PRINTED BEFORE ANY PIN WAS WRITTEN

Executed at the base, in the blob tree, before the first edit
(`laneTEWF1B-probe-base.log`):

```
nicheOf(bare) = "neutral:neutral"
PUSH_MARGIN = 1.1 | claim = 0.9*100 = 90 vs 60*1.1 = 66
A1 nicheOf-built  : {"entered":true,"path":"same_niche_pushout","evicted":"d.A"} A.suppressed=true  A.share=0  A.standing="cult"
A1 hand-authored  : {"entered":true,"path":"open_slot","evicted":null}           A.suppressed=false A.share=60 A.standing="patron"
tick 1 owned=true siegeRef=d.B siegeTicks=1 patron=d.A A.suppressed=false
tick 2 owned=true siegeRef=d.B siegeTicks=2 patron=d.A A.suppressed=false
tick 3 owned=true siegeRef=null siegeTicks=0 patron=d.B A.suppressed=true
A7 suppressed=3 -> deleted=[] return=undefined
A7 suppressed=4 -> deleted=["d.S4"] return=undefined
A7 suppressed=5 -> deleted=["d.S4","d.S5"] return=undefined
```

⛔⛔ **THE nicheOf HAZARD IS REAL AND REPRODUCES AT THIS TIP.** The hand-authored arm enters
through `open_slot`, evicts nothing, and leaves the incumbent unsuppressed — so a site-`:262` pin
built on that fixture passes GREEN having asserted nothing. **A1 pins BOTH arms**, so the control
is part of the case rather than a comment about it.

⭐ **The siege owns the seat on ticks 1 and 2 while suppressing nothing.** A3 asserts all three
rows as one exact array, so the two quiet ticks are a genuine negative rather than missing setup.

**Sub-key order, measured rather than reasoned:** the record's live key order is
`deityRef, snapshot, niche, share, standing, suppressed, legitimacy, tenure`. The writer's spread
re-uses `suppressed`/`share`/`standing` in place, so `suppressedAtTick` appends **last** — A6 pins
it with `Object.keys(...).at(-1)`.

**`advanceShares` returned `undefined` at the base** — P-4 confirmed: no caller reads it, so the
return is a widening and not a change.

## 4 · PER-ACCEPTANCE-CASE RESULT

All eight pass. `tests/domain/patronFall.test.js`: **16 passed (16)** — WF-1a's eight plus these
eight — TRUE_EXIT=**0**.

| case | what it proves | result |
|---|---|---|
| A1 | site `:262` stamps at the fold's tick; the hand-authored-niche fixture is asserted to be vacuous; and DARK the same push-out suppresses and stamps nothing | **PASS** |
| A2 | absent ≡ false at the byte level on a SUPPRESSING deity-bearing world; the LITERAL lit drive materializes the stamp; non-vacuity carried by two positives (the fixture holds faith AND the dark run really suppressed) | **PASS** |
| A3 | the siege's two pre-resolution ticks stamp nothing and the resolving tick stamps, as one exact three-row array | **PASS** |
| A4 | resurgence clears the stamp (`seed()` replaces wholesale); byte-level in both directions; and the creed displaced on the way back IS stamped, so the clear is scoped rather than a blanket failure to write | **PASS** |
| A5 | lit drops the longest-dormant (`d.S2`) where dark drops the codepoint-last (`d.S4`) on a fixture whose two orders DISAGREE; plus the legacy arm and a second arm for two-unstamped-entries | **PASS** |
| A6 | round-trip through `ensureWorldState` both arms; sub-key order pinned; the `{to:2}` migration preserves the stamp through `...rest`; zero new top-level ledger keys; the veil | **PASS** |
| A7 | the returned refs are exactly what was deleted; `[]` never `null`/`undefined`, including on the empty-keys arm; the bar derives from the landed `KEEP` | **PASS** |
| A8 | the write-site census in both directions, plus the affirmative spectacle-awareness scan with its own non-vacuity control | **PASS** (built to purpose — see §8, J-TEWF1B-4) |

**Focused battery (packet §10):** `tests/domain/patronFall.test.js`, `religionState.test.js`,
`patronContest.test.js`, `religionTier2.test.js`, `religiousContest.test.js`,
`advanceReligionStates.test.js`, `religionDormancy.byteIdentity.test.js`, `pietyDynamics.test.js`
→ **8 files passed, 85 tests passed**, TRUE_EXIT=**0**.

**The out-of-`src` caller a `src`-only census misses:** `node scripts/audit/religion-balance.mjs`
→ TRUE_EXIT=**0**, tables render.

⭐ **M6 DISCHARGED AS VERIFY-ONLY.** `tests/domain/religionState.test.js` is **green, unedited** —
its six `attemptEntry` and three `advanceShares` call sites pass no tick and no opts. That is the
executed proof the threading is additive; it did not red, so nothing was edited.

## 5 · THE DARK FENCE, MEASURED AGAINST A PRE-WIRING GOLDEN

Captured BEFORE the first edit and re-run after, on deity-bearing fixtures built through the
doctrine path (the deity-free corpus golden is vacuous for faith and is **not** this fence):

| arm | base | after | verdict |
|---|---|---|---|
| organic absent 16t | `31cf589d2ec495d5` / 1666 B | `31cf589d2ec495d5` / 1666 B | **IDENTICAL** |
| organic false 16t | `31cf589d2ec495d5` / 1666 B | `31cf589d2ec495d5` / 1666 B | **IDENTICAL** |
| eviction absent 20t | `d2f035fac0a304d9` / 2049 B | `d2f035fac0a304d9` / 2049 B | **IDENTICAL** |
| schism absent 40t | `9ff314e35ebfb5c5` / 895 B | `9ff314e35ebfb5c5` / 895 B | **IDENTICAL** |
| organic LIT 16t | `fbceb7e0cd76fada` / 1740 B | `fbceb7e0cd76fada` / 1740 B | unchanged — no suppression fires in this fixture |
| **eviction LIT 20t** | `09f0e3a8bc303142` / 2122 B | **`9d51034ecfd42dc9` / 2143 B, stamp present** | **MOVED — the lit arm that suppresses** |
| schism LIT 40t | `926af81b5ad3431b` / 973 B | `926af81b5ad3431b` / 973 B | unchanged — see the finding in §9 |

**DECLARED SHIFT: NOT INCURRED.** No preset declares `faithUnseatingEnabled`, so no same-seed
golden can reach the lit path. The only motion is inside the flag, on a lane-built fixture.

## 6 · THE MUTANTS

Each planted by `cp`, convicted, restored, and the restore **proved by digest** — never by eye,
and never with the `git checkout` family. **The whole set was re-run after the §13.3 any-cast cure changed the file**, and every conviction
reproduced identically. `religionState.js` returns to `1ff2f3f9b637bba2c65bb4ceaf619d58` and
`religiousContest.js` to `c3533b73dc24426da1216d00406b55d8` after every plant; `cmp` reports
identical every time; the final check re-confirms both.

| mutant | §13 predicted | ACTUALLY convicted | verdict |
|---|---|---|---|
| **(a)** revert `:262` to a raw spread | A8 + the site's own case, nothing else | **A1, A2, A4, A8** (4 failed / 12 passed) | PASS. Both extras exercise `:262` — the eviction fixture's suppression IS that push-out, and A4's resurgence displaces through it. The prediction under-enumerated which arms reach the site; it did not find entanglement |
| **(b)** collapse the lit comparator to codepoint | A5 alone | **A5 alone** (1 failed / 15 passed) | PASS — and ⭐ **it did NOT red A7**, so §13's STOP (ordering entangled with the return seam) **does not fire** |
| **(c)** drop the `Number.isFinite` guard | A2 and A4 | **A1, A2, A8** (3 failed / 13 passed) | PASS on substance. A4 stays green because every stamp it drives is an explicit finite tick and the resurged record comes from `seed()`, which never reaches the writer. A1's and A2's DARK arms are the behavioural convictions |
| **(d)** return `null` from the empty-prune arm | A7 alone | **A7 alone** (1 failed / 15 passed) | PASS |
| **(e)** ⭐ **LANE-ADDED** — delete the fold's `unseating ? tick : null` fork | — | **A2 alone** (1 failed / 15 passed) | PASS |

⚠⚠ **BOTH §13 STOP CONDITIONS CHECKED, NEITHER FIRES.**

1. *"If (b) also reds A7, the return seam is entangled with the ordering."* It reds A5 alone.
2. *"If (a) AND (c) convict the same arms, one of the two guards is vacuous."* They do not:
   `(a) = {A1, A2, A4, A8}`, `(c) = {A1, A2, A8}` — A4 separates them.

⭐ **AND THE REDUNDANT-SECOND-GUARD HAZARD IS DISCHARGED IN BOTH DIRECTIONS, WHICH IS WHY MUTANT
(e) EXISTS.** §13 warns that the `Number.isFinite` guard and the fold's fork can EACH independently
hold dark byte-identity, so convicting one proves nothing if the other covers for it. Deleting the
**fork** reds A2; deleting the **guard** reds A1, A2 and A8. Each fails on its own. Neither is
decorative — proven by execution, not by reasoning about the code.

## 7 · ⛔⛔ THE TERMINAL IS NOT GATE-GREEN, AND THE RED IS INHERITED

### 7.1 What is red

`tests/docs/enforcement-claims.test.js` fails two arms, which reds gate step 15 (`test:ratchet`).
The offending claim is at **`docs/implementation/preambles/WF-PREAMBLE.md:461`** — the family
preamble's own ⚠⚠ warning about the naked-claim scan, which transcribes the banned vocabulary
while warning against transcribing it. The scanner's nil-count pattern matches inside that
sentence's own thirty-count example: precisely the hazard the sentence describes.

### 7.2 The four executed legs proving it is inherited

1. **It reproduces at the PRISTINE BASE.** A second detached worktree at `9d851fae`
   (`…/scratchpad/wf1b-baseproof`, `git rev-parse HEAD` =
   `9d851faeb7cac73217d508f621d1d9ee29f2c145`) carrying nothing of this lane's:
   `npx vitest run tests/docs/enforcement-claims.test.js` → **`2 failed | 19 passed (21)`**,
   TRUE_EXIT=**1**. Both arms were red before this member existed.
2. **This lane touched neither file.**
   `git diff 9d851fae --name-only -- docs/implementation/preambles/WF-PREAMBLE.md tests/docs/enforcement-claims.test.js`
   is **EMPTY**.
3. **This packet contributes ZERO claims.** The live `CLAIM_RE` over `WF-1B.md` returns **0 hits**,
   and `WF-1B.md` appears nowhere in the failure's seven-claim list (which names only
   `FABLE_VALIDATION_QUEUE.md`, `GOLDEN_SHIFT_LEDGER.md`, `IN-0C.md` and `WF-PREAMBLE.md`).
4. **One arm is banked and the other is not.** `scripts/.test-ratchet-baseline.json` banks
   *"every completeness claim carries an `@enforced-by` tag with ≥1 target"* (entry 5 of 11,
   `totalTests` 28274, `totalFiles` 2387) but **NOT** *"the banked naked-claim debt is frozen PER
   CLAIM — a seventh cannot hide inside it"*. The unbanked arm is what reds the ratchet, and in the
   real `&&` chain it takes `build` and `verify:dist` dark behind it.

**Classification: NOT a flake.** It is deterministic, reproduces at the base, and reproduces in
isolation. It is not the known `tests/components/npcAuthoringScope.test.jsx` flake, which did not
appear in any run of this lane.

### 7.3 Why this lane does not repair it

Three independent grounds, any one sufficient:

- **PACKET_STANDARD's gate law** says in terms: *"If a full gate is red, compare failure identities
  with a committed-base run or an integrity-counted archive. Do not repair unrelated rows."* The
  comparison is done; the repair is refused.
- **The preamble is outside the packet's closed §8 change manifest**, and the standard makes
  out-of-manifest editing a session-invalidating act.
- **It is chair-signed law** under ODQ §308. Amending the family preamble is a chair act, not a
  lane act.

**Banking the arm is refused outright** — *"a failing enforcement walker is never banked as
ordinary test debt"*, and this specific guard exists precisely so a seventh claim cannot hide
inside the six.

### 7.4 The cure, offered rather than made

One line, and it is the estate's own standing law (MX-1 and GR-4C both carry it): **paraphrase,
never transcribe**, because transcribing the vocabulary IS an occurrence. The preamble's sentence
needs exactly the treatment this packet's §6 row got at J-TEWF1B-3 — end it, for example,
*"…and never accept a nil-count match that is silently a thirty-count one."* Then re-run
`npx vitest run tests/docs/enforcement-claims.test.js` and confirm the per-claim arm goes green
while the banked arm stays red at its banked identity.

⚠ **This lane hit the same trap while writing §16.2 of the packet** — the first draft quoted both
spellings literally to explain the match. The pre-write scan caught it at one hit and it was
paraphrased. Recorded because explaining the trap is the most natural way to fall into it.

## 8 · THE FIVE LANDING JUDGMENT CALLS (all vetoable, all in the packet's §11)

- **J-TEWF1B-1 — the status row was split from its prose.** `parsePacketHeader` anchors the status
  at end-of-line, so `**Status:** DRAFT — compiled by …` leaves it unparsed and the manifest and
  Markdown disagree at every status. The prose moved to a `Compile note` bullet.
- **J-TEWF1B-2 — landed at `WF-1B.md`, and M7's self-referential path cell was re-addressed to
  match.** ODQ §309 made this a single member, so no `-II` sibling opens and the compile's `-I`
  suffix names a document that does not exist. A LANDED packet whose own change manifest names a
  path it does not occupy is a false record.
- **J-TEWF1B-3 — one banned phrase in §6 was paraphrased**, for the reason in §7.
- **J-TEWF1B-4 — A8's "both counts equal 3" is arithmetically impossible after the cure, so the
  pin was built to its PURPOSE.** Measured comment-stripped after routing: the raw idiom appears
  **once** (inside the sole writer — the whole point of routing) and `suppressDeity(` appears
  **four** times, one declaration and **three** call sites. The landed pin asserts raw writes
  EXACTLY 1, declarations EXACTLY 1, call sites EXACTLY 3, **plus an arm proving the one raw write
  sits INSIDE `suppressDeity`** — without which an inline write could replace the helper's own and
  no count would move. A fourth inline write reds the first; a fourth routed site reds the third.
- **J-TEWF1B-5 (implicit, recorded here) — §2.4's "takes it to two by-name reads" was not
  buildable alongside M2's instruction that `:823` reads the local; M2 governs.** After the hoist
  there is exactly **one** by-name read. Measured: `engineGatedRuleKeys.walker.test.js` keys on
  key *presence* (`manifestWithoutRead` / `unaccountedReads` are SETS of key names), never on a
  count of read sites, so one read and two are equally green. The §2.4 sentence is a prose defect,
  not a build defect. Walker green.

## 9 · FINDINGS THE COMPILE COULD NOT HAVE HAD

- ⭐ **THE SCHISM FIXTURE RESOLVES A SIEGE WITHOUT SUPPRESSING ANYTHING.** WF-1a's `schism()`
  seats two cults in DIFFERENT niches, so the contest is ORGANIC and the resolution loop suppresses
  only *same-niche* rivals of the winner — of which there are none. Executed: the lit schism drive
  is byte-identical before and after this member. **A lane reaching for `schism()` to prove the
  `:485` site would write a silently vacuous pin.** A3 uses a direct `resolvePatronContest` fixture
  whose two creeds SHARE a niche.
- ⚠ **A DOCBLOCK IS POSITIONAL; MOVING A FUNCTION UNDER ONE RE-PARENTS IT SILENTLY.** Placing
  `suppressDeity` directly above `export function attemptEntry` orphaned `attemptEntry`'s JSDoc
  onto the new function. `typecheck:domain:strict` caught it as **9** new errors against a per-file
  baseline of **0** (`TS7006` implicit-any on all three positional parameters; `TS2339` on every
  `opts.force` / `opts.tick` read). ⛔ **`typecheck:ratchet` stayed GREEN throughout** — the
  two-typechecker receipt law earning its keep, and a per-file `tsc` would have shown nothing.
- ⚠ **THE INDEX STATUS COLUMN IS READ LITERALLY, AND `PACKET_STATUSES` IS SCANNED IN ARRAY ORDER**
  (`BLOCKED, DRAFT, LANDED, READY, STALE, SUPERSEDED`), FIRST MATCH WINNING. A status cell reading
  *"promoted from draft in-lane"* let the lowercase `draft` out-rank the bolded `READY` beside it:
  `validate:packets` → `WF-1B status disagrees with index: manifest=READY index=DRAFT`,
  TRUE_EXIT=1. Cured by keeping every status word out of the cell's prose.
- **`advanceShares`'s five call sites are all bare expression statements**, re-confirmed at this
  tip — so the widened return is a widening and not a change (P-4 holds).

## 10 · CENSUS ARITHMETIC — IT CLOSES EXACTLY

**`2,484 / 364 / 2,120 / 20,598 / 5,767` → `2,484 / 364 / 2,120 / 20,606 / 5,767`**

| figure | before | after | delta | why |
|---|---:|---:|---:|---|
| files | 2484 | 2484 | **+0** | the member mints NO test file |
| parked | 364 | 364 | **+0** | no parked-file rule changed |
| credited | 2120 | 2120 | **+0** | `patronFall.test.js` was already credited |
| **titles** | 20598 | **20606** | **+8** | the eight acceptance cases, one `it` each |
| suiteTitles | 5767 | 5767 | **+0** | straight-line `it` calls in the ONE existing `describe` — no new `describe`, no `.each`, no `runIf`, no nesting |

**Attribution is exact: 8 cases → 8 titles, in one file.** The sequenced walker stopped at the
titles figure (`expected 20606 to be 20598`), which is itself the executed proof that `files`,
`parked` and `credited` did **not** move — a sequenced census cannot reach a later figure past a
red earlier one. After re-recording the tuple WHOLE, the walker passes **33/33**, TRUE_EXIT=0,
which reaches and clears `suiteTitles`.

⚠ **The parked-file swallow was checked, not assumed:** the delta equals the number of titles
added, so no title was swallowed by a parked classification.

**Authorization for the census move:** ODQ **§308.4** (this compile's dispatch authority) and
**§309** (the single-member ruling), named in the packet's §6 census row and restated in the
re-record comment block.

## 11 · ADDRESS ROT PAID, NOT DELETED

M2's insertion moved the two receipt lines in `religiousContest.js`, so the six
`.prose-numerics-baseline.json` rows keyed to it rotted. **RE-ADDRESSED: 894 → 909 and 926 → 941,
three categories each** (`floatInterpolation`, `multiplier`, `twoDecimalScore`). The diff is
**six `line` fields and nothing else** (6 added, 6 removed); no row was deleted and the file was
never re-serialized. `tests/lint/proseNumerics.test.js` → **29 passed**, TRUE_EXIT=**0**.

## 12 · OBLIGATIONS RE-VERIFIED AT THE TIP

| obligation | verdict at the tip |
|---|---|
| §49/§50/§148 flag mint | **NOT INCURRED** — no flag minted; `ENGINE_GATED_VIRTUAL_RULE_KEYS` untouched; no certification row; `subsystemRowsVirtual` and `coveringArrayCoverage` literals unmoved (both green in the gate) |
| §104.4 edge-shared closures | **NOT INCURRED** — neither changed file appears in any of the five committed metas' `inputs`; `validate:edge` TRUE_EXIT=0 |
| test census | **INCURRED, TITLES ONLY** — §10 above |
| §102.2 / §P3b mutation coverage | **NOT INCURRED** — no `tests/lint/**` file added |
| §85.4 seeded chooser | **NOT INCURRED** — zero new PRNG streams; `rng.fork('religion-contest::…')` untouched |
| size baseline / hot files | **NOT INCURRED** — `religionState.js` 356 → **368**, `religiousContest.js` 468 → **470**, both far under 800; neither has a `.size-baseline.json` entry; no hot file named |
| `negativeAssertionAnchor` | **INCURRED, ceiling zero, DISCHARGED** — every `not.toContain` added carries an `// anchored:` line; walker green |
| coupling registry, BOTH halves | **NOT INCURRED** — no module created, no import edge in `src`; `couplingInclusion.walker.test.js` green, both ceilings still |
| hand-keyed address rot | **INCURRED, PAID** — §11 above |
| declared shift | **NOT INCURRED** — §5 above |
| chair value signature | **NOT INCURRED** — the member authors no number; the bar derives from the landed `KEEP`; a literal bar value is spelled nowhere |
| `docs/**.md` naked-claim debt | **INCURRED, PAID** — packet scans to 0 hits; the residual red is the preamble's, §7 |

## 13 · THE GATE

Run **BARE** in the lane's own worktree, **never** through `gate-mutex` from outside, each step
separately with its own in-shell `TRUE_EXIT` captured into its own self-named log — so a red step
does **not** black out the steps after it, which the real `&&` chain would.

Run at the final tip **`4f25da23`**, started `16:35:48Z`, finished `16:51:59Z`.

| # | step | TRUE_EXIT | note |
|---:|---|---:|---|
| 1 | `validate:hazard-registry` | **0** | |
| 2 | `validate:premortem` | **0** | |
| 3 | `validate:packets` | **0** | `valid: 130 packets (0 READY)` |
| 4 | `validate:data` | **0** | |
| 5 | `validate:custom-content-manifest` | **0** | |
| 6 | `validate:migration-head` | **0** | |
| 7 | `validate:edge` | **0** | the edge-shared closures are untouched — §104.4 NOT INCURRED, confirmed by the gate rather than by reading the metas |
| 8 | `validate:map` | **0** | |
| 9 | `validate:tuning-bands` | **0** | the member authors no number; no band moves |
| 10 | `validate:foundry-module` | **0** | |
| 11 | `validate:mcp-server` | **0** | |
| 12 | `typecheck:ratchet` | **0** | `tsconfig.full.json` — **173 errors, ceiling 173**, exactly at floor |
| 13 | `typecheck:domain:strict` | **0** | `tsconfig.domain-strict.json` — **1134 errors, ceiling 1134**, exactly at floor |
| 14 | `lint` | **0** | 29 problems, **0 errors**, 29 warnings |
| 15 | **`test:ratchet`** | **1** | ⛔ **the only red — see below; the deterministic half is INHERITED** |
| 16 | `build` | **0** | 314 static route documents prerendered |
| 17 | `verify:dist` | **0** | `STRICT DIST OK — 51 file(s), 409 test(s), zero failed/non-run/uncollected/missing/extra/duplicate` |
| — | **`smoke:boot`** (outside the chain) | **0** | `524/524 chunks initialised`, shell mounted with 31,706 B of markup, **PASS — the built bundle boots** |

⭐ **Steps 16 and 17 and `smoke:boot` all RAN AND PASSED.** In the real `&&` chain they would have
been dark behind the red at step 15; running each step independently is what makes that reportable.

### 13.1 The step-15 identity comparison — EXECUTED at both ends

| | BASE `9d851fae` (pristine worktree) | TIP `4f25da23` |
|---|---|---|
| `test:ratchet` TRUE_EXIT | **1** | **1** |
| failures NOT in the frozen census | **1** | **2** |
| `enforcement-claims :: the banked naked-claim debt is frozen PER CLAIM` | **PRESENT** | **PRESENT** — identical identity |
| `npcAuthoringScope.test.jsx :: … editMode and the caller capability are stale` | absent | present — **FLAKE**, see 13.2 |
| `domainAnyCastBaseline` (3 arms) | absent | absent — **was mine, is cured** (13.3) |

⇒ **THE DETERMINISTIC RED SET IS IDENTICAL AT BOTH ENDS. This member introduces ZERO new
deterministic failures.** The frozen census is 11, measured at `4deb4f026644cba5`, at both ends.

⛔ **The base itself is not gate-green.** `npm run check` reds at step 15 at `9d851fae` too, so no
green terminal was reachable from this base by any lane not editing chair-signed law.

### 13.2 `npcAuthoringScope.test.jsx` — MACHINE/FLAKE, with its four legs

1. **It is the NAMED known flake** for this program, and it is deliberately **not banked** into the
   census — the CG-1 precedent.
2. **It passes in isolation, three times:** `16 passed (16)`, TRUE_EXIT=**0** on every run.
3. **It did not reproduce twice.** It was **ABSENT** from the earlier full `test:ratchet` at
   `39d0ad7c` (which showed the enforcement-claims arm plus three `domainAnyCastBaseline` arms) and
   present only in the run at `4f25da23`. It is absent at the base.
4. **It cannot be reached by this member.** It is a React component test about dossier authoring
   scope; this packet touches no component, no `.jsx`, no dossier surface, and no read model. Its
   change manifest names zero component paths.

### 13.3 `domainAnyCastBaseline` — MINE, FOUND BY THE FULL GATE, CURED

⭐ **This is the one the focused battery could never have caught, and it is why the whole gate is
run rather than sampled.** The sole writer's `@param {any} state` pushed `religionState.js` to
**23** any-holes against a frozen, MONOTONE-DOWN per-file allowance of **22**. The ratchet's own
message forecloses the easy exits in terms: widening the baseline is not the cure, and neither is
a `DECLARED_OVERRUN` row, because both ledger ceilings are monotone-down literals and a new row
only moves the red to the ceiling arm.

**Cured by typing the parameter**, and the first cure was itself wrong in a way only the STRICT
typechecker saw: a spread of an indexed record does **not** carry its index signature onto the
result, so the conditional stamp became a `TS2339` — one new strict error against a per-file
baseline of 0, **while the any-cast ratchet had already gone green and `typecheck:ratchet` never
moved at all**. The local `/** @type {Record<string, unknown>} */` restores the index signature.
Final counts: `religionState.js` **22 = allowance 22**, `religiousContest.js` **45 = allowance 45**.

**Everything was re-proved after that cure**, not assumed: all five mutants re-convict with the
identical conviction sets; all four dark golden arms are still byte-identical to the base; both
typecheckers sit at their exact floors; effective lines unchanged at **368** and **470** (the
annotation and its note are comments, and the measurement skips comments).

### 13.4 ⚠⚠ THE WRAPPER LIED TWICE, LIVE, ON THIS LANE'S OWN RUNS

The harness reported the backgrounded `test:ratchet` as **"completed (exit code 0)"** while the
in-shell `TRUE_EXIT` written into the log was **1** — and reported the whole 17-step gate script as
**"exit code 0"** while step 15 inside it was **1**. Both times the failure existed only in the
log. This is the recorded hazard reproducing exactly: **never believe an exit you did not capture
in-shell.** Every figure in this receipt comes from a self-named log with its own captured
`TRUE_EXIT`.

## 14 · RAISED

1. ⛔⛔ **`claude/composite-r4 @ 9d851fae` IS ALREADY GATE-RED, AND THE CAUSE IS THE §308 PREAMBLE
   LANDING ITSELF.** `docs/implementation/preambles/WF-PREAMBLE.md:461` transcribes the banned
   naked-claim vocabulary inside its own warning against transcribing it, minting an untagged claim
   under a key frozen at zero and reddening a test that passes by design. Proven at the pristine
   base with a full `test:ratchet` (1 failure not in census, TRUE_EXIT=1). **This blocks a green
   terminal for every WF lane, not just this one.** The cure is one paraphrased line in a
   chair-signed file — a chair act, written out at packet §16.2 and receipt §7.4. **This is the
   single highest-value thing the chair can do before the next WF member.**
2. **§2.4 of the packet says the member "takes the by-name reads to two"; M2's instruction row says
   `:823` reads the hoisted local. Both cannot hold.** M2 governs and was followed, so there is
   exactly **one** by-name read after the hoist. Measured: `engineGatedRuleKeys.walker.test.js`
   keys on key *presence* (`manifestWithoutRead` / `unaccountedReads` are SETS of key names), never
   on a count of read sites, so one and two are equally green — the walker passed in the full gate.
   **The §2.4 sentence is a prose defect in a LANDED packet.** Worth a chair correction so a later
   WF member does not "restore" a second read to satisfy it.
3. **A8's specified "both counts equal 3" is arithmetically impossible after the cure** — see
   J-TEWF1B-4. The pin was built to the volume's stated purpose instead. If the chair wants the
   literal arithmetic, the single-writer routing would have to be undone, which is the wave.
4. ⚠ **A STALE `index.lock` SITS IN THE SHARED MAIN WORKTREE:**
   `/Users/cstokes/Desktop/settlement-engine/.git/index.lock`, dated **Aug 19 23:56**, zero bytes.
   It did not affect this lane (a linked worktree carries its own index under
   `.git/worktrees/<name>/index`), but it will block any `git add` run in the MAIN worktree. It is
   foreign and this lane did not touch it. Someone should confirm no process owns it and clear it.
5. **The WF-1a `schism()` fixture cannot prove the `:485` siege site** — it resolves a siege while
   suppressing nothing, because its two cults sit in different niches. Recorded at packet §16.3 so
   WF-1c/WF-1d do not reach for it and write a green, empty pin.
6. **`suppressedAtTick` now exists with a producer and no production reader.** That is this
   member's declared producer-first seam and `pruneSuppressed`'s return is asserted at A7 — but the
   WF-8 beat member is the only chartered consumer. If WF-8 slips, the field is a persisted key
   whose only reader is a prune-order decision, which is exactly the shape WF-1a refused to mint at
   the field level. Not a defect; a dependency worth tracking.
7. **DS-FTH-3 (volume Q4) remains OPEN at the chair.** This member renders `suppressedAtTick` to no
   reader surface, so no corpus row was amended and `gen:dossier-prose` was not run (J-TCWF1B-4).
   The first WF member that RENDERS the stamp inherits that question unresolved.

## 15 · WHAT THE CHAIR SHOULD DO

1. **CAS `claude/composite-r4` to `4f25da23dcb72d77b9fe53a3b3a4318f14b3392e`.** The tree is clean,
   the packet is LANDED and validating, and the deterministic red set is identical to the base's.
2. **Then cure `WF-PREAMBLE.md:461`** (RAISED-1). It is one line and it unblocks the gate for the
   whole family.
3. Optionally rule on RAISED-2 and RAISED-3, both of which are prose corrections to a LANDED packet
   rather than build changes.

**Both worktrees are left in place for verification and neither was removed:**
`…/scratchpad/wf1b-tree` (the lane's, at the tip) and `…/scratchpad/wf1b-baseproof` (pristine
`9d851fae`, whose `node_modules` is a symlink into the lane's own tree).
