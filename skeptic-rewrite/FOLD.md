# THE FOLD — skeptic pass over REWRITE car 8a (lanes' docks at `5c7eadb18` over `29ec62425`)

Seat: Opus 5 — Fable-unvalidated (the FOLDER). Read-only everywhere; this file is the only write.
Every figure below is carried from a named lens file or from the receipt's CAR 8a section; the
one figure this fold derives itself (FOLD-NEW-1) is labelled FOLD-DERIVED and names both sources.

**CONFIRMED 48 · REFUTED 1 · PARTLY 2 · UNTESTED 3 (1 live, 2 discharged by sibling lenses) · NEW 14 (5 MEDIUM · 9 LOW) — porcelain: skepRW1 0 -> 0 · skepRW2 0 -> 0 · skepRW3 0 -> 0 · laneLIGHT 0 -> 0 (read-only base dock)**

Per lens: `kernel-shift-register.md` 16 CONFIRMED · 1 PARTLY · 4 NEW · porcelain 0 -> 0.
`instruments-and-gate.md` 13 CONFIRMED · 7 NEW · porcelain 0 -> 0.
`fences-registers-receipt.md` 19 CONFIRMED · 1 REFUTED · 1 PARTLY · 3 UNTESTED · 2 NEW · porcelain 0 -> 0.
Fold-derived: 1 NEW. Runners were 0 in their own shell before every vitest in all three docks;
every plant was restored with `git show HEAD:<path> > <path>`, never `git checkout --`; no commit,
stash, amend, rebase, reset, `--write` register act or golden re-record was run anywhere.

---

## 1 · EVERY REFUTED, PARTLY AND NEW VERDICT

| # | claim | figure | severity | lens file |
|---|---|---|---|---|
| **R-1 REFUTED** | "Every configuration of the DRIFT corpus carries the same `_seed` (`golden-master-v3`) … one pool has ONE drawn variant across all 525 towns", and the remedy sentence routing a widening to a CAPACITY car | DRIFT carries **four** seeds: `golden-master-v3` 72,108 cells / 516 towns · `gm-seed-a` 394 / 3 · `gm-seed-b` 390 / 3 · `gm-seed-c` 392 / 3. Deliberate: `tests/helpers/goldenMasterCorpus.js:116-121`. Substance survives (516 of 525 do share one seed; the 345 -> 309 / 36 explanation is unaffected); the universal quantifier and the remedy do not | **MEDIUM** | fences §19 |
| **P-1 PARTLY** | "the contract test RECOMPUTES the RE-INDEXED count (plant a wrong count: the test reds by name)" | An INCONSISTENT plant (43685 -> 43686) reds by name (`expected 73285 to be 73284`, 1 failed / 76 passed). A CONSISTENT plant (RE-INDEXED 40000 · UNCHANGED 33284 · dm 20000 · player 20000) passes **77/77** and prints `RE-INDEXED 40000 54.58 %` to the owner as the veto surface. The arm holds internal arithmetic; it never recomputes. The RECEIPT does not overclaim ("declared rather than pinned"); the LENS's wording is refuted | **LOW** | kernel (d-2) |
| **P-2 PARTLY** | the brief's "the SHIFT REGISTER … with its TWO new rows" | base **14** mechanisms, tip **15**; the only NEW id is `passage-shape`; `draw-formula` was REWRITTEN in place with a `reIndexed` block. Receipt says so plainly at 8a-1 ("Row rewritten") — a ledger row repeating the brief would be wrong | **LOW** | fences §10 |
| **N-1 NEW** | the `> 0` regression is GREEN on both named acceptances | with `vid <= 0` planted in `stableVid`: `tests/domain/stateProseKernel.test.js` **37 passed**, contract test **77 passed**, while the seven canonical-led pools silently revert to the modulus. The sweep re-derives the id-less predicate over the leaves and never calls `stableVid`. The only moving instrument (`dossierProseManifest`, 1,050 rows) is ALREADY red on the clean tree in the same two arms with the same 1,050 rows (chartered) — saturated, cannot discriminate | **MEDIUM** | kernel NEW-1 |
| **N-2 NEW** | the connective lists have two homes and now disagree | leaf `src/data/dossierConnectives.generated.js` 3/3/3/3 vs the composer's `CONNECTIVES` constant in `composeStateProse.js`: addition.sentence 1 `[""]` · contrast.sentence 1 `[""]` · tension.sentence 0 · consequence.clause 0. The report prints the WITHHELD refusal naming THE LEAF **408 times** on the taste corpus, and its parenthetical names 8a item 9 as the cure for a condition 8a item 9 already ended. Nothing rendered moves (0 of 708 pools are modifiers) | **MEDIUM** | kernel NEW-2 |
| **N-3 NEW** | two `unitsOfPool` implementations, no arm asserting they agree | `scripts/lib/prose-composed-units.mjs:39` and `scripts/prose-wave-gate.mjs:505`. Executed divergence on shipped input: `DS-DEF-11 :: WALLED-STRAINED` gate **2** / lib **0**; `WALLED-QUIET` **3** / **0**; `DS-DEF-2 :: Disasters & Famine: granary AND hospital` **3** / **0**. The lib's docblock names the gate as its consumer; the only importer is `scripts/prose-shape-report.mjs:32`. Both feed the same sitting | **MEDIUM** | gate NEW-1 |
| **N-4 NEW** | the gate CLOBBERS a shared packet on a bare, read-only-looking run | `PACKETS = path.join(path.resolve(ROOT,'..'),'taste')` resolves to ONE shared directory for every dock cut under the chair's scratchpad; `:1972` writes `measure-<arm>.json` with no `--out`, no env override, no dry flag, no refusal on an existing file, `--arm` defaulting to `draft`. It bit the skeptic: laneTASTE's live round-4 `measure-draft.json` went **169,324 B -> 17,245 B**, restored from `measure-draft-prev.json` (`cmp` clean, `"arm":"draft","round":4,"at":"2026-09-09T05:58:55"`). 8b runs arms A and B concurrently by design | **MEDIUM** | gate NEW-2 |
| **N-5 NEW** | the false seed sentence is committed in the ESTATE, not only in the receipt | `tests/property/dossierProseManifest.test.js:310-312` carries the R-1 sentence as an inherited comment, introduced with "recorded here so the next reader inherits the finding instead of re-deriving it" — which makes the error load-bearing. Cure is one sentence; touches no mechanism | **MEDIUM** | fences §21 |
| **N-6 NEW** | the owner's veto surface is an unguarded hand-typed number | see P-1. Declared figure is CORRECT today (43,685 re-derived from two independent runs of the classifier); nothing machine stands over it | **LOW** | kernel NEW-3 |
| **N-7 NEW** | the accepted 4.5 SE per-pool ceiling has thin headroom | over six seed families the deepest per-pool deviation reads **3.27 · 3.43 · 3.57 · 3.57 · 3.85 · 4.42 SE**. Margin on the pinned family 1.07 SE; one of six alternative families came within 0.08 SE. Not flaky today (10,000 fixed literal seeds); a re-seed is roughly a one-in-sixty firing | **LOW** | kernel NEW-4 |
| **N-8 NEW** | `prose-shape-report.mjs:205` still prints a sentence 8a-5 refuted | the OWED line asserts "the band position is a CONSTANT at the face grain (SITTING §T.5), so a band column printed here would be the same tuple on every row". 8a-5 measured the face grain MOVING on lexicon (em-dash face `exceeded 1 · share 0.125 · mean 1.410` vs plain `0 · 0.259`). The sitting reads this line through `--shapes` | **LOW** | gate NEW-3 |
| **N-9 NEW** | the rename's provenance record no longer verifies | `scripts/mutation-coverage-manifest.json:2635` reads "scripts/prose-wave-gate.mjs became scripts/prose-wave-gate.mjs in the same commit, never aliased" — a blanket rename rewrote the OLD name. Its md5 claim `cb1a88f2c9e8a0d602c380485958593e` matches neither the current file (`3467b2a2dc84d40c18e7b890c935ff76`) nor laneTASTE's `taste-measure.mjs` (`9c614db18ff640d9528b89bbf29d8235`) | **LOW** | gate NEW-4 · fences §22 |
| **N-10 NEW** | the gate's DEFAULT roster composes zero units | `poolRosterOf({section:null, pools:null})` returns the taste's seven names with `why: "no --section and no --pools: the taste's seven"`, and those seven compose **0 units** in this tree. 8a-5's headline cure ("the gate had no subject") holds only when `--section` or `--pools` is passed. Honest (`absentPoolRow` reports NOT-EXECUTABLE), but 8b must never call the gate bare | **LOW** | gate NEW-5 |
| **N-11 NEW** | duplicated paragraph in the gate's header | `scripts/prose-wave-gate.mjs` lines 12-16 and 18-22 are the identical "WHAT IT IS. The writing workflow calls this after EVERY round…" paragraph, twice, in the file the wave's writers read first | **LOW** | gate NEW-6 |
| **N-12 NEW** | the receipt's "23 files taken" is 22 | `83e8acf17` changes 23 files; deduplicating the receipt's own TAKEN column across the eleven picks gives **22 distinct taken files**. The 23rd is `docs/content/wiring-census.json`, the census RE-TAKE the receipt declares separately in its next section. The refused count (15 distinct) checks out | **LOW** | gate NEW-7 |
| **N-13 NEW** | a rationale sentence that says a file became itself | the same `mutation-coverage-manifest.json` entry as N-9, stated from the fences lens: the FACT is true and verified (`scripts/taste-measure.mjs` absent at the tip, `scripts/prose-wave-gate.mjs` present — one implementation, never two); only the sentence is broken | **LOW** | fences §22 |
| **N-14 NEW (FOLD-DERIVED)** | the two-length plant's CORPUS-grain triple does not agree between the receipt and the independent probe, and neither print names its corpus | receipt §8a-5(a): short 12/13 · share 0.923 · mean **0.533**; long **11/13** · share 0.846 · mean **0.500**. Independent probe (`$MY/probeBand.mjs`): short 12/13 · 0.923 · mean **0.819**; long **10/13** · 0.769 · mean **0.627**. The exceeded/share/mean triple is a property of the surrounding 4-sentence corpus, which neither print names, so the two are not comparable and one of them will be carried into the ledger wrong. The PROPERTY ("THEY DIFFER") and the FACE-grain figures (0 · 0.259 both faces; em-dash face 1 · 0.125 · 1.410) agree exactly | **LOW** | receipt §8a-5 vs gate CLAIM 5 |

No mechanism defect landed by car 8a survives this fold. R-1 and N-5 are the same false sentence
in two homes; N-1 through N-4 are the four the chair should rule on before §919 and before 8b.

---

## 2 · THE CURES OWED BEFORE §919

### (a) CODE — nine, each a change to a named file with the arm that proves it

1. **`tests/domain/stateProseKernel.test.js`** — the sweep must exercise the kernel's own guard, not
   re-derive the predicate over the leaves: export `stableVid` from
   `src/domain/display/stateProse/stateProseKernel.js` and assert it on `vid: 0`, or drive
   `drawVariant` on a synthetic `vid: 0` pool and assert the draw differs from the modulus.
   **Arm that proves it:** re-plant `|| vid <= 0) return null;` in `stableVid` — the sweep must red
   by name. Today the plant reads 37 passed / 77 passed on both named acceptances. *(N-1, MEDIUM)*
2. **`scripts/prose-wave-gate.mjs:143-146, :1972`** — namespace the packet target by dock, accept
   `--out` / `$PACKETS`, and refuse to overwrite a packet whose `arm`/`round` header does not match
   the run. **Arm:** `tests/lint/proseWaveGate.walker.test.js` asserts two runs with different
   `--arm` in one tree write two distinct paths and that an existing foreign packet is refused.
   ⛔ **This one blocks 8b**, which runs arms A and B concurrently by design. *(N-4, MEDIUM)*
3. **`scripts/prose-wave-gate.mjs:505`** — consume `scripts/lib/prose-composed-units.mjs` (the
   bare-spine branch moved into the lib behind a flag), OR add the cross-check.
   **Arm:** an arm in `tests/lint/proseWaveGate.walker.test.js` drives both `unitsOfPool` exports over
   `poolRosterOf({section})` and asserts equal unit counts per pool. Today 2/0, 3/0, 3/0. *(N-3, MEDIUM)*
4. **`src/domain/prose/passageShapes.js`** (the `why` string and its source comment) and
   **`scripts/prose-shape-report.mjs`** (the refusal it prints 408 times) — the WITHHELD reason must
   name the COMPOSER'S constant, not the leaf, and 8a-9's parenthetical must go.
   **Arm:** an assertion that `CONNECTIVES` in `src/domain/display/stateProse/composeStateProse.js`
   and the lists in `src/data/dossierConnectives.generated.js` agree, or a DECLARED divergence row
   naming SEAM car 4 as the car that closes it. *(N-2, MEDIUM)*
5. **`tests/property/dossierProseManifest.test.js:310-312`** — replace the seed sentence with
   "516 of the 525 configurations share `golden-master-v3`; nine carry one of `gm-seed-a/b/c`".
   **Arm:** the four-seed tally re-derived from the cells file (72,108 / 394 / 390 / 392 cells;
   516 / 3 / 3 / 3 towns). *(R-1 + N-5, MEDIUM)*
6. **`scripts/prose-shape-report.mjs:205`** — the OWED line asserting the band position is a
   face-grain CONSTANT is refuted by 8a-5; re-word to the two grains.
   **Arm:** the `--shapes` output the sitting reads. *(N-8, LOW)*
7. **`scripts/mutation-coverage-manifest.json:2635`** — first name back to `scripts/taste-measure.mjs`;
   re-record the md5 or note that the file has moved on since the plant.
   **Arm:** `tests/lint/mutationCoverageManifest.test.js`. *(N-9 + N-13, LOW)*
8. **`scripts/prose-wave-gate.mjs` lines 12-22** — delete the duplicated "WHAT IT IS" paragraph.
   **Arm:** none needed; a read of the header the wave's writers open first. *(N-11, LOW)*
9. **`tests/data/dossierStateProseProjection.contract.test.js`** — give the RE-INDEXED block machine
   standing: commit a digest of the two cell files (or the diff's own JSON) beside the row and have
   the arm recompute against it. **Arm:** plant RE-INDEXED 40000 / UNCHANGED 33284 / dm 20000 /
   player 20000 — today that self-consistent falsification passes 77/77 and prints to the owner. *(N-6, LOW)*

⚠ Any of cures 1-9 that touches a `tests/lint` walker or a census-stamped file re-opens the three
rituals in the receipt's own three-commit shape: the wiring census (`--dry` rows/sections), the
lighting census (titles/credited), and the OSR freeze. Budget the commits, not just the edits.

### (b) RECEIPT — seven

1. §8a-3 — **"23 files taken" -> "22 distinct files taken"**, naming `docs/content/wiring-census.json`
   as the census re-take declared in the next section, not a file taken from a pick. *(N-12)*
2. §8a-1 and the CLOSE — strike "Every configuration of the DRIFT corpus carries the same `_seed`"
   and "ONE drawn variant across all 525 towns", and with them the remedy sentence
   ("Widening it needs the recorder's `--seeds` family rather than DRIFT, which is a car of its
   own"): DRIFT already carries nine off-seed configurations across three extra seeds. *(R-1)*
3. §8a-10 and any ledger row — **ONE new register row (`passage-shape`) plus `draw-formula`
   rewritten in place**, 14 -> 15 mechanisms; never the brief's "two new rows". *(P-2)*
4. §8a-5(a) — the corpus-grain plant table must NAME its three companion sentences, or drop the
   exceeded/share/mean triple and carry only "THEY DIFFER" plus the real-corpus discrimination
   (`WALLED-STRAINED` distinct tuples 2 of 2 faces vs `WALLED-QUIET` 1 of 3). *(N-14)*
5. §8a-5 — record that the gate's DEFAULT roster composes **0 units**, so 8b's workflow must always
   pass `--section` or `--pools`. *(N-10)*
6. §8a-1 — record that the accepted 4.5 SE per-pool ceiling has **1.07 SE** headroom on the pinned
   family and that one of six alternative families read **4.42 SE**; any car re-seeding that arm
   should expect it to fire. *(N-7)*
7. §8a-1 append-safety table — note that an independent seed family reads 273,629 / 820,425 /
   273,218 / 547,207 at the same 25.01 % / 74.99 % shares, so the ledger carries the SHARES and the
   PROPERTY (no read moves between two old wordings), never the raw counts as if they were pins.

### (c) RULINGS FOR THE CHAIR — seven

1. **The gate's packet path (cure a-2) before 8b.** 8b runs two arms concurrently; today they write
   one file. Does 8b block on the cure, or does 8b pass `--out` per arm as a workflow discipline? *(N-4)*
2. **The two `unitsOfPool` (cure a-3).** One implementation with a flag, or a cross-check arm? Both
   feed the same sitting, and the gate's own header names this exact hazard one file away. *(N-3)*
3. **Who carries the false seed sentence (cures a-5 / b-2)** — 8b or a standalone cure commit — and
   whether ADDENDUM 1 ruling 8's routing of a DRIFT widening to a CAPACITY-train car still stands
   now that DRIFT is measured to carry nine off-seed configurations. *(R-1)*
4. **Does the `> 0` guard need a green instrument before the freeze act (cure a-1),** or is the
   REWRITE's last car — which re-records the manifest fixture and un-saturates
   `dossierProseManifest` — itself the guard? Between now and that act there is no green instrument
   standing over the corpus's only draw-regime split. *(N-1)*
5. **Whose car wires the composer to the connectives leaf (cure a-4)** — SEAM car 4 as written, or a
   REWRITE cure — and what the sitting reads when it rules on shape 3, given the WITHHELD reason it
   will read is currently false about the leaf it names. *(N-2)*
6. **Does the owner's RE-INDEXED veto surface stand on an unguarded hand-typed number through §919
   (cure a-9)?** The figure is correct today, twice re-derived; the guard does not exist. *(N-6)*
7. **The four decisions the receipt already queued, untouched by this fold and still owed:** the
   exemplar budget of <= 1 citation per unit against a measured 0 over 786 sentences; the pen line
   vs the inline rendering, now printable on both fixture towns; the twelve connective joints as the
   lane's DRAFT (§13 row 27 makes the copy the owner's, signed at the walk); and ARCH §2.3 gaining
   `kin` as a modifier-only `poolMeta` field the composer reads.

---

## 3 · THE FIGURES THE LEDGER WILL CARRY

Each row is CONFIRMED (executed by a lens) or CORRECTED (the lens's figure supersedes the receipt's).

### The RE-INDEXED count and share — CONFIRMED (twice, independently)

    cells 73,284 · RE-INDEXED 43,685 = 59.61 % · towns 525
    REPLACED 0 · ADDITIVE 0 · WORDING-ONLY 0 · ADDED 0 · REMOVED 0 · indexOnly 0 · UNCHANGED 29,599
    dm 21,994 of 36,660 = 59.99 %   ·   player 21,691 of 36,624 = 59.23 %
    blocksTouched 40 of 40 · blocksUntouched 0
    deepest DS-GEN-5 and DS-STR-1 both 100.00 % · shallowest DS-GEN-18 0.47 %
    cells whose POOL changed: 0        recorder footprint drawAgrees: base 5,966 · tip 3,914

Re-derived by the kernel lens from its own `__SKEPTIC_MODULUS` plant and by the fences lens from a
base-dock run at `29ec62425`; the register's `draw-formula.reIndexed` block reads exactly these.

### Uniformity — CONFIRMED to the digit, plus five corroborating families

    547 three-variant pools x 10,000 fixed seeds = 5,470,000 reads
    pooled shares 33.312 % · 33.375 % · 33.313 %   (§N.2 reference 33.30 / 33.35 / 33.35)
    deepest pooled departure from one third 0.0413 pp
    chi-square df 2 = 4.201            (critical 5.991 @.05 · 9.210 @.01 · 13.816 @.001)
    deepest per-pool deviation 3.43 SE at economy :: DS-ECO-11 :: TERRAIN: Forest, over 1,641 shares
    five further families: chi² 0.180 · 0.330 · 0.754 · 2.086 · 2.783 ; deepest per-pool
                           3.27 · 3.57 · 3.57 · 3.85 · 4.42 SE
    ⚠ CARRY THE HEADROOM: the accepted 4.5 SE ceiling has 1.07 SE margin on the pinned family.

### Append-safety — CONFIRMED as a PROPERTY and as shares; counts are family-dependent

    547 three-variant pools x 2,000 seeds = 1,094,000 reads, both draws in one loop
    law 6 (argmax)   moved 273,643 = 25.01 %  ·  to the NEW wording 273,643 = 100.00 %  ·  between two OLD 0
    shipped % length moved 820,444 = 74.99 %  ·  to the NEW wording 273,492 = 33.33 %   ·  between two OLD 546,952
    independent family (skeptic's own spelling of the modulus): 273,629 / 820,425 / 273,218 / 547,207
                                                                 at the same 25.01 % / 74.99 %
    THE ARM'S CLAIM: not one read moves between two old wordings. Carry the property and the shares.

### The files taken and refused per cherry-pick — CORRECTED

    83e8acf17 changes 23 files, +4032 / -98, over eleven source picks
    CORRECTED: 22 distinct files TAKEN (the receipt says 23); the 23rd file in the commit is
               docs/content/wiring-census.json — the census RE-TAKE declared in the next section
    CONFIRMED: 15 distinct files REFUSED, every one byte-identical to 29ec62425 at HEAD, and the
               two refused walkers ABSENT at HEAD (proseTasteAnnex, proseTasteCandidates)
    CONFIRMED: every brief-named instrument PRESENT at HEAD (12 named); scripts/taste-measure.mjs
               ABSENT because 8a-5 RENAMED it — one implementation, never two (e26ad7838 records
               both moves as renames)
    CONFIRMED: the seven typed annex lines are NOT in RECEIPT_POOLS_DOSSIER_STATE.md — all seven
               pool keys grep 0, TO-AUTHOR 0, [plain] 0, ROLE:…modifier 0; the file's only hunk in
               the whole consist is §7b at line 6337 (8a-9's connectives block)
    CONFIRMED: the census re-take is 6 insertions / 1 deletion — totals.modifierRows 0 plus a new
               modifiers section carrying its ruling and rows: []

### The gate's corrected band grain on the two-length plant — CONFIRMED, one triple CORRECTED

    metric partition: RATE 21 = TEXT 13 + FACE 8 · disjoint true · sum == RATE true
    OLD PATH (bandPositionOf, all 21) — 8-word face and 16-word face read IDENTICALLY:
        scored 21 · exceeded 13 · share 0.619 · mean 0.486
        deepest wordsPerSentence.neighbourVariation 1.597 under        (SITTING §T.5, to the digit)
    FACE GRAIN (8 word-level): both faces exceeded 0 · mean 0.259 — alike BECAUSE the lexicon agrees
        the same face with an EM DASH: exceeded 1 · share 0.125 · mean 1.410
        deepest punctuation.emDashRate 8.69 over                       -> the grain MOVES
    CORPUS GRAIN (13 text-level, each face inside its own 4-sentence corpus): THEY DIFFER
        ⚠ CORRECTED/UNSETTLED: receipt short 12/13 · 0.923 · mean 0.533 ; long 11/13 · 0.846 · 0.500
                               probe   short 12/13 · 0.923 · mean 0.819 ; long 10/13 · 0.769 · 0.627
        The triple is corpus-dependent and neither print names its three companion sentences.
        CARRY: "THEY DIFFER" + one named corpus, never the bare triple.
    REAL-CORPUS DISCRIMINATION (CONFIRMED): DS-DEF-11 :: WALLED-STRAINED distinct tuples 2 of 2
        face(s) ; WALLED-QUIET 1 of 3
    NEGATIVE CONTROL (CONFIRMED): bandPositionAt reverted to RATE_METRICS reds by name —
        "⭐ THE PLANT: two faces of different length now read DIFFERENT text-grain figures —
         expected 21 to be 13", 1 failed / 40 passed

### The distance, exemplar-rate and fixture prints — CONFIRMED

    siblingSpreadOf, three planted face sets:
      two near-identical faces  pairs 1 · min/median/max overlap 8000 bp · sameOpenerPairs 1 ·
                                sameSegmentPairs 1 · nearest {overlapBp 8000, sameOpener true}
      two unrelated faces       pairs 1 · overlap 0 bp · sameOpenerPairs 0 · sameSegmentPairs 1 ·
                                nearest {overlapBp 0, sameOpener false}
      one face                  pairs 0 · every overlap null ·
                                why "NOT-EXECUTABLE: a distance needs two faces and this variant carries 1"
      it sits beside A5's swap count on the printed line "sibling distance: #1 1 faces, 0 synonym swap(s)"
      and it SEES WHAT A5 CANNOT: a paraphrase pair with a differing opener reads sameOpenerPairs 0
      and overlap >= 6000 bp

    EXEMPLAR CITATION RATE: 0 citation(s) over 786 sentence(s) = 0 bp per unit, on 3 of 10 leaf
      register(s) — leguin-fiction 161 · leguin-nonfiction-spoken 348 · leguin-nonfiction-written 277
      (161+348+277 = 786, the denominator checks) · detectorLive true
      absent, NAMED: martin-chronicle, martin-narrative, tolkien-elevated, tolkien-plain,
                     dnd-flavor, dnd-rules, dnd-rules-srd52
      NON-VACUITY CONTROL, executed: provenanceCount("The muster roll is the watch's own, and the
      watch is bought.") = 1 — the zero is the prose's, not a dead detector

    THE FIXTURE, printed identically on two different rosters:
      captured (rate-9-2) · treasury · holder Town hall · standing INTERESTED
        1 PLAYER as compiled · 2 DM INLINE replacement [passages differ: YES] · 3 DM PEN LINE beside ·
        4 the compiled passage is identical on both audiences: YES
      clean (rate-3-0) · treasury · holder Weekly market · standing LICENSED
        the same four, [passages differ: no], and the pen slot is EMPTY
      PAIRED-TOWN ARM over the fixture: HOLDS
      the DM sentence is labelled the gate's own illustration in its header lines, so it cannot read
      as projected prose

### The T-F12 five — CONFIRMED, all five, driven against the shipped `objectClassesOf`

| modifier | spine | modifier classes | spine classes | collides | verdict |
|---|---|---|---|---|---|
| `stores: short` | `Disasters & Famine: granary AND hospital` | `[store]` | `[care, storehouse]` | false | **LAWFUL** |
| `stores: import-fed` | the same | `[store, market]` | `[care, storehouse]` | false | **LAWFUL** |
| `country: pressed (walled)` | `WALLED-STRAINED` | `[]` | `[wall]` | false | **LAWFUL** |
| `country: pressed (unwalled)` | `UNWALLED-LARGE` | `[]` | `[wall]` | false | **LAWFUL** |
| `country: pressed (unwalled)` | `UNWALLED-SMALL` | `[]` | `[wall]` | false | **LAWFUL** |

    THE CONSERVATIVE HALF SURVIVES EXECUTION (the split is true, not convenient):
      stores: short x Disasters & Famine: NO reserves, hospital present       collides TRUE (refused)
      stores: short x Disasters & Famine: NO reserves, NO medical provision   collides TRUE (refused)
      stores: short x GRANARY: thin                                           collides TRUE (refused)
    THE TEN MOVED ROWS: exactly ten, and precisely the ten named — three DS-DEF-2 granary rows,
      three DS-DEF-6 Logistics rows, four DS-ECO-2 :: GRANARY: <band> rows
    multi-class rows 12 -> 17 · ten of the seventeen carry storehouse
    keys carrying a trailing polarity parenthetical: 0 of 708 — the cure bites only where the defect was
    proseWiringCensus.walker.test.js 78 passed · classifier UNCHANGED 73,284 · every class 0

### The register doors' movements — CONFIRMED, with P-2 corrected

    mechanisms: base 14 -> tip 15 · the ONE new id is `passage-shape`
    `draw-formula`             REWRITTEN IN PLACE, carrying the declared `reIndexed` block
    `passage-shape`            NEW · source + source, both pins PRESENT in the files they name
    `face-count-per-variant`   integer + integer + digest = [2266, 1,
                               0fa317c13a7610e23ae250994e5dea69ef29be857417b5b2aca7a70936bda366]
                               floors {} — correct by construction, the loop armed for the first growth
    `connective-list-length`   map, 0/0/1/1 -> 3/3/3/3
    `comparator-and-band-rule` source + integer + integer
    4 rows are named NOT mechanisms · measured at 708 pools / 2,266 variants
    every `kind: 'source'` pin is re-read from disk by the contract test
    (tests/data/dossierStateProseProjection.contract.test.js:1174-1179) — a pin is a measurement here
    RATCHET, PROVEN BY PLANT: a dropped FACE reds by name (4 failed / 73 passed); a dropped VARIANT
    with a consistent poolMeta reds by name (6 failed / 71 passed) — clean tree 77 passed

### The gates at the tip — CONFIRMED (two independent whole-`tests/lint` runs)

    npx vitest run tests/lint        154 files passed / 0 failed · 2,590 passed / 0 failed
                                     (109.77 s and 108.47 s; ls tests/lint/*.test.js = 154, so whole)
    contract test                    77 passed          proseWiringCensus.walker      78 passed
    sovereigntyLightingContract      34 passed          observedShapeReaders.walker   44 passed
    stateProseKernel.test.js         37 passed          composeStateProse.test.js     50 passed
    prosePassageShapes.walker        11 passed          proseWaveGate.walker          41 tests, green
    proseCorpusBytes                 18 passed
    check-full-typecheck.mjs         OK — 173 errors, ceiling 173 (38 distinct files; intersection
                                     with the car's 49 changed files EMPTY, by set intersection)
    check-domain-strict.mjs          ✓ no regressions — 1,120 errors, ceiling 1,120
    eslint over every .js/.mjs the car touched          exit 0, no output
    wiring-census.mjs --dry          CURRENT · bytes 2,060,855 = 2,060,855 · delta 0 · sections (none)
                                     · stamped shas (none) · candidate leaves unmoved · ROWS 0
                                     (base-vs-tip: 0 of 7 stamped shas moved)
    generate-dossier-state-prose --check   exit 0 · 708 pools / 2,266 variants · 708 sentence seats /
                                     0 clause · not-a-modifier 708 · 68 blocks / 6 desks ·
                                     78 causal families / 468 variants
    check-observed-shape-readers.mjs 1,972 findings, exactly the frozen inventory (1,972 -> 1,972,
                                     shrink-only satisfied) · frozenAtSha bc441dccc
    lighting census                  files 2,557 -> 2,562 · parked 375 -> 375 (unmoved) ·
                                     credited 2,182 -> 2,187 · titles 24,049 -> 24,158 (+109) ·
                                     suiteTitles 6,422 -> 6,451 · measuredAtSha fa6696860
    byte ratchets                    only dossierConnectives.generated.js moves: raw 1,365 -> 2,046,
                                     gzip 795 -> 1,138, ceilings 2,122 / 1,236 untouched; the six
                                     prose leaves delta 0 (general 249,940 · power 109,224 ·
                                     stressors 83,961 · warFaith 160,797 · defense 154,593 ·
                                     economy 126,213)
    the consist                      49 files, +8,785 / -455, exactly 12 commits
                                     f4005cccd 39c88b02d 83e8acf17 94c41fdb8 e26ad7838 e2ac44a9c
                                     99e8e56e8 31faa63aa bc441dccc fa6696860 12b240397 5c7eadb18
    zero reader-facing bytes         39c88b02d -> tip: every class 0 · UNCHANGED 73,284 / 525 towns ·
                                     ADDED 0 · REMOVED 0 · index-only moves 0
    THE FACT FENCE (bytes, not counts) 525-town corpus: ELIGIBLE-SET CALLS 146,568, sha256
                                     b0173012…b18f39 under BOTH draws, cmp byte-identical;
                                     768-town corpus: 146,537 calls, sha256 8e3af868…b7b8563 under
                                     BOTH draws, cmp byte-identical
    vid histogram                    0:7 1:708 2:708 3:675 4:121 5:32 6:15 · pools with a vid 0: 7 ·
                                     non-contiguous 0 of 708 (reproduced by two lenses independently)
    attach-set / modifier            0 of 708 and 0 of 708 — no unit has a shape question
    connective floors                consequence.clause 3 · tension.sentence 3 · contrast.sentence 3 ·
                                     addition.sentence 3; not one joint carries `which`, an em dash,
                                     a digit or a percent
    THE SHAPE REPORT                 product: 0 pools · 0 units · 0 with a shape question · 0 draws
                                     (the emptiness IS the measurement, ADDENDUM ruling 3)
                                     laneTASTE f07b98529: 7 pools · 408 units · 408 with a shape
                                     question · 26,112 draws · MARGINAL spine-then-sentence 23,907
                                     91.56 % / sentence-then-spine 2,205 8.44 % / clause-seat 0 ·
                                     CONDITIONAL lawful={1} n 21,824 100.00 %, lawful={1,2} n 4,288
                                     48.58 % / 51.42 % (2,083 / 2,205) · DUPLICATE-UNIT RATE fixed
                                     9,844 bp, licensed draw 9,818 bp · RELATION addition 408
                                     100.00 % · CONSTRUCTION V1 339 83.09 % / empty 69 16.91 % ·
                                     REFUSALS 408 clause-seat + 341 sentence-then-spine
    THE THREE REDS, EACH ACCOUNTED   dossierProseManifest 2 failed | 12 passed (the two CHARTERED
                                     arms; pins 309 / 36 / 0 read exactly as declared) ·
                                     generatorGoldenMaster 1 failed | 2 passed AT THE BASE
                                     29ec62425 with a clean tree — INHERITED ·
                                     voiceMechanics 1 failed | 18 passed identical at base and tip
                                     (labelBands.js em 5, generalStateProse.js em 3) — INHERITED,
                                     the train's own three literals genuinely cured
    THE DRIFT CORPUS'S SEEDS         ⚠ CORRECTED: golden-master-v3 72,108 cells / 516 towns ·
                                     gm-seed-a 394 / 3 · gm-seed-b 390 / 3 · gm-seed-c 392 / 3 —
                                     FOUR seeds, not one

---

## 4 · WHAT THE §919 REGISTER DOORS WILL MOVE

1. **`draw-formula`** — gains a committed digest of the two cell files (or the diff's JSON) beside
   the `reIndexed` block, if the chair rules for cure a-9 / c-6. The row's declared figures do not
   change; the row gains machine standing.
2. **`connective-list-length`** — either a SECOND map for the composer's `CONNECTIVES` constant
   (today 1/1/0/0 against the leaf's 3/3/3/3), or a DECLARED divergence field naming SEAM car 4 as
   the closing car. Cure a-4. The leaf half of the pin does not move.
3. **`passage-shape`** — both source pins are re-read from disk by the contract test, so re-wording
   the WITHHELD reason in `src/domain/prose/passageShapes.js` (cure a-4) MOVES the row. Plan the
   register act in the same commit as the re-wording, or the pin reds.
4. **`comparator-and-band-rule`** — its source pin moves if `scripts/prose-shape-report.mjs:205` is
   re-worded (cure a-6) or the band-grain print is re-cut (cure b-4); its two integer pins (13 / 8)
   do not.
5. **`face-count-per-variant`** — unchanged. `[2266, 1, 0fa317c1…bda366]` with `floors: {}` holds
   until a family grows; the declared-floor loop is armed for that day.
6. **No new mechanism id is owed by this fold.** The count stays at **15** unless the chair rules a
   `composed-unit-builder` row onto cure a-3, which would make it 16.
7. **The three rituals, not doors but gated the same way:** any cure touching a `tests/lint` walker
   or a stamped file re-opens the wiring census (ROWS / sections), the lighting census (files,
   credited, titles, suiteTitles, measuredAtSha) and the OSR freeze (`frozenAtSha`). The receipt's
   own three-commit shape (`fa6696860` / `12b240397` / `5c7eadb18`) is the precedent: two rituals
   refuse a dirty tree, so cures 1-9 cannot land in one commit.

---

## 5 · UNTESTED ROWS, AND WHAT WOULD TEST THEM

| # | row | status | what would test it |
|---|---|---|---|
| U-1 | **The final sweep** — `npx vitest run tests/domain tests/property tests/data` = 1,092 files passed / 2 failed · 17,497 passed / 3 failed | **LIVE UNTESTED.** No lens re-ran it: the standing discipline forbids a whole-suite run from a skeptic dock while siblings share the machine, and a whole-suite run under machine load fails vacuously | One quiet-machine run in a single dock: `ps -r` idle, runner count 0 taken in its own one-line shell, no sibling lane. Its THREE reds are already individually CONFIRMED (U-1's untested residue is only the claim "nothing else in 17,500 moved") |
| U-2 | Fences §16.2 — 8a-1's uniformity and append-safety figures (5,470,000 and 1,094,000 reads; chi² 4.201; 25.01 % vs 74.99 %) | **DISCHARGED** by the kernel lens (b-1, b-2), re-derived on six seed families with its own loops | — |
| U-3 | Fences §16.3 — 8a-2 … 8a-7's own per-car acceptances (the gate, the shape module, `armThread`, the arm-Q cure) | **DISCHARGED** by the kernel lens (e-1 … f, g-1 … g-4) and the gate lens (claims 5-13), two of them with negative controls that convict their own reversal | — |
| U-4 | Receipt refusal 2 — "**8 of `proseTasteCandidates.walker.test.js`'s 9 arms red** without the seven candidate functions; the ninth passes vacuously" | **UNTESTED.** The file is ABSENT at HEAD (confirmed twice), so no lens could run it | Cherry-pick the walker alone into a throwaway dock at `5c7eadb18` and run it; count the reds and confirm the ninth arm's vacuity |
| U-5 | Receipt refusal 5 — `closureCost` prices a lexicon import at **+4 files / +150,231 B onto a 70,252 B closure** | **UNTESTED.** No lens drove `scripts/lib/module-closure.mjs` | Run the closure cost on `composeStateProse.js` with the lexicon import planted, then restore; the fence's import roster of three is separately readable |
| U-6 | Receipt 8a-10-voice — "the four walkers that read these strings pass unchanged (**214 tests**)" | **UNTESTED as a named figure.** All four are inside the green `tests/lint` 154 / 2,590, so they pass; the 214 count itself was not taken | `npx vitest run` over the four named walker files in one call and read the total |
| U-7 | Receipt refusal 7 — `proseTasteMeasure`'s "THE FIXTURE IS THE SHIPPED SHAPE" arm could not be kept armed; owed to 8b | **UNTESTED, and untestable today** — it needs a real composed unit, which no shipped pool produces (attach-set 0 of 708) | It becomes testable only when a modifier pool ships; until then the honest record is NOT-EXECUTABLE, which is what landed |
| U-8 | Receipt 8a-3 — the eleven picks' conflict handling on four picks (M-2, M-4, M-8b) "all resolved to HEAD content by restore-and-stage, then `git cherry-pick --quit`" | **UNTESTED as a process claim**; its OUTCOME is fully confirmed (every refused file byte-identical to `29ec62425` at HEAD, every taken file present) | Nothing further is worth running: the outcome test is stronger than the process test, and re-running the picks would mutate a shared tree |

---

## CLOSE

Forty-eight claims CONFIRMED by execution across three docks, one REFUTED, two PARTLY, three
UNTESTED (one live), fourteen NEW findings of which five are MEDIUM. **No mechanism defect landed
by car 8a survives this fold** — the refutation and three of the five MEDIUM findings are false or
divergent SENTENCES and REGISTRIES (the DRIFT seed comment, the connective lists' two homes, the
rename rationale, the shape report's OWED line); the other two MEDIUM findings are an unguarded
guard (N-1) and a shared-packet write hazard (N-4) that must be ruled on before 8b runs two arms.

    porcelain at close   skepRW1 0 · skepRW2 0 · skepRW3 0 · laneLIGHT 0
    runners at close     0
