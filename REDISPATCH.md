# REDISPATCH.md — the prompts VERBATIM for the lanes alive at the 2026-09-07 12:28 cutoff (session 8de5f153). Re-derive the dock tip and porcelain with `git` before sending; never send a prompt whose figures you have not re-read.

## 1. L-MAT-FIX (Opus, background Agent, model "opus") — only if `git -C $SC/laneLMAT log --oneline 7d96e2b72..HEAD` shows fewer than three fix cars
You are LANE L-MAT-FIX (Seat: Opus 5 — Fable-unvalidated; the implementer). Chair: Fable 5.1 (a successor session; re-dispatched after the previous session died on its usage window).

SC=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/26b2203a-14d7-409e-a7aa-8d0f3b0517db/scratchpad

YOUR BRIEF: read `$SC/briefs/brief-L-MAT-FIX.md` IN FULL and execute it in the dock `$SC/laneLMAT` (a git worktree; its CURRENT tip is your base — `git rev-parse HEAD`; six L-MAT cars sit over the product tip 3b1c0eaa5, then any fix cars your predecessor committed). Continue from the tip; never cut a new dock; never touch the main tree or any other dock. Reading list first: `$SC/skeptic-912/FOLD.md`, the six lens files beside it, `$SC/receipt-l-mat.md` (its `## L-MAT-FIX (cars 7–9)` section is YOUR receipt — your predecessor wrote it PARTIAL; read what it landed and what it left), `$SC/briefs/brief-L-MAT.md`.

CONTINUITY: your predecessor died with its session mid-car. If `git status --porcelain` is non-zero, that is its UNCOMMITTED work: read the diff file by file against the brief; keep what the brief asks for and re-run that car's focused proofs before committing it as your own car; anything you cannot account for is restored by `cp` from `git show HEAD:<path>` (never `git checkout`). Re-take the arrival check (`date`; HEAD; porcelain; node_modules ≈ 453–454; `$SC/HOLD-VITEST` absent) and append your own arrival block to the receipt section.

STANDING LAWS: STATE NEVER FATE (the dial stays at 1; no tuning value, golden, register write, signature, migration, applied-head byte, ARCHITECTURE.md edit); THE PROMISE re-proven by execution on every lifecycle path the fold lists; explicit-path commits only, never stash, never push; trailers `Seat: Opus 5 — Fable-unvalidated` + `Lane: L-MAT-FIX`; focused vitest ONE FILE AT A TIME with exits captured in-shell, `$SC/HOLD-VITEST` checked before each (if present, STOP); no whole-suite run; a timeout under load is a false red — re-run once quietly; negative-assertion per-file budgets EXACT (`expectAbsentWithAnchor`); a type error is fixed in the source, never the baseline; if a cure or a chair ruling (R-F…R-J) contradicts a measurement, REFUSE that item with the measurement and land the rest.

DELIVERABLE: the three cars (7 behaviour, 8 instruments, 9 text), the consist proof the brief lists at the composed tip, the receipt section complete. Final message: STATUS, car shas, dock tip, porcelain, every refusal in one line.

## 2. S12 (the seat-split reconciliation) — ONE command builds the resume
`python3 $SC/prose-research/mk-s12-resume.py` then `Workflow({ scriptPath: "$SC/prose-research/reconciliation-workflow.resume-v2.js", args: <the JSON object in $SC/prose-research/sweep/s12-resume-args.json> })`. Every finished stage (its journal result + its file on disk with the right seat line) is skipped; the rest run. Update `_S12.runId` in `sweep/LAST-RUNS.json` after the launch.

## 3. The §912 landing chain — step 4 of the ⏸⏸ card atop RESUME-NOTE.md (after the fix lane reports).
