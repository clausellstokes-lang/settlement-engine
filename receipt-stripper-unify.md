# RECEIPT — STRIPPER-UNIFY — ✅ COMPLETE (one commit), with FOUR BRIEF ERRORS CORRECTED and TWO PRICED REFUSALS

**Lane** STRIPPER-UNIFY · **Seat** Opus 5 — Fable-unvalidated · **Chair** Fable 5.1 · 2026-09-05
**Dock** `$SC/laneSTRIP`, opened detached at `38474a59eba460f30d6596dcb65efda3a446738a` (HEAD and porcelain 0 both
verified on arrival; `node_modules` a symlink into the owner tree — never materialised, never `npm install`ed).
Scratch lives OUTSIDE the measured tree at `$SC/strip/`.

---

## 0 · THE HEADLINE

⛔ **THE BRIEF'S MECHANISM IS ALREADY HALF-LANDED, AND ITS ADDRESS IS WRONG.** "Lift ONE stripper into
`tests/helpers/` (a plain module, no `it`)" describes a file that **exists at my base**:
`tests/helpers/codeOnlySource.js`, a character scanner with **23 adopters**. And the export it warns about is
re-exported from **`tests/lint/engineGatedRuleKeys.walker.test.js`**, not `sovereigntyLightingContract.walker.test.js`
— the sov walker only *mentions* `codeOnly` in two comments about its own title tuple. The count in the brief (12
suites importing through the walker) is **exactly right**; only the address is wrong.

⛔ **L-HOMES-8'S ELEVEN ARE ELEVEN OF TWELVE, AND ONE OF ITS TWO "SOUND" FILES IS UNSOUND.**
`tests/domain/infiltrationDrift.test.js` is named in receipt §9.1 as already ordering it correctly. It does not:
its `LEAF_LOGIC` runs the single-quote pass first, on one line, and it is the WORST member of the class measured
(1,960,194 characters over `src/`, because it has no double-quote pass to close the span). `operationsVoice.test.js`
is the only class-(b) file in the estate, and its header explains the ordering in prose.

⭐⭐ **AND THE CHAIR'S ADDENDUM IS CONFIRMED, SEPARABLY, PER INSTRUMENT.** The reorder alone leaves a residue in six
of the twelve — measured **1,269,249 characters in 266 of 2,174 src files** for the five-file NPC family — because
an apostrophe inside a **double-quoted** string opens the same span the reorder was meant to close. In the other six
the quote classes were already newline-bounded (or the stripper is asked one line at a time) and the reorder is the
whole cure. Both halves are landed everywhere, and each half has its own killing fixture.

✅ **ZERO VERDICT FLIPS. 37,383 verdicts computed across every arm that consumes stripped text, over `src/` whole
and over each instrument's own corpus, base spelling versus cured: 0 differ.** The denominators were intact by luck,
exactly as they were in L-HOMES-8's own walker.

---
## 1 · THE CENSUS, WITH ITS DENOMINATOR

**Denominator: 2,731 `.js/.jsx/.mjs/.cjs` files** under `tests/` (2,564) and `scripts/` (167), at `38474a59e`.
**84 of them carry a source-stripping helper.** Every one is classified; the roster is closed.

| class | what it is | count | ordering hazard? |
|---|---|---|---|
| **(a)** | uses the shared `codeOnly` — 8 import `tests/helpers/codeOnlySource.js` directly, 12 through the `engineGatedRuleKeys.walker.test.js` re-export, 3 are the helper's own home/adopters | **23** | no — one writer, a character scanner |
| **(b)** | private, **correct** order (template pass first) | **1** | no |
| **(c)** | private, **WRONG** order (a quote pass before the template pass) | **13** | ⛔ **YES — this car's subject** |
| **(d)** | private, **comment-only** (no string pass at all) | **39** | no — a comment stripper never opens a quote span |
| **(e)** | private **character scanner** (a state machine, quotes and templates in one pass) | **8** | no — order is not a concept in it |

**(b), the one sound private spelling:** `tests/domain/operationsVoice.test.js`.

**(c), the thirteen — L-HOMES-8's eleven, plus `infiltrationDrift` it wrongly cleared, plus its own walker:**

| # | file | landed spelling | this car |
|---|---|---|---|
| 1 | `tests/domain/infiltrationDrift.test.js` | sq before bt, **no dq pass at all** | ✅ cured |
| 2 | `tests/domain/npc/characterConsumers.test.js` | sq, dq, bt | ✅ cured |
| 3 | `tests/domain/npc/characterDrift.test.js` | sq, dq, bt | ✅ cured |
| 4 | `tests/domain/npc/livedExperienceFunnel.test.js` | sq, dq, bt | ✅ cured |
| 5 | `tests/domain/npc/livedExperienceSources.test.js` | sq, dq, bt | ✅ cured |
| 6 | `tests/domain/npc/paradigmAxisCatalog.test.js` | sq, dq, bt | ✅ cured |
| 7 | `tests/domain/townCartographyDeterminism.test.js` | sq, dq, bt — classes already `\n`-bounded | ✅ cured (order only) |
| 8 | `tests/lint/seedLoopTotality.walker.test.js` | sq, dq, bt — per LINE | ✅ cured |
| 9 | `tests/lint/worldGenerationClockSeam.walker.test.js` | sq, dq, bt — classes already `\n`-bounded | ✅ cured (order only) |
| 10 | `tests/property/dispositionChannelsDormancyGolden.test.js` | sq, dq, bt — per LINE | ✅ cured |
| 11 | `tests/property/oathHolderDormancyFence.test.js` | sq, dq, bt — per LINE | ✅ cured |
| 12 | `tests/property/sovereigntyTradeDormancyFence.test.js` | sq, dq, bt — per LINE | ✅ cured |
| 13 | `tests/lint/densityCreateBoundary.walker.test.js` | sq, dq, bt | ⛔ **NOT TOUCHED — see §6 finding 1** |

⛔ **ROW 13 IS A DELIBERATE REFUSAL.** L-HOMES-8 cured that file in `$SC/laneLH8` at `8776fae10`, which has not
landed at my base. Curing it here would put two docks on the same two lines of the same file for no gain. **But its
cure is ORDER-ONLY** (`git -C $SC/laneLH8 show 8776fae10` — the backtick pass moves first, the classes stay
`[^'\\]` / `[^"\\]`), so **the addendum's second half is still owed on that file**; the cure is four characters and
its address is in §6.

---

## 2 · THE MIS-STRIP, PER INSTRUMENT, IN BOTH VARIANTS

Probe: `$SC/strip/misstrip.mjs`, read-only, run outside the measured tree. Each instrument's stripper is applied to
**all 2,174 `src/` `.js`/`.jsx` files** the way that instrument applies it (whole-file or per-line). The reference is
the fully cured spelling (template pass first **and** newline-bounded quote classes); "chars" is the summed character
difference from that reference. **After** is 0/0 for every row by construction — the cured file *is* the reference —
and that is not the receipt: the independent after-check is §4, which compares the cured strippers against the
estate's own character scanner.

| instrument | mode | LANDED: files / chars | REORDER ONLY: files / chars | after |
|---|---|---|---|---|
| `npc/characterConsumers` | whole | 377 / **1,649,119** | 266 / **1,269,249** | 0 / 0 |
| `npc/characterDrift` | whole | 377 / 1,649,119 | 266 / 1,269,249 | 0 / 0 |
| `npc/livedExperienceFunnel` | whole | 377 / 1,649,119 | 266 / 1,269,249 | 0 / 0 |
| `npc/livedExperienceSources` | whole | 377 / 1,649,119 | 266 / 1,269,249 | 0 / 0 |
| `npc/paradigmAxisCatalog` | whole | 377 / 1,649,119 | 266 / 1,269,249 | 0 / 0 |
| `infiltrationDrift` | whole | 376 / **1,960,194** | 266 / **1,571,427** | 0 / 0 |
| `townCartographyDeterminism` | whole | 7 / 10,230 | **0 / 0** | 0 / 0 |
| `worldGenerationClockSeam` | whole | 527 / 29,705 | **0 / 0** | 0 / 0 |
| `seedLoopTotality` | line | 19 / 471 | **0 / 0** | 0 / 0 |
| `dispositionChannelsDormancyGolden` | line | 6 / 226 | **0 / 0** | 0 / 0 |
| `oathHolderDormancyFence` | line | 6 / 226 | **0 / 0** | 0 / 0 |
| `sovereigntyTradeDormancyFence` | line | 6 / 226 | **0 / 0** | 0 / 0 |

⭐ **THE TABLE IS THE ADDENDUM'S PROOF, AND IT SPLITS THE ESTATE IN HALF.** Six instruments needed **both** halves
(their quote classes crossed newlines); six needed **only** the reorder (their classes already stopped at a newline,
or they are asked one line at a time so a newline never reaches them). ⛔ **A cure that shipped only the reorder
would have left 1.27–1.57 MILLION characters invisible in six of the twelve** — the chair's measurement, confirmed
here per instrument rather than in aggregate.

⚠ **AND THE NEWLINE BOUND IS A MEASURED NO-OP IN THE FOUR LINE-SCOPED INSTRUMENTS.** It is carried anyway, and the
comment beside each says so, because the next reader copies whichever spelling they find. What it does **not** close
there is an apostrophe inside a double-quoted string **on the same line** — no `\n` can bound that. Measured over
`src/`: zero occurrences today. Recorded as a live class in §6, not papered over.

---

## 3 · VERDICT FLIPS — 37,383 COMPUTED, **ZERO** FLIPPED

Probe: `$SC/strip/verdicts.mjs`. Every verdict each instrument actually DERIVES FROM STRIPPED TEXT, computed under
the landed spelling and the cured spelling:

| instrument | verdict measured | population | flips |
|---|---|---|---|
| the five NPC files | `\b(SYMS)\s*[([.]` for each of DRIFT (13 symbols), FUNNEL (12), CONSUMER (11), KNOWN (2), CATALOG (18), SLICER (3) | 2,174 src files × 6 arms | **0** |
| `characterDrift` | `AUTHORED_CORE_READ` **count** per file (the frozen read census) | 2,174 files | **0** |
| `paradigmAxisCatalog` | the `must cite the catalog in prose only` arm | 2,174 files | **0** |
| `worldGenerationClockSeam` | all 8 `READS` **counts** per file | 2,174 files × 8 | **0** |
| `seedLoopTotality` | the bare-seed-loop LINE NUMBERS per test file | every `tests/**/*.test.js` | **0** |
| the three fences | `residue.includes(FLAG)` + `STRICT_READ` + `DECLARATION`, per line, per flag | every src file naming the flag | **0** |
| `townCartographyDeterminism` | `for(` · `Math.random` · `Date.now` · `new Date()` over its own package | every `src/domain/townCartography/*.js` | **0** |
| `infiltrationDrift` | the 8 `LEAF_LOGIC` token claims its arms assert on | its leaf | **0** |

**Total 37,383 verdicts, 0 flips.** ⭐ The denominators were intact **by luck** — the same result L-HOMES-8 reported
for its own walker, now measured over twelve more instruments. A flip would have been a finding; there is none.

---

## 4 · THE INDEPENDENT AFTER-CHECK — the cured strippers against the estate's own character scanner

The same probe recomputes every verdict a third time with `tests/helpers/codeOnlySource.js` (`codeOnly`, the
offset-preserving state machine 23 instruments already share). **Cured versus sound: 33 differences of 37,383, and
every one of them is identical under the LANDED spelling too — this car introduces none of them.**

| where | count | why |
|---|---|---|
| `worldGenerationClockSeam` | **31** (21 files) | its stripper **blanks whole template literals including `${…}`**, on purpose; `codeOnly` KEEPS interpolations because an interpolation is code. A `Date.now()` inside `${…}` is invisible to the walker. **Pre-existing, and a real finding — §6 finding 2.** |
| `seedLoopTotality` | **2** | the same interpolation difference (`${_seed}` inside a template), plus one hit inside its own file, which its `collectSeedFailures` exemption removes from the real scan |
| **every other instrument (10 of 12)** | **0** | the cured regex chain is **verdict-identical to the estate's character scanner** over the whole tree |

⭐ **THAT ZERO IS THE SOUNDNESS RECEIPT.** A regex chain is not a parser, and saying "the cure looks right" would be
reasoning. Ten of twelve cured strippers agree with the estate's parser-shaped stripper on every verdict it has, over
2,174 files; the two that disagree do so for a reason that predates this car and is written down.

---

## 5 · WHAT LANDED — twelve files, all pre-existing, ZERO new test files and ZERO new titles

Each file gets (i) the stripper cured at the habitat, with the measured figures in a comment beside it, and (ii) one
or two killing fixtures planted **inside an existing test title**, so no `it` is minted and no register moves.

| file | cure | fixtures planted |
|---|---|---|
| `tests/domain/infiltrationDrift.test.js` | inline chain → a NAMED `codeWithoutCitations`, template pass first, `\n`-bounded quote class | F1 + F2, in `the leaf declares no floor, no clamp…` |
| `tests/domain/npc/characterConsumers.test.js` | inline chain in `dependsOnSlicer`, both halves | F1 + F2, in `the dead reader really is dead…` |
| `tests/domain/npc/characterDrift.test.js` | `codeWithoutCitations`, both halves | F1 + F2, in `THE SHARPENED DETECTOR IS ANTI-VACUOUS…` |
| `tests/domain/npc/livedExperienceFunnel.test.js` | `codeWithoutCitations`, both halves | F1 + F2, in `THE SHARPENED DETECTOR IS ANTI-VACUOUS…` |
| `tests/domain/npc/livedExperienceSources.test.js` | `codeWithoutCitations`, both halves | F1 + F2, in `THE FUNNEL-FAMILY DETECTOR IS ANTI-VACUOUS…` |
| `tests/domain/npc/paradigmAxisCatalog.test.js` | `codeWithoutCitations`, both halves | F1 + F2, in `THE DETECTOR IS ANTI-VACUOUS…` |
| `tests/domain/townCartographyDeterminism.test.js` | `codeOnly`, order only (classes already bounded) | F1, in the `cartographyParcels` scan title |
| `tests/lint/seedLoopTotality.walker.test.js` | `stripNoise`, order + bound-for-parity | F1, in `the detector fires on every bare seed-loop spelling` |
| `tests/lint/worldGenerationClockSeam.walker.test.js` | `codeOnly`, order only (classes already bounded) | F1, in `LIVENESS: a synthetic unpinned read…` |
| `tests/property/dispositionChannelsDormancyGolden.test.js` | `codeResidue`, order + bound-for-parity | F1, in `the gate census is not vacuous…` |
| `tests/property/oathHolderDormancyFence.test.js` | `codeResidue`, order + bound-for-parity | F1, in `every production read of the flag is the strict === true form` |
| `tests/property/sovereigntyTradeDormancyFence.test.js` | `codeResidue`, order + bound-for-parity | F1, in `FENCE 4 is not vacuous…` |

**The two fixtures, and why the JOIN is load-bearing:**
- **F1 (the ORDER)** — an apostrophe inside a backtick string with live code after it, **all on ONE line**, so a
  newline-bounded quote class cannot save it. Only the template-first pass can.
- **F2 (the NEWLINE BOUND)** — an apostrophe inside a **double-quoted** string, **across LINES**, with live code
  between. The reorder cannot save it (the single-quote pass still runs before the double-quote pass); only the
  `\n`-bounded class can.

⛔ **AND THAT DECOMPOSITION IS A DEFECT THE PLANT-OUT FOUND IN THIS CAR'S OWN MACHINERY.** The first cut wrote F1
multi-line, and **M1 killed only 4 of 12 suites**: in the eight whole-file instruments the newline bound the same car
had just added masked the ordering defect the fixture existed to pin. ⭐ **A fixture for one of two composed
defects must be shaped so the OTHER cure cannot rescue it** — otherwise the plant is green for the wrong reason and
the pin is decoration. Worth a standing row; the note is written into all eight files beside the fixtures.

---

## 6 · FINDINGS RECORDED, NOT BUILT — each with its address

1. ⛔⛔ **L-HOMES-8'S CURE IS ORDER-ONLY, AND THE ADDENDUM'S SECOND HALF IS STILL OWED ON ITS FILE.**
   `$SC/laneLH8` at `8776fae10` moves the backtick pass first in `tests/lint/densityCreateBoundary.walker.test.js`
   but leaves `[^'\\]` and `[^"\\]`, which cross newlines. **The cure is four characters** — `[^'\\\n]`,
   `[^"\\\n]` — and belongs to whichever car lands that file. I did not touch it: two docks on the same two lines
   of the same file buys nothing, and my own measurement of that stripper's residue is in §2's shape (it is the
   NPC-family chain exactly). ⚠ Its receipt §9.1 also mis-clears `infiltrationDrift.test.js`; §1 row 1 corrects it.
2. ⛔ **`worldGenerationClockSeam`'s STRIPPER IS BLIND TO TEMPLATE INTERPOLATIONS, AND IT IS THE ONE ARM WHERE THAT
   MATTERS.** Its `codeOnly` blanks a whole template literal including `${…}`; the estate's shared scanner keeps
   interpolations because an interpolation is code. Measured: **31 read-count differences across 21 src files** —
   e.g. `src/lib/saves.js` (`Date.now` 5 vs 6), `src/lib/authIntents.js` (2 vs 4), `src/store/campaignSlice.js`
   (0 vs 1). ⛔ **A `Date.now()` written inside `${…}` on a world-generation path is invisible to ARM A today.**
   Every one of the 21 files is outside `src/domain`, so no LEDGER_A row moves at this base — but the class is live
   and the walker's own header claims more than it measures. **NOT TAKEN: widening it changes verdicts and needs
   the chair's ruling on whether interpolations enter that census.**
3. ⚠ **THE FOUR LINE-SCOPED STRIPPERS STILL MIS-STRIP AN APOSTROPHE INSIDE A DOUBLE-QUOTED STRING ON THE SAME
   LINE.** No newline bound can close that: the single-quote pass runs before the double-quote pass, and ordering
   them the other way just moves the hole. Only a character scanner closes it. Measured over `src/` today: **zero
   occurrences**, so nothing is hidden at this base. Recorded because the class is real and the cure is adoption
   (§8 row 2), not another regex.
4. ⛔ **THE BRIEF'S MECHANISM — "lift ONE stripper into `tests/helpers/`" — IS ALREADY DISCHARGED, AND THE REST OF
   IT IS REFUSED ON MEASUREMENT.** `tests/helpers/codeOnlySource.js` exists with 23 adopters. Making the remaining
   twelve import it is **verdict-changing** (§4: 33 differences, all in the two instruments whose semantics
   genuinely differ), and for `seedLoopTotality` it would change the BRACE COUNTING contract its `forHeaderEnd`
   rests on, because `codeOnly` preserves `${` and `}`. **For the other ten it is verdict-neutral and would be a
   real single-writer win** — that is the shape of the follow-up car, and it is §8 row 2.
5. **`tests/domain/operationsVoice.test.js` IS THE ONLY SOUND PRIVATE SPELLING IN THE ESTATE, AND ITS HEADER SAYS
   WHY** — "the sibling batteries never hit this because their leaves compose no prose". That comment was right and
   nobody generalised it for eleven months of cars. Left untouched.
6. **NEITHER A GOLDEN-FREEZE ROW NOR AN EXCLUSION IS OWED.** Two of the twelve already appear in
   `tests/fixtures/.golden-freeze-register.json` — `seedLoopTotality.walker.test.js` on the
   `UPDATE_EPISTEMIC_ALLOWLIST` exclusion, `dispositionChannelsDormancyGolden.test.js` as an enrolled row with every
   measured field still `null` (the register is unfrozen). No fixture, no `sha256`, no `rows`, no env spelling and
   no 64-hex literal appears anywhere in this diff (`grep -cE '[0-9a-f]{64}|UPDATE_GOLDEN|GOLDEN_SHIFT_SIGNED|
   REFREEZE|UPDATE_[A-Z_]+'` over the added lines = **0**), and no new test file is minted.

7. ⛔⛔ **THERE IS NO INSTALLED GIT HOOK IN THIS REPO, SO "COMMITTED WITH THE PRE-COMMIT HOOK" IS A CLAIM NO LANE
   CAN CURRENTLY MAKE.** `core.hooksPath` is unset in the shared config, `.git/hooks/` holds only `.sample` files,
   and `.husky/_` (husky v9's hook shim, which is what sets `core.hooksPath`) **does not exist** — only the two
   tracked scripts `.husky/pre-commit` and `.husky/pre-push`, neither executable. **Nothing ran on my commit.** Two
   consequences the chair should hold: (a) the standing hazard *"a pre-commit `eslint --fix` RE-STAGES so
   `git diff HEAD` is BLIND"* is **inert at this base** — I proved it anyway, md5 of all twelve files identical
   before and after the commit; and (b) the lint the hook would have run is **not automatic** — I ran
   `npx eslint` over all twelve by hand, exit 0, and any lane that skipped that step shipped unlinted.
8. ⚠ **THE BRIEF'S TRAILER SPELLING IS THE ONE L-HOMES-8 SELF-REPORTED AS A DEFECT, AND I FOLLOWED THE BRIEF.**
   It prescribes `Seat:` / `Lane:` then a BLANK LINE then `Co-Authored-By:`, which breaks git's contiguous trailer
   block: `git log -1 --format='%(trailers:only=true)'` on my commit returns `Co-Authored-By` alone. ⭐ **And the
   landed cars are the same** — `%(trailers:key=Seat,valueonly=true)` is empty on `38474a59e`, `d73067af6` and
   `ec1b5e3e9` — so no chair tool can be parsing it and `^Seat:` greps still find every car. Recorded, not amended.

---

## 7 · RECEIPTS — every exit captured in-shell

| instrument | exit | figure |
|---|---|---|
| `npx eslint` over all twelve files | **0** | zero problems (run by hand — see finding 7) |
| **A · the twelve suites at the cured tip** | **0** | `Test Files 12 passed (12)` · `Tests 423 passed (423)` |
| **B · M1 — the unsound pass ORDER restored in all twelve** | **1** | ⭐ `Test Files 12 failed (12)` · `Tests 12 failed \| 411 passed` — **one killed title per file, all twelve** |
| restore after M1 | **0** | `cmp` clean on all twelve |
| **C · M2 — the newline bound removed from the six that needed it** | **1** | ⭐ `Test Files 6 failed \| 6 passed (12)` · `Tests 6 failed \| 417 passed` — **exactly the six, and only the six** |
| restore after M2 | **0** | `cmp` clean on all twelve; `md5` identical to the cured tip |
| ⭐ **`tests/lint/` WHOLE, at the tip** | **0** | **`Test Files 139 passed (139)` · `Tests 2146 passed (2146)`** |
| `git status --porcelain --untracked-files=all` at close | — | **0 lines** |

⭐ **M1 AND M2 TOGETHER ARE THE ORTHOGONALITY PROOF.** M1 reds every file (both halves' fixtures depend on the
order being right); M2 reds exactly the six whose quote classes had to be bounded and leaves the other six green.
Neither mutant kills the other's fixture — measured in `$SC/strip/fixcheck.mjs` before either was run.

**Logs:** `$SC/strip/run-12.log` · `run-M1.log` · `run-M2.log` · `run-lintdir.log` · `plantout2.log`.
**Probes (read-only, outside the measured tree):** `census2.mjs` (the census) · `misstrip.mjs` (§2) ·
`verdicts.mjs` (§3, §4) · `mutate.mjs` (M1/M2) · `fixcheck.mjs` (fixture orthogonality).

---

## 8 · REGISTER DELTAS — recorded, and every one is ZERO

| register | delta | how |
|---|---|---|
| lighting census `files` | **0** | no test file added or deleted |
| lighting census `titles` | **0** | ⭐ MEASURED per file, base vs tip: 66/73/45/63/52/52/21/10/12/9/12/10 → identical. Every assertion went inside an existing title |
| lighting census `suiteTitles` | **0** | `describe` counts identical per file (9/11/9/11/9/13/4/1/3/1/6/1) |
| the census as a whole | **0** | ⭐ CONFIRMED BY EXECUTION — `tests/lint/` whole is **139/139 green**, and `sovereigntyLightingContract.walker` is the arm that throws first on any of the three |
| test ratchet `totalTests` / `totalFiles` / `entries` | **0** | 423 tests before and after; no file added; no new known failure |
| golden-freeze register | **0** | §6 finding 6 — both already-listed files keep their disposition; no fixture, env spelling or 64-hex in the diff |
| four censuses per new `src/domain` leaf | **0** | ⛔ **no `src/` file is touched by this car at all** |
| `sizeBaseline` · voice magnitudes · writer-reach · OSR · domain-any · tuning | **0** | all are `src/`-side registers; the diff is twelve `tests/` files |
| same-seed behaviour | **0** | no production code changed; every edit is inside a test-side scanner |
| each-family park ceiling (111 of 112) | **0** | `grep -cE '\.each\('` over the added lines = **0** |

---

## 9 · ⭐⭐ RETROVALIDATION ROW (owed to the Fable 5.1 chair)

| # | what an Opus seat JUDGED | what the chair must RE-DERIVE | receipts by path | priority |
|---|---|---|---|---|
| 1 | **Refusing the brief's mechanism after measuring that half of it is already landed.** "Lift ONE stripper into `tests/helpers/`" describes `tests/helpers/codeOnlySource.js`, which exists at the base with 23 adopters; the remaining migration is verdict-CHANGING for two of twelve instruments and I cured the ordering habitat instead | that curing at the habitat (verdict-neutral, 0 flips of 37,383) beats adopting the helper (33 verdict changes, one of them a semantic contract `seedLoopTotality`'s brace counting rests on) — and whether the ten verdict-neutral adoptions should be their own car | §4 · §6 finding 4 · `tests/helpers/codeOnlySource.js` header | **HIGH** — it re-cuts the brief |
| 2 | **The follow-up car's exact shape, priced but not taken:** ten of the twelve can adopt the shared scanner with **zero** verdict change (measured over 2,174 files); `worldGenerationClockSeam` and `seedLoopTotality` cannot without a ruling | whether one-writer is worth ten more importers, and — the real question underneath — **whether a clock read inside `${…}` should enter ARM A's census** (§6 finding 2: 31 differences, 21 files, all outside `src/domain` today) | §4 table · `$SC/strip/verdicts.mjs` | **HIGH** — it is the only live blindness this car found |
| 3 | **Not touching `densityCreateBoundary.walker.test.js`**, on the ground that L-HOMES-8 holds it in a parallel dock — while reporting that **its cure is order-only and the newline half is still owed there** | that leaving a known-incomplete cure in a sibling's dock beats a two-dock collision on the same two lines, and who carries the four-character follow-up | §6 finding 1 · `git -C $SC/laneLH8 show 8776fae10` | **HIGH** — an incomplete cure that reads complete |
| 4 | **Correcting L-HOMES-8's roster**: `infiltrationDrift.test.js` is class (c), not one of the two sound files, and it is the WORST member measured (1,960,194 characters) | that my classifier is right and its receipt §9.1 is wrong — the check is one `sed -n '100,112p'` on that file at `38474a59e` | §1 row 1 · `$SC/strip/census2.mjs` | MEDIUM |
| 5 | **Carrying the newline bound into the four line-scoped strippers where it is a measured no-op**, for shape parity rather than effect | that uniformity beats a minimal diff here, given the next reader copies whichever spelling they find — and that §6 finding 3's residual same-line class is correctly left open rather than half-cured | §2 note · the comment beside each of the four | MEDIUM |
| 6 | ⭐ **Reshaping the fixtures after the plant-out refuted them.** F1 written multi-line killed only 4 of 12; the newline bound this same car added rescued the eight whole-file instruments | that **"a fixture for one of two composed defects must be shaped so the OTHER cure cannot rescue it"** is the right general rule, and whether it deserves a standing hazard row of its own | §5 · `$SC/strip/fixcheck.mjs` · the first `run-M1.log` (4 of 12) versus the second (12 of 12) | MEDIUM |
| 7 | **Reporting that no git hook is installed** rather than treating "committed with the pre-commit hook" as done | whether the repo is *meant* to have `core.hooksPath` set (husky's `.husky/_` is absent) — and, if so, how many landed receipts claim a hook run that never happened | §6 finding 7 | MEDIUM |
| 8 | **Following the brief's trailer spelling** (blank line before `Co-Authored-By`) although L-HOMES-8 self-reported it as a defect, because the landed cars have the same shape and no tool parses it | whether the chair's landing tooling parses or greps `Seat:` — the fix is the chair's at composition either way | §6 finding 8 · `git log -1 --format='%(trailers:only=true)'` | LOW |

---

## 10 · OUTCOME

| step | outcome | sha |
|---|---|---|
| the census, the twelve cures, the plant-out and the register proof | ✅ **green** — `tests/lint/` 139/139, the twelve suites 12/12, 0 verdict flips | **`4ed6d1da3`** |

**Dock tip: `4ed6d1da3`** · porcelain **0** · no rebase, no push, no ref write, no register act, no `git stash`,
no `git checkout --`, no `git add -A/-u/.`, no `node_modules` materialisation, no `src/` file touched.

