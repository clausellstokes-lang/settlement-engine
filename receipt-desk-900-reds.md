# RECEIPT — lane DESK-900-REDS (Opus 5, Fable-unvalidated)
## ✅ COMPLETE — all cars landed and proved; the owed whole-`tests/lint/` directory run is executed and attributed.

Seat: Opus 5 — Fable-unvalidated · Lane: DESK-900-REDS · Chair: Fable 5.1 · 2026-09-05
Dock `$SC/laneDESKPROOF`, **verified on arrival**: HEAD `8b9031f4c`, porcelain **0**, detached,
`node_modules` = 453 package symlinks (correct form; NOT materialised, no `npm install`, no rebase,
no stash, no `checkout --`, no register door, no `git show HEAD:<path> >`).
Baseline over this lane's 13 files: **15 red arms, `BASELINE_EXIT=1`** — the composition's own count.

## RESULT: 11 of 13 files CURED AT CAUSE in 6 cars · 2 HANDED UP WITH MEASUREMENT

---

## ⚠ BRIEF ERRORS (four; reported at once, none blocking)
1. **Two arm↔file pairs are SWAPPED**, in the table AND in the ASSERTION TEXTS section.
   `settlementSlice` is *ONE definition and FOUR call sites*; `toggleSlice.scope` is *snapshots all four
   live bags*. `economyStateProseDesk` is *the three `canonical` variants*;
   `dossierStateProseProjection.contract` is *every desk fill table … declared shape*. Both pairs reversed.
2. **`treatyOrientationWr10g`'s row quotes a `writerReach` arm** ("the lit arm changes no draw"), and the
   brief's ⭐HIGHEST suspicion rides on it: *"a seeded draw MOVED — the pool-length law"*. **No seeded draw
   moved, and no pool grew.** The real arm is the war-pair spelling census; the offender is
   `display/stateProse/warFaithStateProse.js` (DESK-4). The chair's top alarm was aimed at a red that does
   not exist. (This is the third night running that the chair's RULING was sound and its MECHANISM wrong.)
3. The brief suspects SEAT-78's `irregularForce.js` or ENC-4b's writer for that arm. **Both refuted** —
   the third writer is the war & faith desk's display leaf.
4. The brief's nine dock shas are all **CORRECT** (`git rev-parse` at each).

---

## ATTRIBUTION TABLE — file · arm → dock/car → cause → disposition

| file · arm | dock / car | cause (measured) | disposition |
|---|---|---|---|
| `build/engineChunkLazy` · pipeline metadata via the lazy lane | laneHOR2 · WORKER Car 1 `c9611da70` | ⛔ **an assertion that could not pass at ANY address** — an import SPECIFIER matched through `codeOnly`, which blanks string text. The arm's own header, 4 paragraphs above, states the rule it broke. Tree is correct (`settlementSlice.js:59`). | **CURED** car 1 — pair masked line (code) with raw line (specifier) by index |
| `store/settlementSlice` · ONE def + FOUR call sites | laneHOR2 `c9611da70` | the 4th `resetSettlementIdentity` moved to `settlementGenerateAction.js:253`; surface measured 3 and read a MOVE as a LEAK | **CURED** car 1 — call-site surface widened to the pair |
| `store/toggleSlice.scope` · four bags stamped | laneHOR2 `c9611da70` | all four `_<bag>:` stampings moved to `settlementGenerateAction.js:118-121` | **CURED** car 1 |
| `lint/negativeAssertionAnchor` · inventory honesty | laneHOR2 `c9611da70` | anchored a site, left the frozen row at 6 — a ceiling with a free slot | **CURED** car 1 — row 6→5, measured before it moved |
| `domain/subsystemRowsVirtual` · every lane rule authored | laneSEAT78 `bc3002c55` | `irregularForceEnabled` minted; the THREE module-scope edits unpaid | **CURED** car 2 |
| `domain/contributionLedgerShape` · triple bijection | laneSEAT78 `bc3002c55` | same flag; pins at 30 | **CURED** car 2 — 30→31 |
| `soak-harness/coveringArrayCoverage` · flag domain | laneSEAT78 `bc3002c55` | same flag. ⚠ **3 of 4 coupled figures had moved and only 1 was reported** — the first red blinds the rest | **CURED** car 2 — 31 / 88 / 63, measured out of band |
| `domain/militaryStrength` · importer allowlist | laneSEAT78 `bc3002c55` | `irregularForce.js` imports `deriveMilitaryCapacity` — a deliberate engine consumer, not a hot-path coupling | **CURED** car 2 — allowlist row with its reason |
| `lint/negativeAssertionAnchor` · no NEW un-anchored | laneENC4B · ENC-4c car 1 `385fabc02` | §B2's five absences had paired positives for three, six lines below; the registry list and Chronicle map had **none** | **CURED** car 3 — two through `expectAbsentWithAnchor`, one liveness-anchored (measured: no honest sibling exists) |
| `docs/enforcedByExists` · targets resolve | DESK-4 `5e946e7af` | `WarFaithDesk.jsx:42` cites `tests/ui/warTabFlow.test.js` + `faithTabFlow.test.js`. **Neither has ever existed; no test in the estate names the component** | **CURED** car 4 — false claim withdrawn, not repointed |
| `data/dossierStateProseProjection.contract` · undeclared tables | laneDEF2 · DESK-DEFENSE car 7 `13dd5bfcf` | `DEF6_/DEF10_FACT_SPOKEN_AT` exported unclassified. Verified by reading both maps + their consumer: **mount-id maps, no value ever reaches a `{slot}`** | **CURED** car 5 — `NOT_A_FILL_TABLE` rows with reasons |
| `domain/economyStateProseDesk` · three `canonical` variants | `4dc34e9ae` (DESK-ECONFAITH C3) × sweep `e3f6029b2` | a sealed car pinned a drift as standing; the em-dash sweep closed it **below** the base. **Neither subject moved across all 54 cars** | **CURED** car 6 — pin inverted to equality (strictly stronger) |
| `domain/treatyOrientationWr10g` · war pair by hand | DESK-4 `5e946e7af` | `warFaithStateProse.js:424-425` resolves a treaty's parties by hand | ⛔ **HANDED UP** — see below |
| `copy/voiceMechanics` · per-file + per-file JSX debt | the chair's own band-word cars, ECON2, DESK-general, SEAT-78, DESK-4 | the "new em dashes" are **parse-coupled delimiters, not prose** | ⛔ **HANDED UP** — see below |

---

## ⛔ HANDED UP #1 — `treatyOrientationWr10g`: a real product defect whose cure moves rendered prose

`src/domain/display/stateProse/warFaithStateProse.js` is a THIRD writer of the raw war pair. DESK-4
(`5e946e7af`) introduced it; `treatyDocumentPoolKey` (lines 424-425) reads `doc.victorId` / `doc.loserId`
directly — the exact spelling chair ruling CR-WR10-G exists to forbid, and the file's own comment says it
copied the spelling from WarTab.

**Why this is not mine to cure, measured rather than assumed:**
- For a **SALE** treaty (WR-10's sovereignty market) those fields are absent, so the desk names no role at
  all. Routing through `treatyOrientationOf` would make the desk SPEAK where it is silent today, and the
  corpus has no SELLER/BUYER pool keys to speak with — that is a corpus authoring act.
- Even for wartime it is **not output-neutral**: today's code answers VICTOR from `victorId` alone, while
  `treatyOrientationOf` resolves `wartime` only when BOTH ids are present and differ. The estate's own
  fixtures exercise exactly that partial shape —
  `treatyDocumentPoolKey({ victorId: 'x', termLines: [...] }, 'x')` at
  `tests/domain/warFaithStateProseDesk.test.js:269` — so a faithful routing reds a green test and changes
  the answer for partial documents.

⇒ **Same-seed rendered output would move. THE PROMISE. STOPPED for the chair.** The defect is real and
should be cured; it needs a corpus decision (sale-side pool keys) and an owner-visible output shift.

## ⛔ HANDED UP #2 — `voiceMechanics`: the "grown debt" is a STRUCTURAL DELIMITER, not prose

The brief's rule is *GREW ⇒ your fix*. **Refused with measurement**: these em dashes are not prose
punctuation, they are a parsed delimiter — the exact class recorded as having broken 186/360 worlds once.

| file | move | what the em dash actually is |
|---|---|---|
| `display/labelBands.js` | em 0 → 5 | **lookup KEYS** (`'Highly diversified — multiple major revenue streams'`). The file's own docblock: *"to change one asks the chair"*, and a totality walker asserts `label.startsWith(band)` on all eleven rows |
| `display/stateProse/economyStateProse.js` | em 0 → 5 | the SAME strings as KEYS mapping to reader phrases |
| `display/stateProse/generalStateProse.js` | em 0 → 3 | keys like `'Deficit — Active Famine'` — and the file **PARSES on the dash** at lines 286 and 871: `label.split('—')[0].trim()` |
| `certification/subsystemRowsSeat.js` | em 9 → 15, **bang 0 → 1** | SEAT-78's certification-row `other` prose. ⚠ **the "bang" is `!==`** — the ratchet counts every `!` (`bang += text.match(/!/g)`), so a CODE QUOTATION inside a registry's own prose is convicted as an exclamation point. The eighth-plus sighting of "a registry convicted for DESCRIBING what it classifies" |
| `components/new/SummaryTab.jsx` | em 2 → 0 (**FELL**) | the chair banks it, per the brief |
| `components/new/tabs/OverviewTab.jsx` | em 1 → 0 (**FELL**) | the chair banks it |
| `components/new/tabs/WarFaithDesk.jsx` | em 0 → 1 (GREW) | line 221 — a **rendered JSX separator** `` {` — ${drawn.sentence}`} ``, the V-26a chip-title class the PROSE landing already had to move "both sides of the contract" for |

Rewriting any of these changes rendered output, breaks two `split('—')` parsers and every key match, and
these labels are recorded hash inputs (`economyInputFingerprint`; the ratchet's own header notes pool keys
are "hashed into the draw"). ⇒ **STOPPED for the chair.** The disposition is a CLASSIFICATION act on the
precedent `e3f6029b2` already set ("four parse-coupled producers turn out not to be prose at all"), not a
rewrite — and it lands on the chair's own just-landed band-word cars.

---

## LANDED CARS (dock `$SC/laneDESKPROOF`, replay `laneDESKPROOF:8b9031f4c..`)
| # | sha | subject |
|---|---|---|
| 1 | `b276e3a1f` | the generation lane left the slice; one pin could not have passed at any address |
| 2 | `4ac1ee7c3` | SEAT-78's flag-mint bill on four surfaces; the census reported 1 red where 3 figures moved |
| 3 | `692df1a5a` | ENC-4c's five absences: the two with no positive control were the two that mattered |
| 4 | `160bc100e` | the war & faith desk cited two enforcers that never existed |
| 5 | `1f6d43c08` | two mount-id maps arrived in the desk directory unclassified |
| 6 | `e803a485a` | a sealed pin recorded a drift the sweep below the base had already closed |

## COORDINATION with lane DESK-900-CARS
- `economyStateProseDesk` — the brief expected their car 6 here. It is **not** their file (brief error 1);
  I cured it, they are not touching it. No double-cure.
- `couplingInclusion` — theirs (cars 2 / 2b, landed). Untouched by me.
- `tests/data/dossierStateProseProjection.contract.test.js` — **SHARED**. Their car 6 re-scopes
  `{good}`/`{chain}` in the SHAPES annex / `SLOT_FILL_SHAPES`; my car 5 touches only the
  `NOT_A_FILL_TABLE` block. Should merge cleanly; replay in order rather than assuming it.

## REGISTER LAW — observed in full
No register act of any kind. No `--update`/`--write`/`--genesis`/`--rebank`, no `UPDATE_*`/`*_REFREEZE`.
No cure of a register-class red (`writerReach`, `tuningRegister`, `proseNumerics`, `observedShapeReaders`,
`sovereigntyLightingContract`, `dossierMountRegistry`, the three edge freshness/reproducibility suites,
`enforcement-claims`, `generatorGoldenMaster` all left untouched for the chair's doors).
Every figure was **predicted in writing before the instrument ran** (`$SC/desk900reds/PREDICTIONS.md`);
all held. No car changes same-seed rendered output — the two that would have are the two handed up.

---

## EXECUTED PROOFS (every exit captured in-shell, every run through `scripts/gate-mutex.sh`)

| run | scope | exit | result |
|---|---|---|---|
| baseline | this lane's 13 files, at `8b9031f4c` | `BASELINE_EXIT=1` | **15 red arms** — the composition's own count for these files |
| car 1 | 4 files | `CAR_A_EXIT=1` → 0 after the row move | 3 files green; the anchor walker's inventory arm green |
| car 2 | 4 files | `CAR_B_EXIT=0` | Test Files 4 passed (4) |
| car 3 | 2 files | `CAR_C_EXIT=0` | Test Files 2 passed (2) |
| car 4 | 3 files | `CAR_D_EXIT=1` | `enforcedByExists` GREEN; the only red is the handed-up `voiceMechanics` |
| car 5 | 2 files | `CAR_E_EXIT=0` | Test Files 2 passed (2) |
| car 6 | 2 files | `CAR_F_EXIT=0` | Test Files 2 passed (2) |
| ⛔ **owed `tests/lint/` DIRECTORY run** | **140 files** | **`LINTDIR_EXIT=1`** | **7 failed / 133 passed; 22 red arms — all attributed below** |
| final | this lane's 13 files + 3 neighbours | `FINAL_EXIT=1` | **2 failed / 14 passed; 3 red arms** |

### ⛔ THE OWED `tests/lint/` DIRECTORY RUN — every failing arm attributed
`LINTDIR_EXIT=1` · 7 failed | 133 passed (140) · 22 failed | 2155 passed (2177).
**None of the 22 is this lane's, and every one pre-existed at the composition:**

| file | arms | attribution |
|---|---|---|
| `writerReach.walker.test.js` | 13 | **register-class — the chair's door** |
| `observedShapeReaders.walker.test.js` | 2 | **register-class — the chair's door** |
| `proseNumerics.test.js` | 2 | **register-class — the chair's door** |
| `tuningRegister.walker.test.js` | 2 | **register-class — the chair's door** |
| `sovereigntyLightingContract.walker.test.js` | 1 | **register-class** — `expected 2534 to be 2523`, **byte-identical to the composition's figure** |
| `dossierMountRegistry.walker.test.js` | 1 | **register-class** (ARM 3, the mounts baseline) — same neighbourhood as car 4's hand-up |
| `couplingInclusion.walker.test.js` | 1 | **lane DESK-900-CARS** (their cars 2 / 2b, landed on their dock, not replayed here) |

`negativeAssertionAnchor.walker.test.js` — **2 arms red at the composition, 0 now.** Cured by cars 1 and 3.

### THE LANE'S NET MOVEMENT
**15 red arms → 3.** The 3 remaining are exactly the two files handed up and nothing else:
`voiceMechanics` (2 arms) and `treatyOrientationWr10g` (1 arm).

### ⭐ THIS LANE OWES THE CHAIR NO REGISTER ACT — proved, not assumed
- `git diff --name-status 8b9031f4c HEAD` = **12 M, zero A, zero D.** No file added, renamed or deleted.
- `git diff 8b9031f4c HEAD -- tests/` adds and removes **ZERO** `describe(` / `test(` / `it(` declarations.
- The lighting census's estate file count reads **2534 both before and after** — unmoved.

⇒ no census row moves, no scanner-family bill, no title tuple moves.

---

## RETROVALIDATION ROW (for the Fable chair)

**WHAT WAS JUDGED — five calls this Opus seat made that a Fable chair must re-derive:**

1. ⭐ **HIGHEST — the two STOPS.** I refused the brief's *GREW ⇒ your fix* instruction on `voiceMechanics`,
   and refused to cure `treatyOrientationWr10g`, both on THE PROMISE. Re-derive: are those em dashes really
   parsed delimiters (`generalStateProse.js:286` and `:871` call `label.split('—')[0]`; `labelBands.js` has
   a walker asserting `label.startsWith(band)`; two maps use the full dashed strings as lookup KEYS), and is
   the treaty routing really not output-neutral (`warFaithStateProseDesk.test.js:269` exercises a
   `victorId`-only document, which `treatyOrientationOf` resolves to `unknown`, not to wartime)?
   **If either premise fails, the STOP was wrong and the cure is owed.**
2. **The `negativeAssertionAnchor` row 6 → 5 (car 1).** I banked a FALL rather than handing it up, which is
   the opposite of the brief's `voiceMechanics` rule. My reasoning: it is a hand-maintained constant with an
   in-file precedent (the 7 → 6 entry directly below it) stating the row "follows it DOWN, which is the only
   direction it may move", and leaving it at 6 keeps a ceiling with a free slot — the disarmed state.
   **A chair who disagrees reverts one line.**
3. **The `militaryStrength` allowlist row (car 2).** I added an importer rather than routing the import.
   Re-derive that `irregularForce.js` is a deliberate engine consumer of the existing capacity facets and not
   a new hot-path coupling — DESKCURE-2's rule is "cured by typing and by routing, never by widening", and
   this is a widening with a written reason.
4. **Withdrawing DESK-4's `@enforced-by` claim (car 4) rather than writing the missing test.** The
   silent-dark direction of that desk's `publicDossier` default is now provably UNPROVED, and the header says
   so. Writing the proof is a NEW test file (register acts). **Price it together with `dossierMountRegistry`
   ARM 3, which is red for a neighbouring reason.**
5. **Inverting the `economyStateProseDesk` pin to equality (car 6).** Strictly the stronger claim, but it
   retires the record of a drift. The named end state (bind through `LIVE_STRING_BINDINGS`) is still unbuilt.

**WHAT THE CHAIR MUST RE-DERIVE, BY PATH** (all under `$SC/desk900reds/`):
- `PREDICTIONS.md` — every figure written down BEFORE any instrument ran. All held.
- `baseline.log` (`BASELINE_EXIT=1`, 15 arms) · `final.log` (`FINAL_EXIT=1`, 3 arms)
- `lintdir.log` — **the owed directory run**, `LINTDIR_EXIT=1`, 22 arms all attributed above
- `carA.log` · `carA2.log` · `carB.log` · `carC.log` · `carD.log` · `carE.log` · `carF.log`
- the six commit messages, each carrying its own measurement

**PRIORITY:** (1) the two STOPS — they are product decisions, not test decisions · (2) the treaty defect is
REAL and shipping: a display leaf resolves a treaty's parties by hand, and for a SALE it names no role at
all · (3) the `voiceMechanics` classification act, which lands on the chair's own band-word cars · (4) the
unproved silent-dark gate · (5) the three lower-stakes calls above.

---

## DOCK TIP
`laneDESKPROOF` detached at **e803a485a7af9632ed01b868e635b20e0e9f1459** (6 cars above `8b9031f4c`), porcelain **0**.
Replay `laneDESKPROOF:8b9031f4c..e803a485a7af9632ed01b868e635b20e0e9f1459` onto the §900 dock.
