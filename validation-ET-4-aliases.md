# FABLE VALIDATION — ENTAILMENT TABLE §4, THE 41 ALIAS TRAPS (A-1 … A-41)

Seat: Fable 5.1, sitting 2026-09-12 in retrospective review of `docs/rewrite-retro-2026-09-12/items-ET-3-4.md` §4.
Every `file:line` below was resolved in the dock at `f2da5a3ee` (`scratchpad/dock-f2da5a3ee`, `git log -1` = `f2da5a3ee5743e56b9073570bdd487e1ef1a460f`). CONFIRMED = I executed the read; PLAUSIBLE = reasoning only.

## 0. WHAT WAS CHECKED

- All 41 rows, every column, against the ratified §4 (`verbatim/ENTAILMENT-TABLE.as-ratified.md:546-620`), the fold (`evidence/convert-ET-3-4.md:214-308`), the instrument (`evidence/CONTRADICTION-TABLE.md`) and ADDENDUM 13A item 5 (`verbatim/tables-13.as-run.txt`).
- Verdict tally recomputed: SPLIT 31 · STRUCK 5 (A-6, A-17, A-27, A-31, A-39) · CONTRADICTION 4 (A-24, A-33, A-38, A-40) · SCOPE 1 (A-35) = 41. CONFIRMED.
- Grounds resolved in the dock: `priorityHelpers.js:25-80` (every `inst.*` flag), `safetyProfile.js:25-40, 100-145, 260-380, 455-470`, `defenseInstitutionBuckets.js:78-105, 165-185`, `holderTable.js:60-82, 160-215, 240-250, 268-370`, `governanceNarrative.js:70-145, 490-512`, `defenseGenerator.js:125-335`, `threatDefensePolicy.js:44-52`, `institutionalCatalog.js` (rows at :42, :50, :104, :292-306, :335, :778, :845, :867, :881, :932-952, :1267, :1338-1368, :1436, :1564, :1916-1932, :2182, :2409), `institutionLadders.js:18-28`, `assembleInstitutions.js:278-296`, `institutionServices.js:830-840, 1204-1213, 1320-1328, 1445-1455, 1545-1549, 1558-1563, 1620-1627`, `stressNarrative.js:35-45, 85-100, 130-166`, `factionArchetypes.js:50-90`, `stressFactions.js:1-20, 40-125, 205-245, 355-385`, `occupation.js:868-896`, `supplyChainData.js:868-888`, `institutionVocabulary.js:150-165, 275-285`, `calamityKernel.js:250-278`, `resourceSemantics.js:50-65`, `foodStockpile.js:185-197`, `wiringCensus.js:1298-1325, 1355-1360`, `generationContext.js:193-207`, `generalStateProse.js:818-845, 885-895, 1105-1150, 1220-1270`, `economicState.js:50-62, 690-722`, `foodGenerator.js:118-123`, `stressGenerator.js:95-125`, `rulingStructure.js:158-166, 520-535`, `powerStateProse.js:394-406, 435-442, 852-965`, `EngineSections.jsx:228-236`, `settlementPolitics.js:145-152, 440-470, 585-592`, `npcAgency.js:410-415`, `religionState.js:50-60, 148-157`, `pantheon.js:52-80`, `faithPanelModel.js:37-46, 187-192`, `criminalOpRole.js:35-66`, `defenseDisplay.js:158-195, 225-235, 328-352`, `corruption.js:470-482`, `economyStateProse.js:908-923`, `treatyOrientation.js:164-178`, `peaceTermsCatalog.js:158-185`, `occupationStatus.js:36-60, 80-105`, `customContentSemanticAuthority.js:20-62`, `npcGenerator.js:1505-1540`, `npc/thorpSecondRole.js`, `historyData.js:1180-1186, 1555-1565`, `stressTypes.js:9-22, 150-160`, `stressorsCore.js:53-65, 164-184, 290-300`, `urbanFabricKernel.js:668-676`, `warStatus.js:45-53, 100-118, 328-337`, `activeConditions.js:312-316`, `stressorsStateProse.js:336-360, 420-470`, `conquestIntent.js:1-12`, `stressorDynamics.js:45-55, 740-800, 824-840, 875-895`, `archetypeCatalog.js:20-46`, `stressorGates.js:444-468`, `WorldPulseData.js:215-245`, `threatAssessment.js:35-135`, `defenseStateProse.js:404-461, 1262-1280`, `cultureProfiles.js:181-200`, `OverviewTab.jsx:245-262`, `PowerTab.jsx:160-170`, `DefenseTab.jsx` (grep).

## 1. THREE FACTS THE FOLD DID NOT STATE, AND SEVERAL ROWS TURN ON THEM

**Fact A — the safety panel's denial is BAND-GATED, not universal.** `safetyProfile.js:261-310` (CONFIRMED): "There is no meaningful guard presence." prints only in the `Unsafe` band (`effectiveSafety` 0.6–1.2) and "There is effectively no law enforcement." only in `Dangerous`, and only where none of `hasGarrison / hasWatch / hasMilitia / hasMercenary` is set. In `Very Safe`, `Safe` and `Moderate` a guardless town prints "law enforcement", "Local enforcement is present and responsive" and "Locals watch over the main paths". The contradiction table's F1-01/F1-04 say `:300, :309 print the denial beside the prose` as if always. They do not. The FLAG is the record; the printed sentence is a rendering that appears on two of five bands.

**Fact B — `hasMilitaryInst` includes `walls` and `citadel`** (`priorityHelpers.js:45`, CONFIRMED), so on a walls-only town the guard-effectiveness branch fires with `lawRef = 'local guard'` (`safetyProfile.js:319-326`) and prints e.g. "The local guard maintains standard patrol coverage" (`:373-375`). The ENGINE names a guard on a walls-only town. Meanwhile `threatAssessment.js:117-120` prints, on every walls-only town, "Walls present but no organized military force to man them", and on a plagued one `:80` "no organized force to sustain a watch rotation … there are not enough for sustained watch". These are rendered on the Defense tab (`DefenseTab.jsx:6, :27` mounts `buildThreatAssessment`).

**Fact C — the origin record's neighbour NAME never reaches a dossier tab.** `attackerSettlementId / sponsorSettlementId` are read by name only in `src/components/map/WorldPulseData.js:215-245` (rendered by `WorldPulsePanel`, `RealmDashboard`, `RealmInspector` — map-side). Zero readers of `originContext` in `src/components/new`, `src/components/settlement`, `src/components/dossier`, `src/pdf` (grep, CONFIRMED). DS-STR-2's ORIGIN pool fills only `{settlement}` (`stressorsStateProse.js:459`). R-12 binds floor 1 to the DOSSIER (the PDF's tabs). So the neighbour's name is NOT a same-page field; only the VARIANT (`declared_war` etc.) is.

## 2. ROW-BY-ROW

| id | verdict | (a) old law verbatim | (b) ground real / engine splits as stated | (c) permission safe | VALIDATION |
|---|---|---|---|---|---|
| A-1 the watch | SPLIT | matches ratified (pre-13A cell; see F5) | CONFIRMED: `hasWatch = ['town watch','city watch','professional city watch']` `:48`; `hasMilitaryInst` includes `watch` `:45`; `hasGarrison` includes `professional city watch` `:46`; bucket `watch` `:95-97`, `garrison` `:88-91`; holder kind `watch` = Crime reporting/response/Missing persons backed by Professional city watch + Town watch `holderTable.js:317-323`; `watchLabel` `safetyProfile.js:30-35`; `deriveDefenseGroupLabel` substring `watch` `governanceNarrative.js:502` hits `Watchtower` `institutionServices.js:1561`; world classes `stressorDynamics.js:50-51`; order purse "watch wages, court and gaol" `defenseGenerator.js:246-247`; `forceCorePoolKey` garrison first `defenseStateProse.js:1270`. All as stated. | KEPT half safe (F1-01; R-6 makes the FLAG on THIS town the test, so a custom watch at village is fine). Struck half = house style. ⚠ `'local watch'` is consumed only inside `inst.hasWatch ?` arms (`safetyProfile.js:264-269, 281, 288, 297, 307, 321-322`) and `hasWatch` needs a `town watch`/`city watch` name — none below the town block (`institutionalCatalog.js:923+`). So the string is DEAD below town on shipped rosters; L-121's "the engine's OWN generic below town — use it" is a misreading and collides with this row (fidelity F3). | **OK** — CHALLENGE only to L-121 (outside slice). |
| A-2 the garrison | SPLIT | matches | CONFIRMED: bucket `garrison` = garrison, barracks, professional guard, professional city watch, multiple garrison `:88-91`; `Barracks` "Housing for guards or small garrison" `:1363-1366`; ladder Barracks→Garrison `institutionLadders.js:23`; occupier soldiers "varies" `stressFactions.js:45`; `occupierStillPresent` `occupation.js:882-895`; `Garrison` required at city `:1925-1929`. | ⛔ KEPT clause "a garrison BODY where the bucket resolves only `Barracks` (HOUSING) … contradicts the roster" is OVERRULED by the fold's own reconciliation **R-3** ("STRUCK. 'The garrison' is lawful wherever `inst.hasGarrison` or the garrison bucket resolves, Barracks included") and by the page: `priorityHelpers.js:46` fires on `barracks`, the safety panel prints "The garrison patrols" off the flag (`safetyProfile.js:288`), `threatAssessment.js` prints "Walls and garrison" off the bucket. Nothing on the dossier denies "the garrison" on a Barracks town; the archive row bars a face the page agrees with. The "resolves nothing" clause and the occupation clause (F1-02, F1-121) are right. | **REVERSE (kept half narrowed):** SPLIT stands; strike the Barracks clause; field `priorityHelpers.js:46` + `safetyProfile.js:288`; floor 1 in the NEGATION direction (F1-25). |
| A-3 the guard | SPLIT | matches (the 13A-superseded "DISPUTED" cell; see F5) | CONFIRMED `governanceNarrative.js:506` mints "The guard" on the non-small guardless town; `safetyProfile.js:300` / `:310` print the two denials; `threatDefensePolicy.js:47` admits `guard`; `hasMilitaryInst` includes `guard` `:45`; `communityMilBase` `defenseGenerator.js:138-156`. | KEPT half safe but MIS-FIELDED: the denial sentence is band-gated (Fact A) and the power label "The guard" is minted on the SAME guardless town, so where both print the page contradicts ITSELF (NF-3 — a wiring row under R-1), and where only the label prints "the guard" is the engine's own word. The stable field is `inst.hasMilitaryInst` false (F1-04's own ground) — and by Fact B a walls-only town has it TRUE and prints "The local guard …", so "the guard" there is engine-backed. Permission "use it where a force row resolves, else write the absence" is safe but narrower than the table (F1-04: "Where ANY law body stands"). | **OK** — CHALLENGE: re-ground the kept half on `hasMilitaryInst` (`:45`) with the band caveat; the two-panel collision is NF-3 wiring, not a writer's bar. |
| A-4 militia / muster / levy / roll | SPLIT | matches | CONFIRMED: `Citizen militia` `exclusiveGroup: 'civilianDefense'`, `Town watch` required in the same group `:1340-1354`, splice `assembleInstitutions.js:283-293`; muster kind's twelve fields `holderTable.js:188-199`; ONE keeper, `Muster training` `on:false, p:0.5` `institutionServices.js:836-837`, note at `holderTable.js:279-288`; `Household levy` thorp `:104-110` matches neither `hasMilitia` (`['citizen militia','militia']` `:47`) nor the bucket `:92-93`; census kind backed by Democratic assembly + Royal seat `:289-295`. | KEPT safe (F1-03; R-8). ⚠ a `Household levy` thorp has `forces.militia.present` FALSE while its roster row says it "musters with hunting bows" — the fold files this as **W-02** (wiring). The kept half should carry that rider or a refuter will fail "the militia" on the one tier whose roster prints a levy. Struck half (levy/conscription spellings; "the muster" as class word) = style: "conscription" is `stressTypes.js:156`'s own word. | **OK** — CHALLENGE (W-02 rider). |
| A-5 mercenary company / charter hall | SPLIT | matches | Substance CONFIRMED: bucket `mercenary` = mercenary company / quarter / hired muscle `:98-100`; `hasMercenary` adds hireling hall, free company hall, veteran's lodge `:49`; `Hireling hall` = "Job board for torchbearers … porters" (`institutionalCatalog.js:1436-1439`); `Veteran's lodge` = "Informal security, bar brawls" (`:881-884`). ⚠ CITATIONS WRONG: the ground cites `:1436, :2182` for the two rows; `:2182` is `Mercenary quarter` ("Organized sellsword companies. Major forces (hundreds to thousands)"); Veteran's lodge is `:881`. F1-05 repeats the swap. | KEPT safe (F1-05; W-01 flag-vs-bucket wiring). Struck half = style. | **OK** — CHALLENGE: fix the citation (`:881`, `:1436`). |
| A-6 soldiers / the men / the armed | STRUCK | matches; old law's 13A strike ground was "asserts manning, NOT ENTAILED, and a wall" | CONFIRMED `stressNarrative.js:149` "the men" (gendered summary word); `factionArchetypes.js:55` `watch`/`law` → MILITARY. | ⛔ UNSAFE as written. Worst sentence the cell licenses: **"The men on the wall keep their rotation through the night"** on a walls-only town. The Defense tab prints beside it "Walls present but no organized military force to man them" (`threatAssessment.js:119-120`, every threat level) and on a plagued town "no organized force to sustain a watch rotation … not enough for sustained watch" (`:80`); DS-DEF-2 keys the town `walls with NO force` (`defenseStateProse.js:461`); `forces.garrison/militia/watch.present` all false (`priorityHelpers.js:46-48`). Those strings are renderings of the FLAG, exactly as `safetyProfile.js:300` is — so by the fold's own standard they are the record. The `A-7` cross-reference guards the WALL; nothing in the cell guards the MANNING, and the 13A strike named manning first. "Someone's nephew with a billhook" is lawful (unorganised; L-24's kept permission); an organised rotation of men is not. The archive's own closing criterion ("a strike whose safety depends on the reader also having read another row is mis-scoped") applies. | **REVERSE → SPLIT.** KEPT: an ORGANISED or STANDING manning (a rotation, a force, soldiers) on a town where no force row resolves contradicts `forces.*.present` (`priorityHelpers.js:46-48`) and the printed "no organized military force to man them" (`threatAssessment.js:119-120`; `:80`) — floor 1 (F1-01/F1-02/F1-25). STRUCK: the gendered-word bar and "the town's force" prescription. |
| A-7 wall / walls / perimeter | SPLIT | matches | CONFIRMED: bucket `walls` `:84-87`; `hasWalls` includes palisade/earthwork `:52`; fortification chain feeds every wall row `Quarried stone` `supplyChainData.js:873-884`; `Town walls` "Stone walls" `institutionVocabulary.js:157`, `City walls` "Masonry" `:162`, `Palisade` "sharpened stakes" `:278`, `Palisade or earthworks` "wooden … or earthen" `:155`; demotion along `UPGRADE_CHAIN_PAIRS` `calamityKernel.js:259-275`; `threatAssessment.js:59, :66` hardcode "Palisade" on any walls row (W-09). | KEPT (i)(ii)(iii) safe; (ii) is re-grounded by R-7 on the row's DESCRIPTION, not the chain — the archive's ground cites the chain (`873-884`), which R-7 reserves for the SOURCE bar (F1-33). Struck "perimeter" geometry bar: `hasWalls` is TRUE on a Citadel-only or Gates-only roster (`:52`) and DS-DEF-5 prints walls PRESENT, and `Citadel`'s own vocabulary presumes outer walls ("when the outer walls have fallen", `:165`) — no dossier field denies a perimeter, so the strike is right; ⚠ F1-07's parenthetical ("A CITADEL is inner and a GATE is a point — neither is a line") re-imposes the geometry bar the row struck — the TABLE over-reaches, not the row. | **OK** — CHALLENGE to F1-07's parenthetical (table), and re-cite (ii) to `institutionVocabulary.js:157, :162` per R-7. |
| A-8 the gate(s) | SPLIT | matches | CONFIRMED: `hasGates` includes `palisade` `:53`; gate note `safetyProfile.js:462-464` ("The settlement's gates and walls are checkpoints" / "no gates to bribe and no checkpoints to avoid") — ⚠ printed ONLY under `inst.hasSmuggling` (`:460`); `Town watch` Gate duty `on:true p:0.8` `institutionServices.js:1324`; `Palisade or earthworks` "Gated entry … Controlled access through the perimeter" `:1546`; toll-bar kind backed by Gates (if walled) / Major Port / Town council `holderTable.js:303-309`. | KEPT safe (F1-08; the FLAG is the record whether or not the smuggling note prints). Struck half (gate prose on a palisade town) is RIGHT and the closing §2's objection fails: the fold treats engine FREE PROSE as a defect (L-30, L-120) but a FLAG as the record — `hasGates` is a flag, and the hamlet/village palisade row carries a "Gated entry" SERVICE. A gate on a palisade town agrees with two fields. (W-08 records the Gate-duty-vs-"no gates" collision as wiring.) | **OK** — CHALLENGE: name the smuggling gate on the printed denial. |
| A-9 mill / workings / mine / quarry | SPLIT | matches | CONFIRMED: `foodStockpile.js:194` `has('mill')` raises capacity for any mill; `resourceSemantics.js:57-62` recovery modes (positional/manual); civic class `craft` carries `mill` `wiringCensus.js:1316`. | KEPT safe (F1-15, F1-46). Struck "the workings" = style. | **OK** |
| A-10 granary / stores / reserve | SPLIT | matches | CONFIRMED: `store` vs `storehouse` split `wiringCensus.js:1308, :1320-1322`; `hasGranary` stem `granar` `:63`; capacity fall-through 1.5/2.0 `foodStockpile.js:190-193`. | KEPT safe (F1-09). ⚠ W-07: a granary READING prints with no row — wiring, the kept half is on the BUILDING only, correctly. Struck (T-F12 one-object rule; "never both") = instrument/style. | **OK** |
| A-11 port / harbour / sea / quay | SPLIT | matches | CONFIRMED: `maritimeSupported` / `portKind` `generationContext.js:195-205`; `PORT_INFRA_RE` word-bounded `priorityHelpers.js:32, :61`; `deep_harbour` positional `resourceSemantics.js:57-62`. | KEPT safe (F1-18). Struck (quay/harbour prescriptions) = style — "the harbour" on a river dock is denied by no field; on an inland town `terrainType` denies water (kept). | **OK** |
| A-12 road / approach / pass / crossroads / river | SPLIT | matches | CONFIRMED: `SCENE_OF_ROUTE` `generalStateProse.js:820-826`; `ORIGIN_POOL_OF_ROUTE` has no `road` (else-arm) `:887-892`; `road` holder kind → Listening post / Waystation `holderTable.js:348-354`. | KEPT safe (F1-102). Struck ("the approach"; class-guard) = style/instrument. | **OK** |
| A-13 fields / harvest / farm / land | SPLIT | matches | CONFIRMED: `Agricultural Rents` tier default `economicState.js:55-60`; `hasFarmland`/`hasSubsistence` `foodGenerator.js:120-121`; `hasGrain`/`hasFish` `stressGenerator.js:99-101`. | KEPT safe (F1-102). Struck = style. | **OK** |
| A-14 church / temple / parish / shrine | SPLIT | matches | CONFIRMED: `hasChurch` over eight kinds incl. `shrine`, `priest` `:65`; `Wayside shrine` "No resident clergy" `:42-46` and "No clergy" `:292-296`; `Access to parish church` "Walk 2-5km" `:50-54` / "Travel to village church" `:300-304`; `Priest (resident)` with no building `:778-781`; `temple` class includes `patron` `wiringCensus.js:1312`; `religious` regex `stressorDynamics.js:52`. | KEPT safe (F1-11; F3-02). Struck ("a house of the faith"/"the parish") = style; the deity floor survives in §D. | **OK** |
| A-15 hospital / healer / sick-house | SPLIT | matches | CONFIRMED: `hasHospital` = hospital, monastery, healer, friary `:64`; `Healer (divine, 1st level)` `:845`; `Monastery or friary` "May operate hospital/school" `:1267-1270`. | KEPT safe (F1-14). ⚠ Permission "a woman who knows herbs" — on a `famine`/`plague_onset`/`mass_migration` town the engine emits a NAMED `Healer` NPC with a disposition (`npcGenerator.js:1523, :1529, :1533`); F3-06's rider applies. | **OK** — CHALLENGE (F3-06 rider). |
| A-16 court / law / magistrate / assembly | SPLIT | matches | CONFIRMED: `hasCourtSystem` includes town hall / city hall / democratic assembly `:55`; court kind backed by Courthouse / Multiple court buildings `holderTable.js:324-330`; office kind note "citing the speaker" `:355-363`; `Town hall` "Dispute arbitration … before a magistrate" `p:0.8` `institutionServices.js:1625`. | KEPT safe (F1-12; R-4: word free, criminal procedure the finding). ⚠ "the magistrate at the table" on a `recently_betrayed`/`succession_void`/`insurgency` town where a NAMED `Chief Magistrate` is emitted (`npcGenerator.js:1527, :1530, :1532`) — F3-06 rider. | **OK** — CHALLENGE (F3-06 rider). |
| A-17 prison / a place of confinement | STRUCK | matches | CONFIRMED: `hasPrison` = prison, stocks, large prison, massive prison `:54`; `Small prison/stocks` "Holding cells and public punishment" `:1564-1567`; vocabulary "Holding cells and public stocks" `institutionVocabulary.js:285`; `stocks` appears in exactly ONE catalogue row name (grep); DS-DEF-6 prints `Legal Infrastructure: None` where `hasPrison` false (`defenseDisplay.js:230-231`). | Safe on shipped data; the BODY bar (a prison where `hasPrison` false) survives at **F1-13**, which folds A-17 — so the alias-grain strike removes only the word. Closing §3's SPLIT case is right in principle (a stocks-only row would set the flag from a pillory) but no such row ships and a custom row never reaches the flag (A-40). Residual: a strike true of today's catalogue with no invariant pinning it. | **OK** — CHALLENGE: pin the invariant (every `hasPrison` keyword row's desc names cells) with a test, or the strike silently dies the day a pillory row ships. |
| A-18 hall / council / seat / chamber / office | SPLIT | matches | CONFIRMED: `hall` class `wiringCensus.js:1314`; "No hall-name producer exists" `powerStateProse.js:868-872`; thorp/hamlet governance is a consensus `institutionServices.js:1448-1453`, `rulingStructure.js:161-163` (labels `Household Council` / `Elder Council` / `Elected Reeve`); nobleLabel ladder `:524-531`. | KEPT safe (F1-20; F1-53). ⚠ note the ENGINE prints "Household Council" as the thorp's `governingName` (`rulingStructure.js:162`) — "the council" is the engine's word there; only the PLACE is denied. Struck (W21 baked-noun bar as wording; one-sense-per-block) — see §4: the baked-noun bar was the POOL-grain safety mechanism, which this row's kept half cannot enforce alone. | **OK** — see §4 (class question). |
| A-19 record / rolls / books / clerks / treasury / tradition | SPLIT | matches | CONFIRMED: `HOLDER_KINDS` `holderTable.js:78-80`; `HOLDER_RECORDS` `:271-364`; `tradition` has NO instantiating row `:338-347`; treasury backed by seven incl. Village headman / Village reeve / Town hall `:272-278`; office kind city/town `:355-363`. | KEPT safe (F1-24). Struck: W24's noun list (style) and the person bar → "the reeve who will not open the book". No mandated NAMED `Reeve` exists (`TIER_MANDATORY_ROLES` `npcGenerator.js:1511-1518`; `thorpSecondRole.js` returns Fisherman/Woodcutter/Shepherd/Miller), so F3-06 does not bite this example; on a village whose `governingName` is `Elected Reeve` the reeve is the SEAT, and a disposition on the seat is denied by no field. Safe. | **OK** — CHALLENGE (low): carry the F3-06 rider generically ("clerk, collector, reeve" are fine; the tier's singular named office is not). |
| A-20 market / fair / square / exchange | SPLIT | matches | CONFIRMED: `MARKET_NAME` `generalStateProse.js:1109`; `marketPoolKey` NO-MARKET `:1138-1147`; `hasMarket` includes `fair` `:56`; `Annual fair` "Regional merchants" `:947-950`; market holder = Market square alone `holderTable.js:310-316`, Weekly market also treasury `:276`. | KEPT safe (F1-10, F1-116). Struck = style. | **OK** |
| A-21 guild / trades / craft houses | SPLIT | matches | CONFIRMED: `hasGuild` bare `guild` `:57`; `NAME_RULES` criminal before merchant `factionArchetypes.js:69-79`. | KEPT safe (F1-16). Struck = style. | **OK** |
| A-22 quarter / district / ward | SPLIT | matches | CONFIRMED: `Mages' district` "Quarter inhabited by arcane practitioners: towers, workshops, libraries" `:2409-2412`; `Mercenary quarter` "Organized sellsword companies. Major forces (hundreds to thousands)" `:2182-2185`. | KEPT safe (the row's own desc fixes the scale). Struck ("in most quarters as idiom only") = style; "no ward model" is an absence. ⚠ `Mercenary quarter`'s desc carries a MAGNITUDE ("hundreds to thousands") — repeating it is F2-01 unless the read hands it. | **OK** |
| A-23 institution / house / workshop | SPLIT | matches | CONFIRMED: `craftInstitutionFill` fills from the chain row `generalStateProse.js:1223-1268` (plural head word refused); `TRADE_DEPENDENCY_NEEDS` `economicState.js:692-720`. | KEPT safe (L-30 family, NF-8). Struck = style. | **OK** |
| A-24 the siege | CONTRADICTION | matches | CONFIRMED all six: `external_threat` "News of approaching danger divided the community" `historyData.js:1182-1184`, title `The Siege` `:1565`; `under_siege` `stressTypes.js:11-21`; `siege` `stressorsCore.js:55-63`; `siege_repairs` from `siege_lifted` `urbanFabricKernel.js:672-673`; phantom fronts `warStatus.js:45-53`, a marching army counts `:103-116`; `siege_lifted` `activeConditions.js:314`. | Safe (F1-42, F1-81). ⚠ two things the row should carry: (1) the history template's `lastingEffects` include "Watch towers and muster drills maintained since the alarm" (`historyData.js:1185-1186`) — an ENGINE string asserting towers and drills on a roster that may carry neither (an L-120-class wiring finding); (2) the permission's "the one at the gates" needs `hasGates` (F1-08). | **OK** — CHALLENGE (wiring note on `lastingEffects`; gate rider). |
| A-25 crisis / trouble / this | SPLIT | matches | CONFIRMED the block is play-time and the CALLER selects the world stressor (`stressorsStateProse.js:355-360, :455-459`); the `.find()` is in the Overview caller (W-17 records it as a PAGE-grain F1). | KEPT safe (W-17). Struck (anaphora construction) = style. | **OK** |
| A-26 bound labour / enslaved / servile / overseers | SPLIT | matches | CONFIRMED: generator `slave_revolt` asserts an enslaved population and a slave market `stressNarrative.js:135-138`; world `slave_revolt` `deprecated: true` with the comment "the sim has no slavery substrate" `stressorsCore.js:166-181`; `servile_uprising` gated on two scores `stressorDynamics.js:826-834` with `reason: 'Bound and broken labor rises against the masters who hold it.'` `:837`. | ⛔ KEPT clause "an enslaved population at `ORIGIN: servile_uprising` contradicts the world model's own statement" rests on a SOURCE COMMENT and a `deprecated` flag on a DIFFERENT type (`slave_revolt`), not on any field, flag, band, label or roster row printed on the dossier. The engine's OWN record for this variant — its name and its `reason` string — positively asserts bound labour. By the re-cut's test ("what would have to be false … a field on this town's dossier") the answer is "nothing"; and floor 4 bars denying what the engine MODELS, not asserting what it does not model. The only real floor-1 question is a `Slave market` BUILDING where the roster carries none (F1-10/F1-108). L-120 carries the same over-reach for the reason string. | **REVERSE (kept half narrowed):** SPLIT stands; the `servile_uprising` clause is NOT a finding; replace with "a slave market / slave-trade house where no `Slave market` row resolves contradicts the roster (`institutionalCatalog.js:1033, :1684, :1691`)". Floor 1 on the building only. |
| A-27 crown / kingdom / realm / border / foreign | STRUCK | matches | CONFIRMED: no polity entity — `conquestIntent.js:4-7` uses "realm" as prose ("A realm that could take its neighbour"); `P.includes('Royal Authority')` switches faction names `stressFactions.js:60, :78, :122`; hostility is a neighbour edge `stressorDynamics.js:742-752, :776-786`; `originRegion` fallback `'realm'` `stressorsCore.js:296`; "in a kingdom at war" `stressNarrative.js:147`; `'occupation authority'` fallback `warStatus.js:333-335`. | SAFE, and the closing §1 case against it is weaker than written on THREE counts: (1) fidelity F1 — the cell's own last clause ("do not rename a neighbour the record names") bars the constructed breach; (2) **Fact C** — the neighbour's NAME is a map-panel field, not a dossier field; the dossier carries only the variant key; (3) "realm" and "kingdom" are the engine's own words (`conquestIntent.js:4`, `stressNarrative.js:147`, `stressorsCore.js:296`). Residual risks the cell does not name: NF-9 (`'occupation authority'` is a fallback string, never a proper NAME) and F3-05 (a "crown" is north-European furniture on a `steppe`/`mesoamerican` profile — `cultureProfiles.js:181-200` names judges, market officers and military households for `arabic`, no king; PLAUSIBLE, not a denial). A custom world with no monarchy: the record is silent, and silence is permission by the owner's own word. | **OK** — CHALLENGE: add the NF-9 and F3-05 riders to the cell; strike the closing §1's "only a cross-reference stands between" sentence (it is false on the archive's own page and on the dock). |
| A-28 creditor / collector | SPLIT | matches | CONFIRMED `Crown Creditors (Noble Coalition)` vs `Creditor's Representative` `stressFactions.js:76-84`; four creditor kinds `stressNarrative.js:89-90`. | KEPT safe. Struck → "the collector at the door and the town's dislike of him". ⚠ every `indebted` town emits a NAMED `Moneylender` NPC (`npcGenerator.js:1526`) with a personality and a secret; "the town's dislike of him" on that page reads as the Moneylender's — F3-06's rider, unnamed in the cell. | **OK** — CHALLENGE (F3-06 rider naming `Moneylender`). |
| A-29 camps / newcomers / arrivals | SPLIT | matches | CONFIRMED `getInstFlags` IS imported (`stressFactions.js:7`) so the `Departure Committee` branch is LIVE at `economyOutput < 50` (`:209-241`) — the stressNarrative comment about a dead ESM `typeof` applies to ITS file, not this one; `stressNarrative.js:158-164` always says arrivals. | KEPT safe (F1-75, NF-10). Struck ("no camp model") = absence. "The camp outside the walls" needs `hasWalls` (A-7). | **OK** |
| A-30 mages / hedge wizards / casters / wards | SPLIT | matches | CONFIRMED `defenseGenerator.js:292-331` (magical score; `hasArcaneGuild +8 // organized wards, counterspells`); `defenseDisplay.js:225-227` prints "Detection, wards, counterspell." where `hasMagicInst` (an alchemist shop sets it, `:69`) and "Conventional defense only. Invisible threats go undetected and unanswered." where not. | KEPT safe and correctly LIMITED to "the magic arm prints absent" — because where the arm prints present the ENGINE asserts wards off an alchemist, and a writer agreeing with it cannot be failed. Struck = style. | **OK** |
| A-31 war layer / aggressor side / recovery / counterforce | STRUCK | matches | CONFIRMED `archetypeCatalog.js:22-45` four groups; family pools dark, no `family` field `stressorsStateProse.js:338-352`. | Never bound a writer (dark pools); permission "—" (wiring row, O-13). | **OK** |
| A-32 faction / the houses / a ruler | SPLIT | matches | CONFIRMED `seat`/`faction` both from `governingName` `powerStateProse.js:860, :875-876, :900-901, :909, :953, :962`; `nobleLabel` ladder (Manor Household / Landed Gentry / Noble Families / Noble Houses) `rulingStructure.js:524-531`; noble name rule `house\s+[a-z]` `factionArchetypes.js:82`; incumbent = a faction name `EngineSections.jsx:232`. | KEPT safe (F1-119). Struck → "an unnamed woman who speaks for the guild". ⚠ `Guild Master` is a NAMED mandated NPC under `famine`/`mass_migration`/`wartime` (`npcGenerator.js:1523, :1533, :1534`), `Wealthiest Merchant` at city+ (`:1516-1517`); a woman "who speaks for the guild" on those pages is F3-06's collision. | **OK** — CHALLENGE (F3-06 rider naming `Guild Master`). |
| A-33 a majority / a working majority | CONTRADICTION | matches | CONFIRMED `RULING_CONSOLIDATION_FLOOR: 0.4` `settlementPolitics.js:149`, `:587-589`; the engine's OWN glue detail "seats and revenue traded for a working majority" `:455`. | Safe (F1-51). ⚠ the page contradicts itself (a printed detail string vs the share) — R-1: the SHARE is the field, the detail is prose; a face repeating the detail is charged, and W-16-class wiring should be filed for the string. | **OK** |
| A-34 a leader / the top rung | SPLIT | matches | CONFIRMED "no person-level captured-role record" `powerStateProse.js:394-404`; `capturePoolKey` `capture → 'capture reached a LEADER'` `:437-439`; `leader_champion` at dot-rank ≥3 `npcAgency.js:411-412`. | KEPT safe. Note the ladder's top rung IS a named NPC seat, so "the figure at the top rung" on a DS-POW-3 read is a NAMED person and the cell rightly keeps F3-01. | **OK** |
| A-35 creed / doctrine / patron / cult | SCOPE | matches | CONFIRMED doctrine glue on ≥2 `religious` OR `arcane` `settlementPolitics.js:443-446`; `ARCHETYPE_END` patron = criminal/outsider `:463-467`; standing thresholds 15/30, hysteresis 4 `religionState.js:51-53, :150-155`; pantheon tiers `pantheon.js:54-57, :78`; `legitimacyBand` `faithPanelModel.js:39-44`. | Safe; the cell predates §D (ADDENDUM 15 as corrected) whose D-01…D-18 now govern the faith desk and are NARROWER than "nothing said of the god" only in that the followers may act per axes. | **OK** |
| A-36 underworld / crime / fence / black market | SPLIT | matches | CONFIRMED `criminalOpEcon` roles `criminalOpRole.js:38-49`, `CRIMINAL_OP_ROLES` `:56-64`; `deriveCriminalStructure` returns `null` when nothing matches `defenseDisplay.js:183-195`; `CAPTURE_LADDER` `corruption.js:474`; `{faction}` unfilled on the economy desk `economyStateProse.js:910-921`. | KEPT safe (F1-37, F1-38). ⚠ "'no crime' off a `null` structure contradicts the values printed" — a `null` structure prints NOTHING; and `PowerTab.jsx:170` labels capture `none` as "No organised crime". The clause needs the field that actually prints (the capture chip, or a criminal income line). | **OK** — CHALLENGE (name the printing field). |
| A-37 treaty / peace / victor / term / tribute | SPLIT | matches | CONFIRMED `victorId`/`loserId` are `receiverId`/`giverId` on a WR-10 sale `treatyOrientation.js:171-176`; `TERM_CATALOG` families and `executor: 'seam'` on `puppet_seat`, `disclosure`, `non_intervention` `peaceTermsCatalog.js:160-183`. | KEPT safe (F1-85, F1-87). Struck = construction. | **OK** |
| A-38 contested | CONTRADICTION | matches | CONFIRMED `legitimacyBand` 'contested' `< 0.25` `faithPanelModel.js:44`; `PATRON_FLIP_MARGIN: 6` `religionState.js:56`; occupation `contested` "has not yet taken hold" `occupationStatus.js:40`. | Safe (F1-91). | **OK** |
| A-39 the civic-object classes (T-F12) | STRUCK | matches | CONFIRMED `CIVIC_OBJECT_CLASSES` `wiringCensus.js:1301-1323`; `objectClassesOf` reads the pool KEY `:1358`. | The STRIKE of the instrument guard is right (it read keys, not prose; the wave gate no longer judges prose). ⛔ The PERMISSION CELL is unsafe as phrased: "Put the garrison and the watch … in one unit where both are true." At city, `Professional city watch` is ONE row that sets BOTH `hasGarrison` (`:46`) and `hasWatch` (`:48`) and sits in BOTH buckets (`:89, :96`); `deriveArmedForces` de-duplicates by name (`defenseDisplay.js:335, :347`) so the page prints ONE force. "Both are true" is satisfied by the flags while "the garrison relieves the watch" names two bodies — exactly F1-29's breach, which survives in the table via L-26/OV-1, but the cell hands the writer the opposite instruction. | **OK on the verdict** — CHALLENGE (strong): amend the cell to "where both resolve to DISTINCT roster rows"; field `defenseInstitutionBuckets.js:89, :96` + `defenseDisplay.js:335`. |
| A-40 custom-content rows | CONTRADICTION | matches | CONFIRMED `nativeSemanticName` returns `''` for materialised custom content `customContentSemanticAuthority.js:40-48`; `deriveCriminalStructure` reads raw `.name` `defenseDisplay.js:185`. | Safe (F1-30; R-6). | **OK** |
| A-41 the record-holder kinds | SPLIT | matches | CONFIRMED `holderTable.js:243-247`, `:271-364` (non-partitioning: Weekly market market+treasury, Village headman elders+treasury, Town council elders+toll-bar, Democratic assembly census+court flag). | KEPT safe (F1-24). Struck ("the generic class word is never a citation") = wording. | **OK** |

## 3. REVERSALS (three; two are narrowings of a KEPT clause, one is a STRUCK → SPLIT)

| id | recorded | should be | field:line | the false sentence licensed / the true sentence refused |
|---|---|---|---|---|
| A-6 | STRUCK | SPLIT (manning kept; gendered word and "the town's force" struck) | `priorityHelpers.js:46-48` (`forces.garrison/militia/watch.present` false); printed at `threatAssessment.js:119-120` "Walls present but no organized military force to man them" and `:80`; DS-DEF-2 key `walls with NO force` `defenseStateProse.js:461`. Floor 1 (F1-01/F1-02/F1-25). | LICENSED, FALSE: "The men on the wall keep their rotation through the night" on a walls-only town whose Defense tab says no organised force mans them. CONFIRMED. |
| A-2 | SPLIT (kept: garrison BODY where bucket resolves only `Barracks`) | SPLIT with the Barracks clause STRUCK (R-3 governs) | `priorityHelpers.js:46` (`barracks` sets `hasGarrison`); `defenseInstitutionBuckets.js:89`; `safetyProfile.js:288` prints "The garrison patrols" off the flag; `institutionalCatalog.js:1366` "Housing for guards or small garrison". Floor 1 in the negation direction. | REFUSED, TRUE: "The garrison drills in the barracks yard" on a Barracks town — the page's own safety line says the garrison patrols. CONFIRMED. |
| A-26 | SPLIT (kept: enslaved population at `ORIGIN: servile_uprising` contradicts the model) | SPLIT with that clause narrowed to the ROSTER (a slave market building where no `Slave market` row resolves) | `stressorDynamics.js:833-837` — the variant's own `reason` is "Bound and broken labor rises against the masters who hold it."; the "denial" is a comment at `stressorsCore.js:175-180` on a different type. Floor 1 reaches only `institutionalCatalog.js:1033, :1684, :1691`. | REFUSED, NOT DENIED: "The bound labour of the quarries has risen" on a `servile_uprising` town — the engine's own record says exactly that; no printed field says otherwise. CONFIRMED that the ground is a comment. |

## 4. THE CLASS QUESTION — the strike of the always-safe column

**The count.** Of the 31 struck prescriptions (the struck halves of the 31 SPLIT rows), **17 were the safe-harbour word for a REAL denial** — i.e. the free alternative IS contradicted by a printed field on some town: **A-1** (watch → `hasWatch`), **A-2** (WHOSE → the occupation record has no garrison field, F1-121), **A-3** (guard → `hasMilitaryInst` + the band-gated panel), **A-4** (muster class word → `forces.militia.present`; the roll → the muster holder), **A-5** (class word → `Hireling hall` desc), **A-10** (stores → `hasGranary`), **A-11** (way in by water → `portKind`), **A-12** (approach → `tradeRouteAccess`), **A-13** (land → `terrainType`), **A-14** (house of the faith → `hasChurch` on a shrine/parish-access row), **A-15** (those tending the sick → `hasHospital` on a healer), **A-18** ({seat} → `governingName`; hall-as-place below town), **A-20** (what the town trades → NO-MARKET), **A-21** (the trades → a criminal-only `guild`), **A-29** (no direction → `Departure Committee`), **A-32** ({faction}/{seat} → the noble row), **A-37** ({term} → `termLabel`). **14 were house style or were themselves wrong:** A-7, A-8 (the bar denied a gate the engine asserts), A-9, A-16, A-19, A-22, A-23, A-25, A-26, A-28, A-30, A-34, A-36, A-41. CONFIRMED against the dock for each field named.

**But in all 17 the DENIAL survives** as a kept half and a CONTRADICTION-TABLE row (F1-01…F1-24, F1-75, F1-87, F1-102, F1-119). What the strike removed is not the bar but the harbour — and, with W20 struck in the same fold, the POOL-grain guarantee. That is the real exposure, and it is structural, not lexical: a face is authored ONCE and drawn by EVERY town whose key matches (the table's own F1-126), so "a watch where none resolves contradicts the roster" is enforceable only if the pool's KEY entails `hasWatch` on every town in its domain. The old column's "the community" was the one word a watch-agnostic pool could bake without lying on some town. The re-cut keeps the per-town truth condition and deletes the per-pool mechanism; RETRO-VALIDATION §5 item 3 concedes the refuter's packet cannot yet see thirteen of fourteen page-level collisions.

**Recommendation: the class strike should STAND, WITH ONE NAMED STRUCTURAL EXCEPTION restored and the 17 ids flagged.** Do not restore any spelling (the words were the flavour collapse, and A-8 shows the column was not even uniformly true). Restore W20's content as a contradiction rule at the pool grain: *a body, building, force or record-keeper word baked into a pooled face is a finding if the pool's key does not fix that body on every town that can draw the face* — the false sentence exists on SOME town in the domain, which is floor 1 by construction. The 17 ids above are the rows where a refuter must check the key domain, not merely THIS town. Until CAR 8b-W-3 prints the key domain, the DS-DEF-2 restart is exposed exactly here (its five pools are keyed on walls/force booleans, so its own body words are safe; the exposure is on the power, stressors and general desks, where walls and watches were asserted off keys reading no such row — the live breaches A-1 and A-7 list). PLAUSIBLE on the remedy; CONFIRMED on the mechanism (pooled draw; W20 struck; keys at `defenseStateProse.js:454-461`).

## 5. FIDELITY-REPAIR CHECK (executed by grep on `items-ET-3-4.md`)

| finding | repaired | evidence |
|---|---|---|
| F1 — closing §1 quotes A-27 truncated and argues from the cut | **NO** | "do not rename a neighbour" occurs once (the A-27 row only); closing §1 still reads *"the only thing standing between the permission and that breach is a cross-reference"*. And Fact C makes the argument doubly wrong. |
| F4 — A-6 not in the closing | **NO** | `A-6` occurs 0 times between `## THE STRIKES MOST LIKELY` and `## APPENDIX`. This seat has now reversed it (§3). |
| F5 — "the old law, VERBATIM" is the law AS CORRECTED by ADDENDUM 13A and the archive never says so | **NO** | "ADDENDUM 13" 0 hits; "as corrected" 0 hits. A-1's cell still shows "the guard" and 'local watch' as if live law; A-3's still "DISPUTED". |
| F6 — the packet's `WHAT THE STRIKES BUY` not carried | **NO** | "STRIKES BUY", "every 21 words", "People come back" 0 hits. |
| F7 — intro generalises the prescription column to all 41 | **NO** | "PRESCRIBING one always-safe spelling per word" still present (1 hit); "thirty-one"/"where one exists" 0 hits; A-3 and A-39 prescribe nothing. |
| F8 — three closing inexactnesses | **NO** | closing L-12 quote lacks "(floor 1)" (0 hits); "Royal seat" absent from the closing (0 hits); "four keywords" still asserted (1 hit). |
| (F2, for the record) | YES | two `[corrected]` paragraphs in the front matter. |

## 6. SLICE VERDICT

The 41 alias rows are a substantially sound re-cut: 38 of 41 stand as recorded, every ground I resolved names a real field at a line that still reads that way (with A-5's citation swap the only mis-cite), and the engine splits each word the way the old law said. Three verdicts move: A-6 must be SPLIT because the Defense tab itself prints "no organized military force to man them" on the very town the cell writes men onto; A-2's Barracks clause and A-26's servile-uprising clause are KEPT bars that no printed field backs (one is overruled by the fold's own R-3, the other rests on a source comment), so each refuses a sentence the page agrees with. The class strike is right on the words and wrong on the mechanism: seventeen of the thirty-one prescriptions were the safe-harbour spelling for a denial that survives, and what the fold removed alongside them — silently, via W20 — is the only guarantee that a pooled face is true on every town in its key's domain. The archive's own adversarial section is the weakest part of the slice: its lead argument (A-27) is false on the archive's own page and on the dock, it omits the one strike in the slice that actually licenses a false sentence (A-6), and none of fidelity findings 1, 4, 5, 6, 7 or 8 has been repaired.

## 7. THE ONE THING

The always-safe words were never the law; they were the only spelling a town-agnostic pooled face could bake without lying on some town in its key's domain, and the fold struck W20 — the rule that made that explicit — in the same breath. Restore that rule at the pool grain (not the words), and re-refute the landed DEF blocks against A-6 and A-2/A-26 alone.
