# FOLD — §912 / lane L-MAT — the six-lens skeptic pass folded
Seat: Opus 5 — Fable-unvalidated (the FOLDER). Read-only on every tree.
Dock: `.../26b2203a…/scratchpad/laneLMAT` · receipt: `.../26b2203a…/scratchpad/receipt-l-mat.md`
Lenses folded: `promise.md` · `security.md` · `import.md` · `walker.md` · `build.md` · `refusals.md`

**Fold's own arrival/exit check, executed 2026-09-07 12:08:18 EDT (`date`):**
`git -C <dock> rev-parse HEAD` = `7d96e2b72245fa465182d59dade31621f31ecf2c` ·
`git -C <dock> status --porcelain | wc -l` = **0**. Taken AFTER all six lenses ran, so it is the
strongest single attestation that no lens left residue.

**COUNTS: CONFIRMED 10 · REFUTED 13 · PARTLY 20 · UNTESTED 6 (49 rows).**

The headline: **every arithmetic figure in the receipt reproduces.** What does not survive is a
family of *grounds* — five separate places where the lane reached a right conclusion by a wrong
mechanism, and shipped the wrong mechanism into product prose, test bytes and a failure message a
future engineer will read at the worst possible moment. That is the chair's charter-the-permission,
measure-the-mechanism law firing five times in one consist.

---

## (1) THE TABLE — every car, ruling, lane call, refusal, deferral

### A. CARS

| # | claim | verdict | lens / figure |
|---|---|---|---|
| 1 `442c7f988` | the create boundary leaves first paint; the PIN is refuted by the build | **PARTLY** | build §1/§4. Source closure 239→238 and boundary `true→false` reproduce at three trees; the PIN refusal is confirmed on firmer ground. But at the BUILT bundle the module was never in first paint — entry chunk 570,269 B and `index.html` 8,197 B in **all four** builds, 7 modulepreload links, boundary not among them. **Shipped first-paint benefit is exactly zero bytes**; the lever moves an instrument, not a payload |
| 2 `fe8eb3f56` | the living-content law is WIRED, dark; THE PROMISE re-proven by execution | **PARTLY** | promise §1-5/§12 CONFIRM R-E, the dormant dial, all six read sites, the non-vacuous same-seed control, 10/10 green. But §6 the STOP arm cannot fail, §7 its recorded ground is false, §8 a lifecycle path is untraced |
| 3 `9a9a7856e` | the walker's second hole closed; the plant proves it in both directions | **PARTLY** | walker §2 reproduces the plant in both directions and both restore digests exactly. §1: the widening is worth **one file** (scanned 6→7 of 2,188); three named holes remain, largest `settlementSlice.js:45` — a live dynamic-import pipeline reacher the scan cannot see |
| 4 `525ffa99b` | O-11 path 1 — the public drop PINNED; the DM-full half measured and refused | **PARTLY** | security §4 arm 1 strongly anchored, arms 2/3 substantively anchored, arm 6's measurement real. Arm 4 is an assertion that cannot fail; the header citation is refuted by its own consist; the arm's inertness premise names the wrong barrier |
| 5 `34115c7b7` | O-11 path 2 — resolve-or-drop on account import | **PARTLY** | import §1/§8: grain, miss policy, drop-with-warning, fingerprint re-derivation and re-sort all mirror the provenance seam exactly. §2: resolve-or-drop **collapses to drop** for the population R-B exists to protect; §3 undo reinstates the source roster; §1 the remapper admits any integer `schemaVersion` |
| 5b `7d96e2b72` | the typecheck ratchet made green — car 5 shipped 11 type errors | **CONFIRMED** | import §6 + refusals (b). `npm run typecheck:ratchet` **exit 0, 173 errors / ceiling 173**, executed. Baseline byte-untouched (`git diff --stat 3b1c0eaa5..7d96e2b72` = 12 files, none the baseline); per-file rule, both touched files absent ⇒ allowance zero. Source-fixed: 3 JSDoc + one `=== true`. Two lenses disagreed on whether the green was executed — **import.md executed it; that is the stronger evidence** |
| 6 REFUSED | O-11 path 3 refused with the measurement (R-C) | **PARTLY** | refusals (a): the tripwire is real, `contentHash` really moves on a presentation flip (`75ddb8f6…`→`79a1f5bb…`), the revert left 2 files / 9+9 / porcelain 0. But the refusal's stated GROUND is refuted (below), and its routing is over-escalated |

### B. CHAIR RULINGS

| id | ruling | verdict | lens / figure |
|---|---|---|---|
| R-A | public projection DROPS the roster, PINNED, no migration | **PARTLY** | Outcome CONFIRMED (security §1, executed: default projection drops, DM-full carries). Mechanism corrected by the lane itself. **Composed with R-D it produces an unintended state**: promise §8, gallery import yields a LIT world with **no roster** |
| R-B | resolve-or-drop on account import, per-settlement grain | **PARTLY** | Wiring CONFIRMED (import §8); the regen question answered in the strengthening direction. But import §2 (executed P2/P2b) shows a fully-mapped roster returning `identity_incomplete` when one row is localUid-only — the ruling's own beneficiaries lose the roster |
| R-C | one OPTIONAL core key, omitted when dormant | **PARTLY** | Its byte claim is sound (`canonicalContentJson` sorts and omits). Its **digest input is wrong** — CONFIRMED. The lane's reason for refusing the narrow alternative is REFUTED. Unresolved, and now demonstrably chair-decidable in part |
| R-D | `_livingContentLawVersion` may reach the public projection; RECORDED not changed | **PARTLY** | Ground sound in isolation (security §3: rides allowlisted `config`, no `PRIVATE_KEY_RE` token, roster `null` at zero rows so it discloses birth-era law only). Two dents: the arm is written as a **PIN** (`toBe(2)`) where the ruling says RECORD, with no note telling a future ruler to delete it; and the R-A×R-D composition above |
| R-E | the mint spread inside `birthConfig`, main-thread | **CONFIRMED** | promise §1 + build. `densityCreateBoundary.js:181-187`, one spread beside the density mint; `grep` finds the mint in three places only; both BIRTH callers reach it; refused alternative recorded in the docblock. 0 first-paint bytes, 29 lazy |

### C. LANE CALLS

| id | call | verdict | lens / figure |
|---|---|---|---|
| L1 | refuse the car-1 `manualChunks` pin | **CONFIRMED** | build §3, and on ground the lane did not state: `engineChunkLazy.test.js:238` iterates `ENGINE_SHARED_DOMAIN_EXCISIONS` (18 members) and the boundary is not one — the brief's premise was a class error. `vite.config.js` untouched, empty diff |
| L2 | delete the walker's `MINT_HOMES` helpers row | **CONFIRMED** | walker §3, plant reproduced: row deleted ⇒ **RED**, row kept ⇒ **GREEN**. The deletion converts car 1's cured regression from silent to red. ⚠ attribution correction: it landed in **car 1**, not car 3 |
| L3 | take a build-determinism control | **CONFIRMED** | build §1, the hard way: the two listings are the **same file by md5** (`5e0e4e769d…`), 0 of 1,377 lines differ |
| L4 | widen `MINT_SYMBOLS` in the same car that flips to WIRED | **PARTLY** | walker §1 confirms the compensation is real (the `:251`/`:280` arms scan all 2,188 files). promise §10/§11: the plant that was run used an OFF_THE_BOUNDARY symbol, and after car 2 the ⭐ arm's generation-law denominator is **empty** — the `:251`/`:280` arms are now the only cover for the new mint, and that cover was never plant-proved |
| L5 | land the two stale-header corrections in car 2 | **CONFIRMED** | refusals "WHAT HELD" + build §6: 239→238 corrected; `loadEngine` is at `settlementSlice.js:42`, used only at `:435`, never named by the generation lane. True at the tip |
| L6 | write the PROMISE proof as a NEW test file | **CONFIRMED** | promise §5: the file exists, 10 arms, exit 0 in 2.67 s, and its purpose is genuinely distinct from `livingContentMaterialization.test.js`. Rationale uncontested by any lens |
| L7 | arm the living-content seam at the top of the new file | **CONFIRMED** | security executed the unarmed counterfactual: `THREW: [livingContentSeam] v2 world, roster payload not loaded`. L7's stated reason is exactly right — and it is also the finding the lane failed to promote (see DEF-13) |
| L8 | assert the dormant dial inside the DM-full arm so lighting reds | **PARTLY** | refusals (c): the assertion is real and does fire when the dial constant moves. But its premise sentence names the wrong barrier and its failure message tells the lighting engineer the wrong symptom |
| L9 | carry the roster `schemaVersion` rather than import the builder's constant | **PARTLY** | The import-avoidance is sound. But import §1 (executed P3) shows the consequence was never pinned: a source roster with `schemaVersion: 99`, an invented bucket name and two foreign keys returns `ok:true` and is **persisted intact**. The gate's refusal message is not what the gate enforces |
| L10 | drop an INDEPENDENT source fingerprint rather than carry it | **UNTESTED** | no lens exercised it |
| L11 | round-trip arms in a SIBLING describe | **UNTESTED** | cosmetic; no lens exercised it |
| L12 | the second round-trip arm exercises "no archive-backed identity map" | **UNTESTED** | the `AccountExportPreflightError` claim was not executed. Adjacent finding (import §2): the ⭐ real-builder arm feeds `identifyCustomContentPack`, which stamps ids on **every** definition — so it pins the *identified* variant only |

### D. REFUSALS

| # | refusal | verdict | lens / figure |
|---|---|---|---|
| 1 | the car-1 `manualChunks` pin | **CONFIRMED** | build §3. Chunk present and identical at base, line 265 of both listings, 101 B / `581a4f0b…`; tip chunk verified to carry `birthConfig`'s body, not a facade |
| 2 | the DM-full drop refused to this lane | **PARTLY** | security §5. **Right on the server half** — the SQL twin is a `supabase/migrations/` file, owner-gated, and the lane's no-migration law is real (migrations 120/129 read: both DM-share VIEW and IMPORT payload carry the roster server-side). **Wrong that "curing it needs an SQL twin"**: a client-only `delete clone.customContentRoster` beside the existing `delete clone.dmNotes` in `publicSafe.js` would have reddened nothing and cost no migration |
| 3 | the reconciliation boundary declared, not cured | **PARTLY** | import §4 REPRODUCED the gap and made it worse than "declared": with the reconciliation's own option shape a foreign roster survives `prepareSettlementEntry` whole, reaches `importReconciliationAdmission.js:570` → `importReconciliationExecution.js:53` → `create-and-attach`. **It reaches persistence TODAY.** The provenance-twin argument may still stand; the inertness argument does not |
| 4 | car 6 / R-C | **PARTLY** | refusals (a). Refusing was right; the *reason* is not (below); the routing over-escalates |

### E. DEFERRED ITEMS

| # | item | verdict | lens / figure |
|---|---|---|---|
| 1 | DM-full projection does not drop the roster | **PARTLY** | the gap is CONFIRMED by three lenses; its stated inertness ground is refuted; its blast radius reads smaller than it is — security §1 finds **two further un-allowlisted carriers** (`stripImportConfidential`, `buildWorldExport` dm variant) the deferral does not name |
| 2 | the reconciliation boundary does not remap the roster | **PARTLY** | real (import §4) and reachable today; ground refuted |
| 3 | O-11 path 3 unbuilt — six inherited findings | **PARTLY** | inherited finding 4 CONFIRMED by reading (`buildSettlementContentProvenance` returns `null` ⇒ a roster-only lit world gets no receipt). Findings 1/2/3/5 UNTESTED — car 6 is reverted |
| 4 | the sample-fork path is a marker carrier on the lighting day | **PARTLY** | true, and **strictly too narrow**. It argues `SAMPLE_SETTLEMENTS` is a static fixture set so no user world reaches it. `SettlementsPanel.jsx:102-103` reaches **every user save** |
| 5 | `updateConfig` does NOT drop `_livingContentLawVersion` | **CONFIRMED** | three lenses executed it independently: `isAllowedConfigKey` admits `key.startsWith('_')` (`configSlice.js:99-103`) |

### F. RECEIPT CLAIMS THE LENSES REFUTED OUTRIGHT

| # | claim | lens / figure |
|---|---|---|
| X1 | "every emitted dist file stayed byte-identical" — in the commit message AND at `densityCreateBoundary.js:289` AND inside the executable `why` string at `:326` | build §2: of 1,377 emitted files **719 are byte-identical and 658 are not** (311 HTML + 347 JS). True statement: no file changed SIZE |
| X2 | the ⛔ STOP arm, "driven through the real store's `regenSection`", is the executable proof | promise §6, executed: with the world's own config and with the LIT wizard config the probe returns **identical** results — both keys undefined either way. `regenNPCsPipeline` returns root parts and never `config`; the roster's one writer is unreachable from it. The real guard is a source-text arm in the walker |
| X3 | "the store config … is never hydrated from a saved settlement" | promise §7 + import §7b + refusals (c) — **three independent executions**. `SettlementsPanel.jsx:102-103` does `updateConfig(migrateConfig(data.settlement?._config \|\| data.config))`; `migrateSettlementConfig` strips nothing; marker survives at 2 |
| X4 | every lifecycle path of the config/roster was traced | promise §8 (gallery import), import §3 (undo/versionHistory), security §1 (`stripImportConfidential`, `buildWorldExport` dm) — three untraced carriers |
| X5 | walker `:503-505` guards "a declared row that fell out of the scan" | walker §4, verified by me at `:503-505`: the loop searches `scanned`, which was built two statements above by spreading `Object.keys(PIPELINE_REACHERS)` into it. **Tautology** |
| X6 | car-4 arm 4 proves `PRIVATE_KEY_RE` strips nothing from a roster row | security §4: `rosterKeys` is a hand-transcribed 11-name literal; the real row copies **36 more** manifest-declared fields. `expect(rosterKeys.length).toBeGreaterThan(6)` over an 11-element literal cannot fail |
| X7 | "while the dial is dormant no world carries a roster at all" — the inertness anchor | security §2 + import §7 + refusals (c). The gate is `materializesLivingContent(settlement.config)`, **the world's config, not the dial**; the car-4 file itself mints six lit worlds at dial 1; `birthConfig` spreads `{}` which deletes nothing; importers are ungated writers. `probe-chain.mjs` executed end to end: a roster **minted on a shipped dark build** |
| X8 | "the `writer-dark-register` roster row stays as it is — CONFIRMED" | refusals (c): the row's `writer` is the pipeline alone and the walker probes only `read(row.writer)`. Car 5 created a second assignment site at `accountImportBody.js:474`. 56 green cannot convict a writer it never reads |
| X9 | R-B's resolve-or-drop preserves a user's own estate | import §2, executed: a fully localUid-mapped roster returns `settlement_living_content_roster_identity_incomplete` and the **whole roster is dropped** |
| X10 | the narrow digest "goes blind to a content change that mints no new revision" | refusals (a): the fixture hand-writes `revision:<bucket>:<localUid>:1`, ignoring content. The product's `makeContentRevision` is **content-addressed** (`revision:${definitionId}:${number}:${contentHash.slice(0,16)}`). The blindness belongs to the fixture |
| X11 | car-4 test header: "nothing in `src/` READS the key" | security §6.4, verified by me: `livingContentRosterPublicDrop.test.js:12` vs `livingContentRoster.js:37`, which names `accountImportBody.js` as a reader. **A shipped comment refuted by its own consist** |
| X12 | the `MINT_HOMES` deletion is car 3's | walker §3: it landed in car 1 (`442c7f988`) |
| X13 | "Build A — BASE … 11:58:11→10:58:33" | build §6: `build-base.meta` says 10:58:11. Typo, LOW |

### G. WHAT NO LENS COULD TEST

| # | item | why |
|---|---|---|
| U1 | a plant for the symbol L4 actually added (`newSettlementLivingContentLaw`) | both the promise and walker lenses forbid writes into the dock; the walker's in-memory plants used the OFF_THE_BOUNDARY mint and the `MINT_HOMES` re-export. The `:251`/`:280` cover is reasoned, not measured |
| U2 | the `node_modules` 454-vs-453 drift, and clean-install chunk stability | no build allowed. Bears on build §3: nothing in the tree asserts the boundary keeps its own chunk, so a Rollup/Vite bump could re-parent it silently (contained, not guarded) |
| U3 | car 6's "2 failures instead of 3" under the narrow digest | the car is reverted; rebuilding is outside every lens's fences |

---

## (2) DEFECTS A FIX CAR MUST CURE BEFORE THE GATE

Ranked. DEF-1…DEF-4 change behaviour; DEF-5…DEF-8 repair instruments; DEF-9…DEF-13 are text a
successor will otherwise inherit as fact. All thirteen are cheap; none needs a migration.

### Class: a security hole live at dial 1

**DEF-1 — the gallery-import INGEST carries a foreign roster into a second account's library,
unremapped and unwarned.**
`src/store/galleryImportSettlement.js:73` (`config: scrubImportedConfig(src.config)`, settlement
spread passthrough) + `src/lib/importScrub.js:38-45` (5-key denylist: `_seed`,
`primaryDeityRef`, `primaryDeitySnapshot`, `cultDeitySnapshots`, `faithProfile` — no roster, no
marker). This is the exact mirror of the boundary car 5 built, at the one import boundary the lane
never enumerated, and it is **unrecorded anywhere**. Executed by two lenses.
**Smallest cure:** delete `customContentRoster` and `customContentProvenance` on the ingested
settlement beside the existing scrub (the importer has no identity map, so a drop — not a remap — is
the honest act), and add `_livingContentLawVersion` to `scrubImportedConfig`'s destructured drop
list. Two lines plus one arm.

**DEF-2 — a shipped dark build can be made to MINT a roster from user-supplied data.**
Chain, executed end-to-end in `probe-chain.mjs` (import §7b):
`SettlementsPanel.jsx:102-103` → `configSlice.js:99-103` (`key.startsWith('_')`) →
`densityCreateBoundary.js:181-187`, where `birthConfig` spreads `newSettlementLivingContentLaw()`
and **a spread of `{}` deletes nothing**, so an incoming lit marker passes through unclamped.
Car 2 is what made this live: before the wiring the mint had no caller.
**Smallest cure:** clamp in `birthConfig` — destructure the marker off the incoming config before
spreading the mint (`const { [LIVING_CONTENT_LAW_CONFIG_KEY]: _drop, ...rest } = config || {}`).
At dial 1 that still writes **not one config byte** (the key is simply absent), so the same-seed
control and every dark arm survive unchanged; and it makes "a birth's law is the dial's law" true
on *every* path, retiring DEF-2, deferral 4 and the promise lens's row 6 in one line.

**DEF-3 — the reconciliation path carries a foreign roster to persistence today.**
`src/store/importReconciliationAdmission.js:570` → `importReconciliationExecution.js:53` →
`import.settlement.create-and-attach`. Declared as a deferral on an inertness ground that is false
(the path's input is an import FILE — exactly where a roster comes from without any dial).
**Smallest cure:** call `remapAccountSettlementLivingContentRoster` on that path, or — if the
provenance-twin pairing argument is to be honoured — drop both records there. Either is a chair
call, not an owner one (§3).

### Class: a PROMISE path missed

**DEF-4 — undo/version-history REINSTATES the source-account roster after a correct import.**
`src/store/accountImport.js:390-400` restores `versionHistory` verbatim; snapshots are whole
settlements and carry `customContentRoster`; the car-5 block at
`src/store/accountImportBody.js:467-477` rewrites only `entry.settlement.customContentRoster`.
`revertToSnapshotAction` then re-persists the source roster. Executed (`probe-prepare.mjs` A:
snapshot roster id `"src-secret-def"`). The commit's claim that nothing downstream *corrects* a
foreign roster is true; the untested inverse — nothing downstream *reinstates* one — is false.
`customContentProvenance` has the same exposure. This is the create/read/**undo**/persist trace the
lane's own PROMISE proof claims to have walked.
**Smallest cure:** run the same remap (or the same drop) over every `versionHistory[i].settlement`
inside the existing car-5 loop.

**DEF-5 — resolve-or-drop collapses to DROP for the population R-B protects.**
`src/lib/accountSettlementContentPortability.js:262-397`. A row that is local-only has a `localUid`
and no `customDefinitionId`; the remapper reads "no account-scoped identity" as "unresolved
identity" and drops the **whole** roster. Executed (P2/P2b): a fully-mapped roster returns
`settlement_living_content_roster_identity_incomplete`. Green only because every fixture row sets
`customDefinitionId` and the ⭐ arm feeds `identifyCustomContentPack`, which stamps ids on
everything.
**Smallest cure:** treat a row whose `localUid` resolves and which carries no `customDefinitionId`
as RESOLVED rather than incomplete; add one fixture row with no `customDefinitionId` so the arm can
see the difference.

### Class: a test that cannot fail

**DEF-6 — `tests/lint/densityCreateBoundary.walker.test.js:503-505`.** Verified by me: `scanned` is
built at `:494-500` by spreading `Object.keys(PIPELINE_REACHERS)`; the loop then asserts every one
of those keys is in it. Tautological for any tree. Its comment claims it guards a row falling out of
the scan, which cannot happen by construction.
**Smallest cure:** delete the loop; the `:506-509`
`toContain('src/store/settlementGenerateAction.js')` assertion beneath it is the one that can die
and it already says the same thing honestly. (Sibling nit at `:513-514`: the stray message always
says `names ${law.mint}` even when the DIAL matched.)

**DEF-7 — `tests/security/livingContentRosterPublicDrop.test.js:138-143`.**
`expect(rosterKeys.length).toBeGreaterThan(6)` over an 11-element hand-transcribed literal, missing
the 36 manifest-declared authored fields the real row copies (`livingContentRoster.js:166-172`). A
future manifest field named `dmGuidance` or `notes` would falsify the claim while the arm stays
green. The file already generates a real roster two arms above.
**Smallest cure:** derive `rosterKeys` from that real roster's rows instead of typing them.

**DEF-8 — `tests/domain/livingContentLawWiring.test.js:221-253`, the ⛔ STOP arm.**
Executed: identical output with and without the defect it names. Its three post-conditions are
untouchable by `regenNPCsPipeline` in either direction.
**Smallest cure:** add a positive control (assert the regen actually changed the world) and keep the
marker check — or re-point the receipt's claim at the walker's source-text arm
(`/settlement\.config \|\| config/` present, `/\bconfig \|\| settlement\.config/` absent), which is
the instrument that can actually fail. The receipt currently credits the wrong instrument.

### Class: an unanchored negative

**DEF-9 — `tests/security/livingContentRosterPublicDrop.test.js`, arms 2/3.**
They use `expect(KEY in pub).toBe(false)` rather than the `expectAbsentWithAnchor` helper the same
file uses in arm 1, and `negativeAssertionAnchor.walker`'s `BARE_NEGATIVE_RE` matches only
`not.toContain|toMatch|toHaveProperty` — so the walker's 9-green says **nothing** about this file.
Substantively they are anchored (paired positives, `rowCount > 0`, surviving siblings), so the
severity is instrument-visibility, not vacuity — but the receipt's "ZERO scanned negative sites"
reads as a proof of anchoring and is not one.
**Smallest cure:** convert arms 2/3 to `expectAbsentWithAnchor(pub, 'config', …)`, the helper
already imported in the same file.

### Class: a wrong header fact

**DEF-10 — `src/domain/density/densityCreateBoundary.js:289` and `:326`** —
"every emitted dist file stayed byte-identical". Measured: 658 of 1,377 changed bytes. `:326` sits
inside the executable `GENERATION_LAWS.livingContent` `why` string that a walker reads, so a false
measurement about the very instrument this lane exists to have taken is shipped in the register.
**Smallest cure:** two words — "no emitted dist file changed SIZE", at both lines. (Same sentence is
in the `442c7f988` commit message, which cannot be amended after the register was taken; record the
correction instead.)

**DEF-11 — `src/domain/density/densityCreateBoundary.js:48**, "Nothing anywhere hydrates it from a
saved settlement", re-shipped in NEW bytes at `tests/domain/livingContentLawWiring.test.js:299-300`
and in `src/store/settlementGenerateAction.js:117-120`. False:
`src/components/SettlementsPanel.jsx:102-103`, also reached from `SettlementDetail.jsx:605`.
This is the load-bearing half of the lane's own recorded correction and the reason an admission
surface was left unchanged.
**Smallest cure:** replace the sentence at all three sites with what is true — the Library's Load
DOES hydrate the wizard form from `settlement._config`, and DEF-2's clamp is why it is now harmless.

**DEF-12 — `tests/security/livingContentRosterPublicDrop.test.js:12`** — "nothing in `src/` READS
the key", citing `livingContentRoster.js`'s header, which car 5 amended in the SAME consist:
`src/domain/content/livingContentRoster.js:37` now names `accountImportBody.js` as a reader.
**Smallest cure:** one sentence — the key has exactly one reader, the account-import remap.

**DEF-13 — `tests/security/livingContentRosterPublicDrop.test.js:176` and `:184`** — the arm's
premise says no generated world carries a roster while the dial is dormant (the gate reads the
CONFIG), and its failure message tells the lighting engineer "Shipped worlds can now carry
customContentRoster". What they will actually meet first is a **total generation outage**: I
verified `loadLivingContentRoster` appears in `src/` only as its own definition
(`livingContentSeam.js:92`) and one comment at `:64` — no caller. Executed by two lenses:
`generateSettlementPipeline` on a lit config throws
`[livingContentSeam] v2 world, roster payload not loaded`.
**Smallest cure:** re-anchor the message on the real barrier, and add the outage as a DEFERRED row —
it is the single most consequential unrecorded fact in the consist and appears in no commit message,
no header and no deferral. It is also the true (and stronger) ground for every inertness claim the
lane made.

---

## (3) CHAIR-DECIDABLE vs OWNER-GATED, among the refusals

**The DM-full SQL twin (REFUSAL 2) — SPLIT.**
- **OWNER-GATED:** the server half. A new `supabase/migrations/NNN_*.sql` re-issuing
  `_gallery_dm_full_json` with the two keys in its delete chain is a migration file, and migrations
  are on the standing owner-gated list. The lane's no-migration law held correctly, and the estate's
  own V1 precedent (`townMapEditsPublicDrop.test.js`) declines the same half.
- **CHAIR-DECIDABLE, and landable today:** the client half. A `delete clone.customContentRoster`
  (and `customContentProvenance`) beside the existing `delete clone.dmNotes` / `delete clone._seed`
  in `publicSafe.js` costs no migration and would have reddened nothing. The receipt's "curing it
  needs an SQL twin" is not the whole truth; defense-in-depth was available. **The chair should rule
  on the client half now** and leave the SQL twin on the owner's desk with the migration.

**The receipt digest (REFUSAL 4 / R-C) — SPLIT, and the refusal's ground must be re-taken.**
- **CHAIR-DECIDABLE:** the choice of digest INPUT. The `OWNER-GATED` words in the tripwire message
  attach to presentation→mechanical *promotion*, not to which projection a digest reads, and the
  standing delegation (owner 09-07 01:45, "to these i leave your best judgement") covers exactly
  this class. The receipt's "chair/owner work" is right; reading it as owner-gated is not.
  The chair now has **four** options, not the two the lane weighed — and the lane's reason for
  rejecting the narrow one is refuted (X10), so the option set must be re-scored:
  1. wide (`contentHash` in the triple) — refuted by the F2c tripwire on all 26 blind cases;
  2. narrow (`definitionId` + `revisionId`) — clears the tripwire, and the product's revision minter
     is content-addressed, so it is **not** blind in the product;
  3. `{ lawVersion }` alone, no `rosterHash` — presentation-invariant, keeps R-C's byte claim,
     defers the hash-contract question rather than deciding it wrongly;
  4. re-cut the F2c escape filter instead of the digest — the receipt is not one of the six
     `LIVING_CONTENT_ADOPTION_SURFACES` the governing law names.
  ⚠ And a fact that outranks all four: `buildSettlementContentProvenance` returns `null` for a world
  with only a roster, so **no receipt exists to carry the marker** on the very worlds R-C targets.
- **OWNER-GATED:** actually SHIPPING the optional `livingContent` key onto the persisted,
  hash-validated receipt. That is a persistence-shape change on the standing list, whichever digest
  the chair picks. Note also that neither candidate is tamper-evident over a persisted roster row
  (the row's hash is a copied string, never recomputed) — which the owner should be told before
  approving a key sold as tamper-evidence.

**The reconciliation gap (REFUSAL 3) — CHAIR-DECIDABLE, and no longer deferrable on its stated
ground.** Curing it needs no migration, no persisted-shape change and no new surface: it is calling
an existing remapper on a second path. What the chair must actually decide is the *pairing* — cure
the roster alone and two records governed alike everywhere else disagree on one path, or cure
`customContentProvenance`'s identical pre-existing gap with it (the lane's argument, and the better
one). Either way the **inertness excuse is gone**: import §4 put a foreign roster into persistence
through that path today, with no archive and no dial.

**REFUSAL 1 (the pin)** owes no decision at any level — the build refuted the premise, and the guard
the brief invoked has no jurisdiction over this module.

---

## (4) HEAD AND PORCELAIN, EVERY LENS

| lens | HEAD before | HEAD after | porcelain before / after | vitest run? | flag |
|---|---|---|---|---|---|
| `promise.md` | `7d96e2b72` | `7d96e2b72` | 0 / 0 | yes (1 file, 10 passed) | — |
| `security.md` | `7d96e2b72` | **"(see foot)"** | 0 / 0 | yes (2 files) | ⚠ **the after-HEAD is promised to a foot that the file does not contain.** The state itself is fine — my own post-hoc check covers it — but the lens's attestation is incomplete and should be completed rather than assumed |
| `import.md` | `7d96e2b72` | `7d96e2b72` | 0 / 0 | yes (4 files + `typecheck:ratchet`) | — |
| `walker.md` | `7d96e2b72` | `7d96e2b72` | 0 / 0 | yes (1 file, 14 passed) | — |
| `build.md` | `7d96e2b72` | `7d96e2b72` | 0 / 0 | yes (4 runs) | — |
| `refusals.md` | `7d96e2b72` | `7d96e2b72` | 0 / 0 | yes (4 files) | — |
| **FOLD (this file)** | `7d96e2b72` | `7d96e2b72` | **0 / 0**, measured 12:08:18 EDT | no | — |

**No lens is out of position.** HEAD is `7d96e2b72245fa465182d59dade31621f31ecf2c` at every
before/after that was written down, and my own measurement — taken after all six lenses had
finished — reads the same HEAD with `git status --porcelain | wc -l` = 0. `node_modules/.vite`
noise from the eleven vitest runs across the six lenses is not porcelain and did not appear in it;
no lens reported writing into the dock, and the two lenses that wanted to plant (promise §10,
walker) both declined on their fences and reproduced their plants out of tree instead. The one
correction to make is `security.md`'s missing foot line, which is a bookkeeping omission and not a
state problem.

---

## THE ONE-PARAGRAPH FOLD

Cars 1–5 are real work and every number in them reproduces; car 5b is the cleanest item in the
consist and car 6's refusal was the right act. What the six lenses converge on is a single
repeating failure: **the lane reached true conclusions through false mechanisms, and shipped the
mechanisms.** The roster is inert — but not because of the dial (because nothing calls the loader,
and lighting it will take generation down entirely, which no one has written anywhere). The public
projection is safe — but the store config *is* hydrated from a saved settlement, in three places
that now say otherwise in shipped bytes. The regen cannot stamp a law — but the arm the receipt
credits for proving it cannot fail. The walker's hole is closed — worth one file, with a live
dynamic-import reacher still invisible. The refusal of the narrow digest rests on a fixture's fake
identity rather than the product's content-addressed one. Thirteen cures, none needing a migration,
none touching a golden, would leave this consist honest — and DEF-2's one-line clamp in
`birthConfig` retires three of them at once.
