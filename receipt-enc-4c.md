# RECEIPT — LANE ENC-4c — **PARTIAL: BUILT, PROVEN GREEN ON EVERY INSTRUMENT REACHABLE WITHOUT VITEST**

⚠ **STATUS: PARTIAL, AND THE PARTIAL IS EXACTLY ONE THING — THE POST-CAR VITEST.** The chair
called a vitest hold at ~09:45 ("HOLD ALL VITEST NOW until the chair sends RESUME"). No RESUME has
arrived, so the post-car runs are recorded as OWED-UNTIL-RESUME with their exact commands. ⭐ Both
BASE runs completed at 09:40–09:42, BEFORE the hold, so this lane has something ENC-4b never had:
an executed base to attribute against.

**TIP `74a5cbeca7059e9f03c87ac6eceafa59652320b4`, porcelain 0, two cars over `9d9686c12`.**

⛔⛔ **HEADLINE 1 — THE `tests/lint/` DIRECTORY IS RED AT MY BASE, BEFORE I TOUCHED ANYTHING:
7 files, 9 arms. At least SIX are the ENC train's own unpaid bills.** Neither ENC-4 nor ENC-4b
ran vitest, so neither saw them. Full list, message-attributed, below. ⛔ I did not cure them:
six need a REGISTER ACT, which my brief fences absolutely.

⛔⛔ **HEADLINE 2 — ENC-4b's "THE CASE IS THE SMALLER OF THE TWO" IS MEASURED FALSE.** The
approach direction ties on `codepoint(target.nid)`, so on every tie the case is decided by which
HOME ID SORTS LATER — a fact about town-name spelling that knows nothing about who is the host.
Flipping only the id ordering flips the case 89/89. The withheld half is not an edge; it is
about half the kind. ENC-4b's F2/R2 stand as findings, but that clause of them does not.

⭐ **HEADLINE 3 — THE ENC-4b TRAIN'S OWN FOUR INSTRUMENTS ARE GREEN, EXECUTED.** ENC-4b left
them OWED. Measured at `9d9686c12` before this lane edited anything: 4 files, 90 tests, exit 0.

- Lane: ENC-4c (Opus 5 under Fable 5.1 chair)
- Dock: `$SC/laneENC4B` — verified on arrival: HEAD `9d9686c12ee0eb8385156067985d10cd0a3099ed`,
  detached, porcelain **0**, `node_modules` a directory of **453 symlinks** into the main
  checkout (untouched, never materialised). Matches the brief exactly.

## Progress
- [x] Dock verified
- [x] **Premise re-derived by execution — the host-offered case OCCURS and is WITHHELD today**
- [x] Base vitest captured on four instruments (GREEN) and on `tests/lint/` whole (RED, 9 arms)
- [x] Bill predicted in writing, below, before any car
- [x] The chair's five sentences run through every instrument BEFORE wiring — all pass
- [x] Car 1 `4eb8f1e63`: the annex's third block, the pool leaf, the walker's block table
- [x] Car 2 `74a5cbeca`: the case axis, the writer, the inverted mirror
- [x] Walker re-executed in plain node, **29 of 29**, with **EIGHT mutants**
- [x] Every frozen census figure re-derived and unmoved; the dark arm byte-identical
- [x] The brief's required roster grep — and it found a TENTH red nobody had paid
- [ ] Post-car vitest — **OWED-UNTIL-RESUME**
- [x] RETROVALIDATION ROW

---

## STEP 1 — THE PREMISE, RE-DERIVED BY EXECUTION (brief step 1)

**Driver:** `$SC/enc4c-tmp/premise.mjs` — ENC-4b's `exposed-arm.mjs` harness (real
`advanceChanceMeetings`, real minted errand worlds), with the personality pair swept over the
**engine's own `CORRUPTIBLE_FLAWS` vocabulary** (17 x 17 pairs x 6 rngSeeds = **1734 minted
worlds**) instead of two hand-picked pairs, so the corpus is the input vocabulary rather than a
guess at a distribution.

```
CORPUS: 289 flaw pairs x 6 rngSeeds = 1734 minted worlds
  'meeting' seeds        : 328
  'approach_exposed' seeds: 14
EXPOSED SEEDS BY CASE   : {"host_offered":13,"guest_offered":1,"no_direction":0}
OF THOSE, A LINE WAS BUILT: {"host_offered":0,"guest_offered":1,"no_direction":0}

PREMISE 1 (the case OCCURS)      : TRUE
PREMISE 2 (it produces NO line)  : TRUE — every host-offered seed is WITHHELD today
```

A host-offered sample, read off the real seed:
```
approacherNid "irontown:npc.host.1"  hostId "irontown"
npcIds ["ashford:npc.envoy.1","irontown:npc.host.1"]  settlementIds ["ashford","irontown"]
entry: NULL — WITHHELD
```
…against the guest-offered one that ships today:
```
entry: "Neither court announced it, and Irontown has it anyway: Host Notable refused
        A Named Legate of Ashford."
```

**⇒ THE WORDS ARE NOT A MINT. The case is real, it is frequent, and it is silent.** The act
proceeds.

### ⛔ I CHECKED MY OWN PROBE RATHER THAN TRUSTING IT, AND IT WAS BIASED

13-of-14 looks like a distribution claim. It is not, and I refuse to report it as one.
`envoyChanceMeeting.js` sorts the two directions by `(y.chance - x.chance) ||
codepoint(text(x.target.nid), text(y.target.nid))`, so **on a tie the approacher is the party
whose nid sorts LATER**. My harness pins `ashford` (guest) against `irontown` (host), and
`irontown` sorts later, so every tie resolves host-offered.

**Driver:** `$SC/enc4c-tmp/tiebreak-probe.mjs` — two parties IDENTICAL in every field the chance
reads, so every direction ties by construction; the HOST ROLE is held fixed and only the id
ordering flips:

```
ARM 1 — the HOST id sorts LATER (ashford guest, irontown host)
  exposed receipts: 89   by case: {"host_offered":89,"guest_offered":0}
ARM 2 — the GUEST id sorts LATER (zedmoor guest, ashford host)
  exposed receipts: 89   by case: {"host_offered":0,"guest_offered":89}
```

**⇒ 89/89 FLIPS ON ID SPELLING ALONE.** Neither case is "the smaller of the two": across a real
world's many town-name pairs the tie-decided share is roughly a coin flip. **ENC-4b's F2 and R2
called the withheld half "the smaller"; that clause is refuted.** The finding itself — that §B
is honest on only one case — stands and is exactly what this lane wires.

⚠ A control I attempted and DISCARDED rather than report: re-running `premise.mjs` with the town
ids renamed to flip the sort order produced **zero meetings** (the errand fixture is keyed to the
`ashford`/`irontown` ids). It measured nothing, so it proves nothing, and the tiebreak probe
above is what the refutation stands on. (`$SC/enc4c-tmp/premise-mirror.mjs`, kept for audit.)

---

## STEP 1b — THE BASE, MEASURED BEFORE ANY EDIT (both runs completed 09:40–09:42, before the
chair's vitest hold arrived)

### ⭐ THE FOUR NAMED INSTRUMENTS — GREEN
```
sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/lint/chanceMeetingKindPools.walker.test.js tests/lint/kindPoolFloors.walker.test.js \
  tests/domain/pantheon.test.js tests/domain/envoyChanceMeetingStage.test.js
  → Test Files 4 passed (4)   Tests 90 passed (90)   EXIT=0
```
**This is the first executed proof that ENC-4b's train is green, including its pantheon A5 cure.**

### ⛔⛔ `tests/lint/` WHOLE — RED AT BASE, 7 FILES / 9 ARMS
```
sh scripts/gate-mutex.sh --run -- npx vitest run tests/lint/
  → Test Files 7 failed | 132 passed (139)   Tests 9 failed | 2143 passed (2152)   EXIT=1
```

| # | file · arm | measured | attributable to |
|---|---|---|---|
| 1 | `clampPrimitiveBaseline` · baseline matches local clamp definers | live 62 vs banked 72 — ten files migrated to the kernel (`razing*`, `conquest*`, `cartography*`, `dispositionLedger`, `warAllianceRisk`) | **NOT ENC** — a prior lane's migration, stale baseline |
| 2 | `entropyRootCensus` · "`hash01` CALLER modules = 16" | `expected [18] to have a length of 17` — live **18** | **ENC-4** — `envoyChanceMeetingNews.js` is the 18th `hash01` caller; the arm's own comment names `envoyChanceMeeting` |
| 3 | `mutationCoverageManifest` · TOTALITY | `["tests/lint/chanceMeetingKindPools.walker.test.js"]` has no manifest entry | **ENC-4** — its new test file |
| 4 | `observedShapeReaders` M13 · the door clears exactly its identities | `expected 5 to be 4` — 4 identities, **5** reads | **ENC-4b** — its `approacherNidOf` is a FIFTH read of `grievance.toSid` |
| 5 | `observedShapeReaders` M13 · the notice names the door | notice says "cleared 5 read(s) across 4 of them", pin wants `/cleared 4 read/` | **ENC-4b**, same cause |
| 6 | `sovereigntyLightingContract` · THE CENSUS IS AN ASSERTION | `expected 2522 to be 2521` — `TEST_FILES.length` moved | **ENC-4** — its new test file; the lighting register is stale |
| 7 | `tuningRegister` · P3 shrink-only | `["src/domain/worldPulse/envoyChanceMeetingNews.js: 0 -> 2"]` | **ENC-4 + ENC-4b** — `CHANCE_MEETING_PRESENTATION`'s `0.56` and `0.76` are bare decimals in a file banked at zero |
| 8 | `tuningRegister` · the six ceilings never rise | `["BARE_DECIMAL_CEILING: 6985 -> 6987"]` | same two decimals |
| 9 | `writerReach` · surfaceReach round-trips | `seed on generationCoherenceReceipt` — the LIVE reach carries `news=N`, the frozen register does not | **probably ENC** (a news writer newly reads `seed`); not proven, and I do not assert it |

⛔ **I DID NOT CURE ANY OF THE NINE, AND THE REASON IS THE FENCE.** Six of them are cured only
by a REGISTER ACT (the lighting census tuple, the tuning ceiling and inventory, the writerReach
frozen register, the clamp baseline, the mutation-coverage manifest). My brief says **"No
register act"** and the preamble says the chair takes every register act at the landing. Curing
another lane's bills inside my train would also make my own diff unattributable. **They are the
landing bill, they are quoted above, and they are the chair's.**

---

## STEP 2 — THE BILL, PREDICTED IN WRITING BEFORE ANY INSTRUMENT RAN ON MY CAR

The shape I am building: **a SECOND POOL for an EXISTING kind, selected by the CASE the writer
already computes.** No new registry row, no new routed token, no new kind, no new file.

| # | instrument · figure | frozen | PREDICTED |
|---|---|---|---|
| 1 | `kindPoolFloors` `REGISTRIES` | 12 | **UNMOVED** — no new family |
| 2 | `kindPoolFloors` `REGISTERED_KIND_COUNT` / `ALL_ROWS` | 115 | **UNMOVED** — a second POOL is not a second ROW |
| 3 | `kindPoolFloors` `ROUTED_TOKENS` | 381 | **UNMOVED** — no `EXACT_SECTION` row |
| 4 | `kindPoolFloors` small-family exact list | `[INFORMATION, FAITH, CHANCE_MEETING]` | **UNMOVED** — CHANCE_MEETING is still 2 rows |
| 5 | `kindPoolFloors` `LEGACY_UNVOICED_TOKENS` | 274 | **UNMOVED** |
| 6 | `kindPoolFloors` `LEGACY_UNDER_FLOOR` | 23 rows | **UNMOVED** — `row.pool` stays 5, `major` floor is 4 |
| 7 | `kindPoolFloors` registered-minus-routed | 8 | **UNMOVED** |
| 8 | `pantheon.test.js` A5 — all six freezes | 12 · list · 115 · 381 · 274 · 8 | **ALL SIX UNMOVED** (the brief's explicit demand) |
| 9 | `chanceMeetingKindPools` `EXPECTED` kind census | 2 rows, depths 6 / 5 | **UNMOVED**; a new CASE table is added beside it |
| 10 | `chanceMeetingKindPools` `ANNEX_BLOCKS` | 2 entries | **2 → 3**, and §B's `until` moves `## §C HOLE 3` → the new heading |
| 11 | `chanceMeetingKindPools` the §B MIRROR arm | mirror `=== null` | **INVERTS — the mirror now BUILDS.** That is the lane's whole point and its loudest moved figure |
| 12 | `chanceMeetingKindPools` literal `test(` | 21 | **+N** (register delta, chair's) |
| 13 | `chanceMeetingKindPools` literal `describe(` | 1 | **UNMOVED** — one literal describe, straight-line literal tests, so the file stays CREDITED |
| 14 | `CHANCE_MEETING_RECEIPTS` keys | 2 | **3** — and the third is a POOL KEY, not a kind |
| 15 | `WHAT_PHRASES` · `EXACT_SECTION` · `KIND_SECTION` | — | **UNMOVED** — no new kind is minted anywhere |
| 16 | `chooserTotality` `DEFER_CEILING` · `habitForkRegistry` | 36 · 42 | **UNMOVED** — the new pool goes through the SAME `chanceMeetingLine`; no new `hash01(` site |
| 17 | `entropyRootCensus` `hash01` callers | red at 18 | **UNMOVED at 18** — no new caller module |
| 18 | `tuningRegister` bare decimals | red at `0 -> 2` | **UNMOVED** — I add NO bare decimal (significance and presentation are fenced unchanged) |
| 19 | `sovereigntyLightingContract` `files`/`credited`/`parked`/`suiteTitles` | red on `files` | **UNMOVED BY ME** — no file added, renamed or deleted; `titles` moves by my `test(` count |
| 20 | `.wizard-news-authoring-baseline.json` | 19 debt rows | **UNMOVED** — I add no governed site; I edit the existing builder |
| 21 | domain strict ratchet | 1120 | **UNMOVED** |
| 22 | `scripts/.size-baseline.json` | — | **UNMOVED** — measured: none of the three `envoyChanceMeeting*` files is baselined |
| 23 | the DARK ARM | — | **BYTE-IDENTICAL** |
| 24 | `mechanism-lit-coverage` flag denominator | — | **UNMOVED** — my diff names no `*Enabled` flag anywhere |

### ⚠ AND A REGISTER FACT ENC-4b's RECEIPT GETS WRONG, MEASURED

ENC-4b predicts `titles` **23198 → 23205** and calls `files`/`credited`/`parked`/`suiteTitles`
**UNMOVED**. Measured against the register the chair actually banks:

```
tests/lint/.lighting-census-baseline.json
  measuredAtSha ff9b7a53c11aa18444abe52d55b3bd5ced7cb008   (an ancestor of HEAD)
  files 2521 · parked 371 · credited 2150 · titles 23184 · suiteTitles 6214
git diff --diff-filter=A ff9b7a53c HEAD -- tests/  →  tests/lint/chanceMeetingKindPools.walker.test.js
```
The walker file does **not exist** at the register's own sha, so relative to the frozen tuple the
train moves **`files` 2521 → 2522, `credited` 2150 → 2151, `suiteTitles` 6214 → 6215** as well as
`titles`. ENC-4b measured "unmoved *since ENC-4's tip*", which is a different baseline — and the
live red at base (#6 above, `expected 2522 to be 2521`) is that difference showing up.

Per-file literal counts, register sha → HEAD:
```
tests/domain/pantheon.test.js                      test/it 36 -> 36   describe 12 -> 12
tests/domain/rumorFallbackPhrasePools.test.js      test/it 25 -> 27   describe  6 ->  6   (§894's, not ENC's)
tests/lint/chanceMeetingKindPools.walker.test.js   test/it ABSENT -> 21   describe ABSENT -> 1
tests/lint/chooserTotality.walker.test.js          test/it  8 ->  8   describe  1 ->  1
tests/lint/kindPoolFloors.walker.test.js           test/it 12 -> 12   describe  4 ->  4
```
⇒ `titles` owed at HEAD is **23184 + 21 + 2 = 23207**, not 23205: ENC-4b's figure misses §894's
two. (Grep-derived literal counts, not the census's AST walk — a prediction, not a census run.)

---


---

## STEP 2b — THE CHAIR'S FIVE SENTENCES, RUN THROUGH EVERY INSTRUMENT BEFORE ANY WIRING

The brief says a sentence that fails an instrument is a STOP. Driver `$SC/enc4c-tmp/words-check.mjs`
executes every fence this volume declares against all five, plus the chair's own ROLE law and its
own voice claim:

```
  variant 1: Sera Vane of Bramwell made an offer to Aldo Rell of Kesthorne, a guest that season, …
  variant 2: In Bramwell, Aldo Rell of Kesthorne said no to Sera Vane, one of the town's own, …
  variant 3: … within the walls; Aldo Rell declined it, and the declining did not stay …
  variant 4: Bramwell knows that Aldo Rell of Kesthorne, passing through, turned Sera Vane away. …
  variant 5: Neither court announced it, and Bramwell has it anyway: Aldo Rell of Kesthorne …

  semicolons per variant: [0,0,1,0,0] (the chair declares one, in variant 3)
  pool depth 5 vs the derived 'major' floor of 4: CLEARS by margin

ALL INSTRUMENTS PASS — the five sentences ship verbatim.
```
Scanned: digit/brace/underscore/`%`/`×`/`undefined`/`NaN`; em dash and exclamation; §886
chance-meeting adjacency; STATE-NEVER-FATE's ten words; trim and non-emptiness; the SENTINEL
witness (declared slots vs the slots the sentence really carries, all five); the chair's ROLE law
in both directions; every variant naming `{home}`; the duplicate-sentence and slot-arity pins.
**No sentence was rewritten, and none needed to be.**

---

## THE CARS AS LANDED

| car | sha | what |
|---|---|---|
| 1 | `4eb8f1e63` | the annex's third governed block `## §B2 ENC-4c`, the pool leaf's third corpus, the walker's block table re-keyed to POOL IDS, and the seal statement corrected a second time |
| 2 | `74a5cbeca` | the `cases` axis on the registry row, the picker's case parameter, the writer's withhold turned into a selection, and the §B MIRROR arm inverted |

Files EDITED — **no file added, renamed or deleted anywhere** (`git ls-tree` file set identical to
`9d9686c12`):
`docs/content/RECEIPT_POOLS_CHANCE_MEETING.md` · `src/domain/worldPulse/envoyChanceMeetingNews.js` ·
`src/domain/worldPulse/envoyChanceMeetingReceiptPools.js` ·
`tests/lint/chanceMeetingKindPools.walker.test.js` — 581 insertions, 50 deletions.

### THE SHAPE, AND WHY IT IS THIS SHAPE

**A POOL KEY IS NOT A KIND.** `chance_meeting_exposed_host_offered` is a second authored corpus of
the SAME registered kind, hung off that one registry row's new `cases` axis. No registry row, no
routed token, no desk, no Chronicle row and no reader phrase is minted — which is exactly why the
six pantheon A5 freezes cannot move, and they are asserted unmoved rather than hoped.

⛔ **`cases` IS NOT GR-0's `contexts`, AND THE DIVERGENCE IS MEASURED.** ENC-4 left position six
empty on the recorded reasoning that "this corpus authors no split across a fact the seed carries";
it now does, so the reasoning points the other way and the axis arrives. But GR-0's axis filters
variants WITHIN one pool, and folding the ten sentences into one pool would fork the sealed §B
block's own five-and-five split — **which is the proof that the seal's sentences are untouched**.
Reusing a sibling's NAME for a different type and semantics would be worse than a new one.

⚠ **THE ROW'S OWN `pool`/`requiredSlots` STAY THE DEFAULT CASE'S, and that is load-bearing:**
`tests/helpers/kindPoolWalker.js` measures a row's DEPTH from `row.pool`, so a row that moved its
corpus under a case key would measure ZERO and land on `['starved']`.

### ⛔⛔ THE DEFECT THIS CAR EXISTED TO AVOID, FOUND BY DERIVATION AND CONVICTED BY A MUTANT

`{home}` is **the GUEST'S court in both corpora**, never "the approacher's". ENC-4b could read it
off the approacher only because its withhold guaranteed the approacher was the guest. Under the
host-offered case that same read renders `{counterpart} of {home}` as the guest being of the host
town. Mutant 6 restores it and the walker prints the sentence that would have shipped:

```
"Bramwell knows that Sera Vane of Bramwell, passing through, turned Aldo Rell away.
 It does not know what was offered, and it has not stopped guessing."
```
Fluent, compiling, and wrong about where a man is from — the exact wrong-ROLE defect the whole
family exists to refuse. `{home}` is now derived from the HOST, and the builder additionally
asserts that **exactly one** party is of the host town rather than resting on three census
guarantees it does not own.

---

## THE PROOFS, EVERY EXIT CAPTURED IN-SHELL

### ⭐⭐ END TO END ON REAL MINTED WORLDS — 0 of 13 BEFORE, 13 of 13 AFTER

The same 1734-world corpus, re-run at the tip (`$SC/enc4c-tmp/premise.mjs`):
```
EXPOSED SEEDS BY CASE      : {"host_offered":13,"guest_offered":1,"no_direction":0}
OF THOSE, A LINE WAS BUILT : {"host_offered":13,"guest_offered":1,"no_direction":0}    (was 0 / 1)

a host-offered headline, from a real seed:
  "Neither court announced it, and Irontown has it anyway: A Named Legate of Ashford
   refused Host Notable, the town's own."
```
The Legate is of Ashford (the guest) and REFUSED; Host Notable is Irontown's own and OFFERED. The
roles follow the receipt's direction, not the array order.

### THE WRITER, BOTH CASES, THROUGH THE DISPATCH DOOR (`$SC/enc4c-tmp/writer-smoke.mjs`)
```
GUEST OFFERED (§B)   Bramwell knows that Aldo Rell turned Sera Vane of Kesthorne away. …
                     familyId chance_meeting_exposed.4               major 0.76 / 78
HOST  OFFERED (§B2)  Bramwell knows that Sera Vane of Kesthorne, passing through, turned
                     Aldo Rell away. …
                     familyId chance_meeting_exposed_host_offered.4  major 0.76 / 78

THE ROLE LAW ON THE WORDS, BOTH CASES
  guest case names the OFFERER "Sera Vane of Kesthorne" : true
  guest case never says "Aldo Rell of Kesthorne"        : true
  host  case never says "Sera Vane of Bramwell"         : true
  host  case never says "Aldo Rell of Bramwell"         : true
SPREAD  guest_offered: 5 families    host_offered: 5 families   (every variant reachable)
FAILS CLOSED  unknown approacher · no approacher · neither party of the host · both parties of
              the host · one unresolved name · the other beat  →  NULL, all six
THE PICKER    an unauthored case → null; the row default → chance_meeting_exposed.1;
              the one-case row → chance_meeting_recorded.1
```

### THE WALKER, DRY-RUN IN PLAIN NODE — 29 OF 29, WITH EIGHT MUTANTS

`$SC/enc4c-tmp/dry/` (ENC-4b's shim, re-pointed; **nothing written into the dock**):
```
PASS 29  FAIL 0   exit 0
```
⛔ A dry run that cannot fail is not a proof, so it was convicted eight ways. Every plant was
restored by inverse edit and **`cmp`-verified against a backup taken BEFORE the plant**:

| # | mutant | result |
|---|---|---|
| 1 | the §B2 corpus forks from the annex by one word (`season` → `year`) | **FAIL 1** — the document join |
| 2 | §B's terminator reverts to `## §C HOLE 3` | **FAIL 4** — and it prints the exact `[5,15]` defect predicted |
| 3 | a declared heading gains a `.` metacharacter | **FAIL 5** — the reader refuses before it anchors |
| 4 | a §B2 annex row makes §B's assertion `{counterpart} of {settlement}` | **FAIL 3** — corpus, ROLE and sentinel |
| 5 | the `host_offered` case points at the GUEST corpus | **FAIL 3** — familyId, case id, declaration join |
| 6 | `{home}` reverts to ENC-4b's approacher read | **FAIL 2** — prints "Sera Vane of Bramwell" |
| 7 | an unauthored case FALLS BACK to the default corpus | **FAIL 1** — the silence arm |
| 8 | the exactly-one-host-party guard is dropped | **FAIL 1** — the both-host null |

### ⭐ A DEFECT THIS LANE WALKED INTO AND MEASURED OUT OF, THEN BUILT MACHINERY AGAINST

The walker interpolates each block's declared heading into a `RegExp` **unescaped**. The first pool
id this lane reached for was `chance_meeting_exposed.host_offered`, whose dot would have matched any
byte and could have landed the exactly-once guard on a neighbouring block. The heading is now word
characters and spaces only (`### case host_offered of chance_meeting_exposed`), the reader THROWS on
one that is not, and mutant 3 proves the refusal fires rather than decorating.

### EVERY FROZEN CENSUS FIGURE, RE-DERIVED AT THE TIP (`$SC/enc4c-tmp/census.mjs`)
```
UNMOVED  pantheon A5 · registries.length          12
UNMOVED  pantheon A5 · small families             ["INFORMATION","FAITH","CHANCE_MEETING"]
UNMOVED  pantheon A5 · allRows.length             115
UNMOVED  pantheon A5 · routedTokens.length        381
UNMOVED  pantheon A5 · unvoiced.length            274
UNMOVED  pantheon A5 · registered-minus-routed    8
UNMOVED  floors · REGISTERED_KINDS.size 115 · ROUTED_TOKENS 381 · REGISTRIES 12
UNMOVED  floors · floor violations 23 · CHANCE_MEETING violations 0 · major floor 4
UNMOVED  CHANCE_MEETING rows 2 · kinds [recorded, exposed] · row[1] depth 5 / slots 5
⭐ MOVED  CORPORA 2 -> 3   …of which registered as kinds: 2   …the pool id routes nowhere: false
EVERY FROZEN FIGURE HOLDS.
```
⇒ **`tests/lint/kindPoolFloors.walker.test.js` and `tests/domain/pantheon.test.js` need NO edit**,
which is the brief's explicit demand, proved rather than asserted.

⚠ **AND I CHECKED THIS PROBE TOO.** My first run reported `floor violations 27` against a frozen
23. It was MY OMISSION, not a tree move: the walker computes violations with
`{ declaredExceptions: { 'n/a': FREQUENCY_FLOORS.notable } }` and my probe passed none. Corrected,
it reads 23. I report the correction rather than only the clean number.

### THE DARK ARM — BYTE-IDENTICAL, NOT HOPED
ENC-4b's `dark-arm.mjs` re-run at my tip, ten pulses against hashes frozen at `6c49ecbbf` (a commit
predating ENC-3):
```
ticks hashed 10   distinct 3   errands really moved: true
BYTE-IDENTICAL TO PARENT: YES
  five refusal spellings (explicit false / the number one / "true" / {} / "yes") — identical: YES
ALL DARK ARMS IDENTICAL: YES
```

### NON-VITEST GATE STEPS AT FINAL HEAD `74a5cbeca`

| step | exit | note |
|---|---|---|
| `npx eslint src/ tests/ scripts/` | **0** | `✖ 31 problems (0 errors, 31 warnings)` — the identical 31 ENC-4b measured; this train adds none |
| `node scripts/check-domain-strict.mjs` | **0** | `✓ no strict-type regressions (1120 errors, ceiling 1120)` |
| `node scripts/check-hazard-registry.mjs` | **0** | |
| `node scripts/premortem.mjs --self-check` | **0** | |
| `node scripts/check-tuning-bands.mjs` | **0** | ⛔ this lane moved no tuning value |
| `node scripts/check-migration-head.mjs` | **0** | |
| `node scripts/check-observed-shape-readers.mjs` | **0** | |
| `node scripts/implementation-packets.mjs validate` | **0** | |

### INSTRUMENTS MEASURED NOT TO MOVE, EACH BY EXECUTION

| instrument | verdict | how measured |
|---|---|---|
| `chooserTotality` `DEFER_CEILING` / `habitForkRegistry` | **UNMOVED** | the three idiom counts under `src/domain` are `hash01( 33`, `softmaxWeights( 3`, `stableSampleByWeight( 5` — identical to base; the new pool goes through the SAME `chanceMeetingLine` |
| `tuningRegister` bare decimals / `BARE_DECIMAL_CEILING` | **UNMOVED BY ME** (red at base) | **0** bare decimals in every line this train adds to `src/` |
| `entropyRootCensus` `hash01` callers | **UNMOVED BY ME at 18** (red at base) | no new caller module |
| `mutationCoverageManifest` | **UNMOVED BY ME** (red at base) | no test file added |
| `sovereigntyLightingContract` `files`/`credited`/`parked`/`suiteTitles` | **UNMOVED BY ME** (red at base on `files`) | no file added, renamed or deleted; one literal `describe` throughout, so the file stays CREDITED |
| `couplingInclusion` | **UNMOVED** | `git ls-tree` file set identical to `9d9686c12` |
| `.wizard-news-authoring-baseline.json` | **UNMOVED** | no governed site added; the existing builder is edited |
| `scripts/.size-baseline.json` | **UNMOVED** | measured: none of the three `envoyChanceMeeting*` files is baselined |
| `mechanism-lit-coverage` flag denominator | **UNMOVED** | **0** `*Enabled` names in any added line of the whole diff |
| `.news-voice-baseline.json`, `.news-headline-contract-baseline.json` | **UNMOVED** | generated-world corpora; the car is dark in every preset and the dark arm is byte-identical |
| `WHAT_PHRASES` · `EXACT_SECTION` · `KIND_SECTION` | **UNMOVED** | no kind minted; asserted in the walker with the registered id as positive control |

---

## ⛔⛔ THE BRIEF'S REQUIRED ROSTER GREP — AND IT FOUND A TENTH RED NOBODY HAD PAID

`grep -rn 'chance_meeting_exposed' tests/` → 57 hits in **three** files; widened to the sibling kind
and the family → **ten** files. Every one cleared:

| file | verdict |
|---|---|
| `tests/lint/chanceMeetingKindPools.walker.test.js` | **MOVED WITH ATTRIBUTION** — the mirror arm, the block table, six new arms |
| `tests/lint/kindPoolFloors.walker.test.js` | **PROVABLY DOES NOT MOVE** — every figure re-derived above; no edit |
| `tests/domain/pantheon.test.js` | **PROVABLY DOES NOT MOVE** — all six A5 freezes re-derived; no edit |
| `tests/lint/chooserTotality.walker.test.js` | does not move — the only mention is a comment; idiom counts unmoved |
| `tests/lint/.tuning-register.json`, `.tuning-inventory.json` | do not move — they key on `envoyChanceMeeting.js` and `envoyChanceMeetingStage.js`, neither touched; 0 decimals added |
| `tests/helpers/kindRegistryRoster.js` | does not move — the registry is still 2 rows |
| `tests/domain/envoyChanceMeetingStage.test.js` · `envoyChanceMeeting.test.js` · `envoyChanceMeetingLedger.test.js` · `tests/domain/npc/paradigmAxisCatalog.test.js` | do not move — none imports the writer, registry or corpus symbols |

### ⛔⛔ AND THE ROSTER THE PREAMBLE NAMED: `EXPECTED_VOICE` IS RED, AND HAS BEEN SINCE ENC-4

`tests/domain/impactKindWalkers.test.js` mints impactKinds by scanning `src/domain` for
`impactKind: '<literal>'` and forces every one into an `EXPECTED_VOICE` classification. Measured
(`$SC/enc4c-tmp/expected-voice.mjs`):
```
minted impactKind literals under src/domain : 73
UNCLASSIFIED (this arm reds on a non-empty) : ["chance_meeting_exposed","chance_meeting_recorded"]

  at 7be568047 (ENC-4's tip): literals ["impactKind: 'chance_meeting_recorded'"]
  at 9d9686c12 (ENC-4b's tip): + ["impactKind: 'chance_meeting_exposed'"]
  at HEAD (mine)             : IDENTICAL to 9d9686c12

newsVoiceCategory('chance_meeting_recorded') = null | EXPECTED_VOICE[k] = undefined => ARM ALSO REDS
newsVoiceCategory('chance_meeting_exposed')  = null | EXPECTED_VOICE[k] = undefined => ARM ALSO REDS
```
**TWO arms red, one since ENC-4 and one since ENC-4b, and MY CAR MOVES THEM BY ZERO** — both cases
share the ONE registered `impactKind`, so the literal set is byte-identical to `9d9686c12`. That is
the "provably does not move" the brief asked for, and the pre-existing red is the finding.

⛔ **I did not cure it.** Adding two `EXPECTED_VOICE` rows is a **VOICE DECISION** — a real crier
`VoiceCategory` or an explicit `null` — for another lane's kinds, on a surface where the estate's
own convention is that the chair authors voice. The cure is two lines and the chair should choose
them; guessing `null` would bank a content decision behind a test-file edit.

---

## ⛔ VITEST — OWED-UNTIL-RESUME, AND THE FENCE IS THE CHAIR'S OWN HOLD

The chair sent, at ~09:45: *"HOLD ALL VITEST NOW until the chair sends RESUME."* No RESUME has
arrived. At 10:07 the probe read **0 workers, load-1 2.80** — but a quiet probe is not a RESUME, and
I held. **The exact commands, to run in this order the moment RESUME lands:**

```sh
cd $SC/laneENC4B
sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/lint/chanceMeetingKindPools.walker.test.js tests/lint/kindPoolFloors.walker.test.js \
  tests/domain/pantheon.test.js tests/domain/envoyChanceMeetingStage.test.js
sh scripts/gate-mutex.sh --run -- npx vitest run tests/lint/
sh scripts/gate-mutex.sh --run -- npx vitest run tests/domain/impactKindWalkers.test.js
sh scripts/gate-mutex.sh --run -- npx vitest run tests/copy/
```
**THE PASS BAR IS NOT "GREEN".** `tests/lint/` was RED at my base with 9 arms in 7 files (quoted
above). The bar is **the identical failing set, arm for arm** — a tenth arm is mine and must be
attributed. The four-file run WAS green at base and must stay green.

---

## THE REGISTER DELTAS — ⛔ THIS LANE TOOK NO REGISTER ACT OF ANY KIND

No `--update`, `--write`, `--genesis`, `--rebank`, no `*_REFREEZE`/`UPDATE_*` env var, no baseline
JSON edited, no `npm run build`, no `npm install`, no `node_modules` touched, no `git stash`, no
`git add -A`, no `--amend`, no push, no ref write, no `git checkout`.

**The lighting census is the only register this train moves**, and it is measured against the frozen
tuple's own sha (`ff9b7a53c`), not against ENC-4b's tip:

| key | frozen | owed at my tip | attribution |
|---|---|---|---|
| `files` | 2521 | **2522** | ENC-4's new walker file (+1) — ⚠ ENC-4b called this UNMOVED |
| `credited` | 2150 | **2151** | same file, credited (+1) — ⚠ ENC-4b called this UNMOVED |
| `parked` | 371 | **371** | unmoved |
| `suiteTitles` | 6214 | **6215** | one literal `describe` (+1) — ⚠ ENC-4b called this UNMOVED |
| `titles` | 23184 | **23215** | ENC-4 **+14** · ENC-4b **+7** · **ENC-4c +8** (21 → 29) · §894 **+2** (`rumorFallbackPhrasePools`, not ENC's) |

⚠ ENC-4b predicted `titles` 23198 → 23205. Its FINAL figure for its own car is right; its BASE and
its three "UNMOVED" claims are measured against ENC-4's tip rather than the register, and its total
misses §894's two. Grep-derived literal counts, not a census run — a prediction to check, not a
figure to bank.

**Every other register: UNMOVED, each by execution** (table above).

---

## RETROVALIDATION ROW — ⟦Seat: Opus 5 — Fable-unvalidated · Lane: ENC-4c⟧

| # | what was JUDGED | what the chair must re-derive | receipts (absolute paths) | priority |
|---|---|---|---|---|
| R1 | **ENC-4b's "THE CASE IS THE SMALLER OF THE TWO" WAS REFUTED BY MEASUREMENT.** The approach direction ties on `codepoint(target.nid)`, so on a tie the case turns on which HOME ID sorts later — town-name spelling, not the host role. 89/89 flips when only the id ordering flips. | That the refutation is right, and what it means for ENC-4b's F2/R2, which the chair may have already ruled on. The finding that §B is honest on only one case STANDS; only "the smaller" falls. | `$SC/enc4c-tmp/tiebreak-probe.mjs` · `$SC/enc4c-tmp/premise.mjs` · commit `4eb8f1e63` | **HIGH** — it is the difference between an edge case and half the kind |
| R2 | **THE AXIS IS `cases`, NOT GR-0's `contexts`, AND POSITION SIX IS NOW OCCUPIED.** ENC-4's docblock reserved it and declared it deliberately empty; this lane filled it with a different shape and rewrote that paragraph. | Whether a case-keyed POOL is the right divergence from the estate's nearest sibling, or whether the ten sentences should have merged into one pool under `contexts` — accepting that the merge forks the sealed §B block's five-and-five split. | `…/laneENC4B/src/domain/worldPulse/envoyChanceMeetingNews.js#chanceMeetingKindRow` · commit `74a5cbeca` | **HIGH** — it is a registry SHAPE the whole family inherits |
| R3 | **THE ANNEX GAINED A THIRD TOP-LEVEL BLOCK `## §B2 ENC-4c`, AND §B's TERMINATOR MOVED.** The brief said "add the block under the governed §B section"; a block with numbered rows cannot sit INSIDE §B's slice without making its declaration list read fifteen. | That a separate governed section (rather than a nested sub-block with a third reader anchor) is where "under §B" meant. The sealed §A, §B and §C slices are byte-identical, which is asserted and measured. | `…/docs/content/RECEIPT_POOLS_CHANCE_MEETING.md` · the mutant-2 `[5,15]` receipt | **MEDIUM** |
| R4 | **THE POOL ID IS `chance_meeting_exposed_host_offered` AND ITS ANNEX HEADING IS `### case host_offered of chance_meeting_exposed`.** Two different spellings, deliberately: the id must not read as a kind, and the heading must not collide with the sealed `### chance_meeting_exposed` anchor. | That two spellings for one corpus is acceptable, and that the case tokens `guest_offered` / `host_offered` are the names the chair wants on a durable `familyId`. | the walker's `ANNEX_BLOCKS` and `HEADING_SAFE_RE` | **MEDIUM** |
| R5 | **`familyId` IS NOW THE CORPUS'S, NOT THE KIND'S** (`chance_meeting_exposed_host_offered.4`). The default case's poolKey IS the kind, so every existing family spells exactly as before. | That two cases of one kind deserve two family namespaces — I judged yes, because a variety reader would otherwise treat two different sentences as one family. Swept: no walker outside this volume pins a `familyId` prefix against a kind. | `…#chanceMeetingLine` · `$SC/enc4c-tmp/writer-smoke.mjs` | **MEDIUM** |
| R6 | **THE §B MIRROR ARM WAS INVERTED** from `expect(mirror).toBeNull()` to `toBeTruthy()`, with the inverse ROLE law added on the mirror's own words. | That inverting another lane's arm inside this train is right rather than a separate car. It is the lane's whole point, so it is reported loudly rather than buried. | `…/tests/lint/chanceMeetingKindPools.walker.test.js` §B mirror | **HIGH** |
| R7 | **I DID NOT CURE THE TEN PRE-EXISTING REDS** (9 in `tests/lint/`, 2 arms in `impactKindWalkers`). | That reporting rather than curing is right. Six need a REGISTER ACT my brief fences absolutely; `EXPECTED_VOICE` needs a VOICE DECISION for another lane's kinds. ⛔ **THE CHAIR'S LANDING BILL IS BIGGER THAN ANY RECEIPT SO FAR SAYS.** | the base-run table in this receipt · `$SC/enc4c-tmp/base-lintdir.log` · `$SC/enc4c-tmp/expected-voice.mjs` | **HIGH** |
| R8 | **THE BUILDER NOW ASSERTS EXACTLY ONE PARTY IS OF THE HOST TOWN**, rather than resting on the stage's three census guarantees. | That a belt the stage already guarantees is worth its line — I judged yes, because both corpora's `{settlement}`/`{home}` fills are meaningless otherwise and the builder does not own those guarantees. | `…#chanceMeetingExposedEntry` · mutant 8 | **LOW** |

---

## WHAT THIS LANE STOPPED ON

1. **VITEST.** The chair's explicit hold, still in force. Exact commands and the pass bar above.
   ⚠ The bar is the IDENTICAL failing set, not "green" — `tests/lint/` is red at base.
2. **THE TEN PRE-EXISTING REDS.** Reported, attributed by message, not cured. Six need a register
   act; `EXPECTED_VOICE` needs a voice decision. All are the chair's.
3. **THE traveller × traveller KIND.** Untouched. The R1 fence holds by absence — the stage writes
   no `approacherNid` there, and the builder's null for it is re-asserted in the inverted mirror arm.
4. **THE SEALED §A/§B/§C SENTENCES.** Not one byte. Measured, three slices, byte-identical.
5. **NO REGISTER ACT OF ANY KIND.**
6. **THE DOCK IS NOT SEALED.** My brief bars ref writes, so `refs/preserve/enc4c-*` was NOT created.
   ⛔ Six cars (ENC-4b's four and my two) exist ONLY as the working HEAD of `$SC/laneENC4B` at
   `74a5cbeca7059e9f03c87ac6eceafa59652320b4`, inside a scratch directory — the exact exposure the
   recorded "a scratch-dir death takes every dock with it" hazard describes, and ENC-4b recommended
   the same seal one lane ago. **Recommend the chair seal this HEAD before anything else.**

## DRIVERS AND LOGS BY ABSOLUTE PATH

| what | path |
|---|---|
| the premise: 1734 minted worlds, both cases | `$SC/enc4c-tmp/premise.mjs` |
| the discarded mirror control (measured nothing, kept for audit) | `$SC/enc4c-tmp/premise-mirror.mjs` |
| **the tiebreak refutation, 89/89** | `$SC/enc4c-tmp/tiebreak-probe.mjs` |
| the chair's words against every instrument | `$SC/enc4c-tmp/words-check.mjs` |
| the writer, both cases, end to end | `$SC/enc4c-tmp/writer-smoke.mjs` |
| the pantheon A5 + floors re-derivation | `$SC/enc4c-tmp/census.mjs` |
| the `EXPECTED_VOICE` measurement and its attribution | `$SC/enc4c-tmp/expected-voice.mjs` |
| the walker dry run, its shim and its eight mutants | `$SC/enc4c-tmp/dry/` |
| **the BASE vitest runs (before the hold)** | `$SC/enc4c-tmp/base-vitest.log` · `$SC/enc4c-tmp/base-lintdir.log` |
| eslint logs, car 1 and car 2 | `$SC/enc4c-tmp/eslint-car1.log` · `eslint-car2.log` |
| pre-plant backups (every mutant restore `cmp`-verified against these) | `$SC/enc4c-tmp/preplant/` |
| the quiet-window waiter and its log | `$SC/enc4c-tmp/quiet-window.sh` · `quiet-window.log` |

## DOCK TIP

**`74a5cbeca7059e9f03c87ac6eceafa59652320b4`** — `$SC/laneENC4B`, detached, porcelain **0**,
`node_modules` still 453 symlinks, never materialised.
