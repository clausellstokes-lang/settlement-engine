# DESIGN_FMG_WEAVE — the FMG reconciliation integration program

**Authored 2026-08-29 by the Fable chair under ODQ §728 (owner: "integrate it all cohesively, comprehensively and coherently and carefully before the soak. architecture everything out before implementing"). Inputs: the §727 review (`docs/REVIEW_FMG_RECONCILIATION.md`) and the eight-lane verification fan-out `wf_089ba33d-e4e` (reports landed verbatim in `docs/DESIGN_FMG_WEAVE_VERIFICATION.md` — every file:line in this volume traces to one of them). Seats: Fable architected; Opus implements and verifies, wave by wave, after the skeptic panel rules. This volume is the implementation contract; a lane that discovers the tree disagrees with it reports the disagreement — the tree wins, the volume amends.**

## §0 · Scope and standing

Everything in the §727 review integrates: the twelve candidates, the smaller steals, and the S1–S5 seam repairs — landing BEFORE the endgame tail's walk → ONE regen → terminal soak → tuning signature. The §727.3 guard list binds every wave (no regression toward FMG's RNG/DOM idioms, one-shot regen philosophy, static diplomacy, premade pools). The §11.1 GPL laws are untouched by this program: FMG is MIT and every port here is engine/bridge-side; MIT attribution rides any verbatim copy (LICENSE-FMG.txt precedent).

## §1 · Program laws (bind every car)

1. **RNG:** keyed forks only (`prng.js:84` — `fork(label)` derives from seed string, not stream position; `::` is load-bearing vocabulary, `epoch` head reserved). Never the ambient stream for new draws; never FMG's `Math.random = Alea(seed)` monkey-patch. New entropy roots/compositions graze `entropyRootCensus.walker` (two exact-set shrink-only baselines) — draw through an existing forked rng where possible; a genuinely new composition owes a disposition row + chair ruling in the same act.
2. **Dormancy bar, stated per wave:** persisted-state waves (digest sub-keys, ledger rows, news fields, plan shape) owe the **raw-byte** bar (§713.2 — a dormancy claim is a bit claim); view-only waves owe the `dormancyOracle` structural bar. The charter row of every car names its bar.
3. **Flags:** engine flags via the `ENGINE_GATED_VIRTUAL_RULE_KEYS` idiom — strict by-name `=== true` gate reads, list entry, certification row (`subsystemRowsVirtual.js`), lit credit under `mechanismLitCoverage`, dormancy proof, ONE commit. Capture-side opt-ins via the V-6 additive-key sub-digest idiom, each self-versioned (`BIOME_TEXTURE_VERSION` precedent), absent ⇒ byte-identical.
4. **Version axes:** canon-affecting geometry/cost changes bump `SPATIAL_GEOMETRY_VERSION`/`COST_LAW_VERSION`/slot versions — a DISCRETE re-canonize event (§V.1), old canons keep their axes forever. The route-network genesis lane has NO version constant today — W-NET-b mints a `ROUTE_GENESIS_LAW_VERSION` recorded on the ledger at connect (nothing pins "which law derived this ledger" otherwise).
5. **Test census economics (V-COLLIDE §2):** the test census sits AT its ceiling. Extend the per-family rosters named in the verification report; the program's whole new-file bill is ~4–6 files (NAME Markov module · POLIS territory + placement-assertion arm · VAR heightmap interpreter · possibly SEAM canonize) — **land new files batched, one lighting-census re-derivation for all**; `*Golden*`-basenamed files owe mutation-manifest rationales.
6. **Placement-assertion law (§710.6):** any spatial mark family (territory shading, overlays) ships with a placement assertion, and that assertion stays OUT of the gate until green at its own tip.
7. **Consumer censuses re-run at dispatch** against the then-current tree (V2) — never inherited from this volume's figures.
8. **MIT attribution:** verbatim copies (Urquhart extraction, heightmap interpreter + template strings, Markov chain code if copied, Penman line) are noted in `THIRD-PARTY-NOTICES.md` in the same landing act.
9. **Declared shifts:** same-seed changes to NEW worlds/foundings/plans are legitimate generation changes but every one is DECLARED in its landing act (fingerprint/golden re-records named with cause). Lived worlds never move; anything that would move one is a defect, full stop.

## §2 · Substrate decisions (the cohesion layer — shared spines, decided once)

- **D1 · ONE capture-extension act (W-CAP-1).** All new pack fields (`fl`; `temp`+`prec`+`g`) enter `getSpatialPack` in a single bridge change, with `normalizeSpatialPack` extended in the same act (it silently DROPS unknown keys — every capture assertion must be a survives-normalize assertion) and new arrays excluded from the `cellCount = Math.min(...)` (the `r`/`biome` precedent). Double-capture payload growth is measured and reported against the 2×20s RPC bound. Haven/`t` are NOT captured (render-tier #9 uses them live in the iframe; an engine-side tiered water cost later is a `SEA_LANE_VERSION` bump).
- **D2 · ONE water flood-fill home.** `seaLanes.waterComponents` (currently private) is promoted to a shared module producing BOTH views: sailing components (existing) and interior-vs-ocean components + typology (#10) — never a third ad-hoc BFS. The two-endpoint invariant is already structurally enforced at the lane tier; its steal reduces to an advisory + legibility read (V-FAITH-GOODS §4).
- **D3 · ONE naming module (`src/domain/naming/`).** Corpora authored by US for the **11** culture keys (not the review's 9 — `resolveConfig.js:352`), new data files (NAMING_DATA untouched — decontamination test pins it), lazy-only (an eager edge reds the ~2KB-headroom closure ratchet). The module CENTRALIZES culture resolution, curing the confirmed three-way fork (`mediterranean`/`mixed`/unknown → latin vs germanic vs union-of-11 depending on consumer) behind one exported resolver — every consumer (S1 genesis, S2 steadings, N1/N2/N3 NPC, wards while townCartography lives) migrates to it under the flag. successorNpc's culture-blind 27-name pool is cured by the same act.
- **D4 · Display-side proper nouns by stable-key hash, not PRNG, not persistence.** War names AND route names both land as pure derivers on the `calamityKernel` precedent (`pickLine(pool, stableKey)` — fnv1a32, zero draws, zero state, no flag): war key = sorted participants + warReason type + tick; route key = the codepoint-sorted `a|b` pair. Rendered via new interpolation tokens in the prose registries (`{road}`, the `{payer}` precedent). **This supersedes the review's persisted-route-name flag** — ledger untouched, canon risk zero. If the owner later wants names ON the ledger rows, that is a separate dormant-flagged car (owner row Q-W2).
- **D5 · ONE plan car for `worldPlan.js` (V7).** Genesis diplomacy assignment (#5) and pyramid jitter + sentinel randomization (#11a) both perturb `deriveWorldPlan` — they land as ONE car with ONE declared same-seed-plan shift. Fork labels: `'diplomacy'`, `'pyramid'`, `'tone'` (fork is draw-order-safe by construction).
- **D6 · Genesis diplomacy: no spatial prior.** The scatter is declared cosmetic (worldPlan.js:126-130) and geography does not exist at compose time (iframe-bound) — so the bootstrap uses tier-ratio + weighted drawn tables (FMG's own dominant signal is area-ratio), writing slot-addressed `plan.relations`, materialized as reciprocal `neighbourNetwork` links (ids, not names) BEFORE the `:266` discovery call, with `source:'genesis_diplomacy'` link provenance, NO `relationshipStates` writes (lazy materialization from `RELATIONSHIP_DEFAULTS` is the correct and byte-cheapest path), types drawn ONLY from the existing closed vocabularies, and NO tick-stamped history (a founding-time type+reason is generation; a fake transition-at-tick-N is fabricated lived history). The `instantWorldFingerprint` gains a slot-addressed relations section in the same act (it does not cover edges today — a determinism blind spot exactly where the feature lands). The ongoing-war option (deployments row + war_front) is a SEPARATE sub-car, tone-gated `dramatic_campaign`, dispatched only after the base car's first-tick behavior is observed under the mobilization ramp.
- **D7 · Heightmap DSL determinism kernel.** The interpreter's core uses fractional `**` throughout (not just coastline cosine). Ruling: build `detPow` — a fixed polynomial exp2/log2 kernel from + − × ÷ (the house ban targets built-ins' cross-engine variance, not the operation); preserve Uint8 truncation/clamp semantics exactly; `leastIndex` first-minimum tie order verbatim. Substrate: **lattice 8-neighbour adjacency first** (zero new deps, determinism trivially provable); the voronoi/Delaunator option is a recorded upgrade path if coast character disappoints (vetoable). Template roster pinned to SF_TEMPLATES ∪ `sfArchipelago`; the 8 uncurated upstream templates enter only by owner-visible revisit of the single-landmass rule. Landing zone: pure leaf consumed by `deriveRealmScene` + behind the composer's dynamic import — NEVER eager (the plate occupies zero dist chunks today; keep it that way).
- **D8 · Suitability defaults are #12-coupled.** At compose time nothing exists to read (pack is iframe-bound; instant sites are planned blind); the manual wizard can't know its site (placement requires canon). So: stage 1 = the S3 `terrainAgreement` fit chip at drop time (live pack available); stage 2 = composer-side suggestions once #12's headless heightfield exists. No independent suitability car.
- **D9 · Overlay registry generalizes `WarFaithMapOverlay`** (it already IS the shape: pure read-models, inert-when-absent, layer-toggle idiom). Feeds, all settlement-id-anchored and resolver-complete via `mapState.placements`: sieges, occupations (durable), calamities (per-settlement history — the best feed), epidemic set, conditions, faith standing. Field battles need the ONE engine byte change of the family: a structured `region` field on `fieldBattleNews` entries (owner row Q-W4; future-tick entries only, declared). Ruin markers read settlement state (occupations/powerStructure), never the capped news feed.
- **D10 · Territory view (#8)** is a view-time, RNG-free derivation over the saved `fmgSnapshot`'s live pack (the digest cannot serve it — no coordinates persist), reading pack adjacency directly (`readPackArrays(...).c` — the RealmRaster drops it), tier as expansionism, budget caps leaving honest wilderness, FMG's normalize passes as the hygiene our thin-ring/open-country lessons demand. Consumed as map-overlay shading first (no trig ban there); realm-plate shading only via authored-table ops. Prominence ladder = a derived display extension of hegemony's `SHARE_BANDS` — descriptive always, DM christens, never an engine-minted entity (the frozen hegemony ruling).
- **D11 · Goods unification is a logical namespace, not a file merge.** The split is ≥9 modules, not 5; the eager/lazy chunk law FORBIDS a single file (resourceChains must stay lazy); catalog shapes leak into persisted saves (`resourceAnalysis.exploitation` holds RESOURCE_CHAINS objects — reader fallbacks preserved); `COMMODITY_SCAN` order is load-bearing. Shape: one typed record vocabulary + one index module re-exporting the physical chunks, joins proven by the six existing join gates. **Lands BEFORE DW-0 compiles its charter** (V6).
- **D12 · Composer pipeline-as-data** = an instance-scoped step list (upstream's `new Pipeline` shape) carrying our `reads/provides/mutates` contract — NOT `registerStep` (the global registry would splice composer steps into the settlement pipeline's topo sort, which the composer invokes nested). The car's real content is declaring the four hidden couplings: id-order (shared `_idN` counter — key ids or pin order), the placements-by-index join (join on `slot`), the `dedupeWorldFactionNames` in-place mutation, the shared `now`.

## §3 · The wave families and cars

**W-SEAM — correctness first (engine/bridge; STRIP-independent; dispatchable immediately after panel).**
- SEAM-0 (execution verification, S): reproduce or refute the **instant-world canonize lockout** (composer mints `cellId: null` on every placement; capture drops non-integer cellIds; nothing re-derives ⇒ capture plausibly returns null on an untouched instant world). If real: launch-relevant P1, cured by SEAM-2.
- SEAM-1 (S): S1 session divergence flag (terrainChanged listener + the honest note that tool-activation under-covers) + S3 `terrainAgreement` deriver (typed mapping between the two closed vocabularies, tri-state, drop-time chip + canonize receipt; never auto-rewrites `terrainType`).
- SEAM-2 (S): S2 capture-time re-resolution of every placement's cell from stored x/y against the captured pack (nearest-centroid over `cells.p`), `cell_remapped` receipt rows beside `skippedSettlements`; cures SEAM-0's lockout (re-derives null cellIds at capture).
- SEAM-3 (M, **owner row Q-W1**): the provenance-stamped persisted capture sidecar — ONE persistence-shape decision underwriting the S1 fingerprint diff, S2's stable substrate, S5's headless re-canonize, and the §718 replayable-derivation doctrine. Absent key ⇒ old campaigns untouched.
- SEAM-4 (S): S4 comparison receipt (fixture pack → digest gates vs `computeRoadEdges`; every primary hop drawn or documented) — test-only; the product-legend decision it surfaces goes to the docket only if the receipt finds divergence users would see.
- SEAM-5 (S): S5 cheap half — "N foundings since geography was mapped" owed-re-canonize signal on the existing gate CTA.

**W-CAP — capture + banding (engine; after SEAM-2 lands; raw-byte dormancy bar).**
- CAP-1 (S): D1's single capture-extension act + survives-normalize assertions + payload measurement.
- CAP-2 (S): river navigability banding (`stream|river|great_river`, cut at MIN_NAVIGABLE_FLUX=100) inside `derivePortEligibility`/`buildSeaLanes`; `SEA_LANE_VERSION` 1→2; port = geography ∧ institution ∧ (river ⇒ navigable). Absent `fl` in old fixtures degrades to today's `r≠0` rule.
- CAP-3 (S-M): climate band sub-digest (`harsh|standard|mild` from banded temp/prec at the settlement's grid cell via `g`), own version + virtual opt-in flag (`climateTruthEnabled` on the `biomeTruthEnabled` idiom); consumed by `SEASONS_TUNING` when lit; seasons dormancy suite is the oracle.
- CAP-4 (S-M): lake typology via D2's shared flood-fill (interior `h<20` non-border components; subtype from the water budget once CAP-3's temp/prec exist; `steadingTopography` consumer first). 400KB digest cap respected (ids + shoreline ids + subtype, never per-cell rosters).

**W-NAME (engine + display; Markov car after STRIP-4 removes the wards consumer, display cars anytime).**
- NAME-1 (S): war-name deriver (D4) + `{road}`-token route names, display-side, flagless, zero canon bytes. Herald/news suites extend.
- NAME-2 (M): the Markov module + 11 authored corpora + centralized culture resolution + successorNpc cure, behind `markovNamingEnabled` (virtual flag); dark path keeps every legacy ambient draw byte-exactly (the draw-stream displacement hazard is the WHOLE record, not the name); lit = declared shift, new worlds/foundings only. Golden estate: generatorGoldenMaster + settlementLifecycleDormancyGolden extended, not re-minted.

**W-NET (render tier anytime; canon tier lands dark behind the pre-existing wiring wave).**
- NET-1 (S): render-tier Urquhart supergraph + corridor-reuse (×0.5 via a `usedCellPairs` set threaded through the deterministic edge order; `costFn` gains the current-cell parameter) + off-burg multiplier (×3) + #9 haven-exit + coast-graded `seaCost` — all through the `addEdge` chokepoint (hostile suppression, dedupe, typed reasons survive; new reason word `'urquhart'` extends the closed set) + the render-tier port definition unified onto the digest's `isPort` when a canon exists. Declared visual + `route_count`/`MAP_ROUTE_DRAWN` telemetry shift.
- NET-2 (M): canon-tier Urquhart as the FOURTH candidate key in `genesisCandidatePairs` (+ fourth bound term; planarity keeps the anti-quadratic law assertable), Urquhart pairs READ the access ladder (Law 2: `tradeRouteAccess` is frozen truth — no bypass rung), the ports/water unconditional-mint rule stated (an Urquhart port-pair defers to `portTotalityPairs`' lane logic rather than minting unconditional water edges), flows-admissibility re-pinned, `ROUTE_GENESIS_LAW_VERSION` minted (§1.4). Lands into the dark, unmounted lane; the wiring wave (pre-existing queue item, `spatialUsage.js:349`) decides activation.

**W-POLIS.**
- POLIS-1 = D5's plan car (M): genesis diplomacy assignment + suzerain-closure (plan-side, pure) + pyramid jitter + sentinel randomization + fingerprint extension + worldCode implications (owner row Q-W3: v2 payload bump, folding the pre-existing magic-not-in-code defect into the same versioned fix). One declared plan shift.
- POLIS-2 (S): materialization half of #5 (reciprocal links before discovery, D6 provenance) — separated from POLIS-1 so the plan law and the composer change land as reviewable units.
- POLIS-3 (M): the overlay registry (D9) + the Q-W4 field-battle site field when granted.
- POLIS-4 (M-L): territory partition view (D10) + placement-assertion arm (out-of-gate until green at tip) + prominence display ladder.
- POLIS-5 (sub-car, held): genesis ongoing-war option (D6 tail), dispatched only after POLIS-2 ships and first-tick ramp behavior is observed.

**W-VAR.**
- VAR-1 (L): the heightmap interpreter port under D7 (detPow, lattice substrate, template roster, verbatim-template goldens, MIT notice) as a pure leaf + build-time consumer (plate coast/relief from the real heightfield, sites clamped to land).
- VAR-2 (M): stage-2 composer adoption — heightfield into `worldPlan`'s site scatter (coast-encloses-sites causality inverts) as a VERSIONED, dormant-gated plan law; today's blob is the absent-flag byte-identical path. D8's composer-side suitability suggestions ride this car.

**W-STEALS (small, slot into quiet windows).**
- ST-1 (S): faith `origins` lineage — typed field, closed cause vocabulary (`founding|imposed|assigned|spread`+source id threaded through the existing opts bag), absent-when-dark/appended-LAST idiom, new-entries-only, fieldManifest row; DEITY DOCTRINE: structure only.
- ST-2 (M): goods unification per D11 — before DW-0.
- ST-3 (S-M): composer pipeline-as-data per D12.
- ST-4 (S): two-endpoint advisory + port-without-partner legibility read; calamity flavor table widening (tsunami/avalanche as second words per terrain, cosmetic, keyed fork).

**W-ARMS (L, last, §728-ratified, vetoable):** generative heraldry with kinship inheritance (vassal quarters the liege's charge) over the relationship vocabularies and the 8-emblem substrate; web/PDF seam honored; only the 104 pruned CC0 charges may be referenced.

## §4 · Sequencing and collision laws

1. **STRIP charter amendments (chair authority, landing with this volume):** (a) **STRIP-3 is re-scoped** — `src/store/mapSlice.js` is the WORLD map slice (zero townMap references; 10+ world-map importers) and MUST NOT be deleted; STRIP-3's true surface is `fogEditSlice.js`/`fogEditBody.js`, `domain/townMap/mapEdits.js`, and the town-map snapshot DB contracts. (b) **STRIP-6's terminal roster is a directory allowlist**, never an exact word-set — WEAVE adds realm-surface map-word code afterward by design.
2. **Order:** STRIP waves land first on any shared surface; WEAVE engine cars (SEAM, CAP, NET, NAME-1, ST-1/3/4) are STRIP-independent and may build in parallel worktrees under the one-gate law; WEAVE UI cars (POLIS-3/4 overlays, VAR wizard pins) land after STRIP-6 (baseline churn must not interleave, V3); NAME-2 after STRIP-4; ST-2 before DW-0; NAME-1 before DW-7; W-ARMS last before the endgame residue.
3. **Launch-tail placement:** …STRIP → WEAVE engine + ST-2 → DW (~42) with WEAVE UI cars interleaved post-STRIP-6 → AD (~17, fresh brief) → remaining catalog residue → OSR mint → parity → ultra [owner] → walk + ONE regen → terminal soak → tuning [owner]. Everything here is pre-soak, per §728.
4. **Per-car discipline:** own worktree from the current build tip · own node_modules · full detached gate with two-part exit reading · receipt + 30-minute checkpoints · explicit pathspecs · re-prove at the committed tip · consumer census re-run at dispatch · declared shifts named in the landing act · chair CASes on chair-verified green.

## §5 · Owner rows (batched; nothing here blocks the ungated cars)

- **Q-W1** — SEAM-3's persisted capture sidecar (persistence shape; buys headless re-canonize + the three-seam convergent repair). REC: grant.
- **Q-W2** — route names: display-side only (D4, default) or ALSO persisted on ledger rows later (dormant flag). REC: display-side now, revisit post-launch.
- **Q-W3** — worldCode v2 payload (jitter constants + the magic-not-in-code defect folded into one versioned migration). REC: grant.
- **Q-W4** — the structured `region` field on field-battle news entries (future-tick persisted bytes, declared). REC: grant.
- Standing: W-ARMS remains vetoable; the voronoi/Delaunator upgrade path (D7) and any revisit of the single-landmass template rule are owner-visible decisions.

## §6 · Panel and seal

This volume goes to a five-lens refute-oriented skeptic panel (determinism/dormancy · THE PROMISE/lived history · gate/census economics · architecture cohesion · product value+sequencing) before any car dispatches. Blockers fold as Amendment A1, which outranks the body. The verification appendix (`DESIGN_FMG_WEAVE_VERIFICATION.md`) is the receipts layer; where volume and appendix disagree, the appendix's quoted code wins.


---

# AMENDMENT A1 — the skeptic panel's findings, folded (2026-08-29, panel wf_0f71adeb-14c: five lenses, 5/5 NOT-REFUTED with amendments; verbatim lens reports in the verification appendix). A1 OUTRANKS THE BODY wherever they disagree.

## A1.1 · BLOCKER FOLDS

1. **D4 is re-ruled (three lenses converged).** "Display-side, flagless" was FALSE as written: the cited `{payer}`/calamity precedents are MINT-time interpolations writing persisted news bytes at tick, and no war identity anchor exists anywhere (no warId/openingTick; per-army deployments only; participants drift as coalitions join) — the "stable key" was unstable and the same war would rename at every mention. Re-ruling: (a) war/route name derivers live in the **display leaf family** (`src/domain/display/`, beside `warStatus.js`) and interpolate at RENDER time only — the prose-registry mint paths are untouched; this also lands the mechanismLitCoverage credit correctly. (b) The war key is (sorted ORIGINAL belligerent pair + warReason type + OPENING tick), and NAME-1's first act is locating a durable render-side source for all three; **if none exists, the fallback is ruled now**: drop tick and coalition members from the key (same-pair+same-reason collisions accepted and documented) — the persisted-opening-stamp branch moves to Q-W2's owner-gated side. (c) The route key folds MODE (`a|b|mode`). (d) Name tokens AUGMENT the `{dest}`/`{settlement}` address tokens, never replace them (news-address law). (e) Grammar content is pinned to the typed warReason — a name never asserts events beyond the typed cause.
2. **§4.1(a) is corrected against this volume's own error.** The three `tests/security` map-snapshot/gallery contracts are **WORLD-map security guards** (stored-XSS on gallery snapshot import; the 089-bypass trigger; membership leakage) — they STAY, unconditionally; no town-map DB snapshot column exists (`burg_settlement_map` is world-map). STRIP-3's surface is `fogEditSlice.js`/`fogEditBody.js` + `domain/townMap/mapEdits.js` + a FRESH column census at its own dispatch. (The V-collisions V1 row carried the same mislabel; this supersedes it.)
3. **Two smuggled persistence shapes are owner-rowed.** **Q-W5**: ST-1's faith `origin` field family (record shape + closed cause vocabulary; REC grant). **Q-W6**: NET-2's `ROUTE_GENESIS_LAW_VERSION` ledger stamp (REC grant). Neither blocks its family's other cars. ST-1 additionally: a **virtual flag**, the **raw-byte bar**, and the **no-backfill law** — absent origin = pre-lineage entry forever; the legacy/lazy seed paths mint NO origin field (stamping `founding` onto a faith whose origin the canon never recorded is fabricated history).
4. **D11's "one index module" is struck** (it recreates the exact eager re-export edge `resourceChains.js:1-17` forbids, against ~2KB closure headroom). Shape: a **types-only vocabulary module** (typedefs, zero value exports) + TWO physical index surfaces (eager identity/terrain; lazy chain/demand) + an explicit migration roster naming which of the 17/3/2/3/13 importers moves where.

## A1.2 · MAJOR FOLDS

5. **D6's receipt is corrected**: site x/y ARE in the structural fingerprint (`composeInstantWorld.js:374`) — the in-tree comment at `worldPlan.js:126-130` is stale and gets fixed by the first plan-touching car. D6's no-spatial-prior conclusion stands on the materialize receipt alone. VAR-1's site clamp is **plate-display-side only** (plan x/y untouched).
6. **Q-W3 widens**: the ruling must cover what a PRE-shift v1 worldCode replays after any declared generation shift (the written byte-replay commons promise) — owner-visible. **D5 pre-declares the fallback** so POLIS-1 dispatches on either answer: if v2 is deferred, jitter constants freeze-for-life under v1 and the v2 migration folds into a later car. §5's "nothing blocks the ungated cars" is corrected accordingly.
7. **Per-car dormancy-bar lines are now mandatory** (§1.2 enforced): SEAM-3, NAME-2, NET-2, POLIS-1/2, ST-1, ST-4 = raw-byte; CAP-3 adds a raw-byte arm beside the structural seasons oracle (the shared `dormancyOracle` is normalize-based and cannot discharge §713.2 alone). ST-4 is re-chartered: the calamity `type` is persisted and today minted with ZERO draws — widening the table changes future-tick selection on lived worlds; it takes a declared-shift row and the raw-byte bar, or defers.
8. **POLIS-5 is bounded**: a Dramatic-tone first-N-ticks observation harness lands IN POLIS-2's act, with a terminal fork — dispatch POLIS-5 pre-soak, or convert to an owner row recording the deferral. Its charter enumerates the coherence set it writes (deployments/war_front/warPosture/warExhaustion — which stamped, which empty), and the founding-stamp law: every tick-valued field = founding tick; no back-dated ramps, turningPoints, or incidents.
9. **D10/POLIS-4 gains the second-truth discipline**: a partition-vs-digest comparison receipt (divergence documented) + consumption of SEAM-1's divergence flag, OR an explicit in-legend declaration that shading is impressionistic display — decided in the charter. Territory seeds derive from placement x/y via **the SAME shared nearest-centroid helper SEAM-2 mints** (one resolution law, two consumers; instant worlds carry `cellId: null`, so stored ids cannot seed the flood). That helper's spec: **squared-distance compare (no sqrt/hypot), first-minimum lowest-index tie order** — binding SEAM-2 identically. POLIS-3 carries its placement-assertion arm or a written exemption rationale.
10. **SEAM-3 ↔ CAP-1 are ordered and versioned**: Q-W1's grant covers a **self-versioned sidecar** (`CAPTURE_SIDECAR_VERSION`, sub-digest precedent); CAP-1 fields are tolerated-absent keys in it; a headless re-canonize from an under-versioned sidecar emits a receipt row, never silently degrades; explicit order SEAM-3 then CAP-1, or CAP-1 first with the sidecar minting at the widened shape.
11. **D3 declares the lit resolution law now** (so two lanes cannot fork it): lit table — `mediterranean → latin`, `mixed → stable per-settlement seeded pick` (one culture per settlement, drawn once from the settlement's own keyed fork — per-name randomness ends), `unknown → germanic`; scope = NAME-POOL resolution only (the `cultureProfiles` alias layer is untouched); dark path = legacy call sites textually untouched, resolver consulted only inside the lit branch; the "wards" migration clause is STRUCK (STRIP-4 removes that consumer first). NAME-2 additionally: fork keys use the RESOLVED token; the successorNpc name draws from its own keyed sub-fork (variable draw counts must not displace the personality picks); the successorNpc cure is a FUTURE-TICK change on lived campaigns (declared as such, not "new worlds only"); the regen path is stated (regen under lit flag = declared shift).
12. **Composer collision ordered**: POLIS-2 lands BEFORE ST-3; D6's insertion contract is step-relative (after dedupe, before graph discovery), never line-addressed.
13. **Census economics corrected**: the new-file bill is "~4–6 for the six verified families"; W-ARMS, ST-2, ST-3 owe their own bills at charter compile; SEAM-4's home is named at dispatch (new file, or a declared cross-import extension); new files land **batched per landing window** (one lighting-census re-derivation per window — all five figures, same commit — not one-for-all across months); the placement-assertion arm lives as a `scripts/`-side instrument until it is green at tip, entering `tests/` only in the act that makes it green; the mutation-manifest trigger is the full basename list (census|scan|baseline|ratchet|walker|…|golden|contract|pin) plus the seven enforcer dirs; every new file under a generation-facing root starts at negativeAssertionAnchor ceiling 0. §1.1's entropy-census sentence is corrected: that census binds worldState-rooted compositions only; plan/display-side entropy is guarded by the fingerprint + fork-label walker.
14. **Sequencing addenda**: NET-1's export-visible half lands after STRIP-2 (one parity-golden re-record, not two interleaved); **VAR-3 is minted** (the review's #11b per-knob wizard pins — S, UI-state only, post-STRIP-6; previously a dropped thread); CAP-4's rule: sub-digests read CAPTURED inputs only, never sibling sub-digests — lake subtype is typed `unknown` where climate fields are uncaptured; W-ARMS owes determinism/dormancy/test-bill rows at its dispatch; VAR-1 adds: our-output goldens only (upstream `Math.pow` parity is unattainable by construction — verbatim templates, self-recorded goldens), ToUint8-wraparound vs clamped semantics named per upstream site, a defined detPow 0-base branch, and the two DOM reads parameterized (a bare `window` read is not an import and evades the layer walker).

## A1.3 · Attacks that failed (recorded so the seal is honest)
The panel could not refute: fork-label safety and the dark-path byte-exactness mechanisms; CAP's absent-key dormancy on old canons; NET-2's dark-lane claim; POLIS-1's inability to touch lived worlds; D2's flood-fill promotion; D7's detPow soundness (IEEE-754 + − × ÷ and sqrt are correctly-rounded — the kernel is cross-machine stable by construction); D8's coupling; the guard list row-by-row; SEAM-0's priority; W-ARMS's ratification; the pre-soak placement of the whole program.


---

# AMENDMENT A2 — the published-materials addendum, tie-back-verified and folded (2026-08-29; review `wf_6ce0f553-76f`, tie-back pass `wf_8a4f0e33-8c8` per the owner's TIE-BACK LAW — every claim below is code-verified both directions; full record in `docs/REVIEW_FMG_PUBLISHED_MATERIALS.md`). A2 extends A1; A1 still outranks the body.

## A2.1 · New cars accepted (all SURVIVED tie-back; amendments folded)

- **NAME-3 (S-M) — engagement-narrative deriver + battle-kind taxonomy.** Display-leaf, A1.1.1 family, as **enrichment of the existing `armyStrength.js`/`warStatus.js` display family** (already PDF-wired at `liveWorld.js`) — not a new surface. Kind derived at render from record data (naval/convoy/terrain flavors); phase sequences from closed per-kind vocabularies; every narrated fact must stand in the engagement record; our receipted roll may be narrated verbatim (`warDeployment.js:813-816`). Terrain caveat NARROWED: sieges read `resolveSettlementTerrain` live and field battles carry `battle.region`; the additive terrain field is needed only if region→terrain-class is underivable from the frozen digest — determine before requesting the Q-W4-family grant. Respect WarFaithTab's recorded light-import constraint (stay off the `militaryStrength` heavy chain on first-paint-adjacent surfaces).
- **NAME-4 (M) — typed force-composition clerk.** `deriveForceComposition(settlement, deploymentRecord) → {type: count}`, closed ~6-type medieval vocabulary (+`war_mages` under magic), authored weight tables; derived-on-read, zero bytes, composition NEVER an input to strength. Absence row corrected: the only unit-word hits in the war stack are the two materiel regexes (`militaryStrength.js:85`, `tradeSalience.js:60`).
- **W-DESK (new wave; all display-plane; post-STRIP-6 per §4.2):** **DESK-1** realm census table (register⇄table toggle; DM-tier numeric, player-tier banded — the heraldRegister legibility law is load-bearing; militarization index debuts here) · **DESK-2** map↔word linkage (`hoveredSettlementId` exists at `mapSlice.js:237`; zero pan-to affordances exist; the camera is MODE-SPECIFIC — pan-to must resolve per-mode coordinates) · **DESK-3** authored comparison charts (their group-by device `charts-overview.ts:55-57`; bubble-pack provenance corrected — it lives in burgs-overview/states-editor, not the chart builder) · **DESK-4** perspective-anchored standings ("the world as X believes it", belief-scoped from `display/warStatus.js`; their omniscient N×N matrix stays refused) · **DESK-5** legibility texture batch (sentence footers · door→overlay affinity · palette commands · named display units · `?focus=` deep-link following the existing `?mapview=` precedent).
- **POLIS-3 extension — travel-time rings**, AMENDED to honesty: `hopWeeks` is settlement-pair only (no per-cell cost field), so rings are **hopWeeks-banded settlement groupings** ("within a week's ride"), never continuous contours; feed sits behind the existing entitlement + `spatialCanonVersion` call-site gates; `TravelersLayer.jsx` is the render precedent.
- **POLIS-1 extension option — culture sub-roster** (their per-entry-odds culture-set pool, `cultures-generator.ts:1091`): plans-only, dormant; MUST ride D5's single declared plan shift or defer to a follow-on plan car — never a second worldPlan perturbation.

## A2.2 · New owner rows (extending §5's batch)

- **Q-W7** — typed data export of visible tables after viewerSecrets redaction (free vs entitled is paid-surface). REC: grant as DM-tier first.
- **Q-W8** — state treasury / typed taxation: ⚠ this would **REVERSE the recorded no-conserved-coin ruling** (`treatyTransfer.js:13-17`, ruling f3cf639e — grain is deliberately the only conserved stock). REC: decline for launch; revisit post-launch with the ruling on the table.
- **Q-W9** — DM battle theater: their apply-or-cancel interactive loop is REFUSED as mechanics (engine physics is not a minigame); the only admissible form is an interactive REPLAY of NAME-3's derived narrative. New capability class. REC: defer.
- **ADD-13** (engagement-kind as engine modifiers) is recorded as a vetoable escalation option only; default is narrative-only.
- **W1-4** (culture/faith mobilization cohesion): CONFIRMED absent in `mobilization.js`; the honest hook is a graded extension of the occupation-suppression rungs (`OCCUPIER_CONTROL_STATES`), per-settlement not per-cell. Not chartered — recorded for the tuning-era docket.

## A2.3 · Guard-list additions (extend §727.3; all tie-back-verified at source)

(a) **Fabricated backstory mints** (`military-generator.ts:555-560` — the painted-history idiom) · (b) annihilation-to-zero + ambient-RNG battle rolls + attacker teleport (`regiment-editor.ts:~417`) · (c) AI-as-prose-writer for notes/flavor · (d) canon mutation from editable overview cells (`military-overview.ts:281-295` rescales regiments from a table cell) · (e) mid-battle interactive steering (`battle-screen.ts:1213-1236`). Battle-verdict wording corrected for the record: their results touch notes, a marker, AND the regiments' own troop counts — never a burg, diplomacy, or a population.

## A2.4 · Product signals (recorded for the owner; no cars)

Their own wiki, nine years in: "The map is static. Over-time simulation is planned, but not yet implemented" — with Simulate-History/Economics as unstarted roadmap cards; their loudest user demands (shape authorship, edits-surviving-regeneration, narrative war history, diplomacy depth) map onto #12/#11/NAME-3/ours-better almost one-to-one; a third-party "Fantasy Map Simulator" holds >85% positive on Steam with users requesting Azgaar imports (a paying audience for this category); their ~8% free+Patreon conversion funds the person, not the product. Failure modes banked: data-in-the-render re-homing, rolled-back blanket format changes, forced auto-behaviors, half-measure visualizations. Their late-stage polish wave (search/pagination/column pickers/dialog persistence) is W-DESK's checklist at our equivalent stage.

## A2.5 · Coverage honesty carried forward

55/57 Patreon post bodies login-walled; Reddit unreachable; two secondhand items marked and load-bearing for nothing. All mechanics claims are source reads; the tie-back pass re-verified every load-bearing citation (three mild MISQUOTES corrected above — none fatal; several counts were understated in our favor).

---

# AMENDMENT A3 — owner grants of 2026-08-29(d) (ODQ §731)

**Q-W8 GRANTED over the chair's recommendation — ruling f3cf639e (no-conserved-coin) is REVERSED by owner order.** **W-COIN** is chartered: a state treasury + taxation typed by government form, feeding `warCosts`/`coalitionExpenditure` first. Discipline: architecture-first (W-COIN-0 design doc + skeptic mini-panel before any implementation); finite semantics (typed stocks and flows, closed vocabularies — grain's `storageMonths` idiom is the in-estate precedent); virtual flag; raw-byte dormancy bar; lived worlds gain the ledger only at future ticks under the lit flag, declared; the reversal's scope is exactly what W-COIN-0 defines and the owner sees at its panel. Sequenced at the engine-wave slot, pre-soak. Q-W2/Q-W9 recommendations stand unchanged; the rest of the §5/A2.2 batch remains open.
