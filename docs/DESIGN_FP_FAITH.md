# DESIGN — FP-FAITH: THE FAITH PROGRAM (Foreign Policy volume 5 of 6)

## Fable 5 architecture, 2026-08-02. BOUND BY DESIGN_FP_SPINE.md — where this volume
## and the spine conflict, the spine wins and the conflict is a bug to report. The
## spine's §3 FP-FAITH crux rulings are SETTLED LAW: this volume elaborates them and
## never re-opens them. Implementation is assigned to the external implementer
## (Sol 5.6); the war volume's §10 implementer protocol binds here verbatim
## (DESIGN_WAR_RULINGS_ARCHITECTURE.md §10), with the faith addenda in §10 below.

**Status: ARCHITECTURE. Nothing here is scheduled until the owner sequences it against
the standing pipeline; the FP programs build behind the sim-proof path in spine §5
order (SP → GRAMMAR → INFO → TRADE → FAITH → POP → INTERIOR → COUPLINGS). Every wave
ships DARK. The judgment blocks in §6 are the drafting chair's rulings under
delegation — vetoable there. The eight crux mechanisms are owner/spine law and are
not.**

**Reading order for the implementer:** DESIGN_FP_SPINE.md (the constitution — the
twelve requirements and the FP-FAITH crux rulings) → this document top to bottom →
DESIGN_WAR_RULINGS_ARCHITECTURE.md §1/§10 (the binding laws + protocol this volume
inherits) → DESIGN_FP_COUPLINGS.md (every cross-layer pair this volume names is
walked there) → docs/RELIGION_REWORK.md + docs/PHASE4_FAITH_DELTA.md (the built
substrate's own design law; §2.7's no-local-genesis rule STANDS) →
docs/GEOPOLITICAL_WAR_LAYER.md Feature D (the premium doctrine, LOCKED).

**A naming note:** the Phase-4 religion rework's historical waves are named
`W-F1`..`W-F7` (hyphenated) in tree comments (e.g. deityStance.js's "owner-gated
W-F4 resurrection"). THIS program's waves are `WF-0`..`WF-9` (unhyphenated). They
are different programs; WF-3 below deliberately schedules what the tree calls the
W-F4 resurrection.

---

## §1 THE LAWS THAT BIND EVERY WAVE

### 1a Constitutional (inherited verbatim from DESIGN_WAR_RULINGS_ARCHITECTURE.md §1a)
Same-seed byte identity · dormancy behind virtual flags absent from
DEFAULT_SIMULATION_RULES, golden-fenced BEFORE wiring · seeded purity (no Date.now,
no Math.random, no transcendentals in engine paths) · monotone ratchets (lazy
leaves, zero new any-casts) · receipts carry enforcement (id + full address chain +
typed action + settlements BY NAME + recorded reason) · premium isolation +
audience projection (includeCovert/includeGroundTruth on every new projection) ·
finite semantics (closed banded vocabularies; no float ever reaches a surface).

### 1b The twelve requirements, instantiated for faith (spine §1 — every WF wave
### carries all twelve or it is a defect)
1. **LAW ONE, faith's hardest reading:** the engine models BELIEVERS — shares,
   legitimacy, piety, readings, congregations — never gods. No theology, no doctrine
   trees, no miracle physics. The engine never confirms a divine act, never
   adjudicates whose god is real, never contradicts a DM's canon. Every mechanism
   below is a believer-side transition wearing the believers' own words.
2. **FORCES AND COUNTERFORCES:** every faith force names its opposition off the SAME
   evidence: unseating pressure ↔ incumbency (hold + tenure + backing); pilgrimage
   pull ↔ road danger; suppression zeal ↔ the martyr's backfire; the tithe ↔ tithe
   resentment; the wrath-reading ↔ the failed prophecy. A faith without counterforce
   is a ratchet, and the estate's killer is the ratchet.
3. **DEPTH-DENSITY:** each wave below ships law, force, counterforce, belief
   posture, casting, receipts, pins (negative hardest), bands, couplings, clock,
   posture consumption, endings entries, and the dossier round-trip. The audit
   stage hunts shallow spots wearing deep clothing.
4. **NAMED-ACTOR RULE:** clergy already exist as planes (npcAgency religious roles
   priest/cleric/bishop/abbot/oracle; clergyTraitPlane's per-minister leans;
   rankings). Casting is read-wiring through those planes — never new NPC state,
   never a killed named soul, never a resolved fate. The exposure paths in WF-5
   obey this absolutely (§1c-4).
5. **THE ERRAND SPINE:** legates and named pilgrims ride SP-1 with typed purpose
   `religious`, DECLARED vs TRUE purpose on the covert/revealed seam, the K.2
   decaying snapshot, interceptability, and law M's week floor. The pilgrim SEASON
   is a flow read, not an errand (J-WF-2).
6. **DOSSIER COHERENCE (the crown):** every WF mechanism sources from settlement
   state and lands back in FaithSection / PantheonPanel / the Chronicle / the
   treaty document with a dossier round-trip pin. A faith system with no town-page
   trace does not exist.
7. **HISTORICAL MESSINESS, DERIVABLE:** the famine that becomes a wrath-reading
   that becomes a purge that drives a faith underground that resurges a decade
   later under a new patron — every link named, every receipt walkable backward.
8. **DEEP COUPLING:** every coupling this volume names (tithe→TRADE, pilgrim
   road→WAR, wrath-reading→POP flight, underground exposure→INFO, faith
   terms→GRAMMAR, mandate→INTERIOR) is declared in DESIGN_FP_COUPLINGS.md with
   reads, receipts, and counterforce. Couplings ride existing reads.
9. **THE TEMPORAL CONTRACT:** faith's existing constants are already per-tick
   week-denominated (STAIN_DECAY 0.06/tick ≈ an 11-week half-life; FLIP_TICKS 3);
   every NEW clock below names its cadence against INTERVAL_WEEKS and registers
   with SP-7's walker. Calendar words obey the 52-week year (the peace-terms
   12-tick trap is cured law — WR-0c item 4 is precedent). Anything that moves
   (legates, pilgrims, relic translations) obeys the one travel physics.
   **THE TWO-TIMESCALE ECHO, faith's naming:** the FAST layer is the reading, the
   flip receipt, the panic conversion, the exposure; the SLOW verdict is tenure,
   lagged piety ("a burned church holds its flock for years" — piety.js:57), the
   underground decade, the extinction. Every wave names which of its outputs is
   which.
10. **THE RISK POSTURE:** every faith decision surface consumes SP-4 —
    `postureOf(settlement)` for the seat's verbs (suppress vs tolerate, purge vs
    procession), `riskToleranceOf(temple)` for the temple actor class (SP-4a names
    per-house/temple appetite). Out-of-posture acts are priced and receipted news,
    never forbidden.
11. **NARRATION PARITY:** the survey measured faith at ~9 phrased tokens from ~7
    emission sites against war's ~27 from ~25+ (settlementRumors.js:127-135 vs
    :84-120) — a deep engine with a thin voice, whose stories die at the mouth,
    not the heart. The 3× program (WF-8) is a crux ruling, with the WR-9 endings
    discipline (§5-WF-9) and the TELLABLE register (§2c) as its acceptance.
12. **THE PACING GOVERNOR IS THE EDITOR:** the 3× expansion registers every new
    kind with the pacing/significance machinery. Tripled voice with no flow
    control is wallpaper; the governor's caps are part of WF-8's acceptance.

### 1c Faith-specific laws (standing law; violations are design defects)
1. **THE AGNOSTIC LAW (Law One sharpened):** no receipt, no term executor, no
   reading, no transition may assert a god ACTED. Believers read, courts judge,
   congregations move; the god never testifies. Even "divine schism" narrates the
   faith COMMUNITY acting (deityStanceLane.js:179-205 already holds this line —
   keep it everywhere).
2. **THE TWO-LANE GATE stands (religiousContest.js:17-33):** the LOCAL lane
   (per-settlement shares, patron seat, legitimacy, piety, mandate) runs on deity
   presence alone; the SPREAD lane (carriers, footholds, stances, sacred_claim)
   runs behind `faithSpreadEnabled` (with `religionDynamicsEnabled` its lockstep
   legacy alias, simulationRules.js:707-771). Every WF flag declares which lane it
   extends and ANDs accordingly (§3).
3. **NO LOCAL GENESIS (RELIGION_REWORK.md §2.7, standing):** spread-only; nothing
   in this program mints a deity. WF-5's schism splits a CONGREGATION and a
   TEMPLE, never a god. The deity doctrine (no premade pool) binds every fixture:
   test worlds author deities through the custom-content path
   (SET_PRIMARY_DEITY/IMPOSE_CULT, mutateEntities.js:832/:877), never by poking
   config.
4. **NEVER-RESOLVE-FATES in the dark places:** exposure of an underground
   congregation, the purge after a wrath-reading, the intercepted pilgrim — no
   path writes a named NPC's death. Consequences land on shares, legitimacy,
   facets, and circulation (flight into the wanderer register; recantation as a
   facet write). The nameless mass may suffer in banded arithmetic; the named
   never resolve.
5. **PREMIUM INHERITANCE (Feature D.0, LOCKED — GEOPOLITICAL_WAR_LAYER.md:335-341):**
   deities are premium custom content end-to-end; the pulse is premium by
   inheritance; a free user's god can never act. EVERYTHING in this program is
   therefore inert on free tier by construction. What each wave adds to the gate
   surface is stated in its dossier line and gathered in §3c. The two documented
   residuals stand unchanged and are NOT this program's to fix: the composer-lane
   UI-only gate (deferred Wave R-4 single-sourcing, settlementDeityHelpers.js:49-57)
   and the accepted D.0 client-only fail-open.
6. **NO ATHEIST ENDSTATE (religionState.js:73-77, standing):** the unaffiliated
   sink caps at SINK_MAX 45 — "a secular city, never a godless one." WF waves may
   hollow a faith; none may abolish faith itself.
7. **EXTEND, NEVER DUPLICATE (J-WR-11's idiom, imported):** worldState.religionStates
   and worldState.pantheon keep their ONE writer each; every WF state addition is
   an in-place conditional extension or a new conditional ledger with one new
   writer — never a parallel copy of an existing ledger.

### 1d Recorded hazards that WILL bite these waves (each has bitten this estate)
- **THE DEITY-FREE VACUOUS PIN (this program's own №1):** the release corpus is
  deity-free (whole-world-soak.mjs:112, empty customContent), so EVERY faith
  predicate's TRUE branch is unreachable in default fixtures and every
  `toHaveLength(0)` against them is vacuous. Every WF pin seeds a deity-bearing
  world through the custom-content path first; every gate of ANDed predicates gets
  a reachability pin on a deity-bearing corpus.
- **wizard-news id token skew:** every `wizard_news.*` id classifies mover-family
  `knowledge` — never declare a wizard-news id on a faith certification row; WF-0's
  bearer count exists precisely so certification never leans on those ids.
- **JSON-alias trap:** clergy in factions[].members[] ARE npcs[] objects in memory;
  every covert-congregation / legate / foothold fixture JSON-round-trips.
- **Writer/reader payload-spelling drift:** every new ledger (omen readings, covert
  fields, fall records) gets a pin booting the REAL writer and reading through the
  REAL reader.
- **Id-less news drop:** the 3× program pushes ~20 new kinds; every one carries
  `id` and registers in WHAT_PHRASES + heraldRouting or the totality walkers red.
- **Hot files at ceiling:** religionState.js and religiousContest.js are mature
  hot modules — new logic lands as lazy leaves (pure evaluators consumed at the
  existing apply chokepoints), never as in-place growth past the size ratchets.
  VERIFY-AT-BUILD each target file's headroom before choosing in-place vs leaf.
- **Unreachable predicate conjunctions:** `!suppressed && covert` -style gates get
  seeded-failure lower-bound proofs, per the standing class.

---

## §2 SUBSTRATE CENSUS (verified against the tree 2026-08-02 by three read-only
## survey agents; every row carries executed file:line evidence from the survey
## journals; re-verify anything you build on — live code outranks this table)

### 2a What is BUILT (the physical layer this program stands on)
| Substrate | Where | State |
|---|---|---|
| Deity minting: custom-content only; refs `deity:<scope>:<slug>`; frozen embedded snapshots; latentPantheon = legacy-save reader only | mutateEntities.js:832-861/:877-914, customRegistry.js:133-141, deitySnapshot.js, latentPantheon.js:6-13 | BUILT — the deity doctrine holds |
| Axes: evil01/chaos01 live; temper DERIVED (W_EVIL 0.7/W_CHAOS 0.3, dead-band 0.15); rankAxis → strength 0.95/0.6/0.35 + authority 18/10/5; `domain` inert flavor | deityAxes.js:31-98, cultImpositionApply.js:52, deityConstants.js:29 | BUILT |
| Per-settlement pantheon: shares 0..100, niches, standings, PATRON_HOLD 0.35, organic flip FLIP_MARGIN 6 × FLIP_TICKS 3, seeded schism contest (CONTEST_LEGIT_W 0.78), suppression, tier mass 1..32 clamp [0.12,4] | religionState.js:40-80/:366-389/:444-496/:94-114 | BUILT |
| Legitimacy: W_RULER 0.42 + W_NEIGHBOUR 0.20 + W_TENURE 0.30 + W_CHRONICLE 0.08, institution +0.12, conduct +0.14, heresy stain 0.45 imposed, decay 0.06/tick | religionLegitimacy.js:74-121, religionState.js:56 | BUILT |
| Conversion vehicle: ONE stressor (religious_conversion_fracture) on religious_authority; carriers allied/trade/patron/vassal + garrison channels, MIN_CARRIER 0.15; occupation pull 0.5 ×1.35 warbound; crisis receptivity | religiousContest.js:381-436/:80-93/:118-127/:140-151 | BUILT |
| Cult imposition: SLOTS_BY_TIER thorp 1 → metropolis 7, niche rule, weakest eviction | cultImpositionApply.js:70-105 | BUILT |
| Revival: suppressed kept ≤3 as latent memory; resurgence re-enters as fresh; sink revival 0.06 outpaces drift 0.02, SINK_MAX 45 | religionState.js:352-358/:254/:67-80 | BUILT |
| Stance: 4-field stanceOf; betrayal = REAL stressor; pact/foothold = news-only; cooldowns 8/6/6, realm caps 2/3; targeted foothold recruits a NAMED minister via clergyTraitPlane | deityStance.js:53-73, deityStanceLane.js:51-67/:187-228/:238-267/:278-309 | BUILT; aggression + treatyDurability UNCONSUMED (owner-gated deferral, deityStance.js:164-173, GOLDEN_SHIFT_LEDGER G1c) — WF-3 schedules the resurrection |
| sacred_claim ↔ common_rite: closed 4-quadrant table (schism_axis 0.75 outranks natural_enemy 0.60; rite mirrored 0.80/0.45), both-patrons guard, patronSecurity scaling | sacredClaim.js:67-84/:39-49/:115-123, warReasons.js:804, warReasonTaxonomy.js:61 | BUILT |
| Amendment C's dissolution: sacred_claim dies on patron-anchor change; legacy anchors read anchor_unavailable | warTermination.js:88/:105/:308-322 | BUILT, flag-dark (war program's flag) — WF-1 supplies the typed cause its receipt will name |
| Realm pantheon: wins/losses/seats/tier with dwell+hysteresis; zero-seat entries persist forever; Twilight/Ascendancy arcs | pantheon.js:299-312/:331-384/:166-171, pulseKernel.js:1662-1675, realmEvents.js:284/:307 | BUILT — no deletion path, no realm-level unseating transition exists (by finding, and by ruling it never will — J-WF-1) |
| Church agency: CHURCH_OBJECTIVE levers missionize/legitimacy; religious factions compete for temple_authority; clergy NPC roles + preferredActions; DM verbs behind fail-closed deityWriteGate | scoringObjective.js:85-96, settlementStrategy.js:177/:197, factionCompetition.js:56/:68, npcAgency.js:42-46, settlementDeityHelpers.js:98-111 | BUILT |
| Piety: lagged stock (PIETY_LAG 0.06, DRIFT_ASYMMETRY 2, W_INSTITUTION 0.35), banded secular→devout — the SP-5 confidence-in-god instance, EXISTS and extends | piety.js:44/:57-70, faithPanelModel.js:34-49 | BUILT |
| Institutions: backing (+0.12), moral viability pressure, tolerance ledger, faith-gated founding/abolition arcs, roster rows with status | moralInstitutionPressure.js:1-88/:292-295/:461, institutionTolerance.js:1-25, institutionRoster.js:1-56 | BUILT |
| Temple mediation: generosity relief warmth +0.4; peace mediation reads the mediator's quadrant toward BOTH parties | generosityEV.js:243-253, peaceTerms.js:1121-1140 | BUILT (narrow, scalar — stays scalar) |
| Divine mandate → publicLegitimacy (theocracy 1.0 / royal 0.45), the coup coupling SOAK-PROVEN | religionState.js:557-630, religion-coup-soak.mjs | BUILT — FP-INTERIOR's coupling substrate |
| Stale rite beliefs: beliefMap faithLabel + observanceLabel — a settlement can hold a wrong belief about a neighbour's rite | beliefAxes.js:10-16 | BUILT — SP-2 BELIEVED DEVOTION's seed |
| Display pipeline: Herald 'faith' + 'divination' sections, ~15 routes, prefix rules; PantheonPanel, WarFaithTab→FaithSection (ACTIVE/TEASER/HIDDEN), map overlay, PDF | heraldRouting.js:64/:116-121/:239-240, FaithSection.jsx:11-24, faithPanelModel.js:12-15 | BUILT — the pipe is wide; the supply is thin |
| Five dedicated religion soaks with PASS receipts (300 seeds × 80 ticks; schism unresolved 0%; coup coupling proven) | scripts/audit/religion-*.mjs, docs/evidence/phase4-wf7-certification/CERTIFICATION.md | BUILT — a separate lane from the release corpus |

### 2b Verified ABSENT (NEW WORK, each homed in a wave; never build on these as if
### they existed)
- **A believer-side typed fall record + extinction beat** — patron falls fire
  conversion receipts but no typed cause survives on state; pruneSuppressed
  DELETES silently; no obituary fires on last-seat loss → WF-1.
- **Any pilgrim/legate mover representation** — pilgrimage.js:8-11 computes a
  bounded host lift in [0, 0.1] and DISCARDS the who-came-from-where it computed;
  no mover, no news kind; the Ascendancy headline claims "the faithful walk the
  roads" and no mechanic puts them there → WF-2.
- **Consumption of stance aggression + treatyDurability; any pact/foothold
  consequence** — pacts and footholds are news-only, "the positive
  relationship-WEIGHT coupling is the deferred half" (deityStanceLane.js:238-267)
  → WF-3.
- **Any interpretation layer** — no event kind attributes a calamity to a deity;
  gods_abandonment (realmEvents.js:26-35) is coincidence recognition (three
  co-present stressors), not a reading; the Auspice is a DM-facing forecast with
  an honest label, not believer interpretation → WF-4.
- **Any covert congregation** — suppressed = share 0, inert dormancy;
  clandestineFacet.js is criminal-only with zero faith references; no path today
  can hold, grow, expose, or narrate a hidden faith → WF-5b.
- **Any temple-splitting path** — the schism is a share/stressor phenomenon; no
  religious_conversion_fracture ever fractures a temple institution into two
  congregations → WF-5a.
- **Any faith term in TERM_CATALOG** (peaceTerms.js:164-188 enumerated: eleven
  families, none religious) — a victor cannot demand a creed, missionary access,
  or pilgrim passage at any table; holy wars start over gods and can never end
  with terms about them → WF-6.
- **Any tithe/temple-economy stream** — the only "tithe" is the granary reserve
  mechanism (foodStockpile.js:12-83); 'tithe_rights' is a competition prize
  token; no treasury, no temple wealth, no gold touches a temple anywhere in
  src/domain → WF-7.
- **A persisted faith-relationship ledger** — stanceOf is a pure recompute; no
  typed persistent relationship taxonomy between faiths (war's edge-label parity
  gap). DELIBERATELY NOT BUILT in this program (§4 what-is-not-modeled): WF-3's
  consequences ride existing relationship/stressor state; a parallel faith-edge
  ledger is the ratchet-shaped duplicate J-WR-11 exists to prevent.
- **A deity-bearer count in v4/v5 receipts** (subsystemRowsBaseline.js:232) — a
  faith silence is indistinguishable from an absent precondition in every
  completed release receipt → WF-0.

### 2c THE TELLABLE REGISTER (the survey's drama census becomes pins; §1b-11's law)
| Survey verdict | Story | Becomes | Pin (named in its wave) |
|---|---|---|---|
| TELLABLE-NOW | A god falls | Enriched: the fall gains its typed cause | WF-1 `pin:fall-names-its-cause` |
| PARTIAL (silent) | A faith goes extinct | The obituary beat | WF-1 `pin:last-altar-goes-dark` |
| IMPOSSIBLE | Pilgrims travel | Season read + SP-1 movers | WF-2 `pin:the-roads-fill-at-the-feast` |
| PARTIAL | Duress conversion, the old faith kept secretly | The covert congregation | WF-5b `pin:kept-alive-in-cellars` |
| PARTIAL | A schism splits a temple | The split institution | WF-5a `pin:the-nave-and-the-crypt` |
| PARTIAL (weakest) | A court reads famine as divine wrath | The reading | WF-4 `pin:two-towns-one-famine` |
| PARTIAL (flag-dark) | The holy war's cause dissolves | Cross-program: war's WR-1 owns the flag; WF-1 supplies the named cause the dissolution receipt prints | WF-1 `pin:dissolution-names-the-fall` |

---

## §3 THE FLAG FAMILY + THE LANE RULING

Eight new virtual flags, all absent from DEFAULT_SIMULATION_RULES, lit only at the
owner-signed soak redo, in build order:

| Flag | Gates | Lane (ANDs with) | Wave |
|---|---|---|---|
| `faithUnseatingEnabled` | typed fall records + fall receipts + the extinction beat | LOCAL (deity presence) | WF-1 |
| `pilgrimageEnabled` | the pilgrim season read + shrine economy + named pilgrims/legates as SP-1 errands | SPREAD (`faithSpreadEnabled`) + SP-1 built | WF-2 |
| `faithStanceConsequencesEnabled` | aggression + treatyDurability consumption; pact relationship-weight; foothold share seed | SPREAD | WF-3 |
| `omenReadsEnabled` | calamity readings as belief writes + the failed-prophecy ledger | LOCAL | WF-4 |
| `faithSchismEnabled` | the temple split (5a) + the covert congregation (5b) | LOCAL, with 5b's exposure arm reading the covert/revealed seam | WF-5 |
| `faithTermsEnabled` | the faith term families in SP-3's catalog + their executors + the communion trigger | SPREAD + SP-3 lit | WF-6 |
| `titheEnabled` | the tithe stream, temple wealth band, relief lever, sack-lure coupling | LOCAL | WF-7 |
| `faithNarrationEnabled` | the 3× receipt program over EXISTING silent transitions (entry/eviction/suppression/resurgence voices) | LOCAL | WF-8 |

**THE LANE RULING (binding):** LOCAL-lane flags key on deity presence via the
existing `isSubsystemActive(snapshot,'religion')` discipline and never require
`faithSpreadEnabled`; SPREAD-lane flags AND with `faithSpreadEnabled` exactly as
the existing spread machinery does (religiousContest.js:17-33's two-lane gate is
the template). A SPREAD-lane WF flag lit while `faithSpreadEnabled` is dark is an
invalid config the WF-9 certification walker reds. `religionDynamicsEnabled`
remains the lockstep legacy alias and gains NO new consumers.

**LIT-PRECONDITIONS (the war volume's flag-dependency ruling, §3, applies in
shape):** WF-2 requires SP-1 (the errand ledger) built and lit; WF-6 requires SP-3
(peacetime formation) lit; WF-3 requires nothing beyond `faithSpreadEnabled`
(stance substrate is BUILT); WF-8 lights LAST among faith flags (its voices narrate
the others' transitions; lit early it narrates silence — harmless but empty).
SP flag names are the SP waves' to assign; this volume binds by SP number and the
implementer resolves names at build (VERIFY-AT-BUILD).

**THE PREMIUM GATE MAP (§1c-5; what remains gated where):** every flag above sits
BEHIND the D.0 inheritance — no deities, no faith, and deities are premium custom
content. Per-surface: new FaithSection lines (fall cause, pilgrim season, reading,
tithe band, underground count) render in the ACTIVE state only; the TEASER and
HIDDEN states gain NOTHING; every new event kind registers in faithEventFilter's
FAITH_EVENT_TYPES so non-premium viewers never see faith events (the existing
gate); WF-5b's covert receipts additionally ride `includeCovert` — DM truth only,
double-gated (premium AND covert projection). The PantheonPanel remains self-hiding
when dormant. Free tier sees exactly what it sees today.

---

## §4 CANONICAL MODEL — new state, and it is deliberately small

Everything below is conditionally materialized (drop-when-empty, zero eager bytes,
absent ⇒ byte-identical) with exactly ONE writer each. religionStates and pantheon
keep their existing writers; extensions are in-place fields folded through the
existing apply chokepoints (VERIFY-AT-BUILD: the exact religionStates apply site —
advanceReligionStates via pulseKernel.js:1106-1141 — and each hot file's size
headroom; new logic prefers pure leaf evaluators consumed there).

```
worldState.religionStates[cid]              — EXISTS; WF-1/WF-5/WF-7 EXTEND IN PLACE
  .patronFalls: [ { ref, cause, atTick } ]  // WF-1: typed fall ring, cause ∈ the
                                            // CLOSED fall vocabulary (§5-WF-1);
                                            // capped ≤3 (the pruneSuppressed idiom)
  .deities[ref].covert                      // WF-5b: { share, sinceTick, shepherdRef? }
                                            // — the hidden congregation, banded,
                                            // capped; shepherdRef is read-wiring to
                                            // an existing clergy npc, never new NPC
                                            // state
  .templeWealth: { band, movedTick }        // WF-7: a BANDED stock, not books
                                            // (J-WF-3); fold computed by tithe.js
                                            // (pure leaf), applied by the one writer

worldState.spatialLedgers.omenReadings      — WF-4, writer omenReading.js (ONE writer)
  [ { id, cid, deityRef, kind,              // kind ∈ {wrath, test, abandonment,
      calamityRef, atTick,                  //   vindication, portent} (closed, J-WF-6)
      expectation: { relief, byTick } | null, // the prophecy's falsifiable half —
      state } ]                             //   open | fulfilled | failed
                                            // capped per settlement (≤2 open), realm
                                            // cap per tick (containment idiom)

legate + named-pilgrim errands              — SP-1's ledger (spatialLedgers.errands),
                                            // purpose `religious`, declared/true
                                            // split; NO faith-owned mover state ever
pilgrim season                              — NEVER STORED: pure evaluator
                                            // pilgrimSeason.js over the draw math
                                            // pilgrimage.js already computes;
                                            // receipts only
faith terms                                 — NO new state: SP-3's treaty artifact
                                            // carries the new families; peaceTerms.js
                                            // stays the single terms writer (the war
                                            // volume's seam ruling extends verbatim)
fall/extinction/split/exposure receipts     — receipts only, never persisted beyond
                                            // the ledgers above
```

**What is deliberately NOT modeled:** no theology or doctrine trees; no realm-level
deity death or pantheon coup (J-WF-1 — zero-seat entries persist as remnants, the
tier ladder is the only realm fall, and the Twilight is the obituary); no second
deity minting (§1c-3); no temple double-entry books (J-WF-3 — the HOUSE gets books
in FP-TRADE; temples get a wealth BAND); no persisted faith-relationship ledger
(§2b — consequences ride existing relationship/stressor state); no per-head pilgrim
simulation (the season is a banded flow; only the NAMED ride SP-1); no separate
covert-congregation ledger (the covert field lives inside the existing entry,
J-WF-4).

---

## §5 THE WAVES (dependency order; each: one commit — WF-2 and WF-5 two slices, one
## commit per slice; focused gates per slice, full gate at wave end, ledger row;
## every wave DARK per §3; every fixture deity-bearing per §1d)

### WF-0 — THE OBSERVATION FLOOR (no flag; instrument-side only)
**Historical archetype:** the parish register — before the story, the count.
**Scope:** cure the certifiability gap BEFORE any mechanism lands, so every later
wave's soak evidence is readable.
- **The bearer count:** a v5 receipt field `deityBearers` (count of settlements
  with a live patron or cult embed at soak end) — the exact instrument
  subsystemRowsBaseline.js:232 records as missing. A faith SILENCE (bearers > 0,
  events = 0) becomes distinguishable from an absent precondition (bearers = 0).
  Never derived from wizard-news ids (§1d's skew hazard).
- **The deity-bearing soak case:** ONE authored release-corpus case whose
  customContent bag carries a small authored pantheon (two deities, opposed
  quadrants — the schism_axis pair, so sacred tension is reachable), assigned
  through the store path. This is the case subsystemRowsBaseline.js:60 names as
  the missing observation; it BUILDS here and RUNS at the owner-held soak redo.
- **Certification rows:** the two existing rows (faithSpreadEnabled :202 + legacy
  mirror :239) gain the bearer-count invariant; each WF flag registers its row
  shell now (grown in WF-9).
**Pins:** the bearer field present and correct on a deity-bearing fixture; ZERO on
the deity-free corpus (both arms — the absence pin is non-vacuous because the
bearing fixture exists); the authored case's deities round-trip the custom-content
path (doctrine pin — no config poking).
**Lifecycle:** receipt fields and a fixture only; no world state.
**Dossier round-trip:** none — instrumentation, not a mechanism (the WR-9
exemption).
**Bands:** none.

### WF-1 — THE UNSEATING (crux ruling 1, SETTLED: believer-side; flag
`faithUnseatingEnabled`; LOCAL lane)
**Historical archetype:** Akhenaten's Aten eclipsing Amun and Amun's return — both
movements entirely priesthood-and-populace, the gods never testifying.
**Law:** a patron's fall is a BELIEVER-SIDE transition — a typed share/legitimacy
collapse with named causes. The engine never confirms the god fell; it records that
the believers left, and why they said they did. Amendment C's dependency is CURED
exactly here: warTermination.js:308-322 already keys sacred_claim dissolution on
patron-anchor change; WF-1 makes that change TYPED so the dissolution receipt can
name the cause instead of only the fact. No realm-level pantheon coup is ever built
(J-WF-1); the realm's fall stays the tier ladder and its Twilight.
**Mechanism:** every patron displacement already flows through selectPatron /
resolvePatronContest / occupation flip / DM imposition. WF-1 classifies each fall
at the moment it fires into a CLOSED cause vocabulary and records it on the new
`patronFalls` ring:
  - `displaced` — organic share flip (FLIP_MARGIN crossed, a rival took the pews)
  - `discredited` — legitimacy-floor contest loss (the schism arm; scandal,
    stain, conduct drift named in the receipt's amplifiers)
  - `abandoned` — the sink crossing (share lost to `none`, nobody took the seat)
  - `imposed` — occupation/warbound/DM flip (the creed followed the garrison)
  - `suppressed` — eviction to suppressed standing (the seat emptied by force)
**THE EXTINCTION BEAT:** two obituaries, both currently silent: (a) SETTLEMENT
extinction — when pruneSuppressed deletes a faith's last latent entry, or a last
suppressed entry with no covert share (post-WF-5) is pruned, the beat fires: "the
last altar of X went dark in <town>; none now keep the rite"; (b) REALM twilight
already exists (pantheon_twilight) — WF-1 adds the LAST-SEAT edge case (seats
1 → 0) as its own kind, distinct from tier demotion. Extinction is an ENDING, not
a deletion: the pantheon entry persists as the remnant it already is.
**Force / counterforce (same evidence — the shares and legitimacy ledger):**
unseating pressure (share erosion, stain, contest) ↔ INCUMBENCY — PATRON_HOLD
0.35, W_TENURE 0.30, institution backing +0.12, all already built; the pin proves
the counterforce WINS on a real fixture (a pressured patron that holds).
**Belief posture (Law One):** causes are the believers' own account — `imposed`
records what the garrison did, never what the god failed to do.
**Casting:** the fall receipt names the ranking clergy of the falling and rising
creeds through clergyTraitPlane + rankings (read-wiring); the discredited fall
names the scandal's named amplifiers exactly as conversionOutcome already names
amplifiers (religiousContest.js:392-436's legibility discipline).
**Receipts (house voice):** "The Lady of Harvests fell discredited in Greyfen — a
decade of stain, an empty nave, and the Deep Forge's bell now rings the hour." Full
address chain, typed action, cause named.
**Clock:** falls are FAST (the flip tick); the ring and the obituary are SLOW
(tenure and prune cadence). Denominated per-tick against INTERVAL_WEEKS; SP-7
registered.
**Posture:** the seat's RESPONSE to a rising challenger consumes postureOf —
a bold seat backs suppression (the existing eviction machinery), a cautious one
tolerates; out-of-posture suppression is priced in legitimacy and receipted.
**Couplings (declared in DESIGN_FP_COUPLINGS.md):** FAITH×WAR — the dissolution
receipt prints the fall cause (this wave's export); FAITH×INTERIOR — a fall of the
THEOCRACY's patron reads into the mandate/coup lane already soak-proven.
**Endings entries:** `unseated` is a transition; the endings it feeds are
{converted, hollowed, imposed_held, extinct} (§5-WF-9's vocabulary).
**Pins (negative hardest):** `pin:fall-names-its-cause` — every fall carries
exactly one cause from the closed set, walker-asserted over all five fire sites;
the NEGATIVE — a pressured patron above the hold does NOT fall and no record
mints; `pin:last-altar-goes-dark` — the obituary fires on the true last entry only
(seeded fixture walks a faith down to one entry, then zero; the beat fires once,
never on intermediate prunes); the legacy arm — an anchor_unavailable war record
gets NO invented cause (missing history is not evidence); dormancy golden.
**Lifecycle paths:** `patronFalls` persists inside religionStates — JSON-round-trip
pinned; regen that rebuilds religionStates re-derives NOTHING (falls are history —
regen preserves them with the entry; a full regen that drops religionStates drops
the ring with it, by design); undo restores with the entry; import validates cause
tokens against the closed set.
**Dossier round-trip (the crown pin):** open the town → FaithSection ACTIVE state →
the cause-chain line renders "the patron fell: discredited (scandal of the
granary tithe)" from patronFalls[0]; the Chronicle carries the obituary row. The
pin renders both from a fixture world and asserts the sentences.
**Bands:** fall-classification thresholds ride the EXISTING tuning constants (no
new dials for the causes — they are readings of built transitions); obituary
significance band; ring depth (default 3).

### WF-2 — PILGRIMS + LEGATES (crux ruling 2, SETTLED: SP-1 movers; flag
`pilgrimageEnabled`; SPREAD lane; two slices)
**Historical archetype:** the Camino de Santiago — a road economy with saints at
one end and innkeepers the whole way; the legate a latere for the named traveler.

**WF-2a — THE SEASON (the flow half).** New pure evaluator `pilgrimSeason.js` over
the draw math pilgrimage.js ALREADY computes and discards (pilgrimageDraw's
distance-weighted who-would-come-from-where, :81-104).
- **The season read:** per host shrine (a settlement whose patron faith carries an
  observance-worthy site — the existing traditions substrate; VERIFY-AT-BUILD the
  observance hook pilgrimage.js lifts), a BANDED seasonal flow {trickle, steady,
  feast, flood} composed from the draw sources, believed devotion of the
  destination (SP-2's BELIEVED DEVOTION family — the pull is what pilgrims
  BELIEVE of the shrine, the gold-rush substrate pointed at faith), and route
  viability.
- **The economy:** the feast weeks lift the host's market/inn activity as a banded
  stream through FP-TRADE's stream physics (declared in DESIGN_FP_COUPLINGS.md;
  the executor rides TRADE's machinery, built there — this wave lands the READ and
  the receipt). The existing host observance lift (the [0, 0.1] score) is
  SUBSUMED, not duplicated: lit, the season read replaces the scalar's source;
  dark, the scalar stands byte-identical.
- **Force / counterforce (same evidence — the route ledger):** pilgrimage pull ↔
  ROAD DANGER — war fronts, route decay, and danger reads on the same legs
  suppress the season band ("the roads were watched, and the feast was thin").
  The counterforce pin proves a war on the pilgrim road empties the season on a
  real fixture.
**WF-2b — THE NAMED (the errand half).** Named movers ride SP-1, purpose
`religious`, law M's week floor, the K.2 snapshot, full interceptability:
- **Legates:** a temple dispatches its named ranking clergy (casting via rankings
  + clergyTraitPlane) on typed faith business — the foothold approach, the
  communion summons (WF-6's trigger), schism mediation, the tithe dispute. A
  legate carries DECLARED purpose; the TRUE purpose may differ (the Q amendment
  generalized — the legate who is really the seat's spy is INFO's story on
  faith's roads).
- **Pilgrims of note:** the ailing lord seeking the shrine, the oracle summoned to
  court — goal/flaw-plane casting; a named pilgrim is an errand and therefore an
  interception surface (the pilgrim road as war target — the couplings volume's
  named pair).
- **Interception + never-resolve:** an intercepted named pilgrim/legate rides the
  war volume's hold machinery (WR-7b's foreign-guest hold, built THERE, consumed
  here); no faith path writes a death (§1c-4).
**Belief posture:** the season rides BELIEVED devotion and BELIEVED road safety —
arrivals' letters correct the belief (the SP-2 correction arm; the bust
counterforce is POP's law, shared).
**Receipts:** "Pilgrims on the road to Greyfen — the feast of the Lady fills every
inn, and the tolls ring like bells." / "A legate of the Deep Forge arrived at
court, and did not say all of why."
**Clock:** the season is SLOW (annual cadence — feast weeks named against the
52-week year, INTERVAL_WEEKS-derived, never a 12-tick "year"); the errand is FAST
(legs, week floor). Travel physics: SP-1's kernel, law M absolute.
**Posture:** dispatching a legate onto dangerous roads consumes
riskToleranceOf(temple); a cautious temple waits out the war and THAT is receipted
("no legate came; the Forge keeps its counsel").
**Couplings:** FAITH×TRADE (feast commerce), FAITH×WAR (the road as target; the
intercepted legate), FAITH×INFO (declared/true purpose), all walked in
DESIGN_FP_COUPLINGS.md.
**Endings entries:** feeds `communion` (the legate's summons) and the season's own
arc endings {feast_kept, roads_closed}.
**Pins (negative hardest):** `pin:the-roads-fill-at-the-feast` — a deity-bearing
world with a qualifying shrine produces a season band > trickle and the receipt
(the survey's IMPOSSIBLE story made TELLABLE); the NEGATIVE — an ordinary temple
with no observance-worthy site mints NO season (reachability discipline both
arms); the counterforce arm — same shrine, war on the road, season collapses,
receipt names the road; the K3-shape pin — pilgrimSeason.js's import list excludes
true-state modules outside its declared reads (belief-sourced pull); the errand
pins ride SP-1's own battery (JSON-alias, writer/reader, DM-KILL→lost); dormancy
golden — dark, pilgrimage.js's scalar byte-identical.
**Lifecycle paths:** the season is derived (never stored); errands persist in
SP-1's ledger under SP-1's own lifecycle pins; the subsumption seam (scalar ↔
season) is a read-side switch with both arms golden-pinned.
**Dossier round-trip:** open the town → FaithSection ACTIVE → the season line
("pilgrim season: feast — the Lady's high holy days"); the Herald route lands in
the faith section; the host's market lift is visible on the economy surface via
TRADE's coupling. Pin renders the FaithSection line from a fixture.
**Bands:** season thresholds (trickle/steady/feast/flood), draw composition
weights, danger suppression weight, feast-week cadence, legate dispatch bar.

### WF-3 — THE STANCE LANES' DEFERRED HALVES LAND (crux ruling 3, SETTLED:
scheduled here, VETO OPEN at the owner; flag `faithStanceConsequencesEnabled`;
SPREAD lane)
**Historical archetype:** the Investiture Controversy — the foothold in another's
court stops being a letter and starts being a lever.
**Law:** this wave executes the owner-gated deferral the tree itself records
(deityStance.js:164-173 "wiring the other two is an OWNER-GATED new-capability /
deferred-wave resurrection", GOLDEN_SHIFT_LEDGER G1c; deityStanceLane.js:238-267
"the positive relationship-WEIGHT coupling is the deferred half"). The spine
schedules it; the owner's veto window is THIS document's review. Everything lands
behind the NEW flag, so no golden moves at build time — the G1c shift becomes a
LIGHTING-day disclosure, not a build-day one.
**Mechanism, three consumptions:**
- **Aggression consumed:** stanceOf's aggression field joins sacred-tension
  scoring as a THRESHOLD modifier on the existing sacred_claim/foothold/betrayal
  lanes (E3's law imported: a stance colours bars, never selects targets — the
  quadrant table stays the selector, the guard stays absolute and never
  piety-amplified).
- **treatyDurability consumed:** faith pacts gain a durability read — a
  lawful-faith pact frays slower, a chaotic one faster — consumed by the pact's
  fraying clock (the existing compliance/fraying machinery via SP-3, never a
  fork).
- **Pact + foothold gain consequences:** `faith_pact` writes its deferred
  relationship-weight nudge (banded, capped, reversible — the trust +0.05
  missionary precedent is the scale class); a recruited foothold minister seeds a
  banded covert-lean share for the recruiting creed (consumed by WF-5b's covert
  substrate where lit; a share seed, never an instant conversion).
**Force / counterforce (same axes):** aggression ↔ cooperation are two fields of
ONE stance read off the same two signed axes; realm caps (2 betrayals / 3 pacts
per tick) and cooldowns 8/6/6 stand untouched as the containment counterforce.
**Belief posture:** stances are read on the deities' AUTHORED axes — the one place
faith reads authored truth (the DM's own content) rather than belief; the
CONSEQUENCES land on believer state only.
**Casting:** the foothold minister is already named (deityStanceLane.js:278-309);
the pact receipt names the officiating clergy both sides (read-wiring).
**Receipts:** "The faith of the Deep Forge and the Lady of Harvests kept
communion at the river shrine — the old quarrel cools." Betrayal receipts stand as
built.
**Clock:** consumption is FAST (per-tick threshold reads); durability is SLOW (the
fraying clock, INTERVAL_WEEKS-denominated via SP-3).
**Posture:** the pact/betrayal lanes consume riskToleranceOf(temple) as a
threshold modifier — the bold temple courts the dangerous pact; out-of-posture
pacts are receipted as such.
**Couplings:** FAITH×GRAMMAR (durability → SP-3 fraying), FAITH×WAR (the betrayal
stressor exists), declared in the couplings volume.
**Endings entries:** feeds `communion` and the pact's own {kept, betrayed}.
**Pins (negative hardest):** THE REVERSAL PIN (no courage ratchet — an
aggression-raised threshold falls back when the axes' composition changes; both
directions on one fixture); the COLOUR-NEVER-DROWN pin (the weight nudge is
capped; a pact cannot overturn a hostile relationship's sign — the relationship IS
the bar, E3); the NEGATIVE — dark flag, stanceOf's two consumed fields and every
receipt byte-identical to today (the G1c fence, captured FIRST); the foothold seed
requires the recruited minister still present (a departed minister seeds nothing —
lifecycle pin); guard-absoluteness regression pin (no guard becomes
piety-amplified).
**Lifecycle paths:** no new ledger — the covert-lean seed rides WF-5b's covert
field where lit and is receipt-only where not (declared degraded-dark read, the
WR-1 sunk-cost precedent); pact weight rides existing relationship state and its
own lifecycle pins.
**Dossier round-trip:** open the town → FaithSection ACTIVE → the stance line
gains its consequence clause ("communion kept; the quarrel cools"); the
relationship surface shows the bounded nudge. Pin renders both.
**Bands:** aggression threshold weight cap, durability fraying multipliers, pact
weight nudge + cap, foothold seed size + cap.

### WF-4 — OMEN READS (crux ruling 4, SETTLED: belief writes; flag
`omenReadsEnabled`; LOCAL lane)
**Historical archetype:** Rome's prodigy lists — the Senate never asked whether
the sky truly rained blood; it asked which rite answers it — and the flagellants
of 1348 for the reading that becomes a movement.
**Law:** calamities are interpreted through the LOCAL faith lens as BELIEF WRITES.
A reading attributes; it never confirms (§1c-1). The engine's own causality is
untouched — famine physics stay famine physics; the reading is what the pulpit
SAYS, written into believer state through existing laws.
**Mechanism:** new module `omenReading.js` (ONE writer for
spatialLedgers.omenReadings). When a qualifying calamity lands on a deity-bearing
settlement (famine, plague, disaster, siege — the existing stressor/disaster
event classes; a CLOSED qualifying list), the local lens composes a READING:
- **The lens:** patron temper × alignment quadrant × piety band × the reader's
  character (the ranking cleric's traits — a zealous reader leans `wrath`, a
  gentle one `test`; read-wiring, casting law). Kind ∈ {wrath, test, abandonment,
  vindication, portent} (closed, J-WF-6).
- **The write:** a reading moves EXISTING believer state through EXISTING laws
  only — piety (the crisis-revival arm religionState.js:79 already runs), the
  sink (crisis calls the faithful home OR `abandonment` accelerates the drift),
  legitimacy conduct-fit, and the mandate where the seat is implicated. NO
  physics: omenReading.js's import pin excludes every physical-state writer (the
  K3 structural idiom pointed inward).
- **The falsifiable half:** a `wrath`/`test` reading may carry an EXPECTATION
  (relief by a named tick — "give the granary rite, and the rains return"). The
  expectation is the counterforce's hook: FULFILLED (the calamity lifts in the
  window) pays clergy credibility + piety; FAILED pays the reader's credibility
  DOWN through SP-5's source-credibility family and accelerates the sink — the
  failed prophecy is the skeptic's evidence, off the same record.
- **Two towns, one famine:** the lens is local — the same regional calamity reads
  `wrath` under one patron and `test` (or `vindication` against the neighbour)
  under the schism-axis rival, feeding sacred tension through the existing
  quadrant read. This is the survey's weakest PARTIAL made tellable.
**Force / counterforce (same record):** the reading's pull (crisis revival,
mandate pressure) ↔ the failed prophecy's cost (credibility, sink acceleration) —
one ledger row carries both arms.
**Belief posture:** readings are pure believer speech; SP-2's BELIEVED CONDITIONS
may carry the reading OUTWARD ("they say the gods have abandoned Greyfen") at news
speed, feeding POP's flight belief — coupling, declared.
**Casting:** the reader is the settlement's ranking cleric via existing planes;
the receipt names them.
**Receipts:** "The priests of Greyfen read the failed harvest as the Lady's wrath —
the granary rite was let lapse, they say, and the pews fill with the frightened."
Routed to the Herald's EXISTING `divination` section (heraldRouting.js:64 — built,
underfed; VERIFY-AT-BUILD its routing rules take the new kinds).
**Clock:** the reading is FAST (fires on the calamity tick, cooldown-capped); the
expectation window and credibility echo are SLOW (banded weeks,
INTERVAL_WEEKS-named). Two-timescale echo named.
**Posture:** the SEAT's response to a reading consumes postureOf — the bold seat
answers `wrath` with a purge or a great rite (receipted realm acts through
existing lanes); the cautious seat waits; out-of-posture response priced.
**Couplings:** FAITH×POP (a wrath reading accelerates believed-conditions flight),
FAITH×INTERIOR (a reading against the seat feeds legitimacy pressure),
FAITH×INFO (a PLANTED calamity rumor can bait a reading — THE LURE's sacred arm,
built in FP-INFORMATION, consumed here; the false omen is INFO's story). All in
the couplings volume.
**Endings entries:** feeds {resurgent, hollowed} (readings move both directions).
**Pins (negative hardest):** `pin:two-towns-one-famine` — one regional calamity,
two patrons on the schism axis, two DIFFERENT reading kinds, both receipted (the
TELLABLE pin); the NEGATIVE — the same calamity on a secular (deity-free) town
mints NOTHING (non-vacuous: the bearing arm exists in the same fixture); the
NO-PHYSICS pin — a world advanced with readings suppressed vs fired differs ONLY
in believer state and receipts (byte-diff discipline over the physical ledgers);
the failed-prophecy arm WINS on a fixture (credibility falls, sink accelerates,
receipt names the lapsed expectation); cooldown/caps hold (no reading storm);
dormancy golden.
**Lifecycle paths:** omenReadings persists — JSON-round-trip; open expectations
survive regen/undo with the ledger; import validates kind tokens; a settlement
razed/abandoned mid-expectation closes the reading `failed` through the one
writer (no orphan expectations).
**Dossier round-trip:** open the town → FaithSection ACTIVE → the reading line
("the pulpit's reading: wrath — relief promised by the spring rite") sourced from
the open ledger row; the Herald divination section carries the beat. Pin renders
the line.
**Bands:** qualifying-calamity list (closed), lens weights, expectation window
band, credibility payoff/penalty bands, per-settlement + realm caps, reading
cooldown.

### WF-5 — SCHISM COMPLETES + THE UNDERGROUND (crux ruling 5, SETTLED; flag
`faithSchismEnabled`; LOCAL lane with a covert-seam exposure arm; two slices)
**Historical archetype:** the Great Western Schism for the split temple — two
seats, one god, and every court forced to choose; the Kakure Kirishitan for the
underground — a faith kept alive in cellars for two centuries and still there
when the doors opened.

**WF-5a — THE TEMPLE SPLITS.** The schism stops being only a share phenomenon.
- **The split condition:** a resolved patron contest (resolvePatronContest's
  schism arm — literally commented "A SCHISM", religionState.js:450) at a
  settlement holding a live temple-class institution, where the LOSER retains a
  banded remnant share above a floor, splits the congregation: the institution
  roster gains a second congregation record for the losing creed (institution
  machinery — the existing founding lane at reduced ceremony; the losing
  congregation's institution starts impaired where tolerance is low, per the
  existing tolerance ledger).
- **The negative that pins hardest:** no temple institution ⇒ NO split (the
  schism remains a share event, exactly today's behavior); loser below the
  remnant floor ⇒ suppression as today. The split is the RARE middle outcome —
  its rarity is a band the WF-9 envelope polices (the N-gate discipline
  imported).
- **Force / counterforce:** the split's stability (two congregations coexist
  under tolerance) ↔ the eviction pressure (slots-by-tier, niche rule — the
  existing capacity physics decide whether the town can HOLD two; a thorp cannot,
  cultImpositionApply's SLOTS_BY_TIER is the arithmetic).
**WF-5b — THE UNDERGROUND.** Suppression stops being inert dormancy.
- **The covert congregation:** on suppression (eviction, imposed flip, purge),
  a banded fraction of the displaced share goes COVERT instead of to zero — the
  `covert` field on the suppressed entry (§4): hidden practitioners, a named
  shepherd (read-wired clergy ref), covert rites as DM-only receipts
  (includeCovert — the double gate, §3c).
- **The covert lifecycle:** covert share GROWS slowly under suppression zeal's
  counterpressure (the martyr's arithmetic below), DECAYS toward assimilation
  under tolerance (a tolerated faith has no need of cellars — the counterforce
  off the same tolerance ledger), and RESURFACES as the existing resurgence path
  (attemptEntry's fresh-entry arm, religionState.js:254) when suppression lifts
  or covert share crosses the surfacing band — and the resurgence receipt now
  says what it truly was: "the old faith kept alive in cellars."
- **Exposure:** the covert→revealed seam (the corruption web's machinery, faith's
  sibling at last — clandestineFacet stays criminal-only; the faith arm is its
  OWN read through the same seam discipline, VERIFY-AT-BUILD the seam's exact
  reusable surface). An exposure event: informant-driven (INFO coupling), zeal-
  driven (the seat's purge posture), or accidental (banded). Consequences:
  covert share collapses OR the backfire — exposure under high piety/low seat
  legitimacy converts sympathy into share ("the pews filled in defiance" — the
  Tertullian arm: the blood of the martyrs is seed; nameless-mass arithmetic
  only, §1c-4: the named shepherd flees into circulation or recants as a facet
  write, fate never resolved).
- **Force / counterforce (same record):** suppression zeal ↔ martyr backfire —
  both read the covert record + seat legitimacy; the harder the purge under a
  weak seat, the stronger the backfire. No ratchet: zeal is posture-priced.
**Belief posture:** the covert share is DM-truth; the PUBLIC believes the faith
dead — beliefMap's faithLabel holds the stale public picture (built substrate,
beliefAxes.js:10-16), and the surfacing corrects it at news speed. Neighbours may
hold BELIEVED DEVOTION reads that are simply wrong — the engine's fog applies to
faith like everything else.
**Casting:** the shepherd via clergyTraitPlane lean (the minister whose authored
traits lean toward the suppressed creed — the foothold read pointed inward);
WF-3's foothold seed feeds this covert share where both are lit.
**Receipts:** split — "The temple of Greyfen split: the old rite keeps the nave,
the new keeps the crypt." Underground (DM-only until revealed) — "By candlelight
in the undercroft, the Lady's rite is kept by those who will not leave her."
Exposure — "The hidden congregation was dragged into the light; the pews filled
in defiance."
**Clock:** suppression/exposure are FAST; covert growth/assimilation are SLOW
(generational bands, INTERVAL_WEEKS-named); the surfacing is the two-timescale
echo's textbook case — a decade of silence, then a week of bells.
**Posture:** purge intensity consumes postureOf(seat); covert recruitment consumes
riskToleranceOf shaped for the congregation (bold undergrounds grow faster and
expose likelier — priced both ways, the reversal pinned).
**Couplings:** FAITH×INFO (informant exposure; the LURE's planted-heresy arm),
FAITH×WAR (occupation → imposed flip → underground: the duress story completes),
FAITH×POP (flight of the faithful as believed-conditions push). Declared in the
couplings volume.
**Endings entries:** {gone_underground, resurgent, extinct, split} — four of the
program's eight endings live here.
**Pins (negative hardest):** `pin:the-nave-and-the-crypt` — a contest at a
temple-bearing fixture splits, receipt renders, roster carries two congregations
(TELLABLE); the NEGATIVE — same contest, no temple institution ⇒ byte-identical
to today's share outcome; `pin:kept-alive-in-cellars` — imposed flip under
occupation seeds covert share, a later suppression-lift surfaces it and the
resurgence receipt names the cellar years (TELLABLE, the duress story end to
end); the PREMIUM-ISOLATION pin, hardest — covert receipts NEVER appear in any
non-includeCovert projection (free, shared, lapsed, premium-player views all
audited; the id-less-drop walker covers the new kinds); exposure with covert
share = 0 is unreachable (seeded lower-bound proof, §1d); the backfire arm WINS
on a weak-seat fixture; assimilation empties the cellar under long tolerance (the
counterforce fixture); never-resolve — the exposure path's walker proves no
npc-death write exists in the faith arm; dormancy golden.
**Lifecycle paths:** covert fields ride religionStates' own persistence —
JSON-round-trip both shapes (with/without covert); pruneSuppressed LEARNS THE
CURE: an entry with covert share > 0 is NEVER pruned (the extinction beat
requires the cellar empty first — WF-1's pin already sequences this); regen
preserves covert state with the entry; undo round-trips; import clamps covert
share to band; the shepherd ref heals like every npc ref (departed shepherd ⇒
ref cleared through the one writer, congregation leaderless — a receipt, not a
crash).
**Dossier round-trip:** open the town → FaithSection ACTIVE renders the split
("two congregations keep the Lady and the Forge") from the roster; the DM's
includeCovert view adds the underground line ("and in the cellars, the old rite
persists — 12 souls, the smith's widow shepherds them"); the institution roster
shows both congregation rows. Pin renders all three surfaces from one fixture.
**Bands:** remnant floor for the split, covert seed fraction, covert growth/
assimilation half-lives, surfacing band, exposure hazard + backfire threshold,
purge pricing, slots interaction (tier capacity honored).

### WF-6 — FAITH TERMS JOIN THE PACT GRAMMAR (crux ruling 6, SETTLED: via SP-3;
flag `faithTermsEnabled`; SPREAD lane; requires SP-3 lit)
**Historical archetype:** the Peace of Augsburg — cuius regio, eius religio: the
first time the table could say what the pulpit does — and the 1229 Treaty of
Jaffa for pilgrim passage bought with ink instead of blood.
**Law:** the survey's sharpest asymmetry — "holy wars can start over gods but
never end with terms about them" (TERM_CATALOG carries eleven families, zero
religious) — dies here. Faith becomes DEMANDABLE AND GRANTABLE at the table.
peaceTerms.js stays the single terms writer; SP-3's peacetime formation carries
the families; the war volume's seam ruling extends verbatim.
**The term families (names vetoable, J-WF-7; executors are the substance):**
- `missionary_access` — the grantor's settlements admit the beneficiary creed's
  carriers: executor unblocks/boosts the religious_authority channel toward the
  grantor (the existing spread physics — the term moves a GATE, never a share
  directly) and legalizes footholds. Breach: the channel blocked again, the
  compliance stack notices (the existing fraying/default machinery).
- `pilgrimage_right` — safe passage for the beneficiary's pilgrims/legates on
  named routes: executor feeds WF-2's season (danger suppression lifted on the
  named legs) and SP-1 errand safety; breach = an interception/harassment event
  on a protected leg, receipted as treaty breach.
- `shared_rite_compact` — the communion instrument: a standing joint observance;
  executor damps sacred tension (the common_rite mirror's evidence made
  contractual) and feeds WF-3's pact durability read. The COMMUNION TRIGGER: a
  faith communion (same patron or brothers-quadrant proximity, sacredClaim's
  faithProximityOf) is a typed SP-3 PROPOSAL trigger — the spine's peacetime
  formation consumes it.
- `tolerance_guarantee` — the grantor forswears suppression of a named creed:
  executor gates the eviction/purge lanes for that creed (suppression becomes
  breach); the underground surfaces safely where this holds (WF-5 coupling —
  "the doors opened").
- `temple_restitution` — post-war repair: a named impaired/shell temple-class
  institution is restored on the payer's stream (institution status machinery +
  transfer physics; the war-damage mirror).
**The guard (sacredClaim's own idiom):** no faith term forms unless BOTH parties'
relevant settlements name a patron — absence of a faith is not a shared faith
(sacredClaim.js:39-49's guard, imported verbatim as the family's formation gate).
**Force / counterforce:** the term's demand ↔ the host's backlash — imposed
missionary access under low tolerance feeds resentment through the existing
tolerance/legitimacy reads (an Augsburg that pleases no one), and the breach
lanes are priced exits, never silent ones. Dictated-vs-negotiated both arms:
victors may DEMAND (war-end terms), equals may TRADE (SP-3 peacetime formation).
**Belief posture:** formation triggers and appraisals read each party's OWN
picture through SP-3's two-picture contract (the war volume's negotiationPictures
seam — built there, consumed here; no faith-side evaluator forks it).
**Casting:** the communion's officiants and the legate who carries the proposal
(WF-2b) are named through existing planes.
**Receipts:** "Peace signed at Greyfen: the Forge's missionaries may preach
within the walls, and the Lady's pilgrims walk the east road unmolested." Breach:
"The purge broke the guarantee; the treaty is defaulted and the realm knows why."
**Clock:** formation is SLOW (SP-3's proposal cadence); executors are per-tick
gates; durations denominate in the 52-week year with the treaty's own
`treatyTicksPerYear` marker (the WR-0c item-4 law, inherited).
**Posture:** proposing/accepting faith terms consumes postureOf + the temple
actor's risk read; a proud seat refusing tolerance_guarantee under pressure is a
receipted, priced act.
**Couplings:** FAITH×GRAMMAR is this wave (SP-3 core); FAITH×WAR (war-end faith
terms; the sacred_claim war that ENDS with missionary access — the full arc);
declared in the couplings volume.
**Endings entries:** `communion` (the compact that holds); the treaty's own
{kept, defaulted} ride the compliance stack.
**Pins (negative hardest):** the GUARD pin — no faith term forms where either
side lacks a patron (negative, hardest — the formation gate on a mixed fixture);
executor honesty — missionary_access moves the CHANNEL and never writes share
directly (the no-instant-conversion pin; conversion still flows through the one
stressor vehicle); breach reachability — a purge under tolerance_guarantee
defaults the treaty with the receipt (compliance coupling proven live); the
dictated arm (war-end demand) and the negotiated arm (SP-3 proposal) BOTH
reachable on fixtures; Law One — no executor confirms a god (walker over
executor writes: believer/gate state only); dormancy golden (dark ⇒ TERM_CATALOG
byte-identical — the catalog extension itself is flag-shaped; VERIFY-AT-BUILD
the catalog's dormancy idiom against W1's precedent).
**Lifecycle paths:** terms persist on the treaty artifact under its own
serialization/migration discipline (v2 + marker, per WR-0c item 4); nothing
faith-side stores separately; regen/undo/import ride the treaty's own pins.
**Dossier round-trip:** open the town → the treaty document renders the faith
terms in the house voice; FaithSection ACTIVE notes live access/guarantee
("missionaries of the Forge preach here by treaty"). Pin renders both.
**Bands:** per-family appraisal weights (need-weighted through the §15.1 lens
both directions), channel boost size, danger-lift size, tension damp size,
restitution rate, formation trigger thresholds.

### WF-7 — THE TITHE COUPLING (crux ruling 7, SETTLED; flag `titheEnabled`;
LOCAL lane)
**Historical archetype:** the medieval tithe — one part in ten, and cathedrals
rose from it; Lindisfarne 793 for what wealth on an altar summons.
**Law:** faith gains its economy stream — banded, believer-side, Law One intact:
the tithe is what BELIEVERS RENDER, a function of piety and rank, never a price
engine. Temples get a WEALTH BAND, not books (J-WF-3): the HOUSE keeps books in
FP-TRADE; the temple's coffer is a banded stock with one writer.
**Mechanism:** new pure leaf `tithe.js` computing the per-settlement tithe fold,
applied through religionStates' one writer (§4):
- **The stream:** piety band × temple-class institution presence × patron rank
  (DEITY_RANK_AUTHORITY's ladder as weight) → a banded draw on the settlement's
  streams through TRADE's stream physics (the coupling executor lands in
  FP-TRADE's machinery; this wave lands the read, the wealth fold, and the
  receipts). Devout metropolis with a major patron: a real stream. Lukewarm
  thorp: a trickle.
- **Temple wealth consumed, three ways:** institution durability (a rich temple
  weathers impairment — feeds the existing status machinery's repair side);
  charity (the generosity templeMediated arm, +0.4 warmth EXISTS — wealth bands
  scale its reach); and SPLENDOR — a wealth-band term in legitimacy's
  institution backing (the cathedral effect, capped: gold gilds, never buys,
  legitimacy — the weight rides W_INSTITUTION's existing 0.12 slot, banded
  within it, never a new additive).
- **Force / counterforce (same evidence — the piety/tithe record):** the stream ↔
  TITHE RESENTMENT: heavy tithe pressure under low piety accelerates the
  unaffiliated sink (drift term scaled by the pressure band) and feeds the
  commons voice (FP-POP's petition ladder — the tax dispute that becomes a
  reformation, the couplings volume's own named archetype). TITHE RELIEF is the
  seat/temple's lever: forgone stream buys piety + legitimacy (the famine-year
  remission — receipted mercy).
- **The sack lure (FAITH×WAR):** temple wealth joins sack arithmetic as loot
  weight — a rich shrine raises the raid's take and the moral price together
  (the world judges by the observer's axis; robbing a temple is monumental on
  the good observer's ledger). Conservation pinned: sacked wealth moves, never
  duplicates.
**Belief posture:** neighbours know a shrine's wealth only as BELIEVED wealth
(SP-2 believed-scarcity's sibling read at news speed) — the raid that finds the
coffers already carried to safety is a tellable outcome of the fog.
**Casting:** the tithe dispute names the cleric and the seat's steward (existing
planes); the remission receipt names who forgave.
**Receipts:** "The tithe weighs heavier than the harvest in Greyfen; the coffers
fill as the pews thin." / "In the famine year the Temple forgave the tithe, and
the town remembered."
**Clock:** the draw folds QUARTERLY (13 weeks — INTERVAL_WEEKS-derived, named);
wealth decay/spend is SLOW; resentment accrual is the slow verdict of a fast
pressure change. SP-7 registered.
**Posture:** setting tithe pressure consumes postureOf(seat) + the temple actor's
appetite; out-of-posture squeezing (a bold draw from a lukewarm town) is priced
in resentment and receipted.
**Couplings:** FAITH×TRADE (the stream executor; believed wealth), FAITH×WAR
(sack lure + conservation), FAITH×POP (resentment → commons voice), FAITH×INTERIOR
(remission as a legitimacy lever). Declared in the couplings volume.
**Endings entries:** feeds `hollowed` (the resentment road) and the temple's own
{endowed, plundered}.
**Pins (negative hardest):** the NEGATIVE — no tithe from a secular or
temple-less town, and no wealth band materializes (drop-when-empty, non-vacuous
via the bearing arm); the RESENTMENT ARM WINS — heavy pressure + low piety on a
fixture measurably accelerates the sink and mints the receipt; conservation
through sack (wealth moved = wealth lost, the R-arithmetic discipline); the
SPLENDOR CAP (wealth cannot push legitimacy past the institution slot's ceiling —
gold gilds, never buys); remission's payoff is real and bounded (both arms of
the lever priced); dormancy golden.
**Lifecycle paths:** templeWealth rides religionStates persistence —
JSON-round-trip both shapes; regen preserves; undo restores; import clamps to
band; a temple institution's abolition strands no wealth (the fold closes it to
the charity arm through the one writer — receipted disposition, no silent
vanish).
**Dossier round-trip:** open the town → FaithSection ACTIVE renders the tithe
line ("the tithe: heavy — the coffers gleam, the pews grumble") + the temple
wealth band; the economy surface shows the stream via TRADE's coupling. Pin
renders both.
**Bands:** draw weights (piety × rank), pressure bands, resentment scaling,
remission payoff, wealth decay, splendor cap, sack-loot weight, quarterly
cadence constant (pinned to INTERVAL_WEEKS).

### WF-8 — NARRATION 3× (crux ruling 8, SETTLED; flag `faithNarrationEnabled`;
LOCAL lane; lights LAST among faith flags)
**Historical archetype:** the monastic chronicle — the Anglo-Saxon Chronicle kept
by the very faith it narrates; the deep engine finally keeps its own annals.
**Law:** the survey's verdict — a deep engine with a thin voice, ~9 tokens from
~7 sites vs war's ~27 from ~25+, whose realm vocabulary is one pair — is cured to
war's density class. Faith's stories die at the mouth, not the heart; this wave
is the mouth.
**Scope, three supplies:**
- **Voice the silent machinery (the biggest win — receipt-only, behind the
  flag):** the state transitions that today move numbers silently gain kinds:
  faith ENTRY ("a new rite is kept in Greyfen"), EVICTION, suppression,
  resurgence-from-latency, standing changes (cult→established→ascendant),
  legitimacy band crossings, piety band crossings (SP-5's receipts-on-crossing
  law), niche contests short of the patron seat, tolerance shifts, mandate band
  moves. Dark ⇒ silent as today, byte-identical.
- **Carry the new mechanisms' kinds (landed with their waves, gathered here):**
  fall causes ×5, the two obituaries, season bands, legate arrivals/returns,
  reading kinds ×5 + fulfilled/failed, split, covert surfacing/exposure/backfire
  (covert-gated), term formations/breaches ×5, tithe pressure/remission/plunder.
- **The realm-arc expansion:** the Ascendancy/Twilight pair grows to a battery:
  the Schism realm arc EXISTS (realmEvents.js:104) — joined by the Great
  Pilgrimage (a flood season), the Persecution (coordinated suppression), the
  Awakening (sink revival cresting — "crisis calls the faithful home" finally
  narrated at realm scale), the Reformation (a cluster of falls sharing a
  cause). Each rides existing compound-signature machinery (realmEvents'
  pattern), each with the pacing governor's significance gates.
- **The honesty fix:** the Ascendancy headline's "the faithful walk the roads in
  numbers" keys to WF-2's season read where lit (the sentence becomes true) and
  drops the clause where dark (the sentence stops lying — a one-line fork,
  disclosed).
**SP-6 compliance (the checklist IS the acceptance):** every kind — id, address
chain, typed action, settlements by name, reason; WHAT_PHRASES + heraldRouting
registration (faith/divination sections; totality walkers red otherwise);
faithEventFilter FAITH_EVENT_TYPES registration (the premium gate, §3c);
band-word vocabularies only (finite semantics); the two-voices register at the
Bound-Book floor.
**Force / counterforce:** density ↔ THE GOVERNOR — every kind registers
pacing/significance; the flood test (a faith-dense century must not wallpaper
the Herald — measured, WF-9's envelope).
**Clock:** narration is FAST by nature; the governor's suppression windows are
its slow half.
**Pins (negative hardest):** the TOTALITY pin — every kind this program mints
appears in WHAT_PHRASES + heraldRouting + FAITH_EVENT_TYPES (walker; the
id-less-drop class dies here); the NEGATIVE — flag dark ⇒ zero new entries,
byte-identical (the fence); the GOVERNOR pin — a seeded faith-flood fixture
stays under the section's pacing cap; the premium pin — every new kind
correctly filtered for non-premium viewers; count parity — the phrased-token
census lands ≥3× the surveyed baseline (9 → ≥27; the crux ruling's own number,
asserted by census walker, not by hope).
**Lifecycle:** receipts only; no state.
**Dossier round-trip:** the Herald IS the surface; the pin renders one faith-day
digest from a fixture and asserts the house voice register (the two-voices
check).
**Bands:** per-kind significance, section pacing caps, realm-arc thresholds.

### WF-9 — FAITH CONVERGENCE INSTRUMENTATION (no flag; the program's acceptance
harness — the war volume's WR-9 discipline applied to faith)
- **The endings mix:** {converted, hollowed, gone_underground, resurgent,
  extinct, split, communion, imposed_held} — each with a share envelope over the
  deity-bearing corpus (WF-0's case grown to a small grid); one path carrying
  nearly all endings means the others are decoration (L's criterion, imported).
  Extinction rare; resurgence real; the underground neither ubiquitous nor
  unreachable.
- **The tenure distribution:** patron tenure must show a tail and no immortals —
  no eternal incumbent (the hold too strong), no revolving door (the hold too
  weak); the religion-soak's proven 0%-unresolved-schism discipline extends to
  the new transitions.
- **Per-mechanism envelopes:** fall-cause histogram (all five causes REACHED;
  `imposed` not dominant in a peaceful corpus); season band distribution +
  war-suppression measured; reading frequency + fulfilled/failed ratio (the
  prophecy economy is honest — neither always right nor always wrong); covert
  population share cap + surfacing latency distribution; term-family formation
  counts (each family forms somewhere; breach reachable); tithe pressure
  distribution + resentment coupling measured; narration flood test (WF-8's
  governor holds at soak scale).
- **Counterforce audit (the §1b-2 law, measured):** incumbency wins somewhere;
  road danger empties a season somewhere; a failed prophecy costs somewhere; a
  purge backfires somewhere; assimilation empties a cellar somewhere; resentment
  hollows somewhere. A counterforce that never fires at soak scale is a ratchet
  wearing a name.
- **Certification rows:** every WF flag's row grown from the WF-0 shells (Growth-
  lane discipline); the bearer count invariant on every row; v5 deciding-cause
  histograms for falls (the Herald's "why did the god fall" at soak scale).
- **THE UNOBSERVED VERDICT DIES HERE:** with WF-0's case in the corpus and these
  envelopes authored, faith's release verdict becomes MEASURED — the survey's
  final gap. The soak itself remains owner-held; this wave builds the harness.
**Lifecycle:** no world state — envelopes, rows, receipt fields.
**The program is DONE when these envelopes hold on the owner-ordered soak, and
not before.**

---

## §6 JUDGMENT BLOCKS (the drafting chair's rulings under delegation — vetoable
## here; an implementer NEVER re-rules these silently; the eight crux mechanisms
## themselves are spine law, NOT listed here)

- **J-WF-1 (no pantheon coup, ever):** the believer-side ruling is implemented as
  typed classification of the EXISTING settlement transitions; no realm-level
  deity death/dethroning transition is built, zero-seat remnants persist, the
  tier ladder + Twilight remain the only realm fall. VETO builds a realm
  unseating event (and inherits the amendment-C dependency the crux ruling
  cured).
- **J-WF-2 (the pilgrim split):** the SEASON is a banded flow read (never
  errands); only NAMED pilgrims/legates ride SP-1. VETO in either direction:
  every-pilgrim-an-errand (cap explosion, per-head simulation against Law One)
  or no-errands (no interception surface, the drama dies).
- **J-WF-3 (wealth band, not books):** temples get a banded stock with one
  writer; double-entry stays the HOUSE's (FP-TRADE). VETO gives temples
  house-grade books (and prices the added tuning surface consciously).
- **J-WF-4 (covert-in-place):** the covert congregation is a field on the
  existing religionStates suppressed entry, one writer preserved; no parallel
  covert ledger. VETO builds worldState.covertCongregations (and accepts the
  dual-writer drift risk J-WR-11 exists to prevent).
- **J-WF-5 (the fall vocabulary):** {displaced, discredited, abandoned, imposed,
  suppressed} — names vetoable; the closure and the one-cause-per-fall law are
  not.
- **J-WF-6 (the reading vocabulary):** {wrath, test, abandonment, vindication,
  portent} — names vetoable; closure, the no-physics law, and the falsifiable
  expectation arm are not.
- **J-WF-7 (the term family names):** {missionary_access, pilgrimage_right,
  shared_rite_compact, tolerance_guarantee, temple_restitution} — names
  vetoable; the walker-enforced compliance executors and the both-patrons guard
  are not.
- **J-WF-8 (the G1c handling):** WF-3 lands behind the NEW flag so build-time
  goldens never move; the G1c golden shift becomes a lighting-day disclosure at
  the owner-signed soak redo. VETO wires consumption under the existing spread
  flag (and takes the build-day golden shift consciously, against the dormancy
  law).
- **J-WF-9 (voicing the silent machinery is flag-gated):** WF-8's receipts over
  EXISTING transitions land behind `faithNarrationEnabled` — dark worlds stay
  silent and byte-identical; no retroactive narration of past ticks, ever. VETO
  narrates unconditionally (and moves every deity-bearing golden at build time).
- **J-WF-10 (the tithe clock):** quarterly fold — 13 weeks, INTERVAL_WEEKS-
  derived, pinned. VETO picks annual (slower story) or weekly (noise); the
  pin-to-INTERVAL_WEEKS is law either way.
- **J-WF-11 (the lane map):** WF-1/4/5/7/8 are LOCAL-lane (deity presence
  suffices — they are per-settlement believer stories); WF-2/3/6 are SPREAD-lane
  (they cross settlements). VETO re-maps any flag's lane; the two-lane gate
  itself is standing law (§1c-2).
- **J-WF-12 (the bearer count is instrument-side):** WF-0's deityBearers is a
  receipt field, not world state, and lands unflagged (receipts are soak
  instruments; the WR-9 precedent). VETO flags it (and delays every later wave's
  observability behind a lighting decision).
- **J-WF-13 (the subsumption seam):** WF-2a REPLACES pilgrimage.js's scalar
  source when lit rather than adding a second lift (one read, two transports —
  the seam ruling's shape at small scale). VETO stacks season atop scalar (and
  double-counts the draw).
- **J-WF-14 (deferred, recorded — not built here):** the D3 crown-commitment
  contest amplifier (religiousContest.js:773-781) stays owner-gated and
  UNSCHEDULED — it predates this program and its golden shift is not ours to
  spend; the false-omen LURE arm builds in FP-INFORMATION (consumed here); the
  relic-translation errand (moving a shrine's focus between settlements — a
  natural SP-1 extension) is DELIBERATELY DEFERRED to a future amendment:
  documented, not a bug to re-find.

---

## §7 THE TUNING SURFACE (owner-signed at the soak redo, per THE PROMISE)
Every band named in §5, gathered: WF-1 obituary significance + ring depth · WF-2
season thresholds + draw weights + danger suppression + feast cadence + legate
bar · WF-3 aggression threshold cap + durability multipliers + pact nudge cap +
foothold seed · WF-4 lens weights + expectation window + credibility bands +
caps/cooldowns · WF-5 remnant floor + covert seed/growth/assimilation +
surfacing + exposure/backfire + purge pricing · WF-6 per-family appraisal
weights + executor sizes + formation thresholds · WF-7 draw weights + pressure/
resentment/remission + splendor cap + sack-loot weight + the 13-week constant ·
WF-8 significance + pacing caps + realm-arc thresholds · WF-9 envelope shapes.
Band FAMILIES ride the spine's shared shapes wherever one exists (SP-5 crossing
receipts, SP-4 posture bands) to keep the owner's signature surface tractable —
the spine's priced cost, honored. None a bare float on a surface; all in one
tuning table per wave.

## §8 HERALD + LEGIBILITY CONTRACT (the sentences this program must be able to
## say, in the house voice, at the Bound-Book register floor)
- "The Lady of Harvests fell discredited in Greyfen — a decade of stain, and the
  Forge's bell now rings the hour." (WF-1)
- "The last altar of the Pale Warden went dark; none now keep the rite." (WF-1)
- "Men still dying for a god nobody worships" — war's WR-1 sentence, now able to
  name WHICH fall emptied the cause. (WF-1 × WR-1)
- "Pilgrims on the road to Greyfen: the feast fills every inn." (WF-2)
- "No legate came; the Forge keeps its counsel while the roads burn." (WF-2)
- "A legate arrived at court, and did not say all of why." (WF-2 × INFO)
- "Communion kept at the river shrine — the old quarrel cools." (WF-3)
- "The priests read the failed harvest as the Lady's wrath; the pews fill with
  the frightened." (WF-4)
- "Two towns read one famine: wrath in Greyfen, a test of the faithful in
  Marrow's Ford." (WF-4)
- "The spring rite was kept, and the rains did not return; the pulpit is quieter
  now." (WF-4, the failed prophecy)
- "The temple split: the old rite keeps the nave, the new keeps the crypt." (WF-5)
- "By candlelight in the undercroft, the Lady's rite is kept." (WF-5, DM-only)
- "The hidden congregation was dragged into the light, and the pews filled in
  defiance." (WF-5)
- "Peace signed: the victor's missionaries may preach within the walls." (WF-6)
- "The purge broke the guarantee, and the realm knows why." (WF-6)
- "The tithe weighs heavier than the harvest; the coffers fill as the pews
  thin." (WF-7)
- "In the famine year the Temple forgave the tithe, and the town remembered."
  (WF-7)
Every one carries id + full address chain + typed action + named settlements +
recorded reason (law 1a), registers in WHAT_PHRASES + heraldRouting +
FAITH_EVENT_TYPES, and passes the premium projection audit.

## §9 SEQUENCING

**Against the spine (§5 of DESIGN_FP_SPINE.md):** FAITH is the fifth program —
after SP (the errand system, believed-world extension, pact grammar, posture,
confidence family, narration kit, temporal walker), GRAMMAR, INFO, and TRADE;
before POP and the INTERIOR completions. All behind the sim-proof path; nothing
here lights a flag, runs a soak, or ratifies a band — those are owner-held.

**Internal order:** WF-0 first (the instrument precedes the mechanism) → WF-1
(the fall vocabulary is consumed by WF-5's endings and WF-8's voices) → WF-2
(needs SP-1; the season precedes the pilgrimage_right term) → WF-3 → WF-4 →
WF-5 (consumes WF-1 causes + WF-3's foothold seed) → WF-6 (needs SP-3 + WF-2's
season for its executor) → WF-7 → WF-8 (narrates everything; builds last) →
WF-9 closes. WF-4 and WF-7 are order-slack against their neighbours (LOCAL-lane,
no WF dependencies beyond WF-0) and may interleave by implementer convenience
WITHIN the one-wave-one-commit discipline.

**Lighting order = build order,** at the owner-signed soak redo, after
`faithSpreadEnabled` for the SPREAD-lane flags (§3's lane ruling); WF-8's flag
lights last. A WF flag lit out of order is an invalid config WF-9's walker reds.

**Cross-program joins:** WF-1's typed causes are consumed by war's WR-1
dissolution receipts when `warTerminationEnabled` lights (that flag is war's,
already built, owner-sequenced); WF-6 consumes SP-3 + the war volume's
negotiationPictures seam; WF-2b consumes WR-7b's foreign-guest hold for
intercepted named movers. Each join is read-only from this side; conflicts are
STOP-and-report.

## §10 IMPLEMENTER PROTOCOL — the war volume's §10 binds verbatim, plus the faith
## addenda
1. All of DESIGN_WAR_RULINGS_ARCHITECTURE.md §10 (worktree/branch discipline, no
   stash, pathspec commits, gate-tail, one wave one commit, golden discipline,
   generation-tree helpers, STOP-and-report).
2. **Every faith fixture is deity-bearing through the doctrine path** —
   SET_PRIMARY_DEITY/IMPOSE_CULT via the store lane, never config poking, never
   a premade pool (§1c-3). A faith pin green on a deity-free world is vacuous
   and the review will treat it as absent.
3. **Every new kind registers three ways** — WHAT_PHRASES, heraldRouting,
   faithEventFilter's FAITH_EVENT_TYPES — or the totality walkers red; covert
   kinds additionally prove includeCovert isolation.
4. **Never declare a wizard_news.* id on a faith certification row** (the
   moverFamily skew, §1d); the bearer count is the certification instrument.
5. **VERIFY-AT-BUILD register (assertions this volume makes beyond the survey's
   executed evidence — verify before building on each):** the exact
   religionStates apply chokepoint for in-place folds; hot-file headroom for
   religionState.js/religiousContest.js; SP flag names; the observance hook
   pilgrimage.js lifts; the covert/revealed seam's reusable surface for the
   faith arm; heraldRouting's divination-section rules for new kinds;
   TERM_CATALOG's dormancy idiom for flag-shaped catalog extension; TRADE's
   stream-physics API for the tithe and feast executors.
6. **Report, don't rule.** Any conflict between this volume, the spine, the
   amendments, or the tree is a STOP-and-report to the validation chair.
