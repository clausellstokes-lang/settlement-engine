# W-R2-LIGHT — the lighting wave (the engine becomes reachable)
## Read docs/briefs/W_R2_COMMON_PROTOCOL.md FIRST. Branch: `claude/w-r2-light`, cut from the
## POST-FOLD-IN merge-branch tip (the brief dispatcher supplies the exact hash — verify it).
## Implements the OWNER LIGHTING RULING (2026-07-16, recorded in COMPREHENSIVE_REVIEW_PROGRAM.md).

**CHARGE:** all nine dark wave gates light in the three world-alive presets; the engine the
program built becomes reachable by users. GOLDEN-SHIFTING BY DESIGN for preset-dependent
fixtures — your branch parks red like G2, with a predicted shift map; the regen executes at
the owner-signed moment (after soak-tuning, per the re-sequenced order).

**THE RULING (binding):**
- `dramatic_campaign`, `living_realm`, `full_simulation` each gain ALL NINE:
  momentumEnabled, navalEnabled, interventionEnabled, settlementLifecycleEnabled,
  peaceEngineEnabled, supplyWebWarfareEnabled, upswingArcsEnabled, resourceDynamicsEnabled,
  constructiveFlowsEnabled.
- `quiet_local` / `static_campaign` / `narrative_campaign`: UNCHANGED. commodityFlowEnabled
  stays opt-in everywhere. Existing campaigns byte-identical (stored rules; virtual flags).

**THE WORK:**
1. simulationRules.js preset rows (mind the KEY-ORDER-IS-LOAD-BEARING comment — presetIdForRules
   infers by first structural match; extend the preset-stability pins for the new keys and
   verify legacy saved-rules inference is unchanged — the SEASONS-A/A3 accepted-drift class
   note applies if display-only re-inference occurs: document it).
2. SimulationRulesDialog: the ENGINE WAVES section — nine toggles, each with its one-line
   in-register description (the glossary already carries the terms — link, don't duplicate),
   grouped, default-off for custom configs, honoring the axes-lock UX pattern.
3. realmManifest refusal prose: every darkFlag message names the PRESET + the dialog path
   (closing sim-cohesion-4's misleading-prose half) — the switch it names must now render.
4. Preset-content pins updated; the moverCompositionSmoke and checkpoint-soak configs verify
   they now cover the full stack via full_simulation (they should automatically — assert it).
5. PREDICTED SHIFT MAP (docs/review-r2/LIGHT_SHIFT_MAP.md on your branch): which fixtures move
   because presets changed (worldpulse goldens riding lit presets; preset identity snapshots),
   each mapped. Everything else must stay green — dark-config goldens byte-identical.

**Eager bytes:** simulationRules.js rides the LAZY engine chunk — expected ZERO eager delta;
measure and quote. Full gate at end (the enumerated preset-golden reds + the two known
owner-gated reds excepted and listed). Structured report per the house format.
