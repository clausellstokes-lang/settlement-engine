# TRACK-G2 + W-R2-DEPTH — the golden-shifting batch and the depth wave (two briefs, one file)
## Read docs/briefs/W_R2_COMMON_PROTOCOL.md FIRST.

---

# TRACK-G2 — the golden-shifting batch. Branch: `claude/w-r2-g2`
## ⚠️ EVERY fix here legitimately shifts same-seed bytes. IMPLEMENT + EVIDENCE ONLY — the ONE
## regen is executed by the manager at the owner-signed regen moment (batched with the
## W-R2-LIGHT preset shifts). You leave the goldens RED with a per-fix predicted-shift map.

**FENCE:** the specific files below + tests. Work on the branch; the branch's goldens stay red
BY DESIGN at the end — your deliverable is fixes + pins + THE SHIFT MAP (per fix: which
fixtures/fields move and why that is exactly the intended semantic change).

**THE FIXES** (verdict corrections BIND):
1. `domain-top-state-2` — drop the stale law_order→criminal_opportunity alias (CAUSAL_SYSTEM_
   ALIASES + the crisisLifecycle byte-identical mirror + its string-coupling pin updated);
   conditions declaring law_order reach the real variable; audit whether criminal_opportunity
   should co-receive (record the call). aiGrounding bundle rebuild required (freshness gate).
2. `domain-top-state-1` — condition-drift write-back preserves the raw status when input was
   missing/invalid (or carries driftStatus separately); extend the pin to two ticks. Bundle
   rebuild required.
3. `spatial-engine-1` — banditry loss reads the chosen route's danger (ScoredRoute.danger per-
   hop mean or max) at BOTH arrival sites (supplyShipments + commodityFlow) instead of origin
   embattlement; update the 'embattled source' pin to the new semantics.
4. `data-tables-5` — coal_deposits commodities ['timber']→['coal']; re-verify the
   classification pin (subterranean rule holds); note dead 'peat' row disposition.
5. `data-tables-2` — the terrain double-stack regression: forest's second Sawmill row →
   a distinct live institution or dropped; mountain/hills Quarry+Stone-quarry pairs merged;
   the no-double-match walker pin.
6. `generators-domain-1` — the five second-wave stress types gain their numeric couplings
   (defense scores, food multipliers, slave_revolt prosperity); extend the registration walker
   to source-scan the hasStress() consumer blocks (or record explicit exemptions).
7. `domain-top-state-4` — 'subordinate'→'secondary' consequence-tier mapping + String() coerce
   + NpcRank typedef alignment + the vocabulary pin. (Display-consequence distributions may
   shift — verify; if byte-neutral it folds back to a normal fix, note it.)
8. CANDIDATE (owner-gated, do NOT implement — record only): DATA's slave-trade export enable
   (on:true) — carried on the G2 candidate list for the owner batch.
9. D6 THE UNDERWAYS catalog half (per docs/DESIGN_SIM_DEPTH_R2.md D6): the full catalog +
   generation parity checklist (entry, services, NPC role, geography rows single-match, stress
   integrations, founding mentions, every catalog pin extended). This is the largest shift —
   its map should enumerate: which existing worlds gain the institution (roll-dependent), the
   distribution delta on institution counts, and the zero-impact proof for worlds whose rolls
   don't reach it.

**DELIVERABLE:** THE SHIFT MAP (docs/review-r2/G2_SHIFT_MAP.md, committed on your branch):
per fix — files changed, fixtures that move, fields that move, the one-sentence semantic
justification, and the fold-back list (any fix that proved byte-identical after all).

---

# W-R2-DEPTH — the depth wave. Branch: `claude/w-r2-depth`
## Implements docs/DESIGN_SIM_DEPTH_R2.md D1–D5 + D6's ENGINE-COUPLINGS HALF, exactly as
## frozen (the coherence matrix's discovered couplings are IN SCOPE and binding).

**FENCE:** src/domain/worldPulse/**, src/domain/spatial/** (smuggle modifier read only),
src/domain/display (hegemony read-model + receipts), src/domain (facetOf consumers), src/data
(NOTHING — the catalog half is G2's), tests. Everything dormancy-gated per the design; zero
eager beyond the design's stated budget (~2 key literals); measure.

**THE BUILDS** (each with its design section's pins):
- D1 distance-priced news (flag `distancePricedNewsEnabled`; the recency-fold hop delay; the
  generosity believed-need coupling from the matrix; DM surfaces never delayed).
- D2 THE SCALING LAW (tempo sublinear + starvation weighting; rollCandidates maxAuto/
  maxProposals √N forms; proposal-ring N-scaling on TOP of INTENT's receipted eviction;
  retention caps sublinear + per-settlement rescue; supplyShipments top-k semantics VERIFIED
  first — scale only if realm-global; N ≤ BASE ⇒ byte-identical, pinned).
- D3 doctrine courses (courseKeyOf 'doctrine:' kind; deposits from public religious acts;
  the crack priced once with the synod off-ramp; the bloc-glue coupling from the matrix).
- D4 hegemony read (pure lazy selector; naval strength INCLUDED per the matrix; satellites
  excluded) + fear_of_dominance (typed reason; believed-strength inputs; balances-never-
  bandwagons; the counter-intervention motive weighting; distance discount; pins per design).
- D5 lifespan-scaled memory (memoryHorizon facet, four bands, default 1.0 byte-identical;
  BOTH signs scale; contracts never scale; undying erodes via reconciliation only).
- D6 engine couplings (facet-read: smuggle success/detect modifiers; siege+blockade supply-
  trickle floor; corruption exposure discount + covert-bloc ease; escape receipts; the
  detectInstitutionGaps underways gap). NOTE: with the catalog half on G2 unmerged, the facet
  exists only via CUSTOM institutions in tests — your pins plant custom clandestine fixtures
  (which also proves the facet law's custom-content parity, killing two birds).

**SEQUENCING NOTE:** you may land before or after G2 — the couplings are facet-driven and
dormant, so order is free by construction; assert it (your dormancy pins prove absent-facet ⇒
byte-identical).
