# C1-fin CONTROL CENSUS — the base re-cut's functionality-retention baseline

Generated 2026-07-18 by the census workflow (4 parallel read-only inventories +
synthesis; worktree claude/deep-craft @ fb5e8031 vs master d024286e). Law:
every composite control appears EXACTLY ONCE below; a dropped row at implementation
time is a functionality-retention violation. Dispositions: KEEP = master's single
LayeredConfigurationPanel already hosts the binding; RESEAT:<layer> = post-master
addition, re-seat into the named layer; DEFER-CANDIDATE = flagged, implementer
defers-with-reason or re-seats (do not silently drop).

## Census table

| Control | Binds to | In master | Disposition | Notes |
|---|---|---|---|---|
| Settlement Name (optional) | config.customName (updateConfig, slice 25) | yes | KEEP | Foundations. Master ConfigurationPanel hosts it (confirmed). |
| Use my custom content | config.useCustomContent (updateConfig) | yes | KEEP | Foundations. Tier-gated (canUseCustomContent()&&getCustomContentCount()>0) in both; default ON. |
| Population (tier) | config.settType (updateConfig) | yes | KEEP | Foundations. Writes settType DIRECTLY, bypassing setSettlementType tier clamp — SAME in master, not a restructure regression. blockTownPlus hide/disable intact. |
| Custom Population | config.population (updateConfig) | yes | KEEP | Foundations; visible when settType==='custom'. min10 max500000. |
| Trade Route | config.tradeRouteAccess (updateConfig) | yes | KEEP | Foundations. blockIsolated hide/disable intact. |
| Terrain | config.terrainOverride (updateConfig) | yes | KEEP | Foundations. Feeds NearbyResources compatibility. |
| Culture | config.culture (updateConfig) | yes | KEEP | Foundations. |
| Age (mode) | config.settlementAgeMode (updateConfig) | yes | KEEP | Foundations. Master bundles this as the 'Age' control. |
| Custom years | config.settlementAgeYears (updateConfig) | yes | KEEP | Foundations; conditional on settlementAgeMode==='custom'. Bundled under master's Age control. |
| Regional Threat | config.monsterThreat (updateConfig) | yes | KEEP | Foundations. |
| Magic in the World? | config.magicExists (updateConfig) | yes | KEEP | Foundations. Cross-field side effects (priorityMagic; isolated->road at town+) present in master too. |
| HelpPopover '?' triggers (7: tier/trade-route/terrain/culture/settlement-age/monster-threat/magic-level) | local open state; fires help_popover_opened | yes | KEEP | Foundations. CONFIRMED present in MASTER ConfigurationPanel (import @line6; <Lbl topic=...> on all 7 labels). Flag compendiumInlineHelp self-gates. Guidance mount — folded into checklist. |
| Character: Random chip | setRandomSliderMode(true)+setCustomSlidersExplicit(false) | yes | KEEP | Character preset (Layer 0). CharacterPresetCard byte-identical to master. |
| Character archetype chips (17, 7 groups) | config.priorityEconomy/Military/Magic/Religion/Criminal(+monsterThreat) via archetypePatch->updateConfig; +setRandomSliderMode(false)+setCustomSlidersExplicit(false) | yes | KEEP | Character preset. Arcane group hidden when magicExists===false; active chip derived by exact-match. Byte-identical to master. |
| Character: Custom chip | setRandomSliderMode(false)+setCustomSlidersExplicit(true) | yes | KEEP | Character preset. |
| Priority sliders (5: Economy/Military/Magic/Religion/Criminal) | config.priority* via handleSliderChange (also exits randomSliderMode) | yes | KEEP | Character preset. Magic slider hidden when magicExists===false. PrioritySliders byte-identical to master. |
| Fine-tune disclosure | local Disclosure open state | yes | KEEP | Fine-tune (Layer 2) gate; master's Layer 2 IS this Disclosure, mounted via ConfigurationPanel showFineTune=advancedOnDesktop. |
| Nearby Resources: Random toggle | config.nearbyResourcesRandom (+nearbyResources+nearbyResourcesState) | yes | KEEP | Fine-tune. |
| Nearby Resources: All / None | config.nearbyResources (+nearbyResourcesState) | yes | KEEP | Fine-tune; visible when Random OFF. |
| Nearby Resource cycle chips (per resource) | config.nearbyResources+config.nearbyResourcesState (4-state cycle) | yes | KEEP | Fine-tune; interactive when Random OFF. Compatibility from getCompatibleResources(route,terrain). |
| Settlement Stress: Random toggle | config.selectedStressesRandom (+selectedStresses) | yes | KEEP | Fine-tune. |
| Settlement Stress: All / None | config.selectedStresses | yes | KEEP | Fine-tune; visible when Random OFF. |
| Stress-type chips (per STRESS_TYPE_MAP key) | config.selectedStresses (toggle) | yes | KEEP | Fine-tune; interactive when Random OFF. |
| Place in Region: Upgrade | setPurchaseModalOpen(true) | yes | KEEP | Place-in-Region (Layer 4). Non-premium read-teaser. PlaceInRegionCard drifted cosmetically only; setPurchaseModalOpen identical. |
| Place in Region: Campaign / region | config.targetCampaignId (updateConfig; ''->null) | yes | KEEP | Place-in-Region; premium (canUseCustomContent()). WATCH: default showPlaceInRegion=true re-exposes this surface on desktop-advanced — reviver must consciously pass showPlaceInRegion (orphan red flag #3). |
| Place in Region: Patron deity at birth | config.primaryDeityRef (updateConfig; ''->null) | yes | KEEP | Place-in-Region; premium. Options from customRegistry listCustom('deities'). |
| Search institutions | local search state (InstitutionalGrid) | yes | KEEP | Deep constraints (Layer 3). InstitutionalGrid store bindings byte-identical to master; local view state resets on remount. |
| Clear search (x) institutions | local search='' | yes | KEEP | Deep constraints; visible when search non-empty. |
| Force All institutions | bulkSetInstitutions(catalogGetter,tier,'force') | yes | KEEP | Deep constraints. bulkSetInstitutions confirmed in master (5 call-sites, identical). |
| Reset institutions | bulkSetInstitutions(...,'reset') | yes | KEEP | Deep constraints. |
| Exclude All institutions | bulkSetInstitutions(...,'exclude') | yes | KEEP | Deep constraints. |
| Expand All / Collapse All institutions | local allCollapsed state | yes | KEEP | Deep constraints; local view state. |
| Filter pills All/Forced/Excluded institutions | local filterMode state | yes | KEEP | Deep constraints; local view state. |
| Category section header (per category) | local per-section collapsed + toggleCategory->categoryToggles | yes | KEEP | Deep constraints. toggleCategory/categoryToggles confirmed in master (identical). |
| Institution card (per institution) | institutionToggles[tier::category::name] (toggleInstitution) | yes | KEEP | Deep constraints. In/out-of-tier cycles. OutOfTierSection is DEAD CODE in BOTH master and tip (not new drift). |
| Search services | local search state (ServicesTogglePanel) | yes | KEEP | Deep constraints. ServicesTogglePanel BYTE-IDENTICAL to master (search @line71 confirmed). |
| Clear search (x) services | local search='' | yes | KEEP | Deep constraints. |
| Force All services | servicesToggles (bulkForce -> per-key toggleService{force}) | yes | KEEP | Deep constraints. CONFIRMED bulkForce/onForceAll present in MASTER (@line115/181) — master inventory's 'search+filter+Reset' toolbar prose was terse, not exhaustive. Not a post-master addition. |
| Reset services | servicesToggles (setServiceToggles({})) | yes | KEEP | Deep constraints. Uses setServiceToggles({}) not bulkSetServices — same in master. |
| Exclude All services | servicesToggles (bulkExclude -> per-key toggleService{forceExclude}) | yes | KEEP | Deep constraints. CONFIRMED bulkExclude/onExcludeAll present in MASTER (@line122/183). |
| Expand All / Collapse All services | local expanded map | yes | KEEP | Deep constraints. onExpandAll/onCollapseAll confirmed in master (@line184-185). |
| Filter pills All/Forced/Excluded services | local filterMode state | yes | KEEP | Deep constraints; local view state. |
| Service group header (per institution/svcKey) | local expanded[svcKey] | yes | KEEP | Deep constraints; local view state. |
| Service card (per service) | servicesToggles[svcKey_service_name] (toggleService) | yes | KEEP | Deep constraints. Keyed by catalog svcKey (grouped insts share override) — same in master. |
| Trade Dynamics outer disclosure ('Step 4: Trade Dynamics') | local open state (TradeDynamicsPanel) | yes | KEEP | Deep constraints. QUIRK: this component-internal disclosure nests INSIDE master's DeepSection('trade') Disclosure -> a double-disclosure the implementer should collapse. Legacy 'Step 4' label. Present in both master and tip. |
| Search goods | local search state (GoodsPanel) | yes | KEEP | Deep constraints. CONFIRMED search present in MASTER TradeDynamicsPanel (@line110) — master inventory omitted it in prose (terse). |
| Clear search (x) goods | local search='' | yes | KEEP | Deep constraints. |
| Force All goods | goodsToggles (bulkForce -> per-key toggleGood{force}) | yes | KEEP | Deep constraints. bulkForce confirmed in master (2 call-sites, identical). |
| Reset goods | goodsToggles (setGoodsToggles({})) | yes | KEEP | Deep constraints. setGoodsToggles confirmed in master. |
| Exclude All goods | goodsToggles (bulkExclude -> per-key toggleGood{forceExclude}) | yes | KEEP | Deep constraints. bulkExclude confirmed in master. |
| Expand All / Collapse All goods | local showExport+showImport | yes | KEEP | Deep constraints; local view state (both true / both false). |
| Filter pills All/Forced/Excluded goods | local filterMode state | yes | KEEP | Deep constraints; local view state. |
| Export Goods section header | local showExport | yes | KEEP | Deep constraints; local view state. |
| Import Goods section header | local showImport | yes | KEEP | Deep constraints. WARNING: Import renders the SAME goods list/cards writing the SAME goodsToggles key as Export — no distinct import dimension. SAME in master (not new). |
| Good card (per good) | goodsToggles[tier_good_name] (toggleGood) | yes | KEEP | Deep constraints. Rendered in both Export and Import sections against one key. |
| Back (step nav) | wizardStep (setWizardStep(max(0,step-1))) | NO | DEFER-CANDIDATE | Stepped-wizard navigation. Master's single-surface LayeredConfigurationPanel has NO wizardStep stepping (all layers render at once) — no functionality lost (every control becomes simultaneously visible), but whether to delete wizardStep or retain a progressive-disclosure affordance is structural. FLAGGED, not decided. |
| Next (step nav) | wizardStep (setWizardStep(step+1)) | NO | DEFER-CANDIDATE | Stepped-wizard navigation; obviated by single-surface panel. FLAGGED, not decided (see Back). |
| Ready to Generate (step nav) | wizardStep (setWizardStep(STEPS.length)) | NO | DEFER-CANDIDATE | Advances to the closeout view. Obviated by single-surface panel (master shows WizardCloseout recap + Generate Draft without a step advance). FLAGGED, not decided. |
| Generate Draft (Basic mode) | handleGenerate -> generateSettlement() | yes | KEEP | Master's single fullWidth Generate Draft (config-stage). anon-at-cap -> onSignIn(); fires generation_started from fresh store snapshot. |
| Generate Draft (Advanced closeout) | handleGenerate (WizardCommitBand) | yes | KEEP | CONSOLIDATES into master's SINGLE Generate Draft button; the WizardCommitBand host is dropped. Same handler; generateError surfaces inline above it in master. |
| Mode switch Basic/Advanced (chrome-diet, WizardChipRow) | wizardMode (setWizardMode; resets wizardStep 0) | yes | KEEP | Master hosts the mode switch via ChangeModeBar (+setEntryPath). The wizardChromeDiet WizardChipRow host is dropped; binding preserved. |
| Loaded-config chip clear (x) (chrome-diet) | clearLoadedFromSave | yes | KEEP | Master hosts via WizardLoadedBanners dismiss. Chrome-diet chip host dropped. |
| Neighbour chip clear (x) (chrome-diet) | clearNeighbour | yes | KEEP | Master hosts via WizardLoadedBanners. Chrome-diet chip host dropped. |
| Create breadcrumb (mode exit) | setWizardMode(null) -> card picker | yes | KEEP | Master ChangeModeBar onChangeMode has the `if(m)` guard -> supports the null/Create-exit path (setWizardMode(null), skip setEntryPath). |
| Mode switch Basic/Advanced (ChangeModeBar) | wizardMode (setWizardMode) | yes | KEEP | This IS master's host (ChangeModeBar). The two composite mode-switch rows collapse onto this one master control. |
| Loaded-config / Neighbour clear (x) (legacy banners, WizardLoadedBanners) | clearLoadedFromSave / clearNeighbour | yes | KEEP | This IS master's host (WizardLoadedBanners). The chrome-diet chip clears collapse onto these. |

## Reseat plan

METHODOLOGY / HEADLINE FINDING (CONFIRMED by executed git at worktree tip fb5e8031 vs master d024286e): the census contains ZERO true RESEAT rows. Every configuration/store-binding control in the COMPOSITE stepped wizard has an exact equivalent in master's single LayeredConfigurationPanel, because the deep-craft stepped wizard RE-ARRANGED master's controls into steps rather than adding new controls. Proof: LayeredConfigurationPanel.jsx, CharacterPresetCard.jsx, PrioritySliders.jsx and ServicesTogglePanel.jsx are BYTE-IDENTICAL master<->tip; ConfigurationPanel/PlaceInRegionCard/InstitutionalGrid/TradeDynamicsPanel DRIFTED only cosmetically — their binding surfaces are identical (all 12 updateConfig fields match; toggleInstitution/bulkSetInstitutions(x5)/toggleCategory/categoryToggles match; toggleService/bulkForce/bulkExclude/setServiceToggles match; toggleGood/bulkForce/bulkExclude/setGoodsToggles match; setPurchaseModalOpen matches). The two 'gaps' the master inventory's terse toolbar prose implied — services Force-All/Exclude-All and goods Search — are BOTH already in master (ServicesTogglePanel @line115/122/181/183/184; TradeDynamicsPanel @line110). HelpPopover (all 7 Lbl topics) is likewise in master's ConfigurationPanel. So nothing was 'added after master' at the config layer.

WHICH LAYER RECEIVES WHAT (the re-hosting map — how the stepped controls fold back into the 5 layers): Layer 0 Character preset <- Random/Custom/archetype chips + 5 priority sliders (step 0). Layer 1 Foundations <- Settlement Name, Use-custom-content, Population(+Custom Population), Trade Route, Terrain, Culture, Age(+Custom years), Regional Threat, Magic, and the 7 HelpPopover '?' triggers riding on the Foundations labels (step 0). Layer 2 Fine-tune (Disclosure inside ConfigurationPanel) <- Nearby Resources (Random/All/None/cycle chips) + Settlement Stress (Random/All/None/type chips) (step 0 Fine-tune). Layer 3 Deep constraints (three DeepSection Disclosures) <- all of step 1 InstitutionalGrid (cards + search/clear/force/reset/exclude/expand-collapse/filter/category), all of step 2 ServicesTogglePanel (cards + toolbar), all of step 3 TradeDynamicsPanel (good cards + toolbar + export/import headers + its own outer disclosure). Layer 4 Place-in-Region <- Campaign select, Patron-deity select, Upgrade teaser (step 0). Config-stage CHROME (outside the 5 layers) <- the mode switch, Create-exit crumb, loaded-config/neighbour clears fold onto master's ChangeModeBar + WizardLoadedBanners; the two Generate Draft controls collapse onto master's single fullWidth Generate Draft.

THE ONLY NON-KEEP DISPOSITIONS are the three stepped-wizard NAVIGATION controls (Back / Next / Ready-to-Generate), bound to wizardStep — a concept the single-surface layered panel eliminates because all layers render simultaneously. These are DEFER-CANDIDATE, not RESEAT: no control functionality is lost (every editor becomes simultaneously reachable), but the decision to delete wizardStep outright versus retain a progressive-disclosure/accordion affordance is a structural UX call. FLAGGED for the implementer/owner — not decided here.

TWO RE-HOSTING QUIRKS the implementer must resolve while re-seating Layer 3: (1) TradeDynamicsPanel carries its OWN outer disclosure ('Step 4: Trade Dynamics', legacy label) which will nest inside master's DeepSection('trade') Disclosure -> a double-disclosure to collapse. (2) The Import Goods section writes the SAME goodsToggles key as Export (no distinct import dimension) — preserved as-is (matches master), but worth a note so it is not mistaken for lost functionality.

## Red flags (carry into implementation verbatim)

- [ORPHAN #1] SUPERSEDED, not broken — the primary flag. Every import resolves and it is byte-identical to master, so it compiles/mounts fine; the real danger is that deep-craft deliberately REPLACED this single-surface layered panel with a multi-step wizard (StepIndicator/WizardChipRow/WizardCommitBand/WizardCloseout/…). Reviving it re-introduces the OLD Create UX and would duplicate or conflict with the intended stepped flow — a design regression a module-resolution check won't catch.
- [ORPHAN #2] Analytics funnel double-fire: the orphan fires EVENTS.WIZARD_STEP_VIEWED on mount (step_id 'config', mode 'layered') and on each Deep-section first-open (mode 'layered'). The live tip GenerateWizard ALSO fires WIZARD_STEP_VIEWED (GenerateWizard.jsx:191). Mounting both emits competing/duplicate wizard_step_viewed events with different `mode` tags, corrupting the funnel — a data-integrity hazard, not a compile error.
- [ORPHAN #3] No current parent contract: because tip GenerateWizard dropped it, a naive `<LayeredConfigurationPanel />` gets no `mode` (falls back to 'advanced') and inherits showPlaceInRegion=true, re-exposing the premium PlaceInRegionCard on desktop-advanced. Master always supplied `mode` explicitly; a reviver must too.
- [ORPHAN #4] Overlapping/conflicting mobile strategy: the orphan carries its own mobile handling (useIsMobile + DesktopOnlyGate + a deep-constraints mobile gate). Recent tip work ('Phase 5c per-surface sweep — gate heavy authoring') gives the stepped wizard its own mobile authoring strategy; reviving the orphan layers a second, possibly conflicting mobile gate.
- [ORPHAN #5] Orphaned test coupling: tests/components/layeredConfigurationPanel.test.jsx still targets this file and asserts the OLD single-surface behavior. Deleting the orphan breaks that test; reviving it keeps a test that pins UX the stepped wizard has moved away from.
- [HERO #1] THE GAUGE renders all six TIER_ORDER stations to ANON (thorp/city/metropolis disabled+faint but VISIBLE) — directly violates the owner law 'anonymous render shows Hamlet/Village/Town ONLY'. Not caught by any test: e2e flow-a only asserts the three ARE visible, never that the others are absent.
- [HERO #2] TWO visible anon clerk's notes under the gauge (generate.sizeHint.{picked} + generate.gauge.capMemo) exceed the 'at most 1 visible clerk's note' law.
- [HERO #3] The tip is the PRE-remediation lineage: master d024286e's whole review-remediation (LANDING_MAX unify, PageHeader, quiet ModeSelector, v2-inlined hero, registry cap copy, P10 failure strip, InstantWorldEntry removed from empty state, anon banner deleted) is ABSENT. BASE RESTORATION (#35) is a RE-APPLY of master's composition, not a relocate-the-GAUGE task — scope is larger than the two lane commits.
- [HERO #4] Reconciliation is import-blocked: tip theme.js does not export LANDING_MAX or DANGER_BORDER; master's composition depends on both. They must be surfaced before master's HomeHero/WizardEmptyState can drop in.
- [HERO #5] InstantWorldEntry (premium demo card, f472a350) sits in the signed-in empty state ABOVE the fold, competing with the single-plate law; master removed it from this surface.
- [HERO #6] Dead-ish heroV2 flag branch + old eyebrow/title/antiAi fallback still live in tip HomeHero after the flag was promoted; master inlined and deleted the branch + the `flag` import + old copy keys.
- [SYNTHESIS — CONFIRMED] No config control is a post-master addition: the DRIFTED child components (ConfigurationPanel/PlaceInRegionCard/InstitutionalGrid/TradeDynamicsPanel) are binding-identical to master (verified: every updateConfig field, toggle*, bulk op, setPurchaseModalOpen match). The stepped wizard is a re-chroming, not new functionality — so the config-stage restructure is KEEP-only re-hosting plus dropping the obviated step navigation. Do NOT manufacture RESEAT work from the master inventory's terse toolbar prose (it omitted services Force/Exclude and goods Search that DO exist in master).
- [SYNTHESIS] Double-disclosure at Layer 3: TradeDynamicsPanel's own outer 'Step 4' disclosure will nest inside master's DeepSection('trade') Disclosure when re-hosted — collapse one, and drop the legacy 'Step 4' label.
- [SYNTHESIS] WizardCloseout carries a STALE code comment ('self-gates on the flag') per the composite survey; in master it renders unconditionally when isAdvanced && wizardStep>=STEPS.length && !settlement. Since the swap removes wizardStep stepping, the advanced read-only recap's mount condition must be re-derived (advanced && !settlement) or it silently never renders.

## Implementer step-1 checklist (ordered)

1. PHASE A — REVIVE-ORPHAN FIXES (src/components/generate/LayeredConfigurationPanel.jsx). A1: Confirm the orphan mounts as-is — it is byte-identical to master and all 12 imports resolve at tip; it renders the tip child components which are binding-identical to master (no config-control drift). No edit to the panel body itself is required.
2. A2: Kill the analytics double-fire (ORPHAN #2). The panel fires WIZARD_STEP_VIEWED on mount (step_id 'config') + on each DeepSection first-open, mode='layered'. Ensure the GenerateWizard's own per-step WIZARD_STEP_VIEWED emitter (GenerateWizard.jsx:191) is REMOVED in Phase B so the panel is the sole emitter — reconcile to one mode tag.
3. A3: Reconstruct the parent contract (ORPHAN #3). The reviver MUST pass mode explicitly (wizardMode==='advanced'?'advanced':'basic') and CONSCIOUSLY decide showPlaceInRegion rather than inherit default-true (default re-exposes the premium PlaceInRegionCard on desktop-advanced).
4. A4: Resolve the overlapping mobile strategy (ORPHAN #4). The panel owns useIsMobile + DesktopOnlyGate deep-constraints gate; make it the SINGLE mobile authority for the config stage — remove the stepped wizard's separate Phase-5c mobile gate.
5. A5: Retarget the pinning tests — keep tests/components/layeredConfigurationPanel.test.jsx green under the revived panel (it asserts the single-surface behavior, ORPHAN #5) and reconcile tests/components/generateWizardFocus.test.jsx.
6. PHASE B — GENERATEWIZARD CONFIG-STAGE SWAP (GenerateWizard.jsx, branch if(!settlement)). B1: Replace the stepped wizard (StepIndicator / WizardChipRow / ControlsStrip steps / WizardCommitBand / Back-Next-Ready nav) with master's composition top-to-bottom: ChangeModeBar; WizardLoadedBanners; PageHeader; onboard-highlight wrapper containing <LayeredConfigurationPanel mode={...}/>; WizardCloseout (advanced-only recap); generateError alert; single fullWidth Generate Draft; subline caption.
7. B2: DEFER-CANDIDATE — Back / Next / Ready-to-Generate step navigation. The single-surface panel obviates wizardStep stepping (all layers visible at once). FLAG for implementer/owner: delete wizardStep entirely vs. retain a progressive-disclosure affordance. Do NOT silently decide.
8. B3: Consolidate the TWO Generate Draft controls (Basic-branch + Advanced WizardCommitBand closeout) into master's SINGLE fullWidth Generate Draft (handleGenerate -> generateSettlement; anon-at-cap -> onSignIn). PRESERVE the generation_started analytics fire from a fresh store snapshot (mode/tier/culture/trade_route_access/monster_threat/magic_exists/forced_institution_count/excluded_institution_count/has_trade_overrides).
9. B4: Collapse the chrome variants onto master's ChangeModeBar + WizardLoadedBanners. Drop the wizardChromeDiet WizardChipRow variant; verify all three bindings survive: Basic<->Advanced switch (setWizardMode + setEntryPath, plus the null/Create-exit path), clear-loaded-config (clearLoadedFromSave), clear-neighbour (clearNeighbour).
10. B5: PRESERVE ANALYTICS MOUNTS — wizard_abandoned (GenerateWizard mount effect, pagehide/unmount when not generated) stays; wizard_step_viewed reconciled to the panel's emitter ONLY (per A2, ends the double-fire); help_popover_opened rides along inside ConfigurationPanel (HelpPopover, flag compendiumInlineHelp).
11. B6: RE-WIRE GUIDANCE MOUNTS — data-onboard-highlight on the LayeredConfigurationPanel wrapper @ onboardingStep===0 and on the Generate Draft button @ onboardingStep===1; onboarding auto-advance effects (config.settType leaves 'random' -> step 1; first settlement generated -> step 2). Preserve ClerkNote isolation + magic-off trade notes (they live inside ConfigurationPanel) and the WizardCloseout advanced recap.
12. B7: Re-derive the WizardCloseout mount condition — its old gate was wizardStep>=STEPS.length; with stepping removed, mount it on (isAdvanced && !settlement) so the read-only recap does not silently vanish (SYNTHESIS flag re: stale self-gates comment).
13. B8: Collapse the Layer-3 double-disclosure — TradeDynamicsPanel's own outer 'Step 4' disclosure nests inside master's DeepSection('trade'); flatten one and drop the legacy 'Step 4' label.
14. PHASE C — WIZARDEMPTYSTATE / HOMEHERO BASE-SET (RE-APPLY master's composition; place the hero move-list). C1 (IMPORT-BLOCKER, do FIRST): surface in src/components/theme.js `export const LANDING_MAX = L.LANDING_MAX` and `DANGER_BORDER` — both absent at tip and required by master's HomeHero/WizardEmptyState (HERO #4). GOLD_TXT + PageHeader already resolve.
15. C2: WizardEmptyState — restore master's composition: import LANDING_MAX + PageHeader; outer maxWidth=LANDING_MAX (move-list #11, was 860); !showHomeHero -> PageHeader(eyebrow/title/subtitle, move-list #6, replacing plain h2); showModePicker -> ONE parchment section ('Want full control?' + QUIET ModeSelector mode={undefined}, move-list #4).
16. C3: WizardEmptyState move-list removals: DROP InstantWorldEntry from this surface (move-list #3/#5, HERO #5); DELETE the anon sign-in banner 'unlock Basic & Advanced' (move-list #5, duplicates HomeHero sign-in).
17. C4: WizardEmptyState parent-contract change: drop props isMobile/wizardMode/authTier (master needs only showHomeHero/showModePicker/setWizardMode/onSignIn/onNavigate). Update the GenerateWizard empty-state call site IN LOCKSTEP or those props become dead.
18. C5: HomeHero — place THE GAUGE under the compact-hero law. Move-list #1 (headline reconciliation, HERO #1): the gauge must map `sizes` for anon (render ONLY Hamlet/Village/Town) OR render the six-station strip solely for signed-in — the all-six-to-anon behavior violates the owner law; the strip FORM is law-compatible.
19. C6: HomeHero move-list #2: collapse the two anon clerk's notes (generate.sizeHint.{picked} + generate.gauge.capMemo, HERO #2) to ONE visible line.
20. C7: HomeHero move-list #7 (HERO #6): inline heroV2 (promote v2 as the H1 anti-AI line; delete the flag branch + `flag` import + old eyebrow/title/antiAi fallback + old copy keys). Move-list #8: delete the signed-in 'Instant Generation' eyebrow.
21. C8: HomeHero move-list #9: anon-cap surface -> strip the inner gradient card, plain centered block, route copy through registry templates (hero.anonCap.spent/unlockTpl/signin) instead of hardcoded strings.
22. C9: HomeHero move-list #10: ADD the P10 first-click failure strip (beginError state + role=alert; needs DANGER_BORDER + swatch.dangerBg/danger from C1).
23. C10: HomeHero move-list #11: section maxWidth 720 -> LANDING_MAX; restore the rounded+shadowed plate; footnote + CTA sign-in raw <button> -> Button primitive (variant ghost/primary).
24. C11: Reconcile the pinning tests — e2e flow-a..d (button[data-settlement-size] still satisfied by SizeButton primitive); tests/ui/wizardEmptyState.proofPair.test.jsx, wizardEmptyStateReplay.test.jsx, wizard.smoke.test.jsx, mobile.smoke.test.jsx. ADD an anon assertion that thorp/city/metropolis are NOT rendered (the current gauge law-violation passes e2e silently, HERO #1).
25. FINAL: run the FULL suite (not focused gates — the organic-craft wave proved focused gates have a blind spot) after Phase C; confirm generatorGoldenMaster stays byte-identical (the whole reshuffle is asserted generation-neutral — no config default or config->generator mapping may change).
