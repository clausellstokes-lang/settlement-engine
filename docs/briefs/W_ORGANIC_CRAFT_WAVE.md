# THE ORGANIC CRAFT WAVE — reconciliation brief
## Fable 5 (manager), 2026-07-17. The law: docs/DESIGN_ORGANIC_CRAFT.md (w7-prep @ its commit). Research: wf_cd021516 + wf_65e6aacd (banked; journals in DOSSIER v2's recovery block). Dispatches AT THE DOC-WAVE FOLD off its tip. One Opus lane. Task #32.

## Phasing (commit-per-phase; the taste veto sits between 2 and 3)
1. **THE FOUNDATION**: tokens + primitives — the ink tonal ramp (shadow tokens retired),
   the rule family (hairline/single/double/swelled as SVG primitives), rubrication
   tokens, the fluid type/space scales (clamp; mobile-compressed ratios), subset fonts
   w/ metric-matched fallbacks + the font byte budget, container-query register
   primitives. Zero visual regression intended at this phase (values map 1:1 where
   possible) — byte-identical goldens; dist contracts green.
2. **THE SAMPLE SET** (the owner's taste-veto artifact): five screens re-composed under
   the law — the dossier (artifact register: manuscript grammar, marginalia, dropcap,
   seeded cartouche) · the library (instrument register) · one Compendium hub · the
   pricing page (post-doc-wave, re-checked under the law) · the mobile dossier in FIELD
   MODE (dim). Committed as rendered fixtures + live routes. **STOP. The manager runs
   the preview legibility review on the set; the owner vetoes taste. Only then:**
3. **THE APP-WIDE SWEEP**: surface-by-surface under the recorded keep-vs-stack +
   register rulings; bespoke form per surface (dossier ≠ gallery ≠ settings in
   composition; one type/color system); the one-fiction pass over every empty state,
   error, and loader; visible-authorship microcopy pass (one voice; signed/dated where
   the fiction earns it).
4. **THE MOBILE COMPANION**: the field-mode display state (dim warm theme, glance
   grammar, bottom-third verbs, wake lock where supported, interruption-persistent
   nav/scroll state via the existing store idioms) · keep-vs-stack executed per surface ·
   tap-gloss marginalia · visible bottom nav (3-5 + more) w/ safe-area insets ·
   **THE REALM GATE** per the law §7 (read-only render + capability-forward copy +
   deep-link/QR continuity). BUILD ON the existing isMobile layer (useIsMobile etc.) —
   extend, never greenfield.
5. **THE VERIFICATION RATCHET**: throttled mid-range budgets (TTI/INP) added to
   tests/build; the three-contrast state matrix pinned for ornamented controls; the
   seeded-ornament golden family minted (component-library changes = declared shifts).

## Rulings the lane inherits (decided; do not re-open)
- Registers on narrow screens: STACK with per-entry labels by default; horizontal
  scroll-in-container ONLY for genuinely tabular comparison surfaces. Record per surface.
- **OFFLINE IS OUT OF THIS WAVE** (owner-queued as its own charge): the service worker,
  "ready for the table" caching, iOS 7-day eviction mitigation, and snapshot-vs-migration
  staleness (the ghost-write class) are a separate engineering design — the field mode
  ships ONLINE-REQUIRED at launch; no offline promise appears in any copy (the
  worse-than-nothing precedent). ⚠️ The vendored /map/ webmanifest partial-scope
  install trap is RECORDED — do not extend it; do not add a manifest in this wave.
- Faux small caps banned; runtime turbulence filters banned; blend-modes on long
  registers banned (pre-bake).
- The doc wave's four pages re-check under the law at phase 3 but were BORN under its
  addendum — expect reconciliation, not rework.
- Fences at dispatch time: whatever lanes remain live then; the manager restates the
  fence list in the dispatch prompt.

## Phase 4b — THE TABLET COMPANION (owner amendment; law §10; sweep wf_3c80f28a feeds it)
The three-posture model lands (DESK/FIELD/THE SPREAD via pointer+hover+width+orientation,
extending the isMobile layer); tablet composed at its own size per surface (keep-vs-stack
gains a third column: SPREAD); **THE REALM ON TABLET** — pan/zoom/inspect first-class,
drag-placement w/ tap-then-confirm + snap assists, our chrome at 44-48px, FMG iframe
touch behavior VERIFIED EMPIRICALLY in the preview (never assumed; stop-and-report
genuine iframe touch defects as findings, never patch the vendored fork silently); the
flat-shared-surface mode for town map/fog honors the DM/player visibility split (a flat
tablet facing players shows ONLY player-visible state). Orientation = two compositions.
The preview review gains the tablet viewport, both orientations.

## THE SURFACE CENSUS (owner confirmation 2026-07-17: "every single page" — enforced by enumeration)
Phase 3 opens by ENUMERATING every route, view, panel, and modal in the app (the routes
registry + AppViews + the modal/panel inventory) into a committed census doc; every entry
is dispositioned — RECOMPOSED / INSTRUMENT-EXEMPT-with-reason / DEFERRED-with-reason —
and phases 3-4 close only when the census carries zero undispositioned entries. The
one-fiction pass (empty states, errors, loaders) rides the same census. No silent skips;
the preview legibility review walks the census, not a sample.

## THE DEPTH STANDARD (owner-ratified 2026-07-17 — the standing check on every craft fold)
The bar is not screenshot beauty; it is DEPTH, CONSISTENCY, VALIDATION, AND EXECUTION
ACROSS THE ENTIRE EXPERIENCE ("the best Fable can do" — the owner's framework, ledgered).
Every craft fold (and the manager's preview review) checks these columns explicitly:
1. WHOLE-EXPERIENCE COHERENCE — the census at zero undispositioned; empty/error/loading
   states, forms, account surfaces, and long-form content hold the same standard as the
   showcase pages; the site must not collapse past the front door.
2. SYSTEM, NOT INSTANCES — every visual decision lands as a documented token/primitive/
   rule (the law doc is the system documentation); no one-off styling that a future
   contributor cannot reproduce.
3. EVIDENCE, NOT VIBES — rejected alternatives recorded (the JUDGMENT discipline);
   contrast/targets/reflow verified per state, not per screenshot; the performance
   ratchets green on throttled mid-range devices.
4. CONTENT IS DESIGN — headlines, labels, CTAs, error messages, and onboarding copy are
   in scope for every recomposed surface (the voice ruling + copy-source guards apply).
5. THE HONEST GAPS STAY NAMED — real-user task validation is POST-LAUNCH OWED (every
   surface ships instrumented so judgment converts to evidence at contact); localization is STOPPED
   by owner order (2026-07-18): not built pre-launch; it rests on the post-launch
   roadmap by name (the copy registries keep the door structurally open).
The distinctive-identity test remains: authored-never-defaulted; a polished variation of
a familiar template FAILS this standard even if beautiful.

## Fold gates (in order, all mandatory)
Lane full gate (four validators, tsc + strict explicit, lint, suite, build+dist, closure)
→ the manager's PREVIEW LEGIBILITY REVIEW: desktop + mobile viewports + the dim pass,
every surface readable against its actual rendered ground, navigable, judged against the
$10,000 authored-never-defaulted bar, screenshots as receipts → the owner taste veto
(phase-2 gate). Legibility beats immersion in every collision.
