# TRAIN DISPATCH PACKETS — compiled 2026-08-30 by the chair (ODQ §775; trains ruled §773)

Purpose: every pending train starts HOT. Each packet = charter pointers, target receipts (re-verify line
numbers at the dispatch base — waves land nightly), PRE-DRAFTED at-dispatch rulings (all vetoable), hazards,
and bills. The STANDARD LAWS BLOCK applies to every train and is not repeated: own worktree + `npm ci` ·
explicit pathspec staging + `git diff --cached --name-only` before every commit · no amend (§750.2) · no
stash · package.json bytes = mint trigger (T1 carries the ONE chartered mint) · tombstones in the
walker-invisible register (§769.4) · decide in code, interpolate words (§774.1) · edge bundles from the
metas · read BOTH gate exits (§760.2, fired 3×) · prng flake law §753.2(a) · lighting census WHOLE at the
lane tip, actuals out of execution · new/deleted test files pay the three-census bill same-act (+ the
uncoveredBaseline integer, §754.4) · BUILD then hold; the chair sequences the ONE landing gate and CASes.

## T2 · COIN (W-COIN-2 → 3 → 4)
Authority: DESIGN_W_COIN.md + A1 (A1 outranks) · §763.2-Q10/Q11 · §768 · §771.
- W-COIN-2: the band chip (`empty|…|overflowing`, moved INTO this car by A1.21) + coin news beats
  (§754.3 register; `tax_receipt` joins the receipt-kind vocabulary WITH its emitter, §768.1) + the upkeep
  sink (re-animates the struck `treasury_shortfall`, §771.1) + ⭐ THE CONSERVATION WALKER IS BORN HERE
  (flows sum to zero across every treasury move — the new invariant class, planted control required).
  AFTER the chip lands: light `treasuryEnabled` in dramatic_campaign/living_realm/full_simulation (Q10).
- W-COIN-3: war sinks. W-COIN-4: view-plane only (relief/construction split OUT to an engine sub-car with
  the raw-byte bar, A1.21).
- PRE-RULED (widened Q4): the flow enumeration = the volume's roster as amended + §746.1's two new coin
  flows; enumerate at dispatch against the volume text, closed enum, no free keys.
- Hazard: pulseKernel is at a banked effective-lines ceiling — the 1a/1b join idiom (+0 lines) is the law.

## T3/T4 · SEAT-A (1..4) / SEAT-B (5..8)
Authority: DESIGN_W_SEAT.md + A1 + F1–F7 + A2 (§746.3 sealed end-to-end) · §744.1 (SEAT-2 splits 2a/2b/2c) ·
§745.2 (SEAT-7/8 after SEAT-2 + SEAT-4) · §744.2 (no dispatch until W-COIN-1a LANDS + the coverage table).
- Preconditions state: NAME-4's force vocabulary LANDED (§761); W-COIN-1a lands with the WCOIN train.
  The §744.2 coverage table + the D10/D11 census output (workflow `wf_7192952f-226`, a prior session)
  must be LOCATED at dispatch — if the census artifact is not in docs/, re-derive per §744.2 before car 1.
- Q-S2..Q-S8 + any volume Q-rows: the chair rules each AT dispatch reading the volume's own Q text —
  the packet deliberately does NOT pre-draft these blind. Compile step at dispatch: extract the Q rows
  verbatim into the brief with the chair's ruling under each.
- Hazard: highest integration density remaining (war+faith+power+coin on worldPulse). T7's unit hygiene
  is sequenced BEFORE T3 — do not reorder.

## T5 · GENESIS (POLIS-2 → ST-3 → VAR-1 → VAR-2 → POLIS-1 → SEAM-3 → ST-1)
Authority: DESIGN_FMG_WEAVE body→A1→A2 · R-WEAVE-READY rows · A1.2.6/.8/.12/.13.
- POLIS-2: reciprocal links BEFORE discovery + D6 provenance + the POLIS-5 observation harness (A1.2.8 —
  the harness only; POLIS-5's terminal fork is decided at its first-N-ticks readout, NOT by the lane).
  Anchors (at eba286607, re-verify): `dedupeWorldFactionNames` composeInstantWorld.js:251; staged-connections
  ~:265; insertion is STEP-RELATIVE (after dedupe, before discovery).
- ST-3: composer pipeline-as-data (D12), same lane 2nd car (A1.2.12).
- VAR-1: heightmap interpreter port (SF_TEMPLATES anchor worldPlan.js:78 via public/map/sf-bridge.js;
  `sfArchipelago` :90; MIT notice per volume §1.8; NEVER eager — dynamic import behind the composer;
  new files batch ONE lighting window, A1.2.13). VAR-2: versioned + dormant, absent-flag byte-identical.
- PRE-RULED Q-W3 (POLIS-1): **worldCode v2 ships; v1 codes REPLAY as v1 forever** (THE PROMISE posture —
  an old share-code is a starting world forever; the version byte selects the plan law). One declared
  plan-shift for v2 worlds only; culture sub-roster rides it (A2.1) if the volume's option row is clean.
- PRE-RULED Q-W1 (SEAM-3): **the provenance-stamped capture sidecar SHIPS** — pure additive capture,
  the CAP-1..4 sibling posture (versioned, sentinel-disciplined, absent = byte-identical).
- PRE-RULED Q-W5 (ST-1): **adopt A1.1.3 as written** — faith `origins` lineage field, no-backfill,
  raw-byte bar, virtual flag; enroll the flag in the VIRTUAL_DORMANT_WRITERS fourth door (§771).
- Hazard: everything here touches genesis — dormancy proofs per car on a representative seed, dark≡dark.

## T6 · REALM-NET (E-NET-1..3 → NET-2 → SEAM-4 → SEAM-5 → ST-4 advisory)
Authority: §770.3 (E-NET rows) · R-WEAVE-READY · A1.1.3/§1.4 (NET-2) · A1.2.7 (ST-4).
- E-NET-1: true-cost land-vs-sea mode selection; the magic 1.15 land bias NAMED as a constant; a declared
  shift measured on a real pack (before/after route sets quoted). E-NET-2: lakes vs ocean via captured
  `cells.f` (CAP landed it). E-NET-3: `MAX_ITER` exhaustion becomes counted + reported (silent road
  disappearance today; measured demand ≤16k vs 25k guard).
- PRE-RULED Q-W6 (NET-2): **the `ROUTE_GENESIS_LAW_VERSION` stamp is ADOPTED** — the stamp IS the car's
  persistence (§1.4/A1.1.3); lands DARK into the unmounted canon lane; the wiring wave decides activation.
- SEAM-4: test-only S4 comparison receipt (extends the roadNetworkMst family; new-file census bill).
  SEAM-5: "N foundings since mapped" on the gate CTA (`LivingWorldGates.jsx`; honest count in world words).
  ST-4: advisory half only; the calamity half defers UNLESS the lane prices its declared-shift row and the
  chair countersigns at collection (A1.2.7).

## T7 · HYGIENE (TE-UNITS-1 → TE-KERNEL-1 → TE-NPCGEN-1)
Authority: §759 (R-GEN-ASSESSMENT findings, receipts inside) · §763.2 · §766.2 · §774.1-E-HER-4.
- UNITS (minus the relabel → T8): the PACT-STRENGTH-ZERO cure (route `pactFormation.js:630` through
  `settlementStrength(item, buildPressureSummary(…))` like its two siblings — DECLARED SHIFT on pact
  outcomes, one record) · the shared PROSPERITY_RANK leaf (defenseScoreBands precedent; four consumers:
  corruption.js:495 / interiorModel.js:79 / townLayoutV2.js:433 / neighbourGenerator.js:136 — note the
  'modest'-vs-'Moderate' regex that can never match) · a DECLARED UNIT on `factions[].power` + the
  magnitude-sniff cliff cure (factionCompetition.js:127; share=1 reads as 1.0) · `legitimacyBand` →
  `legitimacyPressureBand` rename (the high=bad one, factionCompetition.js:622) · delete the dead numeric
  `magicLevel` write (neighbourGenerator.js:155) · the economicState VERSIONED-KEY cure (provisional vs
  reconciled read keys; closes the dataFlowContract SCOPE NOTE gap — the §711.6 class inside the pipeline).
- KERNEL: `generateSeed` hardened (per-ms counter or wider suffix; NEW-seed minting only — zero effect on
  existing worlds; retires the §753.2(a) flake class at cause) · the SIX `fnv1a32` copies consolidated onto
  the §761 zero-import leaf with `toBe` IDENTITY pins (roster in R-NAME1; the pdf/ornament copies feed
  goldens — pure re-home, byte-proof required).
- NPCGEN: decompose the 1,690-line relic; honest names (generateNPCGoal returns POWER — rename;
  generateNPCRelType returns the goal; generateCharacterTitle feeds plotHooks; pickLast returns a cultural
  title); split the ~438-line mergeNPCLists; BYTE-IDENTICAL goldens prove the whole refactor; the freeze
  ratchet RETIRES on completion · the 23-site toggle-key lookup gets ONE shared reader (12 in
  assembleInstitutions, 6 cascadeGenerator, 5 factionCorrelation) · the reroll-linkage drift investigated
  (regenNPCsPipeline generateSettlementPipeline.js:477-492 direct-match vs generatePopulation's 3-tier;
  unify = declared shift if outputs move) · E-HER-4's backwards siege band tokens renamed (reader prose
  already derives from pFall — rename is free, §774.1).

## T8 · SHIFT — the one golden window (CONTENT-1 → CONTENT-2 → relabel → sea_battle → NAME-2 → the metronome cure)
Authority: §759.5 · §763.2 · §773.1 · §774.2 · R-NAME1/R-NAME3 · one SHIFT RECORD PER CAUSE, one sitting.
- CONTENT-1: the D&D stratum out (GP prices, spell names, 'Zone of Truth' — institutionalCatalog +
  institutionServices), the newer literary voice in; SETTING-AGNOSTIC law is the bar; paid-surface content,
  every changed row quoted in the record.
- CONTENT-2: faction name pools widened past 4/category (powerData.js:8-63; the 6-per-category dedup extras
  become drawable); the fixed power-tab labels varied (rulingStructure.js:506-682) — same-seed NAME draws
  move; record.
- The priorityCategory relabel (~26 craft rows off 'government', §759.5): faction-support distributions
  shift; record. The dead culture-bias keys (Agriculture ×3) cure rides: fix the keys OR the vocabulary,
  and ADD the missing bias-vocabulary ratchet (cheap, §759.5).
- sea_battle: author the WAR_DESK pool (six siblings' register), retire §761's frozen named exception —
  the suite arm reds until the note is removed WITH the wiring; that is the designed mechanism.
- NAME-2: Markov module + 11 corpora + D3 resolver + successorNpc cure. PRE-RULED (the wards clause):
  `cartographyWards.js:128/:316` consumes NAMING_DATA and ARM B RETAINS it — NAME-2 keeps that consumer
  fed (compat export or direct), never orphans it.
- The §774.2 metronome cure: npcAgency:914 banded + the golden manifest re-record, SHIFT RECORD naming the
  suppressor cause (the ratchet note in proseNumerics.test.js already carries the written mechanism).

## T9 · GUARDS (+ GOLDEN's non-freezing half)
Authority: §759.4 · §763.2 · §764.2 · §768.3 · §769.4 · §771.2 · §774.3 · E-HER-9 · E3/E5 (§768).
- ENFORCER_DIRS gains `tests/generators` (97 invisible files enter the coverage contract) · planted mutants
  over the five uncovered seam contracts (pipelineContract + dataFlowContract FIRST — prove they can RED) ·
  instantWorld under the determinism eslint scopes + the mutation manifest · strict-mode-in-prod: MEASURE
  the runtime cost; if affordable it ships ON (§766.2) · the FIFTH-DOOR design (conditionally-written
  generator keys — typed registry vs one-off admissions, §771.2) · the prose-numerics RECEIPT-KEY detector
  class (the 8 not-prose rows, §774.3; governed re-mint ritual) · E-HER-9: a walker scanning reason-mints
  for tick-varying numerics (the suppressor-defeat class) · E3: the leaf-name collision walker · E5: the
  three timeout-flaky lint arms hardened · the §769.4 tombstone-register lint · **the §776 CURRENCY-AMOUNT
  detector class** (prose-numerics learns to convict absolute in-world prices on dossier/news/services
  surfaces, so the price-heuristics law can never regress silently) · GOLDEN non-freezing half:
  the 24+1 lit-uncovered mechanisms get flag-ON proofs; the dormancy oracle gains the BIT-LEVEL comparator
  arm (§713.2 standard) — the corpus WIDENING/freeze stays in T13's wake.

## T10 · PRODUCT-SURFACE (TE-UX-1 + the commerce car)
Authority: §767.2/.3 (the owner's post-forge ruling + the walk findings a–i) · §765.1 (WEB rows).
- UX-1: dossier lands HEAD-FIRST post-forge; the above-dossier "How this was simulated" panel is DELETED
  OUTRIGHT (§777 — redundant with the dossier's own in-card button, which stays); the three Keep-locks
  relocate to the settlement editor (controls, not receipts); the
  copy defects ("incursions.. ConsiderPalisade", "1 quarters"); the 33/40/39 count reconciliation; the
  "mosque or chapel" template hedge (generator PICKS; the culture-register question ruled at the car);
  REALM restored to the narrow-width nav on the core flow; chrome collisions (real-device pass); the
  unnamed-buttons a11y cure (nav + accordion get accessible names); (h)-class artifacts VERIFIED on a real
  browser first.
- Commerce: WEB-9a client arm (SURVEYOR_PLAN purchase/reconcile, lit by env; server half exists) ·
  WEB-10 (PRICE_MAP arm + the ANNUAL_FACTOR flip; two-way source scan is the acceptance) · WEB-11 annual
  client. STOP-class money-path discipline: smallest possible diffs, the webhook tests are the bar,
  nothing ships lit without the env key.
- THE ECONOMICS-HEURISTICS CAR (§776, owner law): the dossier's market/economics surfaces stop rendering
  ABSOLUTE in-world prices — relative-to-the-settlement's-own-baseline heuristics only ("the price of
  bread has doubled", dear/cheap bands, ±% vs the norm). CENSUS-FIRST: what `MarketPricesSection.jsx`
  renders and where its numbers originate (`EconomicsTab.jsx`, `economy.generated.js`, services price
  mentions); the baseline must be the settlement's own derived norm so every relative is honest
  derivation, never flavor. Goods QUANTITIES stay (facts); PRICES go relative (the DM's mandate).
  Any generated-prose/golden regen batches to T8's window.

## T11 · UI (post-T1: DESK-1..5 → POLIS-3 → POLIS-4 → VAR-3; W-ARMS may ride the tail)
Authority: A2 (DESK) · R-WEAVE-READY rows · §754.3 binds every rendered string.
- DESK-2 anchor `hoveredSettlementId` mapSlice.js:254 (setter :333). DESK-5's deep-link precedent pointed
  at DELETED `mapSubTabs.js:57` — mint the new `?realmview=` home, do not resurrect. POLIS-3: generalize
  `WarFaithMapOverlay.jsx` + `hopWeeks` travel rings (TravelersLayer.jsx:27 precedent); the region byte is
  GRANTED (§763.2). POLIS-4: D10 partition view MUST reuse spatialDigest's nearest-centroid helper
  (§732.2/A1.2.9); the placement arm lives scripts-side until green (A1.2.13). VAR-3: per-knob wizard pins
  (UI-state only). W-ARMS: heraldry with kinship inheritance; owes determinism/dormancy/test-bill rows at
  dispatch (A1.2.14).

## T12 · WAR-MEMORY (W-MEM: design → panel → build; + the granted region field)
Design skeleton (the arch lane answers THESE, then panels):
1. THE RECORD: `concludedWars` ledger — sorted-original-pair · casus roster (types + receipts) · opening
  tick (root attacker's sinceTick at fold time) · closing tick + outcome enum · participant roster with
  join provenance (joinLedger folds in) · named engagements roster (ids only). Append-only, immutable.
2. THE WRITER: the five deletion sites (warDeployment.js:375/:395/:402/:624/:875) become FOLD-INTO-LEDGER
  sites — one fold helper, single-writer.
3. LIFECYCLE (the owner's most-bitten class — trace ALL): accrues on EXISTING worlds during play ⇒ ships
  behind a virtual flag enrolled in the fourth door (§771); regen = a starting world has no concluded wars
  (empty, consistent with A1.7's posture); undo/import inherit worldState's existing machinery; NO pruning
  ever (lived history).
4. READERS: Remembrance register (the Herald door) · NAME-1's war names UPGRADE from pair-only degradation
  to full names post-war · rumor/chronicle references.
5. The Q-W4 region field (GRANTED §763.2): field battles persist `region` (settlement id, sentinel
  discipline) — rides this train; NAME-3's terrain derivation then extends to field battles.

## T13 · TRANS (design → skeptic panel → build; ALONE; the LAST same-seed mover)
Design skeleton (the arch lane answers THESE; the panel gets the fiercest skeptics, §764.2):
1. STRATEGY: replace engine `Math.sin/cos/exp/log/pow` at the 49 sites with OUR OWN deterministic
  implementations — pure IEEE754 add/mul/compare ops are bit-identical across engines BY SPEC, so a
  software implementation is engine-independent by construction (no cross-engine CI needed; prove the
  impl pure). Candidates per family: minimax polynomial vs table+interpolation vs fixed-point; choose per
  call-site tolerance (the design MEASURES each site's precision need first — some need 3 digits, none
  proven to need 15).
2. THE SHIFT: one wave, every golden re-records ONCE, the parent-side totality control per the house
  ritual; sequenced after every other same-seed mover (T7/T8 land first).
3. THE RATCHET RETIREMENT: transcendentalMathBaseline → 0 and the ban TIGHTENS to forbid all Math
  transcendentals in the four seeded trees permanently.
4. PERF: budget measured before/after on the hot paths (the fjord/bucket-index precedent: profile first).
5. THEN the GOLDEN corpus-freeze (T9's widened multi-seed corpus records against the post-TRANS bytes) —
  rides the OSR-mint window, not its own landing.

## NOT COMPILABLE FURTHER (honest residue)
T3/T4's Q-S rulings (need the volume's Q text at dispatch) · the §744.2 coverage-table location · HK-5's
re-scope (chair, at a collection) · the deferral-ledger sweep + CH-6b disposition (the residue stage's own
act) · the MEMORY.md index fold (chair housekeeping, §753 note) · literal build artifacts (nothing usefully
pre-compilable: lanes build their own node_modules from the shared npm cache; gates build fresh by design).
