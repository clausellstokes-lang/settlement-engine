# OWNER DOCKET — the single parking place (§683; law 11's named location)

Every owner-gated or owner-informational item, one place. The chair adds rows with
their §; the owner strikes rows as ruled. Nothing here blocks the arc unless marked.

**Provenance (§685):** seat **RECOVERED as Fable 5** for every row landed before §685;
rows added from §685 forward are Opus-chaired and carry their seat in the landing commit.

## DECISIONS AWAITING THE OWNER
- ✅ **RULED AND STRUCK (§695): the gestalt gate.** You returned the judgment (*"I leave everything to your best judgment"*), and the chair ruled it: `tier` collected-but-ungraded, `arc` moved to REG-D, bar and hard clause unchanged, escalation clause intact — **and the pass must be RE-EARNED by a fresh round, never re-scored from the old data.** Vetoable, as always.
- ✅ **STRUCK, NOT A REAL TRADE (§702.1): the page budget.** The reference **fits the settlement's diameter to the page's short side from the settlement radius alone**, excluding countryside from the calculation — settlement at **44 % of a 16:9 plate**, tier-invariant. Ours does **no framing at all** (fixed 1000×1000 viewBox, no crop, no fit) against a radius varying 20× in area, which is why we measured a range. It still generates countryside to 3× the frame radius, leaves **~89 % off-page**, and paints the rest with **zero ink**. ⭐ The author added fields a year *after* the tight framing, *to reduce empty space*. **So L-REG-33 is not on the scale at all** — the land stays generated and true, it just stops being painted at full strength. Now a chair repair with its shift declared.
- ✅ **STRUCK (§702.2): the rotation dial was the WRONG lever.** My hypothesis that we lack the reference's angle-zeroing rule is **refuted — we already implement it exactly** (same coupling, ±15°, 0.8 window, suppression below 4× stop area), so the deficit cannot come from the subdivision and this signature would have bought nothing. Their diversity comes from **block orientations**; they turn intra-block chaos *down*. Likely cause instead: our grid-chaos base is plan-mode-set at 0.12/0.34/0.58 vs their uniform 0.5–0.7, and I measured our two most orderly modes. A scoped test replaces it — no signature needed.
- ⚠ **INFORMATIONAL (§702.3): the aggregation guard.** "Do not raise" is confirmed **at source**. And the reference gets its apparent mass count by **paint order** — stroke a group at double weight, then fill over it, so interior seams vanish and one silhouette survives — **free, and it does not touch the plot bijection.** No decision owed.
- ⛔⛔ **TWO GEOMETRY SIGNATURES, AND THEY ARE *NOT* ONE CURE (§699 — I corrected this after adversarial verification; the earlier row overstated it): the WRAP RING crosses water on 11 wraps / 7 partitions** (worst `city`/`migration` at **305.0 units, 17.1 % of the ring**) **and the `metropolis` E1 NEEDLE** (min-rect 711 × 31, aspect 22.79). The *drawn* wall is cured; the *ring* is not. The proposed single cure — `builtUmbrella`'s morphological closing, a law the estate owns and the spine's wrap path never inherited — **did not survive the skeptic**, so treat these as two signatures that may share a cure, not one that provably does. Both move every wrap in the corpus.
- ⛔ **TWO SMALLER GEOMETRY SIGNATURES (§699):** **`growthLedger`'s circuit provenance** — measured under mutation, **15 of 17 wraps flip to `recorded` and 16 of 17 rows move geometry** (town gates 3→1, city 5→1, metropolis faces 3,962→4,064). And **the three raise years** — ⚠ **but its stated one-line cure is INERT**: the named locus has `settlement.record` ABSENT on 18/18 leaves and moves zero rows; a positive control proves the instrument live and moves nine frozen radii, so the real seam is `wallVintage.ageAtBuild`. **Do not act on the cure as written.**
- ⚠ **INFORMATIONAL, no longer blocking (§695.8): the wall band is UNDER-WIDTH on elongated rings** — 5 of 17 wraps, worst 0.212 of declared, `shrinkRing` displacing radially. Now taken as a **repair with a declared shift** under your grant, chartered as its own car AFTER SPINE-3 seals (a running lane's boundaries are not changed mid-flight). It moves every band in the corpus, so **the tuning pass must see it** — flagged here for that reason, not for a decision.
- ⛔ **THREE GRANT READINGS ARE YOURS TO SETTLE, NOT FABLE'S (§693.6): §22, §336, §362.** Each is a chair converting ONE owner sentence into standing authority under a carve-out list the chair drew itself, and all three have unknown provenance. **§336's reading confirms or unwinds five landed WEB cars in one paragraph.** Only you can say what your own sentence meant.
- **F1 — a DM-facing surface prints a raw engine token** (§693.7(i); `relationshipRuleHelpers.js`, found 2026-08-04, still live). Owner-gated because closing it **moves same-seed hashes**. Close it, or defer explicitly with the shift declared — 21 days unruled either way.
- Marketing/website 432-file set: name its home branch (pinned safe at tree-triage-pin-2026-08-25; §659.3).
- The 33 staged docs/DESIGN_FP_ARCH_* edits (2026-08-17): yours or strike (§659.3).
- Formal GPL clean-room comfort via counsel (§668/§673 — the conservative discipline binds meanwhile).
- R-MORPH dossier taste gate (§642.2 — consumption stays weights-grade until gated).
- The stale-disk main-checkout sync (deferred §659.3 until the two rows above clear).
- Naming veto scope: does §616's veto extend to shipped generated landmark names (spatialGenerator "Tannery Row" etc., §625.2 R2)?

- ⛔⛔ **INFORMATIONAL-CRITICAL (§705): THE TRAIN CARRIES A 100 % DETERMINISTIC MAP DEFECT — know it before the deploy button, not after.** ✅ **Nothing is wrong on the live site**: `master` (unmoved 51 days, the only branch CI deploys) carries **no town map at all**. But on the build branch, **every walled coastal settlement draws 689 units of wall — 69 % of the page width — out into the sea, 130 units past the shore**, with zero variance (1,440 of 1,440). On the sold PDF plate that is **311 pt of wall over water, four fifths of an inch deep on a 6.3-inch page**. ⚠ **Your existing saved settlements are the first audience**: an absent layout marker means the older law, which is the 100 % case. A second, cheaper defect rides with it — three renderers close the coastline to the page bottom, **assuming a southern shore**, so a northern shore draws the whole town underwater. **It does not block the deploy; the cure is two cars and moves live wall geometry (every golden containing a walled coastal settlement re-records).**
## INFORMATIONAL (no decision owed yet)
- B0/B3 dwellings-band basis re-verifies on WAY-edge frontage at DW dispatch — you will be informed before consumption (§677).
- F1 marketing find: the category leader's saved-cities-change-under-updates wound is THE PROMISE's provable answer (§679.2).
- Wear thresholds drawn at chair-signed provisionals; you re-sign at tuning (§683 PA.4).
- The stop-rule band constants: provisional; you re-sign at tuning (§680.3/§681.3).

## YOURS BY NATURE AT THE ARC'S END (the standing carve-outs)
The cutover flag (REG-P) · every push/deploy · /code-review ultra · the film
specimen verdict (PA.8) · the tuning signature over SIGNED_CONSTANTS (LAST) ·
legal · the cull · the Cohort A reverse-image search.
