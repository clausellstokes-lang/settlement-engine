LANE R2 REPORT — STRIP-4 CONSUMER CENSUS at build tip 1a2471990 (read-only; repo /Users/cstokes/Desktop/settlement-engine; all paths repo-relative, all receipts from `git show`/`git grep` at that tip)

METHOD + DENOMINATORS. 105 files in scope (91 `src/domain/townMap/**` + 14 `src/domain/townCartography/**`). Full import graph extracted at the tip over src/scripts/tests/api/tools (21,346 static-import lines + 2,169 dynamic-`import()`/`new URL` lines, 3,906 importing files), then split into RUNTIME edges vs JSDoc TYPE-ONLY edges — the split matters: the first pass wrongly "retained" townMapDraw/groundDress through `massing.js:44` `/** @typedef {import('./townMapDraw.js').DrawOp} */`, a type-only edge. A second extraction bug (multiline `} from '…'` tails dropped) was caught by a negative control (cartographyDefenses "unreached" while cartographySynthesis:52-56 visibly imports it) and fixed. api/ has zero townMap references. All classifications below are CONFIRMED by mechanical graph walk + quoted receipts unless marked PLAUSIBLE.

== A. RETAINED SUBSTRATE — 25 of 105 (delete NONE of these) ==

Seam receipts, verified at THIS tip:
- S1 canonize: `src/lib/spatialSubstrateDerive.js:26` `import { buildTownMapModel } from '../domain/townMap/townMapModel.js'`, `:27` `import { readMapEdits } from '../domain/townMap/mapEdits.js'` (static, not dynamic — the charter's "dynamic import at the canonize seam" wording is stale; the edge is real either way).
- S2 engine readers: `ladderRead.js` ← PowerTab.jsx:8 (live UI), rulingPowerCoup.js:23, worldPulse/roadsKernel.js:77, worldPulse/traditionsKernel.js:69; `fabricRead.js` ← mapDress.js:39 + retained internals.
- S3 interiors: interiorFootprint.js:27 (buildTownMapModel), :28 (readMapEdits), :29 (anchorForInstitution), :107 (type-cast use); interiorExport.js:39 + interiorModel.js:37 (anchors).
- S4 townScene (STAYS per Q6′): compileTownSceneManifest.js:15-18 (audienceProjection, mapEdits `readSceneOverrides`, townMapModel, **cartographySynthesis `compileTownCartography`**); sceneLiving.js:12-13 (audienceProjection, mapDress); buildingProfiles.js:11-12 (glyphAssign `glyphKindFor`, massing `silhouetteForKind`); sceneGeometryTemplates.js:9 (massing); customBuildingPresentation.js:18 (glyphAssign); manifestRecordValidation.js:11 + sceneProjection.js:15 (mapEdits). Reverse edge: townMapModel.js:42 imports townScene/customBuildingPresentation (both retained — fine).

Roster (14 townMap): anchors, asymmetrySources, audienceProjection, fabricRead, glyphAssign, institutionAssignment, ladderRead, lynchRubric, mapDress, mapEdits, massing, siteGenesis, townLayoutV2, townMapModel. (11 townCartography): cartographyBuildings, cartographyDefenses, cartographyField, cartographyMorphology, cartographyMultiplicity, cartographyParcels, cartographyPlan, cartographySkeleton, cartographySynthesis, cartographyTuning, cartographyWards.

⚠ CHARTER CORRECTION (the census's biggest finding): STRIP-4's roster says "`townCartography/` (14 files)" go. **11 of 14 are runtime-load-bearing for the RETAINED townScene compiler** via compileTownSceneManifest.js:18 → compileTownCartography → the cartography web (and cartographySynthesis.js:48-49 imports back from townScene cartographyContract/stableScene). Deleting them breaks the 3D scene build. Fork to rule before dispatch (flag, not deciding — owner/chair): ARM A — sever the scene's cartography attachment (edit compileTownSceneManifest + cartographyContract; a townScene behavior shift: scene manifest digests/goldens move — townSceneCartography.test.js, townSceneManifest.test.js — must be declared per the honesty law), then all 14 delete; ARM B — the 11 stay as scene substrate and only the Paint trio goes. Note the charter itself expects `cartographyParcels` to survive to DW dispatch, which leans ARM B for at least parcels.

- mapEdits (J-STRIP3-A verified at THIS tip): the ruled consumer set holds exactly — spatialSubstrateDerive.js:27 ✓, interiorFootprint.js:28 ✓, three townScene modules = compileTownSceneManifest.js:16, manifestRecordValidation.js:11, sceneProjection.js:15 ✓. PLUS four UI files still import it (STRIP-1/3 residue): StyleOverhaulPanel.jsx:115, SaveToLibraryButton.jsx:41+93, BuyThisDossier.jsx:158, SettlementsPanel.jsx:166. AND mapEdits.js:37 imports `coerceStyleId, DEFAULT_STYLE_ID` from `src/design/townMapStyles.js` — so the styles registry cannot be deleted before that import is re-homed/trimmed. Disposition: RETAINED, TRIM (never delete).

== B. PRESENTATION — 28 files, deletable in STRIP-4 after the blockers below ==

townMap top-level (8): **index.js (the barrel)**, townMapDraw.js, townPanorama.js, groundDress.js, bespokeStyles.js, changeView.js, fogGeometry.js, fogSessions.js. fabric/ (20): boundaryArrangement, building, content, coordinateAbi, dcel, dcelEmbedding, exactGeometry, fabricRoot, foundation, frontage, index, massingProjection, massingRoster, operations, parcelRegistry, projection, settlementFoundation, shapes, streetGeometry, streetGraph.

Every runtime path into this set at the tip goes through exactly five doors:
1. `src/components/surveyor/StyleOverhaulPanel.jsx` — dynamic imports of the barrel at :62 (TOWN_MAP_STYLE_IDS) and :97 (buildTownMapModel/buildTownMapSvg/hasDrawableMap — renders a live 300×300 legacy-map preview), bespokeStyles :114, mapEdits :115. ⚠ FINDING F1: this is a user-visible pixel of the legacy map still standing after STRIP-1, contradicting J-STRIP-1, and **no strip wave charters this panel**. Its disposition (delete vs strip its map half; incl. `applyMapEdit`, operationRegistry.js:137) must be ruled before the barrel can go.
2. The PDF chain (STRIP-2): SettlementsPanel → SettlementDetail → utils/generateSettlementPDF.js:170 → src/pdf/SettlementPDF.jsx:46 → barrel; also TownMapDocument.jsx:29, sections/TownMapPlate.jsx:24, utils/townMapPdfExport.js.
3. Export libs (STRIP-2): src/lib/townMapExport.js:52-58 (barrel, fogGeometry, audienceProjection), townMapThumb.js:29, realmMapExport.js:34 (town half).
4. ⚠ RETAINED-surface blocker: `src/domain/realmMap/realmPlateRenderer.js:49` `import { drawListToSvg } from '../townMap/townMapDraw.js'` — the realm surface STAYS; townMapDraw cannot be deleted until drawListToSvg is re-homed (+ its test realmPlateRenderer.test.js re-pointed). townMapDraw's own runtime imports then drag groundDress, glyphAssign, design/townGlyphs, design/townMapStyles, design/townMapExportPalette.
5. ⚠ RETAINED-surface blocker, twice: `downloadBlob` from townMapExport.js is imported by retained `src/lib/townScene/townSceneExport.js:18` (static) and live `src/components/map/TimelapsePanel.jsx:66` (dynamic; mounted via RealmInspector.jsx:74 — world map, stays). Re-home downloadBlob before STRIP-2 deletes townMapExport, or the retained townScene/world surfaces break.

Design-registry couplings (STRIP-4's "styles/glyph registries"): townMapDraw.js:51 + townPanorama.js:31 import design/townGlyphs (compileGlyph/compileGlyphFacade — die with the draw stack), BUT retained massing.js:42 (and presentation groundDress.js:36) import `SHADOW_DIR` from design/townGlyphs/glyphCompiler.js — re-home SHADOW_DIR before deleting the glyph registry. glyphCompiler's other townMapDraw references (:50, :133 area) are JSDoc TYPE-ONLY. massing.js:44 typedef → townMapDraw must be re-pointed at deletion (typecheck, not runtime).

== C. ORPHANED ALREADY — 16 files (zero src/scripts runtime reachers; tests only) ==

townCartography: cartographyPaint.js, cartographyPaintRoles.js, cartographyProperty.js (their UI consumer `useTownCartographyBlock` died in STRIP-1; note `src/lib/townScene/townCartographyBlock.js` is itself orphaned now — only tests/lib/townCartographyBlock.test.js:20 imports it). townMap: ageOverlay.js, sceneOverrideOrphans.js. fabric/: architecturalHistory, boundaryNoder, exactIntersectionArea, fabricRng, massPart, solidLegality, spatialReceipt, stageManifest, supportSurface, vegetation. arch/shapeRegistry.js has **zero importers anywhere, including tests** — dead today. (Sibling STRIP-1 residue, out of scope but adjacent: `src/components/townMap/edgeAnnotations.js` — sole survivor of the 56-file delete, zero importers.)

== D. ARCH — 36 files, NOT STRIP-4 SCOPE (chartered-dark kernel) ==

`townMap/arch/**` is the K-1 building kernel: tests/build/archKernelLazy.test.js declares "The whole K-1 arch/ kernel is DARK: imported by NOTHING shipped" — CONFIRMED by graph (zero src importers; consumers are scripts/generate-k0-spike|k0b|k1|k2|k3|k4.mjs + 19 test files in tests/architecture, design/archMeshBudget, lint/archDriftTotality+archLodLadder walkers). It is not legacy-map presentation and not in the STRIP-4 roster; deleting it is a NEW decision, not a strip. Flag: it lives under townMap/ — re-home candidate alongside the ladderRead/fabricRead re-home the charter already assigns to the sweep wave; and STRIP-6's "map scripts" sweep must not treat generate-k*.mjs as map scripts.

== E. DELETION ORDER (never breaks retained substrate) ==

0. PRE-SEVERS (small re-homes, can ride STRIP-2): re-home `downloadBlob` (2 retained consumers above); re-home `drawListToSvg` for realmPlateRenderer; re-home `SHADOW_DIR` for massing; re-home/inline `coerceStyleId`+`DEFAULT_STYLE_ID` for mapEdits; re-point massing.js:44 typedef.
1. Rule F1 (StyleOverhaulPanel) and land STRIP-2 (PDF chain + export libs). After these, the barrel's src importer count is ZERO.
2. Rule the townScene-cartography fork (ARM A severs compileTownSceneManifest.js:18 + cartographyContract attachment with declared golden/digest shift; ARM B keeps the 11).
3. One act: delete the 28 presentation files + design/townGlyphs + design/townMapStyles + design/townMapStyleWall + design/townMapExportPalette (+ townCartography per the fork arm), with the removal-mirror census.
4. Orphan sweep: the 16 files of §C (+ townCartographyBlock.js).
5. Same-act re-freezes per lane law: test:ratchet (remove-only), size-baseline deletions, typecheck floors, observed-shape, dist chunk census, surveyorPanelsLazy (if the panel dies), and CI's `validate:map` step disposition.

== F. TESTS/CENSUSES EACH DELETION MOVES ==

Presentation delete moves 68 direct test files (tests/domain/townMap{Draw,Fog,GroundDress,Fabric*,Street*,Massing*,Model,Styles,SkinRegistry,SkinReskin,ChangeView,…}, tests/property townMap*Determinism + townMapGolden/townMapStyleGolden/townMapPanoramaGolden/illustratedTownGolden/townMapV2Golden, tests/design/townMapOpBudget+townGlyphSetRegistry+contrast(partial — TRIM), tests/pdf/townMapDocument.smoke, tests/lib/townMapFogExport+mapLayerAnalytics(partial), tests/security/townMapGalleryOptIn, 2 tests/fixtures). Ten of those ALSO import retained files and must be TRIMMED, not deleted: styleOverhaulCompile, townLayoutV2, townMapEdits, townMapIllustratedLens, townMapModel, townMapSeasonDress, townMapSkinRegistry, townMapStyles, townPanorama, townMapV2Golden. Orphan sweep moves 19 test files (incl. tests/lint/townMapStageManifest.walker). Retained set keeps 36 test files (powerTabLadder, ladderRead, spatialSubstrate/spatialConsequence suites, the 9 townCartography suites + calibration corpus, townSceneCartography/Manifest, townMapGlyphAssign, townMapMassingSilhouette.walker, security/townMapPlayerProjection, siteCoherenceRatchet) — these must stay green through every step. ARM A of the fork additionally moves the townScene manifest/cartography goldens. Repo-wide at every act: the three-census cost of test-file removal (ratchet/censuses), size ratchets, and the post-gate `git status` check (§747.4).

Scratch artifacts (graph, closures, receipts): /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/{edges_rt.json,recv.json,report.json,graph3.py}.