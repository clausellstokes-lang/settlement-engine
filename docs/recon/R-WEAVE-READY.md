LANE R-WEAVE-READY REPORT — dispatch-readiness matrix for the FMG WEAVE cars (2026-08-29/30 recon; read-only Fable survey seat)

BASE SHAS. Code facts: BUILD tip **`eba286607`** (`git grep -n <pat> eba286607 -- <path>` / `git show eba286607:<path>` throughout — the R-STRIP4-CENSUS method). Docs/ledger facts: HEAD **`8f208c46e`** (ledger branch). The volume (`docs/DESIGN_FMG_WEAVE.md`, deleted on stale disk) was read WHOLE via the HEAD blob — 38,394 bytes, sha1 `cf8486312a…`, verified byte-identical to the 5a850cca scratch copy. ODQ §724–§750 read in full at HEAD; STRIP charters from `docs/DESIGN_MAP_MODULE_SPLIT.md` §11.2/§11.4/§11.5 at HEAD; STRIP-4's surface from `docs/recon/R-STRIP4-CENSUS.md` (its receipts are at `1a2471990`; every one I reuse below was re-taken at `eba286607`).

METHOD + DENOMINATORS. Authority order honored: volume BODY → A1 (outranks body) → A2 → A3 → later ODQ rulings (§731 grants, §737 W-COIN A1, §739/§740 DW/undercity deferral, §744/§746 W-SEAT seals, §747–§750 landings). Every membership claim below carries a two-pass grep at `eba286607` (exact name + alias/idiom sweep). Negative-membership sweep (unbuilt-car proof): `CAPTURE_SIDECAR_VERSION|[Uu]rquhart|ROUTE_GENESIS_LAW_VERSION|markovNaming|climateTruthEnabled|foreignSeatOf|deriveForceComposition|legitimateRemnantOf` = **0 hits in src/ at the tip** (each term searched; SEAM-3, NET-1/2, NAME-2/4-adjacent, CAP-3, W-SEAT surfaces all unbuilt — CONFIRMED). Landed-surface sweep: `terrainAgreement` (8 src hits, deriver at `spatialCost.js:255`), `cell_remapped` (`spatialPackCapture.js:221`), `flagGeographyDiverged` (9 hits incl. `operationRegistry.js:487` + the 68→69 raise note at `:570`) — SEAM-0/1/2 landed, CONFIRMED. Label discipline: CONFIRMED = executed git evidence at the stated sha; PLAUSIBLE = reasoning from the ledger only.

GLANCE LINE. **Ten cars/lanes can build TODAY in parallel worktrees** (CAP-1..4 · ST-2 · POLIS-2→ST-3 · NAME-1 · NAME-4 · VAR-1 · SEAM-4 · SEAM-5 · ST-4 advisory half · NET-1 build-only); **eight are owner-gated** (SEAM-3 · NET-2 · POLIS-1 · ST-1 · W-COIN-1a+ · Q-W7 export · plus the Q-W4 half of POLIS-3 and ST-4's calamity half's declared-shift discipline); **everything UI-visible waits for STRIP-6** (POLIS-3/4 · VAR-3 · DESK-1..5); **NAME-2 waits for STRIP-4 and carries a charter correction** (the wards consumer it assumed dead is RETAINED under ARM B); **W-SEAT waits behind W-COIN-1a**, which waits behind ST-2 + Q10/Q11.

== §1 · LANDED BASELINE (what is already on the branch at `eba286607`) ==

- **SEAM-0 — DONE (verdict car).** The instant-world canonize lockout was REFUTED and replaced by something worse: `Number(null)===0` admitted every unplaced settlement as cell 0 → false canon (§747.2). CONFIRMED landed: the cure below exists at tip.
- **SEAM-1 — DONE.** `terrainAgreement` deriver at `spatialCost.js:255`, exported via `spatial/index.js:28`, consumed at `spatialDigest.js:368` and `useMapBridge.js:83`; the S1 divergence flag `flagGeographyDiverged` at `mapSlice.js:287` with its registry row `operationRegistry.js:487` (session-only, never persisted) and the §747.3 owner-flagged EXEMPT_CEILING 68→69 raise documented at `operationRegistry.js:570`. CONFIRMED.
- **SEAM-2 — DONE.** Capture-time re-resolution with `cell_remapped` receipt rows at `spatialPackCapture.js:221` (doc block :175 pins the seed-cell law). The shared nearest-centroid helper the A1.2.9 ruling demands is homed per §732.2 in `spatialDigest.js` — POLIS-4 must consume THIS helper, not mint a second. CONFIRMED landed (9 commits, 18 files, zero new files — §749.1).
- **W-COIN-0 — DONE (design).** `docs/DESIGN_W_COIN.md` sealed with its own A1 (§737.1); dispatch brief `docs/briefs/WCOIN-1a-1b-BRIEF.md` on the ledger branch (§749.3). No implementation car may dispatch until Q10/Q11 (§737.1).
- **W-SEAT architecture — DONE (design).** `docs/DESIGN_W_SEAT.md` + A1 + F1–F7 + A2 sealed end to end (§746.3). No implementation until W-COIN-1a lands + coverage table + Q-rows (§744.2).
- **STRIP-1 + STRIP-3 — LANDED** at `1a2471990` (§747.1). **WSEAM — LANDED** at `eba286607` (§749.1). **STRIP-2 — IN FLIGHT** (TE-STRIP-2 checkpointed clean at `4ccbfe7d6` on its worktree, 2 of ~7 cars done, rebase target `eba286607` recorded but not executed — §750.1).

== §2 · THE OPEN GATES (owner rows that bind cars) ==

Open per §746.3 + §731: **Q-W1** (SEAM-3 sidecar) · **Q-W2** (persisted route names — display-side is the default, NOT gated) · **Q-W3 widened** (worldCode v2 + pre-shift v1 replay ruling — POLIS-1) · **Q-W4** (field-battle `region` field — POLIS-3's one engine byte; NAME-3 must determine derivability before requesting this family) · **Q-W5** (ST-1 faith origins shape) · **Q-W6** (NET-2's `ROUTE_GENESIS_LAW_VERSION` ledger stamp) · **Q-W7** (typed table export, paid surface) · **Q-W9** (DM battle theater — REC defer) · **Q10/Q11** (W-COIN lighting + mandate reading) · **Q-S2..Q-S8** (W-SEAT batch) · **widened Q4** (W-COIN flow enumeration, grown by §746.1's two new coin flows). GRANTED already: **Q-W8** (§731.1 — W-COIN chartered) and **Q-S1** (§731.1 — consumed by STRIP-3, landed).

== §3 · THE MATRIX ==

Verdict key: **NOW** = dispatchable today in a parallel build worktree; **AFTER-x** = dispatchable once x lands (building earlier in a worktree is legal where noted; GATES SERIALIZE); **GATED** = an open owner row blocks the car itself; **HELD** = volume/ledger holds it behind a non-owner condition. Ordering-law citations are volume §4 as amended by A1.2/A2/§737/§739.

| Car | Scope (one line) | Gated by | Ordering law | STRIP collision | Verdict + strongest receipt |
|---|---|---|---|---|---|
| SEAM-0 | Lockout verdict | — | — | — | **LANDED** (§747.2) |
| SEAM-1 | Divergence flag + terrainAgreement | — | — | — | **LANDED** (`spatialCost.js:255`) |
| SEAM-2 | Capture-time cell re-resolution | — | — | — | **LANDED** (`spatialPackCapture.js:221`) |
| SEAM-3 | Provenance-stamped capture sidecar | **Q-W1** | SEAM-3→CAP-1 or CAP-1-first-widened (A1.2.10) | none | **GATED** — `CAPTURE_SIDECAR_VERSION` 0 hits at tip |
| SEAM-4 | S4 comparison receipt (test-only) | UNGATED | home named at dispatch (A1.2.13); new test file = 3-census cost | none | **NOW** — extends `tests/lib/roadNetworkMst.test.js` family (3 existing roadNetwork suites at tip) |
| SEAM-5 | "N foundings since mapped" on the gate CTA | UNGATED | §729.4 dispatch-now list | none | **NOW** — CTA home `src/components/settlements/LivingWorldGates.jsx` exists at tip |
| CAP-1 | D1 single capture-extension act (`fl`, `temp`+`prec`+`g`) | UNGATED | may precede SEAM-3 minting the widened sidecar shape (A1.2.10); after SEAM-2 ✅ | none | **NOW** — capture home `src/lib/spatialPackCapture.js`; normalize in `spatial/index.js` |
| CAP-2 | River navigability banding, `SEA_LANE_VERSION` 1→2 | UNGATED | after CAP-1 (`fl`) | none | **NOW (same lane, 2nd car)** — `derivePortEligibility` at `seaLanes.js:224` |
| CAP-3 | Climate band sub-digest + `climateTruthEnabled` | UNGATED | after CAP-1; raw-byte arm beside seasons oracle (A1.2.7) | none | **NOW (same lane)** — flag idiom precedent `campaignSpatialCanonize.js:84` (`biomeTruthEnabled === true`) |
| CAP-4 | Lake typology via D2 shared flood-fill | UNGATED | after CAP-1; reads CAPTURED inputs only, subtype `unknown` w/o climate (A1.2.14) — CAP-3 not required | none | **NOW (same lane)** — D2 target `waterComponents` still private at `seaLanes.js:288` |
| NAME-1 | War-name + route-name RENDER-time derivers (A1.1.1 re-ruling) | UNGATED (Q-W2 only for the persisted branch) | before DW-7 — **vacuous at launch** (DW module-side, §739.1) | none | **NOW** — display-leaf home beside `src/domain/display/warStatus.js`; token precedent `{payer}` at `src/data/roadsProse.js:223`; no `{road}` token exists yet (0 registry hits) |
| NAME-2 | Markov module + 11 corpora + D3 resolver + successorNpc cure | UNGATED | **after STRIP-4** (§4.2) | STRIP-4 — see charter correction §5(b) | **AFTER-STRIP-4** + re-rule the wards clause first — `cartographyWards.js:128/:316` still consumes NAMING_DATA and ARM B RETAINS it |
| NAME-3 | Engagement-narrative deriver + battle-kind taxonomy (A2) | UNGATED (Q-W4-family grant only if terrain-class underivable) | display-leaf family; NAME-4 vocabulary seal precedes SEAT-7/8 narration (§746.1) | **STRIP-2** — shares the `src/pdf` surface | **AFTER-STRIP-2** (build now legal) — PDF wiring CONFIRMED at `src/pdf/lib/liveWorld.js:63-65` (imports warStatus + armyStrength), the exact chain STRIP-2's PDF car is editing |
| NAME-4 | Typed force-composition clerk (A2) | UNGATED | §746.2 binds: composition derived FROM strength, never feeds it; its vocabulary seal is SEAT-7/8's precondition | none | **NOW** — `deriveForceComposition` 0 hits at tip (unbuilt); landing early unblocks W-SEAT narration later |
| NET-1 | Render-tier Urquhart + corridor-reuse + haven-exit + seaCost | UNGATED | **export-visible half after STRIP-2** (A1.2.14 — one parity-golden re-record) | **STRIP-2** — `realmMapExport.js:34` imports `downloadBlob` from `townMapExport.js`, the file STRIP-2 car 3/4 re-homes/deletes | **BUILD-NOW / LAND-AFTER-STRIP-2** — render homes `src/lib/roadNetwork.js` (`computeRoadEdges`), `RoadsLayer.jsx:15`, `WorldMap.jsx:273/:542` (`route_count`); 0 `urquhart` hits (unbuilt) |
| NET-2 | Canon-tier Urquhart 4th candidate key + version stamp | **Q-W6** | lands DARK into the unmounted lane; wiring wave (`src/lib/spatialUsage.js`) decides activation | none | **GATED** — `ROUTE_GENESIS_LAW_VERSION` 0 hits; the stamp IS the car's persistence (§1.4, A1.1.3) |
| POLIS-1 | D5 plan car: genesis diplomacy + jitter + sentinels + fingerprint ext (+A2 culture sub-roster option) | **Q-W3 widened** (dispatches on EITHER answer, but an answer is required — A1.2.6; excluded from §729.4's ungated list) | ONE declared plan shift; culture sub-roster rides it or defers (A2.1) | none | **GATED** — plan home `src/domain/instantWorld/worldPlan.js`; fingerprint already carries site x/y (`composeInstantWorld.js:361ff` maps `st.x, st.y`) per A1.2.5 |
| POLIS-2 | Materialization half: reciprocal links before discovery, D6 provenance + POLIS-5's observation harness (A1.2.8) | UNGATED | **before ST-3** (A1.2.12); insertion step-relative: after dedupe, before discovery | none | **NOW** — both anchors CONFIRMED: `dedupeWorldFactionNames(settlements)` at `composeInstantWorld.js:251`, staged-connections discovery comment at ~:265 |
| POLIS-3 | D9 overlay registry (+ A2 hopWeeks travel rings) | Q-W4 **only** for the field-battle `region` byte (base car ungated) | **after STRIP-6** (§4.2); rings behind entitlement gates ⇒ effectively also after STRIP-5 | STRIP-5 (entitlement gates), STRIP-6 (UI window) | **AFTER-STRIP-6** — generalization target `src/components/map/WarFaithMapOverlay.jsx` exists; rings precedent `TravelersLayer.jsx:27` already imports `hopWeeks` |
| POLIS-4 | D10 territory partition view + placement arm + prominence ladder | UNGATED | **after STRIP-6**; arm lives `scripts/`-side until green (A1.2.13); MUST reuse SEAM-2's helper (A1.2.9) | STRIP-6 (UI window) | **AFTER-STRIP-6** — helper landed in `spatialDigest.js` (§732.2); derivation module + scripts arm may build earlier |
| POLIS-5 | Genesis ongoing-war option (D6 tail) | not owner-gated; **HELD** by A1.2.8's terminal fork | after POLIS-2 + first-N-ticks observation → dispatch pre-soak OR convert to owner row | none | **HELD** — the harness rides POLIS-2's act |
| VAR-1 | Heightmap interpreter port (detPow, lattice, templates, goldens, MIT notice) | UNGATED | pure leaf behind composer's dynamic import — NEVER eager; new files batch one lighting window (A1.2.13) | none (lands after STRIP-2's lighting re-freeze to avoid interleave — §6) | **NOW** — roster anchor `SF_TEMPLATES` referenced from `worldPlan.js:78` (`public/map/sf-bridge.js`), `sfArchipelago` at `worldPlan.js:90` |
| VAR-2 | Heightfield into worldPlan site scatter (versioned, dormant) | UNGATED | after VAR-1; absent-flag byte-identical; D8 stage-2 suggestions ride it | none | **AFTER-VAR-1** |
| VAR-3 | Per-knob wizard pins (A1.2.14 mint; UI-state only) | UNGATED | **after STRIP-6** | STRIP-6 (UI window) | **AFTER-STRIP-6** |
| ST-1 | Faith `origins` lineage field | **Q-W5** | no-backfill law; raw-byte bar; virtual flag (A1.1.3) | none | **GATED** — field family absent at tip (worldPulse faith modules: `faithNews.js`, `faithReceiptPools.js`) |
| ST-2 | Goods unification (A1.1.4 shape: types-only vocab + TWO index surfaces) | UNGATED | was "before DW-0" (module-vacuous, §739); **now binds as ST-2 → W-COIN-1a (§737.1)** — critical path | none | **NOW** — eager-edge law it must honor CONFIRMED verbatim at `src/data/resourceChains.js:1-17` ("DO NOT re-export… would re-drag into first paint"); owes its own test bill at charter (A1.2.13) |
| ST-3 | Composer pipeline-as-data (D12) | UNGATED | **after POLIS-2** (A1.2.12) | none | **AFTER-POLIS-2** (same lane, 2nd car, is the clean shape) |
| ST-4 | Two-endpoint advisory + calamity flavor widening | UNGATED | advisory half free; calamity half = declared-shift row + raw-byte bar OR defers (A1.2.7 — future-tick selection on lived worlds) | none | **NOW** (advisory half; the lane decides the calamity half per A1.2.7 at charter) |
| W-ARMS | Generative heraldry w/ kinship inheritance | vetoable standing row | **LAST** before endgame residue (§4.2); owes determinism/dormancy/test-bill rows at dispatch (A1.2.14) | none | **HELD-LAST** |
| DESK-1..5 | Realm census table · map↔word linkage · comparison charts · perspective standings · legibility batch (A2) | UNGATED | **after STRIP-6** (A2.1 "all display-plane; post-STRIP-6 per §4.2") | STRIP-6 (UI window) | **AFTER-STRIP-6** — DESK-2 anchor `hoveredSettlementId` CONFIRMED at `mapSlice.js:254` (+setter :333; A2's ":237" is stale by WSEAM edits — fact holds); DESK-5's deep-link precedent `?mapview=` at `src/lib/mapSubTabs.js:57` |
| W-COIN-1a/1b | Treasury stock + typed taxation (first cars) | **Q10/Q11** (§737.1) | **after ST-2**; brief ready `docs/briefs/WCOIN-1a-1b-BRIEF.md` (§748.5) | none | **GATED** — reversal surface CONFIRMED: `treatyTransfer.js` at `src/domain/worldPulse/`; the inert-spoils cure target `wealthIndex` read-never-written at `warDeployment.js:763` |
| W-COIN-2..4 | Band chip/news · war sinks · peace-terms truth | Q10/Q11 + widened Q4 (flow enumeration) | after 1a/1b per DESIGN_W_COIN as amended | none | **GATED (sequenced)** |
| SEAT-1..8 | The six-directive political architecture + D10/D11 risings/market/instigation | **Q-S2..Q-S8** + coverage table | **after W-COIN-1a lands** (§744.2); SEAT-7/8 after SEAT-2/SEAT-4 (§745.2); SEAT-2 split 2a/2b/2c (§744.1) | none | **BLOCKED** — `foreignSeatOf`/`legitimateRemnantOf` 0 hits at tip (design-only, correctly) |

== §4 · ORDERING LAWS CONSOLIDATED (volume §4 as amended — the one list a dispatcher needs) ==

1. STRIP waves land first on any shared surface; STRIP-6's terminal roster is a DIRECTORY ALLOWLIST (§4.1(b), §11.4) so WEAVE's later realm-side map-word code is legal by design.
2. Engine cars STRIP-independent, parallel worktrees, ONE landing/gate at a time: SEAM ✅(0-2), CAP, NET(build), NAME-1, ST-1/3/4 — and ST-2 (its old "before DW-0" bound is module-vacuous; §737.1 re-binds it as W-COIN-1a's precondition).
3. UI cars after STRIP-6: POLIS-3/4, VAR-3, W-DESK. 4. NAME-2 after STRIP-4. 5. NET-1's export-visible half after STRIP-2 (A1.2.14). 6. POLIS-2 before ST-3 (A1.2.12). 7. SEAM-3→CAP-1, or CAP-1 first minting the widened sidecar shape (A1.2.10). 8. POLIS-5 after POLIS-2 + observed ramp, terminal fork (A1.2.8). 9. VAR-2 after VAR-1. 10. ST-2 → W-COIN-1a (post Q10/Q11) → SEAT-1.. (SEAT-7/8 after SEAT-2/4). 11. W-ARMS last. 12. NAME-1-before-DW-7 and ST-2-before-DW-0 are VACUOUS at launch (§739.1/§740.1 defer DW + undercity to the module) — they re-bind only at module time. 13. New files batch per landing window with ONE lighting-census re-derivation per window (A1.2.13) — relevant to NAME-2, VAR-1, POLIS-4's arm, ST-2's modules, SEAM-4's file if new.

== §5 · STRIP SURFACE-COLLISION PROOFS (shared files at `eba286607`) ==

(a) **STRIP-2 ↔ NET-1:** `src/lib/realmMapExport.js:34` `import { downloadBlob } from './townMapExport.js'` — STRIP-2's car 3 re-homes downloadBlob (new `src/lib/downloadBlob.js` per §750.1) and car 4 deletes `townMapExport.js`; NET-1's export half re-records the same parity goldens. One re-record, not two interleaved (A1.2.14). Also `TimelapsePanel.jsx:66` (dynamic import — the specifier string) and `ChroniclersLetterPanel.jsx` ride the same idiom.
(b) **STRIP-2 ↔ NAME-3:** `src/pdf/lib/liveWorld.js:63-65` imports `warStatus.js` + `armyStrength.js` — NAME-3 lands as enrichment of exactly this family while STRIP-2's PDF car edits the `src/pdf` chain (4 deleted/5 edited at its checkpoint). Serialize: NAME-3 lands after STRIP-2.
(c) **STRIP-4 ↔ NAME-2 — CHARTER CORRECTION (the tree wins; the volume amends).** A1.2.11 struck D3's wards-migration clause on the premise "STRIP-4 removes that consumer first". **FALSE at the current ruling:** §748.3's ARM B (chair-ruled, vetoable) RETAINS `cartographyWards.js` as scene substrate, and at `eba286607` it still consumes NAMING_DATA — `namePoolsFor(settlement, namingPools)` at `cartographyWards.js:128`, called at `:316`, with the injection contract documented at `compileTownSceneManifest.js:513` ("the caller above the lazy boundary must supply NAMING_DATA"). NAME-2's charter must either re-instate a wards-migration clause (migrate the injected pool resolution to the D3 resolver under the flag) or write an explicit exemption (wards naming is retained scene substrate, replaced at module-land). The "after STRIP-4" ordering itself still stands (baseline churn must not interleave), but its RATIONALE is now the churn law, not consumer removal. CONFIRMED both legs.
(d) **STRIP-5 ↔ POLIS-3/Q-W7:** `src/config/entitlementLadder.js:57` `LENS_COUNT = TOWN_MAP_STYLE_IDS.length` — STRIP-5 removes this import + the four settlement-map authoring rows; POLIS-3's travel-rings feed and Q-W7's export tiering sit behind the same ladder. Post-STRIP-6 placement already serializes this; a POLIS-3 charter compiled before STRIP-5 lands would cite rows about to die.
(e) **STRIP-6 ↔ VAR-1/POLIS-4/W-DESK:** the sweep's grep roster must be the directory allowlist (§11.4) or every WEAVE realm-side car reds it; and §748.3 binds STRIP-6 not to treat `generate-k*.mjs` as map scripts.
(f) **No collision (checked, clean):** CAP (all four — `spatialPackCapture.js`/`seaLanes.js`/`spatialDigest.js`, zero STRIP overlap) · POLIS-2/ST-3 (`src/lib/instantWorld/**`) · ST-2 (`src/data/resourceChains.js` + generators/display consumers) · NAME-1/NAME-4 (display leaf + prose registries) · VAR-1 (new pure leaf) · SEAM-4/5 (tests + `LivingWorldGates.jsx`) · NET-2 (dark lane) · W-COIN/W-SEAT (worldPulse engine).

== §6 · STRIP-2-DELTA (rows of THIS report the in-flight TE-STRIP-2 charter can move) ==

TE-STRIP-2 (checkpointed at `4ccbfe7d6`; resumes with `git rebase --onto eba286607 1a2471990`, then cars 3–7: downloadBlob/drawListToSvg/SHADOW_DIR/coerceStyleId re-homes, townMapExport + PDF-chain deletions, StyleOverhaulPanel disposition, the 12 R-PINS-SWEEP packet edits, lighting-census re-freeze) moves:
- **§5(a)/(b) file:line receipts** — `realmMapExport.js:34`, `TimelapsePanel.jsx:66`, everything under `src/pdf/` cited for NAME-3: all become stale the moment its cars land. Re-take at the post-STRIP-2 tip before compiling NET-1's or NAME-3's charter.
- **NET-1's verdict clock** — "LAND-AFTER-STRIP-2" resolves the day STRIP-2 CASes; NET-1's parity-golden re-record must be derived against STRIP-2's re-recorded goldens, never the `eba286607` set.
- **The lighting-census figures** (2493/364/2129/20862/5788, frozen at `eba286607` per §749.1) — STRIP-2's re-freeze moves them; every new-file WEAVE window (VAR-1, NAME-2, ST-2, POLIS-4 arm, SEAM-4) re-derives at its OWN window per A1.2.13 and program law §1.7, never inherits this report's figures.
- **StyleOverhaulPanel disposition** — if the panel dies whole, `operationRegistry` rows move again on top of WSEAM's 68→69 raise; any WEAVE charter citing registry/compendium counts (162 ops at §749.1) re-derives.
- **The 12 figure-pin packet edits** — WEAVE charters citing packet `requiredSymbols` must re-verify post-edit; symbols survive (that is the point of §731.3(ii)), figures do not.
- **mapEdits.js:37** (`coerceStyleId` import) — STRIP-2 re-homes/trims it; mapEdits is retained engine substrate read by the canonize seam (`spatialSubstrateDerive.js:26-27` per R-STRIP4-CENSUS §A, re-confirmed reachable at this tip), so a SEAM-3/CAP-1 lane running concurrently with STRIP-2's re-home must not assume that import line.
- NOT movable by STRIP-2: the CAP/POLIS-2/ST-2/NAME-1/NAME-4/VAR-1 dispatch verdicts (zero shared files — §5(f)).

== §7 · RECOMMENDED NEXT-DISPATCH SET (builds parallelize; gates serialize; STRIP-2 holds the landing token now, STRIP-4 next) ==

Dispatchable TODAY, each in its own worktree from `eba286607`, zero shared files with STRIP-2/STRIP-4 or each other (§5(f)), landing windows queued behind STRIP-2 under the one-gate law:
1. **TE-STRIP-2 resume** (already dispatched — it is the landing-token holder: rebase, cars 3–7, gate).
2. **W-CAP lane** — CAP-1 → CAP-2 → CAP-4 → CAP-3 as sequenced cars (A1.2.10's CAP-1-first branch, minting the widened sidecar shape; raw-byte bar per car).
3. **ST-2 lane** — the goods unification under A1.1.4's corrected shape. Highest strategic value: it is W-COIN-1a's precondition (§737.1), and W-COIN dispatches "the hour Q10/Q11 arrive" (§748.5) — ST-2 done means the treasury is gated on the owner alone.
4. **POLIS-2 → ST-3 lane** — the composer pair in one lane satisfies A1.2.12 by construction; POLIS-2 carries the POLIS-5 observation harness (A1.2.8).
5. **NAME-1 + NAME-4 lane** — display leaf, zero canon bytes; NAME-1's first act locates the durable render-side war anchor (fallback pre-ruled A1.1.1(b)); NAME-4's vocabulary seal pre-pays SEAT-7/8's precondition (§746.1).
6. **VAR-1 lane** — the L-sized port; pure leaf; its new files + goldens + MIT notice batch ONE lighting window landed after STRIP-2's re-freeze.
7. **Small riders** (slot into any engine lane or one short lane): SEAM-4, SEAM-5, ST-4's advisory half.
NET-1 may also BUILD now (roadNetwork/RoadsLayer surface untouched by any STRIP wave) with its landing queued after STRIP-2 — dispatch it seventh if lane capacity allows under the four-lane doctrine (§447); otherwise it is the first post-STRIP-2 dispatch alongside NAME-3.
NOT today: NAME-2 (STRIP-4 + §5(c) re-rule), POLIS-1 (Q-W3 answer either way), POLIS-3/4, VAR-3, DESK-1..5 (STRIP-6), SEAM-3/NET-2/ST-1 (Q-W1/Q-W6/Q-W5), W-COIN cars (Q10/Q11), SEAT cars (W-COIN-1a), W-ARMS (last), POLIS-5 (held).

== §8 · CHARTER-CORRECTION LEDGER (tree/ledger disagreements found by this recon; the tree wins, the volume amends) ==

1. **A1.2.11's struck wards clause rests on a premise ARM B voided** — full proof §5(c). NAME-2's charter compiler must re-decide the wards consumer.
2. **Volume file-path staleness (cosmetic, charters must cite the real homes):** `worldPlan.js` = `src/domain/instantWorld/worldPlan.js`; the composer = `src/lib/instantWorld/composeInstantWorld.js` (dedupe :251, discovery ~:265, fingerprint :361 — the volume's ":266"/":374" figures are within drift of the same anchors, CONFIRMED); the entitlement ladder = `src/config/entitlementLadder.js` (not domain/); resource chains = `src/data/resourceChains.js`; A2's `mapSlice.js:237` → now `:254`.
3. **§4.2's DW-anchored orderings (NAME-1 before DW-7, ST-2 before DW-0) are vacuous at launch** after §739/§740 — they travel to the module program; ST-2's live binding is §737.1's ST-2 → W-COIN-1a. No amendment strictly required, but a dispatcher reading only the volume would sequence against a wave that no longer exists on the launch path.
4. **§729.4's "ungated cars" list is stale in one member:** it lists SEAM-4/5 + CAP-1..4 + NAME-1/2 + NET-1 + POLIS-2 + ST-3 + VAR-1 as dispatching "on the volume as amended", but NAME-2 is bound by §4.2's after-STRIP-4 (unlanded) and now §5(c) — treat NAME-2 as conditionally listed, not dispatch-now.

END OF REPORT. All receipts re-takeable via `git grep -n <pat> eba286607 -- <path>`; docs receipts at HEAD `8f208c46e`.
