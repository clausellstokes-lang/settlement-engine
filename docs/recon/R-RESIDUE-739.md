# R-RESIDUE-739 — the §739.2 launch-tail residue, measured at `518c40880` (2026-08-30 scout; ODQ §765)

Read-only Explore scout; every verdict receipted at build tip `518c40880` + the working-tree ODQ. Measured
AFTER §763's omnibus grant, so nothing below is owner-gated (by-nature carve-outs only: push/deploy, the
ultra keystroke, legal, tuning signature).

| Member | Verdict | The receipt that decides it |
|---|---|---|
| WEB-8 | **DONE** | INDEX.md:1268 LANDED; `pricing.js:251/:336/:382-387` dial-aware SKUs |
| WEB-8b | **PARTIALLY SUPERSEDED** | `change-view` row killed by STRIP-5 (PACKET_MANIFEST:27329 retiredBy); `map-chains` survives at `entitlementLadder.js:109`, sole `VIEWING_PAYWALLS_PENDING_514` member (:166-168). Remainder: one row + §524.5's export pin (hazard de-escalated — module-side) |
| WEB-9a | **NOT STARTED (client arm only)** | Server half built: `.env.example:45`, webhook handles `surveyor_renewal` (`stripe-webhook/index.test.ts:3293`); zero `SURVEYOR_PLAN` readers in components |
| WEB-9b | **PLAUSIBLY DONE-BY-ABSENCE** | `pricing.js:339 priceCents: 1499` + §464.2 floor ruling; no live 19.99 Surveyor claim anywhere (the :42/:50 hits are credit packs). Owes one docs/ confirming sweep |
| WEB-10 | **NOT STARTED** | `ANNUAL_FACTOR = 0` (`pricing.js:357`); `premium_annual` enters `ACTIVE_CHECKOUT_SKUS` iff factor>0 (:387), pinned both ways (`pricing.test.js:385`). Money-path: STOP-class discipline, own car |
| WEB-11 | **NOT STARTED** | Annual client half; one-car train after WEB-10 |
| WEB-12 | **SUPERSEDED** | Zero `commitEdits` src hits at tip (docs only); its chokepoint died in STRIP-1; the remainder (applyMapEdit/writer-inert mapEdits) is TE-QSTYLE car 4 |
| HK-4 | **NOT STARTED** | `razingExecution.js` absent from `metronomeCooldownLint` NONCOMPLIANT_BASELINE (:84-89, new-name-reds rule); named at `MF-CH2B.md:32`; clears a banked red (`FABLE_VALIDATION_QUEUE.md:191`) |
| HK-5 | ⚠ **NOT DISPATCHABLE AS CHARTERED** | §479.2 (no head-FIGURE pins) collides with `deployRunbookFreshness.test.js:121-127` + `architectureFreshness.test.js:161-175`, which COMPEL head figures on DEPLOY.md/CURRENT_STATE.md. The law reads packet-scoped; chair re-derives scope before any car |
| HK-6 | **DONE** | Zero `Eberron` outside docs; `institutionalCatalog.js:2223` + `institutionDescVariants.js:241-244` setting-free (CH-3's sweep `9e5da627c`, PLAUSIBLE attribution; zero state CONFIRMED) |
| CT-4/CT-5/undercity content | **SUPERSEDED (§740)** | Engine stays (7 modules + 6 tests, landed); no CT-4/CT-5 packets exist; content train module-side |
| WF-8 remainder | **NOT STARTED — cheapest on the board** | Code holds four causes (`patronFall.js:76`); doc still enumerates five: `RECEIPT_POOLS_DOSSIER_STATE.md:4435` `· abandoned ·` + `:4516` FALL block. Two doc edits + key-by-key goldens |
| DEITY-LIVE-CHECK | **PARTIALLY DONE** | The healer row CURED by TE-CH-6: `institutionalCatalog.js:831-847` `magicLicense:'low'` + `arcaneInstitutionIdentity.js:216-221` "THE LICENCE OUTRANKS THE TAG". TWO ARMS UNMOVED: `ARCANE_INST_TAGS = ['arcane','planar','enchanting']` (`arcaneInstitutionVocabulary.js:109`) excludes `divine` (a new unlicensed divine row leaks today); `MAGIC_ASSERTION_PATTERN` (`magicAssertionText.js:22`) arcane-only. The prose-arm fix MOVES GOLDENS — shift record owed. Master-tree answer: both vocabulary files ABSENT on master; `magicExists` in 77 files |
| Graveyard | **NOT STARTED** | `institutionalCatalog.js:771` — the catalog's ONLY graveyard, village-block, required/baseChance 1. Every other tier cannot bury anyone. Content car, granted §763.1 |
| C-19 recount | **SUPERSEDED/DEFERRED** | Numbers discharged at §566.5 ("the ruled 61/54 replaces every ~55/~35"); only the EDIT LANDING defers with DW (module) |
| COVER-REFRESH | **DEAD, clean** | Zero grep hits across src/scripts/tests/.github/api/supabase/package.json; no public/**cover* artifacts; §677.2(iv) dies with the cutover |

**Other members:** MF-CH3 + MF-CG2 both wear landed-DRAFT (packet:3 + INDEX rows DRAFT; flips execution-ready
per R-MF-FLIPS) · MF-CH2B DRAFT, mint NOT STARTED · CH-6b chartered-only, NO packet exists · OSR mint +
parity: no code subject, slotted before the ultra keystroke (§763.3).

**BOTTOM LINE:** five members need no work; two are done pending confirmation; **eight small cars remain**
(WF-8 shrink-back · graveyard rows · DEITY's two arms [goldens move] · HK-4 latch · the MF flips + CH2B mint ·
WEB-8b row · WEB-9a client arm · the WEB-10→11 annual pair [money-path, own car]) — and HK-5 needs a scope
re-derivation before it can exist as a car. Disposition: ODQ §765.2 (TE-RESIDUE-1 dispatched; commerce pair
queued as its own car; HK-5 to the chair's next collection).
