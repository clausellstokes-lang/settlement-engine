# ROUND 3 — CYCLE 1 SURVEY REGISTER (2026-07-20, @ composite-r4 ca244100)

16 lanes (13 amended-standard dimensions + 2 intake + completeness critic); 72 raw findings;
6 duplicate-merges; 51 fix-candidates entered adversarial verification (3-lens per P1/P2, skeptic per P3);
7 owner-queue; 8 defer-ledgered. Verification: wf_05cf0104-796. Fix waves follow the verdicts.

## Grade table (cycle 1 entry)

| Dimension | Grade | Gap to A+ |
|---|---|---|
| Performance / Eager Budget | A+ | None for the shipped experience — eager budget is unimpeachable (15.5KB headroom, zero new eager modules, clean lazy-leaf discipline). The only residual purity nicks are two O(scan)-in-loop patterns (perf-1, perf-2), bot |
| Correctness (engine + UI + edge) | A- | One live P1 crash exposure plus two P2 UI-seam gaps keep Dimension 1 below A+. The engine layer — including all the mini-fold lanes (gratitudeBonds/npcLadderKernel/generosityKernel write↔consume, thirdPartyRansom roads-b |
| 3 — Experience/Immersion vs THE EIGHT CROWNS | A- | Per-crown: (1) settlement maps A- — mechanics/provenance/lenses/panorama/interiors are best-in-class, but the beauty axis (v2 layout law, illustrated skins, default-lens flip) rides pending owner TASTE VETOES; (2) world  |
| 4 — Substance benchmarks (the depth standard, DESIGN_SIM_DEP | A- | Two confirmed deltas between the depth claims and executed behavior: (1) the D6 organic-founding pathway is silently dead — institutionLifecycle passes the slug id 'underground_network' to an exact-NAME catalog lookup, s |
| 6 — Security / Abuse | A- | The runtime security posture is A+-grade and defensible to a hostile reviewer: all four FF fraud-fixes are live and correct, and fail-closed is the default everywhere value moves (velocity limiter, AI per-IP burst gate o |
| 2 — Engine cohesion / counterparts | A- | Three things, all in-loop: (1) the one-tick courier ledgers' consume-once guarantee depends on the DEPOSITOR's liveness in 4 of 5 couriers — only gratitudeBondEvents has the consumer-side exact-tick guard ("consume-once  |
| Dimension 7 — AI Cost-Efficiency (prompt caching, tool-force | A- | Two concrete efficiency gaps keep this off A+. (1) The flagship cost optimization — prompt caching on the 15-call narrative / 5-call dailyLife runs — is very likely a silent no-op on the DEFAULT model. The cached prefix  |
| Dimension 9 — Accessibility (WCAG 2.1 AA floor; newest inter | B+ | The a11y INFRASTRUCTURE is A-grade — useDialogFocusTrap (aria-modal backed by real focus-in / Tab-trap / stacking-aware Escape / focus-restore / autofocus-honoring) is adopted by 14+ modals, the money-panel inputs carry  |
| 8 — CONTENT / VOICE (register discipline, config-sourced-fac | A- | The house voice itself is A+ — the newest engine prose (roadsProse departure/capture/ransom/embassy pools, eventProse calamity+war/peace/hegemony receipts, contest and traditions chronicle beats) is period-true, anachron |
| 11 — Determinism / Reproducibility | A | The engine core is determinism-by-construction and defensible to a hostile reviewer: eslint bans (Math.random / no-arg Date / localeCompare / toLocale* / Intl) cover all four sim trees (src/generators, src/domain, src/wo |
| 10 GAME-FEEL (crown 4's lens: pacing, session rhythm, return | A- | The loop's chassis is already A+-grade — four-scale advance cadence with determinate progress and pause/resume, the AdvanceReport zoom pyramid with delta-first lead + deputy's diary + decree tracker (held/absorbed/contes |
| 13 — Claims-vs-enforcement parity (the meta-pin philosophy a | A- | The enforcement spine is real, executed, self-testing, and mutation-proven — but the flagship convention is 95% unvalidated against the roadmap's own A+ bar: only 3 of 64 @enforced-by tags in src/ are ever target-validat |
| State-Lifecycle Integrity (Dimension 12 — NEW) | A- | The data round-trips are solid — every newest persisted shape survives persist/read, undo (wholesale preWorldState restore via cloneJson), clone, and legacy import (absent-key tolerant), all CONFIRMED by executed probes  |
| Coherence — the 105-pair system coherence matrix (COHERENCE_ | A- | Every DEFAULT-LIVE incoherence the matrix flagged is fixed (the ruin-filter class #1-4, occupation×corruption #5, religion×war #6 — all cited in code and #1-4 structurally ratcheted), so the shipped default experience is |
| Intake Lane B — Pre-Stocked Disposition (THE_REMAINING_ARCHI | A- | One genuine correctness regression survives the pre-stocked intake — SettlementsPanel dropped the mount-load cancellation latch master had (prestocked-settlementspanel-latch, P2). Fix that plus the one CONFIRMED AA failu |
| completeness-critic | A- | The panel's 15 lanes covered the newest build density (mini-fold seams, money wave, roads/ladder/depth) superbly with real cross-verification, but coverage was systematically blind to everything outside the newest lanes' |

## Findings (all 72; status at triage)

### Performance / Eager Budget
- **perf-1** (P3, CONFIRMED) — activeBlockadeTargets rebuilt per sea-mission instead of hoisted once per tick
  - claim: In the roads sea-hazard dispatch, resolveSeaHazard calls activeBlockadeTargets(a.worldState) — which does a full Object.values scan of the navalTransit ledger to build a Map — once for EVERY in-transit mission, though the map is tick-invariant. The roads mover loops all missions (roadsKernel.js:501) and calls resolveSeaHazard per mission (roadsKernel.js:540); the blockade map is recomputed inside each call (seaRoads.js:143-144). Cost is O(sea-missions x navalLedger) per tick when both seaRoadsEn
  - anchor: src/domain/roads/seaRoads.js:144
  - fix shape: Hoist activeBlockadeTargets(worldState) to the once-per-tick roads pass (roadsKernel ~494 beside seaLit) and pass the resulting Map into resolveSeaHazard as a parameter, replacing the per-call rebuild at seaRoads.js:143-144. Tick-invariant, zero behavior change, byte-neutral when dark.
- **perf-2** (P3, CONFIRMED) — enumerateIntelOpportunities does a full obligation-ledger scan nested inside the seller x subject x receiver loop
  - claim: hasStakes(receiverId, subjectId) (intelActs.js:279, inside the receiver loop) calls hasLiveObl (intelActs.js:240-247), which linearly scans EVERY obligation-ledger key on each call. Nested inside the seller (262) x fresh-subject (268) x receiver (277) loops this is worst-case O(S x B_fresh x R x O). It is triple-dark-gated (intelTradeEnabled AND beliefsActive AND infoStatecraftEnabled, generosityKernel.js:1017), fresh-belief-gated (only this-tick belief updates reach the receiver loop), and outp
  - anchor: src/domain/spatial/intelActs.js:279
  - fix shape: Precompute an obligation adjacency index once at the top of enumerateIntelOpportunities (Map of id -> Set of counterpart ids, O(obligations)) and make hasLiveObl an O(1) membership check, mirroring the bonds Map already built at 222-239. Pure refactor, identical results, byte-neutral when dark.
- _ceiling note_: True runtime-perf certification of the LIT lanes (intel-trade, sea-roads, third-party-ransom, memory-weave) at large world scale over many ticks needs a real-runtime soak/profiler run — outside this survey's power and forbidden here (no build/full-suite). If the owner lights these experimental flags for a large realm, profile the two loops named below before declaring them free.

### Correctness (engine + UI + edge)
- **correctness-1** (P1, CONFIRMED) [mustFix] — SettlementsPanel white-screens on a campaign missing settlementIds
  - claim: migrateCampaign (campaignSlice.js:92, the single load chokepoint applied to both cached and remote campaigns) normalizes id/mapState/regionalGraph/wizardNews/worldState/accessState but NOT settlementIds, while SettlementsPanel reads c.settlementIds RAW in the unconditional assignedIds useMemo (SettlementsPanel.jsx:601 'for (const id of c.settlementIds)') and again in the campaign-folder map (:757 'campaign.settlementIds.map'). The store defends this field with '|| []' in ~8 places precisely beca
  - anchor: src/components/SettlementsPanel.jsx:601 (also :757; root cause src/store/campaignSlice.js:92 migrateCampaign)
  - fix shape: Structural fix at the chokepoint: add 'next.settlementIds = Array.isArray(next.settlementIds) ? next.settlementIds : []' to migrateCampaign so every campaign entering the store carries the field; belt-and-suspenders guard the two component reads with '|| []'.
- **correctness-2** (P2, CONFIRMED) [mustFix] — Paid Chronicle button sticks busy forever if the request throws
  - claim: WizardNewsPanel.generateChronicle (WizardNewsPanel.jsx:347) awaits requestCampaignChronicle with NO try/catch and calls setChronicleBusy(false) only on the success/handled-error fallthrough (:371). requestCampaignChronicle (lib/campaignChronicle.js) runs buildChronicleGrounding (:22) and 'await supabase.auth.getSession()' (:37) OUTSIDE its own try/catch, either of which can throw/reject; that rejection propagates to generateChronicle's unguarded await, skipping setChronicleBusy(false), so the go
  - anchor: src/components/map/WizardNewsPanel.jsx:347
  - fix shape: Wrap the await in try/catch/finally: setChronicleError(...) on catch, setChronicleBusy(false) in finally. Preferably also move buildChronicleGrounding + getSession inside requestCampaignChronicle's try so the helper is total and always returns {error}.
- **correctness-3** (P2, PLAUSIBLE) — Library initial-load failure is silent; saves reload lacks an owner-keyed cancel latch
  - claim: SettlementsPanel's mount effect (SettlementsPanel.jsx:210-214) handles savesService.list() rejection with only console.error + setSavesLoading(false) and no observable error state — a failed library load is indistinguishable from an empty library, violating the roadmap's own FAIL-VISIBLE-NEVER-FAIL-SILENT principle. Separately, reloadSaves (:204-208) and the mount effect call setSaves(loaded) with no owner-keyed cancelled latch, so an account switch in flight can paint the previous owner's saves
  - anchor: src/components/SettlementsPanel.jsx:210
  - fix shape: Route list() failure into a visible error state (reuse setPersistenceError). Capture the current ownerId before each await and discard the result if it changed (the owner-keyed cancelled-latch idiom used on other library surfaces).
- **correctness-4** (P2, CONFIRMED) — DM-approved decrees record no provenance (recorded-causality gap)
  - claim: appendPulseHistoryWithProvenance — the provenance/pulse-history writer — is invoked ONLY by the organic pulse tick (pulseKernel.js:2486). applyWorldPulseProposal (applyWorldPulse.js:1342), the manual DM Apply path, routes the approved outcome through applyWorldPulseOutcomes and updateProposalStatus but never records provenance, so a DM-approved decree mutates the world with no recorded-causality entry while organic pulses get one. The program ledger (COMPREHENSIVE_REVIEW_PROGRAM.md:4227) names t
  - anchor: src/domain/worldPulse/applyWorldPulse.js:1342
  - fix shape: On the manual apply path, record a provenance entry using the existing writer (appendPulseHistoryWithProvenance / recordProvenanceLedger) with the resolved decree outcome, matching the organic pulse. Uses the existing ledger shape (no schema change); its consumer is post-launch so this is completeness, not user-visible loss.
- _ceiling note_: Empirical confirmation was bounded by the SURVEY-ONLY charter: no full/sharded suite, no build, no React render harness. All four findings are grounded in code read this session plus the program ledger's own corroboration (lines 1086 and 4227), not an executed repro. Baseline taken as pinned per brief: two-shard 14,460 passed / 4 parked-golden fails, first-paint closure 1,024,511, domain-strict 0,

### 3 — Experience/Immersion vs THE EIGHT CROWNS
- **experience-faction-member-staleness** (P2, CONFIRMED) [mustFix] — Faction rosters embed stale pre-enrichment NPC copies (63% diverge from canon)
  - claim: settlement.factions[].members hold references to the RAW NPC objects captured at the generatePopulation step, while the later coherence-enrichment tail (buildPoliticalNarrative → mergeNPCLists → enrichNPCsWithStructure) mints NEW npc objects for settlement.npcs — so the canonical roster carries the stress-woven goals/secrets and every faction's embedded member records keep the discarded generic versions, violating the roadmap's ONE-SOURCE-PER-FACT principle and duplicating full NPC clones in eve
  - anchor: src/generators/power/factionGrouping.js:68 (members embed raw refs) + src/generators/narrativeGenerator.js:990,1030 (npcs replaced with enriched copies; factions never re-linked); reproduced at src/generators/generateSettlementPipeline.js:120-148 for NPC rerolls
  - fix shape: Structural (per the commission): make faction members ID-REFERENCES ({id, name, role} or id-only) resolved through the canonical roster at read time, with a settlementMigration + normalizeSettlement coercion for existing blobs; minimum honest repair is re-linking members to the enriched roster at the end of generateCoherence/regenNPCsPipeline. Either way the same-seed generator output shifts — a D
- **experience-npc-agency-news-no-voice** (P3, CONFIRMED) — The feed's most common beat class (NPC agency) has no crier voice and generic bodies
  - claim: newsVoiceLine covers 10 drama categories (war/faith/trade/pestilence/calamity/migration/authority/succor/prosperity/reframe) but the NPC-agency beat class (reform/mobilize/bargain goal advances) — the most numerous card class in an advance — classifies to none of them, so its cards render only a terse headline plus a lifecycle-generic body pool line, while war/trade cards get evocative crier lines.
  - anchor: src/domain/display/newsVoice.js:41 (VoiceCategory set omits an npc-agency class) + src/domain/display/newsBody.js:92-141 (lifecycle pools carry no actor/goal specificity)
  - fix shape: Add an 'agency' VoiceCategory (or classify npc_goal beats into authority) with a >=7-line authored cell per bucket in the newsVoice idiom — pure view-time, pool-growth-safe by the module's own CONTENT-VT pin, zero engine/golden bytes.
- **experience-news-headline-raw-id-fallback** (P3, PLAUSIBLE) — Raw settlement id can leak into news headlines when a graph node lacks a name
  - claim: headlineForImpact/summaryForImpact fall back through names.get(id) || …SettlementName || raw settlementId before 'Unknown settlement', and nodeNameMap falls back to String(node.id) — so any regional-graph node that exists without a name renders its raw internal id inside a diegetic headline.
  - anchor: src/domain/region/wizardNews.js:382,410-411 (raw-id fallback precedes the safe label) + :223-225 (nodeNameMap String(node.id) fallback)
  - fix shape: Reorder the fallback: names.get → carried …SettlementName → a neutral in-world phrase ('a neighbouring settlement' / 'Unknown settlement') — never the raw id; optionally resolve through the saves roster as a second map.
- **experience-roadscene-json-fallback** (P3, CONFIRMED) — Road-scene panel prints raw JSON for any unrecognized brief item
  - claim: RoadScenePanel's itemLine ends in `return JSON.stringify(it)` — the composer is deliberately registry-driven and growing (PURPOSE_KINDS extends; gates states extend), so a future section item kind renders literal JSON inside the DM's diegetic staging surface.
  - anchor: src/components/map/RoadScenePanel.jsx:39
  - fix shape: Replace the JSON fallback with a neutral in-world line built from whatever known fields exist (state/kind + names), mirroring the composer's own generic purposeLabel fallback.
- _ceiling note_: Crowns 4 and 7 are structurally capped below A+ pre-launch: the engine's headline systems are constitutionally DARK (src/domain/worldPulse/simulationRules.js:58-182 — war layer, faith spread, religion dynamics, seasons, migration flows and more default false) and light only at THE ONE REGEN (owner-executed, pre-signed), after which the soak (CERT-30/CENTURY-100/300) certifies century-scale alivene

### 4 — Substance benchmarks (the depth standard, DESIGN_SIM_DEPTH_R2.md: D1 distance-priced news · D2 scaling law · D3 doctrine courses · D4 hegemony read · D5 lifespan-scaled memory · D6 underways · D7 reframe layer)
- **substance-d6-organic-founding-dead** (P1, CONFIRMED) [mustFix] — D6 organic underways founding is permanently dead: slug id passed to an exact-name catalog lookup
  - claim: institutionLifecycle.js's D6 coupling-5 hook calls buildableCatalogEntry('underground_network', ...) but catalogEntryByName (tierOutcomeApply.js:42) resolves by exact lowercased catalog NAME — the entry's name is 'Underground network' — so the lookup returns null forever and addGap no-ops, even though the G2 catalog entry now exists at village/town/city on this tree. The in-file comment (institutionLifecycle.js:424-427) and the ledger row (W-R2-DEPTH 3-of-6, 'no-ops until G2's catalog merges ...
  - anchor: src/domain/worldPulse/institutionLifecycle.js:434 (with tierOutcomeApply.js:42-49, underwaysCouplings.test.js:77-89)
  - fix shape: Structural one-token repair plus honesty trail: pass the catalog NAME ('Underground network') — or add a slug-aware resolve through catalogIdForName so id-keyed callers can never silently miss again — update the stale comments in institutionLifecycle.js:424-427 and the coupling-5 test, and FLIP that test to assert the clandestine gap now EMITS (plus a negative pin when tunnels already exist). Decl
- **substance-d7-gifts-ledger-orphan** (P2, CONFIRMED) [mustFix] — D7 LEDGER OF GIFTS AND DEBTS read-model is an orphan — no surface hosts the designed motive-divergence irony read
  - claim: src/domain/display/giftsAndDebtsRead.js exports giftsAndDebtsFor/realmGiftsAndDebts/hasGiftsAndDebts/campaignHasGiftsAndDebts (lines 97/141/166/173) but a whole-source grep finds zero importers outside its own file and tests — the design's consumer 5 ('the dramatic-irony surface gains motive-divergence beside fact-divergence') is unbuilt: dramaticIronyBrief (composers.js:272-303) contrasts only conspiracies and rumor fact-divergence, and the AI stateSlicers dramaticIrony route likewise never rea
  - anchor: src/domain/display/giftsAndDebtsRead.js:97 (with src/domain/briefs/composers.js:272)
  - fix shape: Host it per the frozen design (within loop power — D7 consumer 5 is inside the frozen paid-surface design, not beyond it): add a motive-divergence section to dramaticIronyBrief consuming giftsAndDebtsFor/realmGiftsAndDebts with includeGroundTruth=true on the DM lane only (premium seam: the player-safe view keeps only the observer's own reading), and optionally register the read in the stateSlicers
- _ceiling note_: A+ for this dimension is structurally unreachable pre-launch: the depth standard's own external bar (the substance sweep #5 principles — the depth-PERCEPTION gap, the anecdote-factory test) and the designs' soak-bandable effects (NEWS_SPEED_FACTOR, REFRAME_TUNING, REALM_SCALING dials all recorded as tuning-window dials) can only be certified by THE SOAK on lit worlds plus THE ONE REGEN — and the l

### 6 — Security / Abuse
- **security-1** (P2, CONFIRMED) [mustFix] — Money-wave RLS wall (PII + money tables) has zero structural pin — can silently regress
  - claim: The deny-all / owner-only RLS posture on money_events, founder_transfer_cases, founder_transfer_events, founder_transfer_challenges (which store to_email_lower nominee emails), client_error_events, and founder_seat_buybacks is correct in migrations 157/160/081/137, but NO test enforces it: a grep across all of tests/ finds no relrowsecurity/pg_policies introspection and no real anon-denial for any of these tables (the founderTransferCases/moneyCheckConstraints pglite suites run as the table owne
  - anchor: tests/security/denyAllRlsCensus.pglite.test.js:31 (TARGETS omits the money-wave tables); RLS declared unpinned at supabase/migrations/160_founder_transfer_cases.sql:89
  - fix shape: Extend denyAllRlsCensus TARGETS with the deny-all money-wave tables (founder_transfer_cases/events/challenges, client_error_events) asserting relrowsecurity ON + policy-count 0 both functionally (pglite) and over migration source; add companion assertions that money_events and founder_seat_buybacks carry exactly their owner-SELECT policy and no INSERT/UPDATE policy. Test-only addition, no schema c
- **security-2** (P2, PLAUSIBLE) — The shared 'scrubbing' logger does not scrub — PII redaction is per-call discipline, not policy
  - claim: logError() (the shared money/AI-path logger the commission calls the 'shared scrubbing logger') is a passive JSON formatter: toMessage() extracts error.message verbatim and the extra object is spread in as-is, with no email/IP redaction anywhere. It relies entirely on every caller never passing PII. This directly contradicts the A+ backend bar: 'governed by a single shared structured-logger that scrubs emails/IPs by policy (not by per-call discipline).'
  - anchor: supabase/functions/_shared/logError.ts:26 (toMessage — no scrub); leak sink e.g. supabase/functions/founder-transfer/index.ts:494
  - fix shape: Make logError enforce the policy: run toMessage(error) and every stringified extra value through an email/IPv4/IPv6 redaction regex before emitting, and add a pin (extend ipPrivacy.test.js) asserting a synthetic email/IP in an error message is masked in the emitted line. Converts scrubbing from discipline to construction.
- **security-3** (P3, CONFIRMED) — Single-use transfer abort token can be persisted in the client-error table via unsanitized location.href
  - claim: errorReporter.js sends the full location.href (query string included) as the error report's url; log-client-error stores it in client_error_events.url (bounded 2000, so a ~64-char token fits). The founder-transfer cooling email's one-click abort link is /account?section=subscription&transfer_case=<id>&transfer_abort=<token>, so a client crash while a user is on that page persists the single-use abort token in plaintext in an error table.
  - anchor: src/lib/errorReporter.js:49 (url: location.href, unsanitized); abort link minted at supabase/functions/founder-transfer/index.ts:609
  - fix shape: In errorReporter, strip search + hash before sending: url = origin + pathname only (drop any query/fragment). One-line change that also defends every future sensitive query param, honoring the app's own 'no sensitive data in URL params' rule at the log boundary. Optionally belt-and-suspenders: have log-client-error also drop the query string on store.
- _ceiling note_: Several security properties are structurally unverifiable pre-launch and are correctly named-not-graded here (launch/soak-gated, not loop findings): real Cloudflare Turnstile siteverify behavior (inert until TURNSTILE_SECRET_KEY), the Stripe Connect payout limb (KEY-INERT + legal sign-off HARD gate), session-supersession under real refresh tokens (Supabase Pro toggle is an owner activation step), 

### 2 — Engine cohesion / counterparts
- **cohesion-courier-liveness** (P1, CONFIRMED) [mustFix] — Courier consume-once depends on depositor liveness in 4 of 5 seam ledgers
  - claim: The deposit-and-consume couriers roadsRansomSettlements (consumer thirdPartyRansom.js:296 consumeRansomSettlements — no tick filter), roadsBondEvents (thirdPartyRansom.js:319 readRoadsBondEvents — no tick filter), bluffExposures (informationStatecraft.js:1170/1178 — lower-bound-only 'depositTick >= now ⇒ skip'), and intelTransfers (informationStatecraft.js:1330 — same lower-bound-only filter) rely ENTIRELY on the owning pass's next-tick prune for consume-once; the consumers are stateless and wil
  - anchor: src/domain/roads/thirdPartyRansom.js:296
  - fix shape: Structural, consumer-side: add the exact-age guard (consume ONLY deposits exactly one tick old — the readGratitudeBondEvents pattern at src/domain/worldPulse/gratitudeBonds.js:96) to all four consumers, so consume-once holds by double construction everywhere. Behavior-identical on every healthy path (deposits are always exactly one tick old when consumed, tick increments serially incl. catch-up pe
- **cohesion-d7e-bond-read-absent** (P2, CONFIRMED) [mustFix] — D-7e clause (i): friendship is minted but never read — generosityEV has no bond term
  - claim: The mercy counterpart pair is half-built: the WRITE side is complete (generosity mints gratitude bonds beside the debt — generosityKernel.js:797/1040 via noteGratitudeBond, ladder consumes via mintBond; bonds decay on the 156-week band-scaled clock, npcLadderState.js:66) but the designed READ side is absent — generosityEV (src/domain/spatial/generosityEV.js:407) carries no bond/friendship term, and strongestBond (npcLadderState.js:422) has ZERO src consumers (grep this session: only tests/domain
  - anchor: src/domain/spatial/generosityEV.js:407
  - fix shape: Build the read term: a bounded bond-warmth input to generosityEV (the tieContribution clamp idiom — a friendship visibly tilts GIVE, never overturns the reserve floor), read from the two courts' ruling-seat bonds via strongestBond/bondSevToward, memoryWeave-gated so dark stays byte-identical. Dormancy golden + an A/B pin (bonded pair gives where the stranger pair withholds at the same margin).
- **cohesion-matrix-d3-ownership-drift** (P3, CONFIRMED) — §11 D-3 row assigns the transfer ledger to statecraft; the build gives it to generosity
  - claim: DESIGN_DEEP_COUPLINGS.md §11 (line 1161) lists 'statecraft ledgers (injection, intelTrades, credibility deltas)' in D-3's write column, but the built system inverts the transfer-record ownership: generosity OWNS intelTransfers (mint + prune, generosityKernel.js:1266-1280, plus intelCooldown) and statecraft is read-only ('Generosity OWNS + prunes intelTransfers — this mover only READS it', informationStatecraft.js:1315). The built inversion is the STRONGER discipline (single-writer with a read-on
  - anchor: docs/DESIGN_DEEP_COUPLINGS.md:1161
  - fix shape: Amend the §11 D-3 row in place (the doc's own §0.5 boundary-amendment pattern): move the transfer record into generosity's write column with 'statecraft reads only', preserving the deeper law.
- **cohesion-generosity-incident-bypass** (P3, CONFIRMED) — Generosity hand-rolls its relationship-incident write, bypassing applyRelationshipPatch
  - claim: generosityKernel.js:1309-1322 appends recentIncidents rows and scalar patches via a direct relationshipStates write instead of the applicator; the incident shape is compatible (reliefIncident emits {tick,type,severity,summary,holder} — generosityReactions.js:335 — matching the applicator's row shape) and scalars are pre-clamped at the call sites, but the edge's updatedAt is never stamped (applyRelationshipPatch stamps it, relationshipEvolution.js:403), and it is a second write-path to the incide
  - anchor: src/domain/worldPulse/generosityKernel.js:1321
  - fix shape: Either route the writes through applyRelationshipPatch (batched), or stamp updatedAt and annotate the inline as the sanctioned batching exception with an @enforced-by pointer, so the D-7 'applicator ONLY' claim and the code agree.
- _ceiling note_: No structural pre-launch ceiling for this dimension: all three gap items are in-loop buildable (engine-internal, dark-flagged, no schema/golden/budget contact). The §13 soak-band certifications (intel acts-per-year, contest rates, ransom proportions, belief-divergence bands) validate MAGNITUDES, not cohesion, and ride the owner's soak — they cap dimension 4 (substance), not this one.

### Dimension 7 — AI Cost-Efficiency (prompt caching, tool-forced structured output, token-metering honesty, debit/refund path, model selection)
- **ai-cost-1** (P2, PLAUSIBLE) — Prompt caching likely silently inert on the default Opus model (prefix under the 4096-token cache floor)
  - claim: The narrative/dailyLife runs place a cache_control:ephemeral breakpoint after a stable prefix (HOUSE + thesis + compact settlement grounding) that the code describes as 'a few KB regardless of settlement size' (index.ts:534-537). Anthropic's minimum cacheable prefix on Opus 4.8 — the default model preference `anthropic_claude_opus_4_8` (index.ts:72) — is 4096 tokens; a shorter prefix silently caches nothing (cache_creation_input_tokens:0, no error). So a 15-call narrative run re-pays full Opus i
  - anchor: supabase/functions/generate-narrative/prompts.ts:1184
  - fix shape: Run count_tokens on the stable prefix for representative settlements; if under 4096, either enrich the cached prefix to clear the floor, or (owner-gated) set the narrative default to a lower-floor model. Add a soak/canary assertion that usage.cache_read_input_tokens>0 across a multi-pass run so a silent no-op is caught.
- **ai-cost-2** (P2, CONFIRMED) — No tool-forced structured output on the schema-wall / JSON-parsing AI surfaces
  - claim: The §4 pre-stocked check calls for tool-forced structured output 'where the schema wall parses JSON', but zero edge functions use tool_choice/tools/input_schema (grep-confirmed across all of supabase/functions). construct-settlement/realm (constructCore.parseConstructAnswer), ai-analyst (parseModelAnswer), and narrative all parse free-text JSON from prose with fence-stripping + a degrade path. This spends output tokens on preamble/markdown the model is merely asked to omit, and a paid call that 
  - anchor: supabase/functions/_shared/constructCore.ts:166
  - fix shape: Adopt tool_choice:{type:'tool',name:...} with strict input_schema (the config vocabulary IS the schema) on the construct + analyst surfaces so output is schema-guaranteed and preamble-free; keep the free-text parse only as a legacy fallback.
- **ai-cost-3** (P3, CONFIRMED) [OWNER-QUEUE] — Single-call surfaces don't cache their 'cache-priceable' static prefixes
  - claim: constructCore documents its config-vocabulary static prefix as 'the byte-stable cache-priceable prefix' (constructCore.ts:14,116) and analystCore's HOUSE is likewise stable across all requests, but neither emits a cache_control breakpoint — ai-analyst/construct/parley/interpret/chronicle/custom-content/style-overhaul/surveyor-autonomy all send plain content:prompt (e.g. ai-analyst/index.ts:134). The stated cache-priceability is never wired.
  - anchor: supabase/functions/ai-analyst/index.ts:134
  - fix shape: Owner-gated: once traffic volume justifies it, add a cache_control breakpoint after the static prefix on the highest-QPS single-call surfaces (construct, analyst) and confirm cache_read>0 in a canary; leave off where cross-request hit-rate stays below the write break-even.
- **ai-cost-4** (P3, CONFIRMED) [OWNER-QUEUE] — Sonnet model drift: 4-5 pinned in 5 functions vs 4-6 in narrative + pricing
  - claim: custom-content, surveyor-autonomy, construct-realm, construct-settlement, and the analyst BYOK supported set pin claude-sonnet-4-5, while generate-narrative and the pricingResync registry use claude-sonnet-4-6. Both are active (neither deprecated/retired per the claude-api reference) and identically priced ($3/$15 per MTok), so there is no cost delta — but half the AI surface runs a superseded Sonnet.
  - anchor: supabase/functions/construct-settlement/index.ts:47
  - fix shape: Owner decision (model choice = paid-surface/deploy): standardize the fixed-model functions on claude-sonnet-4-6 to match narrative+pricing, or record the 4-5 pin as deliberate; add a single shared Sonnet-id constant so the two never drift again.
- _ceiling note_: Definitive confirmation of finding ai-cost-1 (does prompt caching actually fire on the default Opus model, and at what hit rate) requires a live provider key and representative traffic to read usage.cache_read_input_tokens across a run — a soak/canary activity, not resolvable in a static survey. Likewise, the ROI of wiring caching on the single-call surfaces (ai-cost-3) depends on real cross-reque

### Dimension 9 — Accessibility (WCAG 2.1 AA floor; newest interactive surfaces: money panels, Surveyor door, roads/travelers overlays, contest/chronicle modals)
- **a11y-2** (P1, CONFIRMED) [mustFix] — Founder seat-transfer & buyback announce errors but not success — screen-reader users are stranded mid-purchase (WCAG 4.1.3)
  - claim: AccountSeatTransferPanel's Note component sets role only for danger (`role={tone==='danger' ? 'alert' : undefined}`), so every positive-path status on the money flow renders with no role and no aria-live: 'We emailed you a confirmation code.', 'Confirmed. We've emailed your nominee their invitation.', 'The transfer has been stopped.', and the BuybackAffordance's 'We emailed you a confirmation code.' / 'Your seat has been sold back.' The sibling AccountAutoReloadPanel already does this correctly 
  - anchor: src/components/account/AccountSeatTransferPanel.jsx:51 (Note role logic; success sites at :119,:131,:178,:383,:402,:415)
  - fix shape: Map tone→role in Note (danger→'alert', success/muted→'status' with aria-live=polite), matching the danger path and the AutoReload sibling; optionally move focus to the newly-mounted code input after the initiate/accept state transition so the flow is navigable, not just announced. Pure a11y attribute — no paid-surface behavior change, not owner-gated. Add a render test asserting the success notice
- **a11y-1** (P2, CONFIRMED) [mustFix] — THE ONE DOOR (primary AI entry) is a bare role="dialog" that bypasses the app's focus-trap standard and orphans focus on open
  - claim: SurveyorDoor's prompt slip declares role="dialog" (line 99) but imports no useDialogFocusTrap, sets no aria-modal, has no Escape handler, no focus-in, and no focus-restore — unlike the 14+ modals (Dialog primitive, ChroniclePanel, AuthModal, PurchaseModal, ExportSheet, etc.) that all route through the shared hook. Worse, the trigger Button is conditionally unmounted the moment the slip opens (`{!promptOpen && !dest && <Button…>}`), so there is no element left to hold focus. surveyorDoor.test.jsx
  - anchor: src/components/surveyor/SurveyorDoor.jsx:97-99 (role="dialog" panel; trigger unmount at :84)
  - fix shape: Wrap the slip in useDialogFocusTrap(promptOpen, close) + aria-modal="true" (the exact pattern the 14 sibling modals use), and keep the trigger mounted (or explicitly restore focus to it on close) so focus is never orphaned. Add the same lint/ratchet proposed in notes so a new bare role="dialog" that doesn't wire the trap fails the gate — this also catches the PostGenCoach/AnnotationComposer siblin
- **a11y-3** (P2, CONFIRMED) [mustFix] — HomeSampleDossier callout eyebrows fail WCAG AA text contrast at 9px (amber 2.62:1) on the public landing page
  - claim: The three callout eyebrows render color:accent at fontSize:FS.micro (=9px, uppercase bold) directly on background:bg. Executed WCAG contrast (/tmp/contrast_check.mjs): AMBER #D08020 on #FBEAD0 = 2.62:1 (below even the 3:1 large-text floor), GREEN #4A7A3A on #E2EEDB = 4.23:1, SLATE #5A6E82 on #E4E9EE = 4.31:1 — all under the 4.5:1 small-text AA requirement (9px is small text; the bold large-text exemption starts at 18.66px). The design system's own tests/design/contrast.test.js:324 asserts #D0802
  - anchor: src/components/home/HomeSampleDossier.jsx:136-140 (eyebrow color:accent at FS.micro on background:bg; accents/bgs at :35-43)
  - fix shape: Swap each eyebrow to the AA-safe DEEP variant of its accent (amber → the already-shipped #7A3A00, which passes on #FBEAD0; add deep green/slate analogues cleared to ≥4.5:1), preserving the eyebrow-matches-border-left-accent design intent. The exact replacement shade is design-owner's pick AMONG AA-safe variants (marketing surface = taste-adjacent), but the 4.5:1 floor is non-negotiable. Add a cont
- _ceiling note_: Full A+ (best-in-class, not just AA-floor-clean) needs a real screen-reader soak of the money flows and the door on live AT (VoiceOver/NVDA) — announcement ORDER, focus landing after each async state transition, and reduced-motion behavior can only be judged with a real user/AT in the loop. That is a needs-real-users launch item, not a within-loop mustFix; it unlocks once the app is deployed and c

### 8 — CONTENT / VOICE (register discipline, config-sourced-facts law, en.js coverage)
- **content-1** (P2, CONFIRMED) [mustFix] — HomeSampleDossier accent eyebrows fail WCAG AA on a live landing surface
  - claim: All three colored callout eyebrows in the anon-visitor sample dossier card render 9px bold text in the accent color on a tinted background at contrast ratios below the 4.5:1 AA floor — GREEN 4.23:1, SLATE 4.31:1, and AMBER 2.62:1 which fails even the 3:1 large-text floor.
  - anchor: src/components/home/HomeSampleDossier.jsx:139 (eyebrow color: accent; accents defined :41-43)
  - fix shape: Give the eyebrow its own darker text shade (a per-accent 700-weight ink) while keeping the bright accent for the left border/hairline where contrast is not required. Do NOT simply reuse the accent as text color. Overlaps dimension 9 (accessibility) — pre-stocked into this lane for verification.
- **content-2** (P2, CONFIRMED) [mustFix] — Tradition news-beat reasons leak design-doc section refs (§3/§4/§5) into user-facing World Pulse Pills
  - claim: The tradition-outcome and tradition-change chronicle beats write reasons strings containing raw internal design-doc references ('§3 skip', '§4', 'bounded §5 applicators') and mechanism jargon ('a weighted success roll', 'at half weight', 'score above the notable floor', 'the rumor net') — the only reasons in the entire worldPulse engine that break house voice; every other reason (eventProse war/peace/upswing/etc.) is polished period prose.
  - anchor: src/domain/worldPulse/traditionsKernel.js:466 (also :464-465 CANCELLED reason, :518 change-beat reason)
  - fix shape: Rewrite the three reason strings in the established house voice (match eventProse's reasons register), e.g. 'The observance was kept and its fortune weighed — it lifted the town's trade and the seat's standing' and 'Set aside under hard stress, at a mild cost to the seat's standing'; strip every § reference and mechanism term. Byte-identical while dark; re-mints with the traditions golden at regen
- **content-3** (P2, CONFIRMED) [mustFix] — Hardcoded price/cap literals in copy bypass the config single-source (flagged tooltips survived)
  - claim: The config-sourced-facts law is violated: two LIVE title= tooltips hardcode 'Cartographer ... $5.99 a month' (flagged to die in the About overhaul but still rendered on the How-To 'living' tab), and en.js/emailTemplates/TermsPage/FounderTile hardcode $2.99/$5.99/$99/$144/$6-mo plus a hardcoded '30 seats' — while config/pricing.js declares priceCents/seatLimit as the single source and only the pricing-page bands (pricingPageBands.test.jsx:144) enforce derivation.
  - anchor: src/components/howto/LivingWorldTab.jsx:106 (and :162; class: src/copy/en.js price/cap literals)
  - fix shape: Route the two tooltips and non-legal en.js body strings through config constants (the SINGLE_DOSSIER_PRICE / TIERS.cartographer.priceLabel / seatLimit pattern already used by HomeHero.jsx:378), and widen the interpolation-only pin beyond the pricing bands. The TermsPage.jsx billing copy and emailTemplates.js are ⛔ legal/paid-surface content — queue those to the owner rather than editing in-loop.
- **content-4** (P3, CONFIRMED) — Contest support beat hardcodes 'his patron' against a real 50/50 NPC gender field
  - claim: The contest-support chronicle beat uses masculine pronouns exclusively ('A supporter's cause falls with his patron's', 'a supporter shared in his patron's victory') while NPCs carry a real gender field (npcGenerator.js:68, 50/50 male/female) and the rest of the house voice — roadsProse, eventProse — rigorously uses gender-neutral singular 'them/their'.
  - anchor: src/domain/worldPulse/npcLadderContest.js:827
  - fix shape: Replace 'his patron' with 'their patron' in both the headline and summary (and the 'both ways' line reads fine unchanged), matching the house's gender-neutral convention. Byte-identical while dark.
- _ceiling note_: No structural pre-launch ceiling on this dimension — all four findings are fixable within the loop. The only owner-gated fragment is the legal/paid subset of the hardcoded-price class (TermsPage.jsx billing copy, emailTemplates.js) which is ⛔ legal/paid-surface content per §5; the safe in-power slice (LivingWorldTab tooltips + non-legal en.js body) closes the flagged instance. Findings 2 and 4 are

### 11 — Determinism / Reproducibility
- **determinism-pdf-locale-collation** (P2, CONFIRMED) [mustFix] — Exported PDF orders world content via host-default-locale localeCompare
  - claim: src/pdf/sections/SupplyChainFlow.jsx:240 sorts the exported supply-chain category groups with String.localeCompare and NO explicit locale, on a surface (the paid PDF export) whose content is same-seed constitutional; src/pdf is outside every locale guard (eslint blocks cover only generators/domain/workers/kernel; tests/lint/localeCompareGuard.test.js TREES = those four dirs), and the ledgered exclusion of src/pdf (docs/TEMPORAL_AUDIT.md:41) is argued ONLY for wall-clock legitimacy (export dates,
  - anchor: src/pdf/sections/SupplyChainFlow.jsx:240
  - fix shape: Structural, not spot-patch: replace with compareCodepoint/byNameCodepoint from src/domain/deterministicSort.js, then widen the class guard — add src/pdf to tests/lint/localeCompareGuard.test.js TREES and an eslint no-restricted-syntax block for src/pdf/** (locale set only; keep the ledgered wall-clock allowance for Cover/Timeline user timestamps). Declare the possible one-time en-US ordering shift
- **determinism-instantworld-dedup-tiebreak** (P3, CONFIRMED) — Armed localeCompare tie-break in instant-world faction-name dedup (persisted renames)
  - claim: src/lib/instantWorld/factionDedup.js:199 orders composed-world members by _slot with a String(id).localeCompare tie-break; the order decides which settlement KEEPS a colliding faction name and which gets renamed, and the renames persist into the saved world (dedupeWorldFactionNames mutates s.factions in place). composeInstantWorld.js:157 stamps a unique _slot per site so the tie-break is normally dead — but it is armed for any absent/duplicate slot (`?? 0` coalesce), putting a banned-class const
  - anchor: src/lib/instantWorld/factionDedup.js:199
  - fix shape: One-line: import compareCodepoint from src/domain/deterministicSort.js for the tie-break (byte-identical behavior on the live path since unique _slots mean the tie-break never fires today — zero behavior shift, removes the armed hazard). Fold src/lib/instantWorld into the widened scan scope from the P2 finding if the class guard is extended.
- **determinism-pdf-lens-test-vacuous** (P3, PLAUSIBLE) — PDF lens test asserts byte inequality that non-determinism makes vacuous
  - claim: tests/pdf/townMapDocument.smoke.test.js:39 asserts `Buffer.compare(parch, vtt) !== 0` to prove the parchment and VTT lenses emit different documents — but react-pdf renderToBuffer is recorded non-deterministic (the IT-4 hazard: same element tree twice yields different bytes), so two renders differ regardless of whether the lens plumbing works; the assertion can never fail and proves nothing about the lens.
  - anchor: tests/pdf/townMapDocument.smoke.test.js:39
  - fix shape: Assert on the deterministic layer instead of bytes: walk the element tree or compare the shared renderTownMapOp op-stream/props under the two styles (the same file already imports renderTownMapOp at :25 for exactly this kind of check) — never compare renderToBuffer output across renders, per the recorded react-pdf discipline.
- _ceiling note_: Byte-level golden proof is structurally parked pre-launch: the 4 parked golden reds (generatorGoldenMaster, beliefMapGolden, worldpulseDeityGolden, pdf goldenViewModel) re-mint only at THE ONE REGEN (owner-executed, pre-signed), so the strongest byte-stability pins cannot be green until then — the double-run and dormancy layers carry the claim meanwhile. Cross-MACHINE same-seed validation in pract

### 10 GAME-FEEL (crown 4's lens: pacing, session rhythm, return-pull)
- **game-feel-1** (P1, CONFIRMED) [mustFix] — The ladder's display stock is unwired: a name-resolved, secrets-safe DM read-model (rungs, standings, goals-with-stakes, live contests) renders in zero components
  - claim: ladderRead.js declares itself 'consumed by the dossier Power tab, the NPC card, and the §8 standing-loop power/legitimacy reads when lit' (src/domain/townMap/ladderRead.js:2-4), and the kernel projects a compact display mirror every advance (mirrorOf, src/domain/worldPulse/npcLadderState.js:641 — per-faction rungs with npcId+NAME+standing, per-NPC goals {rung, goal, stakes}, live-contest summaries {signalVar, kind, knownToBoth}), but its only importers are engine modules (rulingPowerCoup, pulseK
  - anchor: src/domain/townMap/ladderRead.js:2-4 (declared consumers never wired); src/domain/worldPulse/npcLadderState.js:641 (mirrorOf); src/components/new/tabs/PowerTab.jsx:1-8 (no ladder import)
  - fix shape: STRUCTURAL (and already the recorded recipe): wire ladderRead into the two hosts it was built for — a Power-tab 'The Ladder' section (per-faction rungs top-first with standing bars + instability/leadership chips + live-contest rows) and an NPC-card rung/goal chip — as lazy display reads (absent mirror ⇒ section hidden ⇒ dark worlds byte-identical; zero eager bytes via the existing lazy dossier chu
- **game-feel-2** (P1, CONFIRMED) [mustFix] — The Ledger of Gifts and Debts irony read (D7's dramatic-irony surface) has no UI host — the 'war brewing over a kindness misremembered' is invisible
  - claim: src/domain/display/giftsAndDebtsRead.js — the D7 consumer-5 read-model whose own header promises 'the DM sees the war brewing over a kindness misremembered' (frozen fact vs observer's current reading vs DM-truth mint intent with divergence) and says it 'rides the inspector / realm-dashboard chunk' — is imported by NOTHING outside the engine: grep over src shows only reframeKernel.js and its own test import it; grep for 'giftsAndDebts' across src/components and src/pdf returns zero files. The ref
  - anchor: src/domain/display/giftsAndDebtsRead.js:1-28 (unconsumed read-model); docs/COMPREHENSIVE_REVIEW_PROGRAM.md:5629 (the recorded display-pass seam)
  - fix shape: Mount the read-model in its designed host: a 'Gifts and Debts' section on the realm inspector / settlement relationships surface (lazy chunk), player-safe reading unconditionally, DM-truth divergence lane spliced only behind the EXISTING includeGroundTruth premium seam (the RumorsTab asymmetry idiom, same file pattern). Absent ledgers ⇒ section hidden (the module is already INERT-NOT-CRASH + codep
- **game-feel-3** (P2, CONFIRMED) [mustFix] — Third-party ransom resolves in the WRONG news voice: the home-paid line fires even when a friend or rival court paid the ransom
  - claim: In roadsKernel.js the release-beat pool selection is `const pool = early === 'party_ransom' ? ROADS_NEWS.partyRansom : early === 'party_rescue' ? ROADS_NEWS.rescue : ROADS_NEWS.ransom;` (src/domain/worldPulse/roadsKernel.js:744-745) — there is no branch for `early === 'third_party'`, so the D-5 fork falls through to ROADS_NEWS.ransom, whose canonical line is '${x.home} ransoms ${x.npc} home from ${x.captor}' (src/data/roadsProse.js:119). Only the machine tag differs (`ransom_third_party`). The p
  - anchor: src/domain/worldPulse/roadsKernel.js:744-751 (the pool ternary with no third_party branch); src/data/roadsProse.js:117-128 (the home-paid lines)
  - fix shape: This IS the recorded 'third-party news pool' intake seam (THE_RESURVEY_HANDOFF.md §3b / task-#40b) — build it: a ROADS_NEWS.thirdPartyRansom pool with a friend variant that NAMES the payer ('${payer}'s coin bought ${x.npc} free — a debt is owed westward') and a rival variant that stays covert-but-ominous in the player voice (the compromise is corruption-web COVERT by design; the true motive belong
- **game-feel-4** (P2, CONFIRMED) [OWNER-QUEUE] — The pacing lever is unreachable: no writer for rules.narrativeTempo exists anywhere and the World-Laws dial is unbuilt, so the tempo governor stays dormant even after the flags light unless the ONE REGEN checklist explicitly includes it
  - claim: narrativeTempoOf reads rules.narrativeTempo (src/domain/worldPulse/narrativeTempo.js:141-147; absent ⇒ null ⇒ every budget ∞ ⇒ governor dormant). Executed receipts: grep for 'narrativeTempo' across src/components and src/store returns ZERO writers; DEFAULT_SIMULATION_RULES (src/domain/worldPulse/simulationRules.js:36-110+) contains no narrativeTempo key; the designed surface — the World-Laws dial, fiction voice 'How loudly does history speak?' (DESIGN_PACING_GOVERNOR.md:35-39) — does not exist i
  - anchor: src/domain/worldPulse/narrativeTempo.js:141-147 (the read with no writer); docs/PHASE55_E0_TEMPO_GOVERNOR_PLAN.md:17 (the recorded dial/lighting deferral)
  - fix shape: OWNER QUEUE (lighting is ⛔ owner-signed; presets are paid-surface-adjacent taste): add to the ONE REGEN checklist (1) a default tempo tier in DEFAULT_SIMULATION_RULES (the design's preset mapping suggests realistic_regional as the default-preset tier) and (2) the World-Laws dial per DESIGN_PACING_GOVERNOR.md §3 so the tier is user-adjustable post-launch. RECOMMENDATION the loop CAN prepare: build 
- **game-feel-5** (P3, PLAUSIBLE) — A long advance's interior story rests entirely on the capped wizardNews feed (240 entries) plus a 24-outcome-per-record chronicle cap — a busy lit realm's year-advance can silently shed minor interior beats
  - claim: The Stage-5 collapse keeps ONE pulseHistory record per advance (advanceInterval.js:128-144), each record capping selectedOutcomes at 24 (pulseKernel.js:1614 'selectedForApply.slice(0, 24)'); interior beats survive only in wizardNews (MAX_ENTRIES=240, wizardNews.js:12, with orphan-head rescue capped at half the window, :526) and the IntervalChronicleSummary lookback that reads that same feed (ChronicleScrollback.jsx:44-63). The chronicle read model documents the degradation honestly (chronicleRea
  - anchor: src/domain/region/wizardNews.js:12 (MAX_ENTRIES=240); src/domain/worldPulse/pulseKernel.js:1614 (slice(0,24)); src/domain/display/chronicleReadModel.js:12-16 (the honest-degradation note)
  - fix shape: A soak-time measurement, not a loop fix: instrument the CENTURY soak runs to record peak per-year news volume at full lighting; if the 240 window shows real eviction of non-arc beats, scale MAX_ENTRIES sublinearly with realm size (the realmScaling.js √N idiom already used for tempo budgets) as a recorded tuning-window dial. The design accepted this degradation knowingly and documents it — do not t
- _ceiling note_: A+ is structurally unreachable pre-launch even with the findings fixed: the drama layers (ladder/contests/roads/gratitude/reframe/tempo) are constitutionally DARK until THE ONE REGEN (owner-executed); the no-cacophony pacing band is certified only by THE SOAK (SOAK_PLAN_R2 §2 asserts realm-wide major-event rate stays within the tempo governor's design band) and tuned in THE TUNING WINDOW (tempo go

### 13 — Claims-vs-enforcement parity (the meta-pin philosophy as a grade)
- **claims-parity-1** (P2, CONFIRMED) [mustFix] — 61 of 64 @enforced-by tags are never target-validated by any gate
  - claim: The enforcement-claims meta-pin validates @enforced-by targets only for lines matching its narrow CLAIM_RE vocabulary (tests/docs/enforcement-claims.test.js:36, gatherClaims :89-123); a scratchpad probe measured 3 of 64 src/ tags within the validation window — the other 61 (dormancy byte-identity, single-writer, lazy-leaf-sentinel headers) are decorative to the gate, violating the roadmap's own A+ bar (docs/A_PLUS_ROADMAP.md:258: 'no header comment can lie: deleting the standard's enforcement tu
  - anchor: tests/docs/enforcement-claims.test.js:36
  - fix shape: Structural: widen the meta-pin (or add a sibling test in tests/docs/) to gather EVERY @enforced-by tag repo-wide — not just those near CLAIM_RE vocabulary — and run the existing resolveTarget on each (existence + gate reachability); normalize the two prose-form tags (src/domain/worldPulse/religionLegitimacy.js:272 'the pin's two join cases', operationRegistry.walker's 'this test') to named paths o
- **claims-parity-2** (P2, CONFIRMED) [mustFix] — About retention receipt 'pinned to the database itself' has no config-to-SQL parity gate
  - claim: The About covenant receipt (src/components/howto/AboutManifesto.jsx:197) says downgrade retention is 'pinned to the database itself... (source: migration 023)', but RETENTION_MONTHS = 3 (src/config/entitlementLadder.js:102) and the migrations' interval '3 months' (023_explicit_models_and_plan_retention.sql:63; 024_billing_retention_and_atomic_mutations.sql:232,341) are held together only by a comment — no test reads the migration SQL. Grep across tests/ finds only pricingPageBands.test.jsx impor
  - anchor: src/config/entitlementLadder.js:102
  - fix shape: Add a parity test (tests/lint/ or tests/config/) that derives `interval '${RETENTION_MONTHS} months'` from the config constant and asserts the ACTIVE retention functions in migrations 023 and 024 (and any later re-mint, found via the searchpath-baseline idiom) contain exactly that interval — the founderSeatsMigration pattern applied to retention. Test-only; no schema change; not owner-gated.
- **claims-parity-3** (P3, CONFIRMED) [OWNER-QUEUE] — Two About covenant receipts overstate build gates (replay/matching vs provenance/emit-time)
  - claim: Covenant 1's receipt (AboutManifesto.jsx:182) says 'a drift-gated replay test fails the build if that town ever changes' and covenant 6's (:207) says 'the plate test fails the build if a plate stops matching the town it claims to show' — but no per-town replay test exists (grep: only tests/ui/homeLanding.test.jsx imports landingFixture, and it pins provenance stamps, not regenerated output), the fixture's own header (src/components/home/landingFixture.js:17-20) delegates drift detection to 'gold
  - anchor: src/components/howto/AboutManifesto.jsx:182
  - fix shape: Two owner-choice paths: (a) copy precision — reword the two receipts to name the real mechanism (emit-time drift gate + provenance pins + the golden-master family) — owner voice on the trust page; or (b) make the claim true — mint a fixture-replay pin that regenerates lf-033 from { seed, config } and byte-compares, joining the golden family policy (minted/re-minted at THE ONE REGEN). Both paths gr
- **claims-parity-4** (P3, CONFIRMED) [OWNER-QUEUE] — Covenant 'each with the receipt it leaves' vs 134/164 registry ops rendered 'no receipt'
  - claim: The About covenant (AboutManifesto.jsx:191-192) tells the skeptic the operation registry lists all operations 'each with the receipt it leaves', but 134 of the registry's entries have receiptRef:null (grep count, src/store/operationRegistry.js) — including 2 of the 5 canon-class ops (undoLastEvent:54, revertToSnapshot:56) — and the Compendium renders those as a literal 'no receipt' badge (src/components/compendium/RegistryHubs.jsx:90). The proof surface the covenant links to visibly contradicts 
  - anchor: src/components/howto/AboutManifesto.jsx:191
  - fix shape: Structurally better path: backfill honest receiptRefs where a real receipt source exists (undoLastEvent -> the eventLog removal + chronicle entry; revertToSnapshot -> versionHistory restore record) and add a pin 'every canon-class op has non-null receiptRef'; then scope the covenant sentence to canon-class ops (which the paragraph is actually about). The copy-side rewording is owner voice; queued 
- **claims-parity-5** (P3, CONFIRMED) — START_HERE.md expected-reds claim ('EXACTLY 5') is stale against the pinned baseline of 4
  - claim: The successor bootstrap docs/START_HERE.md:60 (ledger branch, main tree — absent from the composite worktree) states 'Expected suite reds on the composite lineage — EXACTLY 5' (4 parked goldens + aiGroundingBundle.freshness), and section 3b repeats the aiGrounding red; the pinned baseline (verified 2026-07-20 @ ca244100) is 4 failures = exactly the parked golden families — the aiGrounding freshness red was cured at the deep-wave fold. No freshness test covers START_HERE (tests/docs/ has no pin f
  - anchor: docs/START_HERE.md:60
  - fix shape: One-line amendment on the ledger branch at the next ledger-row commit: correct sections 3/3b expected reds to the 4 parked golden families and add a pointer that THE_RESURVEY_HANDOFF section 3g carries the live baseline. Optionally note in the header that the file's snapshot sections are superseded by the handoff for loop executors.
- _ceiling note_: The determinism covenant's chief executable enforcer (generatorGoldenMaster, plus the beliefMap/deity/pdf golden families) is deliberately parked red until THE ONE REGEN — a ledgered, owner-gated state. Until the owner executes the regen, the marketing claim "the same seed can never yield a different world" is enforced only transitively (rng fail-closed + eslint determinism fences + domainWallCloc

### State-Lifecycle Integrity (Dimension 12 — NEW)
- **lifecycle-1** (P2, CONFIRMED) [mustFix] — spatialLedgerCoverage walker is blind to 4 kernel-written spatial ledgers
  - claim: tests/lib/spatialLedgerCoverage.walker.test.js WRITE_RE (`setSpatialLedger\s*\(\s*[^,]+,\s*['"]<key>['"]`) matches only a string-literal 2nd arg AND assumes the 1st arg is comma-free. Four ledgers actually written by domain kernels evade it: intelTransfers + intelCooldown (generosityKernel.js:1281/1301, keyed by the INTEL_TRANSFERS_LEDGER/INTEL_COOLDOWN_LEDGER constants), roadsEmbassies (embassyLedger.js:64, keyed by EMBASSY_LEDGER_KEY), and npcGrowth (npcGrowthKernel.js:622 — a string literal, 
  - anchor: tests/lib/spatialLedgerCoverage.walker.test.js:43
  - fix shape: Harden the walker: match a balanced first argument (not [^,]+) and resolve exported *_LEDGER/*_KEY constants to their string values before collecting keys (or add a companion lint that BANS non-literal setSpatialLedger keys and forces the 4 sites to inline literals). Then classify intelTransfers, intelCooldown, roadsEmbassies, npcGrowth in spatialUsage.js (TRACKED with a mover, or EXEMPT with a re
- **lifecycle-2** (P2, CONFIRMED) [mustFix] — factionPairStates: the only mutable top-level ledger missing CONDITIONAL_LEDGER_KEYS protection
  - claim: worldState.factionPairStates (mutable across ticks, additive, absent-when-dark, drop-when-empty — properties identical to its registered siblings politicsLedgers and narrativeTempo) is NOT in CONDITIONAL_LEDGER_KEYS (worldState.js:349). It therefore misses BOTH protections ensureWorldState gives every other mutable ledger: (a) empty-strip — CONFIRMED probe: `'factionPairStates' in ensureWorldState({factionPairStates:{}})` === true (the empty {} SURVIVES), whereas `ensureWorldState({politicsLedge
  - anchor: src/domain/worldPulse/worldState.js:349
  - fix shape: Append 'factionPairStates' to CONDITIONAL_LEDGER_KEYS (worldState.js, mutable branch — deepCloneConditionalLedger, NOT the frozen set) so ensureWorldState empty-strips + deep-clones it by construction, exactly like politicsLedgers/narrativeTempo. It MUST move together with WORLD_SNAPSHOT_HARD_DENY (worldSnapshotPublic.js:68) because worldSnapshotDenyCensus.test.js derives HARD_DENY = CONDITIONAL_L
- _ceiling note_: No structural ceiling — A+ is reachable within this loop. Both findings are fixable now with test/telemetry + append-only registration edits; neither needs real users, soak, or launch. The only launch-deferred aspect is that a LIT factionPairStates key-order only exists post-THE-ONE-REGEN (owner-executed), but registering it now is byte-neutral in the dark base.

### Coherence — the 105-pair system coherence matrix (COHERENCE_MATRIX_R3.md) re-verified against claude/composite-r4 @ ca244100
- **coherence-reverify-07-occupation-migration** (P2, CONFIRMED) [mustFix] — Defect #7 STILL-OPEN: ongoing occupation stops shedding refugees after 6 ticks (occupation_resistance absent from flight sets)
  - claim: occupation.js re-emits occupation_resistance on the occupied settlement every tick (occupation.js:885, id carries .${t}), but neither WAR_CRISIS_ARCHETYPES (populationDynamics.js:81) nor CRISIS_FLIGHT_ARCHETYPES (populationDynamics.js:90) contains occupation_resistance or occupation_burden; the only occupation key in the flight sets is vassal_extraction, which pulse-conquest stamps ONCE (applyWorldPulse.js:527) with defaultExpiresAtTicks:6 (activeConditions.js:88) and never refreshes until the o
  - anchor: src/domain/worldPulse/populationDynamics.js:90
  - fix shape: Add 'occupation_resistance' (and 'occupation_burden' for the occupier's rate press) to WAR_CRISIS_ARCHETYPES and 'occupation_resistance' to CRISIS_FLIGHT_ARCHETYPES so the refugee model tracks the ongoing occupation, not just the one-shot conquest proxy. Dormant behind warLayerEnabled (default false), so golden-neutral; a same-seed population shift only in war-layer-enabled worlds.
- **coherence-reverify-08-occupation-ladder** (P2, CONFIRMED) [mustFix] — Defect #8 STILL-OPEN: pulse-installed occupier is category:'military', so the 'occupation' archetype (rank/label/disposition) is unreachable
  - claim: installOccupationAuthority hard-codes the occupier faction as category:'military' (applyWorldPulse.js:438); factionArchetype resolves category BEFORE the name rules (factionArchetypes.js:90-91, category wins), so the occupier always resolves to 'military', never 'occupation' — the name rule that would catch it (factionArchetypes.js:57) is never reached, and the occupier's name is the conqueror's name anyway.
  - anchor: src/domain/worldPulse/applyWorldPulse.js:438
  - fix shape: Install the occupier with category:'occupation' (the CATEGORY_MAP already maps it, factionArchetypes.js:48) so the one canonical detector and all its consumers agree; the existing modifiers:['occupier'] already tags it. Dormant behind warLayerEnabled; golden-neutral.
- **coherence-reverify-09-politics-beliefs** (P2, PLAUSIBLE) — Defect #9 STILL-OPEN: two divergent 'who governs' resolvers can hand beliefMap a different seat than every other consumer
  - claim: governingFactionOf resolves isGoverning → governingName match → null with NO power fallback (rulingPower.js:188-190), while beliefMap's private pickGoverningFaction resolves isGoverning → highest-power faction → null-only-on-empty-roster (beliefMap.js:732-743). On a roster where no faction carries isGoverning and governingName matches nothing, the two return different factions.
  - anchor: src/domain/worldPulse/beliefMap.js:732
  - fix shape: Unify on one canonical resolver — have beliefMap.pickGoverningFaction delegate to governingFactionOf (matching the codebase's one-canonical-accessor discipline: nameOf/governingFactionOf/factionArchetype), or decide the shared fallback once. Defensive/latent (needs a malformed roster to bite), but the divergence is the exact 'two resolvers drift' shape the coherence mandate targets.
- **coherence-reverify-10-traditions-quartet** (P2, CONFIRMED) [mustFix] — Candidate STILL-OPEN (unbuilt): the traditions festival-sensitivity quartet
  - claim: None of the four gated-in traditions hooks exist in traditionsKernel.js: trade_route_cut is absent from HARD_STRESSOR_ARCHETYPES (traditionsKernel.js:161 = plague/famine/war_pressure/occupation_burden/vassal_extraction only), and there are zero calamity-penalty, obligations/generosity, or corruption_exposed terms anywhere in the file. No ledger deferral row exists for this quartet.
  - anchor: src/domain/worldPulse/traditionsKernel.js:161
  - fix shape: Build the four one-edit hooks the matrix specifies (COHERENCE_MATRIX_R3.md:39-43): add trade_route_cut to HARD_STRESSOR_ARCHETYPES; a symmetric fresh-calamity penalty / reconstruction bonus; a bounded obligations-ledger term; a corruption_exposed penalty (verify G4 vs DESIGN_TRADITIONS §16). All dormant behind traditionsEnabled.
- **coherence-reverify-11-ladder-corruption-capture** (P2, CONFIRMED) [mustFix] — Candidate STILL-OPEN (unbuilt): npcLadderChallenge ignores the mirrored faction.captureState
  - claim: faction.captureState is written by factionCapture.js and mirrored across the roster, but npcLadderChallenge.js reads only npc.corrupt and revealed_corruption (npcLadderChallenge.js:118,161-165) — it never reads faction.captureState, so a faction_captured challenge window does not open. No deferral row.
  - anchor: src/domain/worldPulse/npcLadderChallenge.js:161
  - fix shape: Read the already-mirrored faction.captureState in the challenge weight to open a faction_captured window (near-zero plumbing, dead-safe when unset), per COHERENCE_MATRIX_R3.md:45.
- **coherence-reverify-12-politics-institutions** (P2, CONFIRMED) [mustFix] — Candidate STILL-OPEN (unbuilt): controlled/suppressedInstitutions are written but never read (dead-write)
  - claim: faction.controlledInstitutions / suppressedInstitutions are written by factionCompetition.js (init 155-156, patch 480-487, capture 652-653, merge 843-844) but a repo-wide grep finds NO reader outside settlement.schema.js and the writer itself — institutionLifecycle.js does not read them. No deferral row.
  - anchor: src/domain/worldPulse/factionCompetition.js:843
  - fix shape: Have institutionLifecycle read the already-written controlled/suppressedInstitutions (a suppressed institution resists reform / a controlled one entrenches), per COHERENCE_MATRIX_R3.md:46 — near-zero plumbing over existing state.
- **coherence-reverify-13-prosperity-coup** (P2, CONFIRMED) [mustFix] — Candidate STILL-OPEN (unbuilt): the coup contest is blind to the economy
  - claim: Neither coup.js nor rulingPowerCoup.js contains any economicAdj / economic_capacity / prosperity term; resolveCoupVerdict weighs power/legitimacy but reads no economic signal. No deferral row.
  - anchor: src/domain/rulingPowerCoup.js:90
  - fix shape: Thread a bounded economicAdj (from deriveSystemVariable('economic_capacity')) into resolveCoupVerdict, soak-banded, per COHERENCE_MATRIX_R3.md:48.
- **coherence-reverify-14-ladder-politics-bloc** (P3, CONFIRMED) — Candidate STILL-OPEN (unbuilt): ladder challenge ignores the ruling bloc
  - claim: settlementPolitics.rulingBlocOf exists but is read by no ladder file (grep: only its own definition in settlementPolitics.js); no bloc-backed / conspiracy challenge window is opened from it. No deferral row.
  - anchor: src/domain/worldPulse/settlementPolitics.js:1
  - fix shape: Open a bloc-backed/conspiracy challenge window from settlementPolitics.rulingBlocOf in the ladder challenge scorer, per COHERENCE_MATRIX_R3.md:47.
- **coherence-reverify-15-corruption-migration** (P3, CONFIRMED) — Candidate STILL-OPEN (unbuilt): migration ignores criminal-capture as a push factor
  - claim: migrationKernel.js contains no thievesGuildStrength / criminalCaptureState / criminal / corrupt read — migration pressure never sharpens on organized-crime capture. No deferral row.
  - anchor: src/domain/worldPulse/migrationKernel.js:127
  - fix shape: Add a bounded push term reading thievesGuildStrength / criminalCaptureState in migrationKernel, per COHERENCE_MATRIX_R3.md:49.
- **coherence-reverify-16-corruption-upswing** (P3, CONFIRMED) — Candidate STILL-OPEN (unbuilt): upswing's reconstruction skim isn't wired to the corruption onset hazard
  - claim: upswingKernel.js has the reconstruction SKIM (upswingKernel.js:18) and npcAgency.onsetHazard already accepts a pressureMult, but that param is fed by planePressureMult (npcAgency.js:767), NOT the upswing skim — the intended rewire is unbuilt. No deferral row.
  - anchor: src/domain/worldPulse/npcAgency.js:767
  - fix shape: Route upswing's reconstruction skim into onsetHazard's existing pressureMult (replacing the narrative-only condition), per COHERENCE_MATRIX_R3.md:50. Dormant behind upswingArcsEnabled.
- **coherence-reverify-17-beliefs-upswing** (P3, CONFIRMED) [OWNER-QUEUE] — Candidate STILL-OPEN (unbuilt): no believed-distress axis for the calamity/upswing read
  - claim: D-1 built a DEMOGRAPHIC belief axis (populationTrendBand, beliefMap.js:235) but no distress axis; upswing/calamity distress is not exposed as groundTruthDistress + believedDistress01 for a belief read. No deferral row. The read-side is dark-safe; the CONSUMER opt-in that shifts war/peace is an owner-gated step.
  - anchor: src/domain/worldPulse/beliefMap.js:235
  - fix shape: Add the dark-safe groundTruthDistress + believedDistress01 read (COHERENCE_MATRIX_R3.md:51); the consumer step that shifts war/peace stays owner-gated.
- _ceiling note_: Two gated-out rows remain legitimately owner-queued and are NOT loop-fixable: prosperity×calamity_upswing (adding economic_capacity to always-on plague/famine golden-shifts every occurrence → owner-signed re-mint, the slave_revolt precedent) and roads×religion (a 'pilgrimage' purpose = new capability, G4 fail) — both cited at COHERENCE_MATRIX_R3.md:54,57. The five roads×{migration,calamity,trade,g

### Intake Lane B — Pre-Stocked Disposition (THE_REMAINING_ARCHITECTURE §4 items + the task-#40 deep-couplings seams). Survey-only, claude/composite-r4 @ ca244100.
- **prestocked-settlementspanel-latch** (P2, CONFIRMED) [mustFix] — SettlementsPanel mount saves-load dropped its cancellation latch (regression vs master UI base)
  - claim: The mount effect (SettlementsPanel.jsx:210-214) calls savesService.list().then(setSaves).catch(console.error) with NO cancelled/AbortController guard and NO cleanup return — grep confirms 0 occurrences of cancelled/AbortController/cleanup-return/reportError in the whole 814-line file. This is the census-flagged dropped 'cancelled latch (cross-user library-write guard) + reportError seam' (arch §4 census, 2026-07-19), still unretired.
  - anchor: src/components/SettlementsPanel.jsx:210
  - fix shape: Restore the `let cancelled=false` + `return () => { cancelled = true }` cleanup guarding both setSaves and setSavesLoading, and route the catch through the Wave-E reportError seam instead of bare console.error. Same shape master shipped.
- **prestocked-homesample-eyebrow-aa** (P3, CONFIRMED) — HomeSampleDossier amber callout eyebrow fails WCAG AA contrast
  - claim: The Friday's-session callout renders eyebrow text in AMBER swatch['#D08020'] on bg '#FBEAD0' (HomeSampleDossier.jsx:43 config, :139 `color: accent`). Computed contrast = ~2.62:1, below the 4.5:1 AA floor (the eyebrow is small small-caps text). No contrast test covers HomeSampleDossier. The green (#4A7A3A/#E2EEDB) and slate (#5A6E82/#E4E9EE) callouts are borderline but pass; only the amber pair fails.
  - anchor: src/components/home/HomeSampleDossier.jsx:139
  - fix shape: Use a darkened ink token for the eyebrow TEXT (retain accent for the 3px left border), or darken AMBER for text use; extend a contrast pin over the three callouts. Hand to the a11y lane.
- **prestocked-wizardnews-trycatch** (P3, CONFIRMED) — WizardNewsPanel.generateChronicle has no try/finally around the paid AI call
  - claim: generateChronicle (WizardNewsPanel.jsx:347) sets chronicleBusy=true, awaits requestCampaignChronicle, and clears busy only on the success-path tail — no try/finally. The census 'paid button can stick busy' flag. Mitigated primary risk: requestCampaignChronicle (campaignChronicle.js:27) has its OWN try/catch and never throws — it always returns {error} or {chronicle}.
  - anchor: src/components/map/WizardNewsPanel.jsx:347
  - fix shape: Wrap the body in try/finally with setChronicleBusy(false) in the finally — cheap robustness on a paid surface.
- **prestocked-decree-provenance** (P3, PLAUSIBLE) — DM proposal-apply path records no provenance cause-edge (decree-provenance gap)
  - claim: applyWorldPulseProposal (campaignWorldPulseSlice.js:580 → domain applyWorldPulse.js) commits the applied decree's outcomes WITHOUT routing through appendPulseHistoryWithProvenance — the sole provenance writer, called only from pulseKernel.js (the advance path) and provenanceKernel.js. So the DM-decree apply path never mints a recorded parent edge.
  - anchor: src/store/campaignWorldPulseSlice.js:580
  - fix shape: Either route the apply commit through appendPulseHistoryWithProvenance with the proposal id as the parent edge, or confirm the decree's receipt already lands in pulseRecord at generation time (needs an execution trace). Hand to the correctness/coherence lane.
- **prestocked-sharedwar-seam** (P3, CONFIRMED) — task-#40 seam: shared-war-coalition arm — FOUR-GATE verdict = DEFER
  - claim: factionPairLedger.js ships the D-7c pair ledger shape+decay+deposit API; the co-governing 'coalition_standing' trust build (factionCompetition.js:359) and cross-faction contest resentment (ladder deposits) ARE wired. But the war-coalition arm (coalition_betrayal→pair resentment; shared-war-year→pair trust) and the FEEDS side (alliance formation/durability reading .trust) have NO consumer — nothing anywhere reads factionPairStates.trust. Gate (a) existing-machinery-both-sides FAILS: the FEEDS con
  - anchor: src/domain/worldPulse/factionPairLedger.js:31
  - fix shape: Record as a ledger deferral row (deferred-seam). vNEXT: wire coalition_betrayal→resentment + the alliance-formation .trust read + the settlement→faction identity bridge, together, when memoryWeave is scheduled to light.
- **prestocked-unbonded-payer-seam** (P3, CONFIRMED) — task-#40 seam: unbonded-conscience payer channel (D-5) — DEFER (+ design/code divergence)
  - claim: thirdPartyRansom.js produces the succor_unbonded motive ONLY for creditors (obligationHomeOwes>0) and trade relations (thirdPartyRansom.js:213-216). The design §9.1 three-body matrix row 'succor (unbonded conscience) | H↔P relation = none' — a pure altruist paying out of generosity propensity (qualifiesForGenerosity) — is UNREACHABLE: a settlement with no relation/obligation/trade/rivalry `continue`s (line ~219). The design's 'qualifiesForGenerosity(payer,home) OR obligation' candidate criterion
  - anchor: src/domain/roads/thirdPartyRansom.js:213
  - fix shape: Record as deferred-seam AND note the design/code divergence for the record. vNEXT: add the qualifiesForGenerosity candidate branch with a bounded scan; only worth it if D-5 is ever scheduled to light.
- **prestocked-covenant-ui-seam** (P3, CONFIRMED) — task-#40 seam: champion covenant UI — DEFER (display lane, new read surface)
  - claim: The champion/backedBy producer (D-4e) is built (npcLadderContest.js). Surfacing the covenant to the DM needs a new npcLadderState mirror exposing nids PAST the secrets seam (design §'THE SECRETS SEAM': 'no new share surface is created'). Gate (a) fails (requires a new read surface); it is a display-lane item inside launch-dark memoryWeave.
  - anchor: src/domain/worldPulse/npcLadderContest.js:167
  - fix shape: Record as deferred-seam (display lane, post-launch). Any build must add the nid mirror without widening the secrets share surface.
- **prestocked-thirdparty-news-seam** (P3, PLAUSIBLE) — task-#40 seam: third-party news pool — DEFER (launch-dark roads/ransom)
  - claim: Ransom/rescue outcomes emit NOTABLE news to the involved settlements; propagating the event into bystander/third-party settlements' rumor pool (third places learning of it) is unwired. It sits inside launch-dark thirdPartyRansom/memoryWeave and overlaps the coherence-matrix seed (returning travelers write the home rumor ledger). Gate (c) launch-effect fails (dark).
  - anchor: src/domain/roads/thirdPartyRansom.js:1
  - fix shape: Record as deferred-seam; reconcile with the coherence lane's traveler-writes-home-rumor seed (same read-side rumor machinery) so it is built once, not twice. Exact seam identity not fully pinned this pass.
- **prestocked-rawcolor-debt** (P3, CONFIRMED) — Raw-color occurrence budget = 1403 grandfathered debt (only-shrinks ratchet green)
  - claim: rawColorLiteral.test.js BUDGET=1403 (line 58), lowered from 1450 during the deep-craft burn-down (1450→1443→1403). ~1403 raw color literals remain grandfathered; the '23-literal tokenize-down (1450→)' pre-stocked reduction is partly executed. The ratchet forbids growth (green in baseline) but the debt persists.
  - anchor: tests/lint/rawColorLiteral.test.js:58
  - fix shape: Continue opportunistic tokenize-down in craft waves, lowering BUDGET each time; no forced sweep required.
- **prestocked-fmg-currency** (P3, PLAUSIBLE) [OWNER-QUEUE] — FMG-fork upgrade currency — no pinned upstream version; manual cadence
  - claim: docs/fmg-fork.md carries the upgrade runbook but pins NO upstream Azgaar version/tag, so drift vs upstream cannot be measured from the repo. The fork sits outside all code gates by design; its only automated gates (validate:map acorn-parse in `npm run check`:38, and tests/build/vendorManifestExactSet) run and are green, but they check syntax/supply-chain set, not upstream currency.
  - anchor: docs/fmg-fork.md:31
  - fix shape: Owner decision: record the current FMG base tag in fmg-fork.md and set an upgrade cadence; the runbook itself is sound.
- _ceiling note_: No hard pre-launch ceiling on this dimension: A+ is reachable within the loop. The four task-#40 seams are correctly DEFERRED not because the loop lacks power but because all four live inside launch-dark features — thirdPartyRansomEnabled and memoryWeaveEnabled are BOTH absent from the seven ONE-REGEN flags (arch §7: distancePricedNews · reframe · provenanceLedger · urbanFabric · npcGrowth · spati

### completeness-critic
- **critic-1** (P2, CONFIRMED) [mustFix] — Panel blind spot: the account/auth/ops edge-function set (12 of 28) reviewed by no lane
  - claim: No lane's coverage list named account-actions, admin-actions, analytics-export, auth-recovery, create-customer-portal, ingest-events, og-image, send-email, verify-checkout-session, retention-warning-cron, pricing-resync-cron, or log-client-error's interior; the security and AI-cost lanes scoped themselves to the AI + founder-transfer functions. I probed nine of the twelve this session and all postures held (stripe-webhook constructEventAsync before any DB write at supabase/functions/stripe-webho
  - anchor: supabase/functions/retention-warning-cron/index.ts:1
  - fix shape: One bounded read-level lane over the four unprobed functions (retention-warning-cron, pricing-resync-cron, log-client-error, account-actions interiors), banking receipts like the nine I probed. No code change expected — this is evidence-closing, not repair.
- **critic-2** (P2, CONFIRMED) [mustFix] — The signed-out auth funnel (10 components) has no lane coverage this round, including a11y
  - claim: src/components/auth/ (SignInPage, RegisterPage, ForgotPasswordFlow, SecurityQuestionsFields, ConfirmEmailPage, VerifyEmailPage, ResetPasswordPage, SetNewPasswordPage, AuthPanel, authUI) appears in no lane's coverage list; the a11y lane's B+ grade explicitly scoped to 'newest interactive surfaces'. Spot probes clean: authUI.jsx carries aria-labelledby labeling (:116-130) and aria-live status regions (:274), and the auth-recovery edge function's lockout design is sound — but nobody walked the regi
  - anchor: src/components/auth/authUI.jsx:274
  - fix shape: One flow-level read pass over the auth funnel (state machine per page, error paths, a11y of SecurityQuestionsFields specifically) — my kit-level probes cover the primitives, not the flows.
- **critic-4** (P2, CONFIRMED) [mustFix] — Dependency / supply-chain currency is owned by no lane; og-image takes a request-time esm.sh dependency
  - claim: No lane audited npm advisories or version currency (package.json floats carets: dompurify ^3.4.8, @supabase/supabase-js ^2.103.0 client-side vs 2.108.2 pinned in edge functions; deno std pinned at 0.168.0/0.177.0). og-image imports @resvg/resvg-wasm from esm.sh AT REQUEST TIME (supabase/functions/og-image/index.ts:255-257), coupling a public endpoint's availability and integrity to a third-party CDN on every cold path. The A_PLUS_ROADMAP itself treats this class as real (edges.4 'bump dompurify'
  - anchor: supabase/functions/og-image/index.ts:255
  - fix shape: Structural, not a patch: add a dependency-currency check to CI (npm audit at a chosen severity + a pinned-versions manifest for the deno/esm.sh imports with a walker test), and evaluate vendoring or version-pinning+integrity for og-image's runtime wasm fetch. The og-image vendoring choice touches a deployed public surface — flag that half to the owner at deploy time.
- **critic-3** (P3, CONFIRMED) — Gallery/library UGC and import surfaces uncovered — probes clean
  - claim: GalleryComments, GalleryCard/GalleryDetail description rendering, galleryImportSettlement, and accountImportSlice appear in no lane's list. Probes: comments are auth-gated plain text with maxLength 2000 (GalleryComments.jsx:44,88); both description sinks sanitize via sanitizeGalleryHtml/dompurify (GalleryCard.jsx:33, GalleryDetail.jsx:209); a full-src dangerouslySetInnerHTML sweep found all 10 sinks sanitized, static, or locally-built SVG. The import slices remain unread by anyone this round.
  - anchor: src/store/galleryImportSettlement.js:1
  - fix shape: Fold the two import slices into the same read-level pass as critic-1; the UGC render surfaces need nothing (receipts banked here).
- **critic-5** (P3, PLAUSIBLE) — No lane verified any surface at mobile widths
  - claim: Responsive behavior is a dimension without an owner: matchMedia machinery exists in 8+ older components (AdminPanel, AccountPage, AuthModal, CompendiumPanel, HomeLanding, GenerateWizard, OutputContainer, FeedbackWidget), but the newest paid surfaces — money/seat-transfer panels, THE ONE DOOR, roads/travelers overlays, chronicle modals — were reviewed exclusively at desktop assumptions by the a11y and game-feel lanes.
  - anchor: src/components/account/AccountSeatTransferPanel.jsx:1
  - fix shape: A viewport-sweep pass (375px/768px) over the newest paid surfaces, or an explicit owner ruling that mobile is out of scope pre-launch (which would make this a recorded deferral, not a gap).
- **critic-6** (P3, CONFIRMED) — No lane swept the 'AI output → DOM' class; the one suspicious sink probed safe
  - claim: The security lane covered AI input-side abuse (rate guards, BYOK) and the AI-cost lane covered spend, but no lane owned where AI RESPONSES land in the DOM. I swept: StyleOverhaulPanel.jsx:161 injects previewSvg raw, but it is locally built by buildTownMapSvg from style parameters (:98), not AI markup — residual risk is AI-returned style tokens flowing into SVG attributes behind the schema wall, compounded by ai-cost-2's finding that schema walls are parse-based rather than tool-forced.
  - anchor: src/components/surveyor/StyleOverhaulPanel.jsx:161
  - fix shape: One targeted check that the style-overhaul response validator allowlists/clamps every token that reaches SVG attribute interpolation (regex-pin colors, enum-pin names); pairs naturally with ai-cost-2's tool-forced-output fix if the owner takes it.
- _ceiling note_: Deployed-environment verification of the edge-function set (real Stripe events, real Resend sends, real rate-limit behavior under load) and real-device mobile testing are structurally out of loop power (no deploys, no installs, no devices) — they belong to the post-push soak. Coverage receipts at the code level are the honest ceiling pre-launch.
