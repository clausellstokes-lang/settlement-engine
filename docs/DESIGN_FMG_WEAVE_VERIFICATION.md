# DESIGN_FMG_WEAVE_VERIFICATION — the eight verification lane reports (2026-08-29, workflow wf_089ba33d-e4e)

**The receipts layer for `docs/DESIGN_FMG_WEAVE.md`. Where the volume and these reports disagree, the quoted code here wins. Reports verbatim; nothing edited.**


---

# V-GENESIS

I have everything needed. Compiling the report.

**LANE V-GENESIS REPORT — the t=0 diplomacy premise (gates candidate #5)**
All paths relative to `.../fmg-study/ours/` (build tip 73f5dfc02). Denominators stated per claim.

---

## 1. THE PREMISE: "diplomatically cold at genesis" — **CONFIRMED**, and stronger than the review claimed

Read IN FULL: `src/lib/instantWorld/composeInstantWorld.js` (397 lines), `src/domain/instantWorld/worldPlan.js` (224 lines), `src/domain/region/discoverDependencyCandidates.js` (482 lines), `src/domain/region/graph.js` (lines 1–700 of 1000), `src/domain/worldPulse/worldState.js` (795 lines).

**No genesis pass writes any inter-settlement relation, rivalry, or war. Four independent mechanisms guarantee it:**

**(a) The composer stages nothing diplomatic.** `composeInstantWorld.js:213-343` — the full assembly is: mint members → `dedupeWorldFactionNames` → `placements` → `deriveGraphWithDiscoveredCandidates` → `ensureWorldState({simulationRules: tonePreset.rules}, …)` → mapState → campaign. No relation, edge label, war, or deployment is written anywhere in the file. The doc header (:30-35) promises only "N canon member saves, a v2 mapState with placements + a map PLAN, a discovered regionalGraph, and a worldState that is NOT spatially canonized."

**(b) worldState is born diplomatically empty.** `worldState.js:301-341` `createDefaultWorldState`: `relationshipStates: {}`, `deployments: {}`, `warExhaustion: {}`, `tradeWarState: {}`, `stressors: []`; `warPosture`/`occupations` are conditional ledgers "absent while at peace" (:352-356) and the composer passes only `{simulationRules}` into `ensureWorldState` (composeInstantWorld.js:288-291), so every one of these is empty/absent at t=0.

**(c) The relation-discovery arm has nothing to read.** Relations enter the regional graph ONLY from members' `neighbourNetwork` links (`graph.js:423-481`, `discoverDependencyCandidates.js:39-59`) — and instant members have none: zero writers of `neighbourNetwork` exist under `src/generators/` (grep over the whole directory, 0 hits for a write), and the one deriver `withNeighbourNetworkFromRelationship` (`lib/saves.js:233-250`) triggers off `settlement.neighborRelationship`, which is a **config-patch key** from the manual "generate as neighbour" wizard flow (`store/configSlice.js:85` — it lives in `CONFIG_PATCH_EXTRA_KEYS`, not in `DEFAULT_CONFIG`), and `memberConfigFor` (composeInstantWorld.js:110-120) spreads only `DEFAULT_CONFIG` + settType/randomize/magic. So `relationBetween()` returns null for every member pair and `discoverRelationshipChannels` returns `[]` (:178 `if (!rel) return out.filter(Boolean)` with empty `out`). What discovery DOES mint is goods/route-based **trade texture only**: `trade_dependency`/`export_market`/`trade_route`(/R3 `service_dependency`/`migration_pressure`), all born `status:'suggested'` (:221-226 `candidate()`), DM-confirm-gated. The only "edges" in the fresh graph are the auto-minted pair stubs `relationshipType: 'channel_inferred'` (graph.js:310) — not a diplomatic label.

**(d) Even the neutral substrate is dark.** `neutralNeighborsEnabled` is "DELIBERATELY LIT IN NO PRESET" (simulationRules.js:471-503, perf-ratchet-blocked, owner-gated) — so the snapshot's relationship layer at genesis is an edgeless graph even for neutral edges (`worldSnapshot.js:158-160`).

The review's hedge at its line 45 ("`worldPlan.js:174-222` seeds sites/per-settlement seeds only — PLAUSIBLE, composer unread") upgrades to **CONFIRMED**: `deriveWorldPlan` (:177-224) returns exactly `{seed, realmSize, tonePresetId, magic, mapSeed, mapKind, requestedMapKind, sites[]}`; sites carry `{slot, tier, seed, x, y, burgId}` and nothing relational.

**One near-miss worth recording:** the "cold" property is *load-bearingly fragile* against name collision. `findTargetSave` (graph.js:378-387) and `relationBetween` (discoverDependencyCandidates.js:45-48) match neighbour links **by name** as fallback. Today instant members carry no links, so no collision path exists — but any future pass that gives members generated fictional neighbour links would let a fictional "Oakvale, hostile" accidentally bind to a real sibling named Oakvale. The genesis-diplomacy pass, if built on links (recommended below), sidesteps this by using real save ids.

---

## 2. THE SUBSTRATE a genesis bootstrap would write into

**Relations live in TWO planes, plus a third mirror:**

**(i) Structural home: `campaign.regionalGraph.edges`** (`graph.js:19` typedef; `normalizeEdge` :172-184): `{ id: edgeIdFor(from,to) = 'edge.<from>.<to>', from, to, relationshipType, status:'active', channelIds:[], evidence:[{source, reason, outcomeId?}], updatedAt }`. Vocabulary = the **REGIONAL plane**: structural labels including `war`/`subject`/`tributary` kept as first-class, aliases folded by `canonicalRelationshipLabel` (`domain/relationships/canonicalRelationship.js`, `RELATIONSHIP_LABEL_ALIASES`: ally→allied, overlord/suzerain/liege→vassal, trade_partners→trade_partner, coldwar→cold_war…).

**(ii) Effect-profile home: `worldState.relationshipStates`** keyed by `relationshipKeyFromEdge` (= `edge.id`; relationshipState.js:326-331). **Rows materialize LAZILY**: `ensureRelationshipState(edge, existing)` (:427-502) reads `existing.relationshipType || edge.relationshipType || 'neutral'` and fills every numeric from `RELATIONSHIP_DEFAULTS[type]` (:25-116 — ten typed rows: neutral, trade_partner, allied, patron, client, vassal, rival, cold_war, hostile, criminal_network, each with trust/resentment/dependency/leverage/fear/tradeBalance/pactStrength). Vocabulary = the **PLANE aliases** (`RELATIONSHIP_PLANE_ALIASES`: war/enemy→hostile, subject/tributary→vassal). ⚠ The two planes deliberately disagree on 17 of a 40-input corpus, pinned by `tests/domain/regionalNeighbourSeam.test.js`; merging is owner-gated (ODQ §64.2). **A genesis pass therefore need write NO relationshipStates row at all** — writing only the typed edge yields correct numbers by lazy materialization, and keeps worldState byte-identical to today's shape.

**(iii) The mirror: both settlements' `neighbourNetwork` links.** H10 declares "the saves' neighbourNetwork is the canonical relationship source" (graph.js:440-447 — rebuilds refresh edge labels from live links); H11 makes the pulse write label changes back to BOTH ends' links (`applyWorldPulseRelationshipGraph.js:180-227`, stamping `relationshipType, relationshipFrom, relationshipTo, localRelationshipRole, displayRelationshipType, updatedByPulse: tick`). Dossier/threat/PDF/AI read the links, not the graph.

**Channels derived from a label:** `relationshipChannelBundle` (graph.js:636-695) converts one typed edge into its confirmed channel set — hostile ⇒ 2× `war_front` + 2× `resource_competition`; vassal ⇒ political_authority + military_protection + tax_obligation + 2 gm information_flow; allied/trade_partner/rival/criminal each their own bundle. Channel type vocabulary is closed: `REGIONAL_CHANNEL_TYPES` (graph.js:47-64, 13 types).

**How wars start today (the ONE opener), `warDeployment.js` `evaluateWarLayer` step 4 (:888-1077)** — preconditions, in gate order:
1. `simulationRules.warLayerEnabled === true` (pulseKernel.js:910) — **lit only in `dramatic_campaign` and `full_simulation`** (simulationRules.js:544, 608); the instant-world tones map quiet→quiet_local, realistic→realistic_regional, dramatic→dramatic_campaign (worldPlan.js:69-74), so only the Dramatic tone can ever run wars.
2. Target census `hostileTargetsOf` (warIntent.js:377-418): edges whose lazily-ensured state type ∈ `HOSTILE_TYPES = {hostile, cold_war, rival}` (:95), both ends canon members, minus treaty-blocked pairs.
3. Hard gates per besieger (warDeployment.js:937-1002): one-army; not an active intervener; army not just returned; not besieged at home; **"MOBILIZATION POSTURE GATE (the keystone): a settlement cannot launch a serious siege from peace"** (:989-999 — must have ramped to mobilized over prior ticks via `evaluateMobilization`); strength ≥ `HOSTILE_CONFIDENCE` (:1002); per-target `CONQUEST_MARGIN` strength-delta pre-filter (:1065); and the keystone `classifyFeasibility` verdict (:1067-1075).
4. Critically for a genesis *ongoing* war: **"Pre-seeded sieges already in the graph are resolved above regardless of posture; this gate only governs OPENING a NEW one"** (:992-994) — so a bootstrap that writes a `deployments[attackerId]` record + a `war_front` channel IS mechanically consumed by steps 1–3 of the same evaluator. (CONFIRMED for the gate comment; the resolution path for a hand-seeded record is PLAUSIBLE — I did not execute it, and the deployment record shape at :1079+ carries a capacity model seeded "from the origin's capacity model at deploy" that a bootstrap must reproduce.)

**What a genesis row must look like — and the distinguishability call:** A *lived* relation carries scars a genesis row must NOT fake: `updatedByPulse` tick on links, `lastTransitionTick`/`turningPoints`/`history`/`recentIncidents` in state rows, `outcomeId`s in evidence. The right target is **indistinguishable from a DM-authored t=0 relationship** (matching the composer's composition-equivalence spec, :30-35: "state-shape-identical to a hand-built realm at the same stage") — i.e., exactly what `deriveRegionalGraphFromSaves` mints from an authored link: edge with `evidence:[{source:'neighbourNetwork', reason:'Linked as hostile.'}]`, no tick stamps, no history. Provenance should ride the LINK, echoing the existing `fromGeneration: true` precedent (`lib/saves.js:250`): a `fromGenesis: true` (or `source:'genesis_diplomacy'`) marker plus an F4-style reason prose in `description`. Safe because: the H10 refresh only strips evidence rows with `source === 'neighbourNetwork'` (graph.js:453) and the only prefix-gated source is `war_layer*` (graph.js:511-514) — a novel source string flows through every reader inert. Do NOT write tick-stamped `turningPoints`/history (fabricated lived history; THE PROMISE constrains canon to founding-time generation — a founding-time *type + reason* is generation, a fake transition-at-tick-N is history).

---

## 3. INSERTION POINT, PRNG STREAM, ORDERING HAZARDS

**Insertion point (two halves, matching the codebase's own seams):**

**(a) Assignment — in `deriveWorldPlan` (worldPlan.js:177-224), as a new plan field** (e.g. `plan.relations: [{fromSlot, toSlot, relationshipType, reason}]`), keeping the composer rng-free and the plan the single pure source. Slot-addressed, because save ids don't exist yet and are non-deterministic under the store's real-uuid `idFactory` (composeInstantWorld.js:41-45 scopes determinism to content, ids excluded).

**(b) Materialization — in `composeInstantWorld` between `dedupeWorldFactionNames(settlements)` (:251) and `deriveGraphWithDiscoveredCandidates(...)` (:266)**: write reciprocal `neighbourNetwork` links into both members' `settlement` objects (link `id` = the sibling's minted save id, resolving slots→ids exactly as `placements` does at :255-263), then let the EXISTING pipeline derive everything else: `deriveGraphWithDiscoveredCandidates` → `deriveRegionalGraphFromSaves` lifts the links into typed edges (graph.js:423-481) and `discoverRelationshipChannels` mints the correct channel texture per type — hostile pairs additionally get trade-suppression free via `NO_TRADE_RELATIONSHIPS` (discoverDependencyCandidates.js:294-298). This one ordering keeps H10/H11's two-source convergence true from birth and requires no new writer into the graph. The optional dramatic-tone *ongoing war* (deployments row + confirmed `war_front`) would additionally write `worldState.deployments` before the `ensureWorldState` call at :288 — a heavier, separately-gateable increment.

**PRNG stream:** fork the plan's root — `createPRNG('instantWorld::' + outerSeed).fork('diplomacy')` (derived seed `instantWorld::<seed>::diplomacy`). CONFIRMED safe: `fork` is keyed derivation, not stream consumption (`prng.js:84` — `fork: (label) => createPRNG(\`${seed}::${label}\`)`), so the existing root draw (map-kind pick, worldPlan.js:183) and the `'sites'` fork (:190) are untouched and every existing plan byte is preserved. Label `'diplomacy'` contains no `::` and doesn't collide with the reserved `epoch` head (prng.js:99-104) or the existing `'sites'` family. Do NOT touch `worldState.rngSeed` (`world-pulse:${campaignId}` — campaign-id-derived, non-deterministic under real uuids).

**Ordering hazards (ranked):**

1. **⛔ The adjacency prior has no geography to read at compose time.** The review's sketch ("adjacency class from k-nearest, J-D2's k=3, `autoplacement.js:88-89`") collides with two CONFIRMED facts: (i) FMG geometry is iframe-bound and materializes only when the realm map first opens (`hooks/useInstantWorldMaterialize.js:1-22` — "FMG is iframe-bound, so its geometry cannot be generated headlessly"; `mapState.pendingMapGen`); (ii) autoplacement + placementRaster run browser-side post-materialize (consumers: `components/map/WorldMapStage.jsx`, `store/mapSlice.js` — none reachable from the composer). The only positional signal at compose time is the plan's scatter, which is **declared cosmetic**: "every site is freely draggable at t=0, so exact coordinates are cosmetic, not load-bearing (they are excluded from the coherence contract and the structural fingerprint)" (worldPlan.js:126-130). Basing diplomacy on scatter k-nearest silently promotes a documented non-contract to load-bearing. Options for the architecture lane: tier-ratio + drawn-table assignment with no spatial prior (fully headless, FMG's own dominant signal anyway — their vassalization rule is area-ratio); or take F1's influence-partition as the adjacency prior *only if* F1 itself is redesigned to run headlessly at compose time (it can't run on the FMG raster pre-materialize either); or explicitly ratify the scatter as load-bearing (a contract amendment, and it drags coordinates into the fingerprint).
2. **The discovery call consumes the links — write before :266.** Written after, the war_front/hierarchy texture and hostile trade-blocking are silently absent until the next graph rebuild.
3. **The fingerprint doesn't cover edges.** `instantWorldFingerprint` (:361-386) captures channels/sites/members but NOT `regionalGraph.edges`. Genesis-relation channel bundles would appear in `channels` (slot-mapped), but the typed relations themselves must be added to the fingerprint slot-addressed, or the determinism pin has a blind spot exactly where the new feature lives.
4. **First-tick consumers, in pulse order** (`pulseKernel.js` stages): actor_memory relaxes relationship numerics toward baseline from tick 1 (:358-364 — genesis rows are pure defaults, so reversion is a fixpoint; no drift); corruption-web reads hostile/rival channels as covert-asset lanes (:391-403); condition_aging touches edges only via resolved war stressors (:436-441, none at genesis); mover_planes — `evaluateMobilization` reads `buildWantsWarLookup` + hostile edges and starts posture ramps **on tick 1** (:791-810), so under Dramatic tone a genesis rivalry organically brews to a first NEW siege only after the ramp + confidence + feasibility gates — this is the designed-in slow burn, not a bug; `evaluateWarLayer` would resolve any genesis-seeded *ongoing* siege on tick 1 (posture-exempt, warDeployment.js:992-994). Under quiet/realistic tones a genesis-seeded *war* would freeze un-resolvable (war layer no-op) — so the ongoing-war option must be tone-gated to `dramatic_campaign`, exactly as the review sketched.
5. **Behavior-shift declaration owed:** the pass adds only new-fork draws and additive rows, so members/sites/map bytes are unchanged for a fixed seed — but the composed bundle and its fingerprint move for every seed. Same-seed instant worlds minted before vs. after differ. Legitimate (new-founding generation), but it is a golden/fingerprint one-time shift the owner signs (doctrine: never let it ride silently).
6. **Suzerain-closure fit:** their closure (vassal copies suzerain's relations) belongs in the plan-side assignment (pure, before materialization); the substrate already handles vassal direction — edges are authored senior-side-`from` (graph.js:627-629), with `overlordSaveId` stamps only for pulse-driven flips (relationshipState.js:370-393) — a genesis vassal edge should use strict edge direction, no stamp, matching the DM-authored idiom.

**Constitutional frame:** all findings compatible — new-founding-only writes (THE PROMISE holds), keyed fork only (no stream theft), typed closed vocabularies already exist on both planes (finite semantics — the bootstrap must draw types from `RELATIONSHIP_DEFAULTS`/regional structural vocab, never mint a new label), fully headless if and only if hazard #1 is resolved without the FMG raster, and dormancy is inapplicable in the good direction (the composer runs only for new instant realms; existing campaigns never re-enter it — `pendingMapGen` hook is inert once `fmgSnapshot` exists).

---

# V-NAMING

# LANE V-NAMING — VERIFICATION REPORT (naming substrate; gates candidates #1 and #4)

All paths under `.../fmg-study/ours/` are the build-tip extract of 73f5dfc02; tests/vite.config cited via read-only `git show 73f5dfc02:` (nothing in the working tree touched; no gate run; the one execution was a node import of the pure data module `namingData.js` to count pools).

## 1. CENSUS — every engine name-minting call site

Sweep method + denominator: grep over all of `src/**` for `settlementPrefixes|settlementSuffixes` (hits in 4 non-data files), `maleNames|femaleNames|surnames` (3 files), `NAMING_DATA` importers (10 files, listed below), a `name: pick|name: rng|name: draw` sweep over `src/domain` (2 hits), plus targeted reads of every hit's full enclosing scope. **Result: 8 minting families — 5 engine-record mints, 1 flag-gated derived-view family, 1 hash-titled mint, 1 exported-but-unwired mint.** All CONFIRMED unless marked.

**Settlement names (2 engine paths + 1 derived view):**
- **S1 Genesis** — `src/generators/steps/assembleSettlement.js:105` `const settlementName = (effectiveConfig.customName?.trim()) || generateSettlementName(culture);` → `src/generators/npcGenerator.js:1686-1690`, two draws of the **ambient pipeline stream** (`import { random as _rng } from '../kernel/rngContext.js'`, npcGenerator.js:15): `prefixData.settlementPrefixes[Math.floor(_rng() * …)]` + suffix. Draw position is pinned by comment (assembleSettlement.js:101-104 "preserves its exact draw position").
- **S2 Lifecycle foundings (steadings)** — `src/domain/worldPulse/settlementLifecycleKernel.js:374-379` `drawSteadingName(parent?.culture, {random: draw})` inside `mintSteading` (:417), on the keyed fork `satellite:<parentId>:<tick>` (:767, :1195). ":416 // Draw order is FIXED: name prefix, name suffix, founders spread." Header law :355-357: "NEVER generateSettlementName: that draws the GLOBAL generation rng (a draw-order hazard)." Name stored on the satellite record (`satellitesLedger.js:55` "name seeded from NAMING_DATA on the satellite fork").
- **S3 Ward/street names (derived view, flag-gated)** — `src/domain/townCartography/cartographyWards.js:309-330` `compileTownWardLayers`: `root = createPRNG(String(input.digest))` (the manifest's `mapModelDigest`), per-feature fork `root.fork(\`${family}:${id}\`)` (:166), families `carto:names:street` / `carto:names:ward` (:54-55), tails 'Way'/'Lane'/'Ward'. Names "never enter an id or a geometry" (:173-175). Pools **injected** across the lazy edge (CR-TC3A-1, `compileTownSceneManifest.js:505-515` throws when lit without injection); consumers `townScene.worker.js:41`, `townCartographyBlock.js:62`, `townSceneExport.js:161` all dynamic-import `namingData.js` only when `townCartographyActive(...)`.

**NPC names (3 mints):**
- **N1 Genesis** — `npcGenerator.js:98` `pickFirst(culture, gender, true, tier)` (:237-249), ambient `_rng`; surname pool is **tier-sliced** (:245-247: thorp uses first 12% of surnames, metropolis 100%).
- **N2 Successor at tick** — `src/domain/worldPulse/successorNpc.js:34` `name: pick(FIRST_NAMES)` — its **own 27-entry, culture-blind, first-name-only pool** (:13-17). Cohesion gap: a germanic town's ousted Bürgermeister can be replaced by "Yara"; no surname, no culture linkage.
- **N3 Instant NPC** — `src/domain/npc/npcOps.js:218-230` `createPRNG(\`instant-npc:${seed}\`)` + `pickFromNaming` (:193-199). **Zero production callers at 73f5dfc02** (`git grep instantNpc -- src` → only npcOps.js itself); built for the lazy NPC UI per header, not yet wired.

**Faction names** — `src/generators/power/factionGrouping.js:54-67`: draws from authored `FACTION_DESCRIPTORS[dominantCategory]` via ambient `pick`, **draw-count-variable retry loop** (up to 5 rerolls + suffix). `factionDedup.js:44-47` documents the hazard: "a draw-count-VARIABLE retry loop, so widening it would perturb the per-settlement rng stream and cascade the generator golden"; the dedup pass itself is rng-free over the composed bundle.

**Institution names** — NOT minted; authored catalog rows (`institutionalCatalog.js`, ids kernel-slugified from names, :2501-2506). Per-culture **titles** are a 27-role lookup map per culture (`NAMING_DATA[culture].titles`, read at `npcGenerator.js:251-259` `pickLast` — lookup + humanize backstop, no draw).

**Calamity titles** — `src/domain/worldPulse/calamityKernel.js:450` `name: pickLine(CALAMITY_TITLES, \`${settlementName}::${year}\`, …)` — a persisted proper-noun-ish title minted by **pure FNV-1a hash over a stable key, zero rng draws**. The closest existing precedent for the war-name deriver.

**Realm/state names** — none minted. `composeInstantWorld.js:211` defaults the campaign name to `` `Instant Realm ${plan.seed}` `` (mechanical, not cultural); otherwise user-supplied. `contextAnchor.js:53` falls back to the literal 'the realm'.

## 2. `src/data/namingData.js` (4,038 lines, read + counted by execution)

Shape: single frozen-by-convention export `NAMING_DATA` (:4), pure data, no helpers, no other exports. **11 culture keys, not the review's 9** — `germanic, latin, celtic, arabic, norse, slavic, east_asian, mesoamerican, south_asian, steppe, greek` (:5/:426/:809/:1174/:1543/:1899/:2292/:2673/:2997/:3372/:3695). `resolveConfig.js:352` itself says "picked from canonical **11-culture** catalog." Each entry: `{name, description, settlementPrefixes[30], settlementSuffixes[30], maleNames[81-119], femaleNames[68-103], surnames[62-96], titles{27 roles}}`. Executed counts (CONFIRMED): all 30×30, **but with in-pool duplicates** — norse prefixes 2 dupes (Orm, Ulf twice), slavic suffixes 8 dupes (22 unique → ≤660 distinct combos, not 900), east_asian suffixes 2, greek suffixes 1.

**Culture selection is settlement-scoped and resolved differently by every consumer** (a §711.6-class cross-consumer divergence, but for a string enum):
- Genesis: `resolveConfig.js:159-167` — `random_culture`/absent → seeded pick; else `resolveCultureProfileKey` (`src/data/cultureProfiles.js:539-544`: alias `mediterranean→'latin'` (:527-529), unknown → `'mixed'`).
- NPC/settlement name draws: `resolveNameCulture` (npcGenerator.js:230-235) — `'mixed'` → **fresh random culture per name**; unknown (including `'mediterranean'`, which NAMING_DATA lacks) → `'germanic'`.
- Steadings: `namePools` (kernel :359-370) — unknown **and `'mixed'`** → `'germanic'`.
- Wards: `namePoolsFor` (cartographyWards.js:124-153) — unknown/`'mixed'` → **flattened union of all 11 cultures** in codepoint order.

So one token, three answers: a `mediterranean` settlement gets a **latin** culture profile, **germanic** NPC/steading names, and **all-cultures** ward names; a `mixed` town gets cross-cultural NPCs, germanic steadings, union-pool wards. CONFIRMED from the four read scopes. The generator-golden corpus (`tests/fixtures/generator-golden-master.json`, 525 rows keyed `tier|culture|…|seed`) covers all 11 keys **plus `mediterranean`**, so this behavior is golden-pinned as-is. The Markov module should centralize culture resolution or it inherits this fork.

## 3. Route names and war proper-nouns — re-verified ZERO

- **Routes**: `RouteEdge` typedef (`routeNetworkLedger.js:116-131`) — `a`, `b` (codepoint-sorted endpoints), `grade`, `mode`, `charter`, `provenance`, optional `usage/bypass/strategicNeed`. **No name property.** `grep -n "name" routeNetworkGenesis.js` → one comment (":241 a row names its two…" endpoints). CONFIRMED grep-zero.
- **Wars**: `grep -rln "settlementPrefixes|NAMING_DATA|pick.*[Nn]ame"` across all 56 `war*.js` + `tradeWar.js` + `supplyWebWarfare.js` → **exit 1, zero files**. The lone `name?` in `warReasons.js:710` is the `WarSnapItem` settlement-snapshot typedef, not a war name. `warName|nameWar|campaignName` hits are all the user's campaign object. CONFIRMED grep-zero.

## 4. Markov module design substrate

**(a) Where corpora live.** `src/data/` frozen pure leaves; the established split is corpus-in-data + domain boundary re-export (`src/domain/cultureProfiles.js` is a 466-byte re-export shim over `src/data/cultureProfiles.js` "so the meaning of the culture dial has one public contract") — the review's `src/domain/naming/` matches this if corpora ride `src/data/`. `*.generated.js` precedent exists for precompiled tables (`dossierCausalProse.generated.js`, `institutionServiceKeys.generated.js`). **Do not touch NAMING_DATA**: `tests/data/namingDecontamination.test.js` pins exact pool sizes ("1-for-1 replacement — rng draw stability", e.g. `ea.maleNames.length === 110`) and pins specific contaminants OUT — new corpora are a new file.

**(b) Module-eval vs lazy (first-paint budget).** namingData rides the **`data-lazy`** chunk: vite.config.js:353-370 derives eagerness from the eager module graph ("only eager-reachable tables ride the first-paint 'data' chunk… a missed eager edge only re-enters the closure, which the first-paint byte budget fails loudly. @enforced-by tests/build/vendorPdfLazy.test.js"); vendorPdfLazy holds a **monotone DOWN-only byte ratchet** (~1,253,970 measured + ~2 kB margin). The two static importers of NAMING_DATA (`npcGenerator.js:25`, `settlementLifecycleKernel.js:68`) are themselves in lazy chunks. For bounded worker chunks the idiom is CR-TC3A-1 **injection across the async edge** (worker fetches the pools chunk only when lit, `townScene.worker.js:41-44`; `cartographyWards.js:19-21`: importing the "68,656-byte table into the bounded" chunk is forbidden). ⇒ Chain-build-at-module-eval is safe **only inside a lazy leaf**; one static edge from an eager chunk reds the ratchet.

**(c) The keyed-PRNG idiom, exactly.** `src/kernel/prng.js:84`: `fork: (label) => createPRNG(\`${seed}::${label}\`)`. The `::` delimiter is "LOAD-BEARING VOCABULARY" (:66-83): `fork('a::b')` ≡ `fork('a').fork('b')` — "one label family, one spelling"; `tests/kernel/prngForkLabelDelimiter.test.js` freezes the embedded-delimiter families and reserves the root-segment head `epoch` (:87 `RESERVED_ROOT_SEGMENT_HEADS`). Changing the derivation is owner-gated under THE PROMISE (:80-83). Existing naming-fork spellings to imitate: `satellite:<parentId>:<tick>` (+`:site`), `carto:names:street`/`carto:names:ward` + `:{id}` per-feature ("exactly two draws off its own fork… so a reordered loop cannot steal another feature's entropy", cartographyWards.js:156-158), root-seed prefix `instant-npc:<seed>`. The review's `naming:<cultureKey>:<entityId>` is delimiter-safe (single colons, head not reserved). Never the ambient stream: upstream's `names-generator.ts:235` `const rnd = Math.random()` re-verified in the clone — the exact thing `rngContext.js` fails closed against (:8-18, throws on ambient draw with no active PRNG).

**⚠ Draw-order hazard the design must absorb (CONFIRMED, documented twice in-tree):** S1/N1 consume the ambient per-settlement stream, and names draw FIRST (assembleSettlement F8; mintSteading fixed order name→founders). A lit Markov path that stops consuming those ambient draws shifts **every subsequent draw** in that settlement/steading — the whole record moves, not just the name (the factionDedup.js:44-47 cascade class). So: dark path must keep the legacy draws byte-exactly; lit path is a declared same-seed shift, new-worlds/new-foundings-first — consistent with the review's framing, but the mechanism is "draw-stream displacement", stronger than "name strings change".

**(d) Dormancy oracles that police a dark naming flag** (which would red if "dark ≠ byte-identical"):
- `tests/property/generatorGoldenMaster.test.js` + `tests/fixtures/generator-golden-master.json` — 525 sha256 hashes of full settlement JSON across all cultures/seeds; any genesis-path draw or name perturbation reds it (shift-record discipline in header).
- `tests/data/namingDecontamination.test.js` — exact pool sizes/members.
- The same-seed byte-identity estate through `tests/helpers/dormancyOracle.js` (`normalizeForDormancy`: absent === {} === [] — an additive **empty** ledger is byte-neutral, a non-empty one is not); 89 dormancy-named test files at tip; for a route-name flag specifically `tests/property/roadsDormancyGolden.test.js` (full-advance dormancy golden + contract: "NO 'roads' ledger… even lit-war").
- Flag plumbing auto-coverage: a new boolean in `DEFAULT_SIMULATION_RULES` (`src/domain/worldPulse/simulationRules.js:41-144` bank) is picked up by `tests/domain/simulationRulesBooleanFailClosed.test.js` (explicit-true-only coercion; "pins the flags-off byte-identity invariant at the normalization chokepoint") and `simulationRulesPreset.stability.test.js` — both reconstruct the key bank from the live default surface.
- `tests/kernel/prngForkLabelDelimiter.test.js` — new fork-label families scanned; `tests/build/vendorPdfLazy.test.js` — first-paint bytes if the corpus leaks eager.

## 5. Herald/news surfaces where route/war names would render

- **Roads**: `src/data/roadsProse.js` (read in full, 278 lines) — lazy leaf flattened into `EVENT_PROSE_REGISTRY`; 14 event pools, all spelling roads as "the ${dest} road". Its own law 3 (:14-15): "PORTABLE SPECIFICITY… never a canon proper noun (the {npc}/{home}/{dest}/{captor} tokens ARE the world's names)" — a route name enters as a **new interpolation token** (e.g. `{road}`) supplied by the caller, exactly like the D-5 `{payer}` token (:223) was added; the pools' phrasing-only law means variants change, semantics don't.
- **Wars**: `src/domain/worldPulse/warCoalitionNews.js` (+ siblings `warCostsNews.js`, `warRulingsNews.js`) — pure leaves mapping typed receipt kinds to `HEADLINE` templates over `{settlement, counterpart, thirdParty}` (:27-40), joined to "the governed twelve-kind receipt corpus"; header: "It never discovers a call… from ambient state."
- **The display-side selection mechanism**: `eventProse.js:73-78` `pickLine(pool, seed)` — `fnv1a32(seed) % pool.length`, "PURE SELECTION… no rng" (:11); `pickLineWithFamily` returns a `familyId` for receipts.
- **How the war-name deriver slots in with zero engine state (CONFIRMED precedent)**: `calamityKernel.js:450` already mints a display name via `pickLine(CALAMITY_TITLES, \`${settlementName}::${year}\`)` — stable-key hash, zero PRNG draws, zero new state. A war-name deriver = a pure function `(sorted participants, warReason type, tick) → fnv1a32-selected authored grammar (per culture, closed vocabulary)` living beside the `*News.js` leaves; it needs **no PRNG fork at all** (the fnv1a32 idiom is the display-side norm; keyed forks are the engine-side norm) and no flag, since it writes nothing. Route names, by contrast, are dormant-flagged in the review because they'd land on persisted `RouteEdge` rows — but note the same hash-on-stable-key idiom over the codepoint-sorted `a|b` pair could deliver route names **display-side too**, ledger untouched, flagless: an option the architecture round should weigh explicitly.

## Discrepancies vs the review (for the synthesis)

1. **"9 culture keys" is wrong — 11** (namingData.js keys; `resolveConfig.js:352` "canonical 11-culture catalog"). The Markov corpus workload is 11 cultures, not 9, and the review's "9 culture keys" phrasing should be corrected wherever candidate #1/#4 grammars are scoped.
2. **"≤900 combos per culture" overstates 4 cultures** — in-pool duplicates (slavic ≤660 distinct).
3. **The culture-token resolution fork** (`mediterranean`/`mixed`/unknown → latin vs germanic vs union, §2 above) is unreported by the review and is a design input: a `naming:<cultureKey>:<entityId>` fork keyed on an **unresolved** token would silently give two consumers different streams for the same settlement.
4. `instantNpc` is exported but caller-less at tip (counterpart-pin-only) — a naming-module cutover list that only covers wired callers would still be complete today with S1, S2, N1, N2.
5. successorNpc's culture-blind 27-name pool is an additional cohesion seam candidate #1 could cure that the review does not list.

---

# V-BRIDGE

All reads are complete. Compiling the report from first-hand receipts.

---

# LANE V-BRIDGE REPORT — capture surface + the five seams

**Denominator.** Read IN FULL: `public/map/sf-bridge.js` (1,299 ln), `src/domain/spatial/spatialDigest.js` (581), `src/domain/spatial/spatialCost.js` (217), `src/domain/spatial/seaLanes.js` (549), `src/lib/spatialPackCapture.js` (125), `src/store/campaignSpatialCanonize.js` (165), `src/lib/mapBridge.js` (353), `src/lib/spatialCaptureRegistry.js` (33), `src/hooks/useMapBridge.js` (145), `src/lib/roadNetwork.js` (402), `src/components/map/RoadsLayer.jsx` (136), `src/components/map/TerrainToolbar.jsx` (175), `src/domain/resolveTerrain.js` (63). Read in relevant scope: `src/store/mapSlice.js` :330–530, `src/store/campaignWorldPulseSlice.js` :340–424, `LivingWorldGates.jsx` :80–134, vendored `public/map/main.js` (grep + targeted lines), fork bundle `index-Bp79q281.js` (string-probed only), review §1/§5 + FMG-PHYS + SF-REALM appendices. Repo tests checked via `git show 73f5dfc02` (listing + imports only). **Nothing executed; all walltime claims are structural.** All paths below relative to the extraction root (`.../fmg-study/ours/`).

---

## 1. THE CAPTURE SURFACE AND HOW AN EXTENSION RIDES THE IDIOMS

**What is captured today (CONFIRMED).** `sf-bridge.js:875–901` `settlementEngine:getSpatialPack` replies `pack.cells = { h, biome, r, p, c }` and nothing else — `plain()` converts TypedArrays (`:881`), `p`/`c` are deep-copied so the reply "can't alias live pack state" (`:890–893`). Placement rows ride separately from the parent store: `spatialPackCapture.js:76–88` emits `{ id, cellId, institutions, magicExists? }` per placement, scoped to the campaign's settlements (`:81`), institutions from `savedSettlements` (`:62–73`), magic only stamped when *present ∧ false* (`:70–72`).

**The five idioms an extension must ride (all CONFIRMED):**

1. **Version axes (§V.1)** — `spatialDigest.js:95–100`: `SPATIAL_GEOMETRY_VERSION` / `COST_LAW_VERSION` / `OVERLAY_VERSION`, with the law "Bumping any of these is a DISCRETE re-canonize event… an existing frozen digest keeps its own axes forever — only an explicit spatial re-canonize re-derives. Never a silent drift on load."
2. **Opt-in sub-digest (the biomeTexture idiom)** — `spatialDigest.js:245–265` (`buildBiomeTexture`, own `BIOME_TEXTURE_VERSION` at `:105`) + `:558–560, 579`: built only under a boolean opt-in, appended LAST via conditional spread — "Omitted… ⇒ the returned shape is byte-identical to the pre-V-6 digest." The opt-in flag itself is a *virtual* simulationRules flag (`campaignSpatialCanonize.js:69–75`, `biomeTruthEnabled` absent from defaults).
3. **Reserved slots, fixed shape** — `spatialDigest.js:106–121`: exactly four slots (`airField, seaLanes, seasonalOverlay, teleportEdges`), "The KEY ORDER is fixed… a materializing wave changes a slot's VALUE, never the serialized shape." A slot is *self-describing* (carries its own version: `SEA_LANE_VERSION` `seaLanes.js:67–70`, `SEASONAL_OVERLAY_VERSION` `spatialCost.js:82`). **There is no free slot for new families** — a fifth family must use idiom 2, not mutate `reservedSlots`.
4. **Normalize tolerance** — `normalizeSpatialPack` (`spatialDigest.js:171–181`): `cellCount = Math.min(h.length, p.length, c.length)` — `r`/`biome` deliberately excluded (corroborated by `seaLanes.js:212–214`: "cellCount EXCLUDES r"); absent arrays read empty. **Unknown captured keys are silently dropped here** — see the hazard below.
5. **Freeze-first double capture + caps** — `spatialPackCapture.js:113–120` captures TWICE and `JSON.stringify`-compares as determinism evidence; `campaignSpatialCanonize.js:35` caps the digest at 400KB (current realistic ~87KB per its own comment); `:113` deepFreeze; the digest is written only by the explicit action, never on load (`campaignWorldPulseSlice.js:368–370`).

**Per-field extension design:**

| Field | Exists in the iframe? | Capture change | Where banded | Version axis that bumps | What old canons see |
|---|---|---|---|---|---|
| **River flux `fl`** (cand. #3) | CONFIRMED — bundle mints `cells.fl = new Uint16Array(pack.cells.i…)`, `pack.rivers.push({i, source, mouth, discharge,…})`, and even a `cells.fl[m]>100?"River"` cut (bundle string probes); vendored `main.js:1268–1289` reads `cells.fl`/`cells.conf` | add `fl: plain(cells.fl)` to `getSpatialPack` (TypedArray → `plain()` already handles it); optionally a pruned `rivers: [{i, discharge, width}]` | at canonize, inside `derivePortEligibility`/`buildSeaLanes` (`seaLanes.js:243–254, 546–549`): band `stream\|river\|great_river` at flux ≥ 100, gate the river arm of `port` on navigable | **`SEA_LANE_VERSION` 1→2** (the slot is self-describing). Only if flux ever modulates the cost field (riverBias `spatialCost.js:148`) does `COST_LAW_VERSION` bump — not what #3 proposes | frozen slot v1 forever; new derivation only at an explicit re-canonize. An absent `fl` in old fixtures must degrade to today's `r≠0` rule (tolerant-absent), or every fixture reds |
| **Temp/prec grids** (cand. #6) | CONFIRMED — `grid.cells.temp` Int8Array `main.js:994,1016`, `grid.cells.prec` Uint8Array `:1044`, and the pack→grid map `pack.cells.g` `:1245` | add THREE arrays: `temp`, `prec` (grid-indexed) + `g` (pack-indexed) — banding parent-side keeps the bridge dumb and the builder pure | at canonize into a typed per-settlement `climate: harsh\|standard\|mild` sub-digest — idiom 2 exactly (buildBiomeTexture shape, own `CLIMATE_*_VERSION` const, opt-in flag à la `biomeTruthEnabled`) | the sub-digest's OWN version; no top axis moves (geometry and cost law untouched) | no key at all ⇒ readers dormant ⇒ byte-identical; lights per-canon on receipted re-canonize |
| **Haven / `t` cells** (cand. #9) | CONFIRMED-existence — `cells.haven`/`cells.harbor` read at `main.js:1294–1299`; `cells.t` assigned `:860–862`; `.haven=l,pack.cells.harbor` in the bundle (mint site inside bundle, unread — PLAUSIBLE it is burg-independent since `rankCells` at `:739→1262` consumes it during plain generation) | **for #9 as scoped: NO capture change** — the fix is inside the iframe's own `computeRoadNetwork` (`seaCost` `sf-bridge.js:678–684`, sea endpoints `:815–829`), which already has live `pack.cells.t/haven` in scope | n/a (render tier) — grade `seaCost` by `t` rings, force the exit through `haven[cell]` | none — render-only, declared visual shift. IF later digested into `waterPathFrom` pricing (`seaLanes.js:388–408`): capture `t` + `SEA_LANE_VERSION` bump | drawn lanes change on next session for everyone (S4-class visual shift, declared); digest untouched |
| **Lake components** (cand. #10) | components derivable from ALREADY-captured `h`+`c` — the in-repo precedent is `waterComponents` (`seaLanes.js:307–325`, flood-fill) | **none for cells/shoreline**; the salt/dry/frozen *budget* needs temp+prec (couples #10 to #6's capture) or a capture of the fork's own `pack.features` lake rows (bundle-held; unread) | canonize-time flood-fill of `h<20` non-border components; freeze `{cells, shoreline settlementIds, subtype}` as an opt-in additive sub-digest (idiom 2, own version) | the sub-digest's own version | no key ⇒ dormant. Watch the 400KB cap: store component ids + shoreline ids + subtype, never per-cell lists at scale |

**Extension hazards found first-hand (not in the review):**
- **`normalizeSpatialPack` drops unknown keys** (`spatialDigest.js:174–180` destructures exactly five). Extending the bridge without extending normalize is a **silent vacuous capture** — the field arrives and dies, greens everywhere (§714/§709 class). Any capture-extension charter needs a survives-normalize assertion, not just a bridge-reply assertion.
- **`cellCount` discipline**: new per-cell arrays must be *excluded* from the `Math.min` (the `r`/`biome` precedent) or a short `fl` array shrinks the whole world.
- **Double capture doubles the payload cost**: `spatialPackCapture.js:114–120` re-captures and `JSON.stringify`s both sides; +4 arrays ≈ +50–80% on an already full-map postMessage copy, twice, plus the compare. Bounded by 2×20s RPC timeouts (`mapBridge.js:303`).
- **No provenance stamp**: the digest's full key list (`spatialDigest.js:564–580`) carries **no map identity** — no seed, no snapshot fingerprint, no capture timestamp. Nothing ties a frozen digest to the geometry it was cut from. This is load-bearing for S2 and S5 repairs below.

---

## 2. THE FIVE SEAMS, VERIFIED FIRST-HAND

### S1 — no `fmg:terrainChanged` listener. CONFIRMED, and *worse than reported*.
- Emitter: `sf-bridge.js:1099` — `if (activated) postToParent({ type: 'fmg:terrainChanged', tool })`, the only emit site (grep: 2 hits, comment `:44` + `:1099`).
- Complete parent subscription census (grep `\.on('` across src): `useMapBridge.js:92–126` → `ready, burgSelected, settlementPlaced, placementRemoved, allPlacementsCleared`; `MapOverlay.jsx:105` → `viewport`. **Zero `terrainChanged` listeners.** Dead child pushes besides it: `burgList` (`:302`), `mapReset` (`:1052`), `snapshotLoaded` (`:1036`), `placementsRestored` (`:613`).
- `TerrainToolbar.jsx` (all 175 lines): no `canonizedAt`/worldState read anywhere; tools gate only on `bridgeReady`. Only *placement* writes are canon-locked (`mapSlice.js:446` updatePlacement, `:497` applyAutoplacement).
- **Sharpening:** the event itself under-covers even if listened to. It fires on tool *activation* (before any edit), only via `activateTool` — and per `TerrainToolbar.jsx:4–8`, rivers/coastline/lakes editors open by **double-clicking the map**, an FMG-internal path that never crosses the bridge; `terrainUndo/Redo` (`sf-bridge.js:1120–1136`) also emit nothing. So a listener alone repairs less than S1 implies.
- Repro reasoning: canonize (digest freezes territory/distances from captured `h`) → open heightmap editor, raise a sea → digest still narrates the old land; nothing warns, blocks, or offers the receipted re-canonize.
- **Minimal repair shape:** (a) parent listener setting a session "geography may have diverged" flag when `spatialCanonVersion > 0`, surfacing the existing re-canonize CTA (`LivingWorldGates.jsx` SpatialCanonGate) — cheap, but coverage-partial per above; (b) the honest cure is a capture-time **fingerprint diff**: stamp capture provenance (map seed + a pack hash) into the digest at canonize, and compare on the next canonize/at map load — catches every edit path including double-click editors, with zero FMG-internal hooks. (b) adds a digest key ⇒ new-canons-only, additive, but it is a persisted-shape change ⇒ **owner-gated**.
- Blast radius: (a) bridge-parent wiring + one UI note, zero engine bytes; (b) capture + digest assembly + a compare read; old canons byte-identical (no key).

### S2 — stale placement cellIds at capture. CONFIRMED mechanism; the review's PLAUSIBLE sub-question is now settled by read.
- Mint: `sf-bridge.js:529–536` — `cellId = findCell(mapPt.x, mapPt.y)` at drop, "best-effort". Store: `mapSlice.js:367–372` persists `{settlementId, x, y, cellId: cellId ?? null, placedAt}` — **no terrain, no map identity**. Drag-move may patch it (`:451`) but is canon-locked (`:446`).
- Capture consumes the STORED value: `spatialPackCapture.js:79` `const cellId = Number(pl?.cellId)` — nothing re-derives from x/y against the pack being captured in the same breath (`:99–125` read in full).
- `resolveSeeds` (`spatialDigest.js:191–209`), full read: drops non-integers, out-of-range (`off_map`), `h<20` (`not_land`), duplicates (`shared_cell`) — **and records them in `skippedSettlements`, which IS a visible receipt** for the edit-turned-my-cell-to-ocean case. But a stale index that lands on *another valid land cell* passes silently and seeds territory/distances/gates/seaLane eligibility (all keyed off `seed.cellId` — `derivePortEligibility` `seaLanes.js:246–251`, biomeTexture `spatialDigest.js:251–253`) at the wrong location, frozen forever.
- When it goes stale (CONFIRMED trigger classes): terrain edit between drop and canonize (pack re-graph reindexes cells; editor internals live in the bundle — the *reindex-on-edit* step is PLAUSIBLE, unread); `loadSnapshot` of different-vintage geometry while store placements hydrate from the campaign (`WorldMap.jsx:431–432`); a future fork upgrade changing cell indexing. Mitigating scope: `resetMapState` clears placements wholesale on campaign switch/regeneration (`mapSlice.js:752–765`), so the plain reset path does not carry ids across maps.
- **Minimal repair shape:** at capture, re-resolve each placement's cell from stored x/y **against the captured pack itself** — nearest-centroid over `pack.cells.p` is pure, parent-side, no bridge round-trip (O(P·cells), fine at 8k cells) — and diff vs stored `cellId` into a receipt row (extend the `skippedSettlements` idiom with `reason:'cell_remapped'` or a `remappedSettlements` sibling). Choosing to *use* the re-resolved cell changes newly-built digests only (a discovery, declared); merely *reporting* is byte-neutral to the digest bytes proper only if kept out of the digest object — inside it, new-canons-only.
- Blast radius: `spatialPackCapture.js` (+ ~20 pure lines) or `spatialDigest.resolveSeeds`; no store/bridge change; goldens for fixtures whose stored ids match centroids stay green.

### S3 — terrain-enum vs geography double truth. CONFIRMED; both homes quoted; plus a vocabulary finding.
- Truth 1 (engine): `config.terrainType` via the ONE read `resolveTerrain.js:40–46` (chain `terrainType → terrainOverride → terrain`, canonical vocabulary "plains | hills | forest | riverside | coastal | mountain | desert" `:11–13`), minted from the trade route at genesis — `terrainHelpers.js:23–27` `{ port: "coastal", river: "riverside", … }`. Consumer receipt: `src/generators/economy/foodBalance.js:613` `if (route === 'port' && resolveTerrain(config) === 'coastal')`.
- Truth 2 (map): captured FMG geography — port geography `seaLanes.js:200–220` (`isCoastalCell`: land cell with ocean/off-map neighbour; `isRiverCell`: `r≠0`), digest terrain classes `spatialCost.js:118–131` `terrainClassOf(h,biome)`.
- **Sharpening: the two truths don't even share a vocabulary.** `terrainClassOf` speaks `water|desert|grassland|forest|wetland|tundra|glacier|mountain`; the config speaks `plains|hills|forest|riverside|coastal|mountain|desert`. No mapping module exists between them (nothing imports both `resolveTerrain` and `terrainClassOf` — grep), so even a wish to reconcile has no shared type to reconcile *in*. Only the opt-in autoplacement path compares fit (per SF-REALM; `placementRaster` header — not re-read here, PLAUSIBLE carry-over).
- Repro: drop a `terrainType:'coastal'` settlement mid-plain → `foodBalance` feeds it as a port forever, while `derivePortEligibility` reads `coastal:false, river:false` → never a sea-lane port. Permanent disagreement, no flag.
- **Minimal repair shape:** a pure `terrainAgreement(configTerrain, pack, cellId)` deriver (one new small module, a typed mapping table between the two closed vocabularies + a tri-state `agrees|disagrees|unknown`), surfaced (i) at drop time as a non-blocking warning and (ii) at canonize as a receipt row. Never auto-rewrite `terrainType` (genesis derivations hang off it; for canon settlements that write is lived-history-adjacent — the bug-or-truth fork applies). The review's #99 suitability-defaults steal (derivation feeds the wizard *choice*) removes the seam at the source for new placements.
- Blast radius: additive deriver + two call sites (WorldMap drop handler, canonize receipt); zero engine reads change; finite semantics preserved (mapping table is a closed vocabulary→vocabulary function).

### S4 — drawn roads vs digest truth. CONFIRMED, both sides quoted; the "no test compares them" claim upgraded from PLAUSIBLE to CONFIRMED-at-tip.
- Drawn side: `roadNetwork.js:146` `computeRoadEdges` — Prim MST over `rank >= 4` city+ placements (`:252–284`), trade-relationship edges (`:288–299`), supply-chain edges (`:301–372`), lanes (`:374–399`), hostile suppression (`:207–226`). Routed per session by iframe A* (`RoadsLayer.jsx:73`, 20s timeout), stored only in React state (`:44 setPaths`), re-fired on `geometryVersion` (`:42, :86`) — never persisted. Sea choice is a length proxy: "sea only wins if clearly shorter" ×1.15 (`sf-bridge.js:843–845`).
- Sim side: the frozen digest — territory adjacency + gates (`spatialDigest.js:408–439`), Floyd-Warshall matrix (`:451–469`), tiers (`:484–501`), route receipts (`:523–552`) — different edge *selection* (every-settlement territory adjacency vs MST-over-cities+relationships), different sea law (`buildSeaLaneSet` water-Dijkstra ×12/dist with land-domination pruning, `seaLanes.js:411–534` vs the bridge's 1.15 proxy), different times (session vs canonize).
- The one deliberately shared piece: `spatialCost.js:30–34` "a verbatim port of the iframe's BIOME_COST table" = `sf-bridge.js:651–665` (both quoted in full above; values identical, 13 entries).
- Test census at `73f5dfc02` (git ls-tree + import scan of all 11 `roadNetwork|spatialDigest|seaLanes|spatialPack|canonize` test files): road tests import only `computeRoadEdges`; digest tests import only `buildSpatialDigest`/`distanceRead`. **No test imports both. CONFIRMED: no receipt compares the surfaces.**
- Repro: a `tier-3` (distant) digest pair whose placements are both cities gets an MST highway drawn between them; conversely a digest primary hop between two villages (rank<4, no relationship, no chain) draws nothing — the Herald narrates a route no road shows.
- **Minimal repair shapes**, in escalating order: (i) a comparison *test/receipt* (fixture pack → digest gates vs computeRoadEdges set; assert every primary hop is drawn or a documented exception) — test-only, zero product bytes; (ii) candidate #2 tier (a): thread a connections set + off-burg multiplier through the bridge A* so drawn roads merge into trunks — render-only, declared visual shift; (iii) candidate #2 tier (b): digest-side Urquhart candidate edges — `SPATIAL_GEOMETRY_VERSION`/`COST_LAW_VERSION` bump, new-canons-only. The five-seam repair proper is (i) + a legend/product decision about which surface the map *claims* to draw.
- Blast radius: (i) tests only; (ii) `sf-bridge.js:629–863` + RoadsLayer visuals; (iii) digest + version axis + goldens (declared shift).

### S5 — iframe-hostage re-canonize. CONFIRMED, with the exact chain and two sharpenings.
- The chain: `captureSpatialPack` returns null unless `getSpatialCaptureBridge()` is non-null AND `bridge.isReady` (`spatialPackCapture.js:100–101`); the registry is populated ONLY by the WorldMap mount effect (`useMapBridge.js:80` register / `:137` unregister); the canonize then no-ops as `spatial_capture_unavailable` (`campaignSpatialCanonize.js:68`), surfaced as "Open the world map to map geography." (`LivingWorldGates.jsx:120`).
- Not persisted anywhere parent-readable: the only geometry at rest is `mapState.fmgSnapshot`, the ~1MB `.map` text (`WorldMap.jsx:431–432` feeds it back to the iframe's `uploadMap`, `sf-bridge.js:467–478`); no parent-side parser exists (grep across src: consumers only load/route/describe it).
- **Sharpening 1 — the doctrine's re-derive is manual-only.** `campaignSpatialCanonize.js:88–101` says the receipted re-canonize "RE-DERIVES ports on founding events," but the ONLY caller of `canonizeCampaignWorldSpatial` in the tree is the LivingWorldGates button (`LivingWorldGates.jsx:117`; caller grep — single hit). No founding event, advance hook, or queue triggers it, and no "re-canonize owed" marker exists anywhere (reason-string grep: 3 hits, all the same copy). A campaign advanced from non-map surfaces accrues foundings the frozen digest cannot see, with no debt recorded.
- **Sharpening 2 — capture provenance is unstamped**, so even with the map open, nothing asserts the iframe holds *this campaign's* geometry when the pack is read (the digest records no seed/fingerprint — key list `spatialDigest.js:564–580`). Cross-campaign capture is gated only by UI flow (PLAUSIBLE-safe today, unproven).
- **Minimal repair shapes:** (i) cheap, schema-free: a "re-canonize owed" *session* signal — count foundings since `spatialCanonVersion`'s stamp and surface it on the gate ("2 foundings since geography was mapped"); (ii) the real cure the lane report already named: persist the captured `pack.cells` sidecar-style at first canonize (~the digest's own scale; well under the snapshot's 1MB) → every later re-canonize becomes headless and deterministic, and S2's re-resolution gets a stable substrate. That is a persistence-shape decision — **owner-gated** (non-negotiable #3), and it must stamp provenance (seed + hash) to close S5-sharpening-2 and S1's fingerprint diff in the same act. Note the three-repair convergence: one provenance-stamped persisted capture underwrites the S1 diff, the S2 receipt, and the S5 headless path simultaneously — that is the cohesive shape the WEAVE-SEAM wave should charter, not three point fixes.
- Blast radius: (i) UI + a pure counter; (ii) campaign save shape (new key), capture module, canonize body; old canons unaffected (absent key ⇒ current behavior).

---

## 3. WHAT CANONIZE COSTS TODAY, AND THE HEADLESS QUESTION

**What runs (CONFIRMED from code; nothing timed — no execution in this lane):**
1. Gate prefix, synchronous (`campaignWorldPulseSlice.js:377–397`): advance/pause/entitlement (`tier === 'premium'` `:391`)/generated-map checks; then a dynamic import of the lazy body.
2. **Two** sequential `getSpatialPack` RPCs (`spatialPackCapture.js:105, 115`), each a full-map copy of 5 cell arrays across postMessage, each bounded by a 20s timeout (`mapBridge.js:303`); plus a `JSON.stringify` equality compare of both captures (`:116`).
3. `placementsFor`: a scan of `savedSettlements` for institutions + magicLedger (`:49–90`).
4. `buildSpatialDigest` (all pure, parent-side): cost field O(cells); one multi-source integer Dijkstra O(cells·log cells); one edge scan; Floyd-Warshall **O(N³)** on N settlements (fine at N≈30); per-settlement BFS tiers (with an O(N) `indexOf` inside the loop, `spatialDigest.js:494` — quadratic-ish but tiny); with the M8 opt-in, `waterComponents` flood + **one water-Dijkstra per port** (`seaLanes.js:471–517`); teleport clique trivial. Realistic scale per the module's own comment: ~8k cells / 30 settlements ⇒ ~87KB digest (`campaignSpatialCanonize.js:32–34`).
5. `JSON.stringify(digest)` size guard (400KB cap `:35`), `deepFreeze` (`:113`), immer write + `spatialCanonVersion` bump (`:123–137`), analytics, then `syncCampaignSnapshot` — a network await (`:151–156`).

Structural bound: the iframe RPCs dominate the interactive path (up to 2×20s worst-case timeout; typically the two full-array copies + two stringify compares); the pure build is sub-second-class at the documented scale (PLAUSIBLE — reasoning from complexity, not measurement).

**Headless status (CONFIRMED):** the *build* is fully headless — `buildSpatialDigest` and every sub-builder import no DOM/iframe (headers `spatialDigest.js:6–9`, `seaLanes.js:55–58`), and the capture is an injected seam (`options.captureSpatialPack`, `campaignSpatialCanonize.js:63–65`; "tests inject a deterministic fixture capture"). The *capture* is the only iframe-bound step, and it is iframe-bound absolutely: live bridge + ready + mounted WorldMap, no parent-side pack persistence, and the one at-rest geometry (`fmgSnapshot`) is parseable only by the iframe. **There is no headless path for any capture today; there is a complete headless path for everything after capture.** The single missing piece for headless re-canonize is the persisted, provenance-stamped capture sidecar (owner-gated, §2/S5) — after which re-canonize is a pure replayable derivation end-to-end, which is precisely the §718 doctrine's preferred shape.

---

**Open questions this lane could not close (labeled):** whether the heightmap/biomes editors reindex `pack.cells` on every edit path (bundle internals unread — PLAUSIBLE, drives S2 severity); the mint site + burg-independence of `haven/harbor` in the vendored bundle (existence CONFIRMED, provenance PLAUSIBLE); whether the old bundle's `pack.features` lake rows already carry a typed subtype usable by #10 (bundle unread); cross-campaign capture provenance (no counterexample found, no assertion exists).

---

# V-NET

All reads complete. Report follows.

# LANE V-NET REPORT — route-network family in depth (gates candidates #2, #9, port steal)

Basis: full reads of routeNetworkGenesis.js (475 ln), routeNetworkLedger.js (667 ln), roadNetwork.js (402 ln), seaLanes.js (549 ln), routeNetworkConsumersInterdiction.js (287 ln), neutralNeighbourEdges.js (346 ln), sf-bridge.js:560-908, distanceRead.js:230-360 + 556-640, spatialDigest.js:60-130 + 350-495, RoadsLayer.jsx:40-92, seaRoads.js:1-80; headers (28 ln each) of the 14 remaining family files. All paths under `.../fmg-study/ours/`. Everything below is CONFIRMED (read) unless marked PLAUSIBLE.

## 0. THE STRUCTURAL FACT THAT REFRAMES CANDIDATE #2's M TIER

**The entire canon-tier routeNetwork family is complete but UNMOUNTED.** `src/lib/spatialUsage.js:349` (in-repo manifest, quoted): *"writeRouteNetwork is reached only from accrueRouteFlows and ensureGenesisRouteNetwork, and neither has a caller"* — and the certification row (`src/domain/certification/subsystemRowsWaves.js:380`) confirms: *"it is not wired into pulseKernel, exactly as J1 through J3 are not, so every consumer here is a pure read a later integration wave calls."* The review's "~30 files, skimmed" is actually 17 `routeNetwork*.js` files in `src/domain/worldPulse/` (measured). Consequence: Urquhart-as-canon-candidate lands into a dark lane; the wiring wave the certification row calls for precedes or accompanies it, and nothing a candidate-set change does can move a live world today.

## 1. (a) WHERE URQUHART CANDIDATE EDGES ENTER THE CANON TIER

**The candidate-set builder is `genesisCandidatePairs` (`src/domain/worldPulse/routeNetworkGenesis.js:287-321`).** It returns `{ knn, ports, user, all, bound }` — the union of three sources, each linear:
- `knn` = `neutralNeighbourPairs(ids, worldState)` (:294) — the region layer's k-nearest selection, k=3 (`src/domain/region/neutralNeighbourEdges.js:148 NEUTRAL_DEFAULT_K = 3`), ranked resolved-first → frozen `pathCost` → codepoint (:231-251), aspatial fallback = codepoint-rank separation.
- `ports` = `portTotalityPairs(digest, ids)` (:210-237) — one nearest navigable counterpart per port off `seaLaneAdjacency`.
- `user` = `config._userRoutes` rows (:250-267).
- `bound: knn.length + ports.length + user.length` (:319) — *"the structural BOUND the union may never exceed, so the anti-quadratic law is a number a test can assert"* (:279-282); the header (:21-22) says `genesisCandidatePairs` is exported precisely to pin this.

**Urquhart enters as a fourth key** beside knn/ports/user plus a fourth bound term. Urquhart ⊆ Delaunay, which is planar (≤ 3S−6 edges), so the bound stays linear — PLAUSIBLE (mathematics, not code). Two files then need re-proof of the widened set, not one: `routeNetworkFlows.js` header (:23-28) states corridor admissibility = *"the genesis candidate set (k-NN + ports totality + user routes, all linear) unioned with the pairs that already carry an edge"* — widening candidates widens flow-admissible corridors too.

**Version constant: THERE IS NONE in the family.** `grep VERSION src/domain/worldPulse/routeNetwork*.js` → zero hits (measured). The §V.1 discipline the review cites is spatialDigest's: `SPATIAL_GEOMETRY_VERSION = 1` / `COST_LAW_VERSION = 1` at `src/domain/spatial/spatialDigest.js:98-99`, stamped per digest at :350-351/:566, with the comment (:95-97): *"Bumping any of these is a DISCRETE re-canonize event (§V.1): an existing frozen digest keeps its own axes forever."*

**What re-canonize event it implies — two distinct paths, and the architecture must choose:**
1. **Genesis-derivation path (candidates from geometry, computed at world-connect):** NO re-canonize event exists or is needed. The guard is `ensureGenesisRouteNetwork`'s strict no-op contract (:469-475): DORMANT (flag absent) returns input **by reference**; ALREADY CONNECTED (`readRouteNetwork(worldState)` truthy, :471) returns by reference — *"Genesis happens once; re-deriving over a lived network would rewrite history, which Law 2 forbids."* So a widened candidate set reaches only worlds connected after the change, under the virtual `routeLifecycleEnabled === true` gate (`routeNetworkLedger.js:208-212`). THE PROMISE holds structurally, but the gate is **connect-time**, not canonize-time — a version stamp on the *derivation law* would have to be invented if the owner wants same-seed reproducibility of genesis across code versions (nothing pins "which candidate law derived this ledger" today; the ledger just persists its output).
2. **Digest path (Urquhart edges frozen INTO the digest, e.g. a fifth reserved slot):** the reserved-slots idiom exists exactly for this — `reservedSlots()` (`spatialDigest.js:118-120`) freezes key order `airField, seaLanes, seasonalOverlay, teleportEdges`, each slot self-describing with its own version (seaLanes precedent: `SEA_LANE_VERSION = 1`, `seaLanes.js:70`, *"Bumping it is a DISCRETE re-canonize event (§V.1)"*). A new slot is a serialized-shape change ⇒ SPATIAL_GEOMETRY_VERSION bump ⇒ discrete re-canonize; old canons keep null forever.

**Interaction hazard to design around (CONFIRMED):** in `deriveGenesisRouteEdges` pass 2 (:389-402), mode is decided by `genesisPairMode` (:175-180 — 'water' iff both endpoints are lane-linked ports), and **water candidates are minted UNCONDITIONALLY at `PORT_TOTALITY_GRADE`** (:393-396: *"WATER edges are minted UNCONDITIONALLY... The land access vocabulary has no business grading a sea lane"*). An Urquhart pair between two ports therefore bypasses the land grade ladder and mints a road-grade water edge. The land arm also requires `reachable(digest,a,b)` (:397-400) and drops isolated-rank pairs — an Urquhart pair between two `isolated`-access settlements mints nothing (grade ladder :79-82, `genesisPairGrade` :154-159 takes the MINIMUM endpoint rank). The architecture must state whether Urquhart pairs read the access ladder (my reading of the file's own law: they must — Law 2 makes `tradeRouteAccess` frozen truth) or carry their own grade rung.

## 2. (b) THE RENDER TIER — independent A*, and where shared-corridor state threads

**Edge selection** (`src/lib/roadNetwork.js:146-402`, `computeRoadEdges`): three passes — (1) Prim MST over city+ (`rank >= 4`, :252-284) with strict total order `(w, pairKey)` (:260-261, comment :246-251: popped edge sequence *"identical on every run for identical input"*); (2) trade edges from `TRADE_RELATIONSHIPS` links (:286-299) and supply-chain producer/consumer crossings walked in ascending (i,j) (:301-372); (3) country lanes: nearest non-hostile for unconnected placements (:374-399). Every edge is emitted through one chokepoint, `addEdge` (:222-238).

**Path routing** (`public/map/sf-bridge.js:629-863`, `settlementEngine:computeRoadNetwork`): edges arrive as an ordered array (RoadsLayer.jsx:65-71 maps `edges` in computeRoadEdges' emit order; order preserved) and are routed **independently** in `for (const e of edges)` (:804): per edge, a land A* (`aStar`, :710-776) over `landCost` (:667-676: `BIOME_COST[b] × elevMult + riverBias`, biome table :651-665), optionally a sea A* over `seaCost` (:678-684: `h >= 15 ? 1.2 : 0.9`, ocean-only) between `findNearestOcean` cells (:779-794), with `canSea = (e.preferSea || !landPath) && isCoastal(both)` (:815); winner by polyline-length proxy with a 1.15 sea handicap (:843-846). No state crosses iterations today.

**Where FMG's two cost modifiers thread deterministically:**
- **Corridor reuse (×0.5 on already-routed cell pairs):** a `usedCellPairs` set mutated after each edge's path is committed, read inside the cost during later edges. Deterministic **iff edge order is deterministic — which it already is, end to end** (computeRoadEdges' documented emit order → RoadsLayer payload order → bridge's array iteration). Two mechanical preconditions (CONFIRMED from the loop body): (i) the cost callback signature is `costFn(n)` — destination cell only (:758) — corridor reuse is a property of the *pair* `(current, n)`, so `aStar` must pass both; (ii) cost is applied as `nc * edgeDist` (:762-763), so the discount composes multiplicatively per hop, which matches FMG's shape.
- **Non-burg multiplier (×3 off-burg):** a pure function of the placement set (cells within radius of any placement), order-independent; computable once per request from the same payload.
- Both are render-only: paths live in React state (`RoadsLayer.jsx:44 setPaths`), nothing persists. Re-request triggers already include `geometryVersion` (:42, :87).

**Render-tier consumers that move when the edge set/paths change (denominator: 4 real, of 5 grep hits):** RoadsLayer.jsx (draw + style grouping :89-92); WorldMap.jsx:269 and :534 (`route_count` telemetry, count only); mapSlice.js:398-401 (MAP_ROUTE_DRAWN moment *"Only fire when the edge count strictly increases"* — an Urquhart supergraph raises counts, firing this moment on existing maps once); sf-bridge computeRoadNetwork itself. `src/domain/realm/autoplacement.js:24` is a comment-only mention (NOT a consumer — verified the only hit).

## 3. (c) THE seaLanes/PORT PIPELINE, AND WHERE #9 + THE PORT STEAL LAND

**The full chain (all CONFIRMED):**
1. **Capture** — `settlementEngine:getSpatialPack` (`sf-bridge.js:875-901`) copies exactly `{h, biome, r, p, c}`; no `t` (distance-to-coast), no `haven`, no `fl`.
2. **Canonize** — `spatialDigest.js:312` `seaLanesOptIn = input?.seaLanes === true`; the slot is built at :475-482 **after** the Floyd-Warshall land matrix completes, with a `landCost` accessor closing over it (*"a dominated sea lane (land already cheaper) is pruned (§4j)... an unmapped pair reads Infinity ⇒ any water lane between them is non-dominated (an island)"*).
3. **Builder** — `buildSeaLanes` (`seaLanes.js:546-549`) = `derivePortEligibility` + `buildSeaLaneSet`. Port law (:14-27, :243-254): **GEOGRAPHY (coastal `isCoastalCell` :200-209 OR river `isRiverCell` :215-220) ∧ CAPABILITY (`hasWaterAccessInstitution`, catalog-derived maritime set :134-156)**. Edge set (:432-534): water components via BFS (`waterComponents` :307-325), per-port embarkation cells (:332-340), per-port water-path Dijkstra (`waterPathFrom` :388-408 — *"a voyage that must sail AROUND a peninsula... is priced by the long coastal path, NOT the straight-line chord"*), price = water-path distance × `SEA_COST_PER_DIST = 12` (:81), land-domination pruning (:487-495), then MST + K=4 nearest per port (:504-516). Constants: `SEA_LANE_MIN_COST=1`, `SEA_LANE_CAPACITY=10`, `STORM_SEASON_COST {1.3/1.0/1.5/2.5}` ≥1 slow-not-sever (:103-105).
4. **Read** — `distanceRead.js`: `activeSeaLanes` dormancy gate (:238-245), `isPort` O(1) memo (:251-265), `seaLaneAdjacency` (:272-291), `pathCost` fast-path escalation only when a slot is lit AND an endpoint is a port (:590-599), storm factor ≥1 read-time (:330-334), `seaLaneNeighbourMap` for the ship-crew rumor carrier (:307-323).

**Candidate #9 (haven + coast-tier water costs) lands render-side only**, exactly as the review says: the whole mechanism is `seaCost` (:678-684, two flat tiers), `findNearestOcean` (:779-794 — an arbitrary BFS ocean cell, the "clips its own headland" cause), and the sea-leg splice (:815-829). Haven honoring = replace `findNearestOcean(startC)` with the port cell's haven; coast tiers = grade `seaCost` by a distance-from-shore class. The review states `pack.cells.t` and `pack.cells.haven` are *"both already in the vendored pack"* — **PLAUSIBLE here** (I did not read the fork bundle; SF-REALM carries that receipt). An engine-side counterpart (tiered water costs in `waterPathFrom`) would be a `SEA_LANE_VERSION` bump — discrete re-canonize, old canons untouched (:67-70).

**The two-endpoint-per-water-body invariant ALREADY HOLDS STRUCTURALLY at the engine tier** — the steal reduces to a named receipt, not a mechanism:
- `buildSeaLaneSet:472`: `if (members.length < 2) continue; // a lone port in its water body ⇒ no lane`.
- The slot's `ports` roster is **connected nodes only** (:526-527: *"the nodes the lanes actually connect (a lone unconnected port is not a functional hub) — so isPort reads exactly the sea graph's nodes"*). Hence a lone eligible port is not a port to any digest reader.
- Genesis totality composes with it: `portTotalityPairs` (:216-231) only owes ports that HAVE a navigable counterpart; `portsMissingWater` (:433-444) asks the invariant as a question, re-asked every J3 charter eval.
- **Where it does NOT hold: the render tier.** `roadNetwork.js:186` — `isPort: sett?.config?.tradeRouteAccess === 'port'` (a config word; no geography, no water body, no second endpoint — with the 2026-08-11 owner-approved correction block :169-189 documenting the declared preferSea shift). Two "ports" on separate seas are saved only by the sea A* failing to find a water path. If the steal is chartered, this is its real home: a water-body grouping (or simply the digest's `isPort` when a canon exists) feeding `preferSea`. Third port definition in the estate — worth one architecture sentence so the tiers don't drift further.

## 4. (d) CONSUMER RE-PROOF DENOMINATORS WHEN EDGES CHANGE

**Canon tier (routeNetwork ledger edge set) — 22 grep-hit files in src/ for `routeNetworkLedger|readRouteEdges|readRouteNetwork|routeNetworkComponents` (measured), of which 21 substantive + 1 comment-only (`npcRulingRegister.js:30`):**

The family (17), one line each:
- `routeNetworkLedger.js` — model/single-writer of `spatialLedgers.routeNetwork`; edge id law `route.<a>.<b>.<mode>` codepoint-ordered (:236-239); closed vocabularies GRADES/MODES/FLAVORS/FLOW_CLASSES (:64-90); edges never deleted (floor `hidden`, :507-510); explicit-wins edge fold (:438-451) vs replace-wins corridors (:486-494).
- `routeNetworkGenesis.js` — J1, the candidate set + genesis derivation + ports-totality checker (§1 above).
- `routeNetworkFlows.js` — J2 accrual; attribution table `ROUTE_FLOW_SOURCES`; **admissibility = genesis candidates ∪ edge pairs** (re-proof required on any candidate widening).
- `routeNetworkFlowsMaterial.js` / `FlowsObjective.js` / `FlowsSelfSufficiency.js` — material index over lived components; LOCAL×SYSTEM+resilience−cost scorer; bounded 0..1 realm metric — all read components/edges, all shift when the edge set does.
- `routeNetworkCharter.js` / `CharterDanger.js` / `CharterBypass.js` / `CharterEvents.js` — J3 three-gate evaluation; believed-danger pricing via the M1 embattlement scorer; single writer of `bypass`; proposal emission shaped for proposalAdmission.
- `routeNetworkDecay.js` / `DecayLifecycle.js` — single writer of `grade`; anti-flap structural window/dwell; death→hidden cascade + revival warm start.
- `routeNetworkConsumers.js` — `livedNeighbours` adjacency + fail-closed `mayUseHiddenPaths` + H3 `hiddenHopsFor` seam + arterial-seeds projection (feeds the next four).
- `ConsumersTransit.js` / `ConsumersStrategic.js` / `ConsumersInterdiction.js` / `ConsumersRace.js` — one-hop-per-tick travel physics over the lived graph; strategicNeed write + lapse-removal; interdiction severance (§5 below); person-speed vs grade-weighted news-speed race.

External live readers (4): `warCosts.js` (:16, :328 `readRouteNetwork`, :466 reads `routeNetwork.edge.gradeStep`); `demographicsRates.js` (:59-60 `routeLifecycleActive` + `interdictableArteries`); `magicSubstitutionReagents.js` (:72-81, reads flows + full ledger read/write surface); `npcDmVerbs.js` (:108-109 `advanceLivedTraveller`). Census/manifest surfaces (3): `spatialUsage.js:349`, `subsystemRowsWaves.js:349-390`, `tradeConvergenceContract.js:227`. Pins named by the family's own headers (not read, PLAUSIBLE they exist as named): `tests/domain/routeNetworkDormancy.test.js`, `routeNetworkLaw.test.js` (source-scan of every edge-id producer), the J1 dormancy goldens.

**Digest tier (seaLanes slot or geometry/cost version):** `seaLaneAdjacency|isPort` consumers, 7 files (measured): `distanceRead.js` itself, `routeNetworkGenesis.js`, `roads/userRoutes.js`, `spatial/navalLayer.js`, `worldPulse/navalKernel.js`, `navalStrength.js`, `realmVerbExecution.js` — plus transitively every `pathCost`/`hopWeeks`/`candidateRoutes` reader (digest-wide; out of this lane's denominator) and `roads/seaRoads.js` (classifies leg modes off `seaEdgesOfPath`, :68-79). Any slot change is gated to NEW canons by SEA_LANE_VERSION + §V.1 — old worlds need zero re-proof, the goldens prove dormancy.

**Render tier:** the 4 consumers in §2.

## 5. HOSTILE-SUPPRESSION / TYPED-REASON SEMANTICS — the mechanism, quoted

All in `src/lib/roadNetwork.js` (render tier), one chokepoint:
- The vocabulary (:93-99): `TRADE_RELATIONSHIPS = new Set(['trade_partner','allied','patron','client','vassal'])`; `HOSTILE_RELATIONSHIPS = new Set(['rival','cold_war','hostile'])`.
- The pair harvest (:207-220): every `neighbourNetwork` link whose `relationshipType` is hostile adds a sorted burg-pair key to `hostilePairs`.
- **The guard, inside `addEdge` (:226):** `if (hostilePairs.has(key) && tier !== 'trade') return;  // no road between enemies unless forced` — every emitted edge of every pass flows through `addEdge` (:222-238), so the guard is structural, not per-pass.
- The lane pass repeats it for the fallback walk (:393-397): *"a lane road can't cross an enemy pair"* — candidates walked in ascending distance so a placement whose nearest is a rival still gets a lane to the next-nearest (:380-386).
- **Typed reasons** (the closed set actually emitted, measured): `'mst'` (:275), `'rel:' + rel` (:297), `'chain:' + ids` (:369), `'nearest'` (:396) — carried on every edge record (:236 `reason`).

**Survival guarantee for candidate #2:** at the render tier, Urquhart replaces only the *source of candidate pairs* feeding pass 1; provided its pairs are emitted through `addEdge` with tier `'highway'`, hostile suppression, dedupe (`seen`), `preferSea`, and the typed reason all survive verbatim — the architecture should pin `addEdge` as the mandatory chokepoint (a new reason word, e.g. `'urquhart'`, extends the closed set — finite semantics holds). At the canon tier there is deliberately NO hostile suppression to preserve: genesis reads only frozen config + frozen digest (routeNetworkGenesis.js header :24-30 — no entropy, no lived relationships), and the lived-hostility semantics live in J3's believed-danger pricing (`routeNetworkCharterDanger.js` header: *"a corridor merely FEARED dangerous suppresses its own charter"*) and in `seaRoads.js`'s hazard branch (S1 blockade / S2 storm / S3 piracy behind `seaRoadsEnabled`, :29-57, `HOSTILE_RUNGS` :60). The FMG port has nothing at either tier (review §1/§2 concurs); the guarantee needed is purely that the port does not reroute edge emission around `addEdge` or move charter evaluation off the believed picture.

## 6. ONE-LINE RISK LEDGER FOR THE ARCHITECT

1. Canon lane unmounted (§0) — sequence the wiring wave explicitly; certification row already reserves it.
2. Genesis has no derivation-law version; ALREADY-CONNECTED no-op is the only shield — decide path 1 vs path 2 of §1 explicitly.
3. Urquhart pairs between two ports mint unconditional water edges (§1 hazard) — needs a stated rule.
4. Flows admissibility widens with candidates — re-pin the anti-quadratic bound.
5. Corridor-reuse needs `costFn(current, n)` signature change in the bridge A* (§2).
6. MAP_ROUTE_DRAWN + `route_count` telemetry shift on edge-count growth — declared visual/telemetry shift.
7. Three port definitions (config word at render / geography∧institution∧connected at digest / lane-linked at genesis) — the port steal's real value is unifying the render one.
8. `pack.cells.t`/`haven` presence in the vendored pack is SF-REALM's receipt, not re-verified here (PLAUSIBLE).

---

# V-POLIS

LANE V-POLIS REPORT — territory view (#8), overlays (#7), feedable ledgers, prominence, heraldry-late. All paths relative to `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours` unless noted. Denominators stated per section.

## 1. THE CELL SUBSTRATE (candidate #8's ground) — CONFIRMED, three files read in full

**placementRaster.js (259 lines, read whole).** No fixed resolution: `cellCount = Math.min(h.length, p.length, c.length)` over the captured FMG pack (:114); coordinates are FMG map-space per-cell centroids from `cells.p` (:140-142). Terrain law is single-writer: `terrainClassOf(height, biome)` imported from `spatial/spatialCost.js` (:47, :145) over the closed 8-member `TERRAIN_CLASSES = ['water','desert','grassland','forest','wetland','tundra','glacier','mountain']` (spatialCost.js:65-67), with `LAND_HEIGHT=20`, `HILL_HEIGHT=40` (:69, authored judgment), `MOUNTAIN_HEIGHT=60`. Water reads as `cls=null` (:148). Coast = land cell with a water neighbour via pack adjacency `c` (:158-166).

Three load-bearing constraints for a territory flood:
- **The RealmRaster DROPS adjacency.** `realmRasterFromPack` returns `{ cellCount, x, y, cls, h, river, coast }` (:168) — no `c`. A priority-flood needs the adjacency graph, so the territory module must call `readPackArrays(pack).c` itself (:107-116) or the raster typedef must grow a field. CONFIRMED.
- **The digest cannot serve a post-canonize territory view**: header :12-16 — "The per-cell centroid array `p` is CONSUMED at build time and never persisted, so a digest cell index cannot become the x/y a placement needs." A territory view at render time must read the live pack (the saved `mapState.fmgSnapshot` regenerates it stably per save), exactly as the canonize path does (:20-23).
- **Fit is deliberately non-partitioning**: ":179-181 The predicates deliberately OVERLAP where the world does… Fit is a per-terrain question, never a partition of the map." FMG's flood produces a partition; ours would be a new derived structure, not a reuse of `terrainFitsCell`.

**autoplacement.js (563 lines, read whole).** The brief's ":88-89" is `NEIGHBOUR_K: 3` — "The k in 'k-th nearest already-placed settlement' — J-D2's ratified k" (:88-89). Fit/reach/tie weights `FIT_WEIGHT:100, REACH_WEIGHT:30, TIE_WEIGHT:4` (:92-94); tier separation bands thorp 0.8 → metropolis 1.8 (:78-80) — the natural **expansionism analog** for #8 (tier already scales spatial claim). Determinism is stronger than seeded: "NO RNG AT ALL… `seed` enters through a pure integer hash" (:33-39, FNV-1a `tieHash` :157-166); `Math.sqrt` not `Math.hypot`, with the cross-device rationale spelled out (:171-178). Candidate pools are Weyl-subsampled, never strided (:84-87). A territory module claiming "pure RNG-free over placementRaster cells" (review §1 #8) is consistent with this file's idioms — tie-breaks by hash, iteration codepoint-sorted (:358, :508).

**realmMap/realmPlateRenderer.js (378 lines, read whole).** Draw-op vocabulary = the town map's `poly / line / circle / rect / path` serialized only via `drawListToSvg` (:12-13, :49). Trig ban: "NO Date, NO Math.random, NO localeCompare, NO trig, NO `**` — only + − × ÷ and Math.round/min/max/abs/sqrt" (:33-39); direction comes from the 24-ray authored `UNIT_DIRS` table (:76-83) and `DIAG` (:87). The plate draws **no territory**: composition is landmass/relief/roads-MST/markers/furniture (:17-31, roads Prim MST :199-216). Any realm-plate territory shading must be authored-table geometry under this ban; the map-overlay (React/SVG in-browser) path has no such ban.

## 2. FEEDABLE LEDGERS FOR EVENT OVERLAYS (candidate #7)

Headline: **everything is settlement-id-anchored, and exactly one coordinate resolver exists** — `mapState.placements` (`burgId → { settlementId, x, y, cellId, placedAt }`, store/mapSlice.js:154-155), already consumed by the precedent overlay via `pointsBySettlement` (components/map/WarFaithMapOverlay.jsx:50-60). `cellId` on the placement is the natural flood-fill seed cell for footprints. Per-ledger:

| Ledger | Shape + where | Spatial anchor today | Receipt |
|---|---|---|---|
| **Field battles** | Minted per tick in `advanceArmyTransit` (worldPulse/armyTransitKernel.js:803-914); persisted ONLY as a wizardNews entry | ⚠ **Site exists but is not structured.** The battle site `col.region` IS a settlement id — the codepoint-first shared remaining-path node (spatial/armyTransit.js:548-554, typedef :520 "the shared remaining region where they meet") — but `fieldBattleNews` persists `settlementIds: [winnerId, loserId]` (the combatants' homes, :612) and the site only in prose: ":596 A crossing-path collision in ${nameOf(snapshot, battle.region)}'s approaches." The armyTransit spatial ledger is dropped when no army is afield (:916-923), so the site is unrecoverable later. **Overlay needs `region` added as a structured field on the news entry — the one engine-side byte change this lane found** (additive, future-tick entries only; must be declared/gated per dormancy law since news entries persist). | CONFIRMED |
| **Sieges** | Derived read-model `liveSieges` from regionalGraph `war_front` channels + `worldState.deployments[armyId].targetId` (display/warStatus.js:88-113) | Besieged settlement id. Already drawn (siege rings + coalition badge) by WarFaithMapOverlay. | CONFIRMED |
| **Razings** (`believedRazings.js`, 223 lines read whole) | **Not a ledger — a pure read model**: ":14-16 THERE IS NO NEW PERSISTED SURFACE… The source is wizardNews." Underlying surface: entries with `impactKind:'razing'`, victim = `settlementIds[0]` (:106-107), id re-mintable via `razingOutcomeIdFor` → `world_outcome.razing.{road}.{razer}.{victim}.{tick}` (worldPulse/razing.js:181-188) | Victim settlement id. ⚠ **Lossy for ruin markers**: wizardNews caps at `MAX_ENTRIES = 240` (region/wizardNews.js:21) with one-head-per-MAJOR-arc rescue (`capEntries` :650-671); believedRazings :38-44 records the scroll-out hazard. A durable ruin marker should instead read the settlement's own state (see occupations) rather than the feed. | CONFIRMED |
| **Occupations** | `occupiedSettlements`: last `settlement.powerStructure.previousGovernments` entry with `cause === 'conquest'` (warStatus.js:318-335) | Settlement id; **durable** (survives the news cap). Already drawn as occupation shading. | CONFIRMED |
| **Calamities** | Per-settlement `calamityHistory` stamp `{ type, name, year, tick, deaths, exodus, k, targets[], flavorText? }` (worldPulse/calamityKernel.js:77-78, "the settlement's own calamityHistory is the record" :11); news entry scope `'regional'`, significance `'major'`, severity 0.8 (:805-812) | Settlement id; **durable per-settlement history** — the best-anchored feed of all (battlefield-marker analog for #7, footprint seed for #8/zones). | CONFIRMED |
| **Conditions** | `settlement.activeConditions` under the 46-archetype catalog (§3) | Settlement id. | CONFIRMED |
| **Epidemic** | `spatialLedgers.epidemic` keyed by settlement id (spatial/pestilence.js:303, :408-409) | Settlement id per infection record — a "plague reached this far" footprint is the infected-set hull, derivable with no resolver. | CONFIRMED |
| **Faith** | Per-settlement `religionState` (multi-deity shares/niches/patron — worldPulse/religionState.js:2-18), projected onto `config.primaryDeitySnapshot._deityRef` + `config.faithProfile.patronSecurity` (worldPulse/sacredClaim.js:12-18); faith fronts are `religious_authority` regionalGraph edges already drawn by RelationshipEdges (WarFaithMapOverlay.jsx:14-15); realm-level pantheon has no spatial anchor | Settlement id (state); edge-anchored (fronts). Shrine markers per #7 would key off patron standing per settlement — no resolver needed. | CONFIRMED |
| Whole substrate | 48 distinct `spatialLedgers` keys enumerated by grep of `setSpatialLedger` calls (armyTransit, navalTransit, epidemic, migration, embattlement, roads, satellites, tradeFlow, treaties, urbanFabric, …) | — | CONFIRMED (grep, denominators: 48 keys / src/domain) |

## 3. CONDITION CATALOG COMPLETENESS — the tsunami/avalanche/fault gap CONFIRMED but REFRAMED

- The canonical catalog is `CONDITION_ARCHETYPE_TEMPLATES` (src/domain/activeConditions.js:58, re-exported via worldPulse/archetypeCatalog.js:13-15). **All 46 archetypes enumerated** (awk over :58-490): plague, famine, war_pressure, alliance_burden, vassal_extraction, rebellion, faction_challenge, government_overthrown, coup_suppressed, stressor_residual, custom_crisis, magical_instability, trade_route_cut, 13 `regional_*` propagation archetypes, cold_war_sanctions, corruption_exposed, food_anchor_lost, dominant_npc_removed, siege_lifted, war_drain, army_deployed, war_exhaustion, war_mobilization, reinforcement_cost, occupation_lifted, relief_burden, trade_realignment, vassal_trade_coercion, trade_embargo, occupation_resistance, occupation_burden, war_spoils, reconstruction, boom, flourishing. **Zero natural-disaster archetypes** — by design: disasters are not conditions.
- Natural disasters live in `spatial/calamity.js`: `DISASTER_TYPE_BY_TERRAIN = { riverside:'flood', coastal:'storm', mountain:'quake', hills:'quake', forest:'fire', plains:'fire', desert:'storm' }`, default `'storm'` (:87-96) — a closed **4-member flavor vocabulary** {flood, fire, quake, storm}. Crucially, by owner ruling the type is "a COSMETIC flavor HINT… never an engine mechanic. Every mechanical read… is already type-blind" (:80-86); the engine's own title is the bucket-neutral "Great Calamity".
- **Verdict on the review's PLAUSIBLE row (§ line 150):** CONFIRMED that tsunami/avalanche do not exist anywhere (repo-wide grep hit only unrelated files); `quake` already covers the fault register. But the gap is **flavor-table-shaped, not mechanism-shaped**: adding tsunami (coastal) / avalanche (mountain) means widening a one-word-per-terrain cosmetic table — either a second word per terrain picked by keyed fork, or leave it, at zero mechanical cost. The §2 "vocabulary completeness check only" verdict is exactly right. The 15 authored stressor types (data/stressTypes.js:11-159) also contain no natural disasters — consistent, since disasters route through the calamity kernel.

## 4. MARKERS TODAY AND WHERE THE REGISTRY SLOTS

- `MarkersLayer.jsx` (137 lines, read whole): user pin/star/skull/flag with title/note, drag-to-move with once-per-drag undo snapshot (:58-70), double-click edit. Marker record: `{ id: 'mrk_${Date.now()}_${rand}', x, y, icon, color, title, note }` (store/mapSlice.js:630-640) — wall-clock ids are fine here (user annotation, not canon). Persistence: `mapState.markers` (:159), undo via `MAP_UNDO_KEYS = ['placements','labels','markers','forests']` (:174), campaign-scoped (cross-campaign injection guard :729, :745).
- **The registry precedent already exists and is the slot.** Render order in src/components/MapOverlay.jsx: forests → roads → RelationshipEdges → Timelapse → RegionalCausalityLayer → **WarFaithMapOverlay (:337)** → **TravelersLayer (:341)** → PlacementsLayer → MarkersLayer (:343) → LabelsLayer. `WarFaithMapOverlay` is precisely a hand-rolled version of candidate #7's shape: derived from live read-models only, "INERT WHEN ABSENT… renders nothing (returns null) ⇒ byte-identical off-state. Pure read-models only" (WarFaithMapOverlay.jsx:24-26), visibility law reused from the read-model (:16-22), layer toggle `warFaith: true` / `travelers: false` in DEFAULT_LAYERS (mapSlice.js:105, :112). Candidate #7 = generalize its four hand-rolled glyph families into a declarative typed registry (selector + glyph + legend-writer per row) fed by §2's ledgers, rendered as a sibling in the same stack, under the same layers-panel toggle idiom. No persisted state; the regen-hazard dodge in the review holds.

## 5. REALM-HIERARCHY PROMINENCE TODAY (for the prominence-ladder steal)

- **No polity title vocabulary exists.** Grep for kingdom/duchy/empire tokens across src/domain + src/data: hits are prose/comments only (e.g. hegemony.js:10 "THE UNNAMED EMPIRE", convergence.js:244). The only rank ladders are the settlement tier ladder (`TIER_ORDER` thorp→metropolis) and the fixed `REALM_SIZES` pyramids (instantWorld/worldPlan.js:36-60: small 5 / medium 9 / large 14, one dominant centre each).
- **The existing prominence analog is the hegemony read** — and it is a *ruling*, not an accident: "Hegemony is A PATTERN, NEVER AN ENTITY: the engine mints no empire object, no imperial ledger, no name… ZERO persisted state" (worldPulse/hegemony.js:10-16, §F.3b). Sphere = center with `MIN_TIES: 3` subordinate ties over `SUBORDINATING_TERM_TYPES = ['tribute','compelled_alliance','puppet_seat','occupation_continuation']` (:36-37, :57-60); prominence = `SHARE_BANDS` on realm-strength share: dominant ≥0.5 / major ≥0.33 / regional ≥0.2 / minor (:39-45) — a strength-quantile ladder where FMG's is an area-quantile one. Titles are "Descriptive ALWAYS; christened only by the DM (the injected `canonLabelFor` resolver)" (:18-19, :135). Display is a thin re-export (display/hegemonyRead.js:14-19).
- **Framing for the architecture:** the FMG area-quantile prominence ladder (S) fits as an extension of SHARE_BANDS / a derived label tier — but attaching European-subset *forms* (Duchy→Empire) as engine-minted titles would collide with the frozen "never an entity / DM christens" ruling; it must stay a derived display label or go through the owner. Kinship weights for heraldry-late have a real substrate: `TRADE_RELATIONSHIPS = {trade_partner, allied, patron, client, vassal}` + `HOSTILE_RELATIONSHIPS = {rival, cold_war, hostile}` — **in `src/lib/roadNetwork.js:93-100`, not src/domain** (the review's citation says "roadNetwork.js"; the path matters for import hygiene). Our own emblem substrate is exactly 8 hand-authored house emblems ("The 8 house emblems in pools.js", design/organic/ornament/emblemPaths.js:1-12), with a recorded web/PDF seam.

## Cross-cutting for the architecture

1. **Every #7/#8 overlay is settlement-anchored and resolver-complete today** except the field-battle site (structured-field gap, §2 row 1) — the single smallest engine-side prerequisite, and it touches persisted news bytes on future ticks, so it needs a declared shift or a gate.
2. **A #8 territory flood needs pack adjacency the RealmRaster discards** (§1) and cannot be reconstructed from the frozen digest (no coordinates) — it is a view-time derivation over the saved fmgSnapshot's pack, PROMISE-safe by construction.
3. The program's placement-assertion law applies with force: §2 shows the marks' anchors are settlement ids resolved through `placements` — the same single geometry both the dress-page incident and the working market-stall family teach us to anchor to.
4. PLAUSIBLE (not read): naval battle records' anchor (navalKernel.js:246 resolves "shared-sea-edge sea battles" — likely edge-anchored); whether razingExecution.js stamps a durable per-settlement razed mark beyond powerStructure (I verified the occupation path only). Denominator caveat: worldPulse holds 396 files; this lane read the war/faith/calamity/razing/pestilence/hegemony subset in full or targeted scope, plus the five files named in the brief in full.

---

# V-VAR

All verification complete. Assembling the report.

---

# LANE V-VAR REPORT — variability surface + heightmap-DSL landing zone (candidates #11/#12 + suitability defaults)

**Corrections to the brief's own addresses (CONFIRMED):** the plan module is `src/domain/instantWorld/worldPlan.js` (not `domain/campaign/`); `REALM_SIZES` is at worldPlan.js:36-58 (the review's ":37-95" spans the whole knob-vocabulary block). There are now **four** knobs, not three: `MAGIC_CHOICES` (worldPlan.js:109-121, MG-1) rides beside realmSize/tone/mapKind and "draws no rng" (:214-218). Magic must be excluded from any jitter/pin device — it is "asked rather than defaulted, every time" (InstantWorldEntry.jsx:65-68).

## 1. The variability surface today (candidate #11's landing zone)

**Engine (all CONFIRMED, files read in full — worldPlan.js 225 ln, composeInstantWorld.js 398 ln, worldCode.js 145 ln, InstantWorldEntry.jsx 248 ln, kernel/prng.js:22-95):**

- Pyramids are three frozen authored arrays (worldPlan.js:36-58: small 5 / medium 9 / large 14 tiers) with the sizing rationale bound to two external consumers: *"sized to the honest bands the reverse analytics already speak (constructionUsage.js settlementCountBand: 5→'5_9', 9→'5_9', 14→'10_19'; galleryMapsUtils.memberBand)"* (:32-34) and *"counts stay modest so a fresh instant realm sits well inside the canonize-time simulation cap"* (:34-35). **Jitter bands must be chosen against both — a jittered small=4 falls outside every named analytics band.**
- **The randomize-unless-locked device already exists for exactly one knob**: mapKind's `''` sentinel — `rng.pick([...RANDOMIZABLE_MAP_KINDS])` at :183, with the resolved/requested split preserved in the plan (`mapKind` vs `requestedMapKind`, :220-221). realmSize/tone have no sentinel; `normalizeBasicConfig` coerces anything unknown (including `''`) to the fixed default (:153-160). **The cleanest #11(a) landing is extending this exact idiom** rather than importing FMG's gauss-per-slider: `''` realmSize/tone resolved from `rng.fork('pyramid')` / `rng.fork('tone')`.
- **Fork-key safety (CONFIRMED):** `fork` derives from the seed *string*, not stream position (prng.js:84 `createPRNG(\`${seed}::${label}\`)`), so a new fork cannot shift the existing mapKind pick (:183) or the `'sites'` fork (:190) — the same isolation worldPlan already documents for sites (:189-190 "Independent RNG fork so adding/removing a tier can't shift the map-kind roll above"). A pyramid jitter drawn from a new labeled fork is draw-order-safe by construction. Underlying PRNG is `seedrandom(seed)` (prng.js:23) — pure JS, cross-machine stable.
- **UI split (CONFIRMED):** the knobs are ephemeral component state (InstantWorldEntry.jsx:60-63); only magic persists (displayPrefsSlice.js:79, :134-138, clamped at the reader InstantWorldEntry.jsx:52-55). "Surprise me" rerolls **only the seed** (:175, `freshSeed()` = UI-side `Math.random`, :38-40). So #11(b) "keep this" pins are pure UI state: a pinned knob passes its explicit id; an unpinned knob passes the random sentinel. **No engine pin concept is needed at all** — the engine sees only explicit-vs-sentinel, which also makes the share code carry the surprise honestly.

**Three replay contracts bind any jitter (all CONFIRMED):**

1. **worldCode.js is a byte-replay promise**: *"`/world/<code>` regenerates the byte-identical world client-side"* (worldCode.js:7-8), payload `{v:1, s:seed, c:{realmSize?,tone?,mapKind?}}` (:13), decode fails closed on `payload.v !== 1` (:141). ⇒ **once jitter ships, its constants are frozen-for-life for v1 codes, or the payload version must bump** (decode currently rejects any v≠1 — a v2 needs explicit migration support). Note: **magic is NOT in the code** (canonicalPreset keys `['realmSize','tone','mapKind']`, :98) — a shared mundane world replays as magical. Pre-existing, out of lane, but any #11 work in this file will trip over it; flag to the chair.
2. **The composer fingerprint pins sites + tier mix** (composeInstantWorld.js:374-383: members' slot/tier/fp, channels, `sites` slot/tier/x/y). Same-seed jitter ⇒ fingerprint shift ⇒ the determinism-pin goldens re-gold — a declared, dormant-first shift (new plans only; no lived world moves — plans are pre-canonize by construction).
3. **Provenance records the knob id only** (`instantWorld: {realmSize: plan.realmSize, …}`, :336-342). With jitter, the *actual* pyramid is recoverable only while the jitter law is stable — either record the derived tier counts in the provenance marker or version the plan law explicitly.

**Semantic-change caveat (CONFIRMED):** `''` for realmSize/tone today normalizes to the default deterministically (:155-157). Redefining `''` as "seeded random" changes replay only for hand-edited codes (the Segmented UI can never mint one — REALM_OPTIONS/TONE_OPTIONS enumerate only the three ids, InstantWorldEntry.jsx:31-32); declare it anyway.

**Effort check vs review: "S" holds** for the sentinel-extension + fork-jitter + UI pins; it becomes M only if the worldCode payload version is bumped in the same act.

## 2. Suitability-informed wizard defaults — what is actually in hand, per flow

**The capture surface (CONFIRMED, handler read in full):** `settlementEngine:getSpatialPack` returns **exactly** `{cells: {h, biome, r, p, c}}` — heights, biome id, river id, centroids, adjacency — nothing else (sf-bridge.js:875-898). No `temp`, no `prec`, no `fl` (flux), no `t` (coast distance), no `haven`. From these, placementRaster derives the full headless read: 7-way terrain class via `terrainClassOf` + the authored `HILL_HEIGHT` 40 split (placementRaster.js:60-69, :145-148), river flag (:149), and coast = land cell with a water neighbour — *"the ONLY honest coastal signal a pack carries"* (:152-166). It is reachable **only while a live bridge is mounted and ready** (spatialPackCapture.js:100-101 returns null otherwise); no captured pack is persisted anywhere parent-readable (S5, corroborated).

**Per-flow availability at the moment generation inputs are chosen:**

| Flow | Site known at generation? | Pack in hand? | Verdict |
|---|---|---|---|
| Manual wizard (Create) | No — placement requires an already-**canon** save (mapSlice.js addPlacement gate: `not-canon` refusal, :332-337) | No | Suitability defaults **structurally impossible** without a new placement-first wizard entry (new capability, owner-gated by nature) |
| Manual drop of existing settlement | Yes (drop → iframe findCell → `settlementPlaced` echo with cellId → addPlacement, WorldMap.jsx:376-381, useMapBridge.js:105-121, mapSlice.js:339-370) | Yes (live RPC) | Generation already frozen — only levers are the S3 fit/mismatch chip and the existing explicit re-terrain-as-regen offer (autoplacement.js:29-31) |
| Autoplacement | Settlements pre-exist; reads **frozen** genesis terrain (placementRaster.js:50-52 "autoplacement reads it and may never write it") | Yes (full RealmRaster) | Placement-first for *positions* only; suitability→generation is the inverse of what it does |
| **Instant World composer** | Sites planned by worldPlan **before any geography exists** — `mapState.pendingMapGen: true`, `fmgSnapshot: null` (composeInstantWorld.js:294-313); geography materializes later in the iframe (useInstantWorldMaterialize.js:4-18 "FMG is iframe-bound, so its geometry cannot be generated headlessly") | **No — nothing exists to read** | The one flow where suitability-informed tier/resources/tradeRouteAccess would pay, and the one flow structurally locked out |

⇒ **Architecture consequence the synthesis should carry:** the suitability-defaults steal and candidate #12 are **coupled, not independent**. A headless `(template, gridGraph, prng) → heightfield` is the only mechanism that puts terrain in the composer's hands at mint time (and lets worldPlan scatter sites on land instead of a blind central box, worldPlan.js:126-134). Without #12, suitability defaults reduce to the manual-drop fit chip. With today's pack fields, even a live-map suggestion can speak only terrain-class/river-boolean/coast/height — tier suggestions by flux/harbor quality need candidate #3's `fl` and #9's `haven` capture extensions first (one-line getSpatialPack additions; upstream carries both per review #3/#9).

**Cross-lane defect flag (PLAUSIBLE — code-read only, needs an execution lane):** an untouched Instant World may be **unable to spatially canonize at all**. Chain: composer mints every placement with `cellId: null` (composeInstantWorld.js:260); materialize does not re-place (*"staged settlement placements are React overlays … untouched"*, useInstantWorldMaterialize.js:10-11); `restorePlacements` is a documented no-op passing `cellId: p.cellId ?? null` through (sf-bridge.js:594-613); nothing parent-side re-derives cellIds (grep over campaignSpatialCanonize.js + WorldMap.jsx: zero findCell/resolve sites; cellId writers are only addPlacement :370, updatePlacement :451, applyAutoplacement :515-534); and the capture **drops** every non-integer-cellId row (`if (!id || !Number.isInteger(cellId)) continue`, spatialPackCapture.js:79-80) then returns null when none survive (:123). The composer's own comment *"the live FMG cell resolves at canonize"* (:260) matches no code I could find. Cure paths: re-findCell from stored x/y at capture (also the S2 hardening the review already proposes) or an autoplacement/drag pass. This bounds #12's dormancy story too: same-seed instant worlds currently have **no spatial canon to move**.

## 3. The heightmap DSL landing zone (candidate #12)

**Plate today (realmPlateRenderer.js read in full, 377 ln):** draw-op vocabulary is exactly five primitives — `poly / line / circle / rect / path` (townMapDraw.js:62-71), serialized only through `drawListToSvg` (single-writer law, realmPlateRenderer.js:12-14 "It NEVER emits SVG itself"). Purity idiom: *"NO Date, NO Math.random, NO localeCompare, NO trig, NO `**` … only + − × ÷ and Math.round/min/max/abs/sqrt"* (:33-39); trig is replaced by the authored `UNIT_DIRS` 24-ray table (:76-83) and `DIAG` (:87). Terrain is two scalars per mapKind (`KIND_TERRAIN`, :92-100); the coast is a 24-ray star **fitted around the sites** (raw reach per ray :178-186 — the coast encloses sites, not the reverse); relief is `mounts`-many authored carets (:241-259).

**Where the pure heightfield flows in, iframe untouched:** a new pure leaf (e.g. `src/domain/realmMap/heightfield.js`) consumed inside `deriveRealmScene` (:153-219) — heightfield → land/water boundary march → `poly` coast ops; relief from h>60 cells; sea rings from a BFS distance field — with `realmSceneDrawOps`/`drawListToSvg` unchanged. Two-stage consequence: (i) plate-only adoption keeps worldPlan untouched (sites still blind-scattered, coast now real — sites can land in water, so a clamp-to-land pass is needed); (ii) full adoption moves the heightfield **into worldPlan's site scatter**, inverting the coast-encloses-sites causality — a same-seed plan/fingerprint shift, so it must be a versioned, dormant-gated plan law with today's blob as the absent-flag byte-identical path. The iframe keeps generating its own geography; **plate and iframe will never be cell-identical** (different grid, different PRNG, same template family) — state the surface before the arms.

**Graph substrate headless (CONFIRMED): none exists.** Zero importers of `src/domain/realmMap/**` anywhere in src (grep over the whole extracted src tree, clean); placementRaster consumes only a *captured* pack, which requires the live iframe. The DSL needs its own grid. Upstream's substrate (GridGraph.ts, read in full): jittered square lattice + Voronoi — the interpreter actually touches only `points`, `cells.c` (adjacency), `cells.h`, `cellsX/cellsY`, `spacing`, `cellsDesired`, and `Grid.findCell` — and **findCell is pure lattice arithmetic, no Voronoi** (grid-generator.ts:86-90). Adjacency is the sole Voronoi-derived input (voronoi.ts, 177 ln, requires the external MIT `delaunator` package). Two substrate options for the architecture to weigh: (a) port voronoi.ts + take Delaunator (faithful spread character; new dependency; Delaunator is pure float arithmetic, PLAUSIBLE cross-machine-stable); (b) lattice 8-neighbour adjacency over the jittered points (zero deps, ~30 lines; blob fronts follow lattice rings — slightly different coast character). `Invert` assumes row-major indexing (`i % cellsX`, heightmap-generator.ts:490-495) — true under either option.

**Honest port bill — every impurity in heightmap-generator.ts (read in full, 619 ln):**

1. **Global PRNG reseed** `Math.random = Alea(seed)` at :552 (review's cite CONFIRMED), plus ~15 direct `Math.random()` draws inside op bodies (:104, :137, :142, :181, :209-210, :230, :290, :324-325, :343, :386-393, :406) and the util draws behind `rand`/`P`/`getNumberInRange` (probabilityUtils.ts:10-27, :108-127 — note `getNumberInRange("1.5")` = probabilistic fractional count `~~r + P(r-~~r)`; `gauss` is **not** used by the interpreter). All map cleanly onto `createPRNG`'s `random/chance/randInt/randFloat`.
2. **Ambient globals**: `graphWidth`/`graphHeight` (:91-92, :127-128, :200-213, :316-328, :386-393, :472-473), `grid` (:551 default arg), `seed` (:552), `Grid.findCell` (:94, :130, :215-216, :318, :330, :395-396) — all become parameters of the grid graph.
3. **DOM**: `getSelectedId()` → `ensureEl("templateInput")` (:546-548); `window.ERROR` (:65); the `window.HeightmapGenerator` singleton (:619). The grid substrate carries its own DOM read — `getCellsDesired()` → `pointsInput` dataset (grid-generator.ts:82-84). (The review's second DOM read, river-generator.ts:428, is outside a heightfield-only port.)
4. **`fromPrecreated`** canvas/Image path (:586-612) — excluded from a headless port.
5. **d3 helpers** `range/leastIndex/mean` (:2) — pure, trivial re-implementations.
6. **Mutable singleton state** (heights/grid/blobPower/linePower fields) — restructure to a pure closure.
7. ⚠ **The determinism hazard the review under-scoped: fractional `**` is in the interpreter's CORE, not just the coastline cosine.** Blob spread `change[q] ** this.blobPower` (:104), pit/range/trough decay `h ** this.blobPower|linePower` (:137, :232, :345), strait `heights[e] **= exp` (:430), `modify`'s power arg (:449), mask (:474). `Math.pow` is not correctly-rounded (implementation-varying) and the domain idiom bans `**` outright (realmPlateRenderer.js:35). The review's F1 sketch flags authored tables only for F6's cosine roughness. Cure options for the architecture: (a) authored integer-domain tables — `change` values are Uint8 integers 0..100, and blobPower takes only 13 authored values (:24-41), so blob spread tables are 13×101 authorable entries; but Range/Trough's accumulator `h` is a float mid-sequence, needing quantization; (b) a deterministic `detPow` kernel — a fixed polynomial exp2/log2 built from correctly-rounded + − × ÷ is cross-machine stable by construction (the ban targets the *built-ins'* variance, not the operation); (c) accept engine-pinned determinism — weaker than house law, refuse. Also preserve, exactly: Uint8Array ToUint8 truncation on assignment (:97, :104) and `Uint8Array.prototype.map` clamping in smooth/modify/mask — they are load-bearing rounding semantics.
8. **`leastIndex` tie order** in prominence generation (:248-253) — first-minimum wins; keep the iteration order verbatim.

**Templates (CONFIRMED):** 14 upstream with weights (heightmap-templates.ts:158-173) + the fork-authored `sfArchipelago` (sf-bridge.js:96-104, 7 DSL lines, probability 0). The bridge curates only 7 single-landmass kinds — *"no Strait operations that split continents"* (sf-bridge.js:82) — and worldPlan's random pool is 6 (:124). **Pin the headless roster to SF_TEMPLATES ∪ sfArchipelago** so plate and iframe share one mapKind vocabulary; the 8 uncurated upstream templates (continents, archipelago, mediterranean, isthmus, shattered, taklamakan, oldWorld, fractious) enter only if the single-landmass curation rule is deliberately revisited (owner-visible). Template strings verbatim ⇒ MIT notice per `LICENSE-FMG.txt` precedent.

**Size honesty:** interpreter core ≈500 ln + range/probability helpers ≈60 + grid substrate (jittered grid ~80 + either voronoi.ts 177 + Delaunator dep, or ~30 ln lattice adjacency) + coast-march/distance-field ≈100 → **~700-950 ported lines, 0-1 new dependencies**. Review's "L" stands; the `**`-determinism work is the unpriced part.

## 4. Bundle/first-paint budgets that bound all of this

- `verify:dist` = `vitest run tests/build/` (package.json:57 via gate-mutex → check-test-ratchet.mjs `--verify-dist`, runner at :156; 54 test files in tests/build/ at 73f5dfc02).
- **The realm plate occupies ZERO dist chunks (CONFIRMED):** no importer in src (grep clean over the extract); consumers at the tip are only `scripts/generate-realm-preview.mjs`, `tests/domain/realmPlateRenderer.test.js`, and a lint walker (git grep at 73f5dfc02). Its outputs are frozen static `public/landing-maps/*.svg` consumed as `<img>` (realmPlateRenderer.js:41-45).
- Budgets that would bind a *runtime* DSL consumer: the eager-closure byte ratchet — **measured 1,380,062 raw bytes with "~2 KB headroom only"** (vendorPdfLazy.test.js comment lines 130-142) — any eager leak reds immediately; firstPaintNonJs ratchets (HTML 8,600 B / CSS 19,800 B / fonts 90,000 B); engineChunkLazy's modulepreload-exclusion discipline; townMapLazy's `::town-map:v1` fork-key ban — note that fingerprint is minted in **townMapModel.js** and realmPlateRenderer imports only townMapDraw + townMapStyles, so an eager plate leak would evade *that* census and be caught only by the closure byte budget.
- **Budget-safe homes for the port, in order:** (a) build-time script → zero dist bytes (the current plate lane); (b) behind `composeInstantWorld`'s dynamic import — worldPlan already documents the lane (*"reached ONLY behind the composer's dynamic import … costs zero eager bytes"*, worldPlan.js:20-21); (c) its own lazy chunk if a live realm view ever ships. Never eager.

## Coverage denominator

Read IN FULL: worldPlan.js (225), realmPlateRenderer.js (377), composeInstantWorld.js (398), worldCode.js (145), InstantWorldEntry.jsx (248), placementRaster.js (259), spatialPackCapture.js (125, :40-125 quoted), upstream heightmap-generator.ts (619), heightmap-templates.ts (181), GridGraph.ts. Read PARTIALLY: autoplacement.js (:1-150 of 563), mapSlice.js (:320-540 of 838), useMapBridge.js (:95-130), useInstantWorldMaterialize.js (:1-40), WorldMap.jsx (drop + restore regions), sf-bridge.js (:80-150, :594-620, :860-905), kernel/prng.js (:22-95 of 172), grid-generator.ts (:1-150 of 265), voronoi.ts (:1-40), probabilityUtils.ts (:1-127), numberUtils.ts (:15-40), townMapDraw.js (grep survey of 486), displayPrefsSlice.js (grep). Via git show at 73f5dfc02: package.json scripts, tests/build roster, engineChunkLazy/townMapLazy/firstPaintNonJs/vendorPdfLazy heads, .size-baseline.json. NOT read: AutoplacementConsent.jsx body, campaignSpatialCanonize.js body (grep only), spatialCost.js body (review-cited), configVocabulary.js body, the fork bundle. Nothing was executed — every dynamic-behavior claim above (notably the null-cellId canonize lockout) is PLAUSIBLE until an execution lane runs it; all file:line quotes are CONFIRMED reads.

---

# V-FAITH-GOODS

LANE V-FAITH-GOODS REPORT — all paths relative to root `OURS = /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours` (extract of build tip 73f5dfc02); tests cited via `git ls-tree 73f5dfc02` in the repo. Every claim CONFIRMED (read) unless marked PLAUSIBLE.

## 1. FAITH ORIGINS LINEAGE — the review's PLAUSIBLE gap is CONFIRMED: no lineage/provenance DAG field exists anywhere in the faith records

Every faith record shape, read in full at its mint site:

- **Deity snapshot** (the embedded authority record) — `OURS/src/domain/deitySnapshot.js:24-33`: `{ name, alignmentAxis, temperamentAxis, rankAxis, lawAxis, ...(raw.domain ? { domain } : {}) }`. Six fields, no origin.
- **Per-settlement deity ENTRY** — four mint sites, all in `OURS/src/domain/worldPulse/religionState.js`: legacy patron seed (`:175`), imposed-cult seed (`:196`), DM re-assign (`:220`), and arrival via `attemptEntry`'s `seed()` closure (`:305` area): `{ deityRef, snapshot, niche, share, standing, standingHeld, suppressed, legitimacy, tenure, heresyStain }` — plus the conditionally-materialized `suppressedAtTick` (one writer, `suppressDeity`, `:234` "⛔ THE ONE WRITER of `deities[ref].suppressed` and `deities[ref].suppressedAtTick`"). No parent-faith, no source-settlement, no founding-cause field.
- **Realm pantheon ledger entry** — `OURS/src/domain/worldPulse/pantheon.js:122`: `@typedef {{ wins: number, losses: number, seats: number, tier: string, tierHeld: number }} PantheonLedgerEntry`. No origin.
- **Per-settlement religion state** — `religionState.js:168`: `{ deities: {}, patronRef: null, patronHeld: 0, patronChallengeTicks: 0, contestedTicks: 0, capacity }` (+ conditional `patronFalls`, `noneShare`).
- **The closest existing thing to lineage** — `OURS/src/domain/worldPulse/patronFall.js:46-53,76,83,138-145`: a capped ring `worldState.religionStates[cid].patronFalls = [{ ref, cause, atTick }, …]`, `PATRON_FALL_CAUSES = Object.freeze(['discredited','displaced','imposed','suppressed'])`, `FALL_RING_CAP = 3`. It records the OUTGOING patron only — no successor ref, no parent link, capped at 3 — so a chronicle can name the last three falls but **cannot walk creed→creed descent**. Negative control: repo-wide grep for `lineage` hits only regime lineage (`previousGovernments`, `src/pdf/sections/PowerStructure.jsx:285-298`) and town-map prose; grep for `origins\s*[:=]` in domain/store hits only migration origins (`demographicsMigration.js:454-494`). REFUTES nothing; the review's "PLAUSIBLE that we lack one" upgrades to CONFIRMED.

**Where a typed `origin` field would mint** (the writers, exhaustively — 4 entry paths):
1. `attemptEntry` (`religionState.js` `seed()` closure) — the spread/eviction arrival. Note the source settlement is NOT in scope there; the kernel caller (`religiousContest.js` — its conversion outcome already carries `conversionCause` and `sourceEventTargetId` transiently, `:404-430`, discarded at tick end) would thread it through the existing `opts` bag, the exact WF-1b precedent ("`tick` joins the EXISTING bag rather than widening the signature", `religionState.js` attemptEntry JSDoc).
2. `ensureReligionState` three seed sites (founding patron / imposed cult / DM assign) — origin kinds `founding`, `imposed`, `assigned`.
3. `reconcileCultImposition` in `cultImpositionApply.js` (the eager IMPOSE_CULT handler path — re-exported at `religionState.js:28-29`).
4. `latentPantheon.js` activation is a pure COPY of persisted latent records (`:1-45`, "reader of persisted data, never a producer") — no mint there.

**Governing law and censuses the architecture must satisfy** (from the modules' own contracts + test roster at 73f5dfc02):
- Conditional materialization: absent-when-dark, appended LAST — the `suppressedAtTick`/`patronFalls`/`noneShare` idiom ("with no finite tick the key is never created, so the serialized bytes are unchanged", `religionState.js:250-258`; "NO `patronFalls` key at all — absent, never `null`, never `[]`", `patronFall.js:127`). A lineage field on records that already exist would be a byte shift on every live faith world unless minted only for NEW entries — dormancy-safe by construction if absent-tolerated.
- DEITY DOCTRINE binds token content: origin causes must name believer/political acts, closed vocabulary, no free text (`patronFall.js:11-17` is the written precedent). FMG's `origins` DAG structure is adoptable; their name/meaning pools are not.
- Censuses: `tests/domain/patronFall.test.js` (declared `@enforced-by`; write-site census pin per `religionState.js:241-243`), `tests/lint/faithKindPools.walker.test.js`, `tests/lint/noPremadeDeityPool.walker.test.js`, `tests/domain/religionDormancy.byteIdentity.test.js`, `tests/property/worldpulseDeityGolden.test.js`, `tests/domain/deityEmbedWriterParity.test.js`, `tests/domain/religionState.test.js`/`advanceReligionStates.test.js`/`pantheon.test.js`, `tests/lint/.domain-any-baseline.json` (monotone-down any-ratchet — `religionState.js` and `pantheon.js` both state they sit AT their frozen allowance, so new code must be structurally typed), plus the worldState conditional-ledger list (`worldState.js:440` `'pantheon', 'religionStates', …`) and the dormancy oracle (`:504`). **`fieldManifest.js` has ZERO faith rows** (grep count 0 of 453+ lines) — its own contract ("every new generation-written, campaign-relevant field family is expected to add a row") would apply if lineage is generation-seeded. FMG's reserved-origin-id regeneration trick (ANTHRO parity note) is relevant the day lineage records face regen — our preservation path is `regenerationPreservation.js`/`locksPreservation.js`.

## 2. GOODS CATALOG UNIFICATION — the "5-file split" is real but UNDERCOUNTED (≥9 modules), and "pure refactor, no behavior change" has two hard constraints the review glosses

The five named files and their record shapes (each read):

| file | shape (quoted) |
|---|---|
| `OURS/src/data/resourceData.js:14` `RESOURCE_DATA` (+`SPECIAL_RESOURCES` `:371`) | per-resource `{ label, desc, commodities[], instBoosts{inst→mult}, tradeGoods[], forbidden[], terrainRequired[], warning, category }`; SPECIAL_RESOURCES `{ name, description, effects:{ institutionModifiers[{name|tags,modifier}], resources[], tourism } }` |
| `OURS/src/data/resourceChains.js:35` `RESOURCE_CHAINS` (+`INDUSTRY_WATER_NEEDS` `:262`) | per-chain `{ rawResource, processingTags[TAG.*], processingInstitutions[], intermediateGoods[], finalProducts[], exportValue, dependsOn[] }` |
| `OURS/src/data/economicData.js:9` `TRADE_DEPENDENCY_NEEDS` | per-institution-NAME `{ resources[], label, detail, svcs[] }` — "keys are looked up by EXACT institution name … enforced by tests/joins/goods.test.js" (`:5-7`); `:866` re-exports `INSTITUTION_FINISHED_GOODS_DEMAND, finishedGoodsCategoryOf` from `finishedGoodsCategory.js` |
| `OURS/src/generators/tradeCommodity.js:23-39` `COMMODITY_SCAN` | ordered `[keywords[], label]` pairs; "Order is load-bearing: fish before salt keeps 'Salted fish' → fish" (`:14-16`) |
| `OURS/src/domain/resourceSemantics.js:42` `RESOURCE_SEMANTICS` | per-resource `{ type: renewable\|exhaustible\|positional\|infrastructure\|magical, randomDepletionEligible, recoveryMode, depletedDescription, shortageImport }` — "deliberately exhaustive rather than inferred … pinned by resourceTaxonomyClassification.test.js" (`:12-15`) |

**Consumer census (denominator = importer files in `src/`, verified as import lines):**
- `resourceData.js`: **17** — EventComposer.jsx, domain/events/mutateWorld.js, resourceSemantics.js, resourceSites.js, resourceTerrainCompatibility.js, worldPulse/{institutionLifecycle, resourceDynamicsKernel, resourceTaxonomy, steadingTopography, tierResourceDynamics}.js, generators/{computeActiveChains, resourceGenerator, structuralValidator}.js, generators/economy/tradeGoods.js, generators/steps/{assembleInstitutions, resolveResources}.js, lib/customRegistry.js. Plus a hidden alias: `structuralValidator.js:26` `export { SPECIAL_RESOURCES as RELATION_TYPES }` consumed by `domain/display/discourseKernel.js`.
- `resourceChains.js`: **3** — resourceGenerator.js:10, economy/viability.js:8 (`INDUSTRY_WATER_NEEDS`), display/institutionProfile.js:23. (Grep hits in EntityPicker/customRegistry/dependencyEngine/prebuiltResourceChains are the string category `'resourceChains'`, not the file.)
- `economicData.js`: **2** — economy/economicState.js:9, economy/finishedGoodsDemand.js:6.
- `tradeCommodity.js`: **3** — historyGenerator.js:13, narrativeGenerator.js:37, npcGenerator.js:7.
- `resourceSemantics.js`: **13** — resourceSites.js, undercity/monotoneComponents.js, worldPulse/resourceTaxonomy.js, generators/{computeActiveChains, foodGenerator, generationCoherence, resourceGenerator}.js, economy/{economicState, foodBalance, nativeEconomicInputs, tradeGoods}.js, steps/{generateNarratives, resolveResources}.js.

**The split is actually ≥9 modules**: the same vocabulary continues into `src/data/tradeGoodsData.js` (`GOODS_CATEGORIES:10`, `IMPORT_GOODS_BY_TIER:26`, `GOODS_MODIFIERS_BY_TIER:140`, `COMMODITY_CATEGORY_MAP:529` — 9 importers), `src/data/finishedGoodsCategory.js` (`INSTITUTION_FINISHED_GOODS_DEMAND:26` — demand `{consumers:{name:{demand:1..5}}, supplyKeywords, importLabels, exportBonus}`), and `src/data/supplyChainData.js` (`SUPPLY_CHAIN_NEEDS`, 10 importers incl. pdf/SupplyChainFlow.jsx). An FMG-shaped deposit→recipe→demand→price unification that stops at the named five leaves the demand and category halves split.

**Two constraints that bound the "pure refactor" claim:**
1. **First-paint chunk law (CONFIRMED):** `resourceChains.js:1-17` — "these two tables are LAZY-ONLY … extracted out of resourceData.js (whose RESOURCE_DATA/SPECIAL_RESOURCES tables ARE eagerly reached by customRegistry/structuralValidator) … DO NOT re-export these from resourceData.js — an eager re-export would re-drag them into first paint," `@enforced-by tests/build/vendorPdfLazy.test.js`. A literal single-file catalog is FORBIDDEN by an existing gate; unification must be a single logical namespace over ≥2 physical chunks (eager identity/terrain half vs lazy chain/demand half), or record-merge without file-merge.
2. **Catalog shapes leak into PERSISTED saves (CONFIRMED):** `src/pdf/lib/viewModelBodySlices.js:268-270` — "Entries are RESOURCE_CHAINS objects — resource in `rawResource`, processing in `processingInstitutions[]`, outputs in `finalProducts[]`" read back out of a settlement's stored `resourceAnalysis.exploitation`. Renaming fields in a unified record is not display-only: legacy saves hold the old field names, so any reshape needs the reader's existing fallback chains preserved. Behavior-neutrality is provable via the same-seed goldens; the join gates that must stay green: `tests/joins/goods.test.js`, `resourceChainCatalog.test.js`, `resourceChains.test.js`, `finishedGoodsDemand.test.js`, `customTradeGoods.test.js`, `tradeGoodsRegistry.test.js` (all present at 73f5dfc02). Also `tradeCommodity.js`'s scan-list ORDER is load-bearing (`:14-16`) — a unification that re-derives commodity labels from a canonical record must reproduce that order or declare a prose shift.

## 3. PIPELINE-AS-DATA — what the composer would concretely become

**The settlement idiom (CONFIRMED, read whole):** `OURS/src/generators/pipeline.js` — module-global registry `registerStep(name, { deps, provides, reads, mutates, scratch, phase }, fn)` (`:84-89`), topo sort with cycle detection (`:95-119`), per-step keyed PRNG fork `rng.fork(name)` + ambient-RNG save/restore for NESTED runs (`:156,174-178`), strict-mode undeclared-write/premature-read detection (`:55-65,162-171,184-193`). 22 steps registered in `steps/index.js:8-29` in this order: resolveConfig → buildGenerationContext → resolveResources → resolveStress → resolveNeighbour → assembleInstitutions → subsumptionPass → cascadePass → isolationPass → stressConfirmPass → generateEconomy → generatePower → neighbourFactions → factionCorrelationPass → coherenceRepairPass → economyReconcilePass → powerEconomyReconcilePass → structuralValidationPass → generatePopulation → corruptionPass → generateNarratives → assembleSettlement (actual run order is the topo sort of declared deps). Upstream's idiom for comparison (`fmg-upstream/src/generators/generation-pipeline.ts:6-58`): a flat `{ id, run }` array passed to an INSTANCE — `new Pipeline("Generation Pipeline", generationPipelineSteps)` — with a second `erasePipelineSteps` subset reusing the same step bodies; no deps/provides contract at all (ambient globals). Ours is the stronger contract; what upstream has that ours lacks at the composer is *any* declared list.

**composeInstantWorld's current shape (CONFIRMED, read whole, 397 lines):** one hand-ordered function body (`OURS/src/lib/instantWorld/composeInstantWorld.js:193-351`) with these phases: (1) `normalizeBasicConfig`+`deriveWorldPlan` `:202-203`; (2) resource setup — `mkId` counter + single `now` `:206-208`, `campaignId = mkId()` FIRST `:210`; (3) mint members: `plan.sites.map` → `memberConfigFor` + `mintSettlement` (dossier-trust retry loop, `MAX_COHERENCE_RETRIES = 3`, seed re-fork `` `${site.seed}::retry${attempt}` `` `:140-167`), one `mkId()` per site `:223`; (4) `dedupeWorldFactionNames(settlements)` — **in-place mutation post-pass** `:251`; (5) placements `:254-263`; (6) `deriveGraphWithDiscoveredCandidates` `:266-270`; (7) worldState from tone preset + conditional `realmMagicDefault` `:287-291`; (8) mapState `:294-313`; (9) campaign assembly incl. wizardNews `updatedAt` normalization `:326`; (10) `instantWorldFingerprint` LAST `:349`.

**Hidden couplings a step-ization must declare (the actual content of the candidate):**
- **id-order coupling:** the default `idFactory` is a shared counter (`` `iw::${plan.seed}::${_idN++}` `` `:207`); campaign-before-members and members-in-site-order is what makes fixed-idFactory runs byte-identical (`:41-45`). Any step reorder shifts every id. A pipeline-as-data version must either key ids (`iw::seed::campaign`, `iw::seed::slot3`) or pin the mint order as a declared dependency.
- **parallel-array coupling:** placements join `plan.sites[i]` to `settlements[i]` by INDEX (`:255-257`); the join key that exists is `site.slot`/`burgId` and `settlement._slot` — a data pipeline should join on slot, not order.
- **undeclared in-place mutation:** `dedupeWorldFactionNames` mutates minted settlements (`:244-251` — "only the composed bundle's faction names (and the derived fingerprint) change"); it must run after mint and before fingerprint; graph discovery (`discoverDependencyCandidates.js`) reads settlement `exports/imports/name` (`:302-326`), not faction names, so dedupe→graph order is currently inert — but nothing declares that (exactly the class strict-mode `mutates` was built to expose, `pipeline.js:23-31`).
- **the shared `now`:** one timestamp stamped into saves `savedAt`, placements `placedAt`, wizardNews, mapState, campaign — a declared resource, not per-step clocks.
- **no composer-level RNG:** all randomness is behind per-site seeds minted by `deriveWorldPlan` (`worldPlan.js:180-191` — its own fork discipline: "Independent RNG fork so adding/removing a tier can't shift the map-kind roll"). The per-step `rng.fork(name)` feature is NOT needed; the analog (seed keying) already exists.
- **registry reuse hazard (load-bearing):** `pipeline.js`'s `_steps` is ONE module-global Map and `getStepOrder()` walks ALL registered steps (`:117`) — registering composer steps there would splice them into the settlement pipeline's topo sort (which the composer itself invokes per member, nested). So "pipeline-as-data for the composer" concretely = an **instance-scoped** step list (upstream's `new Pipeline(...)` shape) carrying our `reads/provides/mutates` contract + the strict-mode write detector extracted from the global registry — not a literal `registerStep` reuse. Effort matches the review's S-M. Constitution: pure reshuffle of an already-deterministic composer; the byte-identity pin is `instantWorldFingerprint` + the fixed-idFactory determinism note (`:41-45`); no canon, no dormancy surface touched.

## 4. TWO-ENDPOINT-PER-WATER-BODY — a water-body id ALREADY EXISTS engine-side (derived, transient), and the invariant is ALREADY structurally enforced at the lane tier; candidate #10 is NOT a dependency

- **What the capture holds (CONFIRMED):** `OURS/public/map/sf-bridge.js:875-897` `settlementEngine:getSpatialPack` returns exactly `cells: { h, biome, r, p, c }` — no FMG `f` (feature id), no `t` (coast distance), no `haven`, no `features` array. Negative control: grep `haven` across `src/` hits only unrelated prose/preset keys. Water engine-side is the predicate `h < LAND_HEIGHT` (`spatialCost.js:144,161-164` — "Infinity ⇒ impassable (ocean / off-map)"; sea cost `h >= 15 ? 1.2 : 0.9`).
- **But identity is DERIVED at digest build (CONFIRMED — this corrects the review's framing):** `OURS/src/domain/spatial/seaLanes.js:307-325` `waterComponents(pack)` — "Label every navigable-water cell with its connected-component id (BFS over pack adjacency); land cells stay -1. Two ports can only share a lane if the water they touch is the SAME component — so no lane crosses land or joins two separate seas." Navigable = ocean (`h < LAND_HEIGHT`) ∪ river-course land (`r != 0`) (`:292-298`); `buildSeaLaneSet` groups ports into sailing groups via union-find over components a port touches (confluence port unions river+sea, `:438-455`).
- **The invariant itself is already enforced where lanes are built (CONFIRMED):** `seaLanes.js:472` — `if (members.length < 2) continue; // a lone port in its water body ⇒ no lane` — plus the global dormancy floor `if (ports.length < 2) return null` (`:435`). So a lone port can never mint a dangling sea route today.
- **What the FMG steal (`burgs-generator.ts:283-291`) actually adds:** upstream applies the rule at GENESIS — it avoids MINTING a lone port at all. Ours derives port status from geography ∧ institutions (`:14-27`, "A coastal settlement WITHOUT a dock is a beach, not a port"; eligibility record `:248-251` `{ id, cellId, coastal, river, waterAccess, port }`), so a lone port is legal and silently laneless. The adoptable remainder is (a) a wizard/autoplacement-side advisory ("this harbour's water body has no partner") and (b) a legibility surface for WHY a port has no lanes — both pure reads of `waterComponents` output. Note the component labels are TRANSIENT: the frozen slot persists only `{ version, ports, edges, stormSeasonCost }` (`:430`), so any new consumer either re-derives (pure, cheap, deterministic from frozen `h/r/c`) or the digest grows a new opt-in sub-slot (a discrete `SEA_LANE_VERSION`/re-canonize event per `:71-74`).
- **Dependency ordering vs candidate #10 (lake typology):** NOT a prerequisite. Water-body *identity* needs only captured `h + r + c`, already held and already flood-filled. #10 adds *subtype* (`frozen|dry|salt|freshwater`) and needs a DIFFERENT partition — `waterComponents` deliberately fuses lakes, rivers, and ocean into sailing groups, while #10's typology needs interior (`h<20` non-border) components distinguished from the border-touching ocean. Architecture note: mint the canonize-time water flood-fill ONCE with both views (sailing components; interior-vs-ocean + subtype when #10 lands) rather than a third ad-hoc BFS — the existing one is private to `seaLanes.js`. Constitution: everything here is pure derivation from the frozen pack, re-canonize-disciplined (`:71-74` "Never a silent drift on load"), dormant-by-null (`:58`).

**Cross-cutting for the synthesis:** review candidate line 100's "5-file split" should be re-scoped to the ~9-module goods vocabulary with the two physical-chunk and persisted-shape constraints above; the faith-origins steal is cheap and clean exactly because the conditional-materialization idiom it needs is already the house style in the same files; the composer steal's real work is naming the four hidden couplings, not the runner; and the two-endpoint steal is 80% already built — charter it as advisory + legibility, not as a new invariant.

---

# V-COLLIDE

All four questions are answered with receipts. Final report follows.

---

# LANE V-COLLIDE REPORT — sequencing collisions + gate economics for the FMG-WEAVE program

Basis: review synthesis + candidate sketches read in full (`scratchpad/REVIEW_FMG_RECONCILIATION.md:13-161`); STRIP charter §11/§11.2/§11.3 read verbatim (`git show db1d80bee:docs/DESIGN_MAP_MODULE_SPLIT.md`); DW program §3/§9/§10/§16 + spine amendment (`git show 1d0ae3526:docs/DESIGN_DWELLINGS_PROGRAM.md`, 446 lines); extracted tree at build tip (`scratchpad/fmg-study/ours`); test tree enumerated at `73f5dfc02` (2,641 files under `tests/`). Nothing executed; every claim is read-and-cited. Family definitions used: SEAM (S1–S5 repairs), CAP (#3 #6 #10), NAME (#1 #4), NET (#2 #9), POLIS (#5 #7 #8), VAR (#11 #12).

## 1. OVERLAP MATRIX

**Family file rosters (CONFIRMED from the review's own sketches + located in the extracted tree):**

- **SEAM**: `public/map/sf-bridge.js` (S1 no `fmg:terrainChanged` listener; S5 re-canonize; capture at `:875-901` — `'settlementEngine:getSpatialPack'` "READ-ONLY snapshot… freeze-first"), `src/domain/spatial/spatialDigest.js` (S2 `resolveSeeds`), `src/store/campaignSpatialCanonize.js` (S5), terrain-enum vocab (S3), road truth (S4: `sf-bridge.js:626-676` + `src/lib/roadNetwork.js` + `src/domain/worldPulse/routeNetworkGenesis.js`).
- **CAP**: `sf-bridge.js:875-901` (extend capture with `fl`/`temp`/`prec`), `src/domain/spatial/spatialDigest.js` (additive sub-digest idiom), `src/domain/spatial/spatialCost.js`, `src/domain/spatial/seaLanes.js` (#3 `river ∧ navigable`), `src/domain/worldPulse/seasons.js` (#6 `SEASONS_TUNING`, `seasons.js:77-85` amplitude table confirmed), `src/domain/worldPulse/steadingTopography.js` (#10 consumer), `src/store/campaignSpatialCanonize.js` (opt-in threading), `src/domain/worldPulse/simulationRules.js` (flags).
- **NAME**: new `src/domain/naming/` dir; `src/data/namingData.js` (kept as the dark path — **8 importers besides itself**, denominator `grep -rln namingData src` = 9 hits: townSceneExport, townCartographyBlock, townScene.worker, npcGenerator, settlementLifecycleKernel, **townCartography/cartographyWards.js**, compileTownSceneManifest, npcOps); draw site `settlementLifecycleKernel.js:358-377`; #4 route names on `routeNetworkGenesis.js` ledger rows + display/herald estate; war-name deriver display-side over `warReason`.
- **NET**: `src/lib/roadNetwork.js:240-284` (MST), `sf-bridge.js:626-676` (A* handler) + `:678-684` (water cost, #9), `routeNetworkGenesis.js` + the digest edge set (tier b), `seaLanes.js`/`spatialCost.js`. The worldPulse `routeNetwork*` family is **17 src files** (`ls src/domain/worldPulse | grep -c '^routeNetwork'` = 17).
- **POLIS**: `src/lib/instantWorld/composeInstantWorld.js` + `src/domain/instantWorld/worldPlan.js:174-222` (#5), `src/domain/realm/placementRaster.js` + a new territory module (#8) consumed by `src/domain/realmMap/realmPlateRenderer.js`, `src/components/MapOverlay.jsx` + `src/components/map/MarkersLayer.jsx` + `src/store/mapSlice.js` (#7 view), read-only feeds `believedRazings.js`/war ledgers/faith.
- **VAR**: `worldPlan.js:37-95` (`REALM_SIZES`) + wizard UI (#11); new headless heightmap interpreter module + `realmPlateRenderer.js` coast vocabulary + `src/kernel/prng.js` threading (#12).

**Conflict table (family × program → shared files):**

| | STRIP-1 UI | STRIP-2 exports | STRIP-3 store | STRIP-4 domain | STRIP-5 entitl. | STRIP-6 sweep | DW | worldPulse shared |
|---|---|---|---|---|---|---|---|---|
| SEAM | NONE | NONE | **campaignSpatialCanonize.js** (S5) — not on STRIP-3's roster, but same store estate | NONE | NONE | grep-roster (V4) | NONE | simulationRules.js if S-repairs mint flags |
| CAP | NONE | NONE | campaignSpatialCanonize.js (opt-in threading) | NONE | NONE | grep-roster | **goods files if #10 consumers grow** — see V6 | seasons.js, steadingTopography.js, simulationRules.js |
| NAME | NONE | NONE | NONE | **namingData consumer census: cartographyWards.js dies at STRIP-4** (V5) | NONE | grep-roster | **news/herald estate vs DW-7 news hooks** (V6) | settlementLifecycleKernel.js, simulationRules.js |
| NET | NONE | realmMapExport.js (road/lane drawing rides exports; STRIP-2 edits its town half — `realmMapExport.js:34` imports `townMapExport.js`) | mapSlice.js (roads analytics seam, `mapSlice.js` FP-G9 block) | NONE | NONE | grep-roster | NONE | routeNetwork* ×17, simulationRules.js |
| POLIS | lazy-chunk/size censuses only (V3) | realmMapExport.js (overlay content) | **mapSlice.js — DIRECT COLLISION, see V1** | NONE | NONE (dm-pins is townMap authoring: `entitlementLadder.js:92` + claim site `src/components/townMap/SettlementMapEditControls.jsx`; MarkersLayer has zero entitlement reads — CONFIRMED by grep) | **grep-roster — WORST CELL (V4)** | believedRazings/faith read-only; **worldPlan shared with VAR** | worldPlan.js (with VAR), believedRazings.js (read), simulationRules.js |
| VAR | wizard UI censuses | NONE | NONE | NONE | NONE | grep-roster | NONE | worldPlan.js (with POLIS), simulationRules.js |

**WEAVE×DW, precisely (brief item c):** DW's §3 input table is **read-only by charter** ("what each existing system supplies (read-only)"), and the DW doc names **zero** concrete `.js` paths (grep over the ledger doc: 0 hits — CONFIRMED). So WRITE×WRITE intersection is exactly **`simulationRules.js`** (DW-1+ mints dwellings flags; every WEAVE flag-minting family appends the same frozen list — append-contention only). WRITE(WEAVE)×READ(DW): (a) the **goods-catalog unification steal** rewrites the 5-file split DW's "economy/chains" read maps onto — `src/data/{resourceChains,resourceData,tradeGoodsData,finishedGoodsCategory,supplyChainResourceIndex}.js` (CONFIRMED present); (b) the **faith `origins` lineage steal** adds a field to records DW reads tenure/endowment from; (c) NAME #4's war/route names enter the same address-law news estate DW-7's renovation news emits into (DW-7 is late — post-DW-6 — so NAME lands first and DW inherits). The retained townMap substrate (`buildTownMapModel`+layout via `spatialSubstrateDerive.js`, 11 sim consumers, J-STRIP-1) is a **STRIP×DW** shared surface that **no WEAVE family touches** (CONFIRMED: no candidate roster includes it).

## 2. GATE ECONOMICS

**The census law (from memory `a-new-test-file-reds-two-censuses-at-landing.md`, verbatim mechanics):** a new test file reds (1) `sovereigntyLightingContract` (exact `.toBe()` on `TEST_FILES.length` — re-derive **all five figures whole**, same commit); (2) `negativeAssertionAnchor` if under a generation-facing root (every new file starts at ceiling 0); (3) `mutationCoverageManifest` TOTALITY if under the seven enforcer dirs **or its basename contains** `census|scan|baseline|ratchet|walker|killlist|parity|coverage|governance|freshness|integrity|exhaustiveness|roundtrip|golden|contract|pin` — note **"golden" is in the basename list**, so every new `*DormancyGolden.test.js` owes a manifest entry (`rationale` ≥ 40 chars is the estate's normal answer; `uncovered` is refused twice). Also: `scripts/.test-ratchet-baseline.json` is at its **literal ceiling 17, monotone-down** (`testRatchet.test.js:177`) — no new red can be banked, ever. Standing sequence when several new files land: **land all lanes, then re-derive the census ONCE** — sequential re-derivations each cost a full suite run and invalidate the last. Vitest invocation hazard: the working worker cap is `--pool=threads --maxWorkers=2`; `--poolOptions.threads.*`/`--minWorkers` don't exist in vitest 4.1.8 and exit 1 over **zero collected tests**.

**Per-family extension rosters (existing files, all verified present at 73f5dfc02):**

- **SEAM**: `tests/map/sfBridge.harness.test.js` (has a dedicated `getSpatialPack — the constitutional capture seam` describe at `:82`), `tests/map/sfOrigin.harness.test.js`, `tests/domain/spatialDigest.invariants.test.js` (S2 resolveSeeds), `tests/domain/spatialDigestMemo.test.js`. ⚠ `campaignSpatialCanonize` has **no direct test file** (only `tests/lint/engineGatedRuleKeys.walker.test.js` mentions it — denominator: 1 grep hit across tests/) — S5's re-canonize proof plausibly needs a **new file**; budget it.
- **CAP**: `tests/map/sfBridge.harness.test.js` (capture field-set), `tests/domain/spatialDigest.invariants.test.js`, `tests/data/biomeTexture.test.js` + `tests/domain/spatialBiomeTruth.test.js` (the V-6 additive-sub-digest precedent — the exact pattern #3/#6/#10 follow), `tests/domain/seaLanes.test.js` (#3 port gate), `tests/domain/seasonalRoads.test.js` + `tests/domain/seasonalFoodYear.test.js` + `tests/domain/seasonsDormancy.byteIdentity.test.js` (#6), `tests/domain/steadingTopography.test.js` (#10), goldens per §3 below. New-file need: plausibly **zero** if each banding lands as an extension of these.
- **NAME**: `tests/data/namingDecontamination.test.js`, `tests/domain/settlementLifecycleFirstClass.test.js` (kernel draws), `tests/property/settlementLifecycleDormancyGolden.test.js` (flag-dark half), `tests/domain/routeNetworkLedger.test.js`/`routeNetworkGenesis.test.js` (route-name rows), `tests/copy/voiceMechanics.test.js` (roadsProse register), herald component suites (`tests/components/herald*.test.jsx`, 10+ files), `tests/domain/dispositionNews.test.js`. **Genuinely unavoidable new file: the Markov module itself** (new `src/domain/naming/` has no test home; nearest file tests data hygiene, not an algorithm) — budget 1 file (+1 if a naming dormancy golden is minted rather than extending settlementLifecycleDormancyGolden; recommend extending).
- **NET**: `tests/lib/roadNetworkMst.test.js`, `roadNetworkIndex.test.js`, `roadNetworkLaneHostile.test.js`, `tests/map/sfBridge.harness.test.js` (#2a threading + #9 water cost), the 13 `tests/domain/routeNetwork*.test.js` files (Certification/Charter/Consumers×4/Danger/Decay/Dormancy/Flows×2/Genesis/Ledger), `tests/domain/seaLanes.test.js`, `tests/property/roadsCharter.test.js`. Tier (b) digest-edge work extends `spatialDigestGolden`/`routeNetworkDormancy`. New-file need: zero for tier (a); tier (b) plausibly zero.
- **POLIS**: `tests/lib/instantWorld/composeInstantWorld.test.js` (+3 siblings), `tests/domain/envoyDiplomacy.test.js` (#5 relation vocab), `tests/domain/autoplacement.test.js` (imports placementRaster — the only test file that does; denominator: 1), `tests/store/autoplacementStore.test.js`, `tests/domain/realmPlateRenderer.test.js` (#8 shading), `tests/components/warFaithMapOverlay.test.jsx` + `tests/lib/regionalMapOverlay.test.js` + `tests/components/mapOverlayThumbContract.test.jsx` (#7 overlay estate). **Unavoidable new file: the #8 territory-flood module** (M-L sized, program law demands placement-assertion censuses — review §1.8 "requires placement-assertion censuses per program law"); budget 1–2 files (module + placement-assertion arm; keep the assertion OUT of any gate until its bridge lands, per §710.6 law).
- **VAR**: `tests/domain/instantWorldMagicKnob.test.js`, `tests/lib/instantWorld/composeInstantWorld.test.js`, `tests/ui/instantWorldMagicQuestion.test.jsx` (#11), `tests/domain/realmPlateRenderer.test.js` (#12 coast ops). **Unavoidable new file(s): the ~600-line heightmap interpreter** (#12 — new pure module, verbatim template goldens, PRNG-threading proofs; nothing existing covers it); budget 1–2 files. Note "golden" in a basename triggers the manifest bill.

**Structural walkers every family pays regardless of file count:** `tests/property/mechanismLitCoverage.test.js` — enumerates **every flat `*.js` under `src/domain/worldPulse` (~156 today)** and **every `<x>Enabled` key**; a new mechanism or flag with no lit credit **fails against `tests/fixtures/mechanism-lit-coverage-baseline.json`** ("A NEW mechanism or flag without a lit proof fails here"). `tests/lint/entropyRootCensus.walker.test.js` — **two exact-set shrink-only baselines** (COMPOSITIONS + READ_SITES); "A NEW composition or a NEW read landing unclassified REDS, and the fix is a disposition row plus a chair ruling — never a bump" — NAME's `naming:<key>` forks, POLIS #5's genesis draws, VAR #11's jitter all graze this; drawing through an existing forked rng avoids it, composing fresh from a world root does not (PLAUSIBLE on exact trigger conditions; the census anchors on `rngSeed` reads off world-shaped objects). `tests/architecture/layerBoundaries.test.js` — headless spine (no React/Zustand under kernel/data/generators/domain — binds the naming/territory/heightmap modules) + madge cycle allowlist ("New cycles fail the gate"). `tests/lint/engineGatedRuleKeys.walker.test.js` — proves the flag list both ways (see §4).

## 3. DORMANCY ORACLES + GOLDEN SUITES (the per-wave proof obligations)

**49 `*Dormancy(Golden|Fence)*` test files** exist at tip (count via ls-tree). The ones WEAVE fires:

- **Spatial/capture (CAP, NET-b, SEAM)**: `tests/property/spatialDigestGolden.test.js` (+`fixtures/spatial-digest-golden.json`), `tests/property/worldpulseSpatialGolden.test.js` (+fixture), `tests/domain/spatialDormancy.byteIdentity.test.js`, `tests/property/spatialConsequenceDormancyGolden.test.js`, `tests/property/seaLanesGolden.test.js` (+`sea-lanes-golden.json`), `tests/property/seasonalOverlayGolden.test.js`, `tests/property/teleportEdgesGolden.test.js`. The digest header's own law: unlit slots/absent keys ⇒ "byte-identical (every existing golden/canon)" (`spatialDigest.js:269-292`) — each new sub-digest owes a dark-half golden proof against these exact fixtures.
- **Roads/routes (NET)**: `tests/property/roadsDormancyGolden.test.js`, `seaRoadsDormancyGolden.test.js`, `roadsCharter.test.js`, `tests/domain/routeNetworkDormancy.test.js` (+`route-network-dormancy-golden.json`).
- **Naming/lifecycle (NAME)**: `tests/property/settlementLifecycleDormancyGolden.test.js`; and because `npcGenerator.js` imports `namingData` (CONFIRMED), the **settlement-generation suites fire too**: `tests/generation.test.js` ("same seed produces the same output (structurally)") and `tests/property/generatorGoldenMaster.test.js` (+`generator-golden-master.json`) — NAME's flag must gate every namingData draw site or scope explicitly to settlement foundings; state which in the charter.
- **Seasons (CAP #6)**: `tests/domain/seasonsDormancy.byteIdentity.test.js`, `tests/property/worldpulseSeasonsGolden.test.js` (+`worldpulse-seasons-golden.json`), `tests/simulation/seasonsMiniSoak.test.js`.
- **Genesis (POLIS #5, VAR #11)**: `tests/lib/instantWorld/composeInstantWorld.test.js` et al. — plans-only shifts; no lived-world golden fires, but any change to `deriveWorldPlan`'s draw order moves same-seed **new** worlds → declared shift per constitution (the composer's `rng.fork('sites')` comment shows the fork-isolation idiom already in use, `worldPlan.js:187-190`).
- **Cross-cutting determinism**: `tests/domain/determinismLeaks.test.js`, `tests/domain/deterministicSort.test.js`, `tests/lint/determinismBanCoverage.test.js`, `tests/soak-harness/determinismUnderWorkers.test.js`, `tests/property/worldpulseDeityGolden.test.js` (reads `worldpulse-golden-master.json`).
- ⚠ **Oracle-vs-bit-claim gap (flag for the architecture):** the shared comparator `tests/helpers/dormancyOracle.js` is **structural** — "it treats an ABSENT key as its default… A later phase bolting on an additive EMPTY ledger… is byte-neutral under this oracle even though a raw JSON.stringify would flag it as churn" (CONFIRMED, file header). Program law §713.2 says a dormancy claim is a BIT claim. The WEAVE charter must state, per wave, which bar it owes: `normalizeForDormancy`-equal (the estate's enforced bar) or raw-byte (the §713 bar used in this arc's folio proofs). Persisted-state candidates (route names on ledger rows, digest sub-keys) should owe the raw-byte bar.

## 4. FLAG / VERSIONING INVENTORY (quotes)

**Engine flags — `src/domain/worldPulse/simulationRules.js`:** two surfaces. (1) `DEFAULT_SIMULATION_RULES` opt-in-false keys (e.g. `seasonsEnabled: false` — "Opt-in, DEFAULT FALSE ⇒ byte-identical (no fields, no draws, no reads on the off path)", `:140-144`). (2) `ENGINE_GATED_VIRTUAL_RULE_KEYS` (`:185`, **28 keys**, counted) — "A VIRTUAL key is one the engine strictly gates on (`rules.<key> === true`) while appearing in NEITHER `DEFAULT_SIMULATION_RULES` NOR any preset override spread… a dark layer costs a campaign zero persisted bytes" and "MEMBERSHIP IS NOT A JUDGMENT CALL… `tests/lint/engineGatedRuleKeys.walker.test.js` source-scans the tree and proves the list BOTH ways" (`:160-180`; walker CONFIRMED present at tip). **Cost of minting a flag (all in ONE commit, per the in-file precedents):** strict `=== true` by-name gate read(s) — never a frozen-list `.every()` ("a computed member access attributes to NO key and would be fully wired, genuinely gated, and invisible here") + list entry + **authored certification row** (`src/domain/certification/subsystemRowsVirtual.js:36` — "TO ADD A ROW HERE: the key must be in `ENGINE_GATED_VIRTUAL_RULE_KEYS`") or declared-pending + lit credit under `mechanismLitCoverage` + a dormancy proof. Zero persisted bytes on any campaign.

**Capture-side opt-ins — `buildSpatialDigest(input)` keys** `{seasonalRoads?, seaLanes?, teleport?, biomeTexture?}` (`spatialDigest.js:293-296`), threaded from the store: `campaignSpatialCanonize.js:75` `const biomeTexture = priorRules.biomeTruthEnabled === true;` (a rules key driving a canonize opt — the exact idiom #3/#6/#10 reuse) and `:87/:94/:101` (`seasonalRoads: true`, `seaLanes: true`, `teleport: true` at a new canon). Two extension shapes exist: the four **frozen reserved slots** ("The KEY ORDER is fixed (airField, seaLanes, seasonalOverlay, teleportEdges) so a materializing wave changes a slot's VALUE, never the serialized shape", `:107-121` — only `airField` remains unclaimed) and the **V-6 additive-key idiom** ("`biomeTexture:true` appends the additive `biomes` key… OMITTED ⇒ NO biomes key ⇒ BYTE-IDENTICAL", `:288-292`) — the additive idiom is the right home for river banding / climate band / lake typology, each self-versioned like `BIOME_TEXTURE_VERSION` ("Self-describing so a materializing wave can evolve the shape under its own version, never a silent drift on an old canon", `:102-105`).

**Version axes:** `SPATIAL_GEOMETRY_VERSION = 1; COST_LAW_VERSION = 1; OVERLAY_VERSION = 1` (`spatialDigest.js:98-100`) under the law "Bumping any of these is a DISCRETE re-canonize event (§V.1): an existing frozen digest keeps its own axes forever — only an explicit spatial re-canonize re-derives. Never a silent drift on load" (`:95-97`); per-build overrides accepted as integers (`:350-354`); `BIOME_TEXTURE_VERSION = 1` (`:105`); `SEASONAL_OVERLAY_VERSION = 2` (`spatialCost.js:82`); `spatialCost.js:58` "costLawVersion change, never a silent re-derive (§V.1)". NET tier (b) (Urquhart edges into the digest) is chartered by the review against exactly this: a `COST_LAW_VERSION`/`SPATIAL_GEOMETRY_VERSION` bump, new-canons-only.

## 5. NUMBERED FINDINGS (sequencing collisions the architecture must charter around)

- **V1 — STRIP-3 names the wrong slice, and POLIS lands on it (CONFIRMED contradiction, needs charter correction before either program moves).** STRIP-3's roster reads "`mapSlice.js`, mapEdits feature, saved map snapshots + gallery map columns". But `src/store/mapSlice.js` is "Single source of truth for the **World Map**" (header, `:2`), persists "the FMG snapshot blob" (`:5-7`), contains **zero** `townMap|settlementMap|mapEdits` references (grep: 0 hits), and all its importers are world-map components (WorldMap, MapOverlay, MarkersLayer, RoadsLayer estate — 10+ files) — a surface STRIP-6 itself says STAYS. The real town-map store surfaces are `src/store/fogEditSlice.js`/`fogEditBody.js`, `src/domain/townMap/mapEdits.js`, and the DB-side contracts (`tests/security/mapSnapshotImport.contract.test.js`, `savedMapsSnapshotTrigger.pglite.test.js`, `galleryMapMemberCount.pglite.test.js`). POLIS #7's marker view state and NET #2a's road seam live in `mapSlice.js`; if STRIP-3 executes as written the world map dies and both WEAVE families lose their substrate. Recommend: WEAVE architecture records a dependency on a corrected STRIP-3 scope (excise town-map campaign columns, keep the slice).
- **V2 — order WEAVE after the STRIP waves that shrink its denominators.** NAME's consumer census of `namingData` counts `townCartography/cartographyWards.js`, which STRIP-4 deletes; running the census pre-STRIP-4 bakes a stale denominator (8→7 importers). Same class: any POLIS overlay census against `src/components/map/*` should run after STRIP-1 re-freezes the lazy-chunk/size censuses. General rule: **each WEAVE family's consumer census re-runs against the post-STRIP tree at its own dispatch**, mirroring STRIP-4's own "DW's cartographyParcels basis re-verified… at DW dispatch" idiom.
- **V3 — baseline churn is bidirectional and must not interleave.** Every STRIP wave re-freezes ratchets downward "in the SAME act" (lighting census, size-baseline deletions, typecheck floors, observed-shape, dist chunk census); WEAVE UI additions (POLIS overlays, VAR wizard pins) push size/chunk budgets **up** (`mapSlice.js`'s own FP-G9 comment shows the map chunk is byte-budgeted `@enforced-by tests/build/vendorPdfLazy.test.js`). Interleaving a WEAVE UI landing between two STRIP waves forces a baseline re-freeze that the next STRIP wave immediately invalidates. Charter: WEAVE UI-touching cars land **after STRIP-6**, engine-side WEAVE cars (CAP, NET-a/b, NAME engine half, SEAM) are STRIP-independent and may run in parallel worktrees.
- **V4 — the STRIP-6 terminal grep roster is a future census WEAVE will red.** STRIP-6 ends with "a grep roster proving the only surviving map-word hits are the retained substrate set + the realm/world map". Every WEAVE family then adds new map-word code to the realm surface. If that roster is frozen as an exact set (the estate's habitual shape — cf. entropyRootCensus's exact-set law), the first WEAVE landing after STRIP-6 reds it. Charter the roster **by directory allowlist (realm surface dirs), not by word-set**, or pre-declare WEAVE's dirs in it.
- **V5 — the new-file bill, budgeted explicitly (per §2):** unavoidable new test files ≈ **1 (NAME Markov) + 1–2 (POLIS territory + placement-assertion arm) + 1–2 (VAR heightmap DSL) + possibly 1 (SEAM S5 canonize)** — every one owes the lighting-census re-derivation, negative-assertion anchors, and (any `*Golden*`/`*contract*`/`*census*` basename) a mutation-coverage manifest rationale. Land them **batched, one census re-derivation for all** (the standing sequence). CAP and NET can plausibly land at zero new files.
- **V6 — WEAVE×DW ordering:** the goods-catalog unification steal, if chartered, must land **before DW-0 compiles its charter** (DW §3 maps "economy/chains" onto today's 5-file shape) or be deferred past DW; the faith-lineage steal is additive and safe either side; NAME #4 precedes DW-7 trivially (DW-7 is the last DW wave). Only true WRITE×WRITE file: `simulationRules.js` (append order only). No WEAVE family touches the retained townMap substrate DW builds on (that seam is STRIP's and DW's alone).
- **V7 — intra-WEAVE shared file:** `worldPlan.js` is written by both POLIS #5 (genesis pass in the composer) and VAR #11a (pyramid jitter) — same function's RNG stream; the two candidates must share one car or land in a declared order, since both perturb `deriveWorldPlan`'s draw sequence (new-plans-only either way, but the same-seed-plan shift must be declared once, not twice).

**Coverage honesty:** nothing executed; test-file relevance judged from names, headers, and grep-verified imports, not runs. `composeInstantWorld.js` body unread (POLIS #5's "diplomatically cold" gap stays PLAUSIBLE per the review's own flag). The STRIP charter's "56 files" for `src/components/townMap/` measured 35 top-level entries in the extraction (subdir/recursive counting likely; not load-bearing here). Whether `tests/property` counts as a "generation-facing root" for negativeAssertionAnchor is PLAUSIBLE (the memory proves tests/lint is exempt and generation roots are in scope; the root list itself was not re-read).

---

# PANEL LENS 1 — DETERMINISM/DORMANCY (skeptic panel wf_0f71adeb-14c, verbatim)

FINDINGS — LENS 1 (determinism and dormancy), skeptic panel over `DESIGN_FMG_WEAVE.md`

1. **BLOCKER — D4 / NAME-1 (volume §2 D4, §3 NAME-1): the "display-side, flagless, zero canon bytes" claim is false under the volume's own cited mechanism.** The volume charters war/route names as "pure derivers… Rendered via new interpolation tokens in the prose registries (`{road}`, the `{payer}` precedent)" and "zero canon bytes… canon risk zero." But the `{payer}` precedent is a MINT-time interpolation: `src/domain/worldPulse/roadsKernel.js:843` sets `interp.payer` and the result of `pickLine(pool.headline, seed, interp)` is pushed into `newsEntries` (roadsKernel.js:~846-851, `newsEntries.push(roadsBeat({... headline: pickLine(...)...}))`) — persisted news bytes minted at tick on lived worlds. The other cited precedent is the same class: `calamityKernel.js:450` mints `name: pickLine(CALAMITY_TITLES, ...)` into the persisted calamityHistory stamp/news entry. `warCoalitionNewsEntries` likewise feeds `newsEntries.push(...)` from the tick path (`applyWorldPulse.js:1095-1105`, `pulseKernel.js:1316`). There is no render-time interpolation layer for news; the volume's own §1.2 lists "news fields" as persisted-state owing the raw-byte bar, so D4 contradicts §1.2 inside one document. As chartered, NAME-1 would silently write route/war-name strings into persisted entries at future ticks on lived worlds while claiming byte-neutrality. **Amendment:** either (a) re-home the derivers as genuinely display-side (the `src/domain/display/` leaf family — rumorPhrasePools/chroniclersLetter — is the existing display-render precedent; prose-registry mint paths untouched), or (b) keep the registry-token mechanism and re-charter NAME-1 as a declared future-tick persisted-prose shift owing the raw-byte bar — and reconcile with Q-W4, which owner-gates exactly this class.

2. **MAJOR — D6 rests on a receipt the tree refutes: site x/y ARE in the structural fingerprint.** D6 says "the scatter is declared cosmetic (worldPlan.js:126-130)"; that comment claims coordinates "are excluded from the coherence contract and the structural fingerprint" — but `instantWorldFingerprint` includes `sites: {slot, tier, x: st.x, y: st.y}` (`src/lib/instantWorld/composeInstantWorld.js:374` and the returned `stableStringify({... sites ...})`). The in-tree comment is stale against the implementation; V-genesis propagated it (its ordering-hazard #1 offers "ratify the scatter… it drags coordinates into the fingerprint" — they are already in it), V-var read the implementation correctly, and the volume adopted the false half. D6's conclusion (no spatial prior — geography is iframe-bound) survives on the materialize receipt alone, but the volume's preamble binds it to amend where the tree disagrees. Concrete downstream risk: VAR-1's "sites clamped to land" reads as free cosmetic motion; any compose-time clamp of plan x/y is a fingerprint-visible, worldCode-replay-visible plan shift not declared in VAR-1's row (VAR-2 is where the scatter change is chartered as versioned/dormant). **Amendment:** correct D6's receipt to the materialize fact only; state explicitly that VAR-1's clamp is plate-display-side (plan x/y untouched), and fix/flag the stale worldPlan.js:126-130 comment in the first plan-touching car.

3. **MAJOR — POLIS-1's plan-shift reach includes the worldCode v1 byte-replay promise, and Q-W3 as written does not cure it.** `src/lib/worldCode.js:5-8` promises "`/world/<code>` regenerates the byte-identical world client-side… the gallery becomes a commons anyone can reproduce," and decode "returns the RAW preset; the caller passes it to `composeInstantWorld`" (:24-27). Any same-seed plan/composition change — POLIS-1's diplomacy half as much as its jitter half, VAR-2 lit, NAME-2 lit — makes every v1 code minted before the change regenerate a *different* world than the one shared. Q-W3 covers only the payload shape ("jitter constants + the magic-not-in-code defect"); a v2 payload does nothing for existing v1 codes, which still decode (only unknown versions fail closed, :27-28) and replay through the changed composer. The volume never reconciles §1.9's declared-shift law with this written product promise. **Amendment:** add an explicit ruling to Q-W3 (or a new §1 law): what a pre-shift code replays after a declared generation shift — amend the worldCode promise (owner-visible: it is a written product/commons promise), or version the plan law so v1 codes resolve the pre-shift law. Also mark POLIS-1 as partially owner-gated: §5's "nothing here blocks the ungated cars" contradicts POLIS-1 embedding Q-W3 — the jitter/sentinel half cannot land ahead of the grant.

4. **MAJOR — §1.2's own requirement ("the charter row of every car names its bar") is unmet by most of §3, and one named oracle is the wrong bar.** Only W-CAP's family header names raw-byte. Cars with persisted surfaces name no bar: SEAM-3 (capture sidecar), NAME-2 (record-shape/draw stream — its oracle, generatorGoldenMaster sha256, happens to be byte-grade, but the row doesn't say so), NET-2 (ledger version field at connect), POLIS-1/2 (plan shape/links), ST-1 (faith field), ST-4 (persisted `type` stamps). CAP-3 names "seasons dormancy suite is the oracle" — but the estate's shared comparator is structural, not byte: `tests/helpers/dormancyOracle.js` "treats an ABSENT key as its default… an additive EMPTY ledger… is byte-neutral under this oracle even though a raw JSON.stringify would flag it" (V-collide §3, quoted from the file header). Citing existing suites does not discharge the §713.2 raw-byte bar the family header claims. **Amendment:** a bar line per car in §3; persisted-surface cars name raw-byte explicitly and add a raw-byte arm where the cited suite is normalizeForDormancy-based.

5. **MAJOR — ST-1 and ST-4 are the Q-W4 class (future-tick persisted bytes on lived worlds) but ride ungated, undeclared.** Q-W4 owner-gates a structured `region` field on future-tick news entries as "future-tick persisted bytes, declared." ST-1 adds an `origin` field to faith entries minted at ticks on lived worlds (spread/imposed/assigned are tick events — V-faithGoods §1 write-site census); ST-4 widens the persisted calamity flavor `type` (persisted "under the SAME stamp key `type`", `src/domain/spatial/calamity.js:85-87`) and introduces a draw where today there is a deterministic map lookup (:88-96 — zero draws). Additionally, any widening of a `pickLine` pool changes `fnv1a32(seed) % pool.length` for every future key — future-tick prose selection shifts on lived worlds, nowhere declared in ST-4's row. Persistence shape is an owner-gated class by standing law. **Amendment:** either owner-row ST-1/ST-4 like Q-W4, or state in the volume why the fieldManifest additive-field discipline exempts ST-1 (and declare ST-4's future-tick selection shift explicitly); name the raw-byte bar for both (per finding 4).

6. **MINOR — D7's determinism ruling is sound, with two wording hazards to amend.** The core holds: IEEE-754 `+ − × ÷` and `Math.sqrt` are spec-mandated correctly rounded in JS, so a fixed-op polynomial exp2/log2 kernel is cross-machine bit-stable by construction, and Uint8 truncation is self-consistent under it (sufficient for OUR determinism). But (a) upstream parity is impossible by construction — detPow ≠ `Math.pow` in low bits, and near-integer-boundary values will truncate differently — so "verbatim-template goldens" (VAR-1) must be stated as verbatim *templates* with self-recorded *our-output* goldens, never upstream-parity goldens, or a lane will chase an unattainable green; (b) "Uint8 truncation/clamp semantics" conflates two different conversions — plain `Uint8Array` assignment is ToUint8 mod-256 wraparound, `Uint8ClampedArray`/explicit lim() clamps — the car should name which applies at which upstream site (heightmap-generator.ts:97, :104 per V-var) so a lane doesn't "fix" wraparound as a bug. Also detPow needs a defined 0-base branch (`0 ** p`, log2(0)); state it.

7. **MINOR — NAME-2's lit-path RNG root/fork spelling is unstated in a program whose law #1 is keyed forks.** D3/NAME-2 never names the fork family (`naming:<cultureKey>:<entityId>` class) or its root; V-naming's warning stands that a fork keyed on an *unresolved* culture token gives two consumers different streams for one settlement (D3's centralized resolver cures this only if the key uses the *resolved* token — say so). Fresh `createPRNG(...)` roots in the module are new entropy compositions owing the §1.1 disposition row — pre-declare it in NAME-2's row. And the successorNpc cure has a draw-displacement trap inside the lit path: `successorNpc.js:29-45` draws `randInt` → name-pick → dominant → flaw from the one passed rng; a variable-draw-count Markov name drawn inline shifts every subsequent personality pick — the name must draw from its own keyed sub-fork.

8. **MINOR — SEAM-2's nearest-centroid re-resolution lacks a determinism spec on the constitutional capture seam.** The volume charters "nearest-centroid over `cells.p`" with no distance-op or tie law. The estate's own precedent is explicit that this class bites cross-device (autoplacement.js:33-39, :171-178 — no RNG, `Math.sqrt` not `Math.hypot`, hash tie-breaks); `Math.hypot` is not correctly rounded and a tie broken by iteration accident yields different frozen digests on different machines — invisible to the double-capture compare, which runs on one machine (`spatialPackCapture.js:113-120`). **Amendment:** SEAM-2 specifies squared-distance comparison (no sqrt/hypot) and first-minimum (lowest-index) tie order.

9. **MINOR — W-ARMS carries no determinism/dormancy row at all.** "Generative heraldry" states no draw source (keyed fork vs stable-key hash) and no persistence claim, in violation of §1.1/§1.2's per-car requirements. Acceptable only because it is last and vetoable; the volume should say its charter owes both rows at dispatch.

Verified-sound claims worth recording (attacked, not refuted): §1.1's fork-safety claim (`prng.js:84` keyed derivation — fork labels `diplomacy`/`pyramid`/`tone` cannot displace existing draws); NAME-2's *dark*-path byte-exactness mechanism and its oracles (525-hash generatorGoldenMaster is byte-grade); CAP sub-digest absent-key ⇒ byte-identical on old canons under §V.1 (no writer on load; re-derive only at explicit re-canonize); NET-2's dark claim (canon lane confirmed unmounted, `spatialUsage.js:349`); POLIS-1's plan shift cannot touch lived worlds' own bytes (composer runs only for new realms; fingerprint is compose-time-only — no recompute path on lived campaigns found, so the D6 fingerprint extension is a golden re-record, not a lived-world hazard); NET-1's corridor-reuse determinism (edge order deterministic end to end, `costFn` two-arg amendment already chartered).

VERDICT: **NOT-REFUTED** — the volume is sound as architecture, but finding 1 must amend before NAME-1 dispatches, and findings 2-5 must amend before their cars' charters compile (Amendment A1 material: NAME-1's rendering surface, the D6 receipt correction, the worldCode v1-replay ruling folded into Q-W3, per-car dormancy bars, and the ST-1/ST-4 gating consistency).

---

# PANEL LENS 2 — THE PROMISE (skeptic panel wf_0f71adeb-14c, verbatim)

Findings from the PROMISE/lived-history lens, each grounded in the volume, the verification reports, or code receipts quoted therein.

**1. BLOCKER — §2 D6 / §3 NAME-1 (volume line 26, 53): the war-name key is unstable over a war's lifetime; the name would mutate mid-war, displaying wars that never happened.**
Defect: "war key = sorted participants + warReason type + tick" names no tick semantics. A war is an interval, not a point: (a) if "tick" is the rendering/receipt tick, the same war hashes to a different name every tick — a chronicle reader sees several named wars where one existed (display-fabricated multiplicity); (b) "sorted participants" drifts when coalitions join (`warCoalitionNews.js` renders `{settlement, counterpart, thirdParty}` — V-naming.md:64), renaming the war mid-war. The cited precedent does not cover this: `calamityKernel.js:450` keys on `settlementName::year` because a calamity is a point event whose year is its own (V-naming.md:23). No receipted stable source for an opening tick exists post-war: the deployments/armyTransit records are dropped when no army is afield (V-polis.md:22), and no verification lane confirmed that war receipts carry the opening tick or original belligerent pair.
Amendment: specify the key as (original belligerent pair + warReason type + opening tick), name the durable source of those three at every render site (opening receipt fields, or a warReason-carried stamp), and prove it recoverable after the war ends; if no such source exists display-side, either drop tick/coalition members from the key (accepting same-pair same-reason collisions — itself history-implying, so say so) or route the name through a persisted opening stamp (which moves it into Q-W2's owner-gated branch). Also pin pool content to the typed warReason (the roadsProse portable-specificity law, V-naming.md:63) so a name never asserts events beyond the typed cause.

**2. BLOCKER — §3 ST-1 (line 72) contradicts §1.9 (line 19) and the volume's own Q-W4 standard (line 91): an un-flagged persisted field family on lived worlds, plus a backfill path that fabricates origins.**
Defect: ST-1 names no flag and no owner row, yet `origin` is a new generation-written persisted field family (fieldManifest's own contract applies — V-faithGoods.md:22) landing on entries minted every tick in lived worlds (four mint sites, V-faithGoods.md:8/13-17). Un-flagged, every lived faith world's future serialization moves — the exact class the volume owner-gates one page later for Q-W4 ("future-tick persisted bytes, declared", line 91) and forbids at line 19 ("Lived worlds never move"). Worse for this lens: one mint site is the *legacy patron seed* (`religionState.js:175`, V-faithGoods.md:8) — a lazy materialization for faiths that predate religionState. Stamping `origin:'founding'` there writes a founding claim onto a faith whose actual origin the canon never recorded — fabricated history in bytes, on a lived world.
Amendment: give ST-1 a virtual flag (ENGINE_GATED idiom, §1.3) with the raw-byte dormancy bar (§1.2 — persisted state), or an owner row beside Q-W1–Q-W4; and add the law "absent origin = pre-lineage entry, never backfilled" — the legacy/lazy seed paths mint NO origin field, only genuinely-new entries carry one (closed vocabulary unchanged).

**3. MAJOR — §2 D6 vs §3 POLIS-5 (lines 28, 65): the ongoing-war sub-car is in tension with D6's own "NO tick-stamped history" clause, and the indistinguishable-from-DM-authored defense does not extend to it.**
Defect: D6 rules that a fake transition-at-tick-N is fabricated lived history, then spawns a sub-car that writes a `deployments` row + war_front — a record class only the pulse mints today, whose shape carries lived scars (capacity model "seeded from the origin's capacity model at deploy", V-genesis.md:44 — PLAUSIBLE, unexecuted) and whose existence implies a mobilization ramp that never ran (the keystone gate: "a settlement cannot launch a serious siege from peace", V-genesis.md:43). No DM-authoring path mints t=0 deployments, so POLIS-2's "state-shape-identical to a hand-built realm" defense (V-genesis.md:14) cannot be claimed for POLIS-5. The volume gates it only on tone + observation.
Amendment: POLIS-5's charter must (a) enumerate the coherence set it writes (deployments, confirmed war_front, warPosture, warExhaustion — which stay empty, which are stamped), (b) state a founding-stamp law: every tick-valued field = founding tick, no back-dated ramps, no fabricated `turningPoints`/history/incidents (V-genesis.md:46 scar list), and (c) explicitly reconcile with D6's no-tick-history sentence (founding-time siege = starting state; anything implying pre-founding ticks = defect).

**4. MAJOR — §2 D10 / §3 POLIS-4 (lines 32, 64): the territory view mints a second territory truth that can contradict what the canon narrates, and the volume never states their relationship.**
Defect: the frozen digest already holds a territory partition — "territory adjacency + gates (spatialDigest.js:408–439)" (V-bridge.md:69) — which feeds gates, tiers, and the Herald's narration. D10's flood is a different law (tier-as-expansionism, budget caps, FMG normalize) over a different substrate: the *live* pack from the saved fmgSnapshot, which can diverge from the frozen digest after terrain edits (S1, V-bridge.md:42-48). Shading drawn from one truth beside narration derived from the other asserts territorial facts the canon does not hold — the S4 class exactly, and the §710.6 lesson (mark-deriver on one geometry, canon on another). The volume charters a comparison receipt for roads (SEAM-4) but nothing for territory; the placement-assertion arm (§1.6) asserts marks-on-placements, not partition-vs-digest agreement.
Amendment: D10 adds either (a) an S4-analog comparison receipt (view partition vs digest territory assignment at the seed cells, divergence documented) plus consumption of SEAM-1's divergence flag (suppress or caveat shading when geography may have diverged), or (b) an explicit product declaration that the shading is impressionistic display, not canon — stated in-legend, decided in the charter, not left implicit.

**5. MAJOR — §3 SEAM-2 (line 41): re-resolution's charter is missing the assertions that keep it from minting confidently-wrong canon — though it rewrites nothing a user chose.**
The lens question itself is answered clean: SEAM-2 reads stored x/y (the user's preserved choice), writes only capture-side receipts, and never patches store placements — nearest-centroid over `cells.p` is findCell's own law (nearest Voronoi seed), so a healthy placement re-resolves to its own cell. But three gaps: (a) for the SEAM-0 population it exists to cure, the x/y provenance is the plan scatter, "declared cosmetic… excluded from the coherence contract and the structural fingerprint" (worldPlan.js:126-130 via V-genesis.md:62) — re-resolving promotes a documented non-contract to canon seed cells unless materialize/drag has rewritten x/y to real map-space by canonize time, which no lane verified; (b) a CONFIRMED stale-trigger class is loadSnapshot of *different-vintage geometry* (V-bridge.md:55) — there the x/y themselves belong to another map, re-resolution mints a valid-looking wrong cell, and a `cell_remapped` receipt would falsely claim cure; (c) no tie rule stated for equidistant centroids (determinism).
Amendment: charter adds a coordinate-frame assertion for composer-minted placements (verify or establish that stored x/y are captured-pack map-space before trusting them), couples receipt honesty to provenance (until SEAM-3/Q-W1 stamps geometry identity, `cell_remapped` reports "re-derived", never "cured"), pins first-minimum lowest-index tie order, and states explicitly that `mapState.placements` is never written.

**6. MINOR — §2 D6 (line 28): direction encoding for asymmetric relation types is unstated.** Reciprocal links "(ids, not names)" is symmetric, but vassal/patron/tributary are directional and edges are authored senior-side-`from` (graph.js:627-629, V-genesis.md:67); suzerain-closure depends on this. Amendment: one sentence — genesis links encode role/direction exactly as the DM-authored link idiom does, closure inherits edge direction, no `overlordSaveId` stamp (pulse-flip-only field).

**7. MINOR — §2 D9 (line 31): field-battle overlay coverage starts at Q-W4's landing; absence must not read as history.** Pre-landing battles persist only as prose (V-polis.md:22), so the overlay will show wars with zero battle marks. Amendment: legend/charter notes coverage begins at the field's landing tick — the absence-decay discipline applied to a display surface.

**8. MINOR — §3 NAME-2 (line 54): the regen lifecycle path is unstated.** Lit-flag behavior is scoped "new worlds/foundings only", but regenerating an existing lived settlement is neither, and the lit path displaces the ambient draw stream (whole-record shift, V-naming.md:52). Amendment: one charter line stating regen-under-lit-flag behavior (declared shift, or legacy naming pinned on regen), per the trace-every-lifecycle-path law.

**9. MINOR — §2 D4 (line 26): route key collapses mode.** Edge identity is `route.<a>.<b>.<mode>` (V-net.md:63); the `a|b` key gives a land road and a sea lane between the same pair one name. Acceptable if declared; otherwise fold mode into the key. Also tie route-name grammar to the closed FLAVORS/MODES vocabularies (portable specificity).

**Attacks attempted that failed (recorded so the verdict is honest):** D6's base car is genuinely free of fabricated history (typed founding-time edge, lazy state materialization, no tick stamps — V-genesis.md:46 confirms the target shape and the volume adopted it, including ids-not-names, which closes the name-collision near-miss); suzerain-closure writes nothing a lived world could later contradict (plan-side, DM-idiom edges the pulse may freely overwrite); §1.9's declared-shift law for new worlds is consistent with THE PROMISE as ratified in this program (a minted world is immutable; the generator evolves for new mints, declared); D9's ruin markers correctly read durable settlement state rather than the capped believed-razings feed; Q-W1/Q-W3/Q-W4 are properly owner-rowed.

VERDICT: NOT-REFUTED — sound with the listed amendments; findings 1 and 2 must fold into Amendment A1 before NAME-1 or ST-1 dispatches, and finding 5 before SEAM-2 (immediate-dispatch wave).

---

# PANEL LENS 3 — GATES/CENSUSES (skeptic panel wf_0f71adeb-14c, verbatim)

# LENS 3 — GATE AND CENSUS ECONOMICS: SKEPTIC FINDINGS

Verification performed first-hand against `fmg-study/ours` (73f5dfc02 extract), `git show 73f5dfc02:` for tests/migrations, the STRIP charter text in `scratchpad/DESIGN_MAP_MODULE_SPLIT.md`, and the census-law memory. All ten SEAM/CAP/NAME/NET/POLIS/VAR proof-home files named by the receipts layer were existence-checked at 73f5dfc02: all present.

**1. BLOCKER — §4.1(a): the STRIP-3 amendment re-labels three WORLD-map security contracts as "the town-map snapshot DB contracts," and executing the amended roster deletes live security guards on a surface that stays.**
The mapSlice half of the amendment is CONFIRMED correct by my own reads (`ours/src/store/mapSlice.js:2` "Single source of truth for the World Map"; zero town-map references — the only case-insensitive hits are the `burgSettlementMap` selector at :9/:802; 11 world-map importers). But the DB-contracts clause is refuted: `tests/security/mapSnapshotImport.contract.test.js:1-16` guards the **raw FMG snapshot** cross-user stored-XSS sink in `src/store/galleryImportMap.js` (world-map gallery import); `tests/security/savedMapsSnapshotTrigger.pglite.test.js:1-15` guards `saved_maps.gallery_world_snapshot` writes (the 089-bypass trigger); `tests/security/galleryMapMemberCount.pglite.test.js:1-16` guards un-shared settlement-membership leakage on gallery **map** tiles. Negative control: the only "settlement_map"-shaped DB column anywhere in `supabase/migrations/` is `burg_settlement_map` (world-map placements, migrations 002/069/084/102) — **no town-map DB snapshot column exists**. The defect originates in V-collisions V1 (V-collisions.md:70 makes the same mislabel), and the volume's own §6 tiebreak ("the appendix's quoted code wins") would entrench it — but the appendix never quoted these files; I did. Deleting a stored-XSS trigger guard is a security-posture change, owner-gated by nature. **Amendment:** strike "and the town-map snapshot DB contracts" from §4.1(a); state the three contracts and their tables are world-map surfaces that STAY; STRIP-3's true surface is `fogEditSlice.js`/`fogEditBody.js` + `domain/townMap/mapEdits.js` (both verified town-map: fogEditSlice header, mapEdits.js:1-8), with a fresh column census at STRIP-3 dispatch.

**2. MAJOR — §1.5's "land new files batched, one lighting-census re-derivation for all" is unimplementable under the volume's own sequencing.**
The census law is same-commit and exact: `sovereigntyLightingContract.walker.test.js:2754` freezes five figures (`2,328/358/1,970/18,641/5,324`-class), and the memory rule (quoted in V-collisions.md:35) is "re-derive the lighting census WHOLE — all five figures — IN THE SAME COMMIT" as any new test file. One re-derivation for all therefore requires **all 4–6 new test files in ONE landing act**. But the volume disperses their owning cars: W-SEAM "dispatchable immediately after panel" (§3), NAME-2 after STRIP-4, POLIS-4 and VAR UI after STRIP-6 (§4.2), each under per-car discipline with its own landing (§4.4) — and every STRIP wave in between re-freezes the same census downward "in the SAME act" (STRIP charter, DESIGN_MAP_MODULE_SPLIT.md:130-133). The memory's "land all lanes, then re-derive ONCE" pattern was for one collection window of concurrent lanes, not a multi-wave program with interleaved STRIP re-freezes. **Amendment:** either charter a single post-STRIP-6 test-estate-extension act carrying every new test file + census/anchor/manifest updates (stating which car proofs ride existing files until then), or drop the "one for all" claim and accept one same-commit re-derivation per new-file landing (realistically 2–3).

**3. MAJOR — the "~4–6 files" bill is presented as "the program's WHOLE new-file bill" but was derived over six families only.**
V-collisions's own denominator (V-collisions.md:7) defines families as SEAM/CAP/NAME/NET/POLIS/VAR; its V5 arithmetic is 1+1–2+1–2+0–1 = 3–6. **W-ARMS is outside it entirely** — an L-sized generative subsystem (kinship-quartered heraldry over the 8-emblem substrate, §3 W-ARMS) with no existing generative-heraldry test estate; its bill is unpriced. ST-2 (≥9-module goods reshape) and ST-3 (new instance-scoped pipeline runner) plausibly land at zero new test files via the six join gates and composeInstantWorld suites, but nobody verified that. SEAM-4's comparison receipt has no existing home **by construction** — "No test imports both. CONFIRMED" (V-bridge.md:71) is the very gap it closes — so it is either a new file (unbudgeted) or a declared cross-import extension of an existing file. **Amendment:** re-scope §1.5 to "~4–6 for the six verified families"; W-ARMS/ST-2/ST-3 charters owe their own bills at compile; name SEAM-4's home.

**4. MAJOR — §1.5's bill and §1.6's out-of-gate law contradict as written for the POLIS placement-assertion arm.**
The bill budgets the arm as a new **test file**; §1.6 keeps the assertion "OUT of the gate until green at its own tip." A file under `tests/` IS the gate (vitest collects it), the ratchet baseline is at its literal ceiling monotone-down ("no new red can be banked, ever," V-collisions.md:35), and no sanctioned skip status exists. So the arm can enter `tests/` only in the same act that makes it green; before that it must live as a non-test instrument (e.g. `scripts/`) or not exist. **Amendment:** state the mechanism — the arm lands in `tests/` only green-at-tip with POLIS-4 (in which case it is a conditional line of the bill), and any red-at-tip period runs it as a scripts-side instrument outside the gate.

**5. MINOR — §1.1 claims entropy-census protection the census structurally cannot provide for the named forks.**
The walker anchors on `.rngSeed` reads off the closed WORLD_ROOTS set (`entropyRootCensus.walker.test.js:56-63`: `worldState, startingWorldState, state, ws, raw, base`) and on compositions built from those reads. D5's `'diplomacy'`/`'pyramid'`/`'tone'` forks derive from `plan.seed` at compose time, and D4's derivers are fnv1a32 over participants/route pairs — none reads `worldState.rngSeed`, so the census passes **vacuously**; it neither polices nor bills them. The `prngForkLabelDelimiter` walker also stays green (no `::` in the new labels). The real guards for plan-side entropy are the D6 fingerprint extension + composeInstantWorld suites. **Amendment:** re-state §1.1's guard sentence — the census binds only worldState-rooted compositions (e.g. any NAME-2 engine-side key traced from a world root); plan/display-side entropy is guarded by fingerprint + fork-label walker, not this census.

**6. MINOR — mechanismLitCoverage fires on an unbudgeted car if D4's deriver homes in worldPulse.**
AXIS 1 enumerates every flat `*.js` under `src/domain/worldPulse` and awards AUTO lit credit only for a **direct** import from a non-dormancy test file (`mechanismLitCoverage.test.js:11-27`). NAME-1's stated proof plan ("Herald/news suites extend") is component-level/transitive and awards nothing. The existing `*News.js` leaves the deriver would "live beside" (V-naming.md:64) are worldPulse files. **Amendment:** pin the deriver's home display-side (e.g. `src/domain/display/`, beside `warStatus.js`/`hegemonyRead.js`) or budget a direct-import lit extension in an existing `tests/domain` file in NAME-1's act. Related unpriced item: VAR-2's "dormant-gated plan law" fits neither §1.3 flag idiom (engine virtual keys are pulse-read; capture opt-ins are canonize-side) — if minted as an `<x>Enabled` rules key it enters AXIS 2 + the engineGatedRuleKeys walker with its gate read outside worldPulse; name the idiom.

**7. MINOR — §1.5 compresses the mutation-manifest trigger to "`*Golden*`-basenamed files" and omits negativeAssertionAnchor.**
The actual trigger (memory TE-CH-1, quoted V-collisions.md:35) is the seven enforcer dirs OR any basename containing `census|scan|baseline|ratchet|walker|killlist|parity|coverage|governance|freshness|integrity|exhaustiveness|roundtrip|golden|contract|pin` — a walker/census-named POLIS or VAR file owes a manifest row the volume's phrasing misses (file confirmed at tip as `tests/lint/mutationCoverageManifest.test.js`). And every new test file under a generation-facing root starts at negativeAssertionAnchor ceiling 0 — absent from §1.5 entirely.

**Attacks that failed (tried, not refuted):** CAP/NET at zero new files — holds; every named proof home exists at 73f5dfc02 (biomeTexture/spatialBiomeTruth additive-sub-digest precedent files, seaLanes, sfBridge harness, the 13 routeNetwork suites, spatialDigestGolden), and CAP-3/NAME-2's flag bills are genuinely covered by §1.3's ONE-commit law. The one-gate/one-landing law is respected as written: parallel worktrees build, `gate-mutex.sh` (LANE-LAW.md:41) serializes gate runs, chair CAS serializes landings — no contradiction found beyond the batching interplay already in finding 2. The STRIP-6 directory-allowlist amendment (§4.1(b)) is implementable against the charter's actual wording (DESIGN_MAP_MODULE_SPLIT.md:133, which does not commit to an exact word-set).

**VERDICT: NOT-REFUTED** — the volume is sound for implementation under the listed amendments, EXCEPT that finding 1 (the STRIP-3 DB-contracts clause) is a true blocker that must amend before STRIP-3 or any car relying on the amended STRIP charter dispatches: as written it schedules the deletion of a stored-XSS guard and two gallery leak/bypass guards on the world-map surface the program itself declares must survive.

---

# PANEL LENS 4 — COHESION (skeptic panel wf_0f71adeb-14c, verbatim)

# LENS 4 — ARCHITECTURE COHESION: FINDINGS

**1. BLOCKER — §2 D4 (war names): the stable key has no addressable source of truth, so the "stable-key hash" produces UNSTABLE names.** D4 keys war names on "sorted participants + warReason type + tick," but no war entity exists anywhere in the substrate: wars are per-army `worldState.deployments[saveId]` rows plus `war_front` channels (V-genesis.md §2, warDeployment.js:4 "A settlement fields EXACTLY ONE army (`worldState.deployments[saveId]`)"), and grep over `warDeployment.js`/`warReasons.js`/`warCoalitionNews.js` for `warId|startTick|openedAt|startedAt|warStart` returns **zero hits** (executed, this lane). Each news receipt carries only `{settlement, counterpart, thirdParty}` (V-naming.md §5, warCoalitionNews.js:27-40). So every render site derives its own key from local receipt data: the participant set changes as coalitions join and the tick differs per receipt — the same war gets a different name at every mention. The calamity precedent D4 leans on (`pickLine(CALAMITY_TITLES, \`${settlementName}::${year}\`)`, calamityKernel.js:450) works because a calamity is a one-shot event whose key inputs are trivially local; a war is an extended process, and the precedent does not transfer without a war-identity anchor. No gate reds this — it ships silently incoherent, directly against the news-address law's stable-names expectation. **Amendment:** D4 must name the key's source of truth per input — e.g. key = (sorted **opening** pair from the war-opening receipt, warReason type, opening tick), recoverable from the first war_front/deployment receipt and threaded to later receipts — and the NAME-1 charter must state where each input is read at render time.

**2. BLOCKER — §2 D11: the prescribed shape ("one index module re-exporting the physical chunks") is forbidden by the gate D11 itself cites.** `resourceChains.js:1-17` (read this lane, verbatim): "these two tables are LAZY-ONLY … resourceData.js (whose RESOURCE_DATA / SPECIAL_RESOURCES tables ARE eagerly reached by customRegistry / structuralValidator) … **DO NOT re-export these from resourceData.js — an eager re-export would re-drag them into first paint.**" A single index module statically re-exporting both physical halves recreates exactly that eager edge the moment any eager consumer (customRegistry, structuralValidator — eager per the header) imports it, and the closure ratchet has ~2KB headroom (V-variability.md §4: 1,380,062 bytes measured). If eager consumers may NOT import the index, the "logical namespace" silently excludes the eager half's 17-importer surface — unstated either way. **Amendment:** replace "one index module" with: a types-only vocabulary module (typedefs, zero value exports) + two physical index surfaces (eager identity/terrain half, lazy chain/demand half), plus an explicit migration roster naming which of the 17/3/2/3/13 importers (V-faithGoods.md §2) move to which surface.

**3. MAJOR — §2 D3: the centralized resolver's resolution law is the undecided heart of the car.** The volume says the module cures the three-way fork "behind one exported resolver" but never states what `mediterranean`/`mixed`/unknown resolve TO when lit — the three legacy answers (latin profile / germanic names / union-of-11 wards; `mixed` → fresh-random-per-name at N1 vs germanic at steadings, V-naming.md §2) are mutually exclusive, and the generator-golden corpus pins the fork as-is including `mediterranean` rows (V-naming.md §2). Since NAME-2's dark path must keep every legacy draw byte-exact, the resolver must internally carry the legacy modes forever and the "cure" is lit-only — meaning the lit resolution table is a *new* behavior nobody has chosen, and two Opus lanes will choose differently (including whether `mixed` remains per-name-random, which changes draw counts). Scope is also ambiguous: "every consumer (S1 genesis, …)" conflates npcGenerator's `resolveNameCulture` with `resolveConfig`'s culture-profile resolution (`cultureProfiles.js` mediterranean→latin), which is a different layer the naming module should not absorb. **Amendment:** D3 declares (a) the canonical lit resolution table for the three token classes, (b) resolver scope = name-pool resolution only, cultureProfiles alias untouched, (c) dark path = legacy call sites textually untouched, resolver consulted only inside the lit branch.

**4. MAJOR — §2 D12 × D6: ST-3 and POLIS-2 both rewrite `composeInstantWorld.js` with no ordering decision, and D6's contract is line-addressed.** §4.2 places ST-1/3/4 in the parallel-worktree engine set and constrains only POLIS-3/4 (post-STRIP-6); POLIS-1/2 are sequenced nowhere. POLIS-2 inserts the materialization "between `dedupeWorldFactionNames(settlements)` (:251) and `deriveGraphWithDiscoveredCandidates` (:266)" (D6/V-genesis.md §3b; window confirmed by read this lane at :251-266) while ST-3 restructures that same body into an instance-scoped step list — a semantic collision under the volume's own V1-class collision analysis, which caught worldPlan.js (V7 → D5) but missed this file. Line-number contracts die the moment ST-3 lands. **Amendment:** §4.2 adds an explicit order (recommend POLIS-2 before ST-3, or ST-3 first with the materialization landing as a declared step), and D6's insertion contract is restated step-relative: after the dedupe step, before the graph-discovery step.

**5. MAJOR — SEAM-3 × CAP-1: the persisted sidecar's schema is left to fork under two lanes in the most owner-sensitive class.** SEAM-3 persists "the capture" (Q-W1); CAP-1 then widens the capture with `fl`/`temp`/`prec`/`g` (D1). The volume states no order between them, no sidecar version constant, and no ruling on whether CAP-1's fields enter the persisted sidecar — yet the sidecar is the substrate for S5's headless re-canonize, so a pre-CAP-1 sidecar replayed post-CAP-1 silently degrades every CAP-derived sub-digest (CAP-2's absent-`fl` fallback masks this as green). One lane will version the sidecar, another won't; that is a persistence-shape divergence the owner is being asked to grant blind. **Amendment:** the Q-W1 row carries the schema decision — a self-versioned sidecar (`CAPTURE_SIDECAR_VERSION`, the sub-digest precedent), CAP-1 fields declared as tolerated-absent keys, and an explicit SEAM-3↔CAP-1 order in §4.2; a headless re-canonize from an under-versioned sidecar emits a receipt row, never a silent degrade.

**6. MAJOR — §2 D5: the one-plan-car law converts the ungated diplomacy candidate into an owner-blocked one, contradicting the §5 posture.** POLIS-1 folds Q-W3 (worldCode v2, owner-gated) into the same car as genesis diplomacy, and POLIS-2→5 chain behind POLIS-1 — so the whole W-POLIS family waits on a worldCode migration grant that only the jitter half needs (jitter without v2 is possible by freezing constants for v1 codes, V-variability.md §1.1). §5's "nothing here blocks the ungated cars" is false for every POLIS car as chartered. **Amendment:** pre-declare the Q-W3 fallback in D5 (deferral ⇒ jitter constants frozen-for-life under v1, v2 folded into a later car) so POLIS-1 dispatches on either answer; or split the split-condition explicitly.

**7. MAJOR — §2 D10: the territory flood's seed source is undecided, on a substrate where instant worlds carry `cellId: null` on every placement.** Confirmed by read this lane: `composeInstantWorld.js` placements block mints `cellId: null, // …the live FMG cell resolves at canonize` (:255-263 region). SEAM-2 re-resolves at **capture** time for the digest; it does not repair the store's placements, and D10 is a **view-time** derivation over the live pack — so a flood seeded from stored cellIds renders empty territory precisely on the instant-world flagship surface, and D10 never says whether seeds come from stored `cellId` or from x/y re-derivation. Worse, if the territory lane writes its own nearest-centroid resolver while SEAM-2 writes another, the estate mints two cell-resolution laws for one field — the §711.6 cross-consumer class the program exists to prevent. **Amendment:** D10 mandates seed derivation from placement x/y via the SAME shared nearest-centroid helper SEAM-2 mints (one resolution law, two consumers), and POLIS-4's placement assertion asserts against that shared derivation.

**8. MINOR — §2 D3 vs §3 W-NAME self-contradiction on wards.** D3 lists "wards while townCartography lives" as a migrating consumer; §3 sequences NAME-2 "after STRIP-4 removes the wards consumer." By the volume's own ordering the clause can never execute — strike it (V-collisions.md V2 already treats the wards row as census-stale).

**9. MINOR — NAME-2's declared-shift scope line is wrong for the successorNpc cure.** successorNpc mints at tick on existing campaigns (successorNpc.js:1-10, read this lane: ousted-replacement, rng passed in), so a lit flag changes future ticks of lived campaigns — legitimate, but "lit = declared shift, new worlds/foundings only" misstates it. Amend to: dark = byte-identical; lit on an existing campaign = future-tick-only change; and require the car to verify the passed rng at that call site is call-scoped so changed draw counts cannot displace sibling draws.

**10. MINOR — POLIS-3 omits the placement-assertion arm that program law §1.6 mandates for "any spatial mark family (territory shading, **overlays**)."** Either add the arm or write the exemption rationale into the charter (id-anchored via `mapState.placements` = the market-stall anchor class, V-polis.md §2/§Cross-cutting-3) — an unstated exemption is a law the next lane learns to ignore.

**11. MINOR — CAP-4's subtype dependency on CAP-3 is ambiguous: raw captured `temp`/`prec` vs CAP-3's flag-gated banded sub-digest.** Two lanes fork on whether lake typology requires `climateTruthEnabled` lit. Amend with a rule: sub-digests read captured inputs only, never sibling sub-digests; subtype is a typed absent/`unknown` when climate fields are uncaptured.

**12. MINOR — NAME-1's `{road}`/war-name tokens vs the news-address law.** "the Amber Road" replacing "the ${dest} road" deletes the address from the address chain (roadsProse law: the tokens ARE the world's names, V-naming.md §5). Amend: name tokens augment, never replace, the `{dest}`/`{settlement}` address tokens.

Attacks attempted that failed (not findings): D2 vs frozen slots — the additive-key sub-digest idiom + self-versioning absorbs both views and the promotion is a pure move pinned by `seaLanesGolden` (V-bridge.md idioms 2-3, V-faithGoods.md §4); D7's never-eager landing vs VAR-2's runtime consumer — the composer dynamic-import lane covers it (V-variability.md §4); D8's #12-coupling — consistent with V-variability's structural table; §1.5's new-file bill matches V-collisions V5.

**VERDICT: NOT-REFUTED** — the substrate decisions cohere as a system with the listed amendments; findings 1, 2, 5, and 7 must be folded into Amendment A1 before the affected cars (NAME-1, ST-2, SEAM-3/CAP-1, POLIS-4) dispatch.

---

# PANEL LENS 5 — PRODUCT/SEQUENCING (skeptic panel wf_0f71adeb-14c, verbatim)

1. **BLOCKER — §5 (owner rows) / §3 ST-1, NET-2 — the owner batch is incomplete; two cars smuggle persistence-shape decisions past it.** The volume's own bar is Q-W4: a structured field on persisted news entries, "future-tick persisted bytes, declared" ⇒ owner row (DESIGN_FMG_WEAVE.md:91). Two cars make the same class of change ungated: (a) **ST-1** mints a typed `origins` field on faith records — persisted worldState campaign state, new-entries-only, in an estate where the schema manifest has no precedent row ("`fieldManifest.js` has ZERO faith rows (grep count 0…) — its own contract… would apply", weave-verify/V-faithGoods.md:22); (b) **NET-2** mints `ROUTE_GENESIS_LAW_VERSION` "recorded on the ledger at connect" (DESIGN_FMG_WEAVE.md:14,58) — a new persisted field on the route-network ledger ("nothing pins 'which candidate law derived this ledger' today; the ledger just persists its output", weave-verify/V-net.md:24). Both are in the ungated dispatch set (§4.2 lists ST-1 as STRIP-independent; DESIGN_FMG_WEAVE.md:82). §728.1's blanket ratification covers the capabilities, not the shapes — the volume proves this itself by gating Q-W1/Q-W3/Q-W4, all equally inside the ratified scope. CAP-3/CAP-4 and SEAM-2 are distinguishable (they extend the self-versioned additive sub-digest / `skippedSettlements` idioms with in-estate precedent); ST-1 and NET-2 have no such precedent. **Amendment:** add Q-W5 (ST-1 field family + record shape + cause vocabulary) and Q-W6 (NET-2 ledger stamp) to the §5 batch; both REC grant; neither blocks the rest of its car's family.

2. **MAJOR — §3/§4.2 — review candidate #11b (per-knob "keep this" wizard pins) has a sequencing row but no owning car, so it silently becomes post-launch.** §4.2 sequences "VAR wizard pins" after STRIP-6 (DESIGN_FMG_WEAVE.md:82), but no §3 car charters them: W-VAR holds only VAR-1 (interpreter) and VAR-2 (composer adoption) (:67-69), and POLIS-1 carries only #11a "pyramid jitter + sentinel randomization" (:61). The ratified item is "(b) per-knob 'keep this' pins in the wizard, UI-state only" (REVIEW_FMG_RECONCILIATION.md:88), and §728.1 mandates all twelve candidates pre-soak (ODQ §728.1, commit a2bda7869). An item with a sequencing constraint but no car is a dropped thread. **Amendment:** mint VAR-3 (S, UI-state only, post-STRIP-6) or add the pins explicitly to VAR-2's roster.

3. **MAJOR — §3 POLIS-5 — the dispatch condition is unbounded, contradicting §4.3's "Everything here is pre-soak."** POLIS-5 dispatches "only after POLIS-2 ships and first-tick ramp behavior is observed" (DESIGN_FMG_WEAVE.md:65) — no instrument, no observer, no decision deadline. If the observation never lands, a ratified sub-item of candidate #5 (the ongoing-war option) slips past launch with no owner-visible deferral, exactly the silent-post-launch failure this volume's §4.3 forbids. The observation is achievable pre-soak in a harness (V-genesis confirms the organic path: "`evaluateMobilization`… starts posture ramps **on tick 1**… this is the designed-in slow burn", weave-verify/V-genesis.md:65), and the hand-seeded deployment's capacity-model reproduction is still PLAUSIBLE-only (V-genesis.md:44) — more reason the observation step needs a named vehicle. **Amendment:** bind the observation to a Dramatic-tone first-N-ticks harness landing in POLIS-2's act, with a terminal fork: dispatch POLIS-5 pre-soak or convert it to an owner row recording the deferral.

4. **MINOR — §4.2 — NET is labeled STRIP-independent, but the receipts matrix shows a shared export estate with STRIP-2.** V-collisions' conflict table puts `realmMapExport.js` in the NET×STRIP-2 cell ("road/lane drawing rides exports; STRIP-2 edits its town half — `realmMapExport.js:34` imports `townMapExport.js`", weave-verify/V-collisions.md:27), and STRIP-2 re-records PDF parity goldens (charter §11.2, `git show db1d80bee:docs/DESIGN_MAP_MODULE_SPLIT.md`:129). NET-1's declared visual shift landing before STRIP-2 forces two successive parity-golden re-records with interleaved causes. §4.2's own first clause ("STRIP waves land first on any shared surface") already implies the ordering — the parenthetical asserting NET independence invites a lane to violate it. **Amendment:** state that NET-1's export-visible half lands after STRIP-2, or record why the export estate is not a shared surface for NET-1.

5. **MINOR — §1.5 — "land new files batched, one lighting-census re-derivation for all" is unachievable as one batch under the volume's own order.** The new-file bill spans the whole program: SEAM's possible S5 file is dispatchable immediately, NAME-2's waits for STRIP-4, POLIS-4's for STRIP-6, VAR-1's anytime (DESIGN_FMG_WEAVE.md:15 vs :38,52,82; roster per weave-verify/V-collisions.md:74). A single re-derivation cannot cover files that land months of waves apart. **Amendment:** restate as batched-per-landing-window, naming the feasible pairings (e.g. VAR-1 with NAME-2).

6. **MINOR — §2 D7 / §3 VAR-1 — the review's explicit "parameterize the two DOM reads" requirement is absent from the charter text.** REVIEW_FMG_RECONCILIATION.md:104 requires it for #12; guard row 1 makes in-generator DOM reads categorically forbidden (ODQ §727.3). D7 rules on detPow, substrate, roster, Uint8/`leastIndex` semantics, and landing zone (DESIGN_FMG_WEAVE.md:29) but never the DOM reads; "pure leaf" plus the layerBoundaries walker bans React/Zustand *imports*, and a bare `document`/`window` read is not an import. **Amendment:** one clause in VAR-1's row; flagged also to the determinism lens.

Checks that failed to refute (attempted, receipts held): **W-ARMS is genuinely §728-ratified** — "'It all' is read as the whole review INCLUDING the owner-gated generative-heraldry item (this directive is its ratification; it is sequenced last and stays vetoable)" (ODQ §728.1, commit a2bda7869) — so it is mandated scope, not inflation, and the 104-CC0-charge cap is honored. **SEAM-0 is correctly prioritized**: first car, S-sized, dispatchable immediately after panel, its cure (SEAM-2) explicitly identified and nothing downstream gated on the wrong node. **The §727.3 guard list survives row-by-row**: determinism hygiene (D4 hash-not-PRNG, D7 detPow, §1.1), living-tick/dormancy (§1.2), causal explainability (D6 provenance), belief layer (D9 reads ledgers; no omniscient write), diplomacy lifecycle (D6: closed vocabularies, no relationshipStates writes, no fabricated tick history — matching V-genesis.md:34's lazy-materialization ruling), religion dynamics (ST-1 structure-only), lifecycled hazards (V-polis.md:34 resolves the completeness check: disasters are calamities by design; ST-4's cosmetic widening is the correct residual), port geography∧institution (CAP-2 strengthens it; NET-1 unifies onto `isPort`; NET-2 defers to `portTotalityPairs`), anti-star autoplacement (untouched; D10 is view-only), pipeline rigor (D12 rejects the global registry), three-knob front door (stays; only the lock device transfers), economy in motion (ST-2 is namespace-only), regen preservation (untouched). **The STRIP dependency map is right against db1d80bee**: STRIP-4 deletes `townCartography/` (14 files) which holds the `cartographyWards.js` namingData consumer ⇒ NAME-2 after STRIP-4 correct; UI cars after STRIP-6 matches V3; the STRIP-3 re-scope is receipted by V1 (mapSlice.js is the world-map slice the charter's own STRIP-6 clause says STAYS) and is recorded vetoably in the volume; the STRIP-6 allowlist amendment matches V4. **Twelve candidates + smaller steals all have cars** except #11b (finding 2). **Pre-soak placement** matches the ruled tail order (walk → ONE regen → terminal soak → tuning last), and the owner's time-is-not-the-constraint directive removes deadline pressure as grounds for descope.

VERDICT: NOT-REFUTED (sound with the listed amendments; finding 1 must land before any of ST-1/NET-2 dispatches).