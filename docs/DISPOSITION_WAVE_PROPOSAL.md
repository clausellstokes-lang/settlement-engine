# THE DISPOSITION-WAVE PROPOSAL — the red base, dispositioned by measurement

Chair document, 2026-08-04, minifold @ `18f0ced4`. This is the proposal §3e recorded as
owed ("no full `npm run check` can green until dispositioned"). Every row below is
MEASURED — three recon agents (run `wf_f866c392-014`) executed the walkers solo on a
clean tree, attributed by violation rows against git history, and read every assertion
they classified. The owner's standing §5 order (2026-08-02: bank failures, continue the
queue, adjudicate only in the consolidated adversarial pass after the queued build) is
HONORED: nothing here repairs early except the one wave marked as a WR-10 wiring
prerequisite under the owner's 2026-08-04 "finish WR-10" grant.

**The red set, confirmed by execution:** `npx vitest run tests/lint` = 11 files / 32
tests failed (699 passed), byte-identical to the pre-WR-10 base; plus
`mechanismLitCoverage` (tests/property) = a twelfth red file with 2 failures. The three
recon-discovered reds the §3e census missed by name: `deepCloneHotPath`,
`ruinFilterRoster`, `seedLoopTotality`, `transcendentalMathBaseline` (all inside the
11-file set).

## D-W1 — WIRING-WAVE PREREQUISITE (executes NOW, inside the WR-10 wiring wave)

The three war kind-pool walkers (`warCostKindPools`, `warRulingKindPools`,
`warCoalitionKindPools`) + `envoyKindPools`. **The corpus is not missing anything — the
walkers' document address lies:** 13 kinds' pools were relocated to
`RECEIPT_POOLS_LEGACY.md` in ONE commit (`50b69622`) leaving `→ pool lives in…`
pointers the walkers' `indexOf`-slice extractors cannot follow. Cure (one shared
mechanism): teach `annexLines()` to follow the pointer, AND land the exactly-once
anchored-regex guards from the exec-pin sweep in the same change (these four walkers
are also first-match-vulnerable — `indexOf('# WR-7')` even matches `## WR-7a` as a
substring). The legacy blocks carry 7–9 rows with `[live, verbatim]` / `requiredSlots:`
suffixes — the parser must handle the richer shape, not just re-point. Why now: the
wiring wave's Herald kinds add rows these walkers must judge honestly; building new
kinds against walkers that lie about existing ones repeats the attribution sin.
Class B stays out (below).

## D-W2 — CONSOLIDATED PASS, MECHANICAL TIER (fix-cheap, no judgment)

- `deepCloneHotPath` (1): route through sanctioned `src/domain/clone.js`.
- `domainAnyCastBaseline` (warDeployment 17 vs 16): TYPE the `worldStatePatch` field
  (pulseShapes typedef lineage) — the ratchet's own text forbids widening.
- `mechanismLitCoverage`: 21 of 28 gap modules are decomposition leaves of
  already-lit mechanisms → `LIT_COVERED_BY` registry entries (the walker's own
  sanctioned instrument), never baseline rows.
- `proseNumerics`: 13 pure relocations (byte-identical snippets moved by the WR-7b
  decomposition) → one deliberate baseline re-record for row addresses, cause declared;
  plus the stale row `occupation.js:852` (points at a blank line; SOL-BANK-2's class).
- `seedLoopTotality` + `negativeAssertionAnchor`: each holds ONE generation-facing
  breach that is fix-cheap-and-MANDATORY (the epistemic-prevention gates exist for
  exactly these files); the bulk stays in D-W3.

## D-W3 — CONSOLIDATED PASS, JUDGMENT TIER (burn-down, per-row calls)

- `negativeAssertionAnchor` 64 ordinary rows — the roster's own header FORBIDS
  register-and-freeze ("the roster cannot grow"); each row gets `// anchored:` or dies.
- `seedLoopTotality` 13 sites across 7 domain-test files (mechanical but behavior-
  adjacent: `collectSeedFailures` adoption).
- `ruinFilterRoster` 6 readers — each classified live-provider vs ruin-agnostic; a
  wrong call silently counts a razed institution as alive.
- Kind-pool Class B: 4 kinds where CHRONIC-TIER DEEPENING (`1e8bf8a8`) authored 6th+
  variants past the walkers' 5-row assumption — the walker contract needs a ruling
  (cap raised vs corpus trimmed), distinct from Class A's relocation.
- `proseNumerics` 9 genuine new rows (floatInterpolation +7, twoDecimalScore +2 —
  reconciles exactly to the two category-ceiling violations): band or exempt per row.
- `clampPrimitiveBaseline` 11 unregistered clamps — **chair pre-ruling, vetoable:**
  NONE is byte-identical to the kernel (5 NaN-passthrough, 4 Number-coerce, 2 coerce:
  +Infinity→1 where the kernel yields 0). Unification is therefore BEHAVIOR-TOUCHING
  and belongs here, per-site, with the kernel's non-finite→lo doctrine as the default
  and any deliberate divergence registered with an in-file rationale (the CR-WR10-A
  pattern). Register-and-freeze without that review would freeze semantic drift.

## D-W4 — OWNER-FLAG ROW (decide, don't just fix)

`transcendentalMathBaseline`: `dispositionLedger.js` calls `Math.pow(0.5, x)` with a
fractional exponent — NOT correctly-rounded, may differ across JS engines. Under THE
PROMISE ("a seed is a world, forever") the same seed could decay a disposition channel
differently on different machines. This is a DETERMINISM row wearing a lint costume
(also SOL-BANK-4). Proposal: replace with the ratchet's sanctioned deterministic
alternative at the consolidated pass and flag the class in the release notes for the
soak grid. Owner sees this line because it touches the constitutional law.

## D-W5 — DOC-PIN HARDENING (consolidated pass; the first-match class)

The exec-pin sweep found **13 vulnerable rows / 17 sites** among 31 doc-reading pins
(4 land early via D-W1). The remainder: two RECEIPT_POOLS_LEGACY stateful parsers
where a duplicate heading silently makes the LAST block win; `deployRunbookFreshness`
×2, `abuseModelFreshness`, `analyticsDictionaryFreshness`, `docCounts` ×2,
`architectureFreshness` ×5 — all first-match `.match()` extractions where a second
stale claim in the doc is invisible. Cure per site: matchAll + assert-every-claim (the
repo's own `statedCounts` idiom) or exactly-once guards. Full site list: the recon
return (`wf_f866c392-014`, agent `recon:execPins`).

## SOL-BANK reconciliation

SOL-BANK-2 → D-W2/D-W3 (proseNumerics); SOL-BANK-4 → D-W4; SOL-BANK-5/8/9 (max-lines
ceilings incl. `peaceTerms` 823/800, `eventProse` 895/800, `pulseKernel` — the 1580
bank stands under R-BLD-10) → the consolidated pass's decomposition slice; SOL-BANK-1
→ CLOSED by the app-shell census (leaf mocks @ `456271ea` + `ac377738`) — adjudicate
as already-cured; SOL-BANK-3 (typecheck exit 2 at `realmVerbExecution.js:323`) →
D-W2-adjacent typing fix; SOL-BANK-6/7/10 → their recorded owners at the pass.

## Sequencing

D-W1 rides the WR-10 wiring wave (now). D-W2..W5 execute at the consolidated
adversarial pass, which per the owner's §5 order runs AFTER the queued build — the
Lane B census (same recon run) measured the queue at 10 UNBUILT items (SP, GR, IN, TR,
WF, POP, INT, CV/TB/XW) + 5 PARTIAL, so the pass is not yet due. Until then the red
base stays frozen and attributable: any lane growing it by even one violation row is
regressing against a measured floor, not against noise.
