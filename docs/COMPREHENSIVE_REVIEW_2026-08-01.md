# COMPREHENSIVE REVIEW — 2026-08-01 (the Fable validation session)

## Provenance: 10 Fable review dimensions over the Opus-era build (waves C..K, P, W1,
## cartography TC-0/1/2) + whole-system seams, each adversarially verified by an Opus 5
## skeptic (owner model split), + a completeness critic. 23 agents, 0 errors.
## 55 findings survived verification; 18 were REFUTED and are NOT listed
## (the full refutation record lives in the workflow journal). Verdict legend:
## CONFIRMED = the skeptic re-derived it · ADJUSTED = survived with the stated correction
## · UNVERIFIED = the verify pass did not reach it (treat as PLAUSIBLE until executed).
##
## DISPOSITION: this register is INPUT to the external implementer program. Fix specs
## reference DESIGN_WAR_RULINGS_ARCHITECTURE.md where a wave owns the surface; everything
## else lands via the standing remediation lanes. Findings are ranked most-severe first.

### R-01 [CRITICAL/UNVERIFIED] The two disputed goldens reproduce RED on a settled tree — the W1 same-seed behavior shift is real and no dimension adjudicated it
- **Where:** tests/property/momentumDormancyGolden.test.js:269 and tests/property/beliefMapGolden.test.js:166
- **Finding:** Executed: `npx vitest run tests/property/beliefMapGolden.test.js tests/property/momentumDormancyGolden.test.js` on the now-static tree (no vitest workers, cartography agents long stopped, all post-pause commits docs-only) -> 2 failed | 9 passed in 2.98s. momentumDormancyGolden drift rows: `mo-b|8|one_month`, `mo-c|6|one_week`. This refutes the pause handoff's open hypothesis (docs/HANDOFF_2026-08-01_PAUSE.md section 3) that the reds might be moving-tree churn artifacts. Crucially, the verbose run localizes the cause: the momentum-specific dormancy CONTRACT ('gate absent writes NO commitments ledger, even in a marching-war world') PASSES, and the lit-path no-new-rng-draws same-seed byte-identity PASSES — only the whole-world projected hash drifts. So the drift is in the war-state/histogram projection of a war-active fixture, i.e. the W1 war joins (dirty: pulseKernel.js, applyWorldPulse.js, warDeployment.js, settlementStrategy.js, occupation.js, peaceTerms.js, mobilization.js) changed same-seed worlds through the LIT war-layer path, not through a momentum dormancy leak. All 11 review dimensions reviewed code at rest; none executed this adjudication, which is the single gravest open question in the tree because it sits on THE PROMISE (a seed is a world, forever).
- **Failure scenario:** W1 is committed on the current evidence: either (a) a genuine lit-path behavior change lands with the goldens quietly re-recorded — an undisclosed same-seed world shift violating THE PROMISE and non-negotiable 10, or (b) a not-yet-excluded dark-path leak ships unfixed. Conversely, a successor believing the handoff's churn hypothesis re-runs the full gate, sees the same reds, and burns hours re-deriving what one 3-second focused run already proves.
- **Fix sketch:** Before the W1 commit: bisect the seven dirty worldPulse files against the two golden fixtures (both fixtures carry marching wars, so siegeArrivalGate and strategy-opens-wars are the prime suspects) to name the exact join that moves each hash; write the diagnosis into the commit as a disclosed one-time shift; then re-record both goldens under an owner-signed re-record (the handoff's step 3), never silently. If any drifted config turns out to have the war layer DARK, stop — that is a dormancy leak and a hard defect to fix, not re-record. Note the momentum contract sub-tests passing means the momentum fence itself needs no repair.
- **Finder confidence:** CONFIRMED
- **CLOSED 2026-08-02 (self-audit):** the adjudication this item prescribed was executed by the Fable chair 2026-08-01 (FABLE_VALIDATION_QUEUE W1 chair row — the dormancy-break hypothesis REFUTED on four executed grounds; the shift ruled a legitimate lit-path W1 change), and W1 landed WITH the authorized, disclosed re-record @ f5a88ac1, executed by Sol. The drifted rows were exactly the three predicted (bg-c|14, mo-b|8, mo-c|6). The one-time shift is disclosed, not silent; nothing here remains open.

### R-02 [HIGH/CONFIRMED] Autoplacement 'one undo reverts the whole act' is false for the place class
- **Where:** src/store/mapSlice.js:500
- **Finding:** applyAutoplacement pushes one act-opening snapshot ('autoplace settlements', mapSlice.js:500), but every kind:'place' proposal then routes through get().addPlacement, whose own set() unconditionally pushes a second snapshot ('place settlement', mapSlice.js:360). The move class is clean (updatePlacement does not snapshot), and the committed test (tests/store/autoplacementStore.test.js:136-155) only exercises moves — so its 'Exactly ONE snapshot was pushed' pin is vacuous over the flagship class: the live planner emits kind:'place' for every not-yet-placed settlement (src/domain/realm/autoplacement.js:524), which is the feature's primary purpose ('the realm places itself'). Executed repro against the real createMapSlice (scratchpad test, vitest): after a 1-move+2-place act the stack reads ["autoplace settlements","place settlement","place settlement"]; one mapUndo left the moved settlement at its NEW position (x:100,y:110,cellId:44) and one newly placed settlement still on the map — only the last placement reverted. The docstring's claim ('pressing Undo puts every settlement back where it was') and the Wave G commit message ('one undo snapshot covers the act') are both contradicted. Aggravator: mapUndoStack caps at 30 with shift-eviction (mapSlice.js:193), so a consented plan placing ≥30 settlements evicts the act-opening snapshot entirely and whole-act revert becomes impossible at any press count.
- **Failure scenario:** User confirms an autoplacement plan that places 12 unplaced settlements and moves 3; presses Undo once expecting the promised whole-act revert; only the 12th placement disappears — 11 placements and all 3 moves stand, and the user must discover 13 total presses (or, at ≥30 placements, can never fully revert).
- **Fix sketch:** Make the act's snapshot the only one: thread a suppress-snapshot option through addPlacement (e.g. via:'autoplace' skips its internal snapshotForUndo, mirroring how updatePlacement already relies on caller-owned pushMapUndo), or have applyAutoplacement record stack depth before its loop and splice the per-place entries back down to the act entry afterwards. Extend tests/store/autoplacementStore.test.js with a mixed move+place act pinning mapUndoStack length 1 and one-press byte-equal restore — the current pin's vacuity over the place class is exactly the harness-default-empty-state trap this estate has recorded before.
- **Finder confidence:** CONFIRMED

### R-03 [HIGH/CONFIRMED] Completed satellite plan can deliver a founding that never happens: no cap/headroom check at open or completion, and the receipt says "It is done"
- **Where:** src/domain/worldPulse/demographicsPlans.js:472-493 (completion), src/domain/worldPulse/demographicsResponses.js:322-332 (availability), src/domain/worldPulse/settlementLifecycleKernel.js:746-816 (intent consumption)
- **Finding:** scoreResponses' satellite availability checks satelliteLaneLit, satelliteCap > 0, the viability grade, ground saturation and absorption — but never the parent's CURRENT steading count against the cap, and the plan lane is never handed the satellites ledger. A parent already holding cap steadings can open a 26-tick satellite plan, raise 120 provision, and complete it. At completion the plan closes state 'completed' / outcome 'delivered' with the line "<name> sends families out to break new ground. It is done." and emits a FoundIntent. In the lifecycle kernel, `armed && sats.length >= cap` (line 748) emits only a 'satellite_deferred' receipt and the per-tick intent map is discarded — the plan lane never learns, no plan-lane failure fires, and the 182-tick COOLDOWN_COMPLETED still applies. Worse, when the mint refuses for headroom (`debit < FOUNDERS_MIN`, mintSteading:434) the `if ('record' in minted)` block at 772 has NO else branch: the intent evaporates with no receipt of any kind. Executed proof: advanceDemographicPlans on a cap-full town completed 'delivered' with provisionSpent 120 and emitted the intent, while mintSteading on the same parent answered {"refusal":"the town cap (2) is reached"}. This violates §5c (a plan's outcome vocabulary must name why it stopped), acceptance claim 5 (plans persist to receipts) and claim 7 (founding conserves people and capital) — provision is recorded spent on a steading that does not exist and the chronicle affirms a founding that never occurred.
- **Failure scenario:** Town at its wave-E cap (or organic seeding fills the cap during the plan's 26-tick window) crosses the 'pressed' band, wins the satellite response, completes the plan → receipts record a delivered founding + provisionSpent, dossier/Herald narrate success, no steading exists, and the settlement is cooldown-locked 182 ticks with its pressure unanswered.
- **Fix sketch:** Thread the parent's live steading count (or the satellites ledger) into scoreResponses' satellite availability with a named refusal ('cap_reached'), add the same condition to the active-plan failure arms (close 'failed'/'ground_lost'-style outcome when sats.length reaches cap mid-plan), and give the kernel's intent-consumption path a definitive contract: any intent the mint refuses must surface as a receipt carrying the mint's refusal word and, ideally, feed back a plan-lane closure outcome instead of a silent drop.
- **Finder confidence:** CONFIRMED

### R-04 [HIGH/CONFIRMED] DESIGN_ROUTE_LIFECYCLE §5d (the lived economic profile — owner amendment, twice-amended, binding) is entirely unbuilt, assigned to no slice, and absent from the deferral ledger while the J program is recorded COMPLETE
- **Where:** docs/DESIGN_ROUTE_LIFECYCLE.md:147
- **Finding:** §5d promises: derived network roles (crossroads/gateway/entrepot-of-transit) with betweenness-based centrality, the gravity loop with anti-runaway band, role-threshold Herald events ('Karsgard is spoken of as a crossroads now'), carriers (faithSpread + distance-priced news) weighting by edge grade, the canonical mining-town end-to-end integration test, and genesis-immutability/hysteresis pins. None exists: no routeNetwork* module computes roles (module census: Ledger/Genesis/Flows*/Charter*/Decay*/Consumers* only); rg finds no crossroads/gateway/betweenness/livedProfile token in the J family; religiousContest.js and distancePricedNews.js read no routeNetwork or edge grade. Crucially the design doc's own §14 slice plan (J1–J4) never assigned §5d to any slice and §15 DEFERRED does not record it — it fell through the slicing, silently. The nearest existing machinery is the OLDER entrepotKernel.js (M6b earned centrality from shipment gate-crossings), which §5d never reconciles with — a second-centrality-truth hazard when §5d is eventually built. Cross-doc echo: §5d says 'the A-8 institution counts read the LIVED profile, not genesis alone' while DESIGN_TOWN_CARTOGRAPHY §11b defines A-8 counts from population×economic profile(+prosperity) with no lived-profile input (see separate finding).
- **Failure scenario:** The soak redo and tuning pass run on a 'complete' J program whose loop only closes one way (economy→routes) — settlements never gain or lose network identity, the Herald never tells the crossroads arc the owner ruled for, and the TC-3+ cartography waves later build A-8 counts against an input (the lived regime/profile) that does not exist.
- **Fix sketch:** Charter a J5 slice from §5d verbatim: a routeNetworkRoles module deriving role composition from the bounded lived network (degree + betweenness over the candidate set + border placement), persisted role bands with hysteresis, threshold-crossing Herald events, the carrier grade-weighting hooks in faithSpread/distance-priced news, and the canonical example as its integration pin; include an explicit seam ruling versus entrepotKernel's earned-centrality ledger (one centrality truth, or a documented projection relationship).
- **Finder confidence:** CONFIRMED

### R-05 [HIGH/ADJUSTED] Hidden edges are unrevivable outside destruction-resettlement: returning demand can never reopen a decayed road
- **Where:** src/domain/worldPulse/routeNetworkDecay.js:108
- **Finding:** Three mechanisms interlock into a one-way door. (1) PROMOTE_TALLY (routeNetworkDecay.js:108) has rungs only for 'road' and 'highway' — the promotion arm computes target 'track' for a hidden edge, finds no bar, and returns 'busy' forever (evaluateEdgeDecay:463-472). (2) accrueRouteFlows routes every traversal on a pair that carries ANY edge — hidden included — onto edge.usage, never the corridor (edgeIdForPair, routeNetworkFlows.js:608-616,700), so corridor demand never rebuilds. (3) evaluateCorridorCharter refuses any corridor whose pair carries any edge with 'already_served' (routeNetworkCharter.js:359), and edgeIdForPair is grade-blind, so a hidden edge blocks re-chartering. Executed proof: a hidden A–B edge with fresh usage tally 100,000 was evaluated 40 times over 520 ticks and never promoted ('busy' every pass); a corridor at tally 500 (formation bar 48) returned 'already_served'; a live migration column accrued onto the hidden edge (503) while the corridor stayed frozen at 500. The only exits are reviveHiddenCorridors — wired solely to the remnant-resettlement seam — and DM hand-charters (which the genesis adopt seam would also skip, since withRouteEdges is explicit-wins and the hidden edge already owns the edge id). This contradicts Law 5's own rationale ('a hidden path holds every door open for revival'), §0's thesis ('routes charter where accumulated need proves them'), and §6/§7's 'grade promotion follows sustained usage'. §15's DEFERRED list does not record it. Over a long soak the open network can only shrink absent destruction-revival — a necrosis class invisible at short horizons, exactly like the 300y population runaway. Secondary inconsistency folded in: movers that §9 forbids from hidden ways (armies, migration columns) still accrue usage onto hidden edges, so the remnant's tally is fed by traffic the lived road could not have carried.
- **Verifier adjustment:** The one-way door is real and all three mechanisms reproduce, but the exit inventory is wrong in BOTH directions and one clause is overstated. (a) A THIRD, WORKING EXIT exists that the finding does not name: a live strategicNeed at or above the garrison band raises a HIDDEN edge straight to 'road' in a single sweep (evaluateEdgeDecay:455-458 → withEdgeGrade:232-248). Executed: edge grade 'hidden' + strategicNeed 'garrison' → verdict 'garrison_floor', after='road', stepped=1 in one sweep at tick 200. So the accurate claim is narrower and still severe: TRADE AND POPULATION DEMAND can never reopen a decayed way; only remnant resettlement or a war can. (b) reviveHiddenCorridors is NOT 'wired solely to the remnant-resettlement seam' — grep over src/ shows it is called by NOTHING at all (definition at routeNetworkDecayLifecycle.js:156 plus tests/domain/routeNetworkDecay.test.js only); the design records the kernel calls it 'when the wiring slice lands'. (c) The DM-hand-charter clause is speculation about a seam that does not exist and should be dropped. (d) Severity context the finding omits: the whole W-J lane is dark AND unwired — routeLifecycleEnabled is declared false at simulationRules.js:456 with 'LIGHTING IT belongs at the declared golden boundary', and src/domain/certification/subsystemRowsWaves.js:380 records 'it is not wired into pulseKernel, exactly as J1 through J3 are not'. No live world can hit this today; the integration wave inherits it. Severity stays HIGH: the declared envelopes do not catch it (the certification ratchet at subsystemRowsWaves.js:411 counts route_edges, which hidden edges satisfy; the anti-flap envelope at :416 measures churn, not necrosis), and §15 records no deferral.
- **Failure scenario:** A chartered road serves two towns; a war-decade starves it; it demotes road→track→hidden. Peace returns, trade demand comes back at ten times the formation threshold. Every pulse forever: traffic lands on the hidden edge's usage, the corridor never re-forms, the charter sweep says already_served, the ladder says busy. The realm permanently loses the artery — and every port whose only water edge decayed the same way loses ports totality permanently.
- **Fix sketch:** Give the ladder its missing bottom rung as an EVENTED promotion: add a 'track' entry to PROMOTE_TALLY and let the existing promote path (which already mints route_promoted news) lift hidden→track on sustained FRESH usage from franchise-holding sources — or, alternatively, make edgeIdForPair grade-aware in the accrual and charter paths so a hidden-only pair accrues corridor demand again and re-charters through the ordinary §6 gates (retiring the hidden edge into the new charter in the same fold, preserving its usage history as the warm start). Either cure must also filter accrual sources by the §9 access law so army/caravan flows cannot wear a smuggler's path, and needs a pin of the form 'returning demand reopens a decayed road within N evals' plus its negative control (demand below bar stays hidden).
- **Finder confidence:** CONFIRMED

### R-06 [HIGH/ADJUSTED] Law-4 conservation leak survives under the lit flag: aspatial mass emigration still evaporates 55% of every column, and the promised P4 decline-lane reconciliation never landed
- **Where:** src/domain/worldPulse/populationDynamics.js:377-430
- **Finding:** Design law 4: "the realm total changes only through births, deaths, and world edges" and §3/§0 promise "the soak's realm-conservation check becomes exact." Both populationDynamics.js:377 and demographicsKernel.js's header defer the decline-lane reconciliation to P4 ("P4 reconciles the decline term with the demographic death term; until then a pressured settlement is answered by both"). P4 has landed in full (Herald, risk, war, ladder, observation) and no reconciliation exists — grep for it finds only the promise. Executed proof at demographicsEnabled:true, interval one_month: a 10,000-person settlement under famine+plague+war_pressure emitted candidateType population_emigration with deltas [[A,-304],[B,+137]] — realm net -167 with no death record, no migration column, no world edge; the 55% are the legacy "fates unresolved" evaporation (abs*0.45 at line 419), now reachable inside the lit demographic engine whose whole certification is exact conservation. Additionally the unreconciled lane stacks decline channels: the kernel's death multiplier (pressure+deficit), the legacy pressure-rate decline (now deadband-free via P1a so it fires every tick), P2 push departures, AND P4's own disease/raid lifts feeding back through the pressure index into populationPressureRate's stability term — four channels answering one cause.
- **Verifier adjustment:** The aspatial mass-emigration path leaks 55% of every column (and 100% in 'void' mode) with no death record, no migration column and no world edge — violating law 4 and the §3/§0 promise that 'the soak's realm-conservation check becomes exact'. The leak is IDENTICAL dark and lit: it is pre-existing legacy behavior that the wave neither introduced nor enabled, not something 'now reachable inside the lit demographic engine'. It nonetheless blocks the wave's own acceptance contract, because the owner-signed soak redo lights demographicsEnabled and measures conservation. Separately, the decline-term double-count IS a recorded in-code deferral (populationDynamics.js:375-379) whose named resolution slice (P4) has landed without the work; the conservation leak itself has no deferral anywhere.
- **Failure scenario:** The owner-signed soak redo lights demographicsEnabled on an aspatial (or spatial-dark) preset; any settlement crossing the mass-emigration bar silently deletes ~55% of each column from the realm total, failing the wave's own acceptance (claims 9/11-adjacent conservation exactness) — or worse, passing unnoticed and re-poisoning the tuning pass's #1 input; pressured settlements simultaneously decline measurably faster than the §2 rate model's derived 76-83% plateau predicts.
- **Fix sketch:** Finish the deferred reconciliation as its own slice before the soak redo: when demographicsEnabled is lit, either route the legacy lane's mass-emigration entirely through the P2 column ledger (arrivals conserved, unplaced named) and delete the 0.45 aspatial split on the lit path, or gate the legacy decline/emigration candidates off entirely and let kernel deaths + P2 departures own the decline side; simultaneously subtract (or fence) the P4 risk lifts from populationPressureRate's inputs on the lit path so one cause is answered by one channel.
- **Finder confidence:** CONFIRMED

### R-07 [HIGH/ADJUSTED] Lineage-claim war (WR-3 / owner cause #2) may be structurally unreachable: satellite lineage exists only in a cosmetic ledger whose members can never become war-capable settlements
- **Where:** src/domain/worldPulse/settlementLifecycleKernel.js:10
- **Finding:** The census says 'Satellite founding lineage (parent→child edges) | wave E | BUILT'. True at the ledger level: spatialLedgers.satellites is keyed by parent. But the kernel's own contract states satellites 'are NOT digest members… orbit a parent (cosmetic map placement; routing via-parent)', and their lifecycle is die-back-to-parent or converge into a same-ledger hamlet; the spatial digest is constitutionally frozen, and warReasons pairs are keyed `${from}>${to}` over digest settlement ids. If no path ever graduates a satellite into a digest-addressable, war-capable settlement, then both directions of the owner's ruling ('the original and the satellite, or the satellite outgrowing the original') have no reachable pair, and WR-3's casus would be a vocabulary entry whose TRUE branch never fires in any generated corpus — exactly the unreachable-predicate-conjunction hazard class this program treats as a design defect.
- **Verifier adjustment:** Upgraded from PLAUSIBLE to executed-CONFIRMED, and the unreachability is worse than reported — BOTH directions of the owner's ruling fail, for two independent reasons. (1) THE GRADUATION SEAM IS EXPLICITLY PARKED, in writing: docs/DESIGN_SETTLEMENT_LIFECYCLE.md:129 — 'The V2 graduation seam (satellite → first-class digest member) stays parked for its own owner commission, recorded here so nobody builds it by momentum.' src/domain/worldPulse/settlementLifecycleKernel.js:10-13 confirms 'Satellites are NOT digest members … do NOT join the per-settlement mover loops'. So no lineage edge ever connects two digest-addressable settlements, and warReasons pairs are keyed `${from}>${to}` over digest ids. (2) THE TIER INVERSION IS ALSO STRUCTURALLY BLOCKED, which kills the 'satellite outgrowing the original' branch even if graduation shipped: settlementLifecycleKernel.js:827 gates growth on `!next.charterPending`, and :851 sets charterPending the moment population reaches villageMin — so a satellite hard-stops at village scale while parents are town+ by construction ('town+ parents seed SATELLITE THORPS', :6). A satellite can never outgrow its parent by tier. Note also that `satellite` in supplyWebWarfare.js is a DIFFERENT concept (digest supplier villages), not this ledger — the term collision is itself a hazard for whoever builds WR-3.
- **Failure scenario:** WR-3 ships lineage_claim + kinship_bond; the bijection walker is green, the dormancy golden is green, and across every soak ever run the claim scores zero because no lineage edge connects two digest members; the fourteenth-cause taxonomy carries a decorative entry and the owner's dynastic-war ruling silently never happens.
- **Fix sketch:** Before WR-3 is dispatched, verify with an executed corpus probe whether any lineage edge can join two war-capable settlements (satellite graduation, remnant resettlement of a satellite site, or campaign-member satellites); if none, WR-3's spec must first define the graduation/lineage-recording seam for real settlements (e.g., wave-P overflow foundings recording parentRef on campaign members) — and that is new substrate the wave brief must name, not wiring.
- **Finder confidence:** PLAUSIBLE

### R-08 [HIGH/CONFIRMED] NPC consequence economy has no engine trigger — W-H recorded COMPLETE but verdicts/circulation are never called from the pulse
- **Where:** src/domain/certification/subsystemRowsPeople.js:478
- **Finding:** DESIGN_NPC_CONSEQUENCES promises pulse-driven behavior ('the system fires exclusively on the corruption web's covert→revealed transition'; §6 'each pulse, roamers evaluate seeded rehost attempts'; §12 certification aliveness = verdict/rejection/arrival types, expectedTempo 'reactive'). Built reality: applyNpcVerdict (npcVerdictApply.js:503) and the whole circulation family have ZERO pulse-side callers — grep of applyWorldPulse.js/pulseKernel.js/corruptionWeb.js finds no import; the only production caller of npcVerdictApply is npcDmVerbs.js. The certification row itself says so at length: 'NO PULSE PATH ROUTES ANY OF THEM YET: the circulation lane has no caller, exactly as the verdict lane has none… THE SLICE THAT WIRES THEM MUST ADD THEM HERE IN THE SAME EDIT' — but no such slice exists in any recorded plan; Progress records 'THE H AND J PROGRAMS ARE COMPLETE' (batch 5) and batch 6 closed the directive waves. The design's own §16 records the H3-scope deferral, but no owner-visible ledger assigns the trigger wiring anywhere, and DESIGN_WAR_RULINGS_ARCHITECTURE builds WR-5/WR-7 on 'H2 verdicts' as live machinery.
- **Failure scenario:** Owner runs the ordered soak redo 'on the post-wave engine, everything lit': even with npcConsequencesEnabled forced true, zero verdicts, zero circulation, zero Wanderers ever occur because nothing calls the lane from the pulse; the certification row cannot red (its eventTypes list is deliberately empty), so the redo silently grades a dead system as dormant-honest; the tuning pass then ratifies NPC_CONSEQUENCES_TUNING bands against soaks in which the system never executed once.
- **Fix sketch:** Charter an explicit wiring slice (H5 or fold into WR-0/WR-1 sequencing) that (a) calls the verdict lane from the corruption web's covert→revealed transition and the circulation/residency lane from the pulse behind npcConsequencesEnabled, (b) adds npc_verdict/npc_rejection/npc_arrival/npc_dispersal to the certification row's eventTypes in the same edit per the row's own contract, (c) captures the dormancy golden fence first, and (d) lands before any soak redo the owner intends to read as evidence about this system.
- **Finder confidence:** CONFIRMED

### R-09 [HIGH/ADJUSTED] Query stamp is assigned, never earned: belief-only answers stamp 'confirmed' at any distance with a fabricated firsthand provenance
- **Where:** src/domain/worldPulse/brokerageServices.js:405-411
- **Finding:** answerBrokerageQuery builds its ReliabilityStamp from a FABRICATED provenance — `{ hopCount: 0, independentSources: 1, credibility01: 1 }` whenever any belief record exists — instead of reading the telling's actual rumour-ledger provenance the way heraldItemReliability does. hopCount 0 is the firsthand rung, so any house with channel competence >= SHARP_VOUCH_FLOOR (0.70: legal-major trade/politics, illegal-major crime/persons) stamps 'confirmed' (declared truth band 0.90) on an answer read from a possibly stale, distance-degraded belief record. Executed probe: a Chroniclers' Exchange asked about a subject 5 news-ticks away returned floor01 = 0.1558 (the module's OWN accuracy estimate for that pair) alongside stamp.grade 'confirmed', band 0.9, and detail 'It had this from its own ears, in the place it happened.' — a factually false source sentence. This contradicts design Law 2 (calibration honesty: every stamp is a testable claim), the module's own header ('THE STAMP IS EARNED, NEVER DRAWN'), and escapes the calibration-honesty envelope entirely: tests/domain/brokerageCalibration.test.js grades only real rumour-ledger provenance, and tests/domain/brokerageServices.test.js pins the query stamp with a bare toBeTruthy() (line 304). The fabricated grade also reaches the chronicle: evaluateBrokerageServiceRules carries result.receipt.grade into brokerage_query candidate metadata.
- **Verifier adjustment:** The mechanism is exactly as reported at src/domain/worldPulse/brokerageServices.js:405-411 and is fully reproduced, but the reachability clause needs correcting. Through the EXPORTED answerBrokerageQuery the belief-only 'confirmed' cell is live at any distance. Through the ONLY in-engine caller (evaluateBrokerageServiceRules) it is currently NOT reachable, because that caller queries only 'politics' (when a graph edge supplies a relationship) or 'war' (when it does not), and the single cell whose competence clears SHARP_VOUCH_FLOOR is legal-major politics 0.74 — which, at the caller's hardcoded delayTicks 0, also makes floor01 0.74 >= 0.70 so `truthEarned` fires and the claim is truth-sourced. What the producer DOES ship today is the same fabricated hop-0 provenance one rung down: an executed producer run mints receipt.grade 'corroborated' (declared band 0.60) with claimSources ['belief'] and the identical false source sentence. The belief-only 'confirmed' cell becomes reachable through the producer the moment finding 5's hardcoded delayTicks 0 is repaired — fixing 5 without fixing 1 creates precisely the reported defect.
- **Failure scenario:** Flag lit; a legal-major house is asked (via the pulse producer or a future DM surface) about a settlement several roads away; the register holds only a decayed belief whose real accuracy the fidelity model itself prices at ~0.16; the answer ships under 'confirmed' claiming ~0.90 truth and asserting firsthand witness; the crown-jewel theorem ('items stamped confirmed are true at >= 0.90') is false for the query surface while the envelope stays green because it never measures this path.
- **Fix sketch:** Make the query stamp read real provenance: thread the settlement's rumour-ledger telling for the subject (the sharpestTelling read heraldItemReliability already owns) into answerBrokerageQuery, and when the answer is belief-only with no telling, either emit no stamp (fail-closed, the module's own rule elsewhere) or derive the rung from the distance-attenuated floor01 (e.g. map floor01 through the declared truth bands so the grade is the largest band the floor clears). Separate the register sentence from the witness sentence so a belief-derived answer never claims 'its own ears, in the place it happened'. Extend the calibration envelope with a query-surface arm so the theorem covers both stamping seams.
- **Finder confidence:** CONFIRMED

### R-10 [HIGH/UNVERIFIED] The PDF export family is a generation behind the realm model: occupation and treaties render in the UI and the Foundry export but in NO PDF artifact
- **Where:** src/utils/generateWorldBook.js:409 (realmChapterRows), src/utils/generateCampaignPDF.js, src/utils/generateSettlementPDF.js:187
- **Finding:** Consumer census, executed: the occupation ledger (worldState.occupations, persistent regime change) renders at livingWorldSignals.js:194 (UI 'occupied' badge) and src/foundry/journalPages.js:366 ('Occupied — held by X' via vm.liveWorld); treaties-in-force render in src/components/map/TreatyPanel.jsx, RealmDashboard.jsx, WarFaithTab.jsx. The PDF family joins NONE of it: generateWorldBook's State of the Realm chapter is exactly majors/sieges/weary (buildRealmChapter, lines ~434-436); generateCampaignPDF's collectRealmSummary is sieges/weary/standings/pantheon/arcs with realmArcSummary.js containing zero occupation/treaty reads; generateSettlementPDF receives the live campaign world ({ worldState, regionalGraph } at line 187) yet greps zero for occupied/liveWorld/occupation. No review dimension owned PDF export — legibility-product covered live surfaces. (Verified in passing: the 07-30 'Under siege prints undefined' bug IS fixed — the row now reads sg.targetId.)
- **Failure scenario:** A settlement is conquered and occupied (regime changed, UI badge says 'Occupied', Foundry journal says 'held by Ashford'). The DM exports the World Book or the settlement dossier for the table: the realm chapter is silent about the occupation and the dossier prints the DEPOSED power structure as the current one — the most durable player-facing artifact contradicts both the live app and the sim's own truth. Same for a dictated peace whose tribute is visibly draining a granary every tick: no export names the treaty extracting it.
- **Fix sketch:** One canonical living-world projection, three consumers: the projection carrying occupied/besieged/posture already exists (the vm.liveWorld the Foundry journal consumes, and livingWorldSignals' rows). Lift it to a domain-level collector (it currently lives on the components/dossier side), then have realmChapterRows gain 'Occupied' (and optionally 'Treaties in force') sections reading the same collector, the campaign PDF's twin read follow, and generateSettlementPDF join occupation for the dossier's power-structure page with an explicit 'under occupation by X since tick N' line rather than a silent old-regime print. Pin each new section against REAL pipeline data per the collector-not-bytes doctrine already used in realmChapterRows.
- **Finder confidence:** CONFIRMED

### R-11 [HIGH/CONFIRMED] The chooser scores the march against one target but deposits the order for another; the order then waives the margin gate for the target the deliberation never scored
- **Where:** src/domain/worldPulse/settlementStrategy.js:814
- **Finding:** enumerateMoves scores the deploy move off bestTargetId — the MAX-margin hostile target (settlementStrategy.js:586-591) — and applies the causal/treaty factor warFor(bestTargetId) and extractionEV to THAT pair (611-621). But emitMove's deploy branch independently selects the emitted/ordered target as `ctx.hostileTargets.find(t => strengthFor(sId) > strengthFor(t)) || ctx.hostileTargets[0]` (line 814) — the FIRST codepoint-sorted target it merely out-muscles (margin > 0, not > 0.05, and not max). The two diverge whenever a weaker-margin target sorts earlier. warIntent.js's whole contract is 'the war that opens is the war the seat decided on rather than an alphabetical accident' — the emit side reintroduces the alphabetical accident, and the deposited order then waives CONQUEST_MARGIN at the opener (warDeployment.js:1705) for a target whose margin the deliberation never evaluated. Executed proof: world strong(city) + alpha(large town, margin ~0.10) + zeta(hamlet, margin ~0.27). Chooser emits deployTargetId='alpha' ('Ironhold commits to an offensive posture against Alderfen') while the score was justified by zeta's margin; the mechanical opener picks zeta (alpha fails CONQUEST_MARGIN 0.12); with the order stamped the opener opens strong->alpha — a war the margin gate would refuse, opened on the strength of a different target's opportunity. The existing pins never catch this: warIntentJoin.test.js's metadata pin uses a 2-settlement world (targets coincide) and its TARGET pins stamp orders directly via stampWarIntent, bypassing the emit expression.
- **Failure scenario:** Settlement S has hostile targets 'alpha' (margin 0.10) and 'zeta' (margin 0.27). The softmax weight for deploy is computed from zeta's margin, casus, and treaty screen; deploy is sampled; the order names alpha. Next tick the opener pulls alpha to the front, waives the margin pre-filter (0.10 < 0.12 would have refused it), and opens S->alpha. If alpha is a treaty partner and zeta is not, the warFor(zeta) screen was consulted and the pact pair never was — a second, chooser-side treaty bypass.
- **Fix sketch:** Make the scored target and the ordered target one value: return bestTargetId from enumerateMoves alongside the scores (or recompute max-margin identically in emitMove) and emit THAT as deployTargetId/headline target. Add a 3-hostile divergence pin (max-margin target later in codepoint order than a weaker out-muscled one) asserting metadata.deployTargetId === the scored target. The misjudgment stamp and casus receipt lines already read the emitted target, so they align for free once the targets unify.
- **Finder confidence:** CONFIRMED

### R-12 [HIGH/ADJUSTED] Treaty signing beat's summary is engine bookkeeping, not a sentence — on the one beat that ends a war
- **Where:** src/domain/worldPulse/peaceTerms.js:909
- **Finding:** The headline is excellent ('Brightwater dictates the peace with Ashford'), but the reader-facing summary is: 'A treaty is signed — 3 terms at 0.85 of a 1.20 budget, brokered by Marrowgate.' — raw budgetSpent.toFixed(2)/effectiveBudget.toFixed(2) arithmetic verbatim. It names neither WHAT the terms are (tribute? demilitarization? occupation?) nor why the peace was dictated. The beat also carries NO `reasons` array, so the Herald headline's REASON slot ('Driver') renders empty on the arc-closing beat, and NEWS ADDRESS LAW's recorded-reason leg is unmet. The module already owns everything needed: termLabel() (line ~1326) gives world words per term, draftReceipt() gives an authored sentence per term, and treatyFrayingSummary shows the house voice done right ('The peace holds by two terms of five; the tribute frays.'). Note peaceTerms.js is W1-dirty — reconcile with the in-flight war-joins wave, don't fork.
- **Verifier adjustment:** Two clauses in the original are wrong and are corrected here. (a) The Herald does NOT 'render an empty Driver slot': heraldGrammar.js:54-55 documents 'The REASON slot renders nothing when the source holds no reason; it never fabricates a cause', and HeraldHeadline.jsx:81 gates the whole label+value on `{slots.reason && ...}`, so nothing renders — the defect is the missing recorded reason itself (a NEWS ADDRESS LAW / WAR_RULINGS §8 leg unmet on the arc-closing beat), not visible empty chrome. (b) The summary does not print on the Wizard News card body: WizardNewsPanel.jsx:114-117 re-composes the visible body via newsBodyText() and puts the raw summary on a `title=` hover tooltip (line 170). The proven visible leak surface is the Chronicler's Letter (chroniclersLetter.js:389, executed above), plus the chronicle record. Severity stays high on that basis.
- **Failure scenario:** Any war in any campaign ends in a dictated peace -> the trade-door card and Wizard News print 'A treaty is signed — 2 terms at 0.62 of a 1.10 budget.' A DM reading the realm's biggest beat aloud recites budget arithmetic; the terms themselves — the actual content of the peace — appear nowhere in the sentence.
- **Fix sketch:** Compose the summary from the drafted terms' world words: '<victor> dictates the peace with <loser>: <termLabel list, Oxford-joined>, for <years> years<`, brokered by ${mediator.name}` when mediated>.' Carry the terms' draftReceipt() sentences (minus the seam parentheticals) as `reasons`, so the Herald's Driver slot and the cause walk both fill. Keep budget numbers on the treaty ledger record only.
- **Finder confidence:** CONFIRMED

### R-13 [HIGH/CONFIRMED] Treaty war-block (§6) is unreachable at the ONE war opener — a live honored non-aggression pact does not stop the war
- **Where:** src/domain/worldPulse/warDeployment.js:1684
- **Finding:** DESIGN_PEACE_ENGINE.md §6 declares 'an active treaty BLOCKS war between its parties for its duration (breakable, at cost)'. The block is implemented as treatyEnforcement.treatyBlocksWar, consumed only inside warReasons.warReasonFactor (warReasons.js:679) — but evaluateWarLayer step 4, the declared ONE opener, never calls either. It imports only warReasonsFor/aggregateReasons01 (warDeployment.js:80,100), whose casusMult is lift-only (>= 1, line 1704); the target loop (1684-1716) runs hostileTargetsOf -> occupation gate -> margin gate -> classifyFeasibility with no treaty read anywhere. Executed proof (node, real modules): a world with a live honored non_aggression treaty between 'strong' and 'weak' (cold_war edge — exactly the state PEACE_STEP leaves after every negotiated peace, and cold_war is in warIntent's HOSTILE_TYPES), peace gate lit, strong mobilized: treatyBlocksWar=true, warReasonFactor=0, yet evaluateWarLayer opened the war — deployments.strong.targetId='weak', war_front strong->weak minted. No breach event, no treaty_default, no cost of any kind: the pact is silently ignored rather than 'breakable at cost'. The mobilization side is equally blind (pulseKernel.buildWantsWarLookup, line 136-152, is relationship-type-only), so the pact-bound pair keeps ramping to war-ready. Of §6's three enforcement seams, readiness_cap (mobilization.js:471) and occupation_hold (occupation.js:823-833) are real and reachable; war_block is a dead letter at the seam that matters.
- **Failure scenario:** Peace engine lit; A and B sign a treaty containing a non_aggression term (the low-weight 'security' asset drafted in most treaties). PEACE_STEP leaves the pair at cold_war — still a besiege candidate. Both parties re-ramp posture (wantsWarFor is treaty-blind); within a few ticks the victor (never demilitarization-capped) passes the margin gate against the weakened loser, classifyFeasibility permits, and warDeployment opens a fresh siege on its own treaty partner with the pact honored and live — no breach receipt, no grievance, no reputation cost. Every 'Peace of X' that drafts a NAP is structurally worthless.
- **Fix sketch:** Put the block on the opener's own target set: hostileTargetsOf lives in warIntent.js (a leaf warDeployment already imports, so warDeployment's frozen size ceiling is untouched); have it (or a wrapper the opener and the chooser's contextFor both call) drop pairs where treatyBlocksWar(worldState, from, to, tick) holds — treatyEnforcement is dependency-free, so warIntent importing it closes no cycle. Then make deliberate breach a first-class path: a distinct, receipted treaty-break decision (id-carrying news beat, complianceState->defaulted, treaty_default casus against the breaker) rather than a silent bypass. Record the legitimate golden shift for peace-lit worlds.
- **Finder confidence:** CONFIRMED

### R-14 [HIGH/CONFIRMED] WR-2 (disposition) in DESIGN_WAR_RULINGS_ARCHITECTURE is specced greenfield while the tree already carries a learned, war-outcome-fed disposition system — census omission creates a second-truth fork and a flag near-collision
- **Where:** docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md:268
- **Finding:** The §2 substrate census (which claims to be tree-verified) never mentions: disposition.js ('per-settlement aggression DISPOSITION… computeAggressiveness'), dispositionLedger.js ('the ONE ratcheted disposition ledger… worldState.dispositionStats — per settlement (aggressiveness memory)… wins/losses/signed score, saturating ±12'), dispositionDeltas, or the ALREADY-LIT flag warDispositionEnabled:true in full_simulation (simulationRules.js:398 block). warDeployment.js:1135 writes war win/loss deltas that pulseKernel.js:1350 folds into next-tick dispositionStats, and disposition.js already blends govBaseline + NPC personality + win/loss history + deityTemper into war appetite at the candidateBase chokepoint. WR-2 proposes a NEW module dispositionProfile.js, NEW state worldState.dispositions, and a NEW flag 'dispositionEnabled' (one word from warDispositionEnabled) with a 'learned only from outcome events' reader — i.e., a parallel implementation of machinery that exists, un-reconciled. If both run, war appetite double-counts history (existing multiplier × new threshold factor). Note also amendment E's substrate warning ('verify the war history actually records outcomes and not merely occurrences') is answerable affirmatively from this exact machinery — the census that should have answered it omitted the module that answers it.
- **Failure scenario:** The external implementer (the doc claims to be self-contained for zero-context Sol 5.6) builds dispositionProfile.js beside disposition.js; a martial realm's won war raises both the legacy dispositionStats multiplier and the new martial channel; war frequency doubles its intended history sensitivity; the walker/one-writer constitution is violated with no test aimed at the seam because neither doc names the other system.
- **Fix sketch:** Amend §2's census and WR-2's spec before dispatch: WR-2 must either extend dispositionStats into the four-channel shape (one ledger, one writer, migrating the single-channel score) or explicitly subordinate the legacy multiplier (disposition.js consumes the new profile's martial channel and retires its own history term), and rename the flag to avoid the warDispositionEnabled collision; record the choice as a J-WR judgment row.
- **Finder confidence:** CONFIRMED

### R-15 [HIGH/ADJUSTED] War/peace cause receipts print raw 0..1 engine scalars in reader prose (the exact 'pressure01 0.73' class)
- **Where:** src/domain/worldPulse/eventProse.js:191
- **Finding:** The grievance receipt pool (eventProse.js:191-194) — ALL four seeded variants — interpolates raw floats: 'A ledger of grievances stands open: resentment 0.62, memory 0.48.' The exhaustion pool (281-282) does the same: 'The war has worn the town to the bone: exhaustion 0.71; the seat needs peace to survive.' The floats are fed by warReasons.js:481 (`resentment.toFixed(2), memory.toFixed(2)`) and peaceReasons.js:95 (`score.toFixed(2)`), and render verbatim as ReasonRow text on the user-facing WarCausalBrief (src/components/map/WarCausalBrief.jsx:84,94). Because every variant in each pool leaks, seed rotation cannot avoid it. Contrast the neighbouring pools, which are exemplary (revanchism speaks in 'marks in the ledger', coalition_fracture in whole co-belligerent counts). The heraldTranslationFloor guard only covers headline fallbacks and reason-token translation, not digits inside authored receipts — the structural gap that let this class in.
- **Verifier adjustment:** One surface claim narrowed: the compact mount (LiveWarStatus.jsx:110, `compact`) renders ONLY the dramatic-irony line and no receipts (WarCausalBrief.jsx:60-68), so the leak surface is the full brief inside TreatyPanel, not both mounts. Everything else stands as reported.
- **Failure scenario:** A DM opens the war causal brief for any rivalry with live grievance or exhaustion pressure -> under 'Toward war' they read aloud 'resentment 0.62, memory 0.48' — a debug readout in the middle of otherwise in-world prose.
- **Fix sketch:** Replace the interpolated floats with banded clauses per the finite-semantics law ('the resentment runs deep, and the memory is long' / 'the town is worn near to breaking'), banding at the warReasons/peaceReasons call sites so eventProse pools stay number-free; extend the translation-floor test with a no-digit scan over the WAR_RECEIPTS/PEACE_RECEIPTS pools (the demographicsWorldsHand test at line 617 already shows the prose-join no-digit idiom).
- **Finder confidence:** CONFIRMED

### R-16 [MEDIUM/ADJUSTED] A trade-war escalation mints a costless, ageless siege outside the one-opener — no deployment, no drain, no exhaustion, no ceiling, one-army law bypassed
- **Where:** src/domain/worldPulse/tradeWar.js:582
- **Finding:** tradeWar.js:582 mints a confirmed war_front (source 'trade_war_escalation') directly onto the graph when a defeated trade incumbent escalates — a second war-opening path beside warDeployment step 4, contradicting the declared one-opener invariant (warIntent.js header: 'the ONE war opener — warDeployment.evaluateWarLayer step 4'). Executed proof: minted the exact channel, ran evaluateWarLayer 4 ticks — isLiveWarFront(front)=true (the bare-front branch of warFrontReads.js:50-54 reads any non-relationship-tagged confirmed front as a siege), the siege contests every tick, and deployments={}, warExhaustion={}, zero war_drain/army_deployed/war_exhaustion outcomes throughout. Structural consequences, all from code: the besieger holds NO deployment record, so (a) step 5's entire war economy (drain, exhaustion ratchet, conscription, reinforcement cost) skips it — a free war; (b) the attrition loop (1366-1377) requires rec.targetId — the phantom army never degrades; (c) withdrawal only deletes deployments (1419) — the front is retired only if the matchup drops out of the siege band; (d) siegeAge is the max deploymentAge of COMMITTED besiegers (1313-1322) = 0 forever, so the SIEGE_MAX_AGE=60 hard ceiling never fires — a plausible-band stalemate grinds eternally with no homeostasis arc; (e) the one-army law is void: deployments[defeatedId] is empty, so step 4 can open a second, real war elsewhere while this siege stands. It also bypasses the mobilization posture gate (a peace-posture settlement 'answers lost trade with the sword' instantly) and, per finding 1, any treaty. The escalation is receipted (war_pressure beat) and design-intended ('the war layer picks it up next tick'), but the war layer picks it up as a phantom besieger, not as a war.
- **Verifier adjustment:** tradeWar.js:581-591 mints a confirmed war_front directly onto the graph (source 'trade_war_escalation'), a second war-opening path beside the declared ONE opener. Executed: the front reads as a live siege (warFrontReads.js:50-54), the besieger holds no deployment record, and over the run deployments={} and warExhaustion={} throughout — yet at tick 10 it CONQUERED a peer settlement. Real consequences: (a) step 5's whole war economy (drain, exhaustion ratchet, conscription, reinforcement cost) skips it — a free war; (b) the attrition loop (1366-1377) requires rec.targetId, so the phantom army never degrades; (c) the mobilization POSTURE gate is bypassed — the executed escalator was at warPosture 'peace'; (d) the one-army law (warDeployment.js:1646 `if (deployments[fromId]) continue;`) is void, so the escalator can simultaneously open a real siege elsewhere; (e) siegeAge stays 0, so SIEGE_MAX_AGE and the M5 starvation clock never engage for this besieger. NOT true: the front does NOT persist forever — an infeasible matchup is retired on the first tick by the stale-front path at warDeployment.js:1469-1478, and a feasible one terminates by conquest. Re-find of worldpulse-war-military-9 (2026-07-15 review, verdict pending, never dispositioned).
- **Failure scenario:** warLayerEnabled world (no peace flag needed): incumbent A loses a trade contest, strength clears ESCALATION_CONFIDENCE, the escalation roll fires. A confirmed front A->B stands. B suffers full siege mechanics (pressure reads, homeBesieged strategy distortion, can-not-deploy-while-besieged, siege rolls that can conquer it outright — pickOccupier works fine with a record-less besieger) while A pays nothing, fields no army, never tires, never withdraws from a plausible matchup, never hits the 60-tick ceiling, and can simultaneously deploy its actual army against a third settlement.
- **Fix sketch:** Route the escalation through the opener instead of around it: have tradeWar deposit a war-intent order (warIntent.stampWarIntent — the exact seam built for 'a decision reaches the opener') plus a hostility escalation on the edge, and let step 4 open the war through its own gates next tick (posture, one-army, feasibility, and — after finding 1's fix — treaty). If instant escalation is the intended fiction, the alternative is adoption: step 3 seeds a deployment record for any ownerless war-layer-tagged front it resolves, so the war acquires its economics, aging, and ceiling. Either way, add a walker/pin asserting no confirmed non-relationship war_front exists without a matching deployment after a pulse.
- **Finder confidence:** CONFIRMED

### R-17 [MEDIUM/ADJUSTED] All four directive-program flags are declared FALSE in full_simulation, contradicting the recorded build protocol; the required lighting batch exists only as a code comment
- **Where:** src/domain/worldPulse/simulationRules.js:442
- **Finding:** The owner restart order recorded in DESIGN_REALM_DIRECTIVES Progress says waves ship 'dark flags lit in full_simulation (the certification vehicle) with budgets proven lit'. Built reality: npcConsequencesEnabled:false (:442), routeLifecycleEnabled:false (:456), magicEconomyEnabled:false (:475), informationBrokeragesEnabled:false (:490) in the full_simulation spread, while the older war sub-flags (warForageEnabled, warDispositionEnabled, etc.) ARE lit there (:398). The code comment says lighting 'belongs at the single declared golden boundary (the ONE REGEN batch discipline)… Flip this one value there' — a hidden prerequisite step for the owner's 'REDO ALL SOAKS… everything lit' that is recorded nowhere except this comment: not in Progress, not in FABLE_VALIDATION_QUEUE.md, not in the handoff. The deviation from the restart order is itself an unrecorded Fable-chair-class judgment.
- **Verifier adjustment:** The four flags ARE false in the full_simulation spread (src/domain/worldPulse/simulationRules.js: npcConsequencesEnabled:false, routeLifecycleEnabled:false, magicEconomyEnabled:false, informationBrokeragesEnabled:false) while the eight war sub-flags including warDispositionEnabled are lit in the same block — that half is confirmed. But the claim that the deviation is 'recorded nowhere except this comment' is FALSE: DESIGN_REALM_DIRECTIVES Progress records it twice ('Gate exit 0; NO golden moved (every slice dark)' for batch 4; 'Gate exit 0; no golden moved' for batch 5) and FABLE_VALIDATION_QUEUE.md's I1 golden-re-record row states it in the re-examination column ('all remaining ship dark'). The surviving, narrower defect: no Progress entry, queue row, or handoff line ASSIGNS the lighting to a batch or names it as a prerequisite of the owner's 'REDO ALL SOAKS … everything lit' order, and no row records the per-wave deviation from the restart order's 'dark flags lit in full_simulation (the certification vehicle) with budgets proven lit' as a disposition — which the queue protocol's 'Deferral/skip/block dispositions with product consequences' clause requires.
- **Failure scenario:** A successor executes the owner's soak-redo order against full_simulation as written; H, I(effects), J and K all grade DORMANT_BY_CONFIG; the redo whose entire rationale was 'the waves change the simulation' measures none of them — or worse, someone lights the flags ad hoc mid-soak without the golden-boundary batch and shifts goldens with no signed re-record.
- **Fix sketch:** Record the lighting batch as a first-class scheduled step (a Progress entry plus a FABLE_VALIDATION_QUEUE row): one ONE-REGEN-style commit that flips the four values in full_simulation, re-records the disclosed goldens with blast-radius proof (the I1 precedent), and proves budgets lit — sequenced explicitly BEFORE the soak redo in the successor's resume order. [Superseded in one detail 2026-08-02 (self-audit): the ruled batch is FIVE flags — `demographicsEnabled` joins the four (FABLE_VALIDATION_QUEUE's corrected lighting-batch chair row; DESIGN_REALM_DIRECTIVES Progress agrees); a soak with demographics dark would re-prove the pre-cure engine.]
- **Finder confidence:** CONFIRMED

### R-18 [MEDIUM/ADJUSTED] Batch-6 wave acceptances (I3+I4, K2, K3, K4) have no rows in FABLE_VALIDATION_QUEUE — a breach of the binding continuity protocol, and it swallowed two real deviation judgments
- **Where:** docs/FABLE_VALIDATION_QUEUE.md:43
- **Finding:** The protocol is explicit: 'Wave/slice ACCEPTANCE judgments (reading implementer reports, deciding a wave is done)' get a row, as do 'Deferral/skip/block dispositions with product consequences'. The queue carries rows for batches 2–5 and wave P, then nothing for batch 6 (I3+I4 @ 007e0dcf, K2 @ 230e0f22, K3 @ e2614d4f, K4 @ 7b6639f9 — all Opus-era acceptances per the Progress blockquote). Two row-worthy judgments are thereby unrecorded anywhere the re-validator is told to look: (a) K4's withheld change — the commit message states 'The one change that could have shifted a golden — a reagent chain merged into live supply needs — was measured and deliberately NOT written; the accrual ships dark'; (b) K3's stock substitution — magicBufferApply.js:23 records 'REAGENTS ARE DELIBERATELY ABSENT… §5 names reagent [stocks] and none exist in the tree today' and the buffer draws storesMonths (granaries) instead of the design's 'treasury, reagents, reserves'. Also unanswered on the record: the I1 Rookery-deferral row's own question ('Confirm I3 actually closes it') — I3 landed with no row stating how it closed (see separate finding).
- **Verifier adjustment:** The missing-rows fact is confirmed: `grep -n 'I3|I4|K2|K3|K4|batch 6'` over docs/FABLE_VALIDATION_QUEUE.md (76 lines) returns exactly one hit — the I1 Rookery-deferral row at :55 — and no acceptance row exists for 007e0dcf, 230e0f22, e2614d4f or 7b6639f9, while rows for batches 2-5 and all of wave P are present. But sub-claim (a) is wrong: K4's withheld change IS on the record the re-validation protocol tells Fable to read — DESIGN_REALM_DIRECTIVES Progress batch-6 entry states 'K4 @ 7b6639f9 (substitution + reagents; the golden-shifting change deliberately NOT written)', and protocol step 1 is 'Read this queue oldest-first ALONGSIDE DESIGN_REALM_DIRECTIVES.md Progress'. The genuinely unrecorded judgment is (b), K3's stock substitution, which appears in no queue row and in no Progress line — Progress says only 'K3 @ e2614d4f (the buffer; a rounding overdraw caught by the conservation pin)'.
- **Failure scenario:** The Fable-capable re-validator works the queue oldest-first as instructed, finds no batch-6 rows, and never re-derives the K3 stock substitution, the K4 dark accrual, or the I3/I4 service-layer judgments; a later tuning or lighting decision is made assuming §5/§7 of DESIGN_MAGIC_ECONOMY were built as written.
- **Fix sketch:** Backfill four dated rows (evidence = the four commit hashes and their test suites, plus the two code-recorded deviations quoted verbatim) marked as retro-recorded with the recording gap acknowledged; add the K4 dark-accrual lighting decision and the K3 stock-model question to each row's 'what Fable should re-examine' column.
- **Finder confidence:** CONFIRMED
- **CORRECTED 2026-08-02 (self-audit):** sub-claim (b)'s buffer-draw substance is REFUTED within this same register — R-41's verifier adjudication: magicBufferApply.js:14-21 names TWO stocks and both are drawn (granary months AND the ward-charge reserve; reserves ARE built), and the reagent absence is an explicitly recorded deferral (:23-27), which the review rules exclude. What survives of (b) is the missing acceptance ROW itself plus the two-ratified-docs treasury contradiction (DESIGN_MAGIC_ECONOMY §5 vs amendment S) — see R-41 for the adjudicated fact pattern; this item and R-41 no longer disagree. The missing rows themselves were retro-validated by the queue's batch-6 chair row (2026-08-01), which now consumes this corrected reading.

### R-19 [MEDIUM/ADJUSTED] Commissioned plants are charged and narrated but never planted: the PLANT_WIRING into processLies does not exist, yet the producer mints paid plant events
- **Where:** src/domain/worldPulse/brokerageServicesRules.js:346-380
- **Finding:** brokerageServicesPlant.js honestly records (PLANT_WIRING, lines 64-87) that the one edit folding commissionPlants() into informationStatecraft.processLies was 'reported rather than made' because that file belonged to another session. Grep confirms informationStatecraft.js contains zero references to commissionPlant/commissionedPlants, and nothing anywhere consumes candidate metadata.plant. But brokerageServicesRules.js was NOT held back to match: evaluateBrokerageServiceRules mints live brokerage_plant candidates that (a) apply a real factionPatch charge through the payload-keyed applier (applyWorldPulse.js:1132), and (b) print 'Somebody paid the market to put a telling about X into the court of Y. It is not true. Nobody there knows that yet.' — while the DisinfoRecord and belief override in metadata.plant go nowhere. The lie never enters spatialLedgers.disinfo, no audience belief moves, and the constitutionally required contradict → expose → blowback triple (design §6, Law 2's 'miscalibration is itself attributable, priced, and expose-able') can never fire for a commissioned plant. The patron pays up to 0.45 of its political capital for a world-visible no-op.
- **Verifier adjustment:** Split the finding. The MISSING WIRING half is a properly recorded deferral and should not be re-reported as a defect: brokerageServicesPlant.js:64-87 states the exact edit, names why it was not made, and enumerates four safety properties; tests/domain/brokeragePlant.test.js hands a commissioned record to the REAL processLies so the composition is executed rather than asserted. What genuinely survives is the narrower asymmetry the reviewer identifies: the PRODUCER was not held back to match the deferral. evaluateBrokerageServiceRules (brokerageServicesRules.js:346-380) mints live brokerage_plant candidates that carry a real factionPatch charge and a chronicle summary asserting a lie was planted, while metadata.plant has no consumer anywhere. Nothing in the tree documents that decision.
- **Failure scenario:** Flag lit; rotation lands on brokerage_plant for a settlement hosting a Whisper Market; candidate applies; patron momentum drops by cost01 and the Herald records a planted lie; no court's belief changes, no exposure or blowback is ever possible; a soak or certification pass reading the knowledge lane counts plant events as alive while the mechanic behind them is hollow, and the receipt narrative ('Nobody there knows that yet') is false — nobody anywhere knows anything, because nothing was planted.
- **Fix sketch:** Either land the four-line PLANT_WIRING in informationStatecraft.processLies (thread commissionedPlants from advanceInformationStatecraft, keys plant:* disjoint from lie:*, per the module's own safety argument — all four properties are already pinned in brokeragePlant.test.js against the real mover), or gate the brokerage_plant arm of evaluateBrokerageServiceRules off until the wiring exists (drop 'brokerage_plant' from the rotation the same way an absent market drops it), so the world cannot charge for and narrate an act that has no effect. The first option is the design-complete cure; the second is the honest interim.
- **Finder confidence:** CONFIRMED

### R-20 [MEDIUM/ADJUSTED] Garrison-floor RAISE changes a persisted grade with zero events — Law 3 silent drift, measured
- **Where:** src/domain/worldPulse/routeNetworkDecay.js:651
- **Finding:** sweepRouteDecay applies a grade step for verdict 'garrison_floor' (line 651: `verdict.verdict === 'garrison_floor' || verdict.steps`), and withEdgeGrade genuinely moves the grade (track→road) — but gradeStepHeraldItem (line 569) returns null for any verdict with steps=false, and 'garrison_floor' is not a step word. Executed proof: an edge at 'track' with strategicNeed 'garrison' swept once — grade before 'track', after 'road', stepped=1, changed=true, news.length=0. Law 3 ('EVENTS, NEVER SILENT DRIFT: every network change is a charter or abandonment EVENT') and §7 ('Every step is an event with receipts') are both violated: the realm map's road weight changes with no Herald beat and no receipt. The module's own long comment (lines 508-527) documents curing the inverse leak (news without a step); this is the remaining leak in the other direction (a step without news).
- **Verifier adjustment:** Confirmed exactly as reported, and WORSE than reported in one respect the finding understates: the raise is not limited to one rung. Because withEdgeGrade writes the floor grade directly rather than stepping, an edge at 'hidden' with a live garrison need jumps TWO rungs to 'road' in a single silent sweep. Executed: before='hidden', after='road', verdict='garrison_floor', steps=false, stepped=1, changed=true, news.length=0. The reader-visible consequence is sharper than a maintenance upgrade: the Herald has already mourned that way ('The road between A and B is abandoned', ABANDONMENT_CANDIDATE_TYPE at routeNetworkDecay.js:577-582), and the road then comes back with no beat at all — an announced abandonment silently reversed. Also worth recording for the chair: the existing regression pin covers only the HOLD case. tests/domain/routeNetworkConsumersStrategic.test.js:147-165 asserts news.length===0 for a garrisoned edge ALREADY at 'road' (stepped===0 at :173-177), which is correct; the RAISE arm is unpinned in both directions, so the gap is not merely unfixed, it is unguarded. Severity medium stands (dark, unwired lane, but a landed Law 3 violation).
- **Failure scenario:** A war starts; the war layer writes 'garrison' onto a track. The next decay sweep silently raises it to road. The DM's map shows a heavier road than last week with no news item, no docket entry, and no recorded reason — indistinguishable from a rendering bug, and unauditable after the war ends and the need key is dropped.
- **Fix sketch:** Mint a dedicated event for the raise: either extend gradeStepHeraldItem to accept the garrison_floor verdict when its `to` differs from `from` (headline in the military voice — 'the Crown's engineers keep the Eastpass road'), or add a distinct route_garrisoned candidateType so the Herald can tell maintenance from trade. Pin: a garrison raise mints exactly one news item carrying both settlement ids and the war-layer reason; negative control: a garrison_floor HOLD (edge already at floor) mints none.
- **Finder confidence:** CONFIRMED

### R-21 [MEDIUM/ADJUSTED] March time counts as siege time — deploymentAge accrues on the road, pre-charging the starvation clock and the hard siege ceiling
- **Where:** src/domain/worldPulse/warDeployment.js:1213
- **Finding:** Step 0 ages EVERY committed deployment unconditionally (warDeployment.js:1213-1234 — no atWalls check; the JOIN-2 gate at line 1304 only filters the besieger set). Executed proof: a deployment mid-march (transit record departTick 0, arrivalTick 20) at tick 10 shows deploymentAge=11 and no siege activity. On arrival, the first at-walls verdict runs with siegeAge = the whole march (siegeAge = max deploymentAge of at-walls besiegers, 1313-1322). Consequences: (a) M5 spatial path — timeFactor = clamp01(siegeAge/STARVE_FULL_TICKS=12) (line 983); any march >= 12 weeks (MAX_MARCH_WEEKS is 52) arrives with FULL starvation-time credit, so 'a season of total interdiction fully starves the town' becomes 'the town reads as starved the first tick the army arrives' whenever interdiction is high; (b) the SIEGE_MAX_AGE=60 ceiling (line 915) budget is consumed by the road — a 52-week march leaves 8 ticks of actual siege before deterministic auto-resolution, and the auto-resolve direction (coalition edge) then settles a siege that barely happened; (c) the age-scaled war_drain bump charges the home for the march (arguably intended — the army IS away — but it compounds with (a) and (b)). The in-file comment (1105-1107) reasons only about termination ('strictly below SIEGE_MAX_AGE'), not about the clock's semantics.
- **Verifier adjustment:** warDeployment.js:1213-1234 ages EVERY committed deployment unconditionally (no atWalls check; the JOIN-2 gate at 1304 only filters the besieger set), so an army in transit accrues deploymentAge on the road. Because siegeAge is the max deploymentAge of at-walls besiegers (1312-1322), the first at-walls verdict on the M5 spatial path reads timeFactor = clamp01(siegeAge/STARVE_FULL_TICKS) (line 983) with the whole march already credited. Executed: a 14-tick march arrives at deploymentAge 14 having taken zero siege attrition, and at interdiction 0.9 that verdict FALLS on its first at-walls tick where siegeAge 0 or 1 does not — 'a season of total interdiction fully starves the town' collapses to 'the town reads as starved the moment the army arrives' for any march ≥ STARVE_FULL_TICKS (12). The SIEGE_MAX_AGE leg is NOT a defect: the march-consumes-the-ceiling interaction is explicitly reasoned and driven-pinned (armyTransit.js:516-520; tests/domain/siegeArrivalGate.test.js:313-341, comment at 316-317).
- **Failure scenario:** Spatial canon lit. An army marches 14 weeks to a supply-interdicted town. First tick at the walls: timeFactor=1.0, logOdds = 6*interdiction - relief - 3 - will; at high interdiction pFall ~0.95 — the town falls 'starved out by time' on day one of a siege that has not yet starved anyone. Same seed aspatially: a multi-tick contest.
- **Fix sketch:** Split the two clocks: stamp the deployment with an at-walls arrival tick (first tick atWalls(id,target) is true — the record already flows through step 0 where the transit gate is available), and derive siegeAge from that stamp, keeping deploymentAge as total-time-away for war_drain. Dark transit ledger => arrival stamp = sinceTick => byte-identical aspatially. Pin: a 20-week-march army's first siege tick must show timeFactor 0.
- **Finder confidence:** CONFIRMED

### R-22 [MEDIUM/UNVERIFIED] Migration 191's thrice-repeated in-place-amendment license has expired — the file is committed AND pushed while its header still claims UNCOMMITTED/UNDEPLOYED
- **Where:** supabase/migrations/191_surveyor_probe_tier.sql:27
- **Finding:** The header justifies three successive in-place amendments (waves L-7a, L-WIRE, integration-skeptic) with the same argument: 'this migration is UNCOMMITTED and UNDEPLOYED... the argument expires the moment this file is committed and pushed.' Verified: 191 is committed in ada1252d ('THE AI LAYER') and pushed — `git branch -r --contains ada1252d` returns origin/claude/composite-r4. The argument has fully expired, yet the file still teaches it, three times, as live precedent. The 122->193 train is queued for the owner's deploy (per the pause handoff). No review dimension owned migration-train coherence 189-193; the 71-undeployed-migs finding from 07-30 is about deployment lag, not this.
- **Failure scenario:** A wave-4 author needing a probe-schema tweak reads 191's own header, sees the amend-in-place precedent asserted as currently valid, and retro-edits the pushed file. After the train deploys, the next edit of the same shape recreates the exact M38 hazard the header cites: repo replay-from-scratch diverges from prod's applied bytes.
- **Fix sketch:** A one-line dated postscript in 191's header — 'COMMITTED @ ada1252d and PUSHED: the amend-in-place license above is EXPIRED; every further change is a forward migration' — plus, structurally, a walker rule the repo already has the idiom for: a migration file whose header contains the phrase 'AMENDED IN PLACE' must be untracked at HEAD, else red (the E-A untracked-mutation-target rule inverted). That removes the class rather than this instance.
- **Finder confidence:** CONFIRMED

### R-23 [MEDIUM/ADJUSTED] Military charter needs re-propose and re-announce EVERY tick; CHARTER_COOLDOWN_TICKS is a dead band nothing reads
- **Where:** src/domain/worldPulse/routeNetworkCharter.js:457
- **Finding:** evaluateMilitaryCharter (lines 457-497) has no cadence gate and no cooldown — corridors get gate 0 (EVAL_CADENCE_TICKS 13) but strategic-need rows are re-evaluated every sweep, and a persisting garrison need on an unserved pair returns verdict 'charter' every time. charterEmissions then mints BOTH a docket proposal AND a Herald item per verdict, with tick-qualified ids so each pulse's pair is distinct. Executed proof: 10 consecutive ticks with one persisting garrison need produced 10 distinct proposals and 10 Herald items ('A road is chartered between Aldern and Bexley' weekly). Meanwhile ROUTE_CHARTER_TUNING.CHARTER_COOLDOWN_TICKS (line 120), documented as 'the anti-flap dwell a pair serves after ANY lifecycle decision about it, in either direction', is read by nothing in src or tests — the documented anti-flap protection does not exist. The docket admission cap rations the proposals, but the Herald items are unrationed, and §6 says the Herald announces ON ACCEPTANCE, not at verdict time — charterEmissions (routeNetworkCharterEvents.js:237-249) structurally couples the news to the evaluation, so any integration wiring that calls it as shaped will announce roads that were never accepted, weekly.
- **Verifier adjustment:** Both mechanical claims confirmed; the failure scenario as written is wrong and must be corrected before the chair acts on it. The finding says 'one standing deployment against an unreachable-by-road target' — an unreachable target returns verdict 'too_far', not 'charter' (evaluateMilitaryCharter:478-481 applies EXPEDITION_WORTH_WEEKS=20, and expeditionWeeks returns MAX_HOP_WEEKS for an unreachable pair). The real reachable scenario is narrower but genuine: a REACHABLE unserved pair carrying a persisting garrison band whose docket proposal is never admitted. Every sweep re-mints a distinct tick-qualified proposal id, so the docket sees a fresh row per pulse rather than one waiting row, and the flood ends only when a charter is finally applied (after which edgeIdForPair makes it 'already_served'). The Herald half of the claim ('any integration wiring that calls it as shaped will announce roads that were never accepted') is reasoning about a wiring slice that does not exist and should be carried as a shape risk, not as a defect. The confirmed defect is two-part: (a) the military arm has no cadence or cooldown gate while the corridor arm has one (evaluateRouteCharters:565 skips 'cadence' and advances lastCharterEval; the strategicNeeds loop at :570-573 has no cursor at all), and (b) CHARTER_COOLDOWN_TICKS is dead.
- **Failure scenario:** The integration wave wires evaluateRouteCharters + charterEmissions into the pulse as the API shape invites. A realm with one standing deployment against an unreachable-by-road target emits ~52 'road is chartered' Herald beats per year for a road that never materializes, drowning the newspaper and violating the E0 pacing governor's intent.
- **Fix sketch:** Split charterEmissions in two: proposal outcomes at verdict time (docket-rationed, as now), Herald items only from applyRouteCharter's changed=true return (announce what materialized). Wire CHARTER_COOLDOWN_TICKS as a real dwell: stamp the pair's last lifecycle decision (the corridor's lastCharterEval already exists; military needs want an equivalent keyed cursor, e.g. a lastMilitaryEval map or a verdict-memo on the need's corridor id) and refuse re-evaluation inside the dwell. Pin: one persisting need over 60 ticks yields at most ceil(60/cooldown) proposals and zero Herald items before acceptance.
- **Finder confidence:** CONFIRMED

### R-24 [MEDIUM/CONFIRMED] Momentum climb-down summary and reasons expose the commitment/cliff model with raw multipliers
- **Where:** src/domain/worldPulse/momentum.js:1166
- **Finding:** climbDownNews' summary (1166-1167) prints '(commitment 1.42× its cliff)' and its reasons (1169-1173) print 'The commitment stock (1.42× the reconsideration cliff) put Brightwater past the cliff' and '(relief 40%)'. 'Commitment stock' and 'reconsideration cliff' are tuning-doc vocabulary, not world words. The surrounding prose is the house voice at its best ('the price of the proud hold come due', 'climbs down from its war — with honour intact'); only the parenthetical arithmetic breaks register.
- **Failure scenario:** Any war reversal past the cliff -> the war-door card's summary a DM reads aloud contains 'commitment 1.42× its cliff', and the reason line names an internal model ('the reconsideration cliff') as though it were a place in the world.
- **Fix sketch:** Translate depth to a banded clause ('held well past the point of easy return' already IS that translation — the parenthetical merely repeats it as math, so deleting it loses nothing); express the off-ramp relief qualitatively ('the mediation off-ramp let the crown call it a victory, and the price fell accordingly').
- **Finder confidence:** CONFIRMED

### R-25 [MEDIUM/ADJUSTED] Plan-committed satellite site is never re-validated at the mint: the §11b two-parents-one-cell defect is re-openable through the plan lane's proposal-to-completion window
- **Where:** src/domain/worldPulse/demographicsPlans.js:570-579 and 481-489, src/domain/worldPulse/settlementLifecycleKernel.js:443 (siteOverride verbatim)
- **Finding:** §5b/§11b: the occupancy census went realm-wide precisely because "two adjacent parents could plant on one cell, each blind to the other." The plan lane checks the realm-wide census (occupiedCellsOf reads spatialLedgers.satellites — MINTED steadings only) once, at PROPOSAL, when chooseLegalSite fixes the site (deliberately, per §5c no-reroll). A satellite plan then runs 26+ ticks. During that window a second parent's plan can legally pick the same cell (the first plan's site lives only inside spatialLedgers.demographicPlans, which the census never reads), and any ORGANIC founding can take it too (mintSteading's own occupied list at kernel line 446 is parent-local — wave E's known narrowness, uncorrected). At completion the intent's stored site is used verbatim (siteOverride, documented as no-reroll) with no occupancy re-check; the only mid-plan ground check is 'ground_lost' on FULL saturation, which does not fire when just the committed cell is taken. Two steadings can therefore mint on one cell.
- **Verifier adjustment:** The defect mechanism is real and now executed rather than reasoned: siteOverride bypasses EVERY occupancy and legality re-check at the mint. But the stated two-parents-one-cell scenario is impossible — orbitAnnulus refuses any cell not owned by the proposing parent, and territory is a partition, so two parents can never offer the same cell out of the frozen digest. The reachable collision is SAME-parent: the plan fixes cell X at proposal, the parent's own spontaneous seeding integrator mints an organic steading on X during the 26+ tick window (chooseSteadingSite's occupied list is built from minted steadings and cannot see a plan-committed cell), and the plan then mints a second steading on X verbatim. The broader and likelier half is MIN_SEPARATION staleness: any settlement founded near the committed cell during the window makes that site illegal under §5b, and nothing re-checks — the only mid-plan ground arm is 'ground_lost' on FULL annulus saturation.
- **Failure scenario:** Parents A and B, adjacent territories sharing a frontier cell X: A proposes tick 100 (site X), B proposes tick 110 (census still shows X empty), A completes tick 126, B completes tick 136 → two steadings occupy cell X; occupiedCellsOf thereafter counts X once, topography/dossier surfaces render two settlements on one ground, and the §5b MIN_SEPARATION law is violated at distance zero by the very lane built to enforce it.
- **Fix sketch:** Keep §5c's commitment semantics but make the commitment RESERVE the ground: include pending plan sites in occupiedCellsOf's census (read plan.site cells from the demographicPlans ledger alongside minted steadings), and add a cheap completion-time check — if the stored cell is now occupied, close the plan 'ground_lost' instead of emitting the intent; give the organic mint the same realm-wide occupied set the plan lane already computes so wave E's parent-local blindness closes in the one shared mint rather than in two callers.
- **Finder confidence:** PLAUSIBLE

### R-26 [MEDIUM/ADJUSTED] Ports totality is genesis-only: the §8 charter-eval re-check is unimplemented and decay can permanently strip a port's water edge
- **Where:** src/domain/worldPulse/routeNetworkGenesis.js:433
- **Finding:** Design §8: 'the PORTS TOTALITY invariant runs at network genesis and every charter eval'. At genesis it is genuinely structural (water edges minted unconditionally from ports pairs, a subset of candidates.all — good). But portsMissingWater, the checker built for the re-ask, is called by NOTHING in src (grep: only tests/domain/routeNetworkGenesis.test.js) — evaluateRouteCharters never asks it, despite the genesis header's own claim ('J3 re-asks it every charter eval, per §8', routeNetworkGenesis.js:341-343) — a comment-vs-code drift. And evaluateEdgeDecay has no port/water exemption: a port's only water edge under prolonged silence with a failing objective demotes road→track→hidden like any land road. Once hidden, the black-hole finding above applies — the pair can never re-charter (already_served) and never promote (no track rung), so the owner-law invariant is violated permanently, with no repair pass anywhere.
- **Verifier adjustment:** Confirmed, and the finding stops one step short of the sharper defect. portsMissingWater is not merely uncalled — it is GRADE-BLIND, so wiring it exactly as the genesis comment promises would still not detect this failure. routeNetworkGenesis.js:440-441 reads `if (edge.mode === 'water') { owed.delete(edge.a); owed.delete(edge.b); }` with no grade test, so a port whose only water edge has decayed to 'hidden' still reads as satisfying the invariant. The remediation is therefore two changes, not one: call the checker from evaluateRouteCharters AND make it require a non-hidden water edge (or exempt water/port edges from the demotion arm outright). Everything else in the finding stands: the comment-vs-code drift at routeNetworkGenesis.js:341-343 ('J3 re-asks it every charter eval, per §8') is real, and evaluateEdgeDecay has no mode or port branch anywhere in routeNetworkDecay.js:435-538.
- **Failure scenario:** A port's trade collapses for two years (war interdiction elsewhere); its single water edge decays to hidden. Trade returns; the invariant 'every port settlement carries water routes where a navigable counterpart exists — an invariant, not a probability' is now false for the rest of the world's life, and nothing ever detects or repairs it.
- **Fix sketch:** Wire portsMissingWater into evaluateRouteCharters as §8 states: at each sweep, any port it names gets its totality pair re-evaluated (or auto-re-minted at PORT_TOTALITY_GRADE with flavor 'genesis' and a Herald beat, matching the genesis semantics). Alternatively/additionally give water edges of totality pairs a floor at 'track' in evaluateEdgeDecay, mirroring the garrison floor's shape. Pin: demote a port's only water edge to hidden by force, run one charter sweep, assert the port carries an open water edge again and exactly one event was minted; negative control: a non-port pair stays hidden.
- **Finder confidence:** CONFIRMED

### R-27 [MEDIUM/ADJUSTED] Raw FNV-1a rolls without avalanche are tick-correlated — per-tick gates scan linearly instead of drawing independently
- **Where:** src/domain/worldPulse/npcCirculation.js:138
- **Finding:** circulationRoll01 (npcCirculation.js:138-140) returns fnv1a32(key)/2^32 with NO avalanche finalizer, and its keys end in the tick: rehost gate (line 171, ['npcfate:rehost', wnpcId, tick]), sibling roll (line 373), turncoat roll (line 413), plus npcCirculationBelief.js:105-106 (BELIEF_REINFORCE_SHARE gate keyed on elapsedTicks). Executed measurement: consecutive-tick rolls differ by exactly +0.0039 (the last-digit delta times the FNV prime folded down), producing a linear scan of the unit interval at ~0.39%/tick; over 3000 ticks a fixed 0.10 threshold saw a maximum run of 30 consecutive hits (p ~ 1e-33 under independence). The repo's own src/domain/region/contestMath.js:36-42 declares the fmix32 avalanche 'load-bearing: without it a single-character (tick) change barely moves the high bits and a symmetric contest resolves the same way for many ticks running' — these five Opus-era files copied the FNV core and dropped exactly that finalizer. Same idiom, lower exposure (per-event keys, drawn once): npcReplacement.js:114, npcResidency.js:96, npcVerdictTable.js:206. Same-seed replay determinism is NOT broken — this is distribution degeneracy in seeded engine code, the class wave-E's single-seed-vacuity law exists to catch.
- **Verifier adjustment:** The mechanism is CONFIRMED and measurably worse than reported; the reachability is narrower than reported. Corrected claim: circulationRoll01 (src/domain/worldPulse/npcCirculation.js:138-140), rumourReinforcesAt (src/domain/worldPulse/npcCirculationBelief.js:104-106), replacementRoll01 (npcReplacement.js:114), residencyRoll01 (npcResidency.js:96) and verdictRoll01 (npcVerdictTable.js:206) all return fnv1a32(key)/2^32 with no fmix32 finalizer, and the tick/elapsedTicks sits in the key. Because the FNV prime multiply is the LAST operation, a one-digit tick change moves the output by exactly ±0x01000193/2^32 = ±0.003906, so the roll performs a monotone ramp inside each decade of the tick counter and jumps only at a decade boundary. This is a latent-not-live defect: not only is npcConsequencesEnabled false (src/domain/worldPulse/simulationRules.js:442), but attemptRehost / attemptsRehost / turncoatDestination / rumourReinforcesAt have NO src caller at all — grep over src returns only npcCirculation.js's own internal use plus tests, and src/domain/certification/subsystemRowsPeople.js:478 states the circulation lane 'has no caller' and mints no pulse candidates. So the failure scenario is not 'the owner lights the flag' but 'the wiring wave lands' — at which point it ships silently, because every existing pin is structurally blind to it (tests/domain/npcCirculation.test.js:405-415 varies wnpcId at a FIXED elapsedTicks, i.e. it measures the cross-population marginal, never the per-person autocorrelation the defect lives in).
- **Failure scenario:** Owner lights npcConsequencesEnabled (currently false, simulationRules.js:442). A pooled roamer's rehost check 'roll < pressure' re-rolls every tick, but the roll drifts +0.0039/tick instead of redrawing: an NPC whose roll sits just above the slowly-rising pressure curve is refused for dozens-to-hundreds of consecutive ticks, then rehosts at a quasi-deterministic pool age; turncoat and belief-reinforcement gates cluster identically. Waiting-time distributions collapse from geometric to near-deterministic ramps — visible as eerily synchronized NPC fates in any long soak, invisible to double-pass determinism scans and single-seed pins.
- **Fix sketch:** Route all five files' roll helpers through the avalanched contestMath.hash01 (or append the identical fmix32 finalizer h^=h>>>16; h=Math.imul(h,0x85ebca6b); h^=h>>>13; h=Math.imul(h,0xc2b2ae35); h^=h>>>16 before the >>>0) and add a lint-tree pin that any 'fnv1a32(...)/0x100000000' roll site in src/domain must use the avalanched form — the polarity twin of the existing transcendental ratchet. Do it BEFORE the H lane lights: the lane is dark today, so the stream change costs nothing under THE PROMISE; after activation it becomes an owner-gated same-seed shift.
- **Finder confidence:** CONFIRMED

### R-28 [MEDIUM/ADJUSTED] Refused-exodus headline asserts a departure that never happened (glance contradicts sentence)
- **Where:** src/domain/worldPulse/demographicsHerald.js:257
- **Finding:** Executed proof (node, pure function): for a fully-refused migration column (departures 0, unplaced 9) demographicNewsEntries emits headline 'a dozen or so left Ashford' with summary 'a dozen or so would have left Ashford, and the realm had nowhere to put them.' The kernel's own receipt comment (demographicsMigration.js:563) states 'The people stayed, so no population moved.' The headline is the glance under the LEGIBILITY LAW, and it states a false fact the summary then retracts. The test suite pins the refused SUMMARY (tests/domain/demographicsWorldsHand.test.js:653) but never the refused headline, so the contradiction is unguarded. Secondary: both demographic headlines begin lowercase ('several hundred left Ashford') — the only lowercase-leading headlines in the feed, since every other producer opens with a settlement name.
- **Verifier adjustment:** The secondary claim is REFUTED and should be dropped: it is not true that 'both demographic headlines begin lowercase'. The hunger headline at demographicsHerald.js:192 is `Grain ran short in ${name}` — capitalized. Only the exodus headline (:257) leads with a lowercase quantity band. The primary contradiction stands exactly as reported.
- **Failure scenario:** A saturated realm refuses a whole column -> the trade door and rumor net headline 'a dozen or so left Ashford' when nobody left; a DM skimming headlines narrates an exodus that did not occur.
- **Fix sketch:** Branch the headline with the summary: departures > 0 keeps the current line; the refused-only case gets its own glance ('Ashford's people found no road out' or the kernel's own 'would leave' phrasing). Capitalize via a leading clause or sentence-case the quantity phrase. Pin the refused headline in demographicsWorldsHand.
- **Finder confidence:** CONFIRMED

### R-29 [MEDIUM/ADJUSTED] Rookery/Whisper-Market criminal-power precondition is enforced nowhere at generation — I3 closed the I1 deferral as an effects/patronage gate only, and the catalog prose now states a false invariant
- **Where:** src/data/institutionalCatalog.js:1454
- **Finding:** DESIGN_INFORMATION_BROKERAGES §3: 'Rookery (illegal minor; town+): requires a criminal-organization power present'. Built: the catalog entries carry baseChance 0.16/0.2 with no precondition machinery, and no generator consults informationBrokerageTuning's hasCriminalOrganization predicate (rg over src/generators: zero brokerage hits outside unrelated service tables). brokeragePatronage.js:189 makes the honest downstream call — a Rookery in a town with no criminal organization simply 'serves nobody' (genesisPatronOf → null) — so presence is ungated and only patronage/effects respect the criminal-power requirement. Meanwhile the catalog desc asserts in-world that 'one never appears without that backing', which is now false. The queue's I1 row explicitly asked the re-validator to 'Confirm I3 actually closes it rather than inheriting the deferral again'; I3 inherited it in altered form and no row records that outcome.
- **Verifier adjustment:** Core confirmed with a naming correction and one strengthening. The predicate is `brokeragePowerPreconditionMet` (src/data/informationBrokerageTuning.js:339), not `hasCriminalOrganization` — that name exists nowhere in the tree, and `hasCriminalPower` (npcVerdictTable.js:299) is a different lane's function. `rg -n 'brokeragePowerPreconditionMet' src/ tests/` returns FOUR hits: three in tests/data/informationBrokerageCatalog.test.js and one in a doc-comment at src/domain/worldPulse/brokeragePatronage.js:71 — ZERO production call sites. STRONGER THAN REPORTED: I3 did not merely defer the presence gate, it WIDENED the rule at the patronage layer — brokeragePatronage.js PATRON_ELIGIBILITY admits `illegal` houses to both CRIMINAL and OUTSIDER archetypes ('a smuggling ring that has not yet been named a criminal power can shelter a loft'), which is looser than §3's 'requires a criminal-organization power present'. The catalog entry at src/data/institutionalCatalog.js:1454 carries baseChance 0.16 with no precondition machinery and a desc asserting 'Only a standing criminal organization can protect a loft like this, so one never appears without that backing.'
- **Failure scenario:** A generated town with zero criminal factions seats a Rookery; the dossier prose promises criminal backing that the power structure disproves; when I3 services light, the unpatroned loft either trades anyway (violating §3's precondition) or sits inert while its card claims otherwise — either way the world-coherence promise ('the map and the dossier cannot disagree' class) breaks on a player-visible surface.
- **Fix sketch:** Decide and record one reading: (a) presence-gated — teach assembleInstitutions a post-power reconciliation pass (or move brokerage seating after generatePower) using the existing predicate, accepting one more golden re-record; or (b) presence-free — amend §3's catalog spec and soften the two desc strings to match reality ('kept in hope of a patron' when unbacked). Either way, answer the open queue question with a row.
- **Finder confidence:** CONFIRMED

### R-30 [MEDIUM/CONFIRMED] The in-pulse query hardcodes delayTicks 0: distance never prices at the counter and floor01 reads unattenuated competence for any subject
- **Where:** src/domain/worldPulse/brokerageServicesRules.js:244-253
- **Finding:** evaluateBrokerageServiceRules calls answerBrokerageQuery with `delayTicks: 0` for whatever subject subjectOfInterest picked from the seat's belief map — which can be any settlement the seat has ever heard of, at any distance. Design §6 prices a query 'banded by channel + distance + form' and brokerageServices.js implements the surcharge (DISTANCE_SURCHARGE per tick) and the distance-attenuated floor01 — but the only in-engine caller never supplies a distance, so the surcharge is dead code in the pulse, the price never rises with distance, and floor01 = raw competence (0.74 for legal-major politics), which combined with finding 1 makes every ground-truth admission and every 'confirmed' stamp distance-blind at the counter. Law 1 ('distance is never abolished... stated a second way at the counter' per QUERY_PRICE_TUNING's own comment) is structurally bent here: the machinery exists, the wire carries zero. The distancePricedNews/routeAwareHopDelayTicks reader used by brokerageFidelity is available in the same pulse context.
- **Failure scenario:** Flag lit; a settlement's seat believes a hostile ten roads away; rotation picks brokerage_query; the house answers about the far subject at the near-subject price band and (if a graph edge exists) admits ground truth it has not earned under Law 1's own attenuation; the chronicle receipt shows a token/fee price for an answer the tuning table says should cost a purse.
- **Fix sketch:** Thread the real news distance into the producer: compute delayTicks via the same routeAwareHopDelayTicks(digest, subjectId, settlementId) read makeBrokerageFloorFn already performs (the snapshot and digest are in scope at the candidate-evaluation seam), and pin one far-subject case asserting the price band rises and truth admission fails beyond the sharp floor.
- **Finder confidence:** PLAUSIBLE

### R-31 [MEDIUM/ADJUSTED] WR substrate census overstatements: 'jailed applies to any named soul' has no non-corruption jailing path; '14 peace mirrors' is 13; the existing truth-side feasibilityGate.js is omitted from the census WR-8 must reconcile with
- **Where:** docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md:106
- **Finding:** (a) §2 claims 'verdicts (jailed applies to any named soul)' and amendments K.3d/O lean on 'H2's existing jailed verdict, applied to a foreign guest'. Verified: the jail HOLD RECORD generalizes (npcVerdictApply.js:451 jailUntilTick on a placed ledger entry), but the only writer is applyNpcVerdict, which fires solely on corruption exposure of a local roster NPC; no path can jail a travelling foreign envoy today, so WR-7b needs a small new jailing writer, not pure wiring as the amendments imply. (b) §2 says '13 war reasons + 14 peace mirrors'; warReasons.js carries 13 and 13 (PEACE_REASON_TYPES enumerated: exhaustion…common_rite = 13). (c) The census omits feasibilityGate.js — an existing DETERMINISTIC TRUTH-READ classifier of attacker/defender capacity in front of the siege RNG — while WR-8 introduces belief-side 'feasibility' with K3's absolute no-truth-read law; without a named seam ruling, an implementer will either read the existing gate (K3 violation) or leave two same-named feasibility concepts, one truth-side and one belief-side, with no documented relationship. Also minor: the doc title says 'A–R compiled' but §3/§5 include amendment S (WR-10). Nuances verified alongside: relationship 'extreme' exists only as continuous axes (trust/resentment/fear in relationshipState.js) with no authored extreme band — J-WR-10's derivation is new work, consistent with its ruling but not an existing band; deity 'domains' exist only as deitySnapshot's optional `domain` plus derived alignment axes (temperamentAxis retired, deityAxes.js:87).
- **Verifier adjustment:** (a) CONFIRMED and it is the load-bearing half. `rg -n 'jailUntilTick|jailed' src/domain/worldPulse/*.js src/store/*.js` shows exactly one writer: src/domain/worldPulse/npcVerdictApply.js:553-563, inside `applyNpcVerdict`, whose signature requires `args.npc` (the ROSTER record) and `args.exposure` (the corruption lane's exposure record). npcDmVerbs' three verbs are assign/kill/pardon — pardon only DELETES a `jailUntilTick` mark (:602-605); no verb mints one. So no path can jail a travelling foreign envoy, and amendments K.3d (DESIGN_REALM_DIRECTIVES:663) and O (:972) both lean on 'H2's existing jailed verdict, applied to a foreign guest' as existing machinery, as does WR-7b (WR doc :457). WR-7b needs a new jailing writer, not wiring. (b) CONFIRMED by executed count: warReasons.js carries 13 WAR_REASON_TYPES (…opportunism, sacred_claim) and 13 PEACE_REASON_TYPES (exhaustion…common_rite); the census says '13 war reasons + 14 peace mirrors'. Trivial severity — the bijection walker would catch it on the first run. (c) REFUTED. feasibilityGate.js is omitted from the census, but the doc pre-empts the failure it would cause: §1b law K3 (:46-52) forbids any negotiation path — 'terms, vote, interceptor judgment, close-vote comparison, FEASIBILITY' — from reading true world state and mandates 'STRUCTURAL ENFORCEMENT REQUIRED, not convention: pin the negotiation modules' import lists, scan their sources for true-state read tokens, and prove the same scan FINDS those tokens in a module that legitimately reads truth (guard-the-guard)', and §4's storage table (:196) records 'feasibility, comparative costs, home front — NEVER STORED. Pure belief-side evaluators'. An implementer importing feasibilityGate would red the mandated import pin. (d) The 'A–R compiled' title vs amendment S is same-commit lag (daee130d is titled 'amendments R/R2/S … + the compiled architecture volume') — not a defect.
- **Failure scenario:** The zero-context external implementer builds WR-7b assuming a callable jail-any-soul verb, discovers none, and improvises a second jail writer beside the verdict lane's; or builds WR-8 'feasibility' by importing feasibilityGate, silently deleting the epistemic thesis at its most consequential read.
- **Fix sketch:** One census-correction pass over §2 before dispatch: fix the mirror count, restate the jailed claim as 'the hold record generalizes; the writer does not — WR-7b adds a foreign-guest jailing writer through the same ledger', and add a feasibilityGate.js row with an explicit ruling (siege resolution may read truth; every N2 consumer reads the new belief composite; distinct names).
- **Finder confidence:** CONFIRMED

### R-32 [MEDIUM/CONFIRMED] Webwar receipts leak EV arithmetic, confidence floats, and the internal wave label '(M2 failover)'
- **Where:** src/domain/worldPulse/supplyWebWarfare.js:431
- **Finding:** Four reader-facing strings on the new webwar beats carry engine internals: (1) ev.receipt (431-432), reasons[0] of every webwar_campaign_minted entry: 'A direct assault is a losing bet (margin -0.35); the target's web is 78% single-sourced — strangle first (indirect 0.62 vs direct 0.27).' (2) mintNews reasons[1] (907): '(indirect EV 0.62 vs direct 0.27)' — the same numbers a second time. (3) raidNews wrongVillage reason (931): '(read confidence 0.63)'. (4) abandonNews reason (962): '(M2 failover)' — an internal wave/module label a reader cannot possibly parse — plus completeNews (983) 'feels the strangulation (42%)'. The headlines and summaries around them are excellent D&D-newspaper writing ('Brightwater burns the wrong village', 'the atrocity bought nothing'); the parentheticals are the only breach.
- **Failure scenario:** The supply-web doctrine fires in a campaign -> the war-door article's reason lines read 'indirect EV 0.62 vs direct 0.27' and 'M2 failover' aloud — spreadsheet-and-codename voice inside the realm's most dramatic beats.
- **Fix sketch:** Strip the parentheticals and keep the strategic reading already present in each sentence ('the town re-sourced its grain by another road faster than the raids could sever it'); where magnitude matters, band it ('nearly all of its grain comes by one road'). Add the receipts to the no-digit prose scan proposed for eventProse.
- **Finder confidence:** CONFIRMED

### R-33 [MEDIUM/CONFIRMED] markRosterDeath writes the death mark to npcs[] only — the JSON-alias trap in the one H-program roster write that skips the both-homes discipline
- **Where:** src/domain/worldPulse/npcDmVerbs.js:437
- **Finding:** The KILL verb's markRosterDeath patches only settlement.npcs[]; it never walks settlement.factions[].members[] or powerStructure.factions[].members[]. In memory the alias hides it (members[] entries ARE the npcs[] objects), but after a JSON save/reload the two homes are independent objects and the members[] copy carries a living, unmarked record — precisely the recorded estate hazard that npcVerdictApply.stripNpcInfluence walks three homes with per-home hit receipts to avoid, in the same program, for the same key (NPC_CONSEQUENCE_KEY). Any future dossier or faction surface that reads the mark from the membership home (the only home factionGrouping populates on real pipeline output) will see the DM-killed person alive.
- **Verifier adjustment:** Confirmed and strengthened. The finding named markRosterDeath (npcDmVerbs.js:437-457); the SHARPER instance is its sibling clearJailHold (npcDmVerbs.js:592-609), which has the same one-home shape and leaves genuinely stale live state rather than a missing flag: after a save/reload a PARDON deletes jailUntilTick from npcs[] while both membership homes keep jailUntilTick=13 — a hold H2's own stripNpcInfluence wrote to all three homes. This is a direct violation of the binding directive J-D8(e) (docs/DESIGN_REALM_DIRECTIVES.md:1434-1435: 'every path walks BOTH alias homes (npcs[] and factions[].members[]) with JSON-round-trip, regen, and undo pins'). Unlike every other finding here, this path is WIRED and PERSISTED: src/store/npcVerbsBody.js runKillNpc/runPardonNpc -> commitVerbResult (npcVerbsBody.js:96-106) writes result.settlement into the save row and flushes it through persistSaveUpdate; the UI door is src/components/map/WandererVerbControls.jsx. Medium and not high only because there is no reader yet — `deceasedByDm` has zero readers in src (the sole occurrence is the write at npcDmVerbs.js:452) and the only jailUntilTick reader is clearJailHold itself, which reads npcs[].
- **Failure scenario:** Kill a faction-affiliated NPC, save, reload: npcs[] entry carries npcConsequence.deceasedByDm; the faction's members[] entry for the same id carries nothing. A member-list reader renders the dead magistrate as a living member; an in-memory test cannot see the split (the alias covers it until serialization).
- **Fix sketch:** Route the death mark through the same declared-patch machinery as the strip: extract patchFactionRoster/relinquish's both-homes walk into a shared helper (or have markRosterDeath call stripNpcInfluence's roster walker with a mark-only patch), so any mutation stamped on a roster record lands at all three homes by construction. Pin with a JSON-round-tripped fixture asserting the mark at both homes — the vacuous-in-memory version must not be accepted.
- **Finder confidence:** CONFIRMED

### R-34 [MEDIUM/CONFIRMED] warReasonFactor=0 does not actually collapse the deploy weight — a treaty-blocked deploy still softmax-samples at ~11% per tick
- **Where:** src/domain/worldPulse/settlementStrategy.js:1035
- **Finding:** warReasons.js:657-660 documents the W-PEACE-2 war_block as: 'a LIVE non-aggression pact returns 0, which collapses the deploy weight at the one seam both war consumers already read'. But the multiplication happens on the SCORE (settlementStrategy.js:613, deployScore * 0 = 0), and the sampler is softmax over scores (line 1035, softmaxWeights(scores, STRATEGY_K=3.5)): contestMath.js:88 gives a 0-score move weight exp(k*(0-max)) > 0. Executed proof: softmaxWeights([0.35, 0, 0.30, 0.20], 3.5) yields 10.8% sampling probability for the zeroed deploy. So the pact-bound seat keeps 'resolving to march' on its pact partner roughly one tick in nine; each such resolution emits the march headline/army_deployed condition and deposits a war-intent order (applyWorldPulse.js:1151-1153) which — via findings 1 and 2 — opens the war with the margin gate waived. The declared semantics (a blocked lane) and the actual sampling semantics (a merely unattractive lane) disagree. The treatyEnforcement battery pins only that the factor RETURNS 0 (tests/domain/treatyEnforcement.test.js:169-172), never that the move stops being chosen.
- **Failure scenario:** A 20-year NAP = 240 ticks. At ~10% per tick the pact-bound seat resolves dozens of marches on its treaty partner over the pact's life, each one a headline ('X resolves to march'), a posture condition, and a standing order in the warIntents ledger — and with the opener unguarded (finding 1), each order actually opens the war within its 2-tick TTL.
- **Fix sketch:** A block must remove the move from the enumeration, not multiply its score: in enumerateMoves, when causal.warFor(bestTargetId) === 0, do not set scored.deploy at all (mirroring the existing legality gates like homeBesieged), so the move never enters the softmax. Keep the multiplicative form for genuine 0<factor lifts. Pin it: a pact-bound pair must produce zero strategy_deploy candidates across a seed sweep.
- **Finder confidence:** CONFIRMED

### R-35 [LOW/ADJUSTED] '(registration seam)' — engineering jargon inside two authored term receipts
- **Where:** src/domain/worldPulse/peaceTerms.js:491
- **Finding:** draftReceipt's puppet_seat and disclosure cases (491-492) read 'A victor-aligned seat installed (registration seam) — cheap control, brittle control.' and 'Observer/disclosure clause (registration seam) — ...'. 'Registration seam' is this codebase's internal term for a minted-but-unexecuted executor. Exposure today appears latent — TreatyTermView carries no receipt field and the signing beat doesn't embed term receipts — but these strings are authored as reader prose (the other eight cases are pure in-world sentences), so the first surface that renders term receipts ships the jargon. Same class as '(M2 failover)'.
- **Verifier adjustment:** Reclassify from a defect to a LATENT register risk, low. No surface renders term.receipt today, so no reader can see it and nothing is currently broken; the string does persist into the treaty ledger and therefore into saves, so it will ship the day any surface starts rendering term receipts. Worth fixing opportunistically when the treaty article lands, not worth a wave of its own. The two cheap in-world replacements are already implied by termLabel() ('installed seat', 'disclosure clause', peaceTerms.js:1334-1335).
- **Failure scenario:** A treaty article or ledger inspector starts rendering term.receipt (the natural next step for the trade-door article) -> the DM reads 'registration seam' as though it were a clause of the treaty.
- **Fix sketch:** Move the seam marker to the existing structured `seam: true` flag's display (a provenance chip), and keep the receipt sentence purely in-world ('A victor-aligned seat is installed — cheap control, brittle control.').
- **Finder confidence:** PLAUSIBLE

### R-36 [LOW/CONFIRMED] A-8 institution-multiplicity inputs disagree across two binding docs (cartography §11b vs route lifecycle §5d) — a live instance of the two-ratified-docs class before either consumer is built
- **Where:** docs/DESIGN_TOWN_CARTOGRAPHY.md:225
- **Finding:** DESIGN_TOWN_CARTOGRAPHY §11b (binding A-8) defines resolveInstitutionMultiplicity's canonical count from 'population-within-tier x economic profile (+ prosperity), seeded jitter, clamped to the authored range'. DESIGN_ROUTE_LIFECYCLE §5d (later owner amendment, also binding) rules 'the A-8 institution counts read the LIVED profile, not genesis alone' — an input the cartography doc's formula does not contain and which (per the §5d finding above) does not exist in the tree. Neither doc references the other's clause; TC-3/TC-4 will be built from the cartography doc by an implementer told the corpus is frozen.
- **Failure scenario:** TC-4 builds resolveInstitutionMultiplicity per §11b; the settlement that tilted to crossroads shows the same seventeen workshops as its genesis twin; the owner's §5d ruling is silently unhonoured and discovering it later forces a golden-shifting rework of a shipped cartography surface.
- **Fix sketch:** One-line amendment to DESIGN_TOWN_CARTOGRAPHY §11b: 'economic profile' is defined as the LIVED profile (genesis basis + network roles) once §5d ships, genesis-only until then, with the upgrade recorded as a TC-4 input change requiring its own golden acknowledgment.
- **Finder confidence:** CONFIRMED

### R-37 [LOW/ADJUSTED] Band-edge plants sell the truth at full base price and narrate it as a lie
- **Where:** src/domain/worldPulse/brokerageServicesPlant.js:253-255
- **Finding:** commissionPlant clamps assertedBand to [0,4], so a deflate against trueBand 0 or an inflate at trueBand 4 yields assertedBand === trueBand: the 'lie' asserts what is already true. The commission is accepted (no refusal in PLANT_REFUSALS covers it), the patron is charged BASE (0.18) since bands=0, and the producer's summary still prints 'It is not true. Nobody there knows that yet.' (brokerageServicesRules.js:364) — false on both counts. Such a plant can never contradict (|current-asserted| starts at 0) and only ages out at EXPOSE_MAX_AGE_TICKS, so the blowback stake the market's price supposedly carries is zero. tests/domain/brokeragePlant.test.js:187-204 pins the clamping as 'bounded to the band range' but never confronts the paid-no-op consequence, so this reads as an unexamined edge rather than an authored market behavior.
- **Verifier adjustment:** The core is CONFIRMED, but the blowback sub-claim is backwards and the corrected version is worse for the market, not better. It is not true that 'the blowback stake the market's price supposedly carries is zero'. plantIsExposed and the real processLies both fire on `contradicted || agedOut` (brokerageServicesPlant.js:348; informationStatecraft.js:628-630), and the aged-out arm charges the full deception delta to record.liarId — which this module sets to the market's own host settlement — plus a 0.3 relationship grievance and an exposure news beat (informationStatecraft.js:631-676). So once the plant wiring lands, a commission that asserted the truth still costs the market a full credibility charge and a public exposure at EXPOSE_MAX_AGE_TICKS. A further consequence: that exposure beat's summary is hardcoded to the inflate shape ('that its strength was greater than it is'), so a deflate or band-edge plant would be narrated wrongly on exposure as well.
- **Failure scenario:** Rotation mints a deflate plant against a subject the host already believes is band 0; patron pays 0.18 of its capital; the chronicle declares a lie was planted; the record asserts the truth, can never be contradicted or exposed as blowback, and the DM's plant ledger (once wired) carries a 'lie' that is true.
- **Fix sketch:** Refuse the degenerate commission: when assertedBand === trueBand after clamping, return a new closed refusal (e.g. 'nothing_to_assert' — 'the world already believes what the patron wants said'), added to PLANT_REFUSALS; alternatively price it at zero and skip the candidate. Pin both band edges as refusals.
- **Finder confidence:** CONFIRMED

### R-38 [LOW/ADJUSTED] Cartography TC-0/1/2 were built while the recorded owner boundary still says TC-0..8 are HELD — no recorded order lifts the hold
- **Where:** docs/DESIGN_REALM_DIRECTIVES.md:69
- **Finding:** The restart order recorded in Progress explicitly keeps TC-0..8 held ('STILL HELD: TC-0..8 + wave-3 programs (they do not change the simulation — the owner's rationale, applied)'), and no later Progress entry, queue row, or handoff line records an owner order lifting it — yet TC-0/1/2 were mid-build at the pause (HANDOFF_2026-08-01_PAUSE.md §2B) and DESIGN_WAR_RULINGS_ARCHITECTURE WR-0 directs committing them as legitimate work. An off-record owner order may exist (the owner was interactive on 2026-08-01), but the paper trail as it stands shows held work built, and a successor auditing authority from the documents alone cannot distinguish authorized from unauthorized.
- **Verifier adjustment:** The documentary gap is CONFIRMED; the inference that the work was unauthorized stays PLAUSIBLE and should not be asserted. Executed: `rg -n 'TC-0|TC-1|TC-2|cartography|CARTOGRAPHY' docs/DESIGN_REALM_DIRECTIVES.md` returns four hits — :30 'AFTER IS HELD: waves D..J, W-H, W-I, TC-0..TC-8', :54 'STILL HELD after tuning: the build waves (D..K, TC-0..8)', :69 'STILL HELD: TC-0..8 + wave-3 programs (they do not change the simulation — the owner's rationale, applied)', and :1554 (an unrelated prose line). No entry anywhere in Progress lifts the hold. Meanwhile DESIGN_WAR_RULINGS_ARCHITECTURE.md §WR-0 (:210-218) directs 'Commit 1 — cartography TC-0/1/2 (all 11 test files green, 154 tests, verified 2026-08-01)' as legitimate work, and the tree carries the whole src/domain/townCartography/ directory plus 9 untracked test files. TWO NEW PIECES OF EVIDENCE the reviewer did not cite, and they cut both ways: HANDOFF_2026-08-01_PAUSE.md §4 enumerates what is under the stop order — 'The release soak grid · the 300-year research rerun · the subsystem-certification sweep · the tuning pass' — and does NOT list TC-0..8, which reads as the pausing session not regarding TC as held; but §5 'WHAT WAS ACCOMPLISHED TODAY' also does not mention cartography at all.
- **Failure scenario:** The Fable-capable successor, following START_HERE and the Progress boundary literally, treats the TC dirty files as unauthorized work and reverts them (the handoff explicitly contemplates 'finish or revert'), destroying ~154 tests of owner-wanted work — or conversely commits them citing WR-0 while the recorded boundary says held, repeating the exact ambiguity for TC-3+.
- **Fix sketch:** One Progress entry (or queue row) stating when and how the TC-0..2 hold was lifted — quoting the owner's order if it exists, or flagging the start as a recorded deviation for owner ratification if it does not — so WR-0's commit instruction and the boundary text agree.
- **Finder confidence:** PLAUSIBLE

### R-39 [LOW/ADJUSTED] Dispersal leaves locally-placed identities (jailed here) hosted by the destroyed settlement forever, while the news counts them among the scattered
- **Where:** src/domain/worldPulse/npcCirculation.js:664
- **Finding:** disperseCastToPool relies on graduateNpc, whose one-way door (npcLedger.js:604-609) returns an already-graduated identity UNCHANGED. A person jailed at the settlement being destroyed (placed with hostSettlementId = that settlement, per applyNpcVerdict's holding branch) is therefore never moved to roamers: their PlacementRecord keeps pointing at a settlement that 'passed from the living map', permanently, since no engine path ever relocates them (DM verbs only). Meanwhile the existing id IS pushed into wnpcIds (line 675: any truthy wnpcId), so dispersed count and the news item claim they are 'on the road with no home to return to'. This is the zombie-host shape the owner just cured for settlements, reborn in the people ledger, plus a receipts-lie in the Herald item.
- **Verifier adjustment:** Reproduced exactly as described, but it is the same root cause as finding 1 (disperseCastToPool graduates blind to what the ledger already holds) and carries the same zero reachability: the function has one src grep hit, its own definition, and DESIGN_DEMOGRAPHIC_ENGINE.md documents it as unwired. Corrected severity medium -> low; corrected framing: this is a second symptom of one latent defect, not an independent one. The 'permanently' half is accurate — the only other moveNpcRecord callers are npcResidency.js (also unwired: zero src callers of its entry points) and the DM verbs.
- **Failure scenario:** Executed probe: jail 'Ila Fenn' at sav_kelder (placed there), disperse sav_kelder → output: 'P3 dispersed count: 1 | still placed at destroyed host: sav_kelder | roaming: 0 | news claims on the road: 1 people…'. The register shows her settled at a ruin; the news says she is roaming; neither is true of the other.
- **Fix sketch:** In disperseCastToPool, after the graduation walk, sweep ledger.placed for records whose hostSettlementId is the destroyed settlement and move each through moveNpcRecord(hostSettlementId: null, sinceTick: tick) with verdictCause left intact (the sentence survives; the prison does not) — and count a soul in wnpcIds/dispersed only when they actually entered or already occupied the pool. Pin with a jailed-local fixture asserting placed is empty of the dead host and the news count equals the roamer delta.
- **Finder confidence:** CONFIRMED

### R-40 [LOW/ADJUSTED] Dispersal resurrects a DM-killed NPC — the engine overturns the KILL ruling
- **Where:** src/domain/worldPulse/npcCirculation.js:661
- **Finding:** disperseCastToPool graduates every id-carrying roster npc with no check of the NPC_CONSEQUENCE_KEY.deceasedByDm mark that the DM KILL verb stamps (npcDmVerbs.js:452 via markRosterDeath). KILL removes the ledger record (removeNpcRecord), so durableIdForRoster misses, graduateNpc mints a FRESH wnpc_ id for the dead person, and they re-enter the roaming pool. Grep census: deceasedByDm has ZERO readers anywhere in src — it is a write-only tombstone; no engine lane (dispersal, verdict apply, replacement, residency) and no UI surface consults it. applyNpcVerdict (npcVerdictApply.js:503) has the same blindness if the corruption lane exposes a deceased-marked roster record. This is a law-1 violation (the DM's authority is total; the engine un-kills their ruling) that fires the day the documented wiring slice calls disperseCastToPool on a settlement whose roster carries a DM death.
- **Verifier adjustment:** The mechanism is real and reproduced, but the severity was set as if the lane were live. `disperseCastToPool` (src/domain/worldPulse/npcCirculation.js:647) has exactly ONE grep hit in src — its own definition — and its unwired state is documented twice: docs/DESIGN_DEMOGRAPHIC_ENGINE.md ('DISPERSAL IS WRITTEN BUT UNWIRED. `disperseCastToPool` has exactly one grep hit — its own definition') and docs/DESIGN_NPC_CONSEQUENCES.md §16 ('NOTHING IN W-H3 IS CALLED FROM THE PULSE'). The whole economy is additionally behind `npcConsequencesEnabled: false` (src/domain/worldPulse/simulationRules.js:442). So this cannot fire in any campaign today; it is a latent defect the wiring slice will inherit, not a live law-1 violation. The corrected claim: when the documented wiring slice lands, disperseCastToPool will re-graduate a DM-killed roster entry under a fresh durable id, because (a) killNamedNpc removes the ledger record via removeNpcRecord so durableIdForRoster misses, (b) markRosterDeath deliberately leaves the roster entry in place, and (c) the graduation loop at npcCirculation.js:661-676 consults nothing but `npc.id`. `deceasedByDm` is a write-only tombstone: the only src occurrence is the write at npcDmVerbs.js:452. The same blindness in applyNpcVerdict is likewise unwired (zero src callers).
- **Failure scenario:** Executed probe (node, repo modules): graduate 'Maro Venn' → killNamedNpc (deceased mark true, ledger empty) → disperseCastToPool on the marked roster → output: 'P1 RESURRECTED: 1 souls; pool ids: [wnpc_ba5f82f0] names: [Maro Venn]'. The dead man walks, under a new durable id, into the Wanderers register.
- **Fix sketch:** Make the deceased mark a first-class gate in the graduation lanes: a shared predicate (e.g. isDeceasedByDm(npc) reading NPC_CONSEQUENCE_KEY) exported from the module that owns the mark's spelling, consulted by disperseCastToPool's roster walk and by applyNpcVerdict's entry (alongside the id-less fail-closed check). Add an anchored-negative-style pin: a dispersal fixture with one deceased-marked member graduates everyone EXCEPT them, with a positive anchor that the unmarked neighbours did graduate. Trace the mark's own lifecycle too — it must survive JSON round-trip and regen, which today it does not even attempt (see the markRosterDeath finding).
- **Finder confidence:** CONFIRMED

### R-41 [LOW/ADJUSTED] Magic disaster buffer draws only granary months — the §5 treasury/reserves draw and §7 reagent-stock draw were never built, and the reagent half was not revisited after K4 created reagent chains; §5 also contradicts amendment S on treasury's existence
- **Where:** src/domain/worldPulse/magicBufferApply.js:23
- **Finding:** DESIGN_MAGIC_ECONOMY §5: 'reduced structural loss is paid for in named stocks (treasury, reagents, reserves draw down in the same outcome)'; §7: 'the buffer draws down reagent stocks by name'. Built: the buffer's payment is storesMonthsDrawn only (magicBufferApply.js:270–384, 'the granaries paid'). The in-code justification ('none exist in the tree today') was true at K3, but K4 then landed magicSubstitutionReagents.js + supplyChainResourceIndex.js — and the K4 diff does not touch magicBufferApply, so the reagent-draw promise is now buildable and still unbuilt, recorded nowhere as a deferral outside the K3 code comment. Separately this is a clean two-ratified-docs contradiction of the village-seeding class: amendment S in DESIGN_REALM_DIRECTIVES (:1199) rules 'there is NO settlement treasury — verified; prosperity is a label, not a stock', and code agrees (peaceTerms.js:201 treats treasury as an AssetClass label mapping to tribute streams), while DESIGN_MAGIC_ECONOMY §5 names treasury as a drawable stock. One of the two docs must be amended.
- **Verifier adjustment:** The buffer-draw half is REFUTED and only the doc contradiction survives. (1) 'Draws only granary months' is false: src/domain/worldPulse/magicBufferApply.js:14-21 names TWO stocks and both are drawn — 'THE GRANARY economicState.foodSecurity.storageMonths' and 'THE RESERVE the ward charge in this slice's own ledger, which is what makes the second-shock window a fact about state'. §5's triple is (treasury, reagents, reserves); reserves ARE built. (2) The reagent absence is an explicitly recorded deferral, which the review rules exclude: :23-27 reads 'REAGENTS ARE DELIBERATELY ABSENT … Pricing a stock that does not exist would be a fabricated ledger entry dressed as conservation … Recorded here so a later reader finds a deferral, not a gap.' (3) The 'K4 made it buildable and left it unbuilt' premise is unproven: K4 created reagent CHAINS, corridor demand and interdiction marks, not per-settlement reagent STOCKS — magicSubstitutionReagents.js reads `economicState.activeChains` and a REAGENT_CHAIN_SPINE, and no reagent stock quantity exists anywhere to draw down, so the K3 justification still holds verbatim after K4. K4 in fact reinterprets §7's clause explicitly at :179-186 ('THE STAPLE ID: what an interdiction denies … §7's the buffer draws down reagent stocks BY NAME'). WHAT SURVIVES: the two-ratified-docs contradiction. DESIGN_MAGIC_ECONOMY.md:114 (§5) names treasury among the drawable named stocks; DESIGN_REALM_DIRECTIVES.md:1199 (amendment S) rules '⭐ THE PRICE IS DENOMINATED IN WHAT EXISTS (there is NO settlement treasury — verified; prosperity is a label, not a stock)'. One of the two must be amended; the code already sided with amendment S.
- **Failure scenario:** K-program soak/tuning evaluates the second-shock window and gating asymmetry with a buffer whose only cost is food months: a rich-but-well-fed settlement pays the wrong resource, the 'magic eats materials' asymmetry pin measures a narrower truth than the design claims, and reagent interdiction never touches disaster mitigation despite §7's whole-point integration.
- **Fix sketch:** Amend DESIGN_MAGIC_ECONOMY §5 to the true stock model (stores + reagent stocks; strike treasury per amendment S), then a small K3b slice adding reagent-stock drawdown to the buffer payment now that K4's reagent vocabulary exists; record both the doc amendment and the slice as queue rows.
- **Finder confidence:** CONFIRMED

### R-42 [LOW/CONFIRMED] Query headline can name and bill the wrong house: the charged patron is bindings[0]'s while the answering house is chosen by competence
- **Where:** src/domain/worldPulse/brokerageServicesRules.js:242-276
- **Finding:** The producer sets `asker = bindings[0]` (first house in roster order) and charges asker.patronId, but answerBrokerageQuery independently selects the best-competence house for the channel and issues the receipt in THAT house's name (receipt.houseName/houseId). With two houses standing (e.g. a Listening Post first in roster and a Whisper Market), a 'war'-channel query is answered by the market (0.38 > 0.34) while the headline reads '<post's patron> buys an answer from <the post>' and the post's patron pays. metadata.receipt then names the market — the candidate's own headline and receipt disagree about who sold the answer, and the wrong power is charged for it. Under the NEWS ADDRESS LAW (typed action + recorded reason must be truthful) this is a self-contradicting chronicle row.
- **Failure scenario:** Settlement hosts a legal minor and an illegal major house with different patrons; the rotation mints brokerage_query on a war-channel subject; Herald prints patron A buying from house A while receipt.houseId names house B and house B's grade; a DM cross-reading the receipt sees two different sellers for one purchase.
- **Fix sketch:** Choose the asker AFTER the answer: take result.receipt.houseId, find the binding for that institutionId, and charge/narrate that binding's patron (falling back to skipping the candidate if the answering house is unbound). One pin: two-house roster, assert headline house === receipt house === charged patron's house.
- **Finder confidence:** PLAUSIBLE

### R-43 [LOW/CONFIRMED] Second sentencing of an already-graduated identity half-writes: roster mark, exclusion, and news update while the ledger record stays frozen
- **Where:** src/domain/worldPulse/npcVerdictApply.js:529
- **Finding:** applyNpcVerdict's idempotency contract covers re-running the SAME exposure. A genuinely new exposure of an already-graduated person (released, later re-corrupted and re-ousted) takes a different verdictRollKey (tick differs), can yield a different verdict, and then: graduateNpc's one-way door returns the existing record UNCHANGED (verdictCause, reputation — including the timesExposed>=2 notoriety bump verdictReputationFacets just computed — and host map are all discarded), while the exclusion edge IS added, the roster mark IS restamped with the NEW verdict, and the news announces it. Ledger says 'jailed', mark and Herald say 'banished'. Reachability today is thin (graduated people are not re-inserted into rosters by any engine lane), so this is a lifecycle-path latency rather than a live bug.
- **Verifier adjustment:** Upgraded from PLAUSIBLE to CONFIRMED — the mechanism is executed, and the disagreement is sharper than described. Severity stays low: applyNpcVerdict has zero src callers, so this is a latent lifecycle-path defect. Corrected detail: the ledger does not merely keep a stale verdictCause, it keeps the person in the WRONG MAP. A person whose second exposure sentences them to jail stays in `roamers` with hostSettlementId absent, because graduateNpc's one-way door (npcLedger.js:604-609) discards the `hostSettlementId: settlementId` that applyNpcVerdict's holding branch passes (npcVerdictApply.js:538). Three surfaces, two stories: ledger says roaming/criminal_founding, roster mark says jailed with jailUntilTick, Herald headline says 'is imprisoned in Aldermoor'.
- **Failure scenario:** DM releases a jailed official who is later re-exposed and banished: the Wanderers register still shows 'They are held under sentence' (ledger verdictCause 'jailed', stale reputation), the settlement door is shut by a real edge, and the roster mark says banished — three surfaces, two stories.
- **Fix sketch:** Give the verdict lane an explicit re-sentencing path: when graduateNpc reports minted:false and a triggering decision exists, route the state change through moveNpcRecord with a patch (verdictCause, reputation, host per the new verdict, sinceTick restamp) so the single mover keeps conservation while the record follows the court. Pin the repeat-offender bump landing in the ledger on the second sentence.
- **Finder confidence:** PLAUSIBLE

### R-44 [LOW/ADJUSTED] Severity register: authored ladder is coherent, but news cards print raw 'Severity NN%'
- **Where:** src/components/map/WizardNewsPanel.jsx:221
- **Finding:** The per-kind severity assignments on the 07-31 beats form a deliberate, rationalized register (0.35 intel changing hands -> 0.4 spy burned -> 0.45 lie exposed / campaign minted -> 0.5 strangulation complete -> 0.55 treaty -> 0.6 climb-down -> 0.65 village razed), each with an in-code rationale comment — good register discipline, and the id/severity backfill comments show why (absent severity rendered 'Severity 0%'). But the card surface itself, WizardNewsPanel.jsx:221, renders `Severity {percent(entry.severity)}` — a raw percentage — while the Herald strip's facet filter (HeraldStrip.jsx:75+) correctly speaks band words. Pre-existing presentation, newly fed by all these beats.
- **Verifier adjustment:** Drop the first half of the finding — 'the per-kind severity assignments form a deliberate, rationalized register' is praise, not a defect, and should not occupy a findings row. The reportable item is exactly one line: WizardNewsPanel.jsx:221 prints a raw percentage on a mounted reader surface while the codebase's own test asserts band words on the sibling Herald surface. Pre-existing presentation, newly fed by the 07-31 war-doctrine beats; low.
- **Failure scenario:** Any new war-doctrine beat opens in the news panel -> the meta pill says 'Severity 60%', a formula shown rather than translated, beside prose that carefully avoids numbers.
- **Fix sketch:** Map severity to the strip's existing SEVERITY_FACETS band words on the card pill (one shared band function), keeping the numeric field for sorting only.
- **Finder confidence:** PLAUSIBLE

### R-45 [LOW/UNVERIFIED] The W1 warIntents ledger has no certification row while the owner's next authorized action is the certification build
- **Where:** src/domain/worldPulse/warIntent.js:77
- **Finding:** warIntent.js (untracked, W1) mints a new spatialLedgers family (WAR_INTENT_LEDGER_KEY = 'warIntents', conditional drop-when-empty). The certification registry covers the peace engine's spatialLedgers.treaties sidecar (subsystemRowsWar.js:465) and the dirty subsystemRowsWaves.js diff registers town cartography — but zero rows in any subsystemRows* file mention warIntents, treatyEnforcement, or the siege-arrival gate. The cartography row's own text states the v5 census 'walks the top-level worldState keys plus one level into spatialLedgers', so a soak that runs the W1 joins will surface a warIntents key no row owns. The owner's amended stop-order makes the subsystem-certification build the very next authorized step after wave C.
- **Failure scenario:** The certification build runs over a corpus containing W1 wars: the v5 census reads spatialLedgers.warIntents, no subsystem row claims it, and the build either reports unknown state keys (blocking the certification the owner queued) or — worse, depending on census mechanics — silently leaves the newest war machinery outside the certification perimeter while appearing complete.
- **Fix sketch:** As part of landing W1 (same commit as the join code, per the cartography precedent of registering from the first commit): add a subsystem row for the war-intent/enforcement lane declaring spatialLedgers.warIntents as its stateKey and the treaty-enforcement movers as its aliveness channels, or an explicit documented declaration that the key is owned by the existing peace-engine row. The registration discipline already exists in this file; W1 just has not paid into it yet.
- **Finder confidence:** PLAUSIBLE

### R-46 [LOW/ADJUSTED] The alignment axis of the authored compatibility table is unreachable from the composed lane and has zero test coverage
- **Where:** src/domain/worldPulse/npcCirculation.js:573
- **Finding:** J-D8(c) binds rejection to 'an authored compatibility table (alignment x archetype x history flags), closed and pinned'. rejectionFor's alignment arm (npcCirculationTable.js:259) only fires when factionAlignmentRead is a strict opposite of the believed read, but attemptRehost calls admissionFor WITHOUT factionAlignmentRead (npcCirculation.js:573), admissionFor defaults it to 'unknown' (line 268), and grep census shows NO caller in src or tests ever supplies the parameter — nothing in the estate derives a faction's alignment read at all. alignmentReadsOppose(x,'unknown') is always false, so 'alignment_opposed' is a REJECTION_REASONS rung no production path can produce: the recorded unreachable-predicate-conjunction hazard class, on a directive-mandated table axis. Zero grep hits for 'alignment_opposed' in tests/ — the table's totality pin walks archetypes and scandals but never proves this arm alive.
- **Verifier adjustment:** The unreachability is confirmed exactly as stated, but medium overstates it. Corrected severity low, for three reasons the finding omits: (1) the parameter is an explicitly OPTIONAL hook, documented as such at npcCirculationTable.js:236 ('an ALIGNMENT_READS member, when known'), and alignmentReadsOppose's own header (npcCirculationTable.js:203-206) records the deliberate decision that unknown opposes nothing; (2) nothing anywhere in the estate derives a faction alignment read, so no caller — including the deferred wiring slice — has a value to pass, which makes this a missing INPUT rather than a broken predicate; (3) the whole lane is dark (npcConsequencesEnabled: false) and unwired, so no campaign behaviour is wrong today and the cure is purely additive. The directive citation is fair: J-D8(c) (DESIGN_REALM_DIRECTIVES.md:1430-1431) does bind rejection to an 'authored compatibility table (alignment x archetype x history flags), closed and pinned', and the alignment column is authored but inert.
- **Failure scenario:** A good-read roamer knocks at an evil-read cult with an open seat and no scandal aversion match: admitted_lowest, every time, in every campaign, because the composed lane structurally cannot say 'alignment_opposed'. The authored cell exists, is documented, and never executes; a future retune of the alignment column would change nothing and no test would notice.
- **Fix sketch:** Either derive the faction alignment read inside admissionFor from data the settlement already carries (the deityAxes-conformant tokens exist precisely so a faction/patron alignment can be compared without translation) and thread it through attemptRehost, or — if the derivation is deliberately a later slice — record the deferral in the design doc's §16 conventions and add an executed reachability pin the moment the input is wired (the effectReachability idiom). Minimum immediate action: a direct test that rejectionFor with an explicit opposed pair returns 'alignment_opposed', so the arm has any coverage at all.
- **Finder confidence:** CONFIRMED

### R-47 [LOW/ADJUSTED] The deflate plant raises the audience's readiness belief — the inflate bluff's readiness floor was copied into an intent it contradicts
- **Where:** src/domain/worldPulse/brokerageServicesPlant.js:269
- **Finding:** commissionPlant's override sets `readiness: round4(clamp01(Math.max(clamp01(num(prior.readiness, 0.25)), 0.5)))` for BOTH intents. That construction is copied verbatim from the LIE verb's seed (informationStatecraft.js:733-740), where it is correct because the verb only ever INFLATES (a garrison bluff: 'fear this place' — stronger AND readier). PLANT_INTENTS adds 'deflate' ('make them think this place is weak, and watch what they do about it' — the module calls it the reason the market exists), but a deflate override lowers strengthBand by 2 while simultaneously RAISING the audience's belief in the subject's readiness to at least 0.5. A court being sold 'they are weak' ends up believing the target is weak but battle-ready — mixed signals that blunt exactly the reaction the patron paid for (war choosers read both axes). tests/domain/brokeragePlant.test.js pins only the band clamping for deflate (lines 187-197), never the readiness direction, so this is an unexamined copy, not a recorded judgment. Latent today because of the missing plant wiring (finding 2), which is exactly why it should be fixed before the wiring lands.
- **Verifier adjustment:** The defect is real and now EXECUTED rather than reasoned, but the named consumer is wrong. No war or strategy chooser reads belief readiness: settlementStrategy.js:255-263 reads only b.record.strengthBand and b.record.allianceLabel, and grep finds no 'readiness' in settlementStrategy.js, warReasons.js or warIntent.js. The real consumer is dispatchEV.believedDestinationDanger (src/domain/spatial/dispatchEV.js:206-215), called from supplyKernel.js:662 and generosityEV.js:319: believedReadinessDanger = dangerFromReadiness(rec.readiness) x confidence01, with READINESS_IDLE 0.3, so the 0.5 floor manufactures a danger term of 0.286 x confidence out of nothing. The corrected failure scenario: a patron commissions a deflate plant to make a rival look weak; the audience's caravans and aid become MORE deterred from the subject, because the same override that dropped its strengthBand by two rungs raised its believed war readiness.
- **Failure scenario:** Wiring lands; a patron commissions a deflate plant against a rival before striking it; the audience court's belief becomes strengthBand -2 but readiness >= 0.5; a strategy chooser weighing readiness declines the attack the plant was bought to provoke; the market sold a self-defeating lie and no pin notices because the direction was never asserted.
- **Fix sketch:** Make the readiness override intent-directional: inflate keeps max(prior, 0.5); deflate takes min(prior, ~0.25) or leaves readiness untouched (the honest minimal lie moves only the axis the patron bought). Pin both directions in brokeragePlant.test.js beside the existing band-clamp cases, and pin that the real mover's exposure logic behaves identically for both (the contradiction gap is band-based, so the readiness change is free to fix).
- **Finder confidence:** PLAUSIBLE

### R-48 [LOW/UNVERIFIED] The occupation ledger has no canonical domain reader — the sole UI reader is a hand-rolled twin with a dead spelling tolerance
- **Where:** src/components/settlements/livingWorldSignals.js:265
- **Finding:** occupationOf is defined privately in livingWorldSignals.js (line 265) tolerating `worldState.occupations || worldState.conquest` and `entry === true`. Grep proves nothing anywhere writes `worldState.conquest` (the only `.conquest` hits are `world_outcome.conquest.*` id strings in warDeployment.js:1535/1592) — the tolerance is dead code that would mask a real spelling drift instead of surfacing it. Meanwhile src/domain/worldPulse/occupation.js exports 15 functions (the writer side) but no is-this-settlement-occupied reader, and the Foundry path builds its own independent `vm.liveWorld.occupied` join. This is the faction-key defect class shape the memory index names: hand-rolled accessors beside a canonical writer, cure = one exported reader, never hand-rolled.
- **Failure scenario:** A future occupation-ledger shape change (e.g. keying by durable id, or nesting under spatialLedgers as newer W1 families do) updates occupation.js and the Foundry join but not the components-side hand-roll; the UI 'occupied' badge silently goes dark for every occupied settlement, and the dead `worldState.conquest` fallback guarantees the reader still returns false rather than throwing, so nothing reds.
- **Fix sketch:** Export one `isOccupied(worldState, id)` / `occupationRecordOf(worldState, id)` reader from occupation.js (or the display-layer warStatus module beside liveSieges), delete the hand-roll and the dead `conquest` spelling, and point livingWorldSignals, the Foundry liveWorld builder, and the finding-2 PDF sections at it — one reader, four consumers, which also makes finding 2's fix cheaper.
- **Finder confidence:** CONFIRMED

### R-49 [LOW/CONFIRMED] The reputation race gives the story zero-cost hops wherever the geometry cannot price the pair — the story teleports on exactly the edges the person pays for
- **Where:** src/domain/worldPulse/routeNetworkConsumersRace.js:138
- **Finding:** newsHopTicks rides hopDelayTicks (distancePricedNews.js:30-35), which returns 0 when the digest is absent OR hopWeeks is null/non-positive — correct for its original role as an additive staleness surcharge ('no path ⇒ no surcharge'), wrong as a travel time. The person's half prices the same unpriceable hop at UNPRICED_LEG_WEEKS=1 (routeNetworkConsumersTransit.js:92, documented as the user-route rung) with a MIN_LEG_TICKS floor. Executed proof: aspatial world, A—B—C open roads, wanderer vs story: personTicks=3, storyTicks=0, winner 'story' — grade impedance multiplies zero, so grade, distance, and listeners all stop mattering, which breaks the pinned property 'a deterministic function of route grade, distance, and listeners' in the degenerate direction. The same code path fires per-hop on a SPATIAL world for any user-chartered edge over a pair the frozen geometry cannot price: word crosses free while the wagon pays a week. The race test suite pins both outcomes on priced fixtures only; the unpriced rung is unpinned.
- **Failure scenario:** A DM charters a road between two settlements the digest cannot connect (the documented UNPRICED_LEG_WEEKS case). A wanderer flees down it; the story about them crosses the same hop in zero ticks. The signature 'outrun your own reputation' mechanic is structurally impossible along every user-asserted road and on all aspatial worlds.
- **Fix sketch:** Give the news a matching unpriced floor: in newsHopTicks, when the digest is null or hopWeeks is null, price the hop as ceil(UNPRICED_LEG_WEEKS × NEWS_SPEED_FACTOR × impedance) with a minimum of 1 tick where the person's floor is 1 — keeping news structurally faster but never free, and keeping grade impedance meaningful. Do it inside routeNetworkConsumersRace (not distancePricedNews, whose zero is correct for the staleness surcharge). Pin the aspatial and unmapped-pair races both ways.
- **Finder confidence:** CONFIRMED

### R-50 [LOW/ADJUSTED] Treaty breach and expiry are receiptless — only treaty_signed exists, violating §6/§8's 'crier beats for signing, each installment, breach, and expiry'
- **Where:** src/domain/worldPulse/peaceTerms.js:736
- **Finding:** DESIGN_PEACE_ENGINE.md §6 ('crier beats for signing, each installment, breach, and expiry') and §8 ('receipts on every ... signature, installment, breach, expiry') promise the treaty's whole lifecycle as receipted news, and §6 makes BREACH 'a first-class event'. Grep across src/ finds exactly one treaty news kind: treaty_signed (peaceTerms.js:894, correctly id-carrying with settlementIds per the NEWS ADDRESS LAW — the recorded id-less drop class is cured there). But in advanceTreaties PASS 2: a detected default flips ledger fields silently (treaty.defaultedBy/defaultSeverity01, lines 736-738 — it feeds the treaty_default casus, which is state, not a beat); a term expiry silently drops the term (688-691); a fully-lapsed treaty silently prunes (730-731). Nothing narrates 'the tribute has stopped' or 'the Peace of X has run its course' to the feed/chronicle — the arc that the signing beat opened never closes on any reader-facing surface. The module's WAVE-3 deferral list (rendering, coalition negotiation, mediation table) does not name breach/expiry beats, so this is an undocumented gap, not a recorded deferral. Installment beats are arguably noise (12/year/term) and reasonably summarized elsewhere, but breach and expiry are the two arc-closing transitions.
- **Verifier adjustment:** peaceTerms.js emits exactly one treaty beat (`treaty_signed`, minted at line 894, pushed at 665) and none for breach, expiry, or the full lapse/prune — expiry `continue`s at 688-691, the prune deletes silently near 730, and the default only flips treaty.defaultedBy/defaultSeverity01 at 736-738 to feed the treaty_default casus. Real against DESIGN_PEACE_ENGINE §6/§8, but it is a declared-but-unbuilt narration surface in a wave whose header scopes breach to 'minting the warReasons treaty_default casus' (peaceTerms.js:20-22) and defers treaty surfaces to WAVE-3 (36-38); no engine state is wrong and the casus feed works. Low — design-completion item, not a defect.
- **Failure scenario:** A tribute treaty is signed (major beat, severity 0.55). Eight years later the loser defaults and the victor detects it — the single most story-generative transition in the peace engine (§6's oathbreaking cascade) — and the World Book, crier, and chronicle say nothing. Later the whole treaty quietly vanishes from the ledger. A DM reading the feed sees a peace signed and never learns it broke.
- **Fix sketch:** Emit two id-carrying beats from PASS 2 transitions (both are edges the loop already detects): treaty_breached when observed compliance first reaches 'defaulted' (id `wizard_news.<tick>.treaty_breached.<victor>.<loser>`, settlementIds both parties, significance major), and treaty_expired when the last live term lapses (before the prune at line 730). Route both in heraldRouting beside treaty_signed. Dedup on the transition edge (prior complianceState !== 'defaulted') so a persisting default does not re-fire.
- **Finder confidence:** CONFIRMED

### R-51 [LOW/ADJUSTED] World Book 'Under siege' fix VERIFIED green; residual gap — the row drops the besieger names it already has
- **Where:** src/utils/generateWorldBook.js:413
- **Finding:** Verification requested by the chair: the sg.id-vs-targetId bug is fixed. realmChapterRows reads nameFor(sg.targetId) with a documenting comment, and tests/pdf/worldBook.test.js (executed, green in this session, 4 files / 63 tests passing) drives a REAL siege through the real collector and pins the rendered row to the settlement NAME, never 'undefined'. The campaign-PDF twin read is also correct — generateCampaignPDF.js:839 renders '<besiegers> besiege(s) <target>.' with plural handling. (An earlier apparent 'ben' corruption in that line was my own ripgrep -r flag error, refuted on direct read.) Residual legibility gap: liveSieges hands the World Book { targetId, coalition, frontCount }, and the 'Under siege' section prints ONLY the target name — the besieging coalition is dropped, so the World Book names the victim but not the aggressor while its own PDF twin names both. NEWS ADDRESS LAW wants the full chain.
- **Verifier adjustment:** The RESIDUAL half is refuted as a defect and should be logged as an optional enhancement, not a finding. The World Book section is a bullet LIST under the heading 'Under siege' (generateWorldBook.js:429, painted alongside 'Major headlines' and 'War-weary' at :428/:430) — a roster of besieged settlements, which is internally coherent and is the shape the suite deliberately pins (tests/pdf/worldBook.test.js:198, `expect(rows.sieges).toEqual(['Ashford'])`). The NEWS ADDRESS LAW governs news items (id + address chain + typed action + named settlements + recorded reason), not PDF chapter roster rows, so it does not obligate the coalition here; and the campaign PDF's prose line is a different artifact in a different register, not a twin contract the World Book is violating. Adding the besiegers would be a nice enrichment; its absence is not a legibility breach.
- **Failure scenario:** A GM prints the World Book during a siege -> 'Under siege: - Rivermeet' with no word of who besieges it, while the campaign PDF for the same save says 'Ironhold besieges Rivermeet.'
- **Fix sketch:** Render the sieges row through the same sentence the campaign PDF uses (coalition names + besiege/besieges + target), sharing one formatter so the twins cannot drift again.
- **Finder confidence:** CONFIRMED

### R-52 [LOW/ADJUSTED] tickScanBudget asymptotic gate is blind to every new W1 per-tick scan lane
- **Where:** tests/perf/tickScanBudget.test.js:278
- **Finding:** The gate's scanOps counter is incremented only inside src/domain/worldPulse/tickIndices.js (grep confirms no other worldPulse module carries the instrumentation), and its revert-guard REQUIRED map (lines 278-282) pins only the three Wave-4 files: npcAgency.js, roadsKernel.js, stressorDynamics.js. None of the new W1 per-tick read lanes import tickIndices: treatyEnforcement.js imports only distanceRead + kernel/math (lines 31-32), and warIntent.js imports only relationshipEvolution + distanceRead. So treatyBlocksWar (called per pair consultation via warReasonFactor from BOTH the settlementStrategy deploy-weight seam and warDeployment's conquest-margin seam), demilitarizationCapFor (called per settlement per tick from mobilization.evaluateMobilization), occupationHoldFor (per occupation row), and hostileTargetsOf (warIntent.js:298 — an O(S×E) full-edge scan with per-edge normalizeRelationshipEdge + ensureRelationshipState allocations, relocated verbatim from warDeployment, so pre-existing cost but now in a module the guard does not name) are all invisible to the asymptotic ratchet: their scans never increment scanOps, and fallbacks===0 is vacuously true for sites that were never indexed. The gate ran green in this tree (executed: 41 passed), but that green certifies only the Wave-4 sites.
- **Verifier adjustment:** CORRECTED CLAIM: The only uninstrumented per-tick lane with genuinely super-linear shape is `hostileTargetsOf` (src/domain/worldPulse/warIntent.js:298), and it is PRE-EXISTING, not W1-introduced. It is called once per candidate settlement in the opener loop (warDeployment.js:1646 over `candidateIds` = every settlement, warDeployment.js:1684), and each call walks the WHOLE edge array calling `normalizeRelationshipEdge` (relationshipState.js:201, spread-copies the edge) plus `ensureRelationshipState` (relationshipState.js:232, which re-normalizes the edge a second time and allocates a ~22-field object with three array slices). That is O(S x E) heavy allocation per tick, and `scanOps` never sees it because the counter lives only in tickIndices.js (13 increment sites; grep over src/tests finds no other module). tickIndices.js:106 already exports `edgeAdjacencyIndex(graph)` -- the exact WeakMap-cached primitive that would make this O(deg) -- and the opener does not use it. Mitigating: the call is gated behind seven early `continue`s in the opener loop (one-army, intervener, proposal-hold, cleared-attacker, besieged-at-home, `isWarReady` posture gate, HOSTILE_CONFIDENCE floor), so in a quiet world it fires rarely; the worst case needs a realm where most settlements are simultaneously war-ready. Reframed as a prevention/coverage gap (no live regression, no output defect), severity low, not medium.
- **Failure scenario:** A future change reverts a treaty read to an all-pairs × ledger scan, or grows hostileTargetsOf's edge walk super-linearly (e.g. nested per-target rescans) at realm scale. tickScanBudget stays green — scanOps unchanged, no fallback fires — and byte-identity goldens also pass since raw and indexed scans produce identical output. The regression is first noticed as wall-time drift in the ~8h release-profile soak, the most expensive possible detector.
- **Fix sketch:** Either extend tickIndices.js with a per-pass treaty index (treatiesByLoser / treatiesByPartyPair, built once per evaluate pass like siegeArrivalGate) and route the three treatyEnforcement readers plus hostileTargetsOf through it — which automatically brings them under the scanOps instrument — and add warIntent.js/treatyEnforcement.js rows to the REQUIRED revert-guard map; or, if the treaty ledger is ruled permanently small, record that as an explicit exemption in the gate's header comment so the blind spot is a documented deferral rather than an unrecorded gap.
- **Finder confidence:** CONFIRMED

### R-53 [LOW/ADJUSTED] war_block treaty term's real consumer (strategy deploy-weight multiplication) is untested — the read is pinned, the obedience is not
- **Where:** tests/domain/treatyEnforcement.test.js:169
- **Finding:** The file's header claims 'every pin here drives the REAL CONSUMER, not the read', and delivers on that for readiness_cap (drives evaluateMobilization) and occupation_hold (drives evaluateOccupations). But the war_block section's 'CONSUMER' test drives warReasonFactor itself — the read — and asserts it returns 0 for a pact-bound pair. The actual consumer chain is src/domain/worldPulse/settlementStrategy.js:1009 (causal.warFor = (targetId) => warReasonFactor(worldState, sId, targetId)) feeding the multiplication at settlementStrategy.js:612 (warMult zeroes deployScore). CONFIRMED by executed greps: of the six test files that call evaluateSettlementStrategyRules (settlementStrategy, peaceCausalConsumption, warClusterWC1, warIntentJoin, beliefMisjudgment, warMachineObeysPolitics), none contains warFor/causal./warMult/treaties/non_aggression; no test anywhere drives the strategy chooser over a treaty-bound world. This is precisely the failure mode the file's own preamble names ('referenced only by their own battery, which asked them what they returned rather than whether anything obeyed them'), reproduced one level up.
- **Verifier adjustment:** The file's war_block CONSUMER pin is legitimate, not a re-run of the failure mode it names: the READ is treatyBlocksWar (src/domain/worldPulse/treatyEnforcement.js:109) and warReasonFactor (src/domain/worldPulse/warReasons.js:671, which calls treatyBlocksWar at :679) is a separate module and the term's ONLY src consumer — exactly what the header at treatyEnforcement.test.js:13-14 declares it is pinning. The reported failure scenario (dropping or renaming the `warFor` key at settlementStrategy.js:1009) is ALSO already caught. The narrow residual gap that survives is different and much smaller: no test asserts a treaty-suppressed deployScore end-to-end, and nothing would catch a REVERSED/mis-targeted id argument in `warReasonFactor(worldState, sId, targetId)` at settlementStrategy.js:1009 — for a non-aggression pact that reversal is behaviourally invisible (the pact binds both directions, pinned at treatyEnforcement.test.js:172) but for the directional casus ledger it would silently swap which side's grievances load the march.
- **Failure scenario:** A refactor of the causal bundle at settlementStrategy.js:1009 drops or renames the warFor key (or passes the wrong target id into warReasonFactor); warReasonFactor still returns 0 in its own battery, treatyEnforcement.test.js and warReasons.test.js stay green, but the strategy chooser resumes minting strategy_deploy candidates against pact-bound neighbours — non-aggression treaties silently stop suppressing wars, and no test reds.
- **Fix sketch:** One join test in tests/domain/treatyEnforcement.test.js (or a sibling), using warIntentJoin.test.js's world() fixture idiom: a hostile pair inside the deploy band, spatialLedgers.treaties carrying a live non_aggression term, loop evaluateSettlementStrategyRules over a seed family, and assert strategy_deploy candidates naming the bound target vanish versus the untreatied control (the control producing them is the liveness anchor). Add the mirror arm: a repudiated/expired pact restores deploy candidates. This closes the last open link in the war_block enforcement chain.
- **Finder confidence:** CONFIRMED

### R-54 [LOW/ADJUSTED] §2b's three granary gaps — spoilage sink, absolute-food denomination, tick-time need following the head count — are stated as closed by the wave and remain open
- **Where:** src/domain/worldPulse/foodStockpile.js:279-488, src/domain/worldPulse/demographicsPushPull.js:296-304
- **Finding:** DESIGN_DEMOGRAPHIC_ENGINE.md §2b (accepted review amendment, binding): "The wave formalizes it as a named band and closes its three verified gaps (spoilage sink; absolute-food denomination; tick-time need following the head count)." The named band landed (reserveCoverage/RESERVE_BANDS). The three gaps did not: (1) advanceFoodStockpile has no spoilage/decay sink anywhere in the tick advance — the only spoilage in the file is the sack-transfer capture fraction (line 497), a conquest event; (2) storageMonths is still denominated in months-of-need — the sack math (computeSackFoodTransfer) has to multiply by population to recover absolute food, proving the denomination problem in-file; (3) dailyNeed/dailyProduction remain generation-frozen (demographicsRates.js:304-312 documents this as a virtue for the K ceiling, but consumption never follows the head count). Consequence now that the demographic engine MOVES population: a town that doubles by P2 arrivals keeps the same storageMonths reading though its real reserves halved — reserveCoverage (months/capacity, pushPull:301-304) overstates the buffer, RESERVE_DELAY_MAX then understates departures, and the Herald/dossier reserve horizon lies in exactly the state the engine was built to create. §11's slices P1-P4 never picked these items up and no deferral is recorded, so the doc claims a closed gap the tree still has.
- **Verifier adjustment:** All three gaps are open and §2b's claim that the wave 'closes its three verified gaps' is contradicted by the tree — that much is confirmed. But gap 3 is only half a defect: the frozen dailyProduction/dailyNeed is documented IN CODE as deliberate and load-bearing for K_food (demographicsRates.js:304-312 — 'that is exactly what makes it a genuine ceiling rather than a function of the head count, and it is why the runaway dies here'), which is the correct reading of law 2. The unaddressed residue is the GRANARY side: reserveCoverage is population-blind, so a town that doubles by P2 arrivals keeps its reading while its real coverage halves. The consequence is a tuning bias in two dials, not a state error, a conservation break or a determinism break.
- **Failure scenario:** Soak with demographicsEnabled: a famine village empties toward a fed town; the receiving town's granary silently loses half its real coverage with no ledger movement and no receipt, its departure delay and siege endurance are graded off the stale number, and the reserve-horizon acceptance reading (claim 2: reserves delay crisis) is measured against a mispriced stock.
- **Fix sketch:** Either build the promised slice — re-denominate the stockpile in absolute food units (or revalue storageMonths on every population write through one conversion owned by foodStockpile), recompute tick-time need from the live head count at the one foodLedger read-point, and add a small banded spoilage sink to the tick advance — or amend §2b to record the deferral explicitly with its risk named, so the soak redo's evidence set does not silently assume a closed gap. The single-writer ratchet for storageMonths that §0b calls 'the missing structural guard' should land with whichever is chosen.
- **Finder confidence:** CONFIRMED

### R-55 [LOW/ADJUSTED] §7b permit table is 5/6 dead vocabulary and contradicts the live P2 destination gate: 'failing' settlements can never receive the arrivals that would let them recover
- **Where:** src/domain/worldPulse/demographicsLadder.js:63-101, src/domain/worldPulse/demographicsMigration.js:293-311
- **Finding:** LADDER_MOVER_PERMITS is closed on both axes (5 grades x 6 mover kinds) and its comments claim each column gates a live mover ('destination' — "may people be sent HERE (the P2 homeostat's destination menu)"; plus promotion, levy, institution, trade). Grep proves moverPermitted has exactly ONE consumer: demographicsResponses.js:244 ('founding'). The other five columns enforce nothing anywhere. Meanwhile destinationMenuFor carries its own gate — `viable: viability.nonviable !== true` — which is STRICTER than the table exactly on the 'failing' rung: executed probe shows a village with bound 300 < tierFloor 401 and pop 500 grades 'failing', moverPermitted('failing','destination') === true (the file's own rationale: "it may still be sent people (that is how it recovers)"), yet the P2 menu marks it viable:false and competeForDestinations answers refusal 'unreachable', placed 0. Two authorities disagree about one question, the constitution's one-writer-per-concept rule inverted; and the design's recovery path for acceptance claim 9 ("low-population settlements recover, demote, or die") is amputated for the failing-nonviable rung — such a settlement can only demote or die, never be refilled by the homeostat. The unwired levy/trade/institution columns also mean §7b's 'evacuating' refusals (no levy, movers gated on the grade) are promises with no enforcement point.
- **Verifier adjustment:** The dead-vocabulary half stands: LADDER_MOVER_PERMITS (demographicsLadder.js:79-101) is closed on both axes and its inline comments assert each column gates a live mover, but moverPermitted has exactly ONE production consumer — demographicsResponses.js:244, kind 'founding'. The destination, promotion, levy, institution and trade columns enforce nothing anywhere, so §7b's 'movers gated on the grade' is a law with one enforcement point out of six. The BEHAVIORAL half is refuted: the two authorities disagree only on the 'failing' rung, and on that rung the disagreement can never change an outcome, so no recovery path is amputated and the failure scenario as written does not occur.
- **Failure scenario:** A soak realm's food shock drops a village's K_food under its tier floor; migration columns that would resettle it read the realm as 'unreachable' and route elsewhere or stay unplaced; the settlement rides tier drift down and dies even when neighbouring overflow wanted exactly that ground — the bifurcation's lower tail partially resurrected by the gate the ladder was built to open; meanwhile any future mover consulting the permit table gets answers the engine's live lanes contradict.
- **Fix sketch:** Make moverPermitted the ONE destination authority: destinationMenuFor should compute the full grade (gradeFromReading with the readings it already holds — deficit01 is available) and set viable = moverPermitted(grade, 'destination'), which admits 'failing' and refuses 'evacuating'/remnants exactly as §7b writes; then either wire the remaining permit columns at their movers (levy in the war deployment gate, trade/institution at their emitters, promotion in tier drift eligibility) or shrink the table to the columns that are real, with a walker pin asserting every retained column has a consuming call site so the table can never silently advertise enforcement again.
- **Finder confidence:** CONFIRMED

## STRENGTHS THE ARCHITECTURE CHAIR RULES LOAD-BEARING (preserve these; a fix that erodes one is wrong)
- The calibration-honesty envelope (tests/domain/brokerageCalibration.test.js) is real executed science: a 2000+-row corpus from the REAL advanceRumorLedgers over 12 seeds and two topologies, per-grade truth-rate assertions, corpus-quality preconditions, and two mis-calibrated MUTANT ladders that must FAIL the identical check — the negative control that proves the envelope measures the ladder, not the corpus. The hop-truth measurement in brokerageStamps.js's header (corroboration count does NOT predict truth because mergeArrival keeps-the-best rather than votes) visibly reshaped the design: corroboration rides the prose, never the rung. Preserve this measurement-first discipline.
- The ONE GATE law (magic economy Law 3) is held structurally, not by care: K3 and K4 import K2's readers (readMagicRegime/readMagicRegimeRecord) and re-export K2's constants as aliases rather than copies; magicFormFloor takes no economy argument and magicFormCeiling takes no settlement, so design Law 2's presence/exploitation split is enforced by function SIGNATURE and a violation requires a call-site change that reds pins.
- K1's no-orphan law is made structural rather than swept: cause PRESENCE is re-derived from live state every advance and never persisted (only sinceTick/dmSeverity/shell memory are stored), so an impairment cannot outlive its cause by even one advance; auditInstitutionStatusLedger covers the one gap (a ledger from disk), executed before and after advances in the green suite. Confirmed pinned.
- The two-sided band (hunt item 4) is handled: availableTop = max(floor, min(ceiling, tierCap)) resolves floor-above-ceiling explicitly with the floor winning (the magic guarantee the economy cannot take away), both ends are reported separately so a short skyline stays a diagnosis, and the null-floor magicless world overrides any treasury.
- K3 conservation (hunt item 5) is genuinely wired and paid: billRelief defines the ward share as the exact remainder (conservation by construction), clamps the rounding overdraw that case 15/96 of its own sweep caught, the ceiling deliberately overshoots so the cap binds rather than decorates, and calamityKernel persists the drawn charge (line 671/725) so the second-shock window is a fact about state. The receipt names what was paid beside what was saved.
- Dormancy discipline (hunt item 7) is exemplary across both programs: virtual flags absent from DEFAULT_SIMULATION_RULES, every advance returns its input BY REFERENCE when dark (byte-identity provable by object identity), drop-when-empty ledgers where a healed world equals a never-touched one (including the non-obvious base-regime-owes-dwell case), and the inline brokerage gate's mode whitelist exactly matches infoModeOf's canonical acceptance including the 'delayed' alias.
- The certification rows (subsystemRowsWaves.js) are unusually honest evidence documents: they declare empty eventTypes because the lanes are unwired, correct a prior row's prediction out loud rather than leaving it standing, and state exactly what a v5 receipt could and could not observe — the review chair should treat them as the authoritative statement of the deliberate wiring deferral.
- Single-writer discipline holds under pressure: commissionPlant PRODUCES a byte-compatible DisinfoRecord rather than writing the statecraft ledger (deposit/consume idiom), the roster-to-houses read lives in exactly one file so the ruin filter has one spelling inherited by stamps, fidelity, services, patronage and plant alike, and patronage is a derivation (genesis pick + capture ledger read) with no persisted key to go stale — capture IS rebinding, with no second mechanism to drift.
- The durable-id mint is exactly what J-D8 ordered and the reasoning is written into the code: zero-PRNG FNV-1a (structurally incapable of stealing from the pulse stream), tick INCLUDED in the mint key but EXCLUDED from the lookup key so 'minted once, deterministic, idempotent' are simultaneously true, collision handling that probes deterministically and then widens with per-candidate checks (total, never throws, never returns a taken id), and the (settlementId, rosterId, name) reverse key that defeats the positional-reroll trap. 121/121 focused tests green (executed).
- Conservation (law 6) is structural, not disciplinary: disjoint roamers/placed enforced at the READ door (placed wins on a corrupt duplicate), exactly ONE mover (moveNpcRecord) for every engine transition, drop-when-empty serialization so a drained ledger is byte-identical to a dormant one, and the never-kill law is pinned by a real anchored negative that asserts the EXACT file set naming removeNpcRecord across src/domain/worldPulse + src/store AND per-lane anchored absences — the strongest never-kill enforcement shape available short of a lint rule.
- The includeCovert seam is genuinely one seam: projectNpcPool is the only pool projection (Wanderers = world scope, Unaffiliates = same call with settlementId — the fork was structurally prevented by H1's parameter design); the public record is allowlist-assembled, never spread-and-deleted; dmTruth is double-covered by the projection allowlist AND publicSafe's PRIVATE_KEY_RE (\bdm matches dmTruth, verified in src/domain/display/publicSafe.js:97); findDmTruthPaths gives negative assertions a positive anchor; and heraldWanderers' rowOf deliberately takes NO seesSecrets flag (field presence IS the permission), so a projection-gate breach reds in the UI test instead of being masked. UnaffiliatesSection additionally narrows on playerView/publicDossier. One watch item, not a defect: HeraldWanderers claims isOwner from !!auth?.user — truth-constant under the documented 'Herald mounts only in owner sessions' posture (viewerSecrets.js header), but it is the invariant the future campaign-share surface must not forget.
- Pool equilibrium (J-D8b-i) has real teeth at the module layer: pressure exactly zero on the floor (roaming never trivially brief — §14's inversion answered), monotone and capped curve swept over 200 ticks, an EXECUTED 400-tick single-roamer run that actually settles, and a realm-scaled ceiling census. The un-wired state is not a gap: docs/DESIGN_NPC_CONSEQUENCES.md §16 records 'NOTHING IN W-H3 IS CALLED FROM THE PULSE' with an explicit obligation on the wiring slice to add the candidate types to certification aliveness in the same edit — a model deferral record.
- Trait stationarity (J-D8b-ii) is powered and adversarially controlled: a two-sided REGISTERED envelope (manifest, n=1200) carries both the effect-size lower bound and the stationarity upper bound as one pair; the negative controls are EXECUTED, not described (weight-0 collapses under the lower bound through the module's own roulette; a mutant bias that reads the cast escapes by year 100 while the real one does not); and the cure is structural — settlementTraitBias reads settlement identity and deliberately never reads npcs/members/traits, so the corruption-attracts-corruption loop is closed by construction.
- Relinquishment is the estate's alias-trap discipline done right: RELINQUISHED_FIELDS declared once with per-field rationale (including all five npcInFaction affiliation handles and the importance-drop that vacates the ladder rung through the ladder's OWN predicate), the strip walks all three homes with per-home hit receipts so the pin can prove both were reached, fail-closed on an empty roster id (refusing the purge reading AND refusing the whole verdict on an id-less entry, with law 6 cited), and the vacancy is emitted as an ephemeral contested opening — never applied, never silently refilled, and never a fourth ledger map.
- Finite semantics hold at every door hunted: closed vocabularies with fail-closed clamping (closedValue to the neutral rung), the fail-SHUT window type check that dodges Number(null)===0 twice with the reason documented both times, stricter-wins exclusion merge kept single-writer, and the verdict table exports its own domain enumeration (verdictCells) so the totality pin walks the module's idea of the domain rather than the test author's — the mountain_pass lesson institutionalized.
- Hidden-path persistence as absence-of-removal is structurally real: writeRouteNetwork (single writer, confirmed by grep — no other setSpatialLedger('routeNetwork') site exists) drops the ledger only when edges AND corridors are both empty; the only deletion API is withoutCorridors (corridors only, deliberately narrow); every edge-map rewrite in the estate (decay sweep, destruction cascade, revival, strategic apply, bypass sweep) copies every edge id through before touching any — there is genuinely no function that can delete an edge, exactly as the routeNetworkDecay.js header claims, and the decay tests pin it. Preserve this shape; the black-hole cure must be a promotion/re-charter rung, never a removal path.
- The hysteresis pair holds and cannot flap: FORMATION_TALLY 48 vs REMOVAL_TALLY 7 live side by side in one constant (routeNetworkCharter.js:116-127) so neither moves alone, and the anti-flap guarantee is structural rather than tuned — promotion needs traffic inside ACTIVE_WINDOW_TICKS (13) while demotion needs silence past demoteDwellFor's floor (39, base 78), so the two conditions are mutually exclusive by arithmetic. The demoteDwellFor comment also records catching a fresh instance of the estate's unreachable-predicate-conjunction class before it shipped.
- The three flow classes are a closed system: ROUTE_FLOW_CLASSES is exactly {goods, population, military}; all seven sources map through the single ROUTE_FLOW_SOURCES attribution table; no function takes a class as a parameter, so mis-attribution requires editing the one pinned line; and the K4 reagent lane explicitly reuses the existing unmet_import source rather than widening the table. No fourth class anywhere.
- Ports totality at genesis is structural, not asserted: portTotalityPairs ⊂ candidates.all and every water candidate mints unconditionally at PORT_TOTALITY_GRADE, so a port founded 'isolated' still gets its harbour — the invariant needs no repair pass at birth (the gap is only downstream, per the finding).
- The reputation-race integration pin is real: tests/domain/routeNetworkConsumersRace.test.js pins BOTH winners and flips the outcome by grade alone, distance alone, and listeners alone on one fixture family; the hidden-path news exclusion (NEWS_GRADE_IMPEDANCE hidden: null — an absence, not a large number) is the mechanism that makes the race winnable by the person at all, and it is correctly spelled as a refusal.
- Dormancy discipline is exemplary: every entry point returns the input worldState BY REFERENCE when dark (object-identity byte-safety, pinned), and the declared-false routeLifecycleEnabled preset key (simulationRules.js:443-456) deliberately keeps the certification totality walker sighted without perturbing preset identity — a pattern worth reusing.
- The bounded-candidate law (the B1 quadratic lesson) is enforced with numbers, not comments: genesisCandidatePairs and admissibleCorridors both export their structural bound for pins, off-network traversals are counted (offNetwork) rather than silently dropped, and betweenness/solver walks are all over the bounded lived graph.
- Every charter/decay/revival event carries an id, full address chain (both endpoint ids AND names), typed candidateType, severity, and goods/people/strategy-denominated reasons — the id-less wizard-news drop class has no habitat here, and formulas are translated, never shown.
- All 337 focused route-network tests pass on this dirty tree (11 files run in isolation), so the findings above are gaps in what is pinned, not regressions in what is.
- K_food is genuinely FLOW-only (the §2b pin holds): foodCapacityOf reads dailyProduction, dailyNeed x importDependency and the banded obligation draw through the ONE foodLedger read-point — no storageMonths term anywhere in K, reserves only ever scale departure DELAY (RESERVE_DELAY_MAX) and never capacity or destination choice.
- The §3 step is exactly as specified and pinned by executed tests (162+34 green at HEAD): every term integer via one shared integerize primitive, ONE fork demographics:<id> per settlement per tick consuming exactly two draws in fixed order, draw-accounted by a counting-fork test harness, with P2/P3/P1a deliberately on keyed hashes so the two-draw pin survives every later lane (the wave-E stream-theft law applied consistently).
- The H3 floor composes structurally in all three shrink lanes — kernel deaths, P2 departures, and the P1a decline path all draw against pop − residentNamedNpcCount — and the P1a cure is real: the decline-side integer deadband is gone when lit (fraction-as-probability, no nonzero equilibrium), the terminal lane's thorp gate widened to popToTier under the flag, and tier drift gained the viability read as an eligibility input (never a second transition writer), with the promotion mint dispositioned conserved under the same flag.
- No hidden governor: demographicsRisk is structurally isolated from realm totals (no import path to the observation module, every term a function of one settlement's own state, continuous in the head count past the pop>=5000 saturation trap), and the one sanctioned realm read (Σpop/ΣK_food) flows only into capped war-motive shares and the additive v5 observation. [QUALIFIED 2026-08-02 (self-audit): "no import path" is proven single-file and non-transitively — the scan has two on-paper bypass routes (a helper hop through demographicsRates.js, whose own import list is pinned nowhere, and a seam-local governor inside pressureModel.js, which legitimately holds the whole realm and is scanned for exactly one token). Register R-64 names both routes and the tightening; the mitigating fact — realm totals are unreachable from demographicRiskOf's call signature today — is what keeps this bullet load-bearing. The 2026-08-01 queue verdict on P4 no-hidden-governor already carried this adjustment; the two halves of the record now agree.]
- Ledger discipline is clean where it matters most: P2 rides the existing spatialLedgers.migration columns stamped with travelClass, M4's release pass explicitly skips demographic columns (migration.js:598) and vice versa, the plans ledger name is declared once in a zero-import leaf (writer/reader spelling drift pre-empted), every Herald entry carries a house-shaped id (the id-less wizard-news drop class avoided), and all three writers are conditional drop-when-empty with object-identity dormancy gates.
- The spatialLedgers conditional-namespace discipline held across every Opus-era ledger: npcLedger, npcRulings, warIntents, demographicPlans, and the route ledgers all nest under the ONE CONDITIONAL_LEDGER_KEYS entry, with total readers (absent/garbage reads as empty), drop-when-empty at both sub-ledger and namespace level, and a real versioning seam already exercised (WORLD_STATE_MIGRATIONS v2 chief→patron precedent). The H1 persistence-shape disclosure is therefore safe: old saves missing the key load clean — CONFIRMED by the passing E-C worldState ensure/persist/clone/migrate/dormancy suite and npcLedgerDormancyGolden (executed, green).
- operationRegistry ↔ compendiumData regen consistency is execution-verified over the dirty tree: operationRegistry.walker, deadOperationRatchet, and advertisedUndoArming.walker all pass (part of 95 green tests across 6 store files), and all three Opus commits (Wave D, Wave G, H4) regenerated compendiumData.generated.js in the same commit. Even the frozen mid-build TC-0 trio (displayPrefsSlice.mapSubTab + setMapSubTab registry row + regenerated compendium + updated lifecycleRoundTrip pins) is internally consistent and green.
- tests/store/lifecycleRoundTrip.test.js is a genuinely structural persistence gate: a persisted-family completeness walker (new family ⇒ red), worldState fixpoint/dormancy pins, campaign-record lifecycle, the REAL undoLastPulse walked over the campaign registry, the saves envelope, and the partialize↔merge round trip — exactly the machinery that makes the additive-key discipline (displayPrefs.mapSubTab, advanceAutoResolve) safe without version bumps. Ran green including the W1-dirty edits.
- Wave D's bilateral CREATE_ROUTE respects the half-edge invariant end to end: the store-side undo is a HARD BARRIER (BILATERAL_EVENT_TYPES in settlementSliceHelpers.js) with a written deliberate-deferral rationale rather than a partial single-row undo; the client fence (userRouteCharterInFlight), the requestedAt recovery fix, and the Immer-draft reference-equality fix (routePartnerProjectionChanged asked of live state, never a draft) all show lifecycle paths exercised rather than asserted.
- H4's verb architecture keeps single-writer discipline under DM sovereignty: removeNpcRecord is the only ledger-removal door (pinned by anchored negative over engine lanes), typed inverses ride the worldState half, the advance guard fences wholesale worldState replacement races, the rulings register lives under spatialLedgers so pulse undo restores world and saves coherently, and the session undo ring deliberately matches the pulseUndoStack non-persistence contract.
- Wave G's autoplacement store action mints no position write of its own — every coordinate lands through updatePlacement/addPlacement so the canon move-lock and refusal set travel for free (source-scan pinned), the refused act leaves zero trace including no undo entry, and the Herald record is an id-carrying, address-chain wizard-news entry (ids not names) appended through the campaign-persistence chokepoint — the idless-drop class was actively avoided.
- The treatyEnforcement.js leaf extraction is the correct architectural cure and is verifiably wired: the readiness_cap seam is real and reachable (mobilization.js:471 -> stepPosture's treatyCapped cool + the topIdx ramp ceiling), the occupation_hold seam is real and reachable (occupation.js:823-833 — the hold substitutes for physical presence AND vetoes the collapse exit), and the polarity fix (ceiling = magnitude's COMPLEMENT, harshest term governs) is documented in-place and pinned. Writer/reader payload spellings were audited end to end (parties/victorId/loserId/terms[].type/expiresTick/magnitude/complianceState/defaultedBy/defaultSeverity01) — no spelling drift between peaceTerms' writer and any consumer, and the INSTALLMENTS_PER_YEAR==TICKS_PER_YEAR duplication is pin-guarded.
- treatyTransfer.js's conservation design is exemplary: it reuses the proven sink-only computeSackFoodTransfer (floor-both-legs, credit never exceeds debit), draws only above a reserve floor deliberately tied to mobilization's own COOL_FOOD_MONTHS_FLOOR, composes same-tick multi-treaty draws through committedDebit/committedCredit so two victors cannot jointly over-drain one payer, and applies deltas through the existing single food applicator instead of becoming a sixth granary writer. Executed spot-checks of the debit/credit signs at the call site (peaceTerms.js:710-722) confirm the composition is wired correctly.
- warFrontReads.js genuinely closes the phantom-siege class for its declared scope: one provenance-gated read module, verified imported by every siege-DETECTION consumer (warDeployment, deploymentReturn, settlementStrategy, occupation, martialReadiness, peaceReasons, and six kernels), with no bypassing raw reads at siege-severity seams (the remaining raw 'war_front' counts in pressureModel are +0.08 ambient-pressure bumps over a confirmed-only index with honest reason lines, not siege claims).
- warIntent.js's determinism discipline is airtight and executed-verified: zero rng on both sides of the seam, TTL-pruned drop-when-empty ledger, byte-identical-dormant reads (input-reference return in intentTargetOrder), consume-only-earlier-tick making the apply pass order-independent, and the feasibility/posture/one-army hard gates are demonstrably NOT waivable by an order (only CONQUEST_MARGIN is). The declared one-directional import laws across the whole reasons DAG (corruptionWeb, convergence, hegemonyFear, opportunism, sacredClaim, reframeKernel, informationStatecraft, treatyEnforcement, warIntent) were grep-verified — no back-edges; the single cross-reference is a type-only JSDoc import with no runtime cycle.
- The treaty_signed beat is a model citizen of the NEWS ADDRESS LAW and the id-less-drop hazard cure: id, kind, significance/severity/score, settlementIds, and the collision-freedom argument are all written at the mint site (peaceTerms.js:887-917) — preserve this as the template when adding the missing breach/expiry beats.
- stripSuppressedDeployResidue + revertSuppressedDeployExhaustion (warDeployment.js:326-435) is a thorough counterfactual revert — deployment seed, front mint, exhaustion ratchet, per-vassal levy strain (gross-accrued to net exactly right against same-tick decay), and the DEPLOY_RESIDUE_TYPES minor emissions — the kind of dismiss-path hygiene the rest of the estate should copy.
- The epistemic-prevention machinery is honored by every new suite, mechanically verified: both walkers (negativeAssertionAnchor, seedLoopTotality) pass 16/16 over the dirty tree INCLUDING the uncommitted W1 and cartography tests — executed proof, not inference. New generation-tree tests (informationBrokerageGeneration) use collectSeedFailures/expectAbsentWithAnchor exactly as mandated, with 40-seed corpora from the REAL generateSettlementPipeline (not bare generatePowerStructure), and even document what is deliberately NOT pinned ('a pin that lies' rationale) — preserve that deferral note.
- tests/domain/townCartographyDeterminism.test.js is a model implementation of every wave-E hazard cure and should be the template for future determinism suites: 16-identity seed family via collectSeedFailures, cross-seed distinctness as the anti-vacuity half, a frozen per-stage draw ledger ({field:320, arterials:93, lanes:190, defenses:0}) that turns stream theft into a moved number, an arterial/lane stream-isolation pin driven by a lane-only input, and a structural purity scan covering the exact parity-period hole two-pass checks cannot see (including a cross-engine transcendental-float ban). Strongest test: 'the arterial stream is ISOLATED from the lane stream'.
- tests/domain/townCartographyField.test.js carries the rare genuinely-executed mutant arm: strideIndices (the replaced idiom) is defined IN the test and shown to collapse to 1 of 33 columns at the live grid width and 11 of 33 at the live city budget, while spreadIndices reaches 28 and 33 — the recorded aliasing bite reproduced at this slice's own numbers. The instrument provably bites.
- tests/domain/townSceneCartography.test.js proves every validator rule with break-then-restore negative controls (partial block, wrong schemaVersion, five dangling-reference shapes, raw colour, fractional/out-of-extent coordinates, out-of-vocabulary values on eight closed fields, conditional-key polarity both ways), plus JSON round-trip and key-order-independent digests. The vocabulary census is derived from the module source so a ninth unregistered vocabulary reds. 'A validator rule with no red is a rule that does not exist' is executed doctrine here.
- The dormancy-fence discipline is architecturally right and consistently applied: townCartographyDormancyGolden and routeNetworkDormancy both freeze pre-wiring hashes (captured at the only honest moment, before the code that could leak exists), carry ANTI-VACUITY lit arms, anchored dark-absence pins, and guard-the-guard mutant arms ('a network that materialized behind a dark gate would move the dark hash'). routeNetworkDormancy additionally pins dark-path OBJECT IDENTITY, the strongest possible dormancy statement.
- tests/domain/demographicsCure.test.js executes the revert/red/restore/green discipline in-file: REVERT-A (death floor deleted) and REVERT-B (the exact pre-cure model) are run over the identical 300-year fixture and shown to re-create the runaway and its compounding signature, then the unmocked kernel is re-proven green so a leaked mock cannot make the cure pin vacuous. Anti-vacuity anchors prove the fixture genuinely grew into its bound (plateau > 0.6x bound, y150 > start).
- tests/domain/brokerageCalibration.test.js solves the circularity problem correctly (grade reads provenance only, truth reads content only, ground truth from an undegraded run of the same feed), builds its corpus from the real advanceRumorLedgers over a 12-seed family and two witness topologies, and carries TWO executed mutant graders (inverted, flattering) that fail the identical band check with count parity asserted — plus within-house monotonicity and a margin-recording arm for future tuning. The crown-jewel claim has a real theorem-shaped test.
- The W1 suites (uncommitted) are high quality despite never having been gated: warIntentJoin's recording-rng draw-accounting instrument proves the intent read is pure on both a nothing-deploys world AND a world where a siege genuinely rolls (non-vacuous accounting); siegeArrivalGate pins both arms on the same fixture one tick apart, drives the 52-week worst-case march to resolution with besiegedFirstAt === arrivalTick as the anti-vacuity anchor, and RECORDS the deliberate coalition-siege behaviour shift in a named describe block; the peaceTerms diff upgrades stream terms from ledger-counter equality to real conserved granary movement with a no-granary negative control and load-bearing population documentation.
- The JSON-alias trap is addressed head-on where it matters: npcVerdictApply reloads every fixture through JSON.parse(JSON.stringify(...)) (reloaded() at line 94), asserts the alias is genuinely split ('the fixture must actually alias' at line 160), and pins ledger byte-identity across the round trip; npcReplacement, npcLedgerIdentity, npcLedgerState, and all four routeNetwork consumer suites carry round-trips too (grep-verified across 20 files).
- W1's structural registration duties were done, not deferred: the new spatialLedgers.warIntents key is written through the canonical setSpatialLedger accessor and classified EXEMPT in src/lib/spatialUsage.js with a full adoption-legibility rationale; the spatialLedgerCoverage walker passes (executed). The ledger itself is pinned drop-when-empty, codepoint-key-order byte-stable, TTL-pruning, and same-reference on no-op writes.
- The UI suites avoid the vacuous-absence-pin class the estate recorded: mapTabShell anchors every queryByRole-null with exact full-strip equality (tabNames() toEqual [...]) so an empty strip cannot pass an absence pin, drives the REAL displayPrefs slice and the REAL partialize→JSON→rehydrate-merge hop, and covers the stale-deep-link and silent-scene-fallback paths; mapPresentationControlledSeam's switch-removal negative is anchored by the sibling default-arm rendering the switch. All 93 cartography-program tests pass over the mid-build tree (executed).
- Eager first-paint budget is untouched — CONFIRMED by a transitive static-closure walk from src/main.jsx using the repo's own resolution logic: zero townCartography/townScene/townMap/certification modules and zero new war modules (warIntent, treatyEnforcement, treatyTransfer) in the eager closure; pulseKernel and every modified war module stay in the lazy pulse chunk; the three modified eager files (simulationRules.js, displayPrefsSlice.js, operationRegistry.js) added no imports — simulationRules gained only the dark virtual townCartographyEnabled:false key. The FP-G16 ratchet (1,020,590 B) is unthreatened at source level.
- The TC-0 lazy contract (tests/build/mapTabShellLazy.test.js) is the strongest lazy-boundary test in the estate: four layers including a transitive source-closure walk that defeats innocent-intermediary imports, and a chunk-separation assertion that closes the lazy-parent blind spot (a static child riding an already-lazy parent's chunk) — precisely the lazy-import-reparents hazard class. All source-level layers pass green in the dirty tree.
- Size ratchets moved only DOWN and stayed honest: W1 shrank all three touched at-ceiling hot files (applyWorldPulse 912→906, pulseKernel 1380→1368, warDeployment 1121→1108) by extracting to the new warIntent.js leaf while ADDING two joins; sizeBaseline.test.js passes green (3/3) against the dirty tree, so the wins are locked.
- siegeArrivalGate (src/domain/spatial/armyTransit.js:527) is the model per-tick shape: the predicate is built ONCE per war-layer pass over the live ledger, every subsequent check is O(1), and the dark path allocates a constant closure — new engine capability at zero asymptotic cost.
- warIntents ledger discipline is allocation-free when dormant: prune-on-write, drop-when-empty (whole namespace collapses), codepoint-keyed folds independent of write order, and intentTargetOrder returns the INPUT ARRAY REFERENCE when no order applies — byte-identity by object identity, not by equality.
- v5 receipts observation cost is bounded and product-free: the state-key census runs per YEAR and is deliberately shallow (top-level keys + one level into spatialLedgers); the only per-tick hook allocates nothing unless an observer is wired (advanceInterval.js:405 gates collection on typeof onTickObservation === 'function'), so the shipped engine pays zero. The townCartography certification row declares soakEvidence 'unobserved' honestly instead of borrowing a channel, and costs nothing at runtime.
- treatyTransfer.js is an exemplary lazy leaf: header declares the eager-bytes contract explicitly, conservation is inherited from the existing sink-only primitive (computeSackFoodTransfer) rather than forked, and deltas flow through the existing single food applicator — one canonical writer preserved.
- The Herald routing table (src/domain/realm/heraldRouting.js) is the strongest piece of legibility infrastructure in the estate: total over every minted token with a walker enforcing totality both ways (executed green this session), single-home by construction, an explicit catch-all, structural adjudication/divination precedence at the record layer, and every divergence from the precedent letter recorded as a named vetoable judgment. Preserve this pattern for every future producer vocabulary.
- The three register doors (heraldRegister.js, heraldWanderers.js + HeraldGazetteer/HeraldRemembrance/HeraldWanderers.jsx) are the LEGIBILITY LAW executed near-perfectly: every state is a sentence in world words, population banded to tier words, time banded to 'turnings', notoriety/competence/scandal bands rendered as what-people-say sentences with no token fallthrough, ids confined to links, audience redaction by never-building (one gate, documented), and calm in-world empty states ('Nobody wanders this realm...', 'No ruins yet. This realm is young...'). The Wanderers door's presence-not-disablement decision is right and documented.
- demographicsHerald.js is the model for engine-to-newspaper translation: a closed QUANTITY_BANDS vocabulary so no digit reaches prose, address law satisfied field-by-field (ids for the router, names for the reader, names repeated in the prose), authored emission floors so the Herald stays a newspaper not a census, and a test that joins headline+summary+reasons and scans for leaks (the no-digit idiom the war-doctrine receipts should adopt).
- The war-doctrine headline/summary VOICE is genuinely excellent where it stays prose: 'Brightwater climbs down from its war on Ashford — with honour intact', 'burns the wrong village... the atrocity bought nothing', 'Riders from X bring Y word of Z', the infowar exposure beats, the mediation receipts (mediator-name-led, pin-enforced), and treatyFrayingSummary ('The peace holds by two terms of five; the tribute frays.'). The defect class is narrow: parenthetical engine arithmetic pasted into otherwise-good sentences — cheap to cure without touching the voice.
- The 07-31 id/severity backfill discipline is exemplary receipts work: every new push site documents the admission key, collision-freedom per tick, and a rationalized severity register value, with the id-less-drop hazard cited at each site. The World Book siege fix landed with a real-collector fixture test pinning the rendered name, and the campaign-PDF twin is consistent.
- The prng fork is a pure seed-string derivation (src/kernel/prng.js:84) — sibling draw order structurally cannot steal streams; every Opus-era fork key surveyed is per-entity, with the tick either embedded or inherited from the per-tick root seed (pulseKernel.js:275). The one deliberately shared label ('war-layer' at pulseKernel.js:809/919/1077) carries an explicit written invariant, and executed greps confirm neither consumer draws the parent fork directly.
- Zero ambient impurity across all 95 Opus-era files: no Date.now, Math.random, argless new Date, toLocaleString, Intl, storage, or crypto in code (comment-only hits). All four determinism lint walls (determinismBanCoverage, localeCompareGuard, localeFormatGuard, prngForkLabelDelimiter) pass over the LIVE dirty tree, W1/TC frozen edits included.
- The transcendental-math ratchet is green and airtight over the current tree: zero new transcendental sites in any file added since 2026-07-31 (the exact-match test proves it against the working tree, dirty files included), the scanner's TREES glob covers all of src/domain recursively, and the keyed-hash substitutes (contestMath.hash01, fnv1a32) are integer-exact via Math.imul — cross-engine safe by construction.
- Codepoint tie-break discipline held at every sampled selection seam: demographicsKernel.js:268 (visit order), demographicsMigration.js:213 (pull-then-codepoint destination sort) and :510, beliefMap.js:1037/1188 (Set-to-sorted folds), informationStatecraft.js:691, brokerageServicesRules.js:339, warIntent.js:187/312, plus the routeNetwork and institutionStatus sorted walks. Remaining unsorted Object.keys iterations are order-insensitive integer accumulations or walks over ledgers whose writers sort on fold.
- Wave-P demography is a model of the house determinism doctrine: one fork per settlement per tick with documented exactly-two-draw accounting and stream-safe skips (demographicsKernel.js:56-62, 289-296); the P2 homeostat is deliberately draw-free (avalanched hash01 quantization, keyed and seedless by design with the rationale written at demographicsMigration.js:46-50); formatCount everywhere, never toLocaleString.
- The id-less news drop class is actively guarded in new code: every WIRED Opus-era emitter mints the wizard_news admission key at composition, several with explicit collision-freedom arguments in comments (peaceTerms signingBeat, informationStatecraft infowar beats, momentum climbDownNews, demographicsHerald, steadingNews, strikeNews, supplyWebWarfare, realmVerbExecution, npc* verbs, routeNetwork events).
- The wave-E stride-aliasing cure is honored: the Weyl subsampler lives in one zero-import leaf (src/domain/lowDiscrepancy.js) consumed by both steadingTopography and the W-G realm enumeration, with the single-writer rationale recorded at steadingTopography.js:61-64; demographicsLand.js:71 explicitly bans arithmetic strides.
- The frozen W1 war-join edits (dirty armyTransit/mobilization/warDeployment/peaceTerms + untracked warIntent/treatyEnforcement/treatyTransfer) are determinism-clean: pure predicate gates and codepoint-sorted ledger folds, no new rng, clock, sort-default, or transcendental sites in any added hunk.
- Byte-hygiene clean: python NUL/control-byte and mojibake scans over both trees found nothing new — the single raw NUL (supplyCompleteness.js cache-key separator) predates the review window and is an explicit owner-parked exemption pinned in controlBytes.test.js.
- The war-amendments substrate is largely real, verified claim by claim: the casus taxonomy holds 13 war reasons + 13 mirrors including opportunism and sacred_claim with belief-routed reads (warReasons.js:134-249, opportunism routed through the P4 belief selector exactly as the three-causes ruling assumes); ingratitude_debt is wired with its debt_forgiven mirror (REASON_MIRRORS:241) so amendments I2/J's 'nothing new is required' claim is true; the coup lane (coup.js), succession (successorNpc.js), and faction seat-capture (factionCapture.js) all exist for amendments D/H; SACK_POP_FRACTION's conserved arithmetic (warDeployment.js:476) is real for amendment R; and amendment S's 'no settlement treasury' claim is code-true (peaceTerms.js:201 treats treasury as an asset-class label mapping to tribute streams).
- momentum.js's published export surface matches DESIGN_WAR_RULINGS_ARCHITECTURE §1b exactly — all nine claimed exports verified present (commitmentStockOf, commitmentCoursesOf, cliffStockFor, pastCliff, abandonFloorScale, reconsiderationMultiplier, temperamentMomentumOf, faceSavingReliefOf, climbDownConsequence) — so the foreign-file read-only law is honorable as written with no new export needed.
- Amendment C's substrate warning is answerable in the affirmative: deity unseating exists as a real transition (religionState.js:362 incumbent displacement by a leading challenger; religiousContest.js re-embeds the new patron snapshot), so sacred_claim's dissolution condition can be built without inventing a pantheon coup.
- Amendment E's substrate warning is also answerable: war outcomes (not mere occurrences) are recorded today — warDeployment.js:1135 emits win/loss deltas that pulseKernel.js:1350 folds into worldState.dispositionStats (signed, saturating, reversible — losses genuinely subtract, so the existing ledger is not the forbidden one-way ratchet despite its name).
- The I2 credibility ladder is built exactly as the amendments cite it (brokerageStamps.js:105 'confirmed, corroborated, reported, tavern_talk' with measured calibration bands at :119-129 and a mutant-proven envelope), the I4 plant verb exists (brokerageServicesPlant.js over the statecraft LIE triple), H3 travel physics with mid-route positions exists (npcCirculationTransit.js, hiddenHopsOf kept as the designed optional callback), and the reputation race is pinned both ways (routeNetworkConsumersRace.js) — the four legs the envoy program (K/K2/K3/K4 amendments) stands on are genuinely in the tree.
- Route charters honor J-D9a's events-through-the-docket law precisely: routeNetworkCharterEvents.js emits proposal-mode outcomes that proposalAdmission rations under the ordinary cap with the guaranteed-admission exemption deliberately withheld, and the saturated-docket refusal is pinned rather than assumed.
- The NPC §9 population floor — deferred by H3 with a named owner slice — was actually closed by wave P: demographicsKernel.js:282 and populationDynamics.js:346 subtract residentNamedNpcCount so the engine starves the number and never the cast; the deferral chain resolved rather than dangling.
- K2's practitioner rung is built to the letter of §3b: magicForms.js counts the rung as held when EITHER a practitioner institution stands OR a tied roster character lives there, with magicFormsPractitioner.js owning the tie — the thorp's whole magic really is someone you can lose.
- The certification-row instrumentation discipline (subsystemRowsPeople.js) is exemplary and should be preserved as the house pattern: declaring dark keys FALSE in preset spreads so the totality walker can see them, refusing to declare aliveness channels that would read zero forever, and writing the honest limits of each receipt into the row itself — this is the strongest anti-greenwash machinery in the codebase and the right substrate for the WR-9 envelopes.
- The seam ruling at the heart of the envoy program (peaceTerms.js stays the single terms evaluator; transport swaps, math does not) is supported by the code as it stands — believedAdvantage already exposes the truthFor injection seam — so the largest architectural decision in the WR doc is buildable exactly as ruled.
- The lifecycle round-trip walker's completeness registry is doing its job across the new work: worldState families 'occupations' and the whole 'spatialLedgers' container are registered (tests/store/lifecycleRoundTrip.test.js:189-191), army transit and warIntents live INSIDE spatialLedgers so pulse-undo and persistence cover them for free, and TC-0's new displayPrefs.mapSubTab was registered in the very diff that introduced it.
- Certification honesty held under pressure: the dirty subsystemRowsWaves.js registers town cartography with soakEvidence 'unobserved', deliberately empty channels, and an explicit refusal to borrow the place-mover channel that would have graded it vacuously alive — the exact anti-vacuity discipline the contract exists for.
- treatyTransfer.js is a model composition citizen: it reuses the conservation-proven computeSackFoodTransfer primitive (its declared third use), routes deltas through the existing single food applicator instead of becoming a sixth granary writer, and handles same-tick multi-treaty composition via committedDebit/committedCredit.
- The shared-index commit race did NOT fire across the pause: all three post-freeze commits (99e2d54f, b3417677, daee130d) are docs-only — the frozen W1 and cartography partials were not swallowed; and the dirty _shared bundle metas are timestamp-only regen noise (sourceHash unchanged), not drift.
- The 07-30 review's live World Book bug ('Under siege' printing undefined) is verifiably fixed — realmChapterRows now reads sg.targetId, with the defect documented in-line as a pinnable doctrine note; and MapTabShell implements the WAI-ARIA tabs pattern properly (labelled tabpanel focus stop), so the a11y blind spot I probed is clean.

---

## ADDENDUM — THE CHALLENGE-FITNESS AUDIT (same day, 3 agents, measured not recalled)
Question audited: does the architecture meet (1) fraction-legibility — every experienced
receipt retellable with zero context — and (2) variety-width — the experienced fraction
provably differs across worlds?

### Measured facts (all CONFIRMED)
- **A-1 The numeric-leakage class has NO detector.** Exhaustive census of every
  prose instrument (proseLeak, spellBreakCensus, voiceMechanics, composedProseSeams,
  newsBody): detector classes cover flagKeys/ticks/rawIds/jsonFragments/software
  voice — none covers float/percent/scalar interpolation into reader strings. The
  no-raw-color visual lint has no prose sibling. The register's four scalar-leak
  findings are the live members of an UNCOVERED CLASS, not four one-offs.
  **Completion spec: a prose-numerics lint** (detector classes: float interpolation,
  percent tokens, `N×` multipliers, two-decimal scores in headline/summary/reason
  templates), same shape as rawColorLiteral + the E-E JSX corpus.
- **A-2 Receipt quality RISES monotonically era-over-era** (25 templates blind-graded,
  4 eras): June lanes worst (C/D — bookkeeping voice, rng rolls printed on reader
  cards: "Hold chance 0.62, roll 0.41"); war era bimodal (A headlines over C/D
  reasons quoting tuning vocabulary); waves C..K and the 08-01 surfaces BEST (first
  A-grade reason lines in the product's history; closed authored vocabularies
  throughout). The chair's newest-work-drifts suspicion is REFUTED; the debt is
  LEGACY-concentrated. **Completion spec: the legacy receipt sweep** — June lanes
  (wizardNews regional, stressors, coup reasons, faction-capture reasons) + war-era
  reason lines (eventProse casus scalars, momentum climb-down model vocabulary,
  treaty budget arithmetic, W1's nameless mobilization reason — one F grade).
- **A-3 Authoring-side presence is convention.** Id/address enforcement is
  resolution-side + tested-path-only; nothing statically censuses `kind:`+`headline:`
  authoring sites for id/settlementIds presence (peaceTerms shipped `parties` for its
  whole life); kind-only producers are invisible to all three routing walkers (the
  exact 10-kind void of 07-31); severity presence is a code comment. **Completion
  spec: an authoring-site census walker** (source-scan of news-emission sites →
  id + address + severity + registration required, frozen allowlist for legacy).
- **A-4 Variety is architecture-ready, evidence-absent.** The story-mix concentration
  bounds EXIST in behavioralContract (family dominance ≤0.65, ≥12 event types,
  inverse-Simpson ≥4, type dominance ≤0.55) and have NEVER EXECUTED to a verdict
  (release.json: complete:false, checks=[]; all receipts on disk are v4). The
  celebrated "divergent fates" soak check is a HASH-INEQUALITY within 5 years — one
  differing RNG draw satisfies it; it compares no story mixes. Strategy-verb
  monotony was found by HUMAN READING, not an instrument. WR-9's endings-mix +
  deciding-term histogram exist in no schema today. **Completion spec: upgrade the
  divergence probe to compare event-type DISTRIBUTIONS between seed pairs (not
  hashes), and land WR-9's fields in the receipt schema with the soak redo.**

### The chair's fitness verdict
Challenge 1 (fraction legibility): DESIGN meets it fully; IMPLEMENTATION meets it
structurally for routing/vocabulary/causal-bands, by convention for authoring
presence, and not at all for the numeric class — with the drift direction HEALTHY
(newest = best). Challenge 2 (variety width): DESIGN meets it explicitly (variety as
measured acceptance is rare and right); IMPLEMENTATION is dormant machinery plus one
weak probe — every width claim currently rests on the pending soak, and two
instruments (prose-numerics lint aside: the mix-divergence probe, WR-9 fields) must
land BEFORE that soak for its evidence to prove what the thesis needs.

### Addendum implementation receipt — A-1 + A-3 LANDED (2026-08-01)

- **A-1 is now a standing class wall.** `proseNumerics.test.js` parses the complete
  `src/**/*.js{x}` corpus and recognizes float interpolation, percent tokens,
  multipliers, and two-decimal engine scores across the authored
  headline/summary/reason/receipt seams and rendered JSX reader text. Nine executed
  mutants prove all four classes in JS and JSX, including string concatenation; the
  negative matrix proves CSS, layout/control expressions, translations, token
  humanizers, and ordinary whole world counts are not the denominator. The reviewed
  legacy ledger freezes 442 category rows at 267 exact source sites across 120 files
  by path + line + snippet. Rows can only be removed; there is no update mode. Some
  exact rows conservatively freeze legitimate numeric UI (for example money and
  composed progress text) rather than installing a reusable spelling-based escape
  hatch. The wall therefore proves **no new member of the class**, not that the A-2
  legacy prose sweep is complete.
  [CORRECTED 2026-08-02 (self-audit) — two named limits and one ratchet rule:
  **(1) THE MONOTONE-CEILING RULE (substrate missing — a regenerated baseline must
  never silently raise).** "Rows can only be removed; there is no update mode" is not
  what the test enforces: it asserts deep-equality with the committed baseline, and
  NOTHING bounds the row count — a new leak plus a regenerated
  `.prose-numerics-baseline.json` is a green suite. Baseline edits are ROUTINE, not
  rare (WR-0c @ 622a3aab rewrote 28 rows for pure line drift in peaceTerms.js and
  settlementStrategy.js), and no generator script exists, so hand-editing is the fix
  path — exactly where an added row goes unnoticed. Work item **W-A1a**: freeze
  `PROSE_NUMERIC_DEBT_CEILING = 442` beside the equality pin with
  `expect(LIVE.hits.length).toBeLessThanOrEqual(PROSE_NUMERIC_DEBT_CEILING)` — the
  A-3 sibling's own idiom (wizardNewsAuthoring.walker.test.js:80, `<= 19`) — ideally a
  per-category ceiling map too; optionally an `--update` generator that refuses to
  write a baseline whose count exceeds the committed ceiling, so line-drift
  regeneration is safe while debt growth is structurally impossible.
  **(2) THE ONE-HOP LIMIT (named limitation).** The wall proves no new member authored
  INLINE at a prose-NAMED site — not "no new member of the class". Executed probes
  against the shipped scanner: a module-local binding hop, a `parts.push` hop, a local
  function return, and a JSX intermediate variable ALL return zero hits while the
  direct control hits; and PROSE_KEYS is closed to headline/summary/reason/receipt, so
  `body`/`text`/`detail`-keyed authored sentences sit outside the denominator
  entirely. Live census: 3,044 prose-key properties, 773 reaching no string literal,
  322 resolving through a non-prose-named identifier or call the walker cannot follow.
  Work item **W-A1b** (the follow-up detector): follow module-local single-assignment
  bindings and local function returns ONE hop when the value flows into a prose key —
  newsAuthoringCensus.shared.mjs already performs exactly this local resolution
  (`staticStrings`/`collectBindings`); reuse the pattern — OR rule the naming
  convention instead (an opaque prose value is a violation unless its producer is
  prose-named). Either alone closes most of the gap; until one lands, quote this
  receipt at the scope actually proven.
  **(3) DEFINITION:** "267 exact source sites" means path+line+snippet identities;
  path+line alone counts 254. Spelled out so the 13-count gap cannot read as drift in
  a later audit.]
- **A-3 is now a standing source-side wall.** The AST census parses 696 domain JS
  files and finds 82 object-literal authoring sites that co-locate `kind` +
  `headline` across 45 files. Sixty-three already carry id + settlementIds + severity
  and a registered routing token. Nineteen legacy projection/intermediate sites in
  ten files are frozen by path + line/column + source signature + exact issue set;
  they may only disappear. Executed mutants prove all three presence fields,
  unregistered impactKind, unregistered bare kind, unresolved dynamic bare kind,
  and the local closed-choice idiom. The historical peace-terms and 07-31 kind-only
  blind spots are explicit non-vacuity controls.
  [CORRECTED 2026-08-02 (self-audit) — scope and count:
  **(1) SCOPE WIDENS BEYOND src/domain.** The census walks src/domain ONLY
  (newsAuthoringCensus.shared.mjs:250-252), but the codebase authors Wizard News
  outside it: src/store/mapSlice.js:554-572 builds a complete inline entry
  (`id: wizard_news.autoplacement.<tick>.<n>`, kind 'autoplacement', severity,
  settlementIds) and hands it to appendWizardNewsEntries. That live site is
  well-formed and 'autoplacement' IS routed, so widening adds no debt today — which
  is exactly why it is cheap now and expensive later: the next store-side emitter
  that forgets an id or a registration reds NOTHING. Work item **W-A3a**: extend
  `censusNewsAuthoringSites` to walk src/store as well — two shapes there need
  explicit judgment (src/store/campaignWorldPulseDeferred.js:779 is an undo-ring
  entry, a false positive to exclude; src/components/map/heraldFeed.js:103 is a
  read-model projection) — or, minimally, add a companion assertion that every
  non-domain appendWizardNewsEntries caller passes only domain-authored arrays
  (mapSlice.js:554 is the sole inline exception today). Until one lands, the wall
  proves DOMAIN-SIDE authoring presence, not authoring presence.
  **(2) COUNT.** "696 domain JS files" drifted the day it was written: the
  denominator is what `find src/domain -name '*.js' | wc -l` reads — 699 on
  2026-08-02 (WR-0c added treatyBreach.js and relationshipEdgeSeed.js among them).
  The wall's own assertion is the floor `>= 690`
  (wizardNewsAuthoring.walker.test.js:60), so state the floor and the measuring
  command, never an absolute. The paragraph's other counts re-verified exactly:
  82 sites / 45 files / 63 clean / 19 frozen rows in 10 files.]
- **Focused verification:** the two walls plus mutation-coverage governance pass
  33/33; targeted ESLint and `git diff --check` pass. Full-gate evidence belongs to
  the wave commit receipt, not this pre-commit measurement.

### Addendum implementation receipt — A-2 LEGACY PROSE SWEEP LANDED (2026-08-02)

- **The debt moved down, never sideways.** Wizard News regional impacts, stressor
  birth/escalation/spread receipts, coup and ruling-power outcomes, faction-capture
  transitions, war/peace reasons, and peace-term signing prose now speak through
  authored closed vocabularies. Exact pressure, probability, roll, weight, score,
  gate, origin, and treaty-budget evidence remains in typed receipt fields; it is no
  longer interpolated into reader sentences. No simulation score, RNG draw, or
  transition decision changed.
- **Every mounted reader of the protected momentum producer is closed.** The
  producer in `momentum.js` remains byte-untouched and retains exact commitment,
  cliff, and relief evidence. A shared display projection removes those scalars from
  Wizard News, the Chronicler's Letter, and Chronicle scrollback without parsing or
  rewriting persisted records. Wizard News also renders severity as the existing
  authored band and its feed/entry clocks through the existing in-world calendar,
  satisfying the Bound Book's manuscript-chip floor without inventing a second
  band or calendar vocabulary.
- **The numeric-debt ratchet tightened with the sweep.** The reviewed baseline fell
  from 442 to 401 category rows, from 267 to 240 exact source sites, and from 120 to
  112 files. The frozen ceilings are 401 total: 229 float interpolations, 79 percent
  tokens, 24 multipliers, and 69 two-decimal scores. Both the live tree and committed
  baseline must stay at or below every ceiling; unknown detector categories fail
  closed; an executed `+1` mutant proves that regenerating an exactly matching
  baseline cannot make a 402nd row green. This closes W-A1a at the post-sweep floor.
- **Residuals are named, not hidden.** The sweep deliberately leaves the separate
  supply-web warfare EV/confidence prose and peace-term draft-receipt registration
  seam for their owning slices. W-A1b's one-hop value-flow detector and W-A3a's
  store-side authoring census remain follow-up wall work; this receipt does not
  over-claim their coverage.
- **Verification:** the 18-file affected matrix passed 514/514 with domain-any,
  voice, prose-numeric, and negative-assertion governance included. The repository
  gate passed every deterministic check: 2,112 files passed, one skipped; 22,386
  tests passed, 54 skipped. Its only two reds were the common 20-second load
  ceilings in pipeline determinism and magic-buffer conjunction reachability; the
  files passed immediately alone (6/6 and 8/8). The gate began at `31ac37c3` and
  ended at `3b0ba465`; the two intervening commits changed design documents only
  (`DESIGN_OPERATOR_MESSAGES.md` and its badge amendment), not executable code or
  test contracts. No golden or snapshot was re-recorded, no flag was lit, and no
  soak ran.

### Addendum implementation receipt — W-A1b / W-A3a WALL HARDENING LANDED (2026-08-02)

- **The prose-numerics wall now follows the bounded one-hop shapes it names.** A
  prose-key value can no longer evade the detector merely by passing through a
  unique same-file `const`, a direct local function return, a local array literal
  or earlier `.push(...)`, or a string-like JSX binding. The resolver is deliberately
  lexical and one-hop: parameters and imports block shadow fall-through, reassigned
  bindings remain opaque, future pushes do not flow backward, nested returns are not
  borrowed, and imports, member dispatch, recursion, and second-hop calls remain out
  of scope. Executed positive and negative mutants pin every boundary. The live
  baseline remains exactly 401 category rows; no debt or category ceiling increased.
- **The authoring census now covers every current engine authoring root.** It parses
  699 `src/domain` files plus 72 `src/store` files and finds 84 raw candidates. The
  exact proposal undo snapshot in `campaignWorldPulseDeferred.js` is a signed,
  location-bound non-authoring exclusion; the remaining 83 governed sites span 46
  files, with 64 clean and the same 19 frozen legacy rows. The clean inline
  `mapSlice` autoplacement author is explicitly required, so store-side presence can
  no longer disappear behind a vacuous widened scan.
- **Verification:** the focused walker and mutation matrix passed 35/35; targeted
  ESLint and `git diff --check` passed. The complete repository gate began and ended
  at `6e229457`: 2,114 test files passed, one skipped; 22,396 tests passed, 54
  skipped. Production build completed, prerender wrote 311 route documents, and
  distribution verification passed 364/364. No production module, golden, snapshot,
  feature flag, lighting state, or soak artifact changed.

### Addendum implementation receipt — R-58 / R-64 / R-66 EVIDENCE PINS LANDED (2026-08-02)

- **R-58 now has the executed negative it claimed.** A ruined Gaol does not credit
  prison capacity, and a ruined criminal institution does not credit criminal
  power. The sibling anchor keeps the intentionally unfiltered half honest: a live
  criminal faction beside that ruined institution still does credit criminal power.
- **R-64 closes both hidden-governor routes without changing the engine.** The
  demographics-risk leaf still imports exactly its two known readers, and the
  reachable `demographicsRates.js` helper now has an exact seven-import contract and
  joins the P4 pure-leaf wall. At the pressure seam, Ashford's complete disease and
  conflict records are byte/deep-equal when an otherwise unrelated Brill changes
  from 40 to 900,000 people. The same source detector forbids realm-observation
  readers, realm-pressure and target/count aliases, and direct population-field
  aggregation there; its positive control bites both classes in the intentionally
  realm-aware war module.
- **R-66 restores exact-set certification totality.** The shared W-K row must name
  exactly the canonical institution-status, magic-regime, and magic-buffer spatial
  ledgers. Cardinality plus set equality means a fourth channel, a duplicate, or a
  dropped channel is now a visible reviewed change.
- **Verification:** the three-file focused matrix passed 78/78; targeted ESLint and
  `git diff --check` passed. The full repository gate began and ended at `ebc40b08`:
  2,114 test files passed, one skipped; 22,397 tests passed, 54 skipped. Production
  build completed, prerender wrote 311 route documents, and distribution verification
  passed 364/364. `gate-tail` itself reported exit 0; only the caller's post-gate
  diagnostic assignment used zsh's reserved `status` name and failed after the
  verdict, so HEAD and the log were re-read directly. No production module, golden,
  snapshot, flag, lighting state, or soak artifact changed.

### Addendum implementation receipt — R-63 NULL CAPABILITY DEFAULT LANDED (2026-08-02)

- **Null now means absent at both war-capability boundaries.** Resource pressure and
  opportunism normalize `capability01: null` through the same default-one path as an
  omitted or explicit-undefined field. The correction is deliberately nullish rather
  than truthy: a real zero still disables action, finite values still clamp, and the
  existing invalid-number fallback remains one.
- **The pins compare the whole authored result, not only the scalar.** At both scorers,
  omitted, explicit `undefined`, explicit `1`, and `null` return exactly the same
  positive `{score, receipt}` object. The zero arm returns exactly `{score: 0,
  receipt: ''}`, preventing a future `|| 1` regression from reviving an incapable
  aggressor.
- **Verification:** the focused scorer matrix passed 110/110; targeted ESLint and
  `git diff --check` passed. The complete repository gate began and ended at
  `b2274069`: 2,114 test files passed, one skipped; 22,397 tests passed, 54 skipped.
  Production build completed, prerender wrote 311 route documents, and distribution
  verification passed 364/364. No golden, snapshot, flag, lighting state, or soak
  artifact changed.

### Addendum implementation receipt — WR FLAG-COVERAGE SEMANTICS LANDED (2026-08-02)

- **Coverage now measures the release matrix instead of demanding universal
  activation.** Each of the seven WR flags must earn `ALIVE` in at least one release
  case and must earn `UNOBSERVED` in none. Another case may honestly carry
  `off` / `DORMANT_BY_CONFIG`; its `on` count remains inspectable diagnostics but is
  no longer a pass condition.
- **Both loopholes are executed closed.** A two-case fixture distributes the seven
  flags so every row is alive exactly once and dormant exactly once, and earns the WR
  property. A sibling fixture keeps one flag alive in one case but makes it unknown /
  unobserved in the other; non-vacuous war evidence still passes while flag coverage
  and the WR property fail. The existing all-silent fixture still fails because no
  flag earned life anywhere.
- **Verification:** the focused certification matrix passed 39/39; targeted ESLint
  and `git diff --check` passed. The complete repository gate began and ended at
  `b6598624`: 2,114 test files passed, one skipped; 22,399 tests passed, 54 skipped.
  Production build completed, prerender wrote 311 route documents, and distribution
  verification passed 364/364. No schema, collector, feature state, required-property
  membership, golden, snapshot, lighting state, or soak artifact changed.

### Addendum implementation receipt — A-4 INSTRUMENTS LANDED (2026-08-01)

- **Seed divergence is now a story-mix claim.** The different-seed arm aggregates
  selected-event `eventTypeCounts` over the same five-year window as the baseline
  and compares the normalized distributions by total-variation distance. It must
  move at least one tenth of the distribution **and** at least two events at the
  smaller sample size; different tempo with the same mix and one changed draw both
  fail. Composite-hash inequality remains in the receipt as diagnostics only and
  cannot earn `seed_divergent`.
- **The evidence is inspectable and source-bound.** Each child receipt records the
  thresholds, verdict, sample totals, distance, shifted event-equivalents, largest
  typed shifts, evidence validity, and diagnostic hash result. The realm-scale
  evidence digest now binds that complete `seedDivergence` section rather than only
  the older behavioral rows.
- **WR-9's v5 address — "additive" CORRECTED 2026-08-02 (self-audit): it moves the
  certification gate.** The receipt contract owns closed ending,
  termination-deciding-term (`cause`, `cost_to_continue`, `cost_to_stop`,
  `momentum`), and WR-flag vocabularies. Its validator rejects missing, duplicate,
  unknown, negative, and contradictory rows. Behavioral-contract v5 adds three
  fail-closed checks: total receipt shape, non-vacuous war evidence, and flag
  coverage. What RE-BASELINES (the original sentence "old v4 receipts remain
  readable but cannot earn the new property" understated all three effects):
  **(a) The product certificate re-baselines on the WR program.**
  `war_convergence_instrumented` was appended to SOAK_PROPERTIES and
  `CERTIFICATION_REQUIRED_PROPERTY_KEYS = SOAK_PROPERTY_KEYS`
  (certificationSchema.js:74/:85), making it a HARD requirement of every
  `certified` band — not an optional extra property. `flag_coverage` demands every
  one of the seven `WAR_RULINGS_FLAG_KEYS` be ALIVE in at least one release case and
  UNOBSERVED in none; other release cases may honestly report `off` /
  `DORMANT_BY_CONFIG`. The whole-world collector still writes deliberately-failing
  empty observations. Net effect: no band can certify until
  the entire WR-1..WR-10 program lands and lights. That is a release-posture
  change; it is recorded HERE, vetoably, for owner ratification — it was ruled
  nowhere before this correction (see the audit's unresolved ledger for the
  owner-queue routing).
  **(b) Band validation re-baselines.** BEHAVIORAL_CONTRACT_VERSION 4→5 makes any
  band NAMING v4 invalid at BOTH `certified` and `measured`
  (certificationSchema.js:214/:233) — strictly stronger than "cannot earn the new
  property". No on-disk band names v4 today (realm-scale-certification.mjs stamps
  the current constant), so no live evidence is lost.
  **(c) The genuinely additive half is the ENVELOPE.** The receipt envelope stays
  v4-readable — `SUPPORTED_SOAK_RECEIPT_SCHEMA_VERSIONS = [4, 5]`
  (behavioralContract.js:125). "Old v4 receipts remain readable" was true of the
  envelope, not of band validation or the certification gate.
- **No evidence was fabricated.** Until the ordered WR waves expose real endings,
  deciding terms, and flag-certification results, the whole-world collector writes
  zero histograms with `unknown` / `UNOBSERVED` rows. That shape is valid, is bound
  into the aggregate digest, and deliberately fails the non-vacuity and flag-coverage
  checks. The soak and lighting grid have not run; this is the prerequisite
  instrument, not a convergence verdict.
- **Focused verification:** the story-mix, behavioral contract, certification
  schema/parity, and realm-scale suites pass 61/61; the negative-assertion and seed-
  loop governance walls remain green after banking one newly anchored legacy site;
  targeted ESLint and domain-strict are green. Full-gate evidence belongs to the
  wave commit receipt.
- **Wave gate:** the complete repository run passed 22,373 tests and timed out in
  four load-heavy cases at their common 20-second ceiling. All four passed
  immediately in isolation (pipeline determinism 6/6, town-scene build contract
  20/20 with three intentional skips, feed distribution 3/3, magic-buffer
  reachability 8/8). Production build is green and `verify:dist` passes 364/364.
  No golden was rerecorded and no baseline was widened.

---

## ADDENDUM REGISTER — 2026-08-02 SELF-AUDIT (R-56..R-66)
## These are NOT new review findings ranked into the R-01..R-55 severity order. They are
## the homes for corrections the 2026-08-01 validation pass promised "in the register"
## but never landed (the dangling-pointer defect the self-audit confirmed), plus the
## work items the self-audit's chair rulings ordered. Every FABLE_VALIDATION_QUEUE
## verdict cell now points at its item; each work item carries pins so the external
## implementer builds without inventing architecture.

### R-56 [INSTRUMENT/RULED] The rumor-magnitude receipt channel — RULED IN (chair 2026-08-01) as a v5+ envelope field; spec landed here 2026-08-02
- **The gap:** the 2026-07-31 severity-register row's original re-examination instruction was unexecutable — no soak receipt carries severity, rumor, or magnitude data. Re-verified 2026-08-02: certificationSchema.js contains zero severity/magnitude fields, and none of the six artifacts/soak/*.json does either. A further correction recorded here because the Opus-era row is immutable: that row's own corrected instruction describes the envelope as holding "eventTypeCounts/moverCounts" — the validation pass verified NONE of the six artifacts contains either key; the envelope holds case receipts with no per-entry news channels at all.
- **The ruling:** a magnitude channel belongs in the envelope (three queue rows are unexecutable without it: treaty-spam trigger, band distribution under lit realms, any future register re-tune). RULED IN as a v5+ envelope field; the instrument gap closes at the soak redo, not before.
- **Spec (pins):** per-case newsKind counts plus a magnitudeBandOf 4-integer histogram over minted entries — pure counts, no prose; band cut-points are the live ones (src/domain/spatial/rumorNetwork.js:247-253: >=0.8→3, >=0.55→2, >=0.3→1, else 0). Natural landing: the WR-9 v5 receipt work (the collector already grows v5 fields). Additive for real: absence must fail nothing on a v4 receipt.
- **Veto surface:** a new envelope channel is arguably new surface, not repair — the chair ruled it in 2026-08-01; the owner sequences the soak redo that carries it.

### R-57 [NOTE/RECORDED] heraldFeed fourth-source lens — the promised adjustment: the advance lens is the FINAL-TICK window on composed advances
- **The adjustment (executed by the validation pass):** the lens rule (`entry.tick >= latestPulse.tick`) is exact for single-tick (one_week) advances, because pulseRecord.tick = worldState.tick per kernel tick (pulseKernel.js:1588). For a COMPOSED one_month/one_year advance, collapseIntervalHistory keeps only the final tick's pulse record, so the advance lens shows ONLY final-tick beats — executed probe: surviving pulse records at ticks 3 and 7, late-lane beats at 4,5,6,7 → advance lens [7], campaign lens [4,5,6,7]. "tick >= latestPulse.tick IS this advance's beats" over-claims on composed advances.
- **Disposition:** a semantics note, NOT a bug — the final-tick window is exactly consistent with what the collapsed pulse sources themselves show under the advance lens (interior beats deliberately live in the chronicle). If whole-interval semantics are ever wanted, the filter is `entry.tick > previousSurvivingPulse.tick` — one line, plus a composed-advance pin in tests/components/heraldFeedSources.test.js. Everything else in the queue row re-confirmed: the fourth source is genuinely read-only, and the persisted-digest road stays closed (the fourth-source commit touched no kernel file).

### R-58 [TEST WORK ITEM] npcVerdictTable ruin filter — the missing executed negative fixture
- **The fact:** the claim "a ruined gaol does not credit hasPrison" is proven by source reading only. tests/domain/npcVerdictTable.test.js:307-325 exercises hasPrison/hasCriminalPower with STATUS-LESS institutions exclusively; ruinFilter.probe.test.js has no npcVerdictTable coverage; and because the file is fully routed it DROPS OUT of ruinFilterRoster.walker's discovery set (walker header :33-34), so walker 3/3 green does not attest it either.
- **Work item (pins):** one behavioral pin — `hasPrison === false` over a roster holding a `{name:'Gaol', status:'ruined'}` institution — plus the sibling arm for the institution half of hasCriminalPower (a ruined criminal institution is not credited; a live FACTION still is — the deliberately-unfiltered half stays pinned live as the anchor). Timing: at latest with H2's pulse-wiring slice; cheap enough to land any time.

### R-59 [CANDIDATE SWEEP/RECORDED] FP-G17 — the residual ESD candidate-lazy set (module bytes only)
- **The measured set (validation pass, executed reachability split):** of 35 post-excision ENGINE_SHARED_DOMAIN members, TEN have no static path from the true first-paint graph. Two are already routed lazy by the settlement-normalize pin and are classifier-only (normalizeSettlement.js 10,897B; settlementMigrations.js 7,384B). EIGHT ride eager engine-core with no natural first-paint consumer: content/customDefinitionIdentityProjection.js 5,490B · generationContentProfile.js 6,778B · generationOwnership.js 10,288B · historyPreservation.js 9,156B · regenerationPolicy.js 4,903B · regenerationPreservation.js 13,452B · resourceSemantics.js 15,102B · resourceTerrainCompatibility.js 5,512B — ~70.7kB source, MODULE BYTES ONLY (their resourceData import stays eager regardless).
- **The rule that governs any excision (the FP-G11 lesson, restated in FP-G16's own note):** excise AND pin, or the orphan co-locates into the big engine chunk (vite.config.js:145-148, :548-551; the settlement-normalize pin matches before isEngineSharedDomain at :649). Each candidate needs its own consumer verification before excision; minified+gzip reclaim is less than source bytes.
- **Status:** recorded candidate sweep, same class as FP-G16 (reclaim, not raise) — not yet authorized work.

### R-60 [NOTE/RECORDED] I2 — "trades corruptly" is narrative today, not mechanical
- **The fact (executed by the validation pass):** house status feeds NOTHING downstream of the liveness gate. houseChannelCompetence reads only {legality, form}; the stamp payload is {grade, channel, label, detail, band} — no house identity, no status term, no competence discount (brokerageStamps.js:473-501; `grep -i corrupt` over brokerageStamps.js + brokerageFidelity.js → exactly one comment hit). An IMPAIRED house therefore stamps byte-identically to a healthy one, and the stamp is settlement-anonymous, so a player cannot attribute a stamp to the corrupt house even after the exposure beat.
- **Why this is not a defect:** calibration honesty holds precisely BECAUSE no corrupt skew is injected; the I2 liveness pin preserves exactly the right substrate. The "corruptly" half of the queue row's line is an IOU.
- **Forward decision (Fable, when an I3+/Herald surface ships):** whether a revealed-corrupt house's contribution wants a mechanical expression (competence discount, house-attributed stamps, or neither — priced explicitly).

### R-61 [DOC WORK ITEM] The §0 weekly-tick footnote — ordered text, verbatim, for DESIGN_DEMOGRAPHIC_ENGINE
- **The defect:** DESIGN_DEMOGRAPHIC_ENGINE.md §0's "at one_year intervals (the soak's own configuration)" (still unfootnoted 2026-08-02) conflates the harness's DECLARED interval with the EVALUATION interval. The §0 table's arithmetic itself was independently re-derived and stands.
- **The footnote, verbatim (that doc is outside this audit's file assignment — whichever session next edits it lands this):** "†The soak's DECLARED interval is one_year, but the orchestrator decomposes every interval into one_week kernel ticks, so the in-vivo evaluator ran WEEKLY. Same defect, same attractor — but the in-vivo freeze regime is weekly ticks + crisis modifiers, and tuners must model THAT path, not the one_year table above."
- **LANDED 2026-08-02 (self-audit, coherence sweep):** DESIGN_DEMOGRAPHIC_ENGINE §0 now carries the footnote verbatim (the doc was edited the same night, which per the clause above made landing it that editor's duty; the sweep closed the gap). This item is CLOSED.

### R-62 [CORRECTION/RECORDED] P1 acceptance row — the two overstated numeric details, corrected
- **(1) Six settlements, not 8:** the cure fixture spans SIX settlements across six tiers, both at d620a05d and at HEAD (tests/domain/demographicsCure.test.js REALM lists exactly Ashford/Brackwater/Cairnhold/Dunmarch/Elderfen/Fallowmere; the commit message's "eight settlements" repeats the error).
- **(2) The 0.5% literal:** "y150==y300 within 0.5%" does not hold at HEAD — executed 300y run: Brackwater 1.827%, Cairnhold 2.128%, Elderfen 0.938% (small-N integer noise); only the three large settlements sit within 0.5%. The PINNED tolerance is 5% (demographicsCure.test.js:128).
- **Disposition:** the plateau claim and the acceptance verdict survive untouched; neither the "8" nor the "0.5%" literal may be quoted forward.

### R-63 [LATENT HAZARD + WORK ITEM] capability01:null zeroes war scores instead of defaulting to 1
- **The hazard (executed by the validation pass):** the default-1 idiom treats null as 0 — Number(null)=0, Number.isFinite(0)=true ⇒ capability=0 ⇒ every resource_pressure/opportunism score for that pair silently zeroes. Idiom sites: warReasons.js:533 and opportunism.js:256. LATENT, not live: current callers pass only undefined or a real number (warReasons.js:849-856 omits the key when demoTerms is null; :885 passes demoTerms?.capability01; opportunism.js:324).
- **Work item (pins):** guard null explicitly at both sites (null ⇒ absent ⇒ default 1) — the estate's fail-shut Number(null)===0 discipline (the window-type-check precedent). Pin: a score computed with capability01:null byte-equals the score with capability01 undefined, at both call sites. Cheap, dark-safe; land before any new caller appears — this codebase's writer/reader payload-spelling history is exactly how a null gets passed one day.
- **LANDED 2026-08-02:** both scorer boundaries now use nullish defaulting, with
  full-object equality across omitted/undefined/one/null and a separate true-zero
  disabling control. See the implementation receipt above. This item is CLOSED.

### R-64 [TEST WORK ITEM] P4 no-hidden-governor — the two on-paper bypass routes, and the tightening
- **ROUTE A (helper hop):** the import pin (tests/domain/demographicsWorldsHand.test.js:307-308) pins module SPECIFIERS of demographicsRisk.js only. demographicsRates.js — one of the two allowed imports — has its OWN import list pinned NOWHERE, and the P4.6 pure-leaf scan covers demographics{Ladder,Risk,War,Observation,Herald}.js but NOT Rates. A realm-aggregate helper added to demographicsRates.js under an innocent name passes every existing assertion.
- **ROUTE B (seam-local governor):** pressureModel.js legitimately holds the WHOLE realm and is scanned for exactly ONE token ('measureRealmDemography'). An inline reduce over settlement populations needs no import and no forbidden token; the dark-arm byte-identity check is gated on demographicsOn, and the lit-arm fixture holds ONE settlement — realm total equals settlement population, so the fixture structurally cannot see a governor.
- **The mitigating fact (why the STRENGTHS bullet stays load-bearing):** within current call signatures, demographicRiskOf receives {settlement, worldState, settlementId, scores}; realm populations are never passed and are not persisted in worldState. The scan blocks the whole accidental-coupling class; defeating it requires a deliberate two-site edit no token scan can bar.
- **Work item (the tightening; test work only, no engine change):** (a) pin demographicsRates.js's import list — i.e. make the pinned surface the transitive closure of demographicsRisk's two allowed imports — and add Rates to the pure-leaf scan; (b) grow the pressureModel lit-arm fixture to ≥2 settlements with distinct populations so a seam-local realm total is observable, and widen its token scan beyond the single name (keep the guard-the-guard arm proving the scan still bites in demographicsWar.js).

### R-65 [RULING SPEC/EXECUTABLE] Neighbour-mirror INJECTION — the full ONE-REGEN reference (chair ruling 2026-08-01, spec landed 2026-08-02)
- **THE RULING (vetoable; option (c) of the Opus escalation):** when the faction category is occupied, the neighbour faction still LANDS — injected, distinctly labeled, at dampened weight (×0.5 of the band the emergent mirror would have drawn), under a per-settlement cap of ≤1 injected mirror + ≤1 injected opposition. Option (a) emergent-suppression is the EXPLICIT INTERIM STATE until the batch.
- **MEMBERSHIP:** the ONE REGEN batch (golden-shifting; the owner lights it).
- **Blast-radius facts (carried from the ruling):** the step draws from a per-step rng fork; un-neighboured seeds are bit-identical; consumers are duplication-aware. The `source: 'neighbour_mirror'` field already distinguishes mirrors — the injected-under-occupation form must remain distinguishable from the emergent form (a distinct source value or an `injected: true` mark; the ruling requires distinct labeling, and the acceptance pins below need the handle).
- **SCOPE (as corrected 2026-08-01):** the cliff is city-specific + town-MIRROR-channel only. The two era measurements reconcile as DIFFERENT CHANNELS: the Opus row's "0 of 60 at town" measured MIRRORS; the chair's "23/60 hostile, 14/60 rival at town" measured OPPOSITION, which stays live. Provenance caveat: the 23/60 and 14/60 figures are chair-session measurements with no persisted command — the slice's acceptance must RE-DERIVE the interim distribution (seed sweep over the standard 60-seed family at town and city) before pinning the post-injection one.
- **The coupling fact the implementer must handle:** a landed mirror does `existingTypes.add(fType)` (src/generators/steps/neighbourFactions.js:61) BEFORE the opposition loop reads the same set (:80-81) — injection order couples the two channels.
- **OPEN PIN OP-1 (chair to rule before dispatch; do NOT invent):** whether an INJECTED mirror consumes its category's opposition slot via the shared existingTypes set. Escalated 2026-08-02 with a proposal in the self-audit's unresolved ledger.
- **Acceptance pins for the slice:** per-settlement cap enforced (≤1 mirror + ≤1 opposition injected); dampened band asserted at ×0.5 of the emergent band; distinct labeling asserted; un-neighboured-seed bit-identity control; the pre/post distribution re-derivation above; and the OP-1 ruling pinned whichever way it lands.

### R-66 [TEST WORK ITEM] K3's certification-pin widening — restore exact-set totality
- **The fact (the batch-6 retro judgment the pass missed, now recorded):** e2614d4f widened tests/domain/institutionStatusCertification.test.js's "ONE declared channel" pin from EXACT equality to `toContain` + a shape loop (in-test note "AMENDED BY K3"), because K3 added spatialLedgers.magicBuffer as a second declared channel. ACCEPTED as the honest in-flight amendment (the equality claim had become false); compensating pin: tests/domain/magicBufferIntegration.test.js.
- **Work item (pins):** restore an exact-SET assertion over the UNION of declared W-K channels — e.g. `expect(new Set(ROW.aliveness.stateKeys)).toEqual(new Set([the institutionStatus ledger key, the magicBuffer ledger key]))` with every future declared channel added HERE by name — so the totality claim regains teeth and the next widening is a visible one-line diff, not a silent containment pass. Test work only.
