# DESIGN — FP-INFORMATION (the medium's volume: mind + voice)

## Fable 5 architecture, drafted 2026-08-02 for the Foreign Policy program.
## BOUND BY docs/DESIGN_FP_SPINE.md (the constitution — where this volume and the
## spine conflict, the spine wins and the conflict is a bug to report). Format and
## discipline mirror DESIGN_WAR_RULINGS_ARCHITECTURE.md; the war volume's §10
## implementer protocol binds here verbatim. Implementation is assigned to the
## external implementer (Sol); this document is self-contained enough to build
## every wave without inventing architecture.

**Status: ARCHITECTURE. Nothing here is scheduled until the owner sequences it
(spine §5: SP → GRAMMAR → INFO → …). Every wave ships DARK. The §6 judgment
blocks are the drafting chair's rulings under delegation — vetoable here.**

**The thesis, from the three-survey substrate (2026-08-02, executed against
minifold @ claude/composite-r4):** the medium's PHYSICS are already at war's
grade — in one respect above it (the reliability ladder is the estate's only
surface with an executed calibration theorem, 18,377 real arrival records,
brokerageStamps.js:22-130) — but the medium has almost no MIND and almost no
VOICE. The deliberative chooser enumerates zero information verbs
(settlementStrategy.js:576-728 — war/peace/archetype levers only); second-order
belief is structurally absent; the signature read (the reputation race) has zero
consumers; the bought lie charges the buyer and plants nothing (PLANT_WIRING
reported-not-made); and the whole knowledge lane speaks 4 beats among 269 routed
Herald tokens (~1.5%, war carries 68) — a medium that only talks at the moment a
secret dies. This volume adds almost no state. It is reads, wirings, acts, and
narration: a mirror (second-order belief), a bait (the lure), a counter-game
(deliberate counter-intelligence), a road (couriers and the race), a voice (the
spy's arc and the knowledge desk), and a measure (decontamination + envelopes).

**Settled crux rulings this volume elaborates and never re-opens (spine §3-INFO):**
THE LURE (lies that bait, not only deter — planted weakness, planted wealth,
planted devotion; bluff-vs-bluff as the program's signature tragedy) ·
PLANT_WIRING lands · the reputation race gains consumers (outcomes keyed to
who-knew-first) · second-order belief ships as SP-2's derived heuristic, never
recursion · counter-intelligence as deliberate agency joined to SP-4 · narration
from 1.5% to war-class with the spy's ARC.

**Reading order for the implementer:** DESIGN_FP_SPINE.md → this document top to
bottom → DESIGN_INFORMATION_STATECRAFT.md + DESIGN_INFORMATION_BROKERAGES.md (the
built substrate's own law; where this volume and a built module's recorded owner
ruling disagree, STOP and report) → DESIGN_FP_COUPLINGS.md for every coupling
walked here by pointer → DESIGN_WAR_RULINGS_ARCHITECTURE.md §10 (protocol).

---

## §1 THE LAWS THAT BIND EVERY WAVE

### 1a Constitutional (inherited verbatim — the war volume's §1a plus the spine's twelve)
Same-seed byte identity · dormancy behind virtual flags with goldens captured
FIRST · seeded purity (no transcendentals in engine paths) · monotone ratchets ·
receipts carry enforcement (id + address chain + typed action + settlements by
name + reason) · premium isolation and audience projection · finite semantics
(closed vocabularies, banded, no float on a surface). The spine's twelve
requirements are the density contract for every mechanism below.

### 1b Medium-specific laws (each grounded in an owner ruling or a built idiom)
- **LAW ONE, MEDIUM FORM:** the engine models what courts BELIEVE and what the
  record SUGGESTS — never a nested mind, never a truth the DM has not authored.
  Second-order belief is a DERIVED banded heuristic over the outbound record
  (spine SP-2, settled); a stored "what B believes" map is a design defect.
- **THE ANONYMITY / NO-FATES BOUNDARY (owner ruling 2026-07-19, recorded at
  informationStatecraft.js:43-50):** every exposure is a REPUTATION cost — no
  one is removed, killed, or dissolved by an information event. The spy is
  burned, never hanged by the engine; the accused house is shamed, never
  shuttered. Downstream falls happen through the existing legitimacy/coup
  machinery on their own law. ENFORCEMENT, not convention: no house-voice
  string in this volume may apply a fate verb (hanged/killed/executed/exiled/
  shuttered) to a named soul — a phrase scan over the volume's rendered
  strings pins it (the same shape as the address-law scans). POPULATIONS'
  R-28 headline-honesty class is ADOPTED volume-wide: a headline may never
  assert an event that did not happen; the sentence's verb must be entailed
  by the receipt's counts. [CORRECTED 2026-08-02 (fp-audit)]
- **THE ONE-SUBSTRATE DISTANCE LAW:** five independent consumers price the SAME
  physical distance through one shared reader (latency hopWeeks, recency
  surcharge ×0.5, route impedance, fidelity attenuation 1/(1+0.75×delay), query
  counter surcharge — survey-verified). No wave may mint a sixth distance price;
  everything that moves rides the transit kernel and law M's one-week floor
  (SP-7 asserts the time base).
- **THE ASYMMETRY OF TRUST IS PRESERVED:** credibility rises at 0.6 and falls at
  5 on a 52-tick half-life (CREDIBILITY_TUNING, survey-verified) — trust builds
  in years and dies in an afternoon. No mechanism in this volume may make lying
  cheap: every new lie surface prices exposure through the SAME credibility
  stock and the SAME blowback triple (credibility charge + E1-machinery
  grievance + legitimacy prose). One stock, one afternoon.
- **THE ANTI-HUM LAW (the anti-whisper-war ruling, recorded as a deliberate
  absence at informationStatecraft.js:1444-1472):** no autonomous act spammer.
  Every new act is trigger-only, cooldown-gated, per-tick capped — the
  INTEL_TRADE_TUNING idiom (26-week pair cooldown, 12/tick cap) is the house
  shape. A quiet decade must stay quiet.
- **K3'S KINSHIP — STRUCTURAL TRUTH-READ ENFORCEMENT:** every new evaluator that
  claims to be belief-side gets the war program's structural treatment: pinned
  import list, source token scan for true-state reads, and the guard-the-guard
  positive control proving the scan bites on a module that legitimately reads
  truth. Convention is not enforcement.
- **THE CONTAMINATION-HONESTY LAW (knowledgeLaneEvidence.js's discipline,
  survey-verified):** no certification row claims the knowledge mover family
  while the residual lists are non-empty; every NEW beat this volume mints
  classifies knowledge on its OWN vocabulary, never through the bare `news`
  token — and the residual lists only shrink (IN-6's ratchet).
- **THE PACING GOVERNOR IS THE EDITOR (spine §1.12):** every news class below
  registers with pacing/significance machinery. The medium graduating from 1.5%
  to war-class density must never become wallpaper — flow control by law.
- **THE TWO-TIMESCALE ECHO, MEDIUM FORM (spine §1.9):** the fast layer is the
  arrival — the week the word lands, the beat fires; the slow verdict is the
  stock — credibility (52-tick half-life), the grievance ledger, the war a lie
  started, concluding over years. Every wave below names both.

### 1c Recorded hazards that WILL bite these waves (each has bitten this estate)
- **Id-less news drop:** every `newsEntries.push` carries `id`; new kinds
  register in WHAT_PHRASES + heraldRouting or totality walkers red.
  THE MINT-TIME REGISTRATION RULE [CORRECTED 2026-08-02 (fp-audit)]: every
  wave registers its OWN kinds — WHAT_PHRASES + heraldRouting + the pacing
  governor + the knowledge family's OWN-vocabulary token registration
  (BEHAVIORAL_MOVER_FAMILIES.knowledge grows the new kind tokens, stated per
  wave) — in the SAME commit that mints them, or that wave's own gate reds on
  the totality walkers. IN-5(f) is NOT the registration home: it changes only
  the SECTION already-registered kinds route to, plus the desk itself. Each
  minting wave's pin list carries "registration complete for kinds minted
  this wave."
- **Writer/reader payload-spelling drift:** every new consumer of an existing
  ledger gets a pin that boots the REAL writer and reads through the REAL
  reader (the plant fold and the disclosure executor are exactly this shape).
- **Vacuous absence pins:** a `toHaveLength(0)` against harness-default-empty
  state proves nothing — the mirror's and the race's negatives seed non-empty
  state first.
- **Unreachable predicate conjunctions:** the lure's spring gate, the sweep's
  catch gate, and the race's person-first arm each get a reachability pin on
  a real generated corpus (a seeded failure count is a LOWER bound).
- **JSON-alias trap:** couriers and double agents are roster npcs in memory —
  every errand fixture JSON-round-trips.
- **Stream theft:** new keyed forks are per-entity (`plant:<plantId>`,
  `sweep:<settlementId>`, `race:<arrivalId>`), draw-accounted.
- **Parallel-load fake reds:** the survey itself hit this class (focused vitest
  errored under 8 live workers); isolation re-run before diagnosis, always.

---

## §2 SUBSTRATE CENSUS (from the three executed surveys, 2026-08-02; every row
## carries file:line evidence in the survey journal; re-verify anything you build
## on — live code outranks this table; anything asserted beyond these rows is
## marked VERIFY-AT-BUILD)

| Substrate | Where | State |
|---|---|---|
| Belief maps: per-observer, per-faction-slot, decay/update/forgetting laws, fog-preserving seed sentinel, misjudgment detection | `beliefMap.js` (1,458 lines; BELIEF_TUNING: SILENCE_DECAY 0.92/tick ≈ 8-tick half-life, MIN_CONFIDENCE 0.03 prune, HOP_DECAY 0.75; reconcileBelief :485-550; detectMisjudgment :1362-1386), wired pulseKernel.js:1821 | BUILT, rigorous — SP-2's foundation |
| Rumor physics: six carriers, hop latency (`arrivalTick += hopWeeks`, seasonal), per-hop weathering with 0.05 fidelity floor, 12% name-swap to REAL ids, TTL 26/13, top-K 24, echo-independence cap 1 | `rumorNetwork.js`, `migrationRumors.js` (banded magnitudes, 80/300 floors) | BUILT |
| Distance priced once through one shared reader; five consumers incl. the player rumor display | `distancePricedNews.js` (hopDelay = floor(hopWeeks×0.5); V-24b impedance), `settlementRumors.js:574-590` | BUILT |
| Brokerage stack I1–I4: measured stamp ladder (truth bands 0.90/0.60/0.40/0.15 under an 18,377-record corpus; hop count predicts truth 1.000→0.465, corroboration does NOT), fidelity floor competence/(1+0.75×delay), query priced in patron political capital (cap 0.30, five-token refusal, claimFrom the ONLY claim constructor), bounded feed (≤0.6×competence; confidence never outruns accuracy), derived patronage (no persisted key), plant act charging the patron | `brokerageStamps.js`, `brokerageFidelity.js`, `brokerageServices.js`, `brokerageServicesFeed.js`, `brokeragePatronage.js`, `brokerageServicesPlant.js`, `brokerageServicesRules.js`, wired candidateEvents.js:5 | BUILT — with the ONE recorded unwired seam: **PLANT_WIRING** (`brokerageServicesPlant.js:87`: "informationStatecraft.processLies: fold commissionPlants() into nextDisinfo") — the bought lie charges the buyer and plants NOTHING |
| Intel gift/sale (D-3): gift priced by the teller's SACRIFICE, sale by fidelity×stakes credibility-discounted, self-policing ±2 bands at courier arrival, 26-week pair cooldown, 12/tick cap | `intelActs.js`, consume arm informationStatecraft.js:1334-1399 (a ONE-TICK deposit courier — the anti-law-M shape IN-4 retires) | BUILT |
| M9b ally-intel with alignment-styled deception (evil paints hostile at full presented confidence; chaotic garbles ×0.5; lawful ×0.95) + the turned-ally leak at 0.9 | `beliefMap.js:801-1009` | BUILT |
| SEE / HIDE / LIE movers: paid covert sight (upkeep = prosperity affordability GATE, accuracy floor 0.85), secrecy hysteresis on paranoia+weakness pressure, the garrison bluff (+2 bands at 0.7×credW; expose on 2-band contradiction or 8-tick age; blowback triple), credibility stock (rise 0.6 / fall 5 / half-life 52 / weight [0.35, 1.15]) consumed by four independent readers incl. the Blainey discount (wars against proven liars converge slower — pinned) | `informationStatecraft.js`, wired pulseKernel.js:1875 | BUILT — with recorded deferred seams at the file foot (:1444-1472): **HIDE's trade tax** (narrated, unwired), SEE gate-not-drain, the disclosure DEMAND lane, the deliberately-absent SELL spammer |
| Compelled intel: 'disclosure' peace term minted + recorded, executor `'seam'`; intel is an appraised war-spoil asset class (merchant tilt 1.2) | `peaceTerms.js:180,381,492`; seam notes informationStatecraft.js:1460-1464 | PARTIAL — minted, strains, expires; the feed effect never executes |
| Foreign-asset scandal: exposure fires the full blowback triple + exposedCorruption casus ledger + corruption_exposed condition with named reason; organic exposure ousts and reseats | `pulseKernel.js:540-627`, `corruptionWeb.js:891-960`, warReasonTaxonomy.js:18 | BUILT — but no composer joins secret → break → fall (the beats are disjoint) |
| Irony surfaces: dramaticIronyBrief (cured dead branch), settlementBrief "What they say (vs. what is)", WarCausalBrief's dying-war line, council_schism condition, rumor-mill voices for all four beats | `composers.js:262-318`, `BeliefDivergenceBand.jsx`, `WarCausalBrief.jsx`, pulseKernel.js:564-591, settlementRumors.js:111-132 | BUILT — **BeliefDivergenceBand v1 ships WITHOUT its truth join** (documented follow-up in-file): it shows belief, not wrongness |
| The reputation race: deterministic person-vs-story race over the lived network, RACE_OUTCOMES {person, story, together, neither}, pinned | `routeNetworkConsumersRace.js:322`, tests/domain/routeNetworkConsumersRace.test.js | BUILT, **ZERO consumers in src/** (executed grep) — "the thing the machinery is for" narrates nothing |
| Knowledge-lane certification honesty: exactly ONE dispositive literal (belief_misjudgment); 15 impactKinds + 7 kind-only producers reach the family only through the bare `news` id token; per-year beliefDivergence envelope (relationship+faith axes only — **the strength axis the LIE manipulates is a declared gap**); the lit/dark differential instrument is asked for and absent | `knowledgeLaneEvidence.js` (:12-29, :101-147, :159), behavioral-observation.mjs:630-717, subsystemRowsRegen.js:59,79 | BUILT (the honesty), ABSENT (the instrument) |
| Herald presence: 4 knowledge-native beats / 269 routed tokens (~1.5%); no knowledge section; beats scatter war/war/trade/faith; belief_misjudgment filed under the FAITH desk (word-association misfile, walker-pinned); all four late-lane only; ceiling 'notable' | `heraldRouting.js:110,118,140`, heraldFeed.js:163-178 | BUILT, THIN — war carries 68 tokens and a section |
| The chooser: pure information CONSUMER (belief-wrapped strength, belief-aware siege, misjudgment stamping on deploy) — enumerates NO information verb | `settlementStrategy.js:65-71,227-268,310,576-728,843` | The verbs live in parallel autonomous movers only |
| Sight postures ledger | `spatialLedgers.sightPostures` | BUILT, **no UI consumer** — the DM cannot see standing espionage even as DM truth |
| Covert patronage / live-plant projections | `brokeragePatronage.js:319-328`, `brokerageServicesPlant.js:369-373` | BUILT, **zero non-test consumers**; covert patronage has NO organic exposure producer — the 'exposed' list is a parameter nothing supplies |
| Gate stack: seven dark virtual flags, none in DEFAULT_SIMULATION_RULES; NO completed soak case lights `informationBrokeragesEnabled`; the completed soak's "knowledge: 28" was measured contamination | `infoStatecraftEnabled`, `informationBrokeragesEnabled`, `distancePricedNewsEnabled`, `allyIntelSharingEnabled`, `intelTradeEnabled`, `beliefAxesEnabled`, `migrationRumorsEnabled`; subsystemRowsWaves.js:573-610 | The whole medium is soak-unobserved; knowledge-lane starvation is FIRST on the owner-held tuning agenda (DESIGN_REALM_DIRECTIVES.md:49) |

**Verified ABSENT (the surveys' executed negatives — NEW WORK, each homed in a
wave; never build on these as if they existed):** any second-order belief
structure (the belief schema stores world-attributes only; every deception
targets minds through engine omniscience) · any reputationRace consumer · the
plant's fold into the disinfo lifecycle · a brokerage_intercept consumer (no
metadata reader; narrative-only) · a house-side chooser (acts are fnv1a32
rotation-picked; houses never decline on risk-calculus) · HIDE entered as a
RESPONSE to detected espionage (entry pressure is generic paranoia+weakness) ·
any informational casus belli (the 13 WAR_REASON_TYPES carry none; corruption_
exposed is revelation-as-grievance) · an inaction receipt (the pulse narrates
selections, never refusals-to-select) · any arc machinery (the spy is one tick,
exposure-only) · a knowledge Herald desk · the strength-axis divergence measure ·
the lit/dark differential harness · WR-7d's SEND-TWO / envoy-vetting reader
(SPEC-ONLY in DESIGN_WAR_RULINGS_ARCHITECTURE.md — executed grep 2026-08-02:
zero `envoyErrand|sendTwo|SEND_TWO` hits in src/) · any non-strength belief
axis for lies (DisinfoRecord is strength-typed in its own fields,
informationStatecraft.js:500-509; the exposure comparator reads
`belief.strengthBand` exclusively, :626-628; `CHANNEL_BELIEF_AXES.trade` is
frozen EMPTY — BELIEVED SCARCITY / CONDITIONS / DEVOTION are SP-2 work that
does not exist yet). [CORRECTED 2026-08-02 (fp-audit)]

**Cross-volume bug notes (report, not build — spine: conflicts are bugs to
report):** (1) `peaceTerms.js` avgTieStrength (:1231-1241, consumed :852-856)
reads `relationshipStates.trust` ground truth while its docstring says
"believed" — a comment/code mismatch on a K3-adjacent read; flagged to the
validation chair for the GRAMMAR volume's K3 sweep. (2) Test greenness for the
~26 information-layer test files is PLAUSIBLE only — the survey's focused run
errored at server startup under 8 concurrent vitest workers (the recorded
fake-red class); the implementer's first act in IN-0 is an isolation-verified
green baseline of the info suite.

---

## §3 THE FLAG FAMILY + THE THREE SEAM CONTRACTS

Four new virtual flags, all absent from DEFAULT_SIMULATION_RULES; IN-0's seam
closures ride the EXISTING dark flags they complete (they change behavior only
where those flags are lit, which no shipped config does — dark stays
byte-identical, golden-pinned first):

| Flag | Gates | Wave |
|---|---|---|
| `secondOrderBeliefEnabled` | the mirror read + all its consumers | IN-1 |
| `infoLureEnabled` | the lure plant subjects + bait consequences + bluff-vs-bluff receipts | IN-2 |
| `counterIntelEnabled` | sweep / vet / send-two / hide-as-answer + suspicion reads + house exposure producer | IN-3 |
| `reputationRaceEnabled` | race consumers at arrivals + who-knew-first outcome keys | IN-4 |

**LIT-PRECONDITIONS (the war volume's flag-dependency ruling applies verbatim —
independent dark switches, ORDERED lighting) [CORRECTED 2026-08-02 (fp-audit)]:**

*IN-0's four slices carry exact gate compositions (they ride existing flags,
but the compositions were undeclared — a defect, now ruled):*
- IN-0a (the fold) ⇒ `informationBrokeragesEnabled` (the commission writer) ×
  `infoStatecraftEnabled` (the disinfo lifecycle it folds into). Either dark ⇒
  byte-identical.
- IN-0b (the intercept consumer) ⇒ `informationBrokeragesEnabled`.
- IN-0c (the disclosure executor) ⇒ `peaceCausalActive` (warLayerEnabled AND
  peaceEngineEnabled — the term's own gate, peaceTerms.js:24-27) ×
  `allyIntelSharingEnabled` (the M9b machinery the feed rides). The term
  MINTS, STRAINS, and EXPIRES as built when the ally-share flag is dark and
  the feed simply does not execute — a NAMED DEGRADED ARM, pinned: a
  lit-peace/dark-info world is byte-identical on belief state. The one-time
  credibility credit at signing rides `infoStatecraftEnabled` (the stock's
  own flag) × `peaceCausalActive` — a lit-peace/dark-statecraft world writes
  no credit.
- IN-0d (the trade tax) ⇒ `infoStatecraftEnabled` (the HIDE posture's flag);
  dark, the exported factor is the 1.0 identity and trade bytes are
  golden-fenced identical.

*The wave flags:* `infoLureEnabled` ⇒ `informationBrokeragesEnabled` +
`infoStatecraftEnabled` + `secondOrderBeliefEnabled` (the lure aims through
the mirror) — AND, for the WEALTH and DEVOTION baits only, SP-2's
subject-family extension (`beliefAxesEnabled` + the BELIEVED
SCARCITY/CONDITIONS/DEVOTION families landing) is a HARD precondition: those
axes do not exist in the tree (§2's Verified-ABSENT) and the weakness bait is
the only one buildable ahead of them (§5 IN-2, J-INF-14). The war-side
`enforceLiveStrength` constraint (WR-1, Amendment B) is an INTERACTION the
lure must survive in BOTH states — named in IN-2's counterforces and ruled in
J-INF-14; it is NOT a precondition in either direction.
`counterIntelEnabled` ⇒ `infoStatecraftEnabled`; its mirror-gap evidence arm
degrades dark (named degraded read: suspicion scores from
exposure/contradiction evidence only when the mirror is dark — silence
elsewhere means preconditions are hard); IN-3's SEND-TWO verb additionally
depends on the WR-7d divergence reader, which is SPEC-ONLY today — J-INF-15
rules ownership if INFO lights first.
`reputationRaceEnabled` ⇒ `distancePricedNewsEnabled` (the story's leg of the
race IS that machinery) + SP-1's flag for person legs (until SP-1 lights, the
race runs only at arrivals the existing ledgers already stage: armies, exiles,
refugee columns — a named degraded arm). Courier migration (IN-4) rides
`intelTradeEnabled` × SP-1's flag, both lit.
IN-5's DESK CONFIG is dark-safe (routing config moves no bytes while
producers are dark) — but the `belief_misjudgment` REFILE is NOT dark-safe:
its producer is gated on `beliefsActive` (spatial-canon marker AND `infoMode
!== 'omniscient'`, beliefMap.js:195-205), and infoMode is an UNLOCKED
DM-selectable rule with `perfect_delayed`/`full` shipping in preset spreads
(simulationRules.js:370,416). The refile therefore changes Herald output for
reachable, already-supported configurations: Herald goldens for those
infoModes are captured FIRST and the one-time output shift is recorded in the
ledger (§5 IN-5(a)). IN-6 has no flag.

**SEAM CONTRACT ONE — THE FOLD (PLANT_WIRING, settled to land):** the bought lie
enters the world through EXACTLY the existing lifecycle: `processLies` folds
`commissionPlants()` into `nextDisinfo` — one lifecycle, one writer, one
exposure law (2-band contradiction or shelf-life), one blowback triple. The fold
is the ONLY entry for commissioned falsehoods; a parallel lure ledger or second
disinfo writer is a design defect (J-INF-3). The plant's collateral stays as
built: liarId = the market's host settlement — the seller wears the exposure.

**SEAM CONTRACT TWO — THE COURIER MIGRATION:** all purposeful named-person
information movement rides SP-1 (the errand spine): the D-3 sale/gift's one-tick
deposit courier RETIRES when `intelTradeEnabled` and SP-1's flag are both lit —
the transfer becomes a typed covert/commercial errand (law M: one week per leg;
interceptable; the K.2 decaying snapshot IS the cargo). Dark, the deposit ledger
stands byte-identical. One transport law, two states, zero forked physics
(J-INF-4). Ambient rumor is NOT an errand — it stays on the carrier network (a
flow, not a person; the spine's migration distinction applied to words).

**SEAM CONTRACT THREE — THE MIRROR IS DERIVED:** second-order belief is a PURE
READ over the outbound record — what was shared (intelTransfers), fed
(brokerage feeds), planted (disinfo ledger), shown (ally shares, treaty
disclosures), and withheld (HIDE postures) toward that observer, all already
ledgered and all already decaying on their own laws. No mirror state is ever
persisted; no read ever touches the observer's actual belief map (K3-grade
structural enforcement, §1b). Derivation is bounded because every input ledger
prunes (J-INF-1).

---

## §4 CANONICAL MODEL — new state, and it is deliberately almost NOTHING

The medium's state already exists (beliefMaps, rumorLedgers, disinfo,
sightPostures, credibility, intelTransfers). This volume's entire program adds:

```
worldState.spatialLedgers.disinfo[]         — EXISTING ledger, EXISTING writer.
  IN-2 widens each record's closed subject vocabulary:
  subjectKind: 'strength' (today's bands) | 'scarcity' | 'conditions' | 'devotion'
  // [CORRECTED 2026-08-02 (fp-audit)] The widening is REAL LIFECYCLE WORK,
  // priced as such — the earlier "no new lifecycle, adds almost nothing"
  // claim was a substrate error. The tree's truth: DisinfoRecord is
  // strength-typed in its own fields (subjectId = "the strength being lied
  // about", assertedBand/trueBand — informationStatecraft.js:502-505); the
  // exposure comparator reads belief.strengthBand EXCLUSIVELY (:626-628);
  // the exposure receipt is strength-worded (:655); and no scarcity/
  // conditions/devotion belief axis exists (CHANNEL_BELIEF_AXES.trade is
  // frozen empty; SP-2's families are unbuilt — §2 Verified-ABSENT).
  // THE NEW WORK, named: the record generalizes to carry an explicit axis
  // reference — axis: 'strengthBand' | <SP-2 family axis>, assertedValue,
  // trueValue (assertedBand/trueBand stand as the strength spelling; absent
  // axis ⇒ 'strengthBand', LEGACY SEMANTICS — old records load unchanged,
  // the shape-migration pin); the contradiction comparator becomes
  // AXIS-TYPED with a per-axis tolerance band; the exposure receipt prose
  // becomes axis-aware ("that its granaries were emptier than they are",
  // never a strength sentence on a scarcity plant); devotion targets a
  // categorical label (faithLabel), so its contradiction test is
  // label-inequality, not band distance — stated, not discovered. Each
  // piece carries its own pin, incl. "a non-strength plant exposes by
  // CONTRADICTION on a real fixture, not only by age." STILL ONE writer,
  // ONE lifecycle, ONE exposure LAW (J-INF-3) — the law is now axis-typed;
  // there is still no parallel ledger. The wealth/devotion baits are HARD
  // BLOCKED on SP-2's families landing (§3 LIT-PRECONDITIONS).
  // intent: 'inflate' | 'deflate' stands as built.

worldState.spatialLedgers.sightPostures     — EXISTING; IN-3 adds NO fields.
                                            // Gains its first projection
                                            // (the Watch panel) — read-only.

everything else                             — NEVER STORED. The mirror,
                                            // suspicion, the race, the arc:
                                            // pure reads over persisted
                                            // receipts and ledgers. Receipts
                                            // only.
```

**What is deliberately NOT modeled:** no nested belief map (Law One; the mirror
is derived) · no suspicion stock (SP-5 is a closed family and suspicion is not
in it; suspicion derives from persisted incidents, conditions, and the mirror
gap) · no spy NPCs (paid eyes stay settlement-grade postures; the named faces
are read-wired through existing planes, §5 casting lines) · no house
micro-agents (the house acts at faction grain through SP-4a appetite) · no
informational casus belli (the lure feeds EXISTING casus — opportunism,
grievance — J-INF-9) · no partial-leak grading (exposure stays binary per
record — deliberately deferred, recorded here: grading exposure is a new owner
surface; the binary idiom is proven; revisit only on owner ask) · no arc state
(the arc is a composer over receipts that already persist).

---

## §5 THE WAVES (dependency order; each: one commit — IN-0 four slices, one per
## seam; focused gates per slice, full gate at wave end, ledger row; every wave
## DARK per §3; every mechanism carries all twelve spine requirements or it is
## a defect)

### IN-0 — THE PRICES BECOME LAW (four seam closures; rides existing flags;
### four slices, each its own commit)
The survey found four cost laws narrated but unenforced. A narrated price is a
ratchet wearing a costume — the force exists, the counterforce is prose. This
wave lands them. Historical archetype, one clause each below.

**IN-0a — THE FOLD (PLANT_WIRING).** The one-call fold per SEAM CONTRACT ONE.
- **Model:** `processLies` gains the `commissionPlants()` fold into
  `nextDisinfo`; `brokerageServicesPlant.js` stays the commission writer;
  the disinfo ledger stays the lifecycle owner. VERIFY-AT-BUILD: the exact
  fold-point signature against live `processLies` — the survey confirms it
  takes no commissionedPlants param today.
- **Law / force / counterforce:** the force is the bought story entering a real
  court's belief; the NAMED counterforce off the same evidence is the exposure
  machinery it inherits — the same 2-band contradiction test, the same 8-tick
  shelf-life, and the blowback lands on the MARKET's host settlement (liarId as
  built) plus the commissioning patron where lineage collapses. The market
  priced this exact risk at commission (DISREPUTE_SURCHARGE — "the market
  prices the blowback it will wear"); now the priced thing can happen.
- **Belief posture:** the plant writes a BELIEF record through the standard
  fold — reconcileBelief weighs it against independent arrivals like any other
  telling; the engine never marks it "false" in the mark's world, only in
  DM-truth metadata (Law One).
- **Casting:** the commission already attributes through the patron faction and
  the market's named institution; the mouthpiece idiom (informationStatecraft
  :526) extends — a named house factor fronts the story, read-wired, no new
  NPC state.
- **Receipts (house voice):** the built beat stands — "A story is bought… It is
  not true. Nobody there knows that yet" (DM-gated). NEW: `plant_took`
  (DM-truth-only projection line, never public — audience law) when the mark's
  belief record crosses to the planted band; exposure extends the existing
  `infowar_lie_exposed` beat to NAME THE MARKET and, where lineage collapsed
  fully, the commission.
- **Clock:** commission→arrival at rumor physics (hopWeeks, seasonal); take is
  read at the belief fold (fast layer); exposure and the credibility fall are
  the slow verdict (52-tick half-life). All weekly ticks; SP-7 asserts.
- **Posture/risk:** commissioning a plant consumes SP-4 — an out-of-posture
  commission (a cautious court buying a lie) is priced and receipted news,
  never forbidden.
- **Endings entries:** plant {took, died_quiet, exposed, backfired} — the four
  outcomes of every bought story, share envelopes in IN-6.
- **Couplings:** INFO×WAR and INFO×TRADE rows in DESIGN_FP_COUPLINGS.md (the
  planted story is the input side of IN-2's baits).
- **Pins (negative hardest):** a commissioned plant against a mark whose
  independent corroborated sources contradict it DIES at the fold (reconcile's
  contradiction arm wins — the lure is resistible by good sourcing); THE SPENT
  MARKET [CORRECTED 2026-08-02 (fp-audit)]: the same plant from a
  spent-credibility market moves the mark's belief STRICTLY LESS than from an
  unmarked one, by a banded margin the fixture asserts (§7 names the expected
  band) — NOT "by ~nothing": WEIGHT_FLOOR 0.35 is a discount, not silence
  (the span is centred on 1.0, informationStatecraft.js:143-145), and the
  honest sibling is pinned too: a FRESH, low-hop, complete plant from a
  discredited market can still outweigh a stale, high-hop honest prior
  (aggregateReports multiplies credibility against hop/recency/independence/
  completeness, beliefMap.js:445-449) — that asymmetry is the drama, not a
  bug; writer/reader pin boots the REAL commission writer and reads through
  the REAL disinfo reader; reachability pin: in a generated lit corpus at
  least one plant TAKES (lower-bound discipline); registration complete for
  kinds minted this slice (`plant_took` — §1c's mint-time rule); dormancy
  golden (dark = byte-identical).
- **Lifecycle line:** disinfo records already persist/round-trip; the fold adds
  no shape change — the JSON-round-trip pin re-runs on a folded record; a regen
  that rebuilds the ledger preserves live plants or the DM-truth trace ghosts
  (the estate's most-bitten class — pinned).
- **Dossier round-trip pin:** open the town page as DM — "Stories standing
  against this town" renders from `projectPlants` (today zero non-test
  consumers; this slice mounts it, premium/DM-only). AUDIENCE TRUTH
  [CORRECTED 2026-08-02 (fp-audit)]: `projectPlants` is fail-OPEN on its
  default — `audience` defaults to 'dm' and returns records unchanged
  (brokerageServicesPlant.js:369-373); only an explicit `audience: 'player'`
  filters. The mount therefore passes `audience` EXPLICITLY, and the slice
  adds an audience-projection pin: the player projection is empty for live
  plants, and a call site omitting the parameter is caught by a source scan.
  (The safer alternative — flipping the export's default to 'player' — is a
  behavior change to a built export; J-INF-17 records it as the vetoable
  road not taken.) A DM must be able to find every live lie aimed at the
  town where they'd look: the town.
- **Bands:** take threshold; the DM-projection line's significance is a CLASS
  ASSIGNMENT into the spine's SP-6 significance family (routine — a hum),
  never a volume-minted scale [CORRECTED 2026-08-02 (fp-audit)]. Both land in
  §7.
- Archetype: the Fugger newsletters' paid falsehoods — the market that sold
  word and wore the shame when word broke.

**IN-0b — THE INTERCEPT CONSUMER.** brokerage_intercept stops being
narrative-only.
[The whole slice rewritten to instantiated fields — it discharged seven of the
twelve requirements by pointer to IN-0a, whose shapes describe a different
mechanism. CORRECTED 2026-08-02 (fp-audit).]
- **Model:** the intercept act grants the patron ONE claim — constructed by
  `claimFrom` (the only claim constructor, as built) — about the rival's most
  recent OUTBOUND ledgered act within the market's reach (an intelTransfer, a
  feed contract's existence, a commission's existence at banded vagueness —
  never its content at full fidelity). Nothing in reach ⇒ the honest refusal
  token `no_record` — QUERY_REFUSALS' built token for "the register holds
  nothing about that" (brokerageServices.js:320-322; the five-token vocabulary
  extends by ZERO; the earlier `nothing_to_sell` named a token that exists in
  NEITHER frozen refusal vocabulary and must not be minted).
- **Force / counterforce:** the force is bought awareness of a rival's
  traffic. The counterforces off the same evidence, honestly priced: (1) THE
  FOG COMPOSE — NEW WORK, named as such: HIDE's built degradation is
  sight-posture-specific and does NOT touch the outbound-ledger read this
  intercept rides, so this slice WIRES it — the rival's HIDE posture composes
  into the intercept's reach/vagueness read (the existing −0.7 rivals'-reads
  fog factor applied at the claim's vagueness band), with its own pin: an
  intercept against a HIDE court reads at degraded vagueness or refuses.
  (2) IN-3's sweep — interception is exactly the evidence class that arms the
  counter-game — but IN-3 lights LATER behind `counterIntelEnabled`: between
  the two lightings the intercept's only opposition is the fog compose plus
  the act lane's own cooldown/cap, a DECLARED bounded gap (anti-hum law), not
  an accident.
- **Belief posture:** the claim lands as a belief record at the stamp ladder's
  rung, never as truth (a bought intercept can be WRONG — fidelity law).
- **Casting:** the market's named reader plane fronts the claim (the broker
  who "reads what passes"); the rival's side is the ledgered act's own
  attribution (courier, factor, seat) — read-wiring, no new NPC state.
- **Receipts (house voice):** "Word passes quietly between courts — and
  quieter still, someone reads it." (hum, DM-gated; the claim's own line
  lands on the patron's intelligence surface with its stamp rung.)
- **Clock:** the act lane's own cadence — cooldown/cap per the
  INTEL_TRADE_TUNING idiom (§1b's anti-hum house shape); fast layer = the
  claim landing the tick the act resolves; slow verdict = the counter-game it
  arms (caught intercepts persist as IN-3's suspicion evidence and decay on
  that ledger's law).
- **Posture/risk:** commissioning an intercept consumes SP-4 (a trusting
  court buying eyes on a friend is out-of-posture, priced and receipted).
- **Endings entries:** intercept {read, refused, caught} — `read` and
  `refused` minted HERE (the claim delivered / the honest `no_record`);
  `caught` minted by IN-3's sweep (the cross-wave producer, declared — the
  caught-intercept evidence line). Share envelope in IN-6.
- **Couplings:** INFO×INFO — the intercept arms IN-3's suspicion read (its
  row rides IN-3's coupling entry in DESIGN_FP_COUPLINGS.md).
- **Lifecycle line:** NO new persisted state — the claim is a belief record
  under beliefMap's existing round-trip pins; the act receipt rides the
  existing act ledger.
- **Pins:** the negative — an intercept against a rival with an EMPTY outbound
  record refuses honestly with `no_record` (vacuous-absence discipline: the
  positive pin seeds a real transfer first); the claim rides claimFrom (a grep
  pin: no second claim construction site); the fog-compose pin (above); the
  closed-vocabulary pin re-asserts QUERY_REFUSALS unchanged; registration
  complete for kinds minted this slice (the intercept hum — §1c's mint-time
  rule); dormancy.
- **Dossier round-trip pin:** the patron's town page (DM view) shows the
  bought claim with its stamp rung — VERIFY-AT-BUILD: the "intelligence
  lines" surface the query answer lands on (fallback landing if absent: the
  DM intelligence block IN-0a's panel work mounts). [R9]
- **Bands:** intercept vagueness band, reach radius (rides the existing market
  reach read), the act cooldown/cap — all in §7.

**IN-0c — THE DISCLOSURE EXECUTOR (compelled intel).** The 'disclosure' peace
term executes. Archetype: the opened arsenals of a dictated peace — inspection
as a treaty article.
- **Model:** for the term's life, a bounded M9b-STYLE feed runs loser→victor
  (the ally-share machinery pointed across a treaty edge instead of an alliance
  edge; style: lawful relay ×0.95 — compelled truth, not styled deception),
  plus the one-time credibility credit at signing (the recorded W-PEACE seam,
  informationStatecraft.js:1460-1464, closed here). Strain/expiry stand as
  built for the term family. Gate composition per §3's LIT-PRECONDITIONS
  (peaceCausalActive × allyIntelSharingEnabled, with the named degraded arm).
  THE HOT-FILE LAW [CORRECTED 2026-08-02 (fp-audit)]: peaceTerms.js sits at
  its size ceiling (R2's ruling binds) — the executor lands in a LAZY LEAF
  SIBLING consuming peaceTerms' exports (the disclosure term's minting,
  compliance, and expiry stay peaceTerms'); peaceTerms itself gains only
  net-zero seam lines. The disclosure term's catalog identity is GRAMMAR's
  canonical term-family list (R3) — this slice is a CONSUMER, never a second
  speller.
- **Force / counterforce:** the force is the opened court; the counterforce off
  the same evidence is the term's OWN compliance machinery — a strained
  disclosure feeds thinner (fidelity follows compliance state), and a defaulted
  term stops the feed and mints the standard default receipt. The loser's HIDE
  posture during a live disclosure term is an out-of-posture act priced as
  compliance strain, receipted ("they signed the open door and barred it").
- **Belief posture:** the victor receives BELIEF records with inherited
  uncertainty (the D-3 idiom: receiver inherits sharer's uncertainty) — a
  compelled feed is still a telling, not truth.
- **Casting:** the feed attributes through the loser's seat and spokesperson
  (existing planes).
- **Receipts:** `treaty_disclosure_opened` — "The court at [X] stands open to
  [Y]'s eyes, by the terms signed at [place]" — full address chain, both courts
  named.
- **Clock:** term-life denominated per the treaty's own marker (the WR-0c
  item-4 law: current treaties 52-week years); feed cadence per pulse; the slow
  verdict is the credibility credit's decay.
- **Posture / endings / couplings:** consumes SP-4 on the loser side (a proud
  posture strains the term earlier — priced); endings ride the term's own
  TERM COMPLIANCE STATES {honored, strained, defaulted, expired} — the
  per-term union peaceTerms owns (TermRecord.complianceState,
  peaceTerms.js:405-406), spelled VERBATIM ("honored", not "held"), and
  DISTINCT from GRAMMAR GR-7's closed per-treaty PACT ENDINGS vocabulary,
  which this slice neither touches nor extends — no new vocabulary either
  way [CORRECTED 2026-08-02 (fp-audit)]; INFO×GRAMMAR
  row in DESIGN_FP_COUPLINGS.md.
- **Pins (negative hardest):** an EXPIRED disclosure term's feed lifts the
  SAME TICK the term expires — one artifact, ONE lift contract: GRAMMAR's
  enforcement law ("expiry lifts every effect the same tick") binds, and the
  next pulse reads nothing [CORRECTED 2026-08-02 (fp-audit) — the earlier
  "feeds nothing the NEXT pulse" wording licensed a one-pulse-late lift the
  treaty grammar forbids]; a defaulted term mints the default receipt and no
  feed; the writer/reader pin boots peaceTerms' real minter; reachability: a
  real drafted treaty carrying disclosure executes in a lit fixture;
  registration complete for kinds minted this slice
  (`treaty_disclosure_opened` — §1c's mint-time rule); dormancy.
- **Lifecycle line:** the term already persists on the treaty; the executor
  adds no state — regen/undo ride the treaty record's existing pins.
- **Dossier round-trip pin:** the treaty document shows the disclosure
  article LIVE/strained/lapsed — VERIFY-AT-BUILD: the treaty-document surface
  and its article rendering (fallback landing if absent: the settlement
  brief's treaty lines); the victor's town page shows the compelled feed as a
  source with its stamp — VERIFY-AT-BUILD: the intelligence-lines surface
  (fallback: IN-0a's DM intelligence block). [CORRECTED 2026-08-02
  (fp-audit), per R9 — pins assert only survey-verified surfaces or carry
  the mark and the fallback.]
- **Bands:** disclosure fidelity band, strain→fidelity map.

**IN-0d — HIDE'S TRADE TAX.** The narrated toll becomes an enforced read.
Archetype: sakoku — seclusion priced in trade.
- **Model:** a pure read exported from the statecraft layer:
  `secrecyTradeFactorOf(settlementId)` — banded, consumed by the trade layer
  (the consuming join is TRADE's; the read and its receipt are OURS; the
  coupling row in DESIGN_FP_COUPLINGS.md names both sides — built here,
  consumed there, the WR-6/WR-8 pointer discipline).
- **Force / counterforce:** HIDE's force is fog (−0.7 rivals' reads); its
  counterforce is now REAL off the same posture record: the sealed gate pays at
  the counting-house, and the self-blinding arm (−0.5 own reads) stands as
  built — seal the gates and the world stops telling you things too.
- **Receipts:** a hum, pacing-gated: "The gates of [X] are shut to strangers;
  so are its markets, more than its council admits."
- **Pins (negative hardest):** a settlement NOT in HIDE prices at exactly 1.0
  (identity — no ambient tax); the factor is banded and capped (colours, never
  drowns); registration complete for kinds minted this slice (the seclusion
  hum — §1c's mint-time rule); dormancy golden on the trade side (dark flag ⇒
  trade bytes identical — the fence captured FIRST).
[The compound bullet unpacked — clock, endings, and casting were listed and
never given. CORRECTED 2026-08-02 (fp-audit).]
- **Clock:** the factor is a PER-PULSE pure read that follows the HIDE
  posture's own enter/exit hysteresis (one clock, not two across the TRADE
  seam — the cadence is stated here precisely because the consumer lives in
  another volume). Fast layer = the toll appearing at the counting-house the
  week the gates shut; slow verdict = the trade relationships that do not
  come back when they lift. Weekly ticks; SP-7 asserts.
- **Casting:** the gate-keeper / harbour-master plane fronts the hum,
  read-wired from existing roles — no new NPC state.
- **Endings entries:** NONE OF ITS OWN — a deliberate no-new-vocabulary
  ruling, not an omission: the toll rides HIDE's existing posture
  transitions (enter/hold/exit), which are already receipted; what IN-6
  grades is the TOLL INCIDENCE envelope (share of settlement-weeks paying a
  non-identity factor, banded), declared there.
- **Posture:** posture entry already consumes SP-4 (IN-3 adds the response
  entry path).
- **Lifecycle line:** no state — a pure exported read; nothing to regen,
  undo, or migrate.
- **Bands:** the tax curve per secrecy band — in §7.
- **Dossier round-trip pin:** the town page's trade lines show the toll's
  band word next to the gate posture ("sealed, and paying for it") — one
  glance, one sentence, per the legibility law. VERIFY-AT-BUILD: the
  trade-lines surface (fallback landing: the settlement brief's trade
  sentence). [R9]

### IN-1 — THE MIRROR (second-order belief; flag `secondOrderBeliefEnabled`)
SP-2's settled ruling elaborated: a banded "what they likely believe of us"
read DERIVED from the outbound record, never a nested map. Archetype: the
Bureau of Barbarians — the empire's standing file of what each foreign court
had been shown.
- **Model:** new PURE module `secondOrderBelief.js` (no state, no writer, no
  RNG). Export: `mirrorOf(usId, observerId)` → closed banded shape
  { strengthShown: band, wealthShown: band, devotionShown: band,
    lastShownTick, staleness: band, confidence: band } derived per SEAM
  CONTRACT THREE from: outbound intelTransfers, feeds we host that they buy
  from, plants WE commissioned toward them (DM-truth inputs allowed — it is
  OUR record of what we did), ally shares along their edges, disclosure terms,
  and our own HIDE spans (a sealed season lowers confidence — they've seen
  nothing new). Each input already ledgered, already decaying.
- **Law / force / counterforce:** the force is self-knowledge — a court that
  knows what it has shown can bluff, lure, and negotiate deliberately. The
  NAMED counterforce, scoring off the SAME record: THE MIRROR LIES BY
  OMISSION — it derives only from OUR record and cannot see the observer's
  independent sources, so mirror confidence DEGRADES on evidenced
  independent arrival. THE EVIDENCE SOURCE, NAMED AND INSIDE THE FENCE
  [CORRECTED 2026-08-02 (fp-audit)] — the degradation arm was a ratchet as
  first written (its inputs sat outside its own recursion fence): the
  inconsistency evidence is OUR OWN RECEIVED-EVIDENCE RECEIPTS, all
  first-person ledgers — (a) our exposure receipts (a lie of ours exposed IN
  that court), (b) our caught-intercept receipts (they read our traffic —
  they know things we never showed), and (c) OUR OWN BELIEF RECORD of their
  acts ("what our court has heard they did") where the heard act is
  inconsistent with the shown picture — never a read of the observer's
  actual belief map or true selections (that would be the K3 true-state read
  Law One forbids; what we have HEARD can itself be wrong, and the mirror
  degrading on false word is correct behavior, not a bug). Overconfidence in
  the mirror is the priced failure mode, and the degradation receipt names
  the inconsistent act as WE heard it.
- **Belief posture (Law One):** the mirror models the RECORD's implication,
  never the observer's mind. The band vocabulary says so on every surface:
  "the record suggests", "they have been shown", never "they believe".
- **Casting:** the mirror is read BY the seat; its court voice is the existing
  spokesperson plane ("the chancellor keeps the ledger of what the neighbours
  have been given to see") — read-wiring only.
- **Receipts (house voice):** band-crossing hum `mirror_shift`: "By its own
  ledger, [X] has shown [Y] less than it did a year ago — and shown it
  nothing since [season]." [CORRECTED 2026-08-02 (fp-audit) — the earlier
  "in [Y]'s eyes" wording asserted the observer's perception, exactly the
  claim Law One forbids; the record voice is the fixed acceptance string,
  §8 matches verbatim.]
- **Clock:** staleness bands denominate in weekly ticks against each input
  ledger's own half-life; the fast layer is the outbound act (the mirror moves
  the tick we show something); the slow verdict is staleness — an unshown year
  fades the mirror to UNKNOWN.
- **Posture/risk:** consumers below always compose the mirror THROUGH SP-4 —
  a bold posture acts on a thin mirror; a cautious one demands a fresh one.
- **Endings entries:** none of its own (a read, not an episode) — it feeds
  IN-2's and IN-3's endings.
- **Couplings:** INFO×GRAMMAR (negotiation posture reads the mirror — what do
  they think we'd accept), INFO×WAR (the bluff maintenance read: does my
  garrison lie still stand in their eyes), both walked in
  DESIGN_FP_COUPLINGS.md.
- **Pins (negative hardest):** THE EMPTY-RECORD NEGATIVE — with zero outbound
  entries toward an observer the mirror returns UNKNOWN at zero confidence,
  never a fabricated band (and the pin seeds a non-empty sibling to dodge the
  vacuous-absence trap); THE RECURSION FENCE, AS AN ALLOW-LIST [CORRECTED
  2026-08-02 (fp-audit)] — `secondOrderBelief.js`'s import list is pinned to
  the NAMED set: our outbound ledgers PLUS our own received-evidence receipts
  (the counterforce's three inputs above) — and to EXCLUDE beliefMap's
  observer-side reads; the token scan still bites on any read of the
  observer's belief slot, guard-the-guard per §1b (the positive control
  points at a module that legitimately reads the observer's map — beliefMap
  itself); THE REVERSAL PIN — a court that acts inconsistently with the
  shown picture drives mirror confidence measurably DOWN on a real fixture
  (the degradation arm is reachable, or the mirror is a ratchet);
  determinism (two identical records, two identical mirrors); registration
  complete for kinds minted this wave (`mirror_shift` — §1c's mint-time
  rule); THE PHRASE SCAN — the mirror's rendered strings contain no
  perception verb (no "believes", no "in their eyes"; the band vocabulary
  enforced, not stated); dormancy golden.
- **Lifecycle line:** NO persisted state — nothing to regen, undo, or migrate;
  the pin is that a save/load round-trip yields an identical derived mirror
  (pure function of persisted inputs).
- **Dossier round-trip pin:** the town page gains one standing line per
  neighbour under "WHAT THE NEIGHBOURS HAVE BEEN SHOWN" [CORRECTED 2026-08-02
  (fp-audit) — the "In the eyes of" heading broke Law One's phrasing rule] —
  band words + staleness ("less than we fear, and the reckoning is a season
  stale"). DM-only detail expands to the deriving record (which acts built
  this picture). A DM asking "do they know how weak we are?" finds the
  record's answer where they'd look.
- **Bands:** input weights per ledger family, staleness half-life band,
  degradation step per inconsistent-act evidence, confidence caps.

### IN-2 — THE LURE (flag `infoLureEnabled`; the program's signature)
Settled: lies that BAIT, not only deter. Three baits, one tragedy. Archetypes,
one clause each: planted weakness — the feigned rout at Hastings that emptied
the shield-wall; planted wealth — the Seven Cities of Gold that marched
Coronado a thousand miles; planted devotion — the Donation of Constantine, a
forged devotion that moved real crowns; bluff-vs-bluff — the July Crisis
pattern, two courts marching on each other's theatre.
- **Model [CORRECTED 2026-08-02 (fp-audit)]:** the disinfo subject vocabulary
  widens per §4 — ONE writer, ONE lifecycle, ONE (now axis-typed) exposure
  law; the widening is real lifecycle work priced in §4, not a free ride.
  Commission targets: (a) WEAKNESS — a deflate plant about a victim's
  strength aimed at a chosen aggressor's belief slot — BUILDABLE NOW (the
  strength axis exists); (b) WEALTH — a scarcity/conditions plant writing
  SP-2's BELIEVED SCARCITY/CONDITIONS families (the gold-rush substrate);
  (c) DEVOTION — a devotion plant writing SP-2's BELIEVED DEVOTION (sacred
  tension's food). Baits (b) and (c) are HARD BLOCKED on SP-2's
  subject-family extension landing (§3 LIT-PRECONDITIONS; §2
  Verified-ABSENT: those axes do not exist) — a recorded deferral, not an
  omission; the endings and envelopes below cover all three so nothing
  re-opens when SP-2 lands. The audience pick runs through IN-1's mirror
  (what has the mark been shown — aim where the record says the ground is
  soft). NO new consequence wiring: the baits spring through EXISTING
  machinery — opportunism's foe-half already scores believed weakness
  (survey-verified: the casus reads belief). THE LANDED LAW, STATED
  HONESTLY (R8; the earlier "WR-1 dissolves it when belief corrects" was a
  mischaracterization): WR-1's optional coherence restraint
  (`enforceLiveStrength`, Amendment B) suppresses the casus at SCORING TIME
  on a GROUND-TRUTH comparison — whenever the victim's LIVE strength is at
  least the aggressor's, immediately, regardless of belief
  (liveStrengthContradictsOpportunism, opportunism.js:174-186; the header
  law: fog may overstate the DEGREE of a real advantage, never reverse the
  live sign). THE BAIT COMPLIES — the weakness lure re-scopes to the
  compliant channels: (i) DEGREE AND TIMING — against a victim genuinely
  weaker than the aggressor, the plant deepens believed weakness and moves
  the march EARLIER and BOLDER than honest word would have (the live sign
  already favors the aggressor; the restraint never fires); (ii) THE
  MISJUDGMENT CHANNEL — where the plant reverses the live sign, a lit
  restraint refuses the march WITH ITS RECEIPT ("rumour calls them weak,
  but the live muster shows an equal or stronger host") — that refusal is
  itself a story, and the plant still stands, still ages, still exposes on
  the lie's own law; dark-restraint worlds keep the full reversal drama.
  Both states are designed states, pinned (J-INF-14). The pull evaluation's
  belief arm is FP-POPULATIONS' wave (coupling row; until it lands, the
  wealth bait writes the family and the spring is dark there); the
  sacred-tension consumer is FP-FAITH's (same discipline). The lure is a
  belief WRITE aimed at someone else's trigger — the medium supplies the
  bait; the substances supply the jaws.
- **Law / force / counterforce:** force — the bought march, the bought rush,
  the bought schism. The NAMED counterforces, each off the same evidence
  [CORRECTED 2026-08-02 (fp-audit) — counterforce (2) previously cited the
  WRONG reader and assumed a per-observer credibility memory the estate
  deliberately does not keep]: (1) reconcileBelief's contradiction
  machinery — independent corroborated word KILLS the plant (good sourcing
  is armor; IN-0a's negative pin is this); (2) the source-credibility weight
  at the belief fold — `aggregateReports`' `credibilityOf(sourceId)`
  multiply (beliefMap.js:445-449, WEIGHT_FLOOR 0.35): a market whose
  credibility is spent sells a telling the WHOLE WORLD weighs toward the
  floor. The discount is world-shared BY DESIGN — the credibility ledger is
  `{ [settlementId]: { score, … } }`, global, never per-observer; "a mark
  that remembers catching THIS market" would be a per-mark model of the
  liar's mind, the nested map Law One forbids. `makeBlaineyCredibilityFn`
  is NOT this counterforce — it is the PEACE engine's convergence seam,
  keyed on the subject being believed about, and it appears in this wave
  only where it belongs: the two-liars-long-war pin; (3) IN-3's
  counter-game — a swept court re-anchors the planted subject; (4) the
  blowback triple on exposure, priced at commission; (5) the war-side
  live-strength veto — where WR-1's optional `enforceLiveStrength`
  restraint is LIT, a deflate plant that reverses the live sign is refused
  at scoring time with its receipt (the Model's misjudgment channel;
  J-INF-14). The lure is resistible at five gates or it is a ratchet.
- **Belief posture (Law One):** the engine never marks the mark "deceived" in
  its own world — it holds a belief that happens to be wrong, indistinguishable
  in-world from honest error. Only DM-truth metadata and the dramatic-irony
  surfaces know. The march it causes is stamped by the EXISTING misjudgment
  detector ("X marches on a misjudgment") — the lure rides the built beat.
- **Casting:** the commissioner is a seat or house (faction grain); the mark's
  named readers (the mayor on stale word, the merchants who know better —
  council_schism's exact built shape) carry the story's faces. No new NPC
  state; the double-reader schism IS the casting.
- **Receipts (house voice):** commission (exists, DM-gated); spring:
  `lure_sprung` (DM-truth beat joining the plant to the mark's act — "They
  marched on a weakness that was bought for them"); public-side, the world
  sees only the march and the misjudgment beat (audience law — the irony is
  the DM's until exposure); exposure: the extended lie-exposed beat names
  bait, market, and — lineage permitting — buyer.
- **Clock:** plant travels at rumor physics; TAKE needs a dwell (band — a
  story must age into the town before a court stakes a march on it; the
  8-tick shelf-life bounds the window: the lure is a TIMED weapon — spring
  before your own lie expires); the spring is fast, the war it starts is the
  slow verdict.
- **Posture/risk:** commissioning consumes SP-4 (an out-of-posture lure is
  priced news); the MARK's response to the bait runs its own posture — a
  cautious court verifies before marching (IN-3's vet verb — posture picks
  the counterforce).
- **Endings entries:** lure {sprung, resisted, exposed_first, backfired} —
  backfired = exposure AFTER the spring: the mark learns the march was bought,
  and the deception_betrayal grievance re-aims at commissioner and market
  (the existing E1 machinery; the bought war becomes the avenged war).
- **Couplings (all in DESIGN_FP_COUPLINGS.md):** INFO×WAR (weakness→
  opportunism; the named-lie war brief below), INFO×POP (wealth→gold rush;
  counterforce letters-home is POP's), INFO×FAITH (devotion→sacred tension),
  INFO×TRADE (scarcity plants meet the famine-speculator lane — moral drift
  prices the profiteering commissioner).
- **Pins (negative hardest):** THE RESISTED LURE — a mark with corroborated
  independent sources does NOT march (the contradiction arm wins on a real
  fixture; the negative case of the signature mechanism pins hardest); the
  UNATTRACTIVE BAIT — a wealth plant below the pull threshold moves nobody
  (reachability's negative twin); THE SPRING REACHABILITY — in a generated lit
  corpus at least one lure springs end-to-end (plant → take → march →
  misjudgment stamp), the unreachable-conjunction discipline — and the pin
  RUNS UNDER BOTH RESTRAINT STATES [CORRECTED 2026-08-02 (fp-audit)]:
  `enforceLiveStrength` dark (the reversal channel springs) and lit (the
  degree-and-timing channel springs; the veto's refusal receipt is the lit
  state's own positive — J-INF-14); THE BLUFF-VS-
  BLUFF TRAGEDY (the settled signature, one fixture, pinned): two courts each
  running the built garrison bluff at each other, each mirror-confident its
  own lie holds, each reading the other's planted strength as true — PINNED
  ON THE EXPIRY ARM, which the substrate guarantees: the war neither wanted
  when one bluff expires first (EXPOSE_MAX_AGE_TICKS 8 bounds every bluff,
  informationStatecraft.js:422; belief re-anchors; opportunism reads the
  corrected belief); receipts name BOTH lies; the
  two-liars long war (Blainey, already pinned) extends with the retrospective
  naming WHY the reckonings would not reconcile. THE STARVATION ARM IS
  HONESTLY DEFERRED [CORRECTED 2026-08-02 (fp-audit)]: "the standoff that
  starves both" asserted a trade-layer consumer of believed strength that NO
  survey row supports, plus an uncosted bluff-maintenance drain this volume
  does not build (IN-0d's tax prices SECRECY, not a lie's upkeep — a court
  running the garrison lie pays nothing to maintain it). A real starvation
  arm is NEW WORK — a priced bluff-maintenance cost with its own force/
  counterforce/clock — recorded here as deferred to an owner ruling; the
  fixture pins the expiry arm alone until it lands; THE NAMED LIE (the survey's
  PARTIAL story closed): scoreGrievance gains an incident-naming receipt
  variant, so the war brief's grievance line can cite the exposed lie instead
  of stock prose (eventProse.js:190-192 today) — "the ledger of grievances
  opens with a forgery"; NO-FATES — no lure may target a named character's
  fate-resolution (structural: bait subjects are settlement-scoped bands
  only); registration complete for kinds minted this wave (`lure_sprung` —
  §1c's mint-time rule); dormancy goldens per §3's fence discipline.
- **Lifecycle line:** widened disinfo records persist — JSON-round-trip both
  subject shapes (old strength-only records load unchanged: additive field,
  absent ⇒ legacy semantics — the shape-migration pin), regen preserves live
  plants, undo restores them, import validates subjectKind against the closed
  vocabulary.
- **Dossier round-trip pin:** the DM opens the MARK's town page: the believed
  (false) band renders in its standing reads with the divergence surface
  flagging the gap (IN-6 completes wrongness); opens the VICTIM's page:
  "Stories standing against this town" (IN-0a's panel) names the planted
  weakness; opens the COMMISSIONER's page: the commission in its DM-truth
  acts. Three towns, three traces, one lie — walkable end to end per spine
  §1.7.
- **Bands:** dwell-to-take, bait magnitude caps per subject family, spring
  window vs shelf-life, backfire grievance magnitude, commission price tilt by
  posture.

### IN-3 — THE COUNTER-GAME (flag `counterIntelEnabled`; joined to SP-4)
Settled: counter-intelligence as DELIBERATE agency. Today the counter-game is
emergent only (HIDE burns spies by arithmetic, never by decision —
survey-verified). Archetype: Walsingham — the vetted channel, the doubled
courier, the swept court that hanged a plot by reading it first.
- **Model:** four deliberate verbs + one derived read, all pure over existing
  state. THE SUSPICION READ `suspicionOf(settlementId)` — banded, derived
  (never stored, J-INF-2) from persisted evidence: live infowar_spy_exposed /
  corruption_exposed conditions, deception-class grievances held, caught
  intercepts, and (mirror lit) THE MIRROR GAP — a neighbour acting on what we
  never showed them. THE VERBS, each an act through the existing act lanes
  (anti-hum law: trigger-only, cooldown, cap): (1) SWEEP — a paid hunt over
  the town's OWN sightPostures-directed-at-us and covert patron bindings; on
  catch: the existing exposure path fires (the built beat + blowback); on
  MISS: the sweep still costs, and above a zeal band mints the witch-hunt
  (below); (2) VET — a source-vetting read composing I2 credibility ×
  loyalty/facets/associations — the WR-7d vetting SHAPE, which is
  SPECIFIED in DESIGN_WAR_RULINGS_ARCHITECTURE.md §WR-7d and UNBUILT as of
  2026-08-02 (executed grep; §2's Verified-ABSENT row) — instantiated HERE
  for any info source: courier, factor, house, informant [CORRECTED
  2026-08-02 (fp-audit) — the earlier "generalized from envoys" read as
  built-there]; (3) SEND-TWO — two carriers for one cargo, divergent
  accounts caught by the corroboration ladder. SPEC-ONLY UPSTREAM
  [CORRECTED 2026-08-02 (fp-audit) — the earlier "built THERE for envoys"
  was a substrate error]: no envoyErrand/send-two code exists in src
  (executed grep 2026-08-02), so there is no existing read to extend —
  J-INF-15 rules ownership: if INFO lights first, the divergence reader
  builds HERE as a shared lazy leaf that WR-7d later consumes
  (cross-referenced, never duplicated; IN-4's couriers are its first
  cargo); (4) HIDE-AS-ANSWER — a NEW entry path into
  the existing HIDE posture: response to suspicion band-crossing (today's
  generic paranoia+weakness pressure stands; this adds the deliberate arm),
  entered through SP-4 (a paranoid posture crosses earlier; a trusting one
  needs harder evidence).
- **Law / force / counterforce:** the force is the court that defends its own
  mind. The NAMED counterforce off the same evidence is THE WITCH-HUNT: a
  sweep that finds nothing above the zeal band accuses SOMEONE — an existing
  resident NPC selected by the same evidence heuristics (association planes) —
  and the accusation is a REPUTATION event (no-fates law: shamed, never
  removed) minting a people-held grievance against the seat, a legitimacy
  charge, and a false_accusation receipt. Paranoia is priced by the same
  machinery that makes vigilance work; HIDE-as-answer pays IN-0d's trade tax.
  The counter-game must be winnable AND losable or it is decoration.
- **Belief posture:** suspicion is a read over evidence, never over truth — a
  court can be rightly suspicious with no spy present (the evidence was a
  coincidence) and wrongly calm with three (nothing has surfaced). The engine
  never leaks the true posture count into the suspicion read (K3-grade pin).
- **Casting:** the sweep's face is the seat's enforcer plane (existing
  rankings/roles read); the accused is cast from existing association planes;
  the vetted source's loyalty is H1 facets. Read-wiring only.
- **Receipts (house voice):** sweep launch (hum): "The gates grow teeth;
  every stranger at [X] is twice questioned." Catch: the built "paid eyes
  found among us" voice. Miss + witch-hunt: "They found no spy at [X], and
  named one anyway — the town remembers whose name it was." [CORRECTED
  2026-08-02 (fp-audit) — the earlier "hanged a name" narrated an execution
  the engine never resolves; §1b's no-fates phrase scan bites on exactly
  that verb, and §8's acceptance string matches this wording verbatim.]
  House unmasking: "The
  house's ledger, read by lamplight, named its true patron."
- **THE HOUSE PLAYS ITS OWN GAME (homed here — the survey's PARTIAL house
  agency):** (a) covert patronage gains its ORGANIC EXPOSURE PRODUCER (the
  survey's named gap: projections exist, nothing supplies the exposed list) —
  per-tick odds riding the corruption-exposure idiom, raised by the host's
  suspicion band and any sweep; on exposure, `projectPatronBindings` finally
  has a consumer and the house wears partisanship publicly (credibility
  charge; rivals' custom withers via the stamp demotion); (b) house REFUSALS
  gain the risk arm: beyond name-protection (built), a house may refuse a
  commission because the TARGET court's suspicion band prices the blowback
  above appetite — SP-4a's per-house risk tolerance consumed at the
  counter. THE REFUSAL LANDS IN `PLANT_REFUSALS` [CORRECTED 2026-08-02
  (fp-audit) — two frozen refusal vocabularies exist and the earlier text
  conflated them]: the COMMISSION-side frozen five
  (brokerageServicesPlant.js:98-101) grows by ONE to `too_hot`;
  QUERY_REFUSALS (brokerageServices.js:320-322) is UNTOUCHED; the
  closed-vocabulary pin update lands in this wave's pin list; (c) BOTH-SIDES
  SERVICE is legal and receipted — a house feeding two rivals is neutrality
  as business until an exposure makes it betrayal in both courts at once
  (one fixture, pinned).
- **Clock:** sweep cooldown (band, ~26-week idiom); suspicion evidence decays
  on its sources' own laws (conditions expire, grievances decay) — no
  separate suspicion clock (derived read, derived decay); fast layer = the
  catch beat; slow verdict = the town's remembered witch-hunt (grievance
  half-life).
- **Posture/risk:** every verb consumes SP-4 (zeal band = posture-derived);
  out-of-posture sweeps (a trusting town suddenly hunting) are priced
  receipted news.
- **Endings entries:** engagement (watcher-side, closing IN-5's arc):
  {burned, withdrawn, gone_quiet}; sweep: {catch, clean_miss, witch_hunt};
  house: {unmasked, weathered, ruined_name}.
- **Couplings:** INFO×INTERIOR (the witch-hunt feeds the commons-voice
  grievance ladder — DESIGN_FP_COUPLINGS.md), INFO×GRAMMAR (vet/send-two
  shared with the envoy program — the cross-reference discipline).
- **Pins (negative hardest):** THE CLEAN MISS — a sweep against a town with
  NO live posture finds nothing, costs its price, and mints NO spy (the
  engine never fabricates a catch to justify a sweep; seed the negative
  properly — vacuous-absence discipline); THE FALSE ACCUSATION PRICED — the
  witch-hunt arm reachable and its grievance real (one fixture: zealous
  sweep, innocent town, named accused, legitimacy charge lands); SEND-TWO's
  false-positive bound — two HONEST couriers with ordinarily-divergent
  weathered accounts do NOT flag (the corroboration ladder's tolerance band,
  pinned, or every honest pair is a traitor); suspicion-never-reads-truth
  (import pin + token scan + guard-the-guard on the suspicion module); the
  deliberate-HIDE entry fires ONLY on evidence band-crossing (no ambient
  paranoia ratchet — reversal pinned: suspicion decays, gates reopen,
  receipted); house both-sides fixture; the PLANT_REFUSALS closed-vocabulary
  pin updates for `too_hot` while QUERY_REFUSALS is asserted UNCHANGED
  [CORRECTED 2026-08-02 (fp-audit)]; registration complete for kinds minted
  this wave (`false_accusation` + the sweep and watch hums — §1c's
  mint-time rule); dormancy goldens.
- **Lifecycle line:** NO new persisted state; the witch-hunt's grievance and
  the unmasking's exposure land on EXISTING ledgers and round-trip with them;
  the accused's reputation event rides the existing incident machinery.
- **Dossier round-trip pin:** THE WATCH PANEL lands — sightPostures' first UI
  consumer (the survey's named missing surface, the twin of
  BeliefDivergenceBand): DM-only, fail-closed, showing whom the town watches,
  who watches the town (DM truth), the suspicion band, and the gate posture.
  The DM asking "is anyone spying on my town?" finds it on the town page.
  THE HOUSES BLOCK [CORRECTED 2026-08-02 (fp-audit) — the house sub-program
  was a dossier orphan: three mechanisms, no landing]: the same town page
  (DM view) mounts a Houses block — each house's standing in the town, its
  stamp competence, its patron WHERE EXPOSED (`projectPatronBindings`' FIRST
  consumer, named here: the survey's zero-non-test-consumers gap closes with
  a SURFACE, not a sentence), its refusals of record (`too_hot` among them),
  and, after an exposure, which courts it served (the both-sides fixture
  reads this page). Mounted BY this wave, premium/DM-only, fail-closed
  (J-INF-12's audience pattern).
- **Bands:** zeal band (miss→witch-hunt threshold), sweep price/cooldown,
  vet depth by posture, send-two divergence tolerance, house exposure odds,
  `too_hot` appetite threshold.

### IN-4 — THE ROAD (couriers, double agents, and the race; flags per §3)
Settled: the reputation race GAINS CONSUMERS; the errand spine carries the
medium's people. Archetypes: the Thurn-und-Taxis post rider — and Rothschild's
pigeon, the legend of who-knew-first at Waterloo.
- **Model, courier half (SEAM CONTRACT TWO):** intel gift/sale transfers ride
  SP-1 errands (typed covert/commercial; declared vs true purpose — the Q
  amendment generalized: every courier may be traveling for someone else).
  The K.2 decaying snapshot IS the cargo: what arrives is what was true at
  departure, aged hopWeeks — the D-3 self-policing (±2 bands at arrival)
  becomes PHYSICS instead of bookkeeping. Interception rides SP-1's surface;
  an intercepted sale voids through the errand's own lifecycle (lost), and
  the buyer's court runs K.7's inference on the silence. THE DOUBLE AGENT:
  a corruption-web-compromised courier (existing plane — Q's shape exactly)
  delivers the cargo TO THE TRUE PATRON first (one leg's detour, real
  ticks), then delivers a mutated telling onward — caught, if caught, by
  SEND-TWO or the stamp ladder's divergence; the WR-7d compromised-envoy
  discipline cross-referenced, never duplicated.
- **Model, race half:** at every arrival the existing ledgers stage — SP-1
  errand arrivals, army approaches, exile/wanderer arrivals, refugee
  columns — run `reputationRace` (the built, pinned, zero-consumer read)
  between the person and their story. Outcomes key REAL differences (the
  built RACE_OUTCOMES tokens [CORRECTED 2026-08-02 (fp-audit)]):
  `person` — the person first ⇒ the court hears it from the mouth
  (full-fidelity telling, first-teller credibility stake, the gate opens on
  the person's own face);
  `story` — the story first ⇒ the court has already decided (gate posture
  pre-set by the
  story's stamp — the exile arrested on rumor, the hero feasted on legend
  before he speaks); `together` ⇒ corroboration at the gate; `neither` ⇒ no
  beat (anti-hum: the trivial race is silent, J-INF-5).
- **Law / force / counterforce:** the force is who-knew-first as an outcome
  key; the counterforce off the same physics is the road itself — hidden
  paths carry no word (the wanderer's edge, built), winter slows the story
  and the rider alike, and a listener at the gate cuts the story's journey
  (built). Nobody outruns geography; law M binds both racers.
- **Belief posture:** the race compares ARRIVAL ORDER of tellings, never
  fame — the story's leg is the rumor machinery's actual path (distance-
  priced), the person's leg is the transit kernel's. No new physics.
- **Casting:** the racer is a real H1 npc (courier, exile, envoy, wanderer);
  the gate's reader is the seat's existing reader plane.
- **Receipts (house voice):** `person`: "He reached the gate before his
  story did." `story`: "The tale wore the road faster than the man; the
  gate was answered before he knocked." Market coupling: "Word of the fall
  reached the market a day before the survivors did; the grain was gone by
  the time they told it."
- **Clock:** both legs in weekly ticks on the one transit physics; the race
  is an instantaneous READ at arrival (no state); fast layer = the gate
  beat; slow verdict = what the pre-decided court then does (the arrest,
  the war escalated on a false read — K.7's machinery).
- **Posture/risk:** the gate's response to a `story` outcome runs SP-4 (a fearful
  posture arrests on tavern_talk; a deliberate one waits for corroborated) —
  the stamp rung × posture is the response table, banded.
- **Endings entries:** courier errand {delivered, intercepted, lost, turned};
  race {person, story, together, neither} — the built RACE_OUTCOMES tokens
  VERBATIM (routeNetworkConsumersRace.js:104) [CORRECTED 2026-08-02
  (fp-audit) — the earlier `person_first`/`story_first` spelling RENAMED a
  closed vocabulary while claiming it unchanged; under finite semantics the
  long forms are PROSE in the receipt sentences, never tokens, and IN-6's
  envelope grades the built tokens].
- **Couplings:** INFO×TRADE (who-knew-first market moves — the cornering
  lane's input; DESIGN_FP_COUPLINGS.md), INFO×POP (the returning survivor's
  cautionary tale racing the boomtown's legend), INFO×GRAMMAR (the envoy IS
  a racer; WR-7's program consumes this wave's race read at its own
  arrivals — cross-referenced).
- **Pins (negative hardest):** THE TRIVIAL RACE IS SILENT — an arrival with
  no story in flight mints nothing (seeded non-empty twin proves the
  positive arm — vacuous-absence discipline); THE TRUTH THAT ARRIVED TOO
  LATE (the survey's IMPOSSIBLE story, now the wave's jewel, pinned
  explicitly): the exonerating word arrives after the court has acted on
  the story — the receipt joins both tellings and names the hours between;
  the double agent's detour costs REAL legs (a doubled courier is LATER —
  lateness is the tell send-two reads; pinned); an intercepted demand
  leaves the misreading standing (K.7's shape, cross-pinned with WR-7d);
  JSON-round-trip on courier errands (alias trap); DM-KILL mid-errand
  closes lost through the one writer (the ruled lifecycle); registration
  complete for kinds minted this wave (the race gate beats + courier
  receipts — §1c's mint-time rule); dormancy.
- **Lifecycle line:** courier errands live in SP-1's ledger under SP-1's
  own lifecycle pins (this volume adds no second movement state); the
  deposit-courier retirement is flag-gated with the dark path byte-
  identical, golden-pinned first.
- **Dossier round-trip pin:** the town page's comings-and-goings (SP-1's
  dossier contract) shows couriers in transit and arrivals; race outcomes
  land as rumor-mill lines with their stamp rung; the DM asking "did the
  town know before he got here?" reads the gate note.
- **Bands:** race-beat significance floor — a CLASS ASSIGNMENT into the
  spine's SP-6 significance family, referenced never minted [CORRECTED
  2026-08-02 (fp-audit), R5]; gate-response table (stamp ×
  posture), double-agent detour cost, interception weight for cargo
  errands (terms-bearing rich-target idiom).

### IN-5 — THE VOICE (narration to war-class; the spy's ARC; no new engine
### state — beats ride their producing flags, routing is dark-safe)
Settled: 1.5% → war-class density, and the spy gains an arc, not a beat.
War's density came from giving both its hums and its moments a vocabulary
(the survey's diagnosis: every knowledge beat today is an EXPOSURE beat —
the quiet phases that make exposure dramatic are simulated, persisted, and
narrated nowhere). Archetype: the unmasking of Alfred Redl — trusted, then
suspected, then caught, the plans long since sold — a story that is nothing
BUT its quiet phases.
- **Model:** (a) THE KNOWLEDGE DESK — a SEVENTH Herald section, priced as
  what it is [CORRECTED 2026-08-02 (fp-audit) — "routing only" understated
  the surface]: `HERALD_SECTIONS` is a FROZEN closed vocabulary of six with
  a walker-pinned KIND_SECTION_CORRESPONDENCE (heraldRouting.js:61-68,
  :40-43, :401) — the desk GROWS that vocabulary, which is finite-semantics
  work: the new section token, its correspondence row, the walker update,
  and every HERALD_SECTIONS consumer audited (J-INF-7). The kinds routed
  there: every kind THIS program mints (each registered at its own mint per
  §1c), plus the `belief_misjudgment` REFILE from the faith desk (the
  survey's word-association misfile — a conscious, walker-pinned move under
  §3's goldens-first discipline, J-INF-6). `intel_transfer` STAYS UNDER
  TRADE [CORRECTED 2026-08-02 (fp-audit) — "all knowledge-native kinds"
  silently overrode a recorded judgment]: heraldRouting.js:104 carries an
  explicit authoring ruling ("intelligence BOUGHT or GIFTED through the
  obligation machinery" files with its generosity siblings), and this
  volume declines to overturn it — a refile there is a NEW owner question,
  not a side effect; (b) THE
  SPY'S ARC — a pure COMPOSER over receipts that already persist (no arc
  state, J-INF-8): recruit (engagement receipt, DM-gated) → the standing
  watch (a hum from the Watch panel's substrate) → suspicion (IN-3's
  band-crossing) → tightening gates (HIDE-as-answer receipt) → the
  unmasking (the built beat) or the quiet endings — `withdrawn` (a
  DECISION: the patron recalls its eyes) vs `gone_quiet` (the engagement
  fades unresolved), two DISTINCT closed tokens, never one ending with two
  names (J-INF-16) [CORRECTED 2026-08-02 (fp-audit) — the earlier
  "gone_quiet (the withdrawal ending)" conflated them] — five
  phases, every one already receipted by IN-3's machinery, joined by the
  composer into one telling on the chronicle; (c) THE HUMS — pacing-gated
  quiet-phase lines: the bluff still believed ("the lie is old enough now
  that men who repeat it believe they saw it"), the divergence drift (a
  court's picture of a neighbour a band and a season wrong), the standing
  watch, the sealed gates; (d) THE RETROSPECTIVE JOINS — the war brief
  names the lie (IN-2's incident-naming grievance variant consumed here);
  the coup/capture beat cites the standing corruption_exposed condition
  (the survey's secret-that-broke-a-house join: substrate persisted, one
  cause-walk line, composer-side only) — the SCANDAL join is OWNED HERE
  [CORRECTED 2026-08-02 (fp-audit)]: INTERIOR's INT-3a owns the separate
  DECISION join (decision-grievance → verdict receipt; its closed decision
  vocabulary excludes conditions), and CPL-20's registry row points at
  THIS wave; (e) THE INACTION RECEIPT — the
  court that knew and sat still: keyed off (high-confidence hostile belief
  above a strength band) AND (no defensive/offensive selection this pulse)
  — all inputs exist (survey-verified: confidence01, staleness,
  allianceLabel, the strategy chooser's selections); minted sparingly
  (significance-gated) as `court_sat_still`; (f) REGISTRATION DISCIPLINE
  RESTATED [CORRECTED 2026-08-02 (fp-audit) — this wave is NOT the
  registration home]: every wave registers its OWN kinds at mint (§1c's
  mint-time rule — WHAT_PHRASES + heraldRouting + the pacing governor +
  the knowledge family's own-vocabulary tokens); this wave registers only
  the kinds IT mints (the hums, `court_sat_still`) and re-ROUTES
  already-registered kinds to the new desk.
- **Law / force / counterforce:** narration parity is spine law (§1.11);
  the counterforce is the pacing governor (§1.12) — density with flow
  control, or depth becomes wallpaper. The desk's flood gate is authored.
- **Belief posture:** every arc and hum renders BELIEF and RECORD, never
  resolved truth on player surfaces (audience law; the arc composer is
  DM-side where it joins DM-truth phases, with a player-safe projection
  that shows only the public phases).
- **Casting:** the arc's faces are the existing planes — the watcher's
  paid eyes, the enforcer who swept, the accused, the burned spy's
  mouthpiece attribution. Read-wiring; never new NPC state.
- **Receipts (house voice):** the sentences of §8, plus the significance
  ceiling RAISED: the medium may mint a 'major' — today capped 'notable',
  survey-verified — for exactly three shapes: the lure that opened a war,
  the arc's close at war stakes, the race that decided a succession/peace
  (banded, J-INF-8).
- **Clock:** composers and hums are per-pulse reads; the arc spans years
  of receipts (the two-timescale echo made narrative: fast beats, slow
  arc).
- **Posture/risk:** none consumed (narration layer) — but out-of-posture
  ACTS narrated upstream carry their price lines here.
- **Endings entries:** the arc's endings ARE IN-3's engagement endings
  {burned, withdrawn, gone_quiet} plus IN-4's {turned} — four distinct
  closed tokens (J-INF-16); the endings vocabulary is closed here and
  enveloped in IN-6 (all four graded — `withdrawn` is not dropped).
- **Couplings:** INFO×INTERIOR (the schism and the sat-still court are
  council-grade stories — DESIGN_FP_COUPLINGS.md); the desk consumes every
  other volume's knowledge-native beats by registration.
- **Pins (negative hardest):** A QUIET DECADE MINTS NO ARC — the composer
  over a corpus with no espionage produces zero entries (anti-wallpaper;
  seeded-positive twin proves the composer fires when phases exist); the
  refile pin (belief_misjudgment on the knowledge desk, and the walker
  asserts NO kind routes by bare word-association — the misfile class
  dies); WHAT_PHRASES/heraldRouting totality (walkers red otherwise); the
  inaction receipt's negative — a court with LOW-confidence hostile belief
  that sits still mints NOTHING (sitting still on thin evidence is
  prudence, not drama); id discipline on every new kind; the arc composer
  is pure (no persisted arc state — a save/load mid-arc recomposes
  identically); audience projection fail-closed on the arc's DM phases.
- **Lifecycle line:** no persisted state; the arc recomposes from receipts
  on every read (regen/undo trivially safe — the pin above).
- **Dossier round-trip pin:** the town chronicle carries the composed arc
  entry; the Herald's knowledge desk carries the beats; the rumor mill
  already voices all four built beats (survey-verified) and gains voices
  for the new kinds — the DM finds the spy story ON THE TOWN PAGE, start
  to finish, per the crown law.
- **Bands:** desk pacing weights, 'major' eligibility bands, hum cadence
  floors, inaction-receipt confidence/strength gates.

### IN-6 — THE MEASURE (observability + certification; no flag — the WR-9
### sibling; the program is DONE when these envelopes hold on the owner-signed
### lit soak, and not before)
- **The decontamination ratchet:** knowledgeLaneEvidence's residual lists
  (15 impactKinds + 7 kind-only, survey-verified) become an only-shrinks
  baseline — every new kind this program mints classifies on its own
  vocabulary, and the certification row refuses the knowledge family while
  residuals stand (the built honesty made monotone).
- **The strength-axis gap closes:** a DISPLAY-SAFE banded truth provider
  (bands only, never engine-math reconstruction in the audit adapter —
  J-INF-11 rules the fork risk) lets beliefDivergence cover the axis the LIE
  verb actually manipulates; the SAME provider completes BeliefDivergence
  Band's documented v1 follow-up — the flagship DM surface turns from
  belief-viewer into irony-viewer (belief vs truth vs staleness, premium/
  DM-only, fail-closed). This is the wave's dossier round-trip pin: the DM
  opens the map band and SEES wrongness at last.
- **The lit/dark differential instrument:** the paired same-seed soak the
  certification row asks for (subsystemRowsRegen.js:59, survey-verified
  absent) — one seed, info flags dark vs lit, compared over the divergence
  series + the endings envelopes; the differential IS the medium's
  aliveness proof (dormancy bytes prove deadness; this proves life).
- **The envelopes (each with a mutant negative control, per protocol)
  [CORRECTED 2026-08-02 (fp-audit) — the earlier list omitted four
  envelopes that §5's waves promise HERE by name, dropped `withdrawn`
  from the arc histogram, and graded a renamed race vocabulary; an ending
  with no envelope is an ending nobody grades]:**
  knowledge-beat share of routed tokens (the 1.5% baseline → an authored
  band; war-class means the desk earns weight, not that it floods); the
  PLANT outcome mix {took, died_quiet, exposed, backfired} — the four-way
  share IN-0a promises (took vs died_quiet is the lure economy's health;
  `exposed` must carry real share or the exposure counterforce is
  decoration), with the derived lie frequency + exposure rate beside it
  (the boy-who-cried-wolf economy visible); the INTERCEPT outcome mix
  {read, refused, caught} (IN-0b's promise — a corpus where nothing is
  ever `caught` means the counter-game never bites); the TOLL INCIDENCE
  envelope (IN-0d's promise — share of settlement-weeks paying a
  non-identity factor, banded); lure
  outcome mix {sprung, resisted, exposed_first, backfired} (a corpus where
  every lure springs means the counterforces are decoration — resisted
  must carry real share); race outcome mix over the BUILT tokens {person,
  story, together, neither} (`person` rare-but-present;
  `story` dominant at distance — the physics say so); sweep outcome
  mix {catch, clean_miss, witch_hunt} (witch_hunt share is the paranoia
  health metric); the HOUSE outcome mix {unmasked, weathered, ruined_name}
  (IN-3's promise — the both-sides drama's health metric); the COURIER
  ERRAND mix {delivered, intercepted, lost, turned} (IN-4's promise); arc
  completion histogram (burned vs withdrawn vs gone_quiet vs turned — all
  four closed tokens, J-INF-16); divergence01 sane (in 0..1,
  not pinned at 1 — the built check extends to the strength axis).
- **Certification rows** for all four new flags (Growth-lane discipline),
  each naming its dispositive literals (contamination law) and its
  differential evidence path.
- **Pins:** ratchet only-shrinks; provider-parity (the display-safe bands
  agree with engine bands on a fixture corpus — no silent fork); envelope
  negative controls mutate a tuning constant and the envelope REDS (the
  harness idiom); the lit/dark pair is byte-identical at tick 0 (same
  seed, divergence is EARNED); THE EARNED-CLASSIFICATION WALKER [CORRECTED
  2026-08-02 (fp-audit) — the guard-the-guard shape, per §1c's mint-time
  rule]: every kind this program mints classifies `knowledge` with the
  bare `news` token REMOVED from BEHAVIORAL_MOVER_FAMILIES.knowledge —
  the only test that proves classification is earned by the registered
  own-vocabulary tokens rather than inherited from the residual bucket;
  the envelope list above cross-checks mechanically against every Endings
  block in §5 (no promised envelope may go unauthored again).
- **Lifecycle line:** no world state; receipts and instruments only.
- **Bands:** every envelope's authored shape — gathered in §7 for the
  owner's signature.

---

## §6 JUDGMENT BLOCKS (the drafting chair's rulings under delegation —
## vetoable here; an implementer NEVER re-rules these silently)
## [CORRECTED 2026-08-02 (fp-audit), R4: this volume's blocks are J-INF-* —
## the earlier J-IN-* collided with INTERIOR's judgment namespace (J-IN-1
## meant two different rulings in two volumes); INTERIOR renumbers to
## J-INT-*. Every reference in this volume uses J-INF-*.]

- **J-INF-1 (the mirror is derived):** second-order belief is a pure read over
  the outbound record — no persisted mirror state, ever (the spine's SP-2
  ruling made structural; re-derivation is bounded because every input ledger
  prunes). VETO persists a mirror ledger and accepts the Law One risk
  consciously.
- **J-INF-2 (suspicion is derived):** SP-5 is a closed stock family and
  suspicion is not in it — the suspicion read derives from persisted
  incidents/conditions/mirror-gap, decaying on its sources' own laws. VETO
  mints a suspicion stock INTO the SP-5 family (an owner-surface change).
- **J-INF-3 (one lie lifecycle):** the fold is the only entry for commissioned
  falsehoods; disinfo has one writer and one exposure law. VETO builds a
  parallel lure ledger.
- **J-INF-4 (one transport law):** the D-3 deposit courier retires when
  intelTrade × SP-1 are lit; dark, byte-identical. VETO keeps two courier
  physics permanently.
- **J-INF-5 (the trivial race is silent):** race beats mint only on outcomes
  with a story in flight; `neither` is always silent. VETO beats every
  arrival (and answers to the pacing governor).
- **J-INF-6 (the refile):** belief_misjudgment moves from the faith desk to
  the knowledge desk as a conscious, walker-pinned move. VETO leaves it
  under faith and records why a fog-of-war beat sits between pantheon beats.
- **J-INF-7 (the desk):** the knowledge desk is a new Herald section — a
  closed-vocabulary GROWTH priced as vocabulary work (the section token,
  its KIND_SECTION_CORRESPONDENCE row, the walker update, every
  HERALD_SECTIONS consumer audited — never a config toggle), and
  `intel_transfer` stays under trade (heraldRouting.js:104's recorded
  judgment stands unoverturned) [CORRECTED 2026-08-02 (fp-audit)]. Name
  vetoable; the structural need (war has a desk, the medium at
  war-class needs one) is not.
- **J-INF-8 (the ceiling):** the medium may mint 'major' for exactly three
  banded shapes (lure→war, arc-close at war stakes, race deciding a
  succession/peace). VETO holds the 'notable' ceiling or opens it wider.
- **J-INF-9 (no informational casus):** the taxonomy is untouched — the lure
  feeds EXISTING casus (opportunism's foe-half, deception_betrayal grievance)
  and the walker's bijection stays honest (the J-WR-2 discipline applied to
  this volume). VETO mints a `deceived_into_war` pair.
- **J-INF-10 (the planted razing stays deferred):** the lure does NOT arm a
  vengeance license in v1 — J-WR-7's deferral binds across volumes; how far
  a lie can arm a license needs its own owner ruling. Recorded, not built.
- **J-INF-11 (the truth provider is display-safe):** the strength-axis
  divergence closes via banded display-safe truth, never engine-math
  reconstruction in the audit adapter (the declared fork risk in
  knowledgeLaneEvidence stands as law). VETO reconstructs and accepts the
  fork consciously.
- **J-INF-12 (the Watch panel is DM-truth):** sightPostures' projection is
  premium/DM-only, fail-closed (the BeliefDivergenceBand audience pattern).
  VETO exposes any watch line to player surfaces.
- **J-INF-13 (exposure stays binary):** partial-leak grading is deliberately
  deferred with rationale recorded in §4. VETO schedules it (owner surface).
- **J-INF-14 (the lure complies with the landed live-strength law)
  [CORRECTED 2026-08-02 (fp-audit), R8]:** WR-1's optional
  `enforceLiveStrength` restraint (Amendment B) stands UNMODIFIED — it
  vetoes at scoring time on a GROUND-TRUTH comparison whenever the victim's
  live strength is at least the aggressor's (opportunism.js:174-186; the
  header law: fog may overstate the DEGREE of a real advantage, never
  reverse the live sign). The lure is NOT exempted, and the veto is NOT
  relaxed to a belief comparison. The weakness bait re-scopes to the
  compliant channels ruled in IN-2's Model: degree-and-timing where the
  live sign already favors the aggressor, and the misjudgment-refusal
  channel (the veto's own receipt is a story) where it does not;
  dark-restraint worlds keep the full reversal drama. Both states are
  designed states with pins; the veto is IN-2's fifth counterforce and
  `enforceLiveStrength` is an interaction named in §3, a precondition in
  NEITHER direction. VETO exempts the lure from the restraint (a war-law
  change — cross-volume owner surface) or relaxes the veto to believed
  strength (same).
- **J-INF-15 (the send-two divergence reader's ownership) [CORRECTED
  2026-08-02 (fp-audit)]:** WR-7d's SEND-TWO and envoy vetting are
  SPEC-ONLY (executed grep 2026-08-02: no envoyErrand/sendTwo code in
  src/). If INFO lights before the war program builds WR-7d, the
  corroboration-divergence reader builds HERE, as a shared lazy leaf under
  `counterIntelEnabled`, and WR-7d later CONSUMES it (one reader, two
  programs — the cross-reference discipline; forking a second reader is
  the defect the discipline exists to prevent). If WR-7d builds first,
  IN-3 consumes ITS reader and this block is discharged. Either way the
  reader has ONE home, declared at build time in the ledger. VETO assigns
  ownership differently or blocks IN-3(3) on WR-7d unconditionally.
- **J-INF-16 (`withdrawn` and `gone_quiet` are two endings) [CORRECTED
  2026-08-02 (fp-audit)]:** the engagement vocabulary holds them DISTINCT —
  `withdrawn` is a decision (the patron recalls its eyes; attributable,
  DM-visible as an act), `gone_quiet` is an unresolved fade (no decision
  on the record). A closed vocabulary cannot carry two names for one
  ending; IN-5's prose and IN-6's four-token arc histogram match this
  ruling. VETO merges them into one token (and updates IN-3, IN-5, IN-6
  together).
- **J-INF-17 (projectPlants' fail-open default stands, guarded)
  [CORRECTED 2026-08-02 (fp-audit)]:** `projectPlants` defaults
  `audience: 'dm'` and returns records unchanged on that path
  (brokerageServicesPlant.js:369-373) — fail-OPEN on its default. IN-0a's
  mount passes `audience` EXPLICITLY and pins the player projection empty
  for live plants, with a source scan catching any call site that omits
  the parameter. The safer alternative — flipping the export's default to
  'player' — is a behavior change to a built export, recorded here as the
  road not taken. VETO flips the default (and re-audits every existing
  caller).

## §7 THE TUNING SURFACE (owner-signed at the lit soak, per THE PROMISE)
Every band named in §5, gathered — reconciled LINE-BY-LINE against every
wave's Bands entry [CORRECTED 2026-08-02 (fp-audit) — the earlier table had
drifted: the spent-market margin, the DM-projection significance class, and
IN-0b's act cooldown/cap were named in §5 and absent here]:
IN-0a take threshold + the spent-market margin (the pin's asserted band) +
the DM-projection line's SP-6 significance class · IN-0b intercept vagueness
band + reach radius + act cooldown/cap · IN-0c disclosure fidelity band +
strain→fidelity map · IN-0d the HIDE tax curve per secrecy band · IN-1 mirror
input weights + staleness half-life + degradation step + confidence caps ·
IN-2 dwell-to-take + bait caps per family + spring window + backfire
magnitude + posture price tilt · IN-3 zeal band + sweep price/cooldown + vet
depth + send-two tolerance + house exposure odds + `too_hot` threshold ·
IN-4 the race-beat SP-6 significance class + the gate-response table (stamp ×
posture) + detour cost + cargo interception weights · IN-5 desk pacing +
'major' eligibility + hum cadence + inaction gates · IN-6 every envelope
shape. SIGNIFICANCE IS A FAMILY, NOT A SCALE [CORRECTED 2026-08-02
(fp-audit), R5]: every significance entry above is a CLASS ASSIGNMENT into
the spine's SP-6 narration-kit significance family — this volume tunes
assignments and per-desk instantiations, and never mints a scale of its own.
Band FAMILIES ride the spine's shared grammar to keep the owner's signature
surface tractable (spine §5's priced cost). The knowledge-lane starvation
item (selection weights for the brokerage acts) stays FIRST on the
owner-held tuning agenda — this volume builds the instruments that make
that tuning legible, and does not pre-empt it.

## §8 HERALD + LEGIBILITY CONTRACT (the sentences this program must be able
## to say — acceptance criteria, not decoration; every one carries id + full
## address chain + typed action + named settlements + recorded reason)
- "A story is bought in the lower market. It is not true. Nobody there knows
  that yet." (built — IN-0 gives it consequence)
- "They marched on a weakness that was bought for them." (IN-2)
- "Two courts, two bluffs, one war neither wanted." (IN-2)
- "The ledger of grievances opens with a forgery, and now it is named." (IN-2/IN-5)
- "By its own ledger, [X] has shown [Y] less than it did a year ago — and
  shown it nothing since [season]." (IN-1) [CORRECTED 2026-08-02 (fp-audit)
  — the record voice, matching IN-1's receipt VERBATIM; the earlier "in
  [Y]'s eyes" asserted the observer's perception, which Law One forbids]
- "The gates grow teeth; every stranger is twice questioned." (IN-3)
- "They found no spy, and named one anyway — the town remembers whose name
  it was." (IN-3) [CORRECTED 2026-08-02 (fp-audit) — no fate verb on a
  named soul; matches IN-3's receipt VERBATIM and §1b's phrase scan]
- "Paid eyes found among us." (built — IN-3 gives it a before and an after)
- "The house's ledger, read by lamplight, named its true patron." (IN-3)
- "He reached the gate before his story did." (IN-4)
- "The tale wore the road faster than the man; the gate was answered before
  he knocked." (IN-4)
- "Word of the fall reached the market a day before the survivors did." (IN-4)
- "The lie is old enough now that men who repeat it believe they saw it." (IN-5)
- "The court knew, and sat still; the knowing is part of the account now." (IN-5)
- "The court at [X] stands open to [Y]'s eyes, by the terms signed at [Z]." (IN-0c)

## §9 SEQUENCING + DISCIPLINE
1. This volume builds behind the spine (SP-1/2/4/6/7 are lit-preconditions
   named per wave) and after GRAMMAR per the spine's program order
   (SP → GRAMMAR → INFO → …). IN-0 may build early — its seams complete
   EXISTING dark machinery and depend on no spine module (IN-0c depends on
   the treaty substrate only, which is BUILT).
2. IN-0 → IN-1 → IN-2 → IN-3 in order (the fold enables the lure; the
   mirror aims it; the counter-game answers it). IN-4 after SP-1 lands
   (its degraded race arm may build alongside IN-3). IN-3's SEND-TWO verb
   is additionally sequenced on the WR-7d divergence reader, which is
   SPEC-ONLY today — J-INF-15 rules its ownership if INFO lights first
   [CORRECTED 2026-08-02 (fp-audit)]. IN-5 after every
   producer it narrates. IN-6 closes the program and is its acceptance
   harness.
3. Every wave DARK; the lighting order is the build order, after the
   spine's flags, at the owner-signed soak — never before. The knowledge-
   lane starvation tuning item and every envelope ratification are OWNER-
   HELD, unchanged.
4. The war volume's §10 implementer protocol binds verbatim (worktree,
   branch gate, no `git add -A`, no stash, gates through gate-tail.sh,
   isolation re-runs under load, one wave one commit, goldens before
   wiring, STOP-and-report on any conflict between this volume, the spine,
   an amendment, or the tree).
5. The audit stage is part of the build (spine §5): this volume does not
   ship to Sol unaudited.
