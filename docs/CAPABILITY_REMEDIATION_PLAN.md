# Capability Remediation Plan — fixing what the Atlas found

> **PROGRAM COMPLETE 2026-07-27** — see the closing Progress entry below. The resume protocol that follows is HISTORICAL (it describes the mid-R-3 account-switch state); a successor needs only the Progress blockquote, the Deferred section, and the owner-decision queue. R-5 + the owner queue remain OWNER-GATED.

## ⭐ SUCCESSOR RESUME PROTOCOL (written 2026-07-27 ~05:30, anticipating an account switch mid-program — HISTORICAL, see banner above)
A fresh session resuming this program needs exactly this file + docs/SETTLEMENT_CAPABILITY_ATLAS.md (the audit) + the auto-memory index. THIS FILE IS THE PROGRAM LOG — trust it over any session digest. Steps:
1. **Verify the tree first**: `cd /Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold && git rev-parse --abbrev-ref HEAD` → must be `claude/composite-r4`. The tree is LIVE (concurrent sessions commit under you); everything this program built is UNCOMMITTED by design (foreign hunks share every file — staging would commit others' WIP; folding is the owner's, at fold points).
2. **Determine Wave R-3's fate** (it was IN FLIGHT at handoff, run wf_410cf4c0-070; a session/account switch kills background workflows). Check, in order: (a) the task output file `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c991982e-0a25-44fe-a023-b240c17f982e/tasks/wizve774z.output` — if non-empty JSON, R-3 finished; read it, apply its must-fix findings per the wave loop, append the R-3 plan note. (b) If empty/absent: the journal `/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/c991982e-0a25-44fe-a023-b240c17f982e/subagents/workflows/wf_410cf4c0-070/journal.jsonl` lists per-agent results — completed lanes are real (their edits are in the tree); relaunch ONLY what is missing. (c) Ground truth regardless: grep the tree for each R-3 lane's signatures (destroy-writer convergence in settlementSlice.js; allowlist validation in configSlice.js updateConfig + campaignSlice.js updateSavedCampaign; staleness cure per the economy seam) and run the gate. The R-3 workflow SCRIPT (full lane briefs, reusable verbatim) is at `/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/c991982e-0a25-44fe-a023-b240c17f982e/workflows/scripts/wave-r3-single-writer-wf_410cf4c0-070.js` — a new session CANNOT cache-resume another session's run; copy the script's briefs into a fresh Workflow call, dropping lanes already landed.
3. **Also carried in R-3's verify phase**: the two R-2 adversarial re-verifications (readers-mf + prose-mf; the premium-gate change is the paid-surface obligation) — they died twice on server 529. If R-3 died before they ran, they are STILL OWED; their briefs are in the same script file (R2_CARRY).
4. **The standing rules** (owner orders, do not re-derive): Opus = VERIFICATION ONLY; Fable = everything else including implementation (owner, 2026-07-27, standing). NO git mutations of any kind in this tree; agent briefs must FORBID git stash and carry the verify-first preamble. Tests single-threaded (`--no-file-parallelism`), exit codes never through pipes. Gate baseline at handoff: verify:dist **289/289** + typecheck 0 + FULL tests/lint (265) + tests/copy (110) + tests/store (806) + architecture/fieldManifest (380) + pdf parity (51) green. Gates must include the full lint/copy/store dirs (verify:dist alone misses them — learned in R-1/R-2).
5. **Remaining program after R-3 closes**: Wave R-4 (prevention: advertised-undo invariant test, dead-op ratchet, premium-gate single-source scan, lever-field normalization — correctness manifests deliberately LAST), then program completion (final full gate, closing plan note, completion memory per memory-keeper). Wave R-5 + the owner queue (36+ items, atlas Part VII + additions below) are OWNER-GATED — never start without the owner's word.
6. **Session-limit interruptions** (this program hit three): the workflow dies cleanly; VERIFY zero partial edits landed (grep the specific fix signatures) before resuming; agents that died mid-orientation leave no edits.

> **Progress** (append after every wave — this blockquote alone must reconstruct program state)
> - **THE OWNER'S TAIL COMPLETE except T4 (2026-07-28 ~07:00): commit b89a2025** (+ the twin session's b1aeec6d dead-op/VersionsTab fold and a7f3d6c6 destroyed-mark fold, both pushed by owner order — origin carries a7f3d6c6; b89a2025 is LOCAL, push owner-executed). All four owner-ordered rows delivered: COACH EXIT (verdict: never rendered, retired + the never-fired post-signup toast finally minted), SMALL CALLS (problem terms, per-person NPC locks, all 10 dead-op dispositions via both sessions), G-2b RIDERS (four table verbs on existing engine types; blackout residual closed via canReview with the vacuous-M7 pin rebuilt to bind; locks Phase B makes the per-person promise total across full regenerates), DROPPED THREADS (#14 faction-rename converged + door + LAZY cascade — first-paint back under contract at 1,034,021 B; #18 VersionsTab snapshot+diff via the twin's fold). CONSOLIDATED GATE: 1,853 files / 17,818 tests green, ZERO golden movers, dist 41/299, typecheck green three ways (the one strict red cured mid-gate: two JSDoc params). Coordination: the two-writer protocol held throughout — thematic twin folds, hunk-level carves, an eager-budget dispute resolved by measurement (the ~9.8 KB was the faction-rename static chain; cured by de-eager, ratchet untouched). REMAINING: the T4 ONE-REGEN batch (next, last, alone) and the push of b89a2025+ (owner).
> - **THE TWO SMALL OWNER CALLS CLOSED (2026-07-27 ~21:00, UNCOMMITTED increment).** Both R-5b deferrals the closing note named as owner calls are now built, and neither is a deferral any more. (a) LOWER_IS_BETTER BENIGN VOCABULARY: the frozen three problem terms closed only the bad end of the ladder, so the lone lower-is-better variable printed "Criminal opportunity · ADEQUATE", which reads as the crime being adequate. Two strings added, both vetoable — `adequate → Contained`, `surplus → Negligible` — the table renamed `LOWER_IS_BETTER_BAND_TERM` (it is no longer only problem terms), and the walker's frozen-three assertion REPLACED by TOTALITY against CAUSAL_BANDS plus a re-declaration ban at two distinct words (one word is a coincidence: 'Acute' is also a threat-severity label in domain/qualitativeBands.js). Display-only: causalBandWord feeds four renderers and the delta sentence, none of which persist; generation goldens 21/21 unmoved, so NO golden shift and THE PROMISE is untouched. (b) PER-CHARACTER LOCK TOGGLES: the engine has read `locks.npcs` id arrays since Phase A with no way to write one. The roster row now carries a bronze padlock beside the purple pin, three vetoable strings, and the legibility question the mount comment recorded is answered by separation — PIN guards the PROSE from the AI, LOCK keeps the PERSON through a reroll, and neither sentence uses the other's word. Wired through the existing setLock (no new store action, no registry row, no compendium regen). The one real hazard found in the design: `locks.npcs` carries BOTH forms, so a row toggle writing its array over the section boolean would silently unlock the whole roster — the row reports the section lock and refuses instead, pinned. JUDGMENTS, each vetoable: the row lock is gated on `canAuthorNpc` (the same right the Reroll it disarms needs) rather than on the pin's own availability; it keys on the DURABLE npc.id, never the pin's name fallback, because the preservation engine matches and remaps ids; and its 11 pins live in tests/components/npcRowLockToggle.test.jsx, which the E-A enumeration rule does not claim (same standing as the sibling bandPolarityDisplay pin), so the manifest grew no entry — only the bandPolarity scan's rationale was amended where it had gone stale. Evidence: 3 planted regressions proven red-then-green with md5-verified byte-identical restores; band + lock + causal suites 163/163; locks engine + preservation + seeded no-drift 7/7; PDF render 29/29 (a first run showed 2 reds that were CONTENTION at load average 33 with 17 concurrent vitest processes, green solo); voice walker 16/16; manifest enforcer 6/6; eslint clean. FOREIGN red seen and NOT mine: sizeBaseline demands settlementSlice.js ratchet down 1256 → 1235, owed by the concurrent dead-op retirement lane.
> - **WAVE R-5 CLOSES — THE PROGRAM IS COMPLETE, R-0 THROUGH R-5 (2026-07-27 ~18:45).** Batch 2 SHIPPED (+120 pins across six spec-driven lanes; the specs live in the Batch-2 design record): the LOCKS ENGINE Phase A is LIVE (seeded empty-locks dormancy PROVEN against the real pipeline — locks omitted / {} / garbage all deep-equal the pre-locks run; locked sections refuse rerolls typed; identity/geography/history honored on full regenerate; setLock/clearLocks persist, curing the set-a-lock-lose-it-on-reload gap; TWELVE dead ops retired, registry ~182→170, compendium regenerated; Phase B — id arrays through full generate — deferred with its displacement-tail reason); roster PROVENANCE readable (read-side join leaf + OverviewTab badges + single-writer walker; the two write families keep their undo-scrub keys by pinned design); ReadSystemStateBar REMOUNTED (SummaryTabV2 + gallery inheritance); mass-update RETIRED (client vocabulary; mig-185 SQL documented inert); factions bucket PINNED (tier-gate allowlist cure + four-pin family); deity (b) restore-from-world + (c) converted:-namespace unification landed DRIFT-FREE — (a) lawAxis stays T4-batch with its written note in the deity lane report. Verify: 1 PASS_WITH_NOTES + 1 MUST_FIX whose three items (LockControls em-dash copy ×4, redundant tooltip vs the 487 census, locksPreservation plain-tsc types) were cured inline per the escalation precedent and re-verified 75/75 + tsc 0. Gate (Fable, 15 legs incl. tests/security FULL): GREEN — zero golden movers CONFIRMED (generators 733 + sim/prop/app 419 + root corpus 21), dist exactly 293/293, typecheck 0 three ways; the ONE this-wave red (E-H lit-coverage walker demanding the now-redundant tierOutcomeApply LIT_COVERED_BY entry be struck — the new provenance test covers it directly) was cured post-gate and re-run green. CONCURRENCY: a parallel session folded the locks/deity/provenance lanes MID-GATE (9d3e1414 · 9ea94dc1 · 831ba5c0, + the pglite ratchet 92b88c21 and the 73-file epistemic-prevention fold e56cbeaa); the gate re-ran every affected leg post-fold and its results bind to the final content. This closing fold carries the three remaining lanes (mass-update, ReadSystemStateBar, factions), the orphaned manifest entries of the concurrently-folded lanes (curing their TOTALITY-at-HEAD debt), the walker strike, and the docs. NEW OWNER-VISIBLE FINDINGS from the lanes: the onboarding coach may have NO EXIT PATH (completeOnboarding has zero callers — investigate before wiring/retiring the hints subsystem, which was deliberately NOT retired); per-character lock toggles = a legibility question (PIN guards prose from the AI, LOCK guards from the dice — two promises, one row); LOWER_IS_BETTER problem-term extension = two strings, owner copy call; TEN dead ops remain undispositioned each with a stated reason in the ratchet's comment (test-collateral retirements + UI-wiring candidates + other programs' members).
> - **R-5b batch 2 · item #22 — ReadSystemStateBar REMOUNTED, not deleted (2026-07-27 ~16:30, UNCOMMITTED increment; resumed after the prior lane died on the session limit mid-implementation — its partial edits were surveyed by `git diff`, kept where correct, corrected where they overstated).** THE VERDICT: REMOUNT. Delete was rejected on executed evidence, not taste — the strip had ZERO importers in `src/`, orphaned by a Phase-2 hand-off that was false in both halves (`tests/ui/summaryTabPhase2.test.jsx` pointed at a `workshop.test.jsx` that has never existed in either test directory, and the editor mounts the STORE-bound `SystemStateBar` inside `SettlementDetail`'s editMode gate, never this component), and NO other read surface carries the four dimensions at all (the Library health pip is a one-dimension collapse of the worst band; `SessionMode` reads the store copy, which is null for fresh generations and for the gallery). Deleting would have left the drivers/risks + BAND_HINT disclosure premium-only forever and left the atlas's own destination ruling (L9636) unexecuted. THE MOUNT: `src/components/new/SummaryTabV2.jsx`, directly under the header band, in a `padding:'14px 18px 0'` wrapper that aligns it with the body gutter. ONE mount serves BOTH promised audiences — `OutputContainer` renders SummaryTabV2 as the one Summary (`summaryMagazineV2: true`) and renders it on the readOnly arm too (only `FirstDossierCallouts` is readOnly-gated, CONFIRMED at the `case 'summary'` site), so `PublicDossierView` → OutputContainer readOnly gives the public gallery the strip for free. SummaryTabV2 stays read-only-on-props: no store import, derivation lives inside the strip. SEQUENCING HONOURED: this lane deliberately lands AFTER batch 1 #3 — `bands.js` polarity is committed at 68d14324, so the widened audience never sees the inverted band words. HONESTY REPAIRS: the component header claimed a mount that did not exist and a self-gate that does not fire; both are rewritten. MEASURED, not assumed: `deriveSystemState` is total — a near-empty `{ name: 'Bare' }` settlement renders neutral bands rather than nothing, so the ONLY null path is an absent settlement, and the sanitized gallery projection cannot blank or crash the tab (pinned). `SystemStateBar.jsx`'s "the read-view twin below in this file" comment corrected (it is its own file). ACCEPTED DIVERGENCE — VETOABLE JUDGMENT: the strip DERIVES and does not read the layered `campaignState.systemState` that `applyAuthoredStateDeltas` builds (eventPipeline.js:92-114 / batch.js:352), so on an advanced campaign save the edit-mode bar and this strip can disagree; the derived read is the default because it always matches what the rest of the dossier shows, and because the layered copy's own drivers/risks are stale by construction (the delta layer carries them through untouched). The veto path shipped WITH the judgment: `ReadSystemStateBar` now takes an optional `systemState` prop that wins outright, so honest wiring is a one-line change at any future caller. LIFECYCLE TRACE: nothing created, nothing persisted, no allowlist or schema touched; read = pure `deriveSystemState` per render, `useMemo`-keyed on the settlement identity; regen/reroll/undo all replace that object so the strip re-derives with no stale cache. DRIFT: ZERO — display-only, no rng, no store write, no engine input; `deriveSystemState` already runs over every Library card, so an extra call in the Summary cannot move same-seed or seeded-advance output. No golden re-recorded; none exists for this surface. PINS (+3, all plant-verified red-then-green): `tests/ui/summaryTabPhase2.test.jsx` — the dropped Phase-2 assertion RESTORED (the seeded 424242 town renders `read-system-state-bar` and a `system-state-grid` containing "Resilience") plus the sparse gallery-shaped settlement pin; both went red when the mount was wrapped in `{false && …}` and green when restored. `tests/components/readSystemStateBar.test.jsx` — the override pin (a hand-built layered state's numbers/band render, not the derivation's); red when the prop short-circuit was removed. The test file's dangling-pointer header is rewritten to record the remount. No NEW test files ⇒ nothing owed to `scripts/mutation-coverage-manifest.json` (all five touched files git-tracked, so no E-A untracked-subject hazard). GATE, single-threaded, exit codes unmasked: lane pins 2 files / 8 tests PASS · targeted dossier-surface set (dossier + gallery + settlementdetail smokes, tab-groups walker, summary Phase-2/Defense, economy freshness, stat-bands, living-world signals, card signals, band-polarity display, readSystemStateBar) 18 files / 139 tests PASS · `npm run build` EXIT 0 then `VERIFY_DIST=1 vitest run tests/build/ --no-file-parallelism` **39 files / 293 tests PASS, EXIT 0** — NO byte ceiling tripped, so the pre-specified `SystemStateGrid` leaf-extraction cure was NOT needed; Rollup minted `SystemStateBar-*.js` as its own 2.94 kB shared chunk between the SummaryTabV2 and SettlementDetail chunks and nothing entered the first-paint eager closure · eslint over all five touched files EXIT 0. ONE FOREIGN RED, attributed by execution: `npm run typecheck` EXIT 2 with exactly two diagnostics, both `src/domain/locksPreservation.js` TS2339 — an UNTRACKED file belonging to the concurrent #21 locks-engine lane; zero diagnostics in any file this lane touched. NOT touched, deliberately: `OutputContainer.jsx` (exactly at its 600-line ceiling and carrying another lane's in-flight edit — the mount needs no prop threading because SummaryTabV2 already receives `settlement`). Veto = unmount the strip from SummaryTabV2 and drop the three pins; the honesty repairs to the two headers stand on their own either way.
> - **R-5b batch 2 · item #6 — content.definition.mass-update RETIRED (2026-07-27 ~16:00, UNCOMMITTED increment; resumed after the prior lane died on the session limit mid-implementation).** THE FINDING THAT DECIDED IT (and the reason no successor should "re-discover" a missing bulk capability): the kind was a strict SYNONYM of `content.definition.create-revision`, not a distinct bulk lane. Both lived in `ENTRY_COMMAND_KINDS`, both took `entries[]` 1..MAX_ENTRIES(1000), and migration 185 named the string ONLY inside shared `v_kind in (...)` lists — same 0-entry check, same >1000 cap, same ordered multi-head CAS preflight, same apply loop, no mass-update branch anywhere in the SQL. The only deltas were the spec's description text and `surveyor:false`. Zero product callers: the local→cloud cutover the atlas credited (`SETTLEMENT_CAPABILITY_ATLAS.md:9200`) uses `importArchive`, and `tests/store/customContentCutover.test.js` had pinned the bulk verb's ABSENCE on that path all along — that atlas sentence was the one outright-false claim the investigation found and it is now corrected at its source. So "ship the bulk lane" was a false choice: N-entry atomic append ships today as create-revision. RETIRED (6 source files, pure deletion + tombstone comments): the `MASS_UPDATE` kind and its ENTRY_COMMAND_KINDS member (`customContentCommands.js`), `customContentMassUpdateSpec` + its registry row — the live registry goes 11 kinds → 10 (`customContentApply.js`), the `bulkInsert` service verb + its export (`customContentServiceRuntime.js`) and facade line (`customContent.js`), and the kind's arm in all three shared disjunctions (slice `needsDefinitionId`, service `appliedResultMatchesPlan`, local-ledger entries-apply). Fail-closed at three independent layers afterwards: preview refuses the kind, the registry holds no spec, the executor answers `unknown_command`. NOT TOUCHED: `supabase/migrations/185_custom_content_versions.sql` keeps the string as inert server vocabulary — in-place edits to a committed migration are owner-gated (queue M38 precedent), the behavior was byte-identical to create-revision so there is no security or correctness delta, and the tombstone comments in `customContentCommands.js` + the pglite test are the documented answer for the auditor who re-finds it in SQL. LIFECYCLE TRACED CLEAN: historical mass-update revisions are plain `custom_content_versions` rows and no reader re-validates the producing kind (the archive/cutover chain carries definitions and revisions, zero command kinds), so old cloud accounts and old local ledgers load unchanged; journal rows keep the string as inert history and nothing replays by kind except `canonEventCommandRecovery` (pinned to CANON_EVENT_APPLY); the family is append-only, so retiring removes no recovery path. COVERAGE RE-VEHICLED, NOT DELETED (6 test files): the duplicate-definitionId / duplicate-localUid / 1001-entry-cap admission pins, the SQL multi-head preflight test (renamed "preflights every multi-entry head"), the bypassed-adapter tamper rejection, and the mixed-batch all-or-nothing slice pin now ride create-revision through the identical branches; the exact-list `toEqual` in `commandRegistry.test.js` (11→10) is the re-introduction ratchet the walker structurally cannot be, and the cutover's `expect(service.bulkInsert).not.toHaveBeenCalled()` became `toBeUndefined()` — a vacuous negative converted into the service-verb retirement ratchet. The walker's non-vacuity floor was re-baselined 11→10 with a dated in-file reason and its positive control re-anchored to `content.environment.migrate` (still dynamic-vocabulary-only, so the control keeps discriminating). NO manifest edits (all six tests are `kind:"rationale"` entries), NO registry row (the kind never had an opType — `applyCustomContentCommand`'s text is kind-agnostic), so NO compendium regen. ZERO same-seed generation drift and ZERO seeded-advance drift, CONFIRMED: the command vocabulary is read by no generator and no advance path (full-repo census of `mass-update|MASS_UPDATE|bulkInsert` lands only in the command plane, its tests, and mig 185). No goldens; not T4; not owner-signed tuning. GATE (single-threaded, exit codes unmasked): the custom-content family 50/50 + pglite 19/19 (real SQL executed — both re-vehicled tests named in the verbose run) + the wider family 65 passed/1 skipped (the postgres lock-order suite, environment-skipped as always) · eslint over all 12 touched code files EXIT 0 · build EXIT 0 · **verify:dist 293/293** (the deletions shrink chunks; no byte pin moved). FOREIGN REDS, attributed by execution and NOT this lane's: `npm run typecheck` EXIT 2 on `src/domain/locksPreservation.js` (×2, untracked — the concurrent locks-engine lane) and `src/lib/surveyorByok.js` (the standing L-lane TS2339 recorded in batch 1); and 11 reds in tests/lint+docs+architecture (781 tests) whose named subjects are locksPreservation, `__plantProbe.js`, institutionRoster, deitySnapshot, npcs, undoEvent — the string `customContent` appears ZERO times in that entire log, and this lane touches none of those files. DESIGN NOTE (recorded, deliberately NOT implemented): retiring the label does not close bulk writes structurally — create-revision stays N-entry for every caller including the Surveyor-public lane; if AI mass-authoring ever becomes a doctrine concern the cap belongs in the interpret/Surveyor entry-count policy, not in kind vocabulary. Also recorded, not fixed here: the E-A walker's rationale sentence "every mutation target under src/application/commands/ is UNTRACKED" is now stale (they are all committed) — that file's owner owes the reword. Veto = restore the six deletions and re-vehicle the six test files back.
> - **Wave R-5b BATCH 1 SHIPPED (2026-07-27 ~14:45; folded same session)** — the seven owner-ratified implement items, all landed and gated: #3 G9/G10 polarity banding CURED as a DECLARED display-only shift (bands.js DIM_POLARITY + bandForDimension single source, 4 banding sites + 6 renderers routed, the self-contradicting band-crossing sentence gone; NO golden re-recorded — executed search found none pinning the old bands; 52 pins incl. a single-source scan); #8 network effects ADVISORY LABEL (one honest string; wiring-as-input recorded as future owner-signed tuning); #20 stat-bars → BANDS + DefenseTab raw-ratio retirement (21 pins, zero golden movers); catchUp undo PROMOTED + lapsed-panel Remove/Clear controls (5 pins; compendium regenerated); #28 G5 FROZEN_VS_LIVE manifest rows (2 pins, declaration-only); npcAuthoringAllowed → viewerCanAuthor ALIGNED (12 pins; the R-3 blackout WIDENING is now GONE — levers close in lockstep with the Dock; residual blackout = entitled-owner readOnly case, still G-2b-gated); #17 3D quality persistence + reload-into-paused interval arming (10 pins; the cursor's additive snapshot field absent-tolerant on old saves). Verify: 2 Opus passes PASS_WITH_NOTES, ZERO must-fix. Gate: GREEN for the wave — zero this-wave reds, generation family exactly 1143 (zero movers, empty declared-re-record list confirmed by execution), dist 293/293; 3 FOREIGN reds attributed with executed evidence (intentAtlas NUL byte, netCurrentExtractorAnchor manifest gap, surveyorByok TS2339 — all concurrent L-lanes' obligations; two landed DURING the gate). Notable lane deferrals recorded in the journals: LOWER_IS_BETTER problem-term extension = owner copy call (two strings); SubstrateTab intra-band tiebreak polarity-blindness (deliberate scope hold); compareSystemState em-dash (copy lane); economyFreshness TS7053 strict +1 (one-line JSDoc cast owed by the R-4 file's owner).
> - **R-5b batch 1 · item #3 — G9/G10 POLARITY BANDING FIXED (2026-07-27 ~13:15, UNCOMMITTED increment).** ⚠️ **DECLARED ONE-TIME VISIBLE SHIFT (owner-ratified, Part VII #3).** THE DEFECT: three of the four SystemState dimensions are lower-is-better and every one of them was banded through the strictly higher-is-better `bandFor` ladder, so a crisis town banded volatility 95 "Stable" in green and a calm town banded 17 "Critical" in oxblood — and both returned the BYTE-IDENTICAL Library health pip, leaving the "Needs attention" sort with no ordering information at all. THE CURE: `src/domain/state/bands.js` now owns `DIM_POLARITY` + `dimensionPolarity()` + `bandForDimension(key, value)` (bands off the polarity-ORIENTED score, the same shape causalState's `finalizeVariable` already used one layer down); all FOUR dimension-banding sites route through it — `deriveSystemState.finalize`, `compareSystemState.explain`, and the two `applyAuthoredStateDeltas` mirrors (eventPipeline.js, batch.js) — and the THREE stale `higherIsBetter` copies that each declared polarity and were never read (SystemStateBar DIM_META, the PDF DIM_META, livingWorldSignals `dims[]`) are DELETED, so polarity is declared exactly once. `healthPip` needed no polarity code at all once the source was oriented. G10 rode along: `causalBandWord` — which had ZERO callers outside its own file for its whole life — is now routed at FOUR renderers (SubstrateTab BandPill, pdf SystemStateSnapshot CausalSubstrate, ChronicleScrollback DiffRow, RegenerationDeltaCard ripple detail) plus `explainCausalDelta` and the `compareCausalState` band fallback, which banded a lower_is_better score RAW. WHAT MOVES FOR A READER (all display-side): band words + colours on the 4-dim tile, the PDF DimensionCard, the Library "Needs attention" sort and "In crisis" filter, the BAND_HINT one-liner, the event-log band-crossing sentence (the self-contradicting "Volatility rose noticeably (Strained → Stable) — pressure increased" is gone), the persisted `campaignState.systemState` band string on next re-derive, the Foundry journal export table, and "Criminal opportunity · COLLAPSED" → "RAMPANT". WHAT DOES NOT MOVE: **ZERO same-seed generation drift, CONFIRMED** — no generation path reads `dim.band`; the sole engine reader of a dimension is `tierResourceDynamics.js:262`, which reads `.value`, and the structural fingerprint's `causal_bands` come from `deriveCausalState` (untouched — only the display WORD changed, never the model band). Raw 0-100 values, persistence SHAPE, and every higher-is-better variable are byte-unchanged. **NO GOLDEN RE-RECORDED.** The search was executed, not assumed: no committed golden or fixture under tests/fixtures contains a 4-dim band word or a causal delta sentence (`grep` for the band vocabulary and for "rose/fell" explanation strings returns nothing outside src and the atlas), and `tests/pdf/__snapshots__/goldenViewModel.test.js.snap` carries none. PINS (+52, all executed red-then-green against planted regressions): tests/domain/bandPolarity.test.js (33 — both directions for every band family on every dimension, calm-vs-crisis derive, pip distinguishability, delta-sentence self-consistency, the persisted authored layer, and the G10 word + its higher-is-better controls), tests/components/bandPolarityDisplay.test.jsx (7 — the tile word/tooltip and the Causes-tab pill, both directions), tests/lint/bandPolaritySingleSourceScan.test.js (12 — the HABITAT guard: no src module may import the bare `bandFor`, the four banding sites are named, DIM_POLARITY is declared once, no module re-declares a `higherIsBetter` map, all four causal renderers import AND call causalBandWord, the problem-term table stays the frozen three). Manifest: one rationale entry (dirty-target amendment; two plants executed and recorded). GATE, single-threaded, exit codes unmasked: domain 8342/8342 · store+joins 1310/1310 · pdf+property+lint 923/924 · components+ui 2107/2108 · generation+generators+simulation+application 862/863 (ZERO golden movers) · build + **verify:dist 293/293** (first-paint closure 1,035,295 B, budget 1,040,000, headroom 4,705) · `npm run typecheck` EXIT 0 · eslint over all 15 touched files EXIT 0. THE FOUR REDS ARE ALL FOREIGN AND EACH WAS ATTRIBUTED BY EXECUTION: (1) `tests/lint/mutationCoverageManifest.test.js` TOTALITY — untracked `tests/security/surveyorProbeTierSql.pglite.test.js` (created 12:16 by a concurrent lane, no manifest entry, theirs to close); (2) `tests/components/npcAuthoringScope.test.jsx` and (3) `tests/generators/corruptionPass.integration.test.js` — both `findByRole`/20 s TIMEOUTS under two concurrent 600 s suites, both **16/16 and 2/2 GREEN in isolation**; (4) `npm run typecheck:domain:strict` +1 on `src/domain/display/economyFreshness.js:135` (TS7053) — PROVEN pre-existing: restoring HEAD versions of all six of my dirty domain files reproduces the identical diagnostic, and the file's only import (`registryRerunKeys.js`) is clean at HEAD; the one-line cure is a `@type {Record<string,string[]>}` cast on `RERUN_KEYS_FOR_EVENT`, left to the R-4 file's owner. DEFERRED WITH REASONS (below). Fold obligation: the three untracked pin files + the manifest hunk must ride the SAME commit as the twelve tracked-modified subjects.
> - **FOLD OBLIGATIONS DISCHARGED (2026-07-27 ~11:52): commit cf03f1eb** — the seven deferred rationale entries are now STANDING mutation plants (sweep steps 48-54), each executed red-then-green against its tracked-clean target before landing; manifest labels byte-identical to their check_caught steps; uncoveredBaseline untouched (199). The manifest's three co-dirty FOREIGN entries (aiCharter/intentAtlas lane) were carved out by index surgery — they ride their own lane's fold. KNOWN FOREIGN RED in the working tree at close: tests/config/modelRegistryParity.test.js (L-2b lane, untracked, no manifest entry — TOTALITY red, theirs to close) plus the standing 2 aiCharter voiceMechanics reds. The program's THREE commits: b0a137db (the fold, 111 files) · ef338490 (the plan log) · cf03f1eb (the plants). The push remains OWNER-EXECUTED.
> - **THE FOLD EXECUTED (2026-07-27 ~11:40, owner order "continue all the way to the end"): commit b0a137db** — all 111 program files (src + tests + scripts + api + supabase edge + the atlas's R-4 front-matter edits) in ONE fold commit; the plan doc follows as its own commit. Foreign concurrent WIP deliberately left dirty/untracked: the aiCharter/L-1 family (4 files + 2 edge bundles + 2 tests) and the intentAtlas/townMapStyleWall/deno families (13 files that landed mid-fold and were EVICTED from the commit by index surgery after the post-commit survival check caught them — preserved on disk untouched). The push remains OWNER-EXECUTED per the backup protocol. Fold-time obligations now discharged below (kind:mutation upgrades).
> - **PROGRAM COMPLETE (2026-07-27 ~08:50).** Five waves R-0..R-4 shipped as verified uncommitted increments on claude/composite-r4 @ 1af6cd0c; owner order "fix it all thoroughly" (2026-07-26) discharged for everything not owner-gated. FINAL GATE (R-4 wave-end, doubled as the program close — tree sentinel identical before/after, zero edits since): lint 302 · copy 108/110 (only reds anywhere = the 2 FOREIGN aiCharter voiceMechanics reds, L-1 program's untracked file) · store 919 · domain 8302 · joins 377 · architecture 336 · components/ui 2054 · generators/simulation/property/application 1143 with ZERO golden movers · pdf parity 51 · docs 113 · lib 1374 · typecheck 0 · build + verify:dist **293/293** (the 07-26 vendorPdfLazy red no longer reproduces). SURVIVING LEDGERS: the Deferred section (each entry documented-not-a-bug) and the owner-decision queue (R-5 + atlas Part VII + the per-wave additions incl. CONFLICT 4's three-option ruling). STANDING PREVENTION MACHINERY (maintain, don't bypass): advertisedUndoArming.walker (19) · deadOperationRatchet (10, answers to queue #21) · savedSettlementPatchKeysWalker (7) · configDirectWriterExemptions.scan (7) · premiumGateSingleSource (7) · economyReadModelCoverage.walker (12, 4-bucket fail-closed census) · terrainReadSingleSource (18, src/+api/ denominator) — plus the R-0 registry walker, R-3 configPatchAllowlistWalker, and the R-1/R-2 pin families. FOLD OBLIGATIONS (attach when the owner folds): untracked program test files land in the SAME commit as their tracked-modified subjects; the four "upgrade to kind:mutation at fold" manifest entries; the composite-r4 push stays deferred per the backup-exposure protocol (golden re-capture from a quiet tree → gate → push, owner-executed). R-5 and the owner queue were never started — they await the owner's word.
> - Wave R-4 (SHIPPED as verified uncommitted increment, 2026-07-27 ~08:45) — prevention, deliberately LAST, landed through a sustained Opus 529 outage (6 of 8 agents died MID-IMPLEMENTATION in run 1 leaving partial edits — the "agents die in orientation, leave no edits" heuristic is now DISPROVED and re-recorded; all 4 resumed fixers died twice more in run 2 while both Opus verifiers completed and verified the TREE itself; the remaining verifier-specified cures were applied inline by the Fable orchestrator per the escalation clause — see the R-4 judgments blockquote). WHAT STANDS: P-1 advertised-undo invariant walker (both directions, row-class-conditional arming modeled honestly) + dead-op only-shrinks ratchet (measured set = 36: the atlas's 31 mechanical members all still dead + 5 canon/macro outside its slice) + updatePlacement DE-advertised (metadata truth: arming re-derived ABSENT by two independent passes; compendium regenerated, 181 ops — the public badge stops claiming reversibility). P-2 premium-gate single source src/lib/viewerAuthority.js (semantics VERBATIM incl. the dead founder disjunct, owner-queued; fail-closed), both mirrors consume it, exact-set census scan both directions, half-vacuous dock pin cured. P-3 economy-freshness structural cure: ONE shared leaf (EconomyFreshnessNote.jsx) + single-home sentences, SIX covered surfaces (TableView was LIVE — the R-3 brief premise was wrong; SessionMode wired by ratified in-lane judgment), fail-closed 4-bucket census walker at 30 files (the verify pass PROVED a composer hole with a planted probe — tonightAtTheTable pattern added, SettlementPDF/variants classified). P-4 settlement-allowlist source-derived walker (census 21 sites/17 keys EXACTLY equals the allowlist — R-3's hand copy proven accurate and the coincidence converted into an enforced coupling; fail-closed UNRESOLVED semantics) + config direct-writer exact-set (1 door + 4 exemptions incl. the brief-unnamed resetConfig). P-5 CONFLICT 4 = well-evidenced DEFERRAL to an owner semantic ruling (107 rows/18 spellings/4 fatal blockers; 2 defects in the conflict rule ITSELF corrected in atlas front matter). P-6 terrain routing: 4 display readers + the CRAWLER TWIN api/_galleryMeta.js (found by verify: it served the dead un-guarded chain to every scraper) routed through resolveTerrain, twin parity + 'auto' guard pinned, denominator widened to api/; the measured shift is BIGGER than R-3's note and is recorded in Deferred. Net new green tests vs the R-3 gate: +37 lint, +43 store, +13 components, +4 verify:dist. Verify: 2 Opus adversarial passes (run-2) with MUST_FIX verdicts whose every item was closed (the closing cures applied exactly as the verifiers specified and sandbox-proved; focused suites 90/90 + the full gate are the executed evidence). Gate: CONFIRMED GREEN (numbers in the completion entry above).; fix lanes ran ~05:20 pre-account-switch, verification + must-fix + gate completed post-switch per the resume protocol — the interrupted verify workflow's three fix-lane results were recovered from its journal, fixes confirmed in-tree by signature greps, and ONLY the dead verifications were relaunched) — writer convergence & patch truth. DESTROY_SETTLEMENT converged to ONE writer (settlementSlice delegates to domainDestroySettlement, the same mutateEntities handler the composer lane dispatches; destroy rows gain destroyedCause/config._destroyed/config._destroyedByEventId — a save-blob widening flagged in the owner queue beside the standing full-row question); undoLastEvent flat-row corruption closed (typed entry_not_undoable refusal) and the must-fix pass cured the refusal's own rename JAM (Opus verify probe: one canon rename permanently froze every event beneath it — rename rows now stamp beforeState per the recordCanonFlavorEntry precedent and pop as ok:true no-ops; jam pin + hand-built pre-fix negative control; stamp source is per-lane, active→live systemState / non-active→that save's own campaignState.systemState, foreign-state swap proven otherwise); eventNarrativeSnapshots stamp unified into stampPreEventNarrative (real two-lane parity drive replacing the tautological pin, + per-lane argument-shape source scans); structured import now sweeps pulse+proposal undo rings for rewritten campaigns only (mig-184 scope proven); updateSavedSettlement 17-key census allowlist (21 call sites independently re-derived by Opus, atomic refusal); updateConfig FILTER-AND-REPORT key validation (the brief's closed-by-construction premise DISPROVED by census + fixtures: legacy save blobs replay retired keys, ~50 engine-read keys beyond DEFAULT_CONFIG; unknown keys dropped w/ typed report; declared minor shift — legacy `terrain` no longer persists into regenerated configs, a correction [stale value contradicted rolled terrainType], 4 display readers still read config.terrain first → R-4 routing candidate); updateSavedCampaign atomic refusal (3 sites, structural rows unclobberable); economy staleness = honest declaration, cure (a) provably golden-unsafe (rng draw in economyReconcilePass:261 + persist at the applyEvent seam): conditional freshness notes on EconomicsTab + ServicesTab + (must-fix) SummaryTab, byte-identical when fresh, detector = reconciliationLog × registryRerunKeys; census CORRECTION from re-verify: TableView is LIVE (flagRegistry ships tableView:true) and ~7 surfaces render economy read-models un-caveated via composeMaterialTruth — enumerated in economyFreshness.js HONEST LIMITS, structural cure (ONE shared note leaf + reader census walker) deliberately handed to R-4 rather than a fourth hand-copied string; toggle store-GLOBAL scope = INTENDED (four independent layers re-derived, 8 pins, slice header contract). Must-fix pass also cured two live tree reds: stressorEdits composed out of configSlice's allowlist literal ('stressor'+'Edits', frozen STRESSOR_EDITS_FILES untouched) + the config walker's first-key regex blind spot (census 18→20, zero refused, residuals documented); and one R-2 escape: NarrativeArchivePanel native tooltip removed (title= census back to exactly 487, baseline untouched; eventId = opaque content hash with no lookup surface, kept machine-addressable via data-event-id — owner veto point in the component docblock). Pins: +84 fix lanes (24 writers + 35 patch-validation + 25 economy-toggle) + 12 must-fix (5+1+2+4) = +96. Verify: 5 Opus passes (3 lanes + the 2 carried R-2 obligations) → 3 MUST_FIX verdicts → 4 Opus implementer must-fix lanes (per the owner's restored 2026-07-27 model split: Fable architects/validates, Opus implements+verifies) → 2 Opus re-verifies PASS_WITH_NOTES, zero residual must-fix, all lane judgments ratified. Carried R-2 obligations CLOSED: readers-mf PASS (negative control executed); prose-mf premium-gate parity sound (+13 B eager figure independently confirmed, 2.3 KB premise disproved), its record-keeping must-fix (flag-on review-blackout WIDENING) recorded in Deferred + the mount's JUDGMENT comment. Gate (Fable validator; widened further this wave to tests/domain + tests/joins + components/ui + gen/sim/prop/app): lint 265 + copy 108/110 (2 FOREIGN reds: untracked src/domain/aiCharter.js, the concurrent L-1 AI-capability-ladder program, em:23 vs baseline 0, budget 681>670 — created 05:57, after R-2's green copy gate; NOT a remediation escape, L-1 owes the cure) + store 876 (was 806; the R-3 pins) + domain 8302 + joins 377 + architecture 336 + components/ui 2041 + generators/simulation/property/application 1143 (same-seed determinism proof, zero golden movers) + pdf parity 51 + typecheck 0 + build + verify:dist 289/289 — GREEN for the program, zero this-wave reds, both former reds explicitly re-run green (48/48).
> - Wave R-2 (SHIPPED as verified uncommitted increment, 2026-07-27 ~05:00; two adversarial re-verifications pending, see below) — reachability. ChronicleScrollback now renders the FULL scrubbable history at both Herald mounts (was chronicles[0] only); Narrative Archive reader (lazy leaf, narrated Suspense fallback) gives the paid AI snapshot archive its first reader; prose-edit spine live behind settlementWorkbench=false — queueEdit('edit-prose') admits/commits through the G-1 spine, COMMITTABLE_EDIT_KINDS +'edit-prose', EDIT_COMMITTED prose_count honest, paths wired only where the full lifecycle held (ledger in lane report); premium-gate parity on the Create-flow Workbench (free/anon → read-only inspector; also closed the un-gated setEditMode side door) recorded as vetoable JUDGMENT; clear-cults control through the shed seam (orphan cults now sheddable); PDF G5 twins framed. FACTION-RENAME DOOR = correct STOP: renameFaction has ZERO callers — queue #14 stays whole for the owner. Must-fix pass closed 5 findings; finding 4's premise was DISPROVED by measurement (true eager delta +13 B, not +2.3 KB — the heavy modules were already lazy on the real vite build; guarded by tests/build/pendingEditProseLazy.test.js). Pins: +51 pass 1, +11 must-fix. Gate (widened this wave to FULL tests/lint + tests/copy + tests/store — the R-2 lesson): lint 265 + copy 110 + store 806 + pins 124 + arch/fieldManifest 380 + pdf 51 + typecheck 0 + build + verify:dist 289/289 — GREEN, zero reds of any attribution. NEW BASELINE: 289/289 (+4 = the lazy pin file, declared). OUTSTANDING: the two Opus adversarial re-verifications of the must-fix lanes failed twice on server 529 Overloaded — carried into Wave R-3's Verify phase; the wave's gate evidence is complete, the adversarial pass on the premium-gate change (paid-surface substrate) is the one open obligation.
> - Wave R-1 (SHIPPED as verified uncommitted increment, 2026-07-27 ~01:15) — destruction/undo honesty. Headline: the Surveyor protected-consent barrier was FULLY INERT (buildProtectedContext had zero production callers — sharper than the atlas recorded) and is now LIVE, flags computed edge-side AND unioned client-side; EXPOSE_CORRUPTION made phase-independent (draft-phase named-NPC deletion now demands the consent tick; KILL_NPC tombstone ruling deliberately unchanged). uncanonize→canonize now restores the canon eventLog in-session via an identity-keyed session-only tombstone (`action-partial` stays true across reload). destroySavedSettlement requires the settlement's exact name (typed ok:false refusal). The REAL proposal-undo ring landed: session-only proposalUndoStack (CAP=5, structurally separate from advance snapshots), new registered op undoLastProposalApply (registry 180→181, compendium regenerated), monotonic advance-seq stamp curing the saturation fail-open, auth-boundary + both campaign-delete paths sweep the ring; undo history panel shows both stores act-labeled. Paid copy honest: VersionsTab sells only shipped features; staleNarrative archive claim now conditional and true in all three narrated states; G5 remainder framed (ViabilityTab/OverviewTab/PDF header). Registry descriptions synced (3 rows, artifact diff confirmed exact). Pins: +60 pass 1, +15 must-fix. Verify: 5 MUST_FIX pass 1 → closed; 1 residual (tsc TS1110 from a tag-shaped token in JSDoc prose) fixed directly by the orchestrator — typecheck exit 0 + walker/sizeBaseline 22/22 CONFIRMED post-edit. Gate: zero new reds; verify:dist 285/285; the pass-1 walker red CURED. Declared shifts: consent ticks now demanded where silently applied before; confirmName gate; proposal undo appears (Library +1 op row); ~20 one-string copy edits; eager closure +227B measured. CORRECTION to Method: measured closure margin is ~8.9KB post-R-1 — the program-open "~54B" figure was stale memory.
> - Wave R-0 (SHIPPED as verified uncommitted increment, 2026-07-26 ~23:30) — registry undo-truth (tri-state `undoState` field + `partial:<ref>`/`action-partial` grammar, walker-enforced fail-closed; 2 false `undoLastPulse` advertisers de-advertised + applyWorldPulseProposal de-advertised after its arming was REVERTED for cap-flooding/mislabel; canonize/uncanonize reclassified action-partial with honest descriptions; canonizeSavedSettlement `none`) · frozen-tense honesty (DefenseTab threat/internal-security/vulnerabilities first-survey framing + PDF DefenseSecurity caption twin, per fieldManifest displayRules) · Surveyor dispatch assert (non-flagged Surveyor-origin kinds refused at dispatch, typed+translated) · deity gate fail-closed at the panel-lane store seam (single-source predicate; capacity-0 hard guard restored; free refused typed; LAPSED = shed-only, the recorded JUDGMENT below). Pins: +34 pass 1, +16 must-fix pass, −1 removed (foreign-owned). Verify: pass 1 found 10 MUST_FIX across 3 lanes (Opus); all closed in the must-fix pass; final verdicts PASS_WITH_NOTES ×4. Gate (all single-threaded, exit codes unmasked): 105 lane + 336 architecture + 39 pdf-parity + sizeBaseline + build + verify:dist 285/285 — GREEN. Baseline note: the program-open vendorPdfLazy red no longer reproduces (resolved by concurrent tree movement); 285/285 is the new baseline. Declared behavior shifts: deity panel-lane gating (above) and ~10 one-string copy edits (each independently vetoable); PDF Defense-chapter bytes change with its caption; zero same-seed generator drift (CONFIRMED by gate).
> - Program opened 2026-07-26 from docs/SETTLEMENT_CAPABILITY_ATLAS.md (204-gap ledger, Part VI; 30-item owner queue, Part VII; every C/H-grade finding adversarially verified against code by the atlas's own Opus passes). Owner order: "fix it all thoroughly" (2026-07-26). Wave R-0 launched same day.

> **Wave R-3 judgments** (delegated 2026-07-27; each vetoable; all ratified by the Opus re-verify passes; full detail in the lane reports/journals):
> - updateConfig FILTER-AND-REPORT over the brief's atomic refusal — the census + real fixtures disproved "closed by construction" (legacy-save blobs replay retired keys; atomic refusal would break Apply Saved Configuration for them). Veto = refuse whenever unknownKeys.length (one branch flip, noted in code). The refusal-posture SPLIT across the trio is deliberate: updateConfig filters (open legacy surface), updateSavedSettlement + updateSavedCampaign refuse atomically (closed literal call shapes).
> - DESTROY convergence delegates the write and keeps the flat log row; the full applyEvent-shaped destroy row = owner-gated (persistence + undo-capability change).
> - Rename-jam cure = the flavor-row precedent stamp (rename rows pop as no-ops) over un-advertising Undo. Stamp source per-lane (active→live systemState; non-active→that save's own campaignState.systemState — stamping live state onto a foreign save's row was proven to swap worlds on hydrate+undo). No-snapshot rows OMIT the field (null would blank re-derived state) and keep the refusal. `flavor: true` marker + poppedEventType fallback added (both consumer-free, one-line reverts).
> - stressorEdits allowlist member COMPOSED ('stressor'+'Edits') so the crisisTripleSync frozen file set stays frozen; applied to exactly ONE member, deliberately not generalized.
> - NarrativeArchivePanel eventId NOT re-homed visibly or as aria-label (opaque content hash, zero lookup surfaces — visible fails the legibility law, aria-label would replace the heading's accessible name); machine addressability kept via data-event-id. Veto point = component docblock + 2 pins (both DOM and source side).
> - SummaryTab reuses EconomicsTab's exact tallies sentence (one vetoable copy unit across both tally surfaces, string-parity pinned); TableView deliberately NOT patched — escalated to R-4's structural cure (shared leaf + census walker) rather than a fourth hand-copy claiming false closure.
> - registryRerunKeys header keeps a CORRECTED batch.js sentence rather than deletion (prevents the stale belief being rediscovered); header now names economyFreshness.js as a semantic dependent (trimming rows changes user-facing staleness copy).

> **Wave R-4 judgments** (delegated 2026-07-27; each vetoable):
> - 529-OUTAGE STAFFING: Opus went hard-down mid-wave (8 consecutive agent deaths across two runs, initial + retry each). Per the escalation clause the orchestrator (Fable) applied the remaining must-fix cures INLINE — every one verifier-specified and verifier-proven before application: the compendium regen, the two P-1 manifest entries (citing the verifier's four executed plants), the viewerAuthority docblock accuracy reword, the economy walker's tonightAtTheTable pattern + SettlementPDF/variants PRINT_DEFERRED rows, the api crawler-twin routing + parity pins, and the terrain denominator widened to api/. Veto = re-run any of these under an Opus lane.
> - updatePlacement DE-ADVERTISED (undoToken 'mapUndo' -> null, undoState 'none'): the walker-driven metadata-truth fix; the arming was re-derived ABSENT by two independent passes (no snapshotForUndo in the op body; its sole consumer never pushes the ring). Compendium regenerated; the public badge stops claiming reversibility. Veto = restore the row AND build the arming.
> - Routed the CRAWLER twin rather than recording divergence, because two heads disagreeing about the same shared dossier is a quiet lie on a crawled surface; module purity kept (resolveTerrain is import-free). Veto per the Deferred entry.
> - advertisedUndoArming denominator narrowed to genuinely-advertising undoStates (excludes not-applicable/undetermined) — recorded in the walker header.
> - Dead-op ratchet freezes the MEASURED 36 (atlas's 31 mechanical, all still dead, + 5 canon/macro outside its slice), not the atlas's stale count.

> **Wave R-0 judgments** (delegated 2026-07-26; each vetoable):
> - Chose de-advertising applyWorldPulseProposal over rebounding its snapshots, because arming flooded the shared PULSE_UNDO_CAP=10 stack (evicting the pre-advance snapshot) and mislabeled every advance-only undo surface; the REAL arming (separate bounded ring + labeled copy) moved to Wave R-1. Veto reverts one registry row + one string.
> - Chose `action-partial` enum over `partial:<token>` for armed-but-lossy rows (canonize/uncanonize — the event log does not come back). Veto = rename the enum value; walker updates mechanically.
> - Chose LAPSED = shed-only deity writes (may clear/remove, may not assign/change) over refuse-all-unentitled, because locking a lapsed subscriber into content they cannot remove is incoherent with the ungated undoLastEvent reversing the same config. Veto = flip the deityWriteGate shed branch (JSDoc in the gate names this line).
> - Extended honesty edits to canonize/uncanonize description strings (2 extra one-string edits beyond required field changes). Veto per string.

## Sources
- docs/SETTLEMENT_CAPABILITY_ATLAS.md — the audit. 600 rows, 204 numbered gaps (Part VI), 30-item owner queue (Part VII), 4 recorded CONFLICTs (§5). Findings are referenced below by their atlas ids (VI.n #k, queue #k). All load-bearing findings were probe-confirmed during the atlas build (node probes, importer censuses, executed greps) and survived a per-slice Opus adversarial pass plus a completeness critic — no separate probe phase is needed; probes re-run per wave at fix time.

## Method
Model split (owner order this session): Fable implements and manages; Opus verifies.
Gates: fast = focused vitest file(s), single-threaded (`--no-file-parallelism` — parallel reds are FAKE in this tree); quick = affected suites + touched ratchet/walker tests; full = `npm run build && npm run verify:dist` single-threaded. Known pre-existing red at program open: vendorPdfLazy engine-chunk ceiling (693,932 > 660,000), attributed to the concurrent programs' uncommitted work — 1 failed | 284 passed is the baseline; any NEW red is ours.
Every wave: fix → pins → fast gate → Opus adversarial verify → quick gate → plan-note update. **NO git mutations of any kind** — this is a live shared tree (574 foreign modified files; concurrent sessions commit under us). Waves land as verified uncommitted increments; folding is the owner's, at fold points. Staging any shared file would commit foreign hunks — that is why the standard one-commit-per-wave cadence is deliberately suspended (JUDGMENT, vetoable — a veto means the owner directs an explicit-file fold per wave instead).

## Owner decisions honored throughout (do not violate)
- THE PROMISE: no same-seed output drift; any legitimately shifting fix is declared BEFORE its wave and parked if it moves goldens.
- Finite-semantics law; deity doctrine (no premade pool; `deity:core:` lifecycle trace required); never resolve a named character's fate; legibility law (translate, never show formulas).
- Closure budget (~54 B margin): NEW code = lazy leaves only; nothing eager. sizeBaseline exact ceilings (JSX comments count).
- Store-action lifecycle: any new/renamed store action → operationRegistry + `gen:compendium-data` regen + label/description walkers.
- New dispatcher kinds → COMMITTABLE_EDIT_KINDS. Faction access only via nameOf/governingFactionOf/factionArchetype/npcInFaction.
- Owner-parked items (Part VII) stay parked — listed in the queue below; grazing one mid-wave = stop and flag, never ride momentum.

## Wave R-0 — Registry truth & gating honesty (risk: low; no user-visible behavior change except closing a documented fail-open)
Findings: VI.3 #62 (undoToken:null three-meaning overload); queue #5 (4 of 5 `undoLastPulse` advertisers never arm — cure = arm where the snapshot push is semantically correct at the op site, else stop advertising with a typed reason; per-op investigation, no blanket rule); the setLock/Edit-tooltip lie (tooltip promises reroll survival; setLock is inert for regen — fix the tooltip, do NOT touch regen itself: regenSection('history') destruction is owner-parked T4); queue #28/G5 frozen-record family (5 generation-frozen records shown in present tense — apply `src/domain/fieldManifest.js` FROZEN_VS_LIVE displayRules verbatim); queue #7 (Surveyor runtime gate: assert `spec.surveyor` before dispatch — defense in depth over the flag census); queue #27 deity-lane gating (single-source the premium check, fail CLOSED, and run the panel lane through the manifest predicate — JUDGMENT: "fix it all" covers enforcing a documented fail-open on a paid gate; veto reverts settlementDeityHelpers + panel in place).
Lanes (file-disjoint, enforced in briefs): A registry-truth (operationRegistry.js + walker + the 5 undoLastPulse op sites) · B frozen-tense display (DefenseTab + food-security display leaves, per displayRule) · C surveyor-assert (command dispatch seam) · D deity-gate (settlementDeityHelpers.js, DeityAssignmentPanel.jsx, buildEvent.js seam).
Gate: fast + quick per lane; wave-end joint quick gate. Verify: one Opus skeptic per lane (registry metadata and gating are sensitive substrate — plausible-but-wrong metadata has no failing test without the new pins).
Exit: tri-state undo metadata with walker enforcement; zero ops advertising undo they cannot deliver; frozen records read as generation verdicts; Surveyor dispatch asserts capability; deity writes tier-gated at ONE source; +pins counted per lane.

## Wave R-1 — Undo & destruction honesty (risk: medium)
Findings: queue #25 (uncanonize destroys eventLog — add a SESSION-ONLY eventLog snapshot restored by the canonize inverse when present; stays `action-partial` across reload — persisting the tombstone would be a persistence-shape change, owner-gated); queue #2 (named-fate cluster: 3 deletion paths bypass the consent barrier — route all through it; doctrine repair); queue #4 (settlement-terminal-death: THREE lanes with divergent confirm gates and recovery — converge confirm-gate parity and recovery honesty WITHOUT changing which lanes exist); eventNarrativeSnapshots cap disclosure (silent FIFO at 10 on a paid record — disclose at write surface; reader mount is R-2); VersionsTab pitch honesty (interim: stop selling manual snapshot + side-by-side diff that do not exist — copy-only repair, JUDGMENT vetoable; build-vs-copy stays in the owner queue).
Carried in from R-0 (same theme): the REAL proposal-undo arming — separately bounded proposal snapshot ring (session-only, own cap, provably cannot evict advance snapshots) + arm applyWorldPulseProposal against it + label proposal entries distinctly in toolbar/undo-history copy + the advance-then-11-proposals pin. G5 frozen-record remainder — ViabilityTab/OverviewTab render sites for economicViability/coherenceNotes/structuralViolations + PDF DefenseSecurity VULNERABILITIES header; assess threatAssessment.js/defenseDisplay.js present-tense prose and fix pattern-level or defer with evidence.
Exit: no destruction path advertises recovery it lacks; consent barrier is the single door for named-entity deletion; paid surfaces promise only what exists; proposal undo genuinely works without degrading advance undo.

## Wave R-2 — Reachability: mount the built (risk: medium; flag-off surfaces where applicable)
Findings: queue #19 (ChronicleScrollback paid-record reader unreachable — mount it); eventNarrativeSnapshots reader surface (paid content users already own); imposeCult(null) clear-cults path UI; faction-rename DOOR on generated saves (the convergence DIRECTION stays owner-queued — build the door on the existing lane only); the 24 phantom EDITABLE_FIELDS prose paths — wire through the flag-off Workbench EntityInspector (G-2b family work the program already owns; flag-off = no user-visible change until owner promotion).
Exit: every registered capability either reachable, flag-off-reachable, or moved to the owner retire-vs-wire queue with evidence.

## Wave R-3 — Parity forks & single-writer (risk: medium-high)
Findings: VI.12 #163b shapeless-patch trio (updateConfig/updateSavedCampaign/updateSavedSettlement — key validation at all three); DESTROY_SETTLEMENT two-writer convergence (from R-1's gate parity, now the writer seam itself); eventNarrativeSnapshots two-writer unification; economy post-applyEvent stale-derivation window (fieldManifest 'live' contracts are the spec — close the window or declare the staleness at the display site, whichever avoids golden drift); queue #9 toggle store-GLOBAL scope (investigate intended-vs-bug; fix if bug, pin-and-document if deliberate).
Exit: one writer per record family or a declared, pinned reason for two; no silent staleness.

## Wave R-4 — Normalization & prevention (LAST — correctness manifests only over honest data)
Findings: lever-field normalization to `traced input causes` (atlas + slice conventions, CONFLICT 4 cure); advertised-undo invariant test (every non-null undoToken must arm — the class guard for queue #5); dead-op ratchet (only-shrinks inventory of the 31, pending the owner's retire-vs-wire word); premium-gate single-source scan (the R-0 deity cure generalized); walkers for what still lacks them per structural-prevention.
Exit: the bug classes this program fixed have no habitat left; ratchets green over the final tree.

## Wave R-5 — EXECUTING (owner sign-off 2026-07-27: "do all of these" over the recommendations-recorded queue)
The gate opened: the owner ratified the recorded recommendations wholesale. Disposition of every Part VII item + plan addition, in three tranches. Standing laws still bind: THE PROMISE (no same-seed drift without a declared, owner-signed shift — items whose execution shifts goldens land as DECLARED one-time shifts with re-records named), no cross-program collisions (T4 ONE-REGEN batch items belong to the ledger lane), pushes/deploys owner-executed.

**R-5a — RULINGS RATIFIED IN PLACE (doc-only; each was recommendation-recorded; the owner's blanket word executes it):**
- #1 Edit toggle: KEEP (the premium paywall seam; the Workbench inspector routes through it; retire re-opens G-2a's question at its promotion, not before).
- #15 scene/canonical address spaces: NOT JOINED, ratified (the atlas's own recommendation).
- #16 settlementScene3dDefault promotion: STAYS PARKED — the promotion contract requires external human evidence; a chat ratification cannot substitute (contract outranks).
- CONFLICT 4: Option 1 ratified (recorded above + atlas §5).
- Lapsed-shed deity judgment: KEEP (R-0 recommendation confirmed).
- uncanonize-by-id verb: LEAVE until G-2b family migration (R-1 recommendation confirmed).
- Founder disjunct: KEEP verbatim in viewerAuthority (future-proof; retiring stays available any time as a one-line edit).
- Terrain crawler twin: KEEP routed (R-4 recommendation confirmed).
- SessionMode freshness note: KEEP wired (P-3 judgment confirmed).
- Rename-undo semantics + destroy-blob widening + eventId data-only addressing: all three R-3/R-4 judgments RATIFIED as shipped.
- Copyable/exported markdown staleness caveats: SCREEN-ONLY affordance ratified (export text stays clean; revisit only if users report confusion).
- #12 regenSection('history'): the structural cure is BUILT (src/domain/historyPreservation.js, tracked); the residual generated-beat identity question (historicalEvents[]/currentTensions[]) stays a recorded design note for the G-2b family migration — not a bug.
- #2 residual (tombstone-vs-successor dial): the consent barrier + EXPOSE_CORRUPTION phase-independence shipped in R-1; the dial is a G-2b-desk product design, recorded there.

**R-5b — IMPLEMENT (Opus lanes per the model split; two batches by risk):**
Batch 1 (display/registry/small-surface; no golden movers except where declared):
- #3 G9/G10 polarity banding fix — DECLARED one-time visible shift (band words, colours, Library sort, PDF); pins + the declaration ride the lane.
- #8 network effects → ADVISORY LABEL (honest, zero drift; wiring-as-engine-input stays a future owner-signed tuning candidate — recorded, not lost).
- #20 stat-bars → BANDS (legibility law) + extend the OverviewTab raw-ratio retirement to DefenseTab's sibling.
- ~~Lapsed-panel Remove/Clear controls surfaced (R-0 queue, recommended yes — seam already allows shed).~~ **CLOSED by Wave R-5b Batch 1 (owner ratified 2026-07-27).** The LAPSED branch of `src/components/settlement/DeityAssignmentPanel.jsx` is now SHED-ONLY writable instead of wholly read-only: Remove (patron → `setPrimaryDeity(null)`), per-cult Remove (`imposeCult(null, ref)`) and Remove all cults (`imposeCult(null)`). No assign or change control was added — that direction the seam still refuses, so it would be a dead control. NOT a new capability: `deityWriteGate({ shed: true })` has passed for an unentitled owner of a live embed since R-0, on the SAME ownership test the panel's branch condition runs, so panel and seam agree by construction and no control can be dead. Copy: one vetoable string, `assign.lapsedNote` reworded because "read-only" became false ("Your premium has lapsed. This settlement keeps its assigned faith, and you can still remove what it holds. Renew to assign again."); the button labels reuse the existing `assign.remove` / `assign.clearAll`. Zero goldens moved (a render gate over an existing store seam). Pins: the two tests that ENCODED the old park were rewritten, not deleted — `tests/components/deityAssignmentPanel.test.jsx` (lapsed Remove dispatches `setPrimaryDeity(null)`; no picker of either kind; no upsell branch) and `tests/components/deityClearCultsControl.test.jsx` (per-cult Remove and clear-all both dispatch with the right arity, plus a new no-impose-door pin). Veto = restore the read-only branch and both old pins.
- ~~catchUpCampaignWorld undo advertising PROMOTED (registry row + compendium regen; R-0 recommendation).~~ **CLOSED by Wave R-5b Batch 1 (owner ratified 2026-07-27).** The row moved from the referential `undoToken:null / undoState:'external:undoLastPulse'` to a first-class `undoToken:'undoLastPulse' / undoState:'action'`, with the description now naming the recovery and the one-step span. Arming RE-DERIVED at the op site rather than inherited: `runCatchUpCampaignWorld` pushes no ring entry of its own, it routes the whole caught-up span through ONE delegated `get().advanceCampaignWorld(..., { weeks: n })` whose Phase-2 commit pushes the single pre-catch-up snapshot. `'action'` and not a weaker claim because the pop restores world, member saves AND the M10b cursor. Metadata-only, zero goldens moved; `npm run gen:compendium-data` re-ran and moved exactly the one row. Pins: `tests/store/pulseUndoAdvertising.test.js` gains the promoted-row assertions plus two behavioural pins (a 2-week catch-up arms exactly one step and the pop restores tick + cursor; the three no-op paths — seeded / up_to_date / not_living — arm nothing). The `advertisedUndoArming.walker` ARMING entry was re-audited at the source and is unchanged (`kind:'delegates'`, armedBy `advanceCampaignWorld`) — it already existed because `external:undoLastPulse` was already an advertising state. Veto = restore `undoToken:null` + the `external:` state and drop the new pins.
- #28 G5 FROZEN_VS_LIVE manifest rows — the five land as declared snapshot rows with displayRules (the enforcement machinery exists; the atlas amendment names the shape).
- npcAuthoringAllowed → viewerCanAuthor ALIGNMENT (paid-surface change, owner-authorized here): free/anon Create-flow users stop seeing NPC authoring levers they cannot commit — also narrows the flag-on blackout widening. Declared.
- #17 3D quality-ceiling persistence (small saved-state addition via the persist allowlist — persistence-content, not schema).
- Reload-into-paused interval arming (pre-interval snapshot into the pausedAdvance cursor — the R-0 deferral).
Batch 2 (feature/architecture builds; design-first, then implement):
- #18 VersionsTab BUILD: manual snapshot on demand (recordSnapshot is registered and armed — wire the button) + side-by-side diff view; the paid pitch becomes true.
- #14 faction-rename convergence + door, per the recorded direction (store lane's list resolution + dual-write + library lane's cascade).
- #21 dead-op families: per-family retire-vs-wire executed; sharpest first — BUILD THE LOCKS ENGINE (setLock/clearLocks become real; state.locks consulted by regen paths; this is the regen-edit-loss structural cure the descriptions already promise). Families with no wiring case: RETIRE (registry rows removed + compendium regen + the ratchet shrinks).
- #10 provenance-ledger MERGE (composer vs pulse writers of institutions/resources) — investigation lane first (the undo rings stay separate BY DESIGN; the merge is the provenance record, not the stacks).
- ~~#22 ReadSystemStateBar: investigate remount-vs-delete, execute the winner.~~ **CLOSED by Wave R-5b Batch 2 (2026-07-27) — REMOUNT.** The strip mounts in `SummaryTabV2` under the header band, which serves the library/generate dossier AND the public gallery from one mount; the dangling hand-off pointer that orphaned it is corrected at its source. Display-only, zero drift. See the Progress entry.
- ~~#6 mass-update: investigate the one-call-site-away claim; ship the bulk lane if the spine holds, else retire the spec.~~ **CLOSED by Wave R-5b Batch 2 (2026-07-27) — RETIRE.** The one-call-site-away claim was true and irrelevant: `content.definition.mass-update` was a strict SYNONYM of `content.definition.create-revision`, which is itself N-entry (1..1000) for every caller, so the bulk capability already ships and the kind was a dead label. Client vocabulary deleted; migration 185 keeps the string as inert server vocabulary (owner-gated in-place edits). Zero drift, zero goldens. See the Progress entry.
- #24 factions bucket: ship-or-retire per investigation; tierMin reclassified either way.
- #13 deity sub-items (lawAxis drop on organic conversion; undo-less patron replacement; third converted: namespace) — investigate seeded-advance drift exposure first; implement what lands drift-free; anything drift-bearing joins the T4 batch with a written note.

**R-5c — DEFERRED WITH REASONS (not silently dropped):**
- Deity pool removal + #29 Herald record ids: T4 ONE-REGEN batch, ledger lane's sequencing — executing here would collide cross-program.
- #23 CC-CLOUD-CUTOVER rehearsal + platform evidence: owner-run infrastructure (production clone); code side is committed and clean.
- #26 flag-on review blackout full cure: rides G-2b promotion (its blocker list), narrowed meanwhile by the npcAuthoringAllowed alignment above. **RESIDUAL CLOSED by owner order 2026-07-27** — entitled review surface decoupled from the authoring gate at the Workbench mount; free/anon closure preserved (negative-control pin).
- #11 table-event economy verbs: G-2b Adjudication-desk product design — new vocabulary belongs to that program's desk spec.
- #30 DM-parked proposals sweep / order-8 film / K-1 budget / pricing sheet: other lanes' standing picks (ledger lane).
- #5/#7/#9/#19/#25/#27/#4-two-writer/#2-barrier: ALREADY DELIVERED by R-0..R-4 (recorded per wave above); #4's three-lane parity question (FORCE_ABANDON) stays a recorded design note for G-2b.

## Sequencing rationale
Metadata truth before behavior (R-0 makes the registry stop lying so later waves can trust it); destruction/undo honesty before mounting new reachability (R-1 before R-2 — do not widen access to lanes whose recovery story is false); writer unification after both sides are honest (R-3); correctness-asserting prevention deliberately last (R-4) so manifests never enshrine the pre-fix lies; owner-gated last (R-5) because none of it blocks the rest.

## Deferred (documented, NOT bugs to re-find)
- Wave commits suspended in this tree (foreign hunks in every shared file) — fold at owner fold points; this file is the program log.
- mutate.js line-number drift class: atlas citations are dated snapshots in a moving tree; re-resolve before use (documented in atlas §2).
Added by Wave R-1:
- importReconciliationCommandTransaction.js:230 rewrites campaign content but filters NEITHER undo stack (pre-existing for advance, symmetric for the ring) — R-3 writer-convergence scope.
- undoLastEvent corrupts systemState when a flat destroySavedSettlement log row is newest on a hydrated save — R-3 scope (atlas VI.10 #148).
- PDF twins ViabilityAssessment.jsx / Overview.jsx render G5 records unframed — R-2 carries them.
- Draft-phase party-caused KILL_NPC: the PATH-3 world-pulse linkage can escalate a kill to a roster deletion; if reachable from draft context the tombstone ruling may not hold there — flagged for the owner with R-3's writer work.
- interpret-session edge deploy carries the new phase-independent flag only after the next T5 deploy (client union covers meanwhile; noted in interpretCore.ts).
- Per-proposal Undo affordances on WorldPulsePanel/HeraldAdjudication desks (product call); MAP_CONTROL_HELP row for the History chip — owner queue.
- R-1 judgments (each vetoable, recorded in lane reports + code comments): PROPOSAL_UNDO_CAP=5; advance-seq counter decrements on undo-pop (ABA-safe via same-producer prune) to preserve newest-act-first walk; two surgical out-of-enumerated-lane edits (campaignAdvanceSession.js +1 block, campaignWorldPulseSlice.js declaration); net-zero `opts.confirmName` form (frozen max-lines ceiling); VersionsTab headline kept; Magic Dependency chip left as pointer.

Added by Wave R-3 verification (2026-07-27, entries appended before the wave's fold note):
- Timeline component-lane item (MERGED from two verify passes): Timeline renders its Undo button on whatever entry is newest — on a flat DESTROY row the click returns a typed ok:false and nothing visibly happens (Timeline ignores the ActionResult), and its `title="Undo this event. Restores prior state"` (Timeline.jsx:107) now over-promises on flavor rows, where the pop deletes the chronicle line while the settlement stays renamed. ONE component-lane cure: surface the refusal, hide the affordance on non-undoable rows, and make the tooltip honest per row class. Documented in code at undoLastEvent (~settlementSlice.js:2003).
- Residual narrow rename jam (probe-confirmed by re-verify): a rename recorded while state.systemState is null leaves an unstamped row that still refuses — near-unreachable (applyEvent guarantees non-null after any real event; hydrateFromSave re-derives when absent). The complete class cure is the refinement already named in undoLastEvent's comment: make undo SKIP flavor rows and act on the real entry beneath. Future lane, deliberately NOT R-4 (behavior design, interacts with Timeline).
- No-op-pop stale-systemState mechanism (probe-confirmed, NO reachable producer): renameSettlementImpl spreads currentCampaignState without re-pickling campaignState.systemState; if any future systemState writer skips re-pickling (every current one re-pickles), a rename row stamped LIVE over a stale pickled snapshot would make the "no-op" pop MOVE systemState after hydrate. Precondition-gated hazard, recorded here so the class is findable.
- flushSuppressPersist flavor-entry clobber (fixer's out-of-lane finding, DOWNGRADED by re-verify): zero assignments to flushSuppressPersist exist in src (test-only writers), so the rename-replay-drops-uncommitted-flavor-row clobber cannot fire today. Precondition-gated hazard against whoever mounts the change-queue flush; not a remediation lane.
- Copyable/exported text staleness caveats (SummaryTab Copy button still emits `**Economy:** ...` un-caveated) = OWNER CALL, recorded in economyFreshness.js HONEST LIMITS.
- Economy uncovered LIVE surfaces (~7, measured + enumerated in economyFreshness.js HONEST LIMITS "NOT YET WIRED"): composeMaterialTruth() feeds SummaryTabV2, SessionMode, TableView (LIVE — flagRegistry ships tableView:true), pdf TonightAtTheTable; plus OverviewTab/ViabilityTab/DefenseTab direct economicState reads. Structural cure = R-4: one shared freshness-note leaf + a census walker over economy read-model readers so a new reader cannot land uncovered.
- FOREIGN gate observation (so no successor mistakes it for a remediation escape): src/domain/aiCharter.js — untracked, created 05:57 2026-07-27, header names wave L-1 of docs/DESIGN_AI_CAPABILITY_LADDER.md — reds tests/copy/voiceMechanics (em:23 vs baseline 0; total 681 > EM_BUDGET 670). The L-1 program owes the em-dash strip or an owner-signed baseline; every other suite green.
- FOLD hazards (E-A class): the wave's untracked pin files (undoFlatRowGuard, narrativeStampParity, and every other `??` test in this program) must land in the SAME fold commit as their tracked-modified subjects or the manifest walkers red at HEAD; the three deferred standing mutation-sweep plants (narrativeStampParity + the two economy-freshness entries) become addable once their targets are committed — both obligations attach at fold time.
- Lane B walker residual blind spots (shorthand properties, computed keys) documented in the test header — deliberate, with the known shorthand write (`seed`) pinned explicitly.
- Legacy `terrain` declared shift: owner's one-word alternative if byte-parity for legacy display is preferred = add 'terrain' to CONFIG_PATCH_EXTRA_KEYS; otherwise R-4 routes the 4 config.terrain-first readers (lib/gallery.js:691, lib/shareImage.js:71, lib/seoDossier.js:38, GalleryDetail.jsx:123) through resolveTerrain ("the ONE terrain read" per its own docstring).
- R-2's premium-gate parity WIDENED the parked flag-on review-blackout class (found by the Opus re-verification of the carried prose-mf obligation): OutputContainer renders the standalone PendingChangesBar only when `!flag('settlementWorkbench')`, so at flag-ON the Workbench Change Dock is the SOLE pending-edit review/commit surface — and the R-2 gate suppresses that Dock for free/anon in the Create flow while `npcAuthoringAllowed` (OutputContainer) still lets the same users queueEdit through NpcLifecycleControls. Not live today (flag OFF in flagRegistry) and session-only (pendingEditsQueue is outside the persist allowlist). **CLOSED as a WIDENING by Wave R-5b** (owner sign-off 2026-07-27 took the named alternative): `npcAuthoringAllowed` now reads the same `viewerCanAuthor` chokepoint, so those levers close in lockstep with the Dock and free/anon no longer hold queueEdit intents they cannot review. The RESIDUAL blackout — an entitled owner whose saved dossier arrives readOnly with mapCanEdit=false — is untouched and still rides G-2b promotion. Coupling documented in the JUDGMENT comment at src/components/dossier/SettlementWorkbenchMount.jsx. **RESIDUAL CLOSED by owner order 2026-07-27** — entitled review surface decoupled from the authoring gate at the Workbench mount; free/anon closure preserved (negative-control pin). Two-file composition cure: the mount passes `canReview={canAuthor}` from the SAME `useStore(viewerCanAuthor)` read, and `SettlementWorkbench` renders the Change Dock on `(!readOnly || canReview)` with a review-mode sentence, so an entitled viewer holding a non-empty owner-scoped queue always has a commit/discard/review-again/undo surface while every staging lever (Edit NPC details, the prose editor, the rename door, NpcLifecycleControls) stays closed by the untouched `readOnly` plumbing. `canReview` defaults to false, so the older dock-absent-at-readOnly pin survives verbatim as matrix row M7. Matrix M1..M6 plus the elevated-role threading pin live in tests/components/settlementWorkbenchMount.test.jsx (real chain: mount, lazy Workbench, real PendingChangesBar) with dock-level rows in tests/components/settlementWorkbench.test.jsx; both negative controls were executed (forcing `canReview={true}` reds M3; restoring the `!readOnly &&` gate reds 5 rows). Recorded JUDGMENT (veto = one condition): the receipts branch is deliberately NOT gated on `!readOnly`, so committing in review mode does not delete the surface carrying its own result note and Undo.

Added by Wave R-4, lane P-5 (lever-field normalization — DEFERRED, 2026-07-27):
- **The CONFLICT 4 lever-field normalization is NOT APPLIED. Verdict: defer to an owner semantic ruling** (R-4's stated stop condition 3). It is not a mechanical rename. Evidence, all executed against docs/SETTLEMENT_CAPABILITY_ATLAS.md and written into its §3 (with a STATUS pointer added to §5 CONFLICT 4): denominator **107 row blocks carry `class: … influenceable`** (108 raw `- class:` lines; the healthPip cluster L9745 carries two) across 10 slices in **18 distinct spellings** (plus 5 rows carrying none) — not the "three slices, three spellings" CONFLICT 4 records. Changed 0, exempt 107, because four blockers each independently defeat a field rename: (a) `inputs:` is the atlas's **universal row-schema field on every class in all 15 slices** (~400 occurrences), filled `inputs: n/a (derived)` in derived-influenceable on the very rows whose levers live in `reach` — so renaming it on influenceable rows alone forks the shared schema on a class boundary; (b) the 16 causal-variable sub-rows inherit `class:` from an **inline pipe-separated shared preamble** (atlas L9783) and carry no per-row `class:` line, so a rule scoped to `class: influenceable` misses the largest concentration of lever data in the file, and no line-anchored field regex can see the preamble at all; (c) family-A rows carry causes and display-consumers **in one prose sentence** (6 of 33 measured in tabs-read-a), so normalizing means splitting sentences — rewriting verified audit findings, not relabelling them; (d) doing it re-adjudicates 107 verified rows, which §5's own merge rule reserves ("BOTH claims are kept … not adjudicated") and the Part headers' verbatim guarantee forbids.
- **Two defects found IN the CONFLICT 4 rule itself, corrected in the atlas (synthesized front matter only — no embedded slice body was touched).** Its instruction that derived-influenceable's bare `- reach:` lines "ARE lever lines" is **false for 2 of the 17**: `FROZEN_VS_LIVE` (atlas L10292) and `ENGINE_FIELD_REGISTRY` (L10303) are `class: inspect-only` rows whose `reach:` carries genuine downstream consumers. Both were appended by the same critic pass that wrote the rule, which was never re-scoped to its own additions — so a normalizer following it verbatim would have relabelled the field-law authority's consumer data as levers. Second defect: the rule treats `inputs:` as a lever spelling; see (a) above.
- **Gap #172b is over-optimistic and should not be closed by normalizing.** It says the census "cannot be pinned until the canonical `traced input causes` spelling is normalized". P-5's independent fourth run (107 rows / 5 with no lever-ish field under ANY spelling) produced yet another number alongside its recorded 68/64/60. The spread is driven by **what counts as a lever FIELD, not by how it is spelled**, so normalizing the spelling would freeze one reading of the semantics rather than settle it.
- **No walker/scan shipped, deliberately.** A guard against "the un-normalized spelling returning" is only meaningful after a normalization that did not happen; landing one now would either assert a convention no row follows (fail-closed red across 107 rows) or pin the contested 107/19 census as if settled — precisely what the sequencing rationale forbids ("correctness-asserting prevention deliberately last so manifests never enshrine the pre-fix lies"). No `tests/docs` file was added, so no scripts/mutation-coverage-manifest.json entry is owed by this lane.
- Lane P-5 touched **docs only** (SETTLEMENT_CAPABILITY_ATLAS.md, this file). Zero source files, zero goldens, zero persistence shape; no lane-file collision.

## Owner-decision queue (parked, deliberate)
See Wave R-5 list + atlas Part VII (30 items). Recommendations recorded there; nothing in R-0..R-4 depends on them.
Added by Wave R-0:
- CONFIRM the lapsed-shed judgment (shed-only deity writes for lapsed accounts) — recommended keep; veto = one branch flip.
- Surface Remove/Clear controls to lapsed users in the panel UI? The seam now allows shed but the panel still renders lapsed as read-only — product call, recommended yes for coherence.
- ~~catchUpCampaignWorld rides the advance body's snapshot push and could honestly ADVERTISE undo (`external:undoLastPulse` today) — promotion candidate, recommended promote.~~ **PROMOTED by Wave R-5b Batch 1 (2026-07-27)** — see the Batch 1 entry above for the re-derived arming, the pins, and the veto.

**Wave R-5b Batch 1 finding, NOT fixed in-lane (raised by the catchUp promotion, independent of it):** the walk-back row for a catch-up NAMES THE WRONG SPAN. `runAdvanceCampaignWorld` stamps `preSnapshot.interval = interval`, and a catch-up passes `interval:'one_week'` with `weeks:n`, so `UndoHistoryPanel`'s `INTERVAL_LABEL` renders "Undoes a week" for a snapshot that reverts up to 26. The row's return-point label (the calendar date) is correct, and this predates the promotion — the registry `undoToken` feeds the Compendium badge and search index, never this panel — so the promotion neither created nor worsened it. Left alone deliberately: `src/components/UndoHistoryPanel.jsx` belongs to another Batch 1 lane (the reload-into-paused arming), and a blind edit would have collided. The cure is small and session-only (stamp the true span on the snapshot, which is never persisted, and let the panel prefer it). Recorded so it is not re-found as a bug.

**Second Wave R-5b Batch 1 finding, NOT fixed in-lane:** `catchUpCampaignWorld` carries `receiptRef:null` while it demonstrably WRITES a receipt. Its delegated interval composes exactly one `pulseHistory` record for the whole span (pinned: `tests/store/catchUpCampaignWorld.test.js` PIN 2, `expect(ws(caught).pulseHistory.length).toBe(1)`), and both its delegate `advanceCampaignWorld` and its sibling `resolveIntervalMajors` declare `receiptRef:'pulse-record'`. Same metadata-truth family R-0 fixed, opposite direction from the undo work: the row under-claims rather than over-claims. Not changed here because this lane's brief is undo advertising, and a receipts pass should settle the general modelling rule first (does a delegating row inherit its delegate's receipt? `resolveIntervalMajors` says yes). Recommended: promote to `'pulse-record'` in a receipts sweep, with the compendium regen that implies.
- An uncanonize-by-id verb would make canonizeSavedSettlement armable (partially — event log still wiped) — build or leave `none`; recommended leave until G-2b family migration reaches versions/history.
- ~~FROZEN_VS_LIVE manifest rows for the G5 five (atlas queue #28)~~ **CLOSED by Wave R-5b Batch 1 (owner ratified 2026-07-27):** all five landed as `mode: 'snapshot'` rows in `src/domain/fieldManifest.js` (`economicViability`, `structuralViolations`, `structuralSuggestions`, `coherenceNotes`, `defenseProfile.magicDependency`), scores-precedent displayRules, `guards: []`, `pulseWriter: null`. Zero behavior change (no `src/` runtime importer of the module). Two plant-verified pins added to `tests/joins/fieldManifest.test.js`: the five stay declared, and no `src/domain/worldPulse/**` writer keeps any of them live behind a snapshot row. Manifest is now 18 contracts (10 live + 8 snapshot); atlas §2/§4/Part IV 13/10/3 counts amended at the Part IV row + queue #28. Veto = delete the five rows and the two pins; the display framing is independent and stays.
- Reload-into-paused interval arming needs the pre-interval snapshot persisted into the pausedAdvance cursor — persistence-shape change (standing, from R-0 lane A).
Added by Wave R-4:
- Terrain crawler twin ROUTED (recommended keep): api/_galleryMeta.js — the crawler-facing meta shell for every shared /gallery/:slug link — read the dead config.terrain chain un-'auto'-guarded, so the crawler head could disagree with the routed client head (or print "on auto terrain") for the same dossier. Now routed through domain/resolveTerrain with twin parity pinned from both sides (tests/build/injectGalleryMeta.test.js + tests/lint/terrainReadSingleSource.test.js api/ denominator). Veto = restore the old chain in api/_galleryMeta.js and revert the twin pins.
- P-6's terrain shift is BIGGER than the R-3 note assumed (measured; lane-recorded in the terrainReaderRouting.test.jsx header): config.terrain was never generator-written, so the old chains' first leg was dead and three surfaces (gallery tile, share card, shared-dossier head/SEO description) showed NO terrain for wizard-generated saves — they now show the resolved one, including a CRAWLED og/meta description. The one-word owner alternative from R-3 still stands per site.
- SessionMode WIRED with the freshness note (lane P-3 in-lane judgment, recorded at SessionMode.jsx:332-340, ratified by the R-4 verify): the session-play register argument did not distinguish it from the equally-live TableView. Veto = move it to the walker's FROZEN_DEFERRED with a written reason.
- CONFLICT 4 lever normalization = DEFERRED to an owner semantic ruling (lane P-5; census 107 rows / 18 spellings / 4 independently fatal blockers; ruling options + recommendation in the owner queue). No walker shipped — deliberately, to avoid enshrining a contested census.
- Fold obligations added this wave: the four R-4 manifest rationale entries say "upgrade to kind:mutation at fold"; the untracked R-4 walker/test files land in the same fold commit as their tracked-modified subjects (E-A).

Added by Wave R-3:
- destroySavedSettlement now persists destroyedCause + config._destroyed + config._destroyedByEventId onto the save blob (convergence onto the composer lane's existing shape; no reader, no golden moved — Opus-verified acceptable). It IS a save-blob widening: fold into the same owner note as the standing "full applyEvent-shaped destroy row" question.
- Rename-row Undo semantic (owner awareness): clicking Undo on a rename chronicle row now REMOVES that line while the settlement stays renamed (flavor-row precedent; previously the click silently did nothing and jammed the stack). Veto = revert the beforeState stamp (restores the jam — pair any veto with the Timeline affordance cure).
- NarrativeArchivePanel eventId visibility: recommended keep as data-event-id only (content hash, no lookup surface); veto point in the component docblock.
- Exported/copied text staleness caveats (screen-only affordance today) — product call.
- npcAuthoringAllowed ↔ viewerCanAuthor alignment (would change which users see NPC authoring levers in the Create flow — paid-surface behavior): R-4's scan documents the divergence; alignment needs owner sign-off. **RULED + SHIPPED (owner "do all of these" 2026-07-27; Wave R-5b Batch 1).** `npcAuthoringAllowed` now carries a `viewerMayAuthor &&` conjunct reading the chokepoint (src/components/OutputContainer.jsx). DECLARED SHIFT: free/anon lose NpcLifecycleControls in the Create flow; premium/founder/elevated unchanged; the Library, public-dossier and player-view arms unchanged (canAuthorNpc is already a strict narrowing of the same predicate). Zero goldens moved — this is a render gate, not generated content. Pins: tests/components/npcAuthoringScope.test.jsx (per-tier matrix, 4 denial pins negative-controlled). Census: the 'documented-divergence' reason class RETIRED from tests/lint/premiumGateSingleSource.test.js, which now asserts OutputContainer as the third consumer and enforces reason classes in both directions; OutputContainer keeps a census row under 'content-visibility' for its separate `viewerIsPremium` read (War & Faith tab presence), so the row could not retire outright. Veto = drop the conjunct and restore the R-4 comment.
- Dead 'founder' tier branch (no code path produces auth.tier==='founder'; founder-transfer sets tier:'premium' + is_founder): R-4 single-sources the spelling RETAINING the disjunct verbatim; retire-or-keep is the owner's word.
- src/domain/aiCharter.js voice reds (foreign L-1 file): owner should direct the L-1 lane to strip the 23 em dashes or sign a baseline raise — the remediation program will not touch it.
Added by Wave R-4 (lane P-5):
- **CONFLICT 4 lever-field normalization — RULED (owner, 2026-07-27: Option 1 ratified — slices verbatim, §3 as decoder ring, #172b open by construction; options 2/3 retired; atlas §5 STATUS updated).** Original menu kept for the record: Three options, recommendation first. **(1) RECOMMENDED — leave the slices verbatim and keep §3 as the reader's decoder ring.** The atlas is a dated snapshot whose evidentiary value rests on the verbatim guarantee; the corrected §3 rule now tells a reader exactly how to read each of the four families, which is what a consumer actually needs. Cost: `traced input causes` stays aspirational, and gap #172b stays open by construction. **(2)** Re-adjudicate: authorize rewriting the lever field on all 107 influenceable rows, accepting that family-A prose gets SPLIT and the verbatim guarantee is formally retired for those rows — a genuine re-audit, not a rename, and it needs its own verification pass because it changes what the audit says. **(3)** Normalize only economy-family + power-faith (the 25 rows where the split is already clean), leaving derived-influenceable's inversion and family A's hybrids alone — cheap, but it makes the file MORE inconsistent, not less, so it is recommended against. Nothing else in R-0..R-4 depends on this.

Added by Wave R-5b batch 1 (item #3 — G9/G10 polarity banding):
- **The lower-is-better problem-term table stays the frozen three.** `causalBandWord` re-phrases only `collapsed`/`critical`/`strained`; on the one lower-is-better variable a benign band still prints "Criminal opportunity · ADEQUATE", which reads as a statement about crime being adequate rather than about the settlement being fine. Extending the table to `surplus`/`adequate` is NEW READER-FACING COPY on a frozen vocabulary set, so it was deliberately not written here. Owner call: two strings (e.g. contained / quiet) or leave as-is.
- **SubstrateTab's intra-band sort tiebreak is still polarity-blind.** `rows.sort` keys on band rank first (correct — the band is oriented) and then on the RAW score ascending, so criminal_opportunity at score 100 sorts LAST among its same-band siblings instead of first. The row still floats to the top of the grid because the primary key is right; only order WITHIN a band is affected. Left unchanged because the declared shift covers band words and the Library sort, not the Causes-tab row order, and widening a declared shift without need is the thing the declaration exists to prevent.
- **Existing saves keep their stale persisted band string until the next re-derive.** `campaignState.systemState` persists `{value, band, drivers, risks}`; the SHAPE is unchanged and every mutating path re-derives (nine sites, censused in G6), and `hydrateFromSave` re-derives when the snapshot is absent — but a save that is loaded and never touched still shows the pre-fix word until something re-derives it. No migration was written: the band is a display derivation of a value that did not move, so a migration would rewrite saves to produce a string the next re-derive produces anyway.
- **compareSystemState's delta sentence still carries a reader-facing em dash** (` — pressure increased`, VOICE §6). Pre-existing and untouched: fixing it here would move a persisted user-facing string for a reason unrelated to the declared polarity shift. Belongs to a copy lane.
- **`src/domain/display/economyFreshness.js:135` TS7053 (+1 over the zero domain-strict ceiling) is a FOREIGN, pre-existing red**, proven by executing the strict check with HEAD versions of all six dirty domain files in this lane (identical diagnostic) and by the file's single import being clean at HEAD. Cure is one JSDoc cast on `RERUN_KEYS_FOR_EVENT`; it belongs to the R-4 file's owner, not to this lane.

**Wave R-5b item #3 judgments** (delegated 2026-07-27; each vetoable):
- CURE SHAPE: orient the SCORE at band time (`100 - value` for a lower-is-better dimension) rather than introduce a second band vocabulary. Chosen because the health words (Stable/Strained/Vulnerable/Critical) are polarity-NEUTRAL statements about the settlement — "Already failing" is the right line at volatility 95 exactly as at resilience 10 — and because the causal substrate already cured its own version this way (`finalizeVariable`), so the two layers now share one shape. Veto = a distinct lower-is-better word set, which is new copy on a frozen vocabulary.
- SCOPE WIDENED past the atlas's two named G10 renderers to four, plus `explainCausalDelta` and the `compareCausalState` band fallback. The atlas named the two surfaces it had read; the census found two more printing the same raw word and one fallback banding a lower_is_better score RAW. Leaving those is the "partial projection repair silences the certifier" hazard by construction. Veto = revert the ChronicleScrollback and RegenerationDeltaCard hunks (each is one import + one expression).
- `causalBandWord` output is LOWER-CASED in sentence contexts (the delta string, the chronicle band pair) and left as-authored where the surface already uppercases (the two pills). The raw band vocabulary is lowercase, so this keeps one register per surface and leaves all fifteen higher-is-better variables byte-identical. Veto = drop the `.toLowerCase()` at the two sentence sites.
- UNKNOWN DIMENSION KEYS fall back to `higher_is_better` rather than throwing, so a future fifth dimension bands exactly as the pre-fix code did until it declares its polarity in `DIM_POLARITY`. Veto = fail-closed. The scan pins that the table holds exactly the four known keys, so an undeclared fifth is caught by the enforcer rather than by a runtime throw.
- The G9/G10 SLICE ROWS in the atlas were left VERBATIM per the Part headers' guarantee; the amendment is a STATUS pointer on Part VII #3 only, following the R-4 lane P-5 precedent (synthesized front/back matter may be annotated, embedded slice bodies may not).

**Wave R-5 coach-exit lane — the onboarding trio's owner-queue-#21 disposition** (delegated 2026-07-27 under the owner-ordered investigation + the standing risk-appetite directive; each vetoable):
- **RETIRE, not wire.** `completeOnboarding`, `markFeatureUsed` and `resetOnboarding` left the registry together with the first-run coach state machine and the feature-hints subsystem they described. The investigation the R-5 closing note asked for found the coach HEADLESS: its only rendered output was two `data-onboard-highlight` attributes in GenerateWizard.jsx, and no CSS rule anywhere in the repo targets that attribute — so the coach could never be seen. It also had no exit: `completeOnboarding`, the sole writer of `sf_onboarded`, had zero callers, so every session re-entered the invisible coach and stalled at step 3. Meanwhile the live first-run teaching had already been rehomed to the guidance registry (W-GUIDE-1 / host C4: the PostGenCoach whisper host with real exit paths through the unified `sf:guidance:*` dismissals, plus FirstDossierCallouts, whose firsts are derived by `deriveGuidanceFirst`, never flagged). Wiring the trio would have stood a second teaching system up beside the registry, against the one-guidance-system consolidation (single whisper budget, unified dismissals, newborn gating). Reversal path = re-mint the three registry rows + the slice actions and regen the compendium mirror.
- **ONE LIVE DEFECT REPAIRED, and it changes what users see.** `src/store/index.js`'s F34 post-signup save handler has always called `useStore.getState().setOnboardingNudge(...)` behind a `typeof` guard — and no slice ever defined that action, so the "Saved as {name} — view it in Settlements." toast has never once appeared. `setOnboardingNudge` is now minted on the surviving nudge channel (exempt row; session-only, absent from the persist partialize). DECLARED BEHAVIOR SHIFT: that toast starts appearing after a signup-save. Zero generation drift and zero seeded-advance drift — no generator, domain-simulation, SQL or edge file was touched and no golden moved.
- **NO localStorage MIGRATION for `sf_onboarded` / `sf_features_used`.** Both keys' only writers were the callerless actions above, so a present key is not evidence the user was ever taught anything; mapping either onto a `sf:guidance:*` whisper dismissal (LEGACY_DISMISSAL_MIGRATIONS) would wrongly suppress teaching they have not seen. The keys are abandoned residue and their only readers are gone. Veto = add the migration entries in src/lib/guidance.js.
- **The dead-op ratchet's comment-only positive control was RE-ANCHORED** from `markFeatureUsed` (retired here, and its guidanceRegistry JSDoc anchor rewritten) to `requestProgression` / src/hooks/useReaderAudience.js. The spec's proposed anchor, `destroySavedSettlement`, is no longer usable: owner queue #21's wiring half gave it a real consumer (DestroySettlementControl.jsx). Frozen dead list 4 → 1; EXEMPT_CEILING 72 → 69 (four coach rows out, one nudge row in).
