# THE RE-CUT PROGRAMME — the machinery follows the law (plan)

> **Progress** (append after every wave — this blockquote alone must reconstruct program state)
> - Wave 1 (in flight) — dock cars `6a6c2ddf3` (8b-W-18a: rulings 1/2/12) and `6e5ee9716` (8b-W-18b: the round-1 draft retired; the `{defmaterial}` fill, ruling 10; the inherited face-count red gone at this tip); the marker's instruments (car 18d) and the source filter + pair draw (car 18c, ruling 15) pending; then the one-pool v3 run.
> - Wave 0 (shipped, `b9c3fe322` §919.6, sealed `refs/preserve/fable-sitting-2026-09-12`) — the sitting document, 1,137 VALIDATION cells, CONTRADICTION-TABLE §V (35 finding rows, the floors re-worded, 6 new wiring rows, 2 new §1.1 rows), CHAIR-NOTE E-4(b) struck by annotation, ADDENDUM 18 (13 rulings) into the kit brief and `verbatim/`, `items-ADDENDUM-18.md` (13 rows), the kit's law files under `docs/rewrite-kit/`.
> - Program opened 2026-09-12 ~21:00 (session 4e3d2f70, Fable chair) from the FABLE SITTING of the same evening (`docs/rewrite-retro-2026-09-12/FABLE-SITTING-2026-09-12.md`) at the owner's word: *"along this new direction, i am amenable to other changes to previously stated laws and rules that help get to the intent behind why we did everything … without the machinery becoming the very obstacle for its completion. do you have any other suggestions?"* → seven suggestions → **"do all of that."** → four taste additions → **"do it all."** → the owner's two observations (no `{settlement}` inside a face; THE CLERKS) = thirteen rulings, brief ADDENDUM 18.

## Sources
- `docs/rewrite-retro-2026-09-12/FABLE-SITTING-2026-09-12.md` — the Fable seat's retrospective validation of the 2026-09-12 re-cut: 1,137 archive rows validated by twelve seats (every judgment row, every SPLIT, every STRUCK, the floors' twelve code claims, 684 faces refuted across three corpus versions). Counts and reversals are in the sitting.
- `docs/rewrite-retro-2026-09-12/` — the retrospective record (§919.3–§919.5), which this programme amends but never rewrites.
- The sealed kit `refs/preserve/chair-tools-2026-09-05` (a75dbc21f, autosaved 2026-09-12 20:07:09) — the brief with ADDENDA 1–17, the block workflow, the packets.

## Method
Gates (this repo, run in the block dock, one focused file at a time, never the whole suite while another runner is up): fast = `npx vitest run tests/copy/voiceMechanics.test.js tests/data/dossierStateProseProjection.contract.test.js`; quick = the same plus `node scripts/prose-wave-gate.mjs --section defense --out .packets/chair` (mechanical arms only); full = the block gate's whole-suite proof (`run-gate-9xx.sh` shape from the kit) before a landing. Every wave = fix → focused tests → gate → adversarial verify where the substrate is sensitive (prompts a seat reads; the gate's refusals; anything that changes what a game master reads) → one commit naming the wave → plan-note commit. The LEDGER branch (`review-fixes-2026-07-08`, the main tree's HEAD) takes docs; the PRODUCT (`claude/composite-r4`) moves only by CAS landing. Push and the WALK stay the owner's.

## Owner decisions honored throughout (do not violate)
- THE PROMISE (a seed is a starting world forever) — floor 2 is constitutional; it is re-worded, never relaxed.
- The four floors survive; the deity doctrine as amended by ADDENDUM 15 (the followers act, the god does not).
- ONE LANE AT A TIME (owner 2026-09-11).
- REFUTE WITH OPUS (owner 2026-09-11 20:5x) — the seat allocation is untouched by this programme.
- "Keep it all in some historical format for fable retro validation" (owner 2026-09-12) — every strike or re-pointing of ratified law in this programme gets an item row in `docs/rewrite-retro-2026-09-12/` (bounded per wave 0's ruling 7: strikes of RATIFIED law, not every chair ruling).
- The push and the walk are the owner's.

## Wave 0 — THE SITTING LANDS (ledger only; no product bytes)
What lands: `FABLE-SITTING-2026-09-12.md` (the seven rulings signed under "do all of that", the seat-by-seat results, the reversal roster); every validated row's `VALIDATION` cell filled (OK · CHALLENGE · REVERSE) in the six `items-*.md`; the reversals returned as rows of `evidence/CONTRADICTION-TABLE.md` (or corrections to existing rows: F1-29, F1-93, F4-09, F4-12, F3-05's count, Appendix A/B, the W25c line, §D D-10/D-12/D-18); CHAIR-NOTE §2.2 E-4(b) struck by name; the floors' riders (floor 1 "closed to the keys", the office roster dropped from the five, the same-page read set; floor 4's three removal paths and fabric scope); brief ADDENDUM 18 (the owner's word and the seven changes) into the kit and the ledger; the kit's law files (`rewrite-block-v2.workflow.js`, `tables-14.txt`, `rulings-DEF2-v14.txt`, the brief's ADDENDA 14–18) committed under `docs/rewrite-kit/` so the law is never again only in `/private/tmp`.
Exit: the ledger commit exists; `git grep` finds every reversed id in the table; the memory START HERE row points here.

## Wave 1 — THE MACHINERY RE-CUT (dock laneRW-DEF2; product bytes in `scripts/` and `docs/`; risk: medium)
Rulings 1–5. (1) the wave gate's per-face band depth retired — `ENTRY_NUMBERS.depth` and the `failing` band arms no longer hold a pool open; the gate's refusals are MECHANICAL ONLY (counts, tag grammar, projector, the E2 voice ratchet); taste moves to the pool-grain DULL verdict plus a BLOCK-grain measurement (distinct words · content types · `,and` rate · per-pool content ratio, the `measure-prose.py` numbers) reported, not refused. (2) the face contract in `arch-prose/ARCH-COMPOSED-PROSE-v2.md` §2.1/§8.4 amended: faces share slots and marks BY CONSTRUCTION, never a claim set; a face may not CONTRADICT its siblings and need not restate them. (3) `rewrite-block-v3.workflow.js`: Mark → Draft → Refute (contradiction + DULL) → Cure, one round each; the Refine and the separate Judge retired (the refuter's verdict applies unless the chair overrides); licence-era residue purged from VOICE / writer / refiner prompts. (4) the marker's product is WHAT WOULD BE FALSE: the tier's `required: true` rows at the head of the card, the pool key's preimage, the SAME-PAGE READ SET (union of `row.reads` across every producer `dossierMounts.js` mounts on the tab) and the sibling machine sentences; the licence card retires as the seat's primary instrument. (5) `EXEMPLAR-PACK.md` scanned by the floor-2 grammar test and its failing example lines rewritten.
Rulings 8–13 also land here: (8) the SELECTOR — two or three writers per pool under different exemplar lenses, one reader picks the twelve, then the refuter; (9) the writer's brief is ONE PAGE (the pack, the shipped rows, the marker's card); (10) the FILLS — `{defmaterial}` (O-1), the fortification's name, the roster row's name through a typed slot (product bytes; a fill is a register-car change and lands as its own car with the projection contract); (11) the FROZEN-FIELD writer census (`scripts/` — per read field, the count of writers under `worldPulse/`; zero = the durative is licensed) printed on the marker's card; (12) a face's slots are a SUBSET of its spine's (ARCH §2.1, arm A6, the projector) and `{settlement}` never inside a `[face]`; (13) THE CLERKS — the speaker roster per town from the holder table, each speaker's interest from the power fields, spread across a pool's faces (ARCH §2 gains a `speaker` on the face; the register card's stances become the roster; the refuter's two checks: the speaker resolves on this town; no field's opposite).
Verify: the block gate on the DEF-2 dock; a Fable skeptic over the new prompts (does the old test leak back anywhere?); the exemplar scan's output committed; a v3 dry run on ONE pool with two writers and the selector.
Ruling 15 (the owner, 2026-09-13): ONE SENTENCE PER POWER, one drawn or two where they differ — car **8b-W-18c**: (a) the per-town SOURCE FILTER on the face draw (`drawFace` over the faces whose source resolves on this town, fallback the stranger; a shift-register row; a declared text shift inside the rewrite's freeze); (b) the PAIR draw (a pair mark; the composer draws both where the pool is contested). The first pool is WRITTEN per source now; the filter lands before the block does; until then the refuter passes only universal sources.
Ruling 19 (the owner, 2026-09-13 04:3x): THE ARCHIVER IS UNBIASED BY POWERS AND BIASED BY A TEMPERAMENT WE SET — prompts now (default exact); the config field + picker as car 8b-W-18g.
Ruling 18 (the owner, 2026-09-13 03:4x): A SOURCE'S VOICE FOLLOWS ITS RANK AND THE TOWN'S CIRCUMSTANCE — car **8b-W-18f** after 18c: rank and posture bands on the source tag, the face draw filters on them, faces per power per band only where the stake turns.
Ruling 17 (the owner, 2026-09-13 03:0x): DM SECRETS ARE THE ARCHIVER'S NOTEBOOK — third person kept for a successor, two sentences, transparent, toward the fact; the ellipsis and its kin licensed on dm-only faces; the shades varied across a block (prompt-only; no car).
Ruling 16 (the owner, 2026-09-13 02:0x): A POWER EXISTS ONLY WHERE AN INSTITUTION CAN REPRESENT IT — car **8b-W-18e**: the faction/power roster gated on the live institution roster per archetype (enumerated), a declared simulation shift; the speaker roster and the power roster become one fact.
Exit: ONE POOL through v3 (`args-DEF2-v3.onepool.json`: walls with NO force; two writers under two lenses; a FABLE selector; the Opus refuter) — the gate refuses nothing on taste, the twelve faces carry at least three sources and no `{settlement}`, the selector reports how many candidates were worth keeping — AND THE OWNER READS THE POOL. Evidence before instruments: wave 2 is built only if this pool earns it.

> Wave 1 decisions (delegated; each vetoable): the marker's job cut to three sections, the mechanical six printed by `scripts/prose-mark-card.mjs` — veto restores the nine-section prompt · two writers not three for the first pool, the withheld-reason lens folded into the social-position lens — veto restores three · the SELECTOR is FABLE for the first block (the seat that decides the level; quota cost accepted) — veto sets `selectModel:'opus'` · the selector reports `worthKeeping` so the owner sees whether a one-boolean key can carry twelve — veto drops the field · the round-1 draft retired from the annex to the kit (the block restarts from the shipped rows, which is what v3 does anyway; the packets stay at a3e877e6e and in `rewrite/DS-DEF-2/` for the reversal path) — veto restores the block from a3e877e6e.

### Wave 1 design notes (2026-09-12 ~22:2x, three recon seats; each CONFIRMED in the dock at a3e877e6e)
- **Ruling 1 (the gate):** `inBandOf` (`scripts/prose-wave-gate.mjs:809-884`) builds `failing[]` from five sources; the three BAND sources and the licence-era `composed walk` detectors leave `failing` and become report fields; the fragment word ceiling (a count) stays. `ENTRY_NUMBERS` stays exported, read by no verdict. On DEF-2 band depth was the ONLY ground holding pools open (owned FAIL 0 on all 26). Tests re-pinned: `tests/lint/proseWaveGate.walker.test.js` :301-304, :617-647, :633-639.
- **Ruling 2:** NOTHING in the product enforces claim equality (arm A6 is exported and never called; `check-pair.mjs`'s `pairArms` has no caller); the contract is prose in ARCH §2.3/§2.6/§8.4, the kernel JSDoc and `assertFaces`'s refusal text — struck as text.
- **Ruling 12:** slot equality is enforced in exactly one place, `assertFaces` (`scripts/lib/dossier-annex-grammar.mjs:680-687`) → SUBSET, plus a `{settlement}`-in-a-face refusal; a face with FEWER slots is harmless (nothing reads the unused fill), a face with MORE silences the whole unit (`fillSlots` null → `drawPiece` null). Contract test plant :1686 re-pinned.
- **Ruling 13 (the clerks):** the leaf stores faces as bare strings in `wordings[]` with NO per-face fields; eligibility is the VARIANT's by construction, so a source that must "exist on this town" cannot be a per-face fact without a register-car change to the face draw (`drawFace` kernel :462-468) and a shift-register row. DECISION (vetoable): for DEF-2 the sources are verified against the pool's PREIMAGE (universal sources only; conditional ones where the key fixes the seat) — process only, zero product bytes; the per-town source filter is chartered as a register car after the fills. The source seating (holder table recon): the stranger everywhere; the elders below town; the hall / the tavern / the guilds / the register at town+ (required rows); the muster, the watch, the gate only where the key fixes them; the crown's assessor NOWHERE (no typed producer). The archiver's hand (13a) and the restraint veto list (14) are in v3's VOICE and selector.
- **Ruling 11 (frozen fields):** the census must be FIELD-grain — `scores.disaster` has a pulse writer while its four siblings do not; `economicState` is rewritten as an object while `economicState.safetyProfile` never is; the LIVE roster (`settlement.institutions`) has ~99 write sites, so the beasts/invasion keys are LIVE and the durative there is licensed only by the sharpened rule (a face dies with its key).
- **Ruling 4 (the card):** the corpus half of the same-page read set is one filter over the census (`rows[].sites` × `DOSSIER_MOUNTS`); the NON-corpus half is not in the census at all (`DOSSIER_MOUNTS` lists no machine producer) and needs an espree member-expression walk of `threatAssessment.js`, `defenseDisplay.js`, `safetyProfile.js`; the sibling string pack is an espree walk of the same files (predicate chain + text + CLOCK); required rows are one loop over the catalogue (thorp 5 · hamlet 7 · village 7 · town 13 · city 14 · metropolis 1).
- **Ruling 6 (the page):** renders WITHOUT a browser — `rateGrid()` → `generateSettlementPipeline` → `defenseThreatProse` + `deriveDefenseReadiness` + `safetyDesc` + `deriveGuardAssessment`; a world-run town via `composeReaderRegion` + `advanceReaderCampaign` (~3 s/year for 4 towns). ⛔ The PDF path does NOT carry the faces (`src/pdf/` imports no state-prose composer) — the skeletons' "PDF PARITY: parity" line was never true of faces. The recon's sample render already shows the collision class: "Upkeep underfunded: garrison pay at 90%" printing under the `militia only` key (a WIRING defect, `defenseDisplay.js:280`).
- **Ruling 10 (the fills):** the material per row is fixed only by `institutionVocabulary.js`'s printed descriptions (Palisade = stakes :278; Palisade or earthworks :155; Town walls = stone :157; City walls = masonry :162); `Citadel` and `Massive walls and fortifications` name NO material and are UNFILLABLE (a variant naming the slot drops there by anchored liveness); custom walls are invisible to the buckets. A fill alone is ZERO text shift; the first face using it is a rewrite shift. There is NO generated proper name for a fortification anywhere — "the fortification's own name" is the catalogue label, so `{defname}` is not built.

### Deferred (documented, NOT bugs to re-find)
- ~~The per-town SOURCE filter on the face draw — chartered after the fills.~~ PULLED INTO WAVE 1 as car 8b-W-18c by ruling 15 (the owner's design depends on it).
- `{defname}` (a proper fortification name) — no generated name exists; only custom rows carry one; not built.
- The `composed walk` owned detectors (arm Q, F25, A3, Aspect, Ambiguity) — retired from `failing` with the bands (licence-era; refused nothing on DEF-2); kept as report.
- The funding note printing "garrison pay" under a no-garrison key (`defenseDisplay.js:280`) — a WIRING row (W-26) for the register car; the card surfaces it so no refuter charges a face for it.
- The 16-vs-10 discrepancy between the gate agent's verdict and the gate's measurement packet for DEF-2 round 1 — recorded in the sitting; both agree band depth was the only ground; not chased further.

## Wave 2 — THE PAGE REFUTER AND THE REGION ARM (risk: medium; CONDITIONAL on the wave-1 pool earning it)
Ruling 6. A rendered page per pool on two or three towns (one world-run) as the refuter's primary evidence, built on the existing 768-town RATE render; a region-batch recurrence arm counting each face's most distinctive content noun per batch. Both report; neither refuses until measured on DEF-2.
Exit: both scripts run on the DEF-2 dock and their outputs are in the refuter's packet.

## Wave 3 — DEF-2 RESUMED UNDER v3 (risk: high — it is the block that lands)
The round-1 drafts at a3e877e6e re-refuted against the sitting's reversed rows (the reversal path RETRO-VALIDATION §4 promised); DEF-2 continues from those drafts under v3; the two unconditional failures and the six DULL pools cured. Then DEF-11 redone, DEF-1, and the sequence of OPUS_CHAIR_MANUAL §5.
Exit: DEF-2 refuted clean on the page render, banked, its consist cut.

## Owner-gated (not started without sign-off)
- The GM edit path for composed faces (zero of eleven defense blocks editable, `dmFieldProjection.js:85-94`) — a paid-surface capability; the owner already ruled it a CAR, before/after/never is the owner's.
- The criminal-capture badge (`PowerTab.jsx:344-350`, player-visible, no covert path): either a WIRING leak or the 22 `dm-only` marks conceal nothing — a paid-surface visibility decision; recommendation in the sitting.
- `deriveCriminalStructure` reading `r.institutions` raw — changes shipped output; stays a register-car row.

## Sequencing rationale
The sitting lands first so every later change cites a signed ruling and a VALIDATION cell. The machinery follows before any block resumes, because the block that ran under the old machinery burned its quota holding pools open on a colon. The page refuter comes before DEF-2 resumes because floor 1 is a property of the page. Blocks last.

## Deferred (documented, NOT bugs to re-find)
- (filled per wave)

## Owner-decision queue (parked, deliberate)
- ~~CRISIS FACTIONS under ruling 16~~ DECIDED (delegated 2026-09-13 ~02:4x, vetoable): events, not standing powers — exempt from the institution rule, expiring with their crisis; a standing power only if a row comes to back it. Veto gates them on backing classes.
- Ruling 7 (bound the retrospective obligation to strikes of ratified law) — signed by "do all of that"; recorded as ADDENDUM 18 §7; the owner may widen it back.
