# THE RE-CUT PROGRAMME — the machinery follows the law (plan)

> **Progress** (append after every wave — this blockquote alone must reconstruct program state)
> - ⭐⭐⭐ **09-14 ~10:1x — THE SCRIBE IS IN THE PRODUCT, DARK (W0–W2 landed in `lane-scribe`, tip `1a2022cf3`, `FLAGS.scribe` false, migrations 201/202 written not applied; commit 6 the retirements deferred to W5 by ruling 20), AND THE CLARITY RE-CUT IS LANDED, READER-AUDITED AND CHAIR-CUT ON FOUR OF FIVE LEAVES** (DS-DEF-2 · power · warFaith · general; economy/stressors/population in flight). The four audits found 32/189 · 26/288 · 7/353 moved claims with every instrument green and wrote the law: the reader audit is part of the gate; certainty/quantifier/scope are claims; `will` is a modal; never delete a causal link; 42b reaches metaphor. The owner's frame (~08:5x): the corpus is the hopeful final version (a free tier that is truly free); the Scribe is the intermediate for cash flow; when the corpus is done the Scribe moves to a higher paid tier. Pre-existing defects surfaced: `history.age` frozen at generation (ruling 26's roll never re-rolls); the account-import node budget refuses 4 of 7 real towns; both `verify:dist` ceilings over at the base; a cell-run inline pool the projector drops silently. Owner acts: db push 201+202 · the SKU · the key · item 13 · the archive/pins ruling · the checkout · the flag.
> - ⭐⭐ **THE OWNER OPENED A SECOND PROGRAMME IN PARALLEL (2026-09-14 ~05:0x): "let's build both in parallel. Comprehensively architect out this newest ai model integration."** — AI-written dossier prose rendered ONCE per settlement at its first durable save, refuted by the corpus programme's own instruments, persisted on the settlement blob as an inert artefact, the hand corpus as the floor and the exemplar. ARCHITECTED, NOT BUILT: `docs/DESIGN_SCRIBE_GENERATION_TIME_PROSE.md` (three recon seats at HEAD; the town card collapses the 107 KB corpus card to a few KB of exact facts; the refuter's 11 pure-text arms + the town-grounded arms run with no model; five lifecycle cures each one line; W0–W5 with zero product bytes before W2; twelve owner-gated decisions). ⛔ RECON FOUND THE MAIN CHECKOUT IS A STALE SNAPSHOT — 4,236 deletions against HEAD, the whole Surveyor/BYOK layer and migrations 137–195 exist only at HEAD; every cite in the design is `git show HEAD:`; reconciling the checkout is the owner's act and W2's prerequisite. THE CLARITY PROGRAMME CONTINUES: the power leaf's line-edit lane is up (`brief-clarity-POW.md`, six batches in `lane-clarity-POW`); then warFaith · general · economy · stressors; the v3 queue (DEF-1, 5, 3, 4, 6, 8, 9) drafts under a VOICE that now carries 42–42d and 42d's failure mode.
> - ✅ **THE CLARITY RE-CUT IS COMPLETE ON ALL 24 WRITTEN POOLS OF DS-DEF-2 (2026-09-14 04:0x, the chair now Fable 5.1 after Opus): BATCH 4 of 4 landed `7992713d0`** (sealed `refs/preserve/rewrite-8b-def2-clarity-batch4-2026-09-14`) after batches 1–3 (`8e608dd1f` · `a9b460eac` · the batch-1 re-open `8d9a51d9c` · `b5738c2ac`). The lane that cut batches 1–3 DIED with batch 4 in the dock uncommitted; the chair sealed its 93 faces rather than restoring HEAD, and SIXTEEN audit seats + SIXTEEN repair seats (3.2M tokens) found and put back ~20 MOVED CLAIMS in four classes — three of them ruling 42d's cure (a habitual became one past episode · the scope narrowed town→room · the actor changed) and one an added sentence of fact. THE RULE, now in the brief: where 42d and the freeze collide, the freeze wins. TWO NEW INSTRUMENTS find the class in seconds: the move-grammar sweep over every changed line (14 of 103 shifts on the dead cut incl. a PROVENANCE move that raised an A13 row; 5 of 89 landed, all lawful) and the wave gate's findings diffed against HEAD (new C3 3→0, new A13 1→0). The recorder-blind pin held at 2756 by measurement twice (the dead cut −12; a checker's fronted lead +216; both bisected to one line and re-cut to HEAD's claim). LANDED: 335/335 · projection + census green · wave gate rc=0, owned 0/0 on all eight · words 4,063→4,001 · 157 faces and every source unmoved · 78 variants / 572 faces · owner-proxy 101→157 of 181 · verb concentration under the benchmark on five of seven pools. THE 42b LIST is EIGHT lines across the four batches, awaiting refutation. CHARTERED: the UNTOUCHED FACES over the whole block (every auditor found lines left alone that still fail; batches 1–3's 'left alone' were never audited) · THE SHAPE (never executed in batch 4) · THE TWO NEVER-WRITTEN POOLS (`detention without process` · `granary, NO medical provision`). Next, one lane at a time: the 42b refutation · the four inherited C4 reds · the whole-block car · the two pools under v3 · the instrument debt.
> - ⛔ **BATCH 5 SETTLES THE SEAT QUESTION THE CHAIR TRIED TO SETTLE EARLY.** DULL at first refute: 1/5 with Fable selecting, 4/6 all-Opus, **5/6 all-Opus**; still DULL after cure 0 → 1 → 2. The chair's post-batch-4 override ("five of six cured, so it costs a cure round not the prose") is RETRACTED as premature. The failure modes are COUNTS and therefore instrumentable: one verb on 19 of 25 rows (up after the cure), ZERO of 22 faces carrying two sentences, "One of the" opening three faces in every variant (the chair's own ruling-25 construction become a tic), and sibling overlap RISING after a cure. Four measures chartered into `measure-block.py` and given to the selector as targets; ruling 25 amended to vary the role's SYNTAX. Fable returns to the selector seat when its quota does.
> - ⭐⭐ BATCH 4 landed ALL-OPUS after the Fable quota was exhausted mid-run (draft `d794c3a78` → cure `73d32a126`, 6/6 pools). THE SELECTOR MEASUREMENT (9 Fable-selected pools vs 6 Opus-selected): Fable wins sensory presence 2.89 vs 1.70 per 100w, same-opener 0.143 vs 0.230, and DULL-at-first-refute 1/8 vs 4/6; Opus wins sentence-length spread 5.01 vs 6.36 and spending the close variety 3/9 vs 5/6; attributions even. THE CHAIR OVERRODE ITS OWN PAUSE RULE and continued, ground recorded: five of six cured to PASS, so the weaker taste seat costs a cure round rather than the landed prose, and the two deficits are the fingerprint's own measures, so they go into the selector's prompt as a TARGET (sensory 2.9 · same-opener 0.14 · attributions 0.63 · sd 5.0). Fable returns to the selector the moment its quota does.
> - ⭐ THE CHAIR SEAT MOVED FABLE → OPUS (the owner, next day ~11:3x: "Continue the rest with opus"); the seats are untouched (ruling 24). BATCH 3 landed: draft `d57c601cf` → cure `db54f803b` (4/5 pools); first-refute FAIL 9.2% (13 of 142 renderings; batch 2 17%, DS-DEF-11 52% — the rate is falling); one pool DULL, cured to PASS; the FRONTIER packet refused WHOLE for one citation literal and one cure INTRODUCED a floor-1 fault (a garrison presupposing a wall at town) → THE CHAIR'S CUT 4, measured with `provenanceCount` and applied as its own commit. The lens swap settled the seats: Fable 72 faces / 11 spines vs Opus 59 / 4 — the flip condition fails with the confound removed, so the seats stay mixed and the LENSES ALTERNATE each batch. BATCH 4 (6 pools) next.
> - Wave 3 has begun: BATCH 2 (force with NO walls · no legal infrastructure · NO reserves, NO medical provision) through draft `065b9ded6` → cure `fb81210b2`, first-refute FAIL 17% (proceed), re-refute FAIL 0, craft PASS ×3, zero refusal rows, 1.6M tokens per pool; SEATS Fable ≥2:1 (lens-confounded) → batch 3 swaps the lenses before the flip; the close variety unspent on every pool (a selector instruction, not a measure); BATCH 3 (walls AND garrison · full legal chain · Economic STRONG · granary AND hospital · frontier credible deterrence) launched 2026-09-13 16:3x.
> - Wave 1 — cars 18n `0389fe89c` (the witness and the public) · 18o `0b71a8ef7` (opener and close classes, the cap) · 18o-r `f75050761` (the page-state fix; the E2 arms as shrink-only ratchets; the byte baseline row) · cure 3 `f6bdcf591` (the first pool under the whole law; faceCounts 4,5,5; PASS 11/14) — dock `f6bdcf591`, 2026-09-13 14:4x; bytes 31.7% of the signed ceilings. Deferred by the implementer: symptom marks → poolMeta; the measure-block measures. BATCH 2 (pools: force with NO walls · no legal infrastructure · NO reserves, NO medical provision) launched under the new seats.
> - Wave 1 — the chair's cut 2 `e075b0769` · hygiene `95760e212` (the declared face-count grow 2266→2275; six zero-shift arms re-pinned) · car 18l `311589326`+`60efe812b` (attribution by role: 63 rows, 268 roles, 14 office titles; the first pool re-cut to slots; roles render on three towns) · car 18m `48b58031a` (the compromised role; 0.7 measured 0.6989 over 10,000 rolls; the audience-gate arm) — dock `48b58031a`, 2026-09-13 12:3x. OPEN from them: the custom `roles` manifest field (owner-gated); no engine field records a form of rule, a cult's hand on the register, or an unexposed officer; the year is `history.age`.
> - Wave 1 — cars 18j `d02764539` (the face pin derived from the vocabulary) · 18i `a8fd854ff` (the archiver's weighing row + the compound pair's joint; 33,984 composed reads byte-identical) · 18k `7761350e1` (the card prints what a face may not deny of a required row) landed in the dock 2026-09-13 10:2x, zero text shift; ⚠ the cure gate at `569e01793` left the face-count register and the zero-shift arms red — hygiene (B) in flight with the chair's cut 2, then 18l, 18m.
> - Wave 1 — THE FIRST v3 POOL THROUGH (2026-09-13 09:0x): walls with NO force, draft `180acd14d` → cure `569e01793` in the DEF-2 dock; refute 10 PASS · 1 FAIL (floor 1 by inference, the gate's arms) · 1 WITHHELD · CRAFT DULL for assembly; cure 2/2; the re-refute run by hand after a join bug (fixed). Two workflow join bugs found and fixed (absolute vs short packet dirs at SELECT and at the cure gate). The page rendered on three towns. Page-level findings on LANDED blocks banked for wave 2: DS-DEF-11 'the muster … is thinning' on a no-force thorp (floor 1 on the page); DS-DEF-11 'stone keeps itself' (a struck phrase, ADDENDUM 13A).
> - Wave 1 (in flight) — dock cars `6a6c2ddf3` 18a (rulings 1/2/12) · `6e5ee9716` 18b (the draft retired; `{defmaterial}`) · `8a236ea21` 18d (the marker's instruments) · `4cf42141b` 18c (the source filter + pair draw, zero text shift) · `d8a77df97`+`6275e8027` 18e (the power gate, strict; 532/768 towns lose a faction; ⚠ the migration's frozen block grazed in the dock — the owner's before any product landing). THE FIRST POOL RUNNING: `wf_208f835e-a94`, walls with NO force, v3, Fable selector.
> - Wave 0 (shipped, `b9c3fe322` §919.6, sealed `refs/preserve/fable-sitting-2026-09-12`) — the sitting document, 1,137 VALIDATION cells, CONTRADICTION-TABLE §V (35 finding rows, the floors re-worded, 6 new wiring rows, 2 new §1.1 rows), CHAIR-NOTE E-4(b) struck by annotation, ADDENDUM 18 (13 rulings) into the kit brief and `verbatim/`, `items-ADDENDUM-18.md` (13 rows), the kit's law files under `docs/rewrite-kit/`.
> - Program opened 2026-09-12 ~21:00 (session 4e3d2f70, Fable chair) from the FABLE SITTING of the same evening (`docs/rewrite-retro-2026-09-12/FABLE-SITTING-2026-09-12.md`) at the owner's word: *"along this new direction, i am amenable to other changes to previously stated laws and rules that help get to the intent behind why we did everything … without the machinery becoming the very obstacle for its completion. do you have any other suggestions?"* → seven suggestions → **"do all of that."** → four taste additions → **"do it all."** → the owner's two observations (no `{settlement}` inside a face; THE CLERKS) = thirteen rulings, brief ADDENDUM 18.

## Sources
- `docs/rewrite-retro-2026-09-12/FABLE-SITTING-2026-09-12.md` — the Fable seat's retrospective validation of the 2026-09-12 re-cut: 1,137 archive rows validated by twelve seats (every judgment row, every SPLIT, every STRUCK, the floors' twelve code claims, 684 faces refuted across three corpus versions). Counts and reversals are in the sitting.
- `docs/rewrite-retro-2026-09-12/` — the retrospective record (§919.3–§919.5), which this programme amends but never rewrites.
- The sealed kit `refs/preserve/chair-tools-2026-09-05` (a75dbc21f, autosaved 2026-09-12 20:07:09) — the brief with ADDENDA 1–17, the block workflow, the packets.

## Method
Gates (this repo, run in the block dock, one focused file at a time, never the whole suite while another runner is up): fast = `npx vitest run tests/copy/voiceMechanics.test.js tests/data/dossierStateProseProjection.contract.test.js`; quick = the same plus `node scripts/prose-wave-gate.mjs --section defense --out .packets/chair` (mechanical arms only); full = the block gate's whole-suite proof (`run-gate-9xx.sh` shape from the kit) before a landing. Every wave = fix → focused tests → gate → adversarial verify where the substrate is sensitive (prompts a seat reads; the gate's refusals; anything that changes what a game master reads) → one commit naming the wave → plan-note commit. The LEDGER branch (`review-fixes-2026-07-08`, the main tree's HEAD) takes docs; the PRODUCT (`claude/composite-r4`) moves only by CAS landing. Push and the WALK stay the owner's.

## Owner decisions honored throughout (do not violate)
- ⚠ **THE SEATS UNDER A MODEL LIMIT (the Opus chair, next day ~14:1x, vetoable):** when the FABLE quota is exhausted, ruling 24's flip is overtaken (the selector cannot run either) and the block runs ALL SEATS OPUS with the selector warned in its own prompt that it is reading its own hand. This is a forced experiment, not a preference: batch 4's fingerprints and CRAFT verdicts are compared against batches 1–3 (Fable-selected), and if assembly measures worse the chair PAUSES the block until Fable returns. The taste seat is the one seat the programme most wanted on the other model.
- **2026-09-13 ~14:0x — THE GRANT:** "okay continue with the work all the way. i give all permissions you need now or would need in the future and leave all decisions to you, fable." Read as total delegation over the programme's judgment; the chair keeps the push, any deploy, any migration and any data deletion off the table by its own rule and records every decision vetoably; the owner-gated list below stands.
- **2026-09-13 08:3x — THE SEATS (ruling 24):** Opus marks · Opus + Fable draft (one per lens) · Fable selects · Opus refutes and cures. Refutation on Opus stands from 09-11. Flip: Fable wins the shared lens two to one across pools 2–3 → both writers Fable; the weekly quota within a day of its limit → both writers Opus, Fable keeps the selector.
- THE PROMISE (a seed is a starting world forever) — floor 2 is constitutional; it is re-worded, never relaxed.
- The four floors survive; the deity doctrine as amended by ADDENDUM 15 (the followers act, the god does not).
- ONE LANE AT A TIME (owner 2026-09-11).
- REFUTE WITH OPUS (owner 2026-09-11 20:5x) — the seat allocation is untouched by this programme.
- "Keep it all in some historical format for fable retro validation" (owner 2026-09-12) — every strike or re-pointing of ratified law in this programme gets an item row in `docs/rewrite-retro-2026-09-12/` (bounded per wave 0's ruling 7: strikes of RATIFIED law, not every chair ruling).
- The push and the walk are the owner's.

## Wave 0 — THE SITTING LANDS (ledger only; no product bytes)
What lands: `FABLE-SITTING-2026-09-12.md` (the seven rulings signed under "do all of that", the seat-by-seat results, the reversal roster); every validated row's `VALIDATION` cell filled (OK · CHALLENGE · REVERSE) in the six `items-*.md`; the reversals returned as rows of `evidence/CONTRADICTION-TABLE.md` (or corrections to existing rows: F1-29, F1-93, F4-09, F4-12, F3-05's count, Appendix A/B, the W25c line, §D D-10/D-12/D-18); CHAIR-NOTE §2.2 E-4(b) struck by name; the floors' riders (floor 1 "closed to the keys", the office roster dropped from the five, the same-page read set; floor 4's three removal paths and fabric scope); brief ADDENDUM 18 (the owner's word and the seven changes) into the kit and the ledger; the kit's law files (`rewrite-block-v2.workflow.js`, `tables-14.txt`, `rulings-DEF2-v14.txt`, the brief's ADDENDA 14–18) committed under `docs/rewrite-kit/` so the law is never again only in `/private/tmp`.
Exit: the ledger commit exists; `git grep` finds every reversed id in the table; the memory START HERE row points here.

## Wave 1 — THE MACHINERY RE-CUT (dock laneRW-DEF2; product bytes in `scripts/` and `docs/`; risk: medium)
Rulings 1–5. (1) the wave gate's per-face band depth retired — `ENTRY_NUMBERS.depth` and the `failing` band arms no longer hold a pool open; the gate's refusals are MECHANICAL ONLY (counts, tag grammar, projector, the E2 voice ratchet); taste moves to the pool-grain DULL verdict plus a BLOCK-grain measurement (distinct words · content types · `,and` rate · per-pool content ratio, the `measure-prose.py` numbers) reported, not refused. (2) the face contract in `arch-prose/ARCH-COMPOSED-PROSE-v2.md` §2.1/§8.4 amended: faces share slots and marks BY CONSTRUCTION, never a claim set; a face may not CONTRADICT its siblings and need not restate them. (3) `rewrite-block-v3.workflow.js`: Mark → Draft → Refute (contradiction + DULL) → Cure, one round each; the Refine and the separate Judge retired (the refuter's verdict applies unless the chair overrides); licence-era residue purged from VOICE / writer / refiner prompts. (4) the marker's product is WHAT WOULD BE FALSE: the tier's `required: true` rows at the head of the card, the pool key's preimage, the SAME-PAGE READ SET (union of `row.reads` across every producer `dossierMounts.js` mounts on the tab) and the sibling machine sentences; the licence card retires as the seat's primary instrument. (5) `EXEMPLAR-PACK.md` scanned by the floor-2 grammar test and its failing example lines rewritten.
Rulings 8–13 also land here: (8) the SELECTOR — two or three writers per pool under different exemplar lenses, one reader picks the twelve, then the refuter; (9) the writer's brief is ONE PAGE (the pack, the shipped rows, the marker's card); (10) the FILLS — `{defmaterial}` (O-1), the fortification's name, the roster row's name through a typed slot (product bytes; a fill is a register-car change and lands as its own car with the projection contract); (11) the FROZEN-FIELD writer census (`scripts/` — per read field, the count of writers under `worldPulse/`; zero = the durative is licensed) printed on the marker's card; (12) a face's slots are a SUBSET of its spine's (ARCH §2.1, arm A6, the projector) and `{settlement}` never inside a `[face]`; (13) THE CLERKS — the speaker roster per town from the holder table, each speaker's interest from the power fields, spread across a pool's faces (ARCH §2 gains a `speaker` on the face; the register card's stances become the roster; the refuter's two checks: the speaker resolves on this town; no field's opposite).
Verify: the block gate on the DEF-2 dock; a Fable skeptic over the new prompts (does the old test leak back anywhere?); the exemplar scan's output committed; a v3 dry run on ONE pool with two writers and the selector.
Ruling 15 (the owner, 2026-09-13): ONE SENTENCE PER POWER, one drawn or two where they differ — car **8b-W-18c**: (a) the per-town SOURCE FILTER on the face draw (`drawFace` over the faces whose source resolves on this town, fallback the stranger; a shift-register row; a declared text shift inside the rewrite's freeze); (b) the PAIR draw (a pair mark; the composer draws both where the pool is contested). The first pool is WRITTEN per source now; the filter lands before the block does; until then the refuter passes only universal sources.
Ruling 20 (the owner, 2026-09-13 05:0x): PLAIN LANGUAGE — the caliber in the design, the words the plainest that carry it; the selector vetoes any word a reader would reread (prompt-only).
Ruling 19 (the owner, 2026-09-13 04:3x): THE ARCHIVER IS UNBIASED BY POWERS AND BIASED BY A TEMPERAMENT WE SET — prompts now (default exact); the config field + picker as car 8b-W-18g.
Ruling 18 (the owner, 2026-09-13 03:4x): A SOURCE'S VOICE FOLLOWS ITS RANK AND THE TOWN'S CIRCUMSTANCE — car **8b-W-18f** after 18c: rank and posture bands on the source tag, the face draw filters on them, faces per power per band only where the stake turns.
Ruling 17 (the owner, 2026-09-13 03:0x): DM SECRETS ARE THE ARCHIVER'S NOTEBOOK — third person kept for a successor, two sentences, transparent, toward the fact; the ellipsis and its kin licensed on dm-only faces; the shades varied across a block (prompt-only; no car).
Ruling 16 (the owner, 2026-09-13 02:0x): A POWER EXISTS ONLY WHERE AN INSTITUTION CAN REPRESENT IT — car **8b-W-18e**: the faction/power roster gated on the live institution roster per archetype (enumerated), a declared simulation shift; the speaker roster and the power roster become one fact.
Exit: ONE POOL through v3 (`args-DEF2-v3.onepool.json`: walls with NO force; two writers under two lenses; a FABLE selector; the Opus refuter) — the gate refuses nothing on taste, the twelve faces carry at least three sources and no `{settlement}`, the selector reports how many candidates were worth keeping — AND THE OWNER READS THE POOL. Evidence before instruments: wave 2 is built only if this pool earns it.

> Wave 1 decisions (delegated; each vetoable): the marker's job cut to three sections, the mechanical six printed by `scripts/prose-mark-card.mjs` — veto restores the nine-section prompt · two writers not three for the first pool, the withheld-reason lens folded into the social-position lens — veto restores three · the SELECTOR is FABLE for the first block (the seat that decides the level; quota cost accepted) — veto sets `selectModel:'opus'` · the selector reports `worthKeeping` so the owner sees whether a one-boolean key can carry twelve — veto drops the field · the round-1 draft retired from the annex to the kit (the block restarts from the shipped rows, which is what v3 does anyway; the packets stay at a3e877e6e and in `rewrite/DS-DEF-2/` for the reversal path) — veto restores the block from a3e877e6e.

### Wave 1 design notes (2026-09-12 ~22:2x, three recon seats; each CONFIRMED in the dock at a3e877e6e)
- **Ruling 1 (the gate):** `inBandOf` (`scripts/prose-wave-gate.mjs:809-884`) builds `failing[]` from five sources; the three BAND sources and the licence-era `composed walk` detectors leave `failing` and become report fields; the fragment word ceiling (a count) stays. `ENTRY_NUMBERS` stays exported, read by no verdict. On DEF-2 band depth was the ONLY ground holding pools open (owned FAIL 0 on all 26). Tests re-pinned: `tests/lint/proseWaveGate.walker.test.js` :301-304, :617-647, :633-639.
- **Ruling 2:** NOTHING in the product enforces claim equality (arm A6 is exported and never called; `check-pair.mjs`'s `pairArms` has no caller); the contract is prose in ARCH §2.3/§2.6/§8.4, the kernel JSDoc and `assertFaces`'s refusal text — struck as text.
- **Ruling 12:** slot equality is enforced in exactly one place, `assertFaces` (`scripts/lib/dossier-annex-grammar.mjs:680-687`) → SUBSET, plus a `{settlement}`-in-a-face refusal; a face with FEWER slots is harmless (nothing reads the unused fill), a face with MORE silences the whole unit (`fillSlots` null → `drawPiece` null). Contract test plant :1686 re-pinned.
- **Ruling 13 (the clerks):** the leaf stores faces as bare strings in `wordings[]` with NO per-face fields; eligibility is the VARIANT's by construction, so a source that must "exist on this town" cannot be a per-face fact without a register-car change to the face draw (`drawFace` kernel :462-468) and a shift-register row. DECISION (vetoable): for DEF-2 the sources are verified against the pool's PREIMAGE (universal sources only; conditional ones where the key fixes the seat) — process only, zero product bytes; the per-town source filter is chartered as a register car after the fills. The source seating (holder table recon): the stranger everywhere; the elders below town; the hall / the tavern / the guilds / the register at town+ (required rows); the muster, the watch, the gate only where the key fixes them; the crown's assessor NOWHERE (no typed producer). The archiver's hand (13a) and the restraint veto list (14) are in v3's VOICE and selector.
- **Ruling 11 (frozen fields):** the census must be FIELD-grain — `scores.disaster` has a pulse writer while its four siblings do not; `economicState` is rewritten as an object while `economicState.safetyProfile` never is; the LIVE roster (`settlement.institutions`) has ~99 write sites, so the beasts/invasion keys are LIVE and the durative there is licensed only by the sharpened rule (a face dies with its key).
- **Ruling 4 (the card):** the corpus half of the same-page read set is one filter over the census (`rows[].sites` × `DOSSIER_MOUNTS`); the NON-corpus half is not in the census at all (`DOSSIER_MOUNTS` lists no machine producer) and needs an espree member-expression walk of `threatAssessment.js`, `defenseDisplay.js`, `safetyProfile.js`; the sibling string pack is an espree walk of the same files (predicate chain + text + CLOCK); required rows are one loop over the catalogue (thorp 5 · hamlet 7 · village 7 · town 13 · city 14 · metropolis 1).
- **Ruling 6 (the page):** renders WITHOUT a browser — `rateGrid()` → `generateSettlementPipeline` → `defenseThreatProse` + `deriveDefenseReadiness` + `safetyDesc` + `deriveGuardAssessment`; a world-run town via `composeReaderRegion` + `advanceReaderCampaign` (~3 s/year for 4 towns). ⛔ The PDF path does NOT carry the faces (`src/pdf/` imports no state-prose composer) — the skeletons' "PDF PARITY: parity" line was never true of faces. The recon's sample render already shows the collision class: "Upkeep underfunded: garrison pay at 90%" printing under the `militia only` key (a WIRING defect, `defenseDisplay.js:280`).
- **Ruling 10 (the fills):** the material per row is fixed only by `institutionVocabulary.js`'s printed descriptions (Palisade = stakes :278; Palisade or earthworks :155; Town walls = stone :157; City walls = masonry :162); `Citadel` and `Massive walls and fortifications` name NO material and are UNFILLABLE (a variant naming the slot drops there by anchored liveness); custom walls are invisible to the buckets. A fill alone is ZERO text shift; the first face using it is a rewrite shift. There is NO generated proper name for a fortification anywhere — "the fortification's own name" is the catalogue label, so `{defname}` is not built.

#- Car **8b-W-18i** (ruling 22, the owner's, 2026-09-13 06:4x): THE ARCHIVER'S WEIGHING — `archiver` joins the kernel's source vocabulary as a pair-attached token that never draws alone (`[archiver · pair N · weigh]`); the draw renders it iff its disagree/reinforce pair is drawn; the grammar allows one per pair and refuses it on aside/view; the ratchet bars the tells. PLUS ruling 23 THE COMPOUND PAIR: a seeded joint table on `pairKind` (disagree: though · but · while · and yet; reinforce: and · as; or the full stop), applied only where both faces are one sentence, lowercasing the second face's opening common word and never a slot; a `joinable` mark in the grammar; a pin per kind. After the first pool; before DEF-2 resumes under v3.

- Car **8b-W-18j** (the chair, 2026-09-13 07:2x, MEASURED by the first selector): `FACE_PIN = 4` in `scripts/generate-dossier-state-prose.mjs` (the spine + three faces) is a CEILING that binds under ruling 15 — the card seats TEN sources on the walls-with-no-force preimage and nine slots exist, so the register and the market got no face though both had a candidate worth the page. The constant is a self-imposed design constraint nobody re-asked (the owner's 08-27 law). Raise the pin to the source vocabulary's size plus the spine (one face per seated source can always fit); the face-count register re-freezes as a grow (plain `--write`, the shift declared); then a cure slot opens for the excluded sources. Before DEF-2 resumes under v3; may ride with 18i.

- **THE DRAFTING-SEAT MEASUREMENT** (the owner, 2026-09-13 07:3x: 'what if fable drafts and opus refutes? … do it, the measurement'): on the first v3 pool a Fable writer runs under the Opus writer 2's exact prompt and lens, blind; a second Fable selector picks over all three packets into `draft-B.md` (never the dock) and reports slots won per packet, the head-to-head on the shared lens, and tells per packet. Decision rule (chair, vetoable): Fable takes the drafting seat if it wins most shared-lens slots across three pools; even → Opus drafts and the quota goes to selection. Refutation stays Opus (owner 09-11). The instrument persists: mixed seats + the selector's per-writer attribution is a running comparison on every pool. **BATCHES 2–3 (09-13 → next day): the tally is LENS-CONFOUNDED — under lens 2 Fable won ≥2:1; with the lenses swapped Fable 72 faces / 11 spines vs Opus 59 / 4. The flip condition is not met with the confound removed: the seats stay mixed, the lenses alternate between the seats each batch, and lens 1 (the telling particular) is re-cut in the pack to ask for the particular inside an account.** **POOL 1 RESULT (2026-09-13 08:1x):** slots won p1=2 · p2=1 · p3(Fable)=9; head-to-head on the shared lens Opus 4 · Fable 6 (two near ties); tells per face 0.27 · 0.49 · 0.47; the selector's verdict 'the same grade … would not change which model drafts'. Not decisive; pools 2 and 3 run mixed and the tally decides. **THE OWNER'S RULING (08:3x, ruling 24): the best combination from now on — Opus marks · Opus (lens 1) + Fable (lens 2) draft · Fable selects · Opus refutes and cures; v3 `WRITER_MODELS`; the selector's SEATS tally runs on every pool; flip conditions recorded in the ruling.**

- Car **8b-W-18l** (ruling 25, the owner's, 2026-09-13 09:2x): ATTRIBUTION BY ROLE — an `institutionRoles` table by catalogue row (class defaults, custom `roles`), `{<source>}` and number-aware `{v:<verb>}` slots in the fill machinery beside `{defmaterial}`, sentence-head capitalisation, the page-level no-repeat exclusion, realm roles gated on the engine's recorded form of rule, THE TIER'S NAMED OFFICES on the roll as titles where the roster prints them (never the minted name), pins per class; then a mechanical re-cut of the first pool's attributions to slots. After 18i/18j/18k; before pool 2 if the implementer is free, else pool 2 writes the class word and is re-cut with the rest.

- Car **8b-W-18m** (ruling 26, the owner's, 2026-09-13 09:4x): THE COMPROMISED ROLE — the kernel's source set gains a `compromised` subset derived from the engine's covert fields (the birth capture → hall; a covert criminal bloc → watch/court; an unexposed officer → its seat; a cult's hand → register; map from the card's covert section per block); face tag `[<source> · compromised]`; the draw FORCES the compromised source into the unit and prefers its compromised candidate; the grammar refuses the tag on a source no covert field can compromise; the refuter's inverted test for the tag; THE SPEAKS-OR-SILENT ROLL (`COMPROMISED_SPEAKS = 0.7`, the chair's number, vetoable; seeded on world seed + pool key + settlement + current year: speaks seven in ten, silent three in ten; forced only into pools whose symptom the card marks for that covert field, re-rolled at generation and each advance, stable on a re-read); the concealment shapes varied and one true reassurance per pool admitted; the card names the symptom per covert field; the audience gate proven (the page states nothing); pins per covert field and for the roll's determinism. After 18l.

- Car **8b-W-18n** (ruling 27, the owner's, 2026-09-13 10:4x): THE ARCHIVER AS WITNESS — the `archiver` token gains the mark `observed` under which it draws ALONE, eligible everywhere; the grammar allows at most one observed face per variant; the refuter judges it as the survey's own claim (floor 1 in full, no inference; floor 2 with the elapsed-course licence and NO magnitude — the chair kept the number out of the owner's example, vetoable); AND ruling 28 THE PUBLIC: `public` joins FACE_SOURCES as a second universal token, seated by tier in the roles table with plural roles only, judged as a fact, never compromisable; pins for both. After 18m.

- Car **8b-W-18o** (ruling 29, the owner's, 2026-09-13 11:1x): SURVEY FRAMES + OPENER CLASSES — a closed `SURVEY_FRAMES` table drawn like a role with the page-level no-repeat, a `{survey}` frame slot; a mechanical opener-class tag per face at generation (closed, total classifier; pinned) and a page-level preference in the draw for a class the previous unit did not use; the selector's assembly veto extended (no two adjacent faces in one class; a pool's spines in three); AND ruling 30 THE UNIT CAP — the grammar refuses a pair whose half exceeds one sentence (`faceSentenceCount`), the composer asserts the drawn unit at three sentences or fewer, a per-block `simple` mark at two; the first pool's hall/guild pair re-cut to single-sentence halves; pins. After 18n.

- **THE RESEARCH RECONCILIATION (2026-09-13 13:1x):** `docs/rewrite-retro-2026-09-12/RESEARCH-RECONCILIATION-2026-09-13.md` — five slices, 150 findings, rulings 31–39 (the chair's, vetoable), the pack re-cut 3 chartered, the architecture's page-dependence risk fenced in car 18n/18o's brief (B′). Owner items: the senses and the bill as new candidate classes; S2; the PDF gap; time by incident; the cold seat.

- **THE INSTRUMENT DEBT (the Opus chair, next day ~13:0x; `$SC/kit/briefs/brief-instrument-debt.md`, runs after batch 4):** FIVE suites are red at the dock HEAD and have been since the early cars, all of them corpus-census pins the REWRITE legitimately moves in suites nobody re-pointed — `goldenFreeze.walker` · `sovereigntyLightingContract.walker` · `proseWiringCensus.walker` · `proseMeasures.walker` · `proseEntryContradiction.walker` (one of whose three arms, 'annex:5233 did not load', may be a REAL parser defect). The v3 gate's per-commit test list never named them, which is how four batches passed over them. The PRECEDENT for the fix is `b6f1a4695`: a calibration whose substrate we rewrite is re-pointed at the FROZEN corpus with the live figure REPORTED beside it, so nothing is re-recorded; a live invariant is re-recorded with the cause declared and given a direction; a defect is fixed. Then the gate list grows so no future batch can hide one.
- **THE VOICE RATCHET'S BLIND SPOT + the chair's prose-rule ruling (vetoable):** the ratchet scans `src/data` + `src/domain`; the reader-facing copy is authored in `src/generators` (91 quoted em-dash strings, unscanned). The eight em dashes that red the ratchet today are KEYS transcribing the producer's label bands, proved by execution — cutting them breaks seven assertions and silences prose without clearing the ratchet. THE EM-DASH BAR IS A PROSE RULE, NOT A COPY RULE: it governs a face, a spine, a note; a `Word — gloss` label band is glance-grain under the legibility law and keeps its dash. The car classifies all 91 before the ruling stands, extends the scan to the generators, and allowlists the bands BY NAME (never by path).

- **THE EFFICIENCY CAR (`$SC/kit/briefs/brief-efficiency.md`, measured 2026-09-14):** a pool costs ~1.2M tokens across seven seats. The card is 122 KB and its §(4) alone is 52,630 B; sections (2)(2b)(3)(4)(5)(6)(7) are BLOCK-level and have been regenerated per pool and re-read by six seats. The car: a BLOCK CARD + a thin POOL DELTA; the writer and selector stop being handed the refuter's 61 KB (ruling 9 made true at last); one marker per batch; the targeted cure as default; the full suite on the cure gate only. Worth 20–25% — the chair's first estimate of 35–40% was wrong and is corrected in the brief.
- **NOT CHARTERED, worth more than the car: MULTI-POOL DRAFTING.** One writer drafting several pools of a block in one pass collapses twelve writer seats a batch into two. The risk is echoes ACROSS pools from one hand, which no instrument measures at that grain; it waits for wave 2's region arm.
- ⭐⭐ **THE RATE MEASUREMENT (`$SC/kit/rewrite/pool-rates.md`, every known value reproduced exactly, 0 mismatches over 271 rows) AND THE DECISION IT FORCED.** Bands over all 361 resolved pools: under 5% = 168 pools carrying 1.22% of reader exposure · 5–15% = 64 / 12.47% · 15%+ = 129 / 86.32%. **132 pools fire on ZERO towns**, of which **76 are NOT MEASURED** (the RATE corpus generates world-less settlements, so their blocks are unreachable — the roster `MOUNTS_THE_CORPUS_DOES_NOT_REACH` names them) and **56 are real zeros** (~29 rung dark, ~27 silent). THE CHAIR'S DECISION: NOT the lighter-path triage first proposed — **do not write pools that never fire, and establish WHY each is dark before striking it** (`brief-dark-pools.md`). Worth ~67M tokens at 1.2M a pool, and one of the three silent DS-DEF-2 pools (`plagued, NO perimeter and NO force`) looks more like a defect than a dead key, since its sibling fires freely. DS-DEF-2's batch 6 is built from the three pools that FIRE.
- ⚠ **THE CENSUS'S `rateBp` COLUMN IS STALE:** 62 of 271 rows have moved at the dock tip; the fold is a frozen 2026-09-08 artefact carried through every re-take since. Refresh it with a fold at a current tip before any decision rests on it.
- **EXPOSURE AND TRIAGE:** 47% of pools carry 80% of reader exposure; 35 pools fire on under 5% of towns and are 1.2% of all exposure. 134 of 361 resolved pools have NO measured rate — being measured now (`brief-rate-measurement.md`). Triage of the long tail (one writer, no cure round unless it fails, THE REFUTER NEVER CUT) waits on that figure and is the owner's to veto.

- ⭐⭐ **THE DARK-POOL CAR (`$SC/kit/rewrite/dark-pools.md`, 56 pools settled by probe) OVERTURNED THE CHAIR'S HYPOTHESIS.** Not 56 dead pools worth 67M tokens: **18 DEFECT · 24 OUT OF GRID · 12 DEAD BY CONSTRUCTION · 2 unsettled**. Strikable: 12 (9 conservatively), 14.4M tokens. STILL TO WRITE: 42 pools, ~50.4M. The efficiency question found defects, not efficiency, and the defect car (`brief-dark-pool-defects.md`) is ordered by what each costs a READER — first the two DS-POW-2 share pools whose prose already exists and which no reader can ever reach.
- ⚠ **THE CHAIR PLANNED ON A BLIND INSTRUMENT:** `prose-rate-corpus.mjs` omits `crisisBannerRung`, so 14 crisis pools were never measured and were read as zeros. The 273-row fold must be re-taken after the cure; the census's `rateBp` column is separately stale by 62 rows.
- ⚠ **THE GRID IS BLIND ON A WHOLE AXIS:** all five priority sliders pinned at 50 makes the compound-stress threshold exactly 70 against a `n ≤ 65` test, so all thirteen compound-stress flags are false on all 768 towns. Whether that is the grid's sliders or an off-by-one in shipped flag logic is the defect car's first question.
- ⚠ **DS-DEF-2 ASSERTS A FULL LEGAL CHAIN ABOUT A RUINED COURT.** Its civic rows read a generation-time snapshot, so a town whose court and prison are rubble still reads as having both — a floor-1 contradiction reachable with no authoring error, on the block just rewritten.
- **STRUCK FROM DS-DEF-2, dead by construction, shipped rows left untouched:** `Internal Security: detention without process` and `Disasters & Famine: granary, NO medical provision`. NOT struck: `Beasts & Monsters: plagued, NO perimeter and NO force` — it is the AFTERMATH pool (the birth guarantee is never renewed after ruin) and the 42 towns reading `perimeter but NO force` are one wall-strike away from it.

- ⭐⭐⭐ **RULING 41 (the owner's): A FACE IS UNFALSIFIABLE OR COUPLED.** An unfalsifiable face asserts nothing the engine types, needs no preimage check, and is authored ONCE into a shared bank drawn by many pools — it reduces how many faces must EXIST across 708 pools. A coupled face keeps the full card and refuter. THE TARGET IS UNFALSIFIABLE, NOT SIMPLE (aiming at simplicity produced the reverted `f20532e18` corpus at a third of the shipped vocabulary). Two checks survive on a banked face: the page (one list per tab) and the speaker (tier-bound). Car 8b-W-18q chartered; the RATIO is being measured over all 506 landed faces (`brief-unfalsifiable-count.md`), including the test the ruling turns on — whether the pools the refuter judged good carry MORE unfalsifiable faces or fewer.
- ⚠ **THE CHAIR SET THRESHOLDS THAT WOULD HAVE REFUSED THE GOOD PROSE** (a verb-concentration ceiling met by none of the nine Fable-selected pools); an implementer measured it and they are re-cut to what the corpus supports. The machinery-as-obstacle failure in miniature, caught by a seat.
- ⚠ **AN INSTRUMENT THAT PAID FOR DELETING A SPEAKER:** verb concentration is a share of ATTRIBUTED rows, so dropping an attribution improved the score. Fixed by adding measure 5 (unattributed faces). The deeper version — SPEAKERS AT THE THINNEST TIER the key admits — belongs to CAR 8b-W-3 and is the measure this programme most lacks.

## Deferred (documented, NOT bugs to re-find)
- WIRING rows from the first pool's re-refute (banked for the wiring car, not prose findings): W-a `defenseDisplay.js:278-284` + `:317-321` print 'Upkeep underfunded: garrison pay at N%' on the walls-with-NO-force key (garrison fixed false; the gate exists because the wall satisfies `hasAnyDefense`) — twin of W-19/W-22; W-b `journalPages.js:290-293` prints an 'Armed forces' heading over standing = garrison + militia + WATCH on the same page as 'no organized military force to man them' — both correct in the engine's vocabulary, the seam behind the floor-1-by-inference FAIL; the refuter's recorded reading: 'force' on this key = garrison-or-militia, never the watch.
- CARD GAP (measured on pool 1 by the refuter and selector B alike): both writer seats over-read the absence — arms denied of persons on gate duty where a `required: true` Town watch seats Gate duty, and the burial ground placed outside the wall where the card says 'most towns, not all'. The marker's card §8 should print, per required row, the BODIES AND PLACEMENTS A FACE MAY NOT DENY (the row's services with p ≥ 0.8 and the derivation that files the row under standing force). A card-instrument change for the next pool's Mark.
- The wave gate's packet-dir spelling (`ds-def-2-<pool>`, one hyphen) differs from the kit's (`ds-def-2--<pool>`, two), so the gate's round counters read nothing and its verdict is WITHHELD on every v3 pool (CONFIRMED on the first pool, 2026-09-13 07:5x). Information only under ruling 1 (the gate does not judge prose); fix the gate's `$PACKETS` dir derivation to the kit's spelling in a later wiring car.
- Car **8b-W-18h** (the chair's call, vetoable): the catalogue has no manor/estate/town-house row, so 121/128 cities lose their noble houses under ruling 16 — a catalogue gap, not intended thinness; add `Manor house` (village/town) and `Noble town-houses` (city+) with base chances so the noble power is backed where a city has nobles; after the first pool.
- The governing seat is never gated (R14); a council name minted with no Government row is an honest gap — a Government row per tier or no power; the chair leaves it ungated for now.
- 31 crisis-faction names persist past their stressor with no retirement path (only the occupation pair retires) — reported by car 18e; the retirement is a later car.
- The symmetric mint-in-play under the v1 dial: no path exists; the density cadence is v2-only.
- The `unbacked`-power chronicle beat (the temple burned; the clergy left the powers) — the prose lane's, later.
- The custom-content picker for `backsPowers` — a later car.
- Speakers-vs-powers divergences to reconcile in 18f: the register/sexton speaks (burial ground) but backs no power; the tavern speaks but backs none; the hall speaks at town+ while the governing power is never gated; the force class excludes walls where the desk's buckets include them.
- ~~The per-town SOURCE filter on the face draw — chartered after the fills.~~ PULLED INTO WAVE 1 as car 8b-W-18c by ruling 15 (the owner's design depends on it).
- `{defname}` (a proper fortification name) — no generated name exists; only custom rows carry one; not built.
- The `composed walk` owned detectors (arm Q, F25, A3, Aspect, Ambiguity) — retired from `failing` with the bands (licence-era; refused nothing on DEF-2); kept as report.
- The funding note printing "garrison pay" under a no-garrison key (`defenseDisplay.js:280`) — a WIRING row (W-26) for the register car; the card surfaces it so no refuter charges a face for it.
- The 16-vs-10 discrepancy between the gate agent's verdict and the gate's measurement packet for DEF-2 round 1 — recorded in the sitting; both agree band depth was the only ground; not chased further.

## Wave 2 — THE PAGE REFUTER AND THE REGION ARM (risk: medium; CONDITIONAL on the wave-1 pool earning it)
Ruling 6. A rendered page per pool on two or three towns (one world-run) as the refuter's primary evidence, built on the existing 768-town RATE render; a region-batch recurrence arm counting each face's most distinctive content noun per batch. Both report; neither refuses until measured on DEF-2.
Exit: both scripts run on the DEF-2 dock and their outputs are in the refuter's packet.

## Wave 3 — DEF-2 RESUMED UNDER v3 (risk: high — it is the block that lands)
The round-1 drafts at a3e877e6e re-refuted against the sitting's reversed rows (the reversal path RETRO-VALIDATION §4 promised); DEF-2 continues from those drafts under v3; the two unconditional failures and the six DULL pools cured. Then DEF-11 redone, DEF-1, and the sequence of OPUS_CHAIR_MANUAL §5.
Exit: DEF-2 refuted clean on the page render, banked, its consist cut.

## Owner-gated (not started without sign-off)
- **SHOULD AN IMPORTED TOWN BE SEEDLESS AT ALL?** The estate's law comments say yes (three of them, plus a property control); the CODE says no, because `deskSeed` falls through to `r?.id`, which survives import. Executed. Making the laws true moves drawn prose on EVERY imported dossier across all six desks — a large declared shift. Making the code true means rewriting `stateProseKernel.js:780`, `composeStateProse.js:953` and `dossierProseManifest.test.js:30-33`. The owner's call; neither was done.
- **LAW 4 DOES NOT COVER `drawRole` OR `compromisedSpeaks`** — 3 of 9 seed-keyed kernel draws have no seedless branch, and both gaps are the chair's own rulings 25 and 26. A seedless read there is arbitrary-but-fixed rather than canonical.
- **THE ISLAND-FENCE BREACH (proseWiringCensus, left red on purpose):** `src/domain/display/stateProse/faceSources.js:63` imports `officesOf` from the island module `src/domain/institutions/institutionTable.js`, landed by the programme's own car 8b-W-18l. The fence exists so that such a reach is a DECLARED ACT, and `institutionTable.js` names the product-surface question D10, the owner's to sign. Two discharges: `faceSources` obtains `officesOf` another way, or a roster line names the car and its reason as every other island entry does. NOT re-recorded — a re-record would grant the declaration the fence asks the owner for.
- **THE SIXTEEN PROSE EM DASHES** in `src/generators/crossSettlementConflicts.js` (11) and `src/generators/narrative/settlementOriginProse.js` (5): genuine reader-facing prose sentences on surfaces this lane has no card, writer seat or refuter for, and one of them documents in source why its dash is load-bearing. The chair's ruling (the em-dash bar is a PROSE rule; a label band keeps its dash) is CONFIRMED by three live parses, but paying this debt is a prose act on two unlaned surfaces, not a punctuation sweep. Proposed: enrol the sixteen as a SHRINK-ONLY debt when the ratchet is extended to `src/generators`, so no new one can appear and these can only fall.
- ⚠ Car 18e edited `supabase/migrations/185_custom_content_versions.sql` (the frozen SQL projection block the manifest generator rewrites) and `supabase/functions/_shared/customContentManifest.generated.ts` for the optional `backsPowers` field — a persisted-shape and deploy surface. It sits in the DOCK only; it lands on the product branch and deploys only on the owner's word.
- A retirement road for an `unbacked` power (data deletion) — not built; the v2 thin step is the seam.
- The GM edit path for composed faces (zero of eleven defense blocks editable, `dmFieldProjection.js:85-94`) — a paid-surface capability; the owner already ruled it a CAR, before/after/never is the owner's.
- The criminal-capture badge (`PowerTab.jsx:344-350`, player-visible, no covert path): either a WIRING leak or the 22 `dm-only` marks conceal nothing — a paid-surface visibility decision; recommendation in the sitting.
- `deriveCriminalStructure` reading `r.institutions` raw — changes shipped output; stays a register-car row.

## Sequencing rationale
The sitting lands first so every later change cites a signed ruling and a VALIDATION cell. The machinery follows before any block resumes, because the block that ran under the old machinery burned its quota holding pools open on a colon. The page refuter comes before DEF-2 resumes because floor 1 is a property of the page. Blocks last.

## Deferred (documented, NOT bugs to re-find)
- (filled per wave)

## Owner-decision queue (parked, deliberate)
- ~~CRISIS FACTIONS under ruling 16~~ DECIDED (delegated 2026-09-13 ~02:4x, vetoable): events, not standing powers — exempt from the institution rule, expiring with their crisis; a standing power only if a row comes to back it. Veto gates them on backing classes.
- Ruling 7 (bound the retrospective obligation to strikes of ratified law) — signed by "do all of that"; recorded as ADDENDUM 18 §7; the owner may widen it back.
