export const meta = {
  name: 'research-badge-design',
  description: 'Comprehensive research: WoW + RuneScape emblem/badge language, heraldic plaque design at header scale, SVG execution',
  phases: [{ title: 'Research', detail: '4 parallel researchers → synthesis inputs' }],
}
const S = { type:'object', required:['topic','findings','designPrinciples','pitfalls'], properties:{topic:{type:'string'},findings:{type:'array',items:{type:'string'}},designPrinciples:{type:'array',items:{type:'string'}},pitfalls:{type:'array',items:{type:'string'}}}}
phase('Research')
const results = await parallel([
  () => agent(`RESEARCH TASK (web research; no repo edits). Research World of Warcraft's UI emblem/badge design language comprehensively: the WoW logo lockup treatments across expansions; guild crests/tabards; faction emblems (Horde/Alliance); how Blizzard mounts names on plaques/frames (beveled metal borders, embossed depth, filigree corners, inner glows, stone vs metal fields); what makes a WoW badge read 'premium fantasy' at small sizes; the specific material vocabulary (aged gold, dark iron, dragon filigree, gem insets). Cite what you actually found (pages/images described). Return {topic:'WoW badge language', findings, designPrinciples, pitfalls} — pitfalls = what makes imitations look cheap.`, {label:'r:wow', phase:'Research', model:'opus', schema:S}),
  () => agent(`RESEARCH TASK (web research; no repo edits). Research RuneScape's (especially Old School RuneScape's) UI badge/emblem/plaque design language comprehensively: the OSRS logo treatment; the game's stone/parchment/chiseled-metal UI vocabulary; clan/achievement badges; how OSRS mounts titles on plaques (chunky low-detail metals, carved stone, rivets, material grit over gloss); why its look reads 'honest medieval' vs WoW's high fantasy; what survives at small scale. Return {topic:'RuneScape badge language', findings, designPrinciples, pitfalls}.`, {label:'r:osrs', phase:'Research', model:'opus', schema:S}),
  () => agent(`RESEARCH TASK (web research; no repo edits). Research real-world heraldic and craft badge design as applied to small digital marks: escutcheon (shield) proportions; maker's plates and touchmarks on tools/weapons; medieval guild signage (carved wood plaques, gold-leaf lettering, iron corner brackets, strap-and-rivet mounting); wax seals and banner scrolls; PLUS the digital craft: how modern game/fantasy brands execute embossed/engraved lettering legibly at 32-56px header heights (bevel/emboss via layered shadows, AA contrast on metal fields, when engraving fails). Return {topic:'heraldic craft at header scale', findings, designPrinciples, pitfalls}.`, {label:'r:heraldry', phase:'Research', model:'opus', schema:S}),
  () => agent(`RESEARCH TASK (web + technique; no repo edits). Research SVG/CSS execution techniques for photoreal badge/plaque rendering consistent with a painterly site: deterministic metal textures (brushed bronze/iron via feTurbulence+lighting filters, FIXED seeds), embossed text (layered text-shadows vs SVG filters — crispness at 14-20px), beveled borders, rivet/strap details, drop shadows that read as 'mounted proud of a surface', and performance/paint costs of SVG filters in a sticky header. Note which techniques stay byte-deterministic across renders. Return {topic:'SVG badge execution', findings, designPrinciples, pitfalls}.`, {label:'r:svg', phase:'Research', model:'opus', schema:S}),
])
return { research: results.filter(Boolean) }