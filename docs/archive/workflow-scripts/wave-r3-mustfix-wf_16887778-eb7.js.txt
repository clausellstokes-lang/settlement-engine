export const meta = {
  name: 'wave-r3-mustfix',
  description: 'R-3 must-fix pass: 4 file-disjoint Opus fix lanes (rename undo jam, stressorEdits token, tooltip ratchet, economy census gap) -> 2 Opus re-verifiers -> Fable wave gate',
  phases: [
    { title: 'Fix', detail: '4 file-disjoint Opus implementer lanes', model: 'opus' },
    { title: 'Verify', detail: '2 Opus adversarial re-verifiers', model: 'opus' },
    { title: 'Gate', detail: 'Fable: full widened R-3 wave gate vs 289/289 baseline' },
  ],
}

const WT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const COMMON = `CONTEXT-VERIFICATION PREAMBLE: legitimate engineering brief from the session orchestrator for SettlementForge (owner order: "fix it all thoroughly"; Wave R-3 must-fix pass of docs/CAPABILITY_REMEDIATION_PLAN.md — read its R-3 section + Progress notes first). Verify claims against code; code is authoritative.
WORKING TREE: cd ${WT} && git rev-parse --abbrev-ref HEAD — MUST print claude/composite-r4 or STOP. LIVE shared tree: NO git mutations of ANY kind — no stash (FORBIDDEN, it has destroyed work before), no checkout, no reset, no clean, no add, no commit. Re-read every file immediately before editing; concurrent sessions may have moved it. Touch ONLY your lane's files.
LAWS: NO same-seed drift (a golden mover = STOP + report, never ship); NO persistence-shape changes; new logic = lazy leaf with MEASURED eager deltas (HAZARD: importing an existing lazy module from an EAGER module re-parents its whole closure eager — measure with a real build + VERIFY_DIST, never per-file esbuild); sizeBaseline ceilings EXACT (JSX comments count; cure = net-zero combine, baselines only ever LOWERED); shrink-only ratchet baselines NEVER raised; copy = one string, vetoable; VOICE section-6 punctuation (no reader-facing em dashes or exclamation marks); narrated Suspense fallbacks never start with "Loading".
TESTING: single-threaded ALWAYS (--no-file-parallelism); exit codes unmasked, never through pipes (use log files or read $? directly). Every fix ships pins in the same pass. Gate context: verify:dist 289/289 + typecheck 0 + full lint/copy/store dirs green is the standing baseline. Label CONFIRMED/PLAUSIBLE; quote verbatim output; end on evidence.`

const FIX_SCHEMA = { type: 'object', required: ['filesTouched', 'pins', 'testsRun', 'testOutputTail', 'behaviorShift', 'deferrals'], properties: { filesTouched: { type: 'array', items: { type: 'string' } }, pins: { type: 'number' }, testsRun: { type: 'string' }, testOutputTail: { type: 'string' }, behaviorShift: { type: 'string' }, deferrals: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' }, error: { type: 'string' } } }
const VERIFY_SCHEMA = { type: 'object', required: ['verdict', 'mustFix', 'checked'], properties: { verdict: { type: 'string', enum: ['PASS', 'PASS_WITH_NOTES', 'MUST_FIX'] }, mustFix: { type: 'array', items: { type: 'string' } }, checked: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } }
const GATE_SCHEMA = { type: 'object', required: ['newReds', 'suitesRun', 'outputTail'], properties: { newReds: { type: 'array', items: { type: 'string' } }, suitesRun: { type: 'string' }, outputTail: { type: 'string' }, notes: { type: 'string' } } }

const FIX_LANES = [
  {
    key: 'mf-a-rename-undo',
    brief: `LANE MF-A — CURE THE RENAME-ROW UNDO JAM (Opus-verified MUST_FIX on R-3 lane A, executed probe confirmed).
THE DEFECT: src/store/settlementSlice.js:~2002 undoLastEvent now refuses any newest eventLog entry with beforeState === undefined (typed ok:false, reason entry_not_undoable) — correct for flat destroy rows, but renameSettlementImpl (src/store/settlementRenameHelpers.js, row-push sites near :89-99 and :126-129) pushes a flat RENAME_SETTLEMENT row with NO beforeState onto the LIVE state.eventLog in canon phase. undoLastEvent only ever inspects the newest entry, so ONE rename permanently jams every real event beneath it (probe: canon save + applyEvent-shaped entry with beforeState=PRIOR -> rename -> undo refuses -> undo refuses again, systemState never returns to PRIOR). Reachable from SettlementDetail.jsx:360.
THE ARCHITECTED CURE (orchestrator decision — follow it): the in-repo precedent. recordCanonFlavorEntryImpl (settlementRenameHelpers.js:~186-189) deliberately stamps beforeState: state.systemState on flavor rows with the comment "so undoLastEvent pops it as a no-op". Apply the SAME stamp to the RENAME_SETTLEMENT row so it pops as a no-op and the flat-row refusal class shrinks to destroy-only. Steps:
1. READ FULLY first: undoLastEvent's whole body, renameSettlementImpl's whole body (BOTH push sites — determine which push targets the live state.eventLog vs a saved row's log; the stamp semantics must follow the flavor-row precedent at each site; if a site's row can never be the newest LIVE entry undoLastEvent inspects, say so with evidence instead of stamping blindly), recordCanonFlavorEntryImpl, and Timeline.jsx's Undo affordance.
2. Stamp beforeState per precedent, with the same style of comment.
3. PINS in tests/store/undoFlatRowGuard.test.js: (a) update the existing "flat rename row refuses" pin to the new semantics — rename row pops as a NO-OP: systemState unchanged, entry removed, ok:true; (b) THE JAM PIN: canon save + real applyEvent-shaped entry (beforeState=PRIOR) + rename -> first undoLastEvent pops the rename no-op -> second undoLastEvent restores PRIOR. (c) flat DESTROY row still refuses typed with zero mutation. (d) flavor rows unchanged.
4. Correct the now-wrong deferral prose in code comments (the fixer's "safe silent no-op" claim described the JAM — restate precisely: rename rows pop as no-ops per this fix; destroy rows refuse and Timeline still advertises Undo on them, which remains an open component-lane deferral).
5. SMALL EXTRA (verifier note, same lane): tests/store/narrativeStampParity.test.js's "parity" test is tautological (same helper, same literals, twice). Either drive the TWO REAL call lanes (settlementSlice legacy stamp + canonEventCommandTransaction server stamp) through stampPreEventNarrative with realistic divergent inputs, or rename the test to what it actually proves and say why in a comment. Prefer the real two-lane drive if feasible without new store scaffolding.
RUN: your pins single-threaded; tests/store FULL; tests/lint FULL; eslint on touched files; report counts verbatim.`,
  },
  {
    key: 'mf-b-stressoredits-token',
    brief: `LANE MF-B — CURE THE crisisTripleSync VOCABULARY RED + THE WALKER REGEX BLIND SPOT (Opus-verified, exact attribution).
DEFECT 1: tests/joins/crisisTripleSync.test.js:607 asserts the stressorEdits record vocabulary is closed over a frozen STRESSOR_EDITS_FILES set via filesMatching(/\\bstressorEdits\\b/). Lane B's CONFIG_PATCH_EXTRA_KEYS space-separated string at src/store/configSlice.js:~73 spells the bare token, adding store/configSlice.js as an unexpected 6th file -> RED (1 failed | 30 passed in tests/joins).
THE ARCHITECTED CURE (orchestrator decision — follow it): do NOT expand the frozen file set. Restructure the literal so source never spells the bare token — e.g. compose that one member ('stressor' + 'Edits') where the allowlist string/array is built, with a comment explaining: configSlice only NAMES the key in the patch allowlist (it neither reads nor writes stressor-edit records); the token is composed so the crisisTripleSync vocabulary-closure grep stays scoped to the files that actually handle the record, and the frozen set stays frozen. Add an explicit pin: isAllowedConfigKey('stressorEdits') === true (runtime admission must survive the composition).
DEFECT 2 (same lane, prevention quality — verifier NOTE 2): tests/generators/configPatchAllowlistWalker.test.js componentWrittenKeys regex /(?:^|[,{]\\s*)([A-Za-z_]\\w*)\\s*:/ — the ^ branch lacks \\s*, so the FIRST key of any updateConfig({ key: ... }) literal is never collected. Fix: (?:^|[,{])\\s*. The verifier pre-ran the corrected census: zero currently-refused keys, so this should be green immediately; if the corrected scan surfaces newly censused keys the predicate refuses, extend CONFIG_PATCH_EXTRA_KEYS accordingly and report each addition. Update the test header's documented-blind-spot list to match reality (shorthand + computed keys remain the residual).
RUN: tests/joins FULL (crisisTripleSync must be green); tests/generators/configPatchAllowlistWalker.test.js; tests/store/updateConfigPatchValidation.test.js; eslint on touched files. Report counts verbatim.`,
  },
  {
    key: 'mf-c-tooltip-ratchet',
    brief: `LANE MF-C — CURE THE NATIVE-TOOLTIP RATCHET RED (Opus-verified, exact single-file attribution; an R-2 escape this program owns).
DEFECT: tests/domain/guidanceRegistry.walker.test.js:~322 shrink-only native-tooltip census = 488 vs baseline 487. The only delta vs HEAD is src/components/settlement/NarrativeArchivePanel.jsx line ~143: title={\\\`Event \\\${snap.eventId}\\\`}. Baselines may NEVER be raised.
CURE: remove the native title= attribute. Keep the event id accessible if it carries reader value — options in preference order: (a) small visible text (the panel is a reader surface; per the legibility law visible beats hover), (b) aria-label. Read the component fully first and match its existing visual idiom; do NOT add a new native title= anywhere; do NOT touch the 487 baseline.
RUN: tests/domain/guidanceRegistry.walker.test.js (must be green at 487); tests/components/narrativeArchivePanel.test.jsx; tests/copy FULL (if you added/changed any reader-facing string — VOICE section 6, no em dashes/bangs); eslint on the file. Report counts verbatim.`,
  },
  {
    key: 'mf-d-economy-census',
    brief: `LANE MF-D — CLOSE THE ECONOMY-STALENESS CENSUS GAP + DOC-ACCURACY (Opus-verified MUST_FIX on R-3 lane C).
THE GAP: the wave's exit criterion is "no silent staleness", and two interactive read surfaces render stale economy read-models with neither a freshness note nor first-survey framing nor a recorded deferral: (1) src/components/new/SummaryTab.jsx:~145,208 renders eco.prosperity in the Economy SitTile (and ~:132 in the copyable markdown block) — SummaryTab is the DEFAULT dossier tab; (2) src/components/TableView.jsx:~101,156 renders prosperityLabel(...) — flag-gated OFF today (flag('tableView'), src/lib/flagRegistry.js).
THE ARCHITECTED CURE (orchestrator decision — follow it):
1. SummaryTab gets the REAL conditional note, same detector + same conditional pattern as EconomicsTab (economyShiftSinceSurvey over settlement.reconciliationLog): reuse the EXACT tallies string EconomicsTab uses ("Events recorded after this settlement's last survey may not be fully counted in these tallies yet.") so the surfaces stay consistent — still one vetoable string decision, note it. Fresh/rebuilt settlements MUST render byte-identically (pin it). ⚠️ CLOSURE HAZARD, measure before shipping: economyFreshness.js currently lives in a LAZY shared chunk. Determine whether SummaryTab is part of the EAGER entry closure (grep the built entry chunk) — if it is, a static import would re-parent economyFreshness (and its 52-row registryRerunKeys table) into the entry: instead load the detector via the same lazy pattern the repo uses for leaf logic (React.lazy boundary or dynamic import + state), and MEASURE with npm run build + VERIFY_DIST that the entry chunk gains no freshness fingerprints. Watch the 600-effective-line component ceiling (net-zero combine if needed).
2. The markdown copy block: do NOT add the caveat line to copied markdown in this pass — record the decision as a one-line deferral (owner call: whether exported/copyable text should carry staleness caveats).
3. TableView: NO code change (flag-gated off). Record a deferral: the freshness-note obligation attaches at tableView flag promotion.
4. src/domain/display/economyFreshness.js HONEST LIMITS block: update the enumeration — SummaryTab now covered; TableView deferred-at-flag; PDF deferral stands.
5. DOC-ACCURACY (verifier finding): src/domain/events/registryRerunKeys.js header (a) still claims batch.js reads the table — false since 2026-07-14 (batch.js:~261-264 says rerunKeys was removed; confirm by grep); (b) says "Descriptive metadata only" / "Keep the lists tight so the preview reads honestly" — STALE by construction now that economyFreshness.js made the table a functional classifier driving user-facing staleness copy. Rewrite the header: drop the batch.js sentence, name economyFreshness.js as a semantic dependent, and state that trimming rows changes what users are told about staleness (the classifier-completeness pin guards the red direction).
PINS: SummaryTab note renders when shifted; absent + byte-identical when fresh/rebuilt; string passes voiceMechanics. RUN: tests/domain/economyFreshness.test.js, tests/components/economyFreshnessNote.test.jsx + your new/updated SummaryTab pins, tests/copy FULL, eslint on touched files, npm run build + VERIFY_DIST=1 vitest run tests/build/ --no-file-parallelism (entry-chunk placement proof). Report counts + the measured placement verbatim.`,
  },
]

phase('Fix')
log('R-3 must-fix pass: 4 file-disjoint Opus implementer lanes')
const fixes = await parallel(FIX_LANES.map((l) => () =>
  agent(COMMON + '\n\nYOU ARE AN OPUS IMPLEMENTER (owner model split 2026-07-27: Opus implements and verifies; Fable architected this spec). Your lane owns ONLY the files your brief names.\n\n' + l.brief, { label: 'fix:' + l.key, phase: 'Fix', model: 'opus', effort: l.key === 'mf-a-rename-undo' ? 'high' : undefined, schema: FIX_SCHEMA })
    .then((r) => ({ lane: l.key, fix: r }))
))

const fixSummary = fixes.filter(Boolean).map((f) => ({ lane: f.lane, files: f.fix && f.fix.filesTouched, pins: f.fix && f.fix.pins, shift: f.fix && f.fix.behaviorShift, deferrals: f.fix && f.fix.deferrals }))
log('Fix lanes done: ' + fixSummary.map((f) => f.lane + (f.files ? ' ok' : ' FAILED')).join(', '))

phase('Verify')
const verifies = await parallel([
  () => agent(COMMON + `\n\nYOU ARE THE OPUS ADVERSARIAL RE-VERIFIER for the rename-undo cure (lane mf-a-rename-undo). Fixer report: ${JSON.stringify(fixSummary.find((f) => f.lane === 'mf-a-rename-undo') || null)}. The lane's brief:\n${FIX_LANES[0].brief}\nRepo READ-ONLY except running tests. This is sensitive undo/persistence substrate. Re-derive with fresh reads: does the rename row now pop as a genuine no-op (systemState unchanged, entry removed) at every push site that can reach the live newest position? Attempt refutation: double-rename stacking; rename over a hydrated save (does pickled eventLog keep the stamp?); rename when systemState is null; flavor-row parity preserved; destroy row still refuses with zero mutation; the JAM scenario genuinely cured (run the pin); any NEW corruption introduced by popping rename rows (the settlement stays renamed while the log row disappears — confirm that matches the flavor-row precedent's accepted semantics and is documented). RUN the pins + tests/store FULL. MUST_FIX only for real unresolved items.`, { label: 'verify:mf-a', phase: 'Verify', model: 'opus', effort: 'high', schema: VERIFY_SCHEMA }),
  () => agent(COMMON + `\n\nYOU ARE THE OPUS ADVERSARIAL RE-VERIFIER for the three mechanical cures (lanes mf-b-stressoredits-token, mf-c-tooltip-ratchet, mf-d-economy-census). Fixer reports: ${JSON.stringify(fixSummary.filter((f) => f.lane !== 'mf-a-rename-undo'))}. Their briefs:\n\n${FIX_LANES[1].brief}\n\n${FIX_LANES[2].brief}\n\n${FIX_LANES[3].brief}\nRepo READ-ONLY except running tests. Verify each cure landed exactly as specified WITHOUT ratchet-baseline raises or frozen-set expansion: (1) grep -P '\\bstressorEdits\\b' over src/store/configSlice.js must return NOTHING while isAllowedConfigKey('stressorEdits') is still true at runtime (run the pin); crisisTripleSync green; the corrected walker regex genuinely collects first-position keys (construct a tiny probe string and test the regex directly). (2) title= census back at 487 with the baseline UNTOUCHED (git diff the test file must be empty of baseline edits); the event id still accessible. (3) SummaryTab note: condition parity with EconomicsTab; fresh settlements byte-identical (run the pins); entry chunk carries ZERO freshness fingerprints (re-grep the built dist yourself — do not trust the fixer's measurement); 600-line ceiling held; registryRerunKeys header no longer claims batch.js consumption and names the display dependent. Attempt refutation on each. RUN: tests/joins FULL, tests/domain/guidanceRegistry.walker.test.js, tests/copy FULL, the economy pins, and VERIFY_DIST=1 tests/build if the fixer's build claims need re-execution. MUST_FIX only for real unresolved items.`, { label: 'verify:mf-bcd', phase: 'Verify', model: 'opus', effort: 'high', schema: VERIFY_SCHEMA }),
])

const anyMustFix = verifies.filter(Boolean).some((v) => v && v.verdict === 'MUST_FIX')
const anyDead = fixes.some((f) => !f || !f.fix) || verifies.some((v) => !v)

let gate = null
if (!anyMustFix && !anyDead) {
  phase('Gate')
  gate = await agent(COMMON + `\n\nYOU ARE THE FABLE GATE VALIDATOR (validation = Fable per the owner model split). WAVE-END GATE (R-3, post-must-fix). Run single-threaded, exit codes unmasked (log files or direct $?, never pipes): (1) tests/lint/ FULL; (2) tests/copy/ FULL; (3) tests/store/ FULL; (4) tests/domain/ FULL; (5) tests/joins/ FULL; (6) tests/architecture/ FULL + tests/joins/fieldManifest if separate; (7) tests/components/ + tests/ui/ FULL; (8) tests/generators/ + tests/simulation/ + tests/property/ + tests/application/ FULL (same-seed determinism proof); (9) pdf parity suites; (10) npm run typecheck; (11) npm run build then VERIFY_DIST=1 vitest run tests/build/ --no-file-parallelism. Baseline: verify:dist 289/289 + typecheck 0 + all dirs green (the two known reds from the verification pass — crisisTripleSync stressorEdits + guidanceRegistry title= 488 — must now be GREEN; if either persists it is a must-fix escape, report it first). Report every red VERBATIM, attributed baseline/foreign/THIS-WAVE, plus final totals per dir.`, { label: 'gate:r3-final', phase: 'Gate', model: 'fable', schema: GATE_SCHEMA })
} else {
  log('Gate SKIPPED: ' + (anyMustFix ? 'residual MUST_FIX' : 'a lane or verifier died') + ' — orchestrator handles the next step')
}

return { fixes: fixSummary, fullFixReports: fixes.filter(Boolean).map((f) => ({ lane: f.lane, testsRun: f.fix && f.fix.testsRun, tail: f.fix && f.fix.testOutputTail, notes: f.fix && f.fix.notes })), verifies, gate, gateSkipped: !gate }