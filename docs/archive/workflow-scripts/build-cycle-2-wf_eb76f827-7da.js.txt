export const meta = {
  name: 'build-cycle-2',
  description: 'WR-7b part 2 (wire the pulse + decompose + commit) · MG-3 leak closures · Founders Hall',
  phases: [{ title: 'Build', detail: '3 lanes, implement then verify' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS (war volume §10 — read first): ${MF} on claude/composite-r4 only, hard-gate state-mutating compounds; pathspec commits of ONLY your lane's files; no stash; gates via focused npx vitest run + sh scripts/gate-tail.sh; goldens before wiring; STOP-and-report conflicts; commit trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Owner risk law: best-over-safe, always. Everything DARK.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}

phase('Build')
const lanes = await parallel([
  () => agent(`VERIFY-FIRST: owner-ordered implementation, chair rulings binding. ${P10}
LANE A — WR-7b PART 2 (complete + commit the wave). The prior implementer's work is in the tree UNCOMMITTED with a full verifier report in the workflow journal at ~/.claude/projects/-Users-cstokes-Desktop-settlement-engine/1c189f3d-9fc3-4fc9-9cb7-b767525d28d5/subagents/workflows/wf_06885ccd-bcf/journal.jsonl (read Lane A's implement + verify results FIRST — they are your map). CHAIR RULINGS: R-BLD-1 — wire advanceEnvoyDiplomacyPulse to the built interception→hold→resume chain (the five unused imports + three dead functions become live); add pulse-level reachability tests (a fixture where an interception actually fires THROUGH the pulse, a hold opens, a resume completes). R-BLD-2 — decomposition over baseline sanctioning: move this wave's growth out of envoyErrand.js/envoyDiplomacy.js into the existing lazy-leaf siblings (envoyEncounter.js, foreignGuestHold.js, negotiationPictures.js or new leaves) until every LANE file is under its ceiling; inherited over-ceiling files you did NOT touch get documented in the commit body as pre-existing with the verifier's byte-identical-at-HEAD receipts, NOT baseline entries. R-BLD-3 — kind (c) stands as built. Then: focused gates green, the sizeBaseline exactness assertion green for lane files, ONE pathspec commit for WR-7b whole (src + tests + the FABLE_VALIDATION_QUEUE rows if they are Sol's WR-7 protocol rows — audit + attribute). Return {lane:'A WR-7b-2', commits, gates, stopReports, notes}.`,
    {label:'laneA:wr7b-2', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier, Lane A part 2. ${P10}
Report: ${JSON.stringify(r)}. Re-run the gates; PROVE runtime reachability yourself (the pulse fixture fires interception/hold/resume — not just unit tests); eslint max-lines on every lane file (all under ceiling); sizeBaseline test green; commit pathspec-clean (git show --stat); the queue-row attribution honest. Fix small, STOP-report structural. Return {lane:'A verify', verdict, findings}.`,
      {label:'laneA:verify2', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: owner-ordered implementation. ${P10}
LANE B — MG-3 THE LEAK CLOSURES (docs/DESIGN_REALM_MAGIC_TOGGLE.md §4 MG-3 + the §3 leak register L1..L12 — read in full). REPRODUCE-FIRST each: write the failing reproduction test the register specifies, watch it fail, close the leak, watch it pass. Order by structural load per the doc: (a) L1 teleportEdges config gate; (b) L2+L3 war magic gates (feasibilityGate require_magic off mundane materiel; warDeployment magicSupport) — note the war files may carry Lane-A-adjacent history, but L2/L3 touch feasibilityGate.js/warDeployment.js which Lane A does NOT touch this cycle — if you find them dirty, STOP-report instead of editing; (c) L4+L5 neighbour/legacy generator worldLaw gates (mundane name pools per the content-depth floor); (d) L7 magicProfile band-none short-circuit; (e) L6 validator none-arm; (f) L8 migration-writer consolidation; (g) L9 canonBand warning; (h) L10-L12 classifier gates. Commit per closure-cluster (a few small pathspec commits fine). MG-LAW-2 boundary: deity system untouched. Return {lane:'B MG-3', commits, gates, stopReports, notes}.`,
    {label:'laneB:mg3', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier, Lane B (MG-3). ${P10}
Report: ${JSON.stringify(r)}. Re-run every reproduction test BOTH ways (revert the fix in a scratch copy — the reproduction must fail again; the estate's guard-the-guard). Check no deity module touched; commits pathspec-clean; the mundane substitution pools meet the four-variant floor. Fix small, report structural. Return {lane:'B verify', verdict, findings}.`,
      {label:'laneB:verify2', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: owner-ordered implementation. ${P10}
LANE C — THE FOUNDERS' HALL (the dictation-corpus product surface; its design doc is in ${MF}/docs — find it by name/grep 'Founders' in docs/, read it whole; the ruled shape from the ledger: all-invited 30, no-trade, petition letters, alphabetical filled-only roll, bio drawers, role rings, ceremonial removal, presence-sealed). Implement per spec: components + store wiring + tests; premium/audience gates per the estate's standing discipline; any new store action registers in operationRegistry AND runs npm run gen:compendium-data in the same commit (the recorded staleness hazard). DO NOT touch war/envoy, hook/dossier, or instant-world files. Pathspec commits per coherent slice. Return {lane:'C FoundersHall', commits, gates, stopReports, notes}.`,
    {label:'laneC:founders', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier, Lane C. ${P10}
Report: ${JSON.stringify(r)}. Re-run gates; check the spec's ruled invariants hold (no-trade enforced structurally, roll alphabetical filled-only, removal ceremonial per doc); compendium-data freshness pin green if store actions were added; commits pathspec-clean. Fix small, report structural. Return {lane:'C verify', verdict, findings}.`,
      {label:'laneC:verify2', phase:'Build', model:'opus', effort:'high', schema:V})),
])
return { lanes: lanes.filter(Boolean) }