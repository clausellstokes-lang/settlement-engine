export const meta = {
  name: 'build-cycle-39-wr8-truly-closes',
  description: 'WZ-4 — the three remaining quarters: observer-axis hits, deterrence consumed, the casus wired — WR-8 closes',
  phases: [{ title: 'Build', detail: 'one war lane, implement + verify' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits with THE STAGED-SET LAW; no stash; focused vitest + gate-tail (ps aux first) INCLUDING the tests/domain registration walkers; python byte-scan every Write; npm run build + smoke:boot; dormancy fixtures untouched (git status -- tests/fixtures empty at every commit); same-seed whole-pipeline hash byte-identical across every source change (note WZ-3's recorded limit: the hash misses coalition-evidence groups — pair it with exhaustive censuses where the hash is blind); STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY: V4B live on nav/brand/theme — fenced.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE WZ-4 — THE THREE REMAINING QUARTERS OF ITEM 4, each pre-measured by WZ-3 (read its queue row @ 32035947 and the WZ-3 memory file wz3-lint-pair-false-premise-and-razing-accumulator.md first):
(4a) THE OBSERVER-AXIS RELATIONSHIP HITS: the world judges the razing on each OBSERVER'S alignment (the volume's monumental/lesser/recognition bands) — wire the per-observer relationship hits at the mouth's emission, riding the EXISTING relationship-state writers (never a second writer); pin: one razing, two observers of different alignments, two different verdicts, executed.
(4b) THE DETERRENCE CONSUMED: readAllianceWebRisk (warAllianceRisk.js) has EXACTLY ONE runtime consumer today (warCoalitionDecision.js — WZ-3's census). The razing's deterrence pricing CONSUMES that same function pointed at the aftermath — never a rebuilt web; pin the consumer count at exactly TWO with both named (the census becomes the walker WZ-3's memory file demands: nothing may rebuild the web).
(4d) THE CASUS WIRED under CR-WR8-H as WZ-3 answered it: a pure read model filtering razing-kind news whose stamped tick + observer hop delay (distancePricedNews.js hopDelayTicks/routeAwareHopDelayTicks) has arrived — believed razings at news speed, ZERO new persisted surfaces (grep-proven); advanceWarReasons gains its razings argument from it; scoreAtrocityAnswer finally has its caller; both R2 polarities pinned (the unjust razing raises atrocity_answer for future victims; the JUST razing mints none against the avenger).
ALSO: the deferred mouth-level behavioural proof from WZ-3's surviving mutant (i) — run the seed search for a two-sieges-won-on-one-tick fixture (bounded: if no seed lands within your budget, the structural pin stands and the search's extent is recorded); and the razing accumulator's licenseState split gets its cross-check re-run.
IF ALL LANDS: the queue row declares WR-8 CLOSED — every mechanic of the war program built, wired, and verified — enumerating the four owner-gated parked arms (K1 severity, seat ransom, ransom persistence, counterpart re-mint) as parked-not-owed. Return {lane:'WZ-4', commits, gates, stopReports, notes}.`,
  {label:'wz4:three-quarters', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. THE BAR: (a) execute one razing before two observers of different alignments yourself — two verdicts, through the real relationship writers (grep: no second writer); (b) the consumer census at exactly TWO (run it; a third consumer or a rebuilt web anywhere = red); (c) the casus arc END TO END on a real fixture: a razing published to news → the observer's hop delay elapses → the believed-razing read model surfaces it → advanceWarReasons raises atrocity_answer → the JUST razing control mints nothing; grep-prove zero new persisted surfaces; (d) the two-sieges seed search's outcome honestly stated (fixture found and green, or extent recorded); (e) dormancy fixtures untouched; same-seed hashes + the blind-spot censuses; strict flat; stage-neuter every new branch; build + smoke:boot; staged-set + pathspec vs V4B. If EVERYTHING holds, say the words: WR-8 IS CLOSED. Return {lane:'WZ-4 verify', verdict, findings}.`,
  {label:'wz4:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }