export const meta = {
  name: 'build-cycle-35-wr8-close',
  description: 'WZ-2 — THE WR-8 CLOSE: three dead paths driven live, the mouth forked, the license landed net-zero, three chair rulings',
  phases: [{ title: 'Build', detail: 'one war lane, serial steps, implement + verify' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; THE STAGED-SET LAW (git diff --cached --name-only before EVERY commit; git commit -- <paths>); no stash; focused vitest + gate-tail (ps aux | grep vitest first) AND the decomposition lesson: tests/domain registration walkers are part of EVERY war-lane gate; python byte-scan every Write; npm run build + smoke:boot; dormancy goldens BYTE-IDENTICAL with flags dark; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. THE EDGE-BUNDLE LAW: your worldPulse commits make three bundles stale — run npm run build:edge-shared IN THE SAME COMMIT with inputs staged (the reproducibility pin checks the INDEX). CONCURRENCY: the polish lane may be live on a11y.css/components — fenced.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE WZ-2 — THE WR-8 CLOSE, under THREE FRESH RULINGS (each vetoable, cite in the queue row):
R-WZ-2-REVISED (the shell band): the K1 severity-carry would be a persistence-shape change = OWNER-GATED, so v1 ships SHELL-AS-STRONGEST-DAMAGE — same K1 grading, the distinction lives in the RECEIPT (the razing's receipt names a shell as a shell; the impairment prose says 'burned to a shell'); the K1 engine-severity carry is recorded as an OWNER DECISION for later, not silently dropped. Correct the false ruling text wherever recorded.
CR-WR8-B-CLARIFIED (the grievance floor, F3): the extremity composite's grievance conjunct reads THE STRONGEST SINGLE LIVE CAUSE against the adequacy band, NOT the saturated aggregate — the ruling's own text says 'a LIVE grievance', singular, and one razing IS historically enough; the two-strong-causes floor the divisor imposed is retired. Pin single-maxed-cause reachability AND the still-unreachable weak-cause negative.
CR-WR8-H (the atrocity casus wiring, F1): the believed-razings list derives from EXISTING news/receipt read models (belief at news speed through what is already published) — a pure read-model derivation is buildable; if ANY new persisted belief surface proves necessary, STOP — that is owner-gated.
THE WORK, serial commits:
(1) DRIVE THE THREE DEAD PATHS LIVE (the surviving mutants A/B/C die): pins that execute razingDecisionFor's full ACTIVE path on a lit fixture through the real gate; razingBeliefReceipts' body executed (the mercy arc + the mistaken-feasibility arc, real pictures — CR-WR8-G's justification finally proven by execution); edge flips driven with real non-empty holderEdges. Run all three of the verifier's exact mutants — each must now red.
(2) FORK THE MOUTH: the razing branch at evaluateWarLayer's conquest power-transfer site (142 lines headroom) — decision, sack emission, stamps, flips, departure receipt.
(3) LAND THE LICENSE NET-ZERO: the pulseKernel route as measured — ⚠️ the verifier proved the 'single existing line' occurs TWICE in pulseKernel.js; identify the correct occurrence by its context and say WHICH in the commit body; pulseKernel stays at 1580 net-zero or the commit does not land.
(4) observer-axis hits + WR-6 deterrence consumption (never rebuild) + the receipts' emission; the atrocity casus wired under CR-WR8-H.
(5) THE WD DEBT: the five warDeployment leaves + razingExecution enter mechanismLitCoverage honestly (real lit-coverage pins per module, not baseline rows); correct the E-H reason claim where recorded.
(6) the edge-bundle rebuild rides the final worldPulse commit per the law.
DORMANCY LAW absolute throughout. WR-8 CLOSES with this lane if all six land — say so in the final queue row; anything unbuildable STOPS with its measured reason. Return {lane:'WZ-2', commits, gates, stopReports, notes}.`,
  {label:'wz2:wr8-close', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. THE BAR IS THE THREE DEAD PATHS: re-run the exact mutants A/B/C from the prior verify (gut razingDecisionFor's active body; gut razingBeliefReceipts; empty the edge flips) — each MUST red now, quote the reds; execute the FULL razing arc yourself on a lit fixture (decision → conserved sack → shell-named receipts → flips → license mint → consume once, refuse twice → deterrence priced); the mercy arc; the single-maxed-cause extremity (reachable) + weak-cause (not); dormancy re-hashed byte-identical dark; the pulseKernel net-zero (1580 exactly, the RIGHT line changed — read both occurrences); the casus derivation reads ONLY existing read models (grep for new persistence — zero); E-H green with the six modules honestly covered (read two of the new lit pins); the bundle freshness green at the final commit (the same-commit law held); stage-neuter on every new branch; whole-domain strict; build + smoke:boot; staged-set law + pathspec on every commit. If ALL holds, say plainly: WR-8 IS CLOSED. Return {lane:'WZ-2 verify', verdict, findings}.`,
  {label:'wz2:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }