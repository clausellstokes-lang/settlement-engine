export const meta = {
  name: 'fp-cycle5-es3-es4',
  description: 'ES-3 (typed products) then ES-4 (the distant source) — the tail of the critical path to the CR-WR10-H lighting discharge. Both ride espionageEnabled.',
  phases: [
    { title: 'ES-3', detail: 'typed products: CONFIRM / ACQUIRE / REFUTE' },
    { title: 'Verify 3', detail: 'adversarial verification' },
    { title: 'ES-4', detail: 'the distant source — the lighting discharge' },
    { title: 'Verify 4', detail: 'adversarial verification + the marker really registers' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const LAWS = `VERIFY-FIRST PREAMBLE: chair brief, owner-delegated build program. Verify every claim against the tree — LIVE CODE OUTRANKS EVERY TABLE, and a doc overstatement is a STOP-and-report, never a silent correction. Work in ${TREE}. Hard-gate with the \`cd\` FIRST: \`cd ${TREE} && [ "$(git branch --show-current)" = "claude/composite-r4" ] || exit 1\` — the working directory persists between shell calls and this check has already refused a correct commit.

⭐⭐ TWO SCRIPTS EXIST SO YOU DO NOT HAND-MEASURE. USE THEM.
 - \`sh scripts/gate-mutex.sh --wait\` before EVERY vitest invocation. ⛔ NEVER hand-spell \`ps aux | grep -c "[v]itest"\`: it matches your own command line whenever the plain word appears anywhere else on it, and a lane burned twenty minutes on that deadlock.
 - \`sh scripts/ratchet-inventory.sh <walker> <base-ref> [head-ref]\` for the red-ratchet half of wave-end attribution. ⛔ DO NOT read a ratchet's inventory by hand. THE REASON IS MECHANICAL: vitest NEVER prints the MEMBERS of a failed array assertion — the row reads \`expected [ …(76) ] to deeply equal []\` — and that truncation is identical in the default reporter AND in \`--reporter=json\`. Only a custom reporter sees the members. THREE waves in one day wrote FALSE inventory receipts by reconstructing from the cardinality; one reported figures matching neither end. The script exists because the transcription is where the invention happens.
 - Read gates ONLY through \`sh scripts/gate-tail.sh <cmd>\`. ⚠ NEVER read an exit code through a pipe — you get the pipe's status. The chair did this today while re-proving the ratchet tool.

⚠ zsh does NOT word-split unquoted parameters: \`npx vitest run $FILES\` silently runs ONE file and reports a meaningless green. Route multi-file runs through \`sh\`, or spell every path literally.

PROTOCOL (each has bitten): NEVER stash/reset/clean/checkout-family — that family discards uncommitted work; use \`cp\` backups and \`cmp\`-proven restores. Commit with EXPLICIT PATHSPECS; before EVERY commit verify \`git diff --cached --name-only\` against your EXPECTED set BY NAME AND COUNT, not merely that hunks are yours — \`git add\` aborts ATOMICALLY on one bad pathspec and a suppressed stderr makes that silent. Commit messages via file; trailer \`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>\`. python3 byte-scan every authored file for NUL bytes. Strict ceiling 1313; NEW modules carry a PER-FILE ZERO strict baseline — satisfy by NARROWING, never a cast or \`any\`. ⛔ Do NOT tighten the shared domain-strict ratchet even when it reports slack: the surplus is not demonstrably yours (standing precedent, twice recorded). tests/fixtures/, pulseKernel.js and applyWorldPulse.js are UNTOUCHABLE. Navigate by SYMBOL — line addresses rot.

⛔ DO NOT EDIT, STAGE OR COMMIT \`docs/FABLE_VALIDATION_QUEUE.md\`. Ledger rows are DEFERRED TO THE CHAIR. Compose your row and RETURN IT AS TEXT.

⛔ THE REPO GATE IS RED AT BASE — 22 files / 30 tests across tests/{domain,property,lint,design,docs}, plus lint and a red full typecheck. NOT YOURS. "Pre-existing" is an EMPIRICAL claim you must EARN: re-run the exact command against a \`git archive\` of your OWN PARENT (never an older tree — a lane recorded an arithmetically impossible provenance today by archiving a start HEAD), symlink node_modules in, and \`rm -f\` THE SYMLINK before removing the tree.

⚠⚠ THE FOUR PIN-VACUITY SHAPES THAT SHIPPED TODAY. Check your own work against each before you report:
 1. A PIN THAT COMPARES A FUNCTION AGAINST ITSELF — two fixtures that differ in a field the reader never reads, or two calls with identical arguments.
 2. OPERATORS THAT PASS ON EQUALITY — \`toBeGreaterThan(0)\` where you meant "moved". Assert STRICT inequality and the exact expected value on both sides.
 3. A CLAIM IN A HEADER THAT NO PIN IMPLEMENTS — a test file asserted each of eight factors was individually live while its pin fed a hand-written literal to a different module. A COMMENT IS NOT A PIN.
 4. TWO GUARDS OVER ONE JOB — each invisible behind the other. Delete each ALONE and confirm each reds alone.

REPORTING: label every conclusion CONFIRMED (executed evidence quoted) or PLAUSIBLE. Never present the second as the first. If you catch yourself about to write a figure you did not execute, stop and execute it.`

const VERIFIER_LAWS = `

⚠⚠ VERIFIER DISCIPLINE.
 - Do EVERY execution inside a \`git archive\` of the exact commit you are verifying; symlink node_modules; \`rm -f\` THE SYMLINK before removing the tree.
 - You MUST NOT edit the live tree. Mutants go in disposable archive trees ONLY.
 - ⚠ RE-RUN EVERY GATE THE REPORT QUOTES, AT THE COMMITTED SHA. Today's defects were in the RECEIPTS, not the code: a wave wrote a green domain-strict result into the permanent record that exits 1 at the tree it committed, and another reported a ratchet inventory that matched neither end. Checking the code is not enough.
 - Use \`sh scripts/ratchet-inventory.sh\` for the ratchet half. Do not hand-read it.
 - ⚠ A MUTANT THAT PLANTS NOTHING GREENS. Assert the file's bytes CHANGED before measuring; when you plant a process or fixture, verify it EXISTS before reading the result.
 - Check every claimed differential for the four vacuity shapes above.`

const WAVES = [
  { id: 'ES-3', phase: 'ES-3', vphase: 'Verify 3', brief: `YOU ARE THE ES-3 IMPLEMENTER — THE TYPED PRODUCTS (CONFIRM / ACQUIRE / REFUTE). Rides \`espionageEnabled\` (minted at ES-0, \`55674790\`); mints NO flag. NORMATIVE SPEC: the ES-3 block of \`docs/DESIGN_FP_ARCH_ES.md\` — navigate by SYMBOL; that volume's §1 forbids line-number navigation. Read whole. Needs SP-B (landed) and ES-2 (landed at \`84f50fe4\`, repaired at \`987928a3\`).

⚠ READ SP-B's ACTUAL SHAPE from \`beliefAxes.js\` / \`beliefAxisSubjects.js\`, never a volume's description of it. A sibling volume's claim that a related blocker "is gone" was measured only CONDITIONALLY true: \`CHANNEL_BELIEF_AXES.trade\` is still \`Object.freeze([])\`. If your work reads through that spelling, MEASURE before assuming.

PINS: the WRITER-BOUNDARY SCAN — no \`applyBeliefOverrides\` import anywhere in the ES module set, EXECUTED AS A MUTANT (plant the import; the scan must red); the tap-never-purer-than-access matrix; the lose-the-spy-keep-the-intel BOTH-ARMS pin pair. Four-fence, every guard door pinned INDIVIDUALLY, lifecycle totality (create/read/persist/regenerate/undo/import, each by executed round-trip — the write that survives one path and ghosts another is this estate's most-bitten class and it bit twice today).` },

  { id: 'ES-4', phase: 'ES-4', vphase: 'Verify 4', brief: `YOU ARE THE ES-4 IMPLEMENTER — THE DISTANT SOURCE. THIRD MEMBER of the CR-WR10-H lighting discharge and the payoff of the whole chain. Rides SP-B/SP-B2's flags; mints nothing.

⚠⚠ ELEVEN COMMITS CARRY "ES-4" IN THEIR SUBJECT AND THE WAVE HAS NOT LANDED. \`8a4b0aef\` says verbatim "THE WAVE DID NOT LAND" — those eleven built and hardened the LIGHTING INSTRUMENT. Never infer from the log that any part of ES-4 exists; verify by module and symbol.

⛔⛔ THE LIGHTING RECIPE IS EXACT AND A PIN IN THE WRONG SHAPE "CARRIES THE TOKEN AND LIGHTS NOTHING". Read the header clauses of \`tests/lint/sovereigntyLightingContract.walker.test.js\` YOURSELF and satisfy them literally. As measured: the marker \`ES-4-DISTANT-SOURCE-EVIDENCE\` must stand in an \`it\`/\`test\` TITLE — NEVER a \`describe\` title — inside \`tests/domain/espionageDistantSourceEs4.test.js\`, one of exactly three entries in \`EVIDENCE_FILE_ADDRESSES\`. The \`it\` identifier must be imported from 'vitest' UNRENAMED and UNSHADOWED; the call must be statically registered STRAIGHT-LINE (no return, throw, conditional, loop or bare await); a function-body second argument with NO options bag; the callback takes NO parameters. ⚠ A renamed vitest import was once credited while vitest registered ZERO rows.

⚠ THE LIGHTING CENSUS IN THAT FILE IS AN EXACT EXECUTED SELF-ASSERTION and your new test file WILL move it. Re-measure AT YOUR OWN PARENT — never an older tree. Four lanes have re-recorded it today; one wrote a provenance that was arithmetically impossible. Isolate the cause by construction (which layer moved, and why the others cannot) and state the method you used.

CHARTER: prove the SOURCE — a non-neighbour (court, holding) pair appraised \`known:true\` through a COMPLETED confirmation mission. PINS: four-leg totality plus the degraded-arm re-run against espionage-fed rows; the non-neighbour differential; the axis-drift scan.

⛔ DO NOT LIGHT ANY FLAG. Lighting is owner-held at the terminal soak. You prove the condition is SATISFIABLE; you never satisfy it by lighting.` },
]

let prev = null
const results = []
for (const w of WAVES) {
  phase(w.phase)
  const build = await agent(LAWS + `

${w.brief}

${prev ? '⚠ YOUR PREDECESSOR REPORTED: ' + JSON.stringify(prev).slice(0, 6000) + '\nVerify its landed shape against the TREE, never against its report.' : ''}

Wave-end attribution per the LAWS, with the ratchet half read BY THE SCRIPT. COMPOSE your ledger row and RETURN IT AS TEXT.

RETURN JSON: {commits, files, gates, goldenShifts, stopReports, judgments, ledgerRowText, notes}.`, { label: `build:${w.id}`, phase: w.phase, model: 'opus' })

  phase(w.vphase)
  const verdict = await agent(LAWS + VERIFIER_LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER FOR ${w.id}. Default posture: the implementer is WRONG until executed evidence says otherwise. REFUTE. The implementer reported: ` + JSON.stringify(build).slice(0, 12000) + `

VERIFY BY EXECUTION: (1) EVERY pin NON-VACUOUS — plant the mutant that should red each, confirm it reds, and check each against the four vacuity shapes. (2) RE-RUN EVERY QUOTED GATE at the committed sha. (3) Dormancy byte-identity, run by you. (4) Guard doors deleted INDIVIDUALLY. (5) Lifecycle totality by executed round-trip. (6) No flag minted or lit. (7) Wave-end attribution both granularities, ratchet half via the script. (8) New tests use the anchored-negatives helper — and note its lookback is EXACTLY ONE LINE, so a two-line reason silently fails to exempt. (9) strict 1313 and per-file zero; byte-scans; staged-set completeness by name and count.

${w.id === 'ES-4' ? 'SPECIFIC TO ES-4: verify the marker actually REGISTERS — run the walker, confirm it reports the evidence SATISFIED, then MUTATE the pin into a describe title and confirm the walker STOPS crediting it. A marker that lights nothing is the exact failure this instrument was built over ten cuts to catch.' : ''}

A finding is only a finding if you EXECUTED the thing that proves it. RETURN structured JSON only.`, { label: `verify:${w.id}`, phase: w.vphase, model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

  results.push({ wave: w.id, build, verdict })
  if (verdict && verdict.verdict === 'REJECT') {
    log(`${w.id} REJECTED — chain STOPS for the chair.`)
    break
  }
  prev = build
}

return { results }