export const meta = {
  name: 'spatial-rounds-14-16-ground',
  description: 'Ground rounds 14-16 (factional belief + objective-parameterized risk + alignment-style + the 6 technical-hardening requirements) against the tree',
  phases: [
    { title: 'Ground', detail: 'scorer objectives, faction state, distortion-style, cost-law versioning + carrier profiles, rumor lineage/source-correlation, belief-update substrate, test-infra + feedback-loop brakes' },
    { title: 'Stress-test', detail: 'full-16-round coherence + round-16 hardening cost + build placement' },
  ],
}
const DESIGN = 'docs/PHASE55_SPATIAL_ENGINE_DESIGN.md'
const ROOT = '/Users/cstokes/Desktop/settlement-engine'
const GS = { type:'object', additionalProperties:false, properties:{
  system:{type:'string'}, persists_or_exposes:{type:'string'},
  key_files:{type:'array', items:{type:'object', additionalProperties:false, properties:{file:{type:'string'},lines:{type:'string'},what:{type:'string'}}, required:['file','what']}},
  attachment_points:{type:'string'}, gaps:{type:'string'}, feasibility:{type:'string', enum:['green','yellow','red']} },
  required:['system','persists_or_exposes','key_files','attachment_points','gaps','feasibility'] }
const CS = { type:'object', additionalProperties:false, properties:{
  axis:{type:'string'}, findings:{type:'array', items:{type:'object', additionalProperties:false, properties:{
    severity:{type:'string', enum:['high','med','low']}, claim:{type:'string'}, evidence:{type:'string'}, mitigation:{type:'string'}}, required:['severity','claim','mitigation']}},
  verdict:{type:'string'} }, required:['axis','findings','verdict'] }
const pre = `Ground a design doc vs a REAL codebase. Root: ${ROOT} (cwd). Read ${DESIGN} — §4g rounds 14-15 (settlement is a POLITY / factional belief; alignment = distortion STYLE; risk = a FRAMEWORK beliefs+objectives+disposition+constraints, faction-specific objectives) + PART V (round-16 technical hardening: cost-law versioning + route receipts; per-carrier route profiles; rumor lineage + SOURCE-CORRELATION vs false corroboration; belief-update conflict-resolution rule; invariant test suite; feedback-loop brakes) + PARTS II-IV. Report only what EXISTS with file:line. Reference (read-only, NOT ours): /Users/cstokes/Desktop/settlement-generator/settlement-engine.`

const GROUND = [
  { key:'scorer-objectives', label:'settlementStrategy scorer — objective-parameterized or one utility? (round 15B)', focus:`Round 15B: the scorer must become OBJECTIVE-PARAMETERIZED (pluggable objective set per actor/faction), not one war-utility. In settlementStrategy.js: how does it SCORE enumerated moves (enumerateMoves + softmax)? ONE hard-coded war utility or already weight-parameterized? What to plug a merchant objective (revenue/reliability/credit) vs warlord (prestige/opportunity/weakness) vs church? Is disposition.js (computeAggressiveness + proposed computeLawfulness/computeMalice) wired into scoring? Feasibility of parameterization.` },
  { key:'faction-state', label:'powerStructure / faction state — hang per-faction belief + objectives? (round 14/15)', focus:`Round 14 factional belief + round-15 faction-specific objectives. OUR faction model: powerStructure.factions (settlement.schema.js), factionCatalog.js, factionProfile.js, factionRelationshipUpdate.js. Do factions have stable IDs, TYPES (government/military/merchant/religious/craft/criminal), power/influence weights, a governing-coalition concept (governingName + supporters)? Enough to (a) key a belief map by factionId, (b) attach a faction-type objective set, (c) model coalition=strategic-decider + others=dissent/leak/alt-action? Missing?` },
  { key:'fidelitynoise-style', label:'fidelityNoise — a distortion STYLE (5 levers) not a scalar? (round 15A)', focus:`Round 15A: alignment changes HOW distortion occurs (source preference/framing/uncertainty-tolerance/manipulation-incentive/transmission-style), a signature with strengths+weaknesses, NOT a scalar. fidelityNoise.js (fidelityFactor/chaosPull) + how alignment is derived/consumed (chaosPullOf, corruption.js, TRAIT_ALIGNMENT). Is today's distortion a SCALAR magnitude or can it carry a directional/styled signature? What to make it STYLED by law/chaos + good/evil rather than scaled? Feasibility.` },
  { key:'cost-versioning-profiles', label:'cost-law versioning + route receipts + per-carrier profiles (round 16.1/16.2)', focus:`Round 16.1: the spatial canon needs THREE version axes (geometry/cost-law/overlay) + ROUTE EXPLANATION RECEIPTS. 16.2: carrier-specific route PROFILES over shared geometry. Investigate: the map/canon schemaVersion + migration chain (mapSlice/campaignSlice/worldState.js migrations) — can it carry multiple independent version axes? Is there a causal-RECEIPT / explanation pattern to reuse (W-C5 receipts, causeLifecycle, dossier causal read-models, the "why" legibility)? And the routing cost function (roadNetwork.js / sf-bridge.js computeRoadNetwork) — is it ONE cost function or parameterizable per carrier profile? Feasibility of versioned + receipted + per-carrier routing.` },
  { key:'rumor-lineage-correlation', label:'rumor lineage + SOURCE-CORRELATION + cardinality controls (round 16.3)', focus:`Round 16.3 (the critical one — a CORRECTION to round-8 cross-confirmation): cross-confirmation must weight by INDEPENDENCE not count; 5 retellings of 1 origin ≠ 5 corroborations. Need canonical event id + LINEAGE ids + merge rules + salience thresholds + expiration + per-observer budgets + source-correlation. Investigate wizardNews.js: sourceEventId → graph.eventLog (the canonical-event substrate). Is there ANY lineage/parent-telling field, provenance chain, or dedup/merge (admitStrongest, deriveNewsThreads)? Does any cross-confirmation/corroboration logic exist yet? What must be built for independence-weighted corroboration + the cardinality controls?` },
  { key:'belief-update-substrate', label:'belief-update conflict-resolution substrate (round 16.4)', focus:`Round 16.4: contradictory reports must reconcile by a consistent WEIGHTED rule (provenance×recency×independence×prior, filtered by disposition/bias, contradiction→uncertainty, confidence-decay-with-silence) — the consumption point of rounds 12/13/15. Investigate existing UPDATE/FOLD/reconciliation patterns to model it on: relationshipState.js updates, martialReadiness.js updates, perceivedPeaceExhaustion fold (settlementStrategy.js:140-156), fidelityNoise consumption, any conditional-ledger accumulation (conquestFeeds/institutionTolerance read-last/write-next). Where does a per-(observer,subject,attribute) weighted belief-update attach + persist? Is there a decay/confidence precedent?` },
  { key:'testinfra-and-brakes', label:'invariant-test infra + existing feedback-loop brakes (round 16.5/16.6)', focus:`16.5: a spatial INVARIANT test suite (same-event→same-degradation, reroute-isolation, no-reroll-on-unrelated-add, stable tie-break, overlay-isolation, dormant byte-identity, old-save fallback). What determinism/invariant TEST infra exists to extend — the no-Date / unseeded-random grep-gates, golden masters (generator/worldpulse/pdf), byte-identity/dormancy-oracle tests, property tests? 16.6: existing feedback-loop BRAKES/saturation to reuse — thievesGuild saturation, security-drag floor, any capacity ceiling / congestion / diminishing-returns / prosperity saturation in populationDynamics/economy/stressorDynamics. What damping already exists vs must be built for the entrepôt/religious/chokepoint/info-center loops?` },
]
phase('Ground')
const ground = (await parallel(GROUND.map(a => () =>
  agent(`${pre}\n\nYOUR SYSTEM: ${a.label}.\n${a.focus}\n\nStructured facts, file:line, honest feasibility.`,
    { label:`ground:${a.key}`, phase:'Ground', schema:GS, effort:'medium' })
))).filter(Boolean)
log(`Grounded ${ground.length}/${GROUND.length}`)
const digest = ground.map(g=>`### ${g.system} [${g.feasibility}]\nEXPOSES: ${g.persists_or_exposes}\nFILES: ${(g.key_files||[]).map(f=>`${f.file}${f.lines?':'+f.lines:''} (${f.what})`).join('; ')}\nATTACH: ${g.attachment_points}\nGAPS: ${g.gaps}`).join('\n\n')
phase('Stress-test')
const critique = (await parallel([
  { key:'full16', label:'Full 16-round coherence + round-16 hardening cost + build placement', focus:`Read rounds 1-16 + PARTS II-V as ONE system. Do rounds 14-16 stay additive/byte-safe/tractable? SPECIFICALLY: (a) is the SOURCE-CORRELATION fix (16.3) a MUST-FIX before the rumor mill ships (else the entrepôt best-informed advantage is an illusion)? (b) does cost-law VERSIONING (16.1) belong in the cost-field keystone wave? (c) does OBJECTIVE-parameterization (15B) belong in belief Wave A or later? (d) does the belief-UPDATE rule (16.4) + lineage (16.3) fit within Wave A, or split? (e) the 4 new feedback loops (16.6) — which lack a brake in-tree and need one built WITH their mover? (f) the invariant-test burden (16.5) — realistic per-wave budget? Any NEW owner decision 14-16 surface? Contradictions? Concrete build-placement for each round-16 item.` },
].map(c => () =>
  agent(`Adversarial critic, Phase 5.5 (read ${DESIGN} §4g + PARTS II-V; root ${ROOT}). Axis: ${c.label}.\n${c.focus}\n\nGROUNDING DIGEST:\n${digest}\n\nReal failure modes ranked, each a concrete mitigation. No padding.`,
    { label:`critique:${c.key}`, phase:'Stress-test', schema:CS, effort:'high' })
))).filter(Boolean)
log(`Stress-tested ${critique.length}/1`)
return { ground, critique }