export const meta = {
  name: 'build-cycle-27b-shingled-band',
  description: 'THE SHINGLED FLETCH BAND: photo-real overlapping feathers cascading Create→Library→Realm + thinner shaft · plate + seal · AA tail',
  phases: [{ title: 'Build', detail: 'one nav lane, four pieces, implement + verify with eye pass' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files (nav/*, theme.js, nav tests, App.jsx header block); no stash; focused vitest + gate-tail (ps aux | grep vitest first); python byte-scan every Write; npm run build + smoke:boot green; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. SURVEY FIRST: two prior lanes were stopped minutes ago on these files — read git log + status; landed/uncommitted badge work is progress to reuse; sweep/band work is SUPERSEDED here. STANDING LAWS: hang is PAINT never layout; ANCHOR derives; focus rides an aria-hidden vane; screenshot BEFORE pinning geometry; ground-move = full AA re-census on every rider incl. AccountMenu's transparent chip; pin the COMPOSITED ground (grain over cylinder), never the cylinder alone.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: OWNER-DIRECTED implementation from a mockup + a live refinement (the owner: keep it as PHOTO-REAL as possible; the overlap itself should show the direction Create→Library→Realm; final shape delegated). ${P10}
THE COMPOSITION (the owner's mockup, refined): a THIN wooden shaft bar; riding it, ONE CONTINUOUS FLETCH BAND roughly twice the bar's height, top-aligned, HANGING well below the bar (~40-50% of the band under the bar's bottom edge, PAINT only); inside the band, Create/Library/Realm are three goose-feather vanes SHINGLED like real stacked fletching: EACH FEATHER PARTIALLY LAPS THE NEXT — Create's trailing region lies OVER Library's leading edge, Library's over Realm's — with a soft CONTACT SHADOW where each upper vane laps the lower (the lap shadow is what makes it photo-real instead of flat parallelograms); the repeated exposed slanted leading edges form a rightward CASCADE that reads as the flow direction Create→Library→Realm, like fanned cards. NO drawn seam lines, NO wood gaps inside the band (the NavDivider seams inside the cluster are RETIRED). After Realm the band ends (its last trailing edge resolves cleanly); Compendium/Gallery/About sit small and plain on the bar; the red-brown wraps bracket the band's two ends.
LANE SH — FOUR PIECES, one commit each:
(1) THE SHINGLED BAND as above: each vane keeps the full goose treatment (barb comb ALIGNED to its slant with a small DETERMINISTIC jitter in barbGap/alpha — seed-stable arithmetic; rachis; pale leading edge; sheen bands), plus per-lap contact shadows and a whisper of edge-light on each exposed leading edge (the two cues that sell depth); labels keep their CURRENT font size, each in its vane's calm zone, AA against the vane's lightest band; active grammar preserved (brightened vane + gold underline); focus ring proven on a shingled cell.
(2) THE THINNER SHAFT: CHROME.headerDesktop 48 → the thinnest height that seats wordmark/reference tabs/Sign In at CURRENT font sizes (target ~36-40; measure; PIN the font sizes so the change provably shrinks no type); ANCHOR re-derives; the band's height is INDEPENDENT (it keeps label-comfortable height and hangs deeper as the bar thins); mobile follows thinner, no band; full ground-move AA re-census.
(3) THE MAKER'S PLATE + WAX-SEAL WORDMARK exactly to task #78's research-locked spec (read verbatim; reuse any sound partial work from the stopped lanes); plate scales to the thinner bar; the plate stays a half-step quieter than the band.
(4) THE COMPOSITED-AA PIN: assert against the composited worst-case ground (grain max-darkening from the feColorMatrix constants or a rendered-pixel probe); the one-token grain-darkening mutant must red it.
Screenshot desktop 1440x900 + mobile + close crops of the band's laps and the plate at every iteration, honest eye verdicts (acceptance: real shingled feathers on a thin shaft, cascade pointing right, plate quiet at the nock). Return {lane:'SH', commits, gates, stopReports, notes}.`,
  {label:'sh:shingled-band', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier with an EYE PASS. ${P10}
Report: ${JSON.stringify(r)}. Serve, screenshot, JUDGE: (a) do the three vanes read as SHINGLED FEATHERS — each lapping the next with a visible contact shadow — and does the cascade point rightward (cover the labels and ask: which way does it flow)? (b) zero wood gaps and zero drawn seams inside the band (measure rendered pixels); (c) hang ~40-50% below the bar, paint-only (hide-and-diff layout boxes); (d) bar thinner with font sizes UNCHANGED (measure both, quote px); (e) the plate subordinate; the wordmark cold-reads with the seal-o; wax matte; grayscale-binds; (f) photo-real verdict stated plainly (feathers or flat cells?). Mechanical: ANCHOR derivation; composited-AA pin + its mutant run by YOU; AA from REAL rendered pixels on every rider (quote ratios); focus by real Tab on plate and a shingled cell; two-load byte determinism; Rule-of-Tincture mutant; typed contexts plain; mobile; build + smoke:boot; pathspec-clean. Return {lane:'SH verify', verdict, findings}.`,
  {label:'sh:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }