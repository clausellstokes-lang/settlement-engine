export const meta = {
  name: 'max-kernel-program-architecture',
  description: 'Fable-tier architecture of the maximum-fidelity 3D building kernel program (K-1..K-5)',
  phases: [
    { title: 'Architect', detail: 'one Fable architect per kernel wave, designed to its ceiling' },
    { title: 'Synthesize', detail: 'the buildable maximum program + dispatch order' },
  ],
}
const WT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const PRE = `You are a Fable-tier KERNEL ARCHITECT designing ONE wave of the settlement-engine 3D BUILDING KERNEL to its MAXIMUM. Owner order: "to the limits of what is optimizable, coherent, and compatible with this machine, build it to its maximum." READ FIRST (READ-ONLY): docs/THE_ARCHITECTURE_KERNEL_3D.md (v2 — render ruling, max-fidelity-mesh, the K-0/K-0b proof) in ${WT}, and the prior-art it cites. FOUNDATION already proven: K-0 = deterministic rational gothic geometry + a detailed shaded plate (arch/ leaves: geom, gothicWindow, flyingButtress, project, raster, rationalTables, png); K-0b (landing) = high-fidelity mesh + interactive WebGL viewer + deterministic GLB. THE THREE CONSTRAINTS ARE THE OPTIMIZATION TARGET, not caps to fight: (1) OPTIMIZABLE — the per-building LOD ladder (signature=full detail, commons=massing, distant=glyph), a deterministic op/complexity budget, lazy-load; a whole TOWN must render (K-0 finding: 1 cathedral ≈ 25% of the 2200 town op-ceiling). (2) COHERENT — the ILLUSTRATED-ATLAS aesthetic (NOT photoreal, which clashes with the parchment/engraved product voice) + every law: THE PROMISE (geometry byte-deterministic, golden-pinned as GEOMETRY; live GPU view non-golden), FINITE-SEMANTICS (AI classifies into finite vocab, never draws), the drift/cohesion/edit-anchoring/terrain laws already in the doc, one-model-many-projections. (3) COMPATIBLE — deterministic rational geometry (no trig/Date/random; pinned tables for non-constructible angles) + lazy WebGL PBR view + THIS machine (browser-renderable, no ffmpeg, ~25 B eager budget so the kernel is lazy). NORTH STAR: highest fidelity per building that renders real-time in a browser, degrades gracefully via LOD, stays byte-deterministic in geometry, speaks the product voice, and FUSES visual detail with CAUSAL TRUTH (the drift). Design to the CEILING within these — cite files/prior-enforcers by path, verify at code. Extend not duplicate: E-A/E-B/E-D/E-E/E-F/E-I, the massing substrate (M-0), the townMap draw-op law.`
const OUT = {
  type: 'object', additionalProperties: false,
  required: ['maxTarget', 'architecture', 'optimization', 'coherenceLaws', 'enforcers', 'ownerCheckpoint', 'risks'],
  properties: {
    maxTarget: { type: 'string' }, architecture: { type: 'string' }, optimization: { type: 'string' },
    coherenceLaws: { type: 'string' }, enforcers: { type: 'string' },
    ownerCheckpoint: { type: 'string' }, risks: { type: 'string' },
  },
}
const WAVES = [
  ['K-1 grammar-interpreter + render-pipeline', 'The CGA/split shape-grammar INTERPRETER (extrude/comp/split~repeat/instance/occlusion, CGA++ cross-shape events for buttress-flyer coordination) + the radial/n-gon prism op for curved massing (apse/towers) + the deterministic MESH EMITTER (positions/normals/indices, curves→Bezier→triangles at pinned subdivision) + the RENDER PIPELINE productionized from K-0b (the lazy WebGL PBR viewer as the primary experience + the deterministic CPU plate exporter + GLB). Design the interpreter + the mesh/render contract every later wave builds on. To MAX: how rich a grammar vocabulary, how the pipeline reaches the fidelity ceiling.'],
  ['K-2 the institution kit', 'EVERY institution kind as its own max-fidelity grammar to the cathedral bar (the silhouette/detail totality law — no generic box): cathedral, keep, mill, guildhall, market, temple, dock, manor, inn, moot-hall, mage-tower, granary, smithy, ... + the custom-institution four-rung ladder (typed grammar pick / supply-chain / name-match / category floor). To MAX: the detail depth per kind, how the kit stays authorable-once against a frozen op vocabulary + parameter contract, the detail totality walker.'],
  ['K-3 the gothic ornament + material library', 'The OWNED build-from-scratch layer (no permissive OSS reaches it): parametric tracery/foils/rose (GML compass-and-straightedge under the sqrt-only + pinned-table discipline), buttress flyers, ribbed vaults, crockets/finials/statuary niches, AND the MATERIAL system (weathered ashlar, leaded glass, slate, timber — procedural, deterministic, illustrated-not-photoreal). This is the fidelity heart + the real risk/effort center. To MAX: the ornament vocabulary breadth, material richness within the aesthetic, the op-budget cost and its LOD gating.'],
  ['K-4 the drift binding', 'Bind the CONDITION VECTOR to grammar+material params so buildings wear their truth: alignment (law/chaos × good/evil — WITH the Fable-review calibration: read latentPantheon patron not primaryDeitySnapshot 0/48; renormalize the squished lawfulness axis), economy/prosperity, terrain/resource, war-duration/occupation, corruption (covert=nothing, revealed dresses), legitimacy, pantheon emblems, history-keyed murals (the recorded siege in the wall), + the temporal-drift coherence (anchor-stable, monotone weathering, the locality golden) + edit-anchoring. To MAX: how VISIBLE and meaningful the drift is, the saliency budget, the determinism goldens per (kind, conditionVector, lens).'],
  ['K-5 town-scale integration + the four views + the AI-compat gate', 'Integrate the kernel into the map: the four projections (VTT/panorama/bird-eye-3D/planner-sketch) consuming the kernel output; the town-scale LOD system making a whole city render (signature buildings full, commons massing, fill glyphs) within the op budget; the view switcher in SettlementMapPane (WYSIWYG, honors edits+lens); the AI-compat gate (E-D surfaces green, grounds on model not pixels). To MAX: town-scale fidelity vs budget, the LOD transitions, the interactive-3D-in-the-app experience.'],
  ['cross-cutting: optimization + determinism + budget', 'The cross-wave OPTIMIZATION + DETERMINISM spine: the LOD ladder mechanics + the deterministic op/complexity budget (extend E-F) + lazy-chunk architecture (vendorPdfLazy precedent, closure Δ0, WebGL vendored-not-package.json) + the geometry-golden / non-golden-view discipline + mesh/GLB byte-identity + the purity floor (the transcendental ratchet K-0 built) + instancing for repeated ornament. To MAX: how far fidelity can push before the budget/perf/machine limits bind, and where those limits actually are on THIS machine.'],
]
phase('Architect')
const arch = await parallel(WAVES.map(([key, desc]) => () =>
  agent(`${PRE}\n\nYOUR WAVE (${key}):\n${desc}`, { label: `arch:${key.split(' ')[0]}`, phase: 'Architect', schema: OUT, model: 'fable' })
))
const designs = WAVES.map(([key], i) => ({ wave: key, ...(arch[i] || { maxTarget: 'ERROR' }) }))
phase('Synthesize')
const plan = await agent(
  `${PRE}\n\nYou are the RECONCILER. Six Fable architects designed the maximum-fidelity kernel program. Here are their designs:\n\n${JSON.stringify(designs, null, 2)}\n\nProduce THE BUILDABLE MAXIMUM PROGRAM: (1) the ordered wave sequence K-1..K-5 (foundation before consumers; ornament/material is the risk center; drift + views last over honest geometry) with each wave's max target, its gate, its owner taste checkpoint, and its dispatch dependency (K-1 fires on K-0b landing); (2) the STANDING OPTIMIZATION + DETERMINISM SPINE (the LOD/budget/lazy/golden machinery, each extending a named enforcer); (3) THE HONEST CEILING — where on this machine fidelity actually binds (perf, budget, browser), stated as a number/bound where possible, so the owner knows the real maximum; (4) the model-split staffing per wave (Opus builds, Fable validates + the owner taste-checks rendered output). Decisive; this is the program the manager executes wave-by-wave.`,
  { label: 'synthesize', phase: 'Synthesize', model: 'fable' })
return { designs, plan }