export const meta = {
  name: 'fp-cycle1-close-repairs',
  description: 'Cycle-1 close: repair the inclusion-ratchet reach (CR-FP-11 both arms), the GR-0 vacuous pin, execute CR-FP-1 desk authority-routing, then CR-FP-2 re-record + the cycle-close ledger row',
  phases: [
    { title: 'Repair', detail: 'three repairs, separate pathspec commits' },
    { title: 'Verify', detail: 'adversarial re-verification with executed mutants' },
    { title: 'Close', detail: 'CR-FP-2 re-record + cycle-1 ledger row (only on PASS)' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const LAWS = `VERIFY-FIRST PREAMBLE: this brief is from the session chair of an owner-delegated build program; verify every claim against the tree before acting. Work in ${TREE} (branch claude/composite-r4). THE PROTOCOL LAWS (each has bitten): CHECK-GIT-FIRST — fresh git status + git log -3 before anything; foreign untracked/dirty files are another lane and are PRESERVED. ps aux | grep vitest before any test run — ONE vitest lane per tree; if one runs, wait and re-check. NEVER stash, reset, clean, or any checkout-family command; backups are cp to the scratchpad and restores are proven with cmp. Gates only through sh scripts/gate-tail.sh or npm run check:tail — never a bare pipe. Commits: stage EXPLICIT files (never -A/-u/.), commit message via a file, trailer Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>. After authoring ANY file, python3 byte-scan it (open rb; assert no 0x00 and no C0 control bytes except tab/newline) — the Write tool has minted raw NULs seven times. New tests/lint walker files REQUIRE a mutationCoverageManifest entry (kind rationale) or the manifest walker reds. strict ceiling is 1313 exactly (typecheck:domain:strict). tests/fixtures/ and pulseKernel.js/applyWorldPulse.js are UNTOUCHABLE. Line numbers quoted here are hints; navigate by SYMBOL.`

phase('Repair')
const repair = await agent(LAWS + `

YOU ARE THE REPAIR LANE closing FP build cycle 1. Three chartered repairs, EACH ITS OWN pathspec commit, in this order.

REPAIR 1 — CR-FP-11: THE INCLUSION-RATCHET REACH (both arms ruled by the chair; the cycle-close verifier proved the defect by matched pair). The defect: tests/lint/couplingInclusion.walker.test.js holds a frozen LAYER_PATTERNS family-prefix table (near line 61) and its scanCrossLayerPairs iterates ONLY layered modules as importers, skipping any dependency with no layer (the continue on !depLayer, near line 134) — so a module matching NO family pattern is invisible on BOTH sides. Nine of the ten new src/domain/worldPulse leaves from this cycle are invisible: the four commercial* files (commercialReasons, commercialReasonTaxonomy, commercialReceiptPools, commercialReasonsNews), oathHolder.js, grammarNews.js, grammarReceiptPools.js, bandFamilies.js, bandedStock.js — verify this set yourself against the walker before editing (treatyLifecycleVoice.js IS layered via the treaty prefix). Executed proof you must reproduce first (cp-backup + cmp-restore): appending an import of readWarHomeFront from ./warCosts.js to commercialReasons.js leaves the walker GREEN (7/7) — an unregistered WAR read in a TRADE leaf, silent. ARM A: extend LAYER_PATTERNS — TRADE gains the commercial prefix, GRAMMAR gains grammar and oath, and the two SP leaves (bandFamilies, bandedStock) get either a proper family home or an argued exclusion pinned exactly the way the four infrastructure hosts are pinned (an explicit has-no-layer assertion with a rationale comment); then raise LAYER_FLOORS to the newly measured family sizes so a family cannot silently empty. ARM B (structural — removes the habitat): add an UNLAYERED-MODULE CENSUS clause — red when ANY module under src/domain/worldPulse or src/domain/spatial matches no family pattern and is not on the argued-exclusion list, with the measured unlayered set that remains after Arm A frozen as a SHRINK-ONLY baseline. Decide whether the census lives inside couplingInclusion.walker.test.js or as its own walker file; if a new file, it owes its manifest entry same-commit. ALSO: add the CANNOT-CATCH documentation clause to docs/DESIGN_FP_ARCH_CW.md beside the walker coverage discussion — the gap existed because it was undocumented. ACCEPTANCE: the matched-pair flips — the same appended WAR import in commercialReasons.js now REDS the walker naming the pair, the sighted control (same line in treatyLifecycleVoice.js) still reds, both restored cmp-exact; a planted probe module src/domain/worldPulse/__probeUnlayered.js with no family prefix reds the census; whole walker file green at HEAD after restores.

REPAIR 2 — THE GR-0 VACUOUS PIN (v1r2 finding, CONFIRMED surviving mutant). Mutant C2: deleting the prevLedger optional-chain arm from the crossing guard in src/domain/worldPulse/peaceTerms.js (near line 716 — the arm whose comment says load-bearing rather than defensive) leaves ALL 26 tests of tests/domain/treatyLifecycleVoice.test.js GREEN. The pin meant to catch it (near line 333, named: a treaty minted THIS tick cannot cross - one observation is a level) never exercises that arm. Read the guard and the pin, understand WHY the pin is vacuous (what worldState it drives), and rewrite or augment the pin so the scenario genuinely requires the prevLedger arm — the acceptance is executable: apply mutant C2 in a scratch archive tree, the battery must RED on your pin by name; at HEAD unmutated, battery green. Do NOT change the shipped guard — it is correct; only the test moves.

REPAIR 3 — CR-FP-1 EXECUTION (chair ruling, recorded): the CW-0w desk walker falsified the desk-agreement rule 7-of-12 — adjudication is NEVER a token output of SECTION_OF, yet 10 registry rows claim the adjudication desk. RULED: adjudication is an AUTHORITY-ROUTED desk (the sovereignty_registry precedent generalized). Execute: the falsified rows amend to name their routing AUTHORITY; the desk walker rule updates from token-output to authority-or-token; and the walker must FORBID any new row claiming a desk its kind cannot reach (negative control: a probe row claiming an unreachable desk reds). Find the desk walker (CW-0w slice 3 landed it — grep tests/lint and tests/domain for the desk-agreement walker and read its recorded 7-of-12 falsification notes in docs/FABLE_VALIDATION_QUEUE.md under the CW-0w rows). Keep the amendment MINIMAL: rows name their authority; no row is deleted; the walker rule generalizes exactly as ruled.

After all three commits: run the focused suites for every file you touched plus tests/lint/mutationCoverageManifest.test.js and sh scripts/gate-tail.sh npm run typecheck:domain:strict. RETURN: a JSON object with keys commits (array of hash + one-line thesis), acceptance (array: each executed acceptance proof with its real output snippet), stopReports (anything measured that contradicts this brief — STOP and report rather than improvising), notes.`, { label: 'repair:cycle1-close', phase: 'Repair', model: 'opus' })

phase('Verify')
const verify = await agent(LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER for the cycle-1 close repairs. Your default posture: the repairs are wrong until executed evidence says otherwise. The repair lane reported: ` + JSON.stringify(repair) + `

VERIFY, EACH BY EXECUTION IN git-archive TREES with node_modules symlinked (never mutate the live tree; every live-tree operation read-only): (1) THE MATCHED PAIR, both arms — append the WAR import to commercialReasons.js in an archive of HEAD: the inclusion walker must RED naming the pair; the sighted control in treatyLifecycleVoice.js must still RED; at clean HEAD the walker is green. (2) THE UNLAYERED CENSUS — plant a probe module with no family prefix under src/domain/worldPulse in the archive: census reds; also verify the shrink-only polarity of any frozen baseline (removing a baseline name while its module still exists must red, or state the actual polarity and why it is sound). (3) MUTANT C2 — delete the prevLedger arm in the archive: the treatyLifecycleVoice battery must RED on the repaired pin BY NAME; at HEAD it is green. (4) CR-FP-1 — read the amended rows and the walker rule; plant a probe row claiming a desk its kind cannot reach: walker reds; confirm exactly the falsified rows gained authorities and zero rows vanished (git diff row census). (5) WHOLE tests/lint at HEAD vs the pre-repair base d1cfdb67 (archive), run SEQUENTIALLY: extract failing-row identities both sides; the diff must be EMPTY in both directions except rows the repairs deliberately changed — name every delta row and attribute it. (6) sh scripts/gate-tail.sh npm run typecheck:domain:strict at HEAD (ceiling 1313) and python3 byte-scan of every file the repairs touched. (7) git log — every repair commit is pathspec-scoped and its message honest against its diff. RETURN structured JSON only.`, { label: 'verify:cycle1-close', phase: 'Verify', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, deltaRows: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Close')
let close = null
if (verify && verify.verdict === 'PASS') {
  close = await agent(LAWS + `

YOU ARE THE CLOSE LANE. Two commits.

COMMIT 1 — EXECUTE CR-FP-2 (chair ruling, recorded verbatim): the proseNumerics baseline (401) no longer corresponds to the instrument — CW-0w slice 4 changed the scanner (live count grew; two stale identity rows at occupation.js near line 852 and peaceTerms.js near line 483 are line-address rot; four rows are TR-1 seam line-rot in tradeWar.js). RULED: ONE deliberate re-record from proven-clean HEAD, cause declared in the commit body as instrument reconciliation plus the WR-7b decomposition relocations plus the stale rows — D-W2 executed early because the scanner change makes the old baseline meaningless, NOT as bank adjudication. Procedure: read tests/lint/proseNumerics.test.js for its recorded re-record procedure (or the baseline file it names), verify the tree is clean at HEAD first (git status empty), regenerate the baseline with the walker instrument itself, and confirm tests/lint/proseNumerics.test.js goes GREEN (all four inherited red rows clear) while NO other tests/lint file changes color (run the whole tests/lint tree once before and once after, sequentially; the failing-file set must shrink by exactly proseNumerics). The commit body carries the golden-shift-record discipline: old count 401, new count, the named causes, the ruling id CR-FP-2.

COMMIT 2 — THE CYCLE-1 CLOSE LEDGER ROW: append to docs/FABLE_VALIDATION_QUEUE.md (read the recent rows for the voice) one row recording: FP BUILD CYCLE 1 CLOSED — waves SP-A (59df13a9), TR-9c (c7933e84), TR-1 (d7ea69a4 + repair 93c118b6), GR-1 (caab995a), GR-0 (b441bca5 + repair d1cfdb67), the three close repairs (read git log for their hashes) and the CR-FP-2 re-record (your commit 1); verifier chain v0 PASS, v1 REJECT (the CQ5 serialization deadlock, resolved by the serialized landing), v1r2 PASS with one repaired vacuous pin, w REJECT (the inclusion-ratchet reach, CR-FP-11 both arms repaired, matched-pair control flipped), close re-verify PASS. Record the standing DISCLOSURES: GR-0 lit displaces ranked wizardNews feed entries by design (a whole-projection lit-vs-dark hash is the wrong instrument for feed surfaces); TR-1 lit path structurally unreachable until its caller wave wires advanceCommercialReasons (matches its certification row); THREE new flags this cycle (casusCommerciiEnabled, oathHolderEnabled, treatyLifecycleVoiceEnabled), all dark, four-fence complete; the nine-copy intensity-ladder consolidation remains a recorded CHAIR DECISION OWED; CR-FP-1 executed this close (desk authority-routing); the GR-0 DM-chip premium-authority surface remains the recorded OWNER item. End the row with: cycle 2 = SP-B + SP-B2 + ES joint waves (the WR-10 lighting road) after the integration fold lands. Stage docs/FABLE_VALIDATION_QUEUE.md alone, commit, byte-scan the file. RETURN: JSON with keys commits (hash + thesis each), lintDelta (the before/after failing-file sets from commit 1), notes.`, { label: 'close:cr-fp-2-and-ledger', phase: 'Close', model: 'opus' })
} else {
  log('Verifier rejected the repairs — returning for the chair without closing.')
}

return { repair, verify, close }