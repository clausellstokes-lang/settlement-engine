export const meta = {
  name: 'build-cycle-20-boot-fix',
  description: 'THE BOOT FIX + boot smoke gate · V2/LU verifier tails · LU-2 remainder sweep (ruled GO)',
  phases: [{ title: 'Build', detail: '3 lanes, implement + verify each' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); R-BLD-9 strict law; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY: cycle 19 is LIVE on src/domain/worldPulse + certification tests — fenced; queue file may carry foreign hunks — carve or commit-body. The tree moves: re-read status before staging.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const lanes = await parallel([
  () => agent(`VERIFY-FIRST: chair-ruled CRITICAL repair. ${P10}
LANE BT — THE PRODUCTION BUNDLE DOES NOT BOOT (the estate's sharpest live defect; the shipped dist has been un-bootable since a80c0be4 and NO gate sees it — npm run build exits 0, prerender is Node-side, tests are jsdom). THE FAULT, verified by the cycle-18 verifier in a real browser: dist/assets/engine chunk throws "Cannot access 'Ot' before initialization" at src/generators/factionRoles.js's module-scope \`const CANONICAL_TO_ROLE = Object.freeze({ [FA.CRIMINAL]: 'thieves', ... })\` — FACTION_ARCHETYPES read before its chunk-mate initialises; a Rollup CHUNK-ORDERING fault (no source-level cycle within depth 14), introduced when a80c0be4 added factionArchetypes.js → arcaneIdentity.js.
ORDER OF WORK — the gate comes FIRST because the broken dist is the perfect negative control and it exists RIGHT NOW:
(1) THE BOOT SMOKE GATE: author scripts/boot-smoke (wired into npm run build's postbuild or check) that loads the BUILT dist's module graph and FAILS on any module-init throw + asserts the app shell actually mounts (design the mechanism yourself — real-browser load via the available tooling, or a DOM-shimmed module-graph import if it provably catches the TDZ class; whatever you choose must RED ON TODAY'S DIST — prove that first, quote the red).
(2) THE FIX: make the fault site TDZ-safe or the ordering deterministic — prefer the smallest robust cure (e.g. lazy/function-scope resolution of the archetype map, or an import-graph restructure that removes the ordering hazard) over Rollup manualChunks tuning unless tuning is provably the only stable cure; NO behaviour change (the map's contents byte-identical; same-seed goldens untouched — hash a golden corpus row pre/post).
(3) Prove the gate GREEN on the fixed dist; the full arc in the commit body: red-on-broken → fix → green-on-fixed.
Fenced from worldPulse. Return {lane:'BT', commits, gates, stopReports, notes}.`,
    {label:'bt:boot-fix', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify END TO END IN A REAL BROWSER yourself: build, serve dist statically, load clean — #root must render the app shell, zero console errors at init; then REVERT the fix in a scratch copy and prove the boot-smoke gate reds on the broken build (the reproduce-then-clear arc re-run by YOU); confirm the archetype map is byte-identical pre/post (hash both) and a golden corpus row hashes identical; the gate is actually wired into the build/check path (run the npm script, not the file); strict/eslint/pathspec clean. Return {lane:'BT verify', verdict, findings}.`,
      {label:'bt:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled repair implementation. ${P10}
LANE VT — THE CYCLE-18 VERIFIER TAILS (all small, verified findings):
V2 tails: (1) pin the header's OWN minHeight (mutant: delete minHeight+boxSizing from the App.jsx header → a pin must red; today navFletching stays green); (2) the bloom-alpha pin is self-referential (asserts a string interpolated from the token it checks) — freeze the expected literal + a drift message naming which side moved; (3) the grain-band record: measured 1.96% sits OUTSIDE the stated 2-4% band — make the record honest (either widen the stated band to '≈2-4%' citing the owner's 'subtle' intent with the measured values, or nudge the tone inside the band; pick what reads best on screen and SAY which you chose); (4) two stale '84' comments (CompareSection.jsx:42, AboutManifesto.jsx:47) → the derivation, not a number; (5) the raw-button budget unit: NavRibbon.jsx:48's docstring literal '<button' costs the LAST budget unit — respell it in prose.
LU tails: (6) the gate's 3 uncovered named consumers (of 12) — cover them in GATE_PRIMITIVES or correct the IconsContext roster to the true 9 (pick the one that matches reality, machine-checked); (7) StateBadge.jsx:17's false claim 'It consults the gate now' — the icon channel was DELETED, not gated: correct the docstring; (8) the STOP record's two counts (IconButton files = 52 not 55; five-of-seven not four) — correct where recorded.
One commit per family. Return {lane:'VT', commits, gates, stopReports, notes}.`,
    {label:'vt:tails', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Run every mutant yourself: the minHeight delete (must red now), the bloom-token drift (must red naming the side), the gate-roster lockstep (mutant a roster entry → red); confirm the grain-record choice was ACTUALLY made (read the values + the record; they must agree); the raw-button census back under budget with headroom 1; the docstring truths grep-proven; pathspec-clean vs the other lanes. Return {lane:'VT verify', verdict, findings}.`,
      {label:'vt:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE LU-2 — THE REMAINDER SWEEP, RULED GO: the icons-off law burns down the 127 frozen direct importers, family by family (tabs, cards, panels, dialogs, pdf...). THE RULES: where a file's lucide render is DECORATIVE beside text — remove render + import + heal the slot (the dead-slot class); where an IconButton or icon IS the control (52 files carry IconButton) — route it through the GATED primitives so the affordance survives icons-off with its text/aria fallback (never delete a control; never orphan an aria-label); the map subtree and kept classes untouched. The lucideTotality walker's frozen census SHRINKS with every commit — that is the proof of progress; close WHOLE families, STOP-report the exact remainder when budget ends (count it). Suites per family + eslint + sizeBaseline. Return {lane:'LU-2', commits, gates, stopReports, notes}.`,
    {label:'lu2:remainder', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Re-census the walker's frozen list at HEAD (count the shrink yourself); spot-render 6 swept surfaces across families for slot health + control survival (an IconButton file must still expose its control with text/aria under icons-off — render one and prove it); no kept-class or map file in any diff; any STOP remainder exactly enumerated; suites green; pathspec-clean. Return {lane:'LU-2 verify', verdict, findings}.`,
      {label:'lu2:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
])
return { lanes: lanes.filter(Boolean) }