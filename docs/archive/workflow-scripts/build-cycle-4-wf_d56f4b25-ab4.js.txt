export const meta = {
  name: 'build-cycle-4',
  description: 'Land WR-7b under R-BLD-6 · fix MG-4 records + census gap · close the veil hole + gate-clean lane C',
  phases: [{ title: 'Build', detail: '3 lanes, implement then verify' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only, hard-gate compounds; pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (and NEVER diff two gate-tails — full logs for walker diffs); STOP-and-report conflicts; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Best-over-safe. Everything DARK.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const lanes = await parallel([
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE A — LAND WR-7b UNDER R-BLD-6 (binding chair ruling): the size-baseline IS the banked-debt ledger and eslint reads ceilings from it — make the banked ruling mechanical. (1) Update scripts/.size-baseline.json: RE-FREEZE the three grown-past-frozen files at their measured TREE sizes (applyWorldPulse, pulseKernel, warDeployment — measure them yourself with the enforcer's own rule) and ADD entries at measured tree size for every currently-over-ceiling unbaselined file (incl. envoyErrand, peaceTerms, eventProse, roadsKernel, settlementStrategy, warTermination — measure each); verify tests/lint/sizeBaseline.test.js goes GREEN (exactness restored) and eslint max-lines clears on the lane surface. (2) Commit WR-7b WHOLE as one pathspec commit (all wave src + tests + the four leaves + the queue rows with attribution), with the commit body carrying: the banked-debt citation (the WR-7b pre-build ruling row's own guardrail sentence), the per-file inherited-vs-wave attribution numbers from the verifier reports (wf_eb76f827-7da + wf_d1abb4a9-5c5 journals — read Lane A entries first), and THE DECOMPOSITION WAVE burn-down obligation. (3) Commit the verifier's factionPairOf live-defect fix + its walker/test fixes as their own small pathspec commit if still uncommitted. Return {lane:'A land', commits, gates, stopReports, notes}.`,
    {label:'laneA:land', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify: sizeBaseline test green AND baseline entries exactly match measured sizes (re-measure 3 files yourself — a padded freeze is a smuggled allowance); the WR-7b commit landed pathspec-clean with the attribution numbers in the body; the pulse reachability suite green post-commit; no unrelated file swept (git show --stat vs the lane list). Return {lane:'A verify', verdict, findings}.`,
      {label:'laneA:verify4', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE B — TWO RECORD FIXES then THE LEGACY RETROFIT. FIRST (small commits): (1) correct MG-4's recorded twin-census numbers in tests/lib/instantWorld/mundaneRealmAcceptance.test.js's docstring AND the queue/design record to the figures the harness actually produces (the verifier measured: mundane {5,134,22,193,24} vs magical {5,133,24,189,25} — re-derive yourself, record what YOU measure); (2) close the file-scoped allowlist gap in tests/lint/arcaneClassifierCensus.walker.test.js so the census can detect L10-L12 regrowth inside its four protected files (+ fix its two no-useless-assignment lint errors). THEN THE LEGACY RETROFIT (SOL_QUEUE item 24's first wiring slice; the owner PRE-APPROVED the disclosed prose shift and the recorded ruling is the spine's frequency-floor amendment): wire docs/content/RECEIPT_POOLS_LEGACY.md pools into the live token surfaces — per kind: the pool's variant 1 IS the live string (verify byte-equal; STOP on any drift), seeded per-entity selection over the widened pool (the WAR_RECEIPTS shape), goldens: capture affected fixtures BEFORE wiring, quote the per-kind golden plan in the commit body (prose-only shift, same-seed layout stable). Start with ONE coherent desk (e.g. the population/demographics kinds) as this cycle's slice — wire it end to end with its repetition-envelope pin rather than shallow-wiring everything. Return {lane:'B records+legacy', commits, gates, stopReports, notes}.`,
    {label:'laneB:legacy', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify the MG-4 numbers by running the harness yourself (the record must match the machine); the census walker now fires on a seeded regrowth inside a protected file (mutant control); the legacy slice: variant-1 byte-equality proven, per-entity seeding deterministic (same seed twice), the golden plan in the commit body matches the actual diff, no non-slice kind touched. Return {lane:'B verify', verdict, findings}.`,
      {label:'laneB:verify4', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE C — CLOSE THE VEIL HOLE + GATE-CLEAN. (1) THE VEIL HOLE (HIGH, live): the raw DB name outruns the veil — fix at the READ CHOKEPOINT per your own one-resolver law: gallery.js's row.name (and every hoisted raw-name sibling the verifier's probe named — read its Lane C findings in the wf_d1abb4a9-5c5 journal) must pass through the same veil before rendering/export; re-run the verifier's probe (buildWorldExport player variant with a vector term) and prove the payload carries ZERO plain occurrences. (2) THE RATCHET ROWS: clear the four tests/lint ratchets carrying lane-C rows incl. errorCopyBaseline (fix the copy or register per each ratchet's own discipline — never widen a baseline). (3) THE SEARCH_PATH FIX (chair-ruled under the owner's pre-approval): migration 195's update_display_name gains 'public, pg_temp' matching every sibling definer, AND tests/lint/migrationSearchPathPin.test.js gains the missing repin coverage so the class cannot recur. Pathspec commits per fix. Return {lane:'C veil+gates', commits, gates, stopReports, notes}.`,
    {label:'laneC:veil', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Re-run the veil probe yourself with TWO different vector terms (zero plain occurrences in the full serialized payload); all four ratchets green at the lane's tip; the search_path pin test now fails if a bare form is reintroduced (mutant control); commits pathspec-clean. Return {lane:'C verify', verdict, findings}.`,
      {label:'laneC:verify4', phase:'Build', model:'opus', effort:'high', schema:V})),
])
return { lanes: lanes.filter(Boolean) }