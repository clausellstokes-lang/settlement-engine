export const meta = {
  name: 'typecheck-burn-corrected',
  description: 'Burn the typecheck red with the verifier corrections binding: the idiom sweep with 9 carve-outs, then the three live defects TCD-1/2/3, writes serialized, verification fanned out.',
  phases: [
    { title: 'Sweep', detail: 'the idiom cure, 9 sites carved out, set-diff reported' },
    { title: 'Defects', detail: 'TCD-1, TCD-2, TCD-3 — serialized writers' },
    { title: 'Verify', detail: 'parallel adversarial lenses, read-only' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const LAWS = `VERIFY-FIRST PREAMBLE: chair brief, owner-delegated build program. LIVE CODE OUTRANKS EVERY TABLE. Build tree ${TREE} (branch claude/composite-r4). Hard-gate every state-mutating compound with the \`cd\` FIRST: \`cd ${TREE} && [ "$(git branch --show-current)" = "claude/composite-r4" ] || exit 1\`.

⭐ **THE MUTEX WAIT AND THE RUN ARE ONE \`&&\` CHAIN** — \`sh scripts/gate-mutex.sh --wait && sh scripts/gate-tail.sh npx vitest run <paths>\`. Issued as two statements, a wait TIMEOUT lets vitest start anyway. ⛔ Never read an exit code through a pipe. ⛔ Never hand-spell the ps-grep.
⚠ **EVERY FIGURE IS TAKEN IN AN INTEGRITY-COUNTED \`git archive\` OF A COMMITTED SHA**, built ONE AT A TIME with \`git archive -o f.tar <sha>\` + \`tar -xf\` (never through a pipe), \`rm -f\` the node_modules SYMLINK before removing any tree. Never run tsc against the shared tree.

⛔⛔ **A SECOND BUILD LANE — THE EP/HB/WC FOLD — IS LIVE RIGHT NOW AND HAS UNCOMMITTED WORK IN THIS TREE.** It has THREE UNTRACKED FILES: \`docs/DESIGN_FP_ARCH_EP.md\`, \`docs/DESIGN_FP_ARCH_HB.md\`, \`docs/DESIGN_FP_ARCH_WC.md\`, plus edits to \`docs/DESIGN_FP_ARCHITECTURE.md\`, \`docs/SOL_QUEUE.md\`, \`docs/FABLE_VALIDATION_QUEUE.md\` and \`tests/domain/couplingRegistry.test.js\`. **DO NOT TOUCH, STAGE, CLEAN, STASH OR OPEN ANY OF THOSE. Foreign WIP is the owner's.** \`git add\` EXPLICIT PATHSPECS ONLY, and before every commit verify \`git diff --cached --name-only\` against your EXPECTED set BY NAME AND COUNT — a \`git add\` aborts ATOMICALLY on one bad pathspec, so a commit can claim three parts and land one.

⚠ **RECEIPT LANGUAGE IS PART OF THE WORK.** Seven false receipts have landed here in two days and the code was nearly always fine — the defect was in the receipt. Two rules, both learned from the census this brief supersedes: (1) **never call an edit "comment-only" unless the comment-stripped executable text is byte-identical** — verify it, don't assert it; (2) **never report a net.** Report the SET DIFF: N removed, M introduced, and name the M.

REPORTING: CONFIRMED (executed evidence quoted) or PLAUSIBLE.`

const CENSUS = `
THE CENSUS, AS CORRECTED BY ITS ADVERSARIAL VERIFIER (all figures independently re-executed and reproduced EXACTLY unless flagged):
- 351 errors / 45 files at HEAD \`eca65c8a\`; 247 TS2339 (70.4%); top five = 154 (43.9%): envoyErrand.js 39, envoyErrandEncounterWriter.js 34, envoyInterceptionStage.js 31, armyTransitKernel.js 29, peaceTerms.js 21.
- **42 of 45 erroring files are \`src/domain/worldPulse/*\`** (verifier's corrected count, not the census's 43) — the red is ONE SUBSYSTEM.
- Accumulation: 14 errors at the first red commit \`7796954e\` → 351 at HEAD across 366 commits ≈ 0.92/commit, invisible because the gate stopped reporting when it stopped passing.
- ⭐ **ROOT CAUSE of 154 of the 351:** \`export function f({ a, b, c } = {})\`. With a \`= {}\` default and no \`@param\`, tsc types the parameter FROM THE DEFAULTS ONLY — properties with a default get typed, properties without one are absent, so every non-defaulted binding errors. Confirmed by minimal repro: the same signature WITHOUT \`= {}\` produces zero errors. This is also the caller-side root of the 29 TS2353 and the 12 TS2739/2740/2741.
- ⚠ **sizeBaseline DOES NOT CONSTRAIN THIS BURN** (measured): JSDoc annotation is FREE for \`.js\` files — the recorded "comments count" hazard is about \`.jsx\`, where \`{/* */}\` is a JSX expression container espree counts as code. Only 4 of the 45 files are in \`scripts/.size-baseline.json\` at all (applyWorldPulse 941, pulseKernel 1580, settlementStrategy 812, warTermination 818). ⚠ **The BELOW-DEMANDS-RATCHET-DOWN rule still fires if a file SHRINKS — do not delete code while annotating.**
- Honest floor: 184 cheap / 81 comment-only-but-needs-reading / 86 real thought, of which 11 error-sites are the live defects below.
- ⚠ Scope caveat: \`tsconfig.full.json\` EXCLUDES all JSX and its own header estimates ~650 further pre-existing errors would surface if components were included. **351 is the non-JSX logic tree only — never call it "the whole type health".**`

phase('Sweep')
const sweep = await agent(LAWS + CENSUS + `

YOU ARE THE IDIOM SWEEP. Land the mechanical cure for the \`= {}\` root cause.

**THE CURE, AND ITS TWO CORRECTIONS.** The census proposed \`} = {})\` → \`} = /** @type {Record<string, any>} */ ({}))\` and measured 351→197. Its adversarial verifier reproduced the yield and found two defects you MUST NOT repeat:
1. ⛔ **IT IS NOT "COMMENT-ONLY" — IT ADDS PARENTHESES.** Comment-stripped executable text differs in 196/196 files. The parens ARE semantically inert (verified: identical runtime return, zero line delta), so the edit is fine — **but you must label it "annotation + inert parenthesization" and PROVE the inertness** (load a cured module in node and show an identical return), never claim comment-only.
2. ⛔ **THE YIELD IS 163 REMOVED / 9 INTRODUCED, NET 154.** The blanket cast INTRODUCES 9 new errors. They land only in already-red files (0 previously-green file goes red), but they are real. **Find those 9 sites and give them a REAL type instead of the blanket cast.**

**PREFER AN EXISTING TYPEDEF OVER THE BLANKET CAST, EVERYWHERE YOU CAN.** \`Record<string, any>\` silences a signature rather than typing it, and this repo's TYPE SURFACE IS PUBLIC. Most of these functions take recurring shapes (\`{worldState, tick, rng}\` and friends) that **already have typedefs in the worldPulse tree** — find them and use them. ⚠ **DERIVE, DO NOT RESTATE**: reference or derive the existing typedef; never hand-copy a shape into a second place, because hand-restated derivables go stale and green the walkers that should catch it.
Fall back to the blanket cast ONLY where no typedef exists, and **report the count of blanket casts as a debt figure** — it is the honest cost of this commit.

**SCOPE.** Touch only the 45 erroring files, not the 196 that merely carry the idiom. ⚠ The census said 186 idiom files and the verifier measured 196 — **re-measure yourself and report the number you measured**; do not inherit either.

**PROVE IT** in a clean archive at your committed sha: the full error SET before and after (not counts — the set), the removed set, the introduced set, and the file-level table. Run the fast gate on every test touching the worldPulse tree through the mutex chain. Commit body carries \`⏳ OPUS-ERA — FABLE SURVEY OWED\` and \`Co-Authored-By: Claude Opus 5\`.

RETURN JSON.`, { label: 'burn:idiom-sweep', phase: 'Sweep', model: 'opus', effort: 'high', schema: { type: 'object', required: ['errorsBefore', 'errorsAfter'], properties: { errorsBefore: { type: 'number' }, errorsAfter: { type: 'number' }, removedCount: { type: 'number' }, introducedCount: { type: 'number' }, introducedSites: { type: 'array', items: { type: 'string' } }, idiomFilesMeasured: { type: 'number' }, typedefsReused: { type: 'number' }, blanketCastDebt: { type: 'number' }, inertnessProof: { type: 'string' }, filesTouched: { type: 'array', items: { type: 'string' } }, gates: { type: 'array', items: { type: 'string' } }, commits: { type: 'array', items: { type: 'string' } }, stopReports: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Defects')
const DEFECTS = [
  {
    id: 'TCD-1', severity: 'HIGH',
    brief: `**A READER ASKS FOR \`.id\` ON A FACTION THAT NEVER CARRIES ONE — 9 SITES.**
Sites: \`src/domain/rulingPower.js:225\` (×2), \`src/domain/worldPulse/warSeatBooks.js:364\` (×3) and \`:401\` (×3), \`src/domain/worldPulse/applyWorldPulse.js:961\`.
PROVEN ABSENT by EXECUTING the real generator (two independent probes, different seed sets, both invariant): powerStructure faction rows carry \`faction, power, desc, category, rawPower, powerLabel\` (+ situational \`modifier, isGoverning, modifiers, legitimacyCrisis, crisisNote\`) and **ZERO rows carry \`.id\`**. The \`RulingFaction\` typedef is CORRECT to omit it; **the readers are wrong.** \`src/domain/worldPulse/religionLegitimacy.js:249\` ALREADY documents this exact class ("no generated record carries .id") — these 9 sites were simply never swept.
⭐ **THE CONSEQUENCE IS A NEWS ADDRESS LAW BREACH, WHICH IS OWNER LAW.** No crash — every site is defensively guarded, so it degrades silently. \`warSeatBooks.js:401\` emits \`...(factionId ? {factionId} : {})\`, so the war-seat books record NEVER carries factionId on generated data; \`warTermination.js:880\` therefore never fires its own spread; \`warRulingsNews.js:270-337\` falls back to \`installerFactionId || governingFactionId\` and, where neither exists, **omits \`factionIds\` from the news entry entirely — an address-chain gap in the law that says every entry carries the full address chain.** \`warSeatBooks.js:365\` also folds a permanently-null factionId into the authority signature, weakening its discrimination. \`applyWorldPulse.js:961\` masks the defect by substituting \`installerFactionId\`, which is **silently WRONG whenever installer ≠ governing.**
**THE FIX IS A REAL ARCHITECTURAL CHOICE — MAKE IT AND RECORD IT VETOABLY.** Either resolve a genuine faction id at these sites (via the same resolution \`religionLegitimacy.js\` uses — read it first), or establish that no id exists and remove the dead reads plus the misleading fallback. ⚠ Adding a NEW PERSISTED FIELD to the powerStructure record is a persistence-shape change and is **OWNER-GATED — STOP and report it rather than landing it.** Resolving an id that already exists elsewhere is repair and is yours.`
  },
  {
    id: 'TCD-2', severity: 'MEDIUM-HIGH',
    brief: `**A COURT CAN NEVER LEARN ITS OWN EXPORTS — A STRUCTURALLY DEAD ARM.**
Site: \`src/domain/worldPulse/envoyNegotiationPictureBuilder.js:139-142\`, function \`ownExportsOf\`.
It probes \`row.exports\` across three containers — \`settlement.economicState\`, \`settlement.economy\`, \`settlement.trade\`. PROVEN by executing the generator (two independent seed sets, invariant): **\`economy\` and \`trade\` do not exist at all, and 0/12 settlements carry \`economicState.exports\`.** So \`containers.find(row => Array.isArray(row.exports))\` returns undefined on **100% of generated settlements**, \`ownExportsOf\` always returns \`{exportKnowledge:'unknown', exports:[]}\`, and **the \`'known'\` arm at line 145 is structurally unreachable in production** — every negotiation picture says "unknown".
**TWO LAYERS OF DRIFT, AND FIXING ONE IS NOT ENOUGH.** The real homes are \`settlement.resourceAnalysis.exports\` (an array of \`{product, chain, value, reason}\`) and \`settlement.economicState.primaryExports\`. ⚠⚠ **Even pointed at the right container it would still yield \`[]\`**, because the row handler reads \`row.name || row.good || row.resource || row.label\` and those rows key on **\`product\`**. Fix both layers or you will ship a green fix that still returns empty.
⚠ This is ADJACENT TO BUT DISTINCT FROM the parked \`STOP-ES3-1\` ("the exports leg has no belief slot") — that is a missing belief slot; **this is the reader never finding data at all.** Do not treat the parked item as covering this.
⚠ **MAKING A DEAD ARM REACHABLE MOVES OUTPUT.** Measure the golden shift over the 525-settlement corpus and DECLARE it with its legitimate cause. Tuning is the LAST act of the endgame and has not happened, so a declared shift is allowed now — a silent one never is.`
  },
  {
    id: 'TCD-3', severity: 'MEDIUM',
    brief: `**A "LEGACY COMPATIBILITY" FALLBACK IS THE ONLY PATH.**
Sites: \`src/domain/worldPulse/lineageMemberBirth.js:178\` — \`foundingTier: satellite.foundingTier || 'thorp'\` — and a second read at \`lineageClaim.js:156\`.
\`foundingTier\` has **5 non-test hits total** (verifier's corrected count, not the census's 3): these two reads, an \`@property\` line in \`settlement.schema.js\`, and the rest. **No writer exists.** The \`SatelliteRecord\` typedef (\`satellitesLedger.js:53-82\`) has no such field, and the satellite mint (\`settlementLifecycleKernel.js:446-462\`) writes \`tier: 'thorp'\` and nothing else. The comment calling \`|| 'thorp'\` a legacy path for "older satellite records" is therefore **false — it is the only path**, and a steading is normally PROMOTED to hamlet before chartering, so the constant is also probably the wrong value.
⛔ **DO NOT ADD \`foundingTier\` TO THE SATELLITE MINT** — that is a persisted-shape change and is OWNER-GATED. **STOP and report that option.**
**WHAT IS YOURS:** make the code honest. Remove the dead read and the false comment, or replace the fallback with the value actually available at the mint, whichever is truthful — and say plainly which you did and whether it moves any output.`
  },
]

// WRITES ARE SERIALIZED: the concurrency law allows ONE landing slot, and the fold lane holds the other build lane.
const defectResults = []
for (let i = 0; i < DEFECTS.length; i++) {
  const d = DEFECTS[i]
  const r = await agent(LAWS + CENSUS + `

YOU ARE THE ${d.id} REPAIR LANE (severity ${d.severity}). The idiom sweep already landed: ` + JSON.stringify(sweep || {}).slice(0, 3000) + `
${i > 0 ? 'Prior defect lanes in this run landed: ' + JSON.stringify(defectResults.filter(Boolean).map(x => ({ id: x.id, commits: x.commits }))).slice(0, 1500) : ''}

${d.brief}

**HOW TO WORK.** Read the enclosing scope and the direct callers in full before editing — never edit from a grep window. Trace the state lifecycle: create, read, persist, regenerate, undo. Then fix the CLASS, not the instance: if the same reader-without-a-writer shape appears at sibling sites, sweep them and report the denominator ("found N sites, changed M, K exempt because…").
**ADD A REGRESSION PIN THAT ACTUALLY REDS.** Plant the old broken spelling back and watch the pin fail — a pin that stays green under its own mutant is not a pin. ⚠ Anchor on the WRITER's real output, never on a filename and never on a single seed.
**MEASURE THE GOLDEN SHIFT** over the 525-settlement corpus if your fix can move output, and declare it with its cause. ⛔ If the fix would touch a paid surface, a persisted schema, or anything the owner has parked, STOP and report it as owner-gated instead of landing it.

Gate through the mutex chain. Explicit pathspecs; verify the staged set by name AND count. Commit body carries \`⏳ OPUS-ERA — FABLE SURVEY OWED\`.

RETURN JSON.`, { label: `burn:${d.id}`, phase: 'Defects', model: 'opus', effort: 'high', schema: { type: 'object', required: ['id', 'landed'], properties: { id: { type: 'string' }, landed: { type: 'boolean' }, whatChanged: { type: 'string' }, siteDenominator: { type: 'string' }, goldenShift: { type: 'string' }, pinProof: { type: 'string' }, ownerGated: { type: 'array', items: { type: 'string' } }, errorsRemoved: { type: 'number' }, gates: { type: 'array', items: { type: 'string' } }, commits: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })
  defectResults.push(r)
  log(`${d.id}: ${r && r.landed ? 'landed ' + (r.commits || []).join(',') : 'NOT landed'}`)
}

phase('Verify')
const LENSES = [
  { k: 'receipt', p: 'THE RECEIPT LENS. Re-run EVERY gate and EVERY count quoted, at the committed shas, in integrity-counted archives. Check specifically for the four shapes that have produced false receipts here: a figure measured in the live shared tree; a figure measured on a tree the lane later abandoned; an exit code read through a pipe; a "before" and "after" taken at different shas. **Verify the SET DIFF of typecheck errors, never the net** — confirm the introduced set is named and non-hidden. Confirm no edit claimed "comment-only" unless comment-stripped text is byte-identical.' },
  { k: 'correctness', p: 'THE CORRECTNESS LENS. For each defect fix, independently re-prove the writer\'s actual spelling by EXECUTING the real generator over your OWN seed set (not the lane\'s). Confirm the fix reaches the previously-dead path. **Plant the old broken spelling back and confirm the pin REDS** — verify the mutant, do not trust the claim. For TCD-2 confirm BOTH drift layers were fixed (container AND the `product` key) — a fix of one layer still returns [].' },
  { k: 'blast-radius', p: 'THE BLAST-RADIUS LENS. Diff the FULL typecheck error set across the whole burn — per-file drops that do not sum into a total drop mean a lane pushed errors into a neighbour. Verify the golden claims by measuring, not by reading. Check sizeBaseline, the domain-layer ratchets, and ratchet halves via `sh scripts/ratchet-inventory.sh` (a red ratchet\'s INVENTORY grows behind a byte-identical failing row). **Confirm the concurrent fold lane\'s files were never touched**: docs/DESIGN_FP_ARCH_{EP,HB,WC}.md, DESIGN_FP_ARCHITECTURE.md, SOL_QUEUE.md, FABLE_VALIDATION_QUEUE.md, couplingRegistry.test.js — any of those in a burn commit is a REJECT.' },
]

const verdicts = await parallel(LENSES.map(l => () => agent(LAWS + CENSUS + `

YOU ARE AN ADVERSARIAL VERIFIER — ${l.k.toUpperCase()}. Default posture: WRONG until executed evidence. Try to REFUTE. **YOU ARE READ-ONLY: do not edit, stage or commit anything.**

Sweep reported: ` + JSON.stringify(sweep || {}).slice(0, 6000) + `
Defect lanes reported: ` + JSON.stringify(defectResults.filter(Boolean)).slice(0, 10000) + `

${l.p}

Archives ONE AT A TIME, integrity-counted, deleted between (disk is tight). RETURN structured JSON only.`, { label: `verify:${l.k}`, phase: 'Verify', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })))

const live = verdicts.filter(Boolean)
const rejects = live.filter(v => v.verdict === 'REJECT')
log(`verify: ${live.length}/3 returned, ${rejects.length} REJECT`)
return { sweep, defectResults, verdicts: live, verdict: rejects.length ? 'REJECT' : 'PASS' }