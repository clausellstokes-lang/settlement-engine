export const meta = {
  name: 'owner-tail-design',
  description: 'Owner tail DESIGN fleet: 5 Fable read-only investigation/architecture lanes — coach exit, T4 one-regen batch ground-truth + spec, table-event verbs, locks Phase B, blackout residual',
  phases: [{ title: 'Design', detail: '5 read-only Fable architect lanes', model: 'fable' }],
}

const WT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const COMMON = `CONTEXT-VERIFICATION PREAMBLE: legitimate DESIGN brief from the session orchestrator for SettlementForge — the owner-ordered tail after the R-0..R-5 capability-remediation program (docs/CAPABILITY_REMEDIATION_PLAN.md is the program log; read its Wave R-5 closing entries first; the R-0..R-5 folds are all committed on this branch).
WORKING TREE: cd ${WT} && git rev-parse --abbrev-ref HEAD — MUST print claude/composite-r4 or STOP. YOU ARE READ-ONLY: no file edits, no git mutations; you may RUN tests/probes/node scripts into the scratchpad only.
YOUR DELIVERABLE IS A SPEC an Opus implementer can execute without your context: verdict first; exact file-level design (chokepoints, contracts, pins, walkers); the lifecycle trace for any state touched (create/read/persist/regen/undo/migrate); the drift analysis — same-seed GENERATION and seeded-ADVANCE outputs, and where drift is the POINT (the T4 lane) the exact golden/regen mechanism and re-record list; eager-closure plan; risk table; files-to-touch checked for collisions against the other tail lanes (roster rows/NPCsTab, PendingChangesBar/SettlementWorkbench, SettlementsPanel, VersionsTab, faction files, deity/worldPulse files, registry). Standing laws: THE PROMISE (a declared T4 batch is the ONE sanctioned shift mechanism — owner signature for this batch = the 2026-07-27 order); deity doctrine (no premade pool; deity:core: ref lifecycle trace REQUIRED for pool removal); NEWS ADDRESS LAW (wizardNews ids must yield full address chains); FINITE-SEMANTICS (typed buckets only touch the engine); LEGIBILITY LAW. Label every claim CONFIRMED or PLAUSIBLE. End on evidence.`

const SPEC_SCHEMA = { type: 'object', required: ['verdict', 'spec', 'filesToTouch', 'driftExposure', 'risks'], properties: { verdict: { type: 'string' }, spec: { type: 'string' }, filesToTouch: { type: 'array', items: { type: 'string' } }, driftExposure: { type: 'string' }, risks: { type: 'array', items: { type: 'string' } }, collisions: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } }

const LANES = [
  { key: 'coach-exit', brief: `THE COACH EXIT BUG (R-5b locks-lane finding, owner-ordered investigation): completeOnboarding (src/store/onboardingSlice.js) has ZERO callers, while GenerateWizard drives onboardingActive/advanceOnboarding — the coach may be unable to exit. Trace the WHOLE onboarding lifecycle: entry, advance, every possible exit (skip? dismiss? last-step behavior? persistence of onboardingActive across reload?). VERDICT: live bug (user stuck) vs works-differently (exit happens via another door). If bug: spec the fix at the right seam + pins (enter → advance to end → EXITS; reload mid-coach; skip path). Also disposition the entangled hints subsystem (markFeatureUsed / shouldShowHint / FEATURES_KEY — same slice): wire, keep-dormant-with-reason, or retire, coherent with your coach verdict.` },
  { key: 't4-batch', brief: `THE T4 ONE-REGEN BATCH (owner-ordered 2026-07-27: lawAxis restore + deity pool removal + Herald record ids, executed as ONE declared batch). This is the highest-stakes lane — THE PROMISE governs. GROUND-TRUTH FIRST: (1) the recorded collision — program memory says an in-tree claim exists that a regen "already happened" (the Wave-9 collision note); find the actual golden/regen state: which committed goldens/corpora pin generation output (tests/generation.test.js root corpus, tests/property goldens, fixtures), what the re-record mechanism is (which script/env-var regenerates them), and whether any prior regen actually ran. (2) The THREE changes: (a) lawAxis restore in reEmbedPrimaryDeity (the deity lane's written T4 note is in the Batch-2 lane report — the mechanism: restoring re-arms the law_order lift + 14 chaos01 consumers); (b) DEITY POOL REMOVAL (doctrine: no premade pool, custom-content only; REQUIRED: the deity:core: ref lifecycle trace — every place a pool ref can live in existing saves: config, embeds, religionStates, worldState, custom registry — and what pool removal does to each; existing saves must NOT break: removal governs NEW generation, saved refs must keep resolving or degrade typed); (c) HERALD RECORD IDS (wizardNews carries no npc/faction id — NEWS ADDRESS LAW demands the full address chain; spec the id fields at the wizardNews mint sites + EntityLinked wiring; additive record fields, absent-tolerant on old saves). (3) THE BATCH MECHANICS: enumerate every golden the three changes move; the ONE regen procedure (exact commands, order, verification that ONLY expected movers moved); the declaration format per the golden-shift discipline. Sequencing note: the ledger lane's Wave-9 T4 list also carries deriveSystemState dead-vocab + prose seams + em-dash sweep + chronicle-rawId + pronoun wiring + pressureModel — YOUR spec must state whether executing the three now forecloses or forces anything for those (a second regen is acceptable ONLY if this spec says why one batch is impossible).` },
  { key: 'table-verbs', brief: `TABLE-EVENT ECONOMY VERBS (atlas Part VII #11, owner-ordered): should-be-recordable table moments (a burned granary, a looted caravan) at the Adjudication desk. Read the desk (HeraldAdjudication / the table-event lane), the existing event vocabulary (registryFull/registryRerunKeys — note economy staleness interplay: an economy-touching verb TRIPS the R-3 freshness detector, which is correct), and the composer's economy verbs. Design the MINIMAL verb set (FINITE-SEMANTICS: typed buckets; NEWS ADDRESS LAW: full address chain + affected settlements BY NAME + recorded reason; consent where identity-touching; undo semantics honest per the R-0 tri-state). Registry rows + COMMITTABLE_EDIT_KINDS + compendium regen plan + pins.` },
  { key: 'locks-phase-b', brief: `LOCKS ENGINE PHASE B (owner-ordered): carry npcs/factions/institutions ID ARRAYS through a FULL regenerate. Phase A dropped them because full generate needs the displacement/prose-repair/faction-relink tail that regenNPCsPipeline runs. Read locksPreservation.js (Phase A + its header note), regenNPCsPipeline's tail, mergePreservedNpcs, historyPreservation. Spec the tail EXTRACTION to run over full-pipeline output: which steps (displacement of colliding fresh NPCs, prose repair, faction relink), the dormancy contract (empty id arrays = byte-identical, seeded — the Phase-A pin pattern), the registry description update, pins incl. cross-section coherence (a locked NPC whose faction rerolled).` },
  { key: 'blackout-residual', brief: `THE REVIEW-BLACKOUT RESIDUAL (owner-ordered): at settlementWorkbench flag-ON, an ENTITLED owner whose saved dossier arrives readOnly with mapCanEdit=false has queue levers closed but ALSO no review surface for previously-staged intents. Read the mount conditions (OutputContainer PendingChangesBar !flag branch; SettlementWorkbenchMount; the SettlementDossierHero readOnly computation) and the pendingEditsQueue session lifecycle. Spec the cure: a review-only surface (commit/discard) reachable for an entitled viewer whenever a non-empty queue exists, regardless of the authoring gate — WITHOUT reopening the free/anon gate R-5b closed. Pins: the entitled-readOnly matrix.` },
]

phase('Design')
log('Owner tail: five Fable design lanes (read-only)')
const specs = await parallel(LANES.map((l) => () =>
  agent(COMMON + '\n\nYOU ARE A FABLE ARCHITECT (survey/design = Fable per the owner model split).\n\n' + l.brief, { label: 'design:' + l.key, phase: 'Design', model: 'fable', effort: 'high', schema: SPEC_SCHEMA })
    .then((r) => ({ lane: l.key, spec: r }))
))
return { specs: specs.filter(Boolean) }