export const meta = {
  name: 'spatial-ledger-classification',
  description: 'Independently classify commercialReasons + pactProposals against the spatialUsage manifest, then refute the proposed call',
  phases: [
    { title: 'Classify', detail: 'three independent lenses on the two keys' },
    { title: 'Refute', detail: 'adversarial attack on the proposed classification' },
    { title: 'Census', detail: 'consumers of extractSpatialUsage emitted shape' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const CONTEXT = `
You are working in the git worktree ${TREE} (branch claude/composite-r4, HEAD 1e4c493b).
This is the SettlementForge deterministic world-simulation engine. READ FILES, do not guess.

THE PROBLEM. tests/lib/spatialLedgerCoverage.walker.test.js is RED. The walker source-scans
every \`setSpatialLedger(worldState, <key>, ...)\` call site under src/domain and asserts the
written-key set EQUALS TRACKED_LEDGER_KEYS union Object.keys(EXEMPT_LEDGER_KEYS) in
src/lib/spatialUsage.js. Two written keys are unclassified:
  - commercialReasons  (written by src/domain/worldPulse/commercialReasons.js)
  - pactProposals      (written by src/domain/worldPulse/pactProposals.js)

Each must be classified either TRACKED (surfaced as an adoption "mover" in
extractSpatialUsage's mover_counts + MOVER_PRESENCE) or EXEMPT (a documented non-mover,
with the reason recorded in the EXEMPT_LEDGER_KEYS entry).

FILES YOU MUST READ IN FULL BEFORE ANSWERING:
  src/lib/spatialUsage.js                        (the manifest + its stated criteria + ~35 precedent entries)
  src/domain/worldPulse/commercialReasons.js     (writer 1)
  src/domain/worldPulse/pactProposals.js         (writer 2)
  tests/lib/spatialLedgerCoverage.walker.test.js (the walker)

THE MANIFEST'S OWN STATED CRITERIA (read them in the file, do not take my word):
 - EXEMPT_LEDGER_KEYS doc header: "substrate / reason-annotation / shared-bookkeeping ledgers
   whose owning layer's adoption is already visible through a tracked mover or a tracked flag;
   a separate presence signal would be redundant noise, not new information."
 - The routeNetwork TRACKED note inverts it: "the exemption list is for ledgers whose adoption
   is ALREADY visible through a tracked mover or a tracked flag, and the route network's is
   visible through neither ... A reading of zero while the layer is dark is the truth, not a
   blind spot." demographicPlans is TRACKED on the same argument.
 - Several EXEMPT entries OVERRIDE that invisible-lane argument with stronger reasons, and you
   must weigh them: prop-hygiene (npcLedger, foreignGuestHolds — the records are PEOPLE, and
   this module may never emit an npc id or name); artefact-of-rarity + the-key-drains
   (magicBuffer, vengeanceLicenses — a count would measure the realm's recent luck);
   not-written-by-a-mover-at-all (npcRulings — deposited only when a human presses a button);
   instruction-in-flight (warIntents); typed reason ANNOTATIONS (warReasons, peaceReasons,
   reframes); state STOCK not mover (magicRegime, merchantAppetite).

FACTS I HAVE ALREADY MEASURED AT THIS HEAD (verify them yourself; correct me if wrong):
 F1. commercialReasons' writer \`advanceCommercialReasons\` HAS NO CALLER IN src/. The ONLY
     cross-module import of commercialReasons.js in src/ is tradeWar.js line 69 importing
     \`makeCommercialPressureRead\`, which READS the ledger at the T9 escalation seam. The
     lane's own certification row (src/domain/certification/subsystemRowsVirtual.js ~line 789)
     states the same thing and calls the lane "A DARK INSTRUMENT WITH NO MOUNT".
 F2. pactProposals IS mounted on the live pulse:
     pulseKernel.js:76 imports advanceSettlementLifecycle from settlementLifecycleKernel.js,
     which at line 559 calls advancePeacetimePacts (pactFormation.js), which calls
     openPactProposal / settlePactProposal / prunePactProposals -> writePactProposals ->
     setSpatialLedger('pactProposals', ...).
 F3. Both gate flags are VIRTUAL: 'casusCommerciiEnabled' and 'pactFormationEnabled' are both
     in ENGINE_GATED_VIRTUAL_RULE_KEYS (src/domain/worldPulse/simulationRules.js ~203, ~241),
     absent from DEFAULT_SIMULATION_RULES and from every preset spread. NEITHER is in
     spatialUsage.js's TRACKED_FLAGS list.
 F4. pactProposals' ledger value is an ARRAY of row objects (writePactProposals sorts and
     setSpatialLedger's an array), unlike every other ledger which is a record keyed by id.
     spatialUsage's recCount() does Object.keys(x).length, which on an array yields its length.
 F5. pactProposals rows carry \`transport\` ('envoy' when SP-D's errand spine is lit — a named
     person on a diplomatic errand — else 'abstract') and an \`answerDueTick\` priced off
     hopWeeks: two road legs plus a deliberation. Settled rows PRUNE; the key drops when empty.
`

const SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['commercialReasons', 'pactProposals', 'factCorrections', 'confidence'],
  properties: {
    commercialReasons: {
      type: 'object', additionalProperties: false,
      required: ['classification', 'reasoning', 'strongestCounterargument'],
      properties: {
        classification: { type: 'string', enum: ['TRACKED', 'EXEMPT'] },
        reasoning: { type: 'string', description: 'Which manifest precedent(s) it matches and why, citing entries by name.' },
        strongestCounterargument: { type: 'string', description: 'The best case AGAINST your own answer.' },
      },
    },
    pactProposals: {
      type: 'object', additionalProperties: false,
      required: ['classification', 'reasoning', 'strongestCounterargument'],
      properties: {
        classification: { type: 'string', enum: ['TRACKED', 'EXEMPT'] },
        reasoning: { type: 'string' },
        strongestCounterargument: { type: 'string' },
      },
    },
    factCorrections: { type: 'string', description: 'Any of F1..F5 you found to be WRONG, with the evidence. Write "none" if all verified.' },
    confidence: { type: 'string', enum: ['high', 'medium', 'low'] },
  },
}

const LENSES = [
  {
    key: 'precedent',
    prompt: `LENS: MANIFEST PRECEDENT. Classify strictly by pattern-matching each key against the ~35
existing entries. For each key, name the two or three closest existing entries (TRACKED or EXEMPT)
and argue which one it is a sibling of. Treat the manifest's own stated criteria as binding law and
do not invent a new criterion. Pay particular attention to whether commercialReasons is the commerce
twin of warReasons/peaceReasons (both EXEMPT as typed reason annotations), and whether pactProposals
is a sibling of warIntents (EXEMPT, instruction-in-flight) or of armyTransit/supplyShipments/
navalTransit/spatialArrivals (all TRACKED, things in transit across the map with an arrival time).`,
  },
  {
    key: 'signal-quality',
    prompt: `LENS: TELEMETRY SIGNAL QUALITY. Ignore precedent. Ask only: if this key's recCount()
were emitted into mover_counts and movers_active, WHAT WOULD THE NUMBER ACTUALLY MEAN across real
campaigns? Would it be noise with a plausible shape (the magicBuffer failure mode)? Is it
structurally zero, or ambiguous between "never happened" and "happened and drained"? Does it
confound adoption with something else (realm size, road distance, tempo, luck)? Note carefully:
several ALREADY-TRACKED movers (armyTransit, supplyShipments, navalTransit) are in-transit counts
whose magnitude also scales with road distance and realm size — so decide whether that confound is
disqualifying here or is simply the accepted standard of this module. Also weigh the prop-hygiene
law in the file header (never emit a settlement or NPC id or name) — does a bare COUNT violate it?`,
  },
  {
    key: 'lifecycle',
    prompt: `LENS: STATE LIFECYCLE AND MOUNT. Trace, by reading code, the full lifecycle of each
ledger: who creates it, who reads it, when it is pruned/dropped, whether regeneration re-derives it,
and — critically — whether any live code path in src/ can populate it at all. Verify F1 and F2 by
your own greps; report if I am wrong. Then decide the classification primarily on what the lifecycle
proves. Consider explicitly: is a permanently-unreachable container better served by TRACKED (an
honest zero, the routeNetwork argument) or by EXEMPT (a zero that reads as "the layer is adopted at
zero" when the truth is "nothing ever ran")? The lane's own certification row made a closely related
call for its stateKeys channel — read subsystemRowsVirtual.js around line 780-800 and say whether
that reasoning transfers to this manifest or not.`,
  },
]

phase('Classify')
const opinions = await parallel(LENSES.map((lens) => () =>
  agent(`${CONTEXT}\n\n${lens.prompt}\n\nReturn your classification for BOTH keys.`, {
    label: `classify:${lens.key}`, phase: 'Classify', schema: SCHEMA,
  }).then((r) => (r ? { lens: lens.key, ...r } : null))
))
const votes = opinions.filter(Boolean)

const PROPOSAL = `
MY PROPOSED CLASSIFICATION (attack it):

(A) commercialReasons -> EXEMPT. Two reasons. PRIMARY (durable): it is commerce's warReasons —
by its own module header a set of typed severance/partnership grievance ANNOTATIONS "RECOMPUTED
EACH PULSE FROM EXISTING STATE", i.e. a re-reading of trade state the tracked movers entrepots /
trade_flow / caravans already measure, not a record of anything moving. warReasons, peaceReasons
and reframes are all EXEMPT on exactly this ground. A count of "pairs holding a grievance" is a
deterministic function of the entrepot toll ledger, the no-trade relationship predicate and pair
trade salience, so it carries no adoption information those movers do not already carry.
SECONDARY (measured, and it makes the call presently unarguable): the writer has no caller, so the
container is unreachable in every world; a TRACKED entry would put a structurally permanent zero
into mover_counts that reads as "adopted at zero" rather than "never ran".

(B) pactProposals -> TRACKED (add to TRACKED_LEDGER_KEYS, a mover_counts entry, and a
MOVER_PRESENCE row). Reasons: (1) it is literally an in-transit ledger — every row carries a
transport and a road-priced answerDueTick, the same shape as the four already-TRACKED transit
movers; (2) it is mounted and live, so a nonzero count is genuinely reachable; (3) its lane is
visible through NEITHER a tracked mover NOR a tracked flag (pactFormationEnabled is virtual and
not in TRACKED_FLAGS; no existing mover counts peacetime diplomacy — approvals_pending reads
worldState.proposals, the DM approval queue, a different container; treaties is the EXEMPT outcome
record), which is exactly the routeNetwork/demographicPlans criterion for TRACKED; (4) no override
applies — the rows are about SETTLEMENTS not people, so the npcLedger/foreignGuestHolds prop-hygiene
override explicitly does not reach it, and the population is not an artefact of rarity because
openings come from four ordinary peacetime triggers capped at MAX_OPEN_PROPOSALS=2 per proposer.
`

const REFUTE_SCHEMA = {
  type: 'object', additionalProperties: false,
  required: ['refuted', 'strongestObjection', 'evidence', 'wouldChangeTheCall'],
  properties: {
    refuted: { type: 'boolean', description: 'true if the proposed classification for YOUR assigned key is WRONG' },
    strongestObjection: { type: 'string' },
    evidence: { type: 'string', description: 'File paths and line numbers or quoted code/manifest text supporting the objection.' },
    wouldChangeTheCall: { type: 'string', enum: ['yes-flip-it', 'no-but-amend-the-reason', 'no-the-call-stands'] },
  },
}

phase('Refute')
const attacks = await parallel([
  ['commercialReasons', 'Attack (A). Default to refuted=true if you find ANY defensible reading under which commercialReasons should be TRACKED. In particular, press the routeNetwork/demographicPlans argument that an invisible lane belongs in TRACKED, and press whether "no mount" is a legitimate exemption ground at all or is instead an argument for TRACKED-with-an-honest-zero.'],
  ['commercialReasons', 'Attack (A) from the OPPOSITE direction: grant that it should be EXEMPT, but attack the stated REASON. Is the "commerce twin of warReasons" claim accurate given commercialReasons imports no war table and shares no code with warReasons.js (read the J-TR-2 note in its header)? Is the "no caller in src/" fact actually true at this HEAD — verify by your own exhaustive grep including dynamic imports, re-exports and barrel files? Would the exemption reason still hold the day the writer IS mounted?'],
  ['pactProposals', 'Attack (B). Default to refuted=true if you find ANY defensible reading under which pactProposals should be EXEMPT. Press hardest on: the warIntents precedent (an instruction in flight, EXEMPT, whose count reads high exactly where the lane is working badly); whether the count is confounded by road distance since answerDueTick is two hopWeeks legs; whether the v5 subsystems stateKeys census (subsystemRowsVirtual.js ~line 636 declares spatialLedgers.pactProposals) is already the honest instrument, making a mover count redundant noise; and whether rows naming from/to settlements plus an envoy transport raise any prop-hygiene concern.'],
  ['pactProposals', 'Attack (B) on MECHANICS rather than on the design call. Grant TRACKED. Now verify by reading code that the implementation would actually work and would not break anything: does recCount() read an ARRAY ledger correctly? Does adding a mover_counts key and a MOVER_PRESENCE row change any pinned test, golden, snapshot, type surface, size ratchet or emitted-prop contract? Search tests/ exhaustively for anything asserting the SHAPE or key set of extractSpatialUsage output, mover_counts, movers_active, or the spatial-usage telemetry props. Report every file that would need to change.'],
].map(([key, task], i) => () =>
  agent(`${CONTEXT}\n\n${PROPOSAL}\n\nYOUR ASSIGNED KEY: ${key}\n\n${task}`, {
    label: `refute:${key}:${i}`, phase: 'Refute', schema: REFUTE_SCHEMA,
  }).then((r) => (r ? { key, task: i, ...r } : null))
))

phase('Census')
const census = await agent(`${CONTEXT}

CONSUMER CENSUS. extractSpatialUsage(worldState) in src/lib/spatialUsage.js emits a telemetry
props object. I intend to add ONE key to \`counts\` (hence to mover_counts) and ONE row to
MOVER_PRESENCE (hence a possible new member of movers_active).

Establish the DENOMINATOR: find EVERY consumer of this function's output anywhere in the repo —
src/, tests/, scripts/, docs/, edge functions, analytics ingest, type declarations, any schema or
allow-list of telemetry prop names, any size/line ratchet covering src/lib/spatialUsage.js, and any
golden or snapshot that could contain the emitted shape. Search by concept as well as by name:
extractSpatialUsage, spatialUsage, mover_counts, movers_active, spatial_active, migration_pop_band,
sim_config, and the analytics event name the props ride on.

For EACH site report: path, what it does with the output, and whether adding a key BREAKS it,
REQUIRES an update, or is UNAFFECTED. Be exhaustive and state your total count.`, {
  label: 'census:consumers', phase: 'Census',
})

return { votes, attacks: attacks.filter(Boolean), census }
