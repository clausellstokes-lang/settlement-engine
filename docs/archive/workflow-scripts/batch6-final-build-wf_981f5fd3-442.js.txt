export const meta = {
  name: 'batch6-final-build',
  description: 'The final build batch: I3+I4 (brokerage services + the plant), K2 (magic regimes), K3 (disaster buffer), K4 (substitution + reagents)',
  phases: [{ title: 'Build', detail: '4 Opus implementers, disjoint ownership', model: 'opus' }],
}
const ROOT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const SCHEMA = { type: 'object', required: ['agent', 'fixes'], properties: { agent: { type: 'string' }, fixes: { type: 'array', items: { type: 'object', required: ['finding', 'status', 'files', 'tests', 'notes'], properties: { finding: { type: 'string' }, status: { type: 'string', enum: ['fixed', 'skipped', 'deferred', 'blocked'] }, files: { type: 'array', items: { type: 'string' } }, tests: { type: 'string' }, notes: { type: 'string' } } } }, behavior_shifts: { type: 'array', items: { type: 'string' } }, discovered: { type: 'array', items: { type: 'string' } } } }

const SHARED = [
  'Worktree: ' + ROOT + ' branch claude/composite-r4 (HEAD past bc843d33) - verify; stop on mismatch. CONCURRENT: three sibling agents (ownership below, never touch theirs) and the owner wizard-news session (wizardNews/settlementRumors/heraldRouting/informationStatecraft/momentum/supplyWebWarfare + tests - FOREIGN, read-only). A soak is live: never touch artifacts/soak/**; focused vitest only, one at a time; a timeout-shaped red gets ONE isolation re-run.',
  '',
  'THE GOLDEN LAW: your slice must NOT shift a golden. Everything ships DARK behind its virtual flag (informationBrokeragesEnabled / magicEconomyEnabled, both already declared and absent from DEFAULT_SIMULATION_RULES). If a change would move a golden, STOP and report.',
  'FIRST-PAINT: closure 1,020,590 of 1,040,000 (owner-gated). Never let an eager module statically import a heavy derivation - extract to a zero-import leaf (userRouteIdentity precedent).',
  'PRE-GATE HYGIENE (this has cost gate cycles): if you touch anything the edge bundles bake in, run npm run build:edge-shared before declaring done; if you touch a store action, run operationRegistry then gen:compendium-data then generate-sitemap then build:edge-shared IN THAT ORDER.',
  'CROSS-SLICE TYPES: grep siblings for an existing spelling before declaring a world/settlement/ledger param type and CONVERGE on it; a conditional key is declared on the OWNING typedef, never at the call site (the RouteBypass-into-routeNetworkLedger precedent).',
  'CENSUS/WALKER LAW: every interaction is a REGISTRATION with a written reason, never a widening. New roster reads go through liveInstitutions()/isLiveInstitution() unless you can say in source why a ruin still counts. A new store action must be REACHABLE (the dead-op ratchet catches advertised-but-unwired capabilities - if you register it, wire it).',
  '',
  'HARD RULES: no migrations; no git mutations; no full suites/builds; NO ratchet/baseline raises (adopt src/kernel/math.js primitives; ZERO any-casts in new domain files; the voice ratchet counts em dashes in src/domain|src/data STRING LITERALS; loading-narration is tolerance-0 so every Suspense fallback narrates; JSX 600 / js 800); determinism laws (seeded forks; house instruments: lowDiscrepancy Weyl sampler, draw accounting, SEED-FAMILY totality since a single-seed restriction pin is VACUOUS, structural purity scans); headless domain; anchored negatives; JSON-round-trip persistence pins; conditional worldState keys drop-when-empty. Return dense structured data: statuses, files, verbatim test tails, negative-control quotes (revert/red/restore/green per new pin), behavior shifts plainly, judgment calls vetoable, discovered.',
].join('\n')

phase('Build')
const results = await parallel([
  () => agent([
    'You are an Opus IMPLEMENTER for SettlementForge. SLICES I3 + I4 of the information-brokerage program: THE SERVICES AND THE MARKET. You own both because they share one seam.',
    'BINDING LAW: docs/DESIGN_INFORMATION_BROKERAGES.md section 6 (QUERY = SEE institutionalized: a per-question sharp-truth service, the DM or player asks about a subject, pays an IN-WORLD price banded by channel, distance and form, and receives the brokerage best derivation WITH its honest stamp; seeded, receipt-carried, Herald-recorded; query results are PROJECTIONS of existing truth and belief - never new facts minted. FEED = SHARE-SELL institutionalized: the standing patron contract, the patron power belief map gains channel calibration each pulse, and the feed is a visible edge making the house an espionage target. PLANT = LIE institutionalized, Whisper Market only: the ALREADY-BUILT LIE verb in informationStatecraft.js gains a seller; the market credibility stock backs the lie and the existing contradict-expose-blowback triple prices failure; plants are DM-truth until exposed. KNOWLEDGE-LANE CANDIDATES: queries, feeds, plants and intercepts register as knowledge-mover candidates - the starved lane measured in the 2026-07-31 soak gains its institutional generators), section 7 (PATRONAGE: each brokerage binds to a patron power at generation, seeded from the settlement power structure, and may REBIND through the existing faction-competition machinery - capturing the listeners is a coup-adjacent prize; patronage is DM-visible, audience-projected for players).',
    '',
    'YOUR OWNERSHIP: src/domain/worldPulse/brokerageServices*.js and brokeragePatronage*.js (new leaves), extensions to I1/I2 brokerage files, your informationBrokeragesEnabled certification row, tests/domain/brokerage{Services,Patronage,Plant}*.test.js. READ-ONLY and CRITICAL: informationStatecraft.js is FOREIGN this session (the owner is editing it) - you may READ its LIE verb seam and CALL its published exports, but you may NOT edit that file. If the plant genuinely requires an edit there, implement everything else and report the required edit precisely rather than making it.',
    '',
    'PINS with executed negative controls: a query mints no new facts (anchored negative: every returned claim traces to existing truth or belief); the in-world price is actually charged and refusal is honest when it cannot be paid; the feed measurably improves the patron calibration on ITS channels and not others; patronage rebinding rides the existing capture machinery; plants are DM-truth until exposed (audience projection); the knowledge lane now generates candidates (the starved-lane cure, measured); dormancy byte-identical.',
  ].join('\n') + '\n\n' + SHARED, { label: 'I3I4:services-market', phase: 'Build', schema: SCHEMA, model: 'opus' }),

  () => agent([
    'You are an Opus IMPLEMENTER for SettlementForge. SLICE K2 of the magic-economy program: REGIMES, FORMS, AND THE TWO-SIDED BAND.',
    'BINDING LAW: docs/DESIGN_MAGIC_ECONOMY.md section 3a (the two axes: MAGIC PROFILE gates PRESENCE, ECONOMY REGIME gates EXPLOITATION; the closed regime ladder subsistence|funded|patronized|industrial entered by thresholds WITH HYSTERESIS and banded within; AVAILABLE FORMS = [floor(tier x magic), ceiling(economy regime)] - a high-magic metropolis with a weak treasury holds circles but no foundry; a thorp holds one practitioner however magical the wood; the skyline is a DIAGNOSIS), section 3b (the forms ladder practitioner-circle-tower-guild-foundry, where the PRACTITIONER RUNG IS A TIED ROSTER CHARACTER as much as an institution row - the thorp whole magic is someone you can lose, and the consequence economy can reach them), section 4 (THE ONE GATE FORMULA - single-writer for formulas: one definition read by every magic-derived output), and the SHELLS law from section 3c (forms outlive their regimes as shells; economic decline CLOSES the foundry, never demolishes it; recovery WARM-STARTS from the shell - K1 built the status system, you consume it).',
    '',
    'YOUR OWNERSHIP: src/domain/worldPulse/magicRegime*.js and magicForms*.js (new leaves), your magicEconomyEnabled certification row, tests/domain/magic{Regime,Forms}*.test.js. READ-ONLY: K1 institutionStatus leaves (you CALL them for shells), the catalog magic entries (I1-era authored prose already speaks this design - read it and graduate the words into bands rather than inventing over them), magicProfile.js, the economy derivations.',
    '',
    'IMPLEMENT K2 ONLY (no buffer, no substitution - K3/K4 own those): the regime ladder with hysteresis and Herald-announced crossings carrying material receipts; the two-sided band; the ONE exploitation gate formula; the forms ladder with the practitioner as a tied roster character; shells on regime demotion with warm-start on recovery.',
    'PINS with executed negative controls: PRESENCE-INDEPENDENCE (magic-institution presence at high magic invariant across economy bands - the amendment own claim, made falsifiable); ARCHETYPE REACHABILITY (poor+high-magic cells genuinely generate practitioners/circles in real corpora - the unreachable-conjunction hazard class); EXPLOITATION ORDERING (magic-derived output monotone in economy at fixed magic); hysteresis (no flapping foundries under an oscillating-economy fixture); the two-sided band (a rich low-magic town and a poor high-magic one hold DIFFERENT form sets); shells persist and warm-start; dormancy byte-identical.',
  ].join('\n') + '\n\n' + SHARED, { label: 'K2:regimes-forms', phase: 'Build', schema: SCHEMA, model: 'opus' }),

  () => agent([
    'You are an Opus IMPLEMENTER for SettlementForge. SLICE K3 of the magic-economy program: THE DISASTER BUFFER.',
    'BINDING LAW: docs/DESIGN_MAGIC_ECONOMY.md section 5 (mitigation = economy_term + magic_term x gate, both banded, the sum CEILING-CAPPED so tail risk survives at every wealth level; mitigation CONVERTS damage rather than deleting it - reduced structural loss is PAID FOR in named stocks drawn down in the same outcome, with receipts reading like "the wards held; the granaries paid"; repair acceleration follows the same shape after the event; a drawn-down buffer recovers on banded dwells and THE SECOND-SHOCK WINDOW IS A DESIGNED VULNERABILITY, not an accident) and section 1 law 5 (DAMAGE TRANSMUTES, NEVER VANISHES).',
    '',
    'YOUR OWNERSHIP: src/domain/worldPulse/magicBuffer*.js (new leaves) and the calamityKernel integration seam for the buffer only, your magicEconomyEnabled certification row entries for the buffer, tests/domain/magicBuffer*.test.js. READ-ONLY: calamityKernel (you integrate at its published seam - extend, never rewrite), K2 regime leaves (call the ONE gate formula, do not re-derive it - if K2 has not landed its export yet, code against the design-doc contract and say so), the economy/stock derivations.',
    '',
    'PINS with executed negative controls: THE GATING ASYMMETRY (high-magic/poor mitigates approximately like mundane/poor - the proof that magic eats materials, and the amendment central claim); damage-ordering envelope across the magic x economy grid; CONJUNCTION REACHABILITY (the high-magic x high-economy cell genuinely occurs in generated corpora - the unreachable-conjunction hazard class, pre-answered by the design); the ceiling holds (no wealth nullifies a disaster); THE SECOND-SHOCK WINDOW (a drawn buffer measurably mitigates less than a rested one); conservation (what the buffer saves in structure it spends in named stocks - the sum is honest); dormancy byte-identical.',
  ].join('\n') + '\n\n' + SHARED, { label: 'K3:disaster-buffer', phase: 'Build', schema: SCHEMA, model: 'opus' }),

  () => agent([
    'You are an Opus IMPLEMENTER for SettlementForge. SLICE K4 of the magic-economy program: SUBSTITUTION + REAGENT CHAINS - the slice that closes the material loop.',
    'BINDING LAW: docs/DESIGN_MAGIC_ECONOMY.md section 6 (at the highest regimes magic may substitute for food and supply AS A CHANNEL in the existing foodBalance/supply architecture - share CAP-BANDED, costs MATERIAL (reagent draw plus practitioner capacity, flowing through the route ledger and therefore charterable and cuttable); THE DEPENDENCY METRIC: a settlement substitution share is a named fragility - magical dependency - Herald-narratable ("the city eats from the Guild hand") and enemy-targetable; CONSERVATION PIN: remove the magic source and the deficit honestly reappears; substitution shifts WHAT moves, never whether things move) and section 7 (REAGENT SUPPLY CHAINS, ratified: reagent chains join supplyChainData; magic institutions generate corridor demand in the J2 flow ledger scaled by regime; interdiction of a reagent route IMPAIRS (fast, K1 status) before economic failure SHELLS (slow, K2 regime); the buffer draws down reagent stocks BY NAME. One decision, and every mechanism this program built applies to magic for free - anti-magic siegecraft becomes economic warfare by construction).',
    '',
    'YOUR OWNERSHIP: src/domain/worldPulse/magicSubstitution*.js and the reagent chain additions to src/data/supplyChainData.js (+ its generated index if one exists), your magicEconomyEnabled certification row entries, tests/domain/magicSubstitution*.test.js and the reagent data pins. READ-ONLY: foodBalance channels (you ADD a channel in its existing idiom - the mountain_pass rung is the precedent for how a channel is added honestly), the J2 flow ledger (you generate demand into it via its published accessors), K1 status and K2 regime leaves (call them; if K2 exports have not landed, code against the design contract and say so).',
    '',
    'CRITICAL GOLDEN WARNING: adding reagent chains to src/data/supplyChainData.js may be GENERATING data. The I1 lesson is exact: a new catalog entry that generates consumes a PRNG draw and translates the seeded stream, which cost an owner-signed re-record. BEFORE authoring reagent data, determine whether it can generate under DEFAULT rules; if it can, either gate it behind magicEconomyEnabled so dark worlds draw identically, or STOP and report rather than shifting a golden.',
    'PINS with executed negative controls: the substitution CHANNEL enters the existing food architecture (not a bypass of it); the cap-band holds; THE CONSERVATION PIN (remove the magic source, the deficit reappears - the anti-exemption proof); the dependency metric emits and is enemy-targetable; reagent interdiction IMPAIRS before it SHELLS (the two-timescale law, end to end); dormancy byte-identical AND no golden shift (prove it, do not assume it).',
  ].join('\n') + '\n\n' + SHARED, { label: 'K4:substitution-reagents', phase: 'Build', schema: SCHEMA, model: 'opus' }),
])

const all = results.filter(Boolean)
log(all.length + '/4 final-batch slices returned')
return { results: all }