export const meta = {
  name: 'fp-cycle3-espionage-opener',
  description: 'Cycle 3: ES-0 (the espionage foundations + the CR-ES-3 cross-file vocabulary retarget) then ES-4 (the confirmation leg that completes the lighting road) — each through its own adversarial verifier',
  phases: [
    { title: 'ES-0', detail: 'foundations: lawWordFor, the seat-vocabulary retarget, the family flag' },
    { title: 'Verify 0', detail: 'adversarial verification, golden-shift audit' },
    { title: 'ES-4', detail: 'the confirmation leg + the pinned lighting evidence marker' },
    { title: 'Verify 4', detail: 'the instrument must flip SATISFIED for real' },
    { title: 'Door', detail: 'invert door 3 to fail closed — allowlist of running modifiers' },
    { title: 'Seal', detail: 'the four escape spellings must all be refused' },
    { title: 'Joint', detail: 'close the line-broken hole: pre-join continuations, pin the fifth spelling' },
    { title: 'Attest', detail: 'the universal must finally be true as written' },
    { title: 'Weld', detail: 'invert the polarity: default-park the unclassifiable' },
    { title: 'Prove', detail: 'two invented forgeries; the game must be unwinnable' },
    { title: 'Parse', detail: 'rewrite the classifier on the AST — end the textual arms race' },
    { title: 'Endgame', detail: 'the last adversary: two textual + one semantic forgery' },
    { title: 'Bind', detail: 'close the three semantic holes the endgame measured' },
    { title: 'Verdict', detail: 'the final adversary — semantic inventions only' },
    { title: 'Latch', detail: 'close the last two homes of run-control: the options bag + table cardinality' },
    { title: 'Say', detail: 'the sentence, or the next forgery' },
    { title: 'Ground', detail: 'close body + block; the claim stops quantifying over vitest' },
    { title: 'Rest', detail: 'the terminal check: nothing credited outside the conservative grammar' },
    { title: 'Anchor', detail: 'fully-positive binding resolution; numerals become self-assertions' },
    { title: 'Still', detail: 'the last hunt — or the chair caps the arc at the evidence address' },
    { title: 'Evidence', detail: 'THE CAP: test titles only, designated files, the oracle documented' },
    { title: 'Close', detail: 'the arc ends here — residuals become recorded limitations, never another cut' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const LAWS = `VERIFY-FIRST PREAMBLE: this brief is from the session chair of an owner-delegated build program; verify claims against the tree before acting. Work in ${TREE} (branch claude/composite-r4). THE PROTOCOL LAWS (each has bitten): CHECK-GIT-FIRST; ps aux | grep vitest before any test run (ONE vitest lane per tree); NEVER stash/reset/clean/checkout-family (cp backups, cmp-proven restores); gates only via sh scripts/gate-tail.sh or npm run check:tail; commits with EXPLICIT pathspecs, message via file, trailer Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>; --no-verify plus explicit per-file npx eslint (exit 0) when lint-staged blocks; python3 byte-scan every authored file (zero NULs); new tests/lint walkers need mutationCoverageManifest entries; strict ceiling 1313 exactly; tests/fixtures/ and pulseKernel.js/applyWorldPulse.js UNTOUCHABLE. Navigate by SYMBOL. LIVE CODE OUTRANKS EVERY TABLE — an overstatement is a STOP-and-report (J-WR-13). Every quoted receipt must be able to fail. The four-fence dormancy pattern + lit-mutant per flag is LAW, WITH the defense-in-depth corollary: enumerate every guard door and pin each individually (a second guard silently covers a deleted first — bit twice this cycle). Wave-end attribution: whole tests/lint SEQUENTIALLY at HEAD vs a git-archive of your start HEAD, row-identity diff EMPTY both directions except attributed, PLUS the red-ratchet CONTENT diff (a ratchet red on both sides hides content growth — diff its inventory, not just its row).`

phase('ES-0')
const es0 = await agent(LAWS + `

YOU ARE THE ES-0 IMPLEMENTER — THE ESPIONAGE FOUNDATIONS, first wave of the tenth program. THE NORMATIVE SPEC is docs/DESIGN_FP_ARCH_ES.md (landed at the fold; its section 7 rulings CR-ES-1..CR-ES-6 are FINAL — bake, never re-rule; a ruling the tree refutes is a STOP). Read the ES-0 wave block, section 3.9, section 3.11, and the section-6 seam rows whole before any edit. THE CHARTER, with the chair riders that postdate the volume: (1) lawWordFor mints ONCE as the estate's single law-band spelling, spelled with the CONSUMER vocabulary lawless | balanced | lawful, AND section 3.9's chaotic method arm respells to lawless IN THE SAME COMMIT (else a dead branch or a forbidden fourth vocabulary — the recorded collision); (2) THE CR-ES-3 RETARGET IS CROSS-FILE, pre-measured: warSeatBooks' module-private word functions retarget to the consumer vocabularies (chaotic|balanced|lawful -> lawless|balanced|lawful; benevolent|balanced|malicious -> merciful|balanced|malicious; the security ladder to the consumer set) — AND the three live consumers retarget IN THE SAME COMMIT: envoyNegotiationPictureBuilder.js reads the literals 'benevolent' and 'lawful' (a producer-only retarget SILENTLY KILLS those arms — the dead-arm class), warTermination.js re-emits both bands as ruler-prefixed keys, settlementStrategy.js enumerates those keys in a closed list. Land the both-vocab equality pin in the same commit, and DECLARE the golden-shift exposure honestly in the commit body (this edits live war-lane files; if any golden legitimately moves, the golden-shift-record discipline binds — cause declared, never reflexive); (3) the family flag espionageEnabled lands with its full CQ5 trio (manifest + authored cert row + first real gate read, one commit — the row guard: a manifesting flag lands its ROW); (4) whatever else the ES-0 block charters (the warMagicGate re-export and the volume's named foundations) — build per the volume, re-measuring every premise; (5) four-fence dormancy + lit-mutant for the flag, with every guard door pinned individually; anchored negatives; executed mutants with manifest entries. DARK BY CONSTRUCTION: flag-absent worlds byte-identical, proven. Wave-end attribution per the LAWS (including the red-ratchet content diff). RETURN JSON: commits, gates, goldenShifts (declared, with causes — empty if none), stopReports, notes.`, { label: 'build:ES-0', phase: 'ES-0', model: 'opus' })

phase('Verify 0')
const ves0 = await agent(LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER FOR ES-0. Default posture: wrong until executed evidence. The implementer reported: ` + JSON.stringify(es0) + `

VERIFY BY EXECUTION in git-archive trees (node_modules symlinked; live tree read-only): (1) THE VOCABULARY RETARGET — grep the OLD producer words (chaotic/benevolent and the old security set) across src/: zero survivors as OUTPUT values (readers of history/display prose excepted with reasons); the both-vocab equality pin reds when you re-plant ONE old word in the producer (executed, restored); the three consumers read the NEW words (drive envoyNegotiationPictureBuilder's arms over a fixture where the bands matter — the formerly-benevolent arm must still be REACHABLE, not dead); (2) lawWordFor — exactly ONE law-band spelling in the estate (the seam-10 no-fourth-vocabulary law), section 3.9's method arm branches on the new word, and the volume's doc matches the code; (3) the CQ5 trio — git log -S proves co-commitment; engineGatedRuleKeys + subsystemRows walkers green; delete-the-row mutant reds; (4) the four-fence set REALLY SEES — run the lit-mutant control AND your own guard-door deletion hunt (every door pinned individually — the defense-in-depth corollary); (5) GOLDEN AUDIT: if the lane declared golden shifts, verify each shifted golden's cause against its diff (same-seed outputs moved ONLY where the retarget legitimately renames words — any numeric/structural drift is a REJECT); if it declared none, run the war-lane goldens yourself and confirm byte-stability; (6) wave-end attribution both granularities (rows + red-ratchet contents), strict 1313, byte-scans, commit hygiene. Demonstrate two instruments failing on deliberate mutations. RETURN structured JSON only.`, { label: 'verify:ES-0', phase: 'Verify 0', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('ES-4')
let es4 = null, ves4 = null
if (ves0 && ves0.verdict === 'PASS') {
  es4 = await agent(LAWS + `

YOU ARE THE ES-4 IMPLEMENTER — THE CONFIRMATION LEG, the third and final member of the WR-10 lighting road. THE NORMATIVE SPEC is docs/DESIGN_FP_ARCH_ES.md's ES-4 wave block — read it whole and re-measure its stated dependencies FIRST: the recon holds that ES-4 requires SP-B (landed, the conditionsBands fields live) + SP-B2 (landed, beliefLegsFor supplies the four consumer-spelled legs through sovereigntyMarketStage) + ES-0 (just landed); IF the volume's ES-4 block requires machinery from ES-1..ES-3 that does not exist, STOP AND REPORT — never improvise a substitute spine. THE CHARTER: (1) build the confirmation leg per the volume — the espionage-side evidence that upgrades the sovereignty appraisal's distant-source confidence, riding the ALREADY-BUILT seam (sovereigntyMarketStage's beliefLegsFor application sites); (2) THE LIGHTING EVIDENCE MARKER, chair-ruled from the SP-B2 verifier's executed finding: the instrument in src/domain/certification/warConvergenceContract.js currently satisfies its ES-4 row on ANY substring in ANY test file (a bare comment flips it — executed proof exists); land ES-4-DISTANT-SOURCE-EVIDENCE as a REAL test title on a REAL pin in your battery, AND tighten the instrument's address check in the SAME commit (a title-anchored match, not any-substring) with a negative control proving a bare comment no longer flips it; (3) run the instrument at wave end: evaluateSovereigntyLighting must measure {SP-B:true, SP-B2:true, ES-4:true} -> state SATISFIED, satisfiable true, missing empty — quote the executed output; the whole walker green in the SATISFIED state; (4) the wave stays DARK per the flag law (whatever flag the volume assigns ES-4 rides, its dormancy proven); four-fence + guard-door pins + mutants + manifest entries. Wave-end attribution per the LAWS. RETURN JSON: commits, gates, instrumentReading (the executed SATISFIED output), stopReports, notes.`, { label: 'build:ES-4', phase: 'ES-4', model: 'opus' })

  phase('Verify 4')
  ves4 = await agent(LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER FOR ES-4 — the wave that completes the lighting road. The implementer reported: ` + JSON.stringify(es4) + `

VERIFY BY EXECUTION in git-archive trees: (1) THE INSTRUMENT — run evaluateSovereigntyLighting's walker yourself at HEAD: SATISFIED with missing empty, quoted; then the tightened address: plant a bare comment carrying the marker in a scratch test file — the measurement must NOT flip on it (the SP-B2-era hole closed); delete the real pinned title — the measurement returns to UNSATISFIED_TRACKED and the walker stays green (the tracked state is not an error state); (2) THE LEG IS REAL — drive the appraisal over a world with espionage evidence present vs absent and confirm the confidence leg moves as the volume specifies (executed, both states); (3) dependencies honest — git log confirms ES-4 built on landed substrate only (no phantom ES-1..3 reads: grep its imports); (4) dormancy + guard doors + mutants per the standing law; (5) wave-end attribution both granularities; strict 1313; byte-scans; commit hygiene. If EVERYTHING stands, say so plainly: the lighting road is complete and the market awaits only the owner's signature. RETURN structured JSON only.`, { label: 'verify:ES-4', phase: 'Verify 4', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })
} else {
  log('ES-0 verifier rejected — returning for the chair without starting ES-4.')
}

phase('Door')
const door = await agent(LAWS + `

YOU ARE THE DOOR-3 REPAIR LANE — one narrow fix. The ES-4 verifier's verdict: ` + JSON.stringify(ves4) + `

THE DEFECT, verifier-executed: tests/lint/sovereigntyLightingContract.walker.test.js door 3 claims EVERY REFUSAL FAILS CLOSED, but four escape spellings credit a marker inside a suite that never runs (describe.skipIf(true), the computed describe['skip'] access, and the other two the verdict enumerates — each measured against the walker's own regexes). THE NAMED REPAIR, chair-ruled as specified: invert door 3 to FAIL CLOSED — any suite-opening line is PARKED unless its modifier chain matches a CLOSED ALLOWLIST of running modifiers (bare describe and the genuinely-running dotted forms only; xdescribe, any .skip/.todo variant, any computed access, any conditional modifier = parked). Land the four escape spellings as executed negative controls (each must be REFUSED, quoted), keep the six existing door pins green, correct the file's fail-closed comment to match the now-true mechanics, and fix the manifest rationale's inherited overstatement in the same commit. ONE pathspec commit; focused gates (the walker + mutationCoverageManifest) green; whole tests/lint sequential vs your start HEAD with EMPTY row-identity diff except the attributed new negative-control rows + red-ratchet content diff; strict 1313; byte-scans. RETURN JSON: commit, escapeControls (four, each with its executed refusal), notes.`, { label: 'repair:door-3', phase: 'Door', model: 'opus' })

phase('Seal')
const seal4 = await agent(LAWS + `

YOU ARE THE SEALING VERIFIER. The door repair reported: ` + JSON.stringify(door) + `

VERIFY BY EXECUTION in a git-archive tree: (1) re-plant ALL FOUR escape spellings yourself — each REFUSED (the marker not credited), quoted; (2) a genuinely-running suite with the marker in its title IS credited (the positive control — fail-closed must not become fail-everything); (3) the six prior door pins still green; (4) the comment now describes the true mechanics; the manifest rationale caveat corrected; (5) whole tests/lint both granularities vs the repair base, strict 1313, commit hygiene. RETURN structured JSON only.`, { label: 'seal:door-3', phase: 'Seal', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Joint')
const joint = await agent(LAWS + `

YOU ARE THE JOINT LANE — one file, the last hole. The seal verdict (everything else CONFIRMED; one falsified universal): ` + JSON.stringify(seal4) + `

EXECUTE THE SEAL'S OWN REMEDY OPTION (a), chair-ruled: in tests/lint/sovereigntyLightingContract.walker.test.js, PRE-JOIN physical continuation lines before the suite-opener scan (or equivalently park any line whose trailing token is a bare describe / suite / describe-dot — pick whichever composes cleanly with the existing liveTitlesIn split, and say which you chose and why in one sentence); PIN the line-broken spellings in the existing DOOR 3 ESCAPE arm — at minimum the three eslint-clean forgeries the seal proved (describe newline dot-skip, describe-dot newline skip, describe newline dot-only) plus one computed and one skipIf continuation form, each as an executed negative control (planted, REFUSED, quoted, removed); CORRECT the header claim to what is now true (and only what is true — if any residual remains, name it explicitly; the seal's phrasing correction applies: whitespace WITHIN ONE LINE if you keep the split, or the closed claim if you pre-join); align the manifest rationale in the same commit. Acceptance: all NINE escape spellings (the four closed + the five line-broken) REFUSED at HEAD by execution; the positive controls still credited; all prior door pins green; walker + mutationCoverageManifest green; wave-end both granularities vs your start HEAD; strict 1313; byte-scans. ONE pathspec commit. RETURN JSON: commit, joinChoice (one sentence), escapeControls (nine, each executed), notes.`, { label: 'joint:door-3', phase: 'Joint', model: 'opus' })

phase('Attest')
const attest = await agent(LAWS + `

YOU ARE THE ATTESTING VERIFIER — the last check on this walker. The joint lane reported: ` + JSON.stringify(joint) + `

VERIFY BY EXECUTION in a git-archive tree, driving the walker's OWN extracted read (the seal's method — slice the shipped code, never paraphrase): (1) plant all NINE escape spellings yourself, one at a time — every one REFUSED (ES4_titled empty, state UNSATISFIED_TRACKED); (2) invent ONE more forgery spelling of your own design within the recognized vocabulary — if it is credited while vitest skips it, REJECT with the spelling; (3) the three positive controls credited; (4) the header and manifest claims now TRUE as written against your own probes — no unexecuted universal survives; (5) all door pins green, wave-end both granularities, strict, hygiene. If everything stands: say plainly that door 3 is fail-closed as written. RETURN structured JSON only.`, { label: 'attest:door-3', phase: 'Attest', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Weld')
const weld = await agent(LAWS + `

YOU ARE THE WELD LANE — the polarity fix that ends this arc. The attest verdict (nine escapes closed; a TENTH family invented and credited; a remedy EXECUTED by the attester): ` + JSON.stringify(attest) + `

THE CHAIR'S RULING — THE POLARITY PRINCIPLE, which makes the forgery game unwinnable by construction: door 3 must DEFAULT-PARK any line carrying a suite-opener token (describe / suite, any x/f prefix, any punctuation after) that it cannot POSITIVELY classify as a plain RUNNING suite head. Recognition credits; everything else parks; there is no third bucket. Implement it as the attester's own executed remedy direction where it fits (widening the dangling-head tail to accept an unterminated computed member was PROVEN to close the bracket family inside the existing rejoin) PLUS whatever completes the principle: the positive-classification arm is a CLOSED grammar (bare describe/suite + the genuinely-running dotted forms on ONE logical line), and any suite-opener token that fails that grammar parks the file. Add the attester's nine bracket variants (at minimum the three eslint-clean ones) to the escape-control battery, executed. Rewrite the header ONE more time stating the POLARITY, not the reach: this detector never claims to recognize every parked spelling; it claims to CREDIT only what it positively recognizes as running, and to PARK everything else — a claim that cannot be falsified by inventing spellings, only by finding a CREDITED non-running suite. Align the manifest rationale. Acceptance: all THIRTEEN-plus escape spellings REFUSED; the three positive controls credited; every prior pin green; walker + manifest green; wave-end both granularities; strict 1313; byte-scans; ONE pathspec commit. RETURN JSON: commit, polarityStatement (the new header sentence verbatim), escapeControls (count + the new ones executed), notes.`, { label: 'weld:door-3', phase: 'Weld', model: 'opus' })

phase('Prove')
const prove = await agent(LAWS + `

YOU ARE THE PROVING VERIFIER — the last word on this walker. The weld lane reported: ` + JSON.stringify(weld) + `

The bar is the POLARITY CLAIM: the detector credits ONLY what it positively recognizes as a running suite and parks everything else. VERIFY BY EXECUTION (extract the shipped code, never paraphrase): (1) re-plant the full escape battery — all refused; (2) INVENT TWO forgeries of your own, as creative as you can make them within the suite vocabulary (line-broken, computed, template-literal members, comments interleaved, whatever) — under the polarity claim each must be PARKED; if EITHER is credited while vitest skips it, REJECT with the spelling; (3) the positive controls credited — and invent ONE unusual-but-genuinely-running spelling (odd whitespace on one line) to confirm fail-closed did not become fail-everything (if it parks, report it as a named residual, not a reject — parking a running suite costs a wave one honest reformat, never a lie); (4) the header states the polarity and ONLY the polarity; (5) all pins green, wave-end both granularities, strict, hygiene. If the game is unwinnable: say so plainly. RETURN structured JSON only.`, { label: 'prove:door-3', phase: 'Prove', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Parse')
const parse = await agent(LAWS + `

YOU ARE THE PARSE LANE — the weapon-class change that ends this arc. Five rounds of regex classifiers have each been out-invented; the proving verdict (nine credited-non-running suites in three classes, the fail-open default diagnosed, the winnable shape stated): ` + JSON.stringify(prove) + `

THE CHAIR'S RULING: rewrite door 3's classifier ON THE AST. Parse every test file with the parser already in node_modules (espree — eslint's own — or acorn; measure which the estate already depends on and use that one), and classify STRUCTURALLY: (1) find every CallExpression whose callee chain roots in describe / suite (any x/f prefix, any member chain, computed or dotted, any depth — the AST makes these one shape); (2) CREDIT a suite only when its callee is in the CLOSED RUNNING GRAMMAR as AST shapes (bare describe/suite Identifier; the genuinely-running dotted members — measure which modifiers vitest actually runs, .only and .each run, .skip/.todo/.skipIf/.runIf and x-prefix do not or are conditional = PARKED); (3) PARK the file for ANY other suite-rooted callee shape, ANY alias binding whose init references describe or suite (a const d = describe is visible at the binding — park conservatively), and ANY Identifier reference to describe/suite outside a credited direct call; (4) a file that FAILS TO PARSE parks whole (fail-closed at the parser door); (5) marker titles are read from the AST too — a credited marker is a string Literal argument of an it/test call that is a DESCENDANT of a credited suite (or a top-level it/test), never a substring of raw source. Comments and line breaks cease to exist as attack surface. Keep the ENTIRE 29-spelling refusal battery + the prover's nine forgeries as executed controls (they should all park trivially now); keep the positive controls credited; the header rewrites ONE final time to state the structural claim (classification is by parse, the running grammar is closed, parse failure parks). Runtime budget: measure the walker's wall time over the 2,314 files and report it (other whole-tree walkers exist; stay within their family). Align the manifest rationale. ONE pathspec commit; walker + manifest green; wave-end both granularities; strict 1313; byte-scans. RETURN JSON: commit, parserChosen (and why), runningGrammar (the exact AST shapes credited), wallTime, escapeControls (count, all executed), notes.`, { label: 'parse:door-3', phase: 'Parse', model: 'opus', effort: 'high' })

phase('Endgame')
const endgame = await agent(LAWS + `

YOU ARE THE LAST ADVERSARY. The parse lane reported: ` + JSON.stringify(parse) + `

The claim is now STRUCTURAL: classification by AST, a closed running grammar, parse-failure parks. VERIFY BY EXECUTION (slice the shipped code, never paraphrase): (1) the full 38-spelling battery through the extracted classifier — all parked; positive controls credited; (2) invent TWO textual forgeries as creative as you can (encodings, weird unicode identifiers, comments, template members, line noise) — under an AST classifier each must park or fail-parse-park; (3) invent ONE SEMANTIC forgery — an aliasing or indirection trick (bound aliases, destructuring, globalThis access, a helper that calls describe) that makes a non-running suite carry the marker — and report whether the conservative alias rule parks it; if a semantic forgery is CREDITED while vitest skips it, REJECT with the spelling; (4) one unusual-but-running spelling — if it parks, name it as a residual (a reformat cost, never a lie); (5) wall time within the whole-tree walker family; all pins green; wave-end both granularities; strict; hygiene. If the structural claim holds against your best inventions, say the sentence: door 3 credits only what the parser proves runs. RETURN structured JSON only.`, { label: 'endgame:door-3', phase: 'Endgame', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Bind')
const bind = await agent(LAWS + `

YOU ARE THE BIND LANE — the semantic closure. The endgame verdict (the ENTIRE lexical layer held — 59 inventions parked; three SEMANTIC holes, each with a measured, costed remedy): ` + JSON.stringify(endgame) + `

CLOSE ALL THREE, chair-ruled from the endgame's own measurements: (B1, the shadow-swallow) apply Remedy A exactly — the shadowed check skips ONLY running-grammar calls (one token: the continue requires call.running), so a bound suite word parks its non-running suites instead of muting them; the endgame measured the estate cost at ONE file (a genuine stray) — name it in the commit body. Fix the conjunction-blind pin: BOTH halves of the bound-word matrix pinned (bound+running parks as ambiguous, bound+non-running PARKS — the complement the shipped pin missed), the endgame's 6x3 matrix as the executed battery. (B2, test-word bindings) TEST_ROOTS enter the shadowed tracking exactly as SUITE_ROOTS do — a locally declared, destructured, aliased or shim-imported it/test parks the file; pin with the endgame's spellings. (B3, reachability) the conservative static-registration rule: an it/test call is credited ONLY when its call expression is a direct statement in the module body or in a CREDITED suite's callback body; any it/test inside any other function body, callback, loop or conditional parks the file (dynamic registration is statically undecidable — parking costs a reformat to it.each, which IS in the running grammar; crediting risks a lie). MEASURE the estate cost of B3 (how many of the 2,314 files park under it) and report the list — if it exceeds twenty, the rule still lands but the parked files are enumerated in the commit body as reformat debt, never silently. Fix the TWO header overstatements the endgame named (J-WR-13); the header's claim narrows to what is now true: the lexical layer total, the semantic layer closed for bindings and static registration, the test.extend residual named. Keep all 59 battery entries + add the endgame's semantic forgeries as executed controls. ONE pathspec commit; walker + manifest green; wave-end both granularities; strict 1313; byte-scans. RETURN JSON: commit, b3Cost (count + list-or-summary), escapeControls (count), notes.`, { label: 'bind:door-3', phase: 'Bind', model: 'opus', effort: 'high' })

phase('Verdict')
const verdict = await agent(LAWS + `

YOU ARE THE FINAL ADVERSARY — semantic inventions only; the lexical layer is proven. The bind lane reported: ` + JSON.stringify(bind) + `

VERIFY BY EXECUTION (slice the shipped classifier, never paraphrase): (1) the full battery (59 lexical + the semantic controls) — all parked, positives credited; (2) the endgame's three classes re-planted from ITS verdict verbatim — all parked now; (3) invent TWO semantic forgeries of your own — indirection, higher-order registration, module-graph tricks (a re-export of describe from another file, a helper module that registers the marker, dynamic import) — under the rules each must park, fail-parse-park, or be structurally out of reach (a marker in ANOTHER file is out of the walker's evidence address by construction — verify the address is per-file before crediting that as safe); if EITHER is credited while vitest registers nothing, REJECT with the spelling; (4) confirm B3's estate cost matches the bind lane's list by your own census; (5) the header claims true against your probes; all pins green; wave-end both granularities; strict; hygiene. THE SENTENCE, if it holds: door 3 credits only statically-proven registration under an unbound running grammar — say it or reject. RETURN structured JSON only.`, { label: 'verdict:door-3', phase: 'Verdict', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Latch')
const latch = await agent(LAWS + `

YOU ARE THE LATCH LANE. The final adversary held everything except two data-form forgeries it invented, with both cures costed on the live estate by its own census: ` + JSON.stringify(verdict) + `

CLOSE BOTH, chair-ruled in the walker's own polarity: (L1, the options bag) park any it/test/describe/suite call whose SECOND argument is anything but a FunctionExpression or ArrowFunctionExpression — the adversary measured ZERO estate cost; run-control expressed as data is unclassifiable-as-running, so it parks. (L2, the zero-row table) an each/for table is credited ONLY when it is a non-empty ArrayExpression literal with statically present elements; an empty literal, a spread, an identifier, or any non-literal table PARKS — measure the estate cost with your own census and enumerate any parked files in the commit body. Add BOTH forgery families (the adversary's executed spellings verbatim) to the control battery. Fix the two J-WR-13 header overstatements it named and the one drifted doc address. The header's final claim enumerates the THREE statically-visible homes of run-control and states all three closed: callee chain (closed grammar), second-argument form (function only), table cardinality (non-empty literal only) — and keeps the standing falsifier sentence. ONE pathspec commit; walker + manifest green; wave-end both granularities; strict 1313; byte-scans. RETURN JSON: commit, l2Cost, escapeControls (count), notes.`, { label: 'latch:door-3', phase: 'Latch', model: 'opus' })

phase('Say')
const say = await agent(LAWS + `

YOU ARE THE LAST WORD. The latch lane reported: ` + JSON.stringify(latch) + `

The claim: run-control in vitest lives in exactly three statically-visible homes — the callee chain, the second-argument form, and table cardinality — and all three are closed in the walker's polarity. VERIFY BY EXECUTION (slice the shipped classifier): (1) the full battery including both new families — all parked, positives credited; (2) attempt to find a FOURTH home of run-control — read vitest 4.1.8's OWN API surface (its type definitions in node_modules) for any other statically-expressible way a registered-looking test does not run (concurrent, sequential, retry, fixtures, environment pragmas, in-source conditions) and EXECUTE your best candidate — if a credited non-running spelling exists, REJECT with it; if your candidates all park or all genuinely run, say so with the executed evidence; (3) estate censuses reproduce; all pins green; wave-end both granularities; strict; hygiene. THE SENTENCE, only if it is finally true: door 3 credits only what the parser proves registered and running, across all three homes of run-control. RETURN structured JSON only.`, { label: 'say:door-3', phase: 'Say', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Ground')
const ground = await agent(LAWS + `

YOU ARE THE GROUNDING LANE — the terminal cut. The Say adversary found the fourth home (run-control in the BODY and in BLOCK REACHABILITY — ctx.skip / hook-parameter skip / early return before registrations), both at ZERO estate prevalence, both repairs measured at zero cost, and its verdict carries the category diagnosis and repair shapes: ` + JSON.stringify(say) + `

TWO CLOSURES AND ONE RECLASSIFICATION, chair-ruled: (G1, the body) park any file where a test/suite/hook callback declares a parameter (destructured or not) and that parameter's skip is referenced in any form — conservatively: any use of the callback context parameter AT ALL inside credited registration callbacks parks the file (context-free tests only); measure the estate cost with your census and enumerate parked files if any. (G2, the block) park any credited suite whose callback top-level block is not STRAIGHT-LINE registration: any return statement, throw, or conditional/loop that wraps or precedes a registration parks the file — straight-line means every statement is a registration call, a plain declaration, or a hook attachment; measure and enumerate the cost. (G3, THE TERMINAL CLAIM) rewrite the header so it QUANTIFIES ONLY OVER THE WALKER, never over vitest: the claim is credits only straight-line, context-free, statically-registered tests under the closed call grammar; everything else parks — with the standing falsifier reduced to its terminal form: a CREDITED file outside that grammar. Delete every sentence that enumerates vitest homes as if the list were total; keep the four found homes as the RECORD of why the grammar is conservative. Fix the one-off title count (24,457). Add the Say adversary spellings to the battery. ONE pathspec commit; walker + manifest green; wave-end both granularities; strict 1313; byte-scans. RETURN JSON: commit, g1g2Cost, terminalClaim (verbatim), notes.`, { label: 'ground:door-3', phase: 'Ground', model: 'opus', effort: 'high' })

phase('Rest')
const rest = await agent(LAWS + `

YOU ARE THE TERMINAL VERIFIER. The grounding lane reported: ` + JSON.stringify(ground) + `

The claim now quantifies only over the walker: credits only straight-line, context-free, statically-registered tests under the closed call grammar. VERIFY BY EXECUTION (slice the shipped classifier): (1) the full battery including the Say spellings — all parked; positives credited; (2) the terminal falsifier, attempted honestly: find ONE CREDITED estate file (of the ~2,000) that is NOT straight-line/context-free/statically-registered under the stated grammar — walk a random sample of 40 credited files through your own independent grammar check, and if any credited file violates the grammar, REJECT with the file; (3) confirm you can NO LONGER falsify by API hunting — pick vitest 4.1.8 constructs the walker has never seen (in-source test modules, concurrent shuffles, whatever the types offer) and confirm each parks or is out of scope BY THE GRAMMAR rather than by an enumeration row; (4) censuses reproduce; pins green; wave-end both granularities; strict; hygiene. THE TERMINAL SENTENCE, if it holds: the walker credits nothing it cannot statically prove, and the claim cannot be falsified from outside the grammar. RETURN structured JSON only.`, { label: 'rest:door-3', phase: 'Rest', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Anchor')
const anchor = await agent(LAWS + `

YOU ARE THE ANCHOR LANE — the last structural cut, and it DELETES machinery. The Rest verdict (the renamed-vitest-import falsifier, executed at three layers; everything else held including the full 108-entry battery): ` + JSON.stringify(rest) + `

THE CHAIR'S RULING — FULLY-POSITIVE BINDING RESOLUTION: retire the shadow-clause enumeration entirely. A call is CREDITED only when its callee resolves, through the module's own binding structure, to a vitest import whose IMPORTED name (never the local alias) is in the closed running grammar — covering named imports (imported.name checked, renames therefore park or classify by their TRUE name), namespace member access (import * as v: the member name is the imported name; computed members park), and NOTHING else; any callee whose binding cannot be positively resolved to a running vitest opener PARKS the file. This subsumes every shadow/alias/rename case in one positive predicate — delete the shadow tracking it replaces. SECOND RULING — NUMERALS BECOME SELF-ASSERTIONS: the census figures the header states (parked count, credited count, title count) move from prose into EXECUTED assertions in the suite (the walker asserts its own census equals the stated constants), so the recurring stale-numeral class reds instead of lying; fix the two stale sentences the Rest found (the a6 async-suite sentence now describes a PARKED spelling — correct it) while you are there. Add the Rest forgeries (expect-as-it, assert-as-it, vi-as-test, beforeEach variants, expect-as-describe) to the battery as executed controls. Rewrite the terminal claim in binding-resolution terms. Measure the estate cost of the positive predicate (files whose credited calls resolve today) — expect near-zero delta since census figures already reproduce; enumerate any newly parked. ONE pathspec commit; walker + manifest green; wave-end both granularities; strict 1313; byte-scans. RETURN JSON: commit, machineryDeleted (what the positive predicate retired), newlyParked, terminalClaim (verbatim), notes.`, { label: 'anchor:door-3', phase: 'Anchor', model: 'opus', effort: 'high' })

phase('Still')
const still = await agent(LAWS + `

YOU ARE THE STILLNESS CHECK — the last hunt this arc will fund. The anchor lane reported: ` + JSON.stringify(anchor) + `

The claim is now: credited means callee-binding-resolved to a vitest import whose IMPORTED name is a running opener, straight-line, context-free, statically registered; everything else parks, unresolvable parks. VERIFY BY EXECUTION (slice the shipped classifier): (1) the full battery + the Rest forgeries — all parked; accuracy controls credited; (2) hunt ONE more credited-non-running construct with everything you know (module-binding tricks, namespace games, TS-flavored syntax espree accepts, whatever) — if found, REJECT with the spelling AND note it for the chair's cap decision (the chair has pre-announced: one more falsification ends the arc by narrowing the evidence address to a walker-owned file rather than another cut); (3) the self-assertion numerals: deliberately perturb one constant in a scratch copy — the suite must RED (the staleness class now has teeth); (4) censuses reproduce; pins green; wave-end both granularities; strict; hygiene. THE SENTENCE if it holds: credit is proven from the import graph; the walker cannot be lied to about who registered what. RETURN structured JSON only.`, { label: 'still:door-3', phase: 'Still', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings', 'notes'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Evidence')
const evidence = await agent(LAWS + `

YOU ARE THE CAP LANE — THE ARC ENDS WITH THIS COMMIT, by the chair's pre-announced decision. The Still verdict (the suite-title forgery; the oracle discovered; the live estate instance): ` + JSON.stringify(still) + `

THREE CLOSURES, chair-ruled: (E1, the fix) EVIDENCE IS TEST TITLES ONLY — suite titles are NEVER evidence (this matches the instrument's own original spec: a REAL pinned TEST title); the titleArgs feed splits so measure reads only test-layer titles; the Still forgery family (all eight spellings) joins the battery as executed controls; disposition the LIVE estate instance (prerenderRoutes' all-skipIf suite) in the census notes — its suite title was never a marker, so the tree reading should be unchanged; verify and state it. (E2, THE ADDRESS CAP) evidence markers must be TEST titles in files matching a DECLARED EVIDENCE-FILE PATTERN the walker owns (an explicit, small file-pattern allowlist covering the existing evidence homes — the sovereignty wave-close integration family and wherever SP-B2/ES-4 markers legitimately live; a marker title in any file outside the declared pattern is NOT evidence and the walker says so in its census) — generality is retired ON PURPOSE; the header records this as THE CAP with one sentence of why (eleven adversarial rounds proved open-address evidence unwinnable; a designated address is auditable). (E3, THE ORACLE) document vitest list --json as the STANDING AUDIT in the header — the framework's own ground truth of what runs, with the exact command and the reconciliation the Still adversary executed (23,676 titles checked) recorded as the auditor's method; if a cheap runIf-gated slow test can run the oracle reconciliation over ONLY the declared evidence files, land it (measure the cost first — if it exceeds the walker family's runtime, record the method without the test). Fix any prose the changes stale. ONE pathspec commit; walker + manifest green; wave-end both granularities; strict 1313; byte-scans. RETURN JSON: commit, evidencePattern (the declared allowlist verbatim), oracleDisposition (test landed or method recorded, with the measured cost), notes.`, { label: 'evidence:door-3-cap', phase: 'Evidence', model: 'opus', effort: 'high' })

phase('Close')
const arcClose = await agent(LAWS + `

YOU ARE THE ARC-CLOSING VERIFIER. THE ARC ENDS WITH YOUR REPORT REGARDLESS OF WHAT YOU FIND — anything beyond the three closures becomes a RECORDED LIMITATION in the walker's header (with the oracle as the compensating audit), never another cut; that instruction is the chair's, pre-announced two rounds ago. The cap lane reported: ` + JSON.stringify(evidence) + `

VERIFY BY EXECUTION: (1) the full battery + the Still suite-title family — all refused; the positive controls (real test titles in declared evidence files) credited; (2) the address cap — a marker TEST title planted in a file OUTSIDE the declared pattern is NOT credited (executed); inside, credited; (3) the oracle reconciliation over the declared evidence files reproduces (run it yourself with the recorded command); (4) hunt ONE more forgery if you can — and if you find one, write it up as the RECORDED LIMITATION with its oracle-detectability noted, DO NOT reject for it; (5) censuses, pins, wave-end both granularities, strict, hygiene. Your verdict is PASS if the three closures hold as specified (limitations recorded are compatible with PASS); REJECT only if a closure itself is broken. Then say the arc's closing sentence honestly, whatever it turns out to be. RETURN structured JSON only.`, { label: 'close:door-3-arc', phase: 'Close', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings', 'notes'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, recordedLimitations: { type: 'array', items: { type: 'string' } }, closingSentence: { type: 'string' }, notes: { type: 'string' } } } })

return { es0, ves0, es4, ves4, door, seal4, joint, attest, weld, prove, parse, endgame, bind, verdict, latch, say, ground, rest, anchor, still, evidence, arcClose }