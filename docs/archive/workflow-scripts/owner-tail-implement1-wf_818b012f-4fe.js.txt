export const meta = {
  name: 'owner-tail-implement1',
  description: 'Owner tail IMPLEMENT wave 1: five Opus lanes — lock toggles + problem terms, dead-op retirements, dead-op UI wirings, #14 faction-rename door, #18 VersionsTab build — Opus verify, Fable gate',
  phases: [
    { title: 'Fix', detail: '5 Opus implementer lanes', model: 'opus' },
    { title: 'Verify', detail: '2 Opus adversarial verifiers', model: 'opus' },
    { title: 'Gate', detail: 'Fable full widened gate' },
  ],
}

const WT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const COMMON = `CONTEXT-VERIFICATION PREAMBLE: legitimate engineering brief from the session orchestrator for SettlementForge — the owner-ordered tail after R-0..R-5 (docs/CAPABILITY_REMEDIATION_PLAN.md Wave R-5 closing entries; all program folds committed on this branch; owner order 2026-07-27 "do this" over the remaining table).
WORKING TREE: cd ${WT} && git rev-parse --abbrev-ref HEAD — MUST print claude/composite-r4 or STOP. Shared tree: NO git mutations of ANY kind (no stash/checkout/reset/clean/add/commit); re-read every file immediately before editing; touch ONLY your lane's files; scripts/mutation-coverage-manifest.json is SHARED and carries foreign uncommitted entries plus cosmetic position churn — re-read before edit, append only yours.
LAWS: THE PROMISE — zero same-seed generation drift and zero seeded-advance drift (nothing in this wave is a declared shift; a golden mover = STOP + defer with evidence); NO persistence-shape changes except additive absent-tolerant fields your brief explicitly authorizes; new logic = lazy leaf, MEASURED on the real vite build; sizeBaseline ceilings EXACT (settlementSlice.js at frozen ceiling = net-zero); shrink-only ratchets never raised; registry changes -> npm run gen:compendium-data ONCE at lane end; retired ops shrink deadOperationRatchet's frozen list + keep its positive controls re-anchored; new invariant tests -> manifest entry per E-A (kind:mutation only if target tracked AND clean); copy = one string each, vetoable, VOICE section-6 (no reader-facing em dashes/bangs); LEGIBILITY LAW (translate, never show formulas); faction access ONLY via nameOf/governingFactionOf/factionArchetype/npcInFaction; store-action lifecycle (new/renamed action -> operationRegistry row + compendium regen + label walkers).
TESTING: single-threaded ALWAYS (--no-file-parallelism); exit codes unmasked, never through pipes; every fix ships pins; run the determinism/golden suites nearest anything you touch and quote counts. Label CONFIRMED/PLAUSIBLE; end on evidence.`

const FIX_SCHEMA = { type: 'object', required: ['filesTouched', 'pins', 'testsRun', 'testOutputTail', 'behaviorShift', 'deferrals'], properties: { filesTouched: { type: 'array', items: { type: 'string' } }, pins: { type: 'number' }, testsRun: { type: 'string' }, testOutputTail: { type: 'string' }, behaviorShift: { type: 'string' }, deferrals: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' }, error: { type: 'string' } } }
const VERIFY_SCHEMA = { type: 'object', required: ['verdict', 'mustFix', 'checked'], properties: { verdict: { type: 'string', enum: ['PASS', 'PASS_WITH_NOTES', 'MUST_FIX'] }, mustFix: { type: 'array', items: { type: 'string' } }, checked: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } }
const GATE_SCHEMA = { type: 'object', required: ['newReds', 'suitesRun', 'outputTail'], properties: { newReds: { type: 'array', items: { type: 'string' } }, suitesRun: { type: 'string' }, outputTail: { type: 'string' }, notes: { type: 'string' } } }

const LANES = [
  { key: 'toggles-and-terms', brief: `TWO SMALL OWNER CALLS, both ratified. (a) LOWER_IS_BETTER PROBLEM TERMS: the frozen three (collapsed/critical/strained) make a benign band on the lone lower-is-better causal variable print "Criminal opportunity · ADEQUATE" — which reads as crime being adequate. Extend the benign-side vocabulary for lower-is-better variables (TWO strings, the R-5b Batch-1 deferral; read src/domain/state/bands.js + the causalBandWord routing and the bandPolaritySingleSourceScan walker FIRST — the walker pins the frozen-three table, so extend the TABLE AND THE WALKER together, both directions). Each string vetoable, VOICE-clean, band vocabulary consistent with the existing register. (b) PER-CHARACTER LOCK TOGGLES (the R-5b Batch-2 deferral, owner-ratified): the locks ENGINE already supports id arrays under locks.npcs (pinned by locksSurviveReroll + locksEngine tests); add the per-row control to the NPC roster rows (NPCsTab — read its PIN affordance first; the two promises MUST be visually and verbally distinct per the legibility law: PIN guards prose from the AI, LOCK keeps the person through rerolls; the mount comment records this exact question). Wire through setLock (it persists since R-5b). Pins: row toggle arms/disarms the id in locks.npcs; a locked NPC survives a section reroll driven from the UI path; PIN unaffected; free-tier visibility matches the existing roster affordance rules.` },
  { key: 'deadops-retire', brief: `DEAD-OP DISPOSITIONS, RETIREMENT HALF (owner-ratified; the 10 held ops' reasons live in the DEAD_OPERATIONS comment of tests/store/deadOperationRatchet.test.js — read it first). Execute the test-collateral retirements: refreshSystemState (rewrite the ~8 test files using it as a harness helper onto a real path — read each usage; a harness that needs derived state can call deriveSystemState directly), replaceAllPlacements (rewrite its 3 worldMap UI tests + RE-ANCHOR the ratchet's inert-binding positive control to a synthetic fixture), the toggle cluster mergeInstitutionToggles/resetToggles/resetGoodsServices (toggleSlice.scope.test.js rewrites), addCredits/spendCredits (creditsAndNarrateSignal rewrite — CAREFUL: paid-surface adjacent; retire the REGISTRY rows + store actions ONLY if truly callerless in src, the credits SQL/edge lanes are NOT yours), clearCampaignWizardNews (campaignSlice.worldPulse rewrite), setRegionalChannelVisibility (2 tests + the domain-helper twin), resetConfig + handleImportDirect + setNeighbourRelType (their configDirectWriterExemptions walker entries SHRINK in the same edit). For EVERY retirement: prove callerless in src first (grep beyond the ratchet's own census), registry row removal, ratchet list shrink, ONE compendium regen at lane end. Any op that turns out to have a live caller: KEEP + record why the held-reason was stale. renameFaction and requestProgression are NOT yours (other lanes/programs).` },
  { key: 'deadops-wire', brief: `DEAD-OP DISPOSITIONS, WIRING HALF (owner-ratified). Wire the four UI candidates: (1) revertSingleEdit — per-edit revert控 on BOTH pending-edit review surfaces (the standalone PendingChangesBar and the Workbench Change Dock; read both mounts + the R-5b flag-on coupling comments; keep hunks minimal — a blackout-residual cure lands on these mounts LATER, do not restructure them); (2) destroySavedSettlement — the type-the-name confirm control in SettlementsPanel (the action already demands confirmName since R-1; the Library row needs the affordance; read the R-1 confirm-gate pins first); (3) resetAllToggles — the Create-flow reset control beside the toggle grids; (4) bulkSetGoods/bulkSetServices — parity controls on the goods/services grids IF the grids exist (read them; if no grid surface exists, record retire-instead with evidence and shrink the ratchet). Every wired op: it leaves the dead list (ratchet shrink), its registry row's description stays honest, copy one-string vetoable, pins per control (dispatch reaches the action; refusals surface typed).` },
  { key: 'faction-rename-door', brief: `#14 FACTION-RENAME CONVERGENCE + DOOR (owner-signed in "do all of these", dropped by R-5b — read atlas presentation-scene gaps 1/1b + power-faith gap 1 + the R-2 finding that renameFaction has ZERO callers first). The recorded direction: take the STORE lane's list resolution + dual-write AND the library lane's cascade — converge them so one rename updates every surface (roster, powerStructure, wizardNews reverse links, scene labels) on GENERATED saves, then build the DOOR (a rename affordance at the faction's natural edit surface — read where factions render and where the R-2 Workbench EntityInspector shows them; premium-gated via viewerCanAuthor, consistent with the R-5b alignment). FACTION-ACCESS LAW binds every read. Registry row honest + compendium regen; pins: rename cascades everywhere (census the render surfaces with a denominator), refusals typed, free tier read-only.` },
  { key: 'versionstab-build', brief: `#18 VERSIONSTAB FEATURE BUILD (owner-signed, dropped by R-5b): make the paid pitch TRUE. (1) MANUAL SNAPSHOT ON DEMAND: recordSnapshot is registered AND armed (undoToken revertToSnapshot) — wire the button on VersionsTab (read the R-1 pitch-honesty edits + versionsTabPitchHonesty.test.js first; the copy can now sell what exists — restore honestly, each string vetoable). (2) SIDE-BY-SIDE DIFF: a read-only comparison view between two version-history snapshots (read the snapshot shape + how versionHistory renders; the diff view is a LAZY leaf — measure; LEGIBILITY LAW: translate field diffs into plain sentences where the vocabulary exists, table for the rest). Premium gating consistent with the tab's existing gate. Pins: snapshot button dispatches recordSnapshot + row appears; diff renders known-different fixtures; free-tier sees the existing locked card; pitch copy matches shipped reality (update versionsTabPitchHonesty in lockstep, both directions).` },
]

async function withRetry(mk, label) {
  let r = await mk(label)
  if (!r) { log(label + ' died — retrying once'); r = await mk(label + ':retry') }
  return r
}

phase('Fix')
log('Owner tail implement wave 1: five Opus lanes')
const fixes = await parallel(LANES.map((l) => () =>
  withRetry((label) => agent(COMMON + '\n\nYOU ARE AN OPUS IMPLEMENTER (owner model split: Opus implements+verifies; Fable architected this brief).\n\n' + l.brief, { label, phase: 'Fix', model: 'opus', effort: 'high', schema: FIX_SCHEMA }), 'fix:' + l.key)
    .then((r) => ({ lane: l.key, fix: r }))
))
const fixSummary = fixes.filter(Boolean).map((f) => ({ lane: f.lane, files: f.fix && f.fix.filesTouched, pins: f.fix && f.fix.pins, shift: f.fix && f.fix.behaviorShift, deferrals: f.fix && f.fix.deferrals }))
log('Fix lanes: ' + fixSummary.map((f) => f.lane + (f.files ? ' ok' : ' FAILED')).join(', '))

phase('Verify')
const mkVerify = (laneKeys, focus, label) => () => withRetry((lbl) => agent(
  COMMON + `\n\nYOU ARE AN OPUS ADVERSARIAL VERIFIER for tail-wave-1 lanes ${laneKeys.join(', ')}. Fixer reports: ${JSON.stringify(fixSummary.filter((f) => laneKeys.includes(f.lane)))}\n\nBriefs:\n${LANES.filter((l) => laneKeys.includes(l.key)).map((l) => l.brief).join('\n\n')}\n\nRepo READ-ONLY except running tests. ${focus} Fresh reads, RUN the pins + nearest golden suites, attempt refutation. ZERO golden movers are declared — any mover is MUST_FIX. MUST_FIX only for real unresolved items.`,
  { label: lbl, phase: 'Verify', model: 'opus', effort: 'high', schema: VERIFY_SCHEMA }), label)

const verifies = await parallel([
  mkVerify(['toggles-and-terms', 'deadops-retire', 'deadops-wire'], 'Registry/roster substrate: the walker-and-table lockstep on the problem terms; PIN-vs-LOCK legibility distinctness; every retirement proven callerless beyond the census; harness-test rewrites still test what they tested; ratchet controls non-vacuous; wired ops genuinely dispatch.', 'verify:ops'),
  mkVerify(['faction-rename-door', 'versionstab-build'], 'Feature substrate: rename cascade census with denominator (grep beyond the fixer list — roster, power, news, scene, PDF); faction-access law compliance on every read; VersionsTab pitch-vs-shipped lockstep both directions; snapshot/diff against real version-history shapes; premium gates consistent; lazy-leaf measurements on the real build.', 'verify:features'),
])

const anyMustFix = verifies.filter(Boolean).some((v) => v && v.verdict === 'MUST_FIX')
const anyDead = fixes.some((f) => !f || !f.fix) || verifies.some((v) => !v)

let gate = null
if (!anyMustFix && !anyDead) {
  phase('Gate')
  gate = await withRetry((lbl) => agent(COMMON + `\n\nYOU ARE THE FABLE GATE VALIDATOR. TAIL WAVE-1 GATE. Run single-threaded, exit codes unmasked, log files never pipes: tests/lint FULL; tests/copy FULL; tests/store FULL; tests/domain FULL (chunk if needed); tests/joins FULL; tests/architecture FULL; tests/components + tests/ui FULL (chunk); tests/generators + tests/simulation + tests/property + tests/application FULL (ZERO golden movers declared); pdf parity; tests/docs FULL; tests/lib FULL; tests/security FULL; npm run typecheck + npx tsc --noEmit + npm run typecheck:domain:strict; npm run build then VERIFY_DIST=1 npx vitest run --no-file-parallelism tests/build/ (baseline 293/293). Run the NEW wave test files explicitly with counts. Attribute every red baseline/FOREIGN/THIS-WAVE with executed evidence.`, { label: lbl, phase: 'Gate', model: 'fable', schema: GATE_SCHEMA }), 'gate:tail1')
} else {
  log('Gate SKIPPED: ' + (anyMustFix ? 'MUST_FIX present' : 'a lane/verifier died twice') + ' — orchestrator handles next')
}

return { fixes: fixSummary, fullReports: fixes.filter(Boolean).map((f) => ({ lane: f.lane, tail: f.fix && f.fix.testOutputTail, notes: f.fix && f.fix.notes })), verifies, gate, gateSkipped: !gate }