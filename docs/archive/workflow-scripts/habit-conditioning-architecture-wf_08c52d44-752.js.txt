export const meta = {
  name: 'habit-conditioning-architecture',
  description: 'Architect the HABIT-CONDITIONING directive comprehensively: learning-as-state (frozen curves, graded-outcome inputs), believed-doctrine, the anticipation counterforce, the doctrine-sheet espionage economy — cohesive with everything',
  phases: [
    { title: 'Census', detail: 'four substrate lenses in parallel, read-only' },
    { title: 'Architect', detail: 'one compiling architect, the full volume draft' },
    { title: 'Cohesion', detail: 'adversarial critic, executed-logic scrutiny' },
    { title: 'Amend', detail: 'absorb refinements 8-17 + close the twelve findings under chair rulings' },
    { title: 'Recohere', detail: 'fresh-eyes verification of the amended volume' },
    { title: 'Polish', detail: 'the odds-ratio law + the nine residuals' },
    { title: 'Final', detail: 'the closing verification' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const OUT = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c44e5d99-2ba5-49d5-a554-40b68534c8eb/scratchpad/habit-arch'
const DIRECTIVE = '/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/memory/habit-conditioning-directive.md'

const LAWS = `You work for the session chair of an owner-delegated build program on SettlementForge, in ${TREE} (branch claude/composite-r4). READ-ONLY on the repo: never edit/create/delete a repo file, never run vitest/npm/any gate (a committing lane may hold the tree), never run a state-mutating git command. You write ONLY under ${OUT}/ (create it if needed) and python3-byte-scan every file you write (zero NULs, zero stray control bytes). LIVE CODE OUTRANKS EVERY DOCUMENT AND MEMORY: for each load-bearing premise, execute the grep/read that proves it at CURRENT HEAD; label rows CONFIRMED (symbol + module quoted) or REFUTED (prominently — the J-WR-13 stop rule binds). Navigate by SYMBOL. Your final message is a data return.

THE DIRECTIVE (owner, 2026-08-05 — LAW when architected; full text at ${DIRECTIVE}): actors LEARN, as STATE never as function-mutation. Weighted decisions gain one new INPUT — habitStrength(actor, circumstanceClass, action) — derived deterministically from the world's own GRADED OUTCOMES: success in a circumstance reinforces the action's relative weight, failure decays it. This applies to ANYTHING that regards decision-making for the actors. THE COUNTERFORCE: learned predictability is itself an espionage surface — allies and enemies ANTICIPATE known habits in their own strategy (reading their BELIEF about the other's habits, never the truth), doctrine sheets become a spy product, planted doctrine becomes disinformation, and the does-B-know-A-knows war folds into the existing information machinery. THE ACCEPTED REFINEMENTS (chair, vetoable): frozen owner-signed curves (learning rate / decay half-life / caps / floors) with learned INPUTS only; TILT NEVER LOCK (capped + floored, no option to zero); TYPED circumstance classes from a closed vocabulary; CREDIT AT CLOSE only (the MISSION_GRADES precedent — outcomes grade the opening action when the circumstance closes); habits ride the EXISTING decay law (decayTowardNeutral family — never a second decay law); ANTICIPATION BOUNDED AT LEVEL ONE, belief-routed, one typed factor, no regress; the SELF-BALANCING LOOP is design law (known habits are exploitable — the information war regulates variance; a watched actor's deviate-from-pattern candidate gains weight with counterintelligence awareness); DARK-BUILDABLE (flag family, CQ5 trios, four-fence, dark worlds byte-identical). Replay-exactness is NON-NEGOTIABLE: same history yields same habits; THE PROMISE stands.`

phase('Census')
const [c1, c2, c3, c4] = await parallel([
  () => agent(LAWS + `

CENSUS 1 — THE DECISION-SURFACE DENOMINATOR. The directive covers ANYTHING that regards actor decision-making, so build the complete list: every weighted-choice site in src/domain — the war chooser (settlementStrategy), conquest intent/feasibility, tradeWar evaluation, treaty/pact answer machinery, envoy negotiation and parlay choices, standoff resolutions, ransom choices, compromise rounds, the sovereignty buyer, espionage gate/doctrine choices, migration/demographics responses, faction pushes, and anything else a multi-spelling sweep finds (search: weightedPick callers in decision contexts, scorer/chooser/intent/decide/evaluate symbol families, candidate-scoring loops). For EACH site: (1) file + symbol, (2) how its weights are computed today (what context feeds them), (3) its CANDIDATE vocabulary (closed set? where spelled), (4) whether a CIRCUMSTANCE with a graded CLOSE exists for it today (what opens, what closes, is the outcome graded anywhere), (5) disposition: HABIT-READY (close exists, vocabulary closed) / NEEDS-A-CLOSE (the circumstance never grades) / NOT-A-DECISION (with reason). Report the ledger with its denominator. Also: whether each chooser reads TRUTH or BELIEF today (the anticipation seam cares). RETURN JSON: denominator (int), sites (the 5-field rows), hazards, notes.`, { label: 'census:decision-surfaces', phase: 'Census', model: 'opus' }),
  () => agent(LAWS + `

CENSUS 2 — THE GRADING + CIRCUMSTANCE SUBSTRATE. Measure what credit-at-close can stand on TODAY: (a) every graded-outcome structure in src/domain — MISSION_GRADES (espionage volume), war outcome/termination records, treaty compliance/breach lifecycles, trade-war outcomes, occupation outcome states, standoff/parlay resolutions — for each: where it lives, its grade vocabulary, whether it references the DECISION that opened the circumstance (the credit-assignment join: can the close find its opening action, or is that join absent); (b) the TYPED situation vocabularies the closed circumstance-class set could be built from — stressor types, war states, casus families, scarcity/conditions bands, whatever closed vocabularies describe a situation an actor decides IN; enumerate them with sizes; (c) the decay machinery verbatim (decayTowardNeutral + HALF_LIFE_BANDS — signature, where derived from, who rides it today) since habits must ride it; (d) the outcome-stream architecture (the WR-9d lesson: read the OUTCOME STREAM not year-boundary snapshots) as the grading feed. RETURN JSON: gradedCloses (rows), circumstanceVocabularies (rows with sizes), decayMachinery (verbatim contract), hazards, notes.`, { label: 'census:grading-circumstances', phase: 'Census', model: 'opus' }),
  () => agent(LAWS + `

CENSUS 3 — THE BELIEF + ESPIONAGE INTEGRATION SUBSTRATE. Measure the machinery the counterforce rides: (a) the belief-axis extension pattern at HEAD (beliefAxes.js six exports + the SP-B subject-family precedent — how a believed-DOCTRINE family would fold: the exact extension recipe a new axis family follows, quoted); (b) the ES tap ladder (TAP_LEVELS performance/beliefs/delta in the landed docs/DESIGN_FP_ARCH_ES.md + whatever ES-0 landed in src/domain/worldPulse/espionage/) — where a doctrine-sheet tap product slots by access depth; (c) the plant machinery (disinformationPlant.js + the IN-0a transport ruling) — how PLANTED doctrine would ride it; (d) the anticipation seam candidates: where a chooser reads the COUNTERPARTY today (negotiationPictures/envoyNegotiationPictureBuilder — the press/picture pattern; conquestFeasibility reading the target) — the sites where a believed-habit term would enter an existing weight formula; (e) the dossier surface for a doctrine sheet (how existing DM-facing panels read belief state — the projection pattern); (f) counterintelligence awareness state (does any watched-ness/notoriety read exist the deviate-from-pattern candidate could key on — the ES notoriety/taint machinery). RETURN JSON: axisExtensionRecipe (quoted), tapLadder (rows), plantRoad, anticipationSeams (rows), dossierPattern, counterintelReads, hazards, notes.`, { label: 'census:belief-espionage', phase: 'Census', model: 'opus' }),
  () => agent(LAWS + `

CENSUS 4 — PERSISTENCE, TUNING, FLAGS, AND THE TWO SIBLING VOLUMES. Measure: (a) the ledger-home pattern for the habit ledger (spatialLedgers conventions: getSpatialLedger/setSpatialLedger, drop-when-absent, single-writer scans — quote the newest precedent) and the FULL lifecycle-path inventory a new persisted sub-ledger owes (the epoch volume at ${OUT}/../epoch-arch/DESIGN_EPOCH_ARCHITECTURE-DRAFT.md section 8.4 just mapped fourteen paths — read its table and report which apply); (b) THE EPOCH INTERACTION (load-bearing cohesion): under living futures, a re-drawn future re-derives habits from the NEW history deterministically — walk both volumes' claims and state the joint law (habits are DERIVED state; the collapse/backtrack paths; what the epoch volume's byYear stamp machinery means for habit replay); (c) the tuning surface shape (validateTuningBands manifest v2, the 9 ratified soak bands, the section-7 Bands machinery + reconciliation walker — what a habit-curve constant family costs and where its Bands line lands); (d) flag naming + CQ5 surfaces at HEAD (the manifest tail verbatim; certification row homes); (e) the chartered-prefix question: would a HB/DC volume mint cross-layer couplings (read the couplingInclusion LAYER_PATTERNS at HEAD + the eleven-prefix registry) — is a TWELFTH prefix owed at its fold; (f) the war-chooser spy-wiring wave (the signed owner-queue item) — its recorded scope, so the joint landing is architected not assumed. RETURN JSON: ledgerPattern, lifecyclePaths (which of the fourteen apply), epochJointLaw (stated), tuningCost, flagSurfaces, prefixVerdict, warChooserWave, hazards, notes.`, { label: 'census:persistence-siblings', phase: 'Census', model: 'opus' }),
])

phase('Architect')
const arch = await agent(LAWS + `

YOU ARE THE COMPILING ARCHITECT. The four censuses: ` + JSON.stringify({ c1, c2, c3, c4 }) + `

Also read for cohesion: docs/DESIGN_FP_ARCHITECTURE.md sections 1, 3, 5, 10 (binding laws); docs/DESIGN_FP_ARCH_ES.md (the tap ladder + plants this extends); docs/DESIGN_FP_ARCH_SP.md (the axis family this joins); the epoch draft at ${OUT}/../epoch-arch/DESIGN_EPOCH_ARCHITECTURE-DRAFT.md (header + section 8.4 — the sibling whose laws must agree); docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md section 10.

COMPILE ${OUT}/DESIGN_HABIT_ARCHITECTURE-DRAFT.md — the complete build volume for HABIT-CONDITIONING (the ES/WY/EP volumes are the house template): section 0 THE PREMISE with the owner directive verbatim, the learning-as-state resolution (why replay + explainability SURVIVE — state the argument), and the self-balancing loop as design law; section 1 THE CANONICAL MODEL — the habit ledger (keys: actor x circumstanceClass x action; bounded strength; drop-when-neutral; ONE writer; zero new top-level keys via the spatialLedgers home), the believed-doctrine axis family (riding the SP-B extension recipe), and the closed CIRCUMSTANCE-CLASS vocabulary (built from census 2's typed vocabularies — enumerate the initial set, sized, with its totality walker); section 2 THE FLAG FAMILY (name them — likely habitConditioningEnabled + anticipationEnabled + doctrineTapEnabled or the architect's better cut; CQ5 trios; four-fence x N with the guard-door corollary; DARK-STATE BYTE-IDENTITY contract); section 3 THE MECHANISMS — (3a) the grading fold: credit-at-close riding the graded-outcome structures census 2 found, with the credit-assignment join specified per close (and NEEDS-A-CLOSE sites dispositioned: which get closes minted, which defer); (3b) the frozen curve: reinforcement + decay riding decayTowardNeutral, the cap/floor constants, ONE global curve + minimal per-domain multipliers, all owner-signed, the Bands line + section-7 rows; (3c) the chooser read: habit as ONE typed factor entering each HABIT-READY site from census 1 — every site enumerated with its seam, a site with no disposition row is a build STOP; (3d) the anticipation term: belief-routed, LEVEL ONE, one typed factor, entering the anticipation seams census 3 found; (3e) the doctrine sheet: the dossier projection + the ES tap product by access depth + PLANTED doctrine on the plant road; (3f) the deviate-from-pattern candidate keyed on counterintelligence awareness; (3g) receipts: habit as a NAMED reason family in the finite semantics (the news address law: the sentence shapes, pool obligations); section 4 THE WAVES (HB-0..n, dependency-ordered, the JOINT landing with the signed war-chooser spy-wiring wave architected explicitly, each wave: charter, gates, ledger row per section-10 protocol, OPUS-ERA tagging note per START_HERE 6b-3 if landed during the Opus era); section 5 QUEUE INSERTION verbatim-landable (the chair's sequencing recommendation with rationale: after the ES spine so the tap machinery exists, joint with the war-chooser wave, BEFORE the owner-sequenced instrument-hardening wave and the soak); section 6 JUDGMENT BLOCKS (vetoable); section 7 OPEN CHAIR QUESTIONS max 5 with recommendations; section 8 SEAMS PINNED BOTH SIDES — the SP axis seam, the ES tap/plant seams, the war-chooser joint, THE EPOCH JOINT LAW (re-drawn futures re-derive habits; the collapse paths; state both volumes' agreement explicitly), the tuning seam, THE PROMISE statement (learning-as-state does not breach it — the argument in full), and the complete Lifecycle-paths clause for the habit ledger over census 4's applicable paths. Sizing: target 1,200-1,800 lines — exhaustive means every seam named. Byte-scan. RETURN JSON: file, lineCount, waves, flagNames, circumstanceClassCount, chairQuestions (count), anomalies (census contradictions found while compiling).`, { label: 'architect:habit-volume', phase: 'Architect', model: 'opus', effort: 'high' })

phase('Cohesion')
const coh = await agent(LAWS + `

YOU ARE THE ADVERSARIAL COHESION CRITIC. The architect reported: ` + JSON.stringify(arch) + `

Read ${OUT}/DESIGN_HABIT_ARCHITECTURE-DRAFT.md WHOLE and verify against the live tree with EXECUTED-LOGIC SCRUTINY. Your default posture: the volume contains a defect. Check specifically: (1) REPLAY-EXACTNESS — walk the habit derivation end to end: graded outcome -> ledger write -> chooser read; any wall-clock, cross-world, or non-history input anywhere breaks THE PROMISE and is a HIGH; the epoch joint law must hold in BOTH volumes' terms (re-drawn future re-derives habits deterministically; the fourteen lifecycle paths); (2) VARIANCE MATH — do the cap/floor/decay shapes actually prevent lock-in: take the stated constants and walk a worst-case reinforcement sequence by hand (repeated success in one circumstance) — if the likely thing becomes near-certain, the tilt-never-lock law is breached on the volume's own numbers; conversely confirm the deviate-from-pattern + anticipation loop applies real counter-pressure at the stated weights; (3) LEVEL-ONE BOUNDARY — hunt hidden regress: does any anticipation read itself read a habit that was shaped by anticipation of anticipation (a fixed-point loop the level-one law forbids), and is the boundary stated as an executable pin; (4) CHOOSER TOTALITY — census 1's denominator vs section 3c's enumeration: every site dispositioned, the STOP law present, and spot-verify three sites' seams against live code by symbol; (5) CREDIT ASSIGNMENT — for three graded closes, walk the join from close back to opening action in live code: if the join is absent and the volume claims it, REFUTED; (6) DARK-STATE byte-identity by construction (the empty-suffix/absent-ledger pattern, no epoch-0-style residue); (7) COHESION with all eleven landed volumes + the epoch draft + the two owner-sequenced constraints (the hardening wave stays last-before-soak; the war-chooser joint respects that wave's recorded scope); no gated item executed (tuning raw, nothing lit); (8) the receipts — habit reasons ride the finite semantics with pool obligations stated, never freeform. RETURN structured JSON only.`, { label: 'verify:habit-cohesion', phase: 'Cohesion', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings', 'notes'], properties: { verdict: { type: 'string', enum: ['COHESIVE', 'NEEDS-REVISION'] }, findings: { type: 'array', items: { type: 'object', required: ['severity', 'claim'], properties: { severity: { type: 'string', enum: ['HIGH', 'MEDIUM', 'LOW'] }, claim: { type: 'string' }, evidence: { type: 'string' } } } }, notes: { type: 'string' } } } })

phase('Amend')
const amend = await agent(LAWS + `

YOU ARE THE AMENDMENT ARCHITECT. The cohesion verdict (NEEDS-REVISION, twelve findings) and the architect's own compile anomalies: ` + JSON.stringify({ coh, archAnomalies: arch && arch.anomalies }) + `

FIRST, THE DIRECTIVE GREW (anomaly A1 — MANDATORY): re-read the live directive at ${DIRECTIVE} — it now carries SEVENTEEN refinements; the censuses saw only 1-7. ABSORB 8-17 into the volume comprehensively: (8) INSTITUTIONAL-BY-DEFAULT with the seat-change decay shock (per-NPC habits only where per-NPC machinery exists); (9) the circumstance vocabulary stays at or under 12, growth owner-signed (the volume's 12 stands — pin the ceiling); (10) COLD-START = DARK-STATE = NEUTRAL as one fence proving three laws; (11) NO CROSS-CLASS BLEED in v1, recorded as an owner future-widening; (12) SEVERITY-BANDED REINFORCEMENT via the existing SEVERITY_LADDER (already partially in — make it total); (13) OBSERVED-DOCTRINE ACCRUAL (believed-doctrine accrues from PUBLIC actions via the existing news/belief roads, hop-worn; the ES tap JUMPS it toward accuracy — the counterforce works before espionage lights); (14) OWN-SHEET TRUTH; (15) THE GRADED-CLOSE PRECONDITION as stated law (the 30 NEEDS-A-CLOSE rows are the visible deferral book, each with close-minting as nameable future work); (16) LAW-BAND VOLATILITY MODULATION — the learning-rate + half-life constants modulated by lawWordFor (lawless hot/fast-forgetting, lawful slow/long, balanced base; three bands x two constants in the frozen family; caps/floors UNIVERSAL) WITH the intelligence-shelf-life consequence (believed-doctrine decays on the target's modulated clock) stated in the ES seam; (17) THE NAMED-DOMAIN CHECKLIST — walk the 50-site census against the named domains (trade partnerships/embargoes/caravan routing · faith traditions/patrons/clergy · strategy · route creation · route CHOICE incl. cost-argmax forks with the CONVERT/STAY/DEFER call per site · inter-settlement relationship forks · goals selection/persistence/ORIENTATION) and disposition each BY NAME in its own section table; any named domain absent from the census gets its own measured row now.

ALSO anomaly A2: the war-chooser spy-wiring is SIGNED (the owner's blanket queue sign-off) — update every clause that treats it as parked; anticipation MAY wire there per its recorded scope.

THEN CLOSE THE TWELVE FINDINGS, chair-ruled: (H1, softmax exponent) — the idiom table gains a per-site-class application law: at softmax sites the habit load applies to the POST-softmax sampled weights as a renormalized multiplicand on probability (never to the pre-softmax score), and the tilt-never-lock pin MEASURES THE PROBABILITY RATIO at every site class with the per-idiom conversion stated; (H2, clamp annihilation) — RULED asymmetry-by-design: at saturated scores habit cannot push past certainty, only drag — the claim respells to match the idiom (conservative reinforcement: habit never overrides saturation), stated as law with the asymmetry named, never silently; (H3, the episode key) — keys are MINTED AT OPEN AND CARRIED (one stamped field on the opening record) for every close whose record mutates; audit all seven joined closes into carried vs recomputed with the carried set enumerated; (H4) — convergence.reactiveResponse is refuted: drop seam 4 and site 11 to a DEAD-CODE disposition row (chair-owed report, never a delete); (H5) — the fortify branch is LIVE: retract the delete ruling, disposition the branch properly, and correct the false behaviour-identity proof. (M1) the proseNumerics hazard note updates to discharged-at-HEAD; (M2) the CHANNEL_BANDS borrow respells one-directional (HB imports the source of truth, one walker asserts the import is verbatim — the both-directions equality is unimplementable as specified); (M3) NAME the tick-to-week conversion contract now (the symbol, its home, and what the ledger shape must carry for it — resolve the VERIFY-AT-BUILD into a specified seam); (M4) the preview law DEFERS to the epoch volume's statement with a cross-reference — this volume claims no authority over that document; (M5) the load formula becomes PER-SITE ARITY (monadic sites take the habit factor alone; the anticipation factor applies only where a subject exists — the universal formula respells as the dyadic case). (L1) fix the false grep receipt; (L2) correct site 10's addressability row to what is measured. Update every count the amendments move; keep everything the critic confirmed (the single-draw replay chain, dark-state by construction, the gate ladder) untouched. Byte-scan. RETURN JSON: refinementsAbsorbed (8-17 each with one-line evidence), closures (twelve), namedDomainTable (the 17-checklist summary), anomalies.`, { label: 'amend:habit-volume', phase: 'Amend', model: 'opus', effort: 'high' })

phase('Recohere')
const coh2 = await agent(LAWS + `

YOU ARE THE FRESH-EYES RE-VERIFIER (you have not seen this volume; trust nothing). The amendment lane reported: ` + JSON.stringify(amend) + `

The prior twelve findings: ` + JSON.stringify(coh && coh.findings) + `

Read ${OUT}/DESIGN_HABIT_ARCHITECTURE-DRAFT.md WHOLE and verify against the live tree: (1) each of the twelve findings genuinely closed — re-derive, especially H1 (walk the softmax math yourself: does the post-softmax multiplicand keep the probability ratio inside the stated caps at the anchor site's real STRATEGY_K) and H3 (pick two mutating closes and confirm the carried-key spec is executable against the real record shapes); (2) refinements 8-17 EACH genuinely absorbed — spot-verify 16 (the volatility modulator reaches both the habit clock AND the believed-doctrine clock, with the shelf-life consequence in the ES seam) and 17 (the named-domain table dispositions every named domain with real site rows, and route-choice cost-argmax forks carry explicit CONVERT/STAY/DEFER calls); (3) the war-chooser clauses reflect the SIGNED status; (4) counts coherent everywhere; (5) nothing the first critic confirmed has regressed. RETURN structured JSON only.`, { label: 'recohere:habit-volume', phase: 'Recohere', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings', 'notes'], properties: { verdict: { type: 'string', enum: ['COHESIVE', 'NEEDS-REVISION'] }, findings: { type: 'array', items: { type: 'object', required: ['severity', 'claim'], properties: { severity: { type: 'string', enum: ['HIGH', 'MEDIUM', 'LOW'] }, claim: { type: 'string' }, evidence: { type: 'string' } } } }, notes: { type: 'string' } } } })

phase('Polish')
const polish = await agent(LAWS + `

YOU ARE THE POLISH LANE. The recohere verdict (ten findings, the first mathematical): ` + JSON.stringify(coh2) + `

CLOSE ALL TEN, chair-ruled: (P1, H1 — THE ODDS-RATIO LAW) the tilt-never-lock invariant respells on PAIRWISE ODDS RATIOS: for any two candidates i and j, (p-prime_i / p-prime_j) / (p_i / p_j) lies in [(1−s)/(1+s), (1+s)/(1−s)] where s = HABIT_SPAN — which IS true by construction for a bounded per-candidate multiplicand under renormalization (the factor ratio m_i/m_j survives normalization untouched); the per-probability claim is DELETED as false; the pin measures pairwise odds ratios at every site class; restate the by-construction argument on factor ratios and hand-walk the anchor site at its real STRATEGY_K. (P2, H2) measure the counterpart presence PER MOVE at site 7 — arity becomes per-MOVE where the subject exists only on some moves (deploy dyadic, the rest monadic); correct seam 4-prime. (P3, H3) land the CLOSE-COVERAGE TABLE per ACTION at every joined site — close 1 grades deploy ONLY, said plainly; every closeless action explicitly NEEDS-A-CLOSE in the deferral book; where an existing grade already covers an action (the sue arm via the treaty grades at site 6), join it rather than deferring. (P4, H4 — THE BELIEVED CLOCK) believed-doctrine decays on the OBSERVER'S BELIEVED target law band (the believed nature/law bands exist — belief-routed, genuinely no new machinery): you discount intelligence by how volatile you BELIEVE the target to be — MORE cohesive than the truth-clock and it makes shelf life itself deceivable (a target that plants a chaotic image devalues stolen intelligence about itself — record that consequence). (P5) the traditions row reclassifies honestly (score-then-draw is not a selection fork): STAY-DETERMINISTIC with the stated reason, or name the true fork if one exists upstream. (P6) the weeks-never-ticks sweep: all four residual tick spellings corrected including the verbatim-landing text and the walker-tripwire contradiction. (P7) thirteen rows, ONE walker assertion spelling. (P8) the Bands law rewritten against the REAL convention: Bands lines on tuning-bearing waves only, reconciled against the volume's own TUNING section (never section 7, which is chair questions); nine waves without tuning carry no-bands-by-design notes. (P9) the arity field lands on EVERY joined fork-registry row; the HB-7 seam charter reconciled with the five contradicted sites. (P10) the byte-cost numbers reconciled to section 1.4's measured bound. Absorb the amendment's own A-AM2/A-AM3 refutations (the six-outcome tradition vocabulary; route creation is NOT worldgen — re-disposition it as the live chooser it is). Update every count. Byte-scan. RETURN JSON: closures (ten + the two absorptions), oddsRatioWalk (the anchor-site hand-walk numbers), notes.`, { label: 'polish:habit-volume', phase: 'Polish', model: 'opus', effort: 'high' })

phase('Final')
const habitFinal = await agent(LAWS + `

YOU ARE THE FINAL VERIFIER for the habit volume. The polish lane reported: ` + JSON.stringify(polish) + `

The ten findings it closed: ` + JSON.stringify(coh2 && coh2.findings) + `

VERIFY by re-derivation at current HEAD: (1) THE ODDS-RATIO LAW — walk the math yourself at the anchor site with the real constants (pick three candidate weights, apply a bounded factor to one, renormalize, measure the pairwise odds ratios AND the per-probability ratios — confirm the former sit inside the bound and the claim never again quantifies over the latter); (2) the per-move counterpart table matches the live emitter; (3) the close-coverage table is total over every joined site's action vocabulary with no silent closeless action; (4) the believed clock is genuinely belief-routed (no truth read of the target's band anywhere in the doctrine-decay spec) and the deceivable-shelf-life consequence is recorded; (5) the weeks sweep is total (grep the volume for tick-unit spellings in habit contexts — zero outside the conversion contract); (6) rows, arities, Bands, and byte-costs coherent everywhere; (7) nothing previously confirmed regressed. If everything stands: COHESIVE, and say what the volume now is in one sentence. RETURN structured JSON only.`, { label: 'final:habit-volume', phase: 'Final', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings', 'notes'], properties: { verdict: { type: 'string', enum: ['COHESIVE', 'NEEDS-REVISION'] }, findings: { type: 'array', items: { type: 'object', required: ['severity', 'claim'], properties: { severity: { type: 'string', enum: ['HIGH', 'MEDIUM', 'LOW'] }, claim: { type: 'string' }, evidence: { type: 'string' } } } }, closingSentence: { type: 'string' }, notes: { type: 'string' } } } })

return { c1, c2, c3, c4, arch, coh, amend, coh2, polish, habitFinal }