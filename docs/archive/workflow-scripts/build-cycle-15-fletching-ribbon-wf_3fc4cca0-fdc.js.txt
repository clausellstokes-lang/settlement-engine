export const meta = {
  name: 'build-cycle-15-fletching-ribbon',
  description: 'The nav ribbon becomes the arrow: fletched Create/Library/Realm, Welcome retired on desktop, sharp strokes',
  phases: [{ title: 'Build', detail: 'owner-directed UI change, implement + verify' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); strict/size ratchets (App.jsx and OutputContainer sit AT frozen ceilings — shrinking banks the row, growing reds; budget a leaf extraction if needed per the recorded LD recipe); node ESM link + npm run build stay green; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY: cycles 13/14 are LIVE on worldPulse files and heraldCausalVoice/discourseKernel/causeWalk/stateProseKernel — not yours. NavRibbon.jsx, App.jsx's NAV, theme.js, and the nav test files are yours.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: OWNER-DIRECTED UI implementation (the owner asked for this today, verbatim intent recorded below; pins that encode the OLD ribbon spec are updated as a disclosed owner-directed spec change, cited in each test docstring). ${P10}
LANE FL — THE FLETCHED RIBBON (desktop nav). THE OWNER'S VISION: the top ribbon reads as the back half of an arrow in mid-flight — Create, Library, Realm as the FLETCHING; sharper arrow strokes; Welcome retired from the desktop ribbon (the wordmark is the home button); a more prominent bold-brown accent behind the trio; the fletching may extend slightly past the ribbon's edge (desktop only). THE CHAIR'S REFINEMENTS (build these):
(1) WELCOME RETIRED FROM THE DESKTOP RIBBON: the wordmark/logo button carries home (it already navigates there — verify, and give it aria-label 'Home'); the /  route and any mobile mount are untouched; when on the landing page NO tab is active. Update appNavSsot/nav pins as the owner-directed change with the docstring citing this directive; App.jsx shrinks — bank the ratchet win.
(2) ONE FLETCHED BAND, NOT THREE LOUD BUTTONS: Create/Library/Realm sit inside a single continuous band in a richer leather-brown (derive from the theme's brown family — a new theme token, e.g. FLETCH_BROWN, one writer in theme.js; do NOT hand-roll hex in the component; check WCAG AA contrast for the label text on it and record the ratio). Each of the three reads as a FEATHER: slanted parallel edges leaning FORWARD (the flow direction — toward Realm), achieved with clip-path or skewed borders; the separators between them become SHARP straight angled strokes (retire the curved chevrons on desktop), visually parallel to the feather slant. The reference tabs (Compendium/Gallery/About) stay plain — the visual hierarchy IS the point.
(3) THE OVERHANG IS PAINT, NEVER LAYOUT: the band extends ~4px below the ribbon's bottom edge via overflow-visible painting ONLY — the ribbon's layout height stays EXACTLY CHROME.headerDesktop (60), because ANCHOR_OFFSET = 84 is the load-bearing anchor-scroll constant across About/guide/Compendium; a pin asserts the ribbon's layout height is unchanged (assert the CHROME.headerDesktop consumer chain, and that the band's overhang uses no height/margin that grows the header's box). No top overhang (clips at viewport).
(4) ACTIVE STATE: the active feather brightens (gold edge or lifted brown) — keep the existing active-tab affordance grammar, translated to the feather shape; visible keyboard focus ring preserved on the clipped shape (test it — clip-path can swallow outlines; use outline-offset or a focus style that survives).
(5) MOBILE UNTOUCHED: the bottom nav and its NavFlowArrow stay exactly as they are.
Update navDividers/navFlowArrows/appNavSsot/a11y pins to the NEW spec (each updated docstring cites the owner directive + date); add the fletching pins (band present, three feathers, slant direction, overhang paint-only, Welcome absent on desktop + logo carries home, AA contrast recorded). Run the component suites + eslint + sizeBaseline + rawButtonBaseline (NavRibbon is baselined there — keep the census lawful) + npm run build. One pathspec commit (or two if a leaf extraction is needed). Return {lane:'FL ribbon', commits, gates, stopReports, notes}.`,
  {label:'fl:fletched-ribbon', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify on committed bytes: the ribbon's LAYOUT height is byte-for-byte unchanged where ANCHOR_OFFSET consumers read it (re-derive 84 from theme.js and prove the header box height is still 60 in jsdom computed layout; the overhang must be overflow paint — mutant: convert it to real height → the height pin must red); Welcome absent from the desktop NAV render, present nowhere as a dead route, the wordmark navigates home with the aria-label; the three feathers render with the forward slant (assert the clip-path/transform values), the reference tabs unchanged; focus ring visible on a feather (assert the focus style applies and is not display:none'd by the clip); FLETCH_BROWN single-writer in theme.js (grep: no raw hex for it in components) and the recorded AA ratio ≥ 4.5:1 recomputed by you; every updated pin's docstring cites the owner directive; mobile files untouched (git show --stat); rawButton census lawful; sizeBaseline banked not raised; npm run build green; pathspec vs cycles 13/14. Return {lane:'FL verify', verdict, findings}.`,
  {label:'fl:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }