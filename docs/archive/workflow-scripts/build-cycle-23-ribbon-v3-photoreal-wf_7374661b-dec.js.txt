export const meta = {
  name: 'build-cycle-23-ribbon-v3-photoreal',
  description: 'Ribbon v3: photorealistic arrow — honey-wood shaft from the reference, three true feather-shaped tabs with turkey barring',
  phases: [{ title: 'Build', detail: 'owner reference photo, implement + verify' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY: WR-8 is live on worldPulse; other lanes on domain typing + spine prose + icon families — your territory is nav/*, theme.js, and the nav test files ONLY (App.jsx header block if needed). STANDING LAWS that survive every ribbon version: the overhang is PAINT never layout (header box stays CHROME.headerDesktop; ANCHOR_OFFSET derives); focus ring survives clipping; AA measured against the WORST stripe under each label; single-writer tokens in theme.js; pins updated with docstrings citing the owner directive (2026-08-03 evening, reference photo).`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: OWNER-DIRECTED implementation from a reference photograph (described exactly below — build to it; PHOTOREALISTIC is the explicit goal, superseding the stylized v2). ${P10}
THE REFERENCE (a real arrow, photographed on hide): a pale HONEY-TAN wooden shaft — warm golden wood with fine longitudinal grain lines, slight tone variation along its length, satin sheen; mounted on it a large TURKEY FEATHER — deep chocolate-brown vane crossed by irregular lighter TAN/CREAM horizontal BARRING stripes (the classic wild-turkey pattern: bars of varying width, slightly ragged edges, denser toward the tip), a visible central rachis (quill spine) line, the vane's silhouette elongated with a rounded-back tip and a clean straight leading edge; where the fletching meets the shaft, RED-BROWN thread WRAPPING bands (narrow, slightly glossy) frame the feather front and back.
LANE V3 — THE PHOTOREALISTIC RIBBON:
(1) THE SHAFT: the entire ribbon background becomes the reference's honey-tan wood WITH matching texture — build it as an inline-SVG data-URI texture (deterministic: feTurbulence with a FIXED seed + baseFrequency tuned to fine longitudinal grain, layered under a subtle vertical sheen gradient), color-matched to the photo (warm honey, roughly the #C9A46B..#D9BC8A family — tune by eye against the described photo, single-writer tokens in theme.js). Full-width including under the wordmark; the wordmark keeps its dark-brown ink. The ribbon still separates from the cream body (shadow + hairline).
(2) THE THREE FEATHERS: Create, Library, Realm each become ONE true feather shape — the reference's silhouette (elongated vane, rounded tip trailing toward Realm, straight leading edge), drawn as SVG per tab: deep chocolate vane (match the mobile bottom-nav ink family — the existing dark ink token), the turkey BARRING as irregular lighter tan stripes (authored path/pattern with organic edge wobble — not mechanical repeating-gradient bars; vary widths; denser toward the tip), a fine rachis line, a soft drop shadow lifting the feather off the wood. Each feather PEEKS slightly past the ribbon's border (~6px below the bottom edge — PAINT ONLY, the standing law). The label sits SEMIBOLD in the vane's clearest zone; if the barring fights the text, quiet the bars in a band behind the label (a subtle darkened text zone is period-correct — feathers are darker at the rachis).
(3) THE WRAPPING: the red-brown thread wraps from the photo become two narrow accent bands framing the fletching cluster (before Create, after Realm) — glossy red-brown, 3-4px, riding the shaft. They replace the previous gilt hairline as the cluster's frame; active state = the active feather's barring brightens + the gold underline stays (keep one continuous active grammar).
(4) Reference tabs stay plain dark ink on the wood; mobile keeps its palette (bottom nav untouched); the fletch band's old parallelogram/gilt implementation is RETIRED cleanly (no dead tokens — sweep them).
(5) Pins updated to the new spec (feather silhouette present per tab via the SVG path, barring layers present, wraps present, overhang paint-only, AA vs the LIGHTEST bar stripe under each label — quote ratios, focus ring on the feather). Screenshot the result at 1440x900 and mobile in your notes (describe what you see honestly — if it reads as clip-art rather than photoreal, iterate the texture before committing; you own the eye pass).
One pathspec commit (or two: shaft, feathers). npm run build green. Return {lane:'V3', commits, gates, stopReports, notes}.`,
  {label:'v3:photoreal', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier with an EYE PASS. ${P10}
Report: ${JSON.stringify(r)}. Verify on committed bytes: serve the tree, screenshot desktop 1440x900 + mobile, and JUDGE against the reference description (honey-tan grained shaft; chocolate vanes with irregular tan turkey barring; rachis; red-brown wraps; feathers peeking below the edge) — describe what you actually see; mechanical checks: header layout box unchanged (the paint-only mutant reds), ANCHOR derivation intact, AA recomputed by YOU against the lightest stripe under every label (quote all ratios), focus ring visible on a feather (Tab to it), texture DETERMINISTIC (two renders byte-identical — the SVG seed is fixed), no dead v2 tokens remain (grep the retired names), mobile untouched, all nav suites + build green, pathspec-clean. If the visual verdict is 'stylized, not photoreal', say so plainly with what falls short — that is a finding, not a failure. Return {lane:'V3 verify', verdict, findings}.`,
  {label:'v3:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }