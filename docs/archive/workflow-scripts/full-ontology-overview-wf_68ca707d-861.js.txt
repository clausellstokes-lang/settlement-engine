export const meta = {
  name: 'full-ontology-overview',
  description: 'Census every system and subsystem of the simulation and generator, then synthesize ONE exhaustive owner-facing overview: what each part is, how it interacts with the others, and the experience it creates',
  phases: [
    { title: 'Census', detail: 'twelve parallel domain censuses across generation, simulation, information, and surface' },
    { title: 'Unbuilt', detail: 'the architected-but-unbuilt layers, from their volumes' },
    { title: 'Synthesize', detail: 'one exhaustive overview document, equal weight per part, plain language' },
    { title: 'Check', detail: 'completeness critic — what system was missed, what claim is unverified' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const OUT = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c44e5d99-2ba5-49d5-a554-40b68534c8eb/scratchpad/THE_FULL_ONTOLOGY.md'

const RO = `VERIFY-FIRST PREAMBLE: brief from the session chair. ⛔ STRICTLY READ-ONLY. Work from ${TREE} (branch claude/composite-r4) but NEVER edit, commit, or run any test/npm/vitest/build command — a build lane owns that tree's single vitest lane and has live dirty WIP. You may READ, grep, and run git log/show/status. Record the HEAD sha. LIVE CODE OUTRANKS EVERY DOC: if a design document describes something the code does not do, the CODE is the truth and the gap is itself a finding worth reporting.

YOUR OUTPUT FEEDS AN OWNER-FACING OVERVIEW written in PLAIN LANGUAGE for the product's own builder. So for every subsystem you find, capture THREE things: (1) WHAT IT IS — the mechanism, in two or three plain sentences, no jargon, no file paths in the prose; (2) WHAT IT TOUCHES — which other subsystems feed it and which it feeds, by NAME; (3) WHAT THE USER GETS — the concrete experience a Dungeon Master sitting at a table receives because this exists (a sentence they'd recognize, not an abstraction). Also flag anything DARK (built behind a flag, not yet live) versus LIVE. Keep file paths in a separate evidence field, out of the prose.`

const TAIL = ` RETURN structured prose, max ~2500 words: a list of subsystems, each with WHAT IT IS / WHAT IT TOUCHES / WHAT THE USER GETS / LIVE-or-DARK / evidence (paths+symbols). End with ANYTHING YOU EXPECTED TO FIND AND DID NOT.`

phase('Census')
const cens = await parallel([
  () => agent(RO + `\n\nCENSUS 1 — WORLD GENERATION: the map and the ground. Everything that happens BEFORE time starts running — terrain and topography, the region/realm graph, how settlements are placed and sized, tiers, biomes, resources, roads and route genesis, spatial digests and distance. How a seed becomes a world.` + TAIL, { label: 'c1:worldgen', phase: 'Census', model: 'opus' }),
  () => agent(RO + `\n\nCENSUS 2 — THE SETTLEMENT INTERIOR: population and pools, carrying capacity, the economy inside a town, buildings/districts/infrastructure, daily life, defense, internal security, crime and criminal presence, stressors and crises (including the disease/plague crisis class), conditions and their promotion, contradictions.` + TAIL, { label: 'c2:interior', phase: 'Census', model: 'opus' }),
  () => agent(RO + `\n\nCENSUS 3 — PEOPLE: NPCs and named characters, how they are generated, their traits/alignment/goals/ambitions, bonds and relationships, secrets and plots, roles and seats they occupy, factions' membership, lineage and succession, the pinned-NPC machinery, and how the engine deliberately REFUSES to resolve a named character's fate.` + TAIL, { label: 'c3:people', phase: 'Census', model: 'opus' }),
  () => agent(RO + `\n\nCENSUS 4 — POLITICS AND GOVERNANCE: factions and archetypes, the governing seat, legitimacy, rulership and its books, coups and internal politics, blocs, the settlement strategist and how a town DECIDES anything, disposition ledgers and channels, and the new posture/appetite machinery (strategicPosture, landing now).` + TAIL, { label: 'c4:politics', phase: 'Census', model: 'opus' }),
  () => agent(RO + `\n\nCENSUS 5 — DIPLOMACY: treaties and their term catalog, alliances, vassalage, overlordship, occupation, the sovereignty market (settlements bought and sold), pacts and non-aggression, envoys and errands, negotiation and terms, oath-holders, treaty compliance/breach/lineage.` + TAIL, { label: 'c5:diplomacy', phase: 'Census', model: 'opus' }),
  () => agent(RO + `\n\nCENSUS 6 — WAR: casus belli and why wars start, armies and deployment, fronts, sieges, battles and their resolution, supply and attrition, war exhaustion, coalitions, war termination and peace, spoils and reparations, razing and atrocity, occupation lifecycle, and the sovereignty/market consequences of losing.` + TAIL, { label: 'c6:war', phase: 'Census', model: 'opus' }),
  () => agent(RO + `\n\nCENSUS 7 — ECONOMY AND TRADE: commodities and stocks, production and consumption, trade routes and partnerships, caravans, embargoes and tariffs, market access, scarcity and surplus, commercial casus belli, warehouses and seizure, prosperity, and how economic state reaches the rest of the world.` + TAIL, { label: 'c7:trade', phase: 'Census', model: 'opus' }),
  () => agent(RO + `\n\nCENSUS 8 — BELIEF AND INFORMATION: the believed-world (belief axes, belief maps, per-observer partitions), how news is generated and TRAVELS (hop-worn, delayed, degraded), rumors and their durability, the Herald, information statecraft, plants and disinformation, espionage (ES-0 landed, the rest architected), and how actors act on what they BELIEVE rather than what is true.` + TAIL, { label: 'c8:belief', phase: 'Census', model: 'opus' }),
  () => agent(RO + `\n\nCENSUS 9 — CULTURE, FAITH, AND MAGIC: traditions and how they spread/impose, observance, deities and patrons (the no-premade-pool doctrine and alignment keying), clergy and temples, the faith-agnosticism law (the engine never adjudicates theology), realm magic and its toggles, magic works-at machinery, and how culture changes under contact and occupation.` + TAIL, { label: 'c9:culture', phase: 'Census', model: 'opus' }),
  () => agent(RO + `\n\nCENSUS 10 — MOVEMENT AND FLOW: migration (who leaves, who arrives, why), the movement/mover machinery, columns, refugees, the spatial layer's routes and hops, travel time and the week clock, cargo, and how physical movement couples population to economy to politics.` + TAIL, { label: 'c10:flow', phase: 'Census', model: 'opus' }),
  () => agent(RO + `\n\nCENSUS 11 — THE DM SURFACE: what the user actually sees and touches. The settlement dossier, the realm inspector, the Herald/newspaper, the compendium, editing verbs and the operation registry, DM-vs-player projections and the veil, premium surfaces, campaign persistence/save/undo/regen, the advance-time control, presets, and the generated prose corpus (~10,600 sentences).` + TAIL, { label: 'c11:surface', phase: 'Census', model: 'opus' }),
  () => agent(RO + `\n\nCENSUS 12 — THE ENGINE SUBSTRATE: the world pulse and tick/week clock, the PRNG and its forks (the loaded-dice law: situation-weighted draws on stable composite keys), determinism and replay, the kernel and stages, flags and the gated-rule manifest, the certification/convergence instruments, persistence shape, and the verification estate (walkers, ratchets, goldens, pins) that keeps it all honest.` + TAIL, { label: 'c12:substrate', phase: 'Census', model: 'opus' }),
])

phase('Unbuilt')
const unbuilt = await agent(RO + `\n\nCENSUS 13 — THE ARCHITECTED-BUT-UNBUILT LAYERS. These are designed in full and awaiting build. Read them from the LEDGER branch's durability copy at /Users/cstokes/Desktop/settlement-engine/docs/architected-volumes-pending-fold/ (EPOCH_living-futures_SEALED.md, HABIT_conditioning_round4-snapshot.md, WC_war-circulation_in-progress-snapshot.md) plus ${TREE}/docs/DESIGN_FP_ARCH_ES.md and _WY.md. For EACH of the five programs (EPOCH/living futures, HABIT conditioning, WAR CIRCULATION, ESPIONAGE, WAYFARE) give: what it adds, which existing subsystems it extends, what the user will get, and its build state. Be honest about what is designed versus what exists.` + TAIL, { label: 'c13:unbuilt', phase: 'Unbuilt', model: 'opus' })

phase('Synthesize')
const doc = await agent(`You are writing THE FULL ONTOLOGY OF SETTLEMENTFORGE — an exhaustive, owner-facing overview of every system and subsystem in the simulation and the generator, how they interact, and the experience each creates. The owner is the product's solo builder; he asked for this: "comprehensively... from a high level in an organized fashion given equal weights to each part in bullet points and explained simply but comprehensively... every system and subsystem within the simulation and generation, how they all interact with each other, and the experience that they are supposed to bring to the user... exhaustive and comprehensive leaving no detail."

THIRTEEN CENSUSES OF THE LIVE ESTATE AND THE UNBUILT VOLUMES FOLLOW. Build the document from them. Do not invent a subsystem no census reports; do not drop one that any census reports.

` + JSON.stringify({ generation: cens[0], interior: cens[1], people: cens[2], politics: cens[3], diplomacy: cens[4], war: cens[5], trade: cens[6], belief: cens[7], culture: cens[8], flow: cens[9], surface: cens[10], substrate: cens[11], unbuilt }) + `

WRITE THE DOCUMENT TO ${OUT}. REQUIREMENTS:
- **EQUAL WEIGHT.** Every system gets the same treatment and roughly the same depth. No system gets three paragraphs because it is interesting and another gets one line because it is plumbing.
- **PLAIN LANGUAGE.** The builder knows his product but the document must read cleanly to someone who does not know the codebase. NO file paths, NO symbol names, NO wave labels, NO internal jargon in the body. Say "the town's decision-maker", not "settlementStrategy.js". Spell out every term you must use.
- **BULLET POINTS**, organized in tiers: THE SUBSTRATE (what makes it a simulation at all) → THE GENERATOR (how a world is born) → THE LIVING SYSTEMS (each domain) → THE INFORMATION LAYER → THE SURFACE (what the DM touches) → THE UNBUILT (designed, awaiting build).
- For EVERY system: **What it is** · **What feeds it / what it feeds** (by name, so the web is traceable) · **What the DM gets at the table** · **Live or dark**.
- **A DEDICATED INTERACTION SECTION**: the major loops that cross domains — name each loop and walk it in order (for example: a bad harvest becomes scarcity becomes unrest becomes a political fork becomes a trade decision becomes a neighbour's opportunity). Give at least eight such loops, each walked end to end.
- **AN EXPERIENCE SECTION**: what the whole thing is FOR — the promises the product makes to a DM, each tied to the systems that deliver it.
- **AN HONEST BOUNDARIES SECTION**: what the simulation deliberately does NOT do and why (never resolves a named character's fate; never adjudicates theology; no terrain-bound physics so it ports to any medieval-inspired cosmology including a world of clouds; sub-century horizon; world-only rather than party-facing; plausibility rather than empirical calibration).
- Aim for COMPLETENESS over brevity. This is a reference document. Use headings and bullets so it is navigable.
RETURN: the document's section list, its line count, and the count of systems documented.`, { label: 'synth:ontology', phase: 'Synthesize', model: 'opus', effort: 'high' })

phase('Check')
const critic = await agent(RO + `\n\nYOU ARE THE COMPLETENESS CRITIC for the ontology document at ${OUT}. Read it whole. Then hunt what is MISSING or WRONG: (1) walk the live estate yourself — src/domain's directory listing and the main kernel stages — and name any SYSTEM the document omits; (2) check its LIVE-vs-DARK labels against the actual flag manifest; (3) check the interaction loops actually compose (does each step really feed the next, per the code?); (4) flag any place the document's plain language has become inaccurate rather than merely simple; (5) flag jargon, file paths, or wave labels that leaked into the body. RETURN structured JSON: missingSystems[], mislabeled[], brokenLoops[], inaccuracies[], leakedJargon[], overallVerdict.`, { label: 'critic:completeness', phase: 'Check', model: 'opus', effort: 'high', schema: { type: 'object', required: ['overallVerdict'], properties: { missingSystems: { type: 'array', items: { type: 'string' } }, mislabeled: { type: 'array', items: { type: 'string' } }, brokenLoops: { type: 'array', items: { type: 'string' } }, inaccuracies: { type: 'array', items: { type: 'string' } }, leakedJargon: { type: 'array', items: { type: 'string' } }, overallVerdict: { type: 'string' } } } })

return { doc, critic, path: OUT }