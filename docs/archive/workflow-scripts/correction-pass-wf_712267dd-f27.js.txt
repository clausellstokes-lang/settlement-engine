export const meta = {
  name: 'correction-pass',
  description: 'Apply all ~134 self-audit findings to the architecture corpus under the chair\'s crux rulings',
  phases: [
    { title: 'Correct', detail: '6 doc-surgery agents, disjoint files' },
    { title: 'Cohere', detail: 'cross-doc coherence sweeper' },
  ],
}

const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const J1 = '/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/2e3ad88b-afe1-4b9a-98ca-b726d4aaa3d7/subagents/workflows/wf_8a15d306-8e1/journal.jsonl'
const J2 = '/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/2e3ad88b-afe1-4b9a-98ca-b726d4aaa3d7/subagents/workflows/wf_b12d79d8-256/journal.jsonl'

const PRE = `VERIFY-FIRST: you are a CORRECTION agent executing the Fable chair's rulings on its own audited architecture (owner order: "architect them for Sol — make it exhaustive"). Tree: ${MF} (claude/composite-r4; the implementer session Sol is LIVE — re-read your assigned file(s) fresh BEFORE editing, preserve any status edits you did not write, and NEVER run git commands; the chair commits by pathspec after review). YOU EDIT ONLY YOUR ASSIGNED DOC(S) — nothing else, ever, no code, no tests.
METHOD: (1) Read the two audit journals ${J1} and ${J2} (JSONL; results carry findings[] with title/kind/where/detail/fixSketch) and extract EVERY finding whose 'where' names your assigned doc(s). (2) Re-verify each against the current doc + tree where cheap (some may already be fixed — e1db8fff landed two). (3) Apply the fix per the finding's fixSketch AS MODIFIED by the chair's rulings below — the rulings WIN over sketches where they differ. (4) Owner-dictated text is NEVER rewritten: corrections to owner clauses land as bracketed supersession/correction notes beside the original. (5) Mark every edit "CORRECTED 2026-08-02 (self-audit)" or fold naturally where a whole section is rewritten. (6) House voice throughout; specs must let Sol build without inventing architecture — where a finding says substrate is missing, NAME the new work as a numbered work item with its pins. (7) Anything you cannot resolve without a new judgment call goes in 'unresolved' with a proposal — never silently decide a fork.`

const SCHEMA = {
  type: 'object', required: ['changes', 'unresolved'],
  properties: {
    changes: { type: 'array', items: { type: 'object', required: ['finding', 'action'], properties: {
      finding: { type: 'string' }, action: { type: 'string' } } } },
    unresolved: { type: 'array', items: { type: 'object', required: ['finding', 'why', 'proposal'], properties: {
      finding: { type: 'string' }, why: { type: 'string' }, proposal: { type: 'string' } } } },
  },
}

phase('Correct')
const corrections = await parallel([
  () => agent(`${PRE}
ASSIGNED: docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md (yours alone).
CHAIR RULINGS binding on your edits:
- AMENDMENT B becomes a §1b standing law: every new scorer/evaluator (WR-1 four-term, WR-2 threshold, WR-3 lineage, WR-4 trajectory, WR-8 feasibility+intent, WR-10 appraisal) carries a RECEIPT FIELD naming the state that produced its score, walker-asserted; clause (iv)'s mechanic is SUPPRESSION — a casus contradicted by a live read returns 0 with a receipt naming the contradicting state (suppress-the-score, never refuse-the-mint; the reasons layer's decay idiom). Add incoherence-unreachable negative pins to WR-2 and WR-3 (amendment E's peaceable-culture clause; B's you-do-not-sack-the-satellite-you-provisioned clause).
- §3 flag table: rename to dispositionChannelsEnabled, fix the count (seven + the casus sub-flag), add a FLAG-DEPENDENCY ruling: WR flags are INDEPENDENT dark switches but WR-3/WR-8/WR-10 declare demographicsEnabled + their listed predecessors as LIT-PRECONDITIONS (lighting order = the §9 build order; a flag lit out of order is a config the certification walker reds).
- §4: replace worldState.dispositions with the EXTENDED dispositionStats shape, writer dispositionLedger.js; dispositionProfile.js survives ONLY as a pure read-side module (no state, no writer) exporting thresholdFactorOf-shaped reads.
- §2 census: fix the rows in place (jailed = corruption-exposure path only, foreign-guest hold is NEW WORK; 13 peace mirrors not 14); title + header to "amendments A–S" / "the 31 owner rulings"; reduce J-WR-13 to a standing STOP rule listing the three known corrections by name.
- WR-7b: add the FOREIGN-GUEST HOLD as a named new-work item — spatialLedgers-family record {npcId, captorId, heldSinceTick, cause}, single writer, dwell read consumed by WR-7d's ransom gate, release/escape/death paths, JSON-round-trip + regen/undo pins.
- THE SEAM MECHANICS (J-WR-1 refinement): define the TWO-PICTURE CONTRACT — a pure wrapper module (negotiationPictures.js, new work) that invokes the EXISTING evaluators once per party under that party's own truthFor, and acceptance compares each party's own-picture valuation; the pulse injection point is advanceTreaties' call site, named as WR-7 slice work. peaceTerms stays the single terms writer.
- Add the PEACE-TERMS TIME-BASE substrate flag: peaceTerms/treatyEnforcement price a "year" at 12 ticks while the engine ticks weekly — WR-0c gains item (4): re-derive the duration constants against the weekly tick (or document the 12-tick month-year deliberately), with a pin.
- WR-6 gains the JOIN DECISION: an ally's entry runs the E3 alliance-web read + its own books (G) as a scored decision with a receipt, and a REFUSAL is a remembered fact feeding the relationship record (amendment I's "may refuse and be remembered for refusing").
- Amendment A counterforce homes: predation's counterforce (victim's patrons + aggressor restraint via B-suppression) lands in WR-1's dissolution/suppression spec; faith war's (shared rite + rarity) in the sacred_claim scorer notes; lineage's kinship in WR-3 (already there — verify).
- WR-8: add N2's explicitly requested pin (mistaken-feasibility both directions, receipted); add THE ATROCITY-COALITION CASUS as named new work — a walker-satisfying reason pair (atrocity_answer ↔ its mirror, names vetoable) minted from BELIEVED razings, feeding R2's license machinery — WR-8's spec must stop claiming "every piece exists."
- EVERY wave gains a LIFECYCLE PATHS line (persist/serialize/regen/undo/import) for any state it adds — one sentence each, the WR-10 pattern.
- §9: prepend the standing pre-soak order (WR-0 → P5 [landed @ 47b4ed9d] → WR-0c → the LIGHTING BATCH incl. demographicsEnabled → grid), citing the queue ruling.`, { label: 'fix:war-volume', phase: 'Correct', schema: SCHEMA }),

  () => agent(`${PRE}
ASSIGNED: docs/DESIGN_REALM_DIRECTIVES.md (yours alone; owner-verbatim text is sacred — bracketed notes only).
CHAIR RULINGS:
- AMENDMENT R rung derivation (the arithmetic finding): add a bracketed manager correction — SEVERITY PICKS THE TARGET RUNG (one or two below, banded from margin × brutality × defenses per the owner's clause), and the death fraction DERIVES to land population inside that band (conservation-exact, still written through the one popToTier-reading path; the escape share rides unchanged). The fraction-first model is retired.
- R vs R2 alignment contradiction: R's "reachable only through N3's deception road" clause gains a bracketed note: SUPERSEDED IN PART by R2 (initiation is evil-exclusive absolutely; the deception road survives ONLY as the false-license path R2/J-WR-7 defers) — never delete R's text.
- AMENDMENT S clearing rule (stated two ways): add the manager reconciliation note — clearing is TWO-SIDED: seller's-own-lens value of the BUNDLE >= seller's reserve for the asset, AND buyer's-own-lens value of the ASSET >= buyer's-own-lens cost of the bundle; both beliefs, K3 governs. And correct "no new negotiation layer": the market reuses the CATALOG + stacking + appraisal; the war-side margin-derived budget is NOT reused — the two-sided clearing IS the market's quantity (no new artifact/ledger remains true; say it that way).
- J-D11(a) seam correction: the OR never enters the memoized digest isPort hot read — port-capability is a NEW read-side module (portCapability.js, new work) = digest isPort OR earned-port-operational, consumed at the NON-hot seams (route candidacy, navy capability, charter eval), while pathCost keeps the frozen digest read; sea-edge folding for earned ports is named work in the water-corridor wave with its cache-invalidation story (per-tick derived set, never a WeakMap poke). Also correct (c): the >=2-ports condition is structural at the CHARTER/candidate layer, and the pathing layer's port-gating is the existing seaLanes read — say which layer owns what.
- J-D11(a) plan-lane: founding a port is a NEW RESPONSE KIND — the closed response vocabulary (six) EXTENDS by 'found_port' with its own episode gate (water-proximity band + trade-demand crossing, not the pressure band), named as new work with its walker/registry updates.
- Structure: renumber/relocate directive 11 AFTER 10; fix the section header ("The eleven directives" or retitle honestly); the "seven directives" header lies.
- P5 §15 corrections happen in DESIGN_DEMOGRAPHIC_ENGINE.md, NOT here — but the Progress entry recording P5 gains one bracketed line pointing at the equilibrium-criterion correction.
ALSO: fix docs/DESIGN_DEMOGRAPHIC_ENGINE.md §15 (also yours): P5a is re-labeled a CEILING unlock (necessary, not sufficient) — the equilibrium factor (76-83%) means promotion ALSO requires in-migration overshoot above the fixed point (§3's brief-overshoot allowance is the designed path); the "interlock" story is retired for desert AND mountain honestly; ADD two pins as work items: works=WORKS_CAP reachability (a fenced multi-shock fixture, the J-D10(c) discipline) and a desert-promotion-under-surge reachability fixture. 0.55 stands (Sol's build is correct).`, { label: 'fix:directives+demo', phase: 'Correct', schema: SCHEMA }),

  () => agent(`${PRE}
ASSIGNED: docs/DESIGN_GALLERY_SHOWCASE.md (yours alone).
CHAIR RULINGS:
- VOCABULARY: 'tier' is taken (settlement size) — the feature renames to ACCOUNT STANDING badges everywhere (standing ∈ {wanderer, cartographer, surveyor, founder, developer, admin, sponsored-class}); no gallery surface ever says 'tier' for accounts.
- SUBSTRATE HONESTY: rewrite §4 against reality — maps and campaigns galleries ALREADY EXIST as tabs in a different table; the framework EXTENDS the shipped tab UX (render-when-populated applies to the SPONSORED shelf and any future kind, not retroactively to shipped tabs); G-B re-scopes to reconciling the existing tabs + adding the sponsored shelf.
- PRIVACY (the RPC leak): the standing badge is computed SERVER-SIDE and returned ONLY when the author opted in — opt-in is a server-boundary rule, not a display rule; paid status of non-opted authors never leaves the database. showTierBadge → showStandingBadge, stored per-item at publish as the OPT-IN FLAG only (the badge VALUE still derives live at read; re-verify the §2 no-stamping law is preserved and state both halves plainly). Surveyor displays as Cartographer publicly (resolves §10 call 2 — the tree already hides Surveyor naming; cite the aiSurface census the audit named).
- R-G1/R-G2 predicate conflict: 'sponsored' leaves badgeClass — it is a CONTENT class rendering its own chrome in its own shelf; the ordering walker forbids ACCOUNT-STANDING fields in organic ordering, while the sponsored shelf is a separate labeled query (no predicate conflict remains).
- ORDERING REALITY: R-G1's walker targets the PLURAL ordering surface — enumerate the SQL ordering definitions the audit found (migrations 019/063/071/076/147) and spec the walker over the LIVE one + a stale-definition cleanup work item.
- EDITORIAL RECONCILIATION: is_featured and is_curated both exist — the spec absorbs them: curated = editorial pick, featured = the existing spotlight mechanism (document its semantics from migration 168), sponsored = the paid third; a matrix states how they compose and which surfaces each governs.
- SPONSOR AUTHORSHIP: the sponsor world publishes through a named admin-side path (new work: admin publish-on-behalf with sponsorName as the DISPLAYED author line and the admin as the accountable author of record; the standing badge suppressed on sponsored items — the sponsored chrome replaces it).
- The write path: spec it against the ACTUAL role machinery the audit cited (service-role/two-key admin pattern, not bare RLS), plus the mandatory audit row and the missing role the audit named.
- Comments per kind: gallery_comments is settlement-scoped — extending comments to maps/campaigns is NAMED NEW WORK with its migration, or §0.3's claim narrows honestly until it lands (rule: name the work, keep the claim as the target).
- §7 inversion: the import-strip is true by construction — respec §7 around the PROVENANCE LINE as the work item (where it renders, what it says, its pin).
- TELEMETRY: assign the three events their EVENT_CLASS per analyticsEvents.js's registry and correct the consent sentence for the option the audit flagged.
- Founder nuance: "never lapses" gains the transfer-case footnote (migration 160).
- G-A work items gain: the author-slot (sanitizeTile currently drops author identity — the card needs one before any badge attaches).`, { label: 'fix:gallery', phase: 'Correct', schema: SCHEMA }),

  () => agent(`${PRE}
ASSIGNED: docs/DESIGN_AI_CHAT_SURFACE.md (yours alone).
CHAIR RULINGS:
- THE ROUTER: the chat EXTENDS the existing door architecture — SurveyorDoor + doorRouter are the substrate and the chat is their full-width client; a second router is forbidden; the totality walker's DENOMINATOR is the door registry + the op-family registry (name the actual modules the audit cited: src/domain/intent/doorRouter.js and the op families source), and the 'corpus' unroutable finding + the missing subset pin become named work items in SC-1.
- AUDIENCE: the audience toggle SURVIVES into the chat header (never retired); threads are PER-WORLD PER-AUDIENCE (a DM thread and a player-safe thread, separately persisted, the slicer seam honored per question exactly as the control surface mandates); state this as the law that keeps the chat from bridging the DM/player seam.
- APPROVAL COMPONENTS: the audit says the shared approval component does not exist — respec honestly: SC-1 EXTRACTS the approval card from the existing panels (InterpretApplyPanel et al.) into ONE shared component consumed by chat and any remaining panel — extraction named as work, "the same components" claim corrected.
- PIPELINES: audio/multimodal seams do not exist — SC-2/SC-3 name the seam-widening work explicitly (the anthropicCache/provider seam, the interpret 12k cap gaining a chunked-transcript path for SC-3's flagship case); the L-6 wiring claim is corrected to the actual trigger the audit cited; the BYOK paragraph is REWRITTEN against the 2026-07-30 fail-closed ruling (byokFailClosed — cite it; the stale fail-open language dies).
- COSTS: the cost resolver prices OPERATIONS, not durations — transcription pricing is a NEW rate entry (named work: per-minute banded rate through the resolver's registry + migration), and the consent card quotes the resolver's own output.
- CENSUS OBLIGATIONS: SC-1..3 each list their exact-set census updates (the aiSurfaceCensus floors the audit cited: edge/client/roster manifests) + the AI-OFF state (the chat surface when aiEnabled is off: absent, per the presence discipline — say it).
- LIFECYCLE: threads + uploads get the full trace — storage (private bucket as NEW infrastructure with its migration; the gallery bucket is public and never reused), retention bands, export via accountData (extend its manifest — named work), deletion, and the Data-tab wiring; each with pins.
- TWO-VOICES: the chat renders the two-register output (analyst voice vs in-world voice) per the control surface's §3b — named in §2 with the copy affordance per register.
- ROUTE + RETIREMENT: the chat takes the /workshop route's place (route renamed or redirected at SC-1; the retirement work enumerated: panels that fold in, panels that die, the deep links that re-point).
- MOBILE: §2 gains the responsive law — the chat is a bottom-sheet on mobile per the control surface's own §2c.1 precedent; the upward-growing box respects the keyboard inset.
- INJECTION: keep the approval-gate firewall claim but scope it honestly per the audit: it guards WRITES; add the read-path discipline for uploaded content (the detectMetaProbe pattern extended to upload extraction — named work).`, { label: 'fix:chat', phase: 'Correct', schema: SCHEMA }),

  () => agent(`${PRE}
ASSIGNED: docs/FIRST_CONTACT_BACKLOG.md (the LD-1..LD-8 sections ONLY — never touch the pre-existing backlog above them) + docs/DESIGN_LIVING_MINIATURE.md.
CHAIR RULINGS:
- LD-1 §2/§6 mutual exclusivity: the parity pin's replay = FORGE + THE SAME 12-TICK ADVANCE the fixture script runs (the fixture is the post-tick artifact; "Forge this exact town" delivers the forge-time town and the pin covers the WHOLE recorded path via the script's own buildRegion) — restate §6 so the pin and the CTA claim are both true; the uncorded store reads the audit found (five settings generateSettlement reads outside config) become a named work item: the fixture RECORDS them and the replay pins them.
- Fixture v2 is ADDITIVE: the excerpt/voice fields LIVE SURFACES read are KEPT (the audit found three live consumers + both CTAs) — nothing deletes; the full settlement emission is added beside them; the size denylist DIES (it broke digest equality) — the honest size answer is measurement + the lazy chunk.
- The mount: correct the store-free claim per the audit (OutputContainer reads the store; the readOnly/no-saveId mount changes the tab set) — the miniature mount gains a DEMO contract: the cost-resolver RPC is gated off for the fixture mount (a prop or provider stub — named work; no anonymous-visitor RPC), visitor store bleed is fenced (tableView/focusedEntity overlays suppressed under the frame — named), and the ACTUAL tab set under the demo mount is enumerated after a build-time check.
- LD-2: correct the substrate (the existing component is NavFlowArrow, shared with the mobile bottom nav — the divider work creates a desktop-only variant and leaves the mobile consumer untouched); placement map derives FROM routes.js nav order (never a hardcoded second truth — the pin asserts derivation); the divider component lives in the eager header, so it must be byte-frugal (inline SVG, no new module weight beyond the trivial).
- LD-3/3b: scope the fixed-chrome conversion to THE LANDING ROUTE ONLY (app pages keep the sticky header exactly as shipped); chrome-height tokens live in theme.js CHROME (the frozen source the audit named) — LANDING_RIBBON_H joins there; the ribbon renders EAGERLY (slim, above the fold by definition) while the artwork band stays lazy — the persistent ribbon is its own tiny component, not LandingFooter; the scrim fix names background-attachment:fixed as the actual mechanism to correct; keep a Pricing link in the ribbon row (the audit: LD-3 removed the landing's only Pricing path); the "no sibling after LandingFooter" pin is respecced to the real DOM (the audit says false by construction — pin the scroll-container's last child instead); mobile: the ribbon is static at document end with safe-area padding and the mobile bottom nav keeps clearance (the audit's under-nav collision).
- LD-5: correct against reality — the Account six-tab IA ALREADY EXISTS (the spec becomes: map the menu to the EXISTING tabs; the seventh tab (BYOK) homes under Security [chair ruling, vetoable]); nav parents are BUTTONS today — the spec's ruling 1 becomes make-them-links work, not exactly-as-today; the deep links are respecced against routes.js's REAL param patterns (fix each colliding example the audit listed); dropdowns are DESKTOP-ONLY in this order (the mobile nav is untouched; My Saves reaches mobile via the Gallery page's own tab, not the nav) — state it; My Saves' contradiction resolves: it is the CROSS-CATEGORY view (settlements + the one saved_maps/campaigns table the audit found — name the real tables) and the Library relationship note updates to match reality.
- LD-6: add the TWO WEBHOOK WORK ITEMS as blockers (annual price-id → credits mapping; unknown-product-key fail-closed guard with alert — a buyer must never pay-and-get-nothing); enumerate the Terms/copy surfaces the audit found promising auto-refunds (they change WITH the ruling, listed by file); the price-number's real home (the audit's finding) is named so the config-driven rule is executable.
- LD-7/LD-8: verify against the audits (few findings) and tighten any placement facts the audit corrected.`, { label: 'fix:ld-cluster', phase: 'Correct', schema: SCHEMA }),

  () => agent(`${PRE}
ASSIGNED: docs/FABLE_VALIDATION_QUEUE.md (the 2026-08-01/02 sections ONLY — Opus-era rows above are IMMUTABLE) + docs/COMPREHENSIVE_REVIEW_2026-08-01.md.
CHAIR RULINGS:
- The NINE dangling "register R-ref" cells: each verdict cell that promises a correction "in the register" gets its pointer fixed to where the correction ACTUALLY lives now (the §15 P5 doc, the war volume's WR-0c/§ amendments, or a NEW appended register item you create in the ADDENDUM for the ones with no home — list them explicitly; no dangling pointer survives).
- Fix the verdict tally (recount the table honestly; correct the same arithmetic in the DESIGN_REALM_DIRECTIVES Progress line by a one-line bracketed correction — that file is another agent's; DO NOT edit it, instead put the correction text in your 'unresolved' output for the chair) — actually: put ONLY the queue-side fix in the doc; report the Progress-line text needed.
- "6 ADJUSTED (all doc-precision)" → correct the characterization (three adjustments require code; name them).
- Batch-6 retro row: add the K3 certification-pin widening the audit found missed (name it, verdict it); mark "gate exit 0" honestly as self-attested (no independent receipt) and add the compensating fact the auditor verified (no golden moved — independently CONFIRMED by diff).
- The K3 buffer-draw contradiction between two register items: adjudicate per the auditor's evidence (keep the verified one, mark the other REFUTED-in-place with the citation).
- The neighbour-mirror injection ruling gets a real home: append a register item (or a queue NEW ROW) carrying the full spec (inject-dampened ×0.5, caps, blast-radius facts, ONE-REGEN membership) so the ONE REGEN batch has an executable reference — the verdict cell then points at it.
- The orphaned owner-ruling row (P5, after the protocol amendment) moves INTO the NEW ROWS table where it belongs.
- W1 chair row: refresh the stale attribution (the re-record was executed by Sol @ f5a88ac1 — say so).
- Register ADDENDUM fixes: A-1 gains the monotone-ceiling note (the regenerated baseline must never silently raise — a ratchet rule, named); A-1's one-hop-indirection escape becomes a named limitation with the follow-up detector work item; A-3's scope widens beyond src/domain (the live authoring site the audit found outside it, named); the drifted file-count is re-measured or replaced with the command that measures it; WR-9's "additive" claim is corrected (it moves the certification gate — say what re-baselines).`, { label: 'fix:queue+register', phase: 'Correct', schema: SCHEMA }),
])

phase('Cohere')
const summary = corrections.filter(Boolean).map((c, i) => `agent ${i}: ${c.changes.length} changes, ${c.unresolved.length} unresolved`).join('; ')
const sweeper = await agent(`${PRE}
YOU ARE THE COHERENCE SWEEPER (assigned: READ every corrected doc — the six agents just finished: ${summary} — you may edit ANY of tonight's docs but ONLY to fix cross-references and contradictions BETWEEN them, never to reopen a settled correction).
Sweep: (1) every cross-doc pointer resolves (WR volume ↔ directives ↔ queue ↔ register ↔ LD specs ↔ miniature ↔ chat ↔ gallery ↔ demographic §15); (2) the flag list is consistent across all docs (one name per flag, dependency rulings identical); (3) vocabulary consistent (ACCOUNT STANDING never 'tier'; dispositionChannelsEnabled everywhere; the two-sided clearing stated identically in S and WR-10); (4) the sequencing story identical everywhere (WR-0 → P5 → WR-0c → lighting batch incl. demographicsEnabled → grid → tuning); (5) every "named new work" item appears EXACTLY ONCE as the owning spec with pointers elsewhere. Fix what you find; report each fix.`, { label: 'cohere', phase: 'Cohere', schema: SCHEMA })

return { corrections: corrections.filter(Boolean), sweeper }