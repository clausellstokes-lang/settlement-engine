export const meta = {
  name: 'build-cycle-10-war-continuation',
  description: 'R-BLD-9 ratchet truth + walkers · war splits 2-4 · wiring discharge + R-BLD-8d',
  phases: [{ title: 'Ratchets', detail: 'strict + any-cast re-baseline, walkers' }, { title: 'Splits', detail: 'peaceTerms, pulseKernel, applyWorldPulse' }, { title: 'Wiring', detail: 'WR-7 discharge + R-BLD-8d' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest before believing a timeout); STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Everything DARK, nothing pushed. CONCURRENCY: Herald + dossier lanes are live on src/domain/display/herald*, dossier components, and src/data/*.generated.js — never touch those; queue docs get concurrent edits (re-read, carve hunks).`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}

phase('Ratchets')
const r1 = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
STEP A — CHAIR RULING R-BLD-9, THE THIRD RATCHET REPAIRED TO TRUTH (same banked-debt-ledger pattern as R-BLD-6): scripts/.domain-strict-baseline.json is frozen at {"total":0,"files":{}} while ~63 files carry ~1299 inherited strict errors from the landed war waves — every error reads as a regression against a fiction. RULED: re-freeze the baseline at TODAY'S MEASURED per-file census (run the checker's own measurement at your HEAD; exclude any file a live lane is mid-editing — measure committed bytes via a clean worktree if needed); the law stays shrink-only (a file may never worsen, deletion at zero, NEW files enter at zero — the zero-ceiling law survives for new work); the burn-down is owed at THE STRICT BURN-DOWN WAVE, queued first-class. COROLLARY RULED (cycle-8 verifier Finding A): decomposition splits must be strict-NEUTRAL by per-file map (debt may move within the family, never onto non-family files, totals unchanged) — burn-clean is NOT required mid-split, because coupling typing changes into a behavior-identity commit weakens both proofs; debt burns at the wave. One commit + queue row citing R-BLD-9.
STEP B — execute the BANKED any-cast ruling (already chair-ruled, same pattern): re-freeze the any-cast ratchet at its measured census (~2285 holes/157 files — re-measure with node scripts/count-domain-any.mjs) so the 19 inherited drifting files stop reading as regressions; shrink-only law; burn-down owed at the same wave. Same commit or its own.
STEP C — the two enforcement walkers the cycle-8 verifier designed (Findings B+C): (1) a single-writer source-scan walker for worldState.envoyErrands — the ledger key literal/constant may be WRITTEN only in envoyErrandLedger.js's writeErrands (follow the namedPersonTransitTotality pattern); mutant: add a second writer → red. (2) the K3 discovery guard — a readdirSync/glob walk asserting every src/domain/worldPulse/envoyErrand*.js file is a member of NEGOTIATION_MODULES in envoyK3BeliefSeam.test.js; mutant: drop a family file from the allowlist → red. One commit.
Return {lane:'ratchets', commits, gates, stopReports, notes}.`,
  {label:'w1:ratchets', phase:'Ratchets', model:'opus', effort:'high', schema:S})
const v1 = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r1)}. Verify: the strict baseline entries equal the checker's own measurement on committed bytes (re-run it yourself; spot-audit 5 files by hand with tsc); the shrink-only law enforced (mutant: raise one entry → the gate must red or the checker must refuse); new-file zero-ceiling still live (mutant: add a fresh strict error in a NEW temp file → red; delete it after); the any-cast re-freeze matches its own counter; both walkers' mutants re-run by YOU; pathspec-clean vs the live Herald/dossier lanes. Return {lane:'ratchets verify', verdict, findings}.`,
  {label:'w1:verify', phase:'Ratchets', model:'opus', effort:'high', schema:V})

phase('Splits')
const r2 = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
Prior stage: ${JSON.stringify({ratchets:r1?.commits, verdict:v1?.verdict})}. STEP 1 CONTINUES — files 2-4 of THE DECOMPOSITION WAVE's war tranche, one pathspec commit each, largest first: src/domain/worldPulse/peaceTerms.js (1680), pulseKernel.js (1580), applyWorldPulse.js (1395). METHOD, upgraded by the cycle-8 verifier: the writer-family pattern (R-BLD-4: head keeps writer/entry role, pure leaves) + BEHAVIOR-IDENTITY BY STRIPPED-CONCAT DIFF (pre-split file vs concatenation of the family, comments/blanks/imports/exports stripped, export-prefix normalized — zero logic delta is the receipt; this proved stronger than test-only receipts on file 1) + strict-NEUTRALITY per R-BLD-9 (per-file strict map before/after: family totals unchanged, zero debt onto non-family files) + baseline entry DELETED per the ratchet's own instruction + K3/walker registration for every new leaf (the discovery guard from the prior stage now enforces this — run it) + focused suites green before/after per file. pulseKernel/applyWorldPulse are THE PULSE MOUTHS — extract pure stage-leaves, keep the kernel's stage ORDER and stream identity untouched (any reordering is a same-seed shift: forbidden; prove with two same-seed pipeline runs hashed identical pre/post per file). Return {lane:'splits', commits, gates, stopReports, notes}.`,
  {label:'w2:splits', phase:'Splits', model:'opus', effort:'high', schema:S})
const v2 = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r2)}. Per file: re-run the stripped-concat diff YOURSELF (rebuild the normalizer, don't reuse the lane's); re-measure all effective sizes with eslint's own engine; same-seed pipeline hashes pre/post (golden corpus rows if feasible); strict per-file map neutral; baseline deletions lawful (sizeBaseline test green); walkers + K3 discovery guard green with every new leaf registered; single-writer walker still green; pathspec hygiene. Return {lane:'splits verify', verdict, findings}.`,
  {label:'w2:verify', phase:'Splits', model:'opus', effort:'high', schema:V})

phase('Wiring')
const r3 = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
Prior stages: ${JSON.stringify({splits:r2?.commits, verdict:v2?.verdict})}. THE PULSE MOUTHS NOW HAVE HEADROOM. STEP 2 — discharge the three recorded wiring deferrals: wire WR-7b/7c/7d's deferred pulse stages (envoy errands re-minting through the envoyErrand family head, the ransom claim landing at the applyWorldPulse mouth, the WR-7c chooser reachable where DESIGN_WAR_RULINGS_ARCHITECTURE says) per R-BLD-1: stage-neuter negative controls proving runtime reachability, AND the dormancy-golden pin — war flags are DARK so the generation goldens must be BYTE-IDENTICAL pre/post wiring (capture, hash, compare, quote); if any golden moves with flags dark STOP — that is a defect. STEP 3 — R-BLD-8d code half (ruled): in coalitionRatification.js's unionCoalitionWeight walk, re-enforce duplicate_member (reuse ratifyTermSheet's spelling) and ballot-sheet identity; the cycle-6R verifier's probe2 counterexamples (duplicated ballot ratifying a 3-of-6 minority at margin 1.0; ts.zzz chosen under ts.a) become pins, mutants re-opening each hole must red. NOTE the file was touched by b8b6406c (NUL cure) — work from current bytes. One commit per step + queue rows. Return {lane:'wiring', commits, gates, stopReports, notes}.`,
  {label:'w3:wiring', phase:'Wiring', model:'opus', effort:'high', schema:S})
const v3 = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r3)}. Verify: stage-neuter controls re-run by YOU (neuter each new stage → its reachability pin reds); dormancy goldens hashed identical with flags dark (re-hash yourself); hunt stream theft + stride aliasing in the new pulse plumbing (Wave-E hazard classes); R-BLD-8d — re-execute both probe2 counterexamples (must refuse closed), mutants re-opening each hole red their pins; all suites green on committed bytes; pathspec-clean. Return {lane:'wiring verify', verdict, findings}.`,
  {label:'w3:verify', phase:'Wiring', model:'opus', effort:'high', schema:V})

return { ratchets: {r: r1, v: v1}, splits: {r: r2, v: v2}, wiring: {r: r3, v: v3} }