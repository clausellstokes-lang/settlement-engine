export const meta = {
  name: 'epoch-living-futures-architecture',
  description: 'Architect the ADVANCE-EPOCH (living futures) directive comprehensively: fresh histories on every user advance/reroll, immutable lived past, internal byte-exact replay — cohesive with everything',
  phases: [
    { title: 'Census', detail: 'four substrate lenses in parallel, read-only' },
    { title: 'Architect', detail: 'one compiling architect, the full volume draft' },
    { title: 'Cohesion', detail: 'adversarial critic, executed-logic scrutiny' },
    { title: 'Amend', detail: 'close all nine findings under the chair rulings' },
    { title: 'Recohere', detail: 'fresh-eyes verification of the amended volume' },
    { title: 'Polish', detail: 'close the nine residuals from the recohere pass' },
    { title: 'Seal', detail: 'final independent check' },
    { title: 'Touch', detail: 'the Arm A writer + flag-driven spreads + four small fixes' },
    { title: 'Close', detail: 'last independent verification' },
    { title: 'Settle', detail: 'the five residuals + three anomaly absorptions' },
    { title: 'Ratify', detail: 'the last check' },
    { title: 'Fasten', detail: 'the accessor epoch source + three mechanical fixes' },
    { title: 'Cohere', detail: 'the closing verdict' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const OUT = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c44e5d99-2ba5-49d5-a554-40b68534c8eb/scratchpad/epoch-arch'
const DIRECTIVE = '/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/memory/advance-epoch-living-futures-directive.md'

const LAWS = `You work for the session chair of an owner-delegated build program on SettlementForge, in ${TREE} (branch claude/composite-r4). READ-ONLY on the repo: never edit/create/delete a repo file, never run vitest/npm/any gate (other lanes own the tree), never run a state-mutating git command. You write ONLY under ${OUT}/ (create it if needed) and byte-scan every file you write with python3 (zero NUL bytes, zero stray control bytes — the Write tool has minted raw NULs seven times in this program). LIVE CODE OUTRANKS EVERY DOCUMENT AND EVERY MEMORY: for each load-bearing premise, execute the grep/read that proves it at the CURRENT HEAD and label rows CONFIRMED (with the symbol + module quoted) or REFUTED (doc claim vs measured truth, stated prominently — the J-WR-13 stop rule binds). Navigate by SYMBOL, never by quoted line numbers. Your final message is a data return.

THE DIRECTIVE (owner, 2026-08-05 — this is LAW; read the banked file at ${DIRECTIVE} for the full text): user-facing time advances and ALL user-facing generate/reroll affordances draw a FRESH plausible outcome on every click (a genuinely different, fully coherent history/output each time — the DM who backtracks on vibes and re-advances gets a NEW future); the lived past NEVER rewrites (loading a save shows the same history — the trust floor); the seed remains the ADDRESS of the starting world (same seed = same starting world, forever); and INTERNAL determinism is fully retained for debugging via recorded entropy (every user advance mints a generateSeed-class nonce RECORDED in the history/action log; replays feed recorded nonces and are byte-exact). THE PROMISE amends, owner-signed: a seed is a STARTING world forever, and a lived history, once lived, forever. Dark-state discipline: whatever flag gates this, flag-absent worlds must be BYTE-IDENTICAL to today including the exact PRNG stream identity strings.`

phase('Census')
const [c1, c2, c3, c4] = await parallel([
  () => agent(LAWS + `

CENSUS 1 — THE STREAM IDENTITY AND THE KERNEL SEAM. Measure: (a) every site that composes a PRNG stream identity from the world seed — start at the createPRNG call in src/domain/worldPulse/pulseKernel.js that concatenates rngSeed with the tick number and tickInterval (quote the EXACT current composition string verbatim, character for character — the dark-state byte-identity claim depends on it), then census EVERY other createPRNG call in src/ keyed on rngSeed or a derivative (the fork tree matters less; the ROOT compositions matter); (b) every caller of simulateCampaignWorldPulse in src/ (store actions, components, lib) — the full call chain from a user clicking advance down to the kernel, with the interval options the UI offers; (c) the now parameter and assertNowPinnedInTest discipline — what wall-clock currently touches and does NOT touch; (d) the zero-pulse-edit law precedents: find how prior chair-signed kernel seams were executed and documented (the ONE tradeWar seam from TR-1 is the freshest; quote its seam contract style), because this volume needs exactly ONE kernel seam — the stream-identity term; (e) whether any OTHER root stream (generation, instantWorld, reroll machinery) shares the same composition idiom. RETURN JSON: rows (premise/verdict/evidence), streamCompositionVerbatim (the exact string template), callers (array), seamPrecedent (how TR-1 did it), hazards.`, { label: 'census:stream-kernel', phase: 'Census', model: 'opus' }),
  () => agent(LAWS + `

CENSUS 2 — THE ADVANCE ACTION, HISTORY PERSISTENCE, AND UNDO. Measure: (a) the store action(s) that advance time — find them from the UI down (the advance button component, the store slice, what they persist); (b) the campaign/world save shape — where an advance is RECORDED: history entries, saves arrays, worldState fields; quote the entry shape a single advance leaves behind (this is where the nonce field would live — the directive requires it recorded per advance); (c) UNDO today: what backtracking affordances exist (undo advance? restore save? snapshot?) — find every path that can return a campaign to an earlier tick, because each is a fork point under the directive; also check the history-reroll machinery (a memory says history-reroll preservation shipped UNCOMMITTED — measure what is actually at HEAD); (d) the FULL persistence lifecycle for whatever entry gains the nonce: create/read/persist(Supabase)/export/import/clone/migrate — every path, because a field that survives one path and ghosts another is this owner's most-bitten bug class; (e) veilPublicPayload: does any public payload carry campaign history entries (the nonce must not leak anywhere it should not); (f) generateSeed in src/kernel/prng.js — quote its entropy sources and its documented one-place-non-determinism-enters clause verbatim. RETURN JSON: rows, entryShape (verbatim), undoAffordances (array with file+symbol), lifecyclePaths (each with the writer/reader symbols), hazards.`, { label: 'census:advance-persistence', phase: 'Census', model: 'opus' }),
  () => agent(LAWS + `

CENSUS 3 — THE GENERATOR-SIDE REGEN AFFORDANCE DENOMINATOR. The directive makes it LAW that every user-facing generate/reroll/regenerate affordance mints fresh entropy per click and never silently replays the same draw, with user edits preserved. Build the DENOMINATOR: census EVERY user-facing affordance in src/components + store actions that generates or regenerates content — whole-world/settlement generate, any reroll button (history reroll, NPC reroll, name reroll, hook reroll, whatever exists), regenerate-with-edits paths, instantWorld composition. Search WIDE (spellings: reroll, regenerate, regen, randomize, shuffle, generate again, new seed, generateSeed callers in src/components and src/store). For EACH affordance: (1) file + symbol, (2) does it mint fresh entropy (generateSeed-class) or reuse a stored seed (silent replay — a DEFECT under the new law), (3) does it preserve user edits (name the preservation mechanism or its absence — the regen-edit-loss hazard is recorded as LATENT), (4) disposition: ALREADY-COMPLIANT / NEEDS-FRESH-DRAW / NEEDS-EDIT-PRESERVATION / BOTH / NOT-APPLICABLE-with-reason. Report the ledger with its denominator (found N; compliant K; ...). RETURN JSON: denominator (int), affordances (array of the 4-field rows), hazards, notes.`, { label: 'census:regen-affordances', phase: 'Census', model: 'opus' }),
  () => agent(LAWS + `

CENSUS 4 — THE TEST ESTATE AND THE PROMISE HOMES. Measure: (a) the same-seed determinism pin surface: which test families pin seed-to-output identity (goldens, the dormancyOracle byte-identity suites, four-fence dormancy fences, soak fixtures, replay tests) — for each family, HOW the nonce/epoch threads through as a pinned input with zero churn (the architecture requires: flag-dark = byte-identical stream strings, so every existing test should pass UNTOUCHED — verify this is achievable by reading how tests drive the pulse: do any drive through the store action layer where the nonce would mint, or all directly at simulateCampaignWorldPulse where the input is controlled?); (b) THE PROMISE homes: every place the a-seed-is-a-world law is stated — docs (grep docs/ for the promise language), marketing/site copy files in src/, code headers, tests asserting determinism as a product promise — list each with its current sentence verbatim, because the owner-signed amendment must land amended-in-place in EVERY home (the split-brain class); (c) the CQ5 flag-law surfaces for a NEW flag (manifest ENGINE_GATED_VIRTUAL_RULE_KEYS + certification row home + where the first gate read would live) — quote the current manifest tail; (d) whether this volume needs a coupling-registry prefix: would the epoch machinery import across layer families (read the LAYER_PATTERNS table in tests/lint/couplingInclusion.walker.test.js as amended at HEAD, and say whether an EP-prefixed volume minting a store-action + kernel-seam change even touches src/domain cross-layer pairs); (e) the four-fence + lit-mutant pattern the new flag owes (read one fresh fence file for the current house form — casusCommerciiDormancyFence is the newest). RETURN JSON: rows, pinFamilies (array with threading answer), promiseHomes (array with verbatim sentences), flagSurfaces, prefixVerdict, hazards.`, { label: 'census:tests-promise', phase: 'Census', model: 'opus' }),
])

phase('Architect')
const arch = await agent(LAWS + `

YOU ARE THE COMPILING ARCHITECT. The four censuses: ` + JSON.stringify({ c1, c2, c3, c4 }) + `

Also read (for cohesion, from the tree and scratchpad): docs/DESIGN_FP_ARCHITECTURE.md sections 1, 3, 5, 10 (the laws, flag law, wave order, protocol — binding verbatim); the promoted fold volumes at /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c44e5d99-2ba5-49d5-a554-40b68534c8eb/scratchpad/fold-pass/DESIGN_FP_ARCH_ES.md and DESIGN_FP_ARCH_WY.md headers + wave sections (the fold lands before this volume; your queue insertion is POST-FOLD, after 75 waves / 52 flags); docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md section 10.

COMPILE ${OUT}/DESIGN_EPOCH_ARCHITECTURE-DRAFT.md — the complete build volume for the ADVANCE-EPOCH (living futures) program. House template (the ES/WY volumes are your model): section 0 THE PREMISE with the owner directive verbatim and what is genuinely new; section 1 THE CANONICAL MODEL — the nonce: its shape (generateSeed-class string), where it lives (the history/action entry field the census measured), drop-when-absent at every level, legacy entries WITHOUT the field replay as epoch-0/current behavior (back-compat exact), zero new top-level keys if achievable; section 2 THE FLAG — name it (recommend one consistent with the manifest family), the CQ5 one-commit law, the four-fence dormancy set + lit-mutant, and THE DARK-STATE BYTE-IDENTITY CONTRACT: flag absent/false produces the EXACT current stream-identity strings (quote the census verbatim composition; the lit path appends its term ONLY when a nonce is present — spell the exact lit composition and prove dark is byte-identical by construction); section 3 THE MECHANISMS — (3a) the mint site at the store-action layer (never wall-clock inside the kernel; the kernel stays pure taking the nonce as INPUT), (3b) THE ONE KERNEL SEAM with a TR-1-style seam contract (the stream-identity term; zero other kernel edits; draw-count identity in dark state), (3c) undo/backtrack semantics: every fork point the census found — undo deletes the entry, re-advance mints fresh; define what happens to downstream saves on backtrack (the lived-past immutability law: past entries never rewrite; branching semantics stated plainly), (3d) replay: recorded nonces feed byte-exact replays (the debugging door), (3e) THE GENERATOR-SIDE LAW: the affordance disposition table from census 3 verbatim, each NEEDS-* row assigned to a wave, plus the standing walker that enforces the law structurally (an affordance census walker: every user-facing regen affordance minting entropy is registered; a silent-replay affordance reds — design it against the fixture-vacuity classes: non-empty denominator, negative control); section 4 THE WAVES — small, dependency-ordered (likely EP-0 pure nonce plumbing dark, EP-1 the kernel seam + flag trio, EP-2 undo/fork semantics + surfaces, EP-3 generator-side affordance compliance + walker, EP-4 THE PROMISE amendment + marketing copy), each with charter, gates, ledger row, per the section-10 protocol; section 5 QUEUE INSERTION verbatim-landable (post-fold SOL_QUEUE rows; where relative to cycle 2 — RECOMMEND: after the lighting road, before or beside the Wayfare early waves, argue it); section 6 JUDGMENT BLOCKS (vetoable); section 7 OPEN CHAIR QUESTIONS max 5 with recommendations (include: branching model — linear-with-discard vs keep-both-timelines someday; whether the nonce term also covers the interval-cadence variance; the flag name); section 8 SEAMS PINNED BOTH SIDES — THE PROMISE amendment lands amended-in-place in EVERY home census 4 found (list each with its exact new sentence), the veil check, the marketing guard (the same-world-different-histories claim unlocks ONLY at lighting), and the Lifecycle-paths clause for the nonce field covering every path census 2 found (create/read/persist/export/import/clone/migrate/undo). Sizing discipline: this is a SMALL volume — target 600-900 lines, not an ES/WY epic; exhaustive means every seam named, not maximal prose. Byte-scan the output. RETURN JSON: file, lineCount, waves (array), flagName, chairQuestions (count), anomalies (anything a census claim contradicted).`, { label: 'architect:epoch-volume', phase: 'Architect', model: 'opus', effort: 'high' })

phase('Cohesion')
const coh = await agent(LAWS + `

YOU ARE THE ADVERSARIAL COHESION CRITIC. The architect reported: ` + JSON.stringify(arch) + `

Read the draft at ${OUT}/DESIGN_EPOCH_ARCHITECTURE-DRAFT.md WHOLE, the four census returns embedded above it in this brief chain (re-derive their load-bearing claims yourself where cheap), and spot-verify against the LIVE tree. Your default posture: the volume contains a defect. Check with EXECUTED-LOGIC SCRUTINY: (1) THE DARK-STATE BYTE-IDENTITY CONTRACT — walk the exact string compositions character by character: does flag-absent REALLY produce today's exact stream identities on every root composition the census found (not just the pulse one)? An epoch-0 term that APPENDS anything in dark state is a catastrophic golden break — hunt for it; (2) THE PAST-IMMUTABILITY LAW — walk every undo/backtrack fork point: can any path REWRITE an already-lived entry (not just re-draw the future)? The regen-edit-loss and history-reroll machinery interactions especially; (3) THE KERNEL SEAM — exactly ONE kernel edit, contract-complete in TR-1 style, no second seam smuggled; the kernel takes the nonce as INPUT (zero wall-clock inside); (4) LEGACY REPLAY — old saves with no nonce field replay byte-exact through every lifecycle path census 2 enumerated; name any path the volume forgot; (5) THE GENERATOR-SIDE DENOMINATOR — is the affordance table exhaustive against your own independent grep (run the multi-spelling search yourself); does the walker design survive the recorded pin-vacuity classes (filename-anchored vacuity, empty-denominator, self-referential census); (6) TEST-ESTATE ZERO-CHURN — does every existing determinism pin really pass untouched under the dark state, per how tests actually drive the pulse; (7) COHESION — no contradiction with the FP volume laws, the CQ5 flag law, the fold volumes in flight, the war volume, veilPublicPayload, THE PROMISE amendment homes (every home amended, none missed — the split-brain class), and the WY cadence-variance fact (the interval term already varies streams — does the volume state the relationship between cadence variance and nonce variance coherently?); (8) NO GATED ITEM EXECUTED — tuning bands raw, nothing lit, the marketing claim locked until lighting. RETURN structured JSON only.`, { label: 'verify:epoch-cohesion', phase: 'Cohesion', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings', 'notes'], properties: { verdict: { type: 'string', enum: ['COHESIVE', 'NEEDS-REVISION'] }, findings: { type: 'array', items: { type: 'object', required: ['severity', 'claim'], properties: { severity: { type: 'string', enum: ['HIGH', 'MEDIUM', 'LOW'] }, claim: { type: 'string' }, evidence: { type: 'string' } } } }, notes: { type: 'string' } } } })

phase('Amend')
const amend = await agent(LAWS + `

YOU ARE THE AMENDMENT ARCHITECT. The cohesion verdict was NEEDS-REVISION; the full findings object: ` + JSON.stringify(coh) + `

The architect's own anomalies (A1..A8) are in: ` + JSON.stringify(arch) + `

AMEND ${OUT}/DESIGN_EPOCH_ARCHITECTURE-DRAFT.md IN PLACE under these chair rulings (final, vetoable, never re-rule):

R1 (HIGH, the hash01 family): the denominator law extends to EVERY seeded-entropy idiom. Census the ENTIRE hash01-on-rngSeed family yourself (all 5+ modules, 8+ sites) AND run one further sweep for any other idiom deriving draws from worldState.rngSeed by any spelling (two consecutive empty sweeps close the census). EVERY site gets a disposition row in the section-3b table under the same two families (tick-varying nonce-safe / year-keyed nonce-hostile with the display-surface rule). The no-disposition-row-is-a-build-STOP law covers both families.

R2 (HIGH, the preview): previewCampaignWorldPulse is a CONSUMER and joins the consumer table — it must be threaded the SAME epoch the committed advance would use, because predicting the actual future is its entire job. Add the pin: preview and commit from identical state with the same pending epoch produce identical worlds (executed, both flag states).

R3 (HIGH, realmVerbExecution): re-measure the module yourself; correct table row 14 to what stands (three compositions, two idioms, one tick-free); the tick-free composition gets its OWN measured disposition — never inherit the class rationale it falsified.

R4 (MED, flag-driven dark state): the contract becomes FLAG-DRIVEN — the mint site is gated on the flag so the epoch VALUE cannot exist dark, AND the accessor treats any value as absent when the flag is dark (belt and braces), with a gate-polarity fence covering the exact path the critic named.

R5 (MED, accessor fidelity): per-site fallback fidelity is REQUIRED — the accessor takes each site's own absent-seed coercion as an explicit argument (or per-family accessors reproduce each), and the dormancy proof is PER-SITE byte-identity, all fourteen-plus compositions, not one global argument.

R6 (MED, the queue insertion): rewrite section 5 against the ACTUAL post-fold state — read the LIVE docs/SOL_QUEUE.md plus the fold package insertions at /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c44e5d99-2ba5-49d5-a554-40b68534c8eb/scratchpad/fold-pass/QUEUE-AND-AMENDMENTS.md (the fold lands FIRST; your rows compose AFTER its rows), and cite ONLY documents that exist at their real paths.

R7 (MED, EP-4 scope): RULED — every user-visible behavior change in EP-4 goes behind the flag; the TWO repairs touching a paid/founder surface or a persisted shape are OWNER-GATED: extract them from the wave entirely and park them as owner-queue rows with recommendations. EP-4 re-scopes to the flag-gated remainder.

R8 (MED, the affordance census + scan): re-census the generator affordances with wider spellings until two consecutive sweeps add nothing; redesign the source-scan arm on the CR-FP-11 precedent — an ARGUED_EXCLUSION map with written per-site reasons plus a shrink-only baseline for pre-program debt, so day one is GREEN with the debt COUNTED, never red on legitimate sites.

R9 (LOW, the synthetic record): the totality pin carries an anchored negative excluding the synthetic pulseHistory record by its measured identity.

ALSO fold the architect anomalies: A7 is RESOLVED by the chair (memory/the-promise-ratified.md is ALREADY AMENDED to record the owner's 2026-08-05 signature — state this in section 8 and drop any EP-5 obligation to amend it; the MEMORY.md reroll line is also already corrected); A8 (advanceMultiTick): the fence FIXES its stance — dark-state byte-identity is defined PER-PATH as each path composes today (the multi-tick literal one_week path included), stated in the fence spec. Keep the volume's surviving strengths untouched (the empty-suffix pulse-root contract, the one zero-line kernel seam, the collapseIntervalHistory survival proof). Update every count the amendments move. Byte-scan. RETURN JSON: amendmentsApplied (nine+ entries with one-line evidence each), newDenominators (hash01 sites found, affordance count), lineCount, anomalies.`, { label: 'amend:epoch-volume', phase: 'Amend', model: 'opus', effort: 'high' })

phase('Recohere')
const coh2 = await agent(LAWS + `

YOU ARE THE FRESH-EYES RE-VERIFIER (you have not seen this volume before; trust nothing). The amendment lane reported: ` + JSON.stringify(amend) + `

The prior verdict's nine findings: ` + JSON.stringify(coh && coh.findings) + `

Read ${OUT}/DESIGN_EPOCH_ARCHITECTURE-DRAFT.md WHOLE and verify against the LIVE tree at the current HEAD: (1) each of the nine findings genuinely closed — re-derive, do not trust the report; (2) the hash01 census — run your OWN independent sweep (multiple spellings, two-pass rule) and confirm the volume's denominator matches yours exactly; a site you find that the volume lacks is a HIGH; (3) the preview pin, the per-site accessor fidelity, and the flag-driven dark contract are specified as executable proofs, not prose; (4) EP-4 contains ZERO unflagged user-visible change and ZERO owner-gated repair (the two parked items appear as owner-queue rows only); (5) the queue insertion cites only real paths and composes after the fold package rows; (6) counts coherent everywhere the amendments moved them; (7) nothing that survived the first scrutiny regressed (the empty-suffix contract, the one-seam claim, the lifecycle survival). RETURN structured JSON only.`, { label: 'recohere:epoch-volume', phase: 'Recohere', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings', 'notes'], properties: { verdict: { type: 'string', enum: ['COHESIVE', 'NEEDS-REVISION'] }, findings: { type: 'array', items: { type: 'object', required: ['severity', 'claim'], properties: { severity: { type: 'string', enum: ['HIGH', 'MEDIUM', 'LOW'] }, claim: { type: 'string' }, evidence: { type: 'string' } } } }, notes: { type: 'string' } } } })

phase('Polish')
const polish = await agent(LAWS + `

YOU ARE THE POLISH LANE. The recohere verdict (all nine prior findings closed; nine narrow residuals): ` + JSON.stringify(coh2) + `

CLOSE ALL NINE under these chair rulings: P1 (HIGH) — complete the READ-SITE half of the entropy census: every site that READS an entropy root (not only composes one), the two missing sites added with dispositions; the pulseKernel read site JOINS the kernel seam contract explicitly — the contract restates as the exact enumerated token-edit list (honesty over the three-edits slogan), and rows 1 and 13 get re-root specs that include their read sites. P2 — pick an in-scope fold line for section 3b.1 by MEASURING where both names exist (quote the real line). P3 — fix the section-3e Wave column: A4, A5, A11 route to the PARKED owner rows, never EP-4; grep the whole volume afterward for any remaining EP-4 routing of those three. P4 — ONE parked-row count, corrected at all three sites against the four-row table. P5 — EP-4 slice 1: CLASSIFY the persisted-allowlist edit against the owner-gated persisted-shape class explicitly; DEFAULT RULING: when the classification is arguable, PARK it beside A11 as an owner row — only keep it in the wave with a stated values-only-no-shape argument that would survive the critic. P6 — reconcile the disposition tally with the SEED_DOOR_DEBT baseline by RE-MEASURING (the measured truth wins; state which figure moved). P7 — re-run the three walker closure-record figures at HEAD and correct them (the record must reproduce on re-run). P8 — H7 goes BEHIND THE FLAG (a user-facing generate button's yield never changes unflagged; if the change is provably byte-identical state the argument instead, but the default is the flag). P9 — the preview-commit pin NAMES its projection: the normalized-world comparison (the dormancyOracle normalizeForDormancy idiom) with the enumerated recorded-field exclusions listed in the pin spec, so the pin is executable from the text. Keep everything the recohere pass confirmed untouched. Update every count you move. Byte-scan. RETURN JSON: closures (nine entries with evidence), seamContractEditCount, anomalies.`, { label: 'polish:epoch-volume', phase: 'Polish', model: 'opus' })

phase('Seal')
const seal = await agent(LAWS + `

YOU ARE THE FINAL CHECK (fresh eyes). The polish lane reported: ` + JSON.stringify(polish) + `

The nine residuals it closed: ` + JSON.stringify(coh2 && coh2.findings) + `

Verify each is genuinely closed in ${OUT}/DESIGN_EPOCH_ARCHITECTURE-DRAFT.md by re-derivation against the live tree at current HEAD: run your own read-site sweep over the 25 compositions (finding a third missing read site is a HIGH); confirm the seam contract's enumerated edit list matches what re-rooting actually requires; grep the volume for any surviving EP-4 routing of A4/A5/A11; re-run the walker closure figures; confirm the preview pin is executable as specified; confirm every count agrees everywhere it appears. RETURN structured JSON only.`, { label: 'seal:epoch-volume', phase: 'Seal', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings', 'notes'], properties: { verdict: { type: 'string', enum: ['COHESIVE', 'NEEDS-REVISION'] }, findings: { type: 'array', items: { type: 'object', required: ['severity', 'claim'], properties: { severity: { type: 'string', enum: ['HIGH', 'MEDIUM', 'LOW'] }, claim: { type: 'string' }, evidence: { type: 'string' } } } }, notes: { type: 'string' } } } })

phase('Touch')
const touch = await agent(LAWS + `

YOU ARE THE FINAL-TOUCH LANE. The seal verdict (all nine residuals closed; five new findings) and the polish anomalies: ` + JSON.stringify({ seal, polishAnomalies: polish && polish.anomalies }) + `

CLOSE ALL under these chair rulings: T1 (HIGH, the Arm A writer) — design the byYear stamp writer HONESTLY: it executes inside pulseKernel.js AFTER the calendar advances (measure the correct ordering yourself against the live kernel — where the calendar moves, where the year-family reads happen, and whether the stamp must precede any same-tick read); the seam-contract edit inventory GROWS to enumerate it exactly (its own declared line budget; the edit-inventory pin updates; the retired-slogan honesty rule applies — the count is what the count is); the in-pulse read re-aims at the correct world state and the volume states WHY that ordering is the coherent one. T2 (the polish anomaly A-P3-1, RULED) — unify EVERYTHING flag-driven: the two conditional spreads key on the flag-gated epochTerm (null when dark by mint gating), never on raw value presence; state the invariant chain explicitly (dark implies epochTerm null implies spreads vanish implies byte-identical) and make the dormancy fence assert it on the exact path the anomaly names. T3 (MED) — correct the sweep-C figure AND grep the whole volume for any other survival of the refuted figure (report the grep). T4 (LOW) — the economyReconciliation second collision gets its own disposition/negative-control row. T5 (LOW) — the hash01 consumer figure re-counts CALLS with a corrected command that reproduces. T6 (LOW) — personaSlicer has ZERO importers in src: re-anchor the walker negative control on a LIVE module (measure one), and record personaSlicer as a chair-owed dead-module disposition row (report, never delete). ALSO record the polish anomaly A-P3-3 as a stated fact in the preview section (preview and advance are behaviorally identical at HEAD except one record label — the pin design should lean on it). Update every count you move; byte-scan. RETURN JSON: closures (with evidence), armAWriterSpec (one paragraph), newEditInventoryTotal, anomalies.`, { label: 'touch:epoch-volume', phase: 'Touch', model: 'opus', effort: 'high' })

phase('Close')
const close = await agent(LAWS + `

YOU ARE THE CLOSING VERIFIER (fresh eyes; trust nothing). The touch lane reported: ` + JSON.stringify(touch) + `

The five seal findings it closed: ` + JSON.stringify(seal && seal.findings) + `

Verify in ${OUT}/DESIGN_EPOCH_ARCHITECTURE-DRAFT.md against the live tree at current HEAD: (1) the Arm A writer spec — walk the kernel yourself and confirm the specified ordering is executable and coherent (the stamp lands where the spec says, before every same-tick year-family read it must precede); the edit inventory matches what re-rooting requires, nothing unenumerated; (2) the flag-driven invariant chain is stated and fenced on the exact path; (3) the corrected figures reproduce by their own commands; (4) the two LOW dispositions and the personaSlicer re-anchor stand; (5) nothing previously confirmed regressed (spot-check the empty-suffix contract, the 25-composition and 22-read censuses, the preview pin, the owner-parked rows). RETURN structured JSON only.`, { label: 'close:epoch-volume', phase: 'Close', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings', 'notes'], properties: { verdict: { type: 'string', enum: ['COHESIVE', 'NEEDS-REVISION'] }, findings: { type: 'array', items: { type: 'object', required: ['severity', 'claim'], properties: { severity: { type: 'string', enum: ['HIGH', 'MEDIUM', 'LOW'] }, claim: { type: 'string' }, evidence: { type: 'string' } } } }, notes: { type: 'string' } } } })

phase('Settle')
const settle = await agent(LAWS + `

YOU ARE THE SETTLING LANE — the final content round. The closing verdict (all six rulings closed; five residuals) plus the touch anomalies: ` + JSON.stringify({ close, touchAnomalies: touch && touch.anomalies }) + `

CLOSE ALL EIGHT under these chair rulings: F1 — the stamp-survival pin extends to ALL NINE family-2 read sites as an EXECUTABLE spec (one parameterized pin over the enumerated nine is fine; zero prose-carried sites; the volume's own pin-dont-argue standard applies uniformly). F2 — Fence 5's M3 is respelled to match its own step 1: after a paused LIT advance, the ledger EXISTS with stamps for every committed tick; the fence asserts the correct shape (stamps present exactly for committed ticks, none beyond), never absent-entirely — a fence that reds on a correct implementation is a defect. F3 — correct the coverage-walker citation at all four sites by SYMBOL (find the real file/symbol and cite it the way the volume's own law demands). F4 — reconcile the row-1 read-site count everywhere to the binding definition (three; the fourth item reclassified with one stated reason). F5 — J-EP-6's home rationale restates on the independent layering grounds ONLY (the home is right; the gate-avoidance clause is dropped as asymmetric with the volume's own escape-hatch refusal). F6 (anomaly A-R4-1, RULED) — the ordering pin extends to assert the stamp survives into the RETURNED world (not merely past the composition site), closing the wholesale-replacement hole with an executed check instead of trust in intervening reassignments. F7 (A-R4-2, RULED) — EP-3 slice B's charter declares the spatialUsage.js manifest row a CQ5-CLASS SHARED-FILE touch and serializes the slice against flag waves; stated in section 4 and the queue rows. F8 (A-R4-3, RULED) — the byYear-vs-pulseHistory count divergence is stated as a DECLARED NON-INVARIANT with its collapse-semantics reason, placed where the next auditor will look, so it reads as design rather than leak. Update every count you move; byte-scan. RETURN JSON: closures (eight, with evidence), anomalies.`, { label: 'settle:epoch-volume', phase: 'Settle', model: 'opus' })

phase('Ratify')
const ratify = await agent(LAWS + `

YOU ARE THE RATIFYING VERIFIER (fresh eyes). The settling lane reported: ` + JSON.stringify(settle) + `

The eight items it closed: ` + JSON.stringify({ residuals: close && close.findings, anomalies: touch && touch.anomalies }) + `

Verify each in ${OUT}/DESIGN_EPOCH_ARCHITECTURE-DRAFT.md by re-derivation at current HEAD: the nine-site survival pin is executable as written; Fence 5 M3 passes on a correct implementation and reds on the mutant it names; the four citations resolve by symbol; counts agree everywhere; the ordering pin reaches the returned world; the CQ5-class declaration and the non-invariant note stand where stated. Spot-check three previously-confirmed pillars have not regressed (the empty-suffix dark contract, the 25/22 censuses, the owner-parked rows). If and only if EVERYTHING stands, the verdict is COHESIVE. RETURN structured JSON only.`, { label: 'ratify:epoch-volume', phase: 'Ratify', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings', 'notes'], properties: { verdict: { type: 'string', enum: ['COHESIVE', 'NEEDS-REVISION'] }, findings: { type: 'array', items: { type: 'object', required: ['severity', 'claim'], properties: { severity: { type: 'string', enum: ['HIGH', 'MEDIUM', 'LOW'] }, claim: { type: 'string' }, evidence: { type: 'string' } } } }, notes: { type: 'string' } } } })

phase('Fasten')
const fasten = await agent(LAWS + `

YOU ARE THE FASTENING LANE — four items, the last content round. The ratify verdict (all eight prior closures confirmed): ` + JSON.stringify(ratify) + `

CLOSE ALL FOUR under these chair rulings: G1 (HIGH, the accessor epoch source, RULED) — tickStreamSeedOf's epoch source IS the stamped ledger in the worldState it receives: seam edit 9 stamps the current advance's epoch into spatialLedgers.advanceEpoch ON the composed worldState BEFORE the downstream subtree call, so every family-1 site that receives that worldState can read the current tick's stamp through the accessor; SPECIFY the read explicitly in the accessor contract (signature, the exact ledger path, the current-tick selection rule), state WHY the ordering guarantees presence (cite the volume's own F1 ordering walk), keep the absent-fallback arm byte-verbatim for dark identity, and add the pin: a LIT tick's family-1 draws differ across two epochs while DARK draws are byte-identical — executed, not prose. If your measurement finds ANY family-1 site whose received worldState could predate the stamp, name it and give it an explicit threading instead — never let one site ride an assumption. G2 (MED) — respell the collapse-semantics premise ACCURATELY at ALL THREE homes including section 1.5 (what is true: the path composes by SPREAD not allowlist reconstruction, spatialLedgers IS carried through, and the byYear-vs-record-count divergence is the declared non-invariant); grep the volume afterward for any surviving copy of the false neither-read-nor-written phrasing and report the count (with a command that can fail). G3 (LOW) — reconcile the mutant matrix across its two homes to ONE spec (rows 2-9 plus the returned-world arm, per section 3b.1a — the charter home updates). G4 (LOW) — correct the attribution count (seven of nine in argument-identical groups) where stated. Byte-scan. RETURN JSON: closures (four, with evidence), anomalies.`, { label: 'fasten:epoch-volume', phase: 'Fasten', model: 'opus' })

phase('Cohere')
const cohere = await agent(LAWS + `

YOU ARE THE CLOSING VERIFIER. The fastening lane reported: ` + JSON.stringify(fasten) + `

The four items: ` + JSON.stringify(ratify && ratify.findings) + `

Verify each by re-derivation in ${OUT}/DESIGN_EPOCH_ARCHITECTURE-DRAFT.md against the live tree: the accessor contract names its epoch source and the ordering argument holds against the real kernel (walk it); the collapse premise is now true as written at all three homes (falsify-by-grep attempt required, with a command that can fail); the mutant matrix agrees with itself; the counts agree. Spot-check two prior pillars (the empty-suffix dark contract; the nine-site survival pin). If everything stands, COHESIVE. RETURN structured JSON only.`, { label: 'cohere:epoch-volume', phase: 'Cohere', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['verdict', 'findings', 'notes'], properties: { verdict: { type: 'string', enum: ['COHESIVE', 'NEEDS-REVISION'] }, findings: { type: 'array', items: { type: 'object', required: ['severity', 'claim'], properties: { severity: { type: 'string', enum: ['HIGH', 'MEDIUM', 'LOW'] }, claim: { type: 'string' }, evidence: { type: 'string' } } } }, notes: { type: 'string' } } } })

return { c1, c2, c3, c4, arch, coh, amend, coh2, polish, seal, touch, close, settle, ratify, fasten, cohere }