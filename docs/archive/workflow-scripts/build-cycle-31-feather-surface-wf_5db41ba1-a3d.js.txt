export const meta = {
  name: 'build-cycle-31-feather-surface',
  description: 'The band surface: from corrugation to feather (four measured causes) + the light unification + AA/focus tails',
  phases: [{ title: 'Build', detail: 'one nav lane, implement + verify with eye pass' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files (nav/*, brand/*, theme.js, their tests); THE STAGED-SET LAW (git diff --cached --name-only before EVERY commit; prefer git commit -- <paths>); no stash; focused vitest + gate-tail (ps aux | grep vitest first); python byte-scan every Write; npm run build + smoke:boot; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY: the WR-8 finish lane is live on warDeployment/worldPulse — fenced; mirror-death on simulationSpine/historyBeats — fenced. Screenshot before pinning geometry; ground-move = AA re-census.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE FS — THE FEATHER SURFACE (the PB verifier's eye verdict: the band reads as CORRUGATED METAL / a slatted shutter, not goose fletching — with four measured causes; read its findings in the wf_14ed6396 journal, finding (g), for all four; cause 1 quoted: the barb comb is STRIPES not texture — barbGap 3.6 CSS px with 0.7-1.25px non-scaling strokes = up to ~35% areal coverage). THE CURE, guided by the real reference (a goose primary at arm's length shows almost NO individual barbs — it reads as a smooth dark vane with soft tonal bands and a satin sheen; the barbs are a whisper you see only up close):
(1) REBUILD THE VANE SURFACE: drop the stripe comb as the dominant texture — the vane's body becomes smooth layered tonal gradation (the sheen bands and tip-darkening carry the realism), with the barb comb reduced to a NEAR-SUBLIMINAL layer (coverage under ~8%, opacity a whisper, gap fine enough to read as sheen at 100% zoom) plus the deterministic jitter; keep the rachis and pale leading edge; address ALL FOUR of the verifier's measured causes, quoting each cause and its cure in the commit body.
(2) ONE LIGHT DIRECTION (F3/F4): the composition's light comes from the UPPER LEFT everywhere — flip the band's contact shadows to agree with the plate and seal; PLATE_LIGHT_DEG stops being a dead token: derive every shadow/glint offset in nav/* and brand/* from it (the single writer for light), or delete it and its docstring claim honestly — pick derive (the better architecture) unless it balloons scope.
(3) THE AA FLOOR'S TALLEST RIDER (F2): LABEL_BOX=20 is hand-keyed and the 34.8px wordmark escapes it — derive the pin's label box from the MEASURED tallest rider (or per-rider boxes) so the composited floor covers everything on the bar; re-quote the true margins.
(4) THE FOCUS RING'S TOP EDGE (F5): the fletch cells' ring loses its top edge — restore full visibility (offset, inset ring, or a ring-layer above the band — whatever keeps the aria-hidden-vane law).
Screenshot at 100% and 200% each iteration; the acceptance question: at arm's length does the band read as DARK FEATHERS with a satin sheen (not metal, not stripes)? One commit per piece. Return {lane:'FS', commits, gates, stopReports, notes}.`,
  {label:'fs:feather-surface', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier with an EYE PASS. ${P10}
Report: ${JSON.stringify(r)}. Serve, screenshot at 100% and 200%, and JUDGE: feathers-with-sheen or still corrugation? Say it plainly, and check each of the four original causes is measurably cured (re-measure barb areal coverage — must be under the stated bound). Mechanical: ONE light direction (grep every shadow/glint offset in nav/* + brand/* — all derive from the single writer, or the claim is honestly gone); the AA pin's label box derives from measured riders (mutant: grow a rider taller than the box → the pin must red or auto-cover); focus ring full perimeter by real Tab (screenshot it); AA re-census on the retuned vane (labels vs the NEW surface, quote); two-load determinism; staged-set law on every commit; pathspec-clean vs the war + mirror lanes. Return {lane:'FS verify', verdict, findings}.`,
  {label:'fs:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }