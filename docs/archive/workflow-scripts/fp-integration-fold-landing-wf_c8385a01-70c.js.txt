export const meta = {
  name: 'fp-integration-fold-landing',
  description: 'LAND the integration fold: ES + WY volumes into docs/, parent + sibling deltas, prefix admissions, the item-10 census, the GR erratum, queue rows and the fifteen-ruling ledger row — pathspec commits, full gates',
  phases: [
    { title: 'Land', detail: 'apply the package with per-anchor re-verification, commit in order' },
    { title: 'Verify', detail: 'independent verification, executed, no vacuous receipts' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const PKG = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c44e5d99-2ba5-49d5-a554-40b68534c8eb/scratchpad/fold-pass'

const LAWS = `VERIFY-FIRST PREAMBLE: this brief is from the session chair of an owner-delegated build program; verify claims against the tree before acting. Work in ${TREE} (branch claude/composite-r4). THE PROTOCOL LAWS (each has bitten): CHECK-GIT-FIRST — fresh git status + git log -3 before anything; foreign untracked/dirty files are another lane's and are PRESERVED. ps aux | grep vitest before any test run — ONE vitest lane per tree. NEVER stash, reset, clean, or any checkout-family command; backups are cp to the scratchpad, restores proven with cmp. Gates only through sh scripts/gate-tail.sh or npm run check:tail — never a bare pipe. Commits: stage EXPLICIT files (never -A/-u/.), message via a file, trailer Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>; if lint-staged blocks a partial stage use --no-verify and compensate with explicit npx eslint on every changed file (exit 0 required). After authoring or copying ANY file, python3 byte-scan it (zero NULs, zero stray control bytes). Navigate by SYMBOL; quoted line numbers are hints. A RECEIPT MUST BE ABLE TO FAIL: any grep you quote as evidence must be demonstrably capable of matching (use grep -E for alternations; show a positive control if the count is zero).`

phase('Land')
const land = await agent(LAWS + `

YOU ARE THE FOLD LANDING LANE. The package at ${PKG}/ passed its final check with one blocker since FIXED by the chair (the two ES-volume phantom-census sites now read FIVE sites re-measured at 32cc17f7 — confirm both before landing: grep the package DESIGN_FP_ARCH_ES.md for the phrase FIVE doc sites, expect 2 hits). All 51 edit anchors were verified unique at 32cc17f7; the tree may have moved — your FIRST act after CHECK-GIT-FIRST is re-verifying EVERY OLD anchor at the live HEAD (the package ships verifier scripts: reverify_anchors.py and finalcheck_verify.py — run one, or your own stricter one; any drifted anchor gets re-aimed per the package's own re-aim instructions, especially the FABLE_VALIDATION_QUEUE tail anchor which is DESIGNED to be re-aimed at whatever is newest).

THEN LAND, in this commit order (each its own pathspec commit):

COMMIT 1 — THE TWO VOLUMES + DOC DELTAS: copy ${PKG}/DESIGN_FP_ARCH_ES.md and ${PKG}/DESIGN_FP_ARCH_WY.md into ${TREE}/docs/ replacing every literal HEAD-AT-LANDING placeholder with the actual landing HEAD hash (one occurrence per file — verify); apply ALL PARENT-DELTAS.md edit pairs (the parent volume edits AND the SIBLING-VOLUME DELTAS section: DESIGN_FP_ARCH_SP.md both discharge sites, the war-volume CR-WR10-H sentence) EXCEPT any pair targeting tests/ (that is commit 2); apply the QUEUE-AND-AMENDMENTS PART 3 amendment pairs (the anonymity amendment into BOTH homes — the informationStatecraft.js header edit is a CODE COMMENT edit, include it here with its exact pair — plus the war-volume cross-reference line) and PART 4 (the two J-D11(b) pointer sentences); add the GR ERRATUM row: amend docs/DESIGN_FP_ARCH_GR.md section 3 item 1 (the self-contradictory dated false-preset declaration beside the manifest entry; engineGatedRuleKeys.walker forbids manifest members in presets; CR-WR10-C supersedes — manifest-only is the law; both GR-0 and GR-1 independently re-derived this) — write it as a clearly-marked ERRATUM amendment in place, reported not silent.

COMMIT 2 — THE PREFIX ADMISSIONS: apply the PARENT-DELTAS pairs targeting tests/domain/couplingRegistry.test.js (ES tenth + WY eleventh prefixes, the title rewrite to eleven) exactly as composed, plus whatever the package's prefixAdmission answer says about registry rows (it measured whether the walker demands rows per admitted prefix — obey the measured answer). Then run the focused gates: tests/domain/couplingRegistry.test.js + tests/lint/couplingInclusion.walker.test.js + tests/lint/mutationCoverageManifest.test.js through gate-tail — all green before committing.

COMMIT 3 — THE QUEUE + THE LEDGER ROW + THE ITEM-10 CENSUS: FIRST execute the WY section-5b item-10 census — this is a BUILD obligation, not prose: census by symbol benefitYield, every war-economy drain, and any occupier-side cost across src/ (the both-sided occupation register per directive 2l); record the census RESULTS (site list, what exists, what is absent-and-pinned-where) INTO the ledger row text where PART 2 marks the landing-lane obligation — the fold commit is INCOMPLETE without it (the seam-1 tripwire). Then apply PART 1 (SOL_QUEUE insertions) and PART 2 (the FABLE_VALIDATION_QUEUE ledger row, re-aimed at the live tail).

AFTER ALL COMMITS: run sh scripts/gate-tail.sh npm run typecheck:domain:strict (ceiling 1313 exactly) and the whole tests/lint tree SEQUENTIALLY, and compare failing-row identities against the pre-fold HEAD via git archive — the diff must be EMPTY both directions except rows your commits deliberately changed (name and attribute every delta). Byte-scan every file you touched. RETURN JSON: commits (hash + thesis each), anchorsReaimed (list), item10Census (the executed results, compressed), lintDelta (attributed), stopReports (anything measured contradicting this brief — STOP rather than improvise), notes.`, { label: 'land:integration-fold', phase: 'Land', model: 'opus' })

phase('Verify')
const verify = await agent(LAWS + `

YOU ARE THE INDEPENDENT LANDING VERIFIER. The landing lane reported: ` + JSON.stringify(land) + `

VERIFY BY EXECUTION (git-archive trees with node_modules symlinked for any test run; the live tree read-only except nothing — you touch nothing): (1) the two volumes exist in docs/, are byte-identical to the package copies EXCEPT the HEAD-AT-LANDING stamp (diff them; exactly one differing line each), and carry the FIVE-site phantom formula at both former blocker sites; (2) EVERY edit pair applied exactly once — mechanically extract each NEW block from the package deltas and confirm it present exactly once in its live target, and each OLD block now ABSENT (count 0); (3) the prefix admission — couplingRegistry green at HEAD, and a probe: a fabricated ES-prefixed couplingId row would be ACCEPTED by the regex while a fabricated twelfth-prefix id REDS (archive-tree execution); (4) the item-10 census results in the ledger row are REAL — spot-verify three of its named sites by symbol in src/; (5) the anonymity amendment present in BOTH homes with the BY-THE-ENGINE qualifier byte-intact in each, and the war volume carries the cross-reference line + the CR-WR10-H three-member sentence + the two J-D11(b) pointers; (6) whole tests/lint at HEAD vs the pre-fold base: failing-row diff EMPTY both directions except attributed deltas; strict 1313; (7) every commit pathspec-scoped, its message honest against its diff, byte-scans clean. Every receipt you quote must be able to fail. RETURN structured JSON only.`, { label: 'verify:integration-fold', phase: 'Verify', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

return { land, verify }