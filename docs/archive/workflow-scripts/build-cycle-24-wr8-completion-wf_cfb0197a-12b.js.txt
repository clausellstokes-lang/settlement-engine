export const meta = {
  name: 'build-cycle-24-wr8-completion',
  description: 'WR-8 completion: verifier repairs + conquest execution, then the razing + license under CR-WR8-E/F',
  phases: [{ title: 'Repairs3', detail: 'F1-F6 + slice 3 conquest execution' }, { title: 'Razing45', detail: 'the razing + the vengeance license' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); R-BLD-9 strict law; node ESM link + npm run build green; dormancy goldens BYTE-IDENTICAL with flags dark (hash pre/post, quote); one slice one commit + queue row; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. ⚠️ THE NUL HAZARD HAS BITTEN FOUR TIMES: never author escape sequences via raw bytes — use template-literal \\u0000 spellings and PYTHON BYTE-COUNT every new file before committing (grep/tail are blind to NUL). Queue docs carve-or-commit-body with attribution.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}

phase('Repairs3')
const a = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE W8-C — THE W8-A VERIFIER REPAIRS + SLICE 3.
REPAIRS (findings F1-F6, all verified):
F1 — the wiring claim says hostileTargetsOf reaches BOTH war-opening consumers; it reaches ONE (the strategy chooser). Wire the coalition-decision consumer through the same seam (or through its own equivalent gate if its shape demands — but ONE derivation, no fork), pin both consumers' reachability, and correct the queue row's claim.
F2 — MATERIAL: the court's own-strength leg falls back to the literal 'ready' band in every production world (beliefRecord(observer, observer) never populated). CHAIR DIRECTION: the volume wants BELIEVED home-front reserves — investigate the belief substrate: if self-records are architected but unwritten, wire the self-record write (dark) so the fallback is unreachable in generated worlds; if self-belief is genuinely out of scope, derive the own-strength band from the court's OWN observable state through an existing reader (self-reads are not K3 violations — K3 fences OTHERS' truth) and record which path you took vetoably. Pin: a generated world's own-strength band VARIES across courts (the constant is dead).
F3 — openFronts passes the CANDIDATE count; make it count actual open war fronts (the existing war ledger has the truth) or rename the field to what it is — semantics and name must agree, pinned.
F4/F5 — prune the dead exports: conquestVoteWeight01 duplicates a fork ALREADY CLOSED by CR-WIRE-A (the live vote derives from the picture) — DELETE it citing the ruling; the other dead surfaces either gain their consumer in slice 3 or go.
F6 — the 768→2,000 docstring figure.
SLICE 3 — CONQUEST EXECUTION (N), per the unblocked plan the W8-A stop recorded: occupation.js is the mouth (555/800, 245 free), conquestExecution.js the leaf, conquestDoctrineActive the gate (built + pinned): the OVERWHELMING gate (negative case: clearly-winning-but-not-overwhelming still negotiates — the volume's hardest pin); occupation is NOT annexation (K1 siege_occupation, garrison cost, unrest, revolt standing); THE INHERITANCE COUNTERFORCE (the victor's K_food sums over what it now holds — derived from EXISTING engines, no new estimator); stage-neuter controls; dormancy byte-identical. Return {lane:'W8-C', commits, gates, stopReports, notes}.`,
  {label:'w8c:repairs-slice3', phase:'Repairs3', model:'opus', effort:'high', schema:S})
const av = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(a)}. Re-verify F1 (BOTH consumers reach the seam — neuter it, both paths' pins red); F2 (generate a real corpus: own-strength bands VARY, the fallback literal is unreachable — prove by census); F3 (the field's value equals the war ledger's truth on a live fixture); the overwhelming negative case EXECUTED; the inheritance counterforce arithmetic on a real conquest fixture (the victor's food K includes the conquered holding's state); occupation record shape matches K1's existing vocabulary (no new state words); stage-neuter re-run; dormancy re-hashed; strict/build/walkers; pathspec. Return {lane:'W8-C verify', verdict, findings}.`,
  {label:'w8c:verify', phase:'Repairs3', model:'opus', effort:'high', schema:V})

phase('Razing45')
const b = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
Prior: ${JSON.stringify({repairs:a?.commits, verdict:av?.verdict})}. LANE W8-D — THE RAZING + THE LICENSE, under two fresh chair rulings (vetoable, cite both):
CR-WR8-F (the tierEligibility fork): THE RAZING FORCES NO TIER — demotion-follows-truth is the volume's OWN pin. The sack writes POPULATION (deaths + refugees + roaming named cast, conserved to the person); tierEligibility's one law (pop < currentMin * 0.82) reads the new truth and decides; the volume's 'sack severity → 1-vs-2-tier' band is a RECEIPT DESCRIPTOR (how many tiers actually fell), never an input and never a second writer. If a light sack demotes nothing, that is the world being honest — pin that arm too (a sack above threshold leaves the tier standing).
CR-WR8-E (the license persistence scoping): the memoryHorizon refusal does NOT cover the license ledger. The distinguishing test: memoryHorizon had NO ratified lifecycle spec; the vengeance license's persistence IS volume-architected in full (spatialLedgers family, JSON-round-trip, regen/undo carry, import validates, expiry as a READ against heldSince). A persistence surface whose complete lifecycle the owner-ratified volume specifies MAY build dark and vetoable; one it never specified may not. Build the license ledger EXACTLY to the volume's lifecycle paths — anything the volume leaves unspecified about persistence → STOP-report, do not invent.
SLICE 4 — THE RAZING (R+R2): the third intent PUNISH, evil-exclusive at initiation (the hard alignment gate); the double gate (CR-WR8-B's composite + won siege); deaths-not-departures through §3 demographics (conserved sack class, banded escape share, named cast disperses roaming — never engine-killed); CR-WR8-A's edge-type flip to hostile on existing victim-adequate edges; institutions to damage-impairment/shell; the victor LEAVES (the departure is the receipt).
SLICE 5 — THE VENGEANCE LICENSE (R2): minted to victim-adequate relationships, ONE per coalition, consumed once, no counter-license; coupled via CR-WR8-B; sanctioned-not-free for good actors; the world judges on the observer's axis; deterrence through WR-6's web pointed at the aftermath. PINS owed: extremity negative BOTH directions; initiation unreachable for non-evil (walk it); coupling; the closed loop; conservation to the person; demotion-follows-truth BOTH arms (falls when truth falls, stands when it doesn't); license JSON-round-trip through save/load/regen/undo + forged-import refusal; positive-reachability on a REAL generated corpus; both material self-limits (ash pays no tribute; razing twice yields nothing). Return {lane:'W8-D', commits, gates, stopReports, notes}.`,
  {label:'w8d:razing-license', phase:'Razing45', model:'opus', effort:'high', schema:S})
const bv = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(b)}. Walk the initiation table yourself (non-evil cannot reach PUNISH, out-of-band tokens included); execute the conservation arithmetic to the person on a real sack fixture; BOTH demotion arms (a heavy sack demotes because population truth crossed the threshold; a light sack demotes NOTHING); the edge-type flip fires and the healing lattice can later re-type it (probe); the license lifecycle END TO END (save/load/regen/undo carry it byte-faithfully; a forged license refuses at import); positive-reachability on a real generated corpus; both self-limit pins; the composite extremity BOTH negatives; dormancy re-hashed byte-identical; python byte-scan every new file for NULs; strict/build/walkers; pathspec. Return {lane:'W8-D verify', verdict, findings}.`,
  {label:'w8d:verify', phase:'Razing45', model:'opus', effort:'high', schema:V})

return { repairs: {r:a, v:av}, razing: {r:b, v:bv} }