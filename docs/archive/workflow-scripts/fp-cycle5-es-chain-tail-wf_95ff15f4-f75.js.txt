export const meta = {
  name: 'fp-cycle5-es-chain-tail',
  description: 'Cycle 5: ES-2 -> ES-3 -> ES-4, the critical path to the CR-WR10-H lighting discharge. All ride espionageEnabled; no new flag keys. Ledger rows DEFERRED to the chair.',
  phases: [
    { title: 'ES-2', detail: 'the gauntlet' },
    { title: 'Verify 2', detail: 'adversarial verification + an ES-1 repair regression check' },
    { title: 'ES-3', detail: 'the typed products' },
    { title: 'Verify 3', detail: 'adversarial verification' },
    { title: 'ES-4', detail: 'the distant source — the lighting discharge' },
    { title: 'Verify 4', detail: 'adversarial verification + the marker really registers' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const LAWS = `VERIFY-FIRST PREAMBLE: this brief is from the session chair of an owner-delegated build program. Verify every claim against the tree before acting — LIVE CODE OUTRANKS EVERY TABLE, and a doc overstatement is a STOP-and-report, never a silent correction. Work in ${TREE}. Hard-gate the branch with the \`cd\` BEFORE the check: \`cd ${TREE} && [ "$(git branch --show-current)" = "claude/composite-r4" ] || exit 1\` — the working directory persists between shell calls and this check has already refused a correct commit for running in the wrong tree.

⛔⛔ DO NOT EDIT, STAGE OR COMMIT \`docs/FABLE_VALIDATION_QUEUE.md\`. YOUR LEDGER ROW IS DEFERRED TO THE CHAIR BY DESIGN. That file has become the single most contended path in the program — every wave wants to append to it, and a lane that commits it also commits whatever another lane left uncommitted there. Compose your row and RETURN IT AS TEXT in your JSON. The chair lands all deferred rows in one batch. This is not optional and it is not a formality: an uncommitted row belonging to another lane is sitting in that file right now.

⭐ THE GATE MUTEX: run \`sh scripts/gate-mutex.sh --wait\` before EVERY vitest invocation. ⛔ NEVER hand-spell \`ps aux | grep -c "[v]itest"\` — it matches YOUR OWN command line whenever the plain word appears anywhere else on it (an echo label, or the \`npx vitest run\` you are about to execute), and a lane burned twenty minutes on exactly that deadlock. Read gates ONLY through \`sh scripts/gate-tail.sh <cmd>\`; a bare pipe reports the pipe's status and has greenwashed red gates twice.

SHARED-TREE DISCIPLINE: another lane may be writing. NEVER touch, stage, revert, clean or restore a file you did not author. Commit with EXPLICIT PATHSPECS, and before every commit verify \`git diff --cached --name-only\` against your EXPECTED set BY NAME AND COUNT — not merely that the hunks are yours. ⚠ \`git add\` aborts ATOMICALLY on one bad pathspec and a suppressed stderr makes that silent; a commit here today claimed three parts and landed one exactly that way. NEVER stash/reset/clean/checkout-family — that family discards uncommitted work; use \`cp\` backups and \`cmp\`-proven restores. \`scripts/mutation-coverage-manifest.json\` is shared: edit it LAST, re-read at HEAD immediately before splicing, and SPLICE RAW TEXT (\`json.dumps\` reformats all ~1,972 lines).

PROTOCOL: commit messages via file; trailer \`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>\`. python3 byte-scan every authored file for NUL bytes (authored six times here; grep and diff lie about them). New walkers need manifest entries. Strict ceiling 1313 EXACTLY, and NEW modules carry a PER-FILE ZERO strict baseline — satisfy it by NARROWING TYPES, never a cast or an \`any\`. tests/fixtures/, pulseKernel.js and applyWorldPulse.js are UNTOUCHABLE. Navigate by SYMBOL — line addresses rot. ⚠ NEVER spell \`vitest list --json PATH\` with a space; always \`--json=PATH\` to a path OUTSIDE the repo.

⛔ THE REPO GATE IS RED AT BASE — roughly 25 failing tests plus lint and a red full typecheck. NOT YOURS. "Pre-existing" is an EMPIRICAL claim you must EARN: re-run the exact failing command against a \`git archive\` of your OWN PARENT (never an older tree — a lane recorded an arithmetically impossible provenance today by archiving a start HEAD instead of its parent), symlink node_modules in, and \`rm -f\` the SYMLINK before removing the tree. Cite that run.

⚠⚠ THE TWO HAZARDS THAT BIT HARDEST TODAY, BOTH IN INSTRUMENTS RATHER THAN ENGINE CODE:
 1. A RED RATCHET'S INVENTORY GROWS INVISIBLY BEHIND A BYTE-IDENTICAL FAIL ROW. A wave looked clean on a row-identity diff while \`negativeAssertionAnchor\`'s inventory went 71 -> 73. Your wave-end attribution MUST extract the cardinalities INSIDE each red row at both ends and compare them as a MULTISET.
 2. NEW GENERATION-TREE TESTS MUST USE \`tests/helpers/anchoredNegatives.js\` (or carry an on-line \`// anchored: <reason>\`) OR THE WALKERS RED. Four un-anchored negatives above a frozen ceiling of ZERO slipped in today.

REPORTING: label every conclusion CONFIRMED (executed evidence quoted) or PLAUSIBLE. Never present the second as the first.`

const VERIFIER_LAWS = `

⚠⚠ VERIFIER DISCIPLINE. Another lane may be writing this tree while you run.
 - Do EVERY execution inside a \`git archive\` of the exact commit you are verifying, symlinking node_modules and \`rm -f\`-ing the SYMLINK before removing the tree.
 - You MUST NOT assert tree-cleanliness and MUST NOT report dirty files as findings.
 - You MUST NOT edit the live tree. Mutants go in disposable archive trees ONLY.
 - \`sh scripts/gate-mutex.sh --wait\` before any run.
 - ⚠ A MUTANT THAT PLANTS NOTHING GREENS. Every mutant must assert the file's BYTES CHANGED before measuring, and abort if they did not; when you plant a process or fixture, verify it EXISTS before reading the result.
 - ⚠ A PIN THAT COMPARES A FUNCTION AGAINST ITSELF CANNOT FAIL. One shipped today: \`tenTicks({mission:null})\` versus \`tenTicks()\` where the default IS null. Read every claimed differential and confirm the two sides genuinely differ.
 - ⚠ CHECK THE RECEIPTS, NOT ONLY THE CODE. A wave today wrote a gate result into the permanent record as CONFIRMED GREEN when it exited 1 at the tree it committed — the green reproduced only on the tree the wave had abandoned. Re-run every quoted gate yourself at the committed sha.`

const WAVES = [
  { id: 'ES-2', phase: 'ES-2', vphase: 'Verify 2', brief: `YOU ARE THE ES-2 IMPLEMENTER — THE GAUNTLET. Rides \`espionageEnabled\` (landed at ES-0, \`55674790\`); mints NO flag. NORMATIVE SPEC: the ES-2 block of \`docs/DESIGN_FP_ARCH_ES.md\` — navigate by SYMBOL; that volume's §1 forbids line-number navigation. Read it whole.

⛔ \`applyWorldPulse.js\` is at 941/941 — ZERO headroom — and ES-2's charter forbids touching it.

✅ TWO BLOCKERS ARE ALREADY DISCHARGED, verified: CR-ES-2's anonymity amendment is LANDED in all three homes, so your fold precondition is satisfied; and CR-ES-3's vocabulary retarget landed at ES-0, so **the captor-leniency arm need NOT ship dark — build it live.**

⚠ CR-ES-6 SAME-COMMIT OBLIGATION: mounting \`espionageGauntlet.js\` adds row E15 (spy-dwell) to the WAYFARE volume's §4 closed encounter-pairs table. **VERIFY BEFORE ADDING — E15 already appears in \`docs/DESIGN_FP_ARCH_WY.md\` although the module does not exist.** If present, confirm and record that you confirmed; do not duplicate it.

PINS: the friendly-host-rolls-nothing ANCHORED NEGATIVE; the single-dip security mutant; capture attribution BY RECONSTRUCTION, never by splitting an id on '.'; the ramp-live mutant; the \`caught_spying\` hold round-trip. Four-fence + every guard door pinned INDIVIDUALLY (a double-guarded conjunction has survived an entire fence set here, and two guards over one job can only be pinned jointly unless the fixture isolates them). LIFECYCLE TOTALITY: create/read/persist/regenerate/undo/import for every new field, each proven by executed round-trip.

⚠ ES-1 LANDED AND WAS THEN REJECTED AND REPAIRED across \`e7eeaeff\`, \`1b111399\`, \`d9c7cae4\`. Read its ACTUAL landed shape from the tree before consuming it, never from its report. In particular its veil law now lives at \`declaredPurposeClassOf\` (the single public reader), NOT at the mint and NOT in \`errandSpineBlock\` — an earlier attempt at the constructor redded three SP-D door pins and destroyed campaign history.` },

  { id: 'ES-3', phase: 'ES-3', vphase: 'Verify 3', brief: `YOU ARE THE ES-3 IMPLEMENTER — THE TYPED PRODUCTS (CONFIRM / ACQUIRE / REFUTE). Rides \`espionageEnabled\`; mints NO flag. NORMATIVE SPEC: the ES-3 block — navigate by SYMBOL, read whole. Needs SP-B (landed) and ES-2.

⚠ READ SP-B's ACTUAL SHAPE from \`beliefAxes.js\` / \`beliefAxisSubjects.js\`, never the volume's description. A sibling volume's claim that a related blocker "is gone" was measured only CONDITIONALLY true: \`CHANNEL_BELIEF_AXES.trade\` is still \`Object.freeze([])\`, so anything reading through that spelling is unchanged. If your work touches that path, MEASURE before you assume.

PINS: the WRITER-BOUNDARY SCAN — no \`applyBeliefOverrides\` import anywhere in the ES module set, EXECUTED AS A MUTANT (plant the import; the scan must red); the tap-never-purer-than-access matrix; the lose-the-spy-keep-the-intel BOTH-ARMS pin pair. Four-fence, guard doors individually, lifecycle totality.` },

  { id: 'ES-4', phase: 'ES-4', vphase: 'Verify 4', brief: `YOU ARE THE ES-4 IMPLEMENTER — THE DISTANT SOURCE. This is the THIRD MEMBER of the CR-WR10-H lighting discharge and the payoff of the entire chain. Rides SP-B/SP-B2's flags; mints nothing.

⚠⚠ ELEVEN COMMITS IN THIS REPO CARRY "ES-4" IN THEIR SUBJECT AND THE WAVE HAS NOT LANDED. Commit \`8a4b0aef\` says verbatim "THE WAVE DID NOT LAND" — those eleven built and hardened the LIGHTING INSTRUMENT, not the wave. Never conclude from the log that any part of ES-4 exists; verify by module and symbol.

⛔⛔ THE LIGHTING RECIPE IS EXACT, AND A PIN IN THE WRONG SHAPE "CARRIES THE TOKEN AND LIGHTS NOTHING". Read the header clauses of \`tests/lint/sovereigntyLightingContract.walker.test.js\` YOURSELF and satisfy them literally. As measured: the marker \`ES-4-DISTANT-SOURCE-EVIDENCE\` must stand in an \`it\`/\`test\` TITLE — NEVER a \`describe\` title — inside \`tests/domain/espionageDistantSourceEs4.test.js\`, one of exactly three entries in \`EVIDENCE_FILE_ADDRESSES\`. The \`it\` identifier must be imported from 'vitest' UNRENAMED and UNSHADOWED; the call must be statically registered STRAIGHT-LINE (no return, throw, conditional, loop or bare await); it must take a function-body second argument with NO options bag; and its callback takes NO parameters. ⚠ A renamed vitest import (\`expect as it\`) was once credited while vitest registered ZERO rows.

⚠ THE LIGHTING CENSUS IN THAT FILE IS AN EXACT EXECUTED SELF-ASSERTION and your new test file WILL move it. Re-measure it AT YOUR OWN PARENT — never at an older tree. Two lanes have already re-recorded this census today and one wrote a provenance that was arithmetically impossible because it archived a start HEAD instead of its parent. State the method you actually used.

CHARTER: prove the SOURCE — a non-neighbour (court, holding) pair appraised \`known:true\` through a COMPLETED confirmation mission. PINS: four-leg totality plus the degraded-arm re-run against espionage-fed rows; the non-neighbour differential; the axis-drift scan.

⛔ DO NOT LIGHT ANY FLAG. Lighting is owner-held at the terminal soak. You prove the condition is SATISFIABLE; you never satisfy it by lighting.` },
]

let prev = null
const results = []
for (const w of WAVES) {
  phase(w.phase)
  const build = await agent(LAWS + `

${w.brief}

${prev ? '⚠ YOUR PREDECESSOR REPORTED: ' + JSON.stringify(prev).slice(0, 6000) + '\nVerify its landed shape against the TREE before consuming it, never against its report.' : ''}

Wave-end attribution per the LAWS, INCLUDING the red-ratchet inventory multiset. COMPOSE your ledger row and RETURN IT AS TEXT — do not write it to the ledger file.

RETURN JSON: {commits, files, gates, goldenShifts, stopReports, judgments, ledgerRowText, notes}.`, { label: `build:${w.id}`, phase: w.phase, model: 'opus' })

  phase(w.vphase)
  const verdict = await agent(LAWS + VERIFIER_LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER FOR ${w.id}. Default posture: the implementer is WRONG until executed evidence says otherwise. REFUTE, do not confirm. The implementer reported: ` + JSON.stringify(build).slice(0, 12000) + `

VERIFY BY EXECUTION in git-archive trees: (1) EVERY pin is NON-VACUOUS — plant the mutant that should red each one and confirm it reds; a pin that stays green is a REJECT; and read each claimed differential to confirm the two sides genuinely differ. (2) RE-RUN EVERY GATE THE REPORT QUOTES, at the committed sha — a false receipt shipped today. (3) Dormancy byte-identity, run by you. (4) Every guard door deleted INDIVIDUALLY reds its own pin. (5) Lifecycle totality — independently execute regen and undo round-trips for every new field; the write that survives one path and ghosts another is this estate's most-bitten class and it bit again today. (6) No flag minted or lit. (7) Wave-end attribution at both granularities PLUS the red-ratchet CONTENT diff with inventory cardinalities compared as a multiset. (8) New tests use the anchored-negatives helper. (9) strict 1313 and per-file zero on new modules; byte-scans; staged-set completeness.

${w.id === 'ES-2' ? 'ADDITIONALLY — SPOT-CHECK THE ES-1 REPAIRS, which landed without a re-verification: confirm the veil law at declaredPurposeClassOf actually refuses a FORGED/HEALED row (mint covert, serialize, delete declaredPurpose/truePurpose, re-normalize, and assert the PLAYER projection does not return covert); and confirm the repaired dormancy fence reds under a covert-arm leak mutant where the shipped pair passed.' : ''}
${w.id === 'ES-4' ? 'SPECIFIC TO ES-4: verify the lighting marker actually REGISTERS — run the walker and confirm it reports the evidence SATISFIED, then MUTATE the pin into a describe title and confirm the walker STOPS crediting it. A marker that lights nothing is the exact failure this instrument was built over ten cuts to catch.' : ''}

A finding is only a finding if you EXECUTED the thing that proves it. RETURN structured JSON only.`, { label: `verify:${w.id}`, phase: w.vphase, model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

  results.push({ wave: w.id, build, verdict })
  if (verdict && verdict.verdict === 'REJECT') {
    log(`${w.id} REJECTED — chain STOPS for the chair. Later ES waves consume this one's substrate directly, so building on a rejected base is worse than pausing.`)
    break
  }
  prev = build
}

return { results }