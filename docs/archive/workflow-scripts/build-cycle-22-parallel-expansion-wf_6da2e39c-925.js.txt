export const meta = {
  name: 'build-cycle-22-parallel-expansion',
  description: 'Non-war strict/any burn-down · verify the four orphaned pause commits · first-contact prose seams',
  phases: [{ title: 'Build', detail: '3 disjoint lanes, implement + verify each' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); R-BLD-9 shrink-only ratchet law; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY FENCES: WR-8 is LIVE on src/domain/worldPulse/* (war/envoy/relationship/demographics families) — absolutely fenced; the icon sweep may still touch components — avoid component files except those your lane names; queue docs carve-or-commit-body.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const lanes = await parallel([
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE SB — THE STRICT/ANY BURN-DOWN, NON-WAR TRANCHE (task #61's safe half; the war files wait for WR-8 to close). TARGETS, the measured worst offenders OUTSIDE worldPulse: settlement.schema (122 any-holes), factionCompetition (96), npcAgency (87), institutionLifecycle (78), stressorGates (78) — take them largest-first plus as many more non-worldPulse offenders as budget allows (read both baselines for the roster; NEVER a worldPulse file). METHOD: real types, not cast-shuffling — type the actual shapes (JSDoc typedefs per the estate's idiom), burn each file to zero or to an honestly-lower number; the shrink-only ratchets BANK every win (hand-edit each file's baseline entry down; never --update on the live tree); behavior-identical (focused suites green before/after per file; these are typing changes ONLY — any runtime edit is out of scope, STOP-report it). One commit per file. Return {lane:'SB', commits, gates, stopReports, notes}.`,
    {label:'sb:strict-burndown', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Per file: prove typing-only (the diff must contain zero runtime-semantics changes — read every hunk; a behavior edit hiding in a typing commit is the finding); re-run the strict/any counters yourself (banked numbers match measured); focused suites green on committed bytes; no worldPulse file touched; typedefs honest (spot-check 5 against actual call sites — a wrong type that compiles is worse than any). Return {lane:'SB verify', verdict, findings}.`,
      {label:'sb:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: adversarial verifier (verification-only lane — you change NOTHING except, if defects are found, one repair commit per defect). ${P10}
LANE OV — THE ORPHANED FOUR: cycle 13 was killed mid-flight by an owner pause AFTER its lane-M commits landed but BEFORE its verifier ran. These four commits have never been adversarially verified: 86a3e0d3 (E-A manifest: 12 invariant files registered — task #62), d73f2292 (the 13→15 taxonomy: regex updated, golden claimed UNMOVED), f8d330ef (wizardNewsAuthoring walker re-pin with attribution), 76020df7 (ARCHITECTURE.md migration count — task #65). Verify each at full strength: the E-A rationale rows spot-read for honesty + the totality test green; the golden claim re-proven (hash the fixture at the commit's parent and at HEAD — if it moved, that is a MAJOR finding); the walker re-pin's attribution git-log-proven and the pinned rows matching measured truth; docCounts green and the count true against supabase/migrations. Repair anything false in its own commit. Return {lane:'OV', commits, gates, stopReports, notes} with a verdict per commit in notes.`,
    {label:'ov:orphan-verify', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: second-opinion verifier. ${P10}
Report: ${JSON.stringify(r)}. Spot-re-run the two highest-risk checks yourself (the golden hash comparison and one E-A rationale row); confirm any repair commits are pathspec-clean and their pins bite; verdict per commit. Return {lane:'OV verify', verdict, findings}.`,
      {label:'ov:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE PS — THE FIRST-CONTACT PROSE SEAMS (found on the chair's live site walk; the two exact defects, on-screen for every first-run user):
(1) the generation spine renders "It is currently strained by — Strained by under Siege, infiltrated." — a template splice doubling the frame word and mangling case; the spine's strained-by sentence must compose grammatically from the stressor names (find the composer, fix the mold, cover every stressor-count arm: 0, 1, 2, 3+).
(2) "Its people fear — People fear a return of the settlement is under active siege. Every resource decision... survival.." — a fear-template splicing a full description sentence into a noun slot plus a doubled period; the fear line must take the stressor's FEAR form (or a noun phrase derived for it), never its description body; fix the trailing-period stack.
METHOD: locate the composer(s), fix the grammar at the mold (not per-stressor hand edits), add composition pins over every arm (0/1/2/many stressors; each named stressor family), and sweep the SAME splice pattern across the spine's other five rungs (exists-because / survives-by / ruled-by / future) — the same class likely lives there; census what you find, fix what is broken, one commit per rung family. jsdom render pins prove the fixed sentences; quote before/after strings in the commit body. Fenced from worldPulse; the spine composer likely lives in generators/display — verify ownership before editing (check-git-first). Return {lane:'PS', commits, gates, stopReports, notes}.`,
    {label:'ps:prose-seams', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Generate REAL settlements across 20 seeds and READ every spine sentence (all six rungs) — quote at least 8 verbatim; zero splice artifacts (doubled frame words, mid-sentence case breaks, doubled periods, description-bodies-in-noun-slots) may survive; the pins red under the old splice (mutant: restore one old mold → red); same-seed discipline — the fix changes PROSE COMPOSITION only, prove the underlying state selection unmoved (same stressors chosen pre/post on same seeds); pathspec-clean. Return {lane:'PS verify', verdict, findings}.`,
      {label:'ps:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
])
return { lanes: lanes.filter(Boolean) }