export const meta = {
  name: 'build-cycle-30-wr8-finish',
  description: 'The WR-8 finish: warDeployment decomposition → the razing + license WIRED under CR-WR8-G · the W8-D tails',
  phases: [{ title: 'Split', detail: 'warDeployment writer-family decomposition' }, { title: 'Wire', detail: 'the razing mouth + license + downstream emissions' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; ⚠️⚠️ THE STAGED-SET LAW (new, from W8-D's self-reported failure): IMMEDIATELY before EVERY git commit, run git diff --cached --name-only and verify EVERY staged file is yours — a concurrent lane's staged files WILL ride a bare commit; prefer git commit -- <your paths> so the commit itself is pathspec-bound; no stash; focused vitest + gate-tail (ps aux | grep vitest first); R-BLD-9 strict law (whole-domain delta incl. type consumers); python byte-scan every Write; npm run build + smoke:boot green; dormancy goldens BYTE-IDENTICAL with flags dark (hash pre/post, quote); STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY: the nav lane may be live on nav/theme; the mirror-death lane on simulationSpine/historyBeats — fenced.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}

phase('Split')
const s = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE WD — warDeployment.js DECOMPOSITION (1412 effective, frozen at exactly 1412, tolerance-0 — the razing's mouth at the siege-verdict site ~:1660-1740 has zero headroom). THE STANDARD (the estate's split recipe): writer-family (R-BLD-4: the head keeps its writer/entry role, pure leaves), behavior-identity by STRIPPED-CONCAT or AST-canonical diff, strict-NEUTRAL per-file map (typedef aliases per the type-surface law), baseline entry ratcheted down/DELETED lawfully, every new leaf walker/K3-registered, same-seed pipeline hash parent-vs-commit, one commit per extraction. Target: the head under ceiling with real headroom at the siege-verdict site for the wiring. Also close the W8-D tails while here: F1 (the one-line license coherence check — a license is a capability, the gap defeats executed-ONCE), F2 (the over-claim in the test title + LAW 7 prose), F3/F4 hygiene. Return {lane:'WD', commits, gates, stopReports, notes}.`,
  {label:'wd:deployment-split', phase:'Split', model:'opus', effort:'high', schema:S})
const sv = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(s)}. Per extraction: identity diff re-run with YOUR OWN method; sizes with the ratchet's engine; same-seed hashes; strict per-file map neutral; runtime node ESM link of the graph; walkers green with leaves registered; the F1 coherence check mutant (re-open the gap → red); the staged-set law honored (git show --name-only every commit — ONLY lane files); pathspec vs nav + mirror lanes. Return {lane:'WD verify', verdict, findings}.`,
  {label:'wd:verify', phase:'Split', model:'opus', effort:'high', schema:V})

phase('Wire')
const w = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
Prior: ${JSON.stringify({split:s?.commits, verdict:sv?.verdict})}. LANE WZ — THE RAZING WIRED (CR-WR8-D's bar; the last act of WR-8):
CHAIR RULING CR-WR8-G (vetoable, cite it): THE OCCUPATION/RAZING WRITERS MAY READ THE BELIEF STAGE'S OUTPUTS — read-only consultation (an intent read + a feasibility read per hold) is lawful: K3 fences belief/negotiation paths from reading TRUTH, not writers from reading beliefs; the mercy receipt ('they could have taken everything and did not') and the mistaken-feasibility receipt are the amendment's own required outputs and need exactly this read. Pin the DIRECTION (the writer imports belief READERS only; a belief module importing the writer reds the seam).
BUILD, at the opened siege-verdict mouth in the warDeployment family: (1) the razing branch — the PUNISH intent consulted (razing.js's initiation gate), the conserved sack emitted through §3 demographics (the sack cause class, escapee refugees, named cast roaming — never engine-killed), institutions to damage-impairment/shell, the edge-type flips applied, the victor departs (no occupation record — the departure receipt); (2) the license mint + consume through vengeanceLicense.js (one per coalition, the closed loop, CR-WR8-B coupling at prosecution); (3) the observer-axis relationship hits + WR-6's believed-retaliation deterrence read (consume, never rebuild); (4) the mercy + mistaken-feasibility receipts under CR-WR8-G. R-BLD-1 stage-neuter negative controls proving reachability of EVERY branch; conquestDoctrineEnabled stays DARK — dormancy byte-identical (hash, compare, quote); WHAT_PHRASES/heraldRouting totality stays green (the casus already registered). One commit per coherent step + queue rows; WR-8 CLOSES with this lane — say so in the final row. Return {lane:'WZ', commits, gates, stopReports, notes}.`,
  {label:'wz:razing-wired', phase:'Wire', model:'opus', effort:'high', schema:S})
const wv = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(w)}. Re-run stage-neuter on every branch (neuter → its pin reds); dormancy re-hashed vs the pre-wiring baseline; execute a FULL razing arc on a lit fixture (evil victor + composite extreme + won siege → sack conserved to the person, tier told nothing, edges flipped, license minted; then the license consumed ONCE and refused twice); the mercy arc (could-have-razed-and-didn't → the receipt); CR-WR8-G's direction pin (mutant: a belief module importing the writer → red); hunt stream theft/stride aliasing in the new plumbing; the deterrence read consumes WR-6 (grep: no rebuilt web); whole-domain strict; build + smoke:boot; staged-set law on every commit; pathspec. Return {lane:'WZ verify', verdict, findings}.`,
  {label:'wz:verify', phase:'Wire', model:'opus', effort:'high', schema:V})

return { split: {r:s, v:sv}, wire: {r:w, v:wv} }