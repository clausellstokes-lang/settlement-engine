export const meta = {
  name: 'build-cycle-16-resume',
  description: 'Resume after owner pause: finish the fletched ribbon from WIP · CF F4/F5 · pulseKernel extraction → the wiring stage',
  phases: [{ title: 'Resume', detail: 'fletching ∥ CF tail ∥ kernel extraction' }, { title: 'Wiring', detail: 'the WR-7 stage in envoyPulse under CR-WIRE-A/B/C' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); R-BLD-9 strict law (new files zero, touched never worsen, whole-domain delta incl. import('...').Type consumers); node ESM link + npm run build stay green on domain-import changes; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". DARK, nothing pushed. Queue docs: re-read, carve hunks.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}

phase('Resume')
const [fl, cf, k] = await parallel([
  () => agent(`VERIFY-FIRST: owner-directed implementation, RESUMED FROM KILLED-MID-EDIT WIP. ${P10}
LANE FL-2 — FINISH THE FLETCHED RIBBON. A prior lane was stopped mid-edit; its UNCOMMITTED partial work sits in the tree RIGHT NOW: src/components/nav/{NavRibbon,NavDivider,NavFlowArrow}.jsx, src/components/theme.js, src/lib/routes.js, tests/store/appNavSsot.test.js, and a docs/FABLE_VALIDATION_QUEUE.md hunk. SURVEY FIRST (the 529-kill rule): read every dirty diff, decide per hunk whether it serves the spec below, finish what is sound, rewrite what is not — do NOT start from scratch and do NOT revert wholesale. THE SPEC (owner directive 2026-08-03 + chair refinements): (1) Welcome retired from the DESKTOP ribbon; the wordmark carries home (aria-label 'Home'); route + mobile untouched; landing page = no active tab. (2) Create/Library/Realm as ONE fletched band: single leather-brown theme token (FLETCH_BROWN, one writer in theme.js, AA ≥4.5:1 for label text, record the ratio), three feather segments with FORWARD-slanted parallel edges (toward Realm), sharp straight angled separator strokes (curved chevrons retired on desktop); reference tabs stay plain. (3) THE OVERHANG IS PAINT, NEVER LAYOUT: ~4px below the ribbon's bottom edge via overflow painting only; the header's layout box stays EXACTLY CHROME.headerDesktop (60) — ANCHOR_OFFSET=84 is load-bearing estate-wide; pin the layout height. (4) Active feather brightens with the existing gold grammar; keyboard focus ring survives the clip shape (test it). (5) Mobile bottom nav untouched. Update navDividers/navFlowArrows/appNavSsot/a11y pins to the new spec, docstrings citing the owner directive. Gates: component suites + eslint + sizeBaseline (App.jsx shrinks — bank it) + rawButtonBaseline census + npm run build. Pathspec commits. Return {lane:'FL-2', commits, gates, stopReports, notes}.`,
    {label:'fl2:finish-fletching', phase:'Resume', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify on committed bytes: header layout box still exactly 60 with the overhang as paint (mutant: make it real height → the pin reds); Welcome absent from desktop NAV, wordmark navigates home with the aria-label, mobile files byte-untouched (git show --stat); the feathers' slant values asserted; focus ring visible on a clipped feather; FLETCH_BROWN single-writer + AA ratio recomputed by YOU; every updated pin docstring cites the directive; sizeBaseline banked not raised; rawButton census lawful; npm run build green; the pre-pause WIP fully accounted (no dirty nav file remains, no hunk silently dropped — diff the pre-pause WIP against the commit and explain any discard). Return {lane:'FL-2 verify', verdict, findings}.`,
      {label:'fl2:verify', phase:'Resume', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE CF-2 — THE CLAUSE-FLOOR TAIL (F1/F2/F3 landed pre-pause @ 7385dba3/824c22fc/83b18609 — verify their presence, then finish):
(1) CR-HR-F4 (chair ruling, vetoable, record in the spine): causeWalk.resolveReceipt hands over dramaClass, so exposed/refused/breached/dissolved are UNREACHABLE from shipped consumers. RULED: a resolved hop additionally carries recordedType — the receipt's OWN edge/reason type read from the ledger entry (DERIVED read-model field, no persisted shape, no writer); poolForLink prefers recordedType over type; unknown recordedType falls to followed (closed vocabulary). Pin on a REAL buildCauseWalk fixture: a ledger-typed parent edge (e.g. plant_exposed) composes the exposure voice in back direction — the voice's first live-path pin; fwd restriction still holds on the same fixture. Keep the synthetic pins as supplements.
(2) F5 anchor debt: tests/domain/stateProseKernel.test.js's 5 un-anchored negatives (ceiling 0) — anchor all 5 with TRUE structural reasons; the walker stops naming the file.
One pathspec commit per item. Return {lane:'CF-2', commits, gates, stopReports, notes}.`,
    {label:'cf2:recordedtype', phase:'Resume', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Build your OWN real-walk fixture with a differently-typed ledger edge — the voice must compose from the live path; unknown recordedType must fall to followed; grep-prove recordedType is derived-only (no writer, no persisted shape) and the K3/contamination fences did not widen; re-run one pre-pause mutant (MUTANT E vocabulary-drift) to prove F1-F3 still bite; the anchor walker no longer names stateProseKernel.test.js and all 5 reasons are TRUE; strict/ratchets/pathspec clean. Return {lane:'CF-2 verify', verdict, findings}.`,
      {label:'cf2:verify', phase:'Resume', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE K-2 — pulseKernel.js STAGE-BODY EXTRACTION (file 3 of 4, the last over-ceiling war file; the measured plan is in the queue rows around 22e2ac0f and the cycle-10 STOP — read both). THE METHOD: simulateCampaignWorldPulse is ONE 1,425-line function; whole-declaration relocation is unavailable. Extract stage BODIES into pure functions taking an explicit environment object; the nine @pulse-stage: markers STAY in the head at the call sites (pulseStageTopology pins them, exactly once, topological order); largest first (consequence_fold 718, mover_planes 346, settlement_clock 162); THE PRNG CALL ORDER IS THE STREAM IDENTITY — after EVERY extraction, same-seed pipeline hash comparing THIS commit against ITS OWN PARENT (concurrent dark lanes may land elsewhere; parent-vs-own-commit isolates your change), STOP at the first mismatch; one commit per extraction so a mismatch bisects to one move. Ratchet the baseline down/delete lawfully; typedef aliases per the type-surface law; new leaves walker/K3-registered, strict-neutral by per-file map. Return {lane:'K-2', commits, gates, stopReports, notes}.`,
    {label:'k2:kernel-extraction', phase:'Resume', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Per extraction commit: same-seed pipeline hashes re-run by YOU (parent vs commit, golden-corpus rows); PRNG draw-count identity (wrap the RNG — the seedrandom-wrap method); pulseStageTopology green; whole-domain strict delta neutral incl. type consumers; sizes with the ratchet's own engine; module-init side-effect scan; runtime node ESM link of the graph; pathspec hygiene vs the fletching + CF lanes. Return {lane:'K-2 verify', verdict, findings}.`,
      {label:'k2:verify', phase:'Resume', model:'opus', effort:'high', schema:V})),
])

phase('Wiring')
const w = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
Prior: ${JSON.stringify({fletching:fl?.commits, cf:cf?.commits, kernel:k?.commits})}. LANE W — THE WR-7c/7d WIRING STAGE in src/domain/worldPulse/envoyPulse.js (248/800, 552 free — the mouths are NOT touched; cycle-10's premise correction, twice verified). THE THREE CHAIR RULINGS BIND (CR-WIRE-A/B/C, vetoable, cite in the queue row): (A) powerBand PICTURE-DERIVED per member from its OWN frozen picture — K3 pin stays CLOSED; pin: the derivation reads only picture fields. (B) coalition-less episodes ratify through the SOLE-OFFER arm — nothing binds unratified; disclosed flagged-behaviour change; flag dark. (C) capacity counts EPISODES — continuation re-mints carrying an existing episodeKey exempt from origin_capacity; NEW missions at capacity still refuse; pins both directions. BUILD: the ratification stage (enumerate coalition → picture-derived bands → one ballot per member on its own frozen picture → tally → chooseAmongCompetingOffers → gate the carried sheet at the mouth → on close re-mint both mandates through the envoyErrand head) + the ransom stage (hold ledger → mint → both legs priced as a pair through the named-person transit kernel → claim landed). R-BLD-1 stage-neuter negative controls proving reachability of EVERY stage; THE DORMANCY LAW: all war flags DARK — the pre-wiring baseline hashes @ f6828d9d must be BYTE-IDENTICAL post-wiring (re-hash, compare, quote); any movement = STOP, defect. One commit per stage + queue rows. Return {lane:'W', commits, gates, stopReports, notes}.`,
  {label:'w:wiring-stage', phase:'Wiring', model:'opus', effort:'high', schema:S})
const wv = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(w)}. Re-run stage-neuter controls yourself (neuter each stage → its pin reds); re-hash the dormancy baselines vs f6828d9d; hunt stream theft + stride aliasing (Wave-E classes); CR-WIRE-A grep+probe (picture fields only, K3 pin unwidened); CR-WIRE-B single-envoy fixture through the sole arm; CR-WIRE-C both capacity pins; WR-7 family + walkers green on committed bytes; whole-domain strict delta; npm run build; pathspec-clean. Return {lane:'W verify', verdict, findings}.`,
  {label:'w:verify', phase:'Wiring', model:'opus', effort:'high', schema:V})

return { fletching: fl, cf: cf, kernel: k, wiring: {r:w, v:wv} }