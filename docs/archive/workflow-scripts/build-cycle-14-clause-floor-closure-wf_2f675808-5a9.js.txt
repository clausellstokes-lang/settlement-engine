export const meta = {
  name: 'build-cycle-14-clause-floor-closure',
  description: 'Clause-floor totality: root + discourseKernel habitats, sweep de-self-reference, recordedType ruling, anchor debt',
  phases: [{ title: 'Build', detail: 'one closure lane, implement + verify' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); R-BLD-9 strict law (new files zero, touched never worsen, whole-domain delta incl. import('...').Type consumers); node ESM link + npm run build must stay green if you touch domain imports; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". DARK, nothing pushed. CONCURRENCY: cycle 13 is LIVE on pulseKernel/applyWorldPulse, envoyPulse, coalitionRatification, the E-A manifest, wizardNewsAuthoring walker, and peaceCausalDormancyGolden — none of those files are yours. Queue docs: re-read, carve hunks.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE CF — THE CLAUSE-FLOOR CLOSURE (cycle-12 verifier findings F1-F4 + one anchor debt; all chair-ruled):
(1) F1 — THE ROOT JOINS THE FLOOR: heraldCausalVoice.js's telling register floors every link clause but reads walk.root.headline bare (~line 443); in fwd the root is the nearest link's childClause, so a PULSE_OUTCOME_FALLBACK root molds straight into a connective slot (executed: "and after it the turning when World pulse outcome"). Cure beside the existing guard: floor rootClause with isPlaceholderClause the same way and count it toward truncated. Pin on the verifier's real-walk fwd fixture (headline-less root), both directions; mutant un-flooring → red.
(2) F2 — THE SECOND HABITAT: src/domain/display/discourseKernel.js realizeCauseWalk builds its own node list from the same walk (~:364) and composes causal connectives over UNRECEIPTED_HOP (executed: "In turn: an earlier cause.", relation causal, redacted false — consumed by CauseWalkPanel behind discourseProseEnabled). Cure: ONE IMPORT of the existing receiptClauseFloor.js — placeholder nodes compose as bare terminal sentences, never under a connective; the executed counterexample becomes the pin; mutant → red.
(3) F3 — DE-SELF-REFERENCE THE TELLING SWEEP: the sweep decides which links to inspect via PLACEHOLDER_CLAUSES.includes(link.clause) — under a vocabulary-drift mutant it stops looking (list==list, the recorded self-referential-pin hazard). Cure: assert link.clauseless === isPlaceholderClause(link.clause) for EVERY link in the sweep AND compare against a frozen literal copy of the three placeholder strings (drift between the frozen copy and the live export must red with a message saying which drifted).
(4) CR-HR-F4 (chair ruling, vetoable, record in the spine beside the drop clauses): causeWalk.resolveReceipt hands over dramaClass/kind, so exposed/refused/breached/dissolved are UNREACHABLE from every shipped consumer and the vocabulary work guards synthetic fixtures only. RULED: a resolved hop additionally carries recordedType — the receipt's OWN edge/reason type read from the ledger entry (a DERIVED read-model field; no persisted shape changes, no writer); poolForLink prefers recordedType over type when present; the warrant table stays closed-vocabulary (unknown recordedType falls to followed). Pin: a REAL buildCauseWalk fixture whose ledger types the parent edge (e.g. plant_exposed) composes the exposure voice in back direction — the first live-path pin the voice has ever had; and the fwd restriction still holds on the same fixture. Sweep the HR-3/HR-5 synthetic pins: keep them, but the live-path pin carries the law now.
(5) THE ANCHOR DEBT: tests/domain/stateProseKernel.test.js carries 5 un-anchored negative assertions against a frozen ceiling of 0 (the PR lane added 3, 2 pre-existed) — anchor all 5 with TRUE structural reasons (helpers or on-line // anchored:); the walker must stop naming the file.
One pathspec commit per item family. Flags stay DARK. Return {lane:'CF closure', commits, gates, stopReports, notes}.`,
  {label:'cf:clause-floor', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Re-run the cycle-12 verifier's exact executed counterexamples against the committed bytes: the fwd headline-less-root walk (must compose the root as a bare terminal, truncated counted), the discourseKernel 'In turn: an earlier cause' walk (must floor), the vocabulary-drift mutant against the frozen-literal sweep (must red naming the drift), and MUTANT E (constant removed from PLACEHOLDER_CLAUSES — the file must still red). For CR-HR-F4: build your OWN real-walk fixture with a differently-typed ledger edge and prove the voice composes from the live path AND that an unknown recordedType falls to followed; confirm no persisted shape changed (grep writers; the field must be derived-only) and the K3/contamination fences did not widen. The anchor walker must no longer name stateProseKernel.test.js and every anchor reason must be TRUE (read all 5). ESM link + npm run build green. Strict/ratchets/pathspec vs every cycle-13 territory. Return {lane:'CF verify', verdict, findings}.`,
  {label:'cf:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }