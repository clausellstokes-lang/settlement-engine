export const meta = {
  name: 'build-cycle-13-kernel-and-wiring',
  description: 'pulseKernel/applyWorldPulse stage extraction + repairs sweep, then the WR-7 wiring stage under CR-WIRE-A/B/C',
  phases: [{ title: 'SplitsRepairs', detail: 'kernel extraction ∥ repairs sweep' }, { title: 'Wiring', detail: 'the ratification + ransom stage in envoyPulse' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); R-BLD-9 strict law (new files at zero, touched files never worsen, WHOLE-DOMAIN strict delta checked — the TYPE SURFACE IS PART OF THE PUBLIC SURFACE: import('./x.js').SomeType consumers break when typedefs move, re-declare aliases in the head); STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". DARK, nothing pushed. Queue docs: re-read, carve hunks.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}

phase('SplitsRepairs')
const [k, m] = await parallel([
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE K — FILES 3+4 OF THE WAR TRANCHE, BY THE MEASURED PLAN (cycle-10's splits lane wrote it into its STOP — read the FABLE_VALIDATION_QUEUE rows around bb44fccc first). pulseKernel.js (1580): simulateCampaignWorldPulse is ONE 1,425-line function — whole-declaration relocation is unavailable. METHOD: stage-BODY extraction — the nine @pulse-stage: markers STAY in the head at the call sites (tests/domain/pulseStageTopology.test.js pins each exactly once in topological order); extract stage bodies into pure functions receiving an explicit environment object, largest first (consequence_fold 718, mover_planes 346, settlement_clock 162); THE PRNG CALL ORDER IS THE STREAM IDENTITY — after EVERY single extraction run the same-seed pipeline hash (golden-corpus rows) and STOP at the first mismatch; commit per extraction so any mismatch bisects to one move. Then applyWorldPulse.js (1395) the same way if function-shaped, or whole-declaration if it splits like files 1-2. Baseline entries ratcheted down/deleted lawfully; typedef aliases per the type-surface law; every new leaf walker/K3-registered. Return {lane:'K kernel', commits, gates, stopReports, notes}.`,
    {label:'k:kernel-split', phase:'SplitsRepairs', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Per extraction commit: same-seed pipeline hashes YOURSELF (both trees, the golden corpus method); AST-canonical or stripped-block identity where declarations moved whole; PRNG draw-count identity (wrap the RNG if needed — the cycle-7 verifier's seedrandom-wrap method); pulseStageTopology green; whole-domain strict delta neutral incl. import('...') type consumers; sizes re-measured with the ratchet's own engine; module-init side-effect scan; runtime ESM link of the full graph (node import, not vitest — the class that broke the build); pathspec hygiene. Return {lane:'K verify', verdict, findings}.`,
      {label:'k:verify', phase:'SplitsRepairs', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE M — THE REPAIRS SWEEP (each item verified-found, chair-ruled):
(1) R-BLD-8e: unionCoalitionWeight re-enforces shared_picture (two ballots naming one pictureId inside an offer refuse closed — ratifyTermSheet's own spelling; the cycle-10 verifier's forged-tally counterexample becomes the pin) AND offers[].verdict is recounted from the offer's own union arithmetic or the read refuses closed on mismatch (a relabelled 'ratified' row may never publish beside holds:false). Mutants re-opening each → red.
(2) The two receipt imprecisions in the R-BLD-8d queue row (the nesting of unionMargin01 in the quoted artifact; see the cycle-10 wiring verifier's last finding) — correct the cells.
(3) TASK #62 — the E-A manifest TOTALITY red: 12 enumerated invariant test files carry no scripts/mutation-coverage-manifest.json entry (the list is in the R-BLD-9 lane's stop-report row in FABLE_VALIDATION_QUEUE.md). Register each with an honest kind/rationale per the E-A rules (untracked/dirty targets → rationale rows); the totality test green.
(4) wizardNewsAuthoring.walker re-pin: campaignWorldPulseDeferred.js's snapshot drifted 779→852 from landed commits — re-pin the exact rows at measured truth with the causing-commit attribution in the docstring (shrink-only spirit: exact re-pin, cause named).
(5) peaceCausalDormancyGolden 13→15: CHAIR-RULED DISCLOSED RE-RECORD (the silent-re-record ban is honored BY this ruling): the taxonomy legitimately grew (lineage_claim + alliance_obligation landed with WR-3/WR-4); update the /^\\d of 13/ literal to 15, re-record the golden in ITS OWN commit quoting the field-level diff and the cause; the dormancy byte-identity of every OTHER fixture proven unchanged (hash them).
(6) TASK #65 — ARCHITECTURE.md migration count 194→195 (docCounts red).
DO NOT touch pulseKernel/applyWorldPulse (lane K owns them) or envoyPulse. One pathspec commit per item family. Return {lane:'M repairs', commits, gates, stopReports, notes}.`,
    {label:'m:repairs-sweep', phase:'SplitsRepairs', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Re-execute the shared_picture and relabelled-verdict counterexamples pre/post (must refuse closed); run every mutant; E-A totality green with every rationale row spot-read for honesty (a false rationale is worse than a missing row); the walker re-pin's attribution correct (git log the causing commit yourself); the golden re-record's diff exactly matches the stated cause and NO other fixture hash moved (re-hash all 27); docCounts green; pathspec-clean vs lane K. Return {lane:'M verify', verdict, findings}.`,
      {label:'m:verify', phase:'SplitsRepairs', model:'opus', effort:'high', schema:V})),
])

phase('Wiring')
const w = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
Prior: ${JSON.stringify({kernel:k?.commits, repairs:m?.commits})}. LANE W — THE WR-7c/7d WIRING STAGE, in src/domain/worldPulse/envoyPulse.js (248/800 — 552 effective lines free; the mouths are NOT touched: envoyPulse already calls applyWorldPulseOutcomes at :264 and is called by pulseKernel:1980 — cycle-10's premise correction, verified twice). THE THREE CHAIR RULINGS BIND (CR-WIRE-A/B/C, vetoable, cite them in the queue row):
(A) THE POWER BAND IS PICTURE-DERIVED — each member's powerBand comes from that member's OWN frozen negotiation picture, never a truth read; the K3 import pin stays CLOSED (K4: per-party pictures, no merged estimates; coalition legitimacy is a belief BY DESIGN, like N2 feasibility). Pin: the band derivation reads only picture fields.
(B) A COALITION-LESS EPISODE RATIFIES THROUGH THE SOLE-OFFER ARM — the lone court casts the one ballot on its own picture; a sheet binds NOTHING until ratified, single-envoy included (the sole arm distinguishes refusal from stalemate). The flagged behaviour change on single-envoy peace is DISCLOSED in the row; the flag is dark so no lit surface shifts.
(C) CAPACITY COUNTS EPISODES, NOT CONTINUATION LEGS — mintEnvoyErrand's origin_capacity refusal exempts re-mints carrying an EXISTING episodeKey, so the compromise round always opens (law L outranks the mint cap for continuations); NEW missions at capacity still refuse. Pins both directions.
BUILD: the ratification stage (enumerate coalition → picture-derived band per member → one ballot per member on its own frozen picture → tally → chooseAmongCompetingOffers → gate the carried sheet at the mouth → on close, re-mint both mandates through the envoyErrand head) + the ransom stage (hold ledger read → mint → both legs priced as a pair through the named-person transit kernel → claim landed). R-BLD-1: stage-neuter negative controls proving runtime reachability of EVERY new stage; THE DORMANCY LAW: all war flags DARK, so the pre-wiring baseline hashes recorded @ f6828d9d must be BYTE-IDENTICAL post-wiring (re-hash, compare, quote); any movement = STOP, defect. One commit per stage + queue rows. Return {lane:'W wiring', commits, gates, stopReports, notes}.`,
  {label:'w:wiring-stage', phase:'Wiring', model:'opus', effort:'high', schema:S})
const wv = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(w)}. Verify: stage-neuter controls re-run by YOU (neuter each stage → its reachability pin reds); dormancy hashes vs the f6828d9d baseline re-computed by YOU; hunt stream theft + stride aliasing in the new plumbing (Wave-E classes); CR-WIRE-A — grep + probe that the band derivation reaches ONLY picture fields and the K3 pin did not widen; CR-WIRE-B — a single-envoy fixture ratifies through the sole arm, never lands unconditionally; CR-WIRE-C — both capacity pins (continuation opens at cap, new mission refuses at cap); the whole WR-7 family + walkers green on committed bytes; whole-domain strict delta; pathspec-clean. Return {lane:'W verify', verdict, findings}.`,
  {label:'w:verify', phase:'Wiring', model:'opus', effort:'high', schema:V})

return { kernel: {r:k, }, repairs: {r:m}, wiring: {r:w, v:wv} }