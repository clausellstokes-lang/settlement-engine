export const meta = {
  name: 'build-cycle-25-spine-tail',
  description: 'PS verifier tail: seeded pins, splice-guard pin, mirror repair, conjunction fix, rung-1 variety · the disposition golden re-scope',
  phases: [{ title: 'Build', detail: 'one spine lane + one golden lane, implement + verify each' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); R-BLD-9 strict law; python byte-scan every new/edited file for NULs; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. SHARED-RATCHET LAW (new, from cycle 22's incident): before staging ANY shared baseline/ratchet file, git diff it and confirm every hunk is yours — pathspec staging is NOT protection on shared files. CONCURRENCY: WR-8 completion live on worldPulse conquest/razing files; the nav lane may still hold nav/theme. SURVEY FIRST: src/domain/simulationSpine.js may carry uncommitted WIP from the prior spine lane — read git status/diff before editing; finish or fold honestly, never clobber.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const lanes = await parallel([
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE PT2 — THE SPINE TAIL (cycle-22 PS-verifier findings, all verified):
(1) HIGH — the real-generation pins are UNSEEDED (generateSettlementPipeline gets no seed option, so every test run reads a different random world — the flaky-pin class). Seed them: pass an explicit seed through options on every real-generation pin (a small fixed seed family, not one seed — the recorded single-seed-vacuity hazard), and add the determinism assertion (two same-seed runs render identical spine prose).
(2) MEDIUM — the splice guard is unpinned: deleting BOTH nounPhrase() guards (the MAX_PHRASE_CHARS cap + the mid-sentence-terminator check at simulationSpine.js:194-197) leaves every pin green. Pin each guard separately with a fixture that NEEDS it (an over-long phrase; a phrase carrying '. ' mid-string) — the deletion mutant must red.
(3) MEDIUM — the documented mirror diverged: historyBeats.js:366 says it mirrors the spine and its deriveLikelyFuture still carries the dead-key defect the spine fixed. Repair the mirror to the fixed shape AND add the lockstep pin the docstring implies (the two derivations agree on a shared fixture set — drift → red naming the side).
(4) LOW — joinPhrases doubles conjunctions ('furs and pelts and game meat'): the joiner must notice an ' and ' already inside a name (serial-comma or 'with' fallback — read the estate's prose idiom and match it); pin.
(5) MEDIUM, CONTENT — rung 1 mono-output: 'Why it is here' printed ONE distinct body across 140 generations ('Established along a road route…'). Widen at the MOLD: the origin rung should draw from the settlement's actual founding state (route kind, terrain, resources, the founding condition the spine already knows) with authored variants per arm — check docs/content/ for an existing origins pool before authoring (check-git-first for corpus too); every arm pinned reachable, seeded-family diversity pin (≥N distinct bodies across the fixed seed family).
One commit per item family. Return {lane:'PT2', commits, gates, stopReports, notes}.`,
    {label:'pt2:spine-tail', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Re-run: the unseeded-pin cure (two same-seed runs identical, two different-seed runs differ — execute both); BOTH nounPhrase guard-deletion mutants (each must red its own pin alone); the mirror lockstep (drift mutant → red naming the side); the conjunction fix on the exact 'furs and pelts' fixture; rung-1 diversity measured by YOU across the seed family (count distinct bodies, quote 6); same-seed state selection still unmoved (the PS lane's census method); strict/ratchets; pathspec + the shared-ratchet law. Return {lane:'PT2 verify', verdict, findings}.`,
      {label:'pt2:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE DG — THE DISPOSITION GOLDEN RE-SCOPE (chair ruling, vetoable, cite it): cycle-22's OV verifier CONFIRMED tests/property/dispositionChannelsDormancyGolden.test.js's three red wr2-* rows are a MIS-SCOPED GOLDEN, not a WR-2 dormancy leak — the fixture was captured once @ 7796954e and never re-recorded, and the flag's dormancy is genuinely clean. RULED: re-scope the golden lawfully — read the OV/DG findings in the cycle-22 journal (wf_6da2e39c) for the exact mis-scoping mechanism, correct the golden's SCOPE (what it should have pinned), re-record IN ITS OWN COMMIT with the field-level diff + the cause + this ruling quoted (the HK-3 precedent), and prove the flag's actual dormancy with a fresh dark-run byte-identity check alongside. If on re-derivation the mis-scoping explanation does NOT hold (anything suggests a real leak), STOP-AND-REPORT immediately. Return {lane:'DG', commits, gates, stopReports, notes}.`,
    {label:'dg:golden-rescope', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Re-derive the mis-scoping yourself from the fixture's history (one capture @ 7796954e); confirm the re-recorded golden's diff matches the stated cause EXACTLY (field-level, quote it); run the fresh dark-run byte-identity check yourself (two dark runs hashed identical, one lit run differing — the discriminating control); the ruling + cause quoted in the commit; no other fixture moved (re-hash all dormancy goldens); pathspec-clean. Return {lane:'DG verify', verdict, findings}.`,
      {label:'dg:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
])
return { lanes: lanes.filter(Boolean) }