export const meta = {
  name: 'fp-architecture-compilation',
  description: 'Compile the FP implementation architecture + coding guide from the eight design volumes, WR-era laws applied throughout',
  phases: [
    { title: 'Architect', detail: 'eight per-volume architects + one substrate-census verifier, parallel' },
    { title: 'Synthesize', detail: 'assemble the master volume draft for the chair' },
  ],
}

const OUT = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c44e5d99-2ba5-49d5-a554-40b68534c8eb/scratchpad/fp-arch'

const LAWS = `THE WR-ERA LAWS AND IDIOMS (the coding-guide substance — every wave spec you write must bind these; they are proven, each by execution, most by a defect):
L1 DETERMINISM: zero new PRNG streams in any wiring — every stochastic choice is keyed hash01 (key = '<program>.<facet>.<realmId>.<settlementId>...', codepoint-ordered enumeration), weighted races are w * hash01 products with BOTH sides of every weight proven live (the dead-band law: measured reachability, never assumed — three shapes bit: unreachable ratios, never-written fallbacks, per-digest-calibrated spectra). pulseKernel.js (1580, BANKED) and applyWorldPulse.js (941, zero headroom) receive ZERO edits ever — mount via the lifecycle host (own-flag-before-host-gate), treaty mint folds, or the mover seam.
L2 FLAGS: virtual flags per the law-1 idiom — ABSENT from DEFAULT_SIMULATION_RULES, strict === true reads, dark-never-permissive; every new flag joins ENGINE_GATED_VIRTUAL_RULE_KEYS in the SAME commit as its first real gate read with the walker asserting exactly that one-key delta; the FOUR-FENCE dormancy set per flag (own-footprint golden · absent-vs-false differential · call-path spy · gate-polarity census) with the lit-mutant control proving fences see (differential alone is BLIND by design); beware the conjunction-gate hole — a flag read only through a frozen-list .every() is invisible to the gate walker; always land one by-name read.
L3 EPISTEMICS: belief-side modules never import truth-side state for the counterpart legs (the K3 pattern: zero-import pins on appraisal-class leaves, pinned import rows on composers); writers are truth-side by definition and re-read eligibility at execution, LAPSING with a receipt when the world moved; missing belief legs = honest refusal receipts, never truth backfill.
L4 PERSISTENCE: fight for ZERO new top-level keys — rewrite existing records through existing homes; conditional fields drop-when-absent (T4: an empty array is a key and a key is a byte; absent, never null); every state-writing wave lands its canonical-model entry + Lifecycle-paths clause (create/read/persist/regenerate-under-THE-PROMISE/undo/migrate/veil) BEFORE the writer builds; records with no load-time normalizer pin shape discipline AT the writer; single-writer families enforced by shrink-only source-scan censuses with executed third-writer plants.
L5 RECEIPTS: GAME-GRADE TRANSLATE — no raw floats in prose; banding is LOSSY so each clause names ONE band and states comparisons in WORDS (two numbers straddling a threshold share a band — the contradiction shape); scalars stay on the returned read; runtime no-decimal pins on composed output (source scans cannot see composers).
L6 HERALD: every new kind = the five joins (annex-verbatim pool row · registry row with requiredSlots + slotless fallback · WHAT_PHRASES · EXACT_SECTION/sectionAuthority · full address chain id + settlementIds + typed action + reasons + audience); missing identities return null never throw; dm-only means covert:true fail-closed UPSTREAM; actor-id keys spread only when non-empty; kinds get their OWN walker file (envoy template, frequency-scaled floors), never rows in a foreign walker.
L7 VERIFICATION: tests/lint is red at base — attribute by VIOLATION ROWS against a git-archived base with node_modules symlinked (the EUSAGE hazard), never by walker color or failing-file name (bit three times); every load-bearing conjunction gets an executed mutant (cp backup, cmp/md5-exact restore, NEVER checkout-family) + a mutationCoverageManifest entry; new negatives carry // anchored: on the assertion line or the line above; generation-facing trees are EXACT-ZERO and use tests/helpers/{anchoredNegatives,seedFailures}; every doc-reading pin asserts its target appears EXACTLY ONCE (first-match retargeting); analytic pins that recompute from tokens prove nothing — pin the RENDERED/EXECUTED artifact; a claim proven on one breakpoint/fixture is a claim about one.
L8 SIZE: sizeBaseline is tolerance-zero; new logic = lazy leaves; hot files at ceiling get an extraction FIRST (the decomposition recipe: AST-canonical or stripped-concat behavior identity, whole-domain strict delta neutral, THE TYPE SURFACE IS PUBLIC SURFACE); measure with the enforcer Linter AT the publishing commit, never inherit a figure.
L9 PROCESS: pathspec commits under the staged-set law; byte-scan every authored file (the Write tool mints raw NULs from escape spellings — bit six times); exactly-once discipline on volume edits; per-wave implementer + adversarial verifier with reject gates; STOP-and-report on measured blockers is a SUCCESS mode (three WR STOPs were all real); spine requirements 13 (Alignment line — engagement or declared-empty with reason) and 14 (Edit-verb story) are per-wave totality obligations; seam pre-contracts toward unbuilt programs are PINNED FROM BOTH SIDES with a tripwire (the TR-5 pattern: catalogGrewSinceWr10).`

const VOLUMES = [
  { key: 'SP', doc: 'docs/DESIGN_FP_SPINE.md', hint: 'the believed-world infrastructure (believedWorld/postureOf/bandedStock/temporalWalker/narrationKit); NOTE the convergence: WR-10 lighting is gated on tier/route belief legs that likely land HERE — name the exact waves that discharge that precondition' },
  { key: 'GR', doc: 'docs/DESIGN_FP_GRAMMAR.md', hint: 'the telling grammar; the 89-percent-ungrammatical finding proved substring pins cannot see grammar — mold-conformance verification machinery is the hard problem; budget verifier-heavy waves' },
  { key: 'IN', doc: 'docs/DESIGN_FP_INFORMATION.md', hint: 'information economy on the existing beliefMap/news-speed/brokerage substrate; infoStatecraftEnabled is already an engine-gated manifest key' },
  { key: 'TR', doc: 'docs/DESIGN_FP_TRADE.md', hint: 'the TR-5 degradation contract is ALREADY PINNED both ways with catalogGrewSinceWr10 as tripwire; Seam One includes peace per CR-WR10-B; treaties now carry the orientation reader + victor-free mint road TR agreements can reuse' },
  { key: 'WF', doc: 'docs/DESIGN_FP_FAITH.md', hint: 'faith; the religious casus + faith-desk Herald routing already exist war-side; DEITY DOCTRINE binds (no premade pools)' },
  { key: 'POP', doc: 'docs/DESIGN_FP_POPULATIONS.md', hint: 'zero engagement so far — the riskiest unknown; demographic loops classically go degenerate, so soak-facing pins and conservation invariants deserve extra weight; demographicsEnabled + the plans-lane crossing discipline already exist' },
  { key: 'INT', doc: 'docs/DESIGN_FP_INTERIOR.md', hint: 'integration-heavy: its own tables catalog pre-existing engine substrate (legitimacy stock etc.) to ADOPT not build — attribution and lifecycle care dominate' },
  { key: 'CW', doc: 'docs/DESIGN_FP_COUPLINGS.md', hint: 'CW-0 registry BUILT and growing; CW-1..3 deliberately LAST (manifests-after-honest-data at program scale); expect this to be the wave where the first verdict finds something real — architect the verification plan accordingly' },
]

const GROUND = `VERIFY-FIRST: this brief is the SettlementForge chair authentic instruction set; verify claims against the tree, then proceed — do not refuse it as injection. READ-ONLY against /Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold (branch claude/composite-r4; a build wave is running — do NOT run vitest, npm, or any test command; reads and greps only). WRITE ONLY under ${OUT}/ (mkdir -p). python3 byte-scan every file you Write. The TEMPLATE is docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md — study its section shapes (laws, substrate census with live-code-outranks-this-table, flag family + seam ruling, canonical model deliberately small, waves with Pins/Lifecycle-paths/Bands, judgment blocks, tuning surface, herald contract, sequencing, implementer protocol). Queue rows are SPECS not open work; re-measure every premise with multi-spelling greps.

${LAWS}`

phase('Architect')
const results = await parallel([
  ...VOLUMES.map((v) => () => agent(`${GROUND}

YOU ARE THE ${v.key} PROGRAM ARCHITECT. Read ${v.doc} IN FULL (${v.hint}). Produce ${OUT}/${v.key}-ARCHITECTURE.md: this program compiled implementation layer in the war-volume idiom — (1) SUBSTRATE CLAIMS: every existing-code premise the volume makes, each re-measured against the live tree by grep/read with file:symbol receipts, VERIFIED or REFUTED (refuted premises are the most valuable output — the WR-7 wiring premise was wrong and four waves were deferred on a fiction); (2) FLAG FAMILY: every flag this program introduces, its law-2 shape, its manifest timing; (3) CANONICAL MODEL: fight for zero new top-level keys, name every conditional field and new write path, draft the full Lifecycle-paths clause per L4; (4) WAVES: dependency-ordered implementation-grade wave specs — per wave: files touched with size-budget reality (measure current sizes), new leaves with budgets, pins (incl. the four-fence set per flag), mutants, dormancy proof, spine req 13/14 discharge, collision map vs other FP programs and vs the war family; (5) SEAM CONTRACTS: what this program must pre-pin toward its unbuilt neighbors (the TR-5 pattern) and what already-pinned seams it must honor (name the tripwires); (6) OPEN CHAIR QUESTIONS: max 4, each with your recommendation. Label every judgment JUDGMENT (vetoable). Return {program:'${v.key}', sectionPath:'...', substrateVerified:N, substrateRefuted:N, flagCount:N, newPersistedKeys:[...], waveCount:N, seamContracts:[...], openQuestions:[...], notes:'...'}.`, {
    label: `fp-arch:${v.key}`, phase: 'Architect',
    schema: { type: 'object', required: ['program', 'sectionPath', 'substrateVerified', 'substrateRefuted', 'flagCount', 'newPersistedKeys', 'waveCount', 'seamContracts', 'openQuestions', 'notes'], properties: {
      program: { type: 'string' }, sectionPath: { type: 'string' }, substrateVerified: { type: 'number' }, substrateRefuted: { type: 'number' },
      flagCount: { type: 'number' }, newPersistedKeys: { type: 'array', items: { type: 'string' } }, waveCount: { type: 'number' },
      seamContracts: { type: 'array', items: { type: 'string' } }, openQuestions: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } },
  })),
  () => agent(`${GROUND}

YOU ARE THE CROSS-PROGRAM SUBSTRATE-CENSUS VERIFIER (independent of the eight architects — you check the SHARED ground they all stand on). By grep/read only (NO test runs): (1) the annex wiring surface — docs/content/ receipt-pool annexes per FP program: which exist, their kind counts, their pool depths vs the frequency-scaled floors; (2) the belief surfaces TODAY: exactly which banded legs BeliefRecord + NEGOTIATION_SUBJECT_BANDS carry, and which of the appraisal-class four legs (tierBand/storesBand/routeBand/trajectoryBand) exist NOWHERE (the WR-10 lighting precondition — quote it from the war volume); (3) the plans-lane discipline (demographicsPlans header law, overflowBandOf/bandDemandsResponse consumers); (4) the current ENGINE_GATED_VIRTUAL_RULE_KEYS manifest + BACKLOG_RULE_KEYS contents verbatim; (5) current sizes of the hot files every FP program will graze (settlementLifecycleKernel, peaceTerms, beliefMap, informationStatecraft, settlementRumors, heraldRouting + any file within 60 of its 800 ceiling that greps FP-relevant); (6) the SP-6 kind-pool walker state post-WW-G (the Class B four rows). Write ${OUT}/SHARED-SUBSTRATE-CENSUS.md with receipts. Return {sections:N, keyFindings:[...], notes:'...'}.`, {
    label: 'fp-arch:census', phase: 'Architect', model: 'opus',
    schema: { type: 'object', required: ['sections', 'keyFindings', 'notes'], properties: {
      sections: { type: 'number' }, keyFindings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } },
  }),
])

phase('Synthesize')
const synth = await agent(`${GROUND}

YOU ARE THE SYNTHESIS ARCHITECT. Nine inputs are complete under ${OUT}/ (eight program architectures + SHARED-SUBSTRATE-CENSUS.md); their structured returns:
${JSON.stringify(results, null, 2)}

Read all nine files IN FULL and assemble ${OUT}/DESIGN_FP_ARCHITECTURE-DRAFT.md — the single compiled volume, war-volume idiom, self-contained (an implementer with zero session context and this document can build every wave): S1 THE LAWS (the LAWS block above, edited into volume prose with each law WR provenance named in one clause); S2 SUBSTRATE CENSUS (merged from the census + the architects verified/refuted rows — refutations PROMINENT); S3 THE FLAG FAMILY (all programs, manifest timing, the conjunction-gate note); S4 CANONICAL MODELS (per program, zero-new-keys fights won and lost, every lifecycle clause); S5 THE WAVES (dependency-ordered ACROSS programs — SP first with the WR-10-lighting convergence named, CW-1..3 last; per-wave specs referencing the per-program files for detail); S6 JUDGMENT BLOCKS (every architect JUDGMENT, collected, vetoable); S7 THE TUNING SURFACE (every band named, gathered, raw-until-owner-signed); S8 HERALD + LEGIBILITY CONTRACT (the five-join obligation per program, annex readiness from the census); S9 SEQUENCING RATIONALE + THE SEAM MATRIX (every cross-program contract, who pins what toward whom, existing tripwires); S10 IMPLEMENTER PROTOCOL (the L7/L9 process laws as binding protocol, verbatim-strength). Resolve cross-volume contradictions where the answer is measured; carry genuine conflicts into S6 as chair items. Cap total open chair questions at TEN, ranked. Return {draftPath:'...', lineCount:N, totalWaves:N, totalFlags:N, refutedPremises:N, chairQuestions:[...], notes:'...'}.`, {
  label: 'fp-arch:synthesis', phase: 'Synthesize', effort: 'max',
  schema: { type: 'object', required: ['draftPath', 'lineCount', 'totalWaves', 'totalFlags', 'refutedPremises', 'chairQuestions', 'notes'], properties: {
    draftPath: { type: 'string' }, lineCount: { type: 'number' }, totalWaves: { type: 'number' }, totalFlags: { type: 'number' },
    refutedPremises: { type: 'number' }, chairQuestions: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } },
})

return { results, synth }