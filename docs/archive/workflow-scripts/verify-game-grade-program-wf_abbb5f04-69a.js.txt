export const meta = {
  name: 'verify-game-grade-program',
  description: 'Adversarially verify the G-wave program architecture before commit',
  phases: [{ title: 'Refute' }],
}

const COMMON = `You are a READ-ONLY adversarial verifier. The manager (Fable) architected docs/GAME_GRADE_PROGRAM.md in /Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold (read it first, plus docs/GAME_GRADE_AUDIT.md for the evidence base). Composite tree @ 69b7a8d3. Your job is to REFUTE the architecture — find where it is wrong, infeasible, or non-compliant BEFORE it is committed. Default to skepticism; a finding must cite file:line evidence. Do not edit anything. Return findings as your final text: each finding = SEVERITY (MUST-FIX / SHOULD / NOTE) + claim + evidence. If a lens finds nothing, say so explicitly.`

const LENSES = [
  { key: 'feasibility', prompt: `${COMMON}\nYOUR LENS: FEASIBILITY OF REUSE CLAIMS. The program repeatedly claims 'existing machinery, rendering only'. Verify at code every load-bearing reuse claim: (1) G-2a: does regenSection really write lastRegenerationDelta (settlementSlice.js ~1157) and could full generate() write it without a NEW store action? (2) G-2c: do FMG-bridge accessors for terrain/biome-at-cell and region-graph neighbor helpers actually exist and are they importable from a lazy leaf without dragging eager chunks? (3) G-2d: can RealmForecast/RealmDocket machinery render at the Advance confirm outside the inspector chunk without making wizardNews/inspector code eager (E-J hazard: wizardNews is EAGER — check the import graph)? (4) G-2e: do engine coupling tables for institutions/resources/stressors/traditions exist in an importable, typed form the way describeDeityEffects does for deities? (5) G-3: severityBand() location and whether a single needsAttention selector can read proposals+stressors+lapsed orders from savedSettlements/world without new store state. (6) G-4: can Herald headlines dispatch 'Open Adjudication focused on this proposal' with EXISTING actions (is there a proposal-focus mechanism)? (7) G-0: does WHAT_PHRASES cover the heraldFeed human(kind) fallback keys? Attack each; report which claims are false or need a different mechanism.` },
  { key: 'doctrine', prompt: `${COMMON}\nYOUR LENS: DOCTRINE AND SCOPE COMPLIANCE. Attack the program against the laws: (1) THE PROMISE / display-only — does ANY G-wave item actually require engine/generation/persistence changes or same-seed output shifts (e.g. G-2a's lastRegenerationDelta write on generate(): store-only or persisted? G-2c placement forecast: reads only? G-5 usage index: derived or stored?)? (2) No-new-store-actions — which items secretly need one? (3) Owner-gates — does the program correctly leave gated items gated (per-order forecast, compendium deep-link scheme, golden regens), and is the compendiumInlineHelp flag flip properly classified as vetoable-judgment vs owner-gated (is that flag referenced in any paid/pricing surface)? (4) FINITE-SEMANTICS — do the new sentence surfaces (result recap, ticker, effect previews) compose from typed slots/existing composers only, or does any invite freeform composition? (5) closure/size hazards — which proposed components risk the 194-byte margin or the WorldMap.jsx 2-line ceiling; is every new module correctly specified as a lazy leaf? Report violations with the exact program line.` },
  { key: 'completeness', prompt: `${COMMON}\nYOUR LENS: COMPLETENESS VS THE AUDIT. Cross-check docs/GAME_GRADE_PROGRAM.md against docs/GAME_GRADE_AUDIT.md item by item: (1) every numbered violation V-* — which wave closes it? List any with NO home. (2) Every quick win Q-* — same. (3) Every per-point GAP paragraph in the audit — which are addressed, which are silently dropped (distinguish from the program's explicit Deferred list — silent drops are findings, recorded deferrals are fine)? (4) The scoreboard's ABSENT cells (GEN QUERY_WEB, GEN LENSES, SED LENSES) — does the program either fix or explicitly defer each? (5) Are there audit findings that contradict the program's claims (e.g. the program says severityBand exists at heraldFilter.js:20-25 — confirm)? Output the orphan list.` },
]

phase('Refute')
const out = await parallel(LENSES.map(l => () =>
  agent(l.prompt, { label: `refute:${l.key}`, phase: 'Refute', model: 'opus' })
))
return { findings: out.filter(Boolean) }