export const meta = {
  name: 'm3d-fidelity-research',
  description: 'Research: cathedral-grade procedural 3D buildings, deterministic, condition-driven',
  phases: [{ title: 'Research', detail: 'four parallel research lenses' }],
}
const OUT = { type: 'object', additionalProperties: false, required: ['report'], properties: { report: { type: 'string' } } }
phase('Research')
const results = await parallel([
  () => agent(`Web research task. Survey the state of the art in PROCEDURAL ARCHITECTURAL GENERATION at high detail: CGA/split shape grammars (Mueller et al., CityEngine lineage), facade grammars, procedural Gothic/medieval building generation, grammar-driven ornament (buttresses, tracery, arcading, statuary). For each approach report: core mechanism, whether it is implementable as a DETERMINISTIC pure function (seed -> identical geometry, no GPU dependence at generation time), typical parameter sets, and the smallest viable subset that yields a recognizable detailed cathedral/keep/mill. Cite sources with dates. End with a ranked recommendation for a from-scratch JS implementation under strict determinism laws. Dense report, no padding.`, { label: 'research:shape-grammars', schema: OUT, model: 'opus' }),
  () => agent(`Web research task. Compare RENDERING STACKS for displaying procedurally generated, detailed isometric buildings in a React web app under strict constraints: (a) three.js/WebGL lazy-chunked (bundle size gzipped, deterministic mesh generation vs non-deterministic rasterization — is vertex/geometry generation cross-platform deterministic in JS IEEE-754? cite), (b) high-detail static SVG (thousands of paths per building — perf limits, print/export fidelity), (c) hybrid mesh-generate + software-project to SVG (deterministic byte-identical exports from 3D geometry, no GPU in the artifact path). For each: bundle cost, determinism story, LOD strategy, glTF export determinism. Cite sources with dates. End with a ranked recommendation where the CANONICAL artifact must be byte-reproducible.`, { label: 'research:rendering-stacks', schema: OUT, model: 'opus' }),
  () => agent(`Web research task. Inventory EXISTING open-source procedural building/architecture generators reusable in a commercial MIT-compatible product: medieval/fantasy building generators (Watabou lineage, others), shape-grammar libraries in JS/TS, Wave Function Collapse implementations for architectural detailing, parametric ornament libraries, and any three.js-ecosystem procedural-building projects. For each: license, language, detail ceiling (can it approach a textured isometric cathedral render), determinism, maintenance state (last release date). End with: reuse candidates worth vendoring vs a build-from-scratch verdict. Cite with dates.`, { label: 'research:prior-art', schema: OUT, model: 'opus' }),
  () => agent(`Repo feasibility task (READ-ONLY) in /Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold. Answer with file:line receipts: (1) Does the settlement model carry ALIGNMENT axes (lawful/chaos, good/evil) anywhere — settlement, culture, faction, or deity level? Search broadly (alignment, ethos, lawful, chaotic). If absent, which existing typed state could honestly DERIVE a two-axis alignment (legitimacy, corruption covert/revealed, order/crime, piety)? (2) Enumerate the typed state available to drive a building-detail parameter vector: culture, prosperity band, terrain, nearby resources, age, war/occupation — where does each live? (3) Where would a mesh-grammar module live under the domain laws (lazy leaf pattern, purity bans — read src/domain/townMap/massing.js header + tests/build/vendorPdfLazy.test.js for the lazy-vendor-chunk precedent with react-pdf)? (4) What would a lazy three.js chunk mean for the closure ratchet (read how the pdf vendor chunk stays out of the entry closure)? Dense report, receipts everywhere.`, { label: 'research:repo-feasibility', schema: OUT, model: 'opus' }),
])
return { reports: results.filter(Boolean).map((r, i) => ({ lens: ['shape-grammars','rendering-stacks','prior-art','repo-feasibility'][i], report: r.report })) }