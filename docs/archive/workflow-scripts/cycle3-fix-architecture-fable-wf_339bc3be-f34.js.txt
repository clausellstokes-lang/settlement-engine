export const meta = {
  name: 'cycle3-fix-architecture-fable',
  description: 'Fable-tier architecture of the cycle-3 fix program: fix + structural prevention per finding class',
  phases: [
    { title: 'Architect', detail: 'one Fable architect per finding class' },
    { title: 'Synthesize', detail: 'the wave-program fix plan' },
  ],
}
const WT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const PRE = `You are a Fable-tier FIX ARCHITECT for the settlement-engine cycle-3 audit (docs/CYCLE3_AUDIT.md in ${WT}, READ-ONLY — design only, do not edit). Do NOT just patch each finding. For the CLASS below: (1) find the ROOT CAUSE the instances share; (2) design the correct FIX (the chokepoint that fixes the whole class, not N patches — cite the exact files/helpers/APIs to use, by path, verified at code); (3) design the STRUCTURAL PREVENTION that removes the class's habitat, choosing from: Pattern1 inventory-ratchet (only-shrinks), Pattern2 manifest+walker (every-X-must-Y, exact-set discovery), Pattern3 single-writer+source-scan, Pattern4 same-seed golden, Pattern5 purity conventions — specify the walker/ratchet CONCRETELY (what it scans/enumerates, the exact-set assertion, the failure message that names the fix); (4) flag GATE class per finding: reversible-no-golden (fix now) / golden-shifting (owner ONE REGEN) / deploy-SQL-paid (owner). Governing laws: THE PROMISE (determinism), FINITE-SEMANTICS (typed buckets, AI never writes), the closure budget, the FULL lint/copy families run at every fold. Prior enforcers to EXTEND not duplicate: E-A mutation-sweep, E-B candidateType walker, E-D AI-surface scan, E-E voiceMechanics/proseLeak, E-F tickOpBudget, E-G narrative-parity, E-I keyboard-placement. Verify claims at code; repo wins.`
const OUT = {
  type: 'object', additionalProperties: false,
  required: ['rootCause', 'fix', 'prevention', 'gateClass', 'sequencing'],
  properties: {
    rootCause: { type: 'string' }, fix: { type: 'string' },
    prevention: { type: 'string' }, gateClass: { type: 'string' }, sequencing: { type: 'string' },
  },
}
const CLASSES = [
  ['data-contract-dead-vocabulary', 'H1,H3,H4,H14,H15,H16,M2,M5,M15,M16 — a consumer branches on enum/label values the PRODUCER never emits (threat heartland vs safe; stressor occupied vs occupation; safety severity prefixes; rumor shape belief vs truth-object; missing threat/stress arms). Class cure candidate: a producer↔consumer vocabulary walker (Pattern2) that enumerates every value a generator EMITS for a typed field and asserts every consumer switch/map covers exactly that set — dead arms AND missing arms both red. Extend E-B.'],
  ['composed-prose-seams', 'H2,H5,H6,H7,M1,M3,M4 — authoring artifacts leak into user-facing output: unfilled {brace} tokens, doubled articles (the The / the the), The-prefixed names re-prefixed, raw PLOT HOOK: prefix, camelCase simulationRules keys shown raw. Class cure candidate: extend E-E proseLeak into a composed-output walker (Pattern1 ratchet + Pattern4 golden) that scans rendered generator/letter/brief output for {…}, /\\bthe (the|a|an)\\b/i, engine-token/camelKey leakage. Note which are golden-bound (generator prose) vs pure display.'],
  ['fmg-fork-sinks', 'C1(FIXED),H9,H10,H11 — the vendored same-origin /map/ fork: innerHTML sinks interpolating untrusted map names unescaped (tip/general.js — escapeHtml exists); sf-bridge reports pack.seed which FMG never sets (read global seed); versioning.cleanupData() calls localStorage.clear()+Cache nuke on the SHARED host origin, destroying the host Supabase session. Class cure: Pattern3 source-scan — every fork innerHTML sink routes through escapeHtml/sanitizeMapSvg; no localStorage.clear/caches.delete unscoped on the shared origin; the fork is outside code gates so the guard lives in tests/security. Extend mapForkXssChain.'],
  ['perf-quadratic-tick', 'H17,H18,M17,M18,M19,M20 — per-tick O(S²): npcAgency settlementForState linear .find per state; roadsKernel warTargets/dominionTargets full scans per settlement + atWarWith edge-scan per pair; stressorDynamics edgesTouching filters all edges per call; worldSnapshot mints fresh objects defeating the derivationCache WeakMap + calls ensureRegionalGraph not the Once fast-path. Class cure: per-tick indices (settlement-by-id map, edge-by-endpoint index) built once; object-identity preservation for the cache. Prevention: extend E-F tickOpBudget to a deterministic op-count that reddens on quadratic blow-up (Pattern2/5).'],
  ['ui-a11y', 'H12,H13,M9,M10,M12,M13 — keyboard/SR dead-ends: EntityPicker + GalleryDescriptionEditor toolbar mouse-only (onMouseDown, no key handler); PurchaseModal clips on short viewport (overflow:hidden no maxHeight); TableView aria-modal no focus trap; Glossary focus-trap keyed on onClose identity; PostGenCoach×FeedbackWidget z-index collision. Class cure + Pattern2 a11y walker: every interactive control keyboard-operable; every aria-modal focus-trapped; extend E-I. Note the walker must scan JSX for onMouseDown-without-onKeyDown and role=dialog-without-focus-trap.'],
  ['test-integrity', 'H8,M6,M7,M8 — tests that lie: refundPolicy claims total-over-the-FailureStage-union but hardcodes 3 stages; mapBridge/gallery_privacy/snapshotDenylist tests with if(!msg)continue, empty-body functionBody fallback, metachar-stripped token lists → assertions never run. Class cure: Pattern2 anti-vacuity meta-test — every security/contract test must reach ≥1 assertion on non-empty input; exhaustive-claim tests must derive their set from the real union, not a literal. Extend the e2e anti-vacuity idiom.'],
  ['money-deploy', 'H20,H21,M22,M23,M11 (OWNER-DEPLOY, architect the fix + guard only): stripe-webhook treats ANY refund as full reversal (partial goodwill refund claws back the founder seat / poisons the dossier voucher) — needs amount-aware partial-refund handling; migration-142 npc-goal leak (allowlist only under key npcs, members[] bypass); verify-checkout-session unthrottled; Turnstile inert on first verify. Prevention: Pattern2 allowlist-completeness walker (every NPC-bearing path in the public projection hits the same field allowlist); a refund-amount contract test. Flag all as owner-deploy.'],
  ['determinism-leak', 'M21 — Editable.safeName falls back to Math.random() for a falsy field name (PDF field names). Class cure: deterministic fallback (hash of index/content). Prevention: extend the determinism-ban source-scan (Pattern5 rung-1) to cover src/pdf/ — Math.random in any render/derivation path reds.'],
]
phase('Architect')
const arch = await parallel(CLASSES.map(([key, desc]) => () =>
  agent(`${PRE}\n\nYOUR CLASS (${key}):\n${desc}`, { label: `arch:${key}`, phase: 'Architect', schema: OUT, model: 'fable' })
))
const designs = CLASSES.map(([key], i) => ({ key, ...(arch[i] || { rootCause: 'ERROR' }) }))
phase('Synthesize')
const plan = await agent(
  `${PRE}\n\nYou are the RECONCILER. Eight Fable architects designed the fix + structural prevention for each cycle-3 finding class. Here are their designs:\n\n${JSON.stringify(designs, null, 2)}\n\nProduce the WAVE-PROGRAM FIX PLAN: (1) ordered fix waves (data before consumers; correctness-asserting prevention LAST over honest data; deploy/golden waves owner-gated) — each wave = its findings, its fix chokepoint, its prevention guard, its gate; (2) the STANDING PREVENTION LAYER the program leaves behind (the new walkers/ratchets, each extending a named prior enforcer where possible); (3) which waves are dispatch-ready now (reversible-no-golden) vs owner-gated (ONE REGEN / deploy); (4) an honest note on any finding whose fix has no clean structural guard. Decisive; this is the plan a successor executes from.`,
  { label: 'synthesize', phase: 'Synthesize', model: 'fable' })
return { designs, plan }