export const meta = {
  name: 'settlement-map-design-recon',
  description: 'Parallel read-only recon grounding the library settlement-map design',
  phases: [{ title: 'Recon', detail: '7 read-only surveys of the surfaces the map design must honor' }],
}

const SCHEMA = {
  type: 'object',
  required: ['summary', 'facts', 'hazards', 'reuse', 'openQuestions'],
  properties: {
    summary: { type: 'string' },
    facts: { type: 'array', items: { type: 'object', required: ['point', 'path'], properties: { point: { type: 'string' }, path: { type: 'string' } } } },
    hazards: { type: 'array', items: { type: 'string' } },
    reuse: { type: 'array', items: { type: 'string' } },
    openQuestions: { type: 'array', items: { type: 'string' } },
  },
}

const COMMON = `You are a READ-ONLY recon agent in /Users/cstokes/Desktop/settlement-engine (branch review-fixes-2026-07-08). Do NOT run tests or builds, do NOT write or edit any file, do NOT touch git state (read-only git commands ok). A parallel implementer has unstaged deletions in the tree — ignore unstaged noise, read files as they are. Read whole files, not grep windows. Return ONLY the structured object: summary (dense, <300 words), facts (each an exact field/function/component fact with its repo path), hazards (things a settlement-map feature could break or must not violate), reuse (existing code to reuse rather than re-implement), openQuestions (genuine unknowns). Context: we are DESIGNING (not building) a deterministic, dossier-coupled, library-scoped settlement town map: a [Dossier|Map] toggle above the dossier in the library settlement view; SVG town map derived from the settlement's data; hover a building/district shows its institution; basic edits; dossier and map stay mutually consistent.`

const BRIEFS = [
  { key: 'dossier-data', prompt: `${COMMON}\n\nYOUR SLICE — the per-settlement DATA available to drive procedural map synthesis. Survey src/domain generation + view-model code (assembleSettlement, assembleInstitutions, resolveResources, generatePower, the dossier view model builders, display/ modules). Report exactly: what institutions exist per settlement (list shape, categories/types, do they carry STABLE ids that survive regeneration? what fields: name, type, tier?); population + settlement size tier taxonomy (thorp..city? exact names + where defined); terrain/geography/water fields on a settlement; walls/fortification data; districts or quarters if any concept exists; economy/trade-goods fields; the seed fields (how a settlement's seed is stored and forked). Also: how the dossier is composed for display (which module turns settlement -> dossier sections) and whether institutions in the dossier reference ids usable for map<->dossier cross-linking.` },
  { key: 'library-ui', prompt: `${COMMON}\n\nYOUR SLICE — the LIBRARY settlement-view UI. Survey src/components + routing/store: how the library lists settlements and opens one; the component tree from library entry to dossier render; is the settlement/dossier view inside a LAZY-loaded chunk (dynamic import / React.lazy) or eager on first paint (this is load-bearing: first-paint margin is 77 bytes); existing tab/toggle/segmented-control precedents in the app; how the GALLERY / shared / anonymous view renders a settlement (same component? different route?); deep-link/anchor behavior in the dossier (can we scroll-to-institution?); where a [Dossier|Map] toggle would naturally mount. Name exact components + files.` },
  { key: 'map-tech', prompt: `${COMMON}\n\nYOUR SLICE — existing MAP/SVG/raster tech to reuse. Survey the realm-map surface: src/components/map/ (MapOverlay.jsx and siblings), terrain rendering, pan/zoom handling, hover/tooltip/hit-testing precedents on SVG, src/lib/mapThumb.js serializeOverlaySvg + the campaign thumbnail pipeline, public/map/sf-bridge.js exportThumb rasterizer. Report: what is reusable for a per-settlement town map (SVG scaffolding, pan/zoom, tooltip, raster export), the interfaces of each reusable piece, how the realm map lazy-loads (chunk boundaries), and the data-map-overlay-svg attribute situation (a known lineage gap: the attribute was lost on this lineage; campaign thumbs composite bare terrain).` },
  { key: 'edit-lifecycle', prompt: `${COMMON}\n\nYOUR SLICE — settlement EDIT + PERSISTENCE lifecycle (the owner's most-bitten bug class is edits that survive one path and ghost another). Survey src/store/settlementSlice.js (resetSettlementIdentity chokepoint, undo), persistMerge.js, importScrub.js, export/import paths, any regeneration path for a library settlement (can a user reroll/regenerate? what survives?), and any EDIT-DELTA/replay precedent (edits stored as deltas replayed after regen). Report: every lifecycle path a new optional per-settlement field (e.g. cosmetic mapEdits deltas) must survive — create/read/persist/regen/undo/clone/import/export/scrub — with the exact functions; what users can edit on a settlement today and through which ops; whether persistence-shape additions have a dormancy pattern (absent => prior bytes); importScrub's denylist mechanics.` },
  { key: 'tier-seams', prompt: `${COMMON}\n\nYOUR SLICE — ENTITLEMENT + SANITIZE seams the map must respect. Survey src/config/tierFacts.js (the ladder: free / $2.99 single-dossier / premium), src/domain/display/publicSafe.js (constitutional law: free/anon never see deity names or DM truth), the gallery sharing/sanitize path, PDF entitlement gating, and the tests that pin these seams. Report: exactly how an institution's display name is sanitized today for anon/free viewers (a temple named for a deity is the danger case — a map tooltip must not leak what the dossier hides); which projection function a map inspector should consume; how gallery-shared settlements are viewed by anon users; what entitlement precedent fits map VIEWING vs map EDITING vs map-in-PDF.` },
  { key: 'determinism-budget', prompt: `${COMMON}\n\nYOUR SLICE — the HOUSE LAW a new derived surface must follow. Survey: seeded-fork precedents (stable composite keys, the rng fork helpers — exact names/paths), codepoint-sorted iteration rules, the golden/manifest test pattern (the generator manifest ~187 configs, spatial + deity manifests — file paths, how a manifest pins byte-identity, how UPDATE_GOLDEN works), the first-paint budget mechanics (tests/build/vendorPdfLazy.test.js CLOSURE_BUDGET_BYTES = 1_216_350, measured closure 1,216,273 — margin 77 B; how the closure census walks eager imports), lazy-chunk patterns (FP-2a loadEngine dep-import, the Lane-2 eager-leaf/lazy-applier split, stressorsCore leaf), engine purity (no Date.now/Math.random — where enforced), the any-cast ratchet (2,248). Report: what a new deterministic settlementMapModel derivation must register/obey, and the exact pattern for adding a NEW manifest surface additively.` },
  { key: 'pdf-export', prompt: `${COMMON}\n\nYOUR SLICE — the PDF + thumbnail EXPORT pipeline (integration points only — no design). Survey src/pdf/ (document composition, primitives incl. ProseText.jsx, how pages/sections register, the goldenViewModel pin), tests/build/vendorPdfLazy.test.js's lazy contract for pdf vendors, and the settlement/gallery THUMBNAIL pipeline (what image represents a settlement card today; sf-bridge exportThumb usage). Report: how a future map PLATE could join the dossier PDF (vector vs rasterized, where it would register, what the golden pin implies), whether thumbs could later derive from the settlement map, and every contract/test that would notice.` },
]

phase('Recon')
const results = await parallel(BRIEFS.map(b => () =>
  agent(b.prompt, { label: `recon:${b.key}`, phase: 'Recon', schema: SCHEMA })
    .then(r => ({ key: b.key, ...r }))
))
const missing = BRIEFS.filter((b, i) => !results[i]).map(b => b.key)
if (missing.length) log(`MISSING slices (agent died/skipped): ${missing.join(', ')}`)
return { recon: results.filter(Boolean), missing }