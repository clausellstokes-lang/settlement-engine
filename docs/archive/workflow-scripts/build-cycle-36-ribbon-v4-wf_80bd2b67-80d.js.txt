export const meta = {
  name: 'build-cycle-36-ribbon-v4',
  description: 'RIBBON V4 — THE WAR ARROW REPAINT: the consultation spec executed whole (gilded wordmark, cedar shaft, mirrored slashes, photoreal texture)',
  phases: [{ title: 'Build', detail: 'one nav lane, serial commits per section, implement + verify with eye pass' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const SPEC = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/1c189f3d-9fc3-4fc9-9cb7-b767525d28d5/scratchpad/ribbon-v4-spec.md'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files (nav/*, brand/*, theme.js, styles/a11y.css if the polish lane landed there, their tests); THE STAGED-SET LAW (git diff --cached --name-only before EVERY commit; git commit -- <paths>); no stash; focused vitest + gate-tail (ps aux | grep vitest first) INCLUDING tests/domain registration walkers; python byte-scan every Write; npm run build + smoke:boot; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. SURVEY FIRST: the polish lane (a11y focus ring, deepCraft conversions) may have landed on nav/components — build from HEAD's truth. CONCURRENCY: WZ-2 is live on worldPulse — fenced. Screenshot before pinning geometry; two-load byte determinism on every texture.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: OWNER-DIRECTED implementation. THE SPEC IS THE FILE AT ${SPEC} — read it WHOLE first; it is the design consultation's two-part spec, chair-accepted, carrying the owner's five corrections + the photorealism mandate with exact hexes, pins, thresholds, and layer orders. ${P10}
LANE V4 — execute the spec in serial commits:
(1) THE COLOR SYSTEM + PIN POLARITY: the cedar ladder (BODY L ≤ 0.13 — THE DEAD-BAND LAW is absolute), the feather ink ladder, the oxblood wraps, every dark-ink rider flipped to the parchment register (chips retoned), riderFloorTone gains per-rider POLARITY, the re-scoped vane-vs-wood 1.4.11 pin with its identification rationale.
(2) THE GILDED WORDMARK: bole bed (+ its ⊇ink+3px / ≤L0.06 pin) → gold leaf (stops L ≥ 0.44) → keyline; THE PLATE RETIRES from the header (Device stays favicon/PDF; sweep dead plate tokens); the GILDED SEAL-O per spec — and record in the queue row that the gilded-seal treatment is the OWNER-VETO CANDIDATE with the rosette fallback named.
(3) THE BAND GEOMETRY: mirrored lean, SEAT sign flip, four identical parallel slashes (FRAME's squared ends dropped; outer ends resolve at the quill line under the wraps), the relaxed edge-light, the QUILL-LINE indicator in GILT_LIGHT.
(4) THE PHOTOREAL TEXTURE per part 2 exactly: splits (with the calm-zone re-roll), the one-curve law (bowed barbs/sheen/slivers off two shared constants), fray on exposed edges only, the three wood layers (darkening-only; pale streaks banned), thread-true whipping with tie-offs, the 1x/retina threshold discipline (retina-only cues each <2% effective ink), the layer orders as written.
Screenshot at 100%/200% each commit with honest eye verdicts; the composited-AA pin re-derived at every ground change; ANCHOR derivation + font-size pins untouched; mobile follows the cedar bar per the spec's mobile note. Return {lane:'V4', commits, gates, stopReports, notes}.`,
  {label:'v4:war-arrow-repaint', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier with an EYE PASS. Read ${SPEC} whole first. ${P10}
Report: ${JSON.stringify(r)}. JUDGE from served screenshots at 100% and 200%: (a) does the wordmark read as a GILDED MEDIEVAL ARTIFACT mounted on the arrow (bole visible as a branded bed, leaf luminous, seal set in its gold ring)? (b) do the four slashes read as ONE mirrored parallel run with the outer ends resolving under the wraps? (c) do the cells read as REAL FEATHERS now (splits visible, barbs bowing, fray organic) — feathers, not corrugation, not flat cells? (d) does the whipping read as WOUND THREAD at 200%? (e) the indicator ON the quill line, clearly active-state? Say each plainly. MECHANICAL, all re-executed by YOU: the dead-band law (measure the shipped BODY luminance ≤ 0.13); every rider's AA from real rendered pixels with the CORRECT polarity per rider (quote all ratios; mutant the polarity field → the pin must red); the bole pin's mutant (shrink the bole under the ink → red); stop-luminance pin (a gold stop below L 0.44 → red); calm-zone exclusions (a split sliver forced into a label zone must re-roll or red); retina-only cues each <2% effective ink at 1x (measure areal coverage); two-load byte determinism; one-curve law (grep: the bow constants have ONE writer); focus rings by real Tab; ANCHOR + font sizes unchanged; typed contexts still plain; build + smoke:boot; staged-set + pathspec vs the WZ-2 lane. Return {lane:'V4 verify', verdict, findings}.`,
  {label:'v4:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }