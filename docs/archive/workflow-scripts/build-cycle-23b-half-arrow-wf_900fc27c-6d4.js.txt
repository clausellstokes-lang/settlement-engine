export const meta = {
  name: 'build-cycle-23b-half-arrow',
  description: 'Ribbon v3: the HALF-ARROW composition (owner clarified) · the icon-sweep golden ruling',
  phases: [{ title: 'Build', detail: 'half-arrow ribbon + disclosed golden re-record' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY: WR-8 live on worldPulse; typing/prose lanes live on domain/generators — nav lane owns nav/* + theme.js + nav tests; golden lane owns tests/fixtures/generator-golden-master.json + tests/property/generatorGoldenMaster.test.js + queue docs. STANDING RIBBON LAWS: overhang is PAINT never layout; ANCHOR_OFFSET derives; focus survives clipping; AA vs the worst stripe; single-writer tokens; pins cite the directive.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const lanes = await parallel([
  () => agent(`VERIFY-FIRST: OWNER-DIRECTED implementation from a reference photograph + a composition clarification dictated live (2026-08-03 evening). ${P10}
THE REFERENCE PHOTO (described exactly): a real arrow on hide — pale HONEY-TAN wooden shaft, fine longitudinal grain, satin sheen; a large TURKEY FEATHER: deep chocolate-brown vane with irregular lighter tan/cream horizontal BARRING (bars of varying width, ragged edges, denser toward the tip), a visible central rachis, elongated silhouette with rounded trailing tip; RED-BROWN thread WRAPPING bands where fletching meets shaft.
THE COMPOSITION CLARIFICATION (supersedes any per-feather framing): THE TOP RIBBON IS HALF OF AN ARROW, SEEN IN PROFILE — as if the arrow lies along the top edge of the screen and the viewport shows THIS SIDE'S HALF: the shaft's visible half runs the ribbon's full width (under the wordmark too), and the fletching you see is the near side's feathers — Create, Library, Realm as the three visible fletches ROOTED ON the shaft (quill bases meeting the shaft line, vanes sweeping back toward Realm), one continuous object, not three feathers pasted on a plank. Their lower vane edges PEEK slightly below the ribbon's bottom border (~6px, PAINT ONLY) — that peek is the half-arrow read: the feathers hang past the bisection line. PHOTOREALISTIC is the goal.
LANE V3 — BUILD IT:
(1) THE SHAFT HALF: honey-tan wood with matching texture — deterministic inline-SVG data-URI (feTurbulence FIXED seed, fine longitudinal grain, subtle sheen gradient reading as a CYLINDER'S visible half: slightly darker toward the ribbon's bottom edge, lighter at the top — the profile shading is what sells 'half an arrow'); color family from the photo (~#C9A46B..#D9BC8A, tune by eye); single-writer tokens.
(2) THE THREE FLETCHES: per-tab SVG feathers in profile — chocolate vane (the existing dark ink family) with authored irregular turkey barring (organic wobble, varied widths, denser tipward — never mechanical stripes), fine rachis, quill base visually meeting the shaft line (no floating gap), soft shadow; vanes sweep toward Realm; labels SEMIBOLD in the vane's calm zone (darken a rachis band behind the label if barring fights text).
(3) THE WRAPS: two narrow glossy red-brown thread bands riding the shaft, framing the fletching cluster — the photo's wraps. Active grammar: the active feather's barring brightens + the gold underline; focus ring proven on the clipped shape.
(4) Reference tabs plain dark ink on the wood; wordmark dark ink; mobile bottom nav untouched; retire v2's parallelogram/gilt tokens cleanly (sweep dead tokens).
(5) Pins to the new spec (feather paths present, barring layers, wraps, shaft shading, overhang paint-only, AA vs the LIGHTEST stripe under each label — quote ratios); screenshot desktop+mobile in notes with an honest eye verdict; iterate the texture BEFORE committing if it reads clip-art.
One or two pathspec commits; npm run build green. Return {lane:'V3', commits, gates, stopReports, notes}.`,
    {label:'v3b:half-arrow', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier with an EYE PASS. ${P10}
Report: ${JSON.stringify(r)}. Serve, screenshot desktop 1440x900 + mobile, and JUDGE against the clarified composition: does it read as ONE half-arrow in profile (shaft half + rooted near-side fletches hanging past the edge), or as feathers pasted on a plank? Say which, plainly. Mechanical: header layout box unchanged (paint-only mutant reds); ANCHOR derivation; AA recomputed vs the lightest bar stripe under every label (quote); focus ring by real Tab; texture deterministic (two renders byte-identical); quill bases meet the shaft line (no floating gap — check the rendered geometry); dead v2 tokens gone (grep); mobile untouched; nav suites + build green; pathspec-clean. Return {lane:'V3 verify', verdict, findings}.`,
      {label:'v3b:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE GR — THE ICON-SWEEP GOLDEN RULING. THE FACT (bisected by cycle 20's verifier): tests/property/generatorGoldenMaster.test.js is RED at HEAD — all 525 rows drift from the fixture — caused by d9a1ea5a (the icon sweep removing 94 dead icon:"" slots from src/data files that feed generation), four commits before the boot fix. THE CHAIR RULING (vetoable, cite it): this is a DISCLOSED, OWNER-DIRECTED output-shape change — the owner ordered all icons removed; the slots were dead empty strings; the world's semantic content is unchanged. THE LAWFUL PATH: (1) FIELD-LEVEL DIFF FIRST over all 525 rows (old fixture vs regenerated): prove the ONLY movements are icon-slot removals (key deletions of icon/emptied glyph fields and their serialization ripples) — enumerate every differing path-template; if ANYTHING ELSE moved (a name, a count, an id, a draw), STOP-AND-REPORT immediately, that is stream corruption not shape change; (2) only on a clean diff: re-record the fixture IN ITS OWN COMMIT quoting the path-template census + this ruling + the owner directive; (3) the golden test's docstring gains the dated shift record (the HK-3 precedent). Also check the 60-settlement hook corpus + any other fixture the same data files feed (census the fixtures that embed icon fields — same treatment, same proof). Return {lane:'GR', commits, gates, stopReports, notes}.`,
    {label:'gr:golden-ruling', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Re-run the field-level diff YOURSELF with your own flattener (old fixture from git history vs live generation): the differing path-template set must be EXACTLY the icon-slot class — quote the census; regenerate twice for determinism (byte-identical); the golden test green at HEAD; the docstring's shift record present and true; npm run test's golden red CLEARED and no other property test moved (run tests/property whole, attribute any red); pathspec-clean vs the nav lane. Return {lane:'GR verify', verdict, findings}.`,
      {label:'gr:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
])
return { lanes: lanes.filter(Boolean) }