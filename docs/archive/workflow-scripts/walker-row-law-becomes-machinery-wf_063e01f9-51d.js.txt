export const meta = {
  name: 'walker-row-law-becomes-machinery',
  description: 'The law "an enforcement walker may never enter the test census" is prose, and the same commit violates it ten times. Make it checkable — without a filename heuristic that would miss two of the ten.',
  phases: [
    { title: 'Law', detail: 'derive the identification, triage the ten' },
    { title: 'Verify', detail: 'refute the identifier, not just the fix' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const LAWS = `VERIFY-FIRST PREAMBLE: chair brief, owner-delegated build program. LIVE CODE OUTRANKS EVERY TABLE. Build tree ${TREE} (branch claude/composite-r4). **Re-read HEAD yourself.** Hard-gate every state-mutating compound with the \`cd\` FIRST.

⭐ \`sh scripts/gate-mutex.sh --wait && sh scripts/gate-tail.sh <cmd>\` — ONE \`&&\` chain. ⛔ NEVER read an exit code through a pipe (7 recorded, twice by the chair).
⚠ EVERY figure in an INTEGRITY-COUNTED \`git archive\` of a committed sha, **\`git init\`ed and committed** — \`edgeSharedBundleReproducibility\`, \`committedSecretsScan\` and \`sovereigntyLightingContract\` shell out to git and a bare tar extraction makes them LIE. Disk ~9 GiB; one archive at a time.
⛔ **AN OWNER-STARTED SESSION HOLDS FOUR DIRTY FILES**: \`src/domain/worldPulse/{commercialReasons,pactProposals}.js\`, \`src/lib/spatialUsage.js\`, \`tests/lib/spatialUsage.test.js\`. DO NOT touch, stage or clean them. Explicit pathspecs; verify the staged set BY NAME AND COUNT.

REPORTING: CONFIRMED (executed evidence quoted) or PLAUSIBLE.`

phase('Law')
const law = await agent(LAWS + `

YOU ARE CONVERTING A PROSE LAW INTO MACHINERY — and the previous round proved why that matters by violating its own new law ten times in the commit that wrote it.

**THE LAW, established at \`af8815e9\`:** *a failing TEST is debt; a failing WALKER is a DISABLED GUARD; an enforcement walker may NEVER be put in the test census.* It is stated in \`CONTRIBUTING.md\` and repeated in \`tests/lint/testRatchet.test.js\` — **both as comments that check nothing.**

**MEASURED BY THE CHAIR JUST NOW: 10 of the 40 entries in \`scripts/.test-ratchet-baseline.json\` are walker-shaped**, across seven files: \`guidanceRegistry.walker\`, \`spatialLedgerCoverage.walker\`, \`ruinFilterRoster.walker\`, \`sovereigntyLightingContract.walker\`, \`warCostKindPools.walker\` (×3), \`warRulingKindPools.walker\`, and \`mechanismLitCoverage.test.js\` (×2).

⭐⭐ **THE HARD PART, AND THE PLACE THIS LANE WILL FAIL IF IT IS CARELESS: HOW DO YOU IDENTIFY AN ENFORCEMENT WALKER BY MACHINE?**
A \`.walker.\` filename check is the obvious move and it is **WRONG — it misses \`mechanismLitCoverage.test.js\`, which is two of the ten and carries no \`.walker.\` in its name.** A guard that catches 8 of 10 and reports success is worse than no guard, because it converts an open problem into a solved-looking one — the exact failure the hazard census named.
**DERIVE the identification instead of pattern-matching it.** Options worth weighing, and you may find better: a declared marker the walker itself carries (a header tag or an exported const), membership in an existing registry the estate already maintains, or a structural property (it scans the tree / asserts over a frozen inventory / reads a \`.baseline.json\`). **Whatever you choose, prove it classifies all ten correctly AND does not sweep in ordinary tests** — report both the false-negative and false-positive check, by execution.

**THEN TRIAGE THE TEN, ONE AT A TIME, ON THEIR MERITS.** ⚠ Do NOT assume all ten are the same defect. A row whose walker asserts a frozen INVENTORY is a disabled guard and must come out. A row that happens to live in a walker file but asserts ordinary content may be legitimate debt. **Say which each is and why.** For each that comes out, the debt must go somewhere it stays shrink-only and where a NEW violation still reds — the pattern \`af8815e9\` established (re-freeze the walker's own inventory; quarantine re-admissions audited by exact identity both directions). ⛔ Do NOT burn down the underlying debt in this lane; inventory it.
⚠ If a walker has no inventory mechanism of its own, that is a STOP-and-report, not a reason to leave the row in the census.

**ACCEPTANCE:** after the change, (a) adding ANY enforcement-walker row to the census must RED — plant one and prove it; (b) a newly planted violation of each freed walker must RED; (c) the pre-existing debt must stay green. Prove all three by execution, plants restored \`cmp\`-clean.

Append a ledger row at the LIVE tail of \`docs/FABLE_VALIDATION_QUEUE.md\`, re-derived immediately before applying. Commit body carries \`⏳ OPUS-ERA — FABLE SURVEY OWED\`.

RETURN JSON.`, { label: 'law:machinery', phase: 'Law', model: 'opus', effort: 'high', schema: { type: 'object', required: ['landed'], properties: { landed: { type: 'boolean' }, identificationMethod: { type: 'string' }, whyNotFilename: { type: 'string' }, classifiesAllTen: { type: 'string' }, falsePositiveCheck: { type: 'string' }, triage: { type: 'array', items: { type: 'string' } }, rowsRemoved: { type: 'number' }, rowsKeptAsLegitimate: { type: 'array', items: { type: 'string' } }, addingWalkerRowReds: { type: 'string' }, newViolationReds: { type: 'string' }, debtStillGreen: { type: 'string' }, commits: { type: 'array', items: { type: 'string' } }, stopReports: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Verify')
const verdict = await agent(LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER. Default posture: WRONG until executed evidence. REFUTE. READ-ONLY in the shared tree; mutants in throwaway archives. You hold the gate slot alone.

Lane reported: ` + JSON.stringify(law || {}).slice(0, 9000) + `

**REFUTE THE IDENTIFIER FIRST — it is the load-bearing part and the easiest to get subtly wrong.**
1. **Find an enforcement walker the identifier MISSES.** Write your own probe test that is unmistakably an enforcement walker (scans the tree, asserts a frozen inventory) but avoids whatever signal the lane chose, then confirm whether the guard catches it. **A miss is a REJECT** — a guard that catches most is the "solved-looking" failure this whole line of work exists to prevent.
2. **Find an ordinary test it WRONGLY sweeps in.** A false positive that blocks legitimate debt from being recorded is also a defect.
3. **Plant an enforcement-walker row into the census yourself** and confirm it REDS.
4. **For each freed walker, plant a fresh violation** and confirm it REDS — your own plants, in files the lane did not use.
5. **Confirm the underlying debt was inventoried, not burned down** — compare counts at HEAD's parent and HEAD with a detector you rebuild from the walker's own source, not from the lane's report.
6. **Confirm no baseline grew** without a stated verified cause, and that removals are removals only.
7. Run \`npm run check\` stepwise, status captured immediately; report the first failing step by name. ⚠ Expect step 16 \`verify:dist\` to remain red on the three inherited first-paint budgets — **REJECT if this round made any of them worse.**

RETURN structured JSON only.`, { label: 'law:verify', phase: 'Verify', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, identifierMissedAWalker: { type: 'string' }, falsePositiveFound: { type: 'string' }, censusRowPlantReds: { type: 'string' }, ownPlantsRed: { type: 'string' }, debtUnchanged: { type: 'boolean' }, checkFirstFailingStep: { type: 'string' }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

return { law, verdict }