export const meta = {
  name: 'build-cycle-27c-parallelogram-band',
  description: 'THE FINAL BAND: full-parallelogram goose cells, z-order ascending INTO Realm, thinner shaft · plate + seal · AA tail',
  phases: [{ title: 'Build', detail: 'one nav lane, four pieces, implement + verify with eye pass' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files (nav/*, theme.js, nav tests, App.jsx header block); no stash; focused vitest + gate-tail (ps aux | grep vitest first); python byte-scan every Write; npm run build + smoke:boot green; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. SURVEY FIRST: three prior nav lanes were stopped mid-flight — read git log + status on nav files; landed/uncommitted badge work is progress to reuse; band/sweep WIP is SUPERSEDED here. STANDING LAWS: hang is PAINT never layout; ANCHOR derives; focus rides an aria-hidden vane; screenshot BEFORE pinning geometry; ground-move = full AA re-census on every rider incl. AccountMenu's transparent chip; pin the COMPOSITED ground never the cylinder alone.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: OWNER-DIRECTED implementation, final corrected spec (the owner corrected the shingle DIRECTION and simplified the cell geometry — this supersedes all prior band briefs). ${P10}
THE COMPOSITION, FINAL: a THIN wooden shaft bar; ONE CONTINUOUS BAND riding it, roughly twice the bar's height, top-aligned, hanging ~40-50% below the bar's bottom edge (PAINT only); inside, Create/Library/Realm are three FULL PARALLELOGRAMS — simple slanted quads, each COMPLETELY FILLED with the goose vane treatment edge to edge (barb comb aligned to the slant with the deterministic jitter, rachis line, pale leading edge, sheen bands; NO complex feather silhouettes, NO curved tips — the owner wants the clean mockup geometry); the cells SHINGLE with the Z-ORDER ASCENDING INTO REALM: Library's leading edge laps OVER Create's trailing edge, Realm's laps OVER Library's — Realm sits topmost, Create bottommost, so each tab reads as FEEDING INTO the next (Create → Library → Realm); a soft contact shadow where each upper cell laps the one beneath; NO drawn seams, NO wood gaps inside the band; the red-brown wraps bracket the band's two ends; after Realm the band's final trailing edge resolves cleanly onto the bar.
LANE PB — FOUR PIECES, one commit each:
(1) THE BAND as above; labels keep their CURRENT font size, each centered in its cell's calm zone, AA vs the cell's lightest band; active grammar (brightened cell + gold underline); focus ring proven on a lapped cell.
(2) THE THINNER SHAFT: CHROME.headerDesktop 48 → the thinnest that seats wordmark/reference tabs/Sign In at CURRENT font sizes (target ~36-40; measure; PIN the font sizes); ANCHOR re-derives; the band's height independent (hangs deeper as the bar thins); mobile follows thinner, no band; full ground-move AA re-census.
(3) THE MAKER'S PLATE + WAX-SEAL WORDMARK to task #78's spec verbatim (reuse sound partial work from stopped lanes); plate scales to the thinner bar; a half-step quieter than the band.
(4) THE COMPOSITED-AA PIN (grain-over-cylinder worst case; the one-token grain mutant must red).
Screenshot desktop 1440x900 + mobile + close crops of the laps and plate each iteration, honest eye verdicts (acceptance: does it read as the owner's mockup rendered in wood and goose feather, with the stack visibly ascending INTO Realm?). Return {lane:'PB', commits, gates, stopReports, notes}.`,
  {label:'pb:parallelogram-band', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier with an EYE PASS. ${P10}
Report: ${JSON.stringify(r)}. Serve, screenshot, JUDGE: (a) three FULL parallelograms, completely filled, no silhouette cuts (assert the quad geometry); (b) THE Z-ORDER: Realm topmost, Create bottommost — Library laps OVER Create, Realm OVER Library (inspect stacking + lap shadows; cover the labels: does the stack ascend INTO Realm?); (c) zero seams/gaps in the band (rendered pixels); (d) hang proportion + paint-only (hide-and-diff); (e) bar thinner, fonts UNCHANGED (measure, quote px); (f) plate subordinate, seal cold-reads, wax matte, grayscale-binds; (g) photo-real verdict plainly. Mechanical: ANCHOR derivation; composited-AA pin + mutant run by YOU; AA from real rendered pixels on every rider (quote); focus by Tab on plate and a lapped cell; two-load byte determinism; Rule-of-Tincture mutant; typed contexts plain; mobile; build + smoke:boot; pathspec-clean. Return {lane:'PB verify', verdict, findings}.`,
  {label:'pb:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }