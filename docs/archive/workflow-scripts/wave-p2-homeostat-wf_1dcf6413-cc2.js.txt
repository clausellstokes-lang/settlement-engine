export const meta = {
  name: 'wave-p2-homeostat',
  description: 'P2 — THE HOMEOSTAT: push-pull migration with destination competition (existing settlements outcompete new foundings)',
  phases: [{ title: 'Build', detail: 'one Opus implementer', model: 'opus' }],
}
const ROOT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const SCHEMA = { type: 'object', required: ['agent', 'fixes'], properties: { agent: { type: 'string' }, fixes: { type: 'array', items: { type: 'object', required: ['finding', 'status', 'files', 'tests', 'notes'], properties: { finding: { type: 'string' }, status: { type: 'string', enum: ['fixed', 'skipped', 'deferred', 'blocked'] }, files: { type: 'array', items: { type: 'string' } }, tests: { type: 'string' }, notes: { type: 'string' } } } }, behavior_shifts: { type: 'array', items: { type: 'string' } }, discovered: { type: 'array', items: { type: 'string' } } } }

const BRIEF = [
  'You are an Opus IMPLEMENTER for SettlementForge. SLICE P2 of the DEMOGRAPHIC ENGINE: THE HOMEOSTAT. BINDING LAW: docs/DESIGN_DEMOGRAPHIC_ENGINE.md - section 0 (the grounded defect anatomy), section 2b (THREE READINGS: foodFlowRatio, reserveCoverage, urbanLoadRatio - never collapsed into one binding-cause enum), section 4 (push-pull), and section 11 P2 (migration precedes response competition, deliberately: you cannot choose between filling a half-empty village and founding a steading until destinations compete).',
  'Worktree: ' + ROOT + ' branch claude/composite-r4 @ 43903062 - verify with git rev-parse --abbrev-ref HEAD; stop on mismatch. FOREIGN, never touch: wizardNews/settlementRumors/heraldRouting/informationStatecraft/momentum/supplyWebWarfare + their tests, and artifacts/soak/**. Focused vitest only, one at a time.',
  '',
  'WHAT LANDED ALREADY (build ON these, never fork them): P1 @ d620a05d gives demographicsKernel + demographicsRates (K_food from FLOW ONLY - reserves are deliberately excluded from K and that exclusion is pinned; D_tier; the rates; pressure). P1a @ 43903062 gives the unfrozen decline lane, the named-cast floor on decline, the viability ladder read (tierViabilityOf), and conserved promotion. J4 gives the LIVED route network (routeNetworkConsumers - adjacency, hop costs, danger) which is how people must travel. H3 gives npcCirculation for NAMED souls - your lane moves the NUMBER; never duplicate the named-soul path.',
  '',
  'IMPLEMENT (P2 ONLY - no satellite founding, no promotion responses, no plans; P3 owns those):',
  '1. THE PUSH SCORE per settlement, from the owner four drivers PLUS food, each banded and each with an applicability guard for "where appropriate": food deficit (foodFlowRatio), low prosperity, low defense, low internal security. A garrison town does not shed its soldiers over prosperity - guards like that are the point of the amendment.',
  '2. THE PULL SCORE, evaluated ONLY over settlements reachable on the LIVED route network (J4 adjacency), distance-discounted by the hop costs people actually pay, and reading the THREE readings separately: a destination with food slack pulls the starving; a destination with urban slack pulls the crowded; a destination with both pulls hardest. Safety and prosperity weight it.',
  '3. DESTINATION COMPETITION (the review amendment, and the reason this slice precedes P3): the emigration flow resolves against RANKED existing destinations. Spare capacity in a reachable viable settlement is consumed BEFORE any new founding could be justified - P3 will ask this lane first and only found when it comes back empty or unreachable.',
  '4. ARRIVALS AND TRANSIT (law 4, conservation): emigrants ARRIVE. Use the EXISTING migration ledger (J2 already counts population-class flows - the desire-path accounting was built for exactly this); columns travel at their own speed and land. Realm total changes only by births, deaths, and world edges. Refugee vs voluntary classes distinguished (a refugee flees regardless of destination quality; a voluntary migrant is choosy).',
  '5. Extend the demographicsEnabled certification row (extend, never re-author) with the migration channels and invariants.',
  '',
  'PINS with executed negative controls (revert/red/restore/green, quoted): CONSERVATION (a departure is an arrival or an in-transit column - realm total exact across 300 ticks, and the negative control shows a leak when arrivals are dropped); the BIFURCATION CURE (a crowded-and-fed settlement beside a viable underpopulated one measurably levels BOTH toward the middle - this is the wave soul, make it decisive); reachability (an unconnected settlement neither sends nor receives - people are not teleported); distance discount (a far destination pulls measurably less than a near one, same quality); the applicability guards (the garrison case); refugee vs voluntary divergence; the three readings drive DIFFERENT destination choices (food-poor migrant and space-poor migrant pick differently from the same menu - the anti-min() proof); H3 named souls unaffected; dormancy byte-identical by object identity.',
  '',
  'HARD RULES: no migrations; no git mutations; no full suites/builds; NO ratchet raises; ZERO any-casts (declare conditional keys on the OWNING typedef - the RouteBypass precedent); kernel math primitives; determinism (ONE fork keyed per settlement per tick, draw-accounted, or the hash idiom P1a used; seed-family totality, never single-seed); no em dashes in src/domain string literals; conditional worldState keys drop-when-empty; the golden law - dark byte-identical, no generation or dormancy golden moves; reseal edge bundles if you touch their inputs. Return dense structured data: statuses, files, verbatim test tails, negative-control quotes, behavior shifts plainly, judgment calls vetoable, discovered.',
].join('\n')

phase('Build')
const r = await agent(BRIEF, { label: 'P2:homeostat', phase: 'Build', schema: SCHEMA, model: 'opus' })
return { results: [r].filter(Boolean) }