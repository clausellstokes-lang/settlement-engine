export const meta = {
  name: 'batch5-h4-i2-j4',
  description: 'H4 (Wanderers tab + DM verbs), I2 (reliability stamps + fidelity), J4 (consumers: war, interdiction, roamer physics)',
  phases: [{ title: 'Build', detail: '3 Opus implementers, disjoint ownership', model: 'opus' }],
}
const ROOT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const SCHEMA = { type: 'object', required: ['agent', 'fixes'], properties: { agent: { type: 'string' }, fixes: { type: 'array', items: { type: 'object', required: ['finding', 'status', 'files', 'tests', 'notes'], properties: { finding: { type: 'string' }, status: { type: 'string', enum: ['fixed', 'skipped', 'deferred', 'blocked'] }, files: { type: 'array', items: { type: 'string' } }, tests: { type: 'string' }, notes: { type: 'string' } } } }, behavior_shifts: { type: 'array', items: { type: 'string' } }, discovered: { type: 'array', items: { type: 'string' } } } }

const SHARED = [
  'Worktree: ' + ROOT + ' branch claude/composite-r4 (HEAD past 4728af34) - verify; stop on mismatch. CONCURRENT: two sibling agents (ownership below, never touch theirs) and the owner wizard-news session (wizardNews/settlementRumors/heraldRouting/informationStatecraft/momentum/supplyWebWarfare + tests - FOREIGN). A soak is live: never touch artifacts/soak/**; focused vitest only, one at a time; a timeout-shaped red gets ONE isolation re-run.',
  '',
  'THE GOLDEN LAW: your slice must NOT shift a golden. Everything ships DARK behind its existing virtual flag (routeLifecycleEnabled / npcConsequencesEnabled / informationBrokeragesEnabled-or-the-statecraft gate). If a change would move a golden, STOP and report rather than re-recording.',
  'FIRST-PAINT: closure is 1,020,590 of 1,040,000 (owner-gated). Never let an eager module (store, src/domain/events/mutate.js handler table, eager components) statically import a heavy derivation - extract the function to a zero-import leaf (the userRouteIdentity precedent).',
  'PRE-GATE HYGIENE (learned the hard way, twice): if you touch anything the edge bundles bake in, run npm run build:edge-shared before declaring done; if you touch a store action, run operationRegistry + gen:compendium-data + generate-sitemap + build:edge-shared IN THAT ORDER.',
  'CROSS-SLICE TYPES: batch 4 cost a reconciliation because three slices spelled shared shapes differently. Before declaring a world/settlement/ledger param type, grep the siblings for an existing spelling and CONVERGE on it. Declaring a conditional key belongs on the OWNING typedef (the RouteBypass-into-routeNetworkLedger precedent), never at the call site.',
  '',
  'HARD RULES: no migrations; no git mutations; no full suites/builds; NO ratchet/baseline raises (adopt src/kernel/math.js primitives; ZERO any-casts in new domain files; the voice ratchet counts em dashes in src/domain|src/data STRING LITERALS; the loading-narration ratchet is tolerance-0 so every Suspense fallback NARRATES; JSX 600 / js 800); determinism laws (seeded forks; house instruments: lowDiscrepancy Weyl sampler, draw accounting, SEED-FAMILY totality - a single-seed restriction pin is VACUOUS, structural purity scans); headless domain; anchored negatives; JSON-round-trip persistence pins; conditional worldState keys drop-when-empty; census/walker interactions are REGISTRATIONS with written reasons, never widenings; extend your existing certification row rather than minting rule keys. Return dense structured data: statuses, files, verbatim test tails, negative-control quotes (revert/red/restore/green per new pin), behavior shifts plainly, judgment calls vetoable, discovered.',
].join('\n')

phase('Build')
const results = await parallel([
  () => agent([
    'You are an Opus IMPLEMENTER for SettlementForge. SLICE H4 of the NPC-consequences program: SURFACES + DM VERBS - the slice that makes the whole consequence economy visible.',
    'BINDING LAW: docs/DESIGN_NPC_CONSEQUENCES.md section 7 (DM VERBS, command-spine discipline, each with receipts and snapshot undo: ASSIGN a roamer to a settlement - sovereign, may override exclusions explicitly with the override NAMED in the receipt, address-chain news "X takes up residence in Y"; KILL - the only death in the system, DM-explicit, receipt-carried, undoable; PARDON / LIFT EXCLUSION / RELEASE - the mercy verbs, same discipline) and section 8 (SURFACES: the WANDERERS tab beside Gazetteer and Ruins and Remembrance - the roaming pool as an in-world register showing identity, notoriety band, origin story pointer, time wandering, AUDIENCE-PROJECTED per Law 7 so the DM sees dmTruth and players never do; every transition is address-chain news with typed causes; the unaffiliates section per section 6c is the LOCAL projection of the same ledger - one truth, two views).',
    '',
    'YOUR OWNERSHIP: src/components/map/HeraldWanderers.jsx + its register model (mirror the wave-C heraldRegister.js/HeraldGazetteer.jsx/HeraldRemembrance.jsx idiom EXACTLY - read them first), the unaffiliates section in the settlement dossier, the three DM verbs as store actions, tests for all of it. READ-ONLY: the H1/H2/H3 domain leaves (you CALL them; do not edit them - if a leaf needs a new pure verb, add it there and say so).',
    '',
    'IMPLEMENT: the Wanderers tab as a lazy leaf with its build pin carrying the SECOND assertion (the Herald parent is itself lazy); presence gated like sibling Herald tabs (ABSENT when dark, not disabled - the ai_notes lesson); the unaffiliates dossier section; the three verbs through the store with operationRegistry rows and the FULL regen chain in order; audience projection on every surface (anchored negative: a player projection carries ZERO dmTruth keys); in-world prose per the legibility law (no raw bands, no ids on screen).',
    'PINS with executed negative controls: tab presence gating; lazy pin with second assertion; ASSIGN honours and NAMES an exclusion override; KILL is undoable and is the ONLY death path (anchored negative: no engine path kills); PARDON lifts and emits; player projection carries no dmTruth; empty states read as in-world prose (a realm with no wanderers says so).',
  ].join('\n') + '\n\n' + SHARED, { label: 'H4:wanderers-verbs', phase: 'Build', schema: SCHEMA, model: 'opus' }),

  () => agent([
    'You are an Opus IMPLEMENTER for SettlementForge. SLICE I2 of the information-brokerage program: RELIABILITY STAMPS + FIDELITY.',
    'BINDING LAW: docs/DESIGN_INFORMATION_BROKERAGES.md section 5 (Challenge A, ratified: CALIBRATION IS PRIMARY - reliability stamps from the closed ladder confirmed|corroborated|reported|tavern_talk, derived from the item ACTUAL provenance quality (carrier count, distance, credibility stocks, channel competence), surfaced in-world ("confirmed by three roads"); stamps exist ONLY in brokerage settlements - elsewhere the news reads as today, because the baseline uncertainty is unlabelled BY DESIGN; the accuracy bump is SECONDARY and ceiling-bounded per Law 1, entering the belief-derivation fidelity term) and section 10 (THE CALIBRATION-HONESTY ENVELOPE - the program crown jewel: over a seeded corpus, items stamped confirmed are true at or above the confirmed band and tavern_talk at its band, a proper scoring rule executed against the ground truth the sim already knows; plus the DIVERGENCE ENVELOPE: brokerage settlements show measurably lower belief-truth divergence than matched non-brokerage settlements).',
    '',
    'YOUR OWNERSHIP: src/domain/worldPulse/brokerageStamps*.js and brokerageFidelity*.js (new leaves), the stamp surfacing in the Herald news composition seam (find it; extend, never fork), your informationBrokerages certification row, tests/domain/brokerage*.test.js. READ-ONLY: the belief map and distance-priced news derivation (you EXTEND the fidelity term, never rewrite the derivation), the I1 catalog and tuning table, informationStatecraft.js (FOREIGN this session - read only, never edit).',
    '',
    'IMPLEMENT: the reliability ladder as a pure derivation from real provenance (NEVER a random or a constant - the stamp must be earned); the brokerage-presence gate so stamps appear only where a house stands; the ceiling-bounded fidelity term feeding the existing belief derivation (Law 1: distance is never abolished); the in-world stamp vocabulary (prose, never numbers - legibility law).',
    'PINS with executed negative controls: THE CALIBRATION-HONESTY ENVELOPE (the crown jewel - stamps must be true at approximately their band rate over a seeded corpus, executed against ground truth; make it non-vacuous by showing a mis-calibrated mutant fails it); the divergence envelope (brokerage vs matched non-brokerage); stamps absent outside brokerage settlements; the accuracy ceiling holds (a brokerage never abolishes distance); dormancy byte-identical.',
  ].join('\n') + '\n\n' + SHARED, { label: 'I2:stamps-fidelity', phase: 'Build', schema: SCHEMA, model: 'opus' }),

  () => agent([
    'You are an Opus IMPLEMENTER for SettlementForge. SLICE J4 of the route-lifecycle program: THE CONSUMERS.',
    'BINDING LAW: docs/DESIGN_ROUTE_LIFECYCLE.md section 14 J4 (roamer physics hookup with W-H H3, garrison asymmetry with the war layer, interdiction integration, cartography arterial seeds, the reputation-race pin) plus section 9 (TRAVEL PHYSICS CONSUMERS: armies use the existing armyTransit; roamers move one hop per tick, connected-only, mid-route records in the transit ledger pattern; wanderers and smugglers may use hidden paths at a grade penalty, armies may NOT) and J-D9 (l) (MILITARY: the war layer writes strategicNeed into the same ledger; a militarily critical route RESISTS DECAY while strategic need persists even after trade dies - the garrison-road asymmetry).',
    '',
    'YOUR OWNERSHIP: src/domain/worldPulse/routeNetworkConsumers*.js (new leaves) and the wiring seams into the pulse for the ROUTE program only, your routeLifecycleEnabled certification row, tests/domain/routeNetworkConsumers*.test.js. READ-ONLY: H3 roamer transit leaves (you CONSUME their published seam - coordinate by design-doc convention, do not edit H files), supplyWebWarfare (FOREIGN this session - read only), the war deployment/mobilization state, the cartography design doc (the arterial seed is a DECLARED contract for a future TC slice, not a build here).',
    '',
    'IMPLEMENT: the war-layer strategicNeed write into the route ledger and the garrison floor honoured end-to-end; interdiction reading the same flow ledger the charter events write (the design says cutting a route severs a NAMED material artery - make the interdiction read denominated in goods); the roamer travel-physics hookup consuming H3 published seam (one hop, connected-only, mid-route, hidden-path allowance for wanderers and the refusal for armies); THE REPUTATION RACE as an integration property pin (person-speed vs news-speed: whether the wanderer or the story arrives first is a deterministic function of route grade, distance, and listeners at the gate - this is the design signature property, so make the pin genuinely decisive with both outcomes demonstrated on the same fixture by varying only distance/grade).',
    'PINS with executed negative controls: garrison asymmetry (trade dies, strategic need holds the road at road grade); interdiction names goods; roamer one-hop and connected-only; armies refused hidden paths while wanderers are allowed; THE REPUTATION RACE both ways on one fixture; dormancy byte-identical.',
  ].join('\n') + '\n\n' + SHARED, { label: 'J4:consumers', phase: 'Build', schema: SCHEMA, model: 'opus' }),
])

const all = results.filter(Boolean)
log(all.length + '/3 batch-5 slices returned')
return { results: all }