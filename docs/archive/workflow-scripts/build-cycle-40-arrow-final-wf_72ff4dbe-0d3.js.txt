export const meta = {
  name: 'build-cycle-40-arrow-final',
  description: 'The arrow\'s final lane: growth lines + pore flecks, thread-true whipping, and the owner-approved counsel pass (R1-R8)',
  phases: [{ title: 'Build', detail: 'one nav lane, implement + verify with eye pass' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits with THE STAGED-SET LAW; no stash; focused vitest + gate-tail (ps aux first); python byte-scan every Write; npm run build + smoke:boot; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. THE SPEC: docs/DESIGN_RIBBON_V4_SPEC.md (whole, incl. the audit addendum) + task #105's description AND metadata (the seal size ladder). Landed so far: V4 piece 1 @ 898d1c0b (cedar, gilded wordmark, bole hugging ink±3px post-repair, gilded seal-o), V4B @ 334bef48+23e89b18 (mirror, four parallels, quill-line indicator at 2.5px, splits, fray, one-curve law; wrap-corner settled + pinned). CONCURRENCY: WZ-4 live on worldPulse — fenced.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: OWNER-DIRECTED implementation, the arrow's final lane. ${P10}
LANE V4C — serial commits:
(1) THE WOOD FINISHES (spec part 2 §2): growth lines (seed 11, baseFrequency 0.004 0.55, matrix-thresholded to 2-3 wavering streaks per 320px tile, RIM-tone wash ≤9% alpha) + pore flecking retina-only (seed 13, baseFrequency 0.35 0.08, ~4% coverage at 6% alpha, elongated along the shaft); darkening-direction only (pale streaks banned — the dead-band law); sRGB declared; seeds from the vetted allowlist.
(2) THE WHIPPING BECOMES THREAD (spec part 2 §3): ShaftWrap's TURNS → the 2.6px-period 90° repeating gradient (0.4px WRAP_EDGE inter-turn shadow, 1.2px WRAP body, 1.0px WRAP_GLOSS crest on the LEFT of each turn), the barrel gradient multiplying over; one authored diagonal tie-off per wrap at the band's lean, hash-jittered ±2px, 1.5px clipped nub; R5's law rides here: pin the RENDERED crest tone (multiply crushed it in the counsel's render — measure the composited pixel, not the authored hex) so the whipping EXISTS at 1x.
(3) THE COUNSEL PASS, remaining items (task #105 verbatim; R1's plaque test part-discharged by 898d1c0b's ink±3px repair — verify then finish; R3 part-answered — the 2.5px quill line reads as metal at 200%, so keep it unless YOUR 100% glance says progress-bar, then 3px+keyline): R1 bole-as-scorch completion (bleed off the bar, anisotropic displacement along grain, singe halo, AA core untouched); R2 the seal kerning check + THE SIZE LADDER from the metadata (dimple at ribbon; full device impression ≥28px — build the ≥28px rendering path if absent; favicon untouched); R4 the whisper RACHIS hairline on the band's LOWER CUT only; R6 the texture-complete law recorded in theme.js + the coverage ratchet (retina cues each <2% effective ink, enforced); R7 THE NOCK build-and-show (12-20px horn cross-nock, paint-only, WITH/WITHOUT screenshots, one-commit revert); R8 the identity-extension law recorded.
Screenshots 100%/200% every commit with honest eye verdicts; full ground-move AA re-census wherever a tone moved. Return {lane:'V4C', commits, gates, stopReports, notes}.`,
  {label:'v4c:arrow-final', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier with an EYE PASS. Read the spec + task #105 whole. ${P10}
Report: ${JSON.stringify(r)}. JUDGE at 100%/200%: (a) the wood reads as REAL cedar now (growth variation present at 1x as tone, pores at 2x); (b) the whipping reads as WOUND THREAD and EXISTS at 1x (measure the rendered crest contrast); (c) the bole reads SCORCH not plaque (the glance test, on the finished treatment); (d) the seal's three registers (dimple / device impression ≥28px / favicon) — render all three; (e) the R4 hairline saves the silhouette on the dark hero (find-the-nav test); (f) the nock WITH/WITHOUT screenshots delivered; (g) THE WHOLE ARROW, finally: at arm's length, one war arrow — shaft, whipping, feathers, gilded name — or not? Say it plainly. MECHANICAL: the coverage ratchet mutant (a retina cue crossing 2% → red); every rider's composited AA re-censused (quote); seeds vetted; two-load determinism; focus rings; ANCHOR + fonts; build + smoke:boot; staged-set + pathspec vs WZ-4. Return {lane:'V4C verify', verdict, findings}.`,
  {label:'v4c:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }