export const meta = {
  name: 'ontology-accuracy-repair',
  description: 'One revision round closing the completeness critic findings on the ontology document — the live/dark mislabels, the loops that do not say where they stop, and the missing generosity step',
  phases: [{ title: 'Repair', detail: 'status vocabulary, the mislabels, loop termination, the aid loop' }],
}

const DOC = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c44e5d99-2ba5-49d5-a554-40b68534c8eb/scratchpad/THE_FULL_ONTOLOGY.md'
const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

phase('Repair')
const fixed = await agent(`⛔ STRICTLY READ-ONLY against the repo at ${TREE} (branch claude/composite-r4, HEAD cbd348a5, tree clean — do not edit, commit, or run any test/npm/vitest command there; the build stream is deliberately paused). You may READ and grep. You may edit ONLY ${DOC}.

YOU ARE REPAIRING THE ONTOLOGY DOCUMENT at ${DOC} (1,745 lines, 192 systems). It is an owner-facing overview written in plain language for the product's builder. A completeness critic verified it against the live tree and found ONE CLASS OF ERROR THAT MATTERS MORE THAN ALL THE REST: **it mislabels what is actually live**. A document that tells the owner a system is dormant when a DM sees it every week is worse than no document. Fix that class first and completely.

**REPAIR 1 — THE STATUS VOCABULARY (this fixes a whole class at once, do it first).** The status rungs have no value for the estate's single most common real state: "runs only in the three world-alive presets". The document currently collapses that into "LIVE IN SOME WORLD-LAW SETTINGS" in some entries and miscalls it "LIVE by default" in others (§55 and §151 are named instances). ADD THE RUNG, name it plainly, then SWEEP EVERY SYSTEM ENTRY and re-label against it. Verify each re-label against the live preset definitions — do not re-label from inference.

**REPAIR 2 — THE FACTUAL ERROR ABOUT POPULATION.** §35 says "Today a settlement's headcount is set at generation from its tier band and then essentially held." MEASURED FALSE: a population mover runs by default in an ordinary campaign and both grows and shrinks settlements every week (its own receipts read "Organic growth from favorable conditions" and "Population loss from cumulative settlement pressure"). Correct §35, and reconcile it with §137 and with Loop 1 step 9, which were right while §35 was wrong.

**REPAIR 3 — THE PRESET STORY, which the reader will otherwise get backwards.** (a) "the two most active presets" is used for TWO DIFFERENT PAIRS — Dramatic Campaign + Full Simulation for the town's decision-maker and supply-web warfare, but it must mean Living Realm + Full Simulation for the rumour network, because Dramatic Campaign carries instant omniscient news. Name the pair explicitly at every occurrence; never use the bare phrase. (b) §9/§17 present "seven presets" as the DM's menu while §178 says "four honest presets in a grid". SEVEN preset objects exist but THREE are legacy (Quiet Local, Realistic Regional, Dramatic Campaign) — they resolve for old saves and are applied by toolbar chips, while the world-laws dialog surfaces only four. Reconcile this explicitly, and say plainly that the preset which lights the war layer is one of the legacy three, because a reader will otherwise conclude the war layer is a card in the dialog.

**REPAIR 4 — THE LOOPS MUST SAY WHERE THEY STOP.** Every loop that depends on a preset or switch must name where it terminates in each reachable configuration. Specifically: Loop 4 (information) runs steps 2-7 only under Living Realm and Full Simulation — in Dramatic Campaign it collapses to step 1 then step 8 reading a picture that is always true; its step 5 (distance-priced staleness) is switch-gated and must be marked like every other conditional step. Loop 2 cannot reach steps 13-14 in Living Realm, which carries the peace engine but deliberately starts no wars. Loop 5 (corruption) genuinely ENDS at step 8 in every reachable configuration — its step 11 exit rides the same unlit switch as steps 7/9/10, so stop presenting an ending a DM cannot reach. Loop 9 (growth) is headed "largely unlit" but its most vivid step — founding a satellite — is LIVE in the three world-alive presets, triggered by boom, prosperity, population pressure and unabsorbable migration; re-head it and correct step 4. Loop 12 step 10 defers permanent population loss to an unbuilt system, but a live kernel already kills a bounded tier-scaled share and demotes the tier through the ordinary path in the two presets carrying calamities.

**REPAIR 5 — THE MISSING HUMAN STEP.** No loop anywhere names aid or generosity. The hunger loop walks a famine all the way to a neighbour's opportunism WITHOUT passing through the live constructive-flows layer — the system that decides whether an ally RELIEVES that famine, which the document names as a neighbour eight separate times. Add that fork to Loop 1 where it belongs, and add a generosity/aid loop of its own if the machinery supports one. Verify against the live code what the constructive-flows layer actually does before writing it.

**REPAIR 6 — the smaller ones.** §40's foreign-planting corruption is lit by NO preset and its own catalogue marks it dormant — move it to the dark inventory ("engine-side and campaign-gated" wrongly reads as reachable by configuration). §162 omits Traditions (a real view, per §122-123), the steadings/unaffiliated sections, and market prices. §17's "about ninety-four structural census tests" is not reproducible — 65 test files carry "walker" in the name and 581 read the source tree; re-measure and give the figure its own stated definition, or cut it. Strip the leaked toolchain jargon the critic named ("the rules blob", "a versioned blob", "the determinism lint layer", "per-layer lint rules") and any other file paths, symbol names, or wave labels that leaked into the body.

**KEEP UNCHANGED — the critic verified these correct:** the closing counts (192 systems, 13 loops, 16 promises, 12 boundaries — re-verify after your edits and restate), the 13 engine-gated switches and their list, 165 registered operations, the 38-entry product flag registry, 13 faction archetypes, 16 grounds for war each mirrored by a ground for peace, the nine-phase kernel order, the espionage double-block.

METHOD: verify every re-label against the live tree before writing it — the whole point of this pass is that the document's claims about liveness must be true. Re-count the systems and loops after editing and restate the totals. Byte-scan (python3, zero NULs) at the end.
RETURN: per-repair disposition, the new status-vocabulary rungs, how many system entries were re-labelled, the final counts, line count.`, { label: 'repair:ontology', phase: 'Repair', model: 'opus', effort: 'high' })

return { fixed, path: DOC }