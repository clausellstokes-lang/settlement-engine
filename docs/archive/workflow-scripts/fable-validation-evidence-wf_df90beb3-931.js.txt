export const meta = {
  name: 'fable-validation-evidence',
  description: 'Re-derive all 33 Opus-era judgments + dirty-tree adjudications for Fable rulings',
  phases: [{ title: 'Evidence', detail: '13 read-only evidence agents over minifold' }],
}

const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const PREAMBLE = `VERIFY-FIRST: You are an evidence agent for the SettlementForge Fable-validation program, authorized by the repo owner's standing continuity orders (see docs/START_HERE.md section 6b-2 on the ledger branch; the validation protocol is ${MF}/docs/FABLE_VALIDATION_QUEUE.md). Working tree: ${MF} (branch claude/composite-r4, HEAD b3417677). The tree is DIRTY BY DESIGN with two frozen uncommitted programs (war joins W1 + cartography TC-0/1/2) — this is expected; verify branch with git -C ${MF} branch --show-current and STOP if it is not claude/composite-r4.
HARD RULES: (1) READ-ONLY — never edit, write, stage, commit, stash, reset, clean, checkout, or re-record anything; never set UPDATE_GOLDEN; never run the full gate (npm run check). (2) Focused test runs ARE allowed: cd ${MF} && npx vitest run <files>. If a run fails timeout-shaped, re-run it in isolation once before believing it. (3) Code and executed output outrank every doc and every claim in this brief — if you find the brief's premise false, say so. (4) Label every finding CONFIRMED (you executed the proof) or PLAUSIBLE (reasoning only). (5) Your final output is structured data for the validation chair, not prose for a human. Quote file:line and real command output in evidence fields.`

const SCHEMA = {
  type: 'object',
  required: ['findings'],
  properties: {
    findings: {
      type: 'array',
      items: {
        type: 'object',
        required: ['item', 'verdict', 'rationale', 'evidence', 'confidence'],
        properties: {
          item: { type: 'string', description: 'queue row or survey item this finding addresses' },
          verdict: { type: 'string', enum: ['CONFIRM', 'ADJUST', 'ESCALATE', 'INFO'], description: 'recommendation to the validation chair' },
          rationale: { type: 'string' },
          evidence: { type: 'string', description: 'file:line quotes, commit hashes, executed command output' },
          adjustment: { type: 'string', description: 'if ADJUST: the precise correction to record' },
          confidence: { type: 'string', enum: ['CONFIRMED', 'PLAUSIBLE'] },
        },
      },
    },
    openQuestions: { type: 'array', items: { type: 'string' } },
  },
}

phase('Evidence')

const briefs = [
  { label: 'wave-p-anatomy', prompt: `${PREAMBLE}
TASK: Re-derive the GROUNDED DEFECT ANATOMY of the population runaway — the highest-consequence Opus-era finding. Claim under validation: the 300-year runaway (a town reaching 29 trillion on a smooth x1.07/yr curve) and the frozen floor (six settlements pinned ~200-500) are ONE defect — an uncapped proportional growth rate read through an integer deadband at src/domain/worldPulse/populationDynamics.js (Opus cited lines ~303-305 pre-cure), reproduced at pressure 0.68-0.70; four intuitive floor hypotheses were refuted including a named-cast floor with zero production callers.
DO: (1) Read docs/DESIGN_DEMOGRAPHIC_ENGINE.md section 0 fully. (2) Read the current populationDynamics.js and the wave-P cure code (P1 landed @ d620a05d — git show d620a05d --stat to find files). (3) Re-derive the mechanism yourself from the code: does the pre-cure math actually produce BOTH the runaway and the floor from one term? Check git show 99e2d54f~N history if needed to see pre-cure code (git log --oneline --follow on the file). (4) Execute the proof if cheap: run tests/domain/demographicsCure.test.js and demographicsKernel tests; read what the cure test actually measures (fixed points at 300y, y150==y300 within 0.5%, pre-cure control re-creating the runaway). (5) Verdict: is the one-defect anatomy CONFIRMED by your independent derivation, or does anything not add up?` },

  { label: 'wave-p-bands', prompt: `${PREAMBLE}
TASK: Audit the RAW-AUTHORED TUNING BANDS of the demographic engine — none has seen a soak. (1) Read docs/DESIGN_DEMOGRAPHIC_ENGINE.md section 10 (the bands) and the code that consumes each band (grep the constants into src/domain/worldPulse/). (2) For EACH band: state what it governs, whether it is structural (changing it breaks an invariant) or pure tuning (changing it only moves equilibria), and whether any band combination could misbehave at extremes (e.g. density damper vs migration push-pull interactions, curb coupling). (3) Also audit the P4 capability floor 0.20 (row: 'hunger is not a peace treaty: a starving realm can still raid, it cannot campaign' — absent capability defaults to 1 so pre-P4 callers are byte-identical): verify the default-1 dormancy claim in code, and give your judgment whether 0.20 is a sane floor pending soak evidence. (4) Flag any band whose value looks inconsistent with a sibling band or with an executed behavior you can cheaply test. This is pre-soak sanity review, not tuning ratification — the soaks are owner-held.` },

  { label: 'wave-p-claims', prompt: `${PREAMBLE}
TASK: Validate wave P's ACCEPTANCE MACHINERY. (1) Read docs/DESIGN_DEMOGRAPHIC_ENGINE.md: the FOURTEEN acceptance claims (adopted verbatim from an external design review, replacing a plateau criterion that would have failed a healthy realm) and every judgment block J-P1..J-P8 including the delegated village-seeding ruling. For each claim: is it testable as written against soak receipts, and is any claim vacuous or unfalsifiable? (2) P4 'indirect' certification row judgment: the row moved from 'unobserved' to 'indirect' to keep the reviewed override set at 5 against a ceiling of 5 — read src/domain/certification/subsystemRowsGrowth* and judge whether 'indirect' is honest for a channel whose v5 census reader exists but whose lit receipts do not. (3) P4 no-hidden-governor STRUCTURAL enforcement: demographicsRisk.js import list pinned to exactly two modules + realm-observation token scan absent + guard-the-guard arm on demographicsWar.js. VERIFY the scan cannot be satisfied vacuously: could an indirect import path (a helper module that itself reads realm totals) defeat the token scan? Read the actual test and try to construct the bypass on paper. (4) The soak-harness remnant amendment: the population>0 assertion excepts settlements by their own lifecycleDiedAtTick stamp — read tests/… realmScaleCertification and confirm the exception cannot mask a genuine zero-population bug in a NON-died settlement.` },

  { label: 'golden-attribution', prompt: `${PREAMBLE}
TASK: THE GOLDEN ADJUDICATION EVIDENCE — highest priority. On the current static dirty tree, three test files red (I already re-ran them; this is CONFIRMED): tests/property/beliefMapGolden.test.js drifts on exactly config 'bg-c|14|unreliable'; tests/property/momentumDormancyGolden.test.js drifts on exactly 'mo-b|8|one_month' and 'mo-c|6|one_week'; tests/lint/domainAnyCastBaseline.test.js reds because warDeployment.js debt SHRANK 54→53 (ratchet-down lock-in, not new debt). The dirty tree contains wave W1 (war joins: new files src/domain/worldPulse/{warIntent,treatyEnforcement,treatyTransfer}.js + edits to warDeployment/settlementStrategy/applyWorldPulse/peaceTerms/mobilization/occupation/warReasons/pulseKernel/armyTransit/spatialUsage) and cartography TC-0/1/2 (townMap/townScene/townCartography paths — all their tests green).
QUESTION: are the three golden drifts a LEGITIMATE behavior change from W1's lit-path work (strategy can now open wars via spatialLedgers.warIntents; treaties now move real grain), or a DORMANCY BREAK (drift in a config where the relevant layers are dark)?
DO: (1) Read both golden test files: determine EXACTLY which simulation rules are lit/dark in each drifting config (bg-c, mo-b, mo-c row definitions) — especially whether war/treaty/strategy paths are LIT in those configs while momentum/belief layers are dormant. (2) Create a throwaway detached worktree at the last clean commit: git -C ${MF} worktree add /Users/cstokes/Desktop/settlement-engine/.claude/worktrees/fable-base-triage 99e2d54f. That worktree resolves node_modules by walk-up to the main tree — verify with a trivial node -e require check; if resolution fails, note it and fall back to static analysis. (3) In BOTH trees, dump the projection object for ONLY the three drifting configs (write a small node script into /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/2e3ad88b-afe1-4b9a-98ca-b726d4aaa3d7/scratchpad/ — never into the repos — importing the same modules the tests import) and diff field-by-field. (4) Report EXACTLY which fields moved (grain stores? war state? news entries? ledgers?) and whether every moved field is attributable to W1's declared behavior changes. (5) Cleanup: git -C ${MF} worktree remove --force /Users/cstokes/Desktop/settlement-engine/.claude/worktrees/fable-base-triage. Never modify either tree's files.` },

  { label: 'w1-war-joins-review', prompt: `${PREAMBLE}
TASK: Architecture + correctness review of the uncommitted WAVE W1 war joins (the full dirty diff on worldPulse paths). Scope: git -C ${MF} diff for the modified files + the three new untracked files src/domain/worldPulse/{warIntent,treatyEnforcement,treatyTransfer}.js + new tests tests/domain/{warIntentJoin,siegeArrivalGate,treatyEnforcement,peaceTerms}.test.js + tests/store/lifecycleRoundTrip.test.js.
Claims to verify: (1) the strategy chooser deposits an ORDER in a new spatialLedgers.warIntents ledger — conditional, 2-tick TTL, ZERO RNG (verify no rng draws — stream theft is a recorded hazard class); the war layer remains the ONE opener (verify no second opener). (2) Sieges wait for arrival when the transit layer is active, byte-identical when spatial is dark (verify the dark path is structurally unchanged). (3) Treaty terms bite: readiness cap, war block, occupation hold, tribute/reparations/restitution move REAL grain (verify conservation — grain moved must be debited somewhere; check for the recorded writer/reader payload-spelling drift class). (4) The demilitarizationCapFor inversion fix: magnitude means SEVERITY, so a harsher clause must now set a LOWER readiness ceiling — verify the fix and its test. (5) Persistence lifecycle: the new warIntents ledger — trace create/read/persist/serialize/reload paths (the JSON-alias trap is a recorded hazard: JSON-round-trip any fixture claims). (6) General quality: determinism purity (no Date.now/Math.random), finite semantics (typed buckets), receipts carry ids (the id-less wizard-news drop class is a recorded hazard — every news entry MUST carry id). Run the four new test files focused and report results. Verdict per claim.` },

  { label: 'carto-completion-survey', prompt: `${PREAMBLE}
TASK: Cartography TC-0/1/2 completion survey. The pause handoff assumed these were killed mid-build with partial edits, but ALL 11 test files pass (154 tests, exit 0 — CONFIRMED by the chair already). Determine what if anything is actually missing. (1) Read docs/DESIGN_TOWN_CARTOGRAPHY.md: extract the TC-0, TC-1, TC-2 slice specs (done-whens) and the TC-3..TC-8 roadmap. (2) Against each TC-0/1/2 done-when, verify presence in the dirty tree (new files: src/lib/mapSubTabs.js, src/components/townMap/MapTabShell.jsx + subtabs/, src/domain/townCartography/ whole dir, src/domain/townScene/cartographyContract.js; modified: SettlementMapPane/Presentation/useTownMapPresentation, OutputContainer.jsx, displayPrefsSlice, operationRegistry, compendiumData.generated.js, townScene compile chain). (3) Consistency checks that tests may not cover: operationRegistry additions must have matching regenerated compendiumData (the store-action lifecycle hazard); scripts/.size-baseline.json changes on these paths; the mapTabShellLazy test proves lazy loading — verify the eager first-paint budget is untouched (do NOT run a build; static import-graph reasoning is enough, note it PLAUSIBLE). (4) Enumerate precisely what TC-3..TC-8 still need (this feeds the external-implementer architecture spec). (5) Any incoherent/truncated edit you find = ESCALATE with the exact file.` },

  { label: 'i1-brokerage-rows', prompt: `${PREAMBLE}
TASK: Validate the I1 brokerage-catalog judgment cluster from docs/FABLE_VALIDATION_QUEUE.md (read the file for full row text). Rows: (a) 'GOLDEN RE-RECORD AUTHORIZED (I1 catalog)' — the per-wave re-record policy decision, blast radius 273/525 rows = exactly town+city+metropolis, 0 outside predicted tiers, dormancy goldens untouched, commit 99974c4a, sha 810d7d1a→735e7784; (b) 'Batch-3 golden re-record acceptance' — same shift, plus goldenViewModel town row headcounts.institutions 51→54; (c) 'I1 Rookery precondition deferred to I3' — assembleInstitutions runs four steps before generatePower so no catalog field can express requires-criminal-power; verify whether I3 (@ 007e0dcf) actually CLOSED the deferral or inherited it — this is the row's explicit re-examination; (d) the metropolis-inherits-city-brokerages-via-mergeCatalogs question: read src generators (assembleInstitutions, the brokerage catalog) and judge whether metropolis inheriting city brokerages is the intended DESIGN_INFORMATION_BROKERAGES.md section-3 reading; (e) baseChances 0.22/0.16/0.28/0.30/0.20/0.18 — sanity-judge now that the whole town+ band shifted. DO: git show 99974c4a --stat; read the catalog + assembleInstitutions; verify the Rookery predicate's wiring status in I3's commit; check the golden diff row counts if the fixture is inspectable (python on the golden JSON: count changed rows by tier vs the 810d→735e claim is not directly re-derivable post-hoc, so verify structurally instead: which tiers CAN seat brokerages per the catalog gates).` },

  { label: 'neighbour-mirror-escalation', prompt: `${PREAMBLE}
TASK: The ESCALATION row — neighbour mirror/opposition factions structurally unreachable at town/city tier post-I1. Opus measured: 0 of 200 seeds at city, 0/60 at town, 1/60 at village/hamlet, 14/60 at thorp; cause identified by in-memory ablation of the four new catalog entries; the affected test moved to thorp (tests/generators/powerEconomyFreshness.test.js carries the full note). The mechanism: neighbourFactions injects a mirror only when !existingTypes.has(fType), and a post-I1 city already holds every faction category the bias can name (economy, military, religious, criminal). THE DECISION for the chair: (a) accept as emergent, (b) make the mirror gate tier-aware or category-count-aware, (c) let a mirror reinforce an existing category instead of being suppressed.
DO: (1) Read the neighbourFactions code and powerEconomyFreshness.test.js note; confirm the mechanism from code. (2) Re-execute a cheap version of the measurement (e.g. 40-60 seeds at city + thorp) to CONFIRM the reachability numbers still hold on the current tree. (3) Analyze all three options ARCHITECTURALLY: which preserves THE PROMISE (a seed is a world, forever — option b/c change generation for existing seeds unless flag-gated!), which respects the finite-semantics law, what each does to the faction-count distributions and downstream consumers (factionRename, faction keys, wizardNews). (4) Recommend one option with full tradeoff reasoning — note that any generation-changing fix is golden-shifting and likely belongs in THE ONE REGEN batch. Report as ESCALATE with your recommendation attached (final call is the chair's).` },

  { label: 'h-program-rows', prompt: `${PREAMBLE}
TASK: Validate the H-program (NPC consequences) acceptance rows from docs/FABLE_VALIDATION_QUEUE.md (read it for full text). Rows: H1 @ 2bafa5d1 (durable NPC identity: zero-PRNG FNV-1a mint, one-way, idempotent; Law 7 projection structural; DISCLOSED persistence-shape change — campaigns under full_simulation gain the ledger key — re-examine whether it needs migration/versioning treatment: check the store load path for older saves lacking the key); H2 @ 4b1e391f (verdict totality by exhaustive cell walk; the JSON-alias census repair; the liveInstitutions ruling: hasPrison + institution half of hasCriminalPower read live capacity, faction half deliberately unfiltered — judge the asymmetry); npcVerdictTable ruin-filter row (same ruling, plus the open design question whether a ruined prison should DOWNGRADE jailed→exile instead of removing the option once H2 wires to the pulse); H3 @ fc3a6a49 (circulation: belief-not-truth admission; stationarity proven by a compounding mutant escaping the envelope while live weight does not — re-execute that envelope test); H4 @ 6594548d (undo wired ONCE PER SURFACE not per row because KILL removes the record; a second covert gate REMOVED because it masked the projection-leak control — verify the removal did not widen a leak: check what free/anonymous surfaces can now see; also the row's sweep question: is any OTHER advertised capability unreachable — run the dead-op ratchet test and grep for advertised-verb patterns). Read the actual commits (git show <hash> --stat, then key files), run the cited test suites focused, verdict per row.` },

  { label: 'j-program-rows', prompt: `${PREAMBLE}
TASK: Validate the J-program (routes) + wave D acceptance rows from docs/FABLE_VALIDATION_QUEUE.md (read it for full text). Rows: Wave D @ 8c82e8ae (CREATE_ROUTE + migration 193: bilateral one-transaction CAS mirroring 183; two real defects found by exercising recovery — missing requestedAt, Immer draft reference-equality; the open question: config._userRoutes as provenance home vs a first-class column when the train deploys — read supabase/migrations/*193* and the command code, judge); J1 @ ed5ac49d (route ledger + genesis; the implementer's grade-ladder correction: water edges took the LAND grade ladder, ports totality now structural — confirm road-grade water genesis is the intended DESIGN_ROUTE_LIFECYCLE.md section-8 reading); J2 @ 6aa0f205 (counterfactual mercy scores network-minus-edge — re-derive why scoring against a network still containing the edge can never fire; plus three veto-open calls: usage-on-edge vs third container, integer tally beside band word, withCorridors replaces / withRouteEdges explicit-wins — rule on each); J3 @ 49b42f47 (hidden persistence as ABSENCE of a removal path proven by 400-tick soak; the two-danger-reading split road-cost vs endpoint-deferral — judge one blended term vs two); J4 @ bc843d33 (the reputation race pinned both ways on one fixture — read that test, confirm it proves what it claims). Run key suites focused (routeNetworkGenesis, routeNetworkFlows, charter/decay/danger, the race test). Verdict per row.` },

  { label: 'k-i2-fp-rows', prompt: `${PREAMBLE}
TASK: Validate five rows from docs/FABLE_VALIDATION_QUEUE.md (read it for full text): (1) K1 @ 4728af34 — institution status system (operational|impaired|shell), magic-independent, magicEconomyEnabled declared dark; re-examine whether the four-cause vocabulary wants a fifth (read the status model + DESIGN_MAGIC_ECONOMY.md and DESIGN_TRACK_K_COMPLETION.md context; also check K2/K3/K4 landed after — did they strain the vocabulary?). (2) I2 @ 1f464505 — the ruin/impairment brokerage line: RUINED houses filtered, IMPAIRED houses still trade corruptly, pinned so future tightening cannot silently silence compromised brokers; judge whether impaired-still-trades reads right for players seeing stamped news from a corrupt house. (3) FP-G16 ESD trim @ 05786319 — first-paint 1,056,635→1,020,590 via excising cultureProfiles from ENGINE_SHARED_DOMAIN; the row's sweep question: do OTHER ESD boundary re-exports carry the same over-inclusion? Inspect the ENGINE_SHARED_DOMAIN boundary file and classify each re-export as needed-eager vs candidate-lazy (static reasoning; do not run builds). (4) Batch-4 typecheck reconciliation — RouteBypass shape moved into J1's ledger for one-writer; three fixes rejected as silence-not-correctness; the re-examination: did a FOURTH spelling of the world/settlement shape creep in across H/I/J/K? grep the typedefs. (5) The soak remnant amendment (if not covered elsewhere, verify the lifecycleDiedAtTick exception logic). Verdict per row.` },

  { label: 'jul31-herald-rows', prompt: `${PREAMBLE}
TASK: Validate the six 2026-07-31 rows (the oldest — validate first per protocol) from docs/FABLE_VALIDATION_QUEUE.md (read it for full text): (1) npc-credibility dormancy golden nc-b|8|one_month re-record @ 8d71a479 — re-derive the field-level diff from the commit's test-header account; confirm only newsKinds moved (an infowar_spy_exposed beat previously discarded by the id-less drop, now counted) and no ledger drift hides behind the histogram. (2) Herald routing for nine kinds (eight war-doctrine → war; intel_transfer → trade) — read heraldRouting.js; sanity-check each door. (3) treaty_signed upgraded to major (severity 0.55, score 66) — judge World Book majorHeadlines membership. (4) THE SEVERITY REGISTER row — the instruction was corrected: the soak-receipt check is NOT executable (no receipt carries severity). What the chair CAN do per the row: (a) re-derive the estate census (94 severity literals in 28 news-authoring files, bands 13/58/16/7 at cut-points 0.3/0.55/0.8) — re-run that census yourself (grep severity literals in news-authoring files, band them) and check the numbers; (b) judge intel_transfer 0.35 vs dropping under 0.3 to 'a minor stir'; (c) assess whether a rumor-magnitude channel belongs in the soak receipt envelope (instrument gap). (5) diplomacy re-filed events→trade + the SINGLE-PRODUCER KEYS walker — verify the walker test exists and bites (read it; check the two-producer fixture proof); the remaining taste call: when it reds, re-split diplomacy or mint a treaty-specific impactKind — recommend. (6) heraldFeed READ-ONLY fourth source — validate the lens rule (advance = entry.tick >= latestPulse.tick; movers receive the same worldState.tick the pulse record stores) against a MULTI-INTERVAL advance: read heraldFeed.js + tests/components/heraldFeedSources.test.js and reason through a 3-interval advance; confirm the persisted-digest road stays closed. Verdict per row.` },

  { label: 'rename-verdict-rows', prompt: `${PREAMBLE}
TASK: Validate two rows from docs/FABLE_VALIDATION_QUEUE.md (read it for full text): (1) factionRename cascade extended to role, factionGoal, relationships[].npc1Role/npc2Role — Opus classed it REPAIR (latent pre-existing staleness surfaced by I1's stream translation) riding the already-ratified faction prose policy; the re-examination: confirm prose substitution on a TITLE is wanted ('Thieves Guild Master' → 'The Amber Concord Master' — right for a guild rename, odd if the token is coincidental). Read src/domain/factionRename.js CASCADE_BUCKETS + tests/domain/factionRename.test.js totality assertion; ALSO check the recorded hazards: never cascade wizardNews or factions[].id (verify the extension didn't), and JSON-alias trap (factions[].members ARE npcs[] objects — verify the cascade handles the alias on reload; the test should JSON-round-trip). Judge: is title substitution acceptable, or does it need a NON_CASCADED ruling? Consider: how often is a faction token in a title coincidental in generated data? (2) npcVerdictTable ruin filter — hasPrison + institution half of hasCriminalPower read liveInstitutions(); run tests/lint/ruinFilterRoster.walker.test.js + tests/domain/npcVerdictTable.test.js; confirm zero golden risk claim (npcConsequencesEnabled false in full_simulation spread, lit in no preset — verify in simulationRules.js; note the dirty tree modifies simulationRules.js, check whether W1 changed that spread!). Verdict per row.` },

  { label: 'p1-external-review-rows', prompt: `${PREAMBLE}
TASK: Validate three wave-P judgment rows from docs/FABLE_VALIDATION_QUEUE.md (read it for full text): (1) P1 acceptance — cure measured at 300y (8 settlements at fixed points, y150==y300 within 0.5%; pre-cure control re-creates runaway 1200→678M; dark byte-identical by OBJECT IDENTITY against a read-only pre-wire extraction @ d620a05d). Re-examine the growth-side-only suppression judgment: alternatives (a) net-out and (b) full lane move were written down and rejected — read the design doc's judgment block, re-derive whether growth-side-only is right (does decline-side pressure double-count?). (2) External design review row — three amendments accepted: reserves are NOT carrying capacity (three separate readings, no binding-cause enum); migration precedes response competition; responses are persistent PLANS not weekly redraws, scope-trimmed to ONE plan slot per settlement. Re-examine the one-plan-slot trim (the reviewer implied a richer plan set) — read the plan-slot code (grep planSlot / plan ledger in src/domain/worldPulse/demographics*) and judge whether one slot creates pathological plan-thrash or starvation at scale. Also: does the three-reading combination table need a fourth reading? (3) The village-seeding delegated ruling in J-P blocks. Read docs/DESIGN_DEMOGRAPHIC_ENGINE.md sections 2b, 5c, 7b, 11 + the acceptance section. Verdict per row.` },
]

const results = await parallel(briefs.map(b => () =>
  agent(b.prompt, { label: b.label, phase: 'Evidence', schema: SCHEMA })
))

const packaged = briefs.map((b, i) => ({ agent: b.label, result: results[i] }))
const missing = packaged.filter(p => !p.result).map(p => p.agent)
if (missing.length) log(`agents returned null: ${missing.join(', ')}`)
return { packaged, missing }