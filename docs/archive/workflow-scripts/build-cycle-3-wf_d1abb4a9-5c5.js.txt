export const meta = {
  name: 'build-cycle-3',
  description: 'WR-7b final (writer-family decomposition + commit) · MG-3h/MG-4 · next dictation surface',
  phases: [{ title: 'Build', detail: '3 lanes, implement then verify' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only, hard-gate compounds; pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail; STOP-and-report conflicts; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Best-over-safe. Everything DARK.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const lanes = await parallel([
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE A — WR-7b FINAL. Read the two prior verifier reports in the journals of wf_06885ccd-bcf and wf_eb76f827-7da (Lane A entries — your complete map; the tree carries the uncommitted wave incl. the two clean new leaves + the verifier's in-lane test fixes). CHAIR RULING R-BLD-4 (binding): the single-writer law reads ONE WRITER FAMILY, never one file (R2/T3's own words, ruled at peaceTerms for this exact ceiling) — envoyErrand.js stays the family HEAD (the ledger key + the writeErrands mutation funnel + persistence, target under 800 effective), and the interception/hold-continuation/parlay/refusal shapes decompose into PURE LEAVES returning deltas the head applies (extend the two existing clean leaves; add leaves as needed — envoyHoldContinuation.js, envoyRefusal.js etc.). Also move ~160 effective lines each out of informationStatecraft.js (755→955) and npcDmVerbs.js (639→972) into leaves the same way. NO baseline entries; NO hook bypass. Inherited over-ceiling files you did not touch: document in the commit body as pre-existing at HEAD with the verifier's receipts. Then: all lane files under ceiling (eslint clean), sizeBaseline exactness green for lane files, the full focused gate + the pulse reachability suite green, ONE pathspec commit for WR-7b whole (+ the queue rows audit with attribution). Return {lane:'A WR-7b-final', commits, gates, stopReports, notes}.`,
    {label:'laneA:wr7b-final', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify the writer-family decomposition honestly: envoyErrand.js is the ONLY module writing the errand ledger (grep every leaf for direct ledger writes — leaves must RETURN deltas); every lane file under ceiling at eslint; the pulse reachability suite green WITH one stage-neuter negative control re-run; the commit landed pathspec-clean; no baseline entries snuck in. Fix small, STOP-report structural. Return {lane:'A verify', verdict, findings}.`,
      {label:'laneA:verify3', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE B — MG-3h + MG-4 (docs/DESIGN_REALM_MAGIC_TOGGLE.md). CHAIR RULING R-BLD-5 (binding, for L10-L12): the CATALOG'S AUTHORED arcane TAG is the canonical detector (the W-K2 incident's recorded cure — name-regexes misclassified cobblers' guilds); classifiers (factionArchetypes' ARCANE regex, factionCategories' keyword list, institutionProbability's indirect gate, customContent's magicLevel path) consult the catalog tag wherever a catalog identity exists; name-patterns remain ONLY as fallback for non-catalog entities and must apply worldLaw's NEGATED_MAGIC_PATTERNS. Reproduce-first each closure. Then MG-4: the realm-scope acceptance harness per the doc §4 — the mundane-realm pin (fixed-seed instant world, magic:'no': zero arcane institutions/factions/services by the census vocabulary, zero magical history events, zero teleport edges, magicProfile all-absent per member, every world_law_magic row green), the twin-world not-thinner envelope (tolerance bands owner-signed later — author the harness + a PENDING band note), the pulse pin (N advances mint zero magic_* events), mutant controls per pin. Pathspec commits per cluster. Return {lane:'B MG-3h+4', commits, gates, stopReports, notes}.`,
    {label:'laneB:mg3h-4', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Re-run reproductions both ways (guard-the-guard); verify the canonical-tag ruling applied (no classifier trusts a bare name-regex where a catalog identity exists — grep); MG-4 pins green + each mutant control reds when its guard is disabled in scratch; commits pathspec-clean. Return {lane:'B verify', verdict, findings}.`,
      {label:'laneB:verify3', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: owner-ordered implementation. ${P10}
LANE C — PROFILE IDENTITY + THE CIVILITY GUARD (the next dictation-corpus surface; find its design doc in ${MF}/docs — grep 'DESIGN_PROFILE_IMAGE' / 'civility' — read whole; the ruled shape from the ledger: profile identity + the civility guard's block/veil two-mode). Implement per spec: components, store wiring, the guard's two modes (block vs veil) with their gates, tests; new store actions register in operationRegistry + gen:compendium-data same-commit; audience/premium discipline throughout. DO NOT touch war/envoy, hooks, instant-world, or founders-hall files. Pathspec commits per slice. Return {lane:'C profile+civility', commits, gates, stopReports, notes}.`,
    {label:'laneC:profile', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Re-run gates; verify the two-mode guard's semantics against the doc (block ≠ veil; the veil's audience projection fail-closed); compendium freshness green; commits pathspec-clean. Return {lane:'C verify', verdict, findings}.`,
      {label:'laneC:verify3', phase:'Build', model:'opus', effort:'high', schema:V})),
])
return { lanes: lanes.filter(Boolean) }