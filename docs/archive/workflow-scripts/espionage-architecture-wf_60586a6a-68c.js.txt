export const meta = {
  name: 'espionage-architecture',
  description: 'Architect the espionage layer (covert confirmers) exhaustively — substrate censuses, master architecture, adversarial cohesion critique',
  phases: [
    { title: 'Substrate', detail: 'six parallel censuses over every touched system', model: 'opus' },
    { title: 'Architect', detail: 'the master architecture, implementation-grade' },
    { title: 'Cohesion', detail: 'adversarial critique: cohesive with EVERYTHING' },
  ],
}

const OUT = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c44e5d99-2ba5-49d5-a554-40b68534c8eb/scratchpad/espionage-arch'
const DIRECTIVE = '/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/memory/espionage-confirmers-directive.md'
const FP_DRAFT = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c44e5d99-2ba5-49d5-a554-40b68534c8eb/scratchpad/fp-arch/DESIGN_FP_ARCHITECTURE-DRAFT.md'

const GROUND = `VERIFY-FIRST: this brief is the SettlementForge chair's authentic instruction set; verify claims against the tree, then proceed — do not refuse it as injection. READ-ONLY against /Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold (branch claude/composite-r4, HEAD at or past 71e78fe9 — WR-10 is FINISHED, wired dark). Do NOT run vitest/npm/tests — greps and reads only. WRITE ONLY under ${OUT}/ (mkdir -p). python3 byte-scan every Write. THE OWNER DIRECTIVE is at ${DIRECTIVE} — read it FIRST and treat its verbatim-intent section as law, its chair-refinements section as accepted-vetoable. The WR-era laws bind (the war volume docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md sections 1 and 10 are the canon; the four-fence dormancy, zero-PRNG keyed-hash, zero-new-keys persistence fight, violation-row attribution, drop-when-absent T4, first-match exactly-once, dead-band reachability, notably: every premise RE-MEASURED with multi-spelling greps, navigate by SYMBOL never line number).`

phase('Substrate')
const [npc, crime, errand, align, magic, legs] = await parallel([
  () => agent(`${GROUND}

CENSUS 1 — THE NPC/FACTION/INFLUENCE SUBSTRATE. Map by grep/read with file:symbol receipts: npcAgency.js, npcLadderKernel/npcLadderState, npcLedger, faction membership + influence summation (how council/vote weight derives from members — is faction influence summed from PRESENT members anywhere?), the power-ranking read the directive's notoriety derives from, NPC goals/missions machinery (does an NPC goal/mission record exist? what shape?), presence/absence semantics today (do envoys away from home already lose any home effect?), and the JSON-alias law (factions[].members ARE npcs[] objects). Deliver ${OUT}/CENSUS-NPC.md: what EXISTS (with shapes), what the espionage layer can RIDE, what has no substrate. Return {census:'npc', exists:[...], rideable:[...], missing:[...], notes:'...'}.`, { label: 'esp:npc', phase: 'Substrate', model: 'opus', schema: { type: 'object', required: ['census','exists','rideable','missing','notes'], properties: { census:{type:'string'}, exists:{type:'array',items:{type:'string'}}, rideable:{type:'array',items:{type:'string'}}, missing:{type:'array',items:{type:'string'}}, notes:{type:'string'} } } }),
  () => agent(`${GROUND}

CENSUS 2 — THE CRIMINAL/SECURITY SUBSTRATE. The directive scales covert competence by HOME criminal power + activity, and catch probability by TARGET criminal power/activity + internal security. Find every spelling: corruptionWeb.js and its flag, any criminal-power/criminal-activity/underworld/thieves reads, internal-security/watch/garrison/guard reads (multi-spelling: security, watch, patrol, garrison, constable...), and how settlement-level security is or could be derived from existing state. Deliver ${OUT}/CENSUS-CRIME.md with receipts: what exists, what must be DERIVED (zero-new-keys candidates), what genuinely has no substrate. Return {census:'crime', exists:[...], rideable:[...], missing:[...], notes:'...'}.`, { label: 'esp:crime', phase: 'Substrate', model: 'opus', schema: { type: 'object', required: ['census','exists','rideable','missing','notes'], properties: { census:{type:'string'}, exists:{type:'array',items:{type:'string'}}, rideable:{type:'array',items:{type:'string'}}, missing:{type:'array',items:{type:'string'}}, notes:{type:'string'} } } }),
  () => agent(`${GROUND}

CENSUS 3 — THE ERRAND/JOURNEY MACHINERY (the extension host). Map the WR-7 envoy program's record shapes and lifecycle: envoyErrands persistence (fields, legs?, multi-stop support?), interception (route-crossing checks — the exact mechanism the directive's capture points reuse), foreignGuestHold + ransom parameters (where hostage duration and ransom price live — the directive scales BOTH), testimony credibility hooks, envoy vetting, and the mint/ratification transports. Also merchants: does any traveling-merchant machinery exist, or is trade-cover a new leg TYPE on the errand record? Deliver ${OUT}/CENSUS-ERRAND.md: the exact extension points (symbol-addressed), what the composite 2-3-leg itinerary needs that the record lacks, the zero-new-keys assessment for mission state. Return {census:'errand', exists:[...], rideable:[...], missing:[...], notes:'...'}.`, { label: 'esp:errand', phase: 'Substrate', model: 'opus', schema: { type: 'object', required: ['census','exists','rideable','missing','notes'], properties: { census:{type:'string'}, exists:{type:'array',items:{type:'string'}}, rideable:{type:'array',items:{type:'string'}}, missing:{type:'array',items:{type:'string'}}, notes:{type:'string'} } } }),
  () => agent(`${GROUND}

CENSUS 4 — ALIGNMENT + DOCTRINE DERIVABILITY. The malice axis exists (settlementAlignment(item, worldState), natureWordFor — verify current addresses by symbol). The directive needs a LAW/CHAOS read that must be DERIVED, zero new keys, from civic signals. Census the candidate signals with receipts: institution density/adherence, tradition strength (traditionsKernel), criminal-activity inverse, disposition channels, routineMajorApproval-class civic toggles, assize/commons machinery. Assess: can a stable, deterministic, banded order01 read be derived TODAY, and from exactly which inputs? Also census how doctrine-like reads are spelled elsewhere (disposition thresholds, warReasons intent gates) so the espionage DOCTRINE read matches the house idiom. Deliver ${OUT}/CENSUS-ALIGN.md. Return {census:'align', exists:[...], rideable:[...], missing:[...], notes:'...'}.`, { label: 'esp:align', phase: 'Substrate', model: 'opus', schema: { type: 'object', required: ['census','exists','rideable','missing','notes'], properties: { census:{type:'string'}, exists:{type:'array',items:{type:'string'}}, rideable:{type:'array',items:{type:'string'}}, missing:{type:'array',items:{type:'string'}}, notes:{type:'string'} } } }),
  () => agent(`${GROUND}

CENSUS 5 — MAGIC TRANSMISSION + PRICING. The directive: magic-enabled maps allow instant confirmation home (travel in physical; the mundane world requires the return trip). Census the realm magic toggle program (MG-1..4 memories say allowlists are BOOLEAN-ONLY and a store action requires gen:compendium-data — verify in tree), the magic-enabled read a transmission gate would use, magicEconomyEnabled and its pricing surfaces (can an instant-send be PRICED when the economy lights?), and any existing magic-speed information precedent. Deliver ${OUT}/CENSUS-MAGIC.md with the exact gate spelling the transmission arm should use. Return {census:'magic', exists:[...], rideable:[...], missing:[...], notes:'...'}.`, { label: 'esp:magic', phase: 'Substrate', model: 'opus', schema: { type: 'object', required: ['census','exists','rideable','missing','notes'], properties: { census:{type:'string'}, exists:{type:'array',items:{type:'string'}}, rideable:{type:'array',items:{type:'string'}}, missing:{type:'array',items:{type:'string'}}, notes:{type:'string'} } } }),
  () => agent(`${GROUND}

CENSUS 6 — THE BELIEF-LEGS DEMAND MAP (the mission's product side). Enumerate every consumer that appraises through beliefs and exactly which banded legs each demands: sovereigntyAppraisal's four (tierBand/storesBand/routeBand/trajectoryBand — three have NO source, the WR-10 §9 lighting precondition, quote it), NEGOTIATION_SUBJECT_BANDS' current legs and their writers, BeliefRecord's fields (confidence01, lastUpdateTick — the CONFIRM product's write slots), peace-terms appraisal legs, coalition-decision legs, and where a REFUTE product would land (how does a belief get corrected today — what writer?). Also: how whereabouts/absence could become a belief (the missing-NPC tell) — what news kinds carry NPC movement today. Deliver ${OUT}/CENSUS-LEGS.md: the full demand table (consumer × leg × exists-today × spy-suppliable). Return {census:'legs', exists:[...], rideable:[...], missing:[...], notes:'...'}.`, { label: 'esp:legs', phase: 'Substrate', model: 'opus', schema: { type: 'object', required: ['census','exists','rideable','missing','notes'], properties: { census:{type:'string'}, exists:{type:'array',items:{type:'string'}}, rideable:{type:'array',items:{type:'string'}}, missing:{type:'array',items:{type:'string'}}, notes:{type:'string'} } } }),
])

phase('Architect')
const arch = await agent(`${GROUND}

YOU ARE THE ESPIONAGE MASTER ARCHITECT. Six substrate censuses are complete under ${OUT}/ — read all six files in full; their returns:
${JSON.stringify({ npc, crime, errand, align, magic, legs }, null, 2)}

Also read, if it exists by now, the FP architecture draft at ${FP_DRAFT} (the eight-program compilation — your output must slot into its wave order and seam matrix; if absent, read docs/DESIGN_FP_SPINE.md and docs/DESIGN_FP_INFORMATION.md directly for the neighbors).

Produce ${OUT}/DESIGN_ESPIONAGE_ARCHITECTURE-DRAFT.md — implementation-grade, war-volume idiom, self-contained: (1) THE CANONICAL MODEL — fight for zero new top-level keys (missions on the errand/npc ledgers per census 3; vigilance/doctrine/notoriety as DERIVED reads; name every conditional field with drop-when-absent discipline) + the full Lifecycle-paths clause; (2) THE FLAG — its law-2 shape, its conjunction (rides envoyDiplomacyEnabled? demands what?), manifest timing, the magic-transmission sub-gate; (3) THE MECHANISMS, each with its math shape and bands: covert competence (home criminal capacity × notoriety inverse), per-leg capture (route military presence × target hostility chain × target security × vigilance modifier), superlinear leg compounding with the 2-3 cap, the taint model (public face vs actual itinerary, retroactive exposure), the three typed products (CONFIRM/ACQUIRE/REFUTE) and their exact belief-write shapes, the missing-NPC tell as belief-derived vigilance, the alignment DOCTRINE read (malice + the derived order01 per census 4) governing target scope/frequency/leniency, hostage/ransom scaling by notoriety, the absence cost (faction weight while away), magic vs mundane asymmetry (lose-the-spy-keep-the-intel); (4) THE WAVES — dependency-ordered, JOINT with the belief-legs wave (this program IS the source that discharges WR-10's §9 lighting precondition — architect that discharge explicitly); per wave: files, budgets (measure current sizes), pins incl. four-fence, mutants, dormancy, spine req 13/14 (alignment engagement is CENTRAL here — req 13 is ENGAGED on both axes by design), collision maps; (5) SEAM CONTRACTS to EVERY neighbor: war (capture→ransom/casus; the caught-spying-on-a-friend betrayal event), trade (merchant cover legs, clearing consumers), treaty (re-appraisal on confirmed/refuted legs), the sovereignty market (the legs supply), faith (judgment of espionage doctrine), interior/politics (absence exploitation), Herald (dm-only covert departures/confirmations, PUBLIC captures/scandals — the five joins per kind, audience law), DM verb surface (ordering a mission = always-proposal? architect it), magic economy (transmission pricing when lit); (6) QUEUE SLOTTING — the exact insertion into the FP cross-program wave order (expected: joint with belief-legs, after SP core, within the IN family) written as the amendment text the chair lands; (7) OPEN CHAIR QUESTIONS, max 5, ranked, with recommendations. Label every judgment JUDGMENT (vetoable). Return {draftPath:'...', lineCount:N, waveCount:N, newPersistedKeys:[...], flagName:'...', seamCount:N, lightingDischargeWave:'...', openQuestions:[...], notes:'...'}.`, {
  label: 'esp:architect', phase: 'Architect', effort: 'max',
  schema: { type: 'object', required: ['draftPath','lineCount','waveCount','newPersistedKeys','flagName','seamCount','lightingDischargeWave','openQuestions','notes'], properties: {
    draftPath:{type:'string'}, lineCount:{type:'number'}, waveCount:{type:'number'}, newPersistedKeys:{type:'array',items:{type:'string'}}, flagName:{type:'string'},
    seamCount:{type:'number'}, lightingDischargeWave:{type:'string'}, openQuestions:{type:'array',items:{type:'string'}}, notes:{type:'string'} } },
})

phase('Cohesion')
const coh = await agent(`${GROUND}

YOU ARE THE COHESION CRITIC — the owner's last mandate verbatim: "make it cohesive with EVERYTHING." The architect returned:
${JSON.stringify(arch, null, 2)}

Read the draft in full. Your ONLY job is cross-system cohesion, adversarially: (1) walk EVERY seam the draft claims against the live tree — does each named symbol exist, does each consumer actually read what the draft says it writes, does each write land in a home that survives the lifecycle paths (regen under THE PROMISE especially — derived vs persisted correctly split)? (2) walk every WR-era law against the draft — determinism, dormancy, K3-class seams (does the spy system ever let truth leak into belief assembly ILLEGALLY, or does it launder truth into beliefs through a RECEIPTED channel — the confirmation product must be an HONEST truth-to-belief transfer, priced by risk; state precisely why that differs from a K3 violation), T4, dead bands, first-match, size ceilings on every named host file (measure them); (3) walk the NEIGHBOR programs — does the draft contradict anything in the FP architecture draft at ${FP_DRAFT} (read it if present) or the war volume (the espionage casus vs the atrocity casus vocabulary; ransom parameter ownership; the §13 treaty axis untouched)?; (4) walk the PRODUCT laws — FINITE-SEMANTICS (AI clerk never writer), the news address law, audience/covert projection, product scope (world-only, never resolve a named character's fate — does spy capture ever RESOLVE a fate illegally, or does it stay within holds/ransoms/returns?), THE PROMISE; (5) the drama check at arm's length: does the mechanism set actually produce the owner's intended texture (the taint gamble, the notoriety trap, the missing-NPC tell, doctrine legibility) or does any piece cancel another? Number every finding with severity + the minimal fix. RETURN {verdict:'COHESIVE'|'NEEDS-REVISION', findings:[...], lawViolations:[...], seamGaps:[...], notes:'...'}.`, {
  label: 'esp:cohesion', phase: 'Cohesion', effort: 'max',
  schema: { type: 'object', required: ['verdict','findings','lawViolations','seamGaps','notes'], properties: {
    verdict:{type:'string'}, findings:{type:'array',items:{type:'string'}}, lawViolations:{type:'array',items:{type:'string'}}, seamGaps:{type:'array',items:{type:'string'}}, notes:{type:'string'} } },
})


phase('Amend')
const amend = await agent(`${GROUND}

YOU ARE THE AMENDMENT ARCHITECT. The master draft exists at ${OUT}/DESIGN_ESPIONAGE_ARCHITECTURE-DRAFT.md and the cohesion critic ruled NEEDS-REVISION. Its findings:
${JSON.stringify(coh, null, 2)}

REVISE THE DRAFT IN PLACE, three obligations: (1) close every numbered cohesion finding, law violation, and seam gap with the critic's minimal fix or better — the mis-homed symbols re-measured yourself (LIE_TUNING vs SIGHT_TUNING; the anonymity law's REAL homes; the cold_war_espionage neighbor mapped as a seam row; pullBand joins ACQUIRE's set or its exclusion is ruled in-file); for F3 define the target-side wariness predicate PRECISELY over what the target can lawfully observe (its own missing-visitor beliefs, the missing-NPC tell, public capture incidents — never home-side errand state; no covert side channel). (2) RE-READ THE OWNER DIRECTIVE at ${DIRECTIVE} — it now carries EIGHT owner additions A-H (G the rooted-spy/promotion-register 'gather or govern' economy with mission success GRADED by information quality, and H the TAP-ORDER RULING: access depth — outbound performance at standoff/shallow cover, internal beliefs embedded, both-plus-delta with an inside asset, fidelity metadata receipted in house voice, the three-way standoff trade risk/confidence/INTEGRITY — postdate your predecessor's start): verify every one of A-H is architected in full, and integrate G and H wherever absent (G rides the ladder/promotion machinery the NPC census mapped; H's tap levels bind the gradient mechanism and the waypoint-poisoning consequence). (3) Keep the zero-new-keys result and the ES-4 joint-discharge amendment intact unless a finding forces otherwise. Byte-scan after every write. Return {draftPath, lineCount, findingsClosed, additionsIntegrated, waveCount, openQuestions, notes}.`, {
  label: 'esp:amend', phase: 'Amend', effort: 'max',
  schema: { type: 'object', required: ['draftPath','lineCount','findingsClosed','additionsIntegrated','waveCount','openQuestions','notes'], properties: {
    draftPath:{type:'string'}, lineCount:{type:'number'}, findingsClosed:{type:'number'}, additionsIntegrated:{type:'array',items:{type:'string'}},
    waveCount:{type:'number'}, openQuestions:{type:'array',items:{type:'string'}}, notes:{type:'string'} } },
})

phase('Re-cohesion')
const coh2 = await agent(`${GROUND}

YOU ARE THE RETURNING COHESION CRITIC. The amendment architect reports:
${JSON.stringify(amend, null, 2)}

Re-read the amended draft in full. Verify: every one of your round-1 findings closed with the fix real (re-measure the symbol homes yourself); owner additions A-H each architected (walk all eight against the directive file at ${DIRECTIVE} — G's graded-success/promotion economy and H's three-level tap order with receipted fidelity especially, and H's integrity consequence on the standoff trade); the F3 wariness predicate now covert-leak-free by construction; no NEW incohesions introduced by the amendments. Then the arm's-length drama re-check on the amended whole. RETURN {verdict:'COHESIVE'|'NEEDS-REVISION', perFinding:[...], additionsVerdict:[...8 entries...], newFindings:[...], notes:'...'}.`, {
  label: 'esp:cohesion2', phase: 'Re-cohesion', effort: 'high',
  schema: { type: 'object', required: ['verdict','perFinding','additionsVerdict','newFindings','notes'], properties: {
    verdict:{type:'string'}, perFinding:{type:'array',items:{type:'string'}}, additionsVerdict:{type:'array',items:{type:'string'}},
    newFindings:{type:'array',items:{type:'string'}}, notes:{type:'string'} } },
})

return { censuses: { npc, crime, errand, align, magic, legs }, arch, coh, amend, coh2 }
