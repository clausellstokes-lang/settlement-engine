export const meta = {
  name: 'build-cycle-8-war-tranche',
  description: 'Decomposition war tranche + WR-7 wiring discharge + R-BLD-8d · About repair tail R2',
  phases: [{ title: 'Build', detail: 'war lane serial · repair lane R2' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest before believing a timeout; full logs for walker diffs); strict-typecheck your touched files yourself; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Everything DARK, nothing pushed. CONCURRENCY: other lanes may still be live on hook files and LD-ladder components; queue docs (SOL_QUEUE.md, FABLE_VALIDATION_QUEUE.md) get concurrent edits — re-read before writing, carve hunks by pathspec.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const lanes = await parallel([
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE W — THE DECOMPOSITION WAVE, WAR TRANCHE + THE WIRING DISCHARGE + R-BLD-8d. The chair's CR-WR8-D ruling (queue block "CHAIR RULINGS 2026-08-03 — THE WR-8 GATE ANSWERED", committed @ 8c8eda59 — read it first) dispatches this. SERIAL STEPS, one pathspec commit each:
STEP 1 — decompose the four war files under the 800 ceiling, largest first: src/domain/worldPulse/envoyErrand.js (2638), peaceTerms.js (1680), pulseKernel.js (1580), applyWorldPulse.js (1395). METHOD = the WR-7b writer-family pattern (R-BLD-4): the head keeps its writer/entry role, pure delta-returning leaves extracted; focused suites green BEFORE and AFTER per file; ratchet each scripts/.size-baseline.json entry DOWN to the new measured size or delete it under-ceiling (R-BLD-6's only lawful direction; the sizeBaseline test proves it). ⚠️ RECORDED TRAPS: the namedPersonTransit walker scans ALL of src/domain and K3 pins EXACT import lists — REGISTER every new leaf where required or the walkers red; new src/domain files with store imports trip FOUR ratchets — leaves import domain-only.
STEP 2 — discharge the three recorded wiring deferrals now that both pulse mouths have headroom: wire WR-7b/7c/7d's deferred pulse stages (envoy errands re-minting through envoyErrand, the ransom claim landing at the applyWorldPulse mouth, the WR-7c chooser reachable where the volume says) per R-BLD-1: stage-neuter negative controls proving runtime reachability, AND the dormancy-golden pin — the war flags are DARK, so the goldens must be BYTE-IDENTICAL pre/post wiring; capture before, compare after, quote the result. If any golden moves with flags dark, STOP — that is a defect, not a disclosure.
STEP 3 — R-BLD-8d (chair ruling, same family as 8a..8c): in coalitionRatification.js's unionCoalitionWeight walk, re-enforce the tally-level record laws the chooser now trusts ballots for — refuse closed on a duplicated member inside one offer's ballots (reuse ratifyTermSheet's duplicate_member spelling) and on a ballot naming a foreign termSheetId under an offer row (sheet identity). The cycle-6R verifier's probe2 counterexamples (a 3-of-6 minority winning ratified/margin 1.0 via a duplicated ballot; ts.zzz chosen under ts.a) become the pins, and mutants re-opening each hole must red them. Also ONE-CELL CORRECTION in docs/FABLE_VALIDATION_QUEUE.md's R-BLD-8a/8b/8c row: "Effective sizes 527 (module) and 694 (test)" are raw wc -l; the true effective sizes are 335 and 521 — correct the cell and label it effective (the verifier measured with the ratchet's own Linter).
DO NOT start WR-8 itself — it is the NEXT wave, dispatched separately once this tranche is verified. DO NOT touch settlementSlice.js, hook files, LD components, About files. Return {lane:'W tranche', commits, gates, stopReports, notes}.`,
    {label:'laneW:war-tranche', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify on committed bytes: per decomposed file — behavior-identical (focused suites re-run by YOU, before/after sizes re-measured with the sizeBaseline test's own Linter, baselines ratcheted DOWN never up, writer-family single-writer intact, new leaves strict-clean and walker-registered); the wiring discharge — re-run the stage-neuter controls yourself, prove the dormancy goldens byte-identical with flags dark (hash them), and hunt for stream theft (per-tick keyed forks) and stride aliasing in any new pulse plumbing per the recorded Wave-E hazard classes; R-BLD-8d — re-execute both probe2 counterexamples (must refuse closed), run mutants re-opening each hole (pins must red), confirm the queue-row figure now reads effective 335/521; pathspec hygiene against every concurrent lane. Return {lane:'W verify', verdict, findings}.`,
      {label:'laneW:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled repair implementation. ${P10}
LANE R2 — THE ABOUT REPAIR TAIL, ROUND 2 (verified findings from cycle 6R's verifier; one pathspec commit):
(1) SECTION_BLURBS TOTALITY PIN — src/components/HowToUse.jsx renders the nav's sentence rung from SECTION_BLURBS[u.id], a hand-keyed side table with no binding to the manifest: the verifier deleted the 'ref' blurb line and all 29 aboutSplit tests stayed green while the nav card rendered an empty subtitle. Add the pin: Object.keys(SECTION_BLURBS) must equal the guide view's unit ids exactly (both directions), plus a non-empty-text assertion inside the existing navLinks() loop. Prove it by re-running the verifier's mutation (delete a blurb line → red; restore → green).
(2) SCROLL-MARGIN ON THE OTHER PAGE — the same-class defect the About lane disclosed and correctly left out of scope: src/components/howto/AboutManifesto.jsx, src/components/about/CompareSection.jsx and src/components/about/AboutWhatThisIs.jsx contain ZERO scrollMargin, so all 7 published anchors on ABOUT_WHAT_VIEW (what-this-is, the-premise, the-covenant, how-it-works, where-the-ai-fits, examine-it, how-we-compare) park their headings under the sticky ribbon. Apply the exact pattern the guide page uses (scrollMarginTop: ANCHOR_SCROLL_MARGIN from registrySlug.js — the 84px derivation is already proven), and pin it per component the same way the guide's pin works.
Gates: aboutSplit suite + the About-adjacent cohort green on committed bytes; eslint + strict on touched files; the anchor walker must not gain a row. DO NOT touch war/envoy files, settlementSlice, hook files, NavRibbon, App.jsx. Return {lane:'R2 repairs', commits, gates, stopReports, notes}.`,
    {label:'laneR2:about-tail', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify by execution: the totality pin reds on a deleted blurb, a renamed key, an ADDED orphan key, and an empty string (run all four mutants yourself, restore byte-identical); the scroll-margin fix is real on all three components (grep + the per-component pins red when the property is removed) and the 84 constant is imported, not re-derived; the walker gained no row; pathspec-clean. Return {lane:'R2 verify', verdict, findings}.`,
      {label:'laneR2:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
])
return { lanes: lanes.filter(Boolean) }