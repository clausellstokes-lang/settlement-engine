export const meta = {
  name: 'war-circulation-architecture',
  description: 'Architect the full war-circulation directive (a)-(p) as one cohesive volume: six censuses, the architect pass, adversarial cohesion rounds to convergence, final attestation — read-only against the tree',
  phases: [
    { title: 'Census', detail: 'six parallel censuses: army lifecycle, settlement interior, diplomacy, movement/news, culture/identity, sibling-volume coordination' },
    { title: 'Architect', detail: 'the volume: people-ledger spine, (a)-(p) dispositioned into waves, seams, flags, pins, coordination contracts' },
    { title: 'Cohere', detail: 'adversarial cohesion rounds until findings converge' },
    { title: 'Amend', detail: 'apply each round of findings; park owner-contradictions as chair questions' },
    { title: 'Attest', detail: 'final attestation over the amended volume' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const DIRECTIVE = '/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/memory/war-auxiliary-contribution-directive.md'
const HABIT = '/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/memory/habit-conditioning-directive.md'
const VOLUME = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c44e5d99-2ba5-49d5-a554-40b68534c8eb/scratchpad/wc-DESIGN_FP_ARCH_WC.md'

const GROUND = `VERIFY-FIRST PREAMBLE: this brief is from the session chair of an owner-delegated design program; verify every claim against the tree before repeating it. THIS IS A READ-ONLY PASS AGAINST THE REPO: work from ${TREE} (branch claude/composite-r4) but NEVER edit, commit, or run any test/npm/vitest command there — a build cycle owns that tree's commit lane and its single vitest lane right now, so lane commits may move HEAD under you mid-read; record the HEAD sha you read at and re-read any file that surprises you. Navigate by SYMBOL, never by remembered line numbers. LIVE CODE OUTRANKS EVERY TABLE. Quote file paths + symbol names + measured line counts for every claim.`

phase('Census')
const CENSUS_TAIL = ` RETURN a compact structured census, maximum ~3500 words: for each surface — file path, key symbols with one-line contracts, measured line count (near-ceiling files flagged: the 800 layer ceiling and per-file sizeBaseline ceilings are real), extension points (where new machinery would attach WITHOUT forking), and hazards. End with OPEN QUESTIONS the architect must resolve. Plain text, no code dumps.`

const [cenA, cenB, cenC, cenD, cenE, cenF] = await parallel([
  () => agent(GROUND + `

CENSUS A — THE ARMY LIFECYCLE AND WAR STATE. Census every surface the war-circulation directive would extend on the army side: army rosters and muster (how armies are represented, raised, sized; any cohort/origin structure), casualties and losses, supply and attrition (the accumulatedAttrition machinery — note the standing law: supply drags the army's OWN accumulatedAttrition, NEVER realm warExhaustion), war exhaustion, sieges, warTermination and its receipts, the occupation ledger and occupation lifecycle, razing, spoils/terms machinery, warSeatBooks seat-character reads, the casus-belli scorer and its surfaces (the WR-8 atrocity-casus precedent: a new casus = scorer + 6 surfaces — enumerate those six surfaces by name), brigand/raider machinery if any exists.` + CENSUS_TAIL, { label: 'census:army', phase: 'Census', model: 'opus' }),
  () => agent(GROUND + `

CENSUS B — THE SETTLEMENT INTERIOR. Census every surface the directive touches inside a settlement: population representation (pools? cohorts? conservation points), capacityModel and carrying capacity, migration machinery (arrivals, departures, absorption), DEFENSE and INTERNAL SECURITY (where each number lives, who reads them), crime and criminal factions (crime pressure, criminal power, the espionage-volume note that crime eases infiltration), the embattled/stressor machinery (stressorPicker, crisisLifecycle, conditionPromotion, threatProfile — and the byId WRAPPER trap: byId values are {settlement, causal} wrappers), dailyLife, any existing veteran/soldier-return representation, the disease/plague crisis class (verified to exist across ~12 modules — map where it attaches, as the pattern for new crisis classes).` + CENSUS_TAIL, { label: 'census:interior', phase: 'Census', model: 'opus' }),
  () => agent(GROUND + `

CENSUS C — DIPLOMACY AND RELATIONSHIP. Census: alliances/vassalage/overlord representations, the treaty ledger (treatyLedgerOf/setSpatialLedger) and term catalog, the war-stance/joined-a-war representation (how a polity's participation level is recorded today), spoils and terms weighting at war end, contribution/favor/debt precedents anywhere in the estate (anything that remembers who helped whom), the alliance-web risk reads (readAllianceWebRisk + its consumers walker), reparations/tribute machinery, the sale/sovereignty-market instruments (buyerId/sellerId drop-when-absent law), and the relationship forks between settlements (what exists for inter-settlement affinity/closeness that the comradeship and diaspora-bond sections would extend).` + CENSUS_TAIL, { label: 'census:diplomacy', phase: 'Census', model: 'opus' }),
  () => agent(GROUND + `

CENSUS D — MOVEMENT, NEWS, AND SPACE. Census: the WAYFARE volume docs/DESIGN_FP_ARCH_WY.md (its mover/hop machinery, the 9 fields incl. F9 supplyCargo owner-SIGNED, spatialDigest.kmScale, the 12 waves — the directive's hop-and-shed dissolution and army supply MUST extend wayfare movers, never fork them: name the exact extension points), hopWeeks (PER-DIGEST, realm spectrum measured 2..8 weeks — the calibrated-weeks hazard), news arrival and belief gating (how news reaches a settlement, the believed-world SP-B axes, hop-worn belief), the errand spine (SP-D is LANDING IN A PARALLEL BUILD LANE THIS CYCLE — read docs/DESIGN_FP_ARCH_SP.md's SP-D block as the contract; the directive's call-ins ride this spine), relay-network substrate (anything that chains settlement-to-settlement transfers), and the outbound impression/espionage tap ladder as the model for information moving with people.` + CENSUS_TAIL, { label: 'census:movement', phase: 'Census', model: 'opus' }),
  () => agent(GROUND + `

CENSUS E — CULTURE, IDENTITY, AND CHARACTER. Census: traditions and cultural-blending machinery (what exists for a settlement's culture changing under contact — the occupation/assimilation surfaces), the law-band vocabulary (lawWordFor in src/domain/worldPulse/lawWord.js — NEW, the estate's single law-band spelling lawless|balanced|lawful; the directive's lawful-relay-vs-chaotic-skim and block-cohesion forks read THIS), alignment axes and where actor character lives, the bandFamilies intensity/severity ladders (frozen — NEVER a new decay law or scale; the directive's memory-fade and bond-decay MUST ride existing decay families), diaspora/migrant memory precedents, mercenary/adventurer representations if any, faith-as-culture constraints (the engine is theologically agnostic — veteran cultural introduction attempts are CONTESTED BELIEF, never adjudicated truth), and npc cohort machinery relevant to origin-tagged blocks.` + CENSUS_TAIL, { label: 'census:culture', phase: 'Census', model: 'opus' }),
  () => agent(GROUND + `

CENSUS F — THE DIRECTIVE ITSELF AND SIBLING-VOLUME COORDINATION. Read ${DIRECTIVE} COMPLETELY — the owner intent verbatim plus sections (a) through (p) and refinements K1-K8; produce a faithful section-by-section inventory (each section: its mechanism in 2-3 sentences, its dependencies on other sections, its dependencies on existing machinery). Then read ${HABIT} (the habit-conditioning directive: the HB-1 move-vocabulary coordination warning, law-band volatility, believed-doctrine) and docs/DESIGN_FP_ARCH_ES.md section headers (doctrine sheets, tap ladder) and docs/DESIGN_FP_ARCH_WY.md section headers, plus docs/SOL_QUEUE.md (the build order and where war-family rows sit). RETURN: the directive inventory, the coordination-contract list (every point where this volume must share vocabulary or machinery with HABIT / WAYFARE / ESPIONAGE / the built WR lanes), the collision map (surfaces two volumes both want to mint — flag each for the architect), and THE LAW CHECKLIST this volume must satisfy: THE PROMISE (lived history immutable, living futures, tuning owner-signed), FINITE-SEMANTICS (typed buckets, closed vocabularies, AI clerk-never-writer), the LOADED-DICE law, the NEWS ADDRESS LAW, faith agnosticism, product boundary 6 (setting-agnostic: the cloud-world test — no terrain-bound physics), world-only/sub-century/never-resolve-named-fate, LEGIBILITY (glance-sentence-table), GAME-GRADE UX.` + CENSUS_TAIL, { label: 'census:directive+siblings', phase: 'Census', model: 'opus' }),
])

phase('Architect')
const censusBundle = JSON.stringify({ A: cenA, B: cenB, C: cenC, D: cenD, E: cenE, F: cenF })
const architect = await agent(GROUND + `

YOU ARE THE ARCHITECT OF THE WAR-CIRCULATION VOLUME. The owner directive is at ${DIRECTIVE} — read it COMPLETELY first; every section (a)-(p) and every refinement K1-K8 is BINDING design intent unless it contradicts a constitutional law, in which case you PARK the conflict as a chair question, never silently resolve it. Six censuses of the estate follow (verify any load-bearing claim against the tree before building on it): ` + censusBundle + `

WRITE THE VOLUME to ${VOLUME} (create the file; it is a draft for landing at the next integration fold — repo-doc conventions, but do NOT touch the repo). THE VOLUME MUST CONTAIN, in the established FP-volume form: (0) the owner intent verbatim and the law checklist with a compliance line per law — including the conservation constitution: THE PEOPLE LEDGER is the spine, people are CONSERVED through census - block - army - losses/returns/orphans - reabsorption into census OR defense OR the free-lance pulse, origin-tagged all the way; (1) THE MODEL — the full causal architecture: contribution credit WRITTEN AT ARRIVAL never at dispatch; the war-stance ladder (materiel = joined-but-minimal, scaled casus/spoils/terms-weight); lawful RELAY networks vs chaotic SKIM riding lawWordFor; composite armies with origin-tagged blocks; news-gated stay/return/defect/resist forks (a defection fork opens only when NEWS of the triggering event ARRIVES at the block — hop-worn, per the believed world); law-band block cohesion; realized-stance drift from TIME-INTEGRATED contribution share; comradeship and cultural-blend integrals (share x duration); max thresholds + fission into independent units + the concentration calculus (mass fast-burn vs endurance envelopes — carrying capacity is what the economy feeds); partial wick-apart; brigandage targeting the NEAREST settlement (the embattled state, contested on defense + internal security); the map cap tied to supplies/settlements/tiers; hop-and-shed dissolution as an EXTENSION of wayfare mover-absorption (park where capacity, shed town by town); populations running the SAME parking strategy in competition; diaspora bonds scaling with how-many x how-far x how-long-ago, decayed by MORTALITY not by calendar; veterans-first muster with the three consequences (inherited character, burned veteran capital producing greener armies, the field-or-garrison fork); the domestic order dividend (one living veteran cohort read three ways: anti-siege defense, the embattled contest, crime suppression) and embattled-breeds-crime with the smuggling loop; (2) THE MODULE MAP — extend-never-fork: every new mechanism attached to the census-named existing surface, new leaves only where ceilings force them, budgets stated; (3) THE WAVE SPLIT — group (a)-(p) into buildable waves with strict dependencies (a core arc landing the ledger + credit + stance, then the composite/dissolution arc, then the domestic arc), each wave: flag (CQ5 trio, dark-buildable, four-fence + lit-mutant + guard doors individually), pins with the HARDEST NEGATIVES named (conservation walker — no person created or destroyed except by named events; arrival-not-dispatch credit pin; news-gate pin — a defection before news arrival is a RED; the same-rng-draw-count dark/lit pin), dormancy story, tuning constants (RAW-AUTHORED, owner-signed at soak per THE PROMISE), edit verbs + DM parity, alignment line; (4) THE COORDINATION CONTRACTS — explicit shared-vocabulary contracts with HABIT (HB-1 move-vocabulary: this volume does NOT mint move names the habit volume also wants — state the arbitration), WAYFARE (movers/hops/supplyCargo), ESPIONAGE (doctrine sheets for composite armies), and the built WR surfaces (accumulatedAttrition never realm warExhaustion — one-clause veto; attribution by RECONSTRUCTION never id-splitting); (5) HAZARD COMPLIANCE — the standing classes: no new decay laws (bandFamilies owns them), no new intensity ladder, hopWeeks per-digest, banding-is-lossy (one band per clause), dead-band shapes, capacity-ratio POINTS-GAP class, fixture-mirrors-deriver, config-slot silent keys; (6) OPEN QUESTIONS — every unresolved fork parked with a recommendation, chair-facing. Style: LEGIBILITY law (glance - sentence - table); closed vocabularies named in full; no em-dash-authored ratchet violations; ASCII-safe punctuation. Target completeness over brevity — this is the family's largest directive and the volume should match the advance-epoch volume's depth (~3,000+ lines). RETURN: a summary of the volume structure (section list with one line each), the wave count, the coordination contracts, and the open-questions list. The volume itself lives at ${VOLUME}.`, { label: 'architect:WC-volume', phase: 'Architect' })

const rounds = []
let lastVerdict = null
for (let round = 1; round <= 3; round++) {
  const cohere = await agent(GROUND + `

YOU ARE ADVERSARIAL COHESION ROUND ${round} FOR THE WAR-CIRCULATION VOLUME at ${VOLUME}. The owner directive is at ${DIRECTIVE}. Default posture: the volume is incoherent until proven otherwise. Read BOTH completely. HUNT: (1) FIDELITY — every directive section (a)-(p) and refinement K1-K8 accounted for in the volume, none silently dropped, none diluted (quote the directive line and the volume's answer for any you challenge); (2) CONSERVATION — trace the people ledger through EVERY path the volume describes (muster, reinforcement arrival, casualty, return, defection, fission, wick-apart, hop-and-shed, brigandage, absorption, the free-lance pulse, veteran aging-out): any path where people appear or vanish without a named event is a FINDING; (3) INTERNAL CONTRADICTIONS — two sections reading the same quantity with different definitions, a vocabulary used before it is minted, a wave depending on a later wave; (4) LAW VIOLATIONS — THE PROMISE, finite semantics, loaded dice, news address, faith agnosticism, the cloud-world test, sub-century, never-resolve-named-fate, no-new-decay-laws; (5) ESTATE TRUTH — spot-check the volume's module-map claims against the LIVE tree (read-only): a volume that extends a symbol that does not exist, or misstates a ceiling, is wrong; (6) SIBLING COLLISIONS — the HB-1 move-vocabulary arbitration, wayfare extension points, espionage doctrine contracts: is each stated, specific, and two-sided? (7) BUILDABILITY — could an implementer build wave 1 from this volume alone without a question? Name what is missing. RETURN structured JSON only: verdict COHESIVE (zero findings of substance) or AMEND, findings[] (each: section, the defect, the concrete repair), notes.`, { label: 'cohere:round' + round, phase: 'Cohere', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['COHESIVE', 'AMEND'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })
  rounds.push(cohere)
  lastVerdict = cohere && cohere.verdict
  if (!cohere || cohere.verdict === 'COHESIVE' || !cohere.findings || cohere.findings.length === 0) break
  await agent(GROUND + `

YOU ARE THE AMENDER, ROUND ${round}, for the volume at ${VOLUME}. The directive is at ${DIRECTIVE}. Apply EVERY finding below faithfully, editing the volume in place — EXCEPT: any finding whose repair would contradict the owner directive's stated intent goes into the volume's OPEN QUESTIONS section as a parked chair question with both positions stated, never silently resolved either way. Findings: ` + JSON.stringify(cohere.findings) + ` Keep the volume's structure and style; update the section list if sections move; re-read any live-tree claim a finding disputes before amending it. RETURN: per-finding disposition (APPLIED with the edit summarized, or PARKED with the question), and the volume's new approximate line count.`, { label: 'amend:round' + round, phase: 'Amend', model: 'opus' })
}

phase('Attest')
const attest = await agent(GROUND + `

YOU ARE THE FINAL ATTESTATION over the war-circulation volume at ${VOLUME}, after ${rounds.length} cohesion round(s). Read the volume and the directive at ${DIRECTIVE} completely, fresh. Answer FIVE questions with executed evidence (quotes and line references from both documents): (1) is every directive section (a)-(p) + K1-K8 dispositioned (built-into-a-wave, folded-into-another-section, or parked-with-reason)? Produce the 24-row disposition table. (2) does the people-ledger conservation story survive an adversarial trace end to end? (3) are the four coordination contracts (HABIT, WAYFARE, ESPIONAGE, built-WR) each specific enough that the OTHER volume's implementer could honor them without reading this whole volume? (4) is wave 1 buildable from the volume alone? (5) what are the open questions, verbatim, for the chair? RETURN structured JSON: attestation ATTESTED or NOT-ATTESTED, dispositionTable (24 rows), residuals[], openQuestions[], volumeLineCount, notes.`, { label: 'attest:final', phase: 'Attest', model: 'opus', effort: 'xhigh', schema: { type: 'object', required: ['attestation', 'openQuestions'], properties: { attestation: { type: 'string', enum: ['ATTESTED', 'NOT-ATTESTED'] }, dispositionTable: { type: 'array', items: { type: 'string' } }, residuals: { type: 'array', items: { type: 'string' } }, openQuestions: { type: 'array', items: { type: 'string' } }, volumeLineCount: { type: 'number' }, notes: { type: 'string' } } } })

return { architectSummary: architect, rounds, lastVerdict, attest, volumePath: VOLUME }