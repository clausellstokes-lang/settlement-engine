export const meta = {
  name: 'fp-integration-fold-drafting',
  description: 'Draft the ES + WY folds into the FP volume: promoted per-program files, composed parent deltas, queue rows, amendment texts. READ-ONLY on the repo — no commits, no vitest.',
  phases: [
    { title: 'Promote', detail: 'ES and WY drafts promoted to repo-form files, in parallel' },
    { title: 'Compose', detail: 'single-writer parent deltas + queue rows and amendment texts' },
    { title: 'Cohesion', detail: 'count coherence, rulings fidelity, no gated item executed' },
    { title: 'Revise', detail: 'close all nine findings under the chair rulings CR-FP-12 + CR-ES-6' },
    { title: 'Recheck', detail: 'verify every finding closed, anchors unique, no new incohesions' },
    { title: 'Polish', detail: 'close the six residuals from the recheck' },
    { title: 'Final', detail: 'confirm the six closed + full anchor sweep' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const SCRATCH = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c44e5d99-2ba5-49d5-a554-40b68534c8eb/scratchpad'
const ES_DRAFT = SCRATCH + '/espionage-arch/DESIGN_ESPIONAGE_ARCHITECTURE-DRAFT.md'
const WY_DRAFT = SCRATCH + '/wayfare-arch/DESIGN_WAYFARE_ARCHITECTURE-DRAFT.md'
const OUT = SCRATCH + '/fold-pass'

const PREAMBLE = 'VERIFY-FIRST PREAMBLE: this brief was written by the session chair (Fable) of a long-running, owner-delegated build program on the SettlementForge repo. It references real files you can and should verify before acting; nothing here asks you to exfiltrate, delete, or push. Work in the tree at ' + TREE + ' (branch claude/composite-r4). You are READ-ONLY on the repo: do NOT edit, create, or delete ANY file under the repo tree; do NOT run vitest, npm, or any gate (another lane owns the tree right now); do NOT run any git command that mutates state (status/log/show are fine). You write ONLY under ' + OUT + '/ (create it if needed). After writing any file, byte-scan it with python3 for NUL bytes (open rb, assert 0x00 not in data) — the Write tool has minted raw NULs from escape sequences seven times in this program; if a NUL appears, repair by byte replace and re-scan. LIVE CODE OUTRANKS EVERY DOCUMENT TABLE: navigate by symbol, never by quoted line numbers. Your final message is a data return, not prose for a human.'

const RULINGS = [
  'THE CHAIR RULINGS (Fable, 2026-08-05 — bake these VERBATIM where the documents call for the answers; never re-rule, soften, or extend one; every ruling is vetoable and will be recorded in FABLE_VALIDATION_QUEUE.md at the fold commit):',
  '',
  'FP volume section 11 (CR-FP-3..CR-FP-10; CQ2 and CQ5 were ruled earlier this era and stand):',
  'CR-FP-3 (Q1 posture-read naming): ACCEPT the synthesis recommendation — keep postureOf; rename ONLY the colliding second export to courtRiskAppetiteOf at SP-C module; one-row constitution erratum; import-source pin set + both-site headers land regardless of arm.',
  'CR-FP-4 (Q3 IN-0a envelope transport): ACCEPT — primary road: each consumer reads the PRIOR tick applied brokerage_plant events at its own head (zero new keys, zero kernel edits, law-M one-week lag); VERIFY-AT-BUILD the metadata retention; fallback = the pendingPlants conditional deposit under the D-3 contract; both fail = STOP for a chair-signed kernel seam.',
  'CR-FP-5 (Q4 J-INT-13 pre-recording): ACCEPT — pre-record the DARK arm now in FABLE_VALIDATION_QUEUE.md (lit-kind pools ship behind a prose-version flag; every existing golden byte-identical); the owner may later take the re-record arm with the WR-0b field-level diff.',
  'CR-FP-6 (Q6 arrivals feeding mass_migration): ACCEPT NO — deliberately deferred beside J-POP-14; ONE owner ruling covers both arrival-side couplings; this goes on the owner queue, not into any wave.',
  'CR-FP-7 (Q7 D-W3 formal close): COUNTERSIGN the cap-raise arm — the trim arm is forbidden by the spine floor law (measured: five puts all four kinds below their own SP-6 floor); walkers read floors from significance; SP-E shared helper retires the fixed-five class. D-W3 is formally CLOSED.',
  'CR-FP-8 (Q8 sale door vs one-instrument law): ACCEPT — not in this build; GR-2 pins the refusal UNCHANGED (fenced baseline); harmonization recorded as a post-GR-2 candidate owned jointly with the war chair.',
  'CR-FP-9 (Q9 applyWorldPulse second storageMonths fold): PRE-RULED REPORTED-NOT-DEFECT — the banked file own treaty fold; TR-4 routes zero traffic through it; the sixth-writer scan fences it; the queue row is filed at the fold so the lane never stalls.',
  'CR-FP-10 (Q10 errand ledger address): CONFIRM keep-in-place — SP-D generalizes worldState.envoyErrands IN PLACE; the migration arm is DECLINED (owner-gated persistence churn on a live save shape, zero behavioral payoff); the constitution owes the erratum row.',
  '',
  'ES volume section 7 (CR-ES-1..CR-ES-5):',
  'CR-ES-1 (Q1 Roads law-5 amendment): SIGNED — the section 3.11 amendment: flag-gated GRADED COUNCIL-WEIGHT DISCOUNT via the memberNpcIds first consumer; isOffStage untouched; dark worlds byte-identical; the lit shift disclosed. The literal full-off-stage reading is REFUSED as implementation for the draft own reasons (six kernels, every lit-roads golden, a trade trip is not a hostage-taking).',
  'CR-ES-2 (Q2 anonymity-law amendment): SIGNED, amended-in-place in BOTH homes — the informationStatecraft.js BOUNDARY header block AND DESIGN_FP_INFORMATION.md section 1b (law and its phrase-scan enforcer amend together); the war volume gets exactly ONE cross-reference line beside the WR-7 hostage machinery, never the amendment text. The amendment text in the draft is FINAL and verbatim; the BY-THE-ENGINE qualifier is load-bearing and must survive every edit (FOREIGN_GUEST_HOLD_CLOSE_REASONS already contains death — an unqualified no-fates would outlaw the war lane authored closes). FLAG THIS RULING with a warning marker in the ledger row: it amends the header of an owner ruling (2026-07-19), preserving its core (the engine still never executes, permanently turns, or ends a named character) while admitting capture/hold/ransom/release, which the owner own espionage directive requires. One-clause veto restores the old header.',
  'CR-ES-3 (Q3 seat-character vocabulary break): COUNTERSIGNED (the session chair holds the war chair this era) — unify on the CONSUMER vocabularies (lawless/merciful/holding) by retargeting the three module-private word functions in warSeatBooks; both-vocab equality pin; until that lands, ES-2 ships the captor-leniency arm DARK behind the ruling absence, declared not silent.',
  'CR-ES-4 (Q4 spy-before-decision sites): ACCEPT — this program wires the sovereignty buyer, GR-2 pact answers, and its own cadence (J-ES-13); the WAR CHOOSER (settlementStrategy, frozen surface) is DEFERRED to a future owner ruling — owner queue row.',
  'CR-ES-5 (Q5 third-party alliance topology): PARKED, recorded — ship without it; the pairwise allianceLabel REFUTE covers the drama; the new persisted key family ACQUIRE question goes to the owner as a one-line future-widening row. Never smuggle a surface into the SP-B family.',
  '',
  'Standing rulings that the fold RECORDS as already ruled (do not re-open): CR-FP-1 (the desk-agreement seam is authority-routed), CR-FP-2 (the proseNumerics re-record is instrument reconciliation, not drift), CQ2 (early motion authorized for GR-0+GR-1+TR-1+TR-9-contract), CQ5 (flag one-commit law affirmed; WR flags recorded as pre-manifest history). The Wayfare chair translation stands: low supply drags the army OWN accumulatedAttrition, never the realm warExhaustion — seam-1 contract; owner one-clause veto surface at the WY-8a ledger row.',
  '',
  'OWNER-GATED ITEMS THE FOLD MUST NOT EXECUTE (record, park, never build): the F9 supplyCargo sign-off row (no build until the owner signs it), the ES Q5 alliance-topology ACQUIRE, the war-chooser spy wiring, the Q6/J-POP-14 arrival couplings, every tuning band (raw-authored until owner-signed per THE PROMISE).',
].join('\n')

phase('Promote')
const [esPromote, wyPromote] = await parallel([
  () => agent(PREAMBLE + '\n\n' + RULINGS + '\n\nTASK — PROMOTE THE ESPIONAGE VOLUME TO REPO FORM. Read the COHESIVE espionage draft WHOLE at ' + ES_DRAFT + ' (1,592 lines). Read the FP parent volume header + section 3 + section 5 + section 9 + section 11 at ' + TREE + '/docs/DESIGN_FP_ARCHITECTURE.md for the family conventions, and skim one sibling (docs/DESIGN_FP_ARCH_IN.md) for the per-program file voice. Produce ' + OUT + '/DESIGN_FP_ARCH_ES.md — the draft promoted to its final repo home, with exactly these transformations and NOTHING else silently changed: (1) header provenance updated — it is now a landed member of the docs/DESIGN_FP_ARCH_* family, compiled from the drafts named, chair rulings CR-ES-1..5 RECORDED (2026-08-05); keep the live-code-outranks and navigate-by-symbol laws; add one line: HEAD at landing is stamped by the landing lane (leave the literal placeholder HEAD-AT-LANDING for it). (2) Convert the five section-7 open questions to RULED entries: keep each question and recommendation text intact, append the ruling from the CHAIR RULINGS block above (label them CR-ES-1..5), and retitle the section OPEN CHAIR QUESTIONS - ALL FIVE RULED (each vetoable). (3) Repoint any reference to scratchpad paths: the census files remain compile-time inputs archived in the session scratchpad — say so once in the header (live code outranks them; re-measure at build); the draft self-reference becomes docs/DESIGN_FP_ARCH_ES.md. (4) Where the draft carries the anonymity amendment text (section 7 Q2), mark it THE SIGNED TEXT — the landing lane applies it to both homes. Do not renumber sections, do not rewrite prose, do not touch wave specs. Byte-scan the output. RETURN (as your final text): a JSON object with keys file (the path you wrote), lineCount, transformsApplied (array of short strings), and anomalies (array — anything in the draft that contradicts the parent volume or the rulings, found while promoting; empty if none).', { label: 'promote:ES', phase: 'Promote', model: 'opus' }),
  () => agent(PREAMBLE + '\n\n' + RULINGS + '\n\nTASK — PROMOTE THE WAYFARE VOLUME TO REPO FORM. Read the COHESIVE r3 Wayfare draft WHOLE at ' + WY_DRAFT + ' (1,563 lines). Read the FP parent volume header + section 3 + section 5 + section 9 + section 11 at ' + TREE + '/docs/DESIGN_FP_ARCHITECTURE.md for the family conventions, and skim one sibling (docs/DESIGN_FP_ARCH_TR.md) for the per-program file voice. Produce ' + OUT + '/DESIGN_FP_ARCH_WY.md — the draft promoted to its final repo home, with exactly these transformations and NOTHING else silently changed: (1) header provenance updated — a landed member of the docs/DESIGN_FP_ARCH_* family, chair rulings Q1..Q5 RECORDED (they were ruled at r1: Q1 conservation as drafted, Q2 home units DECLARE-ABSENT, Q3 WarFaith deployment arrows DELETED under the ambiguity law, Q4 popup = PortablePopup modal v1 with the anchored popover recorded-not-built, Q5 caravan layer ships against non-food shipments with an honest legend), plus the r3 chair translation (accumulatedAttrition never warExhaustion) restated in the header hazard block; keep live-code-outranks; add the literal placeholder HEAD-AT-LANDING for the landing lane. (2) Mark the section-7 (or wherever the draft holds them) question entries RULED with those rulings inline. (3) The draft self-reference becomes docs/DESIGN_FP_ARCH_WY.md; census files noted once as session-scratchpad compile inputs, re-measure at build. (4) Preserve VERBATIM the section-5b queue-insertion block (it is verbatim-landable), the F9 owner-gated sign-off clause, the section-5b item 9 war-volume pointer sentences, and the section-5b item 10 census obligation with its seam-1 tripwire. Do not renumber sections, do not rewrite prose, do not touch wave specs. Byte-scan the output. RETURN: a JSON object with keys file, lineCount, transformsApplied, anomalies (same semantics as the sibling agent).', { label: 'promote:WY', phase: 'Promote', model: 'opus' }),
])

phase('Compose')
const [parentDeltas, queueAmend] = await parallel([
  () => agent(PREAMBLE + '\n\n' + RULINGS + '\n\nTASK — COMPOSE THE PARENT-VOLUME DELTAS (you are the SINGLE WRITER for every edit touching docs/DESIGN_FP_ARCHITECTURE.md and the prefix admission). Inputs: the two promoted files ' + OUT + '/DESIGN_FP_ARCH_ES.md and ' + OUT + '/DESIGN_FP_ARCH_WY.md (read their headers + wave/flag/seam summaries + queue blocks), the two source drafts at ' + ES_DRAFT + ' and ' + WY_DRAFT + ' (authoritative for the numeric deltas: ES takes waves 60 to 68, flags 43 to 44 with espionageEnabled row 44, seams 45 to 58; WY takes waves 68 to 75, flags 44 to 52 with rows 45-52 and armySupplyEnabled at 52, seams 58 to 66, NINE conditional fields with F9 supplyCargo owner-gated — carry the drafts own stated arithmetic, never recompute a different one; if a draft number contradicts what you find, report it in anomalies and use the draft number), and the LIVE parent ' + TREE + '/docs/DESIGN_FP_ARCHITECTURE.md. Produce ' + OUT + '/PARENT-DELTAS.md: an ordered list of exact edit pairs (for each: the file path, an OLD block quoted VERBATIM from the live file with enough surrounding context to be unique — verify uniqueness with grep -c before writing it down — and a NEW block), covering: (a) the parent header aggregate line (eight programs compiled becomes ten; wave/flag/seam totals); (b) section 3 flag family: 43 becomes 52, the two new flag rows appended in the family table voice (espionageEnabled 44; the eight WY rows 45-52 exactly as the WY draft section-5b names them); (c) section 5 wave charter: the ES-0..7 insertion joint with SP-B/SP-B2 and the WY wave insertion, exactly where each draft says it slots (the WY section-5b block is verbatim-landable — carry it verbatim); (d) section 9 seam matrix delta note 45 to 66 with the two program rows; (e) section 11: append a RULED addendum recording CR-FP-3..CR-FP-10 verbatim from the rulings block (do not delete the question text — append the rulings as a dated addendum block); (f) the closing compiled-read-only italics: inputs now include the ES and WY volumes. ALSO: read the CHARTERED_VOLUME_PREFIXES pin (grep -n CHARTERED_VOLUME_PREFIXES ' + TREE + '/tests — find the test file; the memory says couplingRegistry.test.js around line 45 pins a closed NINE-set) and READ the walker semantics around it: produce the exact edit pair admitting ES and WY as the tenth and eleventh prefixes, and state whether the walker demands at least one registry row per admitted prefix (read the walker code; if yes, produce the minimal correct registry row additions the fold commit owes same-commit; if no, state that zero rows is green and why, quoting the predicate). Byte-scan the output. RETURN: JSON with keys file, editCount, prefixAdmission (an object: testFile, demandsRows true/false, rationale), anomalies.', { label: 'compose:parent-deltas', phase: 'Compose', model: 'opus' }),
  () => agent(PREAMBLE + '\n\n' + RULINGS + '\n\nTASK — DRAFT THE QUEUE ROWS, LEDGER ROW, AND AMENDMENT EDIT PAIRS. Inputs: the ES draft at ' + ES_DRAFT + ' (its queue-insertion language — find the section that names where ES slots: joint with SP-B/SP-B2; and the section-7 Q2 SIGNED amendment text), the WY draft at ' + WY_DRAFT + ' (its section-5b verbatim-landable queue insertion and its section-5b item 9 two pointer sentences for the war volume), and these LIVE files: ' + TREE + '/SOL_QUEUE.md (read the row format and current order), ' + TREE + '/docs/FABLE_VALIDATION_QUEUE.md (read the three most recent rows for the ledger-row voice), ' + TREE + '/docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md (find the WR-7 hostage machinery section and the two J-D11(b) sites the WY draft names), ' + TREE + '/docs/DESIGN_FP_INFORMATION.md section 1b (the anonymity boundary), and the informationStatecraft.js boundary header (grep -n for the 2026-07-19 owner-ruling block in ' + TREE + '/src/domain/worldPulse/informationStatecraft.js). Produce ' + OUT + '/QUEUE-AND-AMENDMENTS.md with four parts, each as exact edit pairs (OLD verbatim-unique from the live file, NEW block): PART 1 — SOL_QUEUE.md insertions for ES (joint with SP-B/SP-B2, per the ES draft) and WY (verbatim from its 5b block, slotted where it says — WY-1 after SP-E etc.). PART 2 — the FABLE_VALIDATION_QUEUE.md ledger row for the integration fold: records the COHESIVE verdicts (ES run wf_60586a6a-68c; WY run wf_6b8020de-71c r3), all rulings CR-FP-3..10 + CR-ES-1..5 in one-line-each compressed form, the warning-flagged CR-ES-2 owner-veto surface, the accumulatedAttrition translation veto surface, the parked owner items (F9 sign-off, ES Q5 acquire, war-chooser wiring, Q6/J-POP-14), and the fold-commit obligations (prefix admissions; the section-5b item 10 census executes at the fold or the commit is incomplete). PART 3 — the anonymity amendment: exact edit pairs for BOTH homes (the informationStatecraft.js header block and DESIGN_FP_INFORMATION.md section 1b) inserting the SIGNED amendment text from the ES draft verbatim (the BY-THE-ENGINE qualifier must survive), plus the ONE cross-reference line beside the WR-7 hostage machinery in the war volume. PART 4 — the two J-D11(b) pointer sentences into the war volume at the sites the WY draft names. IMPORTANT: the section-5b item 10 census (benefitYield / war-economy drains / occupier-side cost) is EXECUTED BY THE LANDING LANE, not by you — state it as a landing-lane obligation at the top of PART 2. Byte-scan the output. RETURN: JSON with keys file, parts (array of 4 short summaries), anomalies (anything live text contradicts — a J-D11(b) site that does not exist as described, a missing 1b section, etc.).', { label: 'compose:queue-amendments', phase: 'Compose', model: 'opus' }),
])

phase('Cohesion')
const coh = await agent(PREAMBLE + '\n\n' + RULINGS + '\n\nTASK — ADVERSARIAL COHESION CHECK ON THE FOLD PACKAGE. You are the skeptic; your default posture is that the package contains a defect and your job is to find it. Read ALL FOUR outputs in ' + OUT + '/ (DESIGN_FP_ARCH_ES.md, DESIGN_FP_ARCH_WY.md, PARENT-DELTAS.md, QUEUE-AND-AMENDMENTS.md), the two source drafts (' + ES_DRAFT + ', ' + WY_DRAFT + '), and spot-verify against the LIVE tree at ' + TREE + '. Check specifically: (1) COUNT COHERENCE — waves 60 to 68 to 75, flags 43 to 44 to 52 (espionageEnabled 44, armySupplyEnabled 52), seams 45 to 58 to 66, nine WY fields with F9 owner-gated, ES thirteen seams, consistent across every produced file with ZERO stale residues of the old totals in the NEW text. (2) RULINGS FIDELITY — every CR-FP-3..10 and CR-ES-1..5 baked verbatim where it belongs, none re-ruled or softened; the CR-ES-2 BY-THE-ENGINE qualifier intact in every copy of the amendment text. (3) EDIT-PAIR SOUNDNESS — sample at least eight OLD blocks across the delta files and verify each is verbatim-present and UNIQUE in its live target file (grep -c equals 1); a first-match retarget or a stale OLD block is a finding. (4) NO GATED ITEM EXECUTED — nothing in the package builds or schedules the F9 sign-off, the ES Q5 acquire, the war-chooser wiring, the Q6 couplings, or any tuning band. (5) PREFIX ADMISSION — the couplingRegistry edit matches the live test text and the walker-row question is answered from the walker code, not assumed. (6) NOTHING LOST — the WY verbatim blocks (5b queue insertion, item 9 pointer sentences, item 10 census obligation with tripwire) and the ES signed amendment text survived promotion unaltered (diff the promoted sections against the drafts). (7) CONSOLIDATION CANDIDATE — if the ES catch/tap ladder machinery and the WY encounter/interception table duplicate a shared shape, RECORD it as a question for the chair (do not unify anything yourself). RETURN structured JSON only.', { label: 'verify:cohesion', phase: 'Cohesion', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings', 'notes'], properties: { verdict: { type: 'string', enum: ['LANDABLE', 'NEEDS-REVISION'] }, findings: { type: 'array', items: { type: 'object', required: ['severity', 'file', 'claim'], properties: { severity: { type: 'string', enum: ['HIGH', 'MEDIUM', 'LOW'] }, file: { type: 'string' }, claim: { type: 'string' }, evidence: { type: 'string' } } } }, consolidationCandidate: { type: 'string' }, notes: { type: 'string' } } } })

phase('Revise')
const revise = await agent(PREAMBLE + `

YOU ARE THE REVISION LANE for the fold package in ` + OUT + `/. The cohesion check returned NEEDS-REVISION with nine findings plus one chair question; the chair has ruled every one. The full verdict object: ` + JSON.stringify(coh) + `

APPLY EXACTLY THESE REVISIONS — the rulings below are FINAL; where a revision needs an OLD block from a live repo file, quote it verbatim at the CURRENT HEAD and verify uniqueness with grep -cF equal to 1 (note: another lane has been committing since d1cfdb67, so re-read every target file fresh; if an existing anchor in the package has drifted at the new HEAD, update it and say so in your return).

R1 (HIGH 0) — ADD THE SP-VOLUME EDIT PAIRS: docs/DESIGN_FP_ARCH_SP.md carries the two-member lighting discharge at two sites (near lines 448-453, the THIS-WAVE-PLUS-SP-B paragraph, and near 688-690, the WR-9-certification-lighting-order sentence). Author edit pairs amending BOTH to the three-member condition (SP-B + SP-B2 + ES-4, ES-4 the confirmation leg per the ES volume section 6 item 5), so the normative per-program file agrees with the amended parent. Append them to PARENT-DELTAS.md as a clearly-headed SIBLING-VOLUME DELTAS section.

R2 (HIGH 1 — chair ruling CR-FP-12) — THE CQ2 WIDENING IS REVERSED: ES-0 early motion is DECLINED. Rationale to bake: ES-0 edits two live war-lane files (warSeatBooks.js, warMagicGate.js) and the CR-ES-3 retarget is a cross-file war-lane vocabulary change with golden-shift exposure — not the pure-density zero-collision profile CQ2's four members shared; ES-0 runs in STRICT ORDER (it sits early in cycle 2 regardless, so nothing real is lost). Revise EDIT 25, EDIT 30, EDIT 18 and any other site so ES-0 is recorded as early-ELIGIBLE (a measured fact from the ES volume) while the CQ2 authorized set stays EXACTLY GR-0 + GR-1 + TR-1 + TR-9-contract. Record CR-FP-12 in the section-11 rulings addendum (EDIT 30) and the ledger row (QUEUE PART 2).

R3 (MED 2) — EDIT 11's exhaustiveness claim is corrected to the measured truth: the inherited phantom lives in THREE homes (the parent section-3 closing, section-9 seam row 4, and docs/DESIGN_FP_ARCH_SP.md near line 689), all three amended by this fold (the third via R1).

R4 (MED 3 + the consolidation question — chair ruling CR-ES-6) — THE WY ENCOUNTER TABLE ADMITS A FIFTEENTH ROW: spy-dwell detection — the covert operative x the host settlement watch, per-stop while dwelling — resolver espionageGauntlet.js, outcomes the SAME hostage/foreign-guest-hold family plus the leak/expulsion variants the ES volume section 3.3 names. Anti-vacuity moves 14 to 15. ES-2's charter gains a SAME-COMMIT obligation: mounting the resolver adds the table row. WY-6's walker is born seeing fifteen. THE CONSOLIDATION BOUNDARY IS RECORDED IN BOTH VOLUMES: the encounter-resolver REGISTRY is ONE (the WY table, closed, walker-scanned); catch/probability MATH stays per-resolver (the ES catch model is mathematics, not a registry). Amend: DESIGN_FP_ARCH_WY.md section 4 and its section-5 restatement (closed at fourteen becomes closed at fifteen, the ES row listed, the boundary note), DESIGN_FP_ARCH_ES.md's ES-2 charter, and the ledger row.

R5 (MED 4) — ADD THE MISSING WAR-VOLUME EDIT PAIR: one sentence into the CR-WR10-H paragraph of docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md (near lines 1617-1631) naming the three-member discharge (SP-B + SP-B2 + ES-4) — the ES volume section 6 item 5 requires it and the package dropped it silently.

R6 (LOW 5) — Remove the stale-title pointer sentence from PART 1.2's SOL_QUEUE NEW block (EDIT 33 repairs that title in the same commit, so the pointer would aim at a defect that never exists).

R7 (LOW 6) — The ES closing italics gain the WY-carries-on note, mirroring WY's own convention (this family makes 68 and 44; the WY volume lands in the same fold and carries the totals to 75 and 52).

R8 (LOW 7) — Restore the deleted blank line before the horizontal rule near DESIGN_FP_ARCH_ES.md line 53 (the setext-heading defect).

R9 (LOW 8) — Correct the PARENT-DELTAS header receipt: 34 OLD anchors verified, EDIT 35 is a declared no-edit verification.

R10 (the promote lane's own anomalies, chair-accepted) — In the promoted ES file: (a) beside section 3.9 or as a section-6 note, record the CR-ES-3 EXECUTED-MEASUREMENT RIDER — the ruling's substance is UNTOUCHED, but its cost note is corrected: lawWordFor is spelled with the consumer law vocabulary (lawless | balanced | lawful) and section 3.9's chaotic method arm respells to lawless in the SAME commit (else a dead branch or a forbidden fourth vocabulary); and the warSeatBooks retarget is a CROSS-FILE war-lane vocabulary change — envoyNegotiationPictureBuilder reads benevolent/lawful literals, warTermination re-emits both bands as ruler-prefixed keys, settlementStrategy enumerates those keys — so consumer-side retargeting lands in the SAME commit as the both-vocab equality pin, with golden-shift exposure declared. (b) Fix the section-6 clause-6 landing sentence to say rulings rather than questions, coordinated with the section-11 retitle edit so the two edits to that paragraph compose. (c) Normalize the informationStatecraft line-address split (43-50 vs 43-51) to a symbol-anchored spelling in both places.

Byte-scan every file you touch (python3, zero NULs, zero stray control bytes). RETURN: JSON with keys revisionsApplied (array, one line each), newEditPairs (count and targets), anchorsRechecked (how many, at what HEAD), anomalies.`, { label: 'revise:fold-package', phase: 'Revise', model: 'opus' })

phase('Recheck')
const recheck = await agent(PREAMBLE + `

YOU ARE THE RE-VERIFIER. The fold package in ` + OUT + `/ was revised under chair rulings CR-FP-12 (ES-0 early motion DECLINED; CQ2 stays exactly GR-0 + GR-1 + TR-1 + TR-9-contract) and CR-ES-6 (the WY encounter table admits the spy-dwell fifteenth row; registry unified, math per-resolver), plus seven mechanical closures. The revision lane reported: ` + JSON.stringify(revise) + `

The PRIOR verdict's nine findings (verbatim): ` + JSON.stringify(coh && coh.findings) + `

VERIFY: (1) every one of the nine findings is genuinely closed in the CURRENT package files — re-derive each from the files, do not trust the revision report; (2) every edit pair in the package (including the new SP-volume and war-volume pairs) has its OLD block verbatim-present and UNIQUE in its live target at the CURRENT HEAD — mechanically extract and grep -cF ALL of them, and report any drifted by another lane's commits; (3) CQ2 appears NOWHERE widened — grep the whole package for ES-0 beside early motion or authorized; CR-FP-12 and CR-ES-6 are recorded in the section-11 addendum, the ledger row, and (for CR-ES-6) both volumes; (4) the CR-ES-3 rider corrects cost only — the ruling's operative clauses (consumer vocabulary, retarget producer, both-vocab equality pin, captor arm dark until landed) are unsoftened; (5) the three-member lighting condition now agrees across ALL FIVE homes: parent section-3 closing, parent section-9 row 4, DESIGN_FP_ARCH_SP.md both sites, the war volume CR-WR10-H paragraph, and SOL_QUEUE row 13; (6) counts still coherent (75 waves, 52 flags, 66 seams, fifteen-row encounter table everywhere the table is counted); (7) byte-scan all package files. RETURN structured JSON only.`, { label: 'recheck:fold-package', phase: 'Recheck', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['LANDABLE', 'NEEDS-REVISION'] }, findings: { type: 'array', items: { type: 'object', required: ['severity', 'claim'], properties: { severity: { type: 'string', enum: ['HIGH', 'MEDIUM', 'LOW'] }, file: { type: 'string' }, claim: { type: 'string' }, evidence: { type: 'string' } } } }, anchorsDrifted: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Polish')
const polish = await agent(PREAMBLE + `

YOU ARE THE POLISH LANE. The recheck returned NEEDS-REVISION with exactly six residual findings, all narrow. The full verdict: ` + JSON.stringify(recheck) + `

CLOSE ALL SIX, chair pre-ruled: (P1, findings 1+2) THE DISCHARGE CENSUS IS RE-EXECUTED, NOT PATCHED — grep the LIVE parent docs/DESIGN_FP_ARCHITECTURE.md at the CURRENT HEAD for every two-member lighting-discharge spelling (search multiple spellings: SP-B + SP-B2, SP-B/SP-B2, the two wave ids, lighting-order, exactly-two phrasings), enumerate EVERY site including the tenth the recheck found in section 9, amend every one to the three-member condition, and write the exhaustiveness clause FROM your executed census output — the count is what you measured, never what a prior text said. (P2, finding 3) Restore CR-ES-3's operative clause VERBATIM at the spec site the recheck names: the captor-leniency arm ships DARK behind the ruling absence, declared not silent, until the retarget lands — no softening. (P3, finding 4) Purge the surviving CQ2-widening spelling from the verbatim-landing file the recheck names; afterward grep the ENTIRE package for ES-0 within three lines of early motion or authorized — the only surviving couplings must be the CR-FP-12 DECLINATION text; report the grep output. (P4, finding 5) The SOL_QUEUE ES row's binding-rulings list gains CR-ES-6 (the same-commit encounter-table-row obligation binds a wave on that row). (P5, finding 6) Unify the two corrections' measurement heads: both state the same measured-at-current-HEAD provenance. Byte-scan every file touched. RETURN: JSON with keys closures (six entries, each with its executed evidence snippet), censusCount (the discharge sites you measured and amended), anomalies.`, { label: 'polish:six-residuals', phase: 'Polish', model: 'opus' })

phase('Final')
const finalCheck = await agent(PREAMBLE + `

YOU ARE THE FINAL CHECK. The polish lane reported: ` + JSON.stringify(polish) + `

The six findings it was closing: ` + JSON.stringify(recheck && recheck.findings) + `

VERIFY: (1) each of the six is genuinely closed in the CURRENT package files — re-derive from the files; (2) the discharge census — independently re-grep the live parent at CURRENT HEAD for two-member spellings and confirm the package amends EVERY site and the exhaustiveness clause matches your independent count; (3) the CQ2 grep — run it yourself across the whole package, only the CR-FP-12 declination couplings survive; (4) ALL edit-pair OLD anchors across both delta files, mechanically extracted, each count exactly 1 in its live target at CURRENT HEAD (report the HEAD you measured at and any drift from 79bceff5); (5) byte-scan all package files. RETURN structured JSON only.`, { label: 'final:fold-package', phase: 'Final', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['LANDABLE', 'NEEDS-REVISION'] }, findings: { type: 'array', items: { type: 'string' } }, headMeasured: { type: 'string' }, notes: { type: 'string' } } } })

return { esPromote, wyPromote, parentDeltas, queueAmend, coh, revise, recheck, polish, finalCheck }