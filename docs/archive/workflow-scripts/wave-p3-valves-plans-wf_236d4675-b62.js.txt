export const meta = {
  name: 'wave-p3-valves-plans',
  description: 'P3 — THE VALVES + PLANS: competing overflow responses, the spatial law, earned promotion, persistent commitments',
  phases: [{ title: 'Build', detail: 'one Opus implementer', model: 'opus' }],
}
const ROOT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const SCHEMA = { type: 'object', required: ['agent', 'fixes'], properties: { agent: { type: 'string' }, fixes: { type: 'array', items: { type: 'object', required: ['finding', 'status', 'files', 'tests', 'notes'], properties: { finding: { type: 'string' }, status: { type: 'string', enum: ['fixed', 'skipped', 'deferred', 'blocked'] }, files: { type: 'array', items: { type: 'string' } }, tests: { type: 'string' }, notes: { type: 'string' } } } }, behavior_shifts: { type: 'array', items: { type: 'string' } }, discovered: { type: 'array', items: { type: 'string' } } } }

const BRIEF = [
  'You are an Opus IMPLEMENTER for SettlementForge. SLICE P3 of the DEMOGRAPHIC ENGINE: THE VALVES + THE PLANS. BINDING LAW: docs/DESIGN_DEMOGRAPHIC_ENGINE.md - section 5 (overflow, earned promotion, the metropolis ceiling), section 5b (THE SPATIAL LAW: the proximity band, any-settlement on BOTH ends - min separation from EVERY settlement tier-pair banded, max reach to the NEAREST settlement so the realm grows contiguously and never as isolated pockets; the USER EXCEPTION is total; saturation is DISCOVERED as an empty candidate set, never a stored census), section 5c (THE PLAN, NOT THE REROLL), and judgment blocks J-P6/J-P6b/J-P7/J-P8.',
  'Worktree: ' + ROOT + ' branch claude/composite-r4 @ ef54119e - verify with git rev-parse --abbrev-ref HEAD; stop on mismatch. FOREIGN, never touch: wizardNews/settlementRumors/heraldRouting/informationStatecraft/momentum/supplyWebWarfare + their tests, and artifacts/soak/**. Focused vitest only, one at a time.',
  '',
  'WHAT LANDED (build ON these, never fork): P1 the rates + the three readings; P1a the unfrozen decline, the viability ladder read (tierViabilityOf), and CONSERVED promotion (lit promotion requires the FULL next-tier minimum and mints nobody - your earned-ascension law is already half-built, do not re-implement it); P2 the homeostat, and CRITICALLY `competeForDestinations` in demographicsMigration.js - the exported seam that ranks reachable existing settlements, fills their spare capacity, and reports `unplaced` plus a closed refusal word. THAT SEAM IS YOUR FIRST QUESTION, ALWAYS. Wave E gives the satellite lane (steadingTopography, need-weighted placement, the orbit annulus, conservation-exact transfers). Wave G gives autoplacement scoring and tier-banded spacing.',
  '',
  'IMPLEMENT (P3 ONLY - no stressor coupling, no realm motive weights, no Herald prose beyond receipts; P4 owns those):',
  '1. THE COMPETING RESPONSES (the owner "where reasonable and appropriate", never a fixed order): when a settlement crosses the overflow band, score the available responses - send to existing capacity (ASK P2 FIRST), found a satellite, promote, invest in infrastructure, seek food imports, encourage emigration. Weighted by conditions, not ordered by rule. A wealthy centralized trade town becomes a city; a farm village with open land spins off steadings; a walled metropolis with no ground imports and intensifies.',
  '2. THE SPATIAL LAW (section 5b): every ENGINE founding must satisfy min-separation against EVERY existing settlement (tier-pair banded) and max-reach to its NEAREST. User placements are exempt, totally. The candidate enumeration is bounded (wave E capped set, Weyl-sampled - NEVER an arithmetic stride, that hazard is recorded) and its EMPTY result is the saturation signal, discovered per attempt.',
  '3. AT SATURATION, THE LADDER: no legal ground means pressure resolves through tier movement. Promotion is P1a existing conserved path - do not duplicate it. Demotion is tier drifts EXISTING path with P1a viability - do not add a second writer. Pin that a saturated realm stops founding and starts deepening, conservation-exact.',
  '4. THE PLAN (section 5c): the response draw fires on a band CROSSING, not per tick. The selection becomes ONE persistent plan per settlement (the scope trim is deliberate - this rides the existing proposal/commitment machinery, it is NOT a general planning system): proposed -> underway -> completed | failed | abandoned, with startup cost, duration, progress, failure conditions, a reconsideration threshold that is a band crossing rather than drift, a cooldown after abandonment, and a receipt naming why it was chosen. Draws keyed by (realm, settlement, episode, response) so an unrelated candidate cannot steal another settlement draws.',
  '5. FOUNDING COSTS (conservation): a satellite costs the parent POPULATION and capital; the child does not appear for free. Extend wave E conservation-exact transfer; add the capital cost.',
  '6. Extend the demographicsEnabled certification row (extend, never re-author).',
  '',
  'PINS with executed negative controls (revert/red/restore/green, quoted): DESTINATION COMPETITION BEATS FOUNDING (a half-empty reachable viable village absorbs the overflow and NO satellite is founded - then remove the village and one IS founded: the review own defect, made a test); NO FLAPPING (an oscillating-pressure fixture over 200 ticks produces ONE plan, not a dozen - the control shows the churn when the plan is removed); the proximity band on both ends (a founding never lands inside min-separation of ANY settlement, never beyond max-reach of its nearest; the user exception unaffected); SATURATION (a realm with no legal ground founds nothing and instead promotes - conservation-exact, and the empty candidate set is the ONLY signal); founding conserves (parent loses exactly what the child gains, JSON-round-tripped); dormancy byte-identical by object identity.',
  '',
  'HARD RULES: no migrations; no git mutations; no full suites/builds; NO ratchet raises; ZERO any-casts (conditional keys declared on the OWNING typedef); kernel math primitives; determinism (fork keyed per the section 5c rule, draw-accounted; seed-family totality, never single-seed; the Weyl sampler for any cell subsampling); no em dashes in src/domain string literals; conditional worldState keys drop-when-empty; the golden law - dark byte-identical, no generation or dormancy golden moves; reseal edge bundles if you touch their inputs. Return dense structured data: statuses, files, verbatim test tails, negative-control quotes, behavior shifts plainly, judgment calls vetoable, discovered.',
].join('\n')

phase('Build')
const r = await agent(BRIEF, { label: 'P3:valves-plans', phase: 'Build', schema: SCHEMA, model: 'opus' })
return { results: [r].filter(Boolean) }