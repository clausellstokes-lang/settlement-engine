export const meta = {
  name: 'build-cycle-21-wr8',
  description: 'WR-8: conquest + the razing, WIRED, under CR-WR8-A..D — two serial slices, each adversarially verified',
  phases: [{ title: 'Conquest', detail: 'N2 feasibility · N3 intent · N execution' }, { title: 'Razing', detail: 'R + R2 licenses + the atrocity casus #16' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); R-BLD-9 strict law (new files ZERO, whole-domain delta incl. type consumers); node ESM link + npm run build green on domain changes; goldens BEFORE wiring; one slice one commit + queue row; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". conquestDoctrineEnabled stays DARK — dormancy goldens BYTE-IDENTICAL (hash pre/post, quote). Nothing pushed. CONCURRENCY: cycle 20 is LIVE on generators (factionRoles/factionArchetypes — the boot fix), primitives, and components — fenced from you; queue docs carve-or-commit-body. THE RULINGS BIND (all @ 8c8eda59 + the queue blocks, cite them): CR-WR8-A edge-type flips to hostile on existing victim-adequate edges (D5 undying arm NOT taken); CR-WR8-B "extreme" = hostile type AND resentment ≥ its 0.78 baseline AND live grievance/atrocity-casus ≥ the license-adequacy band; CR-WR8-C the atrocity pair = atrocity_answer ↔ atrocity_atoned (#16 in the 15↔15 taxonomy; walker-enforced totality+bijection — register WHAT_PHRASES + heraldRouting or the walkers red); CR-WR8-D build WIRED (the pulse mouths have headroom: applyWorldPulse is a split family @ 27fa08d7; follow R-BLD-1 stage-neuter reachability). K3/K4/E3 bind everywhere: feasibility and intent are PURE BELIEF composites (per-party pictures, no merged estimates, no truth reads in negotiation paths).`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}

phase('Conquest')
const c = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE W8-A — WR-8's CONQUEST HALF, per docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md §5 WR-8 (read the WHOLE section + its Pins/Bands/Lifecycle blocks + the substrate-gate RULED text):
SLICE 1 — FEASIBILITY (N2): the pure belief composite (believed relative strength, believed coalition reach, believed home-front reserves) feeding four consumers (motive, movement, terms range, the K.6 vote); the ASYMMETRY BAND (being-conquered moves behaviour more than conquering); PINS: the mistaken-feasibility pin BOTH directions receipted; the books-collapse fixture (existential threat collapses G's two books); the feasibility-asymmetry DIRECTIONAL pin.
SLICE 2 — INTENT (N3): capability never implies intent; the conquest-wanted read (disposition, own history, alignment, patron deity's nature, the enemy's BELIEVED nature); the I4 deception road reaches CONQUEST INTENT ONLY (R2 closed it for razing); mercy as a characterful receipt.
SLICE 3 — CONQUEST EXECUTION (N): the OVERWHELMING gate (negative case: clearly-winning-but-not-overwhelming still negotiates); physical and slow (law M); occupation is NOT annexation (K1 siege_occupation, garrison cost, unrest, revolt standing); THE INHERITANCE COUNTERFORCE (the victor's K_food sums over what it now holds — a realm that conquers a dying neighbour has annexed a famine, derived from EXISTING engines).
Wire each slice's pulse stage per R-BLD-1 (stage-neuter controls); one commit per slice + queue rows; the WF receipt nit rides along: the 04348681 queue row's receipt (22 files/293) is itself unresolvable — correct it to a resolvable suite list. Return {lane:'W8-A', commits, gates, stopReports, notes}.`,
  {label:'w8a:conquest', phase:'Conquest', model:'opus', effort:'high', schema:S})
const cv = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(c)}. Verify on committed bytes: K3 discipline — grep+probe that feasibility/intent read ONLY belief surfaces (a truth-read mutant must red the import pins); the mistaken-feasibility pins carry REAL both-direction fixtures (run them; the receipt must say what was believed and why); the overwhelming negative case (clearly-winning-but-not-overwhelming negotiates — execute it); the inheritance counterforce derived from existing engines (no new estimator — grep for new arithmetic vs reuse); stage-neuter controls re-run by YOU; dormancy hashes byte-identical with the flag dark; strict/build/walkers; pathspec vs cycle 20. Return {lane:'W8-A verify', verdict, findings}.`,
  {label:'w8a:verify', phase:'Conquest', model:'opus', effort:'high', schema:V})

phase('Razing')
const z = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
Prior: ${JSON.stringify({conquest:c?.commits, verdict:cv?.verdict})}. LANE W8-B — WR-8's RAZING HALF:
SLICE 4 — THE RAZING (R+R2): the third intent (PUNISH), evil-exclusive at initiation (the hard alignment gate — the ONE place alignment is a boundary); the double gate (CR-WR8-B's composite extremity + won siege); deaths-not-departures (conserved sack cause class through §3 demographics, banded escape share as refugees, named cast disperses roaming — NEVER engine-killed); tier demotion RIDES popToTier reading the new truth (never a second writer); institutions to damage-impairment/shell; the victor LEAVES (no occupation record — the departure is the receipt); CR-WR8-A executes here: the edge-type flip to hostile on existing victim-adequate edges.
SLICE 5 — THE VENGEANCE LICENSE (R2): minted to victim-adequate relationships on the razing, ONE per coalition, consumed once, mints no counter-license; COUPLED via CR-WR8-B's composite; the just razing sanctioned-not-free (banded-down consequences, never zero for good actors); THE WORLD JUDGES on the observer's axis; deterrence priced through WR-6's alliance-web risk read POINTED at the aftermath (consume, never rebuild); LIFECYCLE per the volume's OWN paths: licenses + casus records persist in the spatialLedgers family, JSON-round-trip pinned, regen and undo carry them, import validates, expiry is a READ against heldSince — anything about persistence the volume does NOT specify → STOP-report, do not invent (the ransom-claim precedent).
SLICE 6 — THE ATROCITY CASUS: mint the #16 pair atrocity_answer ↔ atrocity_atoned from BELIEVED razings (news speed, J-WR-7 mint-on-the-public-fact); state-derived, decay-inherent on its band; BOTH R2 polarities switch a real cause (the just razing mints none against the avenger); register in WHAT_PHRASES + heraldRouting (the totality walkers are the proof).
PINS owed (the volume's list): extremity negative BOTH directions; initiation gate (non-evil structurally unreachable — walk the intent table); license coupling (no own-extreme ⇒ no razing); the closed loop; sack conservation arithmetic; demotion-follows-truth; the license POSITIVE-REACHABILITY pin (a generated corpus holder reaches the coupled extreme and prosecutes); the material self-limit pins (ash pays no tribute; razing the same remnant twice yields NOTHING). One commit per slice + queue rows. Return {lane:'W8-B', commits, gates, stopReports, notes}.`,
  {label:'w8b:razing', phase:'Razing', model:'opus', effort:'high', schema:S})
const zv = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(z)}. Verify on committed bytes: the initiation gate by WALKING the intent table yourself (no non-evil path may reach PUNISH — enumerate); the composite extremity BOTH negative directions executed; the conservation arithmetic on a real sack fixture (population before = deaths + refugees + roaming named cast, to the person); demotion rides popToTier (grep: no second writer); the edge-type flip fires on the razing fixture and the healing lattice can still re-type it later (probe); the license lifecycle — JSON-round-trip a world WITH a live license through save/load/regen/undo and prove the license survives byte-faithfully, and import validates a forged one; the positive-reachability pin runs on a REAL generated corpus (not a synthetic holder); both material self-limit pins; the #16 pair through the totality walkers; dormancy byte-identical dark; stage-neuter; strict/build; pathspec. Return {lane:'W8-B verify', verdict, findings}.`,
  {label:'w8b:verify', phase:'Razing', model:'opus', effort:'high', schema:V})

return { conquest: {r:c, v:cv}, razing: {r:z, v:zv} }