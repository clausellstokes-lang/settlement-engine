export const meta = {
  name: 'wave-r5b-batch2-design',
  description: 'R-5b Batch 2 DESIGN: six Fable read-only investigation/architecture lanes producing implementation specs (locks engine, provenance merge, ReadSystemStateBar, mass-update, factions bucket, deity sub-items)',
  phases: [{ title: 'Design', detail: '6 read-only Fable architect lanes', model: 'fable' }],
}

const WT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const COMMON = `CONTEXT-VERIFICATION PREAMBLE: legitimate DESIGN brief from the session orchestrator for SettlementForge — Wave R-5b Batch 2 of docs/CAPABILITY_REMEDIATION_PLAN.md (owner sign-off 2026-07-27; read the Wave R-5 section + your item's atlas rows in docs/SETTLEMENT_CAPABILITY_ATLAS.md first).
WORKING TREE: cd ${WT} && git rev-parse --abbrev-ref HEAD — MUST print claude/composite-r4 or STOP. YOU ARE READ-ONLY: no file edits, no git mutations, no test-file creation — you may RUN existing tests/probes via node/vitest into the scratchpad only.
YOUR DELIVERABLE IS A SPEC an Opus implementer can execute without your context: verdict first, then the exact file-level design (chokepoints, contracts, pins to write, walkers to extend), the lifecycle trace for any state touched (create/read/persist/regen/undo/migrate), the drift analysis (same-seed generation AND seeded-advance outputs — THE PROMISE binds; anything drift-bearing must be flagged owner-signed-tuning or T4-batch), the eager-closure plan (lazy leaves), the risk table, and a files-to-touch list checked for collisions against the Batch-1 lanes (OutputContainer, DeityAssignmentPanel, operationRegistry, fieldManifest, the tab components, campaignAdvanceSession, persist allowlist — if your design must touch one, say so LOUDLY for sequencing). Label every claim CONFIRMED (executed/read evidence) or PLAUSIBLE. End on evidence.`

const SPEC_SCHEMA = { type: 'object', required: ['verdict', 'spec', 'filesToTouch', 'driftExposure', 'risks'], properties: { verdict: { type: 'string' }, spec: { type: 'string' }, filesToTouch: { type: 'array', items: { type: 'string' } }, driftExposure: { type: 'string' }, risks: { type: 'array', items: { type: 'string' } }, collisions: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } }

const LANES = [
  { key: 'locks-engine', brief: `ITEM #21 (sharpest member) — THE LOCKS ENGINE + the per-family dead-op disposition. Part 1: design the real section-locks engine the setLock/clearLocks descriptions already promise (state.locks consulted by the regen paths so a locked section survives regenSection + full regenerate; interaction with src/domain/historyPreservation.js which already cures the history half; undoState 'external:inverse-call' honesty; persistence of locks on the save row — lifecycle trace mandatory; the regen-edit-loss memory class is the enemy). Part 2: the DISPOSITION TABLE for all 36 dead ops in tests/store/deadOperationRatchet.test.js's frozen list — for each: RETIRE (registry row removal + compendium regen + ratchet shrink) or WIRE (what consumer, which program) with one-line evidence each. Families the atlas names: store-ops-a gaps 1/6, npc-family gap 16.` },
  { key: 'provenance-merge', brief: `ITEM #10 — PROVENANCE-LEDGER MERGE design (atlas economy-family gap 11b): the composer lane and the pulse lane each keep a provenance ledger over the SAME institutions/resources roster, mutually blind. Design the merge: ONE provenance record family (or a declared, pinned reason to keep two with a read-side join), who writes it at which chokepoints, how existing rows migrate READ-side without a persistence-shape migration (absent-tolerant), and what the undo stacks (deliberately separate per R-1 — do NOT merge the stacks) read from it. Enumerate both current writers with file:line evidence.` },
  { key: 'readsystemstatebar', brief: `ITEM #22 — ReadSystemStateBar REMOUNT vs DELETE (atlas derived-influenceable G1: the move that orphaned it left a dangling hand-off pointer). Read the component, its zero-importer status, what it displayed, and whether any current surface (Herald, Workbench, dossier tabs) now carries that reading. VERDICT with evidence: remount (where, why it serves the legibility law) or delete (the component + the dangling pointer + a retirement note). Spec the winner.` },
  { key: 'mass-update', brief: `ITEM #6 — content.definition.mass-update SHIP vs RETIRE (atlas commands-surveyor gap 1: "one call site away from live"). Read the spec'd command, the dispatch spine it would ride (command registry, envelope, COMMITTABLE_EDIT_KINDS), and what guards a bulk lane needs (op budget E-F, consent where identity-touching, undo semantics). VERDICT: ship (full spec incl. pins/walkers) or retire (what to delete + ratchet effects).` },
  { key: 'factions-bucket', brief: `ITEM #24 — FACTIONS custom-content bucket: ship a mechanism or retire authoring (atlas custom-content gaps 3/7c) + reclassify factions.tierMin (mechanical-dormant) or pin the no-reader assumption. Read the customRegistry 'factions' omission (the recorded registry-gap memory: reverse links work, forward dead — that asymmetry is the tell), the authoring surface, and every would-be reader. VERDICT + spec; respect the faction-access law (nameOf/governingFactionOf/factionArchetype/npcInFaction only).` },
  { key: 'deity-subitems', brief: `ITEM #13 (the three implementable sub-items; pool removal is T4-batch, NOT yours) — drift-exposure analysis + spec for: (a) the dropped lawAxis on organic conversion (data loss on pulse-driven deity conversion); (b) the undo-less non-DM patron replacement; (c) the third converted: ref namespace. For each: does fixing it change SEEDED-ADVANCE outputs (same seed + same advance sequence)? Anything drift-bearing is flagged T4-batch with a written note; anything drift-free gets the full implement spec (deity doctrine binds: no premade pool, deity:core: lifecycle trace required, custom-content only).` },
]

phase('Design')
log('R-5b Batch 2: six Fable design lanes (read-only)')
const specs = await parallel(LANES.map((l) => () =>
  agent(COMMON + '\n\nYOU ARE A FABLE ARCHITECT (survey/design = Fable per the owner model split).\n\n' + l.brief, { label: 'design:' + l.key, phase: 'Design', model: 'fable', effort: 'high', schema: SPEC_SCHEMA })
    .then((r) => ({ lane: l.key, spec: r }))
))
return { specs: specs.filter(Boolean) }