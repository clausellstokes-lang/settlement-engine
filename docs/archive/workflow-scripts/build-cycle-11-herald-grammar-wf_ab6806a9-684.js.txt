export const meta = {
  name: 'build-cycle-11-herald-grammar',
  description: 'THE HERALD GRAMMAR WAVE (F1-F5) · dossier tail (one-home cure + scan widening)',
  phases: [{ title: 'Build', detail: 'grammar wave + dossier tail, implement + verify each' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest before believing a timeout); strict-typecheck touched files (NEW files enter the R-BLD-9 baseline at ZERO — they must be strict-clean); STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Everything DARK, nothing pushed. CONCURRENCY: the war lane is live on src/domain/worldPulse/* (never write it); queue docs get concurrent edits (re-read, carve hunks).`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const lanes = await parallel([
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE HG — THE HERALD GRAMMAR WAVE. The cycle-9 verifier PROVED the composed telling prose ungrammatical on ~89% of back-direction draws: heraldCausalGrammar.js authors arg slot types 'N'|'F'|'V'|'A' but heraldCausalVoice.js reads arg exactly once (the A-guard, headline register only) — 49 of 71 back-direction connectives declare a NOUN-PHRASE slot and receive a finite clause (executed sample: "against the refusal of the eastern road was cut"). CHAIR RULING: the cure is §1's JOIN MOLDS — they are the noun-phrase supply the grammar was architected to receive, not optional richness. Build:
(1) wire the 78 join-mold families from docs/content/RECEIPT_POOLS_CAUSAL.md §1 (read the annex §1 whole + the spine SP-6 causal-grammar block): per edge kind, the mold supplies the argument FORM each arg type needs (N noun-phrase, F finite clause, V bare verb phrase); the composer dispatches on connective.arg in BOTH registers and draws the matching mold form — never feeds a finite clause into an N/V slot again; A stays argument-less (drop-the-link, never re-label).
(2) WELL-FORMEDNESS PINS, not substrings: for each arg type, a mold-conformance pin that parses the composed line against the mold's shape (and a negative control feeding the OLD finite-clause form into an N slot → red). Sweep every existing toContain prose pin in heraldCausalVoice.test.js: keep them, but the conformance pins carry the law.
(3) F2 — the planted warrant must fire on the DISINFO SPELLING, not presence: plural lineageIds is the ORDINARY telling lineage (rumorNetwork.js:174 writes it on every arrival); only ids with the disinfo: prefix mark plants. Rewarrant on the prefix (composer stays fence-clean — string discipline only, no ledger reach) + a DIVERGENCE PIN: one popup may never say 'planted' beside heraldIntegrity classifying the same hop worn/clean (fixture with an organic lineageIds row → both surfaces agree).
(4) F3 — export the unreceipted-hop fallback literal from causeWalk.js beside REDACTED_HOP; CausalityPopup.jsx imports it (kill the bare duplicate at :95); a drift mutant must red (unreceipted hop classifying 'clean' is the assumed-from-silence the integrity header forbids).
(5) F4+F5 — the spine record: docs/DESIGN_FP_SPINE.md §6 re-classifies the join-molds deferral as DISCHARGED-BY-THIS-WAVE (it was a lighting BLOCKER, not polish — say so), and records the A-drop reader consequence (a headline's causal gesture appears/disappears by seed under drop-never-relabel) as designed behaviour.
One pathspec commit per coherent step. heraldCausalVoiceEnabled stays DARK. Return {lane:'HG grammar', commits, gates, stopReports, notes}.`,
    {label:'laneHG:grammar-wave', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. THE BAR: re-run the cycle-9 verifier's method — compose real prose through the actual API over seeds a-h AND a 200-seed sweep, both registers, and READ THE SENTENCES: every N-slot draw must carry a noun phrase, every F a finite clause, every V a bare verb phrase, A argument-less; quote at least 10 composed lines verbatim in your findings as evidence. Run the negative control (old finite-clause form into an N slot → the conformance pin must red). Verify F2 with an organic plural-lineageIds fixture (no 'planted' anywhere, integrity agrees), F3 with the drift mutant, F5 in the spine diff. Confirm the fence/one-gesture/seed-not-growth pins still bite (re-run one mutant each). Strict: all touched/new files clean at the R-BLD-9 zero-ceiling for new entries. Pathspec-clean vs the war lane. Return {lane:'HG verify', verdict, findings}.`,
      {label:'laneHG:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled repair implementation. ${P10}
LANE PT — THE DOSSIER TAIL (cycle-9 lane-P verifier findings + one live walker red):
(1) THE ONE-HOME CURE (live red at HEAD): tests/lint/economyReadModelCoverage.walker.test.js's 'freshness sentence minted only in the detector module' reds because src/data/dossierStateProse/economy.generated.js embeds a byte-copy of the freshness sentence. Cure at the GENERATOR: scripts/generate-dossier-state-prose.mjs must emit an import of ECONOMY_FRESHNESS_SENTENCES from src/domain/display/economyFreshness.js (the one home) instead of inlining the string — regenerate, prove the walker green, and add the generator-side pin so a future regeneration cannot re-inline it.
(2) widen the DM-field no-writer source scan + lockstep pin beyond the settlement arm: QUEUE_WIRED_PROSE_PATHS also declares faction ['desc'] and institution ['desc'] as DM-editable; derive the scan's leaf regexes from ALL arms and compare the full object in the lockstep pin (the verifier's exact gap).
(3) add a REACHABLE-band kernel pin beside the SUBSISTENCE one: constants.js says Subsistence is remapped to Struggling/Poor before emission, so pin DS-ECO-8 on a band live generation actually produces (keep the legacy-tolerance pin too, labeled as such).
DO NOT touch worldPulse, Herald modules (lane HG owns them this cycle), or the war lane's files. One pathspec commit. Return {lane:'PT tail', commits, gates, stopReports, notes}.`,
    {label:'lanePT:dossier-tail', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify: the one-home walker green at HEAD AND the generator pin reds when a mutant re-inlines the sentence (run it); the widened scan catches a seeded faction-desc writer (write a temp offender → red → remove); the lockstep pin compares the FULL QUEUE_WIRED_PROSE_PATHS object (mutant: add a fourth arm to one side → red); the new kernel pin's band is provably reachable (generate and show it); dmFieldProjection byte-identity pin still bites; pathspec-clean. Return {lane:'PT verify', verdict, findings}.`,
      {label:'lanePT:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
])
return { lanes: lanes.filter(Boolean) }