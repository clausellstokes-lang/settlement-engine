export const meta = {
  name: 'third-universe-religion-extract',
  description: 'Extract the export branch religion mechanics, gating, and follow-ons for constitutional comparison',
  phases: [
    { title: 'Extract', detail: 'three parallel Opus readers over the unzipped export source' },
  ],
}
phase('Extract')
const SRC = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/95cca3f6-d313-4fe1-91f6-b4b3a29fb5ef/scratchpad/export-inspect/source'
const OURS = '/Users/cstokes/Desktop/settlement-engine'
const results = await parallel([
  () => agent(`READ-ONLY extraction task. A third code lineage (an export of git branch claude/mystifying-ride-90b338) lives unzipped at ${SRC}. Our production lineage lives at ${OURS} (branch review-fixes-2026-07-08) — read-only there too, and note two agents are editing that tree, so expect possible transient file churn; re-read if a read looks torn.
TASK: extract the COMPLETE religion-engine mechanics of the export. Read ${SRC}/src/domain/worldPulse/religionLegitimacy.js, religionState.js, religiousContest.js, pantheon.js, plus anything they import for divine mandate / coup coupling (grep for publicLegitimacy, divineMandate, mandate, coup in ${SRC}/src/domain). Report every named constant with its value (weights, lags, clamps, thresholds, cooldowns), the legitimacy update equation(s), the contest/eruption rules (incl. organic low-legitimacy contests, occupation heresy-stain, religious-institution backing), the divine-mandate→powerStructure.publicLegitimacy coupling (exact formula + where it feeds the coup cluster), and the SET_PRIMARY_DEITY reassign semantics. THEN quantify divergence from ours: diff the four same-named files against ${OURS}/src/domain/worldPulse/ versions (report added/removed/changed line counts per file and the 5 most consequential semantic differences per file — mechanics, not comments). Our tree also has piety.js, deityStance.js, deityAxes.js, clergyTraitPlane.js, deityStanceLane.js, fidelityNoise.js, latentPantheon.js in the same dir — state explicitly whether ANY analog of each exists anywhere in the export (grep by concept: stance, piety, clergy trait, dampener, opposed axis, latent). Return a dense structured text report; your final message is raw data for a manager, not prose for a human.`, { label: 'extract:engine', effort: 'high' }),
  () => agent(`READ-ONLY extraction task. A third code lineage lives unzipped at ${SRC}. TASK: extract how its religion/pantheon system is SURFACED and TIER-GATED. Read: the PantheonTeaser (find it — PR notes say a free-tier teaser on the About page), ${SRC}/src/components/map/PantheonPanel.jsx, compendium/PantheonActivationStrip.jsx and DeityEffectPreview.jsx, settlement/PrimaryDeityPicker.jsx and WarFaithSection.jsx, new/tabs/WarFaithTab.jsx, map/WarFaithMapOverlay.jsx and AssignDeityFromMap.jsx, ${SRC}/src/pdf/sections/FaithWar.jsx, plus the PDF LIVING PANTHEON block (grep 'LIVING PANTHEON' in ${SRC}/src/pdf). For EACH surface report: what it shows (deity NAMES? standings? legitimacy bands? contest odds?), and precisely how it gates by tier/auth state (anon / free / premium / lapsed) — quote the gating conditions with file:line. Key questions: (1) do anon/free users ever see named deities or live pantheon state, and where; (2) is pantheon generation tier-conditional anywhere (does tier touch generation), or is gating purely presentational; (3) how does the editor let a DM assign/change deities and is it premium-gated; (4) what does the PDF export include per tier. Return a dense structured report; raw data for a manager, not prose.`, { label: 'extract:surfacing-gating', effort: 'high' }),
  () => agent(`READ-ONLY extraction task. A third code lineage lives unzipped at ${SRC}. TASK 1 — size-scaled faith influence: find the mass/size-scaling of cross-settlement faith (PR notes: scales reinforcement, legitimacy, carrier spread by relative settlement mass; commit tag 'faithMass'). Report the exact scaling formula, its constants, which channels it multiplies, and what inputs feed it (population? tier? anything exogenous like war/trade/user actions?). TASK 2 — SHIFT_TIER: find the forced promote/demote editor control (grep SHIFT_TIER); report its apply path (does it reuse organic tier-drift?), demotion effects (ruined-remnant institutions), population reband, and undo semantics. TASK 3 — evidence quality: read ${SRC}/scripts/audit/religion-coup-soak.mjs, religion-balance.mjs, religion-soak.mjs, simulate-religion.mjs; report what each actually measures, trial counts, pass criteria, and whether the '~36x fewer coups' and '60k Monte Carlo zero degeneracy' claims in the export's PR-body.md are substantiated by what the scripts compute (read /Users/cstokes/Desktop/settlementforge-export/PR-body.md for the claims). TASK 4 — tests: locate divineMandate/religionTier2/faithMass/shiftTier test files, report counts and the strongest invariants they pin. Return a dense structured report; raw data for a manager, not prose.`, { label: 'extract:followons-evidence', effort: 'high' }),
])
return { engine: results[0], surfacing: results[1], followons: results[2] }