export const meta = {
  name: 'build-completeness-audit',
  description: 'Verify every designed slice landed or is recorded-deferred on composite-r4',
  phases: [{ title: 'Audit', detail: 'read-only slice verification per design source' }],
}

const WT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const LEDGER = '/Users/cstokes/Desktop/settlement-engine/docs/COMPREHENSIVE_REVIEW_PROGRAM.md'

const COMMON = `You are a READ-ONLY build-completeness auditor for an authorized owner-delegated program on the owner's solo repo. HARD RULES: work in ${WT} (branch claude/composite-r4 — verify with git branch --show-current, read-only). NEVER run npm/npx/vitest/node scripts (a test suite is running in this worktree — you would corrupt it). NEVER mutate git (no add/commit/stash/checkout). Allowed: Read, Grep, Glob, and read-only git (log/show/diff --stat). The program ledger at ${LEDGER} (a different tree — read-only) records what landed and what was deliberately deferred; search it to confirm deferral rows. For EVERY slice below return status LANDED (with in-tree evidence: a file:line of the mechanism, or a commit hash from git log), DEFERRED_RECORDED (with WHERE the deferral is written), HUMAN_GATED (owner-only: keys, taste, legal, deploy), or MISSING (designed but absent and no recorded deferral — cite what you searched). Evidence strings must be concrete and verifiable. Do not fix anything; report only.`

const SCHEMA = {
  type: 'object', required: ['source', 'slices'],
  properties: {
    source: { type: 'string' },
    slices: { type: 'array', items: { type: 'object', required: ['name', 'status', 'evidence'], properties: {
      name: { type: 'string' },
      status: { type: 'string', enum: ['LANDED', 'DEFERRED_RECORDED', 'HUMAN_GATED', 'MISSING', 'UNCLEAR'] },
      evidence: { type: 'string' } } } },
    notes: { type: 'string' },
  },
}

const SOURCES = [
  { key: 'deep-couplings', prompt: `${COMMON}\nSOURCE: /Users/cstokes/Desktop/settlement-engine/docs/DESIGN_DEEP_COUPLINGS.md (main tree). Audit slices D-0 (migrationRumors), D-1 (beliefAxes), D-2 (npcCredibility + lie-stigma), D-3 (intel lane intelActs/intelTransfers), D-4a-f (contested goals class + champion producer + support goals), D-5 (thirdPartyRansom), D-6 (seaRoads), D-7a-f (memory weave: grievanceRead, weave core, person bonds, factionPairStates, elite bleed, bond-read API), D-7e clause ii (gratitudeBonds — just landed, verify), and the §15 deferred seams (each recorded?). Verify each flag exists in the code and each mechanism file is present.` },
  { key: 'money-wave', prompt: `${COMMON}\nSOURCE: /Users/cstokes/Desktop/settlement-engine/docs/DESIGN_MONEY_WAVE.md (main tree). Audit M-1 money spine (money_events), M-2 purchase ledger, M-3 auto-reload reload-to-target, M-4 surveyor provisioning, M-5..M-8 founder transfers (five-state machine, dual 2FA, Connect payout, reelect), M-9 single-session last-login-wins + M-9e/f session surfaces + probes, M-10 stewardship (buyback/dormancy/abandonment, seat_buyback_cents dial), the fraud-fix lane FF-a..FF-d (commits a7ecd7e2 b6e4bd98 68d8293f d0eacd45 — verify ancestors via git merge-base --is-ancestor d0eacd45 HEAD), the census upgrade (8 AI surfaces session-gated), §11 activation runbook exists, §12 terms drafts exist. Known HUMAN_GATED: keys, legal sign-off, Connect enablement, Supabase Pro toggle.` },
  { key: 'roads', prompt: `${COMMON}\nSOURCE: /Users/cstokes/Desktop/settlement-engine/docs/DESIGN_THE_ROADS.md (main tree). Audit R-1..R-8 (mission travel, threat classes, hostage stasis, ransom, covert conversion, cadence, embassy peace/third-party/amnesty/escort + purposes 6-7), the Travelers Overlay, the Road Scene, the §0.5 secrets seam, dormancy goldens present (roadsDormancyGolden, seaRoadsDormancyGolden), and §15-style deferred seams (embassy-over-sea etc.) recorded.` },
  { key: 'traditions-waveC', prompt: `${COMMON}\nSOURCE: /Users/cstokes/Desktop/settlement-engine/docs/DESIGN_TRADITIONS.md (main tree). Audit T-1..T-5, then the Wave-C §16 five dark seams: festival-week map dress (groundDressOps hook), REFRAME_VOCAB 'tradition' act class, culture-vector tradition read, tradition trade-lane bonus, cross-settlement pilgrimage. Each shipped dark with dormancy proofs (ledger row 2026-07-19 'WAVE C traditions seams' claims all five @ claude/wave-c-seams 295f27b8 — verify the mechanisms exist in THIS tree).` },
  { key: 'waves-AB', prompt: `${COMMON}\nSOURCE: THE_REMAINING_ARCHITECTURE.md §4 (main tree docs/). WAVE A: the zero×4 sweep — the ledger records fold-batch-3 re-pins to measured (kill-list 906→100 / 98→72 / 234→167 / 214→161, tolerance-0) + 4 taste-deferred items (caliber atoms, HomeLanding, AccountSection radius, avatar-50%). Verify the kill-list/ratchet tests exist and are tolerance-0 at those pinned values, and the 4 deferrals are recorded (memory/ledger). Adjudication data: is the residual ALL recorded-deferral or is sweep work outstanding? WAVE B: the 11 Class-A items — lastingEffects authoring, traditions genesis consumption + manual authoring UI, PDF counterseal structured-path refactor (NOTE: §10 post-launch-owed ALSO lists it — report which state the ledger last records), deterministic-violet re-tones, Viability→adjudication, WhatChangedPanel wire-or-remove, ?cat= deep-link, folder thead a11y, journey_stop enrichment, vendorManifest try/finally, #15 door entitlement law (SurveyorDoor only surveyor-tier + founder; cartographer never renders it). Also wave-b-remainder's recorded deferrals (subscription-pause, WB-g/h/j/k) — confirm recorded.` },
  { key: 'waves-DE', prompt: `${COMMON}\nSOURCE: THE_REMAINING_ARCHITECTURE.md §4 (main tree docs/). WAVE D perimeter — verify each: crawler governance (robots.txt AI directives + noai meta + TDM headers), rate-limit audit/server-side limits on edge+auth endpoints (the fail-closed per-IP AI burst gate, migration 156-range token bucket), server-side quota proofs, bot-wave anomaly detection in the analytics seam, sf-bridge origin audit, ToS anti-automation drafts, Turnstile managed-mode wiring (auth + checkout, key-inert), pglite pins. WAVE E launch ops — verify each of A-E: (A) client error boundary → own edge endpoint + table + admin-errors surface + uptime/health fn, (B) custom SMTP posture + transactional mail seam (mailAdapter), (C) SEO surface (sitemap generation, OpenGraph/social cards, meta on public pages), (D) refund/cancellation policy page linked from checkout, (E) ops runbooks (deploy-rollback, backup/PITR verification docs).` },
  { key: 'deep-craft', prompt: `${COMMON}\nSOURCE: THE_REMAINING_ARCHITECTURE.md §2 (main tree docs/) + DESIGN_DEEP_CRAFT_PAGES.md. Audit slices C1-fin, C2L loading journeys, C2 welcome film machine, C3 library, C4 dossier, C5 realm, C6-C12 document/account pages, C13 surveyor AI door, C14 tableview, C15 export, C16 shell, and Phase D verification ratchets (radii/shadows/rgba/callouts — report their CURRENT pinned ceilings from the ratchet tests and whether the pins are tolerance-0 at measured values). The ledger records many as shipped (C13 @ deep-craft-c13, C2L, C1r-a/b/c2, miniatures, living backdrop) — verify in THIS tree, not the ledger alone.` },
  { key: 'seams-registry', prompt: `${COMMON}\nTASKS: (1) In-code deferral markers: grep src/ for deferral comments naming D-3 self-policing (intelSaleCredibilityDeltas), D-4 bluff charge (bluffDeposits), D-4e champion producer (backedBy), D-5/D-6 — the mini-fold's Fα/Fβ/roads-followon lanes claim they RETIRED these; verify no marker still reads as open/unwired (intelSaleCredibilityDeltas must have callers now; advanceCredibility must consume bluffExposures). (2) The task-#40 seams are LOOP INTAKE not completeness debts — confirm each is recorded somewhere durable: shared-war-coalition arm, champion covenant UI (npcLadderState mirror/nids), unbonded-conscience payer channel, third-party news pool. (3) Store-action registry lifecycle: any store actions added since the last compendium regen have operationRegistry entries (run NO scripts — verify by reading tests/ for compendiumDataFreshness and grepping recent store slice additions vs src/data/operationRegistry). (4) The blissful-bhaskara faction-key sweep folded: tests/lint/factionNamePrecedenceScan.test.js + tests/domain/factionNamePrecedence.test.js exist with KNOWN_OFFENDERS frozen empty.` },
]

phase('Audit')
const results = await parallel(SOURCES.map((s) => () =>
  agent(s.prompt, { label: `audit:${s.key}`, model: 'opus', schema: SCHEMA })))
return { audits: results.filter(Boolean) }