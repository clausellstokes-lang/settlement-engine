export const meta = {
  name: 'fp-cycle5-lane-a-espionage-chain',
  description: 'Cycle 5 Lane A: repair cycle 4 debt, then the ES-1..ES-4 chain — the critical path to the CR-WR10-H lighting discharge. All no-new-flag.',
  phases: [
    { title: 'Debt', detail: 'the two reds cycle 4 opened as its own' },
    { title: 'ES-1', detail: 'the mission: covert arm on the errand spine' },
    { title: 'Verify 1', detail: 'adversarial verification' },
    { title: 'ES-2', detail: 'the gauntlet' },
    { title: 'Verify 2', detail: 'adversarial verification' },
    { title: 'ES-3', detail: 'the products' },
    { title: 'Verify 3', detail: 'adversarial verification' },
    { title: 'ES-4', detail: 'the distant source — the lighting discharge' },
    { title: 'Verify 4', detail: 'adversarial verification' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const LAWS = `VERIFY-FIRST PREAMBLE: this brief is from the session chair of an owner-delegated build program. Verify every claim against the tree before acting — LIVE CODE OUTRANKS EVERY TABLE, and a doc overstatement is a STOP-and-report, never a silent correction. Work in ${TREE} (branch claude/composite-r4). CHECK-GIT-FIRST always.

⭐⭐ THE GATE MUTEX — USE THE SCRIPT, DO NOT SPELL THE CHECK YOURSELF.
Run \`sh scripts/gate-mutex.sh --wait\` before any vitest invocation, and \`sh scripts/gate-mutex.sh\` for a one-shot check (exit 0 = free, 1 = held).
⛔ NEVER write \`ps aux | grep -c "[v]itest"\`. It matches YOUR OWN command line: any other occurrence of the plain word on that line — an echo label, or the very \`npx vitest run\` you are about to execute after the wait — makes the count non-zero with nothing running. A lane in cycle 4 wrote the wait and the run in ONE command and slept TWENTY MINUTES against a mutex nobody held, then gave up without running its gate. The failure looks like patience: nothing errors, nothing reds, the gate silently does not run. The script excludes its own ancestry and requires a real node/npx/vitest EXECUTABLE, and it prints pids rather than a count.

⚠⚠ A SECOND BUILD LANE IS RUNNING CONCURRENTLY IN THIS SAME TREE (Lane B: GR-3, IN-0b/0d, SP-E). Consequences that BIND you:
 - NEVER touch, stage, revert or clean a file you did not author. Dirty files that are not yours are Lane B's — preserve them absolutely.
 - Commit with EXPLICIT PATHSPECS naming only your own files. Verify \`git diff --cached\` before EVERY commit, and check the staged set against your EXPECTED set BY NAME AND COUNT — not merely that every hunk is yours. ⚠ That distinction bit this chair today: a commit claimed three parts and landed one, because \`git add\` aborts ATOMICALLY on a single bad pathspec and stderr had been suppressed. The ownership check passes while the set is incomplete.
 - SHARED-FILE COMPARE-AND-SWAP: \`scripts/mutation-coverage-manifest.json\` is the ONE hard serializer — both lanes touch it. Also shared: the Herald registration tables, the coupling registry, and the ratchet baselines. Edit them LAST, immediately before your commit; re-read at HEAD immediately before splicing; if the on-disk text changed since you first read it, DISCARD your splice and redo it against the fresh text. ⚠ \`json.dumps\` on the manifest REFORMATS all ~1,972 lines — SPLICE THE RAW TEXT.
 - HEAD may move under you mid-wave. Re-read it at the staging step.

THE PROTOCOL LAWS (each has bitten): NEVER stash/reset/clean/checkout-family (the whole checkout family discards uncommitted work; use cp backups and cmp-proven restores; damaged tracked files restore via \`git show HEAD:path > path\`). Gates ONLY via \`sh scripts/gate-tail.sh <cmd>\` or \`npm run check:tail\` — a bare pipe reports the PIPE's status and has greenwashed red gates twice. Commit messages via file; trailer \`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>\`. python3 byte-scan every authored file for NUL bytes (authored SIX times; grep and diff lie silently about them). New walkers need \`scripts/mutation-coverage-manifest.json\` entries. Strict ceiling 1313 EXACTLY. tests/fixtures/, pulseKernel.js and applyWorldPulse.js are UNTOUCHABLE. Navigate by SYMBOL — hand-keyed line addresses rot. ⚠ NEVER spell \`vitest list --json PATH\` with a space: the bare form consumes the NEXT POSITIONAL as its output path and silently overwrites that file at exit 0; always \`--json=PATH\` to a path OUTSIDE the repo.

⛔ THE REPO GATE IS RED AT BASE — roughly 25 failing tests across tests/lint, property, security and domain, plus 3 lint errors and a red full typecheck. THIS IS NOT YOURS. "Pre-existing" is an EMPIRICAL claim you must EARN: re-run the exact failing command against a \`git archive\` of your start HEAD (symlink node_modules in, and \`rm -f\` the SYMLINK before removing the tree) and cite that run. Never repair a foreign red; never claim one without the base-state receipt.

⏳ OPUS-ERA MARKING (owner directive): every ledger row you append to docs/FABLE_VALIDATION_QUEUE.md MUST carry "⏳ OPUS-ERA — FABLE SURVEY OWED" in its heading, with every chair-grade judgment itemized as a J-* entry.

REPORTING: label every conclusion CONFIRMED (executed evidence quoted) or PLAUSIBLE (reasoning only). Never present the second as the first.`

const VERIFIER_LAWS = `

⚠⚠ VERIFIER DISCIPLINE. Two build lanes are writing this tree while you run.
 - Do EVERY execution inside a \`git archive\` of the exact commit you are verifying: \`mkdir -p /tmp/v-$$ && git archive <sha> | tar -x -C /tmp/v-$$ && ln -s ${TREE}/node_modules /tmp/v-$$/node_modules\`. Remove the SYMLINK with \`rm -f\` BEFORE removing the tree, or you delete the real node_modules.
 - You MUST NOT assert tree-cleanliness and MUST NOT report dirty files as findings — they are the other lane's.
 - You MUST NOT edit, stage or revert anything in the live tree. Mutants go in disposable archive trees ONLY.
 - Use \`sh scripts/gate-mutex.sh --wait\` before any run.
 - ⚠ A MUTANT THAT PLANTS NOTHING GREENS. Every mutant must assert the file ACTUALLY CHANGED and abort if it did not — and when you plant a PROCESS or a fixture, verify it EXISTS before reading the result. This chair shipped a green negative control today that had planted nothing.
 - ⚠ RUN EVERY PROPOSED REMEDY AS A MUTANT: a trusted verifier's executed remedy has been DEAD under the new architecture — planted, battery stayed green.
 - Wave-end attribution: compare the wave's commit against a git-archive of its PARENT; row-identity diff EMPTY both directions except attributed rows, PLUS the red-ratchet CONTENT diff. ⚠ A red ratchet's inventory grows INVISIBLY behind a byte-identical fail row — extract the cardinalities inside the red rows from both logs and compare them as a multiset. This exact hazard falsified a landed wave's "zero rows are this wave's" claim.`

phase('Debt')
const debt = await agent(LAWS + `

YOU ARE THE CYCLE-4 DEBT SLICE. Cycle 4's own close row (commit \`f782c14b\`) named TWO reds as cycle 4's own rather than inherited. Clear them before cycle 5 builds on top, because a red you build over becomes a red nobody can attribute.

READ \`git log -1 --format=%B f782c14b\` FIRST — it states both precisely.

(1) THE mutationCoverageManifest LABEL-JOIN RED. Measured GREEN at cbd348a5 and RED at 57fe385b, with \`git log -S\` returning \`94d0c798\` for BOTH orphan sweep labels. The close row names the cure precisely: TWO RAW-TEXT MANIFEST SPLICES. It was left undone only because that slice edited no scripts. ⚠ Splice the RAW TEXT and re-parse to verify — \`json.dumps\` reformats all ~1,972 lines.

(2) THE typecheck:domain:strict +13 IN GR-2's THREE PACT MODULES. The ceiling 1313 is UNTOUCHED; what reds is the PER-FILE ZERO baseline rule for new modules. Bring the three pact modules to zero strict errors. ⛔ Do this by NARROWING TYPES, never by widening a baseline or adding \`any\` — the whole point of the zero-baseline rule is that new code starts clean, and satisfying it with a cast defeats it silently.

For EACH: reproduce the red by execution FIRST, repair at the chokepoint, then prove the repair with a mutant that asserts the file actually changed. Run the wave-end attribution and quote it. Append a ledger row with the ⏳ OPUS-ERA — FABLE SURVEY OWED heading.

RETURN JSON: {commits, redsBefore, redsAfter, gates, judgments, notes}.`, { label: 'debt:cycle-4-reds', phase: 'Debt', model: 'opus' })

const WAVES = [
  { id: 'ES-1', phase: 'ES-1', vphase: 'Verify 1', brief: `YOU ARE THE ES-1 IMPLEMENTER — THE MISSION. ⚠ ES-1 RIDES \`espionageEnabled\`; IT DOES NOT MINT IT. The flag landed at ES-0 (\`55674790\`); the manifest row, the certification row and the first by-name gate read are ALL DONE. Re-minting reds \`engineGatedRuleKeys\`'s exact one-key delta. The volume was corrected at \`fda90599\` — read the FLAG-ATTRIBUTION NOTE in DESIGN_FP_ARCHITECTURE.md §3.

NORMATIVE SPEC: \`docs/DESIGN_FP_ARCH_ES.md\`, the ES-1 block in §4 — navigate by SYMBOL; that volume's §1 forbids line-number navigation. Read it whole plus §2 and §3 before any edit.

⛔⛔ THE ONE THING MOST LIKELY TO SINK THIS WAVE: \`envoyDiplomacy.js\` measures 797/800 EFFECTIVE — THREE LINES OF HEADROOM — and it hosts \`buildEnvoyRoutePlan\`, which is exactly where ES-1's one-word kind fork lands. The volume budgets errandMint, espionageMissions and envoyErrandRecords but gives this file NO BUDGET LINE. The size ratchet is TOLERANCE-ZERO. Budget a lazy-leaf extraction INTO this wave, or measure the file yourself first and prove you fit.

CHARTER: the covert arm in SP-D's \`errandMint.js\` (landed at \`0aac6792\` — read its ACTUAL signature, never the volume's description); mint validation (itinerary <= 3, closed products + demand bands + the six-member legRefs set with pullBand refused, dispatch-refusal seams, concurrency cap); \`normalizeErrand\` taught the \`covert\` sub-record IN THE SAME COMMIT; the \`covert_envoy\` franchise members in \`routeNetworkConsumers.js\`; the one-word kind fork; covert casting (importance-inverse draw) routed through the ONE vetting reader \`vetVolunteerEnvoy\` in sendTwoDivergence.js — neither program forks a second vetting derivation.

⚠ SP-D LANDED A CONSUMER REGISTRATION TRIPWIRE with a BOTH-WAYS walker: a volume minting errands without a registry row REDS, and a registry row with no minting consumer REDS. ES-1 is a new minting consumer — add your registry row in the SAME commit.

PINS: four-fence + lit-mutant with EVERY guard door pinned INDIVIDUALLY (a double-guarded conjunction has survived an entire fence set here); JSON round-trip through REAL serialization including the alias trap (factions[].members[] ARE npcs[] objects); the fail-closed covert projection extended to \`covert.*\` proven with a SEEDED errand, NEVER an empty harness (an empty-harness absence pin passes with the feature deleted); every mint-refusal reason fixtured. LIFECYCLE TOTALITY: create/read/persist/regenerate/undo/import for every new field, each proven by executed round-trip.` },

  { id: 'ES-2', phase: 'ES-2', vphase: 'Verify 2', brief: `YOU ARE THE ES-2 IMPLEMENTER — THE GAUNTLET. Rides \`espionageEnabled\` (no new flag). NORMATIVE SPEC: \`docs/DESIGN_FP_ARCH_ES.md\`'s ES-2 block — navigate by SYMBOL, read whole.

⛔ \`applyWorldPulse.js\` is at 941/941 — ZERO headroom — and ES-2's charter forbids touching it. Do not.

✅ TWO BLOCKERS ARE ALREADY DISCHARGED, verified: CR-ES-2's anonymity amendment is LANDED in all three homes (informationStatecraft.js's BOUNDARY header with the BY-THE-ENGINE qualifier present, DESIGN_FP_INFORMATION.md, and the war volume), so your fold precondition is satisfied. CR-ES-3's vocabulary retarget landed at ES-0, so **the captor-leniency arm need NOT ship dark** — build it live.

⚠ CR-ES-6 SAME-COMMIT OBLIGATION: mounting \`espionageGauntlet.js\` ADDS row E15 (spy-dwell detection) to the WAYFARE volume's §4 closed encounter-pairs table. **VERIFY BEFORE ADDING — row E15 already appears in docs/DESIGN_FP_ARCH_WY.md even though the module does not exist.** If it is there, do not re-add it; confirm and record that you confirmed.

PINS: the friendly-host-rolls-nothing ANCHORED NEGATIVE; the single-dip security mutant; capture attribution BY RECONSTRUCTION, never by splitting an id on '.'; the ramp-live mutant; the \`caught_spying\` hold round-trip. Four-fence, guard doors individually, lifecycle totality.` },

  { id: 'ES-3', phase: 'ES-3', vphase: 'Verify 3', brief: `YOU ARE THE ES-3 IMPLEMENTER — THE PRODUCTS. Rides \`espionageEnabled\` (no new flag). NORMATIVE SPEC: the ES-3 block — navigate by SYMBOL, read whole. Needs SP-B (landed) and ES-2.

CHARTER HIGHLIGHTS: typed products (CONFIRM / ACQUIRE / REFUTE) composed against SP-B's landed belief-axis substrate. ⚠ READ SP-B's ACTUAL SHAPE — \`beliefAxes.js\` / \`beliefAxisSubjects.js\` — never the volume's description of it. A sibling volume's claim that a related blocker "is gone" was measured only CONDITIONALLY true: \`CHANNEL_BELIEF_AXES.trade\` is still \`Object.freeze([])\`, so anything reading through that spelling is unchanged.

PINS: the WRITER-BOUNDARY SCAN — no \`applyBeliefOverrides\` import anywhere in the ES module set, EXECUTED AS A MUTANT (plant the import; the scan must red); the tap-never-purer-than-access matrix; the lose-the-spy-keep-the-intel BOTH-ARMS pin pair. Four-fence, guard doors individually, lifecycle totality.` },

  { id: 'ES-4', phase: 'ES-4', vphase: 'Verify 4', brief: `YOU ARE THE ES-4 IMPLEMENTER — THE DISTANT SOURCE. This wave is the THIRD MEMBER of the CR-WR10-H lighting discharge and the payoff of the whole chain. Rides SP-B/SP-B2's flags; mints nothing.

⚠⚠ ELEVEN COMMITS IN THIS REPO CARRY "ES-4" IN THEIR SUBJECT AND THE WAVE HAS NOT LANDED. Commit \`8a4b0aef\` says verbatim "THE WAVE DID NOT LAND" — those eleven built and hardened the LIGHTING INSTRUMENT, not the wave. Do not conclude from the log that any part of ES-4 exists. Verify by module and symbol.

⛔⛔ THE LIGHTING RECIPE IS EXACT AND A PIN IN THE WRONG SHAPE "CARRIES THE TOKEN AND LIGHTS NOTHING". Read \`tests/lint/sovereigntyLightingContract.walker.test.js\`'s header clauses YOURSELF and satisfy them literally. As measured: the marker \`ES-4-DISTANT-SOURCE-EVIDENCE\` must stand in an \`it\`/\`test\` TITLE — NEVER a \`describe\` title — inside \`tests/domain/espionageDistantSourceEs4.test.js\`, one of exactly three entries in \`EVIDENCE_FILE_ADDRESSES\`. The \`it\` identifier must be imported from 'vitest' UNRENAMED and UNSHADOWED; the call must be statically registered STRAIGHT-LINE (no return, throw, conditional, loop, or bare await); it must take a function-body second argument with NO options bag; and its callback takes NO parameters. ⚠ A renamed vitest import (\`expect as it\`) was once credited while vitest registered ZERO rows.

CHARTER: prove the SOURCE — a non-neighbour (court, holding) pair appraised \`known:true\` through a COMPLETED confirmation mission. PINS: the four-leg totality plus the degraded-arm re-run against espionage-fed rows; the non-neighbour differential; the axis-drift scan. Four-fence, guard doors individually, lifecycle totality.

⛔ DO NOT LIGHT ANY FLAG. Lighting is owner-held at the terminal soak. You prove the condition is SATISFIABLE; you never satisfy it by lighting.` },
]

let prev = debt
const results = []
for (const w of WAVES) {
  phase(w.phase)
  const build = await agent(LAWS + `

${w.brief}

⚠ YOUR PREDECESSOR IN THIS CHAIN REPORTED: ` + JSON.stringify(prev).slice(0, 6000) + `
Verify its landed shape against the TREE before consuming it — never against its report.

Wave-end attribution per the LAWS. Append your ledger row with the ⏳ OPUS-ERA — FABLE SURVEY OWED heading and J-* judgment entries.

RETURN JSON: {commits, files, gates, goldenShifts, stopReports, judgments, notes}.`, { label: `build:${w.id}`, phase: w.phase, model: 'opus' })

  phase(w.vphase)
  const verdict = await agent(LAWS + VERIFIER_LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER FOR ${w.id}. Default posture: the implementer is WRONG until executed evidence says otherwise. Your job is to REFUTE, not confirm. The implementer reported: ` + JSON.stringify(build).slice(0, 12000) + `

VERIFY BY EXECUTION in git-archive trees: (1) every pin the wave claims is NON-VACUOUS — for each, plant the mutant that should red it and confirm it reds; a pin that stays green is a REJECT. (2) Dormancy: run the byte-identity hash yourself through the live pulse. (3) Every guard door deleted INDIVIDUALLY reds its own pin. (4) Lifecycle totality — independently execute the regen and undo round-trips for every new field; this estate's most-bitten class is the write that survives one path and ghosts another. (5) No flag was minted or lit. (6) Wave-end attribution at both granularities PLUS the red-ratchet CONTENT diff. (7) strict 1313; byte-scans; staged-set completeness.

${w.id === 'ES-4' ? 'SPECIFIC TO ES-4: verify the lighting marker actually REGISTERS — run the walker and confirm it reports the evidence SATISFIED, then MUTATE the pin into a describe title and confirm the walker stops crediting it. A marker that lights nothing is the exact failure this instrument was built over ten cuts to catch.' : ''}

A finding is only a finding if you EXECUTED the thing that proves it. RETURN structured JSON only.`, { label: `verify:${w.id}`, phase: w.vphase, model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

  results.push({ wave: w.id, build, verdict })
  if (verdict && verdict.verdict === 'REJECT') {
    log(`${w.id} REJECTED — the chain STOPS here so the chair can rule. Later ES waves consume this one's substrate directly.`)
    break
  }
  prev = build
}

return { debt, results }