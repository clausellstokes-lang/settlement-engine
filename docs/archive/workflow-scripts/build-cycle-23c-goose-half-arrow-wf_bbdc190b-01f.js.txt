export const meta = {
  name: 'build-cycle-23c-goose-half-arrow',
  description: 'Ribbon v3: the half-arrow with GREY GOOSE war fletching (owner corrected the species) · the icon-sweep golden ruling',
  phases: [{ title: 'Build', detail: 'goose half-arrow ribbon + disclosed golden re-record' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY: WR-8 live on worldPulse; typing/prose lanes on domain/generators — nav lane owns nav/* + theme.js + nav tests (+ App.jsx header block if needed); golden lane owns tests/fixtures/generator-golden-master.json + tests/property/generatorGoldenMaster.test.js + related fixtures + queue docs. STANDING RIBBON LAWS: overhang is PAINT never layout; ANCHOR_OFFSET derives; focus survives clipping; AA vs the worst stripe; single-writer tokens; pins cite the directive (2026-08-03 evening, reference photo + two clarifications).`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const lanes = await parallel([
  () => agent(`VERIFY-FIRST: OWNER-DIRECTED implementation from a reference photo + two live clarifications (2026-08-03 evening). ${P10}
THE BRIEF, FINAL FORM — A MEDIEVAL WAR ARROW, HALF-SEEN:
COMPOSITION: the top ribbon is HALF OF AN ARROW IN PROFILE — the arrow lies along the top screen edge and the viewport shows this side's half: the shaft's visible half runs the full ribbon width (under the wordmark too), shaded as a CYLINDER'S half (subtly darker toward the bottom edge, lighter above — the profile read); Create, Library, Realm are the NEAR SIDE'S THREE FLETCHES rooted on the shaft (quill bases meeting the shaft line, vanes sweeping toward Realm), ONE continuous object; the vanes' lower edges PEEK ~6px below the ribbon border (PAINT ONLY — the standing law).
THE SHAFT: honey-tan wood from the reference photo (~#C9A46B..#D9BC8A family, tune by eye), fine longitudinal grain, satin sheen — deterministic inline-SVG data-URI texture (feTurbulence FIXED seed), single-writer tokens in theme.js.
THE FLETCHES — GREY GOOSE, THE ENGLISH WAR-ARROW FEATHER (the owner corrected the species from turkey; medieval-European, historically exact): each tab is a goose PRIMARY in profile — a DARK GREY-BROWN vane (keep the owner's dark chocolate-brown tone family, goose-toned: cooler and more uniform than turkey), NO bold barring — instead the goose texture: FINE PARALLEL BARB STRIATIONS running diagonally off a pale central rachis (dense, subtle, like combed hair), a slightly PALER LEADING EDGE and soft tonal gradation darkening toward the trailing tip, gentle sheen bands where barbs catch light (real goose primaries show soft broad tonal bands, not stripes). This is QUIETER than turkey — which helps the labels. Rounded-back war-fletch silhouette (a low triangular/shield profile is period-correct for war arrows — not a long flowing plume), soft drop shadow, quill base visually bound to the shaft.
THE WRAPS: two narrow glossy RED-BROWN thread bands riding the shaft framing the fletching cluster (the photo's binding); silk/linen whipping look. Active grammar: the active fletch lightens (its sheen bands brighten) + the gold underline; focus ring proven on the clipped shape.
THE REST: reference tabs plain dark ink on the wood; wordmark dark ink; mobile bottom nav untouched; retire the prior turkey/gilt/parallelogram tokens cleanly.
PINS: fletch paths present per tab, barb-striation layers present, wraps present, cylinder shading present, overhang paint-only, AA vs the LIGHTEST tonal band under each label (quote ratios), focus by Tab, texture deterministic. Screenshot desktop+mobile in notes with an HONEST eye verdict (one half-arrow, or feathers-on-a-plank? painterly-real, or clip-art?) — iterate before committing if it falls short. One or two pathspec commits; npm run build green. Return {lane:'V3', commits, gates, stopReports, notes}.`,
    {label:'v3c:goose-arrow', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier with an EYE PASS. ${P10}
Report: ${JSON.stringify(r)}. Serve, screenshot desktop 1440x900 + mobile, and JUDGE: (a) does it read as ONE half-arrow in profile with rooted near-side fletches, (b) does the feather read GOOSE (fine parallel barbs, pale leading edge, soft tonal bands — NOT turkey stripes), (c) painterly-real or clip-art? Say each plainly. Mechanical: header layout box unchanged (paint-only mutant reds); ANCHOR derivation; AA recomputed vs the lightest band under every label (quote); focus by real Tab; texture deterministic (two renders byte-identical); quill bases meet the shaft (rendered geometry); prior-version tokens gone (grep); mobile untouched; nav suites + build green; pathspec-clean vs the golden lane. Return {lane:'V3 verify', verdict, findings}.`,
      {label:'v3c:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE GR — THE ICON-SWEEP GOLDEN RULING. THE FACT (bisected by cycle 20's verifier): tests/property/generatorGoldenMaster.test.js is RED at HEAD — all 525 rows drift from the fixture — caused by d9a1ea5a (the icon sweep removing 94 dead icon:"" slots from src/data files feeding generation). THE CHAIR RULING (vetoable, cite it): a DISCLOSED, OWNER-DIRECTED output-shape change — the owner ordered all icons removed; the slots were dead empty strings; semantic content unchanged. THE LAWFUL PATH: (1) FIELD-LEVEL DIFF FIRST over all 525 rows (old fixture from git history vs regenerated): prove the ONLY movements are icon-slot removals — enumerate every differing path-template; if ANYTHING else moved (a name, count, id, draw) STOP-AND-REPORT — that is stream corruption, not shape change; (2) on a clean diff only: re-record the fixture IN ITS OWN COMMIT quoting the path-template census + this ruling + the owner directive; (3) the golden test's docstring gains the dated shift record (HK-3 precedent); (4) census other fixtures embedding the same data-file icon fields (the 60-settlement hook corpus etc.) — same treatment, same proof each. Return {lane:'GR', commits, gates, stopReports, notes}.`,
    {label:'gr:golden-ruling', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Re-run the field-level diff YOURSELF with your own flattener: the differing path-template set must be EXACTLY the icon-slot class — quote the census; regenerate twice (byte-identical); the golden green at HEAD; the docstring shift record true; tests/property whole-run with every red attributed; pathspec-clean vs the nav lane. Return {lane:'GR verify', verdict, findings}.`,
      {label:'gr:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
])
return { lanes: lanes.filter(Boolean) }