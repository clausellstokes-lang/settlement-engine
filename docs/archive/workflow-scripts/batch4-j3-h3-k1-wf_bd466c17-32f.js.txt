export const meta = {
  name: 'batch4-j3-h3-k1',
  description: 'J3 (charter/decay events), H3 (circulation + replacement), K1 (institution status system) in parallel',
  phases: [{ title: 'Build', detail: '3 Opus implementers, disjoint ownership', model: 'opus' }],
}
const ROOT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const SCHEMA = { type: 'object', required: ['agent', 'fixes'], properties: { agent: { type: 'string' }, fixes: { type: 'array', items: { type: 'object', required: ['finding', 'status', 'files', 'tests', 'notes'], properties: { finding: { type: 'string' }, status: { type: 'string', enum: ['fixed', 'skipped', 'deferred', 'blocked'] }, files: { type: 'array', items: { type: 'string' } }, tests: { type: 'string' }, notes: { type: 'string' } } } }, behavior_shifts: { type: 'array', items: { type: 'string' } }, discovered: { type: 'array', items: { type: 'string' } } } }

const SHARED = `Worktree: ${ROOT} branch claude/composite-r4 (HEAD past 99974c4a) — verify; stop on mismatch. CONCURRENT: two sibling agents (ownership below — never touch theirs) and the owner's wizard-news session (wizardNews/settlementRumors/heraldRouting/informationStatecraft/momentum/supplyWebWarfare + tests — FOREIGN). A soak is live: never touch artifacts/soak/**; focused vitest only, one at a time; timeout-shaped reds get ONE isolation re-run.

⚠️ THE GOLDEN LAW, now the sharpest constraint in this program: batch 3 cost an owner-signed generator-golden re-record because I1 added GENERATING catalog data (assembleInstitutions draws once per candidate clearing its gates). YOUR SLICE MUST NOT SHIFT A GOLDEN. Everything you build ships DARK behind its existing virtual flag (routeLifecycleEnabled / npcConsequencesEnabled / a new magicEconomyEnabled for K1 — absent from DEFAULT_SIMULATION_RULES), consumes ZERO draws when dark, and must leave tests/property green and UNTOUCHED. If your change would move a golden, STOP and report rather than re-recording.

⚠️ FIRST-PAINT: closure is 1,020,590 of 1,040,000 (owner-gated). Never let an eager module (store, src/domain/events/mutate.js's handler table, eager components) statically import a heavy derivation — extract the function to a zero-import leaf (the userRouteIdentity precedent).

HARD RULES: no migrations; no git mutations; no full suites/builds; NO ratchet/baseline raises (adopt the kernel primitives — src/kernel/math.js clamp/clamp01 — never re-implement; new domain files carry ZERO any-casts; the voice ratchet counts em dashes in src/domain|src/data STRING LITERALS; the loading-narration ratchet is tolerance-0); determinism laws (seeded forks; house instruments: src/domain/lowDiscrepancy.js Weyl sampler for cell subsampling, draw accounting for stream-theft, SEED-FAMILY totality for restriction pins — a single-seed restriction pin is VACUOUS, structural purity scans); headless domain; 800-line ceiling (split leaves); anchored negatives; JSON-round-trip persistence pins; conditional worldState keys drop-when-empty; census/walker registrations are REGISTRATIONS with written reasons, never widenings; extend YOUR existing certification row rather than minting new rule keys (except K1, which mints one). Return dense structured data: statuses, files, verbatim test tails, negative-control quotes (revert/red/restore/green per new pin), behavior shifts plainly, judgment calls vetoable, discovered.`

phase('Build')
const results = await parallel([
  () => agent(`You are an Opus IMPLEMENTER for SettlementForge. SLICE J3 of the route-lifecycle program: CHARTER + DECAY. BINDING LAW: docs/DESIGN_ROUTE_LIFECYCLE.md §6 (demand-then-charter: accumulated corridor demand crosses the formation threshold AND the material objective clears its bar AND the expedition-worth ceiling passes ⇒ a CHARTER EVENT proposes through the docket, major-class when realm-shaping, E0 pacing applies, charters re-derive so they may WAIT — guaranteed-admission does NOT apply; on acceptance the edge materializes at 'track' grade, Herald-announced with flavour and goods reason; military charters may be power-initiated off strategicNeed without corridor accumulation), §7 (decay: sustained under-use + failing objective steps the grade DOWN on long dwells; HIDDEN persists; settlement destruction drops all its edges to hidden in the SAME outcome; revival re-evaluates hidden corridors FIRST with a warm-start bonus; garrison asymmetry — live strategicNeed floors an edge at 'road'), §5b (SAFETY VS GREED: danger-adjusted cost using the M1 danger vocabulary, routing reads BELIEVED danger through knownWorld, embattled regions DEFER charters and raise cost without removing, the RISK PREMIUM override lets profit carry a road through danger with loss rates feeding back), §5c (BYPASS GEOMETRY: believed danger above the bypass band lets through-corridors pay a detour premium to skirt a settlement's vicinity — waypoints are GEOMETRY, never new graph nodes; both edges may coexist; hysteresis on geometry too; the through-traffic feedback is the design's gift), §1 law 4 (HYSTERESIS: formation threshold >> removal threshold; wars never remove on the fast timescale). ${SHARED}

YOUR OWNERSHIP: src/domain/worldPulse/routeNetworkCharter*.js and routeNetworkDecay*.js (new leaves), extensions to J1/J2's routeNetwork*.js, your routeLifecycleEnabled certification row, tests/domain/routeNetwork{Charter,Decay,Danger}*.test.js. READ-ONLY: the docket/proposal machinery you EMIT into (never rewrite it), knownWorld, the danger vocabulary, the lifecycle kernel (destruction/revival hooks — coordinate by DESIGN DOC convention, do not edit H-program files).

IMPLEMENT J3 ONLY (no consumers/roamer physics — J4 owns those): the charter evaluator + event emission through the existing docket; the decay ladder with hidden persistence; danger adjustment + embattled deferral + the risk-premium override; bypass geometry with its detour premium and the through-traffic feedback; destruction→hidden and revival warm-start (via the design's convention); the garrison floor. PINS with executed negative controls: hysteresis (no flapping under an oscillating-demand fixture); anti-flap network-stability bound at a soak horizon; charters WAIT under docket pressure (they re-derive — prove they are not guaranteed-admission); hidden persistence (no edge with charter history ever reaches absence); the bypass feedback (an unsafe settlement measurably loses through-traffic); garrison asymmetry; dormancy byte-identical.`, { label: 'J3:charter-decay', phase: 'Build', schema: SCHEMA, model: 'opus' }),

  () => agent(`You are an Opus IMPLEMENTER for SettlementForge. SLICE H3 of the NPC-consequences program: CIRCULATION + REPLACEMENT. BINDING LAW: docs/DESIGN_NPC_CONSEQUENCES.md §5 (REPLACEMENT: banded delay, the slot refills through the EXISTING slot-inheritance lane — a roamer if one passes admission, else a fresh mint whose traits carry a MARGINAL bias toward settlement state, envelope-pinned at the designed effect size AND stationarity-pinned so the corruption-attracts-corruption loop is measured shut; locks and pinnedNpcs ride the existing _preservation remap unchanged), §6 (CIRCULATION: seeded rehost attempts against reachable settlements with exclusion edges filtered FIRST; admission — open capacity ⇒ lowest position, all full ⇒ found a sibling faction under that power, no compatible power ⇒ remain roaming; REJECTION from an authored closed compatibility table, emitting news that REFERENCES THE ORIGINAL SCANDAL, with a cooldown edge; turncoat flow to the compromising power's sphere with the closed capacity vocabulary advisor|agent|quartermaster|envoy|enforcer; EQUILIBRIUM: rehost pressure RISES with time-in-pool so unassigned roamers eventually settle themselves, pool size envelope-bounded, with a FLOOR so roaming is not trivially brief), §6b (REPUTATION AS BELIEF: the ledger records truth; what a settlement BELIEVES is DERIVED through the information layer — distance-priced decay from origin, complicated by infoMode; admission and rejection run against LOCAL BELIEF, never global truth; beliefs are derived ON DEMAND, never stored per-pair), §6c (UNAFFILIATES + RESIDENCY: roamers rest somewhere, stay durations banded, transitions on any advance, preference weighted toward settlements matching their state but never bounded; while resident, experiences reshape them through the EXISTING growth system within facet bands at capped rates), §6d (criminal destination generalization), §9 (the population floor reconciliation: pop >= resident named-NPC count; the empty fast path evaluates against pop MINUS resident named NPCs; a town reduced to its cast is terminal-decline eligible and disperses the cast in the SAME outcome), plus directive 8's travel physics (one hop per tick, connected-only, mid-route via the armyTransit ledger pattern; wanderers may use hidden paths, armies may not) — coordinate with J3's hidden paths by DESIGN DOC convention only, never by editing J-program files. ${SHARED}

YOUR OWNERSHIP: src/domain/worldPulse/npcCirculation*.js, npcReplacement*.js, npcResidency*.js (new leaves), extensions to H1/H2's npcLedger*/npcVerdict* files, your npcConsequencesEnabled certification row, tests/domain/npc{Circulation,Replacement,Residency}*.test.js. READ-ONLY: the growth system (npcGrowthKernel/trait planes — you CALL it), regenerationPreservation, the settlement lifecycle kernel, the belief/rumour derivation seams.

PINS with executed negative controls: replacement bias at the designed effect size (powered envelope, the Wave-A instrument) AND trait stationarity year-100 vs year-10; pool equilibrium bounded with its floor; rejection references the scandal; exclusion filtered FIRST (a banished roamer never rehosts home inside the window); belief-not-truth admission (a wanderer outruns their story on a long road, and does not on a short one); residency drift bounded within facet bands; the population floor reconciliation; travel physics one-hop; dormancy byte-identical.`, { label: 'H3:circulation', phase: 'Build', schema: SCHEMA, model: 'opus' }),

  () => agent(`You are an Opus IMPLEMENTER for SettlementForge. SLICE K1 of the magic-economy program: THE GENERAL INSTITUTION STATUS SYSTEM. BINDING LAW: docs/DESIGN_MAGIC_ECONOMY.md §3c (the closed vocabulary operational|impaired|shell; IMPAIRED is a CAUSE-BOUND CAPACITY MODIFIER in [reduced .. temporarily zero] — the engine derives a default severity from the cause, the DM may override anywhere in the range including full suspension, and the impairment is CAUSE-BOUND: every record references its live cause, lifts AUTOMATICALLY as an EVENT when the cause resolves, and can NEVER orphan; the closed cause vocabulary is supply_shortage | corruption_exposed | damage | siege_occupation | ...authored, and the cure follows the cause; SHELL is intact-but-unfunded, zero output, reactivated by recovery with warm-start; THE TWO-TIMESCALE LAW: impairment is the FAST layer, shell the SLOW verdict; they COMPOSE — an impaired shell is the darkest state short of ruin) and §13 K1's scope note: THIS SLICE IS MAGIC-INDEPENDENT and ships FIRST because every institution benefits before magic does. ${SHARED}

YOUR OWNERSHIP: src/domain/worldPulse/institutionStatus*.js (new leaves), a new magicEconomyEnabled rule key + its certification row in the appropriate lane file (read src/domain/certification/subsystemCertification.js for the row shape — this is the ONE new rule key authorized in this batch), tests/domain/institutionStatus*.test.js. READ-ONLY: the corruption web (the corruption_exposed cause), the lifecycle/ruin machinery, supplyWebWarfare (the supply_shortage cause — but its FILES are foreign-session territory: read only), the institution catalog.

IMPLEMENT K1 ONLY (no magic regimes, no buffer, no substitution — K2/K3/K4 own those): the status model as a conditional drop-when-empty ledger; the cause vocabulary with per-cause default severities in a tuning table; the capacity modifier with its DM-override seam (domain-side only — no store action this slice; expose the pure verbs and note the wiring for a later slice); automatic cause-resolution lift emitting an event; the NO-ORPHAN law as a walker-style pin (no impairment without a live cause); shell state with warm-start; the composition (impaired shell). PINS with executed negative controls: no-orphan totality; cause-resolution lifts automatically and emits; two-timescale (a fast cause impairs without any regime motion); DM override honored across the full range including zero; shell vs zero-capacity-impairment are DISTINCT states with distinct cures (the vocabulary ruling); warm-start; dormancy byte-identical with the fenced golden.`, { label: 'K1:institution-status', phase: 'Build', schema: SCHEMA, model: 'opus' }),
])

const all = results.filter(Boolean)
log(`${all.length}/3 batch-4 slices returned`)
return { results: all }