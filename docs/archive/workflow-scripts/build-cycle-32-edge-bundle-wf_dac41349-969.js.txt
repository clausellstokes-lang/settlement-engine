export const meta = {
  name: 'build-cycle-32-edge-bundle',
  description: 'The edge AI-grounding bundle rebuild (disclosed, dark) + the icon-sweep test leftover',
  phases: [{ title: 'Build', detail: 'one small lane, implement + verify' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; THE STAGED-SET LAW (git diff --cached --name-only before EVERY commit; prefer git commit -- <paths>); no stash; focused vitest + gate-tail (ps aux | grep vitest first); python byte-scan every Write; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed, NOTHING DEPLOYED. CONCURRENCY: the feather-surface lane is live on nav/brand/theme; the WR-8 finish lane on warDeployment/worldPulse — both fenced. Your territory: supabase/functions/_shared/aiGroundingBundle.js (the built artifact), tests/edgeFunctions/, tests/generators/tuningBatchE2.test.js.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE EB — TWO PIECES:
(1) CHAIR RULING CR-EB-1 (vetoable, cite it): THE EDGE BUNDLE REBUILD IS REPAIR, NOT DEPLOY. supabase/functions/_shared/aiGroundingBundle.js is a BUILD ARTIFACT of committed, verified source — it still carries the pre-PS spine (derivePeopleFear reading powerStructure.recentConflict — the retired arm that shipped the spliced 'People fear a return of the settlement is under active siege…' sentence — and deriveLikelyFuture reading .label || .name || 'open conflict', the dead-key defect PT2-3 retired). Rebuild it with the repo's own \`npm run build:edge-shared\`, commit the regenerated artifact IN ITS OWN COMMIT whose body DISCLOSES the fold (it carries lane PS's, PT2-3's, and MD's landed source — cite the commits) and quotes tests/edgeFunctions/aiGroundingBundle.freshness.test.js going 2-red → green. THE DEPLOY of edge functions remains the owner's migration-train step — say so in the commit body; nothing is deployed by this lane. If build:edge-shared regenerates MORE than this one artifact, or the regenerated bundle diffs in any way beyond the three cited lanes' changes (diff it against a hand-derived expectation), STOP-AND-REPORT.
(2) THE ICON SWEEP'S TEST LEFTOVER: tests/generators/tuningBatchE2.test.js asserts every stress-type icon 'is exactly empty' (expects '') but the icon sweep deleted the slots (gets undefined). Correct the test to the POST-SWEEP invariant, which is STRONGER: assert the icon key is ABSENT from every stress type (the dead-slot class must never return); docstring cites the sweep commit d9a1ea5a + the owner's icon directive.
Return {lane:'EB', commits, gates, stopReports, notes}.`,
  {label:'eb:edge-bundle', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify: the freshness suite green on committed bytes AND the bundle's contents actually carry the new spine (grep the ARTIFACT for the retired arm 'recentConflict' in derivePeopleFear and the dead-key chain — both must be GONE; the new refusal-routed derivation present); the rebuild is reproducible (run build:edge-shared again → byte-identical artifact); the diff contains ONLY the three cited lanes' semantic changes (read every hunk); nothing deployed (no supabase CLI deploy invocations in the transcript — check your own lane's report honestly); the stress-type pin asserts key-ABSENCE and reds under a mutant that re-adds an icon:'' slot (run it); pathspec + staged-set law. Return {lane:'EB verify', verdict, findings}.`,
  {label:'eb:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }