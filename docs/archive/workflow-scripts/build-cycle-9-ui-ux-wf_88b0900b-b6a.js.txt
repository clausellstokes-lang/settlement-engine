export const meta = {
  name: 'build-cycle-9-ui-ux',
  description: 'UI/UX parallel cycle: Herald causal-voice surface (display slice) · dossier-native state prose projection',
  phases: [{ title: 'Build', detail: '2 UI lanes, implement + verify each' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest before believing a timeout); strict-typecheck touched files yourself; CHECK-GIT-FIRST on every surface (git log -- <files> + PROGRESS blockquotes — several surfaces are ALREADY BUILT; audit-and-extend, never re-dispatch greenfield); STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Everything DARK/flag-gated per each spec, nothing pushed. CONCURRENCY FENCES (absolute): the war lane is LIVE on src/domain/worldPulse/* (applyWorldPulse, pulseKernel, envoy*, peaceTerms, coalitionRatification) — you may READ worldPulse, NEVER write it; the About repair lane is LIVE on HowToUse/aboutSplit/AboutManifesto/CompareSection/AboutWhatThisIs; LD/hook lanes may be live on NavRibbon/App.jsx/npcGenerator/hook files. Queue docs get concurrent edits — re-read before writing, carve hunks by pathspec. UX LAWS that bind every surface: LEGIBILITY LAW (glance → sentence → table); GAME-GRADE doctrine (TRANSLATE formulas, never show them); popup convention (no acknowledge/continue button ⇒ outside-click closes); DM-edited content is NEVER machine-dropped or overwritten.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const lanes = await parallel([
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE H — THE HERALD CAUSAL-VOICE SURFACE (SOL_QUEUE item 24, DISPLAY SLICE ONLY). The Herald EXISTS (realm inspector = newspaper, landed @ 69b7a8d3) — this is AUDIT-AND-EXTEND. The spec is the SP-6 Herald constitution in docs/DESIGN_FP_SPINE.md (the 2026-08-03 amendment blocks — read them whole) + docs/content/RECEIPT_POOLS_CAUSAL.md (78 join families × HEADLINE/SUBHEADER/TELLING registers + §4 integrity-disclosure lines). Build, in spec order:
(1) THE TWO REGISTERS on the existing Herald surface: headline (ONE causal gesture max — cascades forbidden by pin) + plain subheader (succinct, no embellishment); the TELLING register renders only behind the click.
(2) THE CAUSALITY POPUP: click a headline → popup with that headline's causality (the cause walk); no acknowledge button, so outside-click closes — build this as THE PORTABLE POPUP component (one component, reusable over the dossier later; you own popup work this cycle).
(3) MANIPULATION DISCLOSURE in the popup: the §4 integrity-disclosure lines — clean/worn/planted/planted-then-worn/unknown; a planted idea that mutated still shows the original INTENT (seed-not-growth law); intent and author named when known.
(4) THE TRUTH LAW + CONTAMINATION FENCE: Herald composers must be import-pinned AWAY from rumor/belief/disinfo sources (K3 inverted — write the import-pin test that reds if any Herald composer ever imports a belief surface).
(5) SEARCH over existing receipt fields (settlements, NPCs, goods, services, institutions, merchant houses — whatever fields today's receipts carry), organized and comprehensive; where a search facet NEEDS the mint-time typed slot fills (the Herald index closing the wizard-news id gap), that half is a STOP-REPORT row (mint-path edits are the war lane's territory) — build the reader shell so it lights when the index lands.
(6) RUMOR MILL LINKS UPWARD: each settlement-level rumor referencing a Herald-covered fact links to its headline (reader-side join on existing fields; report what today's fields cannot join).
Selection determinism: seeded draws via the avalanche-mixed hash (fmix32/avalanche32 over fnv1a32 — NEVER raw fnv1a32 % pool, the recorded parity hazard). Deterministic same-seed pins on every draw site. One pathspec commit per coherent step, PROGRESS blockquote in the spine doc at the end. Return {lane:'H herald', commits, gates, stopReports, notes}.`,
    {label:'laneH:herald-surface', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify on committed bytes: the one-causal-gesture headline pin reds on a two-gesture mutant; outside-click-closes proven in the suite and the popup has no acknowledge control; the contamination-fence import pin reds when a composer imports a belief/rumor module (run the mutant); seed-not-growth — a mutated plant still discloses original intent (fixture-proven); draws use the avalanche mix (grep for raw fnv1a32 modulo — zero hits in new code) and same-seed pins are non-vacuous; search facets match what receipts actually carry (probe with a real generated receipt set); rumor→headline links resolve on real fixtures; NO worldPulse file in any commit (git show --stat every commit); DM-editable fields untouched byte-identically where adjacent. Return {lane:'H verify', verdict, findings}.`,
      {label:'laneH:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE P — DOSSIER-NATIVE STATE PROSE PROJECTION. The corpus is BUILT and waiting: docs/content/RECEIPT_POOLS_DOSSIER_STATE.md (77/77 rendered dossier surfaces, 2,149 variants) + docs/content/RECEIPT_POOLS_CAUSAL_DOSSIER.md (78 join families × 6 state-anchored, [angle×arm] tagged). The spec is SP-6's dossier extension blocks in docs/DESIGN_FP_SPINE.md (DOSSIER-NATIVE register + state-prose corpus + foreign desk + THE DM-FIELD PROJECTION RULE). Build:
(1) the state-prose reader: for each dossier surface with a corpus entry, a deterministic seeded selection (avalanche-mixed hash, never raw fnv1a32 % pool) renders the dossier-native prose for the CURRENT state — economy, supply chains, power dynamics, plot hooks, defense, stressors, faith, war, and foreign policy at the local lens;
(2) THE DM-FIELD PROJECTION RULE, load-bearing: machine prose renders BESIDE DM-editable fields, NEVER into them — the byte-identity pin proves a DM-edited field survives a full projection render byte-identical;
(3) the [angle×arm] causal lines join state the settlement actually has — no line renders whose anchor state is absent (anchored-liveness pins: seed the state, see the line; absent state, absent line — use tests/helpers/anchoredNegatives.js or the walkers red);
(4) LEGIBILITY: glance rung → sentence rung → detail, matching each panel's existing pattern.
CHECK-GIT-FIRST hard: dossier panels exist and some prose wiring may already be partial. DO NOT touch: the portable popup or any Herald component (Lane H owns popups this cycle), worldPulse writes, About/LD/hook territories. One pathspec commit per desk-family; PROGRESS blockquote at the end. Return {lane:'P dossier', commits, gates, stopReports, notes}.`,
    {label:'laneP:dossier-prose', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify on committed bytes: the byte-identity DM-field pin is REAL (mutant: make the projector write into the DM field → pin reds; restore); anchored-liveness holds both directions on at least 3 state families you pick adversarially (seed state → line renders; strip state → line absent, and the absence pin is anchored, not vacuous — the recorded vacuous-absence-pin class); determinism (two same-seed renders byte-identical; avalanche mix confirmed, raw-fnv-modulo grep zero in new code); corpus coverage honest (count surfaces wired vs the 77 — report the gap, no silent truncation); panel legibility pattern matched not invented (side-by-side with an existing panel); pathspec-clean against every live lane. Return {lane:'P verify', verdict, findings}.`,
      {label:'laneP:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
])
return { lanes: lanes.filter(Boolean) }