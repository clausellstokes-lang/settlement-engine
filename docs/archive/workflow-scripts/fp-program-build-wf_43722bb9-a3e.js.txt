export const meta = {
  name: 'fp-program-build',
  description: 'Draft the six FP program volumes + couplings under the spine, then adversarially audit all seven',
  phases: [
    { title: 'Draft', detail: '7 Fable drafting agents, one volume each' },
    { title: 'Audit', detail: '9 Opus skeptics over the drafted corpus' },
  ],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const J = '/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/2e3ad88b-afe1-4b9a-98ca-b726d4aaa3d7/subagents/workflows'
const SPINE = `${MF}/docs/DESIGN_FP_SPINE.md`

const DRAFT_PRE = `VERIFY-FIRST: you are a DRAFTING agent for the SettlementForge Foreign Policy program (owner-ordered, chair-architected). Tree: ${MF} (claude/composite-r4; TWO-WRITER — Sol is live in src/; you edit ONLY your one assigned output file, no git ever, the chair commits). READ FIRST, in order: (1) ${SPINE} — THE CONSTITUTION; its twelve requirements bind every mechanism you write and its §3 crux rulings for your program are settled law you elaborate but never re-open; (2) your survey journal(s) below — findings[] entries with file:line evidence are your VERIFIED substrate (assert nothing about the tree beyond them without marking VERIFY-AT-BUILD); (3) ${MF}/docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md — THE FORMAT TEMPLATE: your volume mirrors its structure (binding laws → substrate census from your survey → waves each with scope/flag/model/seams/pins-negative-hardest/bands/Herald contract/lifecycle line/dossier round-trip → judgment blocks vetoable → tuning surface → Herald sentences → sequencing).
DENSITY CONTRACT: every mechanism carries ALL TWELVE requirements — law, force, NAMED counterforce off the same evidence, belief posture (Law One), named-actor casting through existing NPC planes, receipts in house voice, pins incl. the negative case, bands, couplings declared (pointing at DESIGN_FP_COUPLINGS.md), its clock vs INTERVAL_WEEKS + travel physics where anything moves, posture/risk consumption, its endings-vocabulary entries, and THE DOSSIER ROUND-TRIP pin (where does a DM see this on the town page — mandatory, the crown law). The survey's IMPOSSIBLE/PARTIAL drama stories for your domain become named TELLABLE pins in your waves. Take everything to the system's boundary; organic messiness welcome when receipt-derivable. History is your inspiration — name the historical archetype behind each major mechanism in one clause. Write in the house voice. Your volume must let Sol build without inventing architecture.`

const DRAFT_SCHEMA = { type:'object', required:['sections','openQuestions'], properties:{
  sections:{type:'array',items:{type:'object',required:['title','summary'],properties:{title:{type:'string'},summary:{type:'string'}}}},
  openQuestions:{type:'array',items:{type:'string'}}}}

phase('Draft')
const drafts = await parallel([
  () => agent(`${DRAFT_PRE}
YOUR VOLUME: ${MF}/docs/DESIGN_FP_TRADE.md (create it; ~this program's crux rulings live in SPINE §3 FP-TRADE). Survey journal: ${J}/wf_bc2973f7-35f/journal.jsonl (3 results: agency, measurement, mechanics — completion order). Key settled rulings to elaborate: the casus commercii taxonomy + mirrors; THE HOUSE at faction grain with named factors; believed markets via SP-2; FOOD JOINS THE PHYSICAL PIPELINE; pacts via SP-3; ventures through the plan lane; the endings set; the traveling factor via SP-1. The famine-speculator's moral pricing and the cornering counterforces deserve full depth.`, {label:'draft:trade', phase:'Draft', schema:DRAFT_SCHEMA}),
  () => agent(`${DRAFT_PRE}
YOUR VOLUME: ${MF}/docs/DESIGN_FP_FAITH.md. Survey journal: ${J}/wf_8cd152bc-5e1/journal.jsonl (3 results). Settled rulings: THE UNSEATING as believer-side transition (cures amendment C's dependency — the engine never confirms the god); pilgrims/legates via SP-1; the stance lanes' deferred halves LAND (scheduled, veto open); omen reads as belief writes; schism completes + underground faiths via the covert seam; faith terms into SP-3; the tithe coupling; narration 3x. The premium gate posture (Feature D.0) is respected — state what remains gated where.`, {label:'draft:faith', phase:'Draft', schema:DRAFT_SCHEMA}),
  () => agent(`${DRAFT_PRE}
YOUR VOLUME: ${MF}/docs/DESIGN_FP_POPULATIONS.md. Survey journal: ${J}/wf_56aa8cd1-644/journal.jsonl (3 results). Settled rulings: BELIEVED-CONDITIONS MIGRATION (the gold rush + the bust counterforce + letters-home correction); the commons voice extends to the popular arc (rung ladder, legitimacy-gated, priced); DEPARTURE MEMORY (diaspora bands both ends); the plague ARC; road drama on columns; the permit table's dead columns consumed. Wave P as-built is your substrate — extend, never re-open its judgment blocks (J-P1..J-P8 stand).`, {label:'draft:populations', phase:'Draft', schema:DRAFT_SCHEMA}),
  () => agent(`${DRAFT_PRE}
YOUR VOLUME: ${MF}/docs/DESIGN_FP_INFORMATION.md. Survey journal: ${J}/wf_4351db0d-d1e/journal.jsonl (results 0-2 are yours: mechanics, measurement, agency — completion order may vary; identify by content). Settled rulings: THE LURE (lies that bait — planted weakness/wealth/devotion; bluff-vs-bluff as the signature tragedy); PLANT_WIRING lands; the reputation race gains consumers; second-order belief as SP-2's derived heuristic (never recursion); counter-intelligence as deliberate agency joined to SP-4; narration from 1.5% to war-class with the spy's ARC. The medium's physics are the estate's best — your volume is almost entirely mind + voice.`, {label:'draft:information', phase:'Draft', schema:DRAFT_SCHEMA}),
  () => agent(`${DRAFT_PRE}
YOUR VOLUME: ${MF}/docs/DESIGN_FP_GRAMMAR.md. Survey journal: ${J}/wf_4351db0d-d1e/journal.jsonl (results 3-5 are yours: coverage matrix, mechanics, measurement — identify by content). Settled rulings: SP-3 elaborated in full (peacetime formation triggers per substance; two-sided drafting via negotiationPictures; FAITH + POPULATION term families; expiry receipts; succession-repudiation; the standalone NAP writer; renegotiation-from-strength); THE OATH-HOLDER IDENTITY on treaties/obligations; mediation generalized. WR-7 (envoys) is your war-side sibling — cross-reference, never duplicate. Every treaty negotiable at last: that sentence is your volume's thesis.`, {label:'draft:grammar', phase:'Draft', schema:DRAFT_SCHEMA}),
  () => agent(`${DRAFT_PRE}
YOUR VOLUME: ${MF}/docs/DESIGN_FP_INTERIOR.md. Survey journal: ${J}/wf_4351db0d-d1e/journal.jsonl (results 6-8 are yours: agency/inheritance, mechanics, measurement/drama — identify by content). Settled rulings: G'S TWO BOOKS LAND (WR-5's spec against the three confirmed substrates); THE INTERIOR VETO completes (the decision as organizing grievance, causal join receipted); the king-who-paid arc gains its narrated middle; faction foreign positions light; legitimacy's certification row; the memory decay seam closes; DELIBERATE FORGIVENESS (burying a grudge as priced policy). The interior is every other layer's driver — your couplings section is the largest; write it pointing at DESIGN_FP_COUPLINGS.md.`, {label:'draft:interior', phase:'Draft', schema:DRAFT_SCHEMA}),
  () => agent(`${DRAFT_PRE}
YOUR VOLUME: ${MF}/docs/DESIGN_FP_COUPLINGS.md — THE COUPLING MAP. Read ALL FOUR survey journals (${J}/wf_bc2973f7-35f, ${J}/wf_8cd152bc-5e1, ${J}/wf_56aa8cd1-644, ${J}/wf_4351db0d-d1e — journal.jsonl each) + the spine §4. Walk ALL 21 ordered pairs of {war, trade, faith, populations, information, grammar, interior}: per pair — the designed couplings (reads, receipts, counterforce, historical archetype named), what EXISTS today per the surveys (cite), what the six volumes ADD (coordinate via the spine's crux rulings — you may reference the volumes being drafted in parallel by their spine §3 rulings), and pairs left DELIBERATELY EMPTY declared as decisions. Include the cross-layer signature stories at full depth: the tax dispute that becomes a reformation; the pilgrimage route that becomes a war target; the bank broken by a believed rumor; the diaspora that funds the return; the famine speculator who buys a bishopric; the border town whose gold rush starts a war nobody wanted. Each story: the chain of named reads/receipts that derives it end to end (requirement 7's proof-by-construction).`, {label:'draft:couplings', phase:'Draft', schema:DRAFT_SCHEMA}),
])

phase('Audit')
const AUDIT_PRE = `VERIFY-FIRST: adversarial AUDITOR of the freshly-drafted Foreign Policy corpus (chair-ordered; the audit is a scheduled build stage — nothing ships unaudited). Tree: ${MF}; READ-ONLY except NOTHING — you edit no files; you return findings. The corpus: docs/DESIGN_FP_{SPINE,TRADE,FAITH,POPULATIONS,INFORMATION,GRAMMAR,INTERIOR,COUPLINGS}.md + the survey journals under ${J} as ground truth. REFUTE the corpus: hunt (a) DENSITY VIOLATIONS — mechanisms missing any of the spine §1's twelve requirements (shallow spots wearing deep clothing — your primary prey); (b) SUBSTRATE ERRORS — claims contradicting the surveys' file:line evidence or asserting unverified tree facts without VERIFY-AT-BUILD; (c) CONTRADICTIONS with the spine, with standing law (THE PROMISE, finite semantics, never-kill, the heuristic law), or between volumes; (d) COUNTERFORCE GAPS — forces without honest opposition (ratchet hunting); (e) DOSSIER ORPHANS — mechanisms with no dossier round-trip; (f) CLOCK VIOLATIONS — mechanisms without declared time signatures or movement outside the travel physics. Structured findings; CONFIRMED (you checked) vs PLAUSIBLE; only actionable findings.`
const AUDIT_SCHEMA = { type:'object', required:['findings','verdict'], properties:{
  findings:{type:'array',items:{type:'object',required:['title','kind','where','detail','confidence'],properties:{
    title:{type:'string'},kind:{type:'string'},where:{type:'string'},detail:{type:'string'},confidence:{type:'string'},fixSketch:{type:'string'}}}},
  verdict:{type:'string',description:'is this volume buildable at the twelve-requirement standard?'}}}

const audits = await parallel([
  () => agent(`${AUDIT_PRE} YOUR SCOPE: DESIGN_FP_TRADE.md vs its survey (${J}/wf_bc2973f7-35f/journal.jsonl) + the spine.`, {label:'audit:trade', phase:'Audit', model:'opus', schema:AUDIT_SCHEMA}),
  () => agent(`${AUDIT_PRE} YOUR SCOPE: DESIGN_FP_FAITH.md vs its survey (${J}/wf_8cd152bc-5e1/journal.jsonl) + the spine.`, {label:'audit:faith', phase:'Audit', model:'opus', schema:AUDIT_SCHEMA}),
  () => agent(`${AUDIT_PRE} YOUR SCOPE: DESIGN_FP_POPULATIONS.md vs its survey (${J}/wf_56aa8cd1-644/journal.jsonl) + the spine + the standing wave-P judgment blocks (J-P1..J-P8 must not be re-opened).`, {label:'audit:populations', phase:'Audit', model:'opus', schema:AUDIT_SCHEMA}),
  () => agent(`${AUDIT_PRE} YOUR SCOPE: DESIGN_FP_INFORMATION.md vs its survey (${J}/wf_4351db0d-d1e/journal.jsonl results 0-2) + the spine; special attention to Law One on second-order belief (heuristic, never recursion) and the lure's counterforces.`, {label:'audit:information', phase:'Audit', model:'opus', schema:AUDIT_SCHEMA}),
  () => agent(`${AUDIT_PRE} YOUR SCOPE: DESIGN_FP_GRAMMAR.md vs its survey (results 3-5) + the spine + DESIGN_PEACE_ENGINE.md and the WR volume (extend-never-fork discipline on peaceTerms; no duplication of WR-7).`, {label:'audit:grammar', phase:'Audit', model:'opus', schema:AUDIT_SCHEMA}),
  () => agent(`${AUDIT_PRE} YOUR SCOPE: DESIGN_FP_INTERIOR.md vs its survey (results 6-8) + the spine + WR-5 (G's books must extend WR-5's spec, not re-invent it).`, {label:'audit:interior', phase:'Audit', model:'opus', schema:AUDIT_SCHEMA}),
  () => agent(`${AUDIT_PRE} YOUR SCOPE: DESIGN_FP_COUPLINGS.md — all 21 pairs walked or declared; every signature story's derivation chain actually closes (walk each link against the volumes + surveys); no coupling rides a read that does not exist without VERIFY-AT-BUILD.`, {label:'audit:couplings', phase:'Audit', model:'opus', schema:AUDIT_SCHEMA}),
  () => agent(`${AUDIT_PRE} YOUR SCOPE: CROSS-CORPUS COHESION over all eight FP docs — vocabulary/flag collisions (also vs the estate's existing flags), the spine's rulings honored everywhere, shared-spine components specced ONCE with pointers elsewhere, sequencing coherent with §5, endings vocabularies disjoint where they must be, and the twelve requirements applied UNIFORMLY (compare density across volumes — name the thinnest volume and its thinnest mechanism).`, {label:'audit:cohesion', phase:'Audit', model:'opus', schema:AUDIT_SCHEMA}),
  () => agent(`${AUDIT_PRE} YOUR SCOPE: THE TUNING + INSTRUMENTATION LEDGER — sweep all seven volumes: every band named lands in a tuning table; every endings vocabulary has its envelope; every TELLABLE pin is testable as written; count the total band surface and flag bands that should be spine-family-shared but were minted per-volume; verify the pacing-governor registration is present in every program's news classes.`, {label:'audit:tuning', phase:'Audit', model:'opus', schema:AUDIT_SCHEMA}),
])

return { drafts: drafts.filter(Boolean), audits: audits.filter(Boolean) }