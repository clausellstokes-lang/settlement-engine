export const meta = {
  name: 'batch3-j2-h2-i1',
  description: 'J2 (flow ledger + self-sufficiency), H2 (verdict table + influence stripping), I1 (brokerage catalog) in parallel',
  phases: [{ title: 'Build', detail: '3 Opus implementers, disjoint ownership', model: 'opus' }],
}
const ROOT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const SCHEMA = { type: 'object', required: ['agent', 'fixes'], properties: { agent: { type: 'string' }, fixes: { type: 'array', items: { type: 'object', required: ['finding', 'status', 'files', 'tests', 'notes'], properties: { finding: { type: 'string' }, status: { type: 'string', enum: ['fixed', 'skipped', 'deferred', 'blocked'] }, files: { type: 'array', items: { type: 'string' } }, tests: { type: 'string' }, notes: { type: 'string' } } } }, behavior_shifts: { type: 'array', items: { type: 'string' } }, discovered: { type: 'array', items: { type: 'string' } } } }

const SHARED = `Worktree: ${ROOT} branch claude/composite-r4 (HEAD past 05786319) — verify with \`git -C ${ROOT} rev-parse --abbrev-ref HEAD\`; stop on mismatch. CONCURRENT WRITERS: two sibling agents (ownership below — never touch theirs) and the owner's wizard-news session (wizardNews/settlementRumors/heraldRouting/informationStatecraft/momentum/supplyWebWarfare + their tests — FOREIGN, never touch). A soak is live: never touch artifacts/soak/**; focused vitest only, one at a time; timeout-shaped reds get ONE isolation re-run.

⚠️ FIRST-PAINT DISCIPLINE (just reclaimed to 1,020,590 against a 1,040,000 owner-gated budget — do not spend it): NEVER let an eager module (anything reachable from main.jsx: the store, src/domain/events/mutate.js's handler table, eager components) statically import a heavy derivation. If an eager seam needs one function from a heavy module, EXTRACT THE FUNCTION to a zero-import leaf (the userRouteIdentity.js precedent, 2026-08-01) — do not import the module. Verify with a source-graph check if you touch any eager file.

HARD RULES: no migrations; no git mutations; no full suites/builds; no golden re-records (dormancy goldens stay green UNTOUCHED); NO ratchet/baseline raises of any kind (reclaim or fix the types — the any-cast baseline is 0 for new domain files, the raw-button budget is 45, the loading-narration ratchet is tolerance-0, the voice ratchet counts em dashes in src/domain|src/data STRING LITERALS); determinism laws (seeded forks; the house instruments: Weyl sampler src/domain/lowDiscrepancy.js for cell subsampling, draw accounting for stream-theft pins, seed-family totality for restriction pins — a single-seed restriction pin is VACUOUS, structural purity scans); headless domain; 800-line ceiling (split leaves); anchored negatives; JSON-round-trip persistence pins; conditional worldState keys drop-when-empty. New store actions need operationRegistry + gen:compendium-data + generate-sitemap + build:edge-shared IN ORDER — but prefer NO store action in an engine slice. THE CERTIFICATION TOTALITY WALKER guards all rule keys: extend YOUR existing certification row's aliveness channels if your slice adds observable evidence (the rows exist from J1/H1; do not mint new rule keys). Return dense structured data: statuses, files, verbatim test tails, negative-control quotes (revert/red/restore/green per new pin), behavior shifts plainly, judgment calls vetoable, discovered adjacencies.`

phase('Build')
const results = await parallel([
  () => agent(`You are an Opus IMPLEMENTER for SettlementForge. SLICE J2 of the route-lifecycle program: THE FLOW LEDGER + THE SELF-SUFFICIENCY METRIC. BINDING LAW: docs/DESIGN_ROUTE_LIFECYCLE.md §4 (three named flow classes — goods/population/military — written by EXISTING movers; informal flow on non-edges accrues corridor demand; usage on edges accrues decay's inverse; weights are tuning bands; receipts-first: every band step names its contributing flows) and §5 (THE MATERIAL OBJECTIVE: local unmet-demand reduction x system self-sufficiency + resilience credit − cost; REALM SELF-SUFFICIENCY emitted per-pulse into the behavioral observation). ${SHARED}

YOUR OWNERSHIP: src/domain/worldPulse/routeNetworkFlows*.js (new leaves), extensions to routeNetworkLedger.js's corridor accessors (J1's file — yours now), the self-sufficiency emission in scripts/audit/behavioral-observation.mjs (the v5 observation surface — additive only, never change v4/v5 field meanings), your routeLifecycleEnabled certification row's aliveness channels in src/domain/certification/subsystemRowsWaves.js, tests/domain/routeNetworkFlows*.test.js. READ-ONLY: the movers you read from (migrationKernel, commodity/trade flows, foodBalance channels, war deployment/mobilization), goodsCatalog/supplyChainData.

IMPLEMENT (J2 ONLY — no charter/decay events, no consumers; J3/J4 own those):
1. The three-class corridor/edge flow accrual: existing movers WRITE their traversals (they already move — you only COUNT). Population includes migration + mission traffic (desire paths). Military includes deployment/mobilization traversals. Goods includes commodity flow + unmet-demand pressure denominated in the goods vocabulary.
2. The MATERIAL OBJECTIVE as a pure scorer (no events yet): local + system + resilience − cost, with the goods denomination legible in its receipt ("grain wants to move west").
3. REALM SELF-SUFFICIENCY: a bounded 0..1 realm metric emitted per-pulse into the behavioral observation (additive v5 field), plus its derivation leaf. This is the metric the tuning pass will read — make it honest and cheap.
4. Pins with executed negative controls: flow accrual attribution (each class credits only its own movers — a seeded fixture where all three fire); the bounded-candidate law still holds under flow-driven corridors; self-sufficiency monotonicity on a constructed fixture (closing a material loop raises it); dormancy (flag dark ⇒ no ledger, no emission, byte-identical); JSON round-trip.`, { label: 'J2:flows', phase: 'Build', schema: SCHEMA, model: 'opus' }),

  () => agent(`You are an Opus IMPLEMENTER for SettlementForge. SLICE H2 of the NPC-consequences program: THE VERDICT TABLE + INFLUENCE RELINQUISHMENT. BINDING LAW: docs/DESIGN_NPC_CONSEQUENCES.md §3c (the TOTAL seeded verdict table over the closed vocabulary jailed|banished|turncoat|criminal_founding with the exact resolution order: rival-compromised ⇒ turncoat ELIGIBLE via weighted seeded choice; criminal-compromised + criminal power present ⇒ criminal_founding ELIGIBLE; otherwise prison present ⇒ jailed, else banished; banished/turncoat/criminal_founding all RESOLVE INTO roaming; jailed holds in place influence-stripped) and §4 (INFLUENCE RELINQUISHMENT: ladder position, faction role, and influence contributions stripped atomically across BOTH alias homes; the vacancy is NEVER silently refilled — it emits as a contested opening into the EXISTING succession/npcLadderContest/faction-competition machinery with a vacancy_from_disgrace cause tag). Also binding: directive 8's amendments (banished is a VERDICT, roaming is a STATE, the exclusion edge is banishment's mechanical distinction; jailed/roaming relinquish ALL influence). ${SHARED}

YOUR OWNERSHIP: src/domain/worldPulse/npcVerdict*.js (new leaves), extensions to H1's npcLedger*.js (yours now), your npcConsequencesEnabled certification row's channels in src/domain/certification/subsystemRowsPeople.js, tests/domain/npcVerdict*.test.js. READ-ONLY: the corruption web's covert/revealed seam, the institution catalog (prison presence = an institution-presence check), faction competition/succession/npcLadderContest (you EMIT into them, never rewrite them), the region powers (turncoat destinations).

IMPLEMENT (H2 ONLY — no circulation/rehosting/replacement, no UI/verbs; H3/H4 own those):
1. The verdict function: TOTAL over (exposure context x settlement state) — every cell resolves, closed vocabulary, deterministic seeded tie-breaks, NO fall-through (the mountain_pass class is the recorded cautionary tale: prove totality with an exhaustive cell walk, not samples).
2. Influence stripping: atomic across npcs[] AND factions[].members[] (the JSON-alias trap — round-trip your fixtures), emitting the contested opening with its cause tag into the existing machinery.
3. Exclusion edges minted on banishment (H1's ExclusionEdge shape); jail holds with a tunable term; every verdict emits an address-chain Herald item whose receipt names its inputs.
4. FIRES ONLY ON REVEALED corruption (Law 2) — pinned with an anchored negative that covert state never triggers a verdict.
5. Pins with executed negative controls: verdict totality (exhaustive cell walk); the resolution ORDER (a rival-compromised NPC in a prison town can still be jailed — eligibility is not certainty); influence stripping across both alias homes with round-trip; the vacancy emits (never silently refills); exclusion minting; revealed-only; dormancy byte-identical.`, { label: 'H2:verdicts', phase: 'Build', schema: SCHEMA, model: 'opus' }),

  () => agent(`You are an Opus IMPLEMENTER for SettlementForge. SLICE I1 of the information-brokerage program: THE CATALOG + THE RUMOR-SOURCE TAGS. BINDING LAW: docs/DESIGN_INFORMATION_BROKERAGES.md §0b (rumor sources are WHERE TALK HAPPENS; brokerages are HOW TALK IS WEIGHED — distinct roles, never conflated; the data pass adds a closed rumorSource service tag to the existing talk institutions so the plane's sources become census-able), §3 (the four catalog entries: Listening Post legal minor town+; Chroniclers' Exchange legal major city+ subsuming the Post; Rookery illegal minor town+ requiring a criminal-organization power, weighted by rumorSource density; Whisper Market illegal major city+ subsuming the Rookery — each carrying service keys from the closed set info_calibration|info_query|info_feed|info_plant), §4 (the six-channel closed vocabulary trade|war|politics|faith|crime|persons with legal/illegal competence inversion in an authored tuning table). ${SHARED}

YOUR OWNERSHIP: src/data/institutionalCatalog.js (the four new entries + the rumorSource tags on existing talk institutions — brothels, coaching inns, fences and their kin), the generated service-key index (run \`npm run gen:institution-service-keys\` — it is a --check gate step), a new INFORMATION_BROKERAGE_TUNING table leaf, tests/data|tests/generators for the catalog pins. READ-ONLY: everything else. DO NOT implement stamps, fidelity, services, or plants (I2/I3/I4 own those) — I1 is INERT catalog data plus its census tags.

IMPLEMENT (I1 ONLY):
1. The four entries in the house catalog idiom (tier bands, baseChance weighted by trade connectivity, tags, priorityCategory, desc prose in the catalog's voice — study neighbouring entries and match; the catalog already speaks this design's language, e.g. the permanent circle's "extremely expensive to construct and maintain").
2. Subsumption: Exchange subsumes Post, Market subsumes Rookery, through the EXISTING subsumption pass — pinned.
3. The rumorSource data pass: a closed service tag on the existing talk institutions, making the rumor plane's sources census-able (pin the census).
4. The channel-competence tuning table (authored, closed vocabulary, PROPOSED-band shape) — data only, no consumers yet.
5. Pins with executed negative controls: the four entries generate at their tier bands (seed-family, not a single seed); subsumption both pairs; the Rookery's criminal-power precondition (absent power ⇒ absent Rookery — anchored negative); rumorSource census totality; validate:data + the service-key --check gate green; NO generation-golden shift (these are new catalog entries — if a golden moves, the entries are reachable in a golden config and you must report it rather than re-record).`, { label: 'I1:brokerage-catalog', phase: 'Build', schema: SCHEMA, model: 'opus' }),
])

const all = results.filter(Boolean)
log(`${all.length}/3 batch-3 slices returned`)
return { results: all }