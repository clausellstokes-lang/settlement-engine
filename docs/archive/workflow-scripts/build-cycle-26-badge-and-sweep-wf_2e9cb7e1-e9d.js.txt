export const meta = {
  name: 'build-cycle-26-badge-and-sweep',
  description: 'The maker\'s plate + wax-seal wordmark (task #78 spec) + the fletch sweep correction — one nav lane',
  phases: [{ title: 'Build', detail: 'badge + sweep, implement + verify with eye pass' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files (nav/*, theme.js, nav tests, App.jsx header block); no stash; focused vitest + gate-tail (ps aux | grep vitest first); python byte-scan every Write; npm run build + smoke:boot green; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. STANDING RIBBON LAWS: overhang/peek is PAINT never layout; ANCHOR derivation intact; focus ring rides an aria-hidden vane never the clipped button; AA re-censused on EVERY header rider incl. AccountMenu's transparent chip (the ground-move class has bitten 3×); screenshot BEFORE pinning geometry (two cuts passed every pin and looked wrong). Read task #78's FULL description — it is the research-locked badge spec — plus the ribbon memory files (ribbon-ground-move-foreground-break-class, paint-outside-the-box-without-buying-a-box).`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: OWNER-DIRECTED implementation. ${P10}
LANE BG — TWO PIECES, ONE COMPOSITION, on the landed v3 half-arrow @ ea0be549:
PIECE 1 — THE FLETCH SWEEP (the chair's eye-pass finding on the live render): the three vanes currently read as HANGING PENNANTS — tips pointing straight down, no visible lean. Give them the war-fletch SWEEP: leading edges slanted, vanes leaning back toward Realm (the flow direction), barb striations angled WITH the lean; the silhouette stays the low period-correct war-fletch, the peek below the bar stays paint-only. Iterate against screenshots until a squint says "feathers on an arrow," not "bunting on a beam" — then pin the geometry (slant angle asserted, lean direction asserted).
PIECE 2 — THE MAKER'S PLATE + WAX-SEAL WORDMARK, built EXACTLY to task #78's research-locked spec (read it verbatim — it carries every law: mid-tone bronze face at the 5:6 module system, letterform FILL carries all AA with the Rule of Tincture enforced in code, layered zero-blur text-shadow for the engraving, three-shadow mounting stack, one global light-direction token, 1px near-black keyline, grit not gloss, chroma rationed to the seal's red alone, the seal's IMPRESSION IS THE COUNTER at every size, redraw-for-size variants, typed contexts stay plain, favicon = device alone). THE BALANCE LAW (chair): the plate stays a half-step QUIETER than the fletching — if a screenshot shows the eye ping-ponging between two equal dark masses, lighten the plate, never darken the feathers. The plate replaces the current wordmark lockup at the shaft's left, mounted ON the wood (rivets, contact shadow), whole plate = the home button (generous hit target, aria-label Home). Keep the tt-gable and plain-lettering fallback modules behind a comment switch for the eye pass.
Screenshot desktop 1440x900 + mobile + a 200%-zoom crop of the plate in your notes with honest eye verdicts at each iteration. One commit per piece. Return {lane:'BG', commits, gates, stopReports, notes}.`,
  {label:'bg:badge-sweep', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier with an EYE PASS. ${P10}
Report: ${JSON.stringify(r)}. Serve, screenshot, and JUDGE: (a) do the vanes now SWEEP toward Realm (feathers, not bunting)? (b) does the wordmark read "SettlementForge" INSTANTLY at ribbon size with the seal as the o (the typo-effect test — cold-read it), and does the seal read as wax (matte, one highlight, impression-as-counter) rather than a candy button? (c) is the plate SUBORDINATE to the fletching (the balance law)? (d) grayscale screenshot: does the seal still bind into the word without its red? Say each plainly. Mechanical: header layout box unchanged; ANCHOR derivation; AA recomputed on EVERY rider against the new grounds (quote all ratios incl. AccountMenu's chip); the Rule-of-Tincture code constraint exists and a violating pair mutant reds it; focus ring by real Tab on the plate AND a feather; texture seeds from the vetted allowlist (the 31 degenerate seeds excluded, unit-tested); typed contexts still plain (grep title/alt/meta for the seal glyph — zero); favicon unchanged this lane unless the report claims it; npm run build + smoke:boot; pathspec-clean. Return {lane:'BG verify', verdict, findings}.`,
  {label:'bg:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }