export const meta = {
  name: 'build-cycle-29-mirror-death',
  description: 'THE MIRROR DIES: one writer for the likely-future sentence · the re-record record fixes · guard precision + corpus totality pin',
  phases: [{ title: 'Build', detail: 'one lane, implement + verify' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); python byte-scan every Write; SHARED-RATCHET LAW; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY: the nav lane is live on nav/theme; WR-8 razing on worldPulse — both fenced. Your territory: simulationSpine.js, historyBeats.js, their tests, settlementOriginProse module+test, the generatorGoldenMaster UPDATE_GOLDEN comment, the named docs lines, scripts/mutation-sweep.sh + manifest.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE MD — FOUR PIECES, one commit each:
(1) CHAIR RULING — THE MIRROR DIES (the structural cure for a fork class that has now produced FOUR divergences: dead-key, label-refusal, casing, first-null): historyBeats.js's deriveLikelyFuture stops RE-DERIVING and CONSUMES the spine's own derivation — export the spine's likely-future deriver (or extract a shared leaf BOTH call — pick whichever keeps module layering clean; ONE writer for the sentence either way); historyBeats' local tensionName/arm logic for this rung is DELETED; the casing fork and first-null fork die by construction (verify with the verifier's own probes: 'grain owed to House Merrow' must keep its capitals in the beat; the null-first tension list must pick the same arm both sides); the lockstep suite becomes a true identity check (byte-equal on shared fixtures, NO toLowerCase laundering — the blind pin is repaired or retired with its replacement named); the 5 consumers re-run green; docstrings updated to say CONSUMES, not mirrors.
(2) THE TWO CURRENT-TENSE 523s: tests/property/generatorGoldenMaster.test.js:158 (the comment inside the UPDATE_GOLDEN branch — the one a re-recorder reads at the moment of re-recording) and docs/DESIGN_HOOK_NONREDUNDANCY.md:193 (which describes the HK-3 re-record itself) → 525, each with a one-word dated note.
(3) THE RR RECORD FIXES: the shift record's '45 bodies' → the machine-counted 40 (8 arms × 5, state the derivation); THE CORPUS-SIZE TOTALITY PIN: assert arms === 8 AND every pool length === 5 (a pool shrinking to 3 must red — mutant it); the icon-guard false-positive fix: the widened regex matches any identifier ending in 'icon' (e.g. lexicon) — anchor it to a real Icon boundary (uppercase-I suffix or an explicit suffix list), with a negative fixture proving 'lexicon: \"\"' passes and 'resourceIcon: \"\"' reds.
(4) THE COPY NIT: isolated.deficit#1's list-splice comma ('…or patronage, at a price…') → an em-dash or period so the list's end is visible; this touches ONE authored body = a golden shift for any seed drawing it — fold it as a disclosed one-body amendment INSIDE the still-open RR window's recipe (field-diff proving exactly that one body class, own commit, the window's ruling cited) — if the window's discipline cannot be honestly extended, STOP-report instead.
Return {lane:'MD', commits, gates, stopReports, notes}.`,
  {label:'md:mirror-death', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Re-execute the House Merrow casing probe and the null-first arm probe on committed bytes (both sides byte-agree now); confirm ONE writer (grep: historyBeats carries no local likely-future derivation; the shared deriver has exactly the expected importers); the identity check is toLowerCase-free (read it) and reds under a one-character case mutant; the 523s gone (grep, quote); the corpus totality pin reds under a pool-shrink mutant (run it); the guard's negative fixture honest (run both directions); if the copy-nit shift landed, re-run the one-body field-diff yourself (exactly one body class moved, nothing else); all suites + ratchets green; pathspec-clean vs the nav and war lanes. Return {lane:'MD verify', verdict, findings}.`,
  {label:'md:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }