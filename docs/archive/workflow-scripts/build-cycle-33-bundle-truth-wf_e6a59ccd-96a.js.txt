export const meta = {
  name: 'build-cycle-33-bundle-truth',
  description: 'CR-EB-2: all five edge bundles rebuilt from clean HEAD in one disclosed commit + the reproducibility pin',
  phases: [{ title: 'Build', detail: 'one lane, implement + verify' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; THE STAGED-SET LAW (git diff --cached --name-only before EVERY commit; prefer git commit -- <paths>); no stash; focused vitest + gate-tail (ps aux | grep vitest first); python byte-scan every Write; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed, NOTHING DEPLOYED. CONCURRENCY: feather-surface lane on nav/brand/theme, WR-8 finish on warDeployment/worldPulse — fenced. Your territory: supabase/functions/_shared/* (the 10 built artifacts), scripts/build-edge-shared.mjs IF the reproducibility pin needs a builder hook, tests/edgeFunctions/*.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE BT2 — CHAIR RULING CR-EB-2 (vetoable, cite it; answers the EB lane's three questions):
(a) THE FOLD IS ACCEPTED AS REPAIR. Dark modules PRESENT in a bundle do not execute — the flags gate the edge runtime exactly as they gate the app bundle, which already ships every dark module in dist. Presence-not-execution is the estate's standing dark posture. The disclosure carries the burden: the commit body enumerates the full fold (the 16 commits, the 13 new modules, naming the dark families — WR-5/WR-6 coalition graph/ledger/taxonomy, HK-2 retention, MG-3h arcane classifier) so the OWNER's next deploy decision is fully informed.
(b) ALL FIVE BUNDLES REBUILD IN ONE COMMIT from clean committed HEAD — no per-entry builder flag. The right state is every bundle fresh from ONE tree with ONE generatedAt; leaving the two stale siblings would preserve two UNREPRODUCIBLE artifacts as the deploy truth, which is worse than any fold.
(c) THE DIRTY-TREE INCIDENT IS RECORDED AND CURED BY THE REBUILD: aiCharterBundle + aiOutputSchemaBundle were built 2026-08-02 from a dirty tree (sourceHashes resolving to NO commit) — name the incident in the queue row; the rebuild replaces them with reproducible artifacts.
EXECUTE: (1) from a CLEAN verified tree state (git status over all inputs empty of foreign WIP — if input files carry uncommitted lane WIP, STOP), run npm run build:edge-shared; (2) prove REPRODUCIBILITY: run it twice — the artifacts must be byte-identical except generatedAt (if generatedAt is inside the hashed content, note it); (3) commit all 10 files in ONE disclosed commit per the ruling (the fold enumeration per bundle; the freshness suites' before/after counts; DEPLOY REMAINS THE OWNER'S TRAIN stated plainly); (4) THE REPRODUCIBILITY PIN (structural prevention for the dirty-tree class): a test asserting every .meta.json sourceHash resolves to the recorded builder-hash of its inputs AT THE COMMITTED TREE (the dirty-build class must red); mutant: doctor one meta hash → red. All five freshness suites green at the end. Return {lane:'BT2', commits, gates, stopReports, notes}.`,
  {label:'bt2:bundle-truth', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify: all five freshness suites green on committed bytes; the ARTIFACTS grep-clean of the retired arms (recentConflict in derivePeopleFear, the dead-key chain — gone from aiGroundingBundle); rebuild reproducibility re-run by YOU (byte-identical modulo generatedAt); the fold enumeration in the commit body matches YOUR OWN git-diff derivation over the inputs (spot-check 5 commits + 5 new modules); the dark families' flags un-lit in the edge runtime defaults (grep the edge functions' flag reads — presence-not-execution holds); the reproducibility pin reds under the doctored-meta mutant (run it); nothing deployed (audit the lane transcript for any deploy invocation); staged-set law; pathspec-clean vs the nav + war lanes. Return {lane:'BT2 verify', verdict, findings}.`,
  {label:'bt2:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }