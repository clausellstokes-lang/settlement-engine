export const meta = {
  name: 'the-integration-fold-ep-hb-wc',
  description: 'THE FOLD: EP, HB and WC into canonical homes on claude/composite-r4, plus the cross-branch snapshot deletion. Unblocks 33 waves.',
  phases: [
    { title: 'Prep', detail: 'the SP-D seam row, the pre-fold count, and the readiness proof' },
    { title: 'Fold', detail: 'the volumes, the parent, the registry, the queue' },
    { title: 'Verify', detail: 'adversarial verification of the whole fold' },
    { title: 'Delete', detail: 'the three-part cross-branch snapshot deletion' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const LEDGER = '/Users/cstokes/Desktop/settlement-engine'

const LAWS = `VERIFY-FIRST PREAMBLE: chair brief, owner-delegated build program. LIVE CODE OUTRANKS EVERY TABLE; a doc overstatement is a STOP-and-report. Build tree ${TREE} (branch claude/composite-r4). Ledger tree ${LEDGER} (branch review-fixes-2026-07-08). Hard-gate every state-mutating compound with the \`cd\` FIRST: \`cd <tree> && [ "$(git branch --show-current)" = "<expected>" ] || exit 1\`.

⭐ **THE MUTEX WAIT AND THE RUN ARE ONE \`&&\` CHAIN, NEVER TWO STATEMENTS** — issued separately, a wait TIMEOUT lets vitest start anyway, which is the fake-red the mutex exists to prevent. \`sh scripts/gate-mutex.sh --wait && sh scripts/gate-tail.sh npx vitest run <paths>\`. ⛔ Never hand-spell the ps-grep; never read an exit code through a pipe. ⚠ zsh does not word-split unquoted parameters.
⚠ **BUILD SUITE ARCHIVES ONE AT A TIME AND DELETE BETWEEN** — the machine's data volume hit 100% yesterday, ENOSPC truncated a full-suite log to ZERO BYTES, and a truncated log is a silent false receipt with no author. Free space is ~11 GiB; each archive is ~250 MB. INTEGRITY-COUNT every archive before trusting it (\`git ls-tree -r <sha> | wc -l\` vs \`find -type f | wc -l\`), build with \`git archive -o file.tar\` + \`tar -xf\` NEVER through a pipe, and \`rm -f\` the node_modules SYMLINK before removing any tree.
⚠ **EVERY CENSUS FIGURE IS TAKEN IN AN ARCHIVE OF A COMMITTED SHA, NEVER THE WORKING TREE** — six recorded instances of a shared tree measuring another lane's work.
⚠ A SECOND BUILD LANE runs concurrently on src-side burn-downs. Its files are disjoint from yours. NEVER touch, stage or clean a file you did not author. EXPLICIT PATHSPECS; verify \`git diff --cached --name-only\` against your EXPECTED set BY NAME AND COUNT before every commit.

REPORTING: CONFIRMED (executed evidence quoted) or PLAUSIBLE. Never present the second as the first.`

phase('Prep')
const prep = await agent(LAWS + `

YOU ARE THE FOLD PREP SLICE. Do NOT fold. Produce the three things the fold needs and STOP.

READ FIRST, on the LEDGER branch: \`docs/architected-volumes-pending-fold/README.md\` (THE RECIPE, corrected against the worked precedent), \`WC_SEAM_TABLE.md\`, \`EP_ROUND7_CONFIRM.md\` (its tail carries the chair's E5 ruling and three BINDING CONDITIONS), \`HB_ROUND4_CLOSE.md\`.

**(1) ADD THE SP-D SEAM ROW TO \`WC_SEAM_TABLE.md\` AND RE-RUN THE COUNT.**
The chair RULED the count is **23, not the 22 transcribed** — SECTION 4 has no SP-D subsection, but §7.E names "SP-D call-ins" as a pre-pinned seam row, §7.F gates WC-5 on it, and §2.3 attaches call-in carriage to SP-D's errand purpose classes. A seam is a contract plus a tripwire and SP-D has both.
⚠⚠ **THE ROW MUST CARRY A DISCHARGE OBLIGATION: \`errandSpineEnabled\` is LIVE in src — SP-D HAS LANDED — so this is a DISCHARGEABLE pre-pin, not a pending one.** State that the counterpart exists and name what consumes it, or the row certifies nothing while looking green.
Then re-run the counting rule (\`seams()\` in \`HABIT_countsweep.py\`) against the file and **confirm it returns 23**. Do not hand-edit the number. ⚠ Header spelling stays \`Neighbour\` — the parser compares header cells by EXACT equality, so \`Neighbor\` returns LookupError and the count reads as ABSENT rather than zero.

**(2) TAKE THE PRE-FOLD PARENT COUNT.** Binding condition C3: EP's VERIFY-AT-FOLD trigger re-fires on any fold landing before EP's rows are composed, so the parent count must be taken BEFORE anything folds. Measure \`docs/DESIGN_FP_ARCHITECTURE.md\` at the current committed sha: §5 wave rows, §3 flag rows, §9 seam rows. **Expected 75 / 52 / 66 — if any differs, that is a STOP-and-report.**
⚠⚠ **AND KNOW THIS BEFORE YOU COUNT, OR YOU WILL FIRE A FALSE STOP: §9 carries 47 PHYSICAL data rows against a stated 66.** That is the fold convention, not a defect — rows 46 and 47 are COMPRESSED RANGE rows (ES 13, WY 8) and 45+13+8=66. **A folded volume contributes ONE physical row and +N logical seams.**

**(3) COMPUTE AND STATE THE POST-FOLD ARITHMETIC**, with every count-bearing site in the parent ENUMERATED by line so the fold can move all of them and prove the stale spellings are at zero afterwards. Waves 75+33. Flags 52+11. Seams 66 + EP 10 + HB 13 + WC 23.
⚠ **THE PREFIX REGISTRY GOES TO TWELVE, NOT FOURTEEN** — EP explicitly DECLINES a prefix, WC never mentions the registry across 6,492 lines, and only HB claims one (ruled: HB as the twelfth, with its own leaf). Verify all three claims yourself.

Commit ONLY the \`WC_SEAM_TABLE.md\` edit, on the LEDGER branch. RETURN JSON: {seamRowAdded, seamCountVerified, preFoldCounts, postFoldArithmetic, countBearingSites, prefixVerdict, stopReports, notes}.`, { label: 'fold:prep', phase: 'Prep', model: 'opus' })

phase('Fold')
const fold = await agent(LAWS + `

YOU ARE THE FOLD IMPLEMENTER. This unblocks 33 build waves — the largest single blocker in the program.

PREP REPORTED: ` + JSON.stringify(prep).slice(0, 8000) + `
⚠ Verify its numbers against the tree yourself before consuming them.

**THE RECIPE IS WRITTEN AND CORRECTED — read \`docs/architected-volumes-pending-fold/README.md\` on the ledger branch and follow it.** It was corrected against the ES+WY precedent and two things it once said are FALSE: the fold was **FOUR commits, not three** (the fourth cured four one-line misses and a line range that rotted mid-fold — budget it as part of the procedure, not as a mistake you will avoid), and **"folding renumbers sections" is deleted** — the incoming volume moves UNCHANGED in substance, because renumbering would rot every live cross-reference. What renumbers is the PARENT's global sequence.

**⛔ BINDING CONDITION C3 FROM THE E5 RULING: EP'S ROWS COMPOSE LAST**, and the parent count was taken pre-fold. Compose WC and HB first, EP last.

**THE OBLIGATIONS**, from the README's eight:
1. Copy the three volumes to \`docs/DESIGN_FP_ARCH_{EP,HB,WC}.md\` on the build branch, UNCHANGED in substance — header provenance, the promotion note and the self-reference are the ONLY transformations. Read the bytes from the ledger tree's filesystem path or \`git show review-fixes-2026-07-08:<path>\`. ⛔ NEVER \`git checkout <branch> -- <path>\` — that is the checkout family and it writes minifold's index.
2. The parent's §3 flag rows, §4 canonical-model entries, §5 wave blocks, §9 seam rows and §11 pointer.
3. **The parent's header counts at EVERY site prep enumerated**, and DECLARE THE NEGATIVE CONTROL in the commit body: the stale spellings must all be at ZERO afterwards.
4. A \`SOL_QUEUE.md\` row. ⚠ Integers are NEVER renumbered — folds add LETTERED sub-rows.
5. **The prefix admission — HB ONLY, and FOUR EDIT SITES IN ONE FILE.** \`tests/domain/couplingRegistry.test.js\` pins the closed set TWICE and both move together: the \`CHARTERED_VOLUME_PREFIXES\` const the id-shape regex is BUILT FROM, and the literal \`toEqual([...])\`. Editing only the const leaves the toEqual red; editing only the toEqual leaves the regex rejecting new ids. Plus TWO legibility sites — the count-bearing test title ("all eleven…") and the CLOSED-alternation comment.
6. Cross-volume reconciliation. ⚠ **This is where the precedent LEAKED.** A contradiction found in a third volume becomes a REPORTED ERRATUM with the struck text preserved, never a silent rewrite.
7. The ledger row. ⚠ Append at the LIVE tail, **re-derived immediately before applying** — the precedent's old anchor was still UNIQUE and had stopped being the TAIL, and uniqueness is not position.
8. ⚠ Record the fold's own discharge by hand; the precedent's queue item appears nowhere in its commits.

⛔ **DO NOT DELETE THE SNAPSHOTS** — that is a separate cross-branch act and it lands AFTER this.

Gate through \`sh scripts/gate-mutex.sh --wait && sh scripts/gate-tail.sh …\` on the registry test, the coupling walkers and the docs walkers. Wave-end attribution, ratchet half via \`sh scripts/ratchet-inventory.sh\`.

RETURN JSON: {commits, volumesLanded, parentCountsAfter, negativeControl, prefixSites, errata, gates, stopReports, ledgerRowText, notes}.`, { label: 'fold:implement', phase: 'Fold', model: 'opus' })

phase('Verify')
const verdict = await agent(LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER FOR THE FOLD. Default posture: WRONG until executed evidence. REFUTE. The implementer reported: ` + JSON.stringify(fold).slice(0, 12000) + `

⚠ Do EVERY execution inside an integrity-counted \`git archive\`, ONE AT A TIME, deleting between (disk is tight). ⚠ RE-RUN EVERY GATE THE REPORT QUOTES at the committed sha — seven false receipts have landed in this program and the code was nearly always fine; the defects were in the receipts.

VERIFY: (1) **THE NEGATIVE CONTROL** — grep the parent for every stale count spelling and confirm ZERO. That is the fold's own declared proof and it is the first thing to check. (2) The three volumes moved UNCHANGED in substance — diff each against its ledger-branch source and confirm only the declared transformations. (3) **THE PREFIX ADMISSION IS COMPLETE AT ALL FOUR SITES** — plant a thirteenth prefix and confirm the regex rejects it; plant a mismatch between the const and the toEqual and confirm it reds. (4) The post-fold arithmetic is right: re-count §5, §3 and §9 yourself. ⚠ §9's convention is 45 explicit + compressed RANGE rows; a folded volume contributes ONE physical row and +N logical seams — do not fire a false STOP on 47-vs-66. (5) EP's rows composed LAST (C3) — check commit order. (6) Wave-end attribution both granularities, archive-vs-archive, ratchet half via the script. (7) The ledger row appended at the true tail. (8) No src behaviour changed — this is a docs-and-one-test-file fold; any \`src/\` diff outside the declared set is a REJECT.

RETURN structured JSON only.`, { label: 'fold:verify', phase: 'Verify', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

let deletion = null
if (verdict && verdict.verdict === 'PASS') {
  phase('Delete')
  deletion = await agent(LAWS + `

YOU ARE THE SNAPSHOT DELETION SLICE. The fold PASSED; the volumes now live on \`claude/composite-r4\`. Remove them from the ledger branch.

⛔⛔ **THE DELETION IS THREE PARTS AND A \`git rm\` ALONE IS SILENTLY UNDONE.** \`scripts/refresh-archive.sh\` copies each snapshot back unconditionally and then \`git add\`s that whole directory, so the next refresh RECREATES the file AND COMMITS it. Do all three:
1. \`git rm\` the three snapshots (EP, HB, WC) — and DECIDE and STATE what happens to \`HABIT_countsweep.py\` and \`HABIT_VERIFY_oddsratio.py\`, which have no stated destination. ⚠ Note the instruments currently ABORT as packaged (exit 1, FileNotFoundError) because the snapshot prefixed them with \`HABIT_\` while the script resolves its sibling by canonical name.
2. Delete each snapshot's \`cp\` line in \`scripts/refresh-archive.sh\`. The file carries a comment block at those lines telling you this.
3. Update the README Contents table rows.

⚠ **ORDER MATTERS AND THE FOLD ALREADY LANDED, SO THIS IS NOW SAFE** — the volumes' only other home was a session scratchpad under \`/private/tmp\`, which a reboot destroys.

⛔ This is on the LEDGER branch, in the MAIN tree — it cannot be committed from minifold; git refuses a branch checked out in another worktree. \`cd ${LEDGER}\` and hard-gate the branch FIRST.

VERIFY AFTERWARDS: run \`sh scripts/refresh-archive.sh\` (WITHOUT \`--commit\`) and confirm it does NOT recreate the deleted files. That is the whole point of part 2, and it is the check that proves the three-part deletion actually held.

Keep the rulings documents (\`WC_CHAIR_RULINGS.md\`, \`EP_CHAIR_RULINGS.md\`, \`HB_ROUND4_CLOSE.md\`, \`EP_ROUND7_CONFIRM.md\`, \`WC_SEAM_TABLE.md\`) — they are the chair record, not snapshots.

RETURN JSON: {commits, filesRemoved, scriptLinesRemoved, readmeUpdated, refreshProof, instrumentDecision, notes}.`, { label: 'fold:delete-snapshots', phase: 'Delete', model: 'opus' })
} else {
  log('FOLD REJECTED — the snapshot deletion is HELD. The volumes must not leave the ledger branch until the fold is accepted.')
}

return { prep, fold, verdict, deletion }