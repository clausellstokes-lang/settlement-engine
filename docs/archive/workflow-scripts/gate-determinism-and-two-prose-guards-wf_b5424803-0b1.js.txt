export const meta = {
  name: 'gate-determinism-and-two-prose-guards',
  description: 'Make step 12 deterministic (timeouts under load, not assertion failures), and convert two prose-only exceptions the verifier found into machine-checked ones.',
  phases: [
    { title: 'Determinism', detail: 'the flake: timeouts under contention' },
    { title: 'Guards', detail: 'two exceptions that are prose where they should be machinery' },
    { title: 'Verify', detail: 'prove stability by repetition, not by one green run' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const LAWS = `VERIFY-FIRST PREAMBLE: chair brief, owner-delegated build program. LIVE CODE OUTRANKS EVERY TABLE. Build tree ${TREE} (branch claude/composite-r4). **Re-read HEAD yourself — the last two briefs pinned a HEAD that was already stale.** Hard-gate every state-mutating compound with the \`cd\` FIRST.

⭐ \`sh scripts/gate-mutex.sh --wait && sh scripts/gate-tail.sh <cmd>\` — ONE \`&&\` chain.
⛔⛔ **NEVER READ AN EXIT CODE THROUGH A PIPE.** Fired five times in one day, twice by the chair, once by a lane that caught itself. Capture the status immediately or use \`gate-tail.sh\`.
⚠ **EVERY FIGURE IN AN INTEGRITY-COUNTED \`git archive\` OF A COMMITTED SHA.** ⚠⚠ **AND NOTE THE METHOD REFINEMENT A VERIFIER JUST PROVED NECESSARY:** \`edgeSharedBundleReproducibility\`, \`committedSecretsScan\` and \`sovereigntyLightingContract\` **shell out to git**, so a bare \`tar\` extraction produces FAKE failures — the archive must be \`git init\`ed and committed, or those three lie.
⚠ Disk ~11 GiB; one archive at a time; \`rm -f\` the node_modules symlink before deleting.
⚠ A sibling lane (hazard registry) may be live. Check \`git status\` yourself; never stage a file you did not author; verify the staged set BY NAME AND COUNT.

REPORTING: CONFIRMED (executed evidence quoted) or PLAUSIBLE.`

phase('Determinism')
const det = await agent(LAWS + `

YOU ARE THE DETERMINISM LANE. **A flaky gate is worse than a red one — it teaches everyone to re-run until green, and it silently invalidates every census taken through it.**

**THE FINDING, CONFIRMED BY A VERIFIER FROM THE RUNNER'S OWN JSON REPORT.** Three full-suite runs at the SAME sha in the SAME archive produced **0, 2 and 5** out-of-census failures. They are **TIMEOUTS, NOT ASSERTION FAILURES**: durations 22,137 / 23,120 / 21,173 / 23,630 / 26,069 ms against \`testTimeout: 20000\` (\`vite.config.js:801\`), each reporting \`Error: STACK_TRACE_ERROR\` with **no assertion diff**. Run standalone on an idle machine the same five cost **6,602 ms** — a 3-4x inflation purely from contention. The worst offender is the MUTANTS block of \`tests/lint/observedShapeReaders.walker.test.js\`, which the chair already had to fix once for this same reason.

**DIAGNOSE BEFORE YOU TREAT.** Measure which tests sit closest to the ceiling under load and by how much — the fix differs by cause:
 - if a test does genuinely heavy work (real generator runs, full-tree AST scans), **cut its per-run cost** — cache the corpus across cases in the file, or shrink the scan — rather than buying headroom;
 - if it is cheap alone and only slow under contention, **raise its own timeout explicitly** with the measured figure and the reason in a comment;
 - if a whole family is affected, consider whether the family belongs in a pool with different concurrency.
⛔ **DO NOT simply raise the global \`testTimeout\`.** That hides the next one and makes a genuinely hung test take longer to report. Per-test or per-file, with the measurement written beside it.

**THE ACCEPTANCE TEST IS REPETITION, NOT A GREEN RUN.** One green proves nothing about a flake. **Run the affected set N≥5 times under REALISTIC CONTENTION** (not an idle machine — that is the condition that hides it) and report every run's result and duration. Report the worst-case margin against the ceiling as a ratio, so the next lane can see the headroom rather than guess it.

⚠ **THEN CHECK WHETHER THE CENSUS ITSELF IS POISONED.** \`scripts/.test-ratchet-baseline.json\` froze 49 failures. **If any of those 49 was frozen because of a timeout rather than a real defect, it is a phantom entry that will never be burned down and will make the ratchet look permanently indebted.** Identify any such entries and say so; removing one is a legitimate ratchet-down with a stated cause.

RETURN JSON.`, { label: 'flake:determinism', phase: 'Determinism', model: 'opus', effort: 'high', schema: { type: 'object', required: ['landed'], properties: { landed: { type: 'boolean' }, diagnosis: { type: 'string' }, treatmentByTest: { type: 'array', items: { type: 'string' } }, globalTimeoutRaised: { type: 'boolean' }, repetitionRuns: { type: 'number' }, repetitionResults: { type: 'string' }, worstCaseMarginRatio: { type: 'string' }, phantomCensusEntries: { type: 'array', items: { type: 'string' } }, commits: { type: 'array', items: { type: 'string' } }, stopReports: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Guards')
const guards = await agent(LAWS + `

YOU ARE THE GUARD-CONVERSION LANE. A verifier found two exceptions that are **prose where they should be machinery** — which is exactly the class the owner directed us to close this session.

Determinism lane landed: ` + JSON.stringify(det || {}).slice(0, 2500) + `

**G-1 — THE "COUNT-NEUTRAL RETARGET" EXCEPTION IS PROSE ONLY.** The layering repair retargeted a \`couplingInclusion\` baseline entry (\`peaceTermsPrimitives.js\` from \`warReasons.js\` to \`warReasonTaxonomy.js\`) rather than minting a licence row — and that ruling was **correct**, independently confirmed: same importer, same direction, same single read symbol (\`reasonPairKey\`), the two-line template merely moved DOWN into a zero-import leaf to cut a 39-module cycle.
**But the exception permitting it lives only in a comment.** Nothing machine-checks that a retargeted entry keeps the same importer, direction and read symbol, and nothing requires the \`note\` field. **A future lane can repoint any baseline entry at anything and the walker will not notice.**
**CURE:** make the retarget checkable — require a \`note\`/provenance field on any entry whose target changed, and assert the invariants that made this one honest. **Prove it by mutant: repoint an entry at an unrelated module and confirm it REDS.**

**G-2 — \`ARGUED_UNLAYERED\` MAKES EDGES INVISIBLE RATHER THAN UNCLASSIFIED, AND A PRECEDENT WAS JUST SET.** \`src/domain/spatial/spatialLedgerAccess.js\` was admitted as \`ARGUED_UNLAYERED\`. Because \`LIVE_UNLAYERED\` filters on \`ARGUED_UNLAYERED\` **before** the pair scan, **every edge into that module becomes invisible to the cross-layer scan entirely** — not merely unclassified.
The ruling itself is defensible (the file is a genuine zero-import namespace constant leaf). **The mechanism is not**, because it means each future admission silently subtracts coverage.
**Decide and record, with evidence:** either (a) narrow the filter so an argued-unlayered module is exempt from *classification* but still *scanned* for edges, or (b) if that is genuinely wrong, add an explicit assertion that the argued set is small, shrink-only, and each member is a proven zero-import leaf. **Say which you chose and why, and prove the guard reds when a non-leaf is admitted.**

⚠ **AND A DISCLOSED GROWTH TO RECONCILE:** \`scripts/.observed-shape-readers-baseline.json\` GREW (total 3236 → 3261, identities 2149 → 2168, inventory 547 → 551). The growth was disclosed honestly with a stated cause — the resolver grounds member reads by a name prior over the LIVE module set. **Verify that cause is real and that the growth is a resolver-scope consequence rather than new findings being laundered in.** If any of the 25 is a genuine new reader-without-a-writer, it is a defect, not baseline.

RETURN JSON.`, { label: 'flake:guards', phase: 'Guards', model: 'opus', effort: 'high', schema: { type: 'object', required: ['landed'], properties: { landed: { type: 'boolean' }, g1Cure: { type: 'string' }, g1MutantReds: { type: 'string' }, g2Choice: { type: 'string' }, g2MutantReds: { type: 'string' }, baselineGrowthVerdict: { type: 'string' }, launderedFindings: { type: 'array', items: { type: 'string' } }, commits: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Verify')
const verdict = await agent(LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER. Default posture: WRONG until executed evidence. REFUTE. **READ-ONLY in the shared tree**; mutants in throwaway archives. You hold the gate slot alone.

Determinism: ` + JSON.stringify(det || {}).slice(0, 5000) + `
Guards: ` + JSON.stringify(guards || {}).slice(0, 5000) + `

**THE CENTRAL QUESTION: is the gate actually deterministic now, or merely green once?**
1. **RUN THE AFFECTED SET AT LEAST FIVE TIMES YOURSELF, UNDER CONTENTION** — an idle machine is the condition that HIDES this defect, so do not prove stability on one. Report every run's pass/fail and duration, and the worst-case margin against each ceiling.
2. **Confirm the global \`testTimeout\` was NOT simply raised** — that is the cure that hides the next flake. Check \`vite.config.js\`.
3. **Confirm no test was made to pass by doing less real work.** A cheaper test that no longer exercises what it claims is a silenced test wearing a fix's clothes — read the diffs.
4. **Mutate both new guards yourself**: repoint a coupling baseline entry at an unrelated module; admit a NON-leaf module to the argued-unlayered set. Both must RED.
5. **Audit the 25-row observed-shape growth independently** — sample several and determine whether each is a resolver-scope artifact or a real reader-without-a-writer being laundered into baseline.
6. **Run \`npm run check\` stepwise**, status captured immediately, and report the first failing step by name. ⚠ Expect step 14 \`verify:dist\` to remain red on three INHERITED budget failures (render-blocking CSS 19,813 B vs 19,800; first-paint static closure 1,168,130 vs 1,040,000; gzip 367,507 vs 337,000) — **confirm they are unchanged or improved, and REJECT if this round made any of them worse.**

RETURN structured JSON only.`, { label: 'flake:verify', phase: 'Verify', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, repetitionRuns: { type: 'number' }, allRunsIdentical: { type: 'boolean' }, globalTimeoutUntouched: { type: 'boolean' }, guardMutantsRed: { type: 'string' }, firstPaintUnchangedOrBetter: { type: 'boolean' }, checkFirstFailingStep: { type: 'string' }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

return { det, guards, verdict }