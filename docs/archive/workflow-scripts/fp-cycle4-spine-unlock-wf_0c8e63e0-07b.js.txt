export const meta = {
  name: 'fp-cycle4-spine-unlock',
  description: 'Cycle 4: the sign-off row + owed doc edits, then SP-C (posture), SP-D (errand spine), GR-2 (pact formation), IN-0a (the plant handoff) — strict serial, each wave through its own adversarial verifier',
  phases: [
    { title: 'Row', detail: 'the blanket sign-off ledger row + the two owed ES-4 doc clauses + the stale manifest rationale' },
    { title: 'SP-C', detail: 'the posture read: appetite stock + the CR-C4-1 court-read spellings' },
    { title: 'Verify C', detail: 'adversarial verification, golden audit, dormancy' },
    { title: 'SP-D', detail: 'the errand spine generalization: errandMint leaf + consumer tripwire' },
    { title: 'Verify D', detail: 'adversarial verification + the ES-0 gate door 2 re-read' },
    { title: 'GR-2', detail: 'peacetime formation + the standalone NAP, spine reads verified at build' },
    { title: 'Verify G', detail: 'adversarial verification, sale-coexistence, stage order' },
    { title: 'IN-0a', detail: 'the plant-envelope handoff to the fold, riding existing flags' },
    { title: 'Verify I', detail: 'adversarial verification, three-stage writer/reader, regen clause' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const LAWS = `VERIFY-FIRST PREAMBLE: this brief is from the session chair of an owner-delegated build program; verify claims against the tree before acting. Work in ${TREE} (branch claude/composite-r4, expected HEAD at entry per your predecessor lane — CHECK-GIT-FIRST always). THE PROTOCOL LAWS (each has bitten): CHECK-GIT-FIRST; ps aux | grep vitest before any test run (ONE vitest lane per tree; note a compound command spelling vitest self-matches — re-check standalone before believing a positive); NEVER stash/reset/clean/checkout-family (cp backups, cmp-proven restores; damaged tracked files restore via git show HEAD:path > path); gates only via sh scripts/gate-tail.sh or npm run check:tail; commits with EXPLICIT pathspecs, message via file, trailer Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>; --no-verify plus explicit per-file npx eslint (exit 0) when lint-staged blocks; python3 byte-scan every authored file (zero NULs); new tests/lint walkers need mutationCoverageManifest entries; strict ceiling 1313 exactly; tests/fixtures/ and pulseKernel.js/applyWorldPulse.js UNTOUCHABLE. Navigate by SYMBOL. LIVE CODE OUTRANKS EVERY TABLE — an overstatement is a STOP-and-report. Every quoted receipt must be able to fail. The four-fence dormancy pattern + lit-mutant per flag is LAW, WITH the defense-in-depth corollary: enumerate every guard door and pin each individually. Wave-end attribution: whole tests/lint SEQUENTIALLY at HEAD vs a git-archive of your start HEAD (node_modules symlinked, symlink removed by rm -f on the LINK before tree removal), row-identity diff EMPTY both directions except attributed, PLUS the red-ratchet CONTENT diff. NEW STANDING HAZARD (bit this week, reproduced): never spell vitest list --json PATH with a space — the bare form consumes the NEXT POSITIONAL as the output path and OVERWRITES that file silently, exit 0; always --json=PATH (equals form) to a path OUTSIDE the repo, and run git status --porcelain immediately after any vitest-CLI flag invocation. CQ5 FLAG LAW: a manifesting flag lands manifest + authored cert row + first real gate read in ONE commit; flag waves are SERIALIZED in this tree — never start a second flag wave with the first uncommitted.`

phase('Row')
const row = await agent(LAWS + `

YOU ARE THE ROW SLICE — three small owed items, docs and ledger only, ZERO src edits. (1) THE BLANKET SIGN-OFF LEDGER ROW (task #53): read the row format at the tail of docs/FABLE_VALIDATION_QUEUE.md, then append a row recording the owner grant of 2026-08-05, verbatim: "I give permission for everything that requires my sign off in the full queue" — scope: every queued owner-gated item is SIGNED per the chair's recorded recommendation for that item (released-to-build items build at their queue positions; recommended-defer items are RATIFIED as deferred); FOUR carve-outs retained: founder-transfer LEGAL sign-off, the V5 aesthetic cull (offered, not assumed), THE TUNING SIGNATURE (constitutional — the versioned release document is still presented at soak for specific confirmation), and PUSHES (confirm each, per the 2026-08-02 boundary). Cite the memory file owner-blanket-queue-signoff.md as the canonical record. (2) THE TWO OWED DOC CLAUSES from the ES-4 door-3 arc close (verified live at these addresses): docs/DESIGN_FP_ARCH_ES.md:1467 and docs/DESIGN_FP_ARCH_SP.md:515 and :771 currently tell the ES-4 builder only to put the marker in the test NAME — under the tenth cut cap (commit d48224e3) that is no longer sufficient. Add, at each site, a POINTER (point-dont-restate — the first-match document-pin hazard): the ES-4-DISTANT-SOURCE-EVIDENCE pin must live in tests/domain/espionageDistantSourceEs4.test.js (the declared evidence address) as an it/test TITLE, never a describe title — full recipe = clauses (0) and (0b) in the walker header of tests/lint/sovereigntyLightingContract.walker.test.js. Use exactly-once anchors when splicing (assert count 1). These two docs are read by three other walkers — after editing, run the tests/lint walkers that reference them and quote green. (3) THE STALE MANIFEST RATIONALE: scripts/mutation-coverage-manifest.json rationale sovereignty-lighting-condition-executed-2026-08-05 records planting the ES-4 marker in any live test file returning SATISFIED — under the cap the plant must be at a declared address, so a successor re-running it verbatim would misread the instrument as broken. Amend the rationale TEXT to add one sentence noting the point-in-time nature and the cap (d48224e3, declared addresses only). CRITICAL: splice the RAW json text and re-parse to check — json.dumps reformats 1,951 lines (recorded hazard). Then run scripts/mutation-coverage-manifest tests (the mutationCoverageManifest.test.js) and the sovereignty walker, quote green. Commit with explicit pathspecs (one commit is fine; docs + manifest + ledger row). RETURN JSON: commit sha, files, gates quoted, notes.`, { label: 'row:sign-off+docs', phase: 'Row', model: 'opus' })

phase('SP-C')
const spc = await agent(LAWS + `

YOU ARE THE SP-C IMPLEMENTER — THE POSTURE READ, flag strategicPostureEnabled. THE NORMATIVE SPEC is docs/DESIGN_FP_ARCH_SP.md, the SP-C wave block (near :523) — read it whole, plus section 4, before any edit. Prior row-slice commit may sit above your expected HEAD — CHECK-GIT-FIRST. THE CHAIR RULING CR-C4-1 (vetoable, supersedes the volume's naming): the court reads are spelled courtPostureOf / courtRiskAppetiteOf per docs/DESIGN_FP_ARCH_GR.md section 7 Q2, which MEASURED the live collision — riskToleranceOf is TAKEN by an NPC-level read at roads/state.js:319, and this estate has been bitten by the silent-collision class. Land the GR-recommended spellings, update the SP volume's SP-C block naming in the SAME commit (docs may be aligned to a ruling; note the ruling id in the doc edit), document the collision at the new module header, and land a grep pin asserting riskToleranceOf in src/domain resolves ONLY to the roads NPC read. THE CHARTER: (1) extend dispositionLedger.js (560 lines, headroom ~240) with the appetite facet per section 4 — learned from OUTCOME EVENTS only (war won/lost, treaty held/broke, trade enriched, venture failed — the ledger's existing event diet), decayed toward neutral on bandedStock.js's shared shape (the first in-tree SP-5b instantiation, deliberate); NEVER author a new decay law or a new significance/severity scale — bandFamilies.js owns them (standing law). THE REVERSAL PIN is mandatory (no courage ratchets). (2) new leaf strategicPosture.js (budget 300): courtPostureOf(settlementId) composing state x disposition channels x appetite x the ruler's books — books term ABSENT (not zero) until seatBooksEnabled exists; channels term ABSENT while dispositionChannelsEnabled is dark; the posture receipt NAMES which inputs it had; courtRiskAppetiteOf(actorRef) for the closed actor set with a tripwire pin that REDS an unknown actor class rather than guessing. Threshold-shaped reads ONLY: the import list is pinned (the P4 no-hidden-governor pattern — no relationship graph, no target lists; the module CANNOT name a victim), with an executed violation mutant. (3) files: dispositionLedger.js, simulationRules.js (one row), the new leaf, certification row — dispositionProfile.js is READ, not edited; dispositionLedger.js is dark-war-lane substrate (WR-2's file) — re-read at HEAD before editing. (4) the CQ5 trio in ONE commit. (5) pins per the block: four-fence + lit-mutant with every guard door pinned individually; the reversal pin; the conditional-field dormancy pin (lit ledger, dark flag = ledger bytes identical across a 10-tick pulse, hash-compared); the E3 import pin; the two-realms divergence property (same config, different outcome histories = different postures); the band-word receipt pin (no float in any posture receipt); the golden pair (posture with and without each degraded term). (6) dormancy: flag dark = no appetite facet ever written = every golden byte-identical (own-footprint golden captured BEFORE wiring). Alignment line and edit-verb decision land as the block records them. Wave-end attribution per the LAWS. RETURN JSON: commits, gates, goldenShifts (declared with causes — empty if none), stopReports, notes.`, { label: 'build:SP-C', phase: 'SP-C', model: 'opus' })

phase('Verify C')
const vspc = await agent(LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER FOR SP-C. Default posture: wrong until executed evidence. The implementer reported: ` + JSON.stringify(spc).slice(0, 12000) + `

VERIFY BY EXECUTION in git-archive trees (live tree read-only for you except where a mutant protocol says otherwise — mutants ONLY in disposable archive trees): (1) CR-C4-1 — grep proves courtPostureOf/courtRiskAppetiteOf are the ONLY new spellings, riskToleranceOf in src/domain still resolves only to roads/state.js NPC read, the grep pin reds when you plant a colliding spelling (executed in an archive tree); (2) the appetite facet learns ONLY from the ledger's existing event diet — no new event taps (diff the ledger's intake surface); the decay rides bandedStock's shape — no second decay law authored (grep for new half-life/decay constants outside bandFamilies); the REVERSAL pin reds on a courage-ratchet mutant; (3) the import pin — plant a forbidden import (a relationship-graph read) and the pin reds; (4) dormancy — run the four-fence set, the lit-mutant, AND the conditional-field hash comparison yourself (lit ledger, dark flag, 10-tick pulse, byte-identical); every guard door deleted individually reds its own pin; (5) the two-realms divergence property is NON-VACUOUS (seeded histories actually differ — the empty-harness absence-pin class); (6) golden audit: if shifts declared, verify cause against diff; if none declared, run the war-lane goldens and confirm byte-stability (dispositionLedger is WR-2 substrate); (7) CQ5 trio co-commitment via git log -S; delete-the-cert-row mutant reds; (8) wave-end attribution both granularities including the red-ratchet CONTENT diff; strict 1313; byte-scans; commit hygiene; tree clean. RETURN structured JSON only.`, { label: 'verify:SP-C', phase: 'Verify C', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

let spd = null, vspd = null, gr2 = null, vgr2 = null, in0a = null, vin0a = null

if (vspc && vspc.verdict === 'PASS') {
  phase('SP-D')
  spd = await agent(LAWS + `

YOU ARE THE SP-D IMPLEMENTER — THE ERRAND SPINE GENERALIZATION, flag errandSpineEnabled. THE NORMATIVE SPEC is docs/DESIGN_FP_ARCH_SP.md, the SP-D wave block (near :573) — read it whole plus section 1 (SP-1) before any edit. The envoy file family is the war lane's most-recently-edited surface — CHECK-GIT-FIRST is mandatory, re-read every target at HEAD. THE CHARTER: (1) extend envoyErrandVocabulary.js (315 lines): the six purpose CLASSES as a closed vocabulary beside the existing purposes; the mapping row (sue/self_parlay to diplomatic) as DATA, not inference; (2) envoyErrand.js (823 lines — AT the effective ceiling): gains ONLY a delegation call; the generalized mint head lands in a NEW leaf errandMint.js (budget 250) — the L8 recipe; the mint accepts purposeClass + the declared/true split, gated errandSpineEnabled === true; dark, only the war path exists, byte-identical; (3) the declared/true split rides the EXISTING covert/revealed seam: truePurpose is covert-side, fail-closed in envoyErrandProjection.js's audience split; interception exposure routes the EXISTING covert-to-revealed machinery, never a new one; (4) per-errand keyed forks, the K.2 snapshot, interceptability, and the M speed law apply to every class BY CONSTRUCTION (same writer, same transit kernel) — extend namedPersonTransitTotality.walker.test.js to the new mint leaf; (5) THE CONSUMER REGISTRATION TRIPWIRE: a frozen consumer map (GRAMMAR envoys today; factors/legates/pilgrims/couriers/ambitious as their volumes land) with a BOTH-WAYS walker — a volume minting errands without a registry row reds; a registry row with no minting consumer reds; (6) files: envoyErrandVocabulary.js, envoyErrand.js (delegation only), new errandMint.js, envoyErrandProjection.js (audience rows), simulationRules.js, certification row — the CQ5 trio in ONE commit; (7) pins: four-fence + lit-mutant, every guard door individually; JSON-round-trip on the new fields (the alias trap); the fail-closed truePurpose projection pin proven with a SEEDED covert errand, never an empty harness; the legacy-row derivation pin (absent purposeClass reads diplomatic; a legacy save round-trips byte-identically); DM-KILL-closes-lost re-proven on a generalized errand; (8) dormancy: flag dark = mint head unreachable = envoyErrands bytes identical through the live pulse, hash-compared. KNOWN DOWNSTREAM SEAM, verify after landing: the espionage gate (ES-0, src/domain/worldPulse/espionage/espionageGate.js) spells its door 2 as errandSpineEnabled !== true DELIBERATELY — after your manifest row lands, that door reads a REAL key; execute the gate both flag states and confirm the espionage family stays parked while errandSpineEnabled is dark and nothing new lights. Edit-verb decision (no recall verb, recorded) and alignment line per the block. Wave-end attribution per the LAWS. RETURN JSON: commits, gates, goldenShifts, stopReports, notes.`, { label: 'build:SP-D', phase: 'SP-D', model: 'opus' })

  phase('Verify D')
  vspd = await agent(LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER FOR SP-D. Default posture: wrong until executed evidence. The implementer reported: ` + JSON.stringify(spd).slice(0, 12000) + `

VERIFY BY EXECUTION in git-archive trees: (1) envoyErrand.js gained ONLY delegation (diff its numstat; the mint logic lives in errandMint.js under budget); the war path is byte-identical dark — run the dormancy hash yourself through the live pulse; (2) the truePurpose fail-closed pin is NON-VACUOUS: seed a covert errand, render every non-covert projection, truePurpose absent everywhere; then MUTANT: leak truePurpose into a public projection in an archive tree — the pin must red (the public-payload veil seam class); (3) the tripwire walker reds BOTH WAYS (delete a registry row; add a phantom row) — executed; (4) the legacy derivation: build a pre-SP-D errand fixture via JSON round-trip of a real record, absent purposeClass reads diplomatic, bytes identical; (5) THE ES-0 SEAM: execute espionageGate door 2 with the key now real — espionage stays parked dark, and the gate's own pins still pass; quote the executed reads; (6) the transit totality walker extension really covers the new leaf (delete the leaf's registration — walker reds); (7) CQ5 co-commitment, four-fence, guard doors individually, lit-mutant; (8) wave-end attribution both granularities + red-ratchet CONTENT diff; strict 1313; byte-scans; hygiene. RETURN structured JSON only.`, { label: 'verify:SP-D', phase: 'Verify D', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })
} else {
  log('SP-C verifier rejected — halting the chain for the chair; SP-D, GR-2, IN-0a not started.')
}

if (vspd && vspd.verdict === 'PASS') {
  phase('GR-2')
  gr2 = await agent(LAWS + `

YOU ARE THE GR-2 IMPLEMENTER — PEACETIME FORMATION + THE STANDALONE NAP, flag pactFormationEnabled. THE NORMATIVE SPEC is docs/DESIGN_FP_ARCH_GR.md, the GR-2 block (near :431) plus the GRAMMAR volume sections it cites — read whole before any edit. GR-2 was BLOCKED on spine SP-2/SP-4: SP-2 substrate landed in SP-B (belief-axis families), SP-4 landed as SP-C THIS CYCLE — VERIFY-AT-BUILD their landed shapes per R-5 before writing a single consumer read: the court reads are spelled courtPostureOf / courtRiskAppetiteOf (chair ruling CR-C4-1 — NEVER riskToleranceOf, which is the roads NPC read). THE CHARTER, per the compiled deltas: (1) MOUNT: advancePeacetimePacts as a stage in settlementLifecycleKernel.js beside advanceSovereigntyMarket (near :90), own-flag-before-host-gate; NEVER a pulseKernel edit; stage ORDER fixed and pinned — market first, pacts second (recorded JUDGMENT, vetoable: a court that just sold is a court whose believed books changed, and the pact trigger should read the post-sale world); (2) MINT/AMEND: pactFormation.js as a writer-FAMILY member on the peaceTermsSale.js pattern — composes TERM_CATALOG/primitives/clock directly, writes through treatyLedgerOf/setSpatialLedger, NOT re-exported by peaceTerms.js, caller outside the head's import cycle; pactAmendment.js (family member) carries the AMENDMENT act (standing instrument = lineage append); the war door's amendment awareness is the ONE peaceTerms.js edit this wave makes, delta stated in the commit body; (3) new modules + budgets: pactTriggers.js (pure, 220 eff), pactProposals.js (one writer, 250 eff), pactFormation.js (200 eff), pactAmendment.js (180 eff); (4) TRANSPORT: errand-spine dark = answerDueTick from the hopWeeks leg read (dwell bands stated against the MEASURED 2..8-week digest spectrum — hopWeeks is per-digest, the calibrated-weeks hazard); spine lit = the SP-1/WR-7 envoy, purpose diplomatic, transport-mode on every receipt; (5) PINS: the volume's full set (refusal / no-overlap / dependency-fear / posture / spam / four-trigger reachability / NAP symmetry / stacking-refusal / two-sided compliance / serial-disavower / DM parity / war-overtaken / transport-mode / dormancy) plus the compiled additions: the K3 pin set gains the two new modules with guard-the-guard against a truth-reader OUTSIDE the set; the writer/reader payload pin boots the REAL pactProposals.js writer; the SALE-COEXISTENCE pin — a pair holding a live sale instrument receives a pact proposal and acceptance AMENDS that instrument (one record at the pair key) while the sale door's refusal behavior is asserted UNCHANGED; (6) MUTANTS: the two-sided conjunction (drop one side = no-overlap pin reds); the cap; the stacking-refusal receipt; (7) dormancy: dark = the kernel stage body never entered = byte-identical (the sovereigntyMarketStage fence pattern verbatim); (8) edit verb PROPOSE_PACT with the FULL registration set the volume specifies (manifest row + dials + typed veto codes + authority row + decisionTier + handler + parity pin) — AND the standing store-action law: if this lands a new store action, operationRegistry + npm run gen:compendium-data are REQUIRED in the same wave (recorded hazard — a missing regen bites silently); (9) collision map: allianceWebRiskConsumers.walker — GR-2 is the THIRD consumer of readAllianceWebRisk; argue the census update IN-COMMIT; CQ5 trio one commit. Wave-end attribution per the LAWS. RETURN JSON: commits, gates, goldenShifts, stopReports, notes.`, { label: 'build:GR-2', phase: 'GR-2', model: 'opus' })

  phase('Verify G')
  vgr2 = await agent(LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER FOR GR-2. Default posture: wrong until executed evidence. The implementer reported: ` + JSON.stringify(gr2).slice(0, 12000) + `

VERIFY BY EXECUTION in git-archive trees: (1) the mount is a KERNEL STAGE, not a pulseKernel edit — git log confirms pulseKernel.js/applyWorldPulse.js untouched (numstat across the wave commits); the stage order pin reds when you swap the stages in an archive tree; (2) the writer family holds: peaceTerms.js diff is exactly the ONE stated edit; pactFormation.js is not re-exported by peaceTerms.js (grep the export surface); the K3 guard-the-guard reds on a truth-reader planted OUTSIDE the set; (3) SALE-COEXISTENCE executed: seed a pair with a live sale instrument, drive a pact proposal to acceptance, ONE record at the pair key (amendment lineage, not a second instrument), sale-door refusal behavior unchanged against the measured baseline; (4) spine reads: grep proves GR-2 consumes courtPostureOf/courtRiskAppetiteOf and never riskToleranceOf; drive a posture-priced proposal over two divergent-posture fixtures and the pricing MOVES (non-vacuous); (5) transport both arms: dark arm's answerDueTick derives from the real hopWeeks leg (2..8 weeks spectrum, not a constant); (6) the two-sided conjunction mutant, the cap, the stacking-refusal receipt — each executed and restored cmp-clean; (7) dormancy byte-identity via the fence pattern; four-fence + guard doors individually + lit-mutant; (8) if PROPOSE_PACT landed as a store action: operationRegistry row exists AND gen:compendium-data was run (diff the generated data — stale regen is the recorded silent bite); DM parity pin executed; (9) CQ5 co-commitment; wave-end attribution both granularities + red-ratchet CONTENT diff; strict 1313; byte-scans; hygiene. RETURN structured JSON only.`, { label: 'verify:GR-2', phase: 'Verify G', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })
} else if (vspd) {
  log('SP-D verifier rejected — halting the chain for the chair; GR-2 and IN-0a not started.')
}

if (vgr2 && vgr2.verdict === 'PASS') {
  phase('IN-0a')
  in0a = await agent(LAWS + `

YOU ARE THE IN-0a IMPLEMENTER — THE HANDOFF: the applied brokerage_plant envelope carried to the fold. RIDES EXISTING FLAGS (informationBrokeragesEnabled x the statecraft flag) — NO new flag, NO cert-row mint; this is the re-scoped R1 slice. THE NORMATIVE SPEC is docs/DESIGN_FP_ARCH_IN.md, the IN-0a block (near :258), plus the volume sections it cites in DESIGN_FP_INFORMATION.md — read whole first. THE CHARTER: (1) files: brokerageServicesRules.js (383), candidateEvents.js (786 — NEAR the 800 layer ceiling: any addition beyond ~10 lines needs a leaf), a NEW pure leaf for envelope collection (80-line budget), informationStatecraft.js (head gains only the consume read), envoyPulse.js (376 — WAR-owned: the head-read gated on informationBrokeragesEnabled; keep the diff minimal and attributable), a projection mount on the settlement dossier surface; (2) MODEL: the applied plant's envelope reaches BOTH consumers WITHOUT editing the banked pulse mouths (pulseKernel 1580 / applyWorldPulse 941 — ZERO edits EVER). Primary road: each consumer READS the prior tick's applied brokerage_plant events at its OWN head off the existing applied-event/news record (one-week lag = law M). VERIFY-AT-BUILD: applied-event metadata retention through the candidate apply lane — MEASURE it before building; if retention fails, the Q1 fallback (pendingPlants conditional deposit under the D-3 contract); if BOTH fail: STOP-AND-REPORT — a kernel seam is the chair's to grant, never yours; (3) RECEIPTS: plant_took (DM-truth-only projection line) when the mark's belief crosses to the planted band; the exposure beat extends to NAME THE MARKET (and the commission where lineage collapsed); registration COMPLETE in this commit: WHAT_PHRASES (settlementRumors.js:116) + heraldRouting + pacing/significance class (routine) + the knowledge family's own-vocabulary token; (4) DOSSIER: mount projectPlants (first non-test consumer) on the town page, DM/premium, audience passed EXPLICITLY; source scan catches any call site omitting it; player projection pinned EMPTY for live plants; (5) PINS (negative hardest): the corroborated mark's plant DIES at the fold (reconcile's contradiction arm — SEEDED non-empty); the SPENT MARKET banded margin pin exactly as the volume's corrected block (WEIGHT_FLOOR 0.35 is a discount, not silence; the fresh-plant-beats-stale-truth sibling pinned too); the WRITER/READER pin boots the REAL rotation producer through the REAL apply lane through the REAL fold — three stages, no shortcut fixture (the fixture-mirrors-deriver hazard); reachability: one plant TAKES in a generated lit corpus; the handoff mutant: sever the envelope collection and the fold pin REDS (manifest entry); dormancy golden — both flags dark = byte-identical, INCLUDING the ONE_REGEN preset; (6) LIFECYCLE: fold adds no shape change; JSON-round-trip on a folded record; regen preserves live plants (the section-3 clause) — execute the regen path, never assume it. Spine 13/14 as the block records. Wave-end attribution per the LAWS. RETURN JSON: commits, gates, retentionMeasurement (which road the envelope took, with the executed proof), stopReports, notes.`, { label: 'build:IN-0a', phase: 'IN-0a', model: 'opus' })

  phase('Verify I')
  vin0a = await agent(LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER FOR IN-0a. Default posture: wrong until executed evidence. The implementer reported: ` + JSON.stringify(in0a).slice(0, 12000) + `

VERIFY BY EXECUTION in git-archive trees: (1) the pulse mouths: git numstat across the wave proves pulseKernel.js and applyWorldPulse.js untouched; the envelope road is the one the implementer claims — re-run their retention measurement yourself; (2) THE THREE-STAGE WRITER/READER PIN IS REAL: read its fixture construction — it must boot the real rotation producer, the real apply lane, the real fold; a pin that mirrors the deriver is a REJECT (the recorded fixture-mirrors-deriver class); sever the envelope collection in an archive tree — the fold pin reds; (3) the corroborated-mark death arm is SEEDED non-empty (print the seeded state size); the spent-market margin pin asserts the band, not silence; (4) dormancy: both flags dark, byte-identical INCLUDING the ONE_REGEN preset — run it; regen preserves live plants — EXECUTE the regen path with a live plant and diff; (5) the dossier mount: audience explicit at every call site (source scan), player projection EMPTY for live plants (seeded, non-vacuous); (6) registration complete: WHAT_PHRASES + heraldRouting + pacing + vocabulary token all in the commit (git log -S each); if any corpus file changed, gen:dossier-prose was re-run (diff the generated output for staleness); (7) one-week lag: the consumer reads PRIOR-tick events — drive two consecutive ticks and quote the lag; (8) wave-end attribution both granularities + red-ratchet CONTENT diff; strict 1313; byte-scans; hygiene; tree clean at exit. RETURN structured JSON only.`, { label: 'verify:IN-0a', phase: 'Verify I', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })
} else if (vgr2) {
  log('GR-2 verifier rejected — halting the chain for the chair; IN-0a not started.')
}

return { row, spc, vspc, spd, vspd, gr2, vgr2, in0a, vin0a }