export const meta = {
  name: 'typecheck-red-burndown-cycle7',
  description: 'Burn down the invisible typecheck red: 351 errors / 45 files accumulated over ~356 commits since 7796954e. Census, then the top-carrying files, then adversarial verify.',
  phases: [
    { title: 'Census', detail: 'the error taxonomy at a committed sha, and the real-defect triage' },
    { title: 'Burn', detail: 'the top-carrying files, one lane each' },
    { title: 'Verify', detail: 'adversarial: did the count actually drop, and did anything move' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const LAWS = `VERIFY-FIRST PREAMBLE: chair brief, owner-delegated build program. LIVE CODE OUTRANKS EVERY TABLE. Build tree ${TREE} (branch claude/composite-r4). Hard-gate every state-mutating compound with the \`cd\` FIRST: \`cd ${TREE} && [ "$(git branch --show-current)" = "claude/composite-r4" ] || exit 1\`.

⭐ **THE MUTEX WAIT AND THE RUN ARE ONE \`&&\` CHAIN, NEVER TWO STATEMENTS** — \`sh scripts/gate-mutex.sh --wait && sh scripts/gate-tail.sh npx vitest run <paths>\`. Issued separately, a wait TIMEOUT lets vitest start anyway, which is the fake-red the mutex exists to prevent. ⛔ Never hand-spell the ps-grep. ⛔ **NEVER READ AN EXIT CODE THROUGH A PIPE** — \`cmd | tail\` gives you \`tail\`'s status; that is why \`scripts/gate-tail.sh\` exists.
⚠ **EVERY CENSUS FIGURE IS TAKEN IN AN ARCHIVE OF A COMMITTED SHA, NEVER THE WORKING TREE.** Six recorded instances of this shared tree measuring another lane's work. Build archives with \`git archive -o f.tar <sha>\` + \`tar -xf\` (NEVER through a pipe), INTEGRITY-COUNT each (\`git ls-tree -r <sha> | wc -l\` vs \`find -type f | wc -l\`), do them ONE AT A TIME, and \`rm -f\` the node_modules SYMLINK before removing a tree. Disk is ~11 GiB free and hit 100% yesterday, which silently truncated a log to zero bytes.
⚠ **A SECOND BUILD LANE (the EP/HB/WC fold) runs concurrently.** Its surface is \`docs/*.md\`, \`docs/SOL_QUEUE.md\` and \`tests/domain/couplingRegistry.test.js\`. **DO NOT TOUCH ANY OF THOSE.** Never stage or clean a file you did not author. EXPLICIT PATHSPECS; verify \`git diff --cached --name-only\` against your EXPECTED set BY NAME AND COUNT before every commit — a \`git add\` aborts ATOMICALLY on one bad pathspec, so a commit can claim three parts and land one.

REPORTING: CONFIRMED (executed evidence quoted) or PLAUSIBLE. Never present the second as the first.`

const HAZARDS = `
STANDING HAZARDS THAT APPLY DIRECTLY TO THIS WORK:
- ⚠⚠ \`sizeBaseline\` ceilings are EXACT, tolerance ZERO — **adding JSDoc lines to a file at its ceiling REDS THE RATCHET**, and comments count. Several hot files sit at exactly their cap (e.g. generosityKernel at 800/800, OutputContainer at 600/600). Check the baseline for every file BEFORE adding annotation lines; if a file is at cap, the cure is a LAZY LEAF extraction, not a smaller comment.
- ⚠⚠ \`src/domain\` store imports trip FOUR ratchets at once.
- ⚠ THE TYPE SURFACE IS PUBLIC — widening or narrowing an exported type has ratchet consequences beyond the file.
- ⚠ Golden shifts are recorded ONLY from a proven-clean HEAD, and a behaviour-moving fix must be DECLARED, never allowed to ride silently.
`

phase('Census')
const census = await agent(LAWS + HAZARDS + `

YOU ARE THE TYPECHECK CENSUS SLICE. Do NOT fix anything. Measure, triage, and STOP.

**THE SITUATION.** This repo's typecheck gate went red at commit \`7796954e\` and has been red ever since. Roughly 356 commits landed behind that red. Because the gate stops REPORTING once it stops PASSING, ~338 additional errors accumulated completely invisibly — nobody saw them arrive. A prior recon measured **351 errors across 45 files, ~70% TS2339**, with the top five files carrying ~44%.
⚠ **RE-MEASURE ALL OF THAT YOURSELF at the current committed HEAD sha in a clean archive.** The figures above are a prior lane's and are the thing you are checking, not the thing you are assuming. If the count differs, report the delta — it tells us the accumulation rate.

**PRODUCE:**

**(1) THE TAXONOMY.** Every error grouped by TS code and by file, with counts. Name the top-carrying files with their individual counts and their cumulative share.

**(2) THE TRIAGE — THIS IS THE POINT OF THE SLICE.** TS2339 is "property does not exist on type". That code has TWO completely different causes and they need opposite treatments:
  - **(a) A TYPE-KNOWLEDGE GAP** — the property genuinely exists at runtime, the checker just cannot see it (untyped object literal, a JSDoc \`@param {Object}\`, an inferred-\`any\` boundary, a missing typedef). The cure is a type annotation. **Comment-only, zero runtime risk, zero golden risk.**
  - **(b) ⭐ A REAL LIVE DEFECT** — the property does NOT exist at runtime either, and the code is reading \`undefined\` in production right now. Classic shapes: a renamed field the reader never followed (this program has a RECORDED writer/reader spelling-drift class), a misspelling, a wrapper-vs-inner confusion (there is a recorded \`byId\` trap where values are \`{settlement, causal}\` WRAPPERS and bare-settlement reads green nothing), a field on the wrong object in a chain.
  **For EVERY TS2339, decide (a) or (b) by READING THE WRITER — find where the object is actually constructed and check whether the property is ever assigned.** Do not decide from the error text. Report the (b) set separately and prominently: **those are live bugs the gate has been hiding for ~356 commits, and they are the most valuable thing in this task.** For each, state the file:line, what is read, what is actually written, and the observable consequence.

**(3) THE BURN ORDER** — files ranked by (errors removed) / (risk incurred), with each file's \`sizeBaseline\` headroom stated, because a file at its exact cap cannot take JSDoc lines and needs a different cure.

**(4) THE HONEST FLOOR.** Say plainly how many of the 351 are cheap type-only fixes and how many need real thought. Do not flatter the number.

Do NOT commit anything except, if you wish, a findings document — and if you write one, put it at \`docs/TYPECHECK_RED_CENSUS.md\` and NOWHERE the fold lane touches.

RETURN JSON: {headSha, totalErrors, byCode, byFile, topFive, realDefects, typeOnlyCount, burnOrder, sizeHeadroom, notes}.`, { label: 'tc:census', phase: 'Census', model: 'opus', effort: 'high' })

phase('Burn')
const targets = (census && Array.isArray(census.burnOrder) ? census.burnOrder : []).slice(0, 4)
if (!targets.length) log('CENSUS RETURNED NO BURN ORDER — nothing dispatched to the burn phase.')

const burned = await pipeline(
  targets,
  (t, _orig, i) => agent(LAWS + HAZARDS + `

YOU ARE BURN LANE ${i + 1}. Your target: ` + JSON.stringify(t) + `

The census reported: ` + JSON.stringify({ realDefects: census.realDefects, sizeHeadroom: census.sizeHeadroom }).slice(0, 6000) + `
⚠ Verify every claim about YOUR file against the tree yourself.

**FIX THE ERRORS IN YOUR TARGET FILE(S) ONLY.** Other lanes hold the other files — touching theirs is a REJECT.

**THE TWO KINDS, AND THEY LAND DIFFERENTLY:**
- **Type-knowledge gaps** → add the annotation. Prefer a DERIVED typedef over a hand-restated shape: \`@typedef {ReturnType<typeof f>['x'][number]} T\` cannot drift from its source, whereas a hand-written duplicate of a shape goes stale silently (this program has a recorded DERIVE-DON'T-RESTATE law and a hand-restated derivable is how several receipts went bad). Comment-only diffs are free — prove it with \`git diff\` showing only comment lines.
- **⭐ REAL DEFECTS** → **FIX THE BUG.** The owner's standing directive is to take the objectively better option even when it carries risk; a live \`undefined\` read left in place because the fix might move an output is the WORSE option. But the fix must be DECLARED, never silent:
  1. State the defect, the writer's actual spelling, and the consequence.
  2. **MEASURE the golden impact before committing** — the corpus is 525 golden-master settlements and a service-selection change moved 156 of them. If your fix moves goldens, say EXACTLY how many rows and which field paths, and record it as a declared one-time shift with its legitimate cause.
  3. If the fix would move a PAID surface, a persisted schema, or an output the owner has parked, **STOP and report it as owner-gated** — do not land it.
  4. Add a REGRESSION PIN that reds if the wrong spelling comes back. ⚠ A pin anchored on a filename or a single seed goes vacuous — pin the WRITER's real output.

⚠ **BEFORE ADDING ANY LINE, CHECK THE \`sizeBaseline\` HEADROOM FOR YOUR FILE.** Tolerance is ZERO and comments count. At cap, extract a lazy leaf; do not shrink the annotation to fit.

**PROVE IT.** Re-run the typecheck at your committed sha in a clean archive and quote the BEFORE and AFTER error counts for your file. A fix that does not drop the count is not a fix. Run the fast gate on every test touching your file, through the mutex chain.

Commit with explicit pathspecs. Verify the staged set by NAME AND COUNT. Trailer \`Co-Authored-By: Claude Opus 5\` and the body must carry \`⏳ OPUS-ERA — FABLE SURVEY OWED\`.

RETURN JSON: {file, errorsBefore, errorsAfter, typeOnlyFixes, realDefectsFixed, goldenImpact, ownerGated, pinsAdded, gates, commits, notes}.`, { label: `tc:burn:${(t && (t.file || t.name)) || i}`, phase: 'Burn', model: 'opus' })
)

phase('Verify')
const verdict = await agent(LAWS + HAZARDS + `

YOU ARE THE ADVERSARIAL VERIFIER. Default posture: WRONG until executed evidence. REFUTE.
Census: ` + JSON.stringify(census).slice(0, 8000) + `
Burn lanes: ` + JSON.stringify(burned.filter(Boolean)).slice(0, 12000) + `

⚠ RE-RUN EVERY GATE AND EVERY COUNT THE REPORTS QUOTE, at the committed shas, in integrity-counted archives ONE AT A TIME. **Seven false receipts have landed in this program in two days and the code was nearly always fine — the defects were in the receipts.** Specifically check: a count measured in the live shared tree; a count measured on a tree the lane later abandoned; an exit code read through a pipe; a "before" taken at a different sha than the "after".

VERIFY:
1. **THE TOTAL ACTUALLY DROPPED.** Run the whole typecheck at the final sha and compare to the census's HEAD figure. Per-file drops that do not sum into a total drop mean a lane pushed errors into a neighbour.
2. **NO NEW ERRORS ANYWHERE ELSE.** Diff the full error set, not the count — a count can hold steady while the membership churns.
3. **EVERY CLAIMED COMMENT-ONLY DIFF IS COMMENT-ONLY.** Check the actual diffs. Any executable line in a "comment-only" claim is a REJECT.
4. **EVERY REAL-DEFECT FIX IS REAL.** For each, confirm the writer's spelling independently and confirm the pin actually reds — **plant the old spelling back and watch it fail.** A pin that stays green under its own mutant is not a pin.
5. **THE GOLDEN CLAIM IS MEASURED, NOT ASSERTED.** If a lane says "no golden movement", verify it. If a lane says N rows moved, verify N.
6. **THE RATCHETS.** sizeBaseline, the domain-layer set, and the ratchet halves via \`sh scripts/ratchet-inventory.sh\` — a red ratchet's INVENTORY can grow behind a byte-identical failing row.
7. **NOTHING THE FOLD LANE OWNS WAS TOUCHED** — no \`docs/DESIGN_FP_ARCH*\`, no \`SOL_QUEUE.md\`, no \`couplingRegistry.test.js\` in any burn commit.

RETURN structured JSON only.`, { label: 'tc:verify', phase: 'Verify', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, totalBefore: { type: 'number' }, totalAfter: { type: 'number' }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

return { census, burned, verdict }