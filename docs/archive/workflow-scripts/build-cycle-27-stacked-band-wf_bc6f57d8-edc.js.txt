export const meta = {
  name: 'build-cycle-27-stacked-band',
  description: 'THE STACKED FLETCH BAND (owner mockup) + thinner shaft, same font · the maker\'s plate + wax-seal · the AA/jitter tail',
  phases: [{ title: 'Build', detail: 'one nav lane, four pieces, implement + verify with eye pass' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files (nav/*, theme.js, nav tests, App.jsx header block); no stash; focused vitest + gate-tail (ps aux | grep vitest first); python byte-scan every Write; npm run build + smoke:boot green; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. SURVEY FIRST: a prior badge/sweep lane was stopped mid-flight minutes ago — read git log + status on the nav files; any landed or uncommitted badge work is REAL progress to build from, any sweep work is SUPERSEDED by this brief. STANDING LAWS: overhang/hang is PAINT never layout; ANCHOR derives from the token; focus rides an aria-hidden vane; screenshot BEFORE pinning geometry; the GROUND-MOVE law — every ground change re-censuses AA on EVERY header rider incl. AccountMenu's transparent chip; ⚠️⚠️ pin the COMPOSITED ground (grain over cylinder), never the cylinder alone — the recorded vacuous-pin finding.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: OWNER-DIRECTED implementation from a mockup the owner drew (described exactly below — it supersedes every prior sweep/overlap instruction). ${P10}
THE OWNER'S ABSTRACT MOCKUP (a wireframe; its red/gold are PLACEHOLDER colors — keep our landed goose-brown vanes and honey-wood shaft): a THIN horizontal bar (the shaft); riding it, ONE CONTINUOUS BAND containing Create, Library, Realm as three SLANTED-PARALLELOGRAM cells that SHARE their slanted internal edges — the cells are joined edge-to-edge with ZERO gap, the internal boundaries are thin slanted seam lines (like /// strokes), the band's left edge slants the same way, and the whole band is roughly TWICE the bar's height, top-aligned with the bar and HANGING WELL BELOW its bottom edge (roughly half the band sits below the bar). After Realm the band ends and Compendium/Gallery sit small and plain on the bar. The read: one stacked run of fletching — shingled feathers on a thin shaft — not three separate tabs.
LANE SB2 — FOUR PIECES, one commit each:
(1) THE STACKED BAND: rebuild the fletch cluster as ONE band per the mockup — three parallelogram feather cells sharing slanted edges (the NavDivider seams INSIDE the cluster are RETIRED — this is the verifier's own cure for the 38px wood wedges; the LAP unit bug dies with the seams); the band hangs below the bar (PAINT only; size the hang so ~40-50% of the band sits below the bar's bottom edge, matching the mockup's proportions); each cell keeps the goose vane treatment (barb comb ALIGNED to the cell's slant, rachis, sheen) with a small DETERMINISTIC jitter in barbGap/alpha (the machine-regular-comb finding; pure arithmetic, seed-stable); the red-brown wraps bracket the band's two ends; active grammar preserved (brightened cell + gold underline); labels keep their CURRENT font size and stay AA on the vane.
(2) THE THINNER SHAFT: shrink CHROME.headerDesktop from 48 to the THINNEST height that still seats the wordmark, reference tabs, and Sign In at their CURRENT font sizes with sane padding (target ~36-40 — measure, don't guess; pin the label font sizes so this change provably does not shrink type); ANCHOR_OFFSET re-derives automatically (assert the derivation); the band's height is INDEPENDENT of the bar (it keeps label-comfortable height and hangs deeper as the bar thins — that growing hang is the mockup's whole silhouette); full ground-move AA re-census on every rider; mobile follows the thinner bar without the band.
(3) THE MAKER'S PLATE + WAX-SEAL WORDMARK: build EXACTLY to task #78's research-locked spec (read it verbatim); the plate sizes to the NEW thinner bar (its modules scale; it may also hang a few px below like the band — same paint law); THE BALANCE LAW: the plate stays a half-step quieter than the band. Survey the stopped lane's partial badge work first and reuse what is sound.
(4) THE COMPOSITED-AA PIN (the recorded vacuous-guard finding): the AA pins must assert against the COMPOSITED worst-case ground (grain max-darkening derived from the feColorMatrix constants, or a rendered-pixel probe) — the one-token grain-darkening mutant must red them.
Screenshot desktop 1440x900 + mobile + a close crop of band and plate at every iteration with honest eye verdicts (the acceptance question: does it read as the owner's mockup made of wood and feathers?). Return {lane:'SB2', commits, gates, stopReports, notes}.`,
  {label:'sb2:stacked-band', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier with an EYE PASS. ${P10}
Report: ${JSON.stringify(r)}. Serve, screenshot, JUDGE against the mockup description: (a) ONE continuous band, cells sharing slanted edges, ZERO wood gaps inside the cluster (measure the rendered pixels — the old 38px wedges must be GONE); (b) the hang proportion ~40-50% below the bar, paint-only (hide-and-diff layout boxes); (c) the bar genuinely thinner with font sizes UNCHANGED (measure both, quote px); (d) the plate subordinate, the wordmark cold-reads instantly with the seal as the o, wax not candy, grayscale-binds; (e) goose vocabulary retained per cell with the jitter organic not noisy. Mechanical: ANCHOR re-derivation; the COMPOSITED-AA pin present and the grain-darkening mutant reds it (run it); AA recomputed from REAL rendered pixels on every rider (quote all ratios); focus by real Tab across plate and band; texture deterministic (two loads byte-identical); Rule-of-Tincture constraint mutant; typed contexts plain; mobile thinner without band; npm run build + smoke:boot; pathspec-clean. Return {lane:'SB2 verify', verdict, findings}.`,
  {label:'sb2:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }