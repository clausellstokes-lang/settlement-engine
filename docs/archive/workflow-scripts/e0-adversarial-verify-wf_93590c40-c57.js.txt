export const meta = {
  name: 'e0-adversarial-verify',
  description: 'Four adversarial Opus verifiers try to refute E0 constitutional guarantees',
  phases: [{ title: 'Refute' }],
}

const SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['dimension', 'verdict', 'attemptedRefutations', 'evidence', 'residualRisks'],
  properties: {
    dimension: { type: 'string' },
    verdict: { type: 'string', enum: ['HOLDS', 'BROKEN'], description: 'BROKEN if you found ANY concrete violation; HOLDS only if you tried hard and could not.' },
    attemptedRefutations: { type: 'array', description: 'The specific attack scenarios you constructed and their outcome.', items: { type: 'string' } },
    evidence: { type: 'array', description: 'file:line + what it proves.', items: { type: 'string' } },
    residualRisks: { type: 'array', description: 'Anything uncertain or worth the architect double-checking, even if verdict HOLDS.', items: { type: 'string' } },
  },
}

const COMMON = 'You are an ADVERSARIAL VERIFIER for E0, the Narrative Tempo Governor, just implemented in worktree /Users/cstokes/Desktop/settlement-engine/.claude/worktrees/brave-babbage-67b994 (branch claude/zealous-driscoll-f74588). Your job is to REFUTE — try hard to construct a concrete violation of your assigned guarantee. Default to skepticism: only return verdict HOLDS if you genuinely tried to break it and could not. Read the ACTUAL code (do not trust comments or the plan). The diff touches: src/domain/worldPulse/narrativeTempo.js (new), decisionTier.js, candidateEvents.js, pulseKernel.js, worldState.js. Plan: docs/PHASE55_E0_TEMPO_GOVERNOR_PLAN.md. Design: docs/DESIGN_PACING_GOVERNOR.md. You are READ-ONLY — do not edit source. You MAY run focused tests / write throwaway probe scripts under the scratchpad to prove a point. Cite file:line for every claim.'

phase('Refute')
const DIMS = [
  {
    key: 'dormancy-byte-identity',
    prompt: COMMON + '\n\nYOUR GUARANTEE TO REFUTE: "With the governor absent/off (no narrativeTempo axis on rules), every same-seed output is BYTE-IDENTICAL to pre-E0." Try to find ANY dormant-path perturbation.\n\nATTACK SCENARIOS (read the real code at each):\n1. candidateEvents.js rollCandidates: does the dormant path (tempo absent / active:false) add ANYTHING — a new field, a rollExplanation row, a mutated candidate — vs pre-E0? Is deferred always [] when dormant, and does adding that key to the return object perturb any consumer that spreads/serializes the return?\n2. pulseKernel.js: buildTempoContext dormant returns {active:false}; foldNarrativeTempo dormant returns null → the code drops the narrativeTempo key. Is there ANY case where a dormant tick leaves a narrativeTempo key on worldState (a pre-existing empty {} not dropped, or the drop branch not firing)? Is tempoReceiptNews always [] when dormant?\n3. The kernel return spreads tempoDeferred only when non-empty — confirm a dormant/OFF tick NEVER adds tempoDeferred. Does any test/golden snapshot the full return object such that an added key would shift?\n4. worldState.js CONDITIONAL_LEDGER_KEYS: narrativeTempo appended at END. Confirm ensureWorldState strips it when absent/empty and does NOT materialize an empty one, and that the append does not change the serialized key order of any OTHER key.\n5. Run the dormancy byte-identity test + the 3 worldPulse goldens (worldpulseSpatial/Seasons/Deity) + religion/belief dormancy suites and confirm green. Then write a probe: previewCampaignWorldPulse on a hot fixture WITHOUT narrativeTempo, assert NO narrativeTempo/deferred/tempoDeferred/tempo_pressure key anywhere in the result tree.\nVerdict BROKEN if you find any dormant-path byte shift; HOLDS only if airtight.',
  },
  {
    key: 'determinism-no-rng',
    prompt: COMMON + '\n\nYOUR GUARANTEE TO REFUTE: "The governor is fully deterministic — ZERO rng, same-seed ⇒ same deferrals, no insertion-order or locale dependence, correct window basis." Try to break determinism.\n\nATTACK SCENARIOS:\n1. Grep the governor module + diffs for Math.random / Date.now / new Date / .random( / .fork( — the governor must draw NO randomness. Confirm the deferral continue in rollCandidates happens BEFORE candidateRoll (no rng consumed for a deferred candidate).\n2. Insertion-order: computeLowestPendingClass iterates candidates in array order — is its result order-independent (it takes max priority index)? governBirth + readTempoLedger + foldNarrativeTempo: any Object.keys iteration whose RESULT feeds a decision or leaks into serialized output without a codepoint sort?\n3. Window basis: confirm BOTH the READ (buildTempoContext) and WRITE (foldNarrativeTempo) use worldState.calendar.elapsedWeeks, NOT worldState.tick. Would a coarse-interval call or the M10b catch-up collapse mis-size the window if it keyed on tick?\n4. Same-seed ⇒ same deferrals: pure function of (ledger, candidate, budgets, liveArcs)? Any hidden state? Run the property test deferral-determinism pin + a probe: two identical driveTicks runs, assert identical deferred sets (JSON hash).\n5. Locale: any localeCompare in the new code (banned in src/domain)?\nVerdict BROKEN if you find any nondeterminism; HOLDS only if airtight.',
  },
  {
    key: 'chain-immunity',
    prompt: COMMON + '\n\nYOUR GUARANTEE TO REFUTE (design §1 cardinal law): "The governor is provably unable to throttle a RECEIPTED consequence — every consequence chain fires regardless of budget." Try to construct a consequence the governor WOULD defer.\n\nATTACK SCENARIOS:\n1. isChainedConsequence(candidate) — enumerate EVERY receipted-consequence shape and check each returns true: stressor_residual, stressor_spread_<type>, stressor_escalate_<type> (the candidateType ENDS in the type — verify the regex /^stressor_(residual|spread|escalate)(_|$)/ matches on the REAL candidateType, and that the real emitters carry that candidateType), mobilization_reaction_* (ruleFamily), population_emigration (prob 1), coup_succeeded/coup_suppressed (prob 1), conquest, and condition.causes[0].source = world_stressor.*. For each, find the REAL emitter (stressors.js/stressorsCore.js/coup.js/populationDynamics.js/mobilizationReactions.js) and confirm the shape isChainedConsequence keys on is actually present.\n2. Off-seam bypass: coupOutcomes/warOutcomes/structuralCandidates concat at pulseKernel.js:1158 AFTER rollCandidates — confirm they never pass the governor gate. But the WRITE fold reads selectedForApply (which includes them) — confirm foldNarrativeTempo does NOT record them as births (it filters isChainedConsequence + dramaClassOf wired — verify a coup_succeeded/conquest is filtered so the class budget is not inflated by consequences).\n3. The dangerous gap: a stochastic (<1) chained consequence that reaches the seam AND maps to a governed class AND is NOT caught by isChainedConsequence. Try stressor_spread_famine (prob<1): does dramaClassOf return null for it (registry has only stressor_birth_*)? AND does isChainedConsequence return true? Confirm BOTH guards. Try mobilization_reaction (prob 0.9): caught by ruleFamily?\n4. Run the property chain-immunity pin + a probe: drive economic_shock over classMax, inject a stressor_spread_famine, assert it STILL lands in selected.\nVerdict BROKEN if you can get a receipted consequence deferred OR a birth-count inflated by a consequence; HOLDS only if airtight.',
  },
  {
    key: 'budget-laziness-anycast',
    prompt: COMMON + '\n\nYOUR GUARANTEE TO REFUTE: "E0 adds ZERO eager first-paint bytes, no new any-casts, closure (budget 1,216,350) unchanged." Try to find eager leakage.\n\nATTACK SCENARIOS:\n1. Import graph: is narrativeTempo.js reachable from the EAGER entry closure via STATIC imports? Does any eager module (store slices from main.jsx, engine-core spine) statically import narrativeTempo.js / decisionTier.js / pulseKernel.js / candidateEvents.js? The pulse chain should be reached only via dynamic import of advanceCampaignWorld.js at campaignWorldPulseSlice.js and the worker. Confirm narrativeTempo.js has NO importer outside the lazy pulse chunk.\n2. narrativeTempoOf lives in narrativeTempo.js (lazy), NOT simulationRules.js (eager) — confirm simulationRules.js was NOT modified (git diff must show it untouched).\n3. any-cast: run (single quotes) node scripts/count-domain-any.mjs (do NOT pass --update). Confirm narrativeTempo.js does NOT appear (0 holes), decisionTier.js did not rise, candidateEvents.js stayed 24, and tests/lint/.domain-any-baseline.json is git-clean (git diff --quiet).\n4. First-paint closure: statically argue whether E0 CAN change the closure (only if an eager module gained a static edge into the governor). Since the governor is lazy-only, closure should be unchanged (last 1,216,273). Do NOT start a build — the architect is running the full gate (build + verify:dist) in parallel; argue from the import graph + count-domain-any and note the gate will confirm.\nVerdict BROKEN if you find eager leakage / a new any-cast / a closure risk; HOLDS only if airtight.',
  },
]

const results = await parallel(DIMS.map(d => () =>
  agent(d.prompt, { label: d.key, phase: 'Refute', schema: SCHEMA, model: 'opus' })
))
return results.filter(Boolean)
