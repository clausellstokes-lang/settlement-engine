export const meta = {
  name: 'build-cycle-6r-repairs-wr8',
  description: 'Cycle-6 repair tail: three ratification residuals + WR-8 · About-lane fixes + doc nits',
  phases: [{ title: 'Build', detail: 'war lane serial (repairs then WR-8) · repair lane' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest before believing a timeout; full logs for walker diffs); strict-typecheck your new/touched files yourself; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Everything DARK, nothing pushed. CONCURRENCY: three other lanes are live on decomposition targets (causeConjunctionRoleContent/npcGenerator/settlementSlice/viewModel/aiSlice/historyGenerator), hook files, and LD-ladder components — those territories are absolutely excluded from your commits. SOL_QUEUE.md/FABLE_VALIDATION_QUEUE.md edits: carve your hunks by pathspec, expect concurrent edits, re-read before writing.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const lanes = await parallel([
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE A — WAR LANE, SERIAL: first the cycle-6 verifier's three residual repairs (own commit), then WR-8.
STEP 1 — chair rulings R-BLD-8a/8b/8c on chooseAmongCompetingOffers (src/domain/worldPulse/coalitionRatification.js; verified residuals, module dark and unwired): (8a) refuse cross-side unions — beside the existing duplicate_offer check, refuse with a closed reason when any two tallies disagree on sideId or counterpartId (the verifier's executed counterexample: two opposite-side tallies for one episodeKey union into a 9-weight 'coalition' and ratify — pin exactly that shape red); (8b) the sole-offer arm must be self-consistent by construction — derive the sole verdict from its own holds (union math) so a record can never say ratified while offers[0].holds is false; if the caller's closeBand01 differs from the tally's band, refuse closed (band_mismatch) rather than emit a contradiction; amend the docstring to say what the code now enforces; pin the verifier's counterexample (tally ratified at 0.15, chooser called at 0.9); (8c) recompute acceptWeight from the ballots in the loop unionCoalitionWeight already walks — refuse closed when the summary's acceptWeight disagrees (the forged-999 counterexample becomes the pin). Also DELETE tests/domain/zzVerifierProbeTmp.test.js — an untracked leftover probe from our own cycle-6 verifier (its header says so); it is not foreign WIP, remove it and confirm the negativeAssertionAnchor walker's red list returns to its prior width. One commit: the three repairs + their pins + a FABLE_VALIDATION_QUEUE.md R-BLD-8 row (vetoable).
STEP 2 — WR-8, the next war wave. Read the WR-8 section WHOLE in the war architecture doc (docs/ — the WAR_RULINGS/WR-0..WR-10 architecture; find it by grep -l "WR-8" docs/) plus its referenced spine requirements; goldens BEFORE wiring; wire the pulse before committing (R-BLD-1: stage-neuter negative controls proving runtime reachability); alignment coupling + the edit-verb story per spine 13+14; one wave one commit + ledger row. ⛔ THE LICENSE STOP GATE: if WR-8's license-coupling clause triggers a decision, STOP — write the decision brief into stopReports and DO NOT rule; the volume says report-don't-rule and the chair takes it. Return {lane:'A repairs+wr8', commits, gates, stopReports, notes}.`,
    {label:'laneA:r-bld-8+wr8', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify STEP 1 by re-executing the three original counterexamples (cross-side union, band-mismatch self-contradiction, forged acceptWeight) against the committed bytes — each must now refuse closed with its named reason, and each pin must red under a mutant that re-opens the hole; confirm the probe file is gone and the anchor walker's red width shrank accordingly. Verify STEP 2 as a full wave verify: gates re-run on committed bytes, mutant controls on the load-bearing pins, pulse reachability (stage-neuter), goldens honest, pathspec-clean, no foreign lane files. If a license STOP was reported, verify the lane actually stopped (no license ruling encoded in code or docs). Return {lane:'A verify', verdict, findings}.`,
      {label:'laneA:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled repair implementation. ${P10}
LANE R — THE CYCLE-6 REPAIR TAIL (About lane + doc nits; verified findings, all small, one commit).
(1) tests/lint/negativeAssertionAnchor.walker.test.js:173 — the frozen row 'tests/build/sitemap.test.js': 5 → 2 (bank the win the About lane earned; the walker's own red message dictates this).
(2) tests/components/aboutSplit.test.jsx lines ~296,307,308,309 — the four source-scan negatives get ON-LINE anchors: use tests/helpers/anchoredNegatives.js if it fits, else a same-line "// anchored: <structural reason>" naming why the source-scan cannot be vacuous (the walker accepts only on-line anchors; prose above the block does not count).
(3) src/components/HowToUse.jsx:15 — the docstring's mapping-writer path is wrong: it says components/about/aboutMapping.js; the writer is src/lib/aboutMapping.js and its lib/ placement is load-bearing (routing must not import a component module) — fix the path and say why it lives in lib/.
(4) CHAIR RULING on DESIGN_ABOUT_PAGES §3's third VERIFY-AT-BUILD (the MAY): BUILD IT — the Compendium pattern HAS in-page section nav (src/components/compendium/CompendiumDashboard.jsx:17); HowToUse.jsx is a 387-line flat page with five sections; the legibility law wants the glance layer. Match the Compendium pattern exactly (match, never invent), then answer the third VERIFY-AT-BUILD in the doc's PROGRESS blockquote. If AboutWhatThisIs has comparable flat length, apply the same call there; if not, record why not.
(5) DESIGN_ABOUT_PAGES PROGRESS block — correct the overstated phrase 'no menu layer anywhere in src/' (AccountMenu.jsx and SettlementMapExportMenu.jsx are real menus; the true claim is: no NAV-RIBBON dropdown exists, which is LD-5's grammar).
(6) The eventProse path cite — in docs/content/RECEIPT_POOLS_LEGACY.md's header table, docs/SOL_QUEUE.md item 22, and FABLE_VALIDATION_QUEUE.md's J-LEG-WIRE-12 row, the §1 consumer is cited as bare 'eventProse.js'; write it as src/domain/worldPulse/eventProse.js everywhere (a future implementer will otherwise hunt src/domain/display/).
DO NOT touch: war/envoy files, decomposition targets, hook files, LD-ladder components beyond HowToUse/AboutWhatThisIs, App.jsx. Gates: the walker suite + aboutSplit suite + the About-adjacent cohort green on committed bytes; eslint + strict on touched files. Return {lane:'R repairs', commits, gates, stopReports, notes}.`,
    {label:'laneR:c6-tail', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify by execution: the anchor walker's inventory-honesty test green with the row at 2 and red under a mutant restoring 5; the four anchors accepted by the walker (its red list must not name aboutSplit.test.jsx) and each anchor's stated reason true; the in-page nav matches CompendiumDashboard's actual pattern (side-by-side read, no invention) and renders (aboutSplit suite or a new pin proves presence + hrefs resolve to real section ids); the three path cites now byte-agree; the PROGRESS blockquote answers the third VERIFY-AT-BUILD and the overstatement is gone; pathspec-clean against all concurrent lanes. Return {lane:'R verify', verdict, findings}.`,
      {label:'laneR:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
])
return { lanes: lanes.filter(Boolean) }