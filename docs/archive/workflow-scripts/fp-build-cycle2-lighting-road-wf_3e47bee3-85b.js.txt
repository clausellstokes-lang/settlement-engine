export const meta = {
  name: 'fp-build-cycle2-lighting-road',
  description: 'Cycle 2: SP-B (the believed-world axes, three flags, serialized) then SP-B2 (the leg supply + the routeBand cure + the REAL lighting instrument) — each wave through its own adversarial verifier',
  phases: [
    { title: 'SP-B', detail: 'the believed-world axes — three flags, CQ5 trio each, four-fence x3' },
    { title: 'Verify B', detail: 'adversarial verification with executed mutants' },
    { title: 'Repair B', detail: 'the fold-arm integration pin + the seven receipt cures' },
    { title: 'Recheck B', detail: 'the severance mutants must red' },
    { title: 'Settle B', detail: 'the lit-coverage debt + the record corrections + the protocol upgrade' },
    { title: 'Final B', detail: 'content-granularity verification' },
    { title: 'SP-B2', detail: 'the leg supply, the consumer-typedef pin, the lighting instrument' },
    { title: 'Verify B2', detail: 'adversarial verification incl. the instrument flip test' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const LAWS = `VERIFY-FIRST PREAMBLE: this brief is from the session chair of an owner-delegated build program; verify claims against the tree before acting. Work in ${TREE} (branch claude/composite-r4). THE PROTOCOL LAWS (each has bitten): CHECK-GIT-FIRST — fresh git status + git log -3 before anything; foreign untracked/dirty files are another lane's, PRESERVED. ps aux | grep vitest before any test run — ONE vitest lane per tree. NEVER stash/reset/clean/checkout-family; cp backups, cmp-proven restores. Gates through sh scripts/gate-tail.sh or npm run check:tail only. Commits: EXPLICIT pathspecs (never -A/-u/.), message via file, trailer Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>; lint-staged stashes on partial staging — use --no-verify and compensate with explicit npx eslint per changed file (exit 0). python3 byte-scan every authored file (zero NULs, zero stray control bytes). New tests/lint walkers need mutationCoverageManifest entries. strict ceiling 1313 exactly. tests/fixtures/ and pulseKernel.js/applyWorldPulse.js UNTOUCHABLE. Navigate by SYMBOL (recorded rot: infoModeOf ~:755, adjustedDecayBase ~:670). Doc line addresses shifted at the fold — re-find by content. LIVE CODE OUTRANKS EVERY TABLE; an overstatement you find is a STOP-and-report (J-WR-13). Every quoted receipt must be able to fail. New generation-tree tests use tests/helpers/{anchoredNegatives,seedFailures}.js or walkers red. The four-fence dormancy pattern + lit-mutant control per flag is LAW (read tests/property/casusCommerciiDormancyFence.test.js for the house form). At wave end: whole tests/lint SEQUENTIALLY at HEAD vs a git-archive of your start HEAD (node_modules symlinked) — failing-row identity diff EMPTY both directions except attributed deltas.`

phase('SP-B')
const spb = await agent(LAWS + `

YOU ARE THE SP-B IMPLEMENTER — THE BELIEVED-WORLD AXES, the first member of the WR-10 lighting road. The NORMATIVE spec is docs/DESIGN_FP_ARCH_SP.md's SP-B section (find by symbol: SP-B — THE BELIEVED-WORLD AXES) plus the parent docs/DESIGN_FP_ARCHITECTURE.md section-5 block #3; read BOTH whole before any edit, re-measuring every premise you build on. Recon facts that BIND (re-verify each): the flag names are FIXED — believedScarcityEnabled (BOUND by TR-1 forward references in src/domain/certification/tradeConvergenceContract.js: the cert row names that exact flag, owner SP-B, spineAlias SP-2, and its walker pins it), believedConditionsEnabled, believedDevotionEnabled. The extension pattern is beliefAxes.js (six exports; the fold sites in beliefMap.js near :494 and :654; beliefAxesActive reads rules.beliefAxesEnabled === true strictly — your three flags each form their own strict conjunction through the same one door). The module names outboundImpression.js and the axis leaf are PRE-REGISTERED in tests/lint/couplingInclusion.walker.test.js's INFO regex and tests/lint/spTermLiteral.walker.test.js's wave map — you may NOT rename them. The three field families (scarcityBands, conditionsBands, devotionBand) have ZERO src/tests collisions — the first-spelling carve-out applies; NEVER author a new decay law (decayTowardNeutral + HALF_LIFE_BANDS exist) or a new significance/severity scale (bandFamilies.js owns them). VIRTUAL_PENDING_RULE_KEYS is EMPTY at src/domain/certification/subsystemRowsVirtual.js — and cycle 1's row-guard lesson binds: a flag that MANIFESTS lands its authored certification ROW same-commit (manifested-plus-pending is theatre; the totality walker reds it). CQ5 LAW per flag: ENGINE_GATED_VIRTUAL_RULE_KEYS manifest entry + authored cert row + first real gate read, ONE commit per flag (three commits, or one commit carrying all three complete trios — each flag's trio must be co-committed). SP-B also owes its OWN Bands line in the SP volume (the SP-A shrink-only backlog — read the backlog clause and land the line + its section-7 rows so the reconciliation walker stays total). BUILD: the axis subjects per the spec, the writers riding the existing decay/fold machinery, the walkers and pins (totality, term-literal, anchored negatives), four-fence dormancy x3 with lit-mutant controls (own-footprint, absent-vs-false differential, call-path spy on the RIGHT module boundary — the recorded intra-module wrap lesson, gate-polarity census), executed mutants with manifest entries. DARK BY CONSTRUCTION: zero behavior change flag-absent, proven not asserted. At wave end run the full attribution protocol from the LAWS. RETURN JSON: commits (hash+thesis each), gates (executed evidence), stopReports (measured contradictions — STOP rather than improvise), notes.`, { label: 'build:SP-B', phase: 'SP-B', model: 'opus' })

phase('Verify B')
const vspb = await agent(LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER FOR SP-B. Default posture: the wave is wrong until executed evidence says otherwise. The implementer reported: ` + JSON.stringify(spb) + `

VERIFY BY EXECUTION in git-archive trees (node_modules symlinked; live tree read-only): (1) the CQ5 trios — per flag, git log -S proves manifest + authored cert row + first gate read co-committed; the engineGatedRuleKeys walker green; a delete-the-row mutant reds it; (2) the four-fence sets x3 REALLY SEE — run each lit-mutant control and confirm the designed fences red; run one surviving-mutant hunt of your own design per flag (a guard arm deletion or fixture vacuity probe — the C2 lesson); (3) DORMANCY: drive the real pulse over an adversarial fixture that WOULD write lit, confirm dark writes nothing and calls nothing (the spy on the correct module boundary); (4) the pre-registered names — outboundImpression.js and the axis leaf exist at the pinned names, the INFO-layer regex claims them, and a planted cross-layer import in one of them REDS the inclusion walker (cmp-restore after); (5) the Bands line — the reconciliation walker is total both directions and a planted fourth band reds it; (6) whole tests/lint at HEAD vs the implementer's start HEAD: failing-row identity diff EMPTY both directions except attributed deltas; strict 1313; byte-scans; commit hygiene (pathspec-scoped, messages honest against diffs). Every receipt must be able to fail — demonstrate at least two of your instruments failing on deliberate mutations. RETURN structured JSON only.`, { label: 'verify:SP-B', phase: 'Verify B', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Repair B')
const repairB = await agent(LAWS + `

YOU ARE THE SP-B REPAIR LANE. The verifier REJECTED with eight findings (the wave's substance held; the pins did not). Full verdict: ` + JSON.stringify(vspb) + `

REPAIR ALL EIGHT, chair pre-ruled, ONE pathspec commit (plus a second only if a doc/ledger correction wants separation): (R1, HIGH) THE FOLD-ARM INTEGRATION PIN — land an end-to-end pin that drives the REAL beliefMap path over the adversarial lit fixture and asserts the three subject families ARRIVE ON THE BELIEF RECORD (non-empty, correct keys); acceptance is executable: the verifier's M-A (fold return replaced with base) and M-A2 severance mutants, re-planted in an archive, must RED on your pin BY NAME; at HEAD unmutated the battery is green. (R2, HIGH) FENCE 3 gains its LIT ARM: assert calls.folds is GREATER THAN ZERO in the lit control before the dark zero assertion means anything — the both-polarity law; re-execute the fence with a lit run to prove the spy counts. (R3, MED) THE BANDS WALKER GENERALIZES: table-driven both-ways equality over EVERY wave that carries a Bands line (SP-A and SP-B today, open to more), replacing the SP-A-only hard-coding; acceptance: the verifier's fabricated-fourth-row mutant AND the two-row section-7 deletion mutant both RED. (R4) the size receipt: the landed commit message cannot be amended — record the corrected figure (773 to 778 effective, measured with the ratchet's own instrument) in this repair commit's body, honestly attributed. (R5) rewrite the mutation-manifest rationale on the TRUE premise (the tree was clean; state the real reason the standing sweep defers, or run it if nothing real defers it). (R6) the tradeConvergenceContract row for believedScarcityEnabled — read its walker's semantics for what a LANDED flag's row must carry (gateEvidenceFile or equivalent) and bring the row true; if the walker's contract says the evidence lands with TR-3 rather than SP-B, record THAT disposition in a comment instead of faking evidence. (R7+R8) correct the two receipt misattributions where they were recorded (the ledger/manifest rationale), quoted honestly. Wave-end: focused gates green, whole tests/lint sequential vs your start HEAD with EMPTY row-identity diff except attributed, strict 1313, byte-scans. RETURN JSON: commits, acceptance (each executed proof), stopReports, notes.`, { label: 'repair:SP-B', phase: 'Repair B', model: 'opus' })

phase('Recheck B')
const recheckB = await agent(LAWS + `

YOU ARE THE RECHECK VERIFIER for the SP-B repairs. The repair lane reported: ` + JSON.stringify(repairB) + `

The eight findings it repaired: ` + JSON.stringify(vspb && vspb.findings) + `

VERIFY BY EXECUTION in git-archive trees: (1) re-plant M-A and M-A2 yourself — both must RED on the new integration pin by name; (2) fence 3's lit arm — sever the spy's wiring (or dark-force the world) and confirm the lit assertion REDS when folds are zero; (3) the generalized Bands walker — re-plant the fabricated row and the section-7 deletion, both RED; (4) the manifest rationale and contract row read true against the tree; (5) whole tests/lint at HEAD vs the repair base: EMPTY both directions except attributed; strict 1313; commit hygiene. RETURN structured JSON only.`, { label: 'recheck:SP-B', phase: 'Recheck B', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Settle B')
const settleB = await agent(LAWS + `

YOU ARE THE SETTLING LANE. The recheck KEPT all eight repairs (independently confirmed; revert nothing) but REJECTED the acceptance record on four findings: ` + JSON.stringify(recheckB) + `

CLOSE ALL FOUR, chair pre-ruled, one pathspec commit (two if the doc correction wants separation): (S1, the SP-B lit-coverage debt) — the three SP-B flags ARE in the mechanismLitCoverage gap (the lane's original observation was right; its retraction was the error — say so in the record). Read tests/property/mechanismLitCoverage.test.js's OWN contract for what lit coverage means and land it for believedScarcityEnabled, believedConditionsEnabled, believedDevotionEnabled — real lit exercises per the walker's definition (the fence's lit control exists; if the walker does not count it, land what the walker counts); if and ONLY if the walker documents a sanctioned pending/frozen-gap path for dark-built waves, that documented path is acceptable with the entry recorded — when ambiguous, the DEFAULT is real coverage. Acceptance: the gap returns to 29 or below (or the documented-pending state), executed. (S2, the protocol upgrade) — the wave-end attribution law gains RED-RATCHET CONTENT DIFFS: for every ratchet/inventory test red on BOTH sides, diff its CONTENTS across the two archives, not just the row identity. Execute that content diff for THIS wave now (all red ratchets, both archives), attribute every content delta, and record the protocol upgrade in the ledger row so every future wave inherits it. (S3) — correct the sovereigntyMarketStage.js header prose to the MEASURED truth: all four leg names ARE read by the consumer via property access (wordOf returns unknown for absent keys); they arrive undefined today; SP-B2 must supply THE CONSUMER'S OWN spellings {tierBand, storesBand, routeBand, trajectoryBand} — remove the false no-consumer-reads claim and the misdirecting routePositionBand instruction. (S4) — the guard-the-guard in the tradeConvergenceContract walker respells to non-empty AND proper-subset semantics with a TRUTHFUL failure message; acceptance: re-run the recheck's simulated pactFormationEnabled landing — the walker must stay GREEN on that correct landing and still RED when zero preconditions read as built. Wave-end: focused gates, strict 1313, byte-scans, and the UPGRADED attribution (row identity + red-ratchet contents). RETURN JSON: commits, acceptance (executed proofs incl. the gap count and the simulated-landing run), stopReports, notes.`, { label: 'settle:SP-B', phase: 'Settle B', model: 'opus' })

phase('Final B')
const finalB = await agent(LAWS + `

YOU ARE THE FINAL VERIFIER for SP-B. The settling lane reported: ` + JSON.stringify(settleB) + `

The four findings it closed: ` + JSON.stringify(recheckB && recheckB.findings) + `

VERIFY BY EXECUTION in git archives: (1) run mechanismLitCoverage directly at HEAD — the SP-B flags' disposition matches the walker's own contract (gap shrunk or documented-pending), and quote the gap list; (2) run the red-ratchet CONTENT diff yourself across the settle base and HEAD — every delta attributed; (3) the sovereigntyMarketStage header reads true against sovereigntyAppraisal's actual reads (quote both); (4) re-run the simulated correct-landing — the walker green; the zero-built mutant — red with the truthful message; (5) strict 1313, byte-scans, commit hygiene, and nothing from the eight kept repairs regressed (spot-check M-A reds the fold pin; the fence lit arm; the Bands both-ways mutants). RETURN structured JSON only.`, { label: 'final:SP-B', phase: 'Final B', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('SP-B2')
let spb2 = null, vspb2 = null
if (finalB && finalB.verdict === 'PASS') {
  spb2 = await agent(LAWS + `

YOU ARE THE SP-B2 IMPLEMENTER — THE LEG SUPPLY, the second member of the lighting road. NORMATIVE spec: docs/DESIGN_FP_ARCH_SP.md's SP-B2 section (find by symbol) + the parent block #4. SP-B is landed (read its commits first: git log). THIS WAVE IS CQ5-FREE (no flag — it rides SP-B's flags; no-bands-by-design, a supply shim). THE LANDMINE, PRE-MEASURED AND RULED (the doc-as-writer spelling-drift class): the SP volume spells the third leg routePositionBand at BOTH its spec sites, but the ONLY consumer reads routeBand — sovereigntyAppraisal.js consumes wordOf(row.routeBand, SOVEREIGNTY_ROUTE_BANDS) and beliefLegsOf's own return typedef at sovereigntyMarketStage.js already says routeBand. THE CONSUMER TYPEDEF IS THE AUTHORITY: supply the four legs as {tierBand, storesBand, routeBand, trajectoryBand} read off sovereigntyAppraisal.js's own shape; land a pin asserting Object.keys(suppliedLegs) is a subset of that exact set PLUS one executed key-rename mutant proving the pin reds; CORRECT the SP volume's doc at BOTH sites in your landing diff (find by content — line addresses shifted at the fold), reported in the commit body as a doc correction, never silent. FILES-TOUCHED includes the THIRD file the doc omits: tests/domain/sovereigntyWaveCloseIntegration.test.js carries frozen leg fixtures (find by content near its leg rows) — budget and update it coherently. THE SEAM IS ALREADY BUILT AND WAITING: sovereigntyMarketStage.js binds beliefLegsFor = beliefLegsOf and applies it — you WIDEN beliefLegsOf to supply real legs from SP-B's fields; conditionsBands now exists (SP-B landed it). AND BUILD THE LIGHTING INSTRUMENT: the certification lighting-order row for sovereigntyTradeEnabled is a PHANTOM (five doc homes state the three-member condition SP-B + SP-B2 + ES-4; NO artifact can flip green). Build the REAL instrument: follow the tradeConvergenceContract.js requiresForeignFlags PRECEDENT (its TR-3 row already requires believedScarcityEnabled) — a contract/certification row for the sovereigntyTradeEnabled LIGHTING condition that names its three wave evidences (SP-B's flags manifested + this wave's leg-supply pin + ES-4's evidence file, absent today), reads as UNSATISFIED-BUT-TRACKED while ES-4 is unbuilt (it must NOT red the base suite — measure the existing pending/unsatisfied semantics and use them), and FLIPS satisfiable the commit ES-4 lands. Pin both states: unsatisfied today (executed), and a simulated-evidence mutant showing it CAN flip (executed, restored). Wave-end attribution protocol per the LAWS. RETURN JSON: commits, gates, docCorrections (the two routeBand sites, quoted before/after), instrument (where it lives, both pinned states), stopReports, notes.`, { label: 'build:SP-B2', phase: 'SP-B2', model: 'opus' })

  phase('Verify B2')
  vspb2 = await agent(LAWS + `

YOU ARE THE ADVERSARIAL VERIFIER FOR SP-B2. The implementer reported: ` + JSON.stringify(spb2) + `

VERIFY BY EXECUTION in git-archive trees: (1) THE LEG SUPPLY IS ALIVE — drive the real sovereignty appraisal over a world where SP-B's fields exist and confirm the appraisal returns known legs (not known:false) with the four consumer-typedef keys; then the KEY-RENAME MUTANT: rename one supplied key in the archive and confirm the subset pin reds BY NAME; (2) the doc corrections — both routePositionBand sites now read routeBand, zero routePositionBand survivors anywhere in docs/ (a grep that can fail: show it failing on the pre-wave archive); (3) THE INSTRUMENT — at HEAD it is UNSATISFIED-BUT-TRACKED and the whole suite is green modulo the known base; the simulated-ES-4-evidence mutant flips it satisfiable (executed, restored cmp-exact); a delete-the-row mutant reds whatever totality walker governs it; (4) the third-file fixtures updated coherently (its tests green, and the frozen legs match the consumer typedef); (5) whole tests/lint at HEAD vs the wave base: failing-row identity diff EMPTY both directions except attributed deltas; strict 1313; byte-scans; commit hygiene. Demonstrate two instruments failing on deliberate mutations. RETURN structured JSON only.`, { label: 'verify:SP-B2', phase: 'Verify B2', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })
} else {
  log('SP-B final verifier rejected — returning for the chair without starting SP-B2.')
}

return { spb, vspb, repairB, recheckB, settleB, finalB, spb2, vspb2 }