export const meta = {
  name: 'build-cycle-38-v4-completion',
  description: 'V4 pieces 2+3 + the owner-approved V4.1 counsel pass — the war arrow finishes',
  phases: [{ title: 'Build', detail: 'one nav lane, serial commits, implement + verify with eye pass' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits with THE STAGED-SET LAW (git diff --cached --name-only before EVERY commit; git commit -- <paths>); no stash; focused vitest + gate-tail (ps aux | grep vitest first); python byte-scan every Write; npm run build + smoke:boot; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY: WZ-3 live on worldPulse — fenced. THE SPEC IS docs/DESIGN_RIBBON_V4_SPEC.md IN THE REPO (read whole, incl. the cumulative-audit addendum: the bottom underline RETIRES, never duplicates; semibold 600 labels persist; brightened-cell active grammar survives). V4 piece 1 landed @ 898d1c0b (cedar, gilded wordmark, bole, gilded seal-o, plate retired) — build on it; its STOP 2 carries the full mirror analysis including the SEAT sign flip and the FRAME deletion.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: OWNER-DIRECTED implementation. ${P10}
LANE V4B — THE ARROW FINISHES, serial commits:
(1) THE MIRROR (spec §c + V4's STOP 2 analysis, executed): edges run (x0,0)→(x0−RUN,BAND) — vane(), lower/lead/trail, barbBuckets, frayHairs, sheenBand all flip; SEAT → −lean(D/2) with its pin's sign; the FRAME clip and the lane-0/lane-2 overshoots DELETE so all four slashes are identical parallels (the squared-ends test retires, replaced by a four-identical-slashes pin); the edge-light relaxes 0.62→0.50 (mirrored rims genuinely face the light; the leading>trailing pin survives); THE WRAP-CORNER QUESTION resolves by SCREENSHOT as V4 proposed: try the quill-line-corner alignment (~3.083% of band width painting 2-3px over the corner) and if the Create label collides (its ~6px slack), pull the wrap to the band's outer edge instead — screenshot both, pick by eye, pin the winner; THE INDICATOR: the GILT_LIGHT quill-line bar (y 0..2.5, slant-cut ends, ~0.5px sheen above) REPLACES the bottom underline entirely (the audit addendum's pin: NO active paint below the vane).
(2) THE PHOTOREAL TEXTURE (spec part 2, whole): splits with calm-zone re-roll (⚠️ V4's note: the split hairline #6A5F4E is LIGHTER than SHEEN_LIFT — pin it OUT of every calm zone or the label-floor claim breaks); the one-curve law (bow constants x−0.28·RUN, 0.62·BAND, ONE writer); fray exposed-edges-only; growth lines seed 11 + pore flecks seed 13 (the grain retone already landed); thread-true whipping at 2.6px turns with tie-offs on the oxblood trio; the 1x/retina thresholds (retina cues each <2% effective ink); FletchBand has 361 effective lines of room.
(3) THE COUNSEL PASS (task #105, owner-approved — read its FULL description + metadata): R1 bole-as-scorch (bleed off the bar, anisotropic displacement along grain, singe halo, AA core untouched — plaque glance-test FIRST on the landed bole); R2 seal inside the type's advance width + build-time kerning check + the SEAL SIZE LADDER from the metadata (dimple at ribbon size; full device impression ≥28px; verifier checks all three registers); R3 quill-line glance test (→3px + keyline if progress-bar); R4 the whisper RACHIS hairline on the band's LOWER CUT only; R5 whipping visible at 1x (pin the RENDERED crest tone); R6 the texture-complete law recorded in theme.js + a coverage ratchet; R7 THE NOCK build-and-show (12-20px horn cross-nock at the bar's right end, paint-only; screenshot WITH and WITHOUT; one-commit revert ready); R8 the identity-extension law recorded.
Screenshots at 100%/200% every commit, honest eye verdicts. Return {lane:'V4B', commits, gates, stopReports, notes}.`,
  {label:'v4b:arrow-finishes', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier with an EYE PASS. Read docs/DESIGN_RIBBON_V4_SPEC.md + task #105's description whole. ${P10}
Report: ${JSON.stringify(r)}. JUDGE from served screenshots at 100%/200%: (a) the mirror + four identical parallels (measure the rendered edges — one shared slant, ends included); (b) FEATHERS AT LAST — splits, bowed barbs, fray: do the cells read as real dark feathers with satin sheen (not slabs, not corrugation)? (c) the bole reads SCORCH not plaque (the glance test); (d) the whipping reads wound thread AND exists at 1x; (e) the quill line reads active-state ON the shaft, and NO paint survives below the vane (grep + pixels); (f) the seal's three registers (dimple / device ≥28px / favicon); (g) the nock WITH/WITHOUT screenshots delivered for the owner. MECHANICAL: re-run the counsel's own tests (plaque glance, dark-hero find-the-nav with the R4 hairline, 1x whipping visibility); the split-hairline calm-zone exclusion mutant; every rider's composited AA at correct polarity (quote); SEAT sign pin; retina cues <2% ink measured; two-load determinism; focus rings by Tab; ANCHOR + fonts unchanged; semibold 600 persists; build + smoke:boot; staged-set + pathspec vs WZ-3. Return {lane:'V4B verify', verdict, findings}.`,
  {label:'v4b:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }