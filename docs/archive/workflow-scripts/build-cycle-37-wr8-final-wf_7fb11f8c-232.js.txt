export const meta = {
  name: 'build-cycle-37-wr8-final',
  description: 'WZ-3 — the last line: the lint pair ruled, the license spread lands, observer-axis + deterrence + casus, the WD debt — WR-8 closes',
  phases: [{ title: 'Build', detail: 'one war lane, serial, implement + verify' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits with THE STAGED-SET LAW (git diff --cached --name-only before EVERY commit; git commit -- <paths>); no stash; focused vitest + gate-tail (ps aux | grep vitest first) INCLUDING tests/domain registration walkers; python byte-scan every Write; npm run build + smoke:boot; dormancy goldens BYTE-IDENTICAL dark; STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY: the V4 ribbon repaint holds nav/brand/theme (src/components/theme.js is DIRTY with its WIP — never touch); the design counsel is read-only. Your territory: worldPulse, its tests, the E-H walker records, queue docs.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE WZ-3 — THE LAST LINE OF WR-8, under CHAIR RULING CR-PK-1 (vetoable, cite it):
(1) THE LINT PAIR: pulseKernel.js's two no-useless-assignment errors are ruled LOAD-BEARING-OR-FALSE-POSITIVE per WZ-2's own analysis (the reasonCoalitionEvidence initializer at :2583 is READ unconditionally at :2674 on every peace-dark path — dropping it is the unsafe cure). THE RULING'S ORDER OF PREFERENCE: (a) restructure each flagged site to make the dataflow explicit (e.g. a single conditional expression) ONLY IF the same-seed pipeline hash is BYTE-IDENTICAL before/after (the golden-corpus method; pulseKernel's PRNG call order is the stream identity — if the restructure touches any RNG-adjacent flow, abort to (b)); else (b) an inline eslint-disable-next-line no-useless-assignment with the load-bearing justification quoted from WZ-2's analysis (a documented false positive is honest; a bypassed hook is not). Either way the file becomes stageable. One commit for the pair's resolution, with the ruling + before/after hash quoted.
(2) THE SPREAD LANDS: the license worldStatePatch spread at pulseKernel.js:916 — the ON-path re-seat inside warLayerEnabled, NEVER the :1100 wind-down occurrence (WZ-2's pin already forbids it forever); pulseKernel stays 1580 net-zero on the ratchet's own engine (measure, quote); the UNWIRED LEDGER entry in razingExecution.js's header retires with a pointer to this commit.
(3) DISPATCH ITEM 4, now reachable: the observer-axis relationship hits (the world judges on the observer's alignment), WR-6's believed-retaliation deterrence CONSUMED (grep-proven: no rebuilt web), the receipts' emission at the mouth, and the atrocity casus under CR-WR8-H — believed razings derived from EXISTING news/receipt read models ONLY (any new persisted belief surface → STOP, owner-gated). advanceWarReasons gains its razings argument from that derivation; scoreAtrocityAnswer finally has a caller.
(4) ITEM 5: the five WD leaves (warArmyRecord, warCapacityReads, warCoalitionRefusal, warHomeCosts, warSiegeVerdict) get REAL lit-coverage pins satisfying E-H honestly (drive each through its lit path); the false E-H reason claim corrected at the record; mechanismLitCoverage's five WD rows clear.
(5) IF ALL LANDS: the queue row declares WR-8 CLOSED, enumerating what closed it and what remains owner-gated (K1 severity carry, seat ransom, ransom persistence, counterpart re-mint — parked, recorded, not owed by the wave). Return {lane:'WZ-3', commits, gates, stopReports, notes}.`,
  {label:'wz3:last-line', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. THE BAR: (a) CR-PK-1's cure — if restructured, re-run the same-seed hash YOURSELF (byte-identical or the commit is invalid); if disabled, the justification must quote the load-bearing path and a mutant deleting the initializer must break the peace-dark fixture (run it); (b) the spread at :916 — execute a FULL lit razing arc through the REAL pulse: victor razes → no occupation → sack conserved to the person → license MINTED into worldState → consumed ONCE on a vengeance war → refused twice → the wind-down path still spread-free; pulseKernel 1580 on the ratchet engine; (c) dormancy re-hashed byte-identical dark across all 34 fixtures; (d) the casus: a believed razing (news-derived) raises atrocity_answer in advanceWarReasons on a real fixture, and grep proves ZERO new persisted surfaces; (e) the observer-axis hits vary by observer alignment (two observers, two verdicts, one razing — execute it); (f) the deterrence read consumes WR-6 (import census); (g) all five WD lit pins genuinely drive their modules lit (read two); mechanismLitCoverage's WD rows gone; (h) stage-neuter on every new branch; strict flat; build + smoke:boot; staged-set law + pathspec (theme.js untouched). If EVERYTHING holds, say the words: WR-8 IS CLOSED. Return {lane:'WZ-3 verify', verdict, findings}.`,
  {label:'wz3:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }