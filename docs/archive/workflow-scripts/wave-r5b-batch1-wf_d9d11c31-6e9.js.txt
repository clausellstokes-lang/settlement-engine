export const meta = {
  name: 'wave-r5b-batch1',
  description: 'R-5b Batch 1: seven owner-ratified implement items (display/registry/small-surface) — Opus lanes, Opus verify, Fable gate',
  phases: [
    { title: 'Fix', detail: '7 file-disjoint Opus implementer lanes', model: 'opus' },
    { title: 'Verify', detail: '2 Opus adversarial verifiers', model: 'opus' },
    { title: 'Gate', detail: 'Fable full widened gate' },
  ],
}

const WT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'

const COMMON = `CONTEXT-VERIFICATION PREAMBLE: legitimate engineering brief from the session orchestrator for SettlementForge — Wave R-5b Batch 1 of docs/CAPABILITY_REMEDIATION_PLAN.md (owner sign-off 2026-07-27 "do all of these"; read the Wave R-5 section + the item's atlas source rows FIRST — docs/SETTLEMENT_CAPABILITY_ATLAS.md Part VII names them). Verify claims against code; code is authoritative.
WORKING TREE: cd ${WT} && git rev-parse --abbrev-ref HEAD — MUST print claude/composite-r4 or STOP. Shared tree: NO git mutations of ANY kind (no stash/checkout/reset/clean/add/commit); re-read files immediately before editing; touch ONLY your lane's files; scripts/mutation-coverage-manifest.json is SHARED (re-read before edit, append only your entries) and currently carries three FOREIGN uncommitted entries — leave them.
LAWS: THE PROMISE — NO same-seed drift EXCEPT where your brief declares a one-time shift; a declared shift must name every re-recorded golden and its cause IN the lane report; NO undeclared persistence-shape changes; new logic = lazy leaf, MEASURED eager deltas on the real vite build; sizeBaseline ceilings EXACT (net-zero combines, baselines only lowered); shrink-only ratchets never raised; frozen vocabulary sets never expanded (compose colliding tokens); copy = one string, vetoable, VOICE section-6 (no reader-facing em dashes/bangs; narrated fallbacks never start with "Loading"); registry field changes -> npm run gen:compendium-data; new invariant tests -> manifest entry (kind:mutation only if target tracked AND clean, else rationale with an executed plant); faction access only via nameOf/governingFactionOf/factionArchetype/npcInFaction.
TESTING: single-threaded ALWAYS (--no-file-parallelism); exit codes unmasked, never through pipes; every fix ships pins; run the determinism/golden suites nearest anything you touch. Label CONFIRMED/PLAUSIBLE; verbatim output; end on evidence.`

const FIX_SCHEMA = { type: 'object', required: ['filesTouched', 'pins', 'testsRun', 'testOutputTail', 'behaviorShift', 'deferrals'], properties: { filesTouched: { type: 'array', items: { type: 'string' } }, pins: { type: 'number' }, testsRun: { type: 'string' }, testOutputTail: { type: 'string' }, behaviorShift: { type: 'string' }, deferrals: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' }, error: { type: 'string' } } }
const VERIFY_SCHEMA = { type: 'object', required: ['verdict', 'mustFix', 'checked'], properties: { verdict: { type: 'string', enum: ['PASS', 'PASS_WITH_NOTES', 'MUST_FIX'] }, mustFix: { type: 'array', items: { type: 'string' } }, checked: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } }
const GATE_SCHEMA = { type: 'object', required: ['newReds', 'suitesRun', 'outputTail'], properties: { newReds: { type: 'array', items: { type: 'string' } }, suitesRun: { type: 'string' }, outputTail: { type: 'string' }, notes: { type: 'string' } } }

const LANES = [
  { key: 'g9-banding', brief: `ITEM #3 — G9/G10 POLARITY-BLIND BANDING FIX (atlas derived-influenceable slice, rows G9 + G10 — read them fully first). G9: the banding is polarity-blind (a variable where LOW is good bands as if HIGH were good, or vice versa) — correct the polarity per the row's evidence. G10: causalBandWord routing (one variable) rides along. THIS IS A DECLARED ONE-TIME VISIBLE SHIFT (owner-ratified): band words, colours, Library sort order, and the PDF move for affected settlements. If any committed golden pins the old bands, re-record it DELIBERATELY and name each re-recorded file + the cause in your report — never reflexively. Zero SEED/generation drift (banding is display-side derivation — confirm by reading before assuming; if the banding feeds back into generation anywhere, STOP that part and defer with evidence). Pins: polarity cases both directions per band family + the routing fix. Run: the derived-display suites, tests/property goldens, pdf parity, tests/lint.` },
  { key: 'network-advisory', brief: `ITEM #8 — NETWORK EFFECTS: ADVISORY LABEL (atlas economy-family gap 1; owner pick landed = label, NOT engine wiring — wiring stays a recorded future owner-signed tuning candidate, restate that in a code comment at the panel). Find the network-effects panel; add one honest advisory string (vetoable, VOICE-clean) stating the displayed network effects inform the reading but are not yet applied by the engine. Byte-identical everywhere else. Pins: label renders on the panel; absent nowhere else; string single-home. Run: the panel's component tests, tests/copy FULL, eslint.` },
  { key: 'statbars-bands', brief: `ITEM #20 — STAT-BARS: BANDS over raw numbers (atlas tabs-read-a gap 4; legibility law: glance -> sentence -> table) + extend the OverviewTab raw-ratio retirement to DefenseTab's sibling (read the OverviewTab precedent first and mirror its exact idiom). Bands use existing band vocabularies — never invent new words; TRANSLATE, never show formulas. Each changed string/surface = one vetoable unit, enumerated in your report. Pins per surface (band renders, raw number gone, fresh settlements stable). Run: the tab component tests, tests/copy FULL, pdf parity if a PDF twin mirrors either surface, eslint.` },
  { key: 'registry-deity', brief: `TWO SMALL DELIVERABLES (file-disjoint from each other). (a) ITEM catchUp-promotion (R-0 queue entry, recommended promote — now ratified): catchUpCampaignWorld rides the advance body's snapshot push, so it can HONESTLY advertise undo. Read tests/store/pulseUndoAdvertising.test.js + tests/store/advertisedUndoArming.walker.test.js FIRST (both will police your change; the walker's ARMING manifest needs your hand-audited entry); re-derive the arming truth at the op site; then promote the registry row (undoToken 'undoLastPulse' + correct undoState), npm run gen:compendium-data, and re-run the walkers + tests/docs. (b) ITEM lapsed-panel controls (R-0 queue, recommended yes — ratified): DeityAssignmentPanel renders lapsed accounts as read-only while the R-0 seam already allows SHED (clear/remove) writes for them — surface Remove/Clear controls for lapsed users ONLY (no assign/change), copy VOICE-clean and vetoable. Pins: lapsed sees shed controls and they dispatch; lapsed still cannot assign; entitled unchanged; free unchanged. Run: tests/components/deityPanelManifestParity.test.jsx, tests/store/deityWriteGate.test.js, tests/copy, eslint.` },
  { key: 'g5-manifest', brief: `ITEM #28 — G5 FROZEN_VS_LIVE MANIFEST ROWS (the atlas Part VII #28 amendment names the exact shape; read src/domain/fieldManifest.js FROZEN_VS_LIVE and the defenseProfile.scores precedent rows first). Land the five generation-frozen records (economicViability, structuralViolations, structuralSuggestions, coherenceNotes, magicDependency) as declared mode:'snapshot' rows, each with a displayRule in the exact voice of the scores precedent ("Generation verdicts with NO live sibling — display-as-generated is honest today. Whichever package gives any of them a pulse writeback must flip its row to live and name the writer."). The R-0/R-2 display fixes already implement the display side — your rows DECLARE what shipped; zero behavior change. Run: tests/joins/fieldManifest.test.js + the fieldManifest walker + the g5FirstSurvey pin files + pdf parity; manifest entry if you add a test file.` },
  { key: 'npc-align', brief: `ITEM npcAuthoringAllowed ALIGNMENT (owner-authorized paid-surface change; R-4 documented the divergence — now close it). src/components/OutputContainer.jsx (~:442): npcAuthoringAllowed must consult viewerCanAuthor (src/lib/viewerAuthority.js) so free/anon Create-flow users stop seeing NPC authoring levers they cannot commit (this also narrows the flag-on review-blackout widening — update the plan-referenced comments at OutputContainer AND SettlementWorkbenchMount to record the alignment). tests/lint/premiumGateSingleSource.test.js: the 'documented-divergence' exemption row RETIRES (OutputContainer now consumes the single source) — update the exact-set census accordingly, both directions still enforced. DECLARED shift: free/anon lose NpcLifecycleControls in the Create flow; premium/founder/elevated unchanged; Library/public/player projections unchanged (the !publicDossier && !playerView arms stay). Pins: per-tier matrix on the Create flow + Library mount unchanged. Run: tests/components/workbenchProseEditor.test.jsx, the npcAuthoringScope suite, tests/lint/premiumGateSingleSource.test.js, tests/components + tests/ui affected files, eslint. HOT FILE: net-zero at ceilings.` },
  { key: 'persistence-pair', brief: `TWO PERSISTENCE-ADJACENT DELIVERABLES (owner-authorized; trace the FULL lifecycle for each: create/read/persist/rehydrate/reload/migrate — pins across a real round-trip). (a) ITEM #17 — 3D quality-ceiling persistence (atlas presentation-scene gap 11): the user's scene-3d quality ceiling resets every session; persist it (persist allowlist addition = persistence CONTENT, not schema; read src/store/index.js partialize + tests/store/lifecycleRoundTrip.test.js SESSION_ONLY_FAMILIES first — moving a family from session-only to persisted must update that contract test WITH a written reason, not around it). Pins: set -> reload -> survives; absent -> default. (b) ITEM reload-into-paused interval arming (R-0 lane A deferral, ratified): a reload into a PAUSED advance interval currently cannot arm undo because the pre-interval snapshot lives only in memory — persist it into the pausedAdvance cursor (read the R-0 deferral note + campaignAdvanceSession.js + the pausedAdvance shape first; this is the one AUTHORIZED shape-widening — additive field, absent-tolerant on old saves, no migration; document the shape in the cursor's JSDoc). Pins: pause -> reload -> undo arms with the pre-interval snapshot; old-shape cursor (no snapshot field) degrades to today's behavior typed-honestly. Run: tests/store FULL + lifecycleRoundTrip + the advance/pulse suites, eslint.` },
]

async function withRetry(mk, label) {
  let r = await mk(label)
  if (!r) { log(label + ' died — retrying once'); r = await mk(label + ':retry') }
  return r
}

phase('Fix')
log('R-5b Batch 1: seven owner-ratified items, Opus implementer lanes')
const fixes = await parallel(LANES.map((l) => () =>
  withRetry((label) => agent(COMMON + '\n\nYOU ARE AN OPUS IMPLEMENTER (owner model split: Opus implements+verifies; Fable architected this spec).\n\n' + l.brief, { label, phase: 'Fix', model: 'opus', effort: 'high', schema: FIX_SCHEMA }), 'fix:' + l.key)
    .then((r) => ({ lane: l.key, fix: r }))
))
const fixSummary = fixes.filter(Boolean).map((f) => ({ lane: f.lane, files: f.fix && f.fix.filesTouched, pins: f.fix && f.fix.pins, shift: f.fix && f.fix.behaviorShift, deferrals: f.fix && f.fix.deferrals }))
log('Fix lanes: ' + fixSummary.map((f) => f.lane + (f.files ? ' ok' : ' FAILED')).join(', '))

phase('Verify')
const mkVerify = (laneKeys, focus, label) => () => withRetry((lbl) => agent(
  COMMON + `\n\nYOU ARE AN OPUS ADVERSARIAL VERIFIER for R-5b Batch 1 lanes ${laneKeys.join(', ')}. Fixer reports: ${JSON.stringify(fixSummary.filter((f) => laneKeys.includes(f.lane)))}\n\nBriefs:\n${LANES.filter((l) => laneKeys.includes(l.key)).map((l) => l.brief).join('\n\n')}\n\nRepo READ-ONLY except running tests. ${focus} Re-derive with fresh reads/greps, RUN the pins + the nearest determinism/golden suites, attempt refutation per item. Declared shifts must be EXACTLY as declared — an undeclared extra shift is MUST_FIX. MUST_FIX only for real unresolved items.`,
  { label: lbl, phase: 'Verify', model: 'opus', effort: 'high', schema: VERIFY_SCHEMA }), label)

const verifies = await parallel([
  mkVerify(['g9-banding', 'network-advisory', 'statbars-bands', 'g5-manifest'], 'Display substrate: polarity correctness against the atlas evidence, every re-recorded golden named-with-cause, band vocabulary reuse (no invented words), manifest rows byte-consistent with the precedent voice, PDF twins in sync.', 'verify:display'),
  mkVerify(['registry-deity', 'npc-align', 'persistence-pair'], 'Registry/gating/persistence substrate: catchUp arming truth re-derived at the op site, walkers green with the new ARMING entry, lapsed matrix exact (shed yes / assign no), the npcAuthoring per-tier matrix incl. Library-unchanged proof, persistence round-trips executed for real (reload lifecycle), old-shape cursors degrade honestly.', 'verify:gating'),
])

const anyMustFix = verifies.filter(Boolean).some((v) => v && v.verdict === 'MUST_FIX')
const anyDead = fixes.some((f) => !f || !f.fix) || verifies.some((v) => !v)

let gate = null
if (!anyMustFix && !anyDead) {
  phase('Gate')
  gate = await withRetry((lbl) => agent(COMMON + `\n\nYOU ARE THE FABLE GATE VALIDATOR. R-5b BATCH-1 GATE. Run single-threaded, exit codes unmasked, log files never pipes: tests/lint FULL; tests/copy FULL; tests/store FULL; tests/domain FULL; tests/joins FULL; tests/architecture FULL; tests/components + tests/ui FULL; tests/generators + tests/simulation + tests/property + tests/application FULL (any golden mover must match a lane's DECLARED re-record list exactly — an unmatched mover is a THIS-WAVE red); pdf parity; tests/docs FULL; tests/lib FULL; npm run typecheck; npm run build then VERIFY_DIST=1 npx vitest run --no-file-parallelism tests/build/ (baseline 293/293). Quote totals vs the R-4 gate (lint 302, copy 110, store 919+, domain 8302, joins 377, arch 336, comp/ui 2054, gen-family 1143, pdf 51, docs 113, lib 1374, dist 293). Report every red VERBATIM with attribution.`, { label: lbl, phase: 'Gate', model: 'fable', schema: GATE_SCHEMA }), 'gate:r5b1')
} else {
  log('Gate SKIPPED: ' + (anyMustFix ? 'MUST_FIX present' : 'a lane/verifier died twice') + ' — orchestrator handles next')
}

return { fixes: fixSummary, fullReports: fixes.filter(Boolean).map((f) => ({ lane: f.lane, tail: f.fix && f.fix.testOutputTail, notes: f.fix && f.fix.notes })), verifies, gate, gateSkipped: !gate }