export const meta = {
  name: 'hazard-registry-and-premortem',
  description: 'Convert the hazard estate from documents into machinery: a gate-enforced registry with a status per class, and a pre-mortem that warns BEFORE a wave, validated against the commits that already tripped each class.',
  phases: [
    { title: 'Census', detail: 'enumerate the real hazard classes and their true conversion status' },
    { title: 'Registry', detail: 'the machine-readable registry and its gate-enforced walker' },
    { title: 'Premortem', detail: 'the before-the-error instrument, validated against history' },
    { title: 'Verify', detail: 'adversarial: is any of this vacuous' },
  ],
}

const TREE = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const MEM = '/Users/cstokes/.claude/projects/-Users-cstokes-Desktop-settlement-engine/memory'

const LAWS = `VERIFY-FIRST PREAMBLE: chair brief, owner-delegated build program. LIVE CODE OUTRANKS EVERY TABLE. Build tree ${TREE} (branch claude/composite-r4), HEAD \`bd5e49f6\`, tree clean. Hard-gate every state-mutating compound with the \`cd\` FIRST.

⭐ \`sh scripts/gate-mutex.sh --wait && sh scripts/gate-tail.sh <cmd>\` — ONE \`&&\` chain.
⛔⛔ **NEVER READ AN EXIT CODE THROUGH A PIPE, AND NEVER LET A PIPELINE'S LAST COMMAND BE THE ONE YOU READ.** This class fired FOUR times in one day, including twice by the chair. Capture the status into a variable immediately, or use \`gate-tail.sh\`.
⚠ **EVERY FIGURE IN AN INTEGRITY-COUNTED \`git archive\` OF A COMMITTED SHA.** One at a time; \`rm -f\` the node_modules symlink before deleting; disk ~11 GiB.
⚠ The memory estate lives at ${MEM} — **406 files, 191 carrying a "How to apply" cure.** It is READ-ONLY to you: do not edit it. The repo is the deliverable.

REPORTING: CONFIRMED (executed evidence quoted) or PLAUSIBLE.`

const WHY = `
**WHY THIS EXISTS — THE OWNER'S OBSERVATION, WHICH IS CORRECT.** Errors keep recurring, so the recorded cures are not working. Diagnosis: **a recorded hazard is a DOCUMENT, and a document requires someone to read it at exactly the right moment while doing something else.** Proof from a single day: the config-slot trap was recorded WITH its exact cure and a lane walked into it; the piped-exit-code hazard is recorded and the chair hit it twice; the edge-bundle law is recorded and the chair broke it by reading "touching" too narrowly.

⭐ **THE RULE THIS BUILDS: every confirmed hazard class either becomes MACHINERY, or is EXPLICITLY ACCEPTED as a document with a stated reason. Documents are currently the default; that inverts the actual value.**
⚠⚠ **AND THE TRAP TO AVOID: that rule is itself currently a document.** If this round produces a registry nobody validates, it has committed the exact defect it diagnoses. **The registry's integrity must be GATE-ENFORCED, not asserted.**`

phase('Census')
const census = await agent(LAWS + WHY + `

YOU ARE THE HAZARD CENSUS. **READ-ONLY — author no repo file.** Produce the honest inventory the registry will be built from, and STOP.

**WHAT COUNTS AS A CLASS.** A hazard class is a **defect SHAPE that has bitten at least once and has a cure.** Not every memory file is one — many are wave records, directives or program state. **Do not pad the count**; a registry of 150 entries where 60 are not classes is worse than one of 60 that are, because the padding is where staleness hides.

**FOR EACH CLASS, ESTABLISH — BY MEASUREMENT, NOT BY READING THE MEMORY'S OWN CLAIM:**
- \`id\`, \`title\`, and the memory file(s) that hold it.
- \`status\`, and this is the load-bearing field. Use exactly four:
  - **MACHINERY** — a real enforcer FILE EXISTS and the class cannot recur silently. ⚠ **VERIFY THE FILE EXISTS AND THE ENFORCER ACTUALLY RUNS.** A class claiming machinery whose enforcer is absent, or is a test nobody runs, is NOT machinery — it is the worst kind of document, one that looks solved.
  - **PARTIAL** — ⭐ **the worst real state, and name it explicitly: a cure EXISTS but is OPTIONAL.** \`scripts/gate-tail.sh\` completely solves the piped-exit-code class and the chair bypassed it by hand-rolling a pipeline. A cure you can route around is a document wearing a tool's clothes. Expect several of these.
  - **DOCUMENT** — no enforcer, no stated acceptance. This is the OWED pile.
  - **ACCEPTED** — deliberately left as a document, WITH a written reason (some classes genuinely cannot be mechanised).
- \`enforcer\` — path, and whether it is reachable from the \`npm run check\` chain or runs only on demand. **Being in the chain matters**: an on-demand enforcer is closer to PARTIAL than to MACHINERY, and say so.
- \`instances\` — ⭐⭐ **THE METRIC THAT ANSWERS THE OWNER'S QUESTION. Count the recorded occurrences of each class, with shas where recoverable.** The first instance of a class is unpreventable; the question is whether the SECOND was prevented. **A class with one instance and a walker is CLOSED. A class with four instances and no walker is the treadmill.** Rank the output by instance count descending — that ranking IS the build order.
- \`triggers\` — what shape of change exposes this class, in terms a script could evaluate over a changeset (path globs, "renames any file", "adds a new file under src/", "touches a corpus builder"). Be concrete; the pre-mortem consumes these.

**ALSO REPORT, PLAINLY:** how many classes exist, the split across the four statuses, and the total instance count carried by DOCUMENT-status classes — that last number is the honest size of the treadmill.

RETURN JSON with the full class list.`, { label: 'hazard:census', phase: 'Census', model: 'opus', effort: 'high', schema: { type: 'object', required: ['classes'], properties: { totalClasses: { type: 'number' }, statusSplit: { type: 'object', additionalProperties: true }, treadmillInstanceCount: { type: 'number' }, classes: { type: 'array', items: { type: 'object', additionalProperties: true } }, notes: { type: 'string' } } } })

phase('Registry')
const registry = await agent(LAWS + WHY + `

YOU ARE THE REGISTRY LANE. Turn the census into gate-enforced machinery.

CENSUS: ` + JSON.stringify(census || {}).slice(0, 30000) + `
⚠ Verify its status claims yourself on a sample — a census that marked a class MACHINERY whose enforcer does not exist would poison everything downstream.

**BUILD:**
1. **\`scripts/hazard-registry.json\`** — the machine-readable registry, seeded from the census. One entry per class with the fields above. ⚠ **This file is a DERIVED-FROM-MEASUREMENT artifact on its first write and a HAND-MAINTAINED one thereafter** — say so in its own header block, because that transition is exactly where such files rot.
2. **\`scripts/check-hazard-registry.mjs\`** — the walker that makes the rule real. It must assert:
   - every **MACHINERY** entry names an enforcer path that **EXISTS ON DISK** (and say whether it is in the check chain);
   - every **ACCEPTED** entry carries a non-empty stated reason — an acceptance without a reason is a silent surrender and must RED;
   - every entry has a status from the closed set — **a new class added with no status REDS**, which is the arm that keeps the registry alive;
   - the **DOCUMENT** count is **SHRINK-ONLY** against a frozen baseline. That is the ratchet on the treadmill itself: the owed pile can only get smaller.
   - ⚠ **ANTI-VACUITY SENTINEL** (carry the pattern from \`scripts/check-full-typecheck.mjs\`, read it): an empty or unparseable registry must FAIL CLOSED, never read as "no violations". A registry of zero classes passing every assertion is the exact failure this whole round exists to prevent.
   - ⚠ **SCOPE SENTINEL**: if the class count collapses below the frozen floor, fail loudly.
3. **Wire it into the \`npm run check\` chain**, and add a **fail-closed meta-test** under \`tests/lint/\` that exercises the failure paths with an injected bad registry via an env seam.

⚠ **A NEW TEST FILE OWES**: a \`scripts/mutation-coverage-manifest.json\` row (read the conventions; ONE label per entry, a file's second invariant goes under \`meta:\`), and it moves the lighting census — re-measure that off an archive of your ACTUAL PARENT, and if you defer the re-record, say so with a reason.
⚠ Byte-scan every file you author: a \`\\u0000\` spelling becomes a RAW NUL, which has bitten seven times.

**PROVE IT WITH MUTANTS**, each verified to have changed a CODE line before it runs: delete an enforcer file named by a MACHINERY entry → RED; strip an ACCEPTED entry's reason → RED; add a class with no status → RED; grow the DOCUMENT count → RED; hand it an empty registry → FAIL CLOSED, not green.

RETURN JSON.`, { label: 'hazard:registry', phase: 'Registry', model: 'opus', effort: 'high', schema: { type: 'object', required: ['landed'], properties: { landed: { type: 'boolean' }, registryPath: { type: 'string' }, classCount: { type: 'number' }, documentBaseline: { type: 'number' }, inGateChain: { type: 'boolean' }, metaTestPath: { type: 'string' }, mutantsKilled: { type: 'array', items: { type: 'string' } }, manifestRowAdded: { type: 'boolean' }, lightingCensus: { type: 'string' }, commits: { type: 'array', items: { type: 'string' } }, stopReports: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Premortem')
const premortem = await agent(LAWS + WHY + `

YOU ARE THE PRE-MORTEM LANE. **This is the only instrument in the whole estate that runs BEFORE the error instead of after it.**

Registry landed: ` + JSON.stringify(registry || {}).slice(0, 4000) + `

**BUILD \`scripts/premortem.mjs\`** — given a changeset, print the recorded hazard classes that shape exposes, each with its cure. Input modes: the staged set (default), a diff range, or an explicit file list. Wire an \`npm run premortem\` script.

**DERIVE THE TRIGGERS; DO NOT HAND-LIST THEM.** This instrument will be judged by whether it rots, and hand-restated derivables go stale — that is itself one of the recorded classes.
- "touches a bundled edge input" → read the actual input lists out of the **bundle meta files**, never a transcribed list.
- "touches a file at its size ceiling" → read \`scripts/.size-baseline.json\`.
- "adds a new un-baselined source file" → read the ratchet baselines.
- "renames anything" → \`git diff --name-status\` R-records. ⚠ A pure rename is the trigger that bit the chair: it changes no bytes but breaks bundle metas that record input PATHS.
Hand-author a trigger only where nothing derivable exists, and **report how many triggers are derived vs authored** — that ratio predicts the instrument's shelf life.

⭐⭐ **THE ACCEPTANCE TEST IS NON-NEGOTIABLE, AND IT IS THE SAME SHAPE THAT MADE THE READER-WITHOUT-A-WRITER WALKER REAL: RUN IT AGAINST THE CHANGESETS OF COMMITS THAT ALREADY TRIPPED A KNOWN CLASS, AND PROVE IT WOULD HAVE WARNED.** Known ground truth, all on this branch:
- \`67f8a58e\` renamed \`src/generators/hookThemes.js\` → \`src/domain/\` and broke three edge-bundle tests → **must warn: bundled-input path change owes a rebuild.**
- \`67f8a58e\` also added two new un-baselined source files → **must warn: zero-ceiling ratchet.**
- The TCD-1 pin corpus commit used a bare number in a config slot → **must warn: config-slot silent key trap** (if that class's trigger is expressible).
- Any commit adding a \`tests/\` file → **must warn: lighting census + mutation-manifest row.**
**A pre-mortem that cannot retro-warn on commits we KNOW tripped these is decoration. Report the hit rate honestly, including the misses.**

⚠ **ADVISORY, NOT BLOCKING.** Trigger predicates are heuristic and will false-positive; a noisy blocker gets disabled, which is worse than no instrument. Print loudly, exit 0.
⭐ **BUT LOG EVERY FIRING** to a file with the sha and the classes named — without that we can never measure whether it warned before the classes that actually bit, and an instrument whose value cannot be measured will be dropped in a month.

RETURN JSON.`, { label: 'hazard:premortem', phase: 'Premortem', model: 'opus', effort: 'high', schema: { type: 'object', required: ['landed'], properties: { landed: { type: 'boolean' }, scriptPath: { type: 'string' }, triggersDerived: { type: 'number' }, triggersAuthored: { type: 'number' }, retroWarnResults: { type: 'array', items: { type: 'string' } }, retroHitRate: { type: 'string' }, misses: { type: 'array', items: { type: 'string' } }, logPath: { type: 'string' }, commits: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

phase('Verify')
const verdict = await agent(LAWS + WHY + `

YOU ARE THE ADVERSARIAL VERIFIER. Default posture: WRONG until executed evidence. REFUTE. **READ-ONLY in the shared tree**; mutants in throwaway archives. You hold the gate slot alone.

Census: ` + JSON.stringify(census || {}).slice(0, 8000) + `
Registry: ` + JSON.stringify(registry || {}).slice(0, 5000) + `
Pre-mortem: ` + JSON.stringify(premortem || {}).slice(0, 5000) + `

**THE CENTRAL QUESTION: is any of this vacuous?** This round exists because documents do not prevent recurrence. If it has produced two more documents, it has failed on its own terms and you should say so.

1. **SAMPLE THE STATUS CLAIMS.** Take at least eight entries marked MACHINERY and confirm the named enforcer EXISTS, RUNS, and would actually red on its class. A single fabricated MACHINERY status makes the registry worse than nothing, because it converts an open problem into a solved-looking one.
2. **MUTATE THE REGISTRY WALKER YOURSELF**: delete an enforcer file; strip an ACCEPTED reason; add a statusless class; grow the DOCUMENT count; feed it an empty registry. Confirm RED, RED, RED, RED, FAIL-CLOSED. Do not trust the lane's mutant list.
3. **RETRO-TEST THE PRE-MORTEM ON A COMMIT THE LANE DID NOT USE.** Pick your own historical commit that tripped a recorded class, run the pre-mortem against its changeset, and report whether it warned. The lane's own ground truth is the weakest evidence available.
4. **CHECK THE INSTANCE COUNTS.** Spot-check several against git history. The instance metric is what tells the owner whether classes are closing or recurring — a fabricated count corrupts the one number that answers their question.
5. **RUN \`npm run check\`** stepwise at the final sha, status captured immediately, and report the first failing step by name or that all steps exit 0.
6. Confirm nothing was obtained by loosening: diff every baseline and census against the round's parent.

RETURN structured JSON only.`, { label: 'hazard:verify', phase: 'Verify', model: 'opus', effort: 'high', schema: { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['PASS', 'REJECT'] }, machineryClaimsSampled: { type: 'number' }, fabricatedStatuses: { type: 'array', items: { type: 'string' } }, walkerMutantsKilled: { type: 'string' }, independentRetroTest: { type: 'string' }, checkFirstFailingStep: { type: 'string' }, findings: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } } })

return { census, registry, premortem, verdict }