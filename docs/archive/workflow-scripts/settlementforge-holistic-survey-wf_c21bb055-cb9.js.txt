export const meta = {
  name: 'settlementforge-holistic-survey',
  description: 'Exhaustive Fable survey of the entire SettlementForge codebase: 18 subsystem readers + 10 dimension reviewers',
  phases: [
    { title: 'Subsystems', detail: '18 readers, one per code region' },
    { title: 'Dimensions', detail: '10 cross-cutting reviewers' },
  ],
}

const REPORT_SCHEMA = {
  type: 'object',
  required: ['slice', 'summary', 'grade', 'strengths', 'findings', 'product_notes', 'coverage_note'],
  properties: {
    slice: { type: 'string' },
    summary: { type: 'string', description: 'Holistic assessment of this slice as code, as product substance, and fit-to-goals. 300-500 words, complete sentences.' },
    grade: { type: 'string', description: 'Letter grade with +/- for this slice' },
    strengths: { type: 'array', items: { type: 'string' }, description: 'Genuine strengths, each one sentence with a concrete example' },
    findings: {
      type: 'array',
      maxItems: 20,
      items: {
        type: 'object',
        required: ['title', 'severity', 'category', 'evidence', 'why', 'fix_shape', 'confidence'],
        properties: {
          title: { type: 'string' },
          severity: { enum: ['critical', 'high', 'medium', 'low'] },
          category: { type: 'string', description: 'kebab-case, e.g. correctness, sim-logic-gap, lifecycle, determinism, security, perf, ux, immersion, dead-code, docs-drift' },
          file: { type: 'string', description: 'repo-relative path' },
          line: { type: 'integer' },
          evidence: { type: 'string', description: 'Quoted code or concrete observation proving it. Required - no evidence, no finding.' },
          why: { type: 'string', description: 'Why it matters for correctness/product/immersion' },
          fix_shape: { type: 'string', description: 'Proposed fix shape - bold-architecture allowed within the constitution' },
          confidence: { enum: ['confirmed', 'probable', 'speculative'] },
          known_deferred: { type: 'boolean', description: 'true if this is already in a deferral ledger - include only if the deferral itself looks unsound' },
        },
      },
    },
    product_notes: { type: 'array', items: { type: 'string' }, description: 'Observations about product goals/ambitions this slice reveals' },
    open_questions: { type: 'array', items: { type: 'string' } },
    coverage_note: { type: 'string', description: 'What you read fully, what you skimmed, what you did NOT read' },
  },
}

const PREAMBLE = `You are a Fable-tier surveyor doing an exhaustive, objective review of one slice of SettlementForge - a deterministic D&D settlement/world simulator (React 19 + Zustand 5 + Vite, JS with JSDoc types, Supabase backend), ~430k tracked LOC, solo builder, pre-launch. Repo root: /Users/cstokes/Desktop/settlement-engine (git branch review-fixes-2026-07-08, clean tree - READ ONLY, change nothing).

CONTEXT YOU MUST HOLD:
1. THE CONSTITUTION (docs/PHASE55_EXECUTION_PLAYBOOK.md section 0.2): (1) same-seed byte-identity - goldens byte-identical; (2) dormancy/additivity - every new layer gated, absent means prior bytes; (3) premium never touches generation; free/anon never see live deity names or DM truth; (4) endogeneity - the party observes, never feeds fit math; (5) first-paint budget ratchet (CLOSURE_BUDGET_BYTES in tests/build/vendorPdfLazy.test.js); (6) bold-over-safe WITHIN these laws. any-cast ceiling exactly 2252. Engine purity: no Date.now/Math.random in engine paths - seeded forks only.
2. PRODUCT BOUNDARIES (owner, binding): world-only simulation (party-facing content is DM territory); sub-century horizon; simplicity-over-fidelity (deity = alignment not domain); NEVER resolve a named character's fate (mortality is aggregate-only).
3. DEFERRAL LEDGERS - read these FIRST so you do not re-report deliberate deferrals as findings: docs/PHASE55_EXECUTION_PLAYBOOK.md sections 0.0.2 (KNOWN OPEN FINDINGS) + 0.6 + 0.8; docs/PHASE55_ROUND21_BACKLOG_PLAN.md (Progress, Deferred, owner-decision queue); docs/REVIEW_FINDINGS.md if present. If a deferred item's deferral looks UNSOUND, report it with known_deferred=true and say why.
4. The product thesis: "Most generators roll on a table. This one simulates." The moat is constraint-driven coherence + the living-world spatial engine (mover ladder M1-M11). The owner wants the sim COHESIVE, COMPLETE, IMMERSIVE - in code and in experience.

METHOD (non-negotiable):
- READ WHOLE FILES in your scope, not grep windows. For files over 1500 lines, read all of them in chunks. You may run read-only Bash (ls, grep, wc, git log/show) freely.
- Two-pass rule: after your first pass, do a second sweep with different entry points before concluding.
- Every finding needs quoted evidence with file:line. No evidence means not a finding.
- Distinguish severity honestly: critical = corrupts state/violates constitution/security hole; high = real user-visible defect or sim-logic incoherence; medium = quality/robustness; low = polish.
- SUBSTANTIVE LOGIC is in scope, not just code hygiene: does each mechanic have its natural counterpart? Do systems read each other or sit in silos? Would a DM find the outputs coherent and immersive? Flag missing counterweights, one-way ratchets that should be two-way, mechanics that can never fire, tuning that makes drama impossible.
- Your final text IS consumed programmatically via the schema. Keep evidence quotes tight (max 3 lines each). Prioritize: if you have more than 20 findings, keep the 19 most important + one nit-batch finding aggregating the rest as one-liners.
- Do NOT propose fixes that violate the constitution or product boundaries; bold architecture within them is welcome.

`

const SLICES = [
  { key: 'worldpulse-core', title: 'worldPulse core tick engine', scope: 'src/domain/worldPulse/: pulseKernel.js, advanceCampaignWorld.js, advanceInterval.js, applyWorldPulse.js, actorMajorApproval.js, candidateEvents.js, feasibilityGate.js, decisionTier.js, tierOutcomeApply.js, causeLifecycle.js, causeVocabulary.js, chronicle.js, realmEvents.js, reconcile.js, residueStripGuard.js, simulationProfile.js, simulationRules.js, changeAuthorityPolicy.js, subsystemActivation.js, stablePart.js, pulseHelpers.js, pulseShapes.js, index.js, flows.js, pressureModel.js, seasons.js, partyImpact.js, partyImpactKinds.js, fidelityNoise.js', focus: 'Tick orchestration correctness, proposal/approval machinery, rng confluence + fork-key hygiene, the residue strip, interval math, seasons, profile/rules/preset inference, catch-up (M10b) integration. Is the tick loop architecture sound and extensible? Does autoresolve tend to stasis?' },
  { key: 'worldpulse-war', title: 'worldPulse war/factions/stressors', scope: 'src/domain/worldPulse/: warDeployment.js, occupation.js, attrition.js, mobilization.js, mobilizationEffects.js, mobilizationReactions.js, martialReadiness.js, militaryStrength.js, reinforcement.js, deploymentReturn.js, mercenaryMarket.js, coup.js, factionCapture.js, factionCompetition.js, settlementStrategy.js, scoringObjective.js, disposition.js, dispositionDeltas.js, dispositionLedger.js, moralMartialLean.js, conquestFeeds.js, stressors.js, stressorDynamics.js, stressorGates.js, stressorSeverity.js, stressorAftermath.js, thievesGuild.js, corruptionImpair.js, npcAgency.js, successorNpc.js', focus: "War lifecycle completeness (declare, deploy, siege/field, occupy - AND WHAT ENDS IT: is there peace/withdrawal/liberation?), coup two-sidedness, stressor escalation vs resolution symmetry, faction competition balance, NPC agency within the never-resolve-named-fate boundary. Does war have an ending as rich as its beginning?" },
  { key: 'worldpulse-religion-trade', title: 'worldPulse religion/trade/institutions/population', scope: 'src/domain/worldPulse/: religionState.js, pantheon.js, latentPantheon.js, religiousContest.js, religionLegitimacy.js, piety.js, deityAxes.js, deityStance.js, deityStanceLane.js, clergyTraitPlane.js, cultImpositionApply.js, moralInstitutionPressure.js, tradeSalience.js, tradeWar.js, blockadeTransport.js, foodStockpile.js, supplyCompleteness.js, supplyQuality.js, supplyKernel.js, institutionLifecycle.js, institutionTolerance.js, foundingCatalog.js, archetypeCatalog.js, populationDynamics.js, relationshipCompatibility.js, relationshipEvolution.js, relationshipHierarchy.js, relationshipMemory.js, relationshipRuleHelpers.js, relationshipRulesAdversarial.js, relationshipRulesCore.js, relationshipState.js, resourceTaxonomy.js, tierResourceDynamics.js', focus: 'Religion arc coherence (contest, conversion - and what reverses it?), trade war lifecycle, food/famine and its relief counterpart, institution founding vs closure symmetry, population dynamics vs the known stasis/attractor findings, relationship evolution richness. Is deity-equals-alignment simplicity honored?' },
  { key: 'spatial-engine', title: 'Spatial engine (digest + movers M1-M11)', scope: 'src/domain/spatial/ (ALL 21 files) + src/domain/worldPulse/: beliefMap.js, migrationKernel.js, armyTransitKernel.js, entrepotKernel.js, pestilenceKernel.js, calamityKernel.js + src/store/campaignSpatialCanonize.js + src/lib/spatialPackCapture.js if present', focus: 'The mover ladder end-to-end: digest immutability, mover-to-mover coupling (do rumors/beliefs/caravans/armies/plague/calamity genuinely interact?), dormancy gates, ledger growth bounds/pruning, conservation invariants, pestilence recovery + calamity rebuild counterparts, hysteresis brakes. Is the emergent-drama machine actually wired end-to-end or are there dead seams? M11b calamity just landed at commit 62c81a0c with no code-review pass - review it fresh and hard.' },
  { key: 'domain-top', title: 'Domain top-level (schema/migrations/ledgers/lifecycle)', scope: 'src/domain/*.js (ALL ~60 top-level files: settlement.schema.js, settlementMigrations.js, customContent.js, customContentSchema.js, customContentMigrations.js, userEdits.js, pendingEdits.js, provenance.js, contradictions.js, normalizeSettlement.js, clone.js, regenerationDelta.js, regenerationMode.js, simulationSpine.js, capacityModel.js, crisisLifecycle.js, governanceLedger.js, foodLedger.js, defenseLedger.js, magicLedger.js, magicProfile.js, magicFilter.js, historyBeats.js, hookEscalation.js, causalViews.js, canonicalAccessors.js, fieldManifest.js, clock.js, ageBands.js, and the rest) + src/domain/state/ + src/domain/validation/ + src/domain/coherence/ + src/domain/roles/ + src/domain/factions/ + src/domain/institutions/ + src/domain/campaign/ + src/domain/entities/ + src/domain/relationships/', focus: "Schema/migration chain integrity, user-edit + regeneration-delta lifecycle (the owner's most-bitten bug class: does an edit survive regen/undo/import?), clone/normalize correctness, ledger module coherence, dead or orphaned modules." },
  { key: 'domain-readmodels', title: 'Domain read-models (display/dossier/summary/compendium)', scope: 'src/domain/display/ (ALL) + src/domain/dossier/ + src/domain/summary/ + src/domain/compendium/', focus: 'Fail-closed public-safe projection (free/anon never see DM truth/deity names/covert tags - adversarially check the whitelists), read-model purity, voice sidecar quality, display coherence with engine state, dormancy shapes.' },
  { key: 'domain-events-region', title: 'Domain events + regional causality', scope: 'src/domain/events/ (ALL) + src/domain/region/ (ALL) + src/domain/regionalGraph.js', focus: 'Event taxonomy coherence, regional causality engine correctness, cross-settlement conflict propagation, coupling to worldPulse and spatial layers.' },
  { key: 'generators-pipeline', title: 'Generation pipeline + steps', scope: 'src/generators/pipeline.js, generateSettlementPipeline.js, structuralValidator.js, steps/ (ALL 22 files), helpers.js, lookups.js, priorityHelpers.js, terrainHelpers.js', focus: '20-step pipeline correctness and step-order dependencies, PRNG threading, structural validation depth, config resolution, subsumption/cascade/isolation passes. Same-seed determinism by construction.' },
  { key: 'generators-domain', title: 'Domain generators (economy/power/npc/faction/history)', scope: 'src/generators/: economy/, power/, services/, economicGenerator.js, powerGenerator.js, servicesGenerator.js, npcGenerator.js, npcStructure.js, factionCorrelation.js, factionDynamics.js, factionRoles.js, defenseGenerator.js, historyGenerator.js, legacyGenerator.js, resourceGenerator.js, foodGenerator.js, stressGenerator.js, stressNarrative.js, stressPriority.js, neighbourGenerator.js, isolationGenerator.js, cascadeGenerator.js, narrativeGenerator.js, narrativeText.js, hookVariety.js, spatialGenerator.js, tradeCommodity.js, demandProfile.js, safetyProfile.js, institutionProbability.js, chainMagicSubstitution.js, computeActiveChains.js, crossSettlementConflicts.js, aiLayer.js, generators/data/', focus: 'Constraint-driven coherence quality (the moat): does high criminal priority actually produce corrupt guard + black market + aligned NPC secrets? Cross-generator consistency, supply-chain inference soundness, narrative generation quality, NPC secret/hook coherence.' },
  { key: 'data-tables', title: 'Content data tables', scope: 'src/data/ (ALL ~18k lines: institutionalCatalog, namingData, supplyChainData, npcData, historyData, and the rest)', focus: 'The moat itself: content depth/breadth/quality, internal consistency (ids referenced across tables resolve), register/anachronism, coverage gaps (terrains/cultures/tiers with thin content), duplicate-key discipline, structural conventions.' },
  { key: 'store', title: 'Zustand store (15 slices)', scope: 'src/store/ (ALL 28 files)', focus: 'Slice composition, persist partialize discipline, cross-slice coupling, authIntents replay, action correctness (generate/save/canonize/advance/catch-up/undo), loadEngine lazy pattern consistency, selector hygiene, error paths (failed saves, aborted AI).' },
  { key: 'components-dossier', title: 'Components: dossier/map/generate/session', scope: 'src/components/: OutputContainer.jsx, new/ (ALL tabs), dossier/, SettlementDetail.jsx, SettlementsPanel.jsx, TableView.jsx, InstitutionalGrid.jsx, TradeDynamicsPanel.jsx, WorldMap.jsx, MapOverlay.jsx, map/, region/, session/, settlement/, ChroniclePanel.jsx, PipelineRail.jsx, GenerateWizard.jsx, ConfigurationPanel.jsx, ControlsStrip.jsx, generate/, EntityPicker.jsx, ServicesTogglePanel.jsx, StaleNarrativeModal.jsx, PostGenCoach.jsx + App.jsx, AppViews.jsx, main.jsx', focus: 'Does the UI surface the engine depth (the legibility question)? Dossier information architecture, world-pulse panel UX, live-world visibility, a11y, render hygiene in hot paths, error boundaries.' },
  { key: 'components-commerce', title: 'Components: commerce/community/auth/admin/landing', scope: 'src/components/: gallery/, GalleryPage.jsx, ShareToGallery.jsx, GalleryDescriptionEditor.jsx, GalleryMemberVisibility.jsx, auth/, AuthModal.jsx, account/, AccountPage.jsx, AccountMenu.jsx, PrivacySettings.jsx, admin/, AdminPanel.jsx, pricing/, PricingPage.jsx, purchase/, PurchaseModal.jsx, BuyThisDossier.jsx, SingleDossierSuccessPage.jsx, home/, HomeHero.jsx, HomeLanding.jsx, howto/, HowToUse.jsx, legal/, library/, compendium/, CompendiumPanel.jsx, primitives/, dev/, FeedbackWidget.jsx, AnonTierTeaser.jsx, CampaignSyncBanner.jsx, DeleteConfirmation.jsx, FeatureErrorBoundary.jsx', focus: 'Purchase/credit flows, gallery sharing + privacy sanitization, auth UX + save-as-signup, admin surface safety, landing page vs the product thesis (does it sell the simulation?), primitives quality, legal pages.' },
  { key: 'pdf', title: 'PDF generation', scope: 'src/pdf/ (ALL) + PDF_PARITY_AUDIT.md', focus: 'Dossier-to-PDF parity drift, premium chapter gating (Faith&War three-fold gate), viewModel correctness, chapter ordering/completeness, whether new engine layers (spatial/rumors/beliefs) reached the PDF or silently did not.' },
  { key: 'lib-infra', title: 'Client libs + infra', scope: 'src/lib/ (ALL ~55 files) + src/hooks/ + src/utils/ + src/copy/ + src/config/ + src/design/ + src/kernel/ + src/foundry/ + src/workers/', focus: 'saves.js dual-backend integrity, credit ledger/stripe/checkout reconcile, analytics seam + consent, mapBridge RPC, authSecurity, flags, emailLifecycle, kernel PRNG quality, Foundry export correctness, dead modules.' },
  { key: 'backend', title: 'Supabase backend + api/', scope: 'supabase/migrations/ (all 129 - read the security-relevant + latest 30 fully, skim titles of the rest), supabase/functions/ (ALL), supabase/tests/, supabase/config.toml, seed_local.sql, applied-head.json + api/ (gallery-meta.js, _galleryMeta.js, csp-report.js) + vercel.json + .env.example', focus: 'RLS completeness (every table), SECURITY DEFINER RPC safety, credit ledger atomicity, webhook signature handling, admin privilege checks, migration chain consistency vs applied-head, edge-function auth, the built _shared bundle freshness.' },
  { key: 'tests', title: 'Test suite architecture', scope: 'tests/ (structure + load-bearing suites fully: property/goldens, build/vendorPdfLazy, architecture/, lint/ ratchets, simulation/ soaks, store/, security/) + e2e/ + playwright.config.js - ~751 files; read the load-bearing ones fully, sample the rest by directory', focus: 'Do the goldens/pins/ratchets actually pin what matters? Coverage gaps on critical flows (catch-up, spatial mover interactions, purchase, undo), property-test assertion strength, soak assertion depth, flake surface, e2e depth vs the paid flows.' },
  { key: 'build-tooling-docs', title: 'Build/tooling/CI/docs/FMG fork', scope: 'vite.config.js, eslint.config.js, scripts/ (ALL), tsconfig files, .github/, .husky/, index.html, public/ (incl. map/main.js FMG fork + sf-bridge.js), deno.json, .npmrc, .gitattributes, docs/ (freshness audit: does the section 0.0 STATE LEDGER match git truth? Is M11b @ 62c81a0c ledgered? ARCHITECTURE.md accurate? Stale docs?)', focus: 'Gate integrity (can anything slip past npm run check?), custom eslint plugin correctness, chunk-graph/budget mechanics, FMG fork risk, CI parity with local gate, docs-vs-git drift. Verify current-tree facts: run git log --oneline -8, check whether docs claim M11b/M10b status contradicting commits.' },
]

const DIMENSIONS = [
  { key: 'sim-logic-counterparts', title: 'Simulation-logic cohesion & mechanic counterparts', focus: "THE OWNER'S EXPLICIT ASK. Build a mechanic inventory across worldPulse + spatial: for every force, does its natural counterpart exist and actually fire? war vs peace/withdrawal/liberation; siege vs relief; plague vs recovery/immunity; calamity vs rebuild; famine vs relief/imports; embattlement vs pacification; occupation vs uprising/integration; corruption vs reform; migration-out vs return/resettlement; boom vs bust; conversion vs apostasy/revival; escalation vs de-escalation everywhere. Then coupling: which systems read each other, which sit in silos, which signals are emitted but never consumed (dead seams), which consumed but never emitted. Then stasis-vs-drama: given the known autoresolve-stasis finding, trace what actually injects drama endogenously and whether tail events (M11) can chain. Read the actual advance/apply code paths - not docs - to verify firing conditions are reachable. Report a present/absent/asymmetric matrix in your summary plus concrete findings for the worst gaps.", scope: 'src/domain/worldPulse/ + src/domain/spatial/ + src/domain/region/ (read the apply/advance paths in full; docs/DESIGN_STRESSOR_DYNAMICS.md, docs/SIMULATION_LOGIC_AUDIT.md, docs/STRESSOR_WAVE_DESIGN.md for intent)' },
  { key: 'determinism-constitution', title: 'Determinism & constitutional compliance', focus: 'Hunt violations/risks: Date.now/Math.random/locale/toLocaleString/Intl in engine paths (src/kernel, src/data, src/generators, src/domain); object-iteration-order dependence without codepoint sort; fork-key collisions or under-scoped rng forks; dormancy gates that write-on-load (VIRTUAL violated); features whose absent-state is not byte-identical; budget-ratchet gameability; any-cast ratchet health. Grep systematically, then READ each hit in its enclosing scope before calling it a violation.', scope: 'src/kernel/, src/generators/, src/domain/, src/store/ (engine-adjacent), tests/lint/ + eslint rules that enforce purity' },
  { key: 'state-lifecycle', title: 'State lifecycle integrity', focus: "Enumerate every persisted state family (settlement object + schema migrations; worldState keys incl. spatialLedgers, rumorLedgers, beliefMaps, spatialDigest, lastLivingAdvanceAt, proposals, wizardNews; custom content; user edits; saves in Supabase + localStorage; version history; gallery imports; Foundry exports). For each: trace create/read/persist/undo/regen/clone/migrate/import/catch-up. Hunt the write-that-ghosts-a-path bug class (the owner's most-bitten). Check settlementMigrations chain totality (old save to current schema), undoLastPulse coverage of ALL worldState keys (do new mover ledgers + cursors survive undo?), localStorage-vs-Supabase divergence, version-history restore.", scope: 'src/domain/settlementMigrations.js, settlement.schema.js, userEdits.js, regenerationDelta.js, clone.js, src/store/ (undo/persist paths), src/lib/saves.js, campaignSync.js, supabase RPCs for world-pulse persist' },
  { key: 'experience-product-fit', title: 'Experience & product-goal fit', focus: 'Walk the product cold like a new DM: landing, generate, dossier, canonize, living world. Does the UI surface the engine depth within 60 seconds (the thesis)? Is the living world LEGIBLE (can a DM see fronts/caravans/rumors/beliefs, or is engine richness invisible - the known map-as-legibility gap: how bad is it really)? Onboarding friction, AI-layer discoverability, PDF-as-showcase, premium gate placement, copy quality/register, mobile. Judge against the ambition: cohesive, complete, immersive IN EXPERIENCE, not just code.', scope: 'src/App.jsx, AppViews.jsx, components/home/, GenerateWizard, OutputContainer + new/tabs, map/, the WorldPulsePanel area, components/pricing + purchase, src/copy/, docs/PRODUCT_COHERENCE.md, docs/critique-implementation-status.md, ASSESSMENT.md product half' },
  { key: 'performance-scale', title: 'Performance & scale', focus: 'Tick cost at 5-30 settlements over 30-year soaks: find O(settlements-squared x ledger) hot spots, unbounded ledger growth (every conditionally-materialized ledger: pruned? capped?), memory in long sessions, JSON deep-clone frequency, worldState size growth vs the Supabase persist RPC, first-paint chunk graph health, PDF gen memory, React re-render hygiene in the hot dossier/panel paths (unmemoized heavy selectors).', scope: 'src/domain/worldPulse/pulseKernel.js + applyWorldPulse.js + spatial movers (loop structure), store advance paths, sim-report.json + deep-profile.json if informative, tests/simulation/ soaks for cost baselines' },
  { key: 'security-privacy', title: 'Security & privacy', focus: 'RLS coverage per table (enumerate!), SECURITY DEFINER RPC injection/authz, edge-function auth ordering (JWT before spend), Stripe webhook verification, admin allowlists, XSS surfaces (dompurify usage, gallery user content, AI prose rendering), the premium/anon DM-truth seam adversarially (gallery share, PDF, read-models, AI grounding bundle - can free/anon extract covert/deity/ground-truth?), telemetry consent + PII, seed secrecy, CSP, dependency risk.', scope: 'supabase/migrations (RLS), supabase/functions/, src/domain/display/ whitelists, src/lib/gallery.js + analytics + consent, api/, index.html CSP, tests/security/, e2e security tests' },
  { key: 'code-quality', title: 'Code quality & maintainability', focus: 'Duplication (near-dupe reimplementations across worldPulse/spatial/generators - search concepts not names), dead code/orphaned modules/unused exports, error-handling consistency (silent catches?), module size outliers, JSDoc-type honesty vs any-cast pockets, naming drift (same concept different names), the frozen 4-cycle dependency baseline, barrel hygiene, TODO/FIXME inventory with age.', scope: 'whole src/ tree structurally (use wc/grep sweeps + read the outliers), scripts/eslint-plugin-* rules' },
  { key: 'test-quality', title: 'Test-suite quality & gaps', focus: 'Beyond existence: do goldens pin the RIGHT bytes (what escapes them: store behavior, UI, read-models?), are ratchets only-shrink and tamper-evident, which critical flows lack tests (purchase E2E, catch-up edge cases, mover-interaction matrices, undo of every ledger, migration from oldest save shape), property-test assertion strength (trivial invariants vs real ones), soak assertion depth (distributions or just no-crash?), flake inventory (the known pipeline.property timeout), e2e coverage vs paid flows.', scope: 'tests/ + e2e/ (read the golden/ratchet/soak/property suites fully; sample unit suites), .github CI config' },
  { key: 'docs-knowledge', title: 'Docs & knowledge coherence', focus: 'The state ledger vs git truth (is M11b @ 62c81a0c ledgered? M10b rows consistent? the round-21 plan says M11b unbuilt - stale?), ARCHITECTURE.md accuracy against the tree (spot-check 10 claims), briefs vs shipped implementation drift, dead/contradictory docs, the second-contributor onboarding path, enforced-by comment honesty (do the named tests exist and enforce the claim?).', scope: 'docs/ (ALL), ARCHITECTURE.md, ASSESSMENT.md, CONTRIBUTING.md, .claude/ if present; git log -30 for truth' },
  { key: 'content-immersion', title: 'Content & immersion quality', focus: 'The fiction itself: data-table register/anachronisms/depth, news/rumor/crier prose quality and variety (repetition at scale? template seams visible?), name generation quality across cultures, dossier narrative cohesion (do sections contradict each other?), stressor/hook writing quality, PDF as a table-ready artifact, copy voice consistency, whether emergent events read as STORY (a flood-year that reads like a chronicle) or as system-log.', scope: 'src/data/ (sampled deeply), src/domain/display/ voice/prose modules, src/generators/narrative modules, src/copy/, wizardNews templates, pdf/ sections, stressor/hook content in data + generators' },
]

const mkPrompt = (s, kind) => PREAMBLE +
  'YOUR SLICE (' + kind + '): ' + s.title + '\n' +
  'SCOPE: ' + s.scope + '\n' +
  'FOCUS: ' + s.focus + '\n' +
  'Set slice="' + s.key + '" in your output.'

phase('Subsystems')
const all = await parallel([
  ...SLICES.map(s => () => agent(mkPrompt(s, 'subsystem'), { label: 'read:' + s.key, phase: 'Subsystems', schema: REPORT_SCHEMA })),
  ...DIMENSIONS.map(d => () => agent(mkPrompt(d, 'cross-cutting dimension'), { label: 'dim:' + d.key, phase: 'Dimensions', schema: REPORT_SCHEMA })),
])

const subsystem = all.slice(0, SLICES.length).filter(Boolean)
const dimensions = all.slice(SLICES.length).filter(Boolean)
log('Survey complete: ' + subsystem.length + '/' + SLICES.length + ' subsystem + ' + dimensions.length + '/' + DIMENSIONS.length + ' dimension reports')
return { subsystem, dimensions }