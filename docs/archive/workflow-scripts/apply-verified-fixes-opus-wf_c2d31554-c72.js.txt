export const meta = {
  name: 'apply-verified-fixes-opus',
  description: 'Opus implementers apply the no-decision-needed fixes from the verified findings register',
  phases: [{ title: 'Fix', detail: '10 Opus implementers, disjoint file ownership', model: 'opus' }],
}

const ROOT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const DIGEST = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c69772bd-e324-4e1d-b1ee-b7482c7741fc/tasks/digest_full.json'
const VERDICTS = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c69772bd-e324-4e1d-b1ee-b7482c7741fc/tasks/verdicts_full.json'
const INTERIOR = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/c69772bd-e324-4e1d-b1ee-b7482c7741fc/tasks/interior_findings.json'

const COMMON = `You are an Opus IMPLEMENTER agent for SettlementForge (deterministic D&D settlement/world simulator, React/Zustand/Supabase, JS+JSDoc). This brief comes from the repo owner's interactive session; the owner explicitly ordered these fixes applied. A prior review produced findings; an Opus verification pass re-derived each mechanism. Full detail: read ${DIGEST} (original findings, {reports:[...],critic:{...}}), ${VERDICTS} (per-finding verification notes — READ YOUR FINDINGS' NOTES FIRST, they contain corrected line numbers, corrected mechanisms, and counter-evidence), and ${INTERIOR} for interior findings.

WORKTREE: ${ROOT} on branch claude/composite-r4 @ 018e4119. FIRST ACTION: \`git -C ${ROOT} rev-parse --abbrev-ref HEAD\` must print claude/composite-r4 — stop on mismatch. This is a LIVE SHARED tree: touch ONLY the files in your ownership list; if a file you must edit shows unexpected uncommitted changes, STOP that fix and report.

HARD RULES (each has bitten this program):
- NO git mutations of any kind: no add/commit/stash/checkout/reset/clean. Edits only. The manager stages and commits.
- NO full test suites, no npm run check, no builds. Run ONLY your focused test files, one vitest invocation at a time (e.g. \`cd ${ROOT} && npx vitest run tests/path/foo.test.js\`). A timeout-shaped red under load is a FLAKE: re-run once in isolation before believing it. Read exit codes bare, never through a pipe.
- NO golden-master re-records, NO ratchet/baseline/budget raises, NO new SQL migrations, NO edits to supabase/migrations/. If your fix would shift ANY golden or generation output byte, SKIP it and report why.
- Determinism laws: no Date.now/new Date()/Math.random/toLocale in src/kernel, src/generators, src/domain; engine layers never import React/Zustand/store.
- File-size ratchets are tolerance-0 and JSX comments COUNT: src/components/**/*.jsx ceiling 600 effective lines — EventComposer.jsx and several dossier files sit AT 600/600, so edits there must be net-zero. Check with wc -l reasoning before/after.
- New store actions require an operationRegistry row + \`npm run gen:compendium-data\` — avoid adding actions; if unavoidable, do both and report.
- New tests under tests/generators|joins|simulation|property MUST use tests/helpers/anchoredNegatives.js / seedFailures.js idioms (walkers red otherwise). Rename/cascade tests MUST JSON-round-trip fixtures (in-memory aliasing masks the bug — use the reloaded() idiom in tests/domain/factionRename.test.js).
- Write string literals with template literals; never emit control bytes (a prior Edit wrote a raw NUL — the controlBytes scan reds).
- Match surrounding code style exactly: naming, JSDoc, comment cadence. Comments state constraints, not narration. Keep changes minimal-but-architectural: where the verified finding shows a two-lane divergence, converge on ONE writer/helper rather than patching both lanes independently.
- If a fix requires an owner decision, a schema change, or turns out larger than described: SKIP, implement nothing speculative, and report precisely what you found.

DONE BAR per fix: edit applied + focused test(s) green (quote the pass line) + any test you added is semantically anchored (would fail if the bug returned). Return dense structured data.`

const SCHEMA = {
  type: 'object',
  required: ['agent', 'fixes'],
  properties: {
    agent: { type: 'string' },
    fixes: {
      type: 'array',
      items: {
        type: 'object',
        required: ['finding', 'status', 'files', 'tests', 'notes'],
        properties: {
          finding: { type: 'string' },
          status: { type: 'string', enum: ['fixed', 'skipped', 'deferred', 'blocked'] },
          files: { type: 'array', items: { type: 'string' } },
          tests: { type: 'string', description: 'exact focused commands run + verbatim pass/fail tail lines' },
          notes: { type: 'string', description: 'what changed and why; for skipped/blocked: the precise reason' },
        },
      },
    },
    behavior_shifts: { type: 'array', items: { type: 'string' }, description: 'any legitimate output change a user could observe, stated plainly' },
    discovered: { type: 'array', items: { type: 'string' }, description: 'adjacent issues found but not fixed' },
  },
}

const AGENTS = [
  { key: 'pdf-exports', prompt: `OWNERSHIP: src/utils/generateWorldBook.js, src/utils/generateCampaignPDF.js, src/pdf/**, src/foundry/**, foundry-module/**, api/_galleryMeta.js. FIXES (find each in the verdict notes): (1) World Book 'Under siege' prints undefined — generateWorldBook.js:407 reads sg.id but liveSieges emits targetId; fix to sg.targetId (the sibling generateCampaignPDF.js:824 is the correct pattern); add/extend a focused pin so the regression cannot return. (2) PDF Timeline timezone dependence — pin timeZone:'UTC' (or the export family's established injectable) in src/pdf/sections/Timeline.jsx toLocaleString; this legitimately changes rendered bytes for non-UTC exporters — record it under behavior_shifts. (3) Export-date wall clock injectable in World Book but hardcoded in settlement Cover + Campaign PDF — thread the same injectable seam for consistency (default preserves current behavior). (4) Duplicate divergent markdown escapers across the two Foundry lanes — consolidate to one shared escaper with the superset-correct behavior; keep both call sites. (5) foundry-module Plot Hooks page reads dossier.plotHooks which the engine never writes — repoint build-journals.js at the real hook surfaces (economicViability.plotHooks + history.historicalEvents[].plotHooks per src/generators/aiLayer.js:214-218) and make its pin test use an engine-truthful fixture instead of the fabricated field. (6) SettlementPDF isAnonymous docblock contradicts single-dossier purchase export — fix the docblock to match verified behavior. (7) api/_galleryMeta.js lacks og-image's slug bound — add the same bounded-slug guard. SKIP (gated/deferred — report only): realm-level jsPDF faith seam; og-image CDN unpublish TTL.` },
  { key: 'store-persistence', prompt: `OWNERSHIP: src/store/** EXCEPT settlementRenameHelpers.js (another agent owns it), plus src/lib/saves.js if strictly needed. FIXES: (1) pinnedNpcs regen mis-target (verified LIVE): aiData.pinnedNpcs is npc-id-keyed and nothing remaps it after locked-roster regen/generate — only state.locks gets the _preservation report (settlementSliceHelpers.js:290-316, callers settlementSlice.js:866,1086). Extend the remap to aiData.pinnedNpcs symmetrically (same fromId→id report), persisting the same way the pin write persists (aiSlice.js:1087-1098 shape), and add a focused test proving a pinned NPC keeps its pin across a locked regen with a JSON-round-tripped fixture. (2) recordSnapshot phantom success: when targetSaveId is set but absent from savedSettlements the producer no-ops yet a truthy envelope with after.snapshotId returns — make it return an honest failure envelope, and verify the one consumer minting undo tokens (settlementPendingEdits.js commitPendingEditScope) handles the failure without minting a dead token. (3) Draft version-history orphaned at save transition: settlementSlice.js:500-503 promises transfer; implement it — SaveToLibrary carries draftVersionHistory into the new save's versionHistory (existing field, no schema change); if that path unexpectedly requires schema/服务 changes, downgrade to making the docstring honest and report. (4) aiSlice ai_data writes bypass the durable outbox (verified medium): route them through the column-keyed outbox like sibling persistence if achievable without new schema; else report blocked with the exact obstacle. (5) Registry section-header rot: 'K-C MACRO (40)' vs 44 measured — fix the header count. (6) configSlice LOAD-BEARING signature comment stale ('(set, _get)') — fix to match the walker's actual parse. SKIP (documented deferral — report only): cross-save faction-rename undo asymmetry.` },
  { key: 'components-ui', prompt: `OWNERSHIP: src/components/** EXCEPT SettlementsPanel.jsx and src/components/interior/** (other agents own those). FIXES: (1) EventComposer TDZ (verified with corrections — READ THE VERDICT NOTES): the composerIntent effect can fire against a render scope where composerSetters (const after the settlement early-return) is uninitialized. Restructure so the effect cannot touch the TDZ (hoist the setters/memo above the early return or guard the effect on settlement) — file is EXACTLY 600/600 effective lines, so the edit must be NET-ZERO; verify line count under the eslint max-lines(skipBlankLines,skipComments) semantics before and after. (2) Raw formula/multiplier leaks (4 cited sites incl. CatalogTabs.jsx:121 literal formula copy and EngineSections.jsx:239 raw ×multiplier): translate each into plain language per the LEGIBILITY law (glance → sentence), matching the tone of existing translated surfaces (word-banded severities). Do not remove information — reword it. (3) ai_notes tab renders presence on public gallery dossiers despite owner-private grouping — gate tab PRESENCE by audience the same way sibling tabs do (OutputContainer.jsx:491,497,546 pattern). Run the dossier smoke/tab focused tests you touch. SKIP (report only): 'new/' directory naming, dual Summary implementations, HomeLanding loading state.` },
  { key: 'entity-rename', prompt: `OWNERSHIP: src/components/SettlementsPanel.jsx, src/domain/factionRename.js, src/store/settlementRenameHelpers.js, src/domain/mutateHelpers* and the ~dozen ad-hoc faction-identity sites the finding enumerates, tests/domain/ for these. FIXES: (1) NPC rename cascade (verified — the LIVE lane is the LIBRARY lane, store lane is UI-unreachable; read the verdict notes for the corrected census): converge NPC rename on ONE cascade helper rather than patching lanes independently — factionRename.js already owns NPC_FACTION_FIELDS and documents the npcs[]/members[] alias split; add an exported single-writer npc-rename cascade there (name joins: factions[].members[].name, relationships[].npc1Name/npc2Name, interSettlementRelationships[].npcName — reuse renameInterSettlementReference) and make BOTH the library lane (SettlementsPanel.jsx:373-386) and the store lane (settlementSlice renameNPC via its helper) call it. Do NOT retire the store-lane op (op retirement cascades into frozen artifacts — out of scope). Pin with a JSON-round-tripped (reloaded()) whole-blob test in the factionRename.test.js idiom, walking the same denominator style. (2) settlementRenameHelpers.js header: fix the 'eleven name-keyed surfaces' understatement (pinned denominator >=26) and the false 'NPC rename needs no cascade' claim. (3) Hand-rolled faction identity/membership derivations (~12 sites, all in the legal canonical order): migrate each to the canonical accessors (nameOf/governingFactionOf/factionArchetype/npcInFaction) — mechanical, zero behavior change; run the faction-key lint scans' focused tests after. FORBIDDEN: touching tradeRouteSemantics.js (owner ruling pending on mountain_pass).` },
  { key: 'kernel-generators', prompt: `OWNERSHIP: src/kernel/**, src/generators/**, tests/generators/ for these. FIXES: (1) PRNG fork label aliasing: fork(label) derives child seed via string concat with '::' — add a fail-closed guard (throw on '::' in a label) in src/kernel/prng.js:65 region; no current label contains '::' so this is byte-identical dormant hardening; add a focused pin. (2) Departed-name prose rewrite over-rewrite edge (verified DEMONSTRABLE — 22 surname prefix pairs in draw windows; read the verdict notes): replace the raw text.split(from).join(to) swaps in generateSettlementPipeline.js (:213-216, :254-262 region) with a word-boundary-safe replace so a departed name that is a strict prefix of a surviving string cannot corrupt it. This only changes output in the collision case (currently corrupted prose) — verify byte-identity on non-colliding cases by running the locks-related focused tests (locksSurviveFullGenerate.test.js) and record the collision-case change under behavior_shifts. DO NOT re-record any golden. (3) helpers.js header falsely advertises itself as the single source of truth for clamp while carrying the divergent NaN-passthrough policy — fix the header to state the kernel adoption rule (new code imports kernel math). (4) regenNPCsPipeline docblock (generateSettlementPipeline.js:378-380) still claims generators 'SILENTLY fall back to Math.random()' — false since rngContext fails closed; fix the docblock. SKIP (report only): history-reroll seed recording (recorded deferral [generators-pipeline-5]), generateSeed entropy, aiLayer placement.` },
  { key: 'backend-edge', prompt: `OWNERSHIP: supabase/functions/** (NOT supabase/migrations/ — absolutely no migration edits), vercel.json, tests/edgeFunctions/ + tests/security/ for these, and the rollback-discipline contract file wherever it lives (find via rg '097' tests/ scripts/). FIXES: (1) ingest-events device/identity link inserts ignore write errors so concurrent first-contact can mis-stitch an actor — check and handle the insert errors (fail-soft with correct precedence, no new SQL); read the verdict notes for the verified race. (2) Support-ticket write path: add request length caps and a basic fail-safe rate limit at the edge function (match the sibling functions' limiter idioms). (3) surveyor-byok per-user rate limiter is fail-open on RPC error while the sibling IP gate is fail-closed — align the per-user limiter to fail-closed; record under behavior_shifts (availability posture change, hardening). (4) vercel.json /map/* CSP carries a dead 'navigate-to' directive — remove the dead directive ONLY (do not otherwise touch the map CSP). (5) Rollback-discipline forward contract enforces a 097-era money-table list that misses newer money/PII tables — extend the enforced list to the current money/PII table set (coverage extension of a test contract, not a migration). (6) Research-telemetry consent sync: consent lives only in localStorage; profiles.telemetry_consent (column exists since mig 124) never updates. Implement the sync ONLY if achievable with existing tables/RLS/RPCs (client updates own profile row, or an existing settings RPC); if it needs any new migration or policy, implement NOTHING and report blocked with the exact obstacle. SKIP (owner-gated — report only): BYOK house-key fallback posture; Anthropic retention verification.` },
  { key: 'test-machinery', prompt: `OWNERSHIP: scripts/mutation-sweep.sh, scripts/mutation-coverage-manifest.json only if a fix demands, tests/lint/**, tests/generation.test.js, the warInitiateResolveSplit test file, eslint.config.js, tests/build/sizeBaseline.test.js. FIXES: (1) Mutation sweep dirty-tree guard omits 7 files it mutates and reverts (uncommitted-work destruction risk) — add the 7 to MUTATED_FILES (the verdict notes name them). (2) Stale perl anchors fail as misattributed MISSED — add mutation-applied verification: after applying a mutation, verify the file actually changed (non-empty diff) before check_caught; a no-op anchor becomes BROKEN (self-diagnosing), not MISSED. Dry-run the sweep's own self-test path if one exists; do NOT run the full sweep. (3) warInitiateResolveSplit conditional assertion sometimes-vacuous — make the pin unconditional (restructure the fixture so the branch always exercises). (4) tests/generation.test.js stale comment claiming NPC IDs 'may embed timestamps or other entropy' — fix to state the proven byte-identity law. (5) Guard-the-guard floor in the manifest meta-test drifted far below reality — raise the floor to current reality (tightening a minimum, not raising a budget). (6) Size-ratchet coverage hole: src/components/**/*.js matches no max-lines glob (threeSceneRuntime.js at 704) — extend the 800-line rule (the generators/domain/store tier) to src/components/**/*.js in eslint.config.js AND mirror in sizeBaseline ceilingFor; 704<800 so no baseline entry needed; verify with the sizeBaseline focused test. (7) EP walker scope: negativeAssertionAnchor.walker + seedLoopTotality.walker SCAN_ROOTS cover only 4 trees — extend to the remaining tests/ trees, run ONLY those two walkers, and: if ≤15 new offenders, fix them properly (anchor the negatives via tests/helpers/anchoredNegatives.js); if more, freeze a shrink-only baseline of current offenders in the walker (the house idiom, tolerance-0) and fix the worst 5. Report the measured count. SKIP (programs, report only): money coverage floors, the 199 uncovered manifest entries.` },
  { key: 'build-config', prompt: `OWNERSHIP: vite.config.js, tests/build/vendorPdfLazy.test.js (and any test mirroring the modulePreload regex), the pdfRender worker pin comment. FIXES: (1) modulePreload filter regex over-matches the EAGER engine-core chunk (character class includes '-'): tighten the filter so it strips preload hints ONLY for vendor-pdf and the lazy engine chunk, never engine-core (safest: match the exact chunk basenames with a negative guard for engine-core, e.g. exclude 'engine-core-'), AND fix the test's mirror regex in the same change — the verdict notes explain the two-file coordination. Do NOT run a build (the manager runs build + verify:dist in the consolidated gate); prove the regex behavior with a small node one-liner against representative chunk filenames (engine-BHASH.js, engine-core-C9WT3-vc.js, vendor-pdf-X.js) and quote the results. (2) pdfRender.worker 'no dynamic import()' pin rests on a stale iife rationale (worker.format is now 'es') — fix the comment to the true current rationale. SKIP (report only): triple engine copy in worker bundles; worker file placement drift; HomeLanding loading.` },
  { key: 'interior', prompt: `OWNERSHIP: src/domain/interior/**, src/components/interior/InteriorView.jsx, tests/interior/**, tests/build/interiorLazy.test.js if needed. The feature is DORMANT (unmounted, no callers) so these are wiring-time-safe. FIXES: (1) toPublicSafeInterior retains seedFork embedding the raw settlement seed — blank/drop seedFork in the public projection and extend the path-independence pin (tests/interior/interiorModel.test.js:161-187) to assert the public projection carries no seedFork. (2) SVG export lane draws covert geometry as-given while UVTT scrubs unconditionally — make the SVG lane fail-closed by default: interiorExportSvg scrubs via toPublicSafeInterior unless an explicit, documented option (e.g. {audience:'dm'}) is passed; there are zero current callers so this is dormancy-safe; document the asymmetry decision at the export header. Record as a vetoable default choice under behavior_shifts. (3) Header cites tests/interior/interiorPurity.test.js which does not exist — point both headers (interiorModel.js:28, interiorTemplates.js:20) at the real scan location. (4) Edited furnishings can escape the building envelope (clamped to VIEW not room) — clamp pin nudges to the furnishing's room bounds; extend the envelope test to cover an edited model. (5) InteriorView publicSafe default is fail-open — add the publicSafe-required rule to the documented wiring seam note (InteriorView.jsx:6-13); do not change the default (mount decision is wiring-time). Run the interior focused tests (interiorModel, interiorEdits, interiorExport, interiorLens).` },
  { key: 'docs-minifold', prompt: `OWNERSHIP: minifold-side docs ONLY — CONTRIBUTING.md, README.md, docs/README.md, docs/DEPLOY.md, ARCHITECTURE.md, docs/AI_LAYER_OWNER_RUNSHEET.md. (Main-tree/ledger docs are the manager's, not yours.) FIXES, each verified in the verdict notes: (1) CONTRIBUTING.md '~9,800 tests' understates by >2x — update to the measured ~20,100 tests / ~1,988 files with the standard 'counts approximate; executable output is authority' hedge. (2) README.md + docs/DEPLOY.md say the gate has 13 stages; package.json check chain has 14 — fix both (CONTRIBUTING is already correct). (3) docs/README.md canonical/historical index omits the ~32 DESIGN_*.md files and live program docs (CAPABILITY_REMEDIATION_PLAN.md, EPISTEMIC_PREVENTION_PLAN.md, GENERATION_REMEDIATION_HANDOFF.md) — classify them into the existing tables (live-authority vs historical; follow the file's own precedence rules; when unsure mark historical with a pointer). (4) ARCHITECTURE.md worldPulse module count ~157 → measured 167 (keep the '~'). (5) AI_LAYER_OWNER_RUNSHEET.md §C lists two taste picks as open that queue §0 ruled on 2026-07-28 — mark them ruled with the date, preserving the original text (append-style corrections, never rewrite history sections). Doc edits only; run tests/docs/architectureFreshness.test.js focused after the ARCHITECTURE.md edit.` },
]

phase('Fix')
const results = await parallel(
  AGENTS.map((a) => () =>
    agent(`${COMMON}\n\nYOUR ASSIGNMENT (${a.key}):\n${a.prompt}`, {
      label: `fix:${a.key}`,
      phase: 'Fix',
      schema: SCHEMA,
      model: 'opus',
    })
  )
)

const ok = results.filter(Boolean)
log(`${ok.length}/${AGENTS.length} implementers returned`)
return { results: ok }