export const meta = {
  name: 'build-cycle-41-wr8-closes',
  description: 'WZ-5 — the casus read model per the measured design + the ghost-write sibling sweep — WR-8 closes',
  phases: [{ title: 'Build', detail: 'one war lane, implement + verify' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits with THE STAGED-SET LAW; no stash; focused vitest + gate-tail (ps aux first) incl. tests/domain walkers; python byte-scan every Write; npm run build + smoke:boot; tests/fixtures untouched; same-seed hashes with THE CLOCK LAW (strip wall-clock keys like updatedAt AND run each tree twice before believing any diff — WZ-4's harness lesson; its scratchpad seedHash.mjs does both); STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY: V4C live on nav/brand/theme — fenced.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const r = await agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE WZ-5 — TWO PIECES, then the declaration:
(1) THE BELIEVED-RAZING CASUS (4d), built EXACTLY to WZ-4's measured design on task #108 (read it + WZ-4's queue row @ 506598af-adjacent first): the read model filters wizardNews entries with impactKind 'razing' whose stamped tick + observer hop delay (distancePricedNews hopDelayTicks/routeAwareHopDelayTicks) has arrived; THE RAZER RECOVERS BY ID RECONSTRUCTION (rebuild world_outcome.razing.<razerId>.<victimId>.<tick> from candidates and compare for EQUALITY — attribution by proof, immune to dots in ids); prepend plan.road to the id so the JUST-razing polarity decides by reconstruction over the closed RAZING_ROADS vocabulary; advanceWarReasons gains its razings argument; scoreAtrocityAnswer gets its caller; pulseKernel:2608 gains \`wizardNews,\` ON the existing tick line (net-zero at 1580 — measure); ZERO new persisted surfaces (grep-proven). THE LIMIT IS RECORDED AS CHARACTER, not papered: the 240-entry feed cap vs 260-tick decay means lesser razings scroll out while severity ≥0.72 persists — 'the world forgets the smaller fires' goes in the docstring and the queue row as a design fact. Pins: the full arc (razing → news → hop delay elapses → believed → atrocity_answer raised at the observer) + the JUST-razing mints none + the pre-arrival observer believes nothing yet + the scrolled-out lesser razing is forgotten (the limit pinned as behavior).
(2) THE GHOST-WRITE SIBLING SWEEP (WZ-4's found class, out of its scope, in yours): applyRelationshipPatch rebuilds baselines from an EMPTY edge, so any caller writing a not-yet-materialized edge silently RE-TYPES authored hostile/allied edges. WZ-4 cured its own call site; FIVE siblings write through the same door uncarried: peaceTermsOverlay.js (5 writes), corruptionWeb.js, informationStatecraft.js, warPeaceRefusal.js, warCoalitionSettlement.js. THE STRUCTURAL CURE beats six call-site patches: fix applyRelationshipPatch itself to derive the baseline from the EDGE-DERIVED state (type + untouched axes carried) — one writer, the class dead; audit all six callers against the fixed shape; a pin per polarity (an authored hostile edge written through an unmaterialized path KEEPS its type; the WZ-4 call-site carry becomes redundant and is simplified or left with a note). Same-seed hashes before/after under THE CLOCK LAW — this touches live relationship writes, so if ANY hash moves on a dark set, STOP.
(3) IF BOTH LAND: the queue row declares WR-8 CLOSED — every mechanic built, wired, verified; the four owner-gated arms parked-not-owed; the era's war program complete. Return {lane:'WZ-5', commits, gates, stopReports, notes}.`,
  {label:'wz5:casus-and-close', phase:'Build', model:'opus', effort:'high', schema:S})
const v = await agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. THE BAR: run the FULL casus arc yourself on a real fixture (razing published → hop delay → believed → atrocity_answer raised; the JUST razing control; the pre-arrival control; the forgotten-lesser-fire control); the id-reconstruction attribution proof (plant a settlement id containing dots → attribution still exact); pulseKernel at 1580 net-zero (measure); zero new persisted surfaces (grep); the ghost-write cure at the WRITER (drive an authored hostile edge through every one of the six caller paths — the type survives all six; mutant re-opening the empty-edge baseline → red); same-seed hashes under THE CLOCK LAW re-run by YOU (each tree twice, clock keys stripped, negative control); strict flat; dormancy fixtures untouched; stage-neuter; build + smoke:boot; staged-set + pathspec vs V4C. If EVERYTHING holds, say the words: WR-8 IS CLOSED. Return {lane:'WZ-5 verify', verdict, findings}.`,
  {label:'wz5:verify', phase:'Build', model:'opus', effort:'high', schema:V})
return { r, v }