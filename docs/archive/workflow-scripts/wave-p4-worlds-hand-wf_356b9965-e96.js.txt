export const meta = {
  name: 'wave-p4-worlds-hand',
  description: 'P4 — THE WORLD\'S HAND: causal risk conditions, war motive/capability through belief, the v5 census instrument, Herald lines',
  phases: [{ title: 'Build', detail: 'one Opus implementer', model: 'opus' }],
}
const ROOT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const SCHEMA = { type: 'object', required: ['agent', 'fixes'], properties: { agent: { type: 'string' }, fixes: { type: 'array', items: { type: 'object', required: ['finding', 'status', 'files', 'tests', 'notes'], properties: { finding: { type: 'string' }, status: { type: 'string', enum: ['fixed', 'skipped', 'deferred', 'blocked'] }, files: { type: 'array', items: { type: 'string' } }, tests: { type: 'string' }, notes: { type: 'string' } } } }, behavior_shifts: { type: 'array', items: { type: 'string' } }, discovered: { type: 'array', items: { type: 'string' } } } }

const BRIEF = [
  'You are an Opus IMPLEMENTER for SettlementForge. SLICE P4 — THE WORLD\'S HAND. This is the FINAL slice of the FINAL simulation-changing wave of the entire realm-directives program. BINDING LAW: docs/DESIGN_DEMOGRAPHIC_ENGINE.md — section 0 and 0b (the grounded machinery census: READ IT FIRST, it names exactly what exists and what does not), law 5 (curbs are WORLD EVENTS with incidence coupled to demographic state, never invisible math and never a hidden balancing hand), law 6 (banded appetite), section 6 (realm self-sufficiency + the motive/capability/opportunity/belief split), section 7 (stressor coupling), section 7b (the viability ladder).',
  'Worktree: ' + ROOT + ' branch claude/composite-r4 @ 7ce0148b — verify with git rev-parse --abbrev-ref HEAD; stop on mismatch. FOREIGN, never touch: wizardNews/settlementRumors/heraldRouting/informationStatecraft/momentum/supplyWebWarfare + their tests, and artifacts/soak/**. Focused vitest only, one at a time.',
  '',
  'IMPLEMENT, in this order (each is grounded — section 0b already told you what exists):',
  '',
  '1. THE INSTRUMENT (do this FIRST, everything else certifies through it). The demographics certification row currently declares ZERO channels because no receipt can read one — P3 declared the plan-ledger channel and the corpus guard correctly refused it. The kernel ALREADY emits a full per-settlement receipt every tick something is born or buried (kind `demographic_step`, carrying before/after/births/deaths/bound/binding/pressure01/deficit01/bands/namedFloor and an in-world line), but the pulse drops mover receipts at applyPulseMover and no soak envelope collects them. WIRE THAT INTO THE v5 CENSUS: a stateKeys census (or the equivalent the envelope already supports) so `spatialLedgers.demographicPlans` and the demographic step become readable evidence. THEN declare the channel on the row — in the SAME slice as its reader, which is the discipline P3 violated and the corpus guard enforced. Verify the corpus override set does NOT grow (it is ceilinged at 5 and the ceiling may not be raised).',
  '',
  '2. CAUSAL RISK CONDITIONS (law 5, and section 0b names the ONE seam). Incidence today is a function of exactly ONE number, the pressure score (a birth-threshold gate plus an emission probability); population has effectively no coupling and the thing called "density" there is a tier ordinal. So demographic coupling means adding CONTRIBUTORS to the causal derivers (or new condition archetypes), NOT retuning a weight. Crowding plus poor sanitation raises DISEASE susceptibility; dense trade accelerates transmission; frontier sprawl and low defense raise RAID exposure. TWO TRAPS section 0b names: the pop>=5000 steps SATURATE, so any new term must be CONTINUOUS or the whole runaway range collapses into one bucket; and the housing-pressure consumer sits behind a hard gate — check it before assuming your term reaches anything. THE LAW: the engine must never raise a disaster because the realm is "too full". It raises the CONDITIONS that make a disaster likelier, and the disaster fires on its own existing rules.',
  '',
  '3. WAR: MOTIVE vs CAPABILITY, THROUGH BELIEF (law 6, section 6). Section 0b found the insertion point already exists and is wired: `resource_pressure` is a first-class casus belli scored off `pressureBlend` = 0.6*food + 0.4*economy in warReasons.js. Blend the demographic pressure into its own01/foe01 terms — do NOT build a parallel motive path, the casus taxonomy is walker-enforced for totality and bijection. THEN THE SPLIT: scarcity creates MOTIVE, surplus creates CAPABILITY (armies eat) — a starving fractured realm may want war and be unable to wage it. And the actor must read PERCEIVED scarcity through its belief map, never omniscient truth, so a court can invade a neighbour it wrongly believes grain-rich. RESOURCE_ENVY_GAIN and the existing war-factor cap already supply law 6\'s banded appetite — use them, do not add a second cap.',
  '',
  '4. REALM SELF-SUFFICIENCY + realmPressure emission (section 6): the bounded realm metric per pulse into the behavioral observation as an ADDITIVE v5 field beside J2\'s self-sufficiency, feeding the motive weights inside their caps.',
  '',
  '5. HERALD LINES (the NEWS ADDRESS LAW): the demographic receipts become in-world prose — "Grain ran short in Ashford; three hundred took the north road" — full address chain, typed cause, affected settlements BY NAME. Legibility law: no raw bands, no ratios, no ids on screen.',
  '',
  '6. THE VIABILITY LADDER (section 7b): viable / failing / evacuating / remnant-occupied / remnant-empty, with movers gated on the grade (K1\'s liveInstitutions precedent for buildings; this ladder for the settlement itself). Section 0b: P1a already replaced the terminal lane\'s thorp-tier precondition with a head-count read — extend that into the named ladder, never a second writer.',
  '',
  'PINS with executed negative controls (revert/red/restore/green, quoted): the census READS the channel (and the corpus override set does not grow); disease incidence rises with crowding+sanitation CONTINUOUSLY (the saturation trap pinned explicitly — show the term still varies above pop 5000); NO HIDDEN GOVERNOR (an anchored negative: no code path raises incidence from a realm-total or a population target — source-scanned); motive-without-capability (a starving fractured realm wants war and cannot wage it) and its mirror; PERCEIVED not true scarcity (a court acts on a belief that is wrong, and the receipt says so); realmPressure bounded and emitted; Herald lines carry the full address chain and no raw numbers; the ladder gates movers; dormancy byte-identical by object identity.',
  '',
  'HARD RULES: no migrations; no git mutations; no full suites/builds; NO ratchet raises and NO raising the corpus override ceiling; ZERO any-casts (conditional keys on the OWNING typedef); kernel math primitives; NO Math.pow or transcendentals in seeded paths (the ratchet caught P3 — multiplication is correctly rounded, pow is implementation-approximated); determinism (keyed hashes or one accounted fork; seed-family totality); no em dashes in src/domain|src/data string literals; conditional worldState keys drop-when-empty; the golden law — dark byte-identical, no generation or dormancy golden moves; reseal edge bundles if you touch their inputs. Return dense structured data: statuses, files, verbatim test tails, negative-control quotes, behavior shifts plainly, judgment calls vetoable, discovered.',
].join('\n')

phase('Build')
const r = await agent(BRIEF, { label: 'P4:worlds-hand', phase: 'Build', schema: SCHEMA, model: 'opus' })
return { results: [r].filter(Boolean) }