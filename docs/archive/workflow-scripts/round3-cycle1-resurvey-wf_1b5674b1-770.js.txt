export const meta = {
  name: 'round3-cycle1-resurvey',
  description: 'Round 3 cycle 1: full resurvey across the 13-dimension amended standard + intake lanes + completeness critic',
  phases: [
    { title: 'Survey', detail: '13 dimension lanes + 2 intake lanes' },
    { title: 'Critic', detail: 'completeness critic over lane outputs' },
  ],
}

const WT = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const DOCS = '/Users/cstokes/Desktop/settlement-engine/docs'

const COMMON = `You are a Round-3 resurvey lane for an authorized, owner-delegated review program on the owner's solo repo (SettlementForge — a deterministic D&D settlement/world simulator, React/Zustand/Supabase, ~612k LOC). This is the FINAL review of a FINISHED codebase: the build phase is closed, every designed slice landed or is recorded-deferred, and the full gate is green. Your job: judge your dimension against the A+ standard, name the gap, and return probe-confirmed findings.

ENVIRONMENT: survey in ${WT} (branch claude/composite-r4; verify with git branch --show-current; expect tip ca244100 or a descendant). SURVEY-ONLY: never edit files, never git-mutate, never stash. Allowed: Read/Grep/Glob, read-only git, focused SINGLE-FILE test runs (npx vitest run <one file>) and short node probe scripts written ONLY under /tmp or the OS temp dir — NEVER into the repo. FORBIDDEN: npm run build, the full/sharded suite, npm run check (the baseline is already pinned — cite it), any install.

PINNED BASELINE (verified 2026-07-20 @ ca244100 — cite, do not re-run): two-shard suite 14,460 passed / 4 failed = EXACTLY the parked golden families (generatorGoldenMaster · beliefMapGolden · worldpulseDeityGolden · pdf goldenViewModel — re-minted at THE ONE REGEN, not defects) · first-paint closure 1,024,511 ≤ 1,040,000 · domain-strict 0 · tsc full 0 · lint 0 · npm run check green.

THE COMMISSION (owner, verbatim): "comprehensively and exhaustively read, review, and analyze the entire code and codebase… choose objectively better with risk every time… mature my code to absolute perfection… cohesive, complete, immersive… not just in code but in experience." NO COMPROMISES: where you see a safe patch vs a structurally better fix, recommend the structural fix.

CONTEXT DOCS (main tree, read-only): ${DOCS}/A_PLUS_ROADMAP.md (area grades + A+ bars) · ${DOCS}/THE_RESURVEY_HANDOFF.md (the loop brief + constitution) · ${DOCS}/COMPREHENSIVE_REVIEW_PROGRAM.md (the ledger — CHECK IT before calling anything a bug: a behavior with a deferral/judgment row is DELIBERATE, cite the row instead) · ${DOCS}/THE_REMAINING_ARCHITECTURE.md (§4 pre-stocked items, §9 owner queue, §10 post-launch owed).

FINDING RULES: every finding needs a concrete failure scenario + file:line anchor. severity: P0 (user-visible wrongness/data loss/security hole) · P1 (real defect, bounded blast) · P2 (quality/architecture debt) · P3 (polish). mustFix=true only for findings that block an honest A+ within this loop's power (needs-real-users/soak/launch items are ceiling notes, NOT findings). confidence: CONFIRMED (you executed evidence THIS session — quote it) or PLAUSIBLE (reasoning only). Owner-gated territory (paid-surface behavior, schema, goldens, budget, taste, legal) → finding becomes ownerQueue=true, never a fix recommendation. Deliberate-and-ledgered → NOT a finding.

GRADE: an honest letter grade for your dimension as it stands (A+ only if you would defend it to a hostile reviewer), the NAMED gap to A+, and — where A+ is structurally unreachable pre-launch — the ceiling named with what unlocks it.`

const FINDINGS_SCHEMA = {
  type: 'object', required: ['dimension', 'grade', 'gapToAPlus', 'findings'],
  properties: {
    dimension: { type: 'string' },
    grade: { type: 'string' },
    gapToAPlus: { type: 'string' },
    ceilingNote: { type: 'string' },
    findings: { type: 'array', items: { type: 'object', required: ['id', 'title', 'claim', 'failureScenario', 'anchor', 'severity', 'mustFix', 'confidence'], properties: {
      id: { type: 'string' }, title: { type: 'string' }, claim: { type: 'string' },
      failureScenario: { type: 'string' }, anchor: { type: 'string' },
      severity: { type: 'string', enum: ['P0', 'P1', 'P2', 'P3'] },
      mustFix: { type: 'boolean' }, ownerQueue: { type: 'boolean' },
      confidence: { type: 'string', enum: ['CONFIRMED', 'PLAUSIBLE'] },
      fixShape: { type: 'string' } } } },
    surfacesCovered: { type: 'array', items: { type: 'string' } },
    notes: { type: 'string' },
  },
}

const LANES = [
  { key: 'correctness', model: 'opus', charter: `DIMENSION 1 CORRECTNESS (engine + UI + edge). Hunt real defects in the FINISHED tree: read the hottest recently-merged surfaces (the mini-fold lanes: gratitudeBonds/npcLadderKernel/generosityKernel seams · the fold-batch-3 merge composition points · npcLadderContest · thirdPartyRansom · seaRoads · intelActs · informationStatecraft) for composition bugs the per-lane gates could not see (two lanes each green alone, wrong together). Probe-confirm P0/P1s with focused runs. Also sweep the §4 pre-stocked correctness items still relevant: SettlementsPanel:748 missing || [] guard · decree-provenance gap (applyWorldPulseProposal applies without provenance recording) · WizardNewsPanel dropped try/catch (paid button can stick busy) · SettlementsPanel dropped cancelled latch + reportError seam · dailyLifeLogic buildPrompt supersession · realmArcSummary relocation.` },
  { key: 'cohesion', model: null, charter: `DIMENSION 2 ENGINE COHESION / COUNTERPARTS. Every system must be coherent with every other system's intended/logical/intuitive nature. Verify the deposit-and-consume choreographies compose (statecraft→generosity→ladder ordering; bluffExposures; roadsBondEvents + gratitudeBondEvents; roadsRansomSettlements; intelTransfers), the single-writer discipline holds (grep cross-writer violations into factionPairStates/bonds/credibility/obligations), and counterpart pairs behave symmetrically where the design says they do (grudges vs bonds; debt vs friendship clocks; war fronts vs peace embassies). Read DESIGN_DEEP_COUPLINGS §11 (reads→/writes⇒ matrix) and spot-verify 6+ rows against the code.` },
  { key: 'experience', model: null, charter: `DIMENSION 3 EXPERIENCE/IMMERSION vs THE EIGHT CROWNS: 1 settlement map generators · 2 world map generators (score vs the VENDORED FMG fork + living layer at public/map/, NEVER vs Azgaar upstream) · 3 settlement substance · 4 casual game engines · 5 prose & hooks · 6 setting & world builder · 7 world simulator for TTRPGs · 8 AI usages for TTRPG creation. For each crown: what does the current product actually deliver (read the real surfaces: dossier, realm, chronicle, compendium, roads scenes, news/beats prose) and where does it fall short of the crown's best-in-class? Grade per crown + overall.` },
  { key: 'substance', model: null, charter: `DIMENSION 4 SUBSTANCE BENCHMARKS (the depth standard, DESIGN_SIM_DEPTH_R2.md). Judge depth honesty: D1 distance-priced news · D2 scaling law · D3 doctrine courses · D4 hegemony read · D5 lifespan-scaled memory · D6 underways · D7 reframe layer — each LIT-testable? Verify the depth claims made by docs/comments match executed behavior for at least 3 systems (pick the ones with lit walkthroughs recorded in the ledger and re-execute one probe each where cheap). Content density: are generators' catalogs rich enough to sustain the marketed depth (spot-check catalog sizes vs claims)?` },
  { key: 'perf', model: 'opus', charter: `DIMENSION 5 PERFORMANCE / EAGER BUDGET. The closure is 1,024,511/1,040,000 (15.5KB headroom). Verify the lazy-loading discipline on the newest surfaces (everything from fold batch 3 + mini-fold must be leaf/lazy — spot-check imports of the new files: gratitudeBonds, seaRoads, thirdPartyRansom, intelActs, eliteBleed, factionPairLedger). Hunt runtime perf hazards in the tick pipeline: O(n²) scans over settlements/NPCs added by the new lanes (bond/contest/credibility loops), JSON.stringify equality on hot paths, unbounded ledger growth (every new sidecar must decay/prune — verify gratitudeBondEvents/bluffExposures/intelTransfers/factionPairStates incidents rings are bounded).` },
  { key: 'security', model: 'opus', charter: `DIMENSION 6 SECURITY / ABUSE. The fraud-pass ran (SAFE TO FOLD; FF-a..d fixed the P1s — verify the four fixes are live in the edge tree: refund orphaned charge, abort-live-case-first, buyback payout hold, anomaly fail-closed + SHA-256 abort token). Re-check its P2/P3 residue: session_superseded_during_transfer audit event; the unauthenticated takeover-victim abort (owner-queued — confirm recorded). Sweep the 11 rate-limited edge functions' burst gate is fail-closed; Turnstile inert-until-keys posture; RLS on the new tables (purchase_history, transfer/buyback, client-errors); the sf-bridge postMessage origin audit (public/map/sf-bridge.js — the §4 pre-stocked item); no PII in edge logs (shared scrubbing logger).` },
  { key: 'ai-cost', model: 'opus', charter: `DIMENSION 7 AI COST-EFFICIENCY. The §4 pre-stocked AI-adapter check: verify the AI adapters use PROMPT CACHING on stable grounding prefixes (cache_control breakpoints on the aiGrounding bundle) and tool-forced structured outputs where the schema wall parses JSON; token metering honesty (the request gate on all 8 credit-spending surfaces); the credit debit-point vs response-failure refund path; model selection sanity (latest model ids; no deprecated models in the edge functions).` },
  { key: 'content', model: 'opus', charter: `DIMENSION 8 CONTENT / VOICE. Register discipline (the house voice: period-true, no anachronisms) across the newest content: contest beats, roads scenes/news, gratitude/ransom/embassy prose, traditions beats. The config-sourced-facts law: NO hardcoded prices/caps/numbers in copy (grep price-like literals in src/copy + components; the two title= Cartographer price strings were flagged DIES in the About overhaul — check state). en.js coverage (6 master footer/legal keys were flagged absent — verify). HomeSampleDossier accent eyebrow AA failure (pre-stocked) — verify state.` },
  { key: 'a11y', model: 'opus', charter: `DIMENSION 9 ACCESSIBILITY. Names/roles/labels on the newest interactive surfaces (money panels: transfer/buyback/auto-reload/purchase-history; the Surveyor door; roads/travelers overlays; contest/chronicle modals). Live regions for async status (payment states, generation progress). Focus traps in modals. Contrast on the newest chrome (the folder thead a11y item LANDED — verify; the HomeSampleDossier AA eyebrow — state?). Keyboard paths through the critical money flows. Grade honestly vs WCAG 2.1 AA as the floor, best-in-class as A+.` },
  { key: 'game-feel', model: null, charter: `DIMENSION 10 GAME-FEEL (crown 4's lens): pacing, session rhythm, return-pull. Read the actual loop a user lives: generate → advance ticks → read news/beats → inspect dossier/chronicle → intervene (edits/decrees/AI) → advance again. Judge: does the tick cadence produce a satisfying drip of consequence (the tempo governor, catch-up collapse handling)? Do the new layers (contests, bonds, roads, gratitude) surface VISIBLY enough to reward return visits, or are they engine-rich but experience-invisible (the classic sim trap)? Name the top experience-visibility gaps with anchors (which rich state has no surface).` },
  { key: 'determinism', model: null, charter: `DIMENSION 11 DETERMINISM / REPRODUCIBILITY (NEW — the product's constitutional claim, now its own grade). Verify: rng-stream isolation on the newest lanes (every draw from a named fork of the world seed, never Math.random/Date — grep the new files; the determinism eslint guard covers src/domain — verify its scope includes the new leaves); same-seed byte-stability claims carried by dormancy goldens (cite, don't re-run the suite); cross-machine stability hazards (locale/Intl/toLocaleString in engine paths, float formatting, object-key iteration order reliance on non-sorted maps in PERSISTED output — the sortedRecord discipline; spot-check the newest persisted shapes: gratitudeBondEvents sorted? factionPairStates? contests?). react-pdf renderToBuffer is KNOWN non-deterministic (walk the element tree, never bytes — verify no test compares PDF bytes).` },
  { key: 'lifecycle', model: 'opus', charter: `DIMENSION 12 STATE-LIFECYCLE INTEGRITY (NEW). For the newest persisted shapes (gratitudeBondEvents [one-tick], bonds w/ foreignSid, factionPairStates, contests, npcCredibility, intelTransfers/intelCooldown, roads/roadsBondEvents/roadsEmbassies, bluffExposures, RansomRec/MissionRec additive fields): trace EVERY lifecycle path — create, read, persist, REGENERATE (does a settlement regen/reroll preserve or correctly rebuild it?), UNDO (the apply→undo byte-inverse — do the new worldState keys round-trip?), CLONE (save duplication), MIGRATE/IMPORT (legacy saves without the keys load tolerantly — absent-key paths). Execute at least 3 round-trip probes (focused tests exist for some — run those files singly; write temp-dir probes for uncovered paths). A shape that survives the tick but dies on undo/import is a mustFix.` },
  { key: 'claims-parity', model: null, charter: `DIMENSION 13 CLAIMS-VS-ENFORCEMENT PARITY (NEW — the meta-pin philosophy as a grade). Sample 25+ strong claims across: code headers ("@enforced-by", "byte-identical", "single writer", "never X"), docs (START_HERE expected-reds, THE_RESURVEY_HANDOFF constitution numbers), and marketing surfaces (About/Welcome/Pricing/Compendium public claims — determinism, receipts, state-never-fate, schema wall, truth-projection). For each: does a NAMED enforcing gate exist and actually run? The measured gap (claims without enforcers, enforcers that are grep-not-execution) IS the grade. The §4 doc-claims audit + About-claims audit fold in here. Report the unenforced-claim list with anchors.` },
  { key: 'coherence-reverify', model: 'opus', charter: `INTAKE LANE A — COHERENCE MATRIX RE-VERIFY. ${DOCS}/COHERENCE_MATRIX_R3.md was computed against an OLD base (aad6265e); much has landed since. For EVERY row still potentially open — the 9 defects (ruin-filter class 1-4, occupation×corruption 5, religion×war 6, occupation×migration 7, occupation×ladder 8, politics×beliefs 9) and the 12 gated-in candidates + the gated-out/owner-queue rows — verify against THE CURRENT TREE: FIXED (cite the fix in code), STILL-OPEN (a real defect/candidate remains — finding with anchor), or DEFERRED/OWNER-QUEUED (cite the row). Expected mostly-fixed: ruin-filter class (the ruin-filter lane), #5, #6 (RF-b/RF-a), migration×rumor (D-0), beliefs×roads (R-8 purpose 7), roads seams (folded). Genuinely check 7 (occupation flight keys), 8 (factionArchetype category-before-occupation rule), 9 (two governs resolvers), the traditions quartet (trade_route_cut in HARD_STRESSOR_ARCHETYPES? calamity/generosity/corruption festival terms?), the two dead-data closures (npcLadderChallenge reads faction.captureState? institutionLifecycle reads controlled/suppressedInstitutions?), ladder×politics bloc window, prosperity×politics coup economicAdj, corruption×migration push term, corruption×upswing pressureMult rewire, beliefs×upswing read.` },
  { key: 'prestocked', model: 'opus', charter: `INTAKE LANE B — PRE-STOCKED DISPOSITION. Disposition EVERY §4 pre-stocked item (THE_REMAINING_ARCHITECTURE.md §4 "PRE-STOCKED ITEMS" — read it in full) not covered by another lane, plus the task-#40 seams (shared-war-coalition arm · champion covenant UI · unbonded-conscience payer · third-party news pool — for each: does it pass the FOUR GATES [existing machinery both sides / dark-dormancy constitution / soak-bandable / not owner-parked]? If yes it is a BUILD candidate finding; if no, owner-queue/deferred-seam with the failed gate named). Also: W6 delta audit · S1-S3 token retrofit · interior entry hook · edit-chrome teaser · panel analytics · S3-S6 instruction adoption · ladder recorded seams · 23-literal tokenize-down · slugify dedup · annex/survey naming · Wizard/Forge overlap · public/map gate-coverage (docs/fmg-fork.md names its gates — confirm they RUN) · FMG-fork upgrade currency · downgrade-transition audit residue (the three owner-ambiguous grandfather cases recorded?) · Card de-shadow pin · Segmented recomposition · en.js keys · map P-principle review. Status each: RETIRED (cite) / STILL-OPEN (finding) / OWNER-QUEUED (cite).` },
]

phase('Survey')
const results = await parallel(LANES.map((l) => () =>
  agent(`${COMMON}\n\nYOUR LANE: ${l.charter}\n\nReturn the structured result. Prefix your finding ids with '${l.key}-'.`,
    { label: `survey:${l.key}`, phase: 'Survey', schema: FINDINGS_SCHEMA, ...(l.model ? { model: l.model } : {}) })))

const lanes = results.filter(Boolean)
log(`${lanes.length}/15 lanes returned; ${lanes.reduce((n, r) => n + r.findings.length, 0)} raw findings`)

phase('Critic')
const summary = lanes.map((r) => `${r.dimension}: grade ${r.grade}; covered: ${(r.surfacesCovered || []).join(', ')}; findings: ${r.findings.map((f) => f.id + ' ' + f.title).join(' | ')}`).join('\n')
const critic = await agent(`${COMMON}\n\nYOU ARE THE COMPLETENESS CRITIC. 15 survey lanes just reported. Their coverage and findings:\n${summary}\n\nYour job: what did the panel MISS? Name (a) product surfaces or subsystems NO lane covered (check: edge functions tree, supabase migrations, the store slices, the PDF pipeline, the gallery/library, auth flows, the FMG bridge, scripts/ tooling, CI workflow files), (b) cross-dimension interactions no single lane owns (e.g. a11y×money, determinism×AI, perf×PDF), (c) any pre-stocked/ledger item every lane skipped. For each gap EITHER do a quick targeted check yourself NOW (Read/Grep, focused single-file test max) and report what you found, OR return it as an uncovered-surface finding. Same finding rules and schema; dimension='completeness-critic'; prefix ids 'critic-'.`,
  { label: 'completeness-critic', phase: 'Critic', schema: FINDINGS_SCHEMA })

return { lanes, critic }