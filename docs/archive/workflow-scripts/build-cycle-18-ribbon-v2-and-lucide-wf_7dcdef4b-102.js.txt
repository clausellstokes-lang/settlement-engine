export const meta = {
  name: 'build-cycle-18-ribbon-v2-and-lucide',
  description: 'Ribbon v2 (slim grained wood shaft, gilt fletching) · the lucide totality wave (icons-off law enforced structurally)',
  phases: [{ title: 'Build', detail: '2 lanes, implement + verify each' }],
}
const MF = '/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold'
const P10 = `THE §10 PROTOCOL BINDS: ${MF} on claude/composite-r4 only (cd + rev-parse first, every command block); pathspec commits of ONLY your lane files; no stash; focused vitest + gate-tail (ps aux | grep vitest first); STOP-and-report; trailer "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>". Nothing pushed. CONCURRENCY: cycle 16 lanes may still be live on worldPulse (kernel/wiring) and causeWalk/stateProse — fenced; docs/FABLE_VALIDATION_QUEUE.md may carry uncommitted foreign rows — record rulings in COMMIT BODIES if the queue file is dirty with foreign hunks, else carve. The tree moves under you: re-read status/log before staging.`
const S = { type:'object', required:['lane','commits','gates','stopReports','notes'], properties:{lane:{type:'string'},commits:{type:'array',items:{type:'string'}},gates:{type:'array',items:{type:'string'}},stopReports:{type:'array',items:{type:'string'}},notes:{type:'string'}}}
const V = { type:'object', required:['lane','verdict','findings'], properties:{lane:{type:'string'},verdict:{type:'string'},findings:{type:'array',items:{type:'string'}}}}
phase('Build')
const lanes = await parallel([
  () => agent(`VERIFY-FIRST: OWNER-DIRECTED implementation (three refinements dictated live today; build on the landed fletching v1 @ 66b462e3 — read its diff first). ${P10}
LANE V2 — RIBBON V2, THE FULL REFINED SPEC:
(1) SLIM SHAFT: CHROME.headerDesktop 60 → 48 through the single writer in theme.js; ANCHOR_OFFSET derives 84 → 72 automatically — update every pin to assert THE DERIVATION (headerDesktop + SP.xxl), never a literal; sweep any hardcoded 84/80 the About/guide/Compendium pins or components carry (CatalogTabs.jsx:145 has a hardcoded scrollMarginTop:80 — fold it into the token NOW, the drift was recorded); header contents (wordmark, tabs, Sign In) re-padded to sit in 48.
(2) FULL-WIDTH LIGHT-WOOD SHAFT, including under the logo/wordmark — ONE continuous material; the wordmark takes its dark-brown variant (AA-checked); the ribbon separates from the cream body via a slightly deeper wood tone + hairline + soft shadow.
(3) SUBTLE WOOD GRAIN on the shaft: two layered HORIZONTAL streak gradients (asset-free CSS), irregular periods (no visible tiling rhythm), 2-4% luminance variance; AA for every label measured against the DARKEST streak.
(4) THE FLETCHING TRIO on the light shaft: DARK-BROWN feathers; GILT EDGING — a thin gold hairline tracing each feather silhouette + a very soft low-alpha outer bloom (gilding, never neon); fine diagonal BARB texture running WITH the feather slant (different direction + scale than the shaft grain, 3-5% contrast); labels SEMIBOLD 600 (not 700) — active needs a second distinguisher: brighter gilt + the gold underline. THE BALANCE LAW: five emphasis devices stack (band, slant, gilt, texture, weight) — dial EACH to ~half its solo strength.
(5) Mobile follows the palette; the fletching stays desktop-only as before; overhang stays PAINT-ONLY (layout box exactly headerDesktop).
Update pins with docstrings citing the directive; suites + eslint + sizeBaseline + npm run build green. Pathspec commits. Return {lane:'V2', commits, gates, stopReports, notes}.`,
    {label:'v2:wood-shaft', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Verify on committed bytes: ANCHOR derivation — re-derive 72 from the token chain and prove NO literal 84/80 scroll-margin remains anywhere in src (grep); header layout box exactly 48 with the overhang as paint (mutant: real height → pin reds); AA recomputed by YOU for wordmark + every label against the darkest grain streak (quote ratios); the balance law is real (assert the gilt hairline width ≤1px, bloom alpha low, texture variances in the stated bands — read the actual values); active distinguishable from inactive with all three semibold (render both, compare styles); mobile palette follows but no fletching; npm run build; pathspec-clean. Return {lane:'V2 verify', verdict, findings}.`,
      {label:'v2:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
  () => agent(`VERIFY-FIRST: chair-ruled implementation. ${P10}
LANE LU — THE LUCIDE TOTALITY WAVE (the icon sweep's ⛔ finding + four chair rulings, all recorded in your queue row/commit body):
THE RULINGS: (i) the Realm-map exception SURVIVES — the map is a diagram, its icons encode data (TierIcon, MapLegend, RelationshipEdges opt in as today); (ii) the ✦ data markers (custom-ownership + magic) and the 16 tradition heraldic motifs and band/status glyph sets are the WORLD'S OWN SYMBOLS — KEEP, ratified pins stand; (iii) the Surveyor's authorial ✦ eyebrow marks are the Surveyor's mark — KEEP (the prompt itself has no icon; the carve-out attaches to these); (iv) 🔒 canon-lock and ⚠ legitimacy-crisis stay (functional, owner-confirmed).
THE WORK: the estate ALREADY ratified icons-off — primitives/IconsContext.js documents 'the redesign suppresses lucide/SVG icons on EVERY surface except the Realm map' — but only 11 primitives consult it while ~184 files render ~305 lucide icons around it. ENFORCE IT STRUCTURALLY, not with 305 hand edits: make the suppression TOTAL through the provider/primitives where components already route; where files import lucide directly, remove the render + heal the layout (the icon sweep's dead-slot lesson: remove the SLOT, not just the glyph); the map subtree opts back in exactly as today. THE WALKER (structural prevention): a source-scan test — no direct lucide-react import outside the map subtree + the consulting primitives' allowlist; frozen census, shrink-only; mutant: add a direct import → red. Work in surface-family commits (tabs, cards, panels, pdf...) with the census in each body; if the full 184 exceeds this lane's budget, close whole families and STOP-report the exact remainder — no half-families.
Suites per family + eslint + sizeBaseline + npm run build. DO NOT touch: nav/theme/routes (lane V2), the map subtree's own icon renders, worldPulse, fenced CF files. Return {lane:'LU', commits, gates, stopReports, notes}.`,
    {label:'lu:lucide-totality', phase:'Build', model:'opus', effort:'high', schema:S})
    .then(r => agent(`VERIFY-FIRST: adversarial verifier. ${P10}
Report: ${JSON.stringify(r)}. Re-census lucide imports/renders YOURSELF over committed bytes; diff vs the lane's census (a missed site is a finding); the walker's mutant re-run by YOU (add a direct import in a non-map file → red; in the map subtree → green); spot-render 5 de-iconed surfaces for layout health (no orphan slots/gaps — the dead-slot class); the four kept classes byte-untouched (✦ data markers, tradition motifs, Surveyor marks, 🔒/⚠); map renders unchanged; suites green; any STOP-reported remainder EXACTLY enumerated (count it yourself); pathspec-clean vs lane V2. Return {lane:'LU verify', verdict, findings}.`,
      {label:'lu:verify', phase:'Build', model:'opus', effort:'high', schema:V})),
])
return { lanes: lanes.filter(Boolean) }