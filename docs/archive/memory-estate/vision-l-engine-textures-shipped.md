---
name: vision-l-engine-textures-shipped
description: "⭐ V-24a recall rider · V-24b route re-propagation · V-24c interiority-lite · V-24d provenance threading @ claude/vision-l 3b33c1a6 (base 212758ad, dark, NOT folded); ⚠ roadsKernel at EXACTLY 800-line ceiling; ⚠ new EDIT_KIND ⇒ edge-bundle regen + revert spurious generatedAt churn"
metadata: 
  node_type: memory
  type: project
  originSessionId: ff8b4b71-3ea5-4d7a-9793-42190018c11f
  modified: 2026-07-20T18:10:51.333Z
---

VISION LANE V-L (2026-07-20, agent-built, manager-ledgered — the agent wrote no
memory file; this is the manager's bank of its report): tip `3b33c1a6` on
claude/vision-l, base 212758ad, five commits (1d180144 recall · 4b8894d3 re-prop ·
9dfdea98 interiority · 5639eeff provenance · 3b33c1a6 completion). All dark,
eager Δ ≈ 0 (closure 1,025,055 at its tip), strict 0 / tsc 0 / lint 0, ~109 pins.

**What shipped:** V-24a `recall-npc` typed edit kind (registered in lockstep:
pendingEdits EDIT_KINDS+COMMITTABLE, editFingerprint, applyNpcOp, roads/ops.js
write, roads/state.js consume — mission flips to `returning`, real future arrival,
never a teleport). V-24b `routeAwareHopDelayTicks` — recompute-on-read surcharge,
in-flight rumors re-price as routes sever/open; ENDPOINT embattlement only
(mid-path hop = recorded follow-on). V-24c pure `npcInteriorityRead.js` leaf
(player-safe vs includeGroundTruth DM-truth halves; DM relational block composed
but NOT wired — display layer carries no worldState by secrets architecture).
V-24d ransom release beat carries `causedBy` → capture receipt (dark byte-neutral).

**Hazards (each bit or nearly bit):**
- ⚠ roadsKernel.js sits at EXACTLY its 800-line eslint ceiling — any addition goes
  in a leaf (the consume body lives in roads/state.js for this reason).
- ⚠ a new EDIT_KIND requires `node scripts/build-edge-shared.mjs` (analyticsEvents
  re-exports EDIT_KINDS) — AND that regen churns aiGroundingBundle.meta.json's
  `generatedAt` with UNCHANGED sourceHash: revert the spurious churn.
- ⚠ piped-gate exit-masking re-bit: check-domain-strict.mjs BARE, never through tail.

**Cycle-2 intake (deferred-recorded):** V-24d contests edge needs `openedTick` on
ContestRec (lit-golden-affecting schema add — owner-adjacent); gratitude edge has
no durable receipt id to key; V-24b believed-path (mid-path) re-pricing follow-on.

Related: [[quintuple-fold-executed]] [[round3-f3-hazards-and-flags]].
