---
name: wave-c-traditions-seams-shipped
description: "⭐ WAVE C — the 5 dark traditions seams shipped on claude/wave-c-seams (base composite aad6265e, NOT folded); all dark + byte-identical; full suite green except the 4 parked golden families + the unchanged verify:dist budget"
metadata: 
  node_type: memory
  type: project
  originSessionId: c7979c3b-d9d7-48bb-a499-e2271011bf43
  modified: 2026-07-19T16:22:40.838Z
---

2026-07-19: WAVE C IMPLEMENTER lane — the five §16 traditions deferred seams built
DARK to completion on `claude/wave-c-seams` (base = the composite `aad6265e`, NOT
folded; the manager folds). Lettered commits:

- WC-a `c2b8288e` (+ WC-a-fix `295f27b8` typedef) — FESTIVAL MAP DRESS (§10): the
  IT-3 state-dress hook gains a `festival` facet; `pushFestivalDress` strings ≤8
  lantern dots along the HIGH STREET (longest `skeleton.streets`) while an
  observance's window is open. `resolveMapDress` reads the `settlement.traditions`
  MIRROR + the local 4-4-5 week clock.
- WC-b `50b2b38f` — REFRAME 'tradition' ACT CLASS (§8/§16): a 9th REFRAME_VOCAB entry
  {observance · rite_supplanted→culture_effaced · custom_welcomed} + FACT SOURCE 3 in
  advanceReframe reading `spatialLedgers.traditions` suppression rows (a vassal
  reframes the overlord's imposition). Rides `reframeEnabled`.
- WC-c `6f1867ff` — CULTURE-VECTOR READS TRADITIONS (§1/§16): buildCultureVector adds
  `traditionElements` (active motif element set); `traditionKinship01` Jaccard folds
  into the cultureDistance MULTIPLICATIVE closer (the tradeTie01 pattern). Reaches M4
  migration + M7 contraband.
- WC-d `7b7fb59c` — FAIR TRADE-LANE PULSE (§16): `fairTradePulse` → +1 extra
  prosperity band-step for a 'fair' landing GOOD+ on a connective route tier.
- WC-e `027e4e4c` — CROSS-SETTLEMENT PILGRIMAGE (§16): new leaf
  `traditions/pilgrimage.js`; `pilgrimageDraw` lifts a GRAND host observance's §4
  score (≤0.1) by nearby-neighbour attendance over the spatial digest.

**Why (load-bearing findings):**
- ⚠ TRADE IS DERIVED-ONLY — an adversarial map confirmed NO writable trade field
  exists anywhere; every trade→economy effect routes through `prosperity`. So the
  fair "trade pulse" is an extra prosperity band-step through the SAME §5 applicator
  (a new trade field is forbidden by the design). tradeTie01 is a relationship-LABEL
  read, not a stored quantity.
- ⚠ The reframe mover keys on LEDGERS, never news — FACT SOURCE 3 reads the
  suppression rows; news tags/impactKind/sourceEventId are not a match key anywhere.
  The vocab test pins `length === 8` → bumped to 9 (behavioral pin EXTENDED).
- ⚠ DRESS_CAP = 160 is a SHARED op budget (townMapOpBudget.test + townMapSeasonDress):
  the worst crisis-state dress stack is 148, leaving only 12 headroom — festival
  lanterns are capped at 8 on the single high street (not all streets).
- Pilgrimage modulates ONLY the host outcome (a cross-settlement write would breach
  the §14 write-list — deliberately NOT taken).
- Seam-1 "lit" signal = mirror-presence + in-window (the mover writes the mirror only
  when lit), NOT a draw-time flag read.

**How to apply:** the manager folds `claude/wave-c-seams` at the fold (NUL scan; full
suite). Lane-end gate GREEN: build ✓ · typecheck + domain:strict (ceiling 0) ✓ · lint
(0 err) ✓ · validate:data/migration-head/edge/map ✓ · test = 13,636 pass / 4 fail =
EXACTLY the parked golden families (generatorGoldenMaster · beliefMapGolden ·
worldpulseDeityGolden · pdf goldenViewModel — EMPIRICALLY re-confirmed red at base
aad6265e) · verify:dist = 1,040,998 B (byte-for-byte the base composite overage — the
seams added ZERO eager bytes). No migration, no store action, no DB. Dormancy proofs:
traditionsDormancy · reframeDormancy · spatialDormancy.byteIdentity + all
townMap/worldpulse-spatial/seasons goldens byte-identical.

**Vetoable JUDGMENTs (per commit):** map-dress lit-gate = mirror-presence · reframe
predatory+NaN-mintTick imposition read · kinship = Jaccard over motif ELEMENTS @
TRAD_CLOSE 0.2 · fair pulse routes through prosperity, +1, connective+GOOD-gated ·
pilgrimage host-only, grand-gated, cap 0.1.

**DEFERRALS (written, not bugs):** reframe tradition read-model surfacing in
giftsAndDebtsRead.js left INERT (the reframes-ledger reading exists; a nominalActClass
branch is a display refinement, not needed for the seam) · pilgrimage FAITH-AFFINITY
(co-religionist draw) is a soak-tuning refinement, not built · seam 2 is DOUBLE-flag
dormant (rides reframeEnabled AND reads the traditions ledger — byte-identical when
either is dark). Related: [[traditions-lane-complete]] · [[comprehensive-review-fix-program]].
