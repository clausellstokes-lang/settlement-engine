---
name: es5b-reconciliation-and-chair-rulings
description: ES-5b's one-sentence blocker decomposed into three claims (one dissolves); CR-ES5B-1..7 ruled — the insertion must NOT reach the frozen war chooser, and ABSENT_W is chair-authored dark tuning
metadata:
  type: project
---

**⭐ THE BLOCKER WAS THREE CLAIMS WEARING ONE SENTENCE.** INDEX.md:106's "ES-5b waits
on insertion/order reconciliation" decomposed by measurement (2026-08-11):

1. **INSERTION — REAL, documentary.** ⚠⚠ **ES-5b HAS NO CHARTER ANYWHERE.** It exists
   ONLY in ES-5a's landing commit message (`41ddeae0`, itself flagged "JUDGMENT,
   vetoable"); neither design volume was ever amended. `grep -rn "ES-5b" docs/` = two
   POINTERS (INDEX.md:106, SOL_QUEUE.md:95) and no definition. Seven counts are stale
   post-slice (FP §5 "108 waves" :827 / "eight ES waves" :840; ES §4 "Wave count: 8"
   :1577; §6 item 3 "ES-5/ES-6 follow", "60 → 68").
2. **ORDER — REAL, and the answer is FORCED.** `presentShare01`'s two inputs are
   written at opposite ends of one tick: `memberNpcIds` at pulseKernel.js:346, but
   `npc.whereabouts` not until :2563 → roadsKernel.js:1074. BOTH bloc consumers sit
   between them (advanceSettlementPolitics :380; `rulingBlocOf` at roadsKernel.js:1191,
   BEFORE the :1192 write). **The lag is UNIFORM at exactly one tick with NO same-tick
   divergence**, and the only alternative — a pulse reorder — is forbidden by L1.
3. **VOCABULARY — ⭐ DISSOLVES.** ES-5a's handed-down divergence #4 does not survive
   measurement: `roads/state.js:122` exports the frozen totality
   `['traveling','visiting','returning','hostage']`, matching §3.11 once hostages are
   excluded (which §3.11 already does BY NAME). The six-member list ES-5a measured is
   `envoyCasting.js:101` — a DEFENSIVE SUPERSET that also reads `whereabouts.phase`,
   a field the schema does not define.

## CR-ES5B-1..7 (chair, 2026-08-11) — FABLE-ISSUED, no post-boundary row owed

- **1 RATIFY the slice + amend all 7 stale doc sites in the promotion commit.** A
  wave that exists only in a commit message is not a charter; the packet becomes it.
- **2 ACCEPT the one-tick lag, DECLARED AND PINNED.** It is uniform and forced; an
  undeclared lag is the bug, not the lag itself.
- **3 ONE CR-ES-1 signature covers ES-5b and ES-5c** — the same doctrine, split for
  budget, not for scope.
- **4 MINT CPL-20 ANYWAY.** ⚠⚠ Routing through the UNLAYERED `settlementPolitics.js`
  mints no pair only because `scanCrossLayerPairs` iterates LAYERED importers — that
  is **FAIL-OPEN INVISIBILITY, not absence**. Record the edge that the walker cannot
  see. (Building in `factionCompetition.js` as the charter suggested WOULD have minted
  a pair — the coupling trap's sixth near-miss.)
- **⛔⛔ 5 THE INSERTION MUST NOT REACH THE FROZEN WAR CHOOSER.** Inserting at
  `rulingBlocOf` also reaches `settlementStrategy.js:1223` and the corruption web —
  WIDER than §3.11's "council weight" language authorises. **The weight is visible
  ONLY to the council-weight consumer**, proven by a pinned control showing the war
  chooser and corruption web read UNCHANGED values. If a shared derivation makes that
  impossible, that is a STOP and a re-design — never "it also affects war".
- **6 `ABSENT_W` IS CHAIR-AUTHORED DARK TUNING** (the TC-4 O-3 / MAXIMUM_WARDS
  precedent): a dark-flagged, engine-inert value, individually vetoable, NOT the
  owner's tuning signature (which binds LIT-surface tuning at soak). ⛔ The lane was
  right that an implementer must never be forced to author it.
- **7 ES-5c compiles only after ES-5b LANDS** (just-in-time; no second stale backlog).

⚠ Draft: scratchpad `es5b-packet-draft-ES-5B.md`. ES-5b as chartered EXCEEDS the hard
budget (5 existing production files vs 3; 2 behavior families vs 1) — hence the
bench-grain / career-grain split. ⚠ factionCompetition.js has only 48 lines of
headroom (752). Related: [[espionage-confirmers-directive]], [[barrel-hop-drags-the-whole-family]].

## ⛔⛔ CR-ES5B-5 STANDS — AND ES-5b IS **BLOCKED**, OWNER-GATED (chair, 2026-08-11)

The author lane recommended ACCEPTING the reach into the war chooser, citing §3.11's
"rivals exploit the window through machinery that already exists: the ladder's
contest math and factionCompetition read the same bloc weights every tick"
(DESIGN_FP_ARCH_ES.md:1142-1143), and arguing that narrowing needs a SECOND
bloc-weight spelling, which **HZ5 forbids** (`conquestVoteWeight01` is
deleted-and-pinned-absent; no second court-vote-weight under any spelling). Both
points are TRUE — and they do not reach the question, because the SAME VOLUME
carves the chooser out by name **four sections later**:

> `settlementStrategy` (the war chooser, 812 effective, frozen surface — **"NOTHING
> touches the chooser", IN-4**) is NOT wired: war declarations do not wait on spies
> in this program. **RECORDED DEFERRAL, owner-visible — wiring the war chooser is a
> FUTURE OWNER RULING, not an oversight.** — DESIGN_FP_ARCH_ES.md:1189-1192

⚠⚠ **THE READS ARE BOTH RIGHT AND THEY DISAGREE:** §3.11 authorises the reach into
the LADDER + factionCompetition; §3.12 forbids the reach into the CHOOSER and parks
it with the OWNER. The lane's insertion at `rulingBlocOf` routes through
`blocDecisionFactor` into `settlementStrategy.js:1223` — so as drafted it EXECUTES a
change the design reserves for a future owner ruling. **Accepting it "with disclosure"
would have discharged an owner-parked item by lane momentum.**

**RULING: ES-5b is BLOCKED, not READY.** It unblocks by EITHER
(a) a MEASURED control proving the chooser's inputs and outputs are byte-unchanged
    with the discount live (then the §3.11 reach is all that remains and the packet
    promotes), OR
(b) an OWNER RULING authorising the wiring — which is the owner's alone: the design
    named it a future owner ruling, and [[judgment-ledger]] §3 makes anything the
    owner has explicitly parked owner-gated regardless of delegation breadth.
⛔ A third path — narrowing by minting a second bloc-weight spelling — is FORBIDDEN
by HZ5 and must not be attempted.

**Why: the lesson is that a design volume can AUTHORISE a mechanism in one section
and CARVE OUT one of its consumers in another; a reach argument is not complete until
you have searched for the carve-out, and "the machinery already exists" is never
itself authority to touch a frozen surface.** Also recorded: the war chooser is
`settlementStrategy.js`, 812 effective lines, FROZEN — [[first-match-document-pin-class]]
neighbours it, and it is the surface [[owner-risk-appetite-directive]] would still
gate even at the boldest setting.

## ⭐⭐ CR-ES5B-5-R2 (chair, 2026-08-11) — THE BLOCK WAS WRONG; MEASUREMENT REFUTED IT
### The owner gate is LIFTED. Recorded out loud; the prior ruling above is NOT rewritten.

⚠⚠ **I BLOCKED ES-5b ON A MISATTRIBUTED CARVE-OUT.** All three refutations verified at
source by a measurement lane:
1. **Section boundaries: §3.11 = 1103–1156, §3.12 = 1157–1193.** The "NOTHING touches
   the chooser" text at :1189-1192 lives in **§3.12 SPY-BEFORE-DECISION**, its subject
   is `deliberationRead`, and it is **CR-ES-4 answering Q4** — not a `presentShare01`
   ruling at all. ⚠⚠ **I read a carve-out from a NEIGHBOURING section as if it governed
   mine.**
2. **§3.11:1122-1125 names the wire BY NAME as authorised** — "as a multiplier where
   `rulingBlocOf`/`blocDecisionFactor` weigh that faction… dark worlds are
   BYTE-IDENTICAL, **and the one-time bloc-math shift**". The shift is ANTICIPATED in
   the same sentence; `blocDecisionFactor`'s sole consumer IS the chooser.
3. **"NOTHING touches the chooser" is a NO-EDIT law** (DESIGN_FP_ARCH_IN.md:483-485,
   "no chooser edits… AT its 812 baseline", an exact `.size-baseline.json` ceiling).
   ES-5b edits ZERO lines and adds ZERO wires — `blocDecisionFactor` was already wired
   by W-DOCTRINE-4. **A no-EDIT law is not a no-VALUE-CHANGE law.**

**⭐ THE REAL DEFECT IS THE REVERSE OF WHAT I FEARED, and it is chair-side:** §3.11's
claimed "machinery that already exists… with no further code" **DOES NOT EXIST** —
`factionCompetition.js` does NOT import `settlementPolitics.js` and holds ZERO bloc
tokens; the ladder reads only a BOOLEAN (`rulingBloc != null`), inert except at a floor
crossing. The ES-5 charter (:1506-1514) **BUDGETS factionCompetition ≤ 10 composition
lines** — the reach was to be BUILT — and the draft's D2 **DROPS that edit**. So as
drafted ES-5b delivers ~NONE of its intended reach while flipping war-move selection
(measured: `deploy` → `fortify`; a 1% discount already moves the multiplier; at
`presentShare ≤ 0.2` the bloc falls under `RULING_CONSOLIDATION_FLOOR = 0.4` and every
multiplier collapses to 1). ⭐ Dormancy control: `s ≡ 1` is BYTE-IDENTICAL — dark is safe.
⭐ **The algebra that forecloses re-siting:** `share` is a RATIO, invariant only under a
COURT-WIDE CONSTANT discount, so no per-faction shaping escapes the chooser. The
uniform-discount negative control (c ∈ {1, 0.75, 0.5} → identical) proves the probe
non-vacuous.

**Why: a design volume's prose can APPEAR to carve out a consumer while the carve-out
actually belongs to a neighbouring section's different question — so verify a
carve-out's SECTION BOUNDARIES and its RULING ID before blocking on it, and check
whether the law is about EDITS or about VALUES. Blocking on a misread costs a lane and
an owner's attention; the fix is to send the measurement lane FIRST, which is what
finally produced the truth here.** [[first-match-document-pin-class]] is the same
disease in a document.
