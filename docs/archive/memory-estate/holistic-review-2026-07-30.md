---
name: holistic-review-2026-07-30
description: "14-agent holistic codebase review of minifold@018e4119 — verdict late-hardening, zero criticals, 5 highs; sibling-gap failure mode named; state reconciliations (fold LANDED, lanes committed, ingest-JWT claim stale)"
metadata: 
  node_type: memory
  type: project
  originSessionId: c69772bd-e324-4e1d-b1ee-b7482c7741fc
  modified: 2026-07-31T15:08:17.475Z
---

2026-07-30: owner asked for a comprehensive holistic read. Ran 12 parallel subsystem reviewers + completeness critic + interior gap-closer against the code of record (minifold worktree, claude/composite-r4 @ 018e4119, clean tree). Full structured reports: session scratchpad `tasks/digest_full.json` (ephemeral) — headline findings restated to the owner in-session.

**Verdict:** late hardening / launch tail. Zero critical findings. Subsystem maturity: kernel-generators, tests-gates, build-performance = launch-ready; everything else = hardening. Executed receipts: 523-config generator golden master GREEN at HEAD (3 tests, 24.6s, exit 0); first-paint closure 1,035,454 B vs 1,040,000 budget (4,546 B margin).

**The 5 highs:**
1. NPC rename two-lane divergence + reload-alias staleness — the exact class queue #14 cured for factions, uncured for NPCs (store lane writes only `npcs[idx].name`; members chips, `relationships[].npc1Name/npc2Name`, interSettlement `npcName` go stale on reloaded saves; shipped Change Dock routes through the store lane). settlementSlice.js:1336-1349.
2. 71 undeployed migrations (122–192): whole money-hardening stack (refund obligations, webhook leases, clawback 190, tier multiplier 192) code-complete, not live (applied-head 121). Owner-gated deploy; "no partial refunds until webhook train deploys" still standing.
3. customRegistry factions/traditions omission CONFIRMED still open — rivals picker un-authorable, live custom factions render "Deleted custom item", consumer-evidence walker token-scan certifies the half-dead surface green. Presentation-only containment verified.
4. Interior (dormant, unwired): `toPublicSafeInterior` retains `seedFork` embedding the raw settlement seed — latent (no serializer consumes it), one-line fix at wiring time; SVG export lane not fail-closed for covert geometry (UVTT is).
5. (Critic-class) Review coverage itself: src/lib ~2/3 unread by any owner-reviewer, public/map vendored FMG app (~63k lines) never audited beyond its (exemplary) bridge.

**The named systemic failure mode — SIBLING GAPS:** the machinery (ratchets, walkers, goldens) kills local regressions; remaining defects are cured classes with uncured twins: faction rename cured→NPC rename not; settlement export faith seam→realm jsPDF (Campaign PDF/World Book print deity names UI-gated only); UVTT covert scrub→SVG lane not; EP walkers scan 4 trees→~1,200 test files unscanned; mutation manifest 199/447 uncovered; money coverage floors ~0 (stripe.js 0%). When hunting edge cases, hunt at coverage BOUNDARIES.

**Why:** this is the freshest whole-tree ground truth and the sharpest edge-case worklist.
**How to apply:** before proposing fixes in these areas, re-verify against HEAD (tree is live); treat the sibling-gap list as the edge-case backlog seed. See [[exhaustive-remediation-architecture-doc]] for the 202-item program these findings reconcile against.

**OPUS VERIFICATION PASS (owner-ordered, same day):** 14 Opus 5 adversarial verifiers re-derived all 95 findings + surfaced 1 new → 39 CONFIRMED / 52 ADJUSTED / 5 REFUTED. Register document delivered to owner: `scratchpad/SETTLEMENTFORGE_VERIFIED_FINDINGS_2026-07-30.md` (session tmp — ephemeral; regenerable from digest_full.json + verdicts_full.json in the same tasks dir). Post-verification distribution: 0 critical / **2 high** / 21 medium / 59 low / 9 info. Key corrections:
- ⚠️ **mountain_pass UPGRADED medium→HIGH**: not neutral — falls through BOTH arms of every connected-vs-disconnected branch (foodBalance.js:192-202, foodGenerator.js:282+), so importCoverageRate hits terminal 0: mechanically WORSE than `isolated` (which gets minor-route rates). The two remaining highs = mountain_pass + the 71-migration deploy gap.
- **NPC rename lane attribution was BACKWARDS**: the store lane (`renameNPC`) is UNREACHABLE from shipped UI (zero 'rename-npc' queueEdit producers); the LIVE lane is the library lane (SettlementsPanel), which cascades relationships but misses `factions[].members[]`. high→medium.
- **NEW live defect**: World Book 'Under siege' prints `- undefined` per row — generateWorldBook.js:407 reads `sg.id` but liveSieges emits `targetId` (sibling generateCampaignPDF.js:824 gets it right). One-token fix; paid premium artifact.
- **regenSection('history') authored-loss is LATENT, not live** (REFUTED as live): QUEUE_WIRED_PROSE_PATHS refuses historicalEvent/currentTension kinds (`prose_path_deferred`), no UI can author those entries today — supersedes the "STILL OPEN" framing in [[regen-edit-loss-hazard]]; returns to medium the day a UI wires those kinds.
- **pinnedNpcs mis-target is LIVE** (upgraded from inference): `aiData.pinnedNpcs` is npc-id-keyed and nothing remaps it after locked-roster regen (only `state.locks` gets remapReport) — the pin silently follows the slot stranger, persisted to cloud. aiSlice.js:1087-1098.
- The critic's claim that kernel-generators mischaracterized aiLayer.js was itself REFUTED (the original finding stood, low→info).

**FIX WAVES LANDED 2026-07-30 (owner: "fix all of them… I give all permission"):** wave 1 = 8 commits e1adc7bd..ce852c8a (48 fixes); wave 2 = 6 commits aa33eba5..b66e9551 (22 fixes incl. FOUR OWNER RULINGS: mountain_pass seasonal tier — golden 523→525, 2 ADDED 0 CHANGED; coup guaranteed admission — 0/4 golden rows changed, saturation fixture pins it; BYOK fail-closed 503; realm faithUnlocked seam via resolveFaithUnlocked, the census's ONE spelling). Full gate green at both folds. Ledger row appended on review-fixes branch. ⚠️ Edge-bundle reseal ORDER: distillate regen BEFORE build:edge-shared (intentAtlas bundle bakes the distillate; wrong order costs a full gate run). ⚠️ New-test manifest entries: use kind:rationale ref negative-control-executed-2026-07-30 — never grow uncovered. NEXT (owner-ordered): soak:release+soak:research (30/100/300y, launched ~22:10) → tuning over soak evidence → wave 3 programs. NOT authorized-by-blanket: prod migration deploy, budget raises.

**SOAK LADDER RESULTS (2026-07-31, owner-ordered 30/100/300y):** release profile: scale sweep + BOTH 30y×12s seeds fully green (byte-identical replays, worker==direct hashes, bounded perturbation, divergent fates, 4-majors cadence). ⚠️⚠️ **100y×4s FAILED — THE ZOMBIE SETTLEMENT CLASS**: soak-d (village, 629) hit population **0 in YEAR 4**, flatlined 0↔24 for six decades while remaining a full sim participant (terminal death never fires — the settlementLifecycle SILENT finding now has a live victim); small-realm stasis: stressors ~0 for 99 years, mover selection monopolized by politics (4,905 selected vs population 0, war 5, knowledge 0), majors collapse to ~1/decade in the final 20 years. Year-4 zero = REACHABLE IN PRODUCT CONFIGS (not a century artifact). Receipt: artifacts/soak/release.cases/release-100y-4s-seed1.json (passed:false). 300y×12s research leg relaunched separately. ✅ **ZOMBIE CURE LANDED @ 6f74870e** (owner ruled "do it" option 1, 2026-07-31): pop>0 eligibility inversion removed; empty fast path floor 4 / clear 32 / dwell 13 (zeroSince stamp survives 0↔24 bounces); certainty not lottery; organic path unchanged (remnant, relic-ruin scarcity, zero pop moved); dormancy byte-identical; worldpulse goldens ZERO rows; full gate green under soak load; design doc §2 carries the fast-path law. Small-N stasis (stressor starvation, politics mover monopoly) routed to the TUNING agenda. 100y×4s re-run = cure proof, queued post-300y. Failing receipt preserved in session scratchpad. ⚠️ soak chain `release && research` aborts research on release red — run legs separately.

**State reconciliations (supersede older memory flags):**
- Minifold FOLD LANDED — [[minifold-dead-op-fold-coordination]] hold is OVER; tree clean at 018e4119.
- Previously-UNCOMMITTED lanes are COMMITTED: faction-rename convergence + reroll locks @ b89a2025; EP-l S3 self-capture @ 11c295c1; soak route decorrelation + mountain_pass vocab @ 5f8dc783; T5 ONE REGEN recorded EXECUTED 2026-07-28 in minifold plan (ledger branch is one landing BEHIND — its newest row does not record T5).
- mountain_pass at HEAD: narrative/terrain semantics exist; tradeRouteSemantics.ROUTE_TIER has NO entry (scores neutral 'unknown') and GENERATED_ROUTE_VALUES omits it — matches the recorded owner-gated deferral, NOT lost work.
- STALE CLAIM KILLED: "analytics-ingest has NO JWT" ([[exhaustive-remediation-architecture-doc]] grounding note) — verify_jwt=false is deliberate anonymous-sink posture; the function verifies presented JWTs via auth.getUser and clamps consent server-side, with layered compensating controls.
