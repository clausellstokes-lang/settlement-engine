---
name: netcurrent-extractor-anchor-class-closed
description: "Unanchored net-current extractor class FULLY CLOSED + COMMITTED + PUSHED on minifold 2026-07-27 (d0fdcf7c + 24c85e7f + e2e3852e + 20c195e7) — 62-site shrink wave, 8-detector walker, standing sweep plant 55, origin/composite-r4 created"
metadata: 
  node_type: memory
  type: project
  originSessionId: 345e65c8-4bd3-4f8d-989b-2db845f6ac55
  modified: 2026-07-28T00:47:03.221Z
---

UNANCHORED NET-CURRENT EXTRACTOR class FULLY CLOSED, COMMITTED and PUSHED on minifold (claude/composite-r4), 2026-07-27, four commits: **d0fdcf7c** (wave 1: 12 files anchored + walker + carved manifest entry), **24c85e7f** (wave 2 shrink: 62 sites across 50 files + migration-ordering.mjs + sourceContract.sqlFunctionBody + carved surveyorByokHealth), **e2e3852e** (standing sweep plant = step 55 + manifest entry flipped kind:rationale→mutation, label "extractor-anchor/unanchored corpus extractor planted"), **20c195e7** (wave 3: moneyRpc belt FOLDED — walker green at bare HEAD; detector 8 = direct indexOf/search substring extractors, which instantly caught 5 boundary-slicing sites in deletionBillingRace, all offsets parity-identical). **origin/claude/composite-r4 CREATED at 20c195e7** (owner-ordered push 2026-07-27 ~17:00, `--no-verify` per the pre-push hook's own emergency clause — the hook gates on the dirty shared TREE, not the pushed history; CI on the pushed commit is the backstop and deploys stay CI-gated fail-closed). Foreign-red closure same day (all UNCOMMITTED, ride their lanes' folds): 4 E-A rationale entries for the foreign untracked tests (effectReachability/distributionEnvelope/negativeAssertionAnchor/seedLoopTotality — E-A 6/6 in-tree), deitySnapshot + locksPreservation any-holes → real types, ruinFilter → RUIN_AGNOSTIC_EXEMPT (lock-map ids are ruin-agnostic), GenerateWizard's new lazy LockControls boundary narrated, aiGrounding bundle regenerated (ONLY it changed — other four rebuilt byte-identical), probeTier red self-closed by its lane (adjudicated: test side was stale, their fix correct). pricing.test.js CANNOT fold alone — imports getTierMultiplier from foreign-dirty src/config/pricing.js; rides the L-5 fold.

House law: every regex over `create or replace function` in migrations is spelled `^create…` + m. Two sanctioned deviations: migration-ordering.mjs's classifier uses `^[ \t]*create…` (indented DDL/fixtures legal; comment prose still can't match — `--` precedes), and five NEGATIVE-PRESENCE sites stay unanchored BY DESIGN (dossierEntitlements ~424/425/441, gallery_privacy ~128/129 — they catch future re-creations at ANY indentation; anchoring would weaken them; each commented at the site).

**Standing machinery — maintain, don't bypass:** tests/lint/netCurrentExtractorAnchor.walker.test.js — SEVEN detector spellings (escaped literal, escaped RegExp string, plain-space literal, plain-space RegExp string, alternation, optional-group, kind-group with the `^[ \t]*` exemption), a corpus column-0 invariant over supabase/migrations (an INDENTED real definition reds — this is what keeps anchored extraction complete forever), FROZEN_UNANCHORED shrink-only = {moneyRpc:3 negative controls, dossierEntitlements:3, gallery_privacy:2}. Sweep step 55 = the standing plant (check_caught, target aiSpendSafety must stay tracked-clean at sweep time). CANNOT-CATCH (honest gaps): indexOf substring extractors (sourceContract's was converted; future ones invisible), create-trigger/policy sibling families, `\s*` spellings, scripts/*.sh.

**Receipts:** per-site parity 0 diffs on every changed regex (agents executed byte-compares per site; corpus: zero indented creates in 535 occurrences); corpus-wide 369 names → exactly 2 movers (current_user_is_privileged prose, phantom enforce_allocation_); definedObjects parity → only the phantom lost, zero first-index movement; sqlFunctionBody byte-identical on both real consumers; plant red-then-green twice (build-time md5-verified + hand-executed step-55 sequence).

**Why:** text-asserting suites stay GREEN over prose; wave L-5 fed Postgres ~11k chars of English; migration-ordering.mjs was LIVE-harvesting both prose phantoms until 24c85e7f.

**How to apply:** new extractor → copy tierCreditMultiplierSql/moneyRpcNetCurrentGuards. Never anchor a `.not.toMatch` create-regex (negative-presence) — pin it in FROZEN_UNANCHORED instead. New unanchored spelling variant → add a detector + fixture pair, don't widen an existing one.

**Shared-tree lessons that bit this program (2026-07-27):** (1) plumbing commits (temp GIT_INDEX_FILE + commit-tree + CAS update-ref) are the right tool when the shared index is mid-use by a concurrent lane — but they leave the SHARED index stale, showing phantom staged reversions that a concurrent `git commit` could carry; ALWAYS `git reset -- <committed paths>` immediately after update-ref. (2) The Bash tool's cwd RESETS to the session's primary worktree on every new turn — `cd <worktree> && git rev-parse --abbrev-ref HEAD` must prefix every git-touching command or you operate on the wrong tree (bit twice: a wrong-tree read recon and a wrong-tree index staging, both recovered). (3) PGlite cold-start is ~19s under a co-tenant gate (load avg 150) — 20s-timeout suites flake as pure noise; raise --testTimeout/--hookTimeout for the run and attribute, never chase.

**PROGRAM EPILOGUE (2026-07-27 evening):** full 55-step mutation sweep executed in an ISOLATED temp worktree (clean tree satisfies the dirty guard; check_caught reverts from ITS OWN HEAD — the only safe way to run the sweep beside a live shared tree; symlink node_modules in). First run at 20c195e7: 53 CAUGHT / 2 gaps — BROKEN domain-strict (the R-5b-documented pre-existing economyFreshness TS7053, cured with the prescribed Record cast) and MISSED step 24 (⚠️ CLASS LESSON: the plant's python anchor rotted when buildNewCampaign moved out of campaignSlice — check_caught attributes the GATE but NOT the plant's own success, so a failed sed/index = silent MISSED; re-anchored with index() raising loud). Both fixed @ dfd04dd3; re-run: **55/55 CAUGHT, spine holds**. Pushes: origin/claude/composite-r4 CREATED @ 20c195e7, then advanced ..e56cbeaa (the twin's epistemic-prevention fold — which absorbed my four E-A entries, no duplicate keys — landed atop my dfd04dd3 seconds before the second push). Still riding foreign folds (worktree-only): locksPreservation typings (strict 0 + anycast green in-tree), GenerateWizard narration, ruinFilter exemption row.

**FOLD-THE-TREE EPILOGUE (2026-07-27 late, owner "finish the rest here"):** four lane clusters proven FOLDABLE-NOW by a 7-agent fold-readiness workflow (each agent EXECUTED the cluster's suites + eslint + chased imports both ways) and folded by this session: **9d3e1414** LOCKS = owner-queue #21 BOTH arms (locks engine live + 12 dead ops retired, dead-op ratchet 36→22, compendium regen 182→170; settlementSlice committed via carved blob leaving the deity opts seam), **9ea94dc1** DEITY (applyWorldPulse ref chain, `converted:` namespace gone, restore-from-world opts seam, manifest entry carved), **831ba5c0** PROVENANCE (rosterProvenance + OverviewTab badges + single-writer walker), **92b88c21** PAYMENTS+TIMEOUT (pgliteHookTimeoutRatchet standing + six suites adopt the boot allowance; surveyorByokHealth's timeout hunk finally lands). Pushed e56cbeaa..92b88c21. L-5/pricing had ALREADY folded via ada1252d (its own lane). NOT folded, with reasons: CUSTOM-CONTENT cluster (Theme B/#24 program record absent from docs entirely — another lane's record to write; ~26 dirty paths remain incl. SummaryTabV2/SystemStateBar display work + premiumGate/aiCharter manifest entry dirt); CI check impossible locally (no gh CLI/token — Actions tab is the owner's watch). Manifest carve rule held throughout: one entry per fold, python-spliced by key boundaries (never add -p, never whole-file).

**SIBLING-FAMILY SWEEP (2026-07-27 night, owner-greenlit): create-TRIGGER/POLICY class closed @ 064bb460** (pushed 92b88c21..064bb460): 25 sites anchored across 20 files (classification per-site parity executed; one real phantom killed — profileEscalation's policy enumerator minted a row from 009:8's prose), 11 negative-presence sites pinned with site comments, walker grows 4 t/p detectors (12 total). ⚠️ FAMILY ASYMMETRY LAW: triggers/policies have NO corpus column-0 guarantee (005:69 DO-block EXECUTE policy; 003:65 + 004:49 indented DO-block trigger DDL) — anchor a t/p extractor ONLY after verifying that site's own input; negative t/p guards NEVER anchor; corpus col-0 invariant stays function-only. ⚠️ MULTI-LINE RegExp HAZARD bit once: adding `^` to a pattern whose flags string sits on its OWN line without flipping 'i'→'im' = string-start-only match, loud red (ownerConfirmedPrivacyDeletes; scan proved no sibling slips). FROZEN row law amended: a detector-family widening may re-freeze a DELIBERATE row upward (gallery_privacy 2→3), documented in the inventory header — real extractors never raise rows. CI verified via the in-app browser (repo is PUBLIC): #417/#418 failed at the strict ratchet (cured @ dfd04dd3), #420 got past strict and died on ci.yml's own `timeout-minutes: 10` (the full suite exceeds it — the known cap hazard, now in CI); deploy gate SKIPPED-held on every run; e2e red on all branches incl. master (standing); gate-repair territory = the twin's active lane. claude-in-chrome extension NOT connected on this machine (install/sign-in needed for logged-in browsing).

**Foreign reds at close (all attributed, none mine):** aiGrounding freshness (declared staleness), domainAnyCastBaseline + ruinFilterRoster (foreign new src/domain/deitySnapshot.js + locksPreservation.js), loadingNarrationRatchet (foreign new silent boundary), E-A TOTALITY (4 foreign untracked tests: effectReachability.coverage, distributionEnvelopePower, negativeAssertionAnchor.walker, seedLoopTotality.walker), surveyorProbeTierSql (foreign lane in-flight). The intentAtlas NUL was fixed+folded by its own lane @ a794a4f8 (controlBytes green, verified). See [[generation-remediation-gate-state]], [[enforcer-ea-mutation-shipped]], [[wrong-lineage-worktree-trap-2026-07-14]].
