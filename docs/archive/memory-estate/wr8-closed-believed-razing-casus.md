---
name: wr8-closed-believed-razing-casus
description: WR-8 IS CLOSED — WZ-5r repair @ 1b7c1eac/39ba6590 verified ACCEPT 2026-08-04 after the era's first rejected close; casus + attribution laws + the forgetting design fact
metadata: 
  node_type: memory
  created: 2026-08-04
  tags: 
    - wr8
    - war
    - razing
    - atrocity
    - closed
    - design-fact
  commits: 
    - 19dd07e2
    - e36588c7
  originSessionId: 1c189f3d-9fc3-4fc9-9cb7-b767525d28d5
  modified: 2026-08-04T09:10:29.757Z
---

# WR-8 IS CLOSED — via the era's first rejected-then-repaired close

## State (final, 2026-08-04)

**WR-8 (conquest + the razing) is CLOSED at `1b7c1eac` + `39ba6590` (lane WZ-5r),
verdict "ACCEPT — WR-8 IS CLOSED"** from an independent verifier on its OWN
5-settlement prefix-adversarial harness with a discriminating negative control
(the leak commit moves 3/6 cells on it; base↔HEAD all six byte-identical).
History that must stay remembered: the first CLOSES declaration @ `e36588c7`
was PREMATURE — verification REJECTED it (the `channel_inferred` vocabulary
leak, see [[ghost-write-class-cured-at-the-writer]], + a strict red
mis-reported green). The repair: membership-after-normalization typedEdge gate
(CR-WZ5-A), strict cured at the three callers (CR-WZ5-B, ceiling 1313 honestly
met, no re-baseline), ledger corrected by APPEND (the false row preserved,
never rewritten). J-WZ5R-1 (three-spelling guard superset) and J-WZ5R-2
(materializer population bound) both chair-AFFIRMED after independent
corroboration.

⚠️ **F1, OWNER-GATED, pre-existing:** DM-facing prose still renders raw
`channel inferred` (relationshipRuleHelpers.js:181-182, relationshipMemory.js:308)
from the MATERIALIZER population — base-identical, pinned at
tradeWar.test.js:484; closing it moves same-seed hashes = THE PROMISE gate.
Recorded for the owner queue; do not "fix" without a signed shift.

Item 4's four quarters: 4a observer-axis judgment `2654984a`, 4b deterrence consumes
`readAllianceWebRisk` `506598af`, 4c the Herald home `e5ceb2f2`, 4d the believed-razing
casus `19dd07e2`.

**FOUR OWNER-GATED ARMS ARE PARKED, NOT OWED** — do not treat the closure as covering
them: the K1 severity carry (`dmSeverity` on a PERSISTED institution-status record,
R-WZ-2-REVISED), seat ransom, ransom persistence, counterpart re-mint. Each is a
persistence-shape or paid-surface decision the owner holds.

**J-WZ2-1 stands:** the razing is still a MINOR outcome; registering it MAJOR waits on
pulseKernel headroom that R-BLD-10 banks permanently at 1580.

## The mechanism worth remembering

`src/domain/worldPulse/believedRazings.js` produces what `scoreAtrocityAnswer` had
taken since W-PEACE-1 with **no producer at all**. It reads the PUBLIC `wizardNews`
feed and only that, admitting an entry once
`entry.tick + routeAwareHopDelayTicks(victim → observer)` has arrived. **Zero new
persisted surfaces.**

⚠️⚠️ **ATTRIBUTION IS BY PROOF, NEVER BY PARSING.** `razingOutcomeIdFor` in `razing.js`
(the zero-import law leaf) is THE one spelling of
`world_outcome.razing.<road>.<razer>.<victim>.<tick>`. The emission MINTS through it and
the reader RECONSTRUCTS through it and compares for EQUALITY. **Never split that id on
`.`** — settlement ids contain dots, and a mis-parse accuses the wrong court, which
under R2's license machinery is a warrant to burn a city. The ROAD rides in the id so
the JUST-razing polarity (a vengeance answer mints no fresh casus against the avenger)
is decidable by reconstruction over the closed `RAZING_ROADS` vocabulary.

## ⚠️⚠️ THE WORLD FORGETS THE SMALLER FIRES — a DESIGN FACT, not a bug

`wizardNews` caps at **MAX_ENTRIES 240** while **ATROCITY_DECAY_TICKS is 260**. In a
busy realm a LESSER razing scrolls out of the feed *before* its outrage has decayed,
and the casus dies early. `capEntries` rescues one head per MAJOR arc and
`newsEntryForOutcome` grades major at **severity ≥ 0.72**, so the biggest burnings
persist for their full band and the lesser ones may not.

That asymmetry is CHARACTER — a realm remembers the atrocity that horrified it and
loses the one that merely appalled it. It is in the module docstring, DRIVEN THROUGH
THE REAL CAP in `believedRazingCasusWr8.test.js`, and in the queue row.
**Do not re-find it as a bug and do not "fix" it without an owner ruling.**

## Two rulings that constrain future edits

- **J-WZ5-1** — news speed gates on `distancePricedNewsEnabled`, the flag every other
  consumer already reads. An ungated world hears instantly. Do not invent a second gate.
- **J-WZ5-2** — **THE VICTIM holds no atrocity casus of its own.** Its cause rides the
  grievance/revanchism clocks; scoring both counts one fire twice under two names
  (J-WZ4-3 applied a second time). This was UNPINNED and a mutant survived on it —
  the pin now proves the pair exists at distance zero *before* asserting the absence.
