---
name: es-d-refused-the-product-intent-is-undeliverable
description: ⛔⛔ ES-D REFUSED on seven counts — a court can only spy on the court it is suing for peace with, so §3.12's espionage product intent is NOT DELIVERABLE at this HEAD by any packet
metadata:
  type: project
---

**MEASURED 2026-08-11 at `ffc85a90`.** ES-D is **REFUSED FORWARD**. Draft: scratchpad
`esd-packet-draft-ES-D.md`. **There is no seam at which an autonomous per-tick stage can
mint a covert mission.** The only road to a persisted covert row is
accepted-peace-decision → `applyWorldPulse.js:627` → `envoyDiplomacy#dispatchAcceptedPeaceEnvoy`
→ `mintEnvoyErrand` → `writeErrands`. **ES-D is a CARGO DECISION at an existing dispatch,
and the only dispatch that exists is a peace embassy.**

⛔⛔ **R3 IS THE STRUCTURAL BLOCKER, and it is bigger than any wave:**
`envoyErrandRecords.js:590` — `if (payload.offererId !== from || payload.targetId !== to)
return null;` (plus `:568,:580` forcing the id). **A COURT CAN ONLY SPY ON THE COURT IT IS
SUING FOR PEACE WITH.** §3.12's product intent is **not deliverable at this HEAD by any
packet.**

Other refutations: **R1 — `mintCovertMission`'s `fields` HAVE NO WRITER**; `mintEnvoyErrand`
is a SIBLING over the same producer (it calls `mintErrandSpine` itself), and **no function
in `src/` accepts a pre-built spine block — the ES-1 "door" is a VALIDATED DEAD END.**
R2 — executed: its own default `purpose:''` throws `covert_face_required`; only `sue` and
`self_parlay` mint. R4 — the only route producer is war-flag gated and solves ONE
origin→destination, so a multi-stop covert itinerary has no producer. R5 —
`castCovertOperative` is unusable: the site already casts by diplomatic law (one traveller,
two casting laws). R6 — `volunteerBandsFor` has ZERO producers and a `careful` seat accepts
everybody, so **ES-6b is NOT unblocked either.** R7 — **both pulse mouths at EXACT zero
headroom** (`applyWorldPulse.js` 941/941, `pulseKernel.js` 1580/1580, enforced both
directions) and `grep espionage pulseKernel.js` → **no match**: there is no stage registry.

⚠⚠ **COUPLING DIRECTION — THE CHAIR HAD IT BACKWARDS.** `direction =
${depLayer}→${importerLayer}`, so a GRAMMAR file importing an INFO leaf is **`INFO→GRAMMAR`**
— the opposite of the `GRAMMAR→INFO` every existing espionage row uses. ⚠⚠ **NOTHING
cross-checks the id's arrow against `direction`, so a wrong one SHIPS GREEN.**
⭐ The three-file registration bug is now **PROVEN FROM GIT**: ES-5b/5c/5d/6a each declared
only the leaf and each also touched `couplingRegistry.js` + `couplingRegistry.test.js`.

⛔ **OWNER-GATED:** the smallest split, **ES-Da (covert cargo on an already-accepted peace
envoy)**, is owner-gated on THREE grounds — it lights **five landed-dark waves at once**
(new capability, not repair); **every accepted peace envoy becomes a potential spy**
(war-lane product behaviour); and §3.12's carve-out is adjacent enough that CR-ES5B-5's
both-directions-misread precedent applies. ⭐ The lane recommends AGAINST a dark-gatherer
alternative — it would be a SIXTH dark wave on a volume already diagnosed as all consumers
and no producer.
