---
name: archive-2026-08-lane-and-census-mechanics
description: FOLDED VERBATIM from MEMORY.md — how censuses, archives, background gates and parallel lanes lie to you. Moved 2026-08-11 because the index had overflowed its read limit.
metadata:
  node_type: memory
  type: archive (index fold)
  created: 2026-08-11
---

# Lane, archive and census mechanics — index rows, folded verbatim

⚠ MOVED, not rewritten. Every link, sha and figure stands exactly as it did in MEMORY.md.

- ⚠⚠ **ARCHIVE-CENSUS LEGS THAT LIE** — [an old sha's own script fake-GREENS the base half](gate-mutex-run-mode-absent-at-old-shas.md): tell = an EMPTY LOG beside exit 0 · [link the WORKTREE'S OWN node_modules](archive-census-node-modules-leg.md): tell = a ZERO-TEST suite · [an ABSOLUTE-path pin fake-REDS on the checkout's own directory name](absolute-path-substring-pin-fake-red.md): name census trees `<LANE>/tree` · ⚠⚠ [a SIBLING LANE'S pkill kills workers a SYMLINKED node_modules made indistinguishable, and they come back as SKIPS](killed-worker-presents-as-skip-ceiling-red.md): tell = vitest's own `numPendingTests` disagreeing with the row count; cure = `cp -Rc`. ⭐⭐ **THAT TELL IS NOW MACHINERY** → [the scope-collapse sentinel](test-ratchet-scope-collapse-sentinel.md): the count disagreement DETECTS and the degenerate clock NAMES, so a collapse never again reports as skip-ceiling growth; ⚠⚠ compare against pending **plus todo**, never pending alone.
- [⚠⚠ A NEW TEST FILE REDS **TWO** CENSUSES](a-new-test-file-reds-two-censuses-at-landing.md) — the lighting `files` pin AND the anchor walker (new files start at ceiling **0**); a focused re-earn is NOT a gate; tell = `??` under `tests/`.
- [⚠⚠ THE SKIP CEILING IS DIST-CONTINGENT](test-ratchet-skip-ceiling-is-dist-contingent.md) — a stale `dist/` steals 50 skips and looks like an honest ratchet-down; step 12 runs BEFORE build so the canonical env is DIST-FREE; `--update` self-wraps the mutex.

- [⚠⚠ CROSS-LANE `pkill -f` KILLS THE OTHER LANE'S CENSUS](cross-lane-pkill-kills-the-other-lanes-workers.md) — an archive's SYMLINKED node_modules makes its workers advertise the MAIN tree's path, so argv is NOT a lane discriminator; ⚠⚠ `--update` does NOT enforce the vanished guard, so a killed census SILENTLY SHRINKS.
**Verification hazards this era:**
- [⭐⭐ TWO GROWTH RULES WERE PROSE-ONLY — FOUND, MEASURED, CLOSED](unlayered-census-growth-rules-are-prose-only.md) — both rules now EXACT; ⚠⚠ read the ASSERTION, never the test title; ⚠ the PAIR baseline still has no size pin.
- [⚠⚠ AN ALWAYS-PRESENT THROWING DELEGATE NEUTERS EVERY DEFENSIVE `?.`](always-present-throwing-delegate-neuters-optional-chaining.md) — ⭐ SWEPT @ acb35412 — the axis is PRODUCTION-store read vs injected-`get()` read, NOT gatedness; ⚠⚠ **HEAD 9df7e428 IS RED on 8 files / 40 tests — a green shared tree hid it — a sibling lane held the cure UNCOMMITTED**, so gate a MATERIALISED candidate + parent.
- [⭐⭐ THE ESPIONAGE→LADDER HANDOFF IS SAME-TICK, AND THE PACKET+DESIGN BOTH SPECIFIED A DEAD WINDOW](espionage-ladder-handoff-is-same-tick.md) — the depositor runs EARLIER IN THE SAME PULSE than the consumer, so `tick-1` never fires and every unit pin stays green; ⚠⚠ the lag had been TRANSFERRED from a different producer/consumer pair — re-derive lag from the pulse CALL ORDER, never inherit it.
