---
name: ""
metadata: 
  node_type: memory
  type: project
  date: 2026-07-22
  branch: claude/legibility-wave (worktree wave-b; based on composite-r4 tip d097f180)
  status: "shipped, 5 commits, full gate green (awaiting manager fold)"
  tags: 
    - legibility
    - library
    - dossier
    - ux
    - owner-vetoable
    - glossary
    - deferred
  originSessionId: 230c87b4-b237-4d1b-a63e-af48dfb233a0
  modified: 2026-07-22T21:54:10.446Z
---

# LEGIBILITY WAVE — Library + dossier half (SHIPPED)

The regular-human LEGIBILITY-LAW audit, Library + dossier side. Realm-Inspector
side was HARD-FENCED (the Herald rebuild owns src/components/map/**). Display-only:
no engine/generation/persistence edits, no record-shape changes, goldens unshifted.

## Why (the durable facts a successor needs)

Five commits on `claude/legibility-wave` in worktree wave-b:
- 67f2e458 item 1 — library toolbar
- 53d04532 item 2 — settlement ledger rows
- 88222e9c item 3 — campaign folder cockpit
- 8c8be981 item 4 — library shell + bulk flow
- 27760d90 item 5 — dossier tab names

## Vetoable renames / behaviour shifts (each is one string / one gate; say "veto")
- "Linked" -> "Has neighbours" (LibraryToolbar hasNeighbours chip).
- Ledger column heads Tier->Size, Phase->Status, Standing->Health (UnassignedLedger
  VISIBLE heads + CampaignFolder SR-ONLY heads). Pinned in exactly ONE test —
  tests/components/campaignFolderTheadA11y.test.jsx:43 (updated).
- W/L pip "3W/1L" -> "won N, lost N"; pip title "Cross-settlement record"->"War
  record" (LivingWorldSignalRow) — dissolves the Standing-vs-standing collision.
- "Retained inactive until X" -> "Frozen until X. Reactivate or export." (SettlementCard);
  "Frozen until X" (CampaignFolder — no inline reactivate/export there).
- "Regional graph" -> "Between Your Towns" + a plain glance sentence (RegionalGraphSummary).
- "author your pantheon" -> "create your own gods" (SaveQuotaMeter PREMIUM_PITCH) —
  ratified marketing surface.
- Bulk "Export" -> "Export JSON" (BulkActionBar).
- Dossier Systems sub-tab labels "Substrate"->"Causes", "Viability"->"Outlook"
  (OutputContainer TABS; ids substrate/viability FROZEN — id-based pins hold).
- BEHAVIOUR: toolbar minimal face at 1-4 saves (new `minimal` prop; gate at
  SettlementsPanel `minimal={saves.length < 5}`). OWNER-VETOABLE main-surface shift.
- BEHAVIOUR: removed the in-Filters Campaign selector (folders already slice); the
  filters.campaignId plumbing stays for deep links. OWNER-VETOABLE control removal.
- STRUCTURE: campaign-folder header — four export/import doors folded into one
  "Books and export" menu (SettlementCard kebab grammar); PDF's red variant=danger
  gone (red = destructive-only again on that header).
- Standing dash fix: the ledger Health cell now renders the band word for EVERY
  derivable state (was: only severity>=2, else a dash that swallowed Strained). Uses
  the SHIPPED stability-band glossary entries via HealthPip — no new glossary needed.

## DEFERRED (recorded, owner-gated or out-of-scope; NOT bugs to re-find)
- ⭐ THE GLOSSARY EXTENSION (SPEC 4 item 6 — the audit's "highest-leverage single
  mechanism"): extending SurveyorGlossary click-to-learn to Canon/Drafts, the filter
  chips, Tier values (thorp..metropolis), sort "Tier". DEFERRED as OWNER-GATED. Reason:
  src/domain/display/glossary.js is a byte-pinned GENERATED contract — buildGlossaryEntries
  derives entries from ENGINE registries; docs/glossary.md is regenerated by
  scripts/generate-glossary.mjs and pinned byte-identical + both-directions coverage
  by tests/docs/glossaryFreshness.test.js. Adding a new authored category (lifecycle /
  size / filter terms) needs authored definitions + real Compendium anchors per term +
  a domain-layer edit (strict-0, any-ratchet). That is NEW CAPABILITY against a
  generated/persistence-shaped surface. Recommend a dedicated owner-authorized wave.
- Bulk "Canonize" confirm sentence — a confirmation step on a premium bulk MUTATION
  flow = paid-surface behaviour change, not a small copy fix. Deferred.
- Dossier persona doors (DM Summary / Guidance / DM Notes / AI Notes) + "Chronicle"
  name collision — audit flagged, no crisp plain-synonym prescription; owner-taste;
  labels are already plain English (differentiation, not jargon). Realm-Inspector side
  HARD-FENCED anyway.
- Cross-surface Tier/Standing consistency: organic/samples/LibrarySample and shared
  WorldPage still say Tier/Standing (out of this wave's file scope) — a follow-up to
  unify with the new Size/Health.
- SubstrateTab / ViabilityTab internal content headings (src/components/new/tabs/**) —
  out of file scope; "Causal substrate" / "coherence" read coherently with the new
  strip labels, so no visible clash.

## Hazards discovered (how-to-apply)
- ⚠️ persistenceError carries BOTH the library-LOAD failure (SettlementsPanel:238,
  errors.libraryLoadFail) AND the SAVE failure (:368, errors.persistFail). The audit's
  "Saving:" lead-in would mislabel load errors — used "Library:" (accurate umbrella).
- ⚠️ The audit was run against a DIFFERENT worktree (.claude/worktrees/minifold) with
  DRIFTED line numbers. Every site was re-verified against wave-b before editing. Repo wins.
- ⚠️ The audit's Standing band word 'Steady' does NOT exist — real bands.js vocab is
  Stable/Strained/Vulnerable/Critical (domain/state/bands.js). Used 'Stable'.
- ⚠️ SettlementsPanel is a HOT FILE at 598/600 effective (components ceiling 600, NOT
  baselined). Only ~2 lines of headroom. The "New campaign" subtitle was landed at +1
  (599) by compacting the button onto one line; the toolbar `minimal` prop was net-zero
  (swapped the now-unused campaigns prop). Alert lead-ins are net-zero (inline <strong>).
- The "always shows a health pip" pin (settlementCardSignals.test.jsx:77-80) ALIGNS with
  the dash fix — it now holds for every band, not just crisis.
- E2E pins updated: e2e/regional-causality.spec.js "Regional graph"->"Between Your Towns",
  "2 queued"->"2 changes queued" (Playwright, not in the vitest gate, but corrected).
- Column heads pinned in exactly ONE test; dossier labels pinned by ID only (0 label pins).
- The "Books and export" menu is a11y-walker compliant (onClick-based, zIndex 20 local
  band, no focus trap) — mirrors the SettlementCard kebab pattern.

## Gate receipts (CONFIRMED)
- First-paint entry static closure: base d097f180 = 1,039,806 B (my own measure);
  after wave = 1,039,766 B. Delta = -40 B (SHRANK). Budget 1,040,000. VERIFY_DIST 26/26.
- node scripts/check-domain-strict.mjs: 0 errors (ceiling 0).
- npm run typecheck (tsc --noEmit tsconfig.full.json): 0 errors.
- npm run lint: 0 errors (27 pre-existing warnings, none in touched files; the
  SettlementsPanel :207 inactiveRetained warning pre-dates this wave).
- Copy/voice families (tests/copy/): 104/104. voiceMechanics: no new em-dash/! ; no
  baseline drift. a11y walker (uiA11yContract): green.
- Per-item focused suites all green. Full suite run FOREGROUND in quarters (the
  background run went output-stale — dead-suite-hang class — so re-run in shards
  per gate discipline): shard 1/4 + 2/4 + 3/4 + 4/4 over all 1675 files. The ONLY
  reds are EXACTLY the 4 parked golden families (beliefMapGolden [1/4],
  worldpulseDeityGolden + goldenViewModel + generatorGoldenMaster [4/4]); shards
  2/4 + 3/4 fully green. One FAKE parallel-load timeout surfaced (advancePauseResume
  ran 68.7s under shard-1 contention, 1/9 failed) — ISOLATED single-threaded it is
  9/9 PASS (exactly the documented parallel-load-red-is-fake class). 6th commit
  21d36d49 = deep-craft kill-list de-round (borderRadius/boxShadow/rgba back to
  85/72/167 exactly, tolerance-0).
