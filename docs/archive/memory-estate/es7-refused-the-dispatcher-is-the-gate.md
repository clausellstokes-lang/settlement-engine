---
name: es7-refused-the-dispatcher-is-the-gate
description: ⛔⛔ ES-7 REFUSED — five of six Herald kinds have NO REACHABLE PRODUCER; the espionage volume built FIVE waves of consumers and the DISPATCHER that would make them fire was never chartered
metadata:
  type: project
---

**MEASURED 2026-08-11 at `133e300f`.** ES-7 is five behavior families wearing one wave
name, and it is **REFUSED FORWARD**. Draft: scratchpad `es7-packet-draft-ES-7.md`.

⛔⛔ **THE PROGRAM-SHAPING FINDING: FIVE OF SIX HERALD KINDS HAVE NO REACHABLE TRIGGER.**
- `espionage_departed` — **no dispatcher exists**: `castCovertOperative` /
  `mintCovertMission` have **ZERO call sites** (two definitions, one cadence, two comments,
  one registry string), and `espionageMissions.js:13-20` says *"NOTHING UNDER src/ CALLS
  THIS FILE."*
- `espionage_caught` — ES-2 **declared in writing** it could not open custody
  (`espionageGauntlet.js:14-40`, two persisted-DTO blockers, **owner-gated**);
  `foreignGuestHold.js` contains no `news` token at all.
- `espionage_betrayal` — downstream of caught, and §5 files the casus as a WAR-CHAIR row.
- `espionage_exposed` — four blockers (below).
- `confirmed`/`refuted` are code-reachable but **DATA-unreachable**:
  `subsystemRowsVirtual.js` says *"the stage walks an empty ledger."*

⭐ **SO: ES-D — THE DISPATCHER — IS THE ONLY ESPIONAGE SLICE COMPILABLE AT THIS HEAD, AND
EVERYTHING ELSE WAITS ON IT.** The volume built ES-5a/b/c/d and ES-6a — five waves of
CONSUMERS — on a declared build-doors-before-traffic shape, and the producer was never
chartered. That is coherent by design but it means **the tail cannot proceed by picking the
next number.**

**Other measured refusals:** ⛔ the **KNOWLEDGE DESK DOES NOT EXIST** — `HERALD_SECTIONS`
is exactly `war/faith/trade/events/divination/adjudication`, byte-pinned, and
`heraldRouting.js:129` says *"IN-5 mints the KNOWLEDGE…"* — **IN-5 has not landed**, so
route confirmations to `events`, never grow the section list. ⛔ The certification
conversion is impossible AND a live pin forbids a partial one
(`subsystemRowsVirtual.test.js:416-432` asserts `eventTypes).toEqual([])` over a cohort
INCLUDING ESPIONAGE). ⛔ The envelopes are a **SOAK PROGRAM** — hours to tens of hours —
which PACKET_STANDARD names a forbidden dispatch unit. ⛔ The DM verb is its own packet:
precedents cost **9 files/1,152 insertions** and **24 files/3,521 insertions**.
⚠ Cost calibration: IN-0c cost **20 files / 589 insertions for ONE kind**; the frequency
floors imply **30–36 authored prose variants** for six kinds, and no espionage kind
registry or receipt-pool leaf exists yet. ⚠ Three EXACT estate constants move
(`LEGACY_UNVOICED_TOKENS=274`, `ROUTED_TOKENS=375`, `REGISTERED_KIND_COUNT=107`).

**ES-6c's four blockers, two of them NEW and both fatal to "retroactive":**
1. `applyForeignExposureBlowback` returns `newsEntries: []` on ALL THREE paths and its
   caller never reads the field. 2. `pulseKernel.js:1773-1776` drops `patronId`.
3. ⭐ **THE OUSTED PRUNE KILLS "RETROACTIVE"** — `pulseKernel.js:676-682` deletes
   `npcStates[id]` for every `'ousted'` exposure **in the same tick**, so the clause can
   only ever be SAME-TICK, inside `corruptionWeb.js`. 4. ⭐ **The leak leaves no mission
   identity** — ES-6a's `sourceId` is `<homeId>#<operative id>` (a court and an agent,
   never a mission) and `leaks[]` is dropped at `envoyPulse.js:469`.
**→ Re-file ES-6c OUT of ES entirely as a corruption-volume wave, two-party, same-tick.**

⚠⚠ **A MEMORY FACT CORRECTED BY MEASUREMENT: "the venue vocabulary has FIVE homes" is
THREE `src/` homes** (`envoyEncounter.test.js:380-394`), and it is an ENVOY-ENCOUNTER fact
**unrelated to Herald kinds** — [[epq-encounter-carrier-conditional-landed]] should be
re-derived. ⭐ The lighting-census path is **currently UNRESERVED** (the only non-LANDED
packet is IA-2/STALE), so the next promotion may claim it.
