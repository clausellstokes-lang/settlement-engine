---
name: provenance-dag-gap-and-substrate-map
description: "⚠️ LOAD-BEARING (chronicle survey 2026-07-17): NO durable receipt→receipt causal DAG existed — cones/chains entity-INFERRED; substrate = pulseHistory (80-cap, collapsed) + chronicles (24); THE PROVENANCE LEDGER COMMISSIONED (task #32, flag-gated, after-NPC before-soak) to record causality going forward"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7115c211-9732-4751-8d73-170ba6bbcf31
---

The Chronicle wave's survey (2026-07-17, four parallel agents + direct tracing) established
the true narrative substrate — load-bearing for ALL future chronicle/provenance/decree work:

- **NO durable receipt→child-receipt provenance DAG exists.** `domain/trace.js`'s Receipt is
  derive-on-read; its edges name ENTITY/SUBSYSTEM ids (`causes[].source`, `effects[].target`),
  never parent receipt ids. Applied ops persist NO stable causal-receipt id (pending edits are
  session-only; realm-verb ids live in capped wizardNews + per-family spatial ledgers). The
  only durable causal edge is one-hop event→artifact (`sourceEventId`).
- **The durable substrate:** `worldState.pulseHistory` — MAX_HISTORY **80 advances**,
  Stage-5 COLLAPSED one-record-per-advance (span NOT stored; derived from tick deltas;
  interior-week placement LOST) with `selectedOutcomes[≤24]` + `impactDigest[≤18]` carrying
  typed reasons + entity keys; plus `campaign.chronicles[]` (cap 24). `wizardNews` (240) is
  DISPLAY-ONLY — the chronicle never reads it (structural pin).
- Applied DM decrees surface AS durable outcomes (`applyMode:'proposal'`/`proposalPayload`),
  so the decree tracker reads the same substrate.
- **Consequence:** decree CONES, thread CHAINS, and standings are ENTITY-INFERRED (shared
  keys + typed reasons + temporal order), labelled `inferred` in the UI. No "standing" field
  exists — derived.

**Why:** the Decree Tracker/receipts thesis currently rests on inference, not exact
provenance; any future "exact cone" ambition requires new persistence.

**How to apply:** OWNER COMMISSIONED (2026-07-17, task #32, ledger @ 9c24d840): THE PROVENANCE LEDGER builds — durable per-campaign decree/receipt ledger with
cause-edges (engine-adjacent writer + schema = owner-gated shape; MUST land BEFORE the soak
if commissioned — engine freeze). Cross-advance thread stitching + decree-receipt
click-through + D7 decree-reception irony are the recorded downstream seams that would ride
it. Related: [[comprehensive-review-fix-program]].
