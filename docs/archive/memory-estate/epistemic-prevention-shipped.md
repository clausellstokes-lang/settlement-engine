---
name: epistemic-prevention-shipped
description: "Test-truthfulness machinery FOLDED @ composite-r4 e56cbeaa 2026-07-27 — anchored negatives 181→2, seed loops 32→0, powered envelopes, effect-reachability gate; owner picks EP-a..EP-j in queue §9"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2d79d3e0-94e7-4de6-854b-4f5c77f9095f
  modified: 2026-07-28T11:30:19.417Z
---

**EPISTEMIC PREVENTION PROGRAM — FOLDED 2026-07-27 @ composite-r4 `e56cbeaa` (73 files, +3,824/−341); ledger row + queue §9 @ review-fixes `041283da`.**

Owner order "do it all" on the three lane-close epistemic hazards. Built Fable-manager / 7 Opus implementers. Program doc: minifold `docs/EPISTEMIC_PREVENTION_PLAN.md`; doctrine: `docs/GENERATION_CONTRACTS.md` §Test epistemics.

**What stands (all shrink-only, all proven HEAD-green 45/45 in a detached-worktree battery at e56cbeaa):**
- `tests/lint/negativeAssertionAnchor.walker.test.js` — bare `not.toContain/toMatch/toHaveProperty` habitat swept 181→2 sites; cures in `tests/helpers/anchoredNegatives.js` (`expectPresentThenAbsent` / `expectAbsentWithAnchor` / `// anchored:` on the line above ONLY — one line up, never two).
- `tests/lint/seedLoopTotality.walker.test.js` — bare seed loops 32→0; cure `tests/helpers/seedFailures.js` (`collectSeedFailures`+`expectNoSeedFailures`); failure counts are now TRUE counts, not first-hit.
- `tests/lint/distributionEnvelopePower.test.js` + `tests/fixtures/distribution-envelopes.manifest.json` — exact-binomial bound registry (margin ≥2σ, provenance, NEVER loosen silently; `loosenPending` holds the tighter live bound). 5 registered; criminalCity capture TIGHTENED 40→29, equilibrium 200→388.
- `tests/generators/effectReachability.coverage.test.js` — 10 authored effects proven firing over a pinned corpus + anti-vacuity + `UNREACHABLE_STRATA` disjointness guard.

**Why:** seeded failure counts are lower bounds (30/100 reported as 1); seed drift silently neuters negatives; hand-picked bounds pass at zero margin (capture 4-vs-4 @N=40). All three classes now red at authoring time.

**How to apply:** new generation-adjacent tests MUST use the helpers (walkers red otherwise). Distribution bounds: derive from a base measured ON THE ACTUAL CORPUS the test runs (the `envelope-${i}` corner: 4/50 real vs 89/400 family — cause unknown); degenerate 0/N–N/N rates are NOT envelope-able (rule of three; restate as totality). New E-A entries for untracked tests must ride the SAME commit as the test file.

**Sharpest findings banked:** 6 vacuous-green instruments (VS16 pin over all-empty icons; unreachable tanner-rule guard — while `tannery` IS absorbable by `furrier's district`, audit-amendment-#2 class; two empty-quarters spatial pins, fixed; empty-baseline pins, strengthened) · THREE coherence-repair strata UNREACHABLE by construction (`mutual_exclusion`: no `blockedBy` in spatialData; `access_compatibility`; `hard_dependency`: 60/89 gates self-satisfying via validator expansion) · custom-target subsumption is receipt-silent (§14 lacks recordTrace) · `survival_crisis` reaches the repair loop unhandled.

**Owner picks EP-a..EP-j** (queue §9 @ ledger): capture loosen 40→45? · ordinaryTowns 0.34σ powerless bound accept/loosen-to-20? · distribution.test.js's six never-happen thresholds → totality? · distributionEnvelopes N=50→400 (~22s→150s)? · VS16 re-pin vs restore icons · tanner/furrier src check · the three unreachable strata + receipt-silence + survival_crisis · seed-loop walker widening (29 files/54 sites looser rule; `.forEach`/`.map` unscanned) · standing E-A sweep plants now eligible · calamity migration measured-ready (upper 169 tightens; lower stays 100).

**EP-5 (the unclaimed tail, owner "continue") FOLDED @ e04d3749 same day:** envelope roster 12→15 (ordering siege pair — ratio KEPT beside absolute envelope; ancientRuins mint pair — N=40 proven uncarryable at 2σ, corpus doubled to 80; roadsMissions cadence pair — denominator proven FIXED at 36 NPC-years); 8 more loops converted + negative control executed (planted break → TRUE 11-of-80 count). NEW: **EP-l** roads ≤1-journey/NPC-year law VIOLATED 5/16 seeds (src finding — the old test was green over a corpus of one) · **EP-m** unwalledFloor loosen ruling (live 10 vs derived 6).

**Push state at program close (2026-07-27 ~21:35):** ledger FULLY pushed (`2b903a54` ls-remote-verified — carries queue §9 EP-a..EP-m). Composite: origin at `064bb460`, ONE commit behind local `e04d3749` (EP-5); my push attempts were hook-rejected on the LIVE WRITER's in-flight `src/store/index.js` tsc error (TS2554 at 77,40 — THEIR hunk, foreign-WIP law, not repaired) — EP-5 rides the next successful composite push by any session. ⚠️ the ledger tree's pre-push hook runs a full build+verify:dist (~minutes under load), and the map-fork VENDOR-MANIFEST refuses-as-vacuous condition (EP-k) intermittently appears on composite pushes.

**EP-6 (src-side repair, owner "continue" on EP-g/EP-l) FOLDED @ 11c295c1 2026-07-28; ledger 3c275060.** Verdicts all MEASURED: ⚠️⚠️ the coherence audit's own hard_dependency prescription is a REGRESSION (17 of 65 self-satisfying gates are LEGITIMATELY vacuous via roster ladders — the ladder-aware 3-piece fix moves 41% of golden keys → ONE REGEN batch, EP-g2; never implement the audit's line 1093 fix as written) · access_compatibility was NEVER unreachable (⚠️ CLASS: a FORBIDDEN-list permits every route it doesn't name while an inclusion-list rejects them — `mountain_pass`/`none` fall in the polarity gap; both first-party corpora omitted mountain_pass, which is WHY it looked dead) · EP-l WITHDRAWN as instrument artefact (release legs mint new mission ids; cadence law = GENESIS-only, charter test had it right) — real yield = SELF-CAPTURE (court takes own envoy hostage, pays itself ransom): fixed in 4 land arms of the dark roadsKernel; ⚠️ FIFTH instance open in seaRoads.js S3 piracy arm (EP-p, chips filed) · furrier→tannery producer-eater DELETED (severed leather 46/46 DM-forced; tannery now in protectedProducers, teeth proven) · §14 trace + survival_crisis skip-with-receipt shipped (zero goldens A/B-proven). Durable mechanics: cascade tier window = [tierIdx-1, tierIdx] (blocks village→city pulls); plagued worlds FLOOR priorityMilitary to 25 (resolveConfig:183 — closes survival_crisis's only error branch); ⚠️ repairHardViolations runs TWICE per step — dedupe sets must be STEP-scoped, call-scoped double-emits (proven load-bearing). Anchored-negatives habitat now 1 file/1 site (EP-e only). ⚠️ The deity-retirement lane re-recorded generator goldens mid-flight with this program's src edits in-tree (harmless only because A/B-proven neutral) — re-recording goldens from a multi-program tree is the certifier-silencing shape.

**Foreign reds at fold (NOT this program's):** advancePauseResume red AT HEAD · mechanismLitCoverage red vs foreign untracked rosterProvenance.test.js · townScene3dLazy · voiceMechanics · deepCraftKillList · guidanceRegistry.walker · domainAnyCastBaseline · migrationRehearsal (mig 191, AI-ladder lane) · pipeline.property (known flake).

⚠️ The E-A entries for this program's 4 tests were AUTHORED BY A CONCURRENT SESSION with fold-coupling kindNotes; its claimed coupling of the corpus gate to uncommitted src was probed at HEAD and REFUTED. The manifest was staged BY CONTENT (git hash-object + update-index): HEAD's 438 entries + exactly my 4 — other lanes' unfolded entries stayed theirs. Related: [[minifold-tree-is-live]], [[unreachable-predicate-conjunction-class]], [[criminal-capture-shift-owner-ruling]], [[generation-remediation-gate-state]].

**⚠️ `// anchored:` MARKER PLACEMENT (learned 2026-08-03, lane C VH-2).** The walker's escape hatch is a plain line scan for `/\/\/\s*anchored:/` on the ASSERTION LINE or the ONE LINE IMMEDIATELY ABOVE it. A multi-line prose comment whose `anchored:` token sits on its FIRST line does not count — the marker must be the last comment line before the assertion. Write the reasoning first and the `// anchored: <reason>` line last, directly above the `expect(`. Cost one red cycle on `tests/components/civilityBlockWiring.test.jsx`. Related: [[public-payload-veil-seam]].
