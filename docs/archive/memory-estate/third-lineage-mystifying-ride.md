---
name: third-lineage-mystifying-ride
description: "The settlementforge-export = branch claude/mystifying-ride-90b338, a THIRD lineage forked from OUR Phase 3 seal, already merged into GitHub master; master is now 107 ahead of it and 308/129 vs our branch"
metadata: 
  node_type: memory
  type: project
  originSessionId: 95cca3f6-d313-4fe1-91f6-b4b3a29fb5ef
---

Discovered 2026-07-11 when the owner attached /Users/cstokes/Desktop/settlementforge-export/
(source zip + history bundle + PR-body.md; bundle verifies complete; tip 08b11499).

**Topology (verified via git, same remote clausellstokes-lang/settlement-engine):**
- `claude/mystifying-ride-90b338` forked from OUR lineage at the Phase 3 seal `acf59a00`
  (same merge-base as origin/master vs our HEAD).
- It carries a 201-commit religion arc built INDEPENDENTLY of our Phase 4 by a parallel
  claude.ai session: legitimacy axis, seeded patron contest / imposed-cult schism,
  divine-mandate→publicLegitimacy→coup coupling, occupation→conversion, "compromise chain
  amplifies evil faiths", 60k Monte-Carlo balance certs, pantheon-in-pulseKernel,
  faith panel UI, PDF LIVING PANTHEON block, free-tier PantheonTeaser, size-scaled
  cross-settlement faith influence (mass), SHIFT_TIER editor control.
- It is FULLY MERGED into origin/master (0 unique commits left), and master has since
  advanced 107 commits: review-remediation PRs #42–#47, money features (referral+redeem
  migration 107, per-settlement PDF entitlement ladder 108, gallery import premium),
  migrations applied head 112, deploy-gate + security fixes.
- OUR branch review-fixes-2026-07-08: 129 ahead / 308 behind origin/master.

**Extraction verdict (wf_85fe5fdb-631, 3 readers; CORRECTED after full report):**
- The export is the DIRECT ANCESTOR of our religion engine, not a rival: prod = export
  core + Phase 4 (W-F2/F3/F4/F5.5/F8) + W2b factoring; shared numeric core byte-identical
  (LAG .12, W_RULER .42/.30/.20/.08, stain .45/.06, contest .78/.22, mandate constants).
  It reached us through the merge program's worldPulse/religion port. Phase 4 = exactly
  the diff: piety, stance/law-method plane, clergy plane, conduct loop, crisis conversion,
  unaffiliated sink, dampener, gov×law synergy, martial/moral terms, latent pantheon.
- Export-only (deliberately deleted in prod): legacy binary winner-take-all contest driver
  (evaluateReligiousContest) + religionDynamicsEnabled hard gate (prod: faithSpreadEnabled
  split). Merging master→us would try to ADD THESE BACK — fence it.
- Divine mandate→publicLegitimacy→coup IS in our tree (pulseKernel + religionState),
  piety-enhanced in Phase 4 (target ×pietyMult, law-axis mandate fit). Not a gap.
- PantheonTeaser is FICTIONAL sample data only (compliant in spirit). The REAL gating
  leaks in the export/master lineage: (1) dossier War&Faith tab shows FULL live pantheon
  to free/lapsed (useSettlementLiveWorld + WarFaithSection have no tier check; PDF path
  re-gates); (2) gallery.js:593 exposes primaryDeity name as public facet to anon.
  NEITHER exists in our tree (verified) — our FaithSection is the constitutional seam.
  REUNIFICATION FENCE: transplanting their dossier tabs/gallery MUST NOT carry these in.
- Their gating philosophy matches our implementation law by a different route: NO pantheon
  generation at all (dormant until premium DM authors+assigns) vs our latent bake +
  activation seam. Tier never touches generation in either.
- SHIFT_TIER: domain machinery already in our tree (registry/mutateEntities/undoEvent/
  tierOutcomeApply); only the Workshop TierShiftControl UI is missing (parity ledger #17).
- Evidence quality: 36×-coups + 60k-Monte-Carlo claims plausible-in-kind but NO artifacts
  shipped, no assertions in scripts; the 36× cohorts are NOT "otherwise-identical"
  (rank/rival/ruling-faction/prosperity differ). Below our evidence-bundle standard.

**Other consequences:**
- MIGRATION-NUMBER COLLISION RISK: master's ledger head is 112 with new 107/108;
  our chain runs to ~129 after the [[wave0-migration-audit]] renumber. Reconciling with
  master needs a migration-chain audit BEFORE any db push.
- Master also carries money features we lack (referral/redeem 107, PDF entitlement
  ladder 108, gallery import premium) — product-level reconciliation input, not religion.

UPGRADE (2026-07-14, F4 wave): the ENTITY-LINK CONSUMER SYSTEM (EntityLink, PDF EntityRef,
DossierEntityContext, provider, anchors — master commit 6d95adc7) is ENTIRELY ABSENT on the
review-fixes lineage while the edge-function PRODUCER runs live on it. F4 shipped the degrade
path (ProseParagraph/ProseText de-tokenize to plain names, contract-tested), so nothing leaks —
but the master merge must port the full consumer layer and reconcile supabase-function↔client
contract PAIRS as a first-class merge axis (not just the worldPulse 69-file 3-way). Also: 8
post-111 SECURITY DEFINER functions need a pg_temp re-pin migration (walker-baselined, F4).

Related: [[reconciliation-decisions]], [[wave4-product-dossier]], [[owner-vocabulary]], [[comprehensive-review-fix-program]].
