---
name: owner-vocabulary
description: "Owner's domain vocabulary — \"compromised\"/\"compromise tag\" is interchangeable with \"corruption\" (verified matching the code's design)"
metadata: 
  node_type: memory
  type: user
  originSessionId: 95cca3f6-d313-4fe1-91f6-b4b3a29fb5ef
---

The owner uses **"compromised" (and the compromise tag) interchangeably with "corruption"** (stated 2026-07-10). Verified in-code: this matches the system's own design — `src/domain/corruption.js` exports `compromisedSecurityInstitutions`; institutions carry compromise as the corruption climate's concrete expression (with **covert vs revealed** buckets); `rulingStructure` marks criminal-influenced governance `'compromised'`. When the owner says either word, they mean this one system.

**How to apply:** never treat "compromise" mentions as security-breach language in product discussions; map both words to the corruption system. The covert→revealed split means the corruption lifecycle already has two beats — the Phase 5 "corruption arc" candidate is really just the post-exposure fork (purge vs entrenchment). Related: [[exhaustive-review-in-flight]].
