---
name: herald-prose-slot-classes
description: "Two durable Herald facts: UNRECEIPTED_HOP is a noun phrase (guard like REDACTED before any clause slot) · lineageIds PRESENCE marks nothing — only the disinfo: prefix marks a plant"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1c189f3d-9fc3-4fc9-9cb7-b767525d28d5
  modified: 2026-08-03T18:33:29.934Z
---

2026-08-03, cycle-11 verifier (both proven by execution through the shipped
popup path):

1. **`causeWalk.UNRECEIPTED_HOP` ('an earlier cause') is a NOUN PHRASE with
   `redacted:false`** — emitted whenever provenance names a parent no
   pulseHistory record resolves (a root sourceEventId, or a receipt aged past
   MAX_HISTORY=80). Any prose consumer of a cause-walk hop must guard it the
   way it guards `REDACTED_HOP` — by the EXPORTED constant, never the literal
   — or it flows into clause-shaped slots ("in the wake of the day an earlier
   cause"). Same class: chronicleGraph's 'World pulse outcome' / 'World pulse
   impact' fallbacks. Chair ruling (cycle 12): unreceipted = TERMINAL in the
   composer, drop-never-relabel.
2. **`lineageIds` (plural) PRESENCE marks NOTHING** — rumorNetwork.js:622
   writes it on every arrival and :722 appends per relay; it is the ordinary
   telling lineage. Only the **`disinfo:` id prefix** marks a plant (minted
   at informationStatecraft.js:675 + brokerageServicesPlant.js:433, validated
   at disinformationPlant.js:270). A `planted` warrant keyed on presence
   MANUFACTURES INTENT — the mirror image of the Herald truth law.

3. **CR-HR-F4 (cycle 14, chair-ruled):** causeWalk hops carry
   `recordedType` — the receipt's OWN edge type — as a DERIVED field beside
   the dramaClass `type`; poolForLink prefers it. Without it every warranted
   voice (exposed/refused/breached) was unreachable from shipped consumers
   and the vocabulary pins guarded only synthetic fixtures. Live-path pins
   carry the law; synthetic pins are supplements.
4. **The clause floor is a TOTALITY obligation**: every prose surface
   hanging off the cause-walk read model (telling register, headline,
   discourseKernel/CauseWalkPanel, the walk ROOT in fwd) must floor
   placeholders via receiptClauseFloor.js — cycle-12's verifier found the
   same defect one field and one module from the cure; sweep ALL habitats
   when curing one.

**Why:** each is a one-line mistake a future composer/consumer will make
again; each produced live wrong prose or latent wrong intent this cycle.

**How to apply:** new cause-walk prose consumers guard both terminal
constants; anything classifying manipulation keys on the `disinfo:` spelling
or the integrity module's ledger join, never on lineage presence. See
[[public-payload-veil-seam]] for the sibling boundary-discipline class.
