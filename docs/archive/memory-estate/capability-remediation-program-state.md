---
name: capability-remediation-program-state
description: "Capability remediation program FULLY COMPLETE R-0..R-5, 2026-07-27 — all folded on minifold claude/composite-r4 (R-5 closes @ 3aa08f1d); owner sign-off 'do all of these' executed the whole queue; only push + game-content decisions remain owner-side"
metadata: 
  node_type: memory
  type: project
  originSessionId: c991982e-0a25-44fe-a023-b240c17f982e
  modified: 2026-07-27T22:45:59.570Z
---

**The program**: owner ordered "fix it all thoroughly" (2026-07-26) against [[capability-atlas-shipped]]. Plan/log = `docs/CAPABILITY_REMEDIATION_PLAN.md` in minifold (claude/composite-r4) — its **⭐ SUCCESSOR RESUME PROTOCOL** section is the authoritative resume path; this memory is the pointer, not the state.

**PROGRAM COMPLETE (2026-07-27 ~08:50)**: R-0 (~49 pins) · R-1 (~75) · R-2 (~62, both carried obligations closed) · R-3 (+96: writer convergence, patch-validation trio, economy staleness declared, toggle GLOBAL=INTENDED, rename-jam cured) · R-4 (prevention: 7 new walkers/ratchets — advertisedUndoArming 19, deadOperationRatchet 10 [measured 36 dead ops], savedSettlementPatchKeysWalker 7, configDirectWriterExemptions 7, premiumGateSingleSource 7 [viewerAuthority.js single source], economyReadModelCoverage 12 [30-file 4-bucket census; 6 covered surfaces incl. LIVE TableView + SessionMode], terrainReadSingleSource 18 [4 readers + CRAWLER twin api/_galleryMeta.js routed; denominator src/+api/]; updatePlacement de-advertised + compendium regen; CONFLICT 4 = deferred to owner ruling). **FINAL GATE CONFIRMED GREEN**: lint 302 · copy 108/110 (2 FOREIGN aiCharter reds only) · store 919 · domain 8302 · joins 377 · arch 336 · components/ui 2054 · gen-family 1143 ZERO golden movers · pdf 51 · docs 113 · lib 1374 · typecheck 0 · verify:dist **293/293** (vendorPdfLazy red no longer reproduces). **THE FOLD EXECUTED 2026-07-27 (CORRECTED by the folding session itself — an earlier version of this note mis-attributed the upgrades):** the program closes at FOUR commits on claude/composite-r4: `b0a137db` (the fold, 111 files — 13 foreign files that landed mid-fold were EVICTED by index surgery after the post-commit survival check caught them) · `ef338490` (plan log) · `cf03f1eb` (the SEVEN kind:mutation upgrades — they did NOT ride the fold; they are sweep steps 48-54, each executed red-then-green, committed separately with the manifest's 3 foreign co-dirty entries carved out) · `848ba7cb` (closing plan addendum). Post-close dirty set = FOREIGN lanes only: aiCharter family (2 voiceMechanics reds, theirs), intentAtlas/modelRegistry family (incl. untracked tests/config/modelRegistryParity.test.js = 1 TOTALITY red, theirs), townMapStyleWall pair, deno.json, build-edge-shared.mjs. Push stays owner-executed per backup protocol. ⚠️ Bash cwd RESETS to the MAIN tree at turn boundaries — re-cd + rev-parse before EVERY git command or you read/mutate the wrong tree (this bit mid-fold: an "empty diff" was measured in the ledger tree). R-5 + owner queue NEVER STARTED — owner-gated. R-4 rode out a sustained Opus 529 outage; escalation-clause staffing judgment recorded in the plan (Fable applied verifier-sandbox-proven cures inline).

**WAVE R-5 EXECUTED AND CLOSED (owner "do all of these", 2026-07-27 ~19:00)**: R-5a = 13 rulings ratified (CONFLICT 4 Option 1; Edit toggle KEEP; scene/canonical NOT joined; founder disjunct KEEP; markdown caveats screen-only; scene3d promotion still parked — human-evidence contract outranks chat ratification). R-5b Batch 1 @ 68d14324 (+109 pins: G9/G10 polarity banding CURED as declared display shift w/ bandForDimension single source; network advisory label; stat-bars→bands; catchUp undo promoted; lapsed shed controls; G5 FROZEN_VS_LIVE rows; npcAuthoringAllowed aligned to viewerCanAuthor — blackout widening GONE; 3D quality + paused-interval persistence). R-5b Batch 2 (+120 pins; six Fable-specced Opus lanes; locks/deity/provenance folded mid-gate by a parallel session @ 9d3e1414/9ea94dc1/831ba5c0; remainder + R-5 close @ 3aa08f1d): LOCKS ENGINE Phase A LIVE (seeded dormancy proven; 12 dead ops retired, registry ~170; Phase B = id-arrays-through-full-generate, own lane), roster provenance read-side merge, ReadSystemStateBar remounted, mass-update RETIRED, factions pinned, deity (b)+(c) drift-free — (a) lawAxis = T4 batch w/ written note. Gates: zero golden movers throughout, dist 293/293, security 1610 green. NEW OWNER FINDINGS: onboarding coach may have NO EXIT (completeOnboarding zero callers); per-character lock toggles = legibility question; 10 dead ops undispositioned w/ reasons; LOWER_IS_BETTER problem-term = 2 strings. R-5c deferred w/ reasons: deity pool + Herald ids + lawAxis (T4 ONE-REGEN, ledger lane), CC-CLOUD rehearsal (owner infra), table-event verbs + blackout residual (G-2b), #30 other-lane picks.

**Why:** An account switch kills the session's background workflows and cache; everything durable was pushed into the plan doc + tree + this dir before the switch.

**How to apply:** Follow the plan doc's resume protocol steps 1–6 verbatim (tree check → R-3 fate via task output/journal/tree-greps → carried R-2 verifications → standing rules → remaining waves). Gate baseline 289/289 + typecheck 0 + full lint/copy/store dirs. Model split per [[owner-fix-philosophy]]: v3 as of later 2026-07-27 — Fable manages/architects/validates/surveys, Opus verifies AND implements. All program work is UNCOMMITTED in a live shared tree — no git mutations ever; briefs forbid stash + carry verify-first preambles.

**Hazards this program learned (each bit):** agents killed by API 529 MID-IMPLEMENTATION leave PARTIAL EDITS in the tree (the old "die in orientation, leave no edits" heuristic is DISPROVED — R-4 run 1 left 4 lanes of near-complete partial work; survey with git diff + read-the-new-tests before resuming, and resume-briefs must say "survey then complete", never "start over"); a verifier that completes when its fixer died can verify the TREE directly — verifier reports are recoverable work even when the fix phase is a total loss; wave gates need tests/domain + tests/joins TOO — R-2's "zero reds" gate missed tests/domain and let a tooltip-ratchet escape through (caught by R-3's widened gate); a new allowlist literal can red a FROZEN vocabulary-closure scan in tests/joins — cure = compose the colliding token ('stressor'+'Edits'), never expand the frozen set; source-scan walker regexes: `(?:^|[,{]\s*)` misses the FIRST key of every literal — the class fix is `(?:^|[,{])\s*`; at fold, untracked pin files MUST land in the same commit as their tracked-modified subjects (E-A: green in-tree, red at HEAD); verify:dist covers ONLY tests/build — wave gates need FULL tests/lint + tests/copy + tests/store dirs; the operationRegistry walker's isFn regex cannot parse nested parens in param defaults (use `opts = {}` + body access); tsc parses tag-shaped tokens (`@type`) inside JSDoc PROSE (TS1110) — keep them out of comments; voiceMechanics ratchet rejects reader-facing em dashes (VOICE §6: period/comma/colon/parens); loadingNarrationRatchet pins silent Suspense boundaries at an exact count and BARE_LOADING_PIN forbids fallbacks starting with "Loading"; new invariant test files need mutation-coverage-manifest entries — `uncovered` is NEVER legal for new entries (uncoveredBaseline is exact-equality shrink-only), and an UNTRACKED subject makes `mutation` ineligible too, leaving `rationale` as the only legal kind pre-fold; eager-closure claims must be MEASURED on the real vite build, not per-file esbuild (R-2's verifier was wrong by construction: facade dynamic-imports made the heavy modules already lazy; true margin ~180KB against CLOSURE_BUDGET, not the stale ~54B); session-limit deaths leave zero partial edits when agents die in orientation — verify by grepping fix signatures before resume.
