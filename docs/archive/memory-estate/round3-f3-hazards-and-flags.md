---
name: round3-f3-hazards-and-flags
description: F3 wave hazards — supplyKernel↔migrationKernel import cycle; corruption.js EAGER vs piety.js lazy; the 5 new dark virtual flags from the coherence builds
metadata: 
  node_type: memory
  type: project
  originSessionId: ff8b4b71-3ea5-4d7a-9793-42190018c11f
  modified: 2026-07-20T12:04:55.027Z
---

ROUND 3 WAVE F3 (composite-r4 @ 45dffd55, 2026-07-20) banked three durable facts:

1. ⚠️ IMPORT-CYCLE HAZARD: supplyKernel imports FROM migrationKernel — never import
   supplyKernel (or its helpers like criminalStrength01Of) INTO migrationKernel;
   read stamped fields directly instead.
2. ⚠️ EAGER/LAZY HOSTING: corruption.js is EAGER; piety.js and corruptionLeash.js are
   LAZY siblings — host new onset/corruption helper terms in piety.js for closure Δ0.
   npcAgency.js sits at its 833 max-lines ceiling (no new import lines).
3. FIVE NEW VIRTUAL FLAGS (all default-absent, dark; ONE-REGEN membership = owner
   decision): ladderPoliticalWindowsEnabled (faction_captured + bloc_backed windows) ·
   institutionPoliticalControlEnabled · economicCoupReadEnabled ·
   migrationCorruptionPushEnabled · upswingHazardReadEnabled. Plus D-7e clause (i)
   seatBond01 mercy term rides existing memoryWeaveEnabled (W_SEAT_BOND 0.15).

4. ⚠️ CHECK↔CI PARITY IS BIDIRECTIONAL: adding a validate:* step to npm run check
   REQUIRES mirroring it in .github/workflows/ci.yml's check job (ciCheckParity.test
   fails otherwise — bit lane V-I).
5. ⚠️ NEW COMPONENTS: theme tokens ONLY, never raw #hex (rawColorLiteral budget 1403
   shrink-only); native title= forbidden (title census).

**Why:** cycle-2 lanes and the ONE-REGEN flag list must know these exist; the two
hazards bite anyone touching migration/corruption code.
**How to apply:** verify flags in code before citing; owner queue holds 10c/10d
(obligations-spirit + corruption_exposed festival terms — NOT §16-recorded, need a
ruling; manager recommends approving). Related: [[minifold-complete-build-closed]].
