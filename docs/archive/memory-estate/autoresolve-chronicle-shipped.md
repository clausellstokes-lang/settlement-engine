---
name: ""
metadata: 
  node_type: memory
  type: project
  date: 2026-07-22
  branch: claude/autoresolve-chronicle
  base: claude/composite-r4 @ d93f8699
  scope: owner bug (settlement chronicle empty) + owner order (library autoresolve) + NEWS ADDRESS LAW
  originSessionId: 230c87b4-b237-4d1b-a63e-af48dfb233a0
  modified: 2026-07-22T08:07:34.947Z
---

# Autoresolve unification + settlement-chronicle wiring — SHIPPED

Two verbatim owner items (2026-07-22), branch `claude/autoresolve-chronicle` off composite-r4 @ d93f8699 (vision-e worktree, which had drifted onto power-strata — branched off the r4 commit, not HEAD).

## ITEM 2 — settlement Chronicle was WIRED-TO-DEAD-PATHS (verdict: SHAPE-MISMATCH; read-side fixed)
- Owner: "the chronicles in the settlement… advanced a month and nothing showed there."
- ROOT CAUSE (file:line): `OutputContainer.collectChronicle` fed the settlement Chronicle from `saveEntry.campaignState.worldPulse.events` + `campaignState.worldState.eventLog`. **The advance NEVER writes those per-save paths.** `campaignStateForWorldPulse` (src/store/campaignPulseHelpers.js:47) stamps only `worldPulse: {lastTick,lastInterval,updatedAt}` and NO per-save `worldState`. The world's events live ONLY on `campaign.worldState.pulseHistory`. So both world-source feeds read `undefined` → the settlement Chronicle showed nothing after any advance. NOT a mock artifact (client pulse writes pulseHistory in all auth modes). The advance does NOT touch `settlement.recentEvents` either.
- FIX (read-side, no new persistence): new pure module **`src/domain/dossier/settlementWorldChronicle.js`** — `settlementWorldPulseEntries(worldState, saveId, {savedSettlements})` projects `pulseHistory` per-settlement into buildChronicleFeed-shaped rows. `OutputContainer` reaches the owning campaign's worldState via `getCampaignForSettlement(saveId).worldState` (RAW ref — do NOT use `getCampaignWorldState()` in a selector: it runs `ensureWorldState()` → new object every render → select-loop). Threaded through `collectChronicle(saveEntry, settlement, publicChronicle, campaignWorldState, savedSettlements)`; memoized.
- ⚠️ FIRST-PAINT ISOLATION HAZARD (bit once): `chronicleFeed.js` is pulled into the EAGER store closure via `aiSlice` import, and the first-paint budget (tests/build/vendorPdfLazy.test.js `CLOSURE_BUDGET_BYTES = 1_040_000`) had **~5 bytes of headroom** (base 1,039,961 on this machine; manager recorded 1,039,956 — a cross-env Rollup variance). Putting the projection IN chronicleFeed grew the eager index chunk +1,578B → OVER budget. Cure: keep the projection in its OWN module imported ONLY by the lazy `OutputContainer` (lands in the deferred dossier chunk), and re-attach the NEWS ADDRESS block AFTER `buildChronicleFeed` (by row id, world-source only) so `chronicleFeed.js` stays BYTE-IDENTICAL (no `address` passthrough in the eager normalizer). **Verified: base(d93f8699) closure == branch closure == 1,039,961 on this machine → my eager Δ = EXACTLY 0.**
- Per-settlement FILTER: reliable settlement discriminators only — `targetSaveId`, `impactDigest[].settlementIds`, `stressor.affectedSettlementIds`, `relationshipKey` endpoints split on /[:|>-]+/, `proposalPayload.settlementId`. NOT `chronicleGraph.entityKeysOf` (it mixes npc/faction ids in). Cross-settlement wars surface in BOTH belligerents' chronicles via the relationshipKey endpoints.
- Pulse record shape (pulseKernel.js:1603-1649): `createdAt` = the `at`; `selectedOutcomes[]` carry headline/summary/reasons/targetSaveId/npcId/factionId/relationshipKey; `impactDigest[]` carry headline/summary/reasons/settlementIds. `tierChange` has NO settlementId; `outcome.affectedSettlementIds` does NOT exist (only `stressor.affectedSettlementIds`).
- aiSlice grounding (`buildChronicleContextFromSave`) has the SAME dead-path bug — DEFERRED: aiSlice.js is at its exact sizeBaseline ceiling (961) and the fix pushed it to 967. Reverted (a sensitive paid-AI hot file); it is a sized in-class follow-on, NOT the reported bug. See report.

## NEWS ADDRESS LAW (owner doctrine 2026-07-22, mid-task) — derived at the read layer
Every projected world entry carries a 4-part `address`: (1) SUBJECT chain ids `{settlementId, settlementName, npcId, factionId}`; (2) ACTION `eventKind` (candidateType/impactKind/type); (3) AFFECTED settlements BY NAME; (4) REASON = `reasons[0]`. Subject+action ride the byte-verbatim recorded headline (never re-authored). ChronicleTab renders "Affects …" + "Because …" beneath the headline. COMPLIANCE (derivable-now / record-gap / satisfied): parts 2/3/4 DERIVABLE-NOW (implemented+pinned); part 1 subject ids present but the FULL npc→faction→power→settlement EntityLink CHAIN render is a sized follow-on (cross-settlement dossier indexing; power/strata is a RECORD-GAP — the record has no strata id, only opaque `powerTransfer` — so a recorded power level = owner-gated persistence). Multi-hop cause (provenance DAG) is flag-gated/dark.

## ITEM 1 — autoresolve is already TOTAL over what the advance generates; the gap was the LIBRARY mount
- ⚠️ The brief's premise (a single master union of pending-change kinds) is FALSIFIED: there are 12 independent pending-change surfaces; the world-advance autoresolve touches 3 — it DRAINS `worldState.pendingEvents`, AUTO-APPLIES its own tick's majors inline (ON), and only AGES stale `worldState.proposals`. It deliberately does NOT force two DM-OWNED decision surfaces: pre-existing forcing-mode `worldState.proposals` (one-at-a-time via applyWorldPulseProposal; NO resolve-all exists) and the dossier `pendingEditsQueue`. Forcing those overrides deliberate DM decisions (the brief's own "do not silently force owner-ack-required kinds" guardrail) → OWNER-GATED, reported not shipped.
- Owner extension: "there should be an autoresolver in the library advance time as well, it should sync with the realm's." REALITY: both advance mounts ALREADY route through the SINGLE chokepoint — `advanceCampaignWorld` → `campaignAdvanceSession.js:158` `options.autoResolve != null ? … : get().advanceAutoResolve`. `WorldMap.performAdvanceRealm` (useAdvanceSession) and `CampaignFolder`→`useCampaignAdvance.handleAdvanceCampaignTime` BOTH call it WITHOUT an autoResolve arg → both inherit the ONE shared store value. The BEHAVIOR already synced; the only gap was the missing toggle UI on the Library. FIX: compact `Auto-resolve` switch in `CampaignFolder.jsx` advance cluster, wired to the SAME `advanceAutoResolve`/`setAdvanceAutoResolve` (one shared state, not forked), flag-gated on `flag('advanceMultiTick')` (default-on) like the realm toggle. (a11y: nest input in `<label htmlFor/id>` to satisfy jsx-a11y/label-has-for which requires BOTH nesting AND id; stopPropagation on the interactive input, never a wrapper.)

## Structural guards (walkers)
- `tests/lint/autoresolveTwoMount.walker.test.js` — source-scan: BOTH mounts read the shared `advanceAutoResolve`/setter, neither forks a second autoresolve state, the chokepoint resolves via ONE `get().advanceAutoResolve` fallback, neither advance caller passes a forked autoResolve arg.
- `tests/store/commitPendingEditsTotality.walker.test.js` — E-B: every `COMMITTABLE_EDIT_KIND` has a live commit-dispatch arm (settlementSlice.commitPendingEdits switch + settlementRenameHelpers applyEditOp→applyNpcOp/applyTableEvent). E-A VERIFIED: planting a fake committable kind reds it with the silent-drop message; reverting greens it. Closes the all-or-nothing silent-drop risk (queueEdit refuses non-committable at enqueue, but nothing structurally proved every admitted kind has an arm HERE).

## Gate receipts (CONFIRMED)
domain-strict 0 · tsc(full) 0 · eslint 0-new (22 pre-existing warnings, none mine) · NUL 0 · copy/voice families 148/148 · verify:dist 225/225 GREEN · sizeBaseline green · first-paint closure Δ=0 (base-build-verified). pendingEditsQueueReset timeout under 16-file parallel load is a FLAKE (5/5 green in isolation; I never touched pendingEditsQueue logic).
