---
name: wr10-sovereignty-market-state
description: "WR-10 reads/instrument BUILT @ f9a7ddea..a5647976, REJECTED on 3 ratchet-attribution defects + 2 unrecorded obligations; WR-10r repairing under CR-WR10-A/B; wiring (transfer writer, plan trigger, Herald kinds) deferred on measured reasons"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1c189f3d-9fc3-4fc9-9cb7-b767525d28d5
  modified: 2026-08-06T10:27:23.874Z
---

# WR-10 — the sovereignty market: FINISHED (wired dark, 2026-08-04 evening)

✅✅ **WR-10 IS COMPLETE.** Wave span 106df58e..71e78fe9 (18 commits), ledger row
@ 67a907fe. Three reject gates fired, all real; wave-close converted to PASS on
the verifier's own named receipt (mutationCoverageManifest 8/8). The two
whole-wave proofs live at 3754c6f3: the integrated three-roads fixture (cession
sheet / victor-free sale / DM verb → ONE writer, 10/10, four mutants) and the
110-tick multi-year dormancy byte-identity proof. Rulings CR-WR10-C..K recorded
in the row. ⚠ LIGHTING GATE: three of four appraisal belief legs (tier, route,
trajectory) exist in NO belief surface — the belief-legs wave (expected inside
FP Spine) must land first; §9 of the war volume records it. ⚠ Key modules:
sovereigntyTransfer.js (one writer, both roads), peaceTermsSale.js (the
victor-free third transport), treatyOrientation.js (zero imports — who
gives/receives; sale treaties carry drop-when-absent buyerId/sellerId, NEVER
victor fields), satellitesLedger.js (the ledger leaf; satellites census
ratcheted 2→1), sovereigntyMarketStage.js (mounts in the lifecycle host). ⚠ No
realm verb is an operationRegistry row (the spec premise was wrong;
REPUDIATE_TREATY grep 0). ⚠ proseNumerics gained a SECOND stale baseline row
(peaceTerms.js:483) — queued in D-W2.

▶ **THE WIRING WAVE (owner grant "finish WR-10"):** §4 persistence story LANDED
@ `106df58e` (zero new keys/ledgers/PRNG; two conditional fields —
TermRecord.assetId, SatelliteRecord.conveyed — + one rung-preserving occupations
write path; legitimacy echo DECLARED regen-volatile, durable fragility =
occupation `resistance`). Conveyance = one-shot at treaty MINT
(compelled_alliance precedent) — NEITHER pulse mouth touched. Rulings:
CR-WR10-C (cert census: manifest ENGINE_GATED_VIRTUAL_RULE_KEYS + two-way
walker, measured FIVE-key blast radius, routineMajorApproval exempted-with-
rationale, arm (i)'s +32 B rejected both flags), CR-WR10-D (swap = TWO paired
treaties on shared swapId), CR-WR10-E (peacetime mint road under STOP
semantics — never a second treaty writer). Build run `wf_1fa007fb-aa2`
(session c44e5d99): 7 Opus lanes WW-A..G in two gated batches + wave verifier;
WW-G executes D-W1 of docs/DISPOSITION_WAVE_PROPOSAL.md (kind-pool walker
address repair, Class B stays red). ROUND-2 RULINGS after the batch verifiers
bit (all vetoable): CR-WR10-G sale treaties carry drop-when-absent
buyerId/sellerId + ONE shared orientation reader (who gives/receives; four
structurally-requiring rows incl. the writer's own victor/loser derivation —
round 1's victor-free sale conveyed NOTHING); CR-WR10-H composer builds to the
appraisal CONTRACT, missing belief legs (tier/route) = a RECORDED LIGHTING
PRECONDITION + queued belief-legs wave (the dead-lighting trap closed in
writing); CR-WR10-I satellitesLedgerOf/satellitesOf extract to a
dependency-free leaf (the host mount would have closed the tree's FIRST kernel
import cycle — dist TDZ class; smoke:boot mandatory); CR-WR10-J WW-D's
round-trip alias-trap arm repaired (Mutant E survived it); CR-WR10-K
sale_books_diverged stays dm-only covert, the ANNEX amends (books law outranks
the authored audience line). Landed so far: WW-A 5f7e7cd5 (writer, mint-LOOP
hook, four-fence), WW-A-r 4263d472, WW-E aa16d05f, WW-F a128b254 (51-key
walker + shrink-only BACKLOG), WW-G 398f26bc, WW-C a66e0d00+60c455ea (15
Herald kinds), WW-D 8c739719 (16th verb). ⚠ vi.mock third member found
([[vitest-dynamic-import-escapes-module-mock]]). Architecture docs: c44e5d99 scratchpad
wr10w/ (S4/SPEC/CERT/RISKS/CHAIR-CENSUS).

✅ **WR-10r LANDED AND PASSED (2026-08-04)**: `e3d98512` (defects 1-3) +
`454967e0` (CR-WR10-B doc reconciliation) + `01ec1409` (ledger row) +
`18f0ced4` (chair erratum: the size table's own ✅ figures were pre-repair —
true at HEAD: appraisal **160**, bundle **164**; assets 94 / reach 104
unchanged). Verifier verdict REJECT—NARROW converted to PASS by the erratum;
**the war lane's build obligations are COMPLETE** (wiring + owner-held items
recorded). Chair rulings on the stop reports: 11 foreign unregistered clamps
STAY unregistered (attribution honesty; they join the disposition-wave
proposal with the 9 foreign proseNumerics rows + the stale
`occupation.js:852` baseline row); options-object signature AFFIRMED
(one-line wrapper on veto); first-match doc-pin class accepted
([[first-match-document-pin-class]]); checkout-index incident recorded
([[git-checkout-discards-uncommitted-work]]). Banding lesson:
[[lossy-banding-contradiction-class]]. ⚠ Spine address rot for the wiring
wave: `alignmentOf` exists only as an injected pulseKernel closure — the
exported read is `settlementAlignment(item, worldState)`
(settlementAlignment.js:44); `natureWordFor` (conquestDoctrineStage.js:390)
is module-LOCAL and needs exporting before reuse.

## State

Four commits landed: `f9a7ddea` (sovereignty_transfer catalog row on the
non_intervention registration-first precedent, executor:'seam'), `32f4708f`
(sovereigntyAppraisal.js K3-pinned at ZERO IMPORTS + sovereigntyBundle.js — the
reconciled two-sided conjunction with `ceiling_reached` as a named receipted
outcome + the six-pin TR-5 degradation contract), `03b8ecde` (sovereigntyAssets
ledger-membership eligibility + sovereigntyReach three-read geographic bound),
`a5647976` (queue row). **Verifier REJECTED**: architecture/byte-identity/9
mutants/TR-5 contract all CONFIRMED independently; three ratchet defects + two
unrecorded obligations. **WR-10r in flight.**

The three defects: (1) FIVE raw floats interpolated into receipt strings grew
the at-ceiling proseNumerics ratchet (the violation-row class AGAIN — the lane
even cited the law and still measured by name); (2) two local clamps grew
clampPrimitiveBaseline, and the fix COLLIDES: the baseline ceiling is exactly
61/61 while sovereigntyAppraisal is K3-pinned at zero imports so it CANNOT
import the kernel clamp; (3) the WR-10 test's hegemony import granted AUTO lit
credit, turning a GREEN LIT_COVERED_BY assertion RED — the lane's negative
control restored only SRC files while the walker scans TEST files, so it was
structurally blind to its own cause.

## Chair rulings (vetoable)

- **CR-WR10-A the clamp fork**: sovereigntyBundle DROPS its local clamp01 and
  imports the kernel clamp (its K3 list amends +1 — the sanctioned primitive);
  sovereigntyAppraisal KEEPS its local clamp with a baseline row and the
  ceiling widened 61→62 with an in-file rationale naming the K3 zero-import pin
  as the structural reason. Shrink-only spirit preserved: one justified row.
- **CR-WR10-B the twin notes are not twins**: the WAR volume's degraded-bundle
  list (streams/stores/allyship/settlements/**peace**) is CORRECT — `peace` is
  load-bearing (the cession rider is why WR-10 sequences after WR-7); the TRADE
  volume's Seam One list is amended to include `peace`. The lane's PIN 6
  (divergence == exactly {peace}) then tightens to plain equality. Also: the
  CPL-ordered war-volume §3 GR-3/TR-5 lit-precondition row (never landed) is
  authored under this ruling — chair's edit, delegated typing.
- **AFFIRMED, the lane's judgment**: conquestTermsRange/termsRangesOverlap were
  NOT consumed — live code refuted the scout (war-shaped fields + authored
  prose saying "believes it could take X by force" would mint FALSE RECEIPTS
  for a sale; the ZOPA mirror term is meaningless one-directional). WR-10's
  clearing is its own K4-idiom compare-never-merge pair; the conquest
  evaluators' deferral stays open toward the WAR peace path.
- Strike the hegemony LIT_COVERED_BY registry entry (the import IS real lit
  coverage now; the walker's own TO COMPLY text says exactly this).
- Ledger amendment adds: spine req 13 Alignment line (ENGAGED — observer-axis
  judgment; deferred consumers recorded) + req 14 (engine-only decision for
  now, DM verb deferred to wiring); amendment S's Character-and-judgment clause
  moved into the RECORDED deferral list (it was a dropped thread); the two
  off-by-one size figures corrected (94/104, not 95/105); the signature
  deviation recorded (options object vs two positional params — defensible,
  now disclosed).

## Deferred-and-recorded (NOT dropped; the wiring wave's list)

Transfer WRITER (edge rewrite via settlementParentRef — its docstring already
says "an import, sale, or severance must never infer an edge"; grievance mint;
low-legitimacy start), plan-lane trigger kind, Herald kinds. Measured reasons:
WR-10 has NO §4 canonical-model/lifecycle clause in the volume (the write path
cannot be built until the volume declares its persistence story — chair/owner);
pulseKernel BANKED at 1580; the three SP-6 war kind-pool walkers are RED at
base; warDeployment/warSiegeVerdict/pulseKernel shared with the pending
WZ-2/WZ-3 razing lane. Cert row parked on WR-8's +32-byte preset fork
(owner-gated). ⚠ the "sovereign-hand law" has NO code spelling anywhere —
eligibility is LEDGER MEMBERSHIP, never a user-placed flag (none exists).

## Migrated from the memory index (2026-08-06)

⚠ **A SIZE FIGURE IS MEASURED AT THE PUBLISHING COMMIT, NEVER INHERITED.** The
`18f0ced4` chair erratum above is the instance and the standing rule the index
carries: the size table's own ✅ figures had been carried forward from PRE-REPAIR
measurements. True at HEAD: appraisal **160**, bundle **164**; assets **94** /
reach **104** (the earlier 95/105 pair was the off-by-one the ledger amendment
corrected). Re-measure every size figure at the commit you publish it in —
[[wr10-sovereignty-market-dark]] still carries the older 157/165/95/105 reading.
