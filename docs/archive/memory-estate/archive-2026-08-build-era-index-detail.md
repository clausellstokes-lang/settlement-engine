---
name: archive-2026-08-build-era-index-detail
description: Verbatim long-form index lines folded out of MEMORY.md on 2026-08-03 compaction — full detail lives here and in each topic file
metadata: 
  node_type: memory
  type: project
  originSessionId: 1c189f3d-9fc3-4fc9-9cb7-b767525d28d5
  modified: 2026-08-04T07:53:39.115Z
---

Folded from MEMORY.md 2026-08-03 (index size cap). Each line verbatim as it
stood; the topic files remain the authority.

- [⭐⭐ LU-2 ICONS-OFF BURN-DOWN](lu2-icons-off-burndown.md) — 127→55 frozen rows @ d9973d4c·8214872a·c24ae872·1ae224b9·69bf882e; IconButton CLOSED via a `glyph` unicode twin (unconverted call sites unchanged — the incremental arm). ⚠️⚠️ FOUR TRAPS a strip cannot see: ternary `Icon=`, raw-button sole child, and `Icon:` CONFIG FIELD + blanket import-strip = **ReferenceError at module eval that eslint cannot see** (cure = dangling-ref scan + vite build). ⚠️⚠️ the importer ratchet is BLIND to prop-fed renderers (EmptyState + customCategoryDefs rendered lucide UNGATED); ⚠️ LU-2a ACTIVATED 8 latent `glyph` props; ⚠️ pins can HOLD a violation (`querySelector('svg')` as a chrome proxy — invert, don't delete).
- [⭐⭐ THE ICON SWEEP — lane IC](icon-sweep-lane-ic.md) — @ d9a1ea5a: authored-glyph surface CLOSED (146 sites, incl. 94 DEAD icon slots eating flex gaps since an earlier strip); ⚠️⚠️ the AI Surveyor prompt HAS NO ICON; ⚠️⚠️ ✦ is TWO populations — the custom-ownership/magic one is legend-backed + pin-defended, never sweep by character; ⛔ REMAINDER = 184 files / ~305 lucide sites needing the ARCHITECTURAL cure (IconsContext gate exists, 11 primitives honour it, 184 bypass it) + an owner ruling on the Realm-map exception.
- [⭐⭐ GENERATOR GOLDEN shift record + re-record RECIPE](generator-golden-shift-record-established.md) — icon re-record @ 54465b54; proof = 5-template census + TOTALITY check; ⚠️⚠️ never re-record from the dirty tree unproven == CLEAN HEAD. [⚠️⚠️ `\bicon` guard blind to camelCase](case-sensitive-guard-blind-to-camelcase-fields.md) — 4,462 dead `resourceIcon:""` STILL ship; closing them = a SECOND re-record.
- [⭐⭐ ICONS-OFF GATE ENFORCED + the IconButton blocker](icons-off-gate-ratchet-lane-lu.md) — lane LU @ 423270de+bc224ce2; ⚠️⚠️ a docstring that NAMES its own consumers is a CLAIM not a census (4 of 7 never called `useIconsOn`); `tests/lint/lucideTotality.test.js` = shrink-only ratchet, 127 rows, delete-a-row is the ONLY legal move; ⛔ IconButton is icon-ONLY by construction and gates 55 of 127 — chair must pick the fallback shape.
- [⭐⭐ FLETCHED RIBBON FL-2 LANDED](fletched-ribbon-fl2-landed.md) — @ 66b462e3; ⚠️⚠️ `CHROME.headerDesktop`=60 but the LIVE desktop header measures **124** (browser-measured, base-identical), so ANCHOR_OFFSET=84 is ~40px short ESTATE-WIDE — task #66 must not lower the constant without lowering the bar; ⚠️ clip-path clips the FOCUS RING, so the shape rides an aria-hidden vane never the button; ⚠️ mocking `stripe.js` alone leaves the `creditLedger.js` teardown race — mock the leaf.
- [⚠️⚠️ An SVG viewBox became a LAYOUT HEIGHT](svg-viewbox-becomes-layout-height.md) — RIBBON V2 @ b3b2dc2e: an in-flow `height="100%"` SVG in a content-sized chain fell back to its viewBox (100) and SET the header → 124px vs a token saying 60, so EVERY estate anchor + the dossier toolbar landed under the chrome; cure = `position:absolute; inset:0` + header spends the token as `minHeight`. ⚠️ NEVER pin the SUM of a derivation.
- [⭐⭐ WR-8 RAZING BLOCKED BY A DESIGN FORK, NOT SIZE](wr8-razing-tier-demotion-fork.md) — ⚠️⚠️ `tierEligibility` (tierResourceDynamics.js:106) is THE ONE tier writer and demotes off LIVE population, so the razing must emit **NO** `tierChange`; but the SECOND rung falls through `strainedBelowFloor`'s LEGITIMACY conjunct, so severity-picks-the-rung has three costly resolutions and the volume settles NONE — chair must rule. Razing intent needs a NEW leaf (conquestIntent's punish-unreachable walk IS the I4 proof); import `RELATIONSHIP_DEFAULTS.hostile.resentment`=0.78, never hardcode.
- [⭐ WR-8 ATROCITY CASUS LANDED @ a80dd74a](wr8-atrocity-casus-landed.md) — ⚠️ THE TAXONOMY REFUSES A REGISTRATION-ONLY MEMBER: the diversity walker demands a callable WITNESS clearing MIN_SCORE, so a new casus needs a REAL scorer + 6 totality surfaces + 5 count literals; `warTermination.js` had to be decomposed 880→818 to fit three rows.
- [⚠️⚠️ WR-7 wiring blocker MEASURED WRONG](wr7-wiring-blocker-measured-wrong.md) — 4 waves deferred on mouth headroom, but the wiring writes to NEITHER mouth: `envoyPulse.js` is 248/800 (552 free), already CALLS `applyWorldPulseOutcomes` @ :264 and is already called by `pulseKernel.js:1980`. Real blocker = SCOPE + 3 chair questions (⚠️ power band is a K3 fork; ⚠️ compromise round silently dies on `MAX_CONCURRENT_ENVOYS`=2). Six leaves proved UNREACHABLE by a guard-the-guarded 409-module walk. R-BLD-8d built @ 04997090; queue @ f6828d9d.
- [⭐⭐ R-BLD-10 — pulseKernel BANKED PERMANENTLY at 1580](rbld10-pulsekernel-banked-permanently.md) — ⚠️⚠️ NOT burn-down debt; whole-consequence_fold extraction still lands 864, deeper cuts risk THE PROMISE; the wave CLOSES at 3 of 4. Fork census = **22**, not 54. `applyWorldPulse` 941 (141 over) IS still owed.
- [⚠️⚠️ Hand-keyed line addresses rot silently](hand-keyed-line-address-rot.md) — all FIFTEEN `pulseKernel.js:NNN` citations in src/domain/certification were stale and green, one PINNED doc-text-to-doc-text; cure = `module.js \`source token\`` + zero-tolerance containment walker; ~45 sibling markers DEFERRED.
- [⭐⭐ RIBBON V3 — the half-seen war arrow](ribbon-ground-move-foreground-break-class.md) — @ ea0be549; ⚠️⚠️ the header GROUND has moved 3× and each move silently broke correct foregrounds (V3: GOLD_TXT 5.75→3.38, GREEN_DEEP 5.00→2.93, SLATE_DEEP 4.28→3.53); ⚠️ AccountMenu's chip is `transparent` and rides the header paint — it is OUTSIDE the nav lane and gets missed. [Technique + the 2 bad cuts](paint-outside-the-box-without-buying-a-box.md) — `overflow:visible` on an inset:0 SVG paints past the box FREE; a shield cut and a squared quill both passed EVERY pin and looked wrong — screenshot, then pin the geometry.
- [⚠️⚠️ Display class read as an EDGE TYPE](display-class-read-as-edge-type.md) — causeWalk hop `type` = `dramaClass || kind` and NEVER the receipt's own type, so 4 Herald pools shipped unreachable; cure = `recordedType` off the ledger row + closed-vocabulary `edgeTypeOf` (CR-HR-F4 @ 41287742); ⚠️ hand-built-link pins test the regex, not the plumbing.
- [⭐⭐ WR-7b + R-BLD-6 re-baseline](rbld6-size-baseline-rebaseline.md) — @ e51ec17e+; ⚠️ lint-staged lints only STAGED files (ratchets rot silently). [Recipe/traps](wr7b-size-ratchet-decomposition.md) — namedPersonTransit scans ALL src/domain; K3 pins EXACT import lists: register every leaf.

Folded 2026-08-04 (second compaction; verbatim):

- [⭐⭐ WZ-3 — the lint pair's FALSE premise + the razing accumulator](wz3-lint-pair-false-premise-and-razing-accumulator.md) — ⚠️⚠️ a chair ruling was built on an indentation-read that espree disproved (a bare block is not an `if`) and `mergeWarCoalitionEvidence` never could tell `undefined` from `[]`; the razing's `licenseState` split (mint reads the ACCUMULATOR, every decision reads the tick's ORIGINAL picture); ⚠️ a SELF-REFERENTIAL ceiling pin drove the arm with the constant that defines it; ⭐ CR-WR8-H ANSWERED — believed razings need NO new persisted surface (news ledger + `distancePricedNews`), and `readAllianceWebRisk` has exactly ONE consumer so nothing may rebuild the web.
- [⭐ LANE PT2 — the spine tail CLOSED 4-of-5](lane-pt2-spine-tail-closed.md) — @ 0ab5e03e; ⛔ PT2-5 origin widening OWNER-GATED (523-key golden re-record PROVEN); ⚠️ voiceMechanics 1278/670 RED is OTHER LANES', attribution earned.
- [⚠️⚠️ Authored prose is bound by the world law](authored-prose-is-bound-by-the-world-law.md) — a `port.riverside` origin variant saying "sea traffic" dropped the whole dossier to `needs_review`; ⚠️ the 525-key golden has NO port×riverside row and cannot catch that class at all.
- [⚠️⚠️ Pipeline config-slot silent key traps](pipeline-config-slot-silent-key-traps.md) — config-slot `seed`/`tier`/`terrain`/`terrainType` are IGNORED; seed=options 3rd arg, tier=`settType`, terrain=`terrainOverride`; use `gen(config, seed)` + assert `_seed` arrived. Seed now THROWS; the other three stay silent.
- [⚠️⚠️ `build:edge-shared` rebuilds FIVE bundles](edge-shared-builder-rebuilds-five-bundles.md) — NOT a one-artifact repair; 3 of 5 stale @ 2026-08-03; aiCharter/aiOutputSchema gain +113 modules / +59% each; aiGrounding folds 16 commits incl. dark WR-5/6 + HK-2 + MG-3h; resolve `sourceHash`→commit BEFORE rebuilding. (Cured @ f2c10742; standing law lives in the dirty-tree row + receipt-rules law 5.)
- [⚠️⚠️ Unreachable ratio bands + the self-belief never written](unreachable-band-and-absent-self-belief.md) — `theoreticalCapacity`'s MAX EXPRESSIBLE RATIO ANYWHERE IS 1.50, so any "N× stronger" band with N>1.5 is DEAD (cure = POINTS GAP; `PLAUSIBLE_CEILING=4.0` is that same dead constant already in the tree); `beliefRecord(x,x)` is NEVER written, so every self-belief fallback IS the only path (cure = truth self-read, the `makeBeliefStrengthFor` doctrine). ⚠ `Number(null)===0` hands the extreme verdict to the least evidence.

## Fold 2026-08-04 (~13:30) — index compaction: the fat lines verbatim (trimmed to hooks in MEMORY.md; topic files remain canonical)

## ⭐⭐ THE BUILD ERA (2026-08-03 — freshest; this account is the Fable one)
- [⭐⭐ V4C arrow final LANDED, V4D repair in flight](v4c-arrow-final-landed.md) — ⚠⚠ ANALYTIC-PIN MIRROR: a pin that recomputes the value it guards proves nothing; screenshot, then pin geometry.
- [⭐⭐ WR-10r PASSED — WAR LANE BUILD COMPLETE](wr10-sovereignty-market-state.md) — @ e3d98512+454967e0+01ec1409+18f0ced4; CR-WR10-A clamp fork + CR-WR10-B twins landed, mutant-proven; 11 foreign clamps stay UNREGISTERED (attribution honesty) → disposition wave; ⚠ a size figure is measured AT the publishing commit, never inherited; ⚠ `alignmentOf` doesn't exist — the read is `settlementAlignment(item, worldState)`; wiring still deferred on the §4 gap; ⚠ eligibility = LEDGER MEMBERSHIP, no user flag.
- [⚠️⚠️ First-match document pins retarget silently](first-match-document-pin-class.md) — exec returns the FIRST match; a second matching sentence steals the pin; cure = exactly-once uniqueness guard + point-don't-restate; estate sweep QUEUED (chair recon).
- [⚠️ Banding is lossy — same-band comparisons read as contradictions](lossy-banding-contradiction-class.md) — one band per clause, comparison in WORDS, scalars stay on the read; runtime no-decimal pin (source scans can't see composed prose).
- [⭐⭐ WR-9a REPAIRED; 9c/9d CLOSED](wr9-partial-build-and-stops.md) — ⚠⚠ N3 collector-totality: a collector that DROPS an unreadable close restores the cured blindness by another road.
- [⭐⭐ WR-8 IS CLOSED](wr8-closed-believed-razing-casus.md) — @ 1b7c1eac/39ba6590; FOUR owner-gated arms PARKED-not-owed; ⚠⚠ attribution by RECONSTRUCTION, never by splitting an id on `.`.
- [⭐⭐ WR-10 CLOSED AS A DARK INSTRUMENT — THE WAR LANE'S BUILD IS DONE](wr10-sovereignty-market-dark.md) — @ f9a7ddea+32f4708f+03b8ecde+a5647976; five PURE leaves, zero wiring, zero persisted keys, 9 executed mutants; ⚠⚠ `sovereigntyTradeEnabled` is at warConvergenceContract.js:283 NOT :54 (the brief was stale — navigate by symbol); ⚠⚠ `sovereignty_transfer`'s OWN family is FORCED by §13 stacking, never chosen; the TR-5 degradation contract is now PINNED both ways; 4 chair STOP items in the queue row incl. the war/trade twin notes that are NOT twins (`peace`)
- [⭐⭐ Ghost-write class CURED at the writer](ghost-write-class-cured-at-the-writer.md) — ⚠⚠ the brief named 5 sites, a PROBE measured 7/36 — probe before sweeping; every call site MUST pass the edge.
- [⭐⭐ LANE RR combined re-record](lane-rr-combined-rerecord.md) — origin 9→45 bodies + resourceIcon camelCase on ONE 525-key re-record [authored prose bound by the world law](authored-prose-is-bound-by-the-world-law.md) — golden has NO port×riverside row.
- [⭐⭐ LANE V4D — bole axis split + blend contract](v4d-bole-axis-split-and-blend-contract.md) — @ a2dbdd36+; the bed's VERTICAL axis is the BAR's, its horizontal the BOLE's; blend pinned + @supports fallback. ✅ its deferred navFletching gate flake is now CLOSED.
- [⚠️⚠️ vi.mock does NOT catch a dynamic import()](vitest-dynamic-import-escapes-module-mock.md) — mocking stripe's EXPORTS never severed its GRAPH (factory logged ZERO calls); cure = mock the LEAF (creditLedger); post-teardown error count == `render(<App />)` count 1:1; "passes alone" was LUCK (red 2/3 solo); retargeting pins caps at 21/27 and silently narrows 6 shell-bound pins.
- [⭐⭐ App-shell suite census CLOSED — all 5 members cured](app-shell-suite-census-and-app-prefix-grep-trap.md) — @ 456271ea; ⚠️⚠️ `grep 'render(<App'` COUNTS `render(<ApplyControls` (prefix!) — that one trap made a brief name a non-member AND miss the only RED suite (navDividers, 2 of 9 solo). Enumerate by `src/App.jsx` IMPORT; combined gate was green while solo was red.
- [⭐⭐ BUILD TAKEOVER — READ FIRST](fable-build-era-takeover.md) — Fable chairs, Opus implements+verifies; rulings = CR-*/R-BLD-* in FABLE_VALIDATION_QUEUE.md; build order = SOL_QUEUE.md; CHECK-GIT-FIRST.
- [⭐⭐ WR-9d — a YEAR-BOUNDARY census is BLIND to within-year wars](wr9d-year-boundary-census-blind-to-within-year-wars.md) — @ a70c9284/1453676b; the briefed ledger census reported an EMPTY WORLD (wars live 2-14 ticks of a 52-tick year). ⚠️⚠️ the SPATIAL CANON hides them and is ONLY in the soak fixture — probing without it CONFIRMS the broken design. Cure = the OUTCOME STREAM + prefix RE-MINT, never splitting an id.
- [⭐⭐ QUEUE ROWS = SPECS, NOT OPEN WORK](queue-rows-list-specs-not-open-work.md) — `git log -- docs/<DOC>.md` + PROGRESS blockquote before building; LD-5 verbatim DELETES Operator Messages; `?section=` never `?tab=`.
- [⭐ LANE PT2 closed 4-of-5](lane-pt2-spine-tail-closed.md) — ⛔ PT2-5 origin widening OWNER-GATED; voiceMechanics RED is other lanes'.
- [⚠️⚠️ Config-slot silent key traps](pipeline-config-slot-silent-key-traps.md) — `seed`/`tier`/`terrain`/`terrainType` in the config slot are IGNORED; `gen(config, seed)` + assert `_seed` arrived.
- [⭐⭐ LANE P — dossier state-prose BUILT DARK](lane-p-dossier-state-prose-reader.md) — `npm run gen:dossier-prose` after ANY corpus edit; DM-FIELD PROJECTION RULE; the annexes use FOUR pool grammars.
- [Icons program](lu2-icons-off-burndown.md) · [gate](icons-off-gate-ratchet-lane-lu.md) · [sweep IC](icon-sweep-lane-ic.md) — lucideTotality shrink-only; ⚠️⚠️ import-strip of an `Icon:` config field = 
- [Generator-golden shift recipe](generator-golden-shift-record-established.md) — re-record only from proven-clean HEAD; [`\bicon` blind to camelCase](case-sensitive-guard-blind-to-camelcase-fields.md) — 4,462 `resourceIcon:""` = a SECOND re-record owed.
- [Herald cures](display-class-read-as-edge-type.md) — `recordedType` off the ledger row; hop `type` is dramaClass, never the receipt's. [Slot classes](herald-prose-slot-classes.md) — UNRECEIPTED_HOP is a NOUN PHRASE; only `disinfo:` marks a plant.
- [Ribbon hazard set](ribbon-ground-move-foreground-break-class.md) — ground moves silently break correct riders (re-census ALL); [paint past the box](paint-outside-the-box-without-buying-a-box.md) — screenshot then PIN geometry; [FL-2](fletched-ribbon-fl2-landed.md) — clip-path clips the FOCUS RING; [viewBox = layout height](svg-viewbox-becomes-layout-height.md).
- [ANCHOR_OFFSET single writer + side-table class](anchor-offset-single-writer-and-side-table-class.md) — headerDesktop + SP.xxl; manifest-keyed side tables need TOTALITY pins.
- [WR-8 razing tier fork](wr8-razing-tier-demotion-fork.md) — razing emits NO tierChange; import hostile.resentment, never hardcode. [Atrocity casus](wr8-atrocity-casus-landed.md) — a new casus needs a REAL scorer + 6 surfaces + 5 literals.
- [WR-7 premise wrong](wr7-wiring-blocker-measured-wrong.md) — re-measure inherited blockers. [pulseKernel BANKED 1580](rbld10-pulsekernel-banked-permanently.md) — NOT debt; applyWorldPulse 941 owed. [Line-address rot](hand-keyed-line-address-rot.md). [R-BLD-6](rbld6-size-baseline-rebaseline.md) — lint-staged lints only STAGED; [recipe](wr7b-size-ratchet-decomposition.md) — TYPE SURFACE IS PUBLIC SURFACE.
- [⭐⭐ REALM MAGIC](realm-magic-toggle-mg3h-mg4-landed.md) — dark; L2+L3 open; PENDING_BANDS not owner-signed. [MG-1+2](realm-magic-toggle-mg1-mg2-landed.md) — new store action REQUIRES gen:compendium-data; allowlists BOOLEAN-ONLY.
- [⭐⭐ ARCANE CLASSIFIER](arcane-classifier-catalog-tag-cure.md) — arcane-ness is an ENTITY property (catalog tag); ARCANE_INST_KW unanchored ('mage' in 'PILGRIMAGE').
- [⚠️ stressor byId wrapper trap](stressor-kernel-byid-wrapper-trap.md) — byId values are {settlement, causal} WRAPPERS; bare settlements green absence pins.
- [⭐ CONTENT PROGRAM COMPLETE](content-program-complete.md) — ~10,600 sentences / 12 annexes; NEVER wire causal prose into DM-editable fields.
- [⭐⭐ HOOK SPINE](hk3-theme-aware-draws-landed.md) — dark; 525-key re-record DONE+RULED, NEVER redo. [K-scale STOP](hk1-hk2-landed-k-scale-stop.md) — owner signs K. [Architecture](hook-nonredundancy-architected.md) — DM-edited hooks never machine-dropped.
- [⭐⭐ LEGACY RETROFIT R1](legacy-retrofit-r1-complete.md) — 170/170; §1+§2 registry-backed and THROW if wired the same way (LEG-3 walker FIRST); 12 mutilated anchors OWNER-GATED.
- [⭐⭐ FP CORPUS COMPLETE](fp-corpus-handoff-2026-08-02.md) — ontology CLOSED; NEVER re-run the correction machinery.
- Pin-vacuity family: [conjunction-blind pairs](conjunction-coverage-blind-guard-pairs.md) · [empty-harness absence pins](harness-default-empty-state-vacuous-absence-pin.md) · [self-referential pins](self-referential-pin-class.md) · [unreachable conjunctions](unreachable-predicate-conjunction-class.md) · [⚠️⚠️ loop endpoint unreachable when STEP doesn't divide BOUND](loop-endpoint-unreachable-when-step-doesnt-divide-bound.md) — one fixture proves only the conjunction; seed non-empty; and compute BOUND/STEP before believing a range claim (R5: 0.98/0.05=19.6, so the pin never reaches its stated endpoint and EVERY 'float fix' stays green — plus multiply-by-WHITE makes an authored token ratio a REAL pixel ratio at a gradient's lit stop).
- [⚠️⚠️ Dead bands: unreachable ratios, the absent self-belief, and calibrated weeks](unreachable-band-and-absent-self-belief.md) — THREE shapes: max capacity ratio is 1.50 so N>1.5 bands are DEAD (cure = POINTS GAP); `beliefRecord(x,x)` is NEVER written so every self-belief fallback IS the only path; `hopWeeks` is CALIBRATED PER DIGEST — a whole realm spans 2..8 march weeks, so a band ≥ 9 refuses nothing and a bigger map buys longer weeks, not more of them
- [⚠️⚠️ A foreign file at HEAD content is a VANISH, not a destruction](foreign-file-vanish-is-not-destruction.md) — the pre-commit hook is the WRONG default suspect (`git stash create` never touches the worktree; lanes revert their own files for mutants/negative controls). Report the observation, label the cause INFERRED, GUARD the restore — the guard fired at 76ea345c and stopped me clobbering a live lane's own repair.

## 08-05 compaction (cycle-1 close era) — trimmed lines verbatim

- [⭐⭐ FP CYCLE 1 LANDED + the CQ5 row guard](fp-cycle1-serialized-landing-and-cq5-row-guard.md) — @ 93c118b6+caab995a+b441bca5+d1cfdb67, tree clean, tests/lint back to the base 19. ⚠⚠ the flag law's uncaught shape is MANIFESTED **PLUS** PARKED-AS-PENDING (the totality walker already reds the obvious one, so "row OR pending" is theatre); serialized landing = constructed blobs + `git archive` of the INDEX.
- [⭐⭐ TR-1 CASUS COMMERCII LANDED DARK](tr1-casus-commercii-landed.md) — @ d7ea69a4 (parent c7933e84, NOT my start HEAD — the tree moved twice mid-lane); 8-pair taxonomy with ZERO imports (J-TR-2 enforced structurally: disjoint type sets, no war import anywhere in the TR family), writer + amendment-B suppression naming its stock read, 20 script-EXTRACTED annex pools (NEVER hand-edit commercialReceiptPools.js), the ONE tradeWar seam moving the PROBABILITY only so rng draw counts are identical in both flag states, and the registry’s FIRST non-WAR coupling row. ⚠⚠ CARRIES THE CONCURRENT-LANE PARTIAL-STAGE RECIPE — build each shared file’s staged blob as `git show HEAD:path` + your own blocks, stage via `update-index --cacheinfo`, commit with NO pathspec, and PROVE it by running the gate inside a `git archive` of the index (diff-hunk filtering FAILS: lanes insert at the same anchors). ⚠⚠ the authoring tools interpret `\uXXXX` escapes on the way to disk — `/[\d%×_{}[\]]/u` is the only spelling that parses AND lints. ⚠ a guard gated on OPTIONAL evidence CANNOT FIRE (the suppression read was keyed on a good most pairs never name). ⚠ PAIR-ONLY ≠ slotless: 11 of 20 TRADE pools have no slotless variant, so "empty interp returns null" is a WRONG pin. ⚠ CW-0w’s receipt sampler freezes returned-read rows as shrink-only debt — re-aim the row at persisted state, never widen the list.
- [⭐⭐ FP SP-A LANDED — the band families are minted](fp-sp-a-band-families-landed.md) — @ 59df13a9, dark, ZERO src edits; ⚠️⚠️ TWO census premises REFUTED: `routine` IS live (4 kind registries + 4 news composers, so SP-6a is a BORROW not a mint — CW-1 was never blocked on the word), and the INTENSITY ladder {quiet,present,pressing,decisive} is declared NINE times under EIGHT names (frozen shrink-only, all nine pinned to spell the SAME rungs). NEVER author a decay law again (`decayTowardNeutral` + `HALF_LIFE_BANDS`, derived from INTERVAL_WEEKS) or a significance/severity scale (`bandFamilies.js`; severity = glancing/telling/grave/ruinous, zero prior spellings in src).
- [⭐⭐ CW-0w CLOSED — the registry's four owed walkers](cw0w-walkers-landed-two-stops.md) — @ b3fb8f49+f7da6b60+03dee5fd+e30770bd; couplingId regex now the closed NINE-prefix set (SC-7), registry split 3 ways (schema/war-leaf/head, one-way imports — a leaf importing the head = a CYCLE), 152-pair cross-layer INCLUSION RATCHET licensed by registry rows, schema v3 `kinds[]`, SC-9 receipt sampler, prose-numerics FIFTH detector (3 floats were escaping the scanner ENTIRELY). ⛔⛔ TWO CHAIR STOPS: the desk-agreement rule is FALSIFIED 7-of-12 (adjudication is NEVER a token output; 10 rows claim it) · proseNumerics is RED AT BASE (401 baseline vs 410 live) and needs a RULED re-record. ⚠⚠ raw NUL authored a 7th time (caught ONLY by byte-scan) · a perl anchor spelling the arrow as a \u escape matched NOTHING and exited 0.
