---
name: wr10-sovereignty-market-dark
description: "WR-10 CLOSED AS A DARK INSTRUMENT @ f9a7ddea+32f4708f+03b8ecde+a5647976 — five pure leaves, zero wiring, zero persisted keys; the WAR LANE's build obligations are complete pending 4 chair STOP items, 7 parked bands, the flag lighting, the cert row behind WR-8's +32-byte fork, and a §4 lifecycle declaration WR-10's volume never wrote"
metadata:
  node_type: memory
  type: project
  originSessionId: 1c189f3d-9fc3-4fc9-9cb7-b767525d28d5
  modified: 2026-08-04T15:01:37.212Z
---

Lane WR-10 (the sovereignty market, amendment S), 2026-08-04, branch
`claude/composite-r4` in the minifold worktree. Four commits, all COMMITTED, none
pushed: `f9a7ddea` (catalog row) + `32f4708f` (appraisal + bundle) + `03b8ecde`
(eligibility + reach) + `a5647976` (queue row). 74 new tests, nine executed
mutants, all restored md5-exact.

**WHAT EXISTS NOW** — five pure modules, none wired to anything:
`src/domain/worldPulse/sovereigntyAppraisal.js` (157/800, K3-pinned at ZERO
IMPORTS), `sovereigntyBundle.js` (165/800), `sovereigntyAssets.js` (95/800),
`sovereigntyReach.js` (105/800), plus the `sovereignty_transfer` row in
`peaceTermsCatalog.js` and its house-voice row in `display/treatyDocument.js`.
Tests: `sovereigntyTransferTerm`, `sovereigntyAppraisalWr10`,
`sovereigntyBundleWr10`, `sovereigntyMarketReadsWr10` under `tests/domain/`.

**Why:** WR-8's precedent (slices built dark, wiring chair-gated) applies. The
transfer WRITER, the plan-lane trigger and the Herald kinds are deliberately NOT
built, for three measured reasons — the war volume's §4 CANONICAL MODEL has no
WR-10 entry and no "Lifecycle paths" clause although the spec writes world state
(WR-9 got one, WR-10 did not); the write path needs a pulse stage while
`pulseKernel.js` is EXACT at 1580 and BANKED under R-BLD-10; and the three SP-6
war kind-pool walkers are already RED at base. Deferred and documented, not a bug
to re-find.

**FOUR STOP ITEMS live in `docs/FABLE_VALIDATION_QUEUE.md`'s WR-10 row** (the
last section of the file): (1) the war/trade twin notes are declared twins and
are NOT — the war volume includes `peace` in the degraded bundle, FP-TRADE §3
Seam One omits it; built around with `peace` included per WR-10's own volume, and
PIN 6 records the divergence as exactly `{peace}`. (2) the GR-3/TR-5
lit-precondition row `DESIGN_FP_COUPLINGS.md`:1875-1881 ordered was never landed.
(3) the §4 lifecycle gap above. (4) the cert row is blocked on WR-8's unresolved
+32-byte preset fork.

**How to apply.** Before touching WR-10: read the queue row, not this file, for
the chair's answers. Two things are load-bearing and easy to get wrong.
⚠️ `sovereigntyTradeEnabled` lives at `warConvergenceContract.js:283` inside
`WAR_RULINGS_FLAG_KEYS` — a scout brief citing `:54` was STALE, the file grew
through the WR-9 waves (hand-keyed-line-address rot). Navigate by symbol.
⚠️ `sovereignty_transfer` holds its OWN term family and that is FORCED, not
chosen: amendment S composes the asset against "ANY composition of EXISTING term
families", and §13 one-per-family stacking (`peaceTermsDrafting.js:43`) would
make a shared family mutually exclusive with the consideration it is exchanged
for. Putting it in `sovereignty` beside `non_intervention` is a silent bug.
Registration is byte-identical only because it has `executor:'seam'` AND no
`CLASS_TERM` entry — `CLASS_TERM` (read only at `peaceTermsAppraisal.js:202`) is
the single road a term takes to a treaty.

**THE TR-5 DEGRADATION CONTRACT IS NOW PINNED** (it was declared in two volumes
and pinned in neither): `bundleComponentFamilies()` derives from `TERM_FAMILIES`
at call time so an absent family cannot be named; a comment-stripped source scan
proves no `trade_exclusivity`/`market_access`/`toll_exemption` literal is spelled
in `src/` (guard-the-guard both ways); `catalogGrewSinceWr10()` is the tripwire
whose red means "TR-5 landed, widen the bundle, delete this arm". Do NOT mint
those rows here — GR-3 owns them and R3 owns their canonical spelling.

Related: [[unreachable-band-and-absent-self-belief]] (WR-10's reinforcement band
was the third instance of the dead-band class), [[wr8-closed-believed-razing-casus]],
[[wr9-partial-build-and-stops]], [[hand-keyed-line-address-rot]].
