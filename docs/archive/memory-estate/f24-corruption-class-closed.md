---
name: f24-corruption-class-closed
description: F24 em-dash/emoji-strip class — app-side closed on review-fixes; data-layer closure PORTED 2026-07-12 to claude/f24-data-glyph-port (ff-ready onto review-fixes, merge pending); byte-level NUL pin on recursing-lovelace
metadata:
  node_type: memory
  type: project
  originSessionId: 7b76551c-851a-4c04-8fef-152ce951ede2
---

On 2026-07-09 the surviving F24 corruption class (em-dash → `', '` placeholders, stripped-emoji leading spaces/empty spans) was fixed across ~20 files in src/components on branch review-fixes-2026-07-08, including uncited sites found by sweep (SummaryTab, CatalogTabs, EconomicsTab FLOW_STATUS labels, Section/Collapsible titles, empty icon spans). Three corrupted `.split(', ')` calls were restored to `.split('—')` (originals recovered from `38e339fd^`; the strip commit is 38e339fd, the earlier restore was 62c5e8d6, the original sweep e0250c05).

**Data layer closed 2026-07-09 (second session, commit dc3c9e63 + rebase):** 152 glyphs restored across supplyChainData (11 need + 75 resourceIcon), computeActiveChains INSTITUTIONAL_SERVICE_MAP (12), stressTypes (15), resourceData (33), threatAssessment (5), inferSupplyChains custom-chain (1, '⚙' matching its needIcon). The corruption predates the repo's first commit — no git-history restore possible, so glyphs were chosen label-keyed (identical resource label ⇒ identical glyph; scratchpad script restore-icons.mjs pattern: line-state machine keyed on adjacent labels, throws on unmapped sites). Decision: RESTORE not remove, because design intent was evident (dedicated render slots, surviving '⚙'/'⚔️' glyphs, test fixture needIcon:'🌾') — sanctioned deviation from the earlier "don't invent glyphs" convention, per owner's bold-fix philosophy. PDF layer verified untouched: all four PDF stress renders are `label || icon` fallbacks, no chain/service icon renders, golden viewModel snap has zero icon keys. Golden master regenerated (all 155 hashes flip — icons flow into the settlement hash; hash-only diff). DefenseTab's dead `{cap.icon}` span removed (deriveSupportingCapabilities never had an icon field).

**CORRECTION (2026-07-12): the data-layer closure never landed on the prod lineage.** The 152-glyph restore + widened pin (rebased commit 5761d0c5; the dc3c9e63 SHA is orphaned) sat ONLY on branch claude/ecstatic-noyce-115b4f. Task chip task_5a5ab9f9 spawned to merge it in.

**RESOLVED (2026-07-12, later session): ported to the prod lineage on branch claude/f24-data-glyph-port** (based on review-fixes@dd4f522c), three commits, tree green, awaiting a `git merge --ff-only claude/f24-data-glyph-port` into review-fixes-2026-07-08 — NOT executed because the main checkout (where review-fixes is checked out) had 12 dirty files of in-flight EventComposer work (the mid-session-branch-advance gotcha; do the ff there once that session lands):
- 46aee64c = 5761d0c5 ported; only conflicts were stressTypes.js (review-fixes had independently reduced 4 orphan-VS16 icons to '' — took the restored glyphs) and the golden master (regenerated, not hand-merged: corpus is now 187, was 155; all 187 hashes flip, key set identical, hash-only verified).
- b4461bcb = e9f24e20 (`-x` pick), gear-icon VS16; confirmed NO golden impact (custom-chain icon is display-time only).
- 775c63b1 = new: tests/generators/tuningBatchE2.test.js E2.2 narrowed to ORPHAN VS16 (`/(?:^|\s)️/u`, SIG3 shape). GOTCHA CLASS: E2.2 was authored on review-fixes against STRIPPED data as `.not.toContain('️')` — vacuously green then, and inverts into a false failure the moment data is repaired (🗡️ = U+1F5E1+FE0F is legitimate). Guards written against corrupted baselines must encode the corruption shape, not "absence of the character".
Fork-point facts: ecstatic-noyce forked right AFTER 1897377a, so 1897377a + 25597001 were already ancestors of review-fixes; only 3 commits were unique (3eb0ac5e docs migration-count skipped as stale). Verified by 6-agent workflow: suite 7557 pass/11 skip (only real failure was E2.2, fixed; rest were load flakes passing in isolation), lint 0 errors/15 pre-existing warnings, adversarial residue sweep over all 820 src files CLEAN, glyph invariants (no empties, cross-file label→glyph consistency, VS16 consistency, semantic sanity) all pass, PDF label-first claim re-verified against the NEW PDF tree (FaithWar/liveWorld: icon fields provably unreachable — buildStressEntry always sets a truthy label), pin mutation-tested (catches '' in supplyChainData AND `threatIcon: ''` planted in post-fork pulseKernel.js). Out-of-scope finding: 5 bare-VS16 drift sites (⚙ en.js:1009 + AdminSimTuningPanel.jsx:178; ⚔ ChronicleTab.jsx:48, HistoryTab.jsx:103, eventComposer/PreviewPanel.jsx:27) — chip task_2b2dd0e7.
recursing-lovelace forks at the same dd4f522c with fully disjoint files (AccountPage.jsx, supplyCompleteness.js, controlBytes.test.js) — the two merges commute in either order.

**Byte-level extension (2026-07-12, branch claude/recursing-lovelace-65d03c off review-fixes@dd4f522c, commits 2ddd1d9f + 263e53e0):** the regex pin is text-mode and cannot see raw control bytes — a literal NUL (0x00) survived as a `.join()` separator in AccountPage.jsx:79 (`grep -rlP '\x00'` matched nothing; the byte made git classify the file as BINARY, blinding diff review too). New sibling pin tests/lint/controlBytes.test.js scans raw Buffers across ALL of src/** + tests/** (every extension; empty explicit BINARY_ALLOWLIST), bans C0-except-tab/LF/CR plus DEL, reports file:line:col + offset, ~220ms. Its first run caught a SECOND unknown NUL: supplyCompleteness.js cache-key separator (fixed → '|', the separator convention e76df7c9 established). AccountPage fix e76df7c9 cherry-picked onto recursing-lovelace (also on festive-tesla — identical content, merges clean). GOTCHA for any future authored NUL-scan: writing a `backslash-u0000` escape through the Write tool plants the RAW byte in the file (5 of them, self-tripping the pin); spell escapes as backslash-x00 in prose and build payloads with String.fromCharCode(0) — never a NUL escape spelling.

**Master-lineage NUL sweep (2026-07-12, worktree amazing-euclid-6b4519 @ d024286e = master lineage, branch claude/magical-torvalds-9ef90e):** the master lineage carries a DIFFERENT NUL-separator residue set than review-fixes — the two diverged; only supplyCompleteness is common. On df217415: NUL in AccountPage(raw)+supplyCompleteness(raw). On master d024286e: NotesTab+institutionClassify+supplyCompleteness(all raw) PLUS AccountPage(escape). KEY DETECTION GAP (new, complementary to line 23's write-side gotcha): a raw-byte scan (perl `\x00`, controlBytes.test.js Buffer scan) CANNOT see `\u0000`/`\x00` ESCAPE spellings in source — AccountPage master-lineage builds `profileSourceKey` via `.join('\x00')` (escape, byte-invisible). Any hardened pin MUST also grep for the escape spellings, not just raw bytes. Fixed this session (all → `|`, uncommitted working-tree edits, NOT committed): institutionClassify.js:60 (raw; module-private `_regexIdSets` Map, safe like supplyCompleteness), NotesTab.jsx **:28(`\x00` escape)+:68(raw)** — TWO-SIDED: both build the same render-sync key compared at :38; fixing only one desyncs the equality and REINTRODUCES the keystroke-clobber bug the :62 comment warns of, so both must change together; AccountPage.jsx:99 (`\x00` escape, SINGLE build-site so auto-consistent, ephemeral render-sync state — the escape-variant of the exact profileSourceKey e76df7c9 fixed to `|` on the other lineage). supplyCompleteness.js:158 (raw) is FENCED (worldPulse/** = Modulation lane) — NOT touched in-worktree; delivered as a binary patch (scratchpad f24-supplyCompleteness-nul-fix.patch) + portable one-liner `perl -i -pe 's/\x00/|/g' src/domain/worldPulse/supplyCompleteness.js` (safe: only NUL in that file on both lineages). Behavior-safety of ALL sites verified: every key is an ephemeral in-memory/React-state memo, produced+consumed with matching separator, never persisted (saves send the field values, not the key). Routing RESOLVED: the three non-fenced fixes are COMMITTED as c0600f8c on branch claude/magical-torvalds-9ef90e — reflog-confirmed a FRESH branch off origin/master with zero prior commits (NOT the W5 branch; the amazing-euclid worktree's W5 association per [[w5-remerge-base-correction]] is about the worktree, not this branch), now 1 ahead of master and PR-able into master. Correct target because these residues exist ONLY on the master lineage (df217415 review-fixes has a different/already-fixed set). Master carries NEITHER byte-pin (controlBytes/copyCorruption live only on the review-fixes lineage), so nothing gated this — and master will keep regressing until those pins are ported (open follow-up). worldPulse/supplyCompleteness.js still handed off to the Modulation lane separately (patch + one-liner), not in c0600f8c. A lint-staged eslint --fix ran in the pre-commit hook and left the separator swaps intact.

**2026-07-14 MASTER-MERGE SURVEY UPDATE (fresh census at RF 5a78af5a × master d024286e):**
(1) ⚠️ `tests/lint/controlBytes.test.js` is on NEITHER tip — it still sits ONLY on
claude/recursing-lovelace-65d03c @ 263e53e0, so **no raw-NUL gate is live anywhere**; the master-merge
plan (docs/MASTER_MERGE_PLAN.md §5.4/W4) sequences its cherry-pick after the NUL fixes. (2) Residues at
tips: master = NotesTab.jsx:68 raw + :28 escape, institutionClassify.js:60 raw, supplyCompleteness raw,
AccountPage.jsx:99 escape; RF = ONLY supplyCompleteness.js:158 raw (owner-parked). (3) ⚠️ NEW HAZARD
MECHANISM: `git merge` AUTO-MERGES NotesTab.jsx with master's NUL pair winning silently (verified via
merge-tree simulation blob) — cross-lineage merges re-import the class with no conflict marker.
(4) The line-23 Write-tool gotcha STRUCK AGAIN: authoring MASTER_MERGE_PLAN.md with a `backslash-u0000`
spelling inside an Edit planted a RAW NUL in the doc itself (git/file(1) classified it binary; greps
silently returned nothing). Detection: `file` says "data" instead of text. Repair: python byte-level
replace — never re-Edit the escape.

Guards pinning the class:
- eslint.config.js `no-restricted-syntax` block scoped to src/components/**/*.{js,jsx}. Scope must NOT widen to globs overlapping src/generators or src/domain — flat config is last-wins per rule and would clobber the determinism guards. (Unchanged by the data-layer fix.)
- tests/lint/copyCorruption.test.js: 4 regex signatures. Widened version (ALL of src; SIG 1 `\b\w*[Ii]con` catching resourceIcon/needIcon — the plain `\bicon` word-boundary was exactly how the data layer escaped the first pin) is on claude/f24-data-glyph-port (mutation-verified), reaches prod truth when that branch ffs into review-fixes.
- tests/lint/controlBytes.test.js (recursing-lovelace): byte-level raw-control-byte pin, complementary to the regex pin (raw bytes vs decoded-text artifacts — neither subsumes the other).

Remediation conventions (per e0250c05): delete dead artifacts where no design intent exists; restore glyphs where the render slot/design demands one. Legit leading spaces exist: sentence concatenation ([[ServicesTab]]:~114 "Missing categories", SummaryTab safetyNote) — leading-space scans are deliberately NOT guarded.

CRITICAL context: the main checkout /Users/cstokes/Desktop/settlement-engine is SHARED by concurrent sessions. Consequences hit twice on 2026-07-09: (1) concurrent vitest load caused 13 false test-file failures (timeouts, PDF buffer relations) in a full-gate run — re-run suspects standalone or with `--maxWorkers=3 --testTimeout=120000` before believing failures; (2) review-fixes-2026-07-08 advanced two commits mid-session — commit locally then `git rebase review-fixes-2026-07-08` (clean; no file overlap). Always attribute gate failures via `git status` + mtimes before assuming your change broke it. Also: `npm run check | tail` eats the exit code — capture `$?` without a pipe.

Dev drive-through tricks: the PipelineReveal overlay throttles to a crawl in backgrounded preview tabs (setInterval); dismiss via `import('/src/store/index.js')` in preview_eval → `useStore.getState().dismissPipelineReveal()` (its documented Esc fast-path is NOT implemented — chip spawned). Anon free generations are capped at 3/day.
