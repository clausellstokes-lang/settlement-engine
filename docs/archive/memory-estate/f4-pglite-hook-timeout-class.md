---
name: f4-pglite-hook-timeout-class
description: F4 PGlite hook-timeout class — FULLY CLOSED 2026-07-27; cure @ ada1252d, ratchet @ 92b88c21, 8-file helper-boot gap amendment @ 250bc4cf (guards + widened marker + exec-aware walk); residual = new helper names must be added to BOOT_MARKER_RE
metadata:
  node_type: memory
  type: project
  originSessionId: ca0f8521-3047-4eb8-94ed-29586872764a
  modified: 2026-07-28T02:28:31.257Z
---

**F4 class (PGlite boot in a test hook inheriting vitest's 10000ms hookTimeout) — FULLY CLOSED on branch claude/composite-r4 (minifold).** Cure COMMITTED @ ada1252d; structural ratchet COMMITTED @ 92b88c21 ("the PGlite hook-timeout ratchet stands", manifest entry `ref: "self-proving-meta"` in the same commit); the verified 8-file detector gap AMENDED SHUT @ **250bc4cf (2026-07-27 ~20:31)** by the chip session (task_53a2c633). Chain complete; no amendment owed.

**What 250bc4cf did (both arms of the cure, chosen over either alone because the executed census showed zero collateral):**
- The eight gap files (founderTransferPayout — was PER-TEST boot with NO timeout, worst of class — creditAllocationTrigger, creditPackClawback, moneyPathJourney with tuned 30000; accountStatusProfilesCustomContent, profileModerationColumnLock, accountStatusSupportTickets, migration062Authz with no timeout) all adopted the house `PGLITE_BOOT_TIMEOUT_MS = 180_000` shape.
- `BOOT_MARKER_RE` widened to the live helper roster: `new\s+PGlite|\b(?:makeDb|makeCreditLedgerDb|baseDb|buildDb|supportDb)\s*\(`.
- The walk turned exec-aware: boot-bearing = marker match OR (file's FIRST PGlite-touching hook in beforeAll-then-beforeEach source order AND touches `.exec(`/`.query(`) — encoding the lazy-boot mechanic below. Census receipt: on the 2026-07-27 corpus this rule flagged nothing beyond the marker — pure future-proofing.
- All eight joined GUARDED_EXEMPLARS (16 total) — narrowing the marker back now reds the positive pin. FROZEN_UNGUARDED untouched (58 rows); the two false CANNOT-CATCH header claims rewritten truthfully.
- Receipts at 250bc4cf: ratchet 12/12 (9 + 3 new fixtures), eight suites enumerate under `vitest list`, controlBytes 4/4, eslint 0 problems.

**The sharp mechanic (verify any future detector against this):** PGlite boot is LAZY — `new PGlite()` returns instantly; the cold WASM boot lands on the FIRST `.exec()`/`.query()`. So the guarded set = every constructing/helper-booting hook PLUS the first exec-bearing hook while boot is unpaid. Warm maintenance hooks (truncate after a booted hook) are deliberately unguarded across the corpus and must stay out of scope — a naive "flag every exec hook" rule would red ~50 frozen files.

**✅ RESIDUAL CLOSED @ 82e2ff08 (2026-07-27, owner-ordered "fix these", chip session):** the ratchet gained a JURISDICTION test — every file under tests/ + scripts/ mentioning `new PGlite` must live in tests/security or sit in the frozen PGLITE_OUTSIDE_JURISDICTION quoter allowlist (one row: contractTestAntiVacuity.walker quotes the pattern in DERIV_RE) — so an out-of-scope constructor cannot exist to be imported into a hook. The timeout walk + helper-name law widened from *.pglite.test.js to ALL tests/security *.test.js (self-excluded: the ratchet's fixtures quote offending hooks), which caught a real escapee on first run: customContentLockOrder.postgres.test.js — and taught the exec rule its missing precondition: the file must USE PGlite (import `@electric-sql/pglite` or `new PGlite`), not merely mention it in prose, because a real-PostgreSQL suite pays no lazy WASM boot. Ratchet now 18/18. Last theoretical escape: a constructor in src/ (none exists; would be a product-architecture change, not a test helper). When auditing coverage, grep `180_000`/`PGLITE_BOOT_TIMEOUT_MS`, not a single spelling.

**Lexer hazard (bit the verification scanner):** when masking template literals, blank BOTH `${` and its matching `}` or brace-depth counting desyncs. The ratchet's string-naive paren matcher survives today's corpus but truncates silently on an unbalanced paren inside a hook's string (documented in its CANNOT-CATCH).

**Shared-tree observations from the amendment session (2026-07-27 evening):** the minifold pre-commit hook (lint-staged) runs `eslint --fix` and EDITS staged files mid-commit — it deleted an unused `eslint-disable no-console` directive from the ratchet during the 250bc4cf commit (leaving a whitespace-only line), a post-verify edit that required re-running the ratchet + eslint on committed bytes (re-earned green). Its stash/backup cycle ran and cleaned up without eating untracked files this time. lint-staged glob = `*.{js,jsx,mjs,cjs}` only — .json is NOT re-staged, so a carved manifest index blob survives the hook. Pathspec-only `git commit -- <files>` (no staging) again proved the right shape with the twin session live — two twin commits (3aa08f1d, b273d01c) landed mid-task with zero collision. See [[shared-index-commit-race]].

**HELPER-NAME LAW LANDED @ 790aa542 (2026-07-27 ~20:50, chip session):** the ratchet gained a census walker — every named function in tests/security whose body constructs PGlite (declaration/destructured/brace-arrow/expression-arrow/function-expression shapes) is censused, and any before-hook calling a constructor invisible to BOOT_MARKER_RE reds with the fix in the message; roster vacuity pin on 8 known names incl. buildDb's destructured-param shape. The two test-body boot files (customContentBackfill 60_000→house, reviewedQuarantineConflict 2× tuned 30_000→house) adopted PGLITE_BOOT_TIMEOUT_MS as per-test ceilings (testTimeout is 20000 via vite.config.js line ~743 — same band, other door). Receipts: ratchet 16/16, both suites EXECUTED 3/3 in 72s under co-tenant load (~22s/boot — straight through the old ceilings). ⚠️ COORDINATION FOR THE BURN-DOWN FOLD: the ratchet file MOVED past 250bc4cf (new functions matchedClose/constructorHelperNames/helperLawViolations + a new describe; FROZEN_UNGUARDED literal and its freeze comment UNTOUCHED) — re-read before applying the staged literal clear; the ratchet now runs 16 tests, not 12. The chip session holds the owner's in-chat push order (2026-07-27 ~20:35) and has a watcher armed: burn-down fold lands → full gate → push composite-r4. Do not duplicate the push.

**PUSH LANE HANDOFF (2026-07-27 ~21:58, chip session → hungry-driscoll):** the chip session's push attempt at 28ba808d was BLOCKED by the pre-push hook gating the live tree — two typecheck reds both attributed to another lane's uncommitted WIP (untracked src/domain/factionRename.js directly; src/store/index.js:77 as a cascade from dirty slice files into a clean call site); the committed state PROVEN clean (tsc exit 0 in an isolated worktree at 28ba808d). Owner authorized bypass-if-isolated-gate-green in the chip session (~21:55); then hungry-driscoll messaged holding a direct owner order to gate→push at 5f6abbfd (superset SHA) with its gate already mid-tsc in f4-gate-scratch — verified against the tree. Chip session STOOD DOWN per the coordination protocol: duplicate isolated gate stopped, pushcheck worktree removed, no push issued.

**✅✅ PUSHED @ 5f6abbfd (2026-07-27 ~22:20, hungry-driscoll): origin/claude/composite-r4 = 064bb460 → 5f6abbfd, ls-remote verified.** Full gate executed at HEAD in the detached f4-gate-scratch worktree: validates ×7 + both typechecks + lint (0 errors/25 warnings) passed; full suite 19,721 passed with 5 reds that were ATTRIBUTED as cross-session load flakes (all ~20s testTimeout band: townScene3dLazy, fullPdf.render ×2, advancePauseResume, pulseUndoAdvertising — re-run solo single-threaded with zero vitest contention: 4 files / 69 tests green); build exit 0; verify:dist exit 0. Push issued FROM the clean scratch worktree so the pre-push hook gated a green tree — the owner's bypass authorization was never needed. Notable: the full-parallel gate ran all 98 pglite suites with ZERO hook timeouts — the burn-down held at scale on its first full-load exposure. ⚠️ HAZARD RE-CONFIRMED (piped-gate-exit-masking): the gate was first read through `npm run check | tail` — the captured $? was the PIPE's exit (0) while 5 tests had failed and the && chain had stopped before build; the lie was caught only by grepping the log body. Never read a gate through a pipe; capture the command's own exit directly.

**✅ BURN-DOWN LANDED @ 498e545b + plant @ 28ba808d (owner-ordered "do these", 2026-07-27 ~21:40, session hungry-driscoll):** all 63 frozen hooks across the 58 files carry `PGLITE_BOOT_TIMEOUT_MS = 180_000` (one-line form the ratchet message prescribes; tuned-30000 stratum replaced, stale cold-start comments stripped) and FROZEN_UNGUARDED = `Object.freeze({})` — zero-tolerance, never re-add a row. 28ba808d = sweep area 56 (standing plant: unguarded `new PGlite` beforeAll → systemConfigPublicRead; label `pglite-hook-timeout/unguarded boot hook planted`) + manifest entry rationale→mutation, CARVED (HEAD content + only my hunks staged via save/write/commit/restore — the area-52 anchor re-point and ahead-of-fold manifest hunks stayed uncommitted for their owners; lint-staged skips .sh/.json so the carve survives the hook). Receipts on the committed tree: node --check 59/59, live detector zero offenders corpus-wide, ratchet 16/16, 8 representative suites 79/79 executed under co-tenant load, eslint 0, plant control red→md5-identical-restore→green (also proven at 250bc4cf in the temp worktree first). ⚠️ Known foreign red at commit time, attributed in the commit body: untracked tests/components/editNamesCanonicalFactions.test.jsx (another lane's E-A fold friction) reds the manifest TOTALITY check until its own fold adds an entry. NEXT: the chip session holds the owner's push order (burn-down fold → full gate → push composite-r4) — do not duplicate the push. Twin receipts recovered from transcript for the record: 92b88c21 shipped on "ratchet 9/9 + planted-file negative control red→green, manifest meta-test 6/6, six suites 44/44". Transform driver pattern worth reusing: extract the committed gate's own detector at runtime and drive the sweep off it, so transform and gate cannot disagree; verify in a detached temp worktree with the lane's node_modules symlinked, then re-apply at fold.
