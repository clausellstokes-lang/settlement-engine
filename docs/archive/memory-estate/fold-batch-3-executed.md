---
name: fold-batch-3-executed
description: "⭐ FOLD BATCH 3 EXECUTED 2026-07-20 — staging branch claude/composite-r3 @ accd0242 (base 24f46337, NOT promoted, NOT pushed); budget GREEN 1,024,371/1,040,000; only reds = the 4 parked goldens; migration head 167; task-#40 flags inside."
metadata: 
  node_type: memory
  type: project
  originSessionId: c7979c3b-d9d7-48bb-a499-e2271011bf43
  modified: 2026-07-20T04:58:52.344Z
---

FOLD BATCH 3 executed 2026-07-20 in worktree `.claude/worktrees/fold-r3`.

**State:** `claude/composite-r3` @ accd0242, forked from the live composite tip
24f463374162008ab4ac4968855b9ff49153a700 (re-verified at fork time; it had NOT moved).
NOT promoted to claude/the-composite, NOT pushed — both owner-gated.

**All 15 manifest branches merged (--no-ff, NUL-scanned, per-merge focused gates) PLUS
claude/deep-couplings @ 627055b3** — ⚠️ the manifest's "82c8095e = D-0/1/2/7/4 in one
lineage" was WRONG: the contest stack (base 29f7abc9, 14 commits) holds only D-2/7/4;
D-0/D-1 live on claude/deep-couplings (6 commits off aad6265e) and were merged as 1b.

**THE PAYOFF:** first-paint closure 1,024,371 B ≤ 1,040,000 budget (headroom 15,629) —
the 1,040,998 breach is CLEARED by the de-eager reclaim. verify:dist 23/23 green.

**Migration renumber:** contiguous head = 167. purge-spares 162→165, retention cron
163→166, client errors 156→167; wave-d token bucket KEEPS 156; money keeps 157–164.
⚠️ manifest said "ruin-filter 162" — empirically ruin-filter carries NO migration.

**Suite reconciliation (two shards + isolation re-runs):** the ONLY remaining reds are
the 4 parked golden families (generatorGoldenMaster / beliefMapGolden /
worldpulseDeityGolden / pdf goldenViewModel — ONE REGEN re-mints). Everything else
green: shard1 6771 passed, shard2 7591 passed, all non-golden reds fixed + committed.

**Ceiling re-pins (measured):** kill-list borderRadius 906→100 · boxShadow 98→72 ·
rgba 234→167 · tinted 214→161; title= census 487→488→494 (money's 3rd Section prop +
WBF-c's 5 Mechanism props + 1 deliberate retention tooltip — all documented in-file);
raw-color held at 1403 (traditions '#8a5a1a' routed through swatch['#8A5A1A'] in both
category modules); size-baseline settlementSlice 1345→1344; domainAnyCastBaseline
reconciled via --update to 2242 (ceiling 2252 UNTOUCHED — JUDGMENT: the D-7-leaf 0-hole
annotation needs real code edits in golden-pinned files, left to a burn-down lane).

**Fold-composition fixes worth knowing:** (1) D-2's buildLiveNpcIds prune scan now reads
the UNTOUCHED item.save roster (roads §8 master gate would have pruned a captive's
credibility key mid-captivity); (2) WB-j traditions deep-link test pins the PROP contract
(URL parsing lives in CompendiumPanel in the composite).

**⚠️ TASK #40 FLAGS (non-trivial post-fold seams, all with in-code deferral records):**
- D-3 self-policing loop: intelSaleCredibilityDeltas has ZERO live callers — needs a
  sale-accuracy resolver + spokespersonNpcId persisted on intelTransfers records.
- D-4→D-2 bluff charge: npcLadderKernel.js `void res.bluffDeposits` (detected, not
  charged) — needs a deposit sidecar + statecraft consume arm.
- D-4e champion producer: `backedBy` is consumed (contest margin) but NO producer
  writes it (no DM verb / dispatcher kind).
- D-5/D-6 roads-dependent lanes (per the brief, not started).
