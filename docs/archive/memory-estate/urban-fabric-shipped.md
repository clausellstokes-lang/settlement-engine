---
name: urban-fabric-shipped
description: "THE URBAN FABRIC LAYER (task #39, the last engine brick) FOLDED into claude/w7-prep @ 72be500f: 12-class district prominence integrators + regime lingering + alignment drift + typed scars + catastrophe rebirth on lazy urbanFabricKernel.js behind virtual urbanFabricEnabled; #38 read API fabricRead.js; committed dormancy golden; judgment tables signed under the RULING #5 blanket"
metadata:
  node_type: memory
  type: project
  originSessionId: 7115c211-9732-4751-8d73-170ba6bbcf31
---

# THE URBAN FABRIC LAYER — FOLDED @ 72be500f (2026-07-17)

The map's memory (owner commission, task #39). Built on `claude/urban-fabric` (3 commits
off 9b9e525a, tip 0b9107d1), folded conflict-free. NOT pushed.

## Mechanisms (all interval-invariant over calendar.elapsedWeeks deltas, no rng)
- **Prominence stocks**: per-settlement, per-district-class decaying integrators on the
  town map's 12-enum. ⚠️ Categories are PINNED, NOT IMPORTED — districtProfile drags the
  causalState/activeConditions family into the engine graph (aiGrounding freshness trap).
- **Regime lingering = decay + deposit**: the fallen class just stops receiving its
  ruling deposit (merchant→noble fixture: ~6mo dominant, ~2y overtake, exactly ONE turn
  beat via LEAD_FLOOR 1.0 / TURN_MARGIN 1.15 hysteresis; `led` field kernel-internal).
- **Alignment drift**: scalar integrating toward 1−lawfulness01 (settlementAlignment).
- **Stressor scars**: typed kind-keyed decaying records; per-kind masonry half-lives
  (lean_years 78wk … siege_repairs 260wk).
- **Catastrophe fast path**: fresh high-water calamity stamps with toll ≥100 reset the
  struck classes (≥400 town-wide), REBIRTH_CAP 6, chronicle beat.

## Storage / flag / API
Sidecar `spatialLedgers.urbanFabric` (drop-when-empty, EXEMPT_LEDGER_KEYS companion) +
compact `settlement.urbanFabric` mirror (acquiredTraits self-healing idiom). Measured:
844 B/record, <2KB worst-case pinned. Virtual `urbanFabricEnabled` — dormancy golden
COMMITTED (wired-dormant byte-identical; dark path returns exact input references);
lighting rides the pre-signed regen batch. #38 read API `src/domain/townMap/fabricRead.js`
(fabricStocksFor/fabricDriftOf/fabricScarsOf/fabricRebirthsOf/hasFabric): zero engine
imports, absent/dark ⇒ empty, **drift returns null not 0.5** (absence ≠ neutrality).
Chronicle voice registered in WHAT_PHRASES + EXPECTED_VOICE (the growth wave's gotcha).

## Judgment tables (signed under RULING #5, vetoable)
Deposit map (rate/week × signal01, equilibrium ≈ rate×375): ruling 0.020 → archetype
class · institutions 0.006×n (≤3) · faith 0.015×share → religious · income 0.008×n (≤2)
· trade 0.012×thr01 → merchant · entrepôt 0.008 → foreign · tier 0.010 → residential ·
food deficit∪famine 0.012 → criminal. STOCK_HALF_LIFE 260wk, DRIFT 104wk, STOCK_MAX 10,
PRUNE_EPS 0.05. One classifier: canonical factionArchetype + fabric-noun fallback.

## Hazards this lane hit
Worktree deleted by the rate-limit kill → cwd fell to main tree, caught by the location
guard; remediated to `.claude/worktrees/urban-fabric` (main tree never mutated). Zero
eager bytes (closure identical before/after); pulseKernel wired by NAME-SWAP only
(ceiling 1387). A foreign `stash@{0}` exists in the shared repo — another session's,
untouched. Related: [[hot-files-at-max-lines-ceiling]], [[gallery-phase2-shipped]].
