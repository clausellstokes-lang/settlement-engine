# DESIGN_SPINE_COMPLETION — every remaining wave to the port, architected

**Chair-authored (Fable), 2026-08-26, under §682 — the succession document. Written so
that Opus lanes AND an Opus chair can execute the rest of the released arc without
re-deriving any judgment this document can pre-make. Authority chain (later wins):
DESIGN_SPINE body+A1+A2+addendum → the reconciliation (§675/§677/§678, A13/A14) →
THIS DOCUMENT → its panel amendment. Constitution for every wave below: the §652
three-clause contract · §591 totality · gestalt-first at PAGE SCALE (§649.3/§660) ·
the default-off law (§679: the default view is the readable minimum) · counts are
verdicts · every zero has a planted control · seal before declaration · patches
assert-and-chain (§678) · the FULL-ARM law (§681.4: every armed run includes every
arm, `--river` named) · clean-room (§668/§673). Vetoable throughout.**

## §1 · THE DRESS RE-BASE — the world wears ink (two cars)

The largest remaining wave: the partition finally rendered. The binding references:
the detail register (reg-detail, BINDING per-class anchors) · the R-DEVLOG 45 laws
(draft-R-DEVLOG.md, routed by layer) · REG-2's rampart dress contract · the signed
bands (SIGNED_CONSTANTS.md) · the A14.4 survivor rows.

**DRESS-1 — THE STRUCTURAL INK (ground · masses · walls · water):**
- GROUND: street-gap surface with the tone separation the review demanded (I4: street
  ground ≠ plot ground; a value floor so fabric never falls to paper — I3's cure);
  the 0.25/0.72 grain-line family clipped to its lawful surfaces.
- MASSES: the roof grammar (B7's cure) on party-run masses — ONE ridge per party-run
  (truth, hf378), hips at run ends, the two-tone SE-dark law, chimneys astride
  ridges, eaves shadow; ridges CLIPPED to masses (the overshoot class dies); member
  unit-lines per A1.2 (no hollow wash — banned). Monuments at their own register
  with plan-poché (hf323's rank ladder) — never aggregated.
- WALLS: REG-2's vocabulary re-targeted to the BAND FACE (coursing, towers-as-joints
  at the facet economy's vertices, gates seated IN the band with hf313 anatomy, the
  ditch as radiating hachure not dashes — I10's cure); WEAR from the ledger's
  maintenance facts × age (L-REG-18); relict rings get their dressed style or
  coherence-threshold suppression (B4's cure).
- WATER: the bank-bounded body with shore-parallel strokes (the plate corpus is the
  drawing authority — §640.3); decks OVER water with the shadow line (§649.2's
  fresh-eyes exit); the ford glyph re-cut reference-bound (road narrows + transverse
  ripple dashes, no ring, no dot-chain); quay/pier ink on the moored faces with
  V-QUAY at its band; harbor water calm-ruled.
- EXITS: the gestalt gate FIRST (a fresh-context reader answers "what does this read
  as" per leaf at page scale; "bridge" at every crossing crop) · the value-hierarchy
  census (zero near-paper fabric at any tier; squint at 320 px keeps hierarchy) ·
  i1/i5/i6/i7 re-recorded as DECLARED baselines on the new ink · byte/op at pins ·
  dormancy/determinism · **E11's accessible-lens arm LANDS HERE** (it was
  structurally homeless until ink exists) · L-REG-34's legend-agreement census
  (every legend row locatable, every glyph taught).

**DRESS-2 — THE LIVING SURFACE (furniture · relief · fields · scenario · glyphs):**
- V-B13 on VOID faces at the signed band; civic-process furniture per hf342/hf320
  (the I12 rows); the §646 glyph fixes (water-gate weight, greens as common-ground
  fill, diamonds/spirals legended or dropped).
- RELIEF: structured grammar replacing tick noise (I6's cure) — ridge-following
  hachure runs, form-line chains, terrace steps at relief≥band sites; sheet-wide
  expression so a steep-hills cartouche is never a flat sheet (§646.2); gradients
  stay weights-grade (R-MORPH §4's own flag) until sourced.
- FIELDS/HINTERLAND: the mosaic CONTENT on FIELD faces (A14.4's REG-H half): strip
  texture INSIDE boundaries (never facet quilts — the §654 chair-read + FTG's
  quilted caution), meadow on floodplain (the R-MORPH inversion), hedgerow trees,
  lanes reaching furlongs, L-REG-33's intensity gradient at trade tiers; grazed
  commons look grazed.
- SCENARIO WEIGHT (I2's cure): glance-weight marks — camp footprints with tents
  OUTSIDE bowshot (the Corfe band), siege/circumvallation lines, barred-gate
  emphasis at every gate, emptied-market treatment; the element-diff census (a
  scenario leaf differs from its base by ≥ a chair-set floor, with the year-100
  control ≈237 as the calibration).
- EXITS: blind silhouette ROUND 4 (the sharpened §661 class definition; the sheet
  gains the pier/quay-group bin) · the scenario element-diff census · the corpus-
  distance re-read per detail-register section · the gestalt gates again.
**Sequencing note:** DRESS-1 before DRESS-2 (structure before furniture); each sealed
separately; the L4 MF-A1 paint pass integrates at DRESS-2's end (the recovered
module paints over honest ink; wrap-count preconditions per A2.2's old pin).

## §2 · REGISTERS/ZOOM (the REG-10-class wave)
The reveal ladder on partition frames: page → ward → block → plot, driven by the
identity law (every mass resolves; zoom dissolves masses into plots into buildings;
"then DW's interiors" when DW lands). A4's tiered budgets bind per rung. Monument
registers per hf323. EXITS: a zoom-continuity census (no element appears/vanishes
except by the ladder's own law); per-rung byte budgets; the reveal pipeline's
performance at the A1.6 protocol.

## §3 · WORDS (REG-8, reduced)
Cartouche language + the ENUMERATED denylist + the formula-pattern arm (A2.2-era
rows) · the R-DEVLOG label doctrine: the clutter ladder, straight-skeleton label
placement, names decoupled from the geometry seed · L-REG-35 event addresses (the
census: every dated label at its addressable place) · L-REG-29 intact (no generated
street/bridge/gate names — the DM's machinery is the naming path). EXITS: the
legend/label censuses; the arc-label kerning fix (C3); label-over-fabric collision
census (the §657 marginalia lesson).

## §4 · JUDGING (REG-9)
THE RUBRIC, pre-ruled: (1) the §679 constitution — composition beats accuracy; (2)
§591 totality — the weakest element grades the map; (3) §580 triangulation — reads
like the leads, dressed like the corpus, true underneath. ROUND STRUCTURE: render
full-arm → fresh-eyes gestalt reads (page scale, then crops) → convict → cure →
re-round; the owner's eyes close each round. RULINGS THIS WAVE OWNS: the
quarter-name-earned candidate (§646.1iv) · pier-length variation (§641.1) · cigar
retention 52–76 % · town-2 vacuity · region-typing centroid note · the walk-scale
rings' survival (i8 4b's named subtraction candidate). The blind-read program
continues per class until every class passes its sharpened definition.

## §5 · DECLINE DRESS (REG-D)
Draws the ledger's REACHED states only (A10/A14.5): the hf379 ladder time-driven on
the abandoned; subdivided grandeur (rich faces → multi-poor faces, the §wealth
asymmetry); the oversized high-water circuit as memory; DEBRIS/RECLAMATION at their
frozen stages (L-REG-27, the face state machine); ruins-need-addresses (L-REG-36
census). EXITS: a reached-state totality census (every LossRegion state has its
dress; no dress without a state); canon honesty (zero invented decay); the gestalt
gate on a declined specimen (the §611 "city that became a town" read).

## §6 · SHARE + LEGACY SWEEP + THE FILM (REG-11 · REG-F0 · REG-F)
REG-11: the share/landing assets regenerate from the partition pipeline. REG-F0:
the LEGACY SWEEP ONLY (A14.1) — enumerate any pre-partition layer still emitting
(chrome, relief remnants); each gains A6.1 data or retires. REG-F: the film
assembles from the native emissions — L-REG-20 clock (max(30 s, build);
story-paced; skippable; the replay IS the share clip) · L-REG-21's twelve beats
with the §679 restraint laws (stop at the emergent reading; calm ink; salience from
pacing) · beat frames rendered from truncated folds (~20 × ~1.3 s fits the floor,
§643.2) · canon-honesty scan · **the FILM TASTE SPECIMEN: one city's whole life,
judged by the owner before the wave completes** (unchanged — this is the owner's
gate by design). EXITS per A5.2's REG-F row as amended.

## §7 · THE PORT (REG-P) — the cutover the owner throws
The readiness gates, COMPILED (every one already ruled): the §652 three-clause check
· tierForPopulation ≡ popToTier by execution · the partition invariant censuses ·
the A6.1 totality walker · hasDrawableMap re-proven at the partition view (§677.2ii)
· the WEB-12 commitEdits pin re-proven (§677.2iii) · the vocabulary totality walker
(the one bill for REG-3/4/5's five vocabularies — a NEW test file; its three-ratchet
cost is THE PORT'S, budgeted) · the byte/op ratchet instrument landed (CAR-BUDGET's
residue) · the declared-shifts compilation (every §110.3 table since the spine,
one document for the owner). THE RETIREMENTS: legacy painters, carto:bridge, the
legacy port path, townLayoutV2's map consumers, cartographyPaint identity, the LOD
merge (ONE aggregator law). THE D3a REFRESH: the dormant core re-syncs from the
LAST SEAL before cutover — never a stale core. WHAT THE OWNER DOES: reviews the
declared-shifts document, runs /code-review ultra if desired, throws the flag.
POST-CUTOVER: COVER-REFRESH re-bake (§677.2iv) · the AD program on the partition
views (brief authored fresh from §675.2) · DW anchored to plot faces · the walk +
ONE regen with the gates absorbed (§672.1) · soaks · STOP AT TUNING (the owner's
signature, with SIGNED_CONSTANTS as the walking sheet).

## §8 · THE SEATING CAR (post-spine, before/with DRESS-2)
A2.4's decomposition: the QUOTA DECK from the ledger (institution mix per tier from
truth; slum-as-overflow = poverty as emergence under our wealth facts) + §640's
scorers as location weights (hospitals edge-or-gate; noxious water-pull with
intramural lawful; fairs hall-or-field; inns on approaches) + the PORT-ATLAS content
row (maritime categories minted so ports can earn Docks Districts — §660.1) + B6's
physicalViolations===0 gate. EXITS: seating censuses per pull (weights produce
VARIETY — the census checks distributions, never uniformity); town halls DRAWN;
graveyards PRESENT.

## §9 · SEQUENCING + THE SUCCESSION PROTOCOL
**Order:** GROW-A seals → DRESS-1 → SEATING + DRESS-2 (parallel only if file-disjoint;
else SEATING first) → REGISTERS/ZOOM → WORDS → JUDGING rounds (iterate) → REG-D →
REG-11 → REG-F0 → REG-F (owner judges) → REG-P (owner throws) → post-cutover per §7.
**For an Opus chair, the discipline distilled — these are laws, not style:**
1. Gestalt first, page scale first; a census never overrules the owner's glance.
2. Seal BEFORE the ledger entry that declares it; --require-ref on declaring commits.
3. Every patch asserts its anchor; every commit chains on its script's success.
4. Counts are verdicts; exit codes are not; a zero without a live control is
   UNVERIFIED; a control that cannot fail proves nothing.
5. The FULL-ARM law on every armed run; re-read the slot at every proof boundary.
6. Lanes never move refs, never write the ODQ/MEMORY, always leave resume blocks;
   resume MID-LANE from receipts, never restart (§448).
7. Honest reds outrank green claims — never relax a census to reach a count; record
   corrections of your own record without flinching (§655/§681 are the models).
8. Absence decays: re-measure every inherited zero/figure at the slot that consumes it.
9. Weights and varieties, never walls, for anything dossier-grounded; PLAUSIBLE
   consumes nothing; fetched-CONFIRMED mints constants.
10. Every signing updates SIGNED_CONSTANTS in the same act.
11. OWNER-GATED, always: every push/deploy · the cutover flag · the tuning signature
    · legal · the cull · /code-review ultra · the film specimen verdict · new
    capability vs repair when genuinely novel. When in doubt, record vetoably and
    proceed only if repair-class; else park it on the docket.
12. The ledger is append-only truth: corrections are new entries, never edits.
**The escalation list to the owner (park, don't guess):** anything touching THE
PROMISE's storage/physics · public-surface content homes · paid-surface behavior ·
taste calls where two judged rounds disagree.
