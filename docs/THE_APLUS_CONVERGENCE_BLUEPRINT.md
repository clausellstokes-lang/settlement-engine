# THE A+ CONVERGENCE BLUEPRINT — what moves all 20 bars to A+ (Fable, 2026-07-21)

Fixing every cycle-2 finding to zero is NECESSARY but NOT SUFFICIENT for across-the-board A+.
The standard (docs/THE_APLUS_STANDARD.md) defines A+ as excellence made STRUCTURAL —
enforced by machinery that survives negligence — with three tiers: PROVEN (pre-launch,
provable), CERTIFIED (soak-signed), LIVED (human-signed). "A+ across the board, LIVED" is
IMPOSSIBLE pre-launch by definition; the achievable target is: **every provable bar PROVEN,
every certifiable bar INSTRUMENTED + awaiting its certifier, every owner-gated ceiling built
to the edge + queued.** This is the six-tranche path to that state.

## Where each bar sits AFTER fixes-to-zero, and what closes it to A+

### Group 1 — PROVABLE TO A+ BY FIX-TO-ZERO + ENFORCER TRANCHE (mine, no owner, no soak)
- **1 CORRECTNESS** — fixes clear defects; A+ needs a remover per found class. SS4 took
  mutation-sweep 8→12; A+ = extend it to every correctness-asserting invariant (or document
  the residual subset with rationale). CLOSABLE.
- **11 DETERMINISM** — already bit-for-bit; fixes respected the golden discipline. A+ standing.
  (The recorded-depth finding is a bar-20 enhancement, NOT a determinism gap.) CLOSABLE NOW.
- **12 STATE-LIFECYCLE** — SB1 closes orphaned-write findings; the save museum exists. A+ =
  every path round-trips byte-exact (SB1 + a round-trip walker). CLOSABLE.
- **13 CLAIMS-PARITY** — SS4 built the claims gates + idx0 fixed. A+ = every claim mechanically
  bound to its gate (SS4's corpus). CLOSABLE.
- **19 AI STRUCTURE** — the clerk's constitution mostly holds. A+ = a per-AI-surface source-scan
  walker (the import-wall pin generalized) + the no-load-bearing-AI proof (every surface has a
  deterministic fallback, tested). CLOSABLE (enforcer build).

### Group 2 — PROVABLE HALF NOW, FULL A+ NEEDS AN OWNER ACT
- **2 COHESION** — fixes single-source the surface contradictions (isLiveWarFront). A+ needs
  every force to cite its RECORDED law — but the provenance ledger ships dark; lighting it in a
  preset is the ONE REGEN (owner). Provable half closes; full A+ waits on the regen.
- **6 SECURITY** — fixes close the XSS sinks. A+ = every trust boundary proven by EXECUTION incl.
  the CSP Report-Only→ENFORCE flip (owner/deploy) + exploit-attempt CI. Owner-gated tail.
- **8 CONTENT/VOICE** — E2 voiceMechanics walker built, em-dash to zero. A+ = extend E2 to JSX +
  the deferred golden-bound prose cleanup (eventProse/roads/tradition + letter humanizer wiring)
  = ONE REGEN. Owner-gated tail.
- **14 DISCOVERABILITY** — idx36 canonical fixed. A+ (repo half) = prerender/sitemap/JSON-LD
  completeness proven; rankings themselves are the time/links CEILING. Repo half CLOSABLE.
- **17 UI/UX CRAFT** — wave 4 primitives + the ratchet family. A+ = ratchets driven to TARGET;
  deepCraftKillList→0 = de-rounding ~510 styling lines, an owner TASTE decision. Owner-gated.
- **18 IMMERSION** — spell-break census built (shrink-only baseline). A+ = burn it to ZERO;
  the golden-bound breaks (letter tickSpeak, chronicle rawId) = ONE REGEN; lived tier signs the
  rest. Owner-gated + lived tail.

### Group 3 — CERTIFICATION-CEILINGED (A+ pre-launch = instrumented + awaiting signer)
- **3 EXPERIENCE** — repo-addressable gaps (drawer discoverability, mobile living-world) CLOSABLE;
  the "felt in ten minutes" is LIVED-signed.
- **4 SUBSTANCE** — arc-soak instrument BUILT (on-demand proof exists). A+ = per-mechanism lit
  walkthrough walker + the SOAK certifies the rates. Instrument done; soak signs.
- **5 PERFORMANCE** — B+; time not enforced. A+ = a deterministic op-count budget on the tick
  pipeline (mine) + the TTI/INP harness RATIFIED against a prod build (owner) + soak.
- **9 ACCESSIBILITY** — wave 4 closed hover/keyboard-trap. A+ = keyboard map-placement follow-on
  (mine) + LIVED screen-reader certification.
- **10 GAME-FEEL** — designed + instrumented pre-launch; CERTIFIED only by human joy. Lived-signed.
- **16 DATA STEWARDSHIP** — deletion-completeness/export-totality findings were REFUTED (already
  hold); A+ = the id-free prop-hygiene law proven + retention pinned to actual SQL. Mostly CLOSABLE.
- **20 THE STORY (capstone)** — story census B. A+ = arcs proven (arc-soak) + narrated IDENTICALLY
  across letter/chronicle/book/cause-walk (a narrative-parity walker, mine) + the recorded-DEPTH
  enhancement (kernels adopt outcome→outcome causedBy — currently one-hop-to-root) + LIVED (the
  story moves someone). Instrument done; depth-enhancement + lived sign it.

## THE SIX TRANCHES (execution order)
1. **FIX-TO-ZERO** (in progress) — every confirmed/partial finding fixed with a pin, refuted struck.
2. **ENFORCER TRANCHE** (mine, provable) — the missing standing removers: mutation-sweep→all
   invariants · candidateType→apply-arm exhaustiveness walker · lifecycle round-trip walker ·
   per-AI-surface source-scan + no-load-bearing-AI proof · E2→JSX · op-count tick budget ·
   narrative-parity walker (one story, all surfaces). Moves Groups 1 + the provable halves of 2.
3. **RATCHET BURN-DOWN** (mine where not owner-taste) — drive spell-break census, error-copy,
   and the non-taste kill-list entries to their zero targets; the golden-bound + de-round-taste
   remainder passes to tranche 4/5.
4. **THE ONE REGEN** (OWNER) — light the provenance ledger in a preset (unlocks 2/4/20's recorded
   half) · regen the golden-bound prose (8/18) · the idx21/19 + obligations-decay tuning · the
   letter-humanizer wiring · re-record the 4 parked golden families ONCE. Signs the tuning manifest.
5. **OWNER POSTURE ACTS** — CSP enforce-flip (6) · COGS margin readout deploy (7) · DR runbook
   REHEARSAL (15) · AI-generator re-enable decision · kernel outcome→outcome adoption for story
   depth (20) · the de-round taste call (17).
6. **CERTIFICATION RUNS** — the SOAK signs the rates/boundedness (4/5/20-rates) · the LIVED tier
   (real DMs/tables) signs joy (10), felt-depth (3), screen-reader parity (9), story-moves (20).

## THE HONEST CEILING
After tranches 1–3 (all mine): Group 1 bars at A+, Group 2 at "A+ pending one owner act",
Group 3 at "A+ pending soak/lived". After tranche 4–5 (owner): Groups 1+2 fully A+. After
tranche 6: everything provable proven + everything certifiable signed = A+ ACROSS THE BOARD
in the only sense the standard permits pre-launch. The word "everything" is honest ONLY with
tranches 4–6, and 4–6 are NOT mine to execute — they are owner-gated or reality-gated by design.
