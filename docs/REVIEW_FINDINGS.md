# Comprehensive Review Findings (133 confirmed, adversarially verified)

1 critical, 14 high, 65 medium, 53 low. Generated by 13-dimension multi-agent review.


## 01-edge-functions (6)

### [HIGH] supabase/functions/stripe-webhook/index.ts:177 — stripe-webhook uses synchronous constructEvent in a Deno runtime (may fail signature verification at runtime)

**Evidence:** Signature verification uses the SYNCHRONOUS `stripe.webhooks.constructEvent(body, signature, webhookSecret)` (line 177) with the esm.sh stripe@14.14.0?target=deno build. In Supabase Edge (Deno) the documented/portable pattern is `await stripe.webhooks.constructEventAsync(body, signature, secret, undefined, Stripe.createSubtleCryptoProvider())`, because the SubtleCrypto provider cannot run in a synchronous context; the sync call can throw 'SubtleCryptoProvider cannot be used in a synchronous context.' If that applies to this runtime, EVERY webhook hits the catch at lines 178-181 and returns 400 — paying customers are charged by Stripe but never receive credits / premium / founder grants (crown-jewel breakage). CAVEAT: Deno's node:crypto compat may give the Stripe SDK a synchronous HMAC provider, in which case this works fine; I could not execute the function to confirm. Flagging for runtime verification given the severity if real.

**Fix:** Switch to `await stripe.webhooks.constructEventAsync(body, signature, webhookSecret, undefined, Stripe.createSubtleCryptoProvider())`, and add a smoke/integration test that posts a correctly-signed event and asserts a 200 + state mutation.

**Fix note:** The suggested fix is correct. Use `event = await stripe.webhooks.constructEventAsync(body, signature, webhookSecret, undefined, Stripe.createSubtleCryptoProvider())` (the async path: verifyHeaderAsync → computeHMACSignatureAsync exists and works in this build). The same defect exists in any other edge function that verifies Stripe webhook signatures synchronously — only stripe-webhook/index.ts does signature verification here (create-checkout, verify-single-dossier, create-customer-portal use the SDK for API calls, not constructEvent, so they are unaffected). Critically, the existing contract tests are source-text-only and will NOT catch a regression; add a real integration smoke test that posts a correctly-signed event (using stripe.webhooks.generateTestHeaderString with createSubtleCryptoProvider) and asserts HTTP 200 plus the expected state mutation. Severity raised from the reviewer's 'high' to 'critical': this is silent loss of paid revenue / failure to deliver purchased goods on the primary billing path, which fits the 'money' critical bar.

### [MEDIUM] supabase/functions/generate-narrative/index.ts:2037 — generate-narrative charges credits before the refund path becomes reachable; a throw between spend and stream construction is non-refunding

**Evidence:** spend_credits runs at line 2006 (committed immediately). The refund() closure that reverses it is defined only INSIDE the ReadableStream start() at line 2062 and is therefore only reachable once streaming begins. But several non-trivial calls run between the spend and `new ReadableStream(...)` (line 2054) while still inside the outer try whose catch (line 2383) just returns a 400 with NO refund: notably summarizeSettlement(settlement) at line 2037 (defined lines 370-443, including summarizeFoodSituation) is NOT wrapped in try/catch. augmentSummaryWithGrounding and preservationBlockFor are internally guarded, but summarizeSettlement is not. If a malformed settlement payload makes summarizeSettlement throw, control jumps to the outer catch and the user is charged with the credits gone and no refund issued. Trigger probability is low (the summarizer is mostly optional-chained), but the structural seam is real: the spend is committed outside the only code region that can refund it.

**Fix:** Move the spend so it is covered by a refund-capable error path, or wrap the pre-stream summary/grounding construction (2033-2050) in try/catch that calls a refund before re-throwing, or perform the spend inside the stream start() right before generation.

**Fix note:** The suggested fixes are all valid. The cleanest is to wrap the pre-stream summary/grounding construction (lines 2033-2050) in a try/catch that, on error, calls refund_credits via the RPC (using the already-captured spendId/supabaseUser and respecting isElevated) before re-throwing into the outer catch. Note the existing refund() closure is scoped inside start(), so the pre-stream catch must inline its own refund_credits call (or hoist a shared refund helper above line 2054). A simpler structural fix is to harden summarizeSettlement itself by replacing each `(x || [])` with `Array.isArray(x) ? x : []` for factions/conflicts/institutions/npcs, which removes the throw vector entirely and is defensible regardless of the refund-seam question. Doing both (defensive summarizer + refund-capable pre-stream try/catch) is ideal: it fixes this specific trigger and closes the structural seam for any future unguarded pre-stream call.

### [MEDIUM] supabase/functions/send-email/index.ts:221 — send-email cap_warning is an unauthenticated arbitrary-recipient mailer; the claimed per-IP rate limit does not exist

**Evidence:** For templates in ANON_OK_TEMPLATES (only 'cap_warning', line 161) the handler skips auth entirely and takes the destination straight from the request body: `if (typeof recipient !== "string" || !recipient.includes("@"))` then `to = recipient` (lines 221-228). The only gate before this is `botGuard(req, "send-email")` (line 201), which — per _shared/requestMeta.ts botGuard (lines 140-149) → readRequestMeta — ONLY matches the User-Agent against bot regexes. It computes `ip` but performs NO rate limiting or counting. The file header explicitly claims protection that is absent: lines 14-17 say cap_warning is safe because 'we still apply a per-IP rate limit via botGuard to keep this from becoming a spam relay.' There is no such limit. Result: anyone sending a browser-like UA can POST { template:'cap_warning', recipient:'<any address>' } unlimited times and have Resend deliver the 'Anonymous generation cap reached' email from the verified RESEND_FROM_EMAIL domain to arbitrary victims (sendViaResend, lines 169-194). Content is the fixed template (only {capUsed}/{capTotal} are attacker-influenced via interpolate), so this is not arbitrary-content injection, but it is an unauthenticated email-bomb / sender-reputation / Resend-quota abuse vector.

**Fix:** Add a real per-IP (and/or per-recipient) rate limit backed by a table or KV before sending cap_warning, or require a short-lived signed anon token issued by the generation endpoint that hit the cap. At minimum, correct the misleading comment so operators don't assume a limit exists.

**Fix note:** The suggested fix is sound. Concretely: (a) add a real per-IP and per-recipient limit. Since no rate-limit table exists yet, the cleanest minimal option is the signed-token route — have the generation endpoint that detects the anon cap issue a short-lived HMAC/JWT bound to the recipient, and require+verify it here before sending cap_warning. (b) Alternatively gate on a counter table (the comment's own "anonymous_telemetry (migration TBD)" is the natural home) keyed on ip+recipient+day. (c) Regardless of which, also de-duplicate per recipient/day so the same victim can't be hit repeatedly even from rotating IPs. (d) At absolute minimum, correct the two misleading comments (index.ts:16-17 and the "Per-IP bot guard still applies" note at line 160) so operators don't assume a limit exists — but a comment fix alone leaves the abuse vector open. Note botGuard already exposes guard.meta.ip, so a limiter has the IP available; it just never uses it for counting.

### [LOW] supabase/functions/admin-actions/index.ts:174 — admin-actions OWNER_EMAIL override is silently defeated by the RPC actor check for metadata/credit mutations

**Evidence:** The edge authorization gate (lines 173-178) admits a caller whose email equals OWNER_EMAIL even when their profiles.role is not developer/admin. However update_user_metadata and update_user_credits route through service_update_profile_metadata / service_set_credits, which both call _assert_service_admin_actor(actor_user) (migration 017 lines 254-260): it requires the actor's profiles.role to be in ('developer','admin') and raises 'actor is not privileged' otherwise. So an OWNER_EMAIL user with role='user' passes the edge gate but the RPC rejects these two actions — while list_users and get_stats (which use adminClient directly, lines 233-301, no actor assertion) still work. The owner-override is thus inconsistent/partly non-functional. Not exploitable (it fails closed), but a real correctness defect in the privileged path.

**Fix:** Make the owner override consistent: either seed OWNER_EMAIL's profile role as admin and drop the edge-only override, or have the edge function pass an owner flag the RPC honors, so all four actions behave the same for the owner identity.

**Fix note:** The default owner email (clausellstokes@aol.com) is already seeded to role='admin' by migration 018:26-28, so for the default deployment the four actions are consistent in the typical case. The residual gap is specifically for (1) a non-default OWNER_EMAIL env value whose profile role was never elevated, and (2) any owner profile created after migration 018 ran (default role 'user'). A robust fix is to have the edge function pass an owner flag that _assert_service_admin_actor honors, OR drop the edge-only OWNER_EMAIL override entirely and rely solely on profiles.role (keeping migration 018's seed as the bootstrap), so all four actions key off the same privilege source. Note this is a fail-closed correctness/consistency defect, not a security or escalation issue.

### [LOW] supabase/functions/admin-actions/index.ts:245 — admin-actions list_users interpolates raw search into a PostgREST .or() filter

**Evidence:** `query.or(`email.ilike.%${rawSearch}%,display_name.ilike.%${rawSearch}%`)` (line 245) splices caller-supplied metadata.search (rawSearch, line 234) directly into a PostgREST logical-filter string. Commas, parentheses, or operator tokens in rawSearch can break out of the intended ilike clauses and alter the filter (PostgREST or() filter injection). Impact is limited because the caller is already developer/admin and the query uses the service-role client (which bypasses RLS and can already read every profile), so there is no privilege escalation — worst case is a malformed query or reading columns the actor can already read. Still a real un-sanitized-input seam in an admin endpoint.

**Fix:** Sanitize/escape rawSearch (reject or strip PostgREST metacharacters , ( ) . *) or build the filter with parameterized .ilike() calls combined via the array form of .or() rather than string interpolation.

**Fix note:** The suggested fix is sound. Note the identical pattern also exists client-side in src/components/AdminPanel.jsx:213 (query.or with interpolated searchQuery against profiles directly) — same low-impact injection seam, worth fixing in the same pass. Preferred remediation for the edge function: avoid string interpolation; either reject/strip PostgREST metacharacters (, ( ) . *) from rawSearch, or double-quote the interpolated value within the ilike pattern so commas/parens are treated literally, or use two separate .ilike() calls combined via the array form of .or().

### [LOW] supabase/functions/generate-chronicle/index.ts:100 — generate-chronicle refund path does not skip elevated (dev/admin) users, granting phantom credits

**Evidence:** The refund closure (lines 100-106) unconditionally calls refund_credits when the Anthropic call fails (line 128). It lacks the `if (isElevated) return;` guard that generate-narrative deliberately uses (generate-narrative/index.ts line 2063). For developer/admin/privileged callers, spend_credits (migration 024 lines 154-159) inserts a spend row tagged metadata.elevated=true and returns balance=-2 WITHOUT deducting anything; get_credit_balance (migration 018 lines 115-124) explicitly excludes elevated spends. refund_credits (migration 009 lines 284-345) then writes a real `kind='grant'` refund row of amount=cost that get_credit_balance DOES count — so each failed chronicle for an elevated user mints +2 real credits against a spend that never cost anything. Impact is low because elevated users already bypass balance checks, but it is a genuine ledger inconsistency the sibling function was written to avoid. (Separately, line 98/132 returns creditsRemaining = balance = -2 sentinel to elevated clients.)

**Fix:** Skip refund for elevated spends: capture spendResult.elevated and `if (elevated) return;` inside refund(), mirroring generate-narrative.

**Fix note:** Suggested fix is correct. Concretely: capture `const isElevated = Boolean(spendResult?.elevated);` after line 96, then add `if (isElevated) return;` as the first line of refund() (before the `!spendId` check) at line 101, mirroring generate-narrative/index.ts:2063. Note the secondary observation in the report is also accurate but cosmetic: line 132 returns `creditsRemaining: balanceAfter` where balanceAfter = spendResult.balance = -2 for elevated callers (generate-narrative instead surfaces a friendly 999999 sentinel at index.ts:2031). Worth normalizing for client display but not part of the ledger defect.


## 02-sql-rls (6)

### [CRITICAL] supabase/migrations/009_profile_security.sql:363 — refund_credits lets any user reclaim credits for successful AI spends (free/unlimited generations)

**Evidence:** refund_credits(spend_ledger_row uuid, refund_reason text) is SECURITY DEFINER and `grant execute ... to authenticated` (line 363). Its only authorization is line 309 `if spend_row.user_id <> auth.uid() and not is_admin then raise 'not authorized'` — i.e. the OWNER of any spend row may refund it — plus an idempotency guard (line 315-321) that only blocks refunding the SAME spend twice. There is NO check that the spend corresponded to a failed generation. The function inserts a fresh `grant` ledger row of `spend_row.amount` and increments `profiles.credits`, fully reversing the spend. This is reachable directly by clients: AI spends are created via the user's JWT (supabase/functions/generate-narrative/index.ts:2006 `supabaseUser.rpc('spend_credits', ...)`), credit_ledger has a `Users read own ledger` SELECT policy (007), and refund_credits is a PostgREST RPC granted to `authenticated` (the edge function itself calls it as the user at generate-narrative/index.ts:2076). So a paying user can: run AI features normally, `select id from credit_ledger where kind='spend'`, then call rpc('refund_credits',{spend_ledger_row:id}) for each — keeping every generated artifact while getting all credits back. A single 30-credit purchase becomes infinite AI usage. refund_credits is defined only in 009 and never revoked/redefined, so this is the net state.

**Fix:** Refunds of successful spends must not be a user capability. Either (a) revoke refund_credits from `authenticated` and only grant to service_role, having the edge function call it after verifying the generation actually failed; or (b) add a server-tracked 'refundable/failed' flag on the spend row that spend_credits sets false and only a verified-failure path sets true, and gate refund_credits on it. At minimum require is_admin OR a server-set failure marker rather than mere ownership.

**Fix note:** The suggested fix is correct in direction; option (a) (revoke from authenticated, grant only to service_role, edge function refunds only on verified failure) is the clean fix. Important implementation caveat the reviewer did not flag: the edge functions currently call refund_credits as `supabaseUser` (the user's JWT), so merely flipping the grant to service_role will break the legitimate mid-stream refund path — the edge functions must switch to a service-role client for the refund call and pass the verified user/spend. Also note that since spend_credits (024) now allocates against grants via credit_spend_allocations, the refund path should additionally reverse/void those allocations, otherwise even a legitimate refund leaves the allocation ledger inconsistent.

### [MEDIUM] supabase/migrations/020_gallery_public_privacy.sql:41 — Public gallery RPC leaks DM-facing NPC fields: server sanitizer denylists NPC keys while the intended projection is an allowlist

**Evidence:** _gallery_sanitize_public_json (020, net-state still used by get_gallery_dossier in 032:131/152) sanitizes NPCs with a DENYLIST: a key regex (line 38) plus, only when 'npcs' is in the path, dropping just `goal,secret,plotHooks,relationships` (lines 41-45). Every other NPC key survives. The product's authoritative projection, src/domain/display/publicSafe.js toPublicSafe() (lines 105-118), instead reduces each NPC to an explicit ALLOWLIST of ~11 fields (id,name,role,title,category,personality,physical,factionAffiliation,secondaryAffiliation,presentation,influence). publicSafe.js's own header (lines 9-13) states the SERVER (get_gallery_dossier) is the security boundary and the client is only defense-in-depth — yet the server is strictly MORE permissive than the client. Generated NPCs (src/generators/npcGenerator.js:72-98) always carry `gender` and `power`, and enriched NPCs gain `structuralPosition,activeConstraint,settlementCondition,structuralRank` (src/generators/npcStructure.js:430-437; documented on the NPC shape in src/domain/settlement.schema.js:354 and read by src/domain/npcProfile.js:371). None of these match the denylist regex or the npcs-path key list, so anonymous callers of get_gallery_dossier receive them even though the intended public view drops them. The crown-jewel fields (secret, goal, plotHooks) are stripped, so impact is moderate (DM strategic-analysis fields + power score), but the boundary leaks fields it is meant to exclude and any future NPC field leaks automatically.

**Fix:** Make the server NPC projection an allowlist mirroring toPublicSafe (keep only the same ~11 fields) instead of a denylist, or add the missing keys to the denylist; add a parity test asserting get_gallery_dossier output is a subset of toPublicSafe output for the same row.

**Fix note:** The suggested fix is sound. Prefer converting the server NPC projection to an allowlist mirroring toPublicSafe (publicSafe.js:105-118) rather than merely extending the denylist, because the denylist will silently leak any future NPC field again — the same failure mode that produced this bug. Concretely: in _gallery_sanitize_public_json, when 'npcs' is in the path on an NPC object, jsonb_build_object only the ~11 allowlisted keys (id,name,role,title,category,personality,physical,factionAffiliation,secondaryAffiliation,presentation,influence) instead of the current key-by-key denylist filter. Add a contract/parity test in the same spirit as the chronicle exact-allowlist test (gallery_privacy.contract.test.js:97-107): assert the SQL NPC projection's built key set equals toPublicSafe's NPC allowlist, so server and client cannot drift. Note the client gallery.js:406 already re-strips via toPublicSafe(full:false) for UI rendering, so the user-visible fix is primarily for direct anon RPC callers and the stated server-is-the-boundary guarantee — worth fixing but not a UI-breaking change.

### [LOW] supabase/migrations/003_admin_rls_policies.sql:51 — sync_profile_email SECURITY DEFINER trigger function not search_path-pinned

**Evidence:** sync_profile_email() (003:51-57) is `LANGUAGE plpgsql SECURITY DEFINER` with no `SET search_path`, unlike every other SECURITY DEFINER function in the schema (005/007/009/017/etc all pin `set search_path = public`). It is wired as an AFTER UPDATE OF email trigger on auth.users (003:65-68) and is not superseded by any later migration, so it remains net state. Without a pinned search_path the function resolves unqualified operators/objects via the caller's session search_path, the exact pattern Supabase's function_search_path_mutable linter flags. The body uses a schema-qualified UPDATE on public.profiles which limits practical exploitability (an attacker would need to create shadowing objects in a schema ahead of public, which ordinary Supabase roles cannot), so severity is low — but it is a real hardening gap on a trigger attached to the auth schema.

**Fix:** Add `SET search_path = public` (and `pg_temp` last) to sync_profile_email, matching the other SECURITY DEFINER functions.

**Fix note:** Suggested fix is correct. Recreate with `LANGUAGE plpgsql SECURITY DEFINER SET search_path = public` (optionally append pg_temp). Worth doing in the same migration that already pins the sibling handle_new_user (the 017-style pattern), since both are auth.users SECURITY DEFINER triggers and should be treated identically.

### [LOW] supabase/migrations/005_fix_profiles_rls_recursion.sql:54 — 'Developers update any profile' UPDATE policy has USING but no WITH CHECK — unaudited privileged self-mutation

**Evidence:** Net-state policy (created 005:54-55, never dropped by 018 which only replaced the user-facing policies): `CREATE POLICY "Developers update any profile" ON public.profiles FOR UPDATE USING (public.current_user_is_privileged())` with no WITH CHECK. For UPDATE policies Postgres falls back to the USING expression as the check, and that expression does not reference the row, so a developer/admin can set ANY column on ANY profile to ANY value — including role='admin', credits=99999, is_founder=true, tier='premium' — directly via a table UPDATE, completely bypassing the audited SECURITY DEFINER RPCs (admin_set_role, admin_grant_credits, service_set_credits) that 009/017 built specifically so every privileged write lands in admin_actions. Non-privileged users are unaffected (they only satisfy the 018 'safe preferences only' policy whose WITH CHECK locks protected columns). Impact is confined to already-privileged accounts (audit-trail bypass + developer→admin self-promotion), hence low.

**Fix:** Drop this policy and route all privileged profile writes through the audited RPCs, or add an explicit WITH CHECK and keep the policy read-only-shaped; if direct dev writes are desired, still funnel through an audited path.

**Fix note:** The reviewer's suggested fix is sound. Concretely: in a new migration, DROP POLICY IF EXISTS "Developers update any profile" ON public.profiles. If privileged users genuinely need to read all profiles, that is already covered by the separate "Developers read all profiles" SELECT policy (005:51-52) — the UPDATE policy is the only audit hole. All privileged writes already have audited RPC equivalents (admin_set_role 009:423, admin_grant_credits 009:369, service_set_credits 017:368), so dropping the UPDATE policy does not remove any needed capability; it just forces those writes through the audited path. If for some reason a direct-write escape hatch must remain, add a WITH CHECK that pins protected columns (mirroring the is-not-distinct-from pattern in 018:59-64) so it cannot be used for column tampering.

### [LOW] supabase/migrations/016_version_history.sql:34 — version_history has no server-side size bound (unbounded row growth)

**Evidence:** 016 adds settlements.version_history jsonb and documents it as 'Capped at 50 entries client-side to bound row size' (line 37-38) — the cap exists only in the client. The settlements UPDATE policy (024:264-266) and mutate_settlement_batch (024:533 writes `version_history = ... item->'version_history'` straight from client input) let an owner store an arbitrarily long array, each entry a full settlement clone (~5-30 kB per the migration comment). A user can therefore inflate a single row to many MB of TOASTed jsonb, and the same data also rides along on every read of that settlement. No cross-user impact, so low severity, but the documented invariant is unenforced server-side.

**Fix:** Enforce the 50-entry cap server-side: a CHECK on jsonb_array_length(version_history), or trim in mutate_settlement_batch / a BEFORE UPDATE trigger, so the bound does not depend on a cooperative client.

**Fix note:** The suggested fix is sound. Two practical notes: (1) A pure CHECK on jsonb_array_length(version_history) <= 50 would reject the whole UPDATE (raising an error the client must handle) rather than silently trimming — fine, but it changes failure semantics. (2) Trimming in the RPC is cleaner UX: in mutate_settlement_batch, on both the create (024:506) and update (024:533) paths, wrap the value with a server-side cap, e.g. take the last 50 elements via `(select jsonb_agg(e) from (select e from jsonb_array_elements(item->'version_history') with ordinality t(e, ord) order by ord desc limit 50) ...)` reversed, or simpler: keep the tail. Also bound entry/byte size, since 50 entries x 30 kB is still ~1.5 MB; consider an additional pg_column_size guard if total-row-size is the real concern. Note the direct PostgREST UPDATE path (RLS policy) is separate from the RPC, so a CHECK constraint (which covers all write paths) is the more complete defense than RPC-only trimming.

### [LOW] supabase/migrations/024_billing_retention_and_atomic_mutations.sql:307 — Per-tier save limit is enforced non-atomically (count-then-insert TOCTOU race)

**Evidence:** enforce_save_limit() (net state in 024:273-316, originally 014) does `select count(*) ... where user_id = new.user_id and access_state='active'` (lines 307-309) in a BEFORE INSERT trigger and rejects when count >= limit, but takes no lock that serializes concurrent inserts for the same user. Two simultaneous INSERTs (or two concurrent mutate_settlement_batch creates) both read count=2 against a free limit of 3, both pass the check, and both commit — yielding 4+ active settlements for a free user. The limit is still enforced against sequential abuse (this closes the original direct-upsert bypass the migration targeted), but the cap is exceedable by a few rows under deliberate concurrency.

**Fix:** Serialize per-user save accounting, e.g. `select count(*) ... for update` against a locking row, an advisory lock on user_id (pg_advisory_xact_lock), or a partial unique/exclusion constraint that bounds active rows per user.

**Fix note:** The suggested fixes are sound. The cleanest robust fix is a transaction-scoped advisory lock keyed on the user at the top of the count branch, e.g. `perform pg_advisory_xact_lock(hashtextextended(new.user_id::text, 0));` placed just before the count(*) at 024:307 — this serializes all concurrent inserts for the same user without a schema change and works regardless of which entry point (trigger via RLS insert or via mutate_settlement_batch) fired the insert. An exclusion constraint is harder here because the cap is N rows (not 1), so a partial unique constraint cannot express "at most 3 active per user" directly; the advisory lock is the most practical option. If chosen, ensure the same lock key is acquired by mutate_settlement_batch's create path so the trigger and any future direct-RPC accounting agree on the serialization point.


## 03-lib (5)

### [MEDIUM] src/lib/ai.js:175 — AI narrative NDJSON reader treats a truncated stream as success and silently drops a final unterminated line, so partial/charged generations are stored as if complete

**Evidence:** generateNarrative's read loop (lines 175-227) only parses complete '\n'-terminated lines; when reader.read() reports done, any remaining bytes in `buffer` are discarded without a parse attempt (no flush after the loop, and decoder.decode() final flush is never called). If the server's last line — the authoritative `{done, result, creditsRemaining, partialFailure}` message — arrives without a trailing newline, or the connection drops mid-stream after the server has already debited credits, the function falls through to `return { result, creditsRemaining: null, ... }` (line 230) with whatever per-field snapshots accumulated: no throw, partialFailure stays false, failedFields stays empty. The caller cannot distinguish a complete narrative from a half-streamed one, so a user can be charged server-side and have an incomplete narrative persisted as the refined aiSettlement with no retry/refund affordance.

**Fix:** After the read loop, attempt to parse the residual buffer as a final line. Track whether a `done` message was ever received; if not, throw (or return a partialFailure result) so the caller can surface a retry instead of persisting a truncated narrative as success.

**Fix note:** The valuable fix is the second half of the suggestion only: track a `sawDone` boolean set true when a `{done:true}` message is parsed (line 216), and after the read loop, if `!fatalError && !sawDone`, throw (or return a partialFailure result) so the caller surfaces a retry instead of persisting a truncated narrative as complete. The first half ("parse the residual buffer as a final line") is effectively a no-op against this server because every emitted line is '\n'-terminated, so any residual buffer is either empty or an unparseable truncated fragment — harmless to add but it recovers nothing. Also note the report's premise "last line without trailing newline" does not occur with the current edge function; the real-world trigger is a graceful-but-incomplete stream close (proxy/isolate eviction) after credits were already debited at index.ts:2006.

### [MEDIUM] src/lib/campaignSync.js:45 — Deleting a campaign on one device resurrects on every other device and is re-uploaded to the cloud (no deletion tombstone in the merge/sync protocol)

**Evidence:** campaigns delete is a hard row delete (src/lib/campaigns.js:149-152) plus local forgetCampaignSync on the deleting device only (campaignSlice.js deletePersistedCampaign, lines 122-130). mergeCampaignLists (campaignSync.js:45-93) merges the local cache and cloud rows purely by presence/updatedAt with no tombstone concept, so any OTHER device that still has the campaign in its sf_campaigns cache re-adds it on loadCampaigns. Worse, loadCampaigns (campaignSlice.js:401-414) primes lastSyncedSignatures from the REMOTE list only (primeCampaignSync(migratedRemote), line 404), then runs syncCampaignChanges(merged) as a 'backfill' (line 411) — the resurrected campaign has no signature entry, fails the needs-sync check (campaignSync.js:123), and is upserted straight back into saved_maps. The module header (lines 5-7) explicitly intends local-only campaigns to survive cloud loads, but that policy makes cross-device deletion impossible to make stick: delete on device A, open device B, campaign is back in the cloud for both.

**Fix:** Add deletion reconciliation: either soft-delete (access_state='deleted' tombstone rows that mergeCampaignLists treats as authoritative wins) or track per-device 'known-synced' ids so a campaign that was previously synced but is now absent from the cloud is removed locally instead of re-uploaded.

**Fix note:** The suggested fix is sound; the soft-delete/tombstone variant is the more robust of the two. A user delete should write a tombstone (e.g. saved_maps access_state='deleted' or a dedicated deleted_at) instead of a hard row delete, and the read path must return tombstones so mergeCampaignLists can treat a remote tombstone as an authoritative win that drops/marks the local copy and excludes it from getCampaignsNeedingSync (the accessState!=='active' guard at campaignSync.js:120 already excludes non-active states from upload, so a 'deleted' access_state would also stop re-upload for free). The alternative 'known-synced ids' approach is weaker: it relies on per-device bookkeeping that is wiped by primeCampaignSync on every load and lost on cache clear, so the tombstone approach is preferable. Also note loadCampaigns's line 404 primeCampaignSync(migratedRemote) + line 411 backfill is the precise mechanism that turns the merge into a re-upload; whatever fix is chosen must ensure a remotely-absent (or tombstoned) campaign is NOT treated as needing-sync.

### [MEDIUM] src/lib/pendingDossier.js:27 — Paid single-dossier purchase permanently destroyed if user returns more than 1 hour after stashing (TTL shorter than Stripe session lifetime, destructive clear-on-read)

**Evidence:** The anonymous $2.99 dossier flow stashes the settlement to localStorage before redirecting to Stripe (BuyThisDossier.jsx:48-52) and is the ONLY copy of the purchased artifact. pendingDossier.js sets TTL_MS = 60*60*1000 (line 27) and readPendingDossier() (lines 87-90) destroys the stash via clearPendingDossier() as a side effect of any read past the TTL. create-checkout/index.ts sets no custom expires_at, so the Stripe Checkout session stays payable for Stripe's default 24 hours. A buyer who completes payment 1-24h after clicking Buy (left the Stripe tab open, mobile interruption, came back to the success URL later) lands on SingleDossierSuccessPage, whose useState initializer calls readPendingDossier() (SingleDossierSuccessPage.jsx:36) — the stash is wiped on that very read and the page shows 'We couldn't verify this dossier' with a support email. The settlement existed only in that stash, so even support cannot recover the product, only refund. The module's own docstring calls a paid receipt for an empty dossier 'the worst possible failure mode'. Compounding: App.jsx:132-137 strips session_id from the URL via checkCheckoutResult() BEFORE attachPendingDossierCheckout(result.sessionId), and ignores attach's boolean return — if the attach write fails the session id is irrecoverably gone from both URL and stash. tests/lib/pendingDossier.test.js:61 pins the 1-hour clear, so this is a deliberate but wrong constant, not a typo.

**Fix:** Raise TTL_MS to at least 24h (Stripe's default session lifetime) or set expires_at on the Checkout session to match the stash TTL. Make readPendingDossier non-destructive for the success-page path (only clear after a verified download or explicit user action), and have App.jsx check attachPendingDossierCheckout's return value before discarding session_id from the URL.

**Fix note:** The suggested fix is sound. Most robust single change: set expires_at on the Stripe Checkout session in create-checkout/index.ts to match the stash TTL so the two lifetimes can never diverge (or, conversely, raise TTL_MS to >=24h to cover Stripe's default). Independently, the success page should not let a routine read destroy the only copy of a paid artifact: in SingleDossierSuccessPage.jsx defer clearPendingDossier() until after a verified successful download or explicit user action, and consider not auto-clearing in readPendingDossier() at all on the success path (only validate freshness, clear elsewhere). Also have App.jsx check attachPendingDossierCheckout's boolean return before discarding session_id — though note the deeper issue is that checkCheckoutResult() already strips session_id from the URL inside stripe.js before attach is even attempted, so reordering (attach first, strip after) or persisting session_id more durably would be the real fix for the compounding loss.

### [LOW] src/lib/auth.js:110 — rememberMe=false does not actually prevent session persistence — supabase-js re-persists the session on the next token refresh/auth event

**Evidence:** supabaseSignIn (lines 110-126) implements 'remember me = off' by calling signInWithPassword on a client created with persistSession:true + autoRefreshToken:true (supabase.js:41-48) and then deleting the `sb-<ref>-auth-token` localStorage key once. supabase-js rewrites the session to localStorage on every subsequent auth event — including the automatic token refresh (~hourly) and any visibility-triggered refresh — so for any session that lives past the first refresh, the token is back in localStorage and the session survives reloads/restarts on a shared computer despite the user opting out. The one-time key removal also races the SDK's own post-sign-in persistence writes.

**Fix:** For rememberMe=false, use a client configured with sessionStorage (or memory) as the auth storage rather than deleting the key after the fact — e.g. pass `auth.storage: sessionStorage` on a secondary client, or set the storage adapter before signing in.

**Fix note:** The suggested fix is correct in direction. One implementation caveat: the storage adapter must be selected before the client/GoTrueClient is constructed, because storageKey/storage are resolved in the constructor (GoTrueClient.js ~line 162-167) — you cannot mutate it after sign-in. Practical approaches: (a) construct a dedicated client with auth.storage: window.sessionStorage (and ideally persistSession:false or a memory adapter) and route rememberMe=false sign-ins through it, or (b) keep a single primary client and, when rememberMe is false, also set persistSession appropriately at creation. Note sessionStorage still survives intra-session reloads but is cleared on tab/window close, which matches typical "don't remember me" semantics; a pure in-memory adapter is stricter (cleared on any reload). Also, since autoRefreshToken stays on, whichever storage is chosen is what gets rewritten on refresh — so the fix only works if the storage itself is non-persistent, not via post-hoc deletion.

### [LOW] src/lib/authIntents.js:50 — authIntents in-memory fallback is write-only: pending intent silently lost when sessionStorage writes fail but reads succeed

**Evidence:** writeStored (lines 60-68) falls back to `_memoryStore = value` when sessionStorage.setItem throws (quota, Safari lockdown). But readStored (lines 50-58) returns `raw ? JSON.parse(raw) : null` whenever sessionStorage.getItem SUCCEEDS — it only consults _memoryStore when sessionStorage is undefined or getItem/parse throws. In the common restricted-browser failure mode (setItem throws, getItem works and returns null), the intent is written to memory but every read returns null, so the post-auth 'save this town' replay silently no-ops — precisely the dropped-intent failure this module exists to prevent (per its own header).

**Fix:** In readStored, fall through to `_memoryStore` when sessionStorage returns null (e.g. `return raw ? JSON.parse(raw) : _memoryStore;`), and clear _memoryStore in writeStored's success path to keep the two stores coherent.

**Fix note:** The suggested `return raw ? JSON.parse(raw) : _memoryStore;` is correct and addresses the read-side gap. Two refinements: (1) Clearing _memoryStore in writeStored's sessionStorage-success path (as suggested) is good hygiene but not strictly required for correctness — once sessionStorage works, readStored returns the sessionStorage value first anyway; the real risk is a stale _memoryStore masking a later sessionStorage null after a successful clear, so also null _memoryStore in clearStored (it already does, line 74). (2) A cleaner alternative is to make writeStored track whether it fell back (e.g., a `_usingMemory` flag) so readStored prefers _memoryStore deterministically when the last write went to memory, avoiding any ambiguity between 'sessionStorage legitimately empty' and 'sessionStorage write failed'. Either approach fixes the write-only defect.


## 04-store (10)

### [MEDIUM] src/store/campaignSlice.js:154 — Regional-impact apply/resolve persists campaign graph state and settlement save independently with no ordering or failure reconcile; persistSaveUpdate's catch-and-rethrow guarantees unhandled promise rejections at fire-and-forget call sites

**Evidence:** campaignSlice's persistSaveUpdate (lines 154-160) does `savesService.update(saveId, partial).catch(e => { console.warn(...); throw e; })` — the rethrow inside .catch produces a rejected promise. applyQueuedRegionalImpact (line 1109) and resolveRegionalImpact (line 1173) call it with no await and no handler, so any save-update failure (offline, RLS, quota) becomes an unhandled promise rejection. The deeper integrity issue: inside the same action, persistCampaignState (lines 1097/1161) durably marks the impact status='applied'/'resolved' on the campaign graph BEFORE/independently of the settlement save write at 1109/1173. If the settlement write fails there is no rollback, retry, or reconcile — after reload the campaign permanently shows the impact applied while the saved settlement lacks the corresponding condition, the exact 'succeeds in one store, fails in the other' divergence class. (settlementSlice's twin persistSaveUpdate, lines 124-129, correctly swallows instead of rethrowing — the two helpers diverged.)

**Fix:** Drop the rethrow in campaignSlice.persistSaveUpdate (match settlementSlice's swallow-and-warn) or make callers await and surface failures. For integrity, write the settlement save first and only mark the campaign impact applied after that write succeeds, or queue failed save writes for retry.

**Fix note:** Both halves of the suggested fix are appropriate and confirmed by the code. (a) The rethrow at campaignSlice.js:158 should be dropped to match settlementSlice.js's swallow-and-warn (or every call site must await and handle), which removes the unhandled-rejection seam. (b) The deeper integrity fix is the important one: do the settlement save write FIRST and only mark the campaign impact applied/resolved (and call persistCampaignState) after it resolves — or, since persistCampaigns already writes localStorage synchronously and queues cloud sync, route the settlement update through the same durable-then-queue mechanism so both stores share one failure/retry path. Note that the existing global unhandledrejection handler (errorReporter.js:86) reports but does not reconcile, so it does not address the integrity defect. Also worth noting: even the in-action ordering fix only narrows the window — a true fix needs the two writes to share a single durable transaction or a retry queue, because the campaign localStorage write and the settlement cloud write target different backends.

### [MEDIUM] src/store/campaignSlice.js:401 — loadCampaigns has no stale-owner guard after the awaited cloud list: a sign-out/sign-in completing mid-flight writes the previous user's campaigns into state and cache

**Evidence:** loadCampaigns captures ownerId = campaignCacheOwner(get()) at call time (line 394), then awaits campaignService.list() (line 401). When the promise resolves it unconditionally sets state.campaigns = merged (lines 406-409) and localWrite(merged, ownerId) (line 410) - never re-checking that the owner is still the same. If the user signs out (clearCampaigns resets state.campaigns) or another user signs in while list() is in flight, the late resolution overwrites the cleared/new state with the prior user's cloud campaigns and persists them under the captured owner key. The sibling slice proves this race is a known concern: loadCustomContentFromCloud re-checks `if (ownerIdFromState(get()) !== ownerId) return;` after its await (customContentSlice.js:221). campaignSlice lacks the equivalent guard on both the success and catch paths.

**Fix:** After the await, compare campaignCacheOwner(get()) to the captured ownerId and abort (no set, no localWrite, no backfill sync) when they differ, mirroring customContentSlice.js:221.

**Fix note:** The suggested fix is correct and directly portable. After `const remote = await/then`, re-check before mutating: in both the .then and .catch paths, add `if (campaignCacheOwner(get()) !== ownerId) return cached;` (or return merged without applying) before any set/localWrite/syncCampaignChanges, mirroring customContentSlice.js:221. Note the .catch currently only sets campaignsLoaded=true, which would also incorrectly flip the new/cleared owner's loaded flag on a stale rejection, so guarding the catch as well is worthwhile.

### [MEDIUM] src/store/campaignSlice.js:411 — Deleted campaigns resurrect from any other device's local cache: merge keeps locally-cached campaigns missing from remote and the backfill sync re-upserts them

**Evidence:** deleteCampaign on device A removes the row from cloud (lib/campaigns.js supabaseDelete). On device B, loadCampaigns (campaignSlice.js:393-421) loads the stale local cache containing the deleted campaign, primes signatures from remote only (primeCampaignSync(migratedRemote), line 404), merges with mergeCampaignLists (lib/campaignSync.js:45-93) whose stated policy keeps any local campaign not present remotely, then runs syncCampaignChanges(merged) as a 'cloud backfill' (line 411). getCampaignsNeedingSync (campaignSync.js:116-125) sees the deleted campaign's signature is absent from lastSyncedSignatures and upserts it - re-creating the cloud row under the same uuid (rowForCampaign sets row.id for uuid ids, campaigns.js:117). There is no tombstone/uploadedness tracking, so deletion can never propagate; every device that ever cached the campaign deterministically resurrects it.

**Fix:** Track deletions: keep a local tombstone list (id + deletedAt) per owner, filter tombstoned ids out of mergeCampaignLists, and only treat a local-only campaign as 'needs upload' if it carries a created-locally/never-synced marker rather than 'absent from remote'.

**Fix note:** The suggested fix (per-owner local tombstone list of {id, deletedAt}, filter tombstoned ids out of mergeCampaignLists, and only treat a local-only campaign as "needs upload" when it carries a created-locally/never-synced marker) is correct and addresses the root cause. Two practical refinements: (1) tombstones need a retention/expiry policy and should themselves be reconciled so they don't grow unbounded or block a legitimate same-id recreate; (2) a lighter-weight alternative that also closes the hole: have loadCampaigns drop local-only campaigns that carry a "previously synced" marker but are now absent from a successful full remote list (i.e., distinguish "synced before, gone now -> deleted remotely, drop it" from "never synced -> keep and upload"). The current code has no per-campaign synced-once flag, so either approach requires adding that bit of state.

### [MEDIUM] src/store/customContentSlice.js:143 — Optimistic local id vs cloud uuid race: update/delete issued before customContentService.add() resolves targets the temporary local id, failing the cloud op and resurrecting deleted items on next cloud load

**Evidence:** addCustomItem inserts an entry with a client id (makeId, line 130) and fires customContentService.add(); the cloud uuid only replaces the local id when the promise resolves (lines 144-151). In the window before that, deleteCustomItem (line 189) calls customContentService.delete(id) with the LOCAL id - supabaseDelete runs eq('id', '<inst_17...>') against a uuid PK column, which errors (invalid uuid input) and deletes nothing, while the insert completes server-side. The .then replacement then finds idx === -1 (item gone locally, line 147) and skips - leaving an orphan cloud row that resurrects the 'deleted' item on the next loadCustomContentFromCloud. updateCustomItem in the same window (line 174) likewise updates a row id that does not exist in the cloud, so the edit is silently lost server-side (surfaced only as customContentError).

**Fix:** Queue update/delete calls for items whose id is still local (e.g. track in-flight adds by localUid and chain the follow-up op on the add promise, using the returned cloud id), or perform delete/update server-side by the localUid stored in the jsonb body.

**Fix note:** The suggested fix is sound. Cleanest robust option: change supabaseUpdate/supabaseDelete (src/lib/customContent.js) to match on the stable jsonb body field rather than the PK — e.g. .eq('data->>localUid', localUid). localUid (customContentSlice.js:133) is set before the add fires, lives inside the jsonb data column, and is invariant across the cloud round-trip, so update/delete would target the correct row whether or not the add has resolved. This avoids having to track in-flight add promises. Alternatively (as proposed) chain the follow-up op on the add promise keyed by localUid. Either way the slice's update/delete should stop keying off the volatile `id`.

### [MEDIUM] src/store/customContentSlice.js:242 — Custom-content localStorage is written owner-scoped but only ever read from the anon key: grandfathered items are never migrated (flag set after reading the wrong key) and the offline mirror is never consulted

**Evidence:** Boot hydration is `customContent: loadAll('anon')` (line 116) — always the unscoped key `sf_custom_content`. But every write while signed in goes to the uid-scoped key via ownerIdFromState (localWrite at lines 140/150/167/185, scopedLocalKey lines 25-29). Consequences: (1) migrateLocalCustomContentToCloud (lines 242-253) reads flattenLocalContent(ownerId) — the uid-scoped key, which is empty on first premium sign-in because the 'grandfathered localStorage items' the header (lines 13-15) promises to migrate live under the unscoped/anon key. items.length === 0, so localStorage.setItem(flag,'1') runs (line 251) and permanently blocks any future migration; while signed in, loadCustomContentFromCloud then replaces state with cloud-only content (line 222-226), so the grandfathered items vanish from the UI for that account forever. (2) The 'mirror to local for offline read-only access' write (lines 227-228) writes to the uid key, but no code path ever reads the uid key on boot (loadAll('anon')) and an offline cloud load just fails — the offline mirror is dead. The service-layer comment in src/lib/customContent.js:9-10 ('Free users with grandfathered local items still load through localList()') only holds while signed out.

**Fix:** Make migrateLocalCustomContentToCloud read BOTH the anon key and the uid key (grandfathered content lives under the anon key); only set the migrated flag after a successful insert. On boot/sign-in, hydrate from the owner-scoped key (falling back to anon) so the offline mirror is actually used.

**Fix note:** The reviewer's fix is correct: migrateLocalCustomContentToCloud should read BOTH the anon key and the uid key and union them (grandfathered content is under the anon key). Note one nuance to the reviewer's 'only set the flag after a successful insert' wording: when BOTH keys are genuinely empty you still want to set the flag to avoid re-running every sign-in — so gate flag-setting on 'no items found in either key OR insert succeeded', not strictly 'insert succeeded'. Separately, to make the offline mirror real, boot/sign-in hydration should read the owner-scoped key (falling back to anon), and loadCustomContentFromCloud's catch path should hydrate from the uid mirror instead of leaving state empty.

### [MEDIUM] src/store/customContentSlice.js:249 — migrateLocalCustomContentToCloud reads the user-scoped localStorage bucket instead of the anon/grandfathered bucket, then permanently sets the migrated flag - grandfathered custom content never reaches the cloud

**Evidence:** The slice docstring (lines 13-15) says 'On sign-in as premium, any local items are migrated to the cloud once'. Items created before sign-in live under the unscoped key 'sf_custom_content' (scopedLocalKey returns LOCAL_KEY only for owner 'anon', line 27). But migrateLocalCustomContentToCloud computes ownerId = ownerIdFromState(get()) (line 245) - which is the signed-in user's id since canUseCustomContent() gates the call - and then flattenLocalContent(ownerId) (line 249) reads 'sf_custom_content:<uid>', a bucket that is empty on first premium sign-in (nothing writes it until the user is already signed in). With items.length === 0 the migrated flag is set to '1' (lines 250-252) and the function returns - so the real anon-bucket items are never migrated, and the user-scoped flag makes the miss permanent for that account/device. Corroborating: lib/customContent.js:188 provides readLocalForMigration() which reads the UNSCOPED key and is documented 'used for migration to cloud' - it has zero callers.

**Fix:** Migrate from the anon bucket: use flattenLocalContent('anon') (or the existing customContentService.readLocalForMigration()) as the migration source, and only set the migrated flag after a successful push (or after confirming the anon bucket is empty).

**Fix note:** The suggested fix is correct in direction. Use the anon bucket as the migration source: flattenLocalContent('anon') (or customContentService.readLocalForMigration(), which already reads the unscoped key). Important additional point: only set the migrated flag AFTER a successful push (the early flag-set in lines 250-252 is itself fragile — if migration ever runs before the anon bucket has content, or push fails partway, the permanent flag locks out a retry). Also consider: after a successful migration the anon-bucket items should arguably be cleared (customContentService.localClear or equivalent) so a later sign-in on the same device with a different account doesn't re-migrate the same items into the second account — otherwise flattenLocalContent('anon') is non-empty for every subsequent first-sign-in on that device. At minimum, gate the flag on a confirmed-empty-source OR successful-push, not on the empty user-scoped read.

### [MEDIUM] src/store/mapSlice.js:361 — Map undo/redo is dead: pushMapUndo is never called anywhere, so the AnnotateToolbar Undo/Redo buttons are permanent no-ops

**Evidence:** mapSlice defines pushMapUndo (line 361, 'snapshot-per-action'), mapUndo (373) and mapRedo (384). components/map/AnnotateToolbar.jsx:141-146 renders visible Undo/Redo buttons wired to mapUndo/mapRedo. But a repo-wide grep for pushMapUndo/mapUndoStack/mapRedoStack finds zero references outside store/mapSlice.js - no annotation, placement, or terrain action ever pushes a snapshot, so mapUndoStack is always empty, mapUndo() pops nothing and returns, and both toolbar buttons do nothing. Users who delete a label/marker/forest (AnnotateToolbar handleDelete) have no recovery despite the UI affordance promising one.

**Fix:** Call pushMapUndo(actionName) at the start of each mutating annotate/placement action (addLabel/updateLabel/deleteLabel, markers, forests, addPlacement/updatePlacement/removePlacementLocal), or wrap those actions to snapshot first; alternatively remove the Undo/Redo buttons until wired. Note the snapshot includes the multi-hundred-KB fmgSnapshot blob x30 entries - exclude fmgSnapshot from undo snapshots while fixing.

**Fix note:** The reviewer's analysis and fix are accurate. One scoping clarification: TerrainToolbar.jsx has its OWN working Undo/Redo that delegates to FMG's history (bridge.terrainUndo/terrainRedo, lines 66-75), so the defect is strictly limited to the ANNOTATE-mode buttons backed by mapUndoStack — terrain editing undo is unaffected. When wiring pushMapUndo, also note mapUndo/mapRedo replace the WHOLE state.mapState with the snapshot (lines 381/392), which includes fmgSnapshot, placements, layers, and viewport — so an undo of a label delete would also revert any concurrent viewport/layer changes captured between snapshots; if that coarse granularity is undesirable, restrict the snapshot/restore to the annotation sub-trees (labels/markers/forests) rather than the entire mapState. And per the reviewer, exclude fmgSnapshot from the snapshot to avoid 30x deep-cloning a multi-hundred-KB blob.

### [LOW] src/store/campaignSlice.js:147 — newCampaignId() fallback mints non-UUID ids that migrateCampaign immediately invalidates and rowForCampaign cannot upsert idempotently - campaign identity churns every load and each sync inserts a duplicate cloud row

**Evidence:** newCampaignId falls back to `camp_${Date.now()}_...` when crypto.randomUUID is unavailable (line 147 - e.g. non-secure-context deployments or older Safari). That id fails isUuid (line 150), so migrateCampaign re-mints it on EVERY localLoad (line 242) - and in the same environment the re-mint is again a non-UUID camp_ id, so the campaign's identity changes on every page load (activeCampaignId dangles, sync bookkeeping resets). Worse, lib/campaigns.js rowForCampaign only sets row.id when isUuid(campaign.id) (campaigns.js:117), so every syncCampaignChanges upsert for such a campaign omits the id and performs a fresh INSERT - one new saved_maps row per sync - and supabaseDelete(id) with the camp_ id errors on the uuid column so cleanup never works (campaigns.js:150).

**Fix:** Make the fallback generate an RFC-4122 v4 uuid from Math.random (so isUuid passes and the id is stable/upsertable), and have migrateCampaign persist re-minted ids immediately rather than only as a side effect of the next cloud-configured load.

**Fix note:** The suggested fix is correct and matches the existing precedent. Mirror src/components/SettlementsPanel.jsx:newSaveId() — emit an RFC-4122 v4-shaped id from Math.random in the fallback so it passes isUuid, stays stable across loads, and remains upsertable/deletable. Secondary improvement (also suggested): have migrateCampaign persist any re-minted ids immediately (localWrite) rather than relying on the next mutation or the configured-load backfill, so a re-mint converges in the local-only path too. With a UUID-shaped fallback the re-mint at line 242 would also stop firing, since the fallback id would itself pass isUuid.

### [LOW] src/store/campaignSlice.js:1109 — persistSaveUpdate re-throws inside fire-and-forget call sites in applyQueuedRegionalImpact/resolveRegionalImpact, producing unhandled promise rejections on save failure

**Evidence:** persistSaveUpdate (lines 154-160) catches the savesService.update failure, warns, then re-throws - returning a rejected promise. applyQueuedRegionalImpact calls it fire-and-forget with no .catch (lines 1108-1113), as does resolveRegionalImpact (lines 1172-1177). When the cloud save update fails (offline, RLS, expired session), the rejection is unhandled - the impact is already marked applied/resolved in local state and the campaign graph, the settlement mutation never reaches the cloud, and the only signal is an unhandled-rejection console error; no rollback or user-visible error occurs.

**Fix:** Either drop the re-throw in persistSaveUpdate for these fire-and-forget paths, or attach a .catch at the call sites that surfaces a toast/error state (and ideally marks the save as needing re-sync) instead of leaving the rejection unhandled while local and cloud state silently diverge.

**Fix note:** The suggested fix is sound. Preferred: attach a .catch at the two fire-and-forget call sites (1108-1113, 1172-1177) that surfaces a user-visible error/toast and flags the save as needing re-sync, rather than only relying on the global unhandledrejection telemetry. Note: do NOT simply drop the re-throw in persistSaveUpdate, because persistSaveUpdates (the awaited plural helper, 162-170) relies on that rejection to propagate to its awaiting callers; removing the throw would silently weaken that path too. Cleaner to leave persistSaveUpdate as-is and add the .catch at the singular call sites (or introduce a fire-and-forget variant that handles its own rejection).

### [LOW] src/store/mapSlice.js:354 — resetMapState clears selectedBurgId and selectedAnnotationId but not selectedSettlementId/hoveredSettlementId - campaign deselect leaves a dangling settlement selection

**Evidence:** resetMapState (lines 354-358) resets mapState, selectedBurgId, and selectedAnnotationId. It does not touch selectedSettlementId - documented at line 129 as the 'primary key for detail panels' - or hoveredSettlementId. WorldMap's deselect flow (WorldMap.jsx:349-357) calls resetMapState() and clears placements, so after switching away from a campaign the detail panel key still points at a settlement from the previous campaign while the map shows nothing selected. replaceMapState (campaign load, line 337) similarly carries the previous campaign's selection across.

**Fix:** Add `state.selectedSettlementId = null; state.hoveredSettlementId = null;` to resetMapState (and consider doing the same in replaceMapState), unless a caller demonstrably relies on selection surviving a map reset.

**Fix note:** The suggested fix is correct. For replaceMapState (campaign load/switch, mapSlice.js:337), clearing both fields is also warranted, not just "consider" — loading a different campaign carries over a selection that may not be placed in the new campaign, producing the same floating-card glitch and the same SettlementsPanel auto-focus leak. Recommend adding `state.selectedSettlementId = null; state.hoveredSettlementId = null; state.selectedBurgId = null; state.selectedAnnotationId = null;` to BOTH resetMapState and replaceMapState so selection state is always tied to the currently-loaded map.


## 05-pdf (16)

### [HIGH] src/pdf/lib/viewModel.js:387 — Object/array-shaped settlementReason passed raw to <Text> in Overview chapter — crashes or garbles the whole PDF export

**Evidence:** overviewSlice passes `settlementReason: s?.settlementReason || null` with no coercion (viewModel.js:387) and Overview.jsx:283-287 renders `{o.settlementReason}` directly as a Text child. The field is documented and handled elsewhere as `string | string[] | { primary }`: the same file's historySlice coerces it (viewModel.js:822-824 `s?.settlementReason?.primary`), the web guards both shapes (OverviewTab.jsx:201-204 Array.isArray branch + `r.settlementReason?.primary||r.settlementReason`), and supabase/functions/generate-narrative/index.ts:529-560 explicitly preserves the array/object shape on the AI clone ('settlementReason can be string | string[] | { primary: string, ... }'). A plain-object child throws React's 'Objects are not valid as a React child' inside the react-pdf reconciler, failing the entire export; an array renders concatenated with no separators.

**Fix:** Coerce in overviewSlice exactly like historySlice: string passes through, arrays join with newlines/spaces, objects use `.primary`. Add a fixture with object-shaped settlementReason to tests/pdf/sections.smoke.test.js.

**Fix note:** The title's framing ("crashes or garbles the whole PDF") conflates two cases with very different reachability. Be precise: (a) ARRAY shape is the engine's ALWAYS-emitted shape and currently renders WITHOUT crashing but concatenates multi-sentence reasons with no separator (4 of 6 tiers: metropolis/city/hamlet/thorp are 2-element) — this is the real present-day defect (parity drift vs web, which renders each array element as its own <p>). (b) OBJECT {primary} shape WOULD crash the export with "Objects are not valid as a React child", but no current code path produces it — it is defensive-only/future. So this is medium (wrong-but-non-crashing output on a common path + a latent crash guard), not high (no determinism break, no money/security, no current crash). The proposed coercion in overviewSlice is the right fix; to match web semantics for arrays, join with a separator that reads as paragraph breaks rather than a single space (e.g. '\n' or ' '), and add a fixture covering BOTH the multi-element array shape (current reality) and the object shape (latent) to tests/pdf/sections.smoke.test.js — and note that the existing smoke test does not exercise the Overview chapter at all.

### [MEDIUM] src/pdf/lib/viewModel.js:84 — Legacy (flag-off) food-balance path reads fb.production/fb.need but engine emits dailyProduction/dailyNeed — PDF shows 'Not calculated' and loses deficit %

**Evidence:** foodCore's non-canonical branch (viewModel.js:82-93) reads `fb?.production`, `fb?.need`, and computes deficitPct from `Number(fb?.need)`. The engine's metrics.foodBalance is `{ dailyNeed, dailyProduction, deficit, deficitPercent, surplus, importCoverage, rawDeficit, ... }` (economicGenerator.js:657-677) — no production/need keys, and the precomputed `deficitPercent` (which the web uses directly, OverviewTab.jsx:157-159) is ignored. With flag('canonicalViewModel') off, FoodBalanceBlock (EconomicsTrade.jsx:432-466) always shows 'Not calculated' for produced/needed and omits '% of need' even when the engine computed both; the screen shows the full deficit bar. (The canonical-path fix exists per the comment at viewModel.js:51-55, but the legacy path remains live behind the flag; flags.js was unreadable in this checkout.)

**Fix:** In the legacy branch read `fb?.dailyProduction ?? fb?.production`, `fb?.dailyNeed ?? fb?.need`, and `fb?.deficitPercent ?? computed` — or delete the legacy branch once canonicalViewModel is default-on.

**Fix note:** Not actionable as a bug. If anything, since canonicalViewModel is default-on and no path disables it, the legacy branch is dead code in practice; the right cleanup (matching the reviewer's own alternative) is to delete the legacy branch and inline the canonical path per the flags.js doctrine ("If a flag would be on everywhere forever, delete the flag and inline the change"), not to patch the stale field reads. That is a cleanup, not a bug fix, and is out of scope for a defect finding.

### [MEDIUM] src/pdf/lib/viewModel.js:280 — Quarter descriptions never print — viewModel reads q.description but the engine field is q.desc

**Evidence:** identitySlice (:279-281) and overviewSlice (:383-385) map quarters with `description: q?.description || null`. The engine quarter shape is `{ name, location, desc, landmarks }` — documented and consumed in src/domain/districtProfile.js:5-6 ('light fields (name, location, desc, landmarks)'), :67 and :209 (`quarter.desc`), and the web renders `q.desc` (OverviewTab.jsx:250). So the PDF QUARTERS blocks (Overview.jsx:358-381) print quarter names and landmarks but silently drop every description the screen shows.

**Fix:** Map `description: q?.desc || q?.description || null` in both slices.

**Fix note:** Suggested fix `description: q?.desc || q?.description || null` is correct and should be applied in BOTH slices (viewModel.js:279 and :384). The `|| q?.description` fallback is harmless/forward-compatible. Alternatively, given the engine canonically emits `desc`, `description: q?.desc || null` would also fully fix it. Confidence ~0.95.

### [MEDIUM] src/pdf/lib/viewModel.js:403 — Overview 'Warnings & Notes' reads coherenceNotes/structuralSuggestions from economicViability but they live at the settlement root — always empty in PDF

**Evidence:** overviewSlice: `coherenceNotes: v?.coherenceNotes || []` and `structuralSuggestions: v?.structuralSuggestions || []` where v = s.economicViability (viewModel.js:403-404). The engine's economicViability has no such keys (economicGenerator.js:2670-2686 — closest is `suggestions`). The web reads them from the ROOT: OverviewTab.jsx:259-279 (`r.coherenceNotes`, `r.structuralSuggestions`, `r.structuralViolations`) and SummaryTab.jsx:97 destructures `coherenceNotes` from r. So the PDF Overview's WARNINGS & NOTES block silently drops every coherence note and structural suggestion the screen displays.

**Fix:** Read `s?.coherenceNotes || []` and `s?.structuralSuggestions || []` from the settlement root (keep economicViability as a fallback), and make the item renderer handle the actual shapes ({note, severity} and {reason, suggested[]}).

**Fix note:** The suggested fix is correct and complete in spirit. Concretely: in viewModel.js overviewSlice read from the root — `coherenceNotes: s?.coherenceNotes || v?.coherenceNotes || []` and `structuralSuggestions: s?.structuralSuggestions || v?.structuralSuggestions || []`. AND fix the renderer in Overview.jsx:390-399 so item shapes render: coherenceNotes items are {note, severity}; structuralSuggestions items are {reason, suggested[]}. The current `label(it)` path returns '' for both shapes, so the data fix alone is insufficient. Additional parity gap worth addressing in the same change: the web Overview also shows structuralViolations (OverviewTab.jsx:260-262, {institution|group, reason}), which the PDF Overview WARNINGS & NOTES block omits entirely — consider including o.structuralViolations for full parity.

### [MEDIUM] src/pdf/lib/viewModel.js:484 — powerSlice defaults publicLegitimacy.breakdown to an array but the engine emits an object map

**Evidence:** powerSlice: `legitimacyBreakdown: s?.powerStructure?.publicLegitimacy?.breakdown || []` (viewModel.js:484). The engine's breakdown is an object: `breakdown: { prosperity: pContrib, safety: sContrib, defense: dContrib, food: fContrib }` (factionDynamics.js:130), and the web iterates it with Object.entries (PowerTab.jsx:91). The `|| []` default signals the PDF PowerStructure section expects an array — if it `.map()`s this field (PowerStructure.jsx was unreadable in this checkout), the export crashes with 'map is not a function' whenever publicLegitimacy exists; if it Object.entries it, the [] default is merely misleading. Either way the slice's declared shape disagrees with the engine.

**Fix:** Normalize in the slice: `Object.entries(breakdown || {}).map(([k, v]) => ({ key: k, delta: v }))` so the section gets a stable array.

**Fix note:** The reviewer's crash mechanism is WRONG and should be corrected: ScoreWithBreakdown (src/pdf/primitives/Visuals.jsx:199) guards the render with `breakdown?.length > 0` BEFORE calling `breakdown.map()` (line 201). A plain object has `length === undefined`, so the guard short-circuits — there is NO 'map is not a function' crash. The actual bug is silent omission: the PDF renders zero legitimacy breakdown chips while the web (PowerTab.jsx:91) shows them via Object.entries — a real dossier/PDF parity drift, and the `|| []` default misdeclares the shape.

The suggested fix is on the right track but mislabels the keys. ScoreWithBreakdown reads each chip as `b?.delta ?? b?.value` and `b?.label || b?.name` (Visuals.jsx:202-203). The reviewer's `{ key, delta }` would render chips with EMPTY labels because `key` is never read. Correct normalization in viewModel.js:484: `Object.entries(s?.powerStructure?.publicLegitimacy?.breakdown || {}).map(([k, v]) => ({ label: k, delta: v }))`.

### [MEDIUM] src/pdf/lib/viewModel.js:609 — Defense chapter's 'ACTIVE MILITARY STATUS' siege/occupation banner can never render — stress matcher checks icon/key against wrong values

**Evidence:** defenseSlice: `stress.find(x => ['siege', 'occupied', 'civilWar'].includes(x?.icon || x?.key))` (viewModel.js:609). Stress entries are built by buildStressEntry (src/generators/stressGenerator.js:28-37) as `{ type, label, icon, colour, summary, crisisHook, ... }` — `icon` is an emoji, there is no `key` field, and the actual type keys are 'under_siege', 'occupied', 'wartime' (src/data/stressTypes.js:13,40,217; web tabHelpers.js:15 checks `s?.type === 'under_siege'`). 'siege' and 'civilWar' match no key under any field. So a settlement under siege or occupation exports a Defense chapter with no military-status callout (DefenseSecurity.jsx:64-78).

**Fix:** Match on type: `stress.find(x => ['under_siege', 'occupied', 'wartime', 'insurgency'].includes(x?.type))`.

**Fix note:** Direction of the suggested fix (match on `x?.type`) is correct, but the proposed set ['under_siege','occupied','wartime','insurgency'] does NOT match the web parity reference and would create a different drift: famine/politically_fractured/recently_betrayed/plague_onset render the web callout but would still be missing from PDF, while wartime/insurgency would appear in PDF but not web. To achieve true parity, mirror DefenseTab.jsx's STRESS_STATUS keys: stress.find(x => ['under_siege','famine','occupied','politically_fractured','recently_betrayed','plague_onset'].includes(x?.type)). Ideally factor the STRESS_STATUS set into a shared helper so the two surfaces cannot drift again.

### [MEDIUM] src/pdf/lib/viewModel.js:774 — Resources chapter: exportPotential, structuralGaps, and priorityNotes read wrong paths — Export Potential and Gaps & Opportunities sections never render

**Evidence:** resourcesSlice maps `exportPotential: ra?.exportPotential || s?.exportPotential` (:774), `priorityNotes: v?.priorityNotes` from economicViability (:775), `structuralGaps: ra?.structuralGaps || s?.structuralGaps` (:776). The web ResourcesTab reads the real engine fields: `res.exports` (ResourcesTab.jsx:157, items with .product/.good/.value/.reason — exactly the shape ResourcesProduction.jsx:214-247 expects), `res.gaps` (:173,183 — with .chain/.impact/.missing/.severity exactly matching ResourcesProduction.jsx:318-356), and `res.priorityNotes` on resourceAnalysis (:176), not economicViability. The PDF section code was clearly ported from the web tab but the slice feeds it from keys the engine never emits, so EXPORT POTENTIAL and GAPS & OPPORTUNITIES are permanently empty in print while populated on screen. The chapter headline call (ResourcesProduction.jsx:51) tries `r.exports` which isn't a slice key either.

**Fix:** In resourcesSlice read `ra?.exports || ra?.exportPotential`, `ra?.gaps || ra?.structuralGaps`, and `ra?.priorityNotes || v?.priorityNotes`.

**Fix note:** Suggested fix is correct in direction. Cleanest form: in resourcesSlice set `exportPotential: ra?.exports || [],` `priorityNotes: ra?.priorityNotes || [],` `structuralGaps: ra?.gaps || [],`. The `|| s?.exportPotential`, `|| s?.structuralGaps`, and `v?.priorityNotes` branches are dead (no producer) and can be dropped rather than retained. Note the headline at ResourcesProduction.jsx:51 reads `r.exports || r.exportPotential`; once the slice key `exportPotential` is fed from ra.exports it works, but `r.exports` there remains a non-existent slice key (harmless dead-OR). Reviewer's proposed `ra?.exports || ra?.exportPotential` etc. is also fine since ra.exportPotential is always undefined.

### [MEDIUM] src/pdf/lib/viewModel.js:807 — Viability chapter reads three engine fields that don't exist (stressConsequences, byDesignContradictions, activeMagicChains) — web derives them, PDF renders nothing

**Evidence:** viabilitySlice exposes `stressConsequences: v?.stressConsequences || []`, `activeMagicChains: v?.activeMagicChains || []`, `byDesignContradictions: v?.byDesignContradictions || []` (viewModel.js:807-810). generateEconomicViability returns only `{ viable, issues, warnings, dependencies, suggestions, plotHooks, summary, metrics }` (src/generators/economicGenerator.js:2670-2686) — none of those three keys exist. The web derives them: ViabilityTab.jsx:28-30 filters issues/warnings on `type==='stress_consequence'`, :40 filters `severity==='by_design'`, :121 derives magic chains from `economicState.activeChains.filter(c=>c.magicNote)`. ViabilityAssessment.jsx:38-39 and :83-89 render these slice fields, so the PDF's By-Design Contradictions, stress Consequences, and magic-chain tags never appear while the screen shows them — and by_design/stress_consequence issues additionally land unfiltered in the PDF's ACTIVE ISSUES list, unlike the web.

**Fix:** Derive in viabilitySlice the same way the web does: byDesignContradictions = issues filtered by severity 'by_design'; stressConsequences = issues+warnings with type 'stress_consequence' (and exclude both from the main issues list); activeMagicChains = economicState.activeChains.filter(c => c.magicNote).map(c => c.label).

**Fix note:** The suggested fix is correct for stressConsequences (issues+warnings with type 'stress_consequence', and exclude them from the main issues list to match the web) and activeMagicChains (economicState.activeChains.filter(c=>c.magicNote).map(c=>c.label)). NOTE: the slice currently reads economicState as `s.economicState`, but viabilitySlice only has `s` and `v=s.economicViability` — derive activeMagicChains from `s.economicState?.activeChains`, not from `v`. For byDesignContradictions the suggested fix (filter issues by severity 'by_design') will NOT work: by_design items live in s.structuralViolations (from checkStructuralValidity), never in economicViability.issues. To actually populate the PDF's By-Design Contradictions, derive from s.structuralViolations.filter(x=>x.severity==='by_design') (and note the web's own by-design section reads v.issues and is therefore likely empty too — fixing parity may mean fixing the web read as well, or accepting both render empty). Also fix the issue-leak: the PDF should exclude type==='stress_consequence' (and by_design) from the ACTIVE ISSUES list the way the web does, otherwise those rows double-render.

### [MEDIUM] src/pdf/sections/AIAppendix.jsx:179 — AI Appendix friction points print as blank numbered rows — canonical shape is {who, what} but the section reads parties/text/description

**Evidence:** The narrative edge function normalizes friction points to exactly `{ who, what }` (supabase/functions/generate-narrative/index.ts:978-982: filters `typeof x.who === 'string' && typeof x.what === 'string'`). The web renders p.who/p.what (DMCompassTab.jsx:107-111). AIAppendix.jsx:179 reads `fp.parties` (doesn't exist) and the body via textOf(fp) which checks text/description/summary/label/title/detail (:233-242) — `what` is not in the list. Result: the PDF's Friction Points section renders 3-5 rows containing only the index numbers '01'..'05' with no parties and no grievance text, while the screen shows full content. This is paid AI-narrative content silently lost in the export.

**Fix:** Render `fp.who` for the parties line and add `item.what` to textOf()'s field list (or read fp.what directly).

**Fix note:** The suggested fix is correct and sufficient: render fp.who for the parties line (replacing the fp.parties check at AIAppendix.jsx:179-183) and surface fp.what for the body (either read fp.what directly at line 184 or add item.what to textOf's field list at line 240). Note: if textOf gains 'what', order it after the existing keys to avoid changing behavior for other item types that already use text/description; reading fp.who and fp.what directly in the friction block is the cleaner, lower-blast-radius option since this is the only consumer that needs the {who, what} shape.

### [MEDIUM] src/pdf/sections/AIAppendix.jsx:250 — AI Appendix connections map labels every edge 'LINK' and drops `via` — canonical fields are nature/via, section reads relationship/type/kind/note

**Evidence:** The edge function normalizes connections to `{ from, to, via, nature }` (generate-narrative/index.ts:1027-1037, 'from/to/nature are canonical'). The web renders e.nature and e.via (DMCompassTab.jsx:131-136). AIAppendix.jsx relationshipOf() reads `c.relationship || c.type || c.kind || 'LINK'` (:250-253) — never `nature` — so every printed edge pill says 'LINK'; and the section reads `c.note` (:219) instead of `c.via`, so mediated edges lose their mediator. The relationship semantics (the entire point of the connections map) are absent from the PDF.

**Fix:** relationshipOf: `(c.nature || c.relationship || c.type || c.kind || 'LINK')`; render `c.via` (e.g. 'via {c.via}') instead of c.note.

**Fix note:** Fix is correct. Refinements: (1) relationshipOf should prefer nature: `(c.nature || c.relationship || c.type || c.kind || 'LINK').toUpperCase()` — keeping the legacy synonyms as fallback is harmless and tolerates any older persisted snapshots. (2) For the mediator, gate and prefix it to match the web ("via {c.via}") and keep c.note as a legacy fallback, e.g. render when `(c.via || c.note)` and show `via ${c.via || c.note}`. The 90pt-wide Pill column may truncate longer nature phrases like "competes with", so verify the pill width accommodates real `nature` values rather than the short 'LINK' it was sized for.

### [MEDIUM] src/pdf/sections/Services.jsx:196 — PDF Services chapter prints trade-impaired supply chains as 'Active' — ChainCard ignores chain.status/dependency that the web surfaces prominently

**Evidence:** ChainCard derives status only from resourceDepleted/substituteActive/entrepot (Services.jsx:196-210), falling through to 'Active' with a green tone. But the engine sets `chain.status = 'impaired' | 'vulnerable'` from critical trade dependencies (computeActiveChains.js:287-293) and attaches `chain.dependency`; the web ServicesTab's headline feature is exactly this impairment state (computeChainSets in tabHelpers.js + 'N impaired' badges throughout ServicesTab.jsx). A chain impaired by a severed critical import prints as a healthy 'Active' chain in the dossier.

**Fix:** Check `chain.status` first: impaired → tone 'bad'/'Impaired', vulnerable → 'warn'/'Vulnerable', then the existing depleted/substitute/entrepôt branches; optionally render chain.dependency.resource as the reason.

**Fix note:** Direction is correct. Concretely: in ChainCard at src/pdf/sections/Services.jsx:196, prepend status-based branches before the existing resourceDepleted/substituteActive/entrepot ladder — chain.status === 'impaired' -> tone 'bad', label 'Impaired'; chain.status === 'vulnerable' -> tone 'warn', label 'Vulnerable'. Surface the reason via the existing meta/note slots, e.g. push { label: 'DEPENDS', value: humanize(label(chain.dependency.resource)) } and/or chain.dependency.institution when chain.dependency is present (it carries {institution, resource, severity, impact, affectedServices}). Keep the depleted/substitute/entrepot branches as lower-priority fallthroughs since a chain can be both resource-depleted and import-impaired; impaired should win.

### [MEDIUM] src/utils/generateCampaignPDF.js:435 — Campaign PDF 'Cross-Settlement NPC Contacts' always shows the empty state — reads npc.interSettlementNpcs which nothing produces

**Evidence:** buildNPCConnections gathers `npc.interSettlementNpcs || []` per NPC (generateCampaignPDF.js:433-447). A repo-wide grep shows `interSettlementNpcs` appears ONLY in this file — no generator, store, or lib produces it. The real cross-settlement contact data lives at `settlement.interSettlementRelationships` (entries with npcName/partnerName/partnerRole/partnerSettlement/relType — exactly the columns this table wants, see RelationshipsTab.jsx:36,130-156) and `neighbourNetwork[].npcConnections` (RelationshipsTab.jsx:100-103). The campaign export therefore always prints 'No cross-settlement NPC contacts recorded' even for fully linked campaigns.

**Fix:** Iterate `save.settlement.interSettlementRelationships` (untyped entries) and/or `neighbourNetwork[].npcConnections`, mapping npcName/partnerName/partnerSettlement/relType into the table rows.

**Fix note:** Suggested fix is correct in direction. Concretely: in buildNPCConnections, replace the per-NPC `npc.interSettlementNpcs` loop with iteration over `save.settlement?.interSettlementRelationships || []`, filtering out typed conflict/faction_engagement rows (`.filter(r => !r.type)`) to mirror RelationshipsTab.jsx:130 which treats only untyped entries as raw NPC contacts. Map fields directly from the entry: npc=r.npcName, role=r.npcRole, partnerName=r.partnerName, partnerRole=r.partnerRole, partnerSettlement=r.partnerSettlement, relType=r.relType — note these are top-level entry fields (npcName/partnerName), NOT npc.name/npc.role as the current code assumes; the entry already carries partnerSettlement. Optionally also fold in neighbourNetwork[].npcConnections (primaryNPCName/neighbourNPCName) for live/unsaved links, but interSettlementRelationships alone covers saved linked campaigns. The de-dup the legend/colors use REL_COLORS keyed by relType should be checked against the relType vocabulary actually emitted (trade_partner/allied/patron/client/rival/cold_war/hostile/neutral per RelationshipsTab.jsx:136) so colored pips resolve rather than always falling back to neutral.

### [MEDIUM] src/utils/generateCampaignPDF.js:581 — Campaign PDF digest 'OVERVIEW' block is always blank — reads characterSummary/description/overview which no settlement carries

**Evidence:** buildDigest: `const overview = s(st_.characterSummary || st_.description || st_.overview || '')` (generateCampaignPDF.js:581). None of these three fields is produced anywhere in src (grep finds them only in this file); the settlement's character prose lives at `history.historicalCharacter` (rendered by OverviewTab.jsx:90, HistoryTab.jsx:83, and the settlement PDF identity slice). Every digest card prints an 'OVERVIEW' header with three empty lines beneath it.

**Fix:** Use `st_.history?.historicalCharacter || st_.arrivalScene || st_.pressureSentence || ''` as the digest overview source.

**Fix note:** The suggested primary source `st_.history?.historicalCharacter` is correct and matches the canonical field used everywhere else. Two notes on the fallbacks in the suggested fix: `st_.arrivalScene` and `st_.pressureSentence` were not verified as existing top-level fields here, so the fix should not assume they populate — `st_.history?.historicalCharacter || ''` alone resolves the reported defect. Also, the digest currently prints the 'OVERVIEW' header and rule line even when the body is empty; if desired, consider gating the header on a non-empty overview so blank cards don't render a dangling header. Minor evidence correction: the empty value yields zero wrapped lines (a blank section), not literally three empty lines.

### [LOW] src/pdf/lib/headlines.js:144 — Chapter headline helpers read fields that don't exist on the slices they receive — power/resources/relationships headlines degrade or never render

**Evidence:** All visible call sites pass the corresponding vm slice (economicsHeadline(vm.economics), defenseHeadline(vm.defense), npcsHeadline(vm.npcs), viabilityHeadline(vm.viability), hooksHeadline(vm.hooks)). But: resourcesHeadline reads `resources.primaryExports/primaryImports` (headlines.js:144-153) which the resources slice doesn't have — the ResourcesProduction.jsx:51 call site papers over it with `r.exports || r.exportPotential` where r.exports is also not a slice key and exportPotential is empty (see exports-field finding), so the headline is effectively always null; relationshipsHeadline reads `rel.all || rel.relationships` (:216-221) — relationshipsSlice has neither key (it has internal/interSettlement/neighbours), so it always returns null; powerHeadline reads `factions[].faction` (:48-58) but powerSlice normalizes factions to `{ name, ... }` (viewModel.js:434), so the 'X governs' / challenger phrases can never include faction names. defenseHeadline's `def.magicDependency` (:114) is also absent from the defense slice.

**Fix:** Align each headline to its slice shape: powerHeadline use f.name; relationshipsHeadline use rel.neighbours/internal; resourcesHeadline accept the slice's exports field once it's fixed; add magicDependency to defenseSlice.

**Fix note:** Scope correction: the resources and relationships portions of this finding are FALSE positives — those call sites (ResourcesProduction.jsx:51, Relationships.jsx:60) build adapter objects with exactly the keys the helpers read (primaryExports/primaryImports, all), so no fix is needed there for the headline-vs-slice mismatch (the empty-exports issue belongs to the separate exportPotential finding). Only two fixes are warranted: (a) powerHeadline (headlines.js:53,55-56) should read f.name instead of f.faction to match powerSlice's normalized shape (viewModel.js:433); (b) defenseHeadline's magicDependency check (headlines.js:114) needs either adding `magicDependency: !!(active?.defenseProfile?.magicDependency)` to defenseSlice (viewModel.js:623-656) or wiring the helper to read it from the viability slice where it already exists (viewModel.js:808). Both are low-impact cosmetic improvements to a single TLDR line.

### [LOW] src/pdf/sections/Cover.jsx:306 — Cover 'CAMPAIGN' field renders as a dangling label — EditableText with empty default and hideIfEmpty returns null

**Evidence:** Cover footer renders `CAMPAIGN` followed by `<EditableText name="cover.campaign" defaultValue="" />` (Cover.jsx:304-311). After the Editable v2 rewrite, EditableText with empty value and default hideIfEmpty returns null unless showField is set (Editable.jsx:73-79); only NotesField opts in. So the cover prints a 'CAMPAIGN' label followed by a 130pt empty gap — neither a value nor a writable field — despite the file header still declaring 'Editable: ... cover.campaign'.

**Fix:** Pass `showField` + `hideIfEmpty={false}` for cover.campaign (it's a deliberate write-in field like NotesField), or drop the CAMPAIGN label entirely.

**Fix note:** Both suggested options are valid. Note the cited file path in the evidence is wrong — the component is at src/pdf/primitives/Editable.jsx, not src/pdf/components/Editable.jsx (line numbers shift: the early-return is Editable.jsx:75, not 73-79). Before applying showField + hideIfEmpty={false}, check the on-screen dossier cover for parity: if the screen cover has no campaign write-in field, adding a PDF-only field introduces a new dossier/PDF parity drift, so the cleaner fix is to drop the CAMPAIGN label (and its 130pt View) entirely and update the file-header "Editable:" list on line 9 to match.

### [LOW] src/pdf/sections/NPCQuickRef.jsx:25 — NPC power tints and high-power promotion are dead code — thresholds 40/60/80 vs engine NPC power scale 1-10

**Evidence:** powerTone (NPCQuickRef.jsx:25-31) returns 'bad'/≥80, 'warn'/≥60, 'gold'/≥40 — but engine NPC power is 1-10 (npcGenerator.js:54-56: high = 8+rng*3, mid = 4-7, low = 1-3; `influence` is the string level). Every pill is therefore 'muted', contradicting the chapter headline 'Power tints flag who's worth caring about' (:91). Same scale mismatch in NotableNPCs.jsx:59 (HIGH_POWER = 80): the documented 'a settlement with eight 90+ power players isn't artificially trimmed' promotion can never fire, so the major tier is always exactly the top 3.

**Fix:** Calibrate to the engine scale (e.g. ≥8 bad/notable, ≥6 warn, ≥4 gold; HIGH_POWER = 8) or use npc.influence ('high'/'moderate'/'low').

**Fix note:** The suggested fix is correct in direction. Two refinements: (a) For NPCQuickRef.powerTone, calibrating to the 1-10 scale is right; aligning to the engine's own buckets is cleaner than arbitrary cutoffs — e.g. >=8 'bad' (high tier), >=4 'warn'/'gold' (mid), else 'muted' (low). Using npc.influence ('high'/'moderate'/'low') is the most robust option since it is the engine's authoritative level and avoids re-deriving thresholds. Note the influence string is 'moderate' (not 'medium') per npcGenerator.js:55. (b) For NotableNPCs HIGH_POWER, set to 8 (top tier) so genuinely high-power figures past the top-3 cap get promoted to major. Either approach keeps determinism intact since power/influence are seeded engine outputs.


## 06-components (23)

### [HIGH] src/components/SettlementsPanel.jsx:798 — Canonical save path skips neighbour-network migration and bidirectional linking — the only code that did it (saveCurrentSettlement) is now dead

**Evidence:** SettlementsPanel.saveCurrentSettlement (lines 798-890) performs three save-time tasks: migrate generator `neighborRelationship` → `neighbourNetwork` (829-835), bidirectionally link the partner save with canonical edge roles (841-881), and generate inter-settlement NPC pairs + cross-settlement conflicts (872-873). But its UI trigger was removed (comment at 1076-1079: 'saving a fresh draft lives in the generate flow (SaveToLibraryButton)'), making it, `isDuplicate` (795), and the `saved` state (612) dead code — nothing in the file references them. The canonical save path, GenerateWizard's SaveToLibraryButton (GenerateWizard.jsx:215-220) and the SAVE_SETTLEMENT auth intent (GenerateWizard.jsx:242-248), calls `savesService.save()` directly, which only runs migrateSaveToV2 (campaignState defaults) — lib/saves.js has no neighborRelationship→neighbourNetwork migration and never calls buildInterSettlementNPCs/generateCrossSettlementConflicts (grep: those are only invoked from the dead path and SettlementDetail's manual Link flow). domain/normalizeSettlement.js contains no neighbourNetwork handling either. Result: every new save with a generation-time neighbour persists the raw `neighborRelationship` only — partner saves get no back-link, and NetworkEffectsPanel/regionalGraph (which read `neighbourNetwork`, regionalGraph.js:220) silently miss these relationships.

**Fix:** Move the neighborRelationship→neighbourNetwork migration + bidirectional partner linking into lib/saves.js save() (or a shared domain helper invoked by both SaveToLibraryButton and the auth-intent), then delete the dead saveCurrentSettlement/isDuplicate/saved code.

**Fix note:** The suggested fix is correct in direction (move migration + bidirectional linking into a shared domain helper invoked by both SaveToLibraryButton and the SAVE_SETTLEMENT intent, then delete the dead saveCurrentSettlement/isDuplicate/saved). One refinement: the bidirectional partner back-link requires UPDATING the partner save's row too, but the canonical entrypoint uses savesService.save() (a single insert), which cannot touch the partner. The shared helper must perform a batch mutation (insert of the new save + update of the partner), i.e. route through savesService.mutateBatch rather than save(), or the partner back-link still won't persist. The scalar->neighbourNetwork migration alone (no partner present) can be done inline in save(), but the full feature parity needs the batch path. Also note saves.js writes neighbour_links from settlement.neighbourNetwork on both insert (180) and update (201), so once the embedded settlement carries the array the column will populate correctly.

### [HIGH] src/components/SettlementsPanel.jsx:1026 — Library search/sort/filter UI is inert — filtered list is computed but never rendered

**Evidence:** P108/E-6 wires a LibraryToolbar (search + sort + filter chips) and computes `filteredSaves = applyLibraryFilters(saves, {query, sort, filters})` (lines 1023-1032), but `filteredSaves` is used ONLY for the toolbar's `visibleCount` prop (line 1072). The actual list renders campaign folders from `campaign.settlementIds.map(id => saves.find(...))` (line 1112) and the unassigned section from `unassignedSaves` which is derived from raw `saves` (line 1018, rendered line 1145). Typing a query or toggling chips updates the 'showing X of Y' count but never changes the cards shown. LibraryToolbar's own doc comment says its output 'is consumed by applyLibraryFilters(saves, state)' to filter the saves list, so this is drift, not intent.

**Fix:** Derive the rendered collections from `filteredSaves`: compute `unassignedSaves` from `filteredSaves`, and intersect each campaign's `settlementIds` resolution with the filtered set (or render a flat filtered list while a query/filter is active).

**Fix note:** The suggested fix is correct in direction. Two clean options: (1) Render a flat filtered list from `filteredSaves` whenever any query/sort/filter is active, bypassing the folder grouping (simplest, matches 'visibleCount' semantics directly). (2) Keep grouping but derive both collections from the filtered set: compute `unassignedSaves` from `filteredSaves` instead of raw `saves`, and for each campaign resolve `campaign.settlementIds.map(id => filteredSaves.find(...)).filter(Boolean)`. Note one subtlety with option 2: the sort order from applyLibraryFilters (LibraryToolbar.jsx:84) is lost when you re-resolve campaign settlements by id in settlementIds order, and is partially lost for unassigned if you re-filter rather than reuse the sorted array — so to make the 'Sort' control meaningful you should preserve the filtered/sorted ordering when rendering (e.g., iterate filteredSaves and bucket into assigned/unassigned), or accept that sort only applies to a flat view (option 1). A flat filtered list while a filter is active (option 1) is the most faithful to the toolbar's documented contract and avoids the ordering-loss caveat.

### [MEDIUM] src/components/GenerateWizard.jsx:693 — Advanced wizard 'Ready to Generate' state renders a ghost 'Step 5: General Configuration' banner with an empty step card and duplicate nav

**Evidence:** Clicking 'Ready to Generate' calls `setWizardStep(STEPS.length)` (=4, line 765); configSlice.setWizardStep stores it unclamped (configSlice.js:65-66). The step block at line 693 is gated only on `isAdvanced && !settlement` — not on `wizardStep < STEPS.length` — so at step 4 it still renders: the hint banner shows 'Step {wizardStep+1}: {currentStepDef.label}' = 'Step 5: General Configuration' (line 704, since `STEPS[4] || STEPS[0]` falls back at line 591), an aria-label 'Step 5 of 4' (line 718), a step card whose body is empty (none of the `wizardStep === 0..3` branches match, lines 727-730) but whose header still says 'General Configuration', plus a second 'Ready to Generate' button (line 764) — all stacked above the intended WizardCloseout (line 783) and Generate button.

**Fix:** Gate the step-indicator/content/navigation block on `wizardStep < STEPS.length` (e.g. `isAdvanced && !settlement && wizardStep < STEPS.length`), leaving WizardCloseout + Generate as the sole final-state surface.

**Fix note:** Suggested fix is correct: gate the step-indicator/content/navigation block (line 693) on `isAdvanced && !settlement && wizardStep < STEPS.length`, leaving WizardCloseout + Generate as the sole final-state surface. Equivalently the codebase could clamp setWizardStep, but using a distinct sentinel value (STEPS.length) to mean "ready" is fine — the only fix needed is tightening the line-693 guard. Note the stale comment at line 782 ("Self-gates on the flag") should also be corrected since WizardCloseout has no flag gate.

### [MEDIUM] src/components/OutputContainer.jsx:446 — OutputContainer crashes on null settlement: `settlement.stress` is evaluated before the deliberately deferred null check

**Evidence:** The component documents a hooks-safe design: 'do not early-return here… We instead defer the null check until after all hooks have been called' (lines 207-212), with `earlyExitOnNoSettlement` returned at line 567. But line 445-447 run unconditionally in the body BEFORE that return: `const settlement = rawSettlement; const stressObj = settlement.stress ? … : null;` — no optional chaining. With `rawSettlement` null (no prop, empty store), this throws TypeError before reaching the guard, so the early-exit path can never work. Current callers happen to guard (GenerateWizard renders it only when `settlement` is truthy; SettlementDetail/PublicDossierView pass a checked prop), so this is latent — but it defeats the explicit defensive design and will crash on any future direct render without a settlement.

**Fix:** Use `rawSettlement?.stress` (and audit lines 445-447 for other unguarded accesses), or move the lines 445-447 derivation below the line 567 early return.

**Fix note:** Suggested fix is correct. Cleanest is `const stressObj = settlement?.stress ? (Array.isArray(settlement.stress) ? settlement.stress[0] : settlement.stress) : null;` (the `const settlement = rawSettlement` aliasing is harmless; the unsafe part is the `.stress` access). Alternatively move lines 445-447 below the line 567 early return. While there, the rest of the body between hooks and 567 should be quick-audited, but note line 355 already uses optional chaining, so 446 appears to be the lone offender. Severity is medium-as-claimed if you weight defeating an explicit safety mechanism, but I rate it low because no live caller can reach the null path (all three guard upstream) — it cannot crash main flow today; it only crashes a hypothetical future direct render with no prop and empty store.

### [MEDIUM] src/components/OutputContainer.jsx:603 — 'Reroll History' is unreachable: its only UI surface (the dossier header reroll button) never renders on any live path

**Evidence:** REROLLABLE = { npcs: 'Reroll NPCs', history: 'Reroll History' } (line 110) renders only inside the header block (line 580-616), gated by `REROLLABLE[selectedTab] && onRegenerate`. But: (1) the live editor (GenerateWizard.jsx:917) passes `hideHeader`, so the whole header — including this button — is suppressed; (2) SettlementDetail (SettlementDetail.jsx:849) and PublicDossierView (PublicDossierView.jsx:118) pass `readOnly`, which nulls `onRegenerate` (line 188). NPCs keep a working in-tab reroll because renderTab passes `onRerollNPCs` to NPCsTab (line 424, NPCsTab.jsx:68), but `case 'history'` (line 425) passes no reroll prop and HistoryTab has none. So the store's regenSection('history') capability is dead UI everywhere.

**Fix:** Pass an `onReroll` prop to HistoryTab from renderTab (mirroring NPCsTab) or render the reroll affordance outside the hideHeader-gated header.

**Fix note:** Both proposed fixes are valid. The cleaner, parity-matching option is the first: in renderTab `case 'history'` (OutputContainer.jsx:425), pass a reroll prop mirroring NPCs — e.g. `onReroll: onRegenerate ? () => onRegenerate('history') : null` — and render that button inside HistoryTab (HistoryTab.jsx currently has no reroll affordance). This keeps the reroll reachable in the live editor (where hideHeader suppresses the header) and stays correctly hidden on the readOnly saved/public surfaces (where onRegenerate is null), consistent with how NPCs already behaves. Note: NPC reroll is unaffected by this bug precisely because it has the in-tab path; history lacks it.

### [MEDIUM] src/components/WorldMap.jsx:563 — WorldMap ⌘S keyboard save calls non-existent store action `saveMapToCampaign` — silently no-ops while suppressing the browser default

**Evidence:** The worldbuilder keymap (WorldMap.jsx:551-580) handles Cmd/Ctrl+S with `e.preventDefault(); const fn = useStore.getState().saveMapToCampaign; if (typeof fn === 'function') fn();`. No slice defines `saveMapToCampaign` — the campaign slice's action is `saveCampaignMap(campaignId, mapStateOverride)` (store/campaignSlice.js:520), and a repo-wide grep for `saveMapToCampaign` matches only this WorldMap line. So `fn` is always undefined: ⌘S does nothing, shows no toast, and `preventDefault()` has already eaten the keystroke. The advertised P112/M-8 behavior ('⌘S saves') is dead; a DM who relies on the muscle-memory shortcut believes the map saved when it did not (the AutoSaveChip mitigates only when the `mapAutosave` flag is on AND a campaign is active). The correct local handler `handleSaveMapToCampaign` (line 436) exists in the same component and is simply not referenced from the key handler. Even if the store action existed, it would be called with no campaignId argument.

**Fix:** Call the component's own `handleSaveMapToCampaign` from the keydown handler (add it to the effect deps), or implement/rename a `saveMapToCampaign` store action that resolves the active campaign id; show the same success/error toast as the toolbar Save button.

**Fix note:** Reviewer's fix is correct. Cleanest: in the keydown handler call the component's own `handleSaveMapToCampaign()` instead of the non-existent store action, and add `handleSaveMapToCampaign` to the effect dependency array (currently `[setMapMode, handleFit]` at line 580) so the closure stays current. That handler already shows the same success/error toast as the toolbar Save button and resolves activeCampaignId internally. Also delete/correct the misleading 'fallback' comment at lines 559-563. Note `handleSaveMapToCampaign` early-returns silently when there is no active campaign (line 437), so ⌘S with no campaign selected will still be a no-op with no feedback — optionally add a toast for that case to fully honor the advertised shortcut.

### [MEDIUM] src/components/generate/WizardNextSteps.jsx:99 — WizardNextSteps compares auth tier against 'wanderer' (a pricing key, not an auth tier) — anonymous users get the wrong save guidance

**Evidence:** `const signedIn = !!authTier && authTier !== 'wanderer';` — but `auth.tier` is documented and implemented as 'anon' | 'free' | 'premium' (store/authSlice.js:64); 'wanderer' is the marketing key in config/pricing.js. So `signedIn` is true for anonymous users ('anon' !== 'wanderer'). In saveStep() (lines 29-50), an anon who can't save then falls into the signed-in branch and is told "Save it. Free up a slot or upgrade — You've reached your library's save cap" instead of "Save it. Create a free account — Free accounts keep your settlements and unlock Town+ tiers". This card renders after every generation for anonymous users (WizardNextSteps is mounted unconditionally from GenerateWizard.jsx:936), i.e. wrong copy on the main signup-funnel surface.

**Fix:** Change the check to `authTier !== 'anon'` (or `useStore(s => s.auth.tier !== 'anon')`).

**Fix note:** Suggested fix `authTier !== 'anon'` is correct. To preserve the existing null-guard cleanly, prefer `const signedIn = !!authTier && authTier !== 'anon';` (functionally authTier is always one of the three valid strings, so the bare `authTier !== 'anon'` also works). Also worth updating the now-misleading JSDoc at WizardNextSteps.jsx:59 which says "non-wanderer" — that comment is what encoded the original confusion.

### [MEDIUM] src/components/library/LibraryToolbar.jsx:77 — LibraryToolbar 'has neighbours' filter chip checks the nonexistent save.neighbourLinks field — would always yield an empty list

**Evidence:** `if (filters.hasNeighbours) out = out.filter(s => Array.isArray(s.neighbourLinks) && s.neighbourLinks.length > 0);` — same dead field as useReaderAudience: saves carry neighbour data at `s.settlement.neighbourNetwork` (the rendering in SettlementsPanel.jsx:123-131 reads exactly that). Even once the parent's filtered-list bug (filteredSaves never rendered) is fixed, this chip will filter every save out. (Read one hop out from SettlementsPanel because applyLibraryFilters is exported from here and consumed there.)

**Fix:** Filter on `s.settlement?.neighbourNetwork?.length > 0` (optionally also `s.neighbour_links` from the Supabase row, which lib/saves.js:180 mirrors from neighbourNetwork).

**Fix note:** Suggested fix is correct: change line 77 to `out = out.filter(s => Array.isArray(s.settlement?.neighbourNetwork) && s.settlement.neighbourNetwork.length > 0);` To match other consumers' tolerance you could also fall back to `s.neighbourNetwork` (e.g. RelationshipEdges.jsx:69-72 reads both), but `s.settlement.neighbourNetwork` is the canonical, always-present location. Also note the unit test fixtures should be updated to put neighbour data under `settlement.neighbourNetwork`, otherwise the test will keep masking this regression. The identical dead-field read in useReaderAudience.js:103 should be fixed in the same pass.

### [MEDIUM] src/components/pricing/FounderTile.jsx:48 — FounderTile imports non-existent `getFounderSeatsRemaining` — live seat counter permanently dead, and checkout errors are swallowed silently

**Evidence:** FounderTile.jsx:48-49 does `const { getFounderSeatsRemaining } = await import('../../lib/founderSeats.js'); const n = await getFounderSeatsRemaining();` but lib/founderSeats.js exports only `fetchFounderSeatsTaken`, `fetchFounderSeatsRemaining`, and `FOUNDER_SEAT_CAP` (founderSeats.js:36,58). The destructured value is undefined, the call throws TypeError, and the enclosing `catch { /* RPC unavailable; stay null */ }` (line 51) hides it forever. Consequences: `seatsRemaining` is always null, so the "N of 500 seats remaining" line (line 120-127) and the personalized "Claim seat N" CTA (lines 77, 162) never render, and the scarcity eligibility check `seatsRemaining <= 0` (line 62) can never retire the tile when seats sell out — the tile keeps selling a $99 lifetime SKU after the 500-seat cap is exhausted (the edge function is the only backstop). PricingPage.jsx:246 uses the correct name `fetchFounderSeatsRemaining`, confirming this is a one-file drift, and tests/lib/founderSeats.test.js pins the `fetch*` names. Because the mismatch is inside a dynamic import destructure, Vite's unresolved-named-import onwarn promotion cannot catch it. Additionally, `handleClick` (lines 79-89) catches `startCheckout` failures with only `console.warn` — an anonymous worldbuilder (eligibility never checks tier!=='anon') clicks "Claim a Founder seat" and startCheckout throws 'You must be signed in to purchase', producing zero user-visible feedback: a silently stranded purchase entry point.

**Fix:** Import `fetchFounderSeatsRemaining` (the real export) and surface checkout failures in the tile (error line + optionally route anonymous users into the auth flow with the existing INTENTS.CLAIM_FOUNDER auth-intent before checkout).

**Fix note:** Reviewer's fix is correct. Primary: change line 48 to `const { fetchFounderSeatsRemaining } = await import('../../lib/founderSeats.js');` and call it at line 49 (or simply use a static top-level import — the module already only depends on supabase, no need for the dynamic import). Secondary: in handleClick, surface startCheckout failures (an error line in the tile) instead of console.warn-only. For the anon case, either add `tier !== 'anon'` to the eligibility check (line 58-62) so the tile doesn't show a purchase CTA the anon user cannot complete, or route anon clicks through the auth flow first using the existing CLAIM_FOUNDER auth-intent before startCheckout. Worth also adding a render-level test of FounderTile with the flag forced on (the bug survived because no test exercises the live tile with founderRecognition=true — tests/ui/mobile.smoke.test.jsx:60 mocks fetchFounderSeatsRemaining but PricingPage, not the tile).

### [MEDIUM] src/hooks/useGalleryPageState.js:115 — Gallery detail ignores browser Back: navigating /gallery/:slug → /gallery leaves the dossier open while the URL shows the list

**Evidence:** GalleryPage renders the detail view whenever `activeSlug` is truthy (GalleryPage.jsx:44). `activeSlug` is set by openDossier and cleared only by the in-app backToList button (line 143-150). The route-sync effect (lines 115-124) handles routeSlug becoming truthy (opens the dossier) but when routeSlug transitions slug → null (browser Back from /gallery/:slug to /gallery), it falls through to the legacy `?slug=` check and does nothing — `activeSlug`/`dossier` are never cleared. Result: after pressing Back, the URL is /gallery but the detail view stays mounted; Back appears broken and the list is unreachable except via the in-app button.

**Fix:** In the routeSlug effect, when `routeSlug` is null (and no legacy ?slug param), clear the detail state: setActiveSlug(null); setDossier(null); setDossierError(null).

**Fix note:** The suggested fix is correct and complete: in the routeSlug effect, add an explicit clear when routeSlug is falsy and no legacy ?slug param is present — setActiveSlug(null); setDossier(null); setDossierError(null) (and for full parity with backToList, also clear actionError/actionNotice). Note routeSlug arrives as undefined (not null) on the /gallery exact route, so guard on falsiness rather than strict === null. One caveat to avoid a regression: the legacy ?slug branch must still take precedence, so structure it as: if (routeSlug) {open} else if (legacy ?slug) {open} else {clear} — clearing only in the final else so a legacy ?slug deep-link is not wiped.

### [MEDIUM] src/hooks/useGalleryPageState.js:126 — Gallery list fetch state machine: query changes never set loading, and an in-flight loadMore can append stale-query pages onto a fresh result set

**Evidence:** Two related defects in the same state machine. (1) The query effect (useGalleryPageState.js:55-77) never calls `setListLoading(true)` when sort/search/filters change — listLoading is only true from its useState initializer. After first load, changing a filter or typing in search shows no loading state (GalleryTopbar.jsx:71 keeps showing the stale total, GalleryList's Load-more button stays enabled because `disabled={listLoading}` is false). (2) `loadMore` (lines 126-141) has no cancellation token and appends unconditionally: `setItems(prev => [...prev, ...res.items])`, then sets hasMore/page from its own (possibly stale) query. Sequence: click 'Load more' (slow request for page 1 of query A) → change sort/filter → effect resolves and replaces items with page 0 of query B → the stale loadMore resolves and appends query-A page-1 rows to the query-B list, and overwrites hasMore/total with query-A values. Result: a visibly mixed/duplicated list (duplicate `key={item.slug}` rows if the same dossier appears in both pages → React key collision warnings) and wrong pagination state. The page-0 effect protects itself with a `cancelled` flag; loadMore got no equivalent.

**Fix:** Set listLoading(true) at the start of the query effect; give loadMore a generation/abort token tied to galleryQuery (bail out of all setState calls if the query changed since the request started), and/or dedupe appended items by slug.

**Fix note:** Suggested fix is sound. Concretely: (a) call setListLoading(true) as the first statement inside the query effect (line 56-area) so query changes show the loading state; (b) capture a generation token tied to galleryQuery — e.g. reuse a ref/counter incremented per query, or capture `const q = galleryQuery` at loadMore start and bail from all setItems/setHasMore/setPage/setTotal calls if galleryQuery has changed by resolve time. Slug-dedup on append is a useful defense-in-depth but does not fix the stale hasMore/page/total overwrite, so the generation token is the primary fix. Severity medium is correct: secondary (community gallery) feature, UX/state-consistency rather than data loss/money/security/determinism. Note defect (1) (missing loading indicator) reproduces on every post-first query change and is the more reliably observable of the two; the mixed-list race needs a slow request plus the interleaved interaction.

### [MEDIUM] src/hooks/useReaderAudience.js:101 — useReaderAudience worldbuilder signals are permanently false: reads save.neighbourLinks and save.locks, fields no save ever has

**Evidence:** `hasUsedNeighbours` checks `Array.isArray(x.neighbourLinks) && x.neighbourLinks.length > 0` (lines 101-104) and `hasUsedLocks` checks `Array.isArray(x.locks)` (lines 105-108). Grep shows `neighbourLinks` is never written anywhere in the codebase — neighbour data lives at `save.settlement.neighbourNetwork` (every other consumer falls back to it, e.g. domain/region/graph.js:261-266, deriveSystemState.js:226). Locks are persisted at `save.campaignState.locks` and are an OBJECT, not an array (lib/saves.js:100, campaignSlice.js:185), so even at the right path Array.isArray would fail. Both behavior signals are therefore always false and 'worldbuilder' is only reachable via exportCount >= 3 (line 61) — audience-aware copy (useCopy) misclassifies exactly the multi-settlement campaign users it was built for.

**Fix:** Read `x.settlement?.neighbourNetwork?.length > 0` for neighbours and `Object.keys(x.campaignState?.locks || {}).length > 0` for locks (and keep computeReaderAudience's signal extraction in one tested helper).

**Fix note:** Suggested fix is correct. Read neighbours via the same accessor everyone else uses: `(x.settlement?.neighbourNetwork || x.settlement?.neighbourLinks || []).length > 0` (mirror settlementFromSave + graph.js:263-266). Read locks as an object on campaignState: `Object.keys(x.campaignState?.locks || {}).length > 0`. Note there is a sibling instance of the same bug at src/components/library/LibraryToolbar.jsx:77 (`Array.isArray(s.neighbourLinks)` on a save envelope) — its hasNeighbours filter likely never matches either; worth folding the neighbour-presence check into one shared, tested helper used by both the hook and the toolbar.

### [LOW] src/components/CompendiumPanel.jsx:944 — Compendium stressor 'affects' pills render danger-on-danger — text is invisible

**Evidence:** The saved-item card's affects pills (CompendiumPanel.jsx:942-948) set `color: swatch.danger, background: swatch.danger` — identical token for foreground and background, so the channel labels ('economy', 'safety', …) render as a solid red lozenge with unreadable text. Every other pill in this file uses a translucent background derived from the accent (`background: `${accent}14`` in renderPills at line 798, `${color}18` in Tag at line 22), and the adjacent border `'1px solid #8b1a1a44'` confirms swatch.danger is the saturated #8b1a1a-family red used as a text color elsewhere (e.g. AccountPage pairs `color: swatch.danger` with `background: swatch.dangerBg`). This is the read-only display of the same data the renderPills edit control shows correctly, so the saved stressor card contradicts the edit form.

**Fix:** Use a translucent or paired token for the background, e.g. `background: swatch.dangerBg` or `background: `${swatch.danger}14``, keeping `color: swatch.danger`.

**Fix note:** The suggested fix is correct and matches codebase convention: keep `color: swatch.danger` and change the background to a light/translucent token — `background: swatch.dangerBg` or `background: `${swatch.danger}14``. Note the same defect also exists at InstitutionalGrid.jsx:308 and :328 (color: swatch.danger on background: swatch.danger) if a broader cleanup is desired, though those are out of scope for this specific finding.

### [LOW] src/components/ConfigurationPanel.jsx:334 — ConfigurationPanel display-only coercion of blocked tier/route combos lies about what will generate

**Evidence:** When `blockTownPlus` (noMagic && isolated) the Population select displays 'village' (`value={blockTownPlus && isTownPlus ? 'village' : config.settType}`, line 334) and the Trade Route select displays 'road' (line 357) — but neither writes the coerced value back to config; the onChange handlers only block re-selecting the hidden options. The blocked state is reachable: SettlementsPanel.onLoad → migrateConfig loads old saves verbatim, and migrateConfig (SettlementsPanel.jsx:32-38) infers `magicExists = priorityMagic > 0`, so a legacy save with priorityMagic 0 + settType 'town' + tradeRouteAccess 'isolated' lands in exactly this state. Generate then runs with the hidden town+isolated+no-magic config while the UI shows village/road — the 'impossible' combination the panel exists to prevent.

**Fix:** On detecting the blocked combo (e.g. in an effect or in updateConfig/migrateConfig), actually write the coercion into config (settType→'village' or tradeRouteAccess→'road') instead of only displaying it.

**Fix note:** No fix needed for the claimed harm. If the cosmetic config/display dissonance is bothersome, the cleanest place to normalize is migrateConfig (SettlementsPanel.jsx:32) -- write settType/tradeRouteAccess to the coerced values on load -- but this is optional polish, not a correctness fix, since resolveConfig.js:119 already guarantees coherent generation.

### [LOW] src/components/OutputContainer.jsx:419 — renderTab passes narrativeNote: null to every tab — the NarrativeNote per-tab component is dead in the dossier

**Evidence:** All renderTab cases pass `narrativeNote: null` (OutputContainer.jsx:419-432), so the NarrativeNote card every tab mounts (e.g. OverviewTab.jsx:76, EconomicsTab.jsx:213) can never render — `if (!note) return null` (NarrativeNote.jsx:18). The per-tab narrative lens was reimplemented as the banner block inside OutputContainer (lines 783-820, reading `nsrc.narrativeNotes?.[selectedTab]`), but the old prop plumbing through 10+ tabs was left wired to a constant null. Harmless at runtime but a misleading seam: anyone editing a tab reasonably assumes the prop carries data.

**Fix:** Remove the narrativeNote prop and NarrativeNote mounts from the tabs (or actually thread the per-tab note if in-tab placement is preferred over the banner).

**Fix note:** Both proposed remedies are valid. Note that if the team prefers the in-tab placement over the banner, threading the note is straightforward: pass `narrativeNote: (showNarrative && aiSettlement ? aiSettlement : (publicDossier ? rawSettlement : null))?.narrativeNotes?.[selectedTab] ?? null` per case, but then the banner's NOTE_TABS branch (lines 792-816) must be removed to avoid double-rendering the same note. Simpler/safer fix given the banner is the chosen home: delete the narrativeNote prop plus the NarrativeNote import and mount from each of the ~8 tabs. RelationshipsTab needs no change (it ignores the prop).

### [LOW] src/components/PricingPage.jsx:371 — PricingPage tier-card status bugs: Wanderer card shows 'Redirecting…' during Cartographer checkout and shows 'Current plan' to paying subscribers

**Evidence:** Two defects on the tier cards. (1) Line 371: `loading={loading === (tier.key === 'founder' ? 'founder_lifetime' : 'premium')}` — for the WANDERER card this evaluates `loading === 'premium'`, so the moment a user clicks the Cartographer buy button (setLoading('premium'), line 259), the free Wanderer card's CTA also flips to 'Redirecting…' (TierCard line 162) — two cards claim a checkout redirect is happening for one purchase. (2) Line 284: the Wanderer CTA label is `authTier === 'anon' ? t('pricing.tiers.wanderer.cta') : 'Current plan'` — every signed-in user including premium/Cartographer subscribers and founders sees 'Current plan' on the FREE tier card, while the Cartographer card simultaneously shows 'Manage subscription' (line 297). A paying user is told the free tier is their current plan.

**Fix:** Map each tier to its own product key for the loading comparison (wanderer → never loading), and only label Wanderer 'Current plan' when `authTier === 'free'` (non-elevated, non-founder).

**Fix note:** The suggested fix is correct. Two refinements: (1) For the loading prop, map each tier.key to its own product key — wanderer has no purchase product so it should never be in a loading state (e.g. loading === (tier.key==='founder' ? 'founder_lifetime' : tier.key==='cartographer' ? 'premium' : null), which for wanderer compares loading === null and is false while a real product is in flight). (2) For the Wanderer label, gate 'Current plan' on the user actually being on the free tier and not elevated/founder: `authTier === 'free' && !isElevated && !isFounder ? 'Current plan' : t('pricing.tiers.wanderer.cta')` (or similar) — this also keeps premium/founder/elevated users from being told free is their plan. Note authTier values are 'anon'|'free'|'premium' (authSlice.js:64), so checking `=== 'free'` is the right positive condition rather than negating 'anon'.

### [LOW] src/components/WorldMap.jsx:513 — Regenerate-world confirm reads non-existent `mapPlacements`/`mapLabels` store keys — loss counts always zero, P112/M-7 warning never shows

**Evidence:** handleRegenerate (WorldMap.jsx:508-522) under flag('mapAutosave') computes `const placedCount = (useStore.getState().mapPlacements || []).length; const labelCount = (useStore.getState().mapLabels || []).length;`. The map slice stores these as `mapState.placements` (an OBJECT keyed by burgId — mapSlice.js:94,222) and `mapState.labels` (array); `mapPlacements` and `mapLabels` exist nowhere in the store (repo-wide grep: only this call site). Both reads are always undefined → counts always 0 → the explicit "You'll lose N placements and M labels. This cannot be undone." confirm body never renders; users always get the generic line even with a fully-populated map. Note even the intended key would be wrong for placements: `.length` on the placements object is undefined — it needs `Object.keys(...).length` (as done correctly at line 453 and in mapDirtyKey at line 158).

**Fix:** Read `Object.keys(useStore.getState().mapState.placements || {}).length` and `(useStore.getState().mapState.labels || []).length`.

**Fix note:** Suggested fix is correct. Replace lines 513-514 with: `const placedCount = Object.keys(useStore.getState().mapState.placements || {}).length;` and `const labelCount = (useStore.getState().mapState.labels || []).length;`. Note placements needs Object.keys(...).length (object), labels uses .length (array) — matching the correct usage at WorldMap.jsx:453.

### [LOW] src/components/WorldMap.jsx:663 — Corrupted copy family: em-dash separators degraded to ', ' (and one dropped entirely) across user-visible strings

**Evidence:** A consistent corruption where an em-dash separator became ', ' or vanished, producing visibly broken UI strings: (1) WorldMap.jsx:663 `<option value="">, No campaign</option>` — the campaign dropdown's default option reads ', No campaign'. (2) BuyThisDossier.jsx:87 `` `Buy this dossier${SINGLE_DOSSIER.priceLabel}` `` renders the purchase CTA as 'Buy this dossier$2.99' with no separator (the title attr at line 72 shows the intended phrasing). (3) CompendiumPanel.jsx:175 `{s.description||s.desc||', '}` shows a bare ', ' for stress types with no description. (4) map/PlacementDetailCard.jsx:47 and map/QuickInspector.jsx:56 both fall back to `|| ', '` for a missing tier, so the card shows ', ' as the tier line. (5) primitives/LockedDestination.jsx:61 default `ctaLabel = 'Upgrade, $6/mo'` while its own JSDoc at line 49 documents the default as `'Upgrade — $6/mo'` — direct proof the original glyph was an em-dash. Same family also at GenerateWizard.jsx:542,706 (`{', '}` prefixing copy).

**Fix:** Restore the em-dash (or a sensible separator/empty string) at each site: '— No campaign', 'Buy this dossier — $2.99', '' or '—' for missing description/tier fallbacks, 'Upgrade — $6/mo'.

**Fix note:** Suggested fix is correct. Recommended replacements: WorldMap.jsx:663 -> "— No campaign" (keep value=""); BuyThisDossier.jsx:87 -> `Buy this dossier — ${SINGLE_DOSSIER.priceLabel}`; CompendiumPanel.jsx:175 -> '' (empty string) rather than '—', since a bare em-dash for a missing description is also odd; PlacementDetailCard.jsx:47 and QuickInspector.jsx:56 -> '' or '—' for the missing-tier fallback (empty preferred so the line just collapses); LockedDestination.jsx:61 -> 'Upgrade — $6/mo' to match its JSDoc; GenerateWizard.jsx:542 and :706 -> {' — '} so the strong label and following copy read "Basic Generate — Set the foundations…" / "Step N: label — hint".

### [LOW] src/components/map/WorldPulsePanel.jsx:381 — WorldPulsePanel self-defeats its own null-guard: reads `worldState.pulseHistory.length` on a possibly-undefined array

**Evidence:** Line 381: `const latestPulse = (worldState.pulseHistory || [])[worldState.pulseHistory.length - 1] || null;`. The `|| []` fallback proves the author expects `pulseHistory` may be missing (line 379 likewise defends `campaign.worldState || {}`), but the INDEX expression dereferences the original `worldState.pulseHistory.length` — if pulseHistory is undefined this throws TypeError and white-screens the World Pulse workspace. This executes before the `!worldState.canonizedAt` early-return (line 420), so it runs for draft worlds too. In practice campaignSlice.migrateCampaign → ensureWorldState (campaignSlice.js:246, worldState.js:63) backfills `pulseHistory: []` for campaigns that flow through the slice, so the crash needs a campaign object that bypassed migration (e.g. a worldState-less record handed to the panel directly) — hence latent rather than live; but as written the guard is dead code and the panel's defensive contract is broken.

**Fix:** `const history = worldState.pulseHistory || []; const latestPulse = history[history.length - 1] || null;` (or `.at(-1)`).

**Fix note:** Suggested fix is correct. `const history = worldState.pulseHistory || []; const latestPulse = history[history.length - 1] || null;` (or `history.at(-1) || null`). Note `.at(-1)` on an empty array returns undefined, so keep the `|| null` for parity with current behavior.

### [LOW] src/components/new/tabs/PowerTab.jsx:153 — Systemic stripped-glyph copy artifacts across core components: ', ' placeholder fallbacks, missing separators, empty icon strings

**Evidence:** A bulk character-stripping pass left visible artifacts on user-facing strings: PowerTab section title `Power Distribution${pf.length} Factions` renders 'Power Distribution4 Factions' (PowerTab.jsx:153); EconomicsTab `Shadow Economy${bmc}% capture` renders 'Shadow Economy12% capture' (EconomicsTab.jsx:514); DefenseTab 'Criminal Structure{csd.label}' concatenates with no separator (DefenseTab.jsx:215); OverviewTab StatusTag renders the literal string ', ' as the missing-value placeholder (`value || ', '`, OverviewTab.jsx:38, also SummaryTab.jsx:204-205 and DailyLifeTab AnchorFact:25); ConfigurationPanel archetype dropdown shows '<option>, Choose an archetype' (ConfigurationPanel.jsx:81); GenerateWizard banners join headline and body with a bare `{', '}` (GenerateWizard.jsx:543, 706); HistoryTab TENSION_META and DefenseTab STRESS_STATUS icons are all empty strings producing stray gaps. Individually cosmetic, but they appear on the dossier (the flagship surface) and read as data corruption.

**Fix:** Sweep for the artifacts of the strip pass: replace `|| ', '` fallbacks with an em dash or 'Unknown', restore separators in template-literal titles ('Power Distribution · N Factions'), and remove empty icon slots or substitute lucide icons.

**Fix note:** The cited file paths are slightly off: SummaryTab/ConfigurationPanel/GenerateWizard live in src/components/ (and src/components/new/SummaryTab.jsx), not src/components/new/tabs/. Line numbers match. The correct restoration target per git is the original em-dash form, e.g. `Power Distribution — ${pf.length} Factions`, `Shadow Economy — ${bmc}% capture`, `Criminal Structure — {csd.label}`, `<option>— Choose an archetype —</option>`, `{' — '}{currentStepDef.hint}`. For the `|| ', '` fallbacks (OverviewTab:38/122, SummaryTab:204-205, DailyLifeTab:25) use `|| '—'`. Drop the "STRESS_STATUS icons produce stray gaps" item — that icon field is never rendered. For HistoryTab TENSION_META the empty icon span does produce a real gap; either remove the `<span>` when icon is empty or restore icons. A repo-wide regex sweep for `'—'`/`' — '` losses (cross-referencing git e608542) would catch any remaining un-restored instances beyond the ten cited.

### [LOW] src/components/primitives/Dialog.jsx:39 — Shared Dialog primitive (and AuthModal) declare aria-modal but implement no focus management — background stays tabbable

**Evidence:** primitives/Dialog.jsx Shell (lines 22-119) renders `role="dialog" aria-modal="true"` and handles Escape, but: focus is never moved into the dialog on open (except TextInputDialog's input autoFocus), there is no focus trap (Tab walks the obscured page behind the overlay, contradicting aria-modal which tells AT the background is inert), and focus is not restored to the trigger on close. ConfirmDialog is used for destructive confirms (WorldMap regenerate, GenerateWizard unsaved-exit), where a keyboard user's focus remains on the now-obscured page — pressing Enter can activate a hidden control instead of Cancel/Confirm. The same gap exists in the hand-rolled AuthModal.jsx (lines 44-63: overlay + role=dialog + aria-modal, no Escape handler, no focus move/trap/restore) and ChroniclePanel's FullEntryModal (lines 116-125). The project's IconButton primitive documents WCAG-driven rigor ('audit found we strip focus outlines…'), so dialog focus management is an acknowledged requirement the primitive doesn't meet.

**Fix:** In Shell: on open, save document.activeElement, focus the dialog (or first focusable), trap Tab/Shift+Tab within the section, and restore focus on close. Reuse the same Shell for AuthModal or add Escape + the same focus management there.

**Fix note:** The reviewer's fix is sound. One correction to the evidence: ChroniclePanel's FullEntryModal (lines 116-125) does NOT actually declare aria-modal (it omits it), so unlike Shell and AuthModal it doesn't have the aria-modal-vs-tabbable contradiction; its only defect is the missing Escape handler and missing focus management. Best fix is to add focus management once in Shell (save activeElement on open, focus the dialog section or its first focusable, trap Tab/Shift+Tab within the section, restore focus on close) and route AuthModal and FullEntryModal through the same Shell (or replicate the same logic plus an Escape handler for AuthModal, which currently has none).

### [LOW] src/hooks/useReaderAudience.js:105 — useReaderAudience 'hasUsedLocks' signal is structurally dead — saves never have a root-level `locks` array

**Evidence:** Lines 105-108 compute `hasUsedLocks` as `saves.some(x => Array.isArray(x.locks) && x.locks.length > 0)`. Save records are `{ ...settlement, savedAt, id, campaignState }` (settlementSlice.js:930-936); lock state is persisted only as `campaignState.locks`, and it's an OBJECT, not an array (`locks: state.locks ? { ...state.locks } : {}` — settlementSlice.js:195). The settlement schema defines no top-level `locks` field (grep of settlement.schema.js). So `x.locks` is always undefined and the check is permanently false — and even pointed at the right path, Array.isArray would still fail on the object shape. Consequence: one of the three worldbuilder-detection signals documented in the hook header ('regenerate-with-locks usage') never fires, narrowing who sees worldbuilder-gated surfaces (FounderTile recognition, audience-led pricing copy).

**Fix:** Check the real shape: `saves.some(x => x.campaignState?.locks && Object.values(x.campaignState.locks).some(Boolean))`.

**Fix note:** Suggested fix is shape-correct. `Object.values(x.campaignState.locks).some(Boolean)` handles both boolean and array lock values (non-empty array and `true` are both truthy). Slightly cleaner alternative, since setLock (settlementSlice.js:1178-1184) deletes falsy/empty entries so the object only ever holds active locks: `saves.some(x => Object.keys(x.campaignState?.locks || {}).length > 0)`. Note a parallel latent issue worth a separate look: `hasUsedNeighbours` (useReaderAudience.js:101-103) checks `x.neighbourLinks`, and grep found no `neighbourLinks` field in settlement.schema.js either — it may also be reading the wrong/absent path, though that is outside the cited finding.

### [LOW] src/hooks/useReturnVisit.js:75 — useReturnVisit ranks saves with Number(ISO timestamp) — NaN comparator makes the "welcome back" settlement arbitrary

**Evidence:** The lastSettlement ranking (useReturnVisit.js:74-78) does `const aTs = Number(a.campaignState?.editedAt || a.savedAt || 0)`. Every save record gets `campaignState.editedAt = new Date().toISOString()` via pickleCampaignState (store/settlementSlice.js:197), so the `||` always selects the ISO STRING and `Number('2026-06-12T…')` is NaN for every entry. The comparator `bTs - aTs` returns NaN, which Array.sort treats as 'keep order', so `ranked[0]` is just the first element of savedSettlements (push-appended, i.e. typically the OLDEST save — settlementSlice.js:930-936), not the most-recently-touched one. The return-visit hero (X-9 'surface the prior settlement') therefore greets users with the wrong settlement whenever they have more than one save. (`savedAt` itself is numeric — saves.js:142,265 — so the fallback would have worked; the editedAt branch poisons it.)

**Fix:** Parse both shapes: `const aTs = Date.parse(a.campaignState?.editedAt) || Number(a.savedAt) || 0;` (Date.parse handles ISO, Number handles epoch millis).

**Fix note:** The sort comparator is genuinely a no-op (NaN comparison preserves order), which is worth tidying defensively, but it is not currently user-visible because savedSettlements is already hydrated newest-first by savesService.list() (Supabase ORDER BY updated_at DESC at src/lib/saves.js:132; local-mode unshift at saves.js:265/317). The reviewer's claim that ranked[0] yields the OLDEST save is incorrect — it relies on the store's saveSettlement push method (settlementSlice.js:930) which is dead code (no call sites). If the sort is fixed for robustness, note that Supabase-hydrated records carry campaignState=row.campaign_state (saves.js:156) whose editedAt may diverge from updated_at, and savedAt is the numeric updated_at mirror (saves.js:142); the suggested Date.parse(editedAt) || Number(savedAt) || 0 is a reasonable hardening but would change which timestamp drives ranking vs. the current incidental updated_at ordering.


## 07-domain (16)

### [HIGH] src/domain/events/mutate.js:1384 — findFaction searches the wrong faction list — faction-targeted events silently no-op on every generated settlement

**Evidence:** findFaction reads `const list = s.factions || s.powerStructure?.factions || []` (mutate.js:1383-1392). Generated settlements ALWAYS carry a top-level `factions` array — assembleSettlement.js sets `factions` to the NPC social-cluster output of powerGenerator.generateFactions ({name, members, dominantCategory} entries like 'The Tanners Circle'), while the real power factions ({faction:'Merchant Guild', power, ...}) live in `powerStructure.factions`. Since even an empty array is truthy, findFaction NEVER reaches powerStructure.factions on a generated settlement. Every other layer prefers powerStructure first: EventComposer offers targets from `settlement.powerStructure?.factions` (EventComposer.jsx:115), batch validation builds its namespace from `s?.powerStructure?.factions || s?.factions` (batch.js:307), propagate.js factionsList (line 278), factionResponses.js:32 and factionProfile.js:263-266 all do the same. So IMPAIR_FACTION / RESTORE_FACTION / EXPOSE_CORRUPTION (faction fallback) / swapNpcStanding's stable-id stamp pass batch validation, land their authored systemState deltas + narration in the canon timeline, fire the party-impact world ripple (partyEventLinkage maps IMPAIR_FACTION → undermine_faction), yet the local faction entity is never impaired — no impairment record, no propagation, no faction-response visibility. Empirically reproduced: mutateSettlement({factions:[social cluster], powerStructure:{factions:[{faction:'Merchant Guild'}]}}, IMPAIR_FACTION 'Merchant Guild') leaves powerStructure.factions[0].impairments undefined; same with `factions: []`. Tests pass only because the fixture (tests/domain/events.test.js:12-31) has no top-level factions array. Note addFaction (mutate.js:372) uses the OPPOSITE precedence (psFactions first), so an ADD_FACTION followed by IMPAIR_FACTION of that same faction also no-ops.

**Fix:** Make findFaction search powerStructure.factions first (matching every reader and replaceFaction's write preference), or search both lists: `[...(s.powerStructure?.factions||[]), ...(s.factions||[])]`. Add a test fixture carrying both lists, as every generated settlement does.

**Fix note:** Suggested fix is correct. Preferred form matches every other reader and replaceFaction's write target: search powerStructure.factions first, then the top-level list, e.g. `const list = (s.powerStructure?.factions?.length ? s.powerStructure.factions : null) || s.factions || s.powerStructure?.factions || []`. Cleaner and more robust against ID collisions across the two lists is to search the union: `const list = [...(s.powerStructure?.factions||[]), ...(s.factions||[])]` — this also matters because replaceFaction normalizes on powerStructure, so findFaction must return an object that is === an element of the list replaceFaction will scan; with a union, a match found in powerStructure.factions will be correctly replaced there, while a top-level social-cluster match falls back to replaceFaction's second branch (s.factions). Add a regression fixture carrying BOTH a top-level `factions` array (social clusters with `name` only) and `powerStructure.factions` (with `faction:` keys), then assert IMPAIR_FACTION of a powerStructure-only faction writes an impairment to powerStructure.factions[i].impairments. Minor: the reviewer's one-line `[...ps, ...flat]` union is the safest choice given replaceFaction's identity-based (===) matching.

### [HIGH] src/domain/summary/tonightAtTheTable.js:45 — Summary tab 'Tonight at the Table' renders '[object Object]' for NPC secrets

**Evidence:** tonightAtTheTable builds the NPC card body as `secret: ${truncate(npc.secret, 80)}` (line 45) and the TWIST fallback as `truncate(ranked[2].secret, 120)` (line 87), but the generator emits secret as an OBJECT: npcGenerator.js:93 `secret: { what: secret.secret, stakes: secret.stakes }`. truncate() does String(s) so the card body becomes 'secret: [object Object]'. Verified by execution: generateSettlementPipeline(seed 'probe-1') + tonightAtTheTable() returns entries like {kind:'NPC', title:'Aarav Acharya', body:'mayor · secret: [object Object]'}. This is the live path: OutputContainer.jsx:409 renders SummaryTabV2 (flag summaryMagazineV2 default true, flags.js:120) which calls tonightAtTheTable(settlement) at SummaryTabV2.jsx:90; TableView.jsx:58 (flag tableView default true) does the same. The unit test (tests/domain/tonightAtTheTable.test.js:34) masks this by fabricating string secrets.

**Fix:** Read the secret the way dossier/entityLinks.js:95 already does: `typeof npc.secret === 'string' ? npc.secret : npc.secret?.what`. Apply to both the NPC card trait (line 44-46) and the TWIST fallback (line 87), and add a test with the real object shape.

**Fix note:** The suggested fix is correct and matches the established repo pattern (entityLinks.js:95). Read secret via `const secretText = typeof npc.secret === 'string' ? npc.secret : npc.secret?.what;` and use secretText in both the NPC-card trait (lines 44-46) and the TWIST fallback (line 87, on ranked[2].secret). Minor scope clarification: the line-45 NPC-card occurrence is the primary, near-universal hit; the line-87 TWIST occurrence is conditional — it only fires when settlement.history.legacyAnnotations is empty (in the probe run the annotation branch fired, so TWIST showed body ""). Both should be fixed. Add a test using the real { what, stakes } object shape, since the existing tests only exercise string secrets and would not catch a regression.

### [HIGH] src/domain/summary/tonightAtTheTable.js:56 — 'Tonight at the Table' HOOK cards can never render: reads settlement.plotHooks/settlement.hooks which no generator writes

**Evidence:** Lines 56-69 source hooks from `settlement.plotHooks` falling back to `settlement.hooks`, ranked by `hook.tier` ('A'/'B'/'C') and rendered from `hook.title || hook.headline` / `hook.body || hook.summary`. Neither top-level field exists on any generated settlement: live probe (seed 'probe-1') shows both undefined, and aiLayer.js:202 documents 'settlement.plotHooks is never written — the live hooks are economicViability.plotHooks ({category, hook, severity}) and history.historicalEvents[].plotHooks (strings)'. No producer anywhere emits hooks with tier/title/headline/body fields (the real aggregator, dossier/plotHooks.js collectPlotHooks, emits {text, category, priority}). Result: the 'Top hook' cards — half of the composer's documented purpose (header lines 5-12) — never appear for any real settlement on the Summary tab or TableView. Tests fabricate {tier, title, body} hooks (tonightAtTheTable.test.js:43-47), so the suite passes while the feature is dead.

**Fix:** Source hooks via collectPlotHooks(settlement) from src/domain/dossier/plotHooks.js (or economicViability.plotHooks + history event hooks directly), rank by its numeric `priority`, and render `hook.text`; delete the tier/title/body vocabulary or map it from the real shape.

**Fix note:** The suggested fix is correct in direction: source via collectPlotHooks(settlement) from src/domain/dossier/plotHooks.js, rank by its numeric `priority` (descending — higher priority = more urgent), and render `hook.text` (collectPlotHooks has no title/body split). The tier/title/headline/body vocabulary should be removed. Note the existing test at tonightAtTheTable.test.js:41-51 must be rewritten to use the real {text,category,priority} shape, otherwise it will keep masking regressions. Worth also checking the RED-flag path: settlement.supplyChainState is never present on generated settlements, so that card is likely dead too (out of scope here but a candidate for the same fix pass).

### [HIGH] src/domain/summary/tonightAtTheTable.js:76 — 'Tonight at the Table' TWIST card reads nonexistent fields on legacyAnnotations — always generic title + empty body

**Evidence:** Lines 75-82 read `a.title || a.label` and `a.body || a.summary` from history.legacyAnnotations[0], but the producer emits `{ annotation, eventType, yearsAgo, severity, eventName }` (legacyGenerator.js:206-212, wired in generateNarratives.js:53). None of title/label/body/summary exist, so every real settlement renders a TWIST card titled 'The hidden thread' with an EMPTY body — verified by execution (seed 'probe-1' returns {kind:'TWIST', title:'The hidden thread', body:''}). Because annotations.length > 0 takes the if-branch, the legitimate NPC-secret fallback (line 83) is also permanently suppressed whenever annotations exist. The correct field name is proven by historyBeats.js:175-181, which reads `ann.annotation` / `ann.eventName` from the same array. The test (tonightAtTheTable.test.js:55) fabricates {title, body} annotations, masking the drift.

**Fix:** Read `a.eventName` for the title and `a.annotation` for the body (matching historyBeats.js), e.g. title: a.eventName || 'The hidden thread', body: truncate(a.annotation || '', 120); skip the annotation branch when a.annotation is empty so the NPC-secret fallback can run.

**Fix note:** Reviewer's suggested fix is correct and matches the proven convention in historyBeats.js. Concretely: title: a.eventName || 'The hidden thread', body: truncate(a.annotation || '', 120). To also restore the NPC-secret fallback, guard the branch on a non-empty annotation rather than just length: change `if (annotations.length > 0)` to `if (annotations[0]?.annotation)` (or `const a = annotations[0]; if (a?.annotation) {...} else if (ranked.length > 2 && ranked[2]?.secret) {...}`), so an annotation array of malformed/empty entries falls through to the NPC secret. Updating the test fixtures at tonightAtTheTable.test.js:55,95 to use the real shape {annotation, eventName, ...} is required, otherwise the corrected code would fail the now-wrong assertions.

### [MEDIUM] src/domain/display/publicSafe.js:24 — Public-safe projection strips 'landmarks' from every quarter: bare 'dm' substring in PRIVATE_KEY_RE over-matches

**Evidence:** PRIVATE_KEY_RE includes the bare alternation `dm` with no boundary, so any key CONTAINING 'dm' is dropped — including `lan**dm**arks`. Verified by execution: toPublicSafe(pipeline output) reduces spatialLayout.quarters[] from {name, location, desc, landmarks} to {name, location, desc}. landmarks is legitimately public content rendered by the public dossier path (PublicDossierView.jsx:118 → OutputContainer → OverviewTab.jsx:251, SummaryTab.jsx:326, pdf/sections/Overview.jsx:373), so every gallery/anonymous dossier silently loses its quarter landmarks. The server twin has the identical pattern (supabase/migrations/020_gallery_public_privacy.sql:38 `key ~* '(secret|private|dm|...)'`), so stored public reads are equally affected. The same regex's bare `note` also strips simulation-attribution fields the Economics surfaces use (magicNote, upstreamNote, storageNote — registered dossier consumers in fieldManifest.js:219-247). Fail-closed direction, so no leak — but real public-content loss.

**Fix:** Tighten the `dm` alternation to explicit keys or a boundary form (e.g. `dmNotes|dmCompass|\bdm\b|(^|[^a-z])gm([^a-z]|$)`) in BOTH the client regex and a follow-up SQL migration replacing _gallery_sanitize_public_json; per the file's own rule the denylist may only grow, so keep the explicit dm-prefixed keys while removing the substring over-match, and add a regression test that quarters keep landmarks.

**Fix note:** The suggested fix is sound. Replace the bare `dm` and `gm` substring alternations with boundary/explicit forms in BOTH the client regex (publicSafe.js:24) and a NEW follow-up SQL migration (do not edit 020 in place; add a migration that CREATE OR REPLACEs _gallery_sanitize_public_json with the corrected regex — Postgres regex uses \y / \m\M for word boundaries, e.g. `\m(dm|gm)\M|dmNotes|dmCompass`). Also tighten `note`: it over-matches the public economic-attribution fields magicNote/upstreamNote/storageNote, so it should be narrowed to the actually-private keys (e.g. dossierNotes|dmNotes|narrativeNotes|notes) or use a boundary, otherwise the fix leaves the economics notes still stripped. Be careful: toPublicSafe relies on the `note` match to strip top-level notes/dossierNotes in default mode (publicSafe.js comments at 76-80 note full-mode strips them explicitly, but default mode leans on the recursive `note` denylist) — so when narrowing `note`, keep the genuinely-private note keys covered. Add regression tests asserting quarters keep landmarks and activeChains keep magicNote/upstreamNote/storageNote, while dmNotes/dossierNotes/narrativeNotes are still removed, in both publicSafe.test.js and the SQL/contract path.

### [MEDIUM] src/domain/entities/npcs.js:250 — createNpc uses Math.random() for NPC ids inside the pure event pipeline

**Evidence:** createNpc builds ids as `npc.${slug}_${shortRand()}` where shortRand() = `Math.random().toString(36).slice(2,7)` (npcs.js:87, 249-251). It runs inside mutateSettlement (ADD_NPC at mutate.js:634, ASSIGN_NPC_TO_ROLE at mutate.js:684 — assignNpcToRole calls createNpc again at npcs.js:185), which executes in runEventPipeline — documented 'Pure function. Pure; idempotent' (eventPipeline.js:34, 123) and run once for preview and once for apply. So the previewed nextSettlement and the applied one carry different NPC ids, two replays of the same event log produce different settlements (violating the project rule that unseeded global RNG in engine code is a bug), and batch.js explicitly works around it ('NPC ids carry a random suffix at apply time, so only the name is registered', batch.js:52-54). The codebase already has the correct pattern in this same file's neighborhood: exposeCorruptNpc seeds a PRNG from the event id (mutate.js:811 `createPRNG(`successor:${event.id}:...`)`).

**Fix:** Derive the suffix deterministically from available identity — e.g. shortHash(`${event.id}:${name}`) or a createPRNG(`npc:${event.id}`) draw threaded from the mutation handlers — and drop shortRand().

**Fix note:** The suggested fix is sound and matches the existing in-file seeded pattern (createPRNG(`successor:${event.id}:...`) at mutate.js:811). Thread a deterministic suffix: derive it from event identity at the mutation handlers — e.g. addNpc/assignNpcMutation pass an explicit id like `npc.${slug}_${shortHash(`${event.id}:${name}`)}` or seed createPRNG(`npc:${event.id}:${name}`) — and drop shortRand(). Caveat: if a single event can create multiple NPCs in one call, the seed must include a per-NPC discriminator (index or name) to avoid id collisions. Also revisit the batch.js:52-54 workaround afterward: once NPC ids are deterministic, eventProduces (batch.js:75) could register the id instead of only the name, but that is a separate optional improvement, not required for correctness. Severity should be low rather than medium since neither the preview/apply parity break nor the replay divergence actually occurs in the current code paths.

### [MEDIUM] src/domain/events/batch.js:244 — Batch preview computes authored stateDeltas from the already-mutated settlement — RESOLVE_STRESSOR severity drifts from what apply delivers

**Evidence:** applyEventBatch mutates first (`working = mutateSettlement({settlement: working, event, now})`, line 238) and then calls `spec.stateDeltas(event, working)` (line 244) on the post-mutation settlement. The single-event pipeline calls `spec.stateDeltas(event, beforeSettlement)` (eventPipeline.js:184), and registry.js's RESOLVE_STRESSOR spec explicitly depends on that: 'the registry computes from the BEFORE settlement, so the entry is still present here' (registry.js:656-668) — it reads the removed stress entry's recorded severity. In the batch path the entry has already been removed by the event's own mutation, so the lookup misses and severity falls back to 0.5. Reproduced: a 0.9-severity famine yields summedStateDeltas resilience +6/volatility -6/resourcePressure -6 in the batch path vs +11/-11/-11 from the single-event authored deltas. The store previews batches via this function (settlementSlice.js:1434-1442 previewEventBatch → computeEventBatch) but APPLIES via the per-event applyEvent pipeline (settlementSlice.js:1463-1480), so the batch preview the DM confirms disagrees with what is committed — the exact drift class Phase 18 was built to eliminate.

**Fix:** Capture the pre-mutation settlement per event (`const before = working;`) before threading the mutation, and call `spec.stateDeltas(event, before)` — mirroring eventPipeline's contract.

**Fix note:** Suggested fix is correct and matches eventPipeline's contract: capture the pre-mutation settlement per event (`const before = working;`) before `working = mutateSettlement(...)`, then call `spec.stateDeltas(event, before)`. Note also that narrate is already correctly passed `beforeSettlement` (batch.js:251), so only the stateDeltas call needs the change. Minor accuracy caveat for the report: the exact reproduced deltas (+11/-11/-11) require severity 0.9; the EventComposer slider caps at 0.85 (severe → +10), though generated/authored stressors can carry higher numeric severities — the drift exists at any severity != 0.5, the magnitude just scales.

### [MEDIUM] src/domain/events/mutate.js:597 — Undo never reverts neighbour-relationship events; the _relationshipEventId stamp is written but read nowhere

**Evidence:** setNeighbourRelationship (SETTLEMENT_DISPUTE / BROKERED_ALLIANCE / OPENED_TRADE_ROUTE) overwrites link.relationshipType/displayRelationshipType and stamps `_relationshipEventId: event.id` (mutate.js:582-601). A repo-wide grep shows that stamp has exactly one occurrence — the write site; no scrub reads it. undoLastEvent (store/settlementSlice.js:1500-1524) strips causeEventId impairments and calls scrubUndoneEvent, which only handles conditions, stress entries, config annotation ledgers, destruction stamps, and snapshotted records (undoEvent.js:302-312); SNAPSHOT_CONFIG_KEYS (undoEvent.js:53-68) has no entry for these event types. So undoing a BROKERED_ALLIANCE restores systemState to beforeState but leaves the neighbour permanently 'allied' — and the prior relationshipType was overwritten, so it is unrecoverable without a pre-event snapshot. The orphaned provenance stamp strongly suggests undo support that was never wired. undoEvent.js's header claims 'everything mutate writes WITH provenance ... is scrubbed by that provenance', which this violates.

**Fix:** Add these three event types to the snapshot family (snapshot the matched neighbourNetwork link pre-event onto logEntry.undo and restore it in restoreSnapshottedRecords), or scrub by the existing _relationshipEventId stamp with a stored previousRelationshipType.

**Fix note:** Suggested fix is sound. Cleanest path given the existing machinery: add the three event types to the snapshot family. SNAPSHOT_CONFIG_KEYS keys off config-level record keys, but neighbourNetwork is a top-level settlement array, so captureEventUndoSnapshot/restoreSnapshottedRecords would need a small extension to snapshot the matched neighbourNetwork link(s) pre-event onto logEntry.undo and restore them (delete-vs-restore semantics like snapshotKeys). Alternatively, scrub by the existing `_relationshipEventId` stamp, but that requires also storing previousRelationshipType/previousDisplayRelationshipType at write time (mutate.js:597) since the current overwrite destroys them. Note the same overwrite-class limitation called out elsewhere in undoEvent.js applies: two relationship events against the same neighbour, then one undo, can only restore the immediately-prior snapshot, not an arbitrarily older one — acceptable and consistent with the file's documented conventions.

### [MEDIUM] src/domain/events/mutate.js:801 — EXPOSE_CORRUPTION (NPC path) stamps synthetic causeEventIds and irreversibly swaps in a successor — undo cannot scrub it

**Evidence:** exposeCorruptNpc (mutate.js:801-820) routes through applyCorruptionImpairments, which stamps impairments with `causeEventId: `corruption:${e.npcId}:${e.kind}`` (corruptionImpair.js:75, 80, 91) — NOT the triggering event's id — so undoLastEvent's stripImpairmentsForEvent(eventId) (settlementSlice.js:212-225) cannot remove them. It also replaces the exposed NPC wholesale via successorNpc (mutate.js:812), with no provenance and no scrub path. Undoing the event removes only the corruption_exposed condition; the institutions stay impaired under unmatchable cause ids and the original NPC is gone forever. This breaks the documented contract in undoEvent.js (lines 16-19): 'everything mutate writes WITH provenance (condition causes, ... ) is scrubbed by that provenance'. The faction/institution branch of EXPOSE_CORRUPTION (mutate.js:762-795) uses event.id correctly — only the NPC branch diverges because it reuses the organic world-pulse exposure path.

**Fix:** Thread the real event.id into applyCorruptionImpairments as the causeEventId for event-driven exposures (keep the synthetic id only for organic pulse exposures), and snapshot the pre-event NPC record onto logEntry.undo so the successor swap can be reverted.

**Fix note:** The suggested fix is correct and sufficient: (1) thread the real event.id into applyCorruptionImpairments as the causeEventId for event-driven exposures (keep synthetic ids only for organic pulse exposures, so stripImpairmentsForEvent can scrub them) — note this must cover all three stamped impairments including the `type:'corruption'` ...:institutional one at corruptionImpair.js:91, since stripImpairmentsForEvent does not discriminate by impairment type; and (2) capture the pre-event NPC record so the successor swap reverts. The cleanest place for (2) is to add EXPOSE_CORRUPTION to undoEvent.js's snapshot map (snapshotting settlement.npcs) and restore it in scrubUndoneEvent, mirroring the existing resource/stressor snapshot pattern, rather than the ad-hoc logEntry.undo write the reviewer suggests — that keeps it consistent with the established snapshot mechanism. One subtlety: when threading event.id, the home institution gets two impairments that would then share causeEventId=event.id with different descriptions; that is fine for the strip (both removed), but verify no consumer relies on the distinct synthetic home/criminal cause ids elsewhere.

### [MEDIUM] src/domain/events/mutate.js:1048 — Undo of CHANGE_RULING_POWER scrubs the condition but leaves the entire government transfer in place

**Evidence:** changeRulingPower delegates to transferRulingPower (rulingPower.js:376+), which renames the governing faction to the new government label, rewrites desc/modifiers ('seized_power', 'ascendant'), bumps the winner's power +6, reseeds powerStructure.publicLegitimacy by cause, and re-keys factionRelationships (rulingPower.js:399-440). None of these writes carry event provenance (no causeEventId / removedByEventId), and undoEvent.js/scrubUndoneEvent has no handler for them — only the government_overthrown condition (causes[0].eventId) is dropped and systemState reverts to beforeState. After undo, the settlement still shows the post-coup government, renamed factions, and reseeded legitimacy while the timeline and the 4-dim state claim the coup never happened — a persistent contradiction between the entity layer and SystemState. Contrast with DESTROY_SETTLEMENT, which stamps destroyedByEventId precisely so undo is exact (undoEvent.js:277-286).

**Fix:** Snapshot powerStructure (factions + publicLegitimacy + factionRelationships) pre-event onto logEntry.undo for CHANGE_RULING_POWER (extend SNAPSHOT_CONFIG_KEYS' mechanism to a settlement-path snapshot), or document/block undo for this event type.

**Fix note:** The suggested fix is sound. Preferred approach: extend captureEventUndoSnapshot to snapshot the pre-event powerStructure for CHANGE_RULING_POWER (factions, publicLegitimacy, factionRelationships, governingName, government, previousGovernments, stability, recentConflict — i.e. the whole powerStructure subtree is simplest and safest since transferRulingPower rewrites many of its fields), stamp it on logEntry.undo via applyEvent, and add a restore step in scrubUndoneEvent (a settlement-path snapshot restore, since these live on settlement.powerStructure, not settlement.config — the existing SNAPSHOT_CONFIG_KEYS/SNAPSHOT_ECONOMIC_KEYS mechanism only covers config/economicState, so it needs a new top-level snapshot bucket, e.g. snapshot.settlementPaths or a dedicated powerStructure key). Snapshotting the full powerStructure object is the most robust because the transfer touches stability/recentConflict/governingName/government too, which the reviewer's partial list (factions+publicLegitimacy+factionRelationships) would leave un-reverted. If a snapshot is judged too heavy, the lower-effort alternative is to block/disable undo for CHANGE_RULING_POWER and document it, consistent with the existing 'known limitation' posture in undoEvent.js — but that degrades UX. Note also the same defect class applies to APPLY_WORLD_PULSE coup path if it routes through transferRulingPower (worldPulse/coup.js references it) — worth checking whether world-pulse coups are undoable at all before fixing only the editor path.

### [MEDIUM] src/domain/events/undoEvent.js:302 — Creation/flag mutations (ADD_NPC, ADD_INSTITUTION/ADD_FACTION re-add, IMPOSE_CORRUPTION, PROMOTE/DEMOTE swap) persist after undo

**Evidence:** scrubUndoneEvent (undoEvent.js:302-312) covers conditions, stress entries, annotation ledgers, destruction stamps, and snapshotted resource/trade/stressor records — but several mutate.js writes carry no provenance and are never reverted: (1) ADD_NPC appends an npc created by createNpc with no creation stamp (mutate.js:634-645; npcs.js:86-103), so the NPC survives undo; (2) ADD_INSTITUTION / ADD_FACTION on an existing entity wipe impairments wholesale (`impairments: []`, mutate.js:283-285, 377) — undo cannot restore the destroyed impairment list, and a newly created institution/faction record persists; (3) IMPOSE_CORRUPTION sets npc.corrupt/corruptionVector/corruptTies with no eventId (mutate.js:845-866); (4) PROMOTE_NPC/DEMOTE_NPC swap standing fields and stamp factionId with no provenance (mutate.js:1325-1364); (5) severCorruptionTiesTo flips corrupt→false/ousted→true on REMOVE_INSTITUTION (mutate.js:825-837) and is not restored when the removal is undone. The store doc claims undo 'scrubs every settlement artifact the event left behind' (settlementSlice.js:1484-1498), which these contradict. The undoEvent.js header documents some overwrite-class limitations explicitly, but none of these are listed — they are gaps, not accepted trade-offs.

**Fix:** Stamp creation provenance (createdByEventId) on event-created npcs/institutions/factions and corruption flags so scrubUndoneEvent can drop/restore them, and snapshot pre-event entity records (the existing logEntry.undo mechanism) for the wipe/swap cases.

**Fix note:** The suggested fix is sound and matches the codebase's two existing reversal idioms. Concretely: (a) For created entities (ADD_NPC, and the new-record branches of ADD_INSTITUTION/ADD_FACTION), stamp a createdByEventId on the record at creation and have scrubUndoneEvent drop any entity whose createdByEventId === eventId (mirrors the destroyedByEventId/removedByEventId pattern). (b) For the overwrite/wipe and flag-flip cases (ADD_INSTITUTION/ADD_FACTION re-add wiping impairments, IMPOSE_CORRUPTION, PROMOTE/DEMOTE swap, and severCorruptionTiesTo on REMOVE_INSTITUTION), extend SNAPSHOT_CONFIG_KEYS/captureEventUndoSnapshot to snapshot the affected pre-event entity records (the existing logEntry.undo mechanism) and restore them in restoreSnapshottedRecords — the IMPOSE_CORRUPTION / swap / corruption-sever cases are exactly the same overwrite class the header already snapshots stressorEdits for. Note that the new-NPC drop and the entity-record snapshot together also cover the re-add wipe (snapshot restores the impairment list) without needing a separate per-field provenance scheme.

### [MEDIUM] src/domain/normalizeSettlement.js:126 — normalizeSettlement stamps current SCHEMA_VERSION before running migrations — unversioned legacy saves will skip every future migration

**Evidence:** Step 1 (line 126) sets `out.schemaVersion = SCHEMA_VERSION` whenever the field is null/undefined, and only afterwards (line 160) calls migrateSettlementToLatest(out). The migration runner's documented contract is that missing schemaVersion is treated as v0 so the from:0 entry applies (settlementMigrations.js:37-39, 59-66: 'covers any pre-versioned save with no schemaVersion field at all'), but the pre-stamp makes currentVersion() == SCHEMA_VERSION, so the chain's while-loop never executes for unversioned saves. normalizeSettlement is the ONLY caller of migrateSettlementToLatest and is the load boundary for all saves (lib/saves.js:123 migrateSettlementShape). Today this is invisible because the sole 0→1 migration is a no-op stamp, but the moment SCHEMA_VERSION bumps to 2 with a real structural rewrite, every pre-versioning save loads stamped schemaVersion=2 while still carrying the pre-v1 shape — all migrations silently skipped, the exact silent-data-loss case the registry exists to prevent. The integration tests (tests/domain/settlementMigrations.test.js:127-131) can't catch it since the no-op makes both orders indistinguishable.

**Fix:** In normalizeSettlement, do not pre-stamp schemaVersion: leave it absent (or stamp 0) and let migrateSettlementToLatest assign the final version (its 0→1 entry already stamps 1); alternatively run migrateSettlementToLatest BEFORE the version-stamp block. Add a test that a versionless settlement actually passes through the from:0 migration (e.g. by asserting a marker the migration sets).

**Fix note:** Reviewer's fix is correct. Cleanest option: run `out = migrateSettlementToLatest(out)` BEFORE the version-stamp block, letting the chain assign the version from the save's true version; the existing 0→1 entry already stamps schemaVersion=1, and the runner's currentVersion() already treats missing/null as 0. Then keep the simulationVersion/generatorVersion stamps (those are unrelated to the migration runner and can stay as pre-stamps). Alternatively, simply do not pre-stamp schemaVersion at all and let migrateSettlementToLatest own it. Note the defensive nullish-input branch (lines 111-120) returns a fresh object stamped at SCHEMA_VERSION without going through the runner — that is fine since there is no legacy shape to migrate there. Add an integration test that distinguishes the orderings, e.g. give the from:0 migration a detectable side-effect/marker and assert normalizeSettlement on a versionless save carries that marker (the current no-op-equality is why the existing test can't catch it).

### [MEDIUM] src/domain/summary/tonightAtTheTable.js:39 — 'Tonight at the Table' NPC ranking is a no-op: importance vocabulary matches no generator output

**Evidence:** Lines 38-41 rank NPCs by `npc.importance` against {major:0, primary:0, minor:1, supporting:1}. The pipeline's npcGenerator never sets `importance` (live probe: every npc.importance is undefined); the only writer is factionRoles.js:106 for synthesized structural NPCs, whose vocabulary is 'pillar'/'key'/'notable' (factionRoles.js:36-53) — none of which appear in the composer's order map, so every NPC gets rank 2 and the 'promote major NPCs' selection (doc header line 7, test at tonightAtTheTable.test.js:19) degenerates to array order. Similarly line 46 reads `npc.want`, which no generator emits (NPCs carry `goal: {short, long}`), so the wants-fallback is dead too.

**Fix:** Rank on the fields the generators actually emit: structuralRank ('dominant'/'subordinate', npcStructure.js:43) and/or power/influence, plus the factionRoles 'pillar'/'key'/'notable' importance values; read `npc.goal?.short` instead of `npc.want`.

**Fix note:** Fix direction is correct but adjust the field choices. Live probe shows base-generator NPCs do NOT carry structuralRank (it was null) — structuralRank is only present on the subset enriched via npcStructure.js, so it's not a reliable universal key. The fields actually present on every pipeline NPC are influence ('low'/'moderate'/'high') and power (numeric int). Recommend: primary sort on power (descending) or influence rank, then secondary recognition of the factionRoles importance vocabulary (pillar/key/notable) for structural NPCs, e.g. order = {pillar:0, key:1, notable:2} merged with a numeric power tiebreak. Read npc.goal?.short instead of npc.want (confirmed). Also update tests/domain/tonightAtTheTable.test.js to use real generator vocabulary (pillar/key/notable, influence/power) rather than the fictional 'major'/'minor' so the contract pins real behavior.

### [MEDIUM] src/domain/summary/tonightAtTheTable.js:95 — Phantom field settlement.supplyChainState.failures: read by two surfaces, written by nothing

**Evidence:** tonightAtTheTable.js:95 reads `settlement.supplyChainState?.failures` to build the RED 'Don't mention X' card, and src/components/map/RoutesToolbar.jsx:73 reads the same path for its route-risk hint. A repo-wide grep finds no writer: nothing ever assigns `supplyChainState` onto a settlement (live probe confirms the key is undefined), and domain/supplyChainState.js exports only derive functions (deriveSupplyChainState/deriveAllSupplyChainStates, lines 364/424) whose envelope has `status`, not `failures`. Both consumers are permanently dead — the canonical 'Don't mention salt — routes broken' beat documented at lines 92-94 can never appear. The test fabricates the field (tonightAtTheTable.test.js:75-85).

**Fix:** Derive failures live: filter deriveAllSupplyChainStates(settlement) to disrupted statuses ('blocked'/'collapsing'/'scarce', cf. hasDisruptedChains at supplyChainState.js:453) and map needLabel/failureConsequences into the {good, reason} shape both consumers expect — or change both consumers to call the derivation directly.

**Fix note:** Claim is accurate; one metadata nit: the test lives at tests/domain/tonightAtTheTable.test.js (the relevant test is at lines 75-85 there), not src/domain/summary/tonightAtTheTable.test.js as stated. Substance unchanged.

The suggested fix is sound. Note the envelope already exposes failureConsequences (per-chain) and needLabel, and hasDisruptedChains at supplyChainState.js (cited ~line 453) plus the disrupted statuses ('blocked'/'collapsing'/'scarce', and arguably 'captured'/'substituted') give a clean filter. Concrete shape both consumers expect is { good, reason }: map a disrupted chain's needLabel -> good and failureConsequences (or upstreamNote/magicNote) -> reason. Cleanest implementation is a small shared helper, e.g. deriveSupplyFailures(settlement) in supplyChainState.js that filters deriveAllSupplyChainStates to disrupted statuses and maps into {good, reason}, then have BOTH tonightAtTheTable.js:95 and RoutesToolbar.jsx:73 call it — avoids reintroducing the same phantom-path drift in two places and lets the existing tests assert against a real derivation rather than a fabricated field.

### [LOW] src/domain/entities/status.js:120 — Wall-clock fallbacks in domain event code embed nondeterministic timestamps into settlement state

**Evidence:** withImpairment defaults `appliedAt: impairment.appliedAt || new Date().toISOString()` (status.js:120); every impairment minted by mutate.js handlers (damageInstitution, impairFaction, plague, etc.) omits appliedAt, so each preview/apply/replay stamps a different wall-clock time into the settlement. mutate.js:49 and applyEvent.js:32 also fall back to `new Date().toISOString()` when an event carries no timestamp and the caller threads no `now`. propagate.js:127-130 documents that the world-pulse 'bans new Date()' and inherits the origin's appliedAt precisely to stay deterministic — but the origin impairments created in mutate.js have none to inherit. Consequences: preview-time and apply-time settlements differ byte-wise, and undoEvent.js's stated goal that 'an undone settlement stays byte-identical to one that never saw the event' (undoEvent.js:84-85) cannot hold across replays. Mitigated in the store path (applyEvent threads appliedAt as `now`), but the impairment-level default is never threaded.

**Fix:** Thread the event timestamp from mutateSettlement into every withImpairment call (the `timestamp` is already computed at mutate.js:49), and make withImpairment leave appliedAt undefined rather than defaulting to wall-clock.

**Fix note:** Suggested fix is accurate and consistent with existing patterns. Concretely: thread the already-computed `timestamp` (mutate.js:49) into every handler-built impairment as `appliedAt: timestamp`, OR (cleaner) make withImpairment leave appliedAt undefined and stamp it once at the mutate.js/applyEvent layer — mirroring corruptionImpair.js's `appliedAt: now` and propagate.js's inherit-the-origin's-appliedAt pattern. Note the fix must also cover the indirect minters that don't construct the impairment in mutate.js itself: killNpc/assignNpcToRole in entities/npcs.js return impairment objects, and these likewise need the event timestamp threaded (verify npcs.js too) so the propagation origins carry appliedAt for inheritance at propagate.js:130. A regression test in the regionalNowThreading.test.js style (apply same event twice with a moving wall clock, assert impaired entities are JSON-identical) would pin it.

### [LOW] src/domain/events/registry.js:86 — SETTLEMENT_DISPUTE missing from RERUN_KEYS_FOR_EVENT

**Evidence:** RERUN_KEYS_FOR_EVENT (registry.js:86-125) enumerates every event type in EVENT_REGISTRY except SETTLEMENT_DISPUTE (registry.js:222) — its §9 siblings BROKERED_ALLIANCE (line 109) and OPENED_TRADE_ROUTE (line 111) are present. batch.js:255 unions these keys into the batch preview's 'affected subsystems' list, so a staged settlement dispute reports no affected subsystems while a brokered alliance reports powerStructure. The registry's own header (lines 4-7) requires '(2) a rerun-affected map entry' for every event. Descriptive metadata only (the partial-rerun engine was retired), so impact is limited to a dishonest batch preview.

**Fix:** Add `SETTLEMENT_DISPUTE: ['powerStructure', 'narrative']` (matching BROKERED_ALLIANCE) to RERUN_KEYS_FOR_EVENT.

**Fix note:** The suggested fix is correct and harmless: add SETTLEMENT_DISPUTE: ['powerStructure', 'narrative'] to RERUN_KEYS_FOR_EVENT for internal consistency with the registry's documented "(2) a rerun-affected map entry" contract and parity with its siblings. But note the description should be adjusted: this is purely restoring the module's own metadata invariant — it produces NO observable change, because rerunKeys is never rendered in any UI (BatchCart at EventComposer.jsx:1083 renders only systemStateDeltas). A more apt label for SETTLEMENT_DISPUTE's effects would arguably be ['powerStructure','economicState','narrative'] given its externalThreat/volatility deltas, but matching BROKERED_ALLIANCE is fine. Equally defensible alternative: delete the now-dead rerunKeys plumbing entirely (batch.js, the settlementSlice stash, and RERUN_KEYS_FOR_EVENT) since nothing consumes it.


## 08-generators-data (44)

### [HIGH] src/generators/computeActiveChains.js:237 — External-mill export lockout and banalité note applied to EVERY chain, not just the grain chain

**Evidence:** Inside the per-chain loop (`need.chains.forEach(chain => ...)`, line 160), `externalMillOnly` (line 236) is a settlement-level condition (has 'Access to external mill', no local mill). `const effectiveExportable = externalMillOnly ? false : chain.exportable;` (line 237) and `externalMillNote` (lines 238-242) are then stamped onto EVERY active chain (lines 258, 261), not just the grain/milling chain the comment describes ('the grain chain functions ... but surplus cannot be exported'). Consequence: a road-connected hamlet/village with 'Access to external mill' (a common low-tier catalog institution — it appears in economicGenerator's required-eco list, line 2510) has ALL processed chain outputs suppressed in deriveExportsFromChains (filter `ch.exportable`, line 585) — leather, fishing, hunting, pottery chains all become non-exportable — and every one of those chains carries the grain-mill feudal-monopoly note ('Grain is processed at the lord's mill...') in the dossier.

**Fix:** Scope the override to grain/milling chains only, e.g. `const isGrainChain = chain.id === 'grain' || /grain|flour|mill/i.test(chain.label); const effectiveExportable = (externalMillOnly && isGrainChain) ? false : chain.exportable;` and attach externalMillNote only to that chain.

**Fix note:** The suggested fix direction is right but the scoping predicate can be made more robust than a label regex: scope by whether the chain itself is mill-processed, e.g. const isMillChain = chain.processingInstitutions.some(p => /\bmill/i.test(p)) — which captures the grain chain (supplyChainData.js:24 lists 'Maltster', 'Mills (2-5)', 'Mill', 'Access to external mill') without risking false label matches. If the food_processing chain (downstream of grain per CHAIN_DEPS) should also be covered, include it explicitly by chainId rather than regex. Note also that the chain in question has chainId 'grain' under needKey 'food_security' (full id 'food_security.grain'), and that CHAIN_OUTPUT_INST_GATES already independently gates 'Milled flour' on a true local mill via _hasInstForGate, so the grain chain's flour export stays correctly suppressed either way — the fix mainly restores exportability and removes the bogus note on non-grain chains.

### [HIGH] src/generators/historyGenerator.js:909 — History anchor pass writes through mismatched indices — timelines scrambled, events lost/duplicated, severities corrupted to single characters

**Evidence:** generateRelationshipEvent sorts `events` DESCENDING (line 878: `events.sort((a,b)=>b.yearsAgo-a.yearsAgo)`), then builds an ASCENDING copy `anchored` (line 883) and iterates it: `anchored.forEach((event, idx) => { ... events[idx] = anchored; })` (lines 886-913). The idx comes from the ascending copy but is written into the descending array, so each 60%-chance replacement overwrites the MIRROR-position event: the original event at that slot is destroyed and the iterated event's era/category is duplicated. Additionally the fallback template `HISTORICAL_EVENTS_DATA.find(...) || events[idx]` (line 902) passes an already-RENDERED event into generateEventNarrative, whose `pick(eventTemplate.severity)` (line 521) then indexes into the severity STRING — producing one-character severities. Empirical probe (2000 runs of exported generateHistory, city/age 300): 74% of timelines neither ascending nor descending order, 41% contain two events at the identical yearsAgo (e.g. 'The Migration(106y, anchored)' alongside 'The Occupation(106y)'), and ~24% of all events carry corrupted severities like 'o', 'r', 'j', 'm' (3046/13766 events). These corrupted severities flow into the dossier, POLITICAL_FLAVOR catastrophic checks, and legacy annotations.

**Fix:** Iterate `events` directly (or write back via indexOf on the iterated event), and when the anchor typeMap has no entry for the category, skip the rewrite instead of re-rendering the existing event object as a template. Re-sort events after the pass.

**Fix note:** Reviewer's suggested fix is correct. Concretely: (a) do not iterate an ascending copy and write into the descending array — either iterate `events` directly, or compute the write target via indexOf on the iterated event; (b) when `typeMap[cat]` has no entry (occupation_infiltration, exile_return, demographic), SKIP the rewrite rather than passing the already-rendered `events[idx]` back into generateEventNarrative (that is the source of single-char severities); (c) re-sort `events` (e.g. DESC by yearsAgo) unconditionally after the anchor pass, not only inside the resource-event branch. Optionally, hardening pick() in rngContext.js to reject non-array input would prevent this whole class of string-indexing corruption, but the local fixes (a)-(c) are sufficient.

### [HIGH] src/generators/institutionProbability.js:80 — getBaseChance keys tier-scaled logic off the raw settType sentinel ('random'/'custom') instead of the resolved tier

**Evidence:** resolveConfig resolves the tier and writes BOTH the raw sentinel (effectiveConfig.settType stays 'random'/'custom' via `...config`) AND the resolved tier (effectiveConfig.tier) — see src/generators/steps/resolveConfig.js:144-160. But getBaseChance reads `config.settType || config.tier` (settType wins, and it is always set): line 80 `tierMagicPenalty = {thorp:0.15,...}[config.settType || config.tier || 'town'] ?? 0.75` — for settType 'random' or 'custom' the lookup misses and falls back to 0.75 (town-level) regardless of the actual tier. Empirically verified with a node probe: getBaseChance(0.2,'Magic','Hedge wizard', {settType:'thorp',...}) = 0.03 vs {settType:'random', tier:'thorp',...} = 0.15 — a 5x over-spawn of arcane institutions in random-rolled thorps (and under-suppression at city/metropolis: 0.75 vs 1.0). Same pattern at line 134 (`isHighMagicIsolated` checks ['town','city','metropolis'].includes(settType||tier) — never true for settType 'random', so the 4x teleportation/planar boost for isolated high-magic town+ never fires in random mode), lines 211 and 248 (`ownTier = config.settType === 'custom' ? 'village' : (config.settType || 'village')` — 'random' gives TIER_ORDER.indexOf(-1), so tierDiff < -1 for ANY neighbour >= village and the x0.4 government suppression fires regardless of real tier; probe: metropolis + village neighbour, explicit settType gives 0.5, random settType gives 0.2; 'custom' hardcodes 'village' even though popToTier resolved the real tier), and line 260 (getPriorityModifiers keyed GOODS_MODIFIERS_BY_TIER['random'] = {} — goods-toggle penalties inert in random mode). DEFAULT_CONFIG.settType is 'random' (src/store/configSlice.js:10), so this hits the app's default generation path on every run. assembleInstitutions (steps/assembleInstitutions.js:288-290) passes effectiveConfig, which carries the correct resolved `tier` — it is simply consulted second.

**Fix:** In institutionProbability.js, resolve the tier as `config.tier || (TIER_ORDER.includes(config.settType) ? config.settType : 'town')` (resolved tier first); apply the same fix at lines 80, 134, 211, 248, and 260. Add a regression test with settType:'random' + tier:'thorp'/'metropolis' configs.

**Fix note:** The suggested fix is correct but should use tier-first ordering to match the established pattern in economicGenerator.js: `config.tier || (TIER_ORDER.includes(config.settType) ? config.settType : fallback)`. Two refinements: (1) the line-260 sub-claim is overstated — getPriorityModifiers is inert in ALL modes, not just random, because no entry in GOODS_MODIFIERS_BY_TIER defines `institutionKeywords` (the data uses `institutions` arrays); fixing the key there is harmless but changes no behavior today. (2) Same-bug-class siblings exist outside the cited file and are worth a follow-up sweep: servicesGenerator.js lines 206, 347, 2016, 2046 use settType-first ordering (`s.settType || s.tier`), and economicGenerator.js:386 reads `config?.settType` alone for its town+ check — these would silently miss in random/custom mode the same way.

### [HIGH] src/generators/steps/generatePopulation.js:33 — generateNPCs never receives powerStructure/economicState — noble NPC roles are unreachable and goal commodity/faction tokens always fall back

**Evidence:** generatePopulation destructures ctx.powerStructure (line 31) but calls `generateNPCs({ tier, institutions }, culture, effectiveConfig)` (line 33) — the settlement arg lacks powerStructure and economicState. Inside npcGenerator.js: `settlement.powerStructure?.factions` (line 1246) is always undefined, so `powerFactionCats` is empty and the NOBLE_ROLES/CRAFTS_ROLES injection (lines 1409-1435) never fires; and `settlement.economicState?...` (lines 1486-1488) makes `_tradeCommodity`/`_dominantFaction`/`_prosperity` permanently null, so all `{commodity}`/`{faction}` substitutions in NPC goals (npcGenerator.js lines 249, 266, 300, 336, 369-375) degrade to generic fallbacks. Noble roles have NO other path: the base catalog gate `institutions.some(i => i.priorityCategory === 'noble' || i.category?.toLowerCase() === 'noble')` can never pass (the institutional catalog defines no 'noble' category or priorityCategory — verified programmatically). regenNPCsPipeline (generateSettlementPipeline.js:79) has the same gap. Confirmed by running the pipeline: a city with a 'Noble Families' faction (category 'noble') produced 0 noble-category NPCs, while mergeNPCLists (nobleFaction mapping), CATEGORY_COMPAT.noble, and FACTION_ATTRACTION.noble all expect them.

**Fix:** Pass powerStructure and economicState into the settlement arg of generateNPCs in both generatePopulation.js and regenNPCsPipeline (ctx already has them; the step depends on generatePower).

**Fix note:** Reviewer's fix is correct: pass powerStructure and economicState into the settlement arg of generateNPCs in both generatePopulation.js:33 (use ctx.powerStructure / ctx.economicState) and regenNPCsPipeline at generateSettlementPipeline.js:79 (use settlement.powerStructure / settlement.economicState, which are already on the passed-in settlement). Minor accuracy note on the EVIDENCE: the {commodity} fallback technically has a first-choice of config.tradeCommodity (npcGenerator.js:241), but that field is never populated on the config passed to generateNPCs, so the practical outcome (always-null commodity, generic fallback) is unchanged. The {faction}/_dominantFaction degradation is strictly tied to settlement.powerStructure as described.

### [MEDIUM] src/generators/economicGenerator.js:132 — computeFactionPowers resource↔chain matching cannot bridge snake_case resource keys to space-separated chain labels — chain suggestions and supply checks mostly dead

**Evidence:** nearbyResources are RESOURCE_DATA snake_case keys ('grain_fields', 'iron_deposits' — confirmed by resolveResources.js RARE_RESOURCES, DEPLETED_IMPORT_MAP at economicGenerator.js:2299-2309, and EventComposer.jsx's 'stores the underscore key (the config format)'). chain.resource is a Title-Case space label ('Grain fields' — confirmed by RESOURCE_LOCAL_PRODUCERS keys at lines 175-209 being looked up directly by chain.resource, and by resourceLabelToKey existing in computeActiveChains.js precisely to map labels→keys). The matcher at lines 132-133 does raw 8-char prefix `includes`: 'grain fields'.includes('grain_fi') and 'grain_fields'.includes('grain fi') are both false — the underscore/space mismatch falls inside the prefix for almost every resource (grain_fields, iron_deposits, stone_quarry, grazing_land, salt_flats, hunting_grounds, fishing_grounds all fail; only resources whose first word is ≥8 chars, e.g. 'foraging_areas'/'mountain_timber', match). The same defect repeats with slice(0,6) at lines 228-229 for hasTerrainResource. Net effect: 'Opportunity: process X' / 'Incomplete chain' suggestions and the terrain-resource leg of the no-supply implausibility check silently never fire for most resources.

**Fix:** Normalize both sides before comparing (replace [_\s]+ with a single separator), or reuse computeActiveChains' resourceLabelToKey to map chain.resource to a RESOURCE_DATA key and compare keys exactly.

**Fix note:** Separator normalization ([_\s]+ -> one separator) fixes only the mountains/desert snake_case leg. The live input is TERRAIN_DATA[terrain].allowedResources (generateNarratives.js:33), which for the other five terrains contains plain commodity words ('wheat', 'livestock', 'game', 'wool', 'furs', 'freshwater fish') that no separator normalization will bridge to labels like 'Grain fields'/'Grazing land'/'Hunting grounds' — those need an explicit commodity->chain-resource map (or unifying TERRAIN_DATA vocab on RESOURCE_DATA keys plus reusing computeActiveChains.js resourceLabelToKey at line 69). Also fix the false-positive the prefix matcher creates: 'mountain_timber' currently matches the unrelated 'Mountain Pass' chain; exact key comparison after mapping eliminates it.

### [MEDIUM] src/generators/economicGenerator.js:311 — Case-broken processor check makes import-dependency warnings/hooks unreachable (and terrain mustImport is never threaded into config)

**Evidence:** generateStabilityScore lowercases the institution name (`const n = (i.name || '').toLowerCase()`, line 309) then tests `n.includes('Mill')`, `n.includes('Sawmill')`, `n.includes('Smith')`, `n.includes('Smelter')` (lines 311-313) — capitalized substrings can never appear in a lowercased string, so `hasProcessor` is always false and the 'Imports <resource>' dependency warning plus the timber/metal plot hooks (lines 316-337) never fire. Additionally, the whole block is gated on `config?.mustImport` (line 306), and grep shows `mustImport` is only ever defined on TERRAIN_DATA entries in src/data/geographyData.js (lines 109/190/284/388/494/588/685); no materialized code copies terrain.mustImport into config (resolveConfig.js builds effectiveConfig without it), so the terrain must-import data never reaches the viability layer at all.

**Fix:** Use lowercase patterns ('mill', 'sawmill', 'smith', 'smelter') against the lowercased name, and thread the resolved terrain's mustImport list into effectiveConfig (e.g. in resolveConfig or generateEconomy) so the branch is reachable.

**Fix note:** The suggested fix is correct but two additions matter: (1) both defects must be fixed together — fixing only the lowercase patterns still leaves the block unreachable because config.mustImport is never set, and vice versa. (2) Rather than re-deriving terrain.mustImport in resolveConfig, consider reusing resourceGenerator.js buildViabilityReport (line 49), which already computes the must-import list as report.critical (terrain.mustImport plus institution/resource mismatches); threading that existing result into generateEconomicViability avoids two divergent definitions of "must import". Also preserve the semantics: the warning is meant to fire when the settlement HAS the processing institution (mill/sawmill/smith) for a resource it must import — the lowercase-pattern fix keeps that intent.

### [MEDIUM] src/generators/economicGenerator.js:1252 — Goods-toggle key vocabulary split: generator reads tier_export_/tier_import_ booleans while the store's canonical format is tier_good_ objects — exclusion objects would read as 'on'

**Evidence:** store/toggleSlice.js documents exactly one Good key format: `tier_good_name` → `{ allow, force, forceExclude }` (line 8) and bulkSetGoods writes `${t}_good_${name}` objects (line 136). But getGoodsModifiers reads `o[`${r}_export_${m}`]` (economicGenerator.js:1252) and getUpgradeChain reads `goodsToggles[`${tier}_import_${item.name}`]` (line 1493), both treating the value as a boolean (`o[g] !== void 0 ? o[g] : h.on`). No materialized code writes `_export_`/`_import_` keys (grep over src finds only these readers). Two failure modes: per-good export/import toggles via these paths are dead keys; and if any caller ever passes the canonical object value, `{allow:false, force:false, forceExclude:true}` is truthy — an explicitly excluded good would be treated as toggled ON. The `_good_` object-shape handling that does work (deriveExportsFromChains lines 611-623, generateEconomicState 2079-2094) only operates on final lists, masking the inconsistency.

**Fix:** Unify on the canonical `tier_good_name` key + object shape: have getGoodsModifiers/getUpgradeChain read `goodsToggles[`${tier}_good_${name}`]` and respect .allow/.force/.forceExclude rather than truthiness.

**Fix note:** The suggested fix (unify on tier_good_name + object shape in getGoodsModifiers/getUpgradeChain) is directionally right but incomplete and partly cosmetic: (a) fixing getGoodsModifiers changes nothing observable — its exports are unconditionally discarded at economicGenerator.js:2321-2326 in favor of deriveExportsFromChains, which already applies toggles correctly; same for getHistoryModifiers, whose only caller _computeIncomeStreams (line 956) is dead code. (b) The fix with real user-visible effect is the imports side: deriveImportsFromChains (computeActiveChains.js:641) takes no goodsToggles parameter at all, so excluded goods still appear in primaryImports regardless of key format — it needs the same _good_ force/exclude pass deriveExportsFromChains has at lines 611-623. (c) getUpgradeChain should be fixed (its output survives as the returned `transit` list, consumed by EventComposer.jsx:152) using .forceExclude/.force/.allow semantics, not truthiness. (d) Same seam family: the _good_ post-processing block at economicGenerator.js:2135-2158 mutates re/localProduction that are subsequently wiped at 2321-2326/2345-2348 — also dead and worth removing in the same cleanup.

### [MEDIUM] src/generators/economicGenerator.js:1488 — getUpgradeChain compares the trade route against tier names — fromCityOrMetropolis/fromMetropolis/fromHigher import branches unreachable

**Evidence:** `route` is the trade-route key ('isolated'/'road'/'river'/'port'/'crossroads' — see resolveConfig.js route pools, lines 20-27/109-119), but getUpgradeChain tests `route === 'city' || route === 'metropolis'` (line 1488) and `route === 'metropolis'` (line 1491) to select the fromCityOrMetropolis/fromMetropolis buckets — conditions that can never be true. `isFromHigher` is always passed false by the only live caller (line 1221; line 973 is in the dead `_computeIncomeStreams`), so village `fromHigher` is unreachable too. Result: town falls through to fromHinterland, city falls through to fromHinterland, and the luxury/banking/legal import tables in UPGRADE_GOODS_BY_TIER (lines 1339-1434) are dead data. Final primaryImports are chain-derived (line 2327-2328), so the live blast radius is `v.imports` → the `transit` list (line 1239, returned as `transit: I` at line 2536): entrepôt towns/cities show hinterland goods (Food surplus, Raw wool) as transit goods instead of the intended higher-tier through-trade.

**Fix:** Select the source bucket from settlement tier and route semantics (e.g. towns/cities on any non-isolated route import fromCityOrMetropolis/fromMetropolis), not by comparing the route string to tier names; wire isFromHigher from a real signal or remove it.

**Fix note:** The suggested direction is right, with three additions: (a) any fix must also reorder/restructure the branch chain — line 1491 (fromMetropolis) is dead independently of the route comparison because the unconditional `else if (tierData.fromHinterland)` at 1490 precedes it and city tier always has fromHinterland; (b) a settlement realistically imports BOTH hinterland staples and higher-tier goods, so consider merging buckets (e.g. fromHinterland + fromCityOrMetropolis for connected towns) rather than picking one, which also keeps existing fromHinterland output stable; (c) clean up the seam properly: _computeIncomeStreams (line 956) is an uncalled duplicate of generateTradeIncomeStreams and should be deleted, and the mirrored dead buckets in data/tradeGoodsData.js IMPORT_GOODS_BY_TIER and servicesGenerator.js _UPGRADE_CHAINS should be wired or removed in the same change. Note changing the transit list will perturb snapshot tests — expected, but plan for it.

### [MEDIUM] src/generators/economicGenerator.js:2186 — Legacy heuristic export/import writers (military services, entire slave-trade economy block, toggle-forced local production) are computed then unconditionally discarded by chain derivation

**Evidence:** Lines 2113-2213 build the heuristic export list `re` and import list `q`: siege/occupation suffixes (2117-2127), goods-toggle force/exclude into `re` and `v.localProduction` (2135-2158), military-services exports (2175-2185), and the slave-trade block (2186-2213) which rolls `_rng() < ge` (line 2195) and pushes slave-trade export/import labels. At lines 2322-2328 `re.length = 0` / `q.length = 0` and both are refilled exclusively from chainExports/serviceExports/chainImports; `v.localProduction` is likewise cleared and refilled from chainLocalProd at 2345-2348. Siege/occupation suffixes and `_good_` toggles ARE replicated inside deriveExportsFromChains (computeActiveChains.js:600-623), but: (a) the slave-trade roll consumes an rng draw whose entire output is discarded — no materialized chain-side equivalent produces those labels; (b) the military-services heuristic (garrison/barracks/mercenary keywords, militaryEffective>=60) is only partially replaced by INSTITUTIONAL_SERVICE_MAP's much narrower patterns ('mercenary quarter','multiple garrison','professional city watch'); (c) the toggle force-add into localProduction (2147-2151) has no counterpart — deriveLocalProductionFromChains has no toggle handling — so forcing a good never surfaces it in localProduction.

**Fix:** Either delete the dead writers (and accept the rng-stream shift behind a version flag) or move the slave-trade/military heuristics after the chain override so their outputs land in the final lists; add goodsToggles handling to deriveLocalProductionFromChains.

**Fix note:** Two corrections to the suggested fix. (1) Do NOT move the slave-trade heuristic after the chain override: an institution-gated slave_trade chain already exists (supplyChainData.js:1448-1464, exportable, gated on 'Slave market'/'Workhouse'), so resurrecting the probabilistic roll would double-represent the slave economy and contradict the chain-first, institution-coherent design. Delete the dead block instead; note the deletion shifts the seeded rng stream for tier>=town settlements (the draw at 2195 is conditional on tier), so snapshot/property tests will churn — the suggested version flag is appropriate. (2) The military-services narrowing appears intentional (comment at computeActiveChains.js:755: "requires substantial military infrastructure (city+)"); just delete the dead heuristic rather than porting it, unless a design decision says otherwise. The fix that actually matters is the third one: add goodsToggles force/exclude handling to deriveLocalProductionFromChains (or apply the toggles after the refill at economicGenerator.js:2345-2348). The allow===false exclusion is the sharper half — a banned good currently disappears from exports but persists in localProduction, an internal dossier/PDF incoherence. Also remove the now-dead toggle writes into v.localProduction at 2147-2156 and add a test pinning toggle behavior across exports AND localProduction, since no test currently exercises goodsToggles at all.

### [MEDIUM] src/generators/factionRoles.js:137 — ensureFactionStructuralNpcs dedup can never match pipeline NPCs — placeholder seat-holders duplicate generated ones

**Evidence:** The dedup requires sameRole AND sameFaction (lines 137-142), where sameFaction checks `npc.linkedFactionIds` — a field only factionRoles.js itself and event-created NPCs ever write (verified by grep); pipeline NPCs carry only the factionAffiliation string from mergeNPCLists. sameRole compares exact role strings, but structural roles ('Watch Captain', 'Guildmaster', 'High Priestess', 'Lord Mayor') never equal the pipeline catalog's roles ('Guard Captain', 'Guild Master', 'High Priest', 'Mayor'). So the documented idempotency ('skips a role if an existing NPC already matches it') never applies and placeholders are appended to every settlement whose factions match an archetype. Confirmed empirically: a generated city contained both 'Cäcilia Schröder / Guild Master' and the placeholders 'The Guildmaster' + 'The Senior Trader' (cat=undefined, title=undefined); even a 20-person thorp received 'Guildmaster | Senior Trader' placeholders. Two factions of the same archetype also mint identically-named placeholders ('The Kingpin' twice).

**Fix:** Match by normalized role keywords AND by factionAffiliation name (in addition to linkedFactionIds), or write linkedFactionIds in mergeNPCLists; consider giving structural NPCs real names via the naming pools.

**Fix note:** The reviewer's suggested fix is sound but should be hardened. Matching by factionAffiliation NAME is fragile: structural-NPC linkedFactionIds store the faction id/name resolved via faction.id||faction.faction||faction.name (factionRoles.js:96), while existing pipeline NPCs store assignedFaction.faction (the faction's display name). These can diverge. Most robust: (a) normalize role via keyword/synonym matching (guildmaster≈guild master, watch captain≈guard captain, high priestess≈high priest, lord mayor≈mayor) AND (b) match faction by resolving BOTH sides through the same faction identifier (compare proposed's source faction.faction against npc.factionAffiliation, not just linkedFactionIds). Alternatively, the cleaner root-cause fix is to have mergeNPCLists/the population step write linkedFactionIds (derived from factionAffiliation) so all NPC↔faction linkage lives in one canonical field. Separately, give structural NPCs real names from the naming pools (namingData.js has a guild_master title mapping) so even legitimately-added seat-holders aren't bare 'The X' placeholders, and consider filtering or de-emphasizing faction_structural NPCs in viewModel if a real role-holder of the same seat already exists.

### [MEDIUM] src/generators/historyGenerator.js:639 — Stress-to-tension mapping points at tension templates that do not exist — insurgency/mass_migration/religious_conversion/slave_revolt and trade-partner neighbours produce no tension

**Evidence:** STRESS_TO_TENSION maps insurgency/religious_conversion/slave_revolt → 'legitimacy_crisis' and mass_migration → 'demographic_pressure' (lines 639-643), and a trade_partner neighbour maps to 'trade_dispute' (line 652). HISTORICAL_EVENTS_DATA (src/data/historyData.js) contains exactly 16 types — none of these three exist (verified by enumerating `type:` fields). `HISTORICAL_EVENTS_DATA.find(e => e.type === tensionType)` returns undefined and the priority-add silently does nothing (lines 696-702, 725-734), so the five newer stressors and trade-partner relationships never seed their intended currentTensions; random fillers are used instead. The genSuccessionNarr branches keyed on those topTension values can also never fire.

**Fix:** Add 'legitimacy_crisis', 'demographic_pressure', and 'trade_dispute' templates to HISTORICAL_EVENTS_DATA, or remap those stressors to existing types.

**Fix note:** Fix is correct as stated. Either add 'legitimacy_crisis', 'demographic_pressure', and 'trade_dispute' template objects to HISTORICAL_EVENTS_DATA in src/data/historyData.js (and corresponding genSuccessionNarr branches in powerGenerator.js so the new topTension values produce narrative), OR remap the stressors to existing types — e.g. insurgency/religious_conversion/slave_revolt -> 'leadership_vacuum' or 'religious_tension'/'population_friction', mass_migration -> 'population_friction', trade_partner -> 'economic_disparity' or 'outside_debt'. The first option is preferable since it preserves the distinct intent of these newer stressors; remapping to leadership_vacuum/religious_tension loses specificity. Note that simply adding to HISTORICAL_EVENTS_DATA alone leaves the genSuccessionNarr gap, so both files should be updated for full coherence.

### [MEDIUM] src/generators/historyGenerator.js:916 — Resource-anchored history events are dead code — context.nearbyResources is never populated

**Evidence:** generateRelationshipEvent reads `context.nearbyResources || []` (line 916) to gate four bespoke events (The Iron Dispute, The Great Harvest Compact, The Great Construction, The Arcane Incident — lines 921-996). The context object is built solely by buildHistoryContext (lines 116-241) which never sets nearbyResources, and generateNarratives.js:46 calls generateHistory(tier, effectiveConfig, institutions, ...) without threading ctx.nearbyResources (which resolveResources.js does provide on the pipeline ctx). hasResource() is therefore always false and none of these events can ever appear.

**Fix:** Thread ctx.nearbyResources through generateNarratives into generateHistory and set context.nearbyResources in buildHistoryContext.

**Fix note:** The reviewer's suggested fix is half right. The necessary and sufficient change is to set `nearbyResources: config.nearbyResources || []` in the object returned by buildHistoryContext (src/generators/historyGenerator.js, the return at lines 216-240). config (= effectiveConfig) already carries nearbyResources from resolveResources.js:222, so the suggested "thread ctx.nearbyResources through generateNarratives into generateHistory" step is unnecessary — generateNarratives.js:46 and regenHistoryPipeline (generateSettlementPipeline.js:108) already pass a config that contains nearbyResources. After the fix, also confirm determinism is preserved (the block already consumes _rng() unconditionally at line 918, so seeding behavior is unchanged) and consider adding a generateHistory-level test that asserts these events can emerge, since the existing tests in tests/domain/historyBeats.test.js and tests/domain/stressorAftermath.test.js only use them as hand-built fixtures and never exercise the resource gate.

### [MEDIUM] src/generators/isolationGenerator.js:124 — applySubsistenceMode's famine flag (_isolationFoodStress) is written onto the institutions array and never read anywhere — dead seam from the monolith migration

**Evidence:** In applySubsistenceMode (called from steps/isolationPass.js:63 for isolated thorps/hamlets), line 123-125: `if (chance(0.35) && !effectiveConfig.stressTypes?.includes('famine')) { institutions._isolationFoodStress = true; }`. The flag is set as an expando property on the institutions ARRAY (not on effectiveConfig or ctx), and a repo-wide grep (src/ + tests/) finds exactly one occurrence — the write. No consumer exists, so the clearly intended behavior (an isolated subsistence settlement picking up food/famine pressure ~35% of the time) silently does nothing; the seeded roll is consumed for no effect. An array expando also wouldn't survive JSON serialization even if a reader existed. This looks like the consumer was lost when the legacy monolithic generateSettlement was decomposed into pipeline steps (the legacy file itself has since been deleted).

**Fix:** Either wire the seam: set the flag on effectiveConfig (e.g. effectiveConfig._isolationFoodStress = true) and have stressConfirmPass (or resolveStress's overlay) inject/keep a famine-class stressor when present — or delete the roll entirely so it stops consuming an rng draw and implying behavior that doesn't exist.

**Fix note:** The suggested wiring point is ordering-correct: stressConfirmPass declares deps ['resolveStress', 'isolationPass'] (steps/stressConfirmPass.js:60-61) and runs before generateEconomy reads config.stressTypes, so a flag set on effectiveConfig in isolationPass (matching the existing _magicTradeOnly/_isolationInfraType pattern at isolationGenerator.js:90-91) would be visible there. However, stressConfirmPass's documented contract explicitly says it 'only confirms or drops, it NEVER rolls new stressors' — injecting a famine stressor there requires a deliberate carve-out of that contract (or doing the injection in isolationPass itself / teaching resolveStress about isolation+subsistence). Also note: either fix (wiring or deleting the roll) changes the seeded draw sequence for isolated thorp/hamlet configs, so snapshot/property tests covering those seeds will need regeneration. Finally, the project's own audit bundles this with a sibling defect in the same function (subsistence mode stripping the smith/alehouse, isolationGenerator.js:102-116) — worth fixing together since both were flagged in docs/GENERATION_COHERENCE_AUDIT.md:333-334.

### [MEDIUM] src/generators/narrativeGenerator.js:1036 — generatePressureSentence passes the wrong context shape to genSuccessionNarr — most succession/tension narrative branches can never fire

**Evidence:** genSuccessionNarr (powerGenerator.js:318-437) branches on r.topTension (17 tension-specific narratives), r.topNPCName/topNPCRole, r.viabilityIssues/isViable, r.hasNeighborConflict/neighbor, r.milForce, r.isCrossroads/isPort. generatePressureSentence calls it with `summary` from genSettSummary (narrativeGenerator.js:1036), which carries none of those fields (it has name/tier/commodity/prosperity/stability/govFaction/topFaction/...). The shape that matches every field is genPressureDetail's `detail` (lines 740-770), which the same function computes at line 1018 but does not pass. Result: all topTension branches, the viability branch, and the neighbour branch are dead; output degrades to the prosperity/stability generics with 'the most senior figure' placeholders.

**Fix:** Call genSuccessionNarr(detail) instead of genSuccessionNarr(summary) (or merge detail into summary before the call).

**Fix note:** Suggested fix is correct. genSuccessionNarr(detail) supplies all required fields, and detail also carries name/govFaction/topFaction/prosperity/stability/commodity so the generic branches still work. Note one subtlety: detail.topFaction can be null (genPressureDetail line 710: factions[0]?.faction || null) whereas summary.topFaction defaults to 'the dominant faction'; genSuccessionNarr already guards every topFaction use with `|| 'the dominant faction'`/`|| 'the merchant class'`/etc., so passing detail is safe. Alternatively merge the missing fields into summary before the call.

### [MEDIUM] src/generators/neighbourGenerator.js:136 — Neighbour profile reads econ.prosperityLevel — field is `prosperity`, so neighbour economic strength is always 0.5 (identity multiplier)

**Evidence:** extractNeighbourProfile computes `economicStrength = PROSPERITY_RANK[econ.prosperityLevel] ?? 0.5` (line 136), but generated settlements store the label under economicState.prosperity (economicGenerator.js:2530; every other consumer — factionDynamics.js:77, powerGenerator.js:983 — reads `.prosperity`). The lookup always misses, economicStrength is always 0.5, and the consumer institutionProbability.js:242 multiplies market/guild institution chances by (0.8 + 0.5*0.4) = exactly 1.0 — a Wealthy neighbour and a Struggling neighbour have identical economic pull. Several PROSPERITY_RANK keys ('Modest', 'Affluent') also don't exist as produced labels.

**Fix:** Read `econ.prosperity` (with prosperityLevel as a legacy fallback) and align PROSPERITY_RANK keys with the actual label set (Struggling/Poor/Moderate/Comfortable/Prosperous/Wealthy).

**Fix note:** The suggested fix is correct in direction: read `econ.prosperity` as primary with `econ.prosperityLevel` as a fallback only if any legacy producer exists (none currently does, so the fallback is harmless but unnecessary). Align PROSPERITY_RANK to the exact produced label set ['Struggling','Poor','Moderate','Comfortable','Prosperous','Wealthy']. Note 'Subsistence'/'Modest'/'Affluent'/'Thriving' are not final labels emitted by generateEconomicNarrative (powerGenerator does reference a 'Thriving' label elsewhere, so worth confirming the canonical set before hardcoding). The 'Struggling' key already matches and need not be removed. Adding a regression test that a Wealthy neighbour yields economicStrength > a Struggling neighbour (and that institutionProbability differs between them) would lock this down, since the current behavior produces no observable difference and so no existing snapshot test would have caught it.

### [MEDIUM] src/generators/npcGenerator.js:197 — NPC honorific titles fall back to raw lookup keys — every thorp Elder displays title "elder", shepherds display "owner"

**Evidence:** pickLast returns `titles[s] || s` (lines 197-201). NAMING_DATA's titles maps (all 11 cultures, 27 keys each) lack the keys 'elder','owner','agent','contact','warden','advisor','steward','knight','noble','druid','bard' — but 31 of the 142 roles in HISTORY_EVENTS (src/data/historyData.js) plus all NOBLE_ROLES use exactly those keys (verified programmatically). The raw key becomes npc.title and is rendered as the NPC's honorific on-screen (components/new/npcComponents.jsx:192) and in the PDF (pdf/sections/NotableNPCs.jsx:288-290). Confirmed empirically: every generated thorp shows 'Elder title="elder"' and 'Shepherd title="owner"' instead of cultural titles like 'Ältester'. Affected roles include the mandatory Elder (every thorp/hamlet), Miller, Shepherd, Innkeeper, Tavern Owner, Prison Warden, Druid Elder, and all noble roles.

**Fix:** Add the missing title keys to each culture's titles map (or remap those roles to existing keys); optionally make pickLast fall back to a capitalized English default rather than the raw key.

**Fix note:** The reviewer's mechanism is right but the exact wiring is slightly different from what the evidence text implies: npc.title is set in generateSingleNPC via `title: lastName` where `lastName = pickLast(culture, namingTier)` (npcGenerator.js:63,77) and namingTier is candidate.title (passed at lines 1495 and 1523). The line-212 filterByGuild assignment only affects guild-master NPCs. So a fix that only touches line 212/pickLast's guild path would miss the main leak — the real fix must address the title map keys (or the generateSingleNPC title assignment). Suggested fix is otherwise correct: add the 11 missing keys (elder, owner, agent, contact, warden, advisor, steward, knight, noble, druid, bard) to each culture's titles map, and/or have pickLast fall back to a capitalized English default (e.g. titleCase(s)) instead of the raw lowercase key so future unmapped keys degrade gracefully rather than showing bare lowercase strings. Note Widowed Farmer also maps to 'elder' and several roles map to 'agent', so the impact is broader than the listed examples.

### [MEDIUM] src/generators/steps/assembleSettlement.js:110 — assembleSettlement legitimacy patch re-labels publicLegitimacy and multipliers without re-applying them to faction powers or relationships

**Evidence:** Faction powers are multiplied by the PROVISIONAL legitimacy multipliers during generatePower (powerGenerator.js:2184-2187 → factionDynamics.applyLegitimacyMultipliers), using a defense label guessed from institution presence. assembleSettlement then patches publicLegitimacy with the real defenseProfile readiness (lines 110-133): it rewrites score, label, govMultiplier/crimMultiplier and the is* flags — the provisional vs real defense contribution can differ by up to ~17 points (provisional 'Well-Defended' +7 vs real 'Undefended' -10), enough to cross bands (e.g. Tolerated→Contested). But faction powers keep the provisional multipliers, factionRelationships keep narratives/directions derived from the provisional legitimacy flags, and the governing faction's legitimacyCrisis tag is never added/removed. The dossier can therefore show 'Legitimacy Crisis, gov ×0.60' beside faction powers computed at ×1.0.

**Fix:** Either compute defenseProfile before generatePower (it only needs institutions/config/economicState), or re-run applyLegitimacyMultipliers + computeFactionRelationships against the patched legitimacy when the band changes.

**Fix note:** The "re-run applyLegitimacyMultipliers" option needs an idempotency guard: applyLegitimacyMultipliers computes f.power = round(f.power * mult) from the CURRENT f.power, not f.rawPower (factionDynamics.js:499-500), so a second invocation would multiply the already-multiplied value. Before re-running, restore each faction's f.power = f.rawPower (and clear stale f.legitimacyCrisis/f.crisisNote), or refactor the function to base the multiply on rawPower. Also note the divergence is bidirectional: the patch updates only the badge's governanceFractured/isLegitimacyCrisis flags, so the faction-level f.legitimacyCrisis tag (set in the provisional pass) can be both falsely-present (band left crisis after patch) and falsely-absent (band entered crisis after patch). The cleanest fix is the orchestration option: compute defenseProfile before generatePower so the real readiness label feeds computePublicLegitimacy the first time — defenseProfile only needs institutions/config/economicState, all available pre-power.

### [MEDIUM] src/generators/steps/generatePower.js:33 — generatePower passes tradeRoute=null — hostile-neighbour stability and 'Ongoing tensions with {neighbour}' recentConflict branches are unreachable

**Evidence:** generatePowerStructure(tier, economicState, null, {...}, institutions) is called with tradeRoute hard-coded null (steps/generatePower.js lines 32-36), with a comment claiming 'generatePowerStructure itself reads no neighbour bias'. That is false: powerGenerator.js lines 1819-1824 set stability 'Tense (external threat)' when `tradeRoute?.relationshipType` is hostile_rival/cold_war/tense, and line 1962-1963 sets recentConflict to `Ongoing tensions with ${tradeRoute.neighborName}`. With null both branches are dead in the pipeline (the sole generation entry point), so a hostile neighbour never surfaces in the power structure's stability or recent-conflict text, even though resolveNeighbour threads config.neighborRelationship for other consumers.

**Fix:** Pass a {relationshipType, neighborName} object derived from ctx.neighbourProfile as the third argument (or read effectiveConfig.neighborRelationship inside generatePowerStructure) instead of null.

**Fix note:** The suggested fix (pass {relationshipType, neighborName} from ctx.neighbourProfile, or read effectiveConfig.neighborRelationship inside generatePowerStructure) is directionally correct but has a value-space mismatch the reviewer missed: the stability branch at powerGenerator.js:1819-1824 checks for relationshipType 'hostile_rival' and 'tense', which are NOT valid relType values produced upstream (the real keys are neutral/allied/trade_partner/patron/client/rival/cold_war/hostile per REL_DYNAMICS). So threading the real value would only light up the 'cold_war' case; 'hostile' and 'rival' would still fall through. A correct fix must either (a) normalize the branch to test for the actual relType vocabulary (e.g. include 'hostile' and 'rival'), or (b) substring-match like the other consumers do (priorityHelpers.js:361, historyGenerator.js:647, institutionProbability.js:250 all use `.toLowerCase().includes(...)`). The recentConflict branch (truthiness on tradeRoute) needs no value-space change. Also worth verifying that lighting these branches does not alter deterministic snapshots for existing no-neighbour configs — since no-neighbour leaves the key absent, the default path stays identity, so determinism for the common case is preserved.

### [MEDIUM] src/generators/steps/stepMetadata.js:41 — Pipeline-rail step summaries read context keys the pipeline never writes; resolveStress summary always asserts 'No active stressors'

**Evidence:** STEP_METADATA summaries are called with the live pipeline ctx (store/settlementSlice.js:568-574 pushes summary(ctx) into pipelineHistory for the PipelineReveal/PipelineRail 'How this was simulated' UI). Several summaries reference keys that never exist on ctx: line 41 resolveStress reads `ctx.stressors?.active?.length` but the step provides `stress`/`stressTypes` (ctx.stressors only exists on the final settlement object) — so the falsy branch returns the string 'No active stressors' even when stressors ARE active; line 27 resolveConfig reads `ctx.config?.tier` (raw config has settType, not tier — resolved tier lives at ctx.tier); line 33 resolveResources reads `ctx.resources?.local` (step provides nearbyResources); line 88 generateEconomy reads `ctx.economy?.prosperityBand` (step provides economicState, and that object has prosperityLevel — grep shows no generator writes prosperityBand); line 96 generatePower reads `ctx.power?.factions` (step provides powerStructure; ctx.factions is minted later by generatePopulation); line 109 factionCorrelationPass reads `ctx.tensions` / `ctx.history?.currentTensions` (history is generated later by generateNarratives). Empirically verified by running the pipeline with forced famine+plague_onset and evaluating each summary per step: resolveStress printed "No active stressors" while stressConfirmPass printed "2 stressors confirmed"; resolveConfig/resolveResources/generateEconomy/generatePower/factionCorrelationPass all returned null (their facts never display). The resolveStress case is a factually false receipt shown to the user on every stressed generation.

**Fix:** Rewrite the summaries against the real ctx contract: resolveStress -> ctx.stressTypes.length (or Array-normalize ctx.stress), resolveConfig -> ctx.tier, resolveResources -> ctx.nearbyResources?.length, generateEconomy -> ctx.economicState?.prosperityLevel, generatePower -> ctx.powerStructure?.factions?.length, factionCorrelationPass -> ctx._rosterChangedAfterEconomy or trace counts. Add a test that runs the pipeline and asserts each STEP_METADATA summary returns non-null for a representative config.

**Fix note:** The suggested fix is directionally correct with two corrections: (1) generateEconomy's economicState field is `prosperity` (economicGenerator.js:2530), not `prosperityLevel` — `prosperityLevel` belongs to the neighbour-economy shape (neighbourGenerator.js:136); use ctx.economicState?.prosperity. (2) For resolveStress, counting ctx.stressTypes misses custom authored stressors ('dragon_tax' style), which resolveStress.js:148 deliberately filters out of stressTypes (catalog-only); Array-normalizing ctx.stress gives the true count. Also note factionCorrelationPass returning _rosterChangedAfterEconomy (factionCorrelationPass.js:141) means that key IS readable post-step, but economyReconcilePass's summary already reports that exact fact (stepMetadata.js:116) — pick a distinct fact (e.g. conflict/tension counts off ctx.powerStructure) to avoid duplicate receipts. The proposed test (assert each STEP_METADATA summary is exercised non-null for a representative config) is the right prevention.

### [MEDIUM] src/generators/steps/stepMetadata.js:41 — Pipeline rail always reports 'No active stressors' — summary reads a ctx key no step provides

**Evidence:** STEP_METADATA.resolveStress.summary (stepMetadata.js:40-43) computes `const ss = ctx.stressors?.active?.length || 0; return ss ? `${ss} active stressor…` : 'No active stressors'`. But the resolveStress step returns `{ stress, stressTypes }` (src/generators/steps/resolveStress.js:153, provides: ['stress','stressTypes'] at line 16) — no step in the pipeline ever writes a top-level `ctx.stressors`, let alone a `{active:[]}` shape. Verified against the production bundle: dist/assets/engine-D8TwO_ZR.js contains the identical lambda (`e.stressors?.active?.length||0 … "No active stressors"`), and the only `stressors:` writes in the whole engine bundle are the settlement-object dual-write inside assembleSettlement (`stress:C, stressors:C` — the raw container, not {active}) and a frozen alias map. settlementSlice.generateSettlement (src/store/settlementSlice.js:567-573) calls meta.summary(ctx) per step and stores it in pipelineHistory; PipelineRail.jsx:98-102 renders entry.summary. Net effect: every generated dossier's 'How this was simulated' rail shows 'No active stressors' under 'Determine stressors' even when the run rolled active stressors — directly contradicted two rows later by stressConfirmPass's working summary ('N stressors confirmed', which correctly reads ctx.stressTypes). The rail is documented as the falsifiable receipt of the simulation (PipelineRail.jsx:10-14), so this is an affirmatively false claim on the main path, not just a cosmetic gap.

**Fix:** Change the summary to read the keys the step actually provides, e.g. `const ss = Array.isArray(ctx.stressTypes) ? ctx.stressTypes.length : (ctx.stress ? 1 : 0)`, and add a sync test that runs the registered steps' provides lists against every STEP_METADATA summary's accessed keys (or runs the real pipeline once and asserts each non-null-summary step produces a non-null summary at least once across a few seeds).

**Fix note:** The suggested fix is correct. `const ss = Array.isArray(ctx.stressTypes) ? ctx.stressTypes.length : (ctx.stress ? 1 : 0)` reads the keys the step actually provides. One nuance worth noting: stressTypes is filtered to CATALOG types only (STRESS_TYPE_MAP keys) at resolveStress.js:148, so a custom authored-only stressor (e.g. 'dragon_tax') rides `ctx.stress` (the container) but not `ctx.stressTypes`. If 'active stressor' is meant to count custom authored ones too, counting `ctx.stress` (normalize to array length) is more faithful than `ctx.stressTypes.length`; if it's meant to mirror stressConfirmPass's catalog-based count, stressTypes is consistent. Either is a strict improvement over the current always-zero read. The proposed sync test (run real steps' provides lists against each summary's accessed keys, or run the pipeline across a few seeds and assert each non-null summary produces a non-null result at least once) would have caught this and is the right structural prevention.

### [LOW] src/data/geographyData.js:213 — TERRAIN_DATA allowedResources mixes two id vocabularies: mountain/desert use snake_case RESOURCE_DATA-style keys, all other terrains use plain display words

**Evidence:** mountain.allowedResources are snake_case keys ('iron_deposits', 'stone_quarry', 'hunting_grounds', 'ancient_ruins' — lines 213-225) matching the RESOURCE_DATA key vocabulary used throughout the generators (RARE_RESOURCES, DEPLETED_IMPORT_MAP, RESOURCE_EXTRACTION_GATES), and desert likewise (lines 610-620). But coastal/riverside/forest/plains/hills use plain words ('fish', 'salt', 'timber', 'grain', 'iron ore' — lines 54-66, 131-142, 412-424, 517-529). Any consumer that joins allowedResources against resource keys works for two terrains and silently fails for five (or vice versa). No materialized consumer reads allowedResources, so this is either dead data or a break inside a stripped consumer — either way the table is internally inconsistent.

**Fix:** Normalize all seven terrains to the RESOURCE_DATA key vocabulary (or delete the field if it is dead).

**Fix note:** Two corrections to the suggested fix. (1) Do not delete the field — it is live, not dead: resourceGenerator.js:19 calls terrain.allowedResources.some(...) unguarded and would throw, and generateNarratives.js step 14 feeds it into the rendered resourceAnalysis. (2) Normalizing geographyData.js alone is insufficient and would make things worse: RESOURCE_CHAINS.rawResource in resourceData.js mirrors the same mixed vocabulary (plain words for the 14 generic chains, snake_case for the 7 'Terrain-specific chains'), and evaluateEconomicActivity requires an exact nearbyResources.includes(chain.rawResource) match. Converting the five plain-word terrains to snake_case keys without simultaneously converting the generic chains' rawResource values would zero out all active chains for those terrains. Normalize both tables (geographyData.js allowedResources and resourceData.js RESOURCE_CHAINS.rawResource) to the RESOURCE_DATA key vocabulary in one pass, and add display-name mapping where the dossier renders chain.rawResource (ResourcesTab.jsx renders it verbatim, currently showing raw snake_case like 'alpine_pasture' for mountain/desert). Also note mountain's hot_springs_mineral sits at index 7 and is silently dropped by the slice(0,7) in generateNarratives.js:33 — worth fixing in the same pass.

### [LOW] src/data/tradeGoodsData.js:131 — GOODS_CATEGORIES.TRADE referenced but never defined — 'Enslaved persons' goods get category undefined; same records also use a 'requiresInstitution' key the readers don't recognize

**Evidence:** GOODS_CATEGORIES (lines 10-17) defines AGRICULTURAL/RAW_MATERIALS/MANUFACTURED/LUXURY/SERVICES/FOOD_PROCESSED — no TRADE. Yet `category: GOODS_CATEGORIES.TRADE` is used at lines 131 (EXPORT_GOODS_BY_TIER.town), 203 (city), and 390 (IMPORT_GOODS_BY_TIER.town), yielding `category: undefined`. The same town/city 'Enslaved persons' export records use `requiresInstitution` (lines 135, 207) while every other record and both readers (getGoodsModifiers/getHistoryModifiers in economicGenerator.js, lines 1254/1051) use `requiredInstitution` — so even where these records were read, the institution gate would never apply. Currently inert because EXPORT_GOODS_BY_TIER's only materialized consumer (EventComposer.jsx:262) uses keys only, but it is a live landmine for any consumer that groups by category or honors institution gates.

**Fix:** Add TRADE: 'trade' to GOODS_CATEGORIES (or recategorize the records), and rename requiresInstitution → requiredInstitution on all three records.

**Fix note:** The suggested fix is sound but two additions: (1) If adding TRADE: 'trade' to GOODS_CATEGORIES, also check src/domain/customContentSchema.js — its classification enum (comment at line 96) deliberately mirrors the six existing GOODS_CATEGORIES values, so adding a seventh may need a matching schema update; recategorizing the three records to an existing category avoids that ripple. (2) There is a fourth 'Enslaved persons' record at tradeGoodsData.js:594 inside GOODS_MODIFIERS_BY_TIER.town using a third schema (institutions/tradeRoutes/resourceBoost/institutionBoost/routeBoost, no category/p/on) that getGoodsModifiers (economicGenerator.js:1247) can never select — h.on is undefined and _rng() < undefined is always false — so any cleanup of the slave-trade records should decide what that record is supposed to do too. Minor: renaming requiresInstitution → requiredInstitution is correct for schema consistency, but note EXPORT_GOODS_BY_TIER is not read by any institution-gating code today, so the rename is future-proofing, not a behavior change (records are on: false regardless).

### [LOW] src/data/tradeGoodsData.js:389 — IMPORT_GOODS_BY_TIER.town has an object entry ('Enslaved persons') mixed among array buckets; table appears to be dead data drifting from its in-generator duplicate

**Evidence:** IMPORT_GOODS_BY_TIER.town (line 388) contains `'Enslaved persons': { category, desc }` (an object, missing name/on) sitting beside `fromCityOrMetropolis`/`fromHinterland` array buckets — any consumer iterating buckets as arrays (the pattern getUpgradeChain uses on its duplicate) would break or mis-handle it. grep finds no consumer of IMPORT_GOODS_BY_TIER anywhere in materialized src; the generator instead carries a verbatim private duplicate UPGRADE_GOODS_BY_TIER (economicGenerator.js:1267-1478) WITHOUT this entry — classic duplicated-table drift from the strangler-fig migration.

**Fix:** Delete IMPORT_GOODS_BY_TIER or make it the single source consumed by getUpgradeChain; if kept, move 'Enslaved persons' into a properly-shaped bucket entry.

**Fix note:** Do NOT simply delete IMPORT_GOODS_BY_TIER — customRegistry.js (lines 27, 253) imports and ingests it; deletion breaks the Compendium registry build. Correct fix: (1) normalize the 'Enslaved persons' entry into a properly-shaped item inside a bucket array (with name/on), or remove it if towns importing slaves was never wired up; (2) make IMPORT_GOODS_BY_TIER the single source consumed by getUpgradeChain and delete the private UPGRADE_GOODS_BY_TIER duplicate in economicGenerator.js:1267-1478; (3) fix customRegistry's ingest of the import table — it currently assumes the EXPORT name→props shape and mis-ingests the bucket→array shape, registering bucket keys ('basic', 'fromCityOrMetropolis', etc.) as trade goods while skipping the real goods; (4) GOODS_CATEGORIES.TRADE does not exist (also referenced at tradeGoodsData.js:131 and :203 in the export table) — add a TRADE key or use an existing category, otherwise category is undefined on every 'Enslaved persons' entry.

### [LOW] src/data/tradeGoodsData.js:594 — GOODS_MODIFIERS_BY_TIER.town['Enslaved persons'] is a boost-table record in a goods table — no p/on/desc, so it can never be emitted and renders as a malformed entry

**Evidence:** Every other GOODS_MODIFIERS_BY_TIER record has {category, p, on, desc, requiredInstitution?}. The town 'Enslaved persons' entry (lines 594-600) instead has {institutions, tradeRoutes, resourceBoost, institutionBoost, routeBoost} — the shape of a probability-modifier table, evidently pasted into the wrong table. In getGoodsModifiers (economicGenerator.js:1247-1264) `h.on` is undefined (falsy → skipped), and even with an explicit toggle, `_rng() < h.p` is `_rng() < undefined` → always false, so the good can never appear; any UI iterating this table shows an entry with undefined desc/category/p. Note the EXPORT_GOODS_BY_TIER copy of the same good (line 130-135) has the proper goods shape, confirming divergence between the duplicated tables.

**Fix:** Replace the record with the goods-shaped version from EXPORT_GOODS_BY_TIER.town (p: 0.15, on: false, requiredInstitution: 'Slave market', desc), or move the boost-shaped record to whatever modifier table it was written for.

**Fix note:** Two corrections to the suggested fix: (1) Do not copy the EXPORT_GOODS_BY_TIER.town record verbatim — it uses the field name `requiresInstitution` (tradeGoodsData.js:135), but getGoodsModifiers reads `h.requiredInstitution` (economicGenerator.js:1251-1254), so a verbatim copy would silently drop the Slave-market gating. Use `requiredInstitution: 'Slave market'` as the reviewer's fix text correctly shows (the same `requiresInstitution` misspelling also exists at EXPORT_GOODS_BY_TIER lines 135 and 207 and is its own latent seam). (2) The divergence is slightly broader than one record: GOODS_MODIFIERS_BY_TIER.city has no 'Enslaved persons' entry at all while EXPORT_GOODS_BY_TIER.city does (line 202), so the city-tier good is also unreachable in the engine. Also note: since the good is `on: false` by default and engine override keys are `${tier}_export_${name}` while the toggle UI writes `${tier}_good_${name}` keys, impact is limited to the good being permanently unavailable rather than visibly wrong on default runs.

### [LOW] src/generators/crossSettlementConflicts.js:171 — Cross-settlement conflicts mirror asymmetric relationships symmetrically and run unseeded at render time

**Evidence:** For directional relationships the mirrored description is built by swapping the NPC/faction args while keeping the same relType (lines 171, 214): the patron NPC template '`${npcB.name} of ${settB.name} is pressing ${npcA.name} ... invoking the patronage agreement to compel compliance`' (line 79) and the patron faction template '`${facB.name} of ${settB.name} exercises de facto oversight of ${facA.name}`' (line 125) therefore claim BOTH settlements are the patron in the paired forA/forB records — the two dossiers contradict each other about which side holds the patronage. Additionally RelationshipsTab.jsx:27 invokes generateCrossSettlementConflicts during render ('live') with no active seeded PRNG, so rngContext falls back to Math.random: the same settlement shows different conflict NPCs/text after every reload, and the PDF (which reads only persisted s.crossSettlementConflicts, pdf/lib/viewModel.js:1037) omits them entirely — a dossier/PDF parity gap.

**Fix:** Invert the relType (patron↔client) when building the mirrored description, and either persist the live-generated conflicts at generation/link time or seed them deterministically from the settlement seed + linkId.

**Fix note:** Fix for the patron contradiction: when building the mirrored (forB) record, invert the relType's directional sense rather than just swapping args — e.g. pass a per-side role-aware relType so the patron template renders the correct settlement as patron on each side (the generator currently has no 'client' templates, so a 'client' template set would need to be added, or the patron template made directional). For the parity/determinism part: persist the conflicts at generation/link time (the live 'live'-path output is never saved) OR seed the live generation deterministically from the settlement seed + linkId so it is stable across reloads. Separately, note the dead crossSettlementConflicts field — nothing writes it; either wire SettlementsPanel to populate it (and have the PDF/screen read it) or drop the dead read at viewModel.js:1037 and Relationships.jsx:190 to avoid future confusion.

### [LOW] src/generators/economicGenerator.js:1163 — Subsistence prosperity roll yields 70% Struggling / 30% Poor — matches neither of its own two documented distributions

**Evidence:** Comment at line 1161 says '35% chance of Struggling'; inline comment at 1163 says '40% Struggling, ~30% Poor, ~30% luckier'. The code `idx = _rng() < 0.4 ? 0 : _rng() < 0.5 ? 0 : 1` produces 0 with probability 0.4 + 0.6*0.5 = 0.7 and 1 with 0.3 — both branches of the middle case land on 0, and the 'luckier' outcome is impossible even before the `Math.min(1, idx)` cap. The prosperity label for every isolated thorp/hamlet is skewed: 70% Struggling instead of the documented 35-40%.

**Fix:** If the inline comment is the intent: `idx = _rng() < 0.4 ? 0 : 1` (40% Struggling, 60% Poor after the cap), or make the middle branch return 1.

**Fix note:** The simple fix `idx = _rng() < 0.4 ? 0 : 1` changes the number of _rng() draws on 60% of paths (1 instead of 2), which shifts the seeded PRNG stream and changes all downstream values for existing seeds — any snapshot baselines would need regeneration. A fix that matches the inline comment's three-way intent and keeps the existing cap meaningful is `idx = _rng() < 0.4 ? 0 : _rng() < 0.5 ? 1 : 2;` (40% Struggling, 30% Poor, 30% 'luckier' capped to Poor by line 1164 → net 40/60). Whichever is chosen, the leading comment's '35% chance of Struggling' on line 1161 also disagrees with both candidate fixes and should be updated to match the chosen distribution.

### [LOW] src/generators/economicGenerator.js:1752 — River Tolls income matches bare 'dock' — 'Airship docking' counts as river dock, contradicting the adjacent comment that fixed the port branch for exactly this

**Evidence:** The comment at lines 1742-1745 states Port Duties keys on 'docks/port' specifically so "'Airship docking' never reads as a harbour", and the port branch (line 1746) was fixed accordingly. But the river branch right below (line 1752) still uses `hasInst('dock', 'port facilit') && tradeRoute === 'river'` — `'airship docking'.includes('dock')` is true, so a river-route settlement whose only 'dock' is an airship dock collects River Tolls ('Tolls on river traffic, dock fees, and ferry rights') attributed to nonexistent river infrastructure.

**Fix:** Use the same guarded pattern as the port branch, e.g. hasInst('docks/port', 'river dock', 'boatyard', 'ferry') or exclude names containing 'airship'.

**Fix note:** The minimal correct fix is to drop the redundant bare 'dock' keyword: hasInst('port facilit') (or 'docks/port') already matches the only real dock institution, 'Docks/port facilities'. Note the suggested keys in the report are partly wrong: there is no 'river dock' institution in the catalog; river-flavored institutions are 'River boatyard', 'River ferry', and 'Barge and river transport company' (institutionalCatalog.js:474, 483, 1544). Whether those should also qualify for River Tolls (the desc mentions 'ferry rights') is a design decision beyond the bug fix. Also remember hasInst lowercases institution names but not keywords, so any added keywords must be lowercase. A river-route regression test mirroring ports.test.js:214-221 ('Airship docking on a river route earns no River Tolls') would close the untested seam.

### [LOW] src/generators/economicGenerator.js:2019 — User-visible income-source text corrupted by bad find/replace: 'weavers and fullers convert SEVERITY to cloth'

**Evidence:** The 'Wool & Textile Trade' income source desc reads 'Local flocks provide raw wool; weavers and fullers convert SEVERITY to cloth sold across the region.' (line 2019). 'SEVERITY' is the name of an imported constant (line 21) — a refactor/rename clearly clobbered a word ('it'/'fleece') inside a string literal. This desc is rendered in the dossier's income sources for any settlement with grazing_land + weaver/fuller institutions.

**Fix:** Restore the intended word, e.g. '...weavers and fullers convert it to cloth sold across the region.'

**Fix note:** The suggested fix is correct: restore 'convert it to cloth' (the original word was almost certainly 'it', clobbered by a whole-word rename of a minified identifier to SEVERITY). A repo-wide grep found no other string literals corrupted by the same rename, so the one-line fix is complete.

### [LOW] src/generators/economicGenerator.js:2579 — sortBySeverity comparator yields NaN for the 'warning'/'note' severities that generateEconomicViability itself pushes

**Evidence:** sortBySeverity (lines 2579-2582) maps only SEVERITY.CRITICAL/IMPLAUSIBLE/DEPENDENCY/INEFFICIENCY. generateEconomicViability pushes issues with `severity: 'warning'` (line 2642) and warnings with `severity: 'note'` (line 2654); buildFactionList pushes stress issues with raw 'critical'/'implausible' strings (line 644). For unmapped severities `s[o.severity] - s[d.severity]` is NaN, making the sort order implementation-defined for those entries — the viability list ordering (critical-first) silently breaks whenever siege/isolation dependency issues are present.

**Fix:** Add 'warning' and 'note' (and raw-string fallbacks) to the rank map, or default unknown severities with `?? 4`.

**Fix note:** Drop the part of the fix about line 644 raw-string fallbacks — 'critical'/'implausible' already equal the SEVERITY constant values and rank correctly. The needed fix is just explicit ranks for 'warning' and 'note' (deciding intentionally where they belong, e.g., 'warning' between dependency and inefficiency, 'note' last) plus a default for unknowns, e.g. `(s[o.severity] ?? 9) - (s[d.severity] ?? 9)`, so the comparator is always consistent.

### [LOW] src/generators/generateSettlementPipeline.js:78 — Section regen (regenNPCsPipeline / regenHistoryPipeline) runs generators with no seeded PRNG — silent Math.random fallback breaks seed-replayability of the stored settlement

**Evidence:** regenNPCsPipeline (lines 78-102) and regenHistoryPipeline (lines 107-112) call generateNPCs/generateRelationships/generateFactions/generateConflicts/generateHistory directly, without createPRNG, without setActiveRng, and without minting/recording any seed. Those generators draw randomness through rngContext helpers, which silently fall back to Math.random() when no rng is active (src/generators/rngContext.js:35-37). They are invoked from the store's regenSection action (src/store/settlementSlice.js:690-693) and the results are Object.assign'd onto the live settlement, which still carries its original _seed. prng.js:76-79 documents generateSeed() as 'the ONE place non-determinism enters' — this path is a second, undocumented one. Consequence: after a section regen, the persisted settlement's _seed no longer reproduces its own NPCs/history (snapshot/property tests and the 'same seed => same settlement' contract no longer hold for that object), and there is no recorded seed that does. Even if 'reroll = intentionally different' is the product intent, the roll should be seeded and recorded so the result is replayable.

**Fix:** Mint a seed (or derive one, e.g. `${settlement._seed}::regen-npcs::${counter}`), create a PRNG from it, wrap the generator calls in setActiveRng/clearActiveRng (try/finally), and record the regen seed on the settlement so the section regen is reproducible.

**Fix note:** The suggested fix is directionally right but needs three adjustments. (1) Prefer minting via generateSeed() and recording it (e.g. settlement._regenSeeds = [...(settlement._regenSeeds||[]), { section, seed }]) over deriving `${_seed}::regen-npcs::${counter}` — a derived counter must itself be persisted on the settlement or reloaded/saved settlements will reuse counter values and produce identical "rerolls". (2) The setActiveRng/clearActiveRng try/finally must wrap the ENTIRE body of both regen functions (generateRelationships, generateFactions, generateConflicts, and generateHistory all draw randomness, not just generateNPCs). (3) Recording the regen seed alone does not restore the "same _seed reproduces this object" contract — true replay would require re-applying recorded regen seeds on top of the base pipeline; and it does not fix the related seam that applyChange (settlementSlice.js:864-891) re-runs the full pipeline from lastSeed and silently discards any prior section regen regardless. That interaction deserves its own decision (either replay recorded regens after applyChange, or surface that applying a change discards section rerolls).

### [LOW] src/generators/historyGenerator.js:835 — Timeline event variety is collapsed: one fixed template per category and an effective ~8-event cap that makes the metropolis 20-event budget unreachable

**Evidence:** Inside the event loop, `HISTORICAL_EVENTS_DATA.find(e => (typeMap[cat]||[]).includes(e.type))` (lines 835-856) always returns the FIRST template in data order for each category even though typeMap lists 4-8 types (e.g. political lists 8 types but always selects 'succession_crisis', the first in HISTORICAL_EVENTS_DATA). Combined with usedCats/usedNames dedup (lines 824-861), at most ~8 distinct events can ever be produced before every iteration `continue`s — while the metropolis branch budgets up to 20 (line 799). The '(simplified — use a generic template)' comment acknowledges the template part but the count budget silently never binds, and political variety (corruption_scandal, leadership_vacuum, disputed_land, ...) only appears via the (index-buggy) anchor pass fallbacks.

**Fix:** Pick randomly among the typeMap-listed templates (seeded) instead of .find, and dedupe by template name while allowing multiple events per category for large tiers.

**Fix note:** Suggested fix is sound: seeded-pick among the typeMap-listed templates rather than .find, and dedupe by template type while permitting multiple events per category for large tiers so the metropolis budget can actually bind. Caveat to the reviewer's wording: the extra political templates do NOT currently surface via the anchor pass — that pass also collapses political to a single 'succession_crisis' string, so corruption_scandal/leadership_vacuum/disputed_land/etc. are unreachable dead data in generateRelationshipEvent until the .find selection is fixed. Also note the parallel typeMap inside the anchor pass (lines 894-900) needs the same treatment, otherwise re-anchored events stay collapsed even after fixing the main loop.

### [LOW] src/generators/historyGenerator.js:868 — Custom settlement ages 1-4 years produce events older than the settlement (negative timeline years)

**Evidence:** resolveSettlementAge custom mode accepts any 0-5000 (lines 38-41); generateHistory gates the timeline only on age > 0 (line 1048). Event placement uses `randInt(Math.max(minYearsAgo, 5), maxYearsAgo)` (line 868): for age < 5 the min bound (5) exceeds maxYearsAgo, and randInt(5, 4) collapses to 5 — yielding yearsAgo=5 in a settlement aged 1-4, so eventsTimeline's `year: age - e.yearsAgo` (line 1079) goes negative and the dossier shows an event predating the founding.

**Fix:** Clamp the lower bound to Math.min(5, age) and skip timeline generation when age < ~5, mirroring the age<=0 path.

**Fix note:** The suggested fix is sound. Two refinements: (1) The minimum bound of 5 is an undocumented "events must be at least 5 years old" floor — the cleanest fix is to clamp the lower bound to Math.min(5, age) at line 868 (i.e. const lo = Math.min(5, Math.max(minYearsAgo, ... )) but capped at maxYearsAgo) so yearsAgo can never exceed age. Equivalently, clamp yearsAgo with Math.min(yearsAgo, age) after the randInt. (2) Skipping timeline generation for small ages (mirroring the age<=0 path) also works but changes behavior for ages 5-? — note the boundary is exactly age<5, so the skip threshold should be age < 5, not "~5", to avoid suppressing valid timelines at age 5. Also note thorp tier is already unaffected (no Math.max floor), so any guard need only target the tiers with a forced eventCount floor; a clamp at line 868/1079 covers all tiers uniformly and is the lower-risk fix.

### [LOW] src/generators/npcGenerator.js:67 — generateFactionLeader's institutions parameter is never supplied — institution-presence secret weighting is permanently in 'absent' mode

**Evidence:** generateFactionLeader(category, config, institutions = []) keys secret-type weights on hasCriminal/hasMagic/hasReligion derived from the institutions list (lines 385-411: criminal ×1.4 vs ×0.8, magical ×1.3 vs ×0.6, religious ×1.3 vs ×0.6). Its only call site is generateSingleNPC line 67: `generateFactionLeader(category, config)` — two args, so institutions defaults to [] for every NPC ever generated. A cathedral city and a godless mining camp weight religious secrets identically; only the priority sliders move the weights.

**Fix:** Thread the institutions list from generateNPCs → generateSingleNPC → generateFactionLeader (it is already in scope at the generateNPCs level).

**Fix note:** Fix is correct but note generateSingleNPC currently has NO institutions parameter (signature is (role, namingTier, category, culture, tier, config)), so threading requires adding a new param to generateSingleNPC and updating all three call sites: line 1495 and 1523 (generateNPCs has `institutions` in scope from line 1235) and line 211 in filterByGuild (which also receives institutions as its first arg). Alternatively, since npcConfig is already built at line 1484 and passed to every generateSingleNPC call, the lowest-touch fix is to stash institutions onto npcConfig (e.g. _institutions) and read config._institutions inside generateFactionLeader — avoiding signature churn on generateSingleNPC entirely.

### [LOW] src/generators/npcGenerator.js:808 — ROLE_FACTION_MAP duplicate keywords shadow the noble/crafts mappings — manor lords and knights always join the governing/military faction

**Evidence:** mergeNPCLists' ROLE_FACTION_MAP is first-match-wins (lines 929-934). 'lord' appears at line 808 mapped to govFaction and again at line 873 mapped to nobleFaction; 'knight' at 822 (milFaction) and 877 (nobleFaction); 'overseer' at 853 (econFaction) and 891 ('journeyman overseer' → craftsFaction). The later noble/crafts entries are unreachable, so a 'Lord/Lady of the Manor' or 'Knight/Dame' NPC would be affiliated with the Town Council or Military/Guard even when a Noble Families faction exists. (Currently masked for nobles by the generateNPCs powerStructure gap, but it bites event-created and structural noble NPCs and will bite the moment that gap is fixed.)

**Fix:** Order more-specific keywords first ('lord of the manor', 'noble', 'journeyman overseer') or check noble keywords before the generic government block.

**Fix note:** Suggested fix (order specific keywords before generic blocks / check noble keywords before the government block) is correct. While fixing, also move ['crime lord', crimeFaction] above ['lord', govFaction], since the same first-match-wins shadowing currently routes a 'Crime Lord' NPC to the governing faction instead of the criminal faction — that instance is more impactful than the noble/crafts ones the report scopes. A more robust fix is to sort ROLE_FACTION_MAP by descending keyword length before matching, so longer/more-specific phrases ('journeyman overseer', 'crime lord', 'lord of the manor') always win over shorter substrings.

### [LOW] src/generators/npcGenerator.js:1447 — Mandatory NPC roles 'Fisher', 'Woodcutter', and 'Merchant Guild Master' don't exist in the role catalog — silently skipped

**Evidence:** thorpSecondRole returns 'Fisher' for fishing/port/coastal/river thorps and 'Woodcutter' for forest/isolated ones (lines 1442-1453); famine's STRESS_MANDATORY_ROLES includes 'Merchant Guild Master' (line 1468). The mandatory loop resolves roles via `candidates.find(c => c.role === role)` (line 1493) against HISTORY_EVENTS, which contains 'Fisherman' (historyData.js:1007) but no 'Fisher', no 'Woodcutter' at all, and 'Guild Master' but no 'Merchant Guild Master' (verified programmatically). The find misses and the intended archetype NPC never spawns — confirmed empirically: four coastal/port thorp generations produced Elder + random fill (Blacksmith, Traveling Merchant, ...) and never a fisher.

**Fix:** Rename the mandatory strings to match catalog roles ('Fisherman', 'Guild Master') and add a Woodcutter role to HISTORY_EVENTS.other.

**Fix note:** Rename mandatory strings to match the catalog: 'Fisher' -> 'Fisherman' (lines 1446, 1449, 1452) and 'Merchant Guild Master' -> 'Guild Master' (line 1468). For 'Woodcutter' there is no equivalent catalog role, so it must be ADDED to HISTORY_EVENTS.other in src/data/historyData.js with minTier:'thorp' (mirroring the existing Miller/Shepherd/Fisherman 'other' entries) before lines 1447/1450 will resolve. Note 'Guild Master' requiresGuild:true and minTier:'village', so it will only resolve for famine settlements that are village+ with a guild institution — acceptable since famine NPC sets target larger tiers, but worth confirming.

### [LOW] src/generators/npcGenerator.js:1481 — politically_fractured intends two rival Council Members but Set-dedup collapses them to one

**Evidence:** STRESS_MANDATORY_ROLES.politically_fractured is ['Council Member', 'Council Member'] (line 1470) — clearly intending two opposed council members for a fractured settlement — but mandatoryRoles is built as `[...new Set([...tierRoles, ...stressRoles])]` (line 1481), which dedupes the pair to a single entry. The mandatory loop also looks up one shared candidate per role name, so even without the Set only one would spawn.

**Fix:** Allow duplicate mandatory roles (count occurrences instead of a Set) and let the loop generate one NPC per occurrence.

**Fix note:** The suggested fix is correct in direction. Concretely: stop deduping the stress roles. Keep tier roles deduped if desired, but allow stress-role duplicates, e.g. build mandatoryRoles as [...new Set(tierRoles), ...stressRoles] (or otherwise count occurrences) so the forEach at line 1492 runs once per occurrence and pushes two Council Member NPCs. Note the loop already calls generateSingleNPC fresh per iteration, so it will produce two distinct NPCs without further changes; the find() returning the same candidate object is fine because only role/title/category are read from it. Also confirm the downstream fill-stage usedRoles filter (line 1506-1507) is acceptable — it correctly prevents a THIRD council member, which is the desired cap.

### [LOW] src/generators/powerGenerator.js:1561 — Faction rank-comparison narratives are baked before legitimacy multipliers re-scale and re-sort powers — descriptions can contradict final standings

**Evidence:** The IIFE at lines 1561-1802 appends positional prose to faction descs based on the normalized pre-legitimacy ordering ('The gap between them and the governing council is narrow', 'The craft guilds currently outweigh them in raw political numbers', etc., keyed on Ct===1/2 positions and power deltas). applyLegitimacyMultipliers is applied AFTERWARD (line 2187) and multiplies governing power by 0.60-1.30 and criminal by 0.75-1.30, then re-sorts the array by effective power (factionDynamics.js:511). Whenever legitimacy is outside the Tolerated band (score <45 or >=60 — common), the baked comparisons and rank phrasing can invert relative to the powers and ordering the dossier actually displays.

**Fix:** Move the rank-narrative pass after applyLegitimacyMultipliers (it only reads the factions array), or compute it from rawPower consistently and label it as such.

**Fix note:** The suggested fix is sound and low-risk. Moving the entire IIFE (powerGenerator.js:1561-1802) to run AFTER applyLegitimacyMultipliers (line 2187) is the cleanest option: the IIFE only reads the `p` array's `.faction`, `.power`, `.isGoverning` fields and mutates `.desc`, so it is order-independent except that it must observe the final powers/ordering. Caveat to flag for the implementer: applyLegitimacyMultipliers's pure-power sort (factionDynamics.js:511) does NOT keep the governing faction at index 0, whereas the pre-legitimacy sort (line 1560) did. The IIFE's `Ct===1`/`Ct===2` positional prose and `he = p[0]` assume position 0 is meaningful (and the prose explicitly special-cases "the governing council" as p[0]). After moving, p[0] is whoever has the highest effective power, which may not be the governing faction — that is actually the *correct* behavior for the prose to describe, but the implementer should confirm the "governing council" phrasing still reads coherently when the governing faction is not on top (it likely should, since that scenario is exactly a legitimacy crisis the prose can speak to). Alternatively, compute the comparisons from `rawPower` consistently and explicitly label them as pre-performance/structural standing — but mixing rawPower prose with effective-power display is more confusing than just regenerating against final powers.

### [LOW] src/generators/steps/neighbourFactions.js:33 — Neighbour-injected factions bypass normalization, labels, and the relationship matrix

**Evidence:** neighbourFactions runs after generatePower and pushes mirror/opposition factions with raw power randInt(10,30)/randInt(8,26) (lines 33-41, 66-74) into an array whose other members were normalized to sum 100 and then legitimacy-multiplied. The injected entries get no rawPower/powerLabel (applyLegitimacyMultipliers already ran — PowerTab.jsx:192 renders no badge, while the PDF defaults to 'Influence', pdf/sections/PowerStructure.jsx:279 — a small dossier/PDF divergence), are appended unsorted (array elsewhere sorted by power), and are excluded from powerStructure.factionRelationships. genCoherence's 'holds X% of power' notes (narrativeGenerator.js:799, 858) also become inaccurate once total power exceeds 100.

**Fix:** After injection, stamp powerLabel/rawPower, re-sort, and either renormalize or scale injected powers to the post-normalization scale; optionally extend computeFactionRelationships to cover neighbour factions.

**Fix note:** Fix is sound. After injection, stamp powerLabel/rawPower on each new faction (reuse the same threshold logic in applyLegitimacyMultipliers — ideally factor that label computation into a shared helper so PowerTab/PDF stay consistent), then re-sort powerStructure.factions by effective power. For scale coherence, either renormalize the whole array back to sum-100 after injection or scale the injected raw powers into the post-normalization band before pushing (a fixed randInt(10,30) is roughly a third of a 100-point pie for a single neighbour faction, which over-weights them and is what makes the '% of power' notes drift). Extending computeFactionRelationships to cover neighbour factions is genuinely optional and higher-cost; the label/sort/scale fixes are the load-bearing part. Note one nuance for the fix author: the existing faction's own displayed '% of power' value is literally unchanged — the inaccuracy is purely that the set no longer sums to 100, so renormalization (not just relabeling) is what actually restores that note's correctness.

### [LOW] src/generators/steps/resolveConfig.js:86 — resolveConfig's noMagic treats an absent priorityMagic as 0 while the effective magic priority defaults to 50 — contradictory magic semantics for sparse configs

**Evidence:** Line 67: `priorityMagicEffective = config.magicExists === false ? 0 : (basePriorityMagic ?? 50)` — undefined defaults to 50 (magicLevel 'medium'). Line 86: `noMagic = config.magicExists === false || (basePriorityMagic || 0) === 0` — undefined collapses to 0, so noMagic = TRUE. A town+ config that simply omits priorityMagic (headless callers, older saved _config blobs) is simultaneously treated as a medium-magic world (magicLevel, all downstream magic gates) AND a no-magic world for trade-route resolution: 'isolated' is filtered out of random route pools (line 105/109) and an explicit 'isolated' choice is force-rewritten to 'road' (line 119) with a trace claiming 'no-magic forces road access' — even though isolationPass would happily seat a teleportation circle in this medium-magic world. servicesGenerator.js:1964 uses the opposite (correct) convention: `(d.priorityMagic || 50) === 0`. The app's DEFAULT_CONFIG always sets priorityMagic:50, so the main wizard path is unaffected — this bites headless/legacy-config generation only.

**Fix:** Change line 86 to `(basePriorityMagic ?? 50) === 0` so an unspecified magic priority matches the effective default, mirroring servicesGenerator's convention.

**Fix note:** The suggested fix `(basePriorityMagic ?? 50) === 0` is correct and consistent with the codebase convention. A slightly tighter equivalent: `const noMagic = priorityMagicEffective === 0;` — line 67 already encodes both conditions (magicExists===false → 0, else basePriorityMagic ?? 50), so deriving noMagic from it makes the two values impossible to drift apart again. Either form is behavior-identical for all explicit-config cases and passes the existing resolveConfigReceipts tests, which only use explicit magicExists/priorityMagic values.

### [LOW] src/generators/steps/resolveConfig.js:86 — resolveConfig: noMagic=true while priorityMagicEffective=50 when config.priorityMagic is undefined — contradictory magic signals

**Evidence:** `priorityMagicEffective = config.magicExists === false ? 0 : (basePriorityMagic ?? 50)` (lines 66-67) defaults an UNSET priorityMagic to 50 (magic moderately present), but `noMagic = config.magicExists === false || (basePriorityMagic || 0) === 0` (line 86) treats the same unset value as 0 → noMagic = true. A config missing priorityMagic (older saves, API-built configs) then gets isolated routes filtered/rewritten to road on no-magic grounds (lines 105, 119) and townPlus+isolated traces claiming 'no-magic forces road access', while the rest of the pipeline (magicLevel, chain magic substitution, magic income) runs at priority 50.

**Fix:** Compute noMagic from the same resolved value: `const noMagic = config.magicExists === false || priorityMagicEffective === 0;`.

**Fix note:** The suggested fix is correct and matches the canonical semantics in magicFilter.js:43. Two notes: (1) since priorityMagicEffective is already forced to 0 when magicExists === false (line 66-67), the fix simplifies to `const noMagic = priorityMagicEffective === 0;` — keeping the explicit magicExists clause is harmless but redundant. (2) The fix changes rng pool composition under tradeRouteAccess='random_trade' for configs lacking priorityMagic (isolated is no longer filtered out), so route outcomes for existing seed+config combos in that category will shift — check whether any snapshot tests use such minimal configs before landing.

### [LOW] src/generators/steps/stepMetadata.js:23 — corruptionPass is registered in the pipeline but missing from STEP_METADATA; the 'loud in DEV' sync test claimed in the file header does not exist

**Evidence:** steps/index.js:24 imports corruptionPass.js (registered step, runs every generation), but STEP_METADATA (lines 23-144) has no corruptionPass entry — metaForStep falls back to `{ label: name, ... }`, so the user-facing pipeline rail/reveal shows the raw machine name 'corruptionPass' with an empty description. The file's own header (lines 8-11) says 'Keep entries here in sync with the registrations in steps/index.js … Both are loud in DEV through the test below' — but no test imports STEP_METADATA/stepMetadata (repo grep: only src/components/PipelineRail.jsx and src/store/settlementSlice.js import it; tests/domain/trace.coverage.test.js:167-178 merely allowlists step names for traces, and its VALID_STEPS set also omits corruptionPass — it passes only because corruptionPass emits no traces).

**Fix:** Add a STEP_METADATA entry for corruptionPass (label e.g. 'Seed corruption', summary counting npcs with npc.corrupt === true), and add the promised sync test: import getStepMeta() after importing steps/index.js and assert every registered step name has a STEP_METADATA entry (and vice versa).

**Fix note:** The suggested fix is sound. Two refinements: (a) the summary should account for the step's no-op path — corruptionPass only sets npc.corrupt when a criminal institution exists, so use something like counting ctx.npcs with corrupt === true and returning 'No corruption climate' (or null) otherwise; (b) the sync test needs a way to enumerate registered step names — check whether pipeline.js exports the registry (e.g. a getRegisteredSteps/listSteps helper); if not, one must be added or the test can import steps/index.js and read the registry via an existing export. Also worth fixing in the same pass: tests/domain/trace.coverage.test.js:167-174 — its VALID_STEPS allowlist omits 'corruptionPass' (a latent misleading failure if the step ever emits traces) and contains the bogus entry 'stepMetadata', which is not a registered step; deriving that set from the registry instead of hardcoding would close both.

### [LOW] src/generators/steps/stepMetadata.js:23 — corruptionPass step registered but missing from STEP_METADATA; the 'loud in DEV' guard test it references does not exist

**Evidence:** steps/index.js:24 imports './corruptionPass.js' (19 registered steps; registration name 'corruptionPass' confirmed by extracting registerStep names from dist/assets/engine-D8TwO_ZR.js, and the built STEP_METADATA object in the same bundle has 18 keys — no corruptionPass). metaForStep (stepMetadata.js:151-153) falls back to `{ label: name, description: '' }`, so the user-facing 'How this was simulated' rail (PipelineRail.jsx:50) renders the raw machine name 'corruptionPass' with no description for every settlement. The file's own header (stepMetadata.js:8-11) says 'Keep entries here in sync with the registrations in steps/index.js … Both are loud in DEV through the test below' — but no such test exists anywhere in tests/ (the only files mentioning STEP_METADATA/metaForStep are the source files and tests/generators/corruptionPass.integration.test.js, which tests corruption logic, not metadata sync). Adjacent drift from the same step-count growth: copy/en.js pipelineSteps (lines 471-487) has only 15 of 19 steps (missing stressConfirmPass, economyReconcilePass, structuralValidationPass, corruptionPass — the reveal overlay skips unlabeled steps by design, PipelineReveal.jsx:53-61), and en.js:284 onboarding copy still tells users 'the rail on the right shows the fourteen steps the engine took' while the rail now lists 19.

**Fix:** Add a corruptionPass entry to STEP_METADATA (label/description/summary), write the promised sync test (import steps/index.js, compare getStepMeta() names against Object.keys(STEP_METADATA)), and update the en.js 'fourteen steps' copy.

**Fix note:** Suggested fix is sound. Concretely: (a) add a corruptionPass entry to STEP_METADATA with label/description/summary (e.g. summary counting ctx.npcs that carry a corruption vector, returning null when none — keeping the no-op case quiet); (b) write the promised sync test that imports steps/index.js to populate the registry, then asserts the set of names from pipeline.js getStepMeta() equals Object.keys(STEP_METADATA) (this catches both directions of drift); (c) update en.js:284 "fourteen steps" copy to be count-agnostic (e.g. "the steps the engine took") rather than hardcoding a new number that will rot again. Optionally also backfill the 4 missing en.js pipelineSteps entries if those steps are meant to appear in the reveal overlay — but note that omission is currently intentional-by-filter (PipelineReveal.jsx:53-61), so adding them changes the overlay; decide deliberately rather than as part of this fix.

### [LOW] src/generators/steps/stepMetadata.js:27 — Five more STEP_METADATA summaries read context keys the pipeline never writes — their factual receipts can never render

**Evidence:** Cross-checked every summary against the registered steps' actual patches (source for the 8 non-stripped step files; the dist engine bundle's provides lists for the rest — extracted from dist/assets/engine-D8TwO_ZR.js): (1) resolveConfig.summary reads `ctx.config?.tier` (line 27) but the raw input config uses `settType`; resolveConfig builds a NEW `effectiveConfig` (resolveConfig.js:144-160) and returns top-level `tier` — it never mutates ctx.config, and settlementSlice strips `tier` from any config fallback (DERIVED_CONFIG_KEYS, settlementSlice.js:104-110), so 'Target size: …' never renders. (2) resolveResources.summary reads `ctx.resources?.local` (lines 32-35) but the step returns `{ nearbyResources, nearbyResourcesDepleted, nearbyResourcesCustom }` (resolveResources.js:265). (3) generateEconomy.summary reads `ctx.economy?.prosperityBand` (lines 87-90) but the step provides only `economicState` (dist provides list). (4) generatePower.summary reads `ctx.power?.factions || ctx.factions` (lines 95-97) but the step provides `powerStructure`, and `factions` is only provided by the LATER generatePopulation step. (5) factionCorrelationPass.summary reads `ctx.tensions || ctx.history?.currentTensions` (lines 108-111) but the step provides [] and `history` is only provided by the later generateNarratives step; the string `tensions:` appears zero times in the entire engine bundle. All five therefore silently return null forever, so the rail (whose stated purpose is showing 'what *actually* happened on this run', stepMetadata.js:17-20) loses its factual line for config, resources, economy, power, and tensions. Same lambdas confirmed present in the production bundle.

**Fix:** Point each summary at the real context keys (effectiveConfig.tier / nearbyResources / economicState / powerStructure.factions / a tensions key actually threaded through ctx), and add the missing metadata-vs-pipeline sync test.

**Fix note:** Reviewer's fix targets are correct. Concretely: (1) ctx.effectiveConfig?.tier (or ctx.tier); (2) ctx.nearbyResources?.length; (3) ctx.economicState?.prosperityBand (verify the actual field name inside economicState — confirm it exposes a prosperity band rather than a numeric score); (4) ctx.powerStructure?.factions?.length. (5) is the hardest: factionCorrelationPass produces no tensions value at all — it only records _rosterChangedAfterEconomy and pushes faction-pulled institutions. There is no live `tensions`/`history` key at that step, so the summary cannot simply be re-pointed; either thread a real correlation/tensions count through the patch (the step has powerStructure.factions and computes factionBoosts, so a faction-correlation count is available) or relabel the summary to what the step actually does (e.g. faction-pulled-institution count via _rosterChangedAfterEconomy / boostAdditions). Because each summary is snapshotted at its own onStep, fixes must reference keys that already exist AT THAT STEP — relying on a later step's provides won't help. The reviewer's call to add a metadata-vs-pipeline sync test is the durable guard: assert every STEP_METADATA.summary reads only keys present in the cumulative provides set up to and including that step.

### [LOW] src/generators/stressGenerator.js:165 — monster_pressure gets walls/military suppression applied twice in buildStressContext, skewing stressConfirmPass keep-probability

**Evidence:** buildStressContext has two blocks that both match monster_pressure: lines 95-102 (`if (stressType === 'under_siege' || stressType === 'monster_pressure')`) apply hasWalls x0.6 and hasMilitary x0.7, then lines 165-170 — a monster_pressure-only block whose comment scopes it to 'timber boosts — forests hide monsters' — re-apply the identical hasWalls x0.6 and hasMilitary x0.7 on top of the timber x1.3. Net suppression is x0.36 (walls) / x0.49 (military) instead of the single x0.6/x0.7 every other stress type gets. At initial roll time (resolveStress) institutions=[] so it's inert, but stressConfirmPass (steps/stressConfirmPass.js:94-99) computes keepProbability = clamp(modified/unmodified, 0.4, 1) with the real roster: a walls-only town's monster_pressure keep-probability becomes the 0.4 clamp floor instead of 0.6 (60% drop chance vs the intended 40%). The duplicated lines read as a copy-paste remnant since the first block already explicitly covers monster_pressure.

**Fix:** Remove the hasWalls/hasMilitary lines from the second monster_pressure block (keep only the hasTimber x1.3), or remove monster_pressure from the first shared block — pick one location for the fortification suppression.

**Fix note:** Prefer the first option: delete the hasWalls/hasMilitary lines from the second block (lines 168-169), keeping hasTimber ×1.3. The shared block's grouping with under_siege matches the documented model (ordering.test.js:206 comment, SUPPRESSOR_KEYWORDS parity in stressConfirmPass.js). The fix changes no rng draw counts (buildStressContext consumes no rng; keepProbability stays <1 for affected rosters so the confirm-pass draw still occurs), and the existing ordering.test.js thresholds tolerate the 0.40→0.60/0.42 shift, but any settlement snapshot tests covering walled towns with emergent monster_pressure may need regeneration since drop outcomes can flip on some seeds.


## 09-build-gate-deploy (7)

### [HIGH] docs/DEPLOY.md:128 — DEPLOY.md deploys stripe-webhook without --no-verify-jwt — Stripe's webhook posts would be rejected before signature verification, so payments never credit

**Evidence:** docs/DEPLOY.md:128 says `npx supabase functions deploy stripe-webhook` with no `--no-verify-jwt`, while line 130 uses the flag for verify-single-dossier — so the author knows the flag; its absence on the webhook reads as a mistake, not style. Supabase functions deploy defaults verify_jwt=true (supabase/config.toml declares no per-function override), which makes the platform reject any request lacking a valid project JWT before function code runs. Stripe webhook deliveries carry only Stripe-Signature, no Authorization header, so every checkout.session.completed/invoice.paid event 401s and the function's own constructEvent signature check (the intended trust boundary per ARCHITECTURE.md:117-118, 'no user JWT on webhooks') never executes. On an environment provisioned by following DEPLOY.md verbatim, users are charged by Stripe but credits/subscriptions/founder seats are never granted — silent money loss until webhook failure alerts are noticed. scripts/deploy.sh:150 confirms the correct invocation (`stripe-webhook … --no-verify-jwt`), i.e. the two runbooks contradict each other on a money-critical flag.

**Fix:** Add `--no-verify-jwt` to the stripe-webhook deploy line in docs/DEPLOY.md (or add `[functions.stripe-webhook] verify_jwt = false` to supabase/config.toml so the flag can't be forgotten), and note in the doc why the function relies on Stripe signature verification instead of JWT.

**Fix note:** Both proposed fixes are valid; the config.toml option is the more durable one. Recommend adding `[functions.stripe-webhook]\nverify_jwt = false` to supabase/config.toml so the setting is enforced regardless of which runbook (DEPLOY.md vs scripts/deploy.sh) is followed and can't be forgotten on a manual deploy, AND adding `--no-verify-jwt` to DEPLOY.md:128 with a one-line note that the webhook trusts Stripe's signature (constructEvent) instead of a project JWT. Note also that create-checkout is deployed with --no-verify-jwt in scripts/deploy.sh:138 but WITHOUT it in DEPLOY.md:129 — create-checkout requires a Supabase JWT internally per the webhook's own comment (index.ts:149), so leaving verify_jwt on there may be acceptable, but the DEPLOY.md/deploy.sh divergence on that line is worth a glance too while fixing the webhook. Severity high (not critical) because it is a doc-only defect in one of two runbooks and the canonical scripts/deploy.sh path is correct, so only an operator following DEPLOY.md verbatim is affected.

### [HIGH] src/App.jsx:152 — Credit balance is fetched only once at app mount — never refreshed on in-session sign-in, so AI actions are blocked and the purchase modal is pushed despite server-side credits

**Evidence:** `fetchCreditBalance().then(bal => setCreditBalance(bal))` runs once in App's mount effect (App.jsx:127-154) and is the ONLY call site in the app (grep confirms). For an anonymous visitor it returns 0 (stripe.js:168-172 returns 0 when no user). authSlice's onAuthChange SIGNED_IN handler (authSlice.js:157-216) sets auth fields, replays auth intents, and sends welcome email — but never refetches credits. So after the primary funnel path (anon generates → signs up in the AuthModal without a page reload), `creditBalance` stays 0. aiSlice.requestNarrative/requestDailyLife client-gate on this stale value: `if (!elevated && creditBalance < cost) { aiError = 'Insufficient credits…'; setPurchaseModalOpen(true); }` (aiSlice.js:279-283, 450, 585) — telling a user with real ledger credits (e.g. the welcome grant) that they have none and pushing them to buy more, until they hard-reload. AuthModal also displays the stale `creditBalance` (AuthModal.jsx:126).

**Fix:** Refresh the balance on auth transitions: in authSlice's SIGNED_IN handler (or an App effect keyed on authUserId), call fetchCreditBalance() and setCreditBalance(); clear it on SIGNED_OUT. Alternatively drop the client-side pre-check and let the edge function's server-side ledger check be authoritative.

**Fix note:** The suggested fix is correct in direction. Refetch on auth transition is the right move. Concretely: in authSlice's SIGNED_IN/TOKEN_REFRESHED handler (authSlice.js:160-219), after setting state.auth, lazy-import stripe.js and call fetchCreditBalance().then(bal => set(state => { state.creditBalance = bal; })); on SIGNED_OUT (clearAuth) reset creditBalance to 0. An App effect keyed on authUserId would also work and is simpler to reason about (add fetchCreditBalance there with authUserId in the dep array), but note the existing mount effect imports stripe.js lazily, so keep that pattern. The alternate fix (drop the client pre-check, rely on the server spend_credits gate) is cleaner long-term and removes the whole stale-gate class, but you'd need to keep surfacing the server's `Insufficient credits. Need N, have M.` error and trigger the purchase modal from that server response instead of the client pre-check. Either way, when adding a refetch, gate it on a real user (fetchCreditBalance already returns 0 for anon) and avoid resetting a freshly-known post-spend balance.

### [MEDIUM] scripts/deploy.sh:136 — create-customer-portal edge function is invoked by live client code but absent from BOTH deployment runbooks

**Evidence:** src/lib/stripe.js:117 invokes `supabase.functions.invoke('create-customer-portal')` from startCustomerPortal(), which is wired into AccountPage.jsx:199 and PricingPage.jsx:273 ('Manage subscription' — the only way users reach the Stripe billing portal to cancel/update). The function exists at supabase/functions/create-customer-portal/index.ts (3.5KB). But scripts/deploy.sh deploys only 5 functions (lines 138-151: create-checkout, verify-single-dossier, generate-narrative, generate-chronicle, stripe-webhook) while presenting itself as 'Full Deployment Setup … provisions the complete backend' and printing 'Edge functions: 5 deployed' (line 270); docs/DEPLOY.md's deploy list (lines 128-134) deploys 7 — admin-actions and send-email are there, but create-customer-portal is missing from it too. Any environment provisioned per either runbook 404s on the portal call, so paying users cannot manage or cancel subscriptions until someone notices the missing function.

**Fix:** Add `npx supabase functions deploy create-customer-portal` to both deploy.sh and docs/DEPLOY.md (and consider a test/script that diffs `ls supabase/functions/` against the deploy lists so a new function can't be forgotten).

**Fix note:** Suggested fix is correct. Add `npx supabase functions deploy create-customer-portal` to scripts/deploy.sh (and fix the 'Edge functions: 5 deployed' tally on line 270, which is already wrong by 3) and to the docs/DEPLOY.md deploy list (lines 128-134). Stronger remediation: extend scripts/validate-edge-functions.mjs (already iterates supabase/functions/) to also assert every non-_shared function directory appears in both deploy.sh and docs/DEPLOY.md, wiring it into `npm run check` so a newly-added function cannot be silently forgotten from either runbook. Note: deploy.sh deploys generate-narrative/generate-chronicle WITHOUT --no-verify-jwt and the portal function requires an Authorization header, so deploy create-customer-portal with default JWT verification (no --no-verify-jwt), matching the doc's omission of the flag for it.

### [MEDIUM] scripts/deploy.sh:242 — deploy.sh has drifted from the real backend: deploys 5 of 8 functions, omits required secrets, and sets a reserved SUPABASE_-prefixed secret

**Evidence:** Beyond create-customer-portal (separate finding), deploy.sh also never deploys admin-actions (admin panel mutations) or send-email (the entire email lifecycle; DevEmailBanner exists specifically to warn when its Resend secrets are missing), both of which exist in supabase/functions/ and are deployed by the newer docs/DEPLOY.md list. Its secrets step (lines 233-242) sets STRIPE_PRICE_CREDITS_10/50 — the 'early beta' legacy SKUs per supabase/functions/create-checkout/index.ts:32-33 — but none of the ACTIVE catalog prices the function requires (STRIPE_PRICE_CREDITS_25/60/150, STRIPE_PRICE_FOUNDER_LIFETIME, STRIPE_PRICE_SINGLE_DOSSIER, lines 45-50), so every current purchase product resolves to an empty price id and create-checkout throws 'Price ID not configured'. It also omits RESEND_API_KEY/RESEND_FROM_EMAIL (required per DEPLOY.md:142-143). Finally line 242 passes SUPABASE_SERVICE_ROLE_KEY to `npx supabase secrets set` — the SUPABASE_ prefix is reserved by the Supabase CLI (rejected or skipped with a warning depending on CLI version; the runtime auto-injects that key anyway), so under `set -euo pipefail` (line 21) the script may abort at step 8. The script is effectively a stale snapshot of an older product (3 Stripe products vs today's 6+, 5 functions vs 8).

**Fix:** Either delete scripts/deploy.sh in favor of docs/DEPLOY.md, or update it: deploy all 8 functions, create/set the active price catalog from src/config/pricing.js, add the Resend secrets, and drop the SUPABASE_SERVICE_ROLE_KEY secrets-set line.

**Fix note:** The suggested fix is sound. Two refinements: (a) the function list to add is admin-actions, send-email, AND create-customer-portal (the reviewer notes the portal as a separate finding, but a corrected script must include all three to match DEPLOY.md's intent plus the 8th function); (b) for the SUPABASE_SERVICE_ROLE_KEY line, the cleanest fix is to simply drop it — the runtime auto-injects SUPABASE_URL/ANON_KEY/SERVICE_ROLE_KEY into edge functions, so it is both reserved-by-CLI and redundant. Preferred resolution given DEPLOY.md is the maintained source of truth: delete scripts/deploy.sh outright rather than carry two divergent deploy paths.

### [MEDIUM] tests/build/vendorPdfLazy.test.js:28 — vendor-pdf lazy-load contract tests are dist-gated and never run in CI — CI runs tests before build and dist/ is gitignored

**Evidence:** tests/build/vendorPdfLazy.test.js:28 wraps the three build-output contracts (vendor-pdf is its own chunk; chunk is 500KB-3MB; index.html emits no modulepreload for it) in `describe.runIf(distExists)`. dist/ is in .gitignore, and .github/workflows/ci.yml runs 'Run tests' (line 57-58, `npm run test`) BEFORE 'Build' (line 66-67, `npm run build`) with no test re-run afterwards; the husky pre-push `npm run check` uses the same tests-then-build order. So on every CI run and clean pre-push the dist-gated suite silently skips, despite the file's own header claiming 'in CI we run it after the build step' (lines 16-18). Worse, when a developer runs tests locally with a STALE dist/ lying around, the suite validates last build's artifacts rather than current code. Only the source-level half (dynamic-import grep of SettlementDetail.jsx / SingleDossierSuccessPage.jsx, lines 74-91) actually executes. A regression that merges the ~619KB-gz PDF stack into a first-paint chunk (e.g. via a manualChunks edit or a new static import path not covered by the two greps) would pass the whole gate.

**Fix:** Either add a CI step after Build that re-runs vitest on tests/build/ (e.g. `npx vitest run tests/build`), or reorder so build precedes the test step, and make the suite fail loudly (not skip) when dist/ is absent in CI (e.g. assert on process.env.CI).

**Fix note:** The suggested fix is correct. Minor sharpening: the cleanest option is a dedicated CI step after "Build" running `npx vitest run tests/build`, since the existing test step legitimately needs to run before build for the rest of the suite (and rebuilding dist would slow it). Additionally guard against the stale-dist false-pass by failing the suite when process.env.CI is set but dist/ is absent (replace describe.runIf(distExists) with logic that asserts presence under CI), so a misconfigured pipeline surfaces loudly instead of green-skipping. Also note the source-level greps only cover two named files (SettlementDetail.jsx, SingleDossierSuccessPage.jsx); a new static-import path elsewhere would evade both the greps and the skipped dist checks — worth a manualChunks-config-level assertion too.

### [MEDIUM] tsconfig.full.json:25 — tsconfig.full.json uses brace-expansion glob unsupported by tsc — src/components is silently excluded from the gate's typecheck

**Evidence:** tsconfig.full.json:25 includes `"src/components/**/*.{js,jsx}"`. TypeScript's tsconfig include/exclude globs support only `*`, `?`, and `**/` — brace expansion `{js,jsx}` is NOT supported (longstanding TS limitation; patterns containing `{…}` are matched literally), so this pattern matches zero files and tsc raises no error because the other includes still match files. The config's own header (lines 2-13) and ARCHITECTURE.md (lines 147-150, 172-174) both claim the gate typechecks 'the whole src logic tree (domain/store/lib/hooks/generators/components/pdf) … at zero errors', and the brief's gate description repeats it. In reality none of the ~160 files under src/components/ are typechecked; line 26's `src/pdf/**/*.js` additionally omits every pdf/*.jsx section (SettlementPDF.jsx, sections/, primitives/) — only pdf *.js like viewModel.js is covered. So JSDoc type errors in components/pdf-JSX ship without tripping `npm run typecheck`, while the team believes the punch-list holds at zero. (Could not execute tsc to demonstrate — node_modules package.json files are stripped in this snapshot — but `tsc --listFilesOnly` would show zero src/components entries.)

**Fix:** Replace the brace glob with two patterns (`src/components/**/*.js`, `src/components/**/*.jsx`), decide explicitly whether pdf/*.jsx should be included, and re-run `npm run typecheck` expecting a new punch-list from the never-checked components tree.

**Fix note:** Fix is correct. Replace line 25 with two patterns: "src/components/**/*.js" and "src/components/**/*.jsx". Also add "src/pdf/**/*.jsx" alongside line 26 to cover SettlementPDF.jsx, sections/, primitives/ (the dossier/PDF-parity bug class makes this especially worth covering). Then run `npm run typecheck` and expect a fresh punch-list of JSDoc errors from the ~163 components + 33 pdf JSX files that have never been checked — this gate change may surface many real errors that must be triaged before it goes green, so it is not a zero-risk one-line edit. Confidence ~0.97: directly reproduced with the project's own tsc (0 of 163 components and 0 of 33 pdf .jsx files appear in `tsc --listFilesOnly`).

### [LOW] src/App.jsx:578 — Mobile bottom nav silently evicts 'About' for signed-in users (and Gallery too under mobileSingleChrome) after Gallery was added to NAV

**Evidence:** The bottom-nav comment (lines 289-294) says it holds the 5 primary destinations including 'How To Use'. NAV now has 6 entries (gallery inserted before howto, lines 62-69). visibleNav hides 'map' for anon (5 items, all render), but for signed-in users visibleNav has 6 items and `visibleNav.slice(0, _readFlag('mobileSingleChrome') ? 4 : 5)` (line 578) drops 'howto' entirely — and with the flag on, drops 'gallery' too. Signed-in mobile users have no path to the About page (and no other mobile surface links it; HERO_LINKS is empty).

**Fix:** Pick the 5 slots intentionally (e.g. filter by an explicit mobile-priority list) instead of slicing the desktop NAV order, or move About into the account menu on mobile and document the eviction.

**Fix note:** The fix direction is sound. One factual nuance to capture: About is not entirely unreachable on mobile for signed-in users — AccountPage.jsx:569 has a deep link to /how-to?tab=faq reachable via the account chip → Account page FAQ section. So this is "no primary nav affordance" rather than "no path at all," which is why severity stays low. Preferred fix: drive the bottom nav from an explicit mobile-priority list rather than slicing desktop NAV order, so adding/reordering NAV entries can't silently evict a destination again (this is the recurring seam). Also update the stale comment at App.jsx:290-291, which still names "How To Use" as one of the 5 primary destinations that the slice now drops for signed-in users.
