# DESIGN_W_SEAT_VERIFICATION — the seven census lane reports (2026-08-29, workflow wf_f54ab771-ebc)

**The receipts layer for `docs/DESIGN_W_SEAT.md`. Where the volume and these reports disagree, the quoted code wins. Reports verbatim.**


---

# V1 — DECISION SURFACE

LANE V1 — THE DECISION SURFACE (census of src/domain/worldPulse at 73f5dfc02, read from the extract at /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours). 394 flat modules + 2 subdirs (espionage/, habit/) surveyed by full-header read of every decision-bearing module plus full reads of the authority spine. All file:line refs are into that tree. CONFIRMED = quoted/read code; PLAUSIBLE marked inline.

═══ THE PIPELINE (context every family rides) ═══
CONFIRMED: producers mint typed CANDIDATES → `evaluateWorldPulseRules` composes them (candidateEvents.js:560-585) → the political-autonomy AUTHORITY choke routes EVERY candidate's applyMode through `authorityFor` (candidateEvents.js:599-623, "The choke point that routes EVERY stochastic candidate family") → `resolveCandidateConflicts` + `rollCandidates` (candidateEvents.js:651, budgets maxAuto=6/maxProposals=5, per-candidate keyed rng fork `roll:<stableCandidateKey>` at :646-649) → apply arms in applyWorldPulse.js. `decisionTier.deriveDecisionTier` (decisionTier.js:102-108) classifies MAJORS structurally; `proposalAdmission.js:1-19` budgets the DM docket; `changeAuthorityPolicy.CHANGE_AUTHORITY_POLICY` (changeAuthorityPolicy.js:82-315) is the contract-tested map of ~30 candidate families to authority classes (proposal-gated / severity-gated / always-proposal / auto / auto-with-lock-escalation / auto-with-approval-routing / structural-proposal).

═══ DECISION-CLASS INVENTORY ═══

**1. WAR INITIATION.** Decider: the settlement-tier strategy chooser — settlementStrategy.js (header :1-12: "enumerate candidate moves, score each, pick one… softmax sample", moves defend/deploy/hold/sue_for_peace + warlord levers, :612-614, :668-726; deploy score = base + strength margin + aggressiveness − exhaustion ± casus/upswing/embassy terms, :674-693; gated settlementStrategyEnabled default FALSE). Its resolved march crosses the tick boundary as an ORDER via warIntent.js (:1-38, "THE RESOLVED MARCH… there is still exactly ONE opener"); the one opener is warDeployment.evaluateWarLayer step 4 (warDeployment.js:888-901), whose initiation authority is `authorityFor(rules,'strategy_deploy','auto')` at warDeployment.js:901. Hard gates (feasibility, one-army, mobilization posture) still run. Foreign-seat hook: the chooser's score/legal-move set and the warIntent order — a seat-resolution read at the chooser is one read serving all six moves.

**2. WAR TERMINATION / PEACE.** warTermination.js (:1-15) computes the attacker-side four-term read + `suePressure01` "for the strategy chooser"; sue_for_peace is the chooser's structural-proposal branch (changeAuthorityPolicy.js:241-247). The SECOND yes is warPeaceDecision.readWarPeaceDecision — deterministic threshold `suePressure01 >= ACCEPT_AT (0.5)` (warPeaceDecision.js:32, :318, :439), with ONE override: a typed demand carried by the current seat transition ("a faction-installed successor must actually honor the decision that installed it", :1-10, checked against `authorityTransferEpochFor` + `readWarSeatBooks` at :110-140). Peace terms priced/drafted via peaceTerms* (12 modules) and belief-side negotiation pictures (negotiationEvaluation.js). Hook: both yeses already flow through readWarSeatBooks — a foreign-seat weight lands there once.

**3. COALITION JOIN / RATIFICATION.** Join: warCoalitionDecision.readCoalitionJoinDecisions (:59-160) — deterministic `accepted: score01 >= threshold01` where `threshold01 = clamp(0.35,0.7, 0.5*martial/insular)` from disposition (:131-133). Refusal aftermath: warCoalitionRefusal.js. Treaty-sheet ratification: coalitionRatification.js (:1-31) — TWO-LEVEL authority: "inside a member the seat's book may diverge from the realm's… The ballot records WHICH book cast the vote," and LEVEL TWO "THE COALITION VETOES THAT RULER" — the existing structural precedent for an outside authority overruling a settlement's own seat. Wired to the pulse at envoyRatificationStage.js. Hook: the ballot's book-selection is seat-books-derived; a foreign seat is another book that can cast (or veto) the ballot.

**4. SEAT CONTESTS (coup / challenge / succession / intervention).** Coup verdict: coup.coupVerdictOutcomes (coup.js:99-226) — `resolveCoupVerdict` (domain/rulingPowerCoup.js) recomputes from LIVE state; pHold moved by ruling_authority score, war sentiment (×0.22, :45), foreign-intervener tilt `interventionAdjFor` (:117 — a foreign power already tilts seat contests), economic adj; applyMode `authorityFor(rules,'coup_succeeded', locked?'proposal':'auto')` (:195). Faction government challenge: factionCompetition.governmentChallenge (factionCompetition.js:719-731) — gated on legitimacy band ("if (band === 'stable' && warPressure < 0.68) return null"), severity = legitimacy·0.48 + power·0.28 + riskTolerance·0.14 + conflict·0.1 (+WR-5 grievance), always-proposal (changeAuthorityPolicy.js:206-213); applied as `transferRulingPower(cause:'appointment')` (applyWorldPulseFactionRoster.js:121). Coup/rebellion-risk-scales-with-legitimacy is thus ALREADY the live mechanism W-SEAT extends. Ruler succession: npcLadder* family; contested ambitions npcLadderContest.js. Commons uprising: commonsVoiceKernel.js (:6-12). Foreign army in an internal contest: convergence.js `intervention_ordered`, `authorityFor` at convergence.js:1009.

**5. CONQUEST / OCCUPATION / VASSALAGE — the occupier's own decisions.** Conquest resolution is authority-class 'auto' (changeAuthorityPolicy.js:250-257). Apply arm: applyWorldPulse.js:160-197 — cause 'conquest' first calls `installOccupationAuthority` (applyWorldPulseOccupationAuthority.js:42-87: disarm ×0.3, governing cut ×0.6, civic ×0.82, occupier seeded power 90 category 'occupation'), then `transferRulingPower` CROWNS it — **so under occupation the foreign power IS the governing faction returned by `governingFactionOf`** (CONFIRMED). Occupation evolution: occupation.js state ladder contested→unstable→extractive→stabilized→vassalized (:117), decided by a DETERMINISTIC classifier — `stabilizationSuitability = clamp01(0.7 − resistance + 0.22·compliantRegime + 0.12·garrison)` (:386-393) against ADVANCE 0.58 / REGRESS 0.34 / dwell 2 (:160-176); benefit/burden per rung (:127-148) with anti-snowball caps (:188-216). **The occupier currently makes NO posture choice — 'extractive' vs 'stabilized' are emergent states, not chosen postures** — so the cultivate/exploit dilemma hooks the suitability inputs, STATE_BENEFIT/BURDEN_SCALE, and RESISTANCE_GROW/DECAY (:182-183), and `occupation_vassalized` is 'auto' (changeAuthorityPolicy.js:265-272). Razing: razing.js (zero-import law) assembled by razingExecution.js; conquest INTENT is character-only, never capability (conquestIntent.js:10-16 "does not accept a feasibility read at all"). Sovereignty sale: sovereigntyMarketStage.js (band-crossing trigger), sovereigntyIntent.js (seller appetite, seat-books read), `sovereignty_conveyed` always-proposal (changeAuthorityPolicy.js:231-238), conveyance preserves the vassal rung (occupation.js:240-259). Legitimacy reseed by cause: rulingPower.LEGITIMACY_SEEDS (rulingPower.js:340-346 — conquest 18-40, coup 25-55) + govMultiplier bands (:311-333).

**6. DIPLOMACY VERBS.** realmVerbExecution.js is the ONE realm-verb lane (DM- or sim-minted, record-identical): DECLARE_CASUS, SUE_FOR_PEACE, REPUDIATE_TREATY, TRANSFER_SOVEREIGNTY, ORDER_SUPPLY_RAID, DECLARE_TRADE_EMBARGO, ORDER_INTERVENTION, ORDER_CONVOY, DECLARE_BLOCKADE, FORCE_* (:132-145 headline table; apply dispatcher :338-811, every arm re-runs gates against current worldState — "a lapsed order REFUSES VISIBLY"). Organic envoy diplomacy: the envoyErrand* family (offer/parlay/ratification; envoyErrandOffer.js:1-18 — the envoy "carries a decision, not a view"). Pacts: pactProposals.js (single writer, physics-priced answer dwell) / pactFormation.js. Treaty succession questions: treatySuccessionDecision.js (typed proposal); breach/enforcement: treatyBreach*/treatyEnforcement; orientation read-once: treatyOrientation.js (:1-20). Hook: verb availability/gates read "who governs" — a foreign seat with primacy would gate or co-sign verb mints for the occupied court.

**7. TRADE / ECONOMY POSTURES.** tradeWar.js (:1-30): primary-partner prize contests via `contestOverThirdParty`, escalation deposits a war intent; **HARD OVERRIDE already exists for overlords: "if C is a vassal of X and the overlord compels the trade, X wins regardless of the roll"** with a `vassal_trade_coercion` strain keeping vassal_rebellion reachable (:22-27) — the first live foreign-seat-primacy decision in the tree. Institution build/close: institutionLifecycle.js (:1-33, hysteresis+saturation+clamps, proposal-gated). Tier/resource: tierResourceDynamics.js (authorityFor at :217, :486, :537). Demographic works/responses: demographicsResponses.js (:1-19, weighted competing options, zero-PRNG scoring).

**8. MOBILIZATION + REACTIONS.** mobilization.js (:1-30): peace→alert→war_preparation→mobilized ramp, rate gated by disposition/economy/deity/legitimacy, deterministic classifier. Neighbour reactions: mobilizationReactions.js (:1-22): fortify/seek_allies/negotiate/pre_empt chosen DETERMINISTICALLY from disposition + relationship.

**9. JUSTICE / INTERNAL POLITICS.** assizeKernel.js (seated public judgments from exposure deposits); settlementPolitics.js (:1-14): "a ruling bloc LOADS the settlement's decision weights toward its members' interests exactly as a foreign asset's directionBias does — the SAME §H kernel" — i.e. the estate ALREADY has one shared decision-weight-loading kernel with a covert-foreign instance (corruptionWeb.js) and an overt-domestic instance; the W-SEAT foreign seat is the missing overt-foreign third instance of that same pattern (CONFIRMED header claim; kernel internals PLAUSIBLE, not re-read).

═══ THE THREE CHOKEPOINTS ═══

**CHOKE 1 — `readWarSeatBooks` (warSeatBooks.js:445-588): the seat-decomposition read. The foreign seat is a fourth weight with an existing precedent.** It already resolves the seat via `governingFactionOf` + ladder, then splits ONE decision into `settlementWeight01 / seatWeight01 / patronWeight01`, and the covert-patron arm ALREADY REDIRECTS the private book to a foreign court (`interestKind:'patron'`, patron biases substituted, :528-556) — occupier primacy/vassal weight is that exact shape made overt, scaled by occupation state + legitimacy. Consumers inherit it for free: grep -l gives 12 files; live worldPulse consumers = warPeaceDecision, warCoalitionDecision, warCoalitionRefusal, warTermination, envoyInterceptionStage, envoyNegotiationPictureBuilder, peaceTermsCoalition, sovereigntyIntent, armyTransitKernel (9; the others are self/cert-registry/doc-mention). Peace, coalition join/refuse, ratification ballots, envoy negotiation, and sales all read it today. `authoritySignatureFor` (:370-380) is the rename-proof continuity signature the foreign seat must join, not fork.

**CHOKE 2 — `authorityFor` (changeAuthorityPolicy.js:366-380) + the candidateEvents.js:620 routing choke: the decision-approval spine.** 13 call sites across 8 modules (candidateEvents.js:261,620; tierResourceDynamics.js:217,486,537; warDeployment.js:901; convergence.js:1009; resourceDynamicsKernel.js:375,432; coup.js:195; navalKernel.js:362; generosityKernel.js:1149), and the :620 choke covers every stochastic family besides. It already stacks independent escalation axes (player lock, autonomy mode, routine-major-approval). A foreign-seat OVERRIDE axis — the occupier forcing/vetoing an occupied settlement's majors — is one edit here serving all ~30 families in CHANGE_AUTHORITY_POLICY, with `decisionTier`'s campaignAltering markers already identifying which decisions a primacy seat plausibly touches.

**CHOKE 3 — `governingFactionOf` (rulingPower.js:203-209) + `transferRulingPower` (rulingPower.js:402-536): the one "who governs" resolver and the one seat mover.** 14 worldPulse consumer files (applyWorldPulse, armyTransitKernel, assizeKernel, disposition, envoyNegotiationPictureBuilder, factionCompetition, gratitudeBonds, migrationKernel, oathHolder, religionLegitimacy, urbanFabricKernel, warPeaceDecision, warPoliticalLoop, warSeatBooks). Under OCCUPATION the resolver already answers "the foreign power" (the crowned occupation authority, see family 5), so occupier primacy is partly emergent today; under VASSALAGE the overlord appears in NO roster — the scaled foreign weight must be DERIVED from the occupations/treaty ledgers (the hegemony.js template: "A PATTERN, NEVER AN ENTITY… ZERO persisted state", hegemony.js:11-16), layered above this resolver, never a fork of it. ⚠ Second-truth watch: six local `isGoverning` roster scans bypass the resolver and skip its `governingName` fallback — settlementPolitics.js:581,766,838; lineageClaim.js:84; lineageNews.js:56; warPoliticalLoop.js:118 (PLAUSIBLE divergence class; the memory's three-way-resolver-fork warning lands here — W-SEAT must consume `governingFactionOf`/`readWarSeatBooks` and add no seventh spelling). transferRulingPower has exactly 2 live worldPulse call sites (applyWorldPulse.js:191; applyWorldPulseFactionRoster.js:121) plus the DM's CHANGE_RULING_POWER path (rulingPower.js:12-16) — any W-SEAT-driven seat change must route through it, not beside it.

Constitutional fit notes: tradeWar's vassal override, coalitionRatification's LEVEL-TWO veto, convergence's interventionAdj, and settlementPolitics/corruptionWeb's shared §H decision-weight kernel are the four live precedents the W-SEAT design composes; every decider above is either a deterministic classifier, a keyed-PRNG softmax/roll, or a DM proposal — no wall-clock, no unkeyed rng anywhere in the surveyed deciders (CONFIRMED for every module whose decision code was read; header-claim PLAUSIBLE for the rest).

---

# V2 — LEGITIMACY AND UPHEAVAL

All census arms are complete. Deliverable follows.

# LANE V2 CENSUS — LEGITIMACY AND UPHEAVAL (extract mirrors 73f5dfc02; all paths under `src/`)

## (a) THE QUANTITY — shape, writers, readers

### Scale and shape — CONFIRMED
Persisted stock: `powerStructure.publicLegitimacy = { score, label, color, bg?, govMultiplier, crimMultiplier, isEndorsed…governanceFractured, breakdown? }`. `score` is an **integer 0..100** (every writer rounds and clamps). Legacy saves may hold a **bare number** — honored only by the canonical ledger. Bands at **75/60/45/30**: Endorsed(gov×1.30)/Approved(1.15)/Tolerated(1.00)/Contested(0.80)/Legitimacy Crisis(0.60, `governanceFractured`) — `generators/factionDynamics.js:105-135` (`legitimacyBandFor`, the generator chokepoint).

**Three band writers that must stay in step** — CONFIRMED, self-documented at `domain/rulingPower.js:303-305`: *"Band thresholds + multipliers mirror factionDynamics.computePublicLegitimacy and timeProgression's private reBand — the two existing writers. Keep all three in step"*: `factionDynamics.legitimacyBandFor` (genesis), `timeProgression.reBand` (:149), `rulingPower.rebandLegitimacy` (:311). A W-SEAT band change touches all three.

**Canonical read resolver** — CONFIRMED: `domain/governanceLedger.js:52-67` is *"the ONE read-point for the conserved legitimacy quantity"* (`{legitimacyScore, legitimacyLabel, present}`, neutral 50 when absent, legacy bare-number honored). Lenses through it: `causalState.js` derivePublicLegitimacy (:515, verbatim + conditions ±15 via `applyConditions … scale: 15` :529), deriveRulingAuthority (:562, `(score-50)*0.5`), law lens (:782, ×0.4), social_trust (:1168, ×0.4); `capacityModel` deriveAdministrative; `deriveSystemState` deriveVolatility; `factionProfile.legitimacyFor` (:254 — the governing faction *inherits* the settlement score; non-governing get a neutral); `disposition.js:353` (crime enforcement drive); `stressorGates` (:378,:539,:580,:676); `stressorDynamics` resilience `{kind:'governance', key:'legitimacy'}` rows; `autonomy/signalRegistry.js:16`.

**Non-ledger read shapes (drift risk)** — CONFIRMED, three inline `publicLegitimacy?.score` readers that do NOT honor legacy bare-number: `stressorGates.coupSpawnGate` (:746-747), `worldPulse/mobilization.legitimacyOf` (:155-159), `occupation.resistanceTarget` (:342). Plus direct reads at `warSiegeVerdict.js:245` (defender will), `warCapacityReads.js:253`, `rulingPowerCoup.coupContenders` (:85, reads `govMultiplier`, not score). A **label** reader exists: `hookEscalation.js:500-502` gates the legitimacy-crisis clock on `publicLegitimacy?.label` regex — and `signalRegistry.js:16-17` already documents that label as *"stale-prone … whose text drifts when tick kernels move .score"*.

**Second-truth hazard (units)** — CONFIRMED distinct quantities sharing the word: per-deity religion `legitimacy` is **0..1 float** (`religionLegitimacy.js` header: the *rightful claim* axis); factionState `legitimacyClaim` 0..1 (`factionCompetition.js:754`); pressure-model `legitimacy` is a **0..1 inverted deficit** = `clamp01((70 − causal)/70) + 0.16` if legitimacy-archetype conditions active (`pressureModel.js:18-21, 294-301`; `LEGITIMACY_ARCHETYPES` :40 includes `government_overthrown`); institution impairment `type:'legitimacy'`. Four spellings, four units — W-SEAT must name its unit explicitly.

### Writer roster (persisted `score`) — 14 write sites, CONFIRMED each
Full-shape (score + reband):
1. **Genesis**: `generators/power/rulingStructure.js:745` via `computePublicLegitimacy` (`factionDynamics.js:146-190`: 50 + prosperity + safety + tier-scaled defense/food).
2. **`transferRulingPower` reseed** (`rulingPower.js:452-456`): `LEGITIMACY_SEEDS` per cause (:340-346 — coup base 38 oldPull .25 [25,55]; conquest 28/.10 [18,40]; election 56; succession 48; appointment 45), `newScore = clamp(seed.min, seed.max, base + (50−oldScore)×oldPull)` — deposing a hated ruler starts warmer.
3. **`timeProgression.applyLegitimacyDelta`** (:163-181, applied :300) — tick faction deltas, governing faction only (:289-304), re-bands.

Score-only nudgers (int, clamp [0,100], **skip** structured-absent/legacy-bare, **never re-band**) — all clones of the `applyLegitimacyDeltasToUpdates` idiom (`generosityUpdates.js:80-107`, self-described *"the estate's ONE publicLegitimacy applicator over pending settlement writes"* per `sovereigntyTransfer.js:79`):
4. Generosity §9 (`generosityKernel.js:781-788`: hungry giver −3×frac×scarcity / comfortable +1×frac; applied :1365).
5. DM SEND_FOOD twin (`events/mutateWorld.js:1191-1202`).
6. Assize verdicts (`assizeKernel.js:77-84,357-360`: just +2 / sham −2, +1/0.06 commons coupling).
7. Commons voice (`commonsVoiceKernel.js:63,288-289`: dip [0,1,2,3] on entering petition/gathering/riot rung; grievance itself reads legitimacy deficit, LEGIT_FLOOR 55, W_LEGIT 0.55 :254-258).
8. Upswing (`upswingKernel.js:112,124,522,639`: reconstruction +4 / bust −3).
9. Climb-down crack (`momentum.js:1025,1061,1095` `applyLegitimacyHits`: −6×price01; applied via `realmVerbExecution.js:653`).
10. Peace-offer refusal (`warPeaceRefusal.js:24,151-154`: −4).
11. Traditions/festivals (`traditionsKernel.js:153,744-748`: triumph +3 … failure −3).
12. Hostage/roads layer (`roads/state.js:262-268`: detain −2, capture −(1+round(2×importance)), ransom-paid −1).
13. Divine mandate (`religionState.applyDivineMandate` :701-722, called from `pulseKernel.js:1774`): bounded **pull-to-target** — target = 50 + govWeight×(patronSecurity−0.45)×30×fit×piety, per-tick hard clamp ±2. The estate's one bounded-dynamics idiom already ON legitimacy.
14. Sovereignty sale echo (`sovereigntyTransfer.js:105` `LEGITIMACY_DELTA: -6`, applied :386-390; declared **regen-volatile**, the durable fragility rides the occupation record's resistance floor instead — `occupation.conveyOccupationRecord` :272-283).

**Structural finding (CONFIRMED code, consequence PLAUSIBLE)**: the 11 score-only nudgers move `score` while `label/govMultiplier/flags` stay frozen until writer 2 or 3 runs. `coupContenders` amplifies the incumbent by the possibly-stale `govMultiplier` (:85,110) while `coupSpawnGate` gates on the live `score` — two coup-path consumers can disagree about the same seat. The estate knows (`signalRegistry.js:16`); W-SEAT should read score via `governanceLedger` and derive bands, never trust stored band fields.

### Mobilization ramp gates — CONFIRMED
`worldPulse/mobilization.js`: `rampReadiness` (:201-213) blends legitimacy at weight **0.4 → ±0.2** on the multiplier (:103); `shouldCool` (:225-231) floors at **COOL_LEGITIMACY_FLOOR 30** (:109) — *"low legitimacy: the home front will not hold a war footing"*. Deploy gate keys off reaching `mobilized`.

### Hysteresis idioms available in the estate — CONFIRMED, quoted
- **Religion standing**: `religionState.js:144-150` `standingFor(share, prev)` — *"Standing from share, with hysteresis against the previous standing"*; `STANDING_HYSTERESIS: 4` (:48) widens the keep-band for the incumbent standing. Patron flips add **siege hysteresis**: *"a faith must win the roll PATRON_FLIP_TICKS ticks running"* (:559).
- **Occupation state machine**: `occupation.js:157-176` — dwell `STATE_HOLD_TICKS 2`, sticky band `ADVANCE 0.58 / REGRESS 0.34` (*"inside it the state holds"*), single-rung-per-tick, collapse exit 0.12, `MAX_CONTESTED_DWELL 48` force-resolve valve (:442-474). Header calls it *"pantheon hysteresis + bounded-transition idiom"* (:12).
- **Bounded pull**: `applyDivineMandate` (±2/tick toward a target) — see writer 13.
- **NOT present**: no hysteresis on publicLegitimacy bands themselves; band flips are instantaneous threshold crossings.

## (b) UPHEAVAL PATHS

### `transferRulingPower` (rulingPower.js:402) — every caller, CONFIRMED (3 modules, 4 call expressions)
1. **`worldPulse/applyWorldPulse.js:191`** — applies any `power_transfer` outcome (coup verdict fall; deployment-return coup; war-layer conquest). Conquest first seeds the foreign faction via `installOccupationAuthority` (:167; `applyWorldPulseOccupationAuthority.js:42-87` — disarms locals, seeds `category:'occupation'`, power 90, `modifiers:['occupier']`) and stamps `vassal_extraction` (:176-189), because *"transferRulingPower can only promote an EXISTING faction"*.
2. **`worldPulse/applyWorldPulseFactionRoster.js:121`** — approved `government_change` faction proposal, cause `'appointment'`.
3. **`events/mutateWorld.js:740,742`** — `CHANGE_RULING_POWER` canon event (DM), cause from payload, then stamps `government_overthrown` at severityByCause {coup .55, conquest .65, election .25, succession .3, appointment .3} (:745-755).

### What feeds each attempt / success — CONFIRMED math
**Coup (the only sim path that seats a new power from inside):**
- *Birth*: legitimacy pressure vs `birthThreshold 0.6` (`stressorsCore.js:236-249`) × `coupSpawnGate` (`stressorGates.js:741-764`): null unless score < 45; null under an active `occupation` stressor; requires ≥1 non-criminal contender (`coupContenders`); `bandMult = score<30 ? 1 : 0.35`; authorityMult 1.5/1.2/1/0.6 by causal ruling_authority.
- *Timing*: `stressorDynamics.js:296-311` resilience — ruling_authority .35, **governance legitimacy .30**, social_trust .2, security institutions .15; synergies (:511-521): succession_void ×0.7, political_fracture ×0.75, rebellion ×0.8 (*"the palace is distracted"*).
- *Verdict* (`rulingPowerCoup.resolveCoupVerdict` :147-204, invoked by `coup.coupVerdictOutcomes` :99-226): incumbent weight = power × **govMultiplier** (the legitimacy band, :85,110); un-gated incumbent ⇒ pHold 0.08; else `pHold = clamp(0.1,0.9, share×(1.15−0.4×sev) + authorityAdj(±0.125) + warSentimentAdj(0.22×sentiment) + interventionAdj + economicAdj)`. **The adjustment seam is already additive, flag-gated, 0-when-dark** — four precedents in one clamp.
- *Also*: `deploymentReturn.js:427-491` — a vassal's returning army (ratio ≥ SPLINTER_RATIO) rolls the same verdict, severity `0.4+ratio×0.5`.
- User agency: locked governing faction ⇒ proposal (`coup.js:181,195` via `authorityFor`).

**Rebellion — never transfers power today (CONFIRMED absence):** organic birth (`stressorsCore.js:182-190` threshold 0.7; `rebellionGate` :573-590 — null if occupied [insurgency models it] or score ≥ 75, ×1.7 <30 / ×1.3 <45, famine/debt ×1.3, wartime ×1.25) and the vassalage rule (`relationshipRulesCore.js:590-798`: `vassalStrain = mean(vassal legitimacy-pressure, trade, conflict, resentment)` :593, `overlordWeakness` folds the **overlord's** legitimacy pressure :594; success/quash `successPressure = strain + overlordWeakness×0.45 − fear×0.2` :801). Resolution ends in relationship `trajectory: "broken"|"suppressed"` (:817,833) — no `transferRulingPower`, no condition mint of its own beyond the stressor lifecycle. **This is the fork §736's occupied-side upheaval must extend, not duplicate.**

**Condition mints (denominator: 5 archetypes, 9 mint sites):**
- `government_overthrown`: `coup.js:214` (sev clamp(0.45,0.8, sev×0.85)); `deploymentReturn.js:480` (fixed 0.6); `mutateWorld.js:747` (severityByCause). Catalog: `activeConditions.js:115-122` (easing, 6 ticks, affects ruling_authority/public_legitimacy/social_trust).
- `coup_suppressed`: `coup.js:161-177` only (sev clamp(0.3,0.7, sev×0.7)); catalog :123-130.
- `faction_challenge`: `factionCompetition.governmentChallenge` (:720-790 — legitimacy-pressure band 'crisis'≥0.66/'contested'≥0.44 :622-626; severity `legit×0.48 + power×0.28 + risk×0.14 + conflict×0.1`; probability 0.12/0.04 + sev×0.34/0.22; **proposal-only**); pressure candidate (`candidateEvents.js:227-293` — legitimacy pressure ≥0.5, prob min(0.42, 0.06+score×0.3)); `npcAgency.js:1042-1051` (goal culmination, fixed 0.55); `deploymentReturn.js:197` (splinter/disband).
- `occupation_resistance`: `occupation.js:1011-1026` only — severity **= resistance** (0..1 ratchet, floor 0.20; target blends usefulness .5 + **prior-regime legitimacy .3** + popMass .2, :337-355; compliant regime ×0.45).
- `rebellion` condition archetype (`activeConditions.js:99-106`) is minted by generation-era promotion (`conditionPromotion.js:49`) and DM verbs, not by the pulse (the pulse uses the rebellion *stressor*).

**Does anything read legitimacy already?** Yes — the partial wiring §736 predicts is broad: coup gate + verdict (twice: govMultiplier and ruling_authority-which-folds-legitimacy), rebellion/insurgency/political-fracture gates, stressor resilience rows, mobilization ramp+cool, occupation resistanceTarget, faction governmentChallenge, vassal strain/weakness, siege defender will (`warSiegeVerdict.js:245`), war feasibility (`feasibilityGate.js:88-100` — collapse archetypes incl. coup/rebellion/faction_challenge, plus `FRAGILE_LEGITIMACY 28` score floor), commons grievance, crime enforcement. **W-SEAT extends these seams; it forks nothing.**

## (c) AFTERMATH-SEVERITY MACHINERY — CONFIRMED
- **`previousGovernments`**: written only by `transferRulingPower` (:500-503), rows `{label, cause, tick}`, bounded `MAX_PREVIOUS_GOVERNMENTS 6`. Readers: `authorityTransferEpochFor` (:239-256 — the succession discriminator; ⛔ the `id` slot stays id-only, name-keys forbidden :222-234), `previousGovernmentLabelsOf` (:270-284, name-alias bridge), plus display (`viewModel`, `pdf/sections/PowerStructure.jsx`, `EngineSections`, `display/warStatus`, `traditions/politics`, `factionRename`).
- **Receipts**: `transfer` return `{fromGovernment, toGovernment, authorityName, authorityArchetype, cause, tick, legitimacyBefore, legitimacyAfter}` (:505-514); coup outcome `metadata.verdict {holds, pHold, roll, winner, incumbent, challengers, variant}` (`coup.js:144-155`); `STABILITY_BY_CAUSE` + `recentConflict` prose (:348-354, 527-530); roster markers `'seized_power'|cause` on the seat and `'ascendant'` (+6 power, clamped 0-100) on the winner (:427-450); losers get competitive/escalating grudge edges, winner a symbiotic power-behind edge (:462-498).
- **Echo ladder** (`stressorAftermath.js:1-106`): active → residual echo (decaying memoryStrength) → graduated into `history.historicalEvents` (coup_detat→'succession_crisis', rebellion/insurgency→'guild_conflict', cap 20). Echoes bite forward: `echoStrengthAt(coup_detat) > 0.15` boosts political_fracture ×1.3 and succession_void ×1.4 (`stressorGates.js:386,496`); coup's generic residual is suppressed in favor of the verdict's own condition (`coup.isCoupResidualOutcome` :55-57).
- **Fresh-coup readers**: `npcLadderKernel.coupTruncatedFkeys` (:1052-1065 — a same-tick `government_overthrown`/`coup_suppressed` truncates the governing ladder); `convergence.coupFellAt` (:1267-1278 — installed regime owes its foreign patron); `pressureModel` +0.16; `factionRelationshipUpdate.js:312-380` (per-archetype relationship shifts for rebellion/faction_challenge/government_overthrown/coup_suppressed); conditions re-enter causal legitimacy at ±15 scale (feedback loop is derived, not persisted).

## THE THREE INSERTION POINTS (deliverable)
1. **ATTEMPT** — the spawn-gate layer: `stressorGates.coupSpawnGate` / `rebellionGate` / `insurgencyGate` (gateResult mult+reason rows are the native extension shape) and `relationshipRulesCore.vassalRules`' independence/rebellion pressure (:763-775). A W-SEAT foreign-seat weight (derived from `worldState.occupations[id].state` rung + vassal edges + the `modifiers:['occupier']` roster faction — all already present, hegemony-is-a-pattern-compatible) enters as gate multipliers with reasons; occupied-side attempts against the occupier ride `insurgencyGate`'s existing `occupied ×2.0` row. ⚠ One seam gap, PLAUSIBLE: `coupSpawnGate` nulls on the occupation *stressor* type only (:749) — a pulse-conquered town carries the occupations *ledger* + `vassal_extraction`, and I did not verify a matching stressor exists there; W-SEAT's gate read should target the ledger.
2. **SUCCESS** — `resolveCoupVerdict`'s pHold clamp (`rulingPowerCoup.js:176`): add a signed `foreignSeatAdj` as the **fifth** additive term (the warSentimentAdj/interventionAdj/economicAdj precedent: caller-computed, 0-when-dark ⇒ byte-identical), fed at both call sites (`coup.js:128-136`, `deploymentReturn.js:447-452`). Occupied-side success against the occupier already lives in `advanceOccupationState` suitability/collapse — extend `stabilizationSuitability` inputs, not a new resolver.
3. **AFTERMATH** — `transferRulingPower`'s cause-keyed tables (`LEGITIMACY_SEEDS` :340-346, `STABILITY_BY_CAUSE` :348-354) scale the reseed; the mint severities (`coup.js:164/173/215`, `deploymentReturn.js:481`) scale the scar; the `previousGovernments` row carries provenance. ⛔ `RULING_POWER_CAUSES` (:335) is a frozen vocabulary and `previousGovernments` is persisted schema — extending either is **owner-gated** (schema/persistence shape). Seat-weight scaling of the existing seeds is not.

**Constitutional checks**: one quantity/one resolver — read via `governanceLedger` only (do not add a fourth score read-shape; do not trust stored band fields); governing-faction identity via `governingFactionOf` only; derived-not-persisted — the foreign seat is derivable today from occupations ledger + occupier faction + vassal edges, zero new persisted bytes needed for primacy/weight; dormancy — every precedent adjustment in this map is 0-when-dark, and W-SEAT must match; hysteresis for any new seat-weight band should borrow `standingFor`'s prev-band widening or the occupation dwell, both quoted above.

---

# V3 — OCCUPATION LIFECYCLE

CENSUS LANE V3 — THE OCCUPATION LIFECYCLE (build-tip extract mirroring 73f5dfc02; all paths under `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours/`)

## 1. The conquest stamp (entry into occupation) — CONFIRMED

- **Emission**: `src/domain/worldPulse/warDeployment.js:804-874` — a fallen siege emits `candidateType: 'conquest'` with `powerTransfer.cause: 'conquest'` and `sourceStressorId: war_front.<occupierId>.<targetId>`; line 874 records `outcome: razed ? 'razing' : 'conquest'` (razing is the alternate mouth, `razingSiegeEmission` wired at `warDeployment.js:731-751`).
- **Apply side**: `src/domain/worldPulse/applyWorldPulse.js:160-197`. On `cause === 'conquest'`: (a) `installOccupationAuthority` (`applyWorldPulseOccupationAuthority.js:42-87`) — local military ×0.3 +`'disarmed'`, deposed governing seat ×0.6 +`'occupied'`, other civic factions ×0.82 +`'occupied'`, then seeds the occupier faction `{category:'occupation', power:90, isGoverning:false, modifiers:['occupier']}`, idempotent by modifier guard (line 56); (b) stamps `vassal_extraction` severity `clamp01(0.55 + 0.15·severity)` (applyWorldPulse.js:176-189 — "the two faces of an occupation (military strain + economic extraction)"); (c) `transferRulingPower` then **crowns the occupier as the governing faction** (191-196).
- **Ledger seed**: `occupation.js:652-664` (`freshConquestsFrom`) reads occupierId from `o.condition.causes[0].source`; `createOccupationRecord` (`occupation.js:228-238`) = `{ occupierId, state:'contested', sinceTick, stateHeld:0, resistance:0.35, benefitYield:0, lastTick }`, keyed by the OCCUPIED id — one occupier per settlement; same-occupier re-conquest is a no-op (879-881). Ledger is conditionally materialized/dropped at `pulseKernel.js:1114-1122`; it is on the public-snapshot DENY list (`display/worldSnapshotPublic.js:85`); DM display goes through `display/occupationStatus.js` (words only, "NO INTERNALS", lines 18-20).
- **Conquest feeds**: `pulseKernel.js:1642-1672` — a fresh occupation (`sinceTick === tick`) pays the victor a LOOT pulse and (conscience-gated) CAPTIVE pulse; ledger decays to absent.

## 2. The state machine as built — CONFIRMED

`occupation.js` is the single machine. Ladder `contested → unstable → extractive → stabilized → vassalized` (`STATE_LADDER`, :117-119). Tunables (:157-216): dwell `STATE_HOLD_TICKS 2`, `ADVANCE_THRESHOLD 0.58` / `REGRESS_THRESHOLD 0.34` (sticky band), `COLLAPSE_THRESHOLD 0.12`, `MAX_CONTESTED_DWELL 48` force-resolve valve (:462-470), one rung/tick, signed dwell resets on direction flip (:479-493). Suitability = `clamp01(0.7 − resistance + 0.22·compliantRegime + 0.12·garrisonPresent)` (:386-393). Resistance ratchets toward `0.5·usefulness + 0.3·priorLegitimacy + 0.2·popMass`, ×0.45 under a compliant regime (:337-355), grow 0.18/decay 0.14 per tick (:182-183); `compliantRegime` = an `'occupier'`-modifier faction present AND locals `'occupied'`/`'disarmed'` (:365-371). Benefit scales `{contested 0, unstable .12, extractive .45, stabilized .85, vassalized 1.0}`; burden scales `{1.0, .85, .55, .30, .18}` (:127-148). Anti-snowball: per-occupation cap 0.45, occupier hard containment 0.9, diminishing base 0.62; burden per-occ cap 0.6 + overextension +0.12/occupation, deliberately NOT total-capped (:196-211). Emitted conditions: `occupation_resistance` on the occupied (floor 0.20), `occupation_burden` (worsening) and `war_spoils` (easing, relief = benefit×0.55) on the occupier, plus one-shot `occupation_burden_cleared` / `war_spoils_ended` receipts (`occupationRecordMode.js:131-201`). Deterministic, rng-free, gated `warLayerEnabled` (:815-820); WR-8 dark-by-default "overwhelming gate" caps a non-overwhelming victor at `extractive` (:969-981, behind the 8-flag chain `conquestDoctrineStage.js:62-71`).

**How the control state is SET: only three writers exist.** (1) `createOccupationRecord` (conquest seed), (2) `advanceOccupationState` inside `evaluateOccupations` (the deterministic hysteresis), (3) `conveyOccupationRecord` on a sovereignty sale (`occupation.js:272-283`, called from `sovereigntyTransfer.js:274-281` — occupierId rewritten in place, rung preserved, resistance raised to a floor). **The posture is NOT a decision anyone makes** — `extractive` vs `stabilized` is a rung the machine climbs through, driven by resistance/garrison/compliance. The only human levers today are negative: DM-dismiss of the conquest (no seed, `pulseKernel.js:248-271`) and DM-dismiss of `occupation_vassalized` (promotion rolled back to `stabilized`, `occupation.js:958-987`).

## 3. Exit paths and what persists — CONFIRMED

Exits: (a) collapse at contested with s ≤ 0.12 → `liberated` (:447-449); (b) regression below contested (:489-492); (c) max-dwell force-resolve at 48 contested ticks (:462-470); (d) returning-army liberation — `deploymentReturn.js:351-385`, strength-scaled roll (`recordRng.fork('liberation')` — the only rng near occupation), failure = a "failed rising" outcome; `siege_lifted` also drops the entry (`liberatedIdsFrom`, occupation.js:674-683); (e) canon-vanish drop (:901-905); (f) **vassalization is NOT an exit** — a one-shot `relationship_label_change` to a `vassal` edge (`vassalizationOutcomes` :724-780) while the record STAYS at the `vassalized` rung, still yielding benefit 1.0/burden 0.18 forever, and becomes conveyable (`sovereigntyAssets.js:219-224` — only `vassalized` is tradeable). A live `occupation_continuation` treaty term forbids the collapse exit while it runs (:914-924; `treatyEnforcement.js:140-154` — directional, victor-over-loser).

Scars after liberation: `occupation_lifted` easing condition, 6 ticks (`activeConditions.js:381-388`); urban-fabric `occupation_marks` scar (`urbanFabricKernel.js:673`); NPC `siege_survived` trait (`npcGrowthKernel.js:305`); disposition win 0.5 to occupied / loss 0.6 to occupier (occupation.js:929-940); reconstruction/recovery reads (`populationDynamics.js:92`, `upswingKernel.js:238`).

**⚠ FINDING (CONFIRMED by absence): liberation never reverses the roster.** Both `occupation_lifted` producers are condition-only outcomes (`deploymentReturn.js:357-376`; `occupation.js:943-954`) — no `powerTransfer`, no roster patch. No writer anywhere removes the `'occupier'` faction or the `'disarmed'`/`'occupied'` modifiers or restores the cut power (grep over src: the only writer of those modifiers is `applyWorldPulseOccupationAuthority.js`; readers are `occupation.js:365-371` and prose). So a liberated settlement's ledger entry is deleted while the crowned foreign authority faction remains governing and its locals remain disarmed at ×0.3 — the prose says "the settlement reclaims its own authority"; the roster does not. W-SEAT's exit path must specify the un-install, or the derived-seat design must make the persisted faction irrelevant.

## 4. Reader census of the control state (denominator: 27 files carry `occupierId`; the engine-side readers)

`mobilization.js:76-83,465-467` — `OCCUPIER_CONTROL_STATES = {extractive, stabilized, vassalized}`: at extractive+ the occupied's war posture force-cools ("under occupation: the occupier permits no war footing", :322-332); contested/unstable still permit a desperate rising. `warDeployment.js:316-321,1004-1010` — an occupied settlement may march ONLY against its occupier. `supplyKernel.js:658-668` — dest occupation state feeds believed route danger; **`must-go` override: an occupied source MUST ship to its occupier (coerced tribute)**. `generosityKernel.js:686-690` — receiver occupation state in route risk. `disposition.js:497-513` — occupations-held count feeds aggressiveness/lawfulness ("imposed martial rule, not its own law", :357-359). `martialReadiness.js:385-390,459-487` — occupied AND occupier both war-hot for footing/readiness. `mercenaryMarket.js:201-225` — occupied/occupier both "engaged" for market activation. `corruptionWeb.js:826-835` — an occupied patron's foreign leash collapses. `institutionStatusLifecycle.js:212-217` — `occupied` = ledger entry OR occupation conditions (a union read). `religiousContest.js:646-652,770-785` — `occupationFaithPull`: the occupier's deity presses conversion (force-entry). `envoyInterceptionStage.js:107-113` — occupied ground is the occupier's legal encounter venue. `warCoalitionExpenditure.js:192-235` — held/occupied-by = territorial exposure. `conquestDoctrineStage.js:398-408` — conquest-history band (never/once/repeated/habitual). `routeNetworkConsumersStrategic.js:211-216` — occupation = garrison route claim. `traditions/relations.js:78-90` — vassalized-rung overlord imposes traditions. `tickIndices.js:447-461` — occupier→held index. `deploymentReturn.js:159-160`, `applyWorldPulse.js:427`, `causeVocabulary.js:124`, `residueStripGuard.js:86`, `envoy`/`treatyEnforcement` as above. Display: `occupationStatus.js` (3 selectors + panel gate), `briefs/roadScene.js:176-177,243-244`, `livingWorldSignals.js:265-273` (tolerates a legacy `worldState.conquest` key), `chroniclersLetter/heraldRouting/settlementRumors/rumorPhrasePools` (occupation_lifted vocabulary), `subsystemRowsVirtual.js` (certification prose).

## 5. Prose commitments and occupier agency today

- `src/generators/economy/prosperity.js:61-72` — CONFIRMED verbatim: `under_siege` ⇒ "All normal economic activity is suspended…"; `occupied` ⇒ "Revenue flows outward to the occupying authority via <maritime levies|river tolls|road tolls and seizure powers> and compulsory assessment… The officially stated economic situation differs from the experienced one." **Nothing in the engine moves that revenue** — `vassal_extraction` has no receiver (W-COIN census C1: "extraction without a receiver"), and generation-side `occupied` stress (`stressGenerator.js:163`, `priorityHelpers.js:296-341` output multipliers 0.40-0.70, `narrativeGenerator.js:605-614`, promotion to `vassal_extraction` at `conditionPromotion.js:46`) carries **no occupier id at all**.
- What the occupier can already DO: choose to march (belief-driven `CONQUEST_INTENTS = ['none','terms','conquer','punish']`, `conquestIntent.js:45`); raze instead of hold (`razingExecution.js`, plan-only, K1 grades the damage); take loot/captive pulses (conquest feeds); receive coerced tribute shipments (supply must-go); press its faith (occupationFaithPull); intercept envoys on occupied ground; keep a ceded garrison via the `occupation_continuation` peace term ("the garrison stays at the walls", `peaceTermsDrafting.js:94`; a hegemony subordinating tie, `hegemony.js:57-66`); **sell a vassalized holding** (`sovereigntyTransfer.js` vassal arm — rung preserved, resistance floored, legitimacy echo, grievance vs the seller). It cannot: choose a posture, invest in the occupied, relieve extraction, or sit in the occupied settlement's decision loops as a seat.

## 6. Gaps W-SEAT fills (the design deltas, grounded)

1. **Posture-as-decision**: the ladder conflates control LEVEL with treatment POSTURE — `extractive` is a rung passed through mechanically, not a chosen policy. Cultivate/exploit needs a new axis whose consequence hooks already exist as constants to modulate: resistance grow/decay (:182-183), the compliant-regime ×0.45 (:353), STATE_BENEFIT/BURDEN_SCALE, and §736's rebellion scaling ("extractive raises it, stabilized dampens").
2. **The occupier's seat**: today occupation = the occupier IS the crowned governing faction at power 90 in the occupied roster (a persisted roster write), and vassalage installs NO foreign presence at all (edge relabel only). The §735.2 "derived counterpart in all decision-makings" fits the hegemony/derived-not-persisted idiom far better than the current persisted faction — and would cure the liberation-never-uninstalls finding (§3 above) by construction. Faction competition (`factionCompetition.js`) and coalitions currently see only roster power.
3. **Vassal influence scaling**: culture/trade/relationship closeness inputs already exist (`cultureDistance.js`, relationship scalars trust/resentment/dependency/leverage on the vassal edge, `relationshipRulesCore.js:745-830`) but nothing derives an overlord SEAT weight from them.
4. **Coup/rebellion legitimacy scaling (§736)**: partial wiring exists and must be extended, not forked — resistance target already reads the occupied's `publicLegitimacy` (:342, pre-occupation loyalty); mobilization cools below legitimacy 30 (`mobilization.js:109`); `vassal_rebellion` reads strain/weakness/resentment but legitimacy only rhetorically (:763-830).
5. **Second-truth discipline — the estate already holds forks W-SEAT must not widen**: "occupied" has four spellings (generation stressType · occupations ledger · faction modifiers · vassal_extraction condition; `institutionStatusLifecycle.js:216` unions two of them); "vassal/overlord" has three substrates (ladder rung · relationship edge · hegemony treaty ties) — and **two same-named `vassalOverlordOf` functions read different substrates** (`occupation.js:1232-1242` reads edges; `traditions/relations.js:84-90` reads the ladder rung). The chartered foreign seat must name ONE resolver.

## 7. The W-COIN occupied-window intersection

The panel's MAJOR (scratchpad `wcoin-panel/lens-secondTruth.md:15-17`, folded into §735.1): W-COIN-1's stressGate says only "occupied ⇒ §6.3" but the occupation-diversion transfer is W-COIN-3, so in the lit window an occupied settlement taxes into its OWN vault while `prosperity.js:66-71` commits the opposite — wrong-direction lived coin THE PROMISE then freezes. Required amendment: W-COIN-1 defines occupied NOW (yield 0, or mint-and-sink with a typed `diverted` receipt). W-SEAT is that finding's front door (§735.2 "the two share the occupied-economics spec"): (a) the "occupied" predicate for the mint should be **presence in `worldState.occupations`** — the only spelling that names an occupier to divert TO (generation-stress `occupied` has none — CONFIRMED above); (b) the diversion fraction is the natural first consequence hook of the cultivate/exploit posture, and the ladder already grades it (benefit scale 0 → 1.0 by rung); (c) W-COIN-3's `vassal_extraction` receiver and the occupation-extraction transfer should land as the SEAT's flows, so seat, coin, and the committed prose become one system with one meaning per quantity.

PLAUSIBLE (reasoning only, flagged as such): the occupation machine's constants (§2) were soak-calibrated for the anti-snowball bound (`occupation.js:33-36` cites a pinning soak test — not run by this lane), so W-SEAT hooks that modulate benefit/resistance must re-run that bound rather than assume it. Everything else above is CONFIRMED by read receipts at the cited lines.

---

# V4 — VASSALAGE AND SCALING SIGNALS

# LANE V4 CENSUS — VASSALAGE, CLOSENESS, AND THE SCALING SIGNALS

All paths relative to the extract at `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours` (mirrors 73f5dfc02). 114 files under `src/` mention "vassal"; the load-bearing homes below were read in full enclosing scope.

## (a) THE VASSAL SUBSTRATE

### a1. RELATIONSHIP_DEFAULTS vassal row — CONFIRMED
Home: `src/domain/worldPulse/relationshipState.js:25-116` — 10 label rows total (neutral, trade_partner, allied, patron, client, vassal, rival, cold_war, hostile, criminal_network). The vassal row (lines 71-79):

```
vassal: { trust: 0.34, resentment: 0.48, dependency: 0.82, leverage: 0.82,
          fear: 0.48, tradeBalance: 0.32, pactStrength: 0.58 }
```

For contrast, patron is the softer hierarchy: trust 0.42 / resentment 0.32 / dependency 0.72 / leverage 0.72 / fear 0.34 / pactStrength 0.52 (lines 53-61). Client (the junior-authored spelling) is normalized away to patron with the edge flipped (`normalizeRelationshipEdge`, lines 396-424).

The state record is ONE PER EDGE (pair-level), keyed by the edge's own id (`relationshipKeyFromEdge`, lines 326-331), living in `worldState.relationshipStates[key]`; the one writer is `applyRelationshipPatch` (`relationshipEvolution.js:672-678` returns `{ ...worldState, relationshipStates: { ...prev, [outcome.relationshipKey]: updated } }`). `ensureRelationshipState` (relationshipState.js:427-502) fills defaults and adds lifecycle fields: `militaryBurden/aidBurden/obligationFatigue` (default 0), `overlordWeaknessStreak`, `posture`, bounded archives (recentIncidents 8, history 12, turningPoints cap 24, allianceCalls 24, coalitionSettlements 24, hierarchyResolutions 6), and the four seniority stamps (below).

### a2. The vassal channel bundle — CONFIRMED
Home: `src/domain/region/graph.js:636-695` (`relationshipChannelBundle`), header law at 627-630: *"Direction is edge-significant for hierarchical labels: edge.from is the patron/overlord, edge.to is the client/vassal."* The `rel === 'vassal'` branch (649-656) mints FIVE confirmed channels:

| type | direction | strength | confidence | visibility |
|---|---|---|---|---|
| political_authority | overlord→vassal | 0.82 | 0.82 | public ("exercises overlord authority") |
| military_protection | overlord→vassal | 0.70 | 0.76 | public ("is expected to protect") |
| tax_obligation | vassal→overlord | 0.76 | 0.78 | public ("owes tribute or obligation") |
| information_flow | overlord→vassal | 0.46 | 0.62 | gm |
| information_flow | vassal→overlord | 0.40 | 0.58 | gm |

Patron mints only the first three, weaker (0.62/0.55/0.45; lines 657-662). Channel vocabulary is the closed 13-member `REGIONAL_CHANNEL_TYPES` (graph.js:47-64). Label changes flow through `syncRelationshipChannelBundle` (graph.js:708-758): the new bundle's own ids re-confirm out of dormancy, other relationship-generated channels on the pair go dormant, DM 'disabled' survives outright, and war-layer-minted fronts stay war-layer-owned (the `hasWarLayerEvidence` de-alias, 511-514, 728-730). Caller: `applyWorldPulseRelationshipGraph.js:6,94-98`.

A SECOND mint of the same shape exists on the discovery side: `discoverDependencyCandidates.js:127-156` (`addVassalageChannels`) — same channel types, slightly different numerics (military_protection `max(0.6, s−0.12)`, tax_obligation `max(0.64, s−0.06)`, two-way info 0.43/0.6), but born `status: 'suggested'` (line 222) so it never auto-confirms; ids collide deliberately (`channelIdFor` keys type+from+to+goods) and statuses are sticky on merge (graph.js:494-575).

### a3. Suzerain direction laws — CONFIRMED
- Canonical edge: `from` = overlord. `canonicalEdgeForLink` (`src/domain/relationships/canonicalRelationship.js:337-376`): an authored role hint (`localRelationshipRole`/`sourceRole`/`displayRelationshipType` of 'overlord'/'vassal', or the raw word 'overlord') wins; only with no directional signal does it fall back to `strongerFirst` (tier rank + log-population bonus, lines 226-244).
- State stamps: `overlordSaveId`/`vassalSaveId` (+ patron/client twins) on the relationship state (relationshipState.js:484-490 — *"Direction stamps for hierarchy labels born from symmetric edges… Null on DM-authored hierarchy edges, which keep strict edge direction"*). Resolver: `relationshipRoles` (381-393) — state-first, edge-direction fallback. Stamp writers: the vassal tribute patch (`relationshipRulesCore.js:633`), the subjugation proposal (`relationshipRulesAdversarial.js:355`); cleared on any transition away from vassal/patron (`relationshipEvolution.js:620-631` — *"a transition away… clears them so a later re-subjugation can never inherit a stale senior side"*).
- Spelling: regional table folds `overlord/suzerain/liege → 'vassal'` (canonicalRelationship.js:55-57); the relationship-plane table folds `subject/tributary/overlord → 'vassal'` (104-105, 143). The two tables deliberately diverge on 17 of a 40-input corpus, pinned by test, merge owner-gated (lines 31-35).

### a4. THE TWO VASSAL SYSTEMS (the fork W-COIN's panel flagged) — CONFIRMED
**System 1 — relationship-vassal:** the labeled edge + `relationshipStates` numerics + the channel bundle, lifecycled by the pulse rules (below).

**System 2 — treaty subordinating ties:** `worldState.spatialLedgers.treaties` (written via `setSpatialLedger(worldState,'treaties',…)` — `peaceTermsSale.js:275`; read via `getSpatialLedger` — `hegemony.js:83-86`). `SUBORDINATING_TERM_TYPES = ['tribute','compelled_alliance','puppet_seat','occupation_continuation']` (`hegemony.js:56-59`; `resource_share` is *"deliberately EXCLUDED — extraction, not subordination"*). `hegemonyRead` derives spheres per call — *"A PATTERN, NEVER AN ENTITY… ZERO persisted state"* (hegemony.js:10-15) — center = the party the terms are OWED to via `treatyOrientationOf` (obligee over obligor; wartime loser pays, sale buyer pays — `treatyOrientation.js:20-38`), threshold ≥3 distinct subordinate ties (`MIN_TIES: 3`), tie strain = worst `complianceState` (honored<strained<defaulted, `rankCompliance` hegemony.js:73).

**The fork, precisely:** the two systems never write each other's truth.
- No treaty term ever sets `relationshipType`. The ONLY treaty→relationship writes are numeric nudges: `nudgeCompelledAlliance` (+0.15·mag trust, +0.1·mag resentment — `peaceTermsOverlay.js:36-51`) and `accrueStrainResentment` (tribute strain resentment — 63+), both through `applyRelationshipPatch` with `proposalPayload: null`.
- `hegemonyRead` reads ONLY the treaties ledger. A relationship-vassal with no treaty contributes ZERO ties to any sphere; a tribute-paying 'rival' IS a sphere member. There is no 'vassalage' term type in `TERM_CATALOG` at all — treaty terms are minted from the prize-asset map (`peaceTermsCatalog.js:383-392`: treasury→tribute, territory→occupation_continuation, alliance_network→compelled_alliance, government→puppet_seat).
- The one bridge is the OCCUPATION ladder (a third structure, `worldState.occupations`): `contested→unstable→extractive→stabilized→vassalized` (`occupation.js:117`), and on FIRST arrival at `vassalized`, `vassalizationOutcomes` (occupation.js:724-780) emits a canonical `relationship_label_change` to 'vassal' *"through relationshipEvolution's existing vassal-label apply path… NOT a parallel model"* (711). Meanwhile the `occupation_continuation` TERM holds that ladder open (`executor: 'occupation_hold'`).

W-SEAT consequence: "is S under F's seat?" already has THREE partial answers (relationship-vassal edge, treaty subordinating tie, occupations ledger entry). The seat read must be ONE resolver that consults all three, or it becomes the fourth fork of exactly the class the governing-faction resolver was caught in.

## (b) THE THREE SCALING SIGNALS

### b1. CULTURE closeness — CONFIRMED (and the answer is: reuse, don't mint)
The 11 keys: `src/data/cultureProfiles.js:51-481` — germanic, latin, celtic, arabic, norse, slavic, east_asian, mesoamerican, south_asian, steppe, greek (`CULTURE_PROFILE_KEYS`, line 525). **No pairwise distance over these keys exists anywhere** (grep for cultureDistance/affinity/closeness/kinship over src returns only the composite below); the profiles are generation flavor with a bounded ±30% institution-probability multiplier (`cultureInstitutionMultiplier` clamps 0.8–1.3, "texture, not destiny").

What DOES exist is constitutionally settled: `src/domain/spatial/cultureDistance.js` — §II.5-2 owner ruling quoted in its header: *"culture is NOT ethnographic identity — it is a DERIVED behavioural/economic SIMILARITY… NEVER a new authored coordinate"* (lines 4-8). `cultureDistance(a,b,{tradeTie01})` (165-199), symmetric, [0,1], total on garbage:
- FAITH 0.30 (deity evil01/chaos01 axes), ALIGNMENT 0.25 (lawfulness01/malice01), ECONOMY 0.18, GOVERNANCE 0.27 = 0.6·regime-3-axis gap (REGIME_AXES table over faction archetypes, 73-88) + 0.4·same-sovereign identity (`governingName` equality — line 183-186);
- TRADE tie multiplicatively CLOSES up to 0.25; TRADITION kinship (Jaccard over festival motif elements) closes up to 0.2 — never additive (190-198).

The vector is a LIVE per-eval extraction, never persisted: `buildCultureVector` (`migrationKernel.js:154-174`) reads `config.primaryDeitySnapshot`, `settlementAlignment`, `causal.scores.economic_capacity`, the governing faction archetype, `powerStructure.governingName`, `settlement.traditions`. The pairwise `tradeTie01` comes from the relationship label: `TRADE_TIE = { trade_partner: 1, allied: 0.7, vassal: 0.6, patron: 0.6 }` (migrationKernel.js:197). Second consumer: supplyKernel's contraband gate (supplyKernel.js:676,711).

**Verdict for W-SEAT:** it must NOT mint a closed 11×11 table — that would contradict §II.5-2, and the 11-key `culturalIdentity` is not even an input to the existing composite. Use `cultureAffinity` (cultureDistance.js:208-210). Bonus: the governance-identity term already makes a vassal whose `governingName` flips to the overlord's read culturally closer over time — a built-in cultivate-posture feedback (header lines 21-23 say exactly this about conquest). PLAUSIBLE gap worth flagging upward: heritage kinship (norse↔norse) is representable only via a new ruling.

### b2. TRADE — CONFIRMED
Between two specific settlements, three typed layers exist:
1. Pair-level state numerics: `relState.dependency` and `relState.tradeBalance` (defaults per label; vassal 0.82/0.32), moved by rules (vassal tribute tick: tradeBalance −0.025 — rulesCore.js:631; alliance gate tradeBalance>0.55 — 147; sustained-trade read tradeBalance>0.54 — relationshipRuleHelpers.js:475).
2. Regional P0 channels on the directed pair: `trade_dependency` (supplier→dependent), `export_market` (buyer→exporter), `trade_route` (endpoint→endpoint) with strength/confidence ∈[0,1] (graph.js:47-64,81-85; direction contract discoverDependencyCandidates.js:274-280). Discovery numerics: `channelStrengthForGoods` = base + maxGoodCriticality·0.35 + count lift ≤0.2 (199-204); confidence by relationship (`vassal 0.78`, trade_partner 0.9, hostile 0.15 — 207-217); hostile pairs are trade-blocked (`NO_TRADE_RELATIONSHIPS`, 293-298). Read API: `activeChannelsFrom` (graph.js:796-808).
3. The flow ledger: `routeNetworkFlows.js` — integer per-corridor/edge tallies by the closed source table `ROUTE_FLOW_SOURCES` (82-90: migration/mission=population, shipment/unmet_import/supply_starved=goods, army_transit/deployment=military), weights `SOURCE_WEIGHT` (migration 2, shipment 2, army_transit 3…), band vocabulary `['none','trace','stirring','steady','established']` (97-100). *"the movers already move, and this layer only COUNTS"* — integers, never floats, on THE PROMISE grounds.

Precedent for exploit-posture mechanics: an overlord can already compel a vassal's trade (`overlordOver`, `tradeWar.js:184-194` — the hard-override holder).

### b3. RELATIONSHIP — trust/pactStrength/leverage/fear — CONFIRMED
Semantics as executed (vassal arc, `relationshipRulesCore.js:585-842`):
- **leverage** = the senior side's extraction power (tribute severity `0.28 + leverage·0.32 + dependency·0.18`, line 621; probability +0.18·leverage).
- **dependency** = junior reliance (compliance gate dependency>0.62, clientRules 515; deepens under protection, 726).
- **fear** = coercive suppression (rebellion `successPressure = strain + 0.45·overlordWeakness − 0.2·fear`, line 801; quashing raises fear +0.08).
- **trust + pactStrength** = the compact's stability (`stableVassalage = 0.34·trust + 0.28·pactStrength + 0.38·(1−vassalStrain)`, threshold >0.55 — line 736-737; the stable branch pays trust +0.045, resentment −0.035).
- **resentment** = rupture pressure (rebellion probability +0.12·resentment, 775).
- Rebellion arming: `independencePressure = clamp01(vassalStrain + 0.28·overlordWeakness + min(0.28, 0.07·weaknessStreak) + 0.16·vassalConfidence)` (763), fires when strain>0.55 or pressure>0.62; the `overlordWeaknessStreak` is measured every tick but its visible beat is cooldown-damped (E4-2a, lines 10-15, 672-715).

## (c) HOW VASSALAGE BEGINS AND ENDS TODAY

**BEGINS — four paths, all converging on the one apply path:**
1. **Genesis/DM authoring** — CONFIRMED: `RELATIONSHIP_SELECTIONS` offers `overlord_of`/`vassal_of` (canonicalRelationship.js:191-192; `relationshipDefinition` 271-276 orients from=overlord); the generation pipeline stamps `config._neighbourRelType || 'neutral'` onto the neighbour link (`assembleSettlement.js:117`); `deriveRegionalGraphFromSaves` mints the edge from neighbourNetwork via `canonicalEdgeForLink` (graph.js:423-481). No seniority stamp — strict edge direction (from=overlord).
2. **Pulse subjugation** — hostile edge with `powerGap > 0.48 && conflictStress > 0.55` → `subjugationDirection` (stronger side wins; protector backing weighed — relationshipRuleHelpers.js:427-462) → `labelProposal 'vassal', 'hostile_occupation_pressure'` with the third-party cascade PREVIEWED in the proposal summary (relationshipRulesAdversarial.js:314-366); apply stamps overlordSaveId/vassalSaveId.
3. **Occupation maturation** — war conquest seeds `worldState.occupations[occupiedId]` at 'contested'; the ladder tops out at 'vassalized'; on first arrival, `vassalizationOutcomes` emits the auto (probability 1) `relationship_label_change` to 'vassal' on the REAL occupier↔occupied edge (occupation.js:724-780) — skipped if no edge exists.
4. **Legacy import** — persisted 'subject'/'tributary'/'overlord' spellings resolve to the vassal defaults row (RELATIONSHIP_PLANE_ALIASES; RN-B1 owner-signed).

Acceptance cascades third-party realignments through `resolveRelationshipHierarchy` (relationshipHierarchy.js:211-280; gate `vassalState.relationshipType !== 'vassal'` returns no-op at 223).

**ENDS — three paths:**
1. **Rebellion succeeds** — an active rebellion stressor + `successPressure > 0.48` → `labelProposal 'rival'` (`vassal_rebellion_succeeds`, rulesCore.js:800-821), with the constitutional note *"Prior… changes remain as scars rather than reverting"*; otherwise quashed (fear +0.08, dependency +0.03).
2. **Any transition away** clears the seniority stamps (relationshipEvolution.js:620-631).
3. **DM relabel** (curation) — the bundle's channels park dormant, DM-disabled survives, war-layer fronts stay war-layer-owned (graph.js:719-757).

Treaty expiry is NOT a vassalage end (no term writes the label); liberation lifts `occupations`, not vassal edges (`deploymentReturn.js:158-178` reads both separately); a WR-10 sale conveys settlements — *"Who holds a steading or a vassalage is a read"* (realmVerbExecution.js:187).

## THE VASSAL DATAFLOW (one line)
genesis link | DM link | subjugation proposal | occupation-vassalized → `regionalGraph.edges` (from=overlord) + `worldState.relationshipStates[edge.id]` (defaults row + stamps) → per-tick `vassalRules` candidates → `applyRelationshipPatch` (the one writer) → `syncRelationshipChannelBundle` (the 5-channel bundle into `regionalGraph.channels`) → readers: `relationshipRoles` (state-first direction), `protectorBackingScore`, `tradeWar.overlordOver`, `deploymentReturn.isVassal` — and, on a DISJOINT plane, `hegemonyRead` over `spatialLedgers.treaties`.

## DERIVATION SKETCH — the W-SEAT influence band (marginal/present/strong/dominant)
Constitutional shape: a pure per-call read like `hegemonyRead` — zero persisted state, floors-idiom banding (the `SHARE_BANDS` precedent, hegemony.js:40-45), and ONE resolver so this does not become the fourth governing-truth fork. Every input named with its home:

```
foreignSeat(S):                                     // pure; all reads live
  1. OCCUPATION (primacy):  worldState.occupations[S]           [occupation.js]
     F = rec.occupierId; grip01 = BENEFIT_SCALE[rec.state]      [occupation.js:123-132]
                          × (1 − rec.resistance)
     → occupier seat is 'dominant' at stabilized/vassalized, degrading down the ladder.
  2. VASSALAGE (scaled):    relationshipStates[key] where relationshipType==='vassal'
     F = relationshipRoles(edge, relState).seniorId             [relationshipState.js:381-393]
     grip01 = clamp01(0.35·leverage + 0.25·dependency + 0.2·pactStrength
                      + 0.2·fear − 0.25·resentment·(1−fear))    [all fields: ensured state]
  3. TREATY TIES (additive floor): for each subordinating term F is obligee of over S
     (treatyOrientationOf over spatialLedgers.treaties), add magnitude ×
     {honored:1, strained:0.6, defaulted:0.2}                   [reuse rankCompliance, hegemony.js:73]
  4. SCALERS (the owner's three signals):
     closeness01 = cultureAffinity(buildCultureVector(S), buildCultureVector(F),
                     { tradeTie01: TRADE_TIE[label] })          [cultureDistance.js:208; migrationKernel.js:154-197]
     trade01     = max confirmed trade-channel strength on the pair
                     (activeChannelsFrom, types P0)             [graph.js:796-808]
                   ∨ ROUTE_FLOW band index/4 on the pair edge   [routeNetworkFlows.js]
     stability01 = 0.34·trust + 0.28·pactStrength + 0.38·(1−strain)
                                                                [verbatim stableVassalage, rulesCore.js:736]
     influence01 = clamp01(grip01 × (0.6 + 0.2·closeness01 + 0.2·trade01))
  5. BAND (floors, tunable): dominant ≥0.7 · strong ≥0.45 · present ≥0.2 · marginal >0
  6. COUP/REBELLION coupling: reuse independencePressure         [rulesCore.js:763]
     and overlordWeaknessStreak — do NOT mint a second risk resolver; the occupier's
     cultivate/exploit posture is which existing candidate family the seat biases
     (tribute-extraction patches vs stable-compact patches), and stability01 is the
     legitimacy-side scaler the coup risk reads.
```

CONFIRMED throughout except: the exact grip/band weights are proposed tuning (PLAUSIBLE, owner-tunable by design); the heritage-kinship gap flag (b1) is PLAUSIBLE-design, not a code claim. Open question for the chair: whether the seat read should treat a treaty tie WITHOUT a relationship-vassal edge as a seat at all (hegemony says yes for spheres; the relationship plane has no opinion) — that is the fork's sharpest edge and it is owner-adjacent.

---

# V5 — FACTIONS AND COALITIONS

CENSUS V5 — FACTIONS, SEATS, AND COALITIONS (extract mirroring 73f5dfc02 at `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours`; all paths below relative to that root; all claims CONFIRMED by quoted code unless labeled PLAUSIBLE)

## (a) The faction model at depth

**Persisted roster** — `settlement.powerStructure.factions`, typedef at `src/domain/rulingPower.js:52-62`: fields `[faction, power, desc, category, rawPower, powerLabel]` plus situational `[modifier, isGoverning, legitimacyCrisis, crisisNote, modifiers]`. `id` is declared but *"NO GENERATOR WRITES IT. Measured … 2,175 faction rows over 360 settlements … `id` on ZERO of them"* (rulingPower.js:40-51 — measurement quoted from the source header, not re-executed this lane). `id` is the rename-decoupled seam for authored/DM records only; every reader must fall back. `powerStructure` also carries `governingName`, `government`, `publicLegitimacy {score,label,govMultiplier,crimMultiplier,+band booleans}` (rebandLegitimacy, rulingPower.js:311-333: Endorsed≥75 govMultiplier 1.30 … Crisis<30 → 0.60), `factionRelationships[{pair,type,direction,narrative}]`, `previousGovernments` (bounded 6), `stability`, `recentConflict`.

**Power numerics** — roster `power` is a 0–100 **relative weight, not a normalized share**: *"the sum≈100 seen at generation is a generation-time-only normalization … no runtime consumer enforcing it"* (applyWorldPulseOccupationAuthority.js:65-69). factionCompetition normalizes per-read: `raw > 1 ? clamp01(raw/100) : clamp01(raw)`, absent ⇒ `max(0.18, 0.72 − index·0.16)` (factionCompetition.js:125-129); warSeatBooks.normalizedPower tolerates both domains (warSeatBooks.js:100-105). Sim-time writers never renormalize: coup winner +6 clamped 0-100 (rulingPower.js:445), capture +5 / shift 8 (applyWorldPulseFactionRoster.js:40-41), occupier seeded at 90 (below), thieves-guild floor. So relative standings drift from the generation-time sum — any W-SEAT "just greater than the ruler" rule must be a **share/ordering rule at read time**, never an absolute-power write.

**Live mirror** — `worldState.factionStates[`${saveId}:${stablePart(name)}`]` minted by `ensureFactionStates` (factionCompetition.js:227-297): `archetype` (competition's 9-key fold), `governmentPreference`, `powerBases`, `controlledInstitutions/suppressedInstitutions`, `lawPreferences`, `internalSeats {leader_champion, lieutenant_operator, agent_protege}` (filled from NPC dotRank via seatNpcsIntoFactions :469-501), `rivals` (auto-seeded first 2 peers), `legitimacyClaim`, `riskTolerance`, `momentum`, `exhaustion`, `captureState`, `lastActedTick`, `recentAction`. **No power scalar — "the roster IS the live power source"** (projectFactionStatesOntoSettlement header :545-549). Ids are name-keyed ⇒ a coup rename leaves ghosts ⇒ 3-tick-grace pruning (:303-380). Live state projects back onto the roster additively (captureState, momentumBand, rivals-as-names, institutions, powerHighWater) with identity-no-op discipline (:556-614).

**Archetypes** — one canonical detector `src/domain/factionArchetypes.js` (13 values; **category wins over name regexes**, :110-111; arcane decided by the authored catalog per R-BLD-5). factionCompetition folds to 9: `government/other→civic, craft→merchant, occupation→military` (factionCompetition.js:27-32) — note **occupation→military** at factionStates-mint time, relevant to (c).

### The governing-faction resolver fork — it is FOUR-way, not three

1. **`governingFactionOf`** (rulingPower.js:203-209) — the canonical law: `factions.find(f => f?.isGoverning) || factions.find(nameOf(f) === ps.governingName) || null`. Consumers: coupContenders, warSeatBooks, warDecisionOpposition, applyWorldPulse seat transitions, ~20 more modules.
2. **`generosityKernel.governingArchetypeOf`** (src/domain/worldPulse/generosityKernel.js:303-312) — **highest raw `power`, roster-order tiebreak, ignores `isGoverning` entirely, and returns the raw `category` string** (not the canonical archetype) into `structuralLens` (:415, :695). Comment even says "(highest power)" as if that were the definition.
3. **`settlementPolitics.advanceSettlementPolitics`** (src/domain/worldPulse/settlementPolitics.js:766) — `views.find(v => v.isGoverning) || [...views].sort((a,b) => (b.power - a.power) || compareCodepoint(a.key, b.key))[0] || null` — isGoverning first, then highest-power with codepoint tiebreak, never null; decides the autarchy/conspiracy branch (:767).
4. **NEW FINDING — `beliefMap.pickGoverningFaction`** (src/domain/worldPulse/beliefMap.js:864-878), feeding `governingCoalition` (:907-920): isGoverning first, else highest power with **first-in-roster** tiebreak — and its doc comment claims *"codepoint tie-break for determinism"* while the code implements none. A fourth independent resolver with a doc/code mismatch.

Plus a display/generation family of ad-hoc spellings (`find(isGoverning) || factions[0]` at src/pdf/lib/headlines.js:49; `|| topPowerFaction` at src/generators/steps/generatePopulation.js:58; bare `find(isGoverning)` in ~25 generator/display files).

**The fork is live, not theoretical**: the isGoverning entry is routinely NOT the highest-power entry — `installOccupationAuthority` seeds the occupier at power 90 while the governing seat is cut ×0.6 and still isGoverning until the transfer; post-coup the ascendant power-behind (+6, uncapped by others' cuts) can out-power the reshaped seat. In those states resolvers 2 and 1 name **different factions**, and resolver 2 additionally speaks a different vocabulary (raw category, so `watch`/`law`/`temple` tokens never normalize).

**Category-keeps-seat (CONFIRMED)** — `transferRulingPower`'s seat rewrite (rulingPower.js:427-439) spreads `...f` and overwrites only `faction/name/desc/isGoverning/modifiers/legitimacyCrisis/crisisNote` — **`category` is never touched**. Since category outranks name inference (factionArchetypes.js:110-111), a seat generated `category:'noble'` still classifies NOBLE after a merchant appointment relabels it "Merchant Council": name-inference and category-inference of the same row permanently disagree after every transfer. (The one deliberate exception: the conquest occupier row is minted with `category:'occupation'` precisely *because* category wins — applyWorldPulseOccupationAuthority.js:73-77.)

## (b) Challenges and contests — three machineries

1. **`faction_government_challenge`** (factionCompetition.js:720-790): fires only for the **top-3 factions by rawPower** (topFactionEntries :207-224 — selection deliberately on raw power, weight discounted by espionage absence); gated on legitimacy pressure band ≠ 'stable' unless a WR-5 war-decision grievance ≥0.68 opens the lane; severity = `legitimacy·0.48 + power·0.28 + riskTolerance·0.14 + conflict·0.1` + bounded warPressure headroom; **always `applyMode:'proposal'`** (DM-gated). On approval: `applyFactionPayloadEffect` → `transferRulingPower(cause:'appointment')` (applyWorldPulseFactionRoster.js:107-126), plus an npcLadder seat-transition row + war-ruling news (applyWorldPulse.js:983-1057, with the pinned `fac.<slug>` id-space law).
2. **Coup verdict** (worldPulse/coup.js + rulingPowerCoup.js): field = top-3 **non-governing, non-criminal** factions with power ≥ 5; weight = power (× ladder leadership factor) × `COUP_COERCION[archetype]` (military 1.25 … labor/craft 0.85; criminal excluded — "the capture ladder is their path", rulingPowerCoup.js:102-104). Incumbent weight = power × `publicLegitimacy.govMultiplier`, and `gated` = amplified weight re-enters the top-3; ungated ⇒ pHold 0.08. `pHold = share·(1.15 − 0.4·severity) + authorityAdj(±0.125) + warSentimentAdj + interventionAdj + economicAdj`, clamped [0.1, 0.9]; winner sampled ∝ weight. Fall ⇒ `power_transfer` outcome → `transferRulingPower(cause:'coup')`; losers receive `competitive/escalating` grudge edges (rulingPower.js:490-498); player faction-lock downgrades to proposal (coup.js:181-195). **For §736: legitimacy already scales the coup at two of its three moments** — attempt (the legitimacy pressure band gates governmentChallenge; the coup stressor path) and success odds (govMultiplier amplification); aftermath severity reseeds by cause+old score (LEGITIMACY_SEEDS, rulingPower.js:340-346) but is not itself legitimacy-scaled. The design extends, it does not fork.
3. **Bounded roster contests**: `institution_capture` (+5), `institution_suppression` (real impairment, idempotent by causeEventId), `faction_power_shift` (moves min(8, loser's power)) — applyWorldPulseFactionRoster.js:127-158.

**What `transferRulingPower` does and does not change** (rulingPower.js:381-536) — the load-bearing shape for W-SEAT: the governing **BODY persists and keeps `isGoverning`**; it is *reshaped* to the winner-archetype's tier-banded government label (collision → ALT label → "… Ascendant"). The **winner never becomes isGoverning** — it gains +6 power + `'ascendant'` and is joined to the seat by a `'symbiotic'` relationship edge ("the power behind the …", :463-489). Legitimacy reseeds by cause (coup 38 / conquest 28 / election 56 / succession 48 / appointment 45, pulled by the deposed ruler's score). Institutions untouched; factions never added or removed (conquest must pre-seed the occupier); no renormalization. **The "power behind the throne" pattern — seat-row vs authority-row joined by a typed edge — already exists; a foreign seat is a third participant of exactly this kind.**

## (c) Coalitions — internal machinery EXISTS; it does not need inventing

**Settlement-internal blocs (settlementPolitics.js, W-DOCTRINE-4)** — persisted `worldState.politicsLedgers[cid].blocs`; `Bloc = {id (codepoint-sorted member stableParts), members[names], glue[{type: concession|patronage|doctrine|threat|compromise, detail}], end: seats|doctrine|commerce|survival|patron, strain, sinceTick, covert?}` (:62-68). Cap 3 blocs/settlement, min 2 members, dwell 8 ticks (tuning :83-154). Formation: ONE new bloc per tick via a §H loaded draw over pair affinity = structural archetype table (:277-286) × faith/alignment quadrant × rivals repulsion × **leader-tie modulation** (warm ×1.4; soft rivalry floors 0.12; bitter enmity hard-blocks at 0 — ":935-938 the people keep them apart"). Under an **autarchy** (resolver-3's govView archetype → `rulingPowerFromArchetype === 'autocrat'`) an opposition bloc forms **covert** (a conspiracy with a maturing discovery ramp, :953-974, :825-833). Dissolution: membership law, succession (people-held glue), exposure (compromise glue + revealed corruption leash), strain fracture ≥0.7 (differential peace-term burden → revanchism), threat-end, and **OUTBIDDING** — concession glue is a market; a non-member suitor with equal-or-better interest and +25% power margin lures a member away (:856-887). Effects: `rulingBlocOf` (the bloc containing the isGoverning member commanding ≥40% of roster power) → `blocDecisionFactor` loads strategy moves ±0.30 toward the bloc's end + strain revanchism (:625-647); `coalitionConsolidation01` → corruption-web resistance + warSeatBooks security; `factionRevanchism01`.
**The owner's "occupier competing in coalitions against the ruler" maps DIRECTLY onto this machinery.** The occupation case is nearly reachable today: the occupier is a roster faction (below), `ensureFactionStates` mints it a state, `factionViews` joins it, and nothing bars it from bloc formation. Two gaps: (i) at faction-states mint its archetype folds `occupation→military` (factionCompetition.js:31), so its bloc END derives 'seats' rather than 'patron' — the `patron` end and the `outsider` vocabulary exist (ARCHETYPE_END :458-463; goals `serve_patron`/`bind_external_patron`) but occupation is not routed to them; (ii) nothing ties the occupier faction's conduct to the OVERLORD settlement's interest.

**Derived governing coalition** (beliefMap.governingCoalition :907-920) — archetype-set members/opponents from the seat's declared rivals; feeds belief dissent and the **coalition-trust deposit** (factionCompetition.depositCoalitionTrust :438-462 — co-governing pairs bank `coalition_standing` +0.15 trust/year in the symmetric factionPairLedger; memoryWeave-gated).

**Realm-level war coalitions** — *"A coalition is never a membership object. It is the projection of ordinary bilateral `allied` relationship edges around one ordinary deployment"* (warCoalitionGraph.js:1-8) — the hegemony-is-a-pattern law already enforced; join/refusal is a per-court bounded decision that **reads readWarSeatBooks** + alliance-web risk (warCoalitionDecision.js); calls recorded in warCoalitionLedger; peacetime pacts are the fourth transport into the ONE treaty instrument (pactFormation.js). So the honest answer to (c): **internal coalition machinery exists and is the right substrate; what must be designed is the foreign seat's membership rules in it, not the coalition system itself.**

## (d) Slotting a foreign seat into contests — four typed precedents already exist

1. **`interventionAdj`** (rulingPowerCoup.js:140,176 ← convergence.js:501-551): `spatialLedgers.interventions` records `{interId, target, side: 'incumbent'|'challenger', motive, invited, strength, sinceTick…}` → signed strength-share tilt on the coup hold-chance, 0-when-dark. **A typed, sided, strength-weighted foreign participant in the seat contest already exists.** W-SEAT should generalize/ride this channel, not mint a parallel one.
2. **The patron book** (warSeatBooks.readWarSeatBooks :445-587): every war decision is already a **three-way weight split** — `settlementWeight01` (realm books) / `seatWeight01` (ruler's private books) / `patronWeight01` (a covert foreign patron via a corruptionWeb foreign asset on the ruling seat; `interestKind:'patron'` redirects the private book to the *patron court's own ruler's* biases). This is the derived foreign-share-of-decision pattern in production; the W-SEAT occupier/overlord seat is the same shape with **overt** legitimacy — a fourth book, or better, a generalized `books[]`.
3. **The occupation roster faction** (applyWorldPulseOccupationAuthority.js:42-87): conquest seeds `{category:'occupation', power:90, modifiers:['occupier']}` while the locals are disarmed (military ×0.3), humbled (seat ×0.6), suppressed (civic ×0.82), then transferRulingPower crowns it. The "occupier's influence always just greater" hook for OCCUPATION exists numerically; occupation-regime W-SEAT can be a read over this row plus `worldState.occupations`.
4. **`worldState.occupations[occupiedId] = {occupierId, state, resistance, benefitYield…}`** with the ladder `contested→unstable→extractive→stabilized→vassalized` (occupation.js:5-20,117) — **the cultivate/exploit dilemma's natural home**: extractive/stabilized ARE the exploit/cultivate poles, with capped/delayed/conditional benefit, burden, overextension, and resistance already priced; §736's rebellion-under-occupation should read `resistance` + state. Vassalage conveyance rewrites `occupierId` in place (sovereigntyTransfer.js — ONE conveyance writer, zero new keys). For VASSALAGE no roster faction exists at all — the overlord is a settlement id here, so the vassal-regime foreign seat must be a **derived view** (per the hegemony-is-a-pattern law), scaled by culture/trade/relationship closeness — `cultureDistance`, trade connectivity, and relationshipState reads all exist.

**Recommended participant type (design seed, PLAUSIBLE)**: a derived `ForeignSeatView {kind:'foreign_seat', regime:'occupation'|'vassalage', patronSettlementId, weight01, postureBand}` resolved at read time from `occupations` (+ the occupier roster row when present), injected exactly where interventionAdj and the patron book already inject, and admitted to `factionViews`/bloc formation as a typed member for the occupation case. Persist nothing new for vassalage.

## THE ONE-RESOLVER RECOMMENDATION (shared W-SEAT/W-COIN law)

Export ONE law from `src/domain/rulingPower.js` (the module that owns seats): `governingSeatOf(settlement)` with canonical precedence **(1) isGoverning → (2) name === governingName → (3) highest power, codepoint tiebreak on name → (4) null on empty roster** — the union of resolver 1 with the fallback resolvers 3/4 need, returning the entry plus derived `{archetype: factionArchetype(entry), power01}` so consumers stop re-deriving vocabulary. Migrate: settlementPolitics.js:766 (no behavior change — same precedence), beliefMap.pickGoverningFaction (fixes the comment/code tiebreak mismatch; behavior moves only on exact-power ties with no isGoverning — needs a same-seed check), and generosityKernel.governingArchetypeOf (**the one real behavior change**: it currently ignores isGoverning and returns raw category; routing through the law is a fix but moves lit-world generosity outputs ⇒ declared one-time shift + golden fence, owner-visible). `governingFactionOf` remains as the strict no-fallback read where null-means-no-seat is load-bearing (transferRulingPower's bail path). W-SEAT's `foreignSeatOf(worldState, settlementId)` should be a **sibling export in the same module**, so "who rules" and "who looms over the ruler" share one home and the foreign resolver can never fork the way the governing one did four ways.

Open questions ledger: (1) whether any lit consumer of beliefMap's tiebreak actually differs on real worlds (unmeasured — needs a corpus diff); (2) the 2,175-row `id` measurement is quoted from the rulingPower.js header, not re-executed; (3) whether occupation's `→military` fold at faction-states mint was deliberate (the roster row's own `category:'occupation'` comment suggests the canonical bucket was intended to reach dispositions — the fold predates it).

---

# V6 — BELIEF AND NARRATION

LANE V6 — BELIEF, INFORMATION, AND NARRATION: the contract W-SEAT must honor. All receipts from the build-tip extract mirroring 73f5dfc02 (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours/`); repo-relative paths below. Verdicts labeled CONFIRMED (read in full enclosing scope) or PLAUSIBLE.

## (a) THE BELIEF CONTRACT — believed-values or true-values for the foreign seat?

The estate's precedent is not one answer; it is a three-way split by QUANTITY, and W-SEAT must honor the split.

**A1. The belief substrate (CONFIRMED).** `src/domain/worldPulse/beliefMap.js:10-12`: `worldState.beliefMaps = { [observerId]: { [factionId]: { [subjectId]: { readiness, strengthBand, allianceLabel, faithLabel, confidence01, lastUpdateTick } } } }`. Gate (:22-27): "beliefs activate on `spatialCanonVersion` present AND infoMode != 'omniscient' … Marker absent OR omniscient ⇒ the reads fall back to GROUND TRUTH VERBATIM, forking no rng … SELF-reads stay ground truth (§IV.4 carve-out)." Update rule (:37-48): weighted reconciliation, provenance × recency × independence × completeness; confidence decays with silence (SILENCE_DECAY 0.92/tick, half-life ~8 ticks, :110-112); hop-decayed (HOP_DECAY 0.75, :125); distance-priced (`routeAwareHopDelayTicks`, `src/domain/worldPulse/distancePricedNews.js:30,65-66` — news ages by graph hops, worsened by embattled routes).

**A2. The no-omniscience law (K3/K4) (CONFIRMED).** `src/domain/worldPulse/conquestFeasibility.js:12-19`: "K3 GOVERNS (NOBODY IS EVER CURRENT). Feasibility is named explicitly in the amendment's list of paths that may never read true world state, so this module is pinned … at the strongest available setting: ZERO IMPORTS." K4 (:21-24): "no merged estimates. Every read here is ONE PARTY'S picture. Two parties are two calls; there is no shared feasibility object anywhere." The pin is structural, not convention: `tests/domain/envoyK3BeliefSeam.test.js:1-20` (closed import lists + token scan + guard-the-guard positive control). 16 modules in src/domain/worldPulse carry K3 headers (grep "K3" — denominator: 20 hits listed, 16 distinct envoy/negotiation leaves). Same-family: `secondOrderBelief.js:15-22` — the K3 fence made structural by NEVER passing worldState ("a function with no world cannot reach a partition, by construction").

**A3. The structure-vs-state split — the sharpest precedent (CONFIRMED).** `src/domain/worldPulse/settlementStrategy.js:388-390`: "A vassal obligation reads the TRUE label — a vassalage is a formal, known bond (the lord knows his own vassals); the belief fogs their STATE, not the contract." And the STATE side (:228-243, `beliefAwareBesieged`): the lord knows a vassal is besieged only via a held belief at `confidence01 >= SIEGE_AWARENESS_CONFIDENCE` (0.35, beliefMap.js:147-150 — "below this confidence the besieged vassal goes unrelieved (the info-starvation tragedy)"); no belief record ⇒ unaware; never invents a false siege.

**A4. The foreign-influence-weight precedent (CONFIRMED).** `src/domain/worldPulse/corruptionWeb.js:494-513`, `directionBias`: "the bounded covert weight-tamper (0..DIRECTION_MAX) a TARGET court feels toward a PATRON's interest … DM-truth only; the caller applies it as a post-sum, clamped nudge (the modulator idiom)." And :475-478 `foreignGripOf`: "DERIVED (never persisted) from its live foreign assets, saturating." `settlementPolitics.js:11-15` names this the same §H kernel as the overt ruling-bloc load. Sight is separate from weight: `assetSightFidelityOf` (:520-533) — a live asset is "paid eyes," a fidelity scalar that feeds the belief machinery (composed in beliefMap.js:66 via `composeBrokerageSight`/`makeBrokerageFloorFn`), never a direct truth read.

**A5. What the occupier/overlord can know today (CONFIRMED).** Vassal bundle, `src/domain/region/graph.js:649-656`: vassalage mints exactly 5 channels — political_authority 0.82 (overlord→vassal), military_protection 0.7, tax_obligation 0.76 (vassal→overlord), and two `information_flow` channels at strength **0.46/0.40, visibility 'gm'** — WEAKER than allies' (0.5, public, :667) and barely above trade partners' (0.42, :671). Occupation, by contrast, is NOT a relationship edge at all: `worldState.occupations[occupiedId] = { occupierId, state, sinceTick, stateHeld, resistance, benefitYield, lastTick }` (`occupation.js:5-10`), with the 5-rung ladder contested→unstable→extractive→stabilized→vassalized (:117-119) — `relationshipChannelBundle` mints **no channel bundle for occupation** (8 relationship families handled, none of them occupation; graph.js:635-693). Occupier-side reads that exist go straight at the record: `religiousContest.js:216-227` (`occupationFaithPull` reads `rec.resistance` and the occupier's capacity directly).

**A6. THE CONTRACT (the estate's answer, assembled):**
1. **Seat existence and weight = TRUE structural state, derived-not-persisted.** The occupation record's rung/control and the vassal edge (plus culture/trade closeness for the §735.2 scaling) are contracts, and contracts read true (A3). The weight applies as a bounded, clamped modulator in the target's decision arena (directionBias/blocDecisionFactor idiom, A4) — never a persisted second number. CONFIRMED as precedent; the extension is design.
2. **Seat direction (what the foreign power wants and what it reacts to) = the foreign COURT's beliefs, K3-clean.** What the overlord believes about the subject's strength/stores/loyalty must come from its beliefMaps entry / frozen pictures, hop-delayed and confidence-decayed — never `snapshot` truth. Under occupation the garrison is on site, so the design may grant a presence-derived fidelity FLOOR (the paid-eyes precedent, A4; also mobilization's visible `information_flow` signals, `mobilizationEffects.js:10,196-203`) — a floor into belief, not a bypass of it.
3. **Assertiveness gates on confidence.** A foreign seat holding stale/no intel should default toward abstention, exactly as the lord who never hears of the siege never marches (A3). Under vassalage the thin gm-grade 0.46/0.40 information_flow channel makes this bite — flag to W-SEAT-0 that this tuning becomes load-bearing.
4. **K4: no merged pictures.** The foreign seat's read of the settlement and the legitimate ruler's read are two objects; coalition competition compares them, never averages them.
5. **Dormancy:** marker absent / omniscient ⇒ ground truth verbatim, zero forks (A1) — the W-SEAT belief seam must inherit the identical fallback or it forks goldens.
6. **The extension affordance already exists:** beliefMap.js:16-20 — "THE FACTION KEY IS PRESENT FROM DAY ONE … so per-faction belief … can light in Wave B WITHOUT a schema break. Do NOT bake a single-settlement-mind assumption into the key." `FACTION_CARRIER_FRAMING` (:101-106) is frozen at merchant/military/criminal/religious (+ 'public'); a foreign-seat slot is the anticipated extension path, but **no carrier feeds it today** (PLAUSIBLE as design seam; the frozen table and empty religious slot are CONFIRMED).

## (b) THE NARRATION CONTRACT + THE NEWS-ESTATE BILL

**B1. The news-address law (owner doctrine, verbatim in memory/news-address-law.md, 2026-07-22):** every news item carries (1) the subject with its FULL address chain settlement→power→faction→NPC, (2) the typed action/state change, (3) affected settlement(s) explicit, (4) the reason from recorded cause. The wizardNews schema already carries the typed layers (CONFIRMED, `src/domain/region/wizardNews.js:81-96,559-597`): `settlementIds` + `settlementNames` (reader layer — rides `familyId`), `npcIds`/`factionIds` ("THE NEWS ADDRESS LAW's ACTOR layer … TYPED ids, so the Herald links a subject instead of scanning its own headline prose"), `thirdPartyIds`/`venueIds`/`termSheetIds`, `reasons`, `kind`, `familyId`, `audience`, `section` (closed 6-desk vocabulary war/faith/trade/events/divination/adjudication, :17-19), `sectionAuthority` (closed 3-member registry, :617-622).

**B2. Receipted-decision precedents an influenced decision must rhyme with (all CONFIRMED):**
- **The misjudgment receipt** — the canonical narrated-belief precedent: `beliefMap.js:1483-1594` — the chooser stamps `metadata.misjudgment` (what was believed, what was true, which telling misled, confidence) and `beliefMisjudgmentNewsEntries` mints "X marches on a misjudgment" with reasons including "Confidence in the belief it acted on: 0.NN". A foreign seat acting on stale word can reuse this machinery nearly verbatim.
- **Envoy receipts**: `envoyNews.js:1-72` — 16 governed scalar-free families, closed HEADLINE + REASON tables; `readerText` (:88-93) rejects digits, %, and tuning vocabulary (rng/roll/score/threshold/multiplier…) outright. Note `envoy_silence_inference`: "the court hardened a belief from silence rather than truth" — belief-narration is already house voice.
- **The occupation-caused conversion receipt**: `religiousContest.js:1010-1025` — typed outcome with `cause: occupations?.[cid]?.occupierId ? 'occupation' : 'contest'`, headline + reasons + amplifier tag. The foreign hand is NAMED as cause on the receipt.
- **Covert-influence receipts**: `informationStatecraft.js:1031-1036` (spy-exposure blowback receipts); covert entries fail closed off non-DM projections — `wizardNews.js:700-728` `projectWizardNewsForAudience`: "The DM receives the original feed, byte for byte. Every other audience fails closed: covert entries and entries explicitly authored for a DM-only audience are omitted."
- **§736's "typed modifier bands with receipted reasons"** has a live idiom to borrow: `warReasonFactor` / `blocDecisionFactor` — "factor = 1 + SPAN·pull, clamped" (`settlementPolitics.js:142,607-609`); war reasons are "typed, bounded, receipted contributors … recomputed each tick FROM EXISTING STATE" (`warReasons.js:5-12`).
- **Typed authority rows exist for the occupation arc**: `occupation_vassalized` is a campaign-altering candidateType (`decisionTier.js:41`, `changeAuthorityPolicy.js:265`) — W-SEAT's coup/rebellion/coalition outcomes join this vocabulary, not a parallel one.

**B3. THE NEWS-ESTATE BILL a new W-SEAT news kind pays (enumerated, all CONFIRMED at tip):**
1. **The authoring presence wall** — `tests/lint/wizardNewsAuthoring.walker.test.js`: every src/domain|src/store object literal co-locating `kind`+`headline` must carry `id` + `settlementIds` + `severity` + an explicitly registered static routing token; "a new site may never buy itself an exception" (19-row frozen ledger, shrink-only). Denominator floors re-pinned at measured truth: ≥999 files / ≥99 candidate sites / ≥98 governed sites / ≥58 paths.
2. **Herald routing totality** — `tests/lint/heraldRouting.walker.test.js:1-23`: "A NEW minted kind, or a base domain that grows, makes a member unrouted → RED, forcing a human to file it in heraldRouting.js" (EXACT_SECTION entry or family prefix; `isExplicitlyRouted`, heraldRouting.js:453-467 — landing in the events catch-all by decision is legal, by fall-through is not). Plus consistency with chroniclersLetter's KIND_SECTION modulo recorded divergences.
3. **SP-6 family identity** — prose must go through `pickLineWithFamily` (`eventProse.js:80-102`): "Slot fills never create a new family … persisted beside Wizard News prose so the SP-6 repetition instrument can distinguish real authored depth from one sentence dressed in different names."
4. **The address layers** — settlementNames only persists when `familyId` is set (wizardNews.js:562-566), and `significance: 'routine'` survives normalization ONLY with a familyId (:539-543) — an ungoverned kind is force-promoted to 'notable'.
5. **Reader-text law** — scalar-free receipts for governed families (envoyNews `readerText` ban list); reasons in complete natural sentences.
6. **The rumor voice** — the flagship rumor surface computes a mutilation-prone fallback for unknown kinds; the fallback corpus is 107 kinds, append-only, order load-bearing (`rumorFallbackPhrasePoolsEvents.js:1-40`). Degrades rather than reds, but a W-SEAT kind without a WHAT_PHRASES/pool row ships engine-token prose.
7. **Covert/audience discipline** — any covert-influence receipt must set `covert: true` (survives normalization, wizardNews.js:598-603) and accept the fail-closed player projection.
8. **sectionAuthority is a closed 3-member registry** (war_rulings_registry / war_coalition_registry / envoy_registry, wizardNews.js:617-622) — W-SEAT authored receipts wanting an authority stamp require widening normalizeEntry, a schema-visible change.
9. **Second-truth**: any receipt naming "who holds the seat / who governs" must ride the ONE governing-faction resolver the W-COIN A1 fold collapses the three-way fork to (ODQ §735.1 BLOCKER) — W-SEAT must not mint a fourth spelling in prose composers either.

## (c) THE IMPOSED-CULT PRECEDENT — how an occupier already reshapes a settlement

**Shape (CONFIRMED).** Not a direct write — a DERIVED PRESSURE through the same spread machinery every faith uses. `religiousContest.js:216-227`: `occupationFaithPull` ⇒ `control = force × (0.4 + 0.6 × (1 − resistance))` off the occupation record; `warbound = deityTemper(occupier's deity) === 'warlike'`. The lift (:777-782) composes into the ordinary entry strength — `lift = OCC_CONVERSION_GAIN × control × warboundMult × (1 − incumbentCounterForce)` — and the incumbent structurally resists (:229-249: temperament/alignment/law-method opposition each mount counter-force). Entry then runs the SAME three-path gate as any faith with `{ force: Boolean(occupied) }` (:786): `attemptEntry` (`religionState.js:275-328`) — "force (occupation) bypasses the capacity cap" — with typed outcome paths (`same_niche_pushout`/`forced_overflow`/`open_slot`/`cross_niche_eviction`/refusals).

**The stain and the receipt (CONFIRMED).** A force-installed faith carries `heresyStain: LEGIT_STAIN_IMPOSED` = 0.45 ("heresy stain on a force-installed faith (DM impose / occupation)", religionState.js:56), seeded near-illegitimate (LEGIT_SEED_CULT 0.08) — and the share-flip case attemptEntry misses is caught at the patron-change receipt (`religiousContest.js:996-1003`: "a faith that took the seat under the garrison … carries the heresy stain even if it had already entered un-forced → low legitimacy → brittle"), with the conversion news typed `cause: 'occupation'`. The DM path is the same single applier (`cultImpositionApply.js:62-105`, `reconcileCultImposition`, typed action added/replaced/evicted/refused + reason + evicted name).

**Doctrine bounds (CONFIRMED).** Only the occupier's OWN embedded faith projects (`deitySnapshotFor(snapshot, occupierId)`, religiousContest.js:222) — no premade pool, deities enter worlds only by custom content, engine total over 0..MAX deities (deity-doctrine memory, owner ruling 2026-07-21); temperament reads through the derived `deityTemper` shim, never a theological table.

**What W-SEAT should take from it:** the occupier-reshapes-a-settlement idiom is (1) derived pressure into an existing contest, never a state overwrite; (2) force widens the gate but cannot erase resistance; (3) the imposition is STAINED — an influence exercised by garrison is minted with low legitimacy and brittleness, which is precisely the §736 hook (occupation-imposed outcomes should feed rebellion risk the way heresyStain feeds religionLegitimacy); (4) every imposed outcome lands as a typed, cause-named, receipted event. The occupation ladder itself (contested→unstable→extractive→stabilized→vassalized, occupation.js:117-119, with `STATE_BENEFIT_SCALE` extractive 0.45 < vassalized max and mobilization's `OCCUPIER_CONTROL_STATES`) is the existing typed vocabulary for the cultivate/exploit posture — W-SEAT extends these rungs, never forks a second posture enum.

## Open seams W-SEAT-0 must decide (flagged, not defects)

1. **No information substrate for occupation** — occupier intel freshness has no channel today (A5); the garrison-presence fidelity floor would be new derived machinery (precedent: `assetSightFidelityOf`).
2. **Vassal information_flow is gm-grade and weaker than allies'** — if seat assertiveness gates on belief confidence, graph.js:654-655's 0.46/0.40 becomes tuning-signature-relevant (owner-signed class).
3. **The foreign-seat belief slot** — anticipated by the beliefMap faction key, unfed by any carrier; lighting it is a Wave-B-shaped extension, not a schema break.
4. **Foreign-seat misjudgment receipts** — the machinery exists end-to-end (detectMisjudgment → beliefMisjudgmentNewsEntries); a seat that moves the subject on the overlord's stale picture should pay the same receipt, or the fog of war goes silent exactly where the owner's directive makes it most consequential.

---

# V7 — COLLISIONS AND ECONOMICS

LANE V7 — COLLISIONS AND ECONOMICS. Sources: build-tip extract mirroring `73f5dfc02` (src paths below are that tree), ledger docs at `cb0e035b4` (§737–§739, sealed `docs/DESIGN_W_COIN.md` with A1), tests rostered via `git ls-tree 73f5dfc02 tests/`. Read-only; no gates run.

## (a) COLLISION TABLE

**W-SEAT's likely engine surface (CONFIRMED present at tip, with the wiring §736.1 predicted):** the legitimacy machinery is already partially wired on all three coup moments — ATTEMPT: `src/domain/worldPulse/stressorGates.js:741-764` `coupSpawnGate` reads `powerStructure.publicLegitimacy.score`, hard-refuses at ≥45, bandMult 1.0 at Crisis vs 0.35 at Contested ("rare at Contested, likely at Crisis"); SUCCESS: `src/domain/rulingPowerCoup.js:85` (`govMultiplier` from the legitimacy band weights the incumbent) plus the verdict's additive adj slots in `src/domain/worldPulse/coup.js:113-135`; AFTERMATH: `src/domain/rulingPower.js:340-346` `LEGITIMACY_SEEDS` reseed by cause with `oldPull`. Rebellion under occupation is already split: `stressorGates.js:573-590` `rebellionGate` refuses under occupation ("Under occupation the uprising is a RESISTANCE — insurgency models it", :578-579) and `insurgencyGate` :535-547 gives mult 2.0 under occupation with legitimacy floors. Mobilization ramp CONFIRMED (`mobilization.js:109` `COOL_LEGITIMACY_FLOOR = 30`, :155-157, :228). Occupation posture substrate CONFIRMED: `occupation.js:117-119` STATE_LADDER `contested→unstable→extractive→stabilized→vassalized` with benefit/burden scales (:127-148) — "extractive vs stabilized" is the cultivate/exploit dilemma already encoded as a suitability-driven state, not an occupier choice. Occupier-as-roster-faction CONFIRMED: `applyWorldPulseOccupationAuthority.js:70-82` seeds the occupier at power 90, `category:'occupation'`, `isGoverning:false`. Coalitions: `settlementPolitics.js` blocs (the "overt twin of the corruption web", :11-15). Decision-books precedent: `warSeatBooks.js:1-14` already splits a war decision across realm/ruler/**covert patron** books — W-SEAT's overt foreign seat is that leaf's overt twin, and it already resolves the seat via `governingFactionOf`+`factionArchetype` (imports :19-29). Vassal substrate: `occupation.js:1232-1242` `vassalOverlordOf` (relationshipStates `relationshipType:'vassal'`, junior/senior roles), `relationshipHierarchy.js:223`, `hegemony.js:56-58` `SUBORDINATING_TERM_TYPES` (derived-pattern law, zero persisted state), `peaceTermsCatalog.js:174` `puppet_seat`.

| Collision | Shared surface (files) | Verdict |
|---|---|---|
| **W-COIN-1a/1b × W-SEAT: the ONE governing resolver** | `src/domain/rulingPower.js` — A1.12 (sealed doc :440): W-COIN-1 exports `governingFactionOf`+`factionArchetype` as the single resolution law "shared with W-SEAT", and `transferRulingPower` gains the archetype STAMP (declared shift). The estate's three-way fork the resolver collapses: `rulingPower.js:203-209` vs `settlementPolitics.js:766` (`find(isGoverning) || highest-power`) vs `generosityKernel.js` `governingArchetypeOf` (~:302-312, highest power, raw `.category`, no `isGoverning`). | **HARD ORDER: W-COIN-1a first.** W-SEAT consumes the export; a fourth resolver is prohibited (the lane charter's own law). `transferRulingPower` is also W-SEAT's aftermath site (per-archetype legitimacy-sensitivity, §736.1) — two cars editing one function; sequence, don't parallel. |
| **W-COIN-1 × W-SEAT: the occupied-NOW predicate** | A1.14 (sealed :442): occupied ⇒ zero-yield + `suspended_by_occupation` receipt, ruled in car 1, "Both feed W-SEAT's occupied-economics spec." The estate holds ≥4 occupied representations: `worldState.occupations` ledger (materializes on first conquest — `display/occupationStatus.js:22-26`), the `occupation` STRESSOR in activeTypes (`stressorGates.js:542,749`), roster modifiers `occupied/occupier/disarmed` (`applyWorldPulseOccupationAuthority.js:56-63`), and the display projection. | **W-SEAT-0 must charter the SAME predicate W-COIN-1a mints** — one quantity, one resolver. This is W-SEAT's front door (§735.2) and the highest-risk second-truth seam in the whole design. |
| **W-COIN × W-SEAT: the legitimacy read-point** | Sealed doc :217 declares `governanceLedger.js:31-35` "the canonical legitimacy read-point" and routes the tax legitimacy price through `applyLegitimacyDeltasToUpdates` (`generosityUpdates.js:80-107`). But live paths read four ways: raw `powerStructure.publicLegitimacy` (`coupSpawnGate` :746-747, `mobilization.js:155-157`, `occupation.js:342`), `governanceLedger(...).legitimacyScore` (`rebellionGate`/`insurgencyGate`/`politicalFractureGate`), `causal.scores.public_legitimacy` (`opportunism.js:142`), and `causal.ruling_authority` as a proxy. | **PLAUSIBLE fork-family.** `governanceLedger` is a wrapper over `publicLegitimacy` (its :57 reads `leg.score`), so it may be one truth in two coats — but W-SEAT-0's §736.1-ordered census must prove that and route new reads through the declared canonical point. |
| **W-COIN-3 × W-SEAT: extraction is posture's material leg** | W-COIN-3's occupation extraction transfer + vassal→overlord flow (draft §6.3, A1.9's six named flows) vs `occupation.js:515` `computeOccupierBenefit` (war-support units, capped/contained). Shared files: `occupation.js`, `peaceTerms.js`, `pulseKernel.js` (stage home named at W-COIN-3 charter, A1.4). | **Two "what the occupier gets" quantities** (benefit units vs coin) — must land as separate typed quantities or reconcile, and posture (cultivate/exploit) modulates both. W-SEAT's posture spec must be sealed BEFORE W-COIN-3 dispatches; W-COIN-3 is already parked behind Q4/Q8 + the A1.22 harness fork, so this costs nothing. |
| **W-COIN-2 × W-SEAT** | `warCosts.js`/`warCoalitionExpenditure.js` — W-SEAT's war interface is seat-books/decision weights, not the cost coffers. | **Near-zero file overlap.** Shared only in machinery: the A1.18 news-bill discipline (below). |
| **WEAVE POLIS-1/2 × W-SEAT** | POLIS-1 "genesis diplomacy assignment + suzerain-closure" (WEAVE doc :61), POLIS-2 materializes reciprocal links — this WRITES the vassal/suzerain relations `vassalOverlordOf`/`relationshipRoles` read. | **Producer→consumer contract, not a same-file edit** (POLIS is plan/composer-side). Genesis-minted vassal edges must be recognizable to the runtime idiom and must permit derivation of W-SEAT's closeness inputs (culture — `spatial/cultureDistance.js`; trade — `tradeSalience.js`; relationship warmth). Clean split: W-SEAT engine reads runtime edges only; POLIS edges simply populate when they land. |
| **WEAVE DESK-4 × W-SEAT** | DESK-4 "perspective-anchored standings, belief-scoped from `display/warStatus.js`" (WEAVE doc :135); display-plane, post-STRIP-6. | **Display of what W-SEAT computes.** W-SEAT engine exports a pure read (the `occupationStatus.js` projection idiom, "heuristic DM language, no internals"); W-SEAT's own display car joins the DESK family. No engine file shared. |
| **WEAVE W-ARMS / ST-2 × W-SEAT** | W-ARMS reads the vassal vocabulary ("vassal quarters the liege's charge", WEAVE doc :77) — read-only, last. ST-2 = goods unification: zero shared files. | ST-2 matters only via A1.20's global order (ST-2 → W-COIN-1a). |
| **DW × W-SEAT** | — | **DISSOLVED by §739.1**: DW (~42) is deferred to the post-launch module by owner order; C-19/AD/CT-1b/COVER go with it. No file overlap, and the capacity contention in the pre-soak window is gone. (A1.20's "DW-0 is told `economicState.treasury` exists" is now a module-side note.) |

## (b) GATE ECONOMICS — THE CENSUS BILL

- **Flag bill: ONE new treasuryEnabled-class ENGINE-GATED VIRTUAL rule key** (recommend a single `foreignSeatEnabled`-shaped key; naming is W-SEAT-0's). Rationale, CONFIRMED: W-SEAT's coup/rebellion arms sit on the SHIPPED-LIT stressor path, and the estate's precedent for exactly this shape is `economicCoupReadEnabled` (`coup.js:118-127` — "a VIRTUAL flag ABSENT from DEFAULT_SIMULATION_RULES… the flag absent ⇒ 0 ⇒ byte-identical"). Every W-SEAT term on a lit path must be an additive adj that is exactly 0 when dark. Sub-behaviors compose with existing substrate flags rather than minting siblings: coalition arms under the `settlementPoliticsActive` composition (`settlementPolitics.js:26-39`), occupation arms under `warLayerEnabled` (the war-sub-flag nesting precedent). Whether the coup-scaling and the foreign seat need *independently ownable lighting* is a W-SEAT-0 question (sibling of W-COIN's Q10 — lighting is owner-owned, A1.21). Per-key idiom cost, all in ONE commit (the W-COIN §7.1 template): `ENGINE_GATED_VIRTUAL_RULE_KEYS` entry (`simulationRules.js:158-185`; roster ~28 keys today) + `tests/lint/engineGatedRuleKeys.walker.test.js` proves membership both ways + certification row in `subsystemRowsVirtual.js` + **mechanismLitCoverage** credit, which requires a LITERAL `<flag>: true` drive in a test (`tests/property/mechanismLitCoverage.test.js`; literal-only — `contributionLedgerShape.test.js:12,198`) + dormancy proof.
- **Dormancy bar**: engine parts are **raw-byte** (base-dormant vs tip-dormant, §713.2 — never a diff over stale artifacts); the family precedents are the `*Dormancy.byteIdentity.test.js` files plus `tests/property/settlementPoliticsDormancyGolden.test.js`. Display parts ride the dormancyOracle class (W-COIN-4 precedent).
- **News bill** (A1.18 discipline — enumerate in the charter, price the car): legitimacy-receipted coup/rebellion reasons ("emboldened by the crown's waning legitimacy"), posture receipts, and foreign-seat coalition beats are authoring sites under the wizard-news census, whose denominator is *every object literal under src/domain or src/store co-locating `kind` and `headline`* with `REQUIRED_FIELDS ['id','settlementIds','severity']` (`tests/lint/newsAuthoringCensus.shared.mjs:1-20`), plus `newsHeadlineContract.walker`, `newsVoiceContract.walker`, kind pools (`warCoalitionKindPools.walker` precedent), and their three baselines in `tests/lint/`.
- **Test-estate homes (extend before minting)** — of the ~150 political/news test files matching the faction|rulingPower|occup|coup|legitim|relationship|worldPulse grep at tip, the ~20 direct homes: `tests/domain/rulingPower.test.js`, `coupEvents.test.js`, `occupation.test.js`, `occupationConversion.test.js`, `occupationRecordMode.test.js`, `deploymentReturnOccupied.test.js`, `factionCapture.test.js`, `factionArchetypes.test.js`, `worldPulseFactionSeating.test.js`, `settlementPoliticsPins.test.js`, `warSeatBooksFactionAddress.test.js`, `hegemonyRead.test.js`, `hegemonyFear.test.js`, `relationshipHierarchyCascade.test.js`, `religionLegitimacy.test.js`, `religionLegitimacyFactionKey.test.js`, `clergyLegitimacyDrag.test.js`, `coalitionDissentCoupSoak.m9d.test.js`, `tests/components/factionLockCoupShield.test.jsx`, `tests/generators/legitimacyBgConsistency.test.js`/`legitimacyDefenseReconcile.test.js`.
- **New-file bill estimate: 2 test files** (a `foreignSeat.test.js`-class unit home + a `foreignSeatDormancy.byteIdentity.test.js`; a third only if posture lands as its own module). Per new file: lighting-census re-derivation WHOLE (all five figures, same commit, batched per landing window — WEAVE fold 13 / the three-censuses law), negativeAssertionAnchor ceiling 0, and mutation-manifest ONLY if the basename carries invariant nomenclature or lands in an enforcer dir — so keep `census|walker|golden|contract|pin` out of the basenames (the `byteIdentity` naming dodge is the W-COIN §10 precedent). Src side: any new persisted field owes the `settlement.schema` typedef + a fieldManifest FROZEN_VS_LIVE row — home is **`tests/joins/fieldManifest.test.js`** (the estate lens already caught W-COIN's doc misnaming it `tests/domain/`) — plus importScrub-strip membership for any lived numeric (the A1 imports-arrive-coinless precedent). Prefer zero new persisted fields: seat weight and vassal closeness are derivable (hegemony-is-a-pattern law; `hegemonyRead`'s zero-persisted-state header is the constitutional template), leaving persistence only if posture becomes a choice rather than a derivation — that fork is W-SEAT-0's to argue, owner-visible.
- **Engine vs display split**: engine/raw-byte = seat weights in decision paths, the three coup-moment scalings, posture, extraction hooks, any pulse-stage wiring; display/oracle = standings projection, DESK-4 belief-scoping, occupationStatus extension, dossier/PDF rows.

## (c) SEQUENCING RECOMMENDATION

Post-§739 the launch arc is `STRIP 2/4/5/6 · WEAVE · W-COIN (post Q10/Q11) · W-SEAT (post architecture) · content/web tail · OSR · parity → ultra → walk + ONE regen → terminal soak` (§739.2), with §737.1's dispatch order `ST-2 → W-COIN-1a → …` and no coin car before Q10/Q11. Recommended W-SEAT slotting:

1. **W-SEAT-0 (architecture + skeptic panel) dispatches now** — it is not blocked by Q10/Q11. Its charter must include the §736.1-ordered census (this lane's receipts above are its seed), the occupied-NOW predicate ruled JOINTLY with W-COIN-1a's charter, and the legitimacy read-point law.
2. **No W-SEAT implementation car before W-COIN-1a lands** — 1a carries the shared resolver export (A1.12) and the occupied-window rule (A1.14) that are W-SEAT's substrate; `transferRulingPower` is a two-car file (stamp, then per-archetype seeds) and must be serial.
3. **W-SEAT engine cars after W-COIN-1b, interleavable with W-COIN-2's window; W-SEAT's posture spec seals before W-COIN-3 dispatches** (extraction is posture's material leg; W-COIN-3 is already behind the A1.22 harness fork, so this is free).
4. **Genesis-vassal arms decouple from POLIS**: W-SEAT reads runtime edges only; POLIS-1/2's genesis diplomacy populates the same substrate whenever it lands (its edge shape must satisfy the `vassalOverlordOf`/closeness contract — name that in both charters).
5. **W-SEAT display = a DESK-family car, post-STRIP-6**, belief-scoped per DESK-4's law; never blocks the engine cars.
6. DW imposes nothing (module-side); ST-2 touches W-SEAT only through the A1.20 global order.

**Open flags for the chair**: (i) the legitimacy read-point family (PLAUSIBLE one-truth-two-coats — must be proven, not assumed, at W-SEAT-0); (ii) occupier-benefit vs coin-extraction as two typed quantities (second-truth risk at W-COIN-3); (iii) whether W-SEAT's lighting is independently owner-owned or rides Q10's answer.

---

# D10-C1 — FORCE SUBSTRATE (census wf_7192952f-226, verbatim)

All reads executed against the build-tip extract at `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours` (mirrors 73f5dfc02; paths below relative to that root) and the sealed volume `git show e14b9a0db:docs/DESIGN_W_SEAT.md` (93 lines; the D10/D11 addendum is its lines 87–93). Every claim below is CONFIRMED by direct file read unless marked PLAUSIBLE.

# LANE C1 — THE FORCE SUBSTRATE

## A. The exact derivation recipe for "a regular military unit of this settlement in normal wartime"

A regular army is **derived at deploy time from a 0..100 capacity model, then evolved as a stateful record**. The full pipeline, in order:

**A1. The latent base — `deriveMilitaryCapacity` (src/domain/worldPulse/militaryStrength.js:151–314).** Six facets, each 0..100, weighted into `theoreticalCapacity` (L74–81: manpower 0.26, institutions 0.24, materiel 0.20, logistics 0.12, economy 0.10, will 0.08). Facet derivations:
- **manpower** (L171–181): `tierFrac * 70 + popScore * 30` where `tierFrac = TIER_ORDER.indexOf(tier)/5` (TIER_ORDER = `['thorp','hamlet','village','town','city','metropolis']`, src/data/constants.js:3) and `popScore = min(1, log10(max(10,pop))/5)`. **This is the ONLY place population enters army size, and it is log-scaled, ≤30 points.** Tier carries most of the signal.
- **institutions** (L183–200): `defenseLedger(s).military * 0.7 + min(milInstCount,5)/5*30`, over the LIVE (ruin-filtered) institution roster matched by `MILITARY_INSTITUTION_PATTERN` (L87: `/garrison|barrack|armory|…|legion|warden|marshal/i`).
- **materiel** (L202–230): `50 + min(domesticHits,6)*7 + min(importHits,4)*3` — regex-classified war-goods exports/institutions (`MATERIEL_PATTERN` L85); imports discounted ("access without control").
- **logistics** (L232–242): `foodLedger.resilienceScore * 0.6 + min(storageMonths,12)/12*40`.
- **economy** (L244–250): the `economic_capacity` causal score verbatim.
- **will** (L252–273): 50 base, +14 martial government / −10 pacifist (regex on `powerStructure.government`), +8 warlike deity temper / −6 peacelike (via `deityTemper`, dormant when deity-free).
- **currentCapacity** = theoretical − `warExhaustion*22` − `warDrain*18` (L286–305), both read from active conditions stamped only by the war layer.

**A2. The envelope — `buildCapacityLookup` (src/domain/worldPulse/warCapacityReads.js:170–196).** `offensive = max(0, currentCapacity)`; `homeDefense = max(0, offensive − 14)` iff `deployments[key]?.targetId` (ARMY_DEPLOYED_CAPACITY_PENALTY = 14, L56 — a flat one-army-away debit).

**A3. The gate — a settlement may only OPEN a war from posture `mobilized`/`deployed`** (warDeployment.js:999 `isWarReady(warPosture[fromId]?.state)`; mobilization.js:74 `READY_STATES = new Set(['mobilized','deployed'])`). The ramp `peace→alert→war_preparation→mobilized` (mobilization.js:64) advances at `RAMP_BASE_RATE 0.34 × rampReadiness` where `rampReadiness = max(0.25, 1 + 0.9*(aggr−1) + 0.5*(econ/100−0.5) + 0.4*(legit/100−0.5)) × readinessMobilizationMult` (L201–213); readinessMobilizationMult = `1 + 0.6*readiness01` (martialReadiness.js:271–273). Cool triggers: economic_capacity < 30, legitimacy < 30, food < 1.5 months, occupier control at extractive+, treaty demilitarization cap (mobilization.js:108–110, 83, 301–332). **The ramp's force effect is binary (may/may not deploy) plus the `war_mobilization` condition severity 0.32/0.5 (L117–121); it does not scale committed strength.** Also gated: strength ≥ HOSTILE_CONFIDENCE 0.42, margin > CONQUEST_MARGIN 0.12, `classifyFeasibility` verdict (warDeployment.js:172–173, 1066–1075).

**A4. The seed — `seedDeploymentState` (src/domain/worldPulse/warArmyRecord.js:85–138), every field:**
```
start = max(0, cap.offensive) × effectiveStatMult(readiness) × sizingFactor × qMult   (L103)
```
- `readiness` at the deploy site = `clamp01(readinessOf(fromSettlement) + mercSupplementOf(mercLedger, fromId))` (warDeployment.js:1096); `effectiveStatMult = 1 + 0.25*readiness01` (martialReadiness.js:287–289). Merc supplement ≤ `SUPPLEMENT_MAX 0.35 × activity` (mercenaryMarket.js:54, 236).
- `sizingFactor` = the W-C1 rust fork (`fidelityFactor` on site `deployment_sizing`, key `commit:<target>`; rust 0 ⇒ 1, no rng) — rust = `rustOf(settlement) + mercFidelityPenaltyOf` (warDeployment.js:1099), where rust = `min(0.35, 0.35·inexp^1.4)` off `experience01` (martialReadiness.js:235–240).
- `qMult` = `deployedQualityMult(snapshot, id)` under flag `warSupplyQualityEnabled` else 1 (warDeployment.js:474–475): `0.55 + 0.45 × warKitCompleteness` over the basket `['arms','iron','leather','livestock','provisions']` (supplyQuality.js:35–39, 71–75).

Record fields (L104–137): `targetId, sinceTick, role`, `maxStartStrength = currentEffectiveStrength = start`, `accumulatedAttrition: 0`, conditional stamps `readiness` / `sizingBias` / `deployedQuality` (only when non-trivial — byte-identity), `reinforcementFlow: 0`, `deploymentAge: 0`, then the supporting facets (0..1): `manpower = facets.manpower/100`, `supplyIntegrity = (logistics/100)·qMult`, `morale = (will/100 + manpower/100)/2`, `equipmentCondition = (materiel/100)·qMult`, `magicSupport = materiel/100`, **`commandQuality = institutions/100`** (its ONLY derivation — L131; no other producer exists), `foodReserve = logistics/100`, `logisticsBurden`, `objective`, `returnCondition: 'pending'`. Later stamps: `casusReasons` (warDeployment.js:1120), `joinLedger` (1103), `deployedPopulation`/`leviedPopulationBySource` (warHomeCosts.js:362, 449–453), `recalled` (strategy path).

**A5. Per-tick evolution.** Step 0 re-enriches + ages and CAPS strength at the home's live `cap.offensive` (warDeployment.js:411–432 — "the stateful army cannot stay stronger than the worn home can sustain"). The verdict sums member `currentEffectiveStrength` (falling back to fresh offensive for light records) into `coalitionCurrent` vs defender `homeDefense` (warSiegeVerdict.js:132–142). Attrition: `loss = base(0.035 atk) × band(0.8–3.0) × strengthTilt(0.4–2.2) × ageRamp(≤1.5) × fort(≤1.6) × (1−mitigation≤0.65) × attritionDecayMult(readiness) × firstTickMult`, capped 0.42/tick (attrition.js:46–118, 218–219); facets erode with it (L273–280). Reinforcement: ≤6–10% of maxStartStrength/tick from the origin envelope (economy/manpower/materiel/food/trade/legitimacy weights, reinforcement.js:40–56), zeroed if home besieged. **Note (attrition.js:75–87): `BAND_DEFENDER` is RESERVED dead code by documented deferral — a pure defender has no eroding pool unless the `defenderAttritionEnabled` spike is on.**

## B. Army SIZE is a strength scalar, not a headcount — the D10 sizing gap

CONFIRMED: nowhere does a regular army have a mustered head count at birth. `maxStartStrength` is 0..100-scale capacity points. Heads exist only as the **flag-gated per-tick drip**: conscription `0.6%/tick of home pop, floor 250` under `warEconomyDrainEnabled` (warHomeCosts.js:281–282, 356–377) and vassal levy `0.4%/tick, floor 300` under `warLevyEnabled` (L82–83, 402–434), banked on `deployedPopulation`/`leviedPopulationBySource` and returned as `survivors = deployedPopulation × strengthRatio` with largest-remainder per-source apportionment; the shortfall is the only death sink (deploymentReturn.js:266–349). **Consequences for D10:** (a) "size = the returning army's recorded manpower × splinter fraction" — the recorded manpower is `deployedPopulation`, which is ZERO in flag-off worlds and zero on the deploy tick even flag-on (the bank accrues per tick); (b) "size = population × participation01" births a force in HEADS with no existing heads→strength-points conversion — the closest bridge is the manpower facet's log-population term, and the `computeSackTransfer` head arithmetic (warArmyRecord.js:56–63) is the house pattern for conserved head math. The "wartime-levy profile" a popular rising would discount is concretely: the facet vector of A4 at `readiness`/`qMult` of the settlement, with the conscription/levy rates as the only head-rate constants in the estate.

## C. Veteran-share inputs — everything a derivation could read (census)

1. **`experience01`** (settlement scalar, 0..1, recency-EMA of actual engagement; `RUST_FLOOR_EXPERIENCE 0.85` = "seasoned veterans") — martialReadiness.js:93–102, 214–227, persisted at `config.faithProfile.martial` (L246), **but only for settlements with a patron deity** (advanceMartialReadiness L482 `if (!patron) continue`) — a deity-free town has NO veteran scalar; `experienceOf` defaults 1 (L257–260). Its inputs: deployed/besieged/occupied contact + win/loss (winloss deliberately 0 today, L496).
2. **`readiness01`** (structural militarization ratchet, decades-scale decay) — same record; the closest "standing veteran cadre" signal.
3. **`warExhaustion[id]`** (0..1 non-reverting scar ledger, worldState) — warHomeCosts.js:68–69.
4. **`dispositionStats`** win/loss history — banked by every siege resolution (warDeployment.js:647–673, 867–868), read by `computeAggressiveness` (disposition.js:262–271).
5. **Returning-army records** — `deployedPopulation`, `currentEffectiveStrength/maxStartStrength` ratio, `accumulatedAttrition` — but **deleted at return** (warDeployment.js:875 `delete deployments[attackerId]`); no post-war veteran-count ledger survives. The splinter path D10(a) extends mints only a `faction_challenge` condition, severity `0.6 − ratio·0.4` (deploymentReturn.js:193–219, 497–503).
6. **NPC layer**: signal `siege_survived` (= `siege_lifted`/`occupation_lifted` condition present) deposits trait `tenacious`, domain military, loud 0.55 (npcGrowthKernel.js:136, 305).
7. **peopleLedger cohort machinery**: `RESIDENCY_TAGS ['cohort','free_lance']`, selection `veterans_first` on muster/dispatch, `arrive_home` restores banked composition (peopleLedger.js:62–68, 118, 181–200) — **zero consumers in the extract** (grep: only itself) — the W-SEAT census substrate is authored but unwired.
8. Flavor/adjacent: `veteran_unrest` residual strings (stressorsCore.js:61, 197), `veteran_return` travelClass (spatial/migration.js:505), veterans' lodges in the merc-presence regex (mercenaryMarket.js:86).

PLAUSIBLE (design synthesis): the only honest veteran-share derivation available today is `f(experience01, warExhaustion, dispositionStats)` at settlement grain — per-head veteran counts require wiring peopleLedger or a new banked ledger.

## D. The identity law — one army per settlement, and what breaks with a second

CONFIRMED: `worldState.deployments` is `Record<attackerSaveId, DeploymentRecord>` (pulseShapes.js:30) — the KEY is the settlement id, so a second concurrent force is **structurally unrepresentable** without a shape change. The law is stated (warDeployment.js:4 "A settlement fields EXACTLY ONE army (`worldState.deployments[saveId]`)"), enforced forward (L937 `if (deployments[fromId]) continue; // one-army constraint`), enforced in reverse against interventions (L903–918 `activeIntervenerIds` — "DESIGN_CONVERGENCE states the one-army law as bidirectional physics"), and mirrored in convergence.js:1017–1019 (`busy = new Set(Object.keys(deployments))`) and reinforcement.js:55 ("its army is its only army"). Army identity IS settlement identity everywhere (`armyId: fromId` warHomeCosts.js:375; armyTransitKernel.js:298; navalKernel.js:334).

**Denominator: 42 non-test files read `worldState.deployments`** (grep -l). What breaks per reader class if a second force shared the key-space:

- **Single-key "THE army" reads (`deployments[id]`)** — warDeployment step 0/withdraw/effectiveStrengthFor (L411–441, 617), occupation.js:601–603 (`occupierStillPresent` — garrison presence = THE record targeting the occupied town), warTermination.js:520, 964–965 (peace pairs), warPeaceDecision.js:191–396, envoyDiplomacy.js:876, warIntent.js:314, momentum.js:337, settlementStrategy.js:1148, realmVerbExecution.js:352–611 (sue-for-peace/`intervention_busy`), navalKernel.js:312–473 (embark/shared-fate writeback keyed by armyId), armyTransitKernel.js:654, 885, display/armyStrength.js:136, warStatus.js:109–138, pulseKernel.js:1280 (residue strip reads `deployments[receipt.attackerId].sinceTick`). A second force either shadows the first (last-writer-wins clobber) or is invisible.
- **One-cost-per-settlement iterations (`Object.keys(deployments)`)** — warHomeCosts step 5 charges ONE war_drain/army_deployed/exhaustion ratchet/conscription drip per key per tick (L331); two forces under one key = one charge; two keys for one settlement = double conscription against one population floor.
- **Boolean "is at war" reads (`deployments[id] !== undefined`)** — mobilization.js:462 (posture pinned `deployed`), martialReadiness.js:486 (footing/engagement), mercenaryMarket.js:219 (engaged), generosityKernel.js:680, causeVocabulary.js:125, peaceReasons.js:465, warReasons.js:965, briefs/composers.js:122, realmManifest.js:132 — all collapse "how many forces" to one bit; mostly safe, but peace/casus machinery derives the WAR PAIR from `(key, record.targetId)` — one settlement warring two targets is unrepresentable.
- **The flat homeDefense penalty** — warCapacityReads.js:190: 14 points iff a record exists; a second force away should deepen it and cannot.
- **Return machinery** — deploymentReturn keys outcomes/rng forks by homeId (`world_outcome.army_homecoming.<home>.<tick>`, L333); two same-tick returns for one home would collide outcome ids.

**The existing precedent D10/D11 must follow (CONFIRMED):** interventions are the estate's second commitment channel and they solved this exactly by staying OUT of `deployments` — a separate spatial ledger keyed `${patronId}:${targetId}` (convergence.js:1017–1019, 1071–1073) with bidirectional busy-gates. The D10 addendum's own words match ("a typed contest-window ledger row (the armyTransit drop-when-none-afield idiom), never a standing army; dropped at resolution" — DESIGN_W_SEAT.md:89). An irregular force therefore must: (1) live in its own contest-window ledger keyed by contest, never by settlement id alone; (2) decide explicitly whether its bearer joins the `busy`/`activeIntervenerIds` mutual-exclusion sets (a popular rising's home probably should NOT be blocked from... rather, the LOYAL side is the garrison, not a deployment — so neither side should enter `deployments` at all); (3) never trip the boolean at-war reads (mobilization pin, martialReadiness footing, merc engagement) unless that coupling is deliberately chosen; (4) charge home costs through its own arm, since step 5 will never see it.

## Open/not-found

- The "NAME-4 composition clerk" the addendum cites is **not in this extract** (grep over src+docs: zero hits) — it lives in another sealed volume; the composition law could not be grounded here.
- `docs/DESIGN_W_SEAT_VERIFICATION.md` is referenced by the volume but is not among the files touched by e14b9a0db; the seven prior census reports in `.../scratchpad/wseat-census/` (V-belief, V-collisions, V-decisionSurface, V-factions, V-legitimacy, V-occupation, V-vassalage) do not cover the force substrate — no overlap with this lane.
- PLAUSIBLE flag inventory feeding the recipe: `warLayerEnabled` (master), `warSupplyQualityEnabled` (qMult), `warEconomyDrainEnabled` (conscription heads), `warLevyEnabled` (levy heads), `defenderAttritionEnabled`, `allyDefenseEnabled`, `warForageEnabled` — all confirmed read at warDeployment.js:243, 469–479; their defaults were not verified in this lane (rules objects are threaded in).

---

# D10-C2 — SPLINTER AND RETURN (census wf_7192952f-226, verbatim)

LANE C2 REPORT — THE SPLINTER AND RETURN PATH

SURFACE: worldPulse sim layer of the build-tip extract (mirror of 73f5dfc02) at `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours`. Read in full: `src/domain/worldPulse/deploymentReturn.js` (507/507 lines) and `src/domain/worldPulse/warArmyRecord.js` (183/183). Targeted scopes: warDeployment.js, warHomeCosts.js, attrition.js, rulingPowerCoup.js, pulseKernel.js. All claims CONFIRMED by quoted source unless labeled PLAUSIBLE. Consistent with the prior V-legitimacy census (its §60/§82 rows on this file check out against my read).

## 1. The splinter path — trigger and constants (CONFIRMED)

`deploymentReturn.js:67-70`: `STRONG_RETURN_RATIO = 0.62`, `SPLINTER_RATIO = 0.3`, `RETURN_SUCCESS_FLOOR = 0.06`, `RETURN_SUCCESS_CEIL = 0.96`. The spine is `strengthRatioOf(deployment, outcome)` (:82-93) = `clamp01(currentEffectiveStrength / maxStartStrength)`; a light record with no strength fields reads **1.0** ("legacy binary limit", :88), and `outcome === 'withdrawal'` caps the ratio at **0.85** (:91). Splinter triggers, by home context (the branch ladder :351-503, first match wins — occupied → besieged → vassal → generic):

- **Generic home** (:497-503): `ratio < SPLINTER_RATIO` → `failedReturnOutcome({kind:'splinter'...})` — *"Deserters scatter and the survivors are restive."*
- **Vassal home** (:433-443): `ratio < 0.3` → no coup roll; if `ratio < SPLINTER_RATIO * 0.6` (**< 0.18**) → `kind:'disband'` outcome; **the 0.18 ≤ ratio < 0.3 band emits NOTHING** (bare `continue`, :442) — a silent disband.
- Occupied/besieged homes never reach the splinter kinds — their failure kinds are `'rebellion'`/`'relief'`.

What the splinter IS today: a **condition, not a force**. `failedReturnOutcome` (:193-219) mints `candidateType: 'faction_challenge'` (splinter/disband) or `'war_exhaustion'` (rebellion/relief), severity `clamp(0.25, 0.7, 0.6 − ratio×0.4)` — inverse to strength — with `triggeredAt.sourceEventType: 'DEPLOYMENT_RETURN_FAILED'`, `sourceEventTargetId: deployment.<home>.<target>`, and a reasons row carrying `ratio/pSuccess/roll`. **Zero men are partitioned off; no splinter fraction exists anywhere.**

## 2. What the returning record carries at that moment (CONFIRMED)

The record's full field set is seeded at `warArmyRecord.js:104-137` (`seedDeploymentState`): `targetId, sinceTick, role, maxStartStrength, currentEffectiveStrength, accumulatedAttrition`, conditional stamps `readiness` (W-F8), `sizingBias` (W-C1 rust), `deployedQuality` (supply-gap mult), then `reinforcementFlow, deploymentAge`, seven 0..1 facets — `manpower, supplyIntegrity, morale, equipmentCondition, magicSupport, commandQuality, foodReserve` — plus `logisticsBurden, objective, returnCondition:'pending'`. Attrition erodes strength AND facets in step (attrition.js:231-233: *"supply/morale/equipment erode in step"*). Flag-gated banks: `deployedPopulation` (warHomeCosts.js:362, conscription) and `leviedPopulationBySource` (warHomeCosts.js:451-453, per-vassal headcounts).

So: **manpower YES (headcounts), quality/morale YES (on the record) — but the return branch reads NONE of the facets.** `deploymentReturn.js` consumes exactly: `maxStartStrength`, `currentEffectiveStrength` (:84-85), `deployedPopulation` (:266), `leviedPopulationBySource` (:283), and the row's `outcome` string. Morale, equipmentCondition, deployedQuality, readiness, accumulatedAttrition, deploymentAge all arrive home unread. `returnCondition` is a **dead field** — written once (warArmyRecord.js:136), never read or updated (grep over src: 1/1 hit is the writer). **No composition** (no unit types/cohorts) and **no veteran field** exist (grep `veteran` over src: 0 army-record hits; only flavor text and martialReadiness rust constants).

## 3. The liberation roll and the other resolutions (CONFIRMED)

`returnSuccessProbability` (:104-110): `ramp = clamp01(0.5·(ratio−0.3)/(0.62−0.3) + 0.25)` — 0.25 at ratio 0.3, 0.75 at 0.62 — clamped to [0.06, 0.96], plus an optional `contextTilt` (**no caller passes one today**; 0/3 call sites). Determinism scaffold: `rng.fork('deployment-return')` → per-record `baseRng.fork(homeId)` over a codepoint-sorted list (:235-237, :250), then one labeled sub-fork per resolution: `fork('liberation')` (:355), `fork('relief')` (:391), `fork('coup')` (:449). Occupied + roll ≥ pSuccess ⇒ `kind:'rebellion'` failure ("liberation falters"). Vassal + `ratio ≥ 0.3` ⇒ `resolveCoupVerdict({settlement, rng, severity: clamp(0.45,0.85, 0.4+ratio×0.5), rulingAuthorityScore})` (:447-452) — the same politics resolver as coup.js, whose signature already carries the additive-adj slots (`warSentimentAdj, interventionAdj, economicAdj`, rulingPowerCoup.js:147); order-held ⇒ silent `continue` (:453).

## 4. Do numbers conserve? YES — and the splinter withholds no one (CONFIRMED)

The homecoming credit (:266-349) runs **BEFORE every branch**: `survivors = min(deployedPopulation, round(deployedPopulation × ratio))`, `fell = deployedPopulation − survivors` is *"the ONLY population sink"* (:255-257), and survivors are apportioned back per contributor by largest-remainder rounding (overlord conscripts + each vassal's levy, :283-330) so per-settlement AND world conservation hold. **Gated on the bank, not a live flag** (:259-265) — a non-zero bank is always credited. Therefore in EVERY outcome — splinter, disband, failed rebellion, coup, liberation — the survivors are already back in the population when the branch fires; the splinter's "deserters" are narrative + a faction_challenge seed, not a headcount movement. Nothing vanishes; equally, **nothing is set aside to fight with**.

## 5. Grievance / war-outcome material available for cause receipts

The `resolvedDeployments` row is `{attackerId, deployment, targetId, outcome}` with outcome vocabulary of exactly **three values across 5 push sites** (warDeployment.js:263 war-off wind-down, :372 recall order, :399 canon prune, :623 stalled-siege collapse — all `'withdrawal'`; :874 `razed ? 'razing' : 'conquest'`). A **beaten field army also lands as 'withdrawal'** (armyTransitKernel.js:878-884 flags the deployment for recall; next tick warDeployment.js:372 executes it), and warDeployment.js:363-364 states a recall is *"a CHOICE, not a defeat: NO disposition delta"* — so **the outcome string cannot distinguish a lost battle from a negotiated peace**. Grievance raw material that DOES exist per record: `targetId` (which war), `deploymentAge`, `accumulatedAttrition`, `sizingBias` (the crown mis-sized us), `deployedQuality` (the crown under-supplied us), `morale`, `fell`/`survivors` (homecoming `metadata` :346), and the `deployment.<home>.<target>` sourceId chain already threaded into `triggeredAt` (:215).

## 6. D10 birth (a) — reusable verbatim vs missing

**Reusable verbatim (CONFIRMED present):**
1. Trigger predicate: `ratio < SPLINTER_RATIO` with `strengthRatioOf` as-is (light-record→1.0 and withdrawal→0.85 semantics included).
2. Size input: `survivors` — the returning army's recorded manpower, already computed in the same loop iteration, already per-source apportioned (a vassal-levy splinter can know whose men splinter).
3. Determinism scaffold: the `'deployment-return'`/per-home/labeled-sub-fork idiom — a `fork('splinter-force')` slot costs nothing and is order-independent by construction.
4. The receipt style: `failedReturnOutcome`'s ratio/pSuccess/roll reasons + `DEPLOYMENT_RETURN_FAILED` provenance chain — D10's cause receipts can extend this envelope, not replace it.
5. The political resolver: `resolveCoupVerdict` with its additive-adj clamp — the force-ratio term lands as the next caller-computed adj (matches V-legitimacy's SEAT-2 row; both call sites already pass severity `0.4+ratio×0.5`).
6. Conservation law: fell-as-sole-sink + largest-remainder apportionment — the pattern the splinter force's ledger must respect.

**Missing (D10 must build):**
1. **The splinter fraction** — no constant, no partition; today 100% of survivors return to civilian population and 0 men become a force. "Size = manpower × splinter fraction" needs a new tunable.
2. **Force composition** — the record carries aggregate facets only, never composition; the NAME-4 clerk must derive composition fresh from the settlement's own wartime law. Quality proxies exist unread (`deployedQuality`, `readiness`, `morale`, `equipmentCondition`, `accumulatedAttrition`, `deploymentAge`) — "veterans by construction" is derivable from `deploymentAge`+`accumulatedAttrition`, but **no typed veteran-share field exists**.
3. **A persistence surface** — the deployment record is deleted at resolution (warDeployment.js:876) and `resolvedDeployments` lives one tick; the typed contest-window ledger row is new machinery (the armyTransit drop-when-none-afield idiom is the stated template).
4. **Ordering with the homecoming credit** — since survivors are credited BEFORE the branch, birthing a force from them must either debit the splinter headcount out of `populationDeltas` or accept a double-count; today's code forces this decision.
5. **The vassal silent band** (0.18 ≤ ratio < 0.3): emits nothing today — D10 must rule whether it births a force or stays silent.
6. **Grievance typing** — outcome ∈ {withdrawal, conquest, razing} conflates defeat with deliberate recall; cause receipts need either derivation from `accumulatedAttrition`/disposition context (no schema change) or a richer outcome enum (PLAUSIBLE owner-gated: the row shape feeds persisted summaries).
7. **No contextTilt is ever passed** to `returnSuccessProbability` — the documented hook for "the home's own fragility" (:98) is an unused, ready-made slot for `participation01`-side tilts.

Key absolute paths: `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours/src/domain/worldPulse/deploymentReturn.js`, `.../warArmyRecord.js`, `.../warHomeCosts.js` (bank writers :362, :451), `.../warDeployment.js` (push sites :263/:372/:399/:623/:874), `.../src/domain/rulingPowerCoup.js` (:147).

---

# D10-C3 — PARTICIPATION (census wf_7192952f-226, verbatim)

All census arms are complete. Deliverable follows.

# LANE C3 — THE PARTICIPATION SUBSTRATE (extract mirrors 73f5dfc02; paths relative to `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours/src/`; sealed volume read from `git show e14b9a0db:docs/DESIGN_W_SEAT.md` lines 87-93. All claims CONFIRMED by quoted code at the cited lines unless labeled PLAUSIBLE.)

D10's charter (sealed volume :89): `participation01` is "ONE derivation consumed everywhere the rising's mass matters: f(legitimacy deficit band, the commons-voice rung, aligned-faction share, occupation `resistance` where occupied) — it reads legitimacy, never writes it". Below: each input's exact home, the fraction-of-population machinery that exists, and the gaps.

## 1. commonsVoiceKernel.js — read IN FULL (340 lines)

**The grievance formula** (domain/worldPulse/commonsVoiceKernel.js:253-259, full enclosing scope `advanceLitCommonsVoice`):
```
grievance = clamp01( W_LEGIT·legitDeficit + W_CORRUPT·(corrSignal?1:0) + W_UNREST·unrestSeverity )
legitDeficit = clamp01((LEGIT_FLOOR − legit) / LEGIT_FLOOR)   // NaN-legitimacy ⇒ 0
```
Tuning (:54-65): `LEGIT_FLOOR: 55`, `W_LEGIT: 0.55`, `W_CORRUPT: 0.35`, `W_UNREST: 0.55`; rung thresholds `RUNG_PETITION 0.35 / RUNG_GATHERING 0.55 / RUNG_RIOT 0.75`; `DWELL_TICKS 3` (escalate one step after 3 sustained ticks — de-escalation is immediate, :266-274 "relief quiets faster than it inflames"); `LEGIT_DIP [0,1,2,3]` one-time legitimacy cost on entering each rung; `RIOT_UNREST_STEP 0.10`.

- **Inputs it reads**: `powerStructure.publicLegitimacy.score` (integer 0-100, NaN when absent/legacy-bare, :74-80); highest-severity live unrest stressor over `UNREST_TYPES = {rebellion, political_fracture, coup_detat, slave_revolt}` (severity clamp01, :67-96); seated-corruption signal (exposed un-ousted corrupt NPC on the roster, codepoint-stable accused pick, OR an `exposedCorruption` spatial-ledger entry prefixed `${sid}>`, :105-121).
- **The persisted state**: spatial ledger `worldState…commonsVoice[sid] = { rung: 1-3, since: tick, grievance: round4, kind: 'corruption'|'misrule', accusedNid? }` (:280-282), drop-when-rung-0 (:273 comment "A rung that reaches 0 is dropped (drop-when-empty ⇒ dormant)"), byte-stable no-op guard via serialize-compare (:322-337).
- **Population-share semantics: NONE.** The kernel never reads `population`; rung and grievance are unitless 0-1/ordinal indices. The crowd is explicitly unaligned: summary prose "No faction owns it; the crowd speaks for itself" (:194), and stressorDynamics' popular_revolt variant echoes it ("The streets rose on their own. No faction owns this yet.", stressorDynamics.js:849-851).
- **Dormancy**: behind `simulationRules.commonsVoiceEnabled === true`, ABSENT from DEFAULT_SIMULATION_RULES (:39-51). **Consumers of the rung today (denominator: 3 files outside the kernel reference `commonsVoice`)**: assizeKernel.js:262 (petition→judgment coupling), lib/spatialUsage.js:356 (registry row), certification/tradeConvergenceContract.js. participation01 would be the **second engine consumer** of the rung.
- ⚠ **Consequence for participation01**: the rung input is flag-gated state. In a commonsVoice-dark world the ledger key never exists, so `f(…rung…)` must be total on absence (rung 0). Note the raw *grievance* is statelessly re-derivable from always-on reads (legitimacy/corruption/unrest all live outside the flag) — only the rung's dwell/escalation **history** needs the flag. PLAUSIBLE design note: reading the rung-where-lit and the stateless grievance otherwise would make participation01 nearly flag-independent; reading the rung only makes popular risings materially rarer in default worlds. The charter says "rung", so the choice is a design decision to surface to the D10/D11 mini-panel.

## 2. Population: home, unit, and every mover (heads, not shares)

**Home**: `settlement.population` at the TOP level of the sim shape, `population?: number` (domain/settlement.schema.js:357) — **integer HEADS**. ⚠ A second, legacy generation-document shape exists: `@property {Object} [population]` (settlement.schema.js:286), and `occupation.resistanceTarget` defensively reads both: `const pop = typeof popRaw === 'number' ? popRaw : num(popRaw?.total, 0)` (occupation.js:344-346) — while populationDynamics' `finite(settlement.population, 0)` (populationDynamics.js:545) would read the object shape as **0**. A §711.6-class shape fork already latent; participation01's `population ×` read must pick one idiom (the occupation tolerance is the safe one).

**The single delta applier**: `applyPopulationOutcomeToSettlement` (populationDynamics.js:539-583, read in full) — sums the outcome's `populationDeltas` rows for this saveId, clamps ≥0, appends a `populationHistory` ring entry `{tick, delta, population, reason, outcomeId}` capped `slice(-11)` (:571-581), with source-debit/destination-credit conservation for emigration/flow_migration via a per-apply `migrationRealizedFraction` WeakMap (:520-567). History ring feeds the belief layer's ground-truth trend (`trendBandFromHistory`, beliefAxes.js:100-110) → believed `populationTrendBand` −2..+2.

**What moves it** (all in heads):
- **Demographics**: `demographicsRates.js` — `demographicRates → {birth01, death01}` per-tick RATES with logistic crowding ("Nothing anywhere clamps a population", :34-35, :246); legacy `populationPressureRate` (food −0.013, disease −0.020… :268).
- **Calamity deaths**: direct write `population: afterDeaths` + own history entry `{delta: -loss.deaths, reason: 'Killed in the calamity.'}` (calamityKernel.js:410-437); **calamity exodus** rides the emigration shape as a one-shot severity-scaled stock fraction with conserved `distributeMigrants` credits (:731-762) — **the existing precedent for a one-shot bounded fraction-of-population draw**, which is the arithmetic shape of D10's `population × participation01`.
- **War (flag-gated, THE key find for D10)**: a deployed army **banks a real headcount** — conscription debits `min(round(homePop × 0.006), homePop − 250)` per tick (`WAR_CONSCRIPT_RATE_PER_TICK 0.006`, `WAR_CONSCRIPT_POP_FLOOR 250`, warHomeCosts.js:281-282, :352-380) and vassal levy `min(round(srcPop × 0.004), srcPop − 300)` (:82-83, :408), both as conserved `populationDeltas`, banked on `deployment.deployedPopulation` + per-contributor `leviedPopulationBySource` (:437-451). Homecoming credits back `survivors = deployedPopulation × strengthRatio` with largest-remainder apportionment; "The war dead … are the ONLY population sink" (deploymentReturn.js:251-257). Conquest sack transfers via `computeSackTransfer(…population)` (warDeployment.js:781-793).
- So the **wartime-levy profile D10 references is a FLOW (0.6%/0.4% of heads per tick, floored), not a stock capacity** — see gaps.

**Heads-vs-index seam in the force law**: `militaryStrength.js` facets are 0-100 indices — manpower = `tierFrac·70 + popScore·30` with **log10-scaled** population ("so a metropolis doesn't dwarf everything linearly", :171-181) — and deployments carry a 0..1 `strengthRatio` vs muster (deploymentReturn.js:73-90). **Nothing in the force law is a headcount**; heads and strength meet only through the `deployedPopulation` bank riding alongside the strength record. A rising sized `population × participation01` (heads) therefore needs the same two-track shape: heads on the contest-window row for population accounting, facet-law strength for the verdict.

## 3. Aligned-faction share — what exists, and the negative census

**No popular-backing quantity exists anywhere.** Greps over `src/domain/worldPulse` for `popularSupport|alignedShare|alignmentShare|factionShare` return exactly one derivation family (below); `popular` appears only in prose/variant names (popular_revolt) and religion comments. "What share of the town backs faction X" is **underivable today as a population quantity**. What IS derivable:

1. **`governingPowerShare`** (warSeatBooks.js:108-124, full scope quoted in my read): faction power / Σ readable roster powers, tolerant of 0..1 or 0..100 domains (`normalizedPower`, :101-105), used for the seat's security book and `rivalTriumph01` (:126-160). **This is the estate's only true share-of-power derivation — but its unit is share of ELITE faction weight, not share of populace.** And per the V5 census (wseat-census/V-factions.md, cross-lane receipt): roster `power` sums ≈100 only at generation; sim-time writers never renormalize, so the denominator drifts — the per-read share is the right idiom, an absolute read is not.
2. **Per-faction qualitative `publicTrust`** (`FactionResources`, settlement.schema.js:917-927): `'low'|'medium'|'high'` — "Avoiding numbers here keeps the profile legible … without false precision" — generation-authored; tick deltas are only mirrored into a `_timePressure` scratch ("tracked in tick output … but not yet stored on the faction", timeProgression.js:307-325). The closest existing per-faction popular-standing signal; 3-band, never a share.
3. **`factionViews`** (settlementPolitics.js:225-257): the joined roster+state view carries `{key, name, archetype, power, isGoverning, leader…, rivalKeys, captureState}` — **no popular dimension**.
4. **The one true population-share ledger in the estate is RELIGION**: per-deity adherent `share` — "an adherent SHARE (0..100, summing to 100 like faction power)" (religionState.js:5), integer largest-remainder renorm every tick (:124-141), an unaffiliated 'none' sink capped at 45 (:68-77), standings with hysteresis (:144-150). **This is the proven shape/idiom if D10 ever needs a persisted popular-alignment ledger — but it is a whole subsystem, not a read.**
5. NPCs carry `factionId` (settlement.schema.js:929) — a named-roster headcount per faction is derivable but the roster is dozens of souls, not population-representative (PLAUSIBLE as a flavor input only).

**⇒ The gap is real: "aligned-faction share" in the D10 formula can only mean the challenging faction's `governingPowerShare`-style share of roster power (elite weight), unless SEAT-7 defines a new derivation.** Honest options visible from the estate: (a) elite share as proxy, stated as such; (b) elite share modulated by the qualitative `publicTrust` band of the aligned faction (both reads exist today); (c) a religion-style persisted ledger (owner-gated new schema — cuts against derived-not-persisted). The formula's other three inputs are all crowd-side; using raw elite power-share here quietly re-imports the "faction power = popular backing" conflation the commons-voice slice was explicitly built to avoid (kernel header :9-13: the engine had "NOTHING that fires the UNNAMED COMMONS as a discrete … actor").

## 4. Occupation resistance — the occupied-side input (verified first-hand)

`worldState.occupations[occupiedId].resistance`, 0..1, seeded 0.35 (occupation.js:228-238 via cross-lane receipt; ratchet verified first-hand): `resistanceTarget = clamp01(0.5·usefulness + 0.3·legit01 + 0.2·popMass)`, `popMass = clamp01(log10(max(10,pop))/5)` — "a populous town can field a resistance; a hamlet cannot sustain one" — compliant puppet regime ×0.45 (occupation.js:337-355, `hasCompliantRegime` :365-371: `'occupier'`-modifier faction present AND locals `'occupied'`/`'disarmed'`). Emitted as the `occupation_resistance` condition with severity = resistance, floor 0.20 (V3 census receipt, occupation.js:1011-1026). ⚠ Flag-coupling: the ledger exists only under `warLayerEnabled` — the "where occupied" arm of participation01 is total by construction (absent key ⇒ not occupied). ⚠ Note resistance already **contains** a legitimacy term (pre-occupation loyalty, 0.3 weight) and a popMass term — participation01 summing legitimacy-deficit AND resistance double-counts legitimacy with **opposite signs** (high prior legitimacy ⇒ high resistance ⇒ high anti-occupier participation, but low legitimacy-deficit ⇒ low participation). For the occupied case the two inputs pull against each other by construction; the derivation must decide which legitimacy it means (the occupier-seat's vs the deposed regime's) — the K3-adjacent subtlety the mini-panel should settle.

## 5. participation01 — the exact available-input table

| Charter input | Home (file:line) | Unit / range | Persisted? | Dark-world reading |
|---|---|---|---|---|
| legitimacy deficit band | `powerStructure.publicLegitimacy.score` via `governanceLedger.js:52-67` (the ONE read-point, V2 receipt); bands `rebandLegitimacy` 75/60/45/30 (rulingPower.js:311-333) | integer 0-100; 5 labeled bands with govMultiplier 1.30→0.60 | yes | always-on; neutral-50 absent |
| commons-voice rung | spatial ledger `commonsVoice[sid].rung` (commonsVoiceKernel.js:280-282) | ordinal 0-3 (+`grievance` 0..1 round4) | yes (drop-when-empty) | flag `commonsVoiceEnabled` dark ⇒ key absent ⇒ rung 0; raw grievance re-derivable statelessly (:253-259) |
| aligned-faction share | **gap** — nearest: `governingPowerShare` idiom (warSeatBooks.js:108-124) over the challenger row | share of roster power 0..1 (ELITE weight, not populace) | derived per-read | always-on |
| occupation resistance | `worldState.occupations[id].resistance` (occupation.js:228-238, :337-355) | 0..1 | yes | `warLayerEnabled` dark / not occupied ⇒ absent ⇒ 0 |
| (the mass it multiplies) | `settlement.population` (settlement.schema.js:357) | integer heads; history ring slice(-11) | yes | always-on; legacy `{total}` object shape must be tolerated (occupation.js:344-346 idiom) |

**Unit-fork hazards to charter explicitly (the §711.6 law — 3 live forks touch these inputs):** (i) "legitimacy band" has TWO vocabularies — score bands (rebandLegitimacy, 0-100) vs pressure bands (`factionCompetition.legitimacyBand`: crisis ≥0.66 / contested ≥0.44 over a 0..1 **inverted-deficit pressure** = `clamp01((70−causal)/70)+0.16`, factionCompetition.js:622-626 + V2 receipt pressureModel.js:18-21) — D10's "legitimacy deficit band" must name which (commonsVoice's own deficit is a THIRD spelling: `(55−score)/55`); (ii) faction `power` is 0..1-or-0..100 with a drifting sum — share-at-read only, never absolute; (iii) population number-vs-`{total}` object shape.

**Fraction-of-population machinery summary**: stock fractions exist only as calamity deaths/exodus one-shots (severity-scaled, conserved); flow fractions as war conscription/levy (0.6%/0.4% heads/tick, floors 250/300, banked and survivor-returned). **`(1−participation)·levy capacity` for the loyal side has NO existing stock quantity** — "levy capacity" would be new; nearest existing loyal-side reads are the banked garrison/readiness family (`readinessOf/experienceOf/rustOf`, martialReadiness.js:251-264; posture ladder mobilization.js:1-60) and the manpower facet (militaryStrength.js:171-181). The natural conserved shape, given the estate: a rising's heads ride a contest-window ledger row exactly like `deployedPopulation` (banked at birth, survivors/dead reconciled at resolution — the books-balance-by-construction idiom of deploymentReturn.js:251-257), which also gives THE PROMISE its population conservation for free.

**Instigation note (D11)**: the instigator-side believed picture was lane V-belief's arm; from this lane's reads, belief carries `populationTrendBand` (believed −2..+2, beliefAxes.js:7) and per-faction dissent slots with `confidence01` (beliefMap.js:1425-1430) — a foreign court can already believe a trend and a dissent, but there is no believed legitimacy-score surface visible from these files (PLAUSIBLE; defer to V-belief.md).

---

# D10-C4 — ENGAGEMENT REUSE (census wf_7192952f-226, verbatim)

All reads complete. Composing the reuse map from executed evidence.

---

# LANE C4 — ENGAGEMENT REUSE FOR CIVIL BATTLES (D10/D11)

Sources: build-tip extract mirroring `73f5dfc02` (all `src/` paths below are that tree); sealed volume `e14b9a0db:docs/DESIGN_W_SEAT.md` (D10 addendum :89, D11 :91); NAME-3 charter at `e14b9a0db:docs/DESIGN_FMG_WEAVE.md:133`. Read-only; no gates run. Files read whole: `warSiegeVerdict.js` (340), `attrition.js` (302), `armyTransitKernel.js` (1116), `spatial/armyTransit.js` (710), `warArmyRecord.js` (183), `warCapacityReads.js` (303); targeted: `warDeployment.js` (:1-220, :440-880 of 1294), `convergence.js` (:1-200, :480-810, :1160-1300 of 1425), `deploymentReturn.js` (:55-230, :400-507), `feasibilityGate.js` (:1-80 + grep), `militaryStrength.js` (:1-90 + grep).

## 0 · THE HEADLINE FINDING — the adapter already half-exists, and it is convergence.js, not armyTransit

**CONFIRMED.** The estate already resolves an internal contest through the existing engagement machinery with an isolated ledger: `convergence.js` (W-CONVERGENCE, coup-scoped Wave 1). Its `resolveSideBattle` (:654-668) calls `resolveFieldBattle` **verbatim, aspatially, with bare `{armyId, size}` inputs and no transit row, no deployment row, no save-id requirement on the combatant ids** — "Counterforce resolves as battle — the existing collision semantics verbatim, then the survivor tilts the contest" (:9-10). Its records live in a DEDICATED `interventions` spatial ledger, deliberately NOT `deployments`: "This guarantees byte-identity-when-dark by construction and sidesteps warDeployment's one-army law + siege-verdict machinery entirely" (:42-47). Its below-threshold twin is also live: `interventionAdjFor` (:542-551) folds strength-SHARES into a signed bounded tilt consumed as a typed adj in `coup.js:117/134`. D10's contest routing ("below a force threshold, the existing verdict math stands with ONE new typed term (the force ratio); above it … the EXISTING engagement machinery") is structurally the same two-lane pattern convergence already ships. SEAT-7's civil-engagement adapter should be chartered as the **sibling of convergence's Stage 2, not as a new consumer of armyTransit**.

## 1 · WHAT CARRIES OVER VERBATIM (no edits, closures/typed ids only)

| Piece | Receipt | Why it carries |
|---|---|---|
| **`resolveFieldBattle` + `fieldBattleWinProbability`** (`spatial/armyTransit.js:475-504, :440-450`) | Pure; fork key `battle:<a>:<b>:<tick>`; CLAMP_RATIO 3 "no hand of miracle"; bounded margin-scaled losses (LOSER 0.15–0.45, WINNER 0.03–0.18); "no rng ⇒ deterministic favourite" | Proven aspatial-callable by `resolveSideBattle` (convergence.js:648 "Aspatial-callable (resolveFieldBattle is pure)"). Inputs need NO save id, NO deployment row — `{armyId: string, size, readiness?, supplyQuality?, funding?, groundAdvantage01?, fatigue01?}`. |
| **`resolveSiegeVerdict`** (`warSiegeVerdict.js:124-318`) | Fully parameterized: `capacityFor`/`effectiveStrengthFor` are **caller-supplied closures**, `targetId` is an opaque key used only for `capacityFor(targetId)`, the fork key `siege:<stablePart(targetId)>:<tick>` (:291), decisionCid fallback (:168), and reasons text (:257 falls back `defenderItem?.name \|\| targetId`) | An adapter passing a **typed contest id** as `targetId` (e.g. `civil:<saveId>:<sinceTick>`) plus closures resolving typed party ids gets: a distinct PRNG stream (no collision with a same-tick external `siege:<T>` roll), the feasibility gate, the will track (`composeDefenderWillScore` reads the real `defenderItem`'s legitimacy — correct for the loyal side, :243-249), outcome bands, and capitulation — all unmodified. |
| **`computeEngagementAttrition` / `applyAttritionToRecord`** (`attrition.js:175-282`) | "PURE function of the deployment record + the two capacity envelopes + the outcome band … byte-stable" (:18-22); bounded MAX_LOSS_FRACTION 0.42 | Operates on any record carrying the stateful fields. The irregular contest-window row simply carries the `seedDeploymentState` shape (below). |
| **`seedDeploymentState` field shape** (`warArmyRecord.js:85-138`) | `maxStartStrength`/`currentEffectiveStrength` seeded from `cap.offensive`; facets normalized 0..1 from the model facets (morale = (will+manpower)/2 etc.) | The NAME-4 clerk derives the irregular row's envelope by the SAME law with D10's degradations (materiel/institutions scaled to what rebels seize; veteran share lifts readiness); the row then feeds attrition unmodified. Reuse the SHAPE; do not write the `deployments` ledger (see §3). |
| **The below-threshold adj idiom** | `interventionAdjFor` → strength-share tilt → `resolveCoupVerdict({... warSentimentAdj, interventionAdj, economicAdj})` (`convergence.js:542-551`, `coup.js:113-136`) | D10's "ONE new typed term (the force ratio)" is this exact pattern: signed, bounded, 0-when-dark, inside the existing pHold clamp. |
| **The INTERCEPT seam for D9/D11 columns** | `hostilePairFor(records, contestSideOf)` exported for "the convergence INTERCEPT pin" (`armyTransitKernel.js:537-559`); `contestSideLookup` feeder already built (`convergence.js:796-807`) | External repression/support columns marching to a rising and colliding en route is ALREADY a designed, byte-identical-until-threaded seam — "opposing sides of the SAME internal contest" (:548-551). SEAT-7/8 thread it; nothing new is invented. |
| **The drop-when-none-afield ledger idiom** | `setSpatialLedger`/`dropSpatialLedger`, "Drop the whole ledger when no army is afield (sparse → byte-safe)" (`armyTransitKernel.js:916-924`); interventions ledger does the same (`convergence.js:1233-1236`) | D10's persistence spec names this idiom explicitly; the interventions ledger is the closer precedent (keyed `<interId>:<target>`, typed record, dropped at resolution). |
| **The receipt fields NAME-3 narrates** | Siege: `{verdict, ratio, pFall, roll, coalitionCurrent, defenderCurrent, band, reasons, capitulation?}`; the narratable roll rows at `warDeployment.js:813-816` ("Coalition current capacity X vs defender Y … Fall chance P, roll R" — the exact lines NAME-3's charter cites); field: `{winnerId, loserId, pWin, roll, effA, effB, strengthDelta, clamped}` + `battle.region` (`fieldBattleNews`, kernel :591-620) | A civil engagement that emits the same verdict/result objects into its receipt is NAME-3-narratable for free; battle-KIND vocabulary ("street fighting", "storming the keep") is NAME-3's closed display-side taxonomy, zero engine cost. |

## 2 · EVERY ASSUMPTION THAT BREAKS (the adapter's whole job)

1. **Two distinct combatant ids — the same save id cannot fight itself.** `hostilePairFor` returns false for `aId === bId` (:539); `detectCollisions` iterates `i<j` over distinct keys (:538-540); the field fork key `battle:<a>:<b>:<tick>` and the news id `wizard_news.<tick>.field_battle.<a>.<b>` (:601) degenerate on equal ids. **Fix: mint typed party ids** (`rising:<T>` / `loyal:<T>`), which also gives a distinct rng stream. Minor flag: the fork key concatenates raw ids with `:` un-length-prefixed (`armyTransit.js:482`) — use colon-free suffixes or the `stableParts` idiom (`armyTransitKernel.js:104-106`) in any NEW key.
2. **The one-army law: `worldState.deployments[saveId]` is one record per settlement** ("A settlement fields EXACTLY ONE army", `warDeployment.js:4`). An irregular force written there would collide with the settlement's own army and be swept into step-3 siege collection (:453-456, keyed by `dep.targetId`). **Never write deployments** — the convergence isolated-ledger ruling (:42-50) is the constitutional precedent, and it protected "the 6 role:'siege' readers" by construction.
3. **No transit row, ever.** The transit ledger is keyed by settlement id, derived FROM deployments (`kernel :653`), `planMarch` needs a route (origin→dest hops; same-node is meaningless), and a RETREAT's `destId === originId` already has special-cased semantics (CS-A4, :684-699). Intra-settlement contests have no march. D10 borrows the ledger IDIOM only.
4. **`capacityFor(id)` resolves snapshot items** — `buildCapacityLookup` returns the zero envelope for an unknown id (`warCapacityReads.js:178-182`). The adapter supplies its own closures: typed rising id → the NAME-4-derived envelope; typed loyal id / contest id → garrison + (1−participation)·levy per D10. Same for `effectiveStrengthFor` (the contest-window row's `currentEffectiveStrength`).
5. **Siege-of-self semantics — three specific mis-applications:**
   - `fortificationStrength` grants terrain+walls to "the defender settlement" assuming the attacker is outside (`attrition.js:127-136`); a rising is inside. The walls belong to whoever holds the citadel — an adapter DECISION (pass the loyal side's institutions-scaled fortification; the rising fights at ground 0), not a code fork.
   - `defenderHasCollapseSignal` → `require_betrayal` override reads coup/rebellion conditions on the defender (`feasibilityGate.js:128-135, :41-44`). In the civil contest the rising IS the collapse signal — feeding its own stressor back as its own feasibility override is circular. The adapter must exclude the triggering stressor from the defenderItem's collapse read (or deliberately charter the circularity away).
   - The M5 spatial core is starvation-of-the-town by road interdiction (`warSiegeVerdict.js:281-285`) — self-interdiction is nonsense. **Pass `supplyInterdiction: 0, spatialSiege: false` always**; the aspatial capacity-roll core runs verbatim (:286-288).
6. **`SIEGE_MAX_AGE` (60) is internal to the verdict** (:101, :215-233) and cannot be shortened without a fork. It never needs to be: the contest-window ledger resolves on the stressor's own window; the verdict is only consulted while the window is open, so the ceiling simply never binds. Do NOT fork the constant.
7. **Defender-side erosion is a documented deferral, and the civil adapter must not casually become its first consumer.** `BAND_DEFENDER` is "RESERVED — deliberately not yet wired … it would change every siege's outcome (and the golden master) … needs a deliberate balance pass" (`attrition.js:75-87`). Mirror the siege loop instead: the irregular row erodes as attacker (`applyAttritionToRecord(isAttacker:true)`, `warDeployment.js:564-575`), the loyal side recomputes fresh each tick (the homeDefense idiom, :143-158 comment in warSiegeVerdict). If eroding the garrison is wanted, it rides the EXISTING flag-gated `defenderAttritionEnabled` spike pattern (`warDeployment.js:462-480, :577-599`) — same flag family, not a new fork.
8. **`dispositionDeltas` assume two distinct settlements** (`{id, outcome:'win'|'loss'}` pushed for occupier AND target, `warDeployment.js:867-868`). A civil war would bank a win and a loss on the SAME id. Aftermath must ride the coup/rebellion aftermath machinery (`LEGITIMACY_SEEDS`, conditions, the echo ladder — D10's own spec), never the war disposition ratchet.
9. **A civil victory is a power transfer, not a conquest.** `pickOccupier` + the conquest outcome mint an occupation authority (`warDeployment.js:711-712, :801-823`); the civil equivalent is `transferRulingPower` cause `'coup'` (the `deploymentReturn.js:459-489` return-coup outcome is the exact template — `candidateType:'coup_succeeded'`, receipted `pHold`/`roll`). ⛔ `RULING_POWER_CAUSES` is frozen vocabulary (W-SEAT law 6); the design needs no new cause.
10. **News addressing:** `fieldBattleNews` stamps `settlementIds: [winnerId, loserId]` (:612) — typed party ids are not addressable settlements. The civil receipt is a NEW authoring site (`settlementIds: [saveId]`, party names in the text), paying the full news bill (authoring census + kind pools + baselines — lane V7's bill, `wseat-census/V-collisions.md` §b).
11. **The unit hazard is live at the exact D10 seam (§711.6, 5th sighting risk).** The engagement machinery is uniformly on the **0..100 military-capacity axis**: deployments seed from `cap.offensive` (`warArmyRecord.js:103`), transit `strength` = `dep.currentEffectiveStrength` (kernel :660), interventions store `patronStrength01 * 100` (`convergence.js:1164`), the command picture divides by 100 (kernel :310). D10's births are **headcounts** ("population × participation01"; "recorded manpower × splinter fraction" — and note the record has NO manpower headcount: `deploymentReturn.js:82-93` measures return strength as `currentEffectiveStrength / maxStartStrength`, a capacity ratio). A headcount passed as `size` next to a 0..100 garrison would resolve silently and catastrophically wrong — the sigmoid is share-based and NOTHING WOULD RED. **The adapter's law: population converts through `deriveMilitaryCapacity`'s manpower path (log10-scaled, `militaryStrength.js:171-180`) via the NAME-4 clerk BEFORE any engagement quantity exists; every contest-side number is 0..100.**

## 3 · THE MINIMAL CIVIL-ENGAGEMENT ADAPTER (shape, not code)

A typed contest resolution — one new module in the convergence family:
- **Ledger**: `civilContests` (or a row on the existing stressor's window), keyed by typed contest id; row = the `seedDeploymentState`-shaped irregular record (NAME-4-derived, 0..100) + `participation01` + `sinceTick`; sparse, dropped at resolution (the interventions/armyTransit idiom).
- **Per tick while the window is open**: build two closures (typed rising id → row envelope; contest id → loyal envelope = garrison + (1−participation)·levy, fresh each tick), call `resolveSiegeVerdict({targetId: contestId, besiegers: [risingId], capacityFor, effectiveStrengthFor, defenderItem: the settlement item, rng, tick, siegeAge: windowAge, supplyInterdiction: 0, spatialSiege: false})`, then `applyAttritionToRecord(row, {isAttacker: true, band, …})`. Falls ⇒ the rising takes the seat (power transfer, cause `'coup'`); capitulation and bands come free; the loyal side heals by recomputation.
- **D9/D11 interveners**: sided rows in the EXISTING interventions ledger (their tilt and their pairwise battles already work, `convergence.js:1181-1222`); a physical repression force joins the loyal closure's sum; the INTERCEPT seam handles columns en route.
- **Below threshold**: no ledger row at all — the force-ratio adj into the existing verdict, the `interventionAdjFor` pattern.
- **Receipts**: verdict numbers verbatim in `reasons` (the :813-816 pattern) ⇒ NAME-3-narratable.

## 4 · WHAT MUST NEVER FORK

- **The force law**: `deriveMilitaryCapacity` + the NAME-4 clerk (D10's own "no second combat math, ever"). The loyal side too.
- **The contest math**: `fieldBattleWinProbability`'s sigmoid+clamp, the verdict cores, the feasibility bands, `computeEngagementAttrition` — import the exports; never copy a constant.
- **The one-resolver adj clamp**: D10's force-ratio term and W-SEAT law 6's legitimacy term BOTH claim "the next additive adj" in `resolveCoupVerdict` (currently four: authority, warSentiment, intervention, economic — `coup.js:128-136`). ⚠ Two designs, one clamp — the D10/D11 mini-panel must charter the slots jointly.
- **The deployments ledger contract** and the occupation machinery (a rising never mints an occupier).
- **Keyed-PRNG discipline**: stable typed keys, distinct streams per site.
- **The 0..100 axis** as the only combat unit — declare it in the SEAT-7 charter as a named unit, per §711.6.

## 5 · THE THRESHOLD QUESTION — what force-size quantity exists to threshold on

**CONFIRMED inventory** (everything measurable at tip): `deriveMilitaryCapacity` → `theoreticalCapacity`/`currentCapacity` 0..100 + six facets (`militaryStrength.js:53-66`) · `capacityFor(id)` → `{theoretical, offensive, homeDefense}` with `ARMY_DEPLOYED_CAPACITY_PENALTY` 14 (`warCapacityReads.js:56, :170-196`) · `deployment.currentEffectiveStrength` (0..100) · `InterventionRecord.strength` (0..100) · `strengthRatioOf` = cur/max with `SPLINTER_RATIO` 0.3 (`deploymentReturn.js:67-93`) · `defenseLedger.military` (0..100) · `settlementStrength` 0..1 (explicitly NOT the war source, `warCapacityReads.js:47-50`) · population (headcount, pre-conversion only). **There is no absolute-force threshold anywhere in the estate — every live gate is a ratio**: feasibility bands 0.78/0.55/0.32/0.18 (`feasibilityGate.js:73-77`), CLAMP_RATIO 3, SPLINTER_RATIO 0.3, convergence's MOTIVE_FLOOR/FEASIBILITY_FLOOR. **Recommendation for SEAT-7**: threshold on the rising's derived capacity RELATIVE to the loyal side (the feasibility-band idiom — arguably reuse `classifyFeasibility` itself as the router: sub-plausible risings stay in verdict-math land with the force-ratio adj; plausible ones open the engagement). PLAUSIBLE, tuning-adjacent, owner-visible constants — the charter's call, but the ratio shape is the estate's only precedent.

**Open flags for the chair**: (i) the adj-slot collision (§4, third bullet); (ii) whether garrison erosion in long civil contests lights `defenderAttritionEnabled` or stays fresh-recompute (balance-pass gated per `attrition.js:75-87`); (iii) D10's "recorded manpower" wording vs the record's capacity-ratio reality (birth (a) sizes correctly off `currentEffectiveStrength × splinter fraction` on the capacity axis, but the addendum's word "manpower" invites the §711.6 unit slip — the SEAT-7 charter should say "recorded strength (0..100 capacity axis)" explicitly).

---

# D11-C5 — COVERT INSTIGATION (census wf_7192952f-226, verbatim)

All census arms complete. Deliverable follows.

# LANE C5 — THE COVERT SUBSTRATE FOR INSTIGATION (D11, ODQ §743)

All paths under the build-tip extract mirroring 73f5dfc02 (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours/`, `src/` prefixed). Sealed addendum read at `e14b9a0db:docs/DESIGN_W_SEAT.md` :87-93. Read-only; no gates run.

## 1. THE ASSET SUBSTRATE — corruptionWeb.js, full lifecycle (CONFIRMED)

**Gate**: `corruptionWebActive` (`src/domain/worldPulse/corruptionWeb.js:225-230`) = `beliefsActive` AND virtual `corruptionWebEnabled === true`, "NO entry in DEFAULT_SIMULATION_RULES." **Dark at tip in every preset**: the compendium row is machine-generated ground truth — `compendiumData.generated.js:444`: `"flag":"corruptionWebEnabled","presetGated":false,"dormant":true,"presets":[]` — "off in every default preset today and wakes only under a custom ruleset."

**Planting** (`advanceCorruptionWeb`, :580-666): per patron (codepoint-sorted), ONE candidate mint per tick through a gate stack — a live CHANNEL (`rawChannelQuality` :309-338: hostile 0.6 / cold_war 0.5 / rival 0.4 edges, criminal corridors 0.7, smuggle pairs 0.5, returned captives 0.6); scarcity (`MAX_ASSETS_PER_PATRON: 3`, one asset per (patron,target) pair, at-cap ⇒ visible `per_patron_cap` deferral); affordability (`UPKEEP_AFFORD_FLOOR 0.3 + 0.12/asset`, prosperity vocabulary); then the E0 loaded draw `u >= INITIATE_BASE(0.1) × weight²` ⇒ `e0_deferred`. `recruitmentWeight` (:412-438) = channel × (1 + 0.6·obligationDebt) × (1 + 0.5·darkReframe) × (1 + underways ease) × (1 − 0.4·HIDE secrecy) × (1 − 0.7·officialPay) × (1 − 0.55·coalitionConsolidation). Mint writes `npcStates` only: `corruption:true`, `corruptionProfile.vector:'forbidden_patron'`, `corruptionHeat ≥ 0.3`, `corruptionLeash {kind:'foreign_settlement', settlementId: patron, conspiracy:'foreign_web', covert:true}` (:716-737). **Covert, no news** (:657-659 — "A DM-truth receipt rides the returned deferrals/effects channels, never a public headline").

**The registry is derived, never persisted**: `foreignAssetsByPatron` (:451-470) scans every settlement's NPCs, resolves through the ONE chokepoint `resolveLeash` (corruptionLeash.js), so web-minted, betrayal-seeded, and DM-composed leashes all count uniformly. This is D11's "compromised asset" set.

**Effect reads** (all 0-when-dark): `foreignGripOf` (:486-492, saturating `1−e^(−n·0.8)`); `directionBias` (:506-513, ≤0.3 covert weight-tamper the target feels toward its patron — the modulator D11's "raising attempt gates" is the sibling of); `assetSightFidelityOf` (:526-532, 0.5 flat — paid eyes). Espionage already reads the web same-tick: `espionageLeak.js` header — "THE WEB IS READ ON THE SAME TICK IT IS WRITTEN. NO LAG" (a leashed operative copies home's intel to the patron).

## 2. THE EXPOSURE-ODDS SUBSTRATE — what scales exposure today (CONFIRMED)

There is **no per-ACT exposure roll on a web asset today**; the only asset-death rolls are:

- **Organic per-tick exposure** (`npcAgency.js:789-850`): `exposureChance` (`corruption.js:376-392`, constants :114) = clamp(0.05 + 0.20·security + 0.12·prosperity − 0.22·guildStrength + 0.06·visibility, ×(1+0.35·priorExposures), ×deityDisfavor, [0.01, 0.50]), where visibility = dotRank/3 + proximity (:799). Outcome: demoted (leash SURVIVES, heat ×0.7) or ousted (corruption cleared, `ousted:true`, npcState pruned at `pulseKernel.js:721-727`) — `atBottom && u < 0.08` (:830).
- **Endpoint death**: `foreignEndpointLive` (:826-846) — patron occupied/destroyed/gone ⇒ the causeLifecycle terminal re-points the arrangement.

**Per-act exposure precedents to copy**: SEE covert-posture exposure — `SIGHT_TUNING.EXPOSE_BASE 0.06 + EXPOSE_SECRECY_GAIN 0.55×target-secrecy` per tick, rng fork `infowar-see-expose:${w}:${t}:${now}` (`informationStatecraft.js:1016-1046`); LIE exposure — contradiction ≥2 bands or age ≥8 ticks (`disinformationPlant.js:92-93`); espionage dwell-catch `catch01` (`espionageGauntlet.js:378-386` — dark, see §7). D11's "VASTLY increased exposure odds" inserts cleanest as an **act-scoped roll inside the (lazy) web module with its own `CORRUPTION_WEB_TUNING` constant** — because two recorded blockers forbid the obvious alternative: `npcAgency.js` "sits at its max-lines baseline ceiling" AND "exposureChance lives in the EAGER corruption.js first-paint chunk, so a [new] param there costs eager bytes for a dark path (the ratchet forbids that)" (npcAgency.js:802-808, the D6 deferral note — the exact same wall an instigation term in `exposureChance` would hit).

**Write-only stock finding**: `corruptionHeat` has 15 write/decay sites and **zero readers** (grep across `src/`: every hit is a write, decay `NPC_RELAX 0.92` npcAgency.js:663, or zero-on-cleanse) — a pre-existing "how hot is this asset" field that currently feeds nothing. An instigation act could heat it, but wiring heat into exposure odds is a same-seed behavior shift for existing corrupt NPCs unless the read is web-gated — flag in the SEAT-8 brief.

## 3. WHAT EXPOSURE COSTS TODAY — the blowback triple (CONFIRMED)

`applyForeignExposureBlowback` (`corruptionWeb.js:888-970`), consuming exposures `advanceNpcCorruption` annotated `foreign:true` + resolved patron + importance at the moment the leash was in hand (npcAgency.js:821-828). Per foreign exposure, magnitude = clamp01(0.35 + 0.35·rank/3 + 0.15 if ousted) (:758-764):

1. **`exposedCorruption` spatial ledger** mint/refresh on the directed `${corrupted}>${patron}` key, geometric decay 0.94/tick, pruned when read-decayed < 0.05 (:905-917) — **the war-reason fuel** (§4 below).
2. **People-held grievance**: resentment +0.5·mag on the shared edge + `foreign_corruption_exposed` incident via `applyRelationshipPatch` — feeds scoreGrievance the SAME pulse (:929-942).
3. **Credibility charge**: deception 0.6·mag against the patron — **but folded through `advanceCredibility`, which gates on `infoStatecraftActive`** (:965-967; informationStatecraft.js:332-334). ⚠ With corruptionWeb lit and infoStatecraft dark, the credibility leg is a silent no-op — D11's "exposure costs" spec must name this conjunction.
4. **BOTH-court legitimacy hits** returned as `courtHits` (severity 0.3 + 0.4·mag), applied by the kernel as `withActiveCondition` archetype `corruption_exposed` with typed cause rows ("the court looks rotten" / "looks villainous") — `pulseKernel.js:684-709`. Note :693: an off-map patron court silently takes no hit.

Sibling costs for calibration: exposed LIE = deception 1.0 + grievance 0.3 + personal mouthpiece charge + `infowar_lie_exposed` news (informationStatecraft.js:567-615); exposed SEE = deception 0.8 + grievance 0.3 + posture burned + `infowar_spy_exposed` news (:1023-1045).

## 4. CASUS-BELLI GRADING FOR EXPOSED INSTIGATION (CONFIRMED)

- `scoreCorruptionExposed` (`warReasons.js:610-614`) is live and wired: `collectWarReasonInputs` row :884 feeds `exposedCorruptionForPair(ws, fromId, toId, tick)` — the read-decayed ledger read (corruptionWeb.js:806-813), with the directed pair key deliberately inlined both sides ("MUST stay byte-identical to warReasons.reasonPairKey — pinned by a drift-guard test", :83-89) so the import runs one way.
- **Instigation "grades toward casus belli" for free** by refreshing the SAME ledger with an instigation bonus in `exposureMagnitude01` — no new reason type. A NEW typed reason (`instigation_exposed`) instead pays the full taxonomy bill: "the casus taxonomy is walker-enforced for totality AND bijection" (warReasons.js:98-99), plus `DECLARE_CASUS`'s engine-derived refusal class (:1029-1060).
- **The intervention-market casus already exists**: `scoreForeignClash` (:572-583) — "two sponsors backing OPPOSING sides of the same internal (coup) contest are minting their next war BETWEEN themselves. PROXY STAYS PROXY."

## 5. THE RALLY HOOK — the legitimacy applicator a survived plot rides (CONFIRMED paths; choice PLAUSIBLE)

Two lawful applicators exist, and the symmetric insertion is already built:

- **The return channel exists**: `applyForeignExposureBlowback` already RETURNS both-court `courtHits` that the kernel stamps as conditions (pulseKernel.js:691-707). A survived-instigation rally is a third row (`role:'rallied'`, positive) on that same channel; `applyConditions` supports signed direction (`causalState.js:268-286`, mode 'signed' with `conditionDirection`), moving the derived `causal.public_legitimacy`/`ruling_authority` lenses (±15-scale) that `coupSpawnGate` reads for its authorityMult (stressorGates.js:753-756).
- **The persisted-score applicator**: `applyLegitimacyDeltasToUpdates` (`generosityUpdates.js:80-107`) — the estate's ONE `publicLegitimacy.score` applicator over pending settlement writes; positive deltas are precedented (upswing `RECON_LEGITIMACY_DIVIDEND`, upswingKernel.js:522; generosity "granary city" lift). This is the path that moves the score `coupSpawnGate` hard-gates on (`score >= 45 ⇒ null`, stressorGates.js:746-748).
- ⚠ Cross-lane hazard (V-legitimacy's finding, re-confirmed here): the score-only applicator never re-bands, so a rally through it leaves `govMultiplier` stale for `coupContenders` — a rally should ride whichever read-point W-SEAT-0's legitimacy-read-point law canonizes.

## 6. WHERE THE INSTIGATION VERB SLOTS (insertion points)

1. **Sim-side act (REC per Q-S6: sim-only first)** — a new arm in the lazy web family: a mover pass over `foreignAssetsByPatron` (the derived registry), E0-tempo loaded draw per (patron, target, tick) on the `corruptionWebForkKey` idiom (:233-235), motive-weighted by the patron's BELIEVED picture (K3: `seatBeliefRecord(beliefMaps, patronId, targetId)` — the same read `processLies` desperation uses, informationStatecraft.js:648-663 — so a blunder against a believed-weak-but-actually-stable town is expressible). Its writes: a gate `mult+reason` row into `coupSpawnGate`/`rebellionGate` (the `{probabilityMult, reasons[]}` return shape, stressorGates.js:741-764/786) + D10's `participation01` seed; its exposure roll act-scoped per §2.
2. **The paid-covert-act template** — the commissioned plant lane: `commissionedPlants → processLies` fold (2) (informationStatecraft.js:627-639), with `plantExposureReasons` naming seller AND buyer in the exposure news (:598-601) — the exact "exposure names the instigator in receipts" mechanism, already shipped for bought lies (brokerageServicesPlant.js / brokeragePlantHandoff.js).
3. **DM/player verb (later, per the espionage surface's own ladder)** — `src/domain/events/realmManifest.js` entry shape `{verb, label, family, scope, lane, module, dials, coversVetoCodes, predicate}`; the direct sibling is `ORDER_INTERVENTION` (:373-389, family 'War', module convergence.js, gate `interventionActive`, targets `coupContestOptions`).
4. **NOT the espionage mission lane at tip**: `ENVOY_COVERT_PRODUCTS` is frozen `['acquire','confirm','refute']` (envoyErrandVocabulary.js:168) — information products only; an ACT product is a new persisted-vocabulary member (owner-gated DTO shape), and the mission layer has no dispatcher anyway (§7).

**Intervention-market substrate for D11's many-sided evaluation** (CONFIRMED, lit): `convergence.js` — typed MOTIVE catalog with stake-naming receipts (:190-300: preserve_order/install_friendlier_regime/protect_investment/kinship/denial), sided rows in the `interventions` spatial ledger (:498-513), `interventionLegitimacy({side, invited})` already pricing invited vs uninvited (:405-408), and `scoreInstallFriendlier` already reading `leashOnChallenger01` — the corruption leash as a puppet lever (:259-270).

## 7. WHAT IS DARK AT TIP — the PLAUSIBLE-only perimeter

| Layer | Flag/gate | Tip status |
|---|---|---|
| Corruption web | `corruptionWebEnabled` (+beliefsActive) | **Dormant; no preset** (compendiumData.generated.js:444) |
| Info statecraft | `infoStatecraftEnabled` (+beliefsActive) | **Dormant; no preset** (:445) |
| Espionage | `espionageEnabled` (+beliefs +`errandSpineEnabled`, 3-door `espionageGate.js:76-84`) | **Dormant; no compendium row at all** (0 grep hits); missions additionally have NO dispatcher — "NOTHING UNDER src/ CALLS THIS FILE" (espionageMissions.js:14-21) |
| Intervention | `warLayerEnabled && interventionEnabled` (convergence.js:223-229) | **LIT in 3 presets** — dramatic_campaign, living_realm, full_simulation (:442) — the one lit covert-adjacent precedent |

Therefore PLAUSIBLE-only (no executed path can prove them at tip): every claim about how instigation exposure *plays* — the credibility charge (needs infoStatecraft lit, §3.3), the espionage-caught custody arm (computed-not-persisted: gauntlet blockers 1–3, `custodyWritten:false`, espionageGauntlet.js:15-47), and any coupling routed through a dispatched covert mission. CONFIRMED-at-tip by contrast: the intervention verdict channel (`interventionAdj` is the live 4th additive term in `resolveCoupVerdict`, rulingPowerCoup.js:140-176), the corruption_exposed casus wiring, the courtHits condition lane, and every dormancy gate's fail-closed shape.

## 8. RECEIPTS/NEWS HOMES (the bill an instigation-exposed beat pays)

Authoring: the wizardNews admission-key discipline (`id: wizard_news.${tick}.<kind>.<stableParts>`, REQUIRED id/settlementIds/severity — the in-file guards at informationStatecraft.js:584-589/1026-1029 are the template, including the collision-freedom argument each id must state). Routing: `src/domain/realm/heraldRouting.js` SECTION_OF — total, single-home, walker-enforced ("a NEW producer kind fails the totality walker until a human files it", :13-18); the infowar siblings file under 'war' (:126), `corruption_exposed` under 'events' (:265). Reader surface: rumor pool rows (`rumorPhrasePools.js:165-180, 823-824`; `settlementRumors.js:284-285`). Covert discipline: the act itself is covert-no-news (the seedBetrayalTraitor invariant, corruptionWeb.js:657-659); only exposure goes public — with the actor layer (`npcIds`) present only when a named soul was genuinely charged (:602-609), and the news-address stake-naming the convergence receipts already model.

---

# D11-C6 — INTERVENTIONS AND ECONOMICS (census wf_7192952f-226, verbatim)

LANE C6 — INTERVENTIONS, NETTING, AND ECONOMICS. Sources: build-tip extract mirroring `73f5dfc02` (all src paths below are that tree; `convergence.js` read whole, 1,426 lines), sealed volume `e14b9a0db:docs/DESIGN_W_SEAT.md` (+ADDENDUM D10/D11), `e14b9a0db:docs/DESIGN_W_COIN.md`, tests rostered via `git ls-tree 73f5dfc02`. Read-only; no gates run.

## (1) THE INTERVENTION PLUMBING MAP (all CONFIRMED unless marked)

**Gate + lighting posture — the single most consequential fact for SEAT-8.** `interventionActive` = `warLayerEnabled === true && interventionEnabled === true` (`src/domain/worldPulse/convergence.js:223-228`). `interventionEnabled` is a WAVES preset key (`simulationRules.js:429-439`, `interventionEnabled: true` at :432), spread into `dramatic_campaign` (:557, with `warLayerEnabled: true` at :544) and `full_simulation`; `living_realm` gets WAVES but keeps `warLayerEnabled` inherited-false so intervention sleeps there (:596-598). It is NOT in `ENGINE_GATED_VIRTUAL_RULE_KEYS` (only occurrence in the file is :432; the roster at :185+ holds 25 keys, none intervention). **The intervention layer is therefore SHIPPED-LIT on the two war-lit presets** — any SEAT-8 change to this machinery's semantics is a live behavior change there, not a dark extension.

**How `intervention_ordered` mints — three arms, one record shape.**
- *Autonomous*: `advanceIntervention` (:987-1262), staged at `pulseKernel.js:2471-2484` under `rng.fork('intervention')`. Per live coup contest (`liveCoupContests` :951-970 — brewing `coup_detat` stressors, no `resolutionReason`, live `coupContenders` field), every relationship-edge neighbor not holding a deployment (one-army law at derivation, :1017-1019/:1068) scores 5 typed motives (`scoreMotives` :334-355 over `MOTIVE_TYPES` :191-197: preserve_order/install_friendlier_regime/protect_investment/kinship/denial; kinship inputs are hardwired 0 in wave 1, :934-936), × feasibility (:364-370). **One strongest suitor per target per tick** (:1060-1063 comment: "at most one new column commits per contest per tick; interventions accrue over ticks, never a same-tick swarm"); loaded dice fork `intervene:<target>:<tick>`, initiates iff `u < 0.12 × pull²` (:391-395, :1151-1153).
- *DM-mode*: `authorityFor(rules, 'intervention_ordered', 'auto')` consult at :1009; non-auto ⇒ deferral + a PENDING proposal with `proposalPayload {verb:'ORDER_INTERVENTION', args:{patronId,targetId,side,invited,strength,motive}}` (:1114-1127), deduped via `pendingActorMajorFor` (:1110), docket-admitted (:1128-1144); "The DM's approval REPLACES the loaded dice" (:1106-1107).
- *DM verb*: the W-COMPOSER-2 lift LANDED — `realmVerbExecution.js:486-513` is a live `ORDER_INTERVENTION` apply arm writing the verbatim record shape into the same ledger (gate-dark/no-contest/busy/already refusals :488-496), and `realmManifest.js:175` carries its target-options predicate. The convergence.js header's "NOT registered" note (:61-64) is stale; the factory notes (:1397 etc.) reflect the tree. **Q-S6's instigation verb has a complete precedent lane.**

**Ledger row shape**: `InterventionRecord {interId, target, side:'incumbent'|'challenger', motive, invited:boolean, strength:number, sinceTick, lastTick, state?}` (:486-496), keyed `<interId>:<target>` in a DEDICATED `interventions` spatial ledger — deliberately NOT `deployments` (isolated-ledger judgment :42-50), drop-when-empty (:1233-1236). Two writers exist (the mover + the verb arm); `ledgerOwnershipManifest.js` rosters only 3 ledgers (envoy_errands/treaties/belief_maps, :23/:49/:72) — `interventions` is unrostered, so the manifest is selective, not exhaustive.

**How strength arrives — ABSTRACT, instant, one-tick verdict latency; no physical transit.** `strength = round4(max(1, patronStrength01×100) × (1+mercFactor))` (:1164); `patronStrength01Of` is the `economic_capacity/100` snapshot read (:846-850) — an economic proxy, not a mustered army. No armyTransit record, no travel ticks, no spatial position ("intervention columns ride the isolated ledger, not the transit ledger, so nothing to intercept spatially in wave 1", :793-794); aggregate-only, no NPC roster (:34-35). The stage runs after this tick's verdicts, so a committed column tilts the NEXT tick's verdict (`pulseKernel.js:2461-2470`: "a column that arrives after the verdict marched to yesterday's coup"). Mercenary clause: bounded ≤ +30% reinforcement, prosperity-scaled (:875-907). **D9's "physical army act (armyTransit, upkeep)" is therefore a FIRST wiring, not a generalization** — nothing physical exists to generalize.

**`invited` semantics — derived, never chosen**: `invited = side===INCUMBENT && FRIENDLY_REL.has(relType)` (:1111, :1159); `FRIENDLY_REL = {allied, trade_partner, vassal}` (:840, with the CS-B3 `ally→allied` cure note :828-839). `interventionLegitimacy` (:405-418): only an incumbent-prop can be truly invited; cost 0.05 invited vs 0.45 uninvited; `casusGenerative = !trulyInvited`. D9's invited-repression maps directly onto this flag.

**Resolution and expiry — no clock, no decay, no upkeep.** Records leave only by: (a) the aftermath sweep (:1039-1050) — target no longer a live contest ⇒ record deleted; a CHALLENGER-backer whose coup fell (`coupFellAt` :1267-1278, fresh `government_overthrown` since `sinceTick`) mints the predatory obligation `kind:'intervention'` (:454-459, folded :1238-1250, decayPerTick 0); (b) losing a multi-sided field battle ⇒ records deleted, retreat home (:1219); (c) nothing else — no tick-based expiry, no strength attrition while holding. State vocabulary is PARTIALLY DORMANT: of `INTERVENTION_STATES` (:176-182), only HOLDING (:1203) and ATTRITED (:1216) are ever written; BESIEGING/SCREENING/RETREATED are declared, never assigned (grep: 3 write sites total). `overstayOccupation` (:708-721) and `contestSideLookup` (:796-807) have ZERO callers outside convergence.js — typed seams, unwired.

**`interventionAdjFor` netting — multiple simultaneous interveners DO net today, twice over.**
- *Political netting (the verdict read)*: `interventionAdjFor` (:542-551) takes all records at the target, shares = `strength/totalStrengthAtTarget`, then `interventionTilt` (:430-443): `signed = Σ(+share incumbent, −share challenger)`, soft-bound ±1 (inert — shares already sum to 1), × `PHOLD_WEIGHT 0.22`. Consumed at `coup.js:117` as one of four additive adjs into `resolveCoupVerdict`'s pHold sum, clamped [0.1, 0.9] (`rulingPowerCoup.js:147,176`: `share×severityDrag + authorityAdj + warSentimentAdj + interventionAdj + economicAdj`). So D11's "multiple simultaneous interveners net" is ALREADY the live math — SEAT-8 extends the eligible-committer set and stakes, not the netting law.
- *Physical netting (before the read)*: the multi-sided resolution (:1173-1222) — `deriveSides` (:567-588, rivals-never-merge on mutual hostility), engagement EV (:600-643, the vulture/hold dynamic), `resolveSideBattle` → `resolveFieldBattle` verbatim (:654-668) — deletes losing sides each tick, so the verdict often reads an already-net field.
- Other consumers: `foreignClashIntensityOf` → `warReasons.js:886` (foreign_clash) + `peaceReasons.js:572` (spheres_understanding). News: `interventionNews` (:1322-1351, impactKind `intervention`, motive+legitimacy+mercenary receipts, `casus_generative` tag) + `sideBattleNews` (:1356-1381, impactKind `intervention_clash`); rumor pool row `rumorPhrasePools.js:825`; certification `subsystemRowsWaves.js:104`.

## (2) COLLISION TABLE — SEAT-7/8

| Collision | Shared surface (receipts) | Verdict |
|---|---|---|
| **SEAT-8 × the SHIPPED-LIT intervention layer** | `interventionEnabled` preset-lit (simulationRules.js:432,:557); one-suitor-per-target-per-tick law (convergence.js:1060-1063); `interventionDormancyGolden.test.js` pins BOTH a dormant hash AND a lit-path anti-vacuity arm (header, tests/property) | **Highest-risk row.** D11's "every relevant foreign power evaluates… and may commit" widens the one-best-suitor selection — a live semantic change on dramatic_campaign/full_simulation unless every D10/D11 term is additive-0-when-dark behind the NEW seat-family flag, never behind `interventionEnabled`. The lit-path golden arm WILL move if lit behavior changes ⇒ declared-shift discipline, never a silent re-record. |
| **W-COIN-2 upkeep × the isolated `interventions` ledger** | W-COIN-2's upkeep sink reads the ONE-ARMY `deployments` ledger (DESIGN_W_COIN :236, "read from the same one-army ledger the food pass reads (pulseKernel.js:510-512)"); intervention columns deliberately live elsewhere (convergence.js:42-50) | **As chartered, intervention/repression columns are upkeep-FREE even when treasury lights.** D9/D11's "upkeep, a W-COIN sink when lit" needs either W-COIN-2's reader widened to the interventions ledger or a SEAT-8 sibling sink — ONE upkeep law parameterized by ledger (one-resolver-per-quantity), and W-COIN-2's never-block-a-deployment shortfall rule (:237) extends with it. Name the seam in BOTH charters. |
| **Plunder/seizure** | `razingSpoils` inert (`plunder: 0`, razing.js:816-841; phantom `wealthIndex` read warDeployment.js:763) — W-COIN Q8 cures it as a sack TRANSFER | D10's "facets scale to what rebels can seize" must stay FACET-side (stat degradation in the force derivation), never a coin movement, or it forks the sack primitive. No repression-cost quantity exists today anywhere (warCosts.js is comparative bands only — WAR_COST_BALANCE_BANDS :25-31, no coin). |
| **NAME-3/4 (composition clerk) dependency — direction matters** | NAME-4 = `deriveForceComposition(settlement, deploymentRecord)`, "derived-on-read, zero bytes, composition NEVER an input to strength" (DESIGN_FMG_WEAVE :134); NAME-3 = display-leaf narrative deriver (:133); militaryStrength facets manpower/institutions/materiel/logistics/economy/will + weights (militaryStrength.js:78-85) | **The force-STAT provider is militaryStrength alone**; reading D10's "statted by the militaryStrength facets + the NAME-4 composition clerk" as clerk-feeds-stats would invert NAME-4's constitutional law. Contract instead: SEAT-7 mints the civil-battle/engagement record carrying the fields NAME-3/4 read, and shares NAME-4's closed ~6-type vocabulary (one vocabulary, two consumers) — seal the vocabulary before SEAT-7's composition fields land, but SEAT-7's engine math does NOT block on either display car. Note militaryStrength's "MOUNTED NOWHERE (B0)" header is STALE — 12+ importers today (occupation.js, warCapacityReads.js, …); the WarFaithTab light-import constraint still binds display surfaces. |
| **Shared-file edits inside W-SEAT** | `coup.js`/`rulingPowerCoup.js`: SEAT-2 adds `foreignSeatAdj` (the 5th adj); SEAT-7's force-ratio term would be the SIXTH into the same [0.1,0.9] clamp (rulingPowerCoup.js:176) | Serial SEAT-2 → SEAT-7 on the verdict file, with a declared adj-budget composition (six signed terms into one clamp is tuning-signature-adjacent). `convergence.js` is SEAT-8's home and already carries the CS-B3 habitat-pin coupling (:828-839) — the FRIENDLY_REL/HOSTILE_REL sets are load-bearing for `invited`. |
| **D10 birth (a) × deploymentReturn** | Splinter path deploymentReturn.js:427-503: vassal return-coup rolls `resolveCoupVerdict` WITHOUT worldState/adjs (:447-452); generic splinter is a `faction_challenge` CONDITION, not a force (:497-503, :197); levy conservation invariant "deployed − returned === war dead" (simulationRules.js:94 comment; `deploymentReturnLevyConservation.test.js`) | Birth (a) upgrades a condition-seed into a typed force — the conservation invariant binds its sizing (splintered men must not double-count against war dead), and the return-coup verdict site should route through the same adj-bearing verdict as coup.js or the one-resolver law forks. |
| **D11 instigation × corruptionWeb/informationStatecraft** | Foreign-asset substrate: one-live-asset-per-patron scarcity + realm cap (corruptionWeb.js:114-121), `foreignGripOf` saturating (:486), exposure magnitudes (:181-188); exposure/blowback triple EXPOSE_BASE 0.06/SECRECY_GAIN 0.55/CHARGE01 0.8 (informationStatecraft.js:776-778), blowback news precedent (:1017-1036) | Right homes, but BOTH are ENGINE_GATED-flagged (`infoStatecraftEnabled`, `espionageEnabled` in the :185 roster) ⇒ instigation is an AND-composition (seat flag AND info substrate), the espionageGate lighting-order precedent (`errandSpineEnabled` `!== true` read, roster comment :42 block). The legitimacy-RALLY back-edge must ride the governanceLedger read-point law (volume §2.1) — participation01 reads legitimacy, never writes; the rally is a receipted legitimacy DELTA through the existing apply path, not a new writer. |
| **participation01 — a NEW quantity** | Zero hits tree-wide (grep `participation01` over src: empty — CONFIRMED by absence). Inputs all exist: legitimacy band (governanceLedger), commons rung (commonsVoiceKernel.js:59-63 RUNG_PETITION/GATHERING/RIOT + :155 RUNG_KIND), aligned-faction share (settlementPolitics), occupation `resistance` | Mint ONCE in SEAT-7 as the single politics→force bridge; every consumer (rising size, instigation seed, loyal-side complement) imports it. No "wartime-levy profile" resolver exists either (demographicsLadder.js:68 `levy` is a boolean capability) — SEAT-7 defines levy capacity from the manpower facet + mobilization reads, one resolver. |
| **W-COIN sequencing (unchanged)** | Volume §5; V7's receipts | No W-SEAT implementation car before W-COIN-1a; SEAT-7/8 additionally after SEAT-2/SEAT-4 + the D10/D11 mini-panel. SEAT-8's coin legs (upkeep, material commitment = treasury transfer) sit behind the treasury flag regardless — chart them as named seams that light when BOTH flags do. |

## (3) THE CENSUS BILL (SEAT-7/8)

- **Flags**: D10/D11 need ENGINE_GATED_VIRTUAL_RULE_KEYS-class lighting (owner-lit, dark at launch) — `interventionEnabled` cannot host them (preset-lit, above). The honest Q-S3 amendment: D10 is NOT intrinsically foreign (a domestic coup births a force with no seat anywhere), so "one `foreignSeatEnabled` covers D10/D11" couples domestic upheaval physics to the foreign-seat lighting. Recommend surfacing to the owner as a Q-S3 sub-question: one key (simplest, REC) vs a D10 sibling (`irregularForceEnabled`) with D11 under the seat key. Per-key idiom cost, ONE commit each: roster entry (simulationRules.js:185+) + `engineGatedRuleKeys.walker.test.js` both-ways proof + `subsystemRowsVirtual` certification row + mechanismLitCoverage LITERAL `<flag>: true` drive + dormancy proof.
- **Dormancy bar**: raw-byte base-dormant vs tip-dormant (§713.2) for both cars; the existing `interventionDormancyGolden` (hashed-summary + anti-vacuity, tests/property + `tests/fixtures/intervention-dormancy-golden.json`) is the CONTAINMENT instrument for SEAT-8's convergence.js edits — its dormant hash must not move; its lit arm moving is a declared shift.
- **News bill** (D8/V6-B3 discipline): new kinds — civil battle (shape precedent `sideBattleNews`/`intervention_clash`), instigation exposure (informationStatecraft blowback news precedent), legitimacy rally; intervention COMMITMENT already exists (`interventionNews`, stake = motive receipt) — extend stake-naming for political/material commitments rather than minting a duplicate kind. Each new kind pays: the wizard-news authoring census (denominator: every object literal under src/domain|src/store co-locating `kind`+`headline`; REQUIRED_FIELDS `['id','settlementIds','severity']` — `tests/lint/newsAuthoringCensus.shared.mjs:19`), `newsHeadlineContract.walker` + `newsVoiceContract.walker` + the three tests/lint baselines, kind pools (NO intervention pool walker exists — the war family is warCoalition/warCost/warRuling pools; a civil-battle kind either joins one or mints a pool + `kindPoolFloors.walker` row), a `rumorPhrasePools.js` row (:825 precedent), explicit herald routing.
- **Test-estate homes (extend before minting)**: `tests/domain/convergence.test.js`, `tests/property/interventionDormancyGolden.test.js` (+fixture), `playerInterventionEvents.test.js`, `warConvergenceForces.test.js` (the engagement/force-vocabulary coupling pins live HERE — a SEAT-7 vocabulary touch reds it), `coupEvents.test.js`, `deploymentReturnLevyConservation.test.js`, `deploymentReturnOccupied.test.js`, `militaryStrength.test.js`, `mobilization.test.js`/`mobilizationPulse.integration.test.js`/`mobilizationRecordMode.test.js`, `armyTransitFieldCombat.test.js`, `informationStatecraftPins.test.js`, `corruptionWebPins.test.js`, the 13 `espionage*.test.js` homes, plus V7's ~20 political homes for the gate/legitimacy arms.
- **New-file bill: 2** (a SEAT-7 unit home for irregular-force/participation01 + one `*Dormancy.byteIdentity` per new flag; SEAT-8 extends convergence.test.js + the statecraft homes). Per new file: WHOLE lighting-census re-derivation (all five figures, same commit, batched per landing window — the three-censuses law), negativeAssertionAnchor ceiling 0, keep `census|walker|golden|contract|pin` out of basenames. Persistence: ZERO new persisted fields — the contest-window force row is a new spatialLedgers key on the armyTransit drop-when-none-afield idiom (`armyTransitKernel.js:916-923`) with the `interventions` ledger as shape precedent; a `ledgerOwnershipManifest` row is optional by precedent (only 3 of many ledgers rostered) but recommended since the force ledger will have ≥2 writer sites (mover + verb arm) from birth.
- **Pre-soak sequencing**: SEAT-7/8 dispatch after SEAT-2 (verdict-file serial, seat books) and SEAT-4 (D5 bounded set is D11's evaluator roster), after the D10/D11 mini-panel folds; both are engine cars ⇒ pre-soak, and the anti-snowball law (§2.8) applies doubly to SEAT-7 (participation01 feeds force feeds legitimacy-rally feeds participation — the loop the soak must bound; CONVERGENCE_TUNING's own header names checkpoint-soak retuning, convergence.js:108).

**Open flags for the chair**: (i) the D10-flag ownership question above (domestic physics under a foreign-named key); (ii) W-COIN-2's upkeep-reader scope (deployments-only vs +interventions) — needs a ruling in both charters before either car lands; (iii) the addendum's "NAME-4 composition clerk" wording vs NAME-4's composition-never-feeds-strength law (PLAUSIBLE mere wording; the mini-panel should pin the reading); (iv) six signed adjs into one pHold clamp — a declared composition/budget row for the tuning signature.

---

# W-SEAT PANEL LENS 1 — DETERMINISM (verbatim)

FINDINGS — LENS 1 (determinism, dormancy, the flag)

**1. BLOCKER — §2.1 / SEAT-1. The "one truth in two coats" hypothesis is already refuted by the design's own receipts layer, so SEAT-1's migration cannot pass its own raw-byte bar honestly.**
Defect: law §2.1 says "Legitimacy reads route through `governanceLedger` — SEAT-1's first act PROVES the one-truth-two-coats claim (wrapper vs raw reads) and migrates the three raw readers it touches," and the §4 car table puts SEAT-1 under the raw-byte bar. But the wrapper and the raw readers are provably different functions on the legacy save shape. Receipt, verified in the extract: `governanceLedger.js:63-66` — *"Legacy shape: a bare number. Honour it so old saves still move every lens"* (returns the bare number); vs `mobilization.js` `legitimacyOf` — `s?.powerStructure?.publicLegitimacy?.score; return Number.isFinite(score) ? … : 50` (a bare-number legitimacy reads as **50**); `occupation.js` `resistanceTarget` — `num(item?.settlement?.powerStructure?.publicLegitimacy?.score, 50)` (same); `stressorGates.js:746-748` coupSpawnGate (same idiom, and `if (score >= 45) return null` means a legacy score of 20-read-as-50 currently *suppresses* coups that the ledger read would *allow*). V2's own report states this: "three inline `publicLegitimacy?.score` readers that do NOT honor legacy bare-number." Migrating them is a same-seed behavior change on shipped-lit paths (coup verdicts are on the default-true stressor path per `coup.js`'s own comment) with `foreignSeatEnabled` DARK — and a dormancy corpus containing no legacy bare-number save passes while the shift ships (the §713 28-of-29 trap). The same class hits SEAT-1's `vassalOverlordOf` collapse: the two same-named functions read different substrates (`occupation.js:1232-1242` relationship edges; `traditions/relations.js:84-90` the ladder rung), divergent cases exist by construction (liberation lifts occupations, not vassal edges — V4), so one consumer's lit-world answer changes.
Amendment: split SEAT-1. New machinery (foreignSeatOf, the flag) keeps the raw-byte bar. Each unification/migration is either (a) proven set-equivalent at tip with a fixture that CONTAINS the divergent shape (a legacy bare-number save; a rung-without-edge world) — a proof the design must accept may FAIL, with a fork stated for that outcome — or (b) chartered as a declared repair shift, owner-visible, with its own golden fence. Per-car declared-shift lines become mandatory (the WEAVE A1.2.7 precedent).

**2. BLOCKER — D4's occupied-predicate move is an unflagged behavior change or a permanent second truth, and the design refuses to say which.**
Defect: D4 ends "The coupSpawnGate's occupation-null moves from the stressor spelling to THE occupied predicate (law §2.1)" with no flag statement and no shift declaration, inside a car whose bar is raw-byte. The two spellings name different sets structurally: the occupation STRESSOR is pressure-born (`stressorsCore.js:73-81`, birthThreshold 0.76 on conflict/legitimacy pressure, gated by `occupationGate` at `stressorGates.js:351` on siege/war/hostile proximity — nothing mints it at conquest), while the LEDGER is conquest-born (`occupation.js` `freshConquestsFrom`). A pulse-conquered town with the ledger and no stressor changes gate outcomes the tick this lands, flag dark. If instead the move is gated by `foreignSeatEnabled`, the second-truth fork law §2.1 claims to cure survives on every dark world forever.
Amendment: rule it explicitly — either an unflagged declared repair (with a base-vs-tip comparison run on a corpus proven to contain ledger-without-stressor worlds), or gated with the written admission that dark worlds keep the stressor spelling. "0-when-dark" (law §2.4) does not cover a predicate swap and must not be cited for it.

**3. MAJOR — D7/SEAT-5: "restore cut power by the recorded factors" restores from records that do not exist, and the 'liberation' cause silently becomes a coup.**
Defect: nothing records the pre-cut powers or the factors. `createOccupationRecord` (occupation.js) persists `{occupierId, state, sinceTick, stateHeld, resistance, benefitYield, lastTick}` — no cut record; `installOccupationAuthority` maps `power: round(num(f.power) * OCCUPATION_GOVERNING_CUT)` in place — `Math.round` is lossy, powers drift afterward via other writers (+5 capture, +6 ascendant, shifts), and the ledger entry is deleted at liberation. Exact inversion is impossible; approximate inversion is a new judgment call the design presents as bookkeeping. A faithful restore needs a new persisted field on the occupations record — a schema change with NO owner row (only Q-S2/posture is rowed). Second receipt: `rulingPower.js:403` — `RULING_POWER_CAUSES.includes(opts.cause) ? opts.cause : 'coup'`; the frozen vocabulary is `['coup','election','succession','conquest','appointment']`. Passing `cause:'liberation'` does not error — it silently reseeds legitimacy as a COUP (base 38) and stamps `'seized_power'` on the liberated seat. The design's "else 'appointment'" hedge must live at the call site; the code's own fallback is the wrong one. Third gap: no ruling for worlds already liberated before SEAT-5 lands — the design's cure is future-tick only, so pre-existing broken rosters (foreign authority governing forever) persist; the cure-forward/no-backfill fork (THE PROMISE-adjacent — backfill edits lived history) is unstated.
Amendment: owner-row the restore record (or charter an approximate, declared restore with its formula); hard-code the `'appointment'` fallback with a typed reason; write the no-backfill law into D7 explicitly.

**4. MAJOR — D3's posture default is neither dormant nor a decision: it oscillates, and the unlight/residue path is untested by construction.**
Defect: (a) "absent ⇒ derived from the occupier's disposition" — on a LIT world before any posture candidate resolves, the derived default already modulates RESISTANCE_GROW/DECAY, benefit/burden scales, suitability. Disposition is itself dynamic (`disposition.js:497-513` — occupations-held count feeds aggressiveness), so the un-chosen "decision" can flip tick-to-tick, dragging the occupation state machine's inputs with it. The design never says when the derivation is evaluated or frozen. A decision-valued quantity that is derived-until-chosen also silently re-answers on every code change to disposition — exactly why law §2.2 demanded persistence in the first place; the default path contradicts the law's own rationale. (b) Residue: once lit, posture fields persist on occupations records. If the owner unlights, the design never states that the dark path ignores a PRESENT field — and the raw-byte instrument (base-dormant vs tip-dormant) can never construct a present-field-dark world, so this path ships unproven (the §713.3 comparing-nothing class; also the memory's write-survives-one-path-ghosts-another class). (c) No importScrub/regen ruling for the field (A1.8 precedent: the treasury key joined the strip; the occupations record's posture needs its row or an explicit exemption).
Amendment: write-once-on-first-lit-evaluation (persist the derived default as the chosen posture, receipted), or default = `administer` = identity modulation; state "dark path never reads the field, present or absent" as a tested invariant with a hand-built present-field-dark fixture; rule the import/regen lifecycle.

**5. MAJOR — one `foreignSeatEnabled` key certifies one thing and lights three: lighting "the foreign seat" changes settlements with no foreign presence anywhere.**
Defect: D4-ATTEMPT's gate rows scale by "the ruler's legitimacy band AND, per-archetype, by a small closed sensitivity table" — a purely domestic mechanic (§736) extending `coupSpawnGate`/`rebellionGate` for EVERY settlement; D5 (§741) runs on every structurally-major decision of every court. Under Q-S3's one-key REC, an owner lighting "the foreign seat" silently changes coup odds and decision routing on worlds with zero occupations and zero vassals. Three owner directives (§735.2, §736, §741) collapse to one lighting bit named for one of them; the Q-S3 row as drafted ("one key covering seat+reactions vs split keys") does not disclose the no-foreign-presence blast radius, so the owner's grant would be uninformed — against the owner-gated-classes-flagged law. Compounding: one certification row + one `mechanismLitCoverage` literal-drive credit across a six-car train means the flag's meaning widens at SEAT-2/3/4 with no re-certification; and at SEAT-1 the lit credit is vacuous (the flag gates only pure reads no consumer reads yet — a green that means less than it looks, §711.4).
Amendment: either split keys (`foreignSeatEnabled`; a sibling for the §736 upheaval scaling) or rewrite Q-S3 to state the domestic blast radius explicitly and mandate per-car certification-row amendment (the A1.19 precedent: row ships at car 1, AMENDED by each widening car).

**6. MAJOR — law §2.8's anti-snowball "re-run" is a vacuous pass as written.**
Defect: the pinned soak (`occupation.js` header: "A soak test pins this bound across a long run") pins the DARK configuration. Every W-SEAT modulation of benefit/resistance is flag-gated, so "re-runs the pinned soak bound rather than assuming it" re-proves a world the flag cannot touch — a dormancy-instrument-compares-nothing pass (§713.3), and the seed-dependence law makes even one lit run weak. The design also never states whether posture's "benefit up now" scales inside or outside the caps (`PER_OCCUPATION_BENEFIT_CAP 0.45`, `OCCUPIER_BENEFIT_CONTAINMENT 0.9`, `DIMINISHING_BASE 0.62` — "diminishing returns are applied BEFORE the cap so the cap is a hard ceiling").
Amendment: SEAT-3 owes a LIT harness arm (the A1.22 harness-not-soak shape) with posture forced at each pole plus the override-burn/resistance feedback active, and a written invariant that posture modulates `STATE_BENEFIT_SCALE` upstream of the caps so the containment bound stays structural — then pin that.

**7. MINOR — law §2.4's algebra doesn't cover its own D4-ATTEMPT mechanics.** "Every foreign term is ADDITIVE and 0-when-dark" — but gate extensions are multiplicative `mult`+`reason` ROWS. The dark form must be row-ABSENT (a 0 mult would zero the product; a 1.0-mult row still emits a reason into receipts/news). Rewrite as: identity-when-dark in each seam's own algebra — 0 for additive adjs, absent for gate rows and reasons, never a neutral row.

**8. MINOR — flag-composition honesty gaps.** (a) D3: cultivate's cost is a W-COIN sink "when lit" — in the foreignSeat-lit/treasury-dark window cultivate is materially free while exploit still yields real war-support benefit units; the dilemma the design prices is unbalanced in that window; say so or defer the cost leg's asymmetry to a table. (b) D9's rebellion/insurgency arms need army machinery (armyTransit, garrison) that lives under `warLayerEnabled`; the per-arm gate conjunction (`foreignSeatEnabled ∧ warLayerEnabled ∧ …`) is nowhere stated, and one `foreignSeatDormancy.byteIdentity` file cannot prove the composition matrix (foreignSeat lit / warLayer dark, foreignSeat lit / settlementPolitics dark). Name the conjunction per arm and the matrix cells the dormancy suite covers.

**9. MINOR — D9's dispatch decision has no named decider or entropy spec.** The interventions substrate exists (sided rows, `interventionAdjFor` 0-when-dark), but who mints a repression deployment (a candidate family? the strategy chooser — which is `settlementStrategyEnabled` default FALSE?) and its keyed-fork shape are unstated. Also: the posture candidate's `stableCandidateKey` must compose occupier AND occupied ids or two occupations by one court collide. Cheap to specify now; expensive as an implementer's improvisation.

**10. MINOR — §1's "under occupation `governingFactionOf` already answers 'the foreign power'" is only label-true today, which destabilizes D2's premise.** Verified: `transferRulingPower` (rulingPower.js:427-449) relabels the OLD local body (keeps `isGoverning`, category untouched, power = the ×0.6 cut value, now carrying both 'occupied' and the cause modifier) while the seeded `category:'occupation'` row stays `isGoverning:false`, 'ascendant'. Only A1.12's archetype stamp (sequenced first, correctly) makes the answer real — and D2's "the legitimate seat's book still computed" must define WHICH row is "the legitimate seat" when the crowned row IS the relabeled legitimate body. One sentence in D2 fixes it. (Also editorial: D9's "ride D5/D7's laws" — D7 is liberation; the intended cross-refs are D5/D8.)

ATTACKS THAT FAILED (recorded per charter):
- **D4-SUCCESS's fifth adj**: the idiom is real and byte-exact — `rulingPowerCoup.js` clamp sums caller-supplied adjs defaulting 0 (`+ (Number(interventionAdj) || 0) …`), IEEE x+0 is bit-identity for the clamped range, four live 0-when-dark precedents quoted in the code itself. A fifth term is mechanically sound.
- **Posture-choice entropy**: routing through the candidate machinery inherits per-candidate keyed forks (`roll:<stableCandidateKey>`, candidateEvents.js:646-649) — no ambient draws, no stream displacement of sibling families. Keyed-PRNG law satisfied by construction.
- **D5 forecast entropy/determinism**: a pure derivation over a closed vocabulary from beliefMaps entries; the fallback (marker absent/omniscient ⇒ ground truth verbatim, beliefMap.js:22-27) is deterministic and fork-free. No draws needed; none proposed.
- **D2 foreign book moving dark bytes**: `readWarSeatBooks` is a pure read; weights are computed per call and the foreign book can be skipped wholesale when dark — consumers see byte-identical inputs. (The lit-path normalization question is real but belongs to another lens, and §7 already lists it.)
- **The one-key flag as persistence hazard**: refuted — `ENGINE_GATED_VIRTUAL_RULE_KEYS` is census-only ("declaring a key here moves no world byte in any campaign"), strict `=== true` reads; the flag machinery itself is dormancy-clean.
- **D1 derived-seat persistence**: `hegemonyRead`'s zero-persisted-state template is real and the resolver as specified persists nothing; law §2.2 holds for D1.

VERDICT: **NOT-REFUTED** as an architecture — the derived-seat core, the additive-adj integration, and the candidate-machinery entropy story survive attack. But findings 1 and 2 are charter-level contradictions with the design's own raw-byte bars (both instances of migrations smuggled under dormancy claims), and the design must fold them plus 3-6 as amendments before any implementation car dispatches.

---

# W-SEAT PANEL LENS 2 — THE PROMISE (verbatim)

All verification loops are closed. Final receipts confirmed: intervention columns commit instantly with no transit; candidate patrons are relationship-neighbors only; `transferRulingPower` appends (never overwrites) modifiers and coerces unknown causes to `'coup'`; generation writes `'occupied'` (governing only) with unmarked military/civic cuts.

---

# LENS 2 — THE PROMISE, BELIEF, AND FABRICATED HISTORY

All paths into the extract at `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours` (mirrors 73f5dfc02). Design refs into `docs/DESIGN_W_SEAT.md` @ e6e220edf.

## FINDINGS

**1. BLOCKER — §3-D5. The "believed picture" the forecast is computed from does not exist, and its absence-case is neither of the two cases the design names.** D5: reactions are "computed from the DECIDER'S believed picture of that power (beliefMaps entry; dark/omniscient ⇒ ground truth verbatim — zero forks, the beliefMap fallback law)." Receipts: the belief record is `{ readiness, strengthBand, allianceLabel, faithLabel, confidence01, lastUpdateTick }` (`beliefMap.js:10-12`), extended only by axes `populationTrendBand`/`observanceLabel` plus believed scarcity/conditions/devotion (`beliefAxes.js:1-30`). **No believed disposition, treaty stance, legitimacy, exhaustion, or interest structure exists** — the inputs a `{approves, indifferent, protests, sanctions, casus_grade}` reaction is a function of. `casus_grade` requires the other court's warReason appetite; none of the six believed quantities can yield it. Implementation must therefore read TRUE disposition/treaty state under lit beliefs (a K3 violation of the design's own law §2.5/A6.2: "the foreign court's direction and reactions read BELIEVED") or return vacuous output. Second half: under LIT beliefs with no entry, the read is the synthetic neutral — "MAX-UNCERTAINTY reads (marker present, no belief record) — mid strength, low readiness" (`NEUTRAL_STRENGTH_BAND: 2, NEUTRAL_READINESS: 0.25`, `beliefMap.js:147-152`) — not ground truth and not a belief; and entries materialize only when "a rumor actually reached the observer" from the cold-start relationship neighbourhood (`beliefMap.js:30-36`). D5's bounded set explicitly includes "the foreign seat's patron" — typically NOT a relationship neighbour of the decider, so the §741 mandate's key case reads permanent synthetic neutral, forever. AMENDMENT: rule per-input structure-vs-state (treaty ties and the vassal contract read TRUE per the A3 precedent, `settlementStrategy.js:388-390`; strength/readiness read believed via existing bands); restrict the reaction vocabulary to what those inputs can compute; name the entry-absent default explicitly (abstain ⇒ `indifferent` + low-confidence discount) and accept in the charter that the forecast is structurally weakest about distant patrons — or charter the new believed axes + carriers it needs, priced.

**2. BLOCKER — §3-D7/SEAT-5. "Restore cut power by the recorded factors" — no factors and no pre-cut powers are recorded anywhere; restoration fabricates pre-conquest numbers by construction.** Receipts: `createOccupationRecord` persists `{ occupierId, state, sinceTick, stateHeld, resistance, benefitYield, lastTick }` — no roster snapshot (`occupation.js:228-238` per V3). `installOccupationAuthority` writes `power: round(power × factor)` in place; the factors are module CONSTANTS (`OCCUPATION_DISARM = 0.3` etc., `applyWorldPulseOccupationAuthority.js:23-25`), recorded on no row. Four independent fabrication vectors: (a) `round()` is lossy and zero-absorbing — a power-1 militia becomes `round(0.3)=0`, and 0 ÷ 0.3 = 0 forever; (b) interim writers move cut power for the whole occupation (coup ascendant +6 `rulingPower.js:444-446`, faction_power_shift ±8, capture +5), so inverse-multiplying the CURRENT power attributes tick-drift to the pre-conquest baseline — the honest counterfactual is genuinely unrecoverable; (c) **one modifier marks two different factors**: the governing cut ×0.6 and the civic cut ×0.82 both write `'occupied'` (`applyWorldPulseOccupationAuthority.js:56-63`) — after the conquest-crown relabels the seat row (modifiers APPENDED, `rulingPower.js:435`), nothing distinguishes which inverse applies; (d) **generation-occupied rosters carry the same marker for cuts the sim never made**: `stressFactions.js:27-36` (generation) writes `'occupied'` + ×0.6 on the governing row and cuts military ×0.3 / civics ×0.82 with NO modifier — a later pulse conquest's idempotency guard skips the governing row but double-cuts the unmarked military (net ×0.09), and liberation's inverse restores one cut of two while presenting the roster as restored; the generation-minted `Occupation Authority` (power 20) and `Resistance Network` rows (`stressFactions.js:43-53`) survive SEAT-5's un-install entirely, since only the row named for the pulse occupier is removed. AMENDMENT: persist the pre-cut powers at install time (a per-faction snapshot on the occupations record — new persisted bytes, owner-rowed; Q-S2 covers only posture, so this needs its OWN row); for occupations already lived, declare the restore an ESTIMATE and make the receipt say so (news-address law: never "power restored to its former standing"); enumerate the modifier→factor table and the generation-marker collision explicitly in the SEAT-5 charter.

**3. MAJOR — §3-D7. The liberation cause fallback writes a false cause into immutable persisted history.** `RULING_POWER_CAUSES = ['coup','election','succession','conquest','appointment']` frozen (`rulingPower.js:335`); an unknown cause is silently COERCED to `'coup'` (`:403`) and appends `'seized_power'` to the seat (`:435`) — a naive `cause:'liberation'` call records the liberation as a coup, with coup legitimacy seeds and coup prose, in `previousGovernments`. The design's sanctioned fallback ("else 'appointment' with a typed reason") is itself a misrecording: `previousGovernments` rows are `{label, cause, tick}` (V2, `rulingPower.js:500-503`) — **there is no reason slot**, so the "typed reason" has no persisted home and the permanent record reads as a plain appointment, with appointment's `STABILITY_BY_CAUSE` prose and seed. Lived history immutable means this wrong row can never be cured. AMENDMENT: the vocabulary-extension owner row is effectively mandatory for SEAT-5, not optional — present it as such; and pin a test that `transferRulingPower` is never reachable with an out-of-vocabulary cause (the coercion at `:403` is a silent-fabrication landmine independent of W-SEAT).

**4. MAJOR — §3-D3/§2.2. The derived posture default fabricates an un-chosen policy and can silently drift, making law §2.7 unsatisfiable.** "Absent ⇒ derived from the occupier's disposition" — but disposition is a live per-tick derivation that itself reads occupations-held count, legitimacy, and war state (`disposition.js:497-513` per V3), so the derived default FLIPS as the occupier's circumstances drift. Every flip re-modulates RESISTANCE_GROW/DECAY, benefit/burden, §736 rebellion risk, and W-COIN-3's extraction fraction. Either these flips mint no receipt — violating §2.7 ("every … posture change … lands as a typed, cause-named news entry"; there is no recorded cause to name, and finite semantics forbids the clerk inventing one) — or they mint receipts narrating a "choice" no court made. For a years-old occupation the default additionally projects TODAY'S disposition backward over a rung/resistance history accrued under no posture at all (an extractive-rung occupation under a "cultivate"-reading disposition asserts two contradictory pasts at once, and W-COIN-3 prices coin off the fabricated one). Unstated cross-flag coupling: W-COIN-3 lit + `foreignSeatEnabled` dark ⇒ extraction reads a posture band from a system that officially does not exist. AMENDMENT: pin the pre-decision band to a constant (`administer`), not a derivation; require the occupier court's first posture DECISION (receipted, through the candidate machinery) before any consumer reads a non-default band on a pre-existing occupation; state the posture-read's flag topology against Q10/Q-S3.

**5. MAJOR — §3-D9. The "physical army act (armyTransit, upkeep…)" claim contradicts two recorded, vetoable in-tree judgments and assumes eligibility/timing that don't exist.** Receipts, all `convergence.js`: interventions ride an ISOLATED ledger "NOT the shared `deployments` ledger … sidesteps warDeployment's one-army law + siege-verdict machinery entirely" (:42-49); columns have no position and no transit — "intervention columns ride the isolated ledger, not the transit ledger, so nothing to intercept spatially in wave 1 (documented deferral)" (:790-795); they commit INSTANTLY (`sinceTick: nowTick`, :1167) with immediate verdict effect (`interventionAdjFor`), so a distant overlord represses the tick a rising spawns — no march-time, no hop-priced awareness; candidate interveners are relationship NEIGHBOURS only (`neighborsOf(edges, targetId)`, :1069) — an occupier with no edge is ineligible; and initiation reads LIVE TRUE state by recorded judgment: "war-gated, not belief-gated … The loaded-dice initiation is deterministic on live state, not on believed fog. Say 'veto' to AND-in beliefsActive" (:28-31) — directly contradicting D9's "the marching court's belief picture ride[s] D5/D7's laws" and the A3 info-starvation law (the lord who never hears never marches). The `ORDER_INTERVENTION` verb is additionally unregistered (deferred to W-COMPOSER-2, :52-60). Uninvited entry also leaves no settlement-side presence state (no garrison/occupation trace on a free settlement) to hang the promised sovereignty-violation consequences on. AMENDMENT: SEAT-2's charter must either (a) explicitly reverse/complete the two recorded judgments (transit integration + belief-AND-gating — both flagged owner-visible, both priced), or (b) strike the "physical army act (armyTransit…)" language and ship abstract conserved columns with a march-delay term and an edge-or-seat eligibility rule; the presence/consequence terms for uninvited repression must be chartered as NEW derived machinery, not assumed.

**6. MAJOR — §3-D4/§2.1. The coupSpawnGate predicate move is an unconditional dark-path behavior change inside a raw-byte-barred car, over a divergence the census flagged and the design never resolved.** Today the gate nulls on the STRESSOR spelling only: "`if (activeTypesAt(snapshot, sid).has('occupation')) return null;` // the occupier IS the authority" (`stressorGates.js:749`). The two spellings provably diverge in at least one direction (generation-occupied: stress transform with no ledger entry and no occupier id — `stressFactions.js:26-42`, V3 §5) and V2 explicitly flagged the other as unverified ("a pulse-conquered town carries the occupations ledger … I did not verify a matching stressor exists there" — V2 insertion-point 1). Moving the null to the ledger predicate is not behind `foreignSeatEnabled` in the design text, and the coup gates run on shipped-lit paths — any nonzero delta breaks SEAT-2's raw-byte bar; if instead it rides the flag, the "one occupied predicate" law is false while dark and the design must say so. AMENDMENT: measure the two-spelling delta over the corpus first; then either flag-gate the move (recording the two-predicates-while-dark state) or land it as a DECLARED unconditional shift with its own receipt — the charter currently claims both raw-byte and the move, which is self-contradictory.

**7. MINOR — §3-D1/D8. Nothing can date a seat's beginning, and the D1/D2 lighting shift is undeclared while SEAT-5's is.** `ForeignSeatView { regime, patronSettlementId, weight01, band, postureBand, basis }` carries no inception field; D8's five kinds include no seat-standing/first-light kind. With Q-S3's REC "dark at launch," every real world meets its foreign seats mid-story: D2's primacy axis starts vetoing an in-flight war's mobilization with no receipt that a seat now looms, and readers meet "the counterpart seat" with no beginning. The dating is derivable (occupations `sinceTick`; edge history; treaty `sinceTick`) — the design just never routes it into receipts. AMENDMENT: a seat-standing receipt at first exercised influence (carrying the substrate's since-tick), and a declared-shift line in Q-S3's row for lighting on lived worlds, matching the declaration SEAT-5 already carries.

**8. MINOR — §3-D5. "The EXISTING misjudgment receipt machinery narrates it" overstates what exists.** The detector triggers on a strength-band error ≥ `MISJUDGE_BAND_DELTA: 2` or a stale-hostile relationship label only (`beliefMap.js:147-160` tuning; :1483-1594 per V6). A wrong REACTION forecast (predicted `indifferent`, true answer `casus_grade`) matches neither trigger. The receipt SHAPE is reusable; the detection is new work SEAT-4 must price.

## ATTACKS THAT FAILED

- **"Mid-campaign lighting retroactively rewrites who governed."** FAILED: the seat is a pure read; the occupier's roster crown predates W-SEAT (`installOccupationAuthority` + `transferRulingPower` are live today); lighting changes future-tick routing only; no persisted history is touched. The residue is finding 7 (undated/undeclared), not retroactive mutation.
- **"Invited vs uninvited repression is unrepresentable."** FAILED: `invited: true iff an incumbent-prop the seat requested` is a typed field on `InterventionRecord` (`convergence.js:485-495`). The semantic split D9 needs exists at the record level.
- **"Persisting posture violates derived-never-persisted."** FAILED: law §2.2 carves the exception explicitly, owner-rows it (Q-S2), and decision-as-state has the `previousGovernments` precedent.
- **"Generation-occupied settlements get a seat with a fabricated patron."** FAILED: D1's regime-1 predicate is the occupations ledger, and the generation `occupied` stress "carries no occupier id at all" (V3 §5) — `foreignSeatOf` returns null rather than inventing a patron. The residual prose/mechanics split (outward-revenue prose with no seat) predates W-SEAT and is W-COIN A1.14's assigned seam.
- **"anticipatedReactions violates K4 by merging pictures."** FAILED as stated: D5 consumes per-relevant-power reactions as bounded score terms inside ONE decider's own arena; each derives from that decider's own picture; no cross-observer blend occurs. (The defect is finding 1 — the picture's substance — not K4.)
- **"D2's occupied 'legitimate seat book' has no substrate because conquest crowned the occupier."** FAILED on close read: the local governing row (however relabeled — modifiers are appended, never overwritten, `rulingPower.js:435`) supplies the seat book, and the foreign book substitutes the occupier COURT's biases exactly as the covert patron book does (`warSeatBooks.js:528-556` per V1). Only the row's label misleads; both books have substrates.

## VERDICT

**NOT-REFUTED, with two BLOCKER amendments mandatory before seal.** The composition skeleton (derived seat, primacy-as-ordering, additive-0-when-dark terms, the four live precedents) survives this lens. But D5's believed-picture claim is unimplementable against the actual belief substrate, and SEAT-5's restoration clause invokes records that do not exist — as written, both would ship fabricated state or fabricated narration on lived worlds, which is precisely the class THE PROMISE freezes. Findings 3–6 are charter-level amendments; 7–8 are wording/pricing corrections.

---

# W-SEAT PANEL LENS 3 — SECOND-TRUTH (verbatim)

All verification complete. Composing the lens report.

---

LENS 3 — SECOND-TRUTH AND THE RESOLVER LAWS. Code verified against the extract at `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours` (mirrors 73f5dfc02). All receipts below re-read by this lane, not inherited from the census.

**FINDING 1 — BLOCKER — D1 §3 + D3 + law §2.1/A1.14: the precedence is total but the substrates PERMANENTLY disagree at the vassalized rung, and D1 resolves the disagreement backwards.** Vassalization is not an exit from the occupations ledger: `vassalizationOutcomes` (occupation.js:724-780, read in full) only emits a `relationship_label_change` to 'vassal' — the record stays at rung `vassalized` forever (exits are liberation/collapse/canon-vanish only; the sale machinery *requires* it to persist — `sovereigntyAssets.js:219-224` "only `vassalized` is tradeable", `conveyOccupationRecord` rewrites `occupierId` in place). So every ladder-born vassal simultaneously holds an occupations entry AND a vassal edge to the same patron, and D1's order — "(1) occupations ledger (regime `occupation`…)" — classifies it regime `occupation` with grip = BENEFIT_SCALE[vassalized]=1.0 × (1−resistance) FOREVER. Consequences: (a) D3's "the honest road to `vassalized` (occupation maturing into the cheaper regime)" is false under D1 — the regime never becomes `vassalage`, the occupier keeps the D2 primacy override class permanently, and the owner's §735.2 vassalage description (influence "may be lower than an occupier's… remains influential", competing honestly per law §2.3) never obtains for any ladder-born vassal; cultivate is actually the road to permanent maximal primacy at burden 0.18. (b) The shared occupied predicate ("presence in `worldState.occupations`", law §2.1, "shared verbatim with W-COIN A1.14") makes A1.14's occupied⇒zero-own-tax apply to matured vassals forever, while a genesis/DM vassal (edge only, no ledger) taxes normally — two vassals of identical prose standing with opposite economies, decided by substrate history: the exact second-truth class the law was minted against. AMENDMENT: rule the vassalized rung explicitly — either the occupations record at rung `vassalized` resolves regime `vassalage` (grip handed to the edge-derived blend; occupied-predicate qualified to rung < vassalized for both W-SEAT primacy and A1.14 taxation, in BOTH charters), or vassalization becomes a ledger exit with the sale-machinery collision resolved — and either way the §7-invited "occupation→vassalized mid-decision" boundary gets a written answer. Owner-adjacent (it moves future-tick behavior of lived vassalized worlds when lit).

**FINDING 2 — BLOCKER — §1 / D2: the "legitimate seat" D2 promises to keep computing does not exist in the substrate, and §1's crowning claim is false at the row level.** §1 states "`transferRulingPower` crowns it — under occupation `governingFactionOf` already answers 'the foreign power.'" The actual code (rulingPower.js:403-455, read in full): the winner NEVER becomes `isGoverning` — the OLD governing row keeps the seat, renamed to the occupier-archetype label ('Occupation Authority', GOVERNMENT_LABELS at :147), keeping its old `category` (the rewrite overwrites only faction/name/desc/isGoverning/modifiers/legitimacyCrisis/crisisNote) and its cut power (×0.6 + 'occupied'), while the seeded occupier row (power 90, `category:'occupation'`) gets +6 → 96 and 'ascendant'. So `governingFactionOf`+`factionArchetype` (the A1.12 pair) returns the puppet-labeled OLD row with the OLD archetype (category wins, factionArchetypes:110-111) until A1.12's archetype stamp lands — and the entity the owner mandated the occupier be "always just greater" than — the legitimate ruler, competing in coalitions — has NO substrate representation at all: its row was consumed into the occupation administration; only `previousGovernments` remembers the label. D2's "the foreign book takes the senior share with the legitimate seat's book still computed (competition is visible and narratable)" therefore computes the occupier's-administration book and calls it the legitimate seat's: occupier vs occupier, the competition unrepresentable. D7 later says liberation seats "the strongest legitimate faction" — the only occurrence of "legitimate faction", with no resolver named anywhere. AMENDMENT: SEAT-1 must mint the missing resolver (`legitimateRemnantOf`: e.g. strongest non-occupier, non-'occupier'-modifier faction, codepoint tiebreak — one exported law, used by D2's competing book, D4's aftermath, AND D7's liberation seating), and §1's crowning sentence is corrected in the volume (the receipts layer already contradicts it: V5(b) has the true shape).

**FINDING 3 — MAJOR — D2/D6: the occupier's influence keeps THREE live spellings and D6 amplifies rather than reconciles.** After conquest the occupier row (power 96) is: (i) a legal coup challenger — `coupContenders` excludes only CRIMINAL (rulingPowerCoup.js:105-107) and `COUP_COERCION[A.OCCUPATION]=1.0` exists (:43) — closed under occupation only by D4's predicate move; (ii) an ungated `governmentChallenge` contender — grep over factionCompetition.js finds NO occupation gate (only the fold comment at :26), it is top-1 by rawPower, and conquest reseeds legitimacy to 18-40 so the challenge lane is open: the occupier's row can propose to overthrow its own administration; (iii) a favored bloc partner of the puppet seat — `transferRulingPower` converts any seat↔winner edge to `symbiotic` (:462-489, read), and warm leader ties multiply bloc affinity ×1.4, so the ruling bloc {puppet, occupier} loading `blocDecisionFactor` ±0.30 is the LIKELY equilibrium. Stack (iii) + D2's foreignBook senior share + D2's authorityFor override in one decision and the occupier's influence is spelled three times, each individually bounded, jointly unpriced — exactly the class law §2.1 forbids. D6 routes the row's END vocabulary to patron but touches neither contest eligibility nor the bloc/book double-count. AMENDMENT: SEAT-2's charter must rule the overlap — the occupier row is excluded from coupContenders/governmentChallenge under an occupations entry (a typed ineligibility, like criminal), and either the foreignBook subsumes the row's bloc contribution or the row is barred from ruling-bloc membership; the chosen rule gets a test in the seat-books home.

**FINDING 4 — MAJOR — law §2.1's legitimacy proof plan: "migrates the three raw readers it touches" is an undercount, and the migration itself is an undeclared behavior shift on legacy saves inside a raw-byte car.** Counted this lane: raw reads of `publicLegitimacy.{score,govMultiplier,label}` bypassing `governanceLedger` at ≥11 engine sites — stressorGates.js:746-747 (coupSpawnGate), mobilization.js:157, occupation.js:342, feasibilityGate.js:138, warSiegeVerdict.js:245, warCapacityReads.js:253, rulingPowerCoup.js:85 (govMultiplier), hookEscalation.js:500 (label), simulationSpine.js:496 (label), roads/state.js:590, upswingKernel.js:144 — with at least FOUR divergent absent/legacy semantics: default 50 (coupSpawnGate :747), 0 (upswingKernel :144 "0 when unreadable"), NaN (commonsVoiceKernel :71), and the wrapper's honor-legacy-bare-number (governanceLedger.js:63-66, read). The fork is live inside ONE file: rebellionGate/insurgencyGate/politicalFractureGate use `governanceLedger` (stressorGates.js:378,539,580) while coupSpawnGate reads raw (:746) — so D4's new attempt rows inherit a different legitimacy than their sibling gates on any legacy-bare save. Migrating coupSpawnGate to the wrapper changes behavior on legacy saves (bare score 20: today treated as 50 → coups off; migrated → 20 < 45 → coups on) — unflagged plumbing in SEAT-1 whose bar is raw-byte, passing vacuously if the dormancy corpus holds no legacy-bare save (§713 comparing-nothing hazard). And the stale-band second truth survives the plan entirely: 11 score-only nudgers never re-band (V2 writers 4-14), so `rulingPowerCoup.js:85`'s stored `govMultiplier` diverges from the live score — D4's SUCCESS moment "rides beside the already-live govMultiplier amplification", i.e. one coup verdict reads two legitimacy truths. AMENDMENT: SEAT-1's proof act enumerates the FULL raw-reader census (this list is the seed), declares which migrate (each with the legacy-save behavior delta named, fenced by a legacy-bare fixture added to the dormancy corpus) and which are documented-deferred; D4 states whether its band is derived-from-score or stored, and the govMultiplier staleness gets at minimum a documented-second-truth row.

**FINDING 5 — MAJOR — D4/D9 build a self-amplifying back-edge into D1's weight, and §2.8's re-prove duty does not cover it.** The loop, every leg quoted from the design: D4 AFTERMATH "a coup under a dominant foreign seat seats a client, warmer to the patron" → warmth/closeness raise D1's scalers (`cultureAffinity`'s governance-identity term + the `stableVassalage` blend: trust up, strain down) → `weight01` up → D4 SUCCESS "`foreignSeatAdj` … the seat backs or resists a challenger" backs clients harder → more client seatings. Parallel leg: D9 INVITED repression "is legitimacy-neutral for the ruler and deepens dependency" — dependency is a D1 grip input (V4 sketch, 0.25·dependency), and the quash precedent raises fear +0.08 (relationshipRulesCore.js:801-833) whose formula-mate mutes resentment's only discount. Every term is clamped, so it saturates rather than diverges — but it is a monotone ratchet toward maximal foreign weight with no named counterforce inside the resolver, and law §2.8's anti-snowball re-prove covers ONLY "occupation benefit/resistance", so the vassal-side weight loop escapes every named bound. AMENDMENT: extend §2.8 to any D1 input the seat's own decision arms move (closeness, stability, dependency, fear); SEAT-4/SEAT-2 add a soak assertion on weight-band trajectory (e.g. a dominant band must be losable), and D4's client-seating warmth is bounded or priced.

**FINDING 6 — MAJOR — D1 is total across substrates but NOT single-valued across patrons, and the multi-edge pick is unspecified.** Nothing clears a third party's vassal edge on conquest (conquest writes roster + condition + occupations + transfer only — applyWorldPulse.js:160-197 read; "any transition away clears stamps" fires only on that edge's own transition), so `occupations[S]={occupierId:F1}` can coexist with a live vassal edge from F2 and treaty ties owed F3. `foreignSeatOf` returns ONE `ForeignSeatView` with one `patronSettlementId`: F2's and F3's seats are silently erased with no ruling and no receipt — defensible physics, but currently unruled structure, and §741 promised "each relevant foreign power — or its counterpart seat." Within substrate 2 alone, two vassal edges to different seniors (genesis + DM authoring; nothing structurally forbids it) leave the pick unspecified — a determinism hole. AMENDMENT: either the return becomes a codepoint-ordered list with a designated primary, or the single-view ruling is written ("occupation extinguishes rival seats; ties/edges of other patrons surface only through D5 reactions") plus a codepoint tiebreak for same-substrate multiplicity — named in the SEAT-1 charter, tested.

**FINDING 7 — MAJOR — law §2.1's "sibling export in rulingPower.js" violates the module's own eager/lazy contract.** rulingPower.js is first-paint eager with deliberately minimal imports (header lines 18-21, read: the coup contest model "is split out to keep it off the first-paint closure"; enforced by tests/build/vendorPdfLazy.test.js per rulingPowerCoup.js:17). `foreignSeatOf` must read occupations, relationshipStates/roles, treaty orientation, channel strengths, and `buildCultureVector` (migrationKernel.js) — all lazy worldPulse leaves; homing it in rulingPower.js drags them into the first-paint closure, the same class the WEAVE panel struck D11 for (A1.1.4, "the exact eager re-export edge … ~2KB closure headroom"). AMENDMENT: home it in a lazy sibling leaf (`rulingPowerSeat.js`, the rulingPowerCoup.js precedent) — the one-home intent survives as one *family*, and the law's sentence is corrected.

**FINDING 8 — MINOR — D1's grip reuses BENEFIT_SCALE as a control unit (§711.6 class).** "Grip from rung × (1−resistance)" is grounded (V4 sketch) in `BENEFIT_SCALE[rec.state]` — an economic yield fraction (war-support units, occupation.js:127-148) acquiring a second unit (control grip) at a second consumer; a W-COIN-3-era tuning of benefit scales silently moves seat weights. AMENDMENT: a named GRIP_SCALE table (may start numerically identical) or a declared shared-unit comment at both consumers.

**FINDING 9 — MINOR — D1's treaty-tie clause is internally ambiguous.** "Treaty subordinating ties ADD a compliance-scaled FLOOR" (add ≠ floor), while "a subordinating tie alone yields at most `present`" — so tie-alone is capped but tie+edge is (apparently) uncapped additive: the resolver is discontinuous at the edge-existence boundary as edge numerics → 0. AMENDMENT: pin the composition (max-with-floor vs additive) and the cap's scope in the SEAT-1 charter.

**ATTACKS THAT FAILED (recorded):**
- **hegemonyRead as a second answer to "is S under F?"** — FAILED as a one-resolver violation. hegemony.js (read :1-100) answers a realm-pattern quantity (a center with ≥3 subordinate ties; consumers are display + `fear_of_dominance` via believed strength), not seat-in-settlement; D1 *consumes* `treatyOrientationOf`/`rankCompliance` rather than forking them. Residual seam folded into Finding 9.
- **Same-tick feedback circularity in D1/D5** — FAILED. The seat read is pure over pre-tick state; relationship numerics move only through `applyRelationshipPatch` next composition; no cycle, deterministic. (The cross-TICK ratchet is Finding 5 — a different defect.)
- **Keyed-PRNG attack on the resolvers** — FAILED. D1/D5 are pure derivations, zero draws; nothing to key.
- **The override-burn loop** — FAILED as an amplifier: the resistance bump LOWERS grip (1−resistance) and thus weight — genuinely self-damping, as designed.
- **The trade01 scaler loop** — too weak to sustain: bounded at ×0.2, and channel strength moves through discovery/confirmation machinery, not directly by seat decisions.
- **Precedence totality for a single patron** — HOLDS: order (1)>(2)>(3) is total and deterministic when each substrate names one patron; the defect is only multiplicity (Finding 6) and the vassalized-rung semantics (Finding 1).

**VERDICT: NOT-REFUTED, with mandatory amendments.** The architecture (derived seat, primacy-vs-weight split, extend-never-fork) survives this lens; but Findings 1 and 2 are seal-blocking as written — the design's occupied predicate and its "legitimate seat" both dissolve on contact with the quoted substrate, and both sit on the shared W-COIN seam, so their amendments must land in BOTH charters before SEAT-1 dispatches.

---

# W-SEAT PANEL LENS 4 — ESTATE (verbatim)

LENS 4 — ESTATE ECONOMICS AND INTEGRATION HONESTY. All receipts read at the extract mirroring 73f5dfc02 (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours`) or via `git show 73f5dfc02:` for tests; design/verification at e6e220edf.

**FINDINGS (most severe first)**

1. **BLOCKER — §3-D2. "authorityFor gains the PRIMACY AXIS … one edit at the choke serving all ~30 families" is unimplementable as specified.** `authorityFor(rules, changeType, legacyMode)` (changeAuthorityPolicy.js:366-380) receives ONLY simulation rules — no worldState, no settlement id — so it cannot know whether the deciding settlement is occupied or who occupies it. The docstring even says changeType was added "so (future) per-domain authority overrides slot in here without another call-site sweep" — but a per-SETTLEMENT override is a different axis and forces exactly that sweep: either the signature grows and all 13 sites thread context, or the axis lands at the candidateEvents.js:620 choke (which has candidate context) — and then the design's own first override-class member, war initiation, never sees it, because its authority is a DIRECT site: `const warInitMode = authorityFor(rules, 'strategy_deploy', 'auto')` (warDeployment.js:901), outside the :620 routing. Also "resolve to the occupier's side or are vetoed" is not expressible in the choke's contract at all — authorityFor returns an applyMode string, not a verdict; veto/forced-side is a different return type. And "mobilization posture" is a deterministic classifier that never calls authorityFor (occupier force-cool already exists via `OCCUPIER_CONTROL_STATES`, mobilization.js:322-332). AMENDMENT: respecify D2 as (a) the :620 choke edit for stochastic families (context available), (b) named per-site edits for the direct-site majors on the override list (warDeployment:901, coup.js:195 at minimum), (c) a typed override/veto contract distinct from applyMode — and re-class SEAT-2's effort accordingly.

2. **MAJOR — §3-D2. "One read; nine existing consumers … inherit the seat for free" is false, and the failure mode is silent.** The three weights are a partition of unity with a hard XOR: `seatWeight01 = patronId ? 0 : privateWeight; patronWeight01 = patronId ? privateWeight : 0` (warSeatBooks.js:528-530). Consumers fold them explicitly: warTermination's `blendBooksTerm` computes `privateWeight = seatWeight01 + patronWeight01` and normalizes by `realmWeight + privateWeight` — its comment says "without adding a fifth term" (warTermination.js:380-390); sovereigntyIntent sums the same two fields (:263). A fourth `foreignWeight01` is silently DROPPED by every such consumer (the normalization hides the missing mass; nothing reds). So each of the nine consumers needs an edit, or the foreign book must ride the existing private-book slot — but that slot's XOR cannot host a covert patron AND an overt occupier simultaneously (an occupied settlement whose ruler is also privately compromised is a legal state the design never rules on). The unseated early-return branch (`settlementWeight01: 1`, warSeatBooks.js:469-486) is also unaddressed — where does the occupier's book attach when no ladder ruler resolves? AMENDMENT: choose and state the shape (generalized `books[]` vs fourth weight with enumerated consumer edits), rule the patron+occupier collision, rule the unseated branch, and price the nine consumer edits into SEAT-2.

3. **MAJOR — §4/§3-D9. SEAT-2's M-L class hides three cars, and D9's fold invalidates the collision lane's verdict.** SEAT-2 as written carries: the seat-books generalization (+9 consumer edits per finding 2), the primacy axis (+13-site sweep per finding 1), chooser terms, the coup fifth adj at two call sites, D4's three moments across four gates plus `independencePressure`, a new per-archetype sensitivity table (tuning-docket constants), the aftermath reseed scaling (the second edit to the two-car file rulingPower.js), override burn prices (a 15th legitimacy writer plus a resistance writer), AND D9's generalization of the interventions channel to rebellions and insurgencies — armyTransit physical acts, upkeep, a W-COIN sink, invited/uninvited semantics, failure receipts. That last item touches deployment/war-cost surfaces that are W-COIN-2's content; V7's collision table ruled "W-COIN-2 × W-SEAT: near-zero file overlap" BEFORE D9 existed (it came from the owner in-sitting), so §5's "interleavable with W-COIN-2's window" rests on a stale verdict. The sibling program's own A1.24 ("Car split honesty: W-COIN-1 splits into 1a and 1b") sets the precedent over a smaller surface. AMENDMENT: split SEAT-2 into (2a) decision-spine integration, (2b) the §736 three moments + sensitivity table + burn prices, (2c) D9 repression generalization — and re-run the W-COIN-2 collision check for 2c, naming its coin sink against A1.5 (deployment-mint-cost is assigned to W-COIN-2) or A1.9's closed six-flow roster (a seventh flow amends a sealed enumeration, owner-visible).

4. **MAJOR — §3-D4's last sentence is an undeclared lit-behavior shift inside a raw-byte car.** "The coupSpawnGate's occupation-null moves from the stressor spelling to THE occupied predicate." Today the null is `if (activeTypesAt(snapshot, sid).has('occupation')) return null` (stressorGates.js:749) — a STRESSOR read; the occupations LEDGER is independent (occupation stressors have their own birth gate, stressorGates.js:351, and pulse conquest seeds the ledger without one). Post-conquest legitimacy reseeds to 18-40 (< the 45 floor), so a pulse-conquered town with ledger-but-no-stressor can spawn coups TODAY; after the move it cannot — and a stressor-without-ledger town flips the other way. Both are shipped-lit paths (V7: "W-SEAT's coup/rebellion arms sit on the SHIPPED-LIT stressor path"); SEAT-2's bar is raw-byte and the only declared shift in the volume is SEAT-5's. AMENDMENT: either gate the predicate move behind `foreignSeatEnabled` (accepting the fork persists when dark, stated) or declare it a lit shift with its own fence — the current text violates the design's own bar.

5. **MAJOR — §3-D7/SEAT-5: `cause:'liberation'` is silently coerced to 'coup', minting false history.** `transferRulingPower` line 403: `RULING_POWER_CAUSES.includes(opts.cause) ? opts.cause : 'coup'` — and `RULING_POWER_CAUSES = ['coup','election','succession','conquest','appointment']` (rulingPower.js:335). The design's primary spelling would not error: it would reseed legitimacy on COUP semantics (25-55), write a `previousGovernments` row saying 'coup', and stamp "Unsettled — power changed hands by force" — a liberated town recorded as self-couped, the cure-history failure class. The 'appointment' fallback avoids it but ships "an appointed authority is establishing itself" prose for a liberation. This also contradicts §2.6's "the design needs none" (no vocabulary extension) — D7's honest primary path needs exactly that extension, which §6 does list as a chair-drafted row; the two sections should agree. AMENDMENT: strike 'liberation' as the default spelling or promote the vocabulary extension to a real owner row with a LEGITIMACY_SEEDS row and STABILITY_BY_CAUSE prose; never pass an unlisted cause.

6. **MAJOR — §3-D7 "restore cut power by the recorded factors": nothing is recorded, and inversion is lossy.** `installOccupationAuthority` (applyWorldPulseOccupationAuthority.js:42-87) stores NO pre-cut power — it applies `round(power × 0.3/0.6/0.82)` in place. Inverse-multiplying at liberation is wrong three ways: `Math.round` destroys information (a militia at power 1 → round(0.3) = 0, and 0 restores to 0 forever); sim-time writers move power between conquest and liberation (coup +6, capture +5, shift 8, thieves floor — none renormalize); and a re-fired conquest's idempotent skip means the factors may have applied at an unknown tick. An honest restore needs a persisted pre-cut snapshot at install time — a NEW schema field the design's zero-new-persisted-bytes accounting (Q-S2 covers only posture) never prices, and schema shape is owner-gated. AMENDMENT: either add the pre-cut snapshot to the occupations record under a widened Q-S2, or respecify D7 as an approximate restoration (declared as such) that seats and re-powers by rule rather than by memory.

7. **MAJOR — §3-D2's override burn can PACIFY the town it claims to lose.** The design: "a typed legitimacy price on the occupier's standing + a resistance bump — winning every argument loses the town." Receipts: `resistanceTarget` includes `+0.3·clamp01(publicLegitimacy.score/100)` read LIVE (occupation.js resistanceTarget — the comment says "pre-occupation order" but the code reads the current score, which post-conquest IS the crowned occupier's standing); and resistance is a ratchet pulled toward that target at grow 0.18 / decay 0.14 per tick (occupation.js:182-189), so any one-shot "resistance bump" above target decays away in a few ticks. Net: if the burn debits the occupied settlement's publicLegitimacy, it LOWERS the resistance target → resistance decays → `stabilizationSuitability` rises → the ladder advances → occupation stabilizes faster. The two legs fight, and the design never even says whose legitimacy the burn debits (the occupier's home settlement vs the occupied roster's score — different mechanisms entirely). AMENDMENT: specify the debit target; make the durable cost land on a resistance-target INPUT or a typed scar, not a transient bump; and add this loop to §2.8's soak re-run explicitly (§7 asked for exactly this bound — here is the mechanism that breaks it).

8. **MAJOR — SEAT-1's "duplicate `vassalOverlordOf` collapses to one export" collapses two DIFFERENT quantities and cannot hold the raw-byte bar.** The twins are not duplicates: occupation.js:1232-1242 answers "who is my edge-overlord" (relationshipStates `relationshipType==='vassal'`, junior/senior roles); traditions/relations.js answers "who vassalized me by conquest" (`worldState.occupations[sid].state === 'vassalized'`, any other state ⇒ null — quoted above). Traditions' rite imposition is shipped-lit; V4 confirms `vassalizationOutcomes` skips the edge relabel when no edge exists, so rung-without-edge and edge-without-rung states both occur. Collapsing to either predicate changes lit tradition behavior in one direction each — irreconcilable with SEAT-1's raw-byte bar. AMENDMENT: rename, don't merge — one export per QUANTITY (`edgeOverlordOf` vs `vassalizedOccupierOf`, or fold both into `foreignSeatOf`'s regime arms) with a same-seed fence proving traditions unchanged.

9. **MAJOR — §4 "News bill priced per car" is a claim, not a deliverable.** No per-car pricing exists anywhere in the volume. D8 lists five kinds with no car assignment, and against V6-B3's nine points the unpriced residue is real: rumor pool rows in the append-only, order-load-bearing 107-kind fallback corpus (point 6 — "a W-SEAT kind without a pool row ships engine-token prose"); the sectionAuthority question (point 8 — an occupier-veto receipt is war-rulings-flavored; if any kind wants the stamp, widening the closed 3-member registry in normalizeEntry is a schema-visible change, nowhere flagged); explicit herald desk assignment per kind (point 2); and the kind-pool walker family is eight files strong at tip (warRulingKindPools, sovereigntyKindPools, etc. — `git ls-tree 73f5dfc02 tests/lint`). AMENDMENT: a per-car kind table (kind → car → desk → pool row → authority-stamp yes/no) before any car's charter compiles.

10. **MINOR — the "~20 named political test homes" denominator is wrong for SEAT-2.** The homes SEAT-2 actually extends exist but are largely absent from the named list: `tests/domain/changeAuthorityPolicy.contract.test.js` (behavioral + SOURCE-ANCHORED — it asserts the live applyMode expressions, so the primacy axis reds it immediately), `warSeatBooks.test.js` (the list names only warSeatBooksFactionAddress), `settlementStrategy.test.js`, `stressorGates.test.js`, `mobilization.test.js`, `convergence.test.js`, `warDeployment.test.js`, `beliefMisjudgment.test.js` — all confirmed present at 73f5dfc02. The extend-only claim survives; the enumeration does not.

11. **MINOR — SEAT-1's flag green is vacuous by construction.** At SEAT-1 the flag gates pure resolvers with zero consumers, so dark and lit are byte-identical BOTH ways; mechanismLitCoverage's LITERAL credit (any test setting `foreignSeatEnabled: true` — tests/property/mechanismLitCoverage.test.js AXIS 2) is satisfiable by a drive that drives nothing. Per §711.4 the charter should label SEAT-1's dormancy/lit greens REGRESSION-grade and name SEAT-2 as the discovery-grade proof.

12. **MINOR — module placement is load-bearing for the 2-file bill and unstated.** mechanismLitCoverage AXIS 1's denominator is every flat worldPulse module; the design never says where `anticipatedReactions` or the posture producer live. The 2-file bill holds only if the unit home directly imports every new worldPulse module (AUTO credit) and posture lands inside occupation.js — V7 conditioned a third file on exactly the posture-module fork. State the placements in the charters.

13. **MINOR — SEAT-1 ∥ SEAT-5 parallelism contradicts the volume's own serialization law.** §5 declares transferRulingPower "a two-car file — serial, never parallel," then schedules SEAT-5 (a new transferRulingPower call site plus roster-modifier/ledger writes) beside SEAT-1 (which is unifying the occupied predicate and editing rulingPower.js for `foreignSeatOf` + the legitimacy-read migration). Three cars touch rulingPower.js in the combined program (W-COIN-1a, SEAT-1, SEAT-2) and the two scheduled in parallel share the occupied-predicate surface SEAT-1 is changing. Serialize SEAT-5 after SEAT-1, or scope SEAT-5 to consume SEAT-1's exports.

14. **MINOR — Q-S2's posture field has no lifecycle trace.** Regen, import (the A1.8 importScrub precedent), and undo behavior for the persisted posture are unstated — the write-that-ghosts-one-path class. The owner row should carry the lifecycle ruling, not just the shape.

**ATTACKS THAT FAILED**

- *"The two-new-file bill is impossible under the census laws"* — FAILED: one lit-eligible unit home importing each new worldPulse module earns AUTO credit for all of them, one LITERAL flag drive covers AXIS 2, and the byteIdentity file is legitimately NON_LIT; the bill is feasible under the (unstated) placement choices of finding 12.
- *"No existing test homes cover the touched modules"* — FAILED: the estate is richer than the design's list (finding 10); every touched module has a live home at 73f5dfc02.
- *"SEAT-5 early breaks the no-car-before-W-COIN-1a order"* — FAILED: §5's absolute sentence precedes and bounds "early"; beside SEAT-1 means after W-COIN-1b.
- *"sectionAuthority must be widened"* — NOT PROVEN as a defect: no W-SEAT kind demonstrably requires the stamp; recorded as unpriced (finding 9), not as a confirmed schema change.
- *"Chooser terms are dead code behind settlementStrategyEnabled=false"* — FAILED as a defect: double-dark composition is the house idiom; worth one charter sentence, nothing more.
- *"The posture modulation breaks the anti-snowball caps"* — FAILED: the caps are hard (`OCCUPIER_BENEFIT_CONTAINMENT` 0.9 applied after diminishing returns, occupation.js:196-206) and §2.8 already binds a soak re-run; no gap found beyond finding 7's specific loop.

**VERDICT: REFUTED on this lens.** The architecture's direction (derived seat, extend-don't-fork, the four precedents) stands, but the design's two central integration-cost claims — "one edit at the choke serving all ~30 families" and "nine existing consumers inherit the seat for free" — are contradicted by the code they cite, SEAT-2's effort class conceals a three-car scope whose D9 slice invalidates the collision lane's W-COIN-2 verdict, and four mechanism-level receipts (cause-coercion to 'coup', unrecorded power cuts, the burn-pacifies-the-town loop, the vassalOverlordOf false-duplicate) mean §3-D2, §3-D4, §3-D7, §4, and §5 must amend before any implementation car's charter compiles.

---

# W-SEAT PANEL LENS 5 — PRODUCT (verbatim)

LENS 5 — PRODUCT VALUE, THE OWNER'S WORDS, AND THE DECISION SURFACE

FINDINGS

1. MAJOR — §0.1 / §3-D2 / §4 — "ALL decision-makings" is delivered for roughly half the decision inventory, and the other half is silent, not stated.
Receipt: the owner's words are "an equal or greater counterpart in **all** decision-makings derived from the overlord or occupier" (§0.1). The design's coverage: override class = "war initiation & mobilization posture · taxation posture · trade policy/embargo · levy/garrison · treaty signature" (~5 families, §3-D2); seat books = the nine war/diplomacy consumers (V1 CHOKE 1: "peace, coalition join/refuse, ratification ballots, envoy negotiation, sales…"); D2's stated exclusion covers only "faith, internal justice, culture." Against V1's nine-family inventory, these families get NO seat presence and NO stated disposition: **institution build/close** (institutionLifecycle — no `authorityFor` call, verified by grep over the extract), **tier/resource dynamics** (tierResourceDynamics.js:217/486/537 — `authorityFor` sites exist, so the choke edit would reach them for free; the class list omits them without a word — yet "what the town builds and extracts" is precisely what an exploiting occupier would control), **demographic works/responses**, **ruler succession** (npcLadder), **commons-voice handling**, and **mobilization reactions**. D2's third bullet hides the rest behind "the strategy chooser **and other deterministic deciders** take bounded seat terms" — unenumerated, so no one can say which greens are coverage and which are silence. Same defect on the vassal side: the vassal overlord's weight lands only in the war-family seat books; in taxation/trade/institution decisions the overlord has no counterpart at all (only D5 forecasts, which are the *settlement's* read of the overlord, not the overlord's seat). Also note the design never records its interpretation of "always **just** greater" (guaranteed ordering vs slim margin) — structural veto is not "just greater" under the plain reading.
Amendment: add to §6 a chair-drafted, owner-vetoable **coverage table over V1's full inventory** — every decision family marked OVERRIDE / WEIGHTED (name the decider and the kernel term) / PRESSURED-ONLY / EXCLUDED-with-reason — replacing "other deterministic deciders" with names, and record the "just greater = ordering guarantee" reading as a vetoable interpretation in the same block.

2. MAJOR — §3-D6 — "compete in coalitions against the ruler" is delivered as membership plus denial; the occupier's bloc has no positive decision lever, and the design doesn't say so.
Receipt: verified in the extract — `rulingBlocOf` (settlementPolitics.js:546-591) returns only a bloc containing a member with `isGoverning === true` at ≥ RULING_CONSOLIDATION_FLOOR, and `blocDecisionFactor` (:625-627) is `if (!ruling) return 1` — a non-ruling bloc loads **nothing**. The occupier's roster row is seeded `isGoverning:false` (applyWorldPulseOccupationAuthority.js:70-82, V5). So after D6's reroute the occupier can form a bloc with patron goals — and that bloc moves no decision. What actually exists against the ruler is denial competition: outbidding lures members from the ruling bloc (V5 :856-887), collapsing its consolidation and its ±0.30 load. That is a real move, but D6 claims "honest bloc standing… Outbidding, exposure, and dissolution machinery are inherited untouched" without stating that inheritance gives the occupier only the denial half; the owner's "compete in coalitions against the ruler to the overlord's benefit" reads as positive capability.
Amendment: D6 either (a) states explicitly that against-the-ruler bloc competition = outbidding/denial plus the D2 primacy channels, as a vetoable reading, or (b) wires one bounded opposition-bloc term (e.g., an occupier-bloc consolidation feed into challenge severity or the foreign book's share) — pick and say which.

3. MAJOR — §3-D4 / §6 Q-S3 — the domestic half of §736 has no chartered flag, and Q-S3's key question doesn't even name it.
Receipt: D4 ATTEMPT rows are "scaled by the ruler's legitimacy band AND, per-archetype, by a small closed sensitivity table" — the ruler's OWN legitimacy and archetype, no foreign seat involved. These land on the SHIPPED-LIT stressor path (V7: "W-SEAT's coup/rebellion arms sit on the SHIPPED-LIT stressor path"). Law §2.4 guarantees 0-when-dark only for "EVERY **FOREIGN** TERM"; the archetype table is not a foreign term. Q-S3 frames lighting as "one key covering **seat+reactions** vs split keys" — the domestic upheaval scaling is not named as a lighting subject anywhere. Either it silently rides `foreignSeatEnabled` (chaining §736 — a directive that needs no foreign seat — to §735.2's lighting posture) or it ships unflagged (a same-seed shift on lit worlds, prohibited).
Amendment: Q-S3 names the domestic §736 rows as a lighting subject and rules one-key-with-stated-coupling or a split key; SEAT-2's dormancy bar line then covers the archetype table explicitly.

4. MAJOR — §6 Q-S3 REC — "dark at launch" defers every one of the owner's five statements, and the recommendation doesn't state that consequence.
Receipt: Q-S3 REC: "one key, owner-lit, dark at launch until the owner's walk says otherwise." With that key dark, no launched world exhibits the foreign seat (§735.2), the completed upheaval scaling (§736, per finding 3's coupling), anticipated reactions (§741), the posture dilemma, or D9 repression — the entire mandate ships as dormant machinery. The owner's §728 order ("integrate it all… before the soak") is honored at the integration layer (§5: "All engine cars pre-soak"), but integrate ≠ deliver, and the standing law is explicit that a self-imposed conservative constant must be re-asked, never silently bound ("TIME IS NOT THE CONSTRAINT… never present a cheaper option as the best available"). The row exists — good — but the REC's crux is missing.
Amendment: Q-S3's REC states in one sentence: "dark at launch means none of §735.2/§736-completion/§741/D9 reach a launched user until the owner lights the key; the walk is the decision point; lit-after-tuning is the option that delivers the mandate at launch" — so the owner rules with the cost visible.

5. MINOR — §2.6 vs §3-D7 — self-contradiction on the frozen cause vocabulary.
Receipt: §2.6: "⛔ RULING_POWER_CAUSES… frozen persisted vocabulary — no extension without an owner row; **the design needs none**." D7: "`transferRulingPower(cause:'liberation'…existing vocabulary permitting — else 'appointment' with a typed reason)" — contemplating exactly the extension §2.6 says isn't needed, filed under §6's chair-vetoable list rather than a Q-row, though frozen persisted vocabulary is a canonical owner-gated class.
Amendment: make `'appointment'` + typed liberation reason the PRIMARY spec (keeping §2.6 true), and if the owner wants the honest `'liberation'` spelling, that is a Q-S6, not a vetoable aside.

6. MINOR — §1 / §3-D4 — the "one genuinely missing moment" is mischaracterized, and D4 fills it with the wrong quantity's scaling.
Receipt: §1 claims aftermath "is NOT legitimacy-scaled — the one genuinely missing moment," but V2 (writer 2) shows `LEGITIMACY_SEEDS` already pulls the reseed by the deposed ruler's score (`newScore = clamp(seed.min, seed.max, base + (50−oldScore)×oldPull)`, rulingPower.js:452-456 — "deposing a hated ruler starts warmer"). D4's AFTERMATH then completes the moment by **seat weight** — §735.2 delivery wearing §736's label. What is genuinely missing is mint-severity scaling by legitimacy.
Amendment: D4's aftermath paragraph names what is already live (reseed oldPull), what is new-domestic (severity by legitimacy), and what is new-seat (client warmth by seat weight), so SEAT-2 implements the right gap.

7. MINOR — §3-D3 — cultivate's town-side reality is one channel wide before coin, and D3 under-states the dependency.
Receipt: pre-coin, exploit moves real quantities (verified: `computeOccupierBenefit` war-support units, occupation.js:515,1146-1171, relief easing war exhaustion) — but cultivate's listed effects (benefit down, RESISTANCE_DECAY up, suitability up) all improve the **occupier's control trajectory**; the only thing the occupied town feels is decaying `occupation_resistance` severity. The investment half is "occupier pays (W-COIN sink when lit)" — one parenthetical. The owner's "responsibility to cultivate" implies the town visibly gains; until coin, cultivation is mostly forgone extraction.
Amendment: D3 carries a two-column felt-cost table (pre-coin / coin-lit) per posture, and names the town-side channel(s) a cultivating occupier moves pre-coin — or states plainly that town-side benefit waits for W-COIN-3.

8. MINOR — §3-D5 / SEAT-6 — §741's product surface is not chartered.
Receipt: D5 fires inside engine deciders and receipts ("held back, fearing X's answer"); SEAT-6 charters "standings/occupation/seat panels" only. No forecast surface for the DM docket (a proposal's anticipated reactions is the experience-first payoff of §741 for the player), and sim-minted realm-verb mints (DECLARE_CASUS et al., realmVerbExecution's lane) are not among D5's named consumers.
Amendment: SEAT-6's scope names a reactions row on the proposal/docket surface (belief-scoped, DESK-family); D5 states whether verb mints consult the forecast or why not.

ATTACKS THAT FAILED

- "The cultivate/exploit dilemma is toothless before W-COIN" — REFUTED as a whole: benefit units, resistance dynamics, legitimacy prices, and the suitability→vassalized trajectory are all live pre-coin (occupation.js constants CONFIRMED in V3; computeOccupierBenefit read directly). Only the pay-to-cultivate half is coin-gated, and D3 flags it. Residue is finding 7.
- "D9 is a smuggled new capability" — failed: the owner ordered it verbatim in-sitting, and the interventions channel it generalizes is confirmed live (interventionAdj, convergence.js:501-551 per V1/V5).
- "Vassal closeness scaling is vapor" — failed: all three owner signals have confirmed homes (cultureAffinity §II.5-2 composite, trade channels/flow bands, verbatim `stableVassalage` — V4 receipts), and D1 reuses rather than mints.
- "§738 map-boundary breach" — failed: no car touches settlement-layer mapmaking; SEAT-6 is panels-only in the DESK family.
- "A persisted-state decision smuggled past Q-S2..Q-S5" — failed on search: posture is the sole persisted decision (Q-S2); seat/reactions are derived per the hegemonyRead template; SEAT-5 removes state and declares its lit-world shift consistent with the A1.12 precedent.
- "Tuning could invert 'always just greater'" — failed: law §2.3 makes occupation primacy typed ordering, immune to scalar tuning (interpretive residue folded into finding 1).

VERDICT: NOT-REFUTED. The architecture composes real precedents and survives this lens; but as written it delivers "ALL decision-makings" for about half the decision surface with the remainder silent (1), delivers coalition competition as denial-only without saying so (2), leaves the domestic half of §736 with no chartered flag (3), and recommends a launch posture that defers the entire mandate without stating that cost (4). Amendments 1-4 are required before the seal; 5-8 are drafting-grade.

---

# D10/D11 MINI-PANEL LENS 1 — UNITS/ADAPTER (verbatim)

All arms verified. Composing the lens report from executed receipts.

---

**LENS 1 — UNITS, CONSERVATION, AND THE ADAPTER'S REUSE CLAIMS** (D10/D11 full architecture at `2974e907b:docs/DESIGN_W_SEAT.md` F1–F7; code verified against the mirror at `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours` = 73f5dfc02; every receipt below quoted from a file read this session)

## FINDINGS

**1. BLOCKER — F3 (ledger key + one-contest law). The contest id degenerates for simultaneous births, and no civil one-army law exists.** The row is keyed `civil.<stablePart(sid)>.<sinceTick>` and the party ids are `rising.<sid>`/`loyal.<sid>` — neither carries the birth family. A free town with score < 45 and pressure ≥ 0.7 can hold a live `coup_detat` AND a `rebellion` stressor simultaneously (rebellionGate nulls only on occupied/score ≥ 75; coupSpawnGate nulls on score ≥ 45/occupation stressor — stressorGates receipts in V2), and D10 births a force per gate-crossing. Two same-tick births at one settlement mint the SAME ledger key and the same verdict fork `siege:civil_<sid>_<t>:<tick>` — `stablePart` (stablePart.js:16-21) collapses `.`/`:` to `_`, so the id space has no family axis at all — one contest silently clobbers the other in a PERSISTED ledger and shares its PRNG stream. Worse, even at different sinceTicks, two OPEN windows each fight the full garrison: the loyal closure "garrison + (1−participation)·levy" is derived per contest with no mutual exclusion, double-counting the same garrison at full strength in both — exactly the class the one-army law (warDeployment.js:4) exists to kill, and F3 charters no civil sibling of it. AMENDMENT: key the row by the triggering stressor (`civil.<stablePart(stressor.id)>` or add the family: `civil.<kind>.<sid>.<t>`), extend party ids the same way, and rule one-open-contest-per-settlement (or a declared garrison-split law) in the SEAT-7 charter.

**2. BLOCKER — F1 × F3 (conservation at the route boundary). "Banked at birth" and "below threshold: no ledger row at all" contradict, and birth (b)'s debit semantics are never chosen.** F1: heads are "banked at birth for population conservation… survivors/dead reconciled at resolution" — the deployment idiom, where bank = a real population DEBIT (warHomeCosts banks only what it debited; deploymentReturn.js:259-265 "a non-zero bank PROVES real population was debited"). F3: "sub-plausible risings stay in verdict-math land… **no ledger row at all**" — nowhere to bank. Combined: a birth-(a) splinter debited "in the same act" as the homecoming credit whose contest then routes sub-plausible has debited heads with no row to hold them and no reconciliation path — a leak; alternatively the debit is conditional on routing, which is stated nowhere. And for birth (b) the text never decides between the two live idioms: the deployment bank-and-debit (which would hollow `settlement.population` by `population × participation01` for the whole window — every prosperity/food/demographic reader sees it, indefinitely for a structural insurgency) versus the calamity one-shot (C3's named precedent: no debit, only the dead sink at resolution — populationDynamics :731-762). Crediting survivors at resolution is correct under the first idiom and a DOUBLE-COUNT under the second; F1's single reconciliation sentence cannot serve both births. THE PROMISE's books ride on this. AMENDMENT: per-birth conservation table in the SEAT-7 charter — birth (a): debit at birth (per-source, see #4), credit survivors + sink fell at EVERY window exit; birth (b): explicitly choose no-debit/dead-only-at-resolution (REC — the men never left town) and forbid the survivor credit; sub-plausible routing: no bank, no debit, ever; enumerate every window-exit path (falls, forcedLift, capitulation, stressor decay, canon-vanish, razing mid-contest, DM dismissal) with its reconciliation — "dropped at resolution" alone repeats the liberation-never-uninstalls class.

**3. MAJOR — F3 (threshold routing). `classifyFeasibility` reuse is circular through the arm the exclusion doesn't cover: FRAGILE_LEGITIMACY.** The collapse override has TWO arms (feasibilityGate.js:131-141): the COLLAPSE_ARCHETYPES condition scan — which F3's "exclude the triggering stressor" addresses — and the bare-score arm `legit <= FRAGILE_LEGITIMACY (28)`. Risings spawn precisely in low-legitimacy towns (coup gate needs score < 45, full mult < 30), so the score arm fires for most real risings, returns `require_betrayal`, and `verdictPermitsSiege` admits it (feasibilityGate.js:259-261) — a ratio-0.05 rising in a score-25 town OPENS the engagement, defeating "sub-plausible risings stay in verdict-math land." The rising IS the internal collapse; rescuing it by the defender's rot is the same circularity F3 chartered away for conditions. Also mechanism: `classifyFeasibility` is called INSIDE `resolveSiegeVerdict` (warSiegeVerdict.js:179-186) with the caller's `defenderItem` — there is no exclusion parameter; the exclusion must be a filtered defenderItem clone (conditions minus the contest's own archetypes), and the read is over CONDITIONS, not stressors, so "the triggering stressor" needs its condition-archetype mapping named. AMENDMENT: route on the RAW ratio bands (call `classifyFeasibility` with a defenderItem stripped of both the contest conditions AND with the legitimacy-score arm declared inert for routing — e.g., the router uses `feasibilityRatio` + band floors directly), and specify the filtered-clone mechanism for the in-verdict call.

**4. MAJOR — F1/F5 (splinter debit). The per-source homecoming makes "debited from the homecoming credit" under-specified, and the conservation invariant needs its three-way restatement.** The credit is apportioned per contributor by largest remainder — overlord conscripts PLUS each vassal's levy (deploymentReturn.js:283-330), pinned exactly by `deploymentReturnLevyConservation.test.js`: `Σ deltas === metadata.survivors` (:87) and exact per-source allocations 250/500/250 (:97-99). F1's debit `survivors × fraction` names no source: taken from the home's share only, it can exceed that share on a levy-heavy army (home share = `deployedPopulation − leviedTotal`, deploymentReturn.js:302 — can be a minority); taken pro-rata, VASSAL men splinter into the overlord's town and the contest row must carry a per-source bank (F1's row carries only `heads`) or resolution mis-credits them. Either way `Σ credits === survivors` and the documented law "fell remains the sole sink" (:255-257, :280) become false mid-window: the true lit invariant is `credited + splintered + fell === deployedPopulation`. F7 lists the test home to extend but the architecture never restates the equation. AMENDMENT: rule the debit source (REC: home-share only, capped `min(round(survivors×fraction), homeAlloc)` — deserters from the overlord's own muster; vassal levies go home), restate the three-way invariant in the charter, and add the splinter fixture (ratio < 0.3, flag lit) to the conservation test; the existing metadata.survivors semantics must be redefined or a `splintered` field added (persisted-summary shape — flag it).

**5. MAJOR — F3 (SIEGE_MAX_AGE). "The ceiling simply never binds" is FALSE for one of D10's three births.** The ceiling fires whenever `siegeAge >= 60` is passed (warSiegeVerdict.js:215-233): deterministic `falls = coalitionCurrent > defenderCurrent`, else `forcedLift`. Coup and rebellion stressors are `episodic` (maxAge 8 — stressorsCore.js:29, :182-184, :236-238) so their windows can't reach 60. But `insurgency` is **`structural`, maxAge null — unbounded** (stressorsCore.js:30, :149-151). An insurgency contest window CAN pass 60 ticks, whereupon the verdict auto-resolves with siege semantics: `forcedLift` ("the besiegers withdraw") has no defined civil meaning, and an auto-"storm" hands the seat over at pFall 1 with a siege-prose reason string. AMENDMENT: either cap the civil window with its own force-resolve valve (the MAX_CONTESTED_DWELL 48 idiom, occupation.js:462-470) below 60, or embrace the ceiling with chartered civil semantics (forcedLift ⇒ the rising disperses, survivors reconciled; falls ⇒ transfer) and a civil-worded receipt — but strike the "never binds" sentence either way.

**6. MAJOR — F2 (the force derivation). `deriveMilitaryCapacity` has no override seam, and the unstated tier-term ruling dominates every rising's size by ~3×.** Signature confirmed: `deriveMilitaryCapacity(itemOrSettlement, ctx = {})` with ctx carrying only `economicCapacityScore` (militaryStrength.js:151); every facet is computed internally from the settlement — manpower = `clamp0100(tierFrac*70 + popScore*30)` (:171-181). "Reuses it with typed degradations" is therefore not a call — it requires either a new parameterized entry in militaryStrength (an edit on the shipped-lit war path, must be absent⇒byte-identical) or external recomposition through the facet weights, i.e. a second copy of the weighting math F2 itself outlaws. And "manpower from the rising's heads via the same log-scaled path" reaches only the popScore term (≤30 points); the tier term is 70 points and F2 never says whether a rising inherits the settlement's tierFrac (a metropolis mob starts at ~70 manpower regardless of participation) or zeroes it (every rising capped at 30/100 manpower vs a loyal side reading the full formula — with PLAUSIBLE_FLOOR 0.78 nearly no popular rising ever opens an engagement except through finding #3's back door). AMENDMENT: charter the irregular entry point explicitly (a `deriveIrregularCapacity` sibling in militaryStrength.js importing the shared facet weights, with the degradations as typed parameters) and RULE the tier term (REC: a participation-scaled fraction of tierFrac — the town's structure partly fights for whoever rises from it); state the resulting typical ratio envelope so the routing floors are calibrated against it.

**7. MAJOR — F3 (the six-adj budget). The composition is over-budget at spec time: the six-term envelope exceeds the entire clamp window.** Measured bounds: authorityAdj `(score−50)/400` = ±0.125 (rulingPowerCoup.js:167); economicAdj same ±0.125 (coup.js — "±0.125 exactly like authorityAdj (÷400)"); warSentimentAdj `0.22 × sentiment`, sentiment clamped [−1,1] (disposition.js:237-241) = ±0.22; interventionAdj = tilt(±1) × 0.22 = ±0.22. Today's four already reach ±0.69 against a clamp window of width 0.8 (`Math.max(0.1, Math.min(0.9, …))`, rulingPowerCoup.js:176). Adding foreignSeatAdj + forceRatioAdj at the intervention class gives ±1.13; even at the authority class, ±0.94 — both exceed the window, and the terms CORRELATE (an occupied, broke, war-weary town with a strong rising and a hostile seat moves every term the same way), so saturation is a coherent scenario, not an adversarial one. Under saturation the base `share×severityDrag` term — the incumbent's actual standing — contributes nothing, and D8's receipts ("the seat backed the challenger") claim influence a pinned clamp nulled. AMENDMENT: the "declared composition/budget row" must land AT SEAT-2 charter time, not the tuning sheet — either rescale the six terms to a fixed total adj mass (≤ ~0.35) or clamp the adj SUM before entry; and the verdict metadata must carry the pre-clamp sum so narration cannot over-claim a nulled term.

**8. MINOR — F5 (the vassal silent band). The disband receipt is a same-seed lit shift with no flag stated.** The 0.18 ≤ ratio < 0.3 bare `continue` (deploymentReturn.js:433-443 per C2) fires on any war-lit world with vassal returns; F5 marks the new receipt "vetoable" but not flag-gated, silently violating SEAT-7's own raw-byte bar. AMENDMENT: gate it behind `irregularForceEnabled` or charter it as a named declared shift — one sentence, but it must be one of the two.

**9. MINOR — F2 (SEIZED_FRACTION). "Armories in risen quarters" implies geometry the estate does not have.** Wards/quarters are hierarchy nodes without shape (memory law; no quarter-level institution placement exists in the mirror). The band is fine as finite semantics only if its domain is a closed constant (or f(participation01) band) over the LIVE institution roster; the quarter language invites an unimplementable derivation. AMENDMENT: state the band's exact inputs and drop the spatial prose.

**10. MINOR — F3 (the loyal closure's shape). The verdict consumes more than one scalar.** `capacityFor(targetId)` must return `{offensive, homeDefense, facets}` — facets feed feasibility's magic edge (:184-186), the will track reads `facets.will`/`facets.logistics` plus `defenderItem…publicLegitimacy.score` (:242-249). F3 specifies the loyal envelope as one number (garrison + complement·levy). AMENDMENT: the charter names the loyal closure's full contract (envelope numbers new; facets = the settlement's real derived facets — the honest read for a loyal side that IS the settlement).

## ATTACKS THAT FAILED (record for the chair)

- **Fortification-as-adapter-decision**: CONFIRMED clean — `applyAttritionToRecord(record, {…, fortification})` takes a caller-supplied 0..1 (attrition.js:246); `fortificationStrength(defenderFacets, defenderItem)` is an exported pure helper (:127-136). The loyal-walls/rising-at-ground-0 split needs no code fork.
- **`supplyInterdiction: 0, spatialSiege: false`**: both are real parameters defaulting dark; the aspatial capacity-roll core runs verbatim (warSiegeVerdict.js:124, :280-289).
- **The caller-closure claim**: `resolveSiegeVerdict`'s signature is exactly as claimed — `capacityFor`/`effectiveStrengthFor` closures, `targetId` opaque (capacityFor lookup, fork key `siege:<stablePart(targetId)>:<tick>`, prose fallback only).
- **Civil-vs-real-siege PRNG collision**: none — `stablePart('civil.x.12')` = `civil_x_12` vs the real siege's `x`; distinct streams at the same settlement/tick.
- **Credit-before-branch ordering**: confirmed verbatim (credit block deploymentReturn.js:266-349 precedes the branch ladder at :351+; ratio and the splinter predicate are computable before the credit is pushed) — F1's "same act" debit is implementable as written.
- **Heads/strength desync per se**: banked-heads-reconciled-at-resolution is exactly the `deployedPopulation` idiom; no per-tick heads erosion is needed or precedented. The desync attack lands only at the route boundary and exit paths (findings 2, 4).
- **Veteran-share inputs**: `deploymentAge`/`accumulatedAttrition` confirmed present on the record and unread at return — F2's derivation has real substrate.
- **Levy-capacity "none exists today"**: confirmed; the one-resolver mint is honest.
- **SIEGE_MAX_AGE for coup/rebellion births**: the claim HOLDS there — episodic maxAge 8 « 60 (fails only for insurgency, finding 5).
- **BAND_DEFENDER discipline**: F3's erode-as-attacker/loyal-recomputes matches the reserved-dead-code deferral (attrition.js:75-87 per C4); no casual first consumer.
- **`transferRulingPower(cause:'coup')`**: in the frozen vocabulary; no extension needed.

## VERDICT: **NOT-REFUTED** — the spine survives its receipts: one force law, the convergence-sibling adapter, the closure contract, the fortification/attrition reuse, and the conservation DIRECTION are all confirmed against code. But the seal owes two BLOCKER amendments (the contest-id/one-contest law, #1; the route-boundary conservation table, #2) and five MAJORs (#3–#7) before SEAT-7/8 charter — three of them (#3, #5, #6) are places where the architecture's own confident sentences ("never binds", "reuses it", "stays in verdict-math land") are falsified or hollowed by a specific line of code the text did not read.

---

# D10/D11 MINI-PANEL LENS 2 — PROMISE/FLAGS (verbatim)

All verification reads are complete. Composing the panel report from executed evidence.

# LENS 2 — THE PROMISE, BELIEF, FLAGS, AND LOOPS (D10/D11 skeptic mini-panel)

Ground: design at `2974e907b:docs/DESIGN_W_SEAT.md` (F1–F7 + body + A1); receipts layer D10-C1..D11-C6 read in full; every load-bearing claim below re-verified first-hand against the mirror at `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/5a850cca-94f6-4828-bc6e-0936d5f66782/scratchpad/fmg-study/ours` (73f5dfc02). Labels: CONFIRMED = quoted code; PLAUSIBLE = reasoning over the design text.

## Findings

**1. BLOCKER — F6 (the rally). As written, the rally is inert on the stressors instigation targets, or inverted where it fires — neither applicator delivers the promised counterweight, and the design hedges between them without ruling.**
- Defect, traced path (CONFIRMED at every hop): F6's primary channel is "a third `courtHits` row (`role:'rallied'`, positive, signed-conditions mode)". That channel lands as `withActiveCondition` (`src/domain/worldPulse/pulseKernel.js:691-707`) → `applyConditions` signed mode (`src/domain/causalState.js:268-291`), which moves only the **causal lenses** (`causal.scores.*`). But the gates instigation raises read the **persisted score** through `governanceLedger` — which reads *only* `powerStructure.publicLegitimacy.score` (`src/domain/governanceLedger.js:52-67`, no conditions fold): `insurgencyGate` (`stressorGates.js:539-546`, hard-null at ≥75, ×1.5 below 30) and `rebellionGate` (`stressorGates.js:573-586`) never see a conditions-mode rally. Neither does `participation01` (F4 routes its deficit band through `governanceLedger`) nor `resistanceTarget`. The only consumer a courtHits rally reaches is `coupSpawnGate`'s `authorityMult` (`stressorGates.js:753-756` reads `causal.scores.ruling_authority`). So for an instigated **rebellion/insurgency** — half of D11's own verb ("INSTIGATE a coup or rebellion") — the "honest counterweight that keeps instigation a gamble" moves nothing.
- The hedge's other arm ("any persisted-score rally rides the canonical read-point law") is worse under occupation: `resistanceTarget` reads the **live** score at +0.3 weight (`occupation.js:337-349` — the comment says "pre-occupation" but the read is current-tick), so a score rally in an occupied settlement raises resistance → raises F4's occupied participation arm → **the strengthened throne arms the rising** — the exact sign-inversion class A1.2.14 already proved for the burn ("debiting the occupied score PACIFIES the town via the resistance-target read"), here mirrored, and F6 never names the rally's target court under occupation.
- Polarity trap inside the primary channel (CONFIRMED): `conditionDirection` is archetype-keyed, `LIFT_ARCHETYPES.has(archetype) ? +1 : −1` (`causalState.js:236-241`), and the existing courtHits application stamps `archetype:'corruption_exposed'` for every role (`pulseKernel.js:695`). A 'rallied' row reusing the channel as written reads as a **pressure** — the rally *lowers* the rallied court's lenses. This is the precise polarity bug class the single-scan helper exists to kill (`causalState.js:243-249`).
- Amendment: rule the rally ONE way before SEAT-8 charters. REC: the persisted-score applicator (`applyLegitimacyDeltasToUpdates`) with the re-band, because it is the only path that reaches the §736 gates and participation01's deficit input (the counterweight's whole job); PLUS the occupied-target ruling as A1.2.14's sibling — under occupation the rally credits the **occupier's home court** (or is ruled inapplicable), never the occupied settlement's score; PLUS, if any conditions-mode component survives, a new lift archetype enrolled in `LIFT_ARCHETYPES` and a role branch at the pulseKernel application site.

**2. MAJOR — F4 (occupied arm). "Resistance INSTEAD of the deficit, never both" is violated through the commons leg the same ruling keeps.**
- Defect (CONFIRMED): F4's occupied arm swaps the *legitimacy-deficit term* for `resistance`, but keeps the commons input ("commons rung where lit, else the STATELESS grievance re-derivation"). The grievance IS a legitimacy-deficit function: `W_LEGIT(0.55)·clamp01((55−legit)/55)` reading the live score (`commonsVoiceKernel.js:253-259`, score read at :74-80). `resistanceTarget` contains `+0.3·legit01` of the same live score (`occupation.js:342-349`). So occupied participation01 still sums two opposite-signed functions of one score — the exact fight C3 §4 proved and the ruling claims to have cured. Aggravations: `commonsVoiceKernel` has zero occupation handling (grep 'occupation|occupied' over the kernel: empty), and its `UNREST_TYPES` excludes `insurgency` (`commonsVoiceKernel.js:68`) — an occupied town's *own uprising* is invisible to the grievance while its legitimacy term keeps firing.
- Amendment: extend the occupied-arm ruling to the commons leg — under occupation the grievance's legitimacy term is replaced by (or zeroed in favor of) resistance, and the charter states which legitimacy each surviving term means (deposed regime's memory = resistance; current administration's deficit = the free arm). Same act: decide whether `insurgency` joins the kernel's UNREST_TYPES for occupied worlds or is deliberately excluded (stated).

**3. MAJOR — F4/D4 boundary. The "never both" ruling is quantity-scoped to participation01 while D4 re-creates the sum at the gate.**
- Defect (CONFIRMED at tip + PLAUSIBLE composition): `insurgencyGate` today multiplies an occupied mult (×2.0) with a legitimacy-deficit mult (×1.5 below 30), both live (`stressorGates.js:539-546`). D4 adds "under occupation, rebellion risk reads `resistance` + posture" as new gate rows without saying the existing legitimacy row is removed — the amended gate would sum deficit and resistance in one consumer, re-importing the fight one level up from where F4 cured it.
- Amendment: the SEAT-2b charter rules the occupied insurgency gate the same way F4 rules participation01 — resistance-and-posture REPLACE the legitimacy mult under occupation (or the retention is argued and owner-visible), one ruling covering every consumer that reads both quantities.

**4. MAJOR — F6 (instigation belief). The instigator is structurally blind to participation potential; the promised misjudgment receipt has nothing believed to compare against.**
- Defect (CONFIRMED): the desperation read exists and is K3-clean (`informationStatecraft.js:643-669` — own beliefMap seat records, `allianceLabel` + `strengthBand`), but the entire `BeliefRecord` surface is `readiness, strengthBand, allianceLabel, faithLabel, confidence01, lastUpdateTick` + optional `populationTrendBand/observanceLabel/scarcityBands/conditionsBands/devotionBand` (`beliefMap.js:335-349`). **No believed legitimacy, no believed unrest/commons state, no believed resistance, no believed faction landscape** — not one of participation01's four inputs has a believed counterpart (the M9a "dissent" slots are the observer's own split council, not believed target dissent — `beliefMap.js:1419-1445`). So "a blunder against a believed-weak-but-stable town is expressible" is true only on the strength axis; *ripeness* is not merely stale in the picture, it is unrepresentable — "stable" cannot be believed. And the fiasco receipt F6 promises ("the misjudgment receipt narrates the fiasco") requires a believed-vs-true delta; A1.1.3 already conceded the existing detector triggers only on strength-band/hostile-label errors. As chartered, instigation targeting cannot correlate with rising-potential except through hostility/strength, and the narration cannot say what the instigator wrongly believed about the town's temper.
- Amendment (either honest fork, owner-visible): (a) charter instigation as ripeness-blind — motive = believed hostility/strength/stakes only, stated in the volume, with the misjudgment receipt scoped to the axes actually believed; or (b) price a believed-ripeness axis (a coarse believed unrest/legitimacy band via the existing beliefAxes idiom) as the A1.1.3-style "priced FUTURE extension" that D11 either waits on or ships without — never an implied capability.

**5. MAJOR — F1–F7 (mid-campaign lighting). The transition the owner will actually perform — lighting `irregularForceEnabled` on a world with live stressors — is unruled.**
- Defect (PLAUSIBLE from the design text; substrate CONFIRMED): flags are owner-lit, dark at launch (Q-S3), so mid-campaign lighting is the *expected* path. D10 keys birth on "crosses its attempt gate," but F3's machinery is a per-open-tick scan and the contest id embeds the stressor's provenance (`civil.<stablePart(sid)>.<sinceTick>`) with `siegeAge: windowAge` fed to the verdict (C4 §3). For a rebellion whose window opened 40 ticks before lighting, nothing rules: does it birth a force on the first lit scan (a world-wide simultaneous army mint, rows whose `sinceTick` predates the flag, contests opening pre-aged toward capitulation), or never (birth-at-crossing-only, so in-flight risings stay in verdict-math land)? No dated receipt is chartered for the transition — contrast A1.2.16, where the D1–D9 panel forced exactly this (the seat-standing receipt "carrying the substrate's since-tick"). Not a PROMISE violation (forward-only), but an undated retro-provenanced force is the §710.6 family for time.
- Amendment: rule it in the SEAT-7 charter — REC birth-at-crossing-only (only gates crossed *after* lighting birth forces; pre-existing windows keep verdict math), which needs one sentence; if first-lit-scan birth is preferred instead, `sinceTick = max(stressor.since, litTick)` and a dated force-standing receipt per birth, A1.2.16's sibling.

**6. MINOR — F6 flag composition. Instigation's participation01 seed dangles when `foreignSeatEnabled ∧ web` are lit but `irregularForceEnabled` is dark.**
- Defect: F6 assigns D11 to the seat key AND the covert flags; participation01 and its every consumer live in the I-family (SEAT-7, F7). In the S∧W∧¬I combination, instigation's gate-mult row works but its "participation01 seed" targets a quantity with no consumer and no storage (F7: zero new persisted fields beyond the I-gated contest row). Half the verb silently no-ops, unstated.
- Amendment: one sentence in both charters — the participation leg of instigation no-ops when `irregularForceEnabled` is dark (stated in the flag certification rows), or D11's instigation is AND-gated on I as well.

**7. MINOR — F5 (silent band). The disband receipt is a lit-path same-seed shift left unlabeled.**
- Defect: the vassal silent band's new receipt (today a bare `continue`) fires on war-lit presets (dramatic_campaign ships deployments and vassal levies) — same-seed news-byte changes on shipped-lit worlds. The design marks it "vetoable" but neither I-gates it nor names it a DECLARED lit shift, the discipline C6 applies to the neighboring golden ("its lit arm moving is a declared shift"). Raw-byte dormancy on dark worlds survives (no deployments when war-dark) — the exposure is lit presets only.
- Amendment: gate it behind `irregularForceEnabled` OR label it a declared lit-path shift with the golden re-record discipline, in the SEAT-7 charter.

**8. MINOR — F6 (upkeep seam). "Lights only when both flags do" never names which two, and the interventions ledger has no seat-family flag.**
- Defect: the widened upkeep reader covers `interventions` (exists under preset-lit `warLayerEnabled ∧ interventionEnabled` — `convergence.js:223-228`, CONFIRMED) and `civilContests` (I-gated). If "both" means treasury + the ledger's own flag, lighting treasury alone starts charging upkeep on live intervention columns in dramatic_campaign — a preset-lit behavior change governed by no seat-family key, adjacent to the wall the same paragraph draws in bold.
- Amendment: name the conjunction per ledger in both charters (civilContests: treasury ∧ I; interventions: treasury ∧ an owner-visible row of its own, since no seat key governs those columns), and note the rally/upkeep certification sentences in Q-S3's rows — including which key owns the rally (it is described as "§736 machinery" but ships S∧W-gated, so the `legitimacyUpheavalEnabled` certification sentence must not claim it).

## Attacks that failed (recorded)

- **"I lit + U dark ⇒ risings birth without scaled gates."** FAILED: the tip gates are natively legitimacy-scaled — `coupSpawnGate` hard-nulls at score ≥45 with band/authority mults (`stressorGates.js:746-756`), `rebellionGate` nulls ≥75 with tiered mults (:573-586), `insurgencyGate` nulls ≥75 (:539-546). U dark withholds only the NEW sensitivity rows; no unscaled birth path exists.
- **"Seat dark + D11 lit."** FAILED: unlightable — D11 has no key of its own (seat key AND covert flags, the espionageGate 3-door AND precedent confirmed at `espionage/espionageGate.js:76-88`).
- **"The rally re-fuels participation in free settlements."** FAILED: on the conditions route it touches no participation01 input (finding 1's inertness); on the score route a positive delta *lowers* the deficit band — negative feedback, the damping is the deficit read itself. The only positive edge is the occupied inversion (finding 1).
- **"An F6 term leaks behind `interventionEnabled`."** FAILED as a blanket claim: F6's own law (additive-0-when-dark behind seat-family flags, ⛔ never behind `interventionEnabled`), the C6 highest-risk collision row, and the dormancy golden's lit-arm-as-declared-shift tripwire cover the committer/stake widening; residue is only finding 8's upkeep ambiguity.
- **"The processLies desperation read doesn't exist / isn't K3-clean."** FAILED: confirmed at `informationStatecraft.js:643-669` — the liar's OWN beliefMap, hostile-label filter, believed strength bands, no ground-truth import.
- **"Drop-when-empty for the contest-window ledger is an unproven idiom."** FAILED: both cited precedents are real and byte-safe (`armyTransitKernel.js:916-924` null-vs-record with serialize-compare; `convergence.js:1233-1236` interventions drop), and F7 bills one `*Dormancy.byteIdentity` per new flag with the base-dormant-vs-tip-dormant comparator.
- **"participation01 grows a legitimacy back-edge."** FAILED: F4 declares the read-only law; the rally rides an existing applicator, not a new writer; grep confirms `participation01` and all three flags are zero-hit at tip — genuinely new, no second resolver.

## Verdict

**REFUTED** — on F6's rally specification (finding 1, seal-blocking: inert or inverted on either hedged applicator, plus the polarity trap) and the occupied-arm ruling's scope (findings 2–3), with findings 4–5 major and all amendable. The force model (F1–F3), the flag topology's three-key shape, the preset-lit wall, and the dormancy machinery survived attack. The D10/D11 text should not seal for SEAT-7/8 charter until the eight amendments above are folded, A1-style.

---

# D10/D11 MINI-PANEL LENS 3 — ESTATE/OWNER (verbatim)

LENS 3 — ESTATE, COLLISIONS, AND THE OWNER'S WORDS (skeptic mini-panel over D10/D11 F1–F7 at 2974e907b)

FINDINGS

1. MAJOR · F6 (the upkeep seam) / F7 — Two new coin flows enter W-COIN with no owner row, against the precedent set one amendment earlier. F6: "W-COIN-2's upkeep reader widens to the interventions/civilContests ledgers (named in BOTH charters…)". Charters are car-level documents; the owner surface for added coin flows is the Q4 row — the D9 sink was handled exactly that way (W-SEAT A1.2.13: "The D9 coin sink joins W-COIN's enumeration via a Q4 amendment (the owner sees the added flow)"; A1.3 repeats it), and W-COIN A1.9 rules "The owner sees all six W-COIN-3 flows: Q4 is widened to enumerate … by name." Intervention upkeep and civil-contest upkeep are the same class (the census CONFIRMS interventions today have "no clock, no decay, no upkeep" — D11-C6), yet F6/F7 mint no Q-row. Also unaddressed: W-COIN A1.5's law ("the deployment-mint-cost sink is ASSIGNED to W-COIN-2 with a flows field, or does not exist") — does the widened upkeep fold into `flows.upkeep` or mint fields, and does the closed news-kind set (`upkeep_paid | treasury_shortfall | …`, W-COIN §255) cover it? Amendment: extend the standing Q4 amendment to name both flows; assign their flows fields and news kinds per A1.5/A1.18 in the same act.

2. MAJOR · F7 test homes / F2 — "warConvergenceForces — the vocabulary pin lives THERE" misattributes the pin, and a census-ruled precondition was dropped. CONFIRMED: `tests/domain/warConvergenceForces.test.js` exists at 73f5dfc02, but it pins the WR-9c certification force-cell vocabulary (`WAR_CONVERGENCE_FORCE_IDS`/`WAR_FORCE_STATES`/`WAR_ENDING_KEYS`) and the warCosts/demographicsWar coupling ("a rename in warCosts.js … reds this file") — zero occurrences of composition/NAME-4 in the file. The only vocabulary F2 names is NAME-4's closed ~6-type composition set, and `deriveForceComposition` is grep-zero across src+tests at tip: NAME-4 is unbuilt, its vocabulary unminted, no pin for it exists anywhere. The census's own contract (D10-C4) — "seal the vocabulary before SEAT-7's composition fields land" — appears nowhere in F2 or F7's sequencing. Amendment: correct F7's parenthetical (the WR-9c pin guards the engagement vocabulary coupling, not F2's shared vocabulary); add the vocabulary-seal-before-composition-fields precondition to F7, or declare SEAT-7 emits no composition fields until NAME-4's vocabulary seals.

3. MINOR · F7 sequencing — SEAT-8 is unordered against W-COIN-2. The upkeep seam widens W-COIN-2's reader, but the volume only makes W-COIN-2 "interleavable" with SEAT-1/2's window (§ sequencing, line 71); F7 orders SEAT-7/8 solely after SEAT-2/SEAT-4/mini-panel. If SEAT-8 dispatches before W-COIN-2 lands there is no reader to widen. Amendment: one sentence — the upkeep seam is conditional-on-W-COIN-2-landed (or SEAT-8 serial after it).

4. MINOR · F3/F6/F7 vs the census bill — the three-kind news bill is not reconciled and the pool fork is unruled. D11-C6 names THREE new kinds (civil battle, instigation exposure, legitimacy rally), each paying authoring census + headline/voice walkers + kind pools + rumor row + herald routing, and raises the fork: "NO intervention pool walker exists — a civil-battle kind either joins one [warCoalition/warCost/warRuling] or mints a pool + kindPoolFloors.walker row." F3 pays the full bill for civil battle only and leaves the pool fork open; F6 silently supersedes the rally kind (re-homed to the existing courtHits channel) and never says whether instigation exposure mints a kind or reuses the infowar blowback siblings — and SECTION_OF's totality walker reds any new producer kind until filed (D11-C5 §8). Amendment: a three-row kind-disposition table (new / reused / superseded, pool home named) owed at SEAT-7/8 charter, per the W-COIN A1.18 precedent.

5. MINOR · F6 — upkeep payer and flag conjunction per ledger are unstated. For civilContests, the rising has no treasury; presumably the incumbent's vault pays the loyal side and the rising pays nothing — an asymmetry to state. "Lights only when both flags do" names no pair: interventions ride preset-lit `interventionEnabled`, so its upkeep leg must be treasury-flag + seat-family-flag gated; civilContests would be treasury + `irregularForceEnabled`. Amendment: name payer and flag conjunction per ledger in the charter.

6. MINOR · F2 / ODQ §745 — the narrowing of the owner's "statted by makeup" to display-only is recorded only inside F2. F2 corrects the addendum ("statted by … + the NAME-4 clerk" → "displayed via"), grounded in NAME-4's constitutional law, but §745.1's ODQ summary lists the census folds without naming this correction of the owner's own clause. Amendment: one ODQ sentence so the narrowing is owner-visible where his directive lives.

THE OWNER'S WORDS, ONE BY ONE (§743.1 verbatim record)

| Owner's words | Verdict | Receipt |
|---|---|---|
| "partially treated as independent military units" | DELIVERED | F1 two-track contest-window row; F3 `civilContests` ledger + engagement routing above `classifyFeasibility`; window-scoped, never a standing army — "partially" is exactly what shipped |
| "derived from returning rebellious troops OR current population" | DELIVERED | F1 births (a)/(b); F5 splinter upgrade; F4 population × participation01 |
| "how big … returning troops or scaled to legitimacy failings" | DELIVERED (one ruled substitution) | Birth (a): `currentEffectiveStrength × SPLINTER_FORCE_FRACTION` + heads debit (F1/F5). Birth (b): the legitimacy deficit band is the FIRST input of F4's participation01 formula — legitimacy-failings sizing IS present. Occupied arm substitutes `resistance` for the deficit, census-proved (D10-C3 §4: same quantity, opposite signs) and stated in F4 |
| "stats dependent on makeup, veterans, and what a regular unit would be" | PARTIAL | Veterans: F2 (deploymentAge + accumulatedAttrition + `experience01` — all CONFIRMED in the estate). Regular-unit law: F2 = `deriveMilitaryCapacity` six facets (D10-C1). "Makeup": narrowed to display-only by NAME-4's law (composition never feeds strength) — internally consistent (regular units' stats never read composition either), but see finding 6 on owner-visibility |
| "participation a percentage reflection of population" | DELIVERED | F4 minted once, grep-zero-today CONFIRMED; size = population × participation01; heads banked per F1's unit law; legacy `{total}` tolerance ruled |
| "external involvement several fold, some support some suppress, by relationships and what they stand to gain" | DELIVERED (behind flags) | F6: D5 bounded set replaces relationship-neighbors-only; sided rows `incumbent|challenger` (D11-C6 CONFIRMED shape); typed stakes incl. the three seat stakes; netting exists twice over; standing interveners accumulate across ticks (no-expiry ledger), so several-fold is reachable at one-suitor-per-tick — the minting widening is an owner-visible declared lit shift; Q-S3's cost sentence covers dark-at-launch |
| "compromised assets may instigate at vastly increased exposure" | DELIVERED | F6: act-scoped exposure with its own CORRUPTION_WEB_TUNING constant ("a named multiple of the organic rate"); blowback triple, asset burn, casus grading via `exposedCorruption`, rally counterweight; K3 binds the blunder path; Q-S6 rows player visibility |

ATTACKS THAT FAILED (record)

- W-COIN-1a-first: RESPECTED transitively — SEAT-7/8 dispatch after SEAT-2 (after W-COIN-1b, after W-COIN-1a); the mini-panel and charter are authorship, not implementation cars, so running them now violates nothing.
- The ledger-manifest row: the census made it optional-by-precedent (3 of many ledgers rostered) but recommended; F3 upgrades it to mandatory ("A ledgerOwnershipManifest row lands with it (two writers from birth)") — honest, stronger than the census asked.
- New-file bill "2": matches the census's own bill verbatim (D11-C6), and the arithmetic holds — one new flag (`irregularForceEnabled`; the seat/upheaval keys are SEAT-1/2's bill per Q-S3 restated) ⇒ 1 dormancy file + 1 unit home.
- Verdict-file serialization: SEAT-2 → SEAT-7 serial is stated consistently in F3 (adj budget) and F7; the six-term clamp budget joins the tuning sheet.
- "Legitimacy-failings sizing went missing": fails — it is F4's first input; the occupied-arm substitution is census-grounded and surfaced.
- "participation01 silently bypasses the commons flag": fails — F4 explicitly surfaces the rung-where-lit / stateless-grievance-otherwise choice "to the mini-panel as the deliberate reading," exactly as D10-C3 requested.
- "The rally writes legitimacy through a back-edge": fails — F4/F6 route it through the applicator on the canonical read-point law with the re-band, the stale-govMultiplier trap named.

VERDICT: NOT-REFUTED. The architecture stands on the estate as censused; findings 1 and 2 are amendable defects that must fold before the SEAT-7/8 charters (an owner-visibility gap on two new coin flows, and a misattributed vocabulary pin plus a dropped seal-before-composition precondition), with four one-sentence-class minors behind them.