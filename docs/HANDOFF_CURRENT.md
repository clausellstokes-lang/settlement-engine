# HANDOFF — CURRENT PROGRAM STATE (model-agnostic successor bootstrap)

**Updated 2026-09-14 04:0x at the clarity re-cut's landing on all 24 written pools of DS-DEF-2 (dock `7992713d0`); previously 2026-09-13 14:0x (ledger through `44f7dc054`), before that 2026-09-02 at the §881.19 RESUME (ledger through §881.19; refreshed at
every collection per ODQ §233). If you are a successor — any model, any account —
read this whole file, then docs/START_HERE.md, then the tail of
docs/OWNER_DECISION_QUEUE.md (the ledger; ODQ §§150–§276 plus collecting/amending §§287–§289 contain the map
program's decisions; SPEC fold labels §§277–§286 are design sections, not missing ODQ entries).
The repo is authoritative; trust executed evidence over any summary including
this one.**

## ⭐⭐⭐⭐⭐ PICKUP AT §919.6 AND AFTER (refreshed 2026-09-14 22:1x, the chair Fable 5.1 after Opus) — **THE CLARITY RE-CUT IS LANDED ON ALL 24 WRITTEN POOLS OF DS-DEF-2; the dock `laneRW-DEF2` is CLEAN at `7992713d0` (sealed `refs/preserve/rewrite-8b-def2-clarity-batch4-2026-09-14`); NOTHING IS IN FLIGHT** (product `claude/composite-r4` still `f73bdbf16`; ledger through this commit).

**READ FIRST, in this order:** (1) the previous pickup block below (§919.6's law, machinery, dock, seats, delegation, death classes — all still true); (2) `$SC/kit/briefs/brief-block-clarity-recut.md` — the owner's rulings 42/42a/42b/42c/**42d** (42d added 09-14 with its failure mode) under which the block was line-edited; (3) the four batch commits in the dock: `8e608dd1f` · `a9b460eac` · `8d9a51d9c` (batch 1 re-opened under 42d) · `b5738c2ac` · **`7992713d0` (batch 4, the chair's landing — read its body whole: the audit, the four classes of moved claim, the two instruments, the overrulings, the 42b list, the ten least-sure lines, the charters)**; (4) `$SC/kit/briefs/CHARTERS-from-batch4-audit.md`; (5) `$SC/kit/RESUME-NOTE.md` entries `+ 09-14 02:1x` and `+ 09-14 04:0x`.

**WHAT THE LANDING TAUGHT, IN ONE BREATH.** The clarity re-cut is a LINE EDIT under a CLAIM FREEZE (the words move, the fact does not, so the standing refutation still covers every face). The freeze needs an INSTRUMENT, not an instruction: the implementer that cut batches 1–3 believed it had held and it had not — ~20 of 93 changed faces in batch 4 moved their claim, nearly all through ruling 42d's cure (a habitual present became one past episode; the town narrowed to a room; the actor changed). Two instruments now find the class in seconds and are the gate for any further batch: the MOVE-GRAMMAR SWEEP (`classifyMoves` on every changed line, before vs after — `<f86a239c scratchpad>/movesweep.mjs`) and the WAVE GATE'S FINDINGS DIFFED AGAINST HEAD (new `C3 · is this clause historical?` and new `A13` rows are the tell). WHERE 42d AND THE FREEZE COLLIDE, THE FREEZE WINS. The recorder-blind pin (`composeStateProse`, 2756) moves on a face's OPENING WORDS in a blind-roster pool, in both directions, non-additively — bisect it (splice per pool, then per line; ~20 s a run), measure candidate wordings, never re-record when a lawful wording holds. `grain store` in a SPINE is ruled per spine by `orderIdOf`: safe where the spine already has no LEVEL1 order id, refused where it would lose one.

**THE STATE OF DS-DEF-2:** 24 of 26 pools written and cut to the standard; 78 variants / 572 faces (the register unmoved through all four batches); two pools NEVER WRITTEN — `Internal Security: detention without process` and `Disasters & Famine: granary, NO medical provision` (spine-only at `f2da5a3ee` and now). Four inherited C4 reds (`settled, defenses beyond the need` · `militia only` · `walls with citizen militia` · the no-reserves spine pair) byte-identical since before the re-cut and STILL THE CHAIR'S TO RULE. EIGHT 42b sharpenings across the four batches await refutation (four in batch 1's body + one in the re-open; four in batch 4's body).

**⭐⭐⭐ STATE AT 09-14 ~22:1x (refreshed by the Fable 5.1 chair): THE SCRIBE IS BUILT THROUGH W4 — DARK, UNVERIFIED LIVE.** `lane-scribe` `9a8ff7280`, sealed `refs/preserve/scribe-w4-2026-09-14` (W3c `f350d7ed5`, W3d `3def8f219`, W3b `fdc980f4c`, W3a `2f63200d8`, W2 `544ae0339`). W4 landed and chair-verified (523 + 3,022 vitest, pin 2756, 53 Deno): REDRAW (a DM-only button, lit only; the prior render kept in the past lane as redone); THE EPOCH RECORD COMPUTED AT ADVANCE TIME (ruling 21 — W2 had sent `null`; the tab-free card is the ground, records compose across un-opened advances, park→read→clear→undo→redo traced; one deferred edge on the paused-advance resume path, never a false record); DAILY LIFE BY DEFAULT as five beat units from the tab's own offline generator (dark page byte-identical); THE DM'S SURVEY NOTES (the response's verdicts, dropped since W2, now landed as receipts). RUN 5 closed the keyless measurement (84–89 % ship, ~0.5–0.7 % contradict across four runs). W5 = THE TIER PROPOSAL for the owner's signature — drafted next from a recon of the entitlement and credit code. NOTHING FURTHER IS BUILDABLE WITHOUT THE OWNER'S FIVE ACTS (the key first). THE LONG TAIL awaits the owner's word on the order (the chair's proposal: the ten secrets → the money path to the webhook → catalogue hygiene; items 1–14 as one sitting document). THE REWRITE STAYS BANKED. Diary: `$SC/kit/RESUME-NOTE.md` 20:3x → 22:1x.

**⭐⭐⭐ STATE AT 09-14 ~19:3x (refreshed by the Fable 5.1 chair): THE LONG TAIL IS ENUMERATED AND ORDERED — `$SC/kit/briefs/LONG-TAIL-2026-09-14.md` (29 readers over every record the estate keeps; 1,875 raw rows → 58 distinct → 43 OPEN, 23 OWNER-GATED, 15 dropped as done with evidence).** ITS SHAPE: (1–14) the owner's decisions — the Scribe's five (key · db push 201/202 · price · item 13 · flag), the stale checkout, THE DEPLOY GATE (production at migration 121 vs ~200; the record says 188, not 198, deletes real content at push), THE LEGAL SITTING (no ToS/privacy/ownership clause; trademarks; GPL and clean-room postures; notices), the provenance human acts (23 unlicensed images, heightmap attribution, an export shortcut), the unanswered 18+1-item owner batch (§187), the persisted-shape rows, the Stripe console to-do (a price secret before the Surveyor price), the culls/parks, the Fable retrovalidation call; (15–26) THE ENDGAME in the records' order — the ten secrets (one breaks the first sale) → the money path, webhook before any refund → catalogue hygiene (a live deity-doctrine violation: divine healers keyed as magic) → the six display trains → the last same-seed wave then THE GOLDEN FREEZE → the trailing schema mint → the exhaustive review → the billed review keystroke → THE WALK + one regen → THE TERMINAL SOAK → THE TUNING SIGNATURE last → identity, scrub, PUSH; (27) the Scribe's remaining waves; (28–43) the debts and backlogs (the gate that cannot go green; the instrument estate's debt; 21 reader-without-writer cases; the corpus's dark half — 64 of 68 blocks unmounted; ~102 declared engine waves; logistics/vessels; the political/treasury programmes; the post-launch map module; unexecuted security verification; the landing ladder 3 of 11; the relationship chronicle; recorded-not-fixed engine defects; the voice guard's blind spot; the record's housekeeping; the dead-code residue). THE VALIDATION QUEUES: one pass ever on the older (33 rows), 147 unruled; the retrovalidation sitting closed PARTIAL. THE CHAIR'S PROPOSAL, awaiting the owner's word: start the non-gated endgame items in order (the ten secrets → the money path to the webhook → catalogue hygiene) while the Scribe finishes; put items 1–14 to the owner as one sitting document, cheapest first. THE SCRIBE: W3c landed and chair-verified (`lane-scribe` `f350d7ed5`, sealed `refs/preserve/scribe-w3c-2026-09-14`): the static table's desk-local reads resolved from the recipes (68 path · 49 derived · 8 unresolved of 125; the pinned town's unreadable field rows 42 → 8); RUN 5 (`wf_00f13b26-496`): Opus 87 % / Sonnet 84 % / 12 contradicting lines — FLAT against RUN 4 (89 / 85 / 10) within draw variance; RULED 20:3x: THE SCRIBE WITHOUT A KEY IS AT ITS CEILING (84–89 % ship across four runs of one grid), no further simulation before the live pilot; next are the owner's five acts (the key first), W4 the surface, W5 the tier proposal. THE REWRITE STAYS BANKED.

**⭐⭐⭐ STATE AT 09-14 ~18:4x (refreshed by the Fable 5.1 chair): THE PROSE REWRITE IS BANKED ON THE OWNER'S WORD (~17:2x: "bank the prose rewrite and go straight to the rest long tail chain") — DEF-1's packets kept, `rewrite/args-DEF1-v3.batch1.resume.json` written, nothing resumes without the owner; THE LONG TAIL IS BEING ENUMERATED by a 30-agent workflow over every record the estate keeps (the owner-decision queue's 32,530 lines in 11 parts, both Fable validation queues, the roadmap, this handoff, the diary, the plan, the manual, the design ledger, the charters, the backlogs, the memory folds) into one ordered chain (`$SC/kit/briefs/LONG-TAIL-2026-09-14.md` when it lands). THE SCRIBE CONTINUES TO COMPLETION: W3d landed and chair-verified (`lane-scribe` `3def8f219`, sealed `refs/preserve/scribe-w3d-2026-09-14`: the page's machine lines in the writer's turn; `town.bodies` seats the factions/blocs/parties the page names; the RECORD/ROSTER tests grant what the pool's own corpus line names; three card defects pinned as REPORT caveats). RUN 4: **Opus ships 176/198 pools (89 %), Sonnet 168/198 (85 %); 10 contradicting lines in ~1,400 (0.7 %)** — the arc over one afternoon: 26 % (a licence reading) → 84 % (the owner's non-contradiction standard) → 89 % (the card fixed), every step measured without a key. Opus stays the writer. What remains is tier 0's craft column shipping as WITHHELD (ORDER · Q · C3), Q because the static table's `reads` are desk-local and 70 % unreadable — W3c (`brief-scribe-W3c-the-reads-resolve.md`) dispatches next; then the live pilot on a key, W4 the surface, W5 the tier proposal (owner-signed). CORPUS FINDINGS FOR THE BANKED LANE: DS-DEF-2 `full legal chain` face 5 names a garrison on a garrison-less town; `STALLED` fires on all-null reads; `structure null` beside a Dangerous badge; `flagDriven count zero` on an occupied town; `WALLED-STRAINED` at a 0.98 gate. Diary: `$SC/kit/RESUME-NOTE.md` 17:4x → 18:4x.

**⭐⭐⭐ STATE AT 09-14 ~16:5x (refreshed by the Fable 5.1 chair): UNDER THE OWNER'S NON-CONTRADICTION STANDARD THE SCRIBE SHIPS FOUR PAGES IN FIVE.** The owner clarified at 14:4x that the Scribe is held to NON-CONTRADICTION, NOT NON-INVENTION (the 09-12 corpus law; a practice, a motive, a custom or an absence on an undecided field is flavour and ships) — RUN 2's 70 % refusal had been a licence reading. W3b landed and was chair-verified (`lane-scribe` `fdc980f4c`, sealed `refs/preserve/scribe-w3b-2026-09-14`; rulings 32–36): the card says decided / not-decided / UNREADABLE-BY-THIS-CARD per field; a pool is unwriteable only when world-only on a headless town (the pool key is the decided fact); a refused FACE falls back alone to the corpus face at its seat (`PATCHED`); verbatim corpus rows skip the second reader; the reader asks SIX contradiction tests (field · page · roster · model · record · forecast). ⭐ A FINDING WITH ITS OWN CHARTER: 88 of the static table's 125 field paths are the desks' LOCAL names as the wiring census spells them and cannot resolve on a settlement — the card is 70 % blind on fields; W3c (`brief-scribe-W3c-the-reads-resolve.md`) resolves them from the desk-read recipes and is the largest coverage lever left. RUN 3 (16 cells × Opus + Sonnet, 64 seats): **Opus ships 166/198 pools (84 %), Sonnet 155/198 (78 %); 28 contradictions in ~1,400 lines; 6 patched; no forecast, no model-denial.** What remains is the card's: 24 of 28 contradict a machine line the writer never saw (the page rows go back into the writer's turn) and the roster does not list the factions the pool fills name — W3d is written and dispatches next. Opus stays the writer. THE CORPUS IS PAUSED on the owner's 12:05 word (DEF-1 batch 1 resumable from its packets) and is the free/paid tiers' release condition. Skills: `.claude/skills/{corpus-gate,scribe-sim}`; workflow `.claude/workflows/scribe-sim.workflow.js` (re-cut for the six tests). Diary: `$SC/kit/RESUME-NOTE.md` 15:1x → 16:5x; the report `$SC/kit/briefs/SCRIBE-SIM-2026-09-14.md` RUN 3.

**⭐⭐⭐ STATE AT 09-14 ~14:2x (refreshed by the Fable 5.1 chair): THE OWNER RE-FRAMED THE TWO PROGRAMMES AT 13:2x — THE SCRIBE IS THE AI NARRATIVE LAYER AND MAY BE A PAID SUBSCRIPTION TIER OF ITS OWN; FREE AND PAID GENERATION USE THE FULL CORPUS ONLY UNTIL THE FULL REWRITE IS FINISHED; THE SCRIBE CONTINUES TO COMPLETION, DARK.** The corpus rewrite is PAUSED on the owner's 12:05 word (DEF-1 batch 1: Mark + both writers done on all four readiness pools, selector not started, dock `laneRW-DEF1` at `dd79991f5` porcelain 0; resume ONLY on the owner's word from `$SC/kit/rewrite/args-DEF1-v3.batch1.resume.json`, manual 6.5a) — and "until the rewrite is finished" is now the free/paid tiers' release condition, so the pause is flagged at every collection. THE SCRIBE THIS HOUR: (1) SIMULATED WITHOUT A KEY — `$SC/kit/scribe-harness/simulate.mjs` builds the exact prompt and a fresh-context agent is seated as the model; the first run found the product and the harness built TWO prompts (the harness's fork had been measured, never the product's), an untold move-order rule behind four of five drops, and a plausible-invention class no code arm can see. (2) W3a LANDED AND CHAIR-VERIFIED (`lane-scribe` `2f63200d8`, sealed `refs/preserve/scribe-w3a-2026-09-14`; rulings 25–31): ONE prompt builder `src/domain/prose/scribeBrief.js` bundled for Deno and imported by the harness; two cached system blocks (the exemplar pack in tree with four quoted passages paraphrased); the card carries each spine's order and licence; the Q arm's card ground reads field paths with the synonym vocabulary; TIER 1 WIRED (seven questions). Two bugs found on the way: the W2 brief indexed a deduplicated source SET per face, and the one ratified synonym row resolved to an empty vocabulary. (3) RUN 2 on the whole grid (saved workflow `.claude/workflows/scribe-sim.workflow.js`; 16 cells × Opus + Sonnet; 64 seats): tier 0 keeps 91–95 % and the order arm NEVER fired (ruling 27 measured); the second reader refuses ~70 % of units AND IS RIGHT — the writer fills every gap a thin card leaves (absence on null reads, origins, contests, practices behind booleans, verdicts the town's rows deny); final shipped Opus 26 %, Sonnet 30 %; a unit dies on one weak face. W3b is dispatched to cure it at the writer's bars, null = unknown, face-level fallback, verbatim-corpus exemption, whole arm reporting. SKILLS AND BUILD FILES (the owner's 12:5x grant): `.claude/skills/corpus-gate`, `.claude/skills/scribe-sim`, the workflow above — committed here. OWNER-GATED, NEW: the Scribe TIER design (W5) · the two readiness ladders on one shipped page (design §12 item 15) · `WALLED-STRAINED` firing at a 0.98 gate (a corpus threshold). Diary: `$SC/kit/RESUME-NOTE.md` entries 12:05 → 14:1x; the report `$SC/kit/briefs/SCRIBE-SIM-2026-09-14.md`.

**⭐⭐⭐ STATE AT 09-14 ~10:4x (refreshed by the Fable 5.1 chair): THE CLARITY LINE-EDIT IS COMPLETE ON THE WHOLE SHIPPED CORPUS, AND NOTHING IS IN FLIGHT.** All five leaves are cut, read by an independent audit, cured by a chair's commit, every instrument green and the recorder-blind pin at 2756 on each: DS-DEF-2 `laneRW-DEF2` `dd79991f5` · power `lane-clarity-POW` `77dbccb01` · war & faith `lane-clarity-WARFAITH` `b8a7de5e5` · general `lane-clarity-GENERAL` `3a5a0f6c6` · economy + stressors `lane-clarity-ECONOMY` `a6db88752` (the audit found 3/319 moved claims; the chair's cut in three commits; the granary synonym row R-ECO-4 signed with ten of the thirteen `granary` spines re-cut and the three INSTITUTION spines kept, because INSTITUTION is a literal-noun list in `moveGrammar.js`; the shipped-corpus A0b reading in `proseComposed.walker.test.js` given the ratified vocabulary its two sibling consumers already passed — the basis shift DECLARED at the call site, the ceilings 1282→1250 / 13→11 to be tightened by a later car on a second measurement; the census walker's ratification gate re-grounded from "contains a dot" to "the committed register reads it"), each sealed `refs/preserve/clarity-<leaf>-2026-09-14`. The Scribe stands where the 10:1x paragraph below leaves it, at `lane-scribe` `544ae0339` after the fence car. CHAIR-OWNED DEBT, NAMED: `proseWiringCensus.walker.test.js` carries two reds ((e) THE FENCE · custom-content reachability, 30 vs 25) that reproduce at every clarity base and sit in no lane's five. NOTHING CAN START WITHOUT THE OWNER: W3 the pilot needs `ANTHROPIC_API_KEY`; the v3 face-writing queue (DEF-1 onward) needs the owner's word to run the workflow; the owed acts (db push 201+202 · the 5-credit SKU · item 13 · the archive/pins retirement · the stale checkout · the flag last) are unchanged. Diary: `$SC/kit/RESUME-NOTE.md` entries `+ 09-14 ~10:3x` and `+ 09-14 ~10:4x`.

**⭐⭐⭐ STATE AT 09-14 ~10:1x (refreshed by the Fable 5.1 chair before the next window): BOTH PROGRAMMES ARE FAR ALONG AND EVERYTHING IS SEALED.** THE SCRIBE: W0 (the town card) · W1 (the refuter library, 42 arms; the epoch record; the Deno bundle; the pilot harness in `$SC/kit/scribe-harness/`) · W2 5 of 6 (the artefact + five lifecycle cures + `FLAGS.scribe` default false + the FINITE-SEMANTICS pin · the undone/redone past lane · the OPEN trigger · the composer switch · the edge function `scribe-render` with the credit machine, migrations `201_scribe_prose_dm_full_strip.sql` + `202_scribe_claim.sql` WRITTEN NEVER APPLIED) — all in `$SC/kit/lane-scribe`, tip `1a2022cf3`, sealed `refs/preserve/scribe-w2-2026-09-14`; commit 6 (the owner-ordered retirements of the narrative overlay and its SKUs) is DEFERRED TO W5 by ruling 20 with measured blockers (the chronicle's only writers are narrative-family; the SKU pins ride shipped migrations). The live model call has NEVER run (no key). THE CORPUS (never held — the owner: the corpus is the hopeful final version; the Scribe is the intermediate for cash flow; when the corpus is done the Scribe moves to a higher paid tier): the clarity line-edit is landed and READER-AUDITED and chair-cut on POWER (`lane-clarity-POW` `77dbccb01`), WAR & FAITH (`lane-clarity-WARFAITH` `b8a7de5e5`), GENERAL (`lane-clarity-GENERAL` `3a5a0f6c6`), DS-DEF-2 (`laneRW-DEF2` `dd79991f5`), each sealed under `refs/preserve/clarity-<leaf>-2026-09-14`; the LAST leaf (economy + stressors + population, `lane-clarity-ECONOMY`) is in flight. THE LAW THE FOUR AUDITS WROTE (rulings in `$SC/kit/rewrite/clarity-audit-*.md` and `SCRIBE-RULINGS.md` 1–24): every instrument green while a reader finds moved claims — power 32/189, warFaith 26/288, general 7/353 — so THE READER AUDIT IS PART OF THE GATE; certainty, quantifier and scope are claims; `will` is a modal rendered `is going to`/`is willing to`/`refuses to`, never a bare will and never a present habitual; never delete a causal link, convert it; 42b reaches metaphor; the generic he is house convention. INSTRUMENT DEBT: three authoring forms the kit tools could not see (dm-only tag — widened; the inline pool — widened; the cell-run inline pool at annex line 6285 — three variants reach NO generator/census/gate, a chair-gated repair); the 24-word bar's counting convention (ruling 23); the X arm on 42b figures; the A6 role-slot gap. THE OWNER MUST: `db push` 201 + 202 · sign the 5-credit render SKU · a model key for the pilot · rule item 13 (past epochs into the chronicle) · rule the narrative archive/pins retirement · reconcile the stale checkout · light `FLAGS.scribe` last. NEXT: the economy audit + cut → W3 the pilot (needs the key) → W4 → W5.

**⭐⭐ A SECOND PROGRAMME OPENED 09-14 ~05:0x — THE SCRIBE (AI-written dossier prose, once per settlement, refuted by instruments, persisted with the world): ARCHITECTED at `docs/DESIGN_SCRIBE_GENERATION_TIME_PROSE.md`, NOT BUILT; W0 (the town card, pure code) is the first lane when the owner says go; twelve owner-gated decisions in its §12. ⛔ THE MAIN CHECKOUT IS STALE AGAINST HEAD (4,236 deletions; Surveyor/BYOK and migrations 137–195 exist only at HEAD) — read the repo with `git show HEAD:`, never the checkout, until the owner reconciles it. THE CLARITY PROGRAMME runs in parallel: `$SC/kit/briefs/brief-clarity-all-prose.md` (the leaf order and THE GATE) · the power lane in `$SC/kit/lane-clarity-POW` (`git log` there shows the landed batches; a dirty worktree is a dead lane's stage — seal, verify, never restore blindly).**

**NEXT, IN ORDER (one lane at a time):** (1) the 42b refutation of the eight lines — an Opus refuter per line against its card, the cure by name; (2) the chair's ruling on the four inherited C4 reds ("a totality over an open column" on `every household` / `each one that stays in` — cutting the totality moves the claim; the freeze forbids it; either the arm is amended for landed prose or the lines are re-drafted, and that is a Fable sitting's, §7.2); (3) THE WHOLE-BLOCK CAR: the untouched faces + the shape (charter cars 1+2) over all 24 pools, under the freeze and the two instruments; (4) the two unwritten pools under v3 (Mark · Draft · Select · Refute · Cure); (5) the instrument-debt car (`brief-instrument-debt.md`: five suites red at the dock HEAD since the early cars); (6) DEF-11 re-read → DEF-1 → the consist → §920. THE OWNER-GATED LIST IS UNCHANGED.

**DEATH CLASSES, this session:** the chair seat changed model mid-session (Opus → Fable 5.1) with no loss; the previous session's death at ~02:13 09-14 cost only the checkpoint entries (four batch commits unrecorded in the note — now recorded). If this session dies: the dock is clean; nothing is in flight; start at NEXT (1).

## (superseded 09-14) PICKUP AT §919.6 AND AFTER — **THE RE-CUT IS VALIDATED AND THE MACHINERY IS RE-CUT UNDER IT: the Fable sitting (§919.6 `b9c3fe322`) found the non-contradiction re-cut BETTER and HOLDING; brief ADDENDUM 18 then grew to FORTY RULINGS (2026-09-12 21:3x → 09-13 13:3x) that rebuilt how the dossier is WRITTEN, and the first v3 pool is through the whole pipeline in the DEF-2 dock** (product `claude/composite-r4` still `f73bdbf16`; ledger through `44f7dc054`; the DEF-2 dock at `48b58031a` + cars 18n/18o in flight).

**READ FIRST, in this order:** (1) `docs/rewrite-retro-2026-09-12/THE-LAW-CONSOLIDATED-2026-09-13.md` — the whole law of the prose in plain words (twelve sections; §10 the chair's reconciliations; §10b the research reconciled); (2) `docs/rewrite-retro-2026-09-12/RESEARCH-RECONCILIATION-2026-09-13.md` — the prose research read against the law by five reviewers, rulings 31–39, the items put to the owner; (3) `docs/rewrite-retro-2026-09-12/verbatim/BRIEF-ADDENDUM-18.as-signed.md` — the forty rulings with every edge and reversal, binding; (4) `docs/REWRITE_RECUT_PROGRAM_PLAN.md` — waves 0–3, the car list 18a–18p, the deferred and owner-gated ledgers; (5) `docs/OPUS_CHAIR_MANUAL.md` §6.5a–c — resuming a v3 block from the packets on disk, the seats, the hand-edited register.

**THE LAW IN ONE BREATH.** One archiver compiles the dossier. A fact the engine holds is stated BARE on the document's own authority (ruling 40: the page never says 'the survey'). Every ACCOUNT names its source through a ROLE drawn by seed from the town's own institution rows (25), the roster's named offices included as titles. The public witnesses what everyone saw and may be wrong about what it means (28). A compromised role conceals on the symptom of its own secret, seven years in ten, seeded on the year (26). Two sources may disagree and the archiver may weigh in without ever closing the question, because the game master decides what actually happens (22). The unit on the page is two or three sentences (30); restraint is the level (14); plain, never casual (20); the senses once per page (31); who pays, once per variant (32); no forecast (33); no em dash, bang, digit, semicolon. The notebook is the game master's, third person, two sentences, a range of feeling, and the one place the dossier settles a question — where the engine holds the secret (17, 22).

**THE MACHINERY.** `docs/rewrite-kit/rewrite-block-v3.workflow.js` (= `$SC/kit/rewrite/`): Mark (Opus: the card — what would be false, who may speak, what a face may not deny, the covert symptoms, the sibling rungs) → Draft (writer 1 OPUS under the telling particular, writer 2 FABLE under the social position; one FABLE SELECTOR picks one face per seated source with the veto list, the assembly vetoes and the rations; the gate refuses only mechanics and prints the block fingerprint) → Refute (OPUS; the fingerprint read before PASS/DULL) → Cure (OPUS; one re-refute; the chair cuts by name after). Resume from disk: `args.resume.{markedDirs,candidateDirs}` (manual 6.5a). Every join on a packet dir goes through `dirKey` (two deaths taught it). The face-count register is hand-edited (6.5c). The gate runs the FULL per-commit list.

**THE DOCK** (`$SC/kit/laneRW-DEF2`, a worktree of the product): cars 18a–18m landed (`6a6c2ddf3` … `48b58031a`): the band gate retired; `{defmaterial}`; the marker's instruments; sources + pairs on faces; the power gate (a power only where an institution row backs it — 532/768 towns lose a faction; the noble houses are a CATALOGUE GAP, car 18h); FACE_PIN derived from the vocabulary; the archiver's weighing row + the compound pair's joint; the card's (2b)/(2c); ROLES (`src/data/institutionRoles.js`); the COMPROMISED role. The first pool (walls with NO force): draft `180acd14d` → cure `569e01793` → the chair's cut `e075b0769`; rendered page in the packet. In flight: 18n/18o (the witness + the public; opener and close classes; the cap; the page-state fix; the no-forecast/no-self-citation E2 arm; the measures; bytes re-priced). Chartered: 18p (a typed duration slot from a recorded field), 18h (manor rows), the wiring rows (W-a garrison pay on a garrison-less key; W-b the Armed forces heading over the watch), wave 2 (the page refuter + region arm — EARNED: two page-level findings on landed blocks already), wave 3 (DEF-2's 26 pools under v3), then DEF-11 re-read, DEF-1, the consist, §920.

**THE SEATS (ruling 24) and THE MEASUREMENT.** Fable vs Opus drafting on one pool: the same grade (shared-lens 6:4 Fable, two near ties); the seats fixed as above; the selector's SEATS tally runs every pool; flip: Fable 2:1 across two pools → both Fable; the weekly quota within a day → both Opus, Fable keeps the selector.

**THE OWNER'S DELEGATION (09-13 14:0x, verbatim): "okay continue with the work all the way. i give all permissions you need now or would need in the future and leave all decisions to you, fable."** Recorded as TOTAL delegation over the programme's judgment; the chair keeps the PUSH, any DEPLOY, any MIGRATION and any DATA DELETION off the table by its own judgment (a code revert cannot undo them) and records every decision vetoably; the owner-gated list stands: the criminal-capture badge, the GM edit path, `deriveCriminalStructure`, migration 185's frozen block (grazed in the dock by 18e; NOT deployed), the custom `roles`/`backsPowers` manifest fields, retiring an unbacked power's data, the walk.

**DEATH CLASSES seen this programme:** the weekly limit (09-12 07:36), the session limit (09-13 00:3x, three implementers), a reboot (09-12 20:08, the kit re-materialised from the autosave seal). The checkpoint law is in every brief. If this session dies: `$SC/kit/RESUME-NOTE.md` (newest entry first, above the 09-10 anchor) is the diary; the memory index's START HERE row; procedure 6.5a.

## (superseded) PICKUP AT §919 — ****THE REWRITE'S FIRST CAR LANDS — the index-stable draw signed and landed, passage shapes as a licensed fourth draw, the taste's instruments and the wave's gate in the product, zero reader-facing bytes; next is 8b, the defense desk rewritten block by block**** (`29ec62425` -> `f73bdbf16`).

Product `claude/composite-r4` moves 29ec62425 → the CAS above (twenty-seven cars; sealed `landing-rewrite-8a-2026-09-09`); the receipt, the fold and the kit are in the seal. THE TRAINS (`arch-prose/TRAINS.md`): MEASURE ✅ → SEAM ✅ → LIGHT ✅ → the TASTE + the SITTING ✅ (§918) → **REWRITE 8a ✅ → 8b…** (one writing workflow per annex block in its own dock — `rewrite/rewrite-block.workflow.js` with `rewrite/block-args.mjs`; DEFENSE first, DS-DEF-11 then DS-DEF-2; three blocks at a time; Opus drafts · Opus refines · Fable refutes every set · a Fable judge applies SITTING §T.4/§T.5 per variant; Shift 1 declared per block on the classifier's print; the blocks land as consists — §920 for defense) → general → economy → power → stressors → warFaith (after its two wiring cars) → WAVE → TURNS → CUSTOM-PROSE → SURFACES-EVENT → SURFACES-DM → CAPACITY → the WALK (the owner's; STOP) → the soak → TUNE-FREEZE → the beta. Owed to the OWNER: the push; the walk; the seven lost raw exemplar texts (the provenance budget's other seven registers); the ladder route and the crier questions; the causal register and the cause digest when asked; the DM-full SQL twin; the empty-record roster shape; R-I. Owed to the chair: 8b's launch; the fixture-shape arm re-armed at 8b's first block; a second DRIFT seed sweep (CAPACITY); `$SC/prose-research` under git.

## (superseded) PICKUP AT §918 — **THE TASTE AND THE SITTING — the two arms tabled and verified, the sitting signed, the staffing put to the owner on the numbers, the gate's blind spot measured, the REWRITE's car 8a chartered; ledger-only** (`29ec62425` unchanged).

Product `claude/composite-r4` = 29ec62425 — UNCHANGED by this act (§918 is a ledger landing: zero product bytes; the taste lives in two sealed docks, `refs/preserve/taste-2026-09-09-armA` = f07b98529 and `…-armB` = 66d5b6e08, never landed; the table `taste/TASTE-TABLE.md`, its verifier `taste/TASTE-TABLE.verify.md`, the sitting `s12-sitting/SITTING-RULINGS-912.md` §T and the brief `briefs/brief-REWRITE-car8a.md` are in the chair's kit `$SC`, autosaved to `refs/preserve/chair-tools-2026-09-05`). THE TRAINS (`arch-prose/TRAINS.md`): MEASURE ✅ → SEAM ✅ → LIGHT ✅ → the TASTE ✅ + the SITTING ✅ (§918) → **REWRITE** (8a: the kernel's index-stable draw, passage shapes, the taste's instruments, the wave's gate cured — zero reader-facing bytes; then 8b… one desk section per writing workflow, DEFENSE first, on the two phases of Part B §21 with an EXHAUSTIVE Fable refute of every set; the staffing is the owner's row 26 — the chair recommends Opus drafts · Opus refines · Fable refutes) → WAVE → TURNS → CUSTOM-PROSE → SURFACES-EVENT → SURFACES-DM (the pen line) → CAPACITY → the WALK (the owner's; STOP) → the soak → TUNE-FREEZE → the beta. Owed to the OWNER: row 26 (both prices in §T.3); the push; the walk; the seven lost raw exemplar texts; the ladder route and the crier questions; the causal register and the cause digest when asked; the DM-full SQL twin; the empty-record roster shape; R-I. Owed to the chair: 8a's dispatch and skeptic pass; the side-by-side of the interested fact and the shape table at 8a's fold; `$SC/prose-research` under git.

## (superseded) PICKUP AT §917 — **THE LIGHT TRAIN LANDS — the living-content dial lit for every new world on the owner's word, the loader's caller wired, the promise proven by bytes, no migration; next is the taste and the sitting, then the rewrite** (`8961388ce` -> `29ec62425`).

Product `claude/composite-r4` moves 8961388ce → the CAS above (twenty-two cars; sealed `landing-light-2026-09-09`); the receipt, the fold and the kit are in the seal. THE TRAINS (`arch-prose/TRAINS.md`): MEASURE ✅ → SEAM ✅ → LIGHT ✅ → **the TASTE** (dock `laneTASTE`; the mechanics car, then the writing workflow's two arms — Opus vs Fable refiner from one draft, Fable refuters blind, the distribution table) + **the SITTING** (`SITTING-AGENDA-car7.md`: S1–S20, the §13 rows, S2/S3, the provenance budget, the `granary` kind, the wiring cars, the thread, the secret as a pen line, passage shapes, the wave's staffing on the taste's table) → REWRITE (two phases: into the bands, then one refinement toward the ceiling; never trim) → WAVE → TURNS → CUSTOM-PROSE (reads the lit roster for TEXT only) → SURFACES-EVENT → SURFACES-DM → CAPACITY → the WALK (the owner's; STOP) → the soak → TUNE-FREEZE → the beta. Owed to the OWNER: the push; the walk; the seven lost raw exemplar texts; the ladder route and the crier questions; the causal register and the cause digest when asked; the DM-full SQL twin; the empty-record roster shape; R-I. Owed to the chair: the taste; the sitting; `$SC/prose-research` under git.

## (superseded) PICKUP AT §916 — **THE SEAM TRAIN LANDS — the composer built and every desk routed through it with zero drift; the threat block lit in the census; the schema with a stable id per pool and a shift register; 66 arms; the holder census with zero interested facts at birth; next is LIGHT beside the taste and the sitting** (`3b22b5c56` -> `8961388ce`).

Product `claude/composite-r4` moves 3b22b5c56 → the CAS above (thirty-eight cars; sealed `seam-2026-09-08`); the receipt, the fold and the kit are in the seal. THE TRAINS (`arch-prose/TRAINS.md`, order amended 09-08 for the owner's "land everything lit on"): MEASURE ✅ → SEAM ✅ → **LIGHT** (a lane beside the chair's taste + sitting: the roster loader's caller · the dial at 2 · THE PROMISE by bytes on 525 + 768 · no migration, every pre-lighting world is test data · the three import-boundary holes' arms lit; `briefs/brief-LIGHT-car1.md`) → the TASTE (dock only; `brief-SEAM-car6-taste.md`) + the SITTING (`SITTING-AGENDA-car7.md`: S1–S20, the §13 rows, S2/S3 read out, the provenance budget, the `granary` kind, the wiring cars priced) → REWRITE (Shift 1 with the index-stable draw) → WAVE → TURNS → CUSTOM-PROSE → SURFACES-EVENT → SURFACES-DM (the DM face of an interested holder lives here) → CAPACITY → the WALK (the owner's; STOP) → the soak → TUNE-FREEZE → the beta. Owed to the OWNER: the push; the walk; the seven lost raw exemplar texts; the ladder route and the crier questions; the causal register and the cause digest when asked; the DM-full SQL twin; R-I. Owed to the chair: the sitting; the taste; `$SC/prose-research` under git.

## (superseded) PICKUP AT §915 — **THE MEASURE TRAIN LANDS — the composed-prose sequence's first three cars: the census on the selecting branch, committed as data; a balanced 768-town sample; the relation vocabularies measured apart; the 525-town manifest and its classifier; the variety baseline; the byte ratchets; zero reader-facing bytes; next is SEAM** (`8522a17b2` -> `3b22b5c56`).

Product `claude/composite-r4` moves 8522a17b2 → the CAS above (twenty cars; sealed `landing-measure-2026-09-08`); the receipt, the fold and the kit are in the seal. THE TRAINS (`arch-prose/TRAINS.md`): MEASURE ✅ → **SEAM** (ARCH 3a the composer seam unreachable — `drawFace` with the no-hash short-circuit, `composeStateProse.js`, the six candidates leaves, the import fence and the locale ban; 3b–3g one desk each through the composer with empty candidates, zero drift on the manifest after every stage; 4 the schema keys-only — `poolMeta` render half, `vid`, the three leaves with floors, the SHIFT REGISTER; 5 the new arms A0–A12 and C7) → the TASTE (dock only) + the SITTING (S1–S20; the alias table's first ratifications; the numbers) → REWRITE (Shift 1 with the index-stable draw) → WAVE → TURNS → LIGHT + CUSTOM-PROSE (the custom schema widened first) → SURFACES-EVENT → SURFACES-DM → CAPACITY → the WALK (the owner's; STOP) → the soak → TUNE-FREEZE → the beta. Owed to the OWNER: the push; the walk; the seven lost raw exemplar texts; the ladder route and the crier questions; the causal register and the cause digest when asked; the DM-full SQL twin; R-I. Owed to the chair: the alias ratifications at car 7; DS-DEF-2's wiring car; the desk-fact census for mounts-per-fact; `$SC/prose-research` under git.

## (superseded) PICKUP AT §914 — **THE INSTRUMENTS LAND — the INSTR-912 consist joins the product: the two walkers, the loaders, the institution table, the measures, and the owner's two-way wiring census, refuted twice and cured twice, zero product bytes; next is the MEASURE train (ARCH cars 0–2), then SEAM, then the taste and the sitting, then the REWRITE** (`f3ab08f51` -> `8522a17b2`).

Product `claude/composite-r4` moves f3ab08f51 → the CAS above (eighteen cars; sealed `landing-instr-2026-09-08`); the receipts, the two folds (`skeptic-instr/`, `skeptic-instr2/`) and the kit are in the seal. THE ARCHITECTURE OF RECORD is `arch-prose/ARCH-COMPOSED-PROSE-v2.md` (+ the chair's §16; memory `the-composed-prose-architecture-v2-2026-09-08`) and the trains are `arch-prose/TRAINS.md`: **MEASURE** (ARCH 0 the census EXTENDED — `reads`/NARROWS, `absent`, `covert`, `objectClass`, `attach` derived, the RATE corpus 192 cells × 4 seeds with every figure PER TIER, the relation extraction with direction, the k = 0 count, the committed sha-stamped JSON; ARCH 1 the DRIFT manifest 525 × 2 audiences + the classifier WORDING-ONLY/ADDITIVE/RE-INDEXED/REPLACED + the VARIETY corpus 4,200 towns + the duplicate-unit baseline + the PAIRED-TOWN arm; ARCH 2 the prose byte ratchets) → **SEAM** (3a–3g the composer dormant, one desk each, zero drift; 4 the schema keys-only + the SHIFT REGISTER; 5 the new arms A0–A12, C7) → the TASTE (dock only) + the SITTING (S1–S20) → **REWRITE** (8a…8n, Shift 1 — the index-stable draw SIGNED under the delegation, SITTING §N) → **WAVE** (9, 9b) → **TURNS** (10, 11, 12) → SURFACES-EVENT → SURFACES-DM → CAPACITY → the WALK + one regen → the soak → TUNE-FREEZE → the beta. Owed to the OWNER: the push; the walk; the seven lost raw exemplar texts; the ladder route and the crier questions (SURFACES-DM); the causal register (wire or retire) and the persisted cause digest when asked; the DM-full SQL twin; R-I. Owed to the chair: the co-occurrence floor at car 0; `$SC/prose-research` under git; the K.2 rendered/authored pair carried into every V1 figure.

## (superseded) PICKUP AT §913 — **THE LIVING-CONTENT LAW IS WIRED DARK AND ITS SEAMS ARE CLOSED — the L-MAT consist lands: the create boundary out of first paint, the second mint wired at birth with the dial at 1, the roster dropped from the public projection and resolved-or-dropped on every import, undo and reconciliation path, three dial-1 holes closed, a dark build's config mint clamped, and the estate's false sentences about the dial replaced by the true one (lighting it takes generation down — the loader has no caller); +36 first-paint bytes declared; the SQL twin and the marker digest stay the owner's; next is the wave's instrument lane, then CAP-WINDOW-911 and the dossier rewrite** (`3b1c0eaa5` -> `f3ab08f51`).

Product `claude/composite-r4` moves 3b1c0eaa5 → the CAS above (twenty cars; sealed `landing-lmat-2026-09-07`); the receipt, the two skeptic folds (`skeptic-912/`, `skeptic-913/`) and the kit are in the seal. Owed to the OWNER: (a) the DM-full SQL twin `_gallery_dm_full_json` (migrations 121/129) still re-issues both living-content keys server-side on a DM-shared dossier — a migration, owner-gated; (b) R-I / car 6 — no marker rides the persisted receipt until the owner rules, told that the narrow digest is blind in the product and that the roster-only null holds only in the degenerate case; (c) the +36 first-paint bytes, a declared shift (cause untested); the WALK; any push. Owed to the next chair, in order: (1) **INSTR-912** (the wave's instruments — the entry walker, the grammar walker with §912.3's three numbers, the loaders, the derived institution table, the presence measure and the unrendered-facts census, D8's ledger walker; dock `laneINSTR` @ 3b1c0eaa5 → re-cut onto this CAS before its landing, or land as a consist over it) → its skeptic pass → §914; (2) the WIRING CENSUS addendum (owner 19:55) if car 1 did not build it; (3) **CAP-WINDOW-911** (the plateau tripwire aligned to the register's window; zero tuning) and **CAP-SEEDS** (the two-seed cell; owner-signed); (4) **THE WAVE's first register** — the dossier (R1/R2 + the annex) as a Workflow: Fable writers to `REGISTER-CARD.md`, the wired claim set printed per variant, check-pair + the walkers at the gate, Opus refuters sampling, a fold, the chair's ruling — then the chronicle's pools, the news, the DM page, the ladder after the owner's route; (5) the authoring wave from the count; (6) the walk → the soak → tuning → freeze → the beta. R-J's loader wiring is the LIGHTING car's first task when the owner wants the dial lit. Push and the WALK stay the owner's.

## (superseded) PICKUP AT §912 — **THE PROSE RECONCILIATION (S12) IS RATIFIED: Part B (91 rules) and the owner's dossier pass the chair's sitting as corrected after a seven-lens Opus skeptic pass (48 as written · 34 corrected · 9 disposed); the owner's three numbers carry corrected recommendations; 78 rules await signature; four shipped sentences assert facts no table holds; and the latent grammar's variety on today's data is bounded by the FIELDS the blocks hold — next is the taste sample (DS-POW-5, three grammars, two seeds), then the blind DM panel, the instrument cars and the wave** (`3b1c0eaa5`, unchanged)
Product `claude/composite-r4` = 3b1c0eaa5 — UNCHANGED by this act (§912 is a ledger landing: zero product bytes; `RULES-V2-PART-B.md` §0–§14, `sweep/RECONCILIATION-DOSSIER.md` §0–§10, the two specs with their superseded-passages appendices, the seven lens files, the fold and `s12-sitting/SITTING-RULINGS-912.md` are in the kit seal). Owed to the OWNER, in plain words (dossier §0 and §10): the three numbers (the shape ceiling min(1/n + 0.10, 1.5/n); the absence rate per register; the name-opener ceiling one in six against today's one in four); the 78 signatures; the four breaches (the levy line; "the same trades exempt" twice; church land exemptions); the schema acts (a)–(k); the ladder's 376–378 debt lines; the four imperatives; the two crier questions; the war-pool census (definition-dependent, re-taken before any R3 signature); the data question the principal finding raises (twenty of sixty-eight record blocks hold no fact but the town's name). Owed to the next chair, in order: (1) **THE TASTE SAMPLE** per `SITTING-RULINGS-912.md` §F — block `DS-POW-5 :: governing body name`, three variants in V1/V5/V4, both probe seeds (`<5540cfd2 scratchpad>/taste2/`), the pairs file → `node check-pair.mjs $SC/laneB6 <pairs>` → an Opus refuter on U1–U12 + C-pair/C-sibling → findings printed, the owner told "fully passed"; (2) the BLIND DM PANEL (dossier §6.4; the published-gazetteer source is the owner's); (3) the INSTRUMENT CARS (Opus, one build lane): the entry walker (CLERK-LAWS §2, FOUR Brackwater fixtures, the anti-vacuity guard reds on the four live breaches), the B-GRAMMAR walker (MOVE-GRAMMAR §4 as amended, controls for every arm, control 4 at 407/708), D8's `unresolved` class, the derived institution table, the presence-measure arm, the loaders; (4) the WAVE (display-side, in place, claim-preserving, walkers gating at the freeze) → the owner's walk → the launch; (5) when the owner lifts the 12:40 pause: L-MAT-FIX → §913 (`mk-landing-kit.py 913 …`), CAP-WINDOW-911, CAP-SEEDS, the surface-text program. Push and the WALK stay the owner's.

## (superseded) PICKUP AT §911 — **THE HORIZON IS MEASURED: §910's D1 is discharged on a shipped 600-year series, the fixture settles into the realm-load window on this seed, §907's 0.4787 is a SEED fact (a second seed reads 0.6443 under byte-identical `src/`), SOAK-1 is re-priced at ≈ 4.0–4.9 h, and a skeptic pass corrected the receipt before the ledger took it; next are S12's fold and the taste sample, L-MAT's landing (§912), CAP-WINDOW-911, the freeze question and the walk** (`3b1c0eaa5`, unchanged)
Product `claude/composite-r4` = 3b1c0eaa5 — UNCHANGED by this act (§911 is a ledger landing: zero product bytes; the receipt, `capacity-horizon/` and `skeptic-911/` are in the kit seal; `docs/DESIGN_HORIZON.md` amended in place on three price lines, marked CHAIR-AMENDED §911). Owed to the OWNER: three rows re-asked or opened in the §911 row — SOAK-1 at the measured price (≈ 4.0–4.9 h, manual, after the walk; the recommendation stands), CAP-10 reopened (the post-retune re-run ≈ 1.9–2.3 h), CAP-11 NEW the seed grid (recommend a two-seed cell); the WALK; any push. Owed to the next chair, in order: (1) **S12** RUNNING as `wf_8b507f28-6e0` on the owner's 11:15 seat split (six Fable reconcilers, the Opus drafts of three registers judged rule by rule, six Opus refuters, the Opus hunter and critic, the Fable specs and fold) → `RULES-V2-PART-B.md` + `sweep/RECONCILIATION-DOSSIER.md` → the chair's retrovalidation row → the taste sample (one block in two grammars, Fable-authored; `check-pair.mjs` + an Opus refuter before the owner sees it) → the WALK; (2) **L-MAT** in `laneLMAT` (Opus; cars 1–4 landed `442c7f988` `fe8eb3f56` `9a9a7856e` `525ffa99b`, cars 5–6 in flight; car 1's `manualChunks` pin REFUSED by the build) → §912 as ONE consist on a chair gate (`mk-landing-kit.py 912 laneLMAT <tip-full> landing-lmat-2026-09-07 <cars>`; predict → proof → registers → ratchet → after-ratchet → texts → gate → after-cas); (3) **CAP-WINDOW-911** (instrument lane, zero tuning bytes: `capacity_plateau` aligned to the register's last-century window and `final` denominator from one home; declared verdict movement on the 600-year receipt) after §912; (4) the golden-freeze question — the dormancy arm's 600/600 is UNFLOORED until a `generation-corpus-golden` row exists, and the freeze act now waits on the owner's signature (the seed-grid row and the `exact` shape pin's 0.25-soul margin are its inputs), never on a measurement; (5) after S12: the surface-text program (the essay, the Compendium sentence, the successor labels, the mobile nav design). E1 (≈ 50–65 min) and E2 (≈ 1 h 15 m) are priced, not chartered — they precede the tuning sitting. Push and the WALK stay the owner's.

## (superseded) PICKUP AT §910 — **THE CAPACITY ROWS ARE FULLY EXECUTABLE: the soak receipt carries its per-year series, `requires` reads nested paths, the fourth designed row is armed, and the weak zero has no floor a skeptic could find; next are CAP-HORIZON-909 (the 600-year run and the 12-settlement timing probe), L-MAT, the research runs → S10b → S12, and the walk** (`6ebe0ef3b` -> `3b1c0eaa5`).

(`6ebe0ef3b` -> `3b1c0eaa5`)

Product `claude/composite-r4` = 3b1c0eaa5 (sealed `landing-soakhonest-2026-09-07`, 9 cars over 6ebe0ef3b, landed by fast-forward; gate green, 31982 tests; typecheck:ratchet 173 of 173). What landed: the per-year population and died series on the soak receipt (additive on v5), the dotted-path `requires` with the walker reading the observer, the two state-motion bars in one home and `capacity_envelope_30y` armed (roster 12/10), the reachability ratchet at zero with a control, two synthetic register fixtures corrected to the writer's shape and declared, the lighting census at 23665 titles (`6906e1935`), the writer-reach provenance (`3f4914abc`), the census totals 31979 → 31982 (`0a4d59826`) and the capsule (`3b1c0eaa5`).  What it means: `fullInstrument` is TRUE again on every receipt today's writer produces under a lit overlay, so the register door can OPEN on a clean run — the freeze act no longer waits on a schema row; it waits on a run that fires nothing deterministic, and the 30-year lit run fires `capacity_realm_load` at 0.5449 (the 300-year run fired it at 0.4787), which is exactly the reading CAP-HORIZON-909's 600-year run is commissioned to settle (model or window) before anything is re-cut or tuned.  Owed to the OWNER: only what the delegation did not cover — the WALK; any push; the book purchase if wanted (offered, never made); the Kay fingerprint's owned copies; the public labels and copy the surface-text program will bring. Owed to the next chair, in order: (1) **CAP-HORIZON-909** (measurement, ~1 h CPU, zero product bytes, `briefs/brief-CAP-HORIZON-909.md`, dock `laneB6` at this tip) — the 600-year × 4-settlement run under the new writer so `capacity_plateau` can grade it, and the 12-settlement timing probe that re-prices SOAK-1; then **L-MAT** (ruling 8; brief in the kit when written); (2) the research program: dnd r12 + leguin r10 RUNNING in chair session b43943b4 (a new session rebuilds with `NO_TRIAGE=1 CONFIRM_DEAD=1 sh $SC/resume-research.sh dnd leguin` → mk-embedded → two launches); ai r11 BUILT and embedded (the 231-row verify-only pass + the procgen-baseline angle, owner GO 02:20) — launch its scriptPath from `sweep/LAST-RUNS.json` when a slot frees, never rebuild it → `dry-check.py` each → S10b (`exemplar-best-parts-workflow.js`) → S12 under the three conditions, the 01:20 full-form ruling, and the institution-table and contradiction-walker consequences of the 02:27 lesson; hobb/wolfe passes deferred; (3) the golden-freeze question — the dormancy arm's 600/600 is UNFLOORED until a `generation-corpus-golden` row exists; the register door: now OPEN in principle (full instrument under a lit overlay) and CLOSED in fact by the realm-load firing on every lit receipt so far — the model-or-window question is CAP-HORIZON-909's, and no tuning value moves before that measurement and the owner's signature; (4) the owner's WALK + one regen → the exhaustive review → fix cars → FULL STOP before the terminal soak (re-priced by CAP-HORIZON); (5) after S12: the surface-text program. Push and the WALK stay the owner's.

## (superseded) PICKUP AT §909 — **THE OPEN ROWS ARE DECIDED UNDER THE OWNER'S DELEGATION: the series ships, a 600-year run precedes any re-cut, the D&D route rows are relabelled and lawfully replaced, the verify-only pass runs for three sweeps, the successors are named; next are SOAK-HONEST-909 on a gate, the research runs → S10b → S12, the freeze question and the walk** (`6ebe0ef3b`, unchanged)
Product `claude/composite-r4` = 6ebe0ef3b — UNCHANGED by this act (§909 is a ledger landing: zero product bytes; the rulings and their reasons live in memory `owner-decision-2026-09-07-delegated-rows-chair-rulings`, the briefs in the kit seal under `briefs/`). Owed to the OWNER: only what the delegation did not cover — the WALK; any push; the book purchase if wanted (offered, never made); the Kay fingerprint's owned copies; signing the public labels and copy the surface-text program will bring. Owed to the next chair, in order: (1) **SOAK-HONEST-909** (Opus, `laneB6` @ 6ebe0ef3b, dispatched 02:46; receipt `receipt-soak-honest-909.md`) — three cars (the series + schema 5 → 6; the dotted-path `requires`; the constant lift and `capacity_envelope_30y` armed) — lands as ONE consist on a chair gate (§910: predict → proof → registers → ratchet → after-ratchet → texts → gate → after-cas, the §908 kit path), declaring every archived-receipt verdict that moves; then **CAP-HORIZON-909** (measurement, ~1 h CPU, `briefs/brief-CAP-HORIZON-909.md`) and **L-MAT** (ruling 8); (2) the research program: dnd r11 and leguin r9 RUNNING in chair session 405b5e7e (a new session rebuilds with `NO_TRIAGE=1 CONFIRM_DEAD=1 sh $SC/resume-research.sh dnd leguin` → mk-embedded → two launches) → ai r11 (verify-only + the procgen-baseline angle, `sweep/extra-ai-procgen.json`) → `dry-check.py` each → S10b (`exemplar-best-parts-workflow.js`) → S12 under the three conditions, the 01:20 full-form ruling, and the institution-table and contradiction-walker consequences of the 02:27 lesson; hobb/wolfe passes deferred; (3) the golden-freeze question — the dormancy arm's 600/600 is UNFLOORED until a `generation-corpus-golden` row exists; (4) the owner's WALK + one regen → the exhaustive review → fix cars → FULL STOP before the terminal soak (re-priced by CAP-HORIZON); (5) after S12: the surface-text program (the essay, the Compendium sentence, the successor labels, the mobile nav design). Push and the WALK stay the owner's.

## (superseded) PICKUP AT §908 — **THE CAPACITY TRIPWIRES ARE HONEST: a row that cannot run says so, a blind receipt can no longer certify itself full, and the register door closes on today's receipts until the owner's series row ships; next are the research fold and reconciliation (S10b → S12), the series row, the freeze question and the walk** (`4243bdc61` -> `6ebe0ef3b`).

(`4243bdc61` -> `6ebe0ef3b`)

Product `claude/composite-r4` = 6ebe0ef3b (sealed `landing-captrip-2026-09-07`, 9 cars over 4243bdc61, landed by fast-forward; gate green, 31979 tests; typecheck:ratchet 173 of 173). What landed: the NOT-EXECUTABLE channel, the reachability walker with the two blind rows banked shrink-only, the corrected C3 refusal (the row still out, for a measured reason), the envelope header fixed from a run of its own file, the four negatives anchored, the lighting census at 23662 titles (`2e1bb6311`), the writer-reach provenance (`2aec7b0bd`), the census totals 31977 → 31979 (`029e5406d`) and the capsule (`6ebe0ef3b`). What it means: `fullInstrument` is FALSE on every receipt the current soak writer produces, so `mintRefusals` refuses every freeze until the per-year population and died series ship on the receipt — that is §907 owner row (b), and it now gates the terminal soak's freeze act. Owed to the OWNER — new at §907/§908: (b) ship the series (schemaVersion 5 → 6; recommend yes, and before the terminal soak is scheduled); (c) the `capacity_realm_load` reading, model or window (recommend a longer-horizon research run first); (d) SOAK-1 re-priced on the measured 0.635 s/settlement-year; the D&D research route policy (~40 rows read from an archive.org book OCR and paywalled Roll20 pages: relabel, replace with lawful witnesses, or buy the books); the ~120-row verify-only pass (cost). Carried: the voiced Handbook essay (§906); the §904 rows (rebuild-or-bury the single-chrome mobile nav; the MAT dial's public-safe path behind the SQL migration); the §903 rows (the lit successor preset id + label; `infoMode: 'perfect_delayed'`; the Compendium copy); the research triage-by-feature completion pass and the Kay fingerprint's owned copies. Owed to the next chair, in order: (1) the research program — dnd r10 COMPLETE (section-dnd.md round 9, 61 features, the PLACE part new; critic 01:05); ai r10 RUNNING in chair session 405b5e7e (resumable only there; a new session rebuilds with `CONFIRM_DEAD=1 sh $SC/resume-research.sh ai` → mk-embedded → launch) → `dry-check.py ai` → S10b (`exemplar-best-parts-workflow.js`) → S12 under the owner's three conditions and the 09-07 ruling that every dossier element gets the full form (a whole setting, not a campaign; D&D's "size to purpose in play" transfers as HOW, never as WHICH); (2) two small chair cars for a later consist: lift `MOVING_SHARE_FLOOR`/`MOTION_FLOOR_01` into `soakInvariants.mjs` and arm `capacity_envelope_30y` (roster to 12/10); the dotted-path `requires` for `capacity_realm_load` beside the series row; (3) the golden-freeze question — the dormancy arm's 600/600 is UNFLOORED until a `generation-corpus-golden` row exists; (4) the owner's WALK + one regen → the exhaustive review → fix cars → FULL STOP before the terminal soak; (5) after S12: the surface-text program (owner directive 09-06 15:40). Push and the WALK stay the owner's.

## (superseded) PICKUP AT §907 — **HORIZON-B6 IS MEASURED: the runaway is cured, the plateau is not shown, one tripwire fired and the genesis cell stays unfrozen; two capacity tripwires were blind and a repair lane is chartered; next are the CAP-TRIP cars on a gate, the research rounds → S10b → S12, the freeze question and the walk** (`4243bdc61`, unchanged)
Product `claude/composite-r4` = 4243bdc61 — UNCHANGED by this act (§907 is a ledger landing: zero product bytes; the evidence lives in the kit seal under `receipt-horizon-b6.md` and `capacity/` — the 300-year receipt, the 24-figure register proposal the door refused, the plateau chart as numbers — and the skeptic's probe under `skeptic-m1f1/`; docs/DESIGN_HORIZON.md gained one marked line at §2.5 C4′ clause (4)). What the measurement says, in one breath: the lit engine no longer runs away at 300 years, but it has not settled either (four `other` shapes, +26 % over the last fifty years), one deterministic tripwire fired (`capacity_realm_load` 0.4787), the soak took 25 minutes not 2.3 hours, the two gate suites are millisecond items, and two of the three built capacity tripwires could never fire on a real receipt (§907 M1-F1, confirmed twice). Owed to the OWNER — new at §907: (b) ship the per-year population and died series on the soak receipt (schemaVersion 5 → 6) so `capacity_plateau` / `capacity_floor_thaw` can fire — recommend yes; (c) the `capacity_realm_load` reading: model or window — recommend a longer-horizon research run before any re-cut, nothing tuned; (d) SOAK-1 re-priced on the measured 0.635 s/settlement-year (about 4× cheaper than estimated; PLAUSIBLE). Carried: the voiced Handbook essay (§906); the §904 rows (rebuild-or-bury the single-chrome mobile nav; the MAT dial's public-safe path behind the SQL migration); the §903 rows (the lit successor preset id + label; `infoMode: 'perfect_delayed'`; the Compendium copy); the research triage-by-feature completion pass (leguin ~250–300 / hobb ~60 verify-only rows) and the Kay fingerprint's owned copies. Owed to the next chair, in order: (1) **CAP-TRIP-907** (Opus, dock `laneB6` @ 4243bdc61, brief `briefs/brief-CAP-TRIP-907.md`, receipt `receipt-cap-trip-907.md`; in flight at this pickup) — four cars (the not-executable channel; the reachability guard with the two blind rows banked; `capacity_envelope_30y` re-measured, reinstated only if explainable; the envelope header corrected) — land as ONE consist on a chair gate (full vitest, the test-ratchet, the census refreeze if totals move), declaring any movement of `deterministicFirings` or the roster; (2) the research program: dnd r10 and ai r10 are RUNNING in chair session 405b5e7e (resumable only there; a new session rebuilds with `CONFIRM_DEAD=1 sh $SC/resume-research.sh dnd ai` → mk-embedded → two launches) → `dry-check.py` each → S10b (`exemplar-best-parts-workflow.js`) → S12 under the owner's three conditions (allocate, never average; the evidence resets the weights; the two targeted rounds first); (3) the golden-freeze question — the dormancy arm's 600/600 is UNFLOORED until a `generation-corpus-golden` row exists; the golden master's 525 stays banked until the §898 re-record is signed through the door; (4) the owner's WALK + one regen → the exhaustive review → fix cars → FULL STOP before the terminal soak (whose price is now the owner row (d)); (5) after S12: the surface-text program (owner directive 09-06 15:40). Push and the WALK stay the owner's.

## (superseded) PICKUP AT §906 — **THE LIGHTING WAVE'S TAIL IS LANDED: anchor, docs and visibility cars on one tip; the wave is measured, declared and proven read; next is HORIZON-B6, the freeze question and the walk** (`6582958ce` -> `4243bdc61`).

(`6582958ce` -> `4243bdc61`)

Product `claude/composite-r4` = 4243bdc61 (sealed `landing-anchorvis-2026-09-06`, 7 cars over 6582958ce, landed by fast-forward). The lighting wave (POSITIONS 0–5 of `refs/preserve/light-plan-2026-09-05`) is now COMPLETE in every part the owner has not gated: PROSE (§898), L-PROBE (§899), L-HOMES (§901), OSR rung 18 (§902), L-DEFAULT hunk 1 (§903), L-UI + the retirement (§904), L-PROBE-2 + the eleven declarations (§905), and this tail (§906). Owed to the OWNER: the voiced Handbook essay (sign or send to the surface-text program — the §906 row's table); the §904 rows (rebuild-or-bury the single-chrome mobile nav; the MAT dial's public-safe path behind the SQL migration); the §903 rows (the lit successor preset id + label — hunks 2–7 wait on it; `infoMode: 'perfect_delayed'`; the Compendium copy); the research triage-by-feature completion pass (leguin ~250–300 / hobb ~60 verify-only rows); the research decisions taken tonight (the two targeted rounds are RUNNING/BUILT on the owner's go) and still open: the triage-by-feature verify-only pass and the Kay fingerprint's owned copies. Owed to the next chair, in order: (1) **HORIZON-B6** (the ~2.3 h CAPACITY soak) only in a window with no gate for 2.5 h, with the edge `validValue` charset branch and the `WORKER_BUNDLE_CEILING_BYTES` arm; (2) the golden-freeze question — the dormancy generation arm reads 600/600 bit-identical across the whole wave but UNFLOORED (no `generation-corpus-golden` row in the unfrozen register); the golden master's 525 stays banked until the §898 re-record is signed through the door; (3) the owner's WALK + one regen → the exhaustive review → fix cars → FULL STOP before the soak; (4) after S12: the surface-text program (owner directive 09-06 15:40) — the Handbook table above is its first baseline. Research: martin r8 and ai r8 critics RUNNING (the two slots); wolfe r8 and dnd r8 CLOSED tonight (sections rewritten; both critics re-name the triage defect; dnd's still names the PLACE hole — its one targeted round is the owner's call); tolkien/hobb/leguin/kay closed; next the five-author check (ready), the three fingerprints, S12, the taste sample, the walk. Push and the WALK stay the owner's.

## (superseded) PICKUP AT §905 — **THE LIGHTING WAVE IS MEASURED END TO END: ONE PRESET MOVED AT ONE LANDING, EVERYTHING ELSE ENGINE-SIDE IS BIT-IDENTICAL, AND THE ELEVEN DECLARATIONS ARE ON THE LEDGER; next is the anchor car, then the visibility and docs-corpus cars in one consist, then HORIZON-B6 and the walk** (`6582958ce`, unchanged)
Product `claude/composite-r4` = 6582958ce — UNCHANGED by this act (§905 is a ledger landing: zero product bytes; the battery's outputs live in the kit seal under `lprobe-out-905/`, the comparison in `lprobe-out-905/COMPARE-cheap.md`, the declarations in `docs/GOLDEN_SHIFT_LEDGER.md` on this branch). L-PROBE-2 ran at all six landing tips (cheap) and in full at the tip against the §899 control; the §899 control was RE-DERIVED at 38474a59e because its outputs died with the 09-05 reboot — the battery is deterministic (same tip twice, identical digests), so nothing is lost that cannot be re-measured in thirty seconds a tip. Owed to the OWNER (unchanged): the §904 rows (rebuild-or-bury the single-chrome mobile nav; the MAT dial's public-safe path behind the SQL migration), the §903 rows (the lit successor preset id + label; `infoMode: 'perfect_delayed'`; the Compendium copy), and the research triage-by-feature completion pass (leguin ~250–300 / hobb ~60 verify-only rows). Owed to the next chair, in order: (1) **ANCHOR-905** (Opus lane, dock `laneANCHOR905` @ 6582958ce, brief `briefs/brief-ANCHOR-905.md`) — the `mobileSingleChrome` retirement anchor; ⚠ the §904 R4 mechanism ("the anchor moves the negative-assertion baseline 1 → 2") is contradicted by `negativeAssertionAnchor.walker.test.js`'s own header (an ANCHORED negative is not counted); the lane measures and, if the mechanics confirm it, anchors the legacy site too and banks the row 1 → 0; (2) the L-UI-MAT R6 visibility arm (Handbook header/essay + the War & Resolve fold-in) and (3) the docs-corpus car (`UIUX_AUDIT_AND_PLAN.md` :1959–1969) — the three land as ONE consist (§906) over 6582958ce with the cheap battery re-run at its tip as the receipt (thirty seconds; the engine instruments must read byte-identical to `904-tip`); then HORIZON-B6 (the ~2.3 h CAPACITY soak, only in a window with no gate for 2.5 h) → the golden control's freeze question (the dormancy arm is 600/600 UNFLOORED until a `generation-corpus-golden` row exists) → the owner's WALK + one regen → the exhaustive review → FULL STOP before the soak. Research: wolfe r8 and dnd r8 synth + critic RUNNING (the two slots); martin r8 and ai r8 (critic only) built and queued; tolkien/hobb/leguin/kay closed; then the five-author check, the three fingerprints, S12, the taste sample, the walk.

## (superseded) PICKUP AT §904 — **TWO OF O-13'S FLAGS ARE LIT (display-only, proven two ways), THE THIRD IS RETIRED; L-MAT WAITS ON THE OWNER'S MIGRATION; the lighting wave's tail is now chair acts — L-PROBE-2, the eleven declarations, the registers** (`dd5f13218` -> `6582958ce`).

Product `claude/composite-r4` = 6582958ce (sealed `landing-uimat-2026-09-06`, 6 cars over dd5f13218, landed by fast-forward). Owed to the OWNER (§904 rows): rebuild-or-bury the single-chrome mobile nav; the MAT dial's public-safe path (a SQL migration and the gallery security boundary); and the §903 rows still open (the lit successor preset id and label — hunks 3 and 5 wait on it; `infoMode: 'perfect_delayed'` on the premium gate; the Compendium's "measured, realistic pace" copy against nineteen lit systems). Owed to the CHAIR: the retirement-anchor car; L-PROBE-2 + the eleven declarations + the registers (the lighting plan's tail); a visibility arm on the two lit surfaces before the walk (L-UI-MAT R6); the docs-corpus citations in `UIUX_AUDIT_AND_PLAN.md`; the golden control on a quiet machine; HORIZON-B6's soak; rung 20 (or one migration merged with 19). The prose program runs under the owner's 17:40 cap — two research runs and one implementation lane at a time; the queue and the resume recipe are `prose-research/sweep/LAST-RUNS.json` `_queue`; launches are embedded scripts (`sweep/embedded/`, `mk-embedded.py`), never a paste — then the dry-checks, S-BOUND, the five-author check, S12 the reconciliation, the taste sample (check-pair v2 + an Opus refuter), the owner's walk; the SURFACE program after S12. Push and the WALK stay the owner's.

## (superseded) PICKUP AT §903 — **THE DEFAULT PRESET IS LIT (class C+D, twenty-one virtual keys, identity untouched); the rest of L-DEFAULT waits on the owner's successor id and on rung 20** (`fd36f0298` -> `dd5f13218`).

Product `claude/composite-r4` = dd5f13218 (sealed `landing-ldefault-2026-09-06`, 9 cars over fd36f0298, landed by fast-forward). Owed to the OWNER (§903 rows): the lit successor preset id and label (hunk 2 — until it is named, hunks 3 and 5 cannot move: class E is proved impossible on the legacy id); `infoMode: 'perfect_delayed'` on the premium gate (hunk 4); the Compendium's "measured, realistic pace" copy against its nineteen lit systems. Owed to the CHAIR: rung 20 (or one migration merged with 19) for hunk 6's twenty-five discoverable keys and hunk 7's 25-key re-key; the golden control on a quiet machine; HORIZON-B6's soak; the retrovalidation walk; the FULL STOP before the soak. The prose program (owner ask 09-05, EXHAUSTIVE; 09-06: a LATENT GRAMMAR, never a visible template; 09-06 08:05: Opus FINDS under `prose-research/OPUS-FINDER-METHOD.md`, Fable synthesises and criticises) runs round 3 in the chair kit; `PROBE_ALL.md` is being corrected from its refutation receipt (55 verdicts: 22 confirmed, 20 refuted, 13 partly) before any rule rests on it; nothing in the corpus changes before the owner's walk of a refuted-and-cured taste sample.

## (superseded) PICKUP AT §902 — **THE OSR REGISTER IS RE-ANCHORED AND ITS DOOR IS OPEN; the two writer-reach owed rows are retired; next is L-DEFAULT** (`b0cbc67a1` -> `fd36f0298`).

Product `claude/composite-r4` = fd36f0298 (sealed `landing-osr18-2026-09-05`, 8 cars over b0cbc67a1, landed by fast-forward). Owed to the next chair: L-DEFAULT (`briefs/brief-L-DEFAULT.md`; the preset table is the car that lights the lighting consist) → the golden control on a quiet machine, HORIZON-B6's soak → the retrovalidation walk → the FULL STOP before the soak. Rung 19 is spoken for by the ninth exemption's retirement (owner row). The prose program (owner ask 09-05, EXHAUSTIVE; 09-06: the structure is a LATENT GRAMMAR, never a visible template) runs in the chair kit `prose-research/` — the sweeps, rounds and the reconciliation queue are `LANE-QUEUE.md` S1–S12; nothing in the corpus changes before the owner's walk of a refuted-and-cured taste sample.

## (superseded) PICKUP AT §901 — **THE LIGHTING CONSIST IS DOWN; the nine lighting lanes' cars are on one tip; next is OSR rung 18, then L-DEFAULT** (`04bb92d19` -> `b0cbc67a1`).

Product `claude/composite-r4` = b0cbc67a1 (sealed `landing-lighting-2026-09-05`, 25 cars over 04bb92d19). Lit: nothing yet — every lighting car lands DARK behind its door; L-DEFAULT (the preset table) is the car that lights. Owed to the next chair: OSR rung 18 first (the schema-17 receipt's subjectSha lies outside the product lineage since the §900 replay, so the OSR `--write`, the capsule and two writer-reach census rows all wait on it); then L-DEFAULT's six hunks; then the golden control on a quiet machine, HORIZON-B6's soak, and the retrovalidation walk before the FULL STOP. The prose program (owner ask 09-05) is owner-gated on a VERIFIED taste sample: the chair kit `prose-research/` holds the dossier, the corpus inventory and probe, the fingerprints, RULES v2, the checker, and the four critic sweeps' exported state.

## (superseded) PICKUP AT §900 — **THE DESK CONSIST IS DOWN; thirteen docks landed on one tip; next is the LIGHTING wave (L-HOMES ×9 → L-DEFAULT → L-UI-MAT) on the L-PROBE receipts, then HORIZON-B6, then the WALK.** (`38474a59e` -> `04bb92d19`).

**Product** `04bb92d19`, sealed `refs/preserve/landing-desk-2026-09-05`. Kit `refs/preserve/chair-tools-2026-09-05`; resume note `refs/preserve/resume-note-2026-09-05`.
### ⭐ NEXT, IN ORDER
1. **L-HOMES** (9 cars, `briefs/brief-L-HOMES.md`) on the L-PROBE receipts (`lprobe-out-899/` cheap 6/6; the full battery ran at the §899 tip: the cheap steps 6/6 again, `full_simulation` certified (5 y × 4 settlements), the golden control OWED on a quiet machine (its window gave up under four lanes), the overlay STOP on six presets re-framed by L-OVERLAY as a seam question) → **L-DEFAULT** → **L-UI-MAT** → the eleven declarations (`lightingwave/PLAN.md` + `CORRECTIONS.md`).
2. **HORIZON-B6** (the ~2.3 h CAPACITY soak) in a window with no gate for 2.5 h; the edge `validValue` charset branch (deferred by HORIZON-B1) and the `WORKER_BUNDLE_CEILING_BYTES` arm written from the chair's build (the generation worker bundle 1,404,242 B, monotone-down); HORIZON-B1's closure: the charset leaf ships as its own hashed chunk (`custom-charset-*.js`, 11,762 B) and is NOT in the entry's `__vite__mapDeps` — lazy, as designed.
3. Then the owner's WALK + one regen → the exhaustive review → fix cars → FULL STOP before the soak.
### 🟠 OWNER'S DESK: the prose window's golden fixture (the genesis signing, or your sentence) · the paid-rights floor and the arms · DS-GEN-1 · DS-FTH-2's public pool · a founder-name producer · the chance-meeting tie-break · `anchored` at generation.

## (superseded) PICKUP AT §899 — **THE COMPOSED CONSIST IS DOWN; the clamp program is CLOSED; OSR-SCHEMA17 has landed in the desk dock (rung 17); next is the DESK consist (§900) with nine late docks behind it.** (`5e28d5c83` -> `38474a59e`).

**Product** `38474a59e`, sealed `refs/preserve/landing-composed-2026-09-05`. Kit `refs/preserve/chair-tools-2026-09-05`; resume note `refs/preserve/resume-note-2026-09-05`.
### ⭐ NEXT, IN ORDER
1. **RESUME** the held lanes first (DESK-GEN3, DESK-TIMEBAND, DOCKET-3 — ids in the resume note; `RESUME-MESSAGE.txt` in the kit); they hold vitest for the gate.
2. **THE DESK CONSIST (§900)** per `DESK-LANDING-PLAN.md` + its addendum: `land-desk-prep.sh <cas>` replays the 26 desk cars (23 + OSR-SCHEMA17's 3, sealed `osr-schema17-2026-09-05`) and the late docks — ENC-4b+4c (6, `enc4c-2026-09-05`), HORIZON-B1 (7), HORIZON-B2 (2), SEAT-78 (3), DOCKET-2 (+DOCKET-3), PANTHEON-ROSTER (3, over the §899 tip), DESK-TIMEBAND and DESK-GEN3 when they report. Two known replay STOPs: pantheon A5 (ENC-4b's literals vs the roster — the two-kind figures go INTO the roster) and any `dossierMounts.js` row the resolver cannot settle. Then lane **DESK-900-CARS** (`briefs/brief-DESK-900-CARS.md`: the voice row, the coupling row, OSR exemption 9 → 8, the roster figures, `{founder}`, item 8 re-scoped), then the chair's own cars (the six complexity band words if DOCKET-3 has not landed them; the edge-shared re-mint ritual for HORIZON-B1; the build + `WORKER_BUNDLE_CEILING_BYTES` arm for HORIZON-B2), then **the whole suite** at the composed tip (no path filter — the late docks' vitest is OWED: five of them never got a window), then the registers in plan §3 order, the gate, the CAS, the row with the RESIDUE block.
3. **L-PROBE** — `lprobe-cheap-899.sh` on the landed tree (steps c–g + the eager STOP; minutes), then the full battery (needs the gate's build; the golden control, the per-preset certification via `--preset`, the listing diff) → **L-HOMES** (9 cars) → L-DEFAULT → L-UI-MAT → the eleven declarations.
4. HORIZON-B6 (the ~2.3 h CAPACITY soak) only in a window with no gate for 2.5 h. Owner rows opened this landing: a founder-name producer; the chance-meeting tie-break by town spelling; DS-GEN-1; the paid-rights floor vs the arms.
5. Then the owner's WALK + one regen → the exhaustive review → fix cars → FULL STOP before the soak.
### 🟠 OWNER'S DESK (unchanged from §898 unless struck): the prose window's golden fixture (the genesis signing, or your sentence) · the paid-rights floor and the arms · DS-GEN-1 · DS-FTH-2's public pool.

## (superseded) PICKUP AT §898 — **THE PROSE CONSIST IS DOWN — lighting POSITION 0 lands; the L-PROBE dark arm (chair) measures next, on this tree** (`fd8b6df00` -> `5e28d5c83`).

**Product** `5e28d5c83`, sealed `refs/preserve/landing-prose-2026-09-05`. The declared same-seed text shift (133 engine-side paths) is entered in `docs/GOLDEN_SHIFT_LEDGER.md`.

### ⭐ NEXT, IN ORDER
1. **L-PROBE** — the chair runs the dark-arm battery (`$SC/lprobe/run.sh` from L-PROBE-KIT) on THIS tree: golden master 3/3 + 0/525, per-preset certification, the OSR presence dump before any regeneration, the lighting tuple, the stability rosters, REAL-birth fixtures per preset, 52-tick hashes, the hashed-chunk listing diff (a build). STOP: eager catalog or a listing diff over (margin − 100 B).
2. **L-HOMES** (9 cars, `briefs/brief-L-HOMES.md`, CAR 1 first) · **SEAT-78** beside it · then L-DEFAULT → L-UI-MAT → the eleven declarations + registers.
3. The queue (`LANE-QUEUE.md`): OSR-SCHEMA17 → the desk consist → DESK-ECON2 (PDFDRIFT is WITHDRAWN; its residue is an OWNER row on the paid-rights floor) → DESK-DEF2 → HORIZON-B1 → DESK-9 → B2 → B6 → ENC-4 → DOCKET.
### 🟠 OWNER'S DESK (new rows this landing; nothing built on them until the word)
- **The PROSE window's golden fixture waits for your genesis signing** (the freeze act, §881.4) — or, if you prefer it sooner, your sentence in `docs/shift-records/_TEMPLATE.json`'s form ("one word charters it sooner"): the chair executes the door the same day. Until then the arm is banked, owner-gated, declared.
- **Does the paid-rights FLOOR include the W-ARMS arms?** A frozen (plan-lapsed) save may still export a PDF (AUDIT-2.2); once arms ship, that export is un-quartered while a live export is quartered — reachable only across time. Product scope: the owner's. Until ruled, the arms ride the LIVE surfaces only.
- **DS-GEN-1** (tension `severity` is an ARRAY on 48/48 — a property of the tension TYPE, not a reading of the town): the cure is generation-side and moves generated output — owner-signed.
- **DS-FTH-2's PUBLIC / SHARED DOSSIER pool** is authored for the one reader §885.3 forbids; lighting it is the owner's.
4. Then the owner's WALK + one regen → the exhaustive review → fix cars → FULL STOP before the soak.

## (superseded) PICKUP AT §897 — **THE ANCHORS CONSIST IS DOWN (4 cars, `df7cdd37e` -> `fd8b6df00`).** The seat is Fable 5.1 (chairs, architects, validates); every lane is Opus (`model: "opus"`), implements and verifies.

**Product** `claude/composite-r4` = `fd8b6df00`, sealed `refs/preserve/landing-anchors-2026-09-05`. **Ledger** = this act. The chair kit is sealed at `refs/preserve/chair-tools-2026-09-05` (restore: `git archive <ref> | tar -x -C $SC`); the resume note at `refs/preserve/resume-note-2026-09-05`.

### ⭐ NEXT, IN ORDER
1. **OSR-SCHEMA17** — brief `briefs/brief-OSR-SCHEMA17.md` in the kit; dock `laneINTEG-tree` @ `940d161ca` (23 desk cars over `90702c3e9`); topology-only corpus change (stress settlements into `roots`, NOT `generated`) + the schema-17 mint through `migrate-observed-shape-readers.mjs`; ⛔ no CONFIG added (20-new-rows path). Then replay the desk consist onto `fd8b6df00`, registers LAST, gate, land.
2. The desk remainder (23 dark blocks: general 9, economy 5, warFaith 5, defense 4) · clamp waves 2–4 (72 copies vs ceiling 62; ONE ceiling ruling at the final wave) · the lighting wave from `$SC/lightingwave/PLAN.md` (LIGHT-PLAN, Opus, read-only — its plan replaces the LOST `LIGHTING-INVENTORY.md`; 31 items, 0 done, the charter's schema-16 claim is SPENT) · W-ARMS from `$SC/warms/` (Car 0 probes + B3 re-measure + B2 skeptic pass) · HORIZON-DARK (6 items, 2 double-counted with lighting) · ENC-4 with the sealed ENC-5 words · the docket.
2b. **LIGHTING, re-planned (LIGHT-PLAN, sealed `refs/preserve/light-plan-2026-09-05`, the chair adopts it):** PROSE (its own consist, FIRST — dispatched as PROSE-REBASE onto `df7cdd37e`; a DECLARED same-seed text shift) → L-PROBE (chair act, zero bytes, AFTER prose lands) → L-HOMES (nine dark-inert builds) → L-DEFAULT (six hunks) → L-UI + L-MAT + the two door residues → L-PROBE-2 + the eleven declarations + the registers; SEAT-7/8 beside it from day one. ⛔ 14th decay: the GOLDEN-freeze machinery is PRESENT and UNFROZEN — `LGT-P14-WITNESS` owes a register row or a written exclusion in the same commit. `LGT-P15-EP1` owes the 25-key re-key, NOT the EP-1 release. Rung 17 is OSR-SCHEMA17's; the wave's migration is the next free rung after it.
2c. **CS-9 is RULED chair-class and ADOPTED (own-export import = RESTORE path; the charset wall refuses at AUTHORING only)** — HORIZON-DARK B1 / CHARSET Car 2 and `LGT-P9-CS9` are unblocked; brief them from `brief-HORIZON-B1` when written.
2d. **Queued and BRIEFED, in order** (`LANE-QUEUE.md` in the kit): OSR-SCHEMA17 (when no gate runs) → PDFDRIFT → DESK-ECON2 (DESK-7's eleven `{complexity}` forms are AUTHORED by the chair, sealed `refs/preserve/desk7-words-2026-09-05`, owner veto open) → DESK-DEF2 → HORIZON-B1/CHARSET Car 2 → DESK-9.
3. Then the owner's WALK + one regen (NOT READY: deity-free corpus 0/44) → the exhaustive review → fix cars → FULL STOP before the soak.

⛔ **Laws that bit tonight:** a seat change is a CONTRACT change — every instrument that pattern-matches the trailer is a consumer (chair-verify false-redded the first Fable act) · a count assertion over a marker must be LINE-ANCHORED when the marker also appears in prose · `git diff HEAD -- path` on a `D `+`??` ledger file compares HEAD to NOTHING (use `--no-index` against the HEAD blob) · zsh `$C:path` is a history modifier — ALWAYS `${C}:path` · the gate runner REFUSES a stale premise (product tip must equal the declared base) rather than obeying it.

## (superseded) PICKUP AT §896.1 — **THE §892 WALK IS RULED: 28 rows by the Fable seat, from the sealed evidence, no probe re-executed.** The seat is FABLE 5.1 (Fable chairs, Opus implements). Product `df7cdd37e`. Read this, then the §896 card below, then `$SC/RESUME-NOTE.md`.

⛔ **What is NOT ruled and must not be read as ruled:** the 8 EVIDENCE-THIN calls (PLAUSIBLE until re-executed against the current tree) · the §892.5 critic (S99 never ran) · **R1 → R36** — every Opus-authored row from §893 to §896 is ENROLLED, not ruled; that is the next sitting. A Fable trailer on this act certifies the chair's own re-derivation, nothing else.

### ⭐ NEXT, IN ORDER (unchanged from §896's card; §896.1 was the first item)
ANCHORS replay → its census totals → its gate · **OSR-SCHEMA17** (`brief-OSR-SCHEMA17.md`, `model: "opus"`) to free the 23-car desk consist · the desk remainder · clamp waves 2–4 · the lighting wave (31) · W-ARMS (14) · HORIZON-DARK (6) → ⛔ the owner's WALK → the exhaustive review → ⛔ FULL STOP before the soak.

## (superseded) PICKUP AT §896 — **THE CLAMP CONSIST IS DOWN. THE SEAT IS FABLE 5.1 — Fable chairs and architects, Opus implements and verifies (owner, 2026-09-05).** Product `df7cdd37e`, SEVEN landings. Read this, then `$SC/RESUME-NOTE.md` (sealed at `refs/preserve/resume-note-2026-09-05`).

`$SC` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/d5b9a39f-b0b2-4d9c-a1a0-08b291896f89/scratchpad`. ⛔ OWNER CAP FOUR agents of any kind. ⛔ Chair acts: `Seat: Fable 5.1 — validated` + a CHANGED `docs/FABLE_RETROVALIDATION_QUEUE.md` blob when the row enrols anything; **every lane dispatched `model: "opus"`**, its commits `Seat: Opus 5 — Fable-unvalidated` + `Lane: <name>`. ⛔ The seat change validates NOTHING retroactively.

### ⭐ IMMEDIATELY NEXT, IN ORDER
1. **§896.1 — the Fable seat's 28 rulings** (`$SC/rulings-892x.json`, dry-run clean; `collect-8961.sh`).
2. **ANCHORS** — replay `a2a0d2320` (3 cars) onto `df7cdd37e`; lighting is **+0 on all five** (its test file is parked on `describe.each`); owes only the census totals (`run-ratchet-anch.sh`), then gate (`run-gate-anch.sh`, fill `BASE`).
3. **OSR-SCHEMA17** — `brief-OSR-SCHEMA17.md`, `model: "opus"`, dock `laneINTEG-tree` @ `940d161ca`: the topology-only corpus cure + the schema-17 mint that three lanes' refusals converged on. It frees the **23-car desk consist**.
4. The desk remainder (23 dark blocks; `warFaith` 5 of 5), clamp waves 2–4 (10 over the ceiling, 4 with a reason not to move → ONE ruling at the end), the lighting wave (**31 items, not 22**; its charter's schema-16 rung is SPENT), W-ARMS (14; Car 0a landed the volume), HORIZON-DARK (**6, not 5**) → ⛔ the owner's WALK → the exhaustive review → ⛔ FULL STOP before the soak.

### ⛔ THREE LAWS THIS LANDING PAID FOR
A register taken in ANOTHER dock is a register taken mid-consist · an empty file passes `sh -n`, so a lost script reports green — gate on `[ -s f ]` and the command's presence · `pg` is undeclared and a symlinked dock lacks it — the sentinel's `TRUE_EXIT=1` with zero refusals means the gate could not RUN what it was frozen to run.

### ⛔ THE OWNER'S DESK
The WALK + one regen (⚠ NOT READY — the review corpus is deity-free, 0 of 44) · every `git push` and the deploy · the tuning SIGNATURE · **new row:** `SOLE_EARNER_FROM = 50` prints the monopoly sentence for a perfect 50/50 split (the cut reads the leader alone, never the roster's length); `HIGH_INSTABILITY_FROM = 0.35` is byte-equal to `LADDER_TUNING.CHURN_BUMP`. Neither value moved.

## (superseded) PICKUP AT §895 — **THE DESK CONSIST IS DOWN. Product `90702c3e9`, 90 cars across SIX landings. THE SEAT IS OPUS 5.** Read this, then `$SC/clamp/CLAMP-MIGRATION-PLAN.md` before touching any clamp.

`$SC` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/d5b9a39f-b0b2-4d9c-a1a0-08b291896f89/scratchpad` · `$SP` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad`. ⛔ OWNER CAP FOUR agents of any kind. ⛔ Every act: `Seat: Opus 5 — Fable-unvalidated` + a CHANGED `docs/FABLE_RETROVALIDATION_QUEUE.md` blob in the SAME commit; build the queue from `git show HEAD:…`, never the worktree copy.

### ⭐⭐ TWO LAWS THIS LANDING PAID FOR
1. **GATE-WINDOW WORK MUST BE LOAD-BOUNDED, NOT MERELY VITEST-FREE.** A 4-agent read-only workflow beside a full-suite run drove load to 45 against a quiet threshold of 4.0 and produced a FALSE RED that convicted three pglite SECURITY suites. Proved in both directions: same commit, load 45 reds, load 1.4 green. During a full-suite run keep concurrent agents to **one or two**, and never launch a fan-out into a running gate.
2. **A LANE'S GREEN IS A CLAIM ABOUT THE FILES IT RAN, NEVER ABOUT THE TREE.** Sixteen arms were red at the composed tip after a lane reported "all gates green". The brief was the habitat; it now requires a `tests/lint/` DIRECTORY run.

### ⭐ IMMEDIATELY NEXT
**ANCHORS — 3 cars, sealed `refs/preserve/train-anchors-2026-09-04` (`41be350f9`), zero file overlap with the desk landing, so the replay onto `90702c3e9` is clean.** It owes only its ratchet totals (`totalTests +2`, `totalFiles +0`; ⚠ its lighting `titles +2` figure was WRONG — that file is parked on `describe.each`, so titles do NOT move and no lighting act is owed). Then **DEFENSE-TRUTH** (`brief-DEFENSE-TRUTH.md`): the product tells a walled, garrisoned town it has "neither walls nor force" — `flag()` takes bool|number, the producer writes ARRAYS, proved by execution — and ⛔ **F2/F3 are COUPLED: curing the dead read ACTIVATES the ruin bug.** Then the desk remainder (`defense` 9 blocks left, then `economy`/`warFaith`/`general`), SEAT-7a's conflict, the LIGHTING WAVE, W-ARMS, HORIZON-DARK, ENCOUNTERS remainder → ⛔ the owner's WALK → the exhaustive review → ⛔ FULL STOP before the soak.

### ⛔ THE OWNER'S DESK
The WALK + one regen (⚠ NOT READY: the review corpus is deity-free) · every `git push` and the deploy · the tuning SIGNATURE (declined by the chair on its own judgment). **New row:** `HIGH_INSTABILITY_FROM = 0.35` is byte-equal to `LADDER_TUNING.CHURN_BUMP` — one undecayed contested challenge reads as "high instability". Neither value moved.

## (superseded) PICKUP AT §894 — ⚠⚠ **THREE LANDINGS WERE COLLECTED IN ONE ACT BECAUSE THE LEDGER FELL TWO BEHIND. Product `43d49a3a0`, 65 cars landed since `ca651d54b`. THE SEAT IS OPUS 5.** Read this, then `$SC/RULINGS-893*.md`.

`$SC` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/d5b9a39f-b0b2-4d9c-a1a0-08b291896f89/scratchpad` · `$SP` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad`. ⛔ OWNER CAP FOUR agents of any kind. ⛔ Every act: `Seat: Opus 5 — Fable-unvalidated` + a CHANGED `docs/FABLE_RETROVALIDATION_QUEUE.md` blob in the SAME commit; build the queue from `git show HEAD:…`, never the worktree copy.

### ⭐⭐ THE FIVE LANDINGS OF 2026-09-04, ALL SEALED, ALL CASed
`ca651d54b` → **§891** 27 cars `f537ce47e` → **§893.1** 4 cars `6c49ecbbf` → **§893.2** 18 cars `2e563a63b` → **§893.3** 11 cars `1d50822b8` → **§894** 5 cars `43d49a3a0`. Census **10 → 6** with both totals tightened; ceiling still 17.

### ⛔ THE LEDGER DEBT THIS CARD CLOSES — AND THE LAW IT LEAVES
Landings §893.2, §893.3 and §894 were sealed in git and **unwritten on the ledger for three landings**. Nothing was lost (every tip is under `refs/preserve/*`), but a successor reading the ledger would not have known they happened. ⭐⭐ **THE LAW: a landing is not landed until its row exists. Collect at the CAS, not at the end of the session** — the CAS and the collection are one act, and the gap between them is the only window in which the record can lie.

### ⭐ THE ORDERING LAW, PAID FOR TWICE, THEN PROVED
**The totals re-freeze is the LAST car before the gate.** Taking registers mid-consist cost two full gate cycles at §893.3; applying the law at §894 produced the arc's first gate-green-and-chair-verify-green-first-run consist.

### NEXT, IN ORDER
**DESK1's 14 cars are sealed at `refs/preserve/train-desk1-2026-09-04` (`6603f4a9d`) but sit on `1d50822b8` — 5 product commits behind, ZERO file overlap with §894, so the replay is clean.** Then: desk remainder (`defense` 2 of 11 done, then `economy`/`warFaith`/`general`) · SEAT-7a (`ce9910469`, 29 commits behind) · SEAT-7/8 Car 2 · the lighting wave (6 cars + 16 prerequisite builds) · W-ARMS (14) · HORIZON-DARK remainder (5) · ENCOUNTERS remainder (ENC-5's words are the owner's; ENC-7 deferred) · ⛔ the owner's WALK + one regen · the exhaustive review · its fix cars · ⛔ FULL STOP before the terminal soak.

### ⛔ PERMANENTLY THE OWNER'S
Every `git push` and the deploy · the WALK · the tuning SIGNATURE (declined by the chair on its own judgment — a promise made to players is not the owner's to hand over in passing). Everything else was taken under the 09-04 carve-out amendment and is marked for Fable retrovalidation.

## (superseded) PICKUP AT §893.1 — ⚠⚠ **THE CENSUS IS 6, NOT 10, AND IT WAS NEVER FULL.** Product `6c49ecbbf`, 31 cars landed since `ca651d54b`. THE SEAT IS OPUS 5. Read this, then `$SC/RULINGS-893*.md` (eight files).

⛔ **EVERYTHING of this session is sealed at `refs/preserve/session-kit-893-2026-09-04` and `…-893b-…` — the scratch dirs die with the account, and TWO artifacts (the lighting inventory, the 55,812 B W-ARMS volume) had no git home at all.**

### ⭐⭐ THE PREMISE THAT COST THE MOST, CORRECTED
**"The census is FULL at 10/10 with ZERO headroom" was FALSE.** The size cap is `CEILING = 17` (`tests/lint/testRatchet.test.js:268`, `<=`, green); the gate's printed `ceiling N` is just the entry count meaning *no new regressions*; `totalTests`/`totalFiles` are scope-COLLAPSE FLOORS. **A new test file does NOT breach the census.** It was in a dozen briefs, TWO lanes reported phantom blockers from it, and a chair ruling built on it is withdrawn. ⭐ **A premise repeated in every brief is the most expensive thing to get wrong: every lane inherits it and none re-derives it.**

### ⛔ ONLY TWO THINGS ARE STILL GATED: every `git push`/deploy, and the owner's WALK
The tuning SIGNATURE is DECLINED by the chair rather than gated. ⭐ **And the lesson that cost the most: reversibility answers MAY I, never SHOULD I** — pair every carve-out judgment with a measurement of the MERITS. A measuring lane before a ruling hardens into a car caught FOUR bad rulings; vigilance caught none.

### THE ROAD FROM HERE
**ENC-3** — its patch STILL APPLIES cleanly at the new tip (20 hunks, one benign comment-only collision) but carries three live bugs: a **rivalry is silently recorded as a friendship**, a per-subject season cap is a **world-wide lesson lockout** (`taughtRecently` never reads `nid`), and 10 em dashes breach the Tier-2 arm. Plan: `$SC/enc3check/`. Then **GOLDEN** (7/7 needed, landable, `$SC/goldenplan/`), the **CHARSET EMBED** (0 B first paint, cures 41/41 — NOT the cancelled 27-widening), **LIGHTING** (151 doors, 113 dark; ⚠ no bit-level witness), **DESK** (≈29 cars; `{complexity}` is a SHIPPING defect), **W-ARMS** (14 items, not 9) → ⛔ the WALK → the review → ⛔ FULL STOP.

### ⛔ THE CHARACTERISTIC SHAPE OF THIS PROGRAMME'S DEBT — now at SIX instances
**A ruling made, the content authored, and the WIRING STEP NEVER TAKEN.** `respect` → `BOND_KINDS`, `rivalry` → `GRUDGE_KINDS`, `errandSpineEnabled` → `PREVIEW_OVERLAY`, eight families → `WAR_RECEIPTS` (wired at §893.1), the W-ARMS charter never committed, and a chair charter that named a mechanism it had not measured. **Check for it explicitly in every lane brief.**

## (superseded) PICKUP AT §893 — §891 landed at 27 cars; the §893.1 card above supersedes and carries the census fall.

`$SC` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/d5b9a39f-b0b2-4d9c-a1a0-08b291896f89/scratchpad` · `$SP` = `/private/tmp/.../58f0a8e2-.../scratchpad`. ⛔ **EVERYTHING of this session is sealed at `refs/preserve/session-kit-893-2026-09-04` (463 files)** — the scratch dirs die with the account, and the lighting inventory had no git home at all.

### ⭐⭐ THE TWO GRANTS, AND WHAT IS LEFT OF THE OWNER'S DESK
**⛔ ONLY TWO THINGS ARE STILL GATED: every `git push`/deploy, and the owner's WALK.** The tuning SIGNATURE is DECLINED by the chair rather than gated (a promise made to players) and blocks nothing. Everything else is the chair's, marked ⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧ and enrolled at §893 in **fourteen rows**.
⭐ **AND THE LESSON THAT COST THE MOST: reversibility answers MAY I, never SHOULD I.** The amended test is necessary and NOT sufficient. Pair every carve-out judgment with a measurement of the MERITS — a measuring lane before the ruling hardens into a car caught four bad rulings tonight, and vigilance caught none of them.

### ⭐ THE STATE, BY GIT
Product `claude/composite-r4` = **`f537ce47e`** (§891 LANDED, 27 cars, gate TRUE_EXIT=0, CHAIR-VERIFY GREEN, sealed `landing-891-2026-09-04`; pre-CAS base `ca651d54b` preserved). ⚠ **ZERO HEADROOM ON SEVEN MEASURES AT ONCE** — one em dash in a `src/data`/`src/domain` string literal, or one type error, breaches on arrival.

### ⛔⛔ THE BINDING CONSTRAINT ON EVERYTHING REMAINING: CENSUS HEADROOM
The census is FULL at 10/10; a new test file reds three censuses; and **five desk leaves plus the lighting wave's only bit-level witness each need one.** **6 of 10 slots are reachable, four in ONE act** — wire the eight authored annex families into `WAR_RECEIPTS`, ⚠ COUPLED with `kindPoolFloors`' `28 → 24` in the SAME commit or it reds both ways. **Order is mechanics: CURE FIRST (green + `bank the win` advisory), remove the row after (removing first = REGRESSION + red gate).** ⚠ Two slots have NO road: the prose car touches zero `.jsx`. Plan: `$SC/censushead/headroom-plan.md`.

### THE ROAD FROM HERE, RE-SCOPED BY MEASUREMENT
**GOLDEN is landable now** — 7/7 needed, the one collision conflicts on nothing, `commitTrailerRefusal` was a MISGRADE, and the real blocker is one `excludedEnvSpellings` entry (`$SC/goldenplan/landing-plan.md`). Then the **CHARSET EMBED** (0 B first paint, cures 41/41 — NOT the cancelled 27-widening). Then **LIGHTING** (151 doors, 113 dark, 91 dark in the default preset; ⚠ the wave has NO bit-level witness) and **DESK** (≈29 cars, ≈39–43 with the causal register; `{complexity}` is now a SHIPPING defect; 761,458 B of corpus is in NO build). Then W-ARMS → ⛔ the owner's WALK → the review → ⛔ FULL STOP before the soak.

### ⛔ THE LIVE-FAULT LIST IS NOT OPTIONAL READING
`$SC/RESUME-NOTE.md` CUTOFF #3. §893 re-measured four faults and all four are STILL UNWIRED: `respect` → `BOND_KINDS`, `rivalry` → `GRUDGE_KINDS`, `errandSpineEnabled` → `PREVIEW_OVERLAY`, eight families → `WAR_RECEIPTS`. **That is the characteristic shape of this program's debt: ruled, authored, never wired.**

## (superseded) PICKUP AT §892.1 — the seat is OPUS 5 and the Fable retrovalidation is HELD; the §893 card above supersedes, and its LIVE-FAULT pointer still holds.

`$SC` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/d5b9a39f-b0b2-4d9c-a1a0-08b291896f89/scratchpad` · `$SP` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad` · everything of the §892 sitting is in git at `refs/preserve/sitting-892-kit-2026-09-03b`. ⛔ OWNER CAP FOUR agents of any kind.

### ⭐ THE SEAT AND THE HELD PASS
Every ledger act from here: `Seat: Opus 5 — Fable-unvalidated` AND a row in `docs/FABLE_RETROVALIDATION_QUEUE.md` in the same commit (`chair-commit.sh` refuses otherwise; build the queue from `git show HEAD:…`, never the worktree copy). The §892 sitting is PARTIAL: 43 rows ruled; §882.8/§882.8.1/§882.9 · §882.12 + the §883.7 lane row · §885.6–§885.8 · §886 · §887 · §887.1 · §887.2 · §892.1's four lanes · the critic are OWED to a FABLE seat at the owner's trigger. **An Opus chair writes NO `*RULED (§892…)*` line.** ⭐ **THE OWNER'S STANDING CAUTION: before building on ANY Opus artifact, read the LIVE-FAULT LIST** — the paid-surface gap on the §891 train · RR-10 and ENC rows 2/3 ruled but never executed · the uncommitted ENC-3 season cap ignoring `nid` · the prose car's owed rebase · the seven GOLDEN cars UNLANDED (`commitTrailerRefusal` unwired; no freeze act until they land) · `{complexity}` a blocker on the economy desk's C1 pool · the writer-reach grade is GROUNDING, not display · the ratchet's budget misreport · DOOR 3's recurring timeout against a FULL 10/10 census · the tuning register charging a name-for-bare replacement · no prefix gate in `chair-commit.sh` · the hygiene debts · LEG-7 is a lane note, the `now: null` delegation unconfirmed · duplicate queue identifiers, §889.2 rowless.

### ⭐⭐ THE STATE, BY GIT, AND THE FIRST ACTS
**Product `claude/composite-r4` = `ca651d54b`** (§890, NOT CASed). **The §891 train = `15c6368a6`** (`refs/preserve/train891-reg7-2026-09-03`, 18 cars, all seven register acts in). **Lane O2GATE (Opus) is RUNNING on that dock** — the one car that must land before the CAS (brief `$SC/briefs/brief-O2GATE.md`; receipt `$SC/o2gate/receipt-o2gate.md`). In order: collect O2GATE → seal → LIGHTING refreeze re-taken (registers last; predict `files`/`credited`, leave titles unnamed) → `run-ratchet-891c.sh` on a QUIET box → ratchet register → seal `train891-final-2026-09-04` → `run-pregate-891b.sh` → `run-gate-891.sh` → `chair-verify-891.py` → CAS `ca651d54b`→tip → seal `landing-891-2026-09-04` → §893 (enrol O2GATE and the four §891 lanes: receipts `$SP/receipt-capacity.md`, `receipt-charset1.md`, `receipt-deskwiring.md`, `receipt-readerreview2.md`). Then `$SP/LANE-QUEUE.md`: HORIZON-DARK remainder → desk remainder → lighting wave (+ the prose car AFTER its rebase) → GOLDEN (land the seven cars first) → W-ARMS → ⛔ the owner's walk → the review → ⛔ STOP before the soak.

### ⛔ THE OWNER'S DESK — the five §891 items plus the three the walk added (one word on the `now: null` delegation · the twelve mutilated anchors as an OPEN row · the encounters exposure rate for the tuning sitting). Permanently the owner's: the walk · the tuning signature · every push.

### THE DEATH CLASSES, AGAIN, FOR THE NEXT CHAIR
The MODEL limit closed this Fable window (class 3: one seat dies, the other keeps working — O2GATE on Opus survives it); the SESSION limit killed eight Fable agents at ~20:0x (class 2: resume by run id after the reset); an account switch discards run ids (class 1: files and git survive). Read the failure text before choosing a recovery.

## (superseded) PICKUP AT §892 — ⚠⚠ **FABLE-LIMIT CUTOFF MID-SITTING, 2026-09-03 ~23:5x ET (Fable 5.1 chair, session d5b9a39f).** THE SITTING IS PARTIAL AND ON THE LEDGER — the §892.1 card above supersedes; its state block and its OWED list still hold.

`$SC` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/d5b9a39f-b0b2-4d9c-a1a0-08b291896f89/scratchpad` · `$SP` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad`. ⛔ OWNER CAP FOUR agents of any kind. **Everything of this sitting is in git: `refs/preserve/sitting-892-kit-2026-09-03` = `bd9554285` (251 files: every slice JSON, every ruling file, every builder and wrapper, the briefs, RESUME-NOTE).** A new MACHINE restores from that ref; the same machine reads `$SC` directly.

### ⭐ THE STATE, BY GIT
**Product `claude/composite-r4` = `ca651d54b`** (§890, NOT CASed). **The §891 train = `refs/preserve/train891-reg7-2026-09-03` = `15c6368a6`**, dock `$SP/laneKERNELMARK-tree`, 18 cars, porcelain 0, all seven register acts in (the seventh proven at §891.1). **Ledger = this §892 commit.** The queue now carries 43 `*RULED (§892, Fable 5.1)*` paragraphs and the amended §3 clause.

### ⭐⭐ THE SITTING IS PARTIAL — WHAT IS RULED AND WHAT IS OWED
485 calls across 17 complete slices (357 RATIFY · 104 AMEND · 10 REVERSE · 7 EVIDENCE-THIN · 7 OUT-OF-SCOPE); 43 rows RULED. ⛔ **OWED, walked, rulings not written:** §882.8/§882.8.1/§882.9 (S01) · §882.12 + the §883.7 lane row (S04) · §885.6 §885.7 §885.8 §886 §887 §887.1 §887.2 (S15 — COMPLETE on disk, unruled) · §892.1 the four §891 lanes (S20) · the critic S99. **Only a FABLE seat rules them** (`$SC/rulings-part8+.json` → `python3 $SC/build-queue-892.py` → `sh $SC/collect-892.sh`, the same pattern as this act; anchors are FRQ line numbers AT THE PRE-§892 BLOB — re-derive them against the new HEAD before building).

### ⛔ IF YOU ARE AN OPUS SEAT
Mark every act `Seat: Opus 5 — Fable-unvalidated` with a queue row (chair-commit.sh enforces it). You MAY: re-launch the unfinished slices as fact-gathering (`Workflow({scriptPath: <the kit's walk-workflow.js>, args: [ids not COMPLETE]})`, output marked `⟦OPUS-AUTHORED⟧`); dispatch the **O2GATE** lane from `$SC/briefs/brief-O2GATE.md` (the paid-surface gap on the train — car 19, BEFORE the CAS); then, on a QUIET box, the §891 landing: `run-ratchet-891c.sh` (predicted totalFiles 2458→2462; after O2GATE re-take the LIGHTING census first) → commit the ratchet register → seal → `run-pregate-891b.sh` → `run-gate-891.sh` → `chair-verify-891.py <dock> ca651d54b… <log> <cars>` → CAS → seal `landing-891-2026-09-03` → collect §893. You may NOT write a `*RULED (§892, …)*` line.

### ⛔ THE OWNER'S DESK (unchanged + three from the walk)
The prose window (rebase owed first) · the drift door · ENC-4's nine sentences (sheet amended) · 190 B "Room to Grow" · the deity-free corpus · **NEW:** one word confirming the `now: null` delegation §890.2 cites · the twelve mutilated anchors are an OPEN row (LEG-7 is a lane note) · the encounters exposure rate for the tuning sitting. Permanently the owner's: the walk · the tuning signature · every push.

### THE LAWS THIS DAY ADDED
- **A DECLARED AMENDMENT IS NOT AN AMENDMENT** — the clause text is the receipt (§3 was declared amended at §882.14 and never edited).
- **An append-only ruling queue needs FORWARD POINTERS** when a ruling is withdrawn, or a top-to-bottom walk meets it as live.
- **A first-mismatch throw hides the second drift** (the writer-reach bill moved two entries; the card saw one).
- **The writer-reach grade is GROUNDING, not DISPLAY** (a JSX-prop read grades N); vitest prints ACTUAL then EXPECTED.
- Three death classes, again: the SESSION limit killed 8 agents at ~20:0x and the run resumed by id after the reset; the MODEL limit is closing this window now — read the failure text.

## (superseded) PICKUP AT §891.1 — ⚠⚠ **WINDOW-CLOSE HANDOFF MID-WALK, 2026-09-03 ~19:55 ET (Fable 5.1 chair, session d5b9a39f).** — the §892 card above supersedes.

`$SC` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/d5b9a39f-b0b2-4d9c-a1a0-08b291896f89/scratchpad` · `$SP` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad` (the previous chair's: receipts, docks, LANE-QUEUE.md). **THE SEAT IS FABLE 5.1** (`Seat: Fable 5.1 — validated`); ⛔ OWNER CAP FOUR agents of any kind; every push, the walk and the tuning signature stay the owner's.

### ⭐ THE STATE, BY GIT
**Product `claude/composite-r4` = `ca651d54b`** (§890, NOT CASed). **The §891 train = `refs/preserve/train891-reg7-2026-09-03` = `15c6368a6`**, dock `$SP/laneKERNELMARK-tree`, **18 cars** over `ca651d54b`, porcelain 0, every car one strict `Seat:` trailer. **All seven register acts are taken** — the seventh (writer-reach `--write`) was predicted, reproduced byte-for-byte without touching the dock, committed and sealed by this chair (§891.1). ⚠ The §891 card's sentence about that bill was INVERTED (vitest prints actual then expected; the register said R, the tree measured N; the product still renders the string — the grade is a scanner-GROUNDING fact). **ONE act remains before the gate: the test ratchet `--update`** (pre-flight: WILL MOVE, NOT REFROZEN; predicted `totalFiles 2458 → 2462 CERTAIN`, totalTests refused in advance).

### ⭐⭐ THE WALK IS IN FLIGHT — FINISH IT FIRST, THEN RULE (§892)
The owner called the retrovalidation pass ("starting with the fable retrovalidation"). Workflow `wf_0a24cb28-a54` re-derives the Opus stratum §882.8→§891 as **nineteen READ-ONLY Fable verifier slices + a critic**, four at a time, each writing `$SC/walk/<id>.json`. **At the cutoff: S02 S05 S06 S08 S09 S11 S12 S14 S16 S17 S18 S19 COMPLETE; S07 S13 S15 in flight; S01 S03 S04 S10 + S99 not run.** ⛔ A run id dies with the account: re-launch the persisted script (`…/d5b9a39f…/workflows/scripts/fable-retrovalidation-882-891-wf_0a24cb28-a54.js`) with `args` = the slice ids whose file is not `COMPLETE` (re-derive from disk); the critic reads every file. Same session: `resumeFromRunId`. **No ruling is written yet** — the sitting is the chair's act over the slice files: `$SC/apply-rulings.py` (rulings under each row, built from the HEAD blob of the queue) + the §892 tail + `$SC/apply-892.py` + `chair-commit.sh` with the queue mapped (changed blob). ⚠ Never run the ratchet or the gate while slices run.

### THE LANDING, AFTER THE WALK (all scripted, all `sh -n` clean, box QUIET)
`$SC/run-ratchet-891c.sh` (nohup; TRUE_EXIT from the log) → commit the moved ratchet baseline as car 19 → seal `train891-final-2026-09-03` → `$SC/run-pregate-891b.sh` → `$SC/run-gate-891.sh` (~18 min) → `python3 $SC/chair-verify-891.py $D ca651d54b… $SC/gate-891.log 19` → CAS `ca651d54b`→tip → seal `landing-891-2026-09-03` → collect §893. Then `$SP/LANE-QUEUE.md`'s queue after §891.

### THE OWNER'S DESK — UNCHANGED (each releases finished work): the prose window · the drift door (TE-VIRT-1 left it empty deliberately; a NEW decision) · ENC-4's nine sentences · 190 B for "Room to Grow" (HELD) · the deity-free review corpus.

### LAWS THIS DAY ADDED
- **Read the assertion's argument order before narrating a register bill's direction** (vitest: actual, then expected).
- **The writer-reach grade is GROUNDING, not DISPLAY** — a JSX-prop read grades N; every desk extraction darkens web-display grades (memory `writer-reach-web-display-grade-is-a-grounding-fact`).
- A `--write` can be reproduced without touching a dock (`run()` with readBaseline/writeBaseline overrides); `TRUE_EXIT=$?` after a pipe is the pipe's exit.

## (superseded) PICKUP AT §891 — ⚠⚠ **ACCOUNT-SWITCH HANDOFF, 2026-09-03.** A SEVENTEEN-CAR TRAIN IS ONE BILL FROM ITS GATE — the §891.1 card above supersedes; its bill description is INVERTED (see §891.1).

### ⭐ THE ONE ACT TO DO FIRST — everything else on this card is context
The **§891 train is COMPOSED, SEALED and 17 cars deep** at `refs/preserve/train891-registers-2026-09-03` = **`4233031ba`**, in dock `laneKERNELMARK-tree`, porcelain 0, 17 trailers, base `ca651d54b`. **ALL SIX REGISTER ACTS ARE TAKEN AND GREEN.** Exactly **ONE bill** stands between it and its gate:

```
tests/lint/writerReach.walker.test.js :: the surfaceReach grammar round-trips
  situationDesc on economicState is missing from the frozen surfaceReach:
  expected 'dossier-pdf=R web-display=R …' to be 'dossier-pdf=R web-display=N …'
```
**It is a REGISTER, not a defect.** DESK CAR 1 mounted the economics prose so it now REACHES the web display; the frozen register still says that identity is dark (`N`). `.writer-reach-baseline.json` is named in the HORIZON-DARK carlist's standing-bill list as a landing register. **The door is `node scripts/check-writer-reach.mjs --write`** (it conflicts with `--genesis`/`--rebank`; use plain `--write`, this is a shrink/absorb, NOT a genesis). **PREDICT the movement in writing first, then measure.**

**Then, in order:** ratchet (`sh scripts/gate-mutex.sh --run -- node scripts/check-test-ratchet.mjs --update`; predicted `totalFiles 2458 → 2462 CERTAIN (+4)`, totalTests REFUSED IN ADVANCE) → bare gate (`npm run check`) → chair-verify → CAS `git update-ref refs/heads/claude/composite-r4 <tip> ca651d54b` → seal `landing-891-2026-09-03` → collect §891.

### ⚠⚠ ACCOUNT-SWITCH SPECIFICS — read before you assume anything is gone
**Run ids and background tasks DIE with the account; git and the filesystem do not.** The scratchpad root is `/private/tmp/claude-502/…` where **502 is the OS uid, NOT the Claude account** — so a new account on the SAME MACHINE as the same OS user CAN still read every path below. A new machine cannot, and then only what is in git survives. `$SP` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad`:
- `$SP/horizondark-dock-residue/HORIZON-DARK-CARLIST.md` (27 KB) — **the derived HORIZON-DARK car list: 11 chair-class, 3 owner-gated, with §5's standing bill.** 6 of the 11 are built; **5 remain.**
- `$SP/ENC4-TYPED-HOLES-FOR-THE-VOICE-SITTING.md` (10 KB) — **the owner's nine sentences, in the annex format they fill.**
- `$SP/c2-HELD-outlook-surface.patch` (11 KB) — the 190-byte decision below, one `git apply` from taken.
- `$SP/LANE-QUEUE.md` · 12 `$SP/receipt-*.md` · 5 `$SP/salvage-*/` dirs (byte-identical copies of every dirty path from the API mass-death).

### THE BRANCH, AND WHAT IS SEALED BUT NOT LANDED
**`claude/composite-r4` = `ca651d54bab1fc68d5c417226d6a5f719562cc5b` (§890, the FIFTEENTH landing).** Registers at that tip: lighting `2510/370/2140/22841/6142` · ratchet `31,141 / 2,458`, **census FULL at 10/10, ZERO headroom** · tuning 225 tables / 2,094 keys · OSR `1,993 / 1,409 / 388`.

| sealed ref | tip | state |
|---|---|---|
| `train891-registers-2026-09-03` | `4233031ba` | **THE LIVE TRAIN, 17 cars, one bill from its gate** |
| `srcprose-2026-09-03` | `8f4d5c648` | 1,001 cured sentences ⛔ **OWNER: the chartered window** |
| (ENC-3) `laneENC-tree`, 13 dirty paths | — | built, uncommitted ⛔ **OWNER: the drift door** |

### ⛔⛔ THE OWNER'S DESK — THREE DECISIONS AND TWO PRICED ITEMS, EACH RELEASING FINISHED WORK
1. **THE PROSE WINDOW.** A landed fence (`espionageDormancyFence.test.js:332`) re-records the driven-corpus constant **ONLY inside a NAMED CHARTERED WINDOW**, and *"exactly ONE remains: the LIGHTING WAVE (§881.4)"*. The chair RULED the prose car **rides the lighting wave** rather than minting a second window, because both spent windows were named by owner directives. **One owner word charters it sooner.** 8 files / 11 assertions stand owed.
2. **THE DRIFT DOOR.** ENC-3 is blocked. The funnel door is reserved to TE-VIRT-1 by a landed test, and **TE-VIRT-1 ALREADY RAN AND DELIBERATELY LEFT IT EMPTY** (*"two empty files, and the emptiness is the deliverable"*), so homing it is a **NEW decision, not a pending one.** ⚠ The chair's contrary ruling was **WITHDRAWN at §890.1** — do not re-make it.
3. **ENC-4's NINE SENTENCES.** The pool floor needs 6 and 4; the design spells 2 and 1 plus the literal words *"· four more"*. Holes enumerated in `$SP/ENC4-TYPED-HOLES-FOR-THE-VOICE-SITTING.md`.
4. ⭐ **190 BYTES** of a 1,004-byte first-paint margin buys the visible "Room to Grow" section — **built, green, and HELD rather than spent without asking.**
5. ⭐ **THE REVIEW CORPUS IS DEITY-FREE** — 0 of 44 settlements ever carry a patron, the Faith tab is a shell on all 44, and `subsystemRowsBaseline.js:60` already records it. **Faith cannot be judged during the owner's walk until that roster widens, and widening it is the owner's act.**
⛔ Permanently the owner's: **the walk · the tuning signature · every push.**

### ⭐⭐ THE LAWS THIS DAY EARNED — a successor who reads only these still lands cleanly
- ⛔ **A DOCK'S `node_modules` FORM CHANGES THE BUILT ARTIFACT.** Packages are symlinks **by design**; materialising 45 grew first paint **8,551 B past budget** and failed a gate (proved both directions). **NEVER materialise them.** The one exception is the edge-bundle sequence: materialise ONLY `immer`+`seedrandom` → `npm run build:edge-shared` → commit the bundles → **RESTORE THE SYMLINKS**. The reproducibility arm still verifies because its fallback read follows the link.
- ⛔ **A FALLBACK BRANCH MUST NEVER PRINT A FINDING.** `cmd 2>/dev/null || echo "<a claim>"` turns an unexplained failure into evidence. Three false statements today came from this shape.
- ⛔ **THE GUARD AND THE CONSEQUENCE MUST LIVE IN THE SAME PLACE.** A guard in Python with the act in shell declines its own edit while every later step proceeds. **`set -e` at the head of any multi-step act.**
- ⭐ **VERIFY THE ANTECEDENT, NOT ONLY THE INFERENCE.** An empty structure held empty BY A LANDED TEST is the opposite of available. **An absence has a history, and the history is in the log.**
- ⭐ **THE SUM OF INDEPENDENTLY-MEASURED DELTAS IS NOT THE COMPOSED DELTA** (four lanes summed +75 titles; the composed tree measured +78). Same shape as: **the arm count NEVER predicts the census delta.**
- ⭐ **A LANE AND A LANDING ARE NOT THE SAME VANTAGE POINT** — a register act is legitimate at exactly one of them.
- ⚠ **zsh does NOT word-split an unquoted parameter** (`for s in $shas` takes the whole string). ⚠ **A predicate that can match its own command line is not a gate** (`grep -c 'vitest'` counts its own grep; use `'[v]itest'`). ⚠ **A full-suite instrument needs a quiet window for its DURATION, not at its start** — `$SP/run-ratchet-quiet.sh` mechanizes it.
- ⭐ **THE OWNER'S STANDING RULE, ADDED TODAY: WHEN THE OWNER ASKS A QUESTION, ANSWER IT FIRST, BEFORE ANY WORK.**

### ⏱ LIVE STATE AT HANDOFF
**RUNNING: 0 agents of 4.** No gate, no lock held. Board clean, ~7 GB free on `/private/tmp`. ⛔ **OWNER CAP: FOUR agents of ANY kind** — a lane's own recon child exceeded it once today, so **every brief must forbid subagents.** ⚠ The 30-minute heartbeat template is **STALE**: it names the §882/TAIL-F track and asks a `Seat: Fable 5.1` trailer. **The seat is OPUS 5** and the template is overridden, not obeyed — every ledger act carries `Seat: Opus 5 — Fable-unvalidated` AND maps `docs/FABLE_RETROVALIDATION_QUEUE.md` with a CHANGED blob.

## (superseded) ⭐⭐⭐⭐ PICKUP AT §890 (2026-09-03 — the FIFTEENTH landing is down; the four lighting rows the owner delegated are ALL LANDED, a live news-desk bug is cured, and THREE DECISIONS SIT ON THE OWNER'S DESK each releasing finished work) — START HERE; supersedes every card below.

⭐⭐ **§890 IS LANDED.** `claude/composite-r4` = **`ca651d54bab1fc68d5c417226d6a5f719562cc5b`**, sealed `refs/preserve/landing-890-2026-09-03`. **8 cars, 22 paths +855/−88**, porcelain 0, trailers == cars. **Gate 1,058 s, 20 stages**: ratchet **10 known of 31,141** · `STRICT DIST OK 52/440` · typecheck **173/173** · domain-strict **1121/1121**. **WHAT IT PUTS ON THE BRANCH:** the eight war sub-flags are free of a stale ruling; **founder recognition is lit and the gate deciding who qualifies now CARRIES ITS EVIDENCE instead of asserting entitlement**; the faith tuning surface is **candidate-lit and must now SAY it is a draft** — reporting SIGNED is structurally unconstructible; and **five sovereignty headlines stop filing to the wrong desk** (`cession_for_peace`, `bought_seat_fragility`, `kinship_opposes_the_sale`, `sale_books_diverged`, `sovereignty_sale_judged`), because the persistence rebuilder was stripping the authority on EVERY APPEND while the router honoured four registries and it admitted three.

⛔⛔ **THREE DECISIONS ARE THE OWNER'S AND EACH RELEASES WORK THAT IS ALREADY BUILT AND PROVEN.** **(1) THE PROSE WINDOW** — `refs/preserve/srcprose-2026-09-03` `8f4d5c648` holds **1,001 cured reader sentences across 200 paths**, blocked only because a landed fence says the driven-corpus constant is re-recorded ONLY inside a **NAMED CHARTERED WINDOW** and *"exactly ONE remains: the LIGHTING WAVE (§881.4)"*. The chair RULED IT RIDES THE LIGHTING WAVE rather than minting a second window, because both spent windows were named by owner directives — **one word charters it sooner.** **(2) THE DRIFT DOOR** — ENC-3's 13 files are built and its dock is intact, but the funnel door is reserved to TE-VIRT-1 by a landed test, and **TE-VIRT-1 ALREADY RAN AND DELIBERATELY LEFT IT EMPTY** (*"two empty files, and the emptiness is the deliverable"*), so homing it is a NEW decision. **(3) ENC-4's NINE SENTENCES** — the pool floor needs 6 and 4, the design spells 2 and 1 plus *"· four more"*; holes enumerated in the owner's own annex format at `$ME/ENC4-TYPED-HOLES-FOR-THE-VOICE-SITTING.md`.

⚠⚠ **THE HAZARD THIS LANDING FOUND, AND IT CAN TRIP ANY LANE: A DOCK'S `node_modules` FORM CHANGES THE BUILT ARTIFACT.** The first gate attempt failed a REAL verdict — *first-paint static closure 1,056,551 B against a 1,048,000 budget, over by 8,551* — and **the cause was the chair materialising 45 symlinked packages** while chasing an unrelated bug. **Proved in both directions:** a sibling dock with the same lighting cars and symlinks intact built GREEN, and restoring the 45 symlinks returned the landing dock to GREEN with commits untouched. ⭐ **The byte budget is only meaningful in a dock with the canonical symlinked layout; a lane that materialises packages reads a FALSE RED against it.** ⭐ **AND THE TWO CONSTRAINTS THAT LOOKED CONTRADICTORY ARE NOT:** build the edge bundle ONCE with packages materialised so its recorded input paths are clean, commit it, then RESTORE the symlinks — the reproducibility arm re-verifies because its fallback read follows the link. All four arms green simultaneously.

⏱ **LIVE STATE (session 58f0a8e2, 2026-09-03 ~18:5x UTC — refreshed at the §890 collection; RUNNING 0 of 4; ⛔ OWNER CAP: FOUR agents of ANY kind).** ⭐⭐ **THE SEAT IS OPUS 5.** ⚠ The 30-minute heartbeat template is STALE — it names the §882/TAIL-F track and asks a Fable trailer; **the seat is OPUS 5 and the template is overridden, not obeyed.** ⭐ **THE OWNER'S STANDING RULE, ADDED TODAY: WHEN THE OWNER ASKS A QUESTION, ANSWER IT FIRST, BEFORE ANY WORK.** `$ME` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad`.

**Branch `claude/composite-r4` = `ca651d54bab1fc68d5c417226d6a5f719562cc5b` (§890 landed — the FIFTEENTH landing this arc). Ledger through §890.** Sealed `refs/preserve/landing-890-2026-09-03`. **Registers now: lighting `2510/370/2140/22841/6142` · ratchet `31,141 / 2,458`, 10 entries 0 removed · tuning inventory 225 tables / 2,094 keys · OSR `1,993 / 1,409 / 388`.** Census still **FULL at 10/10, ZERO headroom.**

⭐⭐ **SEALED, PROVEN AND WAITING FOR A TRAIN:** `nownull-2026-09-03` **`03da380ab`** (the wall clock leaves the regional seams — and the car turned out to be a CLASS: `now = null` was free while null and undefined were interchangeable and became a bug the moment they diverged; **every other arm of the clock-seam walker is structurally blind to it**) · `srcprose-2026-09-03` **`8f4d5c648`** (1,001 sentences; ⛔ gated on the owner's window) · ENC-3's 13 dirty paths in `laneENC-tree` (⛔ gated on the drift door). ⚠ **ENC-3 committed NOTHING and was right to** — it refused a chair ruling, brought evidence, and the ruling was WITHDRAWN at §890.1.

**THE ARC FROM HERE:** land `nownull` → ⛔ the owner's three decisions → **HORIZON-DARK's 13 chair-class cars** → **desk wiring + the 68 unmounted dossier blocks** → the **LIGHTING WAVE** (which also carries the prose car) → **GOLDEN freeze** (⚠ walk GOLDEN-BUILD's R3/R4/R6/R7 FIRST — they MINT the freeze-register schema and no seat has read them) → **W-ARMS** → ⛔ **the owner's walk + ONE regen** → the **exhaustive review** (§879.6) → ⛔ **FULL STOP before the terminal soak.** ⛔ **STILL THE OWNER'S:** the walk · the tuning signature · every push.

## (superseded) ⭐⭐⭐⭐ PICKUP AT §889 (2026-09-03 — the FOURTEENTH landing is down, gate GREEN ON THE FIRST RUN; the biggest train of the arc at FIFTEEN cars, and the owner has DELEGATED the last of the held rows; the seat is OPUS 5) — START HERE; supersedes every card below.

⭐⭐ **§889 IS LANDED — every sealed car in the program, composed into one train.** `claude/composite-r4` = **`30c1667bc31b9b6f7c64683fddd79a904aad3fee`**, sealed `refs/preserve/landing-889-2026-09-03`. **15 cars, 59 paths +4,746/−1,356**, porcelain 0, **trailers == cars exactly**. **GATE GREEN ON THE FIRST RUN, 1,034 s, 20 stages**: ratchet **10 known of 31,123, ceiling 10** · `STRICT DIST OK 52/440` · typecheck **173/173** · domain-strict **1121/1121**. **WHAT IT PUTS ON THE BRANCH:** the em dashes are gone from the prose a player reads (338 from the dossier corpus, 418 of 445 from the ten annexes); two people who meet across a border can leave a bond, a respect or a rivalry on each other, and a man's standing is no longer handed to whoever takes his slot when a town rerolls; and **the world clock stops leaking into generation at every caller** — fourteen unpinned instants across six lifecycle paths, cured at a chokepoint that already existed and which only 2 of 13 sites were using.

⭐⭐⭐ **THE RECEIPT OF THE DAY: TWO INSTRUMENTS THAT KNOW NOTHING OF EACH OTHER AGREED TO THE UNIT.** The ratchet's shrink-only arm banked **voiceMechanics [em] 720 → 382** — a drop of **exactly 338**, the em-dash count the corpus lane measured and removed by a completely different method. Total voice debt fell **1,108 → 770**, a 31% cut. **Two independent measurements of one removal agreeing exactly is the strongest form of receipt this program produces.**

⭐⭐ **THE CURE THAT MATTERED MOST WAS A DETERMINISM GUARD REFUSING TO BE SILENCED.** ENC-2 minted a fork label embedding `::` — and the kernel derives child seeds by **plain concatenation with no escaping**, so `fork('a::b')` and `fork('a').fork('b')` are ONE STREAM. Two substreams meant to be independent would draw identical values forever and only a coincidence would surface it. The guard refuses a new family until someone proves no chain aliases it, so **the check was run over all 118 literal `.fork(` sites in `src`**: the head appears at exactly ONE (its own), the reserved root head `epoch` at ZERO, and the interpolated parts cannot carry the delimiter — ⚠ **`now` there is `Math.floor` of a TICK, not a wall clock; a wall clock inside a SEED LABEL would break THE PROMISE outright.** The whole check is written into the file beside the row, in the convention its own prior row set.

⏱ **LIVE STATE (session 58f0a8e2, 2026-09-03 ~13:0x UTC — refreshed at the §889 collection; ⭐ FOUR LANES RUNNING, at the owner's cap: ENC-3 (the stage, ATOMIC) · ENC-4 (the Herald, built in parallel against the DESIGN as contract) · the `src/` prose sweep + the voice-guard blind spot · the four delegated LIGHTING rows).** ⭐⭐ **THE SEAT IS OPUS 5.** ⚠ **THE HEARTBEAT TEMPLATE IS STALE** — it names the §882/TAIL-F track and asks a Fable trailer; the seat is OPUS 5 and it is overridden, not obeyed. ⭐ **THE QUIET-WINDOW LAW IS MECHANIZED:** `$ME/run-ratchet-quiet.sh` and `$ME/run-gate-889.sh` each wait for three consecutive readings under 4.0 with zero vitest workers. `$ME` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad`.

⛔⛔ **THE OWNER DELEGATED THE HELD ROWS ON 2026-09-03, IN CHAT, BY NAME.** Now the chair's: **ENC-5's sentences** (still written for the owner's voice sitting, not shipped past it) · the **`now: null` → `string|null` schema change** · the **~908-site `src/` prose sweep** · the **three horizon-dark cars incl. THE CULL** · the **four lighting rows O-12/O-16/O-17/O-10(b)**. The encounters tuning VALUES were clarified as belonging to the tune-and-soak pass — enrolled draft now, numbered then. ⛔ **TWO THE CHAIR DID NOT TAKE:** **the WALK** (it is the owner experiencing the world; a machine reporting "the walk went fine" is the emptiest artifact this program could produce — the chair builds everything up to it) and **the TUNING SIGNATURE** (raised ONCE, because THE PROMISE is constitutional and says in the owner's own words that tuning is owner-signed; awaiting one word, then it is done and recorded as an amendment). ⛔ **EVERY PUSH remains the owner's — it was not in the delegated list.**

**Branch `claude/composite-r4` = `30c1667bc31b9b6f7c64683fddd79a904aad3fee` (§889 landed — the FOURTEENTH landing this arc; §874 · §875 · §876 · §877 · §880 · §881 · §882 · §883 · §884 · §885 · §886 · §887 · §888 · §889). Ledger through §889.** Sealed `refs/preserve/landing-889-2026-09-03`. **Registers now: lighting `2510/370/2140/22823/6139` · ratchet `31,123 / 2,458`, 10 entries 0 removed · tuning inventory **225 tables / 2,094 keys** · OSR `1,993 / 1,409 / 388` · prose-numerics 225 rows.** Census still **FULL at 10/10, ZERO headroom**. ⭐ **FIVE register acts on this train and every derivable figure was PREDICTED EXACT** — lighting files 2510, tuning tables 225 AND keys 2094, ratchet files 2458 — while every underivable one was refused in advance and measured. The one earlier miss (keys +40 vs +26 at §888) is what taught the counting rule that made keys exact here.

⚠⚠ **TWO CHAIR ERRORS ON THIS TRAIN, BOTH BANKED LAWS BROKEN AFTER BEING WRITTEN DOWN THE SAME DAY.** (1) The chair **`sed`-patched a shell script**, its `.*` ate a closing quote, **`sh -n` FAILED and the chair launched it anyway** — because only the echo was gated on the check, not the launch. Both laws were recorded that morning after the identical failure. Cured: the wrapper is rewritten from scratch, never patched, and the launch line is now genuinely conditional. (2) A merge resolution **asserted AFTER writing**, so a wrong assertion still left a bad file and the shell committed it with no `set -e`. ⭐ **BUILD IN MEMORY, ASSERT, THEN WRITE — an asserting edit must ABORT THE ACT, not merely decline it.** Both later edits followed it and both caught real errors before anything was written.

**THE ARC FROM HERE:** ⏳ **the four running lanes** → **HORIZON-DARK's 13 chair-class cars** (incl. the three the owner just delegated) → **desk wiring + the 68 unmounted dossier blocks** → the **LIGHTING WAVE** → **GOLDEN freeze** (⚠ walk GOLDEN-BUILD's R3/R4/R6/R7 FIRST — they MINT the freeze-register schema and no seat has read them) → **W-ARMS** → ⛔ **the owner's walk + ONE regen** → the **exhaustive review** (§879.6) → ⛔ **FULL STOP before the terminal soak.**

⛔⛔ **§889.1 — TWO THINGS A SUCCESSOR MUST NOT LOSE.** **(1) ENC-4 IS STOPPED BEFORE ITS FIRST BYTE AND IS THE OWNER'S:** the Herald pool floor needs **6 + 4** authored variants, the design spells **2 and 1** with the literal words *“· four more”*, so **NINE SENTENCES WERE NEVER WRITTEN BY ANYONE**; the floor walker refuses stubs by design and the car CANNOT be split (registering without pools reds the floor; routing without registering breaks an exact-equality census). Typed holes in the owner's own annex format: `$ME/ENC4-TYPED-HOLES-FOR-THE-VOICE-SITTING.md`. ⭐ **J29 is DISCHARGED on the way** — ENC-4's five files are outside the 237-module first-paint closure, **delta 0 B**. **(2) A LIVE DEFECT IN LANDED CODE:** the Herald router honours an authored desk for **four** registries but the persistence rebuilder's allowlist admits **three** — **`sovereignty_registry` is MISSING** — and it runs on **every append**, not only on load. **Probed with a control: the sovereignty authority is DROPPED, `envoy_registry` SURVIVES.** ⇒ every sovereignty news item loses the authority that gives its desk force, and the desk falls to the prefix rule. **QUEUED as its own car.** ⭐ **RULINGS:** the Herald kind is **`chance_meeting_recorded`** (`chance_meeting` is already a PINNED corruption-leash conspiracy token; the constant is NOT renamed) · the second is **`chance_meeting_exposed`** (the owner's row-6 word outranks the contradictory §5.5) · the seam is **ONE exported builder** ENC-3 calls per outcome. ⚠ **AND THE CHAIR'S OWN BRIEF WAS REFUTED BY MEASUREMENT:** module-eval validation of a catalog row is true of **1 of 11** registries, not all — **totality here is enforced by WALKERS, not by module evaluation.**

## (superseded) ⭐⭐⭐⭐ PICKUP AT §888 (2026-09-03 — the THIRTEENTH landing is down, gate GREEN ON THE FIRST RUN; chance meetings between named people now exist in the engine, and a ~0.45% RANDOM RED that could fell any landing gate is cured at cause; the seat is OPUS 5) — START HERE; supersedes every card below.

⭐⭐ **§888 ENC-1 IS LANDED.** `claude/composite-r4` = **`bbba1be240c84bf0fd969079fc9fd2aa97a18906`**, sealed `refs/preserve/landing-888-2026-09-03`. **8 cars, 11 paths +1,848/−24**, porcelain 0. **GATE GREEN ON THE FIRST RUN, 1,016 s, 20 stages**: ratchet **10 known of 31,048, ceiling 10** · `STRICT DIST OK 52/440 zero non-run` · typecheck **173/173** · domain-strict **1121/1121**. **WHAT IT PUTS ON THE BRANCH:** two named people who are in the same place at the same time can now **meet**, and the meeting can leave a mark that outlives it — a bond, a respect, a rivalry — **without the engine ever deciding either person's fate.** ⭐⭐⭐ **THE FINDING THAT OUTLIVES THIS LANDING: A ~0.45% RANDOM RED HAS BEEN LIVING IN THE GATE.** A byte-identity arm compares two separate appends whose `createdAt` comes from `new Date().toISOString()` at millisecond resolution whenever the pin is null — measured **88 mismatches / 20,000 on an idle box, 100% with a forced 3 ms gap, 0% with one threaded stamp.** It is PRE-EXISTING, unrelated to this consist, and **every landing gate this arc has passed rolled that die and won.** Cured at cause here. ⚠ **TWO LANES REACHED IT INDEPENDENTLY FROM OPPOSITE ENDS** — the determinism lane proved `now: null` is a pin that is not one at 979/200,000 (0.489%), the census lane hit the same fallthrough from a failing byte-identity arm.

⛔⛔ **THE CORRECTION §887.2 OWED, AND IT MATTERS MORE THAN THE ERROR IT FIXES.** §887.2 recorded that both early ratchet refusals were 'contention the chair created'. **That is incomplete and the incompleteness is the lesson.** Contention was real — but what it did was **fire an EARLIER gate than the actual defect**: the SCOPE SENTINEL checks for unmeasurable suites *before* the census check runs, so a starved soak short-circuited both runs and reported a resource artifact while **five genuine census bills sat underneath, unmentioned.** ⭐⭐ **A STARVED RUN DOES NOT MERELY WASTE TIME — IT REPORTS THE WRONG REASON**, and the chair acted on that wrong reason twice. ⭐ The starvation signature banked at §886 ('a DIFFERENT test each time') is also **amended**: `centuryLegSoak` is the most expensive test in the tree, so under contention it is the **FIRST** casualty every time, not a random one. **The true signature is a RESOURCE failure rather than an ASSERTION failure on a test FOREIGN to the consist** — 0 ms against a 900,000 ms budget with no timeout signal, no assertion signal and no failure message did not fail, it never ran.

⭐ **THE FIVE BILLS, AND THE LANE REFUSED THE EASY CURE ON THREE OF THEM.** The leaf had imported straight from the drift module; enrolling that would have minted **a SECOND production door** into a family whose whole invariant is that it has ONE, and adding it to the family list would have **exempted it from the writer seals** — the exact laundering the census exists to prevent. The cost of the easy road was **measured, not asserted**: the leaf's static closure would go **10 modules / 211 KB → 111 modules / ~2.0 MB**, putting the world-state substrate (a clock included) behind eight lines of arithmetic in a file whose header says PURE. The value now **arrives as an argument**. Two arms were honest enrolments after proving the read correct and the leaf reached by nobody. **Behaviour identity proven by 268,800 receipts compared byte-for-byte, 0 diffs, with a negative control that fires.** ⚠ One declared contract shift — `party.known` → `party.knownAxes` plus `positionValue` on the entry bag; **no importer exists in `src`, so no world moves, and ENC-3 inherits the obligation to hand both down.**

⏱ **LIVE STATE (session 58f0a8e2, 2026-09-03 ~13:3x UTC — refreshed at the §889.3 SALVAGE checkpoint; ⭐ FOUR LANES RUNNING AT THE CAP, and ALL FOUR ARE RESUMES after a simultaneous API-500 mass death: ENC-3 the stage · the now-null root fix · the src/ prose sweep · the lighting rows at O-17).** ⛔⛔ **THE SALVAGE, AND A SUCCESSOR MUST UNDERSTAND IT:** the lighting lane held **TWO COMMITTED CARS REACHABLE FROM NO REF** (O-12 `b6ca05958`, O-16 `f47433b32`) — sealed at `refs/preserve/lighting-rows-wip-2026-09-03`. Every dirty path in all four docks is copied byte-identically with a patch and manifest to `$ME/salvage-{enc3,srcprose,nownull,lighting}/`, one copy `cmp`-verified; **the docks were NOT reverted, so the live work is still in place and the salvage is insurance only.** ⭐ **THE §882.14 SURVIVAL LAW HAS NOW PAID OUT SIX TIMES IN ONE DAY.** ⭐⭐ **THE BRIEF-LEVEL LAW THIS EARNS:** every ordinary lane brief says *"if the dock is dirty, STOP"*, which for a RESUMED lane is exactly wrong — **the dirt is its own work**. A resume brief MUST open with the dirty path list and *"THAT DIRT IS YOUR OWN WORK. DO NOT STOP. DO NOT RESET."* ⚠ And **a lane's own recon child is how the four-agent cap gets exceeded** — one did, making five; every resume brief now forbids subagents. ⭐ **O-10(b) IS CLOSED WITHOUT A RAISE: residual MEASURED at ZERO** — three builds (base, dial-flipped, reverted control) all at **675,764 B / ceiling 676,000 / margin +236 / identical content hash**; the documented `+1,047 B` was priced for a shape the source records as **not taken**. `$ME` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad` (LANE-QUEUE.md · salvage-*/ · receipt-*.md · ENC4-TYPED-HOLES-FOR-THE-VOICE-SITTING.md).

**Branch `claude/composite-r4` = `bbba1be240c84bf0fd969079fc9fd2aa97a18906` (§888 landed — the THIRTEENTH landing this arc: §874 · §875 T8 · §876 WAR · §877 HYGIENE · §880 INSTRUMENTS · §881 COUPLED+PAID · §882 TAIL-F · §883 T13 · §884 HORIZON-INSTR · §885 CAPSULE+FILL-LAYER · §886 NEWS · §887 TRAIN 1 · §888 ENC-1). Ledger through §888.** Sealed `refs/preserve/landing-888-2026-09-03`. **Registers now: tuning inventory 225 tables · lighting `2506/370/2136/22748/6122` · ratchet `31,048 / 2,454`, 10 entries, 0 removed; OSR `1,993 / 1,409 / 388` proven NOT owed at this tip (exit 0, baseline undirtied).** The known-failure census stays **FULL at 10/10 with ZERO headroom.** ⚠ **8 cars carry 7 trailers:** `394d758ee` has none, and it was **NOT rewritten because the tuning inventory cites that exact sha as its `measuredAtSha`** — a rebase would leave a register citing a commit that does not exist. Vetoable judgment at `$ME/JUDGMENT-missing-trailer.md`; the cause is fixed forward in the lane-brief template.

⭐⭐ **EIGHT CARS ARE SEALED AND WAITING, ALL PROVEN, NONE LANDED** (`$ME/LANE-QUEUE.md` has the ref table): `enc2-enc6-2026-09-03` **`f052d1e77`** (ENC-2 the ledger + the three willed-leash fences, ENC-6 the reroll fold) · `emdash-2026-09-03` **`4da38b8d3`** (the 338 dossier-corpus em dashes) · `emdash-annex-2026-09-03` **`e46d4c3f1`** (418 of 445 annex dashes; **27 REFUSED because they are byte-twins of shipping `src/` template literals a walker asserts equal — they must be cured on BOTH sides in ONE act**) · `horizondark-cars12-2026-09-03` **`43e063a70`** · `determinism-wip-2026-09-03` **`55d77a8d2`** (the wall-clock leak at every caller + the `now: null` false pin). ⚠ **ENC-2/ENC-6 branch from `394d758ee`, not from the §888 tip** — recompose before boarding.

⭐ **ENC-2's LANE CORRECTED THE CHAIR'S OWN FENCE, and it is a STATE-NEVER-FATE hole.** §882.13's row 5b specified three fences; **fence 3 was a HALF-FENCE** — it guarded `anyCompromise` at `pulseKernel.js:1757`, but that gate is an `||`, so **any world already carrying a cause ledger opens the pass through the other arm and attributes the willed leash anyway.** The fence that was ruled would have held only in worlds that had never had a scandal. The guard now sits on `causeLifecycle`'s per-NPC attribution loop instead. ⚠ ENC-2 also found a contradiction with its **own** committed header: the drop pass must run before the flag read, but `advanceEnvoyDiplomacyPulse` returns early at `:134` **above** the stage's mount point, so **a world that darkens `envoyDiplomacyEnabled` leaks the key** — this program's most-bitten class, caught before the car that would have shipped it.

**THE ARC FROM HERE:** **recompose + land the eight sealed cars** (ENC-2/6 · em dashes ×2 · horizon-dark ×2 · determinism ×2) → **ENC-3 + ENC-4** (⛔ ENC-3 is ATOMIC by its own charter — `livedExperienceSources.js` THROWS at module evaluation for a receipted row with no resolvable adapter, taking seven importers down, so row + entry + symbol are ONE commit) → the **voice-guard BLIND SPOT** (`voiceMechanics` scans only `src/data` + `src/domain` + a jsx arm; **`src/generators/` holds 496 em dashes of reader prose and NO guard sees it** — MACHINERY, chair-class) → **desk wiring** + the 68 unmounted dossier blocks → **HORIZON-DARK's remaining 13 chair-class cars** (⛔ 3 owner-gated: THE CULL, the charset paid-surface car, the COVERAGE ceiling raise) → the **LIGHTING WAVE** → **GOLDEN freeze** (⚠ walk GOLDEN-BUILD's R3/R4/R6/R7 first — they MINT the freeze-register schema and no seat has read them) → **W-ARMS** → ⛔ **the owner's walk + ONE regen** → the **exhaustive review** (§879.6) → ⛔ **FULL STOP before the terminal soak.** ⛔ **STILL THE OWNER'S:** ENC-5 (the words, at the voice sitting) · encounters row 7 (tuning values) · the `now: null` → `string|null` schema change · the ~908-site `src/` prose sweep (chair RECOMMENDS doing it before GOLDEN; awaiting veto) · LIGHTING O-12/O-16/O-17/O-10(b) · the walk · the tuning signature · every push.

## (superseded) ⭐⭐⭐⭐ PICKUP AT §887 (2026-09-03 — the TWELFTH landing is down, gate GREEN ON THE FIRST RUN; the dossier can now say WHERE, and ENC-1 is built and waiting on its own dock; the seat is OPUS 5, every act Fable-unvalidated) — START HERE; supersedes every card below.

⭐⭐ **§887 TRAIN 1 IS LANDED.** `claude/composite-r4` = **`f123601850917f549d0d6e34a44b14676b7e6e66`**, sealed `refs/preserve/landing-887-2026-09-03`. **7 cars, 17 paths +1,423/−100**, porcelain 0, 7 trailers, all single-parent. **GATE GREEN ON THE FIRST RUN, 1,039 s, 20 stages**: ratchet **10 known of 31,016, ceiling 10** · `STRICT DIST OK 52/440 zero non-run` · typecheck **173/173** · domain-strict **1121/1121**. **WHAT IT PUTS ON THE BRANCH IN PLAIN WORDS:** a minor crime wave can no longer print the catastrophic sentence (the kernel finally has the **demotion channel its design assumed and never implemented** — and its arm was **red against a byte-identical copy of the base reader** before it was green), the SECTION-TARGET field is **no longer parsed by splitting on a comma** but read the way its own neighbour always has been (closed vocabulary at last; four prose leaves move 584 bytes for the first time), and the dossier gained **a router that can say WHERE a fact belongs**, one fact to exactly one sentence — shipping **empty at birth on purpose** so its walker's controls red on the defect before the table holds a row, with the dark half now **68 blocks a gate can count**. ⚠⚠ **THE FINDING: A COLLISION THAT EXISTED ONLY AT THE COMPOSED TIP.** §886's fill-layer guard treats every exported string map in the desk directory as a candidate fill table; Train 1's registry landed one there. **Both cars correct, the pair not**, and **no dock could see it because no dock held both** — cured through the guard's own pre-built `NOT_A_FILL_TABLE` door with a true reason: **classified, not exempted.** ⭐⭐ **THE ARM-COUNT LAW IS NOW CONFIRMED FOUR LANDINGS RUNNING, three figures for this one consist: 16 walker arms · +20 census titles · +22 ratchet tests.** Both register acts again predicted the derivable figures EXACTLY and left the underivable ones deliberately UNNAMED. ⚠ **A CHAIR ERROR ON THE RECORD:** the cures-first-registers-last law was inverted at §885 and again here — harmless **only by luck** both times (the cure added no test title, checked not assumed). **A law the chair keeps breaking is not a law, it is a hope**; the mechanization is named and OWED — the register pre-flight already knows which registers a consist disturbs and can equally refuse a register act with cures still ahead of it.

⏱ **LIVE STATE (session 58f0a8e2, 2026-09-03 ~09:4x UTC — refreshed at the §887.2 heartbeat checkpoint; ⭐ ALL FIVE LANES OF THE 09-03 WAVE HAVE REPORTED and RUNNING = 0 of 4; ⛔ OWNER CAP: FOUR agents of ANY kind; the owner's standing grant runs to just before the terminal soak and the carve-outs are undissolved).** ⭐⭐ **THE SEAT IS OPUS 5** — every ledger act carries `Seat: Opus 5 — Fable-unvalidated` AND maps `docs/FABLE_RETROVALIDATION_QUEUE.md` with a CHANGED blob into the SAME commit. ⚠ **THE 30-MINUTE HEARTBEAT TEMPLATE IS STALE IN TWO PLACES** — it still names the §882/TAIL-F landing track and asks for a `Seat: Fable 5.1 — validated` trailer. **The seat is OPUS 5**; a Fable trailer on an Opus act is exactly the unmarked-work class §685 exists to prevent, so the template is overridden, not obeyed. ⏳ **NINE CARS ARE SEALED AND WAITING BEHIND ONE LANDING** (`$ME/LANE-QUEUE.md` carries the ref table): enc1-registers `f4cc5cb7d` (5, IN FLIGHT) · enc2-enc6 `f052d1e77` · emdash `4da38b8d3` · emdash-annex `e46d4c3f1` · horizondark-cars12 `43e063a70` · determinism-wip `55d77a8d2`. ⛔⛔ **THE ENC-1 RATCHET REFUSED TWICE, BOTH TIMES ON CONTENTION THE CHAIR CREATED**, naming the same foreign test (`centuryLegSoak.test.js`, 0 ms against a 900,000 ms budget) at load 216 then 276. **The chair read the load AT LAUNCH both times and the lanes ramped up during the ~26-minute run.** ⭐ **A FULL-SUITE INSTRUMENT NEEDS A QUIET WINDOW FOR ITS DURATION, NOT AT ITS START** — the law is now MECHANIZED in `$ME/run-ratchet-quiet.sh`, which waits for three consecutive one-minute readings under 4.0 with zero vitest workers before it starts. ⚠ Its first spelling counted its own grep and could never reach zero; the bracket form `'[v]itest/dist/workers'` is the cure, and **a predicate that can match its own command line is not a gate.** `$ME` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad`.

**Branch `claude/composite-r4` = `f123601850917f549d0d6e34a44b14676b7e6e66` (§887 landed — the TWELFTH landing this arc: §874 · §875 T8 · §876 WAR · §877 HYGIENE · §880 INSTRUMENTS · §881 COUPLED+PAID · §882 TAIL-F · §883 T13 · §884 HORIZON-INSTR · §885 CAPSULE+FILL-LAYER · §886 NEWS · §887 TRAIN 1). Ledger through §887.** Sealed `refs/preserve/landing-887-2026-09-03`. **Registers now: lighting `2505/370/2135/22716/6114` · ratchet `31,016 / 2,453`, 10 entries, 0 removed, OSR `1,993 findings / 1,409 identities / 388 files`** — the known-failure census stays **FULL at 10/10 with ZERO headroom**, so every car from here lands green or does not land.

**THE DOCKS:** `laneENC-tree` **`ea8451bbb`** — **ENC-1 is BUILT and waiting as its own train** (2 cars: the encounter cars plus the chair's `DEFER_CEILING` 31→35 authorization; ⭐⭐ **CORRECTED AT §887.1 — THE BLOCKER NAMED HERE FOR SEVERAL LANDINGS WAS A PHANTOM.** The design's own §12 names **three** rows as BLOCKING the build — **1, 5 and 5b** — and **all three were RULED on 2026-09-02 at ODQ §882.13** under the owner's restated delegation. ⇒ **ENC-2 (the ledger, adapters and the three willed-leash fences), ENC-3 (the stage, flag and fence), ENC-4 (the Herald) and ENC-6 (the fold, which must land BEFORE the lighting wave) are BUILDABLE NOW.** ⛔ Only **ENC-5 (the words)** is genuinely owner-gated — the pool sentences are the pen's at the voice sitting — and **ENC-7 (the bleed wiring)** is DEFERRED to the lighting wave by row 8's own ruling. The single held encounters row is **7, the tuning register's VALUES**, which is the tuning signature and is signed LAST for the whole program, gating no build.). `lanePREGATE-tree` `41a565e8b` — the pre-gate and register pre-flight, **RULED §885.1 to be used FROM THE DOCK**: they read git and need no landing. ⚠ **Copy them to the landing dock to run them and DELETE THEM IMMEDIATELY** — untracked files fail the seal's own porcelain assertion. `laneINSTRLAND-tree` is CONSUMED at `f12360185` and is the landing dock for the next train. ⭐ **THE INSTRUMENTS HAVE NOW EARNED THEIR KEEP ON TWO CONSECUTIVE LANDINGS:** pre-flight ~2 s, pre-gate **81.9 s** for all 14 cheap stages here.

**THE ARC FROM HERE:** **ENC-1** (2 cars, built, its own train) → **desk wiring** (⚠ **NOT in the lighting charter** — 436 lines, zero mentions of the corpus; ⭐ the owner RULED at §885.3 that **the 338 em dashes leave before any desk lights**; it is also where the **68 unmounted blocks** get wired) → **HORIZON-DARK** (⚠ **its car list must be derived from `DESIGN_HORIZON.md` §10/§11 MINUS what §884 landed, and REPORTED before a line is built**) → the **LIGHTING WAVE** (chartered §882.1) → ⛔ the **`createdAt` leak and its five siblings** (`wizardNews.js` `:552 :712 :759 :779 :819 :867`) → **GOLDEN freeze** (⚠ §882.14: GOLDEN-BUILD's R3/R4/R6/R7 MINT the freeze-register schema and NO SEAT HAS READ THEM — walk them BEFORE the freeze act) → **W-ARMS** → ⛔ **the owner's walk + ONE regen — an OWNER act INSIDE the arc, which the chair cannot perform** → the **exhaustive review** (§879.6) → ⛔ **FULL STOP before the terminal soak.** ⛔ **STILL THE OWNER'S, and only these:** ENCOUNTERS row 7 (tuning values) and the seventeen ENC rows · LIGHTING O-12 and O-17 · O-16 (paid-tier visible behaviour) · O-10(b) (a budget-ceiling RAISE) · plus the walk, the soak, the tuning signature and every push.

## (superseded) ⭐⭐⭐⭐ PICKUP AT §886 (2026-09-03 — the ELEVENTH landing is down, gate GREEN ON THE FIRST RUN; TRAIN 1 and ENC built, proven to compose, and waiting; the seat is OPUS 5, every act Fable-unvalidated) — START HERE; supersedes every card below.

⭐⭐ **§886 IS LANDED — and it is the first landing a customer would NOTICE.** `claude/composite-r4` = **`d1a6c773e7d87dd44ef8fcc1ab2edab53c8081fc`**, sealed `refs/preserve/landing-886-2026-09-03`. **5 cars, 11 paths +987/−95**, porcelain 0, 5 trailers, all single-parent. **GATE GREEN ON THE FIRST RUN, 1,138 s, 20 stages**: ratchet **10 known of 30,994, ceiling 10** · `STRICT DIST OK 52/440 zero non-run` · typecheck **173/173** · domain-strict **1121/1121**. **WHAT IT PUTS ON THE BRANCH IN PLAIN WORDS:** no category noun sits where a person belongs (*"Conflict pressure takes hold in Elmspur"* is gone), no raw engine identifier reaches a townsperson's mouth (a century campaign no longer prints *"settlement terminal death"* twenty times while the authored line for the same event reads *"Thornwall is dying"*), and **a walker now makes an unregistered token impossible to ship across all SIX mint routes** — including the eleven sites that resolve only through module constants, whose seven tokens **no walker in this tree could previously see.** ⭐ **BOTH NEW INSTRUMENTS EARNED THEIR KEEP HERE, ON THEIR FIRST REAL LANDING:** the register **pre-flight** named in ~2 s what cost §885 a full 18-minute gate cycle, and the **pre-gate** cleared all 14 cheap stages in **85.6 s** at this exact tip. ⚠ Copy them to a dock to run them and **DELETE THEM IMMEDIATELY** — untracked files on the landing dock fail the seal's own porcelain assertion. ⭐⭐ **THE FINDING THIS LANDING CONFIRMS FOR THE THIRD TIME, NOW WITH THREE FIGURES FOR ONE CONSIST: the census delta is NOT the arm delta.** The new walker carries **19 test arms**; the lighting census moved **+20 titles**; the ratchet moved **+21 tests**. Budgeting a refreeze by counting the arms you wrote is wrong three separate ways. **Both register acts predicted the derivable figures EXACTLY and left the underivable ones deliberately UNNAMED** — files +1 exact, tests unnamed and measured at +21. ⚠ **The ratchet `--update` refused TWICE before this**, both times on a test foreign to the consist that passed standalone, while the machine ran Apple Remote Desktop indexers at load 94 on eight cores. **Held, not retried**, against a resume condition written down as a MEASURED STATE rather than a timer — met and checked at load 1.90 before the run that worked.


⏱ **LIVE STATE (session 58f0a8e2, ~15:4x ET — refreshed at the §883.6 collection; ⭐ NO LANE IS RUNNING — `INSTR-land` reported and HOLDS, re-openable with its full context; a read-only prose-assessment workflow is running for an owner question and touches no dock; the chair owns §6.2's bare gate, §6.3's chair-verify, §6.4's CAS+seal and §6.5's §884 collection; ⛔ OWNER CAP: FOUR running agents of ANY kind; the owner restated full delegation at §882.13 and the chair ruled nineteen rows, holding five by their nature).** ⭐⭐ **THE SEAT IS OPUS 5** — every ledger act carries `Seat: Opus 5 — Fable-unvalidated` AND maps `docs/FABLE_RETROVALIDATION_QUEUE.md` with a CHANGED blob into the SAME commit; ⭐ since §882.14 `chair-commit.sh` ALSO refuses any row that SPEAKS of retrovalidation without moving the queue, whatever the chair's seat (escape: `Enrols: none — <reason>`). The span's strata are the queue's §882.8 … §883.x; a returning Fable chair walks them FIRST. `$ME` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad` (briefs/ · kit-883/ · LANE-QUEUE.md · retro-audit/ · g0-fold/). **TWO CONSISTS HOLD, PRE-STAGED AT C′ AND READY TO BOARD:** INSTR-WRW `355ff603b` (WRWALKER cars 1–3) and INSTR-TUNEREG `f3d33e4b9` (TUNEREG cars 1–3, 9 commits) — both porcelain 0, both ZERO `src/` bytes. ⛔ **THE ONE CONFIRMED BLOCKER BEFORE THAT LANDING (§882.15):** `tests/lint/sovereigntyLightingContract.walker.test.js:655-661` trims the whole `git status --porcelain` output and eats the first line's status space, so its refreeze REFUSES ITSELF whenever `.lighting-census-baseline.json` is the first dirty line — hit and diagnosed by INSTR-TUNEREG after T13-land correctly refused to act on a rumour of it; **cure AT CAUSE in the first lighting-refreezing lane.** ⭐ **CHAIR-VERIFY RUNS IN PYTHON** (§882.15: three shell checks were wrong from the zsh no-split hazard; re-run in Python all three passed). **THE ARC:** HORIZON-INSTR (the two held consists + SOAKCHAIN cars 0–4, not yet built; its Car 0 MEASUREMENT is DONE at §882.12) → HORIZON-DARK → ENCOUNTERS dark (BUILD-READY, its three blocking owner rows RULED at §882.13) → the LIGHTING WAVE (chartered §882.1) → GOLDEN freeze (⚠ §882.14: GOLDEN-BUILD's R3/R4/R6/R7 MINT the freeze-register schema and NO SEAT HAS READ THEM — walk them BEFORE the freeze act) → W-ARMS → walk + ONE regen (owner) → the exhaustive review (⭐ measured CHEAP: a 30-year campaign renders in 61.88 s, not 14–19 min) → ⛔ STOP before the terminal soak. ⛔ **STILL THE OWNER'S, and only these:** ENCOUNTERS row 7 (tuning values) · LIGHTING O-12 and O-17 (each re-opens a SPECIFIC earlier owner ruling) · O-16 (paid-tier visible behaviour) · O-10(b) (a budget-ceiling RAISE) · plus the walk, the soak, the tuning signature and every push.

**Branch `claude/composite-r4` = `d1a6c773e7d87dd44ef8fcc1ab2edab53c8081fc` (§886 landed — the ELEVENTH landing this arc: §874 · §875 T8 · §876 WAR · §877 HYGIENE · §880 INSTRUMENTS · §881 COUPLED+PAID · §882 TAIL-F · §883 T13 · §884 HORIZON-INSTR · §885 CAPSULE+FILL-LAYER · §886 NEWS). Ledger through §886.** Sealed `refs/preserve/landing-886-2026-09-03`. **Registers now: lighting `2504/370/2134/22696/6110` · ratchet `30,994 / 2,452`, 10 entries, 0 removed, census FULL at 10/10 with ZERO headroom** — every car from here lands green or does not land.

⭐⭐ **EVERY LANE HAS RETURNED. SIX DOCKS, ALL porcelain 0, ALL ENROLLED ON BRANCHES:** `laneINSTRLAND-tree` **`108a02ef4`** = TRAIN 2 composed, 4 cars, census already refrozen and PROVEN (2504/370/2134/22696/6110, plain run 34/34) — **the ratchet `--update` is RUNNING on it now** · `laneKERNELMARK-tree` `f60457296` (1 car, the kernel's missing mark channel) · `lanePARSER-tree` `c461dc296` and `laneREGISTRY-tree` `5e749a0b0` — **both branch FROM the kernel car, so TRAIN 1 composes to 3 cars** · `laneENC-tree` `ea8451bbb` (ENC-1 + the chair's DEFER_CEILING authorization, 2 cars, its own train) · `lanePREGATE-tree` `41a565e8b` (the pre-gate + register pre-flight; ⭐ RULED §885.1 — **USE THEM FROM THE DOCK, they read git and need no landing**; they ride TRAIN 1). ⭐ **THE PRE-FLIGHT AND PRE-GATE HAVE BOTH NOW EARNED THEIR KEEP ON A REAL LANDING:** the pre-flight said in ~2 s what cost §885 a full 18-minute gate cycle, and the pre-gate cleared all 14 cheap stages in **94.7 s**.

**THE ARC FROM HERE:** ⏳ **§886 TRAIN 2** (ratchet `--update` → its register commit → pre-gate → bare gate → chair-verify → CAS → seal) → **§887 TRAIN 1** (3 cars, +the two gate instruments; ⚠ it owes an OSR `--write` and a census refreeze, and the REGISTRY lane's rule is right — **re-derive ONCE at the composed tip, never per-dock**) → **ENC-1** (2 cars; ⛔ **ENC-2…7 need the owner's seventeen rows and CANNOT be built until then**) → **desk wiring** (⚠ **NOT in the lighting charter — 436 lines, zero mentions of the corpus**; and ⭐ the owner RULED at §885.3 that **the 338 em dashes leave before any desk lights**) → HORIZON-DARK (⚠ **its car list must be derived from `DESIGN_HORIZON.md` §10/§11 MINUS what §884 landed, and REPORTED before a line is built**) → the LIGHTING WAVE → ⛔ the `createdAt` leak **and its five siblings** → GOLDEN freeze → W-ARMS → ⛔ **the owner's walk + ONE regen — an OWNER act INSIDE the arc, which the chair cannot perform** → the exhaustive review (§879.6) → ⛔ **FULL STOP before the terminal soak.**

## (superseded) PICKUP AT §882 (2026-09-02 — TAIL-F LANDED, the eighth landing this arc; the seat is FABLE 5.1 on the §447 split) — the §882 card above supersedes.

⏱⏱ **LIVE STATE — ⭐⭐ THE SEAT IS NOW OPUS 5 (2026-09-02 ~10:0x ET, §882.8; chair session 58f0a8e2, unchanged — the FABLE MODEL limit is exhausted, not the session).** Every ledger act of this span carries `Seat: Opus 5 — Fable-unvalidated` AND maps `docs/FABLE_RETROVALIDATION_QUEUE.md` with a CHANGED blob into the SAME commit (`chair-commit.sh` enforces all three); the span's stratum is the queue's **§882.8** section and a returning Fable chair walks it FIRST; every artifact folded in this span carries `⟦OPUS-AUTHORED — Fable retrovalidation OWED⟧` at its header. `$ME` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad` (briefs/ · kit-882/ · kit-883/ · LANE-QUEUE.md · g0-fold/ · the two wf scripts). **PRODUCT `claude/composite-r4` = `9a0584f0f`** (TAIL-F landed §882, sealed `landing-TAILF-2026-09-02`). **THREE DEATH CLASSES NOW ON RECORD:** the account switch (§881.16 — run ids die), the SESSION limit (§882.7 — everything dies, Workflows resume by run id), the **MODEL limit (§882.8 — one seat dies, the other keeps running; read the failure TEXT)**. ⏱ **THE T13 BARE GATE IS RUNNING** (launched 12:10 at `7f974e855` — 28 commits over C′, porcelain 0, NO STOP fired, closure 1,046,732 = 268 B under the UNRAISED ceiling so the §880.8 raise stays banked; wrapper `$ME/kit-883/run-gate-t13.sh`, log `$ME/gate-t13.log`; `TRUE_EXIT=0` → `cp $ME/kit-883/collect-883.sh.template $ME/kit-883/collect-883.sh && sh …` from the repo, ⚠ **re-derive its RATCHET_LINE grep first — the totals are now `10 known … of 30788`**). ALL THREE LANES HOLD: T13-land `7f974e855` · INSTR-WRW `355ff603b` (3 cars) · INSTR-TUNEREG `f3d33e4b9` (9 cars), both pre-staged consists porcelain-clean with ZERO src bytes. ⛔ **A CONFIRMED BLOCKER FOR THE INSTR LANDING (§882.15):** `sovereigntyLightingContract.walker.test.js:655-661` trims the whole `git status --porcelain` output and eats the first line's status space, so its refreeze REFUSES ITSELF whenever `.lighting-census-baseline.json` is the first dirty line — hit and diagnosed by INSTR-TUNEREG after T13-land had correctly refused to act on a rumour of it; **cure it AT CAUSE before the HORIZON-INSTR landing.** ⭐ CHAIR-VERIFY RUNS IN PYTHON FROM HERE (three shell checks were wrong; re-run in Python all three pass). (superseded) **⭐ LANE T13-land** (dispatched ~10:25 from the FOLDED order `$SP/T13-LANDING-ORDER.md` — BOARD-READY WITH CHAIR ROWS 5, all five ruled at §882.9; dock `$SP/laneINSTRLAND-tree` at C′; 26–29 commits predicted; ⛔ CR-14: one product-facing golden row MOVED and is attributed + re-recorded ONCE inside T13's signed window, §7 row 1 re-cut — OWNER-VISIBLE on §883; the chair runs the gate, CAS, seal and collection, never the lane). **ALL FOUR OTHER LANES OF THE MORNING ARE COMPLETE:** the G0 fold → `docs/DESIGN_HORIZON.md` COMMITTED amended (§882.11: 728,943 B, 93 folds, 0 refused, 18 chair rows; **§18 THE INSTR CAR LIST AS RE-CUT is what the INSTR briefs are written from**; §19's two owner rows WK-11 and RR-10) · the ENCOUNTERS design COMPLETE (§882.10: `BUILD-READY WITH OWNER ROWS 17`, ⛔ **rows 1, 5 and 5b BLOCK the build**) · SOAKCHAIN Car 0's measurement COMPLETE (§882.12) · the five G0 probes CLOSED (§882.3–§882.6, §882.8). ⭐⭐ **THE ENROLMENT AUDIT (§882.14, owner-ordered):** the MARK is perfect (56/56 acts marked, 9/9 Opus acts carried a changed queue blob) but the ENROLMENT was not — **eight Opus lanes had no queue row**, seven ruled in a ledger row only and **GOLDEN-BUILD ruled nowhere, its seven cars alive on nothing but a detached worktree head**. THREE SEALS TAKEN: `golden-build-2026-09-02` = `ba08939d7` · `t13-consist-2026-09-02` = `9f0df13a1` · `paidprep-rehearsal-2026-09-01` = `f02fd7c4b` (only the live `laneINSTRLAND-tree` is unreferenced, correctly). All eight enrolled at queue §882.14. **`chair-commit.sh` now carries the ENROLMENT GATE** — a ledger row that speaks of retrovalidation must map a CHANGED queue whatever the chair's seat, or say `Enrols: none — <reason>`; negative control executed. ⭐ THE LAW: **an enrolment audit is also a SURVIVAL audit — ask of EVERY dock whether its work is reachable from a ref.** ⚠ GOLDEN-BUILD's four freeze-register schema calls are HIGH and unread by any seat; the L9 freeze is the arc's own gate. ⭐⭐ **THE DESK IS CLEARED (§882.13, owner: "I leave everything to your judgment" — the standing grant RESTATED, not the carve-outs dissolved):** nineteen open rows RULED (all seventeen ENCOUNTERS rows — **5b NO, the three fences stand, a willed compromise never enters the ousting/replacement/verdict chain** — plus HORIZON WK-11 KEEP the closure margin and RR-10 add `errandSpineEnabled`, plus LIGHTING O-5 BUILD / O-11 SIGN / O-14 status quo), and **FIVE HELD by their nature: ENCOUNTERS 7 (tuning values) · LIGHTING O-12 and O-17 (each re-opens a SPECIFIC earlier owner ruling) · O-16 (paid-tier visible behaviour) · O-10(b) (a budget-ceiling RAISE)**. ⭐ The boundary that decided it, and it EXPIRES AT LAUNCH: pre-launch with no installed users, a persisted-SHAPE decision is reversible by a code revert and carries no migration, so persisted shape is CHAIR-CLASS in this window only. **NEXT, in order:** the T13 landing's HOLD → drain → `$ME/kit-883/run-gate-t13.sh` → chair-verify → CAS → seal `landing-T13-2026-09-02` → `collect-883` (⚠ re-derive its RATCHET_LINE grep from the folded order's §3 FIRST — Car 5 retired titles, the totals FALL) → §883; then the INSTR cars from the volume's §18 (they may be pre-staged in their own docks at C′ while the landing runs — instrument-only, zero src bytes).

(superseded at §882.8) ⏱ **LIVE STATE (session 58f0a8e2, Fable 5.1 chair, ~04:24 ET — refreshed at the §882 collection; ⛔ OWNER CAP: FOUR running agents of ANY kind; all decisions delegated (§881.6: run to just before the soak); the usage window may close at any moment).** `$ME` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad` (briefs/ · kit-882/ the live collection kit · LANE-QUEUE.md the live queue · encounters-design-wf.js · lighting-panel/); the resume-map lineage is the CUTOFF sections at the END of `825f209c…/scratchpad/RESUME-BRIEFS-2026-09-01b.md` plus `$ME/LANE-QUEUE.md`. RUNNING: **T13-PRELAND HOLDS 04:33 (§882.2)** — `laneT13-tree` `9f0df13a1`, 16 commits over `8b07ce45f`, p0, 16/16 trailers: the T13 consist COMPLETE (Car 5 = `f95305812` family (i) + retirement, census 1/1 → 0/0, CEILING 48 → 0; `9f0df13a1` the ban in EIGHT blocks with a roster floor); its ONE build at `822c4f93a`: tree closure 1,046,055 · engine 675,339 EXACT · composed-from-C′ 1,046,712 = 288 B under the UNRAISED 1,047,000, the §880.8 raise BANKED unspent; `vendorPdfLazy` 42/42; goldens 0/525 at 1 ULP max; ⚠ +2 `files` on the landing's lighting refreeze (`sovereigntyLightingContract` 2482 vs 2480 on T13's tree); the ratchet `--update` and OSR `--write` (LAST) owed to the LANDING (CR-7) · **LIGHTING: the fold COMPLETE 04:06 — `$SP/LIGHTING-INVENTORY.md` verdict `BUILD-READY WITH CHAIR ROWS 23`; the 23 chair-class rows RULED at §882.1 (22 as recommended, O-9 amended: ONE funnel door shared with ENCOUNTERS); the wave is CHARTERED, its cars cut from §3 when HORIZON-DARK is in its dock, boarding after HORIZON-DARK + T13; 7 owner rows stay on the desk) · **ENCOUNTERS** (Fable Workflow `wf_b18f6db8-789`: E0 resume → E1 `$SP/DESIGN_ENCOUNTERS.md` → two skeptics → fold, one agent at a time) · **T13-ORDER** (Fable Workflow `wf_3b941f97-72f`, `$ME/t13-order-wf.js`: author → composition skeptic → bills skeptic → fold → `$SP/T13-LANDING-ORDER.md`; picks 15–16 filled at the fold from the dock) · **HORIZON G0 wave 1 at C′:** WRWALKER Car 0 **HELD 04:58 with ONE STOP** (kernel leak-through 86.96 % > 50 %; §882.3 — the split `web` → `web-display`/`web-transitive` ADOPTED PROVISIONALLY; **Car 1 is NOT cut until a Fable G0-FOLD lane amends the volume §1.10/§7 in place after CHARSET Car 0 holds**; the reviewable dark cohort is 301, not 880; receipt `$SP/laneG0WRW-receipt.md`, dock retained) · CHARSET Car 0 **HELD ~05:12 clean** (§882.4: no STOP; `S` = 190 by the context-anchored reading + NBSP banned; a `.generated.js` under src reds the OSR provenance digest → commit→write→commit priced into every such car; fourteen manifest importers, `import()` is Car 1's default; refreeze 2499/371/2128 for Cars 1+2; receipt `$SP/laneG0CHARSET-receipt.md`) · COVERAGE Car 0 **HELD ~05:20 clean** (§882.5: tuple 66/329/146/8/175, carto 132/4/193, 8.9 s solo; ELEVEN drafted roster rows wrong at C′ — the register is minted from the corpus, never the draft; receipt `$SP/laneG0COV-receipt.md`) · WORKER Car 0 **HELD 05:30** (§882.6: the extraction shape −6,366 B first paint — Car 1 takes the FULL shape, no headroom spent; engine +8 B accepted, margin 228; the worker bundle ONE chunk; census 529/529; E37 open; receipt `$SP/laneG0WORKER-receipt.md`) · **G0 FOLD** (Fable; RESUMED 08:35 from ⟦G0 PARTIAL: 19 of ~45⟧ after the 05:40 session-limit death — `fold-batch2.py` was written but never applied; the resumer verifies its anchors; folds all FOUR receipts incl. WORKER's §9; appends §17–§19 and cuts the INSTR cars; the chair then commits the volume to `docs/`) · **READERREVIEW Car 0** (Opus; RE-DISPATCHED 08:35 fresh — the first died before writing; dock `$SP/laneG0RR-tree`) · **T13-ORDER** (Workflow `wf_3b941f97-72f` RESUMED 08:4x by run id — author + both skeptics cached; the fold resumes `$SP/T13-LANDING-ORDER.md` at ⟦A7⟧ of 24 carrying §882.7's four rulings: COMP-1 the moved momentum dormancy golden → attribute + re-record ONCE in T13's window; COMP-2 a landing cure; BILL-1 STOP-10 re-cut; BILL-2 explicit tree args) · **ENCOUNTERS** (Workflow `wf_b18f6db8-789` RESUMED 08:40 — the E2 fold restarts `$SP/DESIGN_ENCOUNTERS.md` at 0 of 20 with the shared-funnel-door ruling). FOUR OF FOUR RUNNING; T13-PRELAND HOLDS; SOAKCHAIN Car 0's measurement is next. ⚠ THE SESSION-LIMIT LAW (§882.7): agents die with ZERO notice; every lane checkpoints its file header per batch; a Workflow resumes by run id in the same session; a message sent to a dead agent is LOST — restate rulings in the re-dispatch. NEXT LANDING: **T13 (+ the mint riders)** — boards onto `9a0584f0f` in `laneINSTRLAND-tree` (repurposed at the new tip) from `825f209c…/scratchpad/T13-PREBOARD-HALF1.md` + `4fb807d8…/scratchpad/t13-panel/CHAIR-ROWS-RULING-INPUT.md` (CR-1…CR-12) + the tip-delta half at boarding; picks 1–16 from `laneT13-tree` when Car 5 HOLDS; MF-CH2B re-homed to the lighting wave (CR-3); REC SIGNED §879.11; the ceiling word GIVEN §880.8 and unspent. THEN per the folded HORIZON volume (§881.15) §10: HORIZON-INSTR → HORIZON-DARK → ENCOUNTERS (dark) → the LIGHTING WAVE (owner §881.4) → GOLDEN freeze (L9; `laneGOLDEN-tree` `ba08939d7`) → W-ARMS → walk + ONE regen (owner) → the exhaustive review (§879.6) → ⛔ STOP before the soak. Dispatcher cron `13,43 * * * *` is SESSION-ONLY — re-arm on every resume with the heartbeat prompt (CUTOFF #2's §HEARTBEAT with `$ME` paths, the cap of four and the Fable trailer).

**Branch `claude/composite-r4` = `9a0584f0fdbf74f30bcf910f53fe429fb1dbdc4a` (TAIL-F landed §882 — the eighth landing this arc: §874 substrate · §875 T8 · §876 WAR · §877 HYGIENE · §880 INSTRUMENTS · §881 COUPLED+PAID · §882 TAIL-F). Ledger through §882.** Sealed `refs/preserve/landing-TAILF-2026-09-02`. The retrovalidation queue carries §882.1–§882.2 (TAILFPREP · TAILF-land, both RULED); the §881.16 Opus-chair stratum was walked at §881.19; the historical strata stay QUEUED at the owner's trigger.

**THE IN-FLIGHT TREES (529 law: SURVEY BEFORE RESUMING):** `laneINSTRLAND-tree` = the landed tip `9a0584f0f`, RETAINED as T13's boarding dock · `laneT13-tree` `9f0df13a1` p0 (T13-PRELAND HOLDS — the sixteen-commit consist, complete) · `laneG0WRW-tree` (HELD; three untracked probes; retained for the G0 fold) / `laneG0CHARSET-tree` (HELD; nine untracked scratch artifacts) / `laneG0COV-tree` / `laneG0WORKER-tree` (HELD; its `dist/` is the PROBE build) / `laneG0RR-tree` (G0 probe docks at C′, zero commits) · `laneGOLDEN-tree` `ba08939d7` p0 · `horizon-recon-tree` `60255ca8e` (read-only) · REMOVABLE after this seal (the chair's retirement sweep — listed, not removed): `tailfprep-tree` `66d6e9235` + `tailfprep-base` `893a66b0a` (both sealed by `tailf-rehearsal-2026-09-01`), `825f209c…/scratchpad/base-C` RETIRED 04:3x (rm -rf, after the chair's D-4 re-derivation), `coupledprep-tree` / `paidprep-tree` / `laneGLYPH-tree` / `lanePAIDFIX-tree` / `mintprep-tree` / `base-T`.

**THE ARC FROM HERE (Efficiency Protocol II + III binding; §880.9's pre-board law binding; §881.4's lighting wave binding):** T13 (+ mint riders) NEXT (onto `9a0584f0f`; the first-paint margin at C′ is 338 B — D-2 §881.19; T13's P′ cure leaves the composed tip 288 B under the unraised ceiling) → HORIZON-INSTR (instrument-only cars, zero src bytes) → HORIZON-DARK (src cars dark by construction; golden 0/525 + fences identical at both arms or STOP) → ENCOUNTERS (dark; rides HORIZON-DARK's gate or its own, ruled on measurement) → the LIGHTING WAVE (every dark door lit as a declared shift, the default preset included; budget-raise and build-program lightings on the owner's desk with a recommendation to FUND; NOT chartered until the fold's §9 is ruled) → GOLDEN freeze (records the LIT world; the door signs values afterwards) → W-ARMS (own gate after L9; owner veto drops it) → walk + ONE regen (owner) → **the owner's exhaustive review+fix phase (§879.6)** → ⛔ STOP before the terminal soak. Gates remaining after this one: T13(+mint) · HORIZON-INSTR · HORIZON-DARK (ENCOUNTERS coupled or alone) · LIGHTING · GOLDEN · W-ARMS.

## (superseded) PICKUP AT §881.19 (2026-09-02 — THE FABLE SEAT RETURNED after the Opus span; TAIL-F finishing its battery, T13 building, the lighting fold and the ENCOUNTERS design running; the seat is FABLE 5.1 on the §447 split) — the §882 card above supersedes.

⏱⏱ **LIVE STATE — RESUMED, THE FABLE SEAT RETURNED (2026-09-02 ~04:1x ET, §881.19; chair session 58f0a8e2, Fable 5.1; `$ME` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/58f0a8e2-2c4f-4073-8635-ecc7cf5010f6/scratchpad`).** ⭐ The seat is the §447/§724 split again: the chair's acts carry `Seat: Fable 5.1 — validated`; the §881.16 stratum is WALKED (13 RATIFIED · 1 AMENDED, rulings under each row of the queue). **FOUR LANES RUNNING (= the owner's cap):** **TAILF-land** (Opus; `$SP/laneINSTRLAND-tree` `9a0584f0f`, 12 over `fab576aba`, p0; finishing order §4 (the probe battery from §4.2's can-it-see control) + §6.1 + its retro rows → HOLD; brief `$ME/briefs/brief-TAILFLAND-resume.md`) · **T13-PRELAND** (Opus; `$SP/laneT13-tree` `822c4f93a`, 14 over `8b07ce45f`, p0; the ONE build FIRST — ⛔ its `dist/` was stale — measured against both predictions (tree 1,046,330 ± 250; composed re-based +58 B from C′), then Car 5 with the roster read from `scripts/count-transcendental-math.mjs`; `brief-T13PRELAND-resume.md`) · **LIGHTING-FOLD** (Fable; batch 3 of `$SP/LIGHTING-INVENTORY.md`'s fold, 20→29, then §6–§8 and a §9 CHARTER QUESTION for the two NOT READY verdicts; `brief-LIGHTFOLD-resume.md`) · **ENCOUNTERS** (Fable Workflow `wf_b18f6db8-789`: E0 resumes `$SP/ENCOUNTERS-RECON.md` (3 of 8) by appending → E1 `$SP/DESIGN_ENCOUNTERS.md` → two skeptics → fold, ONE agent at a time; `$ME/encounters-design-wf.js`). Heartbeat `13,43 * * * *` armed (session-only; re-arm on resume with the prompt in `$ME/LANE-QUEUE.md`'s lineage — CUTOFF #2's §HEARTBEAT with `$ME` paths and the Fable trailer). **D-2 RULED ACCEPTED (§881.19):** first-paint margin 338 B under the unraised 1,047,000; T13's composed arithmetic re-bases +58 B. **THE CHAIR'S NEXT ACTS, in order:** TAILF HOLD → drain-check (T13 must not be building) → `cd $SP/laneINSTRLAND-tree && nohup sh $KIT/run-gate-tailf.sh > $ME/gate-tailf.nohup 2>&1 &` (log `fcc53457…/scratchpad/gate-tailf.log`; dispatch NOTHING alongside; OUTLAST it) → order §6.3 chair-verify with own reads → CAS `fab576aba`→C′ → seal `landing-TAILF-2026-09-02` → §882 (⚠ REGENERATE `$KIT/queue-882.md` from the NEW HEAD first, re-append §882.1's TAILFPREP rows and the `__LANEROWS__` slot; `msg-882.txt` carries `Seat: Fable 5.1 — validated`; fill `row-882-draft.md` from the HOLD report per `KIT-882-TODO.md`). Lane queue: `$ME/LANE-QUEUE.md`. A successor on ANY account: survey by git first (the FIRST FIVE COMMANDS below, `$SP` unchanged), read the four resume points (`$SP/laneTAILFLAND-receipt.md`, `$SP/laneT13-receipt.md` — last RESUME POINT; `$SP/LIGHTING-INVENTORY.md`, `$SP/ENCOUNTERS-RECON.md` / `$SP/DESIGN_ENCOUNTERS.md` — headers), re-arm the heartbeat, re-dispatch dead lanes from `$ME/briefs/` + the resume point (Opus for the two build lanes, Fable for the two design lanes), never discarding partial edits.

(superseded at §881.19 — kept for its figures) ⏱⏱ **LIVE STATE — PAUSED FOR AN ACCOUNT SWITCH (2026-09-02 ~03:30 ET, §881.16; chair session 4fb807d8). ⭐⭐ THE SEAT IS NOW OPUS 5: every ledger act carries `Seat: Opus 5 — Fable-unvalidated` AND maps `docs/FABLE_RETROVALIDATION_QUEUE.md` (changed blob) into the SAME commit — its `## §881.16` section is this span's stratum; a returning Fable chair walks it first.** ⭐⭐ READ FIRST: the **CUTOFF #4** section at the END of `825f209c…/scratchpad/RESUME-BRIEFS-2026-09-01b.md` — it carries every lane's exact state and the successor's first acts in order. ALL FOUR LANES ARE PAUSED. ⭐ **TAIL-F's PICKS AND REGISTERS ARE COMPLETE ON ITS DOCK** (`825f209c…/scratchpad/laneINSTRLAND-tree` = `9a0584f0f`, 12 commits over `fab576aba`, porcelain 0; both registers landed ON PREDICTION — lighting `2495/370/2125/22437/6069`, ratchet `30731/2443`): ⛔ **§881.17: the lane still OWES order §4 (the whole probe battery, starting at §4.2's can-it-see control) and §6.1's close-out — re-dispatch an Opus lane to finish them FIRST; and its tip build read closure 1,046,662 (+58 B vs prediction, cause measured: Rollup split `faithField.js` lazily and `index`'s preload map grew — no ceiling breached, 338 B margin, engine Δ 0) which is a chair row before the CAS and re-bases T13's arithmetic.** Only then: drain → the bare gate (`fcc53457…/scratchpad/kit-882/run-gate-tailf.sh`) → chair-verify → CAS → seal `landing-TAILF-2026-09-02` → the §882 collection (⚠ REGENERATE the kit's `queue-882.md` from the new HEAD and rewrite its `msg-882.txt` to the Opus trailer first). ⭐ **T13-PRELAND paused CLEAN: `laneT13-tree` = `822c4f93a`, 14 commits, porcelain 0 — pick 14 (the P′ placement cure) is COMMITTED and behaviourally PROVEN** (15,071,406 comparisons + 2,449,925 differential cases, zero mismatches; goldens 0/525; both eager `detMath` importers now ZERO). OWED: Car 5 (picks 15–16, not started) and **the ONE build, UNSPENT** — predicted closure 1,046,330 ± 250. ⛔⛔ **TRAP: `laneT13-tree/dist/` is STALE (build #6b at `594c7521f`, 00:35) — the closure probe against it prints 1,047,496 and looks real. BUILD FIRST.** (§881.18) ⛔ **THE LIGHTING WAVE IS NOT CHARTERED**: both skeptics returned **NOT READY** (11 + 14 findings) and the fold stopped at **20 of 29** — resume the fold before any wave car. **ENCOUNTERS** (owner-ordered §881.13, plan `docs/ENCOUNTERS_PROGRAM.md`): E0 recon PARTIAL 3 of 8 sections, E1 not started. HOLDING: GOLDEN-BUILD `laneGOLDEN-tree` `ba08939d7`. The heartbeat cron and both workflows died with the session — re-arm and relaunch per CUTOFF #4. THE ARC: §882 TAIL-F → T13 (+ mint riders) → HORIZON-INSTR → HORIZON-DARK → the LIGHTING WAVE (§881.4) → GOLDEN freeze → W-ARMS → walk + ONE regen (owner) → the exhaustive review (§879.6) → ⛔ STOP before the terminal soak.

**Branch `claude/composite-r4` = `fab576aba5b6030009f0c9794ba59ad83c9b1b53` (COUPLED + PAID landed §881 — the seventh landing this arc: §874 substrate · §875 T8 · §876 WAR · §877 HYGIENE · §880 INSTRUMENTS · §881 COUPLED+PAID). Ledger through §881.** Sealed `refs/preserve/landing-COUPLED-2026-09-01`. The retrovalidation queue carries §881.1–§881.2 (COUPLEDBISECT · COUPLED-land); the historical strata stay QUEUED at the owner's trigger.

**THE IN-FLIGHT TREES (529 law: SURVEY BEFORE RESUMING):** `laneINSTRLAND-tree` = the landed tip, RETAINED as TAIL-F's landing dock (`fab576aba`, porcelain 0) · `tailfprep-tree` `66d6e9235` p0 (the TAIL-F rehearsal, sealed) · `laneT13-tree` (T13-BUILD, live — read its receipt's last RESUME POINT) · `laneGOLDEN-tree` `ba08939d7` p0 · `horizon-recon-tree` `60255ca8e` (read-only, the workflow's) · `coupledprep-tree` `893a66b0a` / `paidprep-tree` `f02fd7c4b` / `tailfprep-base` (rehearsal trees — removable after TAIL-F lands) · `laneGLYPH-tree` / `lanePAIDFIX-tree` (landed; removable) · `mintprep-tree` (removable) · `base-T` (the §881 probe base arm; removable after TAIL-F's pre-board decides its base arm).

**THE ARC FROM HERE (Efficiency Protocol II + III binding; §880.9's pre-board law binding):** TAIL-F alone (NEXT — from the sealed rehearsal onto `fab576aba`) → T13 (REC SIGNED §879.11; the ceiling word GIVEN §880.8; wiring in flight; the mint window's residue acts RIDE its gate per E2) → GOLDEN freeze (machinery BUILT; the freeze act at L9 on the post-T13 tree; every paid-surface byte change is now BEFORE it) → W-ARMS (own gate AFTER L9, 0 B first paint; owner veto drops it) → walk + ONE regen (owner) → **the owner's exhaustive review+fix phase (§879.6; the reader protocol of `DESIGN_HORIZON.md`)** → ⛔ STOP before the terminal soak. The HORIZON volume's other mechanisms are chartered on the volume's landing and slotted by its §10. Gates remaining after this one: TAIL-F · T13(+mint) · GOLDEN · W-ARMS.

## (superseded) PICKUP AT §880 (2026-09-01 — THE INSTRUMENTS CONSIST LANDED, the sixth landing this arc; the seat is FABLE 5.1 on the §447 split) — the §881 card above supersedes.

⏱ **LIVE STATE (session fcc53457, Fable 5.1 chair, 2026-09-02 ~00:25 ET — refreshed at CUTOFF #3 preparation, §880.12; the owner delegates every decision and expects to run out of window).** Background lanes, monitors and the heartbeat DIE with the session; detached runs do NOT. ⭐⭐ **READ FIRST: the CUTOFF #3 section at the END of `825f209c…/scratchpad/RESUME-BRIEFS-2026-09-01b.md`** (mirrored in `ab42fbe4…`, `25b6c8bb…`, `fcc53457…`) — it carries every in-flight state and the successor's first acts in order. IN FLIGHT: **the §881 bare gate** (`run-gate-coupled.sh` pid 75255 at `fab576aba`, launched 00:02, log `ab42fbe4…/scratchpad/gate-coupled.log`; `TRUE_EXIT=0` → `sh $CH/collect-881.sh` from the repo, which CASes `claude/composite-r4` 60255ca8e → fab576aba, seals `landing-COUPLED-2026-09-01` and commits §881) · **T13-BUILD #3** (Opus; `laneT13-tree` `da8cd72fb` + Car 3 applied UNCOMMITTED, porcelain 13 — never discard; resumes Car 3's proofs after the gate; brief `brief-T13-BUILD-resume3.md`) · **the HORIZON five-lens panel** (Fable; `DESIGN_HORIZON.md` COMPLETE by the integrator; findings to `fcc53457…/scratchpad/horizon-panel/<lens>.json`; then the fold, then the chair commits the volume to `docs/`) · **the TAIL-F order author** (Fable; `TAILF-LANDING-ORDER.md`; then two skeptics + fold; then lane TAILF-land). DONE: COUPLED-land HELD GREEN (plain gate #2 OK, tuple + register unmoved). HOLDING: **GOLDEN-BUILD** (`laneGOLDEN-tree` `ba08939d7`; freeze act at L9). ⛔ OWNER DIRECTIVES (§879.6, reaffirmed §880.12): all decisions delegated; finish building and landing everything, THEN the exhaustive review+fix before the soak. Dispatcher cron `13,43 * * * *` is SESSION-ONLY — RE-ARM on resume with the §HEARTBEAT prompt.

**Branch `claude/composite-r4` = `60255ca8ef4af0b802f73817f06d9463fa89ac06` (INSTRUMENTS landed §880 — the sixth landing this arc: §874 substrate · §875 T8 · §876 WAR · §877 HYGIENE · §880 INSTRUMENTS). Ledger through §880.** Sealed `refs/preserve/landing-INSTR-2026-09-01`. The retrovalidation queue carries §880.1–§880.5 (INSTRLAND · GLYPH · PAIDFIX · COUPLEDREH · SEAMCYCLE); the historical strata stay QUEUED at the owner's trigger.

**THE IN-FLIGHT TREES (529 law: SURVEY BEFORE RESUMING):** `laneINSTRLAND-tree` = the landed tip, RETAINED and REPURPOSED as the coupled landing's base dock (`60255ca8e`, porcelain 0) · `coupledprep-tree` `b82987ab8` (17 picks + 10 `REHEARSAL-CURE` commits on `8b07ce45f`; COUPLEDBISECT's cures land here next) · `paidprep-tree` `f02fd7c4b` (the PAID rehearsal, 19 picks; now 20 with rider 16 — dist removed) · `laneT13-tree` `5c2cd6f98` p0 · `laneGOLDEN-tree` `ba08939d7` p0 (+ `laneGOLDEN-ctrl` removable) · `laneGLYPH-tree` `1e9898d01` p0 · `lanePAIDFIX-tree` `6fe6eaa58` (two deliberate untracked probe artifacts) · `bisect-A` (COUPLEDBISECT's throwaway, removal owed) · `mintprep-tree` `8b07ce45f` (removable). Every other dock is a landed lane's and awaits the retirement sweep.

**THE ARC FROM HERE (Efficiency Protocol II + III binding — couple on measurement · lanes DRAIN before a gate · pre-stage off the critical path · measure every census bill):** the COUPLED consist = FOLLOW-ONS + TAIL-O + TAIL-L + **PAID as slot 7** (NEXT — boards on `60255ca8e` from `COUPLED-BOARDING-CHECKLIST.md` + `COUPLED-REHEARSAL-RECEIPT.md` (incl. `## SEAMCYCLE` and `## COUPLEDBISECT`) + `PAID-BOARDING-CHECKLIST.md`; rehearsal tips `b82987ab8` + `f02fd7c4b`; R5 is a hand-copy red git avoids; Q3 option A staged 6→8; D4 and L7 pick bodies owe resolution paragraphs; lighting target composed from the probe; both npm typecheck scripts run at the composed tip) → TAIL-F alone (the fence union, `SOURCES.length` = 5) → T13 (REC SIGNED §879.11, veto window OPEN until Car 4's gate; the mint window's residue acts RIDE its gate per E2; the kernel byte-diet then build #4; a surviving closure residual is the owner's re-ask) → GOLDEN freeze (machinery BUILT; the freeze act at L9 on the post-T13 tree; every paid-surface byte change BEFORE it) → W-ARMS (own gate AFTER L9 — `DESIGN_W_ARMS.md` proves the dossier view model does not carry it; 0 B first paint; owner veto drops it) → walk + ONE regen (owner) → **the owner's exhaustive review+fix phase (§879.6)** → ⛔ STOP before the terminal soak. Owner's standing order (2026-09-01, re-issued at the resume): finish building and landing everything, THEN the review; 30-minute dispatcher, up to four lanes. Gates remaining after this one: COUPLED+PAID · TAIL-F · T13(+mint) · W-ARMS · GOLDEN.

## (superseded) PICKUP AT §879 (2026-09-01 — THE RETROVALIDATION IS PAID; the seat is FABLE 5.1 on the §447 split; the arc RESUMES) — the §880 card above supersedes.

⏱ **LIVE STATE (session 25b6c8bb, Fable 5.1 chair, 2026-09-01 ~16:20 ET — refreshed for the owner's likely usage cutoff).** Background lanes DIE with the session. ⭐ **`25b6c8bb…/scratchpad/RESUME-BRIEFS-2026-09-01b.md` (mirrored at `825f209c…/scratchpad/`) carries every running lane's brief and the chair's own sequence** — re-dispatch from it anything found unwritten. RUNNING (4/4): **INSTRLAND** (Opus; dock `laneINSTRLAND-tree` at `bf902c59f`; re-dispatched 17:30 after the interrupt kill (§879.13); at `cf83a400c` acts 1 (bill A REPLACE `8121dc797`), 2 (four ceilings banked), 4 (dormancy SET PASS), 5 (plants convict) DONE; act 3 the `--update` re-freeze RUNNING under the exclusive lock (predicted 30196/2429); then plain gate → act 6 bills (measured tuple) → HOLD; §879.15) · **COUPLEDBISECT** (Opus, on `coupledprep-tree` `b82987ab8`: attribute the dormant-corpus sha move `26f8bfe8…`→`87e8b25c…` by bisection; cure the seven typecheck-ratchet reds at cause as JSDoc-only cures; §879.16) — SEAMCYCLE ⭐ DONE (`b82987ab8`, cycle cured at zero bundle bytes, both variants measured; lighting target now 2490/372/2118/22222/6020) · **GLYPH** ⭐ COMPLETE + SEALED `refs/preserve/glyph-complete-2026-09-01` = `1e9898d01` (§879.10; measured tuple 2481/371/2110/21884/5966; HOLDS for a pre-GOLDEN landing, coupling target ruled on measurement §879.8-E1) — COUPLEDREH ⭐ DONE (rehearsal tip `653670ca1`, 8 `REHEARSAL-CURE` commits, tuple 2489/372/2117/22214/6019 EXACT, ratchet +384/+8; one STOP → SEAMCYCLE; §879.13) · **T13-BUILD** (Opus, EARLY BUILD under the signed REC §879.11 — dock `laneT13-tree` at `779592644`: §9 checks done; Car 0 ADOPTS (c); Car 1 kernel `3b56c8e32` at ~2e-16 (the REC's 1e-14 was pessimistic); Car 2a done; ⛔ closure red predicted at wiring (kernel 2,086 B vs 995 B margin) → RULED §879.15: lazy-route the kernel beside its consumers (FP-G14 idiom), one probe build authorized, no budget raise; then Cars 3/4 drafts → HOLD; receipt `laneT13-receipt.md`) — MINTPREP-E2 ⭐ DONE (`MINT-E2-MEASUREMENT.md`: the mint window needs NO gate, six → FIVE; rulings §879.11) · **PAIDFIX** ⭐ COMPLETE + SEALED `refs/preserve/paidfix-complete-2026-09-01` = `32ac4980a` (§879.12; nine repairs; repair 5 STOPPED → owner's desk; riders 13–16 COMPLETE + SEALED `refs/preserve/paidfix-rider16-2026-09-01` = `6fe6eaa58` (PAID consist = GLYPH 5 + PAIDFIX 15 commits), tuple 2480/371/2109/21921/5983; repair-5 answer on the desk: RETIRE + the success-toast wording repair, owner's veto) — PAIDPREP-E1 ⭐ DONE (`PAID-BOARDING-CHECKLIST.md`; VERDICT RIDE-COUPLED as slot 7, §879.14; PAID rehearsal tip `f02fd7c4b`; engine Δ 0 B, closure +40 B, golden 0/525) · PAIDFIX rider 16 (the nine un-anchored negatives) in flight on its dock — COUPLEDPREP ⭐ DONE (`COUPLED-BOARDING-CHECKLIST.md`, rehearsal tip `e9ee11a46`; R-Q3 option A + R-2a ruled §879.10). ⛔ OWNER DIRECTIVES THIS SESSION (§879.6): the code assessment is STOPPED — "finish building and landing everything then we do a review"; **the exhaustive review+fix is a chartered phase after the last landing and BEFORE the soak**. When INSTRLAND HOLDS: send "SUSPEND FOR GATE" to GLYPH and PAIDFIX (M2 drain) → bare gate → CAS/seal/§880. Dispatcher cron `13,43 * * * *` is SESSION-ONLY — RE-ARM on resume. ⭐ EFFICIENCY PROTOCOL III (§879.8, vetoable): E1 couple the PAID consist into an existing gate on measurement · E2 couple the mint acts into T13's gate on measurement · E3 composition-class rehearsal (`tests/lint` + walkers) on the COUPLEDPREP rehearsal tip during INSTR's endgame · E4 census bills MEASURED by the standing probe, never predicted · E5 fold the superseded cards + a FIRST FIVE COMMANDS block at §880.

**THE SEAT:** Fable 5.1 chairs/architects/validates; Opus 5 implements/verifies (§447's split). Trailers accepted by `chair-commit.sh`: `Seat: Fable 5 — validated` · `Seat: Fable 5.1 — validated` · `Seat: Opus 5 — Fable-unvalidated`. **THE RETROVALIDATION (§879) IS PAID for the inverted span** — 64 items, 45 RATIFIED · 17 AMENDED · 2 REVERSED · 0 EVIDENCE-THIN; every §877 row carries a `*RULED (§879)*` line and the twelve lanes are enrolled and ruled as §879.1–§879.12 in `docs/FABLE_RETROVALIDATION_QUEUE.md`. ⛔ The historical strata (§685's dark window, §687–§723) stay QUEUED at the owner's trigger — the queue is NOT empty.

**Branch `claude/composite-r4` = `8b07ce45f179b583c6a152412a23777cb1a15a38` (HYGIENE landed §877). Ledger through §879.4.**

**⛔ WHAT THE PASS CHANGED ABOUT THE ARC (binding):** **Rider R5** on the coupled FOLLOW-ONS+TAIL-O+TAIL-L consist — compose slot 4's STEP-1 filter over the BASE's `consumesKnownRead`, never L7's literal regex (verbatim = RED, no cure) · **TAIL-F checklist +1** — compose `faithFieldDormancyFence.test.js` by union in both conflict regions and re-pin `SOURCES.length` to FIVE · **picked-car trailer convention**: a pick keeps its AUTHOR's `Seat:` only · **INSTRLAND's three resume riders** in `825f209c…/scratchpad/INSTRLAND-RESUME-NOTE.md` (bill A REPLACES, HZ-SERVICECAT 1/3, the dormancy anchor over the SET) · the `✅ EP-g2 RESOLVED-EXECUTED` row is landed (§879.3).

**THE IN-FLIGHT TREES (529 law: SURVEY BEFORE RESUMING):** `laneINSTRLAND-tree` `eb150f31b` p0 (22 commits; resumes → ratchet `--update` · bill A · dormancy proof · plants · HOLD) · `lanePAIDFIX-tree` `0dac7b074` (repair 1 COMMITTED + RATIFIED; repair 2 PARKED to the owner — jsPDF mojibake, a design choice; repairs 3–5 + the new repair 6 `DestroySettlementControl.jsx:31` resume; ⚠ its new `src/domain/resolveCulture.js` owes FOUR censuses at its landing) · `laneGLYPH-tree` `f09ce3ff7` (Car B COMMITTED + RATIFIED, paid bytes −1,873/−1,751/−806, owner's veto stands, lands BEFORE the GOLDEN freeze; Car A staged — must land GREEN behind B or STOP; then Car C).

**⛔⛔ OWNER ROWS ON THE DESK (`825f209c…/scratchpad/TUNING-DESK.md`):** the PDF prints the WRONG LETTER (dossier) · the non-Latin-name repair is a DESIGN choice (campaign PDF: embed a font · transliterate · keep-and-mark; chair recommends embed) · `authSlice.js:489` under-grants a tier · four architecture volumes live on ANOTHER LINE (import or annotate — IP class) · `seat_agent` is an unsigned register candidate · the lighting-day cost (+1,047 B engine; margin 266 B).

**THE ARC FROM HERE (Efficiency Protocol II binding — couple on measurement · lanes DRAIN before a gate · pre-stage off the critical path):** finish INSTRUMENTS → gate → CAS/seal/§880 → the COUPLED consist = FOLLOW-ONS + TAIL-O + TAIL-L + **PAID as slot 7** (§879.14; boards from `COUPLED-BOARDING-CHECKLIST.md` + `COUPLED-REHEARSAL-RECEIPT.md` + `PAID-BOARDING-CHECKLIST.md`; rehearsal tips `653670ca1` + `f02fd7c4b`; R5; the SEAMCYCLE cure) → TAIL-F alone (with the fence union) → T13 (REC SIGNED §879.11, veto window OPEN until Car 4's gate; Cars 0–3 building EARLY in `laneT13-tree`; the mint window's residue acts RIDE T13's gate per E2/§879.11 — R1 three.js product half · R4 HK-5 car · R5a/b comment cures · R8 the OSR act LAST; FIVE gates remain) → W-ARMS (own gate after T13, before L9 iff its PDF seam moves `goldenViewModel`; owner veto drops it) → GOLDEN freeze (every paid-surface byte change BEFORE it) → walk + ONE regen (owner) → **the owner's exhaustive review+fix phase (§879.6)** → ⛔ STOP before the terminal soak. Owner's standing order (2026-09-01, re-issued at the resume): finish building and landing everything, THEN the review; 30-minute dispatcher, up to four lanes.

## ⏩ FIRST FIVE COMMANDS — a successor on ANY account or model reaches its first act in ≤5 minutes (E5, §879.9)

1. **SURVEY (529 law) before trusting anything:** `SP=/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/825f209c-0e84-4a1e-b6f0-79a46de834dc/scratchpad; for d in laneINSTRLAND-tree laneGLYPH-tree lanePAIDFIX-tree coupledprep-tree; do echo "$d $(git -C $SP/$d rev-parse --short HEAD 2>/dev/null) p$(git -C $SP/$d status --porcelain 2>/dev/null | wc -l | tr -d ' ')"; done; git rev-parse --short claude/composite-r4` — a killed lane leaves partial edits; never discard, never assume.
2. **READ** the ⏱ LIVE STATE block below, then the resume briefs it names, then each running lane's receipt tail (`$SP/lane*-receipt.md`, last `RESUME POINT`).
3. **RE-ARM the dispatcher** (session-only, dies with every session): CronCreate `13,43 * * * *` with the heartbeat prompt in the resume briefs.
4. **RESUME each lane FROM ITS RECEIPT** (re-run any measurement taken before the death, never reuse it): Opus for build/verify lanes, Fable for architecture/validation/recon; every lane brief carries its seat trailer + the SUSPEND-FOR-GATE protocol + the E4 measured-tuple line.
5. **COLLECT after every act** with the private-index committer (three trailers · subject-anchor gate · queue-blob gate): `SP=<your scratchpad> sh <your scratchpad>/chair-tools/chair-commit.sh <ledger-tip> <msgfile> docs/HANDOFF_CURRENT.md:docs/HANDOFF_CURRENT.md` — toolkit copies: `25b6c8bb…/scratchpad/chair-tools/` (current, Fable 5.1 trailer) and `refs/preserve/chair-tools-2026-08-26-r7`. ⚠ In this tree `git diff HEAD -- <path>` LIES (the shared index carries staged deletions) — verify a delta with `git show HEAD:<path> > tmp; git diff --no-index --numstat tmp <path>`.

**Superseded pickup cards (§878 → §724, the §685 seat block, the 08-24/08-23/08-22/08-20 overrides, the historical topology and pause state) are FOLDED VERBATIM into `docs/archive/HANDOFF_CARDS_2026-09-01.md` (E5, §879.9) — nothing deleted, every § reference preserved; read it when a card below cites a figure this file no longer carries.**

## STANDING SECTIONS THAT SURVIVE EVERY CARD

## What this is
SettlementForge: a deterministic D&D settlement/world simulator (~1.3M lines).
The owner (Clausell Stokes) has delegated ALL judgment to the AI chair under
ODQ §170/§217/§223, with carve-outs BY NATURE: legal, data deletion, the tuning
signature, every git push, paid-surface changes, and anything owner-parked.
CONSTITUTIONAL: THE PROMISE (a seed is a starting world forever; lived history
immutable; tuning owner-signed) · the DEITY DOCTRINE (faith is cultural, never
theological; no premade deity pool) · FINITE-SEMANTICS (typed buckets; AI is
clerk, never writer) · the core world/narrative model may be setting-agnostic,
but current settlement-map morphology is explicitly `EUROPEAN_FANTASY_BASE`
and culture tokens select no geometry · sub-century · never a named character's
fate.

## THE SEAT (pointer — full §685 text in the archive)

Owner order §685 (2026-08-25/26) minted the marking law: every ledger act carries exactly one seat trailer, and an Opus act Fable did not architect/validate/manage lands its row in `docs/FABLE_RETROVALIDATION_QUEUE.md` in the same commit (`chair-commit.sh` refuses otherwise). Lineage: §685 all-Opus → §724 re-split (Fable architects/manages, Opus implements/verifies) → §876.5 inversion for one span → §878 return to the split → §879 the Fable seat moves to Fable 5.1. Trailers accepted: `Seat: Fable 5 — validated` · `Seat: Fable 5.1 — validated` · `Seat: Opus 5 — Fable-unvalidated`. Unmarked is retrovalidation-OWED; the mark is provenance, never a quality gate. Memory: `opus-seat-assignment-and-the-retrovalidation-mark.md`.

## ⚠ FABLE/OPUS CONTINUITY (§236)
If you are an OPUS chair: work continues at full authority, but mark every
ledger row "(chair: Opus 5 — Fable-unvalidated)" and append what needs Fable
re-examination to docs/FABLE_RETROVALIDATION_QUEUE.md. If you are a FABLE
chair returning: walk that queue FIRST (RATIFIED/AMENDED/REVERSED per row),
then resume the §234 sequence.

## ⛔ PERMANENT IP LAWS (§248, §253, §254.5)
- **NO WATABOU (TownGeneratorOS) CODE EVER ENTERS THIS TREE** — GPL-3.0,
  incompatible with a commercial closed product. Study is read-only; the
  implementing lane must never see that source (clean-room by lane separation).
- **FMG (Azgaar) is MIT but its vendored libs are not** — tinymce is GPLv2+,
  and its Urquhart utility was copied in from elsewhere and is not Azgaar's to
  license. A permissive top-level licence does not launder copied-in code.
- Adopt APPROACHES freely; implement clean-room in our own idiom. A mechanism
  adopted without a derivation home in our dossier facts is decoration.

## Hazards a successor must not relearn (the sharpest five)
1. A census over a derived set proves NOTHING about a surface it doesn't
   contain — forensic zoom (render 3000px, crop, LOOK) is standard practice.
2. Trust no exit you did not capture in-shell; only SELF-NAMED logs; a shared
   log dir lies by recency; never wrap check* in gate-mutex (self-deadlock).
3. Comment/prose edits fire ratchets; quoting a forbidden matcher in prose
   convicts; line-bound exclusions drift toward silent UN-exclusion.
4. PACKET_MANIFEST is never-re-serialize (scoped text edits only);
   requiredSymbols pin symbols/anchors, never lines or figures — and they do
   NOT catch renames (substring match).
5. The test census sits AT its ceiling: extend existing test files, never mint
   new ones; a parked file swallows its titles.

Full hazard corpus: the Claude memory dir (~/.claude/projects/
-Users-cstokes-Desktop-settlement-engine/memory/ — MEMORY.md is the index) for
Claude successors; for non-Claude successors this file + the ledger + the lane
receipts are sufficient and the memory dir is a bonus if readable.

## ARCHIVE POINTER
The cards this file used to carry below this line live, verbatim, in `docs/archive/HANDOFF_CARDS_2026-09-01.md` (folded 2026-09-01, §879.9).
