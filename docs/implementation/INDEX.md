# Implementation packet index

- **Status:** CANONICAL
- **Current code of record (first-map continuation):**
  `/Users/cstokes/.codex/worktrees/first-map-vertical-slice` on
  `codex/first-map-vertical-slice`; MF-T1X implementation landed at full commit
  `f4ad467d889ce5467a96604f28f2c49aaafcef57`. The exact registered fantasy
  construction, deterministic receipt replay and PUBLIC/DM privacy tranche is
  complete.
- **Current measured checkpoint (2026-08-21):** `133` packets / `0` READY.
  The bounded first-map sequence is complete: sealed fabric/frontage, explicit
  varied massing, shared fixed-survey projection/draw/SVG, built-in/custom parity,
  save plus visible read-only missing-package reload, and one registered fantasy
  construction with deterministic PUBLIC/DM replay all have landed.
- **Continuation authority (ODQ §291.5 / §299.5):** dispatch authority is the
  chair's, exercised through this packet system — do not redispatch MF-T1X or
  infer a successor from the log; any continuation derives and promotes a new
  finite packet. Deferred scope recorded by the lane: dossier-conditioned
  sampling waits for genuine dossier/seed/candidate/choice-receipt authority;
  UI/store integration, operation chaining and adjacent edge cases sit outside
  this completed slice. (A lane-authored "handoff law" formerly stood here;
  struck per ODQ §299.5 — lanes do not author governance.)
- **Minifold integration record (retained below):** provenance of the build
  program; its standing relative to the first-map continuation is ruled at
  ODQ §299/§301, not relabelled by a lane.
- **Code of record:** `/Users/cstokes/Desktop/settlement-engine/.claude/worktrees/minifold`
- **Measured branch/SHA:** `claude/composite-r4` at the `cs-b` train terminal.
- **Measured:** 2026-08-15 — **THE `cs-b` TRAIN LANDED ALL FOUR MEMBERS: THE SECOND HALF OF THE
  DECLARED-SHIFT CORRECTNESS BATCH, AND THE BATCH IS COMPLETE.** Chain: `6d19d5e7` (CS-B0's
  terminal, lane TE20) → `25b114d8` (P2) → `0242a9c0` (CS-B1) → `ac2d32ac` (CS-B2) →
  `d4aa8f25` (CS-B3) → the flip → the declared-shift re-record → the terminal.
  `validate:packets` 71/0 → 74/3 → 74/0.
  ⭐⭐ **ALL THREE REMAINING MEMBERS PROMOTED TOGETHER, WHICH `cs-a` COULD NOT DO.** Their change
  paths are disjoint, so the §44 staged promotion cs-a needed inside its own train is not
  required here — proven by the validator rather than assumed.
  ⭐⭐ **EACH CURE IS EXACTLY LINE-NEUTRAL, INCLUDING AGAINST THE BATCH'S TIGHTEST CEILING.**
  `upswingKernel` 640 → 640, `demographicsKernel` 191 → 191, and `convergence` **798 → 798**
  against 800 — the two lines §107.3 named are still unspent.
  ⛔ **CS-B1: A PUNISHMENT MINTED FROM NOTHING.** `severed` was computed every tick for every
  settlement and consulted at exactly ONE site, inside the branch that drives the bust; the MINT
  gate never asked whether the town was under siege. An embattled town minted a boom at tick 2 and
  busted at tick 3 — Comfortable → Moderate, 55 → 52, plus a Trade Bust condition, for a boom it
  never had. §107.3 signed the MINT gate and NOT the dwell accumulation, so the siege DELAYS the
  boom rather than killing it, and the pin's second arm is what holds that reading: the boom mints
  on the lift tick itself, off banked dwell.
  ⛔ **CS-B2: 598 SOULS OUT OF NOTHING, IN ONE TICK, AND THEN THE EVIDENCE HIDES.** The named floor
  could only ever bind where binding is wrong, and there it invented people: 2000 → 2600 on two
  births, a history row recording delta 600 under the reason "2 born and 0 buried", and a ten-tick
  closure residual of 598 against 0 cured. Ticks 2 through 10 look perfectly healthy, which is why
  only the ACCUMULATED closure convicts it.
  ⛔ **CS-B3: A SWORN ALLY READ EXACTLY LIKE A STRANGER.** `FRIENDLY_REL` admitted `ally`, which
  no canonicalizer can produce, and missed `allied`, which both produce — so the base allied run
  is byte-for-byte the NEUTRAL run and the interventions ledger stays empty at every tick. The
  paired habitat obligation moved in the same commit, retiring an instrument RN-C had deliberately
  kept, and the non-vacuity population was re-derived (5 → 4) rather than inherited.
  ⭐⭐ **EACH PIN IS A TRAJECTORY PIN BECAUSE EACH DEFECT DEFEATS A PURE-FUNCTION ONE**, which is
  the charter §72.3 wrote for this batch. CS-B1's base and cure agree exactly at ticks 0, 1 and 2;
  CS-B2's mint is a single tick that then hides; CS-B3's set membership can be asserted true while
  the consequence stays broken. **Six mutants planted, every restore verified by `cmp` exit 0**,
  and each pin has one that reds it alone.
  ⚠ **A COMPILE FIXTURE WAS REFUTED BY MEASUREMENT (J-TE21-1).** The plan's "village with
  population 10 and 50 named residents" has an expected birth count of 0.0088, so births are 0
  every tick, the still-week guard skips the block, and base and cure produce IDENTICAL runs. The
  fixture is scaled until the defect actually executes.
  ⚠ **AND A CARRIED FIGURE WAS CORRECTED:** the EXPECTED-DEAD population is **5 → 4** declaration
  rows, not the "10" lane TE20's receipt carried — that was a grep-LINE count over prose.
  Census `2438/364/2074/20220/5676` → `2438/364/2074/20226/5678`: `files`, `parked` and `credited`
  are asserted BEFORE `titles` and they PASSED, which is the receipt that they did not move.
  ⚠⚠ **NINE TITLES WERE ADDED AND THE EVIDENCE LAYER MOVED BY SIX.** The delta was ATTRIBUTED per
  file by reverting one at a time: demographicsMigration +3, convergence +3, upswingKernel **+0**
  — that file is PARKED, and was parked at this train's base too, so no member changed any park
  state. Recorded because a parked file's pins are real coverage the census cannot see.
  ⭐⭐ **THE FOUR DECLARED-SHIFT GOLDEN FAMILIES RE-RECORD IN THEIR OWN DEDICATED COMMIT AT THIS
  TERMINAL, QUOTING THE DECLARATION TABLE** — the amended §72.3 (§110.3), which moved the
  re-record from THE ONE REGEN to the shifting train's own terminal so the gate stops being poisoned
  as an instrument for every intervening train. FLAG-LIGHTING shifts still re-record only at THE
  ONE REGEN.
- **Measured (previous):** 2026-08-15 — **THE `cs-a` TRAIN LANDED ALL FOUR MEMBERS: THE FIRST HALF OF THE
  DECLARED-SHIFT CORRECTNESS BATCH.** Chain: base `eab6eba0` → `8cf97677` (P1) → `75e608ad`
  (CS-A1) → `014a1c60` (CS-A2) → `874c642b` (CS-A3) → `9bb72151` (P2, the within-train staged
  promotion) → `24c675af` (CS-A4) → the terminal. `validate:packets` 66/0 → 70/0.
  ⭐⭐ **THIS IS THE ESTATE'S FIRST TRAIN WHOSE TERMINAL GATE IS DELIBERATELY RED, AND THE RED IS
  THE DECLARATION.** Under §72.3 / §80.2 / §27 the batch is same-seed output-moving BY DESIGN and
  goldens re-record ONCE at THE ONE REGEN, by the owner — never in-lane. Three golden files move
  and every one of them is enumerated: `generatorGoldenMaster` (501 configs, CS-A1),
  `beliefMapGolden` (3 configs) and `beliefAxesDormancyGolden` (2 configs, both CS-A2). Nothing
  else in the same-seed suite moves — 86 of 87 files and 622 of 623 tests byte-identical.
  ⚠ The second belief golden is a DORMANCY golden, and the distinction was checked rather than
  assumed: its dormancy CONTRACT arm is GREEN, as are both lit anti-vacuity arms and the
  deep-determinism arm. Only the projection HASH moved, because the confidences it hashes moved.
  ⭐⭐ **EVERY COMPILE FIGURE WAS RE-MEASURED AT THIS BASE, AND FOUR DID NOT SURVIVE.** The
  predicted `npcGenerator` shrink 1350 → 1346 and its owed size-baseline ratchet-down are NOT
  incurred — the cure is exactly line-neutral. The census tuple in the compile was stale by
  prf-1's +2 titles. The belief round4 separation is 1.364e-4, not 1.0e-4. And CS-A3's specified
  cure shape was refuted outright.
  ⛔⛔ **THE CS-A3 REFUTATION IS THE SHARPEST RESULT.** The compile specified two guard arms and
  asserted neither subsumes the other. Both halves are measured false: the retreat registration
  sits inside the same block that rewrites the record, so four built variants — both arms, either
  alone, and a fought-set — produce BYTE-IDENTICAL output (the redundant-guard-vacuity class), and
  when retreat routing returns no path NEITHER arm fires and the defect stays live. The cure
  therefore keys on having been BEATEN rather than on having successfully retreated.
  ⚠ Reported as measured, not as hoped: the standing suite CANNOT discriminate the two shapes on a
  routable topology (both substitute mutants pass 44/44), because the difference lives only in the
  no-route case, which a hand digest cannot reach. An arm that tried reddened its own non-vacuity
  guard and was REMOVED rather than weakened; the predicate got a test-independent
  `requiredSymbols` guard instead.
  ⭐⭐ **CS-A4 TURNED OUT TO BE MORE THAN A LATENT REPAIR.** The estate's own landed line-digest
  fixture mints a field battle EVERY TICK against an already-beaten, already-recalled column —
  60 → 39.57 → 23.78 → 13.08 — which is exactly the battle-per-tick pathology `hostilePairFor`'s
  own comment says spatial-engine-3 forbids. CS-A3 cannot close it (its set is per-tick while the
  derive loop clobbers the retreat record back into a march each tick); CS-A4 does. A landed test
  was pinning that defect, and its body is corrected with the title byte-identical.
  ⭐ **EIGHT PLANTED MUTANTS ACROSS THE TRAIN, EVERY RESTORE VERIFIED BY `cmp` EXIT 0.** The
  sharpest is CS-A2's: a naive `lastUpdateTick = now` cure produces the RIGHT curve by the WRONG
  mechanism, and reds exactly one arm of 23 — the invariance arm written for it.
  Census `2438/364/2074/20208/5672` → `2438/364/2074/20220/5676`: `files`, `parked` and `credited`
  **UNMOVED** across four members (zero new test files), +12 titles and +4 suites, read from the
  walker's OWN failure output and re-run whole because the arms are sequenced. Both typecheck
  ratchets at their exact floors (173/173, 1134/1134). §104.4 bundle obligation measured NOT
  incurred; §102.3 and §85.4 likewise.
- **Measured (previous):** 2026-08-15 — **THE `prf-1` TRAIN LANDED ITS SOLE MEMBER: THE GOODS-MATCHER
  RESTORATION, AND THE GUARD THAT WOULD HAVE CAUGHT ITS DELETION.** Chain: base `630265b3` →
  `e40e211e` (P1) → `97ece4b7` (PRF-1) → the terminal. `validate:packets` 65/0 → 66/0.
  ⭐⭐ **THE HEADLINE IS HISTORY, NOT CODE.** The cure §84.2 ruled the review program's
  highest-leverage repair ALREADY EXISTED here: it arrived 2026-07-02, survived to 07-05, and was
  destroyed on 2026-07-15 by a master-merge resolution that took the memo-less parent of a
  568-conflict merge. Its test file survived that merge BYTE-IDENTICALLY while the implementation
  it pins did not, so for six weeks the file's header described a cache the module no longer had.
  ⛔ **NOTHING REPORTED THE LOSS BECAUSE THE SURVIVING PIN IS VACUOUS**, and that is measured, not
  inferred: all five of its assertions PASS against the un-memoized base. They are equivalence
  pins — correct as neutrality pins, structurally incapable of detecting the deletion they were
  written beside. A ~5x per-tick regression therefore lived unreported until a performance audit
  went looking.
  ⭐⭐ **NEUTRALITY WAS PROVED AT THE COMMIT, NOT INHERITED.** 46,062 differential checks over
  2,710 inputs in three orders — forward, reverse, re-sorted, so memo-eviction state differs per
  pass — across ten exported surfaces, base being a `git archive` of the pre-cure blob and cured
  being the live module: **0 diffs**. Thrown messages are part of the compared value, so divergent
  failure behavior would also convict. The same-seed goldens are byte-identical at **87 files /
  623 tests**, run after the last edit. Motion there would have been a STOP, not a fork, because
  §84.2 ruled this an ORDINARY repair and motion refutes the ruling's own premise.
  ⭐⭐ **THE GUARD IS THE MEMBER'S REAL DELIVERABLE, AND IT IS EXECUTED-PROVEN TO BITE.** The five
  landed titles stay byte-identical and stay equivalence pins. The added alias-snapshot arm pushes
  an alias onto a catalog entry at runtime and then resolves a label the memo has NEVER seen.
  Planting the destroying blob over the cured file reddened **exactly ONE arm of seven — the new
  one** — restored via cp/cmp with byte-identity verified. That single run is simultaneously the
  proof the guard works and the proof the other six could never have caught the merge. ⚠ Its probe
  label must stay memo-COLD: resolving it before the push lets the result cache answer and the arm
  goes vacuous, which is documented at the site.
  ⭐ **A SECOND, TEST-INDEPENDENT DETECTOR RIDES `requiredSymbols`.** The validator resolves
  symbols against the LIVE tree at every status, so with the same blob planted `validate:packets`
  exits 1 naming both new symbols as missing. The class that destroyed this cure once now has two
  detectors at different layers, one of which needs no test to be run.
  ⭐ **THE WHOLE-TICK CLAIM IS A BAND, NEVER A POINT.** Component speedup measured at this commit:
  fuzzy hit 103.2x, fuzzy miss 78.3x, and **69.0x** on a tick-shaped mix of 3,000 calls over 99
  labels (263.68 → 3.82 ms). By Amdahl over the audit's measured 70–83% tick-CPU share that is
  **3.2x–5.5x whole-tick, 4.7x at the midpoint** — §84.2's "~5x" confirmed at band-top and stated
  as a band, because a point estimate would over-claim at the low end. The share is the one
  INHERITED input; the component was re-measured.
  ⭐ **THE MEMORY BOUND SATURATES, MEASURED.** Instrumenting an integrity-counted archive (6,443
  paths in, 6,443 out) at n=30 x 3 ticks: 29,656 / 29,029 / 28,994 calls per tick — the audit's
  "~3,000 per tick" was conservative by an order of magnitude — and 323 distinct fuzzy keys per
  tick with cumulative 323/323/323, so **every key the run will ever use appears in tick 1**. The
  512 cap is never approached.
  ⛔ **THE FIXTURE CANNOT MIRROR THE CODE IT PINS**: it was generated by the ARCHIVED BASE module
  before the cure was applied, records its own provenance, and regenerating it from the cured tree
  is a named STOP. ⚠ Tier 2 excludes the shared `aliases` array on both sides because the landed
  tamper pin pushes onto it and never pops — found by EXECUTING the pin, not by reading it.
  ⚠ **A MEASURED CONSEQUENCE THE COMPILE DID NOT PRICE:** at base the bundler tree-shook
  `TOKEN_STOPWORDS` and `tokensOf` OUT of both AI bundles because `fuzzyMatch` was unreachable
  from their entry points. The eager candidate IIFE is not provably side-effect-free, so both are
  now retained at **+688 bytes per bundle, on two bundles**. No size row governs these artifacts,
  so nothing reds, but the cost is the direct consequence of the eager-over-lazy judgment and is
  recorded rather than left to be discovered.
  Census `2438/364/2074/20206/5672` → `2438/364/2074/20208/5672`: `files`, `parked` and
  `credited` **UNMOVED** (zero new test files), +2 titles and +0 suite titles. Effective lines
  421 → 435, **+14 against the 15-line shared-file cap — one line of room**, measured with
  eslint's own `Linter`. §104.4's bundle obligation INCURRED and discharged for both AI closures.
- **Measured (previous):** 2026-08-15 — **THE `rn-1` TRAIN LANDED ALL FOUR MEMBERS: the
  RELATIONSHIP-NORMALIZER CURE, from the renames to the signed content repair.** Chain: base
  `cf12c976` → `cd71f0d2` (P1) → `a8f09e14` (RN-A0) → `24ac90fa` (P2) → `d8236665` (RN-A1) →
  `c73ebb3d` (P3) → `196b256a` (RN-C) → `946dbd5b` (P4) → `9c8fe6cd` (RN-B1) → the terminal.
  `validate:packets` 61/0 → 65/0. The §64.7 arm ladder is LAW and is the landing order.
  ⭐⭐ **ZERO NEW TEST FILES ACROSS FOUR MEMBERS, AND THE EXPENSIVE CENSUS FIGURE NEVER MOVED.**
  §64.4's ruled RE-POINT was discharged with three honest hosts — `postureNameCollision.walker`
  for A0's collision class, `implicitNeutralSingleSource` for A1's single-writer law, and
  `vocabularyTotality.walker` for C's dead-arm class — each of which already owned the defect
  class by charter and already shipped the scanner. The census lands
  `2438/364/2074/20206/5672`: `files`, `parked` and `credited` are **UNMOVED**, and only the
  evidence figures move (+23 titles, +5 suites). §102.3 is measured NOT INCURRED for the same
  reason, with one `meta:` rationale key added where A1 puts a SECOND independent invariant
  into a file whose entry was already spent — the case the manifest's own `_doc` names.
  ⭐⭐ **THE OUTPUT-NEUTRALITY OF A0/A1/C IS PROVED, NOT ASSERTED.** A0's three renames measure
  **EXACTLY unchanged** effective lines (267→267, 200→200) because all three bindings are
  module-private `const`s, and the walker pins them as NOT exported, which is what that claim
  rests on. A1's identity pin was generated by executing both resolvers against a `git archive`
  of the pre-feature base, so it cannot mirror the cure, and its negative control holds the two
  planes at a required divergence count so an accidental convergence reds at the pin rather than
  at a golden. C's six one-token deletions leave every touched file's effective count unchanged.
  ⭐⭐ **B1's SAME-SEED PROOF WAS RUN, AND IT IS THE REASON THE SIGNATURE HOLDS.** The full
  golden suite — 87 files / 623 tests — is byte-identical after the persisted-read shift, run
  once at the cure and again after the last edit. Six of the identity pin's forty rows move,
  every one declared in the diff, all on the state plane; the regional column does not move.
  ⛔ **THE HOT FILE WAS MEASURED, NEVER INHERITED.** `convergence.js` sits at 798 of 800 and arm
  C edits it; the packet opened with the executed `Linter` measurement and the file lands at
  **EXACTLY 798** — net zero by construction, no size-baseline entry, no ceiling raised.
  ⛔⛔ **AND THE TRAIN FOUND A LIVE DEFECT IT DELIBERATELY DID NOT CURE.** §67.4's
  ask-the-corpus law, executed, showed `convergence.js`'s `FRIENDLY_REL` admits `ally` — which
  canonicalization makes unreachable — and MISSES `allied`, the live value it reads raw off a
  regional-graph edge. Adding `allied` would move generated output, so it is a chair surface
  (`CR-TE18-CONVERGENCEALLIED`); and `ally` was deliberately NOT swept out either, because a
  tidy-looking set is exactly how the missing `allied` would stop being visible. The habitat
  pin carries that finding as its written expected-dead reason, at the site.
  ⚠ **THREE COMPILE FIGURES WERE CORRECTED BY MEASUREMENT** rather than absorbed: the banked
  shim inventory is THREE, not four (`generosityGate.js` carries an inline fold, not a table);
  the `negativeAssertionAnchor` frozen row on `vocabularyTotality.walker` is 1, not 0; and the
  two-directional anchor trap on `regionRelationshipB06Fixes` was priced but NOT sprung, because
  that file's pins read the regional plane and B1 moves only the state plane.
  ⚠ **`tense` IS THE ONE THING THIS TRAIN REFUSED TO DECIDE.** It is the fifth LEGACY-LIVE
  spelling, but no ruling determines which defaults row it should claim and the candidates carry
  materially different numbers, so it stays unmapped, pinned as a decision, and raised for the
  chair.
- **Measured (previous):** 2026-08-15 — **THE `wc-0e` TRAIN LANDED ITS SOLE MEMBER: both war-circulation
  flags, with the by-name reads that make them honest.** Chain: base `00298c5f` → `00ceda98`
  (D0) → `57e0b8f5` (P1) → `9602952b` (WC-0E) → the terminal. A flag wave is a train boundary,
  so it rode alone. `validate:packets` 60/0 → 61/0.
  ⭐⭐ **THE BIJECTION IS A TRIPLE AND ALL THREE MOVED TOGETHER**, 20 → 22 across the engine-gated
  manifest, `VIRTUAL_RULES` and `VIRTUAL_SUBSYSTEM_ROWS`. §92.2's ordered pin couples the first
  two and `engineGatedRuleKeys` direction 3 couples the first to the third, so a mint that moved
  fewer than three reds two walkers. All three §49/§50 obligations were INCURRED and each proved.
  ⛔⛔ **THE FRESH PREFLIGHT REFUTED PART OF ITS OWN CHARTER, WHICH IS WHY THE PACKET WAS COMPILED
  RATHER THAN INHERITED.** The charter named both the `blocks[]` fail-closed arm and each flag's
  by-name gate-read module; measured, those cannot be the same site — `normalizeDeployments` takes
  no rules and has none in scope, and it must NOT acquire any, because persistence hygiene runs on
  every load and an arm that cleaned malformed saves only while the flag was lit would leave a dark
  world's saves un-normalized, the fail-OPEN direction on a persistence surface. The arm landed
  UNGATED and both reads landed as one conjunction in the ledger's own gate — the
  `warLayerEnabled`/`warTerminationEnabled` shape already live in `settlementStrategy.js`, borrowed
  rather than invented. Lane TC17 predicted a compiler would miss exactly this half of F-1.
  ⛔ **NO `spatialUsage` TRACKED ROW LANDED, AND THE VOLUME CONTRADICTS ITSELF ON IT IN ONE
  SENTENCE**: it charters rows for all three keys at this wave while saying the wave makes no
  writer calls. The coverage walker is exact-set in both directions over a live source scan, so a
  row for a key nothing writes reds — §73.3's F-1 is the correction of that precise sentence, and
  the mutant that plants the row reds the walker, which is F-1 demonstrated mechanically.
  ⭐ **TWO INHERITED FIGURES WERE RE-DERIVED WITH THEIR MECHANISMS NAMED.** §50.1's "seven
  edge-shared bundles" decomposes **2 + 5**: exactly two bundles reach `simulationRules.js`
  transitively, and all five metafiles move on ANY invocation — measured by running the builder
  against a clean tree with no input change at all, which dirtied five metafiles and zero bundles.
  And the coupling verify-at-build fired at COMPILE rather than at build: `contributionLedger.js`
  matched none of the 21 `LAYER_PATTERNS` regexes and took an exact-path WAR home in its own
  commit, a family home rather than an argued admission, so both ceilings stood still.
  ⚠ **ONE MUTANT NEEDED A CORRECTION AND IT IS ON THE RECORD.** The literal-drive mutant first
  replaced ONE drive and convicted nothing, because five others remained and the credit survived —
  redundancy rather than vacuity, but it proved nothing. The corrected plant removes EVERY literal
  drive for one flag and the lit-coverage ratchet reds.

- **Measured (previous):** 2026-08-15 — **THE `wc-0` TRAIN LANDED ALL FOUR MEMBERS: the WAR-CIRCULATION
  family's first code, and the estate's first train built on the landed §77/§2.7 machinery.**
  Chain: base `98c7872e` → `a393b109` (D0) → `79ffba74` (P1) → `511d2ceb` (WC-0A) → `e92e962c`
  (P2) → `4c87bae4` (WC-0B) → `3b660f91` (P3) → `cf7e6466` (WC-0C) → `9da1bec5` (P4) →
  `e47d6763` (WC-0D) → the terminal. Six new production and test leaves, six new suites,
  twenty-six new titles, and `validate:packets` 56/0 → 60/0.
  ⭐⭐ **THE MACHINERY RAN AHEAD OF THE BUILD AND BOTH TOOLS EARNED THEIR KEEP.** The premise map
  validates in both directions and `queue-check` **now HOLDS** — the §77 R1 conviction lane TC17
  reported is DISCHARGED, because `rn-1` (RN) was compiled and TTS-simulated as slot 2 while this
  train ran, so the next two executor slots are two distinct families. The pre-proof harness ran
  all four member batteries concurrently in isolated worktrees at their own commits, TRUE_EXIT 0.
  ⭐ **`timeoutMs` was re-measured rather than inherited.** The compile left it at an
  acknowledged bounded guess of 600 000 ms; a calibration run of the harness itself measured a
  worst case of 10.2 s cold and four-way concurrent, and the live run's worst was 18.7 s (the
  law-band fence walks all of `src/`). Re-stamped at 120 000 ms — real headroom, and a wedged
  process is caught in two minutes instead of ten.
  ⭐⭐ **THREE PINS WERE WRITTEN VACUOUS AND EACH WAS CAUGHT BY ITS OWN MUTANT BEFORE LANDING.**
  WC-0A's derived-set arm first re-computed the derivation inside the test and compared it to the
  export — two things that move together, so the literal-substitution mutant passed 5/5; it now
  plants a dead arm into a copy of the real source and requires the copy's export to LOSE that
  event. Its module-load arm first threw its own error, a self-supplied anchor that survives the
  guard's deletion; it now requires the MODULE to refuse the import. WC-0D's darkness arm asked
  the preset WRAPPER whether it owned the gate key, when every override is spread into `rules` —
  so the wrapper can never own it and the mutant that lit the flag walked straight past. All
  three now red. **The fixture-mirrors-deriver and self-supplied-anchor classes were caught by
  running the mutants, not by reading the tests.**
  ⭐ **A VOLUME UNDER-ENUMERATION WAS FOUND BY RUNNING THE VOLUME'S OWN RULE.** §7.A.4 makes the
  block-touching set a DERIVATION and then illustrates it with a nine-member list prefixed
  "today". Derived, it is TEN: `dm_removed` names the `block` pool on its debit arm, and a DM
  removal that left the share integrals stale is the hole a third declared sink exists to close.
  The hand list rotted exactly as the derivation was introduced to prevent. Docketed to the
  volume prose micro-act; the rule itself is intact and measured true.
  ⛔ **THE FLIP CONVICTED A LANDED PACKET, AND A ROT CLASS WAS CENSUSED AND CLOSED.** `EFF-M4`
  pinned the exact TEXT of an argued-roster ceiling in `requiredSymbols`, which are
  existence-checked at EVERY status — so WC-0C moving that ceiling by the roster arm's own
  instruction broke a landed packet. All 40 value-bearing `requiredSymbols` were enumerated: 37
  pin a stable structural prefix, and exactly THREE pinned a mutable numeric literal, one of them
  authored by this lane at its own P1. All three are converted to value-free existence pins; the
  number carried nothing `verifiedBase` did not already carry.
  ⚠ **A DECLARED CAPABILITY DELTA, not a discovered one.** With `sovereigntyTradeEnabled` lit,
  the offerable family set grows by two and `amnesty` takes index 0 of the stacking order,
  changing which family the sovereignty market offers first. The seeded corpus cannot move
  because that key is virtual and lit in no preset — asserted over the resolved rule objects, not
  claimed — so WC-0's own "DORMANCY: total" stays true of the corpus and is no longer true in the
  capability sense.
  ⭐ **The four numeric catalog fields are TRANSCRIBED with their derivations quoted in the
  source**, awaiting the chair's signature at collection; the train authors no number of its own,
  and both new leaves' scalar and curve tables land as SHAPE with their values scheduled to their
  movers.

- **Measured (previous):** 2026-08-15 — **THE `eff-1b` TRAIN LANDED BOTH MEMBERS: the staged successor
  that pays the records the machinery train left owing.** Chain: base `4f2d37d1` → `937ab5f4`
  (P1) → `d840a3a6` (EFF-M4) → `6bdba84e` (P2) → `ef6b0984` (EFF-M5) → the terminal. Eight docs
  files, zero production lines, zero census motion.
  ⭐ **`EFF-M4` repaired stale RECORDS, and every repair carries the measurement that produced
  it.** The HB preamble's argued-roster ceiling moved 13 → 19 WITH its transition walk (13 at
  `7699e367`, then 15, 17 and 19 across HB-0, HB-1 and HB-2 in a single day) so a reader learns
  the figure rotted by that family's own hand and that a compile must re-read the literal rather
  than inherit it; INT's provenance was un-conflated, because the exact-equality DISCIPLINE
  dates from 08-10 and the FIGURE from 08-14 while the old line asserted both of 08-10; GR and
  INFRA moved from three standing hot files to FOUR, each now naming all four.
  ⭐ **THREE lapsed Measured blocks were restored, not the two the ruling named** — `gr-5a`,
  `int-3b`, and `eff-1a`, whose own terminal had missed the obligation one landing earlier. That
  third one is measured rather than assumed: `git log -S` shows this convention's blocks are
  written by the LANDING LANE in its own terminal commit, so the absence was a lapse of the same
  rule this block restates, and the lapse is named inside the restatement rather than quietly
  repaired.
  ⭐ **The F9 strike-and-point landed at FIVE build-branch sites, not the four dispatched.** An
  exhaustive re-grep found a third `SOL_QUEUE.md` site carrying no adjacent note at all — the
  same bare shape the round called the sharpest of its six, and absent from that six. The
  under-enumeration the census warned about, found again on the sweep meant to close it.
  ⛔ One dispatched item was REFUSED as not landable and ROUTED instead: `docs/START_HERE.md` is
  a LEDGER file and does not exist on this branch. Its premise was re-verified TRUE at this base
  (the check chain is 17 steps, derived from `package.json`'s own script) so the chair's ledger
  sitting acts on a measured figure rather than an inherited one.
  ⭐⭐ **`EFF-M5` re-folded `DESIGN_BUILD_EFFICIENCY.md` whole, and the divergence it cured is
  the fold mechanism biting its own tail.** `INFRA-M1-DOCS` folded that volume and deliberately
  deleted the "until then, the ledger copy is canonical" clause, because two canonical copies is
  the drift a fold exists to end — and the chair then amended the LEDGER copy, so the branch
  silently reacquired exactly that drift while the build copy's header asserted it was canonical.
  The fold is verbatim, PROVED by an inverse check that reproduces the source byte-for-byte, and
  the rewritten clause replaces the assertion with a CONVENTION: the ledger copy is the DRAFTING
  SURFACE because it carries no gate, and the build copy is CANONICAL AT ITS BASE, re-folded by
  a docs member whenever the drafting surface has moved.
  ⚠ **One self-red is on the record.** `EFF-M4`'s landing receipt quoted the four frozen
  naked-claim keys verbatim and thereby minted three new ones in its own file — a packet is in
  the claims corpus like any other doc. `EFF-M5`'s focused proof caught it, and the cure is a
  REWORDING rather than a widened scan or an exemption. The sequencing is the lesson: M4's
  deliverable proof was clean and stayed clean; the regression entered at the promotion rung that
  appended the receipt, because a receipt written after a green run is an edit after a green run.

- **Measured (previous):** 2026-08-15 — **THE `eff-1a` TRAIN LANDED ALL FOUR MEMBERS: the
  efficiency machinery, landing itself.** Chain: base `954b4e0f` → `fdf14f60` (P1) →
  `24688a37` (EFF-M1A) → `dde9ff55` (P2) → `93e3904a` (EFF-M1B) → `e831e93f` (P3) →
  `0e8f39b2` (EFF-M2) → `986b0c22` (P4) → `3d50effb` (EFF-M3) → `85712a1f` (T1) → the
  terminal. Eight handwritten files, ZERO production lines, and a capsule diff of exactly
  three lines — `stampedAt`, the sha inside `method`, and `validatePackets` 50 → 54.
  ⭐ **`scripts/preproof-train.mjs`** runs member batteries concurrently in throwaway
  worktrees outside the repo, with containment enforced on REALPATHS, batteries spawned
  detached so signal cleanup kills process GROUPS rather than reaching for `pkill`, cleanup
  awaited on every exit path through ONE shared promise paired with a `signalled` flag, and an
  exit vocabulary in which a battery red is the ONLY exit 1. It pays both obligations the R-EFF
  audit left to its landing: a REQUIRED per-battery `timeoutMs` (the ceiling is the plan
  author's measured obligation, never a constant the harness invents) and per-member vitest
  cache isolation through a generated override config written OUTSIDE the worktree — because
  the throwaway tree's `node_modules` is a write-through symlink, so Vite's default
  `cacheDir` resolves into the executor's own tree.
  ⭐⭐ **ONE DRAFT CLAIM WAS REFUTED BY EXECUTION AND CURED RATHER THAN CARRIED**:
  `--reporter=basic` no longer exists in vitest 4, so an estate run produced a STARTUP error,
  exit 1, and the harness announcing an early truncation signal for a reporter typo — a setup
  failure masquerading as a battery red, which is the exact class the audit had already cured
  once and which the toy repo could not see because its stub ignored the flag. A battery red is
  now graded only against a JSON result naming at least one executed test; anything else is
  `phase: 'startup'` and exit 2.
  ⭐ **`scripts/premise-map.mjs`** makes the three re-charter redundancies checkable rather
  than asserted, and CONVICTED THE ESTATE on its first application: the next distinct-family
  slot after the `eff-1a`→`eff-1b` staged pair was not compiled, so R1 did not hold. The same
  plan with that one field true reports R1 HOLDS, which is what makes the conviction the
  estate's state and not the tool's.
  ⭐ **`PACKET_STANDARD.md` gained six sections** — differential member caps, parallel
  pre-proof, premise maps and scoped truncation, validator status-sequence simulation, the
  registration obligations a wave prices at compile, and the census-burn law — and the five
  family preambles each gained ONE section, 131 lines, byte-identical across all five apart
  from the section number and a single family-measured stamp line.
  ⚠ **The stamp line refused the easy reading**: the §70 program's seven rounds stamped ten
  volumes, GR and HB are in neither that enumeration nor any acceptance of their own, and INFRA
  has no annex for a round to stamp — so all three record a cap of FOUR with the door named,
  because writing EIGHT into an unstamped family's preamble would manufacture exactly the
  authority the stamp precondition exists to gate.

> ⚠⚠ **THE MEASURED-BLOCK CONVENTION IS BINDING AT EVERY EXPOSURE.** The landing lane — not
> the chair — writes the block, in its own terminal commit, in the same act that flips the last
> member and regenerates the capsule. A block names the train, its chain by sha, and what the
> train actually settled; the prior block is demoted to `Measured (previous)` and nothing is
> ever deleted. **Three blocks had lapsed when the `eff-1b` train restored them** — `gr-5a`
> and `int-3b` (both named in §68.4) and `eff-1a`, whose own terminal missed the obligation
> one landing earlier and whose executor records the lapse here rather than leaving it to be
> found. A lapse is cheap to repair and expensive to notice: the block is the only place a
> reader learns what a landed train MEANT, and `git log` does not carry it.

- **Measured (previous):** 2026-08-15 — **THE `est-1c` TRAIN LANDED (`EST-C`), AND WITH IT THE ESTATE'S
  ORDINARY TEST DEBT REACHES ZERO.** Chain: base `5c774940` → `0adf27ff` (P1) → `5ca2012c`
  (content) → `c632cbf4` (census half + the ruled conversion) → the terminal.
  ⛔⛔ **WHAT IT ACTUALLY FIXED WAS NEVER TEST DEBT.** Three ratchet rows filed as `debt` were
  three guards correctly reporting that **confirming any auto-discovered supply chain was
  refused** — a shipped feature that could not complete for anyone. The recorded cause was
  false in both halves: the "fixtures" are the product's own output, and the writer that
  *"stopped supplying needIcon"* never stopped — it emits the empty string deliberately, under
  a nine-line rationale, pinned by `copyCorruption`. The defect was a **validator asymmetry**:
  two sibling icon fields, written into ONE object literal by ONE writer as the SAME empty
  string, were validated by two different rules. The cure is admission-side and opt-in —
  `boundedText` gains one explicit `emptyOk` and the single `needIcon` call site opts in,
  widening **only** the emptiness arm so the field stays stricter than the sibling it was
  assumed to match. ⭐ **Proven by execution, not argued**: refused at HEAD with the exact
  banked error string, ADMITTED under the cure, and for an already-admitting non-empty icon
  the HEAD and cured chains are **byte-identical** — so no persisted content hash can move and
  no same-seed golden is reachable. ⚠ **A behaviour shift is DECLARED, not discovered**:
  confirming a discovered supply chain stops being refused and starts working. That is the
  deliverable. The persistence-admission grazing stays on the veto surface per §60.1.
  ⭐ **Fixing once cleared all three rows**, which was a packet premise and is now a
  measurement: the rows never had three causes, they had one.
  ⭐⭐ **AND THE CONTROL CONVERTED RATHER THAN EMPTYING (`CR-EST-CONTROLZERO`, §95).** These
  three rows were the estate's ENTIRE remaining ordinary-debt population, so paying them left
  `ORDINARY_TEST_CONTROL` with nothing to sample and no honest way to refill — naming any
  survivor would certify an enforcement walker as ordinary. The ruling refused both easy exits.
  The roster is empty and its floor is **gone rather than lowered to zero**, and the sampling
  test becomes **the victory assertion**: the ordinary-debt population must be EXACTLY ZERO,
  and any new ordinary red is **triaged on arrival, never banked**. That is strictly stronger
  than what it replaces — a classifier false positive and a lane banking an ordinary failure
  are both instances of *an unledgered row exists*, and the conversion reds on either. Its
  partner arm keeps the victory honest: the census must be non-empty, both ledgers non-empty,
  and every surviving row ledgered, so zero-of-an-empty-set can never masquerade as a win.
  ⚠ **SEVEN paths, priced at compile including the conversion** — the lesson `est-1` paid for
  twice, applied: **burning a census row is never a one-path act.**
- **Measured (previous):** 2026-08-15 — **THE `est-1` ESTATE-REPAIR TRAIN LANDED TWO OF THREE MEMBERS AND
  STOPPED AT ITS THIRD, on a blocker no compile could have seen because it only exists once the
  first member's win is banked.** Chain: base `a6fd6395` → `2422f08f` (P1) → `634ceaee` (EST-A
  content) → `c3be5af4` (EST-A census half) → `3be4736f` (EST-A's fourth path) → `7c041e9b`
  (P2: flip EST-A, promote EST-B) → `dae90484` (EST-B) → the terminal.
  ⭐ **WHAT LANDED.** `EST-A` anchored the F6 cross-user map-import source contract to the
  impl's own closing brace: a 6,000-char window had been scanning 6,000 of a 7,717-char body,
  so the seed carry at offset 6,206 was invisible and the banked row blaming *"the import no
  longer carries the safe seed field"* was **statically false** — code right, pin wrong. The
  same truncation hid the only branch that handles the untrusted cross-user
  `sharedMap.fmgSnapshot` blob from the XSS negative that exists to watch it; both negatives
  were re-executed against the widened body and stay false. `EST-B` returned two
  security-adjacent suites to the evidence layer: the estate-wide census gains **16 titles and
  4 suite titles that were already running and simply could not be seen**.
  ⛔ **THE CHARTER WAS WRONG ABOUT BOTH MEMBERS' CURE SITES, AND §60 HAD ALREADY CORRECTED IT.**
  E-3's chartered *"re-point at live tables"* was executed as a counterfactual and measures
  ZERO: the suites park for `TEST_CONTEXT_PARAM` — the `.each` callback taking a parameter —
  and a STATIC LITERAL table is precisely what earns credit, under a predicate the walker's own
  header says *never widens credit, it only ever removes it*. The landed shape spells the cases
  out (visibility) and adds one roster-totality guard against a DISCOVERED source (anti-drift).
  ⭐⭐ **THAT GUARD EARNED ITS KEEP BEFORE IT LANDED**, which is the best argument for the shape:
  the compiled roster spelled three tier-blind fog modules and the discovery found FOUR.
  ⛔⛔ **THE STOP, AND IT IS A GENUINE MILESTONE WEARING A BLOCKER'S CLOTHES.**
  `tests/lint/testRatchet.test.js` holds `ORDINARY_TEST_CONTROL` — the false-positive half of
  the walker-census classifier control — under an arm requiring every named file to carry a LIVE
  census row. That list named **exactly the three files `est-1` repairs**. EST-A's burn evicted
  one and stepped the floor `3 → 2` on the file's own written law (*"It steps down WITH the
  population"*, landed once already as `5 → 3`). Measured set arithmetic then showed the rest:
  of the 15 remaining census rows, **12 are ledgered enforcement walkers and the other 3 are
  EST-C's targets**. Paying them takes the estate's ordinary-debt population to **ZERO** — a
  real achievement — but the control cannot be refilled, because naming any survivor would
  certify a walker as ordinary, the exact 2026-08-07 error. At zero the arm is a tautology under
  a message that says the guard is off. **What replaces a false-positive control when the
  population it samples reaches zero is a governance question about verification adequacy, and
  no build lane may write itself that exemption.** EST-C is not promoted; its cure is compiled
  and its live-path repair is proven by execution (refused → admitted, non-empty icons
  byte-identical), awaiting one ruling.
  ⚠ **EST-A COST FOUR PATHS AGAINST A DECLARED THREE, AND EST-B FOUR AGAINST THREE.** Both are
  amended in place rather than left asserting a refuted claim. The shared lesson is one line:
  **burning a census row is never a one-path act** — a repaired row can be named by inventories
  that assert its debt is still real, and those names go stale the moment the win is banked.
- **Measured (previous):** 2026-08-15 — **THE `in-1c-a` TRAIN LANDED (`IN-1C-A`), a SOLE-MEMBER train that
  STOPPED TWICE, and the estate gains its FIFTH phrased-kind registry family.** Chain: base
  `d5a6c009` → `15889756` (P1) → `22cedb1b` (I1) → `3f8fae00` (the §75 cure) → `24c61190` (the §85
  registration rows) → the terminal. The dossier sentence `IN-1b` renders stops being one
  hand-composed template forever and becomes a seeded pick from the governed INFORMATION corpus.
  ⛔ **STOP ONE — THE TWELFTH PATH THE PACKET RESERVED TO THE CHAIR.** The new walker lives under
  `tests/lint/` and its basename matches the enforced nomenclature, so the mutation-coverage
  totality arm claimed it automatically. The packet pre-ruled that branch as **STOP and report**:
  a `rationale` row is a governance claim about verification adequacy, and no build lane may write
  itself an exemption. §75 ruled OPTION 2, and the cure is TWO SURGICAL TEXT INSERTS — five added
  lines, zero removed, the never-re-serialize hazard intact — carrying a NEW ref authored for the
  IN family on the GRAMMAR and COMMERCIAL precedents.
  ⛔⛔ **STOP TWO — AND IT IS THE ONE THAT MATTERS, BECAUSE ONLY THE FULL GATE COULD SEE IT.** With
  the cure in hand, the first complete `test:ratchet` this member ever received returned THREE
  failing tests outside the frozen census. TE14 attributed them by MEASUREMENT rather than
  argument, running the same three files at three commits: all pass at the base `d5a6c009`, and
  two fail identically at `22cedb1b` and at the cure. Both name this member's own new leaves —
  `informationNews.js#informationReceipt` is an unclassified weighted decision fork, and
  `informationReceiptPools` is a module with no lit proof. **The parked implementation had been
  red under the full gate since it was built**, and the focused sixteen-file battery that declared
  it green structurally could not have seen it.
  ⭐⭐ **§85 RULED BOTH ROWS THE MINTING WAVE'S OBLIGATION, AND MINTED A LAW FROM THEM.** The fork
  row takes HBF-07's DEFER verbatim rather than a fresh reading, because
  `informationNews.js`'s own source comment records its pick as copied from `grammarNews.js` and
  the same KEYED_RACE signature discovers both — so ruling STAY for INFORMATION alone would answer
  for one family a question left open for its identical twin. The two rows close together or not
  at all. ⚠ The ruled "one row" also costs one CONSTANT: `DEFER_CEILING` is exact in both
  directions and moves 29 → 30 in the same commit, which is the argued-roster idiom working as
  designed. ⭐ **§85.4 adopts the REGISTRY-MINT TWO-OBLIGATION LAW**: a registry family that mints
  a seeded pick AND a pool module owes two estate-wide census rows, priced up front. IN-1C-A's
  packet priced neither, so the member cost FIFTEEN paths against a declared eleven.
  ⚠ **THIS TERMINAL IS NOT DOCS-ONLY**, unlike `hb-2b`'s and `int-3b`'s: both ruled cures edit
  `scripts/`, `src/` and `tests/` paths, which `scripts/base-state-capsule.mjs` counts as dirty
  measured scope and refuses to stamp over. The content therefore commits FIRST and the capsule
  stamps that content commit, so its window stays literally true.
  ⚠ **THE HEADER ABOVE HAD ROTTED BY TWO LANDINGS AND THIS RECORDS IT RATHER THAN CLOSING IT
  QUIETLY.** The `Measured branch/SHA` line named the `infra-1` terminal and the block below is
  `hb-2b`'s, so `int-3b`'s terminal — this train's own base — refreshed neither. Its landing is
  recorded in its own packet and in `OWNER_DECISION_QUEUE.md` §65.

- **Measured (previous):** 2026-08-15 — **THE `int-3b` TRAIN LANDED (`INT-3B`), the
  ELEVENTH exposure and the INT family's first wave.** Chain: base `fc8451c4` → `8401efbc`
  (P1, which also landed the INT family preamble) → `28d2824b` (I1 — the parked green
  re-pointed byte-identical) → the terminal. One pure, dark-safe, number-free leaf prices a
  defeated claimant's flight through the estate's ONE purposeful-travel substrate.
  ⭐⭐ **The wave's true blocker was found by measurement, not by reading**: the errand ROW DTO
  is war-welded — `normalizeErrand` refuses any row without a valid peace offer, acceptance
  and snapshot — so the design volume's own *"NO NEW LEDGER — SP-1 errands"* sentence is
  refuted at the base and no émigré record can persist there. The cure is GR-2's road: minting
  through the spine PRICES a journey and VALIDATES a class, and writes no row at all, so the
  member persists nothing and prejudges nothing while the record's home stays owner-gated.
  ⭐ Because the leaf's only import is ARGUED substrate, `scanCrossLayerPairs` produced ZERO
  pairs: no interior coupling registry was minted, no baseline entry bought, and both exact
  ceilings stood still. That single finding is what kept it a one-file wave rather than a
  registry program. ⚠ SIX paths exactly, with the estate-wide lighting census re-derived WHOLE
  in the same commit — which is what keeps a single-member train green at every commit.
  ⚠ **This block was LAPSED and is restored by the `eff-1b` train** (§68.4); its authority is
  the chair's own collection record.
- **Measured (previous):** 2026-08-14 — **THE `gr-5a` TRAIN LANDED (`GR-5A`), the TENTH
  exposure — and it STOPPED TWICE on the way, both STOPs ratified into standing law.** Chain:
  base `6cd18ad3` → `66494971` (P1′, re-promoted on the SIGNED seven-path manifest) →
  `85d19adf` (I1′) → `e93376ec` (I1″, the signed edge-shared bundles and the fourteen-path
  manifest) → the terminal; the superseded `836d1da8` (P1) and `318a7fff` (I1) are preserved
  on their own parked refs. A treaty remembers the worst it was ever observed to do: one
  conditional, monotone, drop-when-absent field written at the one site that already computes
  the tick's worst observation.
  ⭐⭐ **THE FLAG-MINT HABITAT TURNED OUT TO HAVE THREE OBLIGATIONS, NOT ONE, AND THIS TRAIN
  STOPPED TWICE FINDING THEM.** Minting an engine-gated flag costs (a) the ordered
  `subsystemRowsVirtual` equality pin; (b) the seven generated edge-shared bundle artefacts
  whenever `simulationRules.js` moves, without which the freshness gate reds; and (c) the flag
  drive spelled as a LITERAL `<flag>: true` in the acceptance file, because mechanism-coverage
  credit is granted only on a literal and a computed key attributes to NO key at all. Obligation
  (c) is the cheapest of the three to miss, because the code is right and only the machinery is
  blind. All three are standing law for every volume from this train forward.
  ⛔ **GR-5 as chartered was REFUSED IN PART** — the volume's block is six behaviours and five
  bands wearing one wave's name, and each of the five successors carries the measured reason it
  cannot compile at that base. ⭐ It authors ZERO numbers, which is why it landed without a
  chair value signature. ⚠ The lying-exit class was re-confirmed live here: the ratchet outlasted
  its window, backgrounded, and the harness reported the WRAPPER's exit 0 over a red gate —
  caught by reading the log, never by believing the code.
  ⚠ **This block was LAPSED and is restored by the `eff-1b` train** (§68.4); its authority is
  the chair's rulings at §§47/49/50.
- **Measured (previous):** 2026-08-14 — **THE `hb-2b` TRAIN LANDED BOTH MEMBERS (`HB-0B` + `HB-2`), the
  estate's SIXTH §28 train and its FIRST SPLIT PROMOTION.** The live manifest has 44 rows and
  zero dispatchable packets. Chain: `3ad7c082` (P1a) → `c777a158` (I1) → `46f1c942` (P1b) →
  `de214782` (I2) → terminal.
  ⛔ **ITS SHAPE IS THE RESULT OF A REFUTATION, AND THE RULE IT BROKE WAS ALREADY WRITTEN DOWN
  IN THIS FILE.** The compiled train promoted both members at once; `R35` measured that the
  packet validator reserves EVERY `changeManifest` path at every non-terminal status — only
  `LANDED` and `SUPERSEDED` release, and `DRAFT` reserves exactly as `READY` does — so two
  members that both lawfully edit `tests/domain/habitCurve.test.js` cannot stand promoted
  together. The error clears only on a status flip, so no implementation commit could have
  discharged it. The executing lane STOPPED pre-P1 rather than carrying an undeclared red on a
  stop instrument across two interior commits; `OWNER_DECISION_QUEUE.md` §44 ratified the stop
  and signed `CR-HB2B-SPLITP`, the staged promotion this train then executed.
  ⭐ **WHAT THE TWO MEMBERS BUILT.** M1 is a §42.2 micro-act: three structural bounds minted in
  `HABIT_TUNING`, the one lawful home, after §42.1 ratified that the HB-2 charter's claim they
  were already there was unmeasured fiction. The pledge age is INDEXED positionally off the
  shared half-life ladder rather than spelled, because the curve's own landed header forbids a
  rung word and no walker enforces that — the wave adds the machinery that promise lacked. M2
  mints the habit sub-ledger, its single writer, the gate ladder's first door and the
  registration, all dark: `writeHabits` exists and nothing under `src` calls it, which a
  call-path spy pins rather than asserts.
  ⭐ **TWO LANDED PINS NO PRIOR COMPILE NAMED WERE DISCHARGED IN FLIGHT.** `R32`: minting two
  leaves into the habit directory doubles that family's file count, and the single-rounding-door
  scan runs over the WHOLE family, so neither new leaf may spell a rounding operator — every
  rounding routes through the imported `roundToUnits`. And HB-0B's own dormancy case was
  re-aimed IN PLACE at I2, with zero new titles, in the same commit that created its first
  consumer — the discharge that case's failure message demanded.
  ⚠ **THIS TERMINAL IS DOCS-ONLY**, by the same design as `hb-1p`'s: the census re-derived WHOLE
  at I2, the last commit in the train that moves a `tests/` byte, so the base-state capsule
  regenerates in ONE terminal commit and its stamped window is literally true.

- **Measured (previous):** 2026-08-14 — **THE `hb-1p` TRAIN LANDED (`HB-1`), the estate's FIFTH §28 train
  and the re-charter of the member the fourth one STOPPED.** The live manifest has 42 rows and
  zero dispatchable packets. Chain: promotion `dfd7b0ae` → implementation `1013d58b` → terminal.
  It mints the estate's ONE closed strategy-move vocabulary as a dependency-free leaf, the frozen
  habit fork registry classifying every weighted decision fork the estate can see, and the two
  totality walkers that make all three frozen.
  ⛔ **ITS HEADLINE ACT IS A REFUSAL, AND IT IS MEASURED.** The HABIT volume asked twice for the
  `'fortify'` disjunct in the ruling-bloc decision load to be deleted, and both asks died on
  execution (`OWNER_DECISION_QUEUE.md` §38, ratified). The corrected proof assumed the deciding
  site's inputs are limited to EMITTED moves; they are not, because a ratified same-seed golden
  passes the token straight in and records a non-neutral value for it. §39 struck the clause. The
  deletion is replaced by a brancher REGISTER whose owner arm is membership-OR-REGISTERED-FOREIGN,
  exact in BOTH directions, carrying an evidence address that names the golden — so a third
  deletion attempt reds where nothing in the refuted design could see it. `settlementPolitics.js`
  receives a COMMENT-ONLY edit and its executable text is byte-identical, which makes the
  goldens-preserved claim structural rather than argued.
  ⭐ **THE PREFIX'S NAMED ORPHAN WINDOW IS DISCHARGED HERE**, in the same commit that opens it:
  `habitCurve.test.js`'s whole-tree dormancy absolute is re-aimed IN PLACE — zero new titles — to
  a DARK-CLOSURE claim, so "dark" now means the whole habit family is unreachable from the engine
  rather than that two leaves have no importer. The fork-registry importer-scan class is killed at
  the registry's own end with EXTENSIONLESS module ids, for zero effective lines, instead of
  re-pointing twenty-six foreign scans; the one outlier scan is a recorded deferral to the
  `codeOnly()` micro-act family.
  ⚠ **THIS TERMINAL IS DOCS-ONLY, and that is the design rather than luck**: the census re-derived
  WHOLE at the MEMBER commit, which is the last commit in the train that moves a `tests/` byte, so
  the base-state capsule regenerates in ONE terminal commit instead of the prefix's two.

- **Measured (previous):** 2026-08-14 — **THE `hb-1` TRAIN LANDED A PREFIX (`HB-0` ALONE), the estate's
  FOURTH §28 train and its first PARTIAL exposure.** The live manifest has 41 rows and zero
  dispatchable packets. Chain: promotion `436f138e` → implementation `e188760b` → terminal.
  It is the HABIT volume's FIRST CODE: two zero-caller pure leaves — the closed twelve-class
  circumstance vocabulary with its declared precedence and owner-signed ceiling, and the
  frozen curve whose one rounding door every stock value passes through — plus the volume's
  own Bands/§8.2 reconciliation walker, whose sibling provably does not generalize because
  that parser's prefix alternation does not name HB and the habit volume's waves are bold
  paragraphs rather than headings. Dark by construction: no flag, no caller, no world read,
  no persisted shape, so the landing is byte-identical for every generated world and the
  dormancy evidence is a zero-importer source scan rather than a claim.
  ⛔ **THE TRAIN'S SECOND MEMBER (`HB-1`) IS NOT IN THIS LANDING.** It STOPPED at the
  terminal on a REFUTED PREMISE, ratified at `OWNER_DECISION_QUEUE.md` §38, which also rules
  this prefix landable on its own complete proof. The premise that died is the volume's own
  R7-corrected one: deleting the dead `'fortify'` disjunct from the ruling-bloc decision load
  is NOT behaviour-identical, because a ratified same-seed golden names that token in its
  load-bearing move list and records a non-neutral factor for it. Half the proof held — no
  EMITTED move satisfies the disjunct — and the half the volume itself nominated as the check
  is what caught the rest. Two further finds ride to the re-charter: a fork registry that
  stores module paths as text reads as an importer to every source scan keyed on a bare
  filename, and this wave's dormancy case is a whole-tree absolute that its own designed
  first consumer falsifies. ⚠ The prefix leaves a NAMED ORPHAN WINDOW — two `src` leaves
  nothing imports — on the recorded IN-1a precedent, discharged at the re-charter's first
  consumer; `packets/fp/HB-0.md` §15 carries it with its discharge condition.
  ⚠ **THIS TERMINAL IS NOT DOCS-ONLY**, and that is the prefix's own shape: a two-member
  train re-derives the census at its last `tests/`-moving MEMBER commit, and a one-member
  prefix has nowhere for that cure to sit but the terminal. The base-state capsule therefore
  stamps at the terminal rather than at the member.

- **Measured (previous):** 2026-08-14 — **THE `gap-1` TRAIN LANDED (`GAP-1`), the estate's THIRD
  §28 train and its first single-member one.** The live manifest has 40 rows and zero
  dispatchable packets. Chain: promotion `ea98c42f` → implementation `152d3f19` →
  terminal. ⚠ The wave was promoted as `CW-1` and RENAMED under §33: that id is the
  declared FP wave #58, the cascade governor, and reusing it would have poisoned the
  wave census. It cures GAP-1, which lane SD's diagnostic-soak scan ordered and lanes G1
  and G2 measured: the engine-gated census walker's detector regex constrains its
  RECEIVER to two literal tokens while its header claims to scan for the gate idiom
  as such, so a real strict gate read spelled any other way is invisible and the
  walker stays green. Three receiver shapes across four files hide seven keys.
  The widened scan was re-proved BOTH directions at this base before any edit —
  `TODAY=63 WIDE=70 LOST=0 GAINED=7`, a strict superset with zero new false
  positives — and the hidden-key list is proved UNMOVED across the thirteen-commit
  delta from lane G1's base by a population-side argument: not one line containing
  the token `Enabled` was added or removed anywhere in that `src/` delta, so the
  population the list is drawn from did not move.
  The four unaccounted keys are dispositioned by `OWNER_DECISION_QUEUE.md` §32 —
  `settlementPoliticsEnabled` and `underwaysOrganicFoundingEnabled` join the
  manifest with authored certification rows; `institutionPoliticalControlEnabled`
  and `biomeTruthEnabled` take exemptions with written reasons. The backlog stays
  exact at seventeen and its ceiling is never raised.
  ⭐ The member is census-NEUTRAL by construction and §32 ruling 2 signs it as such:
  it creates no test file and adds no literal `test(...)` or `describe(...)` title,
  every control going inside an existing test body, so the tuple
  `2418/366/2052/20024/5643` holds byte-identically and, per J-TE3-1, the plan
  declares NO interior census red. The estate-wide census row stays FREE.
  ⛔ §32 ruling 4's conditional was discharged BEFORE the first edit: `scannerToolFiles()`
  returns eleven governed paths and this manifest intersects them in zero places, so
  no observed-shape schema mint is owed.
  ⚠ One correction to the ruled shape is on the record rather than smoothed away.
  The §4.3(a) cohort widening was drafted as "declared event types equal the literals
  the lane's leaves mint"; live code refuses that spelling, because the only literal
  in question is minted from the top-affinity gap across every gap source, is already
  declared by another row, and the declare-a-channel-and-call-it-unobserved escape
  hatch is at its shrink-only ceiling. The shipped arm asserts declared against the
  SEPARABLE literals and enumerates every minted literal with a written reason, which
  is strictly stronger than the blanket claim it replaces.
- **Historical measurement:** 2026-08-14 — **THE INFRA-1 TRAIN LANDED (M1 + M2 + M3), the estate's
  SECOND §28 train and its first INFRASTRUCTURE one.** The live manifest has 39 rows
  and zero dispatchable packets. Three members were built as one unexposed commit
  sequence on the private ref `refs/trains/infra-1` from base `60083174` and exposed
  atomically by one old-value CAS to the train's single green terminal child. Unlike
  the first train, the PROMOTIONS RIDE THE CHAIN (the J-E1 lesson applied), so the
  order is P1 → I1 → P2 → I2 → P3 → I3 → T.
  **M1** `INFRA-M1-DOCS` — the §28 documentary substrate: nine docs files, zero
  production lines, census motion `+0` on all five figures, which is the property that
  made it the train's lawful truncation boundary. It folds `DESIGN_BUILD_EFFICIENCY.md`
  and `DESIGN_IP_PROTECTION.md` from the ledger branch verbatim, authors
  `INFRA-PREAMBLE.md`, gives `PACKET_STANDARD.md` its three §28 sections, and cures the
  three dispatch-surface contradictions lane FPC measured.
  **M2** `INFRA-M2-CAPSULE` — the base-state capsule stops being hand-stamped. One
  generator shelling only existing measurers, with a frozen `PROVENANCE` table declaring
  each row MEASURED, PINNED or the single ARG row, and every arm failing closed.
  **M3** `INFRA-M3-IP1` — the sourcemap guard IP-1, owed since 2026-08-07, whose
  stale-artifact polarity is deliberately INVERTED from every neighbour in its tree.
  ⛔ THREE CORRECTIONS ARE ON THE RECORD RATHER THAN SMOOTHED AWAY. The compile's
  "zero interior reds" was wrong by construction — a census-moving member reds the
  lighting walker at its own commit until the tuple is re-derived — and the red was
  re-declared with its exact predicted figures and closed at I3. The compile's
  `requiredSymbols` for M1 named prose the member's own cure DELETES, which the
  validator checks at every status; the promotion carries only preserved symbols and the
  created ones were added at the flip, where a CREATE claim first becomes checkable. And
  M2's battery caught two real defects the design was built to catch: its own reader was
  reading the census off a retained ancestry-pin COMMENT, and `GRAMMAR_RECEIPTS` is a
  keyed object whose bare `.length` yielded `undefined`, silently DELETING a figure from
  the artifact while a harness comparing `undefined` to `undefined` agreed. Both cures
  are structural, not point fixes.
  The whole census was re-derived once, at the last commit that moves a `tests/` byte,
  to exactly `2418/366/2052/20024/5643` — placed there so the terminal stays DOCS-ONLY
  and the capsule's stamped window is literally true under OWNER_DECISION_QUEUE §30.
  `parked` moves off 365 for the first time in many landings, by exactly one, for
  exactly M3's deliberately parked file. Full receipts: the three packets and Lane TE3's
  train receipt. No soak started.
- **Historical measurement:** 2026-08-14 — **THE GR-4B-II TRAIN LANDED (W1 + W2), the estate's
  FIRST §28 train.** The live manifest has 36 rows and zero dispatchable packets.
  Two members were built as one unexposed commit sequence on the private ref
  `refs/trains/gr-4b-ii` and exposed atomically by one old-value CAS to the
  train's single green terminal child: **W1** `039f8380` — the chair annex act
  minting A-23 / CR-GR4B-16, docs-only, **zero census motion**, which is the
  property that made it the train's lawful truncation boundary; **W2**
  `b3602c15` — the `reaffirmed` voice, thirteen paths, signed by CR-GR4B-17
  (registration files 3 → 5, paths 12 → 13), CR-GR4B-18 (the store-side dismiss
  mount with **no new persisted key**, exactly-once supplied structurally by the
  transition's compare-and-set) and CR-GR4B-19 (the landed silence pin
  re-addressed in place, census-neutral); and the terminal **T** `7b54c038`.
  W1's delta was proven zero by re-executing its battery at the base commit and
  matching the failure identities exactly. A1-A8 passed 8/8; the 21-file battery
  passed 270/270; all eight mutants convicted by their intended titles and
  restored digest-exact, with M3 convicted by two independent cases; both
  TypeScript ratchets held 173/173 and 1,134/1,134; OSR stayed exact at 1,998;
  `title=` held at 485 and the four kill-list ceilings at 85/69/167/163. The
  whole census was re-derived once, at the terminal, to exactly
  `2416/365/2051/20016/5642`; bare `check:tail` exited 0 at **28,032 tests / 16
  frozen known failures**, prerendered 314 documents, and passed strict dist
  50/403; separate boot smoke passed 521/521 chunks. ⛔ Two corrections are on
  the record rather than smoothed away: the packet's §7 row-5 prohibition on a
  `KIND_SECTION_DIVERGENCES` entry was ruled **defective** (the walker is machine
  truth; both rows landed inside the file's existing `<=2` budget), and the
  terminal's first gate exited 1 on six un-anchored negative assertions, cured
  in place before exposure with the member commit amended once while still
  unexposed. A second red, `implementationSession.test.js`, never reproduced and
  is recorded as PLAUSIBLE rather than diagnosed. The census is FREE. Full
  receipts: the packet §13 and Lane TE2's train receipt. No soak started.
- **Historical measurement:** 2026-08-14 — **GR-4B-IIIB LANDED.** The live manifest has 35
  rows and zero dispatchable packets. Implementation `770167c5` (exact ten
  paths, +100/-25 tracked) is the direct child of promotion `cdd4bf52`. B1-B8
  passed 8/8 (chair re-ran independently); the wider battery passed 212/212
  across 14 files; all 8 mutants convicted by their intended titles and
  restored digest-exact; both TypeScript ratchets held 173/173 and
  1,134/1,134; OSR stayed exact at 1,998. The census landed exactly as
  reserved at `2415/365/2050/20008/5641`; bare `check:tail` exited 0 at
  **28,021 tests / 16 frozen known failures**, prerendered 314 documents, and
  passed strict dist 50/403; separate boot smoke passed 516/516 chunks. The
  census is FREE. Full receipts: the packet §13 and the lane completion
  receipt (SHA-256 `18b8b135…`). No soak started.
- **Historical measurement:** 2026-08-14 — **GR-4B-IIIB READY.** The live manifest has 35
  rows and exactly one dispatchable packet. GR-4d's durable pending question and
  GR-4b-iii-a's landed composer idiom make the dossier line honest, and annex
  chair act A-22/CR-GR4B-12 (landed `c39d2413`, completed `2dd09360`) is its
  immutable corpus substrate at SHA-256 `0beee514…`. CR-GR4B-13 signs the single
  user-facing-surface override 1 → 2 (both mounts are named by the corpus
  block's own heading); CR-GR4B-14 gates eligibility on a stamped, live-term
  treaty via the read-model's own term view; CR-GR4B-15 binds `{settlement}` to
  the acting court viewer-independently. One DISPLAY-owned leaf composes one
  canonically ordered line per validated pending row through
  `successionQuestionPayloadTuple`; `section: null` keeps the kind out of every
  Herald, chronicler, and rumor home, so the registration cost is 2 files, not
  5, and GR-4b-iii-a's CR-GR4B-11 override does not transfer; `successionLines`
  is always-present-`[]` under the dormancy fence. The exact ten-path packet
  reserves the whole census at `2415/365/2050/20008/5641`, runtime
  `28010 → 28021` with the known-failure ceiling frozen at 16, registry rows
  `10 → 11`, floors `109 → 110` with the routing identity `6 → 7` (a legitimate
  no-desk divergence), and `title=` frozen at 485 with zero headroom; no hot
  file is in the manifest. Eight named mutants, both TypeScript ratchets, OSR at
  1,998, the coupling walker, the bare full gate, and separate boot smoke are
  mandatory. No soak is authorized.
- **Historical measurement:** 2026-08-14 — **GR-4b-iii-a LANDED.** The live manifest has
  34 rows and zero dispatchable packets. Version-2 authority `4bc44f8e`,
  capsule `518ee395`, seal `15f71332`, and cumulative attached run
  `000003-7cc4232d-03fc-4ad0-b3c7-7e86dd588a75` bind the exact twelve-path
  direct child `7e7d5205` at **+608/-27**. A1-A8 passed **8/8** directly,
  the exact twenty-file battery passed **296/296**, and the negative-anchor
  walker passed **9/9**. All eight named mutants failed only their intended
  A-title and restored digest-exact; both TypeScript ratchets held
  **173/173** and **1,134/1,134**, and observed-shape stayed exact at
  **1,998**. The attached cumulative state passed all fourteen steps.
  Independent bare `check:tail` exited zero at **28,010 tests / 16 frozen
  known failures**, prerendered **314** documents, and passed strict dist
  **50/403** (log SHA-256
  `dc9662ec9f09f50b6a1d581a52c4d71c68b64736d1f1ece0b708976fc8b2e348`);
  separate boot smoke passed **513 chunks / 6,265 edges / 31,706-byte
  shell**. The census is free at `2414/365/2049/20000/5640`. No
  GR-4B-IIIA soak started.
- **Historical measurement:** 2026-08-13 — **GR-4b-iii-a READY.** The live manifest has 34
  rows and exactly one dispatchable packet. GR-4d's terminal pending-question
  mechanism makes the opening beat honest, and annex-only chair act
  `955acd7a` discharged A-21 before dispatch at exact SHA-256
  `91f4920117ded93d822dedb8b67b5f6972f6fa9fc93857a31d9aee0614209fd7`.
  CR-GR4B-9 releases only `succession_question_opened`; CR-GR4B-10 binds its
  exact one-instrument/fallen-holder corpus; CR-GR4B-11 widens only the
  registration-production row **3 → 5**. The handwritten total stays exactly
  **12** and inherits no `12 → 14` override. One GRAMMAR leaf wraps the landed
  decision once, one-for-one at the late treaty mount, and five mechanical
  registrations file the seven-family notable/public beat under Herald trade
  with an explicit Chronicler-courts divergence. The packet reserves the whole
  census at `2414/365/2049/20000/5640`, exactly +1 file/+8 tests/+1 suite; the
  runtime denominator is predicted `27999 → 28010` with the known-failure
  ceiling frozen at 16. `peaceTerms.js` remains exactly 1264 raw / 797
  effective. Eight named mutants, both TypeScript ratchets, OSR, coupling and
  cross-home contracts, bare full gate, and separate boot smoke are mandatory.
  No soak is authorized.
- **Historical measurement:** 2026-08-13 — **GR-4d LANDED.** The live manifest had 33 rows and
  zero dispatchable packets. Version-2 authority `fcf1efbd`, capsule `83cec036`,
  seal `19d47eb7`, and attached run
  `000001-8a95fbe9-c9fa-4de9-aebc-d87610f3406d` bind the direct exact seven-path
  child `a17d0a02` at **+594/-23**. A1-A8 passed **8/8** directly and **211/211**
  in the 15-file battery; both TypeScript ratchets held **173/173** and
  **1,134/1,134**, observed-shape stayed exact at **1,998**, and all six named
  mutants failed by their intended titles before digest-exact restoration. The
  sealed inner gate passed strict dist **50/403**. Independent bare `check:tail`
  exited zero at the frozen **16 known failures / 27,999 tests**, built and
  prerendered **314** documents, and passed strict dist **50/403** (log SHA-256
  `6537b57a9aed5fb3344d9eebc4c3438f347dd3ff2e12e4a9f83e58c4d952ca97`);
  separate boot smoke passed **513 chunks / 6,265 edges / 31,706-byte shell**.
  The census is free at `2413/365/2048/19992/5639`. No diagnostic-soak worktree
  was created for this GR-4d run, and neither D-1 nor L0 started.
- **Historical measurement:** 2026-08-13 — **GR-4d compiled just in time.** The live manifest
  has 33 rows and exactly one dispatchable packet. GR-4a, GR-4c, and GR-4e are
  terminal, and the late treaty stage can see both same-tick seat-transition
  writers. The bounded mechanism queues only above-band DISAVOW descriptors when
  both existing gates are lit; below-band HONOR stays silent and unqueued, while
  dark mode delegates to the landed scorer byte-for-byte. The exact seven-path
  packet adds two GRAMMAR-owned leaves, changes three existing production files,
  creates one eight-test file, and re-records the whole census to the reserved
  `2413/365/2048/19992/5639`. `peaceTerms.js` must remain 797/800 and
  `applyWorldPulse.js` must remain at its exact 941 baseline. No diagnostic-soak
  worktree was created for this run, and neither D-1 nor L0 has started.
- **Historical measurement:** 2026-08-13 — **GR-4e LANDED.** The live manifest had 32 rows and
  zero dispatchable packets. Promotion `3339188b`, seal `b995d8c9`, capsule
  `a31af4e2` and direct green child `c5ef5c19` bind the exact five-path **+36/-20**
  implementation. The sole logic edit replaces the dead same-tick fracture window
  with the exact prior-tick law while preserving the one-fold stage order and the
  hot file at **780/800**. Disposable mutants separately convicted the old window
  at **64/67** and a detached stage call at **66/67**; restored batteries passed
  **67/67** and **100/100**, and the bare full gate passed with strict dist
  **403/403** across 50 files (log SHA-256
  `d5771a94b64d17630561805e26cae429f9b164e0424dec73e849d6c88216f7a8`).
  Exactly one title was renamed with no cardinality or census movement. The census
  is free at `2412/365/2047/19984/5638`; the next governed act is the live
  **CR-NEXT-1 census only**. No diagnostic-soak worktree was created and neither
  D-1 nor L0 was started.
- **Historical measurement:** 2026-08-13 — **GR-4e compiled just in time.** The
  live manifest had 32 rows and exactly one dispatchable packet. The two live
  treaty-fracture writers ran after the information-statecraft fold in the same
  pulse, so the old same-tick predicate could never consume their records. The
  packet froze the exact prior-tick law, five-path scope, net-zero 780/800 hot-file
  edit, one existing-title rename, two disposable mutants and the corrected
  **67/67 + 100/100** batteries; the proposed seven-file wider battery had measured
  94 rather than 99 tests, so the directly relevant six-test peace-causal dormancy
  golden supplied the honest 100-test closure.
- **Historical measurement:** 2026-08-13 — **AO-6 landed after an immutable proof STOP and governed
  amendment.** The live manifest has 31 rows and zero dispatchable packets. The first immutable
  implementation candidate `b02bc709c81d3d7c3b30d13aa9126a5cbac04b27` remains unlanded:
  its whole sweep exited 4 at `77 CAUGHT / 1 CLEAR / 4 gaps`, with log SHA-256
  `d582b8a06a909f5b05c7321e690a5702a7140f87829ab906ed80709d2eb99d9a`.
  The four measured contradictions license only three exact filtered legacy repairs
  (`seventh`, `en.registry`, `censused`) and removal of the modal plant's stray Perl quote.
  The clean terminal AO-5 tree carries
  541 enumerated mutation-governed invariants split `68 mutation / 275 rationale /
  198 uncovered`, ten additional meta mutations, 78 exact one-to-one sweep labels,
  67 in-place-or-missing plants over 53 guarded target files, and uncovered baseline
  198. AO-6 converts exactly the three corpus-coverage rationales to three named source
  plants whose captured output must carry exactly one detailed `FAIL ... > <full-title>` line,
  adds one semantic CLEAR control that proves exact `2 files / 16 tests` summaries both planted
  and restored, and
  commits exactly four governance paths. The headline walker's one-line setup correction lets
  its deleted-row plant reach A6 itself while existing A3/A4 retain strict closure.
  The landed post-state is `71/272/198`, 81 exact claims/labels, 70 in-place calls,
  56 guarded target files and 56 unique `MUTATED_FILES` rows,
  `81 CAUGHT / 1 CLEAR / 0 gaps`. The exact four-path implementation is +137/-26 and passed
  98/98 focused tests, both typechecks at 173/173 and 1,134/1,134, OSR at 1,998, and the bare
  full gate at exit 0 with 50 strict-dist files and 403/403 tests. It changes no test title,
  takes no census reservation, and leaves the whole tuple at
  `2412/365/2047/19984/5638`.
- **Historical measurement:** 2026-08-13 — **AO-5 landed.** The exact eight-path implementation
  `72b7a87b` passed its focused proof at 205/205 and the bare full gate at exit 0;
  strict dist passed 403/403 tests across 50 files and the build closed at 314 routes.
  One AO-0 scalar build and two byte-identical pure reconstructions froze the other four
  prose families at timeline `4/8/12`, pulse history `50/1286/5665`, regional log
  `2/7/201`, and pure AI Chronicle `7/7/7`, totaling `63/1308/5885` at the exact
  8,280-byte digest. The lighting census re-derived whole at
  `2412/365/2047/19984/5638`. The live manifest has 30 rows and zero dispatchable
  packets. AO-5 is terminal and the census reservation is free.
- **Historical measurement:** 2026-08-13 — **AO-5 promoted just in time.** The live manifest has
  30 rows and exactly one dispatchable packet. AO-0's executed scalar stream closes
  the four remaining families at timeline `4/8/12`, pulse history
  `50/1286/5665`, regional log `2/7/201`, and pure AI Chronicle `7/7/7`, for an
  exact total of `63/1308/5885` identities/distinct values/occurrences. Canonical
  row serialization is 8,280 bytes with SHA-256
  `8f83fa6ca2fc1411376220e55235ea392e76d98596f1bfd8c3eb52480b8469ae`.
  Regional-log zero prose is structural beside 109/109 typed audit rows, not
  vacuity; `narrativeSummary` is timeline's field, not a fifth family. AO-5 solely
  reserves the anticipated whole census `2412/365/2047/19984/5638`, subject to
  implementation-time re-derivation.
- **Historical measurement:** 2026-08-13 — **AO-4 landed.** The exact eight-path implementation
  `09e39ee6` passed its focused proof at 134/134 and the bare full gate at exit 0.
  Address totality closed at 106 identities / 400 distinct / 544 occurrences /
  14 prospective / 92 indicative with canonical SHA-256
  `82ed15a85e457d8595dcb2798f53f699210e77758a7bfa06638722d10abab694`;
  persisted-lane liveness closed at selected 151/80/95/66 + mechanical 77/24/73/23
  = union 228/83/168/69, with 26 rules = 17 active + 9 written inert and zero gaps
  or overlaps. The lighting census re-derived whole at
  `2411/365/2046/19976/5637`. The live manifest has 29 rows and zero dispatchable
  packets. AO-4 is terminal and the census reservation is free.
- **Historical measurement:** 2026-08-13 — **AO-4 promoted just in time.** The live manifest had
  29 rows and exactly one dispatchable packet. Complete final Wizard News address
  totality is 53 homes × headline/summary = 106 rows, 400 distinct values and 544
  occurrences, split 14 prospective / 92 indicative with zero blank or mixed rows;
  canonical row bytes are 12,242 with SHA-256
  `82ed15a85e457d8595dcb2798f53f699210e77758a7bfa06638722d10abab694`.
  Rewrite liveness is the occurrence-disjoint persisted public+mechanical union,
  not the overlapping consequence window: selected is 151/80/95/66, mechanical is
  77/24/73/23, and union is 228/83/168/69. The exact challenge fallback moves the
  registry from 25 = 16 active + 9 inert with one gap to 26 = 17 active + 9 inert
  with zero gaps or overlaps. AO-4 solely reserves the anticipated whole census
  `2411/365/2046/19976/5637`, subject to implementation-time re-derivation.
- **Historical measurement:** 2026-08-13 — **AO-2+3 landed as the ruled atomic train.** The exact
  immutable lineage is contract C1 `c825af92` → cure C2 `d4f213d0` → zero-bank
  C3 `f0c272e8`; only green C3 reached the shared ref. The focused proof closed at
  97/97, the bare full gate exited zero, strict dist passed 403/403, and the whole
  lighting census is `2410/365/2045/19968/5636`. The live manifest has 28 rows and
  zero dispatchable packets. AO-2+3 is terminal and its census reservation is free.
- **Historical measurement:** 2026-08-13 — **AO-2+3 promoted just in time.** CR-AO-11 compiled the
  contract and ruled cure as one coordinator-owned three-commit train: seven-path
  contract, one-path address-gated summary cure, one-path zero bank, then one old-value
  CAS. The live manifest has 28 rows and exactly one dispatchable packet. The scalar
  denominator is 272 introduced Wizard News entries / 53 homes, with 48 distinct
  prospective summary values / 53 occurrences across seven applied NPC summary homes.
  AO-2+3 is the sole census holder; one new test file reserves the anticipated whole
  five-tuple `2410/365/2045/19968/5636`, subject to implementation-time re-derivation.
- **Historical measurement:** 2026-08-13 — **AO-0 landed.** The coordinator-owned pair
  `3df85a3a` → `e71beb84` landed through one old-value CAS after a clean detached
  17-step gate. The live manifest has 27 rows and zero dispatchable packets. The pair preserves
  the default corpus at 1,321 shapes / 8,637 origins / 14,650 transitions and the
  inventory at 1,998 reads / 1,412 identities / 387 files while an opt-in scalar
  consumer observes 26,076 rows across five shipped prose families. Schema 7→8 is
  exactly 1,412 same rows and zero motion; the code-half's sole deliberate schema pin
  is cleared by genesis. AO-0 is terminal; its observed-shape and census reservations
  are free at the unchanged `2409/365/2044/19960/5635` five-tuple.
- **Historical measurement:** 2026-08-13 — **AO-0 promoted just in time.** The live
  manifest then had 27 rows and exactly one dispatchable packet. Its detached compile
  proof established the same topology, inventory, scalar figures, and zero-motion
  reconciliation later frozen by the landed pair.
- **Historical measurement:** 2026-08-13 — **H26 landed.** The coordinator-owned H26 pair
  `d081feee` → `98729f79` landed through one old-value CAS after a clean detached
  17-step gate. Schema 7 freezes 1,998 reads / 1,412 identities / 387 files with
  44 reads banked in 31 tagged rows, an exact four-entry scanner transition, 1,413
  reviewed decisions, 183/183 focused tests, and unchanged lighting census
  `2409/365/2044/19960/5635`. H26 is terminal; its observed-shape and census
  reservations are free. No product, golden, flag, tuning, dependency, ceiling or
  unrelated baseline moved.
- **Historical measurement:** 2026-08-13 — **re-derived at the H26 post-GTR re-promotion.** The coordinator-owned
  code/genesis pair `5f7e7051` → `cd404948` landed through one old-value CAS after a
  clean detached 17-step gate. The source phase passed 27,956 tests with the same 16
  attributed failures and a one-row non-run ceiling; the post-build phase passed 403/403
  tests across the exact 50-file artifact corpus with zero non-running rows. GTR-1 is
  terminal. H26 is READY and remains the sole census holder. Its nine target blobs are
  unchanged; schema 6 re-scans at 1,954 / 1,381 / 381 and five suites pass 164/164.
  GTR moved one governed scanner input, `package.json`, changing only detector provenance.
  H26's code half necessarily changes three more scanner inputs; its strict 6→7 report,
  review and bundle must prove that exact four-entry transition, byte-identical other-seven
  and unchanged unscanned-input digest while retaining exactly the ruled 31-row inventory
  movement. No product, golden, flag, tuning, dependency, or census moved.
- **Historical measurement:** 2026-08-12 — **re-derived at the H26 promotion.** MX-1, PG-1 and
  the five-file CR-AR prose batch are landed and the build tree is clean. A just-in-time
  compile found that the older accepted H26 charter and the live governed source already
  reserve schema 7 for BANK-BY-RULE; the later AO docket reused the ordinal accidentally.
  Ledger ruling `c38e9e24` preserves H26 as the standalone 6→7 additions mint and moves
  AO-0 to schema 8. The fresh governed scan measures exactly 44 re-admitted reads / 31
  per-file identity rows; the current five OSR/census suites are green at 164/164.
- **Historical measurement:** 2026-08-12 — **re-derived at the MX-1 promotion.** The previous header carried
  `0b98626f` from the GR-4c promotion and was stale by event: GR-4c LANDED at `cd2ab894`, and IA-2
  was re-statused at `c73c17ef`. MX-1 is pinned at `41732a51`.
  ⚠⚠ **AND THIS PROMOTION IS THE FIRST TO RECORD A MID-PROMOTION BASE MOVE — A FOREIGN ONE.** MX-1
  was compiled and measured at `c73c17ef`. A concurrent lane landed `41732a51` (`AO-1`, one file:
  `tests/lint/wizardNewsAuthoring.walker.test.js`) while this promotion was in flight. The packet
  was **re-pinned to `41732a51` and every load-bearing figure re-executed there**, rather than
  landing a base the implementer would not find: ancestry proved (`merge-base --is-ancestor`), the
  substrate diff `c73c17ef..41732a51` over all six of MX-1's declared paths proved **EMPTY**, the
  defect shape re-confirmed, the five effective-line figures re-measured **identical**, and the
  lighting census re-read **unmoved at `2409/365/2044/19960/5635`** because the foreign commit added
  no test title. **A base move is not a reason to re-stamp a number; it is a reason to re-execute
  it.**
  ⭐⭐ **AND ONE PROMOTION-TIME MEASUREMENT CORRECTED THE DESIGN LANE'S EDIT SHAPE.** MX-1's design
  instructed that the coding capsule's retirement field ride **two** sites — `capsuleAuthority` and
  `manifestAuthority` — *"in one edit, or neither"*. Executed at promotion, that is one of **two**
  coupling chains: `capsuleAuthority.filePaths` is **derived** from the capsule's own `fileHashes`
  while `manifestAuthority.filePaths` is **hand-built** from the packet, the two are compared
  byte-for-byte, and `assertAncestorAndSubstrate` then asserts that every declared substrate path
  is present in `fileHashes`. The widening is a **FOUR-site edit**, and the two-site instruction
  would have shipped a dispatch that throws `capsule omitted declared substrate`. **A mirror pair
  is not one site, and an instruction is not a trace.**
  ⭐⭐ **A PROMOTION MEASURED A PREDICTION FALSE AND THE RULE GOT SHARPER.** The census-holder
  section predicted that *the next promotion claims the row*. MX-1 is the first promotion that
  cannot move a census figure at all, so it takes no reservation — see the restated rule below.
  ⭐⭐ **AND THE PRIOR PROMOTION'S LESSON STILL STANDS (CR-GR4C-6 / CR-IN1B-7).** The previous
  header carried `b17d32d2` from the IN-1b promotion and was stale by event: IN-1b LANDED at
  `af3d93c8` and the tree has since moved to `0b98626f`. GR-4c was compiled at `0b98626f` and
  is pinned there — **no mid-promotion base move**, proved by an executed `git rev-parse` and
  an empty `git diff HEAD` at both the measurement and the landing.
  ⭐⭐ **AND ONE PROMOTION-TIME MEASUREMENT INVERTED A PACKET'S OWN RECOMMENDATION.** GR-4c's
  compile draft argued that its new import was *same-layer* — both files live in
  `src/domain/worldPulse/` — and therefore owed no coupling-registry row. Executed at
  promotion, the cross-layer walker resolves layers from **frozen module sets matched by
  regex, not from directories**: `treatyBreach.js` is GRAMMAR, `informationStatecraft.js` is
  INFO, and the committed baseline already records same-directory pairs as cross-layer. The
  row was **owed**, the promotion STOPPED on it, and CR-GR4C-7 minted it. **A directory is not
  a layer, and an argument is not a measurement.**
  ⭐⭐ **ONE PROMOTION-TIME MEASUREMENT CHANGED A PACKET, AND IT IS THE THING TO CARRY
  FORWARD.** IN-1b's draft carried a DELEGATED figure of *"~582 effective lines, roughly
  eighteen of headroom"* for `src/components/OutputContainer.jsx` and budgeted a six-line edit
  against it. The chair ordered the measurement EXECUTED at promotion; it reads **599 of a hard
  600 — one line of headroom, wrong by seventeen in the dangerous direction**. The promotion
  STOPPED, and CR-IN1B-7 re-shaped the wiring to cost **zero** effective lines. **The index
  header is stamped by event; git is the existence authority; and a delegated line count is not
  a measurement.**
- **Packet law:** [`PACKET_STANDARD.md`](./PACKET_STANDARD.md)

## Dispatch rule

Only a row marked **READY** may be dispatched, and only after its packet's
preflight still matches the verified base. A design document, queue row, commit
subject, stale progress block, or old brief is not a coding assignment.

This index deliberately exposes only the next bounded product slices plus any
explicitly owner-authorized infrastructure packet. It is not a transcription of
every designed wave. TC-3a and SC-1 are independent lanes; TC-3b depends on TC-3a
and is not a parallel lane; the blocked
foreign-policy rows do not hold them. When a packet lands, the coordinator
re-derives that lane and decides whether to compile its immediate dependent.

## Current packet set

| Dispatch | Packet | Status | Why this is the boundary | Explicitly excluded |
|---:|---|---|---|---|
| MF-T2L | [`MF-T2L`](./packets/town-cartography/MF-T2L.md) | **LANDED**; the chair CASes the TE-MAPSTACK-LANDING tip (stacked landing, ODQ §461.2) | Discharge ODQ §287.5's vegetation sentence WHOLE — *"a durable selectable trunk/canopy mass plus a separate deterministic nonpersistent instance field"* — as ONE dormant leaf, `vegetation.js` (149 eff against the ODQ §425-ratified 230-per-leaf cap). The plan's own law for this member is **both halves or neither**: a member that lands only the durable mass has landed half a contract and made the other half harder. ⭐⭐ HALF ONE, THE MASS: trunk and canopy are REAL landed `solidPartQ` volumes on ONE derived support key, and SPEC §10.6's support-truth law — *"Support truth rejects any consumer or serialized cache that gives trunk and canopy different ground authority"* — is a structural impossibility rather than a check. The caller's `supportSurfaceRef` record is **RE-DERIVED through MF-T2K's own constructor** and refused naming BOTH keys if it disagrees, so a hand-built alias wearing K's record shape cannot pass; the one derived key is threaded into both solids and neither sub-record may carry a key of its own. §10.5's `trunk.partId === canopyAnchor.trunkPartId` identity law is refused naming both ids, and the canopy's vertical base IS the anchor value — derivation rather than validation, so the mismatch cannot be constructed. ⭐⭐ HALF TWO, THE FIELD AND ITS BEHAVIOUR: the KNOWN-COUNT arm of §10.6's materialisation law over the foundation's own published rectangle region law, plus the derivation that makes *"nonpersistent"* a behaviour instead of an adjective — instances regenerate from `(fieldKey, instanceIndex)` alone by `hash32` of a VERSIONED basis folded by integer modulo, so containment in the half-open region holds BY CONSTRUCTION and the record carries **no id field of any kind**, which is what makes it unselectable and unnameable by a receipt. ⛔ R-MF-2 answered head-on: the leaf multiplies NO quanta, its only coordinate arithmetic is `minX + (uint32 % widthQ)`, and it says so in its own docblock. ⭐ ZERO free parameters in the derivation — no spacing, density or jitter dial exists — which is what keeps this member off THE PROMISE's owner-signature surface. ⭐ A DESIGN-IMPLEMENTATION MINT, NOT A PORT, and that was measured with a firing control: four greps over the sealed W3 tip return ZERO vegetation modules, because the sandbox's entire tree surface is presentation. The leaf imports exactly `foundation.js`, `massPart.js`, `supportSurface.js` and `fabricRng.js`, a constraint forced by MF-T2J's frozen `FOUNDATION_READERS`. ZERO existing production files move | the `CanonicalVegetationAggregate` and its whole contract — authority arms, population dispositions, density and rounding laws, subdivision/conservation/mixed-stand laws, `SINGLE_FIELD_EXACT_REGION` — which bind field↔aggregate PAIRS and arrive with the aggregate member; a DERIVED `fieldKey` (ODQ §425 ruling 2 — the SPEC's `DerivationKey` derives from refs that do not exist, and a coordinate-derived stand-in would be determinism theatre; the MF-T2E precedent replayed, with the flip owned by the aggregate member); the density arm; `CanonicalVegetationPhase`, `VegetationObservation`, opacity bands and any phenology MOTION; promotion of an instance to a mass (§10.6 routes it through a declared operation, and that seam is §299.3c-blocked); epoch fields; `closedShell`/`SolidQ`, `receiverSurfacePatchIds`, a `bounds` cache and `offsetXYQ` (its shell-agreement law needs a ring reference point the era cannot define); any polygonal region or point-in-polygon geometry; any `artifactKind`/`schemaVersion`/`lawVersion` stamp or persistence seam; any consumer wiring or barrel edit; `heightQ()` motion, the y-axis flip, any arrangement-quantum rung or tuning constant; a dependency bump; any new `tests/lint/**` file; and BOTH shared walker `changeManifest` rows, deferred to the landing slot under the §417 standing law |
| MF-T2K | [`MF-T2K`](./packets/town-cartography/MF-T2K.md) | **LANDED**; the chair CASes the TE-MAPSTACK-LANDING tip (stacked landing, ODQ §461.2) | Mint the two remaining §287.16 vocabulary items this family's charter routes here — explicit-input support-relative solids and component slots — as ONE dormant leaf, `supportSurface.js` (182 eff against the ODQ §419-ratified 200-per-leaf cap). It TYPES the support surface MF-T2E left as a bare id string, DERIVES the owner-qualified key MF-T2F's `DIFFERENT_SUPPORT_SURFACE` refusal consumes, makes SPEC §10.5's acyclicity law executable, and mints the total PRESENT/NONE/UNKNOWN component-slot axis of SPEC §6.4.1. ⭐⭐ THE CENTRAL BEHAVIOUR: `surfaceId` is DERIVED and a caller-supplied one is REFUSED, because §10.5 requires *"a deterministic owner-qualified key, not a free alias"* and today every caller hand-writes exactly that alias. ⛔ INJECTIVITY IS BOUGHT BY GRAMMAR, NOT BY A SEPARATOR BYTE, and the reason is measured rather than stylistic: the derived key must pass the landed id wall `/^[a-z0-9][a-z0-9:._-]{0,95}$/`, which admits no C0 byte, and `controlBytes.test.js` forbids the raw byte in source — so MF-T2Bf's U+001F cure is unavailable and `:` is RESERVED as the separator instead, with every segment validated against the id grammar minus `:`. The engineered near-collision pair is pinned UNCONSTRUCTIBLE rather than merely unlikely. ⭐ A DESIGN-IMPLEMENTATION MEMBER, NOT A PORT-BY-HASH ONE, and that was measured: four greps over the 54 sealed fabric modules return ZERO support-registry, slot or role machinery, so there is nothing to port. ⭐ THE UNKNOWN VOCABULARY IS IMPORTED FROM `massPart.js` RATHER THAN RE-DECLARED — the SPEC lists are byte-identical and same-ordered, so one vocabulary makes drift structurally impossible — and the leaf imports EXACTLY `foundation.js` and `massPart.js`, a constraint forced by MF-T2J's frozen `FOUNDATION_READERS` roster. ZERO existing production files move | support CONNECTIONS and any cross-support licensing (ODQ §419 ruling 1 — the refusal stays a refusal; a connection resolves host, support and lifecycle through the receipt/operation seam this era cannot validate); `MAINTAINED_FREE_SPACE` CONSTRUCTION (the kind ships in the closed seven per ODQ §419 ruling 4, and its arm refuses naming the §299.3c-blocked operation substrate); `EXTERNAL_MASSING_PHASE`; any registry — ground-surface, datum, massing-phase or material; `localDatumCache`, which needs a height query; the slot schema-conformance law, which has no substrate; renaming `CODEX_SLICE_MODULES`, which rides MF-T2L's landing act (ODQ §419 ruling 2); vegetation records (MF-T2L); architectural-history operations (MF-T2S/MF-T2M); any `artifactKind`/`schemaVersion`/`lawVersion` stamp or persistence seam; any consumer wiring or barrel edit; `heightQ()` motion, the y-axis flip, any arrangement-quantum rung or tuning constant; a dependency bump; any new `tests/lint/**` file; and BOTH shared walker `changeManifest` rows, deferred to the landing slot's staged promotion under CR-HB2B-SPLITP |
| MF-T2M | [`MF-T2M`](./packets/town-cartography/MF-T2M.md) | **LANDED**; the chair CASes the TE-MAPSTACK-LANDING tip (stacked landing, ODQ §461.2) | Land the §287.16 architectural-history half of the massing charter — the GENERATION-SPEC's twelve-kind dated-operation PAYLOAD grammar plus its deterministic fixture — as ONE dormant leaf, `architecturalHistory.js` (210 eff against the ODQ §426-carried 220-per-leaf cap), sealed through the fabric's ONE artifact grammar under the `ARCHITECTURAL_HISTORY_OPERATION` kind the SPEC's own union already names, so the member JOINS the grammar rather than minting beside it. ⭐⭐ AND IT EXECUTES THE §423 SHAPE-B STRIP in the same scope the ruling put it: `fabric/operations.js` DEMOTES from door-claimant to payload compiler — the `FANTASY_CANON_GATE` export and all six use sites out, the artifact kind renamed `CANONICAL_`→`PROPOSED_SPATIAL_OPERATION` so proposal-grade and accepted-grade stop sharing a word, two law versions re-stamped because those two SHAPES lost a field, one barrel row removed. ⛔ THE DEMOTION REMOVED A CLAIM, NOT A CHECK, and A6 proves every survivor positively. ⭐ THE PAYLOAD LEVEL IS THE WHOLE BOUNDARY: no gate, no verb, no executor, no receipt, no store row — its artifacts are PROPOSALS until a registered store verb accepts them (§423(3), the executor tranche). ⭐ ITS STRUCTURAL PREVENTION ARM makes §423.1's Option-D law executable: no fabric module may export an acceptance gate, anchored on the export-declaration grammar because `stageManifest.js` lawfully carries `'gate'` as a §10.14 field name and a `watergate` random namespace — both proved INERT against the matcher in the test itself, after the matcher is proved live on a plant. ⚠ EXACTLY ONE test literal moves (a v1-mechanism seal digest), proved by execution rather than predicted; the six projection pins beside it rode non-fantasy paths and passed unmoved in the same run. ⚠ THE FOUR-RED `validate:packets` WINDOW at DRAFT/READY is the §379.2 discharge's designed shape, quoted verbatim, proved whole by a strip-reverted control reading FULL GREEN, and BLESSED at ODQ §426(4) | any store motion — registry rows, verbs, ActionResult/outbox bytes (§423(3) keeps the door verb with the executor tranche); the executor, any after-state, the `ArchitecturalHistoryReceipt`, the before→after diff-equality law and every kind's state transition; a `TimeKey` structure, epoch derivation or any chronology validator; provenance-ledger or registry RESOLUTION (both refs are validated structurally) and the SPEC's optional `coverageRef`, deliberately absent because it names an evidence registry this era cannot resolve; `RURAL_CHANGE`, `CoreOperationKind` rows and any AMP-promoted operation (owner-gated behind D3b); any `mapEdits`/document-inverse path (§10.13); a barrel row for the new leaf; any persistence seam or seed byte; `heightQ()` motion, the y-axis flip, any arrangement-quantum rung or tuning constant; a dependency bump; any new `tests/lint/**` file; the standing `scripts/mutation-sweep.sh` row for the new arm, deferred per §417 and ODQ §426(5) because MF-T2J reserves that path; and BOTH shared walker `changeManifest` rows, deferred to the landing slot's staged promotion under CR-HB2B-SPLITP |
| MF-T2J | [`MF-T2J`](./packets/town-cartography/MF-T2J.md) | **LANDED**; the chair CASes the TE-MAPSTACK-LANDING tip (stacked landing, ODQ §461.2) | Port §287.8 / SPEC §10.14's executable S0–S23 stage manifest as ONE dormant leaf, `stageManifest.js` (219 eff of the 400 cap), and make it executable in THIS tree rather than in the tree it measured. ⛔⛔ THE PORT'S CENTRAL FACT: the sealed manifest declared one datum and re-derived the rest from a 54-module sandbox fabric; this directory holds 27 modules of which 5 are named by the record and 49 record modules do not exist here, so the table is carried as PUBLISHED DATA and the checking splits in two — the record against itself (an independently written Tarjan and every projection re-derived, in `tests/domain/townMapStageManifest.test.js`) and the record against the LANDED sub-tranche (imports, key spellings and `fabricRng(` fork sites re-parsed with acorn, in `tests/lint/townMapStageManifest.walker.test.js`, a set that grows with every later port member). ⭐⭐ THE HEADLINE RESULT TRAVELS AND IS PRESERVED RATHER THAN REPAIRED: the module import graph is acyclic and the STAGE graph is not — one strongly-connected component spanning S2·S3·S4·S6·S13, closed by two named imports — and the member MEASURED something the sealed record does not publish, that the feedback set is MINIMAL: cutting either inversion alone leaves a different, still-cyclic residue, so a "cleanup" reds against a named component rather than a count. ⛔ AND ITS SHARPEST PORT FINDING: the sealed "FOUNDATIONS has no outbound edge" arm PASSES here for the wrong reason, because the derivation skips unassigned files and four of `coordinateAbi.js`'s seven live readers are codex modules — so the live arm is replaced by a frozen reader roster read from the WHOLE directory (§P2.13). Sealed equivalence is total: 10 values, 5 nullary functions, both `topologicalNodeIds` modes, 25 `stageOrderIndex` and 56 `nodeOfModule` rows, ZERO differences | wiring any consumer; any barrel row (none exists, on purpose); re-deriving the record for this tree, which would retire the SCC finding; carrying the sealed arm 10 as a live law; moving `lawVersion`, `artifactKind`, `schemaVersion` or `COORDINATE_ABI_VERSION`; persisting anything; repairing the stage-graph SCC, which is W4's work; editing the sealed tip (R-MF-4); `heightQ()` motion, the y-axis flip, any arrangement-quantum rung or tuning constant; a dependency bump (`acorn` is already an explicit devDependency, so the sealed walker's landing hazard is discharged by inspection); and the shared census-walker `changeManifest` row, deferred to the landing slot's staged promotion under CR-HB2B-SPLITP |
| WEB-5 | [`WEB-5`](./packets/website/WEB-5.md) | **LANDED**; do not redispatch — built and gated by TE-WEB5 at `19b799ce`, minted retrospectively at the terminal status per §410 so it reserves no change path against the sibling census-holder; the ref move is the chair's CAS | Car 5 of the `website` train, closing the owner-gated question §66.4 recorded and §359.10 opened: is "Import my data" a reset or a restore? THE SURFACE IS THE PROVENANCE. A gallery clone is a stranger's world and keeps the distrust reset byte-identically; an account file is the user's OWN exporter-produced estate, so `campaignState.phase`, `campaignState.eventLog`, `versionHistory` and `aiData` carry through, admission-walled PER FIELD and fail-closed PER FIELD against the live `saveAdmission` wall every read path already runs — a field our own validator cannot read falls back to the reset value for THAT FIELD with a per-item notice by settlement name, never a silent partial and never a whole-file refusal for one bad field. ⭐ THE EXPORTER BRANCH WAS MEASURED, NOT PREDICTED: `preflightAccountExport` places `savedSettlements` into the payload verbatim and the metadata projection that would strip the four fields has zero consumers, so the envelope stays at **v4 with zero exporter motion** — no version bump, no byte-budget change, no migration, no RPC, no persisted-shape motion. ⭐ RESTORE IS OPT-IN and that shape is load-bearing: `prepareSettlementEntry` has exactly two callers, and the second is the reconciliation slice whose own admission machinery DECLARES these fields unsupported — a default-ON restore would have silently rewritten another slice's contract, so the default stays the reset and a live arm proves it. Cross-settlement wiring is re-addressed intra-envelope only, in the second pass that already re-addresses the founding parent ref: an edge survives only when every save id it carries — including the canonical direction in `relationshipFrom`/`relationshipTo`, which the build found are save ids too — resolves to a settlement that landed from the same file, so no foreign id reaches the store even transiently and a dangling endpoint rebuilds empty. 208 effective production lines across three existing files, zero new leaves, zero new test files | any edit to `accountData.js` or `accountTransferContract.js` (the measured branch makes both zero-motion), any edit to `galleryImportSettlement.js`, whose distrust posture is preserved and pinned by an anchored absence arm, any edit to `importReconciliationAdmission.js` or `saveAdmission.js` (consumed, never changed), restoring `neighborRelationship` (matched BY NAME at the save boundary, so it would bind an import onto an unrelated same-named save), making `serviceRecords` importable, any server-side shape motion — column, RPC, migration or trigger, any ownership or publication field crossing either surface, any new UI surface beyond the existing notice channel, any new test file, any `tests/lint/**` re-record, any baseline, ceiling or golden, and a dependency bump |
| WEB-1 | [`WEB-1`](./packets/website/WEB-1.md) | **LANDED**; the chair CASes the TE-WEB1 tip | WEBSITE family, first member (train `web-a`, car 1). §359.6's person-adjacent half: consent model v2 → v3, so `research` returns to opt-IN alongside `ai_prose` and `market` while `essential` stays ON-unless-DNT (chair C1). `CONSENT_KEY` is NOT bumped and `updatedAt > 0` still honours a recorded choice verbatim, so a v2-era explicit opt-IN survives the flip. Migration 197 is the train's FIRST to update existing `telemetry_consent` rows, under a CONJUNCTION — `v` absent AND zero `consent_change_records` — because neither provenance signal subsumes the other: 194's trigger records only CHANGED keys while the `v` stamp lands wholesale, so a no-op save stamps `v` with zero records, and the trigger defaults its version to 1 when `v` is absent, so a non-mirror write leaves records on an unstamped row. Under-flip is bounded by an executed guard: the only writer is `pushTelemetryConsent`, whose only caller is the PrivacySettings toggle handler, and the sign-in path selects-and-narrows locally without ever calling the RPC. 197 takes its own reviewed rehearsal wave rather than extending `simulation-metrics-storage`, and classifies `documented-manual-reversal` from its own `@rollback` note while the wave declares forward-only | the anonymous half of §359.6 (`world_sim_metrics` is PII-free by schema and gated by nothing — VERIFIED here, never modified), O3 the retroactive purge of already-captured research rows (data deletion, owner-gated), any `CONSENT_KEY` or `EVENTS_REV` bump, any PrivacySettings toggle-label rewording (copy is the owner's), any edit to `PrivacySettings.jsx` itself (byte-unchanged — it renders whatever `getConsent()` returns), the retention numbers (WEB-2), and any `package.json` byte motion |
| MF-T2H | [`MF-T2H`](./packets/town-cartography/MF-T2H.md) | **READY**; dispatched to lane TE-T2H, HOLDING for GO | Port §287.4's spatial-receipt seam and the seeding law it draws from: two dormant leaves, `spatialReceipt.js` (137 eff) and `fabricRng.js` (77 eff), 214 effective production lines against the 400 cap. The seam makes three rules UNREPRESENTABLE rather than forbidden — a receipt is dated for LATER, the dependency roster is closed by kind and temporal branch IN ORDER, and a no-effect result is a separately typed diagnostic — and builds its digest from a NAMED FIELD ROSTER rather than from the object, which is what makes §10.1's refusal structural. ⭐⭐ ONE DECLARED DIVERGENCE FROM THE SEALED SOURCE (ODQ §390.1): every composite preimage is LENGTH-PREFIXED, so the non-injective dependency join is cured — and so are the three identical seams beside it, each with an executed collision witness BOTH ways plus a negative control that reads distinct at both spellings. Sealed equivalence is proved where the divergence does not reach: every non-digest field, all five vocabularies, `fingerprint` and all twelve refusal paths byte-identical, and 64 of 64 `fabricRng` rows reproducing the sealed stream. ODQ §390.2 answered by execution — this member PERSISTS no `canonicalBytes` result, so `COORDINATE_ABI_VERSION` holds at 1 and the trigger passes forward. ODQ §390.3 did not fire from the port (0 raw C0 bytes in either sealed source) but fired TWICE from the lane's own authoring and was caught before any gate ran | wiring any consumer; any barrel row (none exists, on purpose); `legacySubstrateForkKey`, deferred to the `substrate.js` member; moving `COORDINATE_ABI_VERSION` or any `artifactKind`/`schemaVersion` stamp; persisting any digest; touching `coordinateAbi.js`, `index.js` or any other landed file; curing the sealed tip itself (R-MF-4 — the four seams remain open there, RAISED-1); widening the control-byte ratchet or the nondeterminism scan (RAISED-2); `heightQ()` motion, the y-axis flip, any arrangement-quantum rung or tuning constant, a dependency bump, and any new `tests/lint/**` file |
| MF-T2G | [`MF-T2G`](./packets/town-cartography/MF-T2G.md) | **LANDED**; do not redispatch | Discharge ODQ §299.3(b) by STRIPPING the §288-nonconforming published light profile: the `PROJECTION_LIGHT_PROFILE` record, its `light:fixed-survey:v1` identity, its one publication field inside the sealed `FIRST_SLICE_PROJECTION` and its barrel export row are deleted, so no published projection carries a dimensional light profile that lacks §288.2/§288.3 content and the conforming fixed survey arrives with D4+ light through §288 own contract or not at all. Net -10 effective across two production files, nothing built. The SHADOW draw-ops and their two module-internal constants are PRESERVED byte-identically (ODQ §375 ratified the record-vs-shadows boundary) and the primitive census, draw ops, artifactIds, save bytes, warnings and audience law are byte-identical through the strip. The declared one-time shift is twelve digest literals across three test files moving by contentHash ONLY, each value captured at the verified base and proved key-by-key with zero pins added and zero removed | building any §288-conforming profile content (source, azimuth/elevation, receiver separation, clamp law, provenance) which is D4 scope under §288.8 safe order, any edit to the shadow machinery or its constants, any edit to `massingProjection.js`, `content.js`, `coordinateAbi.js` (MF-T2Bf owns it) or `foundation.js`, any landed packet record quoting the old digests, any artifactId/draw-op/primitive/save-bytes/seed motion, any consumer wiring, any new record or stamp, any arrangement-quantum rung or tuning constant, the y-axis flip, `heightQ()` motion, a dependency bump, and any `tests/lint/**` file other than the census re-record |
| CT-1A | [`CT-1a`](./packets/content-train/CT-1A.md) | **LANDED**; the chair CASes the TE-CT1a landing | Car 1a of the `content` train, and the first corpus member that answers why a town has the SHAPE it has rather than what its condition IS. ⭐ THE GAP IS MEASURED, NOT ASSERTED: the dossier estate carries 58 state blocks and 78 causal join families, some 2,600 authored variants, and CT-0 found that not one of them explains a wall, a market, a founding or the ground. Four new blocks close the engine-known part of it: `DS-GEN-12` (the ground disposes the town, over a TOTAL partition of the seven-value terrain vocabulary), `DS-GEN-13` (the market and the roads, as orientation and custom, never as map geometry), `DS-GEN-14` (founded once, grown since, record-gated by the typed founding row) and `DS-DEF-11` (why the wall, and why not, over threat, tier and the generator's own upkeep gate, whose asymmetry between stone and wages is an engine rule rather than an authored flourish). ⭐⭐ ADDITIVE-NEW-BLOCK IS THE WHOLE DESIGN AND IT IS LOAD-BEARING: `drawVariant` hashes `seed::blockId::poolKey` and indexes `% eligible.length`, so appending to an existing pool would move EVERY seeded draw over it while a new block moves nothing. Measured key-by-key as 4 ADDED / 0 REMOVED / 0 CHANGED, with five of the seven leaves byte-identical and the goldens run both ways at 104 files / 707 tests. ⛔ THE INSTRUMENT CT-0 DID NOT PRICE: `voiceMechanics` Tier 2 scans `src/data/**`, so the generated prose leaves sit inside a per-file em-dash pin that is RED-AND-BANKED. Four block titles in the house style would have deepened banked debt while every gate figure held still; the headers use a middle dot instead, and the counts are measured unchanged in both directions. ⚠ WHY, NEVER WHEN: the engine holds no wall year, founding date or construction epoch at this tip, re-verified by exhaustive grep, so wall age stays a CT-1b entry and the absence is recorded in the block itself. ⚠ The blocks land DARK; composer wiring is its own lane. ⚠ This cell is prose, and the validator reads the STATUS column literally: keep status words out of it | CT-2 and CT-3, which are separately compiled; any amendment to an EXISTING pool, including `DS-GEN-6` and `settlementOriginProse.js`, which render live and are a declared-shift owner-signed class; Tier-B townMap derivations (`siteGenesis` site kind, `institutionAssignment` placements, `asymmetrySources` market placement) pending CT-0 R-2; category-affinity placement prose pending OB-5; any new census row, rendered surface, desk reader, composer wiring, flag, persisted field, generator edit, new test file or new suite; and every golden, tuning value, dependency, ceiling and unrelated baseline |
| CT-2 | [`CT-2`](./packets/content-train/CT-2.md) | **LANDED**; the chair CASes the TE-CT2 landing | Car 2 of the `content` train, and the chapter where law 2 does the most work, because the subject is change over time and that is exactly where an unchecked writer invents a history. Three new blocks: `DS-POP-3` (the direction of the roll read against the approach), `DS-GEN-15` (the fabric wears it, over the urban-fabric mirror's dated scars, rebirths and drift) and `DS-GEN-16` (what the years left standing, keyed on `anchored`). ⭐⭐ THE THREE REFUSALS ARE THE MEMBER: the ratified draft's cause clauses are NOT licensed by the estate's own standing rulings, and each refusal is recorded in the block that would have carried it. `R-DST-B`, the annex's central law, rules a configuration field licenses STRUCTURAL cause and never historical, and names the prosperity rung among fields carrying no provenance at all; `DS-POP-2` rules the same for the trend band, in terms ("a variant explaining why the town is thinning would be inventing history"); `DS-POP-1` already owns the movement that produced the roll, because the migration ledger and the demographic receipt ARE provenance. So `DS-POP-3` says what direction and approach MEAN together and never where a person came from, and CT-0's third fact (prosperity) is deliberately not joined. ⛔ `DS-GEN-9` rules `anchored` the causal gate and calls it the sharpest line in the shape and the easiest to blur, so `DS-GEN-16` is BUILT around that gate and carries a `RECORDED-UNANCHORED` pool whose job is to give the unanchored case somewhere lawful to go. ⭐ THE NO-MOVEMENT PROOF IS TIGHTER THAN CT-1a's: one leaf touched, SIX byte-identical, key-by-key 3 ADDED / 0 REMOVED / 0 CHANGED, and the goldens run both ways produce logs that are identical line for line once timing is stripped. ⛔ A RETRACTION CARRIED IN THE MEMBER (ODQ §401): this lane briefly asserted in `DS-POP-3`'s RECEIPT that `DS-POP-2`'s title named a function that did not exist. It DOES exist — `populationTrendBand` is exported from `src/domain/display/trendLens.js` with a signature matching the title exactly — so the claim was removed and `DS-POP-3` re-keyed onto that same display-layer reader, which also puts both POP blocks on ONE band per `R-DST-A`. The cause is the lesson: the search behind the claim was a `grep` piped into `head` and the hit sat below the cut, so an in-prose ABSENCE claim owes the same untruncated tree-wide receipt as an absence pin. ⚠ `{calamity}` is MINTED here, not borrowed as the draft claimed. ⚠ The blocks land DARK; composer wiring is its own lane. ⚠ This cell is prose, and the validator reads the STATUS column literally: keep status words out of it | CT-1b and CT-3, which are separately compiled; ribbon suburbs, faubourgs, unequal gates and circuit epochs, which CT-0 marks CT-1b and Tier C; any amendment to an EXISTING pool or block, including `DS-POP-2`'s title; any population-trend surface or selector, which `R-DST-K` still forecloses; Tier-B townMap derivations pending CT-0 R-2; category-affinity placement prose pending OB-5; the OSR mint, which this lane does not perform; and any new census row, rendered surface, desk reader, composer wiring, flag, persisted field, generator edit, new test file or new suite |
| CT-3 | [`CT-3`](./packets/content-train/CT-3.md) | **LANDED**; the chair CASes the TE-CT3 landing | Car 3 of the `content` train, and the chapter on why a town keeps the institutions it keeps. Three new blocks: `DS-GEN-17` (the company the town keeps, the composition booleans read as civic CHARACTER rather than as the defense CAPABILITY `DS-DEF-6` reads from the same rows), `DS-GEN-18` (why these workshops, the supply cascade read only at the joins the chain rows record) and `DS-FTH-4` (why the temple holds its ground, over standing, tenure and endowment). ⭐⭐ THE STATE KEYS ARE TOTAL AND DETERMINISTIC, WITH THE PRECEDENCE WRITTEN INTO THE ANNEX: one principle, most-constrained-antecedent-first, stated once and applied three times, because a key exists to say what is DISTINCTIVE and an antecedent almost every town satisfies distinguishes nothing. Every residue is explicit: `DS-GEN-17` carries a PURE else-arm and the other two carry a stated `R-DST-K` silence. ⛔ `BARE`'s tier clause is DROPPED, because a tier-gated else-arm is not total: a high-tier town carrying none of the four antecedents would resolve to no key at all. ⛔ `DS-FTH-4` inverts the chapter principle ONCE, on TRUTH-PRESERVATION: `CONTESTED` outranks `ROOTED` because `ROOTED` asserts an unquestioned hold that a live rival niche falsifies, and a precedence order exists to stop the corpus asserting what its own receipt contradicts. ⭐ THIS CHAPTER MINTS NO SLOT, a first for the train: the compile declared three mints and §0c already carries every fill, `{institution}` covering both the processing house and the temple. ⛔ THE ONE REAL ENGINE FINDING: `templeWealth` is ARCHITECTED AND NOT BUILT (WF-7), absent from `src`, `scripts` and `tests` and verified absent by the estate's own design rows, so `ENDOWED` is authored, MARKED and NOT DRAWABLE until WF-7 lands, on `DS-FTH-3`'s own convention. ⚠ WF-8A disjointness is made STRUCTURAL, not behavioural: `patronFalls[]` is REMOVED from the receipt, so no pool can narrate a fall and `DS-FTH-1`'s four FALL_SENTENCE strings stay disjoint. ⭐ FIRST CAR TO TOUCH TWO LEAVES (`DS-FTH-` routes to warFaith), so the proof reads five byte-identical leaves, key-by-key 3 ADDED / 0 REMOVED / 0 CHANGED, and ZERO pre-existing pools changed length. ⛔ A LANE-INTRODUCED DEFECT, FOUND AND CURED: `slotsOn()` harvests every brace token on the SLOTS line with no note-stripping, so a slot merely QUOTED in an aside there is DECLARED, and nothing reds because the contract counts only used-but-undeclared. Corrections now sit on their own lines and the refused spellings are written WITHOUT BRACES. ⚠ The blocks land DARK; composer wiring is its own lane. ⚠ This cell is prose, and the validator reads the STATUS column literally: keep status words out of it | CT-1b, which is separately compiled; any amendment to an EXISTING pool or block, including `DS-FTH-3`, whose patron-fall enumeration lists five causes where `PATRON_FALL_CAUSES` is frozen at four; any siting, precinct geometry, processional route or adjacency claim, which OB-5 and CT-1b hold; any narration of a patron fall, which `DS-FTH-1` owns; drawing `ENDOWED` before WF-7 lands `templeWealth`; the OSR mint, which this lane does not perform; and any new census row, rendered surface, desk reader, composer wiring, flag, persisted field, generator edit, new slot, new test file or new suite |
| MF-T2F | [`MF-T2F`](./packets/town-cartography/MF-T2F.md) | **LANDED**; do not redispatch | Land the §287.5 predicate home app-side — *"exact solid intersection, not XY/Z interval overlap, owns volume legality"* — on the real vertical intervals MF-T2E minted, and cure the carried §348.2 conviction in BigInt while doing it. Two dormant leaves: an exact shared-area core (BigInt ear-clip triangulation closed by an exactness IDENTITY rather than a tolerance, homogeneous-coordinate clipping, rational area) and the ported D1 contract answering `REFUSED` / `PLANAR_ONLY` / `VOLUME` with every decision AND every published magnitude exact, so the R-MF-2 class cannot invert a verdict on legal ground — executed: the sealed float spelling answers `DISJOINT_OR_ABUTTING` where the exact answer is `OVERLAPPING`, and drifts a two-building shared volume by 1,784 quanta³ at ordinary scale. The forbidden interval predicate rides along as a falsifiable negative control and the dual-run turns its wrongness into counts. ⛔ The `operations.js` collision-by-identity site is NOT edited and NOT convicted; it keeps its uniqueness role. ZERO existing production files move | any consumer wiring — operations, rosters, generation, projection (the operation-path consultation is the named successor, soak-owing and behind MF-T2S); any edit to `exactGeometry.js` or its float spellings; `SupportSurfaceRef` typing and support connections (MF-T2K); `heightQ()` motion, world-unit conversion or height publication; any `artifactKind`/`schemaVersion` stamp or persistence seam; any barrel edit; any epsilon, tolerance, quantum or tuning constant of any kind; a dependency bump; and any `tests/lint/**` file other than the census re-record |
| MF-T2E | [`MF-T2E`](./packets/town-cartography/MF-T2E.md) | **LANDED**; do not redispatch | Mint the typed functional-volume vocabulary and the quantized vertical truth ODQ §287.16 charters and SPEC §10.16(3) names by name: the three closed SPEC-verbatim vocabularies (functional volume, morphology role, unknown reason), a total KNOWN/UNKNOWN knowledge axis whose UNKNOWN arm carries no value at all so the no-inference law is structural rather than advisory, a half-open `[baseQ, topQ)` vertical interval on a NAMED support surface refused through the landed ABI wall, and frozen `SolidPartQ`/`MassPartQ` records carrying the exact `{kind:'INTERVAL', baseQ, topQ}` discriminant MF-D1's legality predicate already admits — executed read-only against the sealed predicate, which reads the stacked pair as `sharedHeightQ 0 → DISJOINT_OR_ABUTTING`. The footprint degeneracy check is BigInt, internal and unexported because the tree's float spelling reads zero area on valid ground at ABI scale. ZERO existing production files move | any overlap, interval-intersection, shared-volume or legality predicate (MF-T2F ports the one predicate home), `SupportSurfaceRef` typing and component/material slots (MF-T2K), vegetation records (MF-T2L), architectural-history operations (MF-T2M, chair-blocked), roofs, floor programs, the functional PROGRAM axis, origins, closed shells, upper envelopes, provenance and content hashes, any `artifactKind`/`schemaVersion` stamp or persistence seam, `heightQ()` motion, world-unit conversion or height publication, any consumer wiring or barrel edit, any tuning constant, a dependency bump, and any `tests/lint/**` file other than the census re-record |
| MF-T2D | [`MF-T2D`](./packets/town-cartography/MF-T2D.md) | **LANDED**; do not redispatch | Land the boundary noder: the arrangement stage the landed planar embedder's contract requires and nothing app-side supplies. Explicit ABI-quanta segments in, an atomically-noded boundary set the kernel admits verbatim out — proper crossings split at snapped intersections, endpoint touches noded, collinear overlaps decomposed and deduplicated, endpoints snapped to the first quantum-ladder rung that nodes clean, with the rung, the pass count and every residual published as data. The snap/split/dedupe passes, the grid bucketing, the pass cap and the ladder walk port VERBATIM from the sealed sandbox tip; the CUT PREDICATE diverges by ruling (ODQ §334.2) to the landed kernel's own admission predicate, exact, inverted into cuts, because four executed witness classes show the sandbox letter's output being refused by that kernel. ZERO existing production files move | the codex arrangement compiler and its sealed artifact (§334.5 — retirement is §312.2c's dual-run act), the codex census and parcel registry, the fabric-extraction half, any consumer wiring or barrel edit, any `artifactKind`/`schemaVersion` stamp, any arrangement-quantum rung choice or tuning constant, real-leaf noding claims, rational-exact split points, the y-axis flip, a dependency bump, and any `tests/lint/**` file other than the census re-record |
| MF-T2C | [`MF-T2C`](./packets/town-cartography/MF-T2C.md) | **LANDED**; do not redispatch | Extend the landed planar embedder onto the versioned ABI: exact BigInt orientation so the kernel stops throwing a false angular tie on the coordinates MF-T2B's widened wall already admits, the pre-sorted traversal cursor, one outer face per connected component, zero-area cycles typed `DEGENERATE` rather than thrown, the declared hole-capable face record, and point location under `boundaryRule: 'CLOSED'` — with the sealed PLANAR_DCEL artifact and all 157 literal digest pins byte-identical, guarded by a legacy face-triple projection at the seal | the sealed-shape schema shift, any consumer wiring or barrel edit, the noder and arrangement changes (MF-T2D), containment filling of `innerBoundaryHalfEdgeIds`, any reachability claim for `HOLE_CYCLE` or `ENCLOSED_BY_EXTERIOR`, an arrangement-quantum rung or any tuning constant, the y-axis flip, `heightQ()` returning a value, a dependency bump, and any `tests/lint/**` file |
| MF-T2Bf | [`MF-T2Bf`](./packets/town-cartography/MF-T2Bf.md) | **LANDED**; do not redispatch | Restore the four U+001F field separators MF-T2B's port dropped from `canonicalBytes`, so the artifact-digest preimage is INJECTIVE again. Without them the four tagged fields run together and the tags are the only delimiters — and a tag can be spelled inside a field value, so distinct (kind, schemaVersion, deps, body) tuples encode to identical bytes. Measured rather than argued: three engineered tuple pairs collide 3 of 3 at the verified base and 0 of 3 with the member, while a negative control of two plainly distinct tuples reads apart in BOTH readings, so an after-run reporting no collisions cannot be a witness that stopped comparing. ⭐⭐ THE SPELLING IS THE DEFECT'S OWN CAUSE: `tests/lint/controlBytes.test.js` forbids a raw C0 byte under `src/**` or `tests/**`, so the port could not copy the sealed bytes and had to RE-SPELL them — and dropped all four with nothing on screen to show for it, because the character does not print. This lane's first cure wrote raw bytes, that walker convicted it, and the cure now uses the escape spelling with the source-text divergence DECLARED and the OUTPUT proved identical by evaluating both function bodies over six tuples. The determinism pin that froze the absence re-records with its cause stated (injectivity restoration), and the collision pair lands INSIDE the existing test as machinery, so the census does not move at all. Blast radius re-verified here rather than carried forward: no landed production consumer, nothing persisted, no declared shift | `spatialReceipt.js` and every other sealed module, which MF-T2H owns; the dependency-ref join gap, which is present in the sealed source too and is RAISED rather than cured; any `COORDINATE_ABI_VERSION` motion, held deliberately because no artifact was ever minted under the broken encoding; any consumer wiring, barrel edit, persistence seam, `artifactKind`/`schemaVersion` stamp, y-axis flip, `heightQ()` motion, arrangement-quantum rung, tuning constant or dependency bump; widening the control-byte allowlist; and any `tests/lint/**` file |
| MF-T2B | [`MF-T2B`](./packets/town-cartography/MF-T2B.md) | **LANDED** at `db2e386f`; do not redispatch | Land D1's versioned integer coordinate ABI and the 19-declaration exact-geometry closure it rests on, and widen `requireCanonicalInt`'s DEFAULT parameters from the fixture-era `0..1000` to `±MAX_WORLD_UNITS` — the wall was always a default, never a constant, so re-parameterising is argument threading; the arriving half-plane clipper takes the convention-bearing name ODQ §310.3(7) ordered, and all 157 literal digest pins stay frozen and untouched | the `FABRIC_COORDINATE_ABI` string cutover, any consumer wiring, `heightQ()` returning a value, the BigInt conversion and the four DCEL divergences, the y-axis flip, an arrangement-quantum rung, `trigTable.js`/`groundLaw.js`/`fabricDcel.js`, and any `tests/lint/**` file |
| MF-T2A | [`MF-T2A`](./packets/town-cartography/MF-T2A.md) | **LANDED** at `37fb6916`; do not redispatch | Land the fabric single-declaration law: one `tests/lint/**` walker asserting that no exported name is declared in two files under `src/domain/townMap/fabric/`, measured at exactly zero of 94 distinct names across 18 files, so the port about to pour 54 sandbox modules into that directory cannot deliver two same-named opposite-convention exports; its removing power proven by a planted sweep duplicate | any `src/**` byte, the sandbox-side rename, a scan root wider than `fabric/`, a baseline/allow-list/exemption door, a parser dependency, and every later D3a port member |
| MF-T1X | [`MF-T1X`](./packets/town-cartography/MF-T1X.md) | **LANDED** at `f4ad467d`; do not redispatch | Retarget `executeFantasyConstruction` to one opaque saved-byte operation authorized by one replayed v2 exact-recipe mechanism; derive one acyclic three-mass state/receipt and reuse the MF-T1V/T1S projection so the DM-private addition leaves PUBLIC bytes exact | second operation/chaining, caller-loaded/readOnly authority, executable legacy semantic-list mechanism, roster/document widening, state persistence, custom/public fantasy mass, dossier/seed/sampling, new geometry/projection/draw path, UI/store/migration, eighth path or seventh test |
| MF-T1S | [`MF-T1S`](./packets/town-cartography/MF-T1S.md) | **LANDED** at `136efaa5`; do not redispatch | Canonically save the exact MF-T1M bundle and detached compile input, simulate package removal with an empty active installed-snapshot projection, reload visible unresolved custom content read-only, and project only from saved bytes through MF-T1V | fantasy/dossier/seed/sampling, UI, database/store, actual package uninstall, migration, citywide fill, new projection math or canonical projection promotion |
| MF-T1V | [`MF-T1V`](./packets/town-cartography/MF-T1V.md) | **LANDED** at `66dbed7c`; do not redispatch | Replay the exact MF-T1M bundle and project both varied bodies through the one fixed-survey draw/SVG path with origin-neutral PUBLIC privacy and roster-bound DM authority | document/content/persistence, fantasy/sampling, citywide fill, new shapes or lights, UI/export cutover, canonical projection promotion |
| MF-T1M | [`MF-T1M`](./packets/town-cartography/MF-T1M.md) | **LANDED** at `bffd1bcb`; do not redispatch | Compile two explicit parcel-bound varied masses and publish a reference-only transitional roster bundle without adding a second geometry owner | sampling/dossier/seed/weights, citywide fill, world-institution siting, `MASSING_PHASE`, copied geometry, projection/persistence/UI, new shape grammar |
| MF-T1F | [`MF-T1F`](./packets/town-cartography/MF-T1F.md) | **LANDED** at `325e16aa`; do not redispatch | Join the six landed first-slice fabric authorities by exact replay and refs without falsely claiming the full canonical Fabric contract | canonical `FABRIC`, ABI/law/provenance fabrication, copied rows, consumers, buildings, persistence/projection/UI, arbitrary morphology |
| MF-T1P | [`MF-T1P`](./packets/town-cartography/MF-T1P.md) | **LANDED** at `7fd8ad3f`; do not redispatch | Bind the eight landed W3 plot/frontage identities to the eight four-edge bounded DCEL faces without copying geometry or adding legal parcel semantics | geometry/topology copies, ownership/use/access, adjacency, mutation, Fabric root, buildings, persistence/UI, arbitrary parcels |
| MF-T1D | [`MF-T1D`](./packets/town-cartography/MF-T1D.md) | **LANDED** at `b020f743`; do not redispatch | Derive one coordinate-free V24/H64/F10 planar DCEL from replay-equal MF-T1A bytes without adding geometry or face semantics | holes/multiple components, copied geometry, parcels/ownership, graph/routing, Fabric root, buildings, persistence/UI, arbitrary embeddings |
| MF-T1A | [`MF-T1A`](./packets/town-cartography/MF-T1A.md) | **LANDED** at `edf5268d`; do not redispatch | Publish the sole connected 32-line post-W3 cadastral boundary arrangement with exact source lineage and no faces | DCEL/half-edges/faces, parcel/frontage ownership, graph/routing, walls/water, Fabric root, consumers, persistence/UI, arbitrary arrangements |
| MF-T1N | [`MF-T1N`](./packets/town-cartography/MF-T1N.md) | **LANDED** at `40b29038`; do not redispatch | Add one reference-only five-node/four-edge street graph over the landed geometry, reusing junction/segment identities and making boundary clipping explicit | coordinates, routing/direction/cost, road hierarchy, access, arrangement/DCEL/Fabric root, parcels/buildings, persistence/UI, arbitrary graphs |
| MF-T1G | [`MF-T1G`](./packets/town-cartography/MF-T1G.md) | **LANDED** at `875a0f39`; do not redispatch | Derive one canonical, hash-addressed street-geometry artifact from the landed orthogonal-cross foundation: one quantized junction, four exact legs, and source-street lineage | semantic graph, routing, road class, DCEL, cadastral boundaries, parcels, buildings/institutions, consumers, persistence/UI, arbitrary street plans |
| MF-W3S1 | [`MF-W3S1`](./packets/town-cartography/MF-W3S1.md) | **LANDED** at `a227c1d5`; do not redispatch | The sole fabric sealer gains one deterministic orthogonal-cross settlement with two corridors, four conserved blocks, aggregate four-axis frontage W3, and legacy byte stability | arbitrary street graphs/DCEL, buildings/institutions, projection/runtime cutover, walls/water, morphology inference, parcel tuning, adjacent shapes |
| MF-SH1 | [`MF-SH1`](./packets/town-cartography/MF-SH1.md) | **LANDED** at `3cf12008`; do not redispatch | One bounded semantic nonrectangular shape path: circular or regular-polygon shaft, conical or pyramidal cap, rectangular attachment, shell-derived fixed-light shadow, built-in/custom parity | domes, apses, spires, local lights, arbitrary booleans/concavity, institution defaults, full W3, UI/store cutover, cultural morphology packs |
| MF-VS1 | [`MF-VS1`](./packets/town-cartography/MF-VS1.md) | **LANDED** at `7c34f50f`; do not redispatch | One canonical first map slice: sealed surface fabric, frontage-first W3, explicit mass, fixed light, built-in/custom persistence parity, one fantasy construction, privacy and replay | full W3 corpus/soak, wall offset, multiple leaves, advanced light, UI/database cutover, broad migrations, evidence promotion, non-European morphology packs, adjacent edge cases |
| LANDED-1 | [`TC-3a`](./packets/town-cartography/TC-3A.md) | **LANDED** at `5066c34b`; do not redispatch | Adds named streets and wards at schema v2 and moves the naming pools off the bounded chunk by injection | parcels, carving, institution binding, byte band, TC-4..8, buildings, promotion, 3D defaulting, visual soak |
| LANDED-6 | [`TC-3b`](./packets/town-cartography/TC-3B.md) | **LANDED** at `a45c969d`; do not redispatch | Adds bounded contained parcels and the pure institution-to-parcel binding receipt for TC-4 | naming, schema changes, TC-4..8, buildings, promotion, 3D defaulting, visual soak |
| LANDED-2 | [`SC-1A+B`](./packets/surveyor-chat/SC-1.md) | **LANDED** at `455a29b5`; do not redispatch | Ephemeral text-intent shell and typed-op card extraction landed; SC-2+ remain excluded | direct chat proposals, audience/provenance decisions, uploads, SC-2/3, migrations, pricing/legal |
| LANDED-3 | [`GR-3B-ORIENT`](./packets/foreign-policy/GR-3B-ORIENT.md) | **LANDED** at `d56d944c`; do not redispatch | Adds the per-term obligation reader (CR-GR3B-3-R1) that GR-3b and IN-0c's transfer semantics consume | GR-3b producers, IN-0c, any persisted-shape change, treatyOrientationOf edits |
| LANDED-5 | [`GR-3b`](./packets/foreign-policy/GR-3B.md) | **LANDED** at `40afbdd6`; do not redispatch | Finishes the producer half of GR-3: the frozen rung ladders, selection function, and the composable-pair exception | GR-4+, later grammar, tuning, lighting, orientation edits |
| LANDED-4 | [`IN-0c`](./packets/foreign-policy/IN-0C.md) | **LANDED** at `29e2dc3c`; do not redispatch | Finishes the sole missing IN-0 slice: next-tick derived signing credit through the provenTrue seam | IN-1+, broader disclosure redesign, pulseKernel edits, any new persisted family |
| LANDED-7 | [`TC-4`](./packets/town-cartography/TC-4.md) | **LANDED** at `5a6f76fe`; do not redispatch | Fills the reserved v2 buildings layer: A-8 multiplicity, footprint packing inside parcels, dwelling fill, and dress — dark path byte-identical | any schema/contract change, per-class cohesion, TC-5..8, painter/labels, persistence, lit-surface tuning, soak |
| LANDED-8 | [`ES-5b`](./packets/foreign-policy/ES-5B.md) | **LANDED** at `6c0238ad`; do not redispatch | Espionage absence discounts a faction's council weight and contest power — the bench grain, with its disclosed one-time bloc-math shift | the career grain (ES-5c), ES-6/7, chooser edits, a second bloc-weight spelling, tuning of ABSENT_W, any receipt or audience surface |
| LANDED-9 | [`ES-5c`](./packets/foreign-policy/ES-5C.md) | **LANDED** at `c0447b8f`; do not redispatch | The espionage career register: absence discounts a defender's ladder standing, plus the vacuous ladder-writer guard it repairs | arm B (split to ES-5d), ES-6/7, contest-margin edits, `challengeScore`, tuning DEFENSE_WHEN_ABSENT, any ladder state write |
| LANDED-10 | [`ES-5d`](./packets/foreign-policy/ES-5D.md) | **LANDED** at `954592c0`; do not redispatch. ⚠ Deviation **D7**: the handoff is SAME-TICK, not one-tick-lagged — measured at `pulseKernel.js`, where the espionage pass and the ladder chain run unconditionally in that order, in one function, on one `worldState.tick`, so a `tick - 1` window would be silently dead. The sovereignty lighting census was re-derived WHOLE and re-recorded to `2392/365/2027/19659/5552 (19656 at that landing, +3 folded at `58436804`)`, repairing two pre-existing foreign deltas it had been red on | The career CREDIT: a graded mission deposits into the ladder's maintenance road via a one-tick ledger | architecture α, a persisted consume-once marker, ladder-dark writes, debits, tuning MISSION_CREDIT_MET, ES-6/7 |
| LANDED-11 | [`TC-5a`](./packets/town-cartography/TC-5A.md) | **LANDED** at `41b39220`; do not redispatch | The cartography painter's headless half: a pure draw-list plus palette ROLES, no colour, no mount | colour binding + the sub-tab (TC-5b), PNG goldens (TC-5c), skins (TC-5d), AI controls, the lynch-rubric gate (its own synthesis slice) |
| LANDED-12 | [`ES-6a`](./packets/foreign-policy/ES-6A.md) | **LANDED** at `53d538b4`; do not redispatch | The double agent's leak: a compromised operative's mission reaches the enemy court while succeeding silently. ⚠ **ES-6 was refused as chartered and split three ways** — ES-6a (this) is the leak; ES-6b (vetting quality) waits on a DISPATCHER wave nobody has chartered, because its only consumer has zero production callers; ES-6c (the retroactive Herald clause) folds into ES-7. ⛔⛔ **CORRECTED 2026-08-11 per chair order, session `c42c8924` — THE ESPIONAGE TAIL IS PARKED BY OWNER GATE, and the two forward pointers in this cell are HISTORY, not dispatch.** ES-7 was REFUSED on measurement: five of the six espionage Herald kinds have no reachable producer, because the DISPATCHER that would fire them was never chartered. The dispatcher slice ES-D was then REFUSED too — `envoyErrandRecords.js:590` forces a covert row's endpoints to the peace offer's own, so a court can only spy on the court it is suing, and no autonomous per-tick stage can mint a covert mission. The one compilable slice, **ES-Da** (covert cargo riding an already-accepted peace envoy), is OWNER-GATED at `docs/OWNER_DECISION_QUEUE.md` §16 — **which lives on the LEDGER branch `review-fixes-2026-07-08` and is NOT present on this build branch**. ES-6b is not released by any of this: its vetting band still has zero producers. ES-6c re-files OUT of espionage entirely, as a corruption-volume same-tick wave. **A build-branch-only reader must not read "folds into ES-7" or "rides the dispatcher" as live work — there is no dispatchable espionage row at this HEAD.** ⛔ **The sovereignty lighting census is NOT folded by this packet**: TC-5a holds that file's reservation as first claimant, so ES-6a's re-derivation is a CHAIR POST-LANDING STEP — the implementer reports all five figures and its isolated delta and never touches the walker. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | vetting quality (ES-6b, rides the dispatcher), the retroactive Herald clause (folds into ES-7), any new belief key, rumor-reach predicates, the lighting census file |
| LANDED-15 | [`TC-5b-i`](./packets/town-cartography/TC-5B-I.md) | **LANDED** at `9183d52c`; do not redispatch | The producer half of the ruled TC-5b split: one lazily-imported main-thread transport plus one lifecycle hook that compile a settlement's cartography block headlessly and answer presence — no mount, no render, zero production importers, expected bundle delta ZERO. ✅ O-5 is RULED (CR-TC5BI-6): the lighting-census walker is the packet's FIFTH reserved path and the implementer re-records the census in-change per the serialization law, subject to the foreign-title STOP. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the painter component, the sub-tab seat, the shell mount, the ROLE→colour binding and the A-4 notice (all TC-5b-ii), PNG goldens (TC-5c), skins (TC-5d), any worker-protocol or cache change, any persisted change, the TC-5a leaves, `mapTabShellLazy.test.js` and the rest of TC-5b-ii's nine reserved paths |
| LANDED-17 | [`TC-5b-ii`](./packets/town-cartography/TC-5B-II.md) | **LANDED** at `c82cc859`; do not redispatch | The painter's mount: the `cartography` sub-tab seat, the token→colour binding, the SVG render and the A-4 degraded state — the first production importer of TC-5a's leaf and the only user-facing surface of the TC-5 lane. ✅ Its blocking dependency is discharged (the seam is in history at `9183d52c`) and its last open item is closed: **CR-TC5B-4's A-4 notice copy was ruled at this promotion**, chair-authored, with the owner's veto surface at `OWNER_DECISION_QUEUE.md` §17.7. The mount/seat question was already settled and is NOT owner-gated. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the manifest seam (TC-5b-i, whose four reserved paths are forbidden here), PNG goldens (TC-5c), skins (TC-5d), AI editing controls, the lynch-rubric gate (TC-2r), `TOWN_MAP_VIEW_IDS` / `PRESENTATION_SUB_TAB_IDS` / `displayPrefsSlice.js`, `townScene3dLazy.test.js`, any colour literal or entitlement-bearing import |
| LANDED-16 | [`GR-4a`](./packets/foreign-policy/GR-4A.md) | **LANDED** at `a53ef7c6`; do not redispatch | The succession question, answered at the event, in the dark: a legitimate-power change answers the oaths the fallen holder swore — HONOR silently by scoring, or DISAVOW past a band through a factored shared shell. ⚠ **GR-4 was refused as one packet and split four ways at CR-GR4-1**; this is slice (a) alone, and GR-4b (the voice), GR-4c (the credibility charge) and GR-4d (the lit-mode queue) are named but NOT released by its landing. It gives `swornPartiesOf` its first production consumer and cures the two hardcoded `'repudiation'` literals whose absence would reward both courts for a treaty one of them tore up. ⭐ It holds the estate-wide lighting-census walker's reservation for all four packets promoted here — see the census-holder rule below. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the Herald/chronicle beats and the dossier `succession_question_open` line (GR-4b), any credibility delta (GR-4c), the `routineMajorApproval` queue and per-type terminal (GR-4d), the RENEGOTIATE arm (GR-5, and `treatyRenewalEnabled` does not exist), any change to `isRepudiableTreaty` / `repudiableTreatyPairs` output, any new tuning key, `pulseKernel.js`, `informationStatecraft.js`, `actorMajorApproval.js` |
| LANDED-18 | [`SCW-0`](./packets/site-coherence/SCW-0.md) | **LANDED** at `d648e788`; do not redispatch | Site Coherence Wave 0 — the enforcement layer, **instruments only**: the identity-keyed `(terrain, siteKind, decisive-token)` contradiction ratchet over the frozen 462-settlement corpus, the export-predicate liveness census with its `KNOWN_INERT` quarantine, and the hazard registry's repair plus a new `HZ-SITECOHERENCE` MACHINERY class. ⭐ It touches **zero `src/` files** and mints no output; its whole deliverable is the ability to state Waves 1–9 as numbers the repository re-derives. ⚠ Its exit criterion was changed at CR-SCW0-6 from *reproduce the audit exactly* to **re-derive and report against**, because five generation-touching commits — one a declared same-seed correction — have landed since the audit's HEAD. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | every correctness assertion (Wave 8), Waves 1–9 entire, `scripts/.observed-shape-readers-baseline.json` (Wave 1), `exportSemantics.js` (Wave 2), all of `src/**`, `scripts/mutation-sweep.sh`, every golden and every ceiling raise |
| LANDED-19 | [`ES-Da`](./packets/foreign-policy/ES-DA.md) | **LANDED** at `6dbb76c2`; do not redispatch | The composite rider: covert cargo on an already-accepted peace envoy — one pure leaf plus a ≤15-line composition, so a lawful `covert` sub-record rides the errand row the war lane was already minting. Nothing new travels, nothing new is scheduled, no stage is added. ⭐ **The only compilable espionage slice at this HEAD**, and the argued split out of the ES-7 and ES-D refusals, both of which stay refused. It is the first production caller of the orphaned `espionageDoctrineStage.js`, so it wakes ES-5a's doctrine half at zero extra cost. ⛔ Built DARK; the dark path is byte-identical **by construction**, and any motion in that golden is a STOP. ⚠ Authority is `OWNER_DECISION_QUEUE.md` §17 disposition 5 on the **ledger** branch, which a build-branch reader cannot open — the conditions are restated in full inside the packet. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any dispatcher (ES-7 stays refused), any Herald kind or routing row, the vetting band (ES-6b has no producer and is not released), multi-stop routes, `mintCovertMission` and `castCovertOperative` (both stay dead), any new persisted key or ledger, lighting the flag |
| LANDED-20 | [`GR-4b`](./packets/foreign-policy/GR-4B.md) | **LANDED** at `dd457b9a` (α scope per CR-GR4B-8); do not redispatch | The succession road's voice: one pure GRAMMAR leaf plus a widened return on GR-4a's own applier, so a torn-up oath is spoken once by the road that broke it — naming both courts, the hand that swore, and the recorded reason. ⭐ **`peaceTerms.js` pays ZERO net effective and ZERO net raw lines**: both edit sites are one-line-for-one-line replacements, which is what makes a mount possible in a file sitting at 797 of a ceiling of 800 with no baseline entry and no door. ⚠ **GR-4b as chartered was refused in part and split three ways:** of the five authored voice surfaces, only the disavowal has an honest producer at this base; `succession_question_opened`, `honored_by_silence` and the dossier line `succession_question_open` all describe a question standing open across ticks, which GR-4a answers inside a single expression, so their authored prose would be untrue the instant it minted — all three plus `reaffirmed` re-file behind GR-4d. ⛔ **Two budget rows carry an explicit pre-dispatch override (CR-GR4B-2)** — registration-only production files 3 → 5 and handwritten files 12 → 14 — because a Herald DESK kind costs five mechanical registration rows and that cost is per-desk, not per-kind. ✅ **CR-GR4B-3's annex correction is discharged in the promotion commit**, not owed: the `disavowed_by_succession` exemplar's `{npc}` is re-slotted to the fallen holder (the successor's name is on no surface the treaty stage can read) and `repudiated`'s unfillable `{reason}` is struck; `docs/content/RECEIPT_POOLS_GRAMMAR.md` is a forbidden file for the implementer. ⭐ It **was** the sole in-flight holder of the estate-wide lighting-census walker; ⚠ **DATED CORRECTION 2026-08-12 — that clause is HISTORY**: this packet is terminal, a terminal packet reserves nothing, and `IN-1a` holds the row now. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the three open-question kinds and `reaffirmed` (GR-4d, then GR-4b-ii / GR-4b-iii), any credibility delta or the `credibility_charge` pool and the fracture-charge dead-window re-derivation (all GR-4c), the lit-mode queue and its per-type terminal (GR-4d), any dossier line or `treatyDocument.js` edit, any treaty-ledger write or new persisted key, a tenth kind registry, `treatyLifecycleVoice.js`, `treatySuccession.js`, `pactAmendment.js`, `oathHolder.js`, `scripts/.size-baseline.json`, and any further edit to the receipt-pool annex |
| LANDED-21 | [`IN-1a`](./packets/foreign-policy/IN-1A.md) | **LANDED** at `5bf06481`; do not redispatch | The mirror itself: one pure, flag-gated leaf answering *"what does our own durable record say that court has been shown of us?"* — a frozen closed banded shape whose confidence degrades on our own first-person evidence that they know things we never showed them. ⭐ **It composes SP-B's landed `outboundImpression.js` (`4c0f2f38`) and re-derives not one line of it**; no IN design document named that leaf, so a lane reading only the IN volume would have hand-rolled a second derivation, and the dated correction lands with this promotion. ⚠ **IN-1 was refused as chartered and split three ways at CR-IN1-1** — it carries three behavior families against a budget of one, its receipt half is headed for a Herald knowledge desk that does not exist (`HERALD_SECTIONS` is still the frozen six), and three of its seven input families have no durable record; IN-1b (the dossier standing line) and IN-1c (the hums) are named, dependency-ordered behind it, and compiled just-in-time. ⭐ **Producer-first, on the ES-7 precedent**: after this packet the mirror has ZERO production callers, so the dark path is byte-identical **by construction** in BOTH flag states. ⛔ The K3 fence is STRUCTURAL, not an allow-list: the legal and forbidden belief reads are one function with swapped arguments, so the derivation never receives `worldState` at all. ⭐ It **was** the sole in-flight holder of the estate-wide lighting-census walker; ⚠ **DATED CORRECTION 2026-08-12 — that clause is HISTORY**: this packet is terminal, a terminal packet reserves nothing, and `IN-1b` holds the row now. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the dossier standing line, its DM expansion and the RENDERED-surface phrase scan (IN-1b); `mirror_shift` / `mirror_confidence_degraded`, the interim-desk declaration and the INFORMATION kind registry / receipt-pool module / annex URL (IN-1c); the seventh input family, WR-7b's negotiation pictures (deferred at CR-IN1-2); any durable outbound-transfer record (a new persisted family, refused at CR-IN1-7); any new tuning key (CR-IN1-4); `informationStatecraft.js`, `outboundImpression.js`, `pulseKernel.js`, `applyWorldPulse.js`, `heraldRouting.js`, every baseline and every ceiling raise |
| LANDED-22 | [`IN-1b`](./packets/foreign-policy/IN-1B.md) | **LANDED** at `af3d93c8`; do not redispatch | The dossier standing line: the mirror's first production consumer, and the act that closes the orphan window IN-1a deliberately opened. One pure read-model under `src/domain/display/` composes `mirrorInputsAt` + `secondOrderMirrorOf` into one row per counterpart, and the Relationships / Neighbours surface gains a block under *"What the neighbours have been shown"* — band words in record voice, never a perception verb, with a DM-only `<details>` expansion to the deriving record. ⭐ **It ends IN-1a's both-states byte-identity LEGITIMATELY, in the same commit that ends it**: three live dormancy assertions are NARROWED rather than deleted, on the ES-3 template, and the surviving claim is driven by a rendered-surface dark golden rather than asserted. ⭐ **The read-model's home is ruled and it costs nothing**: `src/domain/display/` sits outside the coupling census's scope regex, so the leaf mints no layer claim, no cross-layer pair and no registry row, and the unlayered baseline at exactly 179 is untouched. ⛔⛔ **`src/components/OutputContainer.jsx` MEASURES 599 OF A HARD 600 — one effective line of headroom, EXECUTED at promotion against a delegated draft figure of ~582 that was wrong by seventeen lines.** The drafted six-line edit did not fit; the promotion STOPPED and **CR-IN1B-7 re-shaped the wiring to PROPS-ONLY at ZERO effective lines**, on the landed `'rumors'` shape one switch case above. That file now joins `peaceTerms.js` on the **HOT-FILE LIST** (packet §3.11): any packet naming either one opens with an executed headroom measurement and a net-zero edit shape. ⚠ Six draft-open items are closed by **CR-IN1B-1..7** — band-word composition with the authored pool deferred to IN-1c, the DM gate composed inside the tab from props with no new premium comparison, the British sentence-cased heading, the line on both views with the whole block suppressed on a public dossier, the replacement invariant name, and the seventh input family deferred again. ⭐ It **was** the sole in-flight holder of the estate-wide lighting-census walker — re-measured free at promotion; ⚠ **DATED CORRECTION 2026-08-12 at the landing — that clause is HISTORY**: this packet is terminal, a terminal packet reserves nothing, and the row is FREE until the next dispatch restamps it. ⚠ At the landing the chair rode TWO one-row cures on `af3d93c8` — **CR-IN1B-8** (the `title=` walker's fifth reasoned raise, 484 → 485; the preflight never measured that walker, compilers must) and **CR-IN1B-9** (`requiredSymbols[10]` re-pointed to the successor invariant; the existence check is status-blind, so a symbol-retiring packet names its SUCCESSOR, never the retiree). ⚠ **DATED CORRECTION 2026-08-12:** IN-1b §5b B12 named two legitimate pre-existing reds (`tests/docs/enforcement-claims.test.js`, ratchet entry 5, and the owner-approved `generatorGoldenMaster` golden) and missed a third. `tests/lint/proseNumerics.test.js` carries its own banked ratchet entry, so §5b B14's expectation of exit 0 on a run including that file was wrong at compile time and at landing. A packet's pre-existing-red list is derived from `scripts/.test-ratchet-baseline.json` whole, never from memory. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | `mirror_shift` / `mirror_confidence_degraded`, the Herald beats, the interim-desk declaration and the INFORMATION kind registry / receipt-pool module / annex URL (all IN-1c); projecting `mirror_standing_line` into a runtime corpus (IN-1c); the seventh input family, WR-7b's negotiation pictures (deferred again at CR-IN1B-6); any durable outbound-transfer record or new persisted family (refused at CR-IN1-7); any ninth `MirrorRecord` key, new band member or new `basis` token; any new tab id, kind registration or dossier corpus block; any new raw premium comparison or `EXEMPTIONS` row; **any new effective line in `OutputContainer.jsx`**; `outboundImpression.js`, `secondOrderBelief.js`'s code, `pulseKernel.js`, `applyWorldPulse.js`, `informationStatecraft.js`, `couplingInclusion.walker.test.js`, the dossier prose corpus, every baseline and every ceiling raise |

| LANDED-23 | [`GR-4c`](./packets/foreign-policy/GR-4C.md) | **LANDED** at `cd2ab894`; do not redispatch | The oath that finally costs something: a torn-up treaty charges the breaking court's credibility stock, once, at the act, banded by the severity GR-4a already graded — closing the seam where a live reader chain (`credibilityScoreOf` → `reserveFor` → `OATHBREAKER_PENALTY`) has waited since GR-2 with no producer on the other side. One pure leaf plus a twelve-line gated block in a file with 637 lines of headroom; zero new flags, zero new persisted families, zero new surfaces, zero new tuning keys. ⭐ **Two compile premises were refuted at promotion and both corrections are the packet's substance.** First, the design routes the charge through the `fractureCredibilityDeltas` idiom, whose tick window is measurably dead — the reader folds 507 lines before the writer inside one pulse call, so a copied idiom would ship a born-dead charge; the charge moves to the breach site instead, and the window's repair is the named micro-act GR-4e. Second, the draft argued the new import was same-layer and owed no coupling row; executed measurement showed the cross-layer walker resolves layers from frozen regex module sets, **not directories** — `treatyBreach.js` is GRAMMAR, `informationStatecraft.js` is INFO — so the row was owed, the promotion STOPPED, and CR-GR4C-7 minted it together with the registry head edit four prior packets each forgot. ⭐ It **held** the estate-wide lighting-census walker's reservation — ⚠ **DATED CORRECTION 2026-08-12 at the landing: HISTORY**; this packet is terminal, a terminal packet reserves nothing, and the row is FREE (sixth restamp, same recurring reason). ⚠ Its one authored number, `CHARGE_BASE = 0.05`, is module-local, frozen, unsoaked, rides the endgame tuning signature, and the implementer may not tune it. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the dead-window repair (GR-4e), a fifth delta kind (needs an owner-signed tuning key, and the kind is not persisted), every Herald beat and chronicle line for the charge (GR-4d/GR-7), ungating the DM road from `oathHolderEnabled` (GR-7), the lit-mode queue and per-type terminal (GR-4d), `peaceTerms.js`, `informationStatecraft.js`, `OutputContainer.jsx`, `pulseKernel.js`, `pactFormation.js`, `couplingDesk.walker.test.js`, `couplingReceiptSample.test.js`, the coupling-inclusion baseline JSON, every other baseline and every ceiling raise |
| LANDED-24 | [`GR-4e`](./packets/foreign-policy/GR-4E.md) | **LANDED** at `c5ef5c19`; do not redispatch | The coalition-fracture credibility window now reaches its sole reader: a fracture written after the information-statecraft fold in tick `T` is consumed by that fold at `T + 1`, once. Promotion `3339188b`, seal `b995d8c9` and capsule `a31af4e2` produced the direct exact five-path child at **+36/-20**; the one executable edit is a one-for-one predicate replacement and the hot file remains 780/800. The old-window mutant exited 1 at 64/67 with the named predecessor title, the detached-stage mutant exited 1 at 66/67 with the pure case green and named stage case red, restoration closed at 67/67 and 100/100, and the bare full gate passed strict dist 403/403 across 50 files. Exactly one title was renamed, so no registration or census cardinality moved; the row is free at `2412/365/2047/19984/5638`. No soak was started. | a second credibility fold; pulse reorder; any treaty-writer edit; consume marker or persisted-shape change; new flag, tuning, golden, registry, census, baseline or ceiling; any sixth path; any added/deleted test title; GR-4d/GR-5/GR-7 |
| LANDED-25 | [`GR-4d`](./packets/foreign-policy/GR-4D.md) | **LANDED** at `a17d0a02`; do not redispatch | The lit succession decision, mechanism only. When both existing gates are true, the treaty stage queues one typed proposal for each above-band DISAVOW descriptor; below-band HONOR remains same-reference, silent, and unqueued. Apply and engine-auto use the same validated succession write, while dismiss, six-week expiry, and real ring overflow mean HONOR by making no treaty write; every ordinary actor-major keeps decline. Version-2 authority `fcf1efbd`, capsule `83cec036`, seal `19d47eb7`, and the complete attached outer receipt bind the exact seven-path **+594/-23** direct child. The governed `exactProjection` repair closed observed-shape and insertion-order gaps; A1-A8 passed **8/8** and **211/211**, all six named mutants failed by their intended titles and restored digest-exact, the hot files remained **797/800** and **941**, both TypeScript ratchets and the **1,998** observed-shape count held, and the whole census landed at `2413/365/2048/19992/5639`. Independent bare `check:tail` exited zero at **27,999 tests / 16 frozen known failures** and strict dist **50/403** (log `6537b57a`); separate boot smoke passed **513 chunks / 6,265 edges / 31,706-byte shell**. The version-1 capsule/seal, candidates `aa299e6c` and `556dab40`, recursive-runner STOP, and governed validation STOP remain non-promoted evidence only. No soak started. | GR-4b-ii/iii voice or dossier work; GR-5; a third leaf, eighth path, ninth title, new flag/tuning/state family/migration/golden/baseline/ceiling; public repudiation widening; realm-order-arm movement; any soak work |
| LANDED-26 | [`GR-4b-iii-a`](./packets/foreign-policy/GR-4B-IIIA.md) | **LANDED** at `7e7d5205`; do not redispatch | The one honest post-GR-4d opening surface is landed: a newly inserted valid pending succession question returns one fully addressed public `succession_question_opened` beat for that exact instrument. Version-2 authority `4bc44f8e`, capsule `518ee395`, seal `15f71332`, and cumulative attached run `000003-7cc4232d-03fc-4ad0-b3c7-7e86dd588a75` bind the exact twelve-path **+608/-27** direct child. The leaf is 119/84, `peaceTerms.js` remains 1264/797, A1-A8 passed **8/8**, the exact twenty-file battery passed **296/296**, all eight mutants convicted only their intended titles and restored digest-exact, both TypeScript ratchets and OSR held, and the census landed at `2414/365/2049/20000/5640`. Independent bare `check:tail` passed **28,010/16** and strict dist **50/403** (log `dc9662ec`); boot smoke passed **513/6,265/31,706**. Candidate `18d69d2b`, its anchor STOP, and the first repaired run's timeout aggregate remain non-authorizing. The census row is free. No GR-4B-IIIA soak started. | GR-4b-ii honor/reaffirmation voice; GR-4b-iii-b dossier; terminal voice; any successor/roster lookup; treaty-decision/apply edits; new state/flag/tuning/migration/golden/baseline/ceiling/dependency; mutation, hazard, coupling or OSR governance edits; thirteenth path, sixth registration, ninth title; terminal packets, annex, GR-5, soak |
| LANDED-27 | [`GR-4b-iii-b`](./packets/foreign-policy/GR-4B-IIIB.md) | **LANDED** at `770167c5`; do not redispatch | The open-question dossier line: every treaty document carries one canonically ordered line per validated pending succession question, rendered beneath the age line at both corpus-named mounts (`TreatyPanel`, `WarFaithTab`). Exactly ten paths: one DISPLAY-owned composer leaf validated through `successionQuestionPayloadTuple`, one always-present `successionLines` read-model key under the dormancy fence, one `section: null` no-desk grammar row (registration cost 2 — CR-GR4B-11 does not transfer), two ageLine-idiom mounts, one eight-case acceptance file, and three governed walkers. The whole census is reserved at `2415/365/2050/20008/5641`, runtime `28010 → 28021` with 16 frozen, floors `109 → 110` with the routing identity `6 → 7` (legitimate no-desk divergence), `title=` frozen at 485 with zero headroom, and no hot file in the manifest. Eight named mutants, both TypeScript ratchets, OSR 1,998, the coupling walker, the bare full gate, and separate boot smoke are mandatory. | GR-4b-ii honor/terminal voices; GR-5; any `impactKind` mint, Herald desk, `WHAT_PHRASES`/`EXPECTED_VOICE`/chronicler row; PDF projection edit; successor/roster lookup; `title=` prop; kill-list token; new state/flag/tuning/golden/baseline/ceiling/dependency; eleventh path, third registration file, ninth title; annex or terminal-packet edits; soak |
| LANDED-29 | [`GR-5A`](./packets/foreign-policy/GR-5A.md) | **LANDED** at `e93376ec`; do not redispatch | The monotone memory, and the sole member of `refs/trains/gr-5a`. A treaty remembers the worst it was ever observed to do: one conditional, monotone, drop-when-absent `worstObservedEver` field written at the one PASS-2 site that already computes the tick's worst observation, and only when `treatyRenewalEnabled === true`. ⛔ **GR-5 as chartered is REFUSED IN PART** — the volume's block is six behaviours and five bands wearing one wave's name, and the five successors (5b window, 5c renegotiation, 5d conversion, 5e voice, 5f dossier) each carry the measured reason they cannot compile at this base: a chair-signed band under §42/§43, or a producer that does not exist. ⭐ **It authors ZERO numbers**, which is exactly why it lands without a chair value signature (§47 O-6). ⛔⛔ **`src/domain/worldPulse/peaceTerms.js` MEASURES 797 OF A HARD 800** with no `scripts/.size-baseline.json` entry and no door, so the chartered *"one-line monotone fold"* is a §P8.6 STOP rather than a budget; the three new symbols live in `pactAmendment.js` (127/800, GRAMMAR-layered, zero OSR rows) and the mover pays **THREE ONE-FOR-ONE REPLACEMENTS** — 797 → 797 effective, 1264 → 1264 raw, numstat `3/3` — on the GR-4b `dd457b9a` precedent and the file's own landed two-statement idiom. ⭐ Five volume premises were refuted at compile and each correction is the packet's substance: V-7's addresses have rotted (`:694`/`:741`/`:759`, navigate by symbol), `PACT_LINEAGE_ACTS` is not the set §4 names, GR-5 mints no provenance value (GR-2 already landed all four, two of them producer-less chartered orphans), `PACT_ENDINGS` is two exported symbols in two homes so the endings sentence names neither, and the one-line fold is inexpressible. ⭐ **No GR vocabulary is widened at all**, so every producer lands in the same commit as its vocabulary member. ⚠ FOURTEEN paths exactly — SEVEN handwritten and SEVEN generated — two registration-only production files of three, one acceptance file carrying all four dormancy fences as cases, eight titles, and the estate-wide lighting census re-derived WHOLE in the same commit that creates the test file — which is what keeps that walker green at every commit of a single-member train. ⛔⛔ **THE SEVENTH PATH IS THE FLAG-MINT PIN, AND IT COST THIS TRAIN AN I1 STOP TO FIND.** A certification row is mandatory (the CQ5 walker's `manifestWithoutRow` arm demands it) and reds the ORDERED-equality pin at `tests/domain/subsystemRowsVirtual.test.js:384` by construction, so no six-path shape exists; the first compile omitted it, stopped at I1, and this row is the re-authored **P1′** (the parked P1 is preserved at `refs/trains/gr-5a-p1-parked`). The path costs three module-scope literal edits and **ZERO new test titles**, so the census tuple above is unaffected. ⭐⭐ **AND THE HABITAT TURNED OUT TO HAVE THREE OBLIGATIONS, NOT ONE — the train stopped TWICE finding them, and both STOPs were ratified (§49, §50).** Minting an engine-gated flag costs: **(a)** the `subsystemRowsVirtual.test.js` ordered-equality pin (path 7, three module-scope edits, zero titles); **(b)** the SEVEN generated edge-shared bundle artefacts that `simulationRules.js` feeds (paths 8–14, one `npm run build:edge-shared`, zero handwritten files — the freshness gate is red without them, and HB-2 had already paid this as its deviation D2); and **(c)** ⭐ the flag drive spelled as a **LITERAL** `<flag>: true` in the acceptance file, because `mechanismLitCoverage` grants AUTO credit only on a literal and a computed key attributes to NO key — the code is correct and only the machinery is blind, which makes it the cheapest of the three to miss. **§49 ruling 3 as amended by §50 ruling 2 makes all three STANDING LAW for every volume**; compilers preflight them from the ruling. ⚠ The lying exit was re-confirmed live in this train: the ratchet outlasted its window, backgrounded, and the harness reported the WRAPPER's exit 0 over a red gate — caught by reading the log, never by believing the code. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | GR-5b's renewal window and the `renewal` trigger producer; GR-5c's mid-term renegotiation; GR-5d's conversion arm; GR-5e's four Herald sentences and the five-file DESK cost; GR-5f's dossier and PDF lines; any lineage act, ending, provenance or trigger vocabulary member; any news kind, receipt pool, `WHAT_PHRASES` row or chronicler row; any band, threshold, window, cap, tuning key or authored number; any read-model key, `title=` prop or kill-list token; any seventh path, third registration file or ninth acceptance title; `DEFAULT_SIMULATION_RULES` and every preset; every baseline, ceiling, golden, migration and soak |
| LANDED-28 | [`GR-4b-ii-W2`](./packets/foreign-policy/GR-4B-II-W2.md) | **LANDED** at `b3602c15`; do not redispatch | The honor terminal answered aloud, and the second member of the estate's FIRST §28 train (`refs/trains/gr-4b-ii`: W1 `039f8380` annex act -> W2 `b3602c15` -> terminal `7b54c038`, one CAS). When a DM dismisses a pending succession question, the feed gains exactly one public `reaffirmed` beat naming both courts, the fallen holder and the recorded reason, closing the docket `succession_question_opened` opened. Thirteen paths: one pure `worldPulse` leaf owning the composer and the transition wrapper, one store thread through the existing single-producer `set()`, five mechanical registration homes (a Herald DESK kind costs five), one eight-case acceptance file, the re-addressed silence pin, and three governed walkers. CR-GR4B-18 rules the mount store-side with **no new persisted key** — exactly-once is structural in three layers (the CAS on `status === 'pending'`, no transition out of `dismissed`, entry-id dedupe) — and records the boundary that the first wave to RE-DERIVE this beat owes the key, which is what keeps `honored_by_silence` out. `ROUTED_TOKENS` and `REGISTERED_KIND_COUNT` move TOGETHER (377 -> 378, 110 -> 111) and the registered-minus-routed identity holds at 7 — the exact opposite of iii-b's `section: null` row. Census re-derived whole at the terminal to `2416/365/2051/20016/5642`, runtime `28021 -> 28032` with 16 frozen, `title=` frozen at 485, and no hot file in the manifest. | `honored_by_silence` and the expiry road (no transition site, so the persisted key is owed); GR-4b-beta `repudiated`; GR-4c `credibility_charge`; GR-5; ring-overflow, undo and replay voices; any new persisted key, state family, flag, tuning key, golden, baseline, ceiling or dependency; a fourteenth path, sixth registration file or ninth acceptance title; successor or roster lookup; `title=` prop; kill-list token; annex or terminal-packet edits; soak |
| LANDED-30 | [`INT-3B`](./packets/foreign-policy/INT-3B.md) | **LANDED** at `28d2824b488937e0b51fdd69920574aa2327f9fa`; do not redispatch | The émigré mint seam, and the sole member of `refs/trains/int-3b`. One pure, dark-safe, number-free leaf prices a defeated claimant's flight through the estate's ONE purposeful-travel substrate — one import (`errandMint.js`), a free-form `emigre_flight` purpose on GR-2's landed `pact_proposal` precedent, and the quoted literal `'factional' the frozen `ERRAND_CONSUMERS` row already reserved — and the `built` flip in the SAME commit makes that frozen claim a measurement instead of a promise. ⛔ **INT-3b as chartered is REFUSED IN PART** — the volume's block is six behaviours, five bands, three endings and two UI surfaces wearing one wave's name, and six successors each carry the measured reason they cannot compile at this base. ⭐⭐ **The wave's true blocker was found by measurement, not by reading**: the errand ROW DTO is war-welded (`normalizeErrand` refuses any row without a valid peace `offer`, `acceptance` and `snapshot`), so the design volume's own *"NO NEW LEDGER — SP-1 errands"* sentence is refuted at the base and no émigré record can persist there. The cure is GR-2's road: minting through the spine is PRICING a journey and validating a class, never writing a row — so this member persists NOTHING and prejudges nothing while `Q-TC12-1` (the record's home, a persistence-shape decision and owner-gated) rides the next sitting. ⭐ **No caller is wired, and that is the identity claim rather than an omission** (the landed HB-2 precedent verbatim): dormancy here is STRUCTURAL, so no seed, flag, preset or lifecycle path can reach the module and every lifecycle path is the same path. ⭐ **It authors ZERO numbers and mints ZERO flags**, so it lands with no chair value signature (the §47 O-6 / GR-5A precedent) and all three HZ-FLAGMINT obligations have EMPTY subjects — `interiorVetoEnabled` is INT-3a's to mint against its own first real gate read, on the WW-A precedent that a flag joins in the same commit as that read. ⚠ SIX paths exactly: the leaf, the one-word `built` flip, an EXACT-PATH regex in `LAYER_PATTERNS.INTERIOR` (never an `emigre[A-Z]` prefix — a prefix would claim files nobody has designed and silently widen a frozen family), the errand-registry walker's two exact-equality arrays with its count-bearing title renamed THREE → FOUR, one eight-title acceptance file, and the estate-wide lighting census re-derived WHOLE in that same commit — which is what keeps a single-member train green at every commit. ⭐ Because the leaf's only import is ARGUED substrate, `scanCrossLayerPairs` produces ZERO pairs: no `couplingRegistryInterior.js` is minted, no baseline entry is bought, and both exact ceilings (`ARGUED_ROSTER_CEILING` 19, `UNLAYERED_BASELINE_CEILING` 179) stand still. That single finding is what keeps this a one-file wave rather than a registry program. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | Any persisted émigré record, sub-ledger, `worldState` write or DTO change; widening `ENVOY_PURPOSES`, `ENVOY_PURPOSE_CLASSES`, `PURPOSE_CLASS_BY_PURPOSE` or any endings/news vocabulary; touching `envoyErrandRecords.js`'s `normalizeErrand`; the departure trigger and its share, patience band and per-pair cap; the three-way host decision and its loadings; the harboring grievance and its magnitude; the three endings and their envelopes; the host guest-mark and home away-mark UI; `interiorVetoEnabled` or any flag, `simulationRules.js` edit, certification row or edge-shared bundle regeneration; any band, threshold, window, cap, share or tuning key; a second import in the leaf; a seventh path; a ninth acceptance title; `couplingRegistry*.js`, `.coupling-inclusion-baseline.json`, `subsystemRowsVirtual.js`, `simulationRules.js`, `mutation-coverage-manifest.json`, `scripts/.size-baseline.json` and every `docs/DESIGN_FP_*` edit; every other INT wave |
| LANDED-31 | [`IN-1c-a`](./packets/foreign-policy/IN-1C-A.md) | **LANDED** at `24c61190731687952924de5c146046c07845ebda`; do not redispatch | The INFORMATION registry and the governed standing line, and the sole member of `refs/trains/in-1c-a`. The dossier sentence IN-1b renders today stops being one hand-composed template forever and becomes a seeded pick from the governed corpus — the act CR-IN1B-1 deferred to IN-1c **by name**. The estate gains its **fifth phrased-kind registry family**: `informationReceiptPools.js` (the nine annex-verbatim `mirror_standing_line` variants, zero imports), `informationNews.js` (`INFORMATION_KIND_REGISTRY`, ONE `section: null` row, `informationReceipt`), `INFORMATION_ANNEX_URL` so a governed corpus with zero code consumers finally has a reader, the family's own eight-title kind-pool walker, `kindPoolFloors`' four literals, and the certification row's `module` + `aliveness.other`. ⛔ **IN-1c as chartered is REFUSED IN PART and split three ways** (§55 SIGNED): the hums re-file as IN-1c-b behind chair Q3+Q5, and `mirror_confidence_degraded` is REFUSED with its measurement. ⭐⭐ **The refutation that decides the split is that the mirror is NOT re-derivable at a past tick** — `mirrorInputsAt` applies no `seededTick <= tick` filter to plants at all and filters transfers FORWARD, so a strength crossing can never be observed, and all three cures are closed (a remembered band is a new persisted family, refused at CR-IN1-7 and owner-gated; a collector filter is a leaf edit re-opening four pinned arms; a re-derived staleness is a second spelling of a module-private function). The staleness arm alone is pure and free, which is what HALVES IN-1c-b. ⭐ **It authors ZERO numbers and mints ZERO flags**, so it lands with no chair value signature (the §47 O-6 / GR-5A / INT-3B precedent) and **none of §49/§50's three flag-mint obligations is incurred** — stated affirmatively, because silence on those three has stopped two trains mid-chain. ⭐ **The coupling bill is ZERO and it is a measurement**: both new leaves are claimed by `LAYER_PATTERNS.INFO`'s `information` prefix on the day they land, `neighbourMirror.js` is `src/domain/display/` and outside `CENSUS_SCOPE_RE` entirely, so no cross-layer pair is minted, `couplingRegistryInfo.js` stays at its two rows, and both exact ceilings (`UNLAYERED_BASELINE_CEILING` 179 against a baseline of exactly 179 — ZERO headroom, `ARGUED_ROSTER_CEILING` 19) stand still. ⛔⛔ **THE TWO FILE NAMES ARE THEREFORE LOAD-BEARING AND A RENAME IS A STOP** — `mirrorPools.js`, `humNews.js` or `secondOrderPools.js` match nothing and would each buy an unlayered line the estate does not have. ⚠ ELEVEN handwritten paths, no generated artifacts, no hot file in the manifest (all four re-measured EXACT at this base: 599/798/797/780), 7 of 8 acceptance cases. ⚠ **The compile base MOVED under this packet** (TC13 compiled at `fc8451c4`; INT-3B landed since) and §-2 carries the promoting lane's own re-derivation of every premise at `d5a6c009`: three absolute figures moved — the lighting census, the runtime denominator and the packet count — and **every predicted delta held**. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | `mirror_shift` (both arms), `mirror_confidence_degraded`, their pools, producers and the interim-desk declaration (IN-1c-b/c, behind Q3+Q5); any second registry row; any `EXACT_SECTION`, `KIND_SECTION` or `WHAT_PHRASES` row (the last is FORBIDDEN for a `section: null` kind); any edit to `heraldRouting.js`, `chroniclersLetter.js`, `settlementRumors.js`, `informationStatecraft.js`, `pulseKernel.js`, `applyWorldPulse.js`, `simulationRules.js`; any edit to `secondOrderBelief.js` or `outboundImpression.js`, code **or** header (a fence arm requires the second's flag mentions to stay); repairing the nine forked FNV seeded-pick sites (docketed `CR-IN1C-DRIFT`); supplying `{season}` (chair Q2 — variants 2 and 7 stay declared-unreachable with a two-armed control); editing, reordering, padding or trimming any authored variant; any new flag, persisted key, ledger sub-key, writer, stage, tuning value or tab id; `src/components/**`; `couplingRegistryInfo.js` and both coupling baselines; `mutation-coverage-manifest.json`; every content annex; every baseline and every ceiling raise |

## Authorized implementation infrastructure

| Dispatch | Packet | Status | Why this is bounded | Explicitly excluded |
|---:|---|---|---|---|
| LANDED-I1 | [`IA-1`](./packets/infrastructure/IA-1.md) | **LANDED** at `d7ec3885dbcd7e09ff3bcd28d6f55a51bb1ae78b`; do not redispatch | Automates packet enforcement and feedback topology without changing simulation behavior | event bus, pulse reorder, persistence, automatic worktree deletion, product-scope changes |
| SUP-I2 | [`IA-2`](./packets/infrastructure/IA-2.md) | **SUPERSEDED** | Re-statused by CR-AR-1, 2026-08-12: it drifted at `f1895e60` eleven commits behind, its CREATE row names machinery that already exists in the tree, and its former non-terminal state still reserved TWELVE paths — a collision proved by simulation against the machinery packet's seven. Its live content folds into the IA-3 docket when that is chartered. Sealed one packet dispatch, proved diff scope, checkpointed resumable evidence | test sharding, affected-test inference, auto worktree/commit/merge/cleanup, product code |
| LANDED-M1 | [`MX-1`](./packets/infrastructure/MX-1.md) | **LANDED** at `3c61f42f`; do not redispatch | Machinery: retirement becomes machine-visible. An optional `retiredSymbols` array whose absence assertion binds at the one status where absence is assertable, mirroring the existing `CREATE` arm's nine-line rationale exactly, plus the cross-list disjointness arm that mechanizes CR-IN1B-9's hand cure — the manifest gains a way to say *"this packet retired X"* instead of being unable to tell that from *"this packet forgot X"*. ⭐ **Strictly additive**: zero existing manifest rows invalidated, no schema bump, and the arm the fix is bolted beside keeps its status-blindness intact — the validator gains an arm and loses none. ⛔ **The blocking collision is discharged.** The design lane measured `IA-2` holding seven of the needed paths at `d7b7d225`; CR-AR-1 re-statused it terminal, and MX-1's five paths re-simulated against the live manifest give **ZERO** duplicate-change-path errors. ⭐ **The design lane's two-site capsule instruction was measured wrong and is corrected in the packet**: the authority-projection pair and the derived-versus-hand-built `filePaths` pair are TWO coupling chains, so CR-AR-4's substrate widening is a **FOUR-site edit or every dispatch throws** `capsule omitted declared substrate`. ⭐ **It adds ZERO test titles, suite titles and test files** — every row goes inside an existing `it()` — so no figure of the estate-wide lighting census can move, and **this packet deliberately does NOT take the census row**; it stays free for the next promotion that genuinely needs it. ⚠ It carries CR-AR-9's hot-file section into `PACKET_STANDARD.md` with all three figures re-executed at this base (`OutputContainer.jsx` 599 of 600, `peaceTerms.js` 797 of 800, `informationStatecraft.js` 780 of 800), plus that file's stale header restamp. ⚠ **The packet edits its own validator**, so the implementer captures the tool's exit both before and after the edit; one figure where two are owed is a STOP. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | a status-aware skip exempting terminal packets from the symbol check (refused on the measured 174-of-174), any lint that parses a packet's free prose, `PACKET_MANIFEST_SCHEMA_VERSION`, retro-declaring retirements on packets already in history, editing any terminal packet's manifest row including IN-1B's cured `requiredSymbols[10]`, `scripts/implementation-gate.mjs`, `tests/scripts/implementationGate.test.js`, `INDEX.md` and `PACKET_MANIFEST.json` (coordinator acts), `tests/lint/sovereigntyLightingContract.walker.test.js`, `package.json` / `package-lock.json` (governed paths, and a dependency bump is a mint trigger), every observed-shape file, every other section of `PACKET_STANDARD.md`, all of the prose batch's items (a)–(f), every baseline and every ceiling raise |
| LANDED-GTR1 | [`GTR-1`](./packets/infrastructure/GTR-1.md) | **LANDED** at `cd404948`; code half `5f7e7051`; do not redispatch | Assigns source tests and post-build artifact tests to disjoint, exhaustive phases; the clean 17-step gate proves the source census and strict 403/403 dist phase | any product source, dependency, new test title/file, raw-test filtering, duplicate CI build, skip-ceiling raise, product golden, tuning, flag, timeout, budget, census or non-test baseline |
| LANDED-H26 | [`H26`](./packets/infrastructure/H26.md) | **LANDED** at `98729f79`; code half `d081feee`; do not redispatch | Schema 7 BANK-BY-RULE: 44 reads / 31 tagged rows, target 1,998 / 1,412 / 387, exact reviewed four-entry scanner transition and clean 17-step gate. Reservations are free for post-H26 re-derivation. | AO-0 and every scalar/corpus producer seam; SCW-1b and item (a); any `src/**` edit; a fifth declaration; scan/corpus/sentinel movement; any other scanner drift; hand-edited JSON; untagged raises; every golden, tuning value, budget, timeout, floor and unrelated baseline |
| LANDED-IM1 | [`INFRA-M1-DOCS`](./packets/infrastructure/INFRA-M1-DOCS.md) | **LANDED** at `600831749908e340444087df734d18c27046980d` | Member 1 of the `infra-1` train, the second §28 train and the first infrastructure one: the documentary substrate the rest of the program stands on. Nine docs files, zero production lines, and a census motion of `+0/+0/+0/+0/+0` that is provable rather than predicted — the lighting walker's corpus is a walk of the `tests` tree, so no `docs/` path can enter it. That property is what makes this member the train's lawful truncation boundary. It folds `DESIGN_BUILD_EFFICIENCY.md` and `DESIGN_IP_PROTECTION.md` from the ledger branch verbatim, authors `INFRA-PREAMBLE.md` (§P6 and §P7 lifted from the GR preamble as estate law that happens to have been written down there first, everything else authored fresh against three hazard classes the GR preamble does not name), gives `PACKET_STANDARD.md` its three §28 sections — train landings, family packet preambles, and the base-state capsule with its consumption law — and cures the three dispatch-surface contradictions lane FPC measured: the stale partial-programs sentence that reads two closed slices as still blocked, the FP spine's self-disagreeing wave count (corrected to 111 with the derivation stated, and the `= **108**` arithmetic deliberately left standing as the evidence that reconciles the three figures), and the HB/WC/EP headers whose banner word names the DOCUMENT rather than the code. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any `src/`, `tests/`, `scripts/`, `package.json`, `vite.config.js`, `eslint.config.js` or CI edit; the `= **108**` arithmetic line; any wave spec, section number, ruling or seam row; `docs/content/RECEIPT_POOLS_GRAMMAR.md`; a fourth FP volume header; any census re-record; the capsule generator (M2) and the sourcemap guard (M3) |
| LANDED-IM2 | [`INFRA-M2-CAPSULE`](./packets/infrastructure/INFRA-M2-CAPSULE.md) | **LANDED** at `e6eb4c5d15c827133a8edcd68698f30754dd21ed` | Member 2 of the `infra-1` train: the base-state capsule stops being hand-stamped. One script that regenerates `docs/implementation/BASE_STATE.json` in its exact existing shape by shelling the measurers that already exist, inventing no measurement of its own, so it cannot fork a second spelling of any figure. Its declarative core is a frozen `PROVENANCE` table naming, per row, whether the value is MEASURED (a live import, an eslint `Linter` run, or a shell-out this script parses) or PINNED (an exact-equality constant read from its canonical home by `espree`, which equals the tree exactly when the gate step asserting it is green) — the distinction that keeps the artifact from becoming the restatement engine the design volume warns about. Every arm fails closed: a missing home, an unparseable shell-out, a dirty measured path, or a `PROVENANCE`/emitted-key disagreement each throw rather than emitting a default, because a silently-zeroed row in an artifact other lanes cite as executed is the worst thing this member could ship. ⭐ The one figure with no cheap measurer, the runtime denominator, enters through a REQUIRED argument transcribed from the flip's own executed ratchet line and validated against the exported scope-floor ratio; teaching the ratchet to publish its own totals is filed as deferral INFRA-M4. ⛔ It ships NO `package.json` row — that file is a governed scanner path and a byte change to it is a schema-mint trigger, ratified by OWNER_DECISION_QUEUE §30; the npm alias rides the next mint. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any `package.json` or `package-lock.json` edit; any change to the artifact's key set, key order or `schemaVersion` (owner-gated); emitting the `PROVENANCE` block into the artifact; any edit to a measurer it reads; any `check` chain or CI edit; a second measurement spelling for any figure; the sourcemap guard (M3) |
| LANDED-IM3 | [`INFRA-M3-IP1`](./packets/infrastructure/INFRA-M3-IP1.md) | **LANDED** at `b713b05cbafe6d55eb799455e501803fe9082898` | Member 3 of the `infra-1` train and its terminal member: IP-1, the sourcemap guard, owed since 2026-08-07. The bundle ships no source maps today — but only by Vite's default, measured: there is no `sourcemap` key in `vite.config.js` at all, so one config line silently undoes the posture and nothing in the gate would notice. One test in the strict-dist family asserts, over the real built artifact set the runner already discovers, that no `.map` file exists anywhere under `dist/` and that no emitted chunk carries a `sourceMappingURL` directive; it maintains no chunk list of its own, walking the artifact from disk each run so a new chunk is covered the moment Rollup emits it. ⛔⛔ Its polarity is INVERTED from every other absence check in that tree, and the inversion is the member's central design decision: the house rule leaves absence checks ungated because a stale artifact can only UNDER-report absence, but this absence is about a CONFIG FLIP — flip the key, skip the rebuild, and the stale artifact still has zero maps, so the guard would false-GREEN over exactly the change it exists to catch. The reads are therefore gated like a size read plus an unconditional anti-vacuity case, ruled by OWNER_DECISION_QUEUE §30. ⚠ The directive scan is scoped to emitted chunks for a measured reason: 22 vendored third-party skin assets under the map fork carry real directives, and an unscoped scan would land the guard red at birth against bytes this build never authored. ⭐ The file PARKS in the lighting census by design — it opens a conditional suite so a fresh checkout with no artifact is not red — and it adds ZERO runtime tests, since the source phase excludes that tree and fails closed if a row leaks. This member also carries the train's whole-census re-derivation, which is what makes the terminal docs-only and the capsule's stamped window literally true. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any `vite.config.js` edit that survives the member; any `package.json`, `src/`, CI or `check` chain edit; minification, obfuscation or bundling changes (refused by name in the IP volume); IP-2a, IP-2b, IP-3 and IP-4; any baseline, ceiling or golden movement |
| LANDED-GAP1 | [`GAP-1`](./packets/infrastructure/GAP-1.md) | **LANDED** at `152d3f19cabe8b3006d94e56ff104029bccc5676`; do not redispatch | The single member of the `gap-1` train, and the cure for GAP-1: the engine-gated census walker's detector is one regex whose RECEIVER is pinned to the two literal tokens `rules` and `simulationRules`, which is narrower than the claim its own header makes. A real, strict, engine-gated read spelled any other way is invisible and the walker stays green — measured, not hypothesised: three live receiver shapes across four files hide seven keys, four of them accounted for by nothing at all. The cure keeps today's regex intact as arm 1 and joins two arms to it, an `||`-defaulted parenthesised receiver expression and a nearest-preceding-binding alias resolver, each with guards found empirically rather than reasoned into place. Proved BOTH directions at the base: `TODAY=63 WIDE=70 LOST=0 GAINED=7` — a strict superset that admits exactly the seven known-hidden keys and refuses the three decoys plus three scope artifacts the guards kill. ⭐ Rows 5-7 are the argument: the same hidden receiver sits on both accounted and unaccounted keys, so today's green is an accident of declaration rather than a measurement. Its four dispositions are ruled by OWNER_DECISION_QUEUE §32 — two keys join the manifest with authored certification rows, two take exemptions with written reasons, and the backlog is never touched. ⛔ It widens `HZ-CAMELBLIND` to instance 3 rather than minting a class, because DOCUMENT sits at its baseline and the estate's last owed slot would be spent on a duplicate. ⭐ It adds ZERO test titles, suite titles and test files — every control goes inside an existing test body — so no figure of the estate-wide lighting census moves and the census row stays free. ⚠ One correction to the ruled shape is on the record rather than smoothed away: the cohort widening asserts declared event types against the SEPARABLE literals with an enumerated shared-literal ledger, because the literal predicate would grade a dark subsystem alive off a shared channel another row already declares. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any gate-site edit (the detector moves, the engine does not); `codeOnly` and its ten importers; `BACKLOG_RULE_KEYS` and its ceiling; any new literal test or suite title; any bare collection negative in the walker file; `package.json`, `package-lock.json` and every governed observed-shape path; `src/domain/worldPulse/institutionLifecycle.js`, `convergence.js`, `settlementPolitics.js`, `supplyWebWarfare.js`, `src/store/campaignSpatialCanonize.js`; every baseline, ceiling, golden, flag and tuning value |
| LANDED-AO0 | [`AO-0`](./packets/corpus-coverage/AO-0.md) | **LANDED** at `e71beb84`; code half `3df85a3a`; do not redispatch | Schema 8 adds a second-consumer-only typed scalar projection over five authoritative shipped seams while preserving `foldCorpus`, default topology, OSR artifact, and inventory byte-for-byte. The strict 7→8 proof binds an exact four-entry scanner transition and 1,412 same rows with zero motion. | AO-2..6, prose cures, HZ-CROSSHOME, every `src/**` edit, flat flavor events, `campaign.chronicles[]`, volatile ID/time fields, scalar data in the default artifact, dependency, golden, flag, tuning, timeout, floor, ceiling or unrelated baseline movement |
| LANDED-AO23 | [`AO-2+3`](./packets/corpus-coverage/AO-2-3.md) | **LANDED** at `f0c272e8`; contract `c825af92`, cure `d4f213d0`; do not redispatch | Contract-before-cure without exposing a red branch tip: 272 introductions / 53 homes froze 48 distinct / 53 occurrence debt across seven applied-summary addresses, C1 proved the final-zero red, C2 cured only those addresses and proved exactly seven vanished identities, and C3 banked them to zero. The exact 7-path → 1-path → 1-path train passed 97/97 and only its green final child reached the shared ref. The census row is free. | `reasons`, generic or unowned summary rewriting, headlines, `npcAgency.js`, persisted shape, AO-4 totality/inert quarantine, AO-5's other four families, AO-6's standing mutants, every golden, flag, tuning value, dependency, ceiling, observed-shape artifact and ninth implementation path |
| LANDED-AO4 | [`AO-4`](./packets/corpus-coverage/AO-4.md) | **LANDED** at `09e39ee6`; do not redispatch | Complete 53-home headline+summary totality freezes 106 rows / 400 distinct / 544 occurrences / 14 prospective / 92 indicative at the exact 12,242-byte digest, including eight lawful null impact kinds across two collision-proof `webwar_*|null` homes. Rewrite liveness walks the occurrence-disjoint persisted union: selected 151/80/95/66 + mechanical 77/24/73/23 = union 228/83/168/69. The exact fallback closes the registry to 26 = 17 active + 9 exact reasoned inert with zero gaps/overlaps, while the current producer's explicit applied twin remains byte-identical. The eight-path implementation passed 134/134 and the bare full gate; the census row is free at `2411/365/2046/19976/5637`. | `consequenceOutcomes` as a third/double-counted liveness lane; string `"null"` or delimiter collisions; summaries, `reasons`, producer wording, generic/fuzzy rewrite, current producer output movement, persisted shape, AO-5/6, second corpus build, golden, flag, tuning, dependency, timeout, floor, ceiling, observed-shape artifact, hazard count/status, mutation uncovered movement or ninth path/case |
| LANDED-AO5 | [`AO-5`](./packets/corpus-coverage/AO-5.md) | **LANDED** at `72b7a87b`; do not redispatch | One read-only contract freezes the exact other-four AO-0 denominator: timeline 4/8/12, pulse history 50/1286/5665, regional typed audit 2/7/201 with structural zero prose, and pure AI Chronicle 7/7/7; 63/1308/5885 overall at the exact 8,280-byte digest. The exact eight-path implementation proves one build/two reconstructions, exact null homes and selector axes, the real timeline and Chronicle seams, the 15-item protected substrate and 23-item union, while preserving every product output. It passed 205/205 focused tests and the bare full gate; the census row is free at `2412/365/2047/19984/5638`. | every `src/**` edit or prose cure; a fifth family; flat flavor events; `campaign.chronicles[]`; generic voice/AI classification; cross-path alias deduplication; `consequenceOutcomes` as a third AO-4 liveness lane; volatile ID/time; generic nested-index contiguity inferred from omitted scalar leaves; second in-walker corpus build; OSR/golden/flag/tuning/dependency/timeout/floor/ceiling/hazard-count/mutation-uncovered movement; ninth path or ninth case; AO-6 mutants |
| LANDED-AO6 | [`AO-6`](./packets/corpus-coverage/AO-6.md) | **LANDED** at `0facf170`; do not redispatch | Final correctness closure for the corpus-coverage program. Immutable candidate `b02bc709` stopped unlanded at true exit 4, `77 CAUGHT / 1 CLEAR / 4 gaps` (log SHA-256 `d582b8a06a909f5b05c7321e690a5702a7140f87829ab906ed80709d2eb99d9a`); governed amendment `eff62d6b` authorized only the four measured repairs. The final sealed child (`6bd0e117`, capsule `e7fe66ae`) landed the exact four paths at +137/-26 and closed at `71/272/198 + meta 10`, 81 exact claims/labels, 70 in-place calls, 56 guarded targets and 56 unique `MUTATED_FILES` rows. Its clean disposable sweep passed `81 CAUGHT / 1 CLEAR / 0 gaps` (log SHA-256 `c4e5fb08dc6cfe550db945a69e3c78fa02d172bac0fa6857258504aefcf81249`), focused tests passed 98/98, both typechecks and OSR passed, and the bare full gate passed with strict dist 403/403 (log SHA-256 `53f62e286def95a2f3399a9fcfaa665334afd5d33235403fb54605091f9a6c4d`). No test title moved, so the census is free at `2412/365/2047/19984/5638`. | any committed `src/**` edit; any legacy guard/label/plant/command/result/manifest movement outside packet section 3.5; a fourth positive mutant, second negative control, new test/title/suite/baseline, skipped/broad/unnamed red, exit-only CLEAR, dirty override, shared-tree whole sweep, product/corpus/helper/hazard/pre-mortem/census movement, uncovered-baseline movement, fifth committed path, ninth case, GR-4e or later work |
| LANDED-HB0 | [`HB-0`](./packets/fp/HB-0.md) | **LANDED** at `e188760b8fec296812b39b8c521781e43d266296`; do not redispatch | Member M1 of the `hb-1` train, the HABIT volume's first code and the train's truncation boundary. Two zero-caller pure leaves — the twelve-class circumstance vocabulary with its declared precedence and owner-signed ceiling, and the frozen curve whose one rounding door every stock value passes through — plus the volume's own Bands/§8.2 reconciliation walker, whose sibling `spBandFamilies` provably does not generalize because its wave-block parser names nine volume prefixes and HB is not one of them. Dark by construction: no flag, no caller, no world read, no persisted shape, so the wave is byte-identical for every generated world and the dormancy evidence is a zero-importer source scan. Dispatchable because `OWNER_DECISION_QUEUE.md` §36 signed CR-HB-Q2 and CR-HB-Q3 and §34 narrowed HB Q1's seal to HB-3 onward. It takes two `ARGUED_UNLAYERED` rows and raises the argued roster ceiling 13 → 15 in the same commit, because the baseline door is closed to a new leaf by doctrine | the argued roster ceiling and the unlayered baseline; the severity ladder and every module that could spell one of its rungs; `HABIT_TUNING` and every band it holds; `proposedSoakBands.js`, the soak manifest version and its coupling vocabulary; any flag manifest row; `pulseKernel.js` and `applyWorldPulse.js`; every persisted shape |
| LANDED-HB1 | [`HB-1`](./packets/fp/HB-1.md) | **LANDED** at `1013d58baefeadc10a5da38b350a7fa86e2a1c63`; do not redispatch | The single member of the `hb-1p` train — the HABIT volume's second code, re-chartered after `OWNER_DECISION_QUEUE.md` §38 refuted the volume's R7 delete clause on executed evidence and §39 signed the six re-charter items. It mints the estate's ONE closed strategy-move vocabulary as a dependency-free leaf (the cross-volume collision contract's §4.1.1, and HABIT builds first so HABIT mints), the frozen habit fork registry classifying every weighted decision fork the estate can see, and the two totality walkers that make all three frozen. ⛔ Its headline act is a REFUSAL: the `move === 'fortify' ||` disjunct in the ruling-bloc decision load is LIVE — measured 1.02 at the ratified same-seed golden's own consumer against the 1 a fall-through token returns — so the deletion is struck and a brancher REGISTER replaces it, whose owner arm is membership-OR-REGISTERED-FOREIGN exact in BOTH directions with an evidence field naming the golden, so a future deletion reds. `settlementPolitics.js` receives a COMMENT-ONLY edit and its executable text is byte-identical. It also discharges HB-0.md §15's orphan window by re-aiming that wave's whole-tree dormancy absolute IN PLACE to a DARK-CLOSURE claim in the same commit that creates the first import, and it kills the fork-registry importer-scan class at the registry's own end with EXTENSIONLESS module ids for zero effective lines. It takes two `ARGUED_UNLAYERED` rows and raises the argued roster ceiling 15 → 17 in the same commit, and `settlementStrategy.js` is NET ZERO at 812. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | `scripts/.size-baseline.json`; `scoringObjective.js`, `convergence.js`, `mobilizationReactions.js`, `pulseKernel.js`, `applyWorldPulse.js`; any flag manifest row, `CPL-` coupling row or persisted shape; `scripts/mutation-sweep.sh`; the six volume-text sites that still instruct the refuted delete (CR-HB1′-VOL, its own micro-act); `espionageDoctrineStage.test.js`'s bare importer regex and the whole `codeOnly()` micro-act family; HB-2 and HB-4+, which HB Q1's seal still gates; any golden, baseline, ceiling or tuning movement |
| LANDED-HB0B | [`HB-0B`](./packets/fp/HB-0B.md) | **LANDED** at `c777a158da79830d46862b8df24f427dd9050824`; do not redispatch | Member M1 of the `hb-2b` train, and a §42.2 micro-act rather than a volume wave: it adds three frozen numeric members to an existing frozen object, pins their derivations, and does nothing else. It exists because `OWNER_DECISION_QUEUE.md` §42.1 ratified that the HB-2 charter's *"structural bounds declared in `HABIT_TUNING` at HB-0"* was unmeasured fiction — the bag held ten keys, none a cap or an age, and the three symbols returned zero hits estate-wide — so this member is the commit that makes its successor's own charter sentence true rather than inherited. The three values are chair-signed at §43 item 1 on executed derivations: the row cap at twice the twelve-class vocabulary so eviction stays reachable at 18.2% of the structural product, the pledge age as a POSITIONAL index into the shared half-life ladder past the executed decay/credit crossover (never spelled, because the file's own landed header forbids a rung word and no walker enforces that), and the book cap at the transient/persistent parity point. It authors no band: the soak manifest stays at nine ratified bands and `SOAK_COUPLINGS` names no habit family, so a habit band could not be added without first minting a seventh coupling — the exclusion is the landed law this member complies with, not a gap it exploits. It mints no leaf, wires no caller, reads no world, moves no registration, and adds no file to the habit directory, so the family file count stays at two and the dark closure stays at three. ⚠ It does NOT hold the census reservation — its four titles ride as a declared interior red cured whole at the train's last `tests/`-moving commit. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | `tests/lint/sovereigntyLightingContract.walker.test.js` and every census figure; `scripts/mutation-coverage-manifest.json` (the target battery is not enumerated, so a row would red the no-stale-entries case); `tests/lint/couplingInclusion.walker.test.js`, the argued roster ceiling and the unlayered baseline; any second rounding expression anywhere in the habit family; any circumstance-class token as a quoted literal, JSDoc included; any half-life rung word inside `habitCurve.js`; any `docs/DESIGN_FP_ARCH_HB.md` edit including the R33/R34 corrections (they join CR-HB1′-VOL); `proposedSoakBands.js` and the soak manifest; any flag manifest row; `settlementStrategy.js` and `settlementPolitics.js`; both typecheck ratchets, every golden, baseline and ceiling |
| LANDED-HB2 | [`HB-2`](./packets/fp/HB-2.md) | **LANDED** at `de214782bbff2291c77d0a62c9ec40a66781e727`; do not redispatch | Member M2 of the `hb-2b` train, and the HABIT volume's third code: the habit sub-ledger gets a home, a single writer, a gate ladder's first door and a registration — all four **dark**, so HB-3's credit fold has a place to write and HB-4's chooser read has a place to read. ⛔ **NO CALLER IS WIRED**, and that is the wave's identity claim rather than an omission: `writeHabits` exists and nothing in `src/` calls it. It AUTHORS no tuning value, spending three that M1 minted one commit earlier — which is what makes its own charter sentence true rather than inherited, after §42.1 ratified that those bounds were nowhere in the tree. Its behaviour contract is that with the flag absent or false a campaign's serialized bytes and its entire rng stream are identical to a pre-HB-2 build on every seed and on every one of the fifteen lifecycle paths, carried by two pinned mechanisms only — the gate returns the INPUT REFERENCE before any allocation, so the namespace is never created, and no prose token is composed anywhere. ⚠ It claims no fence it does not build: the factor, draw and spread mechanisms belong to HB-4/5/6 and are refused here. ⛔ It carries `R32`, a landed pin no prior compile named: minting two leaves into the habit directory moves that family's file count from two to four, and because the rounding scan runs over the WHOLE family neither new leaf may spell any rounding operator — every rounding routes through the imported `roundToUnits`. It takes two `ARGUED_UNLAYERED` rows with measured empty `reads`, raises the argued roster ceiling 17 → 19 in the same commit, adds exactly ONE mutation-manifest row, and **holds the census reservation**, re-deriving the tuple WHOLE at its own commit — the train's last `tests/`-moving change — which is what keeps the terminal docs-only. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any chooser, close or foreign record; `pulseKernel.js` and `applyWorldPulse.js` (both banked, zero headroom); `settlementStrategy.js` (812/812, tolerance-zero) and `settlementPolitics.js`; `momentum.js`; any `.jsx`; a circumstance classifier (`CR-HB2-O1`, refused here and blocking for HB-4); any circumstance-class token as a quoted literal, JSDoc included; importing `num`/`asObject` from `npcLadderState.js` (it would give the ledger a real interior read and red its own declared empty `reads` row); any `Math.round`/`trunc`/`floor`/`ceil`/`toFixed` spelling in either new leaf; the median/p95 header pin (re-aimed to HB-3); `docs/DESIGN_FP_ARCH_HB.md`; every golden, baseline, ceiling and tuning value |
| LANDED-ESTA | [`EST-A`](./packets/infrastructure/EST-A.md) | **LANDED** at `3be4736f0119bdbedb627e9bf862782e21c9dc0a`; do not redispatch | Member 1 of 3 of the `est-1` estate-repair train, chartered at `OWNER_DECISION_QUEUE.md` §56 and ruled on the corrected cures at §60. The F6 cross-user map-import source contract stops slicing the campaign-import action with a hard `start + 6000` and anchors on the impl's own closing brace. ⛔ **The banked ratchet row it repairs carries a cause that is statically false** — it says the import stopped carrying the safe `seed` field, and the import carries it at line 381 where the test's own regex matches; the test simply never sees that line. Code right, pin wrong, and a burn-down lane dispatched on that attribution would hunt a product defect that does not exist. ⛔⛔ **The security half is worse than the bookkeeping half**: the XSS negative in the same describe shares the truncated window, so the one branch in the campaign import that handles the untrusted cross-user `sharedMap.fmgSnapshot` blob sits past the window and is unseen by the guard that exists to watch it. ⭐ The width is the wrong dial and no number is authored: the impl is the LAST top-level declaration and the whole file holds exactly one column-0 closing brace after it, so the anchor is an enumeration rather than a claim, and the member's own truncation control makes the boundary self-proving. ⚠ Three paths, no `CREATE` row, no `src/` file, and the §85.4 registry-mint obligations have EMPTY subjects. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the anti-vacuity walker's non-empty-but-truncated rule (docketed `CR-EST-TRUNC`); any width constant; any edit to `src/store/galleryImportMap.js`; the sibling single-import describe; the other fifteen banked ratchet rows; every other member of `est-1` |
| LANDED-ESTB | [`EST-B`](./packets/infrastructure/EST-B.md) | **LANDED** at `dae90484c26e273699890e8b4dfa754a8045e7a8`; do not redispatch | Member 2 of 3 of the `est-1` train, promoted alone under the §60.3 staged-promotion law in the same commit that flips its predecessor. Two security-adjacent suites — the fog premium tier-gate's source contract and the command executor's stale-context refusals — re-enter the estate's evidence layer. ⛔ **The §56 charter's cure for this member was executed as a counterfactual and measured to move the census by ZERO**, and §60.2 signs the correction: the two suites do not park for their `.each` TABLES, they park for `TEST_CONTEXT_PARAM` — the callback taking a parameter — and a STATIC LITERAL table is precisely what EARNS credit, under a predicate the walker's own header says *never widens credit, it only ever removes it*. Re-pointing them at live tables would have imported the exemplar's park reason instead of escaping it. ⭐ The replacement keeps BOTH halves of the charter's intent: the cases are spelled out as straight-line tests (visibility, the SP-D idiom — a loop lives inside a named test, never generates tests) and ONE named roster-totality guard pins the spelled roster against a DISCOVERED source (anti-drift, the intent behind "live tables"). ⭐⭐ **The guard earned its keep before it landed**: the compiled shape spelled three tier-blind modules and the discovery found FOUR, so a fourth case is spelled and its module measured clean against the forbidden vocabulary rather than exempted. ⚠ FOUR paths, not three: burning a per-file row out of the negative-assertion anchor walker's frozen inventory is the door the compile did not price, and the four spelled scans are anchored so the row is DELETED rather than lowered. No `CREATE` row, no `src/` file, no product behaviour, and the §85.4 registry-mint obligations have EMPTY subjects. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any re-pointing at a live or dynamic table (measured: it re-parks the file); a totality guard over the executor's closed freshness triple (the self-referential-pin class, docketed `CR-EST-FRESHTRIPLE`); any exemption entry for a module that DOES match the forbidden vocabulary; lowering the anchor row instead of deleting it; the ratchet baseline (this member repairs nothing); every other member of `est-1` |

| LANDED-ESTC | [`EST-C`](./packets/infrastructure/EST-C.md) | **LANDED** at `c632cbf466f39298317de13603f4e0528d00d211`; do not redispatch |

| EFF-1 | [`EFF-M1A`](./packets/infrastructure/EFF-M1A.md) | **LANDED** | Member 1 of the `eff-1a` train — the efficiency stack landing its own machinery. It creates `scripts/preproof-train.mjs`, the parallel pre-proof harness: one detached throwaway worktree per train member OUTSIDE the repo, containment enforced on REALPATHS so neither a shared prefix nor a symlink can lie about it, batteries spawned detached so signal cleanup kills process GROUPS and never `pkill`, cleanup awaited on every exit path through ONE shared promise paired with a `signalled` flag, and an exit vocabulary in which a battery red is the ONLY exit 1. Its proof is an EXECUTED RECEIPT rather than a test file, deliberately: a new test file would red two censuses sitting at their pinned ceiling. It carries the two obligations the audit named for the landing executor — a REQUIRED per-battery `timeoutMs` (a ceiling the plan author measures; the harness invents no constant) and per-member vitest cache isolation via a generated override config written outside the worktree, so concurrent batteries stop sharing one physical cache through the write-through node_modules symlink. ⭐ One draft claim was refuted by execution and cured here rather than carried: `--reporter=basic` no longer exists in vitest 4, so an estate run produced a startup error, exit 1, and a truncation signal for a reporter typo — the harness now grades a red only against a JSON result naming at least one executed test. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any npm-script wiring or gate step; `gate-mutex.sh` contact; `vite.config.js`, `package.json`, `package-lock.json` and every governed observed-shape path; any test file; any census, ratchet or walker baseline |

| EFF-2 | [`EFF-M1B`](./packets/infrastructure/EFF-M1B.md) | **LANDED** | Member 2 of the `eff-1a` train. It creates `scripts/premise-map.mjs`, the executable half of the three re-charter redundancies: `validate` for the plan shape and the signed-fork law, `scope` for the truncation calculus with its transitive closure over `dependsOn` and flag-slice edges, and `queue-check` for the dual-family ready-queue invariant bound to the plan own train. Pure file-in and file-out — no git state, no network, no test runner — which is why it is lawful beside a live executor. The conservative defaults are structural: a law-shaped refutation, an invalid or absent map, a near-miss row id and a truncation leaving nothing to continue all keep the full-train stop, and a forked member is its own outcome that never appears under stop while its dependents still do. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any git or network access; softening a conservative default to let a plan pass; any test file; any law text (the volume amendment is the chair own act) |

| EFF-3 | [`EFF-M2`](./packets/infrastructure/EFF-M2.md) | **LANDED** | Member 3 of the `eff-1a` train, and the first of its two docs members. It gives `PACKET_STANDARD.md` six new sections — the differential member caps with the Fable-round stamp as a PRECONDITION rather than a default, the parallel pre-proof option line whose whole point is that a pre-proof green is never landing proof, the premise-map and scoped-truncation section with the schema-v2 plan shape and the re-chain rule that a continue-tail never lands as committed, the validator status-sequence simulation and why endpoints cannot check it, the registration obligations a flag mint and a seeded-chooser mint each price at compile, and the census-burn law. ⛔ It authors no law: every section restates a ruled one and cites it. ⚠ It also RECORDS rather than repairs a divergence it found — the build-branch fold of the efficiency volume predates the chair three amendments, so the sections state their law in full instead of delegating to a citation a reader cannot follow here. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any preamble edit (member 4 owns those paths); re-folding the efficiency volume (a chair act, recorded as owed); any per-family or per-wave figure; any code, test, script or config |

| EFF-4 | [`EFF-M3`](./packets/infrastructure/EFF-M3.md) | **LANDED** | Member 4 of the `eff-1a` train and its terminal member. Each of the five family preambles gains ONE section, byte-identical across all five except a single family-measured stamp line: the cap table, the stamp precondition, the pre-proof and premise-map obligations, and the standing battery template per wave class so compiles stop re-deriving proof ARCHITECTURE wave by wave. Proof CONTENT is unchanged. ⭐ The stamp line is measured, not assumed: the seven rounds stamped WC, WF, EP, POP, INT, WY and the TR/IN/CW/ES tails, so GR and HB record FOUR with the door named, and INFRA records FOUR because a build-machinery family has no annex for a round to stamp — writing 8 into an unstamped family would manufacture exactly the authority the stamp exists to gate. Template provenance is preserved: receipt-tagged lines were re-verified against their named receipts, law-tagged lines against their named law texts, and the prose class states plainly that no train receipt exists for it yet. It also carries the INT preamble refresh, whose registries figure rotted in-window and would otherwise fire a FALSE stop on the next INT compile. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any per-wave figure in a preamble; renumbering an existing section; the docs-debt consolidation (that is `eff-1b`, compiled only after this train terminal is exposed); any code, test, script or config | The sole member of the `est-1c` follow-up train — compiled as `est-1`'s third member, STOPPED at that train's terminal, and unblocked by the `CR-EST-CONTROLZERO` ruling. ⛔⛔ **THE HEADLINE IS A RECLASSIFICATION, NOT A REPAIR ORDER**: three ratchet rows filed as `debt` are three guards correctly reporting that a shipped feature does not work. Confirming ANY auto-discovered supply chain is refused — the discoverer emits the empty string for `needIcon` deliberately and pinned, the admission boundary rejects it, and nothing in between fills it. The recorded cause is false in both halves: the "fixtures" are the product's own output, and the writer that "stopped supplying needIcon" never stopped. ⭐ **The chartered cure would have hidden it** by patching a non-empty icon into two test files — greening three guards over a live outage and removing the last instrument pointing at it. The cure is ADMISSION-SIDE: `boundedText` gains one explicit `emptyOk` option and the single `needIcon` call site opts in, so the two sibling icon validators finally agree. ⭐⭐ **Proven by execution against the real modules, not argued**: refused at HEAD with the exact banked error string, ADMITTED under the cure — so `needIcon` is the ONLY blocker rather than the first of several — and for an already-admitting non-empty icon the HEAD and cured chains are BYTE-IDENTICAL, so no persisted content hash can move and no same-seed golden is reachable. ⚠ **A BEHAVIOUR SHIFT IS DECLARED, NOT DISCOVERED**: confirming a discovered supply chain stops being refused and starts working; that is the deliverable. The persistence-admission grazing stays on the veto surface. ⭐⭐ **It also EMPTIES the estate's ordinary test debt**: its three targets are the entire remaining non-walker population, so `ORDINARY_TEST_CONTROL` converts to the ruled zero-assertion — the census becomes enforcement walkers only, and any new ordinary red is triaged on arrival instead of joining a census. ⚠ SEVEN paths, priced at compile including the conversion, because burning a census row is never a one-path act. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | tightening `resourceIcon` into symmetry (a NARROWING with live-data migration risk, docketed `CR-EST-ICONSYM`); any edit to `inferSupplyChains.js` beyond the comment (the empty literal is pinned by `copyCorruption`); a placeholder glyph or a nullable `needIcon`; `CHAIN_KEYS`, the command schema version and every migration; re-wording `boundedText`'s shared throw message; refilling the converted roster with any name; every other baseline, ceiling, golden and flag |

| EFF-5 | [`EFF-M4`](./packets/infrastructure/EFF-M4.md) | **LANDED** | Member 1 of the `eff-1b` train — the staged successor to `eff-1a`, whose terminal is its base. It repairs stale RECORDS in seven docs files and nothing else. The §68.4 debts: the HB preamble `ARGUED_ROSTER_CEILING` corrected 13 to 19 and stamped at this base, WITH the transition walk that produced it (13 at `7699e367` where the exact-equality arm arrived, then 15, 17 and 19 across HB-0, HB-1 and HB-2 in a single day) so a reader learns the figure rotted by the family own hand; INT provenance un-conflated, because the discipline dates from 08-10 and the FIGURE from 08-14 and the earlier line asserted both of 08-10; GR and INFRA hot-file counts three to FOUR, naming `convergence.js` which joined the standing list on an executed measurement. ⭐ THREE lapsed INDEX Measured blocks restored, not two: `gr-5a`, `int-3b` and — measured rather than assumed, since `git log -S` shows the block is written by the LANDING LANE in its own terminal — `eff-1a`, whose executor missed it one landing earlier and records the lapse in the convention block itself. ⭐ The F9 strike-and-point lands C-WYF-1 Arm A at FIVE build-branch sites rather than the four dispatched: an exhaustive re-grep found a third SOL_QUEUE site carrying no adjacent note at all, the same bare shape the round called the sharpest of six — the under-enumeration the census warned about, found again on the sweep meant to close it. ⛔ One dispatched item is NOT landable here and is routed rather than dropped: `docs/START_HERE.md` is a LEDGER file and does not exist on this branch; its premise is re-verified TRUE at this base (the check chain is 17 steps, derived from `package.json` own script) so the chair ledger sitting acts on a measured figure. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the two `DESIGN_FP_ARCHITECTURE.md` F9 sites (the chair own); the efficiency-volume fold (member 2); `docs/START_HERE.md`; any code, test, script or config; any census motion |

| EFF-6 | [`EFF-M5`](./packets/infrastructure/EFF-M5.md) | **LANDED** | Member 2 of the `eff-1b` train and its terminal member. It re-folds `docs/DESIGN_BUILD_EFFICIENCY.md` whole from the ledger branch, restoring ONE canonical text where the branch had acquired two. ⚠ The divergence is the fold mechanism biting its own tail: `INFRA-M1-DOCS` folded the volume and deliberately deleted the "until then the ledger copy is canonical" clause, because two canonical copies is the drift a fold ends — and the chair then amended the ledger copy with the re-charter redundancy block, the differential caps and the parallel-pre-proof section, so the branch silently reacquired exactly that drift while the build copy own header asserted it was canonical. ⭐ The fold is verbatim and PROVED so by an inverse check: replacing the new Status clause with the old one reproduces the source byte-for-byte. ⭐ The rewritten clause states the folded-volume convention once, so the next divergence cannot be silent — the LEDGER copy is the DRAFTING SURFACE because it carries no gate, and the build copy is CANONICAL AT ITS BASE, re-folded by a docs member whenever the drafting surface has moved. ⭐ It authors no wording of its own: the corrected write-through-symlink sense in the pre-proof section (naming per-member cacheDir isolation as what prevents cache sharing, which this train member 1a proved by execution) was already in the source, so the fold inherits it rather than re-authoring it. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any second file; authoring or amending law text (the body is the chair own); re-touching `PACKET_STANDARD.md`; any code, test, script or config |

| WC-1 | [`WC-0A`](./packets/foreign-policy/WC-0A.md) | **LANDED** | Member 1 of 4 of the `wc-0` train — the WAR-CIRCULATION family's first code, and the estate's first train built on the landed §77/§2.7 machinery. It lands TWO closed vocabularies and nothing else: `peopleLedger.js` (the frozen pools, the residency TAGS, the fifteen counting events, the four tag events, the declared sinks beside the one declared source, the three tag selections, and the `EVENT_SIGNATURES` object the law and the movers BOTH read) and `warStance.js` (the five-rung ladder with an explicit RANK MAP, never array position). ⭐ The block-touching set is DERIVED at module load off `EVENT_SIGNATURES` rather than hand-listed beside it, and its acceptance case asserts the DERIVATION — a fixture built by the deriver can never see a dead arm. ⭐ It is dark-complete by absence of callers: two modules land and nothing under `src/` imports either, pinned by a call-path spy rather than asserted. ⛔ Three STOP-class refutations shaped this member and all three were re-verified binding at this base on byte-identical walkers: a registration wave cannot mint a flag (both WC flags leave with WC-0E, and the §49/§50 obligations are priced but NOT incurred, stated affirmatively rather than by silence); a `spatialUsage` TRACKED row is a live-writer claim (all three leave); and `peopleLedger.js` matches none of the twenty `LAYER_PATTERNS` regexes while BOTH doors sit at exact ceilings. ⭐ The cure is an exact-path WAR regex in this member's own commit, refusing the argued roster because the leaf owns people-conservation outright — the identical shape `INT-3B` landed for `emigreErrand.js` one wave after this packet was compiled, so the argument is now precedent. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any flag or certification row (WC-0E's); any `spatialUsage` row; any writer, ledger or persisted byte; any importer of either leaf; the conservation walker (WC-6); the column-class union (WC-0B); the law-band table (WC-0C); the catalog rows and the census tuple (WC-0D); `.coupling-unlayered-baseline.json`, which is forbidden outright |

| WC-2 | [`WC-0B`](./packets/foreign-policy/WC-0B.md) | **LANDED** | Member 2 of 4 of the `wc-0` train. It widens `migration.js`'s column-class vocabulary and turns a two-way discriminator into a three-way one, in TWO paths and about eight effective lines. ⛔ THE DEFECT IT CLOSES IS THE VOLUME'S OWN NAMED "most likely to have shipped GREEN": `isDemographicColumn`'s docstring promises it FAILS CLOSED toward M4, so a `reinforcement_column` stamped and keyed exactly as the design intends would have been RELEASED by M4 and credited to the destination settlement's population as an ordinary migration arrival — outside arrival/arrive_home/shed, invisible to WC-6's conservation walker, delivering lent troops into a census instead of into a block. ⭐ THREE members, not two: an earlier drafting had no member for an OUTBOUND reinforcement, and since the class is part of the column KEY a classless military column would have collided with a demographic column on the same origin/dest/tick — the exact failure the class-in-the-key discipline exists to prevent, reintroduced by an incomplete union. ⭐ The whole vocabulary lands here rather than at WC-13 because WC-6 needs it seven waves earlier, a wave-order violation the dependency graph had no way to express. ⚠ THE BYTE-IDENTITY CLAIM IS TRUE FOR A NAMED REASON AND THE PIN ASSERTS THAT REASON: the seeded corpus does not move because NO producer stamps a military class until WC-6 mints one, not because the widening is behaviourally neutral — it is not, and B3 is the executed red proof that the guard can be reddened at all. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any producer of a military column (WC-6 mints the first); a second column ledger; the key-collision pin (WC-6, with the first real producer); any new persisted FIELD, which is CR-WC-9's owner-parked class; any edit to the two untouched migration suites, whose green is a verify-at-build claim rather than an omission; any flag, leaf or spatialUsage row |

| WC-3 | [`WC-0C`](./packets/foreign-policy/WC-0C.md) | **LANDED** | Member 3 of 4 of the `wc-0` train. ONE frozen table SHAPE with no values in it, plus the fence that keeps it singular. ⛔⛔ IT CARRIES NO CURVE VALUES, and that is the whole reason the shape lands apart from its content: the volume corrected itself in writing after an earlier drafting landed four curves in the wave whose closing line reads TUNING: none — curves are constants and constants are owner-signature surface. Each curve lands with its MOVER instead. ⭐ THE FENCE IS A CURE FOR A DOUBLE VACUITY, both executed: the volume's authored pattern matches an EMPTY candidate pool, so its non-vacuity arm can never pass (the pattern cannot match its own exclusion list) and its EXCLUDES clause subtracts from an empty set. One device was asked to prove two separable things; this member separates them into a GUARD pool asserted against a named empty list and a CONTROL pool that proves the extractor is alive. ⭐ AND THE C3 CONTRACT IS ALREADY BREACHED AT HEAD, TWICE, BY MODULES A NAME-KEYED FENCE CANNOT SEE: habitCurve.js builds its law-word-keyed curves by fromEntries over a derived order, and espionageDoctrine.js carries two private tables. Both predate WC and both are RECORDED rather than repaired, as frozen legacy rows keyed by exact module identity at a ceiling exact in BOTH directions, so a later dissolution is banked in the diff rather than becoming invisible headroom. ⚠ THE MEMBER IS THIRD BY MEASUREMENT: its argued-roster entry and the leaf that entry names must land in the SAME commit, because the roster arm asserts the module exists. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any curve value, tuning key, band or threshold; the move-vocabulary fence, which HB-1 already shipped; repairing either frozen legacy module, which is another volume's landed code; a third legacy row, which is a STOP and a chair question; `.coupling-unlayered-baseline.json`, which is forbidden outright |

| WC-4 | [`WC-0D`](./packets/foreign-policy/WC-0D.md) | **LANDED** | Member 4 of 4 of the `wc-0` train and its TERMINAL member — it carries the lighting census row and re-derives the whole tuple, so the train's landing commit stays docs-only. Two producer-less term-catalog rows, `amnesty` and `jubilee`, each in its own family: the two exits the volume's own "Debt needs exits" section requires a settlement to be able to write into a peace. They land as seam rows with NO producer entry and no draft-lens membership — the `non_intervention` mechanism verbatim, so no producer means never drafted means byte-identical registration. ⭐ BUT THE NO-PRODUCER RECIPE DOES NOT REACH THE FAMILY LIST, which is DERIVED at module scope: registering two producer-less rows mutates an exported array with no producer involved, which is why this member is a 32-reference consumer census with a catalog edit attached rather than a catalog edit with a note. Two consumers red and both are cured here: the NAMED grown set, and a totality loop demanding a house-voice row for every family across three compliance states — which is what makes the six authored lines mandatory rather than decorative. ⛔ THE VOLUME'S OWN OBLIGATION TO WIDEN THE WR-10 LANDING RECORD IS STRUCK: that record is a LANDING RECORD rather than a policy, its own module says widening it would erase the fact the tripwire preserves, and a mutant proves the refusal is enforced. ⭐⭐ AND THE INERTNESS PIN IS RE-AIMED AT A REASON THAT IS TRUE: the volume's stated ground is refuted, because the market's search reads the AVAILABLE family set and stacks a family with no term on the table anyway. Inertness survives for a different reason — the gating flag is virtual and lit in no preset — so the pin asserts that structural darkness and DECLARES the capability delta rather than claiming inertness. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any producer, draft-lens membership or catalog-row property; widening the WR-10 landing record; touching `peaceTerms.js`, which sits at 797 of 800; a house WORD for either type, the default arm being proved total; any golden re-record, and any movement in the committed settlement hash or the persisted coherence receipt, either of which is a STOP |

| WC-5 | [`WC-0E`](./packets/foreign-policy/WC-0E.md) | **LANDED** | The SOLE member of the `wc-0e` train — a flag wave is a train boundary, so it rides alone. It mints BOTH war-circulation flags and INCURS the §49/§50 three obligations rather than pricing them: the subsystemRowsVirtual ordered-equality pin, the edge-shared bundles, and the literal flag drive. It also lands contributionLedger.js as record shapes plus a fail-closed normalizer with NO writer, and the blocks[] persistence arm. ⛔⛔ THE FRESH PREFLIGHT REFUTED PART OF ITS OWN CHARTER, which is why this packet is compiled rather than inherited: the charter names both the blocks[] fail-closed arm and each flag's by-name gate-read module, and MEASURED those cannot be the same site — normalizeDeployments takes no rules parameter and has none in scope, and it must NOT acquire one, because persistence hygiene has to run on every load or a dark world's saves go un-normalized, which is the fail-OPEN direction. The arm therefore lands UNGATED and contributionLedger.js carries both reads as a two-flag conjunction, the war layer gating the lane and the feature flag gating the ledger — the settlementStrategy warLayer/warTermination shape already live in the tree. ⛔ AND IT CARRIES NO spatialUsage TRACKED ROW: the coverage walker is exact-set in both directions over a live source scan, and the volume charters those rows while saying in the same sentence that this wave makes no writer calls; F-1 is the correction of that sentence, and the three rows travel to WC-1, WC-11 and WC-13 with their writers. ⭐ The bundle count is re-derived rather than inherited: seven artefacts move, and they decompose two-with-real-content plus five metafiles that move on ANY invocation including one with no input change at all. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any setSpatialLedger call or TRACKED row; the credit, shortfall, arrival and close folds, which are WC-1's; threading rules into normalizeDeployments; lighting either flag in any preset; the close and call-in grade vocabularies; any curve, band or threshold |

| RN-1 | [`RN-A0`](./packets/infrastructure/RN-A0.md) | **LANDED** at `a8f09e14eba7cc9f7ab40addce1204cbbbf132da`; do not redispatch | Member 1 of 4 of the `rn-1` relationship-normalizer train, chartered at `OWNER_DECISION_QUEUE.md` §64 and accepted at §101. The ladder's first rung: three module-private identifiers that each collide across modules with DIFFERENT membership are renamed, and nothing else changes. ⭐ The class has no runtime symptom by construction — both modules are internally consistent, every gate is green, and the damage lands later on the reader — which is also why the member is provably output-neutral: all three bindings are `const` and NOT exported, so no importer and no test can observe the change, and the manifest carries no fixture edit and no golden. ⛔ THE SWEEP IS FORBIDDEN BY NAME: `HOSTILE_LABELS` is declared in THREE files and only `conquestDoctrineStage.js`'s moves — `informationStatecraft.js` is a hot file at 780 of 800 and `brokerageServicesRules.js` is out of scope — so the residual two-file collision is left standing DELIBERATELY and the walker arm pins it at exactly two, recording the non-cure instead of forgetting it. Likewise `npcLadderState.js`'s exported `BOND_KINDS` and `beliefMap.js`'s `FRIENDLY_LABELS` KEEP their names. ⭐ §64.4 is discharged by an honest RE-POINT with zero new test files: `postureNameCollision.walker.test.js`'s charter names this defect class verbatim and it already ships the reusable definition/export scanner with guard-the-guard fixtures, so the arm adds three names to an existing mechanism — which also means the expensive lighting census `files` figure never moves and the §102.3 coverage row is already carried. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any edit to `informationStatecraft.js` or `brokerageServicesRules.js`; renaming the exported `BOND_KINDS` or `beliefMap.js`'s `FRIENDLY_LABELS`; any membership change to either renamed Set, which is arm C's row; the header correction, which is arm A1's; every table merge, every routing and every content cure |

| RN-2 | [`RN-A1`](./packets/infrastructure/RN-A1.md) | **LANDED** at `d8236665b1e5e6a4c97a74401724823f94509c6e`; do not redispatch | Member 2 of 4 of the `rn-1` train — the output-neutral structure, and the member that makes `canonicalRelationship.js` the single HOME for relationship spelling knowledge. `relationshipState.js` becomes a pure re-exporter: it re-exports the plane alias table from the home and rebuilds `normalizeRelationshipType` over it, keeping its own Axis-2 and Axis-3 policy byte-for-byte, so not one call site moves. ⛔⛔ ONE HOME IS NOT ONE TABLE — the plane's 8-entry set lands BESIDE the 11-entry regional table, never merged into it; merging their CONTENT is arm B1 and owner-gated, and a merge here would move `normalizeRelationshipType('trade_partners')` and destroy the neutrality claim. ⭐ The proof is an exact-equality identity pin over a 40-input corpus generated from a `git archive` of the pre-feature base, so it cannot mirror the cure, and its NEGATIVE CONTROL is that 17 of the 40 must still DIVERGE between the two resolvers — a cure that accidentally converged the planes reds at the pin rather than at a golden. ⭐ The import direction is measured rather than argued: both modules are zero-import leaves so no cycle is possible, `couplingInclusion`'s census scope covers only `worldPulse|spatial` so `relationships/` can never enter the unlayered baseline, and `applyWorldPulseRelationshipGraph.js` already imports from this exact module, so the road exists. ⚠ THE COMPILE'S SHIM LIST IS CORRECTED BY MEASUREMENT: asking the corpus returns THREE banked shims, not four — `generosityGate.js` carries a one-branch inline fold rather than a table, so a declaration-shaped scan correctly does not see it, and that fold is arm B1's row. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | merging the two tables' content, which is B1's; unifying the per-plane case or empty/nullish policies, both load-bearing on `region/graph.js`; breaking the `normalizeType === normalizeRelationshipType` referential identity; re-pointing any banked shim; every routing and every content cure |

| RN-3 | [`RN-C`](./packets/infrastructure/RN-C.md) | **LANDED** at `196b256a44dd6032e2684ed330aba437553bc91c`; do not redispatch | Member 3 of 4 of the `rn-1` train — the orphan dispositions, and the habitat that let them accumulate. Eight dead synonym members leave six frozen `Set` literals across six modules, and a reachability-per-spelling habitat pin lands so a set that acquires an undeclared member reds. ⭐⭐ THE CORPUS WAS ASKED, PER 67.4, AND ANSWERED CLEANLY: every one of the eight tokens has ZERO producers anywhere in `src/` across six relationship-type-bearing key positions, and none appears in `RELATIONSHIP_SELECTIONS`, so no author can mint one either; the three mentions outside the sets themselves were checked individually and are DIFFERENT vocabularies (a lifecycle receipt kind, an intervention motive, an act class). The survivors are reachable too, which is what stops the pin reding tomorrow. ⛔⛔ AND THE MEASUREMENT FOUND A LIVE DEFECT IT DELIBERATELY DOES NOT CURE: `convergence.js`'s `FRIENDLY_REL` admits `ally`, which canonicalization means can never appear, and MISSES `allied`, which is the live value read raw off a regional-graph edge — so an allied neighbour is not read as friendly by the intervention-motive scorer. Adding `allied` would move GENERATED OUTPUT, which is neither this output-neutral arm's class nor B1's persisted-spelling class; it is a chair surface, docketed `CR-TE18-CONVERGENCEALLIED`. ⭐ AND `ally` IS NOT SWEPT OUT EITHER, though that would be output-neutral and would fit this arm's shape: removing it would leave a set that LOOKS consistent and in which nobody would notice `allied` is missing — the recorded never-delete-the-instrument lesson — so it stays, declared EXPECTED-DEAD with the defect as its written reason. ⚠ SIX production files on 101.2's RECORDED door-1 budget, priced here and never inherited silently, and `convergence.js` is HOT at 798 of 800 with the headroom measured by this lane under eslint's own Linter rather than quoted from the standing list. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | adding `allied` to `FRIENDLY_REL`, or removing `ally` from it; removing `defensive_pact` from any of its four readers; touching `cordial` outside `ALLY_LIKE`, or any test fixture; any membership ADDITION to any set; a size-baseline entry or ceiling raise for `convergence.js`; every routing and every content cure, which are B1's |

| RN-4 | [`RN-B1`](./packets/infrastructure/RN-B1.md) | **LANDED** at `9c8fe6cd`; do not redispatch | Member 4 of 4 of the `rn-1` train and its LAST member — the SIGNED legacy-live content cure, ordered last by 64.2 precisely so its owner-gated content can never strand the structural members in front of it. Five legacy spellings stop reading `neutral`'s numbers under their own contradicting label and start reading what the label always claimed, and three readers that took the persisted label RAW are routed through the canonical resolver. ⭐⭐ THE SAME-SEED PROOF IS EXECUTED, NOT ASSERTED: the full golden suite — 87 files / 623 tests — is byte-identical after the shift, run once when the cure landed and again after the last edit so the green binds to the committed tree. Motion there would have been a STOP, not a fork, because it would refute the producer-less premise the signature rests on. ⭐ THE SHIFT IS DECLARED ROW BY ROW: exactly six of the identity pin's forty rows move, all on the state plane, the regional column not at all, and divergence goes 17 to 15 — `smuggling_partner` NEWLY diverges, correctly, because the regional plane keeps it as a structural type while the state plane collapses the family onto the defaults row it always meant. ⭐ Each routing carries a MEASURED before-state from a git archive of the arm-C terminal, not a recollection: a persisted `coldwar` edge used to be DOWNGRADED to `rival` by the very no-downgrade guard meant to forbid it, and a legacy `trade_partners` link used to yield ZERO dependency candidates. G2's population is FIVE spellings, two wider than 66.2 named. ⛔ AND THE FIRST-PAINT LAW IS HONOURED RATHER THAN WAIVED: `generosityGate.js` is genuinely eager and declares itself import-free, so the routing was only taken after measuring that `canonicalRelationship.js` already sits in the SAME entry chunk — zero first-paint bytes — and re-verified against a real build with the three budget guards RUN rather than skipped. ⚠ `tense` is NOT merged: it is the fifth legacy-live spelling but no ruling determines which defaults row it should claim, and the candidates carry materially different numbers, so choosing one would invent owner-gated content rather than execute it. It is pinned unmapped and raised as an open chair question. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | choosing a target for `tense`; merging any arm-B2 speculative spelling; raising the first-paint budget to fit the import; unifying either per-plane Axis-2/Axis-3 policy; re-recording any golden |

| PRF-1 | [`PRF-1`](./packets/performance/PRF-1.md) | **LANDED** at `97ece4b7`; do not redispatch | The sole member of the `prf-1` train and the goods-matcher RESTORATION 84.2 ruled the review program's highest-leverage repair. ⭐⭐ THE HEADLINE IS HISTORY, NOT CODE: the cure already existed — it arrived 2026-07-02, survived to 07-05, and was destroyed on 2026-07-15 by a master-merge resolution that took the memo-less parent of a 568-conflict merge. Its test file survived that merge byte-identically while its implementation did not, and for six weeks the file's header described a cache the module no longer had. Nothing reported the loss because all five of its assertions PASS against the un-memoized base (measured 5/5), which is the vacuous-pin class exactly. `fuzzyMatch` regains two caches — FUZZY_CANDIDATES, the 219 catalog strings tokenized once at module init in the scan's own nested order, and FUZZY_MEMO, a bounded result cache keyed on `comparable(label)`, the only input the match reads, which is what makes the key sound rather than a guess. No export is added, removed, or re-signed. ⭐⭐ NEUTRALITY IS PROVED AT THE COMMIT, NOT INHERITED: 46,062 differential checks over 2,710 inputs in three orders against a git archive of the base, 0 diffs, thrown messages compared too; the same-seed goldens unmoved at 87 files / 623 tests after the last edit. ⭐⭐ THE GUARD IS THE REAL DELIVERABLE AND IT IS EXECUTED-PROVEN: the pre-cure blob was planted over the cured file and exactly ONE arm of seven reddened — the new alias-snapshot arm — which is simultaneously proof the guard bites and proof the other six could never have caught the merge. Its probe label must stay memo-COLD or the arm goes vacuous. A second detector rides `requiredSymbols`, since the validator resolves symbols against the live tree at every status. Component speedup 69.0x on the tick mix; the whole-tick figure is stated as the Amdahl band 3.2x–5.5x, never as a point. Census 2438/364/2074/20208/5672 with files/parked/credited unmoved; +14 of 15 effective lines, one line of room. 104.4's bundle obligation discharged for both AI closures. ⚠ Measured and previously unpriced: the eager candidate IIFE defeats the tree-shaking that had removed TOKEN_STOPWORDS from both bundles, costing +688 bytes each. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | regenerating the identity fixture from the cured tree; resolving the guard's probe label before the alias push; a 15th effective line; adding, removing or re-signing any export; moving files/parked/credited; re-recording any golden |
| CS-A1 | [`CS-A1`](./packets/correctness/CS-A1.md) | **LANDED** at `75e608ad`; do not redispatch | Member 1 of 4 of the `cs-a` train and the first half of the declared-shift correctness batch 107 signed. The no-replacement tell/speech draw: MANNERISMS and SPEECH_PATTERNS are 30 authored entries each, drawn WITH replacement across a metropolis's ~18 NPCs, so within-settlement repetition is near-certain and the corpus agrees — 120 of 240 settlements repeat a tell, 128 repeat a speech pattern, and 40 of 40 metropolises do both. The cure is four line-neutral edits against machinery that already exists: `drawUnique` in hookVariety is the prior art 80.2's cure-pointer got wrong, it already carries HK-LAW-6 (exactly one seeded roll in every arm), and the settlement-scoped hookRegistry already reaches all three call sites. ⭐⭐ THE NEUTRALITY IS MEASURED, NOT ARGUED: 0 of 32 seeds shift their total roll count, 0 of 240 rosters resize, NPC totals hold at 2207, and the corpus-wide field delta is confined to `personality.tell` and `personality.speech` — name, secret and plotHooks show zero deltas across all 2207 NPCs, which is the independent proof the rng stream is unperturbed. The defect goes to nothing in both fields; secrets and hooks are untouched by design, because 107.2 refused the secrets arm in part — pool width, not draw discipline, is that defect, and it re-charters into gen-2. ⭐ THE PIN IS A TRAJECTORY PIN: the registry accumulator is asserted at EVERY prefix length so a registry that stops filling partway is caught, with a no-registry negative control and a roll-budget equality arm. ⚠ Measured against the compile: the predicted 1350 → 1346 shrink and its owed size-baseline ratchet-down are NOT incurred — the cure measures exactly line-neutral, so the baseline row stays at its floor and the path is absent from the manifest. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the secrets arm (refused in part, re-chartered to gen-2); authoring an unused secrets Set; re-recording any golden; touching the size baseline; widening the draw to any third field |

| CS-A2 | [`CS-A2`](./packets/correctness/CS-A2.md) | **LANDED** at `014a1c60`; do not redispatch | Member 2 of 4 of `cs-a` — the belief silence-decay cure 72.1 confirmed. The silence branch persists a decayed confidence while leaving `lastUpdateTick` frozen, so the next pass decays an already-decayed value over a window one tick longer and the exponent goes triangular: measured at this base the trajectory tracks `0.92^(n(n+1)/2)` EXACTLY at every tick and the belief prunes at tick 9 instead of the documented ~42. Beliefs are forgotten roughly five times faster than the tuning table says, and every downstream consumer — siege awareness, council schism, believed-need scale, ally-intel vetting — is silently mis-tuned. The cure is the uniform one-step-per-pass model 107.2 signed: exactly one decay step per advance pass in both branches, `lastUpdateTick` deliberately untouched because it also gates the fresh-report filter and moving it would silently drop reports that arrived during silence. Prune moves 9 → 43; half confidence moves from tick 4 to tick 9, which is the documented half-life told honestly for the first time. ⛔⛔ THE RULING'S ROUND4 NUANCE IS DEMONSTRATED, NOT INHERITED: the cured curve is EXACTLY the round4-accumulated recurrence at all 42 live ticks and is NOT equal to round4(Math.pow(0.92, n)) — they part company at tick 7 — so a pin written the obvious way would be false. The measured worst-case separation is 1.364e-4, wider than the compile's stated 1.0e-4, and the corrected figure is recorded rather than inherited. The pin drives twelve consecutive silent ticks through the real advance, asserts the whole array against the recurrence, asserts `lastUpdateTick` never moves, asserts the prune tick, and carries the triangular base curve as its negative control. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | a persisted lastDecayTick anchor (a schema act, refused as owner territory); bumping lastUpdateTick; re-recording any golden; writing the pin against Math.pow |

| CS-A3 | [`CS-A3`](./packets/correctness/CS-A3.md) | **LANDED** at `874c642b`; do not redispatch | Member 3 of 4 of `cs-a` — the beaten army's second same-tick battle 72.1 confirmed, and the member where the compile's own cure shape was refuted by measurement. The collision list is computed once while the loop REWRITES the loser's record into a retreat with `position01: 0`, and ground advantage is `1 − position01`, so a still-pending pair naming that army resolves anyway and the beaten column fights again at FULL defender's-ground bonus on ground it has just abandoned. Reproduced at this base: two field_battle entries in one tick and the loser mauled twice, 50 → 27.5 → 15.125. ⛔⛔ THE COMPILE SPECIFIED TWO GUARD ARMS AND ASSERTED NEITHER SUBSUMES THE OTHER; BOTH HALVES ARE MEASURED FALSE. The retreat registration sits lexically inside the same block that rewrites the record, so the two arms are populated together and four built variants — both arms, either arm alone, and a fought-set — produce byte-identical output, which is the recorded redundant-guard-vacuity class exactly. Worse, when retreat routing returns no path NEITHER arm fires and the defect stays live: executed with routing forced to fail, base and the compile's cure both still produce two battles while this cure produces one. ⭐ THE CURE THEREFORE KEYS ON HAVING BEEN BEATEN, NOT ON HAVING SUCCESSFULLY RETREATED, which is what the ruling's own invariant says and what its own trajectory pin demands. The winner is deliberately not registered — 72.1 rules this as the BEATEN army's defect and the position01 pathology is the loser's alone. Three ticks, per-tick assertions, and the routing-unavailable arm is the control that discriminates this cure from the refuted shape. Size measured +3, and +4 for this member and CS-A4 together against the +8 the ruling budgeted. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | widening the existing retreated Set, which has a second consumer in the withdrawal write-back; blocking an unbeaten winner from a second engagement; re-recording any golden; a ninth effective line across this member and CS-A4 |

| CS-A4 | [`CS-A4`](./packets/correctness/CS-A4.md) | **LANDED** at `24c675af`; do not redispatch | Member 4 of 4 of `cs-a`, its LAST tests-moving member, and the one that carries the lighting-census re-record. cs-5 as 72.1 confirmed it: the transit derive loop consults only a missing targetId and never `dep.recalled`, while a retreat record's destId equals its originId and so can never equal the target — the "new campaign" test is ALWAYS true for it, and the loop re-seeds a fresh march at the ORIGINAL commitment tick, which position stepping can carry straight to 1. An army recalled from the field re-appears ARRIVED at the objective it abandoned; reproduced here at ticks 0-2. It is LATENT in the composed pulse because the war layer resolves a recalled deployment as a withdrawal and DELETES the record before the derive pass ever runs. ⚠ THE COMPILE'S LATENCY ROW IS CORRECTED BY MEASUREMENT: the kernel stamps `recalled` ITSELF on every retreating loser (cause field_battle_retreat), so any driver that advances transit WITHOUT the war layer — which includes the estate's own kernel-level fixtures — sees the guard live on every field-battle retreat, not only in the same-tick sue_for_peace / return_home window. ⛔ AND THE COMPILE'S PIN FOR THIS MEMBER IS NOT SATISFIABLE AS WRITTEN: it asks that the retreat record still BE there at tick n+1, but the ledger is rebuilt from deployments every tick, so a skipped deployment yields NO record — asserting preservation would red against the very cure it describes. The pin asserts what the cure guarantees and what the defect needs: no march toward the abandoned target is re-seeded, position never reaches 1, at EVERY tick, with an un-recalled deployment as the negative control. Size lands at 796 for this member and CS-A3 together, four of the eight lines 107.2 budgeted. ⚠ The compile's census tuple was stale — prf-1 moved titles +2 after the compile was written — so the re-record starts from the live 2438/364/2074/20208/5672 and is read from the walker's OWN failure output, never predicted. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | preserving the retreat record instead of skipping the deployment (the audit's alternative, which 72.1 did not rule); predicting the census figures; moving files/parked/credited; re-recording any golden |

| CS-B0 | [`CS-B0`](./packets/correctness/CS-B0.md) | **LANDED** at `c376ec52`; do not redispatch | Member 1 of `cs-b`, and the act 110.1 ruled on lane TE20's one open fork. The CULTURAL belief axis had no seeding path of its own: foldBeliefAxes tested KEY PRESENCE, so a belief first folded before its subject's traditions had materialized wrote observanceLabel null, the key then existed, the ground-truth fallback could never fire again, and the label was latched at null PERMANENTLY — an observer that never happened to hear a rededication held NO belief about a neighbour's rite, forever. ⭐⭐ IT WAS INVISIBLE UNTIL cs-1 LANDED, AND THAT IS THE WHOLE STORY: under the old triangular silence decay a belief pruned every ~9 silent ticks and re-materialized with a null prior, which broke the latch and re-seeded the label from truth. The staleness the D-1c fixture appeared to show was CHURN RESIDUE — a periodic re-copy of TRUTH — not the rumor net working. Curing the decay removed the churn and exposed the latch beneath it, which is why 110.1 refused to truncate CS-A2: the cure is signed and correct and the defect it exposed is downstream and real. The seed now fires whenever the observer has no label yet, key absent OR present-but-null. ⛔ THE STALENESS FEATURE IS UNTOUCHED: a held label still wins unless a fresh tradition_change clears the adoption fidelity, so this decides only where a FIRST label comes from, never whether a held one survives. ⭐ THE PIN IS RESTORED UNCHANGED, WHICH IS THE PROOF: not one character of beliefAxesCultural.test.js is edited, and it goes red to green — a pin written long before, left untouched, is worth more than one rewritten to match new behaviour. Effective lines 110 -> 112 against a 800 ceiling; no test file touched, so the census does not move. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | editing the staleness pin to make it pass; letting ground truth overwrite a label already held; moving either dormancy contract arm |

| CS-B4 | [`CS-B1`](./packets/correctness/CS-B1.md) | **LANDED** at `0242a9c0`; do not redispatch | Member 2 of `cs-b` — cs-3 as 72.1 confirmed it, and 107.3 signed its shape. `severed` is computed for every settlement every tick at upswingKernel :617 and then consulted at exactly ONE site, inside the boom branch that drives the bust; the MINT gate asks only about throughput, centrality and dwell. ⛔ So a town embattled from tick 0 accumulates dwell, mints a boom under live siege, and busts on the very next tick — reproduced here at this base: boom_enter at tick 2, then prosperity Comfortable to Moderate, legitimacy 55 to 52, and a Trade Bust condition at tick 3. The town is a band and three points poorer, carrying a crisis, for a boom it never had. The cure is one token: the mint gate carries the predicate its own bust branch already enforces. ⚠ 107.3 SIGNED THE MINT GATE, NOT THE DWELL ACCUMULATION, and the difference is the whole behaviour: dwell keeps building under siege, so the boom ARRIVES the tick the siege lifts — the siege delayed it rather than killing it — and gating the accumulator instead would reset the clock, which is a different world model and was not signed. ⭐ THE PIN IS A TRAJECTORY PIN because the defect is invisible to any single-tick fixture: at ticks 0, 1 and 2 base and cure agree exactly, so only a driven run with the accumulator on show can tell the story. Six embattled ticks assert the dwell sequence 1 through 6 exactly, that no receipt of any kind mints, that no condition mints, and that prosperity and legitimacy never move; a second arm proves the banked dwell by minting on the lift tick itself; an unembattled control mints on schedule. Both mutants executed — reverting the cure and planting the refused accumulate-gating shape each red the two cure arms and leave the control green. Effective lines 640 to 640, exactly neutral. Explicitly deferred and documented: the audit's E-2 flourishing seam, because widening peace01Of would move the flourishing ledger for every embattled settlement in every campaign. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | gating the dwell accumulation (the refused reading); widening peace01Of; re-recording any golden; a single new effective line in upswingKernel |

| CS-B5 | [`CS-B2`](./packets/correctness/CS-B2.md) | **LANDED** at `ac2d32ac`; do not redispatch | Member 3 of `cs-b` — cs-4 as 72.1 confirmed it. `Math.max(named, before + births - deaths)` reads like the H3 cast protection, but H3 is already structural two lines above: the death draw is taken against the ANONYMOUS POOL, so deaths never exceed before minus named and the max is inert whenever the roster fits inside the head count — which is every ordinary settlement. ⛔ IT COULD THEREFORE ONLY EVER BIND IN THE ONE CASE WHERE BINDING IS WRONG, and there it protected nobody because nobody was at risk; it INVENTED people, breaking the module's own header law 1 and making the receipt line false in the same breath. Measured at this base on a 2,000-head town carrying a 2,600-name roster: tick 1 goes 2000 to 2600 on 2 births and 0 deaths, the history row records a delta of 600 under the reason "2 born and 0 buried", and the herald line reads "counts 2,600 souls this week, 2 born and 0 buried". 598 souls out of nothing in one tick — and then the evidence hides, because ticks 2 through 10 look perfectly healthy. The ten-tick closure residual is 598 at base and 0 cured. The cure is the floor without the mint. ⭐ THE PIN IS A TRAJECTORY PIN because the mint is a ONE-TICK event that then hides: a single-tick assertion that population did not fall below the cast PASSES on the defect, and only the accumulated closure convicts it. ⚠ AN HONEST MEASUREMENT IS RECORDED RATHER THAN GLOSSED: deleting the floor entirely passes the whole file, so the retained min term is provably inert and is kept only because 72.1 rules cs-4 as the mint repair and the narrower edit is the one ruled — the pin asserts the consequence, never the term's presence. ⚠ THE COMPILE'S OWN FIXTURE CANNOT SEE THIS DEFECT: a village of 10 has an expected birth count of 0.0088, so births are 0 every tick, the still-week guard skips the block, and base and cure produce identical runs; the fixture is scaled to a town whose expected births clear 1. Effective lines 191 to 191, exactly neutral. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | deleting the floor rather than clamping it; asserting the min term's presence instead of the closure; re-recording any golden; a single new effective line in demographicsKernel |

| CS-B6 | [`CS-B3`](./packets/correctness/CS-B3.md) | **LANDED** at `d4aa8f25`; do not redispatch | Member 4 of `cs-b` and its LAST tests-moving member, so it carries the lighting-census re-record for the whole train. cs-6 as 72.1 confirmed it, and the act that closes the chair surface RN-C chartered as CR-TE18-CONVERGENCEALLIED. `relType` reaches FRIENDLY_REL RAW off a regional-graph edge via neighborsOf at :822 with no canonicalization, and those edges carry CANONICAL labels — so the set admitted a token nothing in the tree can produce while missing the one that is produced. Executed at this base, and both canonicalizer spellings agree: ally, alliance and allies ALL fold to allied, and the authoring selections offer allied and never ally. ⛔ A SWORN ALLY THEREFORE READ EXACTLY LIKE A STRANGER — measured, the base allied run is byte-for-byte the neutral run: the interventions ledger stays empty at every tick, so no ally ever propped an incumbent and the legitimacy-cheap invited path was unreachable from a treaty. Cured, two allied neighbours commit incumbent-side columns stamped invited, one per contest per tick, and hold them. ⛔ THE PAIRED OBLIGATION MOVES IN THE SAME COMMIT: the habitat pin's declaration table is exact-set in both directions, so the ally row is deleted and an allied row added together; the instrument RN-C deliberately kept is retired only because the gap it pointed at is closed. The non-vacuity population is re-derived rather than inherited — 5 rows to 4, exactly what the compile predicted, and lane TE20's figure of 10 was a grep-LINE count over prose rather than the declaration-row count the arm uses. ⭐ A SET-MEMBERSHIP ASSERTION WOULD NOT HAVE BEEN A PIN: it restates the edit instead of proving the consequence, and would pass on a set that gained allied while some caller still canonicalized on the way in — so the pin drives the real mover and the accumulator is the ledger across ticks, with a neutral negative control and an inert-`ally` arm that FLIPS under the revert mutant rather than merely failing. Effective lines 798 to 798 against the batch's tightest ceiling, two lines, and the cure consumes none of them. Census re-derived WHOLE from the walker's own failure output. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | adding a single effective line to convergence; predicting any census figure; deleting the last EXPECTED-DEAD member; a set-membership assertion in place of the trajectory pin |

| DA-A2 | [`DA-A2`](./packets/display-adoption/DA-A2.md) | **LANDED** at `646abc7d`; do not redispatch | Member 1 as landed of the `da-a` display-adoption train, and the precondition the next member needs. 69.3 sanctions a raw engine tick counter on a DM-facing instrument ON ONE CONDITION: that the allowance is RECORDED IN THE FILE. It never was, on any of the eight. This member records it and does nothing else, for 11 counters across the undo history, the auspice, the chronicler's letter span, the live-war deployments, the timelapse scrubber, the World Pulse header and card, and the two regional impact queues. ⛔ IT LANDS FIRST BECAUSE THE FIX-THEN-GUARD LAW SAYS SO, NOT BY PREFERENCE: DA-A1's widened detector READS these blocks to know which counters are sanctioned, so the record must precede the widening or the 11 would have been forced into the JSX baseline as debt, which 113.3 refuses. ⭐ For one commit they are inert comments; from DA-A1 onward the comment a reviewer reads IS the rule the ratchet enforces, and it reds in both directions, on growth and on a grant that outlives its site. There is no allowlist inside the suite to drift out of step with the source. Effective lines unmoved in all eight, because max-lines skips comments. No test file touched, so the census does not move. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | recording an allowance on a player, public or PDF surface; any rendered prose change; any effective line |

| DA-A1 | [`DA-A1`](./packets/display-adoption/DA-A1.md) | **LANDED** at `d77c8841`; do not redispatch | Member 2 as landed of `da-a`, and the act 113.3 ratified. proseLeak has enforced tick 0 and week 0 across all 552 components since the E-E build; the number was true of what it measured and false of the world. ⛔ ITS DETECTOR REQUIRES LITERAL DIGITS WHILE ITS EXTRACTOR DROPS EVERY INTERPOLATION HOLE, so composed they could not match one live site: reader-facing code writes Tick {tick}, never Tick 12. 29 raw counters in 18 files rendered behind a receipt that said zero, on the public gallery share, the PDF, the map, the town map, the surveyor and the marketing landing page. ⭐ THE FAULT IS LOCATED RATHER THAN GUESSED AT: an arm reproduces the committed baseline EXACTLY, so the extractor is faithful and the detector is blind. Minting a second walker was refused as the redundant-guard-vacuity shape; this de-vacuifies the one that is already load-bearing and already lying. The second surface keeps holes as a NUL byte, which no source can contain and no content can forge; the two detector arms are disjoint by construction so nothing double counts; week k of N stays sanctioned on both surfaces. ⭐ THE ALLOWANCE IS PARSED FROM THE FILE, not from a table in the suite. Nine sites cure, PDF and public first per 69.2, and 20 carry recorded 69.3 allowances; the committed JSX baseline is byte-identical, which discharges the compile's DA.U1 fork by making the re-record unnecessary rather than by predicting it. ⛔ THE ADDITIVE-ONLY CONTRACT IS PROVEN BY THE DIFF, NOT BY SAMPLING: humanizeEngineTokens.js is +27/-0 and jsxLiteralWalk.js is +91/-0, so every export whose prose persists keeps a byte-identical body and no sampling gap can hide a change. tickDurationLabel is a SECOND export because routing a duration through the calendar translator yields Roughly the spring of year 1 ticks from marching; the pin asserts the two DISAGREE at every span in a year. 69.3's float half lands with it: the war-weariness bands take the SCREEN's own authored cause sentences and the posture strip takes nothing, because the view model records no cause for aggression. 104.4 is paid once and executed rather than assumed: both AI bundle freshness suites redded on the chokepoint edit and pass after the regeneration in the same commit. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | a single deleted line in humanizeEngineTokens.js; a tick or week entry in the JSX baseline; any behaviour change in extractJsxProseStrings or scanJsxTree; a duration routed through tickCalendarLabel |

| DA-A3 | [`DA-A3`](./packets/display-adoption/DA-A3.md) | **LANDED** at `a87b904c`; do not redispatch | Member 1 of the `da-b` train, and the act 122.3 sharpened. relationshipColors.js called itself the SINGLE source of truth while SEVEN duplicate tables were live. ⛔ SIX WERE MERELY INCOMPLETE; ONE WAS WRONG ABOUT WHICH RELATIONSHIP IT WAS SHOWING, and the paid PDF rendered it: pdf/theme.js and SettlementCard held the same table, in which allied carried canonical TRADE_PARTNER's hue, trade_partner carried a hue in no canonical table at all, rival and cold_war both collapsed onto hostile, and patron collapsed onto client. Five of nine types wrong and three of them mutually indistinguishable. ⛔ MEANWHILE THE jsPDF EXPORTER BESIDE IT PAINTED THE CANONICAL SET, so one product shipped two PDF systems that disagreed with each other about what an alliance looks like. The other four homes carried canonical VALUES but each omitted vassal and criminal_network, so both live edge types fell to neutral grey everywhere but the map. All seven are deleted and every surface calls relColor/relRgb; REL_RGB's orphan status ends here because the jsPDF hand-kept copy is exactly what it was written for. The overclaiming header is cured IN THE SAME PACKET and recorded as history rather than quietly corrected. RelationshipsTab:199 converges - it disagreed with the table thirty lines above it in the same file and the compile found no recorded intent, discharging 67.2's own condition. secret_alliance stays EXPECTED-DEAD with its authored label and no palette row. ⭐ THE WALKER GOES FROM THREE SPOT CHECKS TO EVERY SURFACE and reads SOURCE, because importing and comparing values cannot see a copy at all: no table survives, every surface reaches the module, both channels are bijections with the ten canonical types, every type is DISTINCT, allied is asserted not to be trade_partner by name, and the two channels agree type for type. ⚠ THE DETECTOR CARRIES A POSITIVE CONTROL AND IT EARNED ONE: a first cut matched a bare bracket and convicted twelve rows in two files that HAD converged, an effect-category list and a dash pattern, so the triple must be three integers. Net -22 effective lines; SettlementDetail 587 to 583, exactly the predicted -4. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | adding secret_alliance to the palette; leaving any table in place temporarily; two canonical types resolving to one hue |

| DA-A4 | [`DA-A4`](./packets/display-adoption/DA-A4.md) | **LANDED** at `02c7f952`; do not redispatch | Member 2 of `da-b`. Measured at base: 24 explicit en-US renders against 3 explicit en-GB, so the dominant convention agrees with the locale the Wave-4h ruling already pins and the trio is the outlier - the same product rendered a saved-on date as 1 Jan 26 on the card and Jan 1, 26 a click away. VersionDiffView and VersionsTab flip; the option bags are untouched so only field order and separators move. ⭐ THE THIRD OUTLIER IS NOT HERE: SettlementCard rides DA-A3, which owns that file for the palette convergence, because splitting it out would put two live packets on one change path and the validator convicts that by name. ⚠ ONE PIN MOVED AND IT IS NOT A GOLDEN RE-RECORD: settlementCardSignals asserted the en-GB FIELD ORDER, so it is re-keyed to the en-US order; the pin's subject is unchanged, a real formatted date rather than the literal Invalid Date the suite exists to forbid. The maximal reading - one shared display-date helper across all 24 sanctioned sites - is deferred with its reason rather than dropped. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | touching any of the 20 sanctioned explicit en-US sites; introducing a bare or undefined locale; weakening the Invalid Date assertion |

| DA-B2 | [`DA-B2`](./packets/display-adoption/DA-B2.md) | **LANDED** at `8820dde2`; do not redispatch | Member 3 of `da-b`, and 113.2's signed correction. A bare toLocaleString reads the HOST locale, so one seeded world rendered 8.000 to a de-DE reader and 8,000 to an en-US one. Eight display sites did it, and 69.2 called them the eight formatCount sites - but THREE ARE DATES, not counts, which is why J-TC21-2 re-keyed the member BY NATURE: five counts to formatCount, three dates to an explicit en-US. ⭐ Cover.jsx was not even a locale bug but a DOCUMENT CONTRADICTING ITSELF: num() returns an ungrouped String(Math.round(n)), so the PDF cover read 8000 while Overview.jsx in the SAME DOCUMENT read 8,000 through formatCount. ⛔ THEN THE GUARD, AND THE OBVIOUS EXTENSION IS THE WRONG ONE: adding the two display trees to the sim-path TREES list reds 33 files, 26 of them carrying only the explicit en-US renders the Wave-4h ruling expressly sanctions - the exact red-a-green-tree failure 69.4 exists to prevent, because the display layer is where locale formatting LEGITIMATELY enters. The display arm bans the BARE FORM only: no locale argument, or undefined as the first argument, which is the same host read in a costume. ⭐ THREE ARMS BECAUSE A SOURCE-SCAN NEGATIVE ALONE PROVES NOTHING: a positive control that catches both bare spellings and ADMITS the explicit one, the ban itself, and an ANTI-VACUITY arm asserting the sanctioned explicit form is still PRESENT in those trees - without it, zero bare calls would keep passing after somebody deleted the whole class. ⚠ RealmForecast:162 is cured HERE rather than in DA-B3: the compile parked it there only because that member owns the file for the worker seam, and 113.2 requires all eight to cure or the guard reds on arrival. DA-B3 takes the file in da-c, where this landing has released the reservation. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | extending the guard by adding the display trees to TREES; touching any of the 26 sanctioned explicit en-US renders; landing the guard before the eighth site cures |

| DA-B1 | [`DA-B1`](./packets/display-adoption/DA-B1.md) | **LANDED** at `1b5145fc`; do not redispatch | Member 1 of the `da-c` train, and the act J-TE22-4 deferred out of DA-A1 so the lexicon would land WITH its consumers instead of as unread data. Four residual leaks each re-implement the estate chokepoint one line at a time: WizardNewsPanel routes the typed kind through a FILE-LOCAL human() that handles underscores ONLY, so a camelCase token walks straight past it; ChronicleTab runs enum replacement on a HEADLINE slot; EconomicsTab renders a raw chainId beside a raw status token; DossierHeaderRow renders a raw tradeRouteAccess. ⭐⭐ THE CORPUS WAS ENUMERATED BEFORE THE TABLE WAS AUTHORED, AND IT REFUSED TWO OF THE FOUR CLASSES: news kind measures ~200 distinct tokens across 415 emit sites in worldPulse, and chainId is DERIVED from node uids, so neither has an enumerable domain and both take the general humanizer instead. A lexicon written to what merely LOOKED closed would have rotted on the next emitter. The two that did enumerate get authored labels read off the producer own source — tradeRouteAccess exactly five values from the generator terrain pools, supplyChainStatus the five legacy values the schema records — and the tradeRouteAccess bucket is PINNED AGAINST resolveConfig.js RATHER THAN AGAINST A COPY OF ITSELF, so a sixth route reds instead of silently falling through. ⭐ isolated earns the lexicon outright: the word says nothing about TRADE, which is the only thing it means; and no label invents a fact, so vulnerable stays near its token because inferSupplyChains sets it for a chain that is DISCOVERED-BUT-UNCONFIRMED. ⛔ THE CHRONICLE TITLE IS A MIXED SLOT and the plain humanizer would DAMAGE it — chronicleFeed fills it from raw.title || raw.label || raw.name || raw.type || raw.kind, so lowercasing the slot lowercases every authored headline; humanizeIfToken tests structurally instead. Same-seed NEUTRAL structurally rather than by sampling: +101/−0 on the chokepoint, so DA.M13 persisted-prose hazard cannot be reached. ⛔ THE NAMED STOP IS SPENT EXACTLY: EconomicsTab 599 → 600 against a 600 ceiling, measured with eslint own Linter, and the file now sits AT its ceiling with zero remaining headroom. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | a bucket for news kind or for chainId; sweeping the remaining 42 replace(/_/g sites (J-TC21-6); any behaviour change to a pre-existing chokepoint export; importing the chokepoint into an eager first-paint closure |


| DOM-1 | [`DOM-1`](./packets/documentation-truth/DOM-1.md) | **LANDED** at `77111b58`; do not redispatch | Member 1 of the `dom` train, and the act 87.1 ruled. Eight documented machine figures had rotted at once: the gate ran seventeen stages while README said fourteen, the per-test census held eleven entries across six files while ARCHITECTURE said 49-across-34 and CONTRIBUTING said 35, the suite was described as ~20,100 tests / ~1988 files against 2438 real test files, worldPulse was ~378 against 410, the migration head was 192 against 195 with the production gap 71 against 74, and both typecheck headers placed the ratchet at step 9 of a 14-step chain when it is step 12 of 17. A ninth site was a citation that RESOLVES and is still wrong: the aiGroundingBundle sentence pointed at the analyticsEventsBundle enforcer, so enforcedByExists passed on it. THE RULE THIS MEMBER LANDS BY IS DERIVE-OR-DELETE, and it is not a preference: the compile own recommended replacement number died inside the compile window, the baseline moving from sixteen entries to twelve to eleven while the plan was being written. Re-typing is the same bug one iteration later. Four new pin arms derive what survives, and the NAV arm covers the class that rotted UNDER AN EXPLICIT OWNER DIRECTIVE, the home route losing its nav block on 2026-08-03 while the doc kept listing Welcome. THE SUITE-SIZE ARM WAS REWRITTEN RATHER THAN EXTENDED: it required the figure to EXIST and checked only the files half inside a 25 percent band, which is exactly how a 7,800-test understatement survived, so the figure is deleted and the arm now enforces its absence behind a positive control. applied-head.json is deliberately untouched, its verification prose being a dated evidence record rather than a claim. This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | re-typing a cured figure; editing applied-head.json; opening docs/DEPLOY.md, which belongs to DOM-2 alone; widening a pin band to fit the tree |

| DOM-2 | [`DOM-2`](./packets/documentation-truth/DOM-2.md) | **LANDED** at `3aef7b7a`; do not redispatch | Member 2 of `dom`, AUDIT-ZERO and the launch blocker of 88.4. The runbook listed seventeen required secrets while the deployed edge functions read FIFTY-THREE distinct environment names, so an operator who sets exactly what the doc lists ships code that reads thirty-five of them as empty strings and meets the first failure through a paying user. Measured word-bounded at base: 26 consumed names have zero hits anywhere in docs and 24 of those are absent from .env.example too. STRIPE_PRICE_SURVEYOR is the sharpest, resolving to empty and throwing Price ID not configured for surveyor at the first Surveyor purchase. THE SCAN NEEDS THREE ARMS BECAUSE THE CODE HAS THREE SPELLINGS and a literal-only detector ships this member whole point vacuous at 47 of 53: the const-resolved arm is the only one that sees the three durable-worker cron secrets, and the injected-getter arm is this lane own finding, correcting the compile 50 to 53 by catching the mail adapter which takes its env reader as a parameter. THE CONST ARM IS ALSO WHAT MAKES THE ONE DELIBERATE EXCLUSION EXPRESSIBLE: the runbook reasons in prose about OPERATOR_MESSAGE_CRON_SECRET shipping unset, and a scan that never sees the name cannot carry an allowlist row for it. The two names 120.3 added, ANALYTICS_HASH_PEPPER whose absence silently kills device-actor linkage and EXPORT_SHARED_SECRET, are picked up BY CONSTRUCTION rather than by listing. A planted-secret positive control proves all three spellings read and that a test double stays out, and a non-redundancy control keeps the extra arms earning their keep. THE PIN RUNS ONE DIRECTION ONLY, consumed within documented, with the reverse recorded as a deferral because DOM-3 lands behind this member and would red a bidirectional pin on the founder price line alone. This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | shipping the literal arm alone; an allowlist row without a stated reason; widening the allowlist instead of writing the runbook; making the pin bidirectional here; writing any secret VALUE into the repo |

| DOM-4 | [`DOM-4`](./packets/documentation-truth/DOM-4.md) | **LANDED** at `612d4b10`; do not redispatch | Member 3 of `dom` in landing order, minted by 115.3, and the member whose own ruling MEASUREMENT REFUTED. The onboarding coach told every new user that the rail shows the fourteen steps the engine took; 115.3 minted this to change fourteen to the measured 22. The sentence does not describe the PIPELINE, it describes THE RAIL, and PipelineReveal renders the run history filtered to entries that carry a user-facing label. Three populations, none of them 14: steps registered 22, steps with a label 15, steps a reader can actually count on screen at most 15 and varying with the settlement. Writing 22 would make the copy MORE wrong from the reader seat. So the cure is the derive-or-DELETE rule applied to a figure that cannot be derived at all because it is not one number, and the coach adopts the sentence the same file already uses one screen later, which has never been wrong for any reader on any run. The pin asserts the three populations really do disagree, so a later lane cannot re-add a count believing it safe, and a control feeds the counter the exact prose that was wrong. It lands and flips BEFORE DOM-3 because both open src/copy/en.js and a non-terminal packet reserves its change paths. Note the pin file is PARKED in the lighting census, so four executing pins move that census by zero. This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | putting any number back into the coach sentence; widening the pipelineSteps label map to 22 to make an old sentence true; sweeping ASSESSMENT.md or journeyProgress.js here |


| DOM-3 | [`DOM-3`](./packets/documentation-truth/DOM-3.md) | **LANDED** at `cea076f5`; do not redispatch | The paid-surface member, LAST BY DESIGN so the train prefix stands lawful if it stops, and the one 115.2 HELD on an owner-side precondition. PricingPage.jsx carried a prior lane deliberate deferral IN THE FILE: abolishing the purchase path is gated on a build-time never-sold verification against the seat ledger and purchase history, and renaming the destination is not the same act as removing the door. That check cannot be run from the repo, and the available inference (migration 137 dark, appliedHead 121, the webhook train undeployed) is strong but is NOT the verification, because DESIGN_FOUNDERS_HALL 1 FORKS on it: any seat ever sold, test purchases included, grandfathers its holder with a different copy set. The owner ran the check and attested 2026-08-15, so the fork resolves to the never-sold arm and the grandfather arm is dead; the in-file note is rewritten to cite 118 rather than deleted, so the discharge is legible where the deferral was. THE CTA READS Request a chair AND NAVIGATES to the already-built 5b letterbox, satisfying 5 and 5b at once while REFUSING the one shape that costs a chunk edge; and its kind is never purchase again, which is load-bearing rather than cosmetic because the loud-primary selector requires kind purchase. Checkout fails closed through an explicit ABOLISHED_PRODUCTS set consulted BEFORE the catalog, because a bare deleted row gives an absence pin nothing positive to hold and a future re-add would silently restore the sale; the 30-seat gate goes with the sale it guarded. THE WEBHOOK IS UNTOUCHED and the pin asserts its branches are STILL THERE: they are the inbound refund and replay path, and deleting them would strand a historical session. TWO LIVE SURFACES THE COMPILE NEVER NAMED were found and cured: FounderTile carried a SECOND checkout with its own price arithmetic and a retry loop, and the marketing front door rendered N of 30 seats left, scarcity vocabulary for a thing never on sale. The tile retry is NOT rewritten to retry a navigate, because a link cannot fail and that would be invented behaviour; the seam stays mocked and must never be called. This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any founder seat found ever sold; editing stripe-webhook; dropping ABOLISHED_PRODUCTS for a bare deletion; deleting founderTileRestore.test.jsx; rendering the letterbox inline on the pricing page; touching the owner-parked seat-TRANSFER product |The status in this table and the status inside the packet must agree. A mismatch
| DCS-1 | [`DCS-1`](./packets/documentation-truth/DCS-1.md) | **LANDED** at `77afa6c1`; do not redispatch | Member 1 of the `dcs` micro-batch train. Four rulings were signed into the queue and never reached the standard packets are written against, so every compile since has had to rediscover them — three of the four the expensive way, at a terminal. The compile MEASURED the gap rather than assuming it: of the five texts the dispatch named, exactly one (the census-burn law) is already present with its authority cited and is left untouched. The other four are absent. 101.4 replaces a DEFECTIVE sentence: `requiredSymbols` currently reads "preserves or creates", and the validator resolves those rows against the live tree at EVERY status, so a promotion naming a symbol its deliverable has yet to write cannot pass its own commit — the estate already practises the cure (promote what is preserved, add what was created at the flip) and only the wording lagged. 102.3 adds the third mint class: a new `tests/lint/` file owes its mutation-coverage row, priced at compile. 103.2 gets its own section — an arm that cleans PERSISTED state runs unconditionally, because gating persistence hygiene behind a flag is the fail-OPEN direction. 104.4 gets its own section generalising the bundle-closure law past the one file the existing flag-mint clause names. ⚠ This member writes `docs/**.md` while naked-claim debt is PER CLAIM, so every added body line is screened with the file's own live regex before the commit. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | re-landing or re-wording the census-burn text; renumbering or reordering any other section; adding any figure to a standard whose own rule forbids per-wave numbers |
| DCS-2 | [`DCS-2`](./packets/documentation-truth/DCS-2.md) | **LANDED** at `1b09d453`; do not redispatch | Member 2 of the `dcs` micro-batch train. laneSTS consolidated nine volumes of stale-site corrections; seven were applied against ledger-branch contents and ES/IN were routed here for a measured reason — their anchors sit inside amendment blocks the ledger copy does not carry, so applying them there would either miss every anchor or invent the surrounding block. Re-verified at this base rather than inherited: both volumes are byte-identical to what laneSTS recorded and the ledger copies still lack the amendment blocks, so C-TLF-12's build-branch routing stands. Seventeen blocks, eighteen edits. ⚠ Three apply hazards, each dispositioned: ES-5 and ES-8 are INSERT-AFTER with POSITIONAL anchors and no OLD body (the chair-placed class — ES-5 becomes its own bullet between the C1 and C2 amendment bullets, ES-8 lands one line past the extract, where the anchor's sentence actually completes); IN-4's anchor hard-wraps differently from the live bytes; IN-8 is two in-row fragments in one block. ⛔ THE FIGURE RULING IS THE MEMBER'S REAL CONTENT: the rows being cured are exactly the rows that rotted by carrying a count, and IN-8's own proposed replacement quotes a routed-token figure that is ALREADY STALE at this base — so IN-5 and IN-8 land as DERIVATION POINTERS naming set membership and instructing a fresh count at build, and this member writes no count at all. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the ledger copies (the fold mirrors them per D-X-5); any block outside the seventeen; writing the live routed-token count in place of the stale one |
| GV-1 | [`GV-1`](./packets/infrastructure/GV-1.md) | **LANDED** at `8fb01642`; do not redispatch | Member 1 of the `gv` micro-batch train, and the train's SECURITY member. The BYOK never-logged scan asserted its universally quantified negative PER PHYSICAL LINE — both the sink pattern and the decrypted-key pattern had to match one line — so prettier's multi-line call form, which is the likely shape for a long argument list and therefore the shape a leaked secret travels in, split them across lines and neither half matched. The offender list came back empty and the guard passed while the leak shipped. ⚠ TWO SITES, NOT ONE: the same shape sat at the ai-analyst loop and again over the surveyor-byok MANAGEMENT surface, so curing one would have left the other blind to the identical form. The cure windows each sink match to its STATEMENT, bounded by the call's own brackets so it can never sweep in a neighbouring statement — a restored dimension, not a widened claim. One implementation in the estate's existing fail-closed extractor chokepoint, two call sites. ⛔ A cure FOR vacuity that ships vacuous is the failure mode by name, so five control arms ride with it, and the load-bearing pair is a multi-line fixture the statement scan MUST catch and the pre-cure per-line shape MUST miss — that blind shape is exported on purpose, marked never to be called from a live guard, so the difference is executable rather than argued. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | widening either regex vocabulary; the migration and table assertions below the scans; narrowing the window back to one physical line |
| GV-2 | [`GV-2`](./packets/infrastructure/GV-2.md) | **LANDED** at `2cf1c71a`; do not redispatch | Member 2 of `gv`. The single-session census asked two PRESENCE questions joined by and — does the file import the shared session gate, and does the token appear anywhere — and called that coverage. Presence is not enforcement: a call wrapped in a false conjunct satisfies both halves, evicts nobody, and passes all twenty arms. ⛔ THE STAKES ARE THE FIVE MONEY SURFACES at the roster's tail, which spend no credits and therefore have NO database belt behind them; this scan is their only enforcement and the guard's own comment says so. The cure binds the call to its consequence: it must sit in the condition of an `if` whose consequent answers 401, and no conjunct of that condition may be a falsy literal. Four shapes are enumerated in-file BY NAME and admitted by ONE rule rather than four special cases — and they were read off the sixteen live surfaces before the rule was written, which is why the stricter predicate lands with the whole roster already passing. A shape not in the list is a red, never an allowlist row. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the roster, the spend-belt check, the deferred set, the founder-transfer binding assertions; any per-function allowlist |
| GV-3 | [`GV-3`](./packets/infrastructure/GV-3.md) | **LANDED** at `ce089e2b`; do not redispatch | Member 3 of `gv`. The parity arms read the step TOKEN out of a CI run body, so a step suffixed with a swallow-the-error idiom still yields its token: parity holds perfectly while enforcement is gone, the step runs, fails, and the job reports success. The direct-runner scan cannot see it either, because it SPLITS on the very operator the escape hatch uses and discards it as a delimiter before matching anything. A CI gate can therefore be disarmed without moving a single existing assertion. The cure scans the five gate jobs for the three shapes that disarm a step while leaving its token in place. ⚠ ITS FIRST SPELLING CONVICTED ci.yml IMMEDIATELY, on a dev-dependency audit that is non-blocking BY NAME and is not a gate step at all — the signed fork governs, so the scan is scoped to steps whose own command drives a package script, the exempted line is quoted in-file, and the narrowing is pinned in BOTH directions rather than keyed to a job name that would have hidden the next real instance. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the presence dimension; any allowlist keyed by job or step name; jobs outside the five gate groups |
| GV-4 | [`GV-4`](./packets/infrastructure/GV-4.md) | **LANDED** at `ccc424b2`; do not redispatch | Member 4 of `gv`, and the train's CENSUS HOLDER. The walker pinned that every written ledger key is classified as tracked or exempt, and never that a TRACKED key reaches an emission — so the mover row can be deleted and every arm stays green while the telemetry emits nothing for that key. The recorded hazard has an executed receipt: deleting a mover row leaves this walker and two sibling suites entirely green. The cure asserts a TOTAL mapping onto live mover rows, in both directions, plus a stale-row check. ⚠ SOURCE-SCANNED RATHER THAN EXPORTED: the mover array is a local const, and exporting it would widen a module's public type surface for a test's convenience — the recorded size-ratchet lesson — so the member opens one test path and never touches src at all. ⚠ THE KEY-TRANSLATION TABLE IS THE REAL CONTENT, because mover rows are keyed by the DERIVED signal name rather than the ledger key that feeds them, so a bare set comparison would mean nothing; two mover rows carry no tracked key of their own and both are recorded with their reason so a new unexplained mover reds instead of drifting in. Controls run on FIXTURES, never by mutating the live module mid-run. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | re-classifying any key; the exempt list; exporting the mover array; opening src/lib/spatialUsage.js |
| GVF-1 | [`GVF-1`](./packets/infrastructure/GVF-1.md) | **LANDED** at `3d8ea784`; do not redispatch | Member 1 of the `gvf` micro-batch train. The raw-color ratchet carried TWO defects, and the second is why the first survived four landings. Its budget was asserted with a less-than-or-equal, so a shrink is invisible: the da and dom trains retired 53 literals and nobody lowered the ceiling, leaving seventy literals of silent headroom for net-new raw color to ship into. And the counter reads Literal nodes only, so a hex written inside a template — which one live component carries in a style object today — was invisible to every ceiling this file ever recorded. Both numbers are re-recorded as EXACT equality and the template population gets its OWN constant on its own line, so a future movement stays attributable to the population that moved rather than vanishing into a shared total. ⛔ BOTH FIGURES ARE MEASURED AT THIS BASE BY RE-EXECUTING THE COUNTER, never inherited: the compile's own count is non-authoritative by its own fork, and the two historical figures are forbidden outright. The counting core is lifted so it takes a SOURCE STRING, because a counter that can only run over the whole repository can never demonstrate on a fixture that it counts what it claims to. The RuleTester half is untouched. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the RuleTester half; the eslint rule itself; tokenizing the live template hex; typing either number by hand instead of measuring it |
| GVF-2 | [`GVF-2`](./packets/infrastructure/GVF-2.md) | **LANDED** at `52e0147a`; do not redispatch | Member 2 of `gvf`, and the train's structural fold. The gv train cured five guards of two recurring shapes and nothing stops the sixth, so both shapes fold into the standing anti-vacuity walker as rules: a conjunction requiring two probes to match the same PHYSICAL line, and a guard proving a gate is CALLED without proving its result is consumed. ⛔ THE SCOPE DECISION IS THE MEMBER'S REAL CONTENT, and the compile priced only half of it. Three of the five convicted guards live outside the walker's scope, so the fold rules are widened to reach them — but running the walker's THREE EXISTING rules over those same additional files reports SEVEN pre-existing hits, which would force a choice between a red gate and exemption rows this file's own header calls silencers. The resolution is a PER-RULE scope: the older rules keep the population they were triaged against, the fold rules get the wider one, and the seven are recorded in-file by name and by rule as a measured scope boundary rather than exempted, because those rules do not scan there. ⚠ A THIRD instance of the same-line shape, guarding an OAuth token, is cured in this commit rather than allowlisted. ⚠ AND A LATENT DEFECT IN THE SHARED SKELETON WAS FOUND AND CLOSED: an arrow function put a character before the slash that the regex-trigger set lacked, so the opening slash read as division and the next one blanked the real code after it — the new rule would have shipped blind to the most natural spelling of its own defect. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the three older rules' scope; any exemption row for the seven; opening byokNeverLogged.test.js or sessionGateCensus.test.js |
| GVF-3 | [`GVF-3`](./packets/infrastructure/GVF-3.md) | **LANDED** at `c6d2a139`; do not redispatch | Member 3 of `gvf`, and its second SECURITY member. The admin user-search sanitiser strips PostgREST logical-filter metacharacters so caller input cannot break out of the or-expression, and its own comment scopes itself to exactly that; it says nothing about the two SQL LIKE metacharacters, which pass straight through into both ilike patterns. THE HARM IS PRECISION RATHER THAN VOLUME: the query is already capped at a hundred rows, so a bare wildcard returns what an empty search returns — what the metacharacters buy is a PATTERN ORACLE, letting an elevated admin probe the profile table for email SHAPES instead of searching for a string they already hold. Both join the stripped class. Stripping is the AVAILABLE cure and not merely the simple one: the ilike operator exposes no in-value escape, and the sanitiser already strips backslash, so an inserted escape sequence could not survive its own pass — and the asterisk has always been stripped here for the same reason, being an accepted alias for one of the two. ⚠ A behaviour change is stated rather than left silent: a literal underscore in a search no longer matches. The pin DERIVES the sanitiser's own character class out of the source and executes it, because a hand-copied replica mirrors the deriver and could never see the producer drift away from it. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the break-out class already in force; the redacted column set; the email masking; the row cap; any other action in the function |
| MB-1 | [`MB-1`](./packets/infrastructure/MB-1.md) | **LANDED** at `cf0c7530`; do not redispatch | Member 1 of the `mb` micro-batch train, and its engine seam. ⛔ THE TRAIN LOST A MEMBER BEFORE IT STARTED: the delete-side back-link scrub was graded measured-true by the compile and is REFUTED at this base — both Library delete paths already scrub survivors and persist through the batch RPC, and all seven delete sites were enumerated — so that member is a premise death, the store slice is never opened, and the signed exact-ceiling fork that was the batch's most likely death is never reached. This member moves the realm forecast off the main thread: it runs a full multi-tick interval through the orchestrator synchronously, while the estate already owns the worker transport the committed advance uses, so the one operation a user runs speculatively and repeatedly is the one that freezes the UI. The cure injects the runner with the in-thread orchestrator as its DEFAULT, which is what makes Node, vitest, SSR and every existing caller unchanged by construction rather than by assertion. ⚠ THE DOUBLE-INTERNAL-CALLER TRAP IS THE MEMBER'S REAL RISK: the forecast entry point calls the run function TWICE, baseline and counterfactual, and threading only the baseline leaves the more expensive half on the main thread while NOTHING about the returned value differs — both paths compute the same bytes, so only counting the runner's invocations can see it, and that is what the pin counts. ⚠ The transport is INJECTED rather than imported, so the domain module gains no library edge and the coupling registry stays unincurred. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | a new worker file; the forecast's compute, drain order or no-commit discipline; citing the mocking component suite as evidence for the seam |
| MB-2 | [`MB-2`](./packets/infrastructure/MB-2.md) | **LANDED** at `f6b79ad3`; do not redispatch | Member 2 of `mb`. The persisted service-toggle bag was orphaned by a key change; the device-blob path was cured at rehydrate, and the SAVE-LOAD path never was. The panel hands a loaded save's bag straight to the writer, which stored it raw — and that write lands AFTER rehydrate has already run, so nothing could heal it and a returning user's preferences silently stopped applying, forever, with no error. The cure routes the WRITER through the normalization, because asking each caller to remember is exactly what failed: the one caller that mattered did not. The pass is pure and idempotent, so a current-form bag normalizes to itself. ⚠ A BEHAVIOUR CHANGE IS STATED RATHER THAN LEFT SILENT: legacy-keyed saved preferences start applying again, which is the cure's whole purpose. ⚠⚠ AND A VACUITY THIS CURE WOULD HAVE CREATED WAS CAUGHT INSIDE THE BATCH: the existing hydrate pin seeded its raw bag by calling the very writer now being normalized, so its seed would have arrived pre-normalized and the pin would have passed even if the hydrate action did nothing at all — it would have stopped discriminating at the exact moment it stopped being able to fail. The seed is planted through setState instead. ⚠ The registry half is DEFERRED with its true price re-measured: re-pointing is not the one-path act the compile costed, because a lifecycle suite source-scans the direct call in the rehydrate window and pins both halves of it. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the rehydrate wiring; the registry row and its shrink-only ceiling; the scope suite |
| MB-4 | [`MB-4`](./packets/infrastructure/MB-4.md) | **LANDED** at `b3f80e9a`; do not redispatch | Member 3 of `mb`, the two riders. The Founder band still read a purchase kind in THREE places — a disabled condition, the import that only fed it, and a variant ternary — after the paid path was abolished and the pricing page's own comment forbade that kind outright. ⚠ THE VARIANT IS REMOVED AS A WHOLE DEAD CONDITIONAL RATHER THAN BY DELETING THE NAMED ARM, and that distinction is the rider's only real risk: the condition is always false, so the rendered variant is always the quiet one, and deleting just the purchase half would have left the primary-election prop able to elect a band the file's own comment says it can no longer elect — a behaviour change wearing a cleanup's name. No band pin asserted the dead branch, checked rather than assumed. The second rider qualifies a header that claims a one-to-one archetype mapping: the ruling closed that gap BY MEASUREMENT and the silence is deliberate and machine-pinned, but the bare sentence invited a reader to assume full coverage, so the qualifier records that the mapping describes the KEYING and points at the ruling. Comment-only. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the seat meter and its copy; the prop contract; the delta table or its anti-drift pin |
| TM-1A | [`TM-1A`](./packets/infrastructure/TM-1A.md) | **LANDED** at `52f177d1`; do not redispatch | Member 1 of the `tm-core` train — the launch-critical telemetry half of the tm+sk family. The product event-class map is DERIVED one-to-one from the product event object, and that object ships to the client and to the edge, so a third class value cannot exist without eleven client-eager names no client can ever emit, eleven unpriced metrics-registry coverage rows, and a five-bundle edge regeneration — which contradicts the telemetry charter's own zero-eager-bytes wall. The chair ruled that the simulation vocabulary lives in its OWN Node-side registry and the product registry is not edited at all, and the unification is delivered where it is checkable: ONE name contract imported rather than forked, ONE generated dictionary, ONE storage idiom, ONE read-only discipline, and a DISJOINTNESS pin that says more than a shared object could. ⛔ THE COMPILE PREDICTED ONE EPOCH VALUE TOO MANY: the decade epoch rested on the premise that the succession observation is already decade-folded, and measurement REFUTES it — the observation is per-year and no decade fold exists anywhere in the receipt producers, so a decade row would oblige the emitter to RESAMPLE, which the charter forbids in the same sentence that licences it. The vocabulary is the measured two, and a per-row arity table makes never-resamples checkable rather than promised. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the product event registry, its class map, the metrics registry, any edge-shared bundle, or anything under src/ |
| TM-1B | [`TM-1B`](./packets/infrastructure/TM-1B.md) | **LANDED** at `0b0e282e`; do not redispatch | Member 2 of `tm-core`. The whole-world soak already writes its entire receipt to a path, so the emitter READS THE ARTIFACT and never edits the producer — which is what makes provably-outside-the-deterministic-core structural rather than argued, and what removes a change-path collision with the soak harness family. Purity is a contract, not a habit: no ambient clock, no ambient randomness, no filesystem read inside the transform, and a run identity every component of which is caller-supplied or receipt-read, because a row whose identity the tool invented is a row nobody can compare across runs. An unsupported receipt envelope version is a hard refusal rather than a best-effort parse. ⛔ THE COMPILE'S IMPORT-CLOSURE CLAIM IS REFUTED AS WRITTEN AND THE REFUTATION IS LOAD-BEARING: the module holding the ONE name contract itself imports an edit-kinds constant from a domain leaf, so importing the contract drags exactly one domain path into the closure, unavoidably. It is ENUMERATED rather than silenced, and guarded two ways so it cannot rot — the row must still be reached, and the accepted leaf must import nothing itself. ⚠ THE FIXTURE IS AUTHORED, NEVER CAPTURED, because nothing soaks before this family lands, so its own arity is asserted BEFORE any transform output; that ordering surfaced a second finding, that the findings metric is total on a failing receipt and SILENT on a clean one, which a union-only assertion would have hidden. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the soak script, any engine file, or anything under src/ |
| TM-1W | [`TM-1W`](./packets/infrastructure/TM-1W.md) | **LANDED** at `9c7db38e`; do not redispatch | Member 3 of `tm-core`, and the ruling's own mint: telemetry informs suggestion, never engine math, and the hazard-conversion law says a rule of that shape is machinery from birth or it is nothing. The existing separation test is the seed but its census denominator is one root where the order asks for the union of the deterministic core, so this walker measures all four and FOLDS the old file in rather than duplicating it. ⚠ THE WORLDPULSE FLOOR IS ASSERTED SEPARATELY FROM THE UNION FLOOR, because a union of four roots stays green when a whole root is deleted — the redundant-guard-subsumption shape this estate has already convicted once. ⛔ The soak directory is in the roots pin from BIRTH, discharging the harness charter's obligation so the joint compile cannot lose it, and its emptiness is NAMED in a shrink-only awaiting list rather than floored vacuously; the same idiom carries the migration half of the PII arm, which is what lets this member land before the migration exists. ⭐ MEASURED: not one violation in either direction across 1,043 core files and 11 telemetry files, so the exemption manifest is EMPTY at birth and the arm's proof is its three planted controls rather than its live row count. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | widening the forbidden classes; exempting a live violation; asserting a floor over the empty soak directory |
| TM-2A | [`TM-2A`](./packets/infrastructure/TM-2A.md) | **LANDED** at `633c84e8`; do not redispatch | Member 4 of `tm-core` and its stage-2 opener, promoted only after the walker member LANDED and released the mutation-coverage manifest — the estate-wide chokepoint that forces the two-stage shape. The table declares no actor, session, country or consent column at all, so the PII-free property cannot regress by a policy edit or a forgotten filter: the columns are not there, which is the whole reason the simulation class got its own table rather than a discriminator on the product events table. House security unchanged from the three prior analytics migrations — row security on with no policy, grants revoked from both API roles, a range index on the append-only timestamp and ONE run-scoped composite, every function definer-scoped with a pinned search path and execute granted to the service role alone — and the pins EXECUTE a privilege query rather than matching the revoke text. ⭐ THE COMPILE'S ONE UNVERIFIABLE STORAGE PREMISE IS SETTLED BY EXECUTION: the in-process Postgres hosts the range index, the definer/revoke pattern, the epoch check constraint, the array-expanding loader and the rollup, and the already-wired whole-sequence control stays green with the new migration in place. ⚠ Retention is INDEFINITE, proposed with its rationale in the header and vetoable: nothing here is attributable to a person so no deletion obligation can attach, the rows ARE the tuning evidence, and volume is bounded by soak cadence rather than user growth — the deliberate contrast with the raw-event prune is stated so a reader does not read it as an oversight. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | any policy on the table; a prune job; a third index on speculation; anything under src/ |
| TM-3A | [`TM-3A`](./packets/infrastructure/TM-3A.md) | **LANDED** at `a1a35de7`; do not redispatch | Member 5 of `tm-core`, last in the train and its census holder. The aggregator never opens a database connection, so a full diagnostic run and its tuning-curve report complete on a laptop with no project configured — which is what makes the harness usable in the fix loop rather than only after a deploy. The population trajectory is carried as YEAR-INDEXED ROWS rather than a summary statistic, because the recorded three-century runaway is the tuning pass's own input and a mean hides shapes; the runaway read REPORTS the growth multiple and the crossing year and never fails a run, because the tuning pass owns that verdict. ⛔ THE ANTI-FORK PIN IS RESTATED AS AN ABSENCE PIN, AND THE SUBSTITUTION IS DELIBERATE: the compile specified a parity pin over metric identifiers parsed from the migration's view definitions, but the migration as built enumerates NO metric name at all — it is one generic key-value rollup in the idiom the compile itself mandated — so a name-parity pin would compare an empty set against an empty set and pass forever, which is the self-referential vacuity class. The pin becomes the stronger pair it actually supports: the SQL face names no metric, with a control proving the scan would see one; and the ONE vocabulary the SQL does enumerate, the epoch check constraint, is compared key-for-key in both directions with a planted extra identifier on each side. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | transcribing the census tuple instead of re-deriving it; a name-parity pin with no live subject; signing a tuning band |
| SK-0 | [`SK-0`](./packets/infrastructure/SK-0.md) | **LANDED** at `b8753e28`; do not redispatch | Member 1 of the `sk-a` train — the soak harness, the other half of the tm+sk family. The one-soak law forbids a second soak script, so the grid runner, the combinatorial flag sweep and the fix loop must all reach the world through the maintained soak, and it lacked the four seams they need. ⛔⛔ THE RULES COMPOSITION IS EXTRACTED AND IT IS EXTRACTED INSIDE scripts/audit: the dark control is DERIVED from the lit rules, so its key set IS the lit key set, and an overlay applied below that derivation leaves the new keys ABSENT from the dark control — absence is not falseness for any boolean the normalizer defaults true, and the detector that would eventually catch it is a whole soak away from the edit. Putting the extraction under the runner directory instead would have placed soak-behaviour-determining code outside the certification source fingerprint, so the import direction is pinned with a planted violation. ⛔ THE SKIP-DIVERGENCE FLAG WOULD HAVE PUBLISHED A CUSTOMER-FACING PROPERTY THE RUN DID NOT EARN: the property array is now computed, the absence is a positive statement on the receipt, and the flag is refused outright beside a case id so no case receipt can exist without it. ⭐ THE FAMILY'S LARGEST UNKNOWN SETTLED AS A REAL DEFECT AND WAS CURED RATHER THAN FORKED: a year-boundary realm carries undefined-valued keys that JSON drops and the composite hash cannot see, and the compile's premise that curing it needed an engine-side seam is refuted — the census knows the paths, so the writer records them and the reader replants them. ⭐ The address-chain experience band rides the same declared fingerprint move, per the chair. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | anything under src/; a second soak script; extracting the composition under the runner directory |
| SK-1 | [`SK-1`](./packets/infrastructure/SK-1.md) | **LANDED** at `530c0c12`; do not redispatch | Member 2 of `sk-a`. A soak grid is embarrassingly parallel only if the GRID is a pure function of the config — never of N, core count or scheduling — because the moment worker count reaches the grid, the claim that one seed runs identically in-pool and solo stops being well-formed. The plan is asserted identical at one, three and eight workers, receipt paths included, and a host-seeded mutant is asserted to differ. ⛔ THE COMPARISON SURFACE IS DEFINED RATHER THAN LEFT TO A READER: the full per-year composite-hash SEQUENCE, because a mid-run divergence that reconverges by the horizon is invisible to a final-hash comparison and the pin demonstrates exactly that case; plus the receipt minus a CLOSED volatile list spelled as an EXCLUSION, so a field added later is compared by default and an allowlist implementation fails the mutant. ⭐ THE HARNESS CHARTER'S SCAN-SURFACE OBLIGATION IS DISCHARGED: the wall walker's shrink-only awaiting row is emptied and its floor supplied — and that floor is precisely why the runner SHELLS OUT instead of importing, which turns the charter's never-reaches-into-engine-internals from a promise into a structure. ⚠ The archive substrate refuses an extraction inside any repository, because there the source fingerprint is computed from the wrong git index while the files come from the archive, at exit zero. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | importing any engine module from the runner directory; a live-worktree run; an allowlist comparison surface |
| SK-2A | [`SK-2A`](./packets/infrastructure/SK-2A.md) | **LANDED** at `d2dc4816`; do not redispatch | Member 3 of `sk-a`. ⛔⛔ THE SPLIT IS LOAD-BEARING FOR THE POOL PROOF, NOT TIDINESS: eight pooled workers sharing memory bandwidth fire duration and memory tripwires a solo run never sees, so an unsplit registry breaks the in-pool-equals-solo proof by construction — the harness would convict a cell of non-determinism for the crime of having been run beside seven siblings. Five deterministic rows mint findings and capsules; two host-observability rows are metadata and never a finding by themselves. The partition is asserted total and disjoint, and the class boundary is enforced by SOURCE-SCANNING each deterministic row's own detector for a clock read, with a planted row that does one so the arm cannot pass on an empty registry. ⚠ THE REMNANT LAW OR THE COLLAPSE ROW IS A FALSE-FINDING FACTORY: a properly-died settlement legitimately holds zero, so the died flags the soak computed and then discarded now ship on the receipt and both sides are pinned. ⭐ Two envelopes are INHERITED VERBATIM from the soak's own in-code rationale rather than re-minted, because a second spelling of one envelope is the five-homes defect. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | a new class added by inline check instead of a registry row; a second spelling of the byte envelope; a deterministic row that reads a clock |
| SK-2B | [`SK-2B`](./packets/infrastructure/SK-2B.md) | **LANDED** at `9d43a840`; do not redispatch | Member 4 of `sk-a`, last in the train. ⛔⛔ THE COMPILE'S SIGNED FORK IS REFUTED BY MEASUREMENT: the year-boundary round trip IS lossy — a hundred and forty-six undefined-valued keys across twelve distinct paths — but curing it needed no engine-side seam, because the census already knows the exact paths, so the writer records them and the reader replants them. The thing is made true; the instrument is not made blind, which is the whole difference from normalizing both sides through a lossy round trip so the comparison passes. ⚠ AND THE FIRST INSTRUMENT WAS WRONG IN A WAY WORTH THE RECORD: a census with one global seen-set is alias-sensitive, a live realm is a graph and its round-tripped copy is a tree, so every shared reference reported as a difference — a hundred and twenty-one artifacts around twelve real findings, cured by switching to an ancestor stack. A restored run cannot ask for an official verdict, guarded twice over. A capsule refuses to invent its durable home and refuses a host-observability firing, and its replay refuses a node-major mismatch because same-engine byte identity is guaranteed while cross-engine identity is not. ⚠ The census delta came in below the compile's prediction, which is the exact shape the parked-file hazard wears, so it was run down file by file rather than accepted. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | a restored run computing an official verdict; a capsule inside the run archive; re-serializing either manifest |
| SK-4 | [`SK-4`](./packets/infrastructure/SK-4.md) | **LANDED** at `fa773234`; do not redispatch | Member 1 of the `sk-b` train, the harness instruments. ⛔ NO MACHINE-READABLE CONSTRAINT SOURCE EXISTS: the normalizer enforces exactly two families and spreads its input, so twenty-two engine-gated virtual keys pass through unvalidated and thirty-two preset-declared booleans are never coerced; the rest of the constraints live in code conjunctions, in COMMENTS alone, and in an exclusion set. The manifest is therefore minted, typed, and every row carries its source and its rationale, because a constraint whose provenance nobody recorded is one the next reader deletes. ⭐ THE FLAG DOMAIN IS READ ON THE AUDIT SIDE OF THE WALL AND HAS TO BE: the runner directory sits under the engine-telemetry wall's second arm, which reds on the rules-module specifier, so the census is read where the soak already legitimately imports the preset and the runner imports it from there. ⭐⭐ THE SHRINK GUARD EARNED ITS KEEP ON ITS FIRST EXECUTED RUN: the compile listed an owner-gated flag as an exclusion, and measurement shows it is not in the domain at all — an exclusion for a key that was never included is a row pointing at nothing. It is not deleted but recorded as a DECLARED ABSENCE with its own guard. ⚠ THE ROW-COUNT BAND WAS RE-DERIVED FROM MEASUREMENT: the compile's prediction was for the unconstrained space and would have refused the correct answer, and the greedy pass alone stalls near ninety-five percent, which is exactly the figure that would one day be quoted as full coverage. ⭐ The registry-mint obligations are NOT owed, read from the clause text rather than assumed. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | crediting a pair on a structurally dark key; hand-listing the flag domain; importing the rules module from the runner directory |
| SK-3 | [`SK-3`](./packets/infrastructure/SK-3.md) | **LANDED** at `430ddc3f`; do not redispatch | Member 2 of `sk-b`. ⛔ THE DIFFERENTIAL'S VALUE IS ENTIRELY IN ITS REFUSALS — an accelerant that occasionally scopes wrong is worse than none, because it produces a green re-soak over the wrong cells and calls the fix proven. The scope is the union of three arms, any one sufficient: the reverse-dependency closure including worker and dynamic-import edges, classified by REFERENCES rather than static specifiers because a worker is a separate build; every coupling-registry row intersecting the change, over the seven measured registry files; and a token-reference census of every changed exported symbol, the barrel-hop catcher. ⛔ IT REFUSES ITSELF on the kernel, the rules module, a pipeline reorder, and a dependency bump — the substrate moved, not the code, and no closure over source files can see that — plus two structural refusals: the arms DISAGREEING on a core file, since a file exactly one arm sees is a file the other two are blind to, and a derivation that errored. A failed derivation is a full soak, never a guess. ⛔ At every phase boundary the full grid runs regardless. The three thinnings the soak laws refuse by name are keyed on the SHAPE of the ask rather than a flag name, so a differently-spelled version is still refused, and a rung report carries no verdict and says so on its own face. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | a differential at a phase boundary; a hand-scoped radius; a rung report that carries a verdict |
| SK-6 | [`SK-6`](./packets/infrastructure/SK-6.md) | **LANDED** at `1a57676a`; do not redispatch | Member 3 of `sk-b`. ⭐ THE HOME IS NAMED AT COMPILE and stamped in every report header: outside both trees, outside every archive since the archive is deleted after the run, outside the temp directory so a reboot does not take it, conventional enough that the owner can find and delete it deliberately, and overridable for a lane needing isolation. An unwritable home is a HARD REFUSAL naming the path, never a silent fallback — a ledger that silently relocates reports the density of whatever survived and nobody would know it had moved. ⛔⛔ THE FAILURE MODE IS SURVIVORSHIP BIAS AND IT IS SUBTLE: every individual rule looks reasonable, and together they can make the instrument measure its own history. A cancelled or superseded run leaves the ledger BYTE-IDENTICAL rather than mostly unchanged, because a cancelled run completed only the sensitive head and crediting it inflates exactly the historic seeds while starving the never-fired tail. A repeat firing of a known finding never re-counts, or a cell would be ranked by how long its one open bug has been open. And ordering is a PERMUTATION rather than a filter, so a seed that has never fired can still earn its first entry. ⚠ The decay half-life is marked UNSOAKED and re-derived at the first clean instrument, folded into the freeze act the curve member already requires. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | crediting a cancelled run; bare-seed density keys; truncating the never-fired tail |
| SK-5 | [`SK-5`](./packets/infrastructure/SK-5.md) | **LANDED** at `94ef9b89`; do not redispatch | Member 4 of `sk-b`, and the member that makes this train depend on the telemetry core. ⭐ THE SEAM IS CONSUMED IN CODE RATHER THAN IN A TEST: the band set is enumerated from the closed registry, one family per year-epoch row, by the production module itself — wiring it only in the test would leave the production path free to hand-list and the pin free to agree with itself. ⛔ THESE ARE HARNESS-SIDE REGRESSION INSTRUMENTS AND NOTHING ELSE: never engine tuning bands, never certification entries, never anything under the source tree, so the band firewall is untouched. ⛔⛔ BANDS FREEZE ONLY FROM THE FIRST CLEAN RUN OF THE FULL INSTRUMENT at build-complete-dark, and a rolling soak on a mid-build tip may NEVER freeze one: pre-freeze runs emit curves marked provisional, tripwire verdicts bind, provisional curves inform, nothing freezes. Every ineligible shape THROWS, because a half-written capsule is worse than none — a later reader cannot tell it from a baseline. A red harness or red ratchet makes the capsule unwritable by design. ⛔ THE DECLARED-SHIFT RE-RECORD IS PROVEN THREE WAYS OR REFUSED, and the key-by-key diff is the arm with teeth: a fix may declare one shifted curve and quietly move six others, and only a diff over every key can see that. A declared shift that did not move is also refused. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | freezing from a provisional or rolling run; a silent re-record; touching a tuning band |
| EP-2 | [`EP-2`](./packets/fp/EP-2.md) | **LANDED** at `20df088b`; do not redispatch | Stage 3 of the `ep-1` four-stage split promotion, and it promotes ALONGSIDE EP-3A — lawful because those two are mutually path-disjoint and because EP-1's LANDED status released `campaignAdvanceSession.js`. ⛔ THE FORK SEMANTICS ARE ONE RULE WITH TWO HALVES: a RESUMED advance REUSES its epoch (a constraint, never an opportunity — the resume re-derives the paused tick and must land byte-identically on the minors the pause already committed, so minting there splits one advance across two streams mid-interval with no crash), and every NON-COMMITTING consumer INHERITS AND NEVER MINTS (the rules-dialog preview and the realm forecast exist to predict the advance the DM is about to press; one that minted would show a future the Advance button then refuses to produce). ⛔⛔ ON THE RESUME PATH THE BELT IS LIVE AND THE BRACES ARE STALE, and that is measured rather than argued: the orchestrator re-derives the paused tick from the cursor's PRE-SNAPSHOT world, so the kernel's own flag gate reads the rules FROZEN AT PAUSE TIME and a mid-pause flag flip does not darken it at all — what closes that cell is the store withholding the value, which is the gate this member adds. ⚠ TWO LAWFUL PRODUCTION STATES NOW PUT THE KERNEL IN 'rules strictly true, no epoch threaded' — a rule flipped off mid-pause, and a legacy cursor — where production composes dark correctly and a TEST RUN THROWS, because the structural guard cannot tell those from a caller that forgot; the behaviour is pinned in all four cells and the rejection is used as the observable proof that the store withheld. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the ledger writer, the stamp or any re-root (EP-3A); the year anchor (EP-3B); a second flag mint |
| EP-3A | [`EP-3A`](./packets/fp/EP-3A.md) | **LANDED** at `9e8ea20b`; do not redispatch | Stage 3 of the `ep-1` four-stage split promotion, promoting ALONGSIDE EP-2. ⛔⛔ A PROGRAM THAT FRESHENS ONLY THE PULSE ROOT SHIPS A FRESH FUTURE THAT IS BIT-FOR-BIT THE OLD ONE for everything the pulse rng never reaches: fifteen compositions at EIGHT read sites build their draw key from `worldState.rngSeed` DIRECTLY, five module boundaries downstream of the argument the epoch arrives on, and the only thing every one of them already holds is THE WORLD — so the epoch is STAMPED onto the world the calendar advance produced and the far sites read it back off the world they were handed. ⛔ THE VOLUME'S SKETCHED ACCESSOR SIGNATURE IS REFUTED BY MEASUREMENT: `{ absent }` cannot reproduce five of the six site coercions byte-verbatim and contradicts three of the volume's own per-site notes outright, so the accessor takes the SITE'S OWN EXPRESSION as `base` — which makes the byte-verbatim rule structural rather than transcribed, returns the argument BY IDENTITY when dark, and leaves the read-site census UNMOVED at twenty-two. ⛔ THE RE-ROOT NEEDS AN IMPORT LINE AND `roadsKernel.js` SITS AT EXACTLY ITS FROZEN 838, tolerance-zero in both directions and unpriced by the volume; the leaf import is `;`-joined onto an existing line at +0, the same technique the kernel seam used. ⭐ THE ONE DECLARED RATCHET-UP OF THE WHOLE TRAIN LANDS HERE (J-EP-11): pulseKernel 1580 to 1581 for the leaf import, with seam edit 9 itself at +0. ⚠⚠ ONE RE-ROOT IS UNREACHABLE ON EVERY PRODUCTION PATH and is recorded rather than papered over — the plans site's `realmId` SHADOWS and its only caller always passes one, so an observed-call floor there would have red a correct build. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the cursor field, the resume re-thread, the preview or forecast threads (EP-2); the `byYear` half, `yearStreamSeedOf` or any year-keyed re-root (EP-3B) |
| EP-3B | [`EP-3B`](./packets/fp/EP-3B.md) | **LANDED** at `0fa3ac54`; do not redispatch | Stage 4 of the `ep-1` four-stage split promotion, and THE FINAL MEMBER OF THE TRAIN — it promotes ALONE, lawful because EP-3A's LANDED status released the leaf, the kernel, `roadsKernel.js` and `npcLadderKernel.js`. ⛔⛔ GIVING A YEAR-KEYED DRAW THE TICK'S EPOCH IS NOT A SMALLER VERSION OF THE FEATURE BUT ITS EXACT INVERSION — a lived year would change its weather between two ticks — so the nine year-keyed compositions read a DIFFERENT fact: the epoch of the advance that FIRST ENTERED that year, held under FIRST-WINS in a `byYear` map the one writer grows in the same `setSpatialLedger` call. ⭐ THE CONSEQUENCES ARE THE PRODUCT: a lived year keeps its winter, its festivals and its road rhythms; an undo that un-lives a year takes that year's epoch with it; and a world with history that lights the flag repaints NOTHING it already lived, because a year lived dark carries no row at all. ⛔ `yearBase` IS REQUIRED AND DEFAULTLESS (J-EP-13) because two of the nine sites name the same lived year one lower than the other seven — a default would let those two read a key that is never present, return the bare root, and be silently epoch-blind forever while every dormancy pin stayed green. ⛔ A SECOND FILE WAS PRICED SHORT AND IT IS AT ITS LAYER CEILING RATHER THAN A BASELINE: `generosityKernel.js` sits at EXACTLY the 800-line `src/domain` `max-lines` ceiling with no `.size-baseline.json` row, so nothing flagged it and an own-line import reds eslint outright; the import is `;`-joined at +0, the same technique slice A used at `roadsKernel.js`. ⚠⚠ FIVE OF THE NINE ROWS REPORTED A ZERO AT FIRST WRITE AND EVERY ONE WAS A REAL GATE — `advancePolitics` returns early at the first-lit mint, the tradition rows need the second tick to land inside an observance window AND cross a year boundary, the imposition reads `occupierId` not `overlordId`, and the intel row's third gate is false whenever the info mode is the default — each cured in the FIXTURE and never in an assertion, and all nine are now reached ORGANICALLY. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | a second flag mint; any seam edit outside the frozen nine; `src/lib/spatialUsage.js` (slice A landed its row) |
| EP-1 | [`EP-1`](./packets/fp/EP-1.md) | **LANDED** at `01388030`; do not redispatch | Stage 2 of the `ep-1` four-stage split promotion, and it promotes ALONE — lawful only because EP-0 is LANDED and has released the four `_shared` artefacts they share. ⛔ THIS IS THE TRAIN'S ONE FLAG MINT (`advanceEpochEnabled`), so the §49/§50/§148 bill is paid HERE and never again by EP-2, EP-3A or EP-3B. ⛔⛔ THE KERNEL IS BANKED PERMANENTLY AT 1580 EFFECTIVE LINES with tolerance ZERO IN BOTH DIRECTIONS, and the seam contract is an EXACT ENUMERATED LIST OF SIX TOKEN-LEVEL EDITS ON SIX EXISTING LINES — an edit that is not on the list is a build STOP, and the two new statements fold onto the one line that binds both of their receivers. ⭐ DARK IDENTITY IS EARNED BY CONSTRUCTION, NEVER BY A PARITY ARGUMENT: the store mint is gated so no epoch exists in a dark world, and the kernel RE-READS the flag beside the value because `pausedAdvance` is persisted and a lit advance can pause, go dark, and resume carrying a live epoch. ⚠⚠ THE DEFECT THAT HIDES FROM EVERY STREAM ASSERTION is a materialization keyed on the raw threaded value instead of the flag-gated term: the seed string stays perfectly dark while a brand-new key lands in a flag-dark world's persisted state, and the executed mutant reds ONLY the two serialization arms. ⭐ TWO LAW FIGURES ARE CORRECTED HERE: the §104.4 closure bill is EIGHT paths across THREE bundles (`clock.js` sits in a third closure no charter named), and C-EPF-4's identical-fork-count-and-order claim is FALSE as spelled because most kernel fork labels are CONTENT-DERIVED — the true and load-bearing claim is over the STRUCTURAL subsequence, measured identical at 14/14 and 56/56. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the cursor field or the resume re-thread (EP-2); the ledger writer or any re-root (EP-3); a second flag mint |
| EP-0 | [`EP-0`](./packets/fp/EP-0.md) | **LANDED** at `6e4f9c3d`; do not redispatch | Stage 1 of the `ep-1` four-stage split promotion, and it promotes ALONE — the five EP members are not path-disjoint, so putting them co-live convicts eleven duplicate change paths and the promotion is staged rather than the train unmade. ⛔⛔ AN ADVANCE-EPOCH PROGRAM THAT FRESHENS THE PULSE ROOT SHIPS A FRESH FUTURE THAT IS BIT-FOR-BIT THE OLD ONE for everything the pulse rng never reaches: the kernel composes exactly one root and forks every sub-stream off it, but twenty-five further compositions across fifteen modules build their draw key from the world seed DIRECTLY, in three entropy idioms, under six different receiving variable names. ⛔ TWO CENSUSES HAVE ALREADY BEEN WRONG ABOUT THIS IN TWO DIFFERENT WAYS and both are why the instrument is shaped as it is: a createPRNG-anchored census was blind to a whole entropy family that decides succession contests, city responses and market buyers; and a census that closed its composition half left its read-site half open, reproducing its own count exactly while three reads feeding its own rows were missing. The walker therefore anchors on the READ, follows the value to its consumer, and carries TWO shrink-only baselines rather than one. ⭐ THE BASELINES ARE DERIVED AT THIS BASE, NEVER INHERITED: the volume publishes one set of closure figures and the compile another, and where they disagree the executed figure wins with its command recorded beside it. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | threading advanceEpoch anywhere; the flag mint; re-rooting any of the twenty-five compositions; pulseKernel edits |
| SK-7 | [`SK-7`](./packets/infrastructure/SK-7.md) | **LANDED** at `ff29a44c`; do not redispatch | Member 5 of `sk-b`, last in the train. ⛔⛔ CANCELLATION IS PID-EXACT AND THE DECOY IS THE PROOF: a pattern-matched kill has returned a SIBLING LANE'S WORKERS as skips in this estate, a recorded hazard that has already bitten, because a soak runner and a sibling executor's workers share command-line text and the sibling's run then dies with no error anyone can trace back. The pool records its own children eagerly at spawn so a cancellation arriving early still has exact PIDs, and the pin drives the real decision path with a decoy whose command line matches any plausible pattern and asserts it SURVIVES. ⚠ Ownership is verified before signalling, because a PID is reused by the operating system and a stale ledger entry can name a process that now belongs to something else. ⛔ THE DISPATCH GATE IS DATA RATHER THAN RECOLLECTION: nothing soaks before the process audit passes, nothing soaks before the harness itself lands, and the first rolling run fires at the next family exposure AFTER that landing — so not at this family's own exposure, because this family is that landing. ⚠ The script rows move the certification source fingerprint a second time; both moves are inside this family, so one statement covers both trains. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | a pattern-matched kill; a rolling result substituting for a rung; a finding with no dark control row |
| WF-1A | [`WF-1A`](./packets/fp/WF-1A.md) | **LANDED** at `66fda66d`; do not redispatch | Stage 1 of the `wf-1` four-stage split promotion, and it promotes ALONE — the four WF members are not path-disjoint (`religiousContest.js` is named by a, b and c), and this member is also the train's flag mint, which PACKET_STANDARD's "Train landings" makes a train boundary. ⛔ THE CHARTERED WF-1 IS REFUSED IN PART ON A MEASUREMENT: the volume's thirteen deliverables break FIVE hard scope limits at once, so the wave is the flag, the leaf, the ring and the classifier only — producer-first, zero production readers, zero news kinds, zero UI. ⛔⛔ `abandoned` IS CUT FROM THE FIVE-TOKEN FALL VOCABULARY BECAUSE IT HAS ZERO PRODUCERS: `applyUnaffiliatedSink` touches `state.patronRef` on no path, `'none'` is a scalar accumulator that can never hold the seat, and the sink runs AFTER the seat is decided — so a settlement secularizes to the SINK_MAX ceiling with its patron still seated. ⛔ THE FOLD'S `prevPatron` IS BLIND TO THE DM FLIP and the chartered `imposed` arm could not fire as specified: `ensureReligionState` runs FIRST and its re-assign branch has already written `s.patronRef`, so a `SET_PRIMARY_DEITY` flip leaves `state.patronRef === prevPatron`; the cure captures `priorPatron` from `prior[cid]` BEFORE the ensure call, pinned as A5 with its own dedicated mutant. ⭐ PRECEDENCE IS SETTLED AT COMPILE RATHER THAN BY THE IMPLEMENTER — `imposed` > `suppressed` > `discredited` > `displaced`, total and first-match-wins — because the arms genuinely overlap. ⚠⚠ THE FIXTURE HAZARD IS THE WAVE'S SHARPEST: `PATRON_FLIP_TICKS` is 3 and `SHARE_STEP_MAX` is 6, so a short or under-scaled fixture yields base == cure and a silently vacuous pin — run the fixture and print what the engine did before writing any pin. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | `suppressedAtTick`, the narrative prune key or the extinction beat (WF-1b); the realm last-seat beat (WF-1c); the `warTermination` join or the FaithSection line (WF-1d); lighting the flag anywhere |
| WF-1B | [`WF-1B`](./packets/fp/WF-1B.md) | **LANDED** at the TE-WF1B terminal; do not redispatch | Stage 2 of the `wf-1` split promotion and, per ODQ §309, a SINGLE member: it promotes ALONE because the WF members are not path-disjoint (`religionState.js` is named by b and c-adjacent work, `religiousContest.js` by a, b and c). It makes suppression STAMPED AND ORDERED — one unexported writer `suppressDeity` takes over all THREE `suppressed: true` writes, records `suppressedAtTick` only when the fold hands it a finite tick, and the narrative prune keeps the newest-suppressed so the creed the world forgets is the one longest gone from the light. ⛔ THE BOUNDARY MOVED BY EXACTLY ONE DELIVERABLE against §308.3's default, and the reason is WF-1a's own refusal: the default lands `suppressedAtTick` in a member where nothing reads it, re-minting the orphan persisted key WF-1a declined to mint. The prune key IS the field's only consumer, so it moved into the same member. ⭐ ZERO of the fifteen test call sites and both `scripts/audit/` call sites are edited: `attemptEntry` ALREADY carries a defaulted options bag, so `tick` joins it as a key and is not a signature change at all, and `resolvePatronContest` takes `tick = null` as a defaulted third parameter. ⭐ IT AUTHORS ZERO NUMBERS — the prune's fire bar is the EXISTING `KEEP` const read in its own scope (ODQ §308.3 RAISED-4: a literal 4 anywhere is a STOP), and the comparator's `-Infinity` sentinel is an ordering identity, not a dial — so it lands with no chair value signature. ⛔ DARK IS BYTE-IDENTICAL TWICE OVER: the tick is `null` unless the by-name `faithUnseatingEnabled === true` read is lit, and the fold returns at its head on deity PRESENCE, so a deity-free world is unchanged even LIT. ⚠⚠ THE SHARPEST FIXTURE HAZARD IS NEW AND IS NOT IN THE ANNEX: `nicheOf` is a COMPUTED niche (`temper:alignment`), so an incumbent carrying a hand-authored `niche` string never matches the newcomer's, the entry falls through to `open_slot`, and a site-`:262` pin passes having asserted nothing — the fixture must build the incumbent's niche THROUGH `nicheOf` or the case is vacuous. Executed both arms before any pin was written. ⚠ It mints NO flag, news kind, desk row, receipt pool, kind registry or UI surface, and gives `fallCauseFor` no second caller (preamble §P2.11). ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | the settlement extinction obituary beat (RE-FILED TO WF-8 by ODQ §309 — the FAITH family has no kind registry at all and the beat mints the estate's ELEVENTH as a SECOND one-row family); the realm last-seat beat (WF-1c); the `warTermination` dissolution join and the FaithSection cause-chain line (WF-1d, and `warTermination.js` measures 818 against a frozen 818 — zero headroom, shrink-only); the Chronicle obituary row (WF-1d); promoting `KEEP` into `RELIGION_TUNING` (owner-signature surface under THE PROMISE, refused); a fifth fall cause; `covert` prune sequencing (WF-5b — `.deities[ref].covert` measures ZERO hits in `src`, so a guard here would be an unreachable arm); `ENGINE_GATED_VIRTUAL_RULE_KEYS` and every certification, edge-shared bundle and covering-array literal; lighting the flag anywhere |
| WF-1C | [`WF-1C`](./packets/fp/WF-1C.md) | **LANDED** at the TE-WF1C terminal; do not redispatch | Stage 3 of the `wf-1` split promotion, and it promotes ALONE: the members are not path-disjoint, so §P7.14's serial order binds. It is the SMALLEST of the four stages and stays that way — one realm-scope news kind behind the landed `faithUnseatingEnabled` flag, minting no state, no registry, no leaf, no dial and no test file. ⭐ THE BEHAVIOUR IT ADDS IS A SILENCE MADE LEGIBLE: a one-seat creed is ALREADY at the cult floor, so when it loses that seat the tier does not move, `advancePantheon.changes` is empty, and the realm's own chronicle says nothing at all while the last altar in the world goes out. Executed at the base, and it is the wave's whole premise. ⛔⛔ THE FIRE PREDICATE IS A DERIVATION AND A LITERAL WOULD BE A STOP TWICE OVER: a naive prevSeats-to-zero test emits a realm extinction on the tick the seats vanish AND a realm twilight one tick later for the same deity — two obituaries for one event, measured on a four-seat collapse — while spelling the charter's literal one is a RAISED-4 STOP by coincidence of value, because `MINOR_DEMOTE` is 1 in the same module scope. The beat therefore fires on `lostLastSeat && !ladderSpeaks`, delegating the tier question to the module's own `qualifyingTier`, and authors no number anywhere. ⛔⛔ THE APPARENTLY TIDY REGISTRATION MOVE IS A HARD STOP BY ARITHMETIC: giving the new kind an `EXACT_SECTION` row beside the two siblings that both have one takes `ROUTED_TOKENS` to 379 and `unvoiced` to 275 against a SHRINK-ONLY ceiling with no lawful growth cure. The `pantheon_` PREFIX rule files the kind at the faith desk for free, so the prefix is the lawful door and the exact row is the trap — the family law's usual reading, inverted. A5 asserts the absence and MUTANT (d) plants the row and proves it reds. ⛔ `pulseKernel.js` IS A ZERO-HEADROOM FILE at 1581 effective against a frozen literal of 1581, pinned exact in BOTH directions: this member spends exactly ONE modified line there, at NET ZERO, and buys everything else by riding the `changes` array the kernel already threads. ⚠ It mints no flag, no kind registry, no receipt pool, no desk row, no persisted field and no top-level ledger key; dark is byte-identical by construction and twice over, since the pantheon block sits behind deity PRESENCE rather than config. ⚠ This cell is prose, and the validator reads the STATUS column literally: keep status words out of it | the `warTermination` dissolution join, the FaithSection cause-chain line and the Chronicle obituary row (WF-1d, whose `warTermination.js` measures 818 against a frozen 818 — zero headroom); the SETTLEMENT-scoped extinction obituary's VOICE (WF-8, re-filed by ODQ §309, and it is the beat that would need the FAITH kind registry); repairing `deityNameForRef`'s `custom:` truncation (RAISED-B — measured NOT curable in-member because its single caller's two LANDED arms are live and unflagged); a distinct authored salience for the beat (owner-signature surface — it derives from the twilight's existing constants instead); a fifth fall cause or any `patronFalls` read; `covert` sequencing (WF-5b); lighting the flag anywhere |
| WF-1D | [`WF-1D`](./packets/fp/WF-1D.md) | **LANDED** at the TE-WF1D terminal; do not redispatch | Stage 4 of the `wf-1` split promotion, and the member that finally READS what WF-1a built. ⭐ THE BEHAVIOUR: a holy war can outlive the faith that raised it, and today the receipt can say only THAT the founding claim died, never why — one clause serves whether the creed was evicted by a garrison, lost a contest, or simply lost the pews. This member gives `fallCauseFor` the chartered first production caller preamble §P2.11 reserved for it (executed at base: one definition, ZERO callers), so a dissolution receipt now carries the typed `patronFallCause` token AND says it in the house voice. ⛔⛔ THE WHOLE MEMBER IS SHAPED BY ONE FROZEN NUMBER: `warTermination.js` sits at 818 effective against a frozen literal of 818 with zero headroom, so the join lands at NET ZERO or not at all — 818 → 818 on eslint own Linter and 1045 → 1045 on `wc -l`, measured on BOTH sides. The trick is that the prose selector pays for the faith import: moving the one `DISSOLVED_CAUSE_PROSE` read down into the cause-table leaf collapses a four-line import block to two, and the leaf own header says it exists to absorb exactly this pressure. ⛔ THE COUPLING ROW COSTS THREE FILES, NOT ONE, AND THE VALIDATOR ENFORCES IT: `couplingRegistrationGaps` fails a packet naming a registry leaf without also naming the composing head and the pin file — the compile had filed it against the head alone, which would have failed validation. It is the estate FIRST FAITH→WAR row. ⚠ FOUR COMPILE FIGURES WERE CORRECTED BY EXECUTION at the implementing tip, and one MUTANT SURVIVED its chartered battery: the legacy arm honesty came from an absent pinned anchor rather than from the casus guard, so the guard got its own pin and the mutant now convicts. Dark is byte-identical, proved by digest on both sides of the landing. ⚠ This cell is prose, and the validator reads the STATUS column literally: keep status words out of it | the FaithSection cause-chain line (deferred — it breaks three hard caps with the join and carries a DS-FTH-1 corpus amendment; RAISED-3 puts its name and placement to the chair); the Chronicle obituary row (REFUSED on a measurement — both candidate producers were re-filed away and this member mints a receipt kind rather than an `impactKind`, so the row would have no minter and nothing in the estate would red on it; RAISED-1); a fifth fall cause; any write to `patronFalls`; `pulseKernel.js` and `applyWorldPulse.js` (named by neither manifest row); lighting either flag anywhere |
| WF-1E | [`WF-1E`](./packets/fp/WF-1E.md) | **LANDED** at the TE-WF1E terminal; do not redispatch | Stage 5 of the `wf-1` split promotion, and the member that finally lets a TOWN say its seat changed hands. ⭐ THE BEHAVIOUR: the ring has remembered patron falls since WF-1a and the war receipt has been able to name one since WF-1D, but the settlement's own faith panel still could not say the seat had ever turned over, or why. Lit, a settlement whose ring records a fall now renders ONE sentence in the panel's existing cause-chain block, built from the typed cause of `patronFalls[0]` — and it renders FIRST, because a seat changing hands outranks devotion drift. ⛔⛔ THE FENCE READS THE FLAG AND THE RING TOGETHER, AND THAT IS THE WHOLE DESIGN: the ring is HISTORY and history is immutable under THE PROMISE, so a world lit once and then darkened keeps its ring forever — gating the projected key on the ring alone would print a fall in a dark world. Because the projection is re-derived every tick, the fence SELF-HEALS on the first dark tick, and A5 executes exactly that on the SAME state object rather than asserting it. ⛔ `pulseKernel.js` IS A ZERO-HEADROOM FILE at 1581 effective against a frozen literal of 1581, pinned exact in BOTH directions: this member spends exactly ONE modified line there, packing a sixth argument onto the existing call at NET ZERO — 1581 to 1581 effective and 2865 to 2865 on `wc -l`, measured on both sides, with no PRNG draw added, moved or removed and the fork order untouched. ⭐ VOLUME Q4 IS RULED HERE (ODQ §333.2): the DS-FTH corpus binds as reader-side spelling law, so the DS-FTH-1 signature is amended at source with the generator run in the SAME commit, and the agreement pin slices the block on BOTH sides rather than scanning the whole document — the vacuity class where a document-wide search passes on the block's own surrounding prose. ⚠ It mints no flag, no kind, no registry row, no persisted top-level key, no dial and no test FILE; the census moves by SIX TITLES only, derived by placeholder-and-convict at the base. Dark is byte-identical, executed rather than argued. ⚠ This cell is prose, and the validator reads the STATUS column literally: keep status words out of it | the §326.4 micro-batch — `deityNameForRef`, `subsystemRowsVirtual.js` and the coupling walker row (all three are the sibling member WF-1F by ODQ §333.1, sharing zero paths with this one); the Chronicle obituary half (dead — both producers re-filed, WF-1D §4); a second production caller for `fallCauseFor`; resolving the raw deity slug to a display name in the panel (deliberately withheld — the line names the act, not the creed, and A6 asserts the slug never reaches the DOM); a fifth fall cause; any write to `patronFalls`; `warTermination.js`, `peaceTerms.js` and `applyWorldPulse.js` (named by no manifest row); lighting the flag anywhere |
| WF-1F | [`WF-1F`](./packets/fp/WF-1F.md) | **LANDED** at the TE-WF1F terminal; do not redispatch | The §326.4 micro-batch, split out of WF-1E by ODQ §333.1 because folding it in would breach two PACKET_STANDARD caps; it shares ZERO paths, instruments or proofs with its sibling. ⭐ THE BEHAVIOUR: `realmEvents.js` was the THIRD producer of deity display names still carrying a private, lossy tail-pop — a floor that kept only the ref's LAST token, so an extinct `custom:<slug>` creed printed one word ("The Last Altar of Forge" for a creed authored 'Sun of the Deep Forge', "The Ascendancy of Father" for a war-father slug). `realmArcSummary.js` and `worldSnapshotPublic.js` had each paid the identical defect and converged on `deityDisplayNameFromRef`; this member joins the third to them, so the three cannot drift apart again. ⛔⛔ THE BRANCH IS LIVE ON UNFLAGGED SURFACES, WHICH IS WHY THIS IS A DECLARED SHIFT AND NOT A TIDY: ascendancy, twilight and extinction arcs run on religion ACTIVITY alone, and EVERY extinction beat reaches the floor because an extinct creed is carried by no settlement and production hands the arcs THIS tick's pre-tick snapshot. The shift is proved KEY BY KEY against a `git archive` tree of the base — 45 keys, zero added, zero removed, 14 moved, every mover a name-bearing string of a ref whose LAST segment carries '_' or '-' — and exactly ONE committed pin re-records: the separately labelled measured-truth arm WF-1C authored for precisely this cure. ⛔ THE WIDER RESOLVER WAS REFUSED: `deityNameFromSnapshots` also reads `cultDeitySnapshots`, which would silently widen what the arc producer SCANS. The executed mutant proved that refusal was prose with no machinery behind it (36/36 passed), so a guard was written on the one world where the two resolvers disagree — a town whose patron is Harrow but which still keeps the forge creed as a cult. ⭐ Two falsified sentences also retire here: the certification row at `subsystemRowsVirtual.js:1268`, whose prediction named a member ODQ §309 had re-filed to WF-8 and whose "nothing reads the ring" clause WF-1E itself falsified at this very base; and the FAITH exact-path justification in `couplingInclusion.walker.test.js`, which claimed a single importer WF-1D had already made two. ⚠ NET ZERO effective production lines on both files, `pulseKernel.js` absent from every hunk, and the census still at `2492/364/2128/20677/5780` — CONVICTED figure by figure at the post-rebase base, never carried across it, not assumed, with the lighting walker green UNTOUCHED and no re-record block written. ⚠ This cell is prose, and the validator reads the STATUS column literally: keep status words out of it | widening the arc producer's scan to cult snapshots (refused, and now pinned against); recovering the casing and punctuation the slug itself destroyed (only an authored name on a live snapshot, or a display name persisted at mint, carries those — and persisting one is engine state under THE PROMISE, owner-gated); `realmArcSummary.js` and `worldSnapshotPublic.js` (already cured, verified producer-by-producer and out of radius); the lighting walker file (named by WF-1E only — this member must not touch it, and its stillness is the proof); `pulseKernel.js`, `peaceTerms.js`, `warTermination.js` and `applyWorldPulse.js` (named by no manifest row); any change to the ratio, the coupling ceilings or the certification row's grade |
| WF-8A | [`WF-8A`](./packets/fp/WF-8A.md) | **LANDED**; the chair CASes the TE-WF8 landing | Stage 1 of the `wf-8` NARRATION train, and the member that finally lets a town record that a creed's last altar went out. ⭐ THE BEHAVIOUR: the narrative prune has deleted the longest-dormant creed since WF-1b, and nothing anywhere said so — the settlement's roster simply got shorter. Lit, the fold now mints ONE obituary per creed that has genuinely left the parish, voiced from the GOVERNED FAITH ANNEX rather than from new prose. ⭐⭐ THE ANNEX ALREADY EXISTED AND NOTHING READ IT: `docs/content/RECEIPT_POOLS_FAITH.md` was authored 2026-08-02 under SP-6 and verifier-passed, one hundred three kinds at seven hundred thirty-six variants, and this member is the first to wire a line of it — five authored variants transcribed byte for byte, with the walker re-deriving the whole pool from the document on every run. ⛔⛔ THE SHARPEST FINDING IS THAT A DELETION IS NOT AN EXTINCTION, AND IT WAS FOUND BY RUNNING THE FIXTURE: the drafted per-deletion mint announced the same creed extinct twenty-five times in one sixteen-tick village, because a creed with a road back re-enters, is re-suppressed and is deleted again every tick. TWO roads exist — a neighbour still CARRYING the creed, and the settlement's own EMBEDDED snapshots, which `ensureReligionState` reinstalls as the dominant patron — and both are fenced at the seam from data already in scope, with the cure proven by an independent cross-check rather than by the count falling. ⭐ THE LAWFUL RESURRECTION: the `KIND_SECTION` row WF-1d refused dead lands here WITH its live minter in the same commit, which is the condition ODQ §321.2(a) always set for it, and WF-1d's own absences do not move. ⛔ The §321.2a minter-totality arm joins the walker estate with the written-ruling escape, stale/orphan hygiene and in-suite controls, and C2's block is relocated rather than duplicated. ⚠ It mints no flag, no persisted field and no UI surface; dark is byte-identical twice over, proven by an own-footprint golden captured BEFORE wiring on a deity-BEARING fixture. ⚠ This cell is prose, and the validator reads the STATUS column literally: keep status words out of it | the remaining WF-8 narration kinds and their producers; the realm-scale obituary (WF-1c, landed); `faithNarrationEnabled`, which is untouched and still lights LAST; a fifth fall cause; the FAITH small-family shrink-back, which is a RECORDED OBLIGATION of the next WF-8 member that takes the family to five rows or more |
| RR-1 | [`RR-1`](./packets/osr-repair/RR-1.md) | **LANDED**; the chair CASes the TE-RR1B landing | The §353.3 dead-reader repair at `viewModelBodySlices` `servicesSlice`, and the first member of the un-stamped `osr-repair` family. ⭐ THE FINDING: the slice mapped its `pressures` output through a two-arm fallback over keys no writer in the estate has ever put on an institution record, so the third arm — the empty list — has always been the shipped behaviour and the print block those values feed has never once fired. ⛔ EVERY LEG WAS RE-DERIVED AT THE VERIFIED BASE, not inherited: the sole institution minter carries neither token, the three worldPulse institution writers carry neither, the CLOSED custom-content admission schema admits neither, `EDITABLE_FIELDS.institution` is `['desc']`, and the `-S` history walk puts every committed write of either key on AI-context, narrative-context or settlement records back to genesis. ⚠ ONE COMPILE FIGURE WAS CORRECTED BY EXECUTION: the census recorded two readers of `detailed` and there are three — `src/foundry/journalPages.js` reads it too — but that file carried the same read at the compile pin, so it is an undercount rather than a leg that went live, and it never touches the strain list. ⭐ THE A/B IS EXECUTED, NOT ARGUED: five configurations by twelve seeds, 1957 institution records, no carrier, no base-arm selection, full-slice deep equality, with a planted-carrier control proving the probe can fail. ⛔ THE SHRINK IS THE POINT OF THE SEQUENCING: deleting an observed-shape reader moves the walker inventory, so the member rides the schema-10 mint and cures with the governed `--write` re-freeze rather than a genesis — reconciled key by key against a pre-write copy, two rows gone and nothing else moved, every moved tree-manifest entry attributed to the member's own source edits, and the detector tree unmoved. ⚠ The output field and the render block SURVIVE deliberately, because a cull is an owner carve-out; the block was proved live rather than assumed. ⚠ This cell is prose, and the validator reads the STATUS column literally: keep status words out of it | culling the always-empty print field, its block, or the roughly thirty other speculative alias arms in the same file (owner docket); giving institutions a pressures surface at all (product law, owner-gated); `src/pdf/sections/Institutions.jsx`; any OSR instrument or scanner code, exemption entry, or hand-edit of the baseline JSON; the `EDITABLE_FIELDS.institution` versus `description`/`blurb` gap (recorded for separate assessment); anything that would promote RR-2, which chains behind this member on shared change paths |
| RR-2 | [`RR-2`](./packets/osr-repair/RR-2.md) | **LANDED**; the chair CASes the TE-RR2 landing, and posts the owner exhibit pair first | The §373 institutions-prose restoration at `viewModelBodySlices` `servicesSlice`, and the second and last member of the un-stamped `osr-repair` family. ⭐ THE FINDING: the slice asked each institution record for `description`, then for `blurb` — but the schema's typedef carries `desc` and no `description`, the catalog supplies `desc`, and the variant picker writes its choice back to `desc` while its own comment records the split. So **933 authored one-liners** (311 catalog strings plus 622 authored variants, re-counted at the verified base) have never once printed, and a custom institution printed its generated line even where the DM had written over it. ⛔ EVERY FIGURE WAS RE-DERIVED AT THE VERIFIED BASE, not inherited from the compile, whose numbers were pinned before the closing train, the mint and the sibling member all moved the tip — including the OSR schema, which the compile recorded as 9 and which measures 10. ⭐ THE GENESIS WALK WAS RE-PROVEN RATHER THAN CARRIED: the one commit in `src/generators` history that ever touches `blurb` touches it in a COMMENT about FACTION blurbs, the closed admission schema has zero occurrences, and no persisted save from any app version can carry the key on an institution record — so the arm is struck under the §369 standard. ⭐⭐ THIS IS A DECLARED OUTPUT SHIFT ON THE PAID SURFACE, drafted with exhibits and never silent: the Institutions chapter and the Foundry journal begin printing institution descriptions for every settlement. The §110.3 enumeration is executed key by key over 60 cases — five configurations by twelve seeds, 2396 institution rows — with the raw settlement byte-identical on both trees at every case, zero keys added, zero removed, and every one of the 2396 moved keys the same `services.detailed[i].description` moving from null to authored text. ⭐ THE EXHIBIT PAIR IS THE OWNER'S VETO WINDOW: one settlement rendered both ways shows 1 of 59 cards carrying a description before and 59 of 59 after, and the chair posts it before the exposing CAS. ⛔ THE INSTRUMENT IS THE HALF THAT RETIRES THE CLASS: the reader-with-no-writer ratchet is structurally blind here, because a PARTIAL writer (the custom arm) satisfies its detector while the majority producer writes another key — so the member adds an editable-path to consumer agreement pin that applies a marked DM edit to all sixteen queue-wired prose paths and asserts each lands at the key its rendered surface reads, with a two-direction completeness arm so no future editable path can ship unclassified. ⚠ ONE COMPILE FIGURE WAS CORRECTED BY EXECUTION: the prototype recorded all three `safetyProfile` paths as unreadable, but `guardEffectivenessDesc` DOES reach through `deriveGuardAssessment` into the defense slice, so the production register carries eleven reaching paths and five excluded ones rather than the prototype's nine. ⚠ This cell is prose, and the validator reads the STATUS column literally: keep status words out of it | the write-side convergence of a custom institution's two prose keys (§399 owner docket, and the DM's-pen fork is explicitly out of radius); the settlement-scalar fallback for `arrivalScene` and `pressureSentence` in any PDF slice (owner product call — the member corrects the two exemption captions that falsely claimed no canonical scalar existed, and moves no behaviour there); `economicViability.summary`, whose read-time reconciliation discards the DM's edit on BOTH surfaces (chartered for its own diagnosis); new readers for `safetyDesc` and `economicDragDesc` (owner-cull docket §369.2); `src/pdf/sections/Institutions.jsx` and `src/foundry/journalPages.js`, which both already render the cured key correctly and take zero edits; any OSR instrument or scanner code, exemption entry, or hand-edit of the baseline JSON; the roughly thirty other speculative alias arms in the same file |
| WEB-4 | [`WEB-4`](./packets/website/WEB-4.md) | **LANDED**; the chair CASes the TE-WEB4 landing | Member 4 of the website train's `W-A`, and the rider that discharges J-TE28-3 under ODQ §359.9. The closed simulation-metric registry gains the address-chain row, so the harness's curve bands finally watch whether the world's narration is ADDRESSED and not only how much of it there is. ⭐ THE MEASURED SHAPE REFUTES THE PREDICTED ONE: the compile allowed for the address-chain block arriving as its own receipt key, which would have owed the arity table a new entry; measured here it rides INSIDE each yearly observation exactly as its news-cadence sibling does, so the row declares the year-series it already has, the arity table is byte-unchanged, and the reading is pinned rather than described — a later member that hoists the block to the receipt root reds. ⛔ ZERO MOTION UNDER THE CERTIFICATION SOURCE PATHS, which is why the car is small: the §180.3a price was already paid by the soak member that was moving the fingerprint anyway, and paying it again to re-reach that directory would move the aggregate for nothing. ⛔ THE TRANSFORM COPIES AND NEVER COMPUTES: the instrument publishes integer milli already, so re-deriving a rate here would be resampling a value the receipt had decided — and because the fixture's rates are internally consistent, a value equality alone cannot tell copying from recomputing, so the arm hands the transform a synthetic block whose rates disagree with its counts and proves the receipt wins. ⛔ A YEAR WITHOUT THE INSTRUMENT EMITS NOTHING, NOT ZEROES: a floor of invented zeroes is indistinguishable from a world whose narration carries no addresses, and a band frozen over it is worse than an absent curve. ⚠ The registry header miscounted its own frozen array by one from the previous rider's landing until now; corrected here as a declared comment-only edit, priced rather than free. ⚠ This cell is prose, and the packet validator reads the STATUS column literally: keep status words out of it | anything under `scripts/audit/`; any client byte; a migration (196's rollup enumerates no metric name, so it admits any registered one); a new observation of any kind; the tuning bands themselves; the address-chain instrument's own arithmetic |
| WEB-6 | [`WEB-6`](./packets/website/WEB-6.md) | **LANDED**; the chair CASes the TE-WEB6 landing | The §363.1 anon-ceiling disclosure, closing §320.3 — car 6 of the `website` train (WEB-1 founded the family). ⭐ THE FINDING, AND IT IS AN EXECUTED RENDER RATHER THAN A READING: the owner-era ceiling sentence has been in the copy registry with a green claims-parity arm the whole time, and rendering `LandingBelowFold` to static HTML at the verified base produces 57,200 bytes that carry four other §01 strings and not that one. The Instant Draft widget's removal (owner walk order 10, 2026-07-22) took the only carrier off the page and left the key behind, so the arm went on passing on prose no visitor could reach — the rendered-surface face of the vacuity class. ⭐ ZERO COPY IS AUTHORED: the words are the owner's own, re-referenced and not retyped, so the `tierFacts` binding to `TIER_GATE` is untouched and the member carries no copy-walk dependency. ⭐ THE VACUITY CURE TAKES THE STRONGER OF THE TWO SANCTIONED FORMS: the charter allowed a source scan, the harness supports a real render through `react-dom/server` in the parity file's own node environment, and the render is what shipped — anchored on two §01 siblings asserted FIRST, so an arm that never rendered reds before it can assert anything. ⚠ THE CHARTER'S TWO-FILE MANIFEST WAS SHORT BY TWO, both found by executing the change: `tests/ui/homeLanding.test.jsx` held the INVERSE pin from walk order 10 and was convicted before it was touched, and the estate lighting census is an exact equality that one new title moves. ⛔ THE CENSUS IS ATTRIBUTED BY ISOLATION AT THIS MEMBER'S OWN BASE, never by arithmetic: a baseproof worktree with its own install convicted the walker green at 20,719, reverting each test file alone convicted 20,719 and 20,720 respectively, and every figure was read from the arm's own failure message; at the landing slot `dffa2b97` the tuple was RE-DERIVED, never carried — the slot's 20,750 plus this member's +1 convicted green at 20,751 with the slot's own tuple as the red control. ⚠ THE PACKET ENTERED AT THE TERMINAL STATUS (the §410 form) because MF-T2H's READY row reserves the lighting walker and the validator refuses this row at DRAFT and READY alike — both refusals executed and quoted in the packet. ⚠ This cell is prose, and the validator reads the STATUS column literally: keep status words out of it | HomeHero's at-cap block; HowToUse's own disclosure; any layout or design-system motion; any NEW wording, which is the owner's copy walk; the obsolete `strings.js` clause and the inert `{anonSize}` substitution binding at `TIER_FACT_VARS`, whose lines this member does not touch; `tests/lint/postureNameCollision.walker.test.js`, a base-present varying-cast stray recorded and not cured here |
| WEB-7 | [`WEB-7`](./packets/website/WEB-7.md) | **LANDED**; the chair CASes the TE-WEB7 tip | WEBSITE family, train `web-b`, car 3 — the §362.2 elections record, and the section that had to be sliced to prove it. ⭐ THE RECORD: §3.3 of both notices copies was headed "Dual-licensed packages with no election recorded" and each row's Status cell said so; it is now "The two production-dependency elections" in the §1.3 idiom, resolving `dompurify` 3.4.12 to Apache-2.0 and `rgbcolor` 1.0.1 to MIT, with the same election pointer §1.2 already carries for JSZip and Touch Punch added to the §3.1 counts and the two §3.2 inventory rows. ⛔ EVERY LICENCE FACT WAS RE-DERIVED FROM THE INSTALLED PACKAGE, never recalled (§317.1): both manifest strings, all four shipped licence files by SHA-256, and the executed finding that `rgbcolor`'s own MIT body whitespace-normalises to exactly the text §4.1 already reproduces — which is why NO licence body is added anywhere. ⭐ THE PIN IS SECTION-SLICED ON BOTH SIDES and compares ordered `name@version=licence` lists, because the packages are named in four other registers and a whole-document `includes()` stays green after the row is struck — the control asserts exactly that, and four executed mutations convict, each restored checksum-verified. ⚠ THE OFL RIDER THE COMPILE AND C6 BOTH CARRIED WAS ALREADY DISCHARGED AT THIS BASE: `public/fonts/OFL.txt` is present, its tail hashes to the chair's elected body, and the cure's commit is an ancestor of the compile's own pin — reported as stale rather than re-executed, since re-pasting a byte-correct body is the motion §317.1 exists to prevent. ⚠ This cell is prose, and the validator reads the STATUS column literally: keep status words out of it | lighting the page's navigation link (an owner act, §317.1 — the darkness pin is re-run, not relaxed); the TinyMCE/GPL disposition (owner and counsel, §254.5.5/§295); any licence body re-shipped or retyped; `png-js`'s unstated manifest field, where no choice is offered and so no election is owed; every `src/` file |
means the less permissive status wins.

### ⚠⚠ CENSUS-HOLDER RULE — restamped 2026-08-14 at the GR-4b-iii-a LANDING

The estate-wide lighting census (`tests/lint/sovereigntyLightingContract.walker.test.js`) moves
whenever a packet adds test titles, and the serialization law ruled at `73f5be96` re-derives it
WHOLE in the change that moves any figure. The validator forbids two non-terminal packets naming
one change path, so at most one packet may hold the reservation.

⚠⚠ **THE RULE IS WIDER THAN "THE CENSUS ROW", AND `R35` MEASURED HOW MUCH — 2026-08-14, ratified
at `OWNER_DECISION_QUEUE.md` §44.** The reservation is not special to the census walker: it binds
**every** `changeManifest` path. `implementation-packets.mjs:498` computes
`reservesChangePaths = !TERMINAL_PACKET_STATUSES.has(status)`, and only `LANDED` and `SUPERSEDED`
are terminal — ⛔ **so `DRAFT`, `BLOCKED` and `STALE` reserve exactly as `READY` does, and
demoting a member is not an escape.** The `hb-2b` train's two members both lawfully edit
`tests/domain/habitCurve.test.js`, which is a normal thing for a train to do; standing them
non-terminal together yields `duplicate change path across packets` from the promotion to the
terminal, and **no implementation commit can discharge it** because it clears only on a status
flip. ⇒ **A TRAIN WHOSE MEMBERS SHARE ANY PATH PROMOTES IN STAGES** (`CR-HB2B-SPLITP`, signed at
§44): promote M1 alone, land it, then one docs-only commit flips M1 terminal *and* promotes M2.
⭐ The mid-train flip is safe only for a member with **zero `CREATE` rows** — `CREATE` existence
is asserted only at `LANDED` (`:534`).

⭐ **ROW FREE, 2026-08-14 at the `hb-2b` LANDING.** The train landed both members and re-derived
the tuple WHOLE at `I2` — the last commit in the train that moves a `tests/` byte — leaving the
terminal docs-only and letting the base-state capsule regenerate in ONE terminal commit. Three
new credited test files carrying twenty-six literal `test` titles across the two members and
three literal `describe` titles moved the free tuple `2425/366/2059/20081/5650` to the
re-derived tuple `2428/366/2062/20107/5653`; `parked` stayed at 366 because every one of those
titles is a literal in a straight-line registration. ⭐ M1's edit to `tests/domain/habitCurve.test.js`
moved the census only in `titles` (+4), and M2's row-9 edits moved NO figure at all: the
`HABIT_DARK_CLOSURE` entry, the family-count literal and the A4 re-aim are all in-place changes
inside existing cases. The live manifest has **44 rows and zero dispatchable packets**; no
non-terminal row currently names the census walker, and a terminal packet reserves nothing. For
this run no diagnostic, weekly, release or research soak worktree was created.

⚠ **HISTORICAL, 2026-08-14 — written at the `hb-2b` P1b promotion, when the row was CLAIMED.**
⛔ **ROW CLAIMED.** The live manifest has **44 rows and
one dispatchable packet** (`HB-2`), and **that packet holds the census reservation** — it names
`tests/lint/sovereigntyLightingContract.walker.test.js` on its change manifest and re-derives the
tuple WHOLE at its own implementation commit, which is the train's last `tests/`-moving change.
⭐ It can hold it only because M1 flipped terminal in this same commit and released the shared
path; while both stood non-terminal the reservation could not be taken at all. The free tuple
`2425/366/2059/20081/5650` is predicted to move to `2428/366/2062/20107/5653` — three new credited
test files, twenty-six literal `test` titles across both members, three new `describe` titles, and
`parked` unmoved at 366 because every title in this train is a literal in a straight-line
registration. The paragraph below remains true of the P1a commit.

⚠ **HISTORICAL, 2026-08-14 — written at the `hb-2b` P1a promotion, when the row was FREE.**
⭐ **ROW STILL FREE.** The live manifest has **43 rows
and one dispatchable packet** (`HB-0B`), and that packet **does not name the census walker** — by
design, per the TE7 law: the tuple is re-derived WHOLE at the train's last `tests/`-moving
commit, which is M2's, so M1's four titles ride as a declared interior red instead. The
reservation is therefore unheld at this commit and is claimed by `HB-2` at P1b. The paragraph
below remains true history of the `hb-1p` landing but no longer describes the packet count.

⭐ **ROW FREE.** The `hb-1p` train landed its single member `HB-1`, which held the reservation
and re-derived the tuple WHOLE at its I1 commit — the last commit in the train that moves a
`tests/` byte — leaving the terminal docs-only and letting the base-state capsule regenerate in
ONE terminal commit rather than two. Four new credited test files carrying thirty-one literal
`test` titles and four literal `describe` titles moved the free tuple
`2421/366/2055/20046/5646` to the re-derived tuple `2425/366/2059/20077/5650`; parked stayed at
366 because every one of those titles is a literal in a straight-line registration. ⭐ The
member's edit to `tests/domain/habitCurve.test.js` moved NO figure: HB-0's dormancy case was
re-aimed IN PLACE, one case in and one case out. The live manifest has **42 rows and zero
dispatchable packets**; no non-terminal row currently names the census walker, and a terminal
packet reserves nothing. For this run no diagnostic, weekly, release or research soak worktree
was created.

⚠ **DATED CORRECTION, 2026-08-14 at the `hb-1p` landing.** Both paragraphs below remain true
history — of the promotion and of the prefix landing respectively — but neither describes the
current holder or packet count.

⚠ **HISTORICAL, 2026-08-14 — written at the `hb-1p` promotion, when the row was CLAIMED.**
It correctly recorded that the paragraph below it describes the prefix landing rather than
the current holder, and that reading still holds; the holder it named has since landed.

⭐ **ROW FREE.** The `hb-1` train landed a PREFIX (`HB-0` alone) and re-derived the tuple at
its terminal, which is where a one-member prefix's cure has to sit. Three new credited test
files carrying twenty-two literal `it` titles and three literal `describe` titles moved the
free terminal tuple `2418/366/2052/20024/5643` to the re-derived terminal tuple
`2421/366/2055/20046/5646`; parked stayed at 366 because every one of those titles is a
literal in a straight-line registration. The live manifest has **41 rows and zero
dispatchable packets**; no non-terminal row currently names the census walker, and a terminal
packet reserves nothing. ⛔ The train's second member is NOT in this figure — it stopped on a
refuted premise and its four files, twenty-eight titles and four suite titles return to
compile with it. For this run no diagnostic, weekly, release or research soak worktree was
created.

⚠ **DATED CORRECTION, 2026-08-14 at the `hb-1` prefix landing.** The paragraph below remains
true history but no longer describes the current holder or packet count.

⚠ **HISTORICAL, 2026-08-14.** GR-4b-iii-a LANDED at `7e7d5205`. Its one credited test file,
one literal `describe`, and eight literal `it` registrations moved the free
terminal tuple `2413/365/2048/19992/5639` to the re-derived terminal tuple
`2414/365/2049/20000/5640`. The three existing registry-driven sites added
three runtime cases but no literal-title census entries. The live manifest has
**34 rows and zero dispatchable packets**; no non-terminal row currently names
the census walker. A terminal packet reserves nothing. For this GR-4B-IIIA run,
no diagnostic, weekly, release, or research soak and no GR-4B-IIIA soak
worktree was created.

⚠ **DATED CORRECTION, 2026-08-14 at the GR-4b-iii-a landing.** The promotion
measurement below remains true history but no longer describes the current
holder or packet count.

⚠ **HISTORICAL PROMOTION MEASUREMENT, 2026-08-13.** GR-4b-iii-a legitimately
took the row after GR-4d released it because it added one file, eight test
titles, and one suite title. Its reserved whole tuple was
`2414/365/2049/20000/5640`, and the manifest then had 34 rows and one
dispatchable packet. The GR-4d paragraph below remains earlier history.

⚠ **DATED CORRECTION, 2026-08-13 at the GR-4d landing.** The promotion
measurement below remains true history but no longer describes the current
holder or packet count.

⚠ **HISTORICAL PROMOTION MEASUREMENT, 2026-08-13.** GR-4d took the row because
it added one file, eight test titles, and one suite title. Its reserved whole
tuple was `2413/365/2048/19992/5639`, and the manifest then had 33 rows and one
dispatchable packet. The GR-4e paragraph below remains earlier history.

⚠ **DATED CORRECTION, 2026-08-13 at the GR-4e landing.** The AO-6 paragraph below remains
true history but no longer describes the current packet count. GR-4e also correctly took no
reservation: changing one existing title without adding or deleting any registration cannot
move a census cardinality. Landing returns its sole READY row to terminal state.

AO-6 LANDED title-neutral at `0facf170`: it created no test file and changed no literal
`it`, `test`, or `describe` title, so no figure of the five-tuple moved. Its two TEST rows
alter assertions inside existing AO-4/AO-5 cases only. At that landing the live manifest had
31 rows and zero dispatchable packets.

⚠ **DATED CORRECTION, 2026-08-13 at the AO-6 promotion.** The paragraph below — AO-5
terminal and the row free — remains true history, but it no longer describes the current
dispatch count. AO-6 is the second promotion after MX-1 that correctly does not take the row
because it cannot move a census figure.

AO-5 LANDED at `72b7a87b`; its one new credited test file added exactly eight literal `it`
titles and one literal `describe` title with no existing title changes. The executed whole
re-derivation is `2412/365/2047/19984/5638`. At that landing the live manifest had 30 rows and
zero dispatchable packets. AO-5 is terminal, so its reservation is historical.

⚠ **DATED CORRECTION, 2026-08-12.** The rule this section stated earlier today — `IN-1a` holding
the row — is **HISTORY.** `IN-1a` LANDED at `5bf06481` and a terminal packet reserves nothing
(`scripts/implementation-packets.mjs:43`). The rules before it — `GR-4b` holding the row, and
before that four packets sharing the census behind `GR-4a`'s row — are older history for the same
reason. **This section has now restamped three times in one day for one recurring reason: the
holder is whichever packet is non-terminal, and landing releases the row.**

⚠ **DATED CORRECTION, 2026-08-12 (second of the day).** The `IN-1b` ruling is **HISTORY** too:
`IN-1b` LANDED at `af3d93c8` (census re-derived WHOLE in that change,
`2408/365/2043/19945/5628`, both new files credited) and a terminal packet reserves nothing.
That was the **fourth** restamp in one day, same recurring reason.

⚠ **DATED CORRECTION, 2026-08-12 — THE SIXTH RESTAMP.** The ruling below is now **HISTORY**:
`GR-4c` LANDED at `cd2ab894` (census re-derived WHOLE in that change,
`2409/365/2044/19960/5635` — the +15/+7 delta decomposes exactly to the new test file plus the
fence's two titles) and a terminal packet reserves nothing. **The row is FREE**, and with IA-2
re-statused SUPERSEDED in this same change there are now ZERO non-terminal packets — the next
promotion claims the row at its dispatch.

⭐⭐ **DATED CORRECTION, 2026-08-12 at the MX-1 PROMOTION — THE CLAUSE ABOVE PREDICTED WRONG, AND
THE RULE IS BETTER FOR IT.** *"The next promotion claims the row at its dispatch"* assumed every
promotion needs the row. **`MX-1` is a promotion that does not**, and it is the first: it adds zero
test titles, zero suite titles and zero test files, so no figure of the five-tuple can move, and
taking the reservation would deny it to a packet that genuinely needs it while buying nothing.
**The row is STILL FREE at `41732a51` with MX-1 in flight.** ⇒ **The rule is restated in its
correct form: the holder is whichever non-terminal packet can actually MOVE a census figure, and a
packet that cannot move one must not claim the row.** Landing still releases it; a promotion no
longer automatically claims it. ⛔ The prohibition below is unchanged and now binds MX-1's
implementer by name: a packet that does not carry the row must not re-record the census anyway.

⭐ **RULED (chair, 2026-08-12) — THE FIFTH RESTAMP, AND THE RULE IS WORKING EXACTLY AS WRITTEN.
`GR-4c` holds `tests/lint/sovereigntyLightingContract.walker.test.js` as its `TEST` row and is
the sole in-flight census holder.** The chair MOVES the row at this dispatch, per the rule
stated above: the holder is whichever packet is non-terminal, landing releases the row, and the
next dispatch claims it. **Five restamps in one day is not churn — it is the reservation system
doing its one job**, and each restamp cost exactly one `PACKET_MANIFEST.json` row.

The reservation was **EXECUTED free at this promotion**, not inherited: at `0b98626f`
`PACKET_MANIFEST.json` carried **22** packets, the only non-terminal row was `IA-2` (`STALE`),
its **twelve** reserved paths intersect GR-4c's eight at **ZERO**, and the walker was named only
by LANDED packets. GR-4c is now the **only** non-terminal packet besides `IA-2`, so exactly one
implementer is in flight. The moment a second non-terminal packet is promoted, this rule
re-applies in full.

⛔ An implementer whose packet does not carry the row must **not** re-record the census
anyway: that is an unreserved edit to a shared enforcer and it defeats the reservation system
it routes around. Report and stop. ⛔ **And a manifest reservation is not the same fact as a
clean working file** — a foreign lane holding that walker dirty at dispatch is its own STOP,
which is exactly the condition `GR-4b` was drafted under before `32f4e520` landed it.

### Minimum decisions needed to unblock

⚠ **DATED CORRECTION, 2026-08-11:** both rows below are **HISTORY, not open work.** GR-3b
LANDED at `40afbdd6` and IN-0c LANDED at `29e2dc3c`; their decisions were made and are
recorded in the packets themselves. The rows are kept because they document what a blocking
decision looks like, and because GR-4a builds directly on GR-3b's producers. **No row in the
current packet set is waiting on a decision.**

- **GR-3b:** provide the exact trigger candidate sets, crossing-score selection
  function/cardinality, negotiated obligor/obligee and transfer direction, and
  the closed collision behavior for the composable security pair. Neither code
  nor binding design law settles those four choices.
- **IN-0c:** rule the exact pulse transport/tick semantics for the signing
  credit, the closed event-kind/audience set, and compliance-state fidelity
  values. Information statecraft currently advances before treaty minting, so
  an implementer may not silently make the credit next-tick or add a deposit.

## Reserved foreign work

⚠⚠ **RESTAMPED 2026-08-12 at the GR-4c promotion (CR-GR4C-6): THE EIGHT-`MM` RESIDUE IS
CLEARED — PORCELAIN IS ZERO.** The note this section carried until now recorded **EIGHT `MM`
entries** measured at `b17d32d2` over an empty `git diff HEAD`, and reserved those eight paths
as stale-index residue. **That residue cleared at the IN-1b flip.** Executed at `0b98626f` at
this promotion: `git status --porcelain` returns **ZERO lines** and `git diff HEAD` is
**EMPTY** — the two agree for the first time across four promotions, so there is no residue set
to reserve and no letters to misread.

⛔ **The commit discipline is UNCHANGED and does not depend on the residue being present.** A
commit into this tree still goes through a **private-index plumbing write**: the index is
shared with concurrent lanes, so a plain `git add` can attribute another lane's staged state
even when the porcelain reads clean at the moment you look. ⛔ **`git diff HEAD` remains the
diagnostic, never `git status --porcelain`** — four consecutive promotions have now had to say
so, three of them measuring a different-sized set of letters over the same empty diff, and this
one measuring none at all.

⛔ **An empty list is a snapshot, never permission.** Every dispatch re-runs `git status`,
reserves whatever foreign changes it finds, and stops rather than staging, restoring, or
attributing them. All four observed-shape files remain independently forbidden edits in every
packet in this set.

## Reconciled program state

The census below exists to prevent duplicate greenfield work. Git history and
live symbols were checked at the measured SHA.

### Partial programs

- **Foreign-policy corpus:** SP-A through SP-F, CW-0w, GR-0 through GR-2 plus
  GR-3a, IN-0a/0b/0d, TR-1, TR-9c, and ES-0 through ES-4 plus ES-5a are landed.
  ⭐ **DATED CORRECTION, 2026-08-14 at the `infra-1` train (INFRA-M1-DOCS): GR-3b
  and IN-0c have BOTH LANDED and neither is blocked.** GR-3b landed at `40afbdd6`
  and IN-0c at `29e2dc3c`; both are proven ancestors of this base, both packets
  read LANDED with every `changeManifest` CREATE path present on disk, and the
  LANDED-4 / LANDED-5 rows in the table above plus the dated correction beneath
  this census already said so. This sentence was the last stale reader of a
  closed program and it is the one a dispatching lane meets first, which is why
  it is cured in place rather than annotated. The "Minimum decisions needed to
  unblock" section below is retained as a worked example of what a blocking
  decision looks like, **not as open work**.
- **Town Cartography:** TC-0 through TC-2 are landed, and TC-3a plus TC-3b have
  now landed in order (TC-3 itself stays SUPERSEDED; its preserved implementation
  was their raw material). TC-4 has LANDED at `5a6f76fe` — the buildings layer is
  filled at schema v2. TC-5 was refused as one packet and split four ways; TC-5a has
  LANDED at `41b39220`, and TC-5b was itself refused and split producer-first at
  CR-TC5B-1 into TC-5b-i (the manifest seam) and TC-5b-ii (the painter mount). TC-5c,
  TC-5d and TC-6 through TC-8 remain designed but intentionally uncompiled.
- **Surveyor Chat:** the old Workshop presentation is superseded. SC-1 is the
  next bounded surface packet; later ingestion and migration stages are excluded.
- **Foreign-policy grammar, GR-4:** ⚠ **REFUSED as one packet and split four ways at
  CR-GR4-1** — it needed six-to-nine existing production files against a budget of three, two
  behavior families, two flag conjunctions and three writers. GR-4a (the dark answer at the
  event) is in history at `a53ef7c6`. ⚠ **GR-4b was then refused IN PART and split three ways
  at CR-GR4B-2:** of the five authored voice surfaces only the disavowal has an honest producer,
  so **GR-4b is the breach voice alone** — compiled here — while GR-4b-ii (`reaffirmed`,
  `honored_by_silence`) and GR-4b-iii (the two open-question surfaces) were dependency-gated
  behind **GR-4d**. GR-4d LANDED at `a17d0a02`, discharging that dependency.
  GR-4b-iii-a — the public question-opening beat — LANDED at `7e7d5205`.
  GR-4b-ii (`reaffirmed`, `honored_by_silence`) and GR-4b-iii-b's dossier
  line remain separate, unreleased surfaces that still require their own
  packets. GR-4c is landed.
  GR-5 is untouched and `treatyRenewalEnabled` does not exist.
- **Foreign-policy information, IN-1:** ⚠ **REFUSED as one packet and split three ways at
  CR-IN1-1** — it carries three behavior families against a budget of one, its receipt half is
  headed for a Herald knowledge desk that does not exist (`HERALD_SECTIONS` is still the frozen
  six, and IN-5 mints the seventh), and three of its seven input families have no durable
  record to derive from. **IN-1a — the mirror itself, the pure leaf — LANDED at `5bf06481`, and
  IN-1b — the dossier standing line, the mirror's first production consumer — LANDED at
  `af3d93c8`**, which DISCHARGES the orphan window IN-1a opened under the just-in-time rule
  (the mirror has a live production caller; the both-states byte-identity ended legitimately
  in the same commit, driven by the rendered-surface dark golden). IN-1c
  (the hums, with their five-file kind registration) is named and compiled just-in-time; it also
  inherits three deferrals IN-1b measured and recorded rather than took — the authored
  `mirror_standing_line` pool (unreachable: no INFORMATION kind registry, no receipt-pool module,
  no annex URL, and the dossier prose corpus that does have a projection is unmounted), the
  INFORMATION kind registry itself, and the seventh input family. ⭐ Its raw
  material was already in the tree and no design document named it: **SP-B landed
  `src/domain/worldPulse/outboundImpression.js` at `4c0f2f38`** with zero production consumers
  and a header naming this wave as its one intended consumer, so IN-1a **composes** it and the
  dated correction into the IN volume rides this promotion. ⚠ Three of the volume's sentences
  are measured false and corrected in the same change: the export name `mirrorOf` is already
  taken (CR-IN1-3, on CR-C4-1), the seventh input family is GRAMMAR-layer and deferred
  (CR-IN1-2), and *"each input already ledgered, already decaying"* is untrue for
  `intelTransfers` (pruned every tick), ally shares (never ledgered) and exposure receipts
  (one-shot news) — CR-IN1-7. IN-2 through IN-6 remain designed and uncompiled.
- **Espionage:** ⛔ **the tail remains PARKED.** ES-7 is refused (five of six Herald kinds have
  no reachable producer) and ES-D is refused (a covert row's endpoints are forced to the peace
  offer's own). **ES-Da — the composite rider — is the single argued split and the only
  compilable espionage slice at this HEAD**, authorized under the owner's refreshed grant and
  compiled READY here. ES-6b stays blocked on a dispatcher nobody has chartered; ES-6c re-files
  out of espionage entirely as a corruption-volume wave.
- **Site Coherence:** the plan is owner-signed for Waves 0–8 (Wave 9 separately). State
  corrected 2026-08-12 (chair, matching row LANDED-18): **SCW-0 LANDED at `d648e788`** —
  the frozen 43-row baseline is the program's reference — and **Wave 1 is DISCHARGED**
  (achieved by the schema-4 genesis `2a7fb033`; recorded SUPERSEDED in the plan), so the
  program reads **1 landed, 1 discharged, 7 open**, each re-verifying its premises at
  compile. ⚠ `SC-` was already taken by the LANDED surveyor-chat packet, hence `SCW-`.

### Complete; do not create greenfield packets

The following designs are already materially implemented and must be treated as
code/history, not open work:

- War WR-1 through WR-10;
- Realm Directives H, I, J, and K;
- Demographic P1 through P5;
- Generosity, Supply-Web Warfare, Peace, Numeric Prices, Convergence,
  Corruption Web, Settlement Politics, Resource Dynamics, Settlement Lifecycle,
  Navy, Momentum, Upswing, Pacing Governor, NPC Lifecycle, NPC Consequences,
  Information Statecraft, Information Brokerages, Route Lifecycle, Magic
  Economy, The Ladder, Track K, Analytics V2, Event Composer V2, Guidance,
  Chronicle Legibility, Map Doors, the current Settlement Map V1/V2, and Hook
  Non-Redundancy HK-1 through HK-3.

A missing progress note in one of those design files does not reopen it.

### Designed but not dispatchable now

- **FP:** ES-5b LANDED at `6c0238ad` (its blocker was three claims; one dissolved, the other two were closed by CR-ES5B-1..7). ES-5c was promoted READY at `ca94438d` (its seven open items closed by CR-ES5C-1..6 plus two chair-authored values) and is BUILT: the promotion-risk register, composed at `npcLadderChallenge.js#defenseScore` rather than the `npcLadderContest.js` the design names. ES-5d (the career CREDIT) is compiled just-in-time after ES-5c lands and must BUILD four things — a persisted carrier, an identity bridge, a ladder-owned `stock` writer, and a coupling row. **HB:** the
  volume has no sealed close, and HB Q1's two owner-gated persisted fields
  (`deployment.habitEpisode`, `treaty.habitEpisode`) remain unsigned. **By Q1's own words
  that seal binds HB-3 onward** — `OWNER_DECISION_QUEUE.md` §34's OQ-TC5-2 — **so HB-0 and
  HB-1 are dispatchable once their Q2/Q3 are ruled at compile**, which §36 did. HB-2 and
  HB-4+ remain gated. **EP** is unchanged and still lacks a sealed close/attestation.
  WC's old gate wording must be reconciled with the
  later owner grant. WY, WF, POP, INT, late CW, and later TR/GR/IN waves remain
  dependency-ordered behind nearer work.
- **War Convenience/Tribute/Statecraft:** CV/TB/XW remain at the Lane-A tail.
- **Realm Magic leak L2/L3:** a recorded blocker/design decision, not a coding
  detail for an implementer to invent.
- **Parked/evidence-gated:** tuning, lighting, soaks, pushes, marketplace,
  gallery showcase activation, deep scarred-map promotion, AI Intent Phase B,
  and custom-content promotion evidence.

No packet should be generated for these rows until its named blocker clears.

## Source roles

| Source | Safe role | Unsafe use |
|---|---|---|
| Live code and git log | Existence, landing, symbol, and behavior evidence | None; this is the existence authority |
| `CONTRIBUTING.md`, `ARCHITECTURE.md`, canonical contracts | Invariants and operating law | Open-work status |
| `DESIGN_*.md` | Product intent and durable architecture after reconciliation | Direct dispatch or proof that work is open |
| `FABLE_VALIDATION_QUEUE.md` | Later rulings and landing receipts | Sole proof that a symbol still has the recorded shape |
| `SOL_QUEUE.md` | Historical dependency/order evidence | Current build state |
| `docs/briefs/` | Historical implementation rationale | Current commands, branch, budgets, or dispatch |
| Ledger-only `START_HERE`, `RESUME_STATE`, `THE_REMAINING_ARCHITECTURE` | Recovery and decision provenance | Coding instructions |

## Reconciled legacy traps

- `DESIGN_FP_ARCHITECTURE.md`'s former `d9c7cae4` progress block omitted later
  waves. It is restamped in this documentation change as dated corroboration;
  this index still controls dispatch.
- `SOL_QUEUE.md` retains old F9 and CR-WC-9 gate strings inside historical
  rows. Its new top note records the later blanket grant and points here; do
  not dispatch from the embedded strings.
- `docs/README.md`'s stale design-file count was removed rather than replacing
  it with another count that would drift.
- The ledger root and the build worktree are different trees. Packets live in
  the build worktree.

## Packet opening protocol

Before changing a DRAFT/BLOCKED row to READY, the coordinator must:

1. re-run the open-work census against live git;
2. resolve all owner-ruling and design contradictions;
3. verify every manifest path and symbol;
4. confirm target files are clean and collision-free;
5. state baseline and golden posture;
6. close every operative choice in the packet;
7. keep the acceptance denominator at eight or fewer cases; and
8. update this index and the packet in the same documentation change.

## After a packet lands

1. Record the landing SHA and exact gate evidence in the packet.
2. Change the packet and this index to LANDED.
3. Re-derive the program rather than promoting the next historical queue row
   automatically.
4. Compile at most the next immediate dependent packet.

This just-in-time rule prevents the instruction project from becoming a second,
stale implementation backlog.
