# MF FAMILY PACKET PREAMBLE — the invariants signed once per volume

> **DRAFT compiled by lane TC-D3A (OPUS COMPILE seat, ODQ §291.5) at the code of record
> `claude/composite-r4` @ `9d851fae`, for the FABLE chair to sign.**
>
> ⛔⛔ **A LANE MAY NOT AUTHOR GOVERNANCE.** ODQ §299.5 struck exactly that act — an implementing
> lane wrote a "handoff law" into `docs/implementation/INDEX.md` and the text was declared VOID,
> *"that its text happens to resemble §291.5 is irrelevant; authority is the point."* This file is
> therefore a **compiled draft**, not a landed law. It becomes law only by a chair act, landing at
> `docs/implementation/preambles/MF-PREAMBLE.md`.
>
> Per `docs/DESIGN_BUILD_EFFICIENCY.md` §4 one family preamble per volume lives at
> `docs/implementation/preambles/<VOL>-PREAMBLE.md`; each member packet then carries only its own
> scope, contract, manifest, acceptance, mutants and hazards, plus a line citing this file **BY
> SHA-256**. An edit here re-stamps every citing packet, so drift between the family's members and
> their shared law is structural rather than merely discouraged.

- **Status:** **LANDED, chair-signed**, at `f20b9faa` (ODQ §314; STAMP GRANTED at ODQ §312.2b). Cited by the SHA-256 of this file as landed; a later edit re-hashes it and the editing act owns every citing packet's re-stamp in the same landing (ODQ §314.2).
- **Amendments:** **§315** — the status line above corrected to a landed value and the citation prohibition dropped (raised by MF-T2A). **§324.5** — §P2.2 gains the post-build dormancy clause and the forensic-zoom obligation, raised by MF-T2A's RAISED-2 and executed at MF-T2B's landing, which is the member that first proved the fence discharges only after a build. Each amendment re-hashed this file and re-stamped both citing packets in its own landing, per §314.2.
- **Volume:** `map-corpus/docs/GENERATION-SPEC.md` — THE GENERATION SPECIFICATION, the map
  program's spine and build sheet (§246.3), as corrected by ⟦FOLD §287⟧ **§10**, which *"wins on a
  direct conflict with §§6–§9"*.
- **Family:** `town-cartography` — twenty packets at `9d851fae` (thirteen `MF-*`, seven `TC-*`);
  D3a's members continue as `MF-T2x`.
- **First citing members:** `MF-T2A`, `MF-T2B` (drafts), then the D3a roster of `draft-D3A-PLAN.md` §0.
- **Extraction source:** `map-corpus/docs/GENERATION-SPEC.md`, **worktree blob `aa613cb8d9ea…`**,
  12,667 lines, read in the ledger working tree at `c5ba15e4`. ⚠ **The ledger-HEAD blob is
  `7dcd70abe85c…` and the two DIFFER** — see §P9. Every law below is a quotation or a compression
  of that blob, of the ODQ decisions cited in §P10, or of a figure this lane executed; **this file
  invents nothing.**
- **Compiled against base:** `claude/composite-r4` @ `9d851fae`. ⛔ `BASE_STATE.json` is stamped
  `b8946403` and is **NOT admissible here** (§P8) — every figure in §P9 was measured from scratch.
- **Citation law:** a member packet cites this file BY SHA-256 in its header.
- ⛔ **THIS FILE CARRIES NO PER-WAVE FIGURE.** Every census tuple, effective-line count, seal and
  denominator lives in the member packet and is re-executed there. §P9's rows are stamped facts
  about the base, not budgets.

---

## ⛔⛔ THE STAMP LINE — READ THIS BEFORE PLANNING ANY TRAIN

**`STAMP: GRANTED — `OWNER_DECISION_QUEUE.md` §312.2b (2026-08-21, the Fable chair).`** The
family's stamp substance — Fable examination of its law substrate — is discharged beyond any
§70-era round by the §291–§310 record itself: the retrovalidation sitting over the program's
rows, the CF-1-corrected GENERATION-SPEC, and every wave since D0 collected by the Fable chair
in person under laws L1–L7. `PACKET_STANDARD.md`'s differential member caps therefore put this
family in the **stamped column: EIGHT members per engine train**. (As compiled, this line read
`STAMP: NONE` at `9d851fae`; the chair's §312.2b grant supersedes it at signing — recorded here
so the supersession is legible, per the family-preamble stamp law.)

> ⛔ *"The stamp is a precondition, never a default. A compile that cites a substrate annex
> carrying no Fable-round stamp inherits a STOP. An un-stamped family stays at four however light
> its waves look, and no compiler may read a light wave class as a stamp. **A family's own preamble
> carries its recorded stamp status; that line, not this table, is where a compiler reads which
> column it is in.**"*

⚠ **This line is the one thing in this file a lane could not have invented and must not.** If the
chair grants the family a stamp — the codex slice's thirteen landed packets and W3's seal are the
executed evidence a grant would rest on — this line changes to `STAMP: GRANTED at <ODQ §>` and
every D3a train re-plans at eight. **The grant above executed exactly that; the anticipation is preserved as the lane wrote it.**

---

## §P0 · THE VOLUME'S OWN STANDING WARNING, RESTATED FIRST

> ⚠⚠ **THE GENERATION SPECIFICATION IS A BUILD SHEET, AND ITS §§6–§9 ARE SUPERSEDED WHERE §10
> DISAGREES.** ODQ §287.2: *"`GENERATION-SPEC` fold labels §§277–§286 are accepted as DESIGN-ONLY
> amendments… **They were never built and may not be reported as built.** §10 is ⟦FOLD §287⟧ and
> wins on a direct conflict with §§6–§9."*

⛔ **ARCHITECTED IS NOT BUILT, AND THE VOLUME SAYS SO ABOUT ITSELF.** The spec's own header
records that the §275 adoptions are *"architected here as design, with NO implementation (owner's
instruction — no credits)."* An MF packet that reads a spec section as a description of live code
has read a plan as a receipt.

⛔ **LIVE CODE OUTRANKS EVERY TABLE IN THE VOLUME. NAVIGATE BY SYMBOL, NEVER BY LINE NUMBER.**
Every line number in the volume is an evidence anchor, not an address.

⚠⚠ **AND THE VOLUME IS NOT ON THE BUILD BRANCH AT ALL.** `map-corpus/docs/GENERATION-SPEC.md` is
absent from `9d851fae`; it exists only on the ledger branch. An MF compile therefore reads its
volume from a *different tree* than the one it prices against — which is exactly the condition
under which a stale citation survives unnoticed. **Cite the blob, never the path.**

## §P1 · THE MF-ERA REFUTATIONS THIS PREAMBLE ADDS — each STOP-class, each executed

Five, found by lane TC-D3A at the D3a compile. Each was invisible to reading and visible only to
execution; each binds **every** MF wave, not just the one that found it.

**R-MF-1 · THE CODEX KERNEL IS QUADRATIC, AND THE DOMINANT TERM IS ONE LINE.**
`derivePlanarDcelEmbedding` measured `759.4 / 4,258.6 / 13,634.9 ms` at `2,112 / 4,704 / 8,320`
boundaries — fitted exponents `1.99` and `2.04`. The cost is
`const start = [...unvisited].sort(compareCodepoint)[0]` **inside** the face-traversal loop:
`O(F · E log E)`. Amortising it to a pre-sorted cursor gives **12.7×**, SHA-identical at four
sizes. ⛔ **A port member that re-expresses a sandbox algorithm PRICES ITS COMPLEXITY against the
real leaf sizes `laneMFD1-receipt.md` §6.3 publishes (town 11,603 boundaries, metropolis 17,417),
not against its fixture.** The codex fixture is a 900×900 rectangle; every hot loop in that stack
was written against it.

**R-MF-2 · AT ABI SCALE, `Number` ARITHMETIC DOES NOT DRAW A WRONG MAP — IT THROWS ON VALID
GROUND, WEARING THE WRONG MESSAGE.** ABI quanta reach ~1.3e9, so a cross product of coordinate
differences reaches ~1.66e18, **184× past `MAX_SAFE_INTEGER`**. Constructed and executed on
`a=[0,0] b=[m−1,m] c=[m,m+1]`, `m = 1286630001`:

```
Number orient = 0     BigInt orient = -1     the SIGN FLIPPED
compareRay then THROWS 'DCEL vertex has an angular tie' on a NON-tie
```

⛔ **EVERY MF MODULE THAT DECIDES TOPOLOGY, ORDERING OR IDENTITY FROM A PRODUCT OF ABI QUANTA USES
BigInt**, and says why in the file. A float sign at a vertex makes the angular order
nondeterministic — the one thing a planar embedding rests on. ⚠ And the failure is a *throw with a
misleading message*, so it will be debugged as a geometry defect for as long as it takes someone
to suspect the arithmetic.

**R-MF-3 · WIDENING AN ACCEPTANCE RANGE IS BYTE-NEUTRAL; MOVING A VERSION STRING IS A 157-PIN
SHIFT THAT FITS IN NO PACKET.** `FABRIC_COORDINATE_ABI = 'plan-q1-0-1000-v1'` is stamped by
**eight** fabric modules and pinned by **157 literal digest pins (118 distinct values) across 12 test files**.
Twelve test files alone exceed the twelve-handwritten-file cap. Measured, narrow vs widened
validator over identical in-range fixtures: `IDENTICAL` at four sizes. ⇒ **An MF wave widens a
range freely and NEVER moves a stamped version string as a side effect.** A version-string cutover
is its own declared-shift micro-wave, D0-pattern, and is planned as one.

**R-MF-4 · THE SANDBOX CODE OF RECORD IS NOT IN GIT.** `git rev-parse` in the sealed W3 tip
returns `fatal: not a git repository`. ⇒ **A port packet cannot cite a source commit.** It names
each source module by **path plus SHA-256 at compile time**, in the packet body; the implementer
re-hashes before editing, and **a mismatch is a STOP, not a merge**. ⛔ And because the tip is the
program's only copy of the W3 evidence, **no lane edits it** — a cure that must live in the
sandbox is a cure that should live in the port instead.

**R-MF-5 · A FABRIC LEAF IS FREE ON FOUR ESTATE REGISTERS, AND EXACTLY THOSE FOUR ZEROS END AT THE
DIRECTORY BOUNDARY.** Measured at `9d851fae`: the coupling/layer census scope is
*"total over `src/domain/worldPulse` and `src/domain/spatial` only"* by its own standing
cannot-catch list; no `townMap` path carries a `scripts/.size-baseline.json` entry (so the plain
`src/domain/**` 800-effective-line ceiling applies); no `townMap` file is on the hot-file list; and
`src/domain/townMap/fabric/**` has **0 duplicated export names of 94**. ⛔ **An MF wave that lands
a leaf outside `src/domain/townMap/**` re-prices ALL FOUR from scratch** — see §P4's second table.

## §P2 · THE HAZARD DISPOSITIONS THAT ALWAYS APPLY

1. **THE FABRIC HAS NO INPUT PRODUCER, AND THAT IS THE DORMANCY PROOF — NOT AN ASSUMPTION.** The
   only construction of the admitted plan kind in the whole tree is a hand-typed test fixture. A
   settlement cannot reach this code. ⛔ **Every MF packet still executes its own dormancy
   evidence** (implementation-order step 1) and quotes it; the structural fact makes the claim
   *provable*, it does not make it *proved*.
2. **THE BARREL IS INSIDE THE STATIC IMPORT GRAPH AND IS SURVIVABLE ONLY BECAUSE NOTHING EAGER
   IMPORTS IT.** `src/domain/townMap/index.js` re-exports the fabric and is imported by 14 live
   modules. `tests/build/townMapLazy.test.js` is the standing enforcement. ⛔ **Every MF member
   runs it focusedly** — an added leaf that drags a new transitive import into the entry closure
   is exactly how a dormant port stops being dormant, and the first-paint budget is where it
   shows.
   ⛔⛔ **AND IT DISCHARGES POST-BUILD ONLY: A PRE-BUILD SKIP IS NOT A DISCHARGE** (ODQ §324.5,
   raised by MF-T2A and executed at MF-T2B, the first member to add production leaves). The file
   is `describe.runIf(distExists)` and a second arm is additionally `it.skipIf(!process.env
   .VERIFY_DIST)`, so on a fresh worktree the whole suite is gated off and the run reports exit
   **0 over ZERO executed tests** — the estate's "a green that never ran" shape. **Build `dist/`
   first, then run it under `VERIFY_DIST=1`, and report the EXECUTED counts.** ⚠ And because the
   fence keys on `townMapModel`'s own fingerprint rather than on the arriving leaf, a member that
   adds one owes the forensic zoom as well: walk the entry chunk's transitive static closure and
   confirm that a string literal unique to the new leaf appears in **none** of those chunks and in
   a lazy chunk elsewhere in `dist/` — an absence measured against a denominator that does not
   contain the surface proves nothing about it.
3. **A BARREL HOP DRAGS THE WHOLE FAMILY.** The sandbox's `coordinateAbi.js` imports three symbols
   from a **681-effective-line** module; the measured transitive closure of what the five D1
   foundation modules actually need is **19 declarations / ~170 effective lines**. ⛔ **An MF port
   member measures its import closure before it prices its leaf**, and ports the closure, not the
   module. Find the back-edge by cut-one-edge simulation.
4. **`export *` IS FORBIDDEN IN THE FABRIC BARREL.** The barrel uses named blocks per module. A
   star export makes the single-declaration law unenforceable and makes name ambiguity a runtime
   question.
5. **THE ARRANGEMENT-QUANTUM LADDER IS OWNER SURFACE.** `[1000, 5000, 25000]`; the three rungs
   joined the TUNING-SIGNATURE docket at ODQ §310.3(9). ⛔ **No MF wave picks a rung.** A quantum
   is a constant, constants are owner-signature surface under THE PROMISE.
6. **THE Y-AXIS FLIP IS OWED AND MUST STAY OWED HONESTLY.** The fabric draws in a y-DOWN view
   frame while the world convention is +Y north; the ABI records
   `viewToWorldYFlipApplied: false`. ⛔ **No MF wave applies it** — it would move every published
   coordinate. It is owed by the first wave that publishes a world-framed artifact, and ODQ
   §303.6 says so.
7. **`heightQ()` THROWS, AND AN UNEXERCISED PATH THAT STARTS WORKING SILENTLY IS A DEFECT.** No
   height is published in the plan era. The member that opens that door does so deliberately, in
   its own packet body, and owns height's first publication.
8. **THE INCUMBENT `groundLaw.overlapping` IS NOT CONVICTED.** D1 measured
   `legacyFalsePositive = 0`: §17's boolean is not wrong, it is **unquantified**. ⛔ **No MF
   packet may report it as a defect**, and nothing in the generation path calls the exact
   predicate until a wave declares that cutover.
9. **A FIXTURE MAY BE UNABLE TO EXECUTE ITS OWN DEFECT.** Run the specified fixture and print what
   the engine actually did **before** writing the pin. An early-`continue` guard can swallow every
   tick, making base and cure identical and the pin vacuous.
10. **THE PIN-VACUITY FAMILY, ALL FOUR ARMS.** No MF pin may: assert a list against itself; mirror
    the deriver in its fixture (a fixture built by the deriver can never see a dead arm); assert
    over a surface that never rendered; or sit under an unreachable arm. Every negative carries a
    **positive control in the same test**.
11. **A COUNT MUTANT ON A LITERAL GOES VACUOUS**, and a redundant second guard subsumes the first.
    **MF mutants convict a BRANCH**, never a constant.
12. **A READER OF THE SAME WRONG BAND THE CENSUSES CANNOT REACH.** §310.1's banked lesson: ditch
    gardens were drawn inside the ring street's carriageway **110/110 since §250.5** with **no
    census able to see it**, because a garden is open ground and lives outside `drawnBodies`. ⛔
    **An MF census over a DERIVED set proves nothing about a surface it does not contain.** Name
    the denominator; a forensic zoom is standard, not optional.

## §P3 · THE §31 ANCHOR PREFLIGHT — MANDATORY, PER NEW TEST FILE

OWNER_DECISION_QUEUE §31 ruling 2 is law: *"every new acceptance file runs the negative-assertion
anchor walker focusedly BEFORE its member proof is declared green."* ⛔ **A NEW TEST FILE HAS
CEILING ZERO IN EVERY TREE**, `tests/domain/`, `tests/property/` and `tests/lint/` alike; the
scanned matchers are `not.toContain` / `not.toMatch` / `not.toHaveProperty`.

**TO COMPLY**, one of:
1. route through `tests/helpers/anchoredNegatives.js` (`expectPresentThenAbsent` /
   `expectAbsentWithAnchor`) **called by name on the same line** — an alias or wrapper is not
   recognised; or
2. carry `// anchored: <why this cannot go vacuous>` on the assertion line **or the line
   immediately above it**. ⚠ For a multi-line comment only the **LAST** line counts.

⚠ **THIS BITES THE MF FAMILY HARD**, because the family's signature pins are absences: *no
consumer imports this leaf* · *no digest moved* · *no duplicate export exists* · *no `Date`,
`Math.random` or `Intl` is reachable*. Every one is a negative and every one owes an anchor.

**THE PREFLIGHT COMMAND** (bare, in-shell, never piped):

```
npx vitest run tests/lint/negativeAssertionAnchor.walker.test.js ; echo TRUE_EXIT=$?
```

## §P3b · THE MUTATION-COVERAGE ROW EVERY NEW `tests/lint/**` FILE OWES (ODQ §102.2)

⛔ **PRICED AT COMPILE, NOT DISCOVERED AT THE GATE.** `tests/lint/mutationCoverage.shared.mjs`
enumerates `tests/lint` among its `ENFORCER_DIRS`, and `tests/lint/mutationCoverageManifest.test.js`
asserts every enumerated file owns an `invariants` entry in
`scripts/mutation-coverage-manifest.json`. **A new `tests/lint/**` file is an enumerated invariant
the moment it lands.**

⚠⚠ **AND THE OBLIGATION IS INVISIBLE TO A FOCUSED RUN**, which is how it was found twice in other
families: the member's own focused battery stays green at its own commit, and the red arrives at
the train's terminal gate, where it is most expensive.

**TO COMPLY**, in the ruling's order of preference:
1. plant a standing regression in `scripts/mutation-sweep.sh` proving the file reds (**preferred**;
   it makes the row `kind: 'mutation'`). The family's own precedent is entry 28,
   `check_caught "massing/silhouette totality unmapped kind" … "npx vitest run tests/lint/townMapMassingSilhouette.walker.test.js"`; or
2. add ONE `rationale` entry with a written reason describing **real convicting power**.

⛔ **NEVER as an `uncovered` row** — that list only ever shrinks, and the walker says so in its own
failure message.
⛔ **NEVER by re-serializing the manifest.** Insert surgically beside its siblings and verify the
diff is a pure insert; `uncoveredBaseline` is untouched.
⚠ The sweep's own revert is the `git checkout --` family this program's shared-tree protocol
forbids outright, so **no build lane may run the sweep on this tree** — the deferral is real and
recorded, not an excuse.

## §P4 · THE REGISTRATION TEMPLATE — MANDATORY AT FULL STRENGTH (ODQ §35.3)

Every MF packet that mints a row in a registration file names, **EXPLICITLY AND BY PATH**: the
ROW, the HEAD RE-EXPORT (or the reason none exists), the EXACT-LIST / EXACT-COUNT PIN, and the
REGISTRY TEST PATH. **A packet that names fewer than four is defective on its face.**

**THE REGISTERS A `src/domain/townMap/fabric/**` MEMBER FIRES — measured at `9d851fae`:**

| Register | Instrument | Form | Cost |
|---|---|---|---|
| the test census | `tests/lint/sovereigntyLightingContract.walker.test.js` | the five-figure tuple, **SEQUENCED** (§P5) | per member |
| mutation coverage | `tests/lint/mutationCoverageManifest.test.js` | one `invariants` row per new `tests/lint/**` file | per such file |
| the anchor walker | `tests/lint/negativeAssertionAnchor.walker.test.js` | ceiling **zero** per new test file | per test file |
| the entry-closure fence | `tests/build/townMapLazy.test.js` | the fabric stays out of the entry static closure | per member |
| the single-declaration law | `tests/lint/townMapFabricSingleDeclaration.walker.test.js` | exactly zero duplicated export names (MF-T2A) | per member |
| the coupling/layer census | `tests/lint/couplingInclusion.walker.test.js` | ⭐ **NOT FIRED** — scope excludes `townMap` | **zero** |
| the size ratchet | eslint `max-lines` + `scripts/.size-baseline.json` | ⭐ **plain 800-eff `src/domain/**` ceiling; no baseline row** | **zero** |
| the hot-file law | `PACKET_STANDARD` standing list | ⭐ **no `townMap` path is on it** | **zero** |

**⛔ AND THE REGISTERS AN MF MEMBER FIRES OUTSIDE THAT DIRECTORY — every zero above turns on:**

| Register | Instrument | Form |
|---|---|---|
| the unlayered census | `tests/lint/couplingInclusion.walker.test.js` | `expect([...UNLAYERED_BASELINE].sort()).toEqual([...LIVE_UNLAYERED].sort())` |
| the argued roster | same file | `ARGUED_ROSTER_CEILING` `.toBe(20)` — ⛔ **exact in BOTH directions**: a bound that only forbids growth lets a shrink go unbanked |
| `spatialLedgers` coverage | `tests/lib/spatialLedgerCoverage.walker.test.js` | `expect(classified).toEqual(written)` — the row lands **with** its writer, never ahead |
| the engine-gated flag manifest | `engineGatedRuleKeys.walker` + `subsystemRowsVirtual` | the two-way audit + the ordered-equality pin ⇒ manifest row, first by-name gate read and certification row are **ONE ATOMIC ACT** |

⚠ **A `LAYER_PATTERNS` addition is safe while its target is absent; an `ARGUED_UNLAYERED` addition
is NOT** — the argued arm asserts `expect(DOMAIN_MODULES, '<module> vanished — re-aim the
exclusion').toContain(module)`, so an argued entry **must** land in the same commit as its leaf.
⭐ **And the choice is not "pick the cheap name":** the roster reserves the argued door for
*"modules that own NO subject and are spoken by every port."* A leaf that owns a subject takes a
**family home**, and a family home is what forces the next consumer to register its coupling
instead of reading across a port in silence.

## §P5 · THE GATE-READING LAW, AND THE CENSUS LAW FOR THIS FAMILY

- ⛔⛔ **NEVER WRAP `npm run check*` IN `gate-mutex.sh --run`.** `test:ratchet` re-acquires and
  self-deadlocks; **exit 3 is the mutex giving up, not a red.** Run `check:tail` **bare**, from a
  fresh shell, with `; echo TRUE_EXIT=$?`, and **outlast it in your own turn.**
- ⚠ **AN EXIT NOT READ FROM THE GATE'S OWN TAIL IS NO RECEIPT.** **Trust no exit status you did
  not capture.**
- **`npm run check` is a 17-step `&&` chain.** A red step blacks out every later step; the receipt
  must say which steps actually ran.
- **Report BOTH typecheck configurations by name and window:** `typecheck:ratchet`
  (`tsconfig.full.json`) and `typecheck:domain:strict` (`tsconfig.domain-strict.json`). A ratchet
  being green is evidence only about that ratchet.
- **A failing enforcement walker is never banked as ordinary test debt.**
- ⚠⚠ **THE TEST CENSUS IS SEQUENCED AND STOPS AT ITS FIRST RED FIGURE.** When `files` reds,
  `parked` / `credited` / `titles` / `suiteTitles` **never execute** — a green there is a green
  that never ran. In a multi-member train, `suiteTitles` is proved **separately**: grep the WHOLE
  train diff over `tests/` for added `describe(` lines and require the exact predicted count.
- ⚠ **`credited` IS THE FIGURE THAT LIES.** A `test.each()` case is invisible to the census by
  construction and a `describe.runIf()` parks a file WHOLE; either keeps the arithmetic closing
  while `credited` silently does not move. ⛔ **Every MF acceptance file is ONE literal `describe`
  with straight-line `it`/`test` calls and string-literal titles. No `.each`, no `runIf`, no
  nesting.** A loop over a source tree runs **INSIDE** one named test — the SP-D idiom, which this
  family's own `townMapMassingSilhouette.walker.test.js` already follows.
- ⚠ **A PARKED FILE SWALLOWS ITS TITLES.** A census delta **smaller** than the titles you added is
  not arithmetic to accept — attribute it by reverting ONE test file at a time.
- **A census re-record re-derives ALL FIVE figures together** and names its cause; it never patches
  one figure at a time.
- ⛔⛔ **AND EVERY CENSUS OR RATCHET MOVE NAMES ITS AUTHORIZING DECISION IN THE PACKET BODY.**
  ODQ §299.4 ruled the codex slice's thirteen self-authorised re-records an AUTHORITY breach —
  retroactively authorised because the coverage was real, and **BINDING FORWARD**. Every MF packet
  carries a `censusAuthorization` block with a resolvable `§`-ref. **A packet without one is
  defective on its face**, and the packet validator gains a refusal for exactly this.
- ⭐ **THE TEST CENSUS CANNOT ABSORB DEBT.** `scripts/.test-ratchet-baseline.json` is monotone-down
  against a literal `CEILING`; *"you may burn it; you may never pad it."* ⛔ **An MF wave whose
  honest cure is gated leaves the identity red-with-attribution and ESCALATES; it never censuses
  it.**
- ⚠ **A BANKED FAILURE LIES TWICE.** Cure only with a SEPARATE **passing** pin, never by renaming
  — the title IS the census key — and `introducedAt` can be flatly wrong; re-derive by transition
  walk.

## §P6 · MUTANT HYGIENE

- Every mutant is **planted, convicted, and restored digest-exact** — the restore is proved by
  digest, never by eye.
- **A mutant convicts a BRANCH**, never a literal count (§P2.11).
- ⚠ **A REDUNDANT SECOND GUARD SUBSUMES THE FIRST.** Where two guards can each independently hold
  an invariant, the mutant must delete the one under test **and** prove the other does not cover
  for it, or the conviction is vacuous. A `live` flag once hid a deleted counter through seven
  mutants.
- **A guard that cannot be reddened cannot be proven**: every structural guard lands with its
  executed red proof at the wave that mints it.
- ⭐ **THE FAMILY'S STRONGEST NON-VACUITY SHAPE IS THE `guard-the-guard` ARM**, and it is already
  in the tree: `townMapMassingSilhouette.walker.test.js`'s first test asserts the live vocabulary
  is not empty *before* the totality assertion runs, because *"if either silently emptied, the
  totality assertion below would pass on nothing."* **Every MF walker opens with that arm.**

## §P7 · STANDING STOP CONDITIONS

An MF wave STOPS — and reports rather than repairs — on any of:

1. A live **seed** byte moves. (A declared, captured artifact shift inside a dormant module is that
   member's expected result, named in its own packet, and is not this condition.)
2. A new consumer appears outside `src/domain/townMap/fabric/**`, or `tests/build/townMapLazy.test.js`
   reds — the dormancy boundary breached.
3. A port source's SHA-256 does not match the packet's recorded hash (§P1 R-MF-4).
4. A stamped version string (`FABRIC_COORDINATE_ABI`, `SHAPE_COORDINATE_ABI`, a law or schema
   version) would move as a **side effect** rather than as the member's declared subject
   (§P1 R-MF-3).
5. `parked` moves off its recorded value, or a census figure moves by other than the member's
   predicted delta (§P5).
6. A census or ratchet move without a `censusAuthorization` ref (§P5).
7. An arrangement-quantum rung, or any tuning constant of any kind (§P2.5).
8. The y-axis flip (§P2.6), or `heightQ()` returning a value outside the member that owns height's
   first publication (§P2.7).
9. A new leaf outside `src/domain/townMap/**` without a layer home or a written argument **in the
   same commit** (§P4).
10. A flag declared without its first by-name gate read in the same commit; or a `spatialLedgers`
    row without a live `setSpatialLedger` writer.
11. Two members of one train reserving a shared `changeManifest` path at once. ⛔ **DRAFT reserves
    exactly as READY does**, so demotion is not an escape; the cure is `CR-HB2B-SPLITP` staged
    promotion, **never a validator edit**.
12. An interior red that was not NAMED in the train plan before it existed. ⚠ **A member that adds
    a test file forces one by construction** — the census arm is an exact equality — so a plan
    declaring zero interior reds for an MF train has mis-declared.
13. A `requiredSymbols` row naming a symbol the deliverable CREATES, at `READY`; or one symbol in
    both `requiredSymbols` and `retiredSymbols`.
14. A frontage/parcel equivalence claim — ODQ §310.1 states S9 equivalence is **NOT BUILT** and
    carries it forward.
15. A further overstatement in the volume beyond the ⟦FOLD §287⟧ §10 corrections: **STOP before
    building on it**, and add it to this family's refutation register.

## §P8 · CAPSULE ADMISSIBILITY AND THE RE-EXECUTION LAW

`docs/implementation/BASE_STATE.json` is citable as executed **only** by a compiler whose verified
base is exactly `stampedAt`, **or** whose base is a DOCS-ONLY descendant of it with every measured
path byte-identical across that window (chair judgment **J-T1**). ⛔ **The discharge is an EXECUTED
`git diff --name-only`, quoted in the packet — never an assertion that the window "looks
docs-only".**

⛔ **AT `9d851fae` THE CAPSULE IS INADMISSIBLE.** It is stamped `b8946403` (2026-08-16) and the
window carries D0, D1, CB1, I1 and the WF work. Per its own `consumptionLaw`, *"a capsule stamped
at any other sha is worthless for citation"* — so **every MF figure is measured from scratch**
until a capsule is regenerated at an MF base.

⚠ **And regardless of admissibility: every row a manifest touches is re-executed by the member
that touches it.** The capsule covers the UNTOUCHED base and never the wave's own surface, because
those are exactly the rows where staleness kills.

⚠ **`package.json` AND `package-lock.json` ARE GOVERNED PATHS.** A dependency bump is a schema-mint
trigger; **no MF wave takes one.**

## §P9 · THE FAMILY'S MEASURED SUBSTRATE (executed at `9d851fae`; re-execute what you touch)

- **The volume blob is `aa613cb8d9ea…`** (worktree, 12,667 lines) and the **ledger-HEAD blob is
  `7dcd70abe85c…`**. ⚠⚠ **THEY DIFFER**, established by the §300.2a cmp method rather than by a
  diff listing: `git show HEAD:… | cmp -` reports `differ: char 360486, line 4084`. ⛔ **`git diff
  HEAD` is POISONED in the ledger tree** by the shared index's staged `map-corpus/` deletions — it
  reports whole files deleted. **Attribution here is `git show HEAD:path | cmp`, never a diff
  listing.** An MF packet cites the blob it read.
- **The two fabrics.** The codex slice landed **18** modules at `src/domain/townMap/fabric/`; the
  sealed W3 sandbox tip carries **54** at the same relative path. ⭐ **ZERO basename overlap**
  (`comm -12` over both sorted lists is empty), so the port ADDS into an occupied directory with no
  filename collision and no `validate:packets` path reservation against the LANDED codex rows.
  ⚠ CX-1's figure of 47 sandbox modules is **stale, not wrong** — W3F added modules after it
  measured.
- **The port's total volume: 14,230 effective production lines across 54 modules.** Against the
  400-effective-line packet cap that is **≥ 36 members**. **22** modules exceed the
  250-effective-per-new-leaf cap outright; five exceed 650 (`buildFabric` 811, `substrate` 715,
  `streets` 713, `parcels` 685, `fabricGeometry` 681). ⛔ **The port does not fit one wave, and an
  MF plan that implies it does has mis-priced.**
- **The D1 foundation spine is small.** The transitive closure inside `fabricGeometry.js` seeded
  with exactly what the five D1 foundation modules import is **19 declarations / ~167 effective
  lines of 681** (four one-line bodies measure 0 under `max-lines`, so ~171 is the honest figure).
- **The `src/domain/**` size ceiling is 800** effective lines under
  `max-lines {skipBlankLines: true, skipComments: true}`. Reference effective counts at
  `9d851fae`, measured with eslint's own `Linter`: `dcelEmbedding` **169** · `dcel` **89** ·
  `foundation` **143** · `boundaryArrangement` **247** · `parcelRegistry` **216** ·
  `fabric/index` **101** · `operations` **278** · `projection` **374** · `townMap/index` **133** ·
  `store/operationRegistry` **244** · `townMapSettlementFabricFixtures` **266**.
- **The §287.7 domain door lives in `src/store/`**, not `src/domain/`:
  `operationRegistry.js`, `operations.js`, `actionResult.js`, `outbox.js`. ⛔ **The §299.3c merge
  therefore crosses `src/domain/**` → `src/store/**`** and is a chair question, not a refactor.
- **`deriveHighWater` is SANDBOX-ONLY** (`tierGrammar.js:574`; absent from `9d851fae`). §306.2a's
  *"three evidence channels empty on 16 of 17 leaves"* is a **producer** gap app-side, not a
  reader defect — which is what splits the §306.4a batch into an expensive engine half and a free
  consumer half.
- **Rulings every MF wave consumes rather than re-argues:** **§303.5** (the adopt/replace split;
  D1's ABI wins) · **§299.3** (the three obligations: exact solid legality; the light profile
  conforms or is stripped; the second operation vocabulary merges) · **§287.16** (dormant explicit
  input; live seeds byte-identical; D3b alone may activate) · **§290.1/§290.4** (core-first; the
  mandatory review stop; *"the existence of a conceivable edge case is not by itself authority to
  enlarge the first tranche"*) · **§304.4** (D3a is the port wave, twin-life bounded) ·
  **§310.3(7)** (the rename micro-item and the source scan) · **§310.3(9)** (the quantum ladder is
  owner tuning surface).
- ⚠ **The one item still owed, and named so it is legible from the family's own law:** ODQ §310.1
  carries **S9 frontage/parcel-DCEL equivalence** to the D3a *era* as NOT BUILT. `draft-D3A-PLAN.md`
  §2.5 carries it forward again, to the tranche that ports `parcels.js`. **It is deferred and
  documented, not a bug to re-find.**

## §P10 · BINDING DESIGN-LAW CITATIONS

| Source | What it binds |
|---|---|
| `map-corpus/docs/GENERATION-SPEC.md` @ `aa613cb8…` (ledger branch) | the volume; ⟦FOLD §287⟧ **§10** wins on a direct conflict with §§6–§9 — §10.1 four artifact domains · §10.2 the temporal receipt seam · §10.4 the coordinate/hash ABI · §10.5 support surfaces and solids · §10.14 the executable manifest · §10.15 StreetGraph and DCEL · §10.16 the massing status override |
| ODQ **§287** | the causal/artifact repair, the geometry repair, the authorship repair, the domain door, the restored foundations, the live insertion, the build order, the massing split |
| ODQ **§288** | one `GLOBAL_CELESTIAL` source; shadows are measurement; **implementation is DORMANT** and fixed survey comes after D4 |
| ODQ **§289** | origin is total provenance; custom registries are canonical inputs; removal cannot erase a saved world; the map-tradition boundary |
| ODQ **§290** | core-first; only core failures block; long-tail completeness is recorded, not chased; **the mandatory review stop** |
| ODQ **§299.3** · **§303.5** · **§304.4** · **§306.4a** · **§310** | the three obligations · the adopt/replace split and D1's ABI · the port charter and twin-life bound · the dossier-surface batch · the W3 seal, the port dispatch, and the ten W3 rulings |
| `laneMFD1-receipt.md` · `laneMFW3F-receipt.md` · `retro-CX1-memo.md` | the measured substrate the port re-expresses; **where a receipt and this file disagree, RE-EXECUTE — neither is authority over live code** |
| `docs/implementation/PACKET_STANDARD.md` | statuses, dispatch lifecycle, scope budget, hot-file law, edge-case budget, STOP conditions, completion receipt, train landings, differential caps, this preamble's citation law |
| `DESIGN_BUILD_EFFICIENCY.md` §2/§3/§4 | train topology, the capsule, this preamble's own authority |
| `THE PROMISE` | a seed is a starting world forever; lived history immutable; **tuning owner-SIGNED** |

## §P11 · WHAT EVERY MF PACKET STILL CARRIES ON ITS OWN

Scope and boundary with the non-goals named **affirmatively, never by silence** · the behaviour and
identity contract · the exact change manifest with its budget priced against the `PACKET_STANDARD`
caps · **the port-source SHA-256 table** (§P1 R-MF-4) · the executed effective-line preflight with
eslint's own `Linter` · its own executed dormancy evidence · at most eight acceptance cases · its
wave-specific mutants with their executed red proofs · its own **predicted census motion and its
`censusAuthorization` ref** · its named interior red with predicted figures · and the header line
citing this file **by SHA-256**.
