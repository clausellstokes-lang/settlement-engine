# The Edit-Mode train — packet charter (2026-09-19)

**Authority:** `docs/DESIGN_EDIT_MODE_AND_DECREES.md` (§12 governs) · `docs/ARCH_EDIT_MODE_AND_DECREES.md` as amended by design §12 · ODQ §934.36–§934.40. **Standard:** `docs/implementation/PACKET_STANDARD.md` — one behaviour family per packet, the default hard scope budget, a verified base, a change manifest, required symbols resolved against live source, sealed dispatch, pause-and-resume at every gate (§934.33). **Base:** the `master` tip after the 2026-09-18 consist merges (packets are pre-proved there; until then the consist tip stands in). **Gate:** the whole editor is `TIER_GATE.premium` (Cartographer) via a new `editMode` field. **Order of programs:** this train runs after the push and before the simulator (§934.38).

Each packet below is written to READY by an Opus pre-proof lane that verifies every "verified fact" row against the tree (file:line), fills the change manifest and required symbols, and runs the sealed dispatch; the chair validates and cuts. Collision groups serialize. Every packet lands with its instruments green and the chair's compose.

## Wave 0 — the prerequisites the compile measured (§934.47)

| id | outcome (one behaviour family) | CREATE / MODIFY / TEST | required symbols (existing) | collision |
|---|---|---|---|---|
| **EM-P0** the pipeline seam | `runPipeline` gains a pinned mode: the deriving steps run from a GIVEN roster (`initialContext` pre-populated with the record's institutions, NPCs, factions and seats) and the root-writing steps are skipped or take the pinned output; `generateNPCs`'s root half and deriving half are separated at the seam; fresh generation (no pins) is byte-identical — the golden master and the prose manifest are the standing proof | M `src/generators/pipeline.js` (`runPipeline`, one option), the population step's root/derive split; T `tests/generators/pipelinePinnedMode.test.js` + the goldens plain | `runPipeline`, `rng.fork`, `generateNPCs` | ⚠ the pipeline's spine; alone in its train |
| **EM-P1** stable identity — DECIDED YES (§934.47 A, the owner by deferral) | every NPC, institution and faction carries a permanent id minted from the step's own stream at generation (`npc:<n>`, `inst:<n>`, `fac:<n>` by the seed's order, never by name or position) and, for existing saves, minted on first read by the in-app migration (unconditional); readers that join by name or index take the id (the three convicted sites first) | M the three generators' minting, `src/lib/saves.js`'s in-app migration (v3), the convicted readers; T identity round-trip + the goldens (ids are new keys; the golden fixtures re-record ONCE under a signed record naming this cause) | `migrateSaveToV2`, `generateNPCs`, `generateFactions`, the institution roster writer | ⚠ persisted shape (decided); alone in its train |
| **EM-P2** generation's choosers registered | every `rng.fork(name)` step and every `createPRNG`/`pickRandom` site in `src/generators` that writes a root joins the decision-fork registry with its output key; the entropy census's whole-src figure is re-measured | M the registry file (rows only), T the registry walker's new arm | `HABIT_FORK_REGISTRY` (the registry's real symbol), the entropy census | none |

| **EM-B1c** the world-and-time ops | the three `rename-*` ops (§12.3; `rename-settlement` declared as data for EM-C4's adapter — no pure cascade exists), `set-world-fact` (§14) and `schedule-event` (§18; the catalogue minus `NON_AUTHORABLE_EVENTS`, read by reference; no `when` clamp) — split out of B1a at compile; the sibling-leaf shape (the per-file cap is the law) | C `src/domain/edit/operationsHomeDelegating.js`; M `src/domain/edit/operations.js` (+2, the spread); T the existing suite extended (+6 arms) | B1a's machinery | after B1a (T6) |
| **EM-P3** the world-fact option sets' one home (as measured: two moves, four citations; trade access → EM-P3b) | terrain, culture, trade access, resources, goods, services and stressors get ONE canonical, domain-reachable home (the values `resolveConfig` accepts are the truth — measure where it validates them); the gallery's `TERRAIN_OPTIONS`/`CULTURE_OPTIONS` and the wizard's lists import it; culture's two spellings and terrain's three become one; a walker pins the single source | C `src/data/worldFactOptions.js` (or the measured home); M `src/components/gallery/galleryUtils.js`, the wizard's option lists, `resolveConfig`'s validation reads; T a single-source walker | `resolveConfig`, `CULTURE_OPTIONS`, `TERRAIN_OPTIONS`, `CULTURE_PROFILE_KEYS` | none |

**EM-P3b** (after wave 1): trade access gets its one list (`TRADE_ACCESS`, minted with its decision-fork row and its mechanism-coverage row) and the world-facts card declares it. EM-P2's registry row is `{forkId, module, symbol, outputKey}` (EM-A1 joins on `(cardShape, outputKey)`; symbols resolve against source). EM-A1 declares ONLY fields that exist on the record or that a wave-1 packet creates (§934.47 addendum): the lane enumerates them from the record shape and the generators' choosers; the pencil half of its walker waits for wave 4's nameable cards. EM-A3's census is a LIVE scan of the derivation roots, never a read of the baselines.

**EM-P1b** (after EM-P1): the three convicted readers (locksPreservation.js, characterDrift.js, characterEdit.js) take the stable id. **EM-B4b** (wave 4, with the surfaces): the inline-edit surface's derived and history paths retire — `EDITABLE_FIELDS`, `QUEUE_WIRED_PROSE_PATHS` in lockstep, `historyPreservation.js`'s reroll filter re-pointed, `WorkbenchProseEditor`'s lever leaves the screen in the same wave.

## The train plan (§934.47 addendum 4)

| train | members (collision-disjoint; each pre-proved at the train's base) | interior reds named |
|---|---|---|
| **EM-T1** | EM-B3b | none |
| **EM-T2** | EM-P0 (alone: the pipeline's spine) | none (goldens byte-identical by construction) |
| **EM-T3** | EM-P2 · EM-P3 · EM-B3a · EM-B1d (the vocabulary widening: `jailed` and `ruined` into the two typedefs + seven consumers + a union-totality walker; an approved override of nine files at ≤3 lines each) | the lighting census's test-file tuple (each member's predicted delta); EM-P2's and EM-B1d's mutation-coverage rows |
| **EM-T4** | EM-P1 (alone: persisted shape; the golden door executed once by the chair over the measured movers) | the measured golden suites; the identity round-trip |
| **EM-T5** | EM-A1 · EM-A2b · EM-B1e (the shared ruin writer) · EM-P1b | the lighting tuple; EM-A1's mutation-coverage row |
| **EM-T6** | EM-A2a · EM-B1a (after B1d and B1e) · EM-B2a (the `dmLayer on settlement` exemptions mint at its terminal) | per member |
| then | EM-B1c, EM-B1b (after B1a), EM-A3 (after A1) → EM-B2b, EM-B4 (by dependency) | per member |

A draft waits in the chair kit until its train is next and its manifest paths are free (the just-in-time rule; the validator refuses a path held by two non-terminal packets).

EM-B2 splits: **EM-B2a** (`pinsFrom(record)`, `rederive(record, config, layer)` over the four card kinds through EM-P0's mode, the isolation property) and **EM-B2b** (`config′` world-fact changes + the delta card's DM-fields section). **EM-B4** is an IN-APP save migration (the estate's `migrateSaveToV*` pattern, unconditional on read and write) folding `_userEdits` into `dmLayer.roots` by the §14 classification, derived and history edits into `dmLayer.notes` (owner decision B), the Change Dock retired without migration.

## Wave 1 — declarations, pools, ops, the layer (headless)

| id | outcome (one behaviour family) | CREATE / MODIFY / TEST | required symbols (existing) | collision |
|---|---|---|---|---|
| **EM-A1** declarations | `fieldDeclarations.js` declares the WORLD FACTS card (terrain, culture, trade access, resources, goods, services, stressors; pools = the wizard's option sets; `provenance: 'world-fact'`) and every editable ROOT field of the first card types (institution, npc, faction, power seat) with `kind: pool \| free \| free-cascade`, `group`, `provenance: 'root'` and the field's `writer` by symbol; `declarationsFor(cardType)`; the declaration walker refuses a pencil without a declaration AND refuses a declaration whose writer reads any other world fact (design §14: edit at the source, never at the derivation) | C `src/domain/edit/fieldDeclarations.js`, `src/domain/edit/types.js`; T `tests/domain/editDeclarations.test.js`, `tests/lint/editDeclarations.walker.test.js` | the card components' names (pre-proof names them) | none |
| **EM-A2** pools | `pools.js`: pool ids → sources; institutions from `getInstitutionsForTier` (the generator's tier-gated catalogue, never the display seam); names from the culture's generator with seeded `rollFrom(seed, entryId, n)`; roles from the world's institutions; archetypes; stances; causes; commodities from the resource catalogue; deities; tiers | C `src/domain/edit/pools.js`; T `tests/domain/editPools.test.js` | `src/generators/lookups.js` `getInstitutionsForTier`, `getInstitutionalCatalog`; the name generator's export; the archetype table | none |
| **EM-A3** the flavor census | `flavorFields.census.test.js` proves every `free` field has zero derivation readers by BOTH property reads (writer-reach + observed-shape data) AND value joins (the rename cascade's join list in `src/domain/factionRename.js`); a join-key field may only be `free-cascade` | T `tests/lint/flavorFields.census.test.js`; M `src/domain/edit/fieldDeclarations.js` (readersProof rows) | `scripts/.writer-reach-baseline.json`, `scripts/.observed-shape-readers-baseline.json` (paths pre-proved), `factionRename.js`'s join table | A1 |
| **EM-B1a / EM-B1b** ops (split at compile, §934.36 addendum) | `operations.js`: B1a = the machinery (`makeOp`, `validateOp`, the `stage`/`consequence` partitions, `requires` in its two kinds — world-state predicates that gate a seal, registry conditions the guards judge; design §18) + the eighteen HOME ops incl. `set-npc-status` and `set-institution-state` (§15), the three `rename-*` (§12.3, calling the existing cascade) and `set-world-fact` (§14); B1b = the seven OFF-STAGE ops under §13 + the coverage walker + its mutation-coverage row; per-type guard coverage as FUNCTIONS | C `src/domain/edit/operations.js`; T `tests/domain/editOperations.test.js`, `tests/lint/opGuardCoverage.walker.test.js` | `factionRename.js` exports; `renormalizeFactionPower` (`src/generators/power/rulingStructure.js:35`); `checkInstCompat` (`structuralValidator.js:269`) | none |
| **EM-B2** pinned re-derivation (the one engine) | `dmLayer.js` + the pipeline's pinned mode: `pinsFrom(record)` (registry key → record path for every registered chooser) and `rederive(record, config′, layer)` — the derivation steps run with every chosen fact pinned from the record, a chooser the record lacks drawing on the seed's stream, the layer's root overrides and `config′`'s world-fact changes applied; the lane MEASURES whether the pipeline's derivation steps run from the record's rosters without re-drawing and STOPs with the contradiction if not (a pipeline-seam packet then precedes it); DM-minted ids in `dm:`; the golden isolation property (empty layer = the golden; a one-root layer moves only that root and its derivations). THE LOAD-BEARING PACKET — splits into B2a (the hook at the choosers) and B2b (the orchestration + the delta section) if its budget demands (design §14) | C `src/domain/edit/dmLayer.js`; M the regeneration path that re-applies (pre-proof names it), `src/domain/regenerationDelta.js` (a field-level "the DM's fields" section); T `tests/domain/dmLayer.test.js`, `tests/property/dmLayerGoldenIsolation.test.js` | `deriveRegenerationDelta`; the settlement writer the store uses | B3 |
| **EM-B3** the registry key + travel (as measured at compile) | `decrees` joins the client denylist and its SQL mirror (migration 202, a file; the deploy is the owner's); `dmLayer` is already denied by both; `publicSafe` and `worldSnapshotPublic` name both keys; the backup EXPORT omits them; a RUNTIME travel test proves fork, import, gallery and export carry neither (the fork is a fresh generation; the gallery source is allowlisted); the `importScrub` strip is deferred defense-in-depth | M `src/domain/display/publicSafe.js`, `src/domain/display/worldSnapshotPublic.js`, `src/lib/accountData.js`; C `supabase/migrations/202_*.sql`; T the persistence round-trip + the runtime travel test | `partializeStoreState` (PRESERVED, not modified), `PRIVATE_KEY_RE`, `COVERT_KEY_RE`, `PUBLIC_TOPLEVEL_KEYS` (unchanged) | B2 → B3 → B4 in sequence; no path collision |
| **EM-B4** the migration | `scripts/migrate-edit-registry.mjs` folds inline edits and the Surveyor's staged state into the layer/registry, idempotent, `--check`; the Change Dock (flag-off) is retired, not merged | C the script; T `tests/scripts/migrateEditRegistry.test.js` | the Surveyor's review artifact shape | B3 |

## Wave 2 — the registry and the guards (headless)

| id | outcome | CREATE / MODIFY / TEST | required symbols | collision |
|---|---|---|---|---|
| **EM-C1** the registry | `registry.js`: stage / reorder / withdraw / reopen (pending only; applied is read-only) / markApplied / `revertTick` (re-appends later-staged entries after a snapshot restore); `orderIndex`, optional `when` | C `src/domain/edit/registry.js`; T `tests/domain/decreeRegistry.test.js` | none new | none |
| **EM-C2** the guard engine | `guards.js`: `evaluateGuards` FOLDS entries (each judged against the world with earlier entries applied); kinds prerequisite / totality / contradiction / contention / connection; every guard offers `proceed`; overrides recorded | C `src/domain/edit/guards.js`; T `tests/domain/editGuards.test.js`, `tests/property/guardsAcceptApplies.test.js` (queues of 1..10) | none new | C3 |
| **EM-C3** the rules | `guardRules.js`: prerequisite via `checkStructuralValidity` → {violations, suggestions} + `GATE_FEATURES`' requires table (NOT `checkInstCompat`, a prose picker that consumes a draw — measured at compile); totality via `renormalizeFactionPower` as the `fulfil` writer; contradiction from `conflictsWith`; contention on one fact; connection from `requires/relatedTo` with reorder/fulfil/proceed and the follows-from link; NO count-range rule | C `src/domain/edit/guardRules.js`; T `tests/domain/editGuardRules.test.js` | `checkInstCompat`, `checkStructuralValidity` (`:348`), `renormalizeFactionPower` | C2 |
| **EM-C4** the store slice | `editSlice.js`: `editMode` (transient), actions over the pure modules through ONE generic decree adapter in the command registry, `selectCanonState(saveId)` from `campaignState.phase === 'canon'`, `selectGuards` memoized | C `src/store/editSlice.js`, M `src/application/commands/standardCommandRegistry.js` (one adapter), `src/store/index.js`; T `tests/store/editSlice.test.js`, `tests/lint/editMutationPath.walker.test.js` | `campaignState.phase` (`settlementRenameHelpers.js:249`), the command registry's adapter shape (`:19-26`) | ⚠ the store index |

## Wave 3 — the tick (headless)

| id | outcome | CREATE / MODIFY / TEST | required symbols | collision |
|---|---|---|---|---|
| **EM-E1** the tick hook | `decreeHook.js`: `applyDecreesAtTick` at the head of `simulateCampaignWorldPulse` for `when`-due entries in order, as causes; consumes NO PRNG and preserves the pulse record's key order when there are no decrees (the preset witness hashes the serialized record); `retractDecreesOfTick` wired into `undoLastPulse` with the later-staged re-append | C `src/domain/worldPulse/decreeHook.js`; M `pulseKernel.js` (one call), `campaignAdvanceSession.js` (the undo path), the interval orchestrator for `when` (`advanceInterval.js`); T `tests/simulation/decreeTick.test.js`; the preset witness re-recorded BY HAND with a stated cause if its hash moves (it must not at zero decrees) | `simulateCampaignWorldPulse`, `undoLastPulse`, `pulseUndoStack` (`campaignWorldPulseSlice.js:16`) | ⚠ the kernel |
| **EM-E2** the chronicle's voice | `decreeProse.js` + authored pools under the prose program's law for the table's hand, the off-stage forms, the override line, the follows-from join; `decreeChronicleLine`; the cause walker | C `src/domain/display/stateProse/decreeProse.js` + its pools; T `tests/domain/decreeProse.test.js`, `tests/lint/decreeCause.walker.test.js` | `chronicleTimeline` | the wiring census (forced order) |
| **EM-E3** the advance report | the regeneration card's sections for a tick that applied decrees, plus the field-level DM section from B2 | M `src/components/map/AdvanceReport.jsx`, `RegenerationDeltaCard.jsx`; T `tests/components/advanceReportDecrees.test.jsx` | `RegenerationDeltaCard` | D2 |

| **EM-E0** the simulation's registration (design §19 ruling 1) | the 44 registration-owed draws the survey found (`docs/implementation/surveys/SIM-SEALS-SURVEY-2026-09-19.md`, Table 4 "R") gain `HABIT_FORK_REGISTRY` rows, and every row a seal will name declares its outcome vocabulary (`actionVocabulary`, the survey's quoted column — `SIEGE_FALL_ODDS_WORDS`, `TRADITION_OUTCOME`, `VERDICTS`, `ADMISSION_OUTCOMES`, `ENVOY_ERRAND_STATES`, the `CLOSE_ROADS`, …); the walker's totality re-derived; NO behaviour change (the goldens and the preset witness unmoved); split by card if over budget (E0a war · trade · neighbours; E0b people · faith · the settlement · chronicle) | M `src/domain/worldPulse/habitForkRegistry.js` (rows only); T `tests/lint/chooserTotality.walker.test.js` figures re-measured by execution | `HABIT_FORK_REGISTRY`, `SCAN_ROOTS` | none; before E4 |
| **EM-E4** the director's pins (+ the "make them" directives on the counterparty's forks, design §18; the first seal set, design §19 ruling 3) | `pin-fork` (design §16): a directive decree names a REGISTERED fork WITH A DECLARED VOCABULARY, an outcome from it and a tick; the pulse consults pins at each registered fork (the EM-P0 shape); the first cut = the event lottery (`candidateRoll` keyed `stableCandidateKey`), the chance meetings (`MEETING_OUTCOMES`), the festival + the season, the siege verdict WITH ITS BAND, the coup hold + the winner, the war's end (`CLOSE_ROADS`; the automatic win), the court's verdict (`VERDICTS`), the exile's landing (`ADMISSION_OUTCOMES`), the exposure, the envoy's road (`ENVOY_ERRAND_STATES`); lands the peace-DRAFT record and the pending-offer read (§19 ruling 7); every pin routes through `authorityFor` / `ACTOR_INITIATED_MAJOR_TYPES` (no second write path); applied pins reopen read-only | C `src/domain/edit/directives.js`; M the pulse's fork consult (one hook, the registered forks), the decree hook, `peaceTermsDrafting.js` (the draft record); T `tests/simulation/pinFork.test.js` + the preset witness unmoved at zero pins | `HABIT_FORK_REGISTRY` (+ the outcome key from EM-P2), `simulateCampaignWorldPulse`, `authorityFor` (`changeAuthorityPolicy.js`), `ACTOR_INITIATED_MAJOR_TYPES` (`actorMajorApproval.js`), `stableCandidateKey` | ⚠ the kernel; after E0, E1 |
| **EM-E5** direction and information (design §19 rulings 2 and 6) | DIRECTION = the `REALM_MANIFEST`'s 14 proposal-lane verbs SURFACED on the cards with their own predicates as the §18 conditions and `REALM_VETO_PROSE` as the herald's reason (`SUE_FOR_PEACE`, `DECLARE_CASUS`, `ORDER_INTERVENTION`, `ORDER_SUPPLY_RAID`, `DECLARE_TRADE_EMBARGO`, `DECLARE_BLOCKADE`, `ORDER_CONVOY`, `FORCE_RECONSIDERATION`, `TRANSFER_SOVEREIGNTY`, `REPUDIATE_TREATY`, `FORCE_CALAMITY`, `FORCE_FOUND_STEADING`, `FORCE_ABANDON`, `FORCE_RESETTLE`; `REINFORCE`/`INTERCEPT` grayed with the manifest's own reason) + the directions the survey ruled in: `ENGAGEMENT_MOVES` (direct the force), the muster (`evaluateMobilization`), the levy (`TAX_FORMS`/`TAX_RATE_BANDS`, suspended by siege/occupation), the occupier's hand (`OCCUPATION_POSTURES`), the tempo (`foldNarrativeTempo`), the brokerage menu (`BROKERAGE_SERVICE_MENU_KEYS`), the regional channels (`setRegionalChannelStatus`); INFORMATION = `set-belief-confidence`, `mutate-belief`, `correct-belief` through the belief writer + sight/secrecy/credibility/lies over `processSight`, `processSecrecy`, `advanceCredibility`, `processLies` — informationStatecraft.js is HOT (1,502 raw lines): open with an executed headroom measurement; split if it must | M the layers' action entry points (by symbol, measured); T per op | `REALM_MANIFEST`, `coversVetoCodes`, `REALM_VETO_PROSE`, the belief writer, the supply action, the route action | after E4 |
| **EM-E6** events, predetermined or by the party (design §19 ruling 2) | a decree with `when` from THE catalogue that exists — `affordanceManifest.js` (41 typed settlement events, per-type `predicate`/`targetsFrom`) minus `NON_AUTHORABLE_EVENTS`; `cause: 'party'` IS `PARTY_IMPACT_KINDS` (12 kinds with `targets` and `defaultMagnitude`) applied through `applyPartyImpact`, carried to the chronicle ("by the party's hand" in the herald's pools) — bind, never mint a second catalogue or a second party vocabulary | M `decreeProse.js` pools, the catalogue reader; T the cause walker extended | `affordanceManifest`, `NON_AUTHORABLE_EVENTS`, `PARTY_IMPACT_KINDS`, `applyPartyImpact` | after E2 |

| **EM-E7** missions (design §19 ruling 8) | `send-on-mission` (design §17) = a DIRECTION over `MISSION_KINDS` (7 kinds; the casting `dispatchMissionCandidates` is STAY-deterministic, HBF-35) and the covert vocabulary (`COVERT_MISSION_VOCABULARY`: demand · face · product · tap); the catch is a pin (`stayDetectionRoll`, HBF-05); `DM_VERBS` `assign`/`kill`/`pardon` are already built and `pardon` is §15's return from exile; NO new `NpcStatus` value — the errand record carries the position and the person's card reads it; the return resolved at the tick by the estate's own procedures | M the casting/espionage entry points by symbol; T per op + the espionage dormancy fence unmoved | `MISSION_KINDS`, `COVERT_MISSION_VOCABULARY`, `DM_VERBS`, `stayDetectionRoll` | after E4 |

## Wave 4 — the surfaces

| id | outcome | CREATE / MODIFY / TEST | required symbols | collision |
|---|---|---|---|---|
| **EM-D1** edit mode shell | `EditModeShell`: the mode indicator in the forge's brown-and-gold tokens, pencils on declared cards, SEALS on process cards (design §17: the war, trade, rumour and chronicle cards; "Send on a mission" on a person's card), pluses on sections, provenance notes on derived cards, Done; entered from the library on a saved settlement behind `TIER_GATE.premium.editMode` (new field + tier-facts parity pin) | C `src/components/edit/EditModeShell.jsx`; M `src/store/authSlice.js` (`editMode` gate), `src/config/tierFacts.js`, the library card; T `tests/components/editModeShell.test.jsx`, `tests/config/tierFacts.contract.test.js` | the entity web context, the pipeline rail's tokens | ⚠ authSlice |
| **EM-D2** the card editor dialog | `CardEditorDialog` generated from declarations: PoolField (drop-down / search / roll another, seeded), FreeField (plain text, maxLength, font-coverage check — new capability: a cmap read of the embedded faces), the effect tag from `selectCanonState`, Save/Add/Remove with a cause pool, the §934.31 door, a phone sheet | C `src/components/edit/CardEditorDialog.jsx`, `PoolField.jsx`, `FreeField.jsx`, `src/pdf/lib/fontCoverage.js`; T `tests/components/cardEditorDialog.test.jsx`, `tests/pdf/fontCoverage.test.js` | `DialogClose`, `useDialogDismiss` (`useDialogFocusTrap.js:119`), the font registry (`src/pdf/theme.js:90-116`) | none |
| **EM-D3** the registry page | `DecreeRegistryPage` at the dossier's foot: order, move, withdraw, reopen (pending only), guard badges with offers, "Place it in the Realm to advance" when outside a campaign, the rewind's session limit stated, applied entries with chronicle links | C `src/components/edit/DecreeRegistryPage.jsx`; T `tests/components/decreeRegistryPage.test.jsx`; the reachability / phone-floor / no-clamp walkers extended | the realm's Advance control; `RealmLockedGate` | none |
| **EM-D4** the Surveyor bridge | proposals land as `addedBy: 'surveyor'` decrees with `surveyorCredit`; consent as a guard kind | M `InterpretApplyPanel.jsx` and its command adapters; T the existing Surveyor suites re-pointed | the review artifact | none |

## Wave 5 — phantoms

| id | outcome | CREATE / MODIFY / TEST | required symbols | collision |
|---|---|---|---|---|
| **EM-F1** phantoms as hidden saves | `mintPhantom` writes a minimal save record (`kind: 'phantom'`, seed, pooled traits) hidden from the shelf; the neighbour back-link resolves it like any save; off-stage ops (declare-war / open-trade / send-force / resolve-outcome) under THE PHANTOM CONSEQUENCE RULE (design §13, §934.43): the home procedures + the record only, no world state; `consequenceFor(target)`; the registry's PHANTOM/REAL badge; the chronicle marks off-stage | C `src/domain/edit/phantoms.js`; M the library shelf filter, `neighbourBackLink.js` (no change if a save resolves — pre-proof measures); T `tests/domain/phantoms.test.js`, `tests/store/phantomSaves.test.js` | `findSaveByName` (`neighbourBackLink.js:99`), `buildNeighbourBackLink` | none |
| **EM-F2** promotion | forging a phantom's seed fully replaces the minimal record in place; written history carries | M the generation action (one path); T `tests/store/phantomPromotion.test.js` (parity with a settlement forged from that seed) | the generation action | F1 |

## Amendments of 2026-09-19 14:5x EDT (design §20; ODQ §934.52 — the owner's delegation; each vetoable)

| id | what changes | CREATE / MODIFY / TEST | required symbols | collision |
|---|---|---|---|---|
| **EM-D0** the first door (NEW; design §20.4) | ONE card (the NPC's) on a DRAFT settlement: `CardEditorDialog` generated from EM-A1's declarations for that card — PoolField (drop-down / search / seeded roll another), FreeField (plain text, maxLength), the rename through the cascade — applying PLAIN EDITS through EM-C4a's one adapter and EM-B2a's `rederive`; the forge's brown-and-gold tokens and THE HALO (design §3, §20.1: a gold rim + a parchment bloom from ONE shared glow token lifted from `ARROW_GLOW`; still after kindling; the sheet's top edge on the phone; the pop-up only); the §934.31 door; behind `TIER_GATE.premium.editMode` (the new field + its tier-facts parity pin move here from EM-D1) AND DARK outside the preview persona and staff until the owner opens it | C `src/components/edit/CardEditorDialog.jsx`, `PoolField.jsx`, `FreeField.jsx`, the shared glow token's home; M `src/components/nav/ArrowControl.jsx` (reads the shared token; pixels unmoved — pinned), the tier gate; T `tests/components/cardEditorDialog.test.jsx` (desktop + phone), the halo's pin (token-derived colour; no other surface carries it), the dialog-door / phone-floor walkers extended | `ARROW_GLOW`, `PortablePopup`, `BottomSheet`, `useDialogFocusTrap`, `TIER_GATE` | EM-D1, EM-D2 |
| **EM-C4a** the store's plain-edit half (SPLIT out of EM-C4) | `editSlice.js`: `editMode` (transient), the ONE generic adapter in the command registry, plain-edit application on a draft (the layer written on write; scoped re-derivation through `rederive`); `selectCanonState` | C `src/store/editSlice.js`; M `src/application/commands/standardCommandRegistry.js` (one adapter), `src/store/index.js`; T `tests/store/editSlice.test.js`, `tests/lint/editMutationPath.walker.test.js` | as EM-C4's | EM-C4b |

- **EM-C4b** keeps the registry's actions and `selectGuards` (after EM-C1–C3), extending EM-C4a's slice; it no longer creates the file.
- **EM-D2** EXTENDS EM-D0's dialog to every card type, adds the font-coverage check, the effect tag, Add/Remove with the cause pool; it no longer creates `CardEditorDialog`. **EM-D1** loses the gate field to EM-D0.
- **EM-C1** gains the pure resolver `resolveDecree(entry, catalogues)` and the OPTIONAL persisted field `withdrawnReason` (design §20.3); **EM-E1** calls the resolver on load-time pending entries and again before it applies anything at the tick; a walker proves every op type names the catalogues its payload points into.
- **EM-B1c** (and the op machinery's timing note in EM-B1a): on a canonized settlement `free` and `free-cascade` fields apply on Save, every `pool` field stays an event — read from the declaration's `kind`, no new op property (design §20.2). PRE-PROOF MEASUREMENT OWED before EM-B1c is READY: every pulse stream, keyed race or hash keyed on a DISPLAY NAME, by symbol and file:line; a hit makes the rename wait on that reader taking the stable id (EM-P1b's class) or names the reader in the packet's own manifest.
- **The train plan gains EM-T7** (after EM-T6): EM-C4a · EM-D0 — before EM-B1c/B1b/A3/B2b/B4, whose order is unchanged. Interior reds named at compile: the lighting tuple (new test files), the first-paint closure (the dialog is LAZY — it must not enter the eager graph; measure with `npm run build` + `verify:dist` at the landing, as every landing now is).

## Amendments of 2026-09-19 15:0x EDT — EM-P2 STALE and re-cut; EM-P0b chartered (design §21; ODQ §934.47 addendum 14)

- **EM-P2 (version 3, to be compiled AFTER the recon's executed prototype):** the generation register in two tiers — Tier 1 `(step, key)` for every `provides`/`mutates` key, both-directions equal to `getStepMeta()`, each row classified BY EXECUTION over the golden corpus (the step draws / the key is `drawn` or `pure` under a perturbed step stream / the key reaches the assembled record; fork labels as observed data; stream mints cited from the entropy census, never re-scanned); Tier 2 the field-level root rows EM-A1 joins on `(cardShape, outputKey)`, each resolving on a generated record, under a `drawn` key, entropy-sensitive, its symbol resolved against source by a `registerStep`-aware resolver. NO static idiom scan, NO `forkKey` in a row, NO copy of the model's resolver where it is wrong. C `src/domain/generation/generationForkRegistry.js`; T the static arms under `tests/lint/` (+ the mutation-coverage row), the census by execution under `tests/generators/` with its instrumentation in `tests/helpers/`; the mismatch arm PRINTS the fresh literal and fails — it never writes.
- **EM-P0b (NEW, after EM-P2 v3; before EM-B2a):** the runner's partial-pin refusal is restated over a step's declared `choosers` (its DRAWN keys) instead of every `provides` key; each drawing step declares `choosers` in its registration; the census holds the declaration equal to the measurement; the rule for a drawing step that only MUTATES, and for a drawn key that never reaches the record, is settled from the recon's measurement. An approved-override candidate (one line in each drawing step's registration) — priced at compile.
- **Train EM-T3 proceeds without EM-P2:** EM-P3 · EM-B3a · EM-B1d, one at a time in the slot, each made READY by an Opus pre-proof lane at the tip; EM-P2 v3 joins the first train whose base it is pre-proved at. EM-A1 still depends on it.
- **Ceilings are PRICED AT PRE-PROOF for every packet that touches the generation worker's closure** (the process law of ODQ §934.19 addendum 2): the packet's manifest carries the two bundle-ceiling tests as TEST rows with a stated byte bound, and the build lane re-mints them under the chair's standing conditional ruling — a real `npm run build`, the kit's per-module attribution showing ONLY the packet's own modules moved, growth inside the priced bound — or STOPS. EM-P3 (`resolveConfig.js`) is the first.

## Amendments of 2026-09-19 15:3x EDT — the census recon's findings; the pre-proofs' rulings (design §21.5; ODQ §934.47 addenda 15-16)

- **EM-P2 version 3 — the shape is now MEASURED:** Tier 1 = the recon's 75 `(step, key)` rows (the literal is in the chair kit), each with `draws` (the step draws: rows/rows-run), `class: 'drawn' | 'pure' | 'label'` with the row count behind it, `onRecord: 'absent' | 'same' | 'transformed'`, and `via: 'provides' | 'mutates'`; both-directions equal to `getStepMeta()`; the census corpus = the 63-row sample + the off-corpus random-mode block; stream mints and the HASH channel (`pickVariant` + the `fnv1a32` sites) censused by execution through the loader hook. Tier 2 = EM-A1's ten fields re-cut against the live record (the power card is `powerStructure.governingName` + `factions[].isGoverning`, never `seats[].holder`), each row `{ forkId, step, key, module, symbol, outputKey, cardShape, origin: 'drawn' | 'computed' }` — editability is NOT conditioned on entropy (design §14 as amended by §21.5.3).
- **EM-P1b gains three name-keyed choice sites** (design §21.5.7): `assembleInstitutions.js` (`pickVariant` on the seed + `inst.name`), `npcData.js` (on the two NPCs' names), `townMap/glyphAssign.js` (`createPRNG('glyph:<name>')`) — re-keyed on the stable id. **EM-B1c's rename ops are not promoted before it lands** (or the packet names those sites as its own change).
- **EM-B2a is BLOCKED; EM-P0b is ABSORBED into ARCH-REDERIVE** (design §21.5.5 and the item that closes §21.5): pinning from a record reproduces it 2/9 and lets `corruptionPass` write into the caller's record. The chair architects the re-derivation seam (which value a pin holds; pins final and isolated; multi-producer keys; mutating drawers; intra-step dependence) after a focused Opus recon on `assembleSettlement`'s enrichment; the seam's packets precede EM-B2a. EM-D0 and train EM-T7 wait on it with EM-B2a.
- **EM-B3a — THE VEIL PRECEDES THE WRITER:** its `Depends on` is EM-B3b alone; EM-B2a and EM-C1 depend on IT; the two keys' inner shapes are opaque to it. **EM-B3c (NEW, small):** a security walker with a SQL-side subset arm — the net-current gallery scanner must carry every client token's alternative — because a NEW migration re-creating the scanner is invisible to a path-based substrate check.
- **EM-P3 — THE CURE IS THE PLACEMENT:** the citation map leaves `resolveConfig`'s import closure for the domain-reachable leaf (as compiled it put +634 B into the zero-slack worker for bytes the worker never reads); the worker rise that remains rides the standing conditional ruling and is recorded per landing.
- **EM-B1d:** the union-totality walker matches by TOKEN MEMBERSHIP with the foreign unions DECLARED BY NAME, shrink-only, each with its reason and a guard that the exempted file still holds the union it names (`STASIS_REASONS` — captivity is a per-layer fact, design §19 ruling 7; `ENVOY_LOSS_CAUSES`; the verdict union) — a token matcher can only over-report, and a false positive costs a declared row while a miss costs the class; a writer-reach movement is the CHAIR's to bank at the landing (the lane records the walker's verdict and never writes a baseline; growth = STOP); the legacy spellings `'imprisoned'`/`'killed'` becoming AVAILABLE is ratified as inert (no writer ever assigned either to `.status`). At its placement the STALE EM-P2 version 2 is WITHDRAWN to the chair kit (a STALE packet still reserves its change paths; the just-in-time rule).
- **A TENTH REGISTRATION COST, for the whole family (the EM preamble §P2 is amended on the build branch):** a packet that edits any INPUT of an edge-shared bundle closure owes `npm run build:edge-shared` and the regenerated artifacts in its manifest — the test is INPUT MEMBERSHIP against the metas' `inputs` lists, never entry-hood (a JSDoc-only edit to `src/domain/entities/npcs.js` stales two bundles). Every pre-proof measures it.

## Amendments of 2026-09-19 16:0x EDT — EM-B1f and EM-B3c chartered; EM-B1d's matcher (ODQ §934.47 addendum 17)

| id | outcome (one behaviour family) | CREATE / MODIFY / TEST | required symbols (existing) | collision |
|---|---|---|---|---|
| **EM-B1f** status-based absence at THE ONE PARTICIPATION CHOKEPOINT (NEW; design §15 — "a jailed or exiled holder cannot keep a seat") | `isOffStage` (`src/domain/roads/state.js`, the estate's one participation chokepoint — today `isInStasis` OR a roads hostage; it never reads `.status`) gains ONE arm: an NPC whose `status` is `jailed` or `exiled` is off-stage for every participation read, so the three consumers that pair `=== 'dead'` with it (`warSeatBooks.js` — a SEAT read — `npcLadderState.js`, `npcLadderKernel.js`) need no edit. MEASUREMENT OWED at pre-proof: does any generated or pulse-written NPC carry `exiled` today (it is already in `NpcStatus`), and does the arm move a pulse golden, the espionage fence or a preset witness; whether `missing`/`retired`/`removed` join the arm is ruled from that measurement, never assumed | M `src/domain/roads/state.js`; T the chokepoint's own suite + a seat-read arm (a jailed holder is not returned by `rosterNpcById`) | `isOffStage`, `isInStasis`, `rosterNpcById` | after EM-B1d (it needs the union); ⛔ BLOCKS EM-B1a's promotion — B1a's `set-npc-status` is what makes `jailed` producible, and a producible `jailed` without this arm is a jailed mayor who still governs |
| **EM-B3c** the SQL-side subset arm (NEW; from EM-B3a's pre-proof) | a security walker: the NET-CURRENT gallery scanner (`_gallery_world_snapshot_is_safe`, latest-wins across migrations) must carry a covering alternative for every client denylist token — the direction `tests/security/snapshotDenylistDrift.test.js` does not assert — so a NEW migration re-creating the scanner without the editor's alternatives reds at once (a new migration file is invisible to a path-based substrate check) | T `tests/security/` (one walker; its mutation-coverage row if the directory is an enforcer dir — measured at pre-proof) | the drift test's own extractors | none; any time after EM-B3a lands |

- **EM-B1d's union-totality walker matches by token membership over the DISCRIMINATING subset of the union** — `{dead, exiled, retired}`, the members measured to belong to no other declared vocabulary (`'active'` 74 files / 11 foreign vocabularies, `'removed'` 30 / 8, `'missing'` 5 / 3, `'jailed'` 3 / 3 are HOMONYMS and are excluded from the TRIGGER, never from the totality demand) — because a consumer that tests only `'active'` is total by construction and the class the walker exists for is the ENUMERATION OF NON-ACTIVE MEMBERS. It flags eight files (the five known consumers and three the compile missed), needs zero exemptions, and states its CANNOT-CATCH (a consumer that enumerates only homonym members; none exists at the tip).

## Amendments of 2026-09-19 16:2x EDT — ARCH-REDERIVE version 1; EM-D0 unblocked; the re-entry family chartered (design §22; ODQ §934.47 addendum 18)

- **EM-D0 NO LONGER WAITS ON RE-DERIVATION.** The first door is the NPC's card, and the recon measured an NPC's role change as CLEAN with no reading moving: D0's plain edits are RECORD WRITES through the op boundary — `set-field` on the NPC's held fields, the rename through the existing cascade, the annotation — with no pipeline re-entry at all. Its dependencies are EM-A1, EM-A2a, EM-B1a (after EM-B1d, EM-B1e, EM-B1f) and EM-C4a; EM-B2a leaves the list. Train EM-T7 stands.
- **EM-B2a is RE-CUT, not merely blocked:** `rederive` is "held facts are final" (design §22), and it is preceded by **the re-entry family EM-R**, compiled ONLY after the owed prototype reports: **EM-R1** pins cloned on entry + the writer census as a walker (every writer of a held key, by symbol, consults the pin — producers, mutating passes, the coherence enrichment, the power replay, the name mint); **EM-R2** the roster is final inside `enrichNpcCoherence` (the one seam measured to reproduce 63/63) and `corruptionPass` leaves a held roster alone; **EM-R3** the power structure is final in `reconcilePowerStructure`'s replay, and the two invariants are pin-aware (refresh, never throw, under held mode; byte-identical with no pins); **EM-R4** the settlement's name and the world facts are held through assembly; **EM-R5** the trace's partition by step; **EM-R6** the institution rename-surface list and its cascade (before any institution rename op). Each is golden-sensitive (no pins ⇒ byte-identical, proven on the full 525-row master) and prices its byte budget at pre-proof. **EM-P0b is WITHDRAWN** (it would permit the one seam that re-rolls every relationship). HONEST SIZE: six to nine packets in `src/generators/**`, the editor's critical path for the institution, faction, power and world-fact cards.
- **Relationships and conflicts are HELD; ties for a newcomer are an explicit op** (`roll-ties`, offered by a guard's `fulfil`, drawn on a stream keyed by the newcomer's stable id — after EM-P1) — a wave-1 follow-on, chartered with EM-B1b's family.

## Amendments of 2026-09-19 16:5x EDT — ARCH-REDERIVE version 2: the re-entry family re-cut from the whole-seam prototype (design §22.1; ODQ §934.47 addendum 19)

- **The family is re-cut against the MEASURED touch list (nineteen files), compiled only after the third recon reports:** **EM-R0** the merge — `rederive` returns a re-derivation, and the editor's consequence is the three-way merge `record ⊕ (R1 − R0)` over readings, keyed collections by key, receipts recomputed last (pure, in `src/domain/edit/`; the largest member); **EM-R1** pins cloned on entry + the WRITER CENSUS as a walker with its six classes (producer · mutator · enricher · replay · mint · MIRROR) and the two exemption rules (a mirror always runs; a held writer on a SHARED stream consumes its draws and discards, only a named child stream is skipped); **EM-R2** the roster final inside `enrichNpcCoherence`, with `ensureFactionStructuralNpcs` named and `relinkFactionMembers` exempt; **EM-R3** the power structure final — the replay does not run under a held structure (`resizePoliticalRoster`, `neighbourFactions`, `powerEconomyReconcilePass` named), the freshness assert pin-aware at its THREE call sites, its fingerprint a recomputed receipt; **EM-R4** is reduced to the settlement NAME held through assembly (world facts already flow as `config′`); **EM-R5** the trace partitioned by every step whose entries are ABOUT a held key; **EM-R6** the institution rename-surface list and cascade (still entirely absent). Each is golden-sensitive (no pins ⇒ byte-identical on the full 525-row master) and prices its bytes at pre-proof.
- **`stress` is a re-derived reading, never a held fact**; the DM's way to add a stressor is measured by EM-A1 / EM-B2b.
- **THE PRE-PROOF GAINS STEP 10 (from EM-P3's build):** every `requiredSymbols` row must still hold AFTER the packet's own edits — a symbol the packet MOVES or DELETES at a path is a `retiredSymbols` row (and the successor joins `requiredSymbols` only at the LANDED flip); the pre-proof simulates the post-edit validation, because every check until `check:packet` runs against the pre-edit tree.

## Amendments of 2026-09-19 17:0x EDT — THE DEFERRED WORK, SEQUENCED (the owner: "Put the deferred work into the sequence were appropriate"; ODQ §934.53)

Every item the chair had recorded as deferred-and-unsequenced (the kit's `DEFERRALS-UNSEQUENCED.md`, 22 items) now has a slot, a condition, or an owner's decision point. THE TRAIN PLAN AS IT NOW STANDS (one build at a time in the slot; each member made READY by an Opus pre-proof at the tip; each train ends in a bare full check; parallel lanes that need no sealed dispatch run in their own worktrees and are composed at a train boundary):

| when | what | why there |
|---|---|---|
| **EM-T3 (closing)** | EM-B1d (building) → **CURE-A: the model walker's `codeOnly` regex** (`chooserTotality.walker.test.js`: a space-or-tab class instead of `\s*` under the multiline flag; an Opus cure lane on the branch; proof = the walker's scanned set is IDENTICAL before and after, or the difference is the finding) → ONE lighting refreeze (the chair) → run 17 | the cure is validated by the terminal that follows it instead of waiting for one |
| **TOOLING WINDOW (after run 17 green, before EM-P2 v3 is promoted)** | **TOOL-1** the validator compares a non-terminal packet's §7 change table with its JSON `changeManifest` (EM-B1d's Markdown named eight files against a five-file manifest and every gate passed) — measured first: how many of the estate's packets carry a parseable §7 table, so the arm is scoped and reds nothing historical · **TOOL-2** a dist-gated build test that is run WITHOUT a dist says so in a way a lane cannot read as green (today `describe.runIf(DIST_EXISTS)` skips silently unless `VERIFY_DIST=1`) · **TOOL-3** a census of every emitted WORKER against the byte ceilings, and a measured, monotone-down ceiling minted for each one that has none (`townSceneExport.worker`, `advanceInterval.worker` — the tick packets of wave 3 will land bytes in the second) | every later promotion and landing runs under the cured instruments; TOOL-3 must precede EM-E1 |
| **PARALLEL, from run 17's green base (own worktrees, composed at EM-T4's terminal; they never hold the slot)** | **FIX-F1** the eight tabs still on the tier-noun backstop (ODQ §934.22 addendum 2) · **FIX-F2** the prose-numerics detector's missing `weight` token · **FIX-F3** the two uppercase sites kept because their screen twins still shout (`Services`, `SupplyChainFlow`) · **REVIEW-P** the public-path review (ODQ §934.24), a read-only walk on the consist preview · **RECON-G** the Overview `resilienceScore` against its band label: measure what the 2026-07-22 re-grade would move and put the proposal to the OWNER (a src/domain re-grade is the owner's) | fixes-chain follow-ups are not editor packets and need no seal; the five-agent ceiling and the gate mutex still bind |
| **EM-T3.5** | EM-P2 version 3 (its pre-proof re-runs the 75 rows at the then-tip) | EM-A1 waits on it |
| **EM-T4** | EM-P1 (stable identity; alone; the golden door once, under a signed record) | unchanged |
| **EM-T5** | EM-A1 (its pre-proof ALSO measures how a DM adds a STRESSOR — `stress` is re-derived, never held, and neither terrain nor culture moves it) · EM-A2b · EM-B1e · EM-P1b (with the three name-keyed choice sites) · **EM-B3c** (the scanner's SQL-side subset arm) | EM-B3c must land before the FIRST WRITER of either editor key exists, and that writer is now EM-C4a |
| **EM-T6** | **EM-B1f** (status-based absence at `isOffStage`; FIRST — it blocks EM-B1a) → EM-A2a → EM-B1a | EM-B2a left this train (re-cut behind EM-R) |
| then | EM-B1c (after EM-P1b: a rename must not re-roll prose) · EM-B1b · **EM-B1g `roll-ties`** (a newcomer's relationships as an explicit op, drawn on a stream keyed by the stable id; design §22 ruling 3) · EM-A3 | by dependency |
| **EM-T7** | **EM-B3d** (the `importScrub` strip of the two editor keys — defense in depth, deferred by EM-B3a's K4) → EM-C4a → EM-D0 (the first door) → **BROWSER PASS 4** (an Opus browser lane on the consist preview: the door, the halo, the phone sheet — and the long-owed check of the preview persona's "row appears in the library" claim) | ⛔ EM-C4a IS THE FIRST WRITER of `dmLayer`: its packet carries the arm EM-B3a could not — a REAL written layer run through EM-B3a's travel tests (fork · import · gallery · export · the anonymous envelope) — and a red there is ITS stop |
| **the EM-R family** | EM-R0…R6 after the third recon; EM-R2/R3 also retire the wasted `powerLinkage()` run under a held roster; then EM-B2a (its re-cut repeats the real-layer travel arm for `rederive`'s writes) · EM-B2b · EM-B4 | unchanged in order; two notes attached |
| waves 2–5 | as chartered; **the owner's two unconfirmed defaults have decision points:** the per-tick registry cap (default: none) is asked before EM-C1 is compiled; pending decrees in the PDF (default: no) before EM-D3 | so a default is never built by silence |

**Conditional, not slotted:** the two byte buy-backs (ODQ §934.19 addendum 2) run only on the owner's veto of the re-mints; if no veto has come by the editor program's last terminal, they close as not wanted.
**The owner's hand, with its natural moment named (never the chair's act):** the paused push chain (ODQ §934.42) — run 17's green tip is the first coherent point to resume it, and every train terminal after it is another; migrations 201/202, the admin grant, PRs #51/#52 and the Vercel redeploy ride that push; the ratifications offered today (ODQ §934.19 addenda 2–3; design §20–§22.1) are best given before it.

## Amendments of 2026-09-19 17:12 EDT — STAFFING: four working lanes and one gate seat (the owner's lane law, restated; ODQ §934.54)

The owner, 2026-09-19 17:05 EDT, to a chair that had two lanes running: *"remember that you can have up to four lanes doing stuff. One of those lanes is reserved for if any you're waiting gates and suites. But once a bill [build] has gone to the point where it's waiting on the suite or a gate it is paused and wait until it's resumed for its turn."* THE FIVE SEATS ARE FOUR WORKING LANES + ONE GATE/SUITE SEAT, and the four are a floor the owner expects used. How this train plan is staffed under it:

- **The slot stays single** (sealed dispatch wants the worktree ON the branch): ONE packet builds at a time. Everything else runs AHEAD of it or BESIDE it.
- **AHEAD of the slot — the pipeline.** Compile and pre-proof lanes are read-only at a detached tip, so they run while the slot builds: at all times the next TWO members of the sequence are being compiled or pre-proofed. A pre-proof taken at an older tip is promoted by the chair's WINDOW RE-RUN (the commits since, against the packet's declared paths — EM-B1d's promotion is the model); a member whose substrate an in-flight packet touches waits for that landing instead (EM-P2 version 3 waits for EM-B1d: both edit the mutation-coverage manifest).
- **BESIDE the slot — the PARALLEL lanes START NOW, not after run 17** (this amends the sequencing amendment above in one respect only): FIX-F1/F2/F3, TOOL-1/2/3 and RECON-G are built in their own worktrees on their own branches under `briefs/LANE-PARALLEL.md`, cut from the newest landed tip; what waits for a green terminal is their COMPOSITION onto the integration branch (the tooling cures after run 17's green and before EM-P2 version 3 is promoted; the fixes at EM-T4's terminal), so a terminal never validates a tree it did not run on. REVIEW-P needs the preview server and runs outside a terminal's window.
- **The gate seat.** Every gated run (vitest · eslint · typecheck · build · `verify:dist` · `check:packet`) is PAUSE-AND-RESUME: a lane that reaches one finishes its edits, writes its resume note, ends its turn `PAUSED AT THE GATE`, and its working seat goes to the next queued lane; the chair resumes paused lanes ONE AT A TIME in the order written in the kit's `gate-queue.md`; the chair's own terminals take their turn in the same order. Both shared briefs carry the practice verbatim (`LANE-EM-BUILD.md` verb 3; `LANE-PARALLEL.md` verb 4).

## Amendments of 2026-09-19 17:25 EDT — ARCH-REDERIVE version 3: the re-entry family re-cut from the executed merge (design §22.2; ODQ §934.47 addendum 20)

**THE EM-R FAMILY, IN BUILD ORDER** (each golden-sensitive — no pins ⇒ byte-identical on the full 525-row master — and each pricing its bytes at pre-proof; the merge's modules live in `src/domain/edit/` and must stay out of the generation worker and the first-paint set by PLACEMENT):
| packet | what | notes |
|---|---|---|
| **EM-R0a** | THE CLASS REGISTER: every path of a generated record classed HELD · WORLD · CONSTANT · READING · MIRROR · RECEIPT · HISTORY · AUTHORED; the declared KEY of each of the 41 keyed collections (the three trap keys named); the keyless collections declared ATOMIC; the CONSISTENCY GROUPS (seeded from the recon's MIXED census — the compile proposes the smallest object that closes each invariant, the chair validates) — with a totality walker (a new record key reds until classed) | pure data + one walker; compiles NOW |
| **EM-R0b** | `recordInvariants`: the recon's instrument promoted — every check validated by its own control over the golden corpus (clean on plain records; the one pre-existing defect pinned KNOWN until FIX-G1), the two REJECTED candidates recorded as rejected with their controls | pure; compiles NOW beside R0a |
| **EM-R1** | pins cloned on entry + the WRITER CENSUS walker (producer · mutator · enricher · replay · mint · MIRROR) and its two rules (a mirror always runs; a held writer CONSUMES AND DISCARDS — the first cut skips nothing outside `enrichNpcCoherence`) | unchanged from §22.1 but for the no-skip rule |
| **EM-R2** | the roster final inside `enrichNpcCoherence` (`ensureFactionStructuralNpcs` named; `relinkFactionMembers` exempt); retires the wasted `powerLinkage()` run under a held roster | |
| **EM-R3** | the power structure final: the replay does not run under a held structure; the freshness assert pin-aware at its THREE call sites | the fingerprint's recompute moves to the merge (R0c) |
| **EM-R4** | the settlement NAME held through assembly | |
| **EM-R5** | the trace partitioned by every step ABOUT a held key | precedes any draw-skip |
| **EM-R0c** | THE MERGE: node three-way with the short-circuit and ABSENT · groups whole · keyed collections by declared key · keyless ATOMIC · order three-way · mirrors recomputed · receipts recomputed LAST · HISTORY and AUTHORED untouched · the runtime invariant guard with its three-step escalation · the delta model carrying before / now / the-edit-alone · THE CHAIN (no batch entry point exists) | pure, in `src/domain/edit/`; the largest member — split again at compile if it breaks the 400-line budget |
| **EM-R6** | the institution rename-surface list, its cascade, AND the removal sweep over the same list | |
| **EM-R7** | THE CORPUS RATCHET: over 63 rows × the eight edits × four tiers — identity 63/63 with no edit; zero MIXED objects outside a declared group; `recordInvariants` clean with ZERO escalations; chain determinism; the honesty figures re-measured and recorded; the order rule's second arm counted | lands last; it is the family's terminal proof |

**Rulings that belong to OTHER packets, recorded here so their compiles carry them:** `set-faction-power` renormalises (design §22.2 item 8) — the power-share op's packet; the NEWCOMER is enriched once at birth on a stream keyed by its stable id (item 9) — EM-B1b, after EM-P1; the delta card's copy for the three numbers (item 12) — the EM-D family; **FIX-G1** (NEW, a PARALLEL lane: the generator's viability summary counts six dependencies beside a list of five on one town row — measured first; a cure that moves the prose golden waits at the owner's signed door).

## Amendments of 2026-09-19 17:26 EDT — A CORRECTION TO THE TRAIN ORDER: EM-A2a PRECEDES EM-A2b; EM-B1e FLOATS (ODQ §934.47 addendum 21)

Measured from the compiled drafts while planning the pre-proof pipeline (kit `packets-waiting/`): **EM-A2b's change manifest MODIFIES `src/domain/edit/pools.js` and `tests/domain/editPools.test.js`, which EM-A2a CREATES**, and A2b's own header says `Depends on: EM-P3 and EM-A2a — this packet APPENDS rows to A2a's POOLS`; EM-A2a in turn depends on EM-A1 (a `pool` field names a pool id). The train table above — the original plan's rows for EM-T5 and EM-T6, copied unchanged into the sequencing amendment — carries A2b in T5 and A2a in T6: the wrong way round; the sealed dispatch would have refused A2b for a missing substrate. CORRECTED:

| when | members, in order |
|---|---|
| **EM-T5** | EM-A1 → **EM-A2a** · EM-P1b (with the three name-keyed choice sites) · EM-B3c |
| **EM-T6** | **EM-B1f** (first — it blocks EM-B1a) → **EM-A2b** → EM-B1a |
| **floating** | **EM-B1e** (the pulse's one ruin writer): its header says `Depends on: NONE`, its two paths touch no other member's — it is pre-proofed NOW and built whenever the slot would otherwise wait for a READY packet, joining whichever train is open |

Everything else in the sequencing amendment stands. The lesson, taken into the pre-proof pipeline: a train table is a CLAIM until it is checked against the members' change manifests — the chair diffs every train's order against `changeManifest` CREATE/MODIFY pairs before its first member is promoted.

## Amendments of 2026-09-19 17:36 EDT — EM-B3c RE-CUT (it closes a LIVE gap with migration 203); EM-B1f's rulings; TOOL-3's style; TOOL-4 (ODQ §934.55 and §934.47 addendum 22)

- **EM-B3c, version 2 — the charter's premise was REFUTED and a live gap was found.** The drift test DOES assert that every client denylist token has a covering SQL alternative (36 of 36). What nothing asserts is the scanner's SECOND mechanism: that every member of the client's `WORLD_SNAPSHOT_HARD_DENY` is refused by the net-current scanner's `hard_deny` array (or a covering alternative). Measured, and verified by the chair: `factionPairStates`, `envoyErrands` and `concludedWars` — three private ledgers the client refuses — are named by NO migration; 136 claimed "every CONDITIONAL_LEDGER_KEY but pantheon", the three joined the client list on 07-20, 08-03 and 08-31, and 202 copied 136's array. RE-CUT (one packet, one landing; the red-first proof IS the finding): the hard-deny SUBSET walker is written first and is RED at the base for exactly those three; **migration 203** re-creates the scanner with the array gaining the three and every other byte of the body unchanged from 202 (a contracted mechanical diff), with everything a new scanner migration owes (the rehearsal-train registration, the `scanner.owner` pin re-pointed 202 → 203, every constant or census that names the newest migration); then the walker is green, with a reconciliation arm (a declared-but-unextractable re-creation reds), a pinned NUMERIC migration sort, and a `CANNOT-CATCH:` header. NO exemption list is built: a denylist that mirrors what the client already refuses only grows, which is within the chair's delegation; APPLYING 201–203 stays the OWNER's hand. **It FLOATS WITH PRIORITY** — built as soon as the slot has room after train EM-T3's terminal.
- **EM-B1f — the chair's rulings on its compile (DRAFT, READY-able; every charter premise held; the defect reproduced before the fix: a `jailed` or `exiled` ruler reads `securityBand: holding`, `rulerId` set, while a dead one reads `unseated`):** (1) THE ARM'S MEMBERS: `exiled`, `jailed` (design §15) AND `removed` (every reading in the estate already counts it absent; a removed holder must not govern) — NOT `missing` and NOT `retired`: the estate's readings disagree about them ON PURPOSE (R18: an irreversible consequence may only follow an irreversible cause), the chokepoint FILTERS a person out of the settlement every pulse kernel reads, so joining them would silently override the house-roster reading; a missing or retired holder keeps the place until the DM or the pulse moves it, and what happens to the SEAT is the holder guard's question in EM-B1a's `set-npc-status`. (2) FORM B: an exported frozen vocabulary `OFF_STAGE_STATUSES` read by membership, absence-safe (94 % of generated NPCs carry no `status` key) — one named place a later packet reads and the totality walker judges; its bytes are re-measured at pre-proof and a rise is attributed, never a reason to choose the worse form. (3) The union-totality walker's NINTH roster row (`src/domain/roads/state.js`, which gains a trigger token) belongs to THIS packet, with the reasoned omission naming `active`, `dead` (paired by every caller), `missing` and `retired`. (4) It is placed only after EM-B1d is LANDED (three shared paths) and its pre-proof is re-run at that tip. (5) `envoyCasting`'s double refusal is the settled belt-and-braces idiom. (6) The lazy engine's ~700 B margin is a RAISE-TIME convention (the literal assertion is the law between re-mints).
- **TOOL-3's STYLE, ruled now because two waiting packets price against it:** the pulse worker (`advanceInterval.worker`, a 549-module closure that most domain packets touch) and `townSceneExport.worker` get MEASURED ceilings with a DECLARED HEADROOM (4 KB), ratcheted DOWN by the chair at every train terminal with per-module attribution — growth is noticed and named once per TRAIN, not re-minted once per packet; the generation worker's exact-size ceiling is unchanged (it guards the product's first forge).
- **TOOL-4 (NEW, tooling; after EM-T5):** the latest-wins walk over the migrations directory now exists in three copies (two shipped security guards and EM-B3c's walker); consolidate them into one shared helper with the parsing hazards EM-B3c measured (non-`$$` dollar tags are routine here; `DO`-block and `drop` + `create` re-creations) as its test table. Deferred deliberately: it edits guards that are `checks` members of LANDED packets.

## Amendments of 2026-09-19 17:41 EDT — EM-B1d RE-CUT TO VERSION 5: two readings get two names; the pre-proof gains two steps (ODQ §934.47 addendum 23)

- **What happened.** The Opus build lane built EM-B1d version 4 complete and green and STOPPED UNCOMMITTED on three measured contradictions, each needing a path the manifest did not declare: `build:edge-shared` re-stamps ALL FIVE bundle metas in one build window (seven generated artifacts, not four — and the packet's own sealed `checks` run that generator, so the packet could not pass its own seal); a committed exact pin on `ROSTER_ABSENT_STATUSES` (`tests/generators/densityLaw.test.js`); a committed fixture spelling `status: 'imprisoned'` (`tests/domain/espionageMission.test.js`). Its "noticed, not touched" line convicted the PACKET: version 4 put `jailed` — a REVERSIBLE status — into `ROSTER_ABSENT_STATUSES`, whose one consequence is a house's DISSOLUTION, against that file's own law (R18: an irreversible consequence may only follow an irreversible cause), and re-pointed two availability readers at the roster constant, spreading the conflation. The chair promoted that contract; the error is the chair's.
- **THE RE-CUT (version 5):** `ROSTER_ABSENT_STATUSES` stays exactly `dead · exiled · removed` (its header judges `jailed` PRESENT; the committed pin stands, and gains one new pin: a house whose only figure is jailed reads `crewed`); a NEW exported AVAILABILITY vocabulary, `NPC_UNAVAILABLE_STATUSES` = `dead · exiled · jailed · removed` ("cannot act, hold a place, or succeed — NOT the house-roster reading"), is what `successors.js` and `envoyCasting.js` (+ `missing`) read; its HOME is chosen by measurement against the zero-slack generation worker (the placement before the ceiling); the walker's roster rows carry the lane's `spelling` field (`literals` | `union-read`) as contract; the three re-stamped metas and the two test files join the manifest. The lane drafts version 5 (compile-grade, ungated), the chair validates and places it, the lane re-seals and finishes.
- **EM-B1f INHERITS A MEASUREMENT (before its promotion):** `isOffStage` FILTERS a person out of the settlement every pulse kernel reads. If any IRREVERSIBLE consumer (house dissolution above all) reads that filtered view, then EM-B1f's reversible members (`jailed`, and `exiled` by a later decree) reach an irreversible consequence through the FILTER even though the roster constant refuses them — and the same would already be true today of a shelved or hostage last member. Measured first; the chair rules from it. `OFF_STAGE_STATUSES` should be DERIVED from `NPC_UNAVAILABLE_STATUSES` minus `dead` rather than spelled a third time.
- **THE PRE-PROOF GAINS STEPS 11 AND 12:** (11) RUN THE PACKET'S OWN `checks` IN A THROWAWAY COPY and diff the tree — every path a declared command writes (generated artifacts above all: the edge-shared generator re-stamps every sibling's meta) is a change-manifest row; (12) SWEEP THE TESTS, not only `src/`, for every symbol the packet changes and every literal it retires — an exact pin or a fixture that the contract reds is a declared TEST path with its cure, found at pre-proof, never at the build. The EM preamble gains row 12 (§P2) saying the same of generated siblings.

## Amendments of 2026-09-19 18:10 EDT — THE OWNER'S LAW: THERE IS NEVER ANY DEFERRED WORK; ANYTHING EMERGENT GOES INTO THE SEQUENCE (ODQ §934.56)

The owner: *"there should never be any deferred work. If anything comes up emergent to put it in the sequence."* FROM NOW: the word "deferred" names nothing in this program. A finding, a follow-up, a lane's "noticed, not touched", a recon's "could not measure" is, IN THE SAME TURN IT SURFACES, given exactly one of three fates and written here: **a SLOT** (a train, a window, a parallel lane, or a named packet's compile/pre-proof that must carry it), **an OWNER'S DECISION POINT** with its moment named, or **CLOSED — not work** with the reason. There is no fourth state and no side list: the kit's `DEFERRALS-UNSEQUENCED.md` is retired to a one-line pointer at this section. Every lane brief's report asks for "anything noticed"; the chair slots each item before its next dispatch.

**Everything that surfaced today and had no slot, slotted now:**
| item (where it surfaced) | fate |
|---|---|
| **EM-B1h** (NEW; EM-B1e's pre-proof R2): `worldPulseFate` is an OPEN vocabulary with eight writers — FINITE-SEMANTICS is the estate's law; one frozen `WORLD_PULSE_FATES` and a totality walker over its writers | SLOT: train EM-T6, after EM-B1f and BEFORE EM-B1a (whose decree adds the ninth spelling, `ruined_by_decree`); compiled when EM-B1e has landed |
| **FIX-T1** (EM-B1e R7): 28 hand-built replicas of the ruin shape in `tests/` | SLOT: a PARALLEL test-side lane after EM-B1e lands (it needs the writer), composed at EM-T4's terminal |
| the fourth institution vocabulary in `institutionStatusModel.js` (`operational · impaired · shell`; EM-B1e R4) and the irreversible consequence of a decree-ruin — criminal leashes sever, so undo and the tick must each say what becomes of them (R9) | SLOT: binding measurements of **EM-B1a's pre-proof** |
| the charter's stale EM-T3 row ("`jailed` and `ruined` into the two typedefs"; EM-B1e R5) | CORRECTED HERE: `EntityStatus` is NOT widened; `ruined` is the pulse's own literal, written through EM-B1e's one writer, and the institution-state pool is EM-B1a's |
| the third recon's "could not measure" list (sequences beyond three trials; the 63-row sweep of the edit findings; thorp and hamlet; undo-then-re-edit) | SLOT: **EM-R7's corpus ratchet** covers ALL SIX tiers, every ordered PAIR of the eight edits for chain determinism, and undo-then-re-edit; its compile carries this row |
| EM-A1 has no JSON manifest in the kit (an `.md` only) | SLOT: its compile pass produces one, queued the moment EM-P2 version 3 lands (it reads that register) |
| a packet whose `checks` name a `tests/build/` file would now be refused by the dist-gate guard with no `dist/` | SLOT: the pre-proof brief gains the sentence with TOOL-2's composition (the tooling window) |
| sibling copies of the model walker's import-blanking pattern (CURE-A lists them) | SLOT: **CURE-A2**, the same sitting as CURE-A's report, before run 17 if any sibling is convicted; CLOSED if none is |
| the scratch estate (the second slot `slot-2`; the read-only tips `read-tip-a41a0e109` and `read-tip-58fcfe614`; finished lanes' scratch dirs) | SLOT: the chair retires them at train EM-T3's terminal, after the kit holds what matters |
| REVIEW-P, the public-path review | SLOT sharpened: dispatched the moment run 17 exits (the preview server is free then) |
| the two conditional byte buy-backs (ODQ §934.19 addendum 2) | **CLOSED — not work.** They exist only as the cure for a veto of the ceiling re-mints; a veto, if it comes, is a new order and is sequenced that day |
| a shared `tests/helpers/distGate.js` to unify 29 re-spellings of one gate (TOOL-A) | **CLOSED — not work:** TOOL-2's one setup guard cures the class without touching the 29 files |
| the 39 TERMINAL packets whose Markdown tables disagree with their manifests (TOOL-1's estate run) | **CLOSED — history:** terminal packets are exempt by the arm's design; nothing is re-litigated |

## Amendments of 2026-09-19 18:23 EDT — RECON-G's findings, each given its fate (ODQ §934.57)

| finding (kit `findings/RECON-G/`) | fate |
|---|---|
| THE REPORTED DEFECT IS REFUTED: over all 525 golden rows the food-security WORD is exactly what its thresholds say (0 disagree), and no band was re-cut on 2026-07-22 — that commit touched `OverviewTab.jsx` only and put the word and the bar side by side for the first time | **CLOSED — not a defect** as reported; the 2026-09-18 report's "domain re-grade" reading is withdrawn |
| WHAT IS REAL: the WORD (cut from the food deficit) and the BAR beside it (`resilienceScore`: granary months, source diversity, import independence, adequacy) are two different facts shown as one — a bar of 73 sits beside three different words; "Deficit" averages a LONGER bar than "Active Famine"; 124 of 525 towns print an alarming word beside a bar at or past half | SLOT: **FIX-G2** (NEW, a PARALLEL display-only lane; zero goldens, zero saves): the bar is NAMED for what it measures and the caption is corrected, in the tome's grammar; composed at EM-T4's terminal with the other fixes |
| THE LIFECYCLE FINDING: the bar is re-graded every tick, the word NEVER — after one quiet campaign month 43 of 63 settlements print a word the thresholds contradict, while the Overview's own caption promises "Food Security is re-judged as the campaign advances" | **THE OWNER'S DECISION POINT, put to the owner today:** does "Food Security" name the land's STRUCTURAL condition (frozen at generation — then the caption is wrong and FIX-G2 corrects it) or the table's LIVED condition (re-graded at the tick — then it is a tick-wave packet, it moves no golden, and it changes lived wording on every existing save)? FIX-G2 ships the honest caption either way |
| the `Surplus` rung is unreachable: the largest surplus in 525 towns is 8 against a threshold above 40, and the Compendium documents it | **THE OWNER'S DECISION POINT** (a `src/domain` threshold is the owner's): lower the threshold, or strike the rung from the Compendium — asked with the question above |
| FIX-G1 MEASURED: exactly 1 of 525 rows; `src/generators/economy/viability.js` counts dependencies over `issues` AND `warnings` while the published list reads `warnings` alone (two `foodBalance.js` arms push a DEPENDENCY into `issues`); the LIST is right (`metrics.dependencyCount` agrees with it 525/525); a one-site cure re-records ONE generator-golden row and ZERO prose-manifest rows; saved worlds keep the old sentence (stored verbatim) | SLOT: **FIX-G1** is built as a PARALLEL lane that stops at the golden; its one-row re-record RIDES EM-P1's SIGNED GOLDEN DOOR at train EM-T4 (one signing, each cause attributed — EM-P1's pre-proof measures whether the door allows it). THE OWNER signs the before/after sentence |
| HAZARD: the kit's recon `instrument.mjs` changes directory into the READ TREE at import, so a relative write lands in a tree other lanes read | **CLOSED by cure, same turn:** a loud header on every kit copy, a line in the compile and recon launch texts, and the two running compile lanes warned |
| RECON-G's unmeasured: the orchestrator ticks by the week (shifts the calendar, not the outcome); the prose-manifest row count under the rejected derive-from-score cure; off-corpus frequency | **CLOSED — not work:** each matters only to a cure that was refused |

## Amendments of 2026-09-19 18:30 EDT — the fifth landing; EM-B1d version 5 placed; the re-derivation compiles ruled; EM-R0d (ODQ §934.47 addenda 24 and 25; design §22.3)

- **EM-B1e LANDED** at `8f714cf3c` (flip `50e1f237b`): the pulse's one ruin writer, both calamity records byte-identical, all three goldens unmoved — built in a SECOND SLOT worktree during the first slot's idle window. **EM-B1d VERSION 5 PLACED** at `ad7ddf2c9` and building there. Fates of what the landing surfaced: its +437 B in the uncapped pulse worker (the pre-proof said +270) is named in TOOL-3's launch text so the first per-train attribution carries it; its arm A4 mints a SECOND pin on the preset-witness digest outside the freeze register — **FIX-T1** (the test-side lane already slotted after this landing) makes that pin read the one place a lawful pulse shift re-records, or names it in the witness suite's own header.
- **THE EM-R FAMILY GAINS EM-R0d AND ITS FIRST TWO MEMBERS GO TO A SECOND VERSION** (design §22.3): order is now **EM-R0a (v2) · EM-R0d · EM-R0b (v2) · R1–R5 · R0c · R6 · R7**. EM-R0a v2: the generation receipt classed BY PATH (what it says of the ACT of generation is HISTORY; its checks and judgments are READINGS); groups as `{ id, root, members }`; a collection under a cross-entry total is ATOMIC; the ratified group seed; the save → load key-set measurement. EM-R0d (NEW): the legitimacy, readiness and food-security ladders exported from one domain-layer home, golden-neutral, byte-priced by placement. EM-R0b v2: reads the four ladders by import; ships the cross-key checks and the `stress ≡ stressors` identity; keeps the five evidence checks live; teaches its flag data on the full 525 rows. Both second versions are RESUMES of the lanes that compiled them, queued behind the critical path (the family builds after train EM-T7).

## Amendments of 2026-09-19 18:35 EDT — THE OWNER SIGNED THE GOLDEN DOOR (ODQ §934.58)

The owner, in chat: *"I give my sign off to the golden"*. SCOPE, as it was put to the owner (ODQ §934.57 and the chair's report of the same hour): ONE signed act at train EM-T4 carrying TWO attributed causes — (1) **FIX-G1**: exactly ONE row of `tests/fixtures/generator-golden-master.json` (`town · germanic · mountain · mountain_pass · civilized`), the viability summary's "…has 6 operational dependencies…" becoming "…has 5 operational dependencies…", and ZERO prose-manifest rows; (2) **EM-P1**, stable identity: the re-record of the goldens its permanent ids move — over the MEASURED movers only. HOW IT IS EXECUTED: by the CHAIR, never a lane, through the repo's own door (`tests/helpers/goldenRecordDoor.js`): `GOLDEN_SHIFT_SIGNED` names a signed-record FILE carrying the owner's words verbatim, the action `re-record`, the surface, the proof form and `predictedRows` stated BEFORE the write — a re-record that moves a different number of rows than predicted writes nothing. BOUNDS THE CHAIR HOLDS ITSELF TO: EM-P1's exact price (files and row counts) comes from its pre-proof and is REPORTED TO THE OWNER before the door is opened; any mover the two causes do not explain — a prose row, a non-id key, a second FIX-G1 row — is a STOP and returns to the owner; this signature covers these two re-records and nothing else (a later golden shift is a new signature).

## Amendments of 2026-09-19 18:49 EDT — a red already on the branch (CURE-B); the fix lane's findings; EM-P1 BLOCKED and re-ruled (ODQ §934.47 addendum 26; §934.58 addendum)

**A RED ALREADY ON THE INTEGRATION TIP, found by the FIX-F lane:** the prose-numerics walker's exact-identity arm addresses allowed rows by FILE + LINE; EM-B3a added thirteen lines above a row in `worldSnapshotPublic.js` and nobody re-addressed it — a build lane's focused runs never reach that walker and its baseline is outside every manifest. Run 17 would have red on it. FATES: **CURE-B** (NEW) — every baselined row this train's landings shifted is re-addressed ONCE, addresses only, by the same cure lane and sitting as CURE-A, BEFORE run 17; the shared build brief now runs `tests/lint` WHOLE in every build's last batch (lawful reds named: the lighting census and the lane's own shifted rows, quoted for the terminal); the pre-proof brief gains **step 13** (line-addressed baselines priced at pre-proof).

| FIX-F finding | fate |
|---|---|
| FIX-F1 is LIVE and larger than the ledger said: ELEVEN option bags, not nine, never carried the tier noun, and eight disagreements are READER-VISIBLE today (a city's Economics tab prints "too small to change what the town is") — ODQ §934.22 addendum 2's "correct on screen" is refuted | CURED on the lane's branch (all eleven; a source-level guard with five plants; neither golden moved); gated batch queued; composed at EM-T4's terminal |
| FIX-F2: `weight` is absent from the detector's float tokens, but adding it convicts only four English comparatives ("carries more weight in council"); the detector has no allow-list and forbids raising its ceiling | RE-CUT as **FIX-F2b**, the same lane's next car: the SLOT is renamed `weight` → `weightWord` across the receipt pools (the estate's twice-ruled cure; it reaches the contract-parsed `RECEIPT_POOLS_LEGACY.md` and eight kind-pool walkers — blast radius measured first; a golden or a cell key moving is a STOP), then `weight` joins the tokens with zero hits; the stale `namesAScalar` citation is corrected there |
| FIX-F3 | **CLOSED — cured already** at `475021901`; recorded correction: the label idiom is the `tokenCase` three-rung ladder ending in sentence case, NOT small capitals (the PDF renderer has no small-caps face) — the chair had mis-stated it to two lanes and has corrected both |
| `SummaryTab.jsx`'s fifteen uppercase sites (the typography train's remainder) | SLOT: **FIX-F4**, a parallel lane, composed at EM-T4's terminal |
| `'FOOD SECURITY'` deliberately absent from `LADDER_WORDS` | SLOT: carried by **FIX-G2** (it re-words that card) |
| `EconomicsTab.jsx` at 600 of its 600-line ceiling | **CLOSED — not work now:** lawful and passing; the pre-proof's standing max-lines measurement forces the extraction into whichever packet next touches it |
| the print-parity suite's screen model; a stale row count in a comment; the unwritten DOM-level pair arm | SLOT: all three ride FIX-F1's commit (the pair arm if it fits in about forty lines, else its measured size comes back for a slot) |

**EM-P1 (stable identity) — its pre-proof came back BLOCKED, and the chair re-rules it:**
- **THE NPC ID SPELLING DOES NOT CHANGE.** `npc_<n>` is already persisted, keyed into the pulse's ledgers, carried by sidecars and crossed through the veil; re-spelling it to `npc:<n>` would have turned a key addition into a VALUE move across every golden and every NPC-keyed ledger for cosmetic uniformity. Stable identity adds an id where none exists; it never re-spells one that does.
- **THE FACTION HALF IS A MIGRATION, NOT A MINT, AND IT IS DESIGNED BEFORE IT IS BUILT.** Generated factions carry NO id; `factionRefOf` reads `id || name` at nine sites; the PERSISTED `linkedFactionIds` would silently flip from names to ids the moment an id exists; the packet's named mint site (`densityAscension.js`) is the SIMULATION-time planner whose `faction.<slug>` ids are written into replayed event chains (lived history — they stay). **RECON-ID** (NEW; next free seat): every entity-id namespace in one table, every handle and resolver, what a rename does to each, pinned mode, what old saves hold. The persisted value flip is put to the OWNER with that measurement.
- **ONE MINT FUNCTION PER ENTITY KIND**, pure and idempotent, called by the generator step before any sort, by the save migration, and at the PIN boundary; an arm proves read → reorder → write does not re-point ids.
- **WHERE EM-P1 SITS IS BEING MEASURED NOW:** the pre-proof lane is establishing which entity's names key EM-P1b's three choice sites and whether ANY packet on the way to the first door addresses an institution or a faction by id. If none does, EM-P1 (institutions and factions together: ONE packet, ONE golden shift, ONE signature) moves BEHIND train EM-T7 and RECON-ID, and the program's riskiest packet leaves the critical path; if one does, the narrowest institutions-only EM-P1 serves it. Train EM-T4's content is ruled from that answer.
- Its noticed items: a standing note beside the shift-record README that `pipelinePinnedMode.test.js` reds for ANY golden-moving packet (SLOT: the chair's golden-door sitting); the third NPC id namespace, the nine hand-rolled faction slugs and `linkedFactionIds`' un-owned follow-on (SLOT: RECON-ID, then the faction-identity packet); the 28 files whose observed-shape findings the ids silence (SLOT: a per-file cured-or-silenced table in the re-cut packet's pre-proof).

## Amendments of 2026-09-19 18:54 EDT — EM-P1 LEAVES THE CRITICAL PATH; TRAIN EM-T4 IS RE-DEFINED; EM-P1b RE-CUT (ODQ §934.47 addendum 27)

**Measured by the EM-P1 pre-proof lane (kit `findings/EM-P1-preproof/`, §9):** NOTHING on the way to the first editor door addresses an institution or a faction by an id — EM-D0's first door is the NPC's card (NPC ids exist and are persisted), EM-B1a only shape-checks an op's target, EM-B1c's renames delegate BY NAME to the existing cascade, and the rest address steps, keys, fields, statuses or SQL. And INSTITUTIONS ALREADY HAVE A REGEN-STABLE ANCHOR: `src/domain/townMap/anchors.js`'s `anchorForInstitution` (`cat:<catalogId>` → `uid:<localUid>` → `name:<slug>`), consumed by seven modules, written for exactly the problem EM-P1 states.
- **EM-P1 MOVES BEHIND TRAIN EM-T7 AND BEHIND RECON-ID, WHOLE:** one packet (or none, if the recon shows the existing identities can be made total), ONE golden shift, ONE owner's signature — never an institutions-now / factions-later split, which would move all 525 golden rows twice under two signatures and buy nothing. The program's riskiest packet is off the path to the first door.
- **EM-P1b NO LONGER DEPENDS ON EM-P1.** Its three name-keyed choice sites are re-keyed on identities that ALREADY exist: the institution description variant (`assembleInstitutions.js`) and the glyph choice (`glyphAssign.js`, which already reads `anchorKey` first) on `anchorForInstitution`; the NPC pair site (`npcData.js`) on `npc.id`. The earlier amendment's "re-keyed on the stable id" is read as "the stable identity each entity already carries". It stays in train EM-T5; its compile carries this row.
- **TRAIN EM-T4 IS RE-DEFINED AS THE COMPOSITION TRAIN** (it had been "EM-P1 alone"): after run 17's green — compose TOOL-1 and TOOL-2, then the finished parallel fixes (FIX-F1, FIX-G2, FIX-G1 with its ONE-ROW golden re-record executed by the chair under the owner's signature of 2026-09-19, and FIX-F2b / FIX-F4 / FIX-T1 as they finish); in the slot, EM-B3c version 2 (the security gap, migration 203) and EM-P2 version 3; TOOL-3's ceilings; then its terminal full check validates the composed tree. Every slot that said "composed at EM-T4's terminal" stands under this definition.
- **A DEPENDENCY DEFECT, the A2a/A2b shape again:** EM-B1a imports an `EntityRef` typedef and names EM-A1 as its source, but EM-A1's row for `src/domain/edit/types.js` declares only `FieldKind` and `FieldDeclaration`. FATE: EM-A1's compile (queued behind EM-P2 version 3) CREATES `types.js` with `EntityRef` beside the two field types — a declaration names entity kinds, so the reference type is its to own — and EM-B1a MODIFIES that file to add `Op`. Both pre-proofs carry this row.

**FIX lane G's findings, fated:** FIX-G2 and FIX-G1 are built and staged (the bar is named "Stores and supply" through the `tokenCase` ladder; the viability summary counts the published list at one site, 370 stated counts and 0 disagreements over the 525 rows, and the zero-slack worker SHRINKS by 47 bytes) — gated batch queued. One number wearing three names across the Overview, the Defense tab and the PDF, and the PDF's Systems Health caption naming readings its chapter does not render → SLOT: **FIX-G3**, a parallel lane after FIX-G2 is composed (measure first whether the three are one number). The six title-cased Systems Health bar labels and `'FOOD SECURITY'`'s place on the ladder (both need a structural handle on the category header, pinned by element) → SLOT: **FIX-F4**, with `SummaryTab.jsx`'s fifteen sites. That the severe food-import dependency is filed under `issues` rather than the dependency list → **CLOSED — by design:** a severe dependency is shown to the DM as an issue, and after FIX-G1 the sentence and the list agree.

## Amendments of 2026-09-19 19:03 EDT -- TOOL-1 and TOOL-2 are BUILT on their branch; the lane's noticed items fated (the owner's law)

TOOL-1 (`52be5a1f2`: the validator compares a non-terminal packet's change table with its JSON manifest -- 189 packets valid, zero non-terminal disagreements) and TOOL-2 (`6fefb69e6`: a build test NAMED in a focused run with no `dist/` refuses instead of skipping; every unfiltered sweep is unchanged, proved by an unfiltered collection of 33,996 test entries with zero refusals) sit on branch `tooling-a-2026-09-19`, composed at train EM-T4. FATES of what the lane noticed: **TOOL-2b** (NEW; the same lane, before composition) tightens the guard's dist-exists predicate to `dist/index.html` + `dist/assets` (two build tests spell it that way and would still skip on a half-built tree) and moves the guard's tests out of the 3,200-line `testRatchet.test.js` into their own `tests/lint` file with its mutation-coverage row; the chair adds one sentence to `PACKET_STANDARD.md` at composition saying the arm's fixture suite is its anti-vacuity control (its live population is one packet and will be zero). CLOSED, not work: the terminal packets' table drift (history; exempt by design), per-file anti-vacuity tests in the twenty dist-gated files that lack them (the guard makes them moot for focused runs and `verify:dist` is strict by its environment), the four-character floor on a name filter (an accepted limitation, written in the guard's header), and re-running the 25-minute unfiltered collection (a one-off proof).

## Amendments of 2026-09-19 19:10 EDT — EM-B1h's compile ruled; a live pulse defect found and slotted as EM-B1i (ODQ §934.47 addendum 28)

**EM-B1h compiled READY-able, with the charter's figures corrected by execution:** `worldPulseFate` has THIRTEEN write sites in SIX files (five literal, four computed through three derivers, four writing `null` to CLEAR) and SEVENTEEN live values — `ruined_by_decree` is the eighteenth, not the ninth. A literal-only scan would have minted a five-word vocabulary and refused twelve live values (`disbanded` is produced in a file that never writes the field). Two readers, neither a display surface, neither branching on a value. RULED for its second version (a resume of the same lane): (1) the HOME is a zero-import leaf, `src/domain/worldPulse/worldPulseFates.js` (the kernel's own static closure is 327 modules against one — the placement before the ceiling; `envoyErrandVocabulary.js` is the precedent); (2) `null` is the CLEAR sentinel, never a member, and the four clear sites are declared `clear` roster rows; (3) the eighteen are ONE vocabulary carrying a declared KIND per member — `closure` (the fate ends or diminishes the institution) or `rise` (`founded_by_flourishing`, `upgraded_by_reconstruction`) — declared as DATA in this packet and read by nobody yet, so the packet stays golden-neutral (it freezes, it does not cure); (4) it measures the SECOND vocabulary that shares this alphabet — `institutionHistory[].fate`, written from the same variable, with `LIFECYCLE_CLOSE_FATES` feeding a damping counter — and derives it from the closure kind if that is byte-identical, or declares the relationship if not.
- **A LIVE DEFECT THE COMPILE FOUND (pre-existing; inert in no way):** both upswing fates are stamped on STANDING institutions, and `causeLifecycle.institutionDestroyed` tests the fate's TRUTHINESS — so a flourishing academy reads as DESTROYED and its criminal leash severs. FATE: **EM-B1i** (NEW; train EM-T6, right after EM-B1h, before EM-B1a): that reader tests the `closure` KIND EM-B1h declares; measured first over the pulse goldens and the preset witness — if a golden moves, it is a lived-behaviour change and goes through the OWNER's signed door with the before/after in plain words.
- Fates of its other noticed items: `institutionLifecycle.js` stands at 798 of its 800-line ceiling and is missing from the standing hot-file list — SLOT: the chair adds the row at its next sitting on the branch, and EM-B1a's pre-proof (the next packet likely to reach into it) prices the extraction; the compile brief contradicts the preamble on the census — CORRECTED in the kit the same turn (the preamble is right); design §15's stale sentence about `EntityStatus` — CORRECTED in this commit; production saves cannot be measured from the repo — CLOSED: the guard validates a WRITE, never a read, so an old save's value is never refused; the soak script's replicated fate — CLOSED: it is a member; TOOL-3's first per-train attribution names EM-B1e's +437 B and this packet's bound of 739 B.

## Amendments of 2026-09-19 19:15 EDT — EM-P1 WITHDRAWN; EM-R6 MOVES UP; EM-P1d awaits the owner (design §22.4; ODQ §934.59)

- **EM-P1 (stable identity) is WITHDRAWN** — RECON-ID measured that the smallest stable identity contains no new identity (design §22.4). Its kit drafts are superseded; nothing was ever placed in the tree. EM-P1b stands as re-cut (the institution anchor; the NPC id).
- **EM-R6 (the institution rename + removal cascade) MOVES UP: it lands BEFORE EM-B1c** (whose institution-rename op has no cascade to call) and before the first apply point. Its compile is running from the recon's census (42 paths: 33 strict, 9 polysemous each put to the chair; `factionRoles.js`'s `linkedInstitutionIds`; the member mirror; removal semantics per row; the dangling gate a DELTA by kind; an anchor section correct under both owner answers). If it exceeds the budget it splits at its natural seam — RENAME first (EM-B1c waits on it), REMOVAL second (the removal op and the merge wait on it).
- **EM-P1d (NEW, CONDITIONAL ON THE OWNER): every institution is stamped with its catalog id after the last roster mutation** — the stamp runs at step 6 and 2,083 of 17,361 institutions are added at steps 8 and 15; all 66 of their names resolve in the catalog. It writes one persisted key on those institutions and moves 417 of 525 generator-golden rows: the OWNER's signature, its own record. Placed (if YES) beside EM-R6, before EM-B1c.
- **Fates of RECON-ID's noticed items:** the wrong mint site and the ~25 `id || name` ledger keys — CLOSED with the decision not to mint; the three dark id-only readers — CLOSED: they stay dark, as their own headers say is correct; the NPC id family enumerated in one place — DONE in design §22.4; `relationships[].npc1Id/npc2Id` as a zero-dangle join the NPC rename lane does not use — SLOT: EM-B1c's pre-proof measures whether to adopt it; `conflicts[]` carry no id and no exact faction name — SLOT: a row on EM-C4a's card list at its compile; `powerStructure.factions[].name` never exists on generated data — CLOSED (the cascade row is correct as written); ⚠ `settlement.id` is a hash of the SEED alone — 516 of 525 corpus rows share one id, and it crosses the public veil — SLOT: REVIEW-P (the public-path review, dispatched the moment run 17 exits) measures who keys on it (gallery rows, realm placement, forks) and whether two published worlds grown from one seed can collide.

## Amendments of 2026-09-19 19:15 EDT -- EM-R0a VERSION 2 ACCEPTED (design 22.3 applied and measured)

The class register's second version is READY-able (kit `packets-waiting/EM-R0a.*`): the generation receipt is classed BY THE INPUT EACH SUB-PATH READS -- only `repairs` reads the repair log and is HISTORY; `checks[]`, `judgments[]` with their evidence AND `authoredTensions` (measured a function of the settlement -- the chair's example list was wrong on that one member, and the ruling's own test governs) are READINGS, so all five evidence invariants stay live; the receipt is MERGED, NEVER RECOMPUTED (a recompute moves one judgment's evidence on 63 of 63 records because the receipt is built before the trace is propagated); 47 keys classed including four SAVED-ONLY keys (`neighbourNetwork`, `interSettlementRelationships`, `crossSettlementConflicts` HELD; `populationHistory` HISTORY -- the class now reads 'the record's own account of what has happened', so lived history stays uneditable); the save -> load round trip adds no key and moves no leaf (63 of 63); KEYED 48 / ATOMIC 15 (`incomeSources` and the faction power total are cross-entry totals; two defence candidates were REFUTED by a relative tolerance and ride as the walker's paired negative); six groups, the per-entry group needing no extension. FATES of what it noticed: the receipt-recompute finding and the ORDER-BEARING history collections -> EM-R0c's compile; the malformed-repair seam -> EM-B1a's pre-proof; `crossSettlementConflicts` (written by nothing in `src/`) -> CLOSED as a declared-ahead key whose absence the walker asserts; `resourceAnalysis.gaps` carrying several rows per chain -> THE OWNER'S DECISION POINT at the moment the gaps card is designed. It is placed when no non-terminal packet reserves the mutation register.

## Amendments of 2026-09-19 19:16 EDT -- two items from EM-B1d's build lane, slotted the same turn: CURE-C and TOOL-5

- **CURE-C (NEW; the same cure sitting as CURE-A and CURE-B, before the lighting refreeze and run 17):** measured whole at `ad7ddf2c9` the live lighting tuple is `2649 . 384 . 2265 . 25015 . 6675` -- EM-B1e's landed `tests/domain/ruinInstitution.test.js` is PARKED, not credited (its pre-proof predicted credited, seven titles): a parked file's pins prove nothing to the lighting contract. The cure lane finds the park reason with the walker's own `parkReasonsFor` and makes the file's registration statically provable (straight-line literal `it`s), so it is CREDITED before the chair refreezes; a refreeze must never bank a parked file a packet promised credited.
- **TOOL-5 (NEW; the tooling window):** `scripts/build-edge-shared.mjs` resolves through a symlinked `node_modules` and hashes the resulting `../<other worktree>/node_modules/...` path STRING into `sourceHash` -- the freshness suites pass and only the committed-tree pin convicts it (it stopped EM-B1d's build in a slot the chair had built with a symlink; cured there with a real install). The builder sets esbuild's `preserveSymlinks` (or normalises recorded inputs to repo-relative paths) so a worktree's install can never poison an artifact; measured first: the five committed bundles must be byte-identical before and after in a real-install tree.

## Amendments of 2026-09-19 19:29 EDT -- EM-R0d's compile ruled (ODQ 934.47 addendum 29)

EM-R0d compiled READY-able: the three ladders CONFIRMED by execution at every boundary (legitimacy 75/60/45/30 closed below, exported but under `src/generators`; readiness 76/55/38/24/12 closed below, module-local; food security 40/15/5 on the deficit and 40 on the surplus, OPEN below, reading THREE inputs and exported by nothing). Placement: values in `src/data/` (eager first paint +0; it also SHRINKS the tuning register and takes 678 B OUT of the lazy engine chunk). Price: about +1,657 B estimated into the ZERO-SLACK generation worker (the same method overstated EM-P3 by 2.6 times), bound 3,314 B. RULED: (1) the NAMED frozen table, not positional tuples -- about 600 B is not a reason to choose the form nobody can read, and the rise is re-minted with per-module attribution inside the bound and recorded as a named, vetoable figure (the ODQ 934.19 family); (2) NO `src/domain/` re-export -- ninety-six domain files already import `src/data` directly, and `recordInvariants` does the same; (3) the four SOURCE-TEXT anchors that retire (three test files reading the producers' source for thresholds and labels) are declared TEST rows cured to read the leaf, as the lane drafted; (4) its second version is a RESUME of the same lane, queued behind the composition train's needs. FOR EM-R0b VERSION 2: the food-security band check takes the record's ROUNDED inputs, so it accepts a label that ANY unrounded value within half a unit of the published one would yield (the label is computed before rounding; an exact check would convict an honest record at a boundary); it measures which producer's label lands on `defenseProfile.readiness.label` (`rulingStructure.js` assigns five readiness labels from institutions with no score). FATES of what the lane noticed: the legitimacy ladder's two play-time copies (`rulingPower`, `timeProgression`) read the leaf -> **EM-R0e** (NEW; after EM-R0d; measured against the pulse goldens; about 484 B back); the Compendium spells all three ladders' cuts in prose derived from nothing and documents no rungs for overall readiness -> **FIX-C1** (NEW; a parallel lane after EM-R0d lands: the Compendium's threshold prose is generated from the leaf); the food card's FLAGS cut at 20 where its LABEL cuts at 15 (every record between carries `Import-Dependent` beside `isPressured`) and a third chain cuts at 40/20/8 -> THE OWNER'S DECISION POINT, asked with the food card's other two questions (ODQ 934.57); `Vulnerable` and `Contested` each being two ladders' words -> CLOSED (homonyms, named in the leaf's header); the charter's newest rows living only on the ledger -> the FIFTH DOCS FOLD at the chair's next sitting on the branch.

## Amendments of 2026-09-19 19:35 EDT — EM-B1d version 5 lands under two RULED EXCEPTIONS; two more reds on the tip slotted; EM-B1h version 2 accepted; EM-P2's one question ruled (ODQ §934.47 addendum 30)

- **EM-B1d VERSION 5 was built complete and green and STOPPED one step short of its commit on two wrong FIGURES in the packet itself** (both outside its sixteen paths). RULED, so that a third build round does not re-prove what is proved: (1) the lighting delta is `+1 / +0 / +1 / +4 / +1`, not `+5` titles — `tests/generators/densityLaw.test.js` uses vitest GLOBALS, the census parks it, and a new `it` there counts nowhere; the packet's figure is corrected at the LANDED flip. (2) `check:packet` cannot reach its last five steps because the packet's `checks` run `build:edge-shared` MID-CHAIN, which re-stamps declared paths and the gate correctly invalidates everything after it; the landing proceeds on steps 1–8 executed by the gate, steps 9–13 executed BY HAND at the same tree (all green, quoted in the commit), and the chair's own re-run of those five at its validation; the `checks` order is corrected at the flip. *Rejected:* amending the READY packet (it kills the seal and forces a full re-build for two metadata errors). *Reversal:* none needed — no code is affected. THE CLASS IS CLOSED AT PRE-PROOF: step 14 (a generator among `checks` goes LAST; a title is worth a title only in a CREDITED file) and step 15 (a file stamped by the prose-wiring census owes the plain re-take).
- **TWO MORE REDS ALREADY ON THE INTEGRATION TIP, found by that build's whole-directory lint run — both would have failed run 17:** the prose-wiring census is stale for `resolveConfig.js` (EM-P3 edited a stamped file and carried no re-take) → **CURE-D** (a stamp-only plain re-take, lawful by the standing 2026-09-08 ruling; a moved ROW is the chair's); and EM-B1e's test file landed PARKED → **CURE-C** (already slotted). The terminal's cure sitting is now CURE-A · CURE-B · CURE-C · CURE-D, one lane, four commits, before the ONE refreeze and run 17.
- **FIX-L1 (NEW; a parallel lane):** `densityLaw.test.js` carries 112 `it`s that have never counted as evidence because the file uses vitest globals; the census parks 384 files. Measured first: how many are parked for an unresolved OPENER ALONE (one import line cures each); the lane credits that population, and the census moves DELIBERATELY at its composition train's refreeze.
- **EM-B1h VERSION 2 ACCEPTED (kit `packets-waiting/EM-B1h.*`):** THREE kinds, derived from what each writer does to the record — `closure` 15 (every one sets `_worldPulseInactive` and a non-active status, including the six gently-named demotion fates), `standing` 1 (`demoted_by_disaster`: renamed in place, no status, no flag), `rise` 2; the kind map is the single authored source (structurally total, 910 B); the guard lives in the leaf (`assertWorldPulseFate`, 98 B and two kernel lines); a fifth change row — the coupling-inclusion census is total over `src/domain/worldPulse` and the leaf takes an `ARGUED_UNLAYERED` row with the ceiling 28 → 29. **EM-B1i's population is THREE, not two** (a demoted wizard's tower joins the flourishing academy and the reconstructed guild in reading as destroyed) — its row is widened. `LIFECYCLE_CLOSE_FATES` is 3 of the 15 closure fates (only the ECONOMIC closures damp; abolition, demotion, disaster and settlement death do not) — NOT derived from the kind here (it would be a lived-behaviour change, 3 counted words → 15): EM-B1i's compile measures whether that is deliberate and puts it to the chair. **EM-B1j (NEW; after EM-B1h, golden-neutral):** `institutionHistory[].fate` is a DIFFERENT, overlapping open vocabulary (17 values: 12 shared, 5 its own) and owes the same closure.
- **EM-P2 VERSION 3's PRE-PROOF re-measured everything green** (the 75 rows byte-identical at the tip; the instrument's control with its negative control) and caught that the mutation register owes TWO rows, not one (both test directories are enforcers) — version 3 would have red at the landing. Its one block, RULED: nine acceptance cases against the validator's cap of eight → `A3s` folds into `A3` as its static half, no assertion dropped, the title delta reconciled to one derivable figure. It is placed the moment EM-B1d is LANDED.

## Amendments of 2026-09-19 19:42 EDT -- THE SIXTH LANDING; train EM-T3's members are all in; EM-B3c placed; the cure lane is in the slot (ODQ 934.47 addendum 31)

- **EM-B1d VERSION 5 LANDED** at `95e494bdb` (flip `1feb5c9ba`, with its two figures corrected); the fifth docs fold `63de6da75`; **EM-B3c VERSION 2 PLACED READY** at `e5bdfd031` (the three-ledger security gap and migration 203; the chair re-shaped its bare-string acceptance cases to `{id, case}` -- the second manifest today to fail the validator on that, so the compile brief's next revision says it). TRAIN EM-T3's MEMBERS ARE ALL LANDED: EM-P3, EM-B3a, EM-B1e, EM-B1d. Its close is running: ONE cure lane in the slot (CURE-A the walker's regex, CURE-B the prose-numerics addresses, CURE-C EM-B1e's parked test file, CURE-D the prose-wiring census) -> the chair's ONE lighting refreeze -> run 17. EM-B3c builds in the slot DURING run 17 (its gated batches wait for the terminal to release the gate).
- **EM-B1f goes to VERSION 2** at the new tip (a resume of its compile lane): `OFF_STAGE_STATUSES` derived from the landed `NPC_UNAVAILABLE_STATUSES` minus `dead`; SEVEN generated paths; and the measurement that gates its promotion -- whether any IRREVERSIBLE consumer (house dissolution above all) reads the FILTERED participation view, which would let a reversible absence dissolve a house through the filter; the same question is asked of today's shelved and hostage members first.
- **TOOL-2b is built and waits for the gate** (the guard now takes the corpus's STRICTEST dist predicate, `dist/index.html` + `dist/assets`; its tests have their own file; a fourth mutant SURVIVED its first run and exposed two assertions passing for the wrong reason -- cured). FATES of what it noticed: `tests/build/realmJourneyAsset.test.js` gates on a SOURCE asset, so the guard's advice to build would not help it -> SLOT: TOOL-4 moves it out of `tests/build/`; the loosest and the ENOENT-by-accident dist gates in two build tests, and `bootSmoke`'s own dist variable -> CLOSED: the guard refuses a half-built tree before either can run in a focused run, `verify:dist` runs on a complete build, and the refusal names the opt-out; the ratchet test file still 3,000 lines -> CLOSED (lawful; no ceiling); the four mutants living only in scratch -> CLOSED: a `rationale` row carrying the executed account is the estate's accepted form.

## Amendments of 2026-09-19 19:46 EDT -- A RETRACTION: the cause the chair recorded for the parked test files was WRONG (ODQ 934.47 addendum 32)

The amendments of 19:1x-19:4x (addendum 30 and the sections naming CURE-C and FIX-L1), the landed flip note of EM-B1d (`1feb5c9ba`) and the pre-proof brief's step 14 all say that `tests/generators/densityLaw.test.js` and EM-B1e's `tests/domain/ruinInstitution.test.js` are parked by the lighting census because they 'use vitest GLOBALS' (`OPENER_UNRESOLVED`). THAT CAUSE IS FALSE: both files import their opener from `'vitest'`, and 0 of the estate's 2,649 test files lack that import (executed by the chair at `e5bdfd031`, after EM-P2's pre-proof lane refuted it). The chair relayed a build lane's unverified guess as fact. WHAT STANDS, because it was measured: the live tuple `2649 . 384 . 2265 . 25015 . 6675` (one of the three test files landed since the freeze IS parked) and EM-B1d's delta of `+4` titles (its new `it` in `densityLaw.test.js` counted nowhere, so that file is parked for SOME reason). CORRECTED: CURE-C now MEASURES which new file is parked and why, with the walker's own `parkReasonsFor`, and cures that file's registration without changing an assertion; **FIX-L1's premise is withdrawn and re-written**: it first takes the park-REASON histogram of all parked files and proposes the cheapest class to credit -- no population is assumed; the pre-proof brief's step 14 is re-worded (the rule stands, the guessed cause is struck); EM-B1d's flip note is corrected at the chair's next sitting on the branch. The lesson, which is the estate's own first law: a lane's stated CAUSE is a claim until a command is quoted beside it -- the chair checks a cause before it writes one into a ledger.

## Amendments of 2026-09-19 19:48 EDT -- EM-P2 VERSION 4 accepted; EM-R6's compile ruled; TOOL-3 dispatched

- **EM-P2 VERSION 4 is READY-able with no open question** (kit `packets-waiting/EM-P2.*`): the 75 rows byte-identical at the tip; eight acceptance cases (`A3s` folded into `A3`); the mutation register owes TWO rows (705 -> 707), anchored by key names disjoint from EM-B1d's and EM-B3c's; its lighting delta is `files +2 / credited +2 / suiteTitles +2 / titles +10` (the census counts `it` titles only -- the lane corrected its own `+12`). It is placed at the chair's next sitting on the branch and built in the composition train after EM-B3c. THE TWO REGISTERS, ruled from its measurement: EM-P2's Tier 1 classifies `(step, key)` by HOW a value comes to be; EM-R0a classifies record PATHS by what the EDITOR MAY DO with them -- disjoint vocabularies meeting only at `recordPath`; eighteen keys look like disagreements and none is (`powerStructure` is HELD yet `pure`: an entropy fact, not a permission). EM-R0a's draft sentence that the repair log 'never reaches the record' is amended at its promotion (the copy reaches `generationCoherenceReceipt.repairs` in 20 of 63 rows).
- **EM-R6's compile is READY-able and ruled** (its second version is a resume of the same lane): ONE packet, TWO leaves (`institutionRename.js` 209 effective lines, `institutionRemoval.js` 57); 33 strict cascade rows plus THREE polysemous paths cascaded under an exact-match guard (the quarter landmarks; `secondaryAffiliation` in both homes, with a walker arm asserting the institution and faction match sets stay disjoint; a supply chain's label where it IS its processor's name); six stay non-cascaded with measured reasons; the cascade writes BOTH NPC homes itself (the mirror recompute never runs for a rename: one-home 44 of 63 red, two-home 0 of 63); a removal REPORTS the 47-chain class that loses its last processor as declared `orphaned` entries -- the guard engine judges, the writer cannot refuse; executed: 474 handles, 0 stale, 0 new dangling joins. The dynamic-import obligation falls on its three consumers' pre-proofs. FATES of what it noticed: **FIX-D1** (NEW; a parallel DATA-QUALITY lane: the pre-existing dangling joins a generated town already carries -- median 17, five paths -- and the `Thieves' Guild` / `Thieves' guild` case near-miss, 88 occurrences; measured first; a cure that moves a golden goes through the owner's door); the standing hot-file list is stale (`convergence.js` recorded 798, measures 764; `institutionLifecycle.js` at 798 of 800 is missing) -> the chair's next sitting on the branch; a structured-sample test helper three lanes have each rebuilt -> TOOL-4.
- **TOOL-3 is dispatched** in its own worktree with a REAL install (it builds): a census of every emitted worker and every unguarded chunk, then measured ceilings with a declared 4 KB headroom for the pulse worker and the scene-export worker.

## Amendments of 2026-09-19 19:59 EDT -- the parked-file cause MEASURED; CURE-A2 ruled in; FIX-L1's premise is now a measurement

The cure lane measured past both wrong causes (the build lane's and the chair's correction): EM-B1e's test file parks as `OPENER_UNRESOLVED:it` because `it` is bound a SECOND time -- an arrow parameter named `it`, copied from an existing suite -- and the census resolves an opener only where the module binds it exactly once. CURE-C is the parameter rename (no assertion moves; `parked -1, credited +1, titles +7`). **CURE-A2 is ruled in:** one sibling, `tests/lint/strategyMoveVocabulary.walker.test.js`, carries a verbatim copy of the newline-eating mask and takes the same cure under the same proof; a stale comment in `lawBandTable.walker.test.js` that dockets the defect is corrected with it. CURE-B re-addresses TWO rows (EM-B3a's and one of EM-B1e's the chair's list had missed); CURE-D is a stamp-only re-take. The tuple the chair refreezes to is the cure lane's measured one (expected `2650 . 383 . 2267 . 25027 . 6677`). **FIX-L1's premise, now measured:** 26 of the 384 parked files park ONLY on an unresolved opener, all by a second binding of `it` or `test` (roster in the cure lane's evidence); the cure is a rename per file, no assertion touched; `tests/generators/densityLaw.test.js` is NOT in that class (it parks on unregistered and non-straight-line registration: 11 and 3 reasons) and is out of FIX-L1's scope.

## Amendments of 2026-09-19 20:03 EDT -- TWO LIVE SIMULATION DEFECTS found by EM-B1f's compile: EM-B1k chartered; EM-R6 version 2 accepted (ODQ 934.60)

- **EM-B1f VERSION 2 IS BLOCKED on EM-B1k, and what blocks it is live today.** Its owed measurement came back positive: the pulse builds every update from the FILTERED participation view (`isOffStage` people removed), the faction-density kernel reads THAT view, and an empty roster dissolves a house PERMANENTLY -- reproduced through the shipped `simulateCampaignWorldPulse({commit:true})`: a house whose SOLE member is SHELVED (stasis) or a roads HOSTAGE is dissolved with a `faction_dissolved` beat, while the same house with an active member survives; the confirmation read written to stop exactly such a stale irreversible act reads the same filtered view. EM-B1f's arm would put `jailed` -- a reversible status -- onto that same row. A SECOND, density-independent finding is being settled NOW by the same lane (one hop remained): the tick's WRITTEN settlement lacks the off-stage person and the commit helper replaces the settlement wholesale -- if no layer re-merges the raw roster before the save is written, a shelved NPC is ERASED FROM THE SAVE by advancing time.
- **EM-B1k (NEW; BEFORE EM-B1f in train EM-T6): nothing permanent is decided from, and nothing is persisted out of, the participation view.** Compiled from the lane's RECON-STAGE report (the consumer table: what each reader of the filtered view writes, reversible or not; the population at risk; the two candidate cures priced -- irreversible consumers and the write-back read the RAW roster, or the filter keeps reversible absences in the record under a flag; the participation ratchet widened to `src/domain/density/`, where this read hid). It changes lived simulation behaviour where the bug fires: if a pulse golden or the preset witness moves it goes through the OWNER's signed door. EM-B1f lands AFTER it with all three members; it does NOT narrow.
- **EM-R6 VERSION 2 ACCEPTED (kit `packets-waiting/EM-R6.*`):** 38 declared cascade rows (37 observed paths), five non-cascaded with measured reasons; executed over all 525 rows: 4,227 handles, 0 stale on any declared row, 0 new dangling joins on rename or removal; the institution and faction match sets of the shared `secondaryAffiliation` field are disjoint (1,828 values, zero both) and a walker arm keeps them so; `OrphanKind` is a closed vocabulary of TWO -- `chain-lost-its-last-processor` (304 over the corpus) and `chain-label-names-a-removed-house` (7) -- both ACCEPTED: the writer reports, the guard engine judges; the never-observed `linkedInstitutionIds` member row is accepted as a symmetry declaration (the faction cascade's precedent). 284 effective lines in two leaves. Its anchor section stays conditional on the owner's answer (ODQ 934.59).
- **FIX-D1's measurement is dispatched** (read-only): the 7,399 dangling institution references a generated town already carries (five paths) and the `Thieves' Guild` / `Thieves' guild` near-miss -- classified into real stale references, catalogue lists mis-read as references, and near-miss joins, with each cure's golden price.

## Amendments of 2026-09-19 20:10 EDT -- DATA LOSS CONFIRMED: EM-B1k FLOATS WITH TOP PRIORITY (ODQ 934.60 addendum)

The last hop was taken and **the second defect is CONFIRMED: shelving an NPC and advancing one tick ERASES that person from the save, irreversibly.** Reproduced with everything real but the one Vite-env module: a generated town of seven named NPCs; the DM shelves one through the app's own writer (`applyNpcOp('stasis-npc')` -- the shipped Availability select); one tick with `commit: true` through the shipped entry and the real store commit: the roster the save would hold is SIX of seven, in the store AND the database payload; `return-npc` then answers `npc_target_missing` -- only a whole-tick undo brings the person back. There is no re-merge anywhere: the update's settlement is BORN from the filtered participation view (`buildSettlementMap`) and `campaignPulseHelpers.js:197` lands it wholesale. The roads HOSTAGE path is the same, and `roadsEnabled` is lit in all four presets including the default. Population at risk for the dissolution face: 1,608 of 3,378 factions (47.6 %) hold exactly one rostered member, in 100 % of settlements.
- **THE CURE IS RULED: the participation view is a READ PROJECTION and must never be the WRITE BASE.** Every settlement an update writes back is born from the RAW roster (already on the snapshot item), a mover's legitimate roster writes merged onto it by id; every irreversible consumer (the faction-density kernel's lifecycle read and its confirmation; the two currently-unreachable permanent roster writers, cured with the class) reads RAW; the participation ratchet widens to `src/domain/density`. The alternative (keeping reversible absences in the view under a flag) is REFUSED: it re-opens the chokepoint the roads design closed. Golden-neutral by measurement (no fixture carries `stasis` or `whereabouts`; generation writes neither).
- **EM-B1k is compiling NOW and FLOATS WITH TOP PRIORITY:** it builds in the slot ahead of EM-B3c and everything else waiting, its red-first proof is the DM's own reproduction, and it rides the next full check. If it breaks the budget it splits with THE WRITE BASE (the data loss) first and alone.
- **What cannot be undone:** a person already erased from a save is gone from it; EM-B1k's compile reports what, if anything, still holds them (the tick's undo history, relationship edges, member chips) so the owner can decide whether a repair is worth building. **The owner's hand:** the fix reaches players only through the push and deploy, which stay the owner's -- this finding, with the three-ledger security gap, is the case for resuming the push chain at the next green full check.

## Amendments of 2026-09-19 20:13 EDT -- four cures landed; CURE-D widened to the file its red lives in; the cure lane's noticed items fated

CURE-A `08cb42e31` (the model walker's mask: findings identical under both forms over 1,057 files, while the old form destroyed 26,345 newlines across 700 of them), CURE-A2 `2d72c8b7a` (the move-vocabulary walker's verbatim twin, same proof over 2,246 files; +1 title), CURE-B `8955b67a8` (two prose-numerics rows, addresses only) and CURE-C `145acdb75` (the acceptance file's shadowing parameter renamed; credited, +7 titles, no assertion moved) are on the branch. **CURE-D stopped correctly and is WIDENED by the chair:** the residual red is not in the census JSON but in three `cite:` ADDRESSES in `src/domain/prose/holderTable.js` that EM-P3's three inserted lines left stale (`resolveConfig.js` :195/:198/:203 -> :198/:201/:206); the cure is addresses only plus a stamp-only census re-take, in ONE commit; the pre-proof brief gains STEP 16 (citations by line). The tuple the chair refreezes to, measured out of tree: `2650 . 383 . 2267 . 25028 . 6677`. FATES of what the lane noticed: the 26-file roster parked by a second binding of `it`/`test` -> **FIX-L1** (premise now measured; `tests/domain/calamity.kernel.integration.test.js`, the origin of the copied driver, is in it); `tests/generators/densityLaw.test.js` (11 unregistered + 3 non-straight-line reasons) -> **FIX-L2** (NEW; a parallel lane: its 112 `it`s made statically registered WITHOUT changing an assertion, measured first for what the restructuring costs); `tests/lint/composeStateProseFence.test.js`'s own comment-collapsing mask (latent: it prints no line addresses) -> **CURE-A3**, carried by FIX-L1's lane as its first commit; the stale `namesAScalar` citation -> FIX-F2b (already staged there).

## Amendments of 2026-09-19 20:16 EDT -- EM-R0d and EM-R0b second versions ACCEPTED; EM-R0c's compile dispatched

- **EM-R0d VERSION 2 ACCEPTED** (kit `packets-waiting/EM-R0d.*`): ONE leaf in `src/data/` (no domain re-export; first paint and eager set +0; the lazy engine 716 B SMALLER; the generation worker about +1,801 B estimated, the chair's named bound of 3,314 B kept); FOUR named tables -- legitimacy, readiness, food security, and the food card's FLAG ladder as its own table (its `pressured` and `surplus` cuts REFERENCE the label ladder's; only `deficit: 20` is its own), two ladders on purpose until the owner rules (ODQ 934.57), with an acceptance arm that PINS the known disagreement so that ruling must pass through a named red; `prosperityMod` measured a CHOOSER over the same inputs, not a ladder -- not moved, and pinned untouched; none of its producers is census-stamped or line-cited.
- **EM-R0b VERSION 2 ACCEPTED** (kit `packets-waiting/EM-R0b.*`): 30 checks, each with an isolating mutant; ALL FOUR band checks read the producers' ladders by import and every learned envelope is deleted; the five evidence checks live with no exemption (the receipt split by path); the food-security band check tolerates the record's rounding (proved at both cuts: an exact check convicts an honest 15.4 published as 15; a wrong label still reds) -- and the same hazard explains the flags: 24 of 525 honest rows publish a deficit of 20 beside `isDeficit` because the flag ladder read the unrounded value, which is why the flag table is DECLARED data taught on all 525 rows and never derived from the published number; `readiness.label` equals the ladder on 525 of 525 (the score-less labels in `rulingStructure.js` are a fallback INPUT to legitimacy, never the record's label). FATE of what it found: that fallback is a SIXTH undeclared spelling of the readiness vocabulary and omits `Fortress` -> **EM-R0e** carries it with the legitimacy ladder's two play-time copies.
- **EM-R0c (THE MERGE) is compiling** from design 22.1-22.4, the executed prototype and the three accepted siblings, with one contract added tonight from the pulse's data-loss defect: `record'` is built FROM THE RECORD, and nothing present in the record and untouched by the edit may be absent from it.

## Amendments of 2026-09-19 20:34 EDT — the cure lane CLOSED at `32602dc60`; EM-B1k's compile RULED (split confirmed, version 2 owed); FIX-D1 RULED — a name in the record is one of FOUR KINDS; EM-R6 version 3; two more golden questions are the owner's (ODQ §934.60 addendum 2, §934.61, §934.47 addendum 33)

- **THE CURE LANE IS CLOSED: CURE-A `08cb42e31` · CURE-A2 `2d72c8b7a` · CURE-B `8955b67a8` · CURE-C `145acdb75` · CURE-D `32602dc60`**, each naming only its own paths, `tests/lint` WHOLE at the tip `1 failed | 171 passed` with the one red the lighting census (`expected 2650 to be 2646`) — the chair's refreeze, to the measured tuple **`2650 · 383 · 2267 · 25028 · 6677`**; both goldens byte-identical; no rebuild owed (`holderTable.js` rides in no product chunk and no edge-shared bundle — measured). Its four noticed items already hold fates: the 26-file second-binding roster → FIX-L1; `densityLaw.test.js` → FIX-L2; `composeStateProseFence.test.js`'s collapsing mask → CURE-A3; `proseNumerics.test.js:464`'s stale citation → FIX-F2b.
- **EM-B1k (the participation view is never the write base) — COMPILED, RULED, VERSION 2 OWED BEFORE PLACEMENT.** Measured by its compile: changing ONLY the update's settlement base to the raw save roster stops BOTH defects (the erased person AND the dissolved sole-member house — the density kernel's own `stillEmpty` guard was right and failed only because its `fresh` reading was filtered too); `pulseKernel.js` sits at **1581 / 1581 effective lines, zero headroom**, so the cure is contracted as two single-line replacements, net zero. **RULED:** (Q1) the SPLIT IS CONFIRMED — EM-B1k is the write-base cure alone; **EM-B1k2** (NEW; same train, immediately behind): the irreversible consumers read RAW explicitly (`factionDensityKernel`'s `tickStart`, `settlementLifecycleFirstClass:611`, `successorNpc:66` — cured before either is lit; `successorNpc.js` is in the eager first-paint graph and its delta is priced), the off-stage ratchet widens to `src/domain/density` (it convicts exactly one new file, `factionLifecycle.js`), and `roadsKernel`'s `fullRoster` comment is re-pointed. (Q2) the risk is PROVEN, NOT REMOVED — the alternative (keep the base filtered, merge by id at the write-back) leaves the dissolution live — but the proof must be TOTAL, and version 1's was not: the chair read the seam and found (i) NO CENSUS OF READERS of the base the cure changes (the evidence tabulates the eight roster WRITERS only; every reader of the settlement map's base and of `localSettlements` now sees off-stage people) — version 2 enumerates every such site and classes it (participation predicate · id-keyed mark · count · unguarded selection), and any unguarded selection or moved count is a STOP; (ii) the seam has a SECOND HALF nobody had written down — `postTimeSaves` rebuilds the snapshot from `localSettlements`, so today the raw roster is gone from the tick at the clock stage, not only at the final write; (iii) the new overwrite line is right ONLY BECAUSE the settlement-clock chain (`advanceTime` → `advanceFoodStockpile` → `applyBlockadeTransportImpairment` → `advanceTreasury`) never writes the roster — the day one does, the line drops that write silently for every settlement, which is exactly the owner's most-bitten bug class; a NEW eighth acceptance arm pins that premise BY EXECUTION (the chain returns the roster by reference over corpus towns, treasury dark and lit), its failure message naming the line; (iv) the participation arm gains THE FROZEN CLAUSE — a shelved person's persisted record is deep-equal across the tick. *Rejected:* placing version 1 tonight (it would have landed the estate's most important cure on a proof that names four readers out of an uncounted population). *Reversal:* none needed — nothing was placed. (Q3) `tests/store` joining `ENFORCER_DIRS` → **TOOL-6 (NEW; measured first — the directory's population and what each file owes; a parallel lane in the next composition train)**. (Q4) a repair for saves already damaged → AN OWNER'S DECISION POINT, asked now (ODQ §934.60 addendum 2): the erased person's RECORD is unrecoverable; only the name survives (twelve dangling relationship edges, a faction mention, a rendered pressure sentence).
- **FIX-D1 (the 7,399 "stale" institution references) — MEASURED, RULED (ODQ §934.61): ZERO of the 7,399 are stale references.** A string equal to an institution's name is one of FOUR KINDS, and the cascade (EM-R6) treats each differently: **a REFERENCE** (follows a rename and a removal, exact and NON-case-folded — `secondaryAffiliation === "Thieves' Guild"` is a power-structure FACTION handle, and a case-folding cure would be a live bug); **a CATALOGUE VOCABULARY** (`resourceChains[]`, the three `exploitation` buckets, `gaps[].missing[]`, `upstreamMissing[]` — what a chain NEEDS, authored; never touched by either arm; 4,453 of the 7,399, 1,568 of them the same values counted twice); **a MATCHED PATTERN** (`activeChains[].processingInstitutions[]` — 2,525; NON_CASCADED until the owner signs FIX-D3); **a DERIVED DISPLAY LABEL** (`safetyProfile.criminalInstitutions[]` — 1,164 Title-Cased values on NEITHER of EM-R6's lists, the one real "spelled two ways"; follows a rename by a DECLARED case-folded match on that path only, until the owner signs FIX-D2). 421 more are parenthetical SENTINELS, never names. **EM-R6 VERSION 3 is compiling from these rulings** (its lane resumed), and its denominator pin gains a catalogue-name arm and a normalised-name arm — the exact-name walk was blind by construction to all four paths the measurement found.
- **NEW, EACH WITH ITS FATE:** **FIX-D2** (OWNER-SIGNED; the generator stores a criminal house's OWN name — 273 of 525 golden rows, 0 prose; unifies `corruption.js:596` and `safetyProfile.js:610`, which compute one fact twice in two spellings; retires EM-R6's `label` match kind) and **FIX-D3** (OWNER-SIGNED; the `Via` line names the house the town actually has — 382 golden rows, 1,306 DS-GEN-18 fills across 370 rows, 0 pool keys; carries the 12-character-prefix matcher's false-accept family, which is the engine of three reader-visible falsehoods: a city credited with a criminal power it lacks ×312, a thorp credited with a church it only has access to ×96, a house's scale mis-stated both ways) — both are OWNER'S DECISION POINTS asked now beside the catalogId re-stamp (EM-P1d), three signatures, one record each; if signed they build in the identity train before EM-B1c. *Rejected outright, with its reason recorded:* filtering `resourceChains`/exploitation to the roster (508 rows, and it destroys the one correct reader, the Resources tab's "Needs" list); carrying the matched name BESIDE the pattern (a new persisted key for a cosmetic). **FIX-D4** (SLOT: a parallel measure-first lane in the next composition train): 318 orphan `Thieves' Guild` affiliations (159 per home) in towns with no such faction and no thieves' house — `npcGenerator.js:1337`'s fallback; measured: what a DM sees, and the golden price of a guard. **FIX-D5** (SLOT, conditional): the twelve-spelling criminal-service sentinel vocabulary pinned with its live/dark split (3 live, 9 dark) — rides in EM-R6 version 3 as a test row if the budget holds, else its own lane. **CLOSED with reasons:** the twelve criminal sentinels are an honest register, no cure owed; `resourceChains[]` having no UI reader is recorded on its EM-R6 row; the corpus emits 231 of the catalogue's 280 institution names, and BOTH collision scans ran (231 emitted, 280 catalogue — both empty), so the cover is total. **A SHARPENER for the catalogId question (§934.59):** `institutionMatchesNativeName` is catalogId-first, so under a NO a renamed cascade-added criminal house silently VANISHES from the operations list rather than showing a stale name.

## Amendments of 2026-09-20 02:0x EDT — the successor chair resumes; the owner's four restatements; EM-B1k version 2 placed with the chair's cuts; the ONE refreeze; run 17; this sitting's emergent items fated (ODQ §934.62)

- **The owner, 2026-09-20 01:4x–02:0x:** the seat law (Fable architects, manages, judges, validates; Opus surveys, recons, verifies, implements) and the lane law (four working lanes + one gate seat; pause-and-resume; "if there is nothing to gate, then use that fourth lane until a gate is needed") restated; total delegation renewed and gate concurrency left to the chair; no deferred work restated. The chair's judgments (ODQ §934.62, vetoable): gates one at a time; the refreeze and the terminal are the chair's own seat; the push chain is the chair's one question (recommend: resume at the first green full check carrying EM-B1k + EM-B3c).
- **EM-B1k VERSION 2 PLACED READY** at `c740ded8b` (`1b381485f`) with the chair's cuts — the `:579` seam line in the IDENTITY-PRESERVING single-line form (the compile's addendum §16: both snapshot caches are `WeakMap`s keyed on the settlement object; an unconditional wrapper would miss them every tick), the acceptance count (8), the lighting delta (`+1 files / +0 parked / +1 credited / +6 titles / +1 suiteTitles`), two dangling section pointers, and (`07cc2efb6`) the §7 table made set-equal with its JSON (a measured-negative REGISTER row belongs in §7.1, never in the table — TOOL-1's arm reads it as drift). It builds in the slot now, FLOATING ahead of train EM-T4, its gated batches paused until run 17 exits.
- **THE ONE LIGHTING REFREEZE** landed at `1e5bcf83d` (`2650 · 383 · 2267 · 25028 · 6677`, through the walker's own door); **RUN 17 = train EM-T3's terminal** runs bare and detached at that tip from 02:01 EDT. The compile lanes EM-R6 v3, EM-R0c and EM-B1i are re-dispatched (their predecessors died with the previous session).

| item (where it surfaced) | fate |
|---|---|
| EM-B1k v2's five internal inconsistencies (the chair's validation at placement) | **CLOSED — cured at placement**, each cut recorded in the packet's Last-revalidated row |
| the three compile lanes that died with the previous session (EM-R6 v3, EM-R0c, EM-B1i) | **CLOSED — re-dispatched** on `read-tip-32602dc60`; partial scratch outputs are hints only |
| EM-P2 version 4 collides with EM-B3c on `scripts/mutation-coverage-manifest.json` (measured: one path) | SLOT: **placed at EM-B3c's LANDED flip** — the slot order B1k → B3c → P2 v4 already ruled |
| FIX-G's worktree carries ONE UNSTAGED hunk on `src/generators/economy/viability.js` beside its staged car (its agent died mid-edit ~20:37 on 09-19) | SLOT: **the FIX-G resume at the composition train** reads the hunk first and stages it or withdraws it with its reason, before its gated batch re-runs |
| a measured-negative `REGISTER` row written into a §7 change table (EM-B1k v2) | SLOT: **the pre-proof brief gains the sentence** (a measured negative lives in §7.1's ledger, never in the table) **at TOOL-1's composition**, in the same commit as the standard's anti-vacuity sentence |
| eslint's `Linter` here is flat-config only (`configType: 'flat'`; the eslintrc form throws) | **CLOSED** — a note in the kit's `tools/chair-scripts-a9df403c/README.md`; the build brief for EM-B1k carries the working snippet |
| EM-B1k2's compile has no launch file yet | SLOT: **the chair writes `briefs/launch/COMPILE-EM-B1k2.md`** (from §934.60 addendum 2 and B1k's §12.1) and dispatches it into the next free working seat |

## Amendments of 2026-09-20 02:25 EDT — EM-R6 VERSION 3 accepted with nine rulings; twelve items fated; version 3.1 queued; the delegation applied to the standing decision points (ODQ §934.47 addendum 34, §934.62 addendum)

- **EM-R6 version 3 is READY-able** (kit `packets-waiting/EM-R6.*`; version 2 superseded): the four kinds of §934.61 applied and measured over 525 rows; two new pin arms (catalogue-name, normalised-name) with a red control; five paths version 2 had missed, two reader-visible. The nine rulings are in ODQ §934.47 addendum 34; **version 3.1** (a resume of the same lane, `briefs/launch/RESUME-EM-R6-v31.md`) folds in FIX-D5, the `readable` flag on NON_CASCADED rows, and four small measured additions. **The charter's EM-R6 row gains two facts:** the corpus emits 231 of the catalogue's 280 institution names (every 'total over the corpus' is total over 231; both collision scans are empty), and FIX-D5 rides in version 3.1. **TOOL-4's row gains a second customer** (EM-R6 rebuilt the 63-row structured sample inline).
- **The delegation applied (§934.62 addendum):** decided NO on a repair for already-damaged saves; the golden signatures (FIX-D2, FIX-D3, EM-P1d) stay the owner's; §934.57 is decided by the chair at the composition train's sitting against EM-R0d's ladders.

| item (where it surfaced) | fate |
|---|---|
| `compound.inst`'s frozen flags under an editor REMOVAL (`hasCourtSystem` stays true after the courthouse is removed) | SLOT: **EM-R0c's compile** decides from EM-R0a's class of `compound.inst` — a READING is recomputed by the merge; a HELD/CONSTANT stamp owes a declared-falsehood row on **EM-C2**'s guard; the chair's decision point at EM-R0c's placement |
| the 12-character-prefix processor matcher (`computeActiveChains.js`) — three reader-visible falsehoods; FIX-D3 cures the symptom | SLOT: **FIX-D3's own packet** if the owner signs it, else **EM-P1b**'s class |
| `resourceChains[].processingInstitutions[]` has no UI reader (3,212 stored values) | SLOT: **the chair's next docs sitting** on the branch — one line in `docs/DEAD_CODE_DISPOSITION.md` |
| the 318 orphan `"Thieves' Guild"` affiliations (`npcGenerator.js:1337`) | **FIX-D4** (already slotted; measure-first, the next composition train) |
| A1's failure message for a `catalogId` path crossing the 50 % threshold; the stale 0-of-703 figure on `availableServices.legal[].name`; the aliased frozen stamp declared twice; the 231-of-280 coverage line | SLOT: **EM-R6 version 3.1** |
| EM-B1c and FIX-D3's deferral (175 stale `Via` values after a rename) | **RULED:** not a gate on EM-B1c; the price is declared by count in A2; FIX-D3's moment is before EM-B1c's promotion |

## Amendments of 2026-09-20 02:30 EDT — EM-R0c accepted; EM-R0f chartered; EM-R0b version 3; the EM-R family's order re-cut (ODQ §934.47 addendum 35)

**THE EM-R FAMILY, IN BUILD ORDER (supersedes the 17:25 and 18:30 orders):** **EM-R0a** (v2, accepted) · **EM-R0d** (v2, accepted) · **EM-R0b v3** (compiling: `CHECK_META` — each check's record keys, so a cross-key violation carries both; the two new MIXED objects measured) · **EM-R0f** (NEW: `fingerprintPowerEconomyInput` moves from `src/generators/` to `src/data/economyFingerprint.js`, golden-neutral by a byte-identical proof over 525 rows; the domain→generators boundary set shrinks; the boundary test's stale header corrected) · **EM-R0c** (accepted: the merge, 294 effective lines in two leaves; seam-free acceptance; depends on R0f and R0b v3) · R1–R5 · **EM-R6** (v3 accepted; v3.1 folding FIX-D5) · **EM-R7** (gains an explicit key-order arm). Rulings and fates in the addendum.

## Amendments of 2026-09-20 02:34 EDT — EM-B1i accepted (no golden door owed, by execution); TOOL-7 and FIX-C2 chartered; EM-B1j's compile gains two measurements (ODQ §934.47 addendum 36)

- **EM-B1i** (train EM-T6, after EM-B1h, before EM-B1a) is READY-able in the kit: the total kind→verdict map; +5 effective lines; the witness proved unmoved by three executions; sixteen standing academies per world stop reading as destroyed. It is placed after EM-B1h lands (their walker arm collides by design).
- **EM-B1j's compile carries two measurements:** the price of widening `LIFECYCLE_CLOSE_FATES` from three counted words to the fifteen closure fates (a mirror born 2026-06-10, never re-examined), and whether a rise or standing fate should age out — each a lived-behaviour change; a golden move is the owner's door.
- **TOOL-7 (NEW, tooling; measure-first):** a lint arm over `src/` for the `in` operator against a frozen literal object (it reads prototype members — `constructor`, `toString`, `valueOf`, `__proto__` — executed by EM-B1i's compile); the population is measured before the arm is written.
- **FIX-C2 (NEW, a parallel measure-first lane in the next composition train):** the estate's existing `<file>:<line>` citations in tests and docs swept against live line numbers (one was found stale by eleven lines); pre-proof step 16 guards packets, nothing guards what already exists.

## Amendments of 2026-09-20 02:46 EDT — EM-R6 version 3.1 accepted; the sentinel vocabulary is eleven (FIX-D5 +245 B); its pre-proof gains two arms; EM-R7 gains a `readable` source scan (ODQ §934.47 addendum 37)

- **EM-R6 version 3.1** (kit `packets-waiting/EM-R6.*`) folds the nine rulings of addendum 34 with a declared `rewrite` field and an import-time `readable` resolver; FIX-D5 rides (eleven sentinel spellings, 3 live / 8 dark; +5 effective lines, +245 B). **Its pre-proof adds:** a closed-vocabulary pin on `LABEL_REWRITE` (OrphanKind's shape) and a set-equality arm joining `servicesGenerator.js`'s sentinel literals to the domain's declared list. **EM-R7 adds** a `tests/lint` source-scan arm enforcing `readable` on both cascade ledgers. **EM-B1c's pre-proof** carries the faction-side `readable` typing.

## Amendments of 2026-09-20 02:47 EDT — EM-R0b version 3 accepted (`CHECK_META`; 33 checks; three duplicated-leaf checks); EM-R0a's pre-proof gains the `history` group; EM-R7 gains the duplicated-leaf sweep (ODQ §934.47 addendum 38)

- **EM-R0b v3** (kit `packets-waiting/EM-R0b.*`; v2 superseded): `CHECK_META` total over 33 checks with a violation carrying the keys of the arm that fired; EM-R0c's STOP-4 discharged. **EM-R0a's pre-proof** adds one consistency group rooted at `history` (prevention for `founding.age ≡ history.age`). **EM-R7** carries the exhaustive one-fact-two-paths sweep and records `readiness.score`'s unresolved relation. **EM-R0d's pre-proof** names `severityBands()` as `severityBand`'s plural sibling.

## Amendments of 2026-09-20 02:53 EDT — EM-B1k2 accepted (three premises refuted, the packet smaller); FIX-D4 ruled into one build lane under the owner's signature; EM-B1k3 conditional; TOOL-8 chartered (ODQ §934.47 addenda 39–40)

- **EM-B1k2** (kit `packets-waiting/EM-B1k2.*`; placed after EM-B1k lands): one logic-bearing line plus three comment-only contracts, the ratchet widened to `src/domain/density` (one new conviction) and, at its pre-proof, to `src/generators/density` (`titularSuccession.js`'s disposition); its pre-proof also runs the full-corpus display-name census (Q4 — a duplicate charters **EM-B1k3**, conditional), reads every save writer for the legacy shape (Q5), traces `priced.settlementUpdates`' producer, and removes the dist-gated build tests from the sealed `checks` (Q7).
- **FIX-D4** is a BUILD lane at the next composition train: N-3 (the pool literal) priced first, then (b) resolve-or-drop the handle and (d) the sanitiser's regex and predicate repaired with culture-correct copy; its golden (159 rows) and prose movement are the owner's signature — two records, one sitting. **TOOL-8 (NEW):** the dark-guard census.
- **Preamble amendment owed at the chair's next placement sitting:** §P2.11 says a `skipIf(!requireDistRead)` byte arm in a focused run is not evidence about bytes; the byte row names `factionLifecycle.js` as EAGER. **TOOL-3's resume** records the first-paint closure's live margin in the test's header.

## Amendments of 2026-09-20 02:55 EDT — run 17's one red ruled (a marker-slice scanner, not the product); CURE-E lands in the slot before EM-B3c; run 18 is the terminal; TOOL-7 widens; EM-P3b gains two rows (ODQ §934.47 addendum 41)

- **CURE-E** (the trade-route token scanner re-cut to the found-and-ordered idiom; the second marker-slice instance in `faithPanelModel.test.js` cured with it) lands in the slot between EM-B1k's landing and EM-B3c's dispatch; **run 18** re-runs train EM-T3's terminal on that tip (its log at the scratchpad root, copied into the kit at refresh — never written into the kit while running).
- **TOOL-7** is now the silent-failure-idiom lint (two arms: `in` against a frozen literal; a marker slice with no ordered-markers assertion), measure-first. **EM-P3b** gains: the canonical trade-route list pinning `GENERATED_ROUTE_VALUES` to the pools (the three-copy collapse), and the two fixture oddities (`landingFixture.js:47`'s `random_trade` sentinel; `sampleDossier.json:15`'s unscoreable `"salt road"`).

## Amendments of 2026-09-20 03:08 EDT — ⭐ EM-B1k LANDED (the seventh landing; the data-loss cure on the branch); the dist measurement green; CURE-F ruled; the terminal's order (ODQ §934.47 addendum 42)

- **EM-B1k LANDED** at `19c4cb853` (flip `63e40fe57`): the tick's settlement descends from the raw save roster; a shelved or hostage NPC is never erased and a sole-member house never dissolved; goldens byte-identical; the chair's build + `verify:dist` at the tip green (538). The fix reaches players only through the paused push chain.
- **THE SLOT WINDOW BEFORE EM-B3c:** CURE-E (the marker-slice scanner) and **CURE-F** (the `:579` comparand becomes the tick's input roster; the cast goes; a stubbed-clock pin) land as two pathspec commits; then the chair's ONE lighting refreeze in the slot (B1k's `+6` and CURE-F's `+1` titles); then **run 18** — train EM-T3's terminal — bare and detached in the consist, its log at the scratchpad root; EM-B3c builds in the slot during it, its batches paused until it exits.

## Amendments of 2026-09-20 03:09 EDT — TOOL-6 measured; CURE-G joins the slot window; TOOL-6b chartered as the `tests/store` admission dock (ODQ §934.47 addendum 43)

- **CURE-G** (the cure lane's window, after CURE-E/F): EM-B1k's suite renamed to `participationWriteBase.contract.test.js` (the register's own opt-in token), its `mutation` row with a convicting mutant, and `baseStateCapsule.test.js:369`'s stale floor raised to the live count.
- **TOOL-6b (NEW; a tooling dock after train EM-T4's terminal):** `tests/store` admitted to the mutation register's enforcer set under its own, stricter predicate (134 surgical rows; the vacuity floor 686 → 840; the admitted-tree arm generalised); its first commit gives the shrink-only ratchet a ratchet over itself; it sweeps the manifest sentence the admission falsifies. **EM-B1k2's** test file carries the `contract` token from its pre-proof.

## Amendments of 2026-09-20 03:11 EDT — FIX-C2 measured and built as a parallel lane (the source-citation walker, EOF arm gate-wired); FIX-C2b chartered; EM-B1k2's pre-proof dispatched at the landed tip (ODQ §934.47 addendum 44)

- **FIX-C2** builds now in its own worktree (`fix-citations-2026-09-20` @63e40fe57): the walker's EOF arm gate-wired over `src/tests/scripts` with the 38 live rows re-addressed (the seven certification citations past EOF; the tuning inventory's `realHome` rot; the `Primitives.jsx` cluster), docs-live under a shrink-only baseline, archival excluded, the symbol arm report-only; composed at the next composition train. **FIX-C2b (NEW, measure-first after it lands):** the declaration-aware third arm over the 5,479 citations naming a symbol the target never declares.
- **EM-B1k2's pre-proof** runs at `read-tip-63e40fe57` carrying addenda 39 and 43 (the display-name census; the save writers; the fourth ratchet root; the `contract` token; the sealed `checks` without dist-gated files).

## Amendments of 2026-09-20 03:29 EDT — REVIEW-P walked: sixteen findings fated into FIX-P1…P5 and REVIEW-P2 (ODQ §934.63)

| lane | carries | composed |
|---|---|---|
| **FIX-P1** | F1 the byte-identical anonymous forks (a per-visitor salt in the anonymous envelope; the full user id in `forkSeedFor`); the copy made true; noticed 1, 2, 8 | the next composition train |
| **FIX-P2** (measure-first, the preview persona) | F2 the realm layer keyed on `settlement.id` | its measurement first |
| **FIX-P3** (dispatched now) | F3 the silent daily-cap refusal on the dossier toolbar; F4 Narrate navigates; F10 `/signin` drops the reason; F11 the 404 lands silently; F12 duplicate alerts; F13 "this account" for a visitor; noticed 3 — the refusal walker's population widened to prove every path | the next composition train |
| **FIX-P4** | F5/F6 the forge coach (gated on the reveal; docked clear; a real sheet on the phone); F15 the realm's DEV-only warnings | the next composition train |
| **FIX-P5** | F7/F8/F9 the floors and the phone bar; F14 one tier word by a walker; F16 the lock mark; noticed 4–7 | the next composition train |
| **REVIEW-P2** | the seven unwalked public routes, the gallery hubs, `/world/:code`, and an account walk under the persona on the production build | after the composition train |

## Amendments of 2026-09-20 03:34 EDT — CURE-E/F/G landed; the second refreeze; run 18 is running as train EM-T3's terminal; EM-B3c re-pinned and next in the slot (ODQ §934.47 addendum 45)

- The branch: `9f3455b84` CURE-E · `96036427f` CURE-F · `c71782e7f` CURE-G · `21b991118` the refreeze (`2651·383·2268·25035·6678`) · `dbd077481` EM-B3c re-pinned. **Run 18** validates `21b991118`; EM-B3c builds in the slot from the first freed working seat, its batches behind run 18; on its landing: the flip, EM-P2 v4 placed, then the composition train EM-T4.
- **FIX-C2b's first commit** is the certification file's whole citation set (`subsystemRowsWar.js`, ~40 stale addresses) re-addressed by symbol.

## Amendments of 2026-09-20 03:35 EDT — EM-B1f version 3 accepted (the block lifted by execution); the ninth roster row's omission is `ALL_BUT_DEAD`; train EM-T6 opens EM-B1k2 → EM-B1f; EM-B3c dispatched (ODQ §934.47 addendum 46)

- **CORRECTION to the amendment of 2026-09-19 17:36 (EM-B1f ruling 3):** the ninth roster row's reasoned omission is `omits: ALL_BUT_DEAD` — the walker's own arm refuses an omission naming `active` (not a member of the live union) or one that enumerates the union without the three new members. **Train EM-T6 now opens EM-B1k2 → EM-B1f** (defence in depth lands before the filter widens; they share `roadsParticipation.test.js`), then EM-B1h → EM-B1i → EM-B1j → EM-A2b → EM-B1a. EM-B1f's walker rows gain `spelling: 'derived'`.
- **EM-B1j's compile** measures the readers of the institution status `'remnant'` (written by `tierOutcomeApply.js` outside the `EntityStatus` typedef); a separate vocabulary charters **EM-B1l**. **EM-B3c** builds in the slot now.

## Amendments of 2026-09-20 03:37 EDT — the cure lane's receipt and its ten items fated (ODQ §934.47 addendum 47)

- **TOOL-7** carries: the marker-slice lint (string-and-comment-skipping form), the shared `objectLiteralKeys`/`fnBody` helpers moved to `tests/helpers/`, and the root-anchor tidy-up of `humanizeEngineTokens.test.js`. **TOOL-6b** carries the sweep plant for `pulseKernel.js`. **EM-P3b** carries `GENERATED_ROUTE_VALUES`'s pin and the two fixture oddities, with `landingFixture.js:47`'s reader check deciding whether **FIX-P5** cures it on the public landing now.

## Amendments of 2026-09-20 03:39 EDT — EM-B1k2 pre-proofed (the fourth root cured to the reader token; EM-B1k3 not chartered); placed after EM-B3c; FIX-P4 dispatched (ODQ §934.47 addendum 48)

- **EM-B1k2** (kit `packets-waiting/EM-B1k2.*`, version 2): the participation census gains `-e 'factionRosterOf'` (41 → 44, three files dispositioned); a `mutation` row with its sweep plant; the display-name census closed at zero duplicates; the save writers closed; placed the sitting EM-B3c flips to LANDED, its window re-run over CURE-E/F/G and the refreeze. **EM-E0's compile** carries the access-state precondition (an inactive save must not be walked by a tick). **EM-B1f's pre-proof** banks `npcVerdictPulse.js`'s quarantine row.

## Amendments of 2026-09-20 03:53 EDT — run 18 red by one test (the `contract` token's second enforcer); CURE-H ruled; the terminal is run 19 after EM-B3c and CURE-H (ODQ §934.47 addendum 49)

- **CURE-H** (the slot, after EM-B3c lands): A8 of `participationWriteBase.contract.test.js` derives the clock chain from `pulseKernel.js` by the found-and-ordered source read; no `src/` change; the anti-vacuity walker green. **The pre-proof brief's step 12** gains: a rename is a change to every walker keyed on the filename token. **TOOL-6b's brief** gains the token-consumer census.
- **The terminal's order:** EM-B3c's batches → its landing and flip (EM-B1k2 and EM-P2 v4 placed at that sitting) → CURE-H → the chair's refreeze → **run 19** → the composition train's gate queue (FIX-G, TOOL-2b, FIX-F2b, TOOL-3, FIX-C2, FIX-P1, the P/L lanes as they pause).

## Amendments of 2026-09-20 04:04 EDT — FIX-P3 and FIX-P4 at their gates; the coach's docking is the owner's to veto; FIX-P5 gains the inert-chip rule; the hot-file list gains two rows at the next docs sitting (ODQ §934.47 addendum 50)

- **FIX-P5** carries: decorative chips read as inert (the landing's 'Save to Library' span). **The chair's docs sitting at EM-B3c's landing:** `App.jsx` (575/600) and `SettlementsPanel.jsx` (577/600) join the hot-file list, re-measured. **FIX-P4's** in-flow docking of the desktop coach (§934.29's lifted card changed) stands unless the owner vetoes.

## Amendments of 2026-09-20 04:15 EDT — ⭐ EM-B3c LANDED (the eighth landing; migration 203); EM-B1k2 placed READY; TOOL-9 chartered; FIX-L1 at its gate with CURE-A3 live (ODQ §934.47 addendum 51)

- **EM-B3c LANDED** at `ae4a643f7` (flip `154bd7c07`): the gallery scanner refuses every key the client hard-denies; applying 201–203 stays the owner's hand. **EM-B1k2 READY** at `154bd7c07` (`16e1ec88d`), builds in the slot after CURE-H lands and the chair's refreeze and run 19 start. **EM-P2 v4** is placed when EM-B1k2 lands.
- **TOOL-9 (NEW, tooling; small; the next composition train):** the lighting walker gains a read-only print mode (`LIGHTING_CENSUS_PRINT=1`) so a lane can record the whole tuple without the refreeze door.
- **FIX-L2** carries the walker's stale ':2318' comment (30 → 5 after FIX-L1) and the mixed-family park of `tests/edgeFunctions/contracts.test.js`.

## Pre-proof tasks (Opus, read-only, before each packet is READY)
1. Name every "verified fact" with file:line at the base; 2. fill the change manifest and required symbols; 3. measure the budget (files, lines) and split if over; 4. name the tests the packet's proof shape copies; 5. run the sealed dispatch dry.

**Done (2026-09-19):** the simulation survey — `docs/implementation/surveys/SIM-SEALS-SURVEY-2026-09-19.md` (146 candidate rows, 24 derivation-only, 42/42 registry rows and 51/51 unregistered draws placed, the conditions measured) — is the measured base for E0 and E4–E7; design §19 rules it.

## Amendments of 2026-09-20 04:34 EDT — CURE-H landed and the third refreeze; run 19 launched; EM-B1k2 building under seal; FIX-P5 and TOOL-7 reported: FIX-K1 and TOOL-7a launched, TOOL-7b/7c and FIX-F5 chartered, FIX-F4 re-scoped, §934.64 asked (ODQ §934.47 addendum 52)

- **CURE-H LANDED `902580c71`:** A8 of `tests/store/participationWriteBase.contract.test.js` derives the settlement-clock chain (`advanceTime → advanceFoodStockpile → applyBlockadeTransportImpairment → advanceTreasury`) from `src/domain/worldPulse/pulseKernel.js` by its own `// @pulse-stage: settlement_clock` marker, found AND ordered, five red controls fired in throwaways, every prior assertion kept; suite 5/5, the anti-vacuity walker 15/15, `tests/lint` ×5 42/42, eslint 0; one file (+207/−13); no title moved (the refreeze measured it). The `contract` token's consumer census, delivered: exactly TWO enforcers — the mutation register's `NAME_PATTERN` and `contractTestAntiVacuity.walker.test.js`, whose `inScope` applies FIVE rules (the pre-proof brief's step 12 stands; TOOL-6b's brief carries the census). **THE THIRD REFREEZE `5a3380e8d`:** the tuple measured through the walker's own door — `2652 · 383 · 2269 · 25043 · 6679` = the frozen `2651·383·2268·25035·6678` + EM-B3c's `+1/+0/+1/+8/+1`, CURE-H `+0` — exactly the expected tuple, measured not assumed. **RUN 19 = train EM-T3's terminal** at `5a3380e8d`, bare and detached in the consist (nohup; the log at the scratchpad root), launched 04:18:49 EDT; the ratchet took the exclusive lock at 04:24. GREEN ⇒ T3 closes and the push chain's recommended moment arrives (it carries EM-B1k, EM-B3c, CURE-E…H); RED ⇒ the chair rules the cure from the ratchet's JSON. **EM-B1k2's build is SEALED in the slot** (dispatched 04:2x under `LANE-EM-BUILD.md` with the chair's two cuts restated: the anti-vacuity walker in its instruments, its red a STOP; every 'every …' population read from the producer, CURE-H's idiom the model); its batches pause behind run 19; the chair places nothing while the seal holds (EM-P2 v4 waits for B1k2's flip).
- **THE CHAIR'S GATE JUDGMENT** (the owner: "nvm i leave it to your judgemetn"): SIX live lanes now wait at the gate (FIX-C2 · FIX-P1 · FIX-P3 · FIX-P4 · FIX-L1 · FIX-P5) and four staged dead-agent lanes behind them (FIX-G · TOOL-2b · FIX-F2b · TOOL-3) — more than two — so the SHARED-TIER OVERLAP OPENS after run 19: the slot's build resumes first with ONE paused lane beside it, thereafter two lanes at a time in the order they paused; an EXCLUSIVE-tier build (TOOL-3's two real builds) runs alone. Recorded, vetoable.
- **FIX-P5 REPORTED, paused at its gate** (four commits over six batches; goldens byte-identical; the route census moves exactly one row, `/pricing`): F9 — ten sub-12 px sites and twenty-four 12–13 px paragraphs through the two ladders (`bare 10 → 0`, `floored 7 → 17`) with a NEW opt-in prose-floor arm for the gap no instrument saw (a 12 px `<p>` clearing the chrome floor); F16 — the `$2.99` ladder cell wears the lock mark, both directions pinned; F8 — the phone bar's seats content-sized, 'Compendium' no longer ellipsises (271 + 20 ≤ 375); noticed 7 — the 44 px control's overhang proved clear of content by ≥ 12.92 px at every narrow width; noticed 4 — the `†` became the check the contrast test already calls it; F14 — `tierFacts.js:35-38` REFUTED the review's split reading (the spellings were unified to the wizard's 'Thorpe'; only the machine TOKEN stays `thorp`), so three label tables and `SummaryTabV2`'s upper-cased token now say 'Thorpe'; noticed 6 — thirteen shouted dossier badges became written words wearing `textTransform`. F7 STOPPED BY MEASUREMENT → **§934.64 asked** (below). Noticed 5 → **CLOSED — refuted by the source:** the exclamation is `src/copy/en.js:496`, `voiceMechanics` DOES scan it under a count-pinned Tier-1 exception citing §934.26, where the owner's verbatim order outranks the voice law; re-wording needs the owner's word and the owner has ruled — not re-asked. FATES: F14's two registered-not-cured surfaces (the Compendium's tier label baked into a BYTE-PINNED generated artifact — a generator/record touch, the brief's STOP; the gallery's `capitalize` covering every facet it draws) → **FIX-F5 (NEW; small; after FIX-P5 composes):** the Compendium's artifact re-stamped with the unified word under its own pin's door (the lane names the pin and the door; the chair rules it at the train) and the gallery's tier chip reading the label table, the other facets keeping `capitalize` — the census row P5 executed against the shipped artifact reds the day it is cured, which is the proof; the taste sample `organic/samples/PricingSample.jsx` drawing `†` where the shipped row now draws `✓` → SLOT: **FIX-F4** syncs the sample to the shipped mark (one character; the sample records the shipped taste, it is not a second design); `phoneChromeFloor.census`'s `/create` (bare 98, measured 97) and `/settlements` (files 206, measured 208) rows stale at 63e40fe57, green in the permitted direction → SLOT: **FIX-F4** re-takes both rows to the measured figures (ratchet-tightening only); the five prose-shaped 12–13 px lines in `App.jsx`, `HomeHero.jsx`, `generate/ClerkNote.jsx` (lane 28's remainder — why the prose floor is opt-in by roster) → SLOT: **FIX-F4** extends the prose-floor roster to them, measured. **FIX-F4 IS RE-SCOPED:** its `SummaryTab.jsx` population is re-measured after P5's Group D composes (P5 touched `SummaryTab.jsx` and seven siblings). **§934.64 (the painted Sign In plate: re-cut, or 10 px stands — the chair keeps the executed §934.26 exception until the word; recommendation: keep).**
- **TOOL-7 MEASURED** (read-only at 63e40fe57, one commit behind CURE-E, whose two cured sites are in the count — the §0 delta suffices; Q7 ruled): arm A — 341 membership `in` tests, 96 resolving to an object literal (63 absence checks that read a prototype key as PRESENT, 31 guard-then-reads that hand back a FUNCTION), nine `src/` convictions; arm B — `.substring`/`.substr` ZERO tree-wide, 113 `.slice` calls with an inline finder in their arguments (unassertable by construction: no name is bound to the index) — B1 two-distinct-anchor ≥ 21 (three cured by CURE-E ⇒ ≥ 18 live), B2 6, B3 49, B4 38; arm C1 — ≥ 25 of 36 files brace-count raw source; C2 — five sites, five distinct labels. Priced: ~2.0 s for the whole tree. ⭐ THE FINDING: `tests/helpers/sourceContract.js` already IS the cure (a fail-closed extractor chokepoint whose header names the silent-`''` class) and its enforcer `contractTestAntiVacuity.walker.test.js` declares the exact gap at lines 38-39 (Rule 1b keys on extractor NAMES; an inline slice has no callee) — and both of CURE-E's instances lie OUTSIDE that walker's scopes, which the file's own lines 78-80 call vacuity one level up. RULED (Q1–Q7): **TOOL-7a (Act 1; a parallel lane, LAUNCHED; `tooling-7a-2026-09-20` @5a3380e8d; kit `briefs/launch/TOOL-7a.md`):** ONE throwing `spanBetween` helper in `sourceContract.js` with every live B1 site re-routed (CURE-E's two and its `declarationBody` among them; `surveyorByok.test.js`'s five on the paid BYOK surface, `byokNeverLogged.test.js:89`'s security pin, `autoplacementStore.test.js:101`'s retirement-comment anchor, `aiAnalyst.test.js:461`'s prose anchors); `fnBody`/`objectLiteralKeys` extracted there as THROWING and strip-aware (six matchers → one family; `statementWindowAt` stripped); `codeSkeleton` co-located in `tests/helpers/codeOnlySource.js` as the DECLARED strict strip with `codeOnly` unchanged (Q6 CLOSED as intended: two strips with declared purposes in one file, 23 importers untouched); Rule 5 convicting the B1 shape over `tests/**` + `scripts/**` (a declared new scope; empty allowlist; three adversarial self-tests; the shape parsed on the stripped source with a balanced splitter — class 9; no new mutation row, the file's is `self-proving-meta`); the walker's own three `in` gates (`:357, :369, :382`) → `Object.hasOwn` (Q3: arm B before arm A). Two commits, composed at EM-T4. **TOOL-7b (Act 2; after 7a composes):** B4 (38 sites, drops the last character silently — the sneakiest) cured wholesale; B2/B3 (55) under a shrink-only baseline; C1 the brace-matcher ADOPTION baseline (not a detector — conviction needs data flow), `dossierMountRegistry.walker.test.js`'s four rows among it. **TOOL-7c (arm A; after 7a):** the ESLint `no-restricted-syntax` selector `BinaryExpression[operator='in']` merged into each owning block under the config's one-block-per-file law (`eslint.config.js:441`; ~7 merges into a 58 KB config — tooling, NOT owner-gated, Q2), with a declared exemption register for the class-8 sites and the 55 % unresolvable-RHS blind spot declared chooser-totality style. **FIX-K1 (NEW; product; a parallel lane, LAUNCHED; `fix-hasown-2026-09-20` @5a3380e8d; kit `briefs/launch/FIX-K1.md`):** the nine `src/` convictions cured with `Object.hasOwn` — FIRST and red-first the copied tier-band pair `src/domain/traditions/genesis.js:143` + `politics.js:99`, where a SAVED CAMPAIGN's `tier: 'constructor'` passes the guard and `tierIndex` becomes the `Object` constructor (a live product defect, Q4 — one resolver if the import is acyclic, the copied family collapsed); `commercialReasonTaxonomy.js:138-139` (both lines leak; the JSDoc's `string | null` made true); `pulseFingerprint.js:160` (a prototype-named token minted `"function toString() { [native code] }1"` into a telemetry payload — executed); `flagRegistry.js:177` (`flag('constructor')` reads ON past the unknown-flag warning); `espionageDoctrine.js:170`; `AdminTrendsCharts.jsx:108`; `constructionUsage.js:112-113`; the accumulator-guard family (`commodityFlow.js:500,505`, `generosityKernel.js:1041,1094,1121`, `routeNetworkLedger.js:554`, `traditionsKernel.js:448` — `if (k in writes) continue` SKIPS A WRITE on a prototype-named key, the EM-B1k shape); `no-prototype-builtins` warn → error if its population is zero (item 16). Behaviour-identical for every own key; goldens byte-identical (no seed carries a prototype-named key — the golden suites prove it); the three class-8 sites (`beliefMap.js:1127`, `disinformationPlant.js:162`, `espionageProducts.js:311`) untouched with the reason; composed at EM-T4 with the chair's dist measure. CLOSED WITH REASON: C2 (five sites, five distinct labels, zero cross-file matching — no population); the `substring` arm (zero in the tree — dead on arrival); item 9 (the lint's exemplar is CURE-E's helper, not `tradeRouteSemantics.test.js`'s `match` form — corrected in 7a's brief); item 17 (the exact-equality `.not.` row — a budget note in 7a's brief); item 18 (the instrument's splitter — class 9 in 7a's brief).
- **CURE-H's noticed items:** the stale docblock name at `participationWriteBase.contract.test.js:2` → SLOT: **TOOL-6b** (its admission dock touches the file's register row); the `@pulse-stage` marker family now has FOUR consumers (A8 the fourth) and the `contract` token reaches five rules → both recorded in TOOL-6b's brief.
- **SEATS at this stamp:** gate = run 19; working = EM-B1k2 (slot, sealed) · TOOL-8 (the dark-guard census) · FIX-K1 · TOOL-7a; paused at the gate (live agents, in order): FIX-C2 · FIX-P1 · FIX-P3 · FIX-P4 · FIX-L1 · FIX-P5; (dead agents, staged; `briefs/RESUME-PARALLEL.md`): FIX-G · TOOL-2b · FIX-F2b · TOOL-3. **SEAT QUEUE:** FIX-P2 (measure-first, the persona) → FIX-F4 (after P5 composes) → FIX-T1 → FIX-G3 → TOOL-9 → FIX-C2b → REVIEW-P2 → EM-A1 (after P2 v4) → the EM-R pre-proofs (R0a + the `history` group; R0d; R0b v3; R0f compile; R0c) → EM-B1f (after B1k2 lands; its pre-proof re-run) → EM-B1j compile → R6 v3.1's arms → TOOL-7b → TOOL-7c → FIX-F5 → TOOL-6b (after T4).

## Amendments of 2026-09-20 04:46 EDT — run 19 red by one test (an instrument collision, not a defect); CURE-I ruled and launched; EM-B1k2 resumed with the gate; the directory-whole rule written into the lane briefs (ODQ §934.47 addendum 53)

- Run 19 (train EM-T3's terminal at `5a3380e8d`, bare and detached in the consist) exited 1 at 04:39:19 EDT: 10,478 suites, 33,595 tests, 33,593 passed, ONE failed — every validate step, both typecheck ratchets (167/167; 1,113/1,113) and lint (warnings only) green; the census unmoved (frozen at b43dfb771, 0 failing). THE ONE RED: `tests/security/migrationRefIntegrity.meta.test.js` :: "every referenced migration file exists under supabase/migrations/" — `expected [ …(5) ] to deeply equal []`: `tests/security/galleryScannerMirrorTotality.test.js` (EM-B3c's walker, `ae4a643f7`) references `089_a.sql` (×1), `202_b.sql` (×2) and `1000_z.sql` (×2), none of which exists. They are that walker's own SORT FIXTURES (the 'net-current owner under both sorts' self-test: numeric order must beat lexical order, which needs a four-digit name the directory does not have), not `runIf` gates; the SS4 meta walker scans every `tests/security/*.test.js` for `NNN_….sql` literals and requires each to exist, excluding only itself. DIAGNOSED by the chair with a focused run in the freed consist (`1 failed | 2 passed`; the five names quoted verbatim; log in the kit). The product is sound and migration 203's pin is intact: two instruments disagree about what a quoted literal means. **CURE-I RULED (vetoable) — a synthetic fixture is DECLARED and the declaration is EXECUTED:** the meta walker learns a line marker (`// synthetic-migration-name: <reason>`, placed as the estate's `// anchored:` idiom is placed); a marked literal is EXCLUDED from the existence check AND asserted NOT to exist (a marked literal naming a real migration is a red of its own — the marker lies); the synthetic set is non-empty whenever a marker is present; three adversarial self-tests (an unmarked fake fires; a marked fake clears; a marked real name fires with the lie message); B3c's walker marks its fixture lines; area 12's sweep plant kept true. REJECTED: computing the names past the regex (the walker's own declared CANNOT-CATCH gap — an evasion); a second file-level exclusion (a blanket exemption behind which a real vacated gate could hide); real filenames (the sort property needs the four-digit name). LAUNCHED as a lane in its own worktree (`cure-i-2026-09-20` @5a3380e8d; kit `briefs/launch/CURE-I.md`): it proves the cure there, commits on its branch by pathspec, pauses `PAUSED FOR THE SLOT`, and cherry-picks onto the branch in the slot after EM-B1k2 lands ("the slot and the gate are yours"). **THE LESSON, WRITTEN INTO THE BRIEFS THE SAME TURN:** a landing's new or renamed test file opts into EVERY walker that governs its directory, and a lane's focused runs never reach them — run 17 (a marker-slice scanner), run 18 (the `contract` filename token) and run 19 (a directory's meta walker) are ONE family; `LANE-EM-BUILD.md` verb 4, `LANE-EM-PREPROOF.md` and `LANE-PARALLEL.md` now require the CREATE/RENAME directory WHOLE once (`tests/security` whole for a security file; `tests/lint` whole always), and a synthetic literal shaped like a real artifact name is declared with an executed marker. EM-B3c's receipt said 'security 69/69' — its focused files, not the directory; recorded, not the lane's fault: the brief did not ask. **EM-B1k2 RESUMED WITH THE GATE at 04:43** (its seal exit 0, sealDigest `bdcc8333…`; batch 1 of 2 = the red-first proof — A1 and A4 expected red, A2/A5/A6 green with their counterforces measured — with the anti-vacuity walker in the same batch, its red a STOP); by the chair's judgment the shared tier holds B1k2 + CURE-I, and FIX-C2 resumes when one of them yields. STAGED FOR THEIR MOMENTS: `briefs/launch/FIX-P2.md` (the measure-first brief under the persona; dispatched when TOOL-8's seat frees — TOOL-8's report is written and is fated in the next addendum) and `tools/chair-scripts-a9df403c/place_p2_v4.py` (EM-P2 v4's placement at B1k2's flip: a NEW entry; the window from ad7ddf2c9 with the shared register the only permitted mover; the stale 'EM-B1d is building' header paragraph replaced by a history note as a recorded cut; the anti-vacuity walker added to its checks). RUN 20 = the terminal after B1k2 and CURE-I land and the fourth refreeze is measured.
- **CURE-I (NEW; the cure lane; lands in the slot after EM-B1k2):** the declared, executed synthetic-migration-name marker in `migrationRefIntegrity.meta.test.js` (three self-tests) and the fixture lines of `galleryScannerMirrorTotality.test.js` marked; two files under `tests/security`; then the fourth refreeze and RUN 20.
- **THE DIRECTORY-WHOLE RULE (every lane brief, 2026-09-20):** a CREATE or RENAME of a test file under `tests/<dir>` runs `tests/<dir>` WHOLE once in the lane's instruments; `tests/lint` whole always; a synthetic literal shaped like a real artifact name is declared with an executed marker, never computed past a regex.

## Amendments of 2026-09-20 04:51 EDT — TOOL-8 measured (the eight dark guards); TOOL-8a chartered ahead of the cures; FIX-D6 and FIX-D7 chartered; TOOL-8b slotted; §934.65 asked; FIX-P2 dispatched (ODQ §934.47 addendum 54)

- TOOL-8 (read-only at 63e40fe57; four instruments — an acorn AST scan of 1,171 files → 10,267 candidate guards; a String/RegExp-prototype execution instrument over the FULL 525-row golden corpus, which runs uninstrumented in 8.49 s so no stride was needed; input capture; exact field vocabularies parsed from the 525 serialized settlements): 429 LIVE · **728 CONFIRMED DARK** (an uncapped 841-s pass refuted 107 cap artefacts — `garrison` is evaluated 2,987,768 times per corpus pass, so a 20,000-capture window sees 0.7 % of its life: a truncated instrument INVENTS darkness) · 571 UNREACHED (409 in `src/domain`, which the generation corpus never loads) · 761 UNJOINED · 7,778 `===` sites not instrumentable. The 728 classified against the exact vocabularies: **1 SPELLING_MISS + 7 CASE_MISS = THE EIGHT**; 173 PRECEDENCE_SHADOWED (an earlier arm always wins — redundant, never to red a lint); 545 INPUT_ABSENT (honest dead for this corpus). THE EIGHT: D1 `src/generators/npcGenerator.js:1362` — the FIX-D4 sanitiser reproduced exactly (969 secrets tested, 36 say `thieves`, 159 eligible, 318 replace calls, 0 scrubbed): `/thieves guild/gi` cannot reach `thieves' guild` — an APOSTROPHE — and the corpus writes it that way every time; its predicate half is a VOCABULARY miss, not case (`hasInst` lowercases; `criminal` matches 0 of the 231 institution names; `Street gang`, `Black market`, `Smuggling operation` are missed by both arms); born in the genesis commit `e608542bd2`, never touched, never fired. D2–D8 all in `src/generators/spatialGenerator.js`, whose `has()` (`:21`) is the ONE raw-name predicate among six private copies (five siblings lowercase first; the file's own `:114` comment records a LOCAL cure of one member of this class — the trap stayed armed): D2 `Craft guilds` + `Merchant guilds` ⇒ no Artisan Quarter; D3 `Academy of magic` ⇒ no Mages' Quarter; D4 `Monastery or friary` ⇒ no Religious Quarter (its `shrine` branch dark too); D5/D6 the monastery and the smiths never listed as landmarks (cosmetic); D7/D8 (`fishing`, `alehouse`) dark duplicates an exact-case arm already covers. RULED (the report's Q1–Q6; vetoable): **TOOL-8a (NEW; tooling; a parallel lane at the HEAD of the seat queue; kit `briefs/launch/TOOL-8a.md`):** the lint in the registry + exact-vocabulary shape, scoped to `src/generators/**` (wave 1; the `src/domain` blind spot DECLARED chooser-totality style — Q2), ~8.5 s, patching nothing; every guard site carries a registry row (totality both ways; a new unregistered guard reds); the corpus arm derives each field's exact vocabulary from ONE uninstrumented pass and reds a row whose literal matches nothing that exists, the counter-example quoted; precedence-shadowed arms registered BY CHAIN; a CLOSED `DECLARED_DARK` vocabulary asserted like `FORK_CLASS_VOCABULARY` (Q5: defensive-escape · idempotent-normaliser · negated-dedupe · corpus-absent · precedence-shadowed · catalogue-candidate · KNOWN_DARK_PENDING_CURE), extensible only by a row with a reason; THE EIGHT born as `KNOWN_DARK_PENDING_CURE` rows the walker asserts are STILL dark, so it lands green and reds the day a cure lands without flipping its row (FIX-P5's executed-exception idiom); the mutation proof is the NEGATIVE CONTROL (a synthetic known-absent row must red) as a `self-proving-meta` row — the sweep never plants a golden-moving real fix (Q3). **FIX-D6 (NEW; product; the SIGNED door; the composition train after TOOL-8a):** ONE shared `institutionMatches(names, keywords)` with ONE declared lowercase normalization; the six private copies (N10: `npcGenerator:937`, `narrativeGenerator:471`, `structuralValidator:276`, `economy/viability:235`, `economy/economicState:377`, `spatialGenerator:21`) and `priorityHelpers:25 hasAny` re-pointed; D7/D8's dark duplicate arms retired; its first act executes a settlement with `Craft guilds` AND `Merchant guilds` and reads its quarter list (the report's PLAUSIBLE promoted); the quarter and landmark shifts D2–D6 are a legitimate ONE-TIME output change — the goldens re-recorded under the owner's signature (Q4: the structural cure, not seven per-site literals that leave the trap armed). **FIX-D4** gains D1's predicate-vocabulary widening in the same signed act (Q1). **FIX-D7 (NEW; product hygiene; behaviour-preserving with goldens byte-identical, or the item moves to FIX-D6's signed door; a parallel lane, measure-first per item):** N2 (`npcGenerator.js:613-615`'s `{commodity}` substituter for a token no goal carries, and its unbounded `/grain/g` — bounded or retired); N3 (`serviceClassifier.js`'s header claim 'covers all 260' corrected to the measured 124 of 375 with 251 falling through — the heuristic IS load-bearing); N4 (`narrativeGenerator.js:500,516`'s `professional guard` / `healer` rungs — the corpus's healer spelling measured first); N5 (`foodBalance.js:221`'s declared de-slug exercised by nothing over the two `mountain_pass` rows — pinned or the comment retired); N7 (`historyGenerator.js:471`'s doubled-noun cure that never fires — fossil or corpus miss); N8 (`economyReconciliation.js:62`'s `(dominant)` normaliser whose producer no longer writes the suffix — the layer measured); N9 (`institutionProbability.js:82`'s `armory`-only spelling beside the estate's `armory|armoury`); N13 (`Woodcutter's camp` in the catalogue, 0 of 231 generated, so `Woodcutters' Ground` is unreachable — WHY, measured; a weight defect goes to the signed door). CLOSED WITH REASON: N6 (`'Legitimate heir'` — a corpus-coverage fact, registered `corpus-absent`); N11 (two correct dark guards — the allowlist's exemplars). **TOOL-8b (NEW; after 8a):** the `src/domain` census under a pulse/dossier driver corpus (N12's 409 never-evaluated sites). N1 → FIX-D6; N10 → FIX-D6; N14 (a capped instrument invents darkness) → TOOL-8a's header and brief. **FIX-P2 DISPATCHED** into TOOL-8's freed seat (the persona file confirmed at `consist/.env.development.local`; the read tip `read-tip-5a3380e8d`; its own Vite server on 5233). SEATS: gate = EM-B1k2 (+ CURE-I on the shared tier); working = FIX-K1 · TOOL-7a · CURE-I · FIX-P2. SEAT QUEUE: TOOL-8a → FIX-F4 (after P5 composes) → FIX-T1 → FIX-G3 → TOOL-9 → FIX-D7 → FIX-C2b → REVIEW-P2 → EM-A1 (after P2 v4) → the EM-R pre-proofs → EM-B1f → EM-B1j → R6 v3.1's arms → TOOL-7b → TOOL-7c → TOOL-8b → FIX-F5 → FIX-D6 (the signed door) → TOOL-6b (after T4).
- - **§934.65 — OWNER DECISION POINT (asked 2026-09-20 04:51 EDT): THE DARK-GUARD CURES MOVE THE GOLDEN — SIGN FIX-D4 AND FIX-D6?** Two cures TOOL-8 proved are legitimate one-time output changes: **FIX-D4** (the sanitiser's apostrophe + its criminal-institution vocabulary — 159 eligible NPC secrets today ship a thieves'-guild payroll in settlements with no criminal infrastructure; after the cure that prose is scrubbed, moving the prose manifest golden on those rows) and **FIX-D6** (one shared lowercase institution predicate — settlements with `Craft guilds` and `Merchant guilds` gain their Artisan Quarter, an `Academy of magic` its Mages' Quarter, a `Monastery or friary` its Religious Quarter and landmark; the generator golden moves on those rows). Both are built TO THE GOLDEN and stop; the re-record is executed by the chair under the owner's signature at the composition train (the §934.58 door). RECOMMENDATION: YES, both — the owner's law is depth and coherence, and a settlement with an Academy of magic and no Mages' Quarter is a falsehood the DM reads. Until the word, nothing re-records.

## Amendments of 2026-09-20 04:59 EDT — FIX-K1 at its gate (sixteen sites cured; three findings sharper than the census; two lane judgments accepted); TOOL-7c gains arm D; the zsh-pathspec hazard into the lane briefs (ODQ §934.47 addendum 55)

- FIX-K1 (`lane-fix-k1`, `fix-hasown-2026-09-20` @5a3380e8d) cured all sixteen convicted `src/` sites with `Object.hasOwn`: its scanner re-run at its base drops the convictable `src/` rows 19 → 3, the three survivors exactly TOOL-7's class-8 exemptions (`beliefMap.js:1127`, `disinformationPlant.js:162`, `espionageProducts.js:311`); nine pins in eight EXISTING suites (no new file, so no register row); goldens identical before the first edit and after the last; 20 files, +308/−30; EVERY pin executed red against the uncured tree and green against the cured one in plain node before any gate (`pinProbe.RED.txt`, `refugePinProbe.RED.txt`, `leakProbe*.txt`). SHARPER THAN THE CENSUS PREDICTED: `deriveFoundingTraditions({tier:'constructor'})` returned ZERO records — a saved settlement silently loses every founding tradition, not a `NaN`; `readEspionageDoctrine` THREW (`targeting.replace is not a function`); `commodityFlow.setStock` wrote `Object.grain = 5` ONTO THE GLOBAL `Object` — the highest-severity site in the sweep, and not an `in` operator, so arm A never saw it; the generosity refuge family pin's red is a data-loss proof (posture keys `["a:b","c:d"]` against five — three persisted latches erased from the save). ACCEPTED, vetoable: **(1) the cure WIDENED by four lines** beyond the convictions — `pulseFingerprint.js` `tallyByType`'s and `constructionUsage.js` `tierMix`'s `out[t] || 0`, `commodityFlow.js` `setStock`'s `!stocks[s]` — the same class wearing a truthiness primitive; the brief's own site-3 pin ('the payload carries no function text') cannot pass unless `tallyByType` closes, because `extractPulseSummary` returns its output as `npc_corruption_by_kind`; each is exactly equivalent for every own key (`setStock` keeps the old truthiness as a conjunct); the veto path is written in the lane's note (drop four hunks, re-scope two assertions). **(2) the tier-band pair COLLAPSED to one resolver** — measured acyclic (genesis's 27-module closure excludes politics; politics's only `import(` of genesis is a JSDoc typedef in a comment); `resolveTierBand` exported from `genesis.js`, politics's `TIER_INDEX` and copied body deleted, `tierIndexOf` a one-line delegate; bundle-neutral by measurement (politics's sole importer `traditionsKernel.js:72` already imports genesis). NEW: **TOOL-7c gains ARM D (measure-first):** the truthiness-guard family over plain-object accumulators (`obj[k] || 0`, `!obj[k]`, `obj[k] ?? …`) — invisible to the `BinaryExpression[operator='in']` selector; its structural cure is null-prototype accumulators (`Object.create(null)`) or `Map`, chosen by measurement. HAZARD, into the briefs the same turn: zsh does not word-split an unquoted variable and git swallows the whole list as ONE pathspec — `git diff -- $SRC` printed a 0-byte patch, `git apply -R` of it was a no-op, and the red-first cycle ran against the still-cured tree and reported GREEN (the same tell as the gate law: an output that could not have come from the work claimed); the LANE-EM-BUILD and LANE-PARALLEL zsh warnings now carry it with the rule that a red-first that comes back green is a STOP and the pre-cure text is grepped before any red is believed. FIX-K1 waits its turn on the shared tier in the order the lanes paused (FIX-C2 → P1 → P3 → P4 → L1 → P5 → the dead four → K1 → 7a); its rulings travel with the gate hand-off. Its batches as written: the red-first via the saved patch (a green there is a STOP), the pin suites green, seven consumer directories including the golden proof, the instruments, `tests/lint` whole, the lighting walker once (its counted delta +9 titles, +3 suites, 0 files — PLAUSIBLE until printed), one pathspec commit of the 20 paths, and a conditional second commit flipping `no-prototype-builtins` warn → error if its measured population is zero.
- **TOOL-7c — ARM D (NEW; measure-first, with arm A):** the truthiness-guard family over plain-object accumulators (`obj[k] || 0`, `!obj[k]`, `obj[k] ?? …`), which no `in` selector sees; FIX-K1 cured four instances it met (`tallyByType`, `tierMix`, `setStock`); the population is measured before a rule is cut; the structural cure is `Object.create(null)` accumulators or `Map`.

## Amendments of 2026-09-20 05:02 EDT — CURE-I proved on its branch and validated; the trailer ruling; FIX-C2 resumed; TOOL-8a dispatched (ODQ §934.47 addendum 56)

- CURE-I is PROVED on its branch: commit `b866847bf` on `cure-i-2026-09-20` @5a3380e8d — two `tests/security` files (+162/−16) — and the chair READ THE DIFF WHOLE: the meta walker's reader `refsIn` is extracted so the live arms and the self-tests drive ONE function; the marker is read exactly as `// anchored:` is (the literal's own line or the one immediately above); a marked literal is excluded from the existence check AND asserted absent by a second verdict (`lyingMarkers`, 'THE MARKER LIES'); the marker's own liveness clause is an always-executed assertion (a file carrying the token while the reader finds no synthetic literal reds); three self-tests (an unmarked fake fires; a marked fake clears; a marked real name fires with the lie message); B3c's walker marks its fixtures IN PLACE — the hoist the brief offered was rejected for a sound reason (deriving A5's expected pair from the fixture array would make the assertion circular). RECEIPTS, all executed: the base red reproduced (`1 failed | 2 passed`); the two suites 15/15; PLANT A (a lying marker on the live corpus) `1 failed | 14 passed`, restored; PLANT B (the reader forced to drop the tag) `4 failed | 3 passed`, restored; mutation-sweep area 12 by hand — planted `EXIT=1`, restored `EXIT=0`, three real referencing suites named; `tests/security` WHOLE `147 passed | 1 skipped` files, `1,834 passed | 1 skipped` tests; anchor + manifest 19/19; voice 30/30; eslint 0; the lighting walker once — titles 25043 → 25047 (+4, the four new `it`s; files/parked/credited asserted unmoved before the arm short-circuited; `suiteTitles` +0 PLAUSIBLE until the refreeze measures it); goldens identical before, after and after the hook. It waits for EM-B1k2's flip and lands by cherry-pick in the slot ("the slot and the gate are yours"), re-proved there. ITS ONE DEVIATION, RULED: the commit trailer is `Co-Authored-By: Claude Opus 5`, not the Fable line `LANE-PARALLEL.md` §7 prescribed — ACCEPTED and never amended: an Opus lane signing as Opus is the honest attribution and its harness names it; the two lane briefs now carry the ruling. **FIX-C2 RESUMED with the gate at 05:00** (the second shared run beside EM-B1k2; the directory-whole rule and the zsh-pathspec law in its hand-off). **TOOL-8a DISPATCHED at 05:01** into CURE-I's freed seat (`lane-tool-8a`, `tooling-8a-2026-09-20` @5a3380e8d; frozen tuple named; it pauses at its first gated batch). SEATS: gate = EM-B1k2 + FIX-C2; working = TOOL-7a · FIX-P2 · TOOL-8a; paused at the gate in order: FIX-P1 · P3 · P4 · L1 · P5 · FIX-G · TOOL-2b · FIX-F2b · TOOL-3 · FIX-K1 (its rulings travel with its hand-off) · TOOL-7a/TOOL-8a as they pause; CURE-I awaits the slot.
- **THE TRAILER RULING (every lane brief):** a lane's commit carries the trailer its own harness prescribes — an Opus lane signs `Co-Authored-By: Claude Opus 5`; never amend a commit to change a trailer.

## Amendments of 2026-09-20 05:06 EDT — CURE-I paused for the slot; TOOL-10 chartered (the env-gated suite census); the marker's how-to in the briefs (ODQ §934.47 addendum 57)

- CURE-I reported (`PAUSED FOR THE SLOT`; commit `b866847bf`; receipts as addendum 56, plus `tests/lint` whole run beyond its brief — `1 failed | 2,775 passed`, the one red the lighting census by its own +4). FATES OF WHAT IT NOTICED: (1) `tests/security/customContentLockOrder.postgres.test.js` is `describe.skip` whenever `ROOT_DATABASE_URL` is unset (`:44`) — unset in the lane, the slot and, MEASURED by the chair, in CI (no workflow, script or config names the variable), so its lock-order claim is enforced NOWHERE while vitest exits 0: SS4's own silent-vacancy class in the shape its CANNOT-CATCH declares out of scope; (2) `tests/security/clientErrorReports.pglite.test.js` gates three describes on `describe.skipIf(!exists)` (`:66, :99, :129`) — green today, the same class the day its artifact moves → **TOOL-10 (NEW; measure-first; small; after train EM-T4):** the ENV-GATED SUITE CENSUS — every `describe.skip` / `skipIf` keyed on an environment variable or an artifact's existence under `tests/security`, `tests/ops` and `tests/edgeFunctions`; for each, WHERE it runs (the lane, the slot, CI — read from the workflow, never assumed; `tests/lint/testRatchet.test.js` already names `ROOT_DATABASE_URL` and is read first); a suite inert everywhere gets a pglite port (the estate's `*.pglite.test.js` idiom) where its claim survives the port, or a DECLARED, EXECUTED register row (the CURE-I marker law: the reason written at the site, and an executed liveness that the pin's subject still exists) — never a silent skip; (3) the meta walker self-excludes by BASENAME while `walk` recurses (`abs.endsWith(…)`), one line to tighten to a path compare → TOOL-10 carries it; (4) the synthetic-name marker is documented only inside the meta walker → the chair's act THE SAME TURN: the lane briefs' directory-whole addendum now tells a lane how to write a `tests/security` fixture that spells a migration-shaped name; (5) the anchored-negatives burn-down list (heaviest rows `mapForkXssChain` 61, `worldSnapshotPublic` 24, `edgeFunctions/contracts` 23) read and deliberately left alone → CLOSED — no action owed by a cure lane; the list is its own walker's ratchet. The trailer deviation was ruled in addendum 56 (accepted; never amended).
- **TOOL-10 (NEW; measure-first; small; after EM-T4):** the env-gated suite census over `tests/security`, `tests/ops`, `tests/edgeFunctions` — where each `describe.skip`/`skipIf` actually runs; an inert-everywhere pin gets a pglite port or a declared, executed register row; the meta walker's basename self-exclusion tightened to a path compare in the same lane. SEAT QUEUE: after TOOL-9.

## Amendments of 2026-09-20 05:09 EDT — TOOL-7a at its gate (B1 = 20; a live vacuous green on a security pin cured; two rulings); TOOL-9 dispatched (ODQ §934.47 addendum 58)

- TOOL-7a (`lane-tool-7a`, `tooling-7a-2026-09-20` @5a3380e8d) finished every ungated act — the re-measure, the helper family in `tests/helpers/sourceContract.js`, all the re-routes, Rule 5 and its self-tests, both commit messages; 24 files staged, tree clean, goldens byte-identical. THE POPULATION CORRECTED: B1 at its base is 20, not 18 — `tests/lint/engineTelemetryWall.walker.test.js:119` (anchor `'('`) and `tests/pdf/supplyChainFlowStatusParity.test.js:22` (anchor `'const STATUS = {'`) were missed by the recon's splitter for exactly the limit it declared (brackets counted in raw text, an anchor LITERAL carrying one unbalanced it); neither file moved since 63e40fe57 — missed, not added; one sits inside the walker's own `inScope`. All 20 re-routed; B1 is 0; B2/B3/B4 unchanged at 7/45/38. ⭐ A LIVE VACUOUS GREEN ON A SECURITY PIN, EXECUTED: `tests/security/byokNeverLogged.test.js:89` sliced `indexOf('create table')` → `indexOf(');')`, and the first `);` in migration 138 sits on line 23 inside the header's own `@rollback: drop function …(...);` prose — BEFORE the DDL — so the span was inverted, `slice` returned `''`, and both `.toBe(false)` assertions passed having read nothing. The product is sound (the honest 1,366-character span carries no key-ish column); the anchor was the defect; the cure carries a planted-column control. CURE-E's class found live for the second time (the first was run 17). RULED (vetoable): (1) the lane strips `--` line comments before the column scan and leaves both regexes byte-unchanged — once honest, the span tripped `/\bkey\b/i` on a trailing SQL comment (`-- inference ran on the user's key`); ACCEPTED: the claim is about columns and a comment is not a column, the planted-column control proves the arm still fires, and the rejected alternatives were a red on a sound tree or editing a shipped migration's prose to satisfy a test; reversible by dropping one `.replace()` and two control arms; (2) Rule 5 is WIDENED to `.slice( | .substring( | .substr(` — the lane asked rather than widening a ruling on its own (correct), and the chair widens it: zero population today, zero cost, and a rule that names one spelling invites the other; the widening is an ungated edit the lane makes when resumed, with its self-tests naming all three spellings. Rule 5 dry-runs at 0 violations over 2,894 files in ~1.0 s with the five older rules also 0; the `.not.` register moved zero rows; predicted lighting delta +13 titles, +2 suites (PLAUSIBLE until printed — the reason TOOL-9 launches now). TOOL-7a waits its turn on the shared tier; both rulings travel with its hand-off. **TOOL-9 DISPATCHED at 05:09** into TOOL-7a's freed seat (`lane-tool-9`, `tooling-9-2026-09-20` @5a3380e8d; kit `briefs/launch/TOOL-9.md`): `LIGHTING_CENSUS_PRINT=1`, a read-only mode of the walker that prints all five figures and their deltas and writes nothing — the normal mode's short-circuit order and the refreeze door unchanged; one function derives the figures for the door, the arm and the print; three self-tests. SEATS: gate = EM-B1k2 + FIX-C2; working = FIX-P2 · TOOL-8a · TOOL-9; paused in order: FIX-P1 · P3 · P4 · L1 · P5 · FIX-G · TOOL-2b · FIX-F2b · TOOL-3 · FIX-K1 · TOOL-7a; CURE-I awaits the slot.
- **TOOL-7a's Rule 5 (amended):** convicts `.slice(`, `.substring(` and `.substr(` whose argument expression carries two distinct finder calls, over `tests/**` + `scripts/**`, empty allowlist.
- **TOOL-9 (LAUNCHED):** `LIGHTING_CENSUS_PRINT=1` — the whole tuple printed, nothing written; the lane receipt idiom becomes 'the tuple the print mode prints and the delta per figure'.

## Amendments of 2026-09-20 05:19 EDT — FIX-P2 measured: F2 refuted; N1 (mapDress) built; FIX-P6 / FIX-P7 / FIX-P8 chartered; the persona correction (ODQ §934.47 addendum 59)

- FIX-P2 measured under a seated session (the product's own local-mode door `settlement_mock_auth`, the e2e suite's — the `.env.development.local` persona seats a ROLE only, `user: null`, so the library and the realm are unreachable under it; the chair's brief assumed otherwise, corrected in memory and in every later walk brief). **F2's REACHABLE CLAIM IS REFUTED WITH RECEIPT:** the realm keys every member by the SAVE id — two `fixp2-alpha` saves with one `settlement.id` (`s_08299edf32384bbc`) placed in one campaign produced two graph nodes (`id` = the save id; `settlementId` the twin-shared field), two independent `settlementTickStates` after a real Advance to tick 8, and after two identical forked twins four nodes, ten edges and twenty-six channels, every endpoint a save id; of F2's four sites `regionalGraph.js:302` is the per-dossier graph, `resourceSites.js:142` an RNG seed not a key, `realmItemReadModel.js:245` already leads with `save.id`. The key's lifecycle mapped (one seed-only writer; 41 read sites dispositioned; the one persisted field `regionalGraph.nodes[].settlementId` has NO reader). **N1 (NEW, CONFIRMED, a live defect at F2's fourth site):** `townMap/mapDress.js:102,167` passes `settlement.id` into ledgers keyed by save ids — executed: the same campaign returns `besiegedBy` for the save id and `null` for `settlement.id`, so `resolveMapDress` yields `state: null` for a besieged member (the siege ring never lights, silently — the read is dormant-absent) and the painted seasonal severity disagrees with what `traditionsKernel.js:263` scored in 17 of 20 years; all twelve other `settlementWarStatus` call sites pass a save id. **N2 (CONFIRMED):** forking one sample twice as the same account yields a byte-identical world (`s_c428e8848a3fd616` both times) — F1's defect on the signed-in path. **N3 (CONFIRMED):** `settlement.id` folds `_seed` alone, so one typed seed at village, metropolis and another culture yields three different worlds under one id. THE TWO SHAPES PRICED: A (the save id as the realm's key) is already true everywhere except `mapDress` — its remainder moves no persisted byte and no golden (the dress layer is off the generation path) and carries one declared one-time display shift; B (fold the config into `settlement.id`) MOVES ALL 525 generator-golden hashes — executed: the golden hashes the whole payload and the id is inside it (`b74f4b8…` vs `3552a43…` without it) — and leaves a permanent mixed namespace with no migration door (`normalizeSettlement` mints only when the id is absent), buying the realm nothing. RULED (vetoable): **N1 IS BUILT — shape A's remainder and nothing else** (a chair ruling: no persisted byte, no golden; the shift declared): the FIX-P2 agent resumes as a BUILD lane (`fix-mapdress-namespace-2026-09-20` @5a3380e8d) — red-first pins (a besieged member's dress state non-null; the painted severity equal to the kernel's), the two reads asking by the save id at the chokepoint, and THE GUARD: a totality pin that every `settlementWarStatus` call site and every save-keyed ledger read passes a save id, never `settlement.id` — the population read from the producer; **shape B REFUSED**; **N2 → FIX-P1's hand-off** (its salt must cover a repeat fork by one identity, and the `forkSeedFor` truncation — item 5); **N3 CLOSED AS LATENT under the guard:** no consumer keys a cross-save structure on `settlement.id` (measured), the label stays seed-only (THE PROMISE: one seed, one identity), and the lane's plain words are recorded for the owner — 'type one exact seed and forge it twice at two sizes: two different towns the engine privately labels with the same identity; nothing in a realm confuses them today; giving them different labels would change the identity of every settlement the engine has ever made' — the chair keeps the status quo; re-open only if a consumer ever keys on it, which the guard now forbids. F2 is CLOSED in this queue as REFUTED-WITH-RECEIPT and re-docketed as N1 + N2 + N3 so the premise is not re-found. THE TEN NOTICED ITEMS, FATED: (1) `townMap/fabricRead.js`'s `fabricScarsOf`/`fabricRebirthsOf` — the urban-fabric mirror's key namespace measured in the N1 build, cured there if the same defect; (2) `campaign.regionalGraph.nodes[].settlementId` persisted and read by nothing, (3) the realm minting trade between a town and its own twin with both rails reading the same name, (4) `findTargetSave`'s NAME-matching fallback (`region/graph.js:409-418`) binding a neighbour link to whichever twin sorts first, (9) `galleryImportSettlement` keeping the sharer's `settlement.id` while stripping `_seed`, (10) `liveNeighbourEngagements` seeding a self-mirrored pair `xconflict:S:S:<rel>` → **FIX-P7 (NEW; realm legibility; measure-first; after the composition train):** a realm-side notice when two members share a `settlement.id`, reading `nodes[].settlementId` (the persisted field gains its reader — never dropped: a persistence shape is the owner's), a disambiguating suffix at placement, the name fallback measured for reachability and refused if a persisted link can reach it, the import-id decision WRITTEN at the `if (!out.id)` guard (the chair's decision: KEEP — an import is a copy of a shared world and its identity is the sharer's, THE PROMISE-consistent; the reason goes at the guard), the self-mirrored pair measured before the editor's neighbour surfaces ship; (5) → FIX-P1's hand-off; (6) `TIER_GATE.anon.preGenOptions === false` — is the exact-seed door hidden from an anonymous visitor → **REVIEW-P2** carries the one check; (7) the anonymous draft envelope surviving a reload into a signed-in boot (`draftOrigin: 'anon'`, `auth.user` set) → **FIX-P8 (NEW; measure-first):** `persistMerge`'s adoption rule for a boot with a user and an envelope, with REVIEW-P's noticed item 1; (8) the 'Exact seed' field is INERT for the main Generate Draft button (two of three attempts lost the typed seed with no notice; only the field's own Forge-seed button honours it) → **FIX-P6 (NEW; small; product):** Generate Draft honours a non-empty exact seed, or is disabled while the field is non-empty — the wizard's design notes read first; a field that looks like a config input and is not one is a falsehood the DM meets. Server torn down by PID (33074), no listener on 5233, the read tip clean.
- **FIX-P2 (BUILDING; `fix-mapdress-namespace-2026-09-20` @5a3380e8d):** N1's cure at the chokepoint + the save-id totality guard; composed at EM-T4.
- **FIX-P6 (NEW; small):** the Exact-seed field honoured by Generate Draft, or the button disabled while the field is non-empty. **FIX-P7 (NEW; measure-first; after T4):** realm legibility for twins (the notice, the suffix, the name fallback, the import-id reason at the guard, the self-mirrored pair). **FIX-P8 (NEW; measure-first):** the draft envelope across the auth boundary. **REVIEW-P2** gains the anonymous exact-seed-door check. **FIX-P1's hand-off** gains N2 and the `forkSeedFor` truncation.

## Amendments of 2026-09-20 05:22 EDT — TOOL-9 at its gate (one judgment accepted); FIX-D7 dispatched; FIX-T1 staged (ODQ §934.47 addendum 60)

- TOOL-9 (`lane-tool-9`, `tooling-9-2026-09-20` @5a3380e8d) finished every ungated act in one file, `tests/lint/sovereigntyLightingContract.walker.test.js` (+252/−1, staged by pathspec): `LIGHTING_CENSUS_PRINT=1` is wired ABOVE the five figure assertions of the census arm, so the whole tuple prints on the very run that reds, and the normal mode's executed sequence is byte-identical (the print block is skipped entirely when the variable is unset); ruling 2 needed no extraction — `measureCensus()` was already the one spelling and the print is handed the measurement the arm has already computed, so the estate is walked once per run; `censusRegisterDigest` (sha256 of the register's bytes), `censusMovement` (per-figure live / frozen / delta over `CENSUS_FIGURE_KEYS`) and `censusPrintLines` (lines, not stdout — testable); three self-tests in the file's own idiom inside the existing describe (so `suiteTitles` does not move) using exact structural equality rather than negatives, so the file stays at its measured exact zero un-anchored `.not.` sites; ruling 4's receipt idiom written into the header. Recon that shaped it: five instruments read this walker's source — `goldenFreeze.walker` enrols only `/^UPDATE_[A-Z_]+$/` by AST so the print variable cannot be called unclaimed; `testRatchet`'s `totalTests` is a scope FLOOR, not an exact pin; `proseFamilyContract` and `newsHeadlineContract` pin a historical census string in an untouched comment block; no `max-lines` reaches `tests/**`. ONE MEASURED CONTRADICTION, RULED NOT A STOP and ACCEPTED by the chair (vetoable): ruling 1 asked for 'the parked-roster delta by file', but the register stores `parked` as an INTEGER COUNT and carries no roster — there is nothing to subtract from; the print emits the LIVE roster with park reasons, sorted and labelled as live, and names the workflow that yields a true delta (print at base, print on branch, diff). Predicted delta for its batch to confirm: `files 2652 (=) · parked 383 (=) · credited 2269 (=) · titles 25046 (+3) · suiteTitles 6679 (=)`; register unchanged at `3fb7a7e8…6013b`; goldens unchanged; four gated batches written, including three negative-control mutants in its own new code, none of which writes the register. It waits its turn on the shared tier; the judgment travels with its hand-off. **FIX-D7 DISPATCHED at 05:22** into TOOL-9's freed seat (`lane-fix-d7`, `fix-generator-hygiene-2026-09-20` @5a3380e8d; kit `briefs/launch/FIX-D7.md`: the eight TOOL-8 items, each measured against the exact corpus vocabularies first and cured only behaviour-preserving with goldens byte-identical, else moved to FIX-D6's signed door; the eight dark guards untouched). **FIX-T1's brief is STAGED** (`briefs/launch/FIX-T1.md`: the 28 hand-built ruin replicas re-pointed at EM-B1e's one writer, a replica walker with a declared executed exemption marker, and A4's second preset-witness pin made to read the golden it mirrors) for the next free seat. SEATS: gate = EM-B1k2 + FIX-C2; working = TOOL-8a · FIX-P2 (build) · FIX-D7; paused in order: FIX-P1 · P3 · P4 · L1 · P5 · FIX-G · TOOL-2b · FIX-F2b · TOOL-3 · FIX-K1 · TOOL-7a · TOOL-9; CURE-I awaits the slot.

## Amendments of 2026-09-20 05:27 EDT — ⭐ EM-B1k2 LANDED (the ninth landing) and flipped; the preamble's second amendment; FIX-W1, FIX-C2c, TOOL-11 chartered; §934.65 amended (ODQ §934.47 addendum 61)

- Built in the slot under seal (dispatched 04:25, resumed with the gate 04:43, committed 05:22) and VALIDATED by the chair with the diff read whole: exactly the eight §7 paths (+566/−4). ONE production line — the `tickStart` read in `advanceFactionDensity` becomes `asObject(asObject(item.save).settlement || item.settlement)`, the raw save roster, never the off-stage projection, so the R18 law never has a dissolution PROPOSED for a house whose one member is merely shelved (EM-B1k made the write base raw; this removes the habitat on the read); three comment-only base contracts on the two permanent roster writers (`settlementLifecycleFirstClass.js`'s dispersal, `successorNpc.js`'s `replaceOustedNpcs`) and the roads emit, each re-pointed at EM-B1k; the participation census widened on both axes (roots `src/domain/density` + `src/generators/density`; the `factionRosterOf` pattern; 41 → 44 readers with `factionLifecycle.js`, `applyDensityLaw.js`, `titularSuccession.js` each dispositioned; the false 'via-snapshot' blanket narrowed; two `-e` flags never a BRE alternation — BSD vs GNU grep); the CREATE suite drives shipped entries only — A1 observes the law by wrapping it (`vi.doMock`), A2 proves the chain deep-equal with and without the raw base plus dormancy by reference and DERIVES its 'two consumers and nothing else' claim from the mover's own source between found-and-ordered anchors (CURE-H's idiom), A4 the `fresh` fallback door, A5/A6 the two writers at their real triggers with their counterforces measured (`roster out 2 | the shelved soul absent` against 3 / present); the sweep plant (area 112, proved by cp backup/restore, md5 identical, `title_matches=1`) and the register row. RECEIPTS, every count line executed: red-first `2 failed | 3 passed` (A1 handed `["faction_dissolved::The Weavers","faction_dissolved::The Chandlers"]`; A4 two addresses where one is owed) then `5 passed`; tests/domain 99/99; densityLaw 151/151; the three goldens 25/25; `tests/lint` whole `1 failed | 2,775 passed` (the lighting census alone); voice 30/30; anti-vacuity + anchor 24/24; eslint 0; both typecheck ratchets at their exact floors; observed-shape 1,964 and writer-reach unmoved; validate 192; `check:packet` and `implementation:resume` exit 0 on all sixteen steps; goldens byte-identical; effective lines unchanged in all four src files (the suite 226 of 250). THE CHAIR'S PROCESS-LAW MEASUREMENT at the landing in the detached consist: `npm run build` exit 0, `verify:dist` `STRICT DIST OK — 59 files, 538 tests` (log in the kit). LIGHTING: the packet's stated delta EXACTLY — `files 2653 (+1) · parked 383 · credited 2270 (+1) · titles 25048 (+5) · suiteTitles 6680 (+1)`; not refrozen. THREE LANE JUDGMENTS ACCEPTED at the flip (vetoable): (1) the packet's verbatim optional-chain form reds BOTH typecheck ratchets (`item.save` is `unknown` in a `Map<string, Record<string, unknown>>`; EM-B1k's sister line survives only because its function is untyped) — the module's own narrowing helper agrees with the chain on every input; (2) no lane build — §3.2, §7.1 and §12 assign the byte proof to the chair's landing measurement, which ran (`successorNpc.js`'s zero-byte claim promoted from PLAUSIBLE by the STRICT DIST verdict); (3) two hand-keyed `pulseKernel.js:<n>` citations reddened `pulseKernelLineAddress.walker` and were re-spelled as content anchors. FLIPPED `bf0db7a7e` (the header, the manifest, both index rows — by the chair's robust flip script; 192 packets, 0 READY). **THE PREAMBLE'S SECOND AMENDMENT LANDED `e5f53ae95`** (addendum 39's debt): §P2 row 11 gains the sentence that a skipped `skipIf(!requireDistRead)` byte arm is not a pass (a receipt quotes the executed figure and the dist it read, or says the arm skipped), and `src/domain/density/factionLifecycle.js`'s membership in the DERIVED eager first-paint closure — not a hand list — measured true at EM-B1k2's compile and RE-EXECUTED by the amendment script itself (it imports `vite.config.js`'s `EAGER_FIRST_PAINT_MODULES`: 268 modules, a member); SHA-256 `b90a95b7…` → `16dfb96fa79320abc7dcfbdd8f5152b6aa66998c269287a951079df6c8223195`; every packet placed from here cites it. THE LANE'S EIGHT NOTICED ITEMS, FATED: (1) ⭐ A PRODUCTION PROSE DEFECT — `dissolvedBeat` renders `the ${houseName}` over a seat key that already carries its article ("Ashford: the The Chandlers is no more"; the same shape in `interregnumBeat`, `emergenceBeat`, `foldBeat`), reaching the player as Wizard News → **FIX-W1 (NEW; product; measure-first):** the four beat composers take the estate's article-aware case ladder (FIX-G2's `tokenCase` idiom); which goldens or dormancy pins carry the doubled article is measured first — a moved golden is the OWNER's door, so **§934.65's ask gains a third item, FIX-W1 (recommend YES: a doubled article in Wizard News is a falsehood the DM reads)**; built TO THE GOLDEN and stopped otherwise; (2) the lighting walker fails fast at `files` so a lane cannot measure the tuple → CLOSED: TOOL-9 (at its gate) is exactly that cure; (3) `pulseKernelLineAddress.walker`'s own header documents a DELIBERATELY DEFERRED sibling class (`<module>.js:<n>` addresses live for `seasons.js`, `candidateEvents.js`, `warDeployment.js`, `navalKernel.js`, `roadsKernel.js`, `informationStatecraft.js`, …) → **FIX-C2c (NEW; after FIX-C2b):** the walker widened to the named sibling modules with their hand-keyed addresses re-spelled as content anchors, the cure B1k2 itself applied; (4) `scripts/mutation-sweep.sh` cannot be run whole on a shared tree because its revert is `git checkout -- <file>` → **TOOL-11 (NEW; tooling; small):** the sweep's revert made shared-tree-safe (a cp backup with an md5 check, restored by content — the mechanism every lane already uses by hand), the `MUTATED_FILES` dirty-refusal kept; (5) `npcVerdictPulse.js` sits in the census QUARANTINE undispositioned while being permanent writer 2's direct caller → **EM-B1f's pre-proof** (re-run after this landing) banks its disposition row and burns the ceiling 7 → 6; (6) A5 and A6 cannot be red-first by construction (written-contract arms on comment-only files) and (7) `tests/lint` whole (~2 min) caught a red the packet's twelve sealed steps did not, yet cannot be a sealed check while the lighting walker reds by design → **the THIRD preamble/standard amendment, owed at EM-B1f's placement sitting:** a 'written-contract arm' is proved by its counterforce measured, not red-first; and a `tests/lint` step that EXCLUDES the lighting walker (or reads TOOL-9's print mode) joins the sealed `checks`; (8) the legacy save shape, the name join (re-proved in A6 over its own fixture: 0 duplicate display names across 5,171 NPCs) and the two already-raw proposal maps stay CLOSED by the pre-proof's measurement. The slot's next acts in order: EM-P2 v4 placed at the amended tip (the new hash cited; the chair's cuts) → CURE-I cherry-picked into the slot ("the slot and the gate are yours") → the FOURTH refreeze (B1k2's `+1/+0/+1/+5/+1` + CURE-I's `+4` titles + any P2-placement title, measured whole) → RUN 20 → EM-P2 v4's build dispatched.
- **FIX-W1 (NEW; product; measure-first; the signed door if a golden pins it):** the four Wizard News beat composers (`dissolvedBeat`, `interregnumBeat`, `emergenceBeat`, `foldBeat`) compose their article through the estate's case ladder so a seat key that carries its article is never doubled.
- **FIX-C2c (NEW; after FIX-C2b):** `pulseKernelLineAddress.walker`'s deferred sibling modules brought under the walker with content anchors. **TOOL-11 (NEW; tooling; small):** the mutation sweep's shared-tree-safe revert. **EM-B1f's pre-proof** banks `npcVerdictPulse.js`'s census row (ceiling 7 → 6). **The third preamble/standard amendment** is owed at EM-B1f's placement sitting.

## Amendments of 2026-09-20 06:45 EDT — EM-P2 v4 placed READY; the chair's placement hazard recorded; the usage limit's four cut lanes resumed; CURE-I into the slot (ODQ §934.47 addendum 62)

- **EM-P2 VERSION 4 PLACED READY at `e5f53ae95`** (`913054cbf`): the placement script's window from ad7ddf2c9 EMPTY over its 18 non-register paths; the shared register moved only by four other packets' rows (`statusUnionTotality.walker`, `irreversibleRawRoster.contract`, `galleryScannerMirrorTotality`, `participationWriteBase.contract`); all 19 required symbols resolve; the four CREATE targets absent; `src/domain/generation/` absent (§5 VF-14); no non-terminal reserver; the NEW preamble hash `16dfb96f…3195` cited; the chair's cuts — the stale 'EM-B1d is building' header paragraph replaced by a history note, the anti-vacuity walker added to its `checks` (its `*.contract.test.js` file opts into that walker's five rules); the kit's `packet-merge.mjs` merged the entry (+478 manifest lines, the 663-line packet). THEN TWO INDEX-ROW CURES (`68b11b642`, `fad5af302`), BECAUSE THE CHAIR COMMITTED THE PLACEMENT PAST A VALIDATOR DISAGREEMENT: `validate:packets` printed `EM-P2 status disagrees with index: manifest=READY index=LANDED` and the commit was not gated on its exit — `parseIndexPacketStatuses` takes the FIRST status of `PACKET_STATUSES`' declared order found ANYWHERE in the cell after the link, case-insensitively as a standalone token, so the row's phrase 'depends on EM-P0 (LANDED)' and then its reword 'itself landed at 429ceed54' both read as LANDED; the cell now carries READY alone and `valid: 193 packets (1 READY)`. THE CHAIR'S OWN RULE, INTO THE RUNBOOK AND MEMORY THE SAME TURN: a placement or flip commit is gated on `validate:packets` printing `valid:` in the SAME command (never commit past a disagreement), and a READY/LANDED row's status cell carries no second status word in any case. **THE USAGE LIMIT (reset 06:40 EDT) CUT FOUR RUNNING LANES at ~05:2x:** FIX-C2 while committing — its two commits `9af544fe7` (the walker) and `36c6b5a5c` (58 stale citations re-addressed) are on its branch, tree clean, only its report owed; TOOL-8a mid adversarial proof (4 rows dirty; ordered to verify by sha256 that no planted mutant survived); the FIX-P2 build early (1 row); FIX-D7 mid-measurement (clean, 'several items split from TOOL-8's framing — notably `harbour`'). All four RESUMED FROM THEIR OWN TRANSCRIPTS at 06:43 with verify-state-first orders (no fresh agents needed — the transcripts survive a rate-limit death; the recovery class is recorded). CURE-I handed the slot and the gate at 06:45 on tip `fad5af302`. THEN: the FOURTH refreeze (B1k2's `+1/+0/+1/+5/+1`, CURE-I's `+4` titles, measured whole) → RUN 20 → EM-P2 v4's build dispatched under seal (its batches pause behind run 20) → the paused lanes two at a time.

## Amendments of 2026-09-20 06:47 EDT — FIX-C2 complete on its branch; three judgments accepted; FIX-C2b's charter grown (four commits); FIX-T1 dispatched (ODQ §934.47 addendum 63)

- FIX-C2 (`fix-citations-2026-09-20` @63e40fe57) is COMPLETE on its branch: `9af544fe7` the source-citation walker RED on 38 across 5 files (ARM 1 naming the seven `subsystemRowsWar.js` rows, ARM 3 printing 31); `36c6b5a5c` 58 addresses re-addressed across 32 files and the gate green (7 → 0; 31 → 2); the EOF arm gate-wired over `src`/`tests`/`scripts` with no baseline, over docs-LIVE under a 47-row shrink-only baseline; the symbol arm report-only; `tests/lint` whole `1 failed | 2,787 passed` (the lighting census alone); goldens byte-identical; eslint 0 on 33 files; receipt `FIX-C2.receipt.md` (kit `findings/FIX-C2/`). THREE JUDGMENTS, ALL ACCEPTED (vetoable): (1) the archival rule the ruling named ('anything whose header pins a sha', read as a hex scan) swallowed 269 of 493 documents including live design specs — replaced by the estate's own marker, the blockquote HISTORICAL banner per `docs/README.md`'s index plus a narrow `**Snapshot base:**` pin: 29 files, 377 excluded citations, the roster committed and guarded by its own arm; (2) the symbol arm has a real false-positive rate — of 31 findings one convicted a TRUE citation (`wiring-census.mjs:349` → `defenseGenerator.js:189-191` with `economicGates` backticked beside it) and the automated cure broke it; caught only by reading the staged diff line by line, reverted, and recorded in the walker's header as the standing reason that arm never gates (two range citations audited beside it were genuinely stale — 'ranges are safe' is not the rule); (3) ⭐ AN INSTRUMENT HAZARD: under vitest 4.1.8's default reporter a PASSING test's `console.log` is DROPPED — the report-only arm was invisible as first written; `process.stdout.write` survives both reporters → every report-only arm in the estate written with `console.log` is silent today; INTO THE BRIEFS AND MEMORY THE SAME TURN (a report-only arm writes with `process.stdout.write`; an existing arm's silence is measured before it is trusted — slotted to TOOL-6b's admission dock as a census of report-only arms). THE SEVEN NOTICED ITEMS, FATED: (1) `subsystemRowsWar.js` carries ~33 further IN-RANGE stale addresses (the `warDeployment.js:1280–1291` flag-read block off by exactly 811 after the WR-7b decomposition; `computeAllyRelief` / `composeDefenderWillScore` moved to `warCapacityReads.js:74` / `warSiegeVerdict.js:121`) — shipped certification evidence the EOF arm cannot see → **FIX-C2b's first commit** (already chartered: the certification file's whole citation set re-addressed by symbol); (2) the docs-live baseline's 47 rows across 25 documents (`GEOPOLITICAL_WAR_LAYER.md` alone 12) and (6) two documents asserting EXACTNESS about false addresses (`DESIGN_FP_ARCH_INT.md:31` '(exact)' off by 25; `WF-SUBSTRATE.md:192` 'ALL EIGHT AT AN EXACT ANCHOR' with `pantheon.js:405` off by 71) → **FIX-C2b's second commit** (the docs-live rows cured and the baseline burned to zero; the exactness claims corrected or withdrawn); (3) 377 past-EOF citations in the 29 archival files → CLOSED — archival by the estate's own marker, deliberately dark, guarded by the roster arm; (4) 113 target paths absent from the tree (357 citations; `SettlementMapPane.jsx` ×31, `useTownMapPresentation.js` ×28, `writers.js` ×27) — deleted, renamed or on another branch is unanswered → **FIX-C2b measures each against history** (`git log --all --diff-filter=D`, renames) and re-points or marks archival, the top three by hand; (5) the bare `:NNN` citation form (the path inherited from earlier in the sentence) is invisible to the walker; one instance re-addressed by hand → **FIX-C2b's fourth arm, measure-first** (the population of bare `:NNN` after a path in the same sentence); (7) `docs/content/wiring-census.json` moved two sha lines → a composition note for EM-T4 (re-run the script on conflict). FIX-C2's seat is FREE → **FIX-T1 DISPATCHED at 06:47** (`lane-fix-t1`, `fix-ruin-replicas-2026-09-20` @fad5af302; kit `briefs/launch/FIX-T1.md`; told that the register at its base is stale by B1k2's delta and to measure its base tuple before its first edit).
- **FIX-C2b (RE-CUT; after FIX-C2 composes):** (1) `subsystemRowsWar.js`'s whole citation set re-addressed by symbol (~40 rows, the 811-offset block, the two moved functions); (2) the 47 docs-live baseline rows cured and the baseline burned to zero, the two false 'exact' claims corrected; (3) the 113 absent target paths measured against history and re-pointed or marked archival; (4) the bare `:NNN` form measured as a fourth arm. **TOOL-6b's brief** gains a census of report-only arms written with `console.log` (silent under the default reporter).

## Amendments of 2026-09-20 06:51 EDT — CURE-I landed in the slot; the fourth refreeze; run 20 launched; EM-P2 v4 building under seal (ODQ §934.47 addendum 64)

- **CURE-I LANDED IN THE SLOT `7c233db55`** (cherry-picked onto `fad5af302`; the chair proved the patch IDENTICAL to the validated branch commit `b866847bf` — both files byte-equal at the two commits; exactly two files, +162/−16): re-proved there on the shared tier — the two suites `15 passed (15)`, `tests/security` WHOLE `147 passed | 1 skipped` files / `1,834 passed | 1 skipped` tests — a ZERO delta against the lane's own figures (EM-B1k2 added nothing under `tests/security`: measured, not assumed); goldens unchanged; the lane's plain walker run confirmed the census arm short-circuits at `files` (expected 2653 to be 2652 — B1k2's file), which is TOOL-9's cure at its gate. Run 19's one red is cured on the integration branch. **THE FOURTH REFREEZE `141a1d775`:** through the walker's own door, the measured tuple `2653 · 383 · 2270 · 25052 · 6680` = the frozen `2652·383·2269·25043·6679` + EM-B1k2's `+1/+0/+1/+5/+1` + CURE-I's `+0/+0/+0/+4/+0`, EM-P2 v4's placement moving no figure — EXACTLY the expected tuple, measured whole (the commit was gated on the match; a difference would have been held for inspection). **RUN 20 = train EM-T3's terminal** at `141a1d775`, bare and detached in the consist (launched 06:49:02 EDT; the log at the scratchpad root; a Monitor in the chair's session). GREEN ⇒ T3 CLOSES at last (it carries EM-B1k, EM-B3c, EM-B1k2, CURE-E…I and the two docs amendments) and the push chain's recommended moment has arrived. **EM-P2 v4's BUILD DISPATCHED UNDER SEAL in the slot at 06:51** (`LANE-EM-BUILD.md` with every 2026-09-20 addendum; the NEW preamble hash; the anti-vacuity walker among its instruments and checks; the directory-whole rule for its `tests/lint` and `tests/generators` CREATEs; its batches pause behind run 20). THE GATE ORDER after run 20 exits: P2's batches resume first with one paused lane beside them (the shared tier, two at a time) — FIX-P1 → P3 → P4 → L1 → P5 → FIX-G → TOOL-2b → FIX-F2b → TOOL-3 (exclusive, alone) → FIX-K1 → TOOL-7a → TOOL-9 → the resumed lanes as they pause (TOOL-8a, FIX-P2, FIX-D7, FIX-T1). SEATS: gate = run 20; the slot = EM-P2's build (paused at its first batch); working = TOOL-8a · FIX-P2 (build) · FIX-D7 · FIX-T1. CURE-I's standing item is TOOL-10 (addendum 57).

## Amendments of 2026-09-20 06:52 EDT — TOOL-8a at its gate; four judgments ruled (ODQ §934.47 addendum 65)

- TOOL-8a (`lane-tool-8a`, `tooling-8a-2026-09-20` @5a3380e8d; resumed after the limit with its state verified — no planted mutant survived the cut, both touched files byte-identical to their pre-plant backups by sha256 and `cmp`, the goldens unmoved; its fourth dirty row was a plain-node dry-run harness, removed) finished every ungated act: `tests/lint/darkGuardRegistry.js` (2,177 lines, **1,733 rows, nothing hand-typed**), `tests/lint/darkGuardCensus.walker.test.js` (594 lines, 3 suites / 11 titles) and one `self-proving-meta` rationale row (no `MUTATED_FILES` entry; `uncoveredBaseline` unmoved at 186); three files staged by path. THE CENSUS BY DISPOSITION, born classified at its base: LIVE 632 · UNBOUND 460 · precedence-shadowed 344 · corpus-absent 259 · catalogue-candidate 24 · KNOWN_DARK_PENDING_CURE 10 · idempotent-normaliser 2 · negated-dedupe 1 · defensive-escape 1; all ten pending-cure rows resolve to a site and every one is dark. PRE-PROOF EXECUTED UNGATED (plain node through a vitest shim): 11 passed / 0 failed, and four planted mutants each reddened by name — a deleted row (4 arms), the FIX-D4 anchor reclassified LIVE (THE LAW reds quoting the corpus near-miss `"On the payroll of the thieves' guild as a silent informant"`), a normalization flip, and ⭐ FIX-D6 SIMULATED IN SOURCE, which reddens the binding arm on 44 rows and names each for hand retirement; every plant restored by `cp` and verified by `cmp` + sha256. FOUR JUDGMENTS, RULED (vetoable): (1) the registry's home is `tests/lint`, not `src/generators` — a MEASURED refusal (`eslint.config.js` sets `max-lines: 800` over `src/generators/**` and the only escape is a row in the shrink-only exact-set `scripts/.size-baseline.json`) — ACCEPTED: a generated register lives with the walkers that read it; (2) THE EIGHT became TEN rows (D1 is one defect with three dark literals) — ACCEPTED: a row is a literal at a site; (3) an eighth disposition `UNBOUND` beside `LIVE` (460 rows whose receiver could not be bound to a corpus field) — ACCEPTED ON ONE CONDITION, carried in its hand-off: `UNBOUND` is a SHRINK-ONLY ratchet (its count may only fall; a new unbound guard reds) with its meaning written in the closed vocabulary — never a dumping ground; (4) `why` owed per judgment class rather than per row — ACCEPTED (1,733 rows; the class carries the reason, the row carries the class). ONE CLASS MOVEMENT, recorded as the first executed instance of the boundary the walker's header declares: `npcGenerator.js:1360 hasInst('thieves')` reads LIVE statically (that helper's roster IS lowercased, so the literal can reach `Thieves' guild chapter`) where TOOL-8 measured it never matched at runtime — both true; only the runtime miss is FIX-D4's defect. TOOL-8a waits its turn on the shared tier; its conditions travel with its hand-off. Its seat is free → the EM-R family's first pre-proof fills it (next).

## Amendments of 2026-09-20 06:55 EDT — the FIX-P2 build at its gate (four judgments accepted; item 1 closed); EM-R0d's pre-proof dispatched (ODQ §934.47 addendum 66)

- The FIX-P2 BUILD (`fix-mapdress-namespace-2026-09-20` @5a3380e8d; resumed after the limit) is at its gate with ten paths staged (+437/−19): `townMap/mapDress.js`'s two reads ask the save-keyed ledgers by the SAVE id, threaded from the scene-compile chain (`sceneCompileInput.js → sceneLiving.js → compileTownSceneManifest.js → townSceneExport.js`); five pins in `mapDressResolver.test.js` (the siege pin, the severity pin, the dormancy pin among them); THE GUARD `tests/lint/ledgerKeyNamespace.walker.test.js` — every save-keyed ledger read in `src` keyed by a save id, the population read from the producer, with four in-body controls; `townScenePlayerSafe.test.js` and a manifest row; goldens byte-identical before and after. FOUR JUDGMENTS, ALL ACCEPTED (vetoable; each with its reversal written in the lane's evidence §7): (1) the key is THREADED from the caller, and the graph-node match the chair had offered as the possibly-smaller diff is REFUSED by measurement — two saves grown from one seed share both `settlement.id` and their name (receipts R3/R6: one realm, two nodes, one `settlementId`), so a match would hand one twin the other's war, manufacturing the very collision F2 imagined; (2) UNKEYED ⇒ DORMANT rather than a fallback to `settlement.id` (a fallback keeps the defect alive behind a default and forces the guard to permit it; three fixtures had to name their key, no assertion changed); (3) the key is CONSUMED at the scene-compile seam and never retained on the envelope (retaining it would add a `TOP_LEVEL_KEY` to an audience-authorization boundary and move `inputDigest` for every caller); (4) the walker's `rationale` row rather than a sweep plant, because `check_caught`'s revert is `git checkout --` over the shared worktree — the precedent of `customContentCharsetWiring`, `statusUnionTotality`, `dialogExit`; the four in-body controls are the substitute and run on every gate (a plant may follow once TOOL-11 makes the sweep shared-tree-safe — a note for TOOL-11's brief, not a debt of this lane). ITEM 1 CLOSED with the receipt: `townMap/fabricRead.js` reads no id of any namespace (`fabricStocksFor` / `fabricDriftOf` / `fabricScarsOf` / `fabricRebirthsOf` / `hasFabric` take the settlement alone) — not the same defect. Its hand-off carries the four acceptances. **EM-R0d's PRE-PROOF DISPATCHED at 06:53** into its freed seat (sharing `read-tip-141a1d775` read-only; conditional facts marked where they assume R0a's landing). SEATS: gate = run 20 (the ratchet holds the lock since 06:52); slot = EM-P2 (sealed, paused); working = FIX-D7 · FIX-T1 · EM-R0a pre-proof · EM-R0d pre-proof; paused at the gate in order: FIX-P1 · P3 · P4 · L1 · P5 · FIX-G · TOOL-2b · FIX-F2b · TOOL-3 · FIX-K1 · TOOL-7a · TOOL-9 · TOOL-8a · FIX-P2 (build).
- **TOOL-11's brief gains:** once the sweep's revert is shared-tree-safe, the walkers that took a `rationale` row for that reason alone (`ledgerKeyNamespace`, `customContentCharsetWiring`, `statusUnionTotality`, `dialogExit`) may take a real plant.

## Amendments of 2026-09-20 07:01 EDT — FIX-D7 at its gate (three cured, three closed, N13 to FIX-D6, N4 ruled); EM-R0b v3's pre-proof dispatched (ODQ §934.47 addendum 67)

- FIX-D7 (`fix-generator-hygiene-2026-09-20` @5a3380e8d; resumed after the limit) measured all eight items at its base — the exact corpus vocabularies reproduce TOOL-8's §0 figures exactly (`instNames=231 svcNames=375 … allShort=8287`, 525 rows, 0 errors) — and is at its gate with six files staged by path, no new test file (both directories are enforcer dirs), the dark guards D1–D8 untouched, and BEHAVIOUR-PRESERVATION ALREADY PROVEN UNGATED: the 525-row corpus regenerated at the cured tree is byte-identical to the base (`399b32c0f647…111596` both sides). CURED (3): N2 — `npcGenerator.js`'s BOTH dead token rewrites retired (`{commodity}` AND `{faction}` match 0 of the 108 strings `substitute()` reads and 0 anywhere in `npcData.js`; the live substituter is `replaceTokens` at :742 over a different table); `grain` bounded to `\bgrain\b` (one string carries it, none inside a longer word); the `topFaction` guard deliberately kept (narrowing it would return the shared `NPC_FACTION_GOALS` object instead of a spread copy); N3 — `serviceClassifier.js`'s header corrected (280 keys; 124 of 375 covered; 251 fall through; the heuristic declared LOAD-BEARING) with a pin that asserts NON-totality, never a frozen count, so it cannot fight the walker's shrink-only burn-down; N9 — `institutionProbability.js:82` → `armou?ry`, red-first captured against the real pre-cure module (`Armoury` 0.2 vs `Armory` 0.4; both 0.4 after). CLOSED BY MEASUREMENT (3): N5's premise REFUTED — the de-slug fires (`importChannel "mountain pass trade"`, 1 of 525) and is already pinned at `foodImportChannels.test.js:99-112`; N7 is NOT a fossil — `historyGenerator.js:464` and `:465` both substitute to `govFaction`, so one template away the guard fires → registered `defensive` (TOOL-8a's vocabulary); N8's normaliser is correctly placed — its producer is `rulingStructure.js:500` behind prosperity `Wealthy`/`Thriving` and the corpus tops out at `Prosperous`. MOVED TO THE SIGNED DOOR (2): ⭐ N13 IS A REAL DEFECT, not a rarity — `Woodcutter's camp` is SELECTED in all 12 thorp+forest rows at 100 % final likelihood and then STRIPPED by `isolationGenerator.js:169-178`'s DEFAULT-DENY `isTradeInst` (its tags `['economy','timber']` are on neither `TRADE_TAGS` nor `keepTags`), so timber-felling is deleted as 'requiring external supply chains' — the only forest-gated entry in the catalogue → **FIX-D6 gains it** (the isolation pass admits the timber tags; 12 rows' output moves; under §934.65's signature); N4 — `professional guard`: RULED by the chair — KEEP the rung and let TOOL-8a register it `corpus-absent` (retiring is output-neutral but would settle a product question by deletion; a registered dark rung is visible, a deleted one is forgotten); `healer` must NOT be touched — catalogue-reachable via `Healer (divine, 1st level)` (the lane's finding, accepted). TWO BRIEF CORRECTIONS accepted: N5 as above; N9's site list was wrong — of `fortif · bandit · levy · harbor · feudal · planar trader` only `fortif` and `bandit` live in that file, the rest in `structuralValidator`, `foodBalance`, `upgradeOpportunities`, `factionDynamics`, `computeActiveChains`, `isolationGenerator`, each dispositioned individually in the lane's evidence. Its hand-off carries: no edit for N4; N13 is FIX-D6's. **EM-R0b v3's PRE-PROOF DISPATCHED at 07:01** into its freed seat (sharing `read-tip-141a1d775` read-only). SEATS: gate = run 20; slot = EM-P2; working = FIX-T1 · EM-R0a · EM-R0d · EM-R0b pre-proofs; paused in order: FIX-P1 · P3 · P4 · L1 · P5 · FIX-G · TOOL-2b · FIX-F2b · TOOL-3 · FIX-K1 · TOOL-7a · TOOL-9 · TOOL-8a · FIX-P2 (build) · FIX-D7.
- **FIX-D6 (AMENDED):** its signed act carries (a) the shared lowercase institution predicate (D2–D8), (b) the timber camp's tags admitted to the isolation pass's keep set (N13); both under §934.65.

## Amendments of 2026-09-20 07:15 EDT — FIX-T1 at its gate; seven items fated (EM-B1j and EM-B1h gain rows); EM-R0f's compile dispatched (ODQ §934.47 addendum 68)

- FIX-T1 (`fix-ruin-replicas-2026-09-20` @fad5af302) is at its gate. THE CENSUS, its denominator reconciled EXACTLY against EM-B1e: `status: 'ruined'` = 28 occurrences in 17 files at `8f714cf3c^` (R7's '28 in ~18'); 30 in 18 files at its base, the +2/+1 being EM-B1e's own suite; of the 30, three are comments, two the writer's own suite, **25 live sites in 16 files** — split by the SHAPE read off `ruinInstitution`'s body (`status:'ruined'` plus `_worldPulseInactive` / `_worldPulseEconomyClosed` / `worldPulseFate` / `remnantReason`): **11 true STAMPS in 9 files RE-POINTED** at the one writer (each file gaining one local helper calling `ruinInstitution(inst, { reason: 'Destroyed outright by the disaster.', fate: 'destroyed_by_disaster' })`, the calamity DESTROY site's own two words — the writer defaults neither; no assertion weakened, no title changed) and **14 bare status-name probes OUT OF CLASS** (they write the status alone to exercise a reader's branch — re-pointing them would change what they prove); exempt-with-reason 0. THE WALKER `tests/lint/ruinShapeReplica.walker.test.js` (1 describe, 7 straight-line tests) READS THE SHAPE OFF THE WRITER (it parses `ruinInstitution`'s returned literal), so EM-B1h moves its subject without touching the file; proved under a scratch shim: 7 arms, 0 failures; the live positive control convicts `calamityKernel.js:246` with exactly the four derived keys; RED-FIRST over a `git archive` of the base convicted all 11 sites by file:line and greens on the cured tree across 2,709 scanned sources; a mutant rewriting the writer's status made the 11 stop reading as replicas while the control stayed green; the exemption marker has a ZERO live population, proved by three planted directions; one real defect caught before the gate — a first draft convicted ITSELF seven times on its own header (cured by classifying LINE vs BLOCK comments separately); a `rationale` row (2,570 chars) beside `ruinFilterRoster`'s; 710 ≥ the floor 686, uncovered 186 = baseline. A4's SECOND PIN took the preferred option: `ruinInstitution.test.js:292` now reads the `preset-lighting-witness` row of `tests/fixtures/.golden-freeze-register.json`, guarded so 'the pin follows' cannot decay into 'the pin reads whatever' (the row must exist, its `path` the witness golden, its `sha256` 64 hex); the witness suite's law forbids WRITING through the record door, not reading the row. Goldens unmoved (three hashes); no `src/` byte touched; all ten files parse under espree. ONE DEVIATION, ACCEPTED (vetoable): the CREATE is held in scratch and out of the worktree so its first batch measures a BASE tuple (the staged re-points provably move no census figure); predicted delta `+1/+0/+1/+7/+1`, confirmed by an espree binding count. FATES OF THE SEVEN NOTICED ITEMS: (1) `ruinInstitution.test.js:290-291`'s other two A4 literals are the same shape → **FIX-T1's own hand-off**: route both to their register rows the same way as an ungated edit before its batch (or say why a capture-arm golden legitimately differs and leave it); (2) the `remnant`/shell shape has FOUR writers and no single writer (`institutionLifecycle.js:1021,:1063`, `magicRegimeLifecycle.js:358`, `tierOutcomeApply.js:166`, each spelling `remnantReason`; replicas in five tests) — EM-B1e's class one door over → **EM-B1j's compile** (already queued for `'remnant'` + `LIFECYCLE_CLOSE_FATES`) carries 'one exported remnant writer, EM-B1e's ruling applied'; (3) `destroyed_by_disaster` was one occurrence repo-wide and is now spelled in nine files → **EM-B1h** (the closed fate vocabulary: tests import the constant; a shared `tests/helpers/` ruin helper collapses nine to one when the constant exists — declined here, where a second CREATE would double the directory-whole obligation); (4) `ruinFilterRoster.walker.test.js` carries `uncovered` beside its new sibling's rationale → **FIX-T1's hand-off** (optional): a `rationale` row IF the walker is self-proving in the same shape, else leave it; (5) espree reuses ONE `Identifier` node for `imported` and `local` in a shorthand `ImportSpecifier`, so a naive AST visitor double-counts bindings — and the lighting walker's park rule turns on binding counts → a memory row (the fold of instrument hazards); (6) `INSTITUTION_STATUSES` re-confirmed live, already EM-B1e R4 on EM-B1a's pre-proof — no action; (7) A4's cured pin is a narrower, packet-scoped duplicate of `goldenFreeze.walker:421`'s estate arm → KEPT (it is what makes 'one place re-records' true inside EM-B1e's own suite; a duplicate that names its packet's contract is cheap and honest). FIX-T1 waits its turn on the shared tier. Its seat is free → **EM-R0f's COMPILE DISPATCHED at 07:15** (kit `briefs/launch/COMPILE-EM-R0f.md`; `read-tip-141a1d775`). SEATS: gate = run 20 (its ratchet GREEN — no regressions across 33,604 tests, ceiling 0; `verify:dist` `STRICT DIST OK — 59 files, 538 tests`; the exit line pending as of this stamp); slot = EM-P2; working = R0a · R0d · R0b pre-proofs · R0f compile.
- **EM-B1j's compile** carries one exported remnant writer (four writers today). **EM-B1h** carries the fate constant imported by tests and the shared ruin helper.

## Amendments of 2026-09-20 07:16 EDT — ⭐ RUN 20 GREEN: train EM-T3 closed on 141a1d775; the push chain's recommended moment; the gate opened to EM-P2's build and FIX-P1 (ODQ §934.47 addendum 69)

- **RUN 20 EXITED 0 at 07:09:22 EDT** — train EM-T3's terminal at `141a1d775`, bare and detached in the consist: every validate step green; both typecheck ratchets at their floors; lint; `[test-ratchet] OK — no test regressions (0 known failure(s) of 33,604 tests, ceiling 0)`; `npm run build` green; `verify:dist` `STRICT DIST OK — 59 discovered/reported file(s), 538 test(s), zero failed/non-run/uncollected/missing/extra/duplicate rows` (the log in the kit). **TRAIN EM-T3 IS CLOSED** — after runs 17 (a marker-slice scanner), 18 (the `contract` token) and 19 (a directory's meta walker), each red by one instrument-collision and each cured (CURE-E … CURE-I), the terminal is green on a tip that carries EM-B1k (the data-loss cure), EM-B3c (migration 203, the three-ledger security gap), EM-B1k2 (the density lane's irreversible reads raw by construction), CURE-A…I, the preamble's second amendment, EM-P2 v4 placed READY and four lighting refreezes. ⭐ **THE PUSH CHAIN'S RECOMMENDED MOMENT HAS ARRIVED (§934.42, §934.62):** the chair RECOMMENDS the owner resume the chain now on `141a1d775` — push the consist branch, the ledger and a sealed kit, PR → master, merge on green CI (Vercel builds behind the ignore-build gate), then `supabase db push` for migrations 201–203 and the applied-head bump — because the data-loss cure and the security gap's cure reach production only through it, and every terminal red since the last green was an instrument, never the product; the chair does NOT push (the owner's word, by the chair's own reading of the delegation; the ledger's push is a separate word). Until the word, the branch keeps moving: EM-P2 v4's build is in the slot. THE GATE, freed at 07:09 and opened two at a time by the chair's judgment: **EM-P2's build and FIX-P1 hold the shared tier** (P2 told to run its batches as it reaches them; FIX-P1's hand-off carries N2 — a repeat fork by one identity — and the `forkSeedFor` truncation, plus every law added since it paused, and the instruction to measure its base tuple first because its base predates two refreezes); then in the pause order FIX-P3 → P4 → L1 → P5 → FIX-G → TOOL-2b → FIX-F2b → TOOL-3 (exclusive, alone) → FIX-K1 → TOOL-7a → TOOL-9 → TOOL-8a → FIX-P2 (build) → FIX-D7 → FIX-T1, each with the rulings its record names. A push notice goes to the owner with this addendum. SEATS: gate = EM-P2 + FIX-P1; working = EM-R0a · R0d · R0b pre-proofs · EM-R0f compile.
- **TRAIN EM-T3 — CLOSED** (its terminal green at `141a1d775`). **TRAIN EM-T4 (the composition train) opens** with EM-P2 v4's build in the slot and the paused lanes' gate turns; its terminal is run 21.

## Amendments of 2026-09-20 07:19 EDT — EM-R0a pre-proofed READY-able; seven rulings; the register bottleneck ruled (sequential placement); EM-B1f's pre-proof re-run dispatched (ODQ §934.47 addendum 70)

- EM-R0a version 2's pre-proof (read tip `141a1d775`; plain-node probes and the two read-only registers only; the read tip clean at entry and exit) reports **READY-able at `141a1d775`** — no premise refuted, nothing blocked. The J-T1 window: 33 commits moved under the packet and exactly ONE of its six paths moved — `scripts/mutation-coverage-manifest.json`, by append only (+19/−0) — so every `requiredSymbols` path is byte-identical to the compile's base; all four symbols re-found at the compile's lines (77 · 76 · 60 · 36); both CREATE targets absent. THE CHAIR'S STANDING INSTRUCTION DISCHARGED: the `history` consistency group (addendum 38 ruling (1)) — `history.age ≡ history.founding.age` in 63/63, a number, root and both members resolving 63/63; G8 `history-age` declared with the two-member list `['age','founding.age']` (`members: null` would have been a defect — `history` also carries three order-bearing collections); keys-by-path unchanged (`history` is already READING); the `recordInvariants` guard is NOT this packet's — by the ruling's own division `V-HISTORY-AGE` DETECTS (EM-R0b v3, accepted) and this group PREVENTS, recorded as CANNOT-CATCH row 12. FOUR FACTS CHANGED, ONE CONTRADICTION FOUND INSIDE THE PACKET: A6's 'no root nested inside another's' would have reddened on the packet's own register (`economicViability.metrics.foodBalance` nests inside `economicViability`, executed) — the arm is re-cut to COVERED PATH SETS, under which everything is disjoint (design §22.3 item 2 built the member list for that pair); the change-path reservation changed owner (EM-B1d landed; EM-P2 READY now holds the register); Q4 discharged BY EXECUTION (the leaf's only property access is `Object.freeze`, whose receiver resolves to no shape; 7,301 live occurrences mint zero findings — no migration-bundle door); a governing enforcer was missing — `contractTestAntiVacuity.walker.test.js` governs `tests/lint/*.test.js` — added, plus `tests/lint` whole as a §10 instrument, its Rule 2 a live risk for six of seven exhaustive-claim titles, cured by a construction rule. Also corrected: two dispatch citations off by one (`:889`→`:890`, `:891`→`:892`); 'seven `SURFACE_ROOTS`' is six. Bundle delta 0 B in every budget with `EAGER_FIRST_PAINT_MODULES` re-measured by import (268) as §P2 row 11 demands; 164 effective lines against ≤ 210; lighting as delta only (`+1/+0/+1/+7/+1`). THE SEVEN QUESTIONS, RULED (vetoable): **Q-A** ADOPTED — covered path sets, and addendum 38's OUTCOME kept with its parenthesis RE-CUT: the defence pairs stay detection-only on §22.2 item 3's division (detect vs prevent), NOT on a nesting bar — under covered sets a `defenseProfile` group with a member list would not clash with G7; **Q-B** no new arm — detection is EM-R0b's `V-HISTORY-AGE`; **Q-C** CONFIRMED — R0a's manifest entry is added only after EM-P2's landing flip (one non-terminal holder of the register at a time; the family builds after T7 regardless); **Q-D** as written — the four named files are the sealed array, the 172-file `tests/lint` run is the §10 instrument (sealing it would bake the lighting red into `check:packet`); **Q1** RATIFIED — `authoredTensions` is classed by the ruling's TEST, not its example list (a READING); **Q2** RATIFIED — `populationHistory` → HISTORY on THE PROMISE (the four saved-only classes are behaviour-free today); **Q3** the proposed fate stands (`crossSettlementConflicts` per RECON-ID's table; CLOSED if the recon shows it dead); **Q4** discharged. THE SEVEN NOTICED ITEMS, FATED: (1) packet headers cite `scripts/**` by `path:line` OUTSIDE the citation walker's roots (two off-by-ones survived) → **FIX-C2b's second commit**: `docs/implementation/**` joins the walker's docs-live arm under the shrink-only baseline; until it lands, every pre-proof re-finds header citations by symbol (already the brief's law); (2) 'seven `SURFACE_ROOTS`' is six in the §5.4 tables three sibling packets inherit → **the chair's one-token check at each sibling's placement** (R0b v3, R0c, R6 v3.1) — recorded in the runbook; (3) G8 is the first group whose root also holds ORDER-BEARING collections, where EM-R0c's order arm and its group arm meet → **EM-R0c's pre-proof** carries one measured trial (an edit that moves the age while the timeline's order has settled); (4) `contractTestAntiVacuity`'s Rule 2 is a standing trap for every totality walker this family ships → **the family's shared compile brief (`LANE-EM-COMPILE-2.md`) gains the line THE SAME TURN**: a `tests/lint` totality walker's declared sets are IMPORTED, never re-typed, and every parked-file prediction prints `parkReasonsFor`; (5) CLOSED — cured in-packet (§6 construction rules 1–2), the cure in the shared brief by (4); (6) ⛔ the mutation-coverage manifest is a single-holder bottleneck (EM-B1d → EM-P2 → EM-R0a, four more EM-R members behind) → RULED: **SEQUENTIAL PLACEMENT AT EACH LANDING** — the validator's one-holder law stands (relaxing it would be a rule change the bottleneck does not warrant), the family's order is sequential in the one slot regardless, and a placement is one chair act of a minute; the lane's 'one chair act at the terminal' alternative is REJECTED because a row for a test file that does not yet exist would be a phantom the register walker forbids; (7) CLOSED — a REGISTER-only packet's `verifiedBase` moving to the promotion tip is correct behaviour, documented in the packet's §4 for its siblings. THE KIT: the pre-proofed copies supersede version 2 as the family's current R0a (`packets-waiting/EM-R0a.*`; the report and evidence in `evidence/`); placement at EM-P2's flip. R0a's seat is free → **EM-B1f version 3's PRE-PROOF RE-RUN DISPATCHED at 07:19** (the re-run the charter owed after EM-B1k2's landing: its RAW-base question is answered by the tree; it banks `npcVerdictPulse.js`'s quarantine row, ceiling 7 → 6). SEATS: gate = EM-P2 + FIX-P1; working = R0d · R0b pre-proofs · R0f compile · B1f pre-proof.
- **THE EM-R FAMILY'S PLACEMENT LAW:** one member READY at a time (the register's one-holder law); each placed at the previous member's flip; the family's §5.4 tables get the chair's one-token check (`SURFACE_ROOTS` is six). **FIX-C2b's second commit** gains `docs/implementation/**` in the citation walker's docs-live arm. **EM-R0c's pre-proof** gains the history-order trial.

## Amendments of 2026-09-20 07:23 EDT — EM-R0b pre-proofed READY-able (version 4); three questions accepted; eight items fated; EM-R0c's pre-proof dispatched (ODQ §934.47 addendum 71)

- EM-R0b version 3's pre-proof (read tip `141a1d775`, clean at both ends; the kit untouched by the lane) reports **version 4, READY-able at `141a1d775`** — no premise refuted; every v3 verified fact re-found by symbol and re-executed. The J-T1 window is EMPTY over all four change rows, the five live `requiredSymbols` paths and both `_pending` paths across 24 commits (the five `src/` files that moved are all `src/domain/worldPulse/**`, none in `generateSettlementPipeline`'s 224-module closure); the corpus figures re-executed anyway (the control 1 of 525 and it is the pin; three duplication identities 525/525; D1 525/525; D3 zero; D4 36/12/24; D5 to the row; D7 525/525). TWO CORRECTIONS THE PACKET NEEDED: (1) the lighting delta is `titles +7`, not +8 — the census sums credited files only, and §9 homes A8 in `tests/build/domainGeneratorsBoundary.test.js` 'run, not edited', so it lands no `it` (version 2 made the same count — inherited, not a regression); the delta is `+1/+0/+1/+7/+1`, no absolute restated; (2) 'the 63-row stride' was DEFINED NOWHERE — A1, A3, A6, A7 name it, no helper exists, no arithmetic stride over 525 yields 63, and the pin sits at corpus index 510 where the obvious `i % 8 === 0` MISSES it (A1's pin arm would have been vacuous) → executed as `i % 8 === 6`: 65 rows, 64 clean + the pin. Also: the preamble re-stamped `16dfb96f…`; §P2 row 11's amendment priced with first-paint membership measured BY IMPORTING the set (268 modules, 0 under `src/domain/edit/**`, the amendment's own member as the live control) and the skipped-arm sentence written in; four stale '§12 question 1' citations re-cut (already ruled BOTH at addendum 38); budgets — it lands in no budgeted chunk (`src/domain/edit` has zero importers in `src/`); ONE HARD CONSTRAINT PROVED: `assertAncestorAndSubstrate` builds the substrate from non-CREATE rows ∪ every `requiredSymbols` path, and at promotion R0b's five `_pending` rows join it with files EM-R0a and EM-R0d create — so R0b's base is stamped only AFTER BOTH have landed (the family's sequential-placement law, addendum 70, already orders it; recorded as the reason). THE THREE QUESTIONS, RULED (vetoable): the stride rule `i % 8 === 6` ACCEPTED (the pin inside the stride is the point; full 525 the only alternative); `titles +7` ACCEPTED (the terminal re-derives the census); no re-order of the ladder cuts before R0b builds — EM-R0d's pre-proof states each cut's witnessed status. THE EIGHT NOTICED ITEMS, FATED: (1) ⭐⭐ the `38` readiness cut is UNWITNESSED by the corpus (41 distinct scores, an 11-wide hole at 34..44; the `24` cut witnessed only from above) — A1's green bounds nothing about cut positions → **EM-R0d's pre-proof** (told the same turn: each ladder's cuts marked WITNESSED/UNWITNESSED with the nearest scores; an unwitnessed cut's case a CANNOT-CATCH row) and **EM-R7's corpus ratchet** records the witnessed score set; (2) two test headers assert in prose that `src/domain/edit/**` does not exist (`tests/store/decreeRegistryPersistence.test.js:17`, `tests/lib/editTravel.test.js:15`) — comment-only, nothing reds, false at the family's first CREATE → **EM-R0a's placement: a chair cut adds the two comment-only MODIFY rows** (its build re-addresses the two lines); (3) a second `bandLadders.js` basename arrives with EM-R0d (`src/domain/compendium/bandLadders.js` exists, with three rows in the observed-shape readers baseline) → **EM-R0d's pre-proof** (told the same turn: name the collision; measure which instruments address by basename; rename its own file if any conflates); (4) the claim that `npm run check` / `check:tail` never run the `tests/lint` vitest suite → **REFUTED BY THE CHAIR'S MEASUREMENT:** `check-test-ratchet.mjs` runs `npx vitest run --exclude=<SOURCE_TEST_EXCLUDE>` over the whole tree and run 19's own ratchet JSON reported a `tests/lint` file among its results — the terminal DOES run `tests/lint`; the packet's §10 `tests/lint`-whole instrument stays right for the LANE (the directory-whole rule: a lane's focused runs never reach the walkers, which is the run-17/18/19 family), but its stated reason is corrected in the kit copy; (5) the standard's hot-file list moved in the window, (6) the register moved by +15, (7) design §22.3's 'six' vs §5.4's 'seven' reconciled (six plus `V-STRESS-IDENTITY`), (8) the v2 harness's dead-tree hazard already cured kit-wide → recorded, no slot. THE KIT: version 4's copies supersede v3 as the family's current R0b (`packets-waiting/EM-R0b.*`; v3 kept as `EM-R0b.v3.*`; the report, evidence and resume note in `evidence/`). **EM-R0c's PRE-PROOF DISPATCHED at 07:21** into R0b's freed seat (conditional on R0f and R0b v3; the history-order trial). SEATS: gate = EM-P2 + FIX-P1; working = R0d pre-proof · R0f compile · B1f pre-proof · R0c pre-proof.
- **EM-R0a's placement** gains two comment-only MODIFY rows (the two headers that say `src/domain/edit/**` does not exist). **EM-R0d's pre-proof** carries the witnessed/unwitnessed cut table and the `bandLadders.js` basename collision. **EM-R7's ratchet** records the witnessed score set.
