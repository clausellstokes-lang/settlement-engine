# Settlement editor / EM-R2 — THE ROSTER IS FINAL INSIDE `enrichNpcCoherence`: the one lawful skip, the office appender gated as a producer, the member mirror left running, and `powerLinkage`'s dead outputs retired under a held roster

- **Status:** `READY`
  ⚠ The status value above stands ALONE on its line because `parsePacketHeader`
  (`scripts/implementation-packets.mjs`) anchors the status row at end-of-line and takes `status`
  only when exactly one row matches. Every stamp, caveat and date goes on these continuation
  lines, never on the row.
  ⭐ MEMBER 2 of the re-entry family EM-R (design §22.2's build order: R0a · R0b · **R1** ·
  **R2** · R3 · R4 · R5 · R0c · R6 · R7). It is written to the SIXTH amendment, and at THIS
  version's read tip the sixth is **LANDED** (train EM-T13's first commit `7bc14e003`) — so
  every §P citation below is the sixth's numbering, measured rather than predicted. The
  preamble block below carries the hash executed at that tip; the chair re-stamps the Preamble
  line at placement (§P9(c)).
- **Packet version:** 2
  - Version 1 was the first cut, compiled against train EM-T12's landed tip `91cc9ef5a` by an
    Opus COMPILE seat (session cce01f87, 2026-09-22). It refutes ONE clause of the charter's row
    by measurement — see §0.R — and contracts the narrower behaviour the tree supports.
  - **Version 2 is the PRE-PROOF re-measurement against train EM-T14's composed tip
    `c69d16a5d` (Opus PRE-PROOF seat, session cce01f87, 2026-09-22).** NOT ONE CONTRACT CLAUSE
    MOVED: all thirteen `requiredSymbols` are still verbatim at count 1 and at the same lines,
    all 37 line addresses the body asserted re-verified live, the CREATE target still absent,
    and the effective-line figures (762 · 163 · 156) unchanged. What version 2 changes is
    five classes of STALE TEXT the window created: (a) the sixth amendment is LANDED, so the
    header's "the fifth stands at the read tip" sentence and the fifth's hash are replaced by
    the measured sixth; (b) the worker ceiling has moved twice under this packet
    (`1396015` → `1395972`) and the worker closure is 188, not 187 — both restated as measured
    figures with their instrument named by symbol; (c) §5a's disjointness table is corrected by
    measurement — EM-R1 DOES share `scripts/mutation-coverage-manifest.json` (lawfully, under
    distinct row keys) and EM-R3 and EM-R4 each MODIFY `assembleSettlement.js`; (d) EM-R0d's
    holderTable.js edit is now IN the tree and the four rows this member re-addresses are
    measured UNMOVED, so §4a Q3's reservation is discharged by placement on EM-T15; (e) every
    `path:NNN` citation in the body and in the capsule's `_note`s is re-spelled `path :: symbol`
    under §P2.22 / rule 32 — the member shifts lines in three of the four files its own capsule
    cited, which §P2.16's ⭐ clause (judgment 124) makes its own row to re-address.
- **Verified base:** `em-t15-r2-2026-09-22` at `ec4cd1c2d5aebbda4df788fd4e600252adb81b5c`
  ⚠ Left for the chair's promotion stamp. **The revalidation sentence the chair will use:**
  *"Re-measured at `c69d16a5d6462068cddf140b98129bbb66d80373` (train EM-T14's composed tip;
  read tip `read-tip-em-t14`, DETACHED, `git status --short` EMPTY before and after): the
  CREATE target `tests/generators/rosterFinalUnderHeldRoster.test.js` ABSENT; all four MODIFY
  targets present (`narrativeGenerator.js` 1,122 raw / 762 effective, `assembleSettlement.js`
  325 raw / 163 effective, `generatePopulation.js` 228 raw / 156 effective, `holderTable.js`
  present with its four live `cite:` rows at `:169 :237 :238 :239`, each UNMOVED under EM-R0d's
  landed edit of that file, which re-addressed five OTHER rows only); the REGISTER target
  present and its row key absent; all THIRTEEN `requiredSymbols` present VERBATIM at count 1
  each and at their recorded symbols; `retiredSymbols` EMPTY; the preamble measured
  `125c693214235a81bf4f2b38506b621859a35e0325ff2ea58d335b4e0681a99d` (the SIXTH, landed)."*
  ⛔ A `__BASE__` packet can NEVER pass `validate:packets`; validation is downstream of this
  stamp, never a precondition of it. The chair's placement replaces `__BASE__` with the form
  `parsePacketHeader` parses into BOTH fields — ``- **Verified base:** `em-t15-r2-<date>` at
  `<the train base's 40-char sha>` `` — with the value ALONE on its line (§P9(c)); EXECUTED at
  this tip through the tree's own `validatePacketManifest` with EM-T14's three members flipped
  LANDED and this entry placed READY at `c69d16a5d…`, the ONLY EM-R2 error is
  `EM-R2.packetPath does not exist`, which the chair's placement fixes by writing the body —
  ZERO duplicate change-path and ZERO duplicate register-row-key errors.
- **Last revalidated:** left for the chair, with the sentence above.
- **Depends on:**
  - **EM-R1** — pins cloned on entry at the runner (design §22 ruling 6). A HARD dependency and
    the reason this member declares NO local clone: EM-B2a3 landed two LOCAL clones whose own
    comments say they "retire the day EM-R1 clones the bag on entry at the runner", and a third
    spelling here would go dead the same day. ⚠ **A PACKET, NOT LANDED CODE** — mark every fact
    taken from it PLAUSIBLE. Its compile is running beside this one
    (`COMPILE-EM-R1-cce01f87.md`); its declared change paths are `src/generators/pipeline.js`,
    `src/generators/steps/assembleInstitutions.js`, `src/generators/steps/generatePower.js` and
    a `tests/lint` CREATE — **path-disjoint from this member's five, measured** (§5a).
  - **LANDED at this read tip, read as real code:** **EM-P0** (`runPipeline`'s pins channel and
    the partial-pin refusal), **EM-B2a2** (`export function chooseOrPin` at its one home,
    `src/generators/pipeline.js :: chooseOrPin`), **EM-R0a** (`src/domain/edit/recordRegister.js`, the class
    register this member's battery imports rather than re-typing), EM-P4, EM-B2a1, EM-B2a3,
    EM-A1, EM-C4a and the rest of trains EM-T8–T12.
  - ⛔ **AN ORDERING CONSTRAINT, NOT A DEPENDENCY: EM-R0d MUST BE LANDED BEFORE THIS MEMBER IS
    PLACED — SATISFIED BY EM-T15.** Both MODIFY `src/domain/prose/holderTable.js`, which is not
    a row-keyed register. EXECUTED against the tree's own validator (§4a Q3): `duplicate change
    path across packets: src/domain/prose/holderTable.js (EM-R2, EM-R0d)` with both
    non-terminal, and that error is GONE with EM-R0d LANDED. ⭐ **RE-MEASURED AT
    `c69d16a5d`** (version 2): EM-R0d's edit of that file is IN this tree and it re-addressed
    FIVE rows — `defenseGenerator.js` `:458→:459` and `:467→:468`, `factionDynamics.js`
    `:127→:109`, `:133→:115`, `:179→:161` — and NOT ONE of this member's four. The four rows
    this member re-takes are still at `:169 :237 :238 :239` with their addresses
    `assembleSettlement.js:100`, `:119`, `:120` and `narrativeGenerator.js:1113` byte-identical
    (judgment 174c, executed). EM-R0d rides EM-T14, so a placement on EM-T15 discharges the
    reservation, and the validator run above proves it with the flip simulated.
- **Collision group:** **EM-R0d** on `src/domain/prose/holderTable.js` (above; a placement-order
  constraint, measured and DISCHARGED at `c69d16a5d`). **EM-R1**, this member's train-mate, on
  TWO surfaces, both measured at version 2: at the SYMBOL level (this member calls `chooseOrPin`
  and reads `ctx.__pins`, and EM-R1 MODIFIES `src/generators/pipeline.js`, which carries two of
  this member's `requiredSymbols` rows), and at the ROW-KEYED REGISTER
  (`scripts/mutation-coverage-manifest.json`, which EM-R1 also carries — lawful under TOOL-27
  only while the two row keys differ). Their FILE sets are otherwise disjoint. ⭐ **CORRECTED BY
  MEASUREMENT (version 2): two waiting packets DO name one of this member's source files** —
  `EM-R3` and `EM-R4` each MODIFY `src/generators/steps/assembleSettlement.js`. Both are
  compiled for LATER trains (EM-R3 for EM-T16), so the re-measurement is THEIRS against this
  member's landed fence, not this member's against their predicted one (judgment 134); this row
  exists so that neither is placed non-terminal beside EM-R2. Nine waiting packets carry a row
  on `scripts/mutation-coverage-manifest.json`; it is ROW-KEYED (TOOL-27, `900a2368d`), each
  under a DISTINCT `rowKey`, so they do not collide — EXECUTED in §4a through the tree's own
  validator: zero duplicate-row-key errors with this member placed.
- **Commit authority:** edits only; the chair commits
- **Baseline posture:** RE-MEASURED at `c69d16a5d` (version 2, UNCHANGED from version 1) —
  **no `scripts/.size-baseline.json` row exists for any of the three source files** (measured,
  0 row-lines each; the file holds 19 keys, 13 of them real paths), so the generator layer's
  `max-lines` **800** governs (`skipBlankLines` + `skipComments`,
  `eslint.config.js :: the src/generators/**/*.js block`). ⚠ That block's own comment still
  names `narrativeGenerator` among "the legacy files still over 800"; the comment is stale —
  the file measures **762** effective through eslint's own `Linter` and carries no baseline row.
  Measured headroom after the edit: `narrativeGenerator.js` **37** (763), `assembleSettlement.js`
  **633** (167), `generatePopulation.js` **639** (161). None of the three is in
  `docs/content/wiring-census.json`'s `stamp.files` (7 paths, all under
  `src/domain/display/stateProse/`), so no census re-take is owed. Every register figure this
  packet predicts is a DELTA; the live absolutes are the chair's to stamp.
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: TO BE STAMPED BY THE CHAIR)
  > **Measured at THIS version's read tip `c69d16a5d`** with `shasum -a 256`:
  > `125c693214235a81bf4f2b38506b621859a35e0325ff2ea58d335b4e0681a99d` — **the SIXTH amendment,
  > LANDED** (train EM-T13's first commit `7bc14e003`). Version 1 measured the FIFTH
  > (`fdecd4268286…`) because the sixth had not yet landed at its tip; that is history, not a
  > second claim. Every §P citation below is the sixth's numbering, and the chair re-stamps this
  > line at placement.
- **Evidence:** every VERIFIED row below is receipted in `EM-R2.evidence.md` by command and output.

---

## §0 · THE MEASUREMENT — the charter's row read against the tree, and the one clause it refutes

The charter's row (`docs/implementation/charters/EDIT-MODE-TRAIN.md`, the amendments of
2026-09-19 16:5x and 17:25): *"**EM-R2** the roster final inside `enrichNpcCoherence`
(`ensureFactionStructuralNpcs` named; `relinkFactionMembers` exempt); retires the wasted
`powerLinkage()` run under a held roster."* Four clauses; three hold exactly, one does not.

### §0.A — THE SKIP'S EXACT SITE, AND WHY IT IS THE ONLY LAWFUL ONE · CONFIRMED

`enrichNpcCoherence` has TWO call sites in `src/`. Only one is the re-derivation seam:

| call site | what it is | this member |
|---|---|---|
| `src/generators/narrativeGenerator.js :: generateCoherence`, its `inCoherenceSubstream(coherenceRng, 'npc-enrichment', …)` call | the assembly seam, reached from `assembleSettlement.js :: the `canonical-coherence` inAssemblySubstream call` | **GATES IT** |
| `src/generators/generateSettlementPipeline.js :: regenNPCsPipeline`, its own `enrichNpcCoherence` call | the NPC SECTION REROLL. It takes no pin bag, runs outside `runPipeline`, and re-enriches a freshly rolled roster on purpose | **UNCHANGED, and named so the omission is a decision** |

Design §22.1 correction 1 permits a SKIP only "inside its own named child stream". EXECUTED at
this tip, `src/kernel/prng.js :: fork` reads `fork: (label) => createPRNG(`${seed}::${label}`),` — a
fork is a pure function of (seed, label), so a child stream that is never created cannot move any
sibling. The probe asserts it and carries its own failing control: the four sibling readings are
identical with and without the enrichment (8/8 rows), while a PARENT draw does move the parent's
next value (`0.079113… → 0.957477…`). **That is why the skip is lawful here and nowhere else in
assembly**: `generateSettlementName` draws on the step's SHARED ambient stream and must consume.

### §0.B — THE MIRROR IS LOAD-BEARING · CONFIRMED

`narrativeGenerator.js :: relinkFactionMembers` takes no draw and writes no held fact: it
re-points each `factions[].members[]` chip at the roster by `id`. EM-R0a's LANDED class register
already carries the classification this member relies on —
`CLASS_EXCEPTIONS['factions[].members[]'] = 'MIRROR'` (`recordRegister.js :: CLASS_EXCEPTIONS`). Executed over 8
corpus rows with an edited `role` and `title`: the mirror carries the edit 8/8 and a skipped
mirror leaves the stale chip 8/8. **It is never gated.**

### §0.C — THE OFFICE APPENDER WOULD GROW A FINAL ROSTER · CONFIRMED, with its non-vacuity control

`factionRoles.js :: ensureFactionStructuralNpcs`, called at `assembleSettlement.js :: the step callback`
returns `{ ...settlement, npcs: [...existingNpcs, ...additions] }`. Measured at this tip:

```
thorp  3 → 3 (+0) · hamlet 6 → 6 (+0) · village 8 → 8 (+0) · town 9 → 9 (+0)
city  14 → 14 (+0) · metropolis 21 → 21 (+0) and 19 → 19 (+0)     [registerVii = false, 8/8]
CONTROL, the same rows with an UNCOVERED roster: +2 · +2 · +3 · +5 · +5 · +8 · +8 · +8
```

⚠ Two honest corrections to the recon the family was compiled from
(`findings/RECON-ARCH-REDERIVE-2.report.md` §2.3 and §7.1, 2026-09-19): at **this** tip the
appender writes `npcs` ONLY — the `factions` reference is passed through by the spread, so the
recon's "WRITES npcs AND factions" no longer holds — and its per-row delta is **+0 on 8/8**, not
the metropolis delta the recon recorded. It is NOT dormant: `rollsRegisterVii` is `false` on all
eight rows, so it takes its LEGACY path and the +0 is office coverage, not the version gate. The
control is what makes the gate non-vacuous: the DM op that removes a seat-holder is exactly the
uncovered case, and under a held roster that would mint an NPC into a roster the record calls
final. ⛔ It takes **NO draw at any tier** (measured: zero draw tokens anywhere in
`factionRoles.js`), so §22.1 correction 1's two rules COINCIDE for it — a skip consumes nothing.

### §0.R — ⛔ THE ONE CLAUSE THE TREE REFUTES: `powerLinkage()`'s RUN IS NOT WHOLLY WASTED

The charter says the run is "wasted … under a held roster". Measured:

```
$ git grep -n -P 'powerLinkage\(' -- src tests
src/generators/steps/generatePopulation.js:36:function powerLinkage(powerStructure) {
src/generators/steps/generatePopulation.js:160:  const power = powerLinkage(powerStructure);
(a grep receipt: the two addresses are the command's OWN output, quoted with their text, which is §P2.22's second exception)
```

It computes from `powerStructure` (**not** the roster) and it takes **no draw** (its own JSDoc
says "Pure — it takes no draw", and a token scan of its body finds none). Its result has **TWO**
consumers, not one:

| site | what reads it | under a held `factions` pin |
|---|---|---|
| `:162` | `linkFactions(generateFactions(…), power, rng)` — INSIDE the `chooseOrPin(pins, 'factions', …)` draw thunk | **DEAD** (the thunk never runs) |
| `:190` | `power.powerFactionsByCategory[fg.dominantCategory \|\| 'other']` — the faction-linkage TRACE's direct/attraction classifier | **LIVE** (it runs on every path) |

Per-output census, executed: `pfList`, `governingPF`, `pfAttractionMap` and `totalPower` are read
ONLY by the linkage; `powerFactionsByCategory` is read by BOTH. So a full retirement either
breaks the trace's classifier or takes the trace's partition, which is **EM-R5's** (design §22.2
item 5 and the charter's EM-R5 row), not this member's.

⇒ **THE CONTRACT IS A SPLIT, NOT A DELETION.** The category index the trace reads is computed
once outside the chooser; the four linkage-only lookups move INSIDE the draw thunk, so a held
roster never builds them. Value-identity is EXECUTED (8/8 rows, whole-object hash equal; the
classifier set-equal both directions; a deliberately broken split is caught by the same arm).
§4a **Q1** puts the narrowing to the chair.

### §0.D — ⛔ WHAT THIS MEMBER DOES NOT COVER, AND WHOSE IT IS

`src/generators/steps/corruptionPass.js` declares `mutates: ['factions', 'npcs']` and mutates
`ctx.npcs` IN PLACE (`npc.corrupt`, `npc.corruptionVector`, `npc.secondaryAffiliation`,
`npc.corruptTies`, `npc.goal`), drawing on its own step fork. The charter's FIRST EM-R2 cut
(2026-09-19 16:2x) said *"and `corruptionPass` leaves a held roster alone"*; the 16:5x re-cut
dropped that clause and **no later row picked it up**. Under EM-R1's runner clone, an ungated
`corruptionPass` writes through into the very array the record's roster becomes — a held fact
that is not final. It is NOT taken here: a fourth logic file breaks the standard's cap of three
(§3), and the intake rule forbids a quiet widening. §4a **Q2** slots it.

---

## §1 · Reconciled authority

| Source | What it binds here |
|---|---|
| `docs/implementation/preambles/EM-PREAMBLE.md` | the family preamble, whole; §P2.11 byte budgets, §P2.13 wiring census, §P2.14 citation rows, §P2.16 line-addressed re-address, §P2.17 tuning, §P2.21 the instrument list, §P3.5 deltas, §P9(a)–(f), §P10, §P11 |
| `docs/DESIGN_EDIT_MODE_AND_DECREES.md` §22 ruling 1 | HELD FACTS ARE FINAL: re-derivation never sends a held fact back through the code that first made it |
| §22 ruling 2 | re-derivation recomputes READINGS only, on each deriving step's own forked stream |
| §22.1 correction 1 | a held writer on a SHARED stream CONSUMES AND DISCARDS; only a writer inside its own named child stream is SKIPPED — and `enrichNpcCoherence` is that writer, by name |
| §22.1 correction 2 | a MIRROR is not a producer: it always runs |
| §22.2 item 7 | `factions[].members[]` is MIRROR — recomputed after the merge, never merged |
| §22.2 item 11 | draw-SKIP outside `enrichNpcCoherence` is a later, measured optimisation that lands only after EM-R5's trace partition |
| §22.3 | the rulings the first two compiles forced; the class register is declared BY PATH |
| `docs/implementation/charters/EDIT-MODE-TRAIN.md` | the EM-R2 row (amendments of 2026-09-19 16:5x and 17:25), the "EM-B2a is RE-CUT" paragraph, and the EM-R family build order |
| THE PROMISE | a seed is a STARTING world forever: with no pins, not one generated byte moves |

Additional sources this member names: `findings/RECON-ARCH-REDERIVE-2.report.md` §2.2/§2.3/§7.1
and `RECON-ARCH-REDERIVE-3.report.md` §7.2/§7.3 — read as 2026-09-19 MEASUREMENTS, two of whose
figures this compile re-measured and corrects (§0.C).

---

## §2 · Outcome

**One behaviour family: under a HELD roster, the roster the record carries is the roster the
re-derivation returns — nothing re-enriches it, nothing appends to it, and its mirrors still
follow it.**

1. `generateCoherence` gains a third parameter, `pins`, defaulting to `null`. The
   `npc-enrichment` child stream is entered through the runner's own primitive
   (`chooseOrPin(pins, 'npcs', …)`). With `npcs` held, `enrichNpcCoherence` and its four
   producers (`buildPoliticalNarrative`, `mergeNPCLists`, `disperseNamedRoster`,
   `enrichNPCsWithStructure`) DO NOT RUN and the child stream is never forked.
2. `relinkFactionMembers` is UNGATED and always runs, over whichever roster step 1 produced.
3. `assembleSettlement` reads `ctx.__pins` once and gates `ensureFactionStructuralNpcs` on the
   same record path through the same primitive.
4. `powerLinkage` splits (§0.R): the category index outside the chooser, the four linkage-only
   lookups inside the draw thunk.
5. With NO pin bag, every byte of every generated settlement is what it was.

**NON-GOALS — each is another member's, named so the boundary is executable.**
- NOT the runner's clone or the writer census (**EM-R1**).
- NOT `corruptionPass`'s write-through (§0.D, §4a Q2 — unowned today).
- NOT the power replay or the freshness assert (**EM-R3**).
- NOT the settlement NAME's consume-and-discard (**EM-R4**).
- NOT the trace's partition by step (**EM-R5**) — which is why §0.R is a split.
- NOT the three-way merge (**EM-R0c**), not `rederive` itself (**EM-B2a4**), not the corpus
  ratchet (**EM-R7**).
- NOT the NPC section reroll at `src/generators/generateSettlementPipeline.js :: regenNPCsPipeline`.
- It mints no PRNG stream, renders no number, adds no surface, and writes no persisted key.

---

## §3 · Hard scope budget

| Limit | Packet budget | Standard |
|---|---:|---:|
| Behaviour families | 1 | 1 |
| New persisted record families | **0** | ≤1 |
| Named state writers | 0 (it adds no writer; it gates two existing producers) | ≤1 |
| Feature flags | 0 | ≤1 |
| User-facing surfaces | 0 (headless) | ≤1 |
| Direct consumers | 0 at this member's landing (EM-B2a4's `rederive` is the first caller) | ≤2 |
| New logic-bearing production leaves | **0** | ≤2 |
| Existing logic-bearing production files modified | **3** | ≤3 |
| Additional registration-only files | 2 — the mutation-coverage row and the `holderTable.js` CITATION row (§P2.14: an addresses-only citation row is outside the cap of three modified logic files and never splits a packet) | ≤3 |
| Handwritten files total | **6** | ≤12 |
| New/changed effective production lines | **+10, MEASURED** (`narrativeGenerator.js` +1 · `assembleSettlement.js` +4 · `generatePopulation.js` +5), eslint's own `Linter` under `max-lines` with `skipBlankLines` + `skipComments` (§P9) | ≤400 |
| Effective lines per touched file after the edit | 763 · 167 · 161, all against the generator layer's **800** | ≤250 for a NEW leaf |
| Delta in a shared/baselined file | **0** — no file carries a `scripts/.size-baseline.json` row | ≤15 |
| Acceptance cases | **7** | ≤8 |

Overrides approved before dispatch: **NONE, and none is needed.** ⛔ No budget is raised or
invented here. ⚠ `narrativeGenerator.js` ends at 763 effective against 800 — **37 lines of
headroom**. The contract's §6 spends ONE effective line there on purpose and puts every word of
explanation in comments, which `skipComments` does not count; a build lane that reaches for a
second helper in that file is over the layer ceiling and STOPS (§11).

---

## §4 · Sealed dispatch, preflight, and THE BLOCK

```sh
npm run implementation:dispatch -- EM-R2
```

Dry-read against `scripts/implementation-session.mjs`, check by check, **RE-RUN AT
`c69d16a5d` for version 2**:

| dispatch check | verdict at `c69d16a5d` once the chair stamps the base |
|---|---|
| branch name / ancestry | the lane branch is `em-t15-r2-<date>` cut from the train's promotion commit; the sealed dispatch wants the worktree ON the verified branch (§P10.2) |
| substrate unchanged since the verified base | **PASSES** once the base is the tip; the four MODIFY targets are the substrate, and across the window `91cc9ef5a..c69d16a5d` only `holderTable.js` moved (EM-R0d, five rows, none of this member's four — `git diff --stat` quoted in §4a Q3) |
| CREATE targets ABSENT | **PASSES** — `tests/generators/rosterFinalUnderHeldRoster.test.js` is absent (measured at `c69d16a5d`) |
| MODIFY / TEST / REGISTER targets present and clean | **PASSES** — all five exist at the tip |
| every `requiredSymbols` row resolving | **PASSES** — all THIRTEEN resolve VERBATIM at count 1 AND at their recorded symbols, EXECUTED at `c69d16a5d`; through the tree's own `validatePacketManifest`, with EM-T14's three members flipped LANDED and this entry placed READY at the tip's 40-char sha, the ONLY EM-R2 error is `packetPath does not exist`, which the chair's placement fixes by writing the body |
| git-clean / foreign dirt fingerprinted without target overlap | no EM-T14 or EM-T15 sibling writes any of the three logic files; the `holderTable.js` overlap with EM-R0d is DISCHARGED at this tip (§4a Q3), and the two LATER siblings that share `assembleSettlement.js` (EM-R3, EM-R4) are named in §5a so neither is placed non-terminal beside this member |

### §4a · THE FOUR QUESTIONS ONLY THE CHAIR CAN ANSWER

Each carries the measurement that raises it and a one-line recommendation; none is decided here.

**Q1 — THE `powerLinkage` CLAUSE IS REFUTED AS WRITTEN: TAKE THE SPLIT?** §0.R measures two
consumers, one of them live on the held path, so "retire the wasted run" cannot be executed
literally without moving the faction-linkage trace — which is EM-R5's.
**Recommend: take the SPLIT as contracted** (the category index stays outside the chooser; the
four linkage-only lookups move inside the draw thunk), and let EM-R5 decide whether that trace
entry is re-emitted or carried at all. ⚠ The alternative — EM-R2 stops emitting the linkage trace
under a held roster — would make this member depend on EM-R5 and would re-rule a partition
design §22.2 item 5 has already settled.

**Q2 — `corruptionPass` IS AN UNOWNED WRITE-THROUGH ON A HELD ROSTER (§0.D).** The charter's
first EM-R2 cut named it; the re-cut dropped it and nothing picked it up. Measured: it mutates
`ctx.npcs` in place in five fields and draws on its own step fork.
**Recommend: rule it into EM-R1's §13 gating question** — EM-R1's brief already asks "which
writers this member actually gates under a held key, and how", `corruptionPass` is in its census,
and the gate is one `chooseOrPin` at the head of that step — or charter it as **EM-R2b**. It is
not taken here because a fourth logic file breaks §3's cap.

**Q3 — THE `holderTable.js` RESERVATION AGAINST EM-R0d.** EXECUTED against the tree's own
validator over a copy of `PACKET_MANIFEST.json`: with EM-R2 and EM-R0d both non-terminal,
`duplicate change path across packets: src/domain/prose/holderTable.js (EM-R2, EM-R0d)`; with
EM-R0d LANDED, that error is gone and EM-R2's only errors are the two a `__BASE__` DRAFT always
carries.
**Recommend: place EM-R2 on a train whose promotion commit follows EM-R0d's LANDED flip** —
EM-R0d rides EM-T14, so EM-T15 satisfies it — and have the pre-proof RE-MEASURE the four
addresses at the then-tip rather than copy §7's predictions (§P2.16).
⭐ **VERSION 2, EXECUTED AT `c69d16a5d` (train EM-T14's composed tip, EM-R0d's commit): DONE,
AND THE ANSWER IS "UNMOVED".** EM-R0d re-addressed five rows of `HOLDER_SOURCES` and none of
this member's four; the four still read `:169 assembleSettlement.js:100`,
`:237 …:119`, `:238 …:120`, `:239 narrativeGenerator.js:1113`. The validator run with EM-T14's
three members flipped LANDED returns ZERO duplicate change-path errors and ONE EM-R2 error,
`packetPath does not exist`, which the placement writes away. **The question is now a
placement-order note, not an open ruling.**

**Q4 — GATE `ensureFactionStructuralNpcs`, OR EXEMPT IT?** §0.C measures +0 on 8/8 corpus rows
and +2 to +8 the moment an office is unheld, so the gate changes nothing today and everything
after a DM removes a seat-holder.
**Recommend: GATE it, as contracted and as the charter's row names it** — it is a PRODUCER of
`npcs` and §22 ruling 1 admits no producer into a final roster; the arm that proves the gate
carries its own uncovered-roster control so it cannot pass vacuously.

---

## §5 · Verified tree contract

Every row re-found BY SYMBOL at the read tip; no line number below is a coding instruction.
⭐ **§P2.22 / rule 32 (cite by symbol), version 2:** every citation in this packet and in its
capsule is now `path :: symbol`. Exactly TWO classes of `path:NNN` survive, and both are the
law's own exceptions — the `cite:` VALUES of `HOLDER_SOURCES`, which are a register that keys
BY LINE and whose addresses are this member's C4 payload, and §0.R's grep receipt, which is a
line quoted with its own text as evidence.

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| ⭐ THE SKIP SITE | `src/generators/narrativeGenerator.js` | `export const generateCoherence = (settlement, coherenceRng = null) => {` (count 1) | the coherence tail; it forks FIVE named child streams and returns `npcs`, `factions`, `prominentRelationship`, `coherenceNotes` and `history` | MODIFY: one new parameter, one `chooseOrPin` wrap. The four sibling forks are untouched |
| ⭐ THE PRODUCER GATED | `src/generators/narrativeGenerator.js` | `export const enrichNpcCoherence = (settlement) => {` (count 1) | `buildPoliticalNarrative` per NPC → `mergeNPCLists` → `disperseNamedRoster` → `enrichNPCsWithStructure`; 12–72 draws per run, ALL on the `npc-enrichment` child stream | PRESERVE VERBATIM. The body is not edited; only its call is gated |
| ⭐ THE NAMED CHILD STREAM | `src/generators/narrativeGenerator.js` | `function inCoherenceSubstream(rng, label, operation) {` (count 1) and `'npc-enrichment',` (count 1) | `setActiveRng(rng.fork(label))` with save/restore; the label is what makes the skip lawful | PRESERVE both VERBATIM |
| ⭐ THE MIRROR | `src/generators/narrativeGenerator.js` | `export const relinkFactionMembers = (factions, enrichedNpcs) => {` (count 1) | remaps every `factions[].members[]` to the roster BY `id`, identity fallback when no id matches; zero draws | PRESERVE and LEAVE UNGATED |
| THE STEP | `src/generators/steps/assembleSettlement.js` | `const coherenceUpdates = inAssemblySubstream(` (count 1) | the assembly's own `canonical-coherence` fork, which hands `generateCoherence` its `coherenceRng` | MODIFY: one extra argument. The assembly substream itself is unchanged |
| ⭐ THE APPENDER | `src/generators/factionRoles.js` | `export function ensureFactionStructuralNpcs(` (count 1) | returns `{ ...settlement, npcs: [...existingNpcs, ...additions] }`; version-gated by `rollsRegisterVii`; ZERO draws (measured: no draw token anywhere in the file) | PRESERVE VERBATIM. ⛔ NOT this member's file — only its CALL SITE is gated |
| ⭐ THE PRIMITIVE | `src/generators/pipeline.js` | `export function chooseOrPin(pins, key, draw) {` (count 1) | EM-B2a2's landed export; it returns `pins[key]` WITHOUT calling `draw()` when the key is an own property, measured: pin present ⇒ thunk calls 0; absent ⇒ exactly 1 | IMPORT and CALL. ⛔ NOT this member's file (EM-R1 holds it) |
| ⭐ THE RUNNER CHANNEL | `src/generators/pipeline.js` | `const _PINS_KEY = '__pins';` (count 1) | the reserved key the runner hands every step its bag under, exempted BY NAME from the undeclared-write scan | PRESERVE. `ctx.__pins` is read exactly as `generatePopulation.js` and `assembleInstitutions.js` read it |
| ⭐ THE FORK LAW | `src/kernel/prng.js` | `fork: (label) => createPRNG(`${seed}::${label}`),` (count 1) | a fork is a FRESH stream derived from (seed, label) — it takes nothing from the parent, so a skipped child cannot move a sibling. ⛔ Changing this derivation is owner-gated under THE PROMISE | PRESERVE. It is the reason §0.A's skip is lawful, and the battery executes it |
| ⭐ THE CLASS REGISTER | `src/domain/edit/recordRegister.js` | `export const RECORD_CLASSES = Object.freeze({` and `export const CLASS_EXCEPTIONS = Object.freeze({` count 1 each | EM-R0a's LANDED data: `npcs: 'HELD'`, `factions: 'HELD'`, `'factions[].members[]': 'MIRROR'`. ⛔ Its own header forbids any `src/generators/**` module importing it, so only the BATTERY reads it | IMPORT in the test, never in `src/`. The battery asserts the gate against the register rather than re-typing it (`contractTestAntiVacuity` Rule 2) |
| THE LINKAGE'S OTHER READER | `src/generators/steps/generatePopulation.js` | `const roll = rng.random() * totalPower;` (count 1) | EM-P0's own required row, inside `linkFactions`, which this member's split does not enter | PRESERVE VERBATIM |
| THE CITATION TABLE | `src/domain/prose/holderTable.js` | `export const HOLDER_SOURCES` (count 1) | four live `cite:` rows address lines this member shifts: `:169`→`assembleSettlement.js:100`, `:237`→`:119`, `:238`→`:120`, `:239`→`narrativeGenerator.js:1113`. `tests/lint/proseWiringCensus.walker.test.js`'s SEAM car 5b arm re-derives every producer write from a live syntax-tree pass and reds on a stale one | RE-ADDRESS in the same commit, addresses only (§7). Also EM-R0d's own required row, and it survives byte-identical |
| Test precedent (the stream) | `tests/generators/assemblyCoherenceRngIsolation.test.js` | its `generateCoherence` isolation arms | the shape the battery's A3 copies: two runs, the same coherence rng, the sibling outputs compared |
| Test precedent (the pin) | `tests/generators/pipelinePinnedMode.test.js` | `describe('EM-P0 — the pipeline seam: pins are consulted per chooser, on one stream')`, arms A1–A7 | A1 hashes a golden stride against the committed fixture; A3 counts raw draws on a step's own stream. The battery copies both arm for arm |
| Test precedent (the appender) | `tests/generators/factionStructuralOfficeCoverage.test.js` | its office-coverage arms | the shape A5 copies, with the uncovered-roster control it already establishes |

### §5a · PATH DISJOINTNESS, MEASURED

⭐ **RE-MEASURED AT `c69d16a5d` FROM EACH SIBLING'S OWN CAPSULE** (version 2). Version 1's row
for EM-R1 read `∅` and its last sentence said no waiting packet names a source file of this
member; BOTH ARE CORRECTED HERE BY MEASUREMENT, and neither correction changes a contract
clause.

| sibling | train | its declared change paths | intersection with this member's SIX rows |
|---|---|---|---|
| **EM-R1** | EM-T15, **this train** | `src/generators/pipeline.js` · `src/generators/steps/assembleInstitutions.js` · `src/generators/steps/generatePower.js` · `tests/lint/heldKeyWriterCensus.walker.test.js` CREATE · `tests/generators/pipelinePinnedMode.test.js` TEST · **`scripts/mutation-coverage-manifest.json` REGISTER** | **{ mutation-coverage-manifest.json }** — lawful ONLY under distinct `rowKey`s (TOOL-27). ⚠ Measured: EM-R1's row carries **no `rowKey` field** today, which the tree's own validator refuses by name; that is EM-R1's row to fill, named here so the train's placement cannot miss it. And `pipeline.js` is a `requiredSymbols` path of THIS member (two rows) — a fact that MOVES under this member before placement, so the two rows are re-run verbatim at the train's base (§P9(e), judgment 134) |
| **EM-R5** | EM-T15, this train | `src/domain/edit/tracePartition.js` CREATE · `src/store/settlementRederiveAction.js` · a TEST | **∅** |
| **EM-R0d** | EM-T14, lands BEFORE | `src/data/bandLadders.js` · … · **`src/domain/prose/holderTable.js`** | **{ holderTable.js }** — DISCHARGED: its landed edit is in this read tip and moved five OTHER rows; this member's four are byte-identical (judgment 174c) |
| **EM-R0f** · **EM-R6** | EM-T14, land BEFORE | — | **∅** on every row of this member, change paths and `requiredSymbols` paths alike |
| **EM-R3** · **EM-R4** | EM-T16 and later, land AFTER | each MODIFIES **`src/generators/steps/assembleSettlement.js`** (EM-R4 also carries a `mutation-coverage-manifest.json` row under its own `rowKey`) | **{ assembleSettlement.js }** — theirs to re-measure against THIS member's landed fence, never this member's against their predicted one. Neither may be placed non-terminal beside EM-R2 |
| every other waiting packet (36 manifests scanned) | — | — | **∅** on the four source files |

---

## §6 · Exact contracts

No discretion. Every clause is a signature, a shape, an order or a refusal.

### C1 — `generateCoherence(settlement, coherenceRng = null, pins = null)`

- The third parameter is named `pins` and defaults to `null`. **Absent or `null` ⇒ today's
  behaviour EXACTLY**; every existing caller keeps its arity.
- The enrichment call becomes, verbatim in shape:
  ```js
  const mergedNpcs = chooseOrPin(pins, 'npcs', () => inCoherenceSubstream(
    coherenceRng,
    'npc-enrichment',
    () => enrichNpcCoherence(settlement),
  ));
  ```
  The key is the literal `'npcs'` — the RECORD PATH the producer writes, per `runPipeline`'s
  `@typedef Pins`. ⛔ It is never a step name, never an entity id, and never a second spelling of
  the runner's own-property rule (no `pins && 'npcs' in pins`, no `pins?.npcs ?? …`).
- ⛔ **NO LOCAL CLONE.** EM-R1 clones the bag on entry at the runner (§22 ruling 6); a clone here
  would be a third spelling of that rule and would go dead the day EM-R1 lands.
- `chooseOrPin` is reached by a NEW import line in `narrativeGenerator.js`:
  `import { chooseOrPin } from './pipeline.js';`, placed at the END of the existing import block.
  ⚠ `pipeline.js` is already inside the generation worker's closure without this edge (measured),
  so the edge adds no module to any bundle.
- `relinkFactionMembers(settlement.factions, mergedNpcs)` and its `Array.isArray` guard are
  BYTE-IDENTICAL to today. **The mirror is not gated, not reordered and not made conditional on
  `pins`.**
- The four sibling `inCoherenceSubstream` calls — `historical-character`,
  `prominent-relationship`, `coherence-notes`, `siege-capability` — are byte-identical, in the
  same order, with the same labels.

### C2 — `assembleSettlement`'s step callback

- At the TOP of the callback, before the ctx destructure:
  `const pins = ctx.__pins || null;` — the same spelling `generatePopulation.js :: derivePopulation`'s caller and
  `assembleInstitutions.js :: its step callback` already carry.
- A module-level sentinel above `registerStep`, EM-B2a3's landed idiom:
  `const UNPINNED = Symbol('assembleSettlement:unpinned');`
- `generateCoherence(settlement, coherenceRng)` becomes
  `generateCoherence(settlement, coherenceRng, pins)`. Nothing else in `inAssemblySubstream`
  moves.
- The appender is gated, and ONLY the appender:
  ```js
  if (chooseOrPin(pins, 'npcs', () => UNPINNED) === UNPINNED) {
    const withStructural = ensureFactionStructuralNpcs(
      settlement,
      ctx.generationContext,
    );
    Object.assign(settlement, withStructural);
  }
  ```
  ⛔ The `Object.assign` shape is unchanged on the unpinned path. The sentinel exists because
  `chooseOrPin` returns the held ARRAY while the appender returns a SETTLEMENT, and a `null`
  or `undefined` sentinel could collide with a bag value.
- The existing `import { registerStep } from '../pipeline.js';` is WIDENED, never added:
  `import { chooseOrPin, registerStep } from '../pipeline.js';` — the same move EM-B2a3 made in
  its own two step files.
- ⛔ Nothing else in the step changes: not `generateSettlementName`, not the stress re-render, not
  the three `assertPowerEconomyFreshness` call sites, not `reconcilePowerStructure`, not
  `resizePoliticalRoster`, not `buildGenerationCoherenceReceipt`, not `normalizeSettlement`.
  Those are EM-R3's and EM-R4's.

### C3 — `generatePopulation`'s `powerLinkage` split

- `powerLinkage(powerStructure)` becomes TWO pure functions, neither taking a draw:
  - `powerFactionsByCategory(pfList)` — the `reduce` that builds the category index, lifted
    verbatim.
  - `powerLinkage(powerStructure, byCategory)` — the same object it returns today, with
    `powerFactionsByCategory: byCategory`.
- In `derivePopulation`:
  ```js
  const byCategory = powerFactionsByCategory(powerStructure?.factions || []);
  const factions = chooseOrPin(pins, 'factions', () => {
    const power = powerLinkage(powerStructure, byCategory);
    return linkFactions(generateFactions(npcs, relationships), power, rng);
  });
  ```
  ⛔ `powerLinkage` is evaluated BEFORE `generateFactions` inside the thunk, exactly as it is
  today, so no draw moves. The trace's classifier reads `byCategory[...]` in place of
  `power.powerFactionsByCategory[...]`; nothing else in the trace changes — not its `targetId`,
  not its `step`, not its `result`, not one word of its `reason`.
- ⛔ `linkFactions` is NOT edited: its destructure, its three branches and `const roll =
  rng.random() * totalPower;` are byte-identical.
- The file's existing `import { chooseOrPin, registerStep } from '../pipeline.js';`
  already carries what the split needs; NO import line is added.

### C4 — `holderTable.js`, ADDRESSES ONLY

Four `cite:` values are re-taken. Predicted from the contract's own planned text, and **RE-MEASURED
BY THE BUILD LANE AT DISPATCH** (§P2.16 — a predicted number is never copied):

| row | today | predicted |
|---|---|---|
| `:169` `economicViability` | `src/generators/steps/assembleSettlement.js:100` | `…:111` |
| `:237` `structuralViolations` | `…assembleSettlement.js:119` | `…:130` |
| `:238` `structuralSuggestions` | `…assembleSettlement.js:120` | `…:131` |
| `:239` `prominentRelationship` | `src/generators/narrativeGenerator.js:1113` | `…:1126` |

⛔ No `kind`, no `read` flag, no key and no row is added, removed or reordered;
`export const HOLDER_SOURCES` stays byte-identical, which is also EM-R0d's required row.

### C5 — What the member REFUSES

- It does not skip, consume or reorder ANY writer outside `enrichNpcCoherence` and
  `ensureFactionStructuralNpcs`.
- It does not touch `simulationTrace`'s partition, emission or content (EM-R5).
- It does not clone, and it does not read a pin anywhere but through `chooseOrPin`.
- It does not change `regenNPCsPipeline`, `carryLockedRosterThroughGenerate` or
  `regenHistoryPipeline`.
- It adds no `createPRNG(` site, no numeric literal, no string literal carrying an em dash or an
  exclamation point, and no rendered figure.

---

## §7 · Exact change manifest

⭐ **GENERATED FROM `EM-R2.manifest.json` AND PROVED SET-EQUAL WITH IT** (§P9(d)); the proof is
quoted in §12 and in `EM-R2.evidence.md`.

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `MODIFY` | `src/generators/narrativeGenerator.js` | `generateCoherence` | **1 eff** | C1. One new parameter, one `chooseOrPin` wrap, one NEW import line at the end of the import block (this file carries no `./pipeline.js` import today — measured). ⛔ Do not edit `enrichNpcCoherence`, `relinkFactionMembers` or `inCoherenceSubstream`. |
| `MODIFY` | `src/generators/steps/assembleSettlement.js` | the `registerStep('assembleSettlement', …)` callback + one module const | **4 eff** | C2. ⛔⛔ **WIDEN THE EXISTING `from '../pipeline.js'` IMPORT, NEVER ADD ONE.** Gate only the appender. |
| `MODIFY` | `src/generators/steps/generatePopulation.js` | `powerLinkage` / `derivePopulation` | **5 eff** | C3. ⛔ Do not enter `linkFactions`, `drawPopulation` or the step registration. |
| `MODIFY` | `src/domain/prose/holderTable.js` | `HOLDER_SOURCES` — four `cite:` ADDRESSES | **4 lines** | C4. §P2.14 CITATION ROW: addresses only, no logic byte. RE-MEASURE each address against the built tree before writing it. |
| `CREATE` | `tests/generators/rosterFinalUnderHeldRoster.test.js` | A1–A7 of §9 — **adds exactly 7 literal `it`s**, one per sealed acceptance case, under ONE literal `describe` | **7 `it`s** | Copy `tests/generators/pipelinePinnedMode.test.js` arm for arm. Flat literal `it(...)` under ONE literal `describe`; `it` / `test` / `describe` each bound EXACTLY ONCE and never as a variable or arrow parameter; no bare seed loop (collect, then assert once); every negative assertion anchored on the line IMMEDIATELY above the `expect` itself; every asserted set IMPORTED (`RECORD_CLASSES` and `CLASS_EXCEPTIONS` from `recordRegister.js`, `goldenCorpus()` for the rows), never re-typed. |
| `REGISTER` | `scripts/mutation-coverage-manifest.json` | `invariants['tests/generators/rosterFinalUnderHeldRoster.test.js']` | **+1 row** | Add ONE row beside its siblings, surgically: `{ "kind": "rationale", "ref": "generators-tree-admitted-subject-coupled-2026-08-30" }` — the `ADMITTED_TREE_REF` of `tests/lint/mutationCoverage.shared.mjs :: ADMITTED_TREE_REF`, the exact shape 99 landed rows carry. `tests/generators` IS an enforcer dir (`mutationCoverage.shared.mjs :: ENFORCER_DIRS`), so the row is OWED. ⛔ Never re-serialise the manifest whole; touch no other row; `uncoveredBaseline` (186) UNMOVED. |

**Generated artifacts: `NONE`.**

> ⛔ **THE LIGHTING CENSUS IS A DEFERRED ROW, NOT AN ARTIFACT OF THIS PACKET.**
> `tests/lint/.lighting-census-baseline.json` — a named INTERIOR RED, delta only. This member
> causes **`files +1 · parked +0 · credited +1 · titles +7 · suiteTitles +1`**, all five figures
> in the register's own order because the walker stops at its FIRST moved figure. ⭐ `titles +7`
> is BOUND to the CREATE row's declared seven literal `it`s; `EM-R2.count-prover.mjs` ships
> beside this packet and exits non-zero on any inequality. The predicted red MESSAGE has the
> shape `expected <N+1> to be <N>`. ⛔ `LIGHTING_CENSUS_REFREEZE` is NEVER set by this lane; the
> refreeze is regenerated WHOLE at the train's terminal, BY THE CHAIR, and the absolutes are the
> chair's to stamp. ⚠ `credited +1` is CONDITIONAL on the file not parking: the build lane runs
> the walker's own `parkReasonsFor` (`sovereigntyLightingContract.walker.test.js :: parkReasonsFor`) over the
> new file and QUOTES its output. The three causes to avoid are a re-bound `it`/`test`/`describe`
> (including as an arrow parameter — 26 of the estate's parked files park for that alone),
> registration that is not straight-line (`.each`, a loop, a conditional, a nested `describe`),
> and an unresolved opener.

> ⛔ **THE WIRING CENSUS IS A DEFERRED PREDICTION AND THIS PACKET NAMES ITS PATH NOWHERE**
> (§P2.13 — not in this table, not in the capsule, not in `checks`, and no lane runs
> `node scripts/wiring-census.mjs`). `stamp.producerIndexFiles` **+0**, measured against BOTH
> producer roots: this member CREATEs no `.js` under `src/generators/**` and none under
> `src/domain/**` (its only CREATE is under `tests/`). `stamp.files` **UNMOVED** — it holds
> exactly SEVEN paths, all under `src/domain/display/stateProse/`, and none of this member's four
> source files is among them, so `proseWiringCensus`'s `stale-bytes` arm is NOT owed.
> `totals.*` **UNMOVED**. ⚠ The same walker's SEAM car 5b arm IS owed, and it is paid by the
> `holderTable.js` citation row above — that arm re-derives every producer write from a live
> syntax-tree pass, so a stale address reds it.

> ⭐ **PROSE-NUMERICS: `none found`.** `tests/lint/.prose-numerics-baseline.json` holds **ZERO**
> rows addressed at any of this member's four source files (measured, 0 row-lines each), so no
> row sits above an inserted line and neither this member nor its train's terminal owes a
> re-address there.

**Registers that DO NOT move, measured rather than assumed** — every figure EXECUTED at
`91cc9ef5a` over the tip and over a COPY-ONLY estate carrying the contract's planned text
(`plan/`; HZ-WORKTREE: copies, never a symlink):

- ⭐ **`tests/lint/.tuning-inventory.json` / `tuningRegister.walker.test.js` (§P2.17) — UNMOVED**,
  driven through the inventory's OWN library (`scripts/lib/tuning-inventory.mjs`:
  `discoverTables` → `countUnregisteredNamed` / `countBareDecimals` over `TREES_P2P3`), with the
  rule's POSITIVE CONTROL reading exactly its stated value (`src/domain/activeConditions.js`
  **P2 = 3**). Tip and plan alike: `narrativeGenerator.js` **P2 0 · P3 2**,
  `assembleSettlement.js` **P2 0 · P3 0**, `generatePopulation.js` **P2 0 · P3 0**; tree totals
  **214 tables · P2 sum 544 · P3 sum 7,010** — IDENTICAL. ⚠ The first cut of this measurement
  read RAW text and counted **five decimals inside COMMENTS** as dials (`§22.1` and kin); the
  library's own `codeOnly` strip is what makes the zero true, and the harness now imports it from
  `tests/helpers/codeOnlySource.js` exactly as the library does. The member mints no simulation
  dial, so no owner-signed tuning row is owed (§P11.3).
- ⭐ **`tests/copy/voiceMechanics.test.js` Tier 2 — UNMOVED.** Em dashes and exclamation points
  inside `src/` string literals, counted per file over the `codeOnly` strip: **0 / 0** on all
  three files, tip and plan. The one literal this member adds is `'npcs'`.
- ⭐ **`tests/lint/entropyRootCensus.walker.test.js`, BOTH ARMS — `createPRNG(` +0.** Measured at
  the tip: **36** under `src/domain`, **47** under `src/` whole (§P2.21(a)'s corpus). This member
  adds no `createPRNG(` site: the skip USES the existing `npc-enrichment` child fork rather than
  minting a stream, and it reads no new world root.
- ⭐ **`tests/lint/ruinFilterRoster.walker.test.js` — DISPOSITION: OUT OF CORPUS for three
  files, IN CORPUS AND UNMOVED for the fourth.** The walker's discovery corpus is `src/domain`
  alone (`ruinFilterRoster.walker.test.js :: DOMAIN`), and the three LOGIC files are under
  `src/generators`. ⭐ The fourth MODIFY, `src/domain/prose/holderTable.js`, IS under
  `src/domain` and therefore IS in the corpus — measured at `c69d16a5d`, it contains **zero**
  `.institutions` reads, and this member's edit there changes four `cite:` ADDRESSES and no code
  byte, so it neither joins nor leaves the discovery set. Discovery-set delta **+0**, executed
  over all four paths, and the member adds no `.institutions` read anywhere.
- ⭐ **`tests/build/vendorPdfLazy.test.js`'s ST-2 GOODS ROSTER — not owed.** This member imports
  no goods identity half-table; its ONE new import is `./pipeline.js` (measured: no
  `resourceData` or `goods.schema` reference in any planned file).
- ⭐ **THE EDGE-SHARED BUNDLES — none.** Measured by INPUT MEMBERSHIP against all five metas'
  own `inputs` lists (107 · 74 · 108 · 2 · 2 entries): none of this member's source files appears
  in any of them, so `npm run build:edge-shared` is NOT owed and no generated path is declared.
- ⭐ **THE EAGER FIRST-PAINT CLOSURE — +0 modules, +0 B.** Measured by IMPORTING
  `EAGER_FIRST_PAINT_MODULES` from the tree's own `vite.config.js` (**270** modules, re-measured
  at `c69d16a5d` and unchanged from version 1's figure); none of this member's FOUR source files
  is a member, and neither is `src/generators/pipeline.js`. This probe reads no `dist` and
  therefore CANNOT SKIP (§P2.11).
- ⛔ **THE GENERATION WORKER'S ZERO-SLACK BUDGET — MEMBERSHIP YES, PRICE PREDICTED.** All three
  source files ARE in the worker's static closure (187 modules AT VERSION 1's TIP, walked from
  `src/workers/generationRequest.js` and `generation.worker.js` over all three edge forms —
  a first walk that saw only `from '…'` missed the side-effect `import './steps/index.js';` and
  wrongly reported the two step files OUTSIDE). ⭐ **RE-MEASURED AT `c69d16a5d` (version 2): the
  static closure is 188 modules**, one more than version 1's 187 — EM-R0d's landed
  `src/data/bandLadders.js` — and all three source files are still IN it while
  `src/domain/prose/holderTable.js` is still OUT. ⛔ **AND THE CEILING HAS MOVED TWICE UNDER
  THIS PACKET:** `tests/build/generationWorkerLazy.test.js :: WORKER_BUNDLE_CEILING_BYTES`
  reads **`1395972`** at this tip (version 1 quoted `1396015`, train EM-T12's value; the
  T13 terminal's 12-byte buy-back `5c60fa791` lowered it, and EM-T14's buy-back `0f1a205a0`
  bought 3,901 B of headroom ahead of EM-R0d). The figure is EXACT, with zero slack, and it is
  re-read from the arm itself at the train's terminal rather than copied from here. **Predicted price:
  the +10 effective code lines, doubled as the standard requires — ≤ +600 B minified.** The raw
  source delta is +3,061 B but +2,7xx of that is COMMENT, which minification strips. The new
  import edge adds NO module: `pipeline.js` is reached by the worker without it (measured).
  ⛔ This member carries NO row on either byte-arm file and compiles as if it is **NOT** the
  train's byte-arm holder (§P2.11); it names the holder's bound at placement and the terminal
  takes ONE summed price from ONE real build. ⚠ The worker budget has NO placement cure.
- ⭐ **THE BROWSER SUITE (§P2.15) — NO SPEC.** This member touches no `src/components/**`, no
  route, no `data-testid` and no accessible name, so it names no `e2e/` file. An independent
  pre-proof re-derives that answer rather than inheriting it.
- ⭐ **`scripts/check-writer-reach.mjs` / `check-observed-shape-readers.mjs` — UNMOVED.** The
  member adds no domain reader of a settlement field under `src/domain/edit/**` and no reader of
  a save-time key; it writes neither `dmLayer` nor `decrees`, so §P2.3's exemptions door is not
  opened.

---

## §8 · Ordered coding sequence

1. **THE K-GATE, BEFORE THE FIRST EDIT** (§P10.3's six measurements): branch, HEAD and verified
   base are the three the dispatch note names; §7's path set and the capsule's `changeManifest`
   are SET-EQUAL; the CREATE target ABSENT and the four MODIFY targets PRESENT; all thirteen
   required symbols found verbatim; the count law run through `EM-R2.count-prover.mjs`; every
   line-addressed register grepped for all four MODIFY paths. Any failure STOPS before the seal.
2. Write the battery (A1–A7) RED-FIRST against the unedited tree and quote the COUNT LINE of each
   run — a log with no test count DID NOT RUN (§P7). A1 and A7 are WRITTEN-CONTRACT arms and are
   proved by a measured counterforce, not red-first (§P6).
3. C3 — `generatePopulation.js`'s split. Focused green.
4. C1 — `narrativeGenerator.js`. Focused green.
5. C2 — `assembleSettlement.js`. Focused green.
6. C4 — RE-MEASURE the four `holderTable.js` addresses against the tree AS BUILT and write them.
7. The REGISTER row, surgically.
8. Run the excluded whole-directory instrument and the lighting walker ALONE (§10), and
   `parkReasonsFor` over the new file. Quote all three.
9. Stage exactly the six §7 paths BY EXPLICIT PATH — never `git add -A`, `-u` or `.` — write the
   commit message to scratch, and END. The lane commits nothing (§P10.5).

---

## §9 · Acceptance matrix

Seven cases. Every one names the file that holds it and the §7 row that authorises that file.

| # | Case | Home | Authorised by |
|---|---|---|---|
| **A1** | INERT WITH NO PINS — the goldens cannot move. With no bag and with `pins: {}`, a ≥40-row stride of the golden master's own 525-row corpus hashes byte-identical to the COMMITTED fixture, and `generateCoherence(s, rng)` equals `generateCoherence(s, rng, null)` on `npcs` and `factions`. WRITTEN-CONTRACT: proved by the counterforce (the same arm against a planted pin reds), not red-first | `tests/generators/rosterFinalUnderHeldRoster.test.js` | §7's `CREATE` row |
| **A2** | THE SKIP AND ITS SITE. With `{ npcs: <a DELIBERATELY ALTERED roster> }`, `generateCoherence` returns THAT roster and the `npc-enrichment` stream is never entered (draw count 0 for that label, unchanged for the other four). THE ANTI-VACUITY ARM: at the tip the enrichment runs unconditionally, so "the pinned run reproduces the record" is green before this member exists | same | §7's `CREATE` row |
| **A3** | THE FOUR SIBLING STREAMS ARE UNMOVED. `prominentRelationship`, `coherenceNotes`, `history.historicalCharacter`, `history.siegeNarrative` byte-identical with and without the held roster, every row — with the fork law executed beside it and a PARENT draw as the control that does move a stream | same | §7's `CREATE` row |
| **A4** | THE MIRROR ALWAYS RUNS. A held roster with an edited `role` and `title`: every `factions[].members[]` chip for that id carries the edit. ANCHORED NEGATIVE with its positive control: the chip a gated mirror leaves still reads the pre-edit role | same | §7's `CREATE` row |
| **A5** | THE APPENDER MUST NOT ADD. Held roster short an office-holder ⇒ the roster is exactly the held length and carries no newly minted `generatedAs: 'faction_structural'` entry. BOTH DIRECTIONS: the same input unpinned appends N > 0 | same | §7's `CREATE` row |
| **A6** | THE LINKAGE'S DEAD RUN IS RETIRED. With `factions` held the four linkage-only lookups are not built, while the trace's direct/attraction/scatter classification is SET-EQUAL to the unpinned run's, both directions | same | §7's `CREATE` row |
| **A7** | THE CLASS REGISTER IS THE SOURCE. The gated key and the ungated mirror path are read BY IMPORT from `recordRegister.js` (`RECORD_CLASSES.npcs === 'HELD'`; `CLASS_EXCEPTIONS['factions[].members[]'] === 'MIRROR'`), never re-typed. WRITTEN-CONTRACT: proved by a perturbed-input counterforce | same | §7's `CREATE` row |

### §9a · ⭐ THE NEGATIVE CONTROLS THIS BATTERY MUST CARRY

1. **A5's uncovered-roster control is mandatory.** The appender adds +0 on 8/8 corpus rows at
   this tip, so an arm that only asserts "the roster did not grow" passes against a member that
   did nothing. The arm must also show N > 0 unpinned.
2. **A2's altered pin is mandatory.** A pin equal to the record's own roster is satisfied by the
   enrichment's own output on many rows; only an ALTERED roster distinguishes gated from ungated.
3. **A3's parent-draw control is mandatory.** Proving "the siblings did not move" needs an
   instrument that CAN see a moved stream.
4. **A4's anchor** sits on the line IMMEDIATELY above the `expect` itself — never above a wrapped
   `expect`'s first line (EM-B1c1 version 5's build red).

---

## §10 · Verification commands

Run through the shared gate mutex, one holder at a time; a lane runs no gated line itself under a
parallel train (§P10.5) — it writes ONE gate script with a `records.tsv` row per step and the
chair's queue runs it.

1. The sealed `checks` of `EM-R2.manifest.json` — **14 arrays, 56 test files in 9 directory
   groups**, computed by §P7's interim rule (TOOL-26's `scripts/governing-tests.mjs` does NOT
   exist at this tip — measured), MINUS the lighting walker and MINUS the two dist-reading byte
   arms. The array ENDS with `node scripts/implementation-packets.mjs validate`. ⭐ Re-measured at `c69d16a5d`: all 58
existing test-file arguments of the sealed `checks` are PRESENT, and the only absent one is the
CREATE target itself, by design.
2. **THE EXCLUDED DIRECTORY RUN, the build lane's instrument and never a sealed check:**
   `npx vitest run --pool=threads --maxWorkers=2 tests/lint --exclude=tests/lint/sovereigntyLightingContract.walker.test.js`
   MUST EXIT 0, with the exclusion proved by vitest's own collection listing.
3. **THE LIGHTING WALKER ALONE**, expected NONZERO, its `expected … to be …` line quoted — it
   names which of the five figures it stopped at.
4. `parkReasonsFor` over the new test file, output quoted (the `credited +1` is conditional on it).
5. `EM-R2.count-prover.mjs`, run from the kit path BEFORE the seal: `node EM-R2.count-prover.mjs
   EM-R2.md EM-R2.manifest.json`. ⛔ It is never placed in the tree, never a `checks` entry and
   never a §7 row.
6. Both TypeScript configurations BY NAME over the member's own `src/` paths, measured in plain
   node with the compiler API before any gate slot is asked for (§P7: EM-B3f version 3 reported
   GATE-READY on bare eslint and the queue redded `typecheck:ratchet` at two errors).
7. `tests/property/generatorGoldenMaster.test.js` and `tests/property/dossierProseManifest.test.js`
   — UNMOVED is the contract (§P3.1); motion is a STOP.
8. The mutants of §P6, each proving the source bytes changed, reddening the NAMED title,
   restored digest-exact, then green.

---

## §11 · Mandatory STOP conditions

In addition to `PACKET_STANDARD.md`'s list and §P8's:

1. Any motion of `generatorGoldenMaster` or `dossierProseManifest` with no pin bag.
2. Any `createPRNG(` site added, or either entropy-census arm moving.
3. `narrativeGenerator.js` exceeding **800** effective lines (37 of headroom).
4. A second writer of the roster appearing necessary, or a producer outside
   `enrichNpcCoherence` / `ensureFactionStructuralNpcs` needing a gate — that is §0.D's finding
   and §4a Q2's ruling, not a widening of this member.
5. The `npc-enrichment` fork label changing, or the skip needing to move outside it.
6. `relinkFactionMembers` appearing to need a gate.
7. Any instrument of §P2.21 redding with a figure this packet did not predict — the arm's own
   message decides, and a figure adopted by hand is the failure.
8. The worker's byte price rising past the train's byte-arm holder's stated bound, or any re-mint
   of `tests/build/generationWorkerLazy.test.js :: WORKER_BUNDLE_CEILING_BYTES` — `1395972` at
   `c69d16a5d`, EXACT with zero slack, and RE-READ FROM THE ARM at the terminal rather than
   copied from this packet (§P11.2 — a rise is the owner's; the cure is a buy-back).
9. A number landing under `src/generators` that is a genuine simulation dial (§P11.3 — the
   owner's). This member lands none.
10. The `holderTable.js` reservation firing at placement, i.e. EM-R0d still non-terminal — or
    any of this member's four `HOLDER_SOURCES` rows found MOVED at dispatch from the addresses
    §4a Q3 measured at `c69d16a5d` (`:169 :237 :238 :239`).
11. A spec, walker or ratchet needing to be weakened, widened or skipped.

---

## §12 · Completion receipt

The build lane's receipt quotes, by command and output:

1. The K-gate's six measurements (§8 step 1), each PASS or the smallest contradiction.
2. The count prover's line per file, exit 0, and the failing control's non-zero exit.
3. The red-first log of every arm with its COUNT LINE, and the counterforce beside each
   written-contract arm (A1, A7).
4. The focused battery green at the implementation commit, under the member's own seal.
5. The excluded whole-`tests/lint` run at exit 0 with its exclusion proved, and the lighting
   walker ALONE with its `expected … to be …` line.
6. `parkReasonsFor` over the new file.
7. Both TypeScript configurations by name at their exact floors, and scoped eslint.
8. The four `holderTable.js` addresses RE-MEASURED against the tree as built, before and after.
9. §7 and the capsule proved SET-EQUAL on paths and actions.
10. The tuning inventory, the voice counters, the entropy arms and the ruin-filter disposition
    re-run at the member's own tip, each stated as a DELTA.
11. The worker byte price as measured at the terminal's one real build, inside the holder's bound.
12. The lighting delta as predicted, with the red's own message.
