# Settlement editor / EM-R1 — THE PIN BAG IS CLONED ON ENTRY, AT THE RUNNER, ONCE, AND THE WRITER CENSUS OF EVERY HELD KEY BECOMES A WALKER (the re-entry family; the member every later EM-R consult rests on)

**Preamble:** docs/implementation/preambles/EM-PREAMBLE.md (SHA-256: **TO BE STAMPED BY THE CHAIR, MEASURED AT THE PLACEMENT TIP.** ⛔ **THIS PACKET NAMES NO HASH TO STAMP AND CARRIES NO 64-HEX DIGEST ANYWHERE IN ITS BODY** — §P3.5: the preamble's SHA-256 is measured at the placement tip with `shasum -a 256`, never carried in a document.)
- **The law this packet is written to:** ⭐ **THE SIXTH AMENDMENT, AND IT IS NOW LANDED AND MEASURED.** The compile (read tip `91cc9ef5a`) wrote this member to the sixth as SELECTED (judgment 144) while the fifth still stood in the tree. At this pre-proof's read tip `c69d16a5d` — train EM-T14's composed tip — the sixth is the tree's own preamble, measured rather than quoted: `shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md` at `c69d16a5d`, with §P2 rows 19–21 and the C2 tier-gate clause present in the file (train EM-T13's first commit `7bc14e003`). The sixth's §P2.19–21 instrument list, §P2.21(a)'s whole-`src/` entropy corpus, §P9's one effective-line instrument, §P2.11's byte-arm rule and §P10.11's two methods are the standards this version answers, each with an executed figure at `c69d16a5d`. **The hash itself is the chair's stamp at the placement tip and is named nowhere here.**

- **Status:** `READY`
  — Compiled 2026-09-22 by an Opus COMPILE seat (session cce01f87) at read tip `91cc9ef5a`, train
  EM-T12's landed tip, under `briefs/launch/COMPILE-EM-R1-cce01f87.md`; **RE-MEASURED to version 2
  by an Opus PRE-PROOF seat (same session) at `c69d16a5d`, train EM-T14's COMPOSED tip**, under
  `briefs/launch/PREPROOF-EM-T15-cce01f87.md`.
  ⛔ **READ §0 BEFORE PROMOTING.** §0.2's Q1 and §0.3's Q2 were RATIFIED by the chair as
  recommended (judgments 170a and 170b) and are carried here as RULINGS, not questions; §0.1 and
  §0.4 are measurements the lane does not adjudicate.
  ⚠ The status value stands ALONE on its line because `scripts/implementation-packets.mjs ::
  parsePacketHeader` anchors the status row at end-of-line and takes `status` only when exactly one
  row matches (§P9(c)) — re-read by symbol at `c69d16a5d`.
- **Packet version:** 3
  > ⭐⭐ **WHAT VERSION 3 CHANGED, AND WHY — THE CHAIR'S JUDGMENT 200, 2026-09-22.** The BUILD lane
  > built version 2 exactly as written, ran the pre-seal eight and then MEASURED the member's own
  > arm against the built tree, and A8 was refuted: `ctx.__pins[k] !== ctx[k]` was FALSE at the last
  > step in **63 of 63** census rows on all three pin channels, and §0.3's pristine property failed
  > in **42 / 56 / 11 of 63** rows (population / institutions / power). The cause is one step, not
  > one clone: `chooseOrPin` returns the pin BY REFERENCE (EM-B2a2's landed contract, which §5.1
  > requires stay true), so a held key's PRODUCER writes that very reference back into `ctx` through
  > its own patch, and the runner's second ENTRY clone — spent at the context spread — is overwritten
  > by the first patch that lands. Two clones at entry separate the channels only until each key's
  > producer runs. ⇒ **THE CHAIR RULED SHAPE 4 (judgment 200): THE RUNNER RE-CLONES A TAKEN PIN AT
  > THE PATCH MERGE, UNDER A BAG ONLY.** Version 3 carries that one construction and nothing else:
  > the contract's OUTCOME, its acceptance matrix (A1–A8, unchanged word for word), its manifest,
  > its `checks`, its count law and its two step-file rows are all as version 2 left them. What moved
  > is §6 (the merge-clone paragraph), §5.8 (the byte price is no longer a signed shrink), §7's
  > `pipeline.js` row (re-priced `+8 eff` → `+12 eff`), §0.3 (one sentence naming why the entry clone
  > alone is not enough), §5.6 (A7's negative control had to be re-cut) and §12 (three new receipt
  > rows). Shape 2 (cloning at the primitive) was REFUSED — a landed primitive's contract is not this
  > member's to move — and shape 3 (narrowing A8) was REFUSED — it would ship the census over held
  > facts that are not final, the defect §0.3 exists to prevent. Every figure in this block is
  > EXECUTED; the lane's ledger is `SHAPE-4.MEASUREMENTS.md`.
  > **What version 2 changed, and why.** Nothing in the contract. Version 2 is the pre-proof's
  > re-measurement of every figure against `c69d16a5d`, twenty-three packets and two trains after
  > the compile's base. Eleven figures moved and are re-stated from their own executed commands
  > (the landed `requiredSymbols` rows over these three paths 19 → **22**; the registered estate
  > 215 → **222**, and it is no longer free of non-terminal entries — three READY members of train
  > EM-T14 hold it, none of them on a path of this member's; the mutation-coverage `invariants`
  > 723 → **726**; `stamp.producerIndexFiles` 1,181 → **1,182**; the worker ceiling 1,396,015 →
  > **1,395,972**, which EM-T14's own terminal lowers again, so this packet now names no literal;
  > the worker closure 187 → **222** files with every membership verdict unchanged; the
  > prose-numerics rows over `assembleInstitutions.js` 2 → **6**, at the same two lines; the voice
  > baseline's TIER corrected from 2 to **5**, its figure unmoved; three line citations of
  > `src/generators/power/economyReconciliation.js` shifted −29 by EM-R0f's landed edit and are now
  > spelled `path :: symbol`; §5.5's "nothing addresses `pipeline.js` below the insert" REFUTED and
  > re-stated; and §0.4's path-disjointness re-taken, which found the row-keyed share with EM-R2
  > and EM-R4). One obligation is NEW: `tests/lint/moduleScopeCwdRatchet.test.js` is exact in both
  > directions over `tests/lint`, and this member's source-reading CREATE must spell the cured root
  > or red it by name (§5.7, §7, §11 item 9). The member's own reason — 42/63 → 0/63, the 63/63
  > alias, the 7 held keys, the 12 writer steps, the 11 function-level writers — re-executed at
  > `c69d16a5d` and did not move by one row.
- **Verified base:** `em-t15-r1-2026-09-22` at `ec4cd1c2d5aebbda4df788fd4e600252adb81b5c`
  ⛔ **THE LANE BRANCH RIDES THIS ROW, NOT A ROW OF ITS OWN** — re-measured at this pre-proof against
  the tree's own parser: `scripts/implementation-packets.mjs :: parsePacketHeader` takes
  `verifiedBranch` from the `Verified base` row itself, matching
  `` `<branch>` at `<40-hex sha>` ``, and it reads NO `Verified branch:` row. The chair's placement
  therefore writes this row as ``- **Verified base:** `em-t15-r1-<date>` at `<the train base's 40-hex sha>` ``
  — the value ALONE on the line, every caveat on these continuation lines (§P9(c), which binds this
  row as well as the Status row). The placeholder above is `__BASE__` alone, per the dispatch brief.
- **Last revalidated:** `ec4cd1c2d5aebbda4df788fd4e600252adb81b5c`
  > The chair's revalidation sentence: *re-measured at `__BASE__` — the J-T1 window
  > `c69d16a5d..__BASE__` over every change-manifest and `requiredSymbols` path; the eleven own and
  > the TWENTY-TWO landed `requiredSymbols` rows over the three `src/` paths still resolve verbatim;
  > `assembleInstitutions.js` still ends at 783 lines with `inst.name` on line 774; and the six
  > register figures (entropy 36 / 47, tuning 0 named per file, `stamp.producerIndexFiles` +0, the
  > eager set 270 / +0, the voice Tier-5 baseline `pipeline.js` em 2 / bang 0, and
  > `moduleScopeCwdRatchet`'s `tests/lint` 17) re-read as stated.*
- **Lane branch:** `em-t15-r1-<date>` (§P10.2 — the lane branch IS the verified branch and is written INTO the `Verified base` row above; the pattern is the chair's, the date is the placement's).
- **Depends on:** **EM-P0** (the runner seam; LANDED) · **EM-B2a1** (the population consults; LANDED) · **EM-B2a2** (`src/generators/pipeline.js :: chooseOrPin`, the primitive's one exported home; LANDED) · **EM-B2a3** (the institution and power consults and their two LOCAL clones, which this member RETIRES; LANDED, build `608b0d63d`, composed `2117054e8`) · **EM-R0a** (the class register `src/domain/edit/recordRegister.js`, this member's held-key denominator; LANDED) · **EM-P4**, **EM-A1**, **EM-C4a** and the rest of trains EM-T8–EM-T12 (LANDED).
  ⭐ **NOTHING THIS MEMBER NEEDS IS UNLANDED, AND TWO MORE TRAINS HAVE SINCE ARRIVED.** Train EM-T13
  (EM-A3 v9 · EM-B1b v6 · EM-D0e v1.2 · EM-B2a4 v4) **LANDED** at `35a031ac7`, and train EM-T14
  (EM-R0d v8 · EM-R0f v3 · EM-R6 v8) is COMPOSED at this pre-proof's read tip `c69d16a5d`, its three
  members READY in the manifest. §0.4 re-measures the path intersection against all seven FROM THE
  LIVE MANIFEST rather than from a packet, and it is still ∅ over the six change paths — so the
  dependency remains one of ORDER, not substrate. ⭐ **EM-B2a4 IS NOW CODE, NOT A PACKET:** every
  fact this member took from it as a document is re-measured against the landed file, and its three
  new `requiredSymbols` rows on `src/generators/pipeline.js` are carried in §5.2's count.
- **Collision group:** `src/generators/pipeline.js` (with no waiting member — EM-B2a2, EM-B2a4 and EM-P0 are LANDED; the waiting EM-R2/R3/R4/R5 only PIN symbols here, and every pinned symbol is a declaration or const line this member leaves byte-identical: §0.4 item 5) · `tests/generators/pipelinePinnedMode.test.js` (same) · `scripts/mutation-coverage-manifest.json` (ROW-KEYED, §P10.8 — this member's `rowKey` is `invariants['tests/lint/heldKeyWriterCensus.walker.test.js']`; **EM-R2 and EM-R4 each carry a row on this same file**, which is lawful only under distinct row keys: §0.4 item 4).
- **Commit authority:** edits only; the chair commits (§P10.5).
- **Baseline posture:** **measured, twice.** Every figure in §5 and §7 was executed at `91cc9ef5a` in plain `node`/`git` (`EM-R1.evidence.md`) and RE-EXECUTED at `c69d16a5d` by the pre-proof (`EM-R1.preproof.report.md` and its measurement ledger). Where the two disagree the SECOND stands and §5 carries it; `EM-R1.SEMANTIC-DIFF.md` lists every change with its command.

---

## §0 · ⛔⛔ FOUR MEASUREMENTS THAT SHAPE THIS MEMBER — none adjudicated by the lane

### §0.1 · ⭐⭐ THE LEAK IS LIVE AND IT IS BIGGER THAN THE CHARTER'S ROW SAYS: 42 OF 63 ROWS

The launch brief frames the runner clone as retiring EM-B2a3's two local clones. Executed at the
read tip, that is true but it is the SMALLER half. **EM-B2a1's four population choosers
(the four `chooseOrPin(pins, '<key>', …)` sites of
`src/generators/steps/generatePopulation.js :: generatePopulation`, for `npcs`, `relationships`,
`factions` and `conflicts`, all four re-found by symbol at `c69d16a5d`) **have NO CLONE ANYWHERE** —
`git grep -n -F 'structuredClone' -- src/generators`
returns exactly five sites, all five of them EM-B2a3's, and none in `generatePopulation.js`. The
consults hand the caller's own objects straight into the patch, `corruptionPass` (step 20) mutates
`ctx.npcs` and `ctx.factions` in place, and the DM's record moves.

```
$ EMR1_TREE=<tip> node probe-leak-full.mjs          # the REAL pipeline, census corpus, plain node
corpus rows = 63; keys = [npcs,relationships,factions,conflicts]
MODE=bare      rows=63 callerRecordMutated=42 threw=0 bagAliasedToCtx=63 perKey={"npcs":42,"factions":42}
MODE=preCloned rows=63 callerRecordMutated=0  threw=0 bagAliasedToCtx=63 perKey={}
```

⭐ **RE-EXECUTED AT `c69d16a5d`, the pre-proof's read tip, and the four figures did not move by one
row** — 42/63 bare, 0/63 pre-cloned, 63/63 aliased in BOTH modes, `{"npcs":42,"factions":42}`. The
defect this member cures is live at the base it will be placed on, not only at the base it was
compiled on.

`bare` is the runner as it stands; `preCloned` hands the runner a bag already deep-cloned, which is
behaviourally EXACTLY what this member does inside `runPipeline`. **42/63 → 0/63, by execution.**
The figure triangulates with RECON-ARCH-REDERIVE-2's P1.C (`corruptionPass|npcs 42/63 ·
corruptionPass|factions 42/63`), measured independently three days earlier on an older base.
This member's outcome is therefore stated as the CURE OF A LIVE DEFECT, not as a tidy-up, and §9's
A7 carries the 42/63 as its negative control. ⇒ No question: the charter's row is not refuted, it
is under-stated, and the lane records the wider figure rather than re-scoping the member.

### §0.2 · ⛔⛔ THE CHARTER'S "SKIPS NOTHING OUTSIDE `enrichNpcCoherence`" IS ALREADY RULED THE OTHER WAY, AND THE LANDED CODE IS THE OTHER WAY — **Q1, RATIFIED (judgment 170a)**

> ⭐ **THE CHAIR'S RULING, 2026-09-22 (judgment 170a), carried here so no later seat re-opens it:**
> *"RATIFIED AS RECOMMENDED — charter `:137`'s exemption governs the census: a registered STEP's
> `rng` is a named child fork, so a held step's whole production is SKIPPED (stream-safe), as
> EM-B2a3's landed code does at both its steps; CONSUME-AND-DISCARD applies to a FUNCTION-LEVEL
> writer inside a step that shares that step's stream (the name mint's two draws on the assembly
> step's ambient stream — §22.1 correction 1); only `enrichNpcCoherence`'s named child stream is
> skipped at function level (EM-R2). The charter's row `:178` … is re-worded at the charter's next
> amendment to say exactly this — SLOTTED in the kit INBOX; A5 stands as written to the ruled
> reading."* ⇒ **A5 is unchanged. §11 item 1 is retained only as the record of what a reversal
> would cost.** The analysis below is the measurement the ruling rests on.

The charter's EM-R1 row (`docs/implementation/charters/EDIT-MODE-TRAIN.md`, the EM-R family table
of the 2026-09-19 17:25 amendments) and design §22.2 ruling 11 both say the family's
first cut **CONSUMES AND DISCARDS** at every held writer outside `enrichNpcCoherence`. The launch
brief repeats it as item 2(b). **The tree disagrees, and it disagrees because the chair ruled it.**

- `src/generators/steps/assembleInstitutions.js :: assembleInstitutions`, the held-roster early
  return, **early-returns** on a held roster: the whole production below it, and every draw and
  `recordTrace` in it, is SKIPPED.
- `src/generators/steps/generatePower.js :: generatePower` does not call
  `createPowerGenerationIntent` or `projectPowerGenerationIntent` when both keys are held: those
  draws are SKIPPED too.
- EM-B2a3 raised exactly this as its **Q1** (its own §0) and recommended the chair rule
  `EDIT-MODE-TRAIN.md`'s named-child-stream exemption — *"only a named child stream is skipped"* —
  to govern, because **every registered step's `rng` IS a named child fork**:
  `src/generators/pipeline.js :: runPipeline` takes `rng.fork(name)` inside its step loop and
  `src/kernel/prng.js :: fork` spells `fork: (label) => createPRNG(...)`. The board records the
  ruling: *"EM-B2a3 v3 … its Q1/Q4 ruled as recommended"* (2026-09-21 10:51), discharged at
  placement as judgment 133b and written into the landed packet's §12; judgment 170a re-ratifies it
  for this member's census arm.
- §P10.11(ii) is explicit that **REAL CODE outranks a sibling's packet the moment it exists**.

⇒ **Q1 (RATIFIED, judgment 170a).** EM-R1's walker arm (b) cannot assert
"skips nothing outside `enrichNpcCoherence`" without convicting the landed code of two members of
train EM-T12. §6 therefore writes arm A5 to the RULED rule — *a writer is skippable exactly when
its draws are taken inside a stream forked by name that nothing downstream shares; every other
held writer consumes and discards* — with `enrichNpcCoherence`'s own `'npc-enrichment'` child
stream named as the one FUNCTION-level instance and the per-step forks as the STEP-level family.
**Ruled as recommended (170a): the named-child-stream exemption governs for the census too, and the
charter's `:178` row is re-worded in the same amendment**, so the charter, the design and the code
say one thing. A5 is written to the ruled reading; §11 item 1 records what a reversal would cost.

### §0.3 · ⛔ ONE CLONE LEAVES THE RUNNER'S OWN BAG ALIASED TO `ctx`, AND R2–R5 ARE THE MEMBERS THAT PAY — **Q2, RATIFIED (judgment 170b)**

> ⭐ **THE CHAIR'S RULING, 2026-09-22 (judgment 170b):** *"RATIFIED — the bag is cloned per CHANNEL
> ('cloned on entry, always' is a property of each channel, not a count of calls): the context
> spread receives a clone and `ctx.__pins` receives its own, so no ctx key aliases the caller's
> record and no consult placed at a mutator reads a mutated value as a held fact (A8 is the arm;
> the 63/63 alias measured today is the red-first)."* ⇒ **§6's two-channel clone and A8 stand as
> written.** The measurement below is what the ruling rests on, re-executed at `c69d16a5d`.
>
> ⛔⭐ **AND WHAT THE BUILD MEASURED, WHICH COMPLETES IT (judgment 200, 2026-09-22).** Two clones AT
> ENTRY are necessary and NOT sufficient, because they separate the channels only until each held
> key's PRODUCER STEP runs: `chooseOrPin` hands the pin back BY REFERENCE and the step writes that
> reference into its patch, so `Object.assign(ctx, patch)` re-points `ctx[key]` at the very object
> `_PINS_KEY` holds, and the next in-place mutator writes through the held bag. MEASURED on the
> built member with the entry clones alone: `ctx.__pins[k] === ctx[k]` at the last step in **63 of
> 63** rows on every channel, and the bag itself corrupted in **42 of 63** (population), **56 of
> 63** (institutions) and **11 of 63** (power). **What restores it is a THIRD act of the runner's,
> at the PATCH MERGE:** a patch value that IS one of the bag's own objects by identity is stored
> into `ctx` as a clone. With it, all three channels read **0 of 63** aliased at EVERY step and
> **0 of 63** corrupted, `chooseOrPin` is byte-identical, and the caller's record still moves
> **0 of 63**. The ruling of 170b is not reversed; it is completed.

`runPipeline` builds the context as `{ ...initialContext, ...pins, [_PINS_KEY]: pins }`
(`src/generators/pipeline.js :: runPipeline`, the context-accumulation expression): the bag's
VALUES are spread into `ctx` and the SAME bag object is handed
to every step at `__pins`. So `ctx[key]` and `ctx.__pins[key]` are ONE object. Measured above:
`bagAliasedToCtx = 63/63`, **in both modes** — a single entry clone does not break it, it only
moves it one level in.

Today that is latent, and the lane measured why: every consult runs BEFORE every mutator of its own
key (`assembleInstitutions` step 6 vs its five mutators at 7, 8, 9, 14, 15; `generatePower` step 12
vs 13, 17, 22; `generatePopulation` step 19 vs `corruptionPass` 20). **It stops being latent the
moment EM-R2–EM-R5 add consults AT the mutators**, which is the whole point of the census: a consult
inside `coherenceRepairPass` (step 15) would read `ctx.__pins.institutions` AFTER `cascadePass`
(step 8) had written through it, and would take a MUTATED value as a HELD fact. That is the exact
defect "held facts are final" exists to prevent.

⚠ And dropping EM-B2a3's local clones makes it strictly worse at those two steps: measured today
`bagAliasedToCtx = 0` for `assembleInstitutions` and `generatePower` precisely BECAUSE their local
clones replace `ctx[key]` with a private object. One entry clone plus no local clone takes those
two from 0 to aliased.

⇒ **Q2 (RATIFIED, judgment 170b).** §6 specifies **TWO clones, one per channel** — the bag at
`__pins` keeps the pristine copy, the context spread takes its own — so `ctx.__pins[k] !== ctx[k]`
holds by construction for every key (`structuredClone` breaks deep identity from its source:
measured). Cost: one extra deep clone per run, against a measured 1.0–1.7 % for the first (design
§22.1). Design §22 ruling 6's "cloned on entry, always" is ruled a property of the CHANNEL rather
than a count of calls. A8 is the arm; §11 item 2 records what a reversal would cost.

### §0.4 · THE TWO RESIDUES THIS MEMBER TAKES OVER BY NAME, AND THE PATH INTERSECTION

1. ⭐ **EM-B2a3's §12 slots §0.S's split to this member BY NAME:** *"`generationCoherenceReceipt ·
   defenseProfile | 46/63` is two figures at this tip: `defenseProfile` **46/63**,
   `generationCoherenceReceipt` **34/63** … SLOT: EM-R1's writer census re-takes §0.S as its own
   baseline (the row is EM-R1's)."* §5.4 carries the re-take as a measured row; it is a BASELINE,
   asserted nowhere (asserting it would be EM-R7's corpus ratchet).
2. **The brief's census script name is wrong and the lane says so rather than inheriting it.** The
   brief names `tools/rederive-prototype-2/q2-noedit.mjs`; no such file exists. The prototype's
   census scripts are `p1-census.mjs` (step level) and `p1b-writers.mjs` (function level), and
   `p3-noedit.mjs` is the no-edit reproduction. The lane measured the census at the tip with its own
   probes rather than re-running a three-day-old harness (§P3.5).
3. ⭐ **PATH INTERSECTION, RE-MEASURED AT `c69d16a5d` FROM THE LIVE MANIFEST** (not from a packet —
   judgment 134's law). Train EM-T13 has LANDED and train EM-T14 is COMPOSED, so the whole question
   is now answerable against code and against `docs/implementation/PACKET_MANIFEST.json` itself:
   **222 entries · 217 LANDED · 2 SUPERSEDED · 3 READY.** The three READY are EM-T14's members, and
   their change paths are `src/data/bandLadders.js` · `src/generators/{factionDynamics,defenseGenerator,foodGenerator}.js`
   · `src/domain/prose/holderTable.js` · `src/data/economyFingerprint.js` ·
   `src/generators/power/economyReconciliation.js` · `src/domain/{institutionRename,institutionRemoval,factionRename}.js`
   and their test rows. **Intersection with this member's six change paths: ∅.**
   ⚠ **But EM-R0f's landed edit of `src/generators/power/economyReconciliation.js` moved three
   lines this packet cited** — every one by −29 — which is why §5.4's roster is now spelled
   `path :: symbol` and carries no line (rule 32, judgment 168). The three SYMBOLS all resolve at
   the tip; the roster does not move by one row.
4. **`scripts/mutation-coverage-manifest.json` is row-keyed** (§P10.8, TOOL-27 `900a2368d`), so a
   sibling may carry its own row on the same train under a DISTINCT `rowKey`. This member's key is
   `invariants['tests/lint/heldKeyWriterCensus.walker.test.js']`, ABSENT from the live manifest
   (measured: `invariants` holds 726 rows at `c69d16a5d` and none of them is this file);
   `uncoveredBaseline` is **186**, UNMOVED. ⛔ **TWO WAITING SIBLINGS CARRY A ROW ON THIS SAME
   FILE** — EM-R2 and EM-R4 each declare a `REGISTER scripts/mutation-coverage-manifest.json` row.
   EM-R4's key is `invariants['tests/generators/nameHeldThroughAssembly.test.js']`, distinct from
   this member's. **EM-R2's capsule declares NO `rowKey` at all**, so whether EM-R2 may ride one
   train beside this member is a placement question, not a fact this lane can settle (§P10.8 refuses
   two members on the same key).
5. ⭐ **THE WAITING SIBLINGS PIN SYMBOLS IN THIS MEMBER'S OWN FILE, AND ALL OF THEM SURVIVE.**
   EM-R2, EM-R3, EM-R4 and EM-R5 each carry `requiredSymbols` rows on `src/generators/pipeline.js`:
   `export function chooseOrPin(pins, key, draw) {` · `export function chooseOrPin(pins, key, draw)`
   · `const _PINS_KEY = '__pins';` · `export function getStepOrder() {` ·
   `const _LEDGER_KEYS = new Set(['_traceClock', 'simulationTrace']);`. Every one is a declaration
   or a module-scope const that this member leaves BYTE-IDENTICAL — the insert lands inside
   `runPipeline`'s body — so no sibling owes a version bump to this member (§P2.18).

---

## §1 · Reconciled authority

| Check | Verdict at `c69d16a5d` (train EM-T14's composed tip) |
|---|---|
| CREATE target absent | **PASSES** — `tests/lint/heldKeyWriterCensus.walker.test.js` does not exist (`git ls-files` returns nothing; `ls` says No such file) |
| MODIFY / TEST / REGISTER targets present | **PASSES** — all five exist, plus `src/domain/edit/recordRegister.js` |
| every `requiredSymbols` row resolving | **PASSES** — all eleven found VERBATIM, `count=1` each, zero non-1 (§5.1) |
| the **TWENTY-TWO** LANDED `requiredSymbols` rows over these three `src/` paths | **PASSES, ALL TWENTY-TWO** (nineteen at the compile; EM-B2a4's landing added three on `pipeline.js`) — not one is a line this member edits, so `retiredSymbols` is **NONE** and the post-edit simulation (LANE-EM-PREPROOF step 10) is clean (§5.2) |
| git-clean / no sibling writes these paths | **PASSES** — the registered estate is **222 entries, 217 LANDED + 2 SUPERSEDED + 3 READY**; the three READY are train EM-T14's members and **none of them names a path of this member's**; every one of the 43 entries that does name one is LANDED (§5.3) |
| `assembleInstitutions.js` still 783 lines, `inst.name` still on `:774` | **PASSES** — the only change in the J-T1 window was one line replaced by one line (judgment 187's sentinel buy-back) |
| the design, the charter and the tree agreeing | ✅ **RULED** — §0.2's Q1 and §0.3's Q2 were ratified as recommended (judgments 170a / 170b). The charter's `:178` row is slotted for re-wording; the tree, the design and this packet now say one thing |

**Authority read, by SECTION and ITEM (never by line):** design `docs/DESIGN_EDIT_MODE_AND_DECREES.md`
§22 rulings 1, 2, 6, 8, 9; §22.1 corrections 1 and 2 and the whole-seam finding; §22.2 items 2, 7,
11; §22.3 items 1 and 6. Charter `docs/implementation/charters/EDIT-MODE-TRAIN.md`: the
"EM-B2a is RE-CUT" paragraph, the amendments of 2026-09-19 17:25 (the EM-R family table) and this
member's row. `EM-PREAMBLE` §P1–§P11 as amended by the sixth.

## §2 · Outcome

**What this member makes true.** (1) `runPipeline` deep-clones the pin bag before the first step
runs, so **no step can reach an object the caller owns** — measured 42/63 → 0/63 (§0.1) — and the
runner's own bag stays pristine against the consults EM-R2–EM-R5 will add at the mutators (§0.3).
(2) EM-B2a3's five LOCAL clones are retired, their obligation discharged at the runner exactly as
their own comments promised. (3) **The writer census of every held key becomes a walker**: a frozen
roster, classed producer · mutator · enricher · replay · mint · MIRROR, held EXACT IN BOTH
DIRECTIONS against the live registry and the live source, so a new writer of a held key reds by
name and a retired one reds too.

**Non-goals.** The merge (EM-R0c) · the roster final inside `enrichNpcCoherence` (EM-R2) · the
power replay (EM-R3) · the name (EM-R4) · the trace partition (EM-R5) · the institution rename
surface (EM-R6) · the corpus ratchet (EM-R7) · `rederive` itself (EM-B2a4). ⛔ **This member adds
NO consult.** It changes WHO OWNS the bytes a consult returns, and it writes down the roster the
later members gate. No held key changes hands; no golden moves.

## §3 · Hard scope budget

| Budget | Cap | This member | Instrument |
|---|---:|---:|---|
| handwritten files | 12 | **6** | §7 |
| new LOGIC leaves | 2 | **0** | the CREATE is a test |
| existing LOGIC files modified | 3 | **3** (`pipeline.js`, `assembleInstitutions.js`, `generatePower.js`) | at the cap, not over |
| new/changed effective production lines | 400 | **+12 / −0 net** (see below) | eslint `Linter`, `max-lines`, `skipBlankLines` + `skipComments` (§P9) |
| each new leaf | 250 eff | **n/a** — no new `src/` leaf | — |
| acceptance cases | 8 | **8** | §9 |

Effective lines MEASURED at `c69d16a5d` with the one instrument, and UNMOVED from the compile's
figures: `pipeline.js` **138**, `assembleInstitutions.js` **585**, `generatePower.js` **105** (the
control text reads 2, so the instrument is live; the 800-line layer ceiling governs
the last two). Predicted after, RE-PRICED AT VERSION 3 and measured on the built member: `pipeline.js` **150**
(+12: the entry-clone guard and its two statements, the named refusal, and judgment 200's merge
clone), `assembleInstitutions.js` **585** (+0 — three wrappers unwrapped IN PLACE),
`generatePower.js` **105** (+0 — two wrappers unwrapped IN PLACE). ⛔ **THE +0 FIGURES ARE A
CONTRACT, NOT AN ESTIMATE**: §7 forbids changing the LINE COUNT of either step file, because two
line-addressed registers sit in them (§5.5).

## §4 · Sealed dispatch and preflight

Dispatch on the lane branch `em-t15-r1-<date>`, cut from the train's ONE promotion commit
(§P10.1–§P10.2). The lane's FIRST act is §P10.3's six measurements, each a PASS or the smallest
measured contradiction: (i) branch, HEAD and verified base are the three the dispatch note names;
(ii) §7's path set and the capsule's `changeManifest` are SET-EQUAL; (iii) the CREATE target is
ABSENT and all five other targets PRESENT at HEAD; (iv) all eleven required symbols found verbatim;
(v) the count law through `EM-R1.count-prover.mjs`, run FROM THE KIT PATH; (vi) every line-addressed
register grepped for every path this member modifies (§5.5's commands re-run). Any failure is a STOP
BEFORE the seal. ⭐ **A SEVENTH, ADDED AT THE PRE-PROOF:** `wc -l` on both step files BEFORE the
first edit, recorded, because §7's `+0 LINES` is a contract and `tests/generators/generationForkCensus.test.js`
reads `assembleInstitutions.js:774` live (§5.5).

## §5 · Verified tree contract

### §5.1 · The eleven required symbols, found verbatim (RE-RUN AT `c69d16a5d`: `count=1` on every row, zero non-1)

| Path | Symbol | count | What the deliverable must preserve |
|---|---|---:|---|
| `src/generators/pipeline.js` | `export function runPipeline` | 1 | the runner this member edits; the signature and the `options` contract are untouched |
| `src/generators/pipeline.js` | `const _PINS_KEY = '__pins';` | 1 | ⛔ BYTE-IDENTICAL — EM-B2a2's landed row pins it AND cites its address; it sits ABOVE every line this member adds |
| `src/generators/pipeline.js` | `export function chooseOrPin(pins, key, draw)` | 1 | EM-B2a2's primitive; ⛔ it still clones NOTHING — the clone is the runner's, and `pipelinePinnedMode.test.js`'s by-reference arm stays green (§5.6) |
| `src/generators/pipeline.js` | `const prevRng = setActiveRng(stepRng);` | 1 | EM-P0's ambient binding |
| `src/generators/pipeline.js` | `const stepOrder = getStepOrder();` | 1 | the order this member's clone must precede |
| `src/generators/steps/assembleInstitutions.js` | `provides: ['institutions', 'catalogForTier', 'generationRepairs'],` | 1 | EM-B2a3's chooser list, owed whole |
| `src/generators/steps/generatePower.js` | `provides: ['powerIntent', 'powerStructure'],` | 1 | the second chooser list |
| `src/generators/steps/assembleInstitutions.js` | `registerStep('assembleInstitutions'` | 1 | EM-A1's row |
| `src/generators/steps/generatePower.js` | `registerStep('generatePower'` | 1 | EM-A1's row |
| `src/domain/edit/recordRegister.js` | `export const RECORD_CLASSES` | 1 | ⭐ THE HELD DENOMINATOR, IMPORTED — EM-R0a's landed register; the walker re-types no list |
| `src/domain/edit/recordRegister.js` | `export const SAVED_ONLY_KEYS` | 1 | the four keys generation never writes, subtracted from the denominator |

⛔ **`_note` fields on these rows carry NO `path:line` address.** Judgment 124 makes a live
packet's own `_note` addresses a line-addressed register; this member shifts lines in
`pipeline.js`, so it removes the whole class by construction rather than re-addressing it.

### §5.2 · The post-edit validation, simulated — `retiredSymbols` is NONE (**TWENTY-TWO** rows, not nineteen)

**TWENTY-TWO** LANDED `requiredSymbols` rows name these three `src/` paths, re-derived from the
live manifest at `c69d16a5d`: MF-CH3 ×1, EM-P0 ×3, EM-P2 ×6, EM-A1 ×2, EM-B2a2 ×2, EM-B2a3 ×5 and
⭐ **EM-B2a4 ×3** (`export function runPipeline(initialContext, rng, options = {})`,
`export function getStepMeta()`, `export function chooseOrPin(pins, key, draw) {` — the three that
landed with train EM-T13 after this member was compiled). All twenty-two resolve verbatim at the
tip. **Not one of them is a line this member edits**: every pinned string is a signature, a
`registerStep(` opener, a `provides:` list, a const declaration or an expression this member leaves
byte-identical — the insert goes INSIDE `runPipeline`'s body, below its own declaration line. ⇒ no
retirement, nothing to discharge, and `check:packet` re-asserts all twenty-two verbatim after this
member's own edits.

### §5.3 · The registered estate, and the path reservation

**222 entries · 217 LANDED · 2 SUPERSEDED · 3 READY** (re-measured at `c69d16a5d`; the compile read
215 / 213 / 2 / 0). ⛔ **THE ESTATE IS NO LONGER FREE OF NON-TERMINAL ENTRIES:** train EM-T14's
EM-R0d, EM-R0f and EM-R6 are READY. **None of the three names a path of this member's** (§0.4 item
3 lists their fifteen change paths). Every one of the **43** entries that DOES name one of this
member's paths — `src/generators/pipeline.js` (EM-P0, EM-P2, EM-B2a2, EM-B2a3, EM-B2a4),
`assembleInstitutions.js` (MF-CH3, EM-P2, EM-A1, EM-B2a3), `generatePower.js` (EM-P2, EM-A1,
EM-B2a3), `tests/generators/pipelinePinnedMode.test.js` (EM-P0, EM-B2a2),
`src/domain/edit/recordRegister.js` (EM-R0a, EM-B3d, EM-B2a1, EM-C4a, EM-B2a4) and the
mutation-coverage manifest's many row holders — is **LANDED**, so no PATH is held against this
member. `scripts/mutation-coverage-manifest.json` is ROW-KEYED and reserved by row (§0.4 item 4).

### §5.4 · The writer census, RE-MEASURED at `c69d16a5d` — the roster this member freezes

> ⭐ **EM-R0d RE-WROTE THREE PRODUCERS (`defenseGenerator.js`, `factionDynamics.js`,
> `foodGenerator.js`) IN THIS WINDOW, AND THE CENSUS DOES NOT MOVE BY ONE ROW.** Re-derived from the
> live `getStepMeta()` and the live `RECORD_CLASSES` at `c69d16a5d`: **7** held keys, **22** steps,
> **12** writer steps, the same per-key roster; and all **11** function-level writers plus both
> ASSERT symbols resolve at their paths. None of EM-R0d's three files declares or mutates a held
> key, and none of them holds a roster symbol. ⚠ What DID move is three line ADDRESSES inside
> `src/generators/power/economyReconciliation.js` (EM-R0f's landed edit, −29 each), which is why
> every row below cites `path :: symbol` and no row cites a line (rule 32, judgment 168).

**The denominator, IMPORTED:** `RECORD_CLASSES` minus `SAVED_ONLY_KEYS` gives **7** held keys a
generated record carries — `conflicts · factions · institutions · name · npcs · powerStructure ·
relationships`. (Three further keys are classed HELD but are `SAVED_ONLY` — `neighbourNetwork`,
`interSettlementRelationships`, `crossSettlementConflicts` — and generation writes none of them;
the walker subtracts them from its producer, never by hand.)

**STEP-LEVEL writers — 12 steps of 22, derived from the LIVE `getStepMeta()`:**

| held key | provides | mutates |
|---|---|---|
| `institutions` | `assembleInstitutions` | `subsumptionPass` · `cascadePass` · `isolationPass` · `factionCorrelationPass` · `coherenceRepairPass` |
| `powerStructure` | `generatePower` | `neighbourFactions` · `powerEconomyReconcilePass` · `assembleSettlement` |
| `npcs` | `generatePopulation` | `corruptionPass` |
| `factions` | `generatePopulation` | `corruptionPass` |
| `relationships` | `generatePopulation` | — |
| `conflicts` | `generatePopulation` | — |
| `name` | — (no step declares it) | — |

**FUNCTION-LEVEL writers inside `assembleSettlement` — 11, each found by symbol at the tip:**

| # | home `path :: symbol` | class | stream |
|---:|---|---|---|
| 1 | `src/generators/npcGenerator.js :: generateSettlementName` | **mint** | the assembly step's own SHARED ambient stream — ⛔ consumes and discards (design §22.1 correction 1: skipping it moved `pressureSentence` and `arrivalScene` in 63/63) |
| 2 | `src/generators/narrativeGenerator.js :: generateCoherence` | **enricher** | `inAssemblySubstream(rng, 'canonical-coherence', …)` |
| 3 | `src/generators/narrativeGenerator.js :: enrichNpcCoherence` | **enricher** | ⭐ `inCoherenceSubstream(coherenceRng, 'npc-enrichment', …)` — **the ONE function-level writer inside its own named child stream, and the only lawful skip** (EM-R2's, not this member's) |
| 4 | `src/generators/narrativeGenerator.js :: buildPoliticalNarrative` (module-private) | **enricher** | inside `'npc-enrichment'` |
| 5 | `src/generators/npcGenerator.js :: mergeNPCLists` | **enricher** | inside `'npc-enrichment'` |
| 6 | `src/generators/density/applyDensityLaw.js :: disperseNamedRoster` | **enricher** | inside `'npc-enrichment'` (dormant by default; returns its input by reference with no draw) |
| 7 | `src/generators/npcStructure.js :: enrichNPCsWithStructure` | **enricher** | inside `'npc-enrichment'` |
| 8 | `src/generators/narrativeGenerator.js :: relinkFactionMembers` | ⭐ **MIRROR** | none — a pure remap of `factions[].members[]` onto the enriched npcs, **0 draws; it ALWAYS runs** (design §22.1 correction 2; EM-R0a classes `factions[].members[]` MIRROR) |
| 9 | `src/generators/factionRoles.js :: ensureFactionStructuralNpcs` | **producer** | the assembly step's shared stream |
| 10 | `src/generators/power/economyReconciliation.js :: reconcilePowerStructure` | **replay** | 0 draws on the step's stream — it replays from a privately minted `createPRNG(intent.rngSeed)` |
| 11 | `src/generators/density/applyDensityLaw.js :: resizePoliticalRoster` | **replay** | the assembly step's shared stream (dormant by default) |

**The two ASSERTS, complete, and NOT writers:**
`src/generators/power/economyReconciliation.js :: assertPowerEconomyFreshness` (three call sites)
and `src/generators/power/economyReconciliation.js :: assertStableGeneratedRoster`
(module-private, two sites inside `reconcilePowerStructure`). They are the census's seventh column
and EM-R3's subject; the roster names them as ASSERT rows so that a third assert appearing reds.
⚠ All three of these symbols moved −29 lines under EM-R0f's landed edit of that file; the WALKER
resolves them by symbol from source and carries no address, which is why the move costs this member
nothing.

⭐ **`powerIntent` IS NOT IN THIS CENSUS, AND CANNOT BE.** `src/generators/steps/generatePower.js`
declares `provides: ['powerIntent', 'powerStructure'],` — so `powerIntent` is a CHOOSER — but it is
absent from `src/domain/edit/recordRegister.js :: RECORD_CLASSES` (47 keys, measured), so it is not
a held record key and the walker's IMPORTED denominator excludes it by construction rather than by
omission. The only place the word survives in this member is inside EM-B2a3's pinned `provides:`
row, kept byte-identical. The `recordPath`-less chooser is EM-R1b's subject (judgment 182a), not
this member's, and §11 item 8 forbids widening the denominator by hand to reach it.

**⭐ §0.S RE-TAKEN, as EM-B2a3's §12 slots to this member** (a BASELINE, asserted nowhere):
`defenseProfile` **46/63** and `generationCoherenceReceipt` **34/63** are two figures, not the
merged row EM-B2a3 inherited.

### §5.5 · Every line-addressed register, grepped — and the ONE constraint it writes into §7

`git grep -n -P '<path>:[0-9]+' -- .` over each of the three `src/` paths, plus
`.prose-numerics-baseline.json`, `.tuning-inventory.json` and `PACKET_MANIFEST.json`:

| register | rows over this member's paths, MEASURED at `c69d16a5d` | verdict |
|---|---|---|
| `tests/lint/.prose-numerics-baseline.json` | `assembleInstitutions.js` **SIX rows at two lines** — `:62` ×2 (`floatInterpolation`, `percentToken`) and `:69` ×4 (`floatInterpolation`, `multiplier`, `percentToken`, `twoDecimalScore`); `pipeline.js` and `generatePower.js` **none** | ⛔ the compile said "both rows"; there are six, at the same two lines. Both lines sit ABOVE every line this member touches (the comment at ~`:229-234` and the consults at `:242-244`) ⇒ **UNMOVED**, provided the file's line COUNT does not change |
| `tests/generators/generationForkCensus.test.js` `DISPLAY_NAME_KEYED_SITES` | `{ site: 'src/generators/steps/assembleInstitutions.js:774', channel: 'hash', marker: 'inst.name' }`, read LIVE by the census arm | ⛔ **THE BINDING CONSTRAINT, RE-VERIFIED: `assembleInstitutions.js` ends at EXACTLY 783 lines and line 774 carries `inst.name`** (`inst.desc = pickVariant([inst.desc, ...variants], \`${ctx._seed}:${inst.name}\`);`). No line may be added or removed above `:774`; §7 makes the unwrap line-for-line for this reason |
| `PACKET_MANIFEST.json` `_note`s ABOVE the insert | EM-B2a2's rows cite `pipeline.js:182` and `:59`; other landed rows cite `:112`, `:117`, `:152`, `:171` | ⛔ this member inserts INSIDE `runPipeline`'s body, BELOW its `:182` declaration line ⇒ **every one of these addresses is UNMOVED** |
| ⛔ `PACKET_MANIFEST.json` `_note`s **BELOW** the insert — **THE COMPILE'S "none found" IS REFUTED** | **TWO rows address `src/generators/pipeline.js:259`** (EM-B2a4's landed rows, for `getStepMeta`) | ⛔ they DO sit below the insert and this member shifts them. **They cost nothing, for three measured reasons, and the packet relies on all three rather than on any one:** (i) the address is **already stale at this tip** — `src/generators/pipeline.js :: getStepMeta` is at line **270**, not 259, so the member widens an existing drift rather than minting one; (ii) `tests/lint/sourceCitationIntegrity.walker.test.js` **gates only past-EOF** (ARM 1 over `src/`/`tests/`/`scripts/`, ARM 2 over live docs against a shrink-only baseline) and its SYMBOL arm never gates — a file that GROWS can neither mint a past-EOF citation nor clear one; (iii) `tests/lint/.source-citation-baseline.json` holds **ZERO rows** naming any of this member's three paths, so ARM 2's "a baselined row has cleared" arm has nothing to clear. ⇒ **no re-address is owed here.** ⚠ Under rule 32 the honest cure is the chair's, at a terminal, and is a re-spelling by SYMBOL of a LANDED packet's rows, not this member's edit |

⇒ **One re-address obligation EXISTS and is measured to cost nothing (the row above); every other
register is UNMOVED. The commands are quoted in the pre-proof report.**

### §5.6 · The `tests/` sweep (§P2.12) — what moves, what does not

- `tests/generators/pipelinePinnedMode.test.js` A1's *"The pin is the caller's own object BY
  REFERENCE: the primitive clones nothing"* asserts `chooseOrPin(bag, 'npcs', draw) === bag.npcs`
  **called directly on a plain bag**, never through `runPipeline`. ⇒ **UNMOVED and still true**: the
  PRIMITIVE clones nothing; the RUNNER clones. Its next line, *"Cloning is EM-R1's rule, at the
  runner, and it is not this member's"*, becomes fulfilled rather than stale.
- `pipelinePinnedMode.test.js` A6 (*three pinned calls agree, nothing is mutated*) — **UNMOVED**;
  the clone only strengthens it.
- `tests/generators/pipelinePinnedChoosers.test.js` A6 — **UNMOVED, ALL SIX FIGURES.** Its sealed
  half compares `patch[key] === pins[key]` against the CALLER's bag, and a runner clone keeps that
  `false`; its negative control injects by `onStep`, outside the pins channel entirely, so
  `bareAssemble.mutated 52`, `threw 2`, `barePower.mutated 30` and `rows 63` do not move. ⛔ This
  member therefore names that file NOWHERE and adds no row to it.
- `tests/generators/generationForkCensus.test.js` — **UNMOVED**; no fork label, no `createPRNG(`
  site and no census row changes. ⛔ Its `DISPLAY_NAME_KEYED_SITES` row is the line-count contract of
  §5.5, re-verified at `c69d16a5d`.
- ⭐ **THE LIGHTING INPUTS, RE-COUNTED AT `c69d16a5d`:** `tests/generators/pipelinePinnedMode.test.js`
  carries **2 literal `describe`s and 11 literal `it`s**, all straight-line, and binds `it`, `test`
  and `describe` nowhere but its own `vitest` import — so the file is CREDITED and the TEST row's
  `titles +2 · suiteTitles +1` stands. (The `credited +1` half of the CREATE's own delta stays
  PLAUSIBLE: `parkReasonsFor` is module-private to the lighting walker and cannot be imported in
  plain `node`; §12 makes the build lane run it and quote `[]`.)
- The goldens (`generatorGoldenMaster.test.js`, `dossierProseManifest.test.js`) — **UNMOVED BY
  CONSTRUCTION**: with `options.pins` absent, `pins === null` and not one new statement executes.
  MEASURED at the build by counting every `structuredClone` a run executes: **no pins = the
  pipeline graph's own 1** (the runner contributes none), **empty bag = 3** (the two entry clones,
  and ZERO merge clones), **a full four-key bag = 7**. The no-pins and empty-bag settlements are
  byte-identical, and a 15-row stride over the COMMITTED `generator-golden-master.json` moved none.
- ⛔ **A7's NEGATIVE CONTROL IS RE-CUT FOR THE MERGE CLONE (judgment 200).** A control that undoes
  the aliasing only at the FIRST step boundary is defeated by the merge clone and reports a FALSE
  `0 of 63`. The control this member ships re-points `ctx[k]` AND `ctx.__pins[k]` at the caller's
  own objects at **every** step boundary, which is byte-for-byte the aliasing the pre-member runner
  carried for the whole run; measured, it lands on §0.1's published figures exactly — **42 of 63,
  `npcs` 42 and `factions` 42**. A control that comes back green is a false proof, not a pass.

### §5.7 · The instrument list (§P2.21), every disposition a row

| instrument | measured at `c69d16a5d` | this member's delta | disposition |
|---|---|---|---|
| entropy-root census, both arms | `createPRNG(` **36** under `src/domain`, **47** under `src/` whole — the walker's own pins, unmoved from the compile | **+0 / +0** | NOT OWED — no new stream is minted; the walker is a test and the corpus is `src/` |
| ruin-filter roster | its discovery root is `src/domain` alone (`const DOMAIN = join(ROOT, 'src', 'domain')`) | **+0** | **EXEMPT BY ROOT, MEASURED not recalled** — all three modified files are under `src/generators` |
| ST-2 goods roster | this member imports no goods identity half-table | **+0** | NOT OWED |
| voice mechanics, **TIER 5** (`src/generators`), per file | ⛔ **the compile called this Tier 2; it is Tier 5** — `src/generators/**/*.js` is baselined in `tests/copy/.voice-mechanics-generators-baseline.json`, not in the Tier-2 `src/data + src/domain` baseline. The FIGURE is unmoved: `src/generators/pipeline.js` **em 2 · bang 0**; both step files **ABSENT from the baseline (held at zero)** | **+0 / +0** | ⛔ OWED AS A CONSTRAINT: §7 forbids an em dash or an exclamation point in any `src/` string literal this member writes, and spells `': '` where prose would want one. A per-file baseline NEVER RISES (§P2.19) |
| tuning inventory, per file | `pipeline.js` 0 / 0 · `assembleInstitutions.js` 0 named / **34 bare** · `generatePower.js` 0 / 0 | **+0 on all three** | UNMOVED — this member writes no numeral and removes none. ⚠ The tree TOTALS the compile quoted are not re-stated: a total is the chair's stamp at promotion, never a member's absolute (§P2.1's rule, applied) |
| `stamp.producerIndexFiles` (`docs/content/wiring-census.json`) | **1,182** (1,181 at the compile; EM-R0d's `src/data/bandLadders.js` joined the producer index) | **+0** | this member CREATEs no `.js` under `src/domain` or `src/generators`; `stamp.files` (7 entries, all `src/domain/display/stateProse/**`) names none of its paths and is UNMOVED |
| `EAGER_FIRST_PAINT_MODULES` | **270** modules, imported from the tree's own `vite.config.js` (no `dist` read, cannot skip) | **+0 modules, +0 B** | none of the three paths is a member |
| ⛔ `tests/lint/moduleScopeCwdRatchet.test.js` — **NEW AT THE PRE-PROOF, NOT PRICED BY THE COMPILE** | `POPULATION_CEILING = { 'tests/build': 30, 'tests/lint': 17 }`, **EXACT IN BOTH DIRECTIONS** (A4 reds an overrun, its sibling arm reds an underrun). The predicate is a module-scope `process.cwd()` call in comment-stripped source | **+0, BY CONSTRUCTION AND ONLY BY CONSTRUCTION** | ⛔ OWED AS A CONSTRAINT, not a row: this member's CREATE **reads source**, so it needs a root, and a root spelled `process.cwd()` at module scope takes `tests/lint` **17 → 18** and reds A4 BY NAME. §7's CREATE row therefore spells the cured root — `const ROOT = join(dirname(fileURLToPath(import.meta.url)), '../..')` — which is the shape the ratchet's own CURE names. With the cured root the population is UNMOVED and no manifest row is owed |
| `WORKER_BUNDLE_CEILING_BYTES` | **1,395,972 at this tip** (the compile read 1,396,015; judgment 187's sentinel buy-back lowered it, and train EM-T14's own terminal lowers it again) | **small, SIGN UNSTATED** (version 3) | ⛔ see §5.8. **This packet names no ceiling literal**: the constant is whatever this member's train base carries |
| `e2e/` browser suite (§P2.15) | this member touches no component, route, `data-testid` or accessible name | **none** | NOT OWED, and it names no spec |

### §5.8 · The byte price, and the MEMBERSHIP PROOF that cannot skip

All three modified files are **MEMBERS of the generation worker's static closure** — re-proved at
`c69d16a5d` by a plain-node BFS over `src/workers/generation.worker.js`'s own static import graph,
which reads no `dist` and therefore **cannot skip**: **222 first-party modules** (187 at the
compile; EM-R0d's own comment reads 221 at its base plus its new `src/data/bandLadders.js`, which
triangulates), with `pipeline.js`, `assembleInstitutions.js` and `generatePower.js` ALL PRESENT,
`src/domain/edit/recordRegister.js` ABSENT (confirming §5.4's boundary), and a control file
(`src/components/nav/ArrowControl.jsx`) correctly absent. **The membership verdict — the only fact
the packet rests on — did not move; only the closure's size did.**

**Predicted delta: SMALL AND UNSIGNED (re-stated at version 3).** Five `structuredClone(…)` wrappers
leave two worker-closure files; the runner gains two entry clones, one short refusal message and the
merge-clone guard (a four-line `if`/`for` over `Object.keys(patch)`). Version 2 predicted a signed
**−20 to −80 B** shrink on the strength of the five wrappers alone; judgment 200's third act adds
minified bytes back to `pipeline.js`, so **this packet now predicts only that the movement is small
and does not name its SIGN** — the terminal's one real build measures it, and the ceiling's arm is
`toBeLessThanOrEqual`. ⛔ If that build measures a RISE, §11 item 5 governs and the buy-back is the
owner's, not this lane's.
⛔ **THIS MEMBER MUST NOT RISE THE CEILING** (§P11.2) and **compiles as if it is NOT the train's
byte-arm holder** (§P2.11): it carries **NO row** on `tests/build/generationWorkerLazy.test.js` or
`tests/build/vendorPdfLazy.test.js`, states its delta under the holder's bound, and names the holder
at placement. ⭐ **AND IT NAMES NO CEILING LITERAL.** The constant moved twice between this member's
compile and this pre-proof (1,396,015 → 1,395,972 by judgment 187's buy-back, and EM-R0d's own
comment states that train EM-T14's terminal lowers it again to the composed measurement), so the
only true statement a non-holder can make is the one made here: the ceiling is whatever this
member's train base carries, this member spends none of it, and it is MONOTONE-DOWN, so a re-mint
downward — if the terminal's one real build measures one — is the chair's summed act and not this
member's.

## §6 · Exact contracts

### Inputs and outputs

`runPipeline(initialContext, rng, options = {})` — **the signature is unchanged.** One behaviour is
added, before the step loop and after the existing `options.pins` type validation:

- `options.pins` **absent or `null`** ⇒ `pins === null` ⇒ **today's behaviour EXACTLY, byte for
  byte**, and no clone is taken. This is the golden's whole protection and it is by construction.
- `options.pins` **a plain object** (including `{}`) ⇒ the runner takes **two deep clones by
  `structuredClone`**, one for each channel it seeds:
  - the CONTEXT SPREAD receives its own clone, so a step that reads `ctx[key]` before the key's
    producer runs sees a runner-owned object;
  - `ctx[_PINS_KEY]` receives the other, so the bag every consult reads stays pristine no matter
    what a later pass writes through `ctx` (§0.3).
  `structuredClone({})` is `{}`, so the empty bag stays byte-identical and `pinned` is still false.
- **The ENTRY clone is taken ONCE, before the first step**, never per step and never per consult.
- ⭐⭐ **AND THE RUNNER RE-CLONES A TAKEN PIN AT THE PATCH MERGE (judgment 200).** Two entry clones
  separate the channels only until a held key's PRODUCER runs: `chooseOrPin` returns the pin BY
  REFERENCE, the step puts that reference in its patch, and `Object.assign(ctx, patch)` re-points
  `ctx[key]` at the object `_PINS_KEY` holds — after which the key's in-place mutators write through
  the held bag (MEASURED without this act: aliased at the last step in 63 of 63 rows on all three
  channels; the bag corrupted in 42 / 56 / 11 of 63). So, immediately after the merge and ONLY when
  a bag is present, every patch key that is an OWN key of the bag whose merged value IS the bag's
  own object by IDENTITY is replaced in `ctx` by its own `structuredClone`. ⛔ **IDENTITY, NEVER
  VALUE**: a step that produced an equal-looking value of its own is left alone, so the act cannot
  reach a key the bag did not hand over. ⛔ **`chooseOrPin` IS BYTE-IDENTICAL** — the PRIMITIVE still
  clones nothing, and EM-B2a2's landed by-reference row stays true at its own home. Cost, counted:
  **one clone per held key per run** (a full four-key bag executes four), **zero** with an empty bag,
  and **zero** in the unpinned path, where the whole block is guarded off by `pins !== null`.
- **The partial-pin refusal is EXACTLY as EM-P0 landed it** — same condition, same message, same
  `onStrictViolation` branch — and it is evaluated against the CLONE, which carries the same own
  keys (measured below).
- ⭐ **A BAG THE STRUCTURED-CLONE ALGORITHM REFUSES GETS THE RUNNER'S OWN NAMED REFUSAL.** A pin
  value that is a function or a symbol throws `DataCloneError` with a message that names nothing
  about this pipeline. The runner catches it and re-throws in the shape its sibling already uses:
  `'Pipeline pins: options.pins must be structured-cloneable: '` followed by the original message.
  ⛔ Spelled with a COLON and no em dash and no exclamation point, because the voice walker holds
  `pipeline.js` at **em 2 · bang 0** and a per-file baseline never rises (§P2.19).

**The own-presence semantics `chooseOrPin` decides by are PRESERVED — measured, not argued:**
`structuredClone` keeps an own property whose value is `undefined`, `null`, `0` or `''`, and does
NOT promote an INHERITED key to an own one. So EM-B2a2's four landed pin rows and its
prototype-chain row are all still true through the runner. Cycles and intra-bag shared references
are preserved; frozen-ness is NOT (a clone is extensible), which is the intended effect: a held
fact is the runner's to carry, and a mutating pass no longer throws on a frozen record-built bag.
That behaviour shift is DECLARED here and is exactly what design §22 ruling 6 buys.

### The two step files

Both edits are DELETIONS of a wrapper, made IN PLACE:

- `src/generators/steps/assembleInstitutions.js :: assembleInstitutions` — the three
  `structuredClone(held.institutions)` / `(held.catalogForTier)` / `(held.generationRepairs)` in the
  held-roster early return become `held.institutions` / `held.catalogForTier` /
  `held.generationRepairs`. The docblock immediately above that return is re-worded to say the clone
  is the runner's, **line for line: the file still ends at 783 lines** (§5.5).
- `src/generators/steps/generatePower.js :: generatePower` — `: structuredClone(heldIntent)` and
  `: structuredClone(heldStructure)` in the two held consults become `: heldIntent` and
  `: heldStructure`. The comment above them is re-worded line for line.
  ⚠ Both files' `const UNPINNED = Symbol();` sentinel lines moved bytes (not lines) under judgment
  187's buy-back, which landed between this member's compile and its pre-proof. They are ABOVE the
  consults, they are not this member's, and they stay byte-identical.

⛔ After this member, the only `structuredClone` under `src/generators` is
`src/generators/aiLayer.js :: <the settlement copy>` (unrelated, measured present at the tip), plus
the runner's own two. The five EM-B2a3 sites — three in `assembleInstitutions.js`, two in
`generatePower.js`, all re-verified present and unmoved at `c69d16a5d` — are gone. A6 asserts it in
both directions.

### The walker — `tests/lint/heldKeyWriterCensus.walker.test.js` (CREATE)

A frozen roster plus six arms. **FOUR construction rules, each with its own instrument** (the
`tests/lint/recordRegisterTotality.walker.test.js` shape is the landed precedent for the ARMS;
⚠ it is a value-level walker that reads no source, so it spells no root and rule 0 below comes from
the ratchet itself rather than from that precedent):

0. ⛔ **THE ROOT IS SPELLED THE CURED WAY, AND IT IS A RATCHET, NOT A PREFERENCE.** This walker reads
   SOURCE, so it needs a repository root, and it spells it
   `const ROOT = join(dirname(fileURLToPath(import.meta.url)), '../..');` — never `process.cwd()` at
   module scope. `tests/lint/moduleScopeCwdRatchet.test.js` banks `POPULATION_CEILING` at
   `{ 'tests/build': 30, 'tests/lint': 17 }` EXACT IN BOTH DIRECTIONS, and its predicate is exactly
   a module-scope `process.cwd()` call, so the working-directory spelling takes `tests/lint` to 18
   and reds A4 by name with no row in this manifest to absorb it (§5.7).
1. `it`, `test` and `describe` are bound EXACTLY ONCE each, never re-bound, not even as a callback
   parameter — a stray `(it) =>` parks the whole file (§P2.1; 26 of the estate's parked files park
   for that reason alone).
2. **Every set an arm iterates is IMPORTED from its producer** — the held keys from
   `recordRegister.js`, the step roster from `getStepMeta()`, never a local literal copy, which is
   the shape `tests/lint/contractTestAntiVacuity.walker.test.js` Rule 2 convicts.
3. Every negative assertion carries `// anchored:` on the line IMMEDIATELY above the `expect`
   itself (never above a wrapped expect's first line), or is a both-directions set equality that
   cannot go vacuous.

⛔ No bare seed loop: collect, then assert once. ⛔ The walker READS SOURCE and the live registry; it
runs NO generation and reads no `dist`, so it cannot skip and it costs the gate seconds, not minutes.

### Determinism, flag, lifecycle, receipts

- **Hash/fork key:** unchanged. No fork label, no stream, no draw moves. The clone happens before
  `getStepOrder()` and outside every `setActiveRng` window.
- **No-draw behaviour:** unchanged — this member adds no consult and skips nothing new (§0.2).
- **Golden posture: UNCHANGED.** With no pins nothing this member adds executes. A1 proves it over a
  stride against the committed fixture. ⛔ `UPDATE_GOLDEN=1` is FORBIDDEN to this packet.
- **Flag:** `NONE`. Wave 1 is headless and mints no flag.
- **Lifecycle:** create / read / persist / reload / undo / import / public veil — all unchanged;
  this member writes no record key and persists nothing. **Regenerate** is the member: a pinned
  re-derive now owns its own bytes end to end.
- **Receipts and privacy:** one new refusal message, quoted above. No figure is rendered, so
  prose-numerics is not owed.

## §7 · Exact change manifest

⭐ **GENERATED FROM `EM-R1.manifest.json` AND PROVED SET-EQUAL WITH IT** (§P9(d)).

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `MODIFY` | `src/generators/pipeline.js` | `runPipeline`: the two entry clones after the `options.pins` type guard and BEFORE `const stepOrder = getStepOrder();`, the context-spread expression, and ⭐ the MERGE CLONE immediately after `Object.assign(ctx, patch)` inside the step loop | **+12 eff** (re-priced at version 3 from `+8`; measured 138 → 150 with eslint's `Linter`) | Take the two entry `structuredClone` calls, one per channel, the named refusal, and ⭐ **judgment 200's third act: after the patch merges, and only when `pins !== null`, replace `ctx[key]` with `structuredClone(ctx[key])` for every patch key that is an OWN key of the bag whose merged value IS the bag's own object BY IDENTITY.** ⛔ `chooseOrPin` stays byte-identical: the clone is the RUNNER's at both sites and never the primitive's. ⛔ Insert BELOW `export function runPipeline`'s own declaration line and far below `const _PINS_KEY = '__pins';` — EM-B2a2's landed `_note`s cite both addresses (§5.5). ⛔ `chooseOrPin` is untouched: the PRIMITIVE still clones nothing. ⛔ No em dash and no exclamation point in the new string literal — the voice baseline is `em 2 · bang 0` and never rises. ⛔ No numeral: the tuning inventory reads 0 / 0 here and must still. |
| `MODIFY` | `src/generators/steps/assembleInstitutions.js` | `assembleInstitutions`, the held-roster early return and the docblock immediately above it | **+0 eff, +0 LINES** | Unwrap the three `structuredClone(held.X)` to `held.X`. Re-word the comment LINE FOR LINE. ⛔ **THE FILE MUST STILL BE 783 LINES**: `tests/generators/generationForkCensus.test.js`'s `DISPLAY_NAME_KEYED_SITES` reads `assembleInstitutions.js:774` LIVE and reds with `<site> no longer carries inst.name`, and SIX prose-numerics rows sit at `:62` and `:69` (both above the edit). ⛔ `provides:` and `registerStep('assembleInstitutions'` byte-identical; `const UNPINNED = Symbol();` byte-identical (judgment 187's buy-back, not this member's). |
| `MODIFY` | `src/generators/steps/generatePower.js` | `generatePower`, the two held consults and their comment | **+0 eff, +0 LINES** | Unwrap `structuredClone(heldIntent)` and `structuredClone(heldStructure)`. Re-word the comment LINE FOR LINE. ⛔ `provides:` and `registerStep('generatePower'` byte-identical; `const UNPINNED = Symbol();` byte-identical. ⛔ The trace loop below the consults is untouched — its 62/63 reproduction is EM-R3's seam, recorded by EM-B2a3 and not this member's. |
| `CREATE` | `tests/lint/heldKeyWriterCensus.walker.test.js` | the frozen roster + A1–A6 | 250 eff | ⭐ **ADDS SIX `it`s UNDER ONE `describe`** — the same six §9 homes here and the same six the deferred lighting row prices. Flat literal `it(...)` under ONE literal `describe`; `it`/`test`/`describe` each bound EXACTLY ONCE; no `.each`, no loop, no conditional registration, no nested describe; every asserted set IMPORTED from its producer; every negative anchored. ⛔ **THE ROOT IS `const ROOT = join(dirname(fileURLToPath(import.meta.url)), '../..');` AND NEVER `process.cwd()`** — `tests/lint/moduleScopeCwdRatchet.test.js` banks `tests/lint` at 17 EXACT IN BOTH DIRECTIONS and the working-directory spelling reds A4 by name (§5.7). Copy the arm shape of `tests/lint/recordRegisterTotality.walker.test.js`. Carry a `CANNOT-CATCH:` header block. |
| `TEST` | `tests/generators/pipelinePinnedMode.test.js` | a NEW third top-level `describe` holding A7–A8 | n/a | ⭐ **ADDS TWO `it`s UNDER ONE NEW `describe`** (`'EM-R1 — the pin bag is cloned on entry, at the runner'`). The file is CREDITED (openers bound exactly once; 2 literal describes and 11 literal `it`s, straight-line, no `.each`) ⇒ `titles +2 · suiteTitles +1`. ⛔ **WEAKEN NOTHING**: EM-P0's and EM-B2a2's two describes and eleven `it`s stay byte-identical, including A1's by-reference assertion, which this member leaves TRUE (§5.6). |
| `REGISTER` | `scripts/mutation-coverage-manifest.json` | the `invariants` row for `tests/lint/heldKeyWriterCensus.walker.test.js` | +4 eff | Add the row SURGICALLY beside its siblings; ⛔ never re-serialise the manifest whole. `kind: 'rationale'` with an inline reason of at least 40 characters (judgment 157a) saying the file's catching power is an EXACT PARTITION over a live measurement asserted in both directions, which a mutant could only delete rather than perturb — and that the mutants are planted in `src/generators/pipeline.js` instead, per §P6. ⭐ RE-MEASURED at `c69d16a5d`: `invariants` holds **726** rows (723 at the compile; train EM-T13 added three — `tests/lint/flavorFields.census.test.js`, `tests/lint/opGuardCoverage.walker.test.js`, `tests/property/dmLayerGoldenIsolation.test.js`) and `uncoveredBaseline` is **186**; the DELTA is **+1** and `uncoveredBaseline` is **UNMOVED**. `rowKey` = `invariants['tests/lint/heldKeyWriterCensus.walker.test.js']`, ABSENT from the live manifest. ⛔ **EM-R2 and EM-R4 each carry a row on this same file** (§0.4 item 4): EM-R4's key is distinct, EM-R2's capsule names none, and §P10.8 refuses two members on ONE key. |

**Generated artifacts: `NONE`.**

> ⛔ **THE LIGHTING CENSUS IS A DEFERRED ROW, NOT AN ARTIFACT OF THIS PACKET.**
> `tests/lint/.lighting-census-baseline.json` — a named INTERIOR RED, **delta only**, all five
> figures in the register's own order because the walker stops at its first moved figure:
> **`files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +2`.**
> `+8` is DERIVED and the count law binds it (§P9(f)): the CREATE's **six** `it`s plus the TEST
> row's **two**; `+2` suites is the CREATE's one `describe` plus the TEST row's one.
> `EM-R1.count-prover.mjs` refuses the packet if §7's own words, §9's matrix, §10 and this row ever
> disagree. The predicted red MESSAGE has the shape `expected <N+1> to be <N>`. ⛔ No refreeze is
> run here; the terminal's one re-derivation is the chair's (§P2.1). ⚠ `+1 credited` is CONDITIONAL
> on the new file not parking: the build lane runs the walker's own `parkReasonsFor` over it and
> quotes the output (expected `[]`).
>
> ⛔ **THE WIRING CENSUS IS THE SECOND DEFERRED ROW.** `docs/content/wiring-census.json` —
> `stamp.producerIndexFiles` **+0** against a live **1,182** (this member CREATEs no `.js` under
> `src/domain` or `src/generators`, the two roots the stamp counts), `stamp.files` **UNMOVED** (its
> 7 entries are all `src/domain/display/stateProse/**` and name none of this member's paths),
> `totals.*` **UNMOVED** (no pool, variant or relation). The path
> appears in NEITHER §7 NOR the manifest NOR `checks`, and no lane runs `node scripts/wiring-census.mjs`.

## §8 · Ordered coding sequence

1. Run §P10.3's six pre-seal measurements. Any failure STOPS before the seal.
2. **RED FIRST, at the runner.** Write A7 and A8 into `tests/generators/pipelinePinnedMode.test.js`
   and run them against the UNCHANGED tree: A7 must be RED at 42/63 and A8 RED on the alias. Quote
   the COUNT LINE — a log with no test count did not run (§P7).
3. Edit `src/generators/pipeline.js` per §7 — the two entry clones, the named refusal AND judgment
   200's merge clone. Re-run step 2's arms: both green. ⛔ Then re-measure A8 at EVERY step
   boundary, not only the last: the entry clones alone pass a last-step-only probe on the steps
   above each producer and fail 63 of 63 at the producer, which is what version 2 shipped.
4. Unwrap `assembleInstitutions.js`, then `generatePower.js`. Prove each file's LINE COUNT unchanged
   (`wc -l` before and after is the right instrument for a LINE count) and its effective count
   unchanged with the `Linter`.
5. Write `tests/lint/heldKeyWriterCensus.walker.test.js` with A1–A6 and its frozen roster.
6. Add the mutation-coverage row surgically.
7. Run the focused battery of §10, then the mutants of §P6, then the pre-seal count prover.

## §9 · Acceptance matrix

| ID | Case | Fixture/input | Required observation | Test home |
|---|---|---|---|---|
| A1 | The held denominator is imported, never re-typed | `RECORD_CLASSES` and `SAVED_ONLY_KEYS` from `src/domain/edit/recordRegister.js` | the roster's held-key set equals `{k : RECORD_CLASSES[k] === 'HELD'} \ SAVED_ONLY_KEYS` as a BOTH-DIRECTIONS equality (**7** keys at this base); ANTI-VACUITY: the denominator is refused if empty, and the three HELD-but-saved-only keys are asserted EXCLUDED by name | `tests/lint/heldKeyWriterCensus.walker.test.js` |
| A2 | The STEP-LEVEL census is total, both directions | `getStepMeta()` from the LIVE registry | the roster's step-level rows equal `{(step, key, provides\|mutates) : key is held}` exactly — **12** writer steps of **22** at this base; a new step that provides or mutates a held key reds BY NAME and a retired one reds too; ANTI-VACUITY: the registry is non-empty and contains `assembleSettlement` | same |
| A3 | The FUNCTION-LEVEL roster is live and exact | the source of `assembleSettlement.js` and the modules it imports, comments blanked | every roster row's `(module, symbol)` resolves at that path (**11** rows), and the discovery set — every imported symbol CALLED in the assembly step's own body — equals the roster's rows plus a declared EXEMPT table whose every entry carries a written reason, as a both-directions equality; the two ASSERT rows are named and a third assert reds | same |
| A4 | The six classes are total, and MIRROR is a measurement | the roster + the source of each named symbol | the class vocabulary is exactly `producer · mutator · enricher · replay · mint · MIRROR`, every row carries one, and **each MIRROR row's body consults NO pin** (no `chooseOrPin`, no `__pins` in comment-stripped source) as a both-directions equality against the measured set — so `relinkFactionMembers` always running is an ARM, not prose (design §22.1 correction 2) | same |
| A5 | ⭐ The stream rule, written to the RULED reading (§0.2) | the source of `generateCoherence` and of `runPipeline` | EXACTLY ONE function-level writer is declared inside its own named child stream — `enrichNpcCoherence`, wrapped by `inCoherenceSubstream(coherenceRng, 'npc-enrichment', …)`, read from source — and every other held writer is declared SHARED-STREAM and CONSUME-AND-DISCARD; the step-level family is declared skippable through `rng.fork(name)` at the runner, read from source; a SECOND named child stream around a held writer reds by name. ⛔ **`generateSettlementName` is pinned as SHARED-STREAM with its 63/63 receipt**, which is the one this rule exists for | same |
| A6 | The clone is at the runner, once, and the local clones are gone | the source of the three `src/` files | `src/generators/pipeline.js` carries the entry clone on BOTH channels; ⛔ **`assembleInstitutions.js` and `generatePower.js` carry ZERO `structuredClone(`**, `// anchored:` on the line immediately above the `expect`; POSITIVE CONTROL in the same arm: the runner's own two occurrences ARE found, so the zero is not an empty read. **COUNTERFORCE** (§P6, a written-contract arm): the arm reds against the pre-member tree, which carries five | same |
| A7 | ⭐ THE LEAK AND THE CURE, by execution | the census corpus; a record-built pin bag for `generatePopulation`'s four choosers, handed THROUGH the runner | the caller's record moves **0 of 63** and nothing throws; **NEGATIVE CONTROL**: the same bag handed with the runner's clone bypassed moves **42 of 63** (`npcs` 42, `factions` 42), so the zero can never go silently vacuous; ANTI-VACUITY: every row really took the pin | `tests/generators/pipelinePinnedMode.test.js` |
| A8 | ⭐ The bag is not the context, and own-presence survives the clone | one corpus row run with pins; plus four direct `structuredClone` probes | at every step `ctx.__pins[k] !== ctx[k]` for every held key the bag carries (the two-channel clone, §0.3), asserted with its anchored negative; and `structuredClone` preserves an own `undefined`, `null`, `0` and `''` while NOT promoting an inherited key — the four rows EM-B2a2's landed A1 depends on | same |

This table is the entire edge-case budget — **8 of 8**. Omit nothing; add nothing.

## §10 · Verification commands

```sh
# Focused static checks (no gate slot needed)
npx eslint src/generators/pipeline.js src/generators/steps/assembleInstitutions.js \
  src/generators/steps/generatePower.js tests/lint/heldKeyWriterCensus.walker.test.js \
  tests/generators/pipelinePinnedMode.test.js
npm run typecheck:ratchet          # tsconfig.full.json — measure THIS member's paths (§P7)
npm run typecheck:domain:strict    # tsconfig.domain-strict.json

# The two test directories this member touches, ONE directory per gated run
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 tests/generators

# ⭐ THE CREATE DIRECTORY, RUN WHOLE, MINUS THE LIGHTING WALKER (§P7)
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 tests/lint \
  --exclude=tests/lint/sovereigntyLightingContract.walker.test.js          # MUST exit 0

# The lighting walker ALONE — the member's measured INTERIOR RED, message quoted
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/sovereigntyLightingContract.walker.test.js                    # expected NONZERO

# The goldens — UNMOVED is the claim, so they are run
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js

# PRE-SEAL, from the KIT PATH, never placed in the tree (§P9)
node <kit>/packets-waiting/EM-R1.count-prover.mjs <kit>/packets-waiting/EM-R1.md \
  <kit>/packets-waiting/EM-R1.manifest.json
```

**Sealed `checks`** (files, never the `tests/lint` directory; the lighting walker is NEVER sealed;
the validator last) — the array is in `EM-R1.manifest.json` and is computed, not recalled: every
`tests/` file that names one of this member's changed paths, MINUS the lighting walker.

## §11 · Mandatory STOP conditions

In addition to `PACKET_STANDARD.md`'s list and §P8's, this member STOPS when:

1. ⛔ **The chair REVERSES §0.2's Q1** (ratified as recommended, judgment 170a) — i.e. rules that the
   first cut CONSUMES AND DISCARDS everywhere outside `enrichNpcCoherence`. A5 would then convict
   two LANDED members of train EM-T12, and the reversal costs a version of EM-B2a2 (a second export,
   `consumeOrPin`) or a new prerequisite packet. That is the chair's, not this lane's.
2. ⛔ **The chair REVERSES §0.3's Q2 to a single clone** (ratified two-channel, judgment 170b). A8 is
   then struck, `ctx.__pins` stays aliased to `ctx`, and EM-R2's first consult at a mutator inherits
   a defect this member could have closed. The member is still buildable; the STOP is on shipping
   A8's claim without its shape.
3. Any golden or the prose manifest moves.
4. `assembleInstitutions.js` or `generatePower.js` changes its LINE COUNT (§5.5's census citation at
   `:774` and the two prose-numerics rows).
5. The worker bundle RISES, or any signed ceiling would be re-minted upward (§P11.2 — the owner's).
6. A number lands under `src/domain` or `src/generators` that is a genuine simulation dial
   (§P2.17 — tuning is the owner's). This member writes no numeral.
7. An instrument named in §P2.21 reds with a figure this packet did not predict; the arm's own
   message decides and a figure adopted by hand is the failure.
8. A spec, a walker or a ratchet would be weakened, widened or skipped to make this member pass —
   including the census's own IMPORTED denominator: a key outside `RECORD_CLASSES` (`powerIntent`
   is the live instance) is EM-R1b's subject and is never reached by re-typing the held set here.
9. ⛔ **THE MERGE CLONE REACHES A KEY THE BAG DID NOT HAND OVER.** The act is gated on IDENTITY
   (`ctx[key] === pins[key]` with `hasOwnProperty`), never on value: a step that produced an
   equal-looking value of its own must be left alone, and any run in which a non-pinned key is
   re-cloned is a STOP. The counted budget is ONE clone per held key per run, ZERO with an empty
   bag and ZERO unpinned; a count that exceeds it is the same STOP.
10. ⛔ **`tests/lint/moduleScopeCwdRatchet.test.js` reds** — the CREATE spelled its root from the
   working directory and took `tests/lint` from 17 to 18 (§5.7). The cure is the root's spelling,
   never a raised ceiling: "raising a number here is not a repair" is that file's own law.

## §12 · Completion receipt

- Base SHA; dispatch bundle and seal identity:
- The chair's rulings on §0's Q1 and Q2, quoted, and what changed here because of each:
- Exact changed files and effective-line counts (eslint `Linter`, `skipBlankLines` + `skipComments`), before and after, for all three `src/` files:
- ⛔ `wc -l` on both step files, before and after (must be IDENTICAL), and `tests/generators/generationForkCensus.test.js`'s `DISPLAY_NAME_KEYED_SITES` arm re-run green:
- Acceptance cases A1–A8, executed, with A7's negative control (42/63) and A6's counterforce quoted:
- ⭐ JUDGMENT 200's THREE MEASUREMENTS, each per channel over the whole 63-row census corpus: (i) A8 at EVERY step boundary, rows aliased at ANY step (expected 0 / 63 on all three); (ii) the `__pins` bag's own mutation during the run (expected 0 / 63 on all three); (iii) the merge clone's counted budget — `structuredClone` calls inside one run with NO pins, with an EMPTY bag and with a full bag (expected: the runner contributes 0, 2 and 2 + one per held key):
- ⭐ A7's RE-CUT NEGATIVE CONTROL: the bypass re-aliases at EVERY step boundary and is quoted with its figure (expected 42 / 63, `npcs` 42 · `factions` 42); a control that comes back 0 is a FALSE proof and is reported as one:
- ⭐ EVERY LANDED PINNED-MODE ARM re-run, and the grep that found every identity comparison against a pin object in the estate (expected exactly two: EM-B2a2's A1 at the PRIMITIVE, untouched, and `pipelinePinnedChoosers.test.js`'s `handedBackByReference`, asserted at 0):
- ⭐ The §P6 mutants: each planted in `src/generators/pipeline.js`, each reddening its OWN acceptance title, each restored to the exact pre-mutant SHA-256:
- The red-first log's COUNT LINE for A7 and A8 against the unchanged tree (a log with no count DID NOT RUN):
- The golden stride: rows checked, rows moved (must be zero), and the committed fixture's identity:
- `parkReasonsFor('tests/lint/heldKeyWriterCensus.walker.test.js')` output, quoted (expected `[]`):
- Lighting census DELTA, all five figures, attributed by execution (⛔ never an absolute; ⛔ no refreeze here):
- Mutation-coverage manifest: `invariants` before and after (expected the base's count +1; 726 at `c69d16a5d`) and `uncoveredBaseline` (expected UNMOVED at 186):
- Wiring census: `stamp.producerIndexFiles` before and after (expected UNMOVED, +0) and `stamp.files` (expected UNMOVED):
- Entropy census both arms (expected 36 / 47, +0), tuning inventory per file (expected 0 / 0 / 34 unmoved), voice **Tier 5** for `pipeline.js` (expected em 2 · bang 0):
- ⛔ `tests/lint/moduleScopeCwdRatchet.test.js`: `tests/lint` population before and after (expected UNMOVED at the base's banked number), and the CREATE's own ROOT line quoted:
- The worker byte delta as measured at the terminal's one real build, with per-module attribution showing only this member's modules moved:
- `tests/lint --exclude=…sovereigntyLightingContract…` — exit (must be 0):
- The lighting walker alone — the named interior red, message quoted:
- Both typecheck configurations (`typecheck:ratchet`, `typecheck:domain:strict`):
- §7-versus-capsule set-equality proof, re-run at the landing:
- Generated artifacts: `NONE`
- Deviations: `NONE | STOP`
- Out-of-scope observations, without investigation:
- ⭐ §5.4's census RE-TAKEN at the landing sha, with that sha as its new as-of mark:
- Judgment calls: `NONE` — §0's two questions are the chair's and this packet records how each was ruled.
