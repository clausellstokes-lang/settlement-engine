# Settlement editor / EM-R3 — THE POWER STRUCTURE IS FINAL: the replay does not run under a held structure, the freshness assert is pin-aware at its three call sites, and the receipt stays the record's until the merge recomputes it

**Preamble:** docs/implementation/preambles/EM-PREAMBLE.md (SHA-256: **TO BE STAMPED BY THE CHAIR, MEASURED AT THE PLACEMENT TIP.** ⛔ **THIS PACKET NAMES NO HASH TO STAMP** — §P3.5: the preamble's SHA-256 is measured at the placement tip with `shasum -a 256`, never carried in a document. ⭐ As executed HISTORY with its as-of mark, and for no other use: `125c693214235a81bf4f2b38506b621859a35e0325ff2ea58d335b4e0681a99d` at `6a0178b7b`, this PRE-PROOF's read tip — that is the LANDED **SIXTH** amendment, measured with `shasum -a 256` by the pre-proof seat. ⛔ The compile's own as-of mark — the FIFTH amendment's digest at `91cc9ef5a` — is NOT spelled here: §P3.6 and this packet's own count prover allow exactly ONE 64-hex digest in a body, and it must be the live one. The compile's digest is recorded in `EM-R3.preproof.report.md` §0.)
- **The law this packet is written to:** ⭐ **THE SIXTH AMENDMENT, LANDED** (judgment 144; it landed as train EM-T13's first commit `7bc14e003`, with cite-by-symbol). ⭐ **MEASURED at this pre-proof's read tip `6a0178b7b`**: `docs/implementation/preambles/EM-PREAMBLE.md` hashes `125c693214235a81bf4f2b38506b621859a35e0325ff2ea58d335b4e0681a99d`, so the sixth IS the tree's live preamble and every figure below is measured against a tree that already carries it. The compile measured the FIFTH at `91cc9ef5a` and every one of its figures was RE-EXECUTED here. Every citation in this body is `path :: symbol` (§P2.22); no `path:NNN` address is spelled anywhere, because a READY packet body is a live document in `tests/lint/sourceCitationIntegrity.walker.test.js`'s only-shrinks arm.

- **Status:** `LANDED`
  — Compiled 2026-09-22 by an Opus COMPILE seat (session cce01f87) at read tip `91cc9ef5a`, train
  EM-T12's landed tip, under `briefs/launch/COMPILE-EM-R3-cce01f87.md`.
  ⭐ **PRE-PROOVED IN FULL 2026-09-23 by an Opus PRE-PROOF seat (session cce01f87) at read tip
  `6a0178b7b`**, train EM-T16's STAGE-1 PLACEMENT tip, under
  `briefs/launch/PREPROOF-EM-T16-stage2-cce01f87.md` — **READY-able at version 2**, placed on
  stage 1's flip. The J-T1 window spans FOUR trains (EM-T13, EM-T14, EM-T15 and stage 1's
  placement) and is quoted in §0.6.
  ⛔ **READ §0 BEFORE PROMOTING.** This member carries FOUR measurements the lane does not
  adjudicate; the first is a REACHABILITY GAP that no chartered member owns (§0.1).
  ⚠ The status value stands ALONE on its line because `parsePacketHeader`
  (`scripts/implementation-packets.mjs :: parsePacketHeader`) anchors the status row at
  end-of-line and takes `status` only when exactly one row matches (§P9(c)).
- **Landed at:** `98e7daadf17118a2088b791dfe00c870786bd06e` — the power roster held through reconciliation (version 2): reconcilePowerStructure consults the pin at one module-private site and returns the held roster without replaying (57/57 the DM's seat, shares and legitimacy); the freshness assertion's optional options bag; the trace consequence discharged through the partition (182/211); built at 98e7daadf on train EM-T16 stage 2a
- **Packet version:** 2
  > **What version 2 changed and why.** Version 1 was compiled at `91cc9ef5a`, FOUR trains ago.
  > Version 2 changes no mechanism, no contract clause, no acceptance case and no scope figure.
  > It (a) re-executes every measured figure against the landed tree and re-cuts the four
  > absolutes that MOVED — `economyReconciliation.js`'s effective lines 280 → **259** (EM-R0f took
  > three declarations out of it), `WORKER_BUNDLE_CEILING_BYTES` 1,396,015 → **1,392,364** (the
  > chair's buy-back, judgment 203, LOWERED it), `stamp.producerIndexFiles` 1,181 → **1,185**, and
  > the worker's static closure 220 → **222** files; (b) DISCHARGES §11's STOP condition 3, because
  > EM-R0f has landed; (c) names the ONE landed arm that arrived after the compile and now governs
  > all three of this member's `src/` files — `tests/lint/heldKeyWriterCensus.walker.test.js`
  > (EM-R1's CREATE) — adding it to §5.6, to the sealed `checks` and to §12, with every one of its
  > four relevant arms run over this member's planted text and measured UNMOVED (judgment 148's
  > law); (d) re-takes §0.4's trace figure THROUGH EM-R5's landed `partitionTrace`, which absorbs
  > it whole (**0 of 63** through the partition against **30 of 63** raw), so §12 hands a
  > measurement to the record rather than a figure to a member that has already landed; (e) turns
  > the capsule's `acceptanceCases` from strings into the `{ id, case }` objects the tree's own
  > validator requires — measured: **the validator refuses the string form by name**, and zero of
  > the 226 placed capsules use it; and (f) spells the `vi.hoisted` recorder A5's mint census needs,
  > with the walker's own park classifier executed over the planted shape.
- **Verified base:** `em-t16-r3-2026-09-23` at `17fcf092bb878eb5b168f63f264c2a4dff316f19`
  ⛔ **THE LANE BRANCH RIDES THIS ROW, NOT A ROW OF ITS OWN** — `parsePacketHeader` takes
  `verifiedBranch` from the `Verified base` row itself and reads no `Verified branch:` row. The
  chair's placement writes this row as ``- **Verified base:** `em-t16-em-r3-<date>` at `<the train
  base's 40-hex sha>` `` — the value ALONE on the line, every caveat on these continuation lines
  (§P9(c), which binds this row as well as the Status row).
- **Last revalidated:** `17fcf092bb878eb5b168f63f264c2a4dff316f19`
  > The chair's revalidation sentence: *re-measured at `__BASE__` — the J-T1 window
  > `6a0178b7b..__BASE__` over every change-manifest and `requiredSymbols` path (the FULL
  > pre-proof's own window `91cc9ef5a..6a0178b7b` is executed and quoted in §0.6). ⭐ **EM-R0f HAS
  > LANDED** (`d7d6ceef7`): `src/generators/power/economyReconciliation.js` no longer declares
  > `fingerprintPowerEconomyInput`, `economyProjectionInput` or `ECONOMY_FINGERPRINT_VERSION` — it
  > imports the first from `src/data/economyFingerprint.js` — and its effective-line figure is
  > **259**, re-executed here; §11's STOP condition 3 is DISCHARGED. ⭐ **EM-R1 HAS LANDED**
  > (`ae01c1fac`), so every §0 figure was re-taken against the cloning runner and every one is
  > unmoved. THE ONE THING THIS ROW STILL OWES: EM-R2 version 5 must have landed
  > `const pins = ctx.__pins || null;` in `src/generators/steps/assembleSettlement.js ::
  > registerStep` — `grep -c -F` reads **0** at `6a0178b7b` and must read **1** at `__BASE__`
  > (`EM-R3.remeasure.sh`, which the chair runs at the flip, carries that grep and the verbatim
  > re-run of all six `requiredSymbols` rows). The register figures (entropy 36 / 47, tuning 2 / 0
  > per file, `stamp.producerIndexFiles` 1,185 with +0 of this member's, the eager set 270 with
  > none a member, the voice generators baseline carrying no row for any of the three files and
  > sitting at em 28 of 28 with bang 0 of 0) re-read as stated.*
- **Lane branch:** `em-t16-em-r3-<date>` (§P10.2 — the lane branch IS the verified branch and is written INTO the `Verified base` row above; the pattern is the chair's, the date is the placement's).
- **Depends on:** **EM-P0** (the runner seam; LANDED) · **EM-B2a2** (`chooseOrPin`'s exported home `src/generators/pipeline.js :: chooseOrPin`; LANDED) · **EM-B2a3** (the power step's two consults and the mint-census arm that names this member's seam by id; LANDED, build `608b0d63d`) · **EM-P2** (`GENERATION_TIER1`, `src/domain/generation/generationForkRegistry.js :: GENERATION_TIER1`; LANDED) · **EM-R0a** (`src/domain/edit/recordRegister.js :: RECORD_CLASSES`, which classes this member's receipt; LANDED) · ⭐ **EM-R1** (**LANDED** on train EM-T15 at version 3, `ae01c1fac` — the runner clones the pin bag on entry and re-clones it at the patch merge; a hard dependency of every held path in this family, now a fact of the tree rather than a packet) · **EM-R2** (**READY at version 5** on this train's stage 1 — it lands `const pins = ctx.__pins || null;` at the TOP of `assembleSettlement.js :: registerStep`'s callback, before the ctx destructure, which this member REUSES rather than re-spelling; measured in EM-R2's own §6 C2) · ⭐ **EM-R0f** (**LANDED** on train EM-T14 at version 3, `d7d6ceef7` — it took three declarations out of this member's largest file) · ⛔ **AND `EM-R1b`, WHICH THE CHAIR CHARTERED FROM §0.1's Q1 (judgment 182a) AND HAS PLACED BESIDE THIS MEMBER IN STAGE 2 (judgment 206c)** — NOT a placement dependency (§0.1: this member is provable at its own lane base, where EM-R1b has not landed), but the member that makes this member's outcome REACHABLE for a DM, at this same flip.
  ⭐ EM-R2 and EM-R1b are read AS PACKETS and every fact taken from them is marked PLAUSIBLE. EM-R1 and EM-R0f are no longer packets: every fact taken from them was RE-EXECUTED against the landed code at `6a0178b7b` (judgment 134's law).
- **Collision group:** `src/generators/power/economyReconciliation.js` (**CLEAR** — EM-R0f has LANDED, so the order edge is discharged) · `src/generators/steps/assembleSettlement.js` (with **EM-R2**, READY on stage 1, and with **EM-R4**, which follows on stage 3: that path is NOT row-keyed, so ⛔ **no two of EM-R2, EM-R3 and EM-R4 may be non-terminal together**, §P10.8 — the STAGED order is the cure, judgment 206/207c) · `tests/generators/powerEconomyFreshness.test.js` (**CLEAR** — EM-R0f's TEST row has landed) · `src/generators/steps/powerEconomyReconcilePass.js` (**∅ — no waiting or landed packet names it**, re-measured over all 226 placed capsules and the three waiting ones).
  ⭐ **STAGE 2 IS PAIRWISE DISJOINT, EXECUTED** through the tree's own `validatePacketManifest` at `6a0178b7b` with all three stage-2 capsules added: the ONLY duplicates it reports are the two the chair measured and expects to clear at stage 1's flip (`assembleSettlement.js (EM-R2, EM-R3)` and `pipelinePinnedMode.test.js (EM-R2, EM-R1b)`). This member shares no path and no register row with **EM-R1b** (`pipeline.js` · `dmLayer.js` · `transientChooserRegistry.walker.test.js` · `pipelinePinnedMode.test.js` · `dmLayerGoldenIsolation.test.js` · a row-keyed mutation-coverage row) or with **EM-R0c** (`recordMergeTree.js` · `mergeConsequence.js` · `recordMerge.test.js`), and it names NO register row at all. ⚠ `src/generators/pipeline.js` is a `requiredSymbols` path of this member and EM-R1b MODIFIES it: measured, EM-R1b holds `export function chooseOrPin(pins, key, draw)` BYTE-IDENTICAL and carries the same row itself, so the row survives the stage's composition.
- **Commit authority:** edits only; the chair commits (§P10.5).
- **Baseline posture:** **measured.** Every figure in §0, §3, §5 and §7 was executed at `91cc9ef5a` in plain `node`/`git` and is quoted with its command in `EM-R3.evidence.md`.

---

## §0 · ⛔⛔ FOUR MEASUREMENTS THAT SHAPE THIS MEMBER — none adjudicated by the lane

> ⭐ **EVERY FIGURE IN §0 WAS RE-EXECUTED AT `6a0178b7b` BY THE PRE-PROOF SEAT** (2026-09-23),
> against EM-R1's LANDED cloning runner and EM-R0f's LANDED fingerprint move, and every one is
> UNMOVED. §0.6 quotes the J-T1 window; §0.4 carries the ONE figure the pre-proof RE-TOOK through
> a landed sibling and re-decided.

### §0.1 · ⛔⛔ THE BRIEF'S PREMISE IS REACHABLE ONLY THROUGH A GAP NO CHARTERED MEMBER OWNS — question **Q1**

The launch brief says *"measure what runs today when `powerStructure` is held"*. **Executed at the
read tip, NOTHING runs: the runner refuses the bag in 63 of 63 corpus rows.**

```
$ EMR3_TREE=<6a0178b7b> node probe-power-1.mjs            # RE-EXECUTED by the pre-proof seat
generatePower.provides = ["powerIntent","powerStructure"]
generatePower.mutates  = []
ARM A (powerStructure alone) rows=63 threw=63 partialPinRefusals=63
  A.refusal: Pipeline pins: step "generatePower" has choosers [powerIntent, powerStructure]
             but pins supply only [powerStructure]. Pin every chooser of a step or none of the…
```
⭐ **63 of 63, byte-identical to the compile's, through EM-R1's LANDED cloning runner.**

`powerIntent` is transient pipeline state that `src/generators/power/economyReconciliation.js`'s own
header calls *deliberately not persisted*, and design §22 ruling 5 forbids persisting it. A
record-built bag therefore supplies one of `generatePower`'s two choosers, and
`src/generators/pipeline.js :: runPipeline`'s partial-pin rule — whose own comment calls the
choosers *"the record paths it provides"* — refuses it.

**The estate already knows which chooser is a record path and which is not.** EM-P2's LANDED
register declares, per pair:

| row in `src/domain/generation/generationForkRegistry.js :: GENERATION_TIER1` | `onRecordClass` | `recordPath` |
|---|---|---|
| `generatePower` · `powerIntent` | `absent` | **`null`** |
| `generatePower` · `powerStructure` | `transformed` | `record.powerStructure` |

and `tests/generators/pipelinePinnedChoosers.test.js :: recordPathOf` already reads that field.
⇒ the rule refuses on a field the tree has measured and frozen as `null`.

**The consequence is live in a sibling's ruled table.** EM-B2a4 version 4 (judgment 176) rules that
*"a key is pinned only when EVERY step providing it is wholly pinnable"* and returns a power edit as
`unapplied: [{ key, value, reason: 'step_not_pinnable' }]`. ⇒ **`powerStructure` is never pinned on
the product path, so the power card is dark, and EM-R3's subject cannot occur for a DM.**

⭐ **Q1 IS RULED AND THE CURE IS ALREADY BUILDING** (judgment 182a, then 206c). The chair
chartered **EM-R1b** the same turn, and has PLACED IT BESIDE THIS MEMBER IN STAGE 2 of this very
train: it teaches `runPipeline`'s partial-pin rule to key on `GENERATION_TIER1`'s `recordPath`, so
a chooser with no record path is not counted against a record-built bag. ⇒ **the gap and its cure
close at the SAME flip, and this member's product gap is one train shorter than §12 was written
for.** ⛔ **This does not make EM-R1b a placement dependency and does not change one line of this
member.** The two build on the SAME base in parallel lanes, so at EM-R3's own lane base the refusal
is still 63 of 63 and §9's battery still reaches a held structure through `onStrictViolation`,
which COLLECTS it. Their paths are disjoint (§0.5).

⇒ **Q1's ORIGINAL TEXT, kept as the measurement that produced it.** ⛔ **THE LANE DOES NOT CURE THIS AND DOES NOT ADJUDICATE IT.** Three facts fence the cure
out of this packet: (a) the rule lives in `src/generators/pipeline.js :: runPipeline`, which is
EM-R1's file; (b) `tests/generators/pipelinePinnedChoosers.test.js`'s landed closure arm PINS
`choosersOf('generatePower')` to `['powerIntent','powerStructure']`, so moving `powerIntent` out of
`provides` reds a landed arm; and (c) `tests/generators/dataFlowContract.test.js ::
soundnessViolations` and `:: phantomReads` count a producer as `provides ∪ mutates` and NOT
`scratch`, so the obvious re-declaration reds two more landed arms. **Recommendation: slot a new
member the same turn (the intake rule) — the runner's partial-pin rule keys on `GENERATION_TIER1`'s
`recordPath`, which the estate already declares — and add it to this member's `Depends on`.**
⚠ This does NOT block EM-R3: §9's battery reaches a held power structure through
`onStrictViolation`, which COLLECTS the partial-pin refusal instead of throwing (executed; the pin
is honoured, which is how §0.2's figures were taken), so the member is provable at its own tip.

### §0.2 · ⭐⭐ THE DEFECT IS LIVE AND IT IS BIGGER THAN THE DESIGN'S WORDING · CONFIRMED

Design §22 finding 4 says a faction's power share is *silently discarded*. Executed at the read tip
over the 57 non-desert corpus rows, under §14's flagship edit (terrain → desert) with the DM halving
one faction's share:

```
$ EMR3_TREE=<6a0178b7b> node probe-power-3.mjs            # RE-EXECUTED by the pre-proof seat
MODE1 (powerStructure alone, terrain->desert) rows=57 threw=1 pinRefusalsCollected=57
  staleFingerprintAtPEC=41 powerStructureOverwritten=56 factionsOverwritten=56
  fingerprintOverwritten=41 shareKept=0 shareLost=56
MODE3 (seat edit, config unchanged) rows=57 threw=57 seatKept=0
  m3.msg: Power intent replay changed the generated faction identity roster. Only
          economy-dependent projections may change during finalization.
```
⭐ **EVERY FIGURE IDENTICAL TO THE COMPILE'S**, re-taken against EM-R1's landed clone — which is
the point of the re-take: the clone protects the CALLER's record, and this defect is inside the
run, so the clone does not touch it. ⚠ The probe itself had to be re-pointed first: it imported
`fingerprintPowerEconomyInput` from `economyReconciliation.js`, which EM-R0f emptied, and a stale
probe reported `0` overwrites with the message `fingerprintPowerEconomyInput is not a function` —
a FALSE GREEN caught and recorded rather than believed.

**Not the share: the WHOLE STRUCTURE.** `reconcilePowerStructure` rebuilds `factions` from the
replay and overwrites `governingName`, `government`, `publicLegitimacy`, `factionRelationships`,
`criminalCaptureState`, `powerProjectionVersion` and `economyInputFingerprint` — **56 of 57 rows**,
and the DM's share survives in **0**. The seat change **throws in 57 of 57**. Both are §22's
findings, re-measured live, and §9's A1 and A2 carry them as their negative controls.

### §0.3 · ⭐⭐ THE TWO HALVES ARE ONE UNIT, AND THE MEASUREMENT SAYS SO — the charter's row is CONFIRMED

Suppressing the replay WITHOUT making the assert pin-aware does not work: the held structure's
fingerprint is the record's, the re-derived economy is not the record's, and the assert refuses.
**Measured: 41 of 57 rows carry a stale fingerprint at `powerEconomyReconcilePass`** (42 of 57 when
the intent is held too). ⇒ the charter's row joins the replay and the assert in ONE member because
the first half breaks the pipeline without the second. Nothing in the brief is refuted.

And the three call sites are confirmed **by execution, not by grep**: a counting pass-through over
21 corpus rows recorded `assertPowerEconomyFreshness calls=63` — exactly three per run.

**THE CURE, MODELLED THROUGH AN IN-MEMORY OVERRIDE CHANNEL, WITH ITS CONTROLS FIRST:**

```
$ EMR3_TREE=<6a0178b7b> node --import ./r3-hook.mjs probe-power-4.mjs   # RE-EXECUTED
C0 inert-loader determinism: rows=21 identical=true
C1 channel reached: assertPowerEconomyFreshness calls=63 recordUnmoved=true
CURED rows=57
  (a) NO EDIT, held structure identical=57/57 threw=0
  (b) SHARE EDIT + terrain->desert: kept=57 lost=0 threw=0 structureMoved=0
  (c) SEAT EDIT: rows=57 kept=57 threw=0
C2 no-pins golden control: rows=21 unmoved=true
```
⭐ **Every line identical to the compile's**, with the controls run FIRST at the new tip. The
three-site count `assertPowerEconomyFreshness calls=63` over 21 rows is re-proved exactly 3 per
run, which is §9's A3 arm.

### §0.4 · THE TRACE MOVES, THE TRACE REFRESHER DOES NOT — questions **Q3** and **Q4**

Suppressing the replay moves exactly TWO context keys against today's held run: `simulationTrace`
**30/63** and `settlement` **30/63** (the latter is the settlement object that carries
`powerStructure`). The trace moves because `refreshPowerGenerationTraces` re-keys the
`generatePower` entries the replay moved; with no replay, those entries keep the values the held
structure justifies — which is what §22 ruling 8 and §22.1 correction 4 want.

⭐⭐ **THE RE-TAKE (judgment 182c, the pre-proof's own measurement). EM-R5 HAS LANDED** on train
EM-T15 (`src/domain/edit/tracePartition.js :: partitionTrace`, applied inside
`src/domain/edit/dmLayer.js :: rederive`), so this figure is no longer a hand-over to a waiting
sibling: it is re-taken THROUGH the landed partition, which is the only path a DM's pin bag can
reach this seam by.

```
$ EMR3_TREE=<6a0178b7b> node --import ./r3-hook.mjs probe-power-6.mjs
carriedTraceSteps(["powerStructure"]) = ["generatePower","neighbourFactions"]
rows=63
(a)  RAW simulationTrace moved by the cure:                 30/63
(a2) ... and every moved entry is a generatePower entry:    30/30   (none outside that step)
(b)  THROUGH partitionTrace(record, derived, ['powerStructure']) the trace moved:  0/63
(c)  the carried generatePower run equals the RECORD's own:                       63/63
```

`RECORD_CLASSES.powerStructure` is **`HELD`** (executed), so `carriedTraceSteps` names
`generatePower` and `neighbourFactions`, and `partitionTrace` substitutes the RECORD's own run for
the re-derivation's at exactly those steps. Every entry this member moves is a `generatePower`
entry (`refreshPowerGenerationTraces` filters on `trace.step === 'generatePower'` and touches
nothing else), and no `TRACE_SUBJECTS` pair names `powerEconomyReconcilePass` or
`assembleSettlement` (executed: 0 and 0). ⇒ **the movement is ABSORBED WHOLE on the product path.**

⇒ **Q3, RE-ANSWERED BY MEASUREMENT: this member DECLARES the movement as its own measured
consequence and asserts no trace figure. On the `rederive` path — the ONLY path that carries a pin
bag — the landed partition makes it invisible (0 of 63). The 30 of 63 is a RAW-`runPipeline`
measurement with `6a0178b7b` as its as-of mark, and §12 records it as such rather than handing a
figure to a member that has already landed.**

And `refreshPowerGenerationTraces` itself is a **measured NO-OP** under a held structure — with and
without it the `simulationTrace` is identical **63/63** and the WHOLE record is identical **63/63**
— because `reconcilePowerStructure`'s held return hands it a `beforeFactions` equal to the roster it
compares against. ⭐ **RE-EXECUTED at `6a0178b7b` (judgment 182d):**

```
$ EMR3_TREE=<6a0178b7b> node --import ./r3-hook.mjs probe-power-5.mjs
(i) refreshPowerGenerationTraces under a HELD structure: simulationTrace identical
    with/without it = 63/63; WHOLE record identical = 63/63
(ii) record keys that MOVE when the replay is suppressed: simulationTrace 30/63, settlement 30/63
```

⇒ **Q4: it is left UNGATED**, one branch fewer, and §9's A6 carries the 63/63 as
the arm that makes the choice observable rather than asserted.

### §0.5 · THE PATH INTERSECTION, MEASURED OVER ALL 38 WAITING CAPSULES

⭐ **RE-MEASURED at `6a0178b7b` over all 226 PLACED capsules and the three waiting ones, by the
tree's own `docs/implementation/PACKET_MANIFEST.json` and `validatePacketManifest`.**

| sibling | stage | rows over this member's four paths |
|---|---|---|
| **EM-R0f** | LANDED, EM-T14 | `MODIFY economyReconciliation.js` · `TEST powerEconomyFreshness.test.js` — **DISCHARGED**: it landed first, exactly as Q2 ordered |
| **EM-R1** | LANDED, EM-T15 | **∅** on the change paths; it MODIFIES `src/generators/pipeline.js`, a `requiredSymbols` path of this member — re-run verbatim, `count=1`, unmoved |
| **EM-R2 v5** | **READY, stage 1 of THIS train** | `MODIFY src/generators/steps/assembleSettlement.js` — ⛔ **ONE, and it is non-terminal**: the tree's validator says `duplicate change path across packets: src/generators/steps/assembleSettlement.js (EM-R2, EM-R3)`, which is the MEASURED, EXPECTED collision that CLEARS at stage 1's flip (judgments 206a / 207c). ⭐ Measured line-wise: EM-R2's own §6 C2 puts `const pins = ctx.__pins || null;` at the TOP of the callback BEFORE the ctx destructure, and names this member's three call sites among the things it leaves byte-identical — so the binding this member reuses is ABOVE every use of it and there is no dead zone |
| **EM-R0b v8** | READY, stage 1 | **∅** — four CREATEs under `src/domain/edit/` and `tests/domain/` |
| **EM-R1b v1** | DRAFT, **stage 2 beside this member** | **∅** on change paths. `pipeline.js` is a `requiredSymbols` path of this member and EM-R1b MODIFIES it — measured: EM-R1b holds `export function chooseOrPin(pins, key, draw)` BYTE-IDENTICAL and carries that exact row itself |
| **EM-R0c v5** | DRAFT, stage 2 beside this member | **∅** — three CREATEs |
| **EM-R4 v1** | DRAFT, **stage 3, AFTER this member** | `MODIFY src/generators/steps/assembleSettlement.js` — ⛔ **ONE**. It also CREATEs `tests/generators/nameHeldThroughAssembly.test.js` and REGISTERs a row-keyed `scripts/mutation-coverage-manifest.json` entry, neither of which this member names. ⭐ **It inherits EM-R2's `DISCOVERY_EXEMPT` row and never re-adds it** (judgment 207d), and it must re-measure against THIS member's LANDED fence, never against its predicted one (judgment 134) |
| every other placed or waiting capsule | — | **∅** |

⇒ **Q2, RE-ANSWERED: this member rides EM-T16 at STAGE 2**, after stage 1 flips EM-R2 and
EM-R0b, with EM-R0f and EM-R1 already landed on EM-T14/EM-T15 and EM-R4 following at stage 3.
`src/generators/steps/powerEconomyReconcilePass.js` is named by no packet at all, landed or waiting
— re-measured.

### §0.6 · ⭐ THE J-T1 WINDOW `91cc9ef5a..6a0178b7b` — FOUR TRAINS, EXECUTED

```
$ git diff --stat 91cc9ef5a 6a0178b7b -- <the four change-manifest paths> src/generators/pipeline.js
 src/generators/pipeline.js                     | 32 ++++++++++++++++++++++++--
 src/generators/power/economyReconciliation.js  | 31 +------------------------
 tests/generators/powerEconomyFreshness.test.js |  2 +-
 3 files changed, 32 insertions(+), 33 deletions(-)

$ git log --oneline 91cc9ef5a..6a0178b7b -- <the same paths>
ae01c1fac EM-R1: the pin bag is cloned on entry AND re-cloned at the patch merge, at the runner …
d7d6ceef7 EM-R0f: the economy fingerprint moves down a layer to src/data, proven invisible
```

⭐ **`src/generators/steps/powerEconomyReconcilePass.js` and
`src/generators/steps/assembleSettlement.js` are BYTE-IDENTICAL across all four trains** — they do
not appear in the window's stat at all. What moved, and what it costs this member:

| what landed | what it did | this member's answer |
|---|---|---|
| **EM-R0f** `d7d6ceef7` | took `economyProjectionInput`, `fingerprintPowerEconomyInput` and `ECONOMY_FINGERPRINT_VERSION` out of `economyReconciliation.js`; the file now carries `import { fingerprintPowerEconomyInput } from '../../data/economyFingerprint.js';` | §11's STOP condition 3 **DISCHARGED**; §3's before-figure re-executed **280 → 259**; all four `requiredSymbols` rows over that file re-found verbatim at `count=1`; §6's C1 unchanged (the member edits none of the moved declarations) |
| **EM-R1** `ae01c1fac` | `runPipeline`'s `pins` became a `let`, cloned on entry and re-cloned at the patch merge | every §0 figure RE-EXECUTED against it and **UNMOVED**; `export function chooseOrPin(pins, key, draw)` byte-identical, `count=1`; ⭐ it also CREATED `tests/lint/heldKeyWriterCensus.walker.test.js`, a landed arm over all three of this member's `src/` files — §5.6's new row and a sealed `check` |
| stage-1 placement `6a0178b7b` | placed EM-R2 v5 and EM-R0b v8 READY | EM-R2's binding is still ABSENT (`grep -c -F` = **0**) and stays a `_pendingRequiredSymbols` row; `EM-R3.remeasure.sh` re-proves it at the flip |

⛔ **THE ONE FACT THAT STILL ARRIVES UNDER THIS MEMBER:**
`src/generators/steps/assembleSettlement.js :: const pins = ctx.__pins || null;` — `0` at
`6a0178b7b`, planted by EM-R2 version 5 at stage 1's flip, re-measured VERBATIM at its discharge
(§P10.11, judgment 151), never promoted as written.

---

## §1 · Reconciled authority

| Check | Verdict at `6a0178b7b` (RE-EXECUTED by the pre-proof seat) |
|---|---|
| CREATE targets absent | **N/A — this member CREATEs nothing** |
| MODIFY / TEST targets present | **PASSES** — all four exist |
| every `requiredSymbols` row resolving | **PASSES** — all **six** found VERBATIM at `count=1` (§5.1); the seventh row is `_pendingRequiredSymbols` and reads **0**, by design (§0.6) |
| the three LANDED `requiredSymbols` rows over these paths | **PASSES, ALL THREE** — re-found at `count=1` after EM-R0f's move; not one is a line this member edits, so `retiredSymbols` is **NONE** and the post-edit simulation (§P10.11) is clean (§5.2) |
| the packet capsule against the tree's OWN validator | **PASSES**, executed: `validatePacketManifest` at `6a0178b7b` with this capsule added reports exactly two errors, `packetPath does not exist` (the chair's placement writes the body) and the ONE expected duplicate below. ⚠ Version 1's capsule did NOT pass: its `acceptanceCases` were STRINGS and the validator refuses them by name (`EM-R3.acceptanceCases[0] must be an object`, ×6). **Version 2 carries `{ id, case, home, authorizedBy }` objects**, the shape all 226 placed capsules use |
| git-clean / no sibling reserves a path | **PASSES for three of four** — the registered estate is 226 entries with **ZERO non-terminal LANDED**; ⛔ the READY estate reserves `assembleSettlement.js` through EM-R2, which is stage 1 of this train and clears at its flip (§0.5) |
| the design, the charter and the tree agreeing | ⛔ **ONE GAP, RULED AND BEING CURED IN THIS SAME STAGE** — §0.1's Q1, chartered as EM-R1b (judgment 182a) and placed beside this member (judgment 206c). Nothing in §22, §22.1, §22.2 or the charter's row is REFUTED |

**Authority read, by SECTION and ITEM (never by line):** design `docs/DESIGN_EDIT_MODE_AND_DECREES.md`
§22 rulings 1, 2, 4, 5, 6, 8, 9; §22.1 corrections 1, 3 and 4 and the whole-seam finding; §22.2
items 7, 8 and 11; §22.3 item 6; §14's source rule and its "incoherence is consequence". Charter
`docs/implementation/charters/EDIT-MODE-TRAIN.md`: the "EM-B2a is RE-CUT" paragraph, the amendments
of 2026-09-19 16:5x and 17:25 (the EM-R family table) and this member's row. `EM-PREAMBLE` §P1–§P11
as amended by the sixth.

## §2 · Outcome

**What this member makes true.** (1) **The power replay does not run under a held power structure.**
`reconcilePowerStructure` consults the pin at ONE site and, when `powerStructure` is held, returns
the held roster untouched — so `assertStableGeneratedRoster` (which runs only inside the replay,
§22.1 correction 3) never sees a DM's seat change, and the DM's shares, seat, government, legitimacy
and relationships survive. Measured: seat kept 57/57 where today it throws 57/57; share kept 57/57
where today it is lost 56/57. (2) **`assertPowerEconomyFreshness` is pin-aware at its three call
sites** — `powerEconomyReconcilePass.js :: registerStep` and both sites in `assembleSettlement.js ::
registerStep` forward the bag, and the assert's one new arm returns true for a held structure, so a
held power structure in a changed world is §14's "incoherence is consequence" rather than a throw.
(3) **The receipt stays the record's.** `powerStructure.economyInputFingerprint` is classed RECEIPT
by `src/domain/edit/recordRegister.js :: RECORD_CLASSES`; under a held structure it is neither
refreshed nor asserted here, and its recompute over the merged record is EM-R0c's
`mergeConsequence.js :: recomputeReceipts`, reached through `src/data/economyFingerprint.js ::
fingerprintPowerEconomyInput` once EM-R0f has moved it there. **This member names that seam and
writes no half of it.**

**Non-goals.** The runner clone (EM-R1, **LANDED**) · the roster final inside `enrichNpcCoherence` (EM-R2, READY on stage 1) · the settlement name (EM-R4, stage 3) · the trace partition (EM-R5, **LANDED** — and §0.4 measures that its partition absorbs this member's whole trace movement) · the merge and the receipt's recompute (EM-R0c, stage 2 beside this member) · the fingerprint's layer move (EM-R0f, **LANDED**) · the institution rename surface (EM-R6, **LANDED**) · the corpus ratchet (EM-R7) · ⛔ **and the partial-pin rule's record-path reading (§0.1's Q1 — EM-R1b, stage 2 beside this member)**. ⛔ **This
member adds no consult to any step's chooser, changes no `provides` list, mints no stream, takes no
draw and persists nothing.** No held key changes hands; no golden moves.

## §3 · Hard scope budget

| Budget | Cap | This member | Instrument |
|---|---:|---:|---|
| handwritten files | 12 | **4** | §7 |
| new LOGIC leaves | 2 | **0** | no CREATE |
| existing LOGIC files modified | 3 | **3** | at the cap, not over |
| new/changed effective production lines | 400 | **+17 / −0** | eslint `Linter`, `max-lines`, `skipBlankLines` + `skipComments` (§P9) |
| each new leaf | 250 eff | **n/a** — no new `src/` leaf | — |
| acceptance cases | 8 | **6** | §9 |

Effective lines RE-EXECUTED at `6a0178b7b` with the one instrument (eslint's `Linter`, `max-lines`,
`skipBlankLines` + `skipComments`):
`src/generators/power/economyReconciliation.js` **259** (raw 338),
`src/generators/steps/powerEconomyReconcilePass.js` **32** (raw 47),
`src/generators/steps/assembleSettlement.js` **163** (raw 325),
`tests/generators/powerEconomyFreshness.test.js` **231** (raw 275).
Predicted after: **270 (+11)**, **35 (+3)**, **166 (+3)**, and the test file **≤ 360**.
⭐ **EM-R0f HAS LANDED AND THE BEFORE-FIGURE HAS MOVED: 280 → 259**, exactly the fall its three
lifted declarations predict; the other three files are byte-identical across four trains. The
**+11 / +3 / +3** are contracts, not estimates, and are unchanged. ⚠ EM-R2 version 5 will add
**+4 eff** to `assembleSettlement.js` at stage 1's flip, so this member's before-figure for that
file becomes **167** and its after **170**; the **+3** it owns does not move. The build lane
re-executes all four figures at its own base.

## §4 · Sealed dispatch and preflight

Dispatch on the lane branch `em-t16-em-r3-<date>`, cut from the train's ONE promotion commit
(§P10.1–§P10.2). The lane's FIRST act is §P10.3's six measurements, each a PASS or the smallest
measured contradiction: (i) branch, HEAD and verified base are the three the dispatch note names;
(ii) §7's path set and the capsule's `changeManifest` are SET-EQUAL; (iii) all four targets PRESENT
at HEAD; (iv) all **six** `requiredSymbols` rows found verbatim at `count=1`, and the **seventh**,
the `_pendingRequiredSymbols` row, DISCHARGED and re-measured verbatim (it reads 0 until stage 1
flips); (v) the count law (§P9(f)) through `EM-R3.count-prover.mjs`, run FROM THE KIT PATH; (vi)
every line-addressed register grepped for every path this member modifies (§5.5's commands re-run).
⭐ **ALL SIX WERE DRY-RUN AT `6a0178b7b` BY THE PRE-PROOF SEAT AND ALL SIX PASS** — §7 vs capsule
SET-EQUAL over 4 rows, all 4 targets present, 6 of 6 symbols verbatim, the prover agreeing with
7 of 7 controls convicted, every line-addressed register at 0, and every one of the 22 sealed
`checks` files present in the tree. ⛔ **A SEVENTH, THIS MEMBER'S OWN:**
`grep -c -F 'const pins = ctx.__pins || null;' src/generators/steps/assembleSettlement.js` must
return **1** — EM-R2's landed binding, which §6's C3 REUSES. A **0** means EM-R2 has not landed and
the member STOPS (§11 item 2). ⭐ **AN EIGHTH, ADDED BY THE PRE-PROOF:** that binding's line number
must be BELOW nothing and ABOVE this member's first `assertPowerEconomyFreshness(` call — EM-R2's
own §6 C2 puts it at the TOP of the callback before the ctx destructure, but a `const` read above
its declaration is a temporal dead zone and the grep alone cannot see it. `EM-R3.remeasure.sh`
carries both. Any failure is a STOP BEFORE the seal.

## §5 · Verified tree contract

### §5.1 · The six required symbols found verbatim at `6a0178b7b`, and the one that ARRIVES

| Path | Symbol | count | What the deliverable must preserve |
|---|---|---:|---|
| `src/generators/power/economyReconciliation.js` | `export function reconcilePowerStructure(` | 1 | the replay's one entry point; this member adds a guard INSIDE it and does not move its declaration line |
| `src/generators/power/economyReconciliation.js` | `export function assertPowerEconomyFreshness(` | 1 | the assert's one declaration; the member appends an OPTIONAL fourth parameter below `tier,` and does not move this line |
| `src/generators/power/economyReconciliation.js` | `function assertStableGeneratedRoster(currentFactions, projectedFactions) {` | 1 | ⛔ **NEVER WEAKENED.** It stays exactly as it is and stays module-private; the member does not gate it, it makes it unreachable under a held structure by not entering the replay (§22.1 correction 3) |
| `src/generators/power/economyReconciliation.js` | `export function projectPowerGenerationIntent(` | 1 | ⛔ EM-B2a3's landed row, byte-identical; the member never calls it under a held structure and never edits it |
| `src/generators/steps/powerEconomyReconcilePass.js` | `registerStep('powerEconomyReconcilePass', {` | 1 | the step's registration; `deps`, `reads`, `readsVersion`, `provides`, `mutates` and `phase` are byte-identical |
| `src/generators/steps/assembleSettlement.js` | `const pins = ctx.__pins || null;` | ⛔ **0 at `6a0178b7b`** (executed) → **1 at stage 1's flip** | ⭐ a `_pendingRequiredSymbols` row, NOT a `requiredSymbols` row. EM-R2 version 5 plants it at the TOP of the callback, before the ctx destructure (its own §6 C2, read). Re-measured VERBATIM at its discharge by `EM-R3.remeasure.sh` (§P10.11, judgment 151) and never promoted as written |
| `src/generators/pipeline.js` | `export function chooseOrPin(pins, key, draw)` | 1 | EM-B2a2's primitive, IMPORTED and CALLED; ⛔ NOT this member's file and not edited by it |

⭐ **ALL SIX NON-PENDING ROWS RE-FOUND AT `count=1`, EXECUTED AT `6a0178b7b`** with `grep -c -F`,
after EM-R0f's landing moved 31 lines out of `economyReconciliation.js` and EM-R1's moved 30 into
`pipeline.js`. **Added: NONE. Removed: NONE. Discharged: NONE owed at this tip.**

⛔ **`_note` fields on these rows carry NO `path:line` address** (§P2.22 and judgment 124): EM-R0f
shifted lines in `economyReconciliation.js` and EM-R2 will shift **+11** at the top of
`assembleSettlement.js` before this member rides (its `holderTable.js` re-address table measures
`:100 → :111`, `:119 → :130`, `:120 → :131`), so an address here would be stale by construction.
⭐ Measured consequence for this member: **it spells no address, so it owes no re-address** —
`tests/lint/sourceCitationIntegrity.walker.test.js`'s baseline holds **0 rows** naming any of its
four paths (executed), and `src/domain/prose/holderTable.js`'s four moving `cite:` rows are
EM-R2's, re-addressed in EM-R2's own commit.

### §5.2 · The post-edit validation, simulated — `retiredSymbols` is NONE

**Three LANDED `requiredSymbols` rows name these four paths**, all of them on
`economyReconciliation.js`: EP-0's `rngSeed: stepRng.fork(POWER_STREAM).seed,`, EM-B2a3's
`export function createPowerGenerationIntent` and EM-B2a3's `export function
projectPowerGenerationIntent`. **Not one is a line this member edits** — the member's three edits in
that file are a new import line, a module-private predicate above `deepFreeze`, and two early
returns inside two other functions. ⇒ no retirement, nothing to discharge, and `check:packet`
re-asserts all three verbatim after this member's own edits. `powerEconomyReconcilePass.js`,
`assembleSettlement.js` and `tests/generators/powerEconomyFreshness.test.js` carry **ZERO** landed
rows between them.

### §5.3 · The registered estate, and the path reservation

**215 entries · 213 LANDED · 2 SUPERSEDED · ZERO non-terminal**, so no LANDED entry reserves a path
against this member. The reservation that matters is in the WAITING estate and it is §0.5's: EM-R2
holds `assembleSettlement.js`, EM-R0f holds `economyReconciliation.js` and
`powerEconomyFreshness.test.js`. `scripts/mutation-coverage-manifest.json` is **not owed**: this
member CREATEs nothing under any enforcer directory, so it carries no row and no `rowKey`.

### §5.4 · The seam, measured — what runs, what must not, and on whose stream

| symbol | role | stream | under a held `powerStructure` |
|---|---|---|---|
| `economyReconciliation.js :: reconcilePowerStructure` | **replay**, two call sites | **0 draws on the caller's stream** — it replays inside `projectPowerGenerationIntent`'s `setActiveRng(createPRNG(intent.rngSeed))` window, a STANDALONE MINT restored by `clearActiveRng` | ⛔ **DOES NOT RUN.** Skipping it is stream-safe BY CONSTRUCTION, not merely by the step-fork rule: it consumes nothing the pipeline shares (§22.1 correction 1's consume-and-discard has no purchase here) |
| `economyReconciliation.js :: assertStableGeneratedRoster` | **assert**, module-private, one call site inside the replay | none | unreachable, because the replay is not entered (§22.1 correction 3, which struck §22 ruling 4's second half) |
| `economyReconciliation.js :: assertPowerEconomyFreshness` | **assert**, THREE call sites | none | returns true without comparing; the fingerprint stays the record's |
| `economyReconciliation.js :: refreshPowerGenerationTraces` | trace re-keyer, two call sites | none | **LEFT UNGATED** — a measured no-op (63/63 whole record identical), §0.4's Q4 |
| `economyReconciliation.js :: projectPowerGenerationIntent` | the replay's projector | mints the power stream from the intent's seed | never called; the mint that EM-B2a3's A3 had to be NARROWED around disappears whole-run |
| `generatePower.js :: registerStep` | the producer's two consults | forks the power stream's seed, 0 draws | **UNTOUCHED** — EM-B2a3's landed code already short-circuits the projector at the producer |

### §5.5 · Every line-addressed register, grepped — `none found`, with the commands

| register | rows over this member's four paths | verdict |
|---|---|---|
| `tests/lint/.prose-numerics-baseline.json` | **0** for all four, RE-EXECUTED at `6a0178b7b` | UNMOVED; this member renders no figure |
| `tests/lint/.source-citation-baseline.json` | **0** for all four, RE-EXECUTED | UNMOVED |
| `scripts/.writer-reach-baseline.json` | **0** for all four, RE-EXECUTED | UNMOVED; this member adds no reader of a settlement field under `src/domain/edit/**` |
| `tests/lint/.lighting-census-baseline.json` | **0** rows naming any of the four (it addresses by TOTAL, not by path) | the DELTA is §7's deferred row, executed in §5.7 |
| `docs/content/wiring-census.json` `stamp.files` | **RE-EXECUTED at `6a0178b7b`: 7 entries**, all under `src/domain/display/stateProse/`; **none of this member's four paths** | no `stale-bytes`; the census is neither a §7 row nor a `checks` entry |
| `path:line` citations of the four files (`git grep -n -P`, both engines agreeing) | every hit is in `docs/` — `PACKET_MANIFEST.json` `_note`s and packet bodies of **LANDED** members (EM-A1, EM-B1a, EM-B2a3, EM-P2), plus design and review prose | `sourceCitationIntegrity.walker.test.js` excludes a LANDED packet's body by `landedPacketPaths`; **no live document addresses a line this member shifts** |
| this packet's own `requiredSymbols` `_note`s | **zero addresses spelled** | the class is removed by construction (§5.1) |

### §5.6 · The `tests/` sweep (§P2.12) and judgment 148's importer law

For every module this member's contract imports or edits, `git grep -l -F '<basename>' -- tests`:

- `tests/generators/pipelinePinnedChoosers.test.js` — **the file that governs this seam most
  closely, read whole.** Its `recordBuiltPins` builds the power bag from the STEP'S OWN PATCH, not
  from the record, so it supplies `powerIntent` and never meets §0.1's refusal; its A3 arm counts
  the power stream's MINTS inside `generatePower`'s own drained window and asserts them absent under
  a pin. ⭐ Its module docstring names this member's seam in its own words — *"a member that held
  `powerStructure` but still ran the projection would lose the fork with the intent and pass the
  fork arm"* — and judgment 143 NARROWED that arm to `generatePower`'s window precisely because
  `powerEconomyReconcilePass` re-mints the same seed later. **This member removes that later mint
  under a pin, which can only STRENGTHEN the arm; it is UNMOVED and this member names no row on it.**
  ⛔ Its closure arm pins `choosersOf('generatePower')` to `['powerIntent','powerStructure']` — the
  reason §0.1's cure is not this member's.
- ⭐⭐ **`tests/lint/heldKeyWriterCensus.walker.test.js` — EM-R1's LANDED CREATE, which did not
  exist when this member was compiled, and which reads ALL THREE of this member's `src/` files by
  name (`ASSEMBLY_FILE`, `RECONCILE_FILE`, `RUNNER_FILE`). Read whole; its four relevant arms were
  RUN over this member's PLANTED text in plain `node` and every one is UNMOVED:**
  - **A3(a)** — every roster and assert row resolves at `count=1` in its file. Executed over the
    planted `economyReconciliation.js`: `reconcilePowerStructure` 1 · `assertPowerEconomyFreshness`
    1 · `assertStableGeneratedRoster` 1 · `projectPowerGenerationIntent` 1 ·
    `refreshPowerGenerationTraces` 1 · `createPowerGenerationIntent` 1. **UNMOVED.**
  - **A3(b)** — the assembly step's discovery set, held EQUAL in both directions against
    `ROSTER ∪ ASSERTS ∪ DISCOVERY_EXEMPT`. Executed: the 19 import specifiers of
    `assembleSettlement.js` are IDENTICAL before and after this member's edit, because **this
    member adds NO import and NO new call there — it adds a fourth ARGUMENT to three existing
    calls**. **UNMOVED**, and ⛔ **this member adds NO row to `DISCOVERY_EXEMPT`**; the one row
    that arrives there is EM-R2's, for `chooseOrPin` (judgment 207).
  - **A3(d)** — the assert surface of `economyReconciliation.js`, both directions. Executed over
    the planted file: `["assertIntent","assertPowerEconomyFreshness","assertStableGeneratedRoster"]`,
    identical to the tip. This member's two new symbols are `UNPINNED` and
    `isHeldPowerStructure`; **neither matches the walker's `/^assert/`**. **UNMOVED.**
  - **A4** — the MIRROR measurement. Executed: after the edit `reconcilePowerStructure`'s body
    names neither `chooseOrPin` nor `__pins` (the consult is inside `isHeldPowerStructure`), so it
    stays in the pre-filter set; it is classed `replay`, not `MIRROR`, so `measuredMirrors` is
    still exactly `['relinkFactionMembers']`. **UNMOVED.**
  - **A2 · A5 · A6** — the step-level census, the substream rule and the clone arms read
    `getStepMeta()`, `narrativeGenerator.js`, `pipeline.js`, `assembleInstitutions.js` and
    `generatePower.js`. This member changes no `provides`/`mutates` and edits none of those files.
    **UNMOVED.**
  ⇒ **judgment 148's law is paid by a `checks` row, not a change-manifest row**: the arm is added
  to §10's sealed `tests/lint` array and to §12's receipt, and this member widens nothing in it.
- `tests/generators/dataFlowContract.test.js` — reads `powerEconomyReconcilePass`'s declaration.
  This member changes no `deps`, `reads`, `readsVersion`, `provides` or `mutates`, so its four arms
  and its five standing controls are **UNMOVED**. (Its producer predicate counts `provides ∪
  mutates` and NOT `scratch` — the second reason §0.1's cure is not this member's.)
- `tests/lint/entropyRootCensus.walker.test.js` — names `economyReconciliation.js`; this member
  mints no stream and retires none (§5.7). **UNMOVED.**
- `tests/generators/powerEconomyFreshness.test.js` — this member's TEST row; its four existing
  `test` titles and their assertions are byte-identical, including the fail-closed arm that hands
  the assert a perturbed prosperity and a fingerprint-stripped structure and requires it to throw.
  ⭐ **That arm passes no `options`, so the pin-aware early return is not reached and the arm keeps
  its meaning** — measured: the fourth parameter is optional and defaults to `{}`.
- `tests/generators/generationForkCensus.test.js` and `tests/lint/generationForkRegistry.contract.test.js`
  — they assert `GENERATION_TIER1` against a measurement taken with **no pins**, and this member
  changes nothing without pins. **UNMOVED.**
- The 17 test files naming `assembleSettlement` and the 15 naming `rulingStructure` were swept for
  an exact pin on any text this member changes: **none found**; every one of them runs the pipeline
  with no pin bag.

### §5.7 · The instrument list (§P2.21), every disposition a row

| instrument | measured at the tip | this member's delta | disposition |
|---|---|---|---|
| entropy-root census, BOTH arms | **RE-EXECUTED at `6a0178b7b`, both grep engines agreeing:** `createPRNG(` **36** under `src/domain`, **47** under `src/` whole, and the walker's own pins at its site-count arm read **36** and **47** — matched; **1** site in `economyReconciliation.js`, **0** in the other two | **+0 / +0** | NOT OWED — no stream minted, none retired; the one existing site is inside `projectPowerGenerationIntent`, which this member neither edits nor deletes |
| ruin-filter roster | the walker's discovery root is `const DOMAIN = join(ROOT, 'src', 'domain')` — MEASURED, not recalled | **+0** | **EXEMPT BY ROOT.** All three source files are under `src/generators`. (`economyReconciliation.js` does read `.institutions` once; the root is what excludes it, and the member says so rather than relying on the read) |
| ST-2 goods roster (`src/domain/goods.schema.js`, parsed by `tests/build/vendorPdfLazy.test.js`) | this member imports no goods identity half-table | **+0** | NOT OWED |
| voice mechanics, Tier 5 (`src/generators`), per file | ⭐ **RUN WITH THE WALKER'S OWN `countFile` / `stringLiteralContents` (espree), EXTRACTED VERBATIM** from `tests/copy/voiceMechanics.test.js` and executed at `6a0178b7b` over all **114** generator files: the baseline holds **7 rows**, names **none** of this member's three files, and each of the three measures **em 0 · bang 0**. ⛔ **The tier's live totals are `em 28 · bang 0` against `EM_BUDGET_GENERATORS = 28` and `BANG_BUDGET_GENERATORS = 0` — ZERO HEADROOM on both** | **+0 / +0** | ⛔ OWED AS A CONSTRAINT, and the constraint is now measured to be absolute: ONE em dash or ONE exclamation point in ANY `src/generators` string literal this member writes ADDS a baseline row AND breaches a budget that is exactly full. §6 spells the one new string literal, `'economyReconciliation:unpinned'`, and the walker's own counter reads it **em 0 · bang 0** (executed) |
| tuning inventory, per file (the library's OWN `countUnregisteredNamed` / `countBareDecimals` at their real signature, over **214** discovered tables) | **RE-EXECUTED at `6a0178b7b`:** `economyReconciliation.js` **2** named (`POWER_INTENT_VERSION`, `POWER_PROJECTION_VERSION`) / **0** bare — unmoved, because `ECONOMY_FINGERPRINT_VERSION` was a STRING and EM-R0f took it anyway; the other two **0 / 0**; tree totals **544** / **7,000** | **+0 on all three** | UNMOVED — this member writes no numeral and removes none. ⚠ A first run at a GUESSED signature returned an empty map; that FALSE ZERO is recorded in the evidence beside the live control that convicts it |
| `stamp.producerIndexFiles` | **1,185** at `6a0178b7b` (was 1,181 at the compile tip; the window's CREATEs moved it, none of them this member's) | **+0** | this member CREATEs no `.js` under `src/domain` or `src/generators`; `stamp.files` and `totals.*` UNMOVED (§5.5) |
| `EAGER_FIRST_PAINT_MODULES` | **270** modules, RE-IMPORTED at `6a0178b7b` from `vite.config.js`'s own export (no `dist` read, cannot skip); none of the three files is a member (executed, each `false`) | **+0 modules, +0 B** | the owner-signed quantity is `CLOSURE_BUDGET_BYTES`, and this member lands nothing in that closure |
| edge-shared bundles (§P2.10) | the five metas' own `inputs` lists hold 107 / 74 / 108 / 2 / 2 entries and name **none** of this member's paths | **none** | NOT OWED; no generated artifact, no rebuild |
| `WORKER_BUNDLE_CEILING_BYTES` | ⛔ **1,392,364** at `6a0178b7b` (EXACT, zero slack) — the compile read **1,396,015**; the chair's buy-back LOWERED it (judgment 203) and a lowered ceiling is never re-raised | ⛔ a predicted RISE — see §5.8 | |
| `e2e/` browser suite (§P2.15) | this member touches no component, route, `data-testid` or accessible name | **none** | NOT OWED, and it names no spec |

### §5.8 · The byte price, the MEMBERSHIP PROOF that cannot skip, and the BUY-BACK

All three source files are **MEMBERS of the generation worker's static closure** — RE-PROVED at
`6a0178b7b` by a plain-node BFS over `src/workers/generation.worker.js`'s own static import graph,
which reads no `dist` and therefore **cannot skip**: **222 files** (220 at the compile tip; the
window's landings added two, one of them EM-R0f's `src/data/economyFingerprint.js`), all three
present, and the control file `src/components/nav/ArrowControl.jsx` correctly absent.

**Predicted delta: a RISE of +40 to +120 B minified.** Seventeen effective lines of guard, one new
import specifier and one `Symbol` literal. ⛔ **THE WORKER BUDGET HAS NO PLACEMENT CURE** (§P2.11:
`worker.rollupOptions` is unset, so every worker entry is its own rollup build) and **the ceiling is
zero-slack at `WORKER_BUNDLE_CEILING_BYTES = 1392364`** (re-executed at `6a0178b7b`; the compile
read 1,396,015 and the chair's buy-back LOWERED it, judgment 203), so this member must be covered by
a buy-back or by the train's byte-arm holder's stated bound. ⛔ **A RISE IN THE CEILING IS THE
OWNER'S (§P11.2) AND THIS MEMBER NEVER ASKS FOR ONE.**

⭐ **THE HOLDER OF TRAIN EM-T16 IS `EM-R1b` (judgment 206d), AND THIS MEMBER'S PRICE IS DECLARED TO
IT AS A MEMBERSHIP FACT:** three files, all members of the generation worker's static closure, none
in the eager first-paint closure, none in the lazy-engine closure; **predicted +40 to +120 B
minified**, which EM-R1b SUMS with stage 1's, its own and EM-R4's in its receipt. This member is not
the holder: it carries **NO row** on `tests/build/generationWorkerLazy.test.js` or
`tests/build/vendorPdfLazy.test.js`, and ⛔ **it does not seal `tests/build/generationWorkerLazy.test.js`
as a `check` either** — the recomputed check set names it (it mentions `economyReconciliation.js` in
its own attribution comment), and sealing it would make `check:packet` depend on a real build this
lane never runs against an exact, zero-slack ceiling. ⚠ The chair's `BUY-BACK-LEDGER.md` is
the register the placement draws from, and the terminal's ONE real build with per-module attribution
is the judge.
⚠ The lazy engine (`tests/build/vendorPdfLazy.test.js`, `toBeLessThan(679_000)`) is not reached:
none of the three files is in the eager first-paint closure.

## §6 · Exact contracts

No discretion. Every clause is a signature, a shape, an order or a refusal.

### C1 — `src/generators/power/economyReconciliation.js`: ONE predicate, ONE condition

- A new import line, placed at the END of the existing import block:
  `import { chooseOrPin } from '../pipeline.js';`
  ⚠ `pipeline.js` is already inside the generation worker's closure, so the edge adds no module to
  any bundle; and `pipeline.js` imports nothing from `src/generators/power/**`, so no cycle opens.
- A module-level sentinel above `deepFreeze`, EM-B2a3's landed idiom:
  `const UNPINNED = Symbol('economyReconciliation:unpinned');`
  ⛔ The literal carries no em dash and no exclamation point (§P2.19; the Tier-5 baseline holds this
  file at em 0 · bang 0 and a per-file baseline never rises).
- A module-private predicate beneath it, and it is **the only place this condition is spelled**
  (§P4's one-refusal-site-per-CONDITION law):
  ```js
  function isHeldPowerStructure(pins) {
    return chooseOrPin(pins, 'powerStructure', () => UNPINNED) !== UNPINNED;
  }
  ```
  ⛔ The key is the literal `'powerStructure'` — the RECORD PATH the producer writes, per
  `runPipeline`'s own `@typedef Pins`. ⛔ It is never a step name, and never a second spelling of
  the runner's own-property rule (no `pins && 'powerStructure' in pins`, no `pins?.powerStructure ?? …`).
- ⛔ **NO LOCAL CLONE.** EM-R1 clones the bag on entry at the runner; a clone here would be a third
  spelling of design §22 ruling 6 and would go dead the day EM-R1 lands.
- `reconcilePowerStructure(powerStructure, economicState, intent, options = {})` — **the arity and
  the signature are UNCHANGED**; `options` already exists and already carries `defenseLabel`. One
  behaviour is added, AFTER `assertIntent(intent)` and AFTER the existing roster guard, and BEFORE
  `const beforeFactions = …`:
  ```js
  if (isHeldPowerStructure(options.pins)) {
    const heldFactions = powerStructure.factions.map(faction => ({ ...faction }));
    return { beforeFactions: heldFactions, afterFactions: powerStructure.factions };
  }
  ```
  ⛔ `assertIntent` still runs first, so a stale or missing intent still refuses under a held
  structure: heldness excuses the REPLAY, never a malformed seam. ⛔ The return shape is exactly
  today's `{ beforeFactions, afterFactions }` and `beforeFactions` is a shallow copy per element,
  exactly as the unheld path builds it — which is what makes `refreshPowerGenerationTraces` a
  measured no-op (§0.4) instead of a second branch.
  ⛔ Nothing below the guard changes: not `projectPowerGenerationIntent`'s call, not
  `assertStableGeneratedRoster`'s, not the neighbour renormalization, not the seven field
  assignments.
- `assertPowerEconomyFreshness(powerStructure, economicState, tier, options = {})` — a FOURTH
  parameter, OPTIONAL, defaulting to `{}`, appended below `tier,` in the existing multi-line
  signature. One behaviour is added as the function's FIRST statement:
  ```js
  if (isHeldPowerStructure(options.pins)) return true;
  ```
  ⛔ `options.pins` absent or `null` ⇒ **today's behaviour EXACTLY, byte for byte** — the
  fingerprint comparison, the message and the throw are untouched, which is what keeps
  `powerEconomyFreshness.test.js`'s fail-closed arm meaning what it means. The return value stays
  `true` on the passing path, as today.
- ⛔ **`assertStableGeneratedRoster` IS NOT EDITED, NOT GATED AND NOT WEAKENED.** It becomes
  unreachable under a held structure because the replay is not entered — §22.1 correction 3's own
  reasoning, and the reason ruling 4's second half was struck.

### C2 — `src/generators/steps/powerEconomyReconcilePass.js`: the bag reaches both calls

- At the TOP of the step callback, before the destructure, the spelling
  `generatePopulation.js` and `assembleInstitutions.js` already carry:
  `const pins = ctx.__pins || null;`
- `reconcilePowerStructure(ctx.powerStructure, ctx.economicState, ctx.powerIntent)` gains a fourth
  argument `{ pins }`.
- `assertPowerEconomyFreshness(ctx.powerStructure, ctx.economicState, ctx.tier)` gains a fourth
  argument `{ pins }`.
- `refreshPowerGenerationTraces(ctx, beforeFactions, ctx.powerStructure, ctx.powerIntent)` is
  **BYTE-IDENTICAL** (§0.4's Q4).
- ⛔ `registerStep('powerEconomyReconcilePass', {` and its whole meta object — `deps`, `reads`,
  `readsVersion`, `provides`, `mutates`, `phase` — are BYTE-IDENTICAL. The step still runs, still
  declares that it mutates `powerStructure`, and under a held structure mutates nothing; the fork
  census already records that pair as moving the key in zero rows.

### C3 — `src/generators/steps/assembleSettlement.js`: three forwards and nothing else

- ⭐ **`const pins = ctx.__pins || null;` is EM-R2's landed binding and is REUSED, never re-spelled.**
  §4's seventh preflight measurement proves it present; §11 item 2 STOPS if it is not.
- The first `assertPowerEconomyFreshness(settlement.powerStructure, settlement.economicState, tier)`
  gains a fourth argument `{ pins }`.
- `reconcilePowerStructure(settlement.powerStructure, settlement.economicState, ctx.powerIntent,
  { defenseLabel: settlement.defenseProfile.readiness.label })` gains `pins` as a second property of
  the options object it already passes.
- The second `assertPowerEconomyFreshness(…)` gains a fourth argument `{ pins }`.
- ⛔ The `if (settlement.defenseProfile?.readiness?.label)` guard, the
  `refreshPowerGenerationTraces` call, the ODQ §810 SEAM 1b comment block and
  `resizePoliticalRoster` are BYTE-IDENTICAL. ⛔ Nothing else in the step changes: not
  `generateSettlementName` (EM-R4's), not the stress re-render, not `generateCoherence` (EM-R2's),
  not `buildGenerationCoherenceReceipt`, not `normalizeSettlement`.

### C4 — What this member REFUSES

- ⛔ It does not persist `powerIntent` or any part of it (design §22 ruling 5; §P8).
- ⛔ It does not change `generatePower`'s `provides`, the runner's partial-pin rule, or any step's
  declaration (§0.1's Q1 is the chair's).
- ⛔ It does not recompute `powerStructure.economyInputFingerprint` anywhere. Under a held structure
  the receipt stays the record's through the whole pipeline; the recompute over the MERGED record is
  EM-R0c's `mergeConsequence.js :: recomputeReceipts`, and the function it calls will live at
  `src/data/economyFingerprint.js :: fingerprintPowerEconomyInput` once EM-R0f has moved it — which
  is also what keeps EM-R0c out of a `src/domain` → `src/generators` import edge the boundary test
  refuses (§22.3 item 6's "invert, don't widen").
- ⛔ It weakens, widens or skips no spec, walker or ratchet, and it raises no ceiling.

### Determinism, flag, lifecycle, receipts

- **Hash/fork key:** unchanged. No fork label, no stream and no draw moves. The only draws this
  member can remove are those inside `projectPowerGenerationIntent`'s own standalone mint, which is
  restored by `clearActiveRng` and shares nothing.
- **No-draw behaviour:** with `options.pins` absent or `null`, `isHeldPowerStructure` returns false
  and not one new statement's consequent executes.
- **Golden posture: UNCHANGED, BY CONSTRUCTION.** Measured: with no pins the record is unmoved
  across the corpus stride (§0.3's C2). ⛔ `UPDATE_GOLDEN=1` is FORBIDDEN to this packet.
- **Flag:** `NONE`. Wave 1 is headless and mints no flag.
- **Lifecycle:** create / read / persist / reload / undo / import / public veil — all unchanged;
  this member writes no record key and persists nothing. **Regenerate** is the member: a pinned
  re-derivation now keeps the DM's power structure instead of replaying over it.
- **Receipts and privacy:** no new message, no rendered figure, so prose-numerics is not owed.

## §7 · Exact change manifest

⭐ **GENERATED FROM `EM-R3.manifest.json` AND PROVED SET-EQUAL WITH IT** (§P9(d)).

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `MODIFY` | `src/generators/power/economyReconciliation.js` | the import block; a new module-private `UNPINNED` and `isHeldPowerStructure` above `deepFreeze`; one guard inside `reconcilePowerStructure`; one parameter and one guard in `assertPowerEconomyFreshness` | **+11 eff** | §6's C1, no fifth act. ⛔ `export function projectPowerGenerationIntent`, `export function createPowerGenerationIntent`, `rngSeed: stepRng.fork(POWER_STREAM).seed,` and `function assertStableGeneratedRoster(currentFactions, projectedFactions) {` come through BYTE-IDENTICAL (three are landed `requiredSymbols` rows). ⛔ No em dash and no exclamation point in the one new string literal. ⛔ No numeral: the tuning inventory reads 2 named / 0 bare here and must still. ⚠ EM-R0f has already moved three declarations out of this file; re-measure the before-figure at dispatch. |
| `MODIFY` | `src/generators/steps/powerEconomyReconcilePass.js` | the step callback's first line and two of its three calls | **+3 eff** | §6's C2. ⛔ `registerStep('powerEconomyReconcilePass', {` and the whole meta object byte-identical. ⛔ `refreshPowerGenerationTraces(…)` byte-identical. |
| `MODIFY` | `src/generators/steps/assembleSettlement.js` | the two `assertPowerEconomyFreshness` calls and the one `reconcilePowerStructure` call inside `registerStep` | **+3 eff** | §6's C3. ⛔ REUSE EM-R2's `const pins = ctx.__pins || null;`; adding a second binding of that name is a STOP. ⛔ Every other statement of the step byte-identical. |
| `TEST` | `tests/generators/powerEconomyFreshness.test.js` | a NEW second top-level `describe` holding A1–A6 | n/a | ⭐ **ADDS SIX `test`s UNDER ONE NEW `describe`** (`'EM-R3 — the power structure is final under a held pin'`) — the same six §9's matrix homes here and the same six §12's lighting row prices. ⛔ **WEAKEN NOTHING**: the existing `describe` and its four `test` titles stay byte-identical, including the fail-closed arm, which passes no `options` and therefore still reaches the throw. Flat literal `test(...)` under ONE literal `describe`, matching the file's own opener (`import { describe, expect, test } from 'vitest';`); ⛔ never bind `it`, `test` or `describe` as a variable or parameter; no `.each`, no loop, no conditional registration, no nested describe; ⛔ no bare seed loop — collect over `censusCorpus()` rows, then assert once; every negative assertion carries `// anchored:` on the line IMMEDIATELY above the `expect` itself. Copy the arm shape of `tests/generators/pipelinePinnedChoosers.test.js`'s `expectPresentThenAbsent` idiom for A5. ⭐⭐ **A5's MINT CENSUS NEEDS THE RECORDER, AND THE RECORDER MUST BE `vi.hoisted`.** `vi.mock` is hoisted ABOVE this file's static imports and those imports reach `src/kernel/prng.js` transitively, so a recorder declared as a plain module-scope `const` is read before it initializes and kills the WHOLE FILE at load with `Cannot access '…' before initialization` — MEASURED on `pipelinePinnedChoosers.test.js` and recorded in its own docblock. Write it exactly as that file does: `const mintSeeds = vi.hoisted(() => ({ seeds: [], on: false }));` then `vi.mock('../../src/kernel/prng.js', async (importOriginal) => …)` returning a PURE PASS-THROUGH that records the seed and returns the real stream. ⛔ The existing `vitest` import is WIDENED to `import { describe, expect, test, vi } from 'vitest';`, never re-spelled, and `vi` is bound exactly once. ⭐ **EXECUTED: the lighting walker's own `parkReasonsFor` over the file WITH that recorder and with one new `describe` of six straight-line `test`s returns `[]` — CREDITED — and `liveTitlesIn` counts 10 (4 + 6), `liveSuiteTitlesIn` 2 (1 + 1)**; the same classifier returns `[]` for `pipelinePinnedChoosers.test.js`, which already carries this idiom, so `vi.mock` parks nothing. |

**Generated artifacts: `NONE`.**

> ⛔ **THE LIGHTING CENSUS IS THE ONE DEFERRED ROW.**
> `tests/lint/.lighting-census-baseline.json` — a named INTERIOR RED, **delta only**, all five
> figures in the register's own order because the walker stops at its first moved figure:
> **`files +0 · parked +0 · credited +0 · titles +6 · suiteTitles +1`.**
> `+6` is DERIVED and the count law binds it (§P9(f)): the TEST row's **six** `test`s; `+1` suite is
> its one new `describe`. `EM-R3.count-prover.mjs` refuses the packet if §7's own words, §9's
> matrix, §10 and this row ever disagree. The predicted red MESSAGE has the shape
> `expected <N+6> to be <N>`. ⛔ No refreeze is run here; the terminal's one re-derivation is the
> chair's (§P2.1).
>
> ⭐⭐ **`+6 titles · +1 suiteTitles` IS NO LONGER A PREDICTION: THE WALKER'S OWN CLASSIFIER WAS RUN
> OVER THE PLANTED SHAPE.** Executed at `6a0178b7b` with
> `tests/lint/sovereigntyLightingContract.walker.test.js`'s own `parkReasonsFor` / `liveTitlesIn` /
> `liveSuiteTitlesIn`:
> ```
> tests/generators/powerEconomyFreshness.test.js : parkReasons=[]  titles=4   suiteTitles=1
> PLANTED (+ the vi.hoisted recorder, + 1 describe, + 6 straight-line tests):
>                                                  parkReasons=[]  titles=10  suiteTitles=2
> tests/generators/pipelinePinnedChoosers.test.js: parkReasons=[]   <- the CONTROL: vi.mock parks nothing
> ```
> ⇒ the file is **CREDITED** before and after, and the delta is exactly **titles +6 ·
> suiteTitles +1 · files +0 · parked +0 · credited +0**. It imports `describe`, `expect` and `test`
> from `vitest`, binds none of them a second time (nor `vi`), registers literal `describe`s and
> straight-line literal `test`s, and uses no `.each`, no loop and no conditional. The build lane
> re-runs the same classifier at its own base and quotes the output. The register's LIVE tuple at
> `6a0178b7b` is `files 2692 · parked 359 · credited 2333 · titles 25777 · suiteTitles 6847` —
> recorded here as the chair's refreeze INPUT, never asserted by this member.
>
> ⛔ **THE WIRING CENSUS IS NOT A ROW AT ALL.** `stamp.producerIndexFiles` **1,185 at `6a0178b7b`,
> delta +0** (this member CREATEs no `.js` under `src/domain` or `src/generators`, the two roots the
> stamp counts), `stamp.files` **UNMOVED** (its 7 entries, re-read, name none of this member's
> paths), `totals.*` **UNMOVED**.
> The path appears in NEITHER §7 NOR the manifest NOR `checks`, and no lane runs
> `node scripts/wiring-census.mjs`.
>
> ⛔ **`scripts/mutation-coverage-manifest.json` IS NOT OWED.** This member CREATEs nothing under any
> enforcer directory; `ENFORCER_DIRS` is read from `tests/lint/mutationCoverage.shared.mjs` at the
> lane's own tip and the verdict re-measured there, never recalled (§P2.2).

## §8 · Ordered coding sequence

1. Run §P10.3's six pre-seal measurements plus §4's seventh. Any failure STOPS before the seal.
2. **RED FIRST, and read the COUNT LINE** (§P7: a log with no test count DID NOT RUN). Write A1, A2
   and A3 into `tests/generators/powerEconomyFreshness.test.js` and run them against the UNCHANGED
   tree: A1 red at `shareLost 56/57`, A2 red at `threw 57/57` with `assertStableGeneratedRoster`'s
   message, A3 red at the three-site count with the stale figure.
3. Edit `src/generators/power/economyReconciliation.js` per §6's C1, in C1's order.
4. Edit `src/generators/steps/powerEconomyReconcilePass.js`, then
   `src/generators/steps/assembleSettlement.js`. Re-run step 2's arms: all three green.
5. Write A4, A5 and A6. A4's golden stride and A5's mint census are run against the committed
   fixture, never re-recorded.
6. Measure both files' effective-line counts with the eslint `Linter` before and after, and the
   tuning inventory and the Tier-5 voice counter over all three source files.
7. Run the focused battery of §10, then the mutants of §P6, then the pre-seal count prover from the
   kit path.

## §9 · Acceptance matrix

| ID | Case | Fixture/input | Required observation | Test home |
|---|---|---|---|---|
| A1 | ⭐ **THE REPLAY DOES NOT RUN UNDER A HELD STRUCTURE** | the census corpus, `censusCorpus()`; a record-built bag holding `powerStructure`, handed through `runPipeline` with `onStrictViolation` collecting §0.1's partial-pin refusal; §14's flagship world change (terrain → desert) with one non-governing faction's share halved | the DM's share survives in **every row** and the held structure is byte-identical to the bag in every row (`JSON.stringify` equality, collected then asserted once); **NEGATIVE CONTROL**, same arm: against the PRE-MEMBER tree the same bag is overwritten in **56 of 57** rows and the share survives in **0**, so the pass can never go silently vacuous; ANTI-VACUITY: every row is asserted to have really taken the pin | `tests/generators/powerEconomyFreshness.test.js` |
| A2 | ⭐ **THE SEAT CHANGE STICKS INSTEAD OF THROWING** (design §22 finding 4; §22.1 correction 3) | the same corpus, config UNCHANGED, the governing flag moved from one faction to another | nothing throws and the newly seated faction is governing in **every row**; **RED-FIRST / NEGATIVE CONTROL**: against the pre-member tree the identical bag throws in **57 of 57** with `Power intent replay changed the generated faction identity roster.`, whose text is asserted; ⛔ `assertStableGeneratedRoster` is proved UNWEAKENED in the same arm — it still throws when the replay IS entered, i.e. with no pin bag and a perturbed roster | same |
| A3 | ⭐ **THE FRESHNESS ASSERT IS PIN-AWARE AT ITS THREE CALL SITES** | a counting pass-through around `assertPowerEconomyFreshness`, drained per run; a held bag under the flagship world change | the assert is reached **exactly three times per run** (the count is the arm, not prose) and refuses **none** of them under a held structure; **RED-FIRST**: with the replay suppressed and the assert NOT pin-aware, **41 of 57** rows throw — the measured stale-fingerprint figure, which is why the two halves are one member; ANTI-VACUITY: with no pin bag the same three sites still compare, and a perturbed prosperity still throws | same |
| A4 | **THE GOLDEN IS UNMOVED BY CONSTRUCTION** | `tests/fixtures/generator-golden-master.json`'s committed blob and a corpus stride, with `options.pins` ABSENT | every strided record hashes identically to the committed fixture and not one statement this member adds executes; ⛔ the 525-row master and `tests/property/dossierProseManifest.test.js` are run unmodified and must be green; **ANTI-VACUITY**: the same stride with a held bag DOES differ, so the identity is a measurement and not an empty read | same |
| A5 | ⭐ **NO NEW STREAM, NO NEW DRAW, AND THE LATER MINT DISAPPEARS** | the mint census idiom of `tests/generators/pipelinePinnedChoosers.test.js` — a pass-through recorder over `createPRNG`, drained at every step boundary | under a held structure the power stream's seed is minted **nowhere in the whole run**, where today it is minted again inside `powerEconomyReconcilePass` — the WHOLE-RUN absence that EM-B2a3's A3 had to be narrowed away from (judgment 143), now provable; with no pin bag the mint is present in both windows, unchanged; and the `createPRNG(` site counts are **36 / 47**, unmoved | same |
| A6 | ⭐ **THE RECEIPT STAYS THE RECORD'S AND THE TRACE REFRESHER IS A NO-OP** | a held bag; the final context of a pinned run; and the same run with `refreshPowerGenerationTraces` replaced by a no-op through the test's own seam | under a held structure `powerStructure.economyInputFingerprint` at the end of the pipeline is still the RECORD's value in every row (the recompute is EM-R0c's, §22.2 item 7); and the whole record is identical with and without the trace refresher in every row, which is the measurement that licenses leaving it ungated; **ANTI-VACUITY**: with no pin bag the fingerprint IS refreshed by the replay and the two runs differ | same |

This table is the entire edge-case budget — **6 of 8**. Omit nothing; add nothing.

### §9a · The negative controls this battery must carry

Every one of A1, A2, A3, A4 and A6 carries its control IN THE SAME ARM, because each is a
written-contract claim whose passing shape is an identity (§P6: an arm that already holds at the
base is proved by its counterforce measured, never red-first alone). The counterforces are: A1's
56/57 overwrite, A2's 57/57 throw, A3's 41/57 stale-fingerprint throw, A4's held-bag divergence and
A6's unpinned refresh. Each is quoted in the receipt beside its arm.

## §10 · Verification commands

⛔ **THE TYPE FLOOR, MEASURED: TWO CONFIGS, AND ONLY ONE GOVERNS THIS MEMBER.**
`tsconfig.full.json` (the gate's own `npm run typecheck`) includes `src/generators/**/*.js`, so it
governs **all three** of this member's `src/` paths. `tsconfig.domain-strict.json` extends
`tsconfig.json`, whose `include` is `src/domain/**` alone and whose `exclude` NAMES
`src/generators`, so **the strict domain config governs NONE of this member's paths** and the
"a new or worsened `src/domain` file must be strict-clean" rule has no purchase here. Neither
config includes `tests/`, so the TEST row is governed by neither. Both are still RUN, because a
regression in either is the gate's.

```sh
# Focused static checks (no gate slot needed)
npx eslint src/generators/power/economyReconciliation.js \
  src/generators/steps/powerEconomyReconcilePass.js \
  src/generators/steps/assembleSettlement.js \
  tests/generators/powerEconomyFreshness.test.js
npm run typecheck:ratchet          # tsconfig.full.json — measure THIS member's paths (§P7)
npm run typecheck:domain:strict    # tsconfig.domain-strict.json

# The one test directory this member touches
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 tests/generators

# ⭐ tests/lint WHOLE, MINUS THE LIGHTING WALKER (§P7) — MUST exit 0
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 tests/lint \
  --exclude=tests/lint/sovereigntyLightingContract.walker.test.js

# The lighting walker ALONE — the member's measured INTERIOR RED, message quoted
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/sovereigntyLightingContract.walker.test.js                    # expected NONZERO

# The goldens — UNMOVED is the claim, so they are run
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js

# PRE-SEAL, from the KIT PATH, never placed in the tree (§P9)
node <kit>/packets-waiting/EM-R3.count-prover.mjs <kit>/packets-waiting/EM-R3.md \
  <kit>/packets-waiting/EM-R3.manifest.json
```

**Sealed `checks`** (files, never the `tests/lint` directory; the lighting walker is NEVER sealed;
the validator last) — the array is in `EM-R3.manifest.json` and is COMPUTED, not recalled: every
`tests/` file that names one of this member's changed paths, its two step names, or a symbol whose
call shape it changes, MINUS the `.json` hits, MINUS `tests/helpers/`, and MINUS the lighting
walker.
⭐ **RE-COMPUTED AT `6a0178b7b`: 23 files against the compile's 21, and BOTH new hits are EM-T15
arrivals.**
- ⭐ **ADDED: `tests/lint/heldKeyWriterCensus.walker.test.js`** — EM-R1's landed CREATE, which reads
  all three of this member's `src/` files. Judgment 148's law. Every arm of it that could move was
  run over this member's planted text and is UNMOVED (§5.6); it is source-only, needs no build, and
  costs the gate seconds.
- ⛔ **REFUSED: `tests/build/generationWorkerLazy.test.js`**, and the refusal is the law. It is the
  train byte-arm HOLDER's arm (EM-R1b, judgment 206d), it requires a real `npm run build`, and its
  `WORKER_BUNDLE_CEILING_BYTES` is EXACT with zero slack. Sealing it would make `check:packet`
  depend on a build this lane never runs, against a ceiling only the terminal can measure
  (§P2.11, §5.8). It appears in NEITHER `checks` NOR §7.

## §11 · Mandatory STOP conditions

In addition to `PACKET_STANDARD.md`'s list and §P8's, this member STOPS when:

1. ⛔ **The chair has not ruled §0.1's Q1 and the placement expects this member to cure it.** The
   cure touches `src/generators/pipeline.js`, reds a landed closure pin and reds two landed
   data-flow arms; it is not this lane's and it is not this member's scope.
2. ⛔ **`grep -c -F 'const pins = ctx.__pins || null;' src/generators/steps/assembleSettlement.js`
   returns 0 at dispatch** — EM-R2 has not landed, and §6's C3 has no binding to reuse.
3. ⭐ **DISCHARGED AT THE PRE-PROOF, 2026-09-23.** EM-R0f LANDED at `d7d6ceef7` (train EM-T14):
   `economyReconciliation.js` no longer declares `fingerprintPowerEconomyInput`,
   `economyProjectionInput` or `ECONOMY_FINGERPRINT_VERSION`, §3's before-figure was re-executed
   (280 → **259**) and §5.1's four rows over that file were re-found verbatim at `count=1`. The
   condition is kept as history, not as live work; ⛔ **the standing form of it is the LANE's:** if
   `grep -c -F 'from '\''../../data/economyFingerprint.js'\''' src/generators/power/economyReconciliation.js`
   reads 0 at dispatch, the file is not the one this member was proved against and the lane STOPS.
4. Any golden or the prose manifest moves.
5. The worker bundle's measured growth exceeds the train byte-arm holder's stated bound (the
   holder is **EM-R1b**, judgment 206d), or any signed ceiling would be re-minted upward (§P11.2 —
   the owner's). ⛔ The ceiling at this pre-proof's tip is **`WORKER_BUNDLE_CEILING_BYTES = 1392364`**,
   EXACT and zero-slack; it was LOWERED by the chair's buy-back (judgment 203) and a lowered ceiling
   is never re-raised to make room.
6. A number lands under `src/domain` or `src/generators` that is a genuine simulation dial
   (§P2.17 — tuning is the owner's). This member writes no numeral.
7. An instrument named in §P2.21 reds with a figure this packet did not predict; the arm's own
   message decides and a figure adopted by hand is the failure.
8. A spec, a walker or a ratchet would be weakened, widened or skipped to make this member pass —
   in particular `assertStableGeneratedRoster`, which is made UNREACHABLE under a held structure and
   never softened.
9. Persisting `powerIntent`, or any field of it, appears necessary (design §22 ruling 5; a persisted
   shape is the owner's).

## §12 · Completion receipt

- Base SHA; dispatch bundle and seal identity:
- The chair's ruling on §0's Q1–Q4, quoted, and what changed here because of each:
- Exact changed files and effective-line counts (eslint `Linter`, `skipBlankLines` + `skipComments`), before and after, for all three `src/` files:
- Acceptance cases A1–A6, executed, each with its §9a counterforce quoted (56/57 · 57/57 · 41/57 · the held-bag divergence · the unpinned refresh):
- The red-first log's COUNT LINE for A1, A2 and A3 against the unchanged tree (a log with no count DID NOT RUN):
- ⭐ The §P6 mutants: each planted in `src/generators/power/economyReconciliation.js`, each reddening its OWN acceptance title, each restored to the exact pre-mutant SHA-256:
- The golden stride: rows checked, rows moved (must be zero), and the committed fixture's identity:
- `assertStableGeneratedRoster` re-proved LIVE with no pin bag and a perturbed roster (it must still throw):
- The walker's own park classifier over `tests/generators/powerEconomyFreshness.test.js`, quoted (expected: `parkReasons=[]` — CREDITED — with `titles` 4 → 10 and `suiteTitles` 1 → 2, which the pre-proof already executed over the planted shape):
- Lighting census DELTA, all five figures, attributed by execution (⛔ never an absolute; ⛔ no refreeze here):
- Wiring census: `stamp.producerIndexFiles` before and after (expected UNMOVED, +0) and `stamp.files` (expected UNMOVED):
- Entropy census both arms (expected 36 / 47, +0); tuning inventory per file (expected 2 / 0 for `economyReconciliation.js` and 0 / 0 for the other two, unmoved); Tier-5 voice counts for all three files with the WALKER'S OWN counter (expected em 0 · bang 0, no new baseline row) and the tier's totals (expected `em 28 · bang 0`, the budget exactly full in both):
- The worker byte delta as measured at the terminal's one real build, with per-module attribution showing only this member's modules moved, and the buy-back or **EM-R1b's** (the holder's, judgment 206d) stated bound it lands under, against `WORKER_BUNDLE_CEILING_BYTES` as it stands at the landing (**1,392,364** at the pre-proof tip):
- ⭐ `tests/lint/heldKeyWriterCensus.walker.test.js` — the LANDED held-key writer census (EM-R1's), which reads all three of this member's `src/` files. Its A3(a), A3(b), A3(d) and A4 arms quoted GREEN at the landing, and the statement that this member added NO `DISCOVERY_EXEMPT` row and NO `ASSERT_EXEMPT` row:
- `tests/lint --exclude=…sovereigntyLightingContract…` — exit (must be 0):
- The lighting walker alone — the named interior red, message quoted:
- Both typecheck configurations (`typecheck:ratchet`, `typecheck:domain:strict`), with the measured statement that `tsconfig.full.json` governs all three `src/` paths and the STRICT domain config governs NONE of them (§10):
- §7-versus-capsule set-equality proof, re-run at the landing:
- Generated artifacts: `NONE`
- Deviations: `NONE | STOP`
- Out-of-scope observations, without investigation:
- ⭐ §0.4's trace figure RE-TAKEN at the landing sha, with that sha as its new as-of mark: the RAW `runPipeline` figure (expected ~30/63) AND ⛔ **the figure THROUGH EM-R5's LANDED `partitionTrace`, which the pre-proof measured at 0 of 63** — EM-R5 has landed, so nothing is handed to it; the receipt records that the movement is invisible on the `rederive` path and names the raw figure as a raw-runner measurement with its as-of mark:
- Judgment calls: `NONE` — §0's four questions are the chair's and this packet records how each was ruled.
