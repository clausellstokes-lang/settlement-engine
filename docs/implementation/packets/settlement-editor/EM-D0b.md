# Settlement editor / EM-D0b — THE TIER GATE AND THE DARK RULE: `TIER_GATE.premium.settlementEditor`, its tier-facts parity pin, and the one conjunct the owner deletes when he opens the door

- **Status:** `READY`
  ⚠ The status value above stands ALONE on its line because `parsePacketHeader` anchors the status
  row at end-of-line and takes `status` only when exactly one row matches. Every stamp, caveat and
  date goes on these continuation lines, never on the row.
  ⭐ **THE SECOND OF THE FIVE MEMBERS OF EM-D0**, the first door (design §20.4; the charter's EM-D0
  row; the chair's ruling 2 of 2026-09-20 ~22:15). **D0a** the shared glow token (compiled) ·
  **D0b** (this) · **D0c** the halo-capable surface · **D0d** `PoolField` + `FreeField` · **D0e**
  `CardEditorDialog`. ⛔ The charter's row as written modifies SIX existing logic files against a
  cap of THREE; the partition puts each member at two.
  ⭐ **IT DEPENDS ON NOTHING AND CAN LAND IN PARALLEL WITH D0a.** It is one of only two members of
  the five that is independent of every waiting packet in the editor estate.
  ⛔ **THE FIELD IS `settlementEditor` (the chair's ruling 3, judgment 39), NOT `editMode`** — the
  rename frees the collision with the shipped Cartographer boolean `editMode`
  (`settlementSlice.js:806`) and with EM-C4a's transient `editorMode`. **This packet pins the one
  vocabulary**, and D1 asserts `editMode` ABSENT from both tables by name.
  ⭐ **TRAIN EM-T9's BYTE-ARM HOLDER** (judgment 74): within one train the first-paint budget's
  `TEST` row is carried by exactly ONE member, and it is this one. §7 and §10 carry the train's
  summed arithmetic.
- **Packet version:** 2
  **What version 2 changed and why:** a pre-proof at `e80a6a3f4` found that version 1 sealed six
  acceptance cases while authorizing no file to write five of them in — `canEditSettlement` and
  `settlementEditor` appear ZERO times in the repository, so every case must be newly written, yet
  the capsule held no `CREATE` row and its one editable test row read *"+1 line … add no `it`, no
  `describe`"* — so version 2 adopts the chair's CURE A (judgment 75(1)): ONE `CREATE`,
  `tests/store/settlementEditorGate.test.js`, becomes the declared home of D1's value and vocabulary
  assertions, D2, D3, D4's source scan and D6's source scan; the lighting row's *"zero in all five
  figures"* is replaced by the true counted delta `files +1 · parked +0 · credited +1 · titles +8 ·
  suiteTitles +1`; three `requiredSymbols` the new file calls are added (`createAuthSlice`,
  `STAFF_UNLOCK_ALL_PAID`, `STAFF_ROLES`); the byte row is re-stated to cover the whole train's
  summed price (judgment 74); and §13 records R1 as DOCKETED, R3 as RATIFIED, R5 as ONE OWNER-SIGNED
  ACT, and R2 and R4 as already RULED by judgment 41 questions 5 and 6.
- **Verified base:** `em-t9-d0b-2026-09-21` at `7f0621fdb72f769a285502ff5481247c1f07b04b`
  ⚠ Left for the chair's promotion stamp. **The revalidation sentence the chair will use:**
  *"Re-measured at `e80a6a3f443e2f6a36e28fbebb72e98e46869fac` (read tip `read-tip-em-t9`, detached,
  `git status --short` EMPTY at the start and the end of the lane): the J-T1 window from the compile
  base `bdbf7c89c` is EMPTY over every change-manifest path and every `requiredSymbols` path (18
  commits in the window, none touching any of them); the ONE `CREATE` target
  `tests/store/settlementEditorGate.test.js` is ABSENT (`ls-tree` 0 hits); all thirteen
  `requiredSymbols` are present VERBATIM at the paths in §5, each at `git grep -c -F` = 1;
  `retiredSymbols` is empty and proved so by the post-edit simulation (every edit to an existing
  file is additive); the preamble measured
  `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`; the eager first-paint set
  measured **269**, with `src/store/authSlice.js` a member and `src/config/tierFacts.js` NOT one;
  and `settlementEditor` and `canEditSettlement` are present in **ZERO** files anywhere in the
  repository."* ⛔ A `__BASE__` packet can never pass `validate:packets`; validation is downstream
  of this stamp, never a precondition of it.
- **Last revalidated:** left for the chair, with the sentence above.
- **Depends on:** `NONE`. ⭐ **THIS PACKET FLOATS.** It imports no file a sibling creates, names no
  symbol that does not exist today, and its `_pendingRequiredSymbols` is absent because there is
  nothing pending. It may be promoted and landed independently of EM-A1, EM-A2a, EM-A2b, EM-B1a,
  EM-B1c1, EM-B1h, EM-B1i, EM-B2a1, EM-C4a and of the other four EM-D0 members.
- **Collision group:** **`tests/build/vendorPdfLazy.test.js` (EM-B2a4)**, which declares the same
  `TEST` row in a different train (interim rule 2; within EM-T9 this member is the sole holder, by
  judgment 74). ⭐ `src/store/authSlice.js`, `src/config/tierFacts.js`,
  `tests/config/tierFacts.contract.test.js` and the new `tests/store/settlementEditorGate.test.js`
  carry **NO** collision — measured over every waiting packet's capsule, and over all three EM-T9
  siblings and EM-B3d, none of which carries any `tests/store/` row. ⚠ `src/store/authSlice.js` has
  **23** test importers, which is a `checks`-scoping fact (§10), not a collision.
- **Commit authority:** edits only; the chair commits.
- **Baseline posture:** measured. `TIER_GATE` and the four shipped tier predicates read at source;
  `resolveTier`, `resolveRole` and `previewPersonaRole` read **whole**; `isStaffRole` and
  `staffUnlocksPaidFeatures` read **whole** with their own headers; `TIER_FACTS`, its three
  per-fact constants and its typedef; the parity test's per-tier loop arm read whole; the
  premium-gate census's matcher; the entitlement ladder's `enforcement` resolver map; the eager
  first-paint graph (269 members, by IMPORTING `vite.config.js`'s own export, reading no `dist`)
  **for all four EM-T9 members**; **the byte budget's constant, its pin and its dist gate, all three
  read at source**; the purchases lock by symbol; the mutation register's `ENFORCER_DIRS` and
  `NAME_PATTERN` read by IMPORTING `mutationCoverage.shared.mjs`; the lighting census's park-reason
  vocabulary; the negative-anchor walker's `BARE_NEGATIVE_RE` and its `tests/store` baseline; the
  governing test set by `git grep -l -F`. Receipts in `EM-D0b.evidence.md`; the partition's own
  measurements in `EM-D0.partition.md` and `02-FIRSTPAINT.md`.
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: TO BE STAMPED BY THE CHAIR;
  measured at this read tip with `shasum -a 256` as
  `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`).
  > **Interim compile rules:** `COMPILE-RULES.interim.md` (the chair, 2026-09-20) — the fifth
  > amendment's eleven rules, obeyed before they land. Where this packet and `EM-PREAMBLE.md`
  > (SHA-256 `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`) disagree, the
  > interim rules govern and this row is the record of it.

---

## §1 · Reconciled authority

| source | section + item | what it rules here |
|---|---|---|
| the chair | **ruling 3 on EM-D0**, 2026-09-20 ~22:15 (judgment 39) | *"THE TIER FIELD IS `TIER_GATE.premium.settlementEditor` (not `editMode`)."* One vocabulary, pinned by D0b |
| the chair | **judgment 41**, questions 5 and 6 | (5) for a near-hub file (`authSlice`, 23 test importers) the **changed-symbol** form is lawful for a purely ADDITIVE edit — §10's set is ruled, not proposed. (6) the partition's row giving D0b the `flow-b-auth-credits-ai` spec is **corrected**: that spec is owed when the owner OPENS the door, with the ladder row |
| the chair | **judgment 75**, 2026-09-21 | (1) **CURE A**: one `CREATE` is the authorized home of the newly written acceptance cases, and the lighting row is re-stated as the true counted delta. (2) freezing `TIER_GATE` is **DOCKETED** to the fifth amendment's agenda, not this packet. (3) the instrument-row distinction is **RATIFIED**. (4) **OPENING THE DOOR IS ONE OWNER-SIGNED ACT** |
| the chair | **judgment 74**, 2026-09-21 | **EM-D0b is train EM-T9's byte-arm holder**: one member per train carries the budget's `TEST` row, and the terminal's ONE real build proves the train's SUMMED first-paint price against it |
| design `DESIGN_EDIT_MODE_AND_DECREES.md` | **§20.4**, the first door | *"plain edits through the real op boundary … behind `TIER_GATE.premium.<field>`"*, and ⛔ *"VISIBILITY IS THE OWNER'S"* — which is why §6.2's second conjunct exists |
| the same | **§3**, the surfaces | *"Anonymous drafts never see it"* ⇒ `anon` is `false`, and `TIER_GATE.anon.maxSaves: 0` already means an anonymous visitor has no library row to press Edit on |
| the owner | **2026-09-16** | purchases stay LOCKED until launch ⇒ D6: this packet adds no purchase surface, no price, no CTA and no upgrade path, and the gate grants nothing buyable |
| charter `EDIT-MODE-TRAIN.md` | the **EM-D0** row | `M src/store/authSlice.js`, `M src/config/tierFacts.js`, the tier gate and its parity pin |
| interim rules | **1, 2, 4, 6, 8, 9, 10** | the deferred census row (`+0`); the byte budget as a `TEST` row with its stated bound and the dist arm OUT of `checks`; ⭐ **rule 4 — a new test file is `CREATE`, never `TEST`**; §7 SET-EQUAL to the capsule; sealed `checks` naming FILES and ending with the validator; every register figure a DELTA; the browser suite stated as NOT governing |

⛔ **THE ONE OWNER-SIGNED ACT (judgment 75(4)), RECORDED HERE AND IN §13 R5.** This packet ships the
gate **DARK**. Nothing in it, and nothing in any later packet, flips it. Opening the door is a
single owner-signed act with three inseparable parts: **delete the second conjunct** in
`canEditSettlement`; **add the `src/config/entitlementLadder.js` row**; **name the
`flow-b-auth-credits-ai` browser spec**. No one of the three may land without the other two.

⚠ **ONE RECONCILIATION.** EM-C4a version 1 §2.3 says *"`TIER_GATE.premium.editMode` does not exist;
**EM-D0 creates it**"* and its §2.6 lists `TIER_GATE.premium.editMode` as out of scope. Judgment 39
respells the field, so **the name EM-C4a predicted is not the name this packet mints**; EM-C4a
version 2 carries the corrected spelling in its own §2.6. Recorded so no reader reconciles the two
by re-introducing `editMode`.

---

## §2 · Outcome

### §2.1 What this packet delivers
THREE boolean fields, ONE parity constant, ONE typedef key, ONE predicate, ONE test line and ONE
new test file. **Nothing renders, nothing mounts, and no account in the world gains a capability**
— the predicate is `false` for anonymous, for free, and for every paying Cartographer. It is `true`
for staff and for the dev-only preview persona, and for nobody else, until the owner performs the
one signed act of §1.

**Definition of done:** the gate and the facts agree for all three tiers, asserted by the estate's
own contract test; the predicate is `false` for premium-without-staff, proved by name and by a
mutant; the preview persona reads `true` through the store's own writers; the premium-gate census
stays green and gains no row; the entitlement ladder gains no row and the reason is recorded with
its trigger; the eager first-paint set is `N → N` and the train's summed byte price is priced
against the owner-signed ceiling, never asserted; and the lighting tuple moves by exactly
`files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +1`.

### §2.2 ⛔⛔ THE DARK RULE, MEASURED — AND WHY IT IS TWO CONJUNCTS

The estate already owns the mechanism and it needs nothing new. `previewPersonaRole()`
(`authSlice.js:159-168`) supplies a **role** — read there and nowhere else, claimed to no server —
refused unless `isStaffRole(asked)` accepts it, live only under
`import.meta.env.DEV && import.meta.env.MODE !== 'test' && import.meta.env.VITE_PREVIEW_ROLE`, and
eliminated as dead code in a production build (`tests/build/previewPersonaAbsent.test.js` proves it
against the real `dist`). Its own header: *"⛔ IT CANNOT EXIST IN PRODUCTION."*
⇒ **ONE predicate covers both audiences the charter names, because the preview persona IS a staff
role.**

```js
/** ⛔ DARK BY DESIGN until the owner opens the door. See the note above the second conjunct. */
canEditSettlement: () => {
  const { tier, role } = get().auth;
  return TIER_GATE[tier]?.settlementEditor === true && isStaffRole(role);
},
```

⛔ **IT INVERTS THE HOUSE IDIOM, AND THAT IS THE POINT.** Every shipped predicate in this slice —
`canExport` (`:819`), `canUseMapChains` (`:833`), `canUseCustomContent` (`:840`), `maxAllowedTier`,
`minAllowedTier` — opens for staff **first** and then reads the gate:
`if (staffUnlocksPaidFeatures(get().auth.role)) return true;`. This one **closes** for everyone who
is not staff, and §6.2 carries the comment block that says why, so the next reader does not
"fix" it into the house shape.

⛔ **BOTH CONJUNCTS ARE LOAD-BEARING, AND THE PRE-PROOF MEASURED IT.** `resolveTier` (`:118-120`) is
`staffUnlocksPaidFeatures(role) ? 'premium' : (tier || 'free')`, so a staff account is **written
into the store as tier `'premium'`**. ⇒ **the tier gate ALONE would open the editor to every paying
Cartographer on the day it lands** — the paid-surface decision the delegation does not reach
(design §20.4: *"⛔ VISIBILITY IS THE OWNER'S"*). Executed at `e80a6a3f4` over 33 `(billing tier ×
role)` pairs against the **real** `staffEntitlements.js`: the predicate opens for
`'developer'` and `'admin'` only, at every billing tier, and reads **false** for anonymous, for
free, and for premium with role `'user'`, `'owner'`, `'staff'`, `'ADMIN'`, `42`, `{}` or
`['admin']`; **the one-conjunct mutant opens it for every premium account including role
`'user'`**. **The second conjunct is the ONE LINE the owner deletes**, and D2(d) makes that claim
measured rather than commented.

⛔ **THE SECOND CONJUNCT IS `isStaffRole`, NEVER `staffUnlocksPaidFeatures`.** Read at source
(`staffEntitlements.js:112-134`): `isStaffRole` is **IDENTITY** — *"NOT governed by the kill
switch"*, fail-closed on anything that is not one of the two exact role strings —
while `staffUnlocksPaidFeatures` is **ENTITLEMENT**, `STAFF_UNLOCK_ALL_PAID === true &&
isStaffRole(role)`. Using the second would mean that revoking the paid-feature unlock **also takes
the unbuilt editor away from the staff who are building it**, which is not what the kill switch is
for. D2(e) asserts the difference with the constant stubbed `false`.

### §2.3 ⛔ FIRST PAINT: +0 MODULES, AND THE TRAIN'S BYTES PRICED AGAINST THE OWNER'S OWN CEILING

Executed at `e80a6a3f4` by IMPORTING `vite.config.js`'s exported set (reads no `dist`, cannot skip):

```
EAGER_FIRST_PAINT_MODULES: 269 | anti-vacuity src/main.jsx present: true
  | authSlice.js already eager: true | tierFacts.js eager: false
```

⚠ **269, not the 268 `EM-PREAMBLE.md` §P2 row 11 and four landed packets still quote** — a fourth
independent lane reports the drift at this tip. Every figure here is a **DELTA**.

- **Modules: +0**, and by construction rather than by luck — this packet's only new file is a
  **test**, and it adds **no import statement at all** to either `src/` file. `isStaffRole` is
  already imported by `authSlice.js` (the file's own header: *"⛔ THE ROLE QUESTION HAS ONE HOME:
  `src/lib/staffEntitlements.js`"*).
- **`src/config/tierFacts.js` is NOT eager**, so its half of the edit costs first paint nothing.
- **Bytes in the EAGER file: PRICED, NEVER ASSERTED** (interim rule 2). The measuring arm is
  `tests/build/vendorPdfLazy.test.js:1020` — `it.skipIf(!requireDistRead)('entry static closure raw
  bytes stay under the first-paint budget (…)')` — which `statSync`s the built entry closure's
  files under `assetsDir`, so it measures **BUILT** bytes: comments and JSDoc cost **zero** and the
  price is three boolean object-literal fields plus one minified predicate. **Predicted: well under
  200 B.**

⭐⭐ **THE TRAIN'S SUMMED PRICE (judgment 74), MEASURED MEMBER BY MEMBER AT THIS TIP.** Because this
member holds EM-T9's byte arm, the arithmetic the terminal's one real build must satisfy is:

| EM-T9 member | its `src/` paths | eager at `e80a6a3f4`? | predicted first-paint rise |
|---|---|---|---:|
| EM-A1 | `src/domain/edit/types.js`, `src/domain/edit/fieldDeclarations.js` | **neither** | **+0 B** |
| EM-B1h | `src/domain/worldPulse/worldPulseFates.js`, `calamityKernel.js` | **neither** | **+0 B** |
| EM-D0a | `src/components/theme.js`, `src/components/nav/ArrowControl.jsx` | **both** | **≤ 120 B** |
| EM-D0b | `src/store/authSlice.js` (eager), `src/config/tierFacts.js` (not) | one | **< 200 B** |
| | | **TRAIN SUM** | **< 320 B** |

⭐ EM-B1h's figure was left to its own pre-proof by the chair; this lane **measured it**: neither of
its two `src/` paths is in the eager set, so its first-paint contribution is **zero**.

⛔ **THE STATED BOUND, READ AT SOURCE, AND THE HONEST ANSWER ABOUT HEADROOM.**
`const CLOSURE_BUDGET_BYTES = 1_048_000;` (`vendorPdfLazy.test.js:565`), pinned by its own arm at
`:1769` under the describe *"ARCH car 2 — the three first-paint budgets stay where the owner signed
them"*. **The ceiling is a constant this lane measured; the CLOSURE is not**, and the estate's
record disagrees with itself about it — EM-B1d v5 prices against one figure and EM-B1k2 against
another, a spread of about 8 KB, *"the difference between ~795 B and ~8.7 KB of headroom"*
(EM-B1c §7). ⇒ **THIS PACKET QUOTES NEITHER NUMBER and does not claim the rise fits.** It states:
the train's summed price is a **delta of under 320 B**; the base is **the chair's, summed from ONE
real build** at the train's terminal; and **if the summed closure exceeds `CLOSURE_BUDGET_BYTES` it
is a STOP FOR THE OWNER, never a re-mint** — *the cure is the PLACEMENT before it is the ceiling*
(`EM-PREAMBLE.md` §P2 row 11). ⭐ **The placement cure, if it is ever needed, is named and cheap:**
this member's predicate body can move to `src/lib/staffEntitlements.js` (already an eager import of
`authSlice.js`, so still +0 modules) leaving only the call — but it is **not** taken now, because
moving a gate away from the gate's home to save ~100 B would be the second-home mistake, and the
measurement does not yet say it is needed.

### §2.4 In scope / out of scope
**In:** the three gate fields; the three facts fields with their frozen constant and typedef key;
the one predicate; the one parity line; the one new test file with its eight arms; the byte price
stated against the budget's own bound for the whole train.
**Out:** ⛔ **any surface, route, component, `data-testid` or accessible name** (D0c, D0d, D0e) ·
⛔ **any `src/config/entitlementLadder.js` row** (§7's deferred trigger) · the glow token (D0a) ·
the dialog's mount (D0e) · the transient `editorMode` key (EM-C4a) · the shipped Cartographer
`editMode` boolean, which is **left exactly alone** · ⛔ **freezing `TIER_GATE`** (§5's measured
observation, DOCKETED by judgment 75(2)) · any purchase surface, price, CTA or upgrade path (D6) ·
⛔ **any `scripts/mutation-coverage-manifest.json` row** (§7: not owed, and a voluntary row would be
unlawful).

---

## §3 · Hard scope budget

| limit | this packet | cap |
|---|---|---|
| behaviour families | **1** — one entitlement, gated once | 1 |
| new persisted record families | **0** — `TIER_GATE` and `TIER_FACTS` are module constants | ≤1 |
| feature flags | **0** — a tier field is data, not a flag | ≤1 |
| user-facing surfaces | **0 — nothing renders and nothing mounts** | ≤1 |
| direct production consumers | **0** — D0e is the first, not yet written | ≤2 |
| new logic-bearing production leaves | **0** — the one new file is a TEST | ≤2 |
| existing logic-bearing production files modified | **2** | ≤3 |
| handwritten files total | **5** | ≤12 |
| new/changed effective production lines | **≈14** (`authSlice.js` +8 incl. the comment; `tierFacts.js` +6 incl. the typedef key) | ≤400 |
| each new production leaf | n/a — none | ≤250 each |
| the one new TEST leaf | `tests/store/settlementEditorGate.test.js`, **≈150 effective lines** across 8 arms | ≤250 |
| shared / hot-file delta | `src/store/authSlice.js` **+8** | ≤15 |
| named acceptance cases | **6** | ≤8 |

Overrides approved before dispatch: **NONE — none is needed.**

**Hot files:** `src/store/authSlice.js` has **23** test importers and `src/config/tierFacts.js`
**13**. Both edits are **purely additive** — a key appended to three object literals, a constant
and a typedef key appended beside their siblings, one predicate appended inside the object
`createAuthSlice` returns — so no existing export's declaration, value or order moves and the
headroom question does not arise. The consequence is a `checks`-scoping rule (§10), not a budget
one, and judgment 41 q5 ruled the changed-symbol form lawful for exactly this shape.

---

## §4 · Sealed dispatch and preflight

A dry read of `scripts/implementation-session.mjs`'s checks at the tip:
- **branch name** — the chair's; the packet asserts nothing.
- **ancestry** — the chair sets `Verified base` at promotion; `__BASE__` is replaced there and
  nowhere else. Measured: the compile base `bdbf7c89c` **is** an ancestor of `e80a6a3f4`
  (`merge-base --is-ancestor`, exit 0).
- **CREATE targets absent** — ⭐ **exactly one, and it is absent**:
  `tests/store/settlementEditorGate.test.js`, `git ls-tree -r --name-only HEAD` → **0 hits**. It is
  a TEST file, so the packet still depends on no sibling's CREATE.
- **non-CREATE rows must EXIST** — `scripts/implementation-packets.mjs:834` refuses any row whose
  `action !== 'CREATE'` when `fileExists` is false. All four non-CREATE rows sit at paths present
  at the base, so the packet validates with **no provider stand-in of any kind** (§12's executed
  control).
- **substrate unchanged since the verified base** — of the thirteen `requiredSymbols`, this packet
  writes seven (at `authSlice.js`, `tierFacts.js`, the contract test and `vendorPdfLazy.test.js`),
  and every required text survives its own edits verbatim (§5's simulation).
- **git-clean** — the lane wrote nothing in any tree; `git status --short` in `read-tip-em-t9` was
  EMPTY at the start and at the end.
⚠ **SEALED DISPATCH WANTS THE WORKTREE ON THE VERIFIED BRANCH.** One build lane holds the
integration branch in its own worktree; the chair's worktree stays detached at the tip. EM-B3d's
probe (2026-09-21) proved the capsule is keyed to the **worktree's own git dir**, so a sealed
dispatch works on a packet's own lane branch and four members may hold capsules at once.

---

## §5 · Verified tree contract

Every row proved at `e80a6a3f4` by `git grep -c -F` = **1** (receipts in `EM-D0b.evidence.md`
§D1–§D13).

| what | path | symbol (verbatim) | why it must not move |
|---|---|---|---|
| the gate's home | `src/store/authSlice.js` | `export const TIER_GATE = {` | the three new fields are appended to its three rows. ⚠ **MEASURED AND NOT FIXED: it is a PLAIN object literal, NOT `Object.freeze`d**, unlike `TIER_FACTS`. DOCKETED by judgment 75(2); §13 R1 |
| the idiom this inverts | `src/store/authSlice.js` | `  canUseCustomContent: () => {` | the new predicate is written beside it and deliberately does NOT copy its staff-first shape (§2.2) |
| ⭐ the slice factory the new test calls | `src/store/authSlice.js` | `export const createAuthSlice = (set, get) => ({` | `tests/store/settlementEditorGate.test.js` constructs the slice through it (the shape `staffUnlockEntitlements.test.js:46` uses). **Survives verbatim**: the predicate is appended INSIDE the returned literal |
| ⛔ the DARK mechanism | `src/store/authSlice.js` | `function previewPersonaRole() {` | the reason ONE predicate covers both audiences; its DEV-only body is what keeps the door out of production |
| ⛔ the second conjunct | `src/lib/staffEntitlements.js` | `export function isStaffRole(role) {` | IDENTITY, fail-closed, **not** governed by the kill switch |
| ⛔ why the conjunct exists | `src/lib/staffEntitlements.js` | `export function staffUnlocksPaidFeatures(role) {` | ENTITLEMENT, kill-switched; `resolveTier` uses it to write staff in as `premium`, which is what the tier gate alone would open |
| ⭐ the kill switch D2(e) stubs | `src/lib/staffEntitlements.js` | `export const STAFF_UNLOCK_ALL_PAID = true;` | arm 5 re-imports the module with it `false` and asserts a staff role still reads **true** |
| ⭐ the two role strings | `src/lib/staffEntitlements.js` | `export const STAFF_ROLES = Object.freeze(['developer', 'admin']);` | the whole of staff identity; D2 and D3 assert by name that nothing else widens it |
| the parity pin's other side | `src/config/tierFacts.js` | `export const TIER_FACTS = Object.freeze({` | the new key joins the frozen facts table |
| the constant's exact shape | `src/config/tierFacts.js` | `const CUSTOM_CONTENT = Object.freeze({ anon: false, free: false, premium: true });` | `SETTLEMENT_EDITOR` is written to **this** shape and no other |
| the pin's neighbour | `tests/config/tierFacts.contract.test.js` | `      expect(facts.customContent).toBe(gate.customContent);` | the new line copies it exactly, one key over, inside the same existing arm |
| ⛔ **the owner-signed ceiling** | `tests/build/vendorPdfLazy.test.js` | `expect(CLOSURE_BUDGET_BYTES, 'the raw first-paint budget moved').toBe(1_048_000);` | the bound §7 prices the **whole train** against. ⛔ **The packet never moves it**; a summed closure over it is the OWNER's stop |
| the eager set | `vite.config.js` | `export const EAGER_FIRST_PAINT_MODULES` | the membership probe imports it and reads no `dist` |

**`retiredSymbols`: EMPTY**, and proved row by row by the post-edit simulation (pre-proof step 10).
**Every edit to an existing file is ADDITIVE**: `TIER_GATE = {` keeps its text because a key is
appended inside each of the three rows below it; `canUseCustomContent: () => {` keeps its text
because the new predicate goes after its closing brace; `createAuthSlice = (set, get) => ({` keeps
its text because the predicate is appended *inside* the object it returns; `TIER_FACTS =
Object.freeze({` and `const CUSTOM_CONTENT = …` keep theirs because the new constant and key are
appended beside them; the contract test's line keeps its text because the new `expect` goes on the
line after it; and the budget pin, `previewPersonaRole`, the four `staffEntitlements` exports and
`vite.config.js` sit at paths this packet does not write at all. **No other LANDED or waiting packet
carries a row for any of these thirteen pairs.**

**Forbidden alternatives:** ⛔ no second spelling of the entitlement — `editMode` on either table is
D1's red · ⛔ no raw `=== 'premium'` comparison anywhere (the premium-gate census convicts it) · ⛔
no `entitlementLadder.js` row (§7) · ⛔ no `scripts/mutation-coverage-manifest.json` row (§7: it
would red the no-stale-entries arm) · ⛔ **no rename of the CREATE target** (§6.5: a token in the
basename creates a register obligation) · ⛔ no component, route, `data-testid` or accessible name ·
⛔ no import added to either `src/` file · ⛔ no change to the shipped Cartographer `editMode`
boolean, to `purchasesOpen()` or to `AvailableAtLaunchPill` · no files outside the manifest.

---

## §6 · Exact contracts

### §6.1 `src/config/tierFacts.js` — the facts side

```js
// The settlement editor is a premium (Cartographer) capability — never free, never anonymous.
// Pinned to TIER_GATE.{tier}.settlementEditor by the contract test.
// ⛔ THE GATE IS NOT THE WHOLE ANSWER: the enforcement predicate carries a second, staff-only
// conjunct while the door is DARK (authSlice.js#canEditSettlement). These facts describe the
// TIER's entitlement, which is what the ladder and the copy surfaces will read when it opens.
const SETTLEMENT_EDITOR = Object.freeze({ anon: false, free: false, premium: true });
```
and the typedef (`:190`) gains `settlementEditor: boolean`, and each of the three `TIER_FACTS` rows
gains `settlementEditor: SETTLEMENT_EDITOR.<tier>,` beside its `customContent`.

⛔ **THE VALUE IS A CONSTANT, NEVER A COPY OF THE GATE.** The two halves agree because the contract
test asserts it — exactly how `customContent` and `preGenOptions` already work. A row that read
`TIER_GATE[...]` here would make the parity test vacuous.

### §6.2 `src/store/authSlice.js` — the gate and the one predicate

`TIER_GATE`'s three rows each gain `settlementEditor: <false|false|true>,` beside `customContent`.
Then, immediately after `canUseCustomContent`'s closing brace:

```js
  /**
   * ⛔⛔ DARK BY DESIGN. Whether this account may open the settlement editor (design §20.4).
   *
   * THIS PREDICATE INVERTS THE SHAPE OF EVERY OTHER GATE IN THIS SLICE ON PURPOSE. The others
   * open for staff first and then read the tier; this one reads the tier AND requires staff,
   * because `resolveTier` writes a staff account in as tier 'premium', so the tier gate alone
   * would open the editor to every paying Cartographer the day it lands — and design §20.4 says
   * "⛔ VISIBILITY IS THE OWNER'S".
   *
   * ⭐ `&& isStaffRole(role)` IS THE ONE LINE TO DELETE WHEN THE OWNER OPENS THE DOOR — and that
   * deletion is ONE OWNER-SIGNED ACT with two inseparable companions: the entitlementLadder row
   * and the flow-b-auth-credits-ai browser spec. Deleting it makes this predicate the house shape
   * and nothing else in this file has to change. Until then the audience is staff and the DEV-only
   * preview persona, which supplies a role `isStaffRole` accepts and which cannot exist in a
   * production build.
   *
   * ⛔ `isStaffRole`, NOT `staffUnlocksPaidFeatures`: the second is governed by the
   * STAFF_UNLOCK_ALL_PAID kill switch, and revoking the paid-feature unlock must not take the
   * unbuilt editor away from the people building it.
   */
  canEditSettlement: () => {
    const { tier, role } = get().auth;
    return TIER_GATE[tier]?.settlementEditor === true && isStaffRole(role);
  },
```

⛔ **EXACT, WITH NO DISCRETION.** `?.` and `=== true` are the house's own shape (`canExport`,
`canUseMapChains`, `canUseCustomContent` all spell `TIER_GATE[tier]?.<key> === true`), which is why
an unknown tier reads **false** rather than throwing. ⛔ The predicate reads `get().auth` once and
calls nothing else; it draws no random number, reads no clock, touches no storage.

### §6.3 `tests/config/tierFacts.contract.test.js` — the parity line

Inside the **existing** per-tier arm (the `for (const tier of ['anon','free','premium'])` loop at
`:33`, whose one `it` opens at `:34`), after the `customContent` line at `:43`:

```js
      // The settlement editor is premium-only on the tier axis; mirror the gate exactly.
      // (The DARK rule's staff conjunct lives in authSlice#canEditSettlement, not here: this
      // arm pins the TIER's entitlement, which is what the ladder and the copy will read.)
      expect(facts.settlementEditor).toBe(gate.settlementEditor);
```

⭐ **NO NEW `it`, `describe` OR FILE IN THIS FILE** — which is why **this row's** lighting
contribution is zero in all five figures. The packet's whole lighting delta comes from §6.5's
CREATE row and from nowhere else. ⛔ Unchanged from version 1 by the chair's ruling: still one
effective line, still add no `it`, no `describe`, no loop, widen nothing.

### §6.4 Determinism, lifecycle, receipts
- **Hash / fork key:** NONE. No random number, no clock, no locale, no id minted.
- **Ordering:** n/a — three object keys appended in the house's own order, beside `customContent`.
- **Absence vs empty vs null:** an unknown or absent `tier` reads `undefined` through `?.` and the
  predicate returns **false** (fail-closed); an absent, null, object, array, number or
  truthy-non-string `role` is refused by `isStaffRole`'s own fail-closed body — all measured in the
  pre-proof's 33-pair truth table.
- **Lifecycle, every path:** *create / read / persist / regenerate / undo / import / migrate* — ⛔
  **NONE OF THEM.** `TIER_GATE` and `TIER_FACTS` are module constants; nothing is persisted, no
  save shape moves, and no migration is owed. The stored `auth.tier` and `auth.role` this reads are
  already written by the shipped auth writers.
- **Receipts:** none. No surface, no command, no chronicle line.
- **Golden posture: UNCHANGED.** `tests/property/generatorGoldenMaster.test.js` and
  `tests/property/dossierProseManifest.test.js` do not move; generation is not touched.
- **Flag / dormancy:** no flag; **zero consumers** — D0e is the first, and it is not yet written.

### §6.5 ⭐⭐ `tests/store/settlementEditorGate.test.js` — THE NEW FILE (CURE A, judgment 75(1))

**The authorized home of every newly written acceptance case.** Version 1 sealed six cases and gave
five of them no file to live in; this is that file.

⛔⛔ **THE FILENAME IS LOAD-BEARING AND MAY NOT BE CHANGED.** Measured at the tip by importing
`tests/lint/mutationCoverage.shared.mjs`:

- `ENFORCER_DIRS` is **eight** entries — `tests/lint`, `tests/design`, `tests/docs`, `tests/data`,
  `tests/copy`, `tests/security`, `tests/edgeFunctions`, `tests/generators` — and **`tests/store`
  is not one of them**.
- Outside those dirs a file is enumerated only if its **basename** matches
  `NAME_PATTERN = /(census|scan|baseline|ratchet|walker|killlist|parity|coverage|governance|freshness|integrity|exhaustiveness|roundtrip|golden|contract|pin)/i`,
  and `settlementEditorGate` matches **none of the sixteen tokens**.

⇒ the file is **NOT enumerated**, so **no `scripts/mutation-coverage-manifest.json` row is owed** —
and **a voluntary row would be UNLAWFUL**, because `mutationCoverageManifest.test.js`'s arm
*"no stale entries: every manifest invariant key is an enumerated, existing file"* refuses a key the
enumerator does not pick. `uncoveredBaseline` is **186** and equals the live uncovered count, so
`uncovered` is unlawful in any case. ⛔ **Renaming this file to anything containing one of those
sixteen tokens — `…contract.test.js`, `…Parity…`, `…Scan…`, `…Census…` — silently creates a register
obligation and reds the totality arm.** This is also what keeps EM-D0b off the one path EM-A1 and
EM-B1h both hold row keys on, and so keeps train EM-T9 path-disjoint.

**THE EIGHT ARMS — literal titles, straight-line, under ONE literal `describe`:**

| # | arm | case |
|---|---|---|
| 1 | the gate and the facts carry `settlementEditor`, and neither table spells `editMode` | D1 |
| 2 | a premium account without a staff role cannot edit — the paid-surface line | D2(a) |
| 3 | a staff role can edit, at every billing tier | D2(b) |
| 4 | anonymous, free, and every non-staff role read false | D2(c) |
| 5 | the conjunct is identity, not entitlement: revoking the paid unlock leaves staff editing | D2(e) |
| 6 | the preview persona opens the door and its absence closes it | D3 |
| 7 | the predicate spells no raw premium comparison | D4 |
| 8 | the changed files add no purchase surface | D6 |

⛔⛔ **WRITE IT SO THE CENSUS CANNOT PARK IT.** The promised `credited +1` lands as `parked +1` if
any of the following is violated, and a parked file's titles count **nowhere**:

- **ONE literal-titled `describe`** at module top level; **EIGHT straight-line literal-titled
  `it`s** written directly inside it.
- **NO** nesting, **NO** loop, **NO** `.each`, **NO** conditional registration, **NO**
  `test.extend`, **NO** hook context parameter, **NO** computed or template-literal title. The
  census's own park reasons for these are `SUITE_NOT_STRAIGHT_LINE:describe`,
  `TEST_UNREGISTERED:it`, `SUITE_TABLE_UNPROVEN`, `SUITE_ARG_FORM:describe`,
  `SUITE_CONTEXT_PARAM:describe`, `HOOK_CONTEXT_PARAM` and `TEST_UNCLASSIFIED:test.extend`.
- Import `describe`, `it`, `expect` from `'vitest'` and **bind each of those three words EXACTLY
  ONCE in the module** — ⛔ **never name a variable, parameter or destructured field `it`, `test`
  or `describe`.** An arrow parameter named `it` is how EM-B1e parked on `OPENER_UNRESOLVED:it`,
  and 26 of the estate's 384 parked files park for that reason alone.
- **Never mention a suite opener outside a classified call** — `SUITE_REF` parks the file whole.
- A file that **fails to parse parks whole**. Fail-closed at the parser door.

⛔ **THE NEGATIVE-ANCHOR WALKER GOVERNS `tests/store`.** Its `BARE_NEGATIVE_RE` is
`/not\.(?:toContain|toMatch|toHaveProperty)\(/g`. Assert absence with
`expect(Object.hasOwn(TIER_GATE.premium, 'editMode')).toBe(false)` and
`expect(src.includes('purchasesOpen')).toBe(false)` — **never** with a bare `not.toContain`,
`not.toMatch` or `not.toHaveProperty` — so the file needs no `// anchored:` marker and no baseline
row. (`not.toBe` and `not.toEqual` are out of that walker's scope.)

**Shape of the store arms (2–6):** construct the slice exactly as
`tests/store/staffUnlockEntitlements.test.js:46` does — `state = createAuthSlice(set, get)` — and
read `state.canEditSettlement()`. Arm 5 re-imports the module with `STAFF_UNLOCK_ALL_PAID` stubbed
`false` (the idiom at `staffUnlockEntitlements.test.js:270`). Arm 6 drives the persona through the
store's own auth writers (`resolveRole` → `previewPersonaRole`), **never** by calling
`previewPersonaRole` directly. Arms 7 and 8 are source scans: `readFileSync` the two modified `src/`
files and assert on their text.

⛔ **NO SEED LOOP.** A `tests/` CREATE never writes a bare `for (const seed of …) { expect(…) }`;
`seedLoopTotality` convicts it. Arms 3 and 4 iterate nothing — each writes its cases out literally,
which is also what keeps the file straight-line for the census.

---

## §7 · Exact change manifest

Generated from `EM-D0b.manifest.json` and proved SET-EQUAL to it (interim rule 6; the executed line
is in `EM-D0b.setequal.txt` and `FIRST-DOOR-CHAIN.compile.report.md` §A).

| Action | File | Symbol / region | Max delta | Coding instruction |
|---|---|---|---:|---|
| `MODIFY` | `src/store/authSlice.js` | `TIER_GATE`, `canEditSettlement` | **+8 eff** | §6.2. Append one key to each of the three `TIER_GATE` rows beside `customContent`; append the predicate after `canUseCustomContent`'s closing brace **with its comment block**. ⛔ Additive only; add no import; do not touch the shipped `editMode` boolean. |
| `MODIFY` | `src/config/tierFacts.js` | `SETTLEMENT_EDITOR`, `TIER_FACTS`, the `TierFacts` typedef | **+6 eff** | §6.1. ⛔ The value comes from the new frozen constant, never from `TIER_GATE`. |
| `CREATE` | `tests/store/settlementEditorGate.test.js` | the eight arms | **≈150 eff** | §6.5. **8 literal-titled `it`s in ONE literal `describe`**, straight-line, no loop, no `.each`, no nesting. ⛔ **Do not rename the file** — a `NAME_PATTERN` token in the basename creates a mutation-register obligation. ⛔ Assert absence with `toBe(false)`, never a bare `not.toContain`/`toMatch`/`toHaveProperty`. |
| `TEST` | `tests/config/tierFacts.contract.test.js` | the per-tier parity arm | **+1 line** | §6.3, inside the EXISTING arm. ⛔ Add no `it`, no `describe`, no loop; widen nothing. |
| `TEST` | `tests/build/vendorPdfLazy.test.js` | `CLOSURE_BUDGET_BYTES` | **NO EDIT** | ⛔ Interim rule 2's row, RATIFIED by judgment 75(3). **The stated bound is `1_048_000`** (`:565`, pinned at `:1769`). ⭐ **By judgment 74 this member is EM-T9's SOLE byte-arm holder**: the terminal's ONE real build proves the **train's SUMMED** first-paint price — EM-A1 **+0 B** · EM-B1h **+0 B** · EM-D0a **≤120 B** · EM-D0b **<200 B** ⇒ **sum < 320 B** — against this bound. The row makes no edit. The ceiling moves only if the OWNER moves it. ⚠ EM-B2a4 declares the same row in a different train. |

Generated artifacts: **NONE**.

### The deferred rows — PREDICTIONS, named in no `checks` and in no `changeManifest`

> `docs/content/wiring-census.json` — **+0** (interim rule 1). This packet CREATEs **no `.js` under
> `src/generators/**` or `src/domain/**`** — its one CREATE is a test file under `tests/store/`.
> `stamp.producerIndexFiles`, `stamp.files`, `stamp.candidateLeaves` and `totals.*` are all
> **UNMOVED**. Those two roots are the ones measured against. ⛔ Regenerated whole at the train's
> terminal BY THE CHAIR; the path appears nowhere here.

> `tests/lint/.lighting-census-baseline.json` — ⭐⭐ **RE-STATED IN VERSION 2; NO LONGER ZERO.**
> The delta, in the register's own order, **counted from §6.5's eight authorized arms**:
> **files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +1.** The `+8` and the `+1` are the
> CREATE'd file's eight literal-titled `it`s under its one literal `describe`; the
> `tests/config/tierFacts.contract.test.js` row contributes **zero to all five**. ⛔⛔ **The
> `credited +1` is CONDITIONAL on §6.5's pinned shape** — one top-level literal `describe`, eight
> straight-line literal `it`s, no loop, no `.each`, no conditional registration, no nesting, and
> `it`/`test`/`describe` each bound exactly once. **If the build lane measures any other tuple, that
> is a STOP, not a refreeze** — in particular `parked +1 · credited +0 · titles +0` means the file
> parked and §6.5's shape rules were broken. ⛔ The row names the BASELINE JSON, never the walker;
> the refreeze is the chair's at the terminal, and this member never sets
> `LIGHTING_CENSUS_REFREEZE`.

> `src/config/entitlementLadder.js` — ⛔ **NO ROW, AND THE TRIGGER IS NAMED.** Measured at the tip:
> `tests/config/entitlementLadder.enforcement.test.js` keys its `RESOLVERS` map by `enforcement`
> **marker string** and iterates the ladder's **own** `ROWS` (`:108-112`, `:151`) — it does **not**
> enumerate `TIER_GATE`'s keys, so a new gate key demands no ladder row and the walker stays green
> untouched. A row for a surface **nobody may see** would advertise an unopened capability **and**
> would fail its own resolver on the staff conjunct. **The row is OWED AT THE MOMENT THE OWNER OPENS
> THE DOOR** — one of the three inseparable parts of §1's single owner-signed act.
> *(Deliberately deferred — documented, not a bug to re-find.)*

> `scripts/mutation-coverage-manifest.json` — ⛔ **NOT OWED, AND A VOLUNTARY ROW WOULD BE UNLAWFUL.**
> Measured by importing `tests/lint/mutationCoverage.shared.mjs` at the tip: `tests/store` is not one
> of the eight `ENFORCER_DIRS`, and `settlementEditorGate` matches none of `NAME_PATTERN`'s sixteen
> tokens, so `enumerateInvariants` does not pick the file (**716** picked at `e80a6a3f4`, 15 of them
> under `tests/store`, every one by token). Adding a key anyway reds the *no stale entries* arm.
> `uncoveredBaseline` is **186** and equals the live uncovered count, so `uncovered` is unlawful
> regardless. This keeps the member off the one path EM-A1 and EM-B1h both hold row keys on.

---

## §8 · Ordered coding sequence

1. **K-GATE, BEFORE THE FIRST EDIT.** Re-run the membership probe and quote its line (`authSlice.js`
   eager, `tierFacts.js` not); re-run `git grep -n -E "settlementEditor|canEditSettlement" -- src
   tests e2e` and confirm **zero** hits; confirm `CLOSURE_BUDGET_BYTES` is still `1_048_000`;
   confirm `tests/store/settlementEditorGate.test.js` is ABSENT.
   **Any miss is a STOP, not a workaround.**
2. Read `src/store/authSlice.js`'s `TIER_GATE` block and its predicate block **whole**, and
   `src/config/tierFacts.js` **whole**, before the first edit.
3. **Write `tests/store/settlementEditorGate.test.js`'s arm 2 and D2's mutant FIRST** — the
   one-conjunct planted body — so the rest of the file is written against a detector already proved
   to convict.
4. Edit `src/config/tierFacts.js` (§6.1): the constant, the typedef key, the three rows.
5. Edit `src/store/authSlice.js` (§6.2): the three gate keys, then the predicate with its comment.
6. Add the one parity line (§6.3) and run `tests/config/tierFacts.contract.test.js` **now** — it is
   the first thing that should go green, and if it does not, the two halves disagree.
7. **Write the remaining seven arms of `tests/store/settlementEditorGate.test.js`** (§6.5): arm 1
   (D1's values and vocabulary), arms 3, 4 and 5 (D2 b, c, e), arm 6 (D3), arm 7 (D4's scan) and
   arm 8 (D6's scan). ⛔ Obey §6.5's park-proof shape rules exactly; **do not rename the file**.
8. Run the sealed `checks` in §10's order, the validator LAST.
9. ⭐ **Run the lighting walker ALONE as an instrument and record its five figures** (§7 predicts
   `files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +1`), then hand the byte price to
   the chair for the terminal's one build of the whole train.

---

## §9 · Acceptance matrix

| # | claim | home | how it is proved |
|---|---|---|---|
| **D1** | **Parity for all three tiers, and ONE vocabulary** | `tierFacts.contract.test.js` (the +1 line) **+** `settlementEditorGate.test.js` **arm 1** | the existing per-tier arm's new line asserts `TIER_FACTS[t].settlementEditor === TIER_GATE[t].settlementEditor` for all three; arm 1 asserts the values by name — `anon` **false**, `free` **false**, `premium` **true** — and asserts `TIER_GATE.premium.editMode` and `TIER_FACTS.premium.editMode` **ABSENT BY NAME** via `Object.hasOwn(...)` → `toBe(false)`, so the collision judgment 39 removed cannot be re-introduced |
| **D2** | ⛔⛔ **THE DARK RULE — the arm that matters most** | `settlementEditorGate.test.js` **arms 2, 3, 4, 5**; (d) is a build-lane act | (a) arm 2 — **premium WITHOUT a staff role ⇒ FALSE**, asserted by name, the paid-surface line; (b) arm 3 — a staff role ⇒ **TRUE** at every billing tier (the paired positive control); (c) arm 4 — `anon` and `free` ⇒ false, and every non-staff role plus `undefined`, `null`, a number, an object and an array ⇒ false; (d) ⛔ **THE MUTANT** — the planted **one-conjunct** body must make arm 2 RED, which makes *"the owner deletes one line"* measured rather than commented (the pre-proof measured its blast radius: it opens the door to every premium account including role `'user'`); (e) arm 5 — the conjunct is `isStaffRole`, **not** `staffUnlocksPaidFeatures`: with `STAFF_UNLOCK_ALL_PAID` stubbed **false**, a staff role still reads **true** |
| **D3** | **The preview persona sees the door; production cannot** | `settlementEditorGate.test.js` **arm 6** | the persona's role arrives through the store's own auth writers (`resolveRole` → `previewPersonaRole`), so `canEditSettlement()` is **true** under a persona and **false** without one — asserted through the writers, never by calling `previewPersonaRole` directly. ⛔ The production absence is **not re-proved here**: `tests/build/previewPersonaAbsent.test.js` already proves it against the real `dist` and `tests/store/previewPersona.test.js` proves the predicate inert with DEV false. This packet adds no second proof of an existing one, and says so |
| **D4** | ⛔ **The census stays green; this gate joins no row; the ladder gains none** | `checks` inv. 3 (the walker) **+** `settlementEditorGate.test.js` **arm 7** | `tests/lint/premiumGateSingleSource.test.js` runs green; arm 7 reads the predicate's own source and asserts its matcher — `/(?:===\|!==)\s*['"](?:premium\|founder)['"]/` (`:80`) — does not fire, so the predicate joins **no** census row. ⛔ And **no `entitlementLadder.js` row is added**: measured, that test keys `RESOLVERS` by marker string and iterates the ladder's own rows, never `TIER_GATE`'s keys |
| **D5** | ⛔ **FIRST PAINT: +0 modules, as a DELTA; the train's bytes PRICED** | **`checks` invocation 9 — the membership probe itself.** It is an executed sealed check, not a test title, and needs no arm in the new file | the probe IMPORTS the exported set, reads no `dist` and cannot skip: size N → N; `authSlice.js` a member **before and after**; `tierFacts.js` a member of **neither**; **no** new module enters (the only new file is a test); anti-vacuity — the set is non-empty and contains `src/main.jsx`. ⛔ **The BYTE delta is not asserted here**: §7 and §2.3 price the **whole train's sum** against `CLOSURE_BUDGET_BYTES` (`1_048_000`) and the chair sums ONE real build |
| **D6** | **The purchases lock is untouched** (the owner, 2026-09-16) | `settlementEditorGate.test.js` **arm 8** | measured: the lock is `src/lib/launchGate.js#purchasesOpen()` (`:31`), consumed at `src/components/primitives/LockedDestination.jsx:27` and worn as `AvailableAtLaunchPill`. Arm 8 reads the two modified `src/` sources and asserts neither names `purchasesOpen`, `AvailableAtLaunchPill` or `LockedDestination`; the pre-proof measured **0 hits in all three changed files** and measured that `authSlice.js` does not import `launchGate.js` at all — and the new gate grants nothing buyable, because it is **false for every real account including premium** |

**6 of ≤8.** Every case has exactly one declared home, and every home is a `changeManifest` row or a
sealed check.

---

## §10 · Verification commands

```sh
# THE SEALED CHECKS (the capsule's `checks`, in this order)
npx vitest run --pool=threads --maxWorkers=2 tests/config/tierFacts.contract.test.js \
                                             tests/config/entitlementLadder.enforcement.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/store/settlementEditorGate.test.js \
  tests/store/staffUnlockEntitlements.test.js tests/store/isTierAllowedFailClosed.test.js \
  tests/store/previewPersona.test.js tests/store/lifecycleRoundTrip.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/lint/premiumGateSingleSource.test.js
# ⭐ the TWO walkers that govern a NEW FILE in tests/store, named BY FILE (never the directory):
npx vitest run --pool=threads --maxWorkers=2 tests/lint/negativeAssertionAnchor.walker.test.js \
                                             tests/lint/mutationCoverageManifest.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/components/mapChainsTierGate.test.jsx \
  tests/components/foundingWorldsForkRefusal.test.jsx tests/domain/customContentTierGates.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/ui/anonPreGenLocked.test.jsx \
  tests/ui/homeHeroAnonGauge.test.jsx tests/ui/libraryLivingSurface.test.jsx
npx vitest run --pool=threads --maxWorkers=2 tests/copy/landingClaimsParity.test.js \
                                             tests/docs/docCounts.test.js
npx eslint src/store/authSlice.js src/config/tierFacts.js \
           tests/config/tierFacts.contract.test.js tests/store/settlementEditorGate.test.js

# the membership probe — it IMPORTS the exported set, reads no dist, and therefore can never skip
node -e "const{relative}=require('node:path');const{pathToFileURL}=require('node:url');import(pathToFileURL('vite.config.js').href).then(m=>{const S=new Set([...m.EAGER_FIRST_PAINT_MODULES].map(a=>relative(process.cwd(),String(a))));const want='src/store/authSlice.js';const free='src/config/tierFacts.js';console.log('EAGER_FIRST_PAINT_MODULES:',S.size,'| anti-vacuity src/main.jsx present:',S.has('src/main.jsx'),'| authSlice.js already eager:',S.has(want),'| tierFacts.js eager:',S.has(free));if(!S.has(want)||S.has(free)||!S.has('src/main.jsx')||S.size<200)process.exit(1);})"

npm run typecheck:ratchet
npm run typecheck:domain:strict

# the seal, LAST
node scripts/implementation-packets.mjs validate
```

**How the sealed set was computed (interim rule 8).** TOOL-26's `scripts/governing-tests.mjs` has
not landed at the integration tip, so the interim form applies, at `e80a6a3f4`: the **CHANGED
SYMBOL** grep gives `TIER_GATE` → **14** files and `TIER_FACTS` → **1**; the **CHANGED FILE** grep
gives `authSlice` → **23** and `tierFacts` → **13**. This packet takes the **changed-symbol** form,
and **judgment 41 question 5 RULED that form lawful for a near-hub file on a purely ADDITIVE edit**
— so §13 R2 records a ruling, not a question. ⭐ **Version 2 adds** the CREATE'd file to invocation
2, the **two walkers that govern a new `tests/store` file** as invocation 4 (named BY FILE), and the
CREATE'd file to the eslint invocation. ⛔ Never the `tests/lint` directory; ⛔ never the lighting
walker; ⭐ the validator last.

```sh
# ⛔ THE BUILD LANE'S INSTRUMENTS — NEVER SEALED CHECKS
#  (a) the full importer sets of the two changed files, if the chair ever prefers the literal form:
#      git grep -l -F authSlice -- tests   (23)   ·   git grep -l -F tierFacts -- tests   (13)
#  (b) the EXCLUDED tests/lint directory run, which MUST EXIT 0
npx vitest run --pool=threads --maxWorkers=2 tests/lint --exclude=tests/lint/sovereigntyLightingContract.walker.test.js
#  (c) ⭐ the lighting walker ALONE. This member predicts files +1 · parked +0 · credited +1 ·
#      titles +8 · suiteTitles +1. ANY OTHER TUPLE IS A STOP, not a refreeze — and
#      parked +1 · credited +0 · titles +0 means §6.5's shape rules were broken.
npx vitest run --pool=threads --maxWorkers=2 tests/lint/sovereigntyLightingContract.walker.test.js
#  ⛔ DO NOT set LIGHTING_CENSUS_REFREEZE. The refreeze is the chair's, at the terminal.
#  (d) ⛔ THE BYTE ARM, which is a TEST row and NOT a sealed check (interim rule 2): it reads dist
#      and is gated on VERIFY_DIST=1, so in `checks` it would SKIP and read as a pass.
#      Run by the CHAIR, post-build, at the train's terminal, ONCE FOR THE WHOLE TRAIN (judgment 74):
#         VERIFY_DIST=1 npx vitest run tests/build/vendorPdfLazy.test.js
#      Stated bound: CLOSURE_BUDGET_BYTES = 1_048_000 (vendorPdfLazy.test.js:565, pinned at :1769).
#      The SUM it must cover: EM-A1 +0 B · EM-B1h +0 B · EM-D0a <=120 B · EM-D0b <200 B => < 320 B.
#      A summed closure over the ceiling is a STOP FOR THE OWNER; the cure is the PLACEMENT (§2.3),
#      and this member re-mints nothing.
```

**⛔ THE BROWSER SUITE (interim rule 10): NOT GOVERNING FOR THIS MEMBER.** The rule's test is whether
the member touches `src/components/**`, a route, a `data-testid` or an accessible name. **This member
touches none of them** — it changes `src/store/authSlice.js` and `src/config/tierFacts.js`, adds one
test file and renders nothing — so under the rule's own words *"SAY SO and name no spec."*
⭐ **Judgment 41 question 6 RULED this**: `EM-D0.partition.md` Q6 and `03-PHONE-AND-DOOR.md` §4.4
assigned D0b the spec `flow-b-auth-credits-ai`; that row is **corrected**, and the spec is owed when
the owner OPENS the door, beside the entitlement-ladder row — one of the three inseparable parts of
§1's single owner-signed act. The member that owes the chromium project today is **EM-D0e**.

**Registers that must NOT move:** the two goldens; `tests/copy/.voice-mechanics-*.json` (this member
renders no reader-facing string); `scripts/mutation-coverage-manifest.json` (§7: not owed, and a row
would be unlawful); `docs/content/wiring-census.json`.
**The one register that DOES move:** `tests/lint/.lighting-census-baseline.json`, by
`files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +1`, refrozen by the chair at the
terminal.

---

## §11 · Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and §P8, **stop** if: the predicate would have to open for a
**non-staff** account of any tier, or the second conjunct would have to be dropped, weakened or made
conditional on anything but the owner's own signed act; `staffUnlocksPaidFeatures` would have to
replace `isStaffRole` as the conjunct; a raw `=== 'premium'` comparison would have to be written; an
`entitlementLadder.js` row would be needed to make anything pass; a
`scripts/mutation-coverage-manifest.json` row would be needed; **the CREATE target would have to be
renamed**; the field would have to be spelled `editMode` on either table, or the shipped Cartographer
`editMode` boolean would have to change; a component, route, `data-testid` or accessible name would
have to be touched; **any import would have to be added to either changed `src/` file**; the eager
first-paint set's size changes by any amount, or `authSlice.js` leaves it, or `tierFacts.js` enters
it; **the chair's summed entry closure for the whole train exceeds `CLOSURE_BUDGET_BYTES`
(`1_048_000`)** — a first-paint raise is OWNER-SIGNED and this member re-mints nothing; **the
lighting walker measures any tuple other than `files +1 · parked +0 · credited +1 · titles +8 ·
suiteTitles +1`** (§7); a purchase surface, price, CTA or upgrade path would have to appear; a golden
or the prose manifest moves.

---

## §12 · Receipt, questions and handoff

**The SET-EQUAL proof (interim rule 6)** — a plain-node set comparison over this file's §7 table and
the capsule's `changeManifest`, with a failing control; the executed lines are in
`EM-D0b.setequal.txt`.

**⭐ THE COUNT LAW (from EM-B3d's STOP, 2026-09-21).** Every `TEST` and `CREATE` row's own words, the
acceptance matrix's homes, and the lighting delta in §7, §10 and §12 state the **same** numbers:

| row | its words | titles | describes |
|---|---|---:|---:|
| `CREATE tests/store/settlementEditorGate.test.js` | "**8 literal-titled `it`s in ONE literal `describe`**" | **+8** | **+1** |
| `TEST tests/config/tierFacts.contract.test.js` | "+1 line … add no `it`, no `describe`, no loop" | **+0** | **+0** |
| `TEST tests/build/vendorPdfLazy.test.js` | "NO EDIT" | **+0** | **+0** |
| **§7's deferred lighting row** | `files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +1` | **+8** | **+1** |
| **§10's instrument (c)** | same tuple | **+8** | **+1** |
| **§9's homes** | arms 1–8 = 8 arms; D5 in `checks`, D2(d) a lane act | **8** | — |
| **§6.5's arm table** | 8 rows | **8** | **1** |

All agree. ⛔ The acceptance matrix names **eight** arms and the CREATE row authorizes **eight**
`it`s; every one of the six cases has a declared home and no case is homeless.

**⭐ THE VALIDATION CONTROL.** This packet has **no provider**: its four non-CREATE rows and all
thirteen `requiredSymbols` paths exist at the base, and its one CREATE target is absent, so it
validates against the bare read tip with no stand-in of any kind. The executed lines — alone and
with all four EM-T9 members present — are in `EM-D0b.validate.txt`, beside the poisoned-row control
that proves the validator is reading this packet at all.

**What the next member may rely on.** EM-D0e consults **`canEditSettlement()`** and nothing else;
no other module in the estate may re-spell the entitlement. ⛔ **With a non-staff role the dialog
must not mount at all** (D0e's acceptance 7), and the gate is the reason. ⛔ **`src/components/edit/`
must contribute ZERO members to `EAGER_FIRST_PAINT_MODULES`** — this packet's predicate is data and
a function, and nothing here may become an eager path into a component.

**Questions only the chair can answer** are carried in §13. All five of version 1's are now ruled.

**Completion receipt owed at the landing:** base sha · seal identity · the exact changed files with
effective-line deltas measured by eslint's `Linter` · D1–D6 executed with their homes named · the
mutants planted, convicted and restored digest-exact (D2's one-conjunct body; D4's planted raw
comparison) · focused commands, exits and counts · both typecheck configurations · **the lighting
walker's five figures RECORDED, and compared against `files +1 · parked +0 · credited +1 · titles +8
· suiteTitles +1`** · the byte price handed to the chair for the train's single summed build, with
the `dist` it was read from, or the statement that the arm was not run at this member · generated
artifacts `NONE` · deviations `NONE | STOP` · judgment calls: `NONE`.

---

## §13 · RAISED — for the chair

| # | item |
|---|---|
| **R1** | ⚠ **`TIER_GATE` IS NOT FROZEN.** `export const TIER_GATE = {` is a plain object literal with three plain rows, while its parity partner `TIER_FACTS` is `Object.freeze`d at every level. A gate that can be mutated at runtime is a different safety posture from one that cannot, and the entitlement census reads it. **RULED — judgment 75(2): DOCKETED to the fifth amendment's agenda, not this packet.** This member does not freeze it; that is a behaviour change reaching 14 test files and is not a slice of the first door. |
| **R2** | ⚠ **THE HUB THRESHOLD.** `git grep -l -F authSlice -- tests` is **23**, against ruling 3's *"> ~25 test importers"*. **RULED — judgment 41, question 5: for a near-hub file the CHANGED-SYMBOL form is lawful for a purely ADDITIVE edit.** §10's set is therefore ruled, not proposed; the full 23-file and 13-file sets stay in §10 as the build lane's instruments. No question remains. |
| **R3** | ⚠ **THE BYTE-BUDGET `TEST` ROW.** Interim rule 2 requires it; ESTATE-REPAIR-2 deleted EM-B1c's lighting-walker row because that row made no edit. **RULED — judgment 75(3): RATIFIED as stated — a row for an INSTRUMENT the member's price is measured by is lawful; a row for a register the CHAIR regenerates is not.** ⭐ And **judgment 74** makes this member EM-T9's sole byte-arm holder, so the row now carries the train's summed price (§7, §10). |
| **R4** | ⚠ **THE `e2e/` ASSIGNMENT.** `EM-D0.partition.md` Q6 and `03-PHONE-AND-DOOR.md` §4.4 gave D0b `flow-b-auth-credits-ai`. **RULED — judgment 41, question 6: the partition's row is corrected; the spec is owed when the owner OPENS the door, with the ladder row.** §10 states it; no question remains. |
| **R5** | ⛔ **THE ONE OWNER-ADJACENT LINE.** The tier field alone opens the editor to **every paying Cartographer** on the day it lands, because `resolveTier` writes staff in as `premium` — measured: the one-conjunct mutant opens the door to every premium account including role `'user'`. **RULED — judgment 75(4): OPENING THE DOOR IS ONE OWNER-SIGNED ACT**, recorded in §1 and here, with three inseparable parts: **delete the second conjunct**, **add the `entitlementLadder.js` row**, **name the `flow-b-auth-credits-ai` spec**. No one of the three may land without the other two, and **nothing in this packet or any later packet flips the gate**. This packet ships it DARK. |

⭐ **Nothing in this packet is open.** All five raised items are ruled; the packet is promotable on
the chair's stamp of `Verified base` and `Last revalidated`.
