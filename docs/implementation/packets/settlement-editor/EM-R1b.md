# Settlement editor / EM-R1b — THE PARTIAL-PIN RULE KEYS ON THE TIER-1 RECORD PATH: a chooser with no record path is not counted against a record-built bag, and the DM's POWER CARD stops being refused

**Preamble:** docs/implementation/preambles/EM-PREAMBLE.md (SHA-256
**`125c693214235a81bf4f2b38506b621859a35e0325ff2ea58d335b4e0681a99d`**, MEASURED by this pre-proof
with `shasum -a 256` AS OF the read tip `6a0178b7b` — train EM-T16's stage-1 placement tip — and
re-stamped by the chair at promotion if the tip moves. ⭐ That text IS **THE SIXTH AMENDMENT**,
landed as train EM-T13's first commit `7bc14e003`. ⛔ **§P3.5 IS WHY THE FIGURE IS HERE, NOT WHY IT
WAS ABSENT:** the row makes this file's own SHA-256 a figure a member MEASURES at its own read tip
and never copies from a brief or a sibling, and executed history KEEPS its figure under an as-of
mark. Version 1 read that row as forbidding the digest outright; the row was re-read at this
pre-proof and the landed READY sibling `EM-R0b` version 8 carries the same measured hash the same
way. ⛔ This is the ONLY 64-hex digest in this body, and no golden digest appears at all (§P3.6).)
- **The law this packet is written to:** ⭐ **THE SIXTH AMENDMENT, LANDED AND MEASURED AT THIS COMPILE'S READ TIP.** §P2.19 (the voice counter over the planted text), §P2.21 (the instrument list, each run with its own logic), §P2.22 (cite by `path :: symbol`), §P3.5 (every register figure a DELTA), §P9 (the ONE effective-line instrument; the six capsule-shape rules; the count prover), §P10.8 (the row-keyed register), §P11.2 (a signed ceiling is the owner's) are the standards this version answers, each with an executed figure.

- **Status:** `LANDED`
  — Compiled 2026-09-22 by an Opus COMPILE seat (session cce01f87) at read tip `f76bd62ab`, train
  EM-T15's COMPOSED tip, under `briefs/launch/COMPILE-EM-R1b-cce01f87.md`; chartered by judgment
  182a, its second case by 182e.
  ⛔ **READ §0 BEFORE PROMOTING.** §0.1 is the measurement that keeps the charter's premise alive at
  the composed tip (EM-R2b was retired the same day because its premise did not survive it). §0.2 is
  the PLACEMENT PRECONDITION and §0.4 is the second case, which this member NAMES and does not cure.
  ⚠ The status value stands ALONE on its line because `scripts/implementation-packets.mjs ::
  parsePacketHeader` anchors the status row at end-of-line and takes `status` only when exactly one
  row matches (§P9(c)).
  ⭐ **VERSION 2 — PRE-PROOFED WHOLE at `6a0178b7b`**, train EM-T16's STAGE-1 PLACEMENT tip, by an
  Opus PRE-PROOF seat (session cce01f87, 2026-09-23) under
  `briefs/launch/PREPROOF-EM-T16-stage2-cce01f87.md`. The J-T1 window `f76bd62ab..6a0178b7b` moved
  NOT ONE of this member's ten change-manifest and `requiredSymbols` paths; all twelve required
  symbols are verbatim at `count=1`; and the premise still reproduces **63 of 63** through the real
  `rederive`. ⛔ **THIS MEMBER IS PLACED ON STAGE 1's FLIP** (EM-R2 version 5 · EM-R0b version 8):
  §0.2 names the ONE reservation that clears there, and `EM-R1b.remeasure.sh` re-proves every fact
  stage 1 can move.
- **Landed at:** `282012b8cbcda92d9d83aa2042c5e0609993dccb` — the partial-pin rule keys on the tier-1 record path (version 2): the nineteen transient choosers declared beside _LEDGER_KEYS, pinsFrom reading the same declaration through getStepMeta, the DM's power card applied 63/63 through rederive; the byte-arm holder of the train (206c/206d/210); built at 282012b8c on train EM-T16 stage 2a
- **Packet version:** 2
  > **What version 2 changed, and why (the Opus PRE-PROOF seat, 2026-09-23, at `6a0178b7b`).**
  > NOTHING IN THE CONTRACT MOVED — the twelve required symbols are verbatim at `count=1`, the CREATE
  > target is absent, both effective-line figures re-measure exactly (150 → 157 and 204 → 207), every
  > register figure re-reads as stated, and the refusal still reproduces 63 of 63. What changed is the
  > ADDRESSING of facts stage 1 moves under this member, plus three absolutes that had already moved:
  > the preamble line now carries the MEASURED sixth-amendment hash under an as-of mark (§P3.5 asks
  > for it measured, not absent); §0.2's placement precondition is re-measured (EM-R1 and EM-R5 are
  > LANDED — the one remaining reservation is EM-R2 version 5's TEST row on
  > `tests/generators/pipelinePinnedMode.test.js`); §5.6 and §7 state `stamp.producerIndexFiles` as
  > **1,185** as of this tip (the delta is still +0); §7's REGISTER row states `invariants` **727** as
  > of this tip and names EM-R2's arriving row; §7's two TEST rows and the CREATE row carry judgment
  > 196's title law; §10's `checks` derivation is re-computed and names the one file that joins it at
  > the flip; and §5.8 carries the TRAIN's summed byte price, because judgment 206d makes this member
  > EM-T16's BYTE-ARM HOLDER. Two claims were DISCHARGED by measurement: the four stale `_note`
  > addresses version 1 noticed (the citations cure `80f572e55` removes all four) and any doubt that
  > this member moves EM-R2's arriving `EM-P0 › A2` arm — it moves it by **zero**, 63 of 63 (§0.3).
- **Verified base:** `em-t16-r1b-2026-09-23` at `17fcf092bb878eb5b168f63f264c2a4dff316f19`
  ⛔ **THE LANE BRANCH RIDES THIS ROW, NOT A ROW OF ITS OWN**: `parsePacketHeader` takes
  `verifiedBranch` from the `Verified base` row itself, matching `` `<branch>` at `<40-hex sha>` ``,
  and reads no `Verified branch:` row. The chair's placement writes this row as
  ``- **Verified base:** `em-t16-r1b-<date>` at `<the train base's 40-hex sha>` `` — the value ALONE
  on the line, every caveat on these continuation lines (§P9(c)).
- **Last revalidated:** `17fcf092bb878eb5b168f63f264c2a4dff316f19`
  > The chair's revalidation sentence: *re-measured at `__BASE__` — the J-T1 window
  > `6a0178b7b..__BASE__` over every change-manifest and `requiredSymbols` path (`EM-R1b.remeasure.sh`
  > runs it, with the twelve symbols verbatim beside it); the twelve own and the THIRTY-THREE landed
  > `requiredSymbols` rows over the two `src/` paths still resolve verbatim and none is a line this
  > member edits; the register's recordPath-null set still reads NINETEEN keys over SIX steps; the
  > refusal still reproduces `step_not_pinnable` 63 of 63 through the real `rederive`; EM-R2 version
  > 5's rewritten `EM-P0 › A2` is present and this member's own widening touches none of its lines;
  > and the register figures (entropy 36 / 47, tuning 0 named per file, `stamp.producerIndexFiles` +0
  > on a base of 1,185, the eager set 270 / +0, the voice rows `pipeline.js` em 2 · bang 0 and
  > `dmLayer.js` no row, `moduleScopeCwdRatchet`'s `tests/lint` 17, and the lighting baseline unmoved
  > at the twenty-first refreeze) re-read as stated.*
- **Lane branch:** `em-t16-r1b-<date>` (§P10.2 — the lane branch IS the verified branch and is written INTO the `Verified base` row above).
- **Depends on:** **EM-P0** (the runner seam; LANDED) · **EM-B2a2** (`chooseOrPin`, the primitive this member does not touch; LANDED) · **EM-B2a3** (the institution and power consults; LANDED) · **EM-B2a4** (`pinsFrom` and `rederive`, the leaf this member edits; LANDED) · **EM-P2 / EM-R0a** (`GENERATION_TIER1`, the measured source of the rule; LANDED) · ⭐ **EM-R1** (the runner's clone shape 4) and **EM-R5** (the partitioned trace inside `rederive`) — ⭐ **BOTH LANDED**, on train EM-T15 at `64b75240c`, re-measured from the live manifest at `6a0178b7b` (`EM-R1 = LANDED`, `EM-R5 = LANDED`), so version 1's placement precondition is two-thirds discharged. ⚠ **EM-R2 version 5** is READY and lands at STAGE 1's FLIP, ONE STAGE AHEAD of this member: it REWRITES `EM-P0 › A2` in this member's TEST home and adds one `invariants` row to the row-keyed register — §7's TEST and REGISTER rows name both arrivals and `EM-R1b.remeasure.sh` re-proves them. ⚠ **EM-R0b version 8** is READY beside it and shares NO path with this member.
- **Collision group (RE-MEASURED at `6a0178b7b` against the live manifest, not against a packet):** `src/generators/pipeline.js` and `src/domain/edit/dmLayer.js` are now reserved by **NO** non-terminal packet (EM-R1's and EM-R5's reservations cleared when they LANDED) · `tests/generators/pipelinePinnedMode.test.js` is reserved by **EM-R2** (READY, stage 1) and is the ONE duplicate the dry validator reports today (§0.2) · `tests/property/dmLayerGoldenIsolation.test.js` (EM-B2a4's landed battery; no non-terminal holder) · `scripts/mutation-coverage-manifest.json` (**ROW-KEYED**, §P10.8 — this member's `rowKey` is `invariants['tests/lint/transientChooserRegistry.walker.test.js']`, DISTINCT from EM-R2's and from stage 3's EM-R4, which is why the validator reports no duplicate there: measured, both directions). ⭐ **STAGE 2 IS PAIRWISE DISJOINT** — EM-R3 (`economyReconciliation.js` · `powerEconomyReconcilePass.js` · `assembleSettlement.js` · `powerEconomyFreshness.test.js`) and EM-R0c (`recordMergeTree.js` · `mergeConsequence.js` · `recordMerge.test.js`) share NO path with this member or with each other. ⭐ **STAGE 3** (EM-R4) MODIFIES `assembleSettlement.js`, CREATEs `tests/generators/nameHeldThroughAssembly.test.js` and REGISTERs the same row-keyed manifest under its OWN `rowKey`: **of this member's six paths it moves exactly one, the row-keyed register, lawfully.**
- **Commit authority:** edits only; the chair commits (§P10.5).
- **Baseline posture:** **measured, twice.** Every figure in §0, §3, §5 and §7 was EXECUTED at `f76bd62ab` by the compile and **RE-EXECUTED WHOLE at `6a0178b7b` by the pre-proof**, in plain `node` and `git`, in both directions where a claim has two sides: once over the tip as it stands and once over the PLANTED contract, served in memory by a loader that never touches the tree. ⭐ Every figure this packet states as LIVE was re-read at `6a0178b7b`; a figure quoted as HISTORY carries the sha it was measured at (§P3.5). Receipts: `EM-R1b.evidence.md` E-0…E-13 (the compile) and E-14…E-24 (the pre-proof).

---

## §0 · ⛔⛔ FOUR MEASUREMENTS THAT SHAPE THIS MEMBER — none adjudicated by the lane

### §0.1 · ⭐⭐ THE PREMISE SURVIVES THE COMPOSED TIP: 63 OF 63, THROUGH THE REAL `rederive`

The launch brief's own instruction, and EM-R2b's lesson from the same day: *if the first measurement
shows the refusal is gone at `f76bd62ab`, STOP rather than cure a defect that no longer exists.*

```
$ EMR1B_TREE=<read tip> node probe-1-refusal.mjs        # the REAL rederive, plain node, no runner
corpus rows = 63
ARM B  (REAL rederive, powerSeat.holder): rows=63 applied=0 step_not_pinnable=63
ARM B' (pinsFrom, same inputs)          : rows=63 applied=0 step_not_pinnable=63
ARM C  (pinsFrom, faction.power)        : rows=63 applied=0 step_not_pinnable=63
ARM D  (pinsFrom, institution.state)    : rows=63 applied=0 step_not_pinnable=63
delegation control: rederive.unapplied === pinsFrom.unapplied in 63/63 rows
  63× seat pins=[] runCalls=1
```

⭐ **RE-RUN WHOLE AT `6a0178b7b`** by the pre-proof (the same probe, the tree from its environment):
every figure above reproduced EXACTLY — `rows=63 applied=0 step_not_pinnable=63` on all four arms and
the delegation control 63/63. The premise is measured at the placement tip, not inherited from the
compile's.

**The refusal is live at the tip this member will be placed on.** A DM's power-seat edit, a DM's
faction-share edit and a DM's institution edit all come back `unapplied: [{ reason:
'step_not_pinnable' }]`, and the bag `pinsFrom` builds for each is EMPTY. The delegation control says
the envelope `rederive` returns IS `pinsFrom`'s, on every row, so the leaf's closure is the whole
cause and nothing downstream is hiding a second one.

**And the cause is exactly the charter's.** `generatePower` declares two choosers; the register has
measured and frozen `recordPath: null` for `powerIntent` and `record.powerStructure` for the
structure; `pinsFrom`'s closure demands that every chooser of every provider be HELD on the record;
`powerIntent` never is, so the closure returns `null` and every power override is refused.

⇒ **No STOP. The premise is not refuted, and the member is compiled.**

### §0.2 · ⛔ THE PLACEMENT PRECONDITION, RE-MEASURED: ONE RESERVATION IS LEFT, AND IT CLEARS AT STAGE 1's FLIP

Measured from the LIVE manifest at `6a0178b7b` rather than from any packet (judgment 134):

| Fact | Figure at `6a0178b7b` (train EM-T16's stage-1 placement tip) |
|---|---|
| registered estate | **226 entries · 222 LANDED · 2 SUPERSEDED · 2 READY**, and it validates `ok=true` with 0 errors today |
| the two READY | **EM-R2** (version 5) and **EM-R0b** (version 8) — train EM-T16's STAGE 1, which lands one stage AHEAD of this member |
| who reserves `src/generators/pipeline.js` | **NOBODY** — EM-R1 LANDED at `64b75240c` and its reservation cleared |
| who reserves `src/domain/edit/dmLayer.js` | **NOBODY** — EM-R5 LANDED at the same commit |
| who reserves `tests/generators/pipelinePinnedMode.test.js` | **EM-R2** (its TEST row on `EM-P0 › A2`) — the ONE duplicate, clearing at stage 1's flip |
| the row-keyed register | `scripts/mutation-coverage-manifest.json` is named by EM-R2 (READY) and by stage 3's EM-R4, each under a DISTINCT `rowKey`: the validator reports **no** duplicate there, measured in both directions (TOOL-27, §P10.8) |
| EM-R0b against this member | **DISJOINT** — three CREATEs under `src/domain/edit/` and one under `tests/domain/` |

A change path is reserved at EVERY non-terminal status, DRAFT included. The dry validator was RUN at
this tip with the capsule placed, and its full output is `EM-R1b.preproof.report.md` §9:

```
CONTROL: the live estate as it stands (no EM-R1b) === ok=true errors=0
(1) EM-R1b DRAFT placed NOW, stage 1 still READY === ok=false errors=2
   EM-R1b.packetPath does not exist: docs/implementation/packets/settlement-editor/EM-R1b.md
   duplicate change path across packets: tests/generators/pipelinePinnedMode.test.js (EM-R2, EM-R1b)
(2) the same, with STAGE 1 flipped to LANDED: the duplicate CLEARS
```

⇒ **EM-R1b is placed on STAGE 1's FLIP.** The only error left is then the placement's own act (the
chair writes the body and stamps the base). ⛔ Any OTHER duplicate at the flip is a STOP.

### §0.3 · ⭐ WHAT THE CURE DELIVERS ALONE, AND WHAT IT DOES NOT — MEASURED, SO NO LATER SEAT IS SURPRISED

With the contract planted, the power edit is APPLIED rather than refused, and the run completes:

```
C1 the power-seat edit through the REAL rederive: applied (unapplied EMPTY) in 63/63; reasons []
C2 the bag pinsFrom built for it: [["powerStructure",63]]
C3 that bag through the runner, pin violations: [["none",63]]
```

⛔ **AND THE DM'S VALUE DOES NOT YET REACH THE RECORD.** Measured over the same corpus with only the
structure held: the edited `powerStructure.governingName` survives the `generatePower` step and is
then overwritten downstream, so the finished record is unmoved in **62 of 63** rows. That is EM-R3's
subject — `reconcilePowerStructure`'s replay — and EM-R3 rides the same train for exactly this
reason. **EM-R1b makes the edit APPLICABLE; EM-R3 makes it VISIBLE.** This packet therefore asserts
no survival figure anywhere: a figure its trainmate inverts would red at the composed tip. §12
records the pair.

⭐ **AND TWO FACTS THE PRE-PROOF ADDED, BECAUSE STAGE 1 LANDS AN ARM OVER THIS VERY PATH.** EM-R2
version 5 REWRITES `tests/generators/pipelinePinnedMode.test.js :: EM-P0 › A2` into a RECORD-BUILT
bag driven through the real `rederive` under a NO-OP root override, asserting the re-derived record
BYTE-IDENTICAL. That arm exercises exactly the closure this member widens, so it was executed here in
both directions over the census corpus, at `6a0178b7b`, in the arm's own shape
(`npc:<id>:status` set to its own value — EM-R2's own harness):

```
tip     : ROWS 63 · EM-R2 ARM SHAPE byte-identical 6 · divergent 57 · unapplied reasons 0
planted : ROWS 63 · EM-R2 ARM SHAPE byte-identical 6 · divergent 57 · unapplied reasons 0
row for row: the re-derived record identical 63/63 · the bag identical 63/63 · the reasons identical 63/63
```

⇒ **THIS MEMBER MOVES THAT ARM BY ZERO** (and 6 of 63 is EM-R2's own published red-first figure,
reproduced here independently: its gate, not this one, carries it to 63). The second fact, which
§5.4's widening makes worth stating: under a NO-OP root override on the POWER card — the card this
member lights — the cure applies with an EMPTY `unapplied` and the re-derived record is
**BYTE-IDENTICAL to the record on 63 of 63 rows**, where the tip refuses all 63. The re-roll of a
transient chooser is therefore faithful at corpus scale, not merely argued from the stream law.

### §0.4 · ⛔ THE SECOND CASE (judgment 182e) IS A SECOND DEFECT, AND THIS MEMBER NAMES ITS SLOT INSTEAD OF IMPROVISING

The charter's second case asks whether the chooser-completion CASCADE is this rule's defect or
another. **It is another, and the measurement says so in one line.** The institution card's closure
blocks on `generationRepairs` — BOTH at the tip and with this member's contract planted:

| key in the institution closure | `recordPath` | readable by ctx-key NAME at the record root | readable at its record path |
|---|---|---|---|
| `institutions` | `record.institutions` | **true** | present |
| `catalogForTier` | **null** | false | (none — this member's rule skips it) |
| `generationRepairs` | `record.generationCoherenceReceipt.repairs` | **false** | **present** |
| `isolationSupport` | `record.isolationSupport` | true | present |
| `stress` | `record.stress` | true | present |
| `stressTypes` | `record.config.stressTypes` | **false** | **present** |

`pinsFrom`'s `held()` and its bag builder read `source[ctxKey]` at the record's TOP LEVEL, so a
chooser whose record path is NESTED is unreadable even though the value is right there. That is not
the partial-pin rule's defect — the charter itself says a chooser WITH a record path the bag lacks
is still a partial pin — and behind it sits the cascade EM-R3 noticed: were `generationRepairs`
readable, the closure would drag `isolationSupport` and then `stress`, which design §22.1 correction
5 says is RE-DERIVED and never held. **The institution card therefore needs two things this member
does not have authority for:** a bag builder that reads a chooser's `recordPath` rather than its
ctx-key name (EM-B2a4's ground), and a ruling on whether a re-derived key may be dragged into a bag
at all (the design's). ⇒ **SLOT: one new EM-R family member, the chair's to number and charter.**
§11 item 8 carries it as a STOP condition so no build lane improvises it.

---

## §1 · Reconciled authority

| Check | Verdict at `6a0178b7b` (train EM-T16's STAGE-1 PLACEMENT tip — RE-MEASURED by the pre-proof) |
|---|---|
| CREATE target absent | **PASSES** — `tests/lint/transientChooserRegistry.walker.test.js` does not exist |
| MODIFY / TEST / REGISTER targets present | **PASSES** — all five exist |
| every `requiredSymbols` row resolving | **PASSES** — all twelve found VERBATIM, `count=1` each, zero non-1, at the tip AND in the planted text (§5.1) |
| the **THIRTY-THREE** landed `requiredSymbols` rows over these two `src/` paths | **PASSES, ALL THIRTY-THREE** — not one is a line this member edits, so `retiredSymbols` is **NONE** and the post-edit simulation is clean (§5.2) |
| no OTHER non-terminal packet writes these paths | ⛔ **ONE, AND IT CLEARS AT STAGE 1's FLIP** — EM-R2 (READY) holds `tests/generators/pipelinePinnedMode.test.js`; the two `src/` reservations cleared when EM-R1 and EM-R5 LANDED (§0.2) |
| EM-R2 version 5's arriving rewrite of `EM-P0 › A2` | **MEASURED DISJOINT** — it replaces `:191` and `:195` and renames the title inside `describe('EM-P0 …')`; this member APPENDS a new top-level `describe`, touches none of those lines, and moves that arm's outcome by ZERO over the corpus (§0.3) |
| the charter's premise against the tree | **PASSES** — the refusal reproduces 63/63 through the real `rederive` (§0.1) |
| the design, the charter and the tree agreeing | ✅ — design §22 ruling 5 forbids persisting `powerIntent`; the register's `recordPath: null` is that ruling MEASURED; this member makes the runner and the leaf read it |

**Authority read, by SECTION and ITEM (never by line):** design `docs/DESIGN_EDIT_MODE_AND_DECREES.md`
§12 (governs), §14 (the source rule; HZ-DERIVED's five kinds), §22 rulings 5 and 6, §22.1 corrections
4 and 5, §22.2 item 11. Charter `docs/implementation/charters/EDIT-MODE-TRAIN.md`: the EM-R family
table of the 2026-09-19 17:25 amendments. Judgments **182a** (this member's charter) and **182e** (its
second case). `EM-PREAMBLE` §P1–§P11 as amended by the sixth.

## §2 · Outcome

**What this member makes true.** (1) The partial-pin rule in `src/generators/pipeline.js ::
runPipeline` counts a step's RECORD-BACKED choosers only: a chooser whose `GENERATION_TIER1`
`recordPath` is `null` is neither required of a bag nor reported missing, and it is re-rolled on the
step's own stream exactly as an unpinned run rolls it. (2) `src/domain/edit/dmLayer.js :: pinsFrom`
reads the SAME declaration through the injected engine handle, so its pin closure stops demanding a
key the record can never hold. (3) A DM's POWER-CARD edit therefore comes back APPLIED instead of
`step_not_pinnable` — **63 of 63, measured** — with a bag of exactly `[powerStructure]`. (4) The
declaration and the register are held EQUAL IN BOTH DIRECTIONS by a new walker, so the day a step's
provenance changes, the rule reds by name instead of drifting.

**Non-goals.** The power replay and the freshness assert (EM-R3, the same train) · the merge
(EM-R0c) · the roster final (EM-R2) · the name (EM-R4) · the trace partition (EM-R5, landed) · the
corpus ratchet (EM-R7). ⛔ **This member adds NO consult, changes NO chooser roster, and moves NO
record key.** It changes WHICH choosers a rule counts. ⛔ **The institution card stays dark** and its
cure is slotted (§0.4).

## §3 · Hard scope budget

| Budget | Cap | This member | Instrument |
|---|---:|---:|---|
| behaviour families | 1 | **1** | the partial-pin rule |
| handwritten files | 12 | **6** | §7 |
| new LOGIC leaves | 2 | **0** | the CREATE is a test |
| existing LOGIC files modified | 3 | **2** (`pipeline.js`, `dmLayer.js`) | under the cap |
| new/changed effective production lines | 400 | **+10** | eslint `Linter`, `max-lines`, `skipBlankLines` + `skipComments` (§P9) |
| each new leaf | 250 eff | **n/a** — no new `src/` leaf | — |
| shared/hot-file delta | 15 eff | **+7** on `pipeline.js`, which is on NO hot list and carries no `.size-baseline.json` row | measured |
| acceptance cases | 8 | **8** | §9 |

Effective lines RE-MEASURED at `6a0178b7b` with the one instrument, over the file as it stands and
over the planted text: `src/generators/pipeline.js` **150 → 157 (+7)**, `src/domain/edit/dmLayer.js`
**204 → 207 (+3)** — the compile's figures, unmoved. The instrument's own control reads 2 on a
two-line source and NO MATCH on a comments-and-blanks-only source, so it is live in both directions.

## §4 · Sealed dispatch and preflight

Dispatch on the lane branch `em-t16-r1b-<date>`, cut from the train's ONE promotion commit (§P10.1–2).
The lane's FIRST act is §P10.3's six measurements, each a PASS or the smallest measured contradiction:
(i) branch, HEAD and verified base are the three the dispatch note names; (ii) §7's path set and the
capsule's `changeManifest` are SET-EQUAL; (iii) the CREATE target is ABSENT and all five other targets
PRESENT at HEAD; (iv) all twelve required symbols found verbatim; (v) the count law through
`EM-R1b.count-prover.mjs`, run FROM THE KIT PATH; (vi) every line-addressed register grepped for both
paths this member modifies (§5.5's commands re-run). ⭐ **A SEVENTH, THIS MEMBER'S OWN:** re-run
`probe-1-refusal.mjs`'s ARM B at HEAD and confirm **63/63 `step_not_pinnable`** before the first edit —
the red-first A6 rests on, and the one measurement that would make this member pointless if it had
changed. Any failure is a STOP BEFORE the seal.

## §5 · Verified tree contract

### §5.1 · The twelve required symbols, found verbatim (`count=1` on every row, RE-MEASURED at `6a0178b7b` AND in the planted text)

| Role | Path | Symbol | Required use |
|---|---|---|---|
| State authority | `src/generators/pipeline.js` | `export function runPipeline` | the rule's home; signature and refusal sentence untouched |
| Reserved channel | `src/generators/pipeline.js` | `const _PINS_KEY = '__pins';` | EM-B2a2's row; the declaration lands directly beneath it, BYTE-IDENTICAL |
| Sole seam | `src/generators/pipeline.js` | `export function chooseOrPin(pins, key, draw)` | ⛔ UNTOUCHED — a supplied transient chooser is still handed back |
| The channel | `src/generators/pipeline.js` | `export function getStepMeta() {` | carries the declaration to the leaf; its declaration line is byte-identical |
| Precedent | `src/generators/pipeline.js` | `const _LEDGER_KEYS = new Set(['_traceClock', 'simulationTrace']);` | the shape this member's declaration copies; above the insert |
| Sole writer | `src/domain/edit/dmLayer.js` | `export function pinsFrom(record, layer, declarations, engine) {` | the closure's home; signature and injected-handle law untouched |
| Lifecycle | `src/domain/edit/dmLayer.js` | `export function rederive(record, config, layer, engine, declarations) {` | EM-R5's landed shape; nothing inside it moves |
| Receipt vocabulary | `src/domain/edit/dmLayer.js` | `export const REDERIVE_UNAPPLIED_REASONS = Object.freeze(['step_not_pinnable', 'unknown_key']);` | the closed set, unmoved in both directions |
| Measured source | `src/domain/generation/generationForkRegistry.js` | `export const GENERATION_TIER1` | ⭐ IMPORTED BY THE WALKER AND BY NOTHING UNDER `src/` |
| Roster (power) | `src/generators/steps/generatePower.js` | `provides: ['powerIntent', 'powerStructure'],` | ⛔ THE ROSTER IS KEPT — the rule changes, not the roster |
| Roster (institutions) | `src/generators/steps/assembleInstitutions.js` | `provides: ['institutions', 'catalogForTier', 'generationRepairs'],` | the same, and it carries the second transient chooser |
| Test precedent | `tests/generators/pipelinePinnedChoosers.test.js` | `const choosersOf = (step) => getStepMeta().find((meta) => meta.name === step).provides;` | the landed arm reads `provides`; the new field lives BESIDE it |

⛔ **`_note` fields on these rows carry NO `path:line` address** (judgment 124: a live packet's own
`_note` addresses are a line-addressed register and this member shifts lines in both `src/` files).

### §5.2 · The post-edit simulation: `retiredSymbols` is NONE

**Thirty-three** `requiredSymbols` rows across the estate name one of this member's two `src/` paths
(EM-P0, EM-P2, EM-B2a2, EM-B2a3, EM-B2a4, EM-C4a, and the three READY members EM-R1, EM-R2, EM-R5).
Every one of them names a declaration line, an export or a module-scope const that this member leaves
BYTE-IDENTICAL: the declaration block is an INSERT beneath `_PINS_KEY`, the rule change replaces one
expression inside `runPipeline`'s body, the `getStepMeta` change adds one row to a returned object,
and the leaf's change touches `rosterOf`'s return, two lines of `closureFor` and the typedef. ⇒ **no
row is retired, no row is re-spelled, and the post-edit validation is clean.**

### §5.3 · The two landed arms the charter names, RE-MEASURED WITH THE CONTRACT PLANTED

EM-R3's compile warned that the obvious re-declaration (moving `powerIntent` out of `provides`) reds
two landed arms. **This member does not re-declare anything, and both arms were executed against the
planted contract rather than reasoned about:**

| Arm | At the tip | With the contract planted |
|---|---|---|
| `tests/generators/pipelinePinnedChoosers.test.js :: A4`, the whole sentence | `Pipeline pins: step "coherenceRepairPass" has choosers [generationRepairs, isolationSupport] but pins supply only [generationRepairs]. Pin every chooser of a step or none of them.` | **BYTE-IDENTICAL** |
| the same arm's strict report | `[{"step":"coherenceRepairPass","kind":"pin","keys":["isolationSupport"]}]` | **IDENTICAL** |
| the same arm's `recordBuiltPins(generatePower)` "does not throw" | no throw | **no throw** |
| `tests/generators/pipelinePinnedMode.test.js :: A4`, the whole sentence | `Pipeline pins: step "generatePopulation" has choosers [npcs, relationships, factions, conflicts] but pins supply only [relationships]. Pin every chooser of a step or none of them.` | **BYTE-IDENTICAL** |
| the same arm's strict report | `[{"step":"generatePopulation","kind":"pin","keys":["npcs","factions","conflicts"]}]` | **IDENTICAL** |
| `tests/generators/dataFlowContract.test.js`'s producer predicate (`provides ∪ mutates`, never `scratch`) | `powerIntent` is produced by `generatePower` | **UNCHANGED** — `provides` is byte-identical |
| `tests/property/dmLayerGoldenIsolation.test.js :: A1`, the npc closure both directions | `[conflicts, factions, npcs, relationships]` | **IDENTICAL** (none of the four is transient) |
| the same file's A7 dormant-golden property | the record, byte for byte, 63/63 | **63/63** |

**Why every one of them survives:** the two refusing steps' choosers (`generationRepairs`,
`isolationSupport`, and all four population keys) each have a non-null `recordPath`, so the filtered
list equals the full list and the message's own text is unmoved.

### §5.4 · ⭐ THE ONE DECLARED BEHAVIOUR CHANGE BESIDE THE CURE, MEASURED

A bag that supplies a TRANSIENT chooser and nothing else no longer refuses:

```
L5 a bag of the transient chooser ALONE
   tip:     Pipeline pins: step "generatePower" has choosers [powerIntent, powerStructure] but pins supply only [powerIntent]…
   planted: NO REFUSAL (the declared widening)
```

Such a bag is reachable from no record (the leaf never puts a transient key in one) and from no
landed arm (both refusal fixtures supply record-backed keys); it is reachable only from a hand-built
test bag. The rule is written the charter's way — *not counted* means not counted on either side —
and the alternative (counting a transient chooser as supplied-by-construction, which would keep
today's refusal for that one shape) is **R1 in the compile report**, the chair's to rule.

### §5.5 · Line-addressed registers, and the addresses this member shifts

| Register | Command | Result |
|---|---|---|
| prose numerics | rows of `tests/lint/.prose-numerics-baseline.json` naming either path | **0 and 0 — none found** |
| the dark-guard registry | `ls tests/lint/.dark-guard-registry.json` | **ABSENT at this tip** |
| source citations | rows of `tests/lint/.source-citation-baseline.json` naming either path | **0 and 0** |
| live `path:NNN` citations of these files | `git grep -nE 'src/generators/pipeline\.js:[0-9]+\|src/domain/edit/dmLayer\.js:[0-9]+' -- src tests docs scripts` | one doc row and several `PACKET_MANIFEST.json` / landed-packet `_note`s, ALL of them BELOW the insert |

⛔ **No re-address is owed, and the reason was RE-READ IN THE WALKER at `6a0178b7b` rather than
inherited:** `tests/lint/sourceCitationIntegrity.walker.test.js` gates PAST-EOF ONLY — ARM 1 over live
code with no baseline, ARM 2 over live docs against a shrink-only baseline — while ARM 3, the symbol
arm, is REPORT-ONLY by its own header ("it must never gate", a measured false positive) and excludes
every LANDED packet's rows through `landedPacketPaths`, read from `PACKET_MANIFEST.json`'s `status`.
The baseline holds **zero** rows for either path and both files GROW (`pipeline.js` 318 → 335 lines,
`dmLayer.js` 503 → 509), so this member can neither mint a past-EOF citation nor clear a baselined one.

⭐ **THE FOUR STALE ADDRESSES VERSION 1 NOTICED ARE DISCHARGED, MEASURED BOTH WAYS.** At `6a0178b7b`
the manifest's `_note` fields carry **SIX** `path:line` citations of this member's two `src/` paths —
EM-B2a2's `pipeline.js:182` and `:59` (both LIVE and CORRECT: `runPipeline` is at `:182` and
`_PINS_KEY` at `:59`) and EM-B2a4's four stale ones (`:171`, `:152`, `:259` twice). The citations cure
`80f572e55` (pushed; picked onto the train before this member's placement) removes exactly those four:
the same measurement over its tree reads **TWO**, EM-B2a2's pair. ⚠ **ONE ADDRESS THIS MEMBER ITSELF
SHIFTS, NAMED RATHER THAN OWED:** the declaration block lands beneath `_PINS_KEY`, so EM-B2a2's
`pipeline.js:182` becomes `:198`. No arm can convict it — it is not past EOF, its packet is LANDED, and
the arm that reads symbols never gates — and under §P2.22 the honest cure is a re-spelling by SYMBOL of
a LANDED packet's row, which is the chair's at a terminal (the shape `80f572e55` already used).
`EM-R1b.remeasure.sh` §7 re-runs the whole count at the flip.

### §5.6 · Stamped files and the wiring census

`docs/content/wiring-census.json`'s `stamp.files` holds **7** entries and names NEITHER of this
member's `src/` paths, so `tests/lint/proseWiringCensus.walker.test.js`'s `stale-bytes` arm is not
owed. `stamp.producerIndexFiles` reads **1,185 AS OF `6a0178b7b`** (version 1 measured 1,184 at
`f76bd62ab`; the re-take `037beb38c` moved it inside the window — the ABSOLUTE moved, the DELTA did
not) and this member CREATEs no `.js` under `src/domain` or `src/generators` ⇒ **+0**, a deferred row,
regenerated by the chair at the terminal. ⛔ **THE DECLARATION LAW (judgment 195/195a): this member
declares the walker's disposition and NEVER regenerates the register** — `car 0 stale-bytes` cannot
fire, because neither of its `src/` paths is a stamped producer file.

### §5.7 · The CREATE's own construction rules

`tests/lint/transientChooserRegistry.walker.test.js` reads **no source and no `dist`**: both of its
sets are values imported from their producers (`GENERATION_TIER1` from the register, the roster from
`getStepMeta()`), and its third arm drives `pinsFrom` with an injected STUB roster. So it spells no
repository root at all, `tests/lint/moduleScopeCwdRatchet.test.js`'s banked population
(`{ 'tests/build': 30, 'tests/lint': 17 }`, EXACT IN BOTH DIRECTIONS) is UNMOVED, and
`tests/lint/contractTestAntiVacuity.walker.test.js` Rule 2 has nothing to convict. ⛔ `it`, `test` and
`describe` are bound EXACTLY ONCE each and never re-bound, not even as a callback parameter; no
`.each`, no loop-registered title, no nested `describe`, no bare seed loop (collect, then assert once).

### §5.8 · Bytes

`src/generators/pipeline.js` IS a member of the generation worker's static closure (**222**
first-party files, a plain-node BFS that reads no `dist` and therefore cannot skip; the control file
correctly absent). `src/domain/edit/dmLayer.js` is **NOT**, and neither file is in
`EAGER_FIRST_PAINT_MODULES` (**270**, imported from `vite.config.js`). Neither path appears in any of
the five edge-shared metas' own `inputs` lists, so the rebuild is **NOT OWED**.

The planted text adds **1,388 raw bytes** to `pipeline.js` (re-measured at `6a0178b7b`: 15,799 →
17,187) and **609** to `dmLayer.js` (25,063 → 25,672), most of the first being the declaration's
comment block, which minification removes. What survives is nineteen short string literals, one `Set`
and two `filter` expressions: **PREDICTED +400 to +700 B minified**, into a budget with **ZERO SLACK**
(`WORKER_BUNDLE_CEILING_BYTES` reads **1392364** at this tip — EXACT, and re-read from the arm itself
at the terminal rather than copied from here).

⭐⭐ **THIS MEMBER IS TRAIN EM-T16's BYTE-ARM HOLDER (judgment 206d), SO IT CARRIES THE TRAIN's SUM.**
Every member prices its own closures by the membership probe and states its delta as a MEMBERSHIP
fact; the holder SUMS the declared deltas and the terminal's ONE real build is attributed against that
sum (judgment 198d). Read from each member's own packet at `6a0178b7b` — stage 1's two from the READY
packets in the tree, stage 2's and stage 3's from the chair's `packets-waiting/`:

| member | stage | in the worker's closure? | its OWN declared worker delta | first paint |
|---|---|---|---|---:|
| **EM-R2** v5 | 1 | yes (`narrativeGenerator.js`, `assembleSettlement.js`, `generatePopulation.js`; `holderTable.js` out) | **≤ +600 B** ("the +10 effective code lines, doubled as the standard requires") | +0 |
| **EM-R0b** v8 | 1 | no (`EAGER_FIRST_PAINT_MODULES` and the worker closure hold ZERO `src/domain/edit/**`) | **+0 B** | +0 |
| **EM-R1b** v2 | 2 | yes (`pipeline.js`; `dmLayer.js` out) | **+400 to +700 B** | +0 |
| **EM-R3** v1 | 2 | yes (all three source files) | **+40 to +120 B** | +0 |
| **EM-R0c** v5 | 2 | no | **+0 B** ("ZERO BYTES in all three" budgets) | +0 |
| **EM-R4** v1 | 3 | yes (`assembleSettlement.js`) | **≤ +90 B** (≈45 B × 2), with a NAMED buy-back of ≈ −16 B | +0 |
| **THE TRAIN** | — | — | **+440 B to +1,510 B, upper bound +1,510 B** | **+0** |

⛔ **THE WHOLE SUM IS A RISE AGAINST A ZERO-SLACK, OWNER-SIGNED CEILING** (§P11.2), so train EM-T16
owes a BUY-BACK of up to ~1,510 B before its terminal re-mints anything: the cure is the kit's
buy-back ledger, never a re-mint upward, and the decision is the chair's to execute and the owner's to
be spared. ⚠ **TWO SIBLINGS' PACKETS STILL QUOTE THE STALE CEILING `1396015`** (EM-R3's §5.8 table and
EM-R4's byte note, both written before the EM-T14/T15 buy-backs lowered it twice): their own rows to
re-stamp at their own pre-proofs — named here because the holder must not sum against two different
ceilings. ⚠ The closure SIZE differs by walker across the train's packets (187 · 188 · 220 · 222 · 228
· 230) because each walks a different entry set and edge form; this member's **222 first-party files**
is its own as-of measurement, and MEMBERSHIP — not size — is the fact any member asserts.

This member carries NO row on either budget test and edits `tests/build/generationWorkerLazy.test.js`
NOT AT ALL; the terminal's ONE real build with per-module attribution is the judge. ⚠ This is the
largest single byte price on its train, and R2 in the compile report offers the chair a cheaper,
narrower shape (two declared keys instead of nineteen, about +80 B) at the cost of a walker that can
only assert soundness.

## §6 · Exact contracts

### Inputs and outputs

`runPipeline(initialContext, rng, options = {})` — **the signature is unchanged, the refusal's
sentence is unchanged, and `onStrictViolation`'s payload shape is unchanged.** One thing changes:

```js
// BEFORE
const choosers = step.provides || [];
// AFTER
const choosers = (step.provides || []).filter((k) => !_TRANSIENT_CHOOSERS.has(k));
```

`supplied`, `missing`, the message and the `{ step, kind: 'pin', keys }` report are all computed from
that list exactly as they are today, so a step with no transient chooser behaves identically in every
respect (measured: both landed refusal sentences byte-identical, §5.3).

`_TRANSIENT_CHOOSERS` is a module-scope frozen `Set` declared immediately beneath
`const _PINS_KEY = '__pins';`, in the shape `_LEDGER_KEYS` already uses — a named set of ctx keys the
runner exempts from a rule, stated by name rather than left to an accident of when it happens to be
seeded. Its nineteen members, in codepoint order:

```js
'catalogForTier', 'categoryToggles', 'generationContentProfile', 'generationContext',
'goodsToggles', 'institutionToggles', 'neighbourEconBias', 'neighbourFacBias', 'neighbourProfile',
'noMagic', 'powerIntent', 'priorityMagicEffective', 'rawNeighbour', 'resolvedTerrain',
'servicesToggles', 'structural', 'threat', 'townPlus', 'tradeRoute',
```

⛔ **WHY THE SET IS DECLARED HERE AND NOT IMPORTED.** `src/domain/generation/generationForkRegistry.js`
is PRODUCTION-UNREACHABLE by its own header's law — nothing under `src/` imports it, and acquiring a
production importer is a STOP, not a refactor — and a generator-to-domain import edge routes its
transitive closure into the EAGER `engine-core` chunk (`vite.config.js :: computeEngineSharedDomain`).
So the runner declares the set, the CREATE holds the two EQUAL IN BOTH DIRECTIONS, and drift reds by
name instead of rotting.

`getStepMeta()` gains ONE row on the object it returns, computed from the same set:

```js
transient: (step.provides || []).filter((k) => _TRANSIENT_CHOOSERS.has(k)),
```

Every other row of that object is unmoved and no consumer of the existing rows is affected (measured:
`tests/generators/stepMetadataSync.test.js`, `tests/joins/mutationOrder.test.js`,
`tests/lint/heldKeyWriterCensus.walker.test.js` and `tests/lint/generationForkRegistry.contract.test.js`
all read named rows, never an exhaustive key set).

### The leaf

`pinsFrom(record, layer, declarations, engine)` — **the signature, the four passes, the ordering and
the injected-handle law are unchanged.** Three additions, and ⛔ **not one new import**:

- `rosterOf` carries each step's `transient` list out of the injected handle with the same defensive
  filter it already applies to `provides` (a handle that cannot answer yields `[]`, which is
  fail-closed: the closure then behaves exactly as it does today);
- `pinsFrom` derives `transientKeys` once, from the roster, and `isTransient(key)` reads it;
- `closureFor` SKIPS a transient key: it is neither required to be `held` nor added to `need`, and it
  is not queued from a provider's `provides`. Everything else — the fixpoint, the `null` on an
  unheld record-backed key, the `unapplied` row, PASS 3's clone and PASS 4's leaf write — is unmoved.

⛔ **`missing` IS DEliberately UNCHANGED.** It still reports every chooser the record does not hold,
transient ones included, because it is a report about the RECORD rather than about pinnability, and
`tests/property/dmLayerGoldenIsolation.test.js`'s landed A1 asserts only its shape. §12 records the
figure the build lane measures for it.

### Absence rules

- `options.pins` **absent or `null`** ⇒ `pinned` is false and **not one byte of this member executes**;
  today's behaviour EXACTLY. This is the golden's protection, by construction.
- `options.pins` **`{}`** ⇒ `pinned` is false; unchanged.
- a step with **no** transient chooser ⇒ the filtered list IS `provides`; unchanged in every respect.
- a step ALL of whose choosers are transient ⇒ the filtered list is empty, `supplied.length` is 0 and
  the rule cannot fire. No registered step is in that class today (measured: the six steps that carry
  a transient chooser all carry a record-backed one too).
- an engine handle that answers no roster ⇒ `transientKeys` is empty ⇒ the closure is today's.

### Determinism, flag, lifecycle, receipts

- **Hash/fork key:** unchanged. No stream is minted, forked or advanced by this member; the
  declaration is read outside every `setActiveRng` window and the leaf runs no PRNG at all.
- **No-draw behaviour:** unchanged. A transient chooser that is NOT pinned draws exactly as it does
  in an unpinned run; a transient chooser that IS pinned is still handed back by `chooseOrPin`.
- **Golden posture: UNCHANGED.** A5 proves it over a stride and A6's control proves a dormant layer
  still re-derives the committed record byte for byte, 63/63.
- **Flag:** `NONE`. Wave 1 is headless; the door stays dark behind `TIER_GATE[tier].settlementEditor`.
- **Lifecycle:** create / read / persist / reload / undo / import / public veil — all unchanged; this
  member writes no record key, persists nothing and adds no reader of a save-time key.
  **Regenerate** is the member: a record-built bag for a power edit now exists at all.
- **Receipts and privacy:** no new message, no new refusal reason, no rendered figure ⇒ prose-numerics
  is not owed.

### User-facing copy

`NONE`. This member adds no string a user sees. ⛔ The nineteen literals it does add are ctx KEY
NAMES, already spelled in the steps' own `provides` arrays; none carries an em dash or an exclamation
point, measured with the voice walker's own espree extractor (`pipeline.js` stays at **em 2 · bang 0**,
its baselined row; the `src/generators` directory total is **28 em against a budget of 28**, so a
single new one would red).

## §7 · Exact change manifest

⭐ **GENERATED FROM `EM-R1b.manifest.json` AND PROVED SET-EQUAL WITH IT** (§P9(d)).

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `MODIFY` | `src/generators/pipeline.js` | `runPipeline`: the `_TRANSIENT_CHOOSERS` declaration beneath `const _PINS_KEY = '__pins';`, the `choosers` expression inside the partial-pin block, and one row in `getStepMeta`'s returned object | **+7 eff** (measured 150 → 157) | Declare the frozen set with the docblock §6 spells, filter the rule's `choosers` list with it, and add `transient:` to `getStepMeta`'s row. ⛔ `chooseOrPin` is BYTE-IDENTICAL: the rule changes, never the seam. ⛔ `const _PINS_KEY = '__pins';` and `const _LEDGER_KEYS = …` stay byte-identical and the insert goes BENEATH them. ⛔ No em dash and no exclamation point in any new literal (the directory sits AT its em budget). ⛔ No numeral: the tuning inventory reads 0 / 0 here and must still. |
| `MODIFY` | `src/domain/edit/dmLayer.js` | `pinsFrom`: `rosterOf`'s returned row, the `transientKeys` / `isTransient` pair, two lines of `closureFor`, and the `StepRoster` typedef | **+3 eff** (measured 204 → 207) | Carry `transient` out of the injected roster with the same defensive filter `provides` uses, and skip a transient key in the closure. ⛔ **NOT ONE NEW IMPORT** — the leaf imports nothing from `src/generators` and nothing from the register. ⛔ `rederive`, `applyEdit`, `layerRead`, `mintDmId`, `REDERIVE_UNAPPLIED_REASONS` and passes 1, 3 and 4 are untouched. ⛔ `missing` keeps today's meaning. |
| `CREATE` | `tests/lint/transientChooserRegistry.walker.test.js` | the walker + A1–A3 | 250 eff | ⭐ **ADDS THREE `it`s UNDER ONE `describe`** — the same three §9 homes here and the same three the deferred lighting row prices. Flat literal `it(...)` under ONE literal `describe`; `it`/`test`/`describe` each bound EXACTLY ONCE; no `.each`, no loop, no conditional registration, no nested describe. ⛔ **BOTH SETS IMPORTED FROM THEIR PRODUCERS** and never re-typed; ⛔ reads NO source, NO `dist` and spells NO repository root, so `moduleScopeCwdRatchet`'s `tests/lint` population is unmoved. Every negative carries `// anchored:` on the line immediately above the `expect` itself. Copy the arm shape of `tests/lint/recordRegisterTotality.walker.test.js`. Carry a `CANNOT-CATCH:` header block naming, at least: a declared key no step provides is invisible to both directions. ⭐ **JUDGMENT 196's TITLE LAW, MEASURED:** every new `it` title BEGINS `A<n> — EM-R1b: ` so it is UNIQUE in `tests/` — measured at `6a0178b7b`, no test TITLE anywhere in `tests/` contains `EM-R1b` (the one estate hit is a COMMENT line in `tests/lint/heldKeyWriterCensus.walker.test.js`), while this file already carries the bare ids this member reuses, and `vitest -t` is a REGEX. |
| `TEST` | `tests/generators/pipelinePinnedMode.test.js` | a NEW top-level `describe` holding A4–A5 | n/a | ⭐ **ADDS TWO `it`s UNDER ONE NEW `describe`** (`'EM-R1b — a recordPath-less chooser is not counted against a record-built bag'`), APPENDED after the file's last landed `describe`. ⛔ **WEAKEN NOTHING**: EM-P0's, EM-B2a2's and EM-R1's three describes and thirteen `it`s stay byte-identical, INCLUDING both landed partial-pin sentences, which A4 re-asserts as its own positive control. ⚠ **STAGE 1 MOVES THIS FILE UNDER THIS MEMBER, AND THE OVERLAP IS MEASURED AT ZERO:** EM-R2 version 5 rewrites `EM-P0 › A2` ALONE — it replaces `:191` `const pins = pinsFromContext(first);` and `:195` the `JSON.stringify` compare with a record-built bag driven through `rederive`, renames that ONE title count-neutrally, keeps `:192` and `pinsFromContext`, and adds ZERO `it`s (so the three landed describes still hold thirteen). This member's APPEND touches none of those lines, and its contract moves that arm's OUTCOME by zero over the census corpus (§0.3). ⛔ **"BYTE-IDENTICAL" IS READ AGAINST THE LANE'S OWN HEAD**, which already carries EM-R2's rewrite — never against this packet's compile-era quotation of the file. `EM-R1b.remeasure.sh` §4 re-proves all of it at the flip. ⭐ **JUDGMENT 196's TITLE LAW, MEASURED:** every new `it` title BEGINS `A<n> — EM-R1b: ` so it is UNIQUE in `tests/` — measured at `6a0178b7b`, no test TITLE anywhere in `tests/` contains `EM-R1b` (the one estate hit is a COMMENT line in `tests/lint/heldKeyWriterCensus.walker.test.js`), while this file already carries the bare ids this member reuses, and `vitest -t` is a REGEX. |
| `TEST` | `tests/property/dmLayerGoldenIsolation.test.js` | a NEW top-level `describe` holding A6–A8 | n/a | ⭐ **ADDS THREE `it`s UNDER ONE NEW `describe`** (`'EM-R1b — the power card stops being refused'`), APPENDED after the landed suite. Reuse the file's own `worldFor`, `layerOf`, `rootKey`, `ENGINE` and `DECLARATIONS` helpers rather than minting new ones. ⛔ EM-B2a4's seven landed arms stay byte-identical, including A1's closure equality and A7's dormant-golden property. ⚠ **MEASURED: THIS FILE ALREADY HOLDS `it`s WHOSE TITLES BEGIN `A6 — `, `A7 — ` AND `A8 — `** (EM-B2a4's seven arms are A1, A2, A4–A8), so the bare ids this member reuses would be ambiguous to `vitest -t`. ⭐ **JUDGMENT 196's TITLE LAW, MEASURED:** every new `it` title BEGINS `A<n> — EM-R1b: ` so it is UNIQUE in `tests/` — measured at `6a0178b7b`, no test TITLE anywhere in `tests/` contains `EM-R1b` (the one estate hit is a COMMENT line in `tests/lint/heldKeyWriterCensus.walker.test.js`), while this file already carries the bare ids this member reuses, and `vitest -t` is a REGEX. |
| `REGISTER` | `scripts/mutation-coverage-manifest.json` | the `invariants` row for `tests/lint/transientChooserRegistry.walker.test.js` | +4 eff | Add the row SURGICALLY beside its siblings; ⛔ never re-serialise the manifest whole. `kind: 'rationale'` with an inline reason of at least 40 characters saying the file's catching power is an EXACT PARTITION over two imported sets asserted in both directions, which a mutant could only delete rather than perturb, and that the mutants are planted in `src/generators/pipeline.js` instead, per §P6. RE-MEASURED AS OF `6a0178b7b`: `invariants` holds **727** rows and `uncoveredBaseline` is **186** (an integer, not a list); the DELTA is **+1** and `uncoveredBaseline` is **UNMOVED**. ⚠ **EM-R2 version 5 ADDS ITS OWN ROW AT STAGE 1's FLIP**, so the lane's HEAD will read **728** before this member's edit: the ABSOLUTE moves, the DELTA does not, and the two `rowKey`s differ (TOOL-27), which is why the dry validator reports no duplicate on this path. `rowKey` = `invariants['tests/lint/transientChooserRegistry.walker.test.js']`, ABSENT from the live manifest. ⚠ `ENFORCER_DIRS` was READ at this tip and holds eight directories including `tests/generators`, so a CREATE there would owe its own row; this member CREATEs only under `tests/lint`. |

**Generated artifacts: `NONE`.**

> ⛔ **THE LIGHTING CENSUS IS A DEFERRED ROW, NOT AN ARTIFACT OF THIS PACKET.**
> `tests/lint/.lighting-census-baseline.json` — a named INTERIOR RED, **delta only**, all five figures
> in the register's own order: **`files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +3`.**
> `+8` is DERIVED and the count law binds it (§P9(f)): the CREATE's **three** `it`s plus the two TEST
> rows' **two** and **three**; `+3` suites is one new `describe` in each of the three files.
> `EM-R1b.count-prover.mjs` refuses the packet if §7's own words, §9's matrix, §10 and this row ever
> disagree. The predicted red MESSAGE has the shape `expected <N+1> to be <N>`. ⛔ No refreeze is run
> here. ⚠ `+1 credited` is CONDITIONAL on the new file not parking: the build lane runs the walker's
> own `parkReasonsFor` over it and quotes the output (expected `[]`).
>
> ⛔ **THE WIRING CENSUS IS THE SECOND DEFERRED ROW.** `docs/content/wiring-census.json` —
> `stamp.producerIndexFiles` **+0** against **1,185 as of `6a0178b7b`**, `stamp.files` **UNMOVED** (7 entries,
> none of them this member's), `totals.*` **UNMOVED**. The path appears in NEITHER §7 NOR the manifest
> NOR `checks`, and no lane runs `node scripts/wiring-census.mjs`.

## §8 · Ordered coding sequence

1. Run §P10.3's six pre-seal measurements plus §4's seventh. Any failure STOPS before the seal.
2. **RED FIRST, at the leaf.** Write A6 into `tests/property/dmLayerGoldenIsolation.test.js` and run
   it against the UNCHANGED tree: it must be RED at `step_not_pinnable` 63/63. Quote the COUNT LINE —
   a log with no test count did not run (§P7).
3. **RED FIRST, at the runner.** Write A4 into `tests/generators/pipelinePinnedMode.test.js` and run
   it: the record-built power bag must THROW the whole sentence. Quote the count line.
4. Edit `src/generators/pipeline.js` per §7 — the declaration, the filtered `choosers`, the
   `getStepMeta` row. Re-run steps 2 and 3: A4 green, A6 still RED (the leaf has not moved yet).
5. Edit `src/domain/edit/dmLayer.js` per §7. Re-run: A6 green, and its bag is exactly
   `[powerStructure]`.
6. Write `tests/lint/transientChooserRegistry.walker.test.js` with A1–A3.
7. Write A5, A7 and A8 into their homes; add the mutation-coverage row surgically.
8. Run the focused battery of §10, then the mutants of §P6, then the pre-seal count prover.

Bounded algorithm (the rule, in three lines):

```text
1. choosers := step.provides MINUS the transient set      (the runner)
2. a key in the transient set is never required of a bag, never reported missing,
   never added to the closure and never queued from a provider (the leaf)
3. everything else — the message, the report, chooseOrPin, the closure's fixpoint,
   the clone and the leaf write — is byte-identical to today
```

## §9 · Acceptance matrix

| ID | Case | Fixture/input | Required observation | Test home |
|---|---|---|---|---|
| A1 | The declaration IS the register, both directions | `GENERATION_TIER1` and `getStepMeta()`, each imported from its producer | the union of every step's `transient` equals exactly the `provides` keys whose EVERY tier-1 `provides` row carries `recordPath: null` — **19 keys over 6 steps** at this base — as a BOTH-DIRECTIONS equality; ANTI-VACUITY: both sets are refused if empty and the roster is asserted at its live length | `tests/lint/transientChooserRegistry.walker.test.js` |
| A2 | Soundness, spelled on its own so a red names which half broke | the same two producers | every declared transient key is a `provides` key OF THE STEP THAT DECLARES IT, and no declared key has a non-null `recordPath` at any provider; `// anchored:` on the line immediately above the negative, with the positive control in the same arm (a known record-backed key IS rejected by the same predicate) | same |
| A3 | ⭐ The leaf reads the SAME declaration (the anti-drift arm) | `pinsFrom` driven with an INJECTED STUB roster and a stub declaration set — no real registry, no generation | with a key declared `transient` the closure OMITS it and pins the rest; with the identical roster minus that declaration the same call refuses `step_not_pinnable`. A leaf that stopped reading the channel reds by name; a runner and a leaf that drift apart cannot both pass | same |
| A4 | ⭐ THE RULE, at the runner | the census corpus; a record-built bag for `generatePower` (the structure alone, read at its `recordPath`) | the run completes with NO refusal on **63 of 63** rows and the held structure is taken; **RED-FIRST**: the same bag at the base throws the whole sentence, quoted; **POSITIVE CONTROL in the same arm**: the partial bag `{ relationships }` STILL throws its own whole sentence and still reports `keys: ['npcs','factions','conflicts']`, so the narrowing cannot go silently wide | `tests/generators/pipelinePinnedMode.test.js` |
| A5 | The seam is untouched and the unpinned path is inert | one corpus row with BOTH power choosers pinned, one with no pins, and a golden stride | a SUPPLIED transient chooser is still handed back by `chooseOrPin` (a planted sentinel arrives in `generatePower`'s patch); with `pins` absent the runner takes no new branch and a stride of the committed golden moves **zero** rows | same |
| A6 | ⭐ THE CURE, through the REAL `rederive` | the census corpus; a DM `powerSeat.holder` override in a layer, the injected ENGINE and DECLARATIONS the file already builds | `unapplied` is EMPTY on **63 of 63** rows and the bag `pinsFrom` built is exactly `['powerStructure']`; **RED-FIRST**: at the base every row returns `step_not_pinnable`; ANTI-VACUITY: the layer is asserted non-dormant and the closed reason set is read from the leaf | `tests/property/dmLayerGoldenIsolation.test.js` |
| A7 | ⭐ The two channels agree | every editable collection the declaration table names, over the corpus | every bag `pinsFrom` builds is one the runner ACCEPTS — zero `kind: 'pin'` violations collected and nothing thrown; ANTI-VACUITY: at least one bag is non-empty and the collection set is imported from `fieldDeclarations.js`, never re-typed | same |
| A8 | Case 2 is NOT this member's, and the refusal survives by name | the census corpus; a DM `institution.state` override | the institution edit still returns `step_not_pinnable`, and the blocking key is `generationRepairs`, whose `recordPath` is `record.generationCoherenceReceipt.repairs` — present on the record but unreadable by a bag builder keyed on the ctx-key NAME; the closed refusal set is still exactly two | same |

This table is the entire edge-case budget — **8 of 8**. Omit nothing; add nothing.

## §10 · Verification commands

```sh
# Focused static checks (no gate slot needed)
npx eslint src/generators/pipeline.js src/domain/edit/dmLayer.js \
  tests/lint/transientChooserRegistry.walker.test.js \
  tests/generators/pipelinePinnedMode.test.js tests/property/dmLayerGoldenIsolation.test.js
npm run typecheck:ratchet          # tsconfig.full.json — measure THIS member's two src/ paths (§P7)
npm run typecheck:domain:strict    # tsconfig.domain-strict.json — dmLayer.js lives under it

# The test directories this member touches, ONE directory per gated run
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 tests/generators

# ⭐ THE CREATE DIRECTORY, RUN WHOLE, MINUS THE LIGHTING WALKER (§P7)
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 tests/lint \
  --exclude=tests/lint/sovereigntyLightingContract.walker.test.js          # MUST exit 0

# The lighting walker ALONE — the member's measured INTERIOR RED, message quoted
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/sovereigntyLightingContract.walker.test.js                    # expected NONZERO

# The goldens and the leaf's own property battery — UNMOVED is the claim, so they are run
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 tests/property

# The leaf's other consumers
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/domain/dmLayer.test.js tests/domain/tracePartition.test.js tests/store/editSlice.test.js

# PRE-SEAL, from the KIT PATH, never placed in the tree (§P9)
node <kit>/packets-waiting/EM-R1b.count-prover.mjs <kit>/packets-waiting/EM-R1b.md \
  <kit>/packets-waiting/EM-R1b.manifest.json

# PRE-REPORT, no gate slot: the TypeScript compiler API over THIS member's own src/ paths
node <scratch>/typecheck-paths.mjs src/generators/pipeline.js src/domain/edit/dmLayer.js
```

**Sealed `checks`** (files, never the `tests/lint` directory; the lighting walker is NEVER sealed; the
two dist-reading budget tests are NEVER sealed; the validator last) — the array is in
`EM-R1b.manifest.json` and is COMPUTED, not recalled: every `tests/` file that names `pipeline.js` or
`dmLayer.js`, one directory per argv array, minus the three named exclusions. ⭐ **RE-COMPUTED AT
`6a0178b7b`: 21 and 5** — and the derivation is re-run at the lane's HEAD, because STAGE 1 MOVES IT.
At the flip the figures are **22 and 6**: EM-R2 version 5's CREATE
`tests/generators/rosterFinalUnderHeldRoster.test.js` imports `runPipeline` from `pipeline.js` and so
JOINS the governing set (it is carried in the `tests/generators` array below, and
`EM-R1b.remeasure.sh` §6 prints whether it exists — if EM-R2 leaves the train, the chair DROPS that
one row), and `pipelinePinnedMode.test.js` becomes a `dmLayer.js` importer through EM-R2's own rewrite
while already being sealed. Nothing else joins: EM-R0b's four CREATEs name neither file.

**Browser suite: NOT OWED and no spec is named.** This member touches no `src/components/**`, no
route, no `data-testid` and no accessible name.

## §11 · Mandatory STOP conditions

In addition to `PACKET_STANDARD.md`'s list and §P8's, this member STOPS when:

1. ⛔ **The refusal does not reproduce at the lane's own HEAD** (§4's seventh measurement): if a power
   edit already comes back applied, this member's premise is dead exactly as EM-R2b's was, and the
   lane STOPS with that measurement rather than building.
2. ⛔ **A landed partial-pin sentence moves by one byte.** Both are re-asserted by A4's own positive
   control; the cure is never to edit the landed arm.
3. ⛔ **`tests/generators/dataFlowContract.test.js` reds.** Its producer predicate counts
   `provides ∪ mutates` and NOT `scratch`, so a member that "cures" this by re-declaring `powerIntent`
   has broken the data-flow contract. The roster is kept; only the rule moves.
4. Any golden or the prose manifest moves.
5. ⛔ **The worker bundle RISES beyond the byte-arm holder's stated bound, or any signed ceiling would
   be re-minted upward** (§P11.2 — the owner's). The buy-back is the kit's ledger, never a re-mint.
6. A number lands under `src/domain` or `src/generators` that is a genuine simulation dial (§P2.17 —
   tuning is the owner's). This member writes no numeral.
7. An instrument named in §P2.21 reds with a figure this packet did not predict; the arm's own message
   decides and a figure adopted by hand is the failure.
8. ⛔⛔ **THE BUILD LANE REACHES FOR THE INSTITUTION CARD.** §0.4's second defect — a bag builder that
   reads a chooser's `recordPath` instead of its ctx-key name, and the ruling on dragging a re-derived
   key (`stress`) into a bag — is a SLOT for a new member, not an improvisation inside this one. A
   lane that widens `pinsFrom` to read nested record paths is outside this manifest and STOPS.
9. ⛔ **The declaration is made complete by hand against anything but the register.** The set has ONE
   source: `GENERATION_TIER1`'s `recordPath`. A key added because it "looks transient" reds A2 by name,
   and a key omitted to buy bytes reds A1 — that trade is R2, the chair's, made before the build.
10. ⛔ **A spec, a walker or a ratchet would be weakened, widened or skipped** to make this member pass.

## §12 · Completion receipt

- Base SHA; dispatch bundle and seal identity:
- §4's seventh pre-seal measurement: `step_not_pinnable` at HEAD, with its count line (expected 63 / 63):
- Exact changed files and effective-line counts (eslint `Linter`, `skipBlankLines` + `skipComments`), before and after, for both `src/` files (expected 150 → 157 and 204 → 207):
- Acceptance cases A1–A8, executed, with A4's and A6's RED-FIRST count lines quoted:
- ⭐ A4's positive control, both landed partial-pin sentences quoted byte for byte, and the strict report's `keys` array:
- ⭐ A6's bag, printed: `['powerStructure']` on every row, and the `unapplied` envelope EMPTY:
- ⭐ A7's collected violations over the corpus (expected zero `kind: 'pin'` on every editable collection):
- ⭐ A8's blocking key, printed with its `recordPath`, and the closed reason set read from the leaf:
- ⭐ **EM-R2 version 5's `EM-P0 › A2` IS UNMOVED BY THIS MEMBER:** re-run its arm shape over the corpus before and after the edit and quote both (expected byte-identity count EQUAL in both directions, the bag and the unapplied reasons identical on every row; at `6a0178b7b` the pre-proof read 6/63 both ways, and EM-R2's own gate carries it to 63/63):
- ⭐ **THE NO-OP ROOT OVERRIDE ON THE POWER CARD RE-DERIVES THE RECORD BYTE FOR BYTE:** 63 of 63 with an EMPTY `unapplied` (the tip refuses all 63), quoted:
- ⭐ **THE TRAIN's SUMMED WORKER PRICE, AS ITS BYTE-ARM HOLDER** (§5.8's table re-taken at the terminal's ONE real build; the sum's upper bound is +1,510 B and the ceiling is `1392364` with ZERO SLACK — ⛔ a rise is the OWNER's and the cure is a buy-back):
- ⭐ **THE PAIR WITH EM-R3, MEASURED AT THE COMPOSED TIP AND NOWHERE ASSERTED IN THIS PACKET:** with EM-R1b alone the DM's `powerStructure.governingName` is overwritten downstream (the finished record unmoved in 62 of 63 rows at this compile); with EM-R3 landed beside it, the survival figure is EM-R3's arm to state. Quote both:
- `missing`'s figure for a power edit, before and after (it still reports every unheld chooser):
- The §P6 mutants: each planted in `src/generators/pipeline.js` or `src/domain/edit/dmLayer.js`, each reddening its OWN acceptance title, each restored to the exact pre-mutant SHA-256:
- The golden stride: rows checked, rows moved (must be zero), and the committed fixture's identity:
- `parkReasonsFor('tests/lint/transientChooserRegistry.walker.test.js')` output, quoted (expected `[]`):
- Lighting census DELTA, all five figures, attributed by execution (⛔ never an absolute; ⛔ no refreeze here):
- Mutation-coverage manifest: `invariants` before and after (expected the LANE's OWN base +1 — 727 as of `6a0178b7b`, 728 once EM-R2 version 5 has landed) and `uncoveredBaseline` (expected UNMOVED at 186):
- Wiring census: `stamp.producerIndexFiles` before and after (expected UNMOVED, +0; 1,185 as of `6a0178b7b`) and `stamp.files` (expected UNMOVED at 7 entries):
- Entropy census both arms (expected 36 / 47, +0), tuning inventory per file (expected 0 named / 0 decimals on both, unmoved), voice per file (expected `pipeline.js` em 2 · bang 0 and `dmLayer.js` no row):
- ⛔ `tests/lint/moduleScopeCwdRatchet.test.js`: the `tests/lint` population before and after (expected UNMOVED at 17), and the CREATE's own root-free construction quoted:
- ⭐ Judgment 196: every new `it` title quoted, each proven UNIQUE in `tests/` (`git grep -c -F` on the title text), and the runner file each arm ran under:
- The worker byte delta as measured at the terminal's one real build, with per-module attribution showing only this member's module moved (predicted +400 to +700 B; a rise beyond the holder's bound is §11 item 5):
- `tests/lint --exclude=…sovereigntyLightingContract…` — exit (must be 0):
- The lighting walker alone — the named interior red, message quoted:
- Both typecheck configurations (`typecheck:ratchet`, `typecheck:domain:strict`):
- §7-versus-capsule set-equality proof, re-run at the landing:
- Generated artifacts: `NONE`
- Deviations: `NONE | STOP`
- Out-of-scope observations, without investigation:
- ⭐ §0.4's slot, restated with its measured table so the chair can charter it:
- Judgment calls: `NONE` — §5.4's R1 and §5.8's R2 are the chair's and this packet records both.
