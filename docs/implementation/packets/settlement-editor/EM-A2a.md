# Settlement editor / EM-A2a — the pool machinery and the eleven TABLE pools: one seeded roller on its own stream, and every closed vocabulary read at its lawful source

- **Status:** `LANDED`
- **Landed at:** `e65dd78dc17f3dfd04a1bc5e6775c3fbbe63e635` — the twenty-eighth landing — train EM-T11 (version 8, re-stamped to the fifth amendment): the POOLS leaf (src/domain/edit/pools.js, 56 effective; editPools.test.js, eight arms over eleven pools), tuning inventory unmoved with its positive control live, +0 B; the lane re-sealed itself at 80e855f26 through the --reseal door (the second facet of judgment 125) — cured at `d9cf9156504216a7203036a671fbd56e15cd5afe` (version 10, judgment 131: the goods roster row for `pools.js`, one comment line in `src/domain/goods.schema.js`, after the train verifier's pass-2 STOP-1)
  ⚠ The status value above stands ALONE on its line because `parsePacketHeader`
  (`scripts/implementation-packets.mjs`) anchors the status row at end-of-line (J-TEWF1B-1;
  interim rule 5).
- **Packet version:** 10
  ⭐ **TRAIN RE-SPELLING (the chair, judgment 132, 2026-09-21 16:1x; the train verifier's pass 1C STOP-2 and FIX-7).** The train carried this packet's body and entry at version 8 (two rows) while its landed build touches three paths — the version-9 and version-10 packet commits lived only on the lane branch, whose seal binds them. The LANDED entry on the train is re-spelled at version 10 (four rows) by the chair, the acceptance row's `// anchored:` claim corrected (FIX-7; none is owed) and the version-8 "modifies ZERO" sentence corrected; the version number stays 10 because the sealed build is version 10's. LAW for the placement checklist: a member's packet VERSION commits are picked onto the train with its build (EM-B2a2's were; this one's were not).
  ⭐ **VERSION 10 (the chair, judgment 131, 2026-09-21; the train verifier's pass-2 STOP-1, `findings/VERIFY-EM-T11-2026-09-21/VERIFY-EM-T11.pass2.md`).** `pools.js` imports `src/data/resourceData.js` (a goods identity half-table) and the WEAVE ST-2 roster arm of `tests/build/vendorPdfLazy.test.js` requires every direct importer of an identity half-table to carry a roster row in `src/domain/goods.schema.js` — executed at the composed tip: `1 failed | 564 passed` where the tip before this member's pick was `565 passed`. Version 10 adds ONE §7 row: a `MODIFY` of `src/domain/goods.schema.js` — one comment line in that roster, `R | l | identity | direct | src/domain/edit/pools.js`, in the exact shape of its neighbouring rows — and names the roster among the governors this packet's register list missed (the fourth such miss across the train: the sixth amendment lists WEAVE ST-2 beside the entropy site count). No pool, arm, check or count moves; the builds under seals `bb3c0789…` and `f6fb6012…` stand as committed; the roster line is made by the lane under the version-10 seal and proven by that arm.
  ⭐ **VERSION 9 (the chair, judgment 128, 2026-09-21; the build's gate at queue window 28 job 5, section c1, `lane-em-a2a-t11-scratch/gate/c1-governors.log`).** Version 8 priced the entropy-root census NOT OWED by reading the walker's READ-SITE arms alone; the walker ALSO pins the `createPRNG(` SITE COUNT (`tests/lint/entropyRootCensus.walker.test.js`, the EP-0 closure record, `expected 36 to be 35`), and this leaf's one root composition is a new site: `src/domain/edit/pools.js:191` — `rollFrom`'s ROOT composition `createPRNG(`edit-pool:${poolId}:${String(seed)}:${entryId}:${rollIndex}`)` on its OWN stream, the shape ruling 4 ordered from `instantNpc`. Not one draw of the world's sequence is consumed and the same seed and key roll the same value forever (THE PROMISE holds), exactly the rationale of the store's size-gate re-record of 2026-09-19 (45 → 46 whole-src). Version 9 therefore adds ONE §7 row — a `TEST` row on the walker that re-runs the closure record (35 → 36 in `src/domain`, 46 → 47 whole-src; the test's title said 45 while its own assertion said 46 and is made truthful) with a comment naming the site — corrects P2.16 and the two prose sentences that priced the census NOT OWED, and narrows the §11 STOP to any OTHER movement of that walker. No pool, arm, check or count moves; the eight arms and the two CREATE rows stand; the build made under seal `bb3c0789…` is re-applied BYTE-IDENTICAL under the version-9 seal and the walker edit is made by the lane under that seal.
  ⭐ **VERSION 8 (the chair at placement, 2026-09-21; judgment 102 Q2): re-stamped to the fifth amendment's preamble hash; the interim-rules row struck. No other byte moves. The prior version is 7.**
  - ⭐⭐ **Version 7 (2026-09-21, independent PRE-PROOF lane, read tip
    `583f8f64448b21a58fa64d0b757e93ffffcc7c2b` = train EM-T10's promotion commit; the integration
    tip is its parent `e348d59b6`) re-measures VERSION 6 against the live tree and makes SIX
    edits, each one a measurement, none of them a preference.** **(1) A2's tier claim is
    REFUTED and replaced by the measured truth:** `getInstitutionsForTier` is
    `institutionalCatalog[t] || {}` and has NO random/custom case — `'random'`, `'custom'`,
    `'nonsense'`, `'all'`, `''`, `null` and `undefined` ALL return an EMPTY `Set`, so version 6's
    anchor was vacuous and its **58** came from this packet's own `?? 'village'`. The special case
    lives in `getInstitutionalCatalog` (*"sane default for previews"*), which this leaf does not
    import. Measured further: **a stored settlement's `tier` is ALWAYS a concrete catalogue tier**
    — `resolveConfig.js:92-94` is the one resolver, `popToTier`'s codomain is `TIER_ORDER`, and in
    the whole of `src/` exactly two expressions put `settType` in a key named `tier`, both of them
    analytics/eligibility payloads and neither a settlement record — so A2 now anchors on a REAL
    tier. **(2) `Depends on: EM-A1` stated a fact EM-A1's LANDED code denies** (its walker asserts
    `POOLS` membership NOWHERE and assigns that half to this packet): the clause is corrected and
    **A1 carries EM-A2a's OWN totality arm**, a both-ways set relation against EM-A1's eleven
    declared pool ids. **(3) The last stale absolutes leave the packet** (§P3.5 / interim rule 9):
    `stamp.producerIndexFiles` is **+1** and no number; the tip's committed stamp is 1175, not the
    1172 version 6 quoted. **(4) The READY header's stamp form is spelled out** — measured with the
    tip's own parser, a BARE sha leaves `verifiedBranch` null and reds
    *".packetPath READY header must name a non-blank verified branch"*; the stamp is
    `` `<branch>` at `<40-hex>` ``. **(5) FOUR MORE GOVERNORS were hunted down and priced**, each
    executed with its own detector and its own convicting control: the determinism ban-coverage
    pin, the golden-freeze roster and the size ratchet — three NOT OWED, by measurement; the
    entropy-root census IS OWED (version 9: the walker also pins the `createPRNG(` SITE COUNT — §7's TEST row). **(6) The lighting delta and the count law are now EXECUTED rather than
    predicted.** ⛔ **The contract, the budget, the acceptance COUNT (8), the pool COUNT (11), the
    import fence, the stream key and every non-goal are UNCHANGED from version 6; no instrument was
    weakened and no baseline widened.**
  - ⭐ **Version 6 (2026-09-21, compile lane, read tip `429141e2d1c21cb5ee9dd5005ed1a77bbc9b3cd8`)
    executes the chair's judgment 79 (six rulings), judgment 86's addresses and judgment 88's R5′.**
    Version 5 was BLOCKED at `e80a6a3f4` on three measured contradictions, and all three are cured
    here by RE-AIMING, never by weakening an instrument: **(1)** `NpcStatus` has SEVEN members since
    `95e494bdb` (EM-B1d), so `npc.status` is seven and A8 asserts `jailed` **PRESENT** — R5 is
    answered by the tree and now has the §13 row it lacked; **(2)** the leaf reads
    `src/domain/institutionLookups.js` (EM-P4's stable domain address) instead of
    `src/generators/lookups.js`, so `tests/build/domainGeneratorsBoundary.test.js` — a shrink-only
    ratchet whose own message forbids widening the baseline — stays green, and so does
    `catalogTierGateParity` A5, which version 5's spelling ALSO reddened (measured this lane, and
    named in no earlier document); **(3)** the two state vocabularies are IMPORTED from the homes
    EM-P4 mints (`NPC_STATUS_VALUES`, `ENTITY_STATUS_VALUES`) rather than mirrored as frozen
    literals inside this leaf, which is the only variant that leaves
    `statusUnionTotality.walker`'s DERIVED trigger at `['dead','exiled','retired']`. Also:
    **(4)** terrain and culture re-aim at `src/domain/worldFactOptions.js` (EM-P3's landed address;
    identity-equal values, twelve modules and `rngContext.js` out of the leaf's closure);
    **(5)** `src/domain/dossier/powerStrata.js` LEAVES the A7 fence in both spellings;
    **(6)** the count law is swept (ten → eleven in the title, A6 and three capsule strings; the
    line estimate re-derived ≈148 → **≈140**; `src/data/monsterThreat.js` added to the capsule's
    A7; the missing **R5** row added; the three stale `grep -c` notes refreshed; the Status value
    backticked; `home` and `authorizedBy` on all eight capsule cases). **(7) Judgment 88 R5′:**
    three `requiredSymbols` rows named declarations EM-P4 MOVES out of `src/generators/lookups.js`
    — measured 1 → 0 the moment EM-P4 lands — so each is re-pointed at its new home. ⛔ **The
    packet's shape, budget, acceptance count (8) and non-goals are unchanged; no arm was weakened
    and no baseline was widened.**
  - ⭐ Version 5 (ESTATE-REPAIR-4b, 2026-09-20, read tip `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`)
    is the SWEEP half of the chair's ESTATE-REPAIR-4 **ruling 1** (judgment 54): every sentence there
    that counted on EM-A2b's `worldFact.goods` and `worldFact.services` — §2's non-goal list, §3's
    split rationale and **R4**'s combined arithmetic — was brought to the ruling, which moves both
    rows to **EM-P3c**'s sitting. ⛔ Nothing of this packet's own contract, budget, acceptance case,
    required symbol, change row, pool row or estimate moved in version 5.
  - ⭐ Version 4 (ESTATE-REPAIR-4, 2026-09-20) added ONE pool row — **`worldFact.monsterThreat`**,
    the chair's assignment of EM-A1 version 5's BLOCK-4b (judgment 33) — to §5, §6, §7's CREATE
    instruction and A1 · A3 · A7, and moved the line estimate ≈143 → ≈148. The pool count returned
    to **eleven**: §14's "eleven → ten" was the `stance` DROP, and this is a different row ARRIVING.
  - Version 3 is the DOCS repair only (ESTATE-REPAIR-3, 2026-09-20), bringing this packet under
    `COMPILE-RULES.interim.md`: §7's registration table gained the **P2.13 DEFERRED WIRING-CENSUS
    ROW** it had omitted (interim rule 1).
  - Version 2 is the DOCS repair only (ESTATE-REPAIR + ESTATE-REPAIR-2, 2026-09-20): the lighting
    absolutes became deltas with as-of marks, the deferred row was re-pointed to the baseline JSON
    the refreeze actually writes, and §7's row for the lighting walker was deleted so §7 and the
    capsule are set-equal. Version 1 is in `pre-estate-repair-2026-09-20/`.
- **Verified base:** `em-t11-a2a-2026-09-21` at `252d7ba4eaa7e531abd2ff2d82e3f34c322d3a08`
  ⚠ Left for the chair, per the pre-proof law: the base is stamped at promotion from the one tip
  the chair re-pins the whole train to.
  ⛔ **THE STAMP'S EXACT FORM, MEASURED AT THE PRE-PROOF — a bare SHA IS NOT ENOUGH.**
  `parsePacketHeader` (`scripts/implementation-packets.mjs`) reads this row two ways: a
  `` `<branch>` at `<40-hex>` `` value yields BOTH `verifiedBase` and `verifiedBranch`, while a
  BARE 40-hex sha yields `verifiedBase` and leaves `verifiedBranch` **null** — and the validator's
  READY arm then reds `EM-A2a.packetPath READY header must name a non-blank verified branch`.
  So the stamp is written exactly as the LANDED siblings write it, the whole value standing alone
  on this line (interim rule 5):
  ``- **Verified base:** `<lane-branch>` at `<40-hex sha>` `` .
  Executed on a copy-only estate with EM-P4 landed: a bare sha left **two** EM-A2a errors, the
  branch-at-sha form left **one** — the index row, which is the chair's own placement act.
  **The revalidation sentence the chair will use:** *"Re-pinned to `` `<lane-branch>` at `<tip>` ``
  at promotion; the J-T1 window over both change-manifest paths and every `requiredSymbols` path is
  EMPTY from `429141e2d` and from `e80a6a3f4` forward to `583f8f644`, both CREATE targets are
  ABSENT, and every one of the nineteen `requiredSymbols` rows resolves VERBATIM at that tip
  **with EM-P4 LANDED**."*
  ⛔ **THE BASE MUST BE A TIP AT WHICH EM-P4 HAS LANDED** — four `requiredSymbols` rows name
  `src/data/institutionLookups.js` and `src/domain/institutionLookups.js`, which EM-P4 CREATEs.
  The value on the Verified base line stands ALONE (interim rule 5; the row's own parser returns
  `null` for a trailing clause, which is what would have cost train EM-T10 a member).
- **Last revalidated:** `252d7ba4eaa7e531abd2ff2d82e3f34c322d3a08` (stamped by the chair at promotion)
- **Depends on:**
  - **`EM-P4` — HARD, and it must have LANDED.** This packet imports
    `src/domain/institutionLookups.js#getInstitutionsForTier`,
    `src/domain/entities/npcs.js#NPC_STATUS_VALUES` and
    `src/domain/entities/status.js#ENTITY_STATUS_VALUES`, and names four rows at EM-P4's two new
    files. It rides the train AFTER EM-P4's (judgment 79's ORDER, judgment 86's addresses).
  - `EM-A1` (declarations — a `pool` field names a pool id). **Name-only: this packet imports
    nothing from EM-A1 and needs nothing of its code to build.**
    ⛔ **CORRECTED IN VERSION 7, AGAINST EM-A1's LANDED TEXT.** Versions 1-6 said *"the declaration
    walker asserts membership of `POOLS`"*. It does not, and it says so itself —
    `tests/lint/editDeclarations.walker.test.js`'s header, verbatim: *"⛔ MEMBERSHIP of `POOLS` is
    asserted NOWHERE here: `POOLS` does not exist at this base and that half of the arm is
    EM-A2a's (§11 BLOCK-4)."* Its ARM I-2 asserts only that the eleven `kind: 'pool'` rows carry
    non-blank, unique ids, and PRINTS the id set *"so EM-A2a's own totality arm can be held against
    it"*. **That arm is THIS packet's, and version 7 carries it in A1.**
- **Collision group:** `EM-A2b`, which **appends pool rows to this packet's `POOLS` and imports
  its machinery**. A2a lands FIRST; A2b's own arms re-assert A2a's totality so a dropped row reds.
  ⭐ **Measured at the EM-T10 pre-proof: EM-A2a ∩ EM-A2b is TWO paths** (`src/domain/edit/pools.js`,
  `tests/domain/editPools.test.js`), and the validator refuses two NON-TERMINAL packets claiming one
  path — so **EM-A2b rides a later train than this packet**, placed only once EM-A2a is LANDED.
- **Commit authority:** edits only; the chair commits.
- **Baseline posture:** measured. Executed at the compile tip `429141e2d` and **RE-EXECUTED,
  value by value, at the pre-proof tip `583f8f644` (every figure below unmoved)**, both times
  **with EM-P4's five files materialised as if landed** into a COPY-ONLY scratch estate
  (judgment 88: a tracked directory is never symlinked): `getInstitutionsForTier('town')` → a `Set` of **85** read
  through the domain address, and the function object is `===` the one `src/generators/lookups.js`
  and `src/data/institutionLookups.js` export; `getInstitutionalCatalog('town')` → 10 categories /
  85 rows; `FACTION_ARCHETYPES` **13**; **`NPC_STATUS_VALUES` 7 (frozen, `jailed` present)** and
  **`ENTITY_STATUS_VALUES` 5 (frozen)**; `RESOURCE_DATA` 33 + `SPECIAL_RESOURCES` 6 (34 distinct
  commodity strings after normalization); `TIER_ORDER` 6; `TERRAIN_WEIGHTS` **7** and `CULTURES`
  **11**, both read at `src/domain/worldFactOptions.js`; `MONSTER_THREAT_TIERS` 3;
  `STRESS_TYPE_MAP` 15. ⚠ **AS HISTORY ONLY:** the census tuple version 1 quoted
  (`2645 / 383 / 2262 / 25009 / 6670`, as of `d31af2cee`) is superseded and no absolute is stated
  anywhere in this packet; the live tuple is read from the register when this member commits.
  No test was run by the compiling lane.
- **Preamble:** docs/implementation/preambles/EM-PREAMBLE.md (SHA-256: fdecd426828665cad2dfaf872871f29f46fa4816e1bd4c43c20ccd84dacc9e7c — stamped by the chair at placement)
> **Compile rules:** the fifth amendment of `EM-PREAMBLE.md` (SHA-256 above) — the interim sheet is superseded; nothing in this packet needs it.
  ⓘ Per the chair's ruling (6) this lane does **not** stamp the preamble line: the chair stamps it
  at promotion from the live text.

---

## 1. Reconciled authority

1. **THE CHAIR'S RULINGS, 2026-09-19** (ODQ §934.36 addendum, §934.46), carried with no
   discretion: **(3)** `rollFrom(poolId, world, seed, entryId, n)` is the signature, its stream key
   spelled once here, consuming no world draw; **(4)** names take the `instantNpc` shape — a
   root-composed PRNG with the culture bag passed in; **(5)** the pools gain the world-facts pools
   and the two §15 state vocabularies, re-counted against the leaf cap with the pre-declared
   A2a/A2b split used if over — **it measures over, and this packet is A2a**; **(6)** the chair
   re-pins every packet to one tip at promotion; **(8)** the lighting census counts test files, so
   a `src/` leaf moves nothing there.
2. **ODQ §934.46 / design §15** — NPC `status` and institution `state` are typed roots from a
   pool, **FINITE-SEMANTICS, never free text**; destruction is a state the record keeps, removal
   is erasure.
3. **ODQ §934.45 / design §14 FINAL** — world facts are edited on a fifth card "whose pools are
   the wizard's own option sets", and a change never re-rolls.
4. **THE PROMISE** and the **deity doctrine** (the deity pool is A2b's and reads the world).
5. **design §12.7 GOVERNS** — pools read the GENERATOR's catalogue, never the display seams.
   ⭐ **Version 6's reading, which the chair ruled (judgment 79):** the catalogue is the same
   catalogue; what changes is the ADDRESS the domain reads it at. EM-P4 moves the institution
   readers DOWN a layer (`src/data/institutionLookups.js`) and publishes a stable domain address
   (`src/domain/institutionLookups.js`) whose exports are the **identical function objects**
   `src/generators/lookups.js` still re-exports. There is no second catalogue, no second tier gate
   and no display seam anywhere in this leaf — and the domain no longer edges into
   `src/generators/**`, which a landed shrink-only ratchet forbids.
6. **`EM-PREAMBLE.md`** §P2–§P5 (HZ-PRNG in particular), §P8 — cited by hash, not restated.
7. **ARCH §1** (`POOLS`, `poolValues`, `rollFrom`), §2 (`PoolSource`), §9 (the pool list).
8. Live code at the compile tip `429141e2d`, with EM-P4's five files materialised as if landed.
9. ⭐ **THE CHAIR, 2026-09-20 (judgment 33; ESTATE-REPAIR-4):** `worldFact.monsterThreat`'s pool is
   **EM-A2a's**. EM-A1 version 5's BLOCK-4b measured that ruling (5)'s conditional home has no
   answer; the chair made the assignment and this row is the record of it.
10. ⭐⭐ **THE CHAIR, 2026-09-21 — judgments 79, 86 and 88 R5′, which version 6 executes:**
    (a) `npc.status` is SEVEN and `jailed` is asserted PRESENT; (b) `institution.class` reads
    `src/domain/institutionLookups.js#getInstitutionsForTier`; (c) each status union is read from
    ONE exported frozen vocabulary in its own home file — `src/domain/entities/npcs.js#NPC_STATUS_VALUES`
    and `src/domain/entities/status.js#ENTITY_STATUS_VALUES` — both minted by EM-P4, whose own
    test holds the both-directions pin against each typedef's source text; (d) terrain and culture
    read `src/domain/worldFactOptions.js`; (e) `powerStrata.js` leaves the fence; (f) this leaf
    spells **no union literal** and adds **no `src/domain` → `src/generators` edge**;
    (g) **R5′** — a `requiredSymbols` row may not name a declaration EM-P4 retires.

**Resolved contradictions:**

- ARCH §1's `rollFrom(poolId, world, seed, rollIndex)` cannot express a key containing `entryId`.
  **The chair's ruling (3) settles it**: `rollFrom(poolId, world, seed, entryId, n)`. Closed.
- **ARCH §9's `Pools:` line still lists NINE pools** and names none of the world-facts or §15
  pools, while design §14/§15 and the chair's ruling (5) require them. The rulings govern; the
  ARCH's pool line lags. **RAISED R1 (a documentation correction, not a build question).**
- **design §15's NPC prose and the tree's `NpcStatus` still differ** — but no longer about
  `jailed`, which the tree gained at `95e494bdb`. What remains is `present`/`departed` (the prose)
  against `active`/`retired`/`removed` (the tree). The tree governs (§6 C2). **RAISED R5.**

---

## 2. Outcome

**Observable result:** `src/domain/edit/pools.js` exists with its machinery and its eleven
TABLE-backed pools: for any pool id and any world it answers the exact closed value list, drawn
from the engine's own vocabulary at its lawful source, and `rollFrom` picks one deterministically
from a stream that is a pure function of the settlement seed, the entry id and the roll index.

**Definition of done:** `POOLS` holds exactly the eleven ids of §6; `poolValues` returns a frozen,
codepoint-sorted, duplicate-free `string[]` (never `null`, never a throw); `rollFrom` returns a
member or `null`; the acceptance proves determinism, the tier gate, the no-draw law, the
wizard-parity of the two world-fact vocabularies and the two state vocabularies. Lands DARK.

In scope: (1) the machinery (`POOLS`, `poolValues`, `rollFrom`); (2) no integration — headless;
(3) the SOURCE-IDENTITY arm, which asserts every table pool equals its source's live answer so a
forked copy reds.

Explicit non-goals: **EM-A2b's SIX world-derived pools** (names, `npc.role`, `deity`,
**`power.holder`** and `worldFact.resources`) and its `worldFact.tradeAccess` row (EM-P3's mint).
⭐ **`worldFact.goods` and `worldFact.services` are this packet's non-goals too, and are no longer
EM-A2b's either:** the chair's ESTATE-REPAIR-4 ruling 1 (judgment 54) moved both to **EM-P3c**'s
sitting. ⛔ Neither is minted here; EM-A1's declarations; EM-A3's census; any op, guard, registry
entry or persisted key; the `rederive` engine (EM-B2); any golden, tuning or migration.
⛔ **And, new in version 6:** this packet MINTS neither status vocabulary and MODIFIES neither
`npcs.js` nor `status.js` — both are **EM-P4's** CREATE/MODIFY rows. This leaf is their first
consumer and nothing more.

---

## 3. Hard scope budget

| Limit | Packet budget | Standard |
|---|---:|---:|
| Behavior families | `1` | 1 |
| New persisted record families / writers / flags / surfaces | `0` | ≤1 each |
| Direct production consumers | `0` — lands DARK | ≤2 |
| New logic-bearing production leaves | `1` | ≤2 |
| Existing logic-bearing production files modified | `0` | ≤3 |
| Handwritten files total | `2` (+1 deferred census row) | ≤12 |
| New/changed effective production lines | **≈140 (estimate)** | ≤400 |
| Effective lines per new leaf | **≈140 of 250 (estimate)** | ≤250 |
| Delta in a shared/hot file | `0` — names no hot file | ≤15 |
| Acceptance cases | `8` | ≤8 |

Overrides approved before dispatch: `NONE`. **HOT FILES: none named.**

⭐ **WHY THE SPLIT WAS TAKEN, arithmetically.** The chair's ruling (5) adds nine pools to the ten
this lane first compiled, and the chair's contract facts then dropped `stance` — eighteen in all.
At the measured ~7 effective lines per table row and ~11 per world-derived row, plus ~70 of
machinery, a single leaf estimates **≈245–265** with no margin, and the estate's hot-file lesson is
that an estimate in the dangerous direction authorizes a bad edit. The pre-declared **A2a / A2b**
line — *table pools vs world-derived pools* — is taken exactly as written. ⭐ Two of its rows have
since moved to **EM-P3c** (ESTATE-REPAIR-4 ruling 1), leaving A2b six world-derived rows.

**Decomposition of the ≈140, RE-DERIVED IN VERSION 6 from the version-6 contract** (the version-5
figure was ≈148 and is superseded; the difference is not a squeeze — it is two frozen literal
mirrors REPLACED by two imports):

| part | ≈ eff |
|---|---:|
| **eleven** `POOLS` rows | 44 |
| five reader/normalizer helpers (`institution.class`, `faction.category`, `commodity`, `worldFact.terrain`, `worldFact.stressors`) | 26 |
| `poolValues` | 20 |
| `rollFrom` | 18 |
| freeze / absence helpers | 15 |
| the ONE frozen literal vocabulary (`cause.remove`) | 5 |
| eleven imports + the typedef block | 12 |
| **total** | **140** |

⭐ **Version 5 read ≈148 with "the five frozen literal vocabularies ≈ 17".** Only THREE were ever
literal (`cause.remove` and the two typedef mirrors), and version 6 keeps only `cause.remove`:
the two mirrors become imports, which is where the eight lines go. **A plain-node floor, executed
this compile:** a working spelling of exactly this contract measures **55** non-blank non-comment
lines without its JSDoc, per-row documentation or absence commentary — so ≈140 is conservative in
the safe direction. **An ESTIMATE, re-measured with eslint's own `Linter` at implementation;
crossing 250 is a STOP (§11), never a squeeze.**

---

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- EM-A2a
```

Expected: capsule emitted; ancestry and substrate proven; **CREATE targets ABSENT**
(`src/domain/edit/pools.js`, `tests/domain/editPools.test.js`); no non-CREATE production target;
every `requiredSymbols` row resolving.

⛔ **THE SEAL IS FOREIGN UNLESS EM-P4 HAS LANDED, AND HERE ARE THE EXACT SIX REFUSALS.** Run at
the pre-proof with the tip's OWN `validatePacketManifest` over a copy-only estate, at DRAFT and at
READY alike, EM-P4 ABSENT:

```text
EM-A2a.requiredSymbols[0].path does not exist: src/data/institutionLookups.js
EM-A2a.requiredSymbols[1].path does not exist: src/data/institutionLookups.js
EM-A2a.requiredSymbols[2].path does not exist: src/data/institutionLookups.js
EM-A2a.requiredSymbols[3].path does not exist: src/domain/institutionLookups.js
EM-A2a.requiredSymbols[15].symbol is missing from src/domain/entities/npcs.js: export const NPC_STATUS_VALUES
EM-A2a.requiredSymbols[17].symbol is missing from src/domain/entities/status.js: export const ENTITY_STATUS_VALUES
```

With EM-P4 materialised as LANDED **all six vanish and EM-A2a carries ZERO content errors**; what
remains is only the chair's placement stamps (the index row, and the verified-branch arm the header
row above now spells out). That refusal is correct and must not be worked around.

⚠ **THE WORKTREE IS SHARED.** Re-read `git rev-parse HEAD` in the same command as the dispatch.

---

## 5. Verified tree contract

Every row found BY SYMBOL at the compile tip `429141e2d`, with EM-P4's five files materialised as
if landed; the proving command for each is in `EM-A2a.evidence.md` / `EM-A2a.v6.report.md`.

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| ⭐ **Tier-gated catalogue — THE ADDRESS THIS LEAF IMPORTS** | `src/domain/institutionLookups.js` | `getInstitutionsForTier` | EM-P4's stable domain address. Executed: the exported binding is `===` `src/generators/lookups.js`'s and `===` `src/data/institutionLookups.js`'s — **one function object at three addresses**, so "read at its source" is literally true | The only source of `institution.class`. Spread the `Set`; never mutate it. ⛔ **Never `src/generators/lookups.js`** — that import is a new `src/domain`→`src/generators` edge AND a `catalogTierGateParity` A5 hit |
| ⭐ Tier-gated catalogue — THE BODY | `src/data/institutionLookups.js` | `getInstitutionsForTier` | Returns a **`Set`** (executed through the domain address: `'town'` → size **85**; `'city'` → 81, `'metropolis'` → 115); every row passes the `minTier` gate | The behaviour this packet must find unchanged |
| Catalogue shape | `src/data/institutionLookups.js` | `getInstitutionalCatalog` | `Record<category, Record<name, CatalogRow>>`; executed at `'town'`: **10** categories / **85** rows, name-for-name equal to the Set | The source when the CATEGORY grouping is needed (A3's tier-gate arm) |
| The gate | `src/data/institutionLookups.js` | `institutionAvailableAtTier` | `(def, tier) => !def?.minTier \|\| tierAtLeast(tier, def.minTier)` | Reuse; **never a second gate** |
| ⛔ FORBIDDEN seam | `src/domain/display/resourceDisplayName.js` | `resourceDisplayName` | A relabeling map that **passes unknowns through** | ⛔ Never a pool source (§12.7). Same for `institutionDisplayName` |
| PRNG kernel | `src/kernel/prng.js` | `createPRNG` | `pick(arr)` returns **`undefined`** on an empty/absent array | The only stream source. ⚠ NOT at `src/generators/kernel/prng.js` — that path does not exist |
| ⛔ Fork-label law | `src/kernel/prng.js` | `fork` | `fork(label) => createPRNG(\`${seed}::${label}\`)`; `tests/kernel/prngForkLabelDelimiter.test.js` freezes the embedded-delimiter families and RESERVES the head `epoch` | ⛔ `rollFrom` calls `fork` never and spells no `::` |
| ⛔ AMBIENT RNG — avoid | `src/kernel/rngContext.js` | `random`, `_roll` | Module-level `_activeRng`; **throws** with none active, **consumes a world draw** with one | ⛔ Imported nowhere here (HZ-PRNG). ⭐ Version 6's re-aim removes it from the leaf's closure entirely — it was two hops away through `resolveConfig.js` |
| Roll precedent | `src/domain/npc/npcOps.js` | `instantNpc` | `createPRNG(\`instant-npc:${seed}\`)` — a ROOT composition, not a fork | Copy the shape (chair ruling 4) |
| Archetype table | `src/domain/factionArchetypes.js` | `FACTION_ARCHETYPES` | Frozen 13 values, executed | `faction.category` |
| Resource catalogue | `src/data/resourceData.js` | `RESOURCE_DATA`, `SPECIAL_RESOURCES` | Executed: **33** and **6** keys; each `RESOURCE_DATA` row carries `commodities: string[]`; the flattened, de-duplicated pool measures **34** | `commodity`, read at its source. ⚠ Eager first-paint chunk — §6 bundle law |
| Tier ladder | `src/data/constants.js` | `TIER_ORDER` | `['thorp','hamlet','village','town','city','metropolis']` — order is MEANING | `tier`; the one pool exempt from the codepoint sort |
| ⭐ **NPC status vocabulary** | `src/domain/entities/npcs.js` | `NPC_STATUS_VALUES` | **EM-P4's mint.** `Object.freeze([...])`, **SEVEN** members in the typedef's own order, executed frozen: `active, dead, exiled, jailed, missing, removed, retired`. Lawful as literals **only here**: `npcs.js` is `CONSUMER_ROSTER[0]`, which `statusUnionTotality.walker`'s `foreignVocabularies()` skips | `npc.status` — imported, **never re-spelled** |
| ⭐ **NPC status union** | `src/domain/entities/npcs.js` | `NpcStatus` | The JSDoc typedef, **seven** members since `95e494bdb` (EM-B1d, train EM-T3). EM-P4's `tests/domain/statusVocabularies.test.js` pins it to `NPC_STATUS_VALUES` in BOTH directions | The union the pool IS (A8). ⛔ This packet does not re-implement that pin |
| ⭐ **Entity status vocabulary** | `src/domain/entities/status.js` | `ENTITY_STATUS_VALUES` | **EM-P4's mint.** `Object.freeze([STATUS_ACTIVE, STATUS_IMPAIRED, STATUS_REMOVED, STATUS_DESTROYED, STATUS_VACANT])` — built **BY REFERENCE**, executed **five** members, frozen | `institution.state` — imported, **never re-spelled** |
| ⭐ **Entity status union** | `src/domain/entities/status.js` | `EntityStatus` | The JSDoc typedef, five members (`active\|impaired\|removed\|destroyed\|vacant`) | The union the pool IS (A8) |
| ⭐ **Wizard terrain set** | `src/domain/worldFactOptions.js` | `TERRAIN_WEIGHTS` | EM-P3's landed stable domain address, re-exporting `src/data/worldFactOptions.js`. Executed: **7** keys, set-equal to the wizard's own seven `<option>` values. Its own docblock names **"the editor's world-fact card"** as a reader | `worldFact.terrain`. ⛔ Never `src/generators/steps/resolveConfig.js` — a generators edge, and it drags `rngContext.js` and a `registerStep` side effect into the closure |
| ⭐ **Wizard culture set** | `src/domain/worldFactOptions.js` | `CULTURES` | Same address; executed **11** | `worldFact.culture` |
| ⭐ **Monster-threat tiers** | `src/data/monsterThreat.js` | `MONSTER_THREAT_TIERS` | `Object.freeze(['heartland','frontier','plagued'])` — executed: **3 members, frozen**. `src/domain/worldFactOptions.js`'s `WORLD_FACT_SOURCES` cites this exact address as the fact's one home | `worldFact.monsterThreat` — read at its source. ⛔ Never `normalizeMonsterThreat` (it forwards unknown legacy aliases through) and never `MONSTER_THREAT_RANDOM_POOL` (the generator's WEIGHTED roll pool, a different set) |
| **Stressor vocabulary** | `src/data/stressTypes.js` | `STRESS_TYPE_MAP` | Executed: **15** keys | `worldFact.stressors` — the KEYS, never the labels |
| Stable order | `src/domain/deterministicSort.js` | `compareCodepoint` | The estate's one sanctioned, locale-free string order | Every pool's order except `tier` |
| Test precedent | `tests/domain/institutionFounding.test.js` | `describe('MF-T2Q — the institution founding year')` + seven straight-line `it` | One literal `describe`, no `.each`/nesting, positive control first | Copy this proof shape (preamble §P3.4) |
| Test precedent (tables) | `tests/domain/undercityStrataExistence.test.js` | MF-UC0 A1 | One `it` may walk a table and report a FULL offender list | Copy for the eleven-pool arms |

⛔ **RETIRED FROM VERSION 5's CONTRACT, BY MEASUREMENT (judgment 88 R5′).** Version 5 named
`src/generators/lookups.js` for all three institution rows. EM-P4 MOVES those declarations, and the
seam keeps only a bare re-export block: measured at an estate with EM-P4 landed,
`grep -c -F 'export const getInstitutionsForTier' src/generators/lookups.js` → **0** (it is **1**
at the untouched tip), and the same for `getInstitutionalCatalog` and `institutionAvailableAtTier`.
Three `requiredSymbols` rows on that path would be three validator errors the day EM-P4 lands, so
each is re-pointed above. ⛔ **`src/generators/lookups.js` is named NOWHERE in this packet's
manifest and is imported by nothing this packet writes.**

**Forbidden alternatives:** no second catalogue, vocabulary, tier gate, PRNG stream, time source or
writer; **no second copy of a wizard option set** (chair ruling 5); ⛔ **no `src/generators/**`
import of any kind** (the landed shrink-only ratchet `tests/build/domainGeneratorsBoundary.test.js`
and `catalogTierGateParity` A5 both convict it — measured, this lane); no import of
`src/kernel/rngContext.js`, `src/generators/npcGenerator.js`, `src/data/namingData.js`, either
display seam, `src/components/**` or `src/store/**`; ⛔ **no import of
`src/components/ConfigurationPanel.jsx` or any JSX**; ⛔ **no frozen literal mirroring either
status union inside this leaf** (it empties the union walker's derived trigger — measured);
no edit to any existing file (**this packet modified ZERO existing files at version 8; versions 9 and 10 modify two by two pins and one comment line, and the edge-shared closure is still not owed because no bundle meta names any of its files (judgment 132) — it once said "modifies ZERO"**); no files outside the manifest.

---

## 6. Exact contracts

### Inputs and outputs

```js
/**
 * @typedef {{ tier?: string, institutions?: Array<{name?: string, role?: string}>,
 *   pantheon?: object, namingData?: object, culture?: string,
 *   terrain?: string, tradeRoute?: string }} PoolWorld
 *   The read bag. EVERY field is OPTIONAL and every absence is answered with [].
 */

export const POOLS;              // Readonly<Record<string, PoolSource>> — exactly the eleven ids
export function poolValues(poolId, world);        // → readonly string[]  (never null, never throws)
export function rollFrom(poolId, world, seed, entryId, n);   // → string | null (never undefined)
```

### The eleven pool ids — exact, closed, and their exact sources

| id | source kind | exact read | values, executed at the compile tip |
|---|---|---|---|
| `institution.class` | `catalogue` | `[...getInstitutionsForTier(world?.tier ?? 'village')]` from **`src/domain/institutionLookups.js`** | 85 at `town` |
| `faction.category` | `catalogue` | `Object.values(FACTION_ARCHETYPES)` | 13 |
| `cause.remove` | `literal` | frozen `['burned','died','dissolved','left','seized']` | 5 |
| `commodity` | `catalogue` | every `RESOURCE_DATA[k].commodities[]` flattened + `Object.keys(SPECIAL_RESOURCES)` | 34 distinct |
| `tier` | `catalogue` | `TIER_ORDER` | 6 |
| ⭐ **`npc.status`** | `catalogue` | **`NPC_STATUS_VALUES`**, imported from `src/domain/entities/npcs.js` | **7 — the TREE's, `jailed` PRESENT** |
| ⭐ **`institution.state`** | `catalogue` | **`ENTITY_STATUS_VALUES`**, imported from `src/domain/entities/status.js` | **5 — the TREE's** |
| `worldFact.terrain` | `catalogue` | `TERRAIN_WEIGHTS.map(([k]) => k)` from **`src/domain/worldFactOptions.js`** | 7 |
| `worldFact.culture` | `catalogue` | `CULTURES` from **`src/domain/worldFactOptions.js`** | 11 |
| ⭐ `worldFact.monsterThreat` | `catalogue` | `MONSTER_THREAT_TIERS` | 3 |
| `worldFact.stressors` | `catalogue` | `Object.keys(STRESS_TYPE_MAP)` | 15 |

### ⭐ WHAT A STORED `tier` CAN BE — MEASURED, AND WHY `institution.class` NEEDS NO SPECIAL CASE

Version 5 and version 6 both claimed that a tier of `'random'`/`'custom'`/absent yields the village
catalogue *"(the lookups' own special case)"*. **Executed at the pre-proof tip, that is FALSE of the
function this leaf reads.** `getInstitutionsForTier` is `institutionalCatalog[t] || {}` behind the
`minTier` gate and has no such case:

| argument | `getInstitutionsForTier(…)` | `getInstitutionalCatalog(…)` |
|---|---:|---:|
| `'thorp' · 'hamlet' · 'village' · 'town' · 'city' · 'metropolis'` | 19 · 37 · **58** · **85** · **81** · **115** | — · — · 58 rows · 85 rows · — · — |
| `'random'` · `'custom'` | **0** · **0** | **58 rows** · **58 rows** |
| `undefined` · `null` · `''` · `'nonsense'` · `'all'` | **0** each | 58 · 58 · 58 · **0** · (merged) |

The random/custom/no-tier case belongs to **`getInstitutionalCatalog`** alone
(`src/generators/lookups.js`, whose own docblock calls it *"a sane default for previews"*), and this
leaf imports neither that function nor that module. So the **58** this packet answers for an absent
tier is **its own `?? 'village'` default**, which is a contract decision, not an inherited one.

⛔ **AND IT NEEDS NO SPECIAL CASE, BECAUSE A STORED TIER IS ALWAYS A CONCRETE CATALOGUE TIER.**
The sentinels are WIZARD tokens that die at generation, measured end to end at `583f8f644`:
`src/generators/steps/resolveConfig.js:92-94` is the one resolver — `'custom'` →
`popToTier(population)`, `'random'` → `rng.pick(TIER_ORDER)`, anything else → `config.settType ||
'village'`; `popToTier`'s codomain (`src/data/constants.js`) is exactly the six `TIER_ORDER`
members; `src/generators/steps/assembleSettlement.js:116` writes THAT resolved value as the
settlement record's `tier`, and `resolveConfig.js` returns the same value beside `effectiveConfig`,
so `config.tier` is concrete too (`src/generators/institutionProbability.js`: *"popToTier resolved
the true tier into config.tier"*). `git grep -n -E "tier\s*[:=][^;]*settType" -- src` returns
**exactly two** hits in the whole tree — an analytics payload
(`src/components/GenerateWizard.jsx`) and a custom-content eligibility filter
(`src/store/settlementContentRuntime.js`) — and **neither is a settlement record nor a pool world**.
`src/store/authSlice.js` states the law in the product's own words: the sentinels *"are not
concrete tiers: they resolve to a real tier during generation"*.
⚠ **What is NOT proven and is stated as such:** `(config.settType || 'village')` would pass an
arbitrary settType STRING through to `tier`, but `isTierAllowed` and the wizard's own picker make
that unreachable today. A2's `[]` for a non-catalogue string is therefore a SAFETY answer, not a
reachable product state — which is exactly why A2 anchors on a real tier as well.

### ⛔ THE SPELLINGS THE CHAIR'S CONTRACT FACTS AND JUDGMENT 79 CORRECT

**C1 — `faction.archetype` becomes `faction.category`, and `stance` is DROPPED.** Measured: no
faction record carries `type` or `archetype`. `src/generators/power/rulingStructure.js`'s pushes
carry `faction` (the display name), `power`, `desc` and `isGoverning`; the archetype-like field is
**`category`**. ⭐ **`archetype` is DERIVED, not stored** — `factionArchetype(faction)` computes it
from `f.category` — and design §14 is explicit that *"a derivation is never editable, on any
card."* So the editable ROOT is `category`, its pool is the same thirteen values, and the pool id
is spelled on the root. **No faction record carries a `stance` either**, so that pool is dropped
with it. `RELATIONSHIP_KIND_ORDER` is therefore **not a required symbol of this packet**.
**RAISED R4.**

**C2 — the two state vocabularies are the TREE's, READ AT THEIR HOMES (version 6 rewrites this).**
The tree owns both unions, and both differ from design §15's prose:

| | design §15 says | the tree's canonical typedef | the difference |
|---|---|---|---|
| NPC status | present, exiled, jailed, dead, departed, missing | `NpcStatus` = **`'active'\|'dead'\|'exiled'\|'jailed'\|'missing'\|'removed'\|'retired'`** | `active` not `present`; `retired`+`removed` are the tree's; **`jailed` IS the tree's, since `95e494bdb`**; no `departed` |
| institution state | active, impaired, ruined, destroyed, abandoned, under-construction | `EntityStatus` = **`'active'\|'impaired'\|'removed'\|'destroyed'\|'vacant'`** | `removed`+`vacant` are the tree's; no `ruined`, `abandoned` or `under-construction` |

⛔ **The pools take the TREE's members**, because the chair's fact (3) rules that the ops layer's
existing writers (`src/domain/entities/npcs.js`, `src/domain/events/mutateEntities.js`) are the
writer the op calls and **never a second path** — a value outside `NpcStatus` would break those
writers' own type contract the moment it was written.

⭐⭐ **AND THE MEMBERS ARE IMPORTED, NOT MIRRORED — THIS IS THE WHOLE OF VERSION 6's CURE.**
Version 5 said "each typedef's members are read as a frozen literal mirroring its source". That
spelling was measured at the EM-T10 pre-proof and **empties `statusUnionTotality.walker`'s DERIVED
trigger**: the walker treats every SCREAMING_SNAKE const initialised to an array of string
literals in a non-roster file as a foreign vocabulary, so a mirror here would claim all seven
words and the trigger — `['dead','exiled','retired']` — would collapse to `[]`, reddening that
walker's A3—T2 arm twice (set-equality, then "a trigger set of one token or none cannot
discriminate at all"). Re-ruling that trigger is **the chair's, never a lane's**, by the walker's
own sentence. EM-P4 therefore mints ONE exported vocabulary per union in its own home file, and
this leaf **imports** it. Measured on an estate with EM-P4 landed and this leaf present: TRIGGER
`['dead','exiled','retired']` **UNMOVED**, `FLAGGED \ DECLARED_FLAGGABLE` **`[]`**,
`DISCOVERED_ENUMERATORS` **3 — and `src/domain/edit/pools.js` is not one of them.**

⚠ **`jailed` — R5, ANSWERED BY THE TREE.** Version 5 asserted it ABSENT by name. It has been a
member since `95e494bdb` (EM-B1d, train EM-T3). **A8 asserts it PRESENT**, and the pool is seven.

⚠ **The readers that consume the two fields are named by the packet that ADDS each field** (design
§15's closing clause) — EM-A1/EM-B2, not this one. This packet reads the VOCABULARY only.

⛔ `cause.remove`'s five values are the owner's list (ODQ §934.36 addendum).

⛔ **`worldFact.terrain` and `worldFact.culture` are the WIZARD'S OWN SETS, read at the domain
address, never re-typed.** The wizard renders them as `<option>` literals in
`src/components/ConfigurationPanel.jsx`; this leaf reads `TERRAIN_WEIGHTS` and `CULTURES` from
`src/domain/worldFactOptions.js` — EM-P3's landed address, whose values are identity-equal to the
generator's own and whose docblock names this very consumer — and **A3 asserts the two agree**, so
a future drift between the wizard's menu and the generator's table is caught by this packet.

### State schema

**NONE.** Nothing is persisted, written or minted. `POOLS` is a frozen module constant;
`poolValues` and `rollFrom` are pure functions of their arguments.

Absence rules: **absent** (`world` null/undefined or the needed field missing) → the frozen empty
array; **empty** `[]` is a legal answer and is not distinguished from absence; **`null`** is
forbidden as a `poolValues` return and IS the `rollFrom` return for an empty pool; **invalid
legacy input** (non-string, duplicate, blank) is dropped in normalization — `String(v)`, trim,
drop `''`, dedupe — never repaired, never thrown on; **unknown `poolId`** → the frozen empty array
(membership is EM-A1's walker's business, not a runtime failure).

### Transition table

Not applicable: no state, no transitions.

### Ordering and precedence

- **Pipeline/tick position:** NONE — outside generation and outside the pulse.
- **Merge/replace/deduplicate:** de-duplicated after normalization, first occurrence kept.
- **Stable enumeration:** `compareCodepoint` for every pool **except `tier`**, whose `TIER_ORDER`
  sequence is semantic (small→large) and preserved verbatim. Asserted in both directions (A4).
- **Tie-break:** none can occur — duplicates are gone and `compareCodepoint` is total.

### Determinism

- **Hash/fork key — the EXACT spelling, a ROOT composition, never a fork:**

  ```js
  createPRNG(`edit-pool:${poolId}:${String(seed)}:${entryId}:${n}`)
  ```

  then one `.pick(values)`.

  ⭐ A root composition with single-colon separators touches neither
  `EMBEDDED_DELIMITER_FAMILIES` nor the RESERVED head `epoch`, so
  `tests/kernel/prngForkLabelDelimiter.test.js` **cannot move for this packet** — and `instantNpc`
  already does exactly this. The head token is `edit-pool`, which is not `epoch`.
- **`n` is IN the key, not a number of advances**, so roll 3 is reproducible without replaying
  0–2 — the reopen guarantee of design §11.
- ⭐ **THE CONSUMER'S BINDING, READ AT THE TIP (version 7).** EM-D0d is READY at `583f8f644` and
  its §12 spells the `roll` prop as `(seed: string, entryId: string, n: number) => string | null`,
  *"which the mount binds as `(seed, entryId, n) => rollFrom(declaration.pool, world, seed,
  entryId, n)`"*, and its §5 names this packet's signature back verbatim. **The five parameters,
  their order and their types agree exactly**, so nothing in EM-D0d obliges a change here — and if
  this signature ever moves, EM-D0d's mount is the first thing that breaks (interim rule 16).
- **Rounding/clamping:** none; no float is produced or rendered (prose-numerics not engaged).
- **No-draw behaviour — load-bearing:** `rollFrom` consumes **no world draw and no ambient draw**.
  It never imports `src/kernel/rngContext.js`, never calls `getActiveRng`/`setActiveRng`/`random`/
  `pick`/`chance`/`randInt`/`shuffle`/`weightedPick` from it, and never calls `Math.random`,
  `Date.now` or a locale-sensitive comparator. A6 asserts both halves.

### ⛔ THE IMPORT FENCE — ELEVEN MODULES, AND NOT ONE MORE (A7 asserts it in both directions)

```
src/kernel/prng.js                     src/domain/deterministicSort.js
src/domain/institutionLookups.js       src/domain/factionArchetypes.js
src/domain/entities/npcs.js            src/domain/entities/status.js
src/domain/worldFactOptions.js         src/data/resourceData.js
src/data/constants.js                  src/data/monsterThreat.js
src/data/stressTypes.js
```

⛔ **`src/domain/dossier/powerStrata.js` IS NOT IN IT (judgment 79 ruling 5).** Version 5 fenced it
IN, in both spellings: it is the retired `stance` pool's residue, no §6 row reads it, its closure
is 93 modules, and `npx eslint` — a sealed check of this packet — convicts an unused import. It is
deleted from §9 A7 and from the capsule's A7 in version 6.

### Bundle law

⛔ **No large data table is imported at module scope.** `src/data/resourceData.js` is imported
statically **only** because it is already in the eager first-paint `data` chunk (three first-paint
modules reach it) and adds no new edge; `src/data/stressTypes.js`, `src/data/monsterThreat.js`,
`src/domain/factionArchetypes.js`, `src/domain/worldFactOptions.js` (a two-module closure) and
`src/domain/institutionLookups.js` are small. ⚠ `src/data/namingData.js` (4,038 lines / ~68.6 kB)
is **A2b's** concern and is imported nowhere, in either packet.

⭐ **VERSION 6's RE-AIM IS A CLOSURE SHRINK, MEASURED.** The leaf's static closure as version 5
spelled it was **27 modules**, including `src/generators/pipeline.js`, `src/kernel/rngContext.js`,
`trace.js` and a `registerStep('resolveConfig', …)` side effect executed at module scope. Reading
terrain and culture at `src/domain/worldFactOptions.js` (2 modules) instead of
`src/generators/steps/resolveConfig.js` (17) removes **twelve** modules and `rngContext.js` — the
very module §5 fences out by name — for free. **The price in every budgeted closure is `+0 B`**
(the leaf has zero importers at this landing: `git grep -n -E "domain/edit/pools" -- src tests
scripts e2e` returns NOTHING), and **this packet is not train EM-T10's byte-arm holder**
(interim rule 14): it carries no budget test file as a row, and its own predicted delta is `+0 B`
in the worker ceiling, the lazy engine, the eager first-paint closure and the five edge-shared
metas.

### ⭐ THE TUNING INVENTORY (interim rule 17) — ZERO, AND THE CONTRACT KEEPS IT ZERO

`src/domain/edit/pools.js` is under `src/domain`, one of the two trees
`scripts/lib/tuning-inventory.mjs` counts. **Executed with the library's OWN
`countUnregisteredNamed` and `countBareDecimals` over an estate carrying this contract's text:**

| figure | tip | with this leaf |
|---|---:|---:|
| `unregisteredNamed[src/domain/edit/pools.js]` (P2) | — (file absent) | **0** |
| `bareDecimals[src/domain/edit/pools.js]` (P3) | — (file absent) | **0** |
| `totals.unregisteredNamed` | 535 | **535** |
| `totals.bareDecimals` | 6985 | **6985** |

⛔ **HOW THE CONTRACT KEEPS IT AT ZERO, and this is binding on the build lane.** The seeded
roller's constants are the obvious risk (EM-A1's build met exactly this and was RESUMED):
**no module-top-level `const UPPER_SNAKE = <number>;` may be written in this file, and no
fractional decimal literal may appear in its code text.** `n`'s clamp compares against the integer
literal `0` inside `rollFrom`; the stream key is a template string; there is no weight, no
probability and no ratio anywhere in this leaf. If implementation finds it needs a bound, it is
spelled as an **integer member of ONE frozen object** with a one-sentence comment saying it bounds
nothing the world is seeded from — **never a top-level named numeric const** — because the
inventory is exact in BOTH directions per file and a new file is banked at zero. ⛔ **No number
here is a simulation dial, so NO tuning-register row is owed** (that register is the owner-signed
record of what shapes a seeded world, and a row there would be a claim this leaf tunes generation).
**No line is MOVED out of any counted file either, so no inventory refreeze is owed.**

### Flag and dormancy

**Flag:** `NONE` — wave 1 is headless; `TIER_GATE.premium.editMode` is EM-D1's.
**Dormancy:** zero importers at this packet's tip (A2b adds the first, and it is a sibling, not a
consumer). **Golden posture:** `UNCHANGED` — `generatorGoldenMaster` and `dossierProseManifest`
must not move by one byte; structurally guaranteed, since nothing imports the leaf and it takes no
draw. **Motion is a STOP.**

### Lifecycle

| Create | Read | Persist | Reload | Regenerate | Undo | Import/migrate | Public veil |
|---|---|---|---|---|---|---|---|
| Frozen module constant | pure reads | **Never** | n/a | Pools re-read live catalogues every call | n/a | **Nothing to migrate** | **Nothing to veil** |

⚠ The whole row is "nothing", and that is a verified claim: this leaf reads vocabularies and a
world bag and returns strings.

### Receipts and privacy

`NONE` — no receipt, no figure, no DM-only field, no projection. Those are EM-B3's (§P2.6).

### Alignment and edit story

- **Alignment:** `DECLARED EMPTY: this leaf offers values and ranks nothing; it reads no alignment,
  law or temper axis.`
- **Edit story:** `ENGINE-ONLY: the DM's verb is EM-D2's PoolField; this is the catalogue beneath
  it and has no surface.`

---

## 7. Exact change manifest

Generated from `EM-A2a.manifest.json` and proved SET-EQUAL to it (interim rule 6; the executed
line is in `EM-A2a.v6.report.md` §A).

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `src/domain/edit/pools.js` | `POOLS`, `poolValues`, `rollFrom` | **250 eff (cap); ≈140 estimated** | The machinery of §6 and exactly the eleven table pools. Read every vocabulary at the address §5 names. ⛔ **Import exactly the eleven modules of §6's fence** — no `src/generators/**` path, no JSX, no `rngContext`, no `namingData`, no `powerStrata`. ⛔ **Spell no union literal and no top-level `const UPPER_SNAKE = <number>;`.** Leave the file's structure open for A2b to append rows. |
| `CREATE` | `tests/domain/editPools.test.js` | A1–A8 | `n/a` | ONE literal `describe`, **eight straight-line `it`**, no `.each`/`runIf`/nesting (§P3.4). Table arms report a FULL offender list. The built file carries NO `// anchored:` marker and none is owed (the anchor walker polices `toContain`/`toMatch`/`toHaveProperty`; this file's negatives are `.not.toEqual` ×2 and `.not.toThrow` — corrected at the train re-spelling, judgment 132). ⛔ Never name a variable or parameter `it`, `test` or `describe` (the census resolves an opener only where the module binds the word exactly once). |
| `TEST` | `tests/lint/entropyRootCensus.walker.test.js` | the EP-0 closure record's two pins and the test's title | `+0 eff` (two literals, one title, one comment block) | **VERSION 9 (judgment 128).** The closure record RE-RUN, not transcribed: `createPRNG(` sites 35 → **36** in `src/domain`, 46 → **47** whole-src (the title made truthful — it said 45 while the assertion said 46), with a comment naming the ONE new site — `src/domain/edit/pools.js:191` — `rollFrom`'s ROOT composition `createPRNG(`edit-pool:${poolId}:${String(seed)}:${entryId}:${rollIndex}`)` on its OWN stream, the shape ruling 4 ordered from `instantNpc`: not one draw of the world's sequence is consumed; the same seed and key roll the same value forever. No other arm of the walker moves (`COMPOSITIONS` is untouched — the site is a seed-keyed ROOT, not a tick or year composition; `READ_SITES` and `WRITE_KEY_SITES` are untouched — the leaf reads no `WORLD_ROOT`). The gate that convicted version 8: window 28 job 5, section c1. |
| `MODIFY` | `src/domain/goods.schema.js` | the WEAVE ST-2 roster (a comment table) | `+0 eff` (one comment line) | **VERSION 10 (judgment 131).** One roster row, `R | l | identity | direct | src/domain/edit/pools.js`, in the shape of its neighbours: `pools.js` is a DIRECT importer of the goods identity half-table `src/data/resourceData.js` (the commodity pool reads it), and `tests/build/vendorPdfLazy.test.js`'s ST-2 arm convicts every direct importer without a row. No logic byte; the citation walker and the roster arm are the proof. |

Generated artifacts: `NONE`. ⛔ **Edge-shared closure NOT owed** — zero existing files modified,
so no closure input moves.

> ⛔ **THE §7 ROW FOR `tests/lint/sovereigntyLightingContract.walker.test.js` IS DELETED
> (ESTATE-REPAIR-2).** This packet does not edit that path, and the JSON capsule correctly omits
> it; the deferred box below says everything the row said and names the file the refreeze writes.

### The registration ledger

| # | Obligation | Verdict | Measurement |
|---|---|---|---|
| P2.1 | lighting census | **OWED — `+1 file`, INTERIOR RED** | `files` is `TEST_FILES.length` over `tests/**/*.test.js`, so the driver is the new TEST file, **not** the `src/domain` leaf. **DELTA ONLY: `+1 files / +0 parked / +1 credited / +8 titles / +1 suiteTitles`**, from whatever the live baseline reads when this member commits. ⛔ No absolute is stated anywhere in this packet. Re-derived whole at the terminal (§P3.2). ⭐ **EXECUTED at the pre-proof (version 7), not predicted:** the walker's OWN `measureCensus` was run over a copy-only estate twice — without and with a test file written to §7's CREATE shape — and the five figures moved by exactly that tuple; `parkReasonsFor` over the new file printed **`[]`** (CREDITED, not parked — shared-brief step 14b), `liveTitlesIn` **8** and `liveSuiteTitlesIn` **1**. So the `+1 credited` is a measurement, not a hope. |
| P2.2 | mutation-coverage row | **NOT OWED** | `ENFORCER_DIRS` (`tests/lint/mutationCoverage.shared.mjs`, read at the compile tip) does not contain `tests/domain`; this packet's only test file is `tests/domain/editPools.test.js` (interim rule 15). |
| P2.3 | observed-shape exemption | **NOT OWED** | No save-time key (`dmLayer`, `decrees`) is read. The scanner covers every `.js` under `src/`, so the leaf IS scanned and the check is in `checks`. |
| P2.4 | writer-reach | **CANNOT MOVE** | `SURFACE_CLOSURE_STOP` includes `'src/store/'`; this leaf reaches a surface only through `src/store/editSlice.js`. |
| P2.5 | decision-fork + mechanism-coverage rows | **NOT OWED — measured** | `chooserTotality`'s `SCAN_ROOTS` are `src/domain/{worldPulse,spatial,traditions,region}` — `src/domain/edit` is not among them; the mechanism walker enumerates `src/domain/worldPulse`. **RAISED R2.** |
| P2.7 | prose-numerics | **NOT OWED** | No figure rendered; `tests/lint/.prose-numerics-baseline.json` carries **0** rows under `domain/edit`, so no line-addressed row moves (interim rule 13). |
| ⭐ **P2.13** | **wiring census — the PRODUCER COUNT** | ⛔ **OWED — A DEFERRED ROW, delta only (interim rule 1)** | `docs/content/wiring-census.json` — **a named INTERIOR RED, delta only.** `stamp.producerIndexFiles` **+1**: this member's one new `.js` leaf under the two counted roots, named — `src/domain/edit/pools.js`. ⛔ **NO ABSOLUTE IS STATED (interim rule 9 / §P3.5): the figure is a NUMBER the chair re-derives at the terminal, and this row carries only the `+1`.** Version 6 spelled `1172 → 1173`; at the pre-proof tip `583f8f644` the committed stamp reads a different number entirely (the census was regenerated for train EM-T9), and a build lane would have quoted the stale pair into its receipt. The **+1** is derived from this member's own CREATE rows — one new `.js` leaf under the two counted roots, named: `src/domain/edit/pools.js` — and from nothing else. `stamp.files` (7) and `stamp.candidateLeaves` (6) **UNMOVED** — both key on `src/domain/display/stateProse/` paths this member does not touch. `totals.*` **UNMOVED**. ⛔ **Regenerated WHOLE at the train's terminal, BY THE CHAIR.** ⛔ **The path is in NEITHER §7, NOR the `changeManifest`, NOR `checks`.** |
| ⭐ **P2.15** | **the determinism BAN-COVERAGE pin** | **NOT OWED — measured** | `tests/lint/determinismBanCoverage.test.js` resolves EVERY file in the entropy-sensitive layers (`src/domain/**` among them) through eslint's own `calculateConfigForFile` and demands `no-restricted-syntax` at ERROR with that layer's selectors. Executed on the estate with this leaf present: `src/domain/edit/pools.js` resolves to severity **2** with **34** selectors — byte-identical to its LANDED sibling `src/domain/edit/fieldDeclarations.js` — while the sanctioned entropy-minting seam `src/kernel/prng.js` resolves to **30** with `Math.random` ALLOWED. The leaf widens no glob and replaces no block. `npx eslint src/domain/edit/pools.js` exits **0**. |
| ⭐ **P2.16** | **the ENTROPY-ROOT census (the stream-minting law)** | **OWED — version 9 (the gate refuted version 8's NOT OWED: the walker also pins the `createPRNG(` site count; see §7's TEST row)** | `tests/lint/entropyRootCensus.walker.test.js` pins where a world seed is READ off a `WORLD_ROOTS` receiver and where such a value is COMPOSED into a new stream. Run with the walker's own detectors over the tip and over the tip-plus-this-leaf: live read sites **22** at both (its own arm is `reads + 1 === READ_SITES.length`, so 22 is exactly green), **zero** read sites anywhere under `src/domain/edit`, `SEED_IDENTS` **16** files with this leaf NOT among them, compositions **30 = 30**. `rollFrom`'s `seed` arrives as a plain parameter and is never taken off a world root, so **this leaf mints no entropy root**. |
| ⭐ **P2.17** | **the GOLDEN-FREEZE roster (THE PROMISE's instrument)** | **NOT OWED — measured** | `tests/lint/goldenFreeze.walker.test.js` derives the same-seed roster FROM THE TREE by AST pattern (`process.env.UPDATE_*` as a MemberExpression) over every `tests/**` file, and convicts any golden instrument with neither a register row nor a written exclusion. Executed with the walker's own enumerator over the corpus WITH this packet's test file installed: **2,671** test files scanned, **53** carry such an env, and `tests/domain/editPools.test.js` carries **`[]`** — with a POSITIVE CONTROL proving the detector fires on a planted `process.env.UPDATE_PROBE`. No register row is owed and the roster floor cannot move. ⛔ Binding on the build lane: this packet's test file spells **no** `process.env` of any kind and writes **no** fixture. |
| ⭐ **P2.18** | **the SIZE RATCHET** | **NOT OWED — measured** | `scripts/.size-baseline.json` + `tests/lint/sizeBaseline.test.js` hold an EXACT SET: the baseline's keys must equal the files that actually exceed their LAYER ceiling. `src/domain/**/*.js` resolves to `max-lines 800 (skipBlankLines, skipComments)`; the baseline holds **7** entries and **0** under `src/domain/edit`; a working spelling of this contract measures **55** effective lines under eslint's own `Linter`. The exact-set arm cannot move, and §11's own 250 STOP fires four hundred lines before the layer ceiling does. |
| ⭐ **P2.14** | **tuning inventory (interim rule 17)** | **NOT OWED — measured, and the contract keeps it so** | `tests/lint/.tuning-inventory.json` banks this file at **zero** (it is absent from both maps). Executed with the library's own `countUnregisteredNamed` / `countBareDecimals`: **P2 = 0, P3 = 0** for the new leaf, `totals` **535 / 6985 UNMOVED**. §6's tuning block is the binding instruction that keeps both at zero. No tuning-register row; no inventory refreeze. |

> ⛔ **THE SHARED CENSUS ROW IS DEFERRED (§417 / MF-T2Q shape).** Every wave-1 EM member adding a
> test file owes the same re-record, and `implementation-packets.mjs` refuses one path claimed by
> more than one NON-TERMINAL packet — **DRAFT reserves exactly as READY does.** No edit is made;
> the chair inserts this row once, at the terminal:
>
> ```json
>         { "action": "TEST", "path": "tests/lint/.lighting-census-baseline.json" }
> ```
>
> ⭐ **THE DEFERRED PATH IS THE BASELINE JSON, NOT THE WALKER.** The refreeze writes
> `CENSUS_BASELINE_REL = 'tests/lint/.lighting-census-baseline.json'` and nothing else.
>
> ⚠ **INTERIOR RED, NAMED IN ADVANCE — SHAPE ONLY:** the walker reds at this member's tip with
> `the estate's file count moved — re-measure, do not re-word: expected <N+1> to be <N>`, where
> `<N>` is whatever the live baseline reads when this member commits. ⛔ No absolute is spelled.
> ⭐ `EM-A2a.manifest.json` therefore **omits** that path.

---

## 8. Ordered coding sequence

0. Dispatch and seal; re-read `git rev-parse HEAD` in the same command. **Confirm EM-P4 is LANDED**
   (`src/domain/institutionLookups.js` and `src/data/institutionLookups.js` both present).
1. Capture the baseline: the five census figures; `sha256` of
   `tests/fixtures/generator-golden-master.json` before any edit; the two tuning-inventory counts
   for `src/domain/edit/pools.js` (both must be 0 at the end).
2. Add `tests/domain/editPools.test.js` with A1–A8 **failing**.
3. Implement: `rollFrom`'s stream and the normalizer first, then the ONE frozen literal vocabulary
   (`cause.remove`), then the eleven imports and the five catalogue readers, then `POOLS` and
   `poolValues`.
4. Sole writer / lifecycle seam: **NOT APPLICABLE** — record the step as skipped.
5. Wire consumers: **NOT APPLICABLE** — lands DARK. Record as skipped.
6. Registrations: none owed (§7). The prevention guard is A7, inside the acceptance file.
7. Run focused verification (§10), including the three architecture walkers.
8. Run the wave-end gate per the train's plan; write the completion receipt.

```text
rollFrom:
1. values = poolValues(poolId, world)
2. if values.length === 0 -> return null                 // the typed absence, NOT undefined
3. if !Number.isInteger(n) || n < 0 -> n = 0             // clamp, never throw; the literal 0, not
                                                          // a named numeric const (interim rule 17)
4. rng = createPRNG(`edit-pool:${poolId}:${String(seed)}:${entryId}:${n}`)
5. picked = rng.pick(values)                             // exactly one draw on OUR stream
6. return picked === undefined ? null : picked           // step 2 makes this unreachable; kept
                                                          // because pick's contract is undefined

poolValues:
1. src = POOLS[poolId]; if !src -> return EMPTY (the one frozen [])
2. raw = src.values ?? src.read(world ?? {})
3. if !Array.isArray(raw) -> return EMPTY
4. normalize: String(v) -> trim -> drop '' -> dedupe, first occurrence kept
5. if poolId === 'tier' -> keep TIER_ORDER's sequence   // the ONE stated exception
   else                 -> sort with compareCodepoint
6. return Object.freeze(result)
```

---

## 9. Acceptance matrix

`tests/domain/editPools.test.js`, one literal `describe`, **eight** straight-line `it`.
⭐ Every case below is HOMED in that one file, and §7's first `CREATE` row authorizes it
(interim rule 12).

| ID | Case | Required observation |
|---|---|---|
| **A1** | **Main + GUARD-THE-GUARD, first** | `POOLS` holds exactly the **eleven** ids of §6 (set-equal both directions, full offender list). Every pool returns a frozen, non-empty, duplicate-free `string[]` on a real town world — asserted BEFORE any negative so no later arm passes on nothing. `institution.class` is exactly **85** and set-equals `[...getInstitutionsForTier('town')]` **imported from `src/domain/institutionLookups.js`**; `faction.category` set-equals `Object.values(FACTION_ARCHETYPES)` (13); `tier` equals `TIER_ORDER` (6). ⭐ **AND THE TOTALITY ARM EM-A1 ASSIGNED TO THIS PACKET (version 7):** `POOLS`' ids are held against `FIELD_DECLARATIONS`' `kind: 'pool'` ids in BOTH directions, with a full offender list each way and the two lawful residues named IN THE ARM so neither can silently grow — **declared-but-unpooled** is exactly `npc.role`, `power.holder`, `worldFact.resources` (EM-A2b's three: PENDING, and the arm asserts that set exactly, so EM-A2b's landing shrinks it to `[]` and a FOURTH would red here), and **pooled-but-undeclared** is exactly `cause.remove`, `commodity`, `tier` (this packet's own three: offered to a verb that declares no field today). Measured at `583f8f644`: eleven declared ids, eleven `POOLS` ids, **8 shared · 3 · 3**. |
| **A2** | **Absence is the typed value; nothing throws** | `null`, `undefined`, `{}`, `{tier:'nonsense'}`, `{institutions:null}` and an unknown pool id each return a **frozen array** — never `null`, never `undefined`, never a throw — and `rollFrom` returns **`null`** for each empty pool. ⭐ **The tier gate is anchored on a REAL tier, so the arm cannot be vacuous:** `{tier:'town'}` → **85** and `{tier:'city'}` → **81**; **ABSENCE** (`null`, `undefined`, `{}` — no `tier` at all) reads this module's OWN `?? 'village'` default → **58**; a **NON-catalogue** tier string reads `[]` — `'nonsense'`, and with it the wizard's sentinels `'random'` and `'custom'`, which §6's tier block proves no stored settlement can carry. |
| **A3** | **⛔ SOURCE IDENTITY, AND WIZARD PARITY** | Each catalogue pool set-equals its source live. A source scan proves the module imports **neither display seam, no JSX at all, and no `src/generators/**` path** (matcher proved live on a planted string). ⭐ **`worldFact.terrain` set-equals the wizard's own seven `<option>` values** — read from `ConfigurationPanel.jsx` by the TEST, never by the module — and `worldFact.culture` set-equals `CULTURES` (11) **read at `src/domain/worldFactOptions.js`**, so a drift between the wizard's menu and the generator's table reds HERE. ⭐ **`worldFact.monsterThreat` set-equals `MONSTER_THREAT_TIERS` — exactly the three `heartland, frontier, plagued`, asserted as a sorted list and never as a length** — and is asserted NOT to equal `MONSTER_THREAT_RANDOM_POOL`. The tier gate is proved LIVE: a `minTier:'metropolis'` row is absent from `institution.class` at `city`, present at `metropolis` (executed: 81 vs 115). |
| **A4** | **Boundary: stable order and the ONE exception** | Every pool except `tier` is in `compareCodepoint` order (re-sort and compare). `tier` is `TIER_ORDER` **verbatim** and is asserted NOT codepoint-sorted (`'city'` precedes `'hamlet'` alphabetically but follows it here), pinning the exemption in both directions so it cannot be silently "fixed". |
| **A5** | **Idempotency and the reopen guarantee** | `rollFrom(p,w,s,e,3)` returns the same value on three calls (`toBe`). Rolls 0–9 are taken in order, then roll 3 is taken **alone on a fresh call** and equals the earlier roll 3. Different `entryId`s at the same `n` differ for at least one pool, and different `seed`s likewise — both key segments proved live. |
| **A6** | **⛔ THE NO-DRAW LAW AND THE GOLDEN'S STRUCTURAL PROOF** | With `setActiveRng(createPRNG('probe'))` live, the ambient sequence is fixed by recording two draws; after re-seeding, **fifty-five `rollFrom` calls across all eleven pools** are made and the NEXT ambient draw is asserted equal to the recorded second value — the world's stream did not advance by one step. With **no** active RNG, `rollFrom` does not throw. Plus: a scan over `src/**` finds **zero** importers of the leaf, with guard-the-guard arms on the walk and the matcher. |
| **A7** | **THE IMPORT FENCE, both directions (the prevention guard)** | The import list is asserted EXACTLY: it contains the **eleven** modules — `src/kernel/prng.js`, `src/domain/deterministicSort.js`, `src/domain/institutionLookups.js`, `src/domain/factionArchetypes.js`, `src/domain/entities/npcs.js`, `src/domain/entities/status.js`, `src/domain/worldFactOptions.js`, `src/data/resourceData.js`, `src/data/constants.js`, `src/data/monsterThreat.js`, `src/data/stressTypes.js` — and **none of** `src/generators/lookups.js`, `src/generators/steps/resolveConfig.js`, **any other `src/generators/**` path**, `src/kernel/rngContext.js`, `src/data/namingData.js`, `src/generators/npcGenerator.js`, `src/domain/dossier/powerStrata.js`, any `*.jsx`, any `src/components/**` or `src/store/**` path. Proved non-empty first. |
| **A8** | **⛔ THE TWO STATE VOCABULARIES ARE THE TREE'S, READ AT THEIR HOMES** | `npc.status` set-equals **`NPC_STATUS_VALUES`** (imported from `src/domain/entities/npcs.js`) — the **seven** `active, dead, exiled, jailed, missing, removed, retired` — and `institution.state` set-equals **`ENTITY_STATUS_VALUES`** (five: `active, destroyed, impaired, removed, vacant`); exact sorted lists, never lengths, both frozen. ⭐ **`jailed` is asserted PRESENT by name** — the one value design §15's prose named that the tree lacked until `95e494bdb`, now the tree's own. ⭐ `destroyed` and `removed` are both present and DISTINCT, which is design §15's "destruction is a state the record keeps; removal is erasure" made structural. ⛔ **The test spells no union literal it does not read from those two exports**, and it does NOT re-implement EM-P4's typedef pin. |

**8 of ≤8.** Omit nothing; add nothing during implementation.

---

## 10. Verification commands

```sh
npx eslint src/domain/edit/pools.js tests/domain/editPools.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict          # src/domain/edit/** must be strict-clean (§P4)

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/domain/editPools.test.js

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/build/domainGeneratorsBoundary.test.js

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/statusUnionTotality.walker.test.js tests/lint/catalogTierGateParity.walker.test.js \
  tests/lint/tuningRegister.walker.test.js tests/lint/negativeAssertionAnchor.walker.test.js

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/kernel/prngForkLabelDelimiter.test.js

node scripts/check-observed-shape-readers.mjs
node scripts/implementation-packets.mjs validate
npm run check:packet -- EM-A2a
npm run implementation:resume -- EM-A2a
```

Expected: every command exits `0`, **except** the named interior reds below. ⛔ Never read a gate
through a shell pipe (§P7). A member never runs `npm run check`.

### ⭐ THE BUILD LANE'S INSTRUMENTS — not sealed checks, and each one's expected exit

1. **`tests/lint` WHOLE, EXCLUDING TWO WALKERS, MUST EXIT 0:**
   `npx vitest run --pool=threads --maxWorkers=2 tests/lint --exclude=tests/lint/sovereigntyLightingContract.walker.test.js --exclude=tests/lint/proseWiringCensus.walker.test.js`.
   A CREATE under `tests/domain` opts into every walker that governs that directory, and the
   `tests/lint` whole run is how the estate finds the one nobody predicted.
2. ⭐ **`tests/lint/proseWiringCensus.walker.test.js` ALONE, EXPECTED NONZERO (interim rule 17).**
   This member CREATEs one `.js` leaf under `src/domain`, so `stamp.producerIndexFiles` moves by
   **+1** — ⛔ **read the live pair off the committed census in the receipt; this packet states no
   absolute** — and the committed census is stale until the chair regenerates it WHOLE at the
   train's terminal. It reds on **exactly two arms**, both in the `car 0 — the committed JSON and
   its interlock` describe (the committed-census/stale-stamp interlock; EM-A1's build met the same
   pair this night). ⛔ **The build lane QUOTES the two arm titles verbatim in its receipt** and
   does not cure them: the cure is the terminal's regeneration, and a member that runs
   `node scripts/wiring-census.mjs` produces a conflicting blob (interim rule 1).
3. **`tests/lint/sovereigntyLightingContract.walker.test.js` ALONE, EXPECTED NONZERO** — the
   lighting interior red of §7 P2.1, cured by the terminal refreeze, never by this member.
4. ⭐ **THE COUNT PROVER, BEFORE THE SEAL (interim rule 12; KIT FURNITURE, never placed in the
   tree, never a `checks` entry, never a §7 row):**
   `node <kit>/packets-waiting/EM-A2a.count-prover.mjs <kit>/packets-waiting/EM-A2a.md <kit>/packets-waiting/EM-A2a.manifest.json`
   — exit 0 required. It reads the packet's Markdown and the capsule JSON only.
5. **THE TUNING INVENTORY, AT THE END OF THE EDIT:** re-run the library's own
   `countUnregisteredNamed` / `countBareDecimals` over the staged file; both must read **0** for
   `src/domain/edit/pools.js` and the two totals must be UNMOVED at 535 / 6985. A non-zero count
   is a §11 STOP, not a register row.

---

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and §P8, stop if: the seal is missing or foreign; **EM-P4 is
not LANDED at the base**; HEAD is not the stamped base or a descendant proved non-interfering by
execution; `pools.js` measures **> 250 effective lines** under eslint's own `Linter` with
`skipBlankLines` and `skipComments` (take a further split — **never squeeze**); a golden or the
prose manifest moves by one byte; `prngForkLabelDelimiter.test.js` moves (`rollFrom` reached for
`fork` or spelled `::`); A6 shows the ambient sequence advancing; the leaf would need
`namingData.js`, `npcGenerator.js`, `rngContext.js`, any `src/generators/**` module or any `.jsx`
to satisfy an arm; **A3's wizard-parity arm reds** (the wizard's menu and the generator's table
have drifted — a finding, not a thing to repair here); a pool would need a value the engine does
not hold; `getInstitutionsForTier` is found to return anything but a `Set`.

⛔ **AND, NEW IN VERSION 6 — THREE ARCHITECTURE STOPS, each measured green before this packet was
written, each a STOP if the build finds otherwise:**

- **`tests/build/domainGeneratorsBoundary.test.js` reds** — the leaf has grown a
  `src/domain` → `src/generators` edge. The walker's own message forbids widening `BASELINE_EDGES`;
  the cure is the address, never the baseline.
- **`tests/lint/statusUnionTotality.walker.test.js` reds** — the leaf has spelled a union literal,
  or a sibling's edit moved the DERIVED trigger away from `['dead','exiled','retired']`. ⛔ R6′ is
  the chair's to re-rule, never a lane's.
- **`tests/lint/catalogTierGateParity.walker.test.js` A5 reds** — some module under
  `src/{generators,domain,lib}` now imports a specifier ending `/lookups.js`. This leaf must not be
  that module, and no comment in it may quote such an import line.

⛔ **AND FOUR MORE, NEW IN VERSION 7 — the governors the pre-proof hunted down, each measured green
for this contract with its own convicting control (§7 P2.15–P2.18). Every one of them is a STOP,
never a row to add:**

- **`tests/lint/determinismBanCoverage.test.js` reds** — the leaf's resolved eslint config no longer
  carries the `src/domain/**` `no-restricted-syntax` ban at ERROR with all 34 selectors. The cure is
  never an exemption: a domain leaf that needs `Math.random`, `Date.now`, `localeCompare` or
  `import.meta` is the wrong design, and this one needs none of them.
- **`tests/lint/entropyRootCensus.walker.test.js` reds** — the leaf has grown a read of a world seed
  off a `WORLD_ROOTS` receiver, or a composition the census does not bank. `rollFrom`'s `seed` is a
  PARAMETER; it is never taken off `worldState`, `startingWorldState`, `state`, `ws`, `raw` or
  `base`. ⛔ THE PROMISE: a seeded roller in this estate mints no new entropy root.
  ⭐ **VERSION 9:** the walker ALSO pins the `createPRNG(` site count; this leaf's ONE root composition moves it 35 → 36 in `src/domain` and 46 → 47 whole-src BY DESIGN and is re-recorded in §7's TEST row — that exact movement is NOT this STOP. Any OTHER movement of that walker (a `WORLD_ROOT` read, a second site, a `COMPOSITIONS` row) still is.
- **`tests/lint/goldenFreeze.walker.test.js` reds** — this packet's test file has acquired a
  `process.env.UPDATE_*` reader or writes a fixture, which would enrol it as a same-seed instrument
  with no register row. It must do neither.
- **`tests/lint/sizeBaseline.test.js` reds** — the leaf has crossed its layer ceiling. §11's own 250
  STOP fires first by four hundred lines; if it somehow does not, **decompose, never baseline**.

---

## 12. Completion receipt

Base SHA · seal identity · **EM-P4's LANDED sha** · final tree state · exact changed files and
effective-line deltas (measured with eslint's `Linter`) · acceptance A1–A8 executed · focused
commands, exits and counts · **the three architecture walkers' exits** · **the two tuning-inventory
counts before and after (both 0) with the two totals unmoved** · **the wiring-census walker's two
red arm titles, quoted verbatim** · **the four version-7 governors' verdicts (the ban-coverage
resolution for this file, the entropy census's read-site and composition counts, the golden
roster's `UPDATE_*` result for the new test file, the size ratchet's effective-line count)** ·
**the lighting tuple's five figures before and after, against the predicted `+1/+0/+1/+8/+1`** · sealed per-step receipt and resume status · both typecheck
configurations · gate stages actually executed · base-versus-wave failure identity diff ·
dormancy/golden result (fixture digest before and at the tip) · the ambient-stream no-draw proof ·
census tuple before and at the tip with the interior red quoted verbatim · generated artifacts
`NONE` · deviations `NONE | STOP` · out-of-scope observations without investigation ·
**judgment calls: `NONE`**.

---

## 13. RAISED — for the chair

| # | Item |
|---|---|
| **R1** | **ARCH §9's `Pools:` line still lists nine pools** and names neither the world-facts pools nor the two §15 vocabularies, although §934.46 amended its op list in the same commit. A documentation correction, not a build question. |
| **R2** | **The decision-fork and mechanism-coverage registers do not reach `src/domain/edit`** (`chooserTotality` `SCAN_ROOTS`; the mechanism walker's worldPulse enumeration). The editor's seeded chooser is registered nowhere. Recorded NOT OWED by measurement; the gap is real. Grow the roots (a register act, its own member), or accept the gap on record. |
| **R3** | **The A2a/A2b split is taken** per ruling (5), on the pre-declared table-vs-world line. ⭐ Measured at the EM-T10 pre-proof: the two packets share two change paths, and the validator refuses two non-terminal claimants — **EM-A2b is placed only after EM-A2a is LANDED**. |
| ⭐ **R4** | **RULED, AND THE COMBINED `pools.js` ESTIMATE NOW FITS with room: ≈230 OF 250.** EM-A2b §3 binds on the COMBINED figure, not on either packet alone. Re-derived at the compile tip from each packet's OWN decomposition: this packet **≈140** (§3's table, version 6) plus EM-A2b **≈90** (its own §3, after ESTATE-REPAIR-4 ruling 1 moved `worldFact.goods` ≈6 and `worldFact.services` ≈12 to EM-P3c) ⇒ **≈230, about twenty lines of margin**. ⚠ The history, kept because it is why the ruling was needed: version 4 read ≈256 and was OVER; version 5 read ≈238 combined; version 6's two IMPORTED vocabularies take this packet from ≈148 to ≈140. **EM-A2c stays PRE-DECLARED**, re-aimed at `worldFact.resources` (≈12) and, once EM-A2b's R1 is ruled, `worldFact.tradeAccess`. ⛔ **No row was squeezed to reach ≈230** — the lines come from the ruled re-aim. §11's STOP still fires at implementation if eslint's `Linter` MEASURES over 250. |
| ⭐ **R5** | **ANSWERED BY THE TREE, AND THE ROW EXISTS NOW.** Versions 1–5 cited "RAISED R5" in §6 C2 and carried **no R5 row at all** (§13 held R1–R4 only) — the count-law defect judgment 79 ruling 6 names. The question was: design §15's prose lists `jailed` and the tree's `NpcStatus` did not hold it. **The tree answered on its own at `95e494bdb` (EM-B1d, train EM-T3): `jailed` IS a member, and the union is seven.** Version 6's `npc.status` is seven and A8 asserts `jailed` PRESENT by name. ⚠ **What is NOT answered and is not this packet's to answer:** §15's prose also says `present` and `departed`, which the tree spells `active`, `retired` and `removed`. The tree governs the pool (chair's fact 3); whether the DESIGN TEXT is corrected is a documentation act, slotted beside R1. |
| ⭐ **R7** | ⭐ **THE TRAIN, RE-MEASURED AT THE EM-T11 PRE-PROOF.** EM-A2a's two CHANGE paths intersect **NONE** of train EM-T10's seven registered members (EM-B1i · EM-B2a1 · EM-B3e · EM-B1a · EM-D0c · EM-D0d · EM-P4) and **NONE** of the written EM-T11 roster (EM-C4a · EM-B1c1 · EM-B2a2 · EM-B3f). ⚠ **TOOL-30 has no manifest in the kit or the tree yet**, so its intersection is UNMEASURED and named here rather than assumed — the chair re-runs it once TOOL-30 is written. EM-P4 MODIFIES or CREATEs **four** of this packet's `requiredSymbols` paths, which is the HARD dependency, not a collision: no CHANGE path is shared, and EM-P4 rides the EARLIER train. |
| ⭐ **R6** | **THE WIRING-CENSUS WALKER'S TWO RED ARM TITLES.** Interim rule 17 asks the packet to NAME them. This seat may run no vitest, and the arms are reached only by executing the walker; the two are in the `car 0 — the committed JSON and its interlock` describe and EM-A1's build (queue window 15, this night) met the identical pair. **The chair holds those two titles from EM-A1's log; §10 instrument 2 instructs the build lane to quote them verbatim in its receipt.** If the chair prefers them spelled in the packet, one line in §10 carries them at placement. |

---

## 14. REVISION HISTORY — the contract facts, in the order they landed

Fact (2) landed in revision 2 as **C1**: no faction record carries `type` or `archetype`; the field
is `category`, and `archetype` is a DERIVATION (`factionArchetype(f)` from `f.category`), which
design §14 forbids editing. The pool is therefore `faction.category` over the same thirteen
values, and **`stance` is dropped**. `RELATIONSHIP_KIND_ORDER` left `requiredSymbols` with it.
Fact (3) landed as **C2**: both state vocabularies are the tree's own typedefs, not §15's prose.
Fact (1) is EM-B1a's. Pools: eleven → **ten**. Estimate: ≈147 → **≈143**.

⭐ **IN VERSION 4 (ESTATE-REPAIR-4):** pools **ten → eleven**, estimate ≈143 → **≈148**. The count
is the same numeral revision 2 started from and it is NOT the same set: revision 2 dropped
`stance`; version 4 added `worldFact.monsterThreat`.

⭐⭐ **IN VERSION 6 (judgments 79 · 86 · 88 R5′):** the pool COUNT does not move — it is eleven in
every sentence of this packet, which is itself one of version 6's repairs (the title, §9 A6 and
three capsule strings still said ten). What moves is WHERE four pools read and HOW two of them are
spelled: `institution.class` at `src/domain/institutionLookups.js`; `worldFact.terrain` and
`worldFact.culture` at `src/domain/worldFactOptions.js`; `npc.status` and `institution.state`
IMPORTED from `NPC_STATUS_VALUES` and `ENTITY_STATUS_VALUES` instead of mirrored as frozen
literals — and `npc.status` becomes **seven** because the tree's union did, at `95e494bdb`.
The estimate moves ≈148 → **≈140** (two mirrors out, two imports in), the A7 fence goes from ten
entries to **eleven modules** with `powerStrata.js` deleted, and three `requiredSymbols` rows leave
`src/generators/lookups.js` for the homes EM-P4 gives them. ⛔ **Nothing was weakened: three landed
guards — the domain→generators ratchet, the status-union trigger and `catalogTierGateParity` A5 —
are all kept at full strength, and all three were EXECUTED green against this contract before it
was written.**

⭐⭐ **IN VERSION 7 (the independent pre-proof at `583f8f644`):** nothing of the contract moves —
eleven pools, eight acceptance cases, eleven fenced modules, one stream key, ≈140 of 250. What moves
is what the packet CLAIMS about the tree. A2's random/custom/village sentence is REFUTED and
replaced by the executed table of §6's new tier block, with the settlement record's own tier traced
from `resolveConfig` to `assembleSettlement` to prove no special case is needed. `Depends on:
EM-A1`'s membership clause is corrected against EM-A1's own landed walker text, and the totality arm
that walker assigns to this packet is written into A1 with both residues named. The last stale
absolutes (`1172 → 1173`) leave §7 and §10 for a bare `+1`. The Verified base row gains the stamp's
exact `` `<branch>` at `<sha>` `` form, measured against the validator's READY arm. Four further
governors — the determinism ban-coverage pin, the entropy-root census, the golden-freeze roster and
the size ratchet — are hunted and given §11 STOPs; three are priced NOT OWED with controls, and the
entropy-root census, priced NOT OWED at version 6, was refuted by the gate and is OWED at version 9 (§7's TEST row). And the two
figures this packet had only predicted are now EXECUTED: the lighting tuple's `+1/+0/+1/+8/+1` and
the count law, whose prover passes here and fails on four deliberately wrong copies.
