# `simulation / participation` — EM-B1k2: the irreversible reads take the RAW roster by construction, and the habitat is removed

- **Status:** `READY`
- **Packet version:** `2`
- **Verified base:** `fixes-2026-09-18-consist` at `154bd7c07a2e4f6bf900fd8a7572050b9692c79e`
- **Last revalidated:** 2026-09-20 at `154bd7c07a2e4f6bf900fd8a7572050b9692c79e` — stamped by the chair (session a9df403c) at placement, the window re-run in the same command as the stamp: since the pre-proof's tip `63e40fe57` the branch gained CURE-E, CURE-F (the `:579` comparand this packet quotes — re-read), CURE-G (a row in the shared mutation register; EM-B1k's suite renamed `participationWriteBase.contract.test.js`), the second lighting refreeze, EM-B3c's re-pin and its LANDING (a second register row; migration 203); over every path of this packet outside the expected movers (scripts/mutation-coverage-manifest.json, scripts/mutation-sweep.sh, src/domain/worldPulse/pulseKernel.js, tests/domain/roadsParticipation.test.js) `git diff --stat` printed NOTHING; every `requiredSymbols` row resolves; both CREATE targets absent and untracked; no non-terminal packet reserves a change path (EM-B3c is LANDED). ⛔ THE CHAIR'S CUTS AT PLACEMENT: (1) `checks` gains `tests/lint/contractTestAntiVacuity.walker.test.js` — the `contract` token this packet's test file carries is consumed by that walker too (run 18 red on EM-B1k's renamed suite for exactly this; ODQ §934.47 addendum 49): every 'every…' claim in the new suite derives its population from the producer, never a literal; (2) the build lane runs that walker in its instruments and treats its red as a STOP of its own. Its gated batches wait for run 19 to release the gate — PAUSE-AND-RESUME.
- **Depends on:** `EM-B1k` — ⭐ **LANDED at `19c4cb853`, flipped at `63e40fe57`** (measured; §0 discharged). ⛔ **AND a SECOND gate, new in version 2:** this packet now names `scripts/mutation-coverage-manifest.json`, which **`EM-B3c` (READY) also reserves** — two non-terminal packets on one path red `validate:packets` by construction (`implementation-packets.mjs:676-700`; `TERMINAL_PACKET_STATUSES = {LANDED, SUPERSEDED}` at `:43`). ⇒ **placeable only once EM-B3c is terminal**, or once the chair moves one of the two rows (§13 Q2).
- **Collision group:** ① `EM-B1k` on `tests/domain/roadsParticipation.test.js` — **DISCHARGED** (LANDED). ② ⛔ `EM-B3c` (READY) on `scripts/mutation-coverage-manifest.json` — LIVE. Of the 191 manifest entries nothing else names any path here: `EP-3A`/`EP-3B` (LANDED, `roadsKernel.js`), `EM-B1d` (LANDED, `factionLifecycle.js`), and 30 LANDED entries on the mutation register. `scripts/mutation-sweep.sh` is reserved by **nobody**.
- **Commit authority:** edits only; the chair commits
- **Baseline posture:** measured at `63e40fe57` — the `tickStart` read and its two consumers; the `fresh` fallback; the two permanent roster writers at their real triggers; the widened census's exact convictions (three, not one); the four chunk closures re-derived through vite's own export; the edge-shared input membership; the line-addressed, stamped and mutation registers; the size ceilings of all four production files; the `tests/` fixture sweep; the 525-row name census; every save writer
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: `b90a95b7af484137ecf70bd15cde5054edf66b5db0d9f6e90c974d97b7caa5e1` — stamped by the chair at placement; **re-measured identical at `63e40fe57`**)

> **WHAT VERSION 2 CHANGED AND WHY.** The pre-proof re-measured every verified fact at the tip where EM-B1k is LANDED, and four things moved. (1) ⭐ **The chair's fourth-root instruction is refuted by measurement**: `src/generators/density` does **not** convict `titularSuccession.js` (that file reads the roster only through `factionRosterOf`; the literal `.npcs` never appears in it) — it convicts `applyDensityLaw.js`. Giving `titularSuccession.js` the disposition row the chair ordered therefore requires the scan's PREDICATE to widen by one `-e` pattern; the census goes 41 → **44** with **three** new dispositions, not one (§6.3, §13 Q1). (2) ⭐ **The chair's `contract`-token rename is priced and it is not free**: the rename opts the CREATE file into `enumerateInvariants` (executed: `NAME_PATTERN.test('irreversibleRawRoster.contract.test.js')` → true), which owes a `mutation` row, which is joined one-to-one to a REAL plant in `scripts/mutation-sweep.sh` — two registration files, and a collision with EM-B3c (§7, §13 Q2). (3) The dist-gated `tests/build` pair LEAVES the sealed `checks` under the chair's Q7 ruling; the byte proof is the chair's build + `verify:dist` at the landing, and the measurement stays in the narrative. (4) Q4 and Q5 are ANSWERED by executed measurement — zero duplicate display names across 525 rows / 5,171 NPCs, and no save writer can produce a roster-bearing save without `.settlement` — so EM-B1k3 is not chartered, no raw-fallback token is owed, and A6 gains the uniqueness assertion. Every line address in §5 and §6 is refreshed to the tip (EM-B1k's +118 moved the whole ratchet block), and the eager closure is re-derived through `vite.config.js`'s own `EAGER_FIRST_PAINT_MODULES` (**268** modules; the evidence's replica figure of 251 is retired).

> **REVALIDATION SENTENCE for the chair:** *"Revalidated at `<tip>`: EM-B1k is LANDED at `19c4cb853` and `buildSettlementMap`'s write base is raw (`pulseKernel.js:184`, the `localSettlements` seed at `:579`, both quoted); `factionDensityKernel.js`'s `tickStart` still reads `item.settlement` verbatim at `:699` and `item` is read nowhere else in `advanceFactionDensity` but `:698`; `readFactionLifecycle` still consumes only `governed` (`:714`) and `reactions` (`:732`) there; `foundReaders` (`:425`) still greps exactly `src/domain/worldPulse` and `src/domain/spatial` and returns 41 files; the widened scan convicts exactly `src/domain/density/factionLifecycle.js`, `src/generators/density/applyDensityLaw.js` and `src/generators/density/titularSuccession.js` (44); the four production files measure 395 / 462 / 50 / 838 effective lines; all **twenty-one** `requiredSymbols` resolve EXACTLY ONCE; both CREATE spellings are absent; the preamble hashes `b90a95b7…`; and the only non-terminal reservation against this packet is EM-B3c's on `scripts/mutation-coverage-manifest.json`."*

---

## 0. Scope, and why this packet exists at all

EM-B1k made the tick's **write base** the raw save roster — two single-line replacements in `pulseKernel.js`, now LANDED — and that alone stops both defects. This packet removes the **habitat**, and the habitat is measured, not feared:

1. ⛔ **`factionDensityKernel`'s lifecycle read is still filtered.** `tickStart` (`:699`) is `asObject(item.settlement)` — the participation view. Under EM-B1k the dissolution reaction is still **PRODUCED** from it and merely **REFUSED** downstream by `stillEmpty` over the raw `fresh`. ⭐ **Re-measured at the landed tip** over a 7-town corpus stride: **the reaction is produced 7 / 7 from the filtered read and 0 / 7 from the raw one**, with the 6 legitimate beats firing identically either way (evidence §15 — the compile's §3 figures reproduce exactly, now against real code rather than a model). An irreversible decision computed from a projection and rescued by a second guard is one edit away from firing.
2. ⛔ **The refusal depends on an unpinned invariant.** `fresh` FALLS BACK to `tickStart` when the update entry carries no `.settlement` (`:721-723`). Driven with such an entry, the shelved sole member's house is dissolved **7 / 7** even with EM-B1k's raw write base (evidence §4). The pre-proof traced both producers (`buildSettlementMap`, and the priced path through `applyLegitimacyHits`) and neither can mint such an entry TODAY (evidence §20) — but nothing in the tree *asserts* it, which is the whole difference between a defence and a coincidence.
3. ⛔ **The `.npcs` reader census asserts a protection that does not exist.** `settlementLifecycleFirstClass.js` and `successorNpc.js` sit in `EXPECTED` (`:367`, `:368`) with **no disposition**, inheriting the file's blanket *"everything else is via-snapshot (protected by the gate, no edit)"* (`:191`). Both are permanent roster writers whose base is **their caller's choice**, and handed the filtered view both **DROP the off-stage soul** — measured, executed, both directions (evidence §5).

---

## 1. Reconciled authority

1. **The chair's rulings of 2026-09-20 (ODQ §934.47 addendum 39, Q1–Q7; addendum 43 ruling 3)** — EM-B1k's split is confirmed; EM-B1k2 removes the habitat: the irreversible consumers read RAW explicitly, the permanent roster writers are cured with the class (comment contract + census disposition + pin), A5/A6 drive at the real triggers, the new conviction goes to `EXPECTED` with its disposition, the dist-gated byte arms leave `checks`, and the CREATE file is named with the register's `contract` token from the start.
2. **The chair's ruling of 2026-09-19** — *"the participation view is a READ projection and must never be the WRITE base."*
3. **`DESIGN_THE_ROADS.md` §8** — the ONE participation chokepoint is a *participation* filter. ⛔ Every REVERSIBLE reading keeps it.
4. **§810.4 R18** (`factionLifecycle.js`, the `ROSTER_ABSENT_STATUSES` docblock at `:87`) — *an IRREVERSIBLE consequence may only be triggered by IRREVERSIBLE causes.*
5. **The tree's own confirmation comment** (`factionDensityKernel.js:258-262`).
6. **The tree at `63e40fe57`**, measured — every row of §5.

### §1.1 · Resolved contradictions — four, measured, each against a stated premise

| premise carried into this packet | measured verdict |
|---|---|
| *"`factionDensityKernel.js` carries no disposition comment and inherits the blanket"* (RECON-STAGE §2.1) | ⚠ **HALF FALSE.** The row at `:257` **does** carry a disposition — `TE-DENSITY-1` (`:246-256`), and it is about `applyCadence`'s raw read of `fresh` **only**. The `tickStart` lifecycle read is what inherits the blanket. The cure is a SECOND clause on the existing comment, never a new row. |
| *"`settlementLifecycleFirstClass.js:611` and `successorNpc.js:66` are currently unreachable"* (RECON-STAGE §2.1) | ⛔ **FALSE, BOTH.** `settlementLifecycleEnabled` is a `WAVES` virtual key lit in **all four presets including the default** `realistic_regional`; `advanceNpcCorruption` — the sole producer of `kind: 'ousted'` — is called **unconditionally** at `pulseKernel.js:388` (re-read at the tip). Both lanes are LIVE; their arms are therefore driven at REAL triggers (A5, A6). |
| *"the two permanent roster writers must read RAW explicitly"* (the launch's outcome 2, as worded) | ⛔ **NOT EXPRESSIBLE.** Neither function receives a save: `applySettlementLifecycleOutcomeToSettlement(settlement, outcome)` (`:578`) and `replaceOustedNpcs(settlement, oustedNames, rng)` (`:64`) have no raw roster in hand. Their base is their caller's, and both callers — `applyWorldPulse.js:733` → `:155` (from `buildSettlementMap`) and `npcVerdictPulse.js:133` ← `:99` ← `pulseKernel.js:617-618`'s `localSettlements` — are made raw **by EM-B1k**. The cure available here is the CONTRACT (a comment-only base declaration), the CENSUS DISPOSITION and the PIN. **RULED by the chair (addendum 39 Q2): that IS "cured with the class".** |
| ⭐ *"the fourth root `src/generators/density` gives `titularSuccession.js` its disposition row"* (the chair's pre-proof brief) | ⛔ **REFUTED, MEASURED.** That root convicts **`applyDensityLaw.js`** (`:319`, `const npcs = input.npcs || []` inside `disperseNamedRoster`). `titularSuccession.js` reads the roster ONLY through `factionRosterOf` (`:74`, `:177`, `:231`) and the literal `.npcs` never appears in it, so the scan cannot see it — and an `EXPECTED` row for a file the scan does not return REDS the exactness arm (`roadsParticipation.test.js:430`). Delivering the chair's intent costs one extra `-e` pattern on the scan (§6.3, §13 Q1). |

---

## 2. Outcome

**Observable result:** no permanent consequence in the density lane is ever **computed** from the participation view — not merely refused after the fact — and every reader that writes permanent roster state carries a written statement of which roster it is handed, enforced by a census that now reaches `src/domain/density` and `src/generators/density`.

**Definition of done:** a shelved sole member's house is never even PROPOSED for dissolution; the `fresh` fallback can no longer dissolve it either; the widened census convicts exactly three new files and each is dispositioned; the dispersal and the successor pass each carry the off-stage soul at their real triggers; and **nothing else moves at all** — the chain's output is byte-identical to EM-B1k's (measured, evidence §3, §15).

In scope:

1. one primary behaviour — the lifecycle read's base;
2. one required integration — the census's roots, predicate and six dispositions;
3. one prevention guard — the widened ratchet, the red-first suite, and the suite's own mutation row + plant.

Explicit non-goals: ⛔ no change to `isOffStage`, to `worldSnapshot`'s filter, or to any REVERSIBLE reading (arrivals, emergence founders, marks keep the participation view — the roads design's one chokepoint is not reopened). ⛔ No repair of already-damaged saves (EM-B1k §12.2; the chair decided NO, §934.62). ⛔ No change to `pulseKernel.js` — EM-B1k owns that file, and CURE-F is editing `:579` in the same window. ⛔ No identity-join cure for `replaceOustedNpcs`'s name matching — **CLOSED by measurement** (§12.1 item 2). ⛔ No editor packet work.

---

## 3. Hard scope budget

| Limit | Budget | This packet |
|---|---:|---:|
| Behavior families | `1` | **1** |
| New persisted record families · writers · flags · surfaces | `0 or 1` | **0 · 0 · 0 · 0** |
| New logic-bearing production leaves | `<=2` | **0** |
| Existing logic-bearing production files modified | `<=3` | ⭐ **1** (three further rows are ⛔ COMMENT-ONLY — **RULED not logic-bearing**, addendum 39 Q1) |
| Additional registration-only files | `<=3` | ⭐ **2** (`scripts/mutation-coverage-manifest.json`, `scripts/mutation-sweep.sh` — the price of the chair's `contract` rename, §7) |
| Handwritten files total | `<=12` | **8** |
| New/changed effective production lines | `<=400` | ⛔ **0** |
| Effective lines per new leaf | `<=250` | **≤250** (the one new test file) |
| Delta in a shared/hot file | `<=15` | **0** |
| Acceptance cases | `<=8` | **8** |

### §3.1 · Effective lines and ceilings — executed, at the tip

All four figures from eslint's own `Linter` under `max-lines` with `skipBlankLines: true, skipComments: true` (evidence §6, re-run §20):

| file | effective | governed by | headroom | this packet |
|---|---:|---|---:|---:|
| `src/domain/worldPulse/factionDensityKernel.js` | **395** | `max-lines` 800 (`src/domain/**`) | 405 | **+0** (one-line-for-one-line) |
| `src/domain/worldPulse/settlementLifecycleFirstClass.js` | **462** | `max-lines` 800 | 338 | **+0** (comment only) |
| `src/domain/worldPulse/successorNpc.js` | **50** | `max-lines` 800 | 750 | **+0** (comment only) |
| ⛔ `src/domain/worldPulse/roadsKernel.js` | **838** | `scripts/.size-baseline.json:14` = **838** | ⛔ **0, BOTH DIRECTIONS** | **+0** (comment only) |

⛔ **`roadsKernel.js` is tolerance-zero in both directions** and the estate already knows it: LANDED packet `EP-3A`'s own `requiredSymbols` note reads *"this file sits at exactly its frozen 838, tolerance-zero in both directions, so an own-line import would red eslint and a shrink would red sizeBaseline"*. A comment is the **only** lawful edit into it, and `max-lines`' `skipComments` makes it free. None of the four is on `PACKET_STANDARD.md`'s standing hot-file list at this tip (`EconomicsTab.jsx`, `OutputContainer.jsx`, `convergence.js`, `institutionLifecycle.js`, `peaceTerms.js`, `informationStatecraft.js`).

### §3.2 · Bundle budgets, priced — every chunk closure measured with vite's own export

| budget | ceiling | members among this packet's `src/` paths | cost |
|---|---:|---|---:|
| generation worker `WORKER_BUNDLE_CEILING_BYTES` | `1,401,208` (`generationWorkerLazy.test.js:159`) — ⛔ ZERO slack | ⭐ **NONE of the four** (220-module closure, measured) | ⛔ **0 B, proven by the import graph** |
| eager first paint `CLOSURE_BUDGET_BYTES` | `1,048,000` raw (`vendorPdfLazy.test.js:565`); last figure in the test's own header is **1,047,205** (`:539`) ⇒ **≈795 B of margin**, and raises are **OWNER-SIGNED** | ⭐ **`successorNpc.js` ONLY**, of the four. ⭐ Re-derived through `vite.config.js`'s own `EAGER_FIRST_PAINT_MODULES` (**268** modules — the replica figure 251 is retired): `successorNpc.js` **true**; `factionDensityKernel.js`, `roadsKernel.js`, `settlementLifecycleFirstClass.js` **false**. ⚠ `src/domain/density/factionLifecycle.js` is **true** — it is NOT edited here (census row only), and it is the membership the chair's preamble amendment already carries | ⛔ **0 B REQUIRED.** The `successorNpc.js` row is COMMENT-ONLY and the build strips comments, so the rendered module must not move **one byte**. ⛔ The comment carries no `/*!`, `@license` or `@preserve` token (those survive minification). A non-zero attribution for this module is a **STOP** — the first-paint budget is the owner's, not the chair's. |
| lazy `engine` chunk | `< 679_000` (`vendorPdfLazy.test.js:787`); last measured **678,131** (`:776`) ⇒ **869 B of margin** | `factionDensityKernel.js` (main-graph member: not eager, not in the worker closure) | **≈0 B** — one token-level replacement, no new import. Bounded at **≤ 200 B** (the added source text ×2, stated as such), proven by attribution. |
| `advanceInterval.worker` | ⛔ none today; TOOL-3 mints one with **4 KB per-train headroom** | **all four** (549-module closure) | **a DELTA, ≈0 B; no per-packet re-mint** (the chair's Q3 ruling, recorded at EM-B1k §3.2) |
| edge-shared bundles | INPUT membership against the five metas' own `inputs` | ⭐ **NONE** — 0 hits across `aiCharterBundle` (114 inputs), `aiGroundingBundle` (74), `aiOutputSchemaBundle` (115), `analyticsEventsBundle` (2), `intentAtlasBundle` (2), re-measured at the tip | ⛔ **no rebuild, no `_shared` rows, and NO GENERATOR in `checks`** — §P2.12's seven-path hazard does not arise |

⛔ **THE BUILD LANE'S STEP, because two budgets are near-full — NARRATIVE, NOT `checks` (chair Q7).** After the edits: one real `npm run build` through the exclusive mutex; the kit's per-module attribution (`tools/attrib.config.mjs`, RENDERED lengths; it reads main-graph chunk sizes 742 B low); **ONLY `factionDensityKernel.js` may show any movement at all, and `successorNpc.js` must show ZERO**; then `tests/build/vendorPdfLazy.test.js` and `tests/build/generationWorkerLazy.test.js` green against the fresh `dist`. ⛔ Both arms are `skipIf(!requireDistRead)` and measure nothing without a build, which is exactly why the chair took them OUT of the sealed `checks`: **the byte proof is the chair's build + `verify:dist` at the landing.** Any rise in the first-paint closure is a **STOP for the chair and the owner**; no ceiling is re-minted by this packet.

### §3.3 · Registers — measured, all six

`scripts/.size-baseline.json` — §3.1 (`roadsKernel.js` 838 at `:14`; the other three carry no row). `docs/content/wiring-census.json` `stamp.files` holds **7 entries**, all under `src/domain/display/stateProse/` — **none of the four** ⇒ ⛔ **no re-take**. `tests/lint/.prose-numerics-baseline.json` names **none of the four** (`grep -c` = 0) and this packet renders no figure. `scripts/.writer-reach-baseline.json` names none of them (`grep -c` = 0, all four). `tests/lint/.tuning-inventory.json` holds rows for three of them (2 / 7 / 0 / 19), but `tuningRegister.walker.test.js` keys on `spanDigest` and pins in terms that *"a comment must not move a digest"* (`:212-213`, verbatim at the tip) and *"a comment inserted above the table must leave the digest where it was"* (`:428-430`) ⇒ ⛔ **not a red**. ⛔ **No file in the estate cites any of the six by `path:line`** (`git grep -n "<path>:[0-9]"` over `src`, `docs/content`, `tests` → **zero hits, all six paths**) ⇒ step 16 clean, no citation re-address owed.

⭐ **THE SIXTH REGISTER MOVES.** `scripts/mutation-coverage-manifest.json` — see §7 and §13 Q2: the chair's `contract`-token rename opts the CREATE file into `enumerateInvariants` (706 files today), which owes a row, which owes a plant in `scripts/mutation-sweep.sh` (135 plants; `MUTATED_FILES` 84 entries, `factionDensityKernel.js` absent).

---

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- EM-B1k2
```

**branch/ancestry** — `head === verifiedBase`, the substrate arm returns early. **substrate** — no `REGISTER`-substrate row in the window. **CREATE targets absent** — one CREATE (`tests/domain/irreversibleRawRoster.contract.test.js`), measured absent and untracked at the tip in BOTH spellings. **non-CREATE targets clean** — `git status --short` empty at `63e40fe57`. **required symbols resolve** — the twenty-one rows of §5 and the manifest, each `grep -cF` = **1** (evidence §7, re-run §20; the five added in version 2 measured with the same command). `node scripts/implementation-packets.mjs validate` passes **only once BOTH gates are clear**: EM-B1k is LANDED (✔ discharged) **and** EM-B3c is terminal, or its `scripts/mutation-coverage-manifest.json` row has moved (§13 Q2).

Any mismatch makes this packet STALE. Stop before coding. Edit only exact-manifest paths.

---

## 5. Verified tree contract — every address re-measured at `63e40fe57`

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| ⭐ **The read that must change** | `src/domain/worldPulse/factionDensityKernel.js` | `export function advanceFactionDensity` (`:671`) | `const tickStart = item && item.settlement ? asObject(item.settlement) : null;` (`:699`) — the FILTERED view. `item` is read at **exactly one other place** in the function (`itemById.get(sid)`, `:698`); there is no second `item.` read in the loop | THE one line that changes. |
| **Its only two consumers** | `src/domain/worldPulse/factionDensityKernel.js` | `readFactionLifecycle(tickStart, { tick })` (`:704-707`) and the `fresh` fallback (`:721-723`) | `reading` is consumed for `governed` (`:714`) and `reactions` (`:732`) **and nothing else** — `reading.census` has no consumer here | Both become raw together; that is the whole change. |
| ⭐ **The law** | `src/domain/density/factionLifecycle.js` | `export function readFactionLifecycle` (`:155`) | returns `{governed, census, reactions}`; a `dissolved`/`ruling_interregnum` state mints a reaction; `rollsRegisterVii(config)` gates it to v2 | Preserve. A1 proves the reaction is not minted once its base is raw. |
| **The roster filter** | `src/domain/density/factionLifecycle.js` | `export function factionRosterOf` (`:113`) | filters by `isOnRoster`; consumed by `stillEmpty` (`:262`) over `fresh` | Preserve; it was right all along. |
| **The vocabulary** | `src/domain/density/factionLifecycle.js` | `export const ROSTER_ABSENT_STATUSES` (`:87`) | `Object.freeze(['dead','exiled','removed'])` — EM-B1d's LANDED byte-identical pin | ⛔ Untouched. |
| ⭐ **The raw roster, already on the item** | `src/domain/worldPulse/worldSnapshot.js` | `export function buildWorldSnapshot` | `saveSettlement` is `save?.settlement \|\| save` (`:11-13`); every item carries `save`; the participation view is `{ ..._s, npcs: filtered }` (`:127-129`) — a shallow copy differing from `saveSettlement(save)` in `npcs` **and nothing else**; identical BY REFERENCE when nobody is off-stage (`settlement = _s`, **`:132`**) | ⛔ **Read it. Do not edit this file** — it is an input of two edge bundles. |
| **The reversible reader that keeps the view** | `src/domain/worldPulse/factionDensityKernel.js` | `function applyCadence` (`:579`) | appends the minted founder to `fresh.npcs` — a MUTATION read, already dispositioned `TE-DENSITY-1` | ⛔ **Untouched.** Its base is `fresh` (raw under EM-B1k), so its own disposition is now TRUE rather than aspirational. |
| **The confirmation** | `src/domain/worldPulse/factionDensityKernel.js` | `function applyReactions` (`:234`) | `stillEmpty = factionRosterOf(fresh, faction).length === 0` (`:262`); the mark-clearing arm at `:287` reads the same `fresh` | ⛔ **Untouched.** A2 proves its verdict does not move. |
| **Permanent writer 1** | `src/domain/worldPulse/settlementLifecycleFirstClass.js` | `export function applySettlementLifecycleOutcomeToSettlement` (`:578`) | `:611-614` maps `settlement.npcs` stamping `dispersed: true`; **no save in the signature**; its DIRECT caller is `applyWorldPulse.js:155` inside `applyOutcomeToSettlement`, which is handed `beforeSettlement = entry.settlement` (`:717`) at `:733`, from `buildSettlementMap`'s map (`:290`) | Comment only. A5 pins it at its real trigger. |
| **Permanent writer 2** | `src/domain/worldPulse/successorNpc.js` | `export function replaceOustedNpcs` (`:64`) | `:66` guards on `settlement?.npcs`, `:69-76` maps it replacing by **name** (`:65`, `:70`); **no save in the signature** | Comment only. A6 pins it at its real trigger. |
| **Writer 2's caller** | `src/domain/worldPulse/npcVerdictPulse.js` | `export function applyOrganicNpcVerdicts` (`:79`) | `:133-135` calls `replaceOustedNpcs(replacementSource, …)`; `replacementSource` is the `settlement` argument (`:99`), which `pulseKernel.js:660-662` fills from `localSettlements` (`:617-618`) | ⛔ Untouched; named so the base is provable. ⚠ It also sits in the ratchet's QUARANTINE (`:409-421`) — untouched by this packet, and its debt is not this packet's to bank. |
| **The seam EM-B1k cured** | `src/domain/worldPulse/pulseKernel.js` | `function buildSettlementMap` | `:184` `settlement: localSettlements.get(String(item.id)) \|\| item.save?.settlement \|\| item.settlement,` and the `localSettlements` seed at `:579` — **both LANDED and quoted** | ⛔ **Not edited here.** This packet's whole premise is that both have landed. ⚠ CURE-F is rewriting `:579`'s comparand in the same window. |
| **Writer 1's caller** | `src/domain/worldPulse/applyWorldPulse.js` | `function applyOutcomeToSettlement` | `:717` `beforeSettlement = entry.settlement`, `entry` from `settlementUpdates` = `buildSettlementMap`'s map (`:290`); the writer is invoked at `:155`. The proposal paths (`:1209` `applyWorldPulseProposal`, `:1308` `mintRealmVerbProposal`) build their maps from `save.settlement \|\| save` — already raw. The priced re-set at `:607-609` cannot mint an entry without `.settlement` (evidence §20) | ⛔ Untouched. |
| **The chokepoint** | `src/domain/roads/state.js` | `export function isOffStage` (`:157`) | the ONE participation filter (`isInStasis` ∪ `whereabouts.state === 'hostage'`) | ⛔ **Untouched** (ODQ §934.60: add NOTHING to it). |
| **The census scan** | `tests/domain/roadsParticipation.test.js` | `function foundReaders` (**`:425`**) | `execFileSync('grep', ['-rl', '\\.npcs', 'src/domain/worldPulse', 'src/domain/spatial'], …)` (**`:426`**) — **41 files** today, measured with both BSD grep and ugrep | Widened to four roots and two patterns. |
| **The quarantine** | `tests/domain/roadsParticipation.test.js` | `const UNDISPOSITIONED_NPCS_READERS` (**`:409`**) | 7 rows, ceiling `UNDISPOSITIONED_CEILING = 7` (**`:422`**), monotone-down | ⛔ **Untouched** — the new convictions go to `EXPECTED` **with** dispositions, never to the quarantine, and the ceiling is never raised. |
| **The blanket this packet narrows** | `tests/domain/roadsParticipation.test.js` | the `EXPECTED` header comment | *"everything else is via-snapshot (protected by the gate, no edit)"* (**`:191`**); `factionDensityKernel.js` at `:257` with `TE-DENSITY-1` at `:246-256`; `roadsKernel.js` `:366`; `settlementLifecycleFirstClass.js` `:367`; `successorNpc.js` `:368` — the last two with **no** disposition | ⭐ A3 extends the EXISTING exactness arm (`:430`) in place. |
| ⭐ **NEW — the fourth root's real conviction** | `src/generators/density/applyDensityLaw.js` | `export function disperseNamedRoster` (`:317`) | `const npcs = input.npcs \|\| [];` (`:319`); its ONE importer is `src/generators/narrativeGenerator.js:33`, called at `:995` — the GENERATION-time coherence seam, where no snapshot and therefore no participation view exists | ⛔ Untouched; `EXPECTED` row + disposition. |
| ⭐ **NEW — the dark caller the chair named** | `src/generators/density/titularSuccession.js` | `factionRosterOf` (`:74`, `:177`, `:231`) | reads the roster ONLY through `factionRosterOf`; the literal `.npcs` never appears, so the `.npcs` predicate cannot see it. **No `src/` importer** (only a comment in `successionGrammar.js:4` and `tests/generators/densityLaw.test.js:96`) — dark | ⛔ Untouched; `EXPECTED` row + disposition, reachable only through §6.3's second `-e` pattern. |
| **Test precedent** | `tests/generators/densityLaw.test.js` | `snapOf` (`:1412`, `:1571`) and its **13** `advanceFactionDensity` drives | `snapOf = (...list) => ({ settlements: list.map(s => ({ id: s.id, settlement: s })) })` — the items carry **NO `save` key** | ⭐ The new suite copies this shape **and adds `save`**, because a `save`-less item exercises the fallback and can never prove the cure. |

Forbidden alternatives: ⛔ no edit to `worldSnapshot.js`, `isOffStage`, `pulseKernel.js`, or any REVERSIBLE participation read; ⛔ no new helper, no new import, and no added effective line in any of the four production files; ⛔ no second roster projection, no `save` threaded into either permanent writer's signature; ⛔ no raise of `UNDISPOSITIONED_CEILING`, of `uncoveredBaseline`, of any size baseline, or of any byte budget; ⛔ no files outside the manifest.

---

## 6. Exact contracts

### §6.1 · The one production change — ONE line replaced, NONE added

```js
// src/domain/worldPulse/factionDensityKernel.js :699, inside advanceFactionDensity.
// ONE line replaced, none added. `item.save?.settlement` is saveSettlement()'s exact
// meaning (worldSnapshot.js:11-13), inlined rather than imported.
    const tickStart = item && item.settlement ? asObject(item.save?.settlement || item.settlement) : null;
```

⛔ **No second line. No helper. No import. No reformat.** The guard stays `item && item.settlement` verbatim, so a snapshot item with no `settlement` still `continue`s exactly as today (`:700`).

**Why the whole object and not just the roster:** `item.settlement` is `saveSettlement(save)` itself when nobody is off-stage, and otherwise `{ ..._s, npcs: filtered }` — a shallow copy that differs in `npcs` **and in nothing else** (`worldSnapshot.js:124-133`, read verbatim). So swapping the object is *exactly* swapping the roster, at one token instead of three.

**The `||` fallback is load-bearing and its limit is now MEASURED, not assumed.** When a save carries no `.settlement`, `item.save?.settlement` is `undefined` and the expression falls back to `item.settlement` — today's behaviour, unchanged. ⭐ The pre-proof read **every save writer** (evidence §17): the two persist paths always set the key; the three shapes that carry a falsy `.settlement` (an INACTIVE save at `saves.js:280`, the two meta projections at `:380` and `:725`) carry **no roster at all**, so `saveSettlement` returns the envelope, `worldSnapshot.js:132` assigns it **by reference**, and raw and filtered are the SAME object. ⇒ **the fallback cannot diverge on any shape the tree can produce**; it is the snapshot's own tolerance. ⛔ This is EM-B1k's identical limit at `:184`, deliberately not diverged from; recorded in §12.1 item 1 as CLOSED, not cured here.

### §6.2 · The three comment-only contracts — ZERO effective lines, ZERO rendered bytes

Each states, in the file that could be broken, **which roster it is handed and by whom**. No code, no blank-line churn, and ⛔ **no `/*!`, `@license` or `@preserve` token** (those survive minification; every other comment is stripped, which is why the EAGER `successorNpc.js` row costs zero first-paint bytes).

| file | region | the sentence the comment must carry |
|---|---|---|
| `settlementLifecycleFirstClass.js` | the docblock above `:611`'s dispersal map (the function opens at `:578`) | the dispersal writes a PERMANENT stamp over whatever roster its caller hands it; that base is `applyWorldPulse.js:717`'s `entry.settlement`, reaching it through `applyOutcomeToSettlement` (`:733` → `:155`), made RAW by **EM-B1k**; handed the participation view it silently drops every off-stage soul (measured) and breaks law 6 conservation. ⛔ Never re-base this on a projection. |
| `successorNpc.js` | the `replaceOustedNpcs` docblock (`:57-63`) | same shape: the base is `npcVerdictPulse.js:133`'s `replacementSource` (`:99`), which is `pulseKernel.js:617-618`'s `localSettlements` entry, made RAW by **EM-B1k**; handed the participation view the returned roster is SHORT by every off-stage soul (measured). ⛔ Never re-base this on a projection. |
| `roadsKernel.js` | the three-line comment at `:1094-1096` above the full-roster emit (the symbol `const fullRoster` is at **`:425`**; EM-B1k's §5 hint of `:395` was corrected at its LANDED flip, §934.47 addendum 42) | re-pointed at **EM-B1k**: its sentence *"this full-roster update is the last word, so a hostage is never lost even under a naive save merge"* is no longer the estate's only defence against a filtered write base; the merge is now redundant-but-self-consistent and is deliberately kept. ⛔ Comment text only — this file is tolerance-zero on `scripts/.size-baseline.json` in **both** directions (§3.1). |

### §6.3 · The census — six dispositions, two roots and one pattern

**The scan** (`tests/domain/roadsParticipation.test.js:426`, inside `function foundReaders` at `:425`):

```js
    const out = execFileSync('grep', ['-rl', '-e', '\\.npcs', '-e', 'factionRosterOf', 'src/domain/worldPulse', 'src/domain/spatial', 'src/domain/density', 'src/generators/density'], { cwd: process.cwd(), encoding: 'utf-8' });
```

⛔ **TWO `-e` PATTERNS, NEVER `\|` ALTERNATION** — `-e` is portable across the BSD grep a macOS `execFileSync('grep', …)` resolves to and the GNU grep CI runs; a BRE `\|` is not. Measured with both engines: **41 → 44**, the three new members being `src/domain/density/factionLifecycle.js`, `src/generators/density/applyDensityLaw.js`, `src/generators/density/titularSuccession.js`. ⚠ **If the chair takes §13 Q1's option (b)** (roots only, no second pattern), the figure is **43**, `titularSuccession.js` is NOT a row, and its disposition rides as prose beside `applyDensityLaw.js`'s row — every other line of this packet is unchanged.

**The dispositions** — three new `EXPECTED` rows and three rewrites. ⛔ The new rows go to `EXPECTED` **with** their dispositions, never to the quarantine; `UNDISPOSITIONED_NPCS_READERS` and `UNDISPOSITIONED_CEILING = 7` stay byte-identical.

| row | disposition the comment must carry |
|---|---|
| ⭐ **NEW** `src/domain/density/factionLifecycle.js` | `factionRosterOf` (`:113`) is the estate's roster filter and `readFactionLifecycle` (`:155`) is the R18 law that reads it. **Participation-INDEPENDENT by construction and REQUIRED to be**: it decides an IRREVERSIBLE consequence (a house swept out of `powerStructure.factions`), and §810.4 R18 admits only irreversible causes. Both its production callers in `factionDensityKernel.js` now hand it a RAW roster — the law at `tickStart` (EM-B1k2) and the confirmation at `fresh` (EM-B1k). ⛔ A caller that hands it the participation view dissolves a house because somebody is shelved. |
| ⭐ **NEW** `src/generators/density/applyDensityLaw.js` | `disperseNamedRoster` (`:317`) reads `input.npcs` (`:319`) at **GENERATION time** — its one importer is `narrativeGenerator.js:33`, called at `:995` in the coherence seam, where no `buildWorldSnapshot` and therefore no participation view exists. Participation-independent **by construction, not by choice**: the roster it reads is the one being generated. ⛔ A future caller that hands it a tick-time settlement owes the raw base. |
| ⭐ **NEW** `src/generators/density/titularSuccession.js` | **DARK.** It calls `factionRosterOf` (`:177`, `:231`) and has **no `src/` importer** — nothing in production reaches it, so whatever settlement its future caller passes is what it will read. It is in this census only because the scan's second pattern reaches past the `.npcs` literal. ⛔ **The packet that wires it owes the raw-base disposition** (the chair's fate, addendum 39). |
| **REWRITE** `factionDensityKernel.js` (`:246-257`) | keep the `TE-DENSITY-1` clause verbatim (it is about `applyCadence`), and ADD a second clause: the LIFECYCLE read (`tickStart` → `readFactionLifecycle`) and its confirmation (`stillEmpty` over `fresh`) both read the RAW roster — **irreversible ⇒ RAW** — while every REVERSIBLE reading in the file keeps the participation view. Name EM-B1k (the write base) and EM-B1k2 (this read). |
| **REWRITE** `settlementLifecycleFirstClass.js` (`:367`) | a PERMANENT roster writer whose base is its caller's (`applyWorldPulse.js:717` → `:733` → `:155`), not via-snapshot. ⛔ It is **not** covered by the blanket at `:191`, and saying so is the point of the row. |
| **REWRITE** `successorNpc.js` (`:368`) | the same, base `npcVerdictPulse.js:133` ← `:99` ← `pulseKernel.js:617-618`. ⛔ Not via-snapshot. |

⭐ **AND ONE MESSAGE FIX, owed to EM-B1k's landing** (its receipt item 7, fated here by addendum 42): `roadsParticipation.test.js:104` reads *"the house still reads crewed by the estate own lifecycle reader"* — the possessive is missing. One apostrophe, in an assertion message, in a file this packet already edits.

### §6.4 · Determinism · dormancy · golden posture

**Determinism:** no draw, no key, no ordering change — `orderedIds` and the raw roster's own order are untouched. **Dormancy:** `readFactionLifecycle` returns `{governed:false, census:[], reactions:[]}` for every v1 world before it looks at a roster (`rollsRegisterVii`, `densityLaw.js:174`), so a shipped world is an exact no-op; a lit world with nobody off-stage has `item.save.settlement === item.settlement` BY REFERENCE (`worldSnapshot.js:132`), so the expression returns the identical object. **Absence:** a snapshot item with no `settlement` still `continue`s (`:700`); a save with no `.settlement` falls through to `item.settlement` exactly as today, and on every such shape the two are the same object (§6.1). **Golden posture: UNCHANGED, and EXECUTED** — no fixture in `tests/fixtures/` holds `"stasis"` or `"whereabouts"` (re-measured: zero files) and generation writes neither key, so the cure can only touch worlds where the class fires. **Behaviour shift: NONE, measured** — across the stride, the chain's `{changed, newsEntries, settlementUpdates}` is **identical** with a filtered and a raw `tickStart` (evidence §3), and the reaction split reproduces 7/7 vs 0/7 at the landed tip (evidence §15). ⛔ Any movement is a STOP.

### §6.5 · Lifecycle

| Create | Read | Persist | Reload | Regenerate | Undo | Import/migrate | Public veil |
|---|---|---|---|---|---|---|---|
| nothing | ⭐ the change: irreversible reads take the raw roster | unchanged (EM-B1k's) | unchanged | unchanged | `pulseUndoStack` unchanged | unchanged | unchanged (the veil reads the save) |

---

## 7. Exact change manifest

| Action | File | Symbol/region | Max delta | Coding instruction |
|---|---|---|---:|---|
| `MODIFY` | `src/domain/worldPulse/factionDensityKernel.js` | `advanceFactionDensity` — the `tickStart` line (`:699`) | ⛔ **+0 eff** | §6.1's single replacement, verbatim, plus a WHY comment. ⛔ **NET ZERO effective lines — measure with eslint's `Linter` before and after and quote both (expect 395 → 395).** No helper, no import, no reformat. |
| `MODIFY` | `src/domain/worldPulse/settlementLifecycleFirstClass.js` | the dispersal docblock above `:611` | ⛔ **+0 eff — COMMENT ONLY** | §6.2's sentence. No code byte changes; re-measure 462 → 462. |
| `MODIFY` | `src/domain/worldPulse/successorNpc.js` | the `replaceOustedNpcs` docblock (`:57-63`) | ⛔ **+0 eff — COMMENT ONLY** | §6.2's sentence. ⛔ No `/*!`, `@license` or `@preserve`: this module is EAGER (measured through vite's own export) and must render byte-identical. Re-measure 50 → 50. |
| `MODIFY` | `src/domain/worldPulse/roadsKernel.js` | `const fullRoster` (`:425`) — its comment at `:1094-1096` | ⛔ **+0 eff — COMMENT ONLY** | §6.2's re-point. ⛔ Tolerance-zero both directions: re-measure 838 → 838 and keep `tests/lint/sizeBaseline.test.js` green. |
| `CREATE` | ⭐ `tests/domain/irreversibleRawRoster.contract.test.js` | A1 · A2 · A4 · A5 · A6 | **≤250 eff** | The red-first suite. ⭐ **The `contract` token is deliberate** (chair, addendum 43 ruling 3): it opts the file into `enumerateInvariants` — executed, `NAME_PATTERN.test('irreversibleRawRoster.contract.test.js')` → **true** — so the suite is visible to the mutation register from birth. ⛔ Opener imported from `'vitest'`; ONE literal `describe`; straight-line literal `it`s only; no `.each`, no loop or conditional registration, no nested describe; ⛔ never name a variable or parameter `it`, `test` or `describe`. Snapshot items MUST carry `save`. Model: `tests/domain/roadsParticipation.test.js`'s master-gate describe and `tests/generators/densityLaw.test.js`'s `snapOf` (`:1412`). |
| `REGISTER` | ⭐ `scripts/mutation-coverage-manifest.json` | `invariants["tests/domain/irreversibleRawRoster.contract.test.js"]` | **+1 row** | ⛔ **`kind: "mutation"`, with a `label` and a `what`.** `uncovered` is refused (it would be the first RAISE of `uncoveredBaseline` — 186 today — in the register's life); `rationale` would be false (this suite is not a scanner). The `label` MUST join one-to-one to the plant below (`mutationCoverageManifest.test.js:130`). ⛔ Do not touch `uncoveredBaseline`. |
| `REGISTER` | ⭐ `scripts/mutation-sweep.sh` | the plant + `MUTATED_FILES` | **+~4 lines** | Add `src/domain/worldPulse/factionDensityKernel.js` to `MUTATED_FILES` (84 entries today; it is absent). Add the plant in the file's own idiom (`:1559-1585`): a one-line `perl -0pi -e` that REVERTS §6.1 (`asObject(item.save?.settlement \|\| item.settlement)` → `asObject(item.settlement)`), then `check_caught "<label>" src/domain/worldPulse/factionDensityKernel.js "npx vitest run tests/domain/irreversibleRawRoster.contract.test.js --no-file-parallelism" "<A1's EXACT it title>"`. ⛔ The expected title must match **exactly one** FAIL line (`check_caught` at `:158-190` reds on 0 or ≥2). The plant IS A1's red, so the build lane proves it once and records it twice — with the cp-backup / cp-restore / md5 discipline every neighbouring plant records. |
| `TEST` | `tests/domain/roadsParticipation.test.js` | `foundReaders` (`:425-427`) + `EXPECTED` + `:104` | `n/a` | §6.3: two `-e` patterns and two extra roots; three new `EXPECTED` rows with dispositions; three rewritten dispositions; and the one-apostrophe possessive fix at `:104`. A3 is the EXISTING exactness arm (`:430`) — **no new `it`**. ⛔ Do not touch `UNDISPOSITIONED_NPCS_READERS` (`:409`), `UNDISPOSITIONED_CEILING` (`:422`), or the two `it` EM-B1k added to the master-gate describe (`:65`, `:108`). |

**Generated artifacts: NONE.** ⛔ No `_shared` rows and **no generator among `checks`** — none of the `src/` paths is an input of any of the five edge-shared bundles (re-measured), so §P2.12's seven-path hazard and the step-14a ordering hazard do not arise. No other file may be edited.

### §7.1 · The registration ledger — the measured NEGATIVES live here, never in the table above

| # | Obligation | Verdict | Measurement |
|---|---|---|---|
| P2.1 | lighting census | **OWED** | ⛔ NO ABSOLUTE QUOTED. DELTA: **`+1 files / +0 parked / +1 credited / +5 titles / +1 suiteTitles`** — one new CREDITED test file (opener from `'vitest'`, ONE literal `describe`, five straight-line literal `it`: A1, A2, A4, A5, A6) and **zero** new titles in `roadsParticipation.test.js` (A3 extends an existing arm; the `:104` fix is a message). ⭐ The `contract` rename does NOT touch this: `parkReasonsFor` is `classify(src).reasons` (`sovereigntyLightingContract.walker.test.js:1670`) and reads SOURCE, never a path. An INTERIOR RED, named before it exists; the chair re-derives the whole tuple at the train's terminal. |
| P2.2 | mutation-coverage | ⭐ **OWED — MEASURED, and this is version 2's change** | `ENFORCER_DIRS` still excludes `tests/domain`, but `NAME_PATTERN` matches the `contract` token: executed, `NAME_PATTERN.test('irreversibleRawRoster.contract.test.js')` → **true** (and `…RawRoster.test.js` → false). The file therefore enters `enumerateInvariants` (706 files today) and owes a row, and a `mutation` row owes a plant. Two files, both in §7. ⛔ **`scripts/mutation-coverage-manifest.json` COLLIDES with EM-B3c (READY)** — §13 Q2. |
| P2.3 | observed-shape | **NOT OWED; motion is a STOP** | No save-time key (`dmLayer`, `decrees`) is read. The edit adds `item.save?.settlement` — `save` and `settlement` are keys the snapshot item and the save provably carry, so no reader-without-a-writer identity is expected. ⚠ **PLAUSIBLE, not executed** (a lane runs no estate script): `node scripts/check-observed-shape-readers.mjs` is in `checks` and **any motion is a STOP**. |
| P2.4 | writer-reach | ⚠ **measure, do not assume** | None of the four carries a row in `scripts/.writer-reach-baseline.json` today (`grep -c` = 0, all four, re-measured). A shrink is the plain `--write`; ⛔ **growth is a mint and a chair act.** In `checks`. |
| P2.5 · P2.7 | seeded chooser · prose-numerics | **NOT OWED** | No `rollFrom`, no draw, no rendered figure; the prose-numerics baseline names none of the four (`grep -c` = 0). |
| P2.6 · P2.8 · P2.9 | persisted keys · surfaces · preset witness | **NOT OWED** | No persisted key, no component, no tick hook. |
| P2.10 | edge-shared | ⭐ **NOT OWED — measured** | 0 hits for all five src paths across the five metas' own `inputs` (114 / 74 / 115 / 2 / 2). |
| P2.11 | byte budgets | **PRICED — §3.2** | 0 B into the generation worker (proven by closure); 0 B REQUIRED into first paint (comment-only, EAGER module, 268-module closure re-derived through vite's own export); ≤200 B into the lazy engine; a DELTA into the advance worker. ⛔ The dist-gated arms are NOT in `checks` (chair Q7): the byte proof is the chair's build + `verify:dist` at the landing. |
| steps 13 · 15 · 16 | line-addressed · stamped · `path:line` citations | **NOT A RED — measured** | prose-numerics names none; `wiring-census` `stamp.files` (7 rows, all `stateProse/`) names none; `tuningRegister` keys on `spanDigest` and pins comment-insertion explicitly at `:212-213` and `:428-430`; **zero** `path:line` citations of any of the six files across `src`, `docs/content`, `tests`. |
| step 10 | post-edit `requiredSymbols` simulation | ⭐ **ALL TWENTY-ONE SURVIVE; `retiredSymbols`: NONE** | The replaced line is not a `requiredSymbols` row of ANY packet (of the 191 entries, rows on these paths belong only to `EP-3A`, `EP-3B`, `EM-B1d` and `EM-B1k`, all LANDED, and none names it); the symbol `tickStart` itself survives verbatim. Nothing is moved, renamed or deleted. |
| step 12 | `tests/` fixture sweep | ⭐ **NO FIXTURE REDS — measured** | `tests/generators/densityLaw.test.js` drives `advanceFactionDensity` **13 times** through `snapOf`, whose items carry **no `save` key** ⇒ `item.save?.settlement` is `undefined` ⇒ every drive falls back to `item.settlement` ⇒ **byte-identical**. `roadsParticipation.test.js` names the files only as census strings. No test in the estate spells the replaced line, and no fixture holds `"stasis"`/`"whereabouts"`. The literals this packet ADDS (`'src/domain/density'`, `'src/generators/density'`, `'factionRosterOf'` as a scan pattern) are pinned by no other suite — the three other tests that name `src/domain/spatial'` carry their own unrelated roots. |
| ⛔ | the size ratchet | ⛔ **BINDING — §3.1** | `roadsKernel.js` 838 / 838, tolerance-zero both directions. A non-zero delta anywhere is a STOP. |
| ⛔ | ⭐ the enumeration floors | **NOT A RED — measured** | `tests/scripts/baseStateCapsule.test.js:369` pins `enumerateInvariants(ROOT).length > 440` — a FLOOR, and this packet moves it UP by one (706 → 707, or 708 once CURE-G's rename lands). `uncoveredBaseline` (186) is untouched. |

---

## 8. Ordered coding sequence

0. Dispatch and seal. ⛔ Refuse if EM-B1k is not LANDED **or** EM-B3c still reserves `scripts/mutation-coverage-manifest.json`.
1. **Capture the baselines:** eslint `Linter` effective lines on all four production files (expect **395 / 462 / 50 / 838**); `node scripts/check-writer-reach.mjs`; `node scripts/check-observed-shape-readers.mjs`; the pre-edit `foundReaders()` count (**41**); `enumerateInvariants` count.
2. **Write the reds first and watch them fail with today's exact messages** (§9 quotes them): A1's produced reaction, A4's fallback dissolution, A5's short roster, A6's short roster.
3. **Make the single replacement** (§6.1) and nothing else in that file.
4. **Re-measure the effective lines — 395 again.** A different number is a STOP.
5. **Add the three comment-only contracts** (§6.2); re-measure 462 / 50 / 838. A different number is a STOP.
6. **Widen the scan and write the six dispositions** (§6.3) plus the `:104` possessive; the widened `foundReaders()` must return **44** (or **43** under Q1 option (b)).
7. **Plant the mutant and write its two rows** (§7): the sweep line with the cp-backup / cp-restore / md5 discipline, then the manifest row whose `label` joins it.
8. Run the focused suites, then the golden and dormancy set (§10).
9. Real `npm run build` through the exclusive mutex; attribution; the two `tests/build/` arms against the fresh `dist`; §3.2's STOP conditions. (Narrative — not in the sealed `checks`.)
10. Write the completion receipt.

```text
1. the snapshot is built as today — the FILTERED view still goes to every participation read
2. the LAW is read from the raw roster        (this packet)
3. the WRITE BASE is already the raw roster   (EM-B1k, LANDED 19c4cb853)
4. so an irreversible decision is computed from truth, and the second guard has nothing left to refuse
```

---

## 9. Acceptance matrix

| ID | Case | Required observation | today's RED |
|---|---|---|---|
| **A1** | ⭐ **THE HABITAT IS GONE.** A generated town; the sole member of a NON-governing house shelved through `applyNpcOp('stasis-npc')`; `readFactionLifecycle` driven over the tick-start base. | **no reaction of any kind is minted for that house** — the dissolution is never PROPOSED, not merely refused. The already-empty houses in the same town still mint theirs (non-vacuity). | `reaction: ["faction_dissolved"]`, roster of the house `= 0` — **re-measured 7 / 7 at the landed tip** |
| **A2** | ⛔ **NOTHING MOVES — the chain is byte-identical.** The same town driven through `advanceFactionDensity` with a filtered and a raw tick-start base, the update entry raw in both (EM-B1k landed). | `{changed, newsEntries, settlementUpdates}` **deep-equal**; and a v1 (no `_densityLawVersion`) world returns the same `worldState` and `settlementUpdates` **references**, zero beats. | n/a (regression) |
| **A3** | **THE CENSUS'S NEW CONVICTIONS.** The widened four-root, two-pattern scan. | the reader set is **exactly** `EXPECTED ∪ UNDISPOSITIONED_NPCS_READERS`, **44** files, the three new members being `src/domain/density/factionLifecycle.js`, `src/generators/density/applyDensityLaw.js` and `src/generators/density/titularSuccession.js`; the quarantine list and `UNDISPOSITIONED_CEILING` are unchanged and the honesty arm is green. ⛔ Extends the EXISTING arm — no new `it`. | n/a (two roots, one pattern, 41 files today) |
| **A4** | ⛔ **THE FALLBACK ARM.** The same town, but the update entry carries **no `.settlement`**, so `fresh` falls back to `tickStart`. | the house is **not** dissolved and no `faction_dissolved` beat names it. | the house IS dissolved — measured **7 / 7** towns across the stride, with EM-B1k's raw write base in place |
| **A5** | **THE DISPERSAL CARRIES THE OFF-STAGE SOUL.** `applySettlementLifecycleOutcomeToSettlement` driven at its **real trigger** — a `terminal_death` `lifecyclePatch` on a thorp at the bottom rung (`settlementLifecycleEnabled` is lit in all four presets) — over the RAW base EM-B1k supplies. | every soul is in the output roster and every one carries `dispersed: true`, the shelved person included. Driving the same outcome over the participation view is the counterforce and **must** show the soul missing. | over the filtered base: `roster out 2 \| dispersed stamps 2 \| the shelved soul present: false` (raw: `3 \| 3 \| true`) |
| **A6** | **THE SUCCESSOR PASS KEEPS THE OFF-STAGE SOUL, AND THE NAME JOIN IS SAFE.** `replaceOustedNpcs` driven at its **real trigger** — an `ousted` exposure, the kind `advanceNpcCorruption` mints unconditionally at `pulseKernel.js:388` — over the RAW base. | the ousted person is replaced by a successor **and** the shelved person is still in the roster. The filtered base is the counterforce and must show them missing. ⭐ **PLUS the uniqueness assertion** the chair's Q4 ruling folds in: the drive's own roster carries no two NPCs sharing `String(name).toLowerCase()`, so the name join replaces exactly one soul — anchored on the measured corpus fact (525 rows / 5,171 NPCs / **0** duplicates). | over the filtered base: `roster out 2 \| the ousted replaced: true \| the shelved soul present: false` (raw: `3 \| true \| true`) |
| **A7** | ⛔ **THE GOLDENS AND THE PARTICIPATION GUARANTEES.** | `generatorGoldenMaster`, `dossierProseManifest`, `roadsDormancyGolden` and the preset-lighting witness bytewise unchanged; `roadsParticipation`'s master-gate and ladder-belt arms green untouched — a shelved person is still never cast, never travels, never ladder-eligible. Justified by measurement: no fixture holds `stasis`/`whereabouts` and generation writes neither. | n/a |
| **A8** | ⛔ **THE CEILINGS AND THE REGISTERS HELD.** | `395 / 462 / 50 / 838` effective lines after the edits; `tests/lint/sizeBaseline.test.js` green; `tests/lint/mutationCoverageManifest.test.js` green with the new row and its plant joined one-to-one and `uncoveredBaseline` unmoved at 186. **The byte arms are the chair's** (a real build + `verify:dist` at the landing): zero attributed movement for `successorNpc.js` (EAGER) and `≤200 B` for `factionDensityKernel.js`. | n/a |

Eight of ≤8 — the cap. Test homes: A1, A2, A4, A5, A6 → `tests/domain/irreversibleRawRoster.contract.test.js` (five straight-line `it`); A3 → the existing exactness arm in `tests/domain/roadsParticipation.test.js` (`:430`); A7 and A8 → §10's executed commands, no new title.

⚠ **ONE WORDING NOTE FOR THE CHAIR.** Addendum 39's Q4 ruling says the uniqueness is folded into **A5**'s assertions. A5 is the DISPERSAL case, which maps every NPC and joins on nothing; the name join lives in `replaceOustedNpcs`, which is **A6**. The assertion is placed in A6, where it can be proven rather than recited. One word from the chair moves it.

---

## 10. Verification commands

```sh
npx eslint src/domain/worldPulse/factionDensityKernel.js src/domain/worldPulse/settlementLifecycleFirstClass.js \
  src/domain/worldPulse/successorNpc.js src/domain/worldPulse/roadsKernel.js \
  tests/domain/irreversibleRawRoster.contract.test.js tests/domain/roadsParticipation.test.js
npm run typecheck:ratchet && npm run typecheck:domain:strict

npx vitest run --pool=threads --maxWorkers=2 \
  tests/domain/irreversibleRawRoster.contract.test.js tests/domain/roadsParticipation.test.js \
  tests/domain/settlementLifecycleFirstClass.test.js tests/domain/successorNpc.test.js \
  tests/domain/demographicsFloor.test.js tests/domain/settlementLifecycleVerbs.test.js \
  tests/domain/warH2VerdictComposition.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/generators/densityLaw.test.js
# ⛔ NOTHING MOVES — every golden this packet could touch, named
npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js \
  tests/property/roadsDormancyGolden.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/lint/sizeBaseline.test.js
# ⭐ the register this packet now moves, proven by its own walker
npx vitest run --pool=threads --maxWorkers=2 tests/lint/mutationCoverageManifest.test.js

node scripts/check-observed-shape-readers.mjs     # motion is a STOP
node scripts/check-writer-reach.mjs               # shrink = --write; growth = a chair act
node scripts/implementation-packets.mjs validate
```

⛔ **No generator among them** — this packet owes no edge-shared rebuild, so step-14a's ordering hazard does not arise. ⛔ **And no `tests/build` file** (chair Q7): both its arms are `skipIf(!requireDistRead)` and would read as a pass in a focused run, and TOOL-2's guard would refuse the named file — the bytes are the chair's build + `verify:dist` at the landing, per §3.2's narrative step. Every command exits `0`; the lighting walker's exact-count arm is the packet's ONE named interior red (§7.1 row P2.1) and the chair re-derives it at the terminal.

---

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md`'s list:

1. ⛔ **EM-B1k is not LANDED**, or `buildSettlementMap`'s write base is not raw, or **EM-B3c still reserves `scripts/mutation-coverage-manifest.json`**.
2. ⛔ Any of the four production files does not re-measure **395 / 462 / 50 / 838** effective lines.
3. ⛔ **A2 reds** — the chain's output moved. The read change was supposed to be behaviour-neutral; it was not.
4. ⛔ Any golden, the roads dormancy golden or the preset witness moves.
5. ⛔ `check-writer-reach` or `check-observed-shape-readers` **grows**.
6. ⛔ The widened scan convicts anything other than exactly `src/domain/density/factionLifecycle.js`, `src/generators/density/applyDensityLaw.js` and `src/generators/density/titularSuccession.js` (or, under Q1 option (b), the first two), or the quarantine list or its ceiling would move.
7. ⛔ The edit needs a second line, a helper, an import, or a fifth production file.
8. ⛔ A real build attributes **any** movement to `successorNpc.js`, or the first-paint closure rises at all.
9. ⛔ A1, A4, A5 or A6 does not reproduce its stated red before the fix.
10. ⛔ **The plant does not catch, or does not restore byte-identically**, or `uncoveredBaseline` would move off 186.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into the next wave.

---

## 12. Completion receipt

Record with executed output: the four reds before and their greens after; effective lines before and after for all four files; `foundReaders()` 41 → 44; every golden's result; writer-reach and observed-shape before/after; the plant's CAUGHT line with its md5 backup/restore pair; `enumerateInvariants` before/after; the lighting **DELTA** caused (`+1 files / +1 credited / +5 titles / +1 suiteTitles`) **as a delta, never an absolute**. The build's per-module attribution with `successorNpc.js` at zero belongs to the chair's landing measurement, not to this receipt.

### §12.1 · NOTICED, NOT TOUCHED — each specific enough to slot

1. ⭐ **The legacy save shape — CLOSED by measurement (chair Q5).** Every save writer was read (`src/lib/saves.js`, both persist paths, the three read projections, `migrateSaveToV2`, the store's `campaignSettlements` and `persistMerge`): no writer can produce a save whose `.settlement` is absent *while a roster hides behind the envelope*, and on the three falsy-`.settlement` shapes that do exist the participation view IS the raw object by reference. ⇒ the `||` fallback in both packets is the snapshot's own tolerance. **Slot: CLOSED with the measurement (evidence §17); no raw-fallback token, no fourth file.**
2. ⭐ **`replaceOustedNpcs` joins by NAME, not identity — CLOSED by the full-corpus census (chair Q4).** 525 golden rows, 5,171 rostered NPCs, **0** settlements with a duplicate display name under the exact key the join uses. **Slot: CLOSED with the figure recorded; EM-B1k3 NOT chartered.** The join is still a name — if a future generator change can mint a duplicate, the cure packet (thread `exposure.npcId` from `npcVerdictPulse.js:133`) is the recorded answer.
3. ⭐ **`src/generators/density/titularSuccession.js` is dark and calls `factionRosterOf`** — re-measured here: no `src/` importer at all. **Slot: THIS packet's §6.3 gives it its disposition row** (under Q1 option (a)); the packet that wires it owes the raw base.
4. ⚠ **`tests/store` and the register's door** — superseded by the chair's CURE-G ruling and TOOL-6b's charter (addendum 43). EM-B1k's suite is being renamed `participationWriteBase.contract.test.js` in the cure lane's window; this packet's CREATE is named with the token from birth. **Slot: TOOL-6b.**
5. ⚠ **`factionDensityKernel.js`'s `TE-DENSITY-1` disposition WAS aspirational and is now TRUE** — EM-B1k has landed, so `applyCadence`'s `fresh` really is the raw roster. **Slot: this packet's own §6.3 rewrite states it.**
6. ⚠ **The first-paint closure's live margin is still unknown.** The newest figure in `vendorPdfLazy.test.js`'s own header is 1,047,205 against 1,048,000 (`:539`, `:565`), i.e. ~795 B. **Slot: TOOL-3's resume records the live margin in the test's header (it builds anyway) — the chair's fate, addendum 39.**
7. ⚠ **`tests/build/vendorPdfLazy.test.js`'s byte arms are `skipIf(!requireDistRead)`** — named so no receipt reads a skip as a pass. **Slot: EM-PREAMBLE §P2.11 gains the sentence at the chair's next amendment (the chair's fate), and this packet's §3.2 carries the narrative step.**
8. ⭐ **NEW — the pulse's member list is unfiltered by access state.** `campaignSliceShared.js:167-178` selects campaign members by id alone; `isSaveActive` appears **nowhere** in `src/store/`. An INACTIVE save (`saves.js:280` materialises `settlement: null`) that is a campaign member is therefore handed to `buildWorldSnapshot` as an envelope. Harmless for THIS packet (no roster, so no filter), but it means a tick can walk a save the library considers inaccessible. **Slot: a chair question, or a one-line measurement lane over the advance path.**
9. ⭐ **NEW — `execFileSync('grep', …)` resolves through `PATH`, and this machine's interactive `grep` is `ugrep 7.8.4`.** The census scan therefore runs under whatever `grep` the environment supplies (BSD grep 2.6.0-FreeBSD from node here; GNU grep in CI). Both agree on this packet's patterns — measured — but a BRE `\|` would not be portable, which is why §6.3 uses two `-e` flags. **Slot: a standing note for TOOL-7's silent-failure-idiom lint (a scan whose result depends on the host's grep dialect).**
10. ⭐ **NEW — two further already-raw settlement maps the compile did not name:** `applyWorldPulse.js:1209` (`applyWorldPulseProposal`) and `:1308` (`mintRealmVerbProposal`), both `{ saveId, save, settlement: save.settlement || save }`. Recorded so a future reader does not take §5's single `:1209` citation for the whole population. **Slot: CLOSED — measured, both already raw.**

---

## 13. RAISED — for the chair (numbered; the lane does not wait)

- **Q1 — ⛔ THE FOURTH ROOT DOES NOT CONVICT `titularSuccession.js`, AND THE CHAIR'S OWN INSTRUCTION NEEDS ONE MORE TOKEN.** Measured (evidence §18): `src/generators/density` convicts **`applyDensityLaw.js`** (`:319`); `titularSuccession.js` reads the roster only through `factionRosterOf` and the literal `.npcs` never appears in it, so an `EXPECTED` row for it would RED the exactness arm. **(a) MY RECOMMENDATION: widen the scan's PREDICATE by one `-e 'factionRosterOf'`** — 41 → **44**, exactly the three files above, each dispositioned, and the chair's fate for `titularSuccession.js` is delivered as ordered. Cost: one array token, no production byte. **(b) The alternative:** roots only — 41 → **43**, two rows, and `titularSuccession.js`'s disposition rides as PROSE beside `applyDensityLaw.js`'s row (honest, but the file stays invisible to the ratchet, so the next lane to touch it is policed by nothing). The packet is written for (a) and says in §6.3 exactly what changes under (b).
- **Q2 — ⛔ THE `contract` RENAME COSTS TWO REGISTER FILES AND COLLIDES WITH EM-B3c.** Executed: the token opts the file into `enumerateInvariants`, so it owes a manifest row; `uncovered` would be the register's first-ever baseline raise and `rationale` would be false, so the row is `mutation`; and a `mutation` row is joined one-to-one to a real plant in `scripts/mutation-sweep.sh` (which also needs `factionDensityKernel.js` in `MUTATED_FILES`). ⛔ And `scripts/mutation-coverage-manifest.json` is **reserved by EM-B3c (READY)** — two non-terminal packets on one path red `validate:packets` by construction. ⭐ **My recommendation: keep the rename and the row exactly as the chair ruled, and PLACE EM-B1k2 after EM-B3c lands** (it is building in the slot beside this train anyway) — the ordering is free, and the alternative (dropping the row) would leave a newly enumerated file with no manifest entry, which reds the walker at the landing. If the chair would rather not wait, the second option is for EM-B3c's pre-landing cut to carry B1k2's row as a REGISTER line of its own; I do not recommend it (a packet carrying another packet's row is the drift TOOL-1's arm exists to find).
- **Q3 — the plant is A1's red, but it is still a BUILD-LANE ACT.** The `check_caught` line runs a real vitest under the sweep's own restore discipline. Confirm the build lane owns it (with the cp-backup / md5 / cp-restore record every neighbouring plant carries), rather than the chair's landing.
- **Q4 — the uniqueness assertion's home.** Addendum 39 says A5; the name join lives in A6's function. Placed in A6, with the corpus figure (525 / 5,171 / 0) as its anchor. Confirm or move.
- **Q5 — CLOSED, reported for the record.** The legacy save shape cannot carry a hidden roster (evidence §17). No raw-fallback token, no fourth file. Veto if the chair wants the token anyway.
- **Q6 — `EXPECTED` vs the quarantine** — ruled (addendum 39 Q6); the packet places all three new convictions in `EXPECTED` **with** written dispositions and leaves `UNDISPOSITIONED_CEILING` at 7.
- **Q7 — CLOSED, applied.** The dist-gated `tests/build` pair has left the sealed `checks`; §3.2 keeps the measurement as the build/landing narrative, and `tests/lint/mutationCoverageManifest.test.js` takes its place as the check for the register this packet now moves.
- **Q8 — the window the chair must re-run.** This pre-proof is pinned at `63e40fe57`. A cure lane is committing CURE-E, CURE-F and CURE-G on the branch **right now**: two test files, `pulseKernel.js:579`'s comparand (CURE-F), and the RENAME of `tests/store/participationWriteBase.test.js` → `…participationWriteBase.contract.test.js` with its own mutation row and plant (CURE-G). ⛔ **CURE-G lands in BOTH register files this packet touches**, and the chair's lighting refreeze moves the census tuple. Re-run at the promotion tip: `foundReaders()` = 41; the four effective-line figures; `enumerateInvariants` (706 + CURE-G's +1); the sixteen `grep -cF`; and the EM-B3c reservation.
