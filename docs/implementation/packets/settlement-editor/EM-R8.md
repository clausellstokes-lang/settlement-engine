# `settlement-editor / EM-R8` — THE LADDER'S HELD-KEY LAW: a reading is never repaired by overwriting a held fact

- **Status:** `READY`
  ⚠ The status value above stands ALONE on its line, and it must: `parsePacketHeader`
  (`scripts/implementation-packets.mjs`) anchors the status row at END-OF-LINE and takes `status`
  only when EXACTLY ONE row matches. Every stamp lives here, on the CONTINUATION lines.
  ⭐ **COMPILED AT `a545899ff`** (train EM-T17's base: EM-T16's landed tip `1b073009f` plus the
  hygiene pick `d44159dc0`), by the Opus COMPILE seat of session cce01f87, 2026-09-23, on the
  measurement seat's executed shapes and the chair's judgments 240 and 245. **SHAPE B as ruled.**
  ⭐ **RE-MEASURED AT `bb0497eb4`** (train EM-T17's STAGE-1 PLACEMENT: EM-R1c v3 and EM-C2 v3
  READY on the branch) by the Opus PRE-PROOF seat of session cce01f87, 2026-09-23. **THE J-T1
  WINDOW `a545899ff` -> `bb0497eb4` IS FOUR FILES, ALL UNDER `docs/implementation/`** — the
  placement commit and nothing else — so NO `src/` or `tests/` byte moved and every executed
  figure below was RE-EXECUTED here rather than carried (the 498-trial census, the escalation
  residue at both shapes, the seven `src/domain` instruments, the two test walkers, the title
  sweep, the first-paint membership and the dry validator).
  ⛔ **THIS MEMBER CURES A DATA-LOSS DEFECT.** At the tip, adjusting one faction's power share
  DELETES a named person from a saved town and the delta card reports `cured: true`. Edit Mode is
  DARK behind the tier gate and nothing deployed carries it; the defect is on the branch only.
- **Packet version:** `3`
  - **Version 3 (2026-09-23, the chair, judgment 255):** V23's V11 sentence corrected from an argued "unmoved" to the EXECUTED figure (0 → 11 of 1,890 ride-along in the chains, EM-R7 v2's run on the shape-B plant); no other row, path, count or title moved.
  > **WHAT VERSION 2 CHANGED AND WHY (the Opus PRE-PROOF seat, train EM-T17 stage 2, session
  > cce01f87, 2026-09-23).** NO CONTRACT MOVED: §6's three hunks and the register row are
  > byte-identical to version 1, the eight acceptance cases are unchanged, and the effective-line
  > figures are still `141 -> 171` and `139 -> 140` (`+31`). What changed is SIX MEASURED FACTS
  > that the stage-1 placement moved or that this seat measured more exactly — the whole of the
  > movement is in `docs/implementation/PACKET_MANIFEST.json`, which grew EM-R1c's and EM-C2's
  > entries at `bb0497eb4`: **(a)** §1's collision group is re-measured against the two READY
  > members and now NAMES EM-R1c's three `requiredSymbols` rows on
  > `src/domain/edit/recordRegister.js` (no §7 intersection; the rows are a POST-EDIT obligation,
  > simulated below); **(b)** §4's sole-claimant check is re-run against the 232-packet manifest
  > and the DRY VALIDATOR's result at `bb0497eb4` is quoted; **(c)** V16's row census is corrected
  > from "eighteen rows" to the MEASURED **17 LANDED + 3 READY = 20**, with the id list fixed
  > (EM-R4 holds a row; EM-B3e holds none) and the post-edit simulation executed over all 30 rows;
  > **(d)** V20's importer sentence is corrected — `recordRegister.js` has **THREE** real
  > importers, not five (`dmLayer.js` and `importScrub.js` name it in a COMMENT only), and the
  > zero-byte claim is now carried by a STRONGER measurement: neither modified leaf is reachable,
  > statically OR dynamically, from `src/main.jsx` (1,963 modules) or `src/workers/
  > generation.worker.js` (230 modules); **(e)** V22's drift is corrected from "four `_note` rows"
  > to the MEASURED **three**, with all seven stale addresses enumerated; **(f)** A2's and V6's
  > "48 live" is disambiguated — **48 of 498 TRIALS** carry a live non-exempt violation, **53
  > instances** across them. One row is ADDED, **V23**, recording which sibling packets' asserted
  > figures this landing moves, so EM-R7's and EM-B2b's re-cuts are briefed from a measurement.
- **Verified base:** `em-t17-r8-2026-09-23` at `0d6143b58206e3e4e1b2550ae0ba4c353ee88a21`
  > *Revalidation sentence for the chair:* compiled at **`a545899ff`** and RE-MEASURED at
  > **`bb0497eb4`** (`git rev-parse HEAD` confirmed and `git status --porcelain` EMPTY at both
  > ends of both seats). `git diff --stat a545899ff bb0497eb4` over every change-manifest path and
  > every `requiredSymbols` path is **EMPTY**; the WHOLE window is
  > `docs/implementation/INDEX.md | 4 +`, `docs/implementation/PACKET_MANIFEST.json | 392 +++`,
  > `docs/implementation/packets/settlement-editor/EM-C2.md | 955 +++`,
  > `docs/implementation/packets/settlement-editor/EM-R1c.md | 587 +++` — **4 files changed, 1938
  > insertions(+)**, not one of them under `src/` or `tests/`. All three subject files are
  > byte-identical across the window by `sha256` (`6a03b5cdab9688ca` · `f0d6440ef1762768` ·
  > `038b7b9289d95d63`), the CREATE target is ABSENT (`git ls-files` -> 0), and all ten
  > `requiredSymbols` rows count **1** verbatim at `bb0497eb4`.
- **Last revalidated:** `0d6143b58206e3e4e1b2550ae0ba4c353ee88a21` (the chair stamps the date and sha at promotion)
- **Depends on:** `EM-R0a` **LANDED** (`src/domain/edit/recordRegister.js`, the register this member
  adds ONE row to) · `EM-R0b` **LANDED** (`src/domain/edit/recordInvariants.js` and its
  `CHECK_META` / `CROSS_KEY_CHECKS`, which the ladder reads and this member does not touch) ·
  `EM-R0c` **LANDED** (`src/domain/edit/mergeConsequence.js` and `tests/domain/recordMerge.test.js`,
  the file this member modifies and the arms that guard it). No unlanded dependency; nothing is
  BLOCKED.
- **Collision group:** `src/domain/edit/**`. ⭐ **RE-MEASURED AT `bb0497eb4` THREE WAYS: NO §7
  PATH IS SHARED WITH ANY PLACED OR WAITING MEMBER.** (i) *Inside stage 2* — **EM-C3** v3's §7 is
  `src/domain/edit/guardRules.js`, `tests/domain/editGuardRules.test.js`,
  `tests/build/vendorPdfLazy.test.js`, `tests/lint/flavorFields.census.test.js` and
  `tests/lint/ruinFilterRoster.walker.test.js`: intersection with this manifest **NONE**, and
  neither member holds a row on a row-keyed register, so no `rowKey` can collide. ⚠ EM-C3 MODIFIES
  `tests/lint/ruinFilterRoster.walker.test.js`, which this member names as a SEALED CHECK in §10
  but on NO §7 row — within the stage, whichever lands second runs that check against the other's
  text. (ii) *Against STAGE 1, READY on the branch* — **EM-C2** (`src/domain/edit/guards.js`,
  `tests/domain/editGuards.test.js`, `tests/property/guardsAcceptApplies.test.js`) and **EM-R1c**
  (`src/generators/pipeline.js`, `src/domain/edit/dmLayer.js`,
  `tests/lint/chooserRecordPathRegistry.walker.test.js`,
  `tests/property/dmLayerGoldenIsolation.test.js`, `REGISTER scripts/mutation-coverage-manifest.json`):
  §7 intersection **NONE**, and the dry validator at `bb0497eb4` reports NO duplicate path against
  either. ⛔ **BUT EM-R1c HOLDS THREE `requiredSymbols` ROWS ON `src/domain/edit/recordRegister.js`**
  — `export const KEYED_COLLECTIONS = Object.freeze({`, `  institutions: 'name',` and
  `  npcs: 'id',`. None is a §7 collision; all three are a POST-EDIT OBLIGATION of this member,
  and all three count **1 -> 1** on the planted file (V16). (iii) *Against STAGE 3 and STAGE 4* —
  **EM-B2b** v5 (`dmLayer.js`, `regenerationDelta.js`, `fieldDeclarations.js`, `types.js` and four
  test rows) intersects this manifest **NOWHERE**; **EM-R7** (`tests/property/editCorpusRatchet.test.js`,
  `tests/lint/heldKeyWriterCensus.walker.test.js`, its mutation-coverage `rowKey`) likewise, but it
  holds FOUR `requiredSymbols` rows on this member's two MODIFY paths
  (`mergeConsequence.js :: export function mergeConsequence` and `:: export function enclosingGroup`;
  `recordRegister.js :: export const CONSISTENCY_GROUPS` and `:: export const RECORD_CLASSES`),
  every one of which this member preserves verbatim (V16). **V23 names the sibling FIGURES this
  landing moves.**
  ⛔ This member holds **NO row on any row-keyed register** (§7), so no `rowKey` can collide.
- **Commit authority:** edits only; the chair commits
- **Baseline posture:** measured. Every figure in §5, §7, §9 and §10 was EXECUTED at `a545899ff`
  against the planted contract through a `node:module` loader hook that serves the planned text in
  memory; nothing was written into any git worktree. The receipts are `EM-R8.evidence.md`.
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: TO BE STAMPED BY THE CHAIR)
  > ⛔ **MEASURED AT THIS COMPILE'S TIP `a545899ff`:**
  > `125c693214235a81bf4f2b38506b621859a35e0325ff2ea58d335b4e0681a99d`
  > (`shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md`) — the SIXTH AMENDMENT. The hash
  > moves with every amendment, which is why the header line is the CHAIR'S STAMP and never a
  > lane's figure (§P3.5).

---

## 1. Reconciled authority

1. **The owner**, ODQ §934.36–§934.42: build the settlement editor now.
2. **THE PROMISE** (constitutional) — a seed is a STARTING world forever and lived history is
   immutable. This member exists because the tip's ladder ERASES A NAMED PERSON from a saved town
   to make a receipt true. It touches `generationCoherenceReceipt.repairs`, the declared HISTORY
   sub-path, in neither direction (§9 A5).
3. **Design §22 ruling 9** — *with an edit, no UNEDITED held fact moves*; **§22.2 item 3** (the
   deterministic ladder and the ratchet); **§22.2 item 6** (the chain is the semantics, and two
   orders of two non-commuting edits "may differ, and that is history, not a defect"); **§22.2
   item 7** (the declared MIRROR and RECEIPT sub-paths are RECOMPUTED after the merge, never
   merged); **§22.1 correction 5** and **§22.3 item 3** (a collection under no sound key is ATOMIC).
4. **The chair, judgment 240** (2026-09-23): 237 R2 is WITHDRAWN by measurement; the roster
   escalation is THE LADDER'S, and the shape to measure is *a READING is never repaired by
   overwriting a HELD key*.
5. **The chair, judgment 245** (2026-09-23): **SHAPE B RULED** — (1) no rung's scope carries a HELD
   key; (2) the receipt's two counted evidence rows are recomputed from the MERGED record; (3) a
   `CLASS_EXCEPTIONS` row declares that sub-path MIRROR and STATES WHICH DECLARATION GOVERNS WHICH
   ACT; (4) `.repairs` is neither read nor written; (5) the `narrative` arm owes a CONSTRUCTED
   positive control. **The recorded flip condition:** if the owner rules the judgment evidence
   HISTORY rather than a mirrored READING, B is unlawful and SHAPE A is the shape — this member is
   then re-cut, not repaired (§11 STOP-6).
6. **The measurement seat's executed shapes** (`$SP/lane-measure-EM-R8-scratch/EM-R8.measurement.md`,
   every figure executed at `1b073009f`), re-executed here at `a545899ff` (§5).

**Resolved contradictions**

- *The brief's §7: "REGISTER `scripts/mutation-coverage-manifest.json` (your `rowKey` only)".*
  → ⛔ **REFUTED BY MEASUREMENT, and the row is REFUSED.** `ENFORCER_DIRS` (eight trees) does not
  contain `tests/domain`, and `NAME_PATTERN.test('mergeLadderHeldKeys.test.js')` is **false**, so
  `enumerateInvariants` does not enumerate the CREATE — and
  `tests/lint/mutationCoverageManifest.test.js`'s arm *"no stale entries: every manifest invariant
  key is an enumerated, existing file"* would RED on a row for it. The member carries NO row
  (T17-14). The file's NAME is therefore a contract of §7: a spelling carrying `contract`,
  `census`, `walker`, `golden` or `pin` would enrol it and owe the row.
- *The measurement seat's plant of SHAPE B.* → ⛔ **RE-CUT HERE, TWICE, ON EXECUTED EVIDENCE**
  (T17-16): as the seat wrote it, `npm run typecheck:domain:strict` would have RED at TWO errors
  against a HARD ZERO — `RECORD_CLASSES[<string>]` is an implicit-any index on a frozen
  literal-typed object, and the JSDoc block sat above two regex consts so it attached to the first
  const and left `merged` implicitly any. §6's text reads the class table through a `Map`, exactly
  as `recordMergeTree.js` already does for the same reason, and puts the block on the function.
  Both configurations now measure **0 diagnostics** on this member's two paths.
- *The brief's arm (4): "`.repairs` … an arm asserts it byte-identical across every trial".*
  → ⛔ **REFUTED BY MEASUREMENT.** At the TIP, before this member, `.repairs` already moves in
  **12 of the 120 config-channel trials**, and under the cure it still does — in every case
  because a rung took `generationCoherenceReceipt` WHOLE from `R1` (12/12, T17-7). The ladder's
  rung writes at the TOP-LEVEL key, where no `CLASS_EXCEPTIONS` row reaches it. A5 asserts the
  shape that is true and is this member's — no rung fires ⇒ `.repairs` is byte-identical (450/450)
  — and the ladder's own top-level write is DECLARED, with its cure slotted to the chair (§11).
- *Whether the restatement belongs in `recomputeReceipts` or in `recomputeMirrors`.*
  → **`recomputeReceipts`, and the reason is EXECUTED, not argued** (T17-4). A CALL-SITE control
  with the identical helper called from `recomputeMirrors` leaves **34 of 498 trials carrying a
  LIVE `V-EVIDENCE-ROSTER`**, against **0 of 498** from `recomputeReceipts`: `recomputeMirrors`
  runs ONCE, before the ladder, while `recomputeReceipts` runs after `mergeTree` AND after every
  rung, so only there is the restatement THE LAST WORD on every path.

The implementer does not read other documents to reinterpret this packet.

---

## 2. Outcome

**Observable result:** the merge's deterministic escalation ladder can no longer repair a violated
invariant by overwriting a HELD fact. A rung's scope is filtered to the keys the register does not
class HELD, a rung whose scope empties is skipped, and the disagreement a `prose-count` check
reports is cured the other way round — the coherence receipt's two COUNTED evidence rows are
restated from the MERGED record as a MIRROR of it, recomputed after the merge exactly as
`factions[].members[]` already is.

**Definition of done:** over the 63-row structured stride and all eight edits (498 trials) through
the REAL re-derivation and the REAL merge: **0 HELD keys move**, **0 named people leave or arrive**,
the layer-driven channel carries **0 escalations and 0 live non-exempt violations**, the receipt's
two counted rows equal the merged record in **498/498**, no character outside those digits moves in
**498/498**, `generationCoherenceReceipt.repairs` is byte-identical wherever no rung fires
(**450/450**), chain determinism holds **252/252** and undo-then-re-edit **126/126** — each arm
EXECUTED, not argued.

**In scope**

1. **The held-key law** (design §22 ruling 9): `ladderFor` maps every rung's scope through a filter
   that drops a key whose `RECORD_CLASSES` class at the path's TOP-LEVEL segment is `HELD`, and
   drops a rung whose scope empties.
2. **The mirror** (§22.2 item 7): a module-local `restateProseCounts(merged)` called as the FIRST
   statement of `recomputeReceipts`, rewriting ONLY the digits `V-EVIDENCE-ROSTER`'s and
   `V-EVIDENCE-EVENTS`'s own patterns capture, from the merged record's own `npcs`,
   `relationships` and `history.historicalEvents`.
3. **The declaration** (judgment 245 (i)): ONE `CLASS_EXCEPTIONS` row declaring
   `generationCoherenceReceipt.judgments[].evidence` a MIRROR sub-path, with the note that states
   WHICH DECLARATION GOVERNS WHICH ACT — `ATOMIC_COLLECTIONS` the merge, this row the post-pass —
   and A4 proving both acts on ONE trial.

**Explicit non-goals**

- **The config channel's own repair.** E7 and E8 drive a world-fact change with a DORMANT layer, so
  `R1` there is a plain generation of a DIFFERENT town. The cure leaves `V-DEFENSE-INST` (31) and
  `V-EVIDENCE-CONFLICT` (22) live there, on the seam **EM-B2b** owns and EM-R7 §6 declares dark.
  A2 asserts that figure at this landing and declares it as EM-B2b's; this member does not close it.
- **The ladder's TOP-LEVEL rung write, which no `CLASS_EXCEPTIONS` row reaches** (§11, the slot).
- **The pin closure** (EM-R1c's; judgment 240 (a) refused widening it to a held key's readers).
- **The corpus ratchet** — EM-R7 version 2, re-cut against this member's flip and placed at stage 4.
- Any caller. Nothing in `src/` imports `mergeConsequence.js`; the apply point prices its bytes.

---

## 3. Hard scope budget

| Limit | This packet | Measured |
|---|---:|---|
| one behaviour family | the ladder's held-key law | — |
| new persisted record family | **0** | no persisted shape moves |
| named writer per state | **0** | a pure function; it returns a value |
| feature flag | **0** | wave 1 is headless; Edit Mode is dark behind the tier gate |
| user-facing surface | **0** | renders nothing |
| direct production consumers | **0** | T17-17: `git grep -F mergeConsequence -- src` finds the file itself and nothing else |
| new logic-bearing leaves ≤ 2 | **0** | no CREATE under `src/` |
| existing logic files modified ≤ 3 | **2** | `mergeConsequence.js`, `recordRegister.js` |
| registration-only files touched ≤ 3 | **0** | §7 carries no REGISTER row (T17-14) |
| handwritten files ≤ 12 | **4** (2 production + 2 tests) | §7 |
| new/changed effective production lines ≤ 400 | **+31** | T17-11 |
| each leaf under its layer ceiling | `src/domain/edit/mergeConsequence.js` **141 -> 171** · `src/domain/edit/recordRegister.js` **139 -> 140**, against `src/domain/**`'s `max-lines` **800** | T17-11 |
| shared / hot-file delta ≤ 15 effective | **n/a** — neither path is on the standing hot-file list | `PACKET_STANDARD.md`, "Hot files" |
| acceptance cases ≤ 8 | **8** | §9 |

Counted with eslint's own `Linter` in process: `max-lines` with `{ skipBlankLines: true,
skipComments: true }` — the one effective-line instrument (§P9).

---

## 4. Sealed dispatch and preflight

`scripts/implementation-session.mjs`, check by check, as it would read at `__BASE__`:

1. **Branch name** — this member's own lane branch, cut at the train base (§P10.1/2).
2. **Ancestry** — `__BASE__` is an ancestor of the lane branch tip.
3. **Substrate unchanged since the verified base** — this member's substrate is
   `src/domain/edit/recordRegister.js`, `src/domain/edit/recordInvariants.js`,
   `src/domain/edit/recordMergeTree.js` and `src/data/economyFingerprint.js`. All FOUR are present
   at `a545899ff` and all four are LANDED (EM-R0a, EM-R0b, EM-R0c, EM-R0f).
4. **CREATE targets absent** — `tests/domain/mergeLadderHeldKeys.test.js` is ABSENT at
   `bb0497eb4` (`git ls-files` -> 0) and this member is its SOLE claimant, RE-MEASURED against
   the placement tip's **232-packet** manifest: **0** occurrences of the basename across
   `docs/implementation/PACKET_MANIFEST.json`, **0** across `docs/` in the tree, and 0 in the
   chair kit's `packets-waiting/` once this member's own body is excluded (hazard 49).
5. **Non-CREATE targets present and clean** — the three MODIFY/TEST paths exist at the base.
6. **git-clean** — the build lane's own worktree.

⭐ **THE DRY VALIDATOR, RUN IN MEMORY AT `bb0497eb4`** (`validatePacketManifest(manifest,
{ rootDir })` over a spliced COPY; nothing written to any worktree): `packets 232 -> 233`,
**2 errors, BOTH PLACEMENT-ONLY** — `EM-R8.packetPath does not exist:
docs/implementation/packets/settlement-editor/EM-R8.md` and `EM-R8.verifiedBase must be a
lowercase 40-character SHA` — and **0 errors naming any other packet**. Against the two READY
members the duplicate-path check reports `DUP vs EM-R1c: NONE` and `DUP vs EM-C2: NONE`. Both
errors clear the moment the chair writes the packet into the tree and stamps the base.

**Generators among `checks`: NONE.** No command in §10 writes a file, so §P2.12's generator-last
rule has nothing to order and no generated artifact is owed. **Edge-shared INPUT MEMBERSHIP**
measured against all five metas' own `inputs` lists: **none**.

---

## 5. Verified tree contract

Every row was measured at **`a545899ff`**, by EXECUTION. `T17-n` names the receipt in
`EM-R8.evidence.md`.

| # | Fact | Where | Proof |
|---|---|---|---|
| V1 | `ladderFor` takes a CROSS-KEY check's step-1 scope from the violation's own `keys`, and `V-EVIDENCE-ROSTER`'s three keys are `[generationCoherenceReceipt, npcs, relationships]` — **two of them HELD** | `src/domain/edit/mergeConsequence.js :: ladderFor`; `src/domain/edit/recordInvariantMeta.js :: CHECK_META` | T17-0. The register classes `npcs` and `relationships` HELD, "never re-derived" |
| V2 | ⛔ **THE DEFECT, REPRODUCED AT THIS TIP.** Over the 126 power-card trials the tip's step-1 cure moves `npcs` in **5**, and over the 498-trial census a REAL held fact moves in **66** (`npcs` 52 · `relationships` 47 · `institutions` 31 · `conflicts` 22) | the real `rederive` and the real merge | T17-1, T17-2. The control reproduces EM-R7 V7/V8 and EM-R1c §0.7 exactly |
| V3 | ⛔ **AND WHAT IT COSTS, NAMED.** In **26 of 498** trials a named person LEAVES the saved town and in **20** one ARRIVES — `The High Priestess` leaves `village|germanic|hills|road|civilized|golden-master-v3` on an E5 share edit, and the delta card reports `cured: true` | the same census | T17-3 |
| V4 | **SHAPE B, MEASURED: held keys moved 0/126 and 0/498; named people gone 0/498 and arrived 0/498** | the planted contract | T17-1, T17-3 |
| V5 | **The layer-driven channel is CLEAN**: over E1/E2/E3/E4/E5/E6 (378 trials) the cure leaves 0 escalations and 0 live non-exempt violations | the planted contract | T17-2, T17-9 |
| V6 | **The config channel EXHAUSTS and is DECLARED**: E7 **31 of 57** · E8 **17 of 63** escalating trials (escalation ROWS 136 and 76 — the ladder now runs to step 3 instead of curing at step 1), every one EXHAUSTED, leaving **48 of 498 TRIALS carrying a live non-exempt violation** — **53 INSTANCES** across them, `V-DEFENSE-INST` **31** and `V-EVIDENCE-CONFLICT` **22** (a trial may carry both; E7 17+17, E8 14+5) — and **`V-EVIDENCE-ROSTER` 0**. A `Depends`-free cross-reference: **EM-B2b** owns this seam (EM-R7 §6 declares it dark until `worldFacts` has a reader) | the planted contract | T17-2 |
| V7 | **The blast radius is only where the ladder already fired**: of the 498 merged records, the 432 the TIP does not escalate on are **byte-identical** under the cure (432/432), and 0 of the 66 the tip does escalate on are | a sha256 of every merged record, both shapes | T17-2 |
| V8 | **The mirror is exact**: the two counted rows equal the merged rosters in 498/498 (finalGraph) and 498/498 (narrative), and the evidence with EVERY digit masked equals the PURE TREE MERGE's in **498/498** — nothing but those digits moves | the planted contract | T17-3, T17-6 |
| V9 | **The two declarations, proven on ONE trial**: `receipts.atomicTaken` contains `generationCoherenceReceipt.judgments[].evidence` (the merge takes it from `R1` WHOLE, `"7 NPCs / 12 relationships"`), then `recomputeReceipts` restates it to `"8 NPCs / 12 relationships"`, the merged rosters' own sizes, with the masked text still `R1`'s | `village|germanic|hills|road|civilized|golden-master-v3`, E5 | T17-5 |
| V10 | ⛔ **`generationCoherenceReceipt.repairs` IS ALREADY MOVED BY THE LADDER, BEFORE THIS MEMBER.** 12 of the 120 config-channel trials at the TIP, 12 under the cure, and in **12/12** because a rung took `generationCoherenceReceipt` whole from `R1`. Where no rung fires, `.repairs` is byte-identical: 450/450 under the cure, 432/432 at the tip | the same census, decomposed | T17-7. The ladder's rung writes at the TOP-LEVEL key with `put`, where no `CLASS_EXCEPTIONS` row reaches it — §11's slot |
| V11 | **`restateProseCounts` never reads or writes `.repairs`**, proven directly: `recomputeReceipts` on a corpus record that CARRIES a generation repair restates the planted narrative digit and leaves `.repairs` byte-identical, with the RETURN still the fingerprint's own `false` | `village|germanic|plains|road|civilized|golden-master-v3` | T17-6 |
| V12 | **The chain holds**: determinism 252/252 and undo-then-re-edit 126/126 under the cure, unmoved from the tip; over the eight named ordered pairs on the five named rows, the four carrying the SHARE edit stop disagreeing (8 disagreements -> 0) and the four carrying the SEAT edit still disagree, on `town|germanic|auto|random_trade|civilized|gm-seed-a` alone (16 -> 4) | the real chain, two edits deep | T17-8. Over the whole 1,890-pair census the measurement seat read 50 -> 4; that census is EM-R7 v2's |
| V13 | ⛔ **THE CALL SITE IS LOAD-BEARING AND THE PROOF IS A CONTROL.** The identical helper called from `recomputeMirrors` instead leaves **34 of 498** trials carrying a live `V-EVIDENCE-ROSTER` (finalGraph honest only 464/498); from `recomputeReceipts` it is **0 of 498** and 498/498 | the CALL-SITE control plant | T17-4 |
| V14 | **A MIRROR row is INERT in the tree merge.** `recordMergeTree.js :: KEPT_BY_CLASS` is `['HISTORY','AUTHORED']`, so a MIRROR exception never reaches the node rule's early return; measured end to end, the planted register changes NOTHING — the merged record, the escalations and the evidence are identical with and without it in **126/126** trials | `recordMergeTree.js :: mergeNode` | T17-20, T17-1 |
| V15 | **The register row is LIVE, not dead.** `recordRegisterTotality.walker.test.js :: A2` requires every `CLASS_EXCEPTIONS` path to be OBSERVED on a generated record; the new path is, and A3's KEYED-x-ATOMIC disjointness is untouched (the row is a class exception, not a collection declaration) | the walker's own `buildCensus` walk | T17-20. ⚠ A2's TITLE says "the three class exceptions are live" and the table becomes FOUR — a stale NUMERAL in a title whose assertion iterates `Object.keys` and stays total. §11 names it; the re-word is not this member's path |
| V16 | **Every sibling `requiredSymbols` row on this member's paths is PRESERVED VERBATIM, and the POST-EDIT SIMULATION IS EXECUTED OVER ALL THIRTY.** RE-MEASURED at `bb0497eb4`: **17 LANDED rows** (EM-B3d 1 · EM-B2a1 1 · EM-B2a4 1 · EM-C4a 1 · EM-R0b 1 · EM-R0c 5 · EM-R1 2 · EM-R2 2 · EM-R4 1 · EM-R5 2) **+ 3 READY rows (EM-R1c**, which land at stage 1's flip) — every one on `recordRegister.js`; **no row of any other packet names anything in `mergeConsequence.js` or `tests/domain/recordMerge.test.js`**. With this member's own ten, **30 rows count `1 -> 1`** against the planted files; **non-unity: 0** | `docs/implementation/PACKET_MANIFEST.json`, re-read at the placement tip | T17-15, re-executed. ⛔ Version 1 said "eighteen rows … EM-B3e": EM-B3e holds NO row on these paths and **EM-R4** does; the measured LANDED count is **17**. **NO `retiredSymbols` ROW IS OWED**, and `retiredBy` therefore appears on no row of this capsule |
| V17 | **Both TypeScript configurations are ZERO on the planted text** — `tsconfig.full.json` 0 and `tsconfig.domain-strict.json` 0 diagnostics on this member's two paths. ⛔ The seat's first cut measured TWO strict errors; §6's text is the cure | the TypeScript compiler API over each config's own options | T17-16 |
| V18 | **The seven `src/domain` instruments are at ZERO DELTA on both MODIFYs**: voice mechanics `em +0 · bang +0`; tuning `P2 +0 · P3 +0`; any-cast `{any: 0, suppress: 0} -> {any: 0, suppress: 0}` with no baseline row for either file; prose-numerics hits `0 -> 0` and **0** line-addressed baseline rows naming either path; entropy `createPRNG(` `0 -> 0` (live 36 / 47); ruin-filter `READER_RE` over `codeOnly` **false -> false**; `dmLayerGoldenIsolation :: A8`'s generators regex **false -> false** | each library's OWN logic, in plain node | T17-10 |
| V19 | **The CREATE's lighting posture is EXECUTED**: the walker's own classifier over the draft gives `parkReasonsFor -> []` (CREDITED), `liveTitlesIn -> 8`, `liveSuiteTitlesIn -> 1`; `seedLoopTotality`'s own `stripNoise` and regexes find **NO** bare seed loop; the nine titles collide with **NOTHING** across the estate's 2,697 test files | the two walkers, lifted out of tree and run over the draft text | T17-12, T17-13 |
| V20 | **Zero bytes on every signed budget, as a MEMBERSHIP fact, PROVED TWO WAYS.** (a) The membership probe: `EAGER_FIRST_PAINT_MODULES` holds **270** modules and **0** under `src/domain/edit/`. (b) ⭐ **THE REACHABILITY WALK, re-run at `bb0497eb4`: NEITHER MODIFIED LEAF IS REACHABLE AT ALL** — not statically and not through any `import()` edge — from `src/main.jsx` (**1,963** modules transitively) or from `src/workers/generation.worker.js` (**230** modules, **0** of them under `src/domain/edit/`). So the delta is `+0 B` on the worker ceiling (`WORKER_BUNDLE_CEILING_BYTES = 1391327`, zero slack), `+0 B` on the lazy engine (`vendorPdfLazy` `< 679_000`) and `+0 B` on first paint. `mergeConsequence.js` has **NO** `src/` importer; `recordRegister.js` has **THREE** (`mergeConsequence.js`, `recordMergeTree.js`, `tracePartition.js` — all under `src/domain/edit/`), and this member adds **+1 effective line** to it | the exported set, IMPORTED (it reads no `dist`); the import graph walked from each entry | T17-17, re-executed. ⛔ Version 1 said "five `src/` importers … or `src/lib/importScrub.js`": measured with `git grep -nP "from\s+'[^']*recordRegister\.js'"`, `dmLayer.js` (line 12) and `importScrub.js` (line 198) name the file in a COMMENT and import nothing. A rise is the OWNER's (§P11.2); this member re-mints nothing and edits no budget test |
| V21 | **The wiring census does not move**: `stamp.producerIndexFiles` is a NUMBER (1190 at this tip) counting `.js` files under `src/generators/**` and `src/domain/**` — this member CREATEs no `src` leaf, so its delta is **+0**; `stamp.files` and `stamp.candidateLeaves` name NEITHER modified path | `docs/content/wiring-census.json` | T17-19 |
| V22 | ⚠ **SEVEN ADDRESSES DRIFT ACROSS FOUR LOCATIONS AND NONE REDS.** The register edit inserts nine lines after the `generationCoherenceReceipt.repairs` row (line 66), so every address at or below 67 shifts by +9. RE-MEASURED at `bb0497eb4`: **THREE `_note` rows in `PACKET_MANIFEST.json`, all EM-R0c's** — the `KEYED_COLLECTIONS` row (`:70 -> :79`), the `ATOMIC_COLLECTIONS` row (`:122 -> :131`, and the two BARE sub-addresses in its own prose, `(:125) -> (:134)` and `(:131) -> (:140)`) and the `CONSISTENCY_GROUPS` row (`:175 -> :184`) — plus **`EM-R0c.md:376`**, which cites `recordRegister.js:175`-`:190` (`-> :184`-`:199`). ⭐ **THREE OTHER ADDRESSES DO NOT MOVE** and must not be touched: EM-B2a1's and EM-B2a4's `:59` and EM-R0c's `:14` and `:62` all sit ABOVE the insertion. `sourceCitationIntegrity.walker.test.js` convicts only a PAST-EOF address and this edit GROWS the file, so ARM 1 stays green — the addresses go stale, not red | the walker's own arms; the manifest's `_note` corpus, parsed | T17-18, re-executed. ⛔ Version 1 said "four `_note` rows": the measured count is **THREE** rows carrying FIVE addresses. §11 slots the re-address; it is not this member's path (those rows are other packets') |

| V23 | ⭐ **WHICH SIBLING FIGURES THIS LANDING MOVES** — measured at `bb0497eb4` by running the escalation residue at BOTH shapes, so EM-R7's and EM-B2b's re-cuts are briefed from a measurement and not from a prediction. **EM-R7 version 1's V7** ("no non-exempt violation 498/498; faction share 2/63, seat 3/63, terrain 68 over 40/57, culture 32 over 21/63") becomes **48 of 498 trials live (53 instances); faction share 0/63; seat 0/63; terrain 136 over 31/57; culture 76 over 17/63** — the escalation ROWS rise because the ladder EXHAUSTS instead of curing at step 1. **EM-R7 V8**'s whole subject is REMOVED: the 5 layer-driven `V-EVIDENCE-ROSTER` escalations become **0** and `npcs` moves in **0** trials. **EM-R7 V9**'s "1,890 ordered-pair trials, 50 non-commuting" becomes **4** (V12). **EM-R7 V6b and V10 are UNMOVED** (`settled` / `taken` / `orderMoved` are `mergeTree`'s receipts of the FIRST merge, computed before `guardMergedRecord`) — **but V11 MOVES, 0 → 11 of 1,890 ride-along in the two-edit chains — EXECUTED by EM-R7 v2's seat on the shape-B plant (judgment 255): a chain's SECOND merge is handed the first merge's outputs, so this member's ladder and post-pass change its receipts; version 2's "unmoved, PLAUSIBLE by construction" was refuted by that run (version 3, judgment 255). **EM-B2b v5** routes a `world-fact` root to the config channel, which is the seam E7/E8 drive: its landing moves **A2, A6 and V6's config figures and A5's 12/12** — this member's own assertions, and the reason A2 declares the seam EM-B2b's | the escalation residue at BOTH shapes, 498 trials each | T17-2, re-executed at `bb0497eb4`. §12 item 11 (d) reports it |

---

## 6. Exact contracts

### Inputs and outputs

```js
// src/domain/edit/mergeConsequence.js — PURE. No store, PRNG, clock, locale or I/O.
// The five exports are UNCHANGED in name, arity, signature and return shape.
export function recomputeMirrors(merged);      // -> number of members rewritten
export function recomputeReceipts(merged);     // -> boolean, whether the DIGEST moved
export function enclosingGroup(path);          // -> the SMALLEST declared group, or null
export function guardMergedRecord(merged, R1, exempt);  // -> escalation rows
export function mergeConsequence(record, R0, R1, options);  // -> { record, delta }
```

⛔ **THE HELPER IS MODULE-LOCAL, AND THAT IS A CONTRACT, NOT A STYLE.**
`tests/domain/recordMerge.test.js :: A8` enumerates this module's **FIVE** exports by name and
asserts the list exactly. Exporting `restateProseCounts` would make it six and RED that landed arm.

### The three hunks, exactly

```js
// 1 · beside `vkey`, above `READING_KEYS` — the class table as a real MAP, which is how
//     `recordMergeTree.js` reads the same table and why the strict config stays at ZERO.
/** The register's class table as a real map: a string key reads one with no index cast, exactly
 *  as `recordMergeTree.js` reads the same three tables. */
const CLASS_OF = new Map(Object.entries(RECORD_CLASSES));
/** @param {string} key @returns {boolean} The register classes this path's top-level key HELD. */
const isHeldScope = (key) => CLASS_OF.get(key.split(/[.[]/)[0]) === 'HELD';
```

```js
// 2 · between `recomputeMirrors` and `recomputeReceipts`. The two patterns are the LEAF'S OWN
//     (`recordInvariants.js :: ROSTER_RE` and `:: EVENTS_RE`) with the surrounding text captured,
//     so a replace can move the digits and nothing else. THE CONSTS COME FIRST: a JSDoc block
//     above them attaches to the first const and leaves `merged` implicitly any (V17).
/** `V-EVIDENCE-ROSTER`'s own `ROSTER_RE`, with the surrounding text captured so only the digits move. */
const ROSTER_STATED = /(\d+)(\s+NPCs?\s*\/\s*)(\d+)(\s+relationships?)/;
/** `V-EVIDENCE-EVENTS`'s own `EVENTS_RE`, captured the same way. */
const EVENTS_STATED = /(\d+)(\s+historical events?)/;

/**
 * The two PROSE-COUNT evidence rows of the coherence receipt — the pair `recordInvariants` reads
 * for `V-EVIDENCE-ROSTER` and `V-EVIDENCE-EVENTS` — restated from the MERGED record's own
 * rosters. Only the digits these patterns capture move; no other character of the evidence,
 * and `generationCoherenceReceipt.repairs` (HISTORY) is never read or written.
 * ⛔ MODULE-LOCAL: `tests/domain/recordMerge.test.js` A8 enumerates this module's FIVE exports.
 * @param {RecordCard} merged @returns {number} evidence rows restated
 */
function restateProseCounts(merged) {
  const receipt = merged.generationCoherenceReceipt;
  if (!isObj(receipt) || !Array.isArray(receipt.judgments)) return 0;
  const npcs = merged.npcs;
  const links = merged.relationships;
  const history = merged.history;
  const events = isObj(history) ? history.historicalEvents : undefined;
  let restated = 0;
  for (const judgment of receipt.judgments) {
    if (!isObj(judgment) || !Array.isArray(judgment.evidence)) continue;
    for (const row of judgment.evidence) {
      if (!isObj(row) || typeof row.evidence !== 'string') continue;
      let next = row.evidence;
      if (row.path === 'finalGraph' && Array.isArray(npcs) && Array.isArray(links)) {
        next = next.replace(ROSTER_STATED, (whole, head, mid, tail, end) => `${npcs.length}${mid}${links.length}${end}`);
      } else if (row.path === 'narrative' && Array.isArray(events)) {
        next = next.replace(EVENTS_STATED, (whole, head, end) => `${events.length}${end}`);
      }
      if (next !== row.evidence) { row.evidence = next; restated += 1; }
    }
  }
  return restated;
}

// ...and ONE line inside the existing `recomputeReceipts`, as its FIRST statement:
export function recomputeReceipts(merged) {
  restateProseCounts(merged);
  // ... the landed body, byte-identical from `const power = merged.powerStructure;` on.
```

```js
// 3 · the LAST statement of `ladderFor`, replacing `return steps;`
  return steps
    .map((rung) => ({ ...rung, scope: rung.scope.filter((key) => !isHeldScope(key)) }))
    .filter((rung) => rung.scope.length > 0);
```

```js
// src/domain/edit/recordRegister.js — ONE data row and its note, inside CLASS_EXCEPTIONS,
// immediately after the `generationCoherenceReceipt.repairs` row.
  // ⛔ ONE PATH, TWO DECLARATIONS, TWO DIFFERENT ACTS -- and it is in both tables on purpose.
  //    ATOMIC_COLLECTIONS governs THE MERGE: the evidence array has no sound key, so the tree
  //    merge takes it from R1 as ONE value, exactly as it always has.
  //    This MIRROR row governs THE POST-PASS: the two PROSE-COUNT rows the receipt carries
  //    (finalGraph's NPC and relationship counts, narrative's historical-event count) are a pure
  //    function of the MERGED record's own HELD rosters, RESTATED after the merge and never
  //    merged. Only the digits those two patterns capture move; no other character does.
  //    generationCoherenceReceipt.repairs stays HISTORY and is neither read nor written by it.
  'generationCoherenceReceipt.judgments[].evidence': 'MIRROR',
```

### State schema

**No state, no persisted key, no new record family.** Both files are pure; `recordRegister.js` is
frozen data with no branch, no function and no import.

### Transition table — the ladder, before and after

| a violation of | step 1's scope at the tip | step 1's scope under this member | what happens next |
|---|---|---|---|
| a CROSS-KEY check whose keys are all READING (`V-EVIDENCE-EVENTS`, `V-EVIDENCE-TENSION`, `V-EVIDENCE-STRESS`, `V-STRESS-IDENTITY`) | the check's own keys | **unchanged** | unchanged |
| `V-EVIDENCE-ROSTER` | `[generationCoherenceReceipt, npcs, relationships]` | `[generationCoherenceReceipt]` | it no longer ARISES: the restatement runs before the guard reads the record |
| `V-EVIDENCE-CONFLICT` | `[conflicts, generationCoherenceReceipt]` | `[generationCoherenceReceipt]` | steps 2 and 3 cannot repair it either; the ladder EXHAUSTS and the delta card says so |
| `V-DEFENSE-INST` | `[defenseProfile, institutions]` | `[defenseProfile]` | as above |
| a GROUP-keyed check whose group root is HELD (`V-POWER-100`, `V-LEGIT-SUM`, `V-LEGIT-FLAGS`, `V-BAND-LEGITIMACY`, `V-GOVERNING`, `V-FLAGVEC`'s power arm) | `[powerStructure]` | the rung is SKIPPED | it falls to step 3's READING sweep. ⚠ **Population in this corpus: ZERO** under every shape, so this row is reasoning, not measurement (§9's CANNOT-CATCH 3) |
| any rung, at any step | `R1`'s value at each key, written with `put` at the TOP LEVEL | the same, for the surviving keys | ⛔ unchanged, INCLUDING the top-level write that carries a declared HISTORY sub-path with it (V10) |

### Ordering and precedence

`mergeTree` -> `recomputeMirrors` -> **`recomputeReceipts` (which now restates the two counted rows
FIRST, then recomputes the one declared RECEIPT)** -> `guardMergedRecord`, which calls
`recomputeReceipts` again after EVERY rung. The restatement is therefore the LAST WORD on every
path — the reason it lives in `recomputeReceipts` and not in `recomputeMirrors` (V13).

### Absence rules

- absent `generationCoherenceReceipt`, a non-array `judgments`, a non-array `evidence`, a
  non-string `row.evidence`: the restatement returns 0 and writes nothing.
- a `finalGraph` row with a non-array `npcs` or `relationships`, or a `narrative` row with a
  non-array `history.historicalEvents`: that row is left exactly as it stands.
- a row whose text the pattern does not match: `replace` returns the same string, `next ===
  row.evidence`, and NOTHING IS WRITTEN — which is why calling `recomputeReceipts` on a generated
  record whose counts already agree is a no-op even though that record's evidence rows are FROZEN.
- ⛔ **a FROZEN row whose count DISAGREES throws `TypeError`**, measured (T17-21). That is
  unreachable by construction: the merge only ever writes the private value `mergeTree` cloned for
  it, and a generated record's counts agree in 498/498. The post-pass is not a general-purpose
  repair for a caller's frozen record, and §11 STOP-5 says so.
- a rung whose scope empties: the rung is DROPPED, not run with an empty scope.

### Determinism

Hash / fork key **NONE**. No PRNG, no clock, no locale, no `Math.random`, no `Date`, no `Intl`.
`isHeldScope` reads a Map built once from the register's own declaration order; the filter and the
two patterns are total functions of their input. Chain determinism 252/252 and undo-then-re-edit
126/126, unmoved (V12).

### Flag and dormancy

Flag **NONE**. Dormancy is structural: nothing in `src/` imports `mergeConsequence.js`, so the
code is unreachable rather than switched off. **Golden posture: UNCHANGED** — no golden and no
prose manifest moves, and motion is a STOP (§P3.1).

### Lifecycle

| Create | Read | Persist | Reload | Regenerate | Undo | Import/migrate | Public veil |
|---|---|---|---|---|---|---|---|
| the merge returns a value | three arguments only | **nothing** | nothing | fresh generation never calls this module | the store's snapshot, never an inverse merge; 126/126 | edits do not travel (HZ-TRAVEL) | renders nothing |

### Receipts and privacy

`delta` is unchanged in SHAPE. What changes is its CONTENT on the 66 trials the ladder used to
fire on: 5 rows that said `step 1 … cured: true` are gone, and the 48 config-channel trials now
carry four rows each ending `step: "EXHAUSTED", scope: "every reading", cured: false` — an honest,
visible, un-repaired disagreement in place of a silent rewrite. No PII, no seed, no prose.

### Alignment and edit story

Alignment: `DECLARED EMPTY.` Edit story: `ENGINE-ONLY: this member changes how the merge repairs
itself; it exposes no DM verb and renders no card.`

### User-facing copy

**NONE.** This member spells no user-facing string. The two regular expressions and the register
row carry no prose the DM reads, and the voice walker measures `em +0 · bang +0` on both files.

---

## 7. Exact change manifest

| action | path | symbols / region | bound | note |
|---|---|---|---|---|
| `MODIFY` | `src/domain/edit/mergeConsequence.js` | `ladderFor`, `recomputeReceipts`, and the two module-local additions `CLASS_OF` / `isHeldScope` / `ROSTER_STATED` / `EVENTS_STATED` / `restateProseCounts` | **+30** effective | The three hunks of §6, in that order, and nothing else. ⛔ **The helper stays MODULE-LOCAL** (A8 enumerates FIVE exports). ⛔ **Read the class table through `CLASS_OF`, never by index** — `RECORD_CLASSES[<string>]` is an implicit-any index and `typecheck:domain:strict` is a HARD ZERO (V17). ⛔ **The two regex consts come BEFORE the JSDoc block**, or the block attaches to the first const and `merged` is implicitly any (V17). Measured 141 -> **171** effective lines under eslint's own `max-lines`, against the `src/domain/**` ceiling of 800. The seven instrument pins are at ZERO on this text (V18). |
| `MODIFY` | `src/domain/edit/recordRegister.js` | `CLASS_EXCEPTIONS` — ONE row and its note | **+1** effective (+9 lines, 8 of them comment) | §6's row VERBATIM. ⛔ **The note must state WHICH DECLARATION GOVERNS WHICH ACT** (judgment 245 (i)); A4 proves both acts on one trial. The row is LIVE by A2's own reading and INERT in the node rule (V14, V15). ⛔ **Insert it INSIDE `CLASS_EXCEPTIONS`, after the `repairs` row** — every other table's line numbers shift and V22's drift is measured against exactly that placement. Measured 139 -> **140** effective. |
| `CREATE` | `tests/domain/mergeLadderHeldKeys.test.js` | arms A1-A8 and the `CANNOT-CATCH:` header | `n/a` | ⚠ The action is `CREATE`: §4 measures the path ABSENT at the base. **It adds exactly EIGHT literal `it`s and RE-PINS 0 existing `it`s**, in **ONE literal `describe`** — no `.each`, no loop, no conditional, no nested describe (§P3.4). Derive the 63-row stride from `goldenCorpus()`; commit NO fixture. ⛔ **Never bind `it`, `test` or `describe` as a variable or parameter** (measured 0 re-bindings). ⛔ **Every loop COLLECTS and the arm asserts ONCE** — `seedLoopTotality` finds NO bare seed loop after its own `stripNoise` (V19), and `tests/domain`'s frozen habitat (12 rows) is SHRINK-ONLY and outside this manifest. ⛔ **THE NAME IS A CONTRACT**: `mergeLadderHeldKeys.test.js` matches no `NAME_PATTERN` token, so the mutation-coverage row is NOT owed (T17-14); a spelling carrying `contract`, `census`, `walker`, `golden`, `pin` or `ratchet` would enrol it and owe one, and `*.contract.test.js` would also opt into `contractTestAntiVacuity`. Each arm carries a **300_000 ms** timeout (the `tests/domain/institutionRemoval.test.js` precedent); the whole file measured **81 s** end to end under the planted contract. Lighting posture EXECUTED: `parkReasonsFor -> []`, titles 8, suiteTitles 1, all nine titles unique across 2,697 files (V19). |
| `TEST` | `tests/domain/recordMerge.test.js` | the file HEADER only — its `CANNOT-CATCH` block | `n/a` · **adds exactly ZERO literal `it`s** | ⛔ **EVERY EXISTING TITLE AND EVERY EXISTING ASSERTION IS KEPT BYTE-IDENTICAL.** The edit is documentation: CANNOT-CATCH row 3 gains the sentence that the cross-key escalation arm is exercised in `tests/domain/mergeLadderHeldKeys.test.js` from this landing, and a new row records that `recomputeReceipts` now ALSO restates the receipt's two counted evidence rows, so A7's account of it is not silently stale. The row exists because this is the LANDED arm-holder for both modified `src` files (judgment 148's law) and because §10 seals it. ⛔ **All eight landed arms were REPLAYED IN PLAIN NODE over the planted contract and are GREEN 8/8** (T17-9); nothing in the file reds, so no assertion may be touched to make it pass. No register addresses this file by line (`git grep -nE '<path>:[0-9]'` -> none found). |

**Generated artifacts: `NONE`** — no `checks` command runs a generator, and no `src/` path of this
member is an input to any of the five edge-shared bundle metas.

### Registration obligations, priced

| obligation | owed? | measurement |
|---|---|---|
| sovereignty-lighting census | **YES — an INTERIOR RED, delta only** | ⭐ **`files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +1`**, EXECUTED at `a545899ff` through the walker's OWN `parkReasonsFor` / `liveTitlesIn` / `liveSuiteTitlesIn` over the draft (V19). ⛔ **No absolute tuple is this member's; the census is re-derived whole at the train's terminal, BY THE CHAIR** (§P2.1). The terminal's refreeze is the SUM of every stage's declared delta; this row is only this member's addend. |
| `scripts/mutation-coverage-manifest.json` | ⛔ **NO — REFUSED BY MEASUREMENT** | T17-14. `ENFORCER_DIRS` is eight trees, none of them `tests/domain`; `NAME_PATTERN.test('mergeLadderHeldKeys.test.js')` is **false**; `enumerateInvariants` returns 730 files and does not include the CREATE. A row for it would RED the walker's *"no stale entries"* arm. **This member holds no row on any row-keyed register, so no `rowKey` can collide.** |
| voice mechanics (Tier 2, `src/data` + `src/domain`) | **YES — TWO MODIFIED SCANNED FILES** | **DELTA `em +0 · bang +0` on both**, measured on the planted text with the walker's own literal collector (V18). Neither file carries a baseline row and neither gains one. Tier 5's `src/generators` budget (28 of 28) is untouched: this member modifies no `src/generators` file. |
| tuning register, P2 and P3 | **YES — TWO MODIFIED `src/domain` FILES** | **DELTA `P2 +0 · P3 +0`** on both, measured with `NAMED_NUMERIC_CONST_RE` and `BARE_DECIMAL_RE` themselves (V18). The member lands no number at all. |
| `domainAnyCastBaseline` | **YES — TWO MODIFIED `src/domain` FILES** | **DELTA `any +0 · suppress +0`** on both, measured with `countText` (V18). ⛔ This is the instrument that makes V17 honest: strict-cleanliness bought with `@type {any}` would red HERE instead. Neither file has a baseline row. |
| prose numerics | **NO — and BOTH arms are answered** | Scanner hits `0 -> 0` on both files; the LINE-ADDRESSED arm is separately zero because the baseline holds **0** rows naming either path, so no frozen row sits beneath an insertion (V18). |
| ruin-filter roster · entropy-root census · goods ST-2 roster | **NO** | `READER_RE` over `codeOnly` is `false -> false`; `createPRNG(` is `0 -> 0` with the live pins re-measured at 36 / 47; neither file imports a goods identity half-table (V18). |
| `tests/property/dmLayerGoldenIsolation.test.js :: A8` | **NO ROW OWED, ARM MEASURED** | Its clause-2 regex over both planted files returns **false**: no `src/generators` specifier, static or dynamic (V18). The walk is DYNAMIC, so no manifest row is owed. |
| `tests/lint/heldKeyWriterCensus.walker.test.js` | **NO** | It imports `RECORD_CLASSES` and `SAVED_ONLY_KEYS` (both untouched) and its discovery set is the ASSEMBLY STEP's own body, which this member does not touch. Its `DISCOVERY_EXEMPT` gains no row. |
| observed-shape `EXPLAINED_WRITER_EXEMPTIONS` · `scripts/check-writer-reach.mjs` | **NO** | No `dmLayer` and no `decrees` literal is added; the member adds no reader of a save-time key. |
| `docs/content/wiring-census.json` — BOTH arms | **NO** | ⛔ This member CREATEs no `.js` leaf under `src/domain/**` or `src/generators/**`, so `stamp.producerIndexFiles` is **+0** (1190 at this tip, measured); `stamp.files` and `stamp.candidateLeaves` name NEITHER modified path, so the sha-map arm is unmoved too (V21). The path is in NEITHER §7, NOR the `changeManifest`, NOR `checks`. |
| `path:NNN` citations in other files | ⚠ **DECLARED DRIFT, NO RED** | V22. Three addresses in `recordRegister.js` shift (`:70 -> :79`, `:122 -> :131`, `:175 -> :184`); the walker convicts only PAST-EOF and the file GROWS, so ARM 1 stays green. The four stale `_note`s belong to OTHER packets' rows and to `EM-R0c.md`; §11's slot names the re-address as the chair's, through `tools/cite-drift.mjs`. **This member's own capsule cites by SYMBOL and carries no `path:NNN` address at all** (§P2.22), so judgment 124's self-citation trap cannot fire here. |
| `npm run build:edge-shared` | **NO** | Input membership measured against all five metas' own `inputs` lists: none. |
| generation-worker / lazy-engine / first-paint budgets | **ZERO BYTES in all four, as a MEMBERSHIP fact** | V20. Nothing in `src/` imports `mergeConsequence.js` and this member adds no importer; `EAGER_FIRST_PAINT_MODULES` holds 270 modules and **0** under `src/domain/edit/`. ⛔ The declared delta **`+0 B` per closure** is handed to the train's BYTE-ARM HOLDER for its sum; this member edits no budget test and re-mints nothing. |
| golden posture | **UNCHANGED** | §P3.1; motion is STOP-1. |

---

## 8. Ordered coding sequence

1. **K-GATE, before the first edit.** Confirm at the stamped base: `src/domain/edit/
   mergeConsequence.js`, `src/domain/edit/recordRegister.js`, `src/domain/edit/recordInvariants.js`
   and `src/domain/edit/recordMergeTree.js` all PRESENT; `tests/domain/mergeLadderHeldKeys.test.js`
   ABSENT. Confirm by `grep -cF` -> 1: `export const CLASS_EXCEPTIONS = Object.freeze({`,
   `export const RECORD_CLASSES = Object.freeze({`, `export const ATOMIC_COLLECTIONS = Object.freeze([`,
   `export function recomputeReceipts(merged) {`, `function ladderFor(violation) {` and
   `const KEPT_BY_CLASS = new Set(['HISTORY', 'AUTHORED']);` in `recordMergeTree.js` — that last
   one is what makes the MIRROR row inert in the node rule (V14). If any differs, **STOP**.
2. **RED-FIRST, before any edit.** Write `tests/domain/mergeLadderHeldKeys.test.js` whole and run
   it. ⛔ **EXPECT 0 of 8 ARMS GREEN, each for its own reason** (T17-9 quotes all eight messages);
   read the printed TEST COUNT before claiming the red (§P7). A file that fails to LOAD is not a
   red-first.
3. Apply hunk 3 (`ladderFor`'s filter) and hunk 1 (`CLASS_OF` / `isHeldScope`) to
   `src/domain/edit/mergeConsequence.js`. Re-run: A1, A2, A6 and A7 go GREEN; A3, A4, A5 and A8
   stay red.
4. Apply hunk 2 (the two consts, the JSDoc block, `restateProseCounts`, and its ONE call as the
   first statement of `recomputeReceipts`). Re-run: A3, A5 and A8 go GREEN; A4 stays red.
5. Add the `CLASS_EXCEPTIONS` row and its note to `src/domain/edit/recordRegister.js`. Re-run: A4
   goes GREEN. All eight arms green.
6. Update `tests/domain/recordMerge.test.js`'s HEADER only, and re-run it: **eight titles, all
   green, the printed count unchanged**.
7. Run §10 in order: `npx eslint`, both typechecks, the two focused test files, the goldens, the
   six sealed walkers, the membership probe, `validate`.
8. ⛔ **This member's §8 schedules NO census refreeze and NO wiring-census regeneration.** The
   deltas of §7 are reported to the chair, who re-derives at the terminal (§P2.1, §P2.13).

**Bounded algorithm — the ladder, in words**

```text
1. Build the steps exactly as the landed code does (step 1 group-or-cross-key, step 2 the
   violated path's top-level keys, step 3 every READING key).
2. Map each rung to the SAME rung with its scope filtered: drop a key whose register class,
   taken at the key's TOP-LEVEL segment (split on `.` or `[`), is HELD.
3. Drop a rung whose scope is now empty.
4. Return the surviving rungs, in the same order. Nothing else about the ladder changes.
```

---

## 9. Acceptance matrix

⭐ **THE HOME OF ALL EIGHT CASES IS `tests/domain/mergeLadderHeldKeys.test.js`**, the single
CREATE row of §7, which **adds exactly EIGHT literal `it`s and re-pins exactly 0**; the `TEST` row on
`tests/domain/recordMerge.test.js` adds exactly ZERO literal `it`s and homes **0** cases. The count law therefore
reads `8 declared its === 8 cases homed === the lighting titles delta +8`, and
`EM-R8.count-prover.mjs` executes it over this Markdown and the JSON capsule.

| id | case | required observation | executed at `a545899ff` |
|---|---|---|---|
| **A1** | ⭐ **THE NAMED-PERSON LAW.** Over the 126 power-card trials and the 498-trial census: no rung moves a HELD key (the two declared exceptions removed), and no named person leaves or joins the merged roster. | `heldMoved` empty on the power card and on the census; `gone` and `arrived` empty | **0/126 · 0/498 · gone 0/498 · arrived 0/498.** RED-FIRST at the tip: 5/126, 66/498, 26 lost, 20 arrived, `The High Priestess` named |
| **A2** | **THE ESCALATION CENSUS BY CHANNEL.** The layer channel (378 trials) carries 0 escalations and 0 live non-exempt violations; the config channel (120) escalates E7 31 · E8 17, every one EXHAUSTED, leaving `V-DEFENSE-INST` 31 and `V-EVIDENCE-CONFLICT` 22 INSTANCES across 48 TRIALS. ⛔ The arm asserts the TRIAL count and the per-id INSTANCE table separately; they are different facts and 31 + 22 does not equal 48. | the four lists empty and the two sorted entry tables exact | **378 · 120 · 31/17 escalating trials · 48/498 trials live (53 instances) · `V-EVIDENCE-ROSTER` 0.** Declared as **EM-B2b's seam** |
| **A3** | ⭐ **THE MIRROR.** In every trial the two counted rows equal the merged record's own rosters, and the evidence with EVERY digit masked equals the PURE TREE MERGE's. A PLANTED control (a record whose graph evidence is off by five) is restated by an identity merge. | `wrongGraph`, `wrongEvents` and `otherMoved` empty; the control restated | **498/498 · 498/498 · 498/498**, control cured. RED-FIRST at the tip: the control is not restated |
| **A4** | ⭐ **ONE TRIAL, TWO DECLARATIONS.** `ATOMIC_COLLECTIONS` includes the path and `CLASS_EXCEPTIONS[path]` is `MIRROR`; `receipts.atomicTaken` contains it and the merged row before the post-pass is `R1`'s verbatim; after `recomputeReceipts` the digits are the merged rosters' and the masked text is still `R1`'s. | both declarations read from the register; both acts observed | **`7 NPCs / 12 relationships` (R1's) -> `8 NPCs / 12 relationships` (the merged record's), masked text identical.** RED-FIRST at the tip: `CLASS_EXCEPTIONS[path]` is `undefined` |
| **A5** | ⭐ **THE PROMISE.** `recomputeReceipts` on a record that CARRIES a generation repair restates the planted narrative digit, leaves `.repairs` byte-identical and still returns the fingerprint's own boolean; across the census, `.repairs` moves in NO trial where a rung did not fire, and every trial where it moves is one in which a rung took `generationCoherenceReceipt`. | the unit call, then `noisyRepairs` and `unexplained` empty | **repairs byte-identical across the call · 450/450 quiet · 12/12 explained.** RED-FIRST at the tip: the planted digit is not restated |
| **A6** | **NO RUNG, NO CHANGE.** Where no rung fires, the merged record IS `mergeTree` + `recomputeMirrors` + `recomputeReceipts` of its own inputs, byte for byte; and the ladder's whole blast radius is the 48 config-channel trials A2 measures. | `drifted` empty; the two counts exact | **450/450 · 48.** RED-FIRST at the tip: 432, not 450 |
| **A7** | ⭐ **THE CHAIN.** Determinism 252/252 and undo-then-re-edit 126/126 over the whole stride; over the eight named ordered pairs on five named rows, the four carrying the SHARE edit COMMUTE and the four carrying the SEAT edit still do not. | `nonDeterministic`, `undoDrifted` and `powerPairs` empty; `seatPairs` non-empty | **252/252 · 126/126 · share pairs 8 -> 0 · seat pairs 16 -> 4, all on `town\|germanic\|auto\|random_trade\|civilized\|gm-seed-a`.** RED-FIRST at the tip: the share pairs disagree on two rows |
| **A8** | ⭐ **THE CONSTRUCTED EVENTS CONTROL** (judgment 245 (ii)). `V-EVIDENCE-EVENTS` has no population in the corpus, so the arm plants one: a narrative count off by three, convicted by `recordInvariants` before the merge, restated by it, with nothing outside the receipt moving. | the control live before, cured after, the town otherwise byte-identical | **1 violation -> 0, `8 historical events` -> `5 historical events`, masked text unmoved.** RED-FIRST at the tip: the count stays wrong and the violation stays live. ⭐ **THE COUNTERFORCE** (§P6): the same restatement with `EVENTS_STATED` bent to `/historic events?/` CURES NOTHING — measured, and the build lane plants it as its mutant |

```text
CANNOT-CATCH:
1.  The CONFIG CHANNEL's own repair. E7/E8 drive a world-fact change with a DORMANT layer, so R1
    there is a plain generation of a DIFFERENT town. A2 records what the cure leaves live there;
    EM-B2b owns the seam and this member does not close it.
2.  ⛔ THE LADDER'S RUNG WRITES AT THE TOP-LEVEL KEY, so a declared HISTORY or AUTHORED SUB-PATH
    under a READING key is not protected the way the tree merge protects it: `generationCoherence
    Receipt.repairs` moves in 12 of 120 config-channel trials, before this member and after it
    alike. A5 asserts the SHAPE and no figure of it; the cure is slotted, not taken here.
3.  V-EVIDENCE-EVENTS has NO POPULATION in this corpus (498/498 already agree), so A8 is a
    CONSTRUCTED control with its own counterforce. The five powerStructure-keyed checks and
    V-FLAGVEC's power arm likewise measure ZERO, so §6's transition row about them is reasoning.
4.  The 1,890-pair ordered census and the 498/498 ratchet headline. A7 runs a NAMED five-row
    subset carrying every disagreement the tip produces for its eight pairs; the whole census is
    EM-R7 version 2's, which lands after this member.
5.  The byte price. Nothing in src/ imports mergeConsequence.js; the apply point prices it.
6.  Whether the DM should SEE the EXHAUSTED escalation. The delta card's wording is EM-D*'s.
7.  A frozen caller record. The post-pass writes only the merge's own cloned value; a caller that
    freezes a record whose counts disagree gets a TypeError, unreachable by construction today.
8.  A DM edit outside the eight-edit corpus. The runtime guard is what covers it, by design.
```

---

## 10. Verification commands

```bash
# Focused static checks
npx eslint src/domain/edit/mergeConsequence.js src/domain/edit/recordRegister.js \
  tests/domain/mergeLadderHeldKeys.test.js tests/domain/recordMerge.test.js
npm run typecheck:ratchet          # tsconfig.full.json: measured 0 on both src paths (V17)
npm run typecheck:domain:strict    # a HARD ZERO: measured 0 on both src paths (V17)

# Focused tests: acquire the one test slot in the same command.
# ⚠ tests/domain/mergeLadderHeldKeys.test.js measured 81 s end to end under the planted
#   contract; each arm carries a 300_000 ms timeout.
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/domain/mergeLadderHeldKeys.test.js tests/domain/recordMerge.test.js \
  tests/domain/recordInvariants.test.js tests/domain/dmLayer.test.js \
  tests/domain/tracePartition.test.js

# The GOVERNING SET, computed at this tip (`git grep -l -F` per changed basename and per changed
# export's call shape), minus the lighting walker: tests/generators and tests/lint.
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/generators/rosterFinalUnderHeldRoster.test.js tests/generators/nameHeldThroughAssembly.test.js
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/recordRegisterTotality.walker.test.js tests/lint/heldKeyWriterCensus.walker.test.js \
  tests/lint/tuningRegister.walker.test.js tests/lint/proseNumerics.test.js \
  tests/lint/domainAnyCastBaseline.test.js tests/lint/ruinFilterRoster.walker.test.js \
  tests/lint/entropyRootCensus.walker.test.js tests/lint/seedLoopTotality.walker.test.js \
  tests/lint/negativeAssertionAnchor.walker.test.js tests/lint/sourceCitationIntegrity.walker.test.js
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/copy/voiceMechanics.test.js
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/dmLayerGoldenIsolation.test.js

# Named golden proof: the posture is UNCHANGED and motion is STOP-1
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js

# ⭐ THE MEMBERSHIP PROBE, a standing guard. It imports vite.config.js's own exported set, reads
# no dist and therefore CANNOT SKIP. Exit 1 if either modified leaf has entered the eager closure.
node -e "const{pathToFileURL}=require('node:url');import(pathToFileURL('vite.config.js').href).then(m=>{const S=Array.from(m.EAGER_FIRST_PAINT_MODULES);const bad=['src/domain/edit/mergeConsequence.js','src/domain/edit/recordRegister.js'].filter(p=>S.some(x=>String(x).includes(p)));console.log('EAGER_FIRST_PAINT_MODULES:',S.length,'| this member in the eager closure:',bad.length?bad.join(', '):'NONE');if(bad.length)process.exit(1);})"

# LAST in the sealed set
node scripts/implementation-packets.mjs validate
```

```bash
# PRE-SEAL, from the chair's kit and never from the tree (§P9): the count prover.
node EM-R8.count-prover.mjs EM-R8.md EM-R8.manifest.json

# ⭐ PRE-PLACEMENT, from the kit: THE RE-MEASURE HARNESS. It takes its tree from the CALLER and
# refuses when unset (judgment 247 ruling 4); it imports NO sibling module, reads the capsule
# beside it, excludes this packet's own body from every tree-wide grep (hazard 49), writes
# nothing, runs no test runner and never touches the gate mutex. Exit 0 = every row holds.
bash EM-R8.remeasure.sh <the placement tip's worktree>
EM_PLANT=<dir with the planted mergeConsequence.js + recordRegister.js> \
  bash EM-R8.remeasure.sh <the placement tip's worktree>   # adds the POST-EDIT simulation

# PRE-REPORT, no gate slot: the TypeScript compiler API over THIS member's own src/ paths.
node <scratch>/typecheck-paths.mjs src/domain/edit/mergeConsequence.js src/domain/edit/recordRegister.js
```

⛔ **TWO WALKERS ARE DELIBERATELY ABSENT FROM THE SEALED SET AND RUN ONLY AS §10 INSTRUMENTS:**
`tests/lint/sovereigntyLightingContract.walker.test.js` (it reds BY DESIGN under a train — §7's
interior red) and `tests/lint/proseWiringCensus.walker.test.js`. A guaranteed-red suite inside
`checks` makes `check:packet` unpassable.

```bash
# ⭐ INSTRUMENTS, NOT SEALED CHECKS. Run once each, read by hand, report.
# ⛔ OWED because this member CREATEs a file under tests/: a CREATE under tests/<dir> opts into
#    EVERY walker governing tests/<dir>. Expect EXACTLY ONE red: the lighting census's tuple.
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 tests/lint \
  --exclude=tests/lint/sovereigntyLightingContract.walker.test.js
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/sovereigntyLightingContract.walker.test.js
```

⛔ **No command in the SEALED set writes a file**, so no generated path is owed. ⛔ A gate line with
no printed test count DID NOT RUN (`gate-mutex.sh` gives up after its poll budget and **exits 3**).
Never read a gate through a shell pipe; use `npm run check:tail` or `sh scripts/gate-tail.sh`.
The lane never runs `npm run check`; the full gate moves to the train's terminal.

**The browser suite:** this member touches no `src/components/**` path, no route, no
`data-testid` and no accessible name. **It names no `e2e/` spec.**

---

## 11. Mandatory STOP conditions

- **STOP-1 — a golden moves.** `generatorGoldenMaster` or `dossierProseManifest` shifting by one
  byte is a STOP, not a re-record (§P3.1, HZ-GOLDEN).
- **STOP-2 — A WIDER FILTER.** The filter drops a key whose class is **HELD** and nothing else. A
  lane that widens it to any other class, to a sub-path, or to a named key list is out of contract:
  the filter's whole content is `RECORD_CLASSES`, read at the path's top-level segment.
- **STOP-3 — `.repairs`.** No line of this member may read or write
  `generationCoherenceReceipt.repairs`. THE PROMISE's immutable lived history is that sub-path, and
  A5 is what proves the restatement never reaches it. ⛔ The LADDER's pre-existing top-level write
  that carries it (V10) is **NOT cured here** and a lane that tries is out of contract.
- **STOP-4 — THE CONFIG CHANNEL.** A lane that reaches for the 48 live violations E7/E8 leave
  (`V-DEFENSE-INST` 31 · `V-EVIDENCE-CONFLICT` 22) is in **EM-B2b's** scope, not this one's.
- **STOP-5 — A DEFENSIVE WRITE.** The restatement writes `row.evidence` in place, on the merge's
  own cloned value. A lane that adds a frozen-object guard, a try/catch or a skip is making the
  mirror silently partial, which is the class of defect this member exists to remove.
- **STOP-6 — THE FLIP CONDITION (judgment 245).** If the owner or the chair rules
  `generationCoherenceReceipt.judgments[].evidence` HISTORY rather than a mirrored READING, SHAPE B
  is unlawful: the member is **re-cut to SHAPE A** (the filter alone, the EXHAUSTED escalation
  surfaced on the delta card), not patched. A lane that meets that ruling mid-build STOPS.
- **STOP-7 — EM-R7's FILE.** `tests/property/editCorpusRatchet.test.js` is EM-R7 version 2's, at
  stage 4. A lane that edits or creates it is out of contract.
- **STOP-8 — THE SIXTH EXPORT.** If `restateProseCounts` is exported,
  `tests/domain/recordMerge.test.js :: A8` reds on FIVE versus SIX. Make it module-local; never
  edit A8.
- **STOP-9 — A MOVED INSTRUMENT.** Any of voice mechanics, the tuning register, the any-cast
  baseline, prose numerics, the entropy census or the ruin-filter roster moving off its measured
  ZERO delta is a STOP for the chair, **never a baseline edit** — every one of those baselines is
  outside this manifest.
- **STOP-10 — THE STALE TITLE.** `tests/lint/recordRegisterTotality.walker.test.js :: A2`'s title
  says "the three class exceptions are live" and the table becomes FOUR. Its assertion iterates
  `Object.keys` and stays green (V15). **That file is not in this manifest**; a lane that edits it
  to fix the numeral is out of contract, and the re-word is the chair's slot.
- **STOP-11 — THE CITATION DRIFT.** Three `recordRegister.js` addresses shift (V22) and the
  source-citation walker stays green because it convicts only PAST-EOF. A lane that re-addresses
  another packet's `_note` rows is editing `PACKET_MANIFEST.json`, which is not in this manifest.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into the
next wave.

---

## 12. Completion receipt

The build lane records, each executed and quoted:

1. The CREATE target absent before the first edit, and `git status` clean.
2. ⭐ **THE RED-FIRST**, with its printed TEST COUNT: `tests/domain/mergeLadderHeldKeys.test.js`
   run before any `src` edit, **0 of 8 arms green**, each message quoted beside the arm it belongs
   to (T17-9 has all eight).
3. `npx eslint` clean on all four files; **both** typecheck configurations green with their
   baselines UNMOVED (quote the printed totals).
4. `tests/domain/mergeLadderHeldKeys.test.js` — **eight `it` titles, all green**, with the printed
   test count and the elapsed time; `tests/domain/recordMerge.test.js` — **eight titles, all
   green**, count unchanged.
5. ⭐ **THE BYTE-IDENTITY PLANT-AND-RESTORE:** the pre-change leaf recovered with
   `git show <base>:src/domain/edit/mergeConsequence.js`, the 498-trial census run under it, the
   merged-record shas compared, **432/432 of the non-escalating trials byte-identical**, the leaf
   restored and its sha256 proven equal to the committed one.
6. ⭐ **THE MUTANT (§P6):** `EVENTS_STATED` bent to `/(\d+)(\s+historic events?)/`, A8 red under its
   own title, the exact pre-mutant sha256 restored, focused green re-run.
7. The effective-line count of each modified file under eslint `max-lines`
   `{ skipBlankLines, skipComments }`: expected **171** and **140**, against the `src/domain/**`
   ceiling of 800 and this member's `+31` total.
8. The lighting-census DELTA this member causes — `files +1 · parked +0 · credited +1 · titles +8
   · suiteTitles +1` — restated as an INTERIOR RED for the chair's terminal refreeze, **with no
   absolute tuple**, CONFIRMED against the walker's own `parkReasonsFor` on the landed file (it
   must print `[]`).
8b. **THE SIX ZERO-HELD INSTRUMENTS, RE-MEASURED ON THE LANDED FILES** and quoted: voice
   `em 0 · bang 0` per file; tuning `P2 0 · P3 0` per file; any-cast `{any: 0, suppress: 0}` per
   file; prose-numerics `0` hits per file; entropy `createPRNG(` still 36 / 47; ruin-filter's
   discovery set naming neither file.
8c. **THE WIRING-CENSUS DELTA `stamp.producerIndexFiles +0`**, with the reason (no `src` CREATE).
8d. **THE MEMBERSHIP PROBE's line, quoted with its figure** (`EAGER_FIRST_PAINT_MODULES: <N> |
   this member in the eager closure: NONE`, exit 0) and the declared byte delta **`+0 B` per
   closure**, handed to the train's byte-arm holder.
9. `tests/lint` WHOLE, run once as an instrument, with the printed count and the list of reds.
   **EXACTLY ONE red is expected — the lighting census's interior tuple.** A red from
   `seedLoopTotality` or `negativeAssertionAnchor` is a construction defect of the CREATE, not a
   baseline edit; any other red is reported to the chair before the seal.
10. ⭐ **THE DECLARED BEHAVIOUR SHIFT, STATED PLAINLY** (the honesty law): this member CHANGES THE
    MERGED RECORD in the 66 trials the tip's ladder fired on — 5 power-card trials stop losing a
    person, 61 config-channel trials stop importing another town's roster — and changes nothing in
    the other 432. That is the cure, it is intended, and it is recorded here rather than left to
    be discovered in a diff.
11. **Out-of-scope observations, without investigation:** (a) the ladder's rung writes at the
    top-level key, so `generationCoherenceReceipt.repairs` moves in 12 of 120 config-channel
    trials, before and after this member (V10, CANNOT-CATCH 2) — **slotted to the chair**; (b)
    `recordRegisterTotality.walker.test.js :: A2`'s title says "three" and the table becomes four
    (V15, STOP-10) — **slotted to the chair**; (c) three `_note` addresses in `PACKET_MANIFEST.json`
    and one in `EM-R0c.md` go stale without redding (V22, STOP-11) — **slotted to the chair**;
    (d) the sibling figures this landing moves (V23): EM-R7 version 1's V7, V8 and V9 and this
    member's own A2 / A5 / A6 once EM-B2b lands — **reported to the chair for those re-cuts**,
    and no packet but this one's is edited here.
12. **Deviations:** `NONE` or a STOP. **Judgment calls:** `NONE`.
13. `git show --stat HEAD` naming ONLY this member's four files.
