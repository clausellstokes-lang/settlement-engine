# Settlement editor / EM-R6 — an INSTITUTION gets the rename cascade a faction already has, and a REMOVAL sweeps the same list

- **Status:** `LANDED`
  ⚠ The status value above stands ALONE on its line because `parsePacketHeader`
  (`scripts/implementation-packets.mjs`) anchors the status row at end-of-line.
  > ⭐ **READY-able at `bfca6b7d6`** (train EM-T12's CURED tip) — re-pre-proofed by the Opus
  > PRE-PROOF lane for train EM-T14 (session cce01f87, 2026-09-22). The J-T1 window
  > `141a1d775..bfca6b7d6` carries **164 commits** and is **EMPTY** over every change-manifest
  > path AND every `requiredSymbols` path; all four CREATE targets are absent on disk and unknown
  > to git; all NINE `requiredSymbols` rows resolve verbatim (`1·1·1·1·4·2·1·1·1`); there is no
  > `_pendingRequiredSymbols` block, so judgment 151's discharge owes nothing; `retiredSymbols`
  > is empty by measurement. ⛔ **The status stays DRAFT and the two base fields stay `__BASE__`
  > — the chair promotes and stamps.**
- **Landed at:** `5806628a9699c6670565dfbba76f0c35558e1dd6` — an institution gets the rename cascade a faction already has, and a removal sweeps the same list (version 8: A4 (v) reads every parenthetical literal in servicesGenerator.js with the addCrimeService anchor retained because four sentinels are written by direct pushes — judgment 183; A1 42 exact shapes / 1 catalogue-majority / 4 normalised-majority / 0 undeclared; A2 zero dangling values over 525; the ruin set 94 exact with neither leaf enrolled; 183b the A4 (iv) arm as built; lighting +2/+0/+2/+10/+2); judgments 158 a–c, 183, 189
- **Packet version:** 8
  > **WHAT VERSION 8 CHANGED AND WHY (the Opus BUILD lane for train EM-T14, session cce01f87,
  > 2026-09-22, at its own sealed HEAD `0b9d3ec8d` over base `3aba63857`; ruled by the chair as
  > judgment 183).** ⭐ **ONE ARM'S EXTRACTION RULE AND ONE §5 ROW ARE REFUTED BY THE BUILD, AND
  > NOTHING ELSE MOVES: no contract, no acceptance case, no required symbol, no change row, no
  > budget, no count.** The build STOPPED at §11 rather than improvise, and this version is the
  > chair's cure.
  > **(1) A4 (v)'s EXTRACTION IS RE-CUT TO THE PACKET'S OWN EXECUTED METHOD.** Version 7's arm
  > read every parenthetical literal passed as `addCrimeService`'s THIRD ARGUMENT and required
  > ELEVEN before comparing. Executed at the build's HEAD it yields **SEVEN**:
  > `(arcane underground) (covert) (informal) (lawless) (smuggling) (street gang) (thieves guild)`.
  > The other four are written by a DIFFERENT writer shape in the same file and are unreachable
  > from a third-argument read. The arm now keeps its anti-vacuity half UNCHANGED (the anchor
  > `addCrimeService = (name, desc, institution) =>` is asserted FOUND first, so a silent source
  > scan still refuses) and then extracts every parenthetical string literal IN THE FILE, which is
  > exactly what §5 and §6 always executed (`grep -oE "'\([^']*\)'" | sort -u`). Re-executed by
  > the build lane on that rule: **11 extracted, 0 generator-only, 0 list-only, SET-EQUAL both
  > directions**.
  > **(2) §5's PRODUCER ROW IS REFUTED AND RE-WORDED.** It called `addCrimeService` *"the ONLY
  > writer of a parenthetical provenance sentinel"*. Measured at `3aba63857`: the file carries
  > **23** parenthetical-literal occurrences and **14 of them stand before the declaration**,
  > written by the direct `buckets.criminal.push({ ...item, institution: '(…)' })` shape. The row
  > now names both writer shapes and cites the producer BY SYMBOL rather than by line (§P2.22, the
  > sixth amendment's S8).
  > **(3) §11's STOP SENTENCE FOLLOWS THE ARM** — *"extracts fewer than eleven literals"* now
  > reads over the FILE-WIDE extraction, which is the only reading under which the sentence can
  > convict a real defect instead of a writer shape.
  > **(4) §12 RECORDS A4 (iv)'s LIVE/DARK SPLIT AS A 525-ROW EXECUTED FIGURE, NOT AN ARM PIN
  > (judgment 183b).** `(covert)` occurs **twice in the whole 525-row corpus**, so a 63-row
  > structured sample cannot hold a 3 / 8 pin by arithmetic. The arm keeps FIX-D5's CONTROL at
  > full strength (**0** parenthetical values outside the declared eleven, which is the half that
  > convicts an undeclared spelling), asserts live + dark accounts for every declared spelling, and
  > carries a RAISE-ONLY floor of 2 live; the 3 / 8 split is an executed receipt figure.
  > ⛔ **THE COUNT PROVER'S OWN VERSION PIN MOVES TO 8**, and the §7 sub-heading that opens
  > *"VERSION 7, THE SIXTH AMENDMENT'S INSTRUMENT LIST"* is DELIBERATELY LEFT WORD-FOR-WORD ALONE:
  > it records what version 7 added AND is the slice boundary the prover reads for `## 7`, so
  > re-wording it would silently empty the §7 parse. ⚠ For the same reason this block does not
  > reproduce that heading's own prefix anywhere: a SECOND copy of the needle would move the
  > prover's `indexOf` to the version history and empty the parse just as surely as a rename.
  > No §-count line moves in this version.
  > **WHAT VERSION 7 CHANGED AND WHY (the Opus PRE-PROOF lane for train EM-T14, session
  > cce01f87, 2026-09-22, read tip `bfca6b7d6`).** ⭐ **NOT ONE FACT, ARM, ACCEPTANCE CASE,
  > REQUIRED SYMBOL OR CHANGE ROW OF VERSION 6 IS REFUTED** — the J-T1 window
  > `141a1d775..bfca6b7d6` (164 commits, trains EM-T8–T12, twenty landed packets) is EMPTY over
  > every declared path, and every `path:line` this packet cites was re-read at the tip and still
  > names its subject. Version 7 is that packet re-measured against the SIXTH AMENDMENT and the
  > landed code of EM-C4a and EM-B1c1. Six changes, no contract among them: **(1) THE SIXTH
  > AMENDMENT'S INSTRUMENT LIST (§P2.17 · §P2.19 · §P2.20 · §P2.21) IS PRICED IN §7** — five new
  > registration rows, every figure EXECUTED at the tip as a DELTA: the tuning inventory (new
  > leaves at ZERO, the library's own counters run), the voice walker (Tier 2 AND Tier 4 scan
  > `src/data` + `src/domain`; a new file is held at ZERO; this packet's spelled literals carry
  > **0 em dashes and 0 exclamation points**), the reviewed-set pin (NOT OWED — no application
  > command, no store edit), the entropy census's two arms (**36** under `src/domain`, **47**
  > under `src/` whole, delta **+0 / +0**), and the ruin-filter roster (discovery set **94**,
  > reproduced by the walker's own two regexes; the disposition DECLARED). **(2) §6 SPELLS THE
  > VOICE RULE** the build lane may not invent (judgment 135). **(3) THE LANDED CASCADE SHAPE IS
  > MEASURED INTO §5** — `CASCADE_WRITERS` / `CASCADE_DISPATCH` / `applyCascadeEdit`, and the
  > named exception this packet already carries in `tests/domain/editDeclarations.test.js:193`.
  > **(4) JUDGMENT 124 IS MET ON THIS PACKET'S OWN ROWS**: the MODIFY inserts a comment block
  > ABOVE `factionRename.js:288` and `:289`, which two of its own `requiredSymbols` `_note`s
  > addressed — the two bare addresses are STRUCK and the rows keep their symbol claims.
  > **(5) THE HEADER'S INTERIM-RULES CLAUSE IS REPLACED** by the sixth-amendment sentence
  > (substitution (c)). **(6) FOUR STALE ABSOLUTES REFRESHED** — the registered manifest's entry
  > count (**193 → 215**), `stamp.producerIndexFiles` (**1,171 → 1,181**), the `tests/lint` file
  > count (**177 → 182**) and `ruinFilterRoster.walker.test.js`'s own line (**:159 → :160**).
  - ⭐ Version 6 is the DOCS repair only (ESTATE-REPAIR-4, 2026-09-20, read tip
    `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`): §10's account of `gate-mutex.sh` is corrected — it
    gives up after its poll budget and **exits 3** (`GAVE UP`), NOT 0. The chair retracted the
    "exits 0" claim on 2026-09-20 after checking three versions of the script; the 09-15 note that
    stated it was wrong. ⛔ **The operational instruction is UNCHANGED and still correct** — a gate
    line with no printed test count DID NOT RUN — so no command, budget, contract, acceptance case,
    required symbol or change row moved.
  > **WHAT VERSION 5 CHANGED AND WHY (ESTATE-REPAIR-3, an Opus COMPILE seat, 2026-09-20, read tip
  > `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`; DOCS ONLY).** Version 5 brings this packet under `COMPILE-RULES.interim.md`, the
  > fifth amendment's eleven rules obeyed before they land. Five changes, no contract among them:
  > **(1)** the sealed `checks` row that ran the `tests/lint` DIRECTORY **bare** — so `check:packet`
  > could never pass, the lighting walker being inside a run this member's title delta reds — is
  > replaced by the ELEVEN GOVERNING FILES, which is the lawful cut §7 and P4 had already
  > enumerated and asked the chair for; the excluded directory run and the lighting walker ALONE
  > become §10 INSTRUMENTS (interim rule 8). **(2)** §7's STAMPED-FILES row, which answered the
  > `stamp.files` sha-map arm and was SILENT on `stamp.producerIndexFiles` while this manifest
  > CREATEs two `.js` leaves under `src/domain/**`, gains the DEFERRED ROW (interim rule 1).
  > **(3)** §3b's binding note is RE-AIMED from EM-B1c / EM-B1a / EM-R0c to the APPLY POINT
  > (the chair's ruling on ESTATE-REPAIR-2's NOTE-2). **(4)** the live `268` absolutes become
  > measurements with as-of marks, as does the `172` lint-file count and the stamped preamble
  > hash (interim rule 9). **(5)** the interim-rules header row is added. ⛔ **NOT ONE FACT, ARM,
  > ACCEPTANCE CASE, REQUIRED SYMBOL, CHANGE ROW OR BUDGET OF VERSION 4 IS REFUTED OR MOVED.**
  > **WHAT VERSION 4 CHANGED AND WHY (the Opus PRE-PROOF lane, session a9df403c, 2026-09-20
  > ~07:2x–08:0x EDT, read tip `141a1d775`).** Version 3.1 was ACCEPTED (ODQ §934.47 addendum 37).
  > Version 4 is that packet **re-measured whole at `141a1d775`** plus the two arms the chair
  > FATED TO THIS PRE-PROOF in that same addendum and in the charter's amendments of 2026-09-20
  > 02:46 EDT. ⭐ **NOT ONE FACT OF VERSION 3.1 IS REFUTED**: the J-T1 window
  > `32602dc60..141a1d775` over every change-manifest path, every `requiredSymbols` path, both
  > test precedents and every file §5 names prints **NOTHING**, so every corpus figure, every
  > producer measurement and every closure figure stands byte-for-byte. **(1) THE TWO FATED ARMS
  > ARE ADDED** — A1 (vi) pins `LABEL_REWRITE` as a CLOSED VOCABULARY OF TWO in `OrphanKind`'s
  > exact shape, and A4 (v) is the SET-EQUALITY arm joining `servicesGenerator.js`'s own
  > parenthetical literals to `SERVICE_PROVENANCE_SENTINELS` — the join whose absence is
  > **exactly how the twelfth spelling survived a whole version**. Both are ARMS, so acceptance
  > stays **8 of ≤8**, and both cost **ZERO effective production lines** (the vocabulary is a
  > JSDoc typedef over the frozen map that already exists; `max-lines` for `src/domain/**` is
  > `{ max: 800, skipBlankLines: true, skipComments: true }`, executed). **(2) THREE INSTRUMENT
  > GAPS, MEASURED AND CLOSED IN §7 AND §10:** `tests/lint` WHOLE is owed by the 2026-09-20
  > addendum (a CREATE under `tests/<dir>` opts into every walker governing it) and version 3.1
  > named ONE lint walker of the **172 measured at `141a1d775`** (⚠ **177** at `bdbf7c89c` — the count is a measurement, not a constant); `seedLoopTotality.walker` is SHRINK-ONLY
  > and FORBIDS A NEW FILE TAKING A ROW, so the corpus walks carry a declared idiom; and
  > `goldenFreeze.walker` enrols any test reading `process.env.UPDATE_*`, so A8 is fenced.
  > **(3) FOUR STALE ABSOLUTES REFRESHED** — the preamble SHA-256 (the second amendment landed at
  > `e5f53ae95` in this window), the registered manifest's entry count (**190 → 193**), the byte
  > delta (§0's **+482 B is refuted by the packet's own table: it is +461 B**), and three
  > path/line citations (`rulingStructure.js` is under `src/generators/power/`;
  > `inferInstitutions` is `:393`; `'(background crime)'` never existed as a literal).
  > **(4) ONE CLAIM CORRECTED, ONE REGISTER NAMED**: the sentinel eleven is re-executed with a
  > WIDENED character class that could have found a twelfth and did not; and
  > `scripts/.observed-shape-readers-baseline.json` — which names `factionRename.js` and was
  > outside version 3.1's fourteen-baseline sweep — is measured HARMLESS by its own doc.
  >
  > **WHAT VERSION 3.1 CHANGED AND WHY (kept, because version 4 rests on it).** Version 3 was ACCEPTED; 3.1 folds in the chair's nine
  > rulings (ODQ §934.47 addendum 34) and the four small additions, each **re-measured at the same
  > tip `32602dc60`**. Nothing of version 3's substance is reopened. **(1) Q1 — the rewrite form is
  > now DECLARED, not hard-coded**: each `label` row carries `rewrite: 'verbatim' | 'lower'` and
  > the cascade reads it from the row; an `exact` row always writes verbatim, and a REFERENCE is
  > never case-folded. **(2) Q4 — FIX-D5 RIDES HERE.** The dangle denominator excludes a sentinel
  > **by the declared eleven-spelling list, never by `startsWith('(')`**, and ⭐ **the control says
  > the swap is behaviour-identical: ZERO parenthetical values over 525 rows fall outside the
  > eleven**, so the exclusion is now falsifiable instead of shape-guessed. A4 gains the live/dark
  > pin (**3 live · 8 dark** — ⚠ the ruling said "twelve … 3 live, 9 dark"; §6 carries the correction). Priced: **+5 effective lines, +245 B**, re-measured on this leaf.
  > **(3) Q5 — `NON_CASCADED_SURFACES` rows gain `readable: true | false`**, exactly one row is
  > `true`, and the removal **resolves that path from the ledger** rather than spelling it, so
  > flipping the flag throws at import instead of reading on. The rule — *"never touched" means
  > never WRITTEN* — is written into `factionRename.js`'s ledger header, which **widens the MODIFY
  > from one token to one token plus a comment block** (+0 effective; the line-shift hazards are
  > re-swept clean in §7). **(4) ⛔ ONE VERSION-3 CLAIM IS REFUTED BY THE NEW MEASUREMENT:** the
  > two `compound.inst.names[]` paths are **NOT an alias**. `sameObjectLive` is **0 of 525** —
  > `economicState.js:62` and `safetyProfile.js:26` each call `getInstFlags` independently, so they
  > are two equal-valued stamps, not one object seen twice. An `alias` field would have asserted a
  > falsehood; item 12 therefore takes **the failure-message sentence**, and the row's `why` is
  > corrected. **(5)** `availableServices.legal[].name` is re-measured at this tip (version 1's
  > figure was three versions old) and asserted directly in an A6 arm; A1's failure message gains
  > the `catalogId`-threshold sentence; and the 231-of-280 coverage line now sits wherever the
  > packet asserts corpus totality.
  >
  > **WHAT VERSION 3 CHANGED AND WHY (kept, because 3.1 rests on it).** Re-compiled under ODQ §934.61 and the charter's
  > amendments of 2026-09-19 20:34 EDT, and re-measured whole at `32602dc60` (the cure lane's
  > close). **The J-T1 window over every declared path is EMPTY**, so no fact of version 2 about
  > the tree was refuted — but the *classification* of four of its cascade rows was, by
  > measurement. **(1) A NAME IN THE RECORD IS ONE OF FOUR KINDS.** Version 2 cascaded four paths
  > that are not references at all: `economicState.activeChains[].processingInstitutions[]` is a
  > MATCHED PATTERN and `resourceAnalysis.resourceChains[]` plus two `exploitation` arms are a
  > CATALOGUE VOCABULARY. All four leave the cascade and join `NON_CASCADED` with written reasons;
  > three sibling catalogue paths version 2 never saw (`exploitation.unexploited[]`,
  > `gaps[].missing[]`, `upstreamMissing[]`) are declared beside them. **(2) THE DENOMINATOR PIN
  > GAINS TWO ARMS** — a CATALOGUE-NAME arm and a NORMALISED-NAME arm — because the exact-name walk
  > was blind by construction to every path whose values never equal a roster name. **(3) THE ARMS
  > EARNED THEIR PLACE ON ARRIVAL: they found FOUR paths that neither version 2 nor FIX-D1's own
  > measurement had seen** — `economicState.institutionalServices[].institutions[]` (1,764 values,
  > reader-visible on Economics › Service economy, **a second derived display label**),
  > `economicState.compound.inst.names[]` and its alias under `safetyProfile` (17,361 values each,
  > a frozen lowercased stamp), and `structuralSuggestions[].suggested[]` (authored advice).
  > **(4) THE BASELINE DANGLE COUNT IS NOT 7,399 — IT IS ZERO.** Once the four mis-classified paths
  > leave the reference set and the eleven parenthetical sentinels are declared, the true reference
  > set carries **0 dangling institution joins over all 525 rows before any edit**. **(5) THE MATCH
  > RULE IS NOW TWO KINDS**: `exact` on 34 rows and a DECLARED `label` (case-folded) on the two
  > display-label rows only — and the `"Thieves' Guild"` case near-miss is no longer an accepted
  > cost but an **executed pin**: renaming `Thieves' guild chapter` leaves all 976 faction handles
  > untouched on **156 of 156** rows that carry both. **(6) VERSION 2'S FIGURE OF 88 IS REFUTED**
  > (measured **488 per home / 976 / 327 rows**; ODQ §934.61). Everything the chair ruled for
  > version 2 that §934.61 does not touch stands: the polysemous exact-match guards, the two-home
  > write, both `OrphanKind` spellings, the symmetry declaration, and §3a's conditional anchor.
- **Verified base:** `em-t14-r6-2026-09-22` at `35a031ac7adf2a6614feb48036a6bfddb5923fd9`
  > **THE REVALIDATION SENTENCE THE CHAIR STAMPS.** *Re-measured at `bfca6b7d6` (train EM-T12's
  > cured tip) by the Opus PRE-PROOF lane for train EM-T14 (session cce01f87, 2026-09-22
  > ~13:1x–14:0x EDT). `32602dc60`, `141a1d775` and `bdbf7c89c` are all ancestors; the window
  > `141a1d775..bfca6b7d6` carries **164 commits** (trains EM-T8 through EM-T12, twenty landed
  > packets), and over every change-manifest path and every `requiredSymbols` path
  > `git diff --stat 141a1d775 bfca6b7d6 -- <paths>` printed **NOTHING** — as did the same window
  > from `bdbf7c89c` (93 commits). All four CREATE targets are absent on disk AND unknown to git;
  > all **nine** `requiredSymbols` rows resolve verbatim (counts 1·1·1·1·4·2·1·1·1); every
  > `path:line` the packet and its `_note`s cite was re-read at the tip and still names its
  > subject. The read tree's `git status --short` was EMPTY at the start and at the end.
  > Evidence §1–§14 (compile), §15–§28 (the 2026-09-20 pre-proof) and §29–§36 (this one).*
- **Last revalidated:** `35a031ac7adf2a6614feb48036a6bfddb5923fd9` — stamped by the chair at promotion, the window re-run in the same command as the stamp.
- **Depends on:** `NONE`.
  ⭐ **THIS PACKET FLOATS.** It CREATEs two leaves nothing imports yet and adds one `export` token
  to a third file. **PLACEMENT (the chair, R7): EM-R6 lands BEFORE EM-B1c, in the group after
  train EM-T6 — `EM-R6 → EM-B1c · EM-B1b · EM-B1g · EM-A3`.**
  ⛔ **EM-B1c CANNOT BE PROMOTED BEFORE THIS LANDS** — design §22.1 ruling 7 says so and the
  measurement says why: today a rename leaves the worst case's handles stale and nothing throws.
  ⚠ **FIX-D2 and FIX-D3 are owner-signed and would change two rows of this packet** (§6.1); the
  packet is written to be correct before and after either signature.
- **Collision group:** `NONE` — ⭐ **re-measured against the live registered manifest at
  `bfca6b7d6`: 215 entries** (version 4 said 193 at `141a1d775`, version 3.1 said 190 — the count
  is a MEASUREMENT, never a constant), **of which ZERO are non-terminal**, and NO entry at any
  status names any of this packet's five change paths (executed: a `node` join of every
  registered `changeManifest` row against this packet's path set returned `NONE`). ⛔ **AND THE
  TRAIN-EM-T14 / EM-T13 SWEEP, WHICH THE REGISTERED MANIFEST CANNOT SEE:** of the six waiting
  siblings read in `packets-waiting/` — EM-R0d v7, EM-R0f v2 (EM-T14) and EM-D0e v1.1, EM-B1b v6,
  EM-B2a4 v2, EM-A3 (EM-T13) — **NONE intersects a change path of this packet and NONE names a
  `requiredSymbols` path of it**; the ONE cross-reference is that **EM-A3 holds four
  `requiredSymbols` rows on `src/domain/factionRename.js`** (`export const FACTION_RENAME_SURFACES`
  · `NPC_RENAME_SURFACES` · `NON_CASCADED_SURFACES` · `NPC_NON_CASCADED_SURFACES`), all four of
  which this packet's MODIFY leaves byte-identical (it prepends `export ` at `:168` and inserts a
  COMMENT block in the ledger header), and **none of the four carries a `_note`**, so no sibling
  address is staled. ⚠ **EM-R0d v7 reserves `tests/copy/voiceMechanics.test.js` and EM-B2a4 v2
  reserves `tests/build/vendorPdfLazy.test.js`** — which is why §7 prices both instruments as
  MEASUREMENTS and names NEITHER as a change row (two members naming one non-row-keyed path refuse
  at placement). No entry, at any status, names `src/domain/institutionRename.js`,
  `src/domain/institutionRemoval.js`, `tests/domain/institutionRename.test.js`,
  `tests/domain/institutionRemoval.test.js`, `src/domain/factionRename.js`, `src/domain/clone.js`,
  `src/generators/safetyProfile.js`, `src/generators/computeActiveChains.js` or
  `src/generators/priorityHelpers.js` **or `src/generators/servicesGenerator.js`** (version 4's new
  `requiredSymbols` row) in a `changeManifest` or in `requiredSymbols` (evidence §9, §22).
  ⚠ Registered entries only; the chair runs its own create/modify order check across the kit's
  DRAFTs at placement (R7).
  ⚠ One shared path with whichever train this joins: `tests/lint/.lighting-census-baseline.json`
  (that train's terminal act; never this packet's — §P2 row 1 as CORRECTED).
  ⚠ **FIX-D2 names `src/generators/safetyProfile.js:610` and FIX-D3 names
  `src/generators/computeActiveChains.js:381`** — both are `requiredSymbols` rows here, so if
  either is chartered into the same train the chair sequences them (§6.1).
- **Commit authority:** edits only; the chair commits.
- **Baseline posture:** measured at `32602dc60`, **re-measured and re-confirmed at `141a1d775`
  by the pre-proof through an EMPTY window over every path** (so no corpus figure was re-run;
  the tree that produced them is byte-identical). Executed by the compile lane: the **525-row** rename and removal arms
  **through the priced candidate source itself**; the three-arm denominator pin with its
  version-2 RED control; the member-mirror control pair; the two label arms; the faction-handle
  pin; the sentinel live/dark census; the immutable-form comparison; the placement walk two ways;
  an effective-line counter with a **6/6 EXACT** control against `scripts/.size-baseline.json`;
  the minified byte price with esbuild 0.28.1. ⛔ **NO CENSUS ABSOLUTE IS QUOTED** — only the
  DELTA this packet causes (§7). No test was run by this lane.
- **Preamble:** docs/implementation/preambles/EM-PREAMBLE.md (SHA-256: TO BE STAMPED BY THE CHAIR — measured at the read tip, never quoted as a literal in this packet)
- **Amendment posture:** ⭐ **THIS PACKET IS WRITTEN TO THE SIXTH AMENDMENT AND RE-STAMPED AT
  PLACEMENT.** The preamble MEASURED at this packet's read tip `bfca6b7d6` is the FIFTH amendment
  (`shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md` →
  `fdecd426828665cad2dfaf872871f29f46fa4816e1bd4c43c20ccd84dacc9e7c`, executed); version 7 is
  measured against the SIXTH as the chair selected it (`drafts/amendment-6/EM-PREAMBLE.amended.md`,
  judgment 144), which lands as the FIRST commit of train EM-T13's branch — one train before this
  member's — so the chair re-stamps this row's hash from the live text at promotion. ⛔ **THE
  INTERIM SHEET IS GONE:** `COMPILE-RULES.interim.md` is SUPERSEDED BY THE PREAMBLE'S OWN TEXT
  (the sixth amendment's opening), so this packet carries no interim row and no clause saying
  which of the two governs where they disagree — version 6 carried both and version 7 strikes
  them.
  > ⛔ **RE-MEASURED BY THE PRE-PROOF AT `141a1d775` — THE COMPILE LANE'S HASH IS STALE.**
  > `shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md` →
  > **`16dfb96fa79320abc7dcfbdd8f5152b6aa66998c269287a951079df6c8223195`** (64 hex, executed).
  > Version 3.1 quoted `b90a95b7…caa5e1`, correct at `32602dc60` and **superseded inside this
  > window** by `e5bdfd031`… `e5f53ae95` — *"EM-PREAMBLE: the second amendment — §P2 row 11 gains
  > the sentence that a skipped `skipIf(!requireDistRead)` byte arm is not a pass"*. ⭐ **THE
  > AMENDMENT IS PRICED, NOT MERELY NOTED:** §P2 row 11 now also requires first-paint membership
  > to be measured **BY IMPORTING** `EAGER_FIRST_PAINT_MODULES`, never by reading a list — done in
  > §3b **by IMPORTING the set, its size MEASURED AT THE BASE and never quoted** (interim rule 9;
  > as history with an as-of mark it read **268** at `32602dc60` and **269** at `bdbf7c89c`) — and
  > it makes a SKIPPED byte arm not a pass, which this packet cannot trip because it carries
  > **no byte arm and no budgeted chunk** (§7). Left unstamped by design; ⛔ **the literal
  > `16dfb96f…3195` this note told the chair to stamp is WITHDRAWN by ESTATE-REPAIR-3 as STALE**
  > (the live text hashes `c9f33c8d…6675` at `bdbf7c89c`, and the fifth amendment moves it again).
  > The chair stamps what it MEASURES at the promotion tip.

---

## 0. The shape: ONE PACKET, TWO LEAVES (the chair's R5)

⛔ **ONE LEAF DOES NOT FIT.** Re-measured under version 3's lists, with a counter that reproduces
eslint's `max-lines` arithmetic **6/6 EXACTLY** against `scripts/.size-baseline.json`:

| leaf | effective (v2 → v3 → **v3.1**) | minified (v2 → v3 → **v3.1**) | holds |
|---|---:|---:|---|
| `src/domain/institutionRename.js` | 219 → 215 → **217** | 6,999 → 10,173 → **10,598 B** | the declared 36-row surface list with its two match kinds and its two declared REWRITE FORMS, the 15-row `NON_CASCADED` ledger with its `readable` field, the eleven-spelling sentinel vocabulary, the shared primitives (exported), the rename cascade, its immutable form |
| `src/domain/institutionRemoval.js` | 65 → 73 → **74** | 2,088 → 2,624 → **2,660 B** | the removal sweep over the SAME list, the two-kind orphan report whose readable path is RESOLVED FROM THE LEDGER, its immutable form |
| **total** | 284 → 288 → **291** of 400 (**292** with the MODIFY line) | 9,087 → 12,797 → **13,258 B** | 2 of the allowed 2 new logic leaves; both under 250 |

Version 3.1 costs **+3 effective lines** (Q1's declared rewrite map and lookup, +2; Q5's
ledger-resolved readable path, +1) and ⛔ **+461 bytes, NOT +482** (the `readable` and `rewrite`
fields across 26 rows). **THE PACKET'S OWN TABLE REFUTES BOTH FIGURES ON THE RECORD**, and the
arithmetic is one line: rename `10,173 → 10,598 = +425`; removal `2,624 → 2,660 = +36`; total
`12,797 → 13,258 = +461`. Version 3.1's prose said **+482** (unsourced); ODQ §934.47 addendum 37
said **+425**, which is the RENAME LEAF ALONE quoted as the packet's total. ⭐ **+461 B is the
packet total and +425 B is the rename leaf** — offered to the chair as a correction to
addendum 37's own figure (question P2). ⭐ **VERSION 4 ADDS ZERO EFFECTIVE LINES AND ZERO BYTES TO
PRODUCTION**: its two fated arms are TEST arms, and `LABEL_REWRITE` is a JSDoc typedef over the
frozen rewrite map version 3.1 already declares — `max-lines` for `src/domain/**` is
`['error', { max: 800, skipBlankLines: true, skipComments: true }]` (executed at the tip), so a
comment is 0 effective. The leaves stay **217 / 74** and **13,258 B**. ⭐ **FIX-D5's own price, re-measured on THIS leaf: +5 effective lines and +245 B**
(the same leaf with the declared list removed measures 212 effective / 10,353 B) — exactly the
figures the chair named, confirmed at the new shape.

The leaf shrank by four effective lines against version 2 and grew by 3,620 minified bytes at the
same time, and both movements have one cause: four walk bodies left the module while fifteen
`NON_CASCADED` rulings and three declarative columns entered it. **Prose is cheap in lines and
expensive in bytes**, and the byte figure is the one §3b's binding note is about.

The removal leaf imports the vocabulary, the surface list and every primitive from the rename
leaf. **Nothing is declared twice.**

⭐ **THE CONSUMER MAP IS KEPT EXACTLY AS MEASURED, so the chair can still cut the packet in two:**

| consumer | waits on |
|---|---|
| **EM-B1c** — the `rename-*` ops (§12.3) | **RENAME** only |
| **EM-B1a** — `set-institution-state`, the `removed` state | **REMOVAL** |
| **EM-R0c** — THE MERGE (design §22.3 ruling 10) | **REMOVAL** |
| **EM-R0b** — `recordInvariants`' referential checks | neither (it gates the removal; it does not import it) |

---

## 1. Reconciled authority

1. ⭐ **Design §22.1 ruling 7** — *"Renames never go through re-derivation … INSTITUTIONS GET A
   CASCADE: a declared institution rename-surface list, measured from the reference graph, is its
   own packet before any institution rename op is promoted. The 'no new dangling reference' gate
   is a delta against the record's own pre-existing dangles."*
2. **Design §22.3 item 10** (via §22.2 item 10) — *"A REMOVAL OWES THE SAME CASCADE A RENAME
   DOES … The institution rename-surface list (EM-R6) is also the list a REMOVAL sweeps, and
   `recordInvariants`' referential checks gate it."*
3. **Design §22.2 item 7** — the MIRROR class. ⛔ **A RENAME IS NOT A MERGE** (ruling 7), so no
   mirror recompute runs for a rename and the cascade must write both homes itself. §6 states the
   composition; A3 executes it with a negative control.
4. **Design §20 ruling 2** — a `free-cascade` field applies ON SAVE even on canon: *"the rename
   runs the existing cascade and moves no world fact."* This packet is that cascade.
5. **Design §22.3 item 1** — the generation receipt is classed **BY PATH**: what it says about the
   ACT of generation is HISTORY, never merged, never recomputed. It governs
   `generationCoherenceReceipt.repairs[].subject`.
6. ⭐ **ODQ §934.61 AND THE CHARTER'S AMENDMENTS OF 2026-09-19 20:34 EDT — THE FOUR KINDS.** A
   string equal to an institution's name is **a REFERENCE** (follows a rename and a removal,
   exact and NON-case-folded) · **a CATALOGUE VOCABULARY** (what a chain NEEDS, authored; never
   touched by either arm) · **a MATCHED PATTERN** (`activeChains[].processingInstitutions[]`;
   NON_CASCADED until the owner signs FIX-D3) · **a DERIVED DISPLAY LABEL** (follows a rename by a
   DECLARED case-folded match on the declared label paths only, until the owner signs FIX-D2). The
   denominator pin gains a catalogue-name arm and a normalised-name arm.
7. ⭐ **THE CHAIR'S RULINGS ON VERSIONS 1 AND 2 (R1–R7, R9) — everything §934.61 does not touch
   stands.** R5 one packet two leaves; R2 export `NPC_HOMES`; R3 the three polysemous paths
   cascaded under the exact-match guard, with the disjointness made a walker ARM; R1 the
   dynamic-import obligation falls on the consumers; R6 the orphan report belongs here and this
   writer cannot refuse; **both `OrphanKind` spellings accepted**; R9 the never-observed member
   `linkedInstitutionIds[]` row is a symmetry declaration; R4 §3a stays conditional (ODQ §934.59);
   R7 placement before EM-B1c.
8. **`EM-PREAMBLE.md`** (SHA-256 verified) §P2 rows 1–4 and 10–12, §P3, §P4, §P5 (HZ-JOINKEY,
   HZ-GOLDEN), §P6, §P7, §P8 — cited by hash, not restated.
9. **`src/domain/factionRename.js`** — THE ARCHITECTURE THIS COPIES, at `32602dc60`.
10. Live code and the executed 525-row corpus at `32602dc60`.

**Resolved contradictions:**

- *Version 2's 38-row cascade* → four rows are **not references**, by execution and by their
  producers' own headers. **Closed by ODQ §934.61**; §5's reconciliation table carries the arithmetic.
- *Version 2's "the pre-existing dangle baseline is 7,399, thorp median 11 … city 23"* →
  **REFUTED.** Those counts were taken over a path set that included the four mis-classified paths
  and counted the eleven parenthetical sentinels as names. Over the TRUE reference set the
  baseline is **0 over 525 rows** (evidence §3).
- *Version 2's `"Thieves' Guild"` appears 88 times beside the catalogue institution
  `"Thieves' guild"`* → **REFUTED TWICE.** There is no catalogue institution of that name, and the
  figure is **488 per home / 976 / 327 rows**. It is a power-structure FACTION handle
  (`rulingStructure.js:671`), already on the faction cascade. §6's match rule is CORRECT and a
  case-folding cure there would be a **live bug**. **Closed by ODQ §934.61; now an executed pin (A6 v).**
- *Version 2's "sixteen `tests/lint/.*-baseline.json` files"* → **14 at this tip**, all still 0 for
  `factionRename.js` (evidence §11).
- *Version 2's "`reloaded()` at `tests/domain/factionRename.test.js:172`"* → the symbol is at
  **`:76`**; the file did not move in the window, so version 2's line was wrong when written. Every
  row of §5 below is re-found **by symbol**.
- *Version 2's "the key `orphaned:` is absent from `src/` and `tests/` entirely"* → **2
  occurrences**, both inside comment prose in unrelated test files
  (`tests/domain/institutionStatusLifecycle.test.js:10`, `tests/domain/razingWitnessWr8.test.js:185`).
  No code key; the contract is unaffected. Corrected for the record.

The implementer does not read other documents to reinterpret this packet.

---

## 2. Outcome

**Observable result:** `institutionRenameChanges(settlement, oldName, newName)` and
`institutionRemovalChanges(settlement, name)` exist as pure domain functions over ONE declared
surface list, so renaming or removing an institution rewrites or sweeps **every stored handle to
it**, at both of a character's homes, adding **zero new dangling joins of any kind** — and touches
**nothing that is not a handle**.

**Definition of done:** one frozen `INSTITUTION_RENAME_SURFACES` with a `match`, a `removal` and a
`why` per row; one frozen `NON_CASCADED_SURFACES` with a `kind` and a written reason per row; the
closed sentinel vocabulary; an in-place and an immutable form of each operation; a **three-arm**
totality walker; and the executed rename and removal arms on real pipeline records after a
save→load round trip.

### ⛔ THE DEFECT, REPRODUCED AT THIS TIP BEFORE ANY FIX

```
$ git grep -rn "renameInstitution|applyInstitutionRename|institutionRenameChanges|INSTITUTION_RENAME" -- src tests
(no output)
```

**No institution rename or removal writer exists anywhere.** Executed on real pipeline data over
the full corpus, renaming the worst-case institution of each of 525 settlements leaves
**4,034 handles** pointing at a name that no longer exists, across **33 observed paths**. Nothing
throws. Nothing is logged. Four of those surfaces are ones a DM can read on the page today.

In scope: (1) the declared surface list and the rename cascade over it; (2) the required
integration — the removal sweep over the SAME list; (3) the prevention guard — the independent
**three-arm** totality walker.

Explicit non-goals: the ops that call these functions (EM-B1c, EM-B1a) and any store or surface
wiring; the `catalogId` re-stamp and every anchor consequence (§3a, owner-gated); the two
owner-signed generator cures **FIX-D2** and **FIX-D3** (§6.1); `recordInvariants` (EM-R0b); the
merge (EM-R0c); the NPC and FACTION cascades, measured TOTAL and untouched; any golden, tuning or
migration. Record adjacent discoveries in the receipt; do not investigate them.

---

## 3. Hard scope budget

| Limit | Packet budget | Standard |
|---|---:|---:|
| Behavior families | `1` — an institution's name is a join key | 1 |
| New persisted record families | `0` — no key is minted; the module is pure | ≤1 |
| **Named state writers** | ⭐ **`1`** — and the point is that it becomes the ONLY one | ≤1 |
| Feature flags / user-facing surfaces | `0` each (wave 1 is HEADLESS) | ≤1 each |
| Direct production consumers | **`0` at landing** (EM-B1c, EM-B1a, EM-R0c later) | ≤2 |
| **New logic-bearing production leaves** | **`2`** — forced by the 250-line cap (§0) | ≤2 |
| Existing logic-bearing production files modified | **`1`** (R2's one `export` token) | ≤3 |
| Handwritten files total | `5` (+1 deferred census row, the host train's) | ≤12 |
| **New/changed effective production lines** | **`292`** (217 + 74 + 1); cap ≤320 | ≤400 |
| Effective lines per new leaf | **`217` / `74`** | ≤250 |
| Delta in a shared/hot file | **`1` effective** — `factionRename.js` is **388 effective of 800**, **not** on the standing hot list. ⚠ **Version 3.1 also inserts a COMMENT BLOCK** there (Q5's rule); comments are 0 effective under `skipComments`, and the line-shift hazards are re-swept clean in §7 | ≤15 |
| Acceptance cases | `8` | ≤8 |

Overrides approved before dispatch: `NONE` — the packet fits the default **because it splits**.
⚠ **The one conditional that would break it is §3a under a "NO"** (a third leaf).
⭐ **FIX-D5 is no longer optional — the chair ruled it RIDES HERE (Q4)**, and its +5 effective
lines and +245 B are inside the figures above, not beside them.

### §3.1 · Hot files and `max-lines` — MEASURED, not deferred

```
$ grep -n "factionRename\|institutionRename" docs/implementation/PACKET_STANDARD.md   → no hit
$ <lookup in scripts/.size-baseline.json>                                             → no entry
$ <every max-lines rule in eslint.config.js>  'src/domain/**/*.js' => max 800 (skipBlankLines+skipComments)
$ node tools/efflines3.mjs …   CONTROL 6/6 EXACT against scripts/.size-baseline.json
    src/domain/factionRename.js          raw 960  EFFECTIVE 388   (headroom 412)
    v3.1  institutionRename.js           raw 273  EFFECTIVE 217   ✅ (v3: 215, v2: 219)
    v3.1  institutionRemoval.js          raw  93  EFFECTIVE  74   ✅ (v3:  73, v2:  65)
    v3.1  institutionRename WITHOUT FIX-D5's list
                                         raw 267  EFFECTIVE 212   ⇒ FIX-D5 costs +5 effective
```

---

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- EM-R6
```

Expected: capsule emitted; ancestry and substrate proven; **all four CREATE targets ABSENT**;
`src/domain/clone.js`, `src/domain/factionRename.js`, the three producer files **and
`src/generators/servicesGenerator.js`** (version 4's ninth row) present and clean; **all NINE**
`requiredSymbols` rows resolving. ⭐ **THE POST-EDIT SIMULATION, RUN (brief step 10):** the only
`requiredSymbols` row on a file this packet EDITS is `src/domain/factionRename.js :: 'const
NPC_HOMES'`, and the edit prepends `export ` — so the verbatim text `const NPC_HOMES` **survives
as a substring of `export const NPC_HOMES`** and the row still resolves after the build.
`retiredSymbols` is therefore **empty by measurement, not by omission**: `NPC_HOMES` occurs at
**nine sites, all inside `factionRename.js` itself** (plus one COMMENT mention in
`src/store/settlementRenameHelpers.js:402`), and **no test anywhere names it**, so R2's `export`
retires nothing and discharges no other packet's row.

⚠ **THE WORKTREE IS SHARED AND THE BASE MOVES.** Re-read `git rev-parse HEAD` in the same command
as the dispatch. ⚠ Re-confirm disjointness against the kit's DRAFTs, which are outside this
packet's measured collision check (R7).

---

## 5. Verified tree contract

Every row re-found **by symbol** at `32602dc60`; the J-T1 window over every path is **EMPTY**
(evidence §1–§14).

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| ⭐ **THE ARCHITECTURE THIS COPIES** | `src/domain/factionRename.js` | `FACTION_RENAME_SURFACES`, `NON_CASCADED_SURFACES`, `NPC_HOMES` (`:168`) | one frozen list with a `why` per row; a frozen ledger of written rulings; the two homes declared ONCE and spread across both | **Copy the architecture exactly.** The only edit is R2's one `export` token |
| ⭐ **R2 — THE TWO HOMES, DECLARED ONCE FOR THE ESTATE** | `src/domain/factionRename.js` | `:168` `const NPC_HOMES = Object.freeze(['npcs[]', 'factions[].members[]'])` | not exported today; its own header states the law this packet also lives under — *"a field added for one home can never be forgotten at the other"* | **Add `export`.** The new leaf imports it as `INSTITUTION_HOMES` |
| ⭐ **THE RULING THIS PACKET INHERITS FROM THE FACTION SIDE** | `src/domain/factionRename.js` | `:289` `economicState.safetyProfile.criminalInstitutions[]` | ruled NON_CASCADED **for factions** with the reason *"a fixed vocabulary label from the `CRIMINAL_INST_LABELS` table … naming an INSTITUTION rather than referring to the faction … an institution owns its own name"* | ⭐ That sentence is the argument for this being an **institution** surface. Row 25 takes it |
| ⭐ **THE SUBJECT HAS ONE NAME SPELLING** | the generated record | `institutions[].name` | **17,361 of 17,361** carry `name`; **0** carry `id`; **0** carry `localUid`; no alias | ⇒ **NO dual-write and NO accessor.** `factionRename.js`'s `nameOf` / FACTION-ACCESS LAW has **no counterpart here** and must not be invented |
| ⭐ **NO PROSE SURFACE EXISTS** | the whole-blob walk | all 36 cascade rows | every one is a stored **key join**, exact or case-folded; not one is prose | ⇒ ⛔ **`substituteWholeWord` is NOT imported.** The word-boundary hazard class the faction cascade carries **does not exist here** |
| **The only runtime import beside R2's** | `src/domain/clone.js` | `export function deepClone` (`grep -cF` → 1) | present | Reuse; no second clone |
| ⛔ **A ZERO-SLACK NEIGHBOUR — IMPORT, NEVER MODIFY** | `src/domain/clone.js` | — | **IS in the generation worker's closure** (`WORKER_BUNDLE_CEILING_BYTES = 1401208`, EXACT/zero slack) | ⛔ Modifying it would owe the worker re-mint AND seven generated `_shared` paths. Importing adds no byte to it |
| ⛔ **NOT IMPORTED, AND THAT IS MEASURED** | `src/lib/narrativeMutations.js` | `substituteWholeWord` | **IS in the generation worker's closure** | ⛔ Naming it would drag this packet into the zero-slack budget for a surface class that does not exist |
| ⭐ **ROW 25's PRODUCER — WHY THE `label` MATCH KIND IS CORRECT** | `src/generators/safetyProfile.js` | `CRIMINAL_INST_LABELS` (`grep -cF` → 2) | a frozen map that turns each matched institution into a **Title-Cased display label**; the stored value is the LABEL, never the house's own name. Measured: **1,164 of 1,164 values are case-variants of a roster name and 0 are exact** | ⛔ **NOT edited** (that is FIX-D2). The `label` match kind is lawful only while this producer keeps writing a case-variant — the row pins it |
| ⭐ **ROW 26's PRODUCER — WHY ITS REWRITE IS LOWERCASED** | `src/generators/computeActiveChains.js` | `export function deriveInstitutionalServices` (`grep -cF` → 1) | stores `(i.name \|\| '').toLowerCase()` for each matched house, `.slice(0, 2)`. Measured: **1,764 of 1,764 stored values are all-lowercase and 1,764 are lower-case variants of a roster name; 0 are exact** | ⛔ **NOT edited.** The rewrite writes `newName.toLowerCase()` so the path keeps its own measured invariant |
| ⭐ **ROW 26 IS READER-VISIBLE** | `src/components/new/tabs/EconomicsTab.jsx` | `:241` `svc.institutions.map(institutionDisplayName).join(' · ')` | Economics › Economic Flows › **Service economy** renders it under a `Via:` label; `institutionDisplayName` falls through to the raw string for a DM-renamed house | ⇒ left un-cascaded it is a **fourth surface** still naming the old house |
| ⭐ **THE FROZEN STAMP — WHY IT IS NON_CASCADED** | `src/generators/priorityHelpers.js` | `export const getInstFlags` (`grep -cF` → 1) → `getInstitutionNames` returns `{ …booleans, names }`, the LOWERCASED roster | `economicState.js:51`'s own header: *"⛔⛔ RAW ROSTER, STAMPED ONCE (`compound:` at :883), NEVER RECOMPUTED ON ADVANCE"*, with the owner gate named (`docs/ENGINE_DEFECT_DISPOSITIONS.md §4`). **17,361 of 17,361 values, normalised-equal, on two paths; 0 readers of `.names` anywhere in `src`** | ⛔ **NOT cascaded** — rewriting the names would leave them disagreeing with the booleans they produced, inside an owner-gated freeze. Declared with its reason |
| ⛔ **THE TWO STAMPS ARE NOT AN ALIAS — version 3's row said they were** | `src/generators/economy/economicState.js` · `src/generators/safetyProfile.js` | `:62` `ecoInstFlags = getInstFlags(config, institutions)` · `:26` `const flags = getInstFlags(config, institutions)` | **TWO INDEPENDENT CALLS.** Executed over the corpus: `rowsWithBoth 525 · sameObjectLive 0 · sameObjectReloaded 0` — the two `names` arrays are **never the same object, not even before serialization** | ⇒ the ledger declares **two rows for two stamps**, which is simply correct. ⭐ **This is why item 12 takes the failure-message sentence and NOT an `alias` field** — the field would have asserted a falsehood in a frozen structure nothing reads |
| ⭐ **ITEM 6 — `availableServices.legal[].name`, RE-MEASURED** | the generated record | `availableServices.legal[].name` | version 1's "0 of 703" was three versions old. At this tip over 525 rows: **5,001 entries; `name === institution` on 0; 105 hold a roster house's name and ALL 105 name a house DIFFERENT from the entry's own `institution`** | The `NON_CASCADED` ruling is re-grounded, and **A6 (vi) now asserts the property directly** instead of trusting the ledger's prose |
| **The member mirror** | the generated record | `factions[].members[]` | at generation the member **IS** the `npcs[]` record; on a reloaded save they are separate objects — **4,884 paired people, 0 disagreeing before any edit** | Both homes walked with ONE declared field list |
| ⭐ **R3(ii)'s SAFETY, RE-MEASURED** | the generated record | `{npcs[], factions[].members[]}.secondaryAffiliation` | the free-vocabulary literal `"Thieves' Guild"` is a **FACTION** name (⛔ **PATH CORRECTED BY THE PRE-PROOF:** `src/generators/power/rulingStructure.js:671`, re-found at the tip as `faction: "Thieves' Guild",` — version 3.1 wrote `src/generators/rulingStructure.js`, which does not exist; the line and the content were exactly right): **488 per home, 976 total, on 327 of 525 rows, and 0 of 525 rows carry it as an institution name** | Cascaded under the exact, **non**-case-folded guard; **A6 (v) pins by execution** that renaming `Thieves' guild chapter` moves none of them |
| ⭐ **R3(i)'s READER** | `src/domain/districtProfile.js` | `inferInstitutions` (**`:393`** — re-found by symbol at the tip; `:392` is the closing `*/` of its doc comment) | joins an institution to a quarter when a >4-char stem of its name appears in the quarter's landmark haystack | A stale landmark silently un-homes the renamed house; the row is cascaded. Corpus: **1,664 of 4,214 landmark values are roster copies** |
| ⭐ **R3(iii)'s SHAPE CONSTRAINT** | the generated record | `economicState.activeChains[].label` | **present on 6,676 of 6,676 chains**; 168 of them equal a roster name | ⇒ the rename rewrites it; the **removal CANNOT delete it** — rule `report-only` (§6) |
| ⭐ **THE `linkedInstitutionIds` ROW** | `src/generators/factionRoles.js` | `linkedInstitutionIds` (`grep -cF` → 4) | its own comment calls the name branch **"rename-sensitive"**; a generated institution has no `id`, so the value is **always the name** | A cascade row, rewritten as a **name LIST** so a mixed id/name list heals only its name half |
| **The consumer the join keeps alive** | `src/domain/entities/propagate.js` | `instId` (`:330`) | the estate's institution join is `i?.id \|\| i?.name` | Named so the cascade's purpose is provable from the manifest |
| **The anchor, and what a rename moves** | `src/domain/townMap/anchors.js` | `export function anchorForInstitution` (`grep -cF` → 1) | `cat:` → `uid:` → `name:`; **15,278 `cat:` / 2,083 `name:` / 0 duplicates** over 17,361 | ⛔ **NOT edited.** §3a states what a rename does to the 12 %, under both owner answers |
| ⭐ **VERSION 4 — THE SET-EQUALITY ARM'S PRODUCER (chair-fated; charter 2026-09-20 02:46 EDT)** | `src/generators/servicesGenerator.js` | `addCrimeService = (name, desc, institution) =>` (`grep -cF` → 1, cited BY SYMBOL per §P2.22) | ⛔ **VERSION 8 REFUTES VERSION 7's ROW.** The parenthetical provenance sentinels have SEVERAL WRITERS IN THIS ONE FILE: `addCrimeService` (the anchor) and the direct `buckets.criminal.push({ ...item, institution: '(…)' })` writers at four sites. Measured by the build lane at `3aba63857`: **23** parenthetical-literal occurrences, **14 of them before the declaration**, and a third-argument read reaches only **seven** of the eleven spellings. Re-executed at the tip with a **WIDENED** character class that could have found a twelfth spelling and did not: `grep -oE "'\([^']*\)'"` returns the SAME **eleven** as `'\([a-z ]+\)'`. `'(background crime)'` occurs **nowhere in `src/` or `tests/`** | **A4 (v) reads this file WHOLE** and asserts its parenthetical literal set is SET-EQUAL to `SERVICE_PROVENANCE_SENTINELS`, anchored on the declaration so a silent scan still refuses. ⛔ **NOT edited** |
| **Test precedent — the denominator pin** | `tests/domain/factionRename.test.js` | `describe('faction rename — the INDEPENDENT denominator')` (`:189`) and its first `test` (`:190`) | walks the WHOLE blob on **RELOADED saves**; requires every shape found to be in one of the two lists; anti-vacuity floor `toBeGreaterThanOrEqual(14)` (`:209`) | **Copy this proof shape, then ADD the two new arms** |
| **Test precedent — the reload discipline** | `tests/domain/factionRename.test.js` | `reloaded()` (**`:76`**, re-found by symbol) | *"SAVES, NOT SETTLEMENTS … Testing the live object would reproduce the exact blindness that let the bug ship"* | Every fixture is JSON round-tripped |
| **Corpus precedent** | `tests/helpers/goldenMasterCorpus.js` | `goldenCorpus` (`:76`) | the 525-row master; **no structured-sample helper exists in `tests/`** | The stride is built in the test file (A1) |
| ⛔ **NOT a golden for this claim** | `tests/domain/factionRename.test.js` | the whole file | it pins the FACTION cascade only; **nothing in `tests/` pins an institution cascade** | Named so it is not counted as protection |

### ⭐ VERSION 7 — HOW AN INSTITUTION'S RENAME REGISTERS IN THE **LANDED** CASCADE SHAPE (EM-C4a and EM-B1c1, measured at `bfca6b7d6`)

⛔ **NOT IN THIS PACKET, AND THAT IS THE MEASUREMENT — not an omission.** EM-C4a landed the store
side of the cascade and EM-B1c1 landed `set-npc-name`; both were compiled after this packet, so
every row below is read off the tree rather than off a sibling's prose (§P10.11 (ii), judgment 134).

| landed fact | measured at `bfca6b7d6` | what it means for EM-R6 |
|---|---|---|
| the writer table | `src/store/editSlice.js:63` — `export const CASCADE_WRITERS = Object.freeze({ 'npc:name': 'renameNPC' });` | ⭐ **ONE ROW TODAY.** An institution rename registers as a SECOND row, `'institution:name': '<the store action>'`, keyed `<cardType>:<field>` |
| the dispatch table | `src/store/editSlice.js:79` `CASCADE_DISPATCH`, with `Object.keys(CASCADE_DISPATCH)` asserted SET-EQUAL BOTH WAYS to `Object.values(CASCADE_WRITERS)` (`:75`, and the arm at `tests/store/editSlice.test.js:266-267`) | the action is reached **BY ITS LITERAL NAME** through the frozen table, never `get()[name]` — §P2.20's second half, because `tests/store/deadOperationRatchet.test.js` cannot see a computed dispatch |
| the entry point | `applyCascadeEdit` (`:221`): `CASCADE_WRITERS[\`${coords.cardType}:${coords.field}\`]`; `if (!action \|\| !Object.hasOwn(CASCADE_DISPATCH, action)) return refuse('not_a_draft_field')` (`:222-223`), called from the ONE `free-cascade` branch at `:310-311` | **today an institution-name edit is REFUSED `not_a_draft_field`** — the declaration exists and the writer row does not |
| the declaration | `src/domain/edit/fieldDeclarations.js:94` — `{ card: 'institution', field: 'name', kind: 'free-cascade', provenance: 'root', outputKey: 'institutions[].name', … }` | the `free-cascade` kind (design §20 ruling 2, HZ-JOINKEY) is already declared for this field |
| ⭐ **THE NAMED EXCEPTION THAT NAMES THIS PACKET** | `tests/domain/editDeclarations.test.js:192-193` — a `free-cascade` outputKey must be a declared KEY rename surface **"or the one named exception `institutions[].name` whose cascade EM-R6 mints"**, where `cascadeKeySurfaces` is derived at `:138` from `[...NPC_RENAME_SURFACES, ...FACTION_RENAME_SURFACES].filter((s) => s.kind === 'key')` — **imported from `src/domain/factionRename.js`** | ⛔ **THE EXCEPTION STILL PASSES AND IS STILL NEEDED AFTER THIS PACKET LANDS**, because `INSTITUTION_RENAME_SURFACES` lives in a NEW module and its rows are keyed `match: 'exact' \| 'label'`, not `kind: 'key'`. Widening the derivation would re-shape this packet's row schema; it is **EM-B1c's** to do when the op routes the field — **RAISED Q10** |

⭐ **WHICH LANDED ARMS THIS PACKET'S ROWS WIDEN: EXACTLY ONE, AND ONLY CONDITIONALLY.**

| landed arm | verdict, executed |
|---|---|
| `tests/store/editSlice.test.js:418` — `expect(CASCADE_WRITERS).toEqual({ 'npc:name': 'renameNPC' })` | ⭐ **UNMOVED.** This packet adds no writer row and touches no store file; the pin stays exact. (EM-B2a4 v2 of train EM-T13 MODIFIEs `src/store/editSlice.js` and TESTs this file — its rows, not this member's) |
| `tests/domain/editDeclarations.test.js` — the three rosters (`WAVE1_PACKET_IDS`, `CARD_TYPES` off the table's own keys, `cascadeKeySurfaces`) | **UNMOVED** — none is re-typed here and no declaration row moves |
| `tests/domain/editOperations.test.js` A6 (`the import list is exactly five, and nothing imports the leaf`) and B1–B6 (the composed **fifteen**, `set-npc-name` spliced at **index 11** of 15 in codepoint order — executed by importing `OP_TYPES`) | **UNMOVED** — this packet imports neither `operations.js` nor `operationsNpcRename.js` and mints no op. ⚠ EM-B1b v6 re-words A6's title and appends four `it`s here (the pass-2 addendum's NOTE 2, judgment 156); that is EM-B1b's row |
| `tests/lint/editMutationPath.walker.test.js` — `WATCHED` (`makeOp`, `validateOp`, `OP_TYPES`, `applyEdit`; `expect(Object.values(WATCHED).flat().length).toBe(4)`), `EDIT_PATH` (3), `EDIT_SLICE_IMPORTS` (4) | **UNMOVED** — neither leaf statically imports either producer, and its header says *"Widening WATCHED is not a legal move"* |
| `tests/lint/opGuardCoverage.walker.test.js` | ⚠ **DOES NOT EXIST AT THIS TIP** — it is EM-B1b v6's CREATE on train EM-T13. Its predicate reads the COMPOSED OP CATALOGUE (`guards` / `guardsStated` per row), not `src/domain`, so it cannot reach either leaf. **PLAUSIBLE until its code exists; the placement re-measures it** |
| ⛔ `tests/lint/ruinFilterRoster.walker.test.js` — `expect(readers.length).toBe(94)`, EXACT | ⭐ **THE ONE ARM AT RISK, AND §7 PRICES IT.** See the registration ledger's §P2.21(b) row |

### ⭐ THE PATH-COUNT RECONCILIATION — five numbers, each correct

| number | what it counts |
|---:|---|
| **42** | distinct paths the EXACT-name walk finds holding a roster institution name, over 525 rows — unchanged from version 2 |
| **5** | additional paths the **two new arms** find at ≥50 % predominance and the exact walk can never see: `economicState.safetyProfile.criminalInstitutions[]` · `economicState.institutionalServices[].institutions[]` · `economicState.compound.inst.names[]` · `economicState.safetyProfile.compound.inst.names[]` · `structuralSuggestions[].suggested[]` |
| **36** | the rows the module DECLARES as CASCADE: version 2's 38, minus the four re-classified by §934.61 (rows 13, 24, 25–26 of version 2), plus the two DERIVED DISPLAY LABELS |
| **33** | of those 36, the paths OBSERVED carrying an exact roster name. The three that are not: the two label rows (**0 exact by construction**) and `factions[].members[].linkedInstitutionIds[]` (**0 of 525** — R9's symmetry declaration) |
| **15** | the `NON_CASCADED` ledger, up from five: the original five, plus the four re-classified rows, plus three sibling catalogue paths, plus the two frozen stamps and the authored suggestion list. ⭐ **Exactly ONE carries `readable: true`** (Q5) |

⚠ **THE LIMIT OF EVERY "TOTAL OVER THE CORPUS" CLAIM IN THIS PACKET, STATED ONCE (item 8).** The
525-row corpus emits **231 distinct institution names against the catalogue's 280** — 49 catalogue
institutions are never produced by any golden row. Every totality figure here is total over the
231, not the 280. **The collision claim is the exception and it IS total: both normalised collision
scans — over the 231 emitted and over all 280 catalogue names — are EMPTY**, so no two institution
names this generator can emit, or that the catalogue can supply, collide under normalisation.

**Forbidden alternatives:** ⛔ no second writer of an institution rename or removal; ⛔ no edit to
`src/domain/clone.js` or `src/lib/narrativeMutations.js` (both in the zero-slack worker closure);
⛔ **no edit to `safetyProfile.js`, `computeActiveChains.js` or `priorityHelpers.js` — all three
are in the worker closure AND the lazy `engine` chunk, and two of them are FIX-D2's and FIX-D3's
owner-signed territory**; ⛔ no `substituteWholeWord` import and no prose surface; ⛔ no edit to
`anchors.js` and no `catalogId` re-stamp (§3a); ⛔ no new persisted key; ⛔ no `null`-writing
removal (§6); ⛔ no deletion of a chain `label`; ⛔ no case-folded match on any row but the two
declared label rows; ⛔ no REFUSAL inside the writer (§P8); ⛔ no static import of either leaf
from an eager or store module (§3b); no files outside the manifest.

### §3a · THE ANCHOR — CONDITIONAL, and correct under BOTH owner answers (R4)

The owner has been asked (**ODQ §934.59; the chair recommends YES**) and **has not answered**.

| fact | measurement |
|---|---|
| institutions anchored `cat:` | **15,278 of 17,361 (88.0 %)** — rename-STABLE |
| institutions anchored `name:` | **2,083 of 17,361 (12.0 %)** — a rename MOVES the anchor |
| duplicate anchors | **0** over all 525 rows |

**UNDER "YES" (re-stamp `catalogId`) — WHAT THE CHAIR DELETES:** this entire §3a, and nothing
else. No row of §6, §7, §9 or §10 mentions an anchor; no `requiredSymbols` row is removed
(`anchorForInstitution` stays pinned as the fact that makes §3a cuttable); the budget row does not
move. **One section, deleted whole.**

**UNDER "NO" — WHAT THE THIRD LEAF CONTAINS:** the cascade must carry the `anchorKey`-keyed edits
from the old anchor to the new one, for exactly four stores — ⭐ **all INSIDE the settlement
record**, so a cascade reaches them without touching the save row:

| store | shape | present on a GENERATED record? |
|---|---|---|
| `settlement.mapEdits.pins[].anchor` | `{ anchor, dx, dy }` | **no** |
| `settlement.mapEdits.sceneOverrides[].anchor` | `{ anchor, variantId?, skinId?, … }` | **no** |
| `settlement.interiorEdits` | **the OBJECT KEY is the institution anchor** | **no** |
| `settlement.fogSessions[].buildings[]` | a list of `anchorKey` strings | **no** — ⚰ writer RETIRED (`operationRegistry.js:340-346`); legacy saves only |

⭐ **All four are absent on 525 of 525 generated records** ⇒ the exposure is only a save a DM has
**already** edited.
⛔ **IT DOES NOT FIT HERE:** the rename leaf is at **215 of 250**, so under NO the four rows are a
**THIRD leaf** (`institutionAnchorCascade.js`, ≈45 effective) **or a follow-on packet**, and the
budget row changes from 2 leaves to 3. **RAISED Q8.**

⛔ **NOT IN THIS PACKET EITHER WAY.** A rename also re-seeds the glyph
(`townMap/glyphAssign.js:125`, `createPRNG('glyph:<name>')`) — design §21.5 ruling 7 routes that
site to **EM-P1b's class**, and the charter already forbids promoting EM-B1c before it lands.

### §3b · PLACEMENT — ⛔ A BINDING NOTE TO THE APPLY POINT (`EM-C4a` / `EM-D0`), RE-AIMED

Measured at `32602dc60`, two ways (evidence §8):

| budget | rule / instrument | `factionRename.js` | this packet's two leaves |
|---|---|---|---|
| first-paint eager closure | the repo's OWN `EAGER_FIRST_PAINT_MODULES`, **IMPORTED, its size MEASURED AT THE BASE and never quoted** (interim rule 9; history with an as-of mark: **268** at `32602dc60`, **269** at `bdbf7c89c`). Control `settlementSlice.js` = eager ✓ — and the CONTROL, not the count, is what makes the measurement honest | **NOT eager** | **not eager** — nothing imports them |
| generation worker (**zero slack**) | static **220** / static+dynamic **228** modules from `src/workers/generation.worker.js` | **absent** | **absent** |
| lazy engine (`< 679_000`) | `id.includes('/src/generators/')` (`vite.config.js`) | **not in `engine`** | **not in `engine`** |
| edge-shared (5 metas, **307 inputs**) | INPUT membership | **0 of 307** | **0 of 307** |

⇒ **THIS PACKET IMPORTS NOTHING AT RUNTIME AND IS IN NO CHUNK UNTIL A CONSUMER LANDS.**

⛔ **THE BINDING NOTE, AND IT IS SHARPER THAN VERSION 2's.** `factionRename.js`'s own door test
records the history (`tests/build/factionRenameDoorLazy.test.js`): *"`src/domain/factionRename.js`
did ride the first-paint closure … the 'dependency-light pure leaf' was **8,574 B minified**, and
the first-paint ratchet went over budget."* **These two leaves are 13,258 B minified together —
more than half again as large as the module that broke that ratchet.**

⭐ **RE-AIMED IN VERSION 5 (ESTATE-REPAIR-3, on the chair's ruling of 2026-09-20 over
ESTATE-REPAIR-2's NOTE-2).** Version 4 aimed this note at **EM-B1c, EM-B1a and EM-R0c**. Measured at
`bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`, **none of the three declares an import of either leaf, and two are forbidden one by their
own contracts**: EM-B1c's acceptance case C6 asserts the module's import list contains none of
`src/domain/factionRename.js` — *it names the cascade as DATA, never imports it* — and its §R1 says
`src/domain/edit/**` may not reach the store (§P4); EM-R0c's §7 declares `mergeConsequence.js`'s
imports exhaustively and ends *"and nothing else"*; EM-B1a's rename ops MOVED to EM-B1c at the
compile split, so version 4's naming of B1a is PRE-SPLIT. All three read `Direct production
consumers | 0 — lands DARK`. The note named the wrong LAYER, not the wrong packets.

⇒ **THE FIRST PACKET THAT CAN HOLD A STATIC IMPORT EDGE TO EITHER LEAF IS THE APPLY POINT —
`EM-C4a` / `EM-D0` per the charter — AND THE DOOR BELONGS THERE.** The apply point prices, at its
own pre-proof, how it imports these leaves without entering first paint, and it owes the door test
in `factionRenameDoorLazy.test.js`'s shape. **It must not be discovered at its build.**

⭐ **AND THE GUARD STAYS WHERE IT IS.** EM-B1a, EM-B1c and EM-R0c each keep the first-paint
MEMBERSHIP probe in their `checks` as a STANDING guard: it imports the exported set, reads no
`dist`, therefore cannot skip (§P2.11), costs nothing, and catches the edge **the day someone adds
it** — which is the only day the liability becomes real. Removing the probe because the door moved
would discard the cheap instrument and keep only the expensive one.

---

## 6. Exact contracts

### Inputs and outputs

```js
/** @typedef {{ [key: string]: unknown }} StoredRecord */
/** @typedef {{ kind: OrphanKind, path: string, chainId: string|null }} OrphanNote */
/** @typedef {'chain-lost-its-last-processor'|'chain-label-names-a-removed-house'} OrphanKind */
/** @typedef {'verbatim'|'lower'} LabelRewrite */   // ⭐ version 4, chair-fated

/** PURE. Mutates the settlement it is HANDED (the caller owns the draft), never reaches outside it. */
export function applyInstitutionRenameToSettlement(settlement, oldName, newName)
  // → { changed: boolean, touched: string[] }

export function applyInstitutionRemovalToSettlement(settlement, name)
  // → { changed: boolean, touched: string[], orphaned: OrphanNote[] }

/** The IMMUTABLE forms: only the touched top-level buckets, deep-cloned, ready to spread. */
export function institutionRenameChanges(settlement, oldName, newName)
  // → { changed: boolean, touched: string[], changes: StoredRecord, orphaned: [] }

export function institutionRemovalChanges(settlement, name)
  // → { changed: boolean, touched: string[], changes: StoredRecord, orphaned: OrphanNote[] }
```

**Absence rules.** `changed: false` with `touched: []`, `changes: {}` and `orphaned: []` for: a
non-record settlement, an empty name, `oldName === newName`, and a name no record holds. ⛔ **A
no-op is never a throw and never a partial write.** `touched` names only surfaces that ACTUALLY
moved, so it is a receipt, not a plan.

⛔ **`OrphanKind` IS A CLOSED VOCABULARY OF TWO** — the op's guard engine (EM-C2) branches on it.
Measured at the tip: **neither literal occurs anywhere in `src` or `tests`**, so neither collides.

⭐ **AND SO IS `LABEL_REWRITE` — THE PRE-PROOF'S FIRST CHAIR-FATED ARM (ODQ §934.47 addendum 37;
charter 2026-09-20 02:46 EDT: *"a closed-vocabulary pin on `LABEL_REWRITE` (OrphanKind's shape)"*).**
Version 3.1 made the rewrite form a DECLARED ROW FIELD and read it through one frozen map, which
stops a call site choosing a form — but **nothing stopped a THIRD form entering the map**, which is
the identical omission that let a twelfth sentinel spelling ride a whole version. The cure is
`OrphanKind`'s, exactly:

- the vocabulary is a **JSDoc typedef** `LabelRewrite` (above) — **0 effective lines**, because
  `src/domain/**` is `max-lines { skipComments: true }` (executed at the tip);
- the **runtime vocabulary is the frozen rewrite map's own key set**, never a second hand-typed
  list — the family's law (*declared sets are IMPORTED from their producer, never re-typed*);
- **A1 (vi) pins it closed**, with both members observed firing and the `exact` rows proved to
  carry no `rewrite` at all;
- **§11 STOPs** on a third form, as it already did — now with an arm that would actually red.

### ⛔ THE MATCH RULE — `exact` ON 34 ROWS, `label` ON 2

- **`exact`** — `value.trim() === oldName`: trimmed both sides so an incidentally-padded value
  still joins, **NOT case-folded**, copied verbatim from `factionRename.js`'s `isSameName` and for
  the same reason: two institutions may legitimately differ only in case.
- **`label`** — `value.trim().toLowerCase() === oldName.toLowerCase()`, **plain `toLowerCase`,
  never `toLocaleLowerCase`**, on the two DECLARED display-label rows and **nowhere else**. It is
  lawful there because both producers write a case-variant of the house's own name **by
  construction** and the measurement is total: **1,164 of 1,164** and **1,764 of 1,764** values
  are case-variants, with **0 exact matches** on either path.

⭐ **AND THE REWRITE FORM IS DECLARED PER ROW, NOT HARD-CODED (Q1, RULED).** A `label` row carries
`rewrite: 'verbatim' | 'lower'` and the cascade reads it off the row through one frozen map; an
`exact` row always writes the new name verbatim and carries no `rewrite` field. Executed at the
tip: `["…criminalInstitutions[] :: verbatim", "…institutionalServices[].institutions[] :: lower"]`.
⛔ **The two are not interchangeable and the difference is measured, not stylistic:** row 26's path
is **all-lowercase on 1,764 of 1,764 values**, so writing a Title-Cased new name there would break
the producer's own invariant and `institutionDisplayName`'s lowercase index with it.

⭐ **AND THE CASE NEAR-MISS IS NOW A PIN, NOT AN ACCEPTED COST.** `"Thieves' Guild"` is a
power-structure FACTION handle minted at `src/generators/power/rulingStructure.js:671` (path corrected) — **488 per home, 976 total, on
327 of 525 rows, and 0 of 525 rows carry it as an institution name.** A case-folded match on
`secondaryAffiliation` would rewrite a faction handle on an institution rename: **a live bug.**
A6 (v) executes the pin — renaming `Thieves' guild chapter` leaves all 976 untouched on **156 of
156** rows that carry both.

### THE SURFACE TABLE — 36 declared CASCADE rows (33 observed)

| # | path | match | rewrite | REMOVAL | corpus |
|---:|---|---|---|---|---:|
| 1 | `institutions[].name` | exact | set | **DROP THE RECORD** | 17,361 |
| 2–12 | `availableServices.{equipment,legal,healing,employment,entertainment,food,lodging,magic,information,transport,criminal}[].institution` | exact | set | **DROP THE ENTRY** (`institution` on 25,628/25,628 ⇒ absence is not a shape this record carries) | 7101 · 5001 · 2694 · 2221 · 1883 · 1456 · 1318 · 907 · 886 · 879 · 861 |
| 13 | `economicState.tradeDependencies[].institution` | exact | set | **DROP THE ENTRY** (2,444/2,444) | 2,444 |
| 14 | `resourceAnalysis.gaps[].institution` | exact | set | **DELETE THE KEY** (already absent on 923/2,982) | 2,059 |
| 15 | `economicState.activeChains[].dependency.institution` | exact | set | **DELETE THE `dependency` OBJECT** (already absent on 5,515/6,676) | 1,161 |
| 16–22 | `defenseProfile.institutions.{garrison,magicDef,walls,watch,charter,mercenary,militia}[].name` | exact | set | **DROP THE ENTRY** (the entry IS the institution) | 451 · 370 · 354 · 273 · 171 · 59 · 12 |
| 23 | `spatialLayout.quarters[].landmarks[]` | exact, per item | set item | **DROP THE ITEM** | 1,664 of 4,214 |
| 24 | `economicState.activeChains[].label` | exact | set | ⛔ **REPORT-ONLY — the label STANDS** (6,676/6,676; deleting mints a new shape) and an `orphaned` note carries the loss | 168 of 6,676 |
| ⭐ 25 | **`economicState.safetyProfile.criminalInstitutions[]`** | **label** | `rewrite: 'verbatim'` | **DROP THE ITEM** | **1,164 — 0 exact, 1,164 case-variants** |
| ⭐ 26 | **`economicState.institutionalServices[].institutions[]`** | **label** | `rewrite: 'lower'` | **DROP THE ITEM** | **1,764 — 0 exact, 1,764 lower-variants, 1,764/1,764 all-lowercase** |
| 27–28 | `{npcs[], factions[].members[]}.institution` | exact | set | **DELETE THE KEY** (already absent on 4,874/5,171) | 297 · 297 |
| 29–30 | `{npcs[], factions[].members[]}.secondaryAffiliation` | exact | set | **DELETE THE KEY** (already absent on 4,257/5,171) | 354 · 354 |
| 31–34 | `{npcs[], factions[].members[]}.corruptTies.{thievesGuild,criminalInstitution}` | exact | set | **DELETE THE KEY** (parent already absent on 4,817/5,171) | 354 × 4 |
| 35–36 | `{npcs[], factions[].members[]}.linkedInstitutionIds[]` | exact, per item | set item | **DROP THE ITEM** | 25 · **0 — declared, not observed** (R9) |

⭐ **THE REMOVAL RULE IS THE RECORD'S OWN MEASURED ABSENCE SHAPE — NEVER A NULL.** Version 1's
draft wrote `null` into rows 13–15 and 27–34; the census refuted it (the generator already omits
those keys on most records, so deleting returns the record to a shape it carries while `null`
mints a new one for every tolerant reader to meet for the first time).

**FIVE removal kinds, all five observed firing** over 525 removals: `drop-record`, `drop-entry`,
`drop-item`, `delete-key`, `delete-dependency-object` — plus `report-only`, which by definition
changes nothing and emits a note.

⭐ **AND A SIXTH DECLARED VOCABULARY — THE SERVICE PROVENANCE SENTINELS (FIX-D5, Q4, RULED IN).**
`availableServices.*[].institution` may hold a parenthetical provenance word instead of a house —
a stress flag or a crime type for a service with no building behind it. The ELEVEN spellings are
declared as `SERVICE_PROVENANCE_SENTINELS`, and ⛔ **the dangle denominator excludes them BY THAT
LIST, never by `startsWith('(')`.** Measured at the tip: **3 live** — `(arcane underground)` 246 ·
`(smuggling)` 173 · `(covert)` 2 = 421 values on 260 of 525 rows — and **8 dark**.
⚠ **A CORRECTION TO THE RULING'S OWN FIGURE, MEASURED.** The ruling said *"the twelve-spelling …
3 live, 9 dark"*. `servicesGenerator.js` writes **ELEVEN** parenthetical literals:
`grep -oE "'\([a-z ]+\)'" src/generators/servicesGenerator.js | sort -u` returns exactly
`(arcane underground) (commercial crime) (covert) (criminal governance) (informal) (lawless)
(religious fraud) (smuggling) (state apparatus) (street gang) (thieves guild)`.
⛔ **AND THE PRE-PROOF SHARPENS THE REASON, BECAUSE THE RECORD'S VERSION IS NOT QUITE TRUE.**
Version 3.1 and ODQ §934.47 addendum 37 both say *"`'(background crime)'` is a `crimeType` feeding
the `(covert)` fallback"*. Measured at `141a1d775`: **the literal `(background crime)` occurs
nowhere in `src/` or `tests/`** (`git grep -n 'background crime' -- src tests` → no output). What
exists is the crimeType `'Background crime'` — Title-Cased, unparenthesised — read as a CONDITION
at `src/generators/servicesGenerator.js:347` (`if (crimeTypes.has('Background crime')) {`), whose
branch stores `'(covert)'`. So the twelfth spelling was never a value this generator writes in any
casing; it was a draft's transcription of a crimeType into sentinel shape. The ruling is unchanged
and better grounded; the correction is offered to the chair (question P3). The declared list is the eleven the
generator actually writes, and the split is **3 live / 8 dark**. ⭐ **The control is unaffected:
ZERO parenthetical values over 525 rows fall outside the declared eleven**, so replacing the shape rule with the list is behaviour-identical today and falsifiable
tomorrow. A4 (iv) pins the live/dark split so a future generator edit cannot silently light one.

### THE `NON_CASCADED` LEDGER — 15 rows, with the kind that rules each and whether it may be READ

⛔ **`readable` (Q5, RULED): "never touched" means never WRITTEN.** A `NON_CASCADED` path may still
be read, and the field says which. **Exactly one row is `readable: true`**, and this packet is its
only reader — the removal's orphan report asks whether a chain still names a surviving house
without ever rewriting the pattern list. ⭐ **The removal RESOLVES that path from the ledger rather
than spelling it**, so flipping the row to `readable: false` throws at import instead of reading
on. The rule is written into `factionRename.js`'s ledger header for the estate (§7's MODIFY row).

| path | kind | corpus | ruling |
|---|---|---:|---|
| `simulationTrace[].downstreamEffects[].target` | receipt | 708 of 39,338 | the generation RECEIPT — the identical ruling `simulationTrace[].causes[].reason` carries in `factionRename.js:288` |
| `generationCoherenceReceipt.repairs[].subject` | receipt | 283 of 408 | **design §22.3 item 1 — HISTORY BY PATH.** THE PROMISE: lived history is immutable |
| `availableServices.legal[].name` | label | 105 of 5,001 | ⭐ **RE-MEASURED AT THIS TIP (item 6):** 5,001 entries; `name === institution` on **0**; 105 hold a roster house's name and **all 105 name a DIFFERENT house** from the entry's own `institution`. The entry's handle is its sibling `institution`, which is row 3. **A6 (vi) asserts this directly** |
| `resourceAnalysis.resourceConditions[].label` | label | 108 of 2,816 | the SAME literal (`"Stone quarry"`) appears both matching and non-matching ⇒ a fixed condition word |
| `economicState.activeChains[].resource` | label | 60 of 2,834 | the resource's display label, keyed by its own `resourceKey`. The key is the identity |
| ⭐ `economicState.activeChains[].processingInstitutions[]` ⭐ **`readable: true`** | **matched-pattern** | 10,507 | **§934.61.** `computeActiveChains.js:381` stores the static PATTERN that matched, joined by `institutionMatchesProcessor` (catalogId-first, 12-char-prefix fallback). **All 10,507 still match a present institution**; a by-name cascade would touch 7,982 and split one chain's `Via` line into renamed and un-renamed halves. **NON_CASCADED until the owner signs FIX-D3 — but READ (never written) by the removal's orphan report** |
| ⭐ `resourceAnalysis.resourceChains[].processingInstitutions[]` | **catalogue** | 3,212 | **§934.61.** Byte-identical to the static `RESOURCE_CHAINS` row on **1,327 of 1,327** arrays; the roster is not an input to its producer. The field's own header calls it *"the human-readable gap-row vocabulary"* |
| ⭐ `resourceAnalysis.exploitation.fullyExploited[].processingInstitutions[]` | **catalogue** | 1,040 | the same chain objects, bucketed. The three arms **PARTITION** the parent on **525 of 525** rows — rewriting one home and not the others would make a record disagree with itself |
| ⭐ `resourceAnalysis.exploitation.partiallyExploited[].processingInstitutions[]` | **catalogue** | 855 | the second arm of that partition |
| ⭐ `resourceAnalysis.exploitation.unexploited[].processingInstitutions[]` | **catalogue** | 1,317 | the third arm, **never declared by version 2** — and the ONE place the catalogue list reaches a reader: `ResourcesTab.jsx:114` prints it as **"Needs: …"**, where naming absent houses is CORRECT |
| ⭐ `resourceAnalysis.gaps[].missing[]` | **catalogue** | 2,016 | what a chain needs and the town lacks; **0 exact matches**, invisible to the exact walk |
| ⭐ `economicState.activeChains[].upstreamMissing[]` | **catalogue** | 636 | the same needs-vocabulary on the economy's chains; **0 exact matches** |
| ⭐ `structuralSuggestions[].suggested[]` | **catalogue** | 136, **100 % catalogue names** | the structural validator's AUTHORED advice (`structuralValidator.js:718,726,734,…` — `['Town walls', 'City walls and gates']`), rendered as *"Consider: …"*. A statement about what the town **should build**, never a reference to what it has |
| ⭐ `economicState.compound.inst.names[]` | **frozen-stamp** | 17,361, **100 % normalised** | `getInstFlags` → `getInstitutionNames` returns the LOWERCASED roster beside the booleans it produced. `economicState.js:51`: *"RAW ROSTER, STAMPED ONCE … NEVER RECOMPUTED ON ADVANCE"*, owner-gated at `ENGINE_DEFECT_DISPOSITIONS.md §4`. **0 readers of `.names` in `src`.** Rewriting it would make the names disagree with the flags inside an owner-gated freeze |
| ⭐ `economicState.safetyProfile.compound.inst.names[]` | **frozen-stamp** | 17,361 | ⛔ **NOT an alias — version 3 said it was, and the measurement refutes it.** `economicState.js:62` and `safetyProfile.js:26` each call `getInstFlags` independently: `sameObjectLive 0 of 525`. Two equal-valued stamps, two rows. **Aliased stamps would still declare once per path** (the walker addresses by path, and a reloaded save has split any alias anyway) — which is why item 12 is a failure-message sentence and not an `alias` field |

### §6.1 · THE TWO OWNER-SIGNED CURES THIS PACKET IS WRITTEN AROUND

| cure | what it would change here | what this packet does about it |
|---|---|---|
| **FIX-D2** — the generator stores a criminal house's OWN name (273 of 525 golden rows) | **retires the `label` match kind on row 25**: the stored value becomes exact, so `exact` suffices | Row 25's `why` names FIX-D2 as its retirement condition. When it lands, the row's `match` flips `label → exact` and **nothing else moves**; the walker's normalised arm then finds that path at 0 % and the row is still declared |
| **FIX-D3** — the `Via` line names the house the town actually has (382 golden rows) | **row `activeChains[].processingInstitutions[]` becomes a REFERENCE** and joins the cascade | Declared NON_CASCADED with `kind: 'matched-pattern'` and FIX-D3 named. ⚠ **The visible price of the deferral is measured:** after a rename, **175 pattern values over 525 rows** still spell the old catalogue label on the Economics tab's `Via` line. That is by ruling, not by omission — **chair question 9** |

### ⭐ HOW THE CASCADE AND THE MIRROR COMPOSE (design §22.2 item 7)

The design classes `factions[].members[]` as a **MIRROR** — "a pure function of held facts, so it
is RECOMPUTED after the merge, never merged". **That recompute does not run for a rename**:
§22.1 ruling 7 rules that renames never go through re-derivation, and §20 ruling 2 applies a
`free-cascade` field ON SAVE. ⇒ **the cascade itself is the only thing that can heal the member
chip**, so it writes both homes with ONE declared field list, and any later mirror recompute is
then a no-op rather than a second answer.

Measured on RELOADED saves, joined **per person by the NPC's own `id`** (evidence §6):

```
pairedPeople                   4884    of whom 0 disagree before any edit      ← clean baseline
ONE-HOME walk (npcs[] only, all four scalar fields)   257 of 525 rows red      ← the defect
TWO-HOME walk (the cascade)                             0 of 525 rows red      ← the cure
```

### ⭐ VERSION 7 — THE VOICE RULE, SPELLED HERE BECAUSE A BUILD LANE MAY NOT CHOOSE IT (§P2.19, judgment 135)

⛔ **NO EM DASH AND NO EXCLAMATION POINT IN ANY `src/` STRING LITERAL OF EITHER LEAF.**
`tests/copy/voiceMechanics.test.js` Tier 2 walks `src/data` **and `src/domain`** and holds a file
it has no baseline row for at **ZERO** (`.voice-mechanics-baseline.json` is per-file
`{em, bang}`, **55 rows**, shrink-only; `src/domain/factionRename.js` carries **no row**, so this
packet's MODIFY — a COMMENT block, which the Tier-2 char scanner skips by construction — moves it
by **+0**). Tier 4's setting-agnostic tell ban walks the SAME `SCANNED_FILES` roots at HARD ZERO.
EM-B1c1 version 4 BUILT and then STOPPED on ONE character under a live seal; the cure cost a
version, a re-seal and a re-applied build.

- **The replacement is `': '`, never `' — '`**, in every `why`, every reason string and every
  failure message either leaf spells. A comma or a colon, never a dash.
- **Measured at the tip over the literals THIS PACKET SPELLS** — the 36 cascade paths, the 15
  `NON_CASCADED` paths, the eleven sentinel spellings, both `OrphanKind` members, both
  `LABEL_REWRITE` members, the five `kind` values, the six removal-rule names and both match kinds
  — **0 em dashes and 0 exclamation points** (executed with the walker's own char tokenizer).
  ⛔ **THE `why` PROSE IS THE ONLY UNSPELLED TEXT AND IT IS WHERE THE RISK LIVES**: §11 STOPs.
- ⚠ **`tests/copy/voiceMechanics.test.js` IS NOT A CHANGE ROW OF THIS PACKET** — EM-R0d v7
  reserves it on train EM-T14, and two members naming one non-row-keyed path refuse at placement.
  It is a §10 instrument and a §7 priced measurement only.

### Determinism, lifecycle, flags

- Hash/fork key: **NONE.** No draw, no clock, no locale, no `Math.random`. ⛔ Plain `toLowerCase`
  on the two label rows — `toLocaleLowerCase` would make the cascade locale-dependent.
- Stable enumeration: the declared list's own order; the removal walks arrays **backwards** so a
  splice cannot skip a neighbour.
- Flag: **NONE** (wave 1 is headless; the tier gate is the op's).
- Lifecycle: **create** — nothing; **read** — only keys the generator demonstrably writes;
  **persist** — no new key, ever; **reload** — the whole point; **regenerate** — untouched;
  **undo** — the store's snapshot, never an inverse sweep; **import/migrate** — nothing owed;
  **public veil** — nothing new crosses.
- **Golden posture: ⛔ UNCHANGED, and free by construction** — two pure functions nobody calls.
  A8 executes the digests anyway (§P3.1 makes motion a STOP).

---

## 7. Exact change manifest

| Action | File | Symbol/region | Max delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `src/domain/institutionRename.js` | `INSTITUTION_RENAME_SURFACES`, `NON_CASCADED_SURFACES`, `SERVICE_PROVENANCE_SENTINELS`, `applyInstitutionRenameToSettlement`, `institutionRenameChanges`, and the primitives the removal leaf imports | **≤250 eff; measured 217** | Copy `factionRename.js`'s architecture exactly: the frozen 36-row list with a `match`, a `removal` AND a `why` per row **plus a `rewrite` on each `label` row**; the frozen 15-row ledger with a `kind` **and a `readable`**; the eleven-spelling sentinel vocabulary; the minimal-clone immutable form. ⛔ Import `deepClone` and `NPC_HOMES` ONLY. ⛔ No `substituteWholeWord`, no `nameOf`, no prose surface, no `null` write, no case-folded match off rows 25–26, **no rewrite form spelled inline where a row declares one** |
| `CREATE` | `src/domain/institutionRemoval.js` | `applyInstitutionRemovalToSettlement`, `institutionRemovalChanges` | **≤90 eff; measured 74** | Import the vocabulary, the surface list and every primitive from the rename leaf — **declare nothing twice.** The five removal rules plus `report-only`, and the TWO-KIND `orphaned[]` report. ⭐ **The readable path is RESOLVED from `NON_CASCADED_SURFACES` (`kind === 'matched-pattern' && readable`), never spelled as a literal** — that is what makes `readable` load-bearing rather than decorative |
| `CREATE` | `tests/domain/institutionRename.test.js` | A1, A2, A3, A6, A7, A8 | `n/a` | ONE literal `describe`, **six straight-line `it`**, no `.each`/`runIf`/nesting (§P3.4 and brief step 14b, so the file lands CREDITED). ⛔ **Never bind the word `it`, `test` or `describe` to a variable or parameter** — 26 of the estate's parked files park for that alone. Every fixture JSON round-tripped through a `reloaded()` copy of `tests/domain/factionRename.test.js:76` (re-found by symbol at the tip). Negatives carry `// anchored:` on the line immediately above. ⭐ **VERSION 4 — THREE WALKER FENCES, MEASURED (§7's new rows):** ⛔ **no loop whose HEADER names `seed`/`seeds`/`SEED` may contain a bare `expect(`** — `tests/lint/seedLoopTotality.walker.test.js` scans ALL of `tests/`, its `FROZEN_BARE_SEED_LOOPS` is SHRINK-ONLY and its own header says *"never add a file"*, so a corpus walk written `for (const seed of …) { expect(…) }` reds with NO lawful cure but a rewrite; walk the corpus by ROW and name the binding anything but a seed, or adopt `collectSeedFailures` (`tests/helpers/seedFailures.js`). ⛔ **no `process.env.UPDATE_*` read** — `tests/lint/goldenFreeze.walker.test.js` enrols by `GOLDEN_ENV_PATTERN = /^UPDATE_[A-Z_]+$/` over the espree AST, and enrolment owes a row in `tests/fixtures/.golden-freeze-register.json`, which this manifest does not carry; A8 compares digests it computes and reads NO capture env. ⛔ **ZERO un-anchored `not.toContain` / `not.toMatch` / `not.toHaveProperty` sites** — otherwise `negativeAssertionAnchor.walker` needs a frozen row in its own source, which this manifest does not carry either (that is a STOP, §11) |
| `CREATE` | `tests/domain/institutionRemoval.test.js` | A4, A5, and the removal halves of A6, A7 | `n/a` | ONE literal `describe`, **four straight-line `it`**, same discipline and the same three walker fences. ⭐ **A4 (v) READS `src/generators/servicesGenerator.js` by `readFileSync`** — a source READ, never an import (the file is in the zero-slack worker closure), anchored on the declaration `addCrimeService = (name, desc, institution) =>`, asserted FOUND, and then extracting **every parenthetical string literal IN THE FILE** with **≥11** of them before any comparison (⭐ **version 8: a third-argument read reaches only seven, because four spellings have a different writer shape in the same file**) |
| `MODIFY` | `src/domain/factionRename.js` | `:168` `const NPC_HOMES` → `export const NPC_HOMES`, **and the `NON_CASCADED_SURFACES` ledger header** | **≤1 eff + a comment block (0 eff)** | ⭐ **R2, RULED: take it.** The estate then has ONE declaration of the two homes for both cascades. ⭐ **AND Q5's RULE, WRITTEN FOR THE ESTATE** in this file's `NON_CASCADED_SURFACES` header: *"never touched" means never WRITTEN — a non-cascaded path may still be READ, and a reader declares itself on the row.* ⛔ Nothing else is touched: **no row of `FACTION_RENAME_SURFACES` or `NON_CASCADED_SURFACES` moves, gains a field or changes a `why`** — the faction ledger stays exactly as it is; only its header gains the rule |

Generated artifacts: `NONE`. ⭐ **And that is a MEASUREMENT** — the edge-shared row below.

⛔ **THE TEST FILE BASENAMES ARE PART OF THE CONTRACT.** `institutionRename.test.js` and
`institutionRemoval.test.js` match **none** of `NAME_PATTERN`'s sixteen tokens (executed against
the regex at `tests/lint/mutationCoverage.shared.mjs:48`: both `false`;
`institutionRenameContract.test.js` and `institutionRenameCensus.test.js` both `true`). Renaming
either to carry `contract`, `census`, `walker` or `pin` would **silently create a
mutation-coverage obligation** this manifest does not carry.

### The registration ledger — every row priced against `32602dc60`

| # | Obligation | Verdict | Measurement |
|---|---|---|---|
| P2.1 | lighting census | **OWED — `+2 files`, INTERIOR RED** | ⛔ No absolute is quoted (§P2 row 1 as CORRECTED). The DELTA, derived from this packet's own CREATE rows: **`+2 files / +0 parked / +2 credited / +10 titles / +2 suiteTitles`**. ⭐ **CONFIRMED FROM THE WALKER ITSELF** at this tip: `titles` sums `liveTitlesIn(src)` and `suiteTitles` sums `liveSuiteTitlesIn(src)`, both over CREDITED files only (`:604-605`), and its own comment reads *"A CREDITED SUITE'S TITLE IS NOT EVIDENCE — it goes to `suiteTitles`"* (`:1478`) ⇒ the ten `it`s are titles and the two `describe`s are suiteTitles. Both files are NEW and register straight-line literal `it`s under ONE literal `describe`, so they land CREDITED rather than parked (`parkReasonsFor`, `:602-603`) |
| P2.2 | mutation-coverage row | **NOT OWED — EXECUTED** | `tests/domain` ∉ the eight `ENFORCER_DIRS` (`:36-45`); **neither basename matches `NAME_PATTERN`** (regex executed) |
| P2.3 | observed-shape exemption | **NOT OWED — no save-time key is read** | No `dmLayer`, no `decrees`. ⚠ The register is a per-file, per-finding-IDENTITY ceiling and **a NEW FILE has ceiling 0**. Every key read is measured PRESENT in the corpus (`dependency` 1,161/6,676, `corruptTies` 354/5,171, `secondaryAffiliation` 914/5,171, `linkedInstitutionIds` 287/5,171, `criminalInstitutions` 1,164, `institutionalServices` 1,764 — situational, therefore written). In `checks`; **motion is a STOP.** ⚠ **PLAUSIBLE, not CONFIRMED — the build lane executes it** (§12) |
| P2.4 | writer-reach | ⚠ **MEASURE AND RECORD — never write** | No new key is written ⇒ no new DARK identity. Adding readers can only SHRINK. A shrink is banked by the CHAIR through the instrument's own `--write` door; **GROWTH is a STOP.** ⚠ **PLAUSIBLE — the build lane executes it** (§12) |
| P2.5 | decision-fork / mechanism-coverage | **NOT OWED** | No seeded chooser, no pool, no `rollFrom`, no draw |
| P2.7 | prose-numerics, and every line-addressed register | **NOT OWED — SWEPT** | No figure is rendered. ⭐ **Brief step 13, executed:** `src/domain/factionRename.js` — the packet's only MODIFY — appears in **NONE of the 14 `tests/lint/.*-baseline.json` files** (all `grep -cF` → 0), so no register addresses it by line. ⚠ **Version 2 said sixteen; there are 14 at this tip** |
| ⭐ | **WIRING CENSUS (brief step 15), BOTH ARMS** | ⚠ **THE SHA-MAP ARM NOT OWED — MEASURED; THE PRODUCER-COUNT ARM OWED AS A DEFERRED ROW** | **The sha-map arm:** `docs/content/wiring-census.json`'s `stamp.files` holds **7 entries, all under `src/domain/display/stateProse/`**. `src/domain/factionRename.js` is **not stamped**, so `tests/lint/proseWiringCensus.walker.test.js` cannot red on `stale-bytes`. ⛔ **VERSION 4 STOPPED THERE, AND THAT WAS THE OMISSION** — the sha-map answer reads as *"the register does not move"* while this manifest CREATEs two `.js` leaves under a root the register counts by a DIFFERENT aggregate. ⛔ **THE PRODUCER-COUNT ARM — A DEFERRED ROW, interim rule 1.** `docs/content/wiring-census.json` — **a named INTERIOR RED, delta only.** `stamp.producerIndexFiles` **+2**, this member's new `.js` leaves under the two counted roots, named one by one: `src/domain/institutionRename.js` and `src/domain/institutionRemoval.js` (re-measured from this packet's own CREATE rows at `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`; both are under `src/domain/**`, neither under `src/generators/**`). `stamp.files` and `stamp.candidateLeaves` **UNMOVED** — both key on `src/domain/display/stateProse/` paths this member does not touch. `totals.*` **UNMOVED** — this member produces no pool, variant or relation. ⛔ **Regenerated WHOLE at the train's terminal, BY THE CHAIR** — a member never runs `node scripts/wiring-census.mjs`, because N lanes each regenerating a global COUNT produce N conflicting blobs. ⛔ **The path is in NEITHER this §7 table, NOR the `changeManifest`, NOR `checks`** — naming it would reserve the estate's most contended path against every sibling. |
| ⭐ | **CITATIONS BY LINE (brief step 16)** | ⭐ **NOT OWED — RE-SWEPT FOR VERSION 3.1** | ⚠ **Version 3.1's MODIFY now INSERTS lines** (Q5's comment block in the ledger header), so the "it shifts no line" argument version 3 relied on is gone and the sweep has to carry the verdict on its own. It does: `git grep -n "src/domain/factionRename.js:[0-9]"` across `src/`, `docs/content/` and `tests/` → **0 hits**, so no file addresses this one by line; it is in **0 of 14** line-addressed baselines; and it is **not** in `wiring-census.json`'s `stamp.files`. ⇒ inserting a comment block there is safe on all three registers |
| ⭐ | **edge-shared closure (§P2 row 10)** | ⭐ **NOT OWED — MEASURED** | Both CREATE paths are new files nothing imports, and **0 of 307** inputs across the five committed metas name `factionRename`, `clone.js`, `safetyProfile`, `computeActiveChains` or `priorityHelpers` |
| ⭐ | **bundle budgets (§P2 row 11)** | ⭐ **NO BUDGETED CHUNK — no TEST row, no build step** | §3b, measured two ways. Bytes priced anyway: **10,598 + 2,660 = 13,258 B minified** (esbuild 0.28.1). ⛔ **THE CURE IS THE PLACEMENT** and R1 makes it the consumers' |
| ⭐ | **declared-command write set + `checks` ORDER (§P2 row 12; brief step 14a)** | **CLEAN — AND NO GENERATOR IS AMONG `checks`** | Every command in `checks` was read for what it WRITES. `check-observed-shape-readers.mjs` and `check-writer-reach.mjs` are invoked as **readers** (no `--write`, no `--genesis`, no `--rebank`); `implementation-packets.mjs validate` writes nothing. ⇒ **no generator runs, so step 14a's "a generator goes LAST" has nothing to order** — recorded as a measurement, not an omission |
| ⭐ | **`tests/` sweep (§P2 row 12, second half)** | **NO TEST PATH OWED** | Swept by LITERAL: `institutionRename`, `institutionRemoval`, `INSTITUTION_RENAME_SURFACES`, `applyInstitutionRename`, `SERVICE_PROVENANCE_SENTINELS` and both `OrphanKind` spellings are **absent from `src/` and `tests/` entirely**. The key `orphaned:` has **2 occurrences, both comment prose** in unrelated test files. `NPC_HOMES` occurs only inside `factionRename.js`; no test pins its declaration form, so R2's `export` retires nothing. **And the set-consequence question, which version 3.1 must ask twice because it adds two fields to an existing shape:** `readable` and `rewrite` are new keys on THIS packet's own frozen lists, which no other module or test reads — `NON_CASCADED_SURFACES` and `INSTITUTION_RENAME_SURFACES` are CREATEd here. ⛔ **The faction ledger's rows are NOT given either field** (the MODIFY touches its header only), so no existing consumer of `factionRename.js`'s lists meets a new key |

| ⭐ | **VERSION 4 — `tests/lint` WHOLE (the 2026-09-20 addendum), OWED AND ADDED** | ⭐ **OWED — §10 CORRECTED** | The addendum is unconditional: *"a CREATE or RENAME of a test file under `tests/<dir>` opts into EVERY walker that governs `tests/<dir>` … run that directory WHOLE once in your instruments (`tests/lint` whole always)"*, written after runs 17, 18 and 19 turned out to be ONE family — a lane's new test file read by a walker the lane never ran. Version 3.1 named **one** lint walker; there were **172** `tests/lint/*.test.js` at `141a1d775` and **177** at `bdbf7c89c` (ESTATE-REPAIR-3) — ⭐ **which is the point: the denominator moves, so the ELEVEN are named individually and the count is never the contract.** Measured, **eleven** of them scan the whole `tests/` tree and therefore read these two new files the day they land: `contractTestAntiVacuity` (`walk(join(ROOT,'tests'))`), `controlBytes` (`SCAN_DIRS=['src','tests']`), `goldenFreeze` (`walk(join(ROOT,'tests'))`), `negativeAssertionAnchor` (`SCAN_ROOTS=['tests']`), `netCurrentExtractorAnchor`, `proseDrawnAnchors`, `pulseKernelLineAddress`, `seedLoopTotality`, `sovereigntyLightingContract`, `testRatchet` and `worldGenerationClockSeam`. ⛔ **`tests/lint` WHOLE is now a §10 instrument.** Three of the eleven impose a shape and are fenced in the CREATE rows above; the rest are measured clean |
| ⭐ | **VERSION 4 — `contractTestAntiVacuity` SCOPE, EXECUTED** | **NOT OWED — OUT OF SCOPE, and that is a measurement** | Its `inScope` admits `tests/security/**/*.test.*`, `tests/**/*.contract.test.*` and `tests/lint/*.test.js`; its fold scope (Rules 3–4 only) adds `tests/edgeFunctions`, `tests/build`, `tests/lib`. **`tests/domain` is in neither**, so A1 (iv)'s deliberately hardcoded VERSION-2 lists — a literal exhaustive set, which is Rule 2's exact shape — cannot convict. ⚠ **Recorded as a boundary, not a licence:** were either file ever renamed to `*.contract.test.js` it would enter scope, and Rule 2 is file-gated on *"derives nothing from source"*, which A4 (v)'s `readFileSync` now discharges for the removal file. **Do not rename either file** (the basenames are already contractual above) |
| ⭐ | **VERSION 4 — THE OBSERVED-SHAPE REGISTER, NAMED AT LAST** | **NOT OWED — MEASURED, and version 3.1 never named it** | `scripts/.observed-shape-readers-baseline.json` **names `src/domain/factionRename.js` four times** — an `inventory` row (`"interSettlementRelationships on settlement": 4`) and a `sha256`/`size` entry in each of `manifests.{scanTree,sourceTree,executionTree}`. It sat OUTSIDE version 3.1's sweep, which covered only the fourteen `tests/lint/.*-baseline.json` files. It is harmless, for two measured reasons and not by luck: **(a)** its own `_doc` states *"The line number is excluded so unrelated line churn does not rewrite the governed identity"*, so Q5's comment block moves no row; **(b)** `provenanceDriftOf` compares shas only for `detectorTree` and for **the UNSCANNED half** of the subject tree (`if (scannedNow.has(entry.path)) continue`), and `factionRename.js` is IN `scanTree` — so its sha changing is absorbed, not refused. ⛔ **THE ONE LIVE CONSTRAINT:** the identity is content-addressed with **ceiling 4**, so the Q5 comment block must not contain text the detector reads as a key access; it is prose about a ledger rule and contains none. `check-observed-shape-readers.mjs` stays in `checks` and motion stays a STOP |
| ⭐ | **VERSION 4 — WRITER-REACH, RE-MEASURED** | **NOT OWED** | `scripts/.writer-reach-baseline.json` names `factionRename` **0 times** (`grep -cF` → 0). `SURFACE_ROOTS` is **SIX** classes at the tip (`web`, `dossier-pdf`, `campaign-pdf`, `world-book`, `foundry`, `json-export`; imported from `scripts/lib/writer-reach-scan.mjs`, never re-typed) and neither new leaf is reachable from any root at landing, because nothing imports them |

### ⭐ VERSION 7 — THE SIXTH AMENDMENT'S INSTRUMENT LIST, EVERY FIGURE EXECUTED AT `bfca6b7d6` AS A DELTA

⛔ Each row below ran the instrument's **OWN** logic in plain `node` over the tip as it stands and
over this packet's planted text (§P2.21's method). A figure adopted by hand is the failure.

| # | Instrument | Verdict | Measurement, and the DELTA |
|---|---|---|---|
| **P2.17** | the TUNING INVENTORY (`scripts/lib/tuning-inventory.mjs`; `tests/lint/tuningRegister.walker.test.js`, EXACT in both directions per file, new files at ZERO) | **NOT OWED — EXECUTED** | The library was imported and its OWN `countUnregisteredNamed` and `countBareDecimals` run at the tip: `src/domain/factionRename.js` → `{ counts: {}, sites: {} }` and `{}` (**0 dials**), so the MODIFY moves nothing. Both new leaves are **frozen string tables and walk logic with no module-top-level `const UPPER_SNAKE = <number>` and no fractional decimal literal**: predicted **0 / 0**, which is what a new file must be. ⛔ A number that turns out to be a genuine simulation dial is a STOP (§11, §P11.3) |
| **P2.19** | the VOICE WALKER's counter (`tests/copy/voiceMechanics.test.js`, Tier 2 + Tier 4 over `src/data` + `src/domain`) | ⭐ **OWED AS A MEASUREMENT — §6 SPELLS THE RULE** | Baseline `.voice-mechanics-baseline.json` holds **55** per-file `{em, bang}` rows (311 em, 8 bang estate-wide); `src/domain/factionRename.js` has **no row**, so a new file is held at **ZERO** and the MODIFY (a comment block, skipped by the Tier-2 char tokenizer) is **+0**. Run over every literal this packet SPELLS: **0 em dashes, 0 exclamation points**. ⛔ **NOT a change row** — EM-R0d v7 reserves that path on train EM-T14 |
| **P2.20** | the REVIEWED-SET PIN and the LITERAL-NAME DISPATCH (`tests/application/commands/commandRegistry.test.js`; `tests/store/deadOperationRatchet.test.js`) | **NOT OWED — MEASURED** | ⭐ **This packet registers NO application command and makes NO store edit.** Its deliverable is two pure domain functions with **0 direct production consumers at landing** (§3); the capability-set pin is untouched by construction, and the ratchet's "a dispatch the scanner can SEE" rule has nothing to see. The row that WILL owe both is the APPLY POINT (`EM-C4a` / `EM-D0`), per §3b |
| **P2.21 (a)** | the ENTROPY-ROOT CENSUS, **BOTH ARMS** (`tests/lint/entropyRootCensus.walker.test.js`: the `WORLD_ROOTS` read-site arms AND the `createPRNG(` SITE-COUNT pins, corpus `src/` WHOLE per the sixth's C4) | **NOT OWED — EXECUTED, DELTA `+0 / +0`** | Counted at the tip with the walker's own needle: **`createPRNG(` = 36 under `src/domain`** (1,067 files) **and 47 under `src/` whole** (2,265 files). Neither leaf mints a stream, reads a world root or consumes a draw (§6 "Determinism": no draw, no clock, no locale, no `Math.random`), so both pins are **unmoved**. ⚠ A rename re-seeds the glyph at `src/domain/townMap/glyphAssign.js:125` (`createPRNG('glyph:<name>')`, re-read at the tip) — that site is **EM-P1b's class** by design §21.5 ruling 7 and is NOT touched here |
| **P2.21 (b)** | the RUIN-FILTER ROSTER (`tests/lint/ruinFilterRoster.walker.test.js`) | ⭐ **DISPOSITION DECLARED — NOT ENROLLED, and the count is EXACT so this is the one arm at risk** | The walker source-scans **`src/domain`** for `READER_RE = /\.institutions\b/` over `codeOnly(src)` (comments AND string/template CONTENTS blanked, offsets kept) and pins `expect(readers.length).toBe(94)` — **reproduced here by running its own two regexes over the tip: 94 readers, 27 compliant**, and `src/domain/factionRename.js` IS in the set, exempted under `mutation` (`tests/lint/ruinFilterRoster.walker.test.js:160`). ⛔ **BOTH NEW LEAVES RESOLVE EVERY SURFACE FROM A DECLARED STRING PATH** (`'institutions[].name'`, `'defenseProfile.institutions.garrison[].name'`, …), and `codeOnly` blanks string contents, so **no `.institutions` token exists in executable code and the discovery set stays 94 — DELTA `+0`, no exempt row, no §7 TEST row, no reserved path.** ⛔ **THE STOP (§11): the moment the implementer spells `.institutions` in executable code the arm reds `expected 96 to be 94`** and the lawful cures are two `RUIN_AGNOSTIC_EXEMPT` rows in the `mutation` shape (`factionRename.js`'s own row is the model) — which makes the walker a §7 TEST row and is the CHAIR's to grant. **RAISED Q11** |
| **P2.21 (c)** | the GOODS ROSTER (`src/domain/goods.schema.js`'s ST-2 roster, parsed by `tests/build/vendorPdfLazy.test.js`) | **NOT OWED — MEASURED** | Neither leaf imports a goods identity half-table: the whole runtime import list is **`deepClone` from `src/domain/clone.js` and `NPC_HOMES` from `src/domain/factionRename.js`, and nothing else** (§7's CREATE instruction makes that exact). No `src/data/resourceData.js` or kin. ⛔ **AND `tests/build/vendorPdfLazy.test.js` IS NOT A CHANGE ROW** — EM-B2a4 v2 reserves it on train EM-T13, and §P10.8 makes it a PATH-reserved register |
| ⭐ **P2.16 / judgment 124** | LINE-ADDRESSED REGISTERS, **INCLUDING THIS PACKET'S OWN `requiredSymbols` `_note`s** | ⭐ **OWED AND CURED IN THIS VERSION** | Version 6 swept the fourteen `tests/lint/.*-baseline.json` files and the wiring census and found `src/domain/factionRename.js` in none; **re-run at the tip, that still holds** (`git grep -n "src/domain/factionRename.js:[0-9]" -- src docs/content tests` → 0 hits; not in `stamp.files`' seven entries). ⛔ **BUT JUDGMENT 124 EXTENDS THE REGISTER TO THE MANIFEST**, and version 6 was written the day before it: the `_note` of `requiredSymbols` row 3 addressed **`:288`** (`simulationTrace[].causes[].reason`) and **`:289`** (`economicState.safetyProfile.criminalInstitutions[]`), both of which sit **BELOW** the ledger header this packet's own MODIFY inserts a comment block into. Both lines were re-read at the tip and still name their subjects; **version 7 STRIKES the two bare addresses** and keeps the rows' symbol claims, so no address of this packet's can be staled by its own edit. ⚠ The citation walker's gated arms are past-EOF only (ARM 1 / ARM 2) and an INSERTION cannot push a line past EOF, so this was a LAW obligation rather than a predicted red — recorded as such |

> ⛔ **THE CENSUS ROW IS DEFERRED** — no edit; the manifest omits the path. Predicted interior
> red, SHAPE ONLY: `the estate's file count moved — re-measure, do not re-word: expected <N> to be <N-2>`.
> ⭐ **RE-MEASURED AT `bfca6b7d6` (history, as-of):** `stamp.producerIndexFiles` reads **1,181**
> (version 5 read 1,171 at `bdbf7c89c`), `stamp.files` holds **7** entries and `stamp.candidateLeaves`
> **6**, none of them a path this member touches; the lighting baseline reads
> **`2683 · 359 · 2324 · 25713 · 6837`** as of `e7d9fb7c5`. ⛔ **Both absolutes are history with an
> as-of mark (§P3.5); the DELTAS above are the packet's only claims**, and `tests/lint` now holds
> **182** `*.test.js` files (version 5 measured 177) — which is again the point: the denominator
> moves, so §10 names its walkers one by one and the count is never the contract.

---

## 8. Ordered coding sequence

0. Dispatch and seal; re-read `git rev-parse HEAD` in the same command; re-confirm disjointness
   against the kit's DRAFTs (R7).
1. **Capture the baselines.** (a) `sha256` of `tests/fixtures/generator-golden-master.json` and of
   the prose-manifest fixture; (b) `node scripts/check-writer-reach.mjs` and
   `node scripts/check-observed-shape-readers.mjs`, **recorded, never written** — ⭐ **their
   verdicts are this lane's PLAUSIBLE predictions and the build lane's to CONFIRM** (§12);
   (c) ⭐ **the pre-fix defect, captured**: over A1's stride, the worst-case institution per row
   and its handle count — expect **4,034 handles over 525 rows / 532 over the 63-row stride**.
   This is A2's oracle and it must exist BEFORE the fix.
2. Add both test files with A1–A8 **failing**.
3. Implement `src/domain/institutionRename.js`.
4. Implement `src/domain/institutionRemoval.js`, importing everything shared.
5. Apply R2's one `export` token to `src/domain/factionRename.js`.
6. Registrations: none owed (§7). The prevention guard is A1's **three-arm** totality walker
   **plus version 4's two chair-fated pins** — A1 (vi) `LABEL_REWRITE` closed at two, and
   A4 (v) the generator-to-ledger set-equality. ⛔ **Both are red-first like everything else:
   write them before the module declares a second rewrite form or an eleventh sentinel.**
7. Run focused verification (§10), including the writer-reach and observed-shape comparison
   against step 1.
8. Run the wave-end gate per the host train's plan; write the completion receipt.

**Bounded algorithm (the cascade):**

```text
1. Guard: not a record, empty name, or oldName === newName ⇒ { changed: false, touched: [] }.
2. For each declared surface, in the list's own order, visit its stored container and apply
   the row's DECLARED match: `exact` (String(value).trim() === oldName) on 34 rows,
   `label` (trim().toLowerCase() === oldName.toLowerCase()) on rows 25 and 26 ONLY.
3. On a match: set (scalar) or set-the-item (string list), through the row's DECLARED
   `rewrite` form — `verbatim` on every `exact` row and on row 25, `lower` on row 26.
   ⛔ The form is read off the row; it is never spelled at the call site. Mark the surface touched.
4. Both NPC homes are walked with ONE declared field list; a nested parent record must
   ALREADY exist and is never minted.
5. Return { changed: touched.length > 0, touched }.
```

**Bounded algorithm (the removal sweep):** the identical walk; at step 3 apply the row's declared
removal rule (DROP THE RECORD · DROP THE ENTRY · DROP THE ITEM · DELETE THE KEY · DELETE THE
`dependency` OBJECT · REPORT-ONLY), walking arrays BACKWARDS; then **READ — never write — the ONE
`readable: true` row resolved from `NON_CASCADED_SURFACES`** and append an `orphaned` note when a
chain names the removed house among its processors and names no surviving roster house, or when a
chain's `label` names the removed house — **and continue.**

---

## 9. Acceptance matrix

| ID | Case | Required observation |
|---|---|---|
| **A1** | ⭐ **THE INDEPENDENT DENOMINATOR (the prevention guard) — THREE ARMS AND A RED CONTROL** | **(i) THE EXACT ARM.** A walk of the WHOLE settlement blob over the **63-row structured sample** reports the path shape of every string exactly equal to a roster institution name, and **every shape must appear in `INSTITUTION_RENAME_SURFACES` or in `NON_CASCADED_SURFACES`.** It copies `tests/domain/factionRename.test.js:190` and **never reads the module's declarations for its denominator**. Anti-vacuity FIRST: **≥40** distinct shapes (measured **42**) and **≥2,000** institutions (measured 2,428 / 17,361 at 525). **(ii) ⭐ THE CATALOGUE-NAME ARM (§934.61).** The same walk, for every string equal to one of the **280** names in `institutionalCatalog`: every path whose values are catalogue names in the MAJORITY (≥50 %) must be declared. Floor: **≥1** such path (measured **1** — `structuralSuggestions[].suggested[]`, 136 of 136). **(iii) ⭐ THE NORMALISED-NAME ARM (§934.61).** The same walk, normalised (lower-cased, apostrophes stripped, whitespace collapsed): every path whose values are normalised-equal to a roster name in the MAJORITY must be declared. Floor: **≥4** such paths (measured **4** — the two label rows and the two frozen stamps). **(iv) ⛔ THE RED CONTROL, REQUIRED:** run arms (ii) and (iii) against **version 2's two lists** and assert they find **≥1 undeclared path** — measured **5**. Without it the arms are unfalsifiable. **(v) THE DISJOINTNESS ARM (R3 ii):** no `secondaryAffiliation` value may be BOTH a roster institution name and a roster faction name. ⭐ **ALL FOUR FLOORS ARE RAISE-ONLY (Q7, RULED): ≥40 shapes · ≥1 catalogue path · ≥4 normalised paths · ≥1 red-control finding.** ⛔ **THE FAILURE MESSAGE CARRIES TWO SENTENCES A LATER READER WILL NEED.** (a) *A `catalogId` path crossing the 50 % threshold is a CATALOGUE-SHAPE change, not a missing cascade row* — today `institutions[].catalogId` sits at 12.6 % and `defenseProfile.institutions.garrison[].catalogId` at 40.3 %, and a catalogue whose ids drift toward its names would cross the line and red this arm for a non-defect. (b) *Aliased or duplicated stamps DECLARE ONCE PER PATH by design* — the walk addresses by path, and a reloaded save has already split any alias, so two rows for two stamps is the correct ledger and not a duplicate. ⭐ **(vi) VERSION 4 — THE `LABEL_REWRITE` CLOSED-VOCABULARY PIN (chair-fated, addendum 37).** In `OrphanKind`'s exact shape, and every half is required because each one alone is satisfiable by a defect: **(a)** the frozen rewrite map's **key set is exactly `{'verbatim','lower'}`** — size 2 asserted, so a third form reds the day it is added rather than the day a DM reads a mangled label; **(b)** **every `label` row's `rewrite` is a member of that key set** (2 of 2 rows, derived from `INSTITUTION_RENAME_SURFACES`, never re-typed); **(c)** **no `exact` row carries a `rewrite` field at all** (34 of 34), so the field cannot drift into meaning "verbatim by omission" on a row whose match kind forbids the question; **(d)** ⛔ **BOTH MEMBERS ARE OBSERVED FIRING** — `verbatim` on row 25 and `lower` on row 26, executed through the cascade, never asserted from the declaration, so the pin cannot pass over a vocabulary whose second member is dead. ⛔ Anti-vacuity: assert the map is non-empty and the label-row set is non-empty BEFORE the membership assertions, so an empty derivation cannot pass all four |
| **A2** | ⭐ **THE RENAME IS TOTAL ON REAL PIPELINE DATA — the packet's central claim** | For each row of the stride, on a **RELOADED save**, rename the institution carrying the most cascade-row handles. **Measured over 525 rows: 4,034 handles before; ZERO still spelling the old name on ANY of the 36 declared cascade rows; ZERO new dangling reference joins.** The residue is on declared `NON_CASCADED` paths only and is asserted **by path and by count**: `simulationTrace[].downstreamEffects[].target` **550** and `economicState.activeChains[].processingInstitutions[]` **175** (the measured price of FIX-D3's deferral). ⭐ Guard-the-guard: assert `handlesBefore > 0` and `changed === true` **before** the staleness assertion |
| **A3** | ⭐ **THE MEMBER MIRROR — proved by a CONTROL PAIR, not asserted** | Joined **per person by the NPC's own `id`**, on a reloaded save: (i) baseline — **4,884 paired people, 0 disagreeing before any edit**; (ii) ⛔ the NEGATIVE CONTROL — the identical cascade with the SECOND home dropped leaves a disagreeing person in **257 of 525 rows**; (iii) the cascade's two-home walk leaves **0 of 525**. The control is required: without it "both homes agree" is true of a fixture where neither moved. ⓘ `factions[].members[]` is a strict SUBSET of `npcs[]`, so the comparison must be per person |
| **A4** | ⭐ **THE REMOVAL SWEEPS THE SAME LIST — the DELTA-BY-KIND gate** | Removing the same institution adds **ZERO new dangling joins of any kind**, measured **against the record's own pre-existing baseline, never an absolute** (design §22.1 correction 6). ⭐ **AND THE BASELINE IS NOW ITSELF A FINDING: over the TRUE reference set it is 0 on 525 of 525 rows** — version 2's "7,399, city median 23" counted four catalogue/pattern paths and the eleven sentinels as references. The gate stays a DELTA because a SAVED record may carry dangles the generator never wrote. ⭐ All **five** removal kinds must be observed firing (measured: all five, over 525 removals). ⭐ And the PARTITION holds: `resourceChains[] == fullyExploited ∪ partiallyExploited ∪ unexploited` on **525/525 before, 525/525 after a rename, 525/525 after a removal** — the invariant version 2's cascade would have broken. ⭐ **(iv) FIX-D5, THE SENTINEL VOCABULARY (Q4, RULED IN).** The denominator excludes a sentinel **by `SERVICE_PROVENANCE_SENTINELS`, never by `startsWith('(')`** — and the arm pins why that is safe and what it protects. **THE CONTROL:** over 525 rows, **ZERO** parenthetical values at `availableServices.*[].institution` fall outside the declared eleven, so the swap is behaviour-identical today (the baseline is 0 either way) and falsifiable tomorrow. **THE PIN:** the split is **3 live** — `(arcane underground)` 246 · `(smuggling)` 173 · `(covert)` 2, on 260 of 525 rows — and **8 dark** (`(lawless)`, `(informal)`, `(state apparatus)`, `(criminal governance)`, `(religious fraud)`, `(commercial crime)`, `(street gang)`, `(thieves guild)`). A generator edit that lights a dark spelling, or darkens a live one, reds here and reaches the chair instead of quietly entering the denominator as a dangling house. ⭐ **(v) VERSION 4 — THE SET-EQUALITY ARM, AND IT IS THE ONE THAT WOULD HAVE CAUGHT THE TWELFTH (chair-fated, addendum 37: *"a set-equality arm joining `servicesGenerator.js`'s sentinel literals to the domain's declared list"*).** (iv)'s control is a CORPUS control — it proves no VALUE in 525 rows falls outside the list. It is blind to the only failure that has actually happened: **the declared list and the generator's source disagreeing**, which is how `'(background crime)'` rode from FIX-D1 through §934.61 and addendum 34 into version 3's draft with nobody joining the two. So: read `src/generators/servicesGenerator.js`, extract **every parenthetical string literal IN THE FILE** (⭐ **VERSION 8, judgment 183: version 7 read `addCrimeService`'s THIRD ARGUMENT and the build measured SEVEN of the eleven that way, because four spellings are written by the direct `buckets.criminal.push({ ...item, institution: '(…)' })` shape instead; the file-wide read is what §5 and §6 always executed**), and assert the extracted set is **SET-EQUAL** to `SERVICE_PROVENANCE_SENTINELS` — **both directions named separately** (a literal the generator writes and the list lacks; a list entry the generator no longer writes), because a one-way containment is how a retired spelling rots in a frozen list. ⛔ **THE EXTRACTION IS ANTI-VACUOUS BY CONSTRUCTION AND THAT IS NOT OPTIONAL:** a marker-slice source scan that silently returns nothing is the estate's own live defect class (run 17's one red), so the arm asserts the anchor `addCrimeService = (name, desc, institution) =>` is **FOUND** and that the extracted set has **≥11 members** before it compares anything. ⭐ **THE ANCHOR IS THE ANTI-VACUITY HALF AND IT IS UNCHANGED IN VERSION 8**: it is what refuses a scan that has gone quietly empty because the file moved. What version 8 changes is only the EXTRACTION the anchor guards, from one writer shape to every parenthetical literal the file spells, so the arm measures the file rather than one way of writing into it. ⛔ **`servicesGenerator.js` is READ, never imported and never edited** — it is in the generation worker's zero-slack closure (§3b), so a `readFileSync` is the only lawful join |
| **A5** | ⭐ **THE ORPHAN REPORT IS A REPORT, NOT A REFUSAL — AND IT HAS TWO KINDS** | **(i)** A chain that names the removed house among its processors and names no surviving roster house is reported as `{ kind: 'chain-lost-its-last-processor', … }` — measured **125 over 525 removals**. ⛔ **The path it reads is `NON_CASCADED`: the report READS it and never WRITES it.** **(ii)** A chain whose `label` names the removed house is reported as `{ kind: 'chain-label-names-a-removed-house', … }` and **the label is NOT deleted** (present on 6,676/6,676) — measured **28 over 525 removals, 20 of them on a chain that kept a surviving processor.** ⛔ The sweep COMPLETES in both cases: `EM-PREAMBLE` §P8 makes "a guard would refuse rather than offer `proceed`" a STOP and design §2.7 makes guards suggestive, so **this writer cannot refuse**; whether the DM is warned is **EM-C2's** judgment (R6). Asserted with its negative: `changed` is still true and no path is left half-swept. ⭐ **(iii) THE READ IS DECLARED, NOT ASSUMED (Q5, RULED).** The note's `path` must be **the path the module RESOLVED from `NON_CASCADED_SURFACES`** (`kind === 'matched-pattern' && readable === true`), not a literal — asserted by comparing the emitted `path` against that lookup, and by asserting **exactly one** row carries `readable: true`. ⛔ Flip that row to `false` and the module throws at import; the arm makes the flag load-bearing instead of decorative |
| **A6** | **WHAT IT MUST NOT TOUCH — five arms** | **(i)** A sibling institution keeps its name. **(ii)** Every declared `NON_CASCADED` path is **byte-identical** before and after, asserted by name — the two receipts, the six catalogue paths and the two frozen-stamp aliases especially. **(iii) No record GAINS a key it did not carry** (deep key-set equality; measured **0 of 525**). **(iv)** The chain `label` survives a REMOVAL (row 24's `report-only`), asserted directly; a no-op rename reports `changed: false`, `touched: []` and mutates nothing. **(v) ⭐ THE FACTION-HANDLE PIN, EXECUTED:** on the **156 rows** that hold a `Thieves' guild chapter` institution, renaming it leaves **every one** of the `"Thieves' Guild"` `secondaryAffiliation` values untouched — **156 of 156** — because the match is exact and NOT case-folded. ⛔ **This arm is the one that fails first if anyone "fixes" the case near-miss.** **(vi) ⭐ THE LEDGER'S ONE PROSE-BACKED RULING IS NOW ASSERTED DIRECTLY (item 6).** `availableServices.legal[].name` is ruled NON_CASCADED because where it names a roster house it names a **different** house from the entry's own `institution`. Re-measured at this tip rather than inherited from version 1: **5,001 entries; `name === institution` on 0; 105 hold a roster house's name and all 105 differ from the sibling `institution`.** The arm asserts both halves, so the ruling rests on a live measurement instead of a three-version-old sentence. Every negative carries `// anchored:` on the line above |
| **A7** | **THE IMMUTABLE FORMS** | `institutionRenameChanges` and `institutionRemovalChanges` return **only the touched top-level buckets**, never mutate their input (deep-equal against a pre-call clone, plus `not.toBe` on every returned bucket), carry the **same `touched` set** as the in-place forms, and the removal form carries the **same `orphaned` notes**. Measured **525 of 525 on all three** |
| **A8** | **GOLDEN POSTURE UNCHANGED** | `generatorGoldenMaster` and `dossierProseManifest` bytewise unchanged, asserted as the fixture digests before and at the tip. Free by construction — two pure functions nobody calls — and executed anyway, because §P3.1 makes motion a STOP |

**8 of ≤8.**

---

## 10. Verification commands

```sh
npx eslint src/domain/institutionRename.js src/domain/institutionRemoval.js \
  src/domain/factionRename.js \
  tests/domain/institutionRename.test.js tests/domain/institutionRemoval.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/domain/institutionRename.test.js tests/domain/institutionRemoval.test.js \
  tests/domain/factionRename.test.js tests/domain/npcRename.test.js

# The goldens §P3.1 makes a STOP.
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js

# R2 touches a file two store suites pin.
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/store/factionRenameConvergence.test.js tests/store/settlementSlice.test.js

# ⭐ VERSION 4: the ONE walker version 3.1 named — kept, because it is the one with a shape rule.
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/negativeAssertionAnchor.walker.test.js

# ⭐ VERSION 5: THE GOVERNING LINT FILES — SEALED in `checks` (interim rule 8). These are the ten
# tree-wide walkers §7 enumerated (the eleventh, sovereigntyLightingContract, is the interior red
# and runs ALONE below), plus ruinFilterRoster from the name arm of the rule-8 computation
# (`git grep -l -F factionRename -- tests`).
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/contractTestAntiVacuity.walker.test.js \
  tests/lint/controlBytes.test.js \
  tests/lint/goldenFreeze.walker.test.js \
  tests/lint/negativeAssertionAnchor.walker.test.js \
  tests/lint/netCurrentExtractorAnchor.walker.test.js \
  tests/lint/proseDrawnAnchors.walker.test.js \
  tests/lint/pulseKernelLineAddress.walker.test.js \
  tests/lint/seedLoopTotality.walker.test.js \
  tests/lint/testRatchet.test.js \
  tests/lint/worldGenerationClockSeam.walker.test.js \
  tests/lint/ruinFilterRoster.walker.test.js

# ⭐ VERSION 7: THE `src/domain` SIDE OF THE SAME COMPUTATION — SEALED. Version 5 enumerated only
# the walkers that read the two new TEST files; these four read the two new `src/domain` LEAVES,
# and each is a §7 priced instrument above (§P2.17 · §P2.19 · §P2.21(a) · the clone seam from
# judgment 148's import-roster re-derivation over `src/domain/clone.js`).
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/entropyRootCensus.walker.test.js \
  tests/lint/tuningRegister.walker.test.js \
  tests/lint/deepCloneHotPath.test.js \
  tests/copy/voiceMechanics.test.js

# ⛔ VERSION 5: THE BUILD LANE'S INSTRUMENTS — NOT sealed checks. This is the 2026-09-20
# directory-whole addendum, unconditional for any tests/ CREATE, split into its two verdicts,
# which is the only shape a seal can carry.
#   (a) the EXCLUDED directory run — MUST EXIT 0
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint --exclude=tests/lint/sovereigntyLightingContract.walker.test.js
#   (b) the LIGHTING WALKER ALONE — the named census interior red; its message must match §7's
#       predicted DELTA, never a tuple copied from this document
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/sovereigntyLightingContract.walker.test.js

node scripts/check-observed-shape-readers.mjs   # compare against step 1; MOTION IS A STOP
node scripts/check-writer-reach.mjs             # compare against step 1; RECORD, never --write
node scripts/implementation-packets.mjs validate
npm run check:packet -- EM-R6
npm run implementation:resume -- EM-R6
npm run check:tail
```

⭐ **No generator is among these commands** (brief step 14a), so nothing re-stamps a declared path
mid-chain and no step is ordered last for that reason.
Expected: every command exits `0`, **except** the named census interior red until the host train's
terminal. ⛔ Never read a gate through a shell pipe (§P7). ⛔ Never wrap `npm run check` in the
mutex. ⛔ `gate-mutex.sh` gives up after its poll budget and **exits 3** (`GAVE UP`) — ⛔ **never 0**; the chair retracted the "exits 0" claim on 2026-09-20 with three versions of the script checked. A gate line with no
printed test count DID NOT RUN.

---

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and §P8, stop if: the seal is missing or foreign; HEAD is not
the stamped base or a descendant proved non-interfering; **a golden or the prose manifest moves**;
`check-observed-shape-readers` moves; `check-writer-reach` shows **growth**; either leaf exceeds
**250 effective lines** (measured 217 / 74); a **third** leaf appears necessary (that is §3a's
anchor walk — it **returns to the chair**, R4); **a SECOND `NON_CASCADED` row appears to need
`readable: true`** (the rule is the estate's now, and a second reader is the chair's to grant);
**a rewrite form appears to be needed at a call site rather than on its row**, or a THIRD rewrite
form appears necessary; **a parenthetical value appears at `availableServices.*[].institution`
that the declared eleven does not cover** (the denominator would silently count it as a dangling
house — that is FIX-D5's whole point); a `null` would have to be written where §6 says
DELETE; a chain `label` would have to be deleted; **a case-folded match appears necessary on any
row but 25 and 26** (the faction handle is the reason — A6 v); **`toLocaleLowerCase` appears
anywhere** (it would make the cascade locale-dependent); the removal would have to **REFUSE**
rather than report (§P8 forbids it); a THIRD `OrphanKind` appears necessary (the vocabulary is
closed at two and EM-C2 branches on it); **the removal would have to WRITE a `NON_CASCADED` path
to produce its report**; **the frozen rewrite map's key set is anything but exactly `{'verbatim','lower'}`**, or an `exact` row appears to need a `rewrite` field (A1 vi); **`servicesGenerator.js`'s parenthetical literals and `SERVICE_PROVENANCE_SENTINELS` are not SET-EQUAL in EITHER direction**, or A4 (v)'s anchor `addCrimeService = (name, desc, institution) =>` is not FOUND, or **the FILE-WIDE parenthetical extraction that anchor guards returns fewer than eleven literals** (a silent source scan is the run-17 defect class; ⭐ **version 8 re-points this sentence at the file-wide read, because under version 7's third-argument read it convicted a WRITER SHAPE rather than a defect and STOPPED the build at seven**); **either new test file would need a row in `tests/lint/seedLoopTotality.walker.test.js`'s `FROZEN_BARE_SEED_LOOPS`** (shrink-only — there is no lawful cure but a rewrite), **in `negativeAssertionAnchor.walker`'s frozen map**, or **in `tests/fixtures/.golden-freeze-register.json`** (all three registers live outside this manifest); **`tests/lint` whole reds on anything these two files caused**; `substituteWholeWord`, `nameOf` or any prose surface appears necessary;
⭐ **VERSION 7's FOUR NEW STOPS, each with its measured arm:** **an em dash or an exclamation point
would enter a `src/` string literal of either leaf** (the Tier-2 baseline holds a new file at ZERO
and EM-B1c1 v4 paid a version, a re-seal and a re-applied build for one character — the
replacement is `': '`, §6); **`.institutions` would be spelled in EXECUTABLE code in either leaf**
(the ruin-filter roster's discovery set is pinned EXACT at **94** and would red
`expected 96 to be 94`; the surfaces are resolved from declared STRING paths, and the only lawful
cure is two `RUIN_AGNOSTIC_EXEMPT` rows in the `mutation` shape, which is the CHAIR's to grant —
Q11); **`JSON.parse(JSON.stringify(` would be written in either leaf** (`tests/lint/deepCloneHotPath.test.js`
bans it across `src/store` and `src/domain` outside `clone.js` itself — `deepClone` is the seam);
**a module-top-level `const UPPER_SNAKE = <number>` or a fractional decimal literal would enter
either leaf** (the tuning inventory holds a new file at ZERO and a genuine simulation dial is
owner-signed, §P11.3). ⭐ **AND ONE ANCHOR CORRECTION THE SIXTH AMENDMENT MAKES (§P6, EM-B1c1
version 5's build red):** a `// anchored:` marker counts only on **the line immediately above the
`expect` ITSELF, never above a WRAPPED `expect`'s first line** — a wrapped assertion leaves its own
continuation line between the marker and the assertion and the walker reads the one line above.
Stop also if
`src/domain/clone.js` or `src/lib/narrativeMutations.js` appears to need editing (**both are in
the zero-slack generation-worker closure**); **`safetyProfile.js`, `computeActiveChains.js` or
`priorityHelpers.js` appears to need editing** (all three are in that closure AND the lazy
`engine` chunk, and two are owner-signed FIX-D2/FIX-D3 territory); anything in `factionRename.js`
beyond `:168`'s one token appears to need editing; `anchors.js`, `catalogId` or any
map/interior/fog container appears to need editing (§3a is owner-gated); a persisted key would be
minted; **A1's RED CONTROL passes** (if version 2's lists survive the new arms, the arms are not
doing their job); **A1's disjointness arm reds**; or A1's walk finds a path in **neither** list.

⛔ **AND THE ESCAPE HATCH THAT IS NOT AVAILABLE:** "put it all in `factionRename.js`" — that file
is **388 effective of 800** and would take the 291, but it is the estate's **faction and person**
rename writer and its header, its tests and `tests/lint/ruinFilterRoster.walker.test.js:159` all
describe it as such. Merging a third entity's cascade into it is an architecture change, not a
budget cure. **If it is wanted, the packet RETURNS TO THE CHAIR.**

---

## 12. Completion receipt

Base SHA · seal identity · final tree state · **the effective-line count of each new leaf, measured
with a counter proved against `scripts/.size-baseline.json` (expect 217 / 74 of 250)** · exact
changed files and effective-line deltas · ⭐ **the pre-fix defect captured at step 1, quoted:
handles before and stale after, per row** · ⭐ **A2's zero-stale and zero-new-dangle figures, with
the declared NON_CASCADED residue beside them, by path and count (expect 550 + 175)** · ⭐ **A3's
control pair quoted: one-home 257/525 red, two-home 0/525 green** · ⭐ **A4's five removal kinds
each observed, the partition 525/525 three ways, and the reference-set baseline quoted (expect 0)**
· ⭐ **A5's two orphan kinds with their counts (expect 125 and 28)** · ⭐ **A1's full shape list
quoted whole, every arm's anti-vacuity floor's ACTUAL value, and THE RED CONTROL's finding against
version 2's lists (expect 5 undeclared paths, named)** · ⭐ **A6 (v)'s faction-handle pin: rows
carrying both, and the count untouched (expect 156/156)** · the mutants planted, convicted and
restored digest-exact (§P6) · focused commands, exits and counts · ⭐ **`node
scripts/check-observed-shape-readers.mjs` and `node scripts/check-writer-reach.mjs` EXECUTED, with
their before/after output quoted — the compile lane's verdicts for both are PLAUSIBLE predictions
(it is forbidden to run tree scripts) and CONFIRMING them is this build's job; motion in the first
is a STOP, growth in the second is a STOP, and a shrink is recorded for the chair, never written**
· ⭐ **A4 (iv)'s sentinel control, and the live/dark split as a 525-ROW EXECUTED FIGURE rather
than an arm pin (judgment 183b): the count of parenthetical values outside the declared eleven
(expect 0, and THAT is the arm) and the split measured over the whole corpus (expect 3 live: 
`(arcane underground)` 246 · `(smuggling)` 173 · `(covert)` 2, and 8 dark). ⛔ The 63-row
structured sample cannot hold the pin by arithmetic, so the arm carries the control, the
live-plus-dark accounting and a RAISE-ONLY floor of 2 live, and the split is a receipt** · ⭐ **A5 (iii)'s readable check: the count of
`readable: true` rows (expect 1) and the emitted note path matching the ledger lookup** · ⭐ **A6
(vi)'s re-measured `legal[].name` figures (expect 5,001 / 0 / 105 / 105)** · ⭐ **A1 (vi)'s
`LABEL_REWRITE` pin: the frozen map's key set quoted (expect exactly `verbatim`, `lower`), the
label-row membership (expect 2 of 2), the `exact`-row absence (expect 34 of 34 with no `rewrite`),
and BOTH members quoted as OBSERVED FIRING through the cascade** · ⭐ **A4 (v)'s set-equality:
the anchor FOUND, the FILE-WIDE extracted literal count (expect 11), and the two directions
separately (expect 0 generator-only and 0 list-only) — and, because version 8 exists for it, the
third-argument count the refuted rule reached (measured 7)** · ⭐ **`tests/lint` WHOLE, with its printed test count
— a gate line with no count DID NOT RUN — and the named census interior red quoted verbatim as the
only failure** · sealed per-step
receipt and resume status · both typecheck configurations · gate stages actually
executed · base-versus-wave failure identity diff · dormancy/golden result · census tuple before
and at the tip with the interior red quoted verbatim · ⭐ **the measured minified byte total of the
two new leaves (predicted 13,258 B), for the consumers' placement obligation (R1)** · edge-shared
verdict: **NOT OWED, and no `supabase/functions/_shared/**` path written** · ⭐ **VERSION 7's FIVE
INSTRUMENT FIGURES, EACH RE-RUN AT THE BUILD'S OWN HEAD AND QUOTED AS A DELTA:** the voice
walker's per-file `{em, bang}` for both new leaves (**expect `{0, 0}` each**, and the Tier-2
baseline UNCHANGED at 55 rows); the tuning inventory's `countUnregisteredNamed` and
`countBareDecimals` over both leaves (**expect 0 / 0 each**); the entropy census's two
`createPRNG(` counts (**expect 36 under `src/domain` and 47 under `src/` whole, both unmoved**);
the ruin-filter roster's discovery count (**expect 94, unmoved**, with the walker's own message
quoted if it is not); and `tests/lint/deepCloneHotPath.test.js` green with its printed test count
· ⭐ **AND THE COUNT PROVER, RUN FROM THE KIT PATH BEFORE THE SEAL** (`node EM-R6.count-prover.mjs
EM-R6.md EM-R6.manifest.json`, exit 0 quoted) · deviations
`NONE | STOP` · out-of-scope observations, without investigation · **judgment calls: `NONE`**.

---

## 13. RAISED — for the chair

⭐ **ALL NINE OF VERSION 3'S QUESTIONS ARE RULED (ODQ §934.47 addendum 34) AND CARRIED HERE**, and
**version 3.1 itself was ACCEPTED at ODQ §934.47 addendum 37** (2026-09-20 02:46 EDT). Nothing in
this table waits on an answer except R4, which is the owner's. ⭐ **VERSION 4's OWN LIST IS
SEPARATE AND FOLLOWS IT** — four items, each with a recommendation.

| # | ruling, and where version 3.1 carries it |
|---|---|
| **Q1** | ✅ **CONFIRMED AND CARRIED.** `economicState.institutionalServices[].institutions[]` is cascade row 26 under `match: 'label'` with its own `rewrite: 'lower'`. ⭐ **Taken further than the ruling required:** the rewrite form is now DECLARED ON THE ROW for both label paths and read off it by the cascade, so "each declared label path carries its own rewrite form" is enforced by the module rather than promised by its prose (§6; executed: `verbatim` / `lower`). Cost **+2 effective lines**. |
| **Q2** | ✅ **CONFIRMED AS COMPILED** — both frozen-stamp rows, `readable: false`. ⚠ **With one correction the chair should see: they are NOT an alias.** `sameObjectLive` is **0 of 525** — `economicState.js:62` and `safetyProfile.js:26` each call `getInstFlags` independently. Version 3's row prose said "the SAME object under a second path"; it is two equal-valued stamps. The ruling is unaffected; the reason is now the measured one. |
| **Q3** | ✅ **CONFIRMED AS COMPILED** — `structuralSuggestions[].suggested[]`, `kind: 'catalogue'`, `readable: false`. |
| **Q4** | ✅ **FIX-D5 RIDES HERE.** The eleven-spelling list is declared in the rename leaf; **the dangle denominator excludes by the list, never by `startsWith('(')`**; A4 (iv) pins **3 live / 8 dark** and carries the control that makes the swap safe — **0 parentheticals over 525 rows fall outside the eleven**. Priced **inside** the budget: **+5 effective lines, +245 B**, re-measured on this leaf (212 eff / 10,353 B without it). |
| **Q5** | ✅ **CONFIRMED READ-ALLOWED.** All 15 `NON_CASCADED` rows carry `readable`; exactly one is `true`. ⭐ **Taken further than the ruling required:** the removal RESOLVES that path from the ledger instead of spelling it, so the flag is load-bearing — flip it and the module throws at import. A5 (iii) asserts both. The rule — *"never touched" means never WRITTEN* — is in the MODIFY row for `factionRename.js`'s ledger header, which **widens that edit from one token to one token plus a comment block**; the three line-shift registers are re-swept clean in §7. |
| **Q6** | ✅ **CONFIRMED AS COMPILED** — `economicState.activeChains[].label` cascaded under the exact guard, `report-only` on removal, second orphan kind intact. |
| **Q7** | ✅ **CONFIRMED — the measured floors, RAISE-ONLY:** ≥40 shapes · ≥1 catalogue path · ≥4 normalised paths · ≥1 red-control finding. Written into A1 with that word. |
| **Q8 (R4)** | ⚠ **UNCHANGED AND STILL THE OWNER'S (ODQ §934.59).** §3a stays cuttable: under **YES** the chair deletes it whole and nothing else moves; under **NO** the four `anchorKey` stores become a third leaf (≈45 effective) or a follow-on packet and the budget row goes from 2 leaves to 3. All four containers are absent on 525 of 525 generated records. |
| **Q9** | ✅ **EM-B1c IS NOT GATED ON FIX-D3.** A2 keeps asserting the **175**-value residue **by count**, named as the declared price, so the partiality is on the record rather than in the tree. |
| **item 4** | ✅ A1's failure message carries the sentence: a `catalogId` path crossing the 50 % threshold is a **catalogue-shape change**, not a missing cascade row (today 12.6 % and 40.3 %). |
| **item 6** | ✅ `availableServices.legal[].name` **re-measured at this tip** (5,001 / 0 / 105 / 105) and asserted directly in **A6 (vi)** instead of resting on version 1's sentence. |
| **item 8** | ✅ The coverage line sits in §5's reconciliation, where the packet's totality claims are made: **231 of 280 emitted; both collision scans (231 emitted, 280 catalogue) EMPTY**, so the collision claim is total while the others are total over the 231. |
| **item 12** | ✅ **THE FAILURE-MESSAGE SENTENCE, NOT AN `alias` FIELD — and the measurement decided it.** An `alias` field would have asserted that the two stamps are one object; they are not (`sameObjectLive 0 of 525`), so the field would have been a falsehood frozen into a structure nothing reads. The sentence states the rule that is actually true and useful to a future reader — *aliased or duplicated stamps declare once per path by design, because the walk addresses by path and a reloaded save has already split any alias.* ⓘ The contrast with `readable` is the reason to accept the asymmetry: `readable` has a consumer (the removal resolves its path through it) and is therefore enforceable; `alias` would have had none. |

### ⭐ VERSION 4 — THE PRE-PROOF'S LIST FOR THE CHAIR (four questions, each with a recommendation)

| # | item | recommendation |
|---|---|---|
| **R10** | ⓘ **NOT A QUESTION — VERSION 3.1's NOTICE, CARRIED.** Q5's rule now lives in `factionRename.js`'s ledger header, which makes it **the estate's rule and not this packet's**: (a) the FACTION ledger's own rows do **not** gain a `readable` field here (the MODIFY touches the header only), so the estate has a written rule with a typed implementation on one side and prose on the other; (b) **a second reader of any `NON_CASCADED` path is a chair act** and §11 makes it a STOP | Both already have SLOTS on the record (addendum 37: EM-B1c's pre-proof carries the faction-side typing; EM-R7 gains the `tests/lint` source scan over both ledgers). **No action owed here** |
| **P1** | ⛔ **THE BYTE DELTA ON THE RECORD IS WRONG TWICE, AND ONE OF THEM IS THE CHAIR'S.** The packet's own table gives rename `+425`, removal `+36`, total **`+461 B`**. Version 3.1's prose said **+482** (unsourced); ODQ §934.47 addendum 37 and the charter's 02:46 amendment both say **+425**, which is the RENAME LEAF ALONE published as the packet's figure | **Correct the record to `+461 B` total (`+425` rename / `+36` removal)** in addendum 37's successor row. Nothing else moves: the absolute **13,258 B** is unchanged and correct, and no budget is near |
| **P2** | ⚠ **THE `'(background crime)'` EXPLANATION ON THE RECORD IS NOT MEASURABLE.** Addendum 37 and §6 both say it *"is a `crimeType` feeding the `(covert)` fallback"*. The literal `(background crime)` occurs **nowhere in `src/` or `tests/`**; what exists is the crimeType `'Background crime'` read as a condition at `servicesGenerator.js:347`, whose branch stores `'(covert)'` | **Accept §6's corrected sentence** (already written into version 4). The RULING — eleven spellings, 3 live / 8 dark — is unaffected and is now re-executed with a **widened** character class that could have found a twelfth and did not |
| **P3** | ⚠ **THE DESIGN'S OWN PARENTHESIS IS A STALE ABSOLUTE.** Design §22.1 ruling 7 still reads *"a declared institution rename-surface list, measured from the reference graph **(30 of 56 joins)**"*. This packet declares **36 cascade + 15 non-cascaded = 51** rows and §5 reconciles five numbers (42 · 5 · 36 · 33 · 15). The design's figure is FIX-D1-era and predates §934.61 | **Re-cut the parenthesis to the measured counts, or strike it**, at the chair's next design sitting. It is quoted as authority by this packet and will be quoted by EM-B1c. ⛔ Not touched by this lane — the chair owns the design |
| **P4** | ⓘ ⭐ **ANSWERED IN VERSION 5, AND THE ANSWER IS THE CUT THIS ROW ASKED FOR.** The row read: *"`tests/lint` WHOLE is now a 172-file gate stage on a packet whose own tests are ten `it`s … if the chair wants it cheaper, the lawful cut is the eleven tree-wide walkers by name (enumerated in §7), never a guess."* | **RULED BY INTERIM RULE 8, not by cost:** a sealed `checks` array names FILES, never the `tests/lint` DIRECTORY — and the directory run cannot pass anyway, because `sovereigntyLightingContract.walker.test.js` is inside it and reds by design under a train this member moves titles on. So `checks` now names **the eleven tree-wide walkers MINUS the lighting walker = ten**, plus `ruinFilterRoster.walker.test.js` from the name arm of the rule-8 computation. ⛔ **THE WHOLE DIRECTORY IS NOT DISCARDED** — it moves to §10 as the build lane's INSTRUMENT in its EXCLUDED form (must exit 0), with the lighting walker run ALONE beside it as the named interior red. The runs 17/18/19 gap stays closed: the same files are read, by a lane that must run them, in the only shape a seal can carry |

### ⭐ VERSION 7 — THE PRE-PROOF'S LIST FOR THE CHAIR (three questions, each with a recommendation)

| # | item | recommendation |
|---|---|---|
| **Q10** | ⚠ **THE NAMED EXCEPTION IN `tests/domain/editDeclarations.test.js:193` SURVIVES THIS PACKET, AND ITS OWN WORDS SAY OTHERWISE.** The arm admits `institutions[].name` as *"the one named exception … whose cascade EM-R6 mints"*, while `cascadeKeySurfaces` (`:138`) is derived from `[...NPC_RENAME_SURFACES, ...FACTION_RENAME_SURFACES].filter(s => s.kind === 'key')` — a set this packet's NEW module cannot join, because its rows are keyed `match: 'exact' \| 'label'`, not `kind: 'key'`. The arm **passes** before and after (measured), so this is a stale SENTENCE, never a red | **Leave the exception standing and slot the tightening to EM-B1c's pre-proof**, which is the member that actually routes `institution:name` through a writer and can widen the derivation and the row schema in one act. ⛔ Re-shaping `INSTITUTION_RENAME_SURFACES` to carry `kind: 'key'` here would change this packet's contract for a test it does not red |
| **Q11** | ⛔ **THE RUIN-FILTER DISPOSITION IS A DESIGN CONSTRAINT ON THE BUILD, NOT JUST A PREDICTION.** `expect(readers.length).toBe(94)` is EXACT, and the packet's `+0` holds **only while both leaves resolve every surface from a declared STRING path**. An implementer who writes `settlement.institutions.splice(...)` for row 1's `DROP THE RECORD` enrols the leaf and reds the arm | **Take §11's STOP as written** (the generic path resolver is the shape §6's bounded algorithm already describes, and it is what keeps the 36 rows declarative). If the chair would rather PRE-AUTHORISE the two exempt rows, they go in the `mutation` shape (`factionRename.js:160` is the model) and `tests/lint/ruinFilterRoster.walker.test.js` becomes a sixth §7 TEST row — ⚠ which would collide with nothing in EM-T14 or EM-T13 (measured), so it is safe either way. **The pre-proof recommends the STOP**: the walker is a review gate and enrolling a new cascade in it is a decision someone should make on purpose |
| **Q12** | ⓘ **TWO CROSS-TRAIN RESERVATIONS SHAPE §7 AND THE CHAIR SHOULD SEE THEM NAMED.** EM-R0d v7 (this train) reserves `tests/copy/voiceMechanics.test.js` and EM-B2a4 v2 (train EM-T13) reserves `tests/build/vendorPdfLazy.test.js`; both are PATH-reserved registers (§P10.8), so this packet prices each as a MEASUREMENT and names neither | **Confirm the shape at placement.** If EM-R0d leaves train EM-T14, nothing here changes — the rows stay measurements, because this packet's delta on both is `+0` and a member that moves a register by zero has no row to write |

---

## 14. ⭐ WHAT THE PRE-PROOF NOTICED AND DID NOT TOUCH

Each is specific enough to slot; nothing is deferred by this lane (owner law, ODQ §934.56).

| # | observation | where it belongs |
|---|---|---|
| **N1** | `tests/lint/contractTestAntiVacuity.walker.test.js` records **SEVEN pre-existing hits** in its fold scope, by name and rule (Rule 1a ×2 `tests/lib/emailTemplates.test.js:169,:185`; Rule 1b ×1 `tests/edgeFunctions/surveyorByok.test.js::block`; Rule 2 ×4 across `tests/edgeFunctions/providerErrors`, `tests/lib/founderChairRequest`, `tests/lib/importScrub`, `tests/lib/townMapExport`), exempted by NOTHING — the three older rules simply do not scan there. Its header calls triaging them *"its own act"* | A TOOL slot of its own; not this family's. Raised so the chair can charter it |
| **N2** | The `readable` rule is **enforced by nothing today**. Version 4's A1 (vi) closes the `rewrite` vocabulary, but `readable` is load-bearing only because THIS packet's removal resolves through it | Already slotted: **EM-R7**'s `tests/lint` source-scan arm over both ledgers (addendum 37). Recorded so the slot is not lost |
| **N3** | `src/store/settlementRenameHelpers.js:402` cites `factionRename.js NPC_HOMES` in a COMMENT. Prose, not a join, so R2's `export` does not reach it — but the two-homes law is now written in THREE places (the const, this comment, version 3.1's ledger header) | **EM-B1c's pre-proof**, beside the faction-side `readable` typing it already carries |
| **N4** | ⭐ **RE-ADDRESSED BY VERSION 7 — `:160`, not `:159`** (`:159` is the section comment `── mutation / write / undo ──`; the row itself is `:160`, re-read at `bfca6b7d6`). `tests/lint/ruinFilterRoster.walker.test.js:160` carries a per-PATH ruling for `src/domain/factionRename.js` (*"a faction rename rewrites `institutions[].factionSource`…"*). Keyed by path, not line, so this MODIFY cannot move it — **but `institutions[].factionSource` is an institution-adjacent field this packet's surface list declares in neither direction** | A one-line census at **EM-B1c's pre-proof**: is `factionSource` a FACTION reference (already cascaded there) or a fourth kind? The walker's prose says faction; the packets should say so too |
| **N5** | The lighting census baseline records `measuredAtSha: 7c233db55…` while the tuple it holds (`2653·383·2270·25052·6680`) was committed at `141a1d775` | Cosmetic and correct — the register is measured on the parent tree. Recorded only so a successor does not read it as drift |
