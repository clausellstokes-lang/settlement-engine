# `settlement-editor / wave 2` — implementation contract: EM-C3, THE GUARD RULES

- **Status:** `LANDED`

  Compiled by an Opus COMPILE seat at read tip `1b073009f` (train EM-T16's landed tip),
  session cce01f87, 2026-09-23. READY-able: every verified-fact row was re-found BY SYMBOL
  at that tip and every register figure below is a DELTA measured by running the
  instrument's own logic over this packet's planted text.

  **WHAT VERSION 4 CHANGED AND WHY (the chair's judgment 251, 2026-09-23; the same Opus
  PRE-PROOF seat, still at `bb0497eb4`).** EM-C2b — the slot version 2 opened for populating
  `OP_TYPES[type].guards` — is **CLOSED**, and its work is RE-SLOTTED here. Its compile measured
  that `OP_TYPES` is a frozen module-scope literal whose rows can only reach a rule function if
  `operations.js` imports this leaf, that `.guards` has ZERO runtime readers, and that
  `rulesFor(type)` in this leaf is ALREADY the single home of the derived coverage. Version 4
  therefore adds **ONE §7 TEST row and nothing else**: `tests/lint/opGuardCoverage.walker.test.js`
  is WIDENED to import `rulesFor` and assert the DERIVED coverage per op type — every op type
  reaching a non-empty rule set, the **19 × 4 / 3 × 5** table in both directions, and C7's
  `populated` row given its semantics IN THE CODE: coverage is DERIVED FROM THE RULES, never
  authored on a row, and `OP_TYPES[*].guards` stays the inert empty array ARCH §5's "stated when
  empty" asks for. ⛔ **NOTHING ELSE MOVES:** no `src/` path is added, the §5 clause is untouched
  (same digest), the ruin-filter row stays, `_pendingRequiredSymbols` is unchanged, the acceptance
  matrix is still A1–A8, and the widen is **+0 B** (a test file enters no emitted chunk). The widen
  lands INSIDE the landed C5 and C7 bodies, so it ADDS ZERO `it`s: **the lighting delta is
  UNCHANGED at `files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +1`** and no landed
  title moves (judgment 148's law). A new `C9` arm was refused by measurement, not preference: the
  count law ties a declared arm to a homed acceptance case (§P9(f)) and §3's case budget is at
  **8 of 8**, so a ninth case would be a STOP AND SPLIT rather than a widen.

  **WHAT VERSION 3 CHANGED AND WHY (the Opus PRE-PROOF seat for train EM-T17 stage 2,
  session cce01f87, 2026-09-23; the chair's judgments 243 and 245).** Version 3 re-measures
  version 2 against the train's STAGE-1 PLACEMENT tip `bb0497eb4` (EM-R1c v3 and EM-C2 v3
  READY on the branch), where the J-T1 window over every manifest and every requiredSymbols
  path is EMPTY. NO CONTRACT MOVED, no acceptance case changed, the §5 clause was not touched
  (its SHA-256 is unchanged and still byte-identical to EM-C2's) and the effective-line figure
  is still 168. What changed: **(a) ONE §7 ROW IS ADDED** — `tests/lint/ruinFilterRoster.walker.test.js`,
  because that walker's census arm pins `expect(readers.length).toBe(94)` over every `src/domain`
  file whose CODE reads `.institutions`, this CREATE ENROLS in exactly that set, and the arm
  therefore reds `expected 95 to be 94` at the build. EM-PREAMBLE §P2.21(b) asks for the
  disposition AS A TEST ROW WITH THE DISCOVERY COUNT AS A DELTA, and EM-A2a is the precedent
  that proves the cost: it CREATEd `src/domain/edit/pools.js`, declared the walker not owed, and
  the red was found after the member was sealed — cured as EM-A2b version 7 (judgment 138).
  **(b) THE `vendorPdfLazy.test.js` ROW IS RE-WRITTEN AGAINST THE ELEVEN-ROW ARRAY** this seat
  measured itself, and every sentence about EM-C2 adding a row is DROPPED: EM-C2 version 3
  carries no `editorTrain` row (the chair's judgment 243). **(c)** the voice walker's own
  literal count reads **80**, not the 107 version 2's §6 stated in one place (the `em 0 / bang 0`
  figures the walker reds on were and are correct). **(d)** the wiring census's novel-key delta
  is stated at BOTH tips, because EM-C2's `guards.js` produces `fulfil` and `relatedEntryId`
  first: **+4 at `bb0497eb4`, +2 at EM-C2's flip**. **(e)** two version-1 residues in §8 and
  §6's lifecycle row (`GUARD_RULE_SET`, "the three imports") are re-spelled to version 2's real
  surface. **(f)** §12 ruling 2's two byte figures are re-measured at this tip and its one
  line address is re-spelled by symbol (§P2.22). **(g)** the observed-shape and writer-reach
  registers are dispositioned by measurement, and the whole instrument list is re-executed here.

  **WHAT VERSION 2 CHANGED AND WHY (the chair's judgment 236, 2026-09-23).** Version 1
  compiled the shared §5 rules interface with its own draft of three deltas against EM-C2's.
  The chair closed the fork: deltas 1 (a rule `needs` field) and 2 (the injected `ctx.deps`,
  the skipped-rule `unevaluated` report and the engine's `{ guards, unevaluated }` return) are
  ADOPTED from this packet verbatim; delta 3 — where the `GUARD_KINDS` and `GUARD_OFFERS`
  VALUES live — is decided for the ENGINE on EM-C2's executed closure measurement, so
  `guards.js` exports them, this leaf IMPORTS them at run time and names the engine's typedefs
  TYPE-ONLY, **EM-C2 LANDS BEFORE EM-C3**, and version 1's sentence "EM-C3 lands before EM-C2"
  is RETIRED. Version 2 therefore carries: the ONE TEXT copied byte for byte with its measured
  digest, a `Depends on: EM-C2` row, a `_pendingRequiredSymbols` block for the engine
  symbols this leaf names (judgment 151 — they do not exist at this packet's own base and are
  re-measured VERBATIM at the placement, never promoted as written), the re-added FOURTH ROOT of the flavour census (EM-C2's finding,
  measured here), and every instrument re-measured over the version-2 planted leaf.
- **Landed at:** `fbcc4b327ab33cca23f5d098de84cf74e3ad794f` — the guard rules (version 4): design §12.8's five rules over the record with the two generator writers injected on ctx.deps (the domain→generators boundary held; 235/236); built at fbcc4b327 on train EM-T17
- **Packet version:** `4`
- **Verified base:** `em-t17-c3-2026-09-23` at `0d6143b58206e3e4e1b2550ae0ba4c353ee88a21`
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` at SHA-256 `125c693214235a81bf4f2b38506b621859a35e0325ff2ea58d335b4e0681a99d` — MEASURED at the read tip (`shasum -a 256`), the SIXTH amendment.
- **Last revalidated:** `0d6143b58206e3e4e1b2550ae0ba4c353ee88a21`
- **Depends on:** **EM-C2** (the engine — this leaf IMPORTS `GUARD_KINDS` and `GUARD_OFFERS` from `src/domain/edit/guards.js` at run time and names its typedefs type-only; judgment 236 stages EM-C2 first), EM-A1 (LANDED: `fieldDeclarations.js`, `types.js`), EM-B1a/EM-B1b (LANDED: `operations.js` and its seven off-stage rows), EM-A3 (LANDED: `tests/lint/flavorFields.census.test.js`, whose fourth root this member re-adds). ⛔ **NOT** EM-C1: no entry shape beyond the §5 clause is read here.
- **Collision group:** EM-C2 — by CONTRACT (the §5 clause below is ONE TEXT in both packets, digest quoted) and by DEPENDENCY (this leaf imports `guards.js`), and **BY NO PATH AT ALL**: measured at `bb0497eb4` over the branch manifest and the waiting capsules, this member's five paths intersect EM-C2's three (`guards.js`, `editGuards.test.js`, `guardsAcceptApplies.test.js`) in the EMPTY SET, and EM-R1c's five (`pipeline.js`, `dmLayer.js`, `chooserRecordPathRegistry.walker.test.js`, `dmLayerGoldenIsolation.test.js`, the row-keyed mutation register) in the EMPTY SET. Version 2 claimed `tests/build/vendorPdfLazy.test.js` as a shared TEST path; EM-C2 version 3 carries no row on it (the chair's judgment 243), so the claim is RETIRED. `tests/lint/flavorFields.census.test.js` is EM-A3's LANDED file and `tests/lint/ruinFilterRoster.walker.test.js` was EM-A2b's and `tests/lint/opGuardCoverage.walker.test.js` is EM-B1b's LANDED CREATE; none of the three is claimed by any in-flight member (measured at `bb0497eb4`: the only manifest entries naming any of them are LANDED, and no waiting capsule of a live member carries one). No `src/` path is shared with any other member.
- **Commit authority:** edits only; the chair commits.
- **Baseline posture:** measured at `bb0497eb4`. The live layering ratchet reads `[]` / `liveCount 5`; the live lighting census reads `2697 · 359 · 2338 · 25820 · 6855`, exactly the committed twenty-second refreeze (the harness's own anti-vacuity control); the ruin-filter walker's discovery set reads **94**, exactly the figure its census arm pins; `tests/lint/opGuardCoverage.walker.test.js` is CREDITED with FOUR titles (C5–C8) and ONE suite title and its `populated` roster reads `[]` over 22 rows, all 22 `guardsStated` sentences matching `/\bno guard\b/i`; `validatePacketManifest` over the live **232**-entry manifest reads `ok=true, 0 errors`.

## §1 · Reconciled authority

1. The owner, 2026-09-19 ~20:2x/~20:3x — guards are suggestive and never blocking; the DM decides whether two entries connect (design §2.7, §2.7a).
2. THE PROMISE and design §12 — **§12 GOVERNS** wherever an earlier section or the ARCH disagrees.
3. `docs/DESIGN_EDIT_MODE_AND_DECREES.md` §2.7, §2.7a, §12.8, §18, §20.3, §22.3 item 6; `docs/ARCH_EDIT_MODE_AND_DECREES.md` §1, §2, §5.
4. `docs/implementation/charters/EDIT-MODE-TRAIN.md`, the Wave 2 table, row **EM-C3**.
5. The tree at `1b073009f` — every row of §5 re-found by symbol, every figure of §7 executed.

Resolved contradictions:

- **Design §2.7 and §12.8 name `checkInstCompat` as the prerequisite rule's ground** -> **REFUTED, and now by execution rather than by reading.** Under an instrumented `setActiveRng` at the read tip, `checkInstCompat(institutions, 'city', 50)` consumes EXACTLY ONE DRAW and returns a narrative sentence through `pickRandom`. The charter's own row already carries the correction (`NOT checkInstCompat, a prose picker that consumes a draw — measured at compile`) and EM-B1a's LANDED `requiredSymbols` row records it. The prerequisite rule's ground is `checkStructuralValidity` plus `GATE_FEATURES`.
- **The charter's required-symbols column reads as though this leaf IMPORTS the two generator writers** -> **that shape REDS a LANDED ratchet**, `tests/build/domainGeneratorsBoundary.test.js`: arm 1 prints `NEW importer: src/domain/edit/guardRules.js -> ...` and arm 3 goes from 5 to 7, for a STATIC or a DYNAMIC specifier alike. The instrument's own message names the cure and design §22.3 item 6 already ruled the principle for the band ladders — *invert, do not widen* — with EM-R0d and EM-R0f as the landed precedents. The two writers therefore arrive INJECTED (§5 clause item 10); `GATE_FEATURES` is imported directly because it already lives a layer down, in `src/data`.
- **ARCH §5's "coverage is declared per op type in `OP_TYPES[type].guards`"** -> **RULED AND CLOSED (the chair's judgment 251).** The rows' `guards: []` + `guardsStated` STAY as ARCH §5's "stated when empty"; coverage is DERIVED from `rulesFor` and the DERIVATION is what the instrument asserts. EM-C2b's compile measured why a population cannot be the shape: `OP_TYPES` is a frozen module-scope literal, so a rule function reaches a row only if `operations.js` imports this leaf — which §5 clause item 2 forbids in BOTH packets — and a rule-id STRING population yields 44 offenders from the landed walker's own predicate, while `.guards` has ZERO runtime readers under `src/`. Version 4 carries the re-slot as ONE §7 TEST row on that walker.
- **ARCH §2's `Guard` typedef carries no `id`, while design §2.5/§12.8 record `overrode: [guardId]`** -> the guard's id is MINTED BY THE ENGINE from the rule's own `id` (§5 clause item 6), so an override names something a reader can resolve. The rule ids are stable across versions for exactly that reason.

The implementer does not read other documents to reinterpret this packet.

## §2 · Outcome

**Observable result:** `src/domain/edit/guardRules.js` exists and exports the five guard rules of design §12.8 — prerequisite, totality, contradiction, contention, connection, and NO count-range rule — as a pure, frozen rule set the folded engine runs, with the two generator writers injected rather than imported.

**Definition of done:** the CREATE lands DARK (zero importers under `src/`), its eight acceptance cases pass in `tests/domain/editGuardRules.test.js`, and the eleven instruments of §7 move by exactly the deltas declared there.

In scope:

1. The five rules, each judging one pending entry against the FOLDED world, each returning a finding or `null`, none able to refuse.
2. `project` — the rule set's own fold for the two facts its rules read (the institution roster; the faction share space), because only a rule set knows what an op does to the fact it judges.
3. The prevention guard the rules themselves are: the prerequisite rule reads `liveInstitutions`, so a calamity-ruined wall can never satisfy a gate.

Explicit non-goals:

- The ENGINE (EM-C2) and the REGISTRY (EM-C1). This packet imports neither and declares no entry shape of its own beyond the §5 clause.
- Populating `OP_TYPES[type].guards` — **CLOSED, not deferred** (judgment 251; §12 ruling 2 carries the measurement). Widening `tests/lint/opGuardCoverage.walker.test.js` to assert the DERIVED coverage IS in scope from version 4 and is §7's fifth row.
- Any store surface, any component, any feature flag, any persisted key. Wave 2 is HEADLESS.
- Re-homing the five message templates into `src/copy/en.js` — §12, ruling 5.
- Record adjacent discoveries in the receipt; do not investigate or repair them.

## §3 · Hard scope budget

| Limit | Packet budget | Measured |
|---|---:|---:|
| Behavior families | `1` | 1 |
| New persisted record families | `0` | 0 |
| Named state writers | `0` | 0 |
| Feature flags | `0` | 0 |
| User-facing surfaces | `0` | 0 |
| Direct consumers | `<=2` | **0** (the CREATE lands DARK) |
| New logic-bearing production leaves | `<=2` | **1** |
| Existing logic-bearing production files modified | `<=3` | **0** |
| Additional registration-only files | `<=3` | 0 |
| Handwritten files total | `<=12` | **6** |
| New/changed effective production lines | `<=400` | **168** |
| Effective lines per new leaf | `<=250` | **168** |
| Delta in a shared/hot file | `<=15` | **+1 line** (`tests/build/vendorPdfLazy.test.js`), **+2 lines** (`tests/lint/flavorFields.census.test.js`'s `SCAN_ROOTS`, its dropped-root paragraph rewritten) **+0 effective lines** (`tests/lint/ruinFilterRoster.walker.test.js`: ONE literal moves, `94` to `95`, beside a comment the file's own idiom requires; `max-lines` with `skipComments` counts the comment as nothing) and **at most +12 effective lines** (`tests/lint/opGuardCoverage.walker.test.js`: one import and the derived-coverage assertions inside the LANDED C5 and C7 bodies; no new `it`, no new `describe`) |
| Acceptance cases | `<=8` | **8** |

Effective lines measured with eslint's own `Linter` under `max-lines` with `skipBlankLines` and `skipComments` (EM-PREAMBLE §P9's one instrument): `src/domain/edit/guardRules.js` 0 -> **168** effective (345 raw) at version 2 (154 at version 1; the rise is the narrowing helpers the engine's loosely-typed `GuardContext` needs to keep the any-cast ratchet at zero).

Overrides approved before dispatch: `NONE`.

Exceeding any limit is a STOP and split, not an invitation to renegotiate.

## §4 · Sealed dispatch and preflight

Run from the packet's worktree before any edit:

```sh
npm run implementation:dispatch -- EM-C3
```

Expected, and dry-read at compile: both CREATE targets are ABSENT at the read tip
(`src/domain/edit/guardRules.js`, `tests/domain/editGuardRules.test.js`, re-measured `ABSENT` at
`bb0497eb4` with `git ls-files` empty for each); the three non-CREATE targets are present and
clean; all eight `requiredSymbols` resolve VERBATIM at **count 1** (`grep -c -F`, executed at
`bb0497eb4`), and the two `_pendingRequiredSymbols` rows read **count 0** here and resolve only
once EM-C2 has flipped (judgment 151: a pending row is re-measured VERBATIM at its discharge,
never promoted as written; `src/domain/edit/guards.js` is ABSENT at this tip). `validatePacketManifest` over the live **232**-entry manifest with this capsule
placed returns exactly TWO errors, both placement-only:

```
EM-C3.packetPath does not exist: docs/implementation/packets/settlement-editor/EM-C3.md
EM-C3.verifiedBase must be a lowercase 40-character SHA
```

The first is the placement's own act; the second is the chair's `__BASE__` stamp. **Zero content
errors and zero duplicate change paths** — no other entry reserves any of this member's three paths.

Any mismatch makes this packet STALE. Stop before coding.

## §5 · Verified tree contract

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| Prerequisite ground | `src/generators/structuralValidator.js` | `checkStructuralValidity` | `(institutions, config = {}) -> { violations, suggestions }`. MEASURED BY EXECUTION: **0 draws**, and two calls with no active RNG are byte-identical. Violation rows carry `{type, institution, reason, severity}`, plus `missing` on a dependency violation and `conflicting, group` on an exclusivity one | CALL THROUGH `ctx.deps`. Never import; never re-implement |
| Prerequisite table | `src/data/spatialData.js` | `GATE_FEATURES` | 96 keys over eight field shapes; `requires`, `requiresAny`, `requiresAccess`, `minTier`, `reason`, `accessViolationReason`, `suggestionOnly` | IMPORT DIRECTLY. It already lives a layer down |
| Totality writer | `src/generators/power/rulingStructure.js` | `renormalizeFactionPower` | `(factions) -> factions`. ⚠ MUTATES IN PLACE and returns the SAME reference; largest-remainder to an exact total, ties by current order; returns the argument UNCHANGED on an empty or all-zero roster | CALL THROUGH `ctx.deps`, on a CLONE. The rule reads its TARGET from the clone's sum, never from a literal |
| Liveness accessor | `src/domain/institutions/institutionRoster.js` | `liveInstitutions` | `(settlement) -> settlement.institutions` filtered by `isLiveInstitution` (refuses `_worldPulseInactive === true` and `ruined\|removed\|destroyed\|remnant`); a non-array roster yields `[]`. The module imports NOTHING | Only roster read. It is also this CREATE's compliance under the ruin-filter walker |
| Order | `src/domain/deterministicSort.js` | `compareCodepoint` | the estate's one locale-free string order | Every list a guard's text names is sorted through it |
| Catalogue | `src/domain/edit/operations.js` | `OP_TYPES` | `requires` is the `{ world, registry }` SPLIT on the declaration and FLAT on the built `Op`; `relatedTo` and `conflictsWith` are per-row; every key required on every row; `guards: []` and `guardsStated` on all 22 rows | READ ONLY THROUGH `ctx.decl`, never imported under `src/` |
| Vocabulary (PENDING) | `src/domain/edit/guards.js` | `GUARD_KINDS`, `GUARD_OFFERS` | EM-C2's CREATE, ABSENT at this packet's own base and LANDED before it by judgment 236. `GUARD_KINDS` is the design's five in `compareCodepoint` order; `GUARD_OFFERS` is ARCH §2's seven. Its whole static closure is itself plus `../deterministicSort.js`, two modules | VALUE IMPORT at run time. Never re-spelled here |
| Engine types (PENDING) | `src/domain/edit/guards.js` | `GuardContext`, `GuardFinding`, `GuardRule`, `ProjectFn` | the same CREATE's typedefs. ⭐ **ALL FOUR ARE DECLARED BY EM-C2 VERSION 3** — its §6 names all SIX (`ProjectFn`, `GuardContext`, `GuardFinding`, `GuardRule`, `Guard`, `GuardVerdict`) by name, precisely because version 2 of that packet declared only two of the four this leaf imports (the chair's judgment 243, ruling 3), so a type-only import of a name the engine never declared is no longer possible. `GuardContext.decl` is `Record<string, unknown>\|null` and `folded` is `unknown`, so this leaf narrows them through its own predicate rather than casting | TYPE-ONLY `import('./guards.js')` in a `@typedef`; it erases at emit and creates no runtime edge. They are NOT `requiredSymbols` and NOT `_pendingRequiredSymbols`: a JSDoc typedef is not a code symbol a `grep -c -F` row can pin |
| Types | `src/domain/edit/types.js` | `export {};` | the edit volume's types-only leaf; `Op` and `EntityRef` live here | Named by a TYPE-ONLY `import('./types.js')`; NOT modified |
| Test precedent | `tests/domain/editOperations.test.js` | `describe('EM-B1a ...')` | a `tests/domain` battery over a frozen catalogue: one literal `describe`, straight-line literal `it`s, every set imported from its producer | Copy this proof shape |

Forbidden alternatives:

- ⛔ **no second guard vocabulary.** `GUARD_KINDS` and `GUARD_OFFERS` are the ENGINE's and are imported; re-spelling either here is the two-vocabularies defect judgment 236 refused;
- no second rule registry, no second validator, no second renormaliser, no PRNG, no clock, no locale, no store, no I/O;
- ⛔ **no import of anything under `src/generators/`** — static or dynamic (`tests/build/domainGeneratorsBoundary.test.js` matches both);
- ⛔ **no import of `OP_TYPES`, `makeOp` or `validateOp`** under `src/` (`tests/lint/editMutationPath.walker.test.js`);
- no edit to `src/domain/edit/operations.js`, `src/domain/edit/types.js`, or any baseline/register file;
- no files outside the manifest.

### THE RULES INTERFACE — THE ONE TEXT (the chair's judgment 236)

> ⛔ ONE TEXT IN TWO PACKETS, COPIED BYTE FOR BYTE. The chair landed it after EM-C2's
> reconciliation measured the two drafts: items 1, 2, 3, 6, 7 and 8 were already identical;
> this packet's Delta 1 (a rule `needs` field) and Delta 2 (the injected `ctx.deps`, the
> skipped-rule `unevaluated` report and the `{ guards, unevaluated }` return) were folded in
> verbatim as items 4, 5, 10 and 11; Delta 3 went to the ENGINE, so item 9 gained the value
> half and EM-C2 lands first. **The fenced block below is 8,288 bytes and its SHA-256 is**
> **`20ff767bc6a53ff5b3f178932017eab9af721b03bc98f65bbd62d100a8576aae`** — this packet's count prover
> recomputes that digest over its OWN copy and refuses a byte that differs. A divergence
> between the two packets, at either placement or at the build, is a STOP (§11).

```text
THE RULES INTERFACE (EM-C2 · EM-C3, one clause in both packets).

1. THE ENGINE TAKES ITS RULES AS AN ARGUMENT AND IMPORTS NO RULE.
   `evaluateGuards(registry, world, catalogue, ruleSet)` — ARCH §5's three-argument spelling
   EXTENDED BY ONE, because the rules cannot reach the engine any other way that measurement
   allows: `src/domain/edit/guards.js` imports only `../deterministicSort.js`, so EM-C2 lands
   with no edge to EM-C3 and each member's battery is provable alone.
   `EMPTY_RULE_SET` (exported by `guards.js`) is the absence value:
   `{ rules: [], project: null, deps: {} }` — the three keys of item 7's projector, item
   10's injected dependencies and the rule list itself.

2. THE CATALOGUE IS ALSO AN ARGUMENT, AND IT IS `OP_TYPES` (`src/domain/edit/operations.js`).
   The engine passes it through to each rule as `ctx.catalogue` and passes the row for the
   entry's own op type as `ctx.decl`. Neither member reads the catalogue's `guards` array:
   `tests/lint/opGuardCoverage.walker.test.js` C7 pins every row's `guards: []` AND its "no
   guard is wired here" sentence, so a population of `OP_TYPES[type].guards` is a MODIFY of
   `operations.js` plus a widen of that walker, and neither is in either member's charter row.
   Guard COVERAGE therefore stays declared where the walker already reads it, and EM-C3's own
   suite proves its rule roster's op-type keys are a SUBSET of `Object.keys(OP_TYPES)`.

3. ⛔ A RULE READS `ctx.decl.requires.registry`, NEVER `ctx.op.requires`.
   `makeOp` FLATTENS the declaration's `{ world, registry }` pair into one array, so
   `op.requires` of `remove-npc` and of `set-npc-status` reads `['npcPresent']` — a WORLD
   predicate that design §18 rules is not a guard's business at all (it decides which seals a
   card OFFERS). MEASURED at the tip through the planted engine: a rule reading `op.requires`
   emits 5 prerequisite guards over the 22-row catalogue where a rule reading
   `decl.requires.registry` emits 3, and the 2 extra are exactly `remove-npc` and
   `set-npc-status`.

4. ONE RULE.
   `{ id: string, appliesTo: readonly string[]|null, needs: readonly string[], evaluate: (ctx)
   => GuardFinding|readonly GuardFinding[]|null }`. Every field is REQUIRED on every rule
   (EM-B1a's law for the catalogue's rows, kept here): `appliesTo` is a list of op types or
   `null` for every type, `needs` is the list of item 10's dependency names the rule uses (`[]`
   when it uses none), and an OMITTED key is not an absence value — its rule is skipped. `id`
   is unique across the rule set, stable across versions, and is half of a guard's id, so
   renaming one orphans an override the DM already recorded. Rules run in `compareCodepoint`
   order of their ids.

5. ONE CONTEXT (`ctx`), frozen, built by the engine:
   `{ entry, op, decl, index, world, folded, priorEntries, laterEntries, entries, catalogue,
   deps }`. `world` is the world as it stands; `folded` is §12.8's world with every EARLIER
   pending entry applied; `priorEntries` / `laterEntries` / `entries` are the pending entries in
   the engine's own order. A rule reads and returns; it writes nothing it was handed.

6. ONE FINDING.
   `{ kind, message, offers?, fulfil?, relatedEntryId?, facet? }`. `kind` is one of
   `GUARD_KINDS` = connection · contention · contradiction · prerequisite · totality (design
   §12.8: the range rule is DROPPED). `message` is a non-empty string in the herald's voice.
   `offers` are members of `GUARD_OFFERS` = fulfil · keepBoth · keepFirst · keepLast · proceed
   · reorder · self, in the order a reader should see them; the engine dedupes them, drops any
   member outside the vocabulary and APPENDS `proceed` when the rule left it out, so no rule
   can mint a refusal. `fulfil` is an `Op` (built by `makeOp`) or absent. `relatedEntryId`
   names the other entry in a two-entry finding. `facet` is the rule's own discriminator when
   it yields more than one finding for one (entry, related) pair; the guard's id is
   `<ruleId>:<entryId>:<relatedEntryId or ->:<facet or ->`, so a rule that omits a needed
   facet gets an ordinal rather than a collision, and every finding survives.
   A finding that is not a plain object, or whose `kind` is outside `GUARD_KINDS`, or whose
   `message` is not a non-empty string, is SKIPPED: design §9 rules that a false warning costs
   more trust than a missing one, and a guard is a suggestion either way.

7. `project` IS THE FOLD, AND IT IS THE RULE SET'S.
   `ruleSet.project(world, op, catalogue) => world` is pure and returns a NEW world; returning
   `undefined` or `null` leaves the world where it was, and a rule set with `project: null` is
   judged against the world as it stands for every entry (measured: `ctx.folded === world`
   identically). The engine never synthesises a post-op world itself — only a rule knows what
   an op does to the fact it judges, and a world the engine invented would be a fiction every
   rule then read as truth.

8. NEITHER MEMBER THROWS, AND THE ENGINE DOES NOT CATCH.
   `evaluate` and `project` are contracted TOTAL: no throw, no clock, no PRNG, no locale, no
   store, no I/O. The engine wraps neither in a `try`, because a swallowed throw is a guard
   that silently stopped guarding; EM-C3's suite proves totality over the whole catalogue.

9. THE TYPEDEFS LIVE IN `guards.js`, AND `guardRules.js` NAMES THEM TYPE-ONLY.
   `/** @typedef {import('./guards.js').GuardRule} GuardRule */` and its kin erase at emit and
   create no runtime edge, which is the same idiom `operationsOffStage.js` already uses to
   name `operations.js`'s types without an import cycle. `src/domain/edit/types.js` is NOT
   modified by either member: a second member's rows on a landed leaf buy nothing here.
   ⭐ AND THE TWO VALUE VOCABULARIES LIVE THERE TOO, WHICH FIXES THE ORDER. `GUARD_KINDS`
   and `GUARD_OFFERS` are VALUE exports of `guards.js` and `guardRules.js` IMPORTS them at
   run time (judgment 236, from the executed closure measurement): on the engine its whole
   static closure is itself plus `deterministicSort.js`, two modules, provable alone and
   landable on any train; on the rules the engine would pull `guardRules.js` and the
   22,393-byte `src/data/spatialData.js` leaf it reads `GATE_FEATURES` from, and be
   unbuildable until EM-C3 lands. The engine defines the interface and a rule set
   implements it, so EM-C2 LANDS BEFORE EM-C3, which carries `Depends on: EM-C2`.

10. ⛔ THE TWO GENERATOR WRITERS ARE INJECTED, AND THAT IS MEASURED RATHER THAN CHOSEN.
   `ctx.deps` is `{ checkStructuralValidity, renormalizeFactionPower }`, frozen, supplied by
   the engine's CALLER from OUTSIDE `src/domain/**`. `GUARD_RULE_DEPENDENCIES` (exported by
   `guardRules.js`) names them in `compareCodepoint` order. WHY: `tests/build/
   domainGeneratorsBoundary.test.js` freezes four domain-to-generators edges and pins their
   count at five, and it matches a DYNAMIC specifier as well as a static one; a planted
   `guardRules.js` that imports either writer takes its arm 1 to `NEW importer:
   src/domain/edit/guardRules.js -> ...` and its arm 3 to 7 (static) or 6 (dynamic), while the
   injected plant leaves both arms exactly where the tip has them. The instrument's own message
   names the cure, and design §22.3 item 6 ruled the principle for the band ladders: invert, do
   not widen. `GATE_FEATURES` is NOT injected — it already lives in `src/data`.

11. A RULE WHOSE `needs` ARE UNMET IS NOT RUN, AND THE ABSENCE IS REPORTED.
   The engine checks `rule.needs` against `Object.keys(deps)` before calling `evaluate`, skips
   the rule when a name is missing, and returns the skipped ids as `unevaluated`, sorted by
   `compareCodepoint`. It never substitutes a stub, never throws and never reports a guard the
   rule did not make: ARCH §5's "coverage is stated when empty" is a RUNTIME statement here as
   well as a catalogue one, so a mis-wired caller is visible rather than silently guard-free.
   The engine's return is therefore `{ guards, unevaluated }`, both frozen: `guards` in the
   fold order, `unevaluated` the skipped rule ids in `compareCodepoint` order. Design
   §2.7's bare array spelling is superseded by this clause, deliberately and in both
   packets.
```

## §6 · Exact contracts

### Inputs and outputs

```js
// src/domain/edit/guardRules.js — the whole public surface (version 2).
// GUARD_KINDS and GUARD_OFFERS are NOT exported here: they are the ENGINE's, imported from
// './guards.js' at run time (judgment 236). Re-exporting them would be a second home.
import { GUARD_KINDS, GUARD_OFFERS } from './guards.js';

export const GUARD_RULE_DEPENDENCIES;  // frozen ['checkStructuralValidity','renormalizeFactionPower']
export const GUARD_RULES;              // frozen readonly GuardRule[], ids in compareCodepoint order
export function project(world, op, catalogue); // => a NEW world, or null for "nothing to fold"
export function makeGuardRuleSet(deps);        // => frozen { rules, project, deps }, EMPTY_RULE_SET's three keys
export function rulesFor(type);        // => the rules whose appliesTo admits `type`, in id order
```

### State schema

```js
// NONE. This leaf holds no state, persists nothing, and every exported value is frozen.
// Every rule is a pure function of its `ctx` and returns a fresh object or null.
```

Absence rules:

- **absent** (`evaluate` returns `null`): the rule has nothing to say. It is SILENCE, never a verdict, and never an assertion that the entry is safe.
- **empty** (`GUARD_RULES.filter(...)` returns `[]` from `rulesFor`): forbidden in practice and asserted so — four of the five rules carry `appliesTo: null`, so every catalogue type yields at least four.
- **an absent dependency** (`makeGuardRuleSet({})`): a LAWFUL rule set. The engine skips `prerequisite` and `totality` and returns their ids in `unevaluated`; it never substitutes a stub and never reports a guard the rule did not make (§5 clause item 11).
- **`null` from `project`**: "this op moves no fact any rule here reads", and the engine leaves the world where it stands. It is the correct answer for 17 of the 22 catalogue rows and is never an error.
- **invalid legacy input** (a malformed world, a missing `op`, a payload that is not an object): every accessor degrades through `isObject` to an empty bag or an empty list. No rule throws, per §5 clause item 8.

### Transition table

| Prior state | Input/event | Guard | Next state | Receipt |
|---|---|---|---|---|
| entry pending, `decl.requires.world` unmet in `folded` | `prerequisite.evaluate` | — | finding `{kind:'prerequisite', facet:'world', offers:['self']}` | the unmet predicate names, `compareCodepoint`-sorted |
| entry pending, payload names a `GATE_FEATURES` key, validator reports a violation against it | `prerequisite.evaluate` | `liveInstitutions(folded)` | finding `{kind:'prerequisite', facet:'gate'}` | the gate's own `reason`; `fulfil` = `add-institution` of the first missing prerequisite |
| faction share sum differs from the renormalised clone's sum | `totality.evaluate` | `appliesTo` = add-faction, rebalance-power, remove-faction | finding `{kind:'totality', facet:'share'}` | both sums and the rebalanced shares line |
| a PRIOR entry on the SAME target whose type is in `decl.conflictsWith` | `contradiction.evaluate` | — | finding `{kind:'contradiction', relatedEntryId}` | `keepBoth`/`keepFirst`/`keepLast` |
| a PRIOR entry on the SAME target sharing a payload field | `contention.evaluate` | — | finding `{kind:'contention', relatedEntryId}` | `keepFirst`/`keepLast` |
| `decl.requires.registry` names a type no prior entry carries | `connection.evaluate` | — | finding `{kind:'connection', facet:'sequence'}` | `reorder` only when the owed type is staged LATER; `fulfil` when exactly one type is owed |
| a LATER entry whose type is in `decl.relatedTo` | `connection.evaluate` | the sequence facet did not fire | finding `{kind:'connection', facet:'related'}` | the follows-from pair, `reorder` |
| none of the above | any rule | — | `null` | silence |

### Ordering and precedence

- **Rule order:** `compareCodepoint` over rule ids — connection, contention, contradiction, prerequisite, totality. The engine runs them in that order and the suite asserts it.
- **Within the prerequisite rule:** the WORLD facet is judged first and SHORT-CIRCUITS the gate facet, because a seal whose world condition fails is design §18's "not offered" case and the gate question does not arise.
- **Within the connection rule:** the SEQUENCE facet short-circuits the RELATED facet, because an owed prerequisite is the stronger statement about the same pair.
- **Merge/replace/deduplicate:** none here. A rule yields at most one finding per entry; the engine dedupes offers and mints ids.
- **Tie-break:** every list a message names is `compareCodepoint`-sorted before it is joined, so a guard's text is a function of the world and never of iteration order.

### Determinism

- **Hash/fork key:** `NONE`. This leaf mints no stream and consumes no draw; `createPRNG(` site count is 0 in the planted text and the census moves by +0.
- **Stable enumeration:** `compareCodepoint` everywhere a list becomes text; `GUARD_RULES` is authored and asserted in that order.
- **Rounding/clamping:** `Number(x) || 0` is the only coercion, and the only arithmetic is the share sum. The renormalisation itself is the injected writer's.
- **No-draw behavior:** total. The rules are pure functions of `(ctx)`; the one non-obvious case is `checkStructuralValidity`, MEASURED at 0 draws and byte-identical across two calls with no active RNG, which is what makes calling it inside a pure guard lawful.

### Flag and dormancy

- **Flag:** `NONE`. Wave 2 is headless.
- **Gate:** `NONE` in this leaf. The editor's tier gate (`TIER_GATE.premium.editMode`) is a surface fact and lives in wave 4.
- **Absent / False / True:** not applicable.
- **Dormancy:** the CREATE lands **DARK** — **zero importers under `src/`** at the verified base, exactly as `operations.js` did at EM-B1a. Its first `src/` caller is EM-C2's engine, composed at EM-C4b. TWO TEST files import it at this member's own landing: its acceptance battery `tests/domain/editGuardRules.test.js`, and — from version 4 — `tests/lint/opGuardCoverage.walker.test.js`, which imports `rulesFor` to assert the DERIVED coverage (judgment 251). A test importer moves no emitted chunk, so DARK in the budget sense is unchanged and every byte figure below still reads **+0**.
- **Golden posture:** `UNCHANGED`. `tests/property/generatorGoldenMaster.test.js` and `tests/property/dossierProseManifest.test.js` must not move, and they cannot: nothing reachable from a generation or a prose run imports this leaf.

### Lifecycle

| Create | Read | Persist | Reload | Regenerate | Undo | Import/migrate | Public veil |
|---|---|---|---|---|---|---|---|
| module-scope frozen constants, once | `rulesFor` / `makeGuardRuleSet` by the engine | ⛔ NOTHING. A guard is derived, never stored | n/a | a guard is recomputed from the record and the registry every time | n/a — `overrode` lives on the ENTRY (EM-C1), never here | n/a | ⛔ nothing here reaches a projection; the veil is EM-B3's and this leaf adds no key |

### Receipts and privacy

- **Closed kinds:** `GUARD_KINDS`, exactly five, frozen and exported.
- **Address chain:** a finding names `relatedEntryId` only; the engine mints `<ruleId>:<entryId>:<relatedEntryId or ->:<facet or ->`.
- **Numeric-to-word bands:** `NONE`. The totality message prints two integers and a share list, which are counts rather than bands, and `tests/lint/proseNumerics.test.js` holds zero rows naming this path.
- **DM-only fields:** `NONE`.
- **Player/public projection:** `NONE` — no guard is ever projected; see the lifecycle row.

### Negative assertions, and the TYPE FLOOR's two configs

- **Every negative this member adds lives in `tests/domain/editGuardRules.test.js`** and carries
  `// anchored: <why>` on the ONE line immediately above the `expect` ITSELF (never above a
  wrapped `expect`'s first line — §P6's 2026-09-21 correction). The POSITIVE CONTROL that
  anchors each is in the same arm: A3's `null` on a seated wall is anchored by the same world
  firing the finding without it; A4's silence once the person is present is anchored by the
  finding the absent person produced; A6's two `null`s on different targets are anchored by the
  two findings on the same target; A7's "no connection finding for `remove-npc`" is anchored by
  `promote-phantom`'s finding in the same arm. `tests/lint/negativeAssertionAnchor.walker.test.js`
  walks `tests/` whole, so this CREATE opts into it by existing.
- **THE TYPE FLOOR IS TWO CONFIGS AND THEY DISAGREE.** Read at `bb0497eb4`:
  `tsconfig.full.json` includes `src/domain/**/*.js` and EXCLUDES `tests`, so it governs
  `src/domain/edit/guardRules.js` alone of this member's five paths (`npm run typecheck:ratchet`).
  `tsconfig.domain-strict.json` EXTENDS `tsconfig.json`, whose include is exactly
  `src/domain/**/*.js` + `src/domain/**/*.ts` with `tests` excluded and `checkJs: true`, and flips
  `strict` and `noImplicitAny` on — so it ALSO governs `guardRules.js` and nothing else here
  (`npm run typecheck:domain:strict`; a NEW or WORSENED `src/domain` file must be strict-clean).
  The four test paths are governed by NEITHER. The leaf therefore carries zero `@type {any}`,
  zero `{*}` and zero `@ts-` directives (measured 0 and 0 over the planted text) and narrows the
  engine's `unknown` doors through its own type predicates.

### Alignment and edit story

- **Alignment:** `DECLARED EMPTY: a guard rule judges an edit; it is not itself an editable fact and no card declares it.`
- **Edit story:** `ENGINE-ONLY: the DM's verbs are the ops of EM-B1a/EM-B1b; this leaf only advises about them.`

### User-facing copy

Five templates, SPELLED. Every one is a plain sentence, no em dash and no exclamation point
(measured at `bb0497eb4` by `tests/copy/voiceMechanics.test.js`'s own espree literal walk: `em 0,
bang 0` over **80** string literals — version 2's §6 said 107 in this one place and the capsule
said 80; 80 is the walker's own count. The `src/generators` Tier-5 budget is at 28 of 28 and this
member spells NO `src/generators` literal, so it moves by +0). Interpolations use the estate's template-literal idiom and the parameter
names below.

| id | the words |
|---|---|
| `prerequisite` / `world` | `This act needs the world to be a way it is not yet: ${unmet}.` |
| `prerequisite` / `gate` | `${named} rests on something the town does not have: ${reason}` |
| `totality` / `share` | `The factions hold ${sum} points of power between them, not ${target}. Rebalanced they would read: ${shares}.` |
| `contradiction` | `This undoes what ${otherType} already does to the same thing.` |
| `contention` | `Two waiting acts set the same fact. The later one wins unless you say otherwise.` |
| `connection` / `sequence` | `This follows from ${owed}, which is not staged before it.` |
| `connection` / `related` | `The catalogue reads this and ${otherType} as one account.` |

`${reason}` is `GATE_FEATURES[named].reason`, the estate's own authored sentence, quoted and never rewritten.
`${unmet}`, `${owed}` and `${shares}` are `compareCodepoint`-sorted joins on `', '`.

## §7 · Exact change manifest

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `src/domain/edit/guardRules.js` | `GUARD_RULE_DEPENDENCIES`, `GUARD_RULES`, `project`, `makeGuardRuleSet`, `rulesFor` | `250 eff` | The five rules and the fold, at §6's contract. Import EXACTLY four modules: `../../data/spatialData.js` (`GATE_FEATURES`), `../deterministicSort.js` (`compareCodepoint`), `./guards.js` (`GUARD_KINDS`, `GUARD_OFFERS` - the ENGINE's vocabularies, judgment 236) and `../institutions/institutionRoster.js` (`liveInstitutions`), and name the engine's four typedefs TYPE-ONLY. ⛔ Nothing from `src/generators/`, nothing from `operations.js`, and NO second spelling of either vocabulary. Zero `any`, zero `@ts-` directives, zero UPPER_SNAKE numeric constants. MEASURED over the planted text: **168 effective lines**, static closure **5 modules / 62,950 raw B** (+1 module / +17,281 B over version 1, which is `guards.js` itself). |
| `CREATE` | `tests/domain/editGuardRules.test.js` | the battery + A1-A8 | `250 eff` | ⭐ **ADDS EIGHT `it`s UNDER ONE `describe`** - the same eight §9 homes here and the same eight the deferred lighting row prices. ⭐ **JUDGMENT 196's TITLE LAW, MEASURED:** every `it` title BEGINS `A<n> - EM-C3: ` so it is UNIQUE in `tests/` - measured at `1b073009f`, `git grep -n -F 'EM-C3' -- tests` returns FIVE hits and every one is a COMMENT line of `tests/lint/opGuardCoverage.walker.test.js`; no test TITLE anywhere in `tests/` contains the string, and `vitest -t` is a REGEX. ONE literal `describe`, eight straight-line literal `it`s, `it`/`test`/`describe` each bound EXACTLY once from `vitest` and never rebound (not even as a parameter). Every set IMPORTED from its producer — `OP_TYPES`, `GATE_FEATURES`, and the two REAL generator writers, which a test file may import because the boundary ratchet walks `src/domain` alone. No bare `for (const seed of ...) { expect(...) }`: collect, then assert once. Every `not.toContain`/`not.toMatch`/`not.toHaveProperty` carries `// anchored: <why>` on its own line or the ONE line above. |
| `TEST` | `tests/build/vendorPdfLazy.test.js` | the `editorTrain` roster | `+1 eff` | ⭐ **ADDS ZERO `it`s** (a roster row inside a landed arm). ⭐ **THE ARRAY IS ELEVEN ROWS, MEASURED HERE AT `bb0497eb4`** (the chair's judgment 243: version 2's premise that EM-C2 leaves a twelve-row array is FALSE — EM-C2 version 3 carries no `editorTrain` row, and this row is written against the array this seat read itself). Add `'src/domain/edit/guardRules.js'` as the TWELFTH entry, between `'src/domain/edit/fieldDeclarations.js'` and `'src/domain/edit/operations.js'` — the `src/domain/edit/*` block's own codepoint order. ⚠ The array as a whole is NOT codepoint-sorted (`'src/domain/density/densityCreateBoundary.js'` stands after the `edit` block) and NO arm asserts an order, so the position is legibility, never a pin. ⭐ **THE HOLDER'S RECEIPT — WHICH ASSERTION MOVES, AND BY HOW MUCH.** The arm carries four assertions and exactly ONE changes CARDINALITY while NONE changes VALUE: `expect(present).toEqual(editorTrain)` goes from eleven rows on both sides to twelve on both sides, because the CREATE exists at this member's landing commit; `expect(present.filter((rel) => eagerRel.has(rel))).toEqual([])` stays **`[]`**, measured — `EAGER_FIRST_PAINT_MODULES` is 270 at this tip and names NOTHING under `src/domain/edit/`; and the two anti-vacuity legs (`eagerRel.size > 50`, `eagerRel.has('src/main.jsx')`) are untouched. The arm has NO totality over `src/domain/edit/`, so an unlisted CREATE would red nothing — the row is carried because this member HOLDS this file's row on train EM-T17, not because an arm compels it. |
| `TEST` | `tests/lint/flavorFields.census.test.js` | `SCAN_ROOTS` and its docblock | `+2 eff` | ⭐ **ADDS ZERO `it`s.** ⭐ **THE FOURTH ROOT, RE-ADDED** (EM-C2's finding; judgment 236). The census's own docblock records that the governing document named `src/domain/edit/guards*`, that the pattern `/(^\|\/)guard[A-Za-z]*\.js$/` matched ZERO files, and that a root resolving to an empty set would red case A4's own fence - so the root was DROPPED with the measurement written down for a later lane. That lane is this one. `filesUnder` takes a directory or a FILE and applies no glob, so the fourth root is spelled as TWO FILE roots, `'src/domain/edit/guards.js'` and `'src/domain/edit/guardRules.js'`, appended after `'src/domain/causalState.js'`; the dropped-root paragraph is REWRITTEN to record that the pattern now matches and what re-added it, never deleted. ⛔ **THE CENSUS'S CLAIM IS UNMOVED, MEASURED:** its `freeRows()` filters `kind === 'free'`, which at this tip is exactly `institution.note` and `npc.note`, and NEITHER new root spells `.note` in comment-blanked code (0 and 0). `guardRules.js`'s three `.name` reads are `institutions[].name`, a `free-cascade` row the census does not govern. A4's empty-root arm is satisfied because both files exist at this member's landing commit. |
| `TEST` | `tests/lint/ruinFilterRoster.walker.test.js` | the census arm's `readers.length` pin | `+0 eff` | ⭐ **ADDS ZERO `it`s.** ⭐ **THE DISCOVERY COUNT, RE-MEASURED AS A DELTA (EM-PREAMBLE §P2.21(b)).** The walker's `discoverReaders()` walks every non-test `.js` under `src/domain` and enrols each file whose `codeOnly`-blanked CODE matches `/\.institutions\b/`; `project` reads `.institutions` in this CREATE's code, so the leaf ENROLS. EXECUTED at `bb0497eb4` with the walker's own two regexes: the tip reads **94**, exactly the figure the census arm pins; with this member's planted leaf it reads **95**, and the row is `{ rel: 'src/domain/edit/guardRules.js', compliant: true }`. Change the ONE literal `expect(readers.length).toBe(94)` to `toBe(95)` and record the cause in the file's own ⭐-comment idiom, beside the `93 → 94` note EM-A2b version 7 left. ⛔ **NO OTHER ARM MOVES, MEASURED:** the COMPLIANT-or-EXEMPT arm stays `[]` because this leaf imports `liveInstitutions` (the walker's `COMPLIANT_RE` reads RAW bytes); `RUIN_AGNOSTIC_EXEMPT` gains NOTHING and its honesty arm is untouched; the quarantine arm, the non-vacuity floor (`> 40`) and the two `not.toContain` legs are unmoved. ⛔ **NO EXEMPT ROW IS OWED AND NONE MAY BE ADDED:** a compliant reader that is also exempted reds the exempt-honesty arm by name. The member's mutation-coverage obligation does not move: the register already holds `invariants['tests/lint/ruinFilterRoster.walker.test.js'] = { kind: 'uncovered' }` and a literal change leaves that row where it is. ⚠ **THE ARM'S OWN MESSAGE DECIDES AT THE BUILD:** a figure other than 95 is a STOP for the chair (§11), never a number adopted by hand. |
| `TEST` | `tests/lint/opGuardCoverage.walker.test.js` | the landed `C5` and `C7` bodies | `+12 eff` | ⭐ **ADDS ZERO `it`s AND ZERO `describe`s** — the widen lands INSIDE two landed arm bodies, so no title is created and none moves (judgment 148's law), and the lighting delta of this member is unchanged. ⭐ **THE RE-SLOT OF EM-C2b (the chair's judgment 251).** Import `rulesFor` from `../../src/domain/edit/guardRules.js` and assert the DERIVED coverage: in **C5**, the guard-the-guard leg — `rulesFor` is a function, the rule roster is non-empty, and every key of the LIVE `OP_TYPES` reaches a NON-EMPTY rule set (an empty one anywhere is the vacuity this leg exists to refuse); in **C7**, beside the existing current-state pin, the coverage table in BOTH DIRECTIONS — the types whose derived coverage has FIVE rules set-equal `compareCodepoint`-sorted to the `appliesTo` of the `totality` rule read off `GUARD_RULES`, **3** of them, and the remaining **19** at FOUR, the two limbs partitioning `Object.keys(OP_TYPES)` exactly. ⛔ **EVERY SET IS DERIVED OR IMPORTED, NEVER RE-TYPED** (`tests/lint/contractTestAntiVacuity.walker.test.js` Rule 2 convicts a hardcoded exhaustive claim): the op types come from the imported `OP_TYPES`, the five-rule roster from the imported `GUARD_RULES`, and only the two COUNTS (3 and 19) and the partition are written down. ⭐ **C7's `populated` ROW KEEPS ITS SEMANTICS AND GAINS THEM IN THE CODE:** `OP_TYPES[*].guards` stays the INERT empty array and every `guardsStated` keeps its `/\bno guard\b/i` sentence — ARCH §5's "stated when empty" — because coverage is DERIVED FROM THE RULES and never authored on a row; the comment says so, so the next reader does not re-open EM-C2b. ⛔ **THE FOUR LANDED ARMS ARE LISTED AND KEPT, titles verbatim:** `C5: the scan, the roster and the catalogue are all real, ...`, `C6: the declared coverage set and the live source scan are SET-EQUAL in both directions, ...`, `C7: a declared rule that cannot fire reds, ...`, `C8: this walker owns its entry in the mutation-coverage manifest, ...`. C6's two planted controls, C7's four mutants and C8's register assertions are UNTOUCHED, and `coverageProblems`, `declaredKeys`, `bend`, `authoredKeys` and `catalogueModules` are not re-shaped. ⛔ Add NO `not.toContain` / `not.toMatch` / `not.toHaveProperty` negative (none is needed; the claims are equalities), so no new `// anchored:` marker is owed. |

**Register dispositions, each figure a DELTA measured by running the instrument's OWN logic over the planted text:**

| instrument | disposition | delta |
|---|---|---|
| `tests/lint/entropyRootCensus.walker.test.js` | the leaf mints no stream and reads no world root | `createPRNG(` SITE COUNT **+0** (`src/domain` 36, `src` 47, unmoved) |
| `tests/lint/ruinFilterRoster.walker.test.js` | **ROUTED / COMPLIANT, AND THE COUNT MOVES** — `project` reads `.institutions` in code so the leaf ENROLS in the discovery set, and it carries the accessor import, which is the walker's own compliance evidence on raw bytes. The disposition is a §7 TEST row above (§P2.21(b)); version 2 declared the walker unmoved and that was the measured error EM-A2a made before it | discovery count **94 → 95** (EXECUTED with the walker's own `discoverReaders()` over the tip and over the planted text); exempt table **+0**; quarantine **+0**; `compliant: true` |
| `tests/lint/observedShapeReaders.walker.test.js` · `tests/lint/writerReach.walker.test.js` | the CREATE joins `sourceFiles()`'s 2,263-file scan corpus (`isObservedShapeScanPath` returns true), but no row is owed: it reads no save-time key (`dmLayer`, `decrees`), `EXPLAINED_WRITER_EXEMPTIONS` names no `src/domain/edit` path, `osrBuiltinMembersFromSource` reads a `scripts/lib` module this member never touches, and the estate's own disposition is measured rather than argued — **0 of the 50 EM packets** name an observed-shape or writer-reach path in a change manifest while **10 LANDED members CREATE under `src/domain/edit/`**, `pools.js` (a `.institutions` reader) among them | **NOT OWED.** `osr.size` **+0**, `live.explainedWriters.banked` **+0** — the register's own arms are corpus-driven, so this is the one PLAUSIBLE row of the table and the build's real run settles it |
| `tests/build/vendorPdfLazy.test.js` ST-2 goods roster | the leaf imports no goods identity half-table | **NOT OWED** |
| `tests/copy/voiceMechanics.test.js` Tier 2 | a CREATE under `src/domain` is held at ZERO | **em +0, bang +0** (107 literals, measured with the walker's own espree walk) |
| `tests/lint/domainAnyCastBaseline.test.js` | a NEW `src/domain` file with any suppression debt FAILS | **any 0, suppress 0** |
| `tests/lint/tuningRegister.walker.test.js` P2 / P3 | new files at zero, both arms, over `codeOnly` | **0 unregistered named constants, 0 bare decimals.** The totality target is read from the writer's own renormalised clone rather than spelled as a literal, which is why P2 is zero |
| `tests/lint/proseNumerics.test.js` | line-addressed register | **none found** — 0 baseline rows name either path |
| `tests/lint/sourceCitationIntegrity.walker.test.js` | line-addressed register | **none found** — 0 baseline rows name either path |
| `tests/lint/opGuardCoverage.walker.test.js` (C7's coverage pin) | the walker becomes the SINGLE READER of the derived coverage (judgment 251, closing EM-C2b). MEASURED at `bb0497eb4`: `OP_TYPES` is 22 rows, `populated` is `[]`, all 22 `guardsStated` match the emptiness claim, and the derived table from this leaf's `rulesFor` is **19 × 4 / 3 × 5, zero rows empty** | `populated` **+0** (it stays `[]` — no row is authored), `unexplained` **+0**, titles **+0**, suiteTitles **+0**; C8's register entry (`kind: 'rationale'`, a 1,787-character reason) **unmoved** |
| `scripts/mutation-coverage-manifest.json` | `tests/domain` is not one of the eight `ENFORCER_DIRS` and `editGuardRules.test.js` matches no `NAME_PATTERN` token (measured with `tests/lint/mutationCoverage.shared.mjs`'s own predicate) | **NO ROW OWED** |
| edge-shared bundles | neither path appears in any of the five metas' `inputs` lists (107 / 74 / 108 / 2 / 2) | **NOT OWED** |
| `tests/lint/.lighting-census-baseline.json` | ⚠ **A DECLARED INTERIOR RED, never sealed.** MEASURED with the walker's own `parkReasonsFor` / `liveTitlesIn` / `liveSuiteTitlesIn`: the CREATE is **CREDITED** (zero park reasons) with **8 titles and 1 suite title** | `files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +1`. The refreeze is the chair's at the train's terminal; this member never runs one |
| `docs/content/wiring-census.json` | ⚠ **A DECLARED INTERIOR RED** on car 0 ("byte-identical to a fresh build"). MEASURED at `bb0497eb4` with the census's own `producerIndex` and `astTokens` over the version-3 text, AT BOTH TIPS because the answer differs | `stamp.producerIndexFiles` **+1** — 1190 → 1191 measured at `bb0497eb4`, and **1191 → 1192 at EM-C2's flip**, since `guards.js` takes the first `+1` (`producerCitations` walks `src/generators/**` and `src/domain/**`, and this CREATE is one `.js` under the second). The produced-key delta is **+4 at `bb0497eb4`** (`appliesTo`, `evaluate`, `fulfil`, `relatedEntryId`, against a live 13,580) and **+2 AT EM-C2's FLIP** (`appliesTo`, `evaluate`) — EXECUTED: `guards.js` itself produces `fulfil` and `relatedEntryId` among its nine novel keys, so two of version 2's four stop being novel the moment the engine lands. `stamp.files` **UNMOVED** (its 7 entries name neither path). The chair regenerates once at the terminal |
| `tests/build/generationWorkerLazy.test.js` · the lazy engine · the first-paint closure | the CREATE lands **DARK** (zero importers under `src/`; it IMPORTS `guards.js` but nothing imports IT), so it enters no emitted chunk | **+0 B on all three.** `WORKER_BUNDLE_CEILING_BYTES` 1391327 (exact, zero slack) is untouched; `EAGER_FIRST_PAINT_MODULES` is 270 and names nothing under `src/domain/edit/` |

> THE DEFERRED LIGHTING ROW, in the register's own order: **`files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +1`.**
> `+8` is DERIVED and the count law binds it (§P9(f)): the CREATE's **eight** `it`s and nothing else;
> the THREE `TEST` rows add no title — `vendorPdfLazy` gains one array entry, `flavorFields.census`
> two root entries and a rewritten paragraph, and `opGuardCoverage` assertions INSIDE two landed
> arm bodies. MEASURED: `opGuardCoverage.walker.test.js` is CREDITED today with FOUR titles and ONE
> suite title, and an in-place widen leaves both where they are. `+1 suiteTitles` is its one literal `describe`.

Generated artifacts: `NONE`.

This table and the JSON `changeManifest` are SET-EQUAL on paths and on actions.

No other file may be edited.

## §8 · Ordered coding sequence

0. Dispatch and seal the packet; stop on any preflight mismatch.
1. Capture the baseline: run the three governing arms of the layering ratchet, the any-cast ratchet and the tuning walker BEFORE the first edit, and record their figures.
2. Write `tests/domain/editGuardRules.test.js` RED FIRST, all eight arms against an absent module; the log must print a real test count.
3. Implement `src/domain/edit/guardRules.js`: the FOUR imports of §7's CREATE row (`GATE_FEATURES`, `compareCodepoint`, the engine's two vocabularies, `liveInstitutions`), the four TYPE-ONLY typedefs, the accessors. The vocabularies are IMPORTED, never spelled here (judgment 236).
4. Implement the five `evaluate` functions in `compareCodepoint` order of their ids.
5. Implement `project` for the five op types that move a fact a rule reads, returning `null` for every other catalogue row.
6. Freeze `GUARD_RULES` and `GUARD_RULE_DEPENDENCIES`; add `makeGuardRuleSet(deps)` at `EMPTY_RULE_SET`'s three keys and `rulesFor`.
7. Add the one `editorTrain` row to `tests/build/vendorPdfLazy.test.js` (the ELEVEN-row array becomes twelve) and re-address the ruin-filter census arm in `tests/lint/ruinFilterRoster.walker.test.js` from `toBe(94)` to `toBe(95)` with its ⭐-comment. Then widen `tests/lint/opGuardCoverage.walker.test.js` in place (judgment 251): the `rulesFor` import, C5's guard-the-guard leg, C7's derived table and its semantics comment. RUN ALL THREE WALKERS' OWN ARMS BEFORE AND AFTER: the ruin walker's red at the pre-edit tree is this member's red-first for that row and its message names the figure; `opGuardCoverage` is GREEN before and after (its widen is a WRITTEN-CONTRACT arm, proved by its counterforce per §P6 — plant a rule set whose `rulesFor` returns `[]` for one type, watch C5's new leg red BY NAME, restore the exact pre-mutant SHA-256, green).
8. Run focused verification (§10), then the wave-end gate, then write the completion receipt.

Bounded algorithm (one rule, one entry):

```text
1. The engine folds: world_0 = world; world_i = ruleSet.project(world_{i-1}, entries[i-1].op, catalogue) ?? world_{i-1}.
2. For each rule in compareCodepoint id order:
     a. if rule.appliesTo !== null and does not include op.type -> skip, silently.
     b. if any name in rule.needs is absent from ctx.deps -> skip, and the engine reports the id as unevaluated.
     c. finding = rule.evaluate(ctx). null -> silence.
3. The engine validates the finding, dedupes offers, appends 'proceed', mints the id.
```

## §9 · Acceptance matrix

| ID | Case | Fixture/input | Required observation | Test home |
|---|---|---|---|---|
| A1 | The roster and the vocabulary, both directions | `GUARD_KINDS`, `GUARD_RULES`, imported `OP_TYPES` | `GUARD_KINDS` is exactly the design's five; `GUARD_RULES`' ids are that list in `compareCodepoint` order; every rule carries all four required keys with `evaluate` a function; every op type in any `appliesTo` is a key of `OP_TYPES` | `tests/domain/editGuardRules.test.js` |
| A2 | No rule can refuse (counterforce) | every row of `OP_TYPES`, driven twice | every finding's `kind` is in `GUARD_KINDS` and every offer is in `GUARD_OFFERS`; collected once and asserted once | same |
| A3 | Prerequisite, gate facet, through the REAL validator | `add-institution` of `Citadel` into a roster without walls, then with `City walls and gates` | the finding carries `GATE_FEATURES.Citadel.reason` and a `fulfil` naming the first missing prerequisite; the seated case is `null`; an ungated name returns `null` before the validator is called | same |
| A4 | The standing-roster law, and the world facet | the same world with `_worldPulseInactive: true` on the wall; `remove-npc` against an absent and then a present person | the ruined wall fires the gate finding the live wall silenced; the world facet names `npcPresent` and falls silent once the person is present | same |
| A5 | Totality reads its target from the writer (idempotency) | rosters summing to 97, to 100, and all-zero | 97 fires naming 97 and 100 with a sorted shares line; 100 and all-zero are silence; the world is BYTE-IDENTICAL after the call | same |
| A6 | Contradiction and contention fire only on ONE target (boundary) | `remove-institution` after `set-institution-state`, same then different targets; two `set-field` sharing a field | both fire on the same target and both are `null` on different targets | same |
| A7 | Connection judges the registry half, never `op.requires` (real integration) | `promote-phantom` with `found-phantom` absent, staged later, staged earlier; `remove-npc` | `reorder` only when the owed entry is staged later; `remove-npc` yields NO connection finding although its flattened `op.requires` reads `npcPresent` | same |
| A8 | The fold and purity (lifecycle/regression) | every `OP_TYPES` row through `project`; every rule over a malformed world | `project` returns a new world for its five types and `null` for the rest, with its input byte-identical; `rulesFor` yields >= 4 for every type and 5 for the three share types; nothing throws | same |

Every case names the file that holds it and the §7 row that authorizes that file's edit: all eight
live in the `CREATE` of `tests/domain/editGuardRules.test.js`. The row's declared arm count (8), the
cases homed there (8) and §7's lighting title delta (+8) state the SAME number; the red-first plant
is not an `it` and is not counted.

## §10 · Verification commands

```sh
# Focused static checks
npx eslint src/domain/edit/guardRules.js tests/domain/editGuardRules.test.js tests/build/vendorPdfLazy.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict

# Focused tests: acquire the one test slot in the same command
sh scripts/gate-mutex.sh --run -- npx vitest run tests/domain/editGuardRules.test.js tests/domain/editOperations.test.js tests/domain/editDeclarations.test.js tests/domain/mundaneDisplayAndValidator.test.js
sh scripts/gate-mutex.sh --run -- npx vitest run tests/lint/flavorFields.census.test.js tests/lint/opGuardCoverage.walker.test.js tests/lint/editMutationPath.walker.test.js tests/lint/editDeclarations.walker.test.js tests/lint/ruinFilterRoster.walker.test.js tests/lint/entropyRootCensus.walker.test.js tests/lint/tuningRegister.walker.test.js tests/lint/domainAnyCastBaseline.test.js tests/lint/domainStrictBaseline.test.js tests/lint/negativeAssertionAnchor.walker.test.js tests/lint/contractTestAntiVacuity.walker.test.js tests/lint/seedLoopTotality.walker.test.js tests/lint/moduleScopeCwdRatchet.test.js tests/lint/proseNumerics.test.js tests/lint/sourceCitationIntegrity.walker.test.js tests/lint/mutationCoverageManifest.test.js
sh scripts/gate-mutex.sh --run -- npx vitest run tests/generators/structuralValidatorDeterminism.test.js tests/generators/structuralOverrideDedup.test.js tests/generators/isolationSupport.test.js tests/generators/neighbourFactionRenorm.test.js tests/generators/normalizeFactionsZeroPowerGuard.test.js
sh scripts/gate-mutex.sh --run -- npx vitest run tests/copy/voiceMechanics.test.js

# Named golden/dormancy proof
sh scripts/gate-mutex.sh --run -- npx vitest run tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js

# THE BOUNDARY ARM, and it is the one that decides this member's shape.
# It reads dist for nothing and cannot skip.
sh scripts/gate-mutex.sh --run -- npx vitest run tests/build/domainGeneratorsBoundary.test.js

# PRE-SEAL, before any of the above: the count prover that ships beside this packet.
node EM-C3.count-prover.mjs EM-C3.md EM-C3.manifest.json
# It RECOMPUTES the §5 clause's SHA-256 over this packet's own copy and prints it.

# PRE-REPORT, no gate slot: the TypeScript compiler API over this packet's own src/ path.
node <scratch>/typecheck-paths.mjs src/domain/edit/guardRules.js

# The instruments a new src/domain leaf meets, each run with its OWN logic in plain node at
# compile (done; §7) and through the gate at the build.

# ⛔ NOT in `checks` and run under verify:dist at the landing, because it reads dist:
#   tests/build/vendorPdfLazy.test.js  (this member's editorTrain row)
#   tests/build/generationWorkerLazy.test.js

# Sealed receipt and exact-state handoff; neither is landing authority
npm run check:packet -- EM-C3
npm run implementation:resume -- EM-C3

# Wave-end presentation-safe invocation of the authoritative npm run check; never pipe
npm run check:tail
```

Expected: every command exits `0` — including `tests/lint/ruinFilterRoster.walker.test.js`, whose census arm this member RE-ADDRESSES in the same commit (94 → 95), so it is a cured red rather than a declared one — except the two DECLARED INTERIOR REDS of §7 —
`tests/lint/sovereigntyLightingContract.walker.test.js` (the +1/+0/+1/+8/+1 delta) and
`tests/lint/proseWiringCensus.walker.test.js` car 0 (`producerIndexFiles` +1). Neither is sealed;
both are the chair's at the train's terminal. Report actual counts; do not copy historical counts.

## §11 · Mandatory STOP conditions

In addition to `PACKET_STANDARD.md`, stop if:

- the dispatch seal is missing, invalid, or belongs to another worktree state;
- a governing e2e spec, walker or ratchet reds and the packet would weaken, widen or skip it to pass;
- a sealed acceptance case has no file and no §7 row authorizing that file's edit;
- a governing instrument reds with a figure this packet did not predict (the arm's own message decides);
- a red-first's log prints no test count, or `Tests no tests`;
- resume reports authority, HEAD, foreign-work, or receipt-integrity drift;
- **the §5 clause's SHA-256 over this packet's own copy is not `20ff767bc6a53ff5b3f178932017eab9af721b03bc98f65bbd62d100a8576aae`**, or EM-C2's copy digests differently. ONE TEXT, two packets (judgment 236); a divergence is a STOP at either placement and at the build;
- **`src/domain/edit/guards.js` is absent, or does not export `GUARD_KINDS` and `GUARD_OFFERS` verbatim, at dispatch.** EM-C2 lands FIRST, and the `_pendingRequiredSymbols` rows are re-measured VERBATIM at their discharge (judgment 151) because a contract's spelling is not the code's;
- **either vocabulary is re-spelled in this leaf** rather than imported from the engine;
- **the build reaches for `checkStructuralValidity` or `renormalizeFactionPower` with an import** rather than through `ctx.deps`, for any reason including a failing test. That is the measured red of `tests/build/domainGeneratorsBoundary.test.js` and the cure is never to widen its baseline;
- **the build reaches for `OP_TYPES` with an import under `src/`.** `tests/lint/editMutationPath.walker.test.js` convicts it by name and `READ_ONLY_CONSUMERS` is not this member's to widen;
- **a rule is found to need a fact `ctx` does not carry.** The `ctx` shape is the shared clause; adding a field is a change to EM-C2's packet as well, and improvising one is a STOP;
- **the `stress` key, or any re-derived reading, is found to be something a rule must hold.** Design §22.1 correction 5 rules `stress` re-derived and never held; a guard that needs it is a different member's ground;
- **`tests/lint/ruinFilterRoster.walker.test.js`'s census arm reds with a figure other than 95**, or the COMPLIANT-or-EXEMPT arm names this leaf, or the exempt table or the quarantine list moves at all. The arm's own message decides and a figure adopted by hand is the failure (§P8; EM-A2b version 7's shape);
- **`tests/lint/opGuardCoverage.walker.test.js` reds on any of C5, C6, C7 or C8 after the widen**, or the derived table is not 19 × 4 / 3 × 5 with zero empty rows, or a landed arm's title, mutant, control or predicate helper has moved. The widen is ADDITIVE inside two bodies; a member that has to reshape `coverageProblems`, `declaredKeys` or the mutants to pass has found something the chair must rule (judgment 251);
- **populating `OP_TYPES[type].guards` looks necessary after all.** It is CLOSED, not deferred: EM-C2b's compile measured that the only firing shape is `operations.js` importing this leaf, which the §5 clause item 2 forbids in both packets;
- **`tests/build/vendorPdfLazy.test.js`'s `editorTrain` array is not ELEVEN rows at dispatch**, or its eager-membership assertion returns anything but `[]` once the row is added. Either means a sibling moved the file under this member and the row was written against a tree that no longer exists (judgment 243's whole shape);
- **populating `OP_TYPES[type].guards` looks necessary to make an arm pass.** It is out of this member's manifest (§12, ruling 2) and its price is recorded there.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into the next wave.

## §12 · Completion receipt

- Base SHA:
- Dispatch bundle and seal identity:
- Final commit or working-tree state:
- Exact changed files and effective-line deltas:
- Acceptance cases:
- Focused commands, exits, and counts:
- Sealed per-step receipt and exact-state resume status:
- Both typecheck configurations:
- Wave-end gate stages actually executed:
- Base-versus-wave failure identity diff:
- Dormancy/golden result:
- Bundle/first-paint result:
- Generated artifacts: `NONE`
- Deviations: `NONE | STOP`
- Out-of-scope observations, without investigation:
- Judgment calls: `NONE`

### THE RULINGS — ALL FIVE RULED BY THE CHAIR AT VERSION 1 (judgments 235a to 235e, 236)

Version 1 put five calls to the chair; every one is now ruled, and version 2 carries the ruling
rather than the question. They are recorded here because a packet's own body is where a build
lane and a pre-proof find them.

1. **THE INJECTED SEAM — ADOPTED (235a).** The two generator writers arrive on `ctx.deps`. A
   static import of `src/generators` from `src/domain/edit` reds the landed boundary ratchet;
   the injected shape leaves both of its arms exactly where the tip has them. REVERSAL: none
   available — moving either symbol down a layer is a `retiredSymbols` row that must discharge
   EM-B1a's LANDED rows.
2. **`OP_TYPES[type].guards` STAYS EMPTY — AND EM-C2b IS NOW CLOSED, NOT SLOTTED (the chair's
   judgment 251, superseding 235b).** EM-C2b's compile STOPPED on a measured contradiction: `OP_TYPES`
   is a frozen module-scope literal whose every row is frozen, and the two leaves authoring 8 of its
   22 rows import nothing, so a rule function can reach a row ONLY if `operations.js` statically
   imports this leaf — which §5 clause item 2 forbids in both packets — and a resolver reading the
   rule set off `ctx` cannot fire, since item 5 fixes `ctx` with no rule set on it. Measured beside
   it: a rule-id STRING population yields **44** offenders from the landed walker's own predicate; a
   FUNCTION population reds **22** contradiction rows and needs all 22 `guardsStated` sentences
   rewritten across THREE landed leaves, after which ARCH §5's "stated when empty" half has no
   population left to police; `git grep -P '\.guards\b' -- src` finds TWO hits, both the row-freezing
   writers, so `.guards` has **zero runtime readers** and none is chartered through wave 5; and this
   leaf's `rulesFor(type)` is already the single home of the applies-to fact, so a populated array
   would be a second enumeration of it. THE RE-SLOT IS §7's TEST row on
   `tests/lint/opGuardCoverage.walker.test.js`: the instrument asserts the DERIVATION instead of a
   copy of it, **+0 B**, no `src/` path, no landed leaf recomposed. The closure price of the refused
   shape is kept here because EM-C4b inherits the number: **+2 modules / 25,445 raw B** on
   `operations.js`'s import closure with the writers injected, against **+77 modules / 1,395,659 raw
   B** with them imported (both RE-MEASURED at `bb0497eb4`; version 2 read 1,395,639 against
   `rulingStructure.js`'s 1,185,188, and the hygiene commit's one comment line moved each by +20 B),
   and the imported shape is refused outright because `src/store/editSlice.js :: validateOp` is
   imported by the STORE, which puts `operations.js` in the store's closure.
3. **THE GUARD JUDGES THE STANDING ROSTER (235c)** while `checkDraftEdit` passes the raw one, and
   this packet STATES the divergence rather than hiding it: a decree must not rest on a ruin.
   REVERSAL: one accessor call.
4. **THE ENGINE MINTS THE GUARD's `id` FROM THE RULE's STABLE `id` (235d)**, at the §5 clause's
   item 6 spelling, so `overrode: [guardId]` names something a reader can resolve.
5. **THE FIVE MESSAGE TEMPLATES STAY IN THIS LEAF (235e)**, at ARCH's `message: string` shape,
   because a headless wave needs a testable string. EM-D3's registry page may re-home them.

**AND THE FORK IS CLOSED (236).** The §5 clause is ONE TEXT, landed by the chair, copied here
byte for byte with its measured digest; deltas 1 and 2 came from this packet, delta 3 went to
the engine on its executed closure measurement, and the staging order is EM-C2 then EM-C3.
Version 1's contrary sentence is RETIRED. The one OPEN item version 3 still carried — *"EM-C2b's slot has no train row yet"* — is
**CLOSED by judgment 251**: there is no EM-C2b, its measurement is recorded in ruling 2 above, and
its payload is this member's fifth §7 row. Nothing of this member is deferred.
