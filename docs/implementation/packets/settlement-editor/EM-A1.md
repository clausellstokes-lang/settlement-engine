# EM / EM-A1 — FIELD DECLARATIONS for the five card types (train `em-w1`, wave 1)

**Preamble:** docs/implementation/preambles/EM-PREAMBLE.md (SHA-256: TO BE STAMPED BY THE CHAIR)

> **Interim compile rules:** `COMPILE-RULES.interim.md` (the chair, 2026-09-20) — the fifth amendment's eleven rules, obeyed before they land. Where this packet and `EM-PREAMBLE.md` (SHA-256 `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`, MEASURED at this packet's read tip with `shasum -a 256`) disagree, the interim rules govern and this row is the record of it.

- **Status:** `READY`
  ⚠ The status value stands ALONE on its line because `parsePacketHeader`
  (`scripts/implementation-packets.mjs`, cited BY SYMBOL — a line number moves with the next
  edit) anchors the status row at END-OF-LINE and takes `status` only when exactly one row
  matches (J-TEWF1B-1).
  ⭐ **§11 BLOCK-0 IS CLOSED** (V-2, V-10, V-11) and **BLOCK-6 IS CLOSED** by the chair's
  ruling 3 (W-6: EM-A3's `free` denominator is exactly TWO). **CHAIR-1 and CHAIR-2 are
  DECIDED** — the world-fact card and `FieldKind`'s `'share'` are built here.
- **Packet version:** 5.2
  - ⭐ **What version 5.2 changed, and why (ESTATE-REPAIR-6, 2026-09-21, read tip
    `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`; judgments 33 and 54): the four sentences version
    5.1 REPORTED and did not touch are corrected to the rulings** — §6.2's `card`-union pool-id
    list (`worldFact.goods` and `worldFact.services` left EM-A2b for EM-P3c); §11 BLOCK-4b's A2a
    and A2b id tables (**eleven** and **six**, re-measured from the siblings' own tables) and its
    `monsterThreat` tie-break (ANSWERED — the chair assigned the id to EM-A2a, whose version 5
    carries it; the three-and-three measurement is KEPT as history with its as-of mark, interim
    rule 9); and §7's mutation-coverage row-disjointness, which named the superseded `EM-B2a` and
    now names the MEASURED holder set. ⭐ **Two §5 cells that cite §11 BLOCK-4b BY NAME (the
    `holder` row and the `monsterThreat` row) carry the same correction in their ⚠ annotation
    column ONLY** — no declaration field moved. ⛔ **No declaration, typedef, arm, acceptance case,
    change row, check, budget, figure or non-goal moved, and the capsule is UNTOUCHED.**
  - ⭐ **What version 5.1 changed, and why (ESTATE-REPAIR-5, 2026-09-21, read tip
    `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`; judgment 54): ONE sentence in §11 BLOCK-7's
    recommendation.** It said EM-A2b's `worldFact.goods` and `worldFact.services` pools *"stand
    ready either way"*; the chair's ESTATE-REPAIR-4 ruling 1 moved both pool rows out of EM-A2b
    to **EM-P3c**'s sitting, so neither pool is minted in wave 1 and the sentence now says so.
    ⛔ **Nothing else in this packet moved** — no declaration, typedef, arm, acceptance case,
    figure, change row, check, block or non-goal. ⚠ Four further sentences the same ruling dates
    (§6.2's `card`-union pool list, §11 BLOCK-4b's A2a/A2b id tables and its `monsterThreat`
    tie-break, and §7's mutation-coverage row-disjointness naming the superseded `EM-B2a`) are
    REPORTED to the chair by ESTATE-REPAIR-5 and deliberately NOT touched here, because the brief
    holds this packet to one sentence.
  - **What version 5 changed, and why.** Version 4 was ACCEPTED as this version's base and was
    never placed, because four of its eight questions changed the typedef it CREATEs. The chair
    ruled all four (`findings/COMPILE-EM-A1-v4-2026-09-20/CHAIR-RULING.md`, judgment 29) and
    version 5 builds them: (a) **the world-fact card** — `provenance` widens and a fifth card is
    declared, its rows joined to `WORLD_FACT_SOURCES` and to their **Tier-1** `(step, key)` rows,
    ARM I-4 gaining a third branch with two plants of its own; (b) **`'share'`** — `FieldKind`
    gains a fourth member and `faction.power` is declared, so the root set is TEN again;
    (c) **the annotation rows** — `provenance` gains `'annotation'` and ARCH §9's measured set
    (`institution.note`, `npc.note`, and no others) is declared `kind: 'free'`, which CLOSES
    BLOCK-6 with a denominator of two; (d) **`EntityRef` AND `Op`** join `types.js`, because
    EM-B1a's change manifest was measured and it neither CREATEs nor MODIFIEs that file (W-7) —
    version 4's report said it did, and that claim is RETRACTED here. Three further changes are
    this lane's own measurements: (e) **`goods` and `services` are held out of the world-fact
    card** with the smallest measured contradiction (§11 BLOCK-7, W-2/W-3) — neither is a fact
    the settlement record holds; (f) **the frozen wave-1 id list gets exactly ONE home** — ARM
    I-1 owns the membership check and ARM I-4 takes presence only, because running the same
    predicate in two arms made version 4's third plant red twice (W-5); (g) `types.js`'s
    self-imposed line cap is re-priced from 60 to 90 for five typedefs instead of two, inside
    the standard's 250. Every figure is re-measured at `bdbf7c89c`; no contract of version 4
    survives untouched.
- **Verified base:** `em-t9-a1-2026-09-21` at `7f0621fdb72f769a285502ff5481247c1f07b04b`
  ⚠ **Revalidation sentence for the chair:** *"EM-A1 version 5 is re-pinned to `<TIP>`. Version
  4's whole §5 was re-executed at `bdbf7c89c` and version 5 re-executed it again with the four
  rulings in: all FOURTEEN `requiredSymbols` rows resolve verbatim, `count=1` each (W-8), all
  four CREATE targets are absent (W-9), and the whole seventeen-row table passes all five arms
  with every one of ten plants red on its own arm only (W-4, W-5)."*
- **Last revalidated:** `7f0621fdb72f769a285502ff5481247c1f07b04b` (this packet's whole §5 was re-executed at `bdbf7c89c`;
  every figure kept from an earlier base carries an AS-OF mark naming that base)
- **Depends on:** `EM-P2` — **LANDED `ec0a30da2`**; the root arm reads its `GENERATION_TIER2` / `tier2For` **and its `GENERATION_TIER1` / `tier1For`** (the world-fact branch), and the writer arm imports its `declaredSymbols` resolver · `EM-P3` — LANDED; the world-fact card's `WORLD_FACT_SOURCES` is its mint · **`EM-A2a`** (not EM-A2: EM-A2 was SPLIT) — `institution.class`, `institution.state`, `npc.status`, `faction.category`, `worldFact.terrain`, `worldFact.culture`, `worldFact.stressors` · **`EM-A2b`** — `npc.role`, `worldFact.resources`. ⛔ **`power.holder` and `worldFact.monsterThreat` are minted by NEITHER** (§11 BLOCK-4b). None of A2a/A2b is imported here; both are ORDER dependencies. ⚠ **EM-B1a depends on THIS packet, not the reverse** — it imports `Op` and `EntityRef` from `./types.js` (W-7).
- **Collision group:** `EM-A3` — EM-A3 MODIFIES `src/domain/edit/fieldDeclarations.js` (its `readersProof` values) and REGISTERs the same `scripts/mutation-coverage-manifest.json`. **EM-A1 lands first, and the two cannot be PLACED together at all** — measured, three independent refusals (V-16). ⭐ **EXECUTED CURE:** with EM-A1 LANDED (terminal ⇒ reserves nothing) and its four CREATE targets on disk, EM-A3 validates unchanged — `valid: 197 packets (0 READY)`, exit 0, **with no TOOL-27**. The declared placement order IS the cure.
- **Commit authority:** edits only; the chair commits
- **Baseline posture:** measured, and re-measured at this base with the four rulings in. Executed here: the whole seventeen-row table through ARM I-1, I-2, I-3, I-4 and the §6.2 shape law, 17 of 17 green (W-4); ten planted rows, each red on its OWN arm only, live rows green in the same run (W-5); the seven world facts' record paths and Tier-1 holders over 36 generated settlements (W-1); `goods` and `services` refuted as held facts (W-2, W-3); EM-A3's `free` denominator counted (W-6); EM-B1a's change manifest read for `types.js` (W-7); fourteen `requiredSymbols` rows verbatim (W-8); four CREATE targets absent (W-9); the four live registers as deltas (W-10); the sealed-checks grep re-run over version 5's symbol set (W-11). Version 4's V-0…V-16 are carried and are NOT rewritten.

---

## 1. Reconciled authority

1. ⭐⭐ **ODQ §934.45 — design §14's FINAL shape, AS AMENDED BY §21.5.3.** §14: *"**Root means CHOSEN, not computed.** … The estate registers every seeded chooser in its decision-fork classification registry, so the root set is that registry's rows — a measurement, never a judgment."* §21.5.3 amends it: *"**§14 is amended: an editable root is a fact the record HOLDS, not a fact a draw chose.** … How generation first produced a held fact — `drawn` or `computed` — is RECORDED per row as its `origin`, and is never the test of editability."* ⭐ This is why five of the ten root rows are declarable at all: the power structure consumes zero entropy at generation (`origin: 'computed'` on five Tier-2 rows), and the DM still seats a mayor.
2. ⭐⭐ **THE CHAIR'S RULING ON VERSION 4** (`findings/COMPILE-EM-A1-v4-2026-09-20/CHAIR-RULING.md`, 2026-09-20, ODQ §934.47 addendum 121, judgment 29). Rulings 1, 2, 3 and 7 are this version's charter and are built; rulings 4, 5, 6 and 8 are carried from version 4 unchanged.
3. ⭐ **DESIGN §14's FIVE KINDS OF FACT, quoted, because `provenance` is now their vocabulary.** *"1. **World facts** … the editor edits them on a fifth card, whose pools are the wizard's own option sets … 2. **Root** — a registered chooser's output … 3. **Derived** … Never editable, on any card … 4. **Annotation** — the free notes; the DM's layer only; the flavor census proves nothing reads them … 5. **Minted** — an entity the DM adds."* ⇒ `provenance` is exactly THREE members: derived facts are never declared, and minted is a property of an ENTITY, not of a field.
4. ⭐ **DESIGN §14's ONE EDGE, quoted:** *"A faction's power share is a chosen share the guard renormalizes; it stays a ROOT under the totality guard. Vetoable."* ⇒ `FieldKind` gains `'share'`; a packet that cannot declare the share is short of the design, not the design short of a kind.
5. **EM-P2, LANDED `ec0a30da2` — `src/domain/generation/generationForkRegistry.js`.** THE registry design §14 names is **`GENERATION_TIER2`**, not `HABIT_FORK_REGISTRY`; §14 names a ROLE, never a file. `HABIT_FORK_REGISTRY` is the SIMULATION's register and appears here only as this sentence of history. ⭐ **Version 5 also reads TIER 1**, which the same module's header calls *"the `(step, key)` CENSUS"* — the world facts live there, not in Tier 2.
6. **CHAIR AMENDMENT, 2026-09-19, ODQ §934.44.** ⛔ Its ROOT TEST — *"whose writer reads no other world fact"* — is SUPERSEDED by §934.45 and appears nowhere in this packet except as A6's regression control, where it is the thing proved wrong.
7. `docs/OWNER_DECISION_QUEUE.md` §934.42, §934.43, §934.47 addendum 121.
8. THE PROMISE · the deity doctrine · the product scope — via `EM-PREAMBLE.md` §P1.
9. `docs/DESIGN_EDIT_MODE_AND_DECREES.md` **§12, §14 FINAL, §15, §20.2 and §21.5 GOVERN**; `docs/ARCH_EDIT_MODE_AND_DECREES.md` §1, §2, §8, §9.
10. `docs/implementation/PACKET_STANDARD.md`, `EM-PREAMBLE.md` §P1–§P9, and `COMPILE-RULES.interim.md` where they disagree (header row).
11. Live code and executed evidence at this base — `EM-A1.evidence.md` §V-0…§V-16 (version 4) and §W-1…§W-12 (version 5).

Resolved contradictions:

- Design §2.1's per-field `effect: 'correction' | 'event'` → **WITHDRAWN** by §2.6's canon rule; a field declares only its kind. §20.2 then makes the KIND decide for the two free kinds on canon.
- Design §2.1's two kinds (`pool | free`) → **SUPERSEDED** by §12.3 (three kinds) and by the chair's ruling 2 (four: `'share'` joins them).
- ARCH §2's *"`provenance` is always 'root' by construction … when its `writer` … reads nothing but the seed and the dials"* → ⛔ **SUPERSEDED IN BOTH HALVES.** The predicate went with §934.45; the VALUE is now one of three by the chair's rulings 1 and 3. ARCH §2 is the chair's to amend; this packet quotes it and states the divergence rather than editing it.
- ARCH §2's `Op` typedef carries `requires?`/`enables?`/`relatedTo?`/`conflictsWith?`/`duration?` as OPTIONAL and has no `consequence` → **SUPERSEDED by the compiled EM-B1a §6**, which the chair's ruling 7 names as the shape's authority: its transition table says `makeOp` returns *"an `Op` carrying the type's declared `stage`, `consequence` and four relations"* and its §6 rule reads *"EVERY FIELD IS REQUIRED ON EVERY ROW. None is optional; none may be omitted 'when empty.'"* §6.2 authors the required form. ⚠ Recorded as judgment 4.
- ARCH §9's system-state cards (security, food security, order) → **STRUCK** by §14.
- ARCH §9's `institution.class` / `faction.archetype` → **MAPPED** to the record's `category` in both cases (V-3). ⚠ EM-A2a's pool id is spelled `institution.class` while this packet's field is `category`; the id is A2a's to name and this packet uses A2a's spelling verbatim.
- ARCH §9's `institution.standing`, `npc.disposition`, `faction.stance` → **DROPPED**: no counterpart on the record at any tier over 36 settlements (V-3).
- ⭐ **ARCH §9's ANNOTATION SET IS MEASURED, NOT ASSUMED** (the chair's ruling 3: *"unless ARCH §9 names a different set … the packet follows it and says so"*). ARCH §9 reads, verbatim: *"institution (name free-cascade; class pool; standing pool; **note free**), npc (name free-cascade with the generator's roll; role pool …; disposition pool; **note free**), faction (name free-cascade; archetype pool; power share as a root under the totality guard; stance pool), power seat (holder pool over factions/npcs)"*. ⇒ **`note free` appears TWICE and only twice** (W-6, `grep -n -o 'note free'` → two hits, both on ARCH line 104). **ARCH §9 names a different set than "one per card", and the packet follows ARCH §9: TWO annotation rows, on `institution` and `npc`.** Faction, power seat and the world-fact card get none.
- ⛔ VERSION 3's §1 nine-drop bullet and §1b remain DELETED IN FULL.

## 1c. THE CHAIR'S RULINGS CARRIED — what version 5 is compiled against

### 1c.1 · The arms read the LANDED registry, in BOTH tiers (chair rulings 1 and the version-4 ruling 1)

`src/domain/generation/generationForkRegistry.js` exports, verbatim at this base:

```js
export const GENERATION_TIER1;                            // 75 frozen (step, key) census rows
export function tier1For(step, key);                      // → GenerationTier1Row | null (never undefined)
export const GENERATION_TIER2;                            // 10 frozen editable-root field rows
export function tier2For(cardShape, outputKey);           // → GenerationTier2Row[]  (one-to-many, [] on a miss)
```

**Two joins, two tiers, and they are not interchangeable.** A ROOT row joins Tier 2 on
`(cardShape, outputKey)`. A WORLD-FACT row joins **Tier 1** on `(step, key)` — the module's own
header says Tier 1 is *"the `(step, key)` CENSUS"* and Tier 2 is *"the ten EDITABLE-ROOT FIELD
rows EM-A1 joins on"*, so a world fact has no Tier-2 row and never will. Executed: TIER1 75 rows,
TIER2 10 rows, cardShapes exactly `["institution","npc","faction","powerSeat"]` (W-4).

⭐ **THE ARMS ARE NOT DARK.** Over version 5's seventeen rows, ARM I-1 is satisfied 15 of 15 in
its population, ARM I-3 5 of 5 in its population, ARM I-4 15 of 15 in its population, ARM I-2
reports zero offenders over eleven printed pool ids, and the shape law reports zero offenders —
**17 of 17 rows green on every arm** (W-4). The *"REDS on an empty registry"* clauses stay, as the
anti-vacuity fences they always were.

### 1c.2 · ⭐ THE DECLARED FIELDS — SEVENTEEN rows across FIVE cards

Measured over **36 settlements** (3 seeds × 6 tiers × 2 cultures) by executed pipeline generation
at this base: 1,301 institutions, 336 NPCs, 204 power factions, 36 power structures, 36 configs
(V-3, W-1). Only fields that EXIST on the record, or that a NAMED wave-1 packet creates, or that
are declared ABSENT-BY-RULE (the annotations), are declared.

**institution** — `settlement.institutions[]` · cardShape `institution`

| field | provenance | `outputKey` | occupancy | `kind` | pool / cascade | writer or createdBy |
|---|---|---|---:|---|---|---|
| `name` | `root` | `institutions[].name` | **100 %** | `free-cascade` | ⚠ the cascade is `EM-R6`'s `src/domain/institutionRename.js`, NOT LANDED (V-9) | `…/assembleInstitutions.js#assembleInstitutions` |
| `category` | `root` | `institutions[].category` | **100 %** | `pool` | `institution.class` (EM-A2a) | the same |
| `state` | `root` | `institutions[].state` | **0 %** | `pool` | `institution.state` (EM-A2a) | **`createdBy: 'EM-B1a'`** |
| `note` | `annotation` | ⛔ **NONE — absent by rule** | `n/a` | `free` | — | — (`readersProof`, EM-A3 mints the value) |

**npc** — `settlement.npcs[]` · cardShape `npc`

| field | provenance | `outputKey` | occupancy | `kind` | pool / cascade | writer |
|---|---|---|---:|---|---|---|
| `name` | `root` | `npcs[].name` | **100 %** | `free-cascade` | `NPC_RENAME_SURFACES[0]`, kind `key` (V-9) | `…/generatePopulation.js#generatePopulation` |
| `role` | `root` | `npcs[].role` | **100 %** | `pool` | `npc.role` — **EM-A2b's row** | the same |
| `status` | `root` | `npcs[].status` | **24 of 336 = 7.1 %** | `pool` | `npc.status` (EM-A2a) | the same |
| `note` | `annotation` | ⛔ **NONE — absent by rule** | `n/a` | `free` | — | — (`readersProof`) |

⚠ **`status` rides with the structural-seat family and with nothing else.** All six companions —
`importance`, `linkedFactionIds`, `linkedInstitutionIds`, `generatedAs`, `legitimacyContribution`,
`stabilityContribution` — are present on exactly the same 24 NPCs (V-3), and the only observed
value is `'active'`. **The card must show it EMPTY** (design §2.1). ⭐⭐ **Two denominators, both
true, neither interchangeable:** EM-P2's Tier-2 row reads **18 of 42** over its census corpus, and
this packet's 7.1 % is per-NPC over 336 NPCs.

**faction** — `settlement.powerStructure.factions[]` · cardShape `faction`

| field | provenance | `outputKey` | occupancy | `kind` | pool / totality | writer |
|---|---|---|---:|---|---|---|
| `faction` | `root` | `powerStructure.factions[].faction` | **100 %** | `free-cascade` | a declared `FACTION_RENAME_SURFACES` key (V-9) | `…/generatePower.js#generatePower` |
| `category` | `root` | `powerStructure.factions[].category` | **100 %** | `pool` | `faction.category` (EM-A2a, 13 archetypes) | the same |
| ⭐ `power` | `root` | `powerStructure.factions[].power` | **204 / 204 = 100 %** | **`share`** | ⛔ NO pool. Its totality is `renormalizeFactionPower` (§6.2) | the same |

⚠ `category` is inferred from the faction's display NAME (`inferFactionCategory`), so editing the
name moves it — a coupling EM-B2's pinned re-derivation carries.

**powerSeat** — `settlement.powerStructure` · cardShape **`powerSeat`** (ONE spelling, the register's)

| field | provenance | `outputKey` | occupancy | `kind` | pool | writer |
|---|---|---|---:|---|---|---|
| `holder` | `root` | `powerStructure.governingName` | **36 / 36** | `pool` | ⭐ **`power.holder` — MINTED BY EM-A2b VERSION 4 (§11 BLOCK-4b)** | `…/generatePower.js#generatePower` |

⚠ **DECLARED WITH A NAMED HAZARD.** `governingName` has **TWO writers** (`rulingStructure.js:787`
and `power/economyReconciliation.js:277`) against `EM-PREAMBLE.md` §P4's one-writer law; the seat is
a projection of `factions[].isGoverning` (36 of 204 = 17.6 %), so a `set-power-holder` op must
**move the flag and let the name follow, never write the name**. ⭐ `powerStructure.governingName`
is ITSELF a declared `FACTION_RENAME_SURFACES` key (V-9), which is what makes the pool the faction
roster.

**worldFact** — `settlement.config` · card **`worldFact`** (the FIFTH card; the register is silent on its spelling, so the packet takes EM-A2a/EM-A2b's own pool-id prefix `worldFact.*` verbatim)

| field | provenance | `outputKey` | occupancy | `kind` | pool | Tier-1 `(step, key)` holder |
|---|---|---|---:|---|---|---|
| `terrain` | `world-fact` | `config.terrainType` | **36 / 36** | `pool` | `worldFact.terrain` (EM-A2a) | `(resolveConfig, terrainType)` → `record.config.terrainType` — **EXACT** |
| `culture` | `world-fact` | `config.culture` | **36 / 36** | `pool` | `worldFact.culture` (EM-A2a) | `(resolveConfig, culture)` → `record.config.culture` — **EXACT** |
| `monsterThreat` | `world-fact` | `config.monsterThreat` | **36 / 36** | `pool` | ⭐ `worldFact.monsterThreat` — **MINTED BY EM-A2a VERSION 5** (§11 BLOCK-4b) | `(resolveConfig, effectiveConfig)` → `record.config` — **PREFIX** |
| `stressors` | `world-fact` | `config.stressTypes` | **36 / 36** | `pool` | `worldFact.stressors` (EM-A2a) | `(resolveStress, stressTypes)` → `record.config.stressTypes` — **EXACT** |
| `resources` | `world-fact` | `config.nearbyResources` | **36 / 36** | `pool` | `worldFact.resources` (EM-A2b) | `(resolveResources, nearbyResources)` → `record.config.nearbyResources` — **EXACT** |

⭐ **THE PREFIX FORM IS THE REGISTER'S OWN, NOT A LOOSENING.** `GENERATION_BLIND_HALVES`'s
`field-rootness` row says, verbatim: *"Tier 1 classes a `(step, key)` PAIR, never a field inside the
key."* `config.monsterThreat` is placed into `effectiveConfig` at `resolveConfig.js:201`
(`monsterThreat: threat`), so the pair that HOLDS it is `(resolveConfig, effectiveConfig)` and the
sibling pair `(resolveConfig, threat)` — the chooser — reads `recordPath: null` because the ctx key
`threat` never lands under its own name (W-1). ARM I-4 admits exactly two forms, equality and
`recordPath + '.'` prefix, and the packet says which row takes which; that is a stated predicate,
not discretion.

⛔⛔ **`goods` AND `services` ARE NOT DECLARED. §11 BLOCK-7 is the smallest measured
contradiction.** Both are keys of `WORLD_FACT_SOURCES` and the chair's ruling 1 names all seven, so
this is a refutation of two rows of that ruling and nothing else: **neither is a fact the settlement
record holds** (W-2, W-3). See BLOCK-7 for the four measurements and the recommendation.

**Total declared: 17 fields across 5 cards — 10 `root`, 5 `world-fact`, 2 `annotation`; by kind,
11 `pool`, 3 `free-cascade`, 2 `free`, 1 `share`** (W-4, counted by the probe, not by hand).

### 1c.3 · The five arms, their homes, and the case that proves each

```text
ARM I-1  EXISTENCE.  A ROOT row either (a) appears on its card's record in a corpus this test
         GENERATES — at least one tier, the corpus asserted non-empty — or (b) names a
         `createdBy` that is a member of WAVE1_PACKET_IDS, a frozen list spelled ONCE, HERE.
         A WORLD-FACT row's `outputKey` is a live path on the generated record.
         An ANNOTATION row is OUT of this arm's population by construction (absent by rule).
                                                      HOME: tests/domain/editDeclarations.test.js
ARM I-2  POOLS.      Every `kind: 'pool'` row carries a non-empty, unique pool id; every other
         kind carries none; the id set is PRINTED. ⛔ Membership of `POOLS` is EM-A2a's arm.
                                                      HOME: tests/lint/editDeclarations.walker.test.js
ARM I-3  WRITERS.    Every row that CARRIES a `writer` parses as `path#symbol`, the path exists,
         and the symbol is declared in that file EXACTLY ONCE by the LANDED `declaredSymbols`
         resolver. A row with no `writer` is out of the population by construction.
                                                      HOME: tests/lint/editDeclarations.walker.test.js
ARM I-4  ROOTS.      THREE BRANCHES. (a) a ROOT row joins a `GenerationTier2Row` on
         `(card, outputKey)`, or carries a `createdBy` (PRESENCE only — membership is I-1's);
         (b) an ANNOTATION row is out of the population; (c) ⭐ a WORLD-FACT row's `field` is a
         key of `WORLD_FACT_SOURCES` AND `tier1For(tier1.step, tier1.key)` resolves AND that
         row's `recordPath` equals or prefixes `'record.' + outputKey`. REDS on an empty
         registry rather than passing vacuously.      HOME: tests/lint/editDeclarations.walker.test.js
ARM P    PENCIL.     The pencil marker EM-D1 will mint is found EXACTLY ZERO times under
         `src/components/**`, over a corpus asserted non-empty in the same test.
                                                      HOME: tests/lint/editDeclarations.walker.test.js
```

**The mapping is TOTAL, both directions** — no arm without a case, no case without a home:

| arm | case | arm | case |
|---|---|---|---|
| ARM I-1 | **A3** | ARM I-4 | **A5b** |
| ARM I-2 | **A5** | ARM P | **A5** |
| ARM I-3 | **A6** | (the §6.2 shape law) | **A4** |
| (the reader contract) | **A1, A2, A7** | | |

⭐ **ONE PREDICATE, ONE ARM (version 5's correction of version 4).** Version 4 ran the wave-1
membership check in BOTH ARM I-1 and ARM I-4, in two different test files, each with its own
re-typed frozen list. Executed, that made version 4's third plant red on two arms at once (W-5,
first run). Version 5 gives the frozen list exactly one home — ARM I-1, the EXISTENCE arm, which is
where "this field does not exist yet and a named packet will make it" belongs — and ARM I-4 takes
`createdBy` presence only. The plant moves with the predicate, from A5b to A3, and **A3 gains the
guard-the-guard it never had.**

⭐ **ARM I-1 lives in the DOMAIN test and not in the walker**, because it needs an EXECUTED corpus
and a `tests/lint` walker that generates settlements is a three-minute lint.

## 2. Outcome

**Observable result:** `src/domain/edit/fieldDeclarations.js` declares every editable field of the
five card types with `kind`, `group`, `provenance` (`root` | `world-fact` | `annotation`), the
field's `outputKey` where it has one, and either its `writer` by symbol, its `createdBy` packet id,
or its Tier-1 `(step, key)` holder; `declarationsFor(cardType)` returns them;
`tests/lint/editDeclarations.walker.test.js` refuses a pencil without a declaration, a `pool` field
with no pool id, a `writer` that does not resolve to exactly one declaration, **a root declaration
that joins no row of `GENERATION_TIER2` and names no creating wave-1 packet, and a world-fact
declaration that is not a key of `WORLD_FACT_SOURCES` or whose Tier-1 pair does not hold its
record path.**

**Definition of done:** the two pure leaves exist, the declaration battery is green, and the walker
is green with its root arm, its writer arm and its shape law each convicting their planted rows.

In scope:

1. **One primary behaviour:** the declaration table and its reader.
2. **One required integration:** NONE — wave 1 is HEADLESS.
3. **One prevention guard:** `tests/lint/editDeclarations.walker.test.js`, including the root arm's
   three branches (§6.3).

Explicit non-goals:

- the pool catalogue (`EM-A2a`/`EM-A2b`) and `POOLS` membership (§11 BLOCK-4), the flavor census's
  `readersProof` VALUES (`EM-A3`), ops, the layer, the registry;
- ⛔ **the `goods` and `services` world facts** (§11 BLOCK-7) — held out with the measured
  contradiction that holds them out, not deferred;
- the DERIVED cards' provenance rendering (§14's *"follows from X — change X"*) — a surface, and
  wave 1 is headless;
- **the numeric control the `'share'` kind implies.** EM-D0 owes it (the chair's ruling 2); design
  §3's modal vocabulary knows only pool controls and free text with a length limit. Recorded in §12
  and in the chair's charter row, not built here;
- any component, store surface, feature flag, persisted key, golden, tuning, migration or UI;
- ⛔ **`history.age` and every other `history` field.** Design §14 ruling 3 makes history the
  chronicle's and immutable (THE PROMISE); `history` is not one of §14's five cards; and
  `history.age` is an owner-gated, generation-time constant pinned by
  `docs/ENGINE_DEFECT_DISPOSITIONS.md` §5 and asserted by `tests/domain/worldPulseTickClock.test.js`.
  **This packet makes no `history` field editable and touches that gate nowhere.**
- Record adjacent discoveries in the receipt; do not investigate or repair them.

## 3. Hard scope budget

| Limit | Packet budget | Measured / estimated |
|---|---:|---|
| Behavior families | `1` | one declaration table |
| New persisted record families | `0` | nothing persists |
| Named state writers | `0` | — |
| Feature flags | `0` | — |
| User-facing surfaces | `0` | headless |
| Direct consumers | `0` | zero importers by construction |
| New logic-bearing production leaves | `2` | `fieldDeclarations.js`, `types.js` |
| Existing logic-bearing production files modified | `0` | — |
| Additional registration-only files | `1` | `scripts/mutation-coverage-manifest.json` |
| Handwritten files total | `5` | 4 CREATE + 1 REGISTER |
| New/changed effective production lines | `≤ 260` | **≈ 169 estimated** (17 rows ≈ 90 + two readers ≈ 25 + the frozen empty ≈ 4 + five typedefs ≈ 50) |
| Effective lines per new leaf | `≤ 210` (`fieldDeclarations.js`; **≈ 119 est.**) · `≤ 90` (`types.js`, JSDoc only; **≈ 50 est.**) | — |
| Delta in a shared/hot file | `n/a` — **NO hot file is named.** | none of the five standing rows appears in §7 |
| Acceptance cases | `8` | exactly 8 (the validator's maximum; `:797`) |

Overrides approved before dispatch: `NONE`. **The budget FITS with all four rulings in**, and no
split is owed. ⚠ `types.js`'s per-leaf cap is re-priced from version 4's self-imposed `≤ 60` to
`≤ 90`: that figure was sized for TWO typedefs and version 5 authors FIVE (`FieldKind`,
`FieldProvenance`, `FieldDeclaration`, `EntityRef`, `Op`). The estate's own per-leaf cap is 250, so
this is a re-pricing inside the standard, not a raise of it — recorded as judgment 6.

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- EM-A1
```

Expected: all four CREATE targets ABSENT (W-9 — re-measured at this base; ⚠ `src/domain/edit/`
itself EXISTS and holds EM-R0a's `recordRegister.js`, so the directory's absence is a premise
nowhere in this packet), the one REGISTER target clean, every `requiredSymbols` row resolving
verbatim (W-8: 14 of 14, `count=1` each). Any mismatch makes this packet STALE.

## 5. Verified tree contract

### 5.1 ⭐ THE WHOLE VERIFIED CONTRACT, RE-EXECUTED AT THIS BASE

| Role | File | Symbol | Verified fact (executed at this base) | Required use |
|---|---|---|---|---|
| **Root authority** | `src/domain/generation/generationForkRegistry.js` | `GENERATION_TIER2` | **10 frozen rows** over exactly four `cardShape` strings `institution \| npc \| faction \| powerSeat`; `outputKey` present on every row; `origin` `drawn` ×5 / `computed` ×5 (V-2, W-4) | ARM I-4 branch (a); the four root card spellings |
| **Root join** | the same | `tier2For(cardShape, outputKey)` | returns an ARRAY; `[]` on a miss, never `null`; 9 of the 10 root rows join 1 row each, `institution.state` excepted by `createdBy` (V-2, W-4) | ARM I-4 branch (a)'s predicate |
| ⭐ **World-fact authority** | the same | `GENERATION_TIER1` | **75 frozen `(step, key)` census rows**; `recordPath` is `null` exactly when `onRecordClass` is `'absent'` (the module's own contract) (W-1, W-4) | ARM I-4 branch (c)'s denominator |
| ⭐ **World-fact join** | the same | `tier1For(step, key)` | returns the row or **`null` on a miss, never `undefined`**; all five declared pairs resolve (W-1) | ARM I-4 branch (c)'s predicate |
| ⭐ **The per-field blind half, declared** | the same | `GENERATION_BLIND_HALVES` | its `field-rootness` row: *"Tier 1 classes a `(step, key)` PAIR, never a field inside the key"* | why ARM I-4 branch (c) admits a PREFIX as well as an equality |
| **Symbol resolver** | `tests/helpers/generationForkCensus.js` | `declaredSymbols(raw)` | `Map<symbol, count>`; its `DECL_FORMS` carry **`registerStep('<name>'`**; resolves all three Tier-2 step symbols at exactly 1 (V-2, W-4) | ARM I-3's predicate |
| ⛔ **The superseded form set** | *(this packet, version 3 §6.3)* | — | *"a function declaration or a const arrow"* finds **0** declarations for `assembleInstitutions`, `generatePopulation` and `generatePower` (V-2, W-4) | **DELETED.** A6's regression control, never a predicate |
| Writer (institution) | `src/generators/steps/assembleInstitutions.js` | `registerStep('assembleInstitutions'` | present once (W-8) | the two institution root rows' `writer` |
| Writer (npc) | `src/generators/steps/generatePopulation.js` | `registerStep('generatePopulation'` | present once (W-8) | the three npc root rows' `writer` |
| Writer (faction / seat) | `src/generators/steps/generatePower.js` | `registerStep('generatePower'` | present once (W-8) | the three faction rows' and the seat row's `writer` |
| ⭐ **Share totality** | `src/generators/power/rulingStructure.js` | `renormalizeFactionPower` | `export const renormalizeFactionPower = (factions) => {…}` — **mutates in place and returns `factions`**; its own header: *"rescale every faction's `power` to integer percentage points summing to exactly 100, using largest-remainder rounding so the rounded points always total 100 (never 99 or 101) … preserves rank order … No-op for an empty/zero-power roster."* Guards `!factions.length` and `total <= 0` (W-12) | `faction.power`'s totality citation (§6.2). ⭐ Already EM-B1a's `requiredSymbols` row, so the guard that enforces it is a compiled sibling's contract, not this packet's invention |
| Join-key authority | `src/domain/factionRename.js` | `NPC_RENAME_SURFACES` | **5** surfaces, `[0]` is `npcs[].name`, kind `key` (V-9) | `npc.name` is `free-cascade`, never `free` |
| Join-key authority | `src/domain/factionRename.js` | `FACTION_RENAME_SURFACES` | **32** surfaces, including `powerStructure.factions[].faction` (key) and `powerStructure.governingName` (key) (V-9) | `faction.faction` is `free-cascade`; the seat's value is a faction name |
| ⛔ Absent cascade | *(whole tree)* | — | `git grep -l -i 'institutionRename\|renameInstitution' -- src tests` → **no output, exit 1** (V-9). The cascade is EM-R6's CREATE, a WAITING DRAFT | `institution.name`'s named exception + STOP-7 |
| Corpus | `src/generators/generateSettlementPipeline.js` | `generateSettlementPipeline` | signature `(config, options, { seed })` — a `seed` inside the first bag THROWS by design; 36 settlements generated (V-3, W-1) | ARM I-1's denominator |
| Status vocabulary | `src/domain/entities/npcs.js` | `NpcStatus` | seven members with `jailed` (EM-B1d LANDED) (V-10) | `npc.status`'s pool, owned by EM-A2a |
| State vocabulary | `src/domain/entities/status.js` | `EntityStatus` | five, NOT widened; `ruined` is the pulse's literal via `calamityKernel.js:242 ruinInstitution` (V-10) | `institution.state`'s pool, owned by EM-A2a |
| ⭐ **World-fact vocabulary** | `src/domain/worldFactOptions.js` | `WORLD_FACT_SOURCES` | **ONE home, ONE declaration**; SEVEN keys `terrain, culture, monsterThreat, stressors, resources, goods, services`, each valued with the `module#SYMBOL` that OWNS that vocabulary — *"CITATIONS, NOT COPIES"*; the domain file RE-EXPORTS `src/data/worldFactOptions.js` (V-11, W-1) | ARM I-4 branch (c)'s first half |
| ⛔ **`goods` is not a held fact** | `src/generators/steps/resolveConfig.js` | `goodsToggles` | the step EXTRACTS `config._goodsToggles` into a ctx key (`:89`) and never writes it into `effectiveConfig` (`:189-201`); its Tier-1 row reads `recordPath: null`, `onRecordClass: 'absent'`; **no `GOODS_CATEGORIES` id appears anywhere on a generated record** (W-2, 0 hits) | §11 BLOCK-7 |
| ⛔ **`services` is not a held fact** | `src/generators/steps/economyReconcilePass.js` | `availableServices` | `servicesToggles` is the same shape and its Tier-1 row is also `recordPath: null`; the record's `availableServices` is COMPUTED from `tier, institutions, servicesToggles` (`:195`), so it is the DIAL's DERIVATION, which §14 forbids declaring (W-3) | §11 BLOCK-7 |
| ⭐ **`EntityRef`, the other two layers** | `src/pdf/primitives/EntityRef.jsx` · `src/lib/entityRefTokenizer.js` | — | the first is *"the PDF mirror of the web's EntityLink primitive"* rendering an in-dossier cross-reference by stable id; the second splits prose into segments around `⟦entity:<id>\|<displayName>⟧` tokens. **Both are id-bearing references to one entity; neither declares a typedef and neither is imported here** (W-11) | the one sentence §6.2 owes the shared name |
| ⭐ **`Op` / `EntityRef` ownership** | `packets-waiting/EM-B1a.md` + `EM-B1a.manifest.json` | — | EM-B1a's §6 opens `@typedef {import('./types.js').Op}` and `…EntityRef`; its `changeManifest` holds exactly THREE rows — `operations.js`, `worldConditions.js`, `tests/domain/editOperations.test.js` — and **`src/domain/edit/types.js` is in NEITHER §7 NOR the capsule** (W-7) | EM-A1 owes BOTH typedefs |
| Registration instrument | `tests/lint/mutationCoverage.shared.mjs` | `ENFORCER_DIRS` | `tests/lint` first of eight. ⚠ AS OF `bdbf7c89c`, AS HISTORY ONLY: `invariants` 716, `tests/lint` rows 177, `uncoveredBaseline` **186 (a NUMBER)** (W-10) | §7 row 5 |
| Census instrument | `tests/lint/sovereigntyLightingContract.walker.test.js` | `TEST_FILES` · `CENSUS` | counts **TEST files** under `tests/` (`:515`). ⚠ AS OF `bdbf7c89c`, AS HISTORY ONLY: `2665 · 359 · 2306 · 25550 · 6816`, `measuredAtSha 069de409f` (W-10). ⛔ The integration tip carries the ELEVENTH refreeze — the live tuple is read from `tests/lint/.lighting-census-baseline.json` | §7's deferred lighting row |
| ⭐ **Src-tree governing test** | `tests/architecture/layerBoundaries.test.js` | `describe('layer boundaries (F29)')` | it `walk(SRC)`s **whole** (`:89`, `:183`), so a CREATE under `src/domain/**` opts into it; its arms are the headless-engine spine (no React/Zustand/store, direct or transitive) and a shrink-only dependency-cycle baseline (W-11) | a SEALED check of §10 — version 4 named it nowhere |
| Golden posture | `tests/property/generatorGoldenMaster.test.js` · `tests/property/dossierProseManifest.test.js` | — | neither appears in §7 | UNCHANGED; motion is a STOP |
| Test precedent (domain) | `tests/domain/factionRename.test.js` | `describe('faction rename — the INDEPENDENT denominator')` › `test('every stored path that carries a faction name is declared, or ruled out in writing')` | an independent denominator that never reads the module's own declarations | copy this proof shape |
| Test precedent (walker) | `tests/lint/habitBandsReconciliation.walker.test.js` | `describe('HB — the Bands line reconciles against the tuning seam, both directions')` › `test('GUARD-THE-GUARD: a Bands line planted on a no-bands wave REDS')` | literal titles, both-directions reconciliation, one planted-mutation arm | copy this walker shape |

Forbidden alternatives:

- no second declaration table, registry, classifier, time source, PRNG stream or writer;
- ⛔ **no re-typed copy of `GENERATION_TIER1`, `GENERATION_TIER2`, their `cardShape` strings,
  `WORLD_FACT_SOURCES`'s keys, or `DECL_FORMS`.** Every declared set is IMPORTED from its producer
  (`contractTestAntiVacuity` Rule 2, and the EM-R family's totality law);
- ⛔ **no second spelling of the frozen wave-1 id list.** It has ONE home, ARM I-1's file;
- no new top-level `worldState` key; no persisted key at this wave;
- no direct edits to `src/components/**`, `src/store/**`, `src/generators/**`,
  `src/domain/generation/**`, `tests/helpers/**`, `src/domain/factionRename.js`, either golden,
  `tests/lint/.lighting-census-baseline.json`, or `docs/content/wiring-census.json`;
- ⛔ **no declaration that satisfies no ARM I-4 branch** — widening the arm to admit one is a STOP;
- ⛔ **no `goods` or `services` row** until BLOCK-7 is ruled;
- no files outside the manifest.

## 6. Exact contracts

### 6.1 Inputs and outputs — SETTLED (unchanged from version 4)

```js
/**
 * @param {string} cardType
 * @returns {readonly FieldDeclaration[]}  frozen, authored order; the SAME shared frozen
 *   EMPTY array for an unknown or undeclared cardType — never null, never a throw.
 */
export function declarationsFor(cardType) {}

/** @param {string} cardType @returns {boolean} `declarationsFor(cardType).length > 0` */
export function isEditableCard(cardType) {}

/** @type {Readonly<Record<string, readonly FieldDeclaration[]>>} deep-frozen */
export const FIELD_DECLARATIONS = Object.freeze({ /* … */ });
```

### 6.2 State schema — SETTLED, at the WIDENED typedef (the chair's rulings 1, 2, 3 and 7)

```js
/** @typedef {'pool'|'free'|'free-cascade'|'share'} FieldKind */
/** @typedef {'root'|'world-fact'|'annotation'} FieldProvenance */
/** @typedef {{ card: 'institution'|'npc'|'faction'|'powerSeat'|'worldFact', field: string,
 *   kind: FieldKind, provenance: FieldProvenance, label: string, group: string,
 *   outputKey?: string, pool?: string, maxLength?: number, readersProof?: string,
 *   writer?: string, createdBy?: string,
 *   tier1?: { step: string, key: string } }} FieldDeclaration */

/** @typedef {{ kind: 'settlement'|'institution'|'npc'|'faction'|'power'|'phantom'|'section',
 *   id: string }} EntityRef */

/** @typedef {{ type: string, target: EntityRef, payload: Readonly<Record<string, unknown>>,
 *   stage: 'home'|'off-stage', consequence: 'home'|'by-target-reality',
 *   requires: readonly string[], enables: readonly string[], relatedTo: readonly string[],
 *   conflictsWith: readonly string[], duration: number|null }} Op */
```

**`FieldKind` gains `'share'` (the chair's ruling 2).** A `'share'` field is a BOUNDED NUMBER whose
totality is a guard's, not a pool's. Its one member today is `faction.power`, a NUMBER present on
204 of 204 faction records (V-3) which design §14 rules *"a chosen share the guard renormalizes;
it stays a ROOT under the totality guard"*. ⭐ **Its totality citation, by symbol:**
`renormalizeFactionPower` in `src/generators/power/rulingStructure.js`, signature
`(factions) => factions` — it **mutates in place and returns the same array**, and it GUARANTEES,
in its own words, *"integer percentage points summing to exactly 100, using largest-remainder
rounding so the rounded points always total 100 (never 99 or 101)"*, with **rank order preserved**
and a **no-op on an empty or all-zero roster** (W-12). It is already a `requiredSymbols` row of the
compiled EM-B1a, whose `totality` guard rule calls it as the `fulfil` writer (ARCH §5), so this
packet CITES a sibling's contract rather than inventing one. ⛔ **`pool`, `maxLength` and
`readersProof` are ABSENT on a `'share'` row** — a `'share'` row carrying a `pool` is plant P8 and
reds (W-5).

**`provenance` is a THREE-member type (the chair's rulings 1 and 3).** Design §14's five kinds map
to exactly three declarable values: world facts → `'world-fact'`, roots → `'root'`, annotation →
`'annotation'`; **derived facts are never declared** (a derivation is not editable on any card) and
**minted is a property of an ENTITY, not of a field**. The kinds and the provenances are
independent: a `'pool'` row may be `root` or `world-fact`; a `'free'` row is always `annotation`.

**`card` is a CLOSED FIVE-member union.** Four are the register's own `cardShape` strings, IMPORTED
from `GENERATION_TIER2` and never re-typed; the fifth, `'worldFact'`, the register does not name —
so the packet takes the spelling EM-A2a and EM-A2b already use for its pool ids — **MEASURED at
`bdbf7c89c`: EM-A2a version 5 holds `worldFact.terrain`, `worldFact.culture`,
`worldFact.monsterThreat` and `worldFact.stressors`; EM-A2b version 4 holds
`worldFact.resources`** — and states that here, so no sibling has to guess. ⛔ **`worldFact.goods`
and `worldFact.services` LEFT EM-A2b** with the chair's ESTATE-REPAIR-4 ruling 1 (judgment 54) and
ride **EM-P3c**, so neither is a sibling's pool id in wave 1 and neither is named here; the five
spellings above are exactly this packet's five world-fact rows. ⛔ *"world fact"* with a
space is not a card name anywhere in this packet.

**`tier1` is the world-fact join, and only that.** It names the Tier-1 `(step, key)` pair that HOLDS
the fact on the record, exactly as a `GenerationTier2Row` names its own holding pair in its `step`
and `key` columns. It is absent on every other row.

**`writer` is `'<repo-relative path>#<symbol>'`.** Three rules, each settled because the measurement
forced it:

1. The symbol is resolved **against that file's source through the LANDED `declaredSymbols`
   resolver**, never against exports and never against a re-typed form set. Every Tier-2 step symbol
   is declared only by `registerStep('<name>', …)`; a resolver without that form finds **zero** (V-2).
2. The path must exist and the symbol must be declared in it **exactly once**. Zero, two or a bad
   shape is a walker red. The habitat is real and measured — **52 of the 114 `.js` files under
   `src/generators` declare at least one symbol more than once** (V-5).
3. The symbol named is the one the REGISTER names for that `(cardShape, outputKey)`, so the
   declaration and the register cannot drift.

**`EntityRef` and `Op` join this file (the chair's ruling 7), and the measurement that settles `Op`.**
EM-B1a's §6 opens with `@typedef {import('./types.js').Op}` and `@typedef {import('./types.js').EntityRef}`,
and its dependency sentence reads *"EM-A1 (`src/domain/edit/types.js` — the `Op` and `EntityRef`
typedefs this packet's JSDoc references). EM-A1 must land FIRST or the typedef imports have no
home."* ⭐ **MEASURED (W-7): EM-B1a's change manifest holds exactly three rows —
`src/domain/edit/operations.js` (CREATE), `src/domain/edit/worldConditions.js` (CREATE),
`tests/domain/editOperations.test.js` (CREATE) — and its own §7 note says *"§7 and the capsule are
set-equal at three rows"*. `src/domain/edit/types.js` is in NEITHER. EM-B1a only IMPORTS.
⇒ EM-A1 owes BOTH typedefs, EM-B1a's dependency sentence is CORRECT, and VERSION 4's REPORT — which
said EM-B1a *"already MODIFIES that file to add `Op`"* — IS RETRACTED. That sentence is the docs
slip, and it is version 4's, not EM-B1a's.**

- `EntityRef`'s `kind` is the CLOSED SEVEN-member set EM-B1a names, quoted from its
  `OpTypeDeclaration.target`: `'settlement'|'institution'|'npc'|'faction'|'power'|'phantom'|'section'`
  — the same seven ARCH §2 spells, and the set EM-B1a's algorithm step 3 checks
  (*"if `op.target` is not `{ kind, id }` with kind in the closed EntityRef set"*) and its A2 plants
  (`{kind:'nonsense'}`).
- ⭐ **ONE CONCEPT, THREE LAYERS, ONE NAME.** `src/pdf/primitives/EntityRef.jsx` RENDERS an entity
  reference in the PDF and `src/lib/entityRefTokenizer.js` TOKENIZES one in prose; this typedef is
  the same concept's shape in the domain, so one name is right and a second spelling would be the
  drift. Neither of those modules declares a typedef and neither is imported here (W-11).
- `Op`'s shape is EM-B1a §6's, not ARCH §2's: its transition table returns *"an `Op` carrying the
  type's declared `stage`, `consequence` and four relations"* and its §6 rule is *"EVERY FIELD IS
  REQUIRED ON EVERY ROW. None is optional; none may be omitted 'when empty.'"* — so every field is
  required, an empty relation is `[]` and an absent duration is `null`. ⚠ `duration` is carried
  although EM-B1a's transition table does not name it, because `OpTypeDeclaration` carries it as a
  required `number|null` and EM-B1a's own note says *"THE OP SHAPE IS PERSISTED BY A LATER PACKET,
  WHICH IS WHY IT IS RIGID … A field added after EM-B3 lands is a stored-shape change with a
  migration cost."* Recorded as judgment 4; reversal is one deleted line.

**Absence rules, BY PROVENANCE — each one an A4 assertion, each executed (W-4, W-5):**

| field | `root` | `world-fact` | `annotation` |
|---|---|---|---|
| `card`, `field`, `kind`, `provenance`, `label`, `group` | REQUIRED, non-blank | REQUIRED, non-blank | REQUIRED, non-blank |
| `card`'s value | one of the register's four `cardShape`s | exactly `'worldFact'` | one of the register's four |
| `kind`'s value | `pool` \| `free-cascade` \| `share` — ⛔ never `free` | exactly `'pool'` | exactly `'free'` |
| `outputKey` | REQUIRED, non-blank | REQUIRED, non-blank | ⛔ **ABSENT** — an annotation has no record path, and the absence IS the claim that nothing on the record reads it |
| `writer` / `createdBy` | **exactly ONE**, never both, never neither | ⛔ both ABSENT | ⛔ both ABSENT |
| `tier1` | ⛔ ABSENT | REQUIRED `{ step, key }`, both non-blank | ⛔ ABSENT |
| `pool` | present **iff** `kind === 'pool'` | REQUIRED (the kind is always `pool`) | ⛔ ABSENT |
| `maxLength` | present **iff** kind is `free-cascade` (⇒ absent on `pool` and on `share`) | ⛔ ABSENT | REQUIRED |
| `readersProof` | ⛔ ABSENT (no root is `free`) | ⛔ ABSENT | REQUIRED, non-blank — EM-A3 mints the VALUE; EM-A1 authors `'tests/lint/flavorFields.census.test.js (EM-A3 mints the proof string)'`, which names the census design §2.1 asks for and claims no proof EM-A3 has not run |

Table-wide:

- `pool` ids are **unique across the whole table** (eleven today, PRINTED by ARM I-2).
- one row per `(card, field)`; a duplicate is a walker red.
- an empty `FIELD_DECLARATIONS[cardType]`: **forbidden** — an undeclarable card type is absent from
  the table, never present-and-empty.
- `null` anywhere in a row: **forbidden**.
- ⛔ `writer: 'EM-B1a#set-institution-state'` — version 3's spelling — is FORBIDDEN and is planted
  as a red (P6, W-5): `EM-B1a` is not a path.

### 6.3 ⭐ The walker's arms — specified with no discretion

⛔⛔ **THE SUPERSEDED PREDICATE IS DELETED.** `WORLD_FACT_PARAMS`, `WORLD_FACT_READS`, the
three-candidate-source table and the source-position body scan that read them are GONE. **They must
not be built**, and a build lane that finds them quoted anywhere is reading version 3. The
predicate's own measurement survives in ONE place only — A6's regression control, where it is the
thing proved WRONG.

**ARM I-4 — THE ROOT ARM, THREE BRANCHES. The exact predicate:**

```text
0. Import GENERATION_TIER1, tier1For, GENERATION_TIER2 and tier2For from
   src/domain/generation/generationForkRegistry.js, and WORLD_FACT_SOURCES from
   src/domain/worldFactOptions.js. ASSERT GENERATION_TIER2.length > 0 AND
   GENERATION_TIER1.length > 0 AND Object.keys(WORLD_FACT_SOURCES).length > 0 FIRST — the arm
   reds on an empty producer rather than passing vacuously — and assert the ROOT rows' `card`
   values are a SUBSET of the register's own distinct cardShape set, read from the register,
   never re-typed.
1. For each declaration row, by provenance:
   a. 'annotation'  -> OUT of this arm's population by construction. (The §6.2 shape law owns
                       "an annotation row carries no outputKey"; asserting it here too is what
                       made version 4's plants red twice.)
   b. 'root'        -> if `createdBy` is PRESENT, the row passes this arm (its MEMBERSHIP of
                       WAVE1_PACKET_IDS is ARM I-1's, and the frozen list has ONE home).
                       Otherwise RED unless tier2For(row.card, row.outputKey).length >= 1.
   c. 'world-fact'  -> RED unless Object.hasOwn(WORLD_FACT_SOURCES, row.field).
                       RED unless t1 = tier1For(row.tier1.step, row.tier1.key) is non-null.
                       RED unless t1.recordPath === 'record.' + row.outputKey
                              OR  ('record.' + row.outputKey).startsWith(t1.recordPath + '.')
                       — the second form ONLY because GENERATION_BLIND_HALVES' `field-rootness`
                       row says Tier 1 classes a (step, key) PAIR and never a field inside the
                       key. Four of the five rows take the first form; `monsterThreat` takes the
                       second, and §1c.2 says so.
2. The failure message names the card, the field, the outputKey (or the tier1 pair) and which
   branch was taken.
```

**ARM I-3 — THE WRITER ARM. The exact predicate:**

```text
0. Import declaredSymbols from tests/helpers/generationForkCensus.js. ⛔ NEVER re-type
   DECL_FORMS: the registerStep form is the whole reason this arm works.
1. A row with no `writer` is OUT of this arm's population, by construction (every world-fact
   row, every annotation row, and the one `createdBy` root row).
2. Split `writer` at '#' — RED unless it yields exactly two non-empty parts.
   RED unless the path exists. RED unless declaredSymbols(source).get(symbol) === 1,
   the message naming the count.
3. The arm asserts its own population is NON-EMPTY before it reports zero offenders.
```

**ARM I-2 — the pool arm.** Every `kind: 'pool'` row carries a non-empty `pool` id; **no row of any
other kind carries one** (this is what convicts plant P8); ids are unique; the id set is PRINTED so
EM-A2a's own totality arm can be held against it. ⛔ **Membership of `POOLS` is NOT asserted here** —
`POOLS` does not exist at this base (W-10) and the arm is EM-A2a's; §11 BLOCK-4 says so in both
packets.

**ARM P — the pencil arm.** At wave 1 no pencil exists (`src/components/edit` is absent — V-6), so
the arm is a **TOTALITY OVER THE EMPTY SET WITH A PROVEN NON-EMPTY DENOMINATOR**: it scans
`src/components/**` for the marker EM-D1 will mint, asserts the found set is exactly empty, and
asserts in the same test that the scan walked a non-empty corpus. It becomes load-bearing the day
EM-D1 mints the first pencil, with no edit here.

**ARM I-1 — the existence arm** (in the DOMAIN test). A ROOT row is a live key on its card's record
at ≥1 generated tier, or names a `createdBy` that is a member of `WAVE1_PACKET_IDS` — **the frozen
list, spelled once, in this file**. A WORLD-FACT row's `outputKey` is a live path on the generated
record. An ANNOTATION row is out of the population. The corpus is asserted non-empty FIRST.

**Executed, both directions, at this base (W-4, W-5).** All seventeen live rows pass every arm and
the shape law — `LIVE ROWS GREEN ON EVERY ARM: 17 of 17`, `ARM I-2 offenders: 0` over eleven printed
pool ids. Ten planted rows, each red **on its own arm only** — `plants that RED on their own arm
ONLY: 10 of 10`:

| plant | arm | case | the message the arm gives |
|---|---|---|---|
| `powerSeat.stability` — **a REAL derived reading**, present 36/36 | I-4 | A5b | *no `GENERATION_TIER2` row joins (cardShape 'powerSeat', outputKey 'powerStructure.stability') and the row carries no createdBy* |
| `faction` + `powerStructure.factions[].isGoverning` — a REAL outputKey on the WRONG card | I-4 | A5b | the same shape, proving the join takes BOTH halves of the key |
| ⭐ `worldFact.tradeAccess` — **the charter's own eighth key** | I-4 | A5b | *'tradeAccess' is not a key of WORLD_FACT_SOURCES* |
| ⭐ `worldFact.terrain` with `tier1: { step: 'resolveConfig', key: 'noSuchCtxKey' }` | I-4 | A5b | *no `GENERATION_TIER1` row joins (step 'resolveConfig', key 'noSuchCtxKey')* |
| `…generatePopulation.js#noSuchChooser` | I-3 | A6 | *'noSuchChooser' is declared 0 times in … (exactly once required)* |
| `…steps/noSuchStep.js#generatePopulation` | I-3 | A6 | *writer path '…' does not exist* |
| `EM-B1a#set-npc-status` — **version 3's spelling**, on a LIVE row | I-3 | A6 | *writer path 'EM-B1a' does not exist* |
| `createdBy: 'EM-ZZ9'` | **I-1** | **A3** | *createdBy 'EM-ZZ9' is not a wave-1 packet id* |
| ⭐ a `'share'` row carrying a `pool` | SHAPE | A4 | *pool is present iff kind is 'pool'* |
| ⭐ an `annotation` row carrying an `outputKey` | SHAPE | A4 | *an annotation row carries no outputKey* |

⭐ **Two plants moved, and why.** `createdBy: 'EM-ZZ9'` moved from A5b to A3 with its predicate
(one arm owns the frozen list). `EM-B1a#set-institution-state` was re-pointed onto the LIVE
`npc.status` row as `EM-B1a#set-npc-status`, because planting it on `institution.state` deletes that
row's `createdBy` and reds ARM I-1 and ARM I-4 as well — measured (W-5, first run). The plant keeps
its point (a packet id is not a path) and gains isolation.

⭐⭐ **A6's REGRESSION CONTROL, executed (W-4).** A symbol declared ONLY by
`registerStep('<name>', …)` resolves to exactly one declaration — `assembleInstitutions` PASSES
through the landed resolver and REDS (0 matches) through version 3's form set. **This is the one
place the superseded predicate survives, and it survives as the thing the test proves wrong.**

### 6.4 Ordering, determinism, flag, lifecycle, receipts — SETTLED

- Stable enumeration: authored order, returned verbatim; `declarationsFor` sorts nothing.
- Pipeline/tick position: **NONE.** Pure data.
- Hash/fork key `NONE`; no rounding; **no PRNG is reachable** (`EM-PREAMBLE.md` §P5 HZ-PRNG —
  `rollFrom` is EM-A2a's); no ambient time, locale or environment read.
- Flag: `NONE`. Golden posture: **UNCHANGED**, proved by the zero-importer scan (§8 step 1).
- Lifecycle: module load, frozen; read by `declarationsFor`; **never persisted**; every other
  column `n/a`.
- Receipts: `NONE`. DM-only fields: the two `annotation` rows are DM-layer-only BY DECLARATION
  (they carry no `outputKey`), which is a contract, not a persisted key — nothing is stored here.
- Public projection: `n/a` — nothing persists, so nothing travels.
- Alignment: `DECLARED EMPTY: a declaration table takes no alignment position.`
- Edit story: `ENGINE-ONLY: this wave is the source the editor is generated FROM; it exposes no DM verb.`

## 7. Exact change manifest

⚠ **§7 and `EM-A1.manifest.json` are SET-EQUAL** (interim rule 6): five paths, the same five,
compiled capsule-first and diffed by a plain-node comparison (W-11).

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `src/domain/edit/types.js` | `FieldKind`, `FieldProvenance`, `FieldDeclaration`, `EntityRef`, `Op` JSDoc typedefs | `≤ 90` (**≈ 50 est.**) | Author §6.2's five typedefs VERBATIM. `EntityRef`'s seven-member `kind` set and `Op`'s ten required fields are EM-B1a §6's, quoted there. Include the ONE sentence naming `src/pdf/primitives/EntityRef.jsx` and `src/lib/entityRefTokenizer.js` as the same concept in two other layers. Export nothing executable; import nothing. |
| `CREATE` | `src/domain/edit/fieldDeclarations.js` | `FIELD_DECLARATIONS`, `declarationsFor`, `isEditableCard` | `≤ 210` (**≈ 119 est.**) | Author the deep-frozen table of §1c.2's SEVENTEEN rows plus the two readers at §6.1's exact signatures. Import only `./types.js`. ⛔ No import of the register, of `WORLD_FACT_SOURCES`, of `POOLS`, or of anything under `src/components/**` — the table holds STRINGS and the arms do the joining. |
| `CREATE` | `tests/domain/editDeclarations.test.js` | cases A1–A4, A7 + the dormancy case | `n/a` | SIX literal `test` titles under ONE literal `describe`; no `.each`, no loop-generated registration, no nested `describe` (`EM-PREAMBLE.md` §P3.4). Holds `WAVE1_PACKET_IDS`, the frozen list, in its ONE home. ⛔ **Never bind `it`, `test` or `describe` as a variable or a parameter** — the census resolves an opener only where the module binds that word EXACTLY ONCE, and 26 of the estate's parked files park for that alone. ⛔ **No bare `for (const seed of …) { expect(…) }`** — `seedLoopTotality` convicts it; collect, then assert once. |
| `CREATE` | `tests/lint/editDeclarations.walker.test.js` | cases A5, A5b, A6 | `n/a` | THREE literal `test` titles under ONE literal `describe`. The arms of §6.3, with every declared set IMPORTED from its producer. Every negative assertion carries `// anchored:` on the line immediately above (`EM-PREAMBLE.md` §P6). Same opener-binding and seed-loop bans as the row above. |
| `REGISTER` | `scripts/mutation-coverage-manifest.json` | `rowKey`: `invariants['tests/lint/editDeclarations.walker.test.js']` | `≤ 12` | ONE row, surgically beside its `tests/lint/` siblings. ⛔ Never re-serialise the manifest whole; ⛔ never touch `uncoveredBaseline`. |

Generated artifacts: `NONE`. **Neither new leaf is inside an edge-shared bundle closure** —
`scripts/build-edge-shared.mjs`'s `ENTRIES` are exactly `src/domain/aiGrounding.js`,
`src/lib/analyticsEvents.js`, `src/domain/aiCharter.js`, `src/domain/intentAtlas.js` and
`src/domain/aiOutputSchema.js`, and `grep -c 'src/domain/edit'` on that script returns **0** (W-10).

No other file may be edited.

**BYTE PRICES, PREDICTED PER CLOSURE (interim rule 2).** Both new leaves have **ZERO importers by
construction** (wave 1 is headless; §8 step 1 lands that as a registered case), so:

| closure | predicted price | how membership is proved |
|---|---:|---|
| the generation worker (`WORKER_BUNDLE_CEILING_BYTES`, EXACT, zero slack) | **+0 B** | nothing under `src/generators/**` imports either leaf; the zero-importer scan is the probe |
| the lazy engine (`< 679_000`) | **+0 B** | the same |
| the eager first-paint closure | **+0 B** | EXECUTED: `EAGER_FIRST_PAINT_MODULES` imported from `vite.config.js` holds **269** members and **none is under `src/domain/edit`** (W-10). The set is seeded from every `src/domain` module a `src/generators` module imports; nothing imports these |
| the five edge-shared metas | **+0 B** | `ENTRIES` measured above |

⛔ **No `skipIf(!requireDistRead)` dist arm is in `checks`**, and no budget test is a `TEST` row,
because no budget moves.

**Predicted register moves, priced here rather than discovered at the terminal — EVERY FIGURE A DELTA:**

| Register | Moves? | Predicted delta | Door |
|---|---|---|---|
| Lighting census | **YES — a named INTERIOR RED at this member's commit** | **DELTA ONLY: `files +2` · `parked +0` · `credited +2` · `titles +9` · `suiteTitles +2`**, in the register's own order. `T = 9` is FIXED BY CONSTRUCTION and is UNCHANGED from version 4 because version 5 adds rows and plants, never cases: six literal `test` titles in the domain file (A1, A2, A3, A4, A7 and the dormancy case) and three in the walker (A5, A5b, A6), one literal `describe` each. `credited +2` / `parked +0` hold only if both files obey §7's opener rule; the build lane runs `parkReasonsFor` over both new files and QUOTES it before trusting `+2` (`LANE-EM-PREPROOF` step 14b). ⛔ **The deferred row names `tests/lint/.lighting-census-baseline.json`, never the walker.** | Re-derived WHOLE at the train terminal, BY THE CHAIR (`EM-PREAMBLE.md` §P3.2) |
| | | ⛔ **The cause is the TWO NEW TEST FILES, not the two new `src/domain/**` leaves** — `EM-PREAMBLE.md` §P2.1's stated cause is refuted; §11 BLOCK-5. | |
| Wiring census | **YES — a named INTERIOR RED, delta only** | `docs/content/wiring-census.json`: **`stamp.producerIndexFiles` +2** — `src/domain/edit/types.js` and `src/domain/edit/fieldDeclarations.js`, the two new `.js` leaves under `src/domain/**` (the root measured against; `src/generators/**` gets **+0**). ⛔ `producerIndexFiles` is a NUMBER (1172 at this tip), so the answer is a DELTA. `stamp.files` (7 entries) and `stamp.candidateLeaves` (6 entries) **UNMOVED** — both key on `src/domain/display/stateProse/` paths this member does not touch. `totals.*` **UNMOVED**. ⛔ **Regenerated WHOLE at the train's terminal, BY THE CHAIR.** ⛔ **This path is in NEITHER §7, NOR `changeManifest`, NOR `checks`** (interim rule 1). | The chair, at the terminal |
| Mutation-coverage manifest | **YES** | **RELATIVE, never absolute: `invariants` +1 row keyed `tests/lint/editDeclarations.walker.test.js`; `tests/lint/` rows +1**; **`uncoveredBaseline` UNMOVED** (it is a NUMBER, 186 at this tip — W-10). The row is keyed by this member's own test file and is ROW-DISJOINT from every other holder of the path — EM-A3's, EM-B1b's, EM-B1h's, **EM-B2a3's, EM-B2a4's and EM-C4a's** — MEASURED over all 29 waiting capsules at `bdbf7c89c` (ESTATE-REPAIR-6). ⛔ **The unsplit `EM-B2a` no longer exists:** judgment 33 split it into a1–a4 behind the EM-R family, and a1 and a2 hold NO row on this path. ⚠ **PATH-disjoint it is NOT, until TOOL-27 lands** — the capsule carries TOOL-27's `rowKey` today, which the validator ignores (V-14). | §7 row 5, this member's commit |
| Writer-reach register | **NO** | zero — this wave adds **no reader of any settlement field**. `fieldDeclarations.js` holds field names, record paths, Tier-1 pairs and writer names as STRINGS in a frozen table and dereferences no record; the register grades four read-site kinds and a string literal is none of them. **Neither `--write` nor a mint is owed.** | — |
| Observed-shape register | **NO** | zero | no save-time key is read; EM-B3 owns that door |
| Prose-numerics · size-baseline · edge-shared · first-paint budget | **NO** | zero | nothing rendered, baselined, or inside a closure (the byte table above) |
| ⭐ Dependency-cycle baseline (`tests/architecture/layerBoundaries.test.js`) | **NO** | zero — the one new edge is `fieldDeclarations.js → types.js`, acyclic; the baseline is shrink-only and a non-cycle never enters it. **PLAUSIBLE** (reasoned from the arm's source; this lane runs no vitest) | — |
| Browser suite (`e2e/`) | **NO** | zero | interim rule 10: this member touches no `src/components/**` path, no route, no `data-testid` and no accessible name. **No `e2e/` spec is named, and that is the measurement, not an omission.** |

## 8. Ordered coding sequence

0. Dispatch and seal; stop on any preflight mismatch.
1. **Capture the dormancy evidence FIRST:** a source scan asserting the two new leaves have ZERO
   importers anywhere in `src/`, landed as a registered case inside
   `tests/domain/editDeclarations.test.js` (`PACKET_STANDARD.md` step 1; the HB-0 precedent). This
   is also §7's byte-price probe.
2. Add the failing tests for A1–A7 (eight cases: A1–A5, A5b, A6, A7).
3. Implement `src/domain/edit/types.js` — the five typedefs.
4. Implement `src/domain/edit/fieldDeclarations.js` — the seventeen rows and the two readers.
5. Wire consumers: **NONE** — headless by construction.
6. Add the walker (ARM I-2, I-3, I-4, P of §6.3) and the one manifest row.
7. Run focused verification (§10).
8. Write the completion receipt. ⛔ The bare full gate and the boot smoke belong to the TRAIN TERMINAL.

Bounded algorithm (`declarationsFor`):

```text
1. If cardType is not a non-empty string, return the shared frozen EMPTY array.
2. Look up FIELD_DECLARATIONS[cardType] with Object.hasOwn — never a prototype walk.
3. If absent, return the SAME shared frozen EMPTY array (identity-stable across calls).
4. Otherwise return the frozen authored array by reference; never a copy, never a sort.
```

## 9. Acceptance matrix

| ID | Case | Arm | Fixture/input | Required observation | Test home |
|---|---|---|---|---|---|
| A1 | Main behaviour | reader | each of the five declared cardTypes | authored rows, authored order, frozen; `isEditableCard` true | `tests/domain/editDeclarations.test.js` |
| A2 | Absent/unknown | reader | `undefined`, `''`, `'nope'`, `42`, `'constructor'`, `'toString'`, **`'world fact'`** (the banned spacey spelling) | the SAME frozen empty array by identity every time; `isEditableCard` false; no throw | `tests/domain/editDeclarations.test.js` |
| A3 | **ARM I-1 EXISTENCE + GUARD-THE-GUARD** | **I-1** | settlements generated at village, town and city through `generateSettlementPipeline(config, null, { seed })`; **one planted row** | corpus asserted non-empty FIRST; every ROOT row is a live key on its card's record at ≥1 tier OR carries a `createdBy` in the frozen `WAVE1_PACKET_IDS` (spelled ONCE, here); every WORLD-FACT row's `outputKey` is a live record path; every ANNOTATION row is out of the population; **the planted `createdBy: 'EM-ZZ9'` REDS**, naming the field and the card | `tests/domain/editDeclarations.test.js` |
| A4 | **Boundary — the §6.2 shape law, BY PROVENANCE + GUARD-THE-GUARD** | §6.2 | the whole table; **two planted rows** | every cell of §6.2's absence table asserted for all seventeen rows: exactly one of `writer`/`createdBy` on a root, both absent elsewhere; `tier1` iff world-fact; `outputKey` non-blank on root and world-fact and ABSENT on annotation; `pool` iff `kind === 'pool'` and unique; `maxLength` iff `free-cascade` or `free`; `readersProof` iff `free`; `card` in the closed five; no duplicate `(card, field)`; every `free-cascade` row's `outputKey` is a declared KEY surface of `NPC_RENAME_SURFACES` or `FACTION_RENAME_SURFACES`, **or** is the one named exception `institutions[].name`, whose cascade EM-R6 mints. **The two planted rows RED** — a `'share'` row carrying a `pool`, and an `annotation` row carrying an `outputKey` | `tests/domain/editDeclarations.test.js` |
| A5 | The pencil arm and the pool arm | **P, I-2** | the source tree; the table | ARM P's found set is exactly empty over a proven non-empty corpus; every `pool` row carries a non-empty unique id, **no non-`pool` row carries one**, and the eleven ids are PRINTED; ⛔ membership of `POOLS` is asserted NOWHERE here (§11 BLOCK-4) | `tests/lint/editDeclarations.walker.test.js` |
| A5b | ⭐ **ARM I-4 THE ROOT ARM, ALL THREE BRANCHES + GUARD-THE-GUARD** | **I-4** | the live table; **four planted rows** | `GENERATION_TIER2.length > 0`, `GENERATION_TIER1.length > 0` and `Object.keys(WORLD_FACT_SOURCES).length > 0` asserted FIRST; every root row joins Tier 2 or carries a `createdBy`; every world-fact row is a `WORLD_FACT_SOURCES` key whose `tier1` pair resolves and whose `recordPath` equals-or-prefixes its `outputKey`; **the four planted rows RED** — `powerSeat` + `powerStructure.stability`, `faction` + `factions[].isGoverning`, `worldFact.tradeAccess`, and a world-fact row whose Tier-1 pair resolves to nothing — each message naming the card, the field and the branch | `tests/lint/editDeclarations.walker.test.js` |
| A6 | ⭐ **ARM I-3 THE WRITER ARM + GUARD-THE-GUARD** | **I-3** | the live table; **three planted rows**; the regression control | the population is asserted non-empty; every writer resolves to exactly one declaration through the imported `declaredSymbols`; ⭐ **a `registerStep`-only symbol PASSES** and version 3's two-form set is shown to find ZERO for it; **the three planted rows RED** — a symbol declared 0×, a path that does not exist, and `EM-B1a#set-npc-status` | `tests/lint/editDeclarations.walker.test.js` |
| A7 | Idempotency / purity | reader | repeated calls | `declarationsFor(t) === declarationsFor(t)` by identity; the table is deep-frozen (a write throws in strict mode) | `tests/domain/editDeclarations.test.js` |

⛔ **THE CASE IDS ARE EXACTLY EIGHT AND DO NOT MOVE.** Version 5 adds rows, branches and plants but
no case, so `acceptanceCases` stays at the validator's maximum of eight and the lighting census's
`titles +9` is unchanged.

Rows for persisted duplication, lifecycle round trip, writer-to-reader integration and privacy are
**omitted, not replaced**: this wave persists nothing, has no writer and no reader, and carries no
private field.

## 10. Verification commands

```sh
npx eslint src/domain/edit/types.js src/domain/edit/fieldDeclarations.js \
  tests/domain/editDeclarations.test.js tests/lint/editDeclarations.walker.test.js
npm run typecheck:ratchet          # tsconfig.full.json
npm run typecheck:domain:strict    # tsconfig.domain-strict.json — src/domain/edit/** must be clean

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/domain/editDeclarations.test.js --maxWorkers=2
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/lint/editDeclarations.walker.test.js tests/lint/mutationCoverageManifest.test.js \
  tests/lint/newsHeadlineContract.walker.test.js tests/lint/proseFamilyContract.walker.test.js \
  tests/lint/contractTestAntiVacuity.walker.test.js tests/lint/goldenFreeze.walker.test.js \
  tests/lint/negativeAssertionAnchor.walker.test.js tests/lint/netCurrentExtractorAnchor.walker.test.js \
  tests/lint/proseDrawnAnchors.walker.test.js tests/lint/pulseKernelLineAddress.walker.test.js \
  tests/lint/seedLoopTotality.walker.test.js tests/lint/sourceCitationIntegrity.walker.test.js \
  tests/lint/worldGenerationClockSeam.walker.test.js --maxWorkers=2
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/architecture/layerBoundaries.test.js --maxWorkers=2

node scripts/implementation-packets.mjs validate
```

⭐ **`tests/architecture/layerBoundaries.test.js` IS NEW IN VERSION 5 AND IS SEALED.** It
`walk(SRC)`s the whole `src/` tree twice (`:89`, `:183`), so a CREATE under `src/domain/**` opts
into it by construction; its spine arm asserts `src/domain` reaches no React, Zustand or store
directly or transitively, and its cycle arm is a shrink-only baseline. Version 4 named it nowhere,
which was a gap: both new leaves land under `src/domain/`.

**THE BUILD LANE'S INSTRUMENTS — run, never sealed:**

```sh
# the ONE named interior red, run so its five figures are RECORDED rather than discovered
GATE_MUTEX_TIER=shared … -- npx vitest run tests/lint/sovereigntyLightingContract.walker.test.js --maxWorkers=2
# the CREATE directories, WHOLE (a CREATE opts into every walker that governs the directory)
GATE_MUTEX_TIER=shared … -- npx vitest run tests/lint --exclude=tests/lint/sovereigntyLightingContract.walker.test.js --maxWorkers=2   # must EXIT 0
GATE_MUTEX_TIER=shared … -- npx vitest run tests/domain --maxWorkers=2                                                                 # must EXIT 0
```

Expected: every sealed command exits `0`. The lighting walker is the ONE named interior red at §7's
deltas and is **excluded from `checks` by construction** (interim rule 8). ⛔ A gate-mutex line that
prints no test count DID NOT RUN, and `gate-mutex.sh` exits `3` on a give-up. Report actual counts;
copy no historical count.

⚠ **Eleven further test files match `git grep -l -F 'mutation-coverage-manifest' -- tests` and are
DELIBERATELY NOT sealed** — every one of their matches is a COMMENT (V-13), so none reads the file
this member edits: `tests/config/modelRegistryAgreement.test.js`,
`tests/domain/espionageWariness.test.js`, the three `tests/edgeFunctions/*Bundle.freshness.test.js`,
`tests/lint/deadCodeDisposition.walker.test.js`, `tests/lint/discourseLexiconCoverage.test.js`,
`tests/lint/realmEntityWebNoFabrication.walker.test.js`, `tests/security/intentAtlasIdFree.test.js`,
and `tests/lint/sovereigntyLightingContract.walker.test.js` (also the instrument above);
`tests/lint/mutationCoverage.shared.mjs` is a helper, not a test.

⚠ **THREE `EntityRef` TEST FILES ARE NAMED AND NOT SEALED** —
`tests/edgeFunctions/entityRefWrapper.test.js`, `tests/lib/entityRefProducerConsumer.contract.test.js`
and `tests/pdf/proseLigatureDefuse.test.js` match `git grep -l -F 'EntityRef' -- tests` (W-11).
Measured: all three read `src/pdf/primitives/EntityRef.jsx` and `src/lib/entityRefTokenizer.js` by
PATH and none walks `src/domain`, so none can see this packet's typedef. They are named here
because the NAME is shared and a future reader will grep it.

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and `EM-PREAMBLE.md` §P8, stop if:

1. the dispatch seal is missing or belongs to another worktree state;
2. either golden moves;
3. a declared ROOT field is not a live key on its record and carries no wave-1 `createdBy`;
4. **a declared row satisfies no ARM I-4 branch** — widening the arm to admit one is a STOP, not an
   edit;
5. **any re-typed copy of the register's rows, its `cardShape` strings, `WORLD_FACT_SOURCES`'s keys,
   `DECL_FORMS`, or a SECOND copy of `WAVE1_PACKET_IDS` appears anywhere in the built files**;
6. **`WORLD_FACT_PARAMS`, `WORLD_FACT_READS` or the phrase "reads another world fact" appears
   anywhere in the built files** other than inside A6's regression control;
7. **an institution-rename OP is built anywhere in this wave.** `institution.name` is declared
   `free-cascade` and the cascade does not exist (V-9); EM-R6 mints it. EM-B1c / EM-D0 may not ship
   an institution rename before EM-R6 lands;
8. a component imports either new leaf, or either leaf acquires any importer at all;
9. a second declaration home appears necessary;
10. `parkReasonsFor` prints a reason for either new test file (the `credited +2` prediction is then
    wrong and §7's delta must be restated before the commit);
11. ⭐ **a `goods` or `services` row is authored before BLOCK-7 is ruled**, or a world-fact row is
    given an `outputKey` that is not a live record path;
12. ⭐ **the `Op` typedef is authored from ARCH §2 rather than from EM-B1a §6** — the optional-field
    form is the superseded one (§1, resolved contradictions).

---

### ⛔ THE BLOCKS, THE CHAIR'S DECISION POINTS, AND WHAT IS CLOSED

**BLOCK-0 — CLOSED. All three conditions discharged, each measured at this base** (V-2, V-10, V-11).
Item 1 (a registry the census can read) by **EM-P2 `ec0a30da2`** in `GENERATION_TIER2`, not in
`HABIT_FORK_REGISTRY`; item 2 (a domain-reachable home for the world-fact option sets) by **EM-P3**,
`WORLD_FACT_SOURCES`, one home and one declaration; item 3 (design §15's `npc.status` against the
tree's `NpcStatus`) by **EM-B1d**, seven members with `jailed`.

**BLOCK-4 — STANDS, and it blocks exactly ONE arm.** `export const POOLS` still exists only at
`src/domain/worldPulse/peopleLedger.js:59`, an unrelated four-member vocabulary (W-10).
- **What it blocks:** the MEMBERSHIP half of ARM I-2 and nothing else.
- **CAN EM-A1 LAND WITHOUT IT? YES — and NOT by shipping a dark arm.** ARM I-2 ships LIVE in a
  narrowed shape: `pool` ids present, non-blank, unique, non-`pool` rows carrying none, and the set
  PRINTED. **The membership assertion is EM-A2a's**, which already owns `POOLS`; it needs ONE added
  sentence — *"every `kind: 'pool'` row of `FIELD_DECLARATIONS` names an id present in `POOLS`"* —
  owed by EM-A2a's pre-proof. ⛔ **The three alternatives are REFUSED**: a dark arm is the vacuous
  green §P6 forbids; a file-existence conditional makes EM-A2a's landing red EM-A1's walker and owes
  an undeclared edit; a frozen re-typed id list is the class `contractTestAntiVacuity` Rule 2
  refuses.
- ⭐ **BLOCK-4b — ANSWERED BY THE CHAIR: BOTH POOL IDS NOW HAVE A NAMED HOME.** Re-measured at
  `bdbf7c89c` from the siblings' own tables (ESTATE-REPAIR-6): A2a's is **eleven** ids
  (`institution.class`, `faction.category`, `cause.remove`, `commodity`, `tier`,
  `npc.status`, `institution.state`, `worldFact.terrain`, `worldFact.culture`,
  **`worldFact.monsterThreat`**, `worldFact.stressors`) at **EM-A2a version 5**; A2b's is **six**
  (`name.settlement`, `name.npc`, `npc.role`, **`power.holder`**, `deity`,
  `worldFact.resources`) at **EM-A2b version 4**.
  - **`power.holder`** — design §2.2 lists the pool (*"powers → the power structure's seats"*), so it
    was an omission. ⭐ **MINTED: EM-A2b version 4 CARRIES THE ROW**, by the chair's ESTATE-REPAIR-4
    ruling 2 — it is the FACTION ROSTER read off the record, with the NPC half absent by name; it is
    world-derived and A2b owns the world-derived pools. Version 5's *"EM-A2b's pre-proof owes the
    row"* is DISCHARGED.
  - ⭐ **`worldFact.monsterThreat`** — NEW in version 5, and the chair RULED its home: **EM-A2a**
    (`findings/COMPILE-EM-B2a4-2026-09-20/CHAIR-RULING.md`, EM-A1 version 5's question 2, riding
    ESTATE-REPAIR-4), whose **version 5** table carries it as a `catalogue` of **3**.
    ⭐ **HISTORY, with its as-of mark — the conditional of ruling 5 had NO answer when version 5
    measured it (as of EM-A2a version 4 and EM-A2b version 3, W-2):** *"it joins WHICHEVER of
    EM-A2a / EM-A2b already mints the other six world-fact option-set pools"* — the six were split
    three and three (EM-A2a: `worldFact.terrain`, `worldFact.culture`, `worldFact.stressors`, all
    `catalogue`; EM-A2b: `worldFact.resources` (`world`), `worldFact.goods`, `worldFact.services`,
    both `catalogue`), `catalogue` appeared in both, and neither the condition nor the
    `catalogue`/`world` split broke the tie. ⛔ **THE TIE IS GONE ON BOTH SIDES:** the chair
    assigned the id directly, and `worldFact.goods` and `worldFact.services` LEFT EM-A2b for
    **EM-P3c** with ESTATE-REPAIR-4 ruling 1 (judgment 54), so A2b's world-fact half is
    `worldFact.resources` alone. What is measured about the pool itself: `monsterThreat`'s
    vocabulary is a fixed three-member catalogue,
    `MONSTER_THREAT_TIERS = ['heartland','frontier','plagued']` (executed, W-2), cited
    by `WORLD_FACT_SOURCES` as `src/data/monsterThreat.js#MONSTER_THREAT_TIERS`, and EM-A2a's
    `catalogue` reader shape takes it unchanged.
  - ⛔ **ARM I-2's MEMBERSHIP half (EM-A2a's arm) passes over `worldFact.monsterThreat` only once
    EM-A2a version 5 has LANDED** — the row is DECLARED, not yet landed, so the sequencing this
    block existed to protect still binds. EM-A1's own ARM I-2 is unaffected: it asserts presence,
    non-blankness and uniqueness, never membership.

**⭐ BLOCK-6 — CLOSED by the chair's ruling 3, with its count.** Version 4 measured EM-A3's `free`
denominator as EMPTY and the cause as structural. Ruling 3 declares the ANNOTATION rows in the same
typedef act, and ARCH §9's measured set gives **exactly TWO**: `institution.note` and `npc.note`
(W-6 — `note free` appears twice in ARCH §9 and nowhere else; faction, power seat and the world-fact
card have none). ⇒ **EM-A3's subject is 2 rows, not 0.** Its A5 case (*"every `free` row carries a
live proof"*) and its A2 case (*"a card type with no `free` row"*) are both non-vacuous: three of
the five cards have no `free` row and two do. ⚠ **EM-A3's pre-proof must still re-measure its own
denominator** rather than assume this number; and EM-A3's MODIFY row must be re-read against
version 5's table, because it says *"Touch no `kind`, no `pool`, no `field`, no `group`, no
`label`"* and version 5 adds `provenance` and `tier1` to the row shape — **neither is EM-A3's to
touch either, and its row should say so.** An ESTATE-REPAIR-4 line, not this packet's edit.

**⭐⭐ BLOCK-7 — NEW AND MEASURED: `goods` AND `services` ARE NOT FACTS THE RECORD HOLDS, so two of
the chair's seven world-fact rows are not declarable at this base.** The smallest measured
contradiction, in four parts (W-2, W-3):

1. The wizard's dials are `config._goodsToggles` and `config._servicesToggles`, which
   `resolveConfig` EXTRACTS into the ctx keys `goodsToggles` / `servicesToggles` (`:89`) and never
   writes into `effectiveConfig` (`:189-201`).
2. Both Tier-1 rows read `recordPath: null`, `onRecordClass: 'absent'` — the register's own verdict.
3. `record.config` carries neither key over 36 generated settlements (0 of 36), and **no
   `GOODS_CATEGORIES` id appears anywhere on a generated record** (a full record walk: 0 hits, while
   the same walk finds `config.monsterThreat = 'frontier'` — the positive control that proves the
   walk works).
4. The record's nearest holdings are DERIVATIONS of those dials, not the dials:
   `record.availableServices` is computed from `tier, institutions, servicesToggles`
   (`economyReconcilePass.js:195`) and `record.economicState.{primaryExports, primaryImports,
   localProduction}` from tier, institutions, toggles and resources. **Design §14: *"A derivation is
   never editable, on any card."***

⇒ Declaring either row would make ARM I-1 red (no live record path) or would declare a derivation
editable. **The packet declares FIVE world-fact rows and names the two.** ⭐ **RECOMMENDATION:** the
two rows belong to whichever packet gives the wizard's toggle bundle a home the editor can pin —
the save row's `toggles` JSONB column (`src/lib/saves.js:9`) is where they live today, which is
OUTSIDE the settlement record `rederive(record, config′, layer)` takes. That is EM-B2's question
(pinned re-derivation) or a charter row of its own; it is not EM-A1's to decide. ⛔ **And no
pool waits on the answer:** the chair's ESTATE-REPAIR-4 ruling 1 (2026-09-20, judgment 54) took
`worldFact.goods` and `worldFact.services` OUT of EM-A2b to **EM-P3c**'s sitting, so neither
pool is minted in wave 1 and the two rows travel with their pools. ⛔ **This is not a
deferral** (owner law, 2026-09-19 18:1x): it is a measured refutation of two rows with its cause,
its consequence (an editor whose world-fact card shows five facts, not seven) and its home named.

**BLOCK-3 — reported, not blocking.** `NPCInlineCard` is module-local (`npcComponents.jsx:186`, no
export); the per-faction card is an anonymous `roster.map` block inside `TheFactions`; §14's
power-seat card has no component; **and the world-fact card has no component either**. Whether ARM P
binds by a minted marker attribute, by file, or by an export EM-D1 adds is the chair's ruling, and
nothing here waits on it.

**BLOCK-5 — reported, not blocking.** `EM-PREAMBLE.md` §P2.1 prices the lighting census on the wrong
cause: it says a new `src/domain/**` or `src/components/**` file moves it, but the figure is
`TEST_FILES.length` over `walk(join(ROOT, 'tests'))` (`:515`). **A `src/` file moves nothing; a
`tests/**/*.test.js` file moves it by one.** §7's delta is stated on the correct cause.

**CHAIR-1 — DECIDED (ruling 1) AND BUILT**, with two riders measured here: the seven keys are the
LANDED seven (`monsterThreat`, not `tradeAccess` — the charter's EM-A1 row names *"trade access"*,
which is absent BY RULING and is EM-P3b's eighth key; **the charter row is the chair's to correct**,
and `worldFact.tradeAccess` is now plant P7); and two of the seven are BLOCK-7.

**CHAIR-2 — DECIDED (ruling 2) AND BUILT.** `FieldKind` gains `'share'`; `faction.power` is
declared; the root set is TEN. ⭐ **EM-D0 OWES THE NUMERIC CONTROL** — bounded, showing the
renormalized result before Save — which design §3's modal vocabulary (*"pool controls … free fields
as plain text with a length limit"*) does not list. The chair has slotted it as a charter row for
EM-D0's compile plus one sentence for §3 at the next design pass; it is recorded in §12 so no build
lane can lose it.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into the
next wave.

## 12. Completion receipt

- Base SHA: `7f0621fdb72f769a285502ff5481247c1f07b04b`
- Dispatch bundle and seal identity: **NOT DISPATCHED — the packet is DRAFT and has not been built.**
- Final commit or working-tree state: **no edit made.** This compile lane wrote only under
  `$SP/lane-em-compile-EM-A1-v5-scratch/`.
- Exact changed files and effective-line deltas: `NONE`.
- Acceptance cases: `0 of 8 executed`.
- Focused commands, exits, and counts: **no vitest, eslint, tsc, build or npm script was run, and
  the gate mutex was never touched.** Every measurement command and its output is in
  `EM-A1.evidence.md` §V-0…§V-16 (version 4, carried) and §W-1…§W-12 (version 5).
- Sealed per-step receipt and exact-state resume status: `n/a`.
- Both typecheck configurations: `n/a`.
- Wave-end gate stages actually executed: `NONE`.
- Base-versus-wave failure identity diff: `n/a`.
- Dormancy/golden result: `n/a`. Golden posture DECLARED UNCHANGED for the eventual build.
- Generated artifacts: `NONE`
- ⭐ **OWED BY A NAMED PACKET, each with its slot (owner law: no deferred work):**
  1. **EM-D0 — the numeric control** for `kind: 'share'` (the chair's ruling 2). Slot: a charter row
     at EM-D0's compile, plus one sentence for design §3.
  2. **EM-A2a (measured) or EM-A2b (the chair's word) — `worldFact.monsterThreat`.** Slot:
     ESTATE-REPAIR-4's docs pass over the waiting estate; ARM I-2's membership half cannot pass over
     that id until it lands.
  3. **EM-A2b — `power.holder`.** Slot: the same.
  4. **EM-A2a — the membership sentence** for ARM I-2's other half. Slot: EM-A2a's pre-proof brief.
  5. **EM-A3 — its MODIFY row's untouchable list** must name `provenance` and `tier1`. Slot:
     ESTATE-REPAIR-4.
  6. **The chair — the charter's EM-A1 row** still names *"trade access"* among the seven. Slot: the
     same dated-correction treatment T-12 got.
  7. **The chair — BLOCK-7** (`goods` and `services`). Slot: a decision point, with its
     recommendation above.
- Deviations: **NONE that block.** BLOCK-0 **CLOSED**; **BLOCK-6 CLOSED** with a denominator of two;
  **BLOCK-4 STANDS** (one arm's membership half) and **BLOCK-4b** grew by one id; **BLOCK-7 IS NEW**
  and refutes two rows of ruling 1; BLOCK-3 and BLOCK-5 reported, not blocking.
- Out-of-scope observations, without investigation:
  1. `powerStructure.governingName` has **TWO writers** — `rulingStructure.js:787` and
     `power/economyReconciliation.js:277` — against `EM-PREAMBLE.md` §P4's one-writer law.
  2. `powerStructure.factions[].category` is inferred from the faction's display NAME
     (`inferFactionCategory`), and `factionRoles.js` infers an archetype by name pattern.
  3. **52 of the 114 `.js` files under `src/generators` declare at least one symbol more than once**
     (V-5), so ARM I-3's "exactly once" rule guards a live habitat.
  4. EM-A2a's §6 heading and its `POOLS` comment both still read *"the eleven pool ids"* over a
     table of ten, which its own §12 corrects to ten.
  5. `GENERATION_BLIND_HALVES`'s `hash-channel` row names the `pairProse` FNV pick at
     `src/generators/generateSettlementPipeline.js:216` as UNATTRIBUTED. **It bears on no field this
     packet declares.**
  6. ⭐ **`renormalizeFactionPower` has a FILE-LOCAL COPY** at `src/generators/factionDynamics.js:508`,
     declared in its own comment as *"File-local copy of power/rulingStructure's
     renormalizeFactionPower"* because importing would close an import cycle the architecture test
     bans. Two homes for one totality rule, named and not investigated — it bears on `faction.power`
     only insofar as a future guard must pick the right one.
  7. ⭐ **`config.tradeRouteAccess` IS on the record, 36/36** (`'road'`), while `tradeAccess` is
     absent from `WORLD_FACT_SOURCES` by ruling. EM-P3b's eighth key therefore has a live record
     path waiting for it. Named, not investigated.
- Judgment calls, recorded vetoably:
  1. **The world-fact card declares FIVE rows, not seven** (§11 BLOCK-7), because `goods` and
     `services` are not facts the record holds. REVERSAL: the chair names a home for the toggle
     bundle and the two rows are added with that `outputKey`.
  2. **`monsterThreat`'s Tier-1 citation is the PREFIX form** `(resolveConfig, effectiveConfig)`,
     with the exact-form sibling `(resolveConfig, threat)` named as the chooser whose `recordPath`
     is `null`. REVERSAL: cite `threat` and drop the `recordPath` clause from ARM I-4 branch (c).
  3. **The frozen wave-1 id list has ONE home, ARM I-1's**, and plant P3 moved from A5b to A3 with
     it. REVERSAL: restore the duplicate check in ARM I-4 and accept a plant that reds twice.
  4. **`Op` carries `duration: number|null`** although EM-B1a's transition table names only `stage`,
     `consequence` and the four relations. REVERSAL: delete the one line.
  5. **`tests/architecture/layerBoundaries.test.js` is SEALED**, because it walks `src/` whole and
     both new leaves land under `src/domain/`. REVERSAL: move it to §10's instruments.
  6. **`types.js`'s self-imposed per-leaf cap is re-priced 60 → 90** for five typedefs instead of
     two, inside the estate's 250. REVERSAL: split `EntityRef` and `Op` into their own leaf — which
     would give EM-B1a two import addresses instead of one.
  7. **`institution.name` is declared `free-cascade` with a named, executed exception** rather than
     dropped or declared `free` (V-9). REVERSAL: hold the row until EM-R6 lands.
  8. **The sealed `checks` name three manifest readers, nine tests-tree walkers and one src-tree
     walker** rather than the fourteen files interim rule 8's grep returns, because eleven match on
     a COMMENT and a twelfth is the banned lighting walker (V-13). REVERSAL: seal all thirteen
     non-banned files.
