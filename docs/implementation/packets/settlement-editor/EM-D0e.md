# Settlement editor / EM-D0e — `CardEditorDialog`: THE FIRST EDITOR DOOR, GENERATED FROM THE DECLARATION TABLE, dispatching every edit through ONE injected seam whose only lawful shape is EM-C4a's `settlement.plain-edit.apply`, wearing EM-D0c's `editorHalo` on both surfaces, at +0 eager first-paint modules and +0 eager bytes

- **Status:** `READY`
  ⚠ The status value above stands ALONE on its line (§P9(c)): `parsePacketHeader` anchors the row at
  end-of-line and takes `status` only when exactly one row matches. Every stamp, caveat and date is
  on these continuation lines, and the same law binds **Verified base**.
  ⭐ **THE FIFTH AND LAST MEMBER OF EM-D0, THE FIRST DOOR** (`EM-D0.partition.md` section "D0e";
  `CHAIR-RULING.md` item 2). D0a · D0b · D0c · D0d have LANDED; this member is the door itself.
  ⛔ **NO INTERIM-RULES ROW.** The fifth amendment has landed at this member's read tip —
  `shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md` measures
  `fdecd426828665cad2dfaf872871f29f46fa4816e1bd4c43c20ccd84dacc9e7c`, the exact hash the amendment
  commit `fead56357` states — so `COMPILE-RULES.interim.md` is SUPERSEDED and this packet carries no
  interim citation and no clause saying which of the two governs.
  ⭐⭐ **WRITTEN TO THE SIXTH AMENDMENT AS DRAFTED, NOT RE-STAMPED INTO IT** (the chair's dispatch
  addendum, 2026-09-22 12:05; judgment 144). The sixth amendment is drafted and selected in the kit
  (`drafts/amendment-6/EM-PREAMBLE.amended.md`) and lands as the FIRST COMMIT of train EM-T13's
  branch, the train this member rides, so this packet obeys its §P2 rows 19-21 and its §P6, §P7 and
  §P9 changes from version 1 (§7a and §6.6 carry the measurements). The header's hash above is the
  FIFTH amendment's, MEASURED at this member's read tip, which is what stands at `efd0eb448`; the
  chair re-stamps it to the landed sixth at placement.
  ⛔⛔ **TWO CLAUSES OF THE LAUNCH BRIEF ARE REFUTED BY MEASUREMENT AND §2.2 CORRECTS THEM**: the
  `M src/App.jsx` lazy mount has no lawful subject at this landing (nothing in the landed or placed
  estate can open the dialog, and nothing can bind its writer), and `src/copy/en.js` — not
  `src/App.jsx` — is the one file this member modifies.
- **Packet version:** 1.2
  Version 1 was the compile's, measured at `efd0eb448` against the four EM-T12 members AS PACKETS.
  **WHAT VERSION 1.1 CHANGED AND WHY (the independent Opus pre-proof at train EM-T12's FLIPPED tip
  `16f0bc71c`, 2026-09-22):** train EM-T12 LANDED, so judgment 134's law applies — a sibling's
  LANDED fence, never its predicted one — and every fact version 1 marked PLAUSIBLE was re-measured
  against real code. Five changes, none of them to a contract, a case, a budget or a figure:
  **(1)** the two `_pendingRequiredSymbols` rows are DISCHARGED into `requiredSymbols` (fifteen rows
  → seventeen), and ONE SPELLING MOVED in the promotion — `PLAIN_EDIT_REFUSALS`' landed text is
  `export const PLAIN_EDIT_REFUSALS = Object.freeze([` at `src/store/editSlice.js:46`, while the
  pending row carried the contract form `export const PLAIN_EDIT_REFUSALS;`, which `git grep -c -F`
  finds **0** times at the tip and which would have refused this member's own sealed dispatch;
  **(2)** `tests/lint/editMutationPath.walker.test.js` — a walker EM-C4a LANDED inside the EM-T12
  window, which source-scans EVERY `src/` module and convicts by name any static import of `makeOp`,
  `validateOp`, `OP_TYPES` or `applyEdit` outside its three declared `EDIT_PATH` members — now
  GOVERNS this member's one `src/` CREATE, and joins §10's `checks` and §5; **(3)** §6.6's §P2.21(a)
  disposition is CORRECTED: the entropy-root census walks `src/` WHOLE, not `src/domain`, so it DOES
  reach this leaf and the delta is `+0` by CONTENT rather than by root; **(4)** four line addresses
  are refreshed to the tip; **(5)** §13's R2, R3, R5 and R6 carry the chair's rulings (judgment 146)
  and R4 carries its measured answer. Every later change to this body bumps it again (§P9(e)).
  ⭐⭐ **WHAT VERSION 1.2 CHANGED AND WHY (the BUILD lane's pre-seal measurement, 2026-09-22;
  ruled by the chair as judgment 171).** The lane STOPPED before its seal on difference 0 line
  (viii). The sixth amendment's §P10.11 (i) requires that *"a pre-proof RE-DERIVES THE GOVERNING SET
  FOR EVERY MODULE ITS CONTRACT IMPORTS … because dormancy arms and consumer rosters key on
  IMPORTERS and not on the edited file"*; §10's governing set was computed from the files this
  member EDITS and from the DIRECTORY of its one test CREATE, and never per import. Two LANDED arms
  over the two `src/domain/edit/` modules §7's CREATE row orders this leaf to import were therefore
  unseen, and — neither being in any T13 member's `checks` — both would have reddened for the first
  time at the train's TERMINAL full check, attributed to no member.
  **ARM 1** `tests/domain/editDeclarations.test.js :: EXPECTED_IMPORTERS` (EM-A1's DORMANCY arm,
  asserted set-equal in BOTH directions) measured **2 rows at the lane's base and 3 with this
  leaf**, the third being
  `src/components/edit/CardEditorDialog.jsx imports src/domain/edit/fieldDeclarations.js`. The
  arm's own guard-the-guard plants `src/components/edit/PlantedField.jsx` importing
  `declarationsFor` and requires it CONVICTED — this member's leaf, one directory over.
  **ARM 2** `tests/domain/editPools.test.js :: POOL_LEAF_SPECIFIER` (EM-A2a's darkness roster inside
  `tests/domain/editPools.test.js :: A6 the no-draw law, and the golden structural proof`, asserted
  `[]`) measured **`[]` at the base and `['src/components/edit/CardEditorDialog.jsx']` with this
  leaf**: ⛔ **this member is the FIRST importer of `src/domain/edit/pools.js` under `src/`**, which
  is judgment 148's law verbatim.
  ⇒ **FIVE CHANGES, none of them to a contract, a case, a budget, a figure or a title:** **(1)** §7
  gains TWO `TEST` rows at EM-C4a version 4.3's exact shape, spelled in the new §6.3a and §6.3b;
  **(2)** `checks` gains both files as entry 6, so the widening is proved inside this member's own
  batch instead of at the terminal, and the validator stays LAST; **(3)** §3's handwritten-files
  count moves 4 → 6 and the shared-file row carries each measured delta; **(4)** §10's governing-set
  paragraph is corrected to the per-import derivation the amendment requires, with the executed
  per-import sweep quoted; **(5)** the header's **Preamble** SHA-256 is corrected to the LANDED
  SIXTH amendment's. ⛔ **NO acceptance case, no `it`, no `describe` and no title is added or
  re-worded**, so the count law and the lighting delta are both unchanged, and `requiredSymbols`
  stays SEVENTEEN — EM-C4a version 4.3 added none for the two files it widened, and a row asserting
  a line this member's own edit moves would refuse its own build (§7's standing note).
- **Verified base:** `em-t13-d0e-2026-09-22` at `e1750d7454c1dff7e81342efadd05e47719ca6aa`
  ⚠ Left for the chair's promotion stamp; the value stands alone on its line.
  **The revalidation sentence the chair will use:** *"Re-measured at
  `16f0bc71cff32445164079542c0f38b2acb498c7` — train EM-T12's FLIPPED tip, where EM-B2a3, EM-B1c1,
  EM-C4a and EM-A2b are LANDED CODE (read tip `$SP/read-tip-em-t11`, detached, `git status --short`
  EMPTY at the start and at the end of the lane). THE J-T1 WINDOW `efd0eb448..16f0bc71c` over every
  change-manifest path and every `requiredSymbols` path moved exactly TWO files —
  `src/domain/edit/pools.js` (+88) and `src/store/editSlice.js` (+350, a new file) — and not one
  of this member's own four. Both CREATE targets ABSENT (`git ls-tree -r --name-only 16f0bc71c --
  src/components/edit` → `FreeField.jsx PoolField.jsx` only; the same over
  `tests/components/cardEditorDialog.test.jsx` → EMPTY); both non-CREATE targets PRESENT; all
  SEVENTEEN `requiredSymbols` rows found VERBATIM at `git grep -c -F` = 1, the two newest discharged
  from `_pendingRequiredSymbols` against real code; `retiredSymbols` EMPTY and proved by the
  post-edit simulation; the preamble measured
  `fdecd426828665cad2dfaf872871f29f46fa4816e1bd4c43c20ccd84dacc9e7c`, still the fifth amendment's;
  the eager first-paint set measured 270 members — unchanged across the window — with
  `src/components/edit/**` at 0, `src/copy/en.js` absent, `src/copy/index.js` absent and
  EM-C4a's own `src/store/editSlice.js` at 0, by a probe that IMPORTS the exported set and reads no
  `dist`; and `src/copy/en.js` measured 1260 effective lines with the planned text at 1275, `+15`
  exactly, under eslint's `Linter`."*
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` at SHA-256
  `125c693214235a81bf4f2b38506b621859a35e0325ff2ea58d335b4e0681a99d` — the LANDED SIXTH
  amendment, MEASURED with `shasum -a 256` at this member's own lane HEAD, never copied from a
  brief or a sibling (§P3.5).
  ⚠ **VERSION 1.1 CARRIED THE FIFTH'S `fdecd4268…` HERE AFTER THE SIXTH HAD LANDED.** The body
  was written to the sixth (§1 item 3a) and this header says the chair re-stamps the hash at
  placement; the placement tool re-stamped only **Status**, **Verified base** and **Last
  revalidated**, so the stale stamp is the TOOL's omission and not the body's, and EM-B1b and
  EM-B2a4 carry the same one at this placement. Measured at the lane HEAD, the landed
  `docs/implementation/preambles/EM-PREAMBLE.md` is BYTE-IDENTICAL to the kit's selected draft
  `drafts/amendment-6/EM-PREAMBLE.amended.md` (both `125c693214235a81…`), so nothing in this
  packet's body moves under the correction.
- **Last revalidated:** `e1750d7454c1dff7e81342efadd05e47719ca6aa` (the chair's stamp)
- **Depends on:**
  **LANDED and read AS REAL CODE at `efd0eb448`** — EM-A1 (`src/domain/edit/fieldDeclarations.js`) ·
  EM-A2a version 10 (`src/domain/edit/pools.js`) · EM-B1a (`src/domain/edit/operations.js`) ·
  EM-B1h · EM-B1i · EM-B2a1 (`src/domain/edit/dmLayer.js`) · EM-B3d/B3e/B3f ·
  EM-D0a (`EDITOR_HALO`/`houseBloom` in `src/components/theme.js`) ·
  EM-D0b (`canEditSettlement` at `src/store/authSlice.js:866`) ·
  EM-D0c (`editorHalo` on both primitives) · EM-D0d (`PoolField.jsx` / `FreeField.jsx`) ·
  EM-V10 (`tests/components/editFields.test.jsx` A9/A10).
  ⭐⭐ **LANDED SINCE, AND RE-READ AS REAL CODE AT `16f0bc71c`** (version 1 called these four NOT
  LANDED and marked every fact taken from them PLAUSIBLE; train EM-T12 flipped them LANDED in that
  commit) — EM-C4a version 4.2 (`PLAIN_EDIT_REFUSALS` at `src/store/editSlice.js:46`, the seven
  reasons frozen in codepoint order; `plainEditApplySpec` registered as
  `settlement.plain-edit.apply`) · EM-A2b version 7 (`POOLS['npc.role']`, reading
  `rosterField(world?.institutions, 'role', 'name')`) · EM-B1c1 version 5 (`set-npc-name` in
  `src/domain/edit/operationsNpcRename.js`, spread into `OP_TYPES` at index 11 of 15) · EM-B2a3.
  ⇒ **this member rides train EM-T13**, lane-branch pattern `em-t13-d0e-2026-09-22`, now exactly ONE
  train ahead of what has landed, beside EM-B1b version 5.
- **Collision group:** `src/copy/en.js` (EM-D0d, LANDED — the `edit` block is the file's LAST block
  and this member appends INSIDE it) · `tests/components/primitives/editorHalo.test.jsx` (EM-D0c,
  LANDED — the one-token set cure below) · `tests/components/editFields.test.jsx` is **NOT** touched
  (EM-V10's A9 and A10 are read, never edited). No waiting packet names any of this member's four
  paths: measured by `grep -l` over every `packets-waiting/*.md` and every capsule.
  ⭐ **INSIDE TRAIN EM-T13: PATH-DISJOINT FROM EM-B1b VERSION 5, ZERO INTERSECTIONS** (measured at
  the pre-proof over its capsule): its five rows are `src/domain/edit/operationsOffStage.js` CREATE ·
  `src/domain/edit/operations.js` MODIFY · `tests/domain/editOperations.test.js` TEST ·
  `tests/lint/opGuardCoverage.walker.test.js` CREATE · `scripts/mutation-coverage-manifest.json`
  REGISTER. **TWO SHARED REGISTER ROWS, both named rather than assumed:** (1)
  `tests/lint/.lighting-census-baseline.json`, a DEFERRED prediction in both members and a manifest
  row in neither, whose terminal refreeze is the chair's and sums the two deltas; (2)
  `scripts/mutation-coverage-manifest.json`, which belongs to whichever non-terminal member NAMES it
  — **EM-B1b names it and this member does not** (`tests/components` is in none of `ENFORCER_DIRS`,
  re-measured at the tip). ⛔ EM-B1b MODIFIEs `src/domain/edit/operations.js`, which this member
  imports nowhere and cites only in §12's noticed row and §13's raised rows, BY SYMBOL.
- **Commit authority:** edits only; the chair commits (§P10.5).
- **Baseline posture:** measured. ⭐ **AT VERSION 1.1 NOTHING IN THIS PACKET IS PLAUSIBLE ANY
  LONGER.** Version 1 marked every fact taken from EM-C4a v4.2, EM-A2b v7 and EM-B1c1 v5 PLAUSIBLE
  and named the real file that would confirm it; train EM-T12 landed those files and the pre-proof
  executed each confirmation at `16f0bc71c`: the seven refusal reasons and their codepoint order,
  `PLAIN_EDIT_REFUSALS`' export and its frozen shape, `npc.role` in `POOLS` and the empty answer its
  `institutions: []` read gives, `set-npc-name`'s home and its index in a fifteen-row catalogue, and
  `makeOp('set-field', …)` validating `{"ok":true,"errors":[]}` for all four NPC fields while
  `field:'salary'` is refused `payload.field is not one of the declared values`.

## §1 · Reconciled authority

1. **The owner, design §20.4** (the first door): *"ONE card (the NPC's) on a DRAFT settlement: plain
   edits through the real op boundary — `set-field` on its pooled roots …, the rename through the
   cascade, the annotation — in `CardEditorDialog`'s real generated shape, in the forge's scheme WITH
   THE HALO"*, and *"⛔ VISIBILITY IS THE OWNER'S: D0 lands behind the tier gate AND stays DARK"*.
2. **§P11.1** — opening edit mode's door is one owner-signed act and no packet flips it.
3. **`EM-PREAMBLE.md`** (SHA-256 measured above, the fifth amendment's): §P2 rows 1, 2, 11, 13, 15,
   16, 17 · §P3.5 · §P4's coupling law · §P8's STOP list · §P9(a)–(f) · §P10 · §P11.
3a. **THE SIXTH AMENDMENT AS DRAFTED AND SELECTED** (`drafts/amendment-6/EM-PREAMBLE.amended.md`,
   judgment 144; it lands as train EM-T13's first commit, ahead of this member): §P2 rows 19, 20 and
   21 · §P6's negative-assertion rule and its marker correction · §P7's two additions (the
   no-count red-first; the type measurement over a member's own `src/` paths) · §P9's ONE
   effective-line instrument · §P8's new STOP on an unpredicted §P2.21 figure. §6.6 and §7a carry
   the measurement for each.
4. **Design §2.1** (empty fields are declared and shown empty) · **§2.5a** (reversibility and the
   reopened card) · **§2.6 as amended by §20.2** (THE CANON RULE) · **§3** (the modal, the halo, the
   phone sheet, the §934.31 door, the copy registry) · **§12** (it governs every earlier section) ·
   **§14** (edit at the source; the five kinds).
5. **The chair**: judgment 109 (the off-list pool value is EM-D0e's to decide, pinned by ONE case,
   the control stays pure) · judgment 114 (EM-V10's two pins must not be undone) · `CHAIR-RULING.md`
   items 2, 3, 5, 6 · judgment 126 (`set-npc-name`, not `rename-npc`) · judgment 138 (`npc.role`
   reads the record's institutions roster).
5a. ⭐ **JUDGMENT 146 (the chair, 2026-09-22) — ALL FOUR OF VERSION 1's QUESTIONS RULED AS §13
   RECOMMENDED, and this packet's contract does not move under any of them:** **146a** the
   `src/App.jsx` lazy mount rides EM-D1 (§13 R2 ratified, §2.2(a) stands); **146b** the self-opening
   phone sheet is RATIFIED for version 1, a controlled-`open` `BottomSheet` being an
   EM-D0c-successor member and an inbox row (§13 R3); **146c** the binder of `apply` is ONE exported
   function in EM-C4a's own `src/store/editSlice.js`, carried by **EM-C4b**, so this member's
   `apply` stays injected with a `null` default until EM-D1 wires it (§13 R5); **146d** ONE op shape
   for every plain edit — `set-field` for all four fields — RATIFIED, the typed per-field ops being
   the registry's (§13 R6). R1 and R4 were raised, not asked; R4 now carries its measured answer.
6. **Current code and receipt evidence at `16f0bc71c`** (version 1 measured `efd0eb448`) — every row
   of §5, each with the command that proved it, in `EM-D0e.evidence.md`; the pre-proof's
   re-measurements are that file's sections 19 onward, appended and never rewritten.

**Resolved contradictions:**

- *"the four members you stand on … EM-A2a's `resolvePool`"* (launch body) → ⛔ **`resolvePool` does
  not exist.** `src/domain/edit/pools.js` exports exactly `POOLS`, `poolValues`, `rollFrom`
  (`node -e` over the landed module: `exports: POOLS poolValues rollFrom`). The contract below names
  the real two.
- *"dispatching EM-B1c1's `rename-npc` op"* (launch body clause 1) → ⛔ **EM-B1c1 version 5 names its
  op `set-npc-name`** (judgment 126), **and the door dispatches NEITHER**: EM-C4a's cascade branch
  keys on the DECLARATION's kind `free-cascade`, so a rename through the one adapter carries
  `set-field` like every other plain edit (§6.1 item 7; §13 R1).
- *"the tier gate `TIER_GATE.premium.editMode`"* (§P8's STOP list and the charter's EM-D0 row) →
  **the landed spelling is `TIER_GATE.premium.settlementEditor`** with the predicate
  `canEditSettlement()` (`src/store/authSlice.js:83-85, 866-868`), ruled by `CHAIR-RULING.md` item 3
  and LANDED by EM-D0b. This member spells the gate NOWHERE (§6.1 item 1) and therefore cannot
  violate either spelling; the preamble's sentence is stale and is raised, not adapted to (§13 R4).
  ⭐ **RE-MEASURED AT `16f0bc71c` FOR THE CHAIR'S AMENDMENT CORRECTION, AND THE ANSWER IS SHARPER
  THAN "STALE".** `git grep -n -E 'settlementEditor|editMode' 16f0bc71c -- src` finds `TIER_GATE`'s
  three `settlementEditor` rows at `authSlice.js:83-85` and the predicate at `:868`, and finds **no
  `TIER_GATE…editMode` literal anywhere under `src/`** — but it also finds that **`editMode` is
  itself a REAL, SHIPPED, UNRELATED flag**: `src/store/settlementSlice.js:806-808`
  (`editMode: false`, `setEditMode`, `toggleEditMode`), the Cartographer's dossier-edit mode, read
  by `SettlementDetail.jsx`, `npcComponents.jsx`, `EditableText.jsx` and the detail chrome, and
  carrying **no `TIER_GATE` row at all**. EM-C4a's own landed leaf says so in one line
  (`editSlice.js:17`: *the key is `editorMode`, never `editMode`*). ⇒ the preamble's §P8/§P11.1
  clause does not merely misspell a key; it names a DIFFERENT live feature, which is how a stale
  STOP clause convicts the wrong member. §13 R4 carries the one-line correction.
- *"`M` the lazy mount (`src/App.jsx`)"* (launch clause 1 and the partition's D0e row) → **refuted;
  §2.2.**

The implementer does not read other documents to reinterpret this packet.

## §2 · Outcome

### §2.1 What this packet delivers

**Observable result:** the NPC card's editor dialog exists as ONE generated surface — four controls
built from `declarationsFor('npc')` in the table's own authored order, wearing EM-D0c's `editorHalo`
on `PortablePopup` (desktop) and on `BottomSheet` (phone), carrying the §934.31 door it inherits from
both primitives, and turning every field change into ONE call on an injected `apply` seam whose only
lawful binding is EM-C4a's `settlement.plain-edit.apply`.

**Definition of done:** the leaf renders a control for every declared field and for no undeclared
one; it resolves its options through `poolValues` and its roller through `rollFrom` and draws no
random number of its own; it names its refusals from EM-C4a's closed set and invents none; it
imports no mutation module, no store module and no command module; `src/components/edit/**`
contributes ZERO members to `EAGER_FIRST_PAINT_MODULES`; and NOTHING a non-staff account can reach
changes, because nothing mounts this leaf at its landing.

### §2.2 ⛔⛔ THE TWO REFUTED CLAUSES, MEASURED

**(a) THE `src/App.jsx` LAZY MOUNT HAS NO LAWFUL SUBJECT AT THIS LANDING, AND IT IS NOT IN THIS
MANIFEST.** Three measurements, each executed at `efd0eb448`:

1. **Nothing can OPEN it.** The charter's **EM-D1 `EditModeShell`** row is the member that carries
   *"the mode indicator … pencils on declared cards"* — the affordance that names WHICH entry a
   door opens on. EM-D1 is unwritten and unplaced (`ls packets-waiting` names no EM-D1; `git grep -n
   EditModeShell -- src` → 0 hits). EM-C4a's transient vocabulary carries a MODE
   (`EDITOR_MODES = ['off','plain']` under `uiSlice`'s shipped pref bag) and **no target**, so an
   App-root mount could not choose a person to edit without minting a second transient key, which is
   EM-C4a's vocabulary and not this member's.
2. **Nothing can BIND its writer.** EM-C4a compiled to SHAPE A: `src/store/editSlice.js` is a LAZY
   leaf that `src/store/index.js` does not compose, so there is no store ACTION to call; its
   `runPlainEditCommand(scope, dependencies)` takes `applyPlainEdit` as an INJECTED dependency and
   the estate mints neither that binding nor a `plainEditCommandScope` builder (`grep -l` over every
   waiting packet: `applyPlainEditToDraft` appears in EM-C4a and EM-B1c1 only; `runPlainEditCommand`
   in EM-C4a only). A component may not close that gap itself: EM-C4a's own walker plants *"a
   fabricated `src/components/*.jsx` importing `makeOp` → convicted"* (its §6.3 plant **P2**), and
   §P4 forbids a store writer being reachable from `src/components/edit/**` except through the op
   boundary.
3. **The mount would cost the budget this member exists to protect, twice.** `src/App.jsx` IS a
   member of the eager first-paint closure (probe: `src/App.jsx  members: 1`), so every byte of a
   mount that can never open is a first-paint byte; and the launch brief's own cap is *"at most TWO
   new logic leaves and ONE modified file"*, which `src/copy/en.js` already spends (§2.2(b)).

⇒ **The mount rides the member that owns the pencil and the bag.** §13 R2 puts it to the chair with
its recommendation. ⭐ Dropping it also discharges the browser suite honestly rather than by
assertion: with no route, no mount and no reachable name, **no `e2e/` spec can reach this surface**
(§6.5), which is `CHAIR-RULING.md` item 6's D0c row applied to the same facts.

**(b) THE ONE MODIFIED FILE IS `src/copy/en.js`, NOT `src/App.jsx`.** Every rendered string comes
from the copy registry through `t()` (design §3; EM-D0d's landed A7; `tests/lint/
copyKeyResolution.walker.test.js` resolves every `t()` key live), so the dialog's own words are a
`MODIFY` of `src/copy/en.js`. Measured: the registry's `edit` block is the file's **last** block
(`:1825-1833` of 1,834 lines), the new keys append INSIDE it, **no line-addressed register row of
this estate sits below the insertion** (`grep -c -F 'src/copy/en.js' tests/lint/
.prose-numerics-baseline.json` → 0; `grep -c 'src/copy/en.js' tests/copy/.voice-mechanics-baseline.json`
→ 0), and the file is **not** in the eager closure (`src/copy/en.js  members: 0`).

### §2.3 WHAT THE DIALOG IS, AND WHAT IT IS NOT

It is a CONTAINER, not a writer: it owns the DM's in-flight text and the notice it shows, and nothing
else. It holds no store state, resolves no save, reads no phase of its own, mints no op and knows no
command name. Everything it cannot know arrives as a prop, which is EM-D0d's landed idiom one layer
up (`roll` injected, defaulted `null`, guarded by `typeof roll === 'function'`) and the reason that
member could land before `rollFrom` existed.

### §2.4 In scope / out of scope

**In:** the one generated dialog leaf; its four copy keys plus one refusal block; the halo pin's
one-token cure; the acceptance battery.
**Out:** ⛔ every mount (§2.2(a)) · ⛔ the pencil, the mode indicator and the Done control (EM-D1) ·
⛔ Add and Remove and the cause pool (EM-D2) · ⛔ the `'share'` control (`CHAIR-RULING.md` item 5:
EM-D2's; the NPC card declares none) · ⛔ search beside the drop-down (EM-D0d's ratified `<select>`) ·
⛔ any edit to `PoolField`, `FreeField`, `PortablePopup` or `BottomSheet` · ⛔ any second glow recipe
(the dialog passes `editorHalo` and composes nothing) · ⛔ joining `src/lib/refusalReasons.js`
(§6.2) · ⛔ any `e2e/` spec (§6.5) · ⛔ persistence, undo, re-derivation and the registry.

## §3 · Hard scope budget

| Limit | Packet budget | This member |
|---|---:|---:|
| Behavior families | 1 | **1** |
| New persisted record families | 0 or 1 | **0** |
| Named state writers | 0 or 1 | **0** — the writer is injected and lives in another member |
| Feature flags | 0 or 1 | **0** — the gate is EM-D0b's and this leaf spells it nowhere |
| User-facing surfaces | 0 or 1 | **1** (dark: nothing mounts it) |
| Direct consumers | ≤2 | **0** (EM-D1 is the first) |
| New logic-bearing production leaves | ≤2 | **1** |
| Existing logic-bearing production files modified | ≤3 | **1** (`src/copy/en.js`, a data registry) |
| Additional registration-only files | ≤3 | **0** |
| Handwritten files total | ≤12 | **6** (version 1.2: the two landed importer rosters) |
| New/changed effective production lines | ≤400 | **155** (measured) |
| Effective lines per new leaf | ≤250 | **140** (measured) |
| Delta in a shared/hot file | ≤15 | `src/copy/en.js` **+15** (measured, AT the cap) · `editorHalo.test.jsx` **+2** · version 1.2: `editDeclarations.test.js` **+1** · `editPools.test.js` **+0** (one-site widenings by ADDITION; a comment costs no effective line) |
| Acceptance cases | ≤8 | **8** |

Overrides approved before dispatch: **NONE**.

⛔ **EVERY FIGURE ABOVE IS MEASURED WITH THE ONE INSTRUMENT** (§P9, sixth amendment): eslint's own
`Linter` under `max-lines` with `skipBlankLines` and `skipComments`, run over each file AS IT STANDS
and over THE PLANNED TEXT — never `wc -l`, never a hand convention, never an inherited figure.
Executed: `src/copy/en.js` **1260 → 1275 = +15**; the planned leaf **140**; the packet total
**155 of 400**. ⭐ **THE COPY BLOCK WAS SHAPED TO THE CAP RATHER THAN THE CAP RENEGOTIATED**: the
first draft nested a `refusal: {` sub-object and measured **+17**, two over the shared-file cap of
fifteen, so the eight refusal lines were FLATTENED to `refusal<Reason>` keys at the `dialog` level
and the two brace lines disappeared. The words are unchanged; only the shape is.

⭐ **NO FILE IN THIS MANIFEST IS ON THE STANDING HOT-FILE LIST** (`PACKET_STANDARD.md`, "Hot files"):
`src/copy/en.js` carries no ceiling it approaches (1,275 effective after the edit, no `max-lines`
row), and the two test files are tests. ⛔ **The one file the partition named — `src/App.jsx` — IS
on that list, at 575 of 600 with 25 of headroom**, and it is not in this manifest (§2.2(a)): a
third, independent reason the mount rides the member that can spend that headroom on something the
product can reach.

## §4 · Sealed dispatch and preflight

Run from this member's own lane worktree, on branch `em-t13-d0e-2026-09-22`, before any edit:

```sh
npm run implementation:dispatch -- EM-D0e
```

Expected: the exact packet Markdown and the structured capsule are emitted; verified-base ancestry
and unchanged declared substrate are proven before the seal pins exact HEAD; the two non-CREATE
targets are clean, the two CREATE targets absent, and every required symbol resolves; Git-visible
foreign dirt is fingerprinted without target overlap. Any mismatch makes this packet STALE — stop
before coding.

⛔ **§P10.3's SIX MEASUREMENTS ARE THE LANE'S FIRST ACT, in plain `node` and `git`, before the seal:**
(i) branch, HEAD and verified base are the three the dispatch note names; (ii) §7's path set and the
capsule's `changeManifest` are SET-EQUAL; (iii) both CREATE targets ABSENT and both `MODIFY`/`TEST`
targets PRESENT at HEAD; (iv) every required symbol found VERBATIM; (v) the count law through
`EM-D0e.count-prover.mjs` (run from the KIT path — it is kit furniture, never a `checks` entry and
never a §7 row, §P9); (vi) every line-addressed register grepped for both modified paths.
⛔ **A LANE WORKTREE IS NEVER MOVED OR COMMITTED TO AFTER ITS DISPATCH** — a seal binds HEAD, branch
AND the manifest's bytes.

## §5 · Verified tree contract

Every row re-found BY SYMBOL at `efd0eb448`; the command for each is in `EM-D0e.evidence.md`.

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| Declaration authority | `src/domain/edit/fieldDeclarations.js` | `declarationsFor` `:130` · `isEditableCard` `:142` | returns the frozen AUTHORED array BY REFERENCE, never a copy and never sorted; the SAME shared frozen empty array for an undeclared card type, never null and never a throw. `FIELD_DECLARATIONS.npc` `:99-104` is FOUR rows — `name` `free-cascade` · `role` `pool`/`npc.role` · `status` `pool`/`npc.status` · `note` `free` | The ONE source of the dialog's field list; no hand-written list, no sort, no filter |
| Pool reader | `src/domain/edit/pools.js` | `poolValues` `:237` (was `:149` at version 1: EM-A2b v7's six new `POOLS` rows moved it; the FUNCTION BODY is byte-unchanged across the window) | frozen, de-duplicated, codepoint-ordered (`tier` excepted); ⛔ **EVERY empty answer is a FROZEN array of length 0 and the IDENTITY differs by case — stated precisely here because version 1's row over-claimed and the pre-proof measured it**: an unknown pool id (`if (!source) return EMPTY;`) and an unanswerable read (`if (!Array.isArray(raw)) return EMPTY;`) return the SHARED frozen empty array, while a KNOWN pool whose read yields no member — `poolValues('npc.role', { institutions: [] })`, which is A2's own fixture — returns a FRESH `Object.freeze([])`. Nothing in this member reads that identity: every empty test is `length === 0` | The ONE options source; the dialog never reads a catalogue |
| Seeded chooser | `src/domain/edit/pools.js` | `rollFrom` `:272` (was `:184`; same cause, body byte-unchanged) | `(poolId, world, seed, entryId, n) → string|null`; composes ONE root stream `edit-pool:…`, draws exactly once, never forks, never reads the ambient RNG or a clock | The ONE roller; bound per declaration and INJECTED into `PoolField` |
| Pool control | `src/components/edit/PoolField.jsx` | default export `:51` | props `{ declaration, options, value, onChange, seed, entryId, roll = null, disabled = false }`; renders the array's OWN order, never sorted/filtered/de-duplicated `:84-91`; empty options ⇒ empty AND disabled; `String(value ?? '')` keeps the select CONTROLLED `:68` | Mounted unchanged; EM-V10's two pins must not be undone |
| Free control | `src/components/edit/FreeField.jsx` | default export `:39` | props `{ declaration, value, onChange, disabled = false }`; branches on the declared KIND (`free` ⇒ textarea, else input); reads `declaration.maxLength` and never a literal | Mounted unchanged for `free` AND `free-cascade` |
| Desktop surface | `src/components/primitives/PortablePopup.jsx` | default export `:45` | props `{ open, title, onClose, children, testId = 'portable-popup', width = 560, editorHalo = false }`; `useDialogFocusTrap(open, onClose)` `:51`; `IconButton … label={`Close ${title}`}` `:117`; `boxShadow: 'none'` `:98` | The desktop door, CONTROLLED by `open`; `editorHalo` passed, nothing else |
| Phone surface | `src/components/primitives/BottomSheet.jsx` | default export `:42` | ⛔ **NOT a controlled dialog**: it owns `open` as internal `useState(false)` `:53` and renders its OWN trigger `Button` `:75-84`; `useDialogFocusTrap(open, close)` `:64`; `aria-label="Close"` `:152`; `boxShadow: ELEV[3]` `:124` stays (`CHAIR-RULING.md` item 4) | The phone door, SELF-OPENING from its own trigger (§6.1 item 9; §13 R3) |
| The gate | `src/store/authSlice.js` | `canEditSettlement` `:866-868` | `TIER_GATE[tier]?.settlementEditor === true && isStaffRole(role)`; `TIER_GATE` `:83-85` carries `settlementEditor` false/false/true | ⛔ **READ BY NO LINE OF THIS MEMBER.** The gate is the MOUNT's conjunct and the mount is not in this manifest; the leaf therefore cannot open a door the gate closes |
| Copy | `src/copy/index.js` · `src/copy/en.js` | `t` `:124` · the `edit` block `:1825-1833` | `t(key, vars)` interpolates and returns the KEY itself for a missing key; the `edit` block is the registry's LAST block and already carries EM-D0d's five `edit.field.*` keys | Every rendered string; the new keys append inside `edit` |
| Notice shape | `src/components/generate/ClerkNote.jsx` | `ClerkNote` `:28` | `({ rubric, children, role, actions, style })`; `role` passes through so alert semantics survive | The refusal and canon notices, with THIS member's own words and no register join (§6.2) |
| The halo pin | `tests/components/primitives/editorHalo.test.jsx` | `filesNaming('editorHalo')` `:180` | an EXACT set, both directions: `[SHEET_REL, POPUP_REL].sort()`, where `filesNaming` reads every `src/` leaf's TEXT `:97-100` | ⛔ The one-token cure of §6.3; the arm is widened by the member whose landing needs it, never weakened |
| ⭐ The mutation-path gate (NEW at version 1.1; EM-C4a's, LANDED inside the EM-T12 window) | `tests/lint/editMutationPath.walker.test.js` | `mutationPathOffenders` · `EDIT_PATH` · `WATCHED` | source-scans **every** `.js`/`.jsx` under `src/` for a STATIC named import of `makeOp`, `validateOp` or `OP_TYPES` from `src/domain/edit/operations.js`, or `applyEdit` from `src/domain/edit/dmLayer.js`, and convicts it BY NAME unless the file is one of the three declared `EDIT_PATH` members (`src/application/commands/adapters/plainEditApply.js`, `src/application/commands/plainEditRuntime.js`, `src/store/editSlice.js`) or a declared read-only consumer (that set is EMPTY today). ⛔ A DYNAMIC `await import(...)` is deliberately NOT an edge — it is the lazy boundary, and the walker's P4 case is its control | ⛔ **THIS LEAF IS IN THE SCANNED SET AND IN NEITHER DECLARED SET**, so §6.1 item 1's import ban is enforced by a LANDED walker and not only by this contract. It is `checks` entry 3's newest file. EM-C4a's §6.3 plant **P2** — *a fabricated `src/components/*.jsx` importing `makeOp` → convicted* — is this walker's own `A4 — P1 and P2` case, executable at the tip |
| Test precedent | `tests/components/editFields.test.jsx` | `describe(…)` `:102`, `it('A1: …')` `:105` | jsdom environment; ONE literal `describe`; straight-line literal `it`s; positive set-equalities in place of bare negatives; sources read with `readFileSync` for the scans | Copy this proof shape exactly |

**Forbidden alternatives:** no second glow recipe, no new hex, no thirteenth motion behavior; no
second options source, no second roller, no random draw and no clock in the leaf; no op minted in a
component and no import of `src/domain/edit/operations.js`, `src/domain/edit/dmLayer.js`,
`src/store/**` or `src/application/commands/**`; no hand-written field list; no second refusal
vocabulary; no file outside this manifest.

## §6 · Exact contracts

### §6.1 `src/components/edit/CardEditorDialog.jsx` — THE DOOR

```js
/** @typedef {import('../../domain/edit/types.js').FieldDeclaration} FieldDeclaration */

/**
 * One edit, in the coordinates the adapter's caller needs and in NO other shape.
 * @typedef {{ cardType: string, entityId: string, field: string, value: string }} CardEdit
 */

/**
 * The injected seam's answer. `reason` is a member of EM-C4a's closed set, or any other
 * word the executor may return; an unknown word takes the fallback line and invents nothing.
 * @typedef {{ ok: true } | { ok: false, reason?: string }} ApplyResult
 */

/** The closed refusal set, mapped to copy. FROZEN. Keys are EM-C4a's seven, in codepoint order. */
export const REFUSAL_COPY_KEYS;
// exactly Object.freeze({
//   canon_locked:       'edit.dialog.refusalCanonLocked',
//   invalid_op:         'edit.dialog.refusalInvalidOp',
//   no_save:            'edit.dialog.refusalNoSave',
//   not_a_draft_field:  'edit.dialog.refusalNotADraftField',
//   rename_not_applied: 'edit.dialog.refusalRenameNotApplied',
//   undeclared_field:   'edit.dialog.refusalUndeclaredField',
//   unknown_target:     'edit.dialog.refusalUnknownTarget',
// })

/** The one line any other word takes — the executor's seven among them. */
export const REFUSAL_FALLBACK_KEY;   // exactly 'edit.dialog.refusalUnknown'

/**
 * @param {{ open: boolean, cardType: string, entityId: string,
 *   values: Readonly<Record<string, string>>, world: object|null, seed: string,
 *   phase: 'draft'|'canon',
 *   apply?: ((edit: CardEdit) => Promise<ApplyResult>)|null,
 *   onClose: () => void }} props
 */
export default function CardEditorDialog({ open, cardType, entityId, values, world, seed,
  phase, apply = null, onClose });
```

**The ordered algorithm, exactly.**

1. ⛔ **THE LEAF SPELLS NO GATE AND READS NO STORE.** It imports nothing from `src/store/**`,
   `src/application/commands/**`, `src/domain/edit/operations.js` or `src/domain/edit/dmLayer.js`,
   and it names neither `TIER_GATE`, `settlementEditor`, `canEditSettlement`, `editMode` nor
   `editorMode`. ⭐⭐ **AT VERSION 1.1 THIS CLAUSE IS ENFORCED BY A LANDED WALKER, NOT ONLY BY THIS
   CONTRACT.** EM-C4a's `tests/lint/editMutationPath.walker.test.js` landed inside the EM-T12 window
   and source-scans EVERY `.js`/`.jsx` under `src/` for a STATIC named import of `makeOp`,
   `validateOp`, `OP_TYPES` or `applyEdit`, convicting it by name unless the file is one of its
   three declared `EDIT_PATH` members — which this leaf is not. ⇒ **ONE such import reds that
   walker's live-tree arm at this member's own build**, which is why it is a `checks` entry and a §5
   row. ⚠ A DYNAMIC `await import(...)` is deliberately not an edge there; this leaf uses neither
   form, so the point is moot and stays moot. The DARK rule is the mount's conjunct (§2.2(a)); this leaf cannot open a door the
   gate closes because nothing hands it a door to open.
2. `if (!open) return null;` — **nothing at all**, not an empty wrapper.
3. `const declarations = isEditableCard(cardType) ? declarationsFor(cardType) : NO_ROWS;`
   `if (declarations.length === 0) return null;` — a card the table does not declare has no pencil
   (design §2.1). `NO_ROWS` is the leaf's own shared frozen empty array, never a fresh `[]`.
4. `const readOnly = phase !== 'draft';` — ⛔ ONE derivation, spelled ONCE. The first door is a
   DRAFT surface (design §20.4); on canon the record's fields are not this door's to write
   (EM-C4a §6.1 step 2 refuses `canon_locked` for `pool` AND `free-cascade`), so the dialog SAYS SO
   rather than offering a control that will refuse — EM-C4a's own instruction to this member.
5. **THE DRAFT TEXT.** One `useState` holding a plain object of edited values, initialised `{}` and
   keyed by field. `valueOf(d) = Object.hasOwn(draft, d.field) ? draft[d.field] : String(values?.[d.field] ?? '')`
   — `Object.hasOwn`, never truthiness, so a field the DM has CLEARED reads as `''` and not as the
   record's old value. A control's `onChange` writes `draft[field]` and nothing else; **no `apply`
   call happens on a keystroke.**
6. **THE CONTROLS, GENERATED.** `declarations.map(...)` in the array's own AUTHORED order:
   - `kind === 'pool'` → `<PoolField declaration options={optionsFor(d)} value={valueOf(d)}
     onChange={(next) => setDraft(field, next)} seed={seed} entryId={entityId}
     roll={(s, e, n) => rollFrom(d.pool, world, s, e, n)} disabled={readOnly} />`
   - `kind === 'free' || kind === 'free-cascade'` → `<FreeField declaration value={valueOf(d)}
     onChange={…} disabled={readOnly} />`
   - **any other kind → NO CONTROL AT ALL**, and no label, no placeholder and no note. `'share'` is
     EM-D2's (`CHAIR-RULING.md` item 5) and the NPC card declares none; the faction card's one
     `share` row is the landed negative control A1 reads.
   ⛔ The `key` is `d.field`, which the declaration table makes unique per card.
7. ⛔ **`optionsFor` AND THE OFF-LIST STORED VALUE — the cure judgment 109 assigned to this member.**
   ```
   const pool = poolValues(d.pool, world ?? null);            // frozen, the producer's own order
   const held = valueOf(d);
   optionsFor(d) = held !== '' && !pool.includes(held) ? [held, ...pool] : pool;
   ```
   The stored value is PREPENDED, never sorted in and never dropped: `PoolField` renders the array's
   own order, so the pool's order survives untouched and the record's truth is visible and
   re-selectable. **A value the pool no longer offers is not an empty field** (design §2.1 governs
   the EMPTY case, not the unknown one), and hiding it would silently misreport what the save holds
   — the provenance rule of design §14. ⛔ The control stays PURE: `PoolField` is not edited.
8. ⛔ **THE SEEDED ROLL IS THE PRODUCER'S.** The `roll` prop is bound per declaration to
   `rollFrom(d.pool, world, …)` and the dialog draws no random number, reads no clock and holds no
   roll counter of its own — `PoolField` owns the index. An empty pool answers `null` and the
   control shows its own `rollUnavailable` line; EM-V10's pins are untouched, because the prop is
   supplied (optional either way) and every value handed to the select is a string.
9. **THE TWO DOORS.** `const mobile = useIsMobile();`
   - **desktop** → `<PortablePopup open title={t('edit.dialog.title')} onClose
     testId="card-editor-dialog" editorHalo>{body}</PortablePopup>` — CONTROLLED by this leaf's
     `open`, closed by Escape, the scrim and the labelled corner control, all inherited.
   - **phone** → `<BottomSheet title={t('edit.dialog.title')} triggerLabel={t('edit.dialog.open')}
     editorHalo onClose={onClose}>{body}</BottomSheet>` — ⛔ **MEASURED: the sheet owns its own
     `open` and renders its own trigger** (`BottomSheet.jsx:53, 75-84`), so on the phone this leaf's
     `open` decides whether the SHEET AND ITS TRIGGER exist, and the sheet's own trigger raises it.
     The leaf adds no second open state and edits no shared primitive (§13 R3).
   - `body` is identical in both branches: the notice, then the generated controls in order, then
     the Save control.
10. **SAVE — ONE CALL PER CHANGED FIELD, IN THE AUTHORED ORDER, AWAITED, STOPPING AT THE FIRST
    REFUSAL.** The control is `<Button variant="primary" disabled={readOnly || !dispatchable ||
    busy}>{t('edit.dialog.save')}</Button>` where `dispatchable = typeof apply === 'function'` —
    ⛔ the SAME predicate shape EM-D0d's landed `roll` uses, checked **twice**: once where the
    button's disabled state is computed and again as the handler's first statement, so an absent
    seam has no call path even if a caller re-enables the button through a spread prop.
    On click, for each declaration in AUTHORED order whose `valueOf(d) !== String(values?.[d.field] ?? '')`:
    ```
    const result = await apply({ cardType, entityId, field: d.field, value: valueOf(d) });
    if (!result || result.ok !== true) { notice = refusalNotice(d, result?.reason); return; }
    ```
    then `onClose()` when every changed field answered `ok:true`. ⛔ **An unchanged field is never
    dispatched.** ⛔ **The loop stops at the first refusal and the fields already applied STAY
    applied** — EM-C4a's atomicity is `single-target` and the estate has no multi-field transaction,
    so the notice NAMES THE FIELD (`declaration.label`) beside the reason rather than implying the
    card rolled back.
11. **THE NOTICE.** `<ClerkNote role="alert" rubric={t('edit.dialog.refusalRubric')}>` carrying
    `t(REFUSAL_COPY_KEYS[reason] ?? REFUSAL_FALLBACK_KEY, { field: d.label })`. On canon the same
    component carries `t('edit.dialog.canonNotice')` with no rubric change. ⛔ **No word is
    invented**: every reason this member can be handed is either one of EM-C4a's seven or takes the
    one declared fallback line.
12. ⛔ **A THROWN SEAM IS A REFUSAL THE DM SEES.** `apply` is a foreign function; the `await` sits in
    a `try` whose `catch` shows the fallback line and clears `busy`. An unhandled rejection would
    leave the door silent, which is the class `refusalNoticeCoverage.walker.test.js` exists against.
13. ⛔ **NO EFFECT, NO TIMER, NO PORTAL, NO STORAGE.** The leaf runs no `useEffect`, sets no timeout,
    reads no `window`, `localStorage`, `document` or URL, and mounts no portal. Two renders with the
    same props reach the same markup.

### §6.2 `src/copy/en.js` — THE THIRTEEN KEYS, SPELLED VERBATIM (judgment 93)

Appended INSIDE the registry's existing last block, `edit:` (`:1825`), beside EM-D0d's `field:`
sub-block and never renaming it. ⛔ A build lane builds exactly these words and may invent none.

```js
  edit: {
    field: { /* EM-D0d's five keys, BYTE-UNMOVED */ },
    dialog: {
      title:                   'Editor',
      open:                    'Edit this card',
      save:                    'Save',
      canonNotice:             'This settlement is canon, so its fields are read-only at this door.',
      refusalRubric:           'The edit did not apply',
      refusalCanonLocked:      'A canon settlement takes no plain edit, so {field} is unchanged.',
      refusalInvalidOp:        'This build does not accept that edit, so {field} is unchanged.',
      refusalNoSave:           'This settlement is not the open save, so {field} is unchanged.',
      refusalNotADraftField:   '{field} is not a field this door edits yet.',
      refusalRenameNotApplied: 'The new name was not taken, so {field} is unchanged.',
      refusalUndeclaredField:  '{field} is not declared for this card.',
      refusalUnknownTarget:    'The record this card names could not be found, so {field} is unchanged.',
      refusalUnknown:          'The edit did not apply, so {field} is unchanged.',
    },
  },
```

⭐ **THIRTEEN STRINGS IN FIFTEEN EFFECTIVE LINES — FLAT, NOT NESTED, AND THE SHAPE IS THE BUDGET.**
A `refusal: {` sub-object measured **+17** under the one effective-line instrument, two over the
standard's shared-file cap of fifteen; flattening the eight reasons to `refusal<Reason>` keys at the
`dialog` level removes exactly the two brace lines and lands at **+15**, with not one word changed.
⛔ **NO EM DASH AND NO EXCLAMATION POINT, MEASURED BY THE WALKER'S OWN PREDICATE** (§P2.19, sixth
amendment): `tests/copy/voiceMechanics.test.js:144`'s `v.includes('—') || v.includes('!')` run in
plain `node` over all thirteen planned strings AND over the leaf's fourteen non-copy literals —
**27 literals measured, 0 em dashes, 0 exclamation points**, with a planted control proving the
predicate fires. Tier 1 is a HARD ZERO over the registries and this block meets it by measurement,
not by assertion. ⛔ **NO FIGURE IS RENDERED**, so `tests/lint/proseNumerics.test.js` gains no row
(§P2.7). ⭐ The interpolation `{field}` is `t()`'s own shipped idiom (`copy/index.js:124`), the same
pair-passing EM-D0d's `edit.field.limit` uses.

⛔ **THIS MEMBER DOES NOT JOIN `src/lib/refusalReasons.js`, AND THAT IS MEASURED, NOT PREFERRED.**
That register is the GENERATION lane's refusal vocabulary (ODQ §934.24(c)); its walker's arm 3
derives its denominator from *"a component that binds `s.generateSettlement`"*
(`refusalNoticeCoverage.walker.test.js:105`, `GENERATION_ENTRY_RE`), which this leaf does not and
will not, and its arm 2 demands every registered reason be RAISED somewhere in `src/` — the editor's
reasons are raised in the STORE, by EM-C4a's writer, not here. Registering them would put a dark
surface's words into a shipped public register and would owe a raise site this member may not write.
The leaf reuses the register's *shape* — `ClerkNote role="alert"` with a rubric and a body, the
exact composition `RefusalNotice.jsx:92-96` uses — and none of its vocabulary.

### §6.3 `tests/components/primitives/editorHalo.test.jsx` — THE ONE-TOKEN SET CURE

EM-D0c's A6 pins an EXACT set in both directions (`:180`):

```js
expect(filesNaming('editorHalo')).toEqual([SHEET_REL, POPUP_REL].sort());
```

`filesNaming` reads every `src/` leaf's TEXT (`:97-100`), so the moment this member's leaf names the
prop that arm reds. The cure is TWO lines and no more, in the member whose landing needs it
(§P2.12's sweep; judgment 110's law that the arm is widened under its own seal, never by a cure lane
and never by the chair at a terminal):

```js
const DIALOG_REL = 'src/components/edit/CardEditorDialog.jsx';   // beside POPUP_REL/SHEET_REL at :30-31
…
expect(filesNaming('editorHalo')).toEqual([SHEET_REL, POPUP_REL, DIALOG_REL].sort());
```

⛔ **NO OTHER ARM MOVES, NO ARM IS WEAKENED, NO `it` IS ADDED AND NO TITLE IS MINTED.** The two
sibling exact sets in the same case — `filesNaming('EDITOR_GROUND')` and `filesNaming('houseBloom')`
— stay BYTE-UNMOVED, and they are the proof from the other side that this leaf composes no glow of
its own (it passes `editorHalo` and imports neither `EDITOR_GROUND`, `GOLD`, `PARCH_100` nor
`houseBloom`). ⚠ The new `const` inserts ONE line above the rest of the file; the register sweep
found **no line-addressed row** for this path (`git grep -c -F 'tests/components/primitives/
editorHalo.test.jsx' -- tests/lint scripts` → 0), so no re-address is owed (§P2.16: `none found`).

### §6.3a `tests/domain/editDeclarations.test.js` — EM-A1's DORMANCY ROSTER, WIDENED BY ONE ROW (version 1.2; judgment 148, ruled 171)

EM-A1's landed dormancy arm holds the importer roster of `src/domain/edit/fieldDeclarations.js`
EXACT in both directions. Its corpus is every `.js`/`.jsx` file under `src/`
(`tests/domain/editDeclarations.test.js :: walkSources`), read through the estate's ONE shared
comment strip (`tests/helpers/codeOnlySource.js :: commentsOnly`, TOOL-32) by
`tests/domain/editDeclarations.test.js :: dormancyImporters`, so a JSDoc typedef is NOT an importer
and a runtime `import … from` is. §7's CREATE row orders this leaf to import `declarationsFor` and
`isEditableCard` from that module, so this member is a new RUNTIME importer and the set-equality
reds. Measured with the arm's own matcher at the lane HEAD: **2 rows before, 3 after.**

`tests/domain/editDeclarations.test.js :: EXPECTED_IMPORTERS` gains EXACTLY ONE row (the assertion
sorts both sides, so codepoint position is readability and not law):

```js
    const EXPECTED_IMPORTERS = [
      // ⭐ EM-D0e WIDENS THIS ARM IN PLACE, BY ADDITION AND NEVER BY DELETION. The third entry is
      // the first editor door, and it is LAZY in the only sense this arm measures: NOTHING MOUNTS
      // IT — `src/App.jsx` is out of its manifest (§2.2(a), judgment 146a) and no `e2e/` spec can
      // reach it — so it is composed by no eager slice, contributes 0 members to
      // EAGER_FIRST_PAINT_MODULES, and the +0 B price this arm defends is unmoved. Measured by
      // this member's own `checks` entry 5, the first-paint membership probe, which imports the
      // exported set and reads no `dist`.
      'src/components/edit/CardEditorDialog.jsx imports src/domain/edit/fieldDeclarations.js',
      'src/domain/edit/operations.js imports src/domain/edit/fieldDeclarations.js',
      'src/store/editSlice.js imports src/domain/edit/fieldDeclarations.js',
    ];
```

⛔ **THE ARM'S SECOND-ORDER SCAN IS READ WHOLE AND STAYS BYTE-UNMOVED, AND THAT IS MEASURED RATHER
THAN ASSUMED** (the consequence judgment 150 requires a member to state).
`tests/domain/editDeclarations.test.js :: B1A_LEAVES` and
`tests/domain/editDeclarations.test.js :: EXPECTED_SECOND_ORDER` are a SECOND exact roster, over
EM-B1a's `src/domain/edit/operations.js` and `src/domain/edit/worldConditions.js`. §6.1 item 1
forbids this leaf from importing either, §7's CREATE row lists neither, and A8's source scan pins
the absence with a planted control ⇒ the roster stays at its landed ONE row,
`src/store/editSlice.js imports src/domain/edit/operations.js`, and the world-condition leaf still
has NO importer under `src/` at all. Re-measured with the same matcher: **1 row before, 1 row
after.** ⛔ No assertion is weakened, no arm is added, and the arm's TITLE is NOT re-worded: it
names no count and reads exactly as true after this widening as before it, so the lighting census
sees no rename.

### §6.3b `tests/domain/editPools.test.js` — EM-A2a's DARKNESS ROSTER, WIDENED TO ONE ROW (version 1.2; judgment 148, ruled 171)

`tests/domain/editPools.test.js :: A6 the no-draw law, and the golden structural proof` closes with
a darkness roster over `src/domain/edit/pools.js`: every `.js` and `.jsx` file under `src/`
(`tests/domain/editPools.test.js :: sourceFiles`) whose text matches
`tests/domain/editPools.test.js :: POOL_LEAF_SPECIFIER`, asserted `[]`. §7's CREATE row orders this
leaf to import `poolValues` and `rollFrom` from that module ⇒ ⛔ **this member is the FIRST importer
of `src/domain/edit/pools.js` under `src/`**, the exact case judgment 148 governs. Measured with the
arm's own matcher at the lane HEAD: **`[]` before, one row after.**

```js
    expect(sources.filter(({ text }) => POOL_LEAF_SPECIFIER.test(text)).map(({ rel }) => rel),
      // ⭐ EM-D0e is the pool leaf's FIRST and ONLY runtime importer under src/, and it is LAZY in
      // the sense this arm measures: nothing mounts the dialog (§2.2(a)), so the leaf still enters
      // no eager closure and no golden can move behind this arm's back.
      'the pool leaf has EXACTLY ONE importer under src/, EM-D0e\'s editor door, which nothing'
      + ' mounts, so no bundle closure gains it and no golden can move')
      .toEqual(['src/components/edit/CardEditorDialog.jsx']);
```

⛔ **THE MATCHER'S LIVENESS PROBE AND THE PLANTED NEGATIVE CONTROL BESIDE IT ARE UNTOUCHED.**
`tests/domain/editPools.test.js :: TYPES_LEAF_SPECIFIER` — the same shape aimed at a sibling edit
leaf that IS imported — measured **2** hits at the lane HEAD, so the matcher is proved live from the
other side and the one-row roster above cannot read as agreement; and A7's import fence
(`tests/domain/editPools.test.js :: FENCE` and `:: FORBIDDEN`) reads the POOL LEAF'S OWN import
list, which this member does not touch, so it cannot move. ⛔ No assertion is weakened, no arm is
added, and A6's title is NOT re-worded: it names no roster, so no title moves.

### §6.4 Determinism, lifecycle, receipts

- **Hash / fork key:** NONE. This leaf draws no random number, mints no id and reads no clock. Its
  only source of a value is the injected roller, which composes ONE root stream inside `rollFrom`.
- **Ordering:** the declaration table's AUTHORED order, everywhere — the controls, the Save loop and
  the notice. `poolValues`' codepoint order is passed through untouched; the off-list value is
  PREPENDED and nothing is sorted by this leaf.
- **Absence vs empty vs null:** `values` absent, `null` and `undefined` all read as `''` through ONE
  normalization, so every control stays CONTROLLED (EM-V10's pin); an empty POOL renders empty AND
  DISABLED (`PoolField`'s own arm) while an empty VALUE renders empty AND ENABLED; an undeclared
  card type renders `null` and never an empty shell; `apply` absent renders the Save control
  DISABLED and never a throw.
- **Lifecycle, every path:** *create* — the first accepted edit is the injected seam's business, not
  this leaf's. *read* — `values` is a prop; the leaf reads no record and no layer. *persist* — ⛔ NOT
  THIS MEMBER (EM-B3). *regenerate / re-derive* — ⛔ NOT THIS MEMBER (EM-B2a4; EM-C4a §6.4's seam is
  a typed no-op). *undo* — ⛔ **the dialog mints NO second undo** (design §2.5a): the adapter joins
  the estate's existing per-save edit ring through the declared writer's own
  `persistActiveSaveEdit`, and a reopened card shows its values because `values` is recomposed by
  the caller, not cached here. *import / fork / gallery / export* — the leaf holds no persisted key
  and cannot travel. *migrate* — none.
- **Receipts:** the notice only. No chronicle line (design §2.6: a plain edit on a draft writes
  none), no decree entry, no analytics event, no console write.
- **Golden posture: UNCHANGED.** `tests/property/generatorGoldenMaster.test.js` and
  `tests/property/dossierProseManifest.test.js` do not move; generation is not touched (§P3.1).

### §6.5 §P2.15 · THE BROWSER SUITE — NAMED, AND THE ANSWER IS NONE

This member touches `src/components/**`, so the rule binds and its answer is computed, not assumed.
Every name, test id and label the member adds, searched on its own at the read tip
(`git grep -n -F '<name>' -- e2e`):

| the name this member adds | `e2e/` hits |
|---|---:|
| `card-editor-dialog` (the `testId`) | **0** |
| `CardEditorDialog` | **0** |
| `Editor` (the dialog's accessible name, and `Close Editor`) | **0** |
| `Edit this card` (the phone trigger) | **0** |
| `edit.dialog` (every new copy key) | **0** |

⇒ **NO `e2e/` SPEC GOVERNS THIS SURFACE AND THIS MEMBER NAMES NONE.** It adds no route, and with the
mount out of the manifest (§2.2(a)) no spec can reach the leaf at all — `CHAIR-RULING.md` item 6's
D0c row, on the same facts. **SLOT:** the first editor spec belongs to **EM-D1**, the member that
mounts the door; it is recorded here as deliberately deferred with that trigger, not as a bug to
re-find.
⚠ **ONE MEASURED COLLISION IS HANDED FORWARD RATHER THAN CURED HERE.**
`e2e/flow-b-auth-credits-ai.spec.js:203` locates `getByRole('button', { name: /Save to library|Save
settlement|Save\b/i }).first()`. The dialog's Save control is the design's own word (§3: *"Save, Add
or Remove"*), so the word stays; but the member that mounts this dialog on a route inherits a
locator that can match it. ⛔ The spec is never weakened; the cure at that landing is the mount's
own scoping. Named for EM-D1's compile, with its exact line.

### §6.6 · What the SIXTH AMENDMENT asks a member to say in §6

**§P2.20 — THIS MEMBER REGISTERS NO APPLICATION COMMAND.** It adds no row to
`src/application/commands/standardCommandRegistry.js`, so
`tests/application/commands/commandRegistry.test.js`'s reviewed-capability pin cannot move and is
NOT a `TEST` row here; there is no review sentence to write. The command this door's edits travel on
— `settlement.plain-edit.apply` — is REGISTERED BY EM-C4a, which carries that pin as its own version
4.2 `TEST` row with its own review sentence. ⛔ And this member performs no store dispatch at all:
it calls ONE injected function prop, so `tests/store/deadOperationRatchet.test.js`'s premise — no
dispatch by computed name — is untouched, and there is no writer table for §6 to spell.

**§P6 — THIS MEMBER ADDS NO NEGATIVE ASSERTION OF THE ANCHOR WALKER'S SHAPES, and that is a
contract, not an accident.** Every would-be negative is spelled as a POSITIVE equality: A1's share
row is `expect(labels).toEqual([...])` over the rendered labels, not a `not.toContain`; A7's dark
arm is `expect(container.innerHTML).toBe('')`; A6's stop is `expect(calls.length).toBe(1)`; A3's
purity arm and A8's source scan are set equalities against the empty set with their scanners driven
over a source that DOES contain a member. ⇒ no `// anchored:` marker is owed anywhere in this
member. ⛔ **IF the build lane finds it cannot express an arm positively, the marker goes on the line
IMMEDIATELY ABOVE THE `expect` ITSELF — never above a wrapped `expect`'s first line** (the sixth
amendment's own correction, from EM-B1c1 version 5's build red), and the anchor walker must be green
before the arm is claimed.

**§P2.21 — EACH INSTRUMENT'S DELTA IS `+0`, AND THE PRE-PROOF CORRECTED HOW ONE OF THEM IS
DISPOSITIONED.** ⛔⛔ **VERSION 1 SAID ALL THREE "DO NOT REACH THIS MEMBER" BECAUSE THEIR ROOTS ARE
`src/domain` AND `src/generators`. FOR (a) THAT IS REFUTED BY MEASUREMENT** — the entropy-root
census walks `src/` WHOLE — **and the correction matters because §P8's new STOP fires on an
unpredicted figure from exactly these three.** The delta is unchanged at `+0`; the REASON is
content, not root. Each instrument's own logic, run over the planned leaf at `16f0bc71c`:
**(a) the entropy-root census — IT DOES REACH THIS LEAF, AND THE DELTA IS `+0` BY CONTENT.** Its
corpus is `const SRC = join(ROOT, 'src')` (`:53`) walked whole into `ALL_FILES` (`:131`), so
`src/components/edit/CardEditorDialog.jsx` is scanned. It carries **TWO** `createPRNG` site counts,
not one: `count((f) => f.startsWith('src/domain/'), /createPRNG\(/)` → **36** (`:824`), which this
leaf cannot reach, and `count(() => true, /createPRNG\(/)` → **47** (`:831`) over the whole of
`src/`, **which it can**; and a third whole-`src/` count,
`/function (fnv1a32|hash01|hashUnit|hash32|fnv1a)/` → **31** (`:967`). The planned leaf spells
`createPRNG(` **0 times**, defines **none** of those five names, and reads none of `WORLD_ROOTS`
(`:57`: `worldState · startingWorldState · state · ws · raw · base` — the leaf's read bag is a prop
named `world`); the one stream its roll affordance reaches is composed INSIDE `rollFrom`, in
`src/domain/edit/pools.js`, and is ALREADY counted inside both the 36 and the 47. ⇒ all three arms
predicted **UNMOVED**, no `TEST` row owed — and if any of them reds with a figure this member did
not predict, §P8's STOP fires and the arm's own message decides.
**(b) the ruin-filter roster — the root GENUINELY does not reach it, and the root is measured.**
That walker's corpus is `const DOMAIN = join(ROOT, 'src', 'domain')` (`:74`), read by
`READER_RE = /\.institutions\b/` (`:77`); this leaf is outside that root, and its own code names
`.institutions` nowhere (only A2's TEST fixture spells `world = { institutions: [] }`, and tests are
not scanned). ⇒ the discovery set is UNMOVED and no disposition is owed. ⭐ RE-READ AT THE TIP:
EM-A2b v7 landed its OWN row in `RUIN_AGNOSTIC_EXEMPT` for `src/domain/edit/pools.js` (`:98`, the
name-lookup class, judgment 138) and moved the discovery count 93 → 94 under its own seal, so the
producer this member READS is already dispositioned and this member inherits nothing.
**(c) the goods roster — dispositioned by IMPORT, which holds whatever the root is.** The ST-2
surface law names `GOODS_IDENTITY_TABLES = ['src/data/resourceData.js']`
(`tests/build/vendorPdfLazy.test.js:1207`) beside the machine-read roster in
`src/domain/goods.schema.js`; the planned leaf imports **none** of them, nor any of their kin. ⇒ no
ST-2 row owed, and that arm's parse cannot red on this member's account.
⚠ **§P8's new STOP applies in force:** if any of these three reds at the build with a figure this
member did not predict, the arm's own message decides and the member STOPS for the chair. A figure
adopted by hand is the failure.

## §7 · Exact change manifest

⚠ Generated FROM the capsule (`EM-D0e.manifest.json`) and proved SET-EQUAL to it on paths and on
actions (§P9(d)); the proof line is in §12.

| action | path | symbols | effective lines | the exact instruction |
|---|---|---|---|---|
| `CREATE` | `src/components/edit/CardEditorDialog.jsx` | `CardEditorDialog` (default), `REFUSAL_COPY_KEYS`, `REFUSAL_FALLBACK_KEY` | ≤250 (**140, measured**) | §6.1's exact algorithm. Imports EXACTLY: `useState` from `react`; `declarationsFor`, `isEditableCard` from `../../domain/edit/fieldDeclarations.js`; `poolValues`, `rollFrom` from `../../domain/edit/pools.js`; `t` from `../../copy/index.js`; `useIsMobile` from `../../hooks/useIsMobile.js`; `PoolField`, `FreeField` from `./`; `PortablePopup`, `BottomSheet`, `Button` from `../primitives/`; `ClerkNote` from `../generate/ClerkNote.jsx`; tokens from `../theme.js` and `chromeFontSize` from `../../design/proseScale.js` if the body needs spacing. ⛔ **IMPORT NOTHING from `src/store/**`, `src/application/commands/**`, `src/domain/edit/operations.js` or `src/domain/edit/dmLayer.js`**; mint no op; spell no gate; draw no random number; read no clock, no `window`, no storage and no URL. |
| `MODIFY` | `src/copy/en.js` | `edit.dialog.*` | **+15 effective (measured), 13 strings, +0 logic** | §6.2's THIRTEEN strings VERBATIM, FLAT (no `refusal:` sub-object), appended as a `dialog:` sub-block INSIDE the existing `edit:` block (`:1825`), beside `field:` and never renaming it. EM-D0d's five keys stay BYTE-UNMOVED. The delta is measured with eslint's `Linter` (`max-lines`, `skipBlankLines` + `skipComments`) over the file as it stands (1260) and over the planned text (1275) — the one instrument (§P9), never `wc -l`. |
| `TEST` | `tests/components/primitives/editorHalo.test.jsx` | A6's `filesNaming('editorHalo')` set | **+2** | §6.3's one-token cure: one `const DIALOG_REL` beside `:30-31` and the dialog's path added to A6's expected array. ⛔ No arm weakened, no `it` added, no title minted, the two sibling exact sets byte-unmoved. |
| `CREATE` | `tests/components/cardEditorDialog.test.jsx` | A1–A8 | — | §9's EIGHT cases as EIGHT literal `it`s under ONE literal `describe`; `/** @vitest-environment jsdom */` first line, at `tests/components/editFields.test.jsx`'s exact shape. ⛔ No `.each`, no loop, no conditional and no nested `describe` (§P3.4); ⛔ no binding named `it`, `test` or `describe` anywhere in the file (26 of the estate's 384 parked files park for that alone). |
| `TEST` | `tests/domain/editDeclarations.test.js` | the DORMANCY arm's `EXPECTED_IMPORTERS` roster of `src/domain/edit/fieldDeclarations.js` (no case; no arm added; no title added) | **+1** | ⭐ **VERSION 1.2 (judgment 148; ruled 171).** §6.3a exactly: `EXPECTED_IMPORTERS` gains EXACTLY ONE row, `src/components/edit/CardEditorDialog.jsx imports src/domain/edit/fieldDeclarations.js`, with ONE comment naming EM-D0e and the door's laziness (nothing mounts it, §2.2(a); 0 members of `EAGER_FIRST_PAINT_MODULES`, proved by `checks` entry 5). ⛔ The arm's SECOND-ORDER roster (`tests/domain/editDeclarations.test.js :: EXPECTED_SECOND_ORDER`) is read WHOLE and stays BYTE-UNMOVED — this leaf imports neither EM-B1a leaf — and both guard-the-guard plants are untouched. No assertion weakened, both rosters stay exact in both directions, no title added or re-worded. |
| `TEST` | `tests/domain/editPools.test.js` | A6's darkness roster over `src/domain/edit/pools.js` (no case; no arm added; no title added) | **+0** | ⭐ **VERSION 1.2 (judgment 148; ruled 171).** §6.3b exactly: A6's expected importers become EXACTLY `['src/components/edit/CardEditorDialog.jsx']`, its `expect` MESSAGE re-worded to the truth it now asserts, with ONE comment naming EM-D0e and the door's laziness — this member is the pool leaf's FIRST importer under `src/`. ⛔ The matcher's liveness probe (`tests/domain/editPools.test.js :: TYPES_LEAF_SPECIFIER`, measured 2) and A7's import fence are untouched; no assertion weakened, no title added or re-worded. |

**`requiredSymbols` — SEVENTEEN rows at version 1.1** (fifteen at version 1, plus the TWO
discharged below), **every one found VERBATIM at `git grep -c -F` = 1 at `16f0bc71c`, and every one
still present AFTER this member's edits (the post-edit simulation re-run at the tip):**

| path | symbol |
|---|---|
| `src/domain/edit/fieldDeclarations.js` | `export function declarationsFor(cardType) {` |
| `src/domain/edit/fieldDeclarations.js` | `export function isEditableCard(cardType) {` |
| `src/domain/edit/pools.js` | `export function poolValues(poolId, world) {` |
| `src/domain/edit/pools.js` | `export function rollFrom(poolId, world, seed, entryId, n) {` |
| `src/components/edit/PoolField.jsx` | `export default function PoolField({` |
| `src/components/edit/FreeField.jsx` | `export default function FreeField({ declaration, value, onChange, disabled = false }) {` |
| `src/components/primitives/PortablePopup.jsx` | `export default function PortablePopup({ open, title, onClose, children, testId = 'portable-popup', width = 560, editorHalo = false }) {` |
| `src/components/primitives/BottomSheet.jsx` | `export default function BottomSheet({` |
| `src/components/primitives/Button.jsx` | `export default function Button({` |
| `src/components/generate/ClerkNote.jsx` | `export function ClerkNote({ rubric, children, role, actions, style }) {` |
| `src/copy/index.js` | `export function t(key, vars) {` |
| `src/hooks/useIsMobile.js` | `export default function useIsMobile(breakpoint = DEFAULT_BREAKPOINT) {` |
| `src/copy/en.js` | `      rollUnavailable: 'Rolling is unavailable here',` |
| `tests/components/primitives/editorHalo.test.jsx` | `const POPUP_REL = 'src/components/primitives/PortablePopup.jsx';` |
| `tests/components/primitives/editorHalo.test.jsx` | `const filesNaming = (needle) => srcFiles()` |
| ⭐ `src/store/editSlice.js` | `export const PLAIN_EDIT_REFUSALS = Object.freeze([` |
| ⭐ `src/domain/edit/pools.js` | `'npc.role':` |

⭐⭐ **THE TWO ⭐ ROWS ARE THE DISCHARGED `_pendingRequiredSymbols`, AND ONE SPELLING MOVED IN THE
DISCHARGE.** Version 1 held them in the sibling key because EM-C4a v4.2 and EM-A2b v7 were PACKETS;
train EM-T12 landed them at `16f0bc71c` and judgment 134's law took effect — a sibling's LANDED
fence, never its predicted one. ⛔ The pending row carried `export const PLAIN_EDIT_REFUSALS;`, the
**contract** form out of EM-C4a's §6.1, and `git grep -c -F 'export const PLAIN_EDIT_REFUSALS;' --
src/store/editSlice.js` returns **0** at the tip: promoting that text unexamined would have made
this member's own sealed dispatch throw on a row nobody had measured. The LANDED text is
`export const PLAIN_EDIT_REFUSALS = Object.freeze([` at `:46`, count **1**, and the array it opens
is exactly `REFUSAL_COPY_KEYS`' seven keys in the same codepoint order (`canon_locked`,
`invalid_op`, `no_save`, `not_a_draft_field`, `rename_not_applied`, `undeclared_field`,
`unknown_target`), frozen, measured by importing it. `'npc.role':` was measured at count **1** as
spelled. ⚠ Both rows enter the dispatch's SUBSTRATE set, which is the point: a descendant that moved
either file between the verified base and the lane's HEAD now stops the seal instead of surprising
the build.

⛔ **`retiredSymbols` is EMPTY**, and the post-edit simulation proves it: this member renames nothing,
moves nothing and deletes nothing. The ONE line it changes in a landed test
(`expect(filesNaming('editorHalo')).toEqual([SHEET_REL, POPUP_REL].sort());`) is therefore
**deliberately NOT a `requiredSymbols` row** — a row asserting it verbatim would refuse this
member's own build after its own edit (pre-proof step 10). The two rows above are its stable
neighbours.

**`_pendingRequiredSymbols` — a SIBLING KEY, never an element of `requiredSymbols` (§P9(a)) — EMPTY
at version 1.1.** Both of version 1's rows are discharged into the table above, train EM-T12 having
landed. The key is kept, empty, because the shape is the law and an absent key reads as an
oversight.

### The deferred rows — PREDICTIONS, named in no `checks` and in no `changeManifest`

> **`tests/lint/.lighting-census-baseline.json`** — a named INTERIOR RED, **DELTA ONLY**, all five
> figures in the register's own order: **files +1 · parked +0 · credited +1 · titles +8 ·
> suiteTitles +1.** This member CREATEs exactly ONE test file, with EIGHT literal `it`s under ONE
> literal `describe`; the `TEST` row on `editorHalo.test.jsx` adds no title. ⛔ No absolute tuple is
> quoted. The predicted red's SHAPE is `expected <N+1> to be <N>` on the first figure the walker
> reaches. ⛔ **The terminal refreeze is the CHAIR's** and this member never edits the baseline and
> never sets `LIGHTING_CENSUS_REFREEZE`. ⛔ The row names the BASELINE JSON, never the walker. ⚠ The
> `parked +0` is a CLAIM the build lane re-measures by running the walker's own `parkReasonsFor`
> over the new file before it seals: the file must register straight-line literal `it`s under ONE
> literal `describe` and must bind no variable or parameter named `it`, `test` or `describe`.
>
> **`docs/content/wiring-census.json`** — **NOT NAMED, and deliberately so** (§P2.13). This member
> CREATEs no `.js` under `src/generators/**` or `src/domain/**`: its one `src/` CREATE lands under
> `src/components/edit/` and its one `src/` MODIFY under `src/copy/`. Measured against BOTH roots ⇒
> `stamp.producerIndexFiles` **+0**; `stamp.files` and `stamp.candidateLeaves` UNMOVED (no
> `src/domain/display/stateProse/` path is touched); `totals.*` UNMOVED (no pool, variant or
> relation is produced). The path appears in neither §7, nor the capsule, nor `checks`, and
> `tests/lint/proseWiringCensus.walker.test.js` is predicted GREEN rather than red on two arms.
>
> **`scripts/mutation-coverage-manifest.json`** — **NOT OWED** (§P2.2). `ENFORCER_DIRS`, READ at this
> member's own read tip (`tests/lint/mutationCoverage.shared.mjs:36-45`), is
> `tests/lint · tests/design · tests/docs · tests/data · tests/copy · tests/security ·
> tests/edgeFunctions · tests/generators` — **`tests/components` is not among them** — and the new
> file's basename, `cardEditorDialog.test.jsx`, matches no token of `NAME_PATTERN` (`:48-49`). The
> register is named in no row of this packet and reserves nothing against any sibling.
>
> **The tuning inventory** — **NOT OWED** (§P2.17). `TREES_P2P3` measured at
> `scripts/lib/tuning-inventory.mjs:60` is `['src/domain', 'src/generators']`; neither of this
> member's `src/` paths lies under either root, so `countUnregisteredNamed` and `countBareDecimals`
> cannot move and `tests/lint/tuningRegister.walker.test.js` is predicted GREEN. ⛔ The leaf mints no
> module-top-level `const UPPER_SNAKE = <number>;` and no fractional decimal literal in any case.
>
> **The edge-shared bundles** — **NONE** (§P2.10). Measured against all five metas' own `inputs`
> lists: `grep -c -F 'src/copy/en.js'` and `grep -c -F 'src/components/edit'` are **0 of 5 and 0 of
> 5**. No `npm run build:edge-shared`, no generated row, no re-stamped meta.

### §7a · The three statements §P2.11, §P2.16 and §P2.10 require — each MEASURED at `efd0eb448`

**§P2.16 · LINE-ADDRESSED REGISTERS — `none found`.** For every path this member modifies:
`git grep -n -F '<path>' -- tests/lint scripts`. `src/copy/en.js` returns **fourteen** hits at the lane HEAD (version 1.1 stated seven,
measured at `efd0eb448`; the build lane re-measured at its own HEAD and the DISPOSITION is
unchanged) and **not one addresses a line**: three are `{mode, path, sha256, size}` entries in
`scripts/.observed-shape-readers-baseline.json` (a CONTENT DIGEST, and `provenanceDriftOf` refuses
only on a DETECTOR SOURCE — `scripts/check-observed-shape-readers.mjs:2651`,
`if (scannedNow.has(entry.path)) continue;` — while a scanned entry's sha drift joins `inputs`),
and four are PROSE inside a `rationale` string or a header comment
(`scripts/mutation-coverage-manifest.json:88, 928, 1084, 1094`,
`scripts/mutation-sweep.sh`, `tests/lint/sourceCitationIntegrity.walker.test.js:230`,
`tests/lint/errorCopyBaseline.test.js:4`, `tests/lint/dialogExit.walker.test.js`,
`tests/lint/settlementMapSurfaceAllowlist.walker.test.js`). `tests/components/primitives/
editorHalo.test.jsx` returns **0**. `tests/lint/.prose-numerics-baseline.json` carries **0** rows for
either path; `tests/copy/.voice-mechanics-baseline.json` carries **0** rows for `src/copy/en.js`
(its Tier 2 scans `src/data` and `src/domain`, not `src/copy`, whose Tier 1 is a HARD ZERO instead).
⇒ **no re-address is owed, and the member re-measures this at dispatch rather than copying the
number.**

**§P2.11 · THE BYTE ARM — this member is NOT the train's holder, and its predicted delta is `+0` on
every budget that exists.** No `tests/build/**` path appears in §7, the capsule or `checks`. Each
figure measured by a probe that IMPORTS the exported set (or walks static imports) and reads no
`dist`, so neither can skip:
- **eager first paint — `+0` modules, `+0` bytes.** `EAGER_FIRST_PAINT_MODULES` measured **270** at
  `efd0eb448` (an as-of figure under §P3.5, never an assertion): `src/components/edit/**` at **0**,
  `src/copy/en.js` **absent**, `src/copy/index.js` **absent**, both primitives **absent**,
  `src/domain/edit/**` at **0**. ⇒ the ONE `src/` CREATE lands outside the closure and the ONE
  `src/` MODIFY adds bytes to a module the closure does not contain. This member's own prediction is
  the DELTA **`N → N` modules and +0 bytes**, and it names no holder's row because it moves no
  budget.
- **the generation worker — `+0` B.** A static-import walk from `src/workers/generation.worker.js`
  (187 modules at this tip, an instrument-dependent as-of figure) contains **0**
  `src/components/edit/**` and **0** `src/copy/**`. The same walk over
  `customContentPreview.worker.js` (186) and `advanceInterval.worker.js` (494) reads **0** and
  **0**. ⇒ the EXACT, zero-slack `WORKER_BUNDLE_CEILING_BYTES` cannot move.
- **the lazy engine / PDF vendor closure — `+0` B.** `src/utils/pdfRender.worker.js` (326) contains
  **0** of either. ⇒ `tests/build/vendorPdfLazy.test.js`'s budget cannot move.
- ⭐ **THE LOAD-BEARING CLAIM IS MEMBERSHIP, NOT A COUNT.** The four module counts are kept only as
  an as-of mark naming the instrument that produced them.
- ⭐⭐ **RE-MEASURED AT `16f0bc71c` BY THE PRE-PROOF WITH A SECOND, INDEPENDENT INSTRUMENT, AND THE
  TWO AGREE ON EVERYTHING THAT MATTERS.** A fresh static-import walk reads **220 · 267 · 546 · 371**
  modules for the four closures where the compile's own walker read 187 · 186 · 494 · 326 — the two
  resolvers differ — and reads the **SAME ZERO membership** for `src/copy/en.js` and
  `src/components/edit/**` in **all four**. The eager set, taken by IMPORTING
  `EAGER_FIRST_PAINT_MODULES` from the tip's own `vite.config.js` and reading no `dist`, is still
  **270**, UNCHANGED across the J-T1 window, with `src/components/edit/**` 0, `src/copy/en.js` 0,
  `src/copy/index.js` 0, `src/domain/edit/**` 0, both primitives 0 and — newly measurable —
  **EM-C4a's own `src/store/editSlice.js` at 0**, so that member's `+0 eager` claim is confirmed
  from the outside. All five edge-shared metas still name neither path in their `inputs`: 0 of 5 and
  0 of 5. ⇒ that two instruments disagree on the counts and agree on the membership is precisely why
  the counts are declared as instrument-named as-of marks and never as assertions.

**§P2.10 · THE EDGE-SHARED INPUT MEMBERSHIP — NONE**, measured against the five metas' own `inputs`
lists rather than by entry-hood (the row above carries the figures).

**§P9 (SIXTH AMENDMENT) · THE EFFECTIVE-LINE MEASUREMENT, WITH THE ONE INSTRUMENT.** eslint's own
`Linter` under `max-lines` with `skipBlankLines` and `skipComments`, run in plain `node` over each
file AS IT STANDS and over THE PLANNED TEXT (the planned leaf is a scratch artifact of the compile,
never written into any worktree):

```
src/copy/en.js  AS IT STANDS                            effective lines: 1260
src/copy/en.js  WITH THE PLANNED TEXT                   effective lines: 1275   -> DELTA +15
src/components/edit/CardEditorDialog.jsx PLANNED        effective lines:  140   -> of 250
PACKET TOTAL (new/changed effective production lines)                     155   -> of 400
```
⛔ Never `wc -l`, never a hand convention, never an inherited figure. The first draft of the copy
block measured **+17** and was RESHAPED to +15 rather than renegotiating the cap (§3).

**§P2.19 (SIXTH AMENDMENT) · THE VOICE WALKER'S OWN COUNTER, RUN AT COMPILE.**
`tests/copy/voiceMechanics.test.js:144`'s predicate — `v.includes('—') || v.includes('!')` — run in
plain `node` over every literal this member plans to spell: the thirteen copy strings and the leaf's
fourteen non-copy literals. **27 literals measured · 0 em dashes · 0 exclamation points · exit 0**,
with a planted control (`'… plain edit — so the field is unchanged!'`) proving the predicate fires
on both tells. ⇒ Tier 1's HARD ZERO is met by MEASUREMENT, and the per-file JSX baseline
(`tests/copy/.voice-mechanics-jsx-baseline.json`, shrink-only) cannot rise: the leaf renders no JSX
text literal at all — every rendered word arrives through `t()`.

## §8 · Ordered coding sequence

1. **MEASURE BEFORE YOU SEAL** — §P10.3's six measurements, in plain `node` and `git`, each a PASS
   or the smallest measured contradiction. Any failure is a STOP BEFORE the seal.
2. **RED FIRST, AND BEFORE THE LEAF EXISTS.** Write `tests/components/cardEditorDialog.test.jsx`
   WHOLE and run it: with the leaf absent the file cannot collect, so no arm here was ever green
   before its subject was built (EM-D0d's landed idiom, §8 step 2). Quote the collection failure.
3. Write `src/copy/en.js`'s `dialog:` sub-block — §6.2's THIRTEEN strings VERBATIM, FLAT. Run the copy
   directory: `tests/copy` whole is the enforcer directory this MODIFY opts into.
4. Write `src/components/edit/CardEditorDialog.jsx` — §6.1's algorithm in its numbered order. Run
   the battery: A1–A8 green.
5. Apply §6.3's one-token cure to `tests/components/primitives/editorHalo.test.jsx` — ⛔ **A6 is RED
   between steps 4 and 5 and that red is EXPECTED and QUOTED**: it is the landed pin doing its job.
   Run `tests/components` whole afterwards and quote A6 green.
6. **THE MUTANTS** (§P6, in this order, each one proved): (i) delete the off-list PREPEND in
   `optionsFor` ⇒ A3 red by its own title; (ii) replace the AUTHORED order with
   `[...declarations].sort()` ⇒ A1 red; (iii) drop the `typeof apply === 'function'` guard from the
   handler ⇒ A6's belt arm red; (iv) change one `REFUSAL_COPY_KEYS` key's spelling ⇒ A5 red both
   ways. Prove the source bytes changed, require a nonzero exit with the NAMED title red, restore
   the exact pre-mutant SHA-256, rerun focused green.
7. **THE WRITTEN-CONTRACT COUNTERFORCES** (§P6's EM amendment): A8's source scan and A5's
   set-equality already hold at the base, so each is proved against a PLANTED perturbation that reds
   the named title — A8 with the control string the case itself plants, A5 with one key removed from
   the frozen map — restored digest-exact, then green. The counterforce is quoted beside the arm.
8. Run the sealed `checks` in §10's order. Run the two BUILD-LANE INSTRUMENTS (never sealed checks):
   the excluded `tests/lint` directory run, which MUST EXIT 0, and the lighting walker ALONE, the
   measured interior red whose `expected … to be …` line names which of the five figures it stopped
   at. ⛔ Do NOT set `LIGHTING_CENSUS_REFREEZE`.
9. Stage EXACTLY the four §7 paths by explicit path — never `git add -A`, `-u` or `.` — write the
   commit message to the lane's scratch, and END. The chair's one queue commits.

## §9 · Acceptance matrix

**EIGHT cases, EIGHT literal `it`s, ONE literal `describe`, ONE file.** Every case names the file
that holds it and the §7 row that authorizes that file's edit (§P9(f)).

| # | case | file | authorizing §7 row |
|---|---|---|---|
| **A1** | **GENERATED FROM THE TABLE, NEVER A HAND-WRITTEN LIST.** For `cardType='npc'` the dialog renders exactly `declarationsFor('npc').length` controls in the table's AUTHORED order — a single-line input for `name`, a select for `role`, a select for `status`, a textarea for `note` — each labelled with the declaration's OWN `label`, read from the producer and never re-typed (anti-vacuity rule 2). **AND the landed negative control:** for `cardType='faction'` the two rendered controls are `faction` and `category` and the `'share'` row renders NO control and NO label. | `tests/components/cardEditorDialog.test.jsx` | its `CREATE` row |
| **A2** | **THE TWO EMPTIES ARE DIFFERENT, AND BOTH ARE SHOWN.** With `world={ institutions: [] }` the `role` pool resolves EMPTY, so its select is present, EMPTY and **DISABLED**, carrying `t('edit.field.noOptions')` — never absent (design §2.1). In the SAME render, `name` with no value is present, EMPTY and **ENABLED**. ⚠ The arm reads the pool through `poolValues` itself, so it holds both before and after EM-A2b lands `npc.role`. | same | same |
| **A3** | **THE OFF-LIST STORED VALUE (judgment 109).** With `values.status` a word the producer's own `poolValues('npc.status', world)` does NOT contain (asserted absent from that set in the same arm), the `status` control's value IS that word and its first option IS that word, with the pool's own members following in the producer's order, unsorted and unfiltered. ⛔ `PoolField` is not edited: the same arm asserts the leaf's source names no `sort`, `filter` or `splice` over an options array. | same | same |
| **A4** | **THE SEEDED ROLL IS THE PRODUCER'S, AND THE DIALOG DRAWS NOTHING.** Clicking "roll another" on a populated pool sets the control to exactly `rollFrom(declaration.pool, world, seed, entityId, 1)` — computed in the arm by calling the producer, never by mocking — and clicking again with the same props reaches the same value twice. In the same arm, a positive set equality: the list of draw-and-clock spellings in the leaf's source (`Math.random`, `Date.now`, `new Date`, `performance.now`, `crypto.`) EQUALS the empty set, with the scanner driven over a control source that DOES contain one so the equality cannot read as agreement. | same | same |
| **A5** | **THE CLOSED REFUSAL SET, BOTH WAYS, AND THE ONE FALLBACK.** `Object.keys(REFUSAL_COPY_KEYS)` is set-equal BOTH DIRECTIONS to `PLAIN_EDIT_REFUSALS`, imported from `src/store/editSlice.js` and never re-typed; every value resolves through `t()` to a string that is not the key itself; and a stub answering `{ ok:false, reason:'no_verb' }` — a word outside the seven, the executor's own — renders `REFUSAL_FALLBACK_KEY`'s line and no invented word. | same | same |
| **A6** | **ONE WRITER, THE RIGHT COORDINATES, THE AUTHORED ORDER, AND THE STOP.** With a recording stub, changing `role` and then `name` and pressing Save calls the stub EXACTLY TWICE, `name` FIRST, each call carrying exactly `{ cardType:'npc', entityId, field, value }` and no other key; an unchanged field is never dispatched. With the stub refusing the FIRST call `canon_locked`, the second field is NOT dispatched and the notice names the refused field's label beside that reason's line. ⛔ **THE BELT:** with `apply` absent the Save control is disabled AND the handler returns before any call, proved by re-enabling the button through the rendered DOM and firing the click. | same | same |
| **A7** | **DARK, AND CANON.** `open={false}` renders NOTHING — `container.innerHTML` is the empty string, not a wrapper; an undeclared `cardType` renders nothing in the same way. With `phase='canon'` every control is DISABLED, the canon line is present, and a Save click dispatches nothing (the stub is not called). | same | same |
| **A8** | **THE TWO DOORS, THE HALO, AND THE SOURCE SCAN WITH ITS PLANTED CONTROL.** Desktop (`useIsMobile` false): the surface carries `role="dialog"`, `aria-modal="true"`, exactly ONE control whose accessible name contains "Close", and the GOLD rim of `editorHalo`. Phone (mobile true): the surface is `BottomSheet` with `editorHalo`, its own trigger, and the same door facts once opened. ⛔ **AND THE LEAF'S OWN BYTES:** a scan of `CardEditorDialog.jsx`'s source finds none of `set-npc-name`, `rename-npc`, `makeOp`, `applyEdit`, `OP_TYPES`, `src/store/`, `src/application/`, `EDITOR_GROUND`, `houseBloom`, `TIER_GATE`, `settlementEditor` — as a POSITIVE set equality against the empty set, with a PLANTED control string proving the scanner fires. | same | same |

### §9a · Anti-vacuity, stated

1. **No bare negative without its counterforce.** A4, A8 and A3's purity arm are POSITIVE SET
   EQUALITIES against the empty set, each with its scanner driven over a source that DOES contain a
   member, so none is a `not.toContain` and none owes an `// anchored:` marker.
2. **No fixture mirrors the deriver.** A3's absent word and A4's rolled value are computed by
   CALLING `poolValues` and `rollFrom`, so the arm cannot agree with a broken leaf by construction.
3. **No count mutant goes vacuous.** A1 compares against `declarationsFor(...).length` read live,
   never a literal four.
4. **The rendered surface really rendered.** A7's negative (`open={false}` renders nothing) is
   paired in the same case with a positive render, so "nothing" is distinguishable from "the test
   rendered nothing at all".

### §9b · THE TWO LANDED ARMS VERSION 1.2 WIDENS — NO CASE, NO ARM, NO TITLE

⛔ **NEITHER `TEST` ROW OF VERSION 1.2 HOMES AN ACCEPTANCE CASE AND NEITHER ADDS AN `it`,** at
EM-C4a version 4.3's exact shape: the matrix stays at EIGHT cases in ONE file, the count law stays
`arms declared 8 · cases homed 8`, and the lighting delta is unchanged in all five figures. What the
two rows buy is that the widening is PROVED INSIDE THIS MEMBER'S OWN BATCH — `checks` entry 6 runs
both files — instead of reddening for the first time at the train's terminal, attributed to no
member. Neither widened arm can go vacuous under the addition, and that is the arms' own doing, not
a claim of this packet: `tests/domain/editDeclarations.test.js :: dormancyImporters` is re-entered
inside its own arm on a planted runtime import (convicted) and a planted JSDoc typedef (acquitted),
and `tests/domain/editPools.test.js :: TYPES_LEAF_SPECIFIER` proves the same matcher still finds a
sibling edit leaf that IS imported. Both arms also carry anti-vacuity floors on the walk itself
(more than 400 and more than 1,000 sources), which this member does not touch.

## §10 · Verification commands

```sh
# THE SEALED CHECKS — FILES, never a directory; the lighting walker appears nowhere;
# the validator is LAST (§P7).
#  1  tests/components   — the battery, EM-V10's two landed pins, the halo set cure, the raw-key scan
#  2  tests/copy         — the enforcer directory the src/copy/en.js MODIFY opts into
#  3  tests/lint         — the governing walkers, BY FILE, MINUS the lighting walker.
#     ⭐ NEW AT VERSION 1.1: tests/lint/editMutationPath.walker.test.js, EM-C4a's one-mutation-path
#     gate, which LANDED inside the EM-T12 window and did not exist when version 1 computed this
#     set. It source-scans EVERY src/ module, so it governs this member's one src/ CREATE; §6.1
#     item 1's import ban is the thing it enforces.
#  4  tests/design       — the bloom's home and the closed motion vocabulary
#  5  the FIRST-PAINT MEMBERSHIP PROBE — imports the exported set, reads no `dist`,
#     therefore CANNOT SKIP; exits non-zero if `src/components/edit/**` or `src/copy/en.js`
#     has entered the eager closure (the launch brief's clause 5)
#  6  tests/domain      — ⭐ NEW AT VERSION 1.2 (judgment 148; ruled 171): the TWO LANDED
#     IMPORTER ROSTERS this member widens (§6.3a, §6.3b), so the widening is proved in this
#     member's own batch and not for the first time at the train's terminal. Placed last but
#     one so entries 1-5 keep their numbers; the validator stays LAST (§P7).
#  7  node scripts/implementation-packets.mjs validate
```

| # | argv |
|---|---|
| 1 | `npx vitest run --pool=threads --maxWorkers=2 tests/components/cardEditorDialog.test.jsx tests/components/editFields.test.jsx tests/components/primitives/editorHalo.test.jsx tests/components/copyRawKeyRender.test.jsx` |
| 2 | `npx vitest run --pool=threads --maxWorkers=2 tests/copy/voiceMechanics.test.js tests/copy/localeParity.test.js tests/copy/copy.test.js tests/copy/proseLeak.test.js` |
| 3 | `npx vitest run --pool=threads --maxWorkers=2 tests/lint/copyKeyResolution.walker.test.js tests/lint/editMutationPath.walker.test.js tests/lint/dialogExit.walker.test.js tests/lint/bottomAnchoredChrome.walker.test.js tests/lint/negativeAssertionAnchor.walker.test.js tests/lint/contractTestAntiVacuity.walker.test.js tests/lint/seedLoopTotality.walker.test.js tests/lint/proseNumerics.test.js tests/lint/tuningRegister.walker.test.js tests/lint/mutationCoverageManifest.test.js tests/lint/refusalNoticeCoverage.walker.test.js tests/lint/proseWiringCensus.walker.test.js` |
| 4 | `npx vitest run --pool=threads --maxWorkers=2 tests/design/houseBloom.test.js tests/design/organicMotion.test.js` |
| 5 | `node -e "const{pathToFileURL}=require('node:url');import(pathToFileURL('vite.config.js').href).then(m=>{const S=[...m.EAGER_FIRST_PAINT_MODULES].map(String);const mine=S.filter(p=>p.includes('src/components/edit/'));const copy=S.filter(p=>p.endsWith('src/copy/en.js'));const idx=S.filter(p=>p.endsWith('src/copy/index.js'));console.log('EAGER_FIRST_PAINT_MODULES:',S.length,'| src/components/edit members:',mine.length,'| src/copy/en.js members:',copy.length,'| src/copy/index.js members:',idx.length);if(mine.length!==0)process.exit(1);if(copy.length!==0)process.exit(1);if(!S.some(x=>x.endsWith('src/components/theme.js')))process.exit(1);})"` |
| 6 | `npx vitest run --pool=threads --maxWorkers=2 tests/domain/editDeclarations.test.js tests/domain/editPools.test.js` |
| 7 | `node scripts/implementation-packets.mjs validate` |

**THE GOVERNING SET, COMPUTED** (TOOL-26's `governing-tests.mjs` has not landed, so the rule's
fallback is used): `git grep -l -F '<basename without extension>' -- tests` for every file changed
and for every exported symbol whose call shape changes, plus every walker that governs a directory
in which a test file is CREATEd, MINUS the lighting walker. Measured: `CardEditorDialog` → **0**
(nothing names it yet); `editorHalo` → **2** (`tests/components/editFields.test.jsx`,
`tests/components/primitives/editorHalo.test.jsx`); the walkers naming `tests/components` → the
twelve files above minus the lighting walker.
⭐⭐ **RE-DERIVED AT `16f0bc71c` AND IT GREW BY ONE** (pre-proof step 17: the governing set keys on
IMPORTERS and on the DIRECTORY of a CREATE, not only on the files a member edits). The J-T1 window
landed a new `tests/lint` walker whose corpus is every `.js`/`.jsx` under `src/` —
`tests/lint/editMutationPath.walker.test.js`, EM-C4a's — so this member's one `src/` CREATE opts
into it the moment the file exists. It is now `checks` entry 3's first-listed addition and a §5 row.
⛔ Nothing else moved: the twelve `tests/lint` files version 1 named all exist at the tip, and
`tests/lint/ruinFilterRoster.walker.test.js` — the other `tests/lint` file the window touched —
scans `src/domain` only and stays out of this set.

⛔⛔ **AND AT VERSION 1.2 THE DERIVATION ITSELF IS CORRECTED, BECAUSE THE ONE ABOVE IS THE WRONG
ONE** (the build lane's pre-seal measurement; judgment 171). The sixth amendment's §P10.11 (i)
requires the governing set to be re-derived FOR EVERY MODULE THE CONTRACT IMPORTS —
`git grep -l -F '<the import target's basename>' -- tests`, ONE SEARCH PER IMPORT — *"because
dormancy arms and consumer rosters key on IMPORTERS and not on the edited file"*. Versions 1 and 1.1
keyed on the files this member EDITS and on the DIRECTORY of its one test CREATE, and so never saw
the two landed arms over the two `src/domain/edit/` modules §7's CREATE row orders this leaf to
import. Executed per import at the lane HEAD:
- `fieldDeclarations` → **7** files, of which `tests/domain/editDeclarations.test.js` holds an EXACT
  importer roster in both directions ⇒ §7's first new `TEST` row (§6.3a).
- `pools.js` → **5** files, of which `tests/domain/editPools.test.js` holds a darkness roster
  asserted `[]` ⇒ §7's second new `TEST` row (§6.3b), this member being the FIRST importer.
- `PoolField` and `FreeField` → `tests/components/editFields.test.jsx` and
  `tests/domain/editDeclarations.test.js`. This member is their first `src/` importer too, and
  NEITHER file holds an importer roster over them: the first scans their SOURCE
  (`tests/components/editFields.test.jsx :: LEAF_SOURCES` — draws, clocks and copy keys), the second
  only names them in a comment. ⇒ **no row owed, measured rather than assumed.**
- `copy/index.js`, `useIsMobile`, `Button`, `ClerkNote`, `PortablePopup`, `BottomSheet` and
  `proseScale` each already have `src/` importers at the lane HEAD, so no first-importer arm of any
  of them can fire on this member's account.
⇒ `checks` gains entry 6 and §7 gains two `TEST` rows. ⛔ The FALLBACK derivation above stays in
this packet as written, because it is still owed for every file this member EDITS; the per-import
sweep is an ADDITION to it and never a replacement.

```sh
# THE BUILD LANE'S INSTRUMENTS — ⛔ NEVER SEALED CHECKS (§P7)
#  (a) the excluded directory run, which MUST EXIT 0:
npx vitest run --pool=threads --maxWorkers=2 tests/lint \
  --exclude=tests/lint/sovereigntyLightingContract.walker.test.js
#  (b) the lighting walker ALONE — the measured, NAMED interior red:
npx vitest run --pool=threads --maxWorkers=2 tests/lint/sovereigntyLightingContract.walker.test.js
#      expected NONZERO; its `expected <N+1> to be <N>` names the first of the five figures it reaches.
#  ⛔ DO NOT set LIGHTING_CENSUS_REFREEZE. The refreeze is the chair's, at the terminal.
#  (c) the PRE-SEAL count prover, run from the KIT path (§P9 — kit furniture, never a checks entry):
node <kit>/EM-D0e.count-prover.mjs <kit>/EM-D0e.md <kit>/EM-D0e.manifest.json
#  (d) scoped lint over the two edited src paths:
npx eslint src/components/edit/CardEditorDialog.jsx src/copy/en.js
#  (e) ⛔ THE TYPE MEASUREMENT OVER THIS MEMBER'S OWN src/ PATHS, BEFORE ANY REPORT (§P7, sixth
#      amendment): the TypeScript compiler API over `src/components/edit/CardEditorDialog.jsx` and
#      `src/copy/en.js` under `tsconfig.full.json` — a PLAIN-NODE measurement that needs no gate
#      slot — plus both ratchets at their exact floors and the observed-shape count exact. A
#      pre-seal table carrying eslint without the type measurement is INCOMPLETE.
#  (e2) ⛔ A RED-FIRST WHOSE LOG PRINTS NO TEST COUNT, OR `Tests no tests`, DID NOT RUN (§P7, sixth
#      amendment): §8 step 2's red-first is a COLLECTION failure by design, so its receipt says so
#      in those words and claims no executed red; every other red-first quotes its count line.
#  (f) ⛔ THE BROWSER SUITE: NONE (§6.5) — no route, no mount, no name any spec reads.
```

Under a parallel train the lane runs NO gated line itself: it writes ONE gate script whose every
vitest line goes through `sh scripts/gate-mutex.sh --run -- …` with a `records.tsv` row per step,
each exit captured from the command itself and never piped, and the chair's one queue runs it
(§P10.5). ⛔ Never read a gate through a pipe (§P7).

**Registers that must NOT move:** `tests/property/generatorGoldenMaster.test.js`,
`tests/property/dossierProseManifest.test.js`, `tests/fixtures/.golden-freeze-register.json`,
`scripts/mutation-coverage-manifest.json`, `docs/content/wiring-census.json`,
`tests/lint/.prose-numerics-baseline.json`, `tests/copy/.voice-mechanics-*.json`,
`tests/build/vendorPdfLazy.test.js`, `tests/build/generationWorkerLazy.test.js`.

## §11 · Mandatory STOP conditions

In addition to `PACKET_STANDARD.md`'s list and §P8's:

1. **A control would import a mutation module.** If the contract cannot be built without
   `makeOp`, `validateOp`, `OP_TYPES`, `applyEdit` or any `src/store/**` / `src/application/commands/**`
   specifier in the leaf — STOP. The seam is injected; a component minting an op is EM-C4a's own
   planted red **P2** and §P8's "a component would import a domain edit module directly". ⭐ AT
   VERSION 1.1 THE STOP HAS AN EXECUTABLE PROOF: `tests/lint/editMutationPath.walker.test.js` is
   LANDED and is `checks` entry 3's, so the build lane does not have to take this clause on trust —
   the walker convicts by name, and its own P1/P2 case shows the conviction on a planted component.
2. **A second glow recipe, a new hex, or a thirteenth motion behavior** would be needed — STOP.
   `editorHalo` is passed and nothing else; `MOTION_GRAMMAR` is frozen at twelve.
3. **A shared primitive would have to change** — STOP. `PoolField`, `FreeField`, `PortablePopup` and
   `BottomSheet` are landed and out of this manifest; a controlled-open `BottomSheet` is a new
   member's work and §13 R3 is where it is raised.
4. **The off-list value would be dropped, sorted in, or cured inside `PoolField`** — STOP. Judgment
   109 assigned the decision to the dialog and required the control to stay pure.
5. **A refusal word would be invented, or a reason would have no line** — STOP. The set is EM-C4a's
   seven plus one declared fallback, pinned both ways by A5.
6. **A walker, a spec or a ratchet would be weakened, widened or skipped to pass** — STOP. The
   THREE widenings this member performs are §6.3's halo set cure and version 1.2's two importer
   rosters (§6.3a, §6.3b), each of them the arm's own owner-of-the-landing rule (judgment 110;
   judgment 148's first-importer law, ruled 171) and none of them a weakening: no assertion is
   deleted, no tolerance is widened, no title is added or re-worded, and every roster stays
   EXACT in both directions. ⛔ A FOURTH widening, of any arm, is a STOP.
7. **`declarationsFor` or `poolValues` would be re-implemented, mirrored or cached** — STOP.
8. **A figure would be rendered** — STOP and add the prose-numerics row first (§P2.7); this member
   renders none.
9. **The eager first-paint set would gain a member, or `src/copy/en.js` would enter it** — STOP;
   that is the owner's (§P11.2).
10. **The gate would be spelled in this leaf** — STOP. `canEditSettlement` is the MOUNT's conjunct
    and the mount is not in this manifest.
11. **`declarationsFor('npc')` would return other than the four rows §5 records, or `POOLS` would
    lack `npc.role` at the member's own base** — STOP: a provider's landed contract differs from
    this packet's quotation, and the member returns to compile.

## §12 · Receipt, questions and handoff

**The SET-EQUALITY proof (§P9(d)), to be pasted by the build lane:**
`§7 table rows parsed : 6 · capsule changeManifest: 6 · IN §7 AND NOT THE CAPSULE: NONE · IN THE
CAPSULE AND NOT §7: NONE · SET-EQUAL: YES`.

**The count law (§P9(f)), one line per file:**
`tests/components/cardEditorDialog.test.jsx · arms declared 8 · cases homed 8 · titles delta +8` ·
`tests/components/primitives/editorHalo.test.jsx · arms declared 0 · cases homed 0 · titles delta +0` ·
`tests/domain/editDeclarations.test.js · arms declared 0 · cases homed 0 · titles delta +0` ·
`tests/domain/editPools.test.js · arms declared 0 · cases homed 0 · titles delta +0`.
Proved by `EM-D0e.count-prover.mjs` from the kit path, and by its deliberately wrong copy exiting
non-zero.

**The lighting delta this member predicts:** files **+1** · parked **+0** · credited **+1** · titles
**+8** · suiteTitles **+1**, in the register's own order.

**NOTICED AND NOT TOUCHED** (recorded, each with its slot — never "deferred" without one):

1. ⚠ **`e2e/flow-b-auth-credits-ai.spec.js:203`'s `/Save\b/i` locator** will be able to match the
   dialog's Save control once a member mounts it on a route. **SLOT: EM-D1's compile** (§6.5).
2. ⚠ **EM-C4a's `undeclared_field` witness is unreachable through THIS door.** Its §12 table names
   `cardType 'npc', field 'salary'` as the witness; measured, `makeOp('set-field', {kind:'npc',
   id}, {field:'salary', …})` is refused EARLIER as `invalid_op` — `validateOp` reports
   `payload.field is not one of the declared values`, because `SET_FIELD_FIELDS` is DERIVED from the
   declaration table (`src/domain/edit/operations.js`, the `const SET_FIELD_FIELDS` declaration —
   ⛔ **NAMED BY SYMBOL AND NOT BY LINE AT VERSION 1.1**: it stands at `:149` at `16f0bc71c`, version
   1 cited `:147-149`, and **EM-B1b version 5 rides this same train and splices that same file
   again**, so any address written here is stale before the train lands). Re-executed at the tip:
   `makeOp('set-field', {kind:'npc', id}, {field, value})` validates `{"ok":true,"errors":[]}` for
   `name`, `role`, `status` and `note`, and is refused `payload.field is not one of the declared
   values` for `salary`. The reason is reachable by another op type, so nothing of EM-C4a moves; the
   witness's wording does. **SLOT: the Edit-Mode inbox, for EM-C4a's next version.**
3. ⚠ **`src/App.jsx` HAS 25 EFFECTIVE LINES OF HEADROOM** against a 600 ceiling that will not be
   raised (`PACKET_STANDARD.md`'s standing hot-file list, measured there under the same instrument).
   The mount that EM-D1 will write spends part of it, and the hot-file law says that edit is shaped
   to net zero where it can be. **SLOT: EM-D1's compile**, which opens with its own executed
   headroom measurement.
4. ⚠ **The partner-save reach.** EM-C4a §6.1a records that the declared writer reaches the HOST save
   only, so a neighbour's `interSettlementRelationships[].npcName` keeps the old name after a
   rename. Pre-existing, named by EM-C4a with its own slot; this member neither widens nor hides it,
   and the dialog shows no neighbour surface.

5. ⚠ **VERSION 1.1's HEADER CARRIED THE FIFTH AMENDMENT'S PREAMBLE SHA-256 AFTER THE SIXTH HAD
   LANDED.** The placement tool re-stamps **Status**, **Verified base** and **Last revalidated**
   only, so the `Preamble:` hash a compile measured at its own read tip survives a later amendment;
   EM-B1b and EM-B2a4 carry the same stale stamp at this placement. It blocks nothing —
   `scripts/implementation-packets.mjs` reads no preamble hash, so no seal and no `validate` can
   catch it — which is what makes it a STAMP-IS-A-CLAIM finding rather than a refusal. Corrected in
   this member's own header at version 1.2. **SLOT: the chair's placement checklist for train
   EM-T13**, for the two siblings and for the tool.
6. ⚠ **`scripts/mutation-sweep.sh` rewrites a `save:` key inside `src/copy/en.js`** by a literal
   whose indentation belongs to an earlier, unrelated block, and its `perl` substitution is
   un-global, so §6.2's `edit.dialog.save` cannot be hit by it at this landing. Measured, and
   recorded because a re-indent of either block would create the collision silently. **SLOT: the
   Edit-Mode inbox.**

**QUESTIONS ONLY THE CHAIR CAN ANSWER — FOUR, each with a one-line recommendation:** they are §13's
R2, R3, R5 and R6.

## §13 · RAISED — for the chair

| # | item |
|---|---|
| **R1** | ⛔⭐ **THE NAME FIELD DISPATCHES `set-field`, AND THE DOOR NEVER MINTS `set-npc-name` — the divergence, named as the dispatch note requires.** MEASURED at `efd0eb448`: `makeOp('set-field', { kind:'npc', id:'npc_3' }, { field:'name', value:'Mara' })` validates `{"ok":true,"errors":[]}` (executed), because `SET_FIELD_FIELDS` is DERIVED from EM-A1's table and `validateOp` checks the target's KIND for membership of the closed seven, never for a MATCH against the row's declared target. EM-C4a dispatches on NEITHER op key: its cascade branch keys on the DECLARATION's kind `free-cascade` and on `CASCADE_WRITERS['npc:name'] → renameNPC`. ⇒ **one op shape for every plain edit this door makes, `set-field` on the declared field**, which is also design §20.4's own word. ⛔ **`set-npc-name` (EM-B1c1 v5's row) has NO PRODUCER** at this read tip or in any placed packet, and one act carrying two payload shapes (`{ newName }` versus `{ field, value }`) is a real divergence. A8 pins that this leaf never mints it, with a planted control. *Recommend: ratify `set-field` as the door's one op shape and leave the producer question where the chair slotted it (the Edit-Mode inbox: EM-C4b or EM-B1c); nothing in this packet moves either way.* |
| **R2** | ⭐ **RULED 146a: the mount rides EM-D1, as recommended.** ⛔⛔ **THE `src/App.jsx` MOUNT IS OUT OF THIS MANIFEST, AND THE MEASUREMENT IS §2.2(a).** Nothing landed or placed can OPEN the dialog (EM-D1 `EditModeShell` owns the pencil and is unwritten and unplaced; EM-C4a's transient vocabulary carries a mode and no target) and nothing can BIND its writer (EM-C4a mints no store action, no `plainEditCommandScope` and no `applyPlainEdit` binding; a component may not close that gap — EM-C4a's own plant P2 and §P4). A mount that no reachable state can open is dead code in an EAGER hub file. *Recommend: ratify the drop — the mount rides **EM-D1**, the member that owns the pencil, the target and the values bag, and that member also inherits §6.5's `e2e/` obligation and the `/Save\b/i` locator. The alternative (carry a gated, never-opening mount now) buys nothing and spends first-paint bytes twice.* |
| **R3** | ⭐ **RULED 146b: the self-opening phone sheet is RATIFIED for version 1; a controlled-`open` prop is an EM-D0c-successor member and an inbox row.** ⛔ **`BottomSheet` IS NOT A CONTROLLED DIALOG, so the phone branch is shaped differently from the desktop one.** Measured (`BottomSheet.jsx:53, 75-84`; EM-D0c's §5(b) recorded the same fact): it owns `open` as internal `useState(false)` and renders its own trigger `Button`; its props carry no `open`. So on the phone this leaf's `open` decides whether the SHEET AND ITS TRIGGER exist, and the DM's tap on the sheet's own trigger raises it — one tap more than the desktop popup. *Recommend: ratify the self-opening sheet for the first door. Adding a controlled-open prop is a change to a shared primitive with two foreign consumers, which is exactly what D0c was cut out of D0e to hold, and it would be a SECOND modified src file over the brief's cap; if the chair wants one tap on both surfaces, it is a D0c-successor member and should be said now, because changing it later is a contract change to a landed primitive.* |
| **R4** | ⚠ **§P8's STOP list still spells the gate `TIER_GATE.premium.editMode`, which `CHAIR-RULING.md` item 3 replaced with `settlementEditor` and EM-D0b LANDED** (`authSlice.js:83-85, 866-868`). This member spells neither, so it cannot be convicted either way. ⭐⭐ **MEASURED AT `16f0bc71c` FOR THE SIXTH AMENDMENT'S CORRECTION C2, AND THE FINDING IS WORSE THAN A MISSPELLING.** `git grep -n -E 'settlementEditor\|editMode' 16f0bc71c -- src`: the gate is `TIER_GATE[tier].settlementEditor` (`src/store/authSlice.js:83-85`, `anon:false · free:false · premium:true`) behind the predicate `canEditSettlement: () => TIER_GATE[tier]?.settlementEditor === true && isStaffRole(role)` (`:868`), pinned from the other side by `src/config/tierFacts.js:185` (*"Pinned to TIER_GATE.{tier}.settlementEditor by the contract test"*). There is **no `TIER_GATE…editMode` literal anywhere under `src/`** — but **`editMode` IS a live, shipped, UNRELATED flag**: `src/store/settlementSlice.js:806-808` (`editMode: false`, `setEditMode`, `toggleEditMode`), the Cartographer's dossier-edit mode, read across `SettlementDetail.jsx`, `npcComponents.jsx`, `EditableText.jsx`, `SettlementDetailActions.jsx` and `SettlementDossierHero.jsx`, and carrying **no `TIER_GATE` row at all**. EM-C4a's landed leaf warns of the same collision in one line (`src/store/editSlice.js:17`: *"THE KEY IS `editorMode`, NEVER `editMode`"*). *Recommend, unchanged and now with its measurement: ONE line of correction C2 in the sixth amendment before it lands — §P8's and §P11.1's clauses re-point to `TIER_GATE.premium.settlementEditor` with `canEditSettlement()` named as the predicate, and say in the same breath that `editMode` is a DIFFERENT shipped feature so the STOP cannot convict a member for touching the Cartographer's flag. No packet's contract moves.* |
| **R5** | ⭐ **RULED 146c: the binder is ONE exported function in EM-C4a's own `src/store/editSlice.js`, carried by EM-C4b; this member's `apply` stays injected with its `null` default and EM-D1 wires it.** ⚠ RE-MEASURED AT `16f0bc71c` AFTER EM-C4a LANDED, AND THE GAP IS REAL CODE NOW: `git grep -n -E 'applyPlainEditIntent\|plainEditCommandScope' -- src tests` returns **0 hits**, `src/store/index.js` names `editSlice` **0 times** (the leaf is lazy and uncomposed, exactly as EM-C4a declared), and the landed `runPlainEditCommand(scope, dependencies)` takes `applyPlainEdit` as an INJECTED dependency — so nothing at the tip can bind this member's seam, and the ruling's EM-C4b is the first member that can. ⛔ **WHO BINDS `apply`, AND IN WHAT SHAPE — the one seam this member declares and does not fill.** §6.1's contract is `apply(edit: { cardType, entityId, field, value }) → Promise<{ok}|{ok:false,reason}>`: the coordinates, and nothing the component could not honestly know. Turning those four coordinates into EM-C4a's `request` — `makeOp('set-field', {kind:cardType, id:entityId}, {field, value})`, `rootKeyFor(cardType, entityId, field)`, `saveId` from the live store, then `runPlainEditCommand` — is store work, and EM-C4a mints no home for it. *Recommend: rule that the binder is **ONE exported function in EM-C4a's own `src/store/editSlice.js`** (`applyPlainEditIntent(get, set, intent)`, about fifteen lines, reusing `rootKeyFor` and `applyPlainEditToDraft` that already live there), carried by **EM-C4b or a one-row EM-C4a version 5** and consumed by EM-D1's mount. It keeps the op vocabulary inside the mutation path the walker declares, costs the eager closure nothing (the leaf is lazy), and leaves this member's contract untouched whichever member writes it.* |
| **R6** | ⭐ **RULED 146d: `set-field` for all four fields is RATIFIED; the typed per-field ops are the registry's, and the `set-npc-name` producer question stays an Edit-Mode inbox row.** ⚠ **ONE OP SHAPE FOR EVERY PLAIN EDIT, INCLUDING `status`.** `OP_TYPES` also carries `set-npc-status` (target `npc`, payload `{ status: enum(NPC_STATUS_POOL), cause }`), which is the DECREE vocabulary's typed act; EM-C4a's adapter never dispatches on op type, so the door's `set-field` is sufficient and uniform, and a generated dialog cannot carry a per-field op table without becoming the hand-written list clause 1 forbids. *Recommend: ratify `set-field` for all four fields at the first door, and record that the typed per-field ops are the REGISTRY's (EM-C4b), where an act carries a cause and becomes an event — which is also why `set-npc-status` declares a `cause` the plain door has nowhere to put.* |
