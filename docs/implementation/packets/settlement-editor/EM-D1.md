# `settlement-editor / tests-only mode` — EM-D1: THE EDIT-MODE SHELL (RECORD)

- **Status:** `LANDED`
- **Packet version:** `record-1` — written after the fact by the HISTORIAN seat (U3), 2026-09-23
- **Landed at:** `d09fd3cf727d23b8aee3822c2e8079574e0d9d39` — EM-D1 (cure): the editor's lazy boundary is narrated
- **Verified base:** `feat/em-d1-edit-mode-shell` at `ac14093c79a1c6715164a9c1f31eb221111842c5`
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: TO BE STAMPED BY THE CHAIR)

⛔ **A RECORD, NOT A DISPATCHABLE CONTRACT.** This member was built under TESTS-ONLY MODE — the owner, 2026-09-23 ~09:1x, *"Can we just have it where we only have the testing apparatus? As we did so many months ago?"* → *"Then do it and start building! Thank you"* (`docs/OPERATING_STANDARD.md` rule 39; stasis row S11). Under that word the estate's own ritual — packets, seals, placements, picks, flips, count provers, pre-proofs — is PARKED, never deleted, and the record is the branch's commit receipts, the progress board and this file. It pins no symbol, reserves no path, and may not be dispatched; the authority that governed the build is §1's brief and the design section that brief cites. The unfreeze train restores the ritual (U1, U3).

## §1 — The brief this member was built from

**The brief:** `briefs/launch/BUILD-EM-D1-tests-only.md` version 3, after judgments 264 and 268.

**The charge, in one sentence.** The shell mounts the first door behind the tier gate exactly as it stands (the gate is READ, never lifted — an owner-signed act, U16), through one lazy edge, with its copy in `en.js`; the cure narrates that boundary so the silent-Suspense pin stays 37.

## §7 — What moved (the commits, and their files)

| commit (on the train) | on its branch | subject |
|---|---|---|
| `b5ee6041c` | `1b632654f` | EM-D1: the edit-mode shell mounts the first door behind the gate exactly as it stands |
| `d09fd3cf7` | `88a66c59c` | EM-D1 (cure): the editor's lazy boundary is narrated |

| action | path |
|---|---|
| MODIFY | `src/App.jsx` |
| CREATE | `src/components/edit/EditModeShell.jsx` |
| MODIFY | `src/components/settlements/SettlementCard.jsx` |
| MODIFY | `src/copy/en.js` |
| TEST | `tests/build/vendorPdfLazy.test.js` |
| CREATE | `tests/components/editModeShell.test.jsx` |
| TEST | `tests/components/phoneChromeFloor.census.test.js` |
| TEST | `tests/domain/editDeclarations.test.js` |

## §9 — The acceptance cases, by title, as the commits record them

- **A1** — the landed gate refuses anon and free in the house shape, and the account it admits enters
- **A2** — the register wears a pencil on every DECLARED card and on no other, a plus on every roster, a seal with its herald line on each process card, the provenance note and Done
- **A3** — a pencil opens the door, and the door hands applyPlainEditIntent the four coordinates
- **A4** — the library row offers Edit behind the landed gate alone, and entering sets the editor mode and opens the same dossier
- **A5** — the App root reaches the shell through exactly one lazy edge and no static one
- **A6** — every string the two surfaces render comes from en.js through t(), and every key resolves
- **A7** — the shell spells no tier fact and mints no pencil marker, each scanner driven over a control
- **A8** — the mode the slice reads as OFF renders nothing at all

## §10 — The checks the builder ran

- `npx vitest run --pool=threads --maxWorkers=2 tests/build/vendorPdfLazy.test.js tests/components/editModeShell.test.jsx tests/components/phoneChromeFloor.census.test.js tests/domain/editDeclarations.test.js`
- `node scripts/implementation-packets.mjs validate`

## §12 — How it reached the train

Branch `feat/em-d1-edit-mode-shell`, built at `1b632654f`, `88a66c59c`; pushed as TRANSPORT (rule 24a — a lane push opens no pull request and merges nothing) and read on GitHub Actions (runs `35881309468`, `35887245219`). The chair read the run against the DECLARED reds — the two census cars under the `CENSUS_STASIS` switch (stasis S6), the lighting walker's title count, the observed-shape family's level at the branch's base (U37, closed by the schema-23 rung at `8cc2926d0` + `3b21ba791`) and the declared phone-floor rows (U25, re-recorded once at the tip by `56b085fc5`) — and MERGED it into the train branch `em-train-17-2026-09-23` by pick: `b5ee6041c`, `d09fd3cf7`. Nothing was pushed as a landing; the pull request, the merge to `master`, the deploy and every migration remain the OWNER's.

THE GATE WAS NOT LIFTED. Making Edit Mode visible to premium is an OWNER-SIGNED act, decided 2026-09-23 ~11:1x and reserved for the LAST ACT BEFORE LAUNCH (U16; judgment 268 withdrew 264b). The plus on the three roster roots landed as a DISABLED stub (U44) and is opened by EM-D1c v2 after EM-C4d and EM-E8 (judgment 277).
