# Settlement editor / EM-D0a — THE SHARED GLOW TOKEN: one recipe home in the eager `src/components/theme.js`, the arrow reading it, and the arrow's pixels proved unmoved by a pin this packet may not touch

- **Status:** `READY`
  ⚠ The status value above stands ALONE on its line because `parsePacketHeader` anchors the status
  row at end-of-line and takes `status` only when exactly one row matches. Every stamp, caveat and
  date goes on these continuation lines, never on the row.
  ⭐ **THE FIRST OF THE FIVE MEMBERS OF EM-D0**, the first door (design §20.4; the charter's EM-D0
  row). The partition and its measurement are `EM-D0.partition.md`: **D0a** (this) · **D0b** the
  tier gate and the DARK rule · **D0c** the halo-capable surface · **D0d** `PoolField` + `FreeField`
  · **D0e** `CardEditorDialog` on the NPC card. ⛔ The charter's row as written modifies SIX existing
  logic files against a cap of THREE; the partition puts each member at two.
  ⭐ **IT DEPENDS ON NOTHING AND CAN LAND TODAY.** It is the only member of the five that is
  independent of every waiting packet in the editor estate.
- **Packet version:** 2.1
  ⚠ **WHAT VERSION 2.1 CHANGED (one sentence).** The pre-proof's four-member validation run quoted
  `duplicate change path across packets: tests/build/vendorPdfLazy.test.js (EM-D0a, EM-D0b)`, which
  is NOT a row-keyed register and which TOOL-27 does not cure, so under the chair's judgment 74 —
  *within one train the budget's TEST row is carried by exactly ONE member, the train's BYTE-ARM
  HOLDER* — version 2's `TEST tests/build/vendorPdfLazy.test.js` row is REMOVED from both the
  capsule and §7 and replaced by a stated prediction naming EM-T9's holder, EM-D0b; nothing else
  moved, and the membership probe stays in `checks` unchanged.
  ⚠ **WHAT VERSION 2 CHANGED AND WHY (one sentence).** The pre-proof lane re-measured this packet
  at `e80a6a3f4` and found that `tests/design` is `ENFORCER_DIRS[1]` in
  `tests/lint/mutationCoverage.shared.mjs` — byte-unmoved since version 1's compile base — so
  version 1's "the mutation-coverage row is NOT OWED" was false and this packet's CREATE would have
  redded the TOTALITY arm at its landing; version 2 therefore adds that one row-keyed `REGISTER`
  row, adds interim rule 2's un-edited `TEST` row for the first-paint budget's own test with a
  stated byte bound, adds `tests/lint/mutationCoverageManifest.test.js` to the sealed `checks`,
  anchors §6.3's two bare-negative arms, drops one walker citation whose scope excludes this file,
  and re-points the revalidation sentence — and **no contract, no acceptance case and no required
  symbol moved**, only the one register row and the two check rows (the chair's judgment 73).
- **Verified base:** `em-t9-d0a-2026-09-21` at `7f0621fdb72f769a285502ff5481247c1f07b04b`
  ⚠ Left for the chair's promotion stamp. **The revalidation sentence the chair will use:**
  *"Re-measured at `e80a6a3f443e2f6a36e28fbebb72e98e46869fac` (read tip `read-tip-em-t9`,
  detached, `git status --short` EMPTY at the start and the end of the lane): the one CREATE target
  ABSENT (`git ls-files tests/design` lists seventeen files and none is `houseBloom`); all five
  `requiredSymbols` present VERBATIM at the paths in §5, each at `git grep -c -F` = 1, at
  `theme.js:76`, `ArrowControl.jsx:42`, `arrowHeader.test.jsx:389` and `:51`, and
  `vite.config.js:338`; `retiredSymbols` empty and proved so by the post-edit simulation; the
  preamble measured `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`; the eager
  first-paint set measured **269** by importing `vite.config.js`'s own export, with
  `src/components/theme.js` and `src/components/nav/ArrowControl.jsx` both already members and no
  third module; the literal `radial-gradient(closest-side` present in EXACTLY ONE file under
  `src/`; and `enumerateInvariants` = 716 matching the mutation manifest's 716 keys exactly, so
  this packet's CREATE is the 717th and its `REGISTER` row is what keeps that arm green."* ⛔ A `__BASE__` packet can never pass `validate:packets`; validation is downstream of
  this stamp, never a precondition of it.
- **Last revalidated:** left for the chair, with the sentence above.
- **Depends on:** `NONE`. ⭐ **THIS PACKET FLOATS.** It imports no file a sibling creates, names no
  symbol that does not exist today, and its `_pendingRequiredSymbols` is absent because there is
  nothing pending. It may be promoted and landed independently of EM-A1, EM-A2a, EM-A2b, EM-B1a,
  EM-B1h, EM-B1i, EM-B2a1, EM-C4a and of the other four EM-D0 members.
- **Collision group:** `NONE` measured against the waiting estate — no packet at any status names
  `src/components/theme.js`, `src/components/nav/ArrowControl.jsx` or `tests/design/houseBloom.test.js`.
  ⚠ `src/components/theme.js` is a **HUB FILE** (49 test files name it), which is a `checks`-scoping
  fact (§10), not a collision.
- **Commit authority:** edits only; the chair commits.
- **Baseline posture:** measured. The eager first-paint graph (269 members, by IMPORTING
  `vite.config.js`'s own export, reading no `dist`); `ARROW_GLOW`'s definition and its single
  consumer read at source; `PARCH_100`'s home; the existing pixels pin read line by line; the
  single-home scan over `src/`; the governing test set by `git grep -l -F`. Receipts in
  `EM-D0a.evidence.md`; the partition's own measurements in `EM-D0.partition.md`,
  `02-FIRSTPAINT.md` and `03-PHONE-AND-DOOR.md`.
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: TO BE STAMPED BY THE CHAIR;
  measured at this read tip as
  `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`).
  > **Interim compile rules:** `COMPILE-RULES.interim.md` (the chair, 2026-09-20) — the fifth
  > amendment's eleven rules, obeyed before they land. Where this packet and `EM-PREAMBLE.md`
  > (SHA-256 `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`) disagree, the
  > interim rules govern and this row is the record of it.

---

## §1 · Reconciled authority

| source | section + item | what it rules here |
|---|---|---|
| design `DESIGN_EDIT_MODE_AND_DECREES.md` | **§20.1** (which GOVERNS where §3 disagrees) | *"One recipe home: `ARROW_GLOW` (`src/components/nav/ArrowControl.jsx`) is lifted to a shared token beside the rail's gold, and both the arrow and the editor read it."* |
| the same | **§3**, the halo bullet | *"lifted to ONE shared home so the door and the forge glow alike by construction — never a second recipe, never a new hex"*; and (1) *"LIGHT, NOT ELEVATION"* |
| charter `EDIT-MODE-TRAIN.md` | the **EM-D0** row | `C … the shared glow token's home; M src/components/nav/ArrowControl.jsx (reads the shared token; pixels unmoved — pinned)` |
| the same | the **EM-T7** row | EM-D0 lands in train EM-T7, after EM-C4a |
| interim rules | **2** | a rise in the eager first-paint closure is a STOP for the OWNER; membership is proved by a probe that imports the exported set |
| interim rules | **9** | every register figure is a DELTA; a predicted red spells its SHAPE |
| interim rules | **10** | the browser suite governs a packet that touches a surface — §10 names what it found |

⚠ **ONE RECONCILIATION.** The charter's row lists the glow token's home under **C** (create). This
packet does **not** create a file for it: the measured home is the EXISTING, already-eager
`src/components/theme.js`, and a new leaf would be **+1 eager first-paint module**, which is the
owner's stop. The row's intent — *one home* — is honoured exactly; its `C` is honoured as a `MODIFY`,
and §2.2 is the measurement. **The chair rules on the charter row.**

---

## §2 · Outcome

### §2.1 What this packet delivers
ONE exported recipe, `houseBloom(tone)`, in `src/components/theme.js`, and `ARROW_GLOW` rewritten to
read it. **Nothing else.** No component renders differently, no token changes value, no surface gains
a halo. The halo itself — the forge's GOLD rim over the arrow's PARCHMENT bloom — is **EM-D0c's**,
because D0c is the member that has a consumer for it, and **this packet mints no unconsumed export.**

### §2.2 ⛔ THE FIRST-PAINT MEASUREMENT, AND WHY THE HOME IS `theme.js`
Executed at `bdbf7c89c` with `fp-d0.mjs`, which takes its tree from `$READ_TIP` **with no default**,
PRINTS the tree it read, IMPORTS `vite.config.js`'s own exported set and reads no `dist`:

```
TREE READ: …/scratchpad/read-tip-em-t7
EXPORT KIND: Set
EAGER COUNT: 269
   EAGER   src/components/nav/ArrowControl.jsx
   EAGER   src/components/theme.js
   not     src/design/organic/ink.js
```

⚠ **269, not the 268 that `EM-PREAMBLE.md` §P2 row 11 and four landed packets still quote** — the
absolute has drifted, independently confirming EM-B1c's §7 report at the same tip. Every figure in
this packet is therefore a **DELTA**, and the absolute is the chair's at promotion.

`ArrowControl.jsx:39` reads `import { INK, PARCH_100 } from '../theme.js';` — **the edge already
exists**, and `PARCH_100` is `theme.js:76`. So the lift is **+0 eager modules and +0 new eager import
edges**; only the specifier list grows by one name. The two rejected homes, with their measured cost:
a new leaf `src/design/glow.js` is **+1 eager module** (the owner's stop); `src/design/tokens.js` is
eager but `ArrowControl.jsx` reaches it only through `theme.js:81`'s re-export, so a direct edge
would be a **NEW** edge. ⛔ The BYTE delta is not zero by construction — one exported template
function moves between two already-eager modules — so §7 **prices** it and §9 does **not assert** it;
the chair sums it from ONE real build (interim rule 2).

### §2.3 ⭐ WHY THE PIN IS A FILE THIS PACKET MAY NOT TOUCH
The charter asks for the arrow's pixels *"pinned unmoved"*. That pin **already exists**, at
`tests/components/arrowHeader.test.jsx:389-390`, and it pins `ARROW_GLOW`'s spelling by value and
its rendered output by substring. This packet therefore does not write one: it carries the existing
assertion as a **`requiredSymbols` row** and **does not modify that file at all**. A pin the packet
cannot edit, asserting a value the packet must not move, is stronger than any pin the packet could
write — and A2 makes editing it a named DEVIATION.

### §2.4 In scope / out of scope
**In:** the one recipe; `ARROW_GLOW` reading it; one new test file with the recipe's five arms and
its anti-vacuity denominator.
**Out:** ⛔ the editor's halo constants and the rim (EM-D0c) · any change to `PortablePopup` or
`BottomSheet` (EM-D0c) · `TIER_GATE.premium.*` and the DARK rule (EM-D0b) · every component under
`src/components/edit/` (EM-D0d, EM-D0e) · any change to `ARROW_GLOW`'s VALUE, to the arrow's
geometry, to its ring, to its hover gating or to `tests/components/arrowHeader.test.jsx`.

---

## §3 · Hard scope budget

| limit | this packet | cap |
|---|---|---|
| behaviour families | 1 (one recipe has one home) | 1 |
| new persisted record families | 0 | ≤1 |
| feature flags | 0 | ≤1 |
| user-facing surfaces | **0 — no surface renders differently** | ≤1 |
| direct production consumers | **1** (`ArrowControl.jsx`; EM-D0c is the second, not yet written) | ≤2 |
| new logic-bearing production leaves | **0** | ≤2 |
| existing logic-bearing production files modified | **2** | ≤3 |
| handwritten files total | **4** — the two `src/` files, the new test, and ONE appended row in `scripts/mutation-coverage-manifest.json` | ≤12 |
| new/changed effective production lines | **≈ 6** (`theme.js` +5 incl. the JSDoc; `ArrowControl.jsx` net +0: one line replaced, one specifier added) | ≤400 |
| each new production leaf | n/a — none | ≤250 each |
| shared / hot-file delta | `src/components/theme.js` **+5** | ≤15 |
| named acceptance cases | **6** | ≤8 |

Overrides approved before dispatch: **NONE — none is needed.** This is the smallest member of the
five and it asks for nothing.

**Hot files:** `src/components/theme.js` is a HUB (49 test files name it). The delta is +5 effective
lines and is **purely additive** — no existing export's declaration, value or order moves — so the
headroom question does not arise; the consequence is a `checks`-scoping rule (§10), not a budget one.

---

## §4 · Sealed dispatch and preflight

A dry read of `scripts/implementation-session.mjs`'s checks at the tip:
- **branch name** — the chair's; the packet asserts nothing.
- **ancestry** — the chair sets `Verified base` to the integration tip at promotion; the `__BASE__`
  placeholder is replaced there and nowhere else.
- **substrate unchanged since the verified base** — of the five `requiredSymbols` paths, this packet
  writes exactly one (`src/components/nav/ArrowControl.jsx`), and its required text survives the
  edit verbatim (§5's post-edit simulation).
- **CREATE targets absent** — `tests/design/houseBloom.test.js` verified absent at `bdbf7c89c`
  (`git ls-files tests/design` lists seventeen files and none is it).
- **git-clean** — the lane wrote nothing in any tree; `git status --short` in `read-tip-em-t7` was
  EMPTY at the start and at the end.
⚠ **SEALED DISPATCH WANTS THE WORKTREE ON THE VERIFIED BRANCH.** The build lane holds the
integration branch in its own worktree; the chair's worktree stays detached at the tip.

---

## §5 · Verified tree contract

Every row proved at `bdbf7c89c` by `git grep -c -F` = **1** (receipts in `EM-D0a.evidence.md`
§E1–E5).

| what | path | symbol (verbatim) | why it must not move |
|---|---|---|---|
| the tone the recipe takes | `src/components/theme.js` | `export const PARCH_100 = L.PARCH_100;` | the recipe's only argument at its only call site; the packet is additive and must not disturb it |
| ⛔ the lifted symbol | `src/components/nav/ArrowControl.jsx` | `export const ARROW_GLOW` | **the packet WRITES this path.** The required text is a PREFIX of the declaration, so it survives the right-hand side's replacement verbatim — which is why no retirement is owed |
| ⭐ the pixels pin, by value | `tests/components/arrowHeader.test.jsx` | ``expect(ARROW_GLOW).toBe(`radial-gradient(closest-side, color-mix(in srgb, ${PARCH_100} 34%, transparent), transparent)`);`` | **PRESERVE UNTOUCHED.** §2.3 — the packet's whole claim is that this assertion passes unedited |
| ⭐ the pin's import | `tests/components/arrowHeader.test.jsx` | `import { ARROW_GLOW } from '../../src/components/nav/ArrowControl.jsx';` | the forwarding export must keep resolving from its ORIGINAL path, or the pin cannot run |
| the eager set | `vite.config.js` | `export const EAGER_FIRST_PAINT_MODULES` | the membership probe imports it; a replica seeded by hand would drift silently (EM-PREAMBLE §P2 row 11) |

⛔ **`import { INK, PARCH_100 } from '../theme.js';` (`ArrowControl.jsx:39`) IS DELIBERATELY NOT A
REQUIRED SYMBOL.** The packet changes that line — the specifier list gains `houseBloom` — so naming
it would make the packet refuse against its own post-edit validation. Recorded here rather than
silently omitted.

**`retiredSymbols`: EMPTY**, and proved row by row (pre-proof step 10). Rows 1, 3, 4 and 5 sit at
paths this packet does not write. Row 2's path is written, and its text is a prefix of the line that
is edited, so it is present verbatim after the packet's own edits. **No other packet, LANDED or
waiting, carries a row for any of these five (path, symbol) pairs**, so nothing is owed a discharge.

---

## §6 · Exact contracts

### §6.1 `src/components/theme.js` — the one recipe home

```js
/**
 * THE HOUSE BLOOM — one recipe, read by the arrow's glow (nav/ArrowControl.jsx) and, from
 * EM-D0c, by the editor's halo. Design §20.1: "ARROW_GLOW is lifted to a shared token beside
 * the rail's gold, and both the arrow and the editor read it."
 *
 * A radial light at the CENTRE of its box and none at its edge, through the house color-mix
 * idiom — never a translucent literal, never a new hex. It returns a BACKGROUND value and
 * never a shadow: print has no z-axis (src/design/organic/ink.js), so the halo built on this
 * recipe is LIGHT and cannot become an elevation by accident.
 *
 * @param {string} tone a house colour token (PARCH_100, GOLD, …)
 * @returns {string} a CSS background value
 */
export function houseBloom(tone) {
  return `radial-gradient(closest-side, color-mix(in srgb, ${tone} 34%, transparent), transparent)`;
}
```

⛔ **EXACT, WITH NO DISCRETION.** The `34%` is the arrow's own figure, carried across unchanged — it
is not a parameter, because a second percentage would be a second recipe, which §3 forbids in those
words (*"never a second recipe"*). Measured: `34%, transparent` occurs in exactly ONE file under
`src/` today. ⛔ The function is PURE: no clock, no random number, no locale, no module state.

### §6.2 `src/components/nav/ArrowControl.jsx` — the reader

```js
import { INK, PARCH_100, houseBloom } from '../theme.js';   // the specifier list gains one name

/** The hover glow: PARCH_100 light at the centre of the glow box, none at its edge. */
export const ARROW_GLOW = houseBloom(PARCH_100);
```

⛔ **THE EXPORTED STRING'S VALUE IS BYTE-IDENTICAL** and `export const ARROW_GLOW` survives verbatim,
so the symbol keeps its original home and every existing importer resolves unchanged. ⛔ **Nothing
else in the 129-line file moves:** not the JSDoc block (lines 1-36), not `hovers()`, not
`keyboardFocused()`, not the component, not the ring at line 117, not the glow span's
`background: ARROW_GLOW` at line 111.

### §6.3 `tests/design/houseBloom.test.js` — the recipe's five arms

Six straight-line literal `it`s under ONE literal `describe`; the openers come from this file's own
`vitest` import and each word is bound **exactly once** (⛔ no variable, parameter or alias named
`it`, `test` or `describe` — 26 of the estate's 384 parked files park for that reason alone); no
bare seed loop (collect, then assert once).

1. **byte identity** — `houseBloom(PARCH_100)` equals the arrow's pinned spelling, character for
   character, built from the same interpolation.
2. **the single home** — a source scan over every `.js`/`.jsx` under `src/` finds
   `radial-gradient(closest-side` in exactly one file, and it is `src/components/theme.js`. The
   denominator is derived by reading the tree, never transcribed.
3. **no new hex** — `houseBloom`'s source text contains no `#` colour literal and no `rgba(`.
   ⛔ **THIS ARM IS A BARE NEGATIVE AND MUST BE ANCHORED.**
   `tests/lint/negativeAssertionAnchor.walker.test.js` governs `tests/design` (its frozen map
   already carries four `tests/design/*` rows) and convicts `not.toContain(` / `not.toMatch(` /
   `not.toHaveProperty(` unless the site carries `// anchored: <why this cannot go vacuous>` —
   **ON ONE LINE**, or with `anchored:` on the comment's LAST line (the MARKER RULE; a wrapped
   marker anchors nothing). Write the marker on the assertion's own line.
4. **the argument is read** (NEGATIVE CONTROL) — `houseBloom('red') !== houseBloom('blue')`, and each
   output contains its own argument. Without this arm a body returning a constant would pass 1–3.
5. **light, never elevation** — the output contains `radial-gradient` and contains neither a `px`
   offset nor the token `shadow`. ⛔ **ALSO A BARE NEGATIVE:** both `.not.` sites take the
   one-line `// anchored:` marker under arm 3's rule.
6. **anti-vacuity** — the scanned denominator of arm 2 is non-empty and contains
   `src/components/nav/ArrowControl.jsx`, so a scan that found nothing cannot read as a pass.

### §6.4 Determinism, lifecycle, receipts
- **Hash / fork key:** NONE. The packet draws no random number, mints no id and reads no clock.
- **Ordering:** n/a — one pure function of one argument.
- **Absence vs empty vs null:** `houseBloom` is called at exactly one site, with `PARCH_100`, which
  §5 proves present. It is not defended against `undefined`, because a call with no tone is a
  programming error the type annotation names and no runtime path can produce.
- **Lifecycle, every path:** *create / read / persist / regenerate / undo / import / migrate* — ⛔
  **NONE OF THEM.** This packet touches no record, no save, no store and no persisted key. It is a
  string constant's definition site moving between two modules.
- **Receipts:** none. No surface, no command, no chronicle line.
- **Golden posture: UNCHANGED.** `tests/property/generatorGoldenMaster.test.js` and
  `tests/property/dossierProseManifest.test.js` do not move; generation is not touched.

---

## §7 · Exact change manifest

Generated from `EM-D0a.manifest.json` and proved SET-EQUAL to it (interim rule 6; the executed line
is in `EM-D0a.compile.report.md` §A).

| Action | File | Symbol / region | Max delta | Coding instruction |
|---|---|---|---:|---|
| `MODIFY` | `src/components/theme.js` | `houseBloom` | **+5 eff** | Mint §6.1 verbatim beside `PARCH_100`. ⛔ Additive only: no existing export's declaration, value or order moves. ⛔ Mint no halo constant here — that is EM-D0c's, which has a consumer for it. |
| `MODIFY` | `src/components/nav/ArrowControl.jsx` | `ARROW_GLOW` | **net +0 eff** | §6.2: one specifier added to line 39, one right-hand side replaced on line 42. ⛔ Nothing else in the file moves. |
| `CREATE` | `tests/design/houseBloom.test.js` | the six arms | **≤60; ≈45 est.** | §6.3. Straight-line literal `it`s under one literal `describe`; the denominator read from source. Both `.not.` arms carry the one-line `// anchored:` marker. |
| `REGISTER` | `scripts/mutation-coverage-manifest.json` | `invariants['tests/design/houseBloom.test.js']` | **+1 row** | ⭐ **OWED, and version 1 said otherwise.** `tests/design` is `ENFORCER_DIRS[1]`. ONE row, `kind: 'rationale'` with a reason of **more than 40 characters** (the meta-test's own threshold). ⛔ `kind: 'uncovered'` is UNLAWFUL here: the live uncovered count is **186** and `uncoveredBaseline` is **186**, and the SHRINK-ONLY arm fails in BOTH directions, so a 187th reds. ⛔ **SURGICAL, NEVER RE-SERIALISED WHOLE:** insert immediately AFTER the close of `"tests/design/archMeshBudget.test.js"` and immediately BEFORE `"tests/docs/abuse-model-paths.test.js"` — a pure **+N / −0** diff, anchored on key NAMES and never on line numbers, and textually distinct from EM-A1's and EM-B1h's rows, which both key under `tests/lint/`. ⛔ Do not touch `uncoveredBaseline`. |

Generated artifacts: **NONE**.

### The deferred rows — PREDICTIONS, named in no `checks` and in no `changeManifest`

> ⭐ **THE TRAIN'S BYTE ARM (the chair's judgment 74, version 2.1).** **FIRST-PAINT BYTES: predicted ≤ 120 B (estimated +55 B ×2) against an arm with 8,733 B of room; this packet carries NO row on `tests/build/vendorPdfLazy.test.js`: train EM-T9's byte-arm holder is EM-D0b, whose TEST row and the terminal's ONE real build prove the summed price of the whole train (the chair's judgments 72 and 74).** The membership
> probe in §10's sealed `checks` stays exactly as it is — it IMPORTS the eager set, reads no
> `dist` and therefore can never skip — and it proves MEMBERSHIP (**+0 eager modules**), which is
> this packet's own claim; the BYTES are the holder's and the terminal's.

> `docs/content/wiring-census.json` — **+0** (interim rule 1). This packet CREATEs no `.js` under
> `src/generators/**` or `src/domain/**` — the two roots measured against — because its only `src/`
> writes are under `src/components/`. `stamp.files`, `stamp.candidateLeaves` and `totals.*` are all
> UNMOVED. ⛔ Regenerated whole at the train's terminal BY THE CHAIR; the path appears nowhere here.

> `tests/lint/.lighting-census-baseline.json` — **a named INTERIOR RED, delta only**, in the
> register's own order: **files +1 · parked +0 · credited +1 · titles +6 · suiteTitles +1.** From the
> ONE new test file with six literal `it`s under one literal `describe`. The predicted red's SHAPE is
> `expected <N+1> to be <N>` on the first moved figure (the walker stops there), never a numeral. ⛔
> Regenerated at the train's terminal BY THE CHAIR; this member never sets
> `LIGHTING_CENSUS_REFREEZE` and never edits the baseline. ⚠ **The credited/parked split is a
> PREDICTION until the build lane prints `parkReasonsFor` over the new file** (pre-proof step 14b);
> if it parks, the delta is `parked +1` and `titles +0`, and the receipt says which it was.

> ⛔ **CORRECTION, VERSION 2 — `scripts/mutation-coverage-manifest.json` IS OWED, AND IS NOW A §7
> ROW, NOT A DEFERRED ONE.** Version 1 wrote "NOT OWED" on the premise that `tests/design` is not an
> enforcer directory. **The premise is false**, and was false when it was written:
> `tests/lint/mutationCoverage.shared.mjs` declares `ENFORCER_DIRS` with `'tests/design'` as its
> **SECOND** entry, and `git diff --stat <compile base> e80a6a3f4` over that file, over
> `tests/lint/mutationCoverageManifest.test.js` and over the manifest itself is **EMPTY** — nothing
> drifted; the compile read the register wrongly. Measured with the CREATE simulated present,
> `enumerateInvariants` yields `missing = ["tests/design/houseBloom.test.js"]`, which is exactly
> what the TOTALITY arm refuses ("new invariants need a mutation or a rationale … Do NOT add it as
> uncovered — the gap list only shrinks"). ⭐ Because the row is **row-keyed**, it is the ONE lawful
> shared path inside a parallel train: EM-A1 and EM-B1h already carry `REGISTER` rows on this same
> file under `tests/lint/` keys, and this member is the THIRD, under a `tests/design/` key.

---

## §8 · Ordered coding sequence

1. Read `src/components/theme.js` whole and `src/components/nav/ArrowControl.jsx` whole (129 lines)
   before the first edit — the enclosing scope before any change.
2. Mint §6.1 in `theme.js`, beside `PARCH_100`. Run nothing yet.
3. Rewrite `ArrowControl.jsx`'s line 39 specifier list and line 42's right-hand side (§6.2).
4. ⭐ **RUN `tests/components/arrowHeader.test.jsx` NOW, BEFORE WRITING THE NEW TEST.** It is the
   negative control and it is already written: if the value moved by one character it reds here,
   with no test of this packet's own authorship involved. ⛔ If it reds, STOP — do not edit it.
5. Write `tests/design/houseBloom.test.js` (§6.3), arm 4 (the negative control) **first**, so the
   other arms are written against a detector already proved to read its argument.
6. Run the sealed `checks` in §10's order, the validator last.
7. Print `parkReasonsFor` over the new file and record whether it credited or parked (§7's deferred
   lighting row), then run the lighting walker ALONE as an instrument so its five figures are
   RECORDED rather than discovered.

---

## §9 · Acceptance matrix

| # | claim | how it is proved |
|---|---|---|
| **A1** | **Byte identity, not likeness.** `houseBloom(PARCH_100)` is character-for-character the string `ARROW_GLOW` held at the base | asserted in `tests/design/houseBloom.test.js` against the same interpolation the arrow's pin spells, and a second time from the other side by `arrowHeader.test.jsx:389` running UNMODIFIED |
| **A2** | **The arrow is unmoved in pixels.** `tests/components/arrowHeader.test.jsx` passes with ZERO edits to it | its line 51 import still resolves; line 389 still equals the pinned spelling; line 390's rendered span still contains `radial-gradient(closest-side`. ⛔ **Editing that file at all is a named DEVIATION** |
| **A3** | **One home, from source, both directions.** `radial-gradient(closest-side` is in exactly ONE `src/` file, and it is `theme.js` | at the base the same scan finds exactly one and it is `ArrowControl.jsx` (**measured: 1 of 1**) ⇒ the arm is a DELTA of HOME, not of count: the recipe moved and did not multiply |
| **A4** | **No new hex, and the function reads its argument** | (a) the source text carries no `#` literal and no `rgba(`; (b) NEGATIVE CONTROL: `houseBloom('red') !== houseBloom('blue')` and each output contains its own argument |
| **A5** | **First paint: +0 eager modules, as a DELTA** | the membership probe imports the exported set, reads no `dist` and cannot skip; size N before and after, both homes members before and after, no third module entering. ⛔ The BYTE delta is PRICED in §7, never asserted here |
| **A6** | **Light, never elevation** | `houseBloom`'s output contains `radial-gradient` and contains neither a `px` offset nor the token `shadow` — design §3 (1) made structural on the recipe itself |

---

## §10 · Verification commands

```sh
# THE SEALED CHECKS (the capsule's `checks`, in this order)
npx vitest run --pool=threads --maxWorkers=2 tests/design/houseBloom.test.js tests/design/contrast.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/components/arrowHeader.test.jsx tests/components/accountMenuRows.test.jsx

# the walker that governs the CREATE's directory, because tests/design is ENFORCER_DIRS[1]
npx vitest run --pool=threads --maxWorkers=2 tests/lint/mutationCoverageManifest.test.js

# the membership probe — it IMPORTS the exported set, reads no dist, and therefore can never skip
node -e "const{pathToFileURL}=require('node:url');import(pathToFileURL('vite.config.js').href).then(m=>{const S=[...m.EAGER_FIRST_PAINT_MODULES];const want=['src/components/theme.js','src/components/nav/ArrowControl.jsx'];const have=want.filter(p=>S.some(x=>String(x).endsWith(p)));console.log('EAGER_FIRST_PAINT_MODULES:',S.length,'| both homes already eager:',have.length,'of',want.length);if(have.length!==want.length)process.exit(1);})"

# the seal, LAST
node scripts/implementation-packets.mjs validate
```

**How the sealed set was computed (interim rule 8).** `src/components/theme.js` is a HUB FILE —
`git grep -l -F theme -- tests` returns **50**, measured at `e80a6a3f4` (version 1's **49** was its
own compile base's figure and is superseded, not contradicted) — so by the chair's ruling 3 on EM-B2a4 the sealed set
takes the grep of the **CHANGED SYMBOLS** only: `ARROW_GLOW` → **1** hit
(`tests/components/arrowHeader.test.jsx`); `ArrowControl` → **3** hits (+ `accountMenuRows.test.jsx`,
`tests/design/contrast.test.js`). `houseBloom` is a NEW symbol and names no existing test — stated,
not silently omitted. ⭐ **AND, VERSION 2,** interim rule 8's other clause: *every walker that
governs a directory in which you CREATE a test file.* `tests/design` is `ENFORCER_DIRS[1]`, so
`tests/lint/mutationCoverageManifest.test.js` is named **as a FILE** and joins the sealed set — the
same file EM-A1 and EM-B1h both already seal. ⛔ Never the `tests/lint` directory; ⛔ never the
lighting walker; ⭐ the validator last.

```sh
# ⛔ THE BUILD LANE'S INSTRUMENTS — NEVER SEALED CHECKS
#  (a) tests/design WHOLE — the CREATE directory (the chair's addendum after runs 17/18/19: a CREATE
#      under tests/<dir> opts into EVERY walker that governs tests/<dir>). The walkers that govern it,
#      measured: sovereigntyLightingContract, negativeAssertionAnchor, seedLoopTotality,
#      swatchResolves, and mutationCoverage.shared.mjs's roster.
npx vitest run --pool=threads --maxWorkers=2 tests/design
#  (b) tests/components WHOLE — the largest importing directory of the hub file (9 of the 49)
npx vitest run --pool=threads --maxWorkers=2 tests/components
#  (c) the EXCLUDED tests/lint directory run, which MUST EXIT 0
npx vitest run --pool=threads --maxWorkers=2 tests/lint --exclude=tests/lint/sovereigntyLightingContract.walker.test.js
#  (d) the lighting walker ALONE, as the measured, named INTERIOR RED (§7's deferred row)
npx vitest run --pool=threads --maxWorkers=2 tests/lint/sovereigntyLightingContract.walker.test.js
#  ⛔ DO NOT set LIGHTING_CENSUS_REFREEZE. The refreeze is the chair's, at the terminal.
#  (e) parkReasonsFor over the new file, printed, before the lighting delta is believed
```

**⛔ THE BROWSER SUITE (interim rule 10): NOT GOVERNING FOR THIS MEMBER.** It changes no route, no
`data-testid`, no accessible name, no nav, header, footer, bottom bar or page chrome — the rendered
markup is byte-identical by A1 and A2. Measured, not assumed — and **re-spelled in version 2**, because version 1's one-command form put an
alternation inside a NON-`-E` grep, whose zero is a false zero: `git grep -c -E
'arrow-glow|ArrowControl|arrow_glow' -- e2e` → **ZERO**, and, per name,
`git grep -l -F` over `ARROW_GLOW`, `ArrowControl`, `arrow-glow` and `data-sf-arrow-glow` → **ZERO
files each**. ⚠ Two specs do exercise the arrow HEADER by a different prefix —
`e2e/arrow-header.spec.js` and `e2e/pinned-footer.spec.js` select `.sf-arrow-hang` and
`[data-sf-arrow-paint]` — and neither names the glow, the export or either file this packet writes;
by A1 and A2 the rendered markup is byte-identical, so they are named here for the chair and are
**not** governing specs. ⭐ The member that owes the
chromium project is **EM-D0e**, whose governing specs are named in `03-PHONE-AND-DOOR.md` §4.4.

**THE BYTE ARM, STATED AND NOT ROWED.** **FIRST-PAINT BYTES: predicted ≤ 120 B (estimated +55 B ×2) against an arm with 8,733 B of room; this packet carries NO row on `tests/build/vendorPdfLazy.test.js`: train EM-T9's byte-arm holder is EM-D0b, whose TEST row and the terminal's ONE real build prove the summed price of the whole train (the chair's judgments 72 and 74).**

**Registers that must NOT move:** the two goldens above; `tests/copy/.voice-mechanics-*.json` (this
member renders no reader-facing string, so no baseline row shifts);
`docs/content/wiring-census.json`; and, inside
`scripts/mutation-coverage-manifest.json`, **`uncoveredBaseline` and every row this packet does not
add**. ⛔ **VERSION 2 REMOVED that whole file from this list**: version 1 listed it as immovable on
the same false premise §7's correction records, and it is now a `REGISTER` row of exactly one key.

---

## §11 · Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and §P8, **stop** if: `tests/components/arrowHeader.test.jsx`
reds at step 4 or would have to be edited to pass; `houseBloom(PARCH_100)` is not byte-identical to
the base's `ARROW_GLOW`; the eager first-paint set's size changes by any amount, or a third module
enters it, or either home leaves it; the single-home scan finds `radial-gradient(closest-side` in
more than one `src/` file after the edit; a second percentage or a second recipe would have to be
written; a new hex or a translucent literal would have to be minted; `ARROW_GLOW` would have to stop
being exported from `src/components/nav/ArrowControl.jsx`; a halo constant or a rim would have to be
minted here to make anything work (that is EM-D0c's, and its arrival here would mean an unconsumed
export); `PARCH_100`'s declaration at `theme.js:76` has changed; a golden or the prose manifest moves;
`scripts/mutation-coverage-manifest.json` would have to be re-serialised whole rather than gain one
surgical row, or its `uncoveredBaseline` would have to move, or the new row would have to be
`kind: 'uncovered'`; the live uncovered count is not **186** when the build lane reads it; or
another EM-T9 member's `REGISTER` row is found at a `tests/design/` key.

---

## §12 · Receipt, questions and handoff

**The SET-EQUAL proof (interim rule 6)** — `setEqual.mjs` over this file and the capsule; the
executed line is in `EM-D0a.compile.report.md` §A.

**What the next member may rely on.** EM-D0c imports `houseBloom` from `src/components/theme.js` and
composes the halo as a `GOLD` rim over `houseBloom(PARCH_100)`; it mints the two halo constants
there, at the moment it has a consumer, and carries the halo's own pin (*the colour derives from the
shared token and no other surface carries it*) as a source scan over `src/`.

**Questions only the chair can answer** are carried in `EM-D0.partition.md`'s closing section and in
the lane's final message: the rename; the fifth member's ratification; the ONE vocabulary for the
three `edit*` names; `BottomSheet`'s `ELEV[3]`; the `'share'` numeric control's true owner; the
`e2e/` governing set.

**⭐ THE COUNT, STATED ONCE AND THE SAME EVERYWHERE (the EM-B3d probe's law).** **SIX** acceptance
cases (§9 A1–A6, capsule `acceptanceCases` = 6). **SIX** arms in the one CREATE, and §7's CREATE row
says "the six arms" in those words. **SIX** literal `it`s under ONE literal `describe` (§6.3's
numbered list). Therefore `titles` **+6** and `suiteTitles` **+1**, which is what §7's deferred
lighting row states and what §12's receipt will be read against. ⭐ Every case has a declared home
and no case is homeless: A1 → arm 1; A3 → arm 2; A4(a) → arm 3; A4(b) → arm 4; A6 → arm 5; arm 6 is
the anti-vacuity denominator; **A2's home is `tests/components/arrowHeader.test.jsx` running
UNMODIFIED** and **A5's home is the sealed membership probe in `checks`** — neither adds an `it`,
and that is why six cases cost six titles and not eight.

**Noticed and not touched** — `EM-D0a.compile.report.md` §G.

**Completion receipt owed at the landing:** base sha · seal identity · the exact changed files with
effective-line deltas measured by eslint's `Linter` · A1–A6 executed · the mutants planted,
convicted and restored digest-exact · focused commands, exits and counts · both typecheck
configurations · the census tuple before and at the tip **with the interior red quoted verbatim and
`parkReasonsFor`'s printed output for the new file** · generated artifacts `NONE` · the `REGISTER` row quoted with
`git diff -U0 -- scripts/mutation-coverage-manifest.json` showing **+N / −0 and `uncoveredBaseline`
unmoved at 186** · `tests/lint/mutationCoverageManifest.test.js` green with its own exit line ·
deviations `NONE | STOP` · judgment calls: `NONE`.
