# `settlement editor / record merge` — EM-R0f: the economy fingerprint moves down a layer, and the move is proven invisible

- **Status:** `LANDED`
- **Landed at:** `d7d6ceef7f30fab49a4840c1a968e6842c9e6745` — the economy fingerprint moves down a layer to src/data/economyFingerprint.js and the move is proven invisible (version 3: its byte row withdrawn under the one-holder rule; every importer, golden and baseline re-measured; the domain-generators boundary walker and the power-economy freshness test re-addressed; lighting +0); judgment 161 a–e; re-sealed at the re-base (judgment 189)
- **Packet version:** `3`
  - **Version 3 is the train EM-T14 pre-proof** (Opus PRE-PROOF seat, session cce01f87, 2026-09-22),
    re-measured WHOLE against train EM-T12's cured tip `bfca6b7d6`. Six facts moved and one was
    refuted. (1) ⛔ **The byte-arm row LEAVES this packet.** §P2.11 as the sixth amendment corrects
    it (judgment 129) gives a train's budget row to ONE member, the byte-arm holder **named by the
    chair at the placement**, and a member compiles as if it is NOT the holder unless the brief says
    it is. The `TEST` row on `tests/build/generationWorkerLazy.test.js` is therefore withdrawn to a
    PREDICTED DELTA with a MEMBERSHIP PROOF (§3.1), which dissolves the ORDER EDGE below: the
    measured intersection with EM-R0d is now EMPTY and all three EM-T14 members are path-disjoint.
    (2) ⛔ **A past-EOF citation of this packet's own making was found and cured** — the file is
    **367 raw lines today and 338 after the move (executed)**, so the PRE-MOVE address version 2 spelled for the freshness caller would have been a
    NEW past-EOF citation in a LIVE document and `sourceCitationIntegrity.walker.test.js`'s ARM 2 is
    a gating, never-growing ratchet; every address into that file is now the POST-EDIT one, read off
    the simulated remainder. (3) ⭐ **TOOL-31 (`e343ca859`) already landed §6.4 item 1** with exactly
    the figure this packet proposed, and moved every address §5/§6.4 cited; item 1 is struck and the
    rest are refreshed. (4) The step-10 census and the `path:line` grep were both wrong and are
    replaced by their measurements. (5) §P2.19-21's instrument list is run and added to §7.1 as
    rows. (6) Every absolute is now a delta with an as-of mark. ⛔ **No contract clause, no
    acceptance case and no budget figure moved** — the two eslint effective-line figures and the
    leaf's 22 reproduce EXACTLY at the tip.
- **Verified base:** `em-t14-r0f-2026-09-22` at `35a031ac7adf2a6614feb48036a6bfddb5923fd9`
- **Last revalidated:** `35a031ac7adf2a6614feb48036a6bfddb5923fd9` — stamped by the chair at placement.
- **Depends on:** nothing. EM-R0f is a leaf move with no predecessor. ⭐ **EM-R0c depends on IT** (charter, amendments of 2026-09-20 02:30: the family's order is R0a · R0d · R0b v3 · **R0f** · R0c · R1–R5 · R6 · R7), because R0c's merge recomputes the fingerprint from `src/domain/edit/` and must not import `src/generators/` to do it.
- **Collision group:** ⭐ **EMPTY — MEASURED IN BOTH ESTATES at `bfca6b7d6`.** (a) REGISTERED: `docs/implementation/PACKET_MANIFEST.json` holds **215 entries — 213 LANDED, 2 SUPERSEDED, ZERO non-terminal** (as of `bfca6b7d6`; it held 193 with 3 non-terminal at this packet's compile). The only entry naming any path of this one is `EM-P3` on `tests/build/generationWorkerLazy.test.js`, and `EM-P3` is **LANDED** — `TERMINAL_PACKET_STATUSES = {LANDED, SUPERSEDED}` do **not** reserve change paths. (b) WAITING: over the chair kit's own capsules, **EM-R6 (EM-T14) · EM-D0e · EM-B1b · EM-B2a4 · EM-A3 (EM-T13) MODIFY or CREATE not one path of this packet**, and **EM-R0d's one intersection is gone** now that version 3 carries no row on `tests/build/generationWorkerLazy.test.js` (see the order edge below).
- **Ordered after:** **`EM-R0d`** — the charter's family order (R0a · R0d · R0b · **R0f** · R0c), which version 3 does not disturb. The byte-arm REFUSAL that once made this an absolute bar is discharged; see below.

> ⭐⭐ **THE ORDER EDGE IS DISCHARGED AT VERSION 3, AND THE CHAIR RULES WHETHER IT STAYS DISCHARGED.**
> The chair's AMENDMENT-5 ruling 7 (2026-09-20; ODQ §934.47 addendum 121) held that **EM-R0d and
> EM-R0f may never ride one train**, because both carried a `TEST` row on
> `tests/build/generationWorkerLazy.test.js` — EM-R0d's for a **≤3,314 B** worker-ceiling bound,
> EM-R0f's for a **≤184 B** one — and that file is NOT row-keyed (§P10.8), so the estate's own
> validator refuses the pair: *duplicate change path across packets:
> tests/build/generationWorkerLazy.test.js (EM-R0d, EM-R0f)*, executed by ESTATE-REPAIR-3 at
> `bdbf7c89c`. ⭐ **The sixth amendment's §P2.11 supplies the cure the fifth did not have:** within
> ONE train a budget's test row is carried by **exactly one member, the byte-arm holder, NAMED BY
> THE CHAIR AT THE PLACEMENT** (judgment 129), *"and a member compiles as if it is NOT the holder
> unless the chair's brief says it is"*. Version 3 does exactly that: it carries **no row on that
> path**, states its **predicted delta with a membership proof** (§3.1), and names the holder's bound
> as the one that must cover the train's sum. ⛔ **MEASURED at `bfca6b7d6`: with that row withdrawn,
> EM-R0d ∩ EM-R0f = ∅ and EM-R6 ∩ EM-R0f = ∅** — the three EM-T14 members are path-disjoint. The
> chair still owes the ruling in §13 Q1, and a batch composer that puts **two byte rows** on one
> branch remains a STOP.

- **Commit authority:** edits only; the chair commits
- **Baseline posture:** **re-measured WHOLE at `bfca6b7d6`** (train EM-T12's cured tip), superseding the `141a1d775` posture — the function's body and its three-declaration closure; the consumer census with its denominator; the `src/data` purity rule and its paired test; the boundary ratchet's frozen set at its TOOL-31 addresses; the closures by MEMBERSHIP PROBE through vite's own exported set (no `dist` read); the edge-shared input membership; the tuning, observed-shape, writer-reach, prose-numerics, source-citation, wiring-census, mutation and entropy-root registers; §P2.19-21's instrument list run in plain `node` over the planted text; the 525-row golden-neutrality digest.
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: **MEASURED AT THE READ TIP, STAMPED BY THE CHAIR AT PROMOTION** — §P3.5 forbids a carried hash. As of `bfca6b7d6` the tree hashes `fdecd426828665cad2dfaf872871f29f46fa4816e1bd4c43c20ccd84dacc9e7c`, the FIFTH amendment.)
- **The law this packet is written to:** ⭐ **THE SIXTH AMENDMENT**, which lands as the FIRST commit of train EM-T13's branch — one train before this one — and whose draft hashes `ea78e7b94ef82b815867bd39681b120b3ae2a18c13e273d423cb10b1f2faddde` in the chair's kit (judgment 144, `CHAIR-SELECTION.md`). Version 3 measures itself against that text: §P2.11's one-holder rule, §P2.19-21's instrument list, §P2.16's citation law including a packet's own rows, §P3.5's deltas-not-absolutes, §P9's single effective-line instrument and §P10.8's path reservation. **The packet is RE-STAMPED AT PLACEMENT to whatever `shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md` prints at the train's base.** ⛔ The withdrawn `COMPILE-RULES.interim.md` row is struck: the sixth amendment's own preamble states that sheet is **SUPERSEDED BY THIS TEXT** and that *"a packet compiled after this amendment carries no interim row and no clause saying which of the two governs where they disagree"*.

> **REVALIDATION SENTENCE for the chair:** *"Revalidated at `<tip>`: `fingerprintPowerEconomyInput` is still declared at `src/generators/power/economyReconciliation.js` (POST-LANDING: this member moved it to `src/data/economyFingerprint.js :: fingerprintPowerEconomyInput`) and the whole-repo census is still exactly five rows (three in that file, two in `tests/generators/powerEconomyFreshness.test.js`); `economyProjectionInput` and `ECONOMY_FINGERPRINT_VERSION` are still read only inside it, and `fnv1a32` only at `:109`; the file is still 367 raw lines, so the post-move 338 and every post-edit address in §2/§5/§6 still hold; `BASELINE_EDGES` still holds 4 files / 5 specifiers at `:70-75` and names `economyReconciliation.js` in none of them, with the live count still 5 and the cardinality arm still at `:169`/`:174`; the `src/data/**` purity rule still bans only generators/store/lib/kernel-prng/kernel-rngContext and `tests/domain/dataPurity.test.js` still carries its three purity arms at `:57`/`:73`/`:101`; the worker closure still holds the file and the eager set (imported from `vite.config.js`, its SIZE measured and never quoted) still does not; the five edge-shared metas still return zero input hits; `tests/fixtures/generator-golden-master.json` is still the blob the 525-row digest was taken over; `src/data/economyFingerprint.js` is absent and untracked; EP-0's and EM-B2a3's three `requiredSymbols` rows on this path are all outside the moved region; and no non-terminal packet reserves a change path here."*

---

## 0. Scope, and why this packet exists at all

EM-R0c (the record merge, accepted at 294 effective lines) must recompute the power/economy fingerprint when a DM edit moves one of the four economic fields it reads. There are exactly three ways it could get that fact, and two are refused by measurement:

1. ⛔ **Import the generator from `src/domain/edit/`.** `tests/build/domainGeneratorsBoundary.test.js` freezes the `src/domain → src/generators` edge set **shrink-only**. Its `BASELINE_EDGES` holds **4 files / 5 specifiers** today. R0c would be the **fifth file** — a new importer, which the walker's first arm reds by name.
2. ⛔ **Take `R1`'s digest instead.** Refuted by execution at the R0c compile: wrong for the merged record in **31 of 315** trials (ODQ §934.47 addendum 35).
3. ⭐ **Move the leaf DOWN into a layer both can import.** One fact, one spelling, no new edge. This packet.

Re-implementing the fingerprint in the domain would spell one fact twice, and two spellings of a freshness digest drift silently — which is the precise failure `assertPowerEconomyFreshness` exists to catch.

## 1. Reconciled authority

1. **ODQ §934.47 addendum 35, ruling (1)** (2026-09-20 02:30, the chair, vetoable) — EM-R0f chartered as its own golden-neutral member: the pure move, the byte-identical proof over all 525 rows, the boundary ratchet's frozen set never growing, `domainGeneratorsBoundary.test.js`'s stale header prose corrected here, the worker's bytes measured.
2. **`docs/implementation/charters/EDIT-MODE-TRAIN.md`, amendments of 2026-09-20 02:30** — the family's build order places **R0f before R0c**.
3. **`docs/implementation/preambles/EM-PREAMBLE.md` §P2** rows 1, 2, 3, 4, 7, 10, 11, 12 — the registration cost, priced in §7.1.
4. **`docs/implementation/PACKET_STANDARD.md`** — the hard scope budget, the exact contracts, the verified tree contract, the STOP conditions.
5. **The tree at `141a1d775`**, measured — every row of §5.

### §1.1 · One premise qualified by measurement — reported, not adjudicated

⛔ **THE FROZEN SET DOES NOT SHRINK. IT IS UNCHANGED.** The charter's parenthesis reads *"the domain→generators boundary ratchet's frozen set SHRINKS, never grows"*. Re-measured at `bfca6b7d6`: `BASELINE_EDGES` (`:70-75`, moved from `:60-65` by TOOL-31 `e343ca859` with its CONTENT byte-identical) names `checkDraftEdit.js`, `neighbourBackLink.js`, `institutionLifecycle.js` and `resourceDynamicsKernel.js` — **not one row names `economyReconciliation.js`**, and **no `src/domain/**` file imports `fingerprintPowerEconomyInput`** (the whole-repo census is five rows, all inside the generator and its own test). ⇒ **the rows this move retires number ZERO.**

This does **not** refute the ruling: the charter's own `because` clause is forward-looking — *"a `src/domain/edit/` import of `src/generators/` would be the ratchet's fifth file"*. EM-R0f is therefore a **PREVENTIVE** member, not a burndown member: it removes the reason the set would have grown, and the set's correct post-move cardinality is the same 4 / 5 it is today. The packet declares the retired rows as **none** and changes `BASELINE_EDGES` **not at all**. §13 Q1 asks the chair to re-cut the charter's clause to say so.

## 2. Outcome

`fingerprintPowerEconomyInput`, its private input-projector and its version constant leave `src/generators/power/economyReconciliation.js` and become `src/data/economyFingerprint.js` — a pure leaf whose only import is the kernel's deterministic hash. Both of today's callers keep their exact call text and reach the function through one new import: the stamp inside `projectPowerGenerationIntent` (**post-move `:142`**) and the freshness assertion inside `assertPowerEconomyFreshness` (**post-move `:329`**). The one external importer, `tests/generators/powerEconomyFreshness.test.js`, is re-pointed in the same commit, so **no re-export stub is left behind**. `tests/build/domainGeneratorsBoundary.test.js`'s stale baseline list is corrected and the file gains EM-R0f's citation.

> ⛔ **WHY THIS SECTION SPELLS POST-MOVE ADDRESSES AND NOT TODAY'S** (§P2.16; found at the EM-T14 pre-proof). `economyReconciliation.js` is **367 raw lines today and 338 after the move**, both executed. A READY packet body is a LIVE document in `tests/lint/sourceCitationIntegrity.walker.test.js`'s ARM 2 — *"ONLY-SHRINKS: no NEW past-EOF citation in a live document"*, a gating arm whose baseline never grows, and whose own control proves a READY packet is live while a LANDED one is excluded. Version 2 spelled both callers at their PRE-MOVE addresses; the second of those sits past the end of the 338-line file this packet's own contract produces, so it would have been a NEW past-EOF row against a ratchet that admits none. ⛔ THE NUMERAL ITSELF IS THEREFORE NOT WRITTEN ANYWHERE IN THIS PACKET -- a bare address inherits the nearest preceding path, so even a sentence EXPLAINING the stale address would re-create it. Every address this packet spells into that file is now the POST-EDIT one, read off the simulated remainder rather than derived by arithmetic.

**Nothing observable changes.** The fingerprint over all 525 golden rows is byte-identical. ⭐ **EXECUTED HISTORY, with its as-of mark** (§P3.5's one exception): the digest `318fb81a94ca724d664907bc3cdd1602a995df2b6c7c32f5dca4022887618e18` was measured at `141a1d775`, computed three ways — from the function at its current home, from the moved leaf, and from what the pipeline actually writes onto `powerStructure.economyInputFingerprint`. It is quoted as a RECORD, not as a live assertion: the build lane's §8 step 2 takes the pre-move digest in its OWN run and the STOP condition compares the two digests **of that run** (§11 item 1). ⭐ `tests/fixtures/generator-golden-master.json` is **byte-identical at `141a1d775` and at `bfca6b7d6`** (blob `754222593`), so the corpus the digest was taken over has not moved.

## 3. Hard scope budget

| Measure | Cap | This packet | Receipt |
|---|---:|---:|---|
| Handwritten files | 12 | **4** | §7 |
| New/changed effective production lines | 400 | **44** | §P9's ONE instrument: eslint's `Linter` under `max-lines` with `skipBlankLines` + `skipComments`, **re-executed at `bfca6b7d6`** |
| Each new leaf | 250 | **22** | `src/data/economyFingerprint.js` — executed over the planted text |
| New logic leaves | 2 | **1** | the fingerprint leaf |
| Existing logic files modified | 3 | **1** | `economyReconciliation.js` (**280 → 259 eff, −21**, both re-executed at `bfca6b7d6`; raw **367 → 338**) |
| Acceptance cases | 8 | **5** | §9 |
| Launch-brief small-member threshold | 120 eff / 5 files | **44 eff / 4 files** | under both |

⛔ **No hot file is named.** ⛔ **Golden posture: UNCHANGED** — `generator-golden-master.json`, `dossier-prose-manifest-golden.json` and the preset witness are byte-identical; `UPDATE_GOLDEN` and `GOLDEN_SHIFT_SIGNED` are FORBIDDEN, and a moving golden is a STOP.

### §3.1 · The byte budgets, priced (§P2 row 11 as the sixth amendment corrects it)

⛔ **THIS PACKET COMPILES AS IF IT IS NOT THE TRAIN'S BYTE-ARM HOLDER.** §P2.11: *"WITHIN ONE TRAIN A BUDGET'S TEST ROW IS CARRIED BY EXACTLY ONE MEMBER, THE TRAIN'S BYTE-ARM HOLDER, NAMED BY THE CHAIR AT THE PLACEMENT … every other member that moves the same budget carries NO row on that path, states its own predicted delta with the holder's bound, and names the holder … a member compiles as if it is NOT the holder unless the chair's brief says it is."* Version 3 therefore states a **PREDICTED DELTA + a MEMBERSHIP PROOF** on every budget and carries **no byte row in §7**. The membership probe imports the tree's own exported set and **reads no `dist`, so it cannot skip** — which is precisely why §P2.11 accepts it in place of a dist-gated arm.

| Budget | Membership (the FACT; probes read no `dist`) | Instrument | Predicted delta |
|---|---|---|---|
| **Generation worker** | ⭐ **IN** — `economyReconciliation.js` is in the generation worker's import closure: **196 src modules static / 204 with dynamic edges**, measured at `bfca6b7d6` by a plain-node resolver from `src/workers/generation.worker.js` (this packet's compile read 220 / 228 from a different instrument; ⭐ **the MEMBERSHIP is the fact and it does not move with the count**). The new leaf joins that closure. | `WORKER_BUNDLE_CEILING_BYTES = 1401208` at `tests/build/generationWorkerLazy.test.js:159` — **EXACT, zero slack, monotone-down**; the constant is unmoved since this packet's compile | **+92 B esbuild per-module, bound ≤184 B (×2).** ⛔ **NO §7 ROW.** The bound is offered to **the train's byte-arm holder**, whose own stated bound must cover the SUM of EM-T14's members. If the chair names EM-R0f the holder, the row returns as §13 Q1 describes. |
| **Lazy engine** | ⭐ **IN** — reachable from `main.jsx` only through a dynamic boundary (**1,927** modules with dynamic edges; **absent** from the **238**-module static closure), and absent from the eager set | `tests/build/vendorPdfLazy.test.js:831`, `expect(size).toBeLessThan(679_000)` (the arm moved from `:787`; the ceiling is unmoved) | **≤184 B, declared, NO §7 ROW.** ⚠ **THE MARGIN IS THE TERMINAL'S MEASUREMENT, NOT THIS PACKET'S ARITHMETIC.** The newest figure the tree records is **677,935 B at `ec0a30da2`** (FIX-B2's own note at `:786`), and `ec0a30da2` is **not an ancestor of this packet's compile base**, with **43 commits touching `src/` since it** and FIX-B2's own pin-out (static importers 38 → 8) between. Version 2's "~181 B to spare" was arithmetic on a figure no longer live and is **withdrawn**. The holder's one real build prices it; reaching 679,000 is a **STOP**, never a lane's raise. |
| **Eager first paint** | ⛔ **OUT — measured, and by REACHABILITY not by root** | `EAGER_FIRST_PAINT_MODULES` **IMPORTED** from `vite.config.js`'s own export (`EAGER_MODULES`, the graph walk — not a hand list), its size measured at the base and never quoted: **270** at `bfca6b7d6` (269 at `bdbf7c89c`, 268 at `141a1d775`) | `economyReconciliation.js` is **not** a member, so the leaf only it imports cannot be one. **0 B of first-paint cost.** ⚠ **13 of the set's 270 members are `src/data/` files**, so "it is under `src/data`" would be the WRONG argument; the right one is that the leaf's only importer is not eager. `CLOSURE_BUDGET_BYTES` is the owner-signed quantity (judgment 86); the module count moves by **+0**. |
| **Edge-shared metas** | ⛔ **OUT — measured by INPUT membership** (§P2.10), never by entry-hood | the five metas' own `inputs` lists (114 / 74 / 115 / 2 / 2) | **0 hits** for the modified path, the created path and `src/kernel/proseHash.js`. **No `build:edge-shared`**, so §P2.12's seven-path hazard and §14(a)'s generator-last ordering hazard do not arise. |

⭐ **THE CURE IS THE PLACEMENT BEFORE IT IS THE CEILING**, and here the placement is already minimal: the worker genuinely executes this function (both call sites, post-move `:142` and `:329`, are on the generation path), so its bytes are irreducible, and §P2.11 records that **the worker budget has NO placement cure at all** (`worker.rollupOptions` is unset, so `manualChunks` never runs for a worker build). The +92 B is module-boundary overhead alone — the same expression tree, one extra wrapper, one export binding — and rollup's scope hoisting normally renders less than esbuild's per-module estimate. There is no cheaper placement; there is only the holder's honest summed price.

## 4. Sealed dispatch and preflight

Sealed dispatch **on this member's own lane branch at the train base** (§P10.2). Preflight must find `src/data/economyFingerprint.js` **absent and untracked** (measured absent, tracked and on disk, at `bfca6b7d6`), the substrate unchanged since the verified base, and the worktree git-clean. The four `checks` directories are `tests/generators`, `tests/domain`, `tests/build` and `tests/property` — **one directory per vitest invocation**, files named and never the `tests/lint` directory (§P7). ⛔ **No generator is among `checks`**, so §P2.12's ordering hazard does not arise. ⛔ **A LANE WORKTREE IS NEVER MOVED OR COMMITTED TO AFTER ITS DISPATCH** (§P10.2, judgment 125): the seal binds HEAD, the branch AND the bytes of the whole packet manifest.

## 5. Verified tree contract

Every row re-found BY SYMBOL at **`bfca6b7d6`** (train EM-T12's cured tip); line numbers are that tip's and the chair refreshes them at promotion. ⭐ **THE J-T1 WINDOW over every change-manifest path and every `requiredSymbols` path, `141a1d775 → bfca6b7d6` (164 commits), moved EXACTLY ONE FILE:** `tests/build/domainGeneratorsBoundary.test.js`, `1 file changed, 40 insertions(+), 3 deletions(-)` — TOOL-31 at `e343ca859`. Rows 8 and 9 below carry its new addresses; every other blob in the window is byte-identical.

| # | Path | Symbol | The fact | Proved by |
|---|---|---|---|---|
| 1 | `src/generators/power/economyReconciliation.js` | `fingerprintPowerEconomyInput` | declared and exported at `:100`; called from `projectPowerGenerationIntent` and from `assertPowerEconomyFreshness` — **post-move `:142` and `:329`** (§2's blockquote: the pre-move addresses are deliberately not spelled, because the second is past the end of the 338-line file this packet produces) | `git grep -n 'fingerprintPowerEconomyInput' HEAD -- src tests scripts` → 5 rows; the post-move addresses read off the simulated remainder |
| 2 | same | `economyProjectionInput` | private helper at `:85`; read **only** at `:101` | `grep -n` → 2 rows, repo-wide 2 |
| 3 | same | `ECONOMY_FINGERPRINT_VERSION` | private `const` at `:37`; read **only** at `:103` and `:110` | `grep -n` → 3 rows, repo-wide 3 |
| 4 | same | `fnv1a32` | imported at `:24`, used **only** at `:109` ⇒ the import moves with the function | `grep -n 'fnv1a32'` → 2 rows |
| 5 | same | `rngSeed: stepRng.fork(POWER_STREAM).seed,` | EP-0's `requiredSymbols` text (NC-1, the entropy census's MANDATORY live control) at `:132` → **post-move `:103`**, **outside the moved region** | `grep -c` on the simulated remainder → **1, executed** |
| 6 | same | `createPRNG(intent.rngSeed)` | the walker's second NC-1 `toContain` at `:160` → **post-move `:131`**, **outside the moved region**; it is also this file's ONE `createPRNG(` site | `grep -c` on the simulated remainder → **1, executed** |
| 7 | `src/kernel/proseHash.js` | `export function fnv1a32(` | at `:28`; already imported by `src/data/historyData.js` and `src/data/npcData.js`, so the leaf's one edge is an existing `src/data → src/kernel` edge | `git grep -n "from '../kernel/" HEAD -- src/data` |
| 8 | `tests/build/domainGeneratorsBoundary.test.js` | `const BASELINE_EDGES = Object.freeze({` | ⭐ **`:70-75` (TOOL-31 `e343ca859` moved it from `:60-65`; the CONTENT is byte-identical)** — **4 files / 5 specifiers**; names `economyReconciliation.js` in none. Two LANDED/waiting `requiredSymbols` rows quote this text (EM-P4 LANDED, EM-R0d and EM-B2a4 waiting) and this packet touches it not at all | the walker's own `liveEdges()` re-run in plain node → 4 files / 5 edges |
| 9 | same | `it('baseline is exactly the 6 known edges (cardinality guard)')` | ⭐ **`:169`, asserting `toBeLessThanOrEqual(6)` at `:174`** (TOOL-31 moved both from `:159`/`:164`) against a live count of **5** | same probe → `SLACK = 1` |
| 9a | same | `it('derives its repository root from this module URL, never the working directory')` | ⭐ **NEW at `:177`, landed by TOOL-31** — a FOURTH arm that reads this file's own source and pins the line `const repoRoot = join(dirname(fileURLToPath(import.meta.url)), '../..');` **by text**, found with `startsWith('const repoRoot =')`. §6.4's edits are confined to comment prose and (per Q2) one title plus one numeral, none of which is that line or introduces a second one | read whole at the tip |
| 10 | `tests/generators/powerEconomyFreshness.test.js` | the import block at **`:16-19`** | `import {` opens at `:16`; `assertPowerEconomyFreshness` `:17`, `fingerprintPowerEconomyInput` `:18`, the `from` at `:19` — both from the generator. Version 2 wrote `:17-19`, which named the rows but not the opener | `sed -n '10,26p'` at the tip |
| 11 | `eslint.config.js` | the `src/data/**` override | `:859` (`files: ['src/data/**/*.{js,jsx}']`) through `:877`; bans only `**/generators/**`, `**/store/**`, `**/lib/**`, `**/kernel/prng*`, `**/kernel/rngContext*`. **Blob unmoved across the J-T1 window** | read whole; `git diff --stat 141a1d775 HEAD -- eslint.config.js` → empty |
| 12 | `tests/domain/dataPurity.test.js` | its three purity arms | `:57`, `:73`, `:101` (under the `describe` at `:50`, beside a fourth non-vacuity arm at `:53`); **no arm forbids an exported function in `src/data`** | read whole; **14 of `src/data`'s 58 files** already match `^export function ` (`git grep -l -P '^export function ' -- 'src/data/*.js'`) |

## 6. Exact contracts

### §6.1 · `src/data/economyFingerprint.js` — CREATE

The file holds **exactly three declarations, lifted VERBATIM** from `economyReconciliation.js` (`:37`, `:85-92`, `:94-111` — the JSDoc travels with its function), preceded by a header and one import. Byte-for-byte fidelity is a contract, not a courtesy: it is what makes §9's A1 a tautology rather than a hope.

- `import { fnv1a32 } from '../kernel/proseHash.js';` — the ONLY import. ⛔ Nothing from `src/generators`, `src/domain`, `src/store`, `src/lib`, `kernel/prng` or `kernel/rngContext`.
- `const ECONOMY_FINGERPRINT_VERSION = 'power-economy-v1';` — **not exported** (nothing in the estate reads it; §13 Q4).
- `function economyProjectionInput(economicState, tier)` — module-private, unchanged.
- `export function fingerprintPowerEconomyInput(economicState, tier)` — unchanged, including its JSDoc.

**Signature and return, unchanged and exact:** `(economicState: object|null|undefined, tier: string|null|undefined) → string` of the form `` `power-economy-v1:${8-hex-digit FNV-1a}` ``. Absence semantics are the four `||` defaults, preserved verbatim: missing `tier` → `''`; missing `prosperity` → `'Moderate'`; missing `safetyProfile.safetyLabel` → `'Moderate'`; missing `foodSecurity.label` → `'Secure'`. Tuple ORDER is the contract (version, tier, prosperity, safetyLabel, foodLabel) and the explicit array is what avoids key-order ambiguity. **Pure:** no draw, no clock, no mutation of either argument.

The header states WHY the leaf is here: two layers need one fact; a domain→generator import would be the ratchet's fifth file; a second spelling would drift; the kernel-hash edge is the one `historyData.js` and `npcData.js` already take and is outside every pattern the purity rule bans.

### §6.2 · `src/generators/power/economyReconciliation.js` — MODIFY

Four textual acts, no fifth:
1. **DELETE** `:24` `import { fnv1a32 } from '../../kernel/proseHash.js';` — it becomes unused and would red `no-unused-vars`.
2. **INSERT**, at the head of the import block (before `:22`'s `../../domain/clone.js`, the file's own alphabetical idiom — `data` < `domain` < `kernel`): `import { fingerprintPowerEconomyInput } from '../../data/economyFingerprint.js';`
3. **DELETE** `:37` and **DELETE** `:85-112` (the helper, the JSDoc, the function and one of the two surrounding blank lines), leaving exactly one blank line between `powerLabelFor`'s close and `createPowerGenerationIntent`'s JSDoc.
4. ⛔ **Nothing else.** Both call sites keep their text byte-for-byte (they land at **post-move `:142` and `:329`**); no reformat, no comment churn, no reordering of any other import. The import block after the edit reads, in order: the new `../../data/economyFingerprint.js` (`:22`), `../../domain/clone.js`, `../../domain/factionProfile.js`, `../../kernel/prng.js`, `../../kernel/rngContext.js`, `./rulingStructure.js` — the file's own `data < domain < kernel` idiom, executed.

⛔ **NO RE-EXPORT.** `export { fingerprintPowerEconomyInput }` must NOT be added here. The one external importer is re-pointed in this same commit (§6.3), so a stub would preserve nothing and would leave the old home as a second address for one fact — the ambiguity the move exists to remove.

**Measured outcome, executed over the planted text at `bfca6b7d6`:** effective lines **280 → 259**; raw lines **367 → 338**; the seam leaves exactly ONE blank line (`:83`) between `powerLabelFor`'s close (`:82`) and `createPowerGenerationIntent`'s JSDoc (`:84`); in the remainder `fingerprintPowerEconomyInput` → 3, and `fnv1a32` → **0**, `ECONOMY_FINGERPRINT_VERSION` → **0**, `economyProjectionInput` → **0**; EP-0's two NC-1 texts → **1 each**.

### §6.3 · `tests/generators/powerEconomyFreshness.test.js` — TEST

Split the import block at **`:16-19`** (`import {` opens at `:16`). `assertPowerEconomyFreshness` keeps its import from `../../src/generators/power/economyReconciliation.js`; `fingerprintPowerEconomyInput` gets its own `import { fingerprintPowerEconomyInput } from '../../src/data/economyFingerprint.js';`. The call at `:156` is untouched — the binding name is identical, and that call is this packet's own `requiredSymbols` row.

⛔ **No `describe` and no `test`/`it` is added, removed or renamed here.** This re-point is also the packet's structural pin: if a later hand moved the function back, this import would fail to resolve.

### §6.4 · `tests/build/domainGeneratorsBoundary.test.js` — MODIFY (the charter's own row)

⛔ **`BASELINE_EDGES` IS NOT TOUCHED.** The frozen set stays at its measured 4 files / 5 specifiers (§1.1).

> ⭐ **ITEM 1 OF VERSION 2 IS STRUCK: IT LANDED UPSTREAM.** TOOL-31 (`e343ca859`, 2026-09-21) already replaced the header's "`src/domain/worldPulse`, ~22.7k LOC" with **"183,678 lines across 443 files, re-measured 2026-09-20 at `ee2406191`"** — the identical correction, and the identical figure, this packet proposed. The work is done; §7's row and this section shrink accordingly. (Judgment 134's law in its happiest form: a sibling's LANDED fence, measured, not predicted.)

Correct the remaining header staleness and cite EM-R0f as the reason the set will not grow:
1. **`:54-60`** (moved from `:44-51` by TOOL-31) — the six-row "Baseline captured at HEAD 8e10816" list contradicts the W6 note immediately below it (three of the six are gone) and `BASELINE_EDGES` itself. Replace it with the **four** live rows, or state plainly that the list is historical and `BASELINE_EDGES` is authoritative.
2. Add EM-R0f's citation: the fingerprint moved to `src/data/economyFingerprint.js` **so that EM-R0c's `src/domain/edit/` merge reads it without becoming this baseline's fifth file** — the inversion this header prescribes, executed once.
3. **`:169` / `:174`** (moved from `:159`/`:164`) — the `it` title says "exactly the 6 known edges" and the arm asserts `<= 6` against a live count of **5**: one unit of slack in a set whose header says it can only shrink. **The chair's call (§13 Q2).** Recommended: retitle to name 4 files / 5 edges and tighten the arm to `toBeLessThanOrEqual(5)`.

⛔ **`BASELINE_EDGES` IS NOT TOUCHED** and **no `describe`/`it` is added or removed** — only a title may be reworded, which moves no census count (the baseline stores five integers and metadata, no title text). ⭐ **§P2.18's estate grep, executed at `bfca6b7d6`, clears the Q2 rewording:** `git grep -n -F 'baseline is exactly the 6 known edges'` and `... 'toBeLessThanOrEqual(6)'` over `tests scripts docs/implementation` find the arm itself and no `requiredSymbols` row anywhere — EM-P4 (LANDED), EM-R0d and EM-B2a4 each hold a row on this file, and every one of them quotes `const BASELINE_EDGES`, never the title or the bound. ⛔ **The fourth arm at `:177` reads this file's own source**: no edit here may introduce a second line beginning `const repoRoot =`, and none of the three edits above does.

## 7. Exact change manifest

| Action | File | Symbol/region | Max delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | ⭐ `src/data/economyFingerprint.js` | `fingerprintPowerEconomyInput`, `economyProjectionInput`, `ECONOMY_FINGERPRINT_VERSION` | **≤250 eff** (measured **22**) | §6.1. The three declarations **VERBATIM** from `:37`, `:85-92`, `:94-111` — diff them against the pre-edit file and quote the zero. One import: `../kernel/proseHash.js`. ⛔ No other import. ⛔ No em dash and no exclamation point in any string literal (`tests/copy/voiceMechanics.test.js`). |
| `MODIFY` | `src/generators/power/economyReconciliation.js` | the import block; `:37`; `:85-112` | **−21 eff** (280 → 259; raw 367 → 338) | §6.2's four acts, no fifth. ⛔ **No re-export.** ⛔ Both call texts and EP-0's two NC-1 texts must come through byte-identical — `grep -c 'rngSeed: stepRng.fork(POWER_STREAM).seed,'` and `grep -c 'createPRNG(intent.rngSeed)'` both **1** after the edit, quoted in the receipt (executed at this pre-proof: **1 and 1**). |
| `TEST` | `tests/generators/powerEconomyFreshness.test.js` | the import block `:16-19` | **+1 line** | §6.3. ⛔ No new or renamed `describe`/`test`/`it`. ⛔ Never name a variable or parameter `it`, `test` or `describe`. |
| `MODIFY` | `tests/build/domainGeneratorsBoundary.test.js` | the module header `:47-69` (the stale six-row list at `:54-60` and EM-R0f's citation); `:169`/`:174` per the chair's Q2 ruling | **comment-only**, plus at most one title + one numeral if Q2 says tighten | §6.4. ⛔ **`BASELINE_EDGES` (`:70-75`) unchanged.** ⛔ No `describe`/`it` added or removed. ⛔ No second line beginning `const repoRoot =` (the arm at `:177`). |

⛔ **THE BYTE ROW IS DELIBERATELY ABSENT** (§P2.11, the sixth amendment's one-holder rule): this member carries **no row on `tests/build/generationWorkerLazy.test.js` and none on `tests/build/vendorPdfLazy.test.js`**, states its predicted deltas and membership proofs in §3.1, and names **the train's byte-arm holder** as the one whose stated bound must cover EM-T14's sum. That is also what makes this member path-disjoint from EM-R0d (§13 Q1).

**Generated artifacts: NONE.** No `supabase/functions/_shared` row (§P2.10, measured zero input hits) and **no generator among `checks`**. No register file is written by this packet. No other file may be edited.

### §7.1 · The registration ledger — the measured NEGATIVES live here, never in the table above

| # | Obligation | Verdict | Measurement |
|---|---|---|---|
| P2.1 | sovereignty lighting | ⭐ **DELTA `files +0 · parked +0 · credited +0 · titles +0 · suiteTitles +0`** | ⛔ NO ABSOLUTE QUOTED, all five figures in the register's own order. The packet CREATEs and RENAMEs **no** test file and adds/removes **no** `describe`/`it`; the baseline stores five integers and metadata only — **no title text** — so a reworded title (§6.4 item 3) moves nothing. A new `src/` leaf moves nothing (§P2.1). The census is re-derived whole at the train's terminal, BY THE CHAIR; the deferred row names `tests/lint/.lighting-census-baseline.json`, never the walker, and the walker is in no `checks` array. |
| P2.2 | mutation coverage | **NOT OWED — `ENFORCER_DIRS` READ at the tip, never recalled** | `tests/lint/mutationCoverage.shared.mjs:36-44` lists `tests/lint · design · docs · data · copy · security · edgeFunctions · generators` (⛔ **`tests/build` is NOT one**), plus `NAME_PATTERN` over basenames outside them. The packet **creates or renames no test file**, so nothing enters `enumerateInvariants` and no entry goes stale. Measured in `scripts/mutation-coverage-manifest.json` at `bfca6b7d6`: `invariants` is an object of **723** keys; `tests/generators/powerEconomyFreshness.test.js` **is** a key (`kind=rationale`) and is unaffected by an import-block split; `tests/build/domainGeneratorsBoundary.test.js` is **not** a key; `uncoveredBaseline` is **186** and does not move. No `rowKey` is added, so §P10.8's row-keyed-register clause is not engaged. |
| P2.3 | observed-shape readers | **NOT OWED** | The three tree manifests are content-addressed and **frozen at `31ab5d18b`**, not re-taken at a landing. ⭐ EXECUTED PRECEDENT: `EM-P3` (LANDED) modified a generator and created `src/data/worldFactOptions.js`, names this baseline nowhere, and that file is **absent** from all three trees. The file's one `inventory` row, `{"source on factions": 1}`, is `isNeighbourFaction`'s read and **stays**. The moved body reads no `dmLayer`/`decrees` key and its reads sit in the scanner's unresolved population. |
| P2.4 | writer-reach | **NOT OWED** | `grep -c 'economyReconciliation' scripts/.writer-reach-baseline.json` → **0** (re-run at `bfca6b7d6`; 0 for all four of this packet's names). The row `"economyInputFingerprint on powerStructure"` names a key and shape; the WRITE itself is unmoved text, only re-addressed (`:171` → `:142`). Nothing is added under `src/domain/edit/**`. |
| P2.5 · P2.7 | seeded chooser · prose-numerics | **NOT OWED** | No `rollFrom`, no draw, no rendered figure; the prose-numerics baseline names neither path. |
| P2.6 · P2.8 · P2.9 | persisted keys · surfaces · preset witness | **NOT OWED** | No persisted key, no component, no tick hook. |
| P2.10 | edge-shared | ⭐ **NOT OWED — measured by INPUT membership** | 0 hits for the modified path, the created path and `kernel/proseHash.js` across all five metas' own `inputs` (114 / 74 / 115 / 2 / 2). |
| P2.11 | byte budgets | ⭐ **PRICED AS DELTAS, NO ROW CARRIED — §3.1** | This member compiles as if it is **not** EM-T14's byte-arm holder. Worker **IN** by membership (closure 196 static / 204 with dynamic, measured at `bfca6b7d6`); lazy engine **IN** by membership (absent from the 238-module static closure, present in the 1,927-module dynamic one); eager first paint **OUT** by REACHABILITY (`EAGER_FIRST_PAINT_MODULES` IMPORTED from `vite.config.js`: **270** at `bfca6b7d6`, 269 at `bdbf7c89c`, 268 at `141a1d775`; `economyReconciliation.js` is not a member, and 13 of the 270 ARE `src/data/` files, so the root is not the argument); edge-shared **OUT**. Predicted **+92 B** esbuild per-module, bound **≤184 B**, offered to the holder. ⚠ Version 2's "~181 B to spare" on the engine is **withdrawn** as arithmetic on a stale figure (§3.1). |
| P2.12 | paths a declared command writes | **NONE** | No generator among `checks`; no declared command writes any tracked path. |
| P2.13 · 15 · 16 | wiring census · browser suite · line-addressed registers | ⭐ **MEASURED — one live cure, named** | **WIRING CENSUS, BOTH ARMS:** the SHA-MAP arm — `stamp.files` holds 7 entries and names `economyReconciliation.js` in none (and no `src/data` path), so no `stale-bytes`; the PRODUCER-COUNT arm — `stamp.producerIndexFiles` is a NUMBER, **`1181` at `bfca6b7d6`** (1172 at `bdbf7c89c`, 1171 at `141a1d775`), counting every `.js` under `src/generators/**` and `src/domain/**`, and this packet's delta is **+0** because its one CREATE is under `src/data/`, ⛔ **NEITHER counted root** — the whole point of the move, so the count is unmoved *by construction*. §P2.13 therefore keeps `docs/content/wiring-census.json` out of §7, the manifest and `checks`, and no `proseWiringCensus` red arises. **BROWSER SUITE (§P2.15): NOT OWED** — the packet touches no `src/components/**`, no route, no `data-testid` and no accessible name; it names no `e2e/` spec. **PROSE-NUMERICS:** the baseline names neither path (`grep -c` → 0), and neither does `scripts/.writer-reach-baseline.json` or `tests/lint/.source-citation-baseline.json`. ⛔⛔ **THE `path:line` CITATION GREP WAS WRONG AND IS REPLACED BY ITS MEASUREMENT.** Version 2 claimed `git grep -n 'economyReconciliation\.js:[0-9]' … → 0 hits`. Re-run at `bfca6b7d6`, **both engines agreeing (-P and -E): SIX hits**, all under `docs/` — `PACKET_MANIFEST.json:35084` (`:68`), `:35099` (`:277`), `:36863` (`:132`, `:160`), `EM-A1.md:176` and `:972` (`:277`), `EM-B1a.md:611` (`:277`). **Every citing packet is LANDED**, and `sourceCitationIntegrity.walker.test.js` excludes a LANDED packet's body by `landedPacketPaths` (its own control proves a READY packet is LIVE and a LANDED one is not); all six addresses also stay **in range** after the move (post-edit EOF 338), and ARM 3 (symbol moved) is REPORT-ONLY. ⇒ **no red from any of them.** ⭐ **The one live row was this packet's own**, and §2/§5 now cure it: version 2's pre-move address for the freshness caller would have become a NEW past-EOF citation in a live document against ARM 2, a gating ratchet that never grows, because this packet's own contract takes the file to 338 lines. |
| — | tuning register (§P2.17) | ⭐ **UNCHANGED — EXECUTED BOTH WAYS at `bfca6b7d6`** | The library's own `discoverTables` (214 tables) then `countUnregisteredNamed`/`countBareDecimals` over the tip and over a simulated root carrying both edited files: **`{economyReconciliation.js: 2}`, sites `[POWER_INTENT_VERSION, POWER_PROJECTION_VERSION]`, BOTH times**; `bareDecimals` `{}` both; committed row `tests/lint/.tuning-inventory.json:7608` → **2**. The moved `ECONOMY_FINGERPRINT_VERSION` is a STRING (never matched by `NAMED_NUMERIC_CONST_RE`) and `16`/`8` are integers (`BARE_DECIMAL_RE` counts fractional literals only). `TREES_P2P3 = ["src/domain","src/generators"]` — `src/data` is out of scope, **and the new leaf scores 0 even when `src/data` is forced in**. No deferred refreeze row is owed. |
| — | entropy-root census (§P2.21(a)) | ⭐ **BOTH ARMS MEASURED — DELTA +0 / +0** | **Arm 1, the read sites:** NC-1, the walker's MANDATORY live negative control, IS this file (`entropyRootCensus.walker.test.js:185` and `:605`). It asserts by **TEXT** (`toContain`), not by line, on two strings outside the moved region; `WRITE_KEY_SITES`' exact-set equality is by **path**, unchanged. **Arm 2, the `createPRNG(` SITE COUNTS — the arm version 2 was silent on and the sixth amendment now requires:** the walker pins **36 in `src/domain`** (`:824`) and **47 whole-`src/`** (`:832`, the sixth's C4 corpus). Measured at `bfca6b7d6` with `git grep -o -F 'createPRNG('`: **`src/domain` 36 · `src/` whole 47 · `src/data` 0 · `economyReconciliation.js` 1** (at `:160`, OUTSIDE the moved region). The remainder keeps that one site and **the new leaf mints none** (executed: `createPRNG(` → 0 in the planted text; its only import is `fnv1a32`). ⇒ **36 → 36 and 47 → 47. No TEST row on that walker is owed.** |
| — | ruin-filter roster (§P2.21(b)) | ⭐ **OUT OF CORPUS — measured, not argued** | `tests/lint/ruinFilterRoster.walker.test.js` source-scans **`src/domain` only** (its header, line 12) with `READER_RE = /\.institutions\b/`. `economyReconciliation.js` lives under `src/generators` and the new leaf under `src/data`: **neither is in the discovery set**, and `git grep -F 'economyReconciliation' -- tests/lint/ruinFilterRoster.walker.test.js` → **0**. Belt and braces, by CONTENT: the planted leaf reads `.institutions` **0 times** (executed). No ROUTED/EXEMPT row is owed and the discovery count moves **+0**. |
| — | goods roster (§P2.21(c)) | ⭐ **NOT OWED — measured** | `tests/build/vendorPdfLazy.test.js`'s ST-2 roster names `src/data/goods/identity.js`, `src/data/goods/chains.js`, `GOODS_IDENTITY_TABLES = ['src/data/resourceData.js']` and the chain tables (`:1205-1211`). The new leaf imports **none of them** — its single import is `../kernel/proseHash.js` — so it is not a goods identity half-table consumer and owes no row in `src/domain/goods.schema.js`. |
| — | voice mechanics (§P2.19) | ⭐ **DELTA +0 / +0 — the counter run over the planted text** | A file the packet CREATEs is held at ZERO. Executed over the planted leaf: **em dashes 0, exclamation points 0**, counted over the whole file (stricter than the walker's string-literal-only Tier 2). `tests/copy/.voice-mechanics-baseline.json` and `.voice-mechanics-generators-baseline.json` name none of this packet's four paths. §6.1's header prose is written without either character. |
| — | reviewed-set pin (§P2.20) | **NOT OWED — stated** | The packet registers **no application command**, so `tests/application/commands/commandRegistry.test.js`'s exact-count capability pin cannot move, and no review sentence is owed. It performs no store edit, so `tests/store/deadOperationRatchet.test.js` is not engaged either. |
| — | the leaf's own governing set (§P10.11 (i), judgment 148) | ⭐ **RE-DERIVED PER IMPORT — none owed** | The contract imports exactly two modules. **`src/kernel/proseHash.js`:** `git grep -l -F 'proseHash' -- tests` → 9 files, read; the only roster over it is `tests/lint/fnv1a32Identity.walker.test.js`, whose `collectDefinitions()` collects **definitions** of `function fnv1a32` under `src/` against `COPY_CEILING = 22` — the leaf **imports** and defines none, so the population is **unmoved**. `tests/lint/couplingInclusion.walker.test.js`'s `CENSUS_SCOPE_RE = /^src\/domain\/(?:worldPulse\|spatial)\//` puts both paths out of scope; `tests/lint/seedLoopTotality.walker.test.js` keys on `tests/` files and this packet CREATEs none. **`src/data/economyFingerprint.js`:** `git grep -l -F 'economyFingerprint' -- tests scripts docs` → **0 hits anywhere** (it does not yet exist). And `git grep -l -F 'economyReconciliation' -- tests` → four files, all accounted for: `powerEconomyFreshness.test.js` (§6.3's row), `entropyRootCensus.walker.test.js` (NC-1, above), `.tuning-inventory.json` (the register, above) and `pipelinePinnedChoosers.test.js`, whose `:488` names `'../power/economyReconciliation.js'` only as the ANCHOR inside **`generatePower.js`**'s import list — a file this packet does not touch. |
| §P10.11 step 10 | post-edit `requiredSymbols` simulation | ⭐ **ALL 14 SURVIVE, EXECUTED; `retiredSymbols`: FOUR, all discharged** | Every one of the 14 rows was re-measured VERBATIM at `bfca6b7d6` by substring count: **count=1 each**. The four texts that leave `economyReconciliation.js` — `export function fingerprintPowerEconomyInput(economicState, tier) {`, `function economyProjectionInput(economicState, tier) {`, `const ECONOMY_FINGERPRINT_VERSION = 'power-economy-v1';` and `import { fnv1a32 } from '../../kernel/proseHash.js';` — are `retiredSymbols` rows (each count=1 today) and appear in `requiredSymbols` **nowhere**; the successor joins only at the LANDED flip, and TOOL-29's seal-aware cross-packet discharge (§P10.9) is what makes a retiree-carrying member buildable at all. ⛔ **VERSION 2'S CENSUS WAS WRONG AND IS REPLACED.** It said the **only** `requiredSymbols` row on `economyReconciliation.js` was EP-0's. Re-run over all **215** entries: **three** rows, all LANDED and all OUTSIDE the moved region — EP-0's `rngSeed: stepRng.fork(POWER_STREAM).seed,` (`:132` → `:103`) and **EM-B2a3's two**, `export function createPowerGenerationIntent` (`:118` → `:89`) and `export function projectPowerGenerationIntent` (`:154` → `:125`); EM-B2a3's two carry `_note`s citing `:132` and `:160`, and both packets being LANDED the citation walker excludes them, with both addresses still in range after the move. Two further LANDED rows sit on this packet's other paths — EM-P3's `export const WORKER_BUNDLE_CEILING_BYTES` and EM-P4's `const BASELINE_EDGES = Object.freeze({` — and this packet touches neither text. **All five discharge by construction.** |

## 8. Ordered coding sequence

1. **K-GATE, before the first edit.** `git status --short` empty; `shasum -a 256 tests/fixtures/generator-golden-master.json tests/fixtures/dossier-prose-manifest-golden.json` recorded; `shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md` **measured and quoted** (§P3.5 — never compared to a hash this packet carries; it must equal the one the chair stamped at placement); `src/data/economyFingerprint.js` absent **and untracked**; the five census rows of §5 row 1 re-grepped and quoted; `wc -l src/generators/power/economyReconciliation.js` recorded, so §12 can quote the raw pair. **Then the packet's count prover from the KIT path** (§P9): `node <kit>/EM-R0f.count-prover.mjs <packet.md> <capsule.json>`, exit 0 required before the seal.
2. **RED FIRST, and it is the packet's own acceptance case.** Before any edit, run the golden-neutrality probe at the base and **record the digest THIS RUN produces** (the record at `141a1d775` was `318fb81a…618e18`, quoted in §2 with its as-of mark, never as a live assertion). Then create the leaf and re-run it importing the **new** file: the two digests **of this run** must be identical. A difference is a **STOP**; a probe that cannot run is a STOP, not a pass.
3. **CREATE** `src/data/economyFingerprint.js` (§6.1). Diff its three declarations against the pre-edit source and quote the zero-byte delta.
4. **MODIFY** `src/generators/power/economyReconciliation.js` (§6.2). Re-measure effective lines with §P9's ONE instrument (expect **280 → 259**) and raw lines (expect **367 → 338**) and quote all four. `grep -c` EP-0's two NC-1 strings → 1 each. ⭐ **Then re-read §2/§5/§6's post-move addresses against the file you just produced** (`:22 · :89 · :103 · :125 · :131 · :142 · :191 · :268 · :324 · :329`) and quote any that differ — a packet that shifts lines re-addresses its OWN rows in the same version (§P2.16, judgment 124).
5. **TEST** `tests/generators/powerEconomyFreshness.test.js` (§6.3).
6. **MODIFY** `tests/build/domainGeneratorsBoundary.test.js` (§6.4), per the chair's Q2 ruling.
7. **GATED BATCH A** — §10 rows 1–5, PAUSE-AND-RESUME. ⛔ Under a parallel train the lane runs **no gated line itself** (§P10.5): it writes ONE gate script with a `records.tsv` row per step and the chair's one queue runs it.
8. **THE BYTE MEASUREMENT, NOT A RE-MINT.** ⛔ **This member is not the byte-arm holder unless the chair's placement names it** (§P2.11): it runs **no** build, re-mints **no** ceiling and carries **no** dist-gated arm. It hands the holder its two membership facts (§3.1) and its predicted **≤184 B**. The train's terminal takes ONE summed price from ONE real build with per-module attribution and makes **at most one** re-mint; exactly **two** of this member's modules may appear in that attribution — `src/generators/power/economyReconciliation.js` (down) and `src/data/economyFingerprint.js` (new) — and a third is a STOP. ⛔ **Any re-mint of `WORKER_BUNDLE_CEILING_BYTES` is the OWNER's** (§P11.2).
9. **GATED BATCH B** — the standing instruments: `tests/lint` WHOLE, `tests/lint/negativeAssertionAnchor.walker.test.js`, `tests/copy/voiceMechanics.test.js`, `tests/lint/mutationCoverageManifest.test.js`, `npx eslint` on all four touched files. `tests/lint/sovereigntyLightingContract.walker.test.js` run ONCE, separately — **expected GREEN** (delta 0/0/0/0/0); ⛔ never refreeze it.
10. Re-hash both goldens: identical to step 1.

## 9. Acceptance matrix

| id | case |
|---|---|
| **A1** | ⭐ **GOLDEN NEUTRALITY, the packet's own proof.** Over all **525** rows of `goldenCorpus()`, `fingerprintPowerEconomyInput` imported from `src/data/economyFingerprint.js` returns, row for row, exactly what the pre-move function at `src/generators/power/economyReconciliation.js` returned, and exactly what the pipeline writes to `powerStructure.economyInputFingerprint`. **The proof is the equality of the two SHA-256s over `keyOf(c) \t fingerprint` in corpus order taken in ONE run, before and after** — zero disagreements. (Executed history, with its as-of mark: that digest read `318fb81a94ca724d664907bc3cdd1602a995df2b6c7c32f5dca4022887618e18` at `141a1d775`, and `tests/fixtures/generator-golden-master.json` is byte-identical at `bfca6b7d6`, blob `754222593`. §P3.5/§P3.6: the record is annotated, never asserted live.) |
| **A2** | Both goldens and the preset witness are byte-identical across the change; `tests/property/generatorGoldenMaster.test.js` and `tests/property/dossierProseManifest.test.js` are green with no door set. |
| **A3** | `tests/generators/powerEconomyFreshness.test.js` is green with `fingerprintPowerEconomyInput` imported from its new home and `assertPowerEconomyFreshness` still from the generator — the freshness invariant still throws on a perturbed prosperity and on a fingerprint-stripped power structure. |
| **A4** | `tests/build/domainGeneratorsBoundary.test.js` is green and `BASELINE_EDGES` is byte-identical: the live set is still **4 files / 5 specifiers**, and `src/data/economyFingerprint.js` creates **no** `src/domain → src/generators` edge (it is not under `src/domain`, and `generatorSpecifiers` matches only `/generators/`). |
| **A5** | `tests/domain/dataPurity.test.js` is green with the new leaf in the walked set: it imports nothing from `generators`/`store`/`lib`, captures no `rngContext`, and imports no `kernel/prng`. `npx eslint src/data/economyFingerprint.js` passes the `src/data/**` `no-restricted-imports` override. |

## 10. Verification commands

1. `npx vitest run --pool=threads --maxWorkers=2 tests/generators/powerEconomyFreshness.test.js`
2. `npx vitest run --pool=threads --maxWorkers=2 tests/domain/dataPurity.test.js`
3. `npx vitest run --pool=threads --maxWorkers=2 tests/build/domainGeneratorsBoundary.test.js`
4. `npx vitest run --pool=threads --maxWorkers=2 tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js`
5. `npx eslint src/data/economyFingerprint.js src/generators/power/economyReconciliation.js tests/generators/powerEconomyFreshness.test.js tests/build/domainGeneratorsBoundary.test.js`
6. `npm run typecheck:domain:strict`
7. `npm run typecheck:ratchet`
8. `node scripts/implementation-packets.mjs validate`

⭐ **THE BUILD LANE'S PRE-SEAL INSTRUMENT, run from the KIT path and never placed in the tree** (§P9): `node <kit>/EM-R0f.count-prover.mjs <packet.md> <capsule.json>` — exit 0 required before the seal, and it is neither a `checks` entry nor a §7 row.

⭐ **THE LANE'S OWN INSTRUMENTS, outside `checks`** (§P7): `npx vitest run --pool=threads --maxWorkers=2 tests/lint --exclude=tests/lint/sovereigntyLightingContract.walker.test.js` **must exit 0**, and the lighting walker run **ALONE** is expected GREEN here (this member's delta is `0/0/0/0/0`); both are quoted in the receipt. `tests/lint` whole is mandatory and NOT sufficient.

⛔ **The two dist-gated byte arms are deliberately NOT in `checks`** (the chair's Q7 ruling at EM-B1k2, 2026-09-20, and now §P2.11 itself): `describe.runIf(DIST_EXISTS)` means a sealed run with no dist **skips** them and `check:packet` exits 0 on a proof that never executed — *"a skipped arm proves nothing about a budget"*. Under the sixth amendment this member goes further and carries **no byte row at all** (§3.1): its proof is the membership probe, which imports the exported set, reads no `dist` and therefore **cannot skip**, and the executed byte figure is the train terminal's one summed build.

⛔ Every gated line goes through the mutex, SHARED tier, worker-capped, with **both** exports spelled inline: `GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 sh scripts/gate-mutex.sh --run -- npx vitest run …`. **A gate line with no printed test count DID NOT RUN.**

## 11. Mandatory STOP conditions

1. The 525-row digest taken AFTER the move differs from the one taken BEFORE it **in the same run**, or the row count is not 525, or the probe cannot run. (⛔ Not "anything but `318fb81a…`": §P3.5/§P3.6 forbid a literal digest as a live assertion, and a signed golden re-record would make that form refuse a correct build.)
2. Either golden or the preset witness moves a byte. `UPDATE_GOLDEN` / `GOLDEN_SHIFT_SIGNED` are FORBIDDEN.
3. Any moved declaration is not byte-identical to its pre-edit text.
4. `BASELINE_EDGES` would need to change, or the live edge count is anything but 5.
5. The terminal's one real build shows a third module of this member's moving, or its contribution exceeds the **≤184 B** this member predicted, or the byte-arm holder's stated bound is breached, or the lazy engine reaches 679,000. ⛔ Any re-mint of `WORKER_BUNDLE_CEILING_BYTES` or of `CLOSURE_BUDGET_BYTES` is the OWNER's (§P11.2) and a refusal truncates the train rather than raising a ceiling.
6. `grep -c` on either of EP-0's NC-1 strings returns anything but 1 after the edit.
7. Any register in §7.1 moves against its measured "NOT OWED".
8. A gate line prints no test count, or a dist-gated arm reports a skip where a figure was promised.
9. Anything owner-gated appears (schema, persisted shape, security posture, paid surface, a new capability).

## 12. Completion receipt

The receipt quotes, as executed output: the K-gate hashes (including the measured preamble SHA-256) and the two golden hashes before and after; the count prover's exit 0 from the kit path; the 525-row digest **before the edit and after, both from this run**, with the row count and the zero disagreement count; the verbatim diff of the three moved declarations (zero bytes) and the absence check on the remainder; the effective-line pair **280 → 259**, the raw pair **367 → 338** and the leaf's **22**; the re-read of this packet's own post-move addresses; `grep -c` = 1 for each of EP-0's two NC-1 strings; the `createPRNG(` counts in both entropy arms (expected **36** and **47**, unmoved); the tuning counter over the edited file (expected `{economyReconciliation.js: 2}`, unmoved); every `checks` line with its printed test count — ⛔ **a gate line with no printed count DID NOT RUN** (§P7); the excluded `tests/lint` directory run at exit 0 and the lighting walker run ALONE with **this packet's delta of `files +0 · parked +0 · credited +0 · titles +0 · suiteTitles +0`**; both TypeScript configurations by name with their exact floors; and the byte facts this member hands the holder (its two membership answers and its predicted ≤184 B), with the holder named. Judgment calls are named with the alternative rejected and how to reverse them.

## 13. Questions only the chair can answer

⭐ **FOUR QUESTIONS. Q3 and Q5 of version 2 are ANSWERED BY MEASUREMENT and closed below; Q6 is folded into Q1.**

**Q1 — ⛔ DOES THE SIXTH AMENDMENT'S ONE-HOLDER RULE DISCHARGE AMENDMENT-5 RULING 7, AND WHO HOLDS EM-T14'S BYTE ARM?** Ruling 7 (ODQ §934.47 addendum 121) held that EM-R0d and EM-R0f **may never ride one train**, on the ground that both carried a `TEST` row on `tests/build/generationWorkerLazy.test.js` and that file is not row-keyed (§P10.8), so the validator refuses the pair. The chair's launch brief places **EM-R0d v7 · EM-R0f v2 · EM-R6 v6 on train EM-T14 together** and directs each member to compile as if it is not the holder — which is §P2.11's cure, written after ruling 7. Version 3 has withdrawn its byte row, and **MEASURED at `bfca6b7d6`: EM-R0d ∩ EM-R0f = ∅ and EM-R6 ∩ EM-R0f = ∅**, so the refusal ruling 7 names cannot arise. **Recommendation: RULE THE EDGE DISCHARGED AND NAME EM-R0d THE HOLDER.** EM-R0d's bound is the larger (**≤3,314 B** against this member's **≤184 B**), it lands first in the family order, and its bound is the natural one to state as covering EM-T14's SUM (≤3,498 B before EM-R6's own price). If the chair names **EM-R0f** the holder instead, this packet returns the row exactly as version 2 spelled it and **EM-R0d drops its own** — the choice is symmetric, and only one of the two may be made. ⛔ **Whichever way it falls, any re-mint of the ceiling remains the OWNER's** (§P11.2), which is also the answer to version 2's Q6: the holder MEASURES and the owner SIGNS, so no standing conditional ruling needs to be confirmed for this member.

**Q2 — does §6.4's correction TIGHTEN the cardinality arm?** `:169`'s title says "exactly the 6 known edges" and `:174` asserts `<= 6` against a live count of **5** — one unit of slack in a set whose header says it can only shrink. **Recommendation: YES** — retitle to name 4 files / 5 edges and tighten to `toBeLessThanOrEqual(5)`. It is a shrink of a shrink-only ratchet, the measurement is its receipt, and the per-file arm already reds any new importer, so nothing lawful is newly blocked. ⭐ **§P2.18's estate grep clears it, executed:** no `requiredSymbols` row anywhere quotes the title or the bound — EM-P4 (LANDED), EM-R0d and EM-B2a4 all quote `const BASELINE_EDGES`, which this packet does not touch; the only other estate hits for the title are packet PROSE in EM-B2a4, which will be LANDED and therefore outside the citation walker by the time this member builds. If the chair prefers strict "prose only", the title is reworded to stop claiming six while the arm keeps its slack and a comment names it.

**Q3 — is `ECONOMY_FINGERPRINT_VERSION` exported from the new leaf?** (Version 2's Q4.) Nothing reads it today; re-measured at `bfca6b7d6`, `git grep -l -F 'economyFingerprint' -- tests scripts docs` → **0 hits anywhere**. **Recommendation: NO** — keep the move minimal and private. If EM-R0c needs the version string to tag a recomputed fingerprint, R0c adds the export in its own commit, where the consumer is visible.

**Q4 — does the packet's own body carry the `:NNN` addresses, or should it carry none?** Version 3 re-spells every address into `economyReconciliation.js` as the POST-EDIT one, because a READY packet body is live in `sourceCitationIntegrity.walker.test.js`'s ARM 2 and version 2's pre-move address for the freshness caller would have been a NEW past-EOF row against a ratchet that never grows. The alternative is to strip the addresses entirely and navigate by symbol alone. **Recommendation: KEEP the post-edit addresses.** They are what §8 step 4 re-reads against the file the build actually produces, which is the cheapest possible detector of a mis-executed move; symbols alone would lose that check. ⛔ Either way the pre-move address of the freshness caller may not return, in any sentence, in any framing.

---

### §13.1 · CLOSED BY MEASUREMENT AT THE EM-T14 PRE-PROOF — no chair act owed

- **Version 2's Q1 (the charter says the frozen set SHRINKS; measured, it does not).** Still true at `bfca6b7d6` and still not a refutation: the charter's `because` clause is forward-looking, so EM-R0f is a **PREVENTIVE** member whose correct post-move cardinality is the same 4 / 5. §1.1 states it and §6.4 item 2 writes it into the ratchet's own header. Left as a one-line charter re-cut at the chair's convenience, not a blocker.
- **Version 2's Q3 (do the dist-gated arms belong in `checks`?).** ⭐ **ANSWERED BY THE SIXTH AMENDMENT ITSELF**, §P2.11: a member's *"dist-reading byte ARMS LEAVE the sealed `checks`"* and its proof is the membership probe that cannot skip. No ruling is owed.
- **Version 2's Q5 (does EM-R0c's row need amending?).** ⭐ **MEASURED: NO.** EM-R0c's kit copy already names the new home — its §6 spells `import { fingerprintPowerEconomyInput } from '../../data/economyFingerprint.js';`, its §5 row **V5′** is written CONDITIONAL (*"the symbol this packet actually IMPORTS is `fingerprintPowerEconomyInput` at `src/data/economyFingerprint.js`, which DOES NOT EXIST at `141a1d775` … It is EM-R0f's CREATE … The chair stamps `__BASE__` at or after EM-R0f's landing"*), and its §0 records that *"EM-R0f alone touches `src/generators/power/economyReconciliation.js`"*. ⚠ **One divergence, recorded and not adjudicated:** EM-R0c's blockquote of an earlier plan asks EM-R0f to *"keep a forwarding export"*. §6.2 refuses one and retires the text instead, which TOOL-29's seal-aware cross-packet discharge makes lawful (§P10.9) and which is the reading that leaves ONE address for one fact. If the chair prefers the stub, §6.2 and the four `retiredSymbols` rows change together — but the ambiguity the move exists to remove would come back.
