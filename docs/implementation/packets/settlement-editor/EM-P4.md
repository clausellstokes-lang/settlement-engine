# Settlement editor / EM-P4 — THE POOLS' SOURCES GET LAWFUL ADDRESSES: the institutional catalogue's tier lookups move DOWN a layer behind a stable domain address, and the two status unions gain a value-level home in their own roster files

- **Status:** `READY`
  ⚠ The status value above stands ALONE on its line because `parsePacketHeader` anchors the status
  row at end-of-line and takes `status` only when exactly one row matches. Every stamp, caveat and
  date goes on these continuation lines, never on the row.
  ⭐ **THIS PACKET IS A PREPARATION, NOT A FEATURE.** It writes no pool, no editor code, nothing
  under `src/domain/edit/`, nothing a user can see. It exists because EM-A2a's pre-proof at
  `e80a6a3f4` measured two independent BLOCKS that no edit to EM-A2a can cure: a landed shrink-only
  architecture ratchet forbids the import EM-A2a's own fence requires, and a landed union walker's
  derived trigger collapses if EM-A2a spells the status words itself.
  ⭐ **CANDIDATE FIFTH MEMBER OF TRAIN EM-T9** (EM-A1 v5.2 · EM-B1h v6 · EM-D0a v2.1 · EM-D0b v2).
  Change-manifest intersection with all four, and with EM-B3d, is **EMPTY** — executed.
  ⛔ **EM-D0b IS THE TRAIN'S BYTE-ARM HOLDER** (interim rule 14). This packet carries **no** row on
  `tests/build/vendorPdfLazy.test.js` and states its own predicted price in §2.3 and §7.
- **Packet version:** 1
- **Verified base:** `em-t10-p4-2026-09-21` at `e348d59b609e2c62c957a4cd78653849792043b7`
  ⚠ Left for the chair's promotion stamp (interim rule 5). **The revalidation sentence the chair
  will use:** *"Re-measured at `e80a6a3f443e2f6a36e28fbebb72e98e46869fac` (read tip
  `read-tip-em-t9`, detached, `git status --short` EMPTY at the start and the end of the lane;
  nothing was written inside it): both `CREATE` production targets and both `CREATE` test targets
  are ABSENT; all fifteen `requiredSymbols` are present VERBATIM; all six `retiredSymbols` retirees
  are present VERBATIM in `src/generators/lookups.js` (which is what the non-terminal arm of the
  retirement check demands while the packet is DRAFT or READY); the preamble measured
  `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`; the eager first-paint set
  measured **269**, with `src/generators/lookups.js` a member; the generation worker's static
  closure measured **219** modules and does NOT contain `src/generators/lookups.js`."*
  ⛔ A `__BASE__` packet can never pass `validate:packets`; validation is downstream of this stamp.
- **Last revalidated:** left for the chair, with the sentence above.
- **Depends on:** `NONE`. ⭐ **THIS PACKET FLOATS.** It imports no file a sibling creates and names
  no symbol that does not exist today. ⭐ **EM-A2a DEPENDS ON IT**, not the other way round: EM-A2a
  version 6 re-aims at the addresses §6.2 and §6.4/§6.5 mint.
- **Collision group:** **NONE.** Executed against the four EM-T9 members' capsules and EM-B3d's:
  every intersection is `[]`. ⚠ `src/generators/lookups.js` is named by the LANDED packet **MF-CH3**
  — not as a collision (MF-CH3 is terminal and reserves nothing) but as **six `requiredSymbols`
  rows this packet must discharge**; see §4 and the capsule's `_retiredSymbolsNote`.
- **Commit authority:** edits only; the chair commits.
- **Baseline posture:** measured. `src/generators/lookups.js` read **whole** (193 lines);
  `src/domain/entities/npcs.js` and `src/domain/entities/status.js` read **whole**;
  `tests/build/domainGeneratorsBoundary.test.js`, `tests/lint/catalogTierGateParity.walker.test.js`
  and `tests/lint/statusUnionTotality.walker.test.js` read **whole**; EM-P3's landing `f4e5b64c5`
  read by `--stat` and by its two created files whole; the eager first-paint graph by IMPORTING
  `vite.config.js`'s own export in two scratch estates; the generation worker's closure by a static
  import walk; `ENFORCER_DIRS` read at source; `docs/implementation/PACKET_MANIFEST.json` walked by
  program for every row naming a change path. Receipts in `EM-P4.evidence.md`.
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: TO BE STAMPED BY THE CHAIR;
  measured at this read tip with `shasum -a 256` as
  `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`).
  > **Interim compile rules:** `COMPILE-RULES.interim.md` (the chair, 2026-09-20) — the fifth
  > amendment's rules (1–16 as of 2026-09-21), obeyed before they land. Where this packet and
  > `EM-PREAMBLE.md` (SHA-256 `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`)
  > disagree, the interim rules govern and this row is the record of it.

---

## §1 · Reconciled authority

| source | section + item | what it rules here |
|---|---|---|
| the chair | **judgment 79**, 2026-09-21 02:38 | the three clauses of *WHAT EM-P4 IS*: the institutions lookup moves DOWN a layer on EM-P3's landed pattern; each union gets ONE exported frozen vocabulary IN ITS OWN HOME FILE with a both-directions pin; terrain and culture need nothing |
| the owner | **2026-09-21 01:48** | *"push all the fixes back that don't regard the edit mode"* — Edit Mode first. This packet is Edit Mode's, and nothing else rides it |
| design `DESIGN_EDIT_MODE_AND_DECREES.md` | **§12.7** | pools read the **GENERATOR's** catalogue, never the display seams and never a second catalogue. This packet relocates that catalogue's reader; it does not copy it |
| `tests/build/domainGeneratorsBoundary.test.js` | its own failure message | *"If the new coupling is unavoidable, **invert it** (move the shared leaf down a layer or DI the generator fn) **rather than widening the baseline**."* §6.1–§6.2 are that inversion |
| `tests/lint/statusUnionTotality.walker.test.js` | ODQ §934.47 ruling **R6′**, and A3 — T2's own message | *"a SHRINK means a foreign vocabulary claimed one of these words and R6′ must be re-ruled by the chair — it is never a lane's choice."* §6.4/§6.5 are the shape that leaves the trigger untouched, proved rather than argued |
| the landed `EM-P3` (`f4e5b64c5`) | its subject line and both created files | the pattern: values in `src/data/<name>.js`, a stable `src/domain/<name>.js` address for readers outside the generator, and ⛔ *"NO `src/generators/**` MODULE MAY IMPORT THIS FILE"* — because `computeEngineSharedDomain()` seeds the eager chunk from every `src/domain` module a generator imports |
| the landed `MF-CH3` | its six `requiredSymbols` rows | the discharge obligation §4 and the capsule's `retiredSymbols` answer (§379.2) |
| interim rules | **1, 2, 4, 5, 6, 8, 9, 10, 12, 13, 14, 15, 16** | the deferred census row (`+1`); the byte price stated, the dist arm out of `checks`; a new test file is `CREATE`; the Status row alone on its line; §7 set-equal to the capsule; sealed `checks` naming FILES and ending with the validator; every register figure a DELTA; the count prover; the line-addressed register sweep (`none found`); **EM-D0b is the byte-arm holder**; `tests/domain` is not an `ENFORCER_DIR`; the old-spelling sweep, which is what found MF-CH3 |

---

## §2 · Outcome

### §2.1 What this packet delivers
**TWO new pure leaves, ONE seam, TWO appended vocabularies and TWO new test files.** After it:

- `src/data/institutionLookups.js` holds the institutional catalogue's four tier readers and their
  three private helpers. It imports `./constants.js` and `./institutionalCatalog.js` and nothing else.
- `src/domain/institutionLookups.js` re-exports the three a domain reader needs. **No
  `src/domain/**` module ever has to import `src/generators/**` to read the catalogue again.**
- `src/generators/lookups.js` keeps **every export name it has today**, four of them as bare
  re-exports of the new home — **measured identity, not equality**.
- `NpcStatus` and `EntityStatus` each have an exported, frozen, value-level vocabulary **in the
  union's own home file**, pinned both directions against the typedef's own source text.
- EM-A2a version 6 can then be written with **no domain→generators edge and no status literal**.

### §2.2 ⛔ WHY THE HOME IS `src/data/` AND NOT A NEW `src/domain/` LEAF — MEASURED
`src/generators/lookups.js` is an explicit **EAGER FIRST-PAINT SEED** (`vite.config.js`, the
`join(SRC, 'generators/lookups.js')` line), and it is routed into the `data` chunk by a
manualChunks rule of its own. Anything it re-exports from therefore joins the eager closure by
construction; there is no placement that avoids the `+1` module. Putting the BODIES in
`src/domain/` instead would be worse in both directions: it would make the generator's own reader
import `src/domain/**`, which `computeEngineSharedDomain()` reads as a seed — the exact +2-module,
three-budget trap EM-P3's `src/domain/worldFactOptions.js` docblock records. `src/data/` is below
both layers, is what the reader already depends on, and is what EM-P3 chose for the same reason.

### §2.3 ⛔ THE PRICE, PER CLOSURE, PREDICTED (interim rule 2) — AND WHO HOLDS THE ARM

| closure | live figure at `e80a6a3f4` | EM-P4's predicted price |
|---|---|---|
| **generation worker** | `WORKER_BUNDLE_CEILING_BYTES = 1401208` (EXACT, zero slack) | **+0 B, by construction.** The worker's static closure is **219 modules** and does **not** contain `src/generators/lookups.js` — measured. Nothing this packet touches is reachable from it |
| **eager first paint** | `CLOSURE_BUDGET_BYTES = 1_048_000` (`vendorPdfLazy.test.js:565`, pinned at `:1769`) | **+1 MODULE (269 → 270) and ≈+0 B.** The one new member is `src/data/institutionLookups.js`; `src/domain/institutionLookups.js` is **NOT** a member (measured in the packet's own estate). The bytes are code RELOCATED inside one closure whose single eager importer is pinned to the `data` chunk, so the delta is re-export wiring only |
| **lazy engine** | `expect(size).toBeLessThan(679_000)` | **+0 B** — no lazy-engine module imports either new leaf |
| **five edge-shared metas** | — | **+0 B** — `grep -c 'institutionLookups' scripts/build-edge-shared.mjs` → **0**; that script names no `src/data` path at all |

⛔ **NO TEST ASSERTS THE EAGER MODULE COUNT.** Measured across all five importers of
`EAGER_FIRST_PAINT_MODULES`: every arm is a membership test or a `>50` / `>100` anti-vacuity
bound. The only literal `268` in `tests/build/` is a historical AS-OF comment in
`generationWorkerLazy.test.js:155`, already stale at the tip (the live figure is **269**) and **not
this packet's to rewrite** (interim rule 9: executed history keeps its figure).
⛔ **THE SUMMED BYTE ARM IS EM-D0b's** (interim rule 14). EM-P4 adds `≈+0 B` to that sum.

### §2.4 In scope / out of scope
**IN:** the two new leaves; the seam; the two appended vocabularies; the two new test files.
**OUT:** `src/domain/edit/**` (EM-A2a's); any pool; any surface; `getTierOrder` and
`getPopulationRanges` (they are not institution lookups); terrain and culture — **they already have
their lawful address, `src/domain/worldFactOptions.js` (EM-P3), and EM-P4 adds nothing for them**;
widening either status union; re-ruling R6′; the MF-CH3 packet's own text.

---

## §3 · Hard scope budget

| limit | this packet | cap |
|---|---|---|
| behaviour families | **0 — nothing behaves differently** | 1 |
| new persisted record families | **0** | ≤1 |
| feature flags | **0** | ≤1 |
| user-facing surfaces | **0 — nothing renders, nothing mounts** | ≤1 |
| direct production consumers of the new addresses | **1** (`src/generators/lookups.js`, the seam) | ≤2 |
| new logic-bearing production leaves | **2** | ≤2 |
| existing logic-bearing production files modified | **3** | ≤3 |
| handwritten files total | **7** | ≤12 |
| new/changed effective production lines | **≈157** (the home ≈120 relocated; the domain address ≈10 code; `lookups.js` −110/+9; `npcs.js` +4; `status.js` +4 — comments excluded) | ≤400 |
| each new production leaf | home **≈120** · domain address **≈10** | ≤250 each |
| the two new TEST leaves | `institutionLookupsAddress` **≈120 eff / 6 arms** · `statusVocabularies` **≈95 eff / 5 arms** | ≤250 each |
| shared / hot-file delta | `src/generators/lookups.js` is a **MOVE**, not an accretion: net **−101 eff** | ≤15 growth |
| named acceptance cases | **8** | ≤8 |

Overrides approved before dispatch: **NONE — none is needed.**

**Hot files.** `src/generators/lookups.js` has **8** live test importers by basename and three
production importers; every one of them keeps working **because the export names and the function
objects are identical** — which is P2's assertion, not a hope. `src/domain/entities/npcs.js` has
**13** test importers and `status.js` **7**; both edits are a single appended `export const` after
an existing constant, so no existing export's declaration, value or order moves.

---

## §4 · Sealed dispatch and preflight

- **branch name** — the chair's (`em-t9-p4-2026-09-21` is the lane branch the brief names); the
  packet asserts nothing.
- **ancestry / `Verified base`** — `__BASE__`, stamped at placement (interim rule 5).
- **CREATE targets absent** — **four, all absent** at `e80a6a3f4`: `src/data/institutionLookups.js`,
  `src/domain/institutionLookups.js`, `tests/domain/institutionLookupsAddress.test.js`,
  `tests/domain/statusVocabularies.test.js`.
- **non-CREATE rows must EXIST** — all three `MODIFY` paths exist.
- **`retiredSymbols` while non-terminal** — the retirement check demands every retiree still be
  **present**; all six are, verbatim, in `src/generators/lookups.js`. This is why the six are **not
  also `requiredSymbols`**: a packet naming one pair as both is an error *and* "contributes NO
  discharge".
- ⛔⛔ **THE ORDERING FENCE THE BUILD LANE MUST OBEY.** §379.2's discharge fires **only for a
  LANDED** `retiredSymbols` row. MF-CH3 is LANDED and holds six `requiredSymbols` rows on exactly
  the six declarations §6.3 deletes. Therefore **the code move and EM-P4's flip to `LANDED` must be
  ONE commit.** A tree whose `lookups.js` is already moved while EM-P4 is still `READY` fails
  `node scripts/implementation-packets.mjs validate` with **six** errors naming MF-CH3, and §7.4's
  sealed-burn exemption cannot help — it covers a packet's own retirees, never another packet's
  required rows. **Executed, red first:** the moved tree without this capsule → `ok:false`, six
  errors; with it, LANDED → `ok:true`, zero.
- ⛔ **A LANDED retirement is a SUBSTRING scan of the whole file.** After the move, none of the six
  retired texts may survive anywhere in `src/generators/lookups.js` — **comments included**. §6.3's
  re-export block names bare identifiers only, and its comment never spells `export const <name>`.
- **git-clean** — the lane wrote nothing in any tree; `git status --short` in `read-tip-em-t9` was
  EMPTY at the start and at the end.

---

## §5 · Verified tree contract

Every row re-found BY SYMBOL at `e80a6a3f4` with `git grep -c -F` (the fixed-string form; `-E`
silently ignores `\s`).

| path | symbol | count | why this packet needs it |
|---|---|---:|---|
| `src/generators/lookups.js` | `export const getTierOrder` | 1 | a survivor — proves the file was relocated, not emptied |
| `src/generators/lookups.js` | `export const getPopulationRanges` | 1 | the second survivor |
| `src/data/constants.js` | `export const tierAtLeast` | 1 | the gate's arithmetic, travels with the bodies |
| `src/data/constants.js` | `export const TIER_ORDER` | 1 | the merge and the full-meta view iterate it |
| `src/data/institutionalCatalog.js` | `export const institutionalCatalog` | 1 | the home's second and last import |
| `src/domain/entities/npcs.js` | `export const NPC_UNAVAILABLE_STATUSES` | 1 | the append anchor; `statusUnionTotality` A1 asserts this export from this exact file |
| `src/domain/entities/npcs.js` | `{'active'\|'dead'\|'exiled'\|'jailed'\|'missing'\|'removed'\|'retired'} NpcStatus` | 1 | the union the new value mirrors, read as source text |
| `src/domain/entities/status.js` | `{'active'\|'impaired'\|'removed'\|'destroyed'\|'vacant'} EntityStatus` | 1 | ⭐ **`EntityStatus`'s home is `status.js:23`, MEASURED** — `npcs.js` only cites it |
| `src/domain/entities/status.js` | `export const STATUS_VACANT` | 1 | the append anchor and the fifth constant the array is built from |
| `tests/lint/catalogTierGateParity.walker.test.js` | `if (rel === 'src/generators/lookups.js') continue;` | 1 | A5's exemption by EXACT path — still correct after the move, and un-edited |
| `tests/build/domainGeneratorsBoundary.test.js` | `const BASELINE_EDGES = Object.freeze({` | 1 | the ratchet this packet satisfies; no entry added |
| `tests/lint/statusUnionTotality.walker.test.js` | `.toEqual(['dead', 'exiled', 'retired']);` | 1 | the derived trigger's pin, which EM-P4 must leave exactly where it is |
| `vite.config.js` | `export const EAGER_FIRST_PAINT_MODULES = EAGER_MODULES;` | 1 | the derivation the membership probe imports |
| `vite.config.js` | `join(SRC, 'generators/lookups.js'),` | 1 | the eager SEED — the reason the new home is a first-paint member |
| `vite.config.js` | `if (id.includes('/src/generators/lookups.js'))` | 1 | the `data` chunk rule the new home rides |

**RETIRED (six, `src/generators/lookups.js`):** `export const institutionAvailableAtTier` ·
`export const getInstitutionalCatalog` · `export const getFullCatalogWithTierMeta` ·
`export const getInstitutionsForTier` · `const filterCatalogByTierGate` · `const effectiveTierOf`.
Each present verbatim at the base (`grep -c -F` → 1) and each named by **MF-CH3**.

---

## §6 · Exact contracts

### §6.1 `src/data/institutionLookups.js` — THE ONE HOME (`CREATE`)
Moved **verbatim** out of `src/generators/lookups.js`, docblocks and `[CH-3 §3.1]` annotations
included: `institutionAvailableAtTier`, `effectiveTierOf`, `filterCatalogByTierGate`,
`mergeTierCatalogs`, `getInstitutionalCatalog`, `getFullCatalogWithTierMeta`,
`getInstitutionsForTier`, and the `CatalogRow` / `CatalogShape` typedefs they are annotated with.
Two imports and no more: `import { TIER_ORDER, tierAtLeast } from './constants.js';` and
`import { institutionalCatalog } from './institutionalCatalog.js';`
⛔ **No behaviour change of any kind.** Not one expression is rewritten.
⭐ **The measured MINIMAL closed set is three exports** — `getInstitutionsForTier`,
`getInstitutionalCatalog`, `institutionAvailableAtTier` — plus `filterCatalogByTierGate` and
`mergeTierCatalogs`. `getFullCatalogWithTierMeta` and `effectiveTierOf` move **with** them because
`effectiveTierOf` and `filterCatalogByTierGate` are two spellings of the same `minTier` gate and
the file's own docblock says the two filters must not disagree. **§13 R1 records this as the
chair's to confirm or narrow.**

### §6.2 `src/domain/institutionLookups.js` — THE STABLE DOMAIN ADDRESS (`CREATE`)
One `export { … } from '../data/institutionLookups.js';` naming exactly
`institutionAvailableAtTier`, `getInstitutionalCatalog`, `getInstitutionsForTier`, plus the
docblock EM-P3's address carries in its own voice: no `src/generators/**` module may import this
file, and the generator takes the `src/data` address directly.
⛔⛔ **THE BASENAME AND THIS FILE'S COMMENT WORDING ARE BOTH LOAD-BEARING.**
`catalogTierGateParity` A5 scans **raw source, comments included**, of every `.js`/`.jsx` under
`src/generators`, `src/domain` and `src/lib` for `from '<…>/lookups.js'` and reds on a hit.
**MEASURED PASS → FAIL → PASS:** naming the home `src/data/lookups.js` reds A5 by this file's
import; so does a COMMENT here that quotes such an import line. `institutionLookups.js` does not
match, and no comment in this packet quotes the pattern.

### §6.3 `src/generators/lookups.js` — THE SEAM (`MODIFY`)
Delete the six declarations named in §5's RETIRED list, the `tierAtLeast` import and the
`institutionalCatalog` import. Keep the header docblock (amended to name the new home),
`getTierOrder`, `getPopulationRanges` and the `TIER_ORDER, POPULATION_RANGES` import. Append ONE
block:

```js
export {
  institutionAvailableAtTier,
  getInstitutionalCatalog,
  getFullCatalogWithTierMeta,
  getInstitutionsForTier,
} from '../data/institutionLookups.js';
```

⛔ Bare re-export, never `import`-then-`export`: identity must survive. ⛔ No comment in this file
may contain any of the six retired texts (§4).

### §6.4 `src/domain/entities/npcs.js` — `NPC_STATUS_VALUES` (`MODIFY`)
Appended immediately after `NPC_UNAVAILABLE_STATUSES`, with its own docblock:
`export const NPC_STATUS_VALUES = Object.freeze(['active','dead','exiled','jailed','missing','removed','retired']);`
— the SEVEN members in the typedef's own codepoint order, spelled as **literals**, which is lawful
**only here**: `foreignVocabularies()` skips consumer-roster files, and `npcs.js` is
`CONSUMER_ROSTER[0]`.

### §6.5 `src/domain/entities/status.js` — `ENTITY_STATUS_VALUES` (`MODIFY`)
Appended immediately after `STATUS_VACANT`:
`export const ENTITY_STATUS_VALUES = Object.freeze([STATUS_ACTIVE, STATUS_IMPAIRED, STATUS_REMOVED, STATUS_DESTROYED, STATUS_VACANT]);`
⛔ **BY REFERENCE, NEVER AS FRESH LITERALS.** `status.js` is **not** a roster file, so an array of
string literals here would be read as a foreign vocabulary. Built from the five constants, the
walker's declaration regex captures no quoted literal, `FOREIGN` is untouched, and
`wordsIn(status.js, NON_ACTIVE)` stays at **one** — below the `>= 2` enumerator threshold.

### §6.6 Determinism, lifecycle, receipts
- **Hash / fork key / ordering:** none minted. Both new arrays are authored in their typedef's own
  order; `TERRAIN_WEIGHTS`-style positional consumption is why order is pinned by P7 rather than
  left to a sort.
- **Lifecycle, every path** — *create / read / persist / regenerate / undo / import / migrate*:
  **nothing persists.** Two frozen module constants and seven relocated pure functions. No stored
  byte moves and no save shape changes on any path.
- **Golden posture: UNCHANGED, and it is structural rather than argued.** The generator never
  imports `src/generators/lookups.js` — `catalogTierGateParity` A5 is the standing proof, re-run
  green in `checks` — and the generation worker's closure does not contain it. The two vocabularies
  have **zero** importers at this landing.
- **Flag / dormancy:** no flag. `src/domain/institutionLookups.js`, `NPC_STATUS_VALUES` and
  `ENTITY_STATUS_VALUES` have **zero** production importers until EM-A2a lands; that dormancy is
  what makes P1–P8 the only proof this packet owes.

---

## §7 · Exact change manifest

Generated from `EM-P4.manifest.json` and proved SET-EQUAL to it (interim rule 6; executed line in
`EM-P4.compile.report.md` §A).

| Action | File | Symbol / region | Max delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `src/data/institutionLookups.js` | the four readers + `effectiveTierOf`, `filterCatalogByTierGate`, `mergeTierCatalogs`, the two typedefs | **≈120 eff** | §6.1. Move VERBATIM. ⛔ Two imports only: `./constants.js`, `./institutionalCatalog.js`. Rewrite no expression. |
| `CREATE` | `src/domain/institutionLookups.js` | the three-name boundary | **≈10 eff** | §6.2. ⛔ Basename is NOT `lookups.js`, and no comment here may quote an import specifier ending in the reader's basename. |
| `MODIFY` | `src/generators/lookups.js` | the six retired declarations → one bare re-export block | **−110 / +9 eff** | §6.3. ⛔ `export … from`, never import-then-export. ⛔ None of the six retired texts may survive, comments included. |
| `MODIFY` | `src/domain/entities/npcs.js` | `NPC_STATUS_VALUES` | **+4 eff** | §6.4, appended after `NPC_UNAVAILABLE_STATUSES`. Literals, seven, typedef order. |
| `MODIFY` | `src/domain/entities/status.js` | `ENTITY_STATUS_VALUES` | **+4 eff** | §6.5, appended after `STATUS_VACANT`. ⛔ Built from the five `STATUS_*` constants BY REFERENCE. |
| `CREATE` | `tests/domain/institutionLookupsAddress.test.js` | six arms | **≈120 eff** | §9 P1–P6. **6 literal-titled `it`s in ONE literal `describe`**, straight-line, no loop, no `.each`, no nesting. ⛔ Do not rename the file. |
| `CREATE` | `tests/domain/statusVocabularies.test.js` | five arms | **≈95 eff** | §9 P7–P8. **5 literal-titled `it`s in ONE literal `describe`**, same shape rules. |

Generated artifacts: **NONE**.

### The deferred rows — PREDICTIONS, named in no `checks` and in no `changeManifest`

> `docs/content/wiring-census.json` — **a named INTERIOR RED, delta only** (interim rule 1).
> `stamp.producerIndexFiles` **+1**: the one new `.js` leaf under the two producer roots
> `src/generators/**` and `src/domain/**` is `src/domain/institutionLookups.js`.
> `src/data/institutionLookups.js` is **NOT** under a producer root — measured by reading
> `scripts/wiring-census.mjs`'s own two `jsFilesUnder` roots. `stamp.files`,
> `stamp.candidateLeaves` and `totals.*` **UNMOVED** — all key on
> `src/domain/display/stateProse/` paths this member does not touch. ⛔ Regenerated WHOLE at the
> train's terminal BY THE CHAIR; the path appears nowhere in §7, the capsule or `checks`.

> `tests/lint/.lighting-census-baseline.json` — the delta, in the register's own order:
> **files +2 · parked +0 · credited +2 · titles +11 · suiteTitles +2.** The `+11` is `6 + 5`.
> ⛔⛔ `credited` rather than `parked` is **CONDITIONAL** on both CREATEs' pinned shape (one
> top-level literal `describe`, straight-line literal-titled `it`s, no loop, no `.each`, no
> nesting, `it`/`test`/`describe` each bound exactly once from the file's own `vitest` import).
> **Any other tuple is a STOP, not a refreeze** — `parked +2 · credited +0 · titles +0` means the
> shape rules were broken. The row names the BASELINE JSON, never the walker, and this member never
> sets `LIGHTING_CENSUS_REFREEZE`.

> `tests/build/vendorPdfLazy.test.js` — ⛔ **NO ROW** (interim rule 14). **EM-D0b is train EM-T9's
> byte-arm holder.** EM-P4's contribution to the summed price is **≈+0 B** with **+1 eager module**
> (§2.3). The stated bound is `CLOSURE_BUDGET_BYTES = 1_048_000`.

> `scripts/mutation-coverage-manifest.json` — ⛔ **NOT OWED, and a voluntary row would be
> unlawful** (interim rule 15). `ENFORCER_DIRS`, read at
> `tests/lint/mutationCoverage.shared.mjs:36-45`, is `tests/lint · tests/design · tests/docs ·
> tests/data · tests/copy · tests/security · tests/edgeFunctions · tests/generators`. **`tests/domain`
> is not one of them**, and neither new basename matches a `NAME_PATTERN` token. Adding a key anyway
> reds the *no stale entries* arm.

> `scripts/.observed-shape-readers-baseline.json` — ⛔ **NO ROW.** Neither changed file is an
> `inventory` or `rowTags` key (`git grep -F 'src/generators/lookups.js'` finds it only inside the
> three content-addressed tree manifests); the additions introduce no property read on an
> unvalidated object, so `total` (**1964**) and `identities` (**1390**) are **UNMOVED**. ⭐ **The
> landed precedent is exact:** EM-P3 (`f4e5b64c5`) created `src/data/worldFactOptions.js` and
> `src/domain/worldFactOptions.js`, modified a `src/generators` leaf, and carried **no** row on that
> register. *(Deliberately deferred — documented, not a bug to re-find.)*

> **Interim rule 13 — the line-addressed sweep: `none found`.** Executed for all five `src/` paths:
> `git grep -n -F '<path>' -- tests/lint scripts`. `tests/lint/.prose-numerics-baseline.json`
> carries **0** rows for `generators/lookups`, `entities/npcs` and `entities/status`. The hits that
> do exist are path-keyed, not line-addressed: `tests/lint/.tuning-inventory.json` (a per-file
> count — the additions contain no numeric literal), `scripts/.clamp-primitive-baseline.json` (a
> path list — the additions clamp nothing), and two `scripts/mutation-coverage-manifest.json`
> rationale strings, which quote no line number of a changed file.

---

## §8 · Ordered coding sequence

1. **Red first.** Create `tests/domain/institutionLookupsAddress.test.js` with arm 2 alone and run
   it: it must RED, because `src/data/institutionLookups.js` does not exist. Quote the failure.
2. Create `src/data/institutionLookups.js` (§6.1) by MOVING the region — cut, do not retype.
3. Rewrite `src/generators/lookups.js` (§6.3). Re-run arm 2: it must GREEN on identity.
4. Create `src/domain/institutionLookups.js` (§6.2).
5. Write arms 1, 3, 4, 5, 6 (§9 P1, P3–P6). Run the file.
6. Append `NPC_STATUS_VALUES` (§6.4) and `ENTITY_STATUS_VALUES` (§6.5).
7. Create `tests/domain/statusVocabularies.test.js` (§9 P7–P8). Run it.
8. Run `checks` invocations 1–8 in order, then the membership probe (9), then the two typechecks,
   then the validator LAST.
9. Run the build lane's §10 instruments and report the lighting tuple **before** any refreeze.

---

## §9 · Acceptance matrix

| # | claim | home | how it is proved |
|---|---|---|---|
| **P1** | **The home is below both layers** | `institutionLookupsAddress.test.js` **arm 1** | its import list, read from its own source, is EXACTLY `./constants.js` and `./institutionalCatalog.js`; the matcher is proved live on a planted `../generators/pipeline.js` specifier first, so the empty offender list is a verdict |
| **P2** | ⛔ **SOURCE IDENTITY at the old address** | `institutionLookupsAddress.test.js` **arm 2** | `Object.keys(lookups)` is the same SIX names, exact sorted list; and `lookups.X === home.X` for all four moved bindings — identity, not deep-equality, so a re-implementation at either address reds. **EXECUTED at compile: four of four `true`** |
| **P3** | **The domain address is the same objects** | `institutionLookupsAddress.test.js` **arm 3** | `domain.X === home.X` for the three; `Object.keys(domain)` is EXACTLY those three, so `getFullCatalogWithTierMeta`'s absence is a recorded verdict. **EXECUTED: three of three `true`** |
| **P4** | ⛔ **The inversion is real, not nominal** | `institutionLookupsAddress.test.js` **arm 4** | `src/domain/institutionLookups.js` names no specifier containing `/generators/`, static or dynamic, with a planted positive control. **EXECUTED on the ratchet's own head: ARM 1 PASS, `liveCount=5 ≤ 6`, both for EM-P4 alone and for EM-P4 plus a simulated `pools.js` on the domain addresses** |
| **P5** | ⛔ **Nothing the catalogue answers moved** | `institutionLookupsAddress.test.js` **arm 5** | for every `TIER_ORDER` member plus `'all'`, `undefined`, `null`, `''`, `'random'`, `'custom'`, both readers agree with an eligible set **rebuilt in the test from the RAW catalogue**, never from the predicate under test; `institution.class` at `'town'` is EXACTLY **85**. **EXECUTED across ten tier arguments: byte-identical to the base tree** |
| **P6** | **The tier gate still bites, through the domain address** | `institutionLookupsAddress.test.js` **arm 6** | a metropolis-gated city row is ABSENT at `'city'` and PRESENT at `'metropolis'`, read through `src/domain/institutionLookups.js`; anchored by an un-gated shelf-sibling that must be present at both, so the absence cannot pass on a drifted collection |
| **P7** | ⛔ **The two unions get values that cannot drift** | `statusVocabularies.test.js` **arms 1, 2, 3** | each array set-equals its union **parsed from its own home file's source text**, both directions, plus the exact sorted seven and five; both frozen, duplicate-free, in the typedef's own order; the parser guarded in both directions on synthetic sources |
| **P8** | ⛔ **No second spelling, and the walker's trigger is untouched** | `statusVocabularies.test.js` **arms 4, 5** | every element of `ENTITY_STATUS_VALUES` is IDENTICAL to the `STATUS_*` constant of that name and the source is asserted to build from those constants, not from fresh literals; the two vocabularies' intersection is EXACTLY `['active','removed']`. ⛔ The walker's derived `TRIGGER` is **not** re-asserted here — `statusUnionTotality` owns it and is a sealed check. **EXECUTED on that walker's own head across three estates: `TRIGGER` `['dead','exiled','retired']`, `FLAGGED∖DECLARED` `[]`, `DISCOVERED_ENUMERATORS` 3 — byte-identical to the untouched tip** |

**8 of ≤8.** Every case has exactly one declared home, and every home is a `CREATE` row of this
packet's own §7 table.

---

## §10 · Verification commands

```sh
# THE SEALED CHECKS (the capsule's `checks`, in this order)
npx vitest run --pool=threads --maxWorkers=2 tests/domain/institutionLookupsAddress.test.js \
                                             tests/domain/statusVocabularies.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/lint/catalogTierGateParity.walker.test.js \
  tests/build/domainGeneratorsBoundary.test.js tests/generators/metropolisCatalogReachable.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/lint/statusUnionTotality.walker.test.js \
  tests/generators/densityLaw.test.js tests/lint/provenanceStampSingleWriter.walker.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/domain/entities.test.js \
  tests/domain/successors.test.js tests/domain/statusImpairmentSeverity.test.js \
  tests/domain/institutionStatusLifecycle.test.js tests/domain/institutionStatusModel.test.js \
  tests/property/npcs.property.test.js tests/property/status.property.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/ui/catalogTabsRestore.test.jsx \
  tests/ui/generalDeskTabFlow.test.js tests/generation.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/lint/negativeAssertionAnchor.walker.test.js \
  tests/lint/contractTestAntiVacuity.walker.test.js tests/lint/seedLoopTotality.walker.test.js \
  tests/lint/clampPrimitiveBaseline.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/lint/observedShapeReaders.walker.test.js \
                                             tests/lint/writerReach.walker.test.js
npx eslint src/data/institutionLookups.js src/domain/institutionLookups.js \
  src/generators/lookups.js src/domain/entities/npcs.js src/domain/entities/status.js \
  tests/domain/institutionLookupsAddress.test.js tests/domain/statusVocabularies.test.js

# the FIRST-PAINT membership probe — it IMPORTS the exported derivation, reads no dist, cannot skip.
# It asserts the +1 in BOTH directions: the new data home IS eager, the domain address is NOT.
node -e "const{relative}=require('node:path');const{pathToFileURL}=require('node:url');import(pathToFileURL('vite.config.js').href).then(m=>{const S=new Set([...m.EAGER_FIRST_PAINT_MODULES].map(a=>relative(process.cwd(),String(a))));const added='src/data/institutionLookups.js';const free='src/domain/institutionLookups.js';console.log('EAGER:',S.size,'| anti-vacuity main.jsx:',S.has('src/main.jsx'),'| seed lookups.js:',S.has('src/generators/lookups.js'),'| new data home eager:',S.has(added),'| domain address eager:',S.has(free));if(!S.has('src/main.jsx')||!S.has('src/generators/lookups.js')||!S.has(added)||S.has(free)||S.size<200)process.exit(1);})"

npm run typecheck:ratchet
npm run typecheck:domain:strict

# the seal, LAST
node scripts/implementation-packets.mjs validate
```

**How the sealed set was computed (interim rule 8).** `scripts/governing-tests.mjs` (TOOL-26) has
not landed at the integration tip, so the interim form applies, at `e80a6a3f4`:
`git grep -l -F 'lookups' -- tests scripts` → **18** files (**14** live `.test.` files);
`git grep -l -F 'entities/npcs' -- tests` → **13**; `git grep -l -F 'entities/status' -- tests` →
**7**; and per changed symbol, `getInstitutionsForTier` / `getInstitutionalCatalog` /
`getFullCatalogWithTierMeta` / `institutionAvailableAtTier` reach
`catalogTierGateParity.walker.test.js`, `metropolisCatalogReachable.test.js`,
`catalogTabsRestore.test.jsx` and the two selector-driven UI flows. ⛔ Never the `tests/lint`
directory; ⛔ never the lighting walker; ⭐ the validator last.

```sh
# ⛔ THE BUILD LANE'S INSTRUMENTS — NEVER SEALED CHECKS
#  (a) the EXCLUDED tests/lint directory run, which MUST EXIT 0
npx vitest run --pool=threads --maxWorkers=2 tests/lint --exclude=tests/lint/sovereigntyLightingContract.walker.test.js
#  (b) the lighting walker ALONE. This member predicts files +2 · parked +0 · credited +2 ·
#      titles +11 · suiteTitles +2. ANY OTHER TUPLE IS A STOP, not a refreeze.
npx vitest run --pool=threads --maxWorkers=2 tests/lint/sovereigntyLightingContract.walker.test.js
#  ⛔ DO NOT set LIGHTING_CENSUS_REFREEZE. The refreeze is the chair's, at the terminal.
#  (c) the BYTE ARM is a TEST row of EM-D0b's, not a check of this packet's, and it reads dist:
#      VERIFY_DIST=1 npx vitest run tests/build/vendorPdfLazy.test.js   # run ONCE for the train
#  (d) the browser suite: interim rule 10 — this packet touches NO src/components/**, no route, no
#      data-testid and no accessible name, so it names NO e2e spec.
```

---

## §11 · Stop conditions

STOP and report, do not adjudicate, if: the boundary ratchet's `BASELINE_EDGES` or `liveCount`
moves; `catalogTierGateParity` A5 reds (re-read §6.2 — the cause is a basename or a COMMENT);
`statusUnionTotality`'s `TRIGGER` is anything but `['dead','exiled','retired']`, or
`FLAGGED∖DECLARED` is non-empty, or `DISCOVERED_ENUMERATORS` is anything but 3; any identity in P2
or P3 is `false`; `getInstitutionsForTier('town').size` is not **85**; the eager first-paint set is
anything but **270** with the domain address absent; the lighting tuple is not
`+2/+0/+2/+11/+2`; or the validator reports any error naming **MF-CH3** (that is §4's ordering
fence, and the cure is the commit boundary, never a change to MF-CH3).

---

## §12 · Definition of done

All twelve `checks` green in order, the validator last and clean; the lighting tuple exactly
`files +2 · parked +0 · credited +2 · titles +11 · suiteTitles +2`, reported and NOT refrozen; the
excluded `tests/lint` run exit 0; §9's P1–P8 green in their two declared homes; the count prover
(`EM-P4.count-prover.mjs`) exit 0; the eager membership probe exit 0; `git status --short` showing
only this packet's seven files.

---

## §13 · Open rulings

| id | question | state |
|---|---|---|
| **R1** | The move's SCOPE: the measured minimal closed set is **three** exports (+2 privates); this packet moves **four** (+3), keeping the `minTier` gate's two spellings in one file. | **THE CHAIR'S.** Recommendation: move the four — splitting `effectiveTierOf` from `filterCatalogByTierGate` re-creates the drift CH-3 §3.1 exists to prevent. |
| **R2** | `src/domain/institutionLookups.js` re-exports **three**, not four. | **THE CHAIR'S.** Recommendation: three. An unread re-export is a claim nobody checks; EM-A2a reads exactly these three. |
| **R3** | The eager first-paint set rises **269 → 270 modules** at ≈+0 B. Interim rule 2 calls "a rise in the eager first-paint closure" an OWNER stop. | **THE CHAIR'S.** Recommendation: the owner-signed figure is the BYTE budget `CLOSURE_BUDGET_BYTES = 1_048_000`; the module count is a derivation no test asserts. EM-D0b's summed arm covers the bytes. If the chair reads rule 2 as counting MODULES, this is an owner question and the packet parks. |
| **R4** | §4's ordering fence — the code move and the `LANDED` flip must be ONE commit, or MF-CH3's six rows red. | **THE CHAIR'S to carry to the terminal.** Recommendation: state it in the train's landing order; no packet edit can substitute for it. |
| **R5** | `EM-A2a` version 6 must re-aim `institution.class` at `src/domain/institutionLookups.js`, `npc.status`/`institution.state` at the two new vocabularies, and terrain/culture at `src/domain/worldFactOptions.js`. | **RECORDED, not this packet's.** EM-P4 makes all three addresses exist; the re-version is EM-A2a's. |

---

## §14 · Scope-budget arithmetic

`≈120` (the moved home, relocated lines) `+ 10` (the domain address) `+ 9` (the seam's re-export
block) `+ 4 + 4` (the two vocabularies) `= ≈147` new-or-changed effective production lines, against
a `≤400` cap, with `−110` of the 120 being lines that LEAVE `src/generators/lookups.js` — the file's
net effective delta is **−101**. Test leaves: `≈120 + ≈95 = ≈215`, each under its own `≤250` cap.
Acceptance cases **8 of ≤8**. Handwritten files **7 of ≤12**.
