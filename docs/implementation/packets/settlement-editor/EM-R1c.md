# Settlement editor / EM-R1c — THE NESTED-RECORDPATH BAG BUILDER AND THE RULED ENTITY JOIN: a chooser is read AT THE RECORD PATH THE REGISTER NAMES, and the DM's value lands at the DECLARED RECORD PATH OF THE NAMED ENTITY

- **Status:** `READY`
  Compiled under the chair's judgment 233 (a–d), which ruled this member's two units, the dragged
  `stress` and the shape, and judgment 237 R1, whose three faction fields §0.6 folds in and §9
  asserts at the VALUE. ⛔ ONE instruction of judgment 237 IS REFUTED BY MEASUREMENT and is owed
  back to the chair: 237 R2's roster cure is NOT this member's units (§0.7) — no acceptance case
  claims it, and §11 item 7 STOPs a lane that improvises it. Its two units are ONE behaviour family: either alone is unfalsifiable at
  the corpus (unit 1 alone moves a refusal reason and lights nothing; unit 2 alone is unreachable
  behind unit 1) — measured, §0.3.
- **Owner:** the chair (Fable 5.1, session cce01f87) · compiled by an Opus COMPILE seat, 2026-09-23
- **Packet version:** `4`
  version 4 = version 3 with the type-floor cure’s rewrite of one pinned line retired
  (judgment 253) and §6 re-cut to the built text (judgment 248). The BUILD lane measured the
  compiled contract RED on the type floor — the three new functions sat BETWEEN `pinsFrom`’s
  JSDoc block and `pinsFrom`, detaching all four `@param` tags (four TS7006 under the strict
  config); `StepRoster` did not carry `paths` (TS2339 under BOTH configs); and
  `_ATOMIC_EDIT_JOINS`’ inferred literal type refused a `string` index (TS7053). The cure is
  REAL TYPES ONLY (no `any`, no `@ts-` directive, no baseline row) and it REWRITES
  `requiredSymbols[4]`, the PASS 4 array-branch write — so judgment 227’s law gives that row a
  `retiredBy` and §5.2 spells it verbatim: **`retiredSymbols` is FOUR**. Every other
  `requiredSymbols` row was re-audited against the BUILT tree at the lane’s HEAD (`grep -c -F`
  per row): eleven PRESENT at count 1, three already retired, one newly retired — no other row
  moved. §6, §3 and §5.8 now state what was BUILT, measured, not what was planned.
  version 3 = version 2 re-measured at train EM-T17's base `a545899ff` by an Opus PRE-PROOF seat.
  The J-T1 window over every manifest and `requiredSymbols` path is EMPTY, so no contract fact
  moved; what version 3 changes is what the compile could not yet see. (i) §0.3, §9's A4 and §12
  now assert ALL THREE institution ROOT fields at the value, not `state` alone — `name` and
  `category` were measured 0/63 → 63/63 exactly as `state` is, the same omission judgment 237 R1
  cured on the faction card, and the widening costs NO new case. (ii) §5.3 gains the general
  judgment-148 measurement: every one of the SEVENTEEN declared (card, field) pairs, tip versus
  plant, of which ONLY the institution card's three root fields move — which subsumes the five
  transcribed arms and covers `tests/generators/pipelinePinnedMode.test.js`, whose seventeen
  `pinsFrom`/`rederive` arms the compile had not reached. (iii) §5.4's LIGHTING DELTA is corrected
  from the walker's own classifier: the property file is CREDITED with 10 titles and 2 suite
  titles, so the delta is `titles +8 · suiteTitles +2`, not `+4 · +1`. (iv) THE COUNT LAW (§P9(f)):
  §5.4, §5.6 and §7 carried version 1's `it` counts against version 2's matrix; every row now
  states the same numbers and the count prover gains check (i) with its own failing control.
  (v) §5.5 is rewritten from measurement — EM-B2a2's `pipeline.js:182` `_note` was re-spelled by
  symbol in the base commit and the one surviving line address, `:59`, sits ABOVE the insertion
  point, so this member owes NO re-address. (vi) §5.4 gains the negative-assertion anchor
  constraint the compile did not carry: `tests/property` is held at EXACT ZERO and the TEST file
  carries no quarantine row.
- **Verified base:** `em-t17-r1c-2026-09-23` at `a545899ff55b7e4c3f219b3ca58cd86ddcf0f3cd`
- **Preamble:** docs/implementation/preambles/EM-PREAMBLE.md at SHA-256 `125c693214235a81bf4f2b38506b621859a35e0325ff2ea58d335b4e0681a99d` — RE-MEASURED at this pre-proof's read tip `a545899ff` (the SIXTH amendment; identical to the hash measured at `1b073009f`); the chair re-stamps at promotion.
- **Last revalidated:** `a545899ff55b7e4c3f219b3ca58cd86ddcf0f3cd`
- **Depends on:** EM-R1 (LANDED, the runner's shape-4 clone) · EM-R1b (LANDED, `_TRANSIENT_CHOOSERS` and the roster's `transient` channel, which unit 1 extends) · EM-R5 (LANDED, `partitionTrace`) · EM-R0a (LANDED, `recordRegister.js`'s `KEYED_COLLECTIONS`, which unit 2 reads) · EM-B2a4 (LANDED, `rederive`'s ground)
- **Collision group:** `src/generators/pipeline.js` and `src/domain/edit/dmLayer.js` with any later re-entry member; EM-R7's corpus ratchet reads both and asserts the VALUE at its declared leaf (judgment 233d).
- **Commit authority:** edits only; the chair commits
- **Baseline posture:** measured — every figure in §0 and §5 executed at `1b073009f` in plain `node`, the two files served in memory and the tree never written (`EM-R1c.evidence.md`)

## §0 · ⛔⛔ FIVE MEASUREMENTS THAT SHAPE THIS MEMBER — none adjudicated by the lane

### §0.1 · THE PREMISE REPRODUCES: `step_not_pinnable` 63 of 63, through the REAL `rederive`

An institution edit returns `unapplied: [{ reason: 'step_not_pinnable' }]` in 63 of 63 census rows at
the read tip, and the bag is EMPTY. The blocker is judgment 206h-4's: `pinsFrom`'s `held()` reads
`Object.hasOwn(source, key)` — the record's TOP LEVEL — while `generationRepairs` lands at
`record.generationCoherenceReceipt.repairs`, where the value IS present in 63 of 63 rows.

⭐ The register is well formed for the cure: over `GENERATION_TIER1`, **(key → recordPath) is a
FUNCTION** (0 of 51 keys carry two paths) and **FOURTEEN** chooser keys — not the slot's two — do not
live at `record.<ctxKey>`: `culture`, `effectiveConfig` (`record.config`), `generationRepairs`,
`magicLevel`, six `nearbyResource*` keys, `nearbyResourcesNative` (→ `record.config.nearbyResources`,
a RENAME rather than a nesting), `settlement` (`record` itself), `stressTypes`, `terrainType`.

⛔ The cascade is NOT reachable at the tip: `closureFor` RETURNS at the first unheld key, so
`isolationSupport`, `stress` and `stressTypes` are never demanded until the nested read exists.

### §0.2 · ⛔ THE CURE FORCES A RE-DERIVED KEY INTO THE BAG — RULED (judgment 233a)

With unit 1 planted the closure completes and the bag is, in 63 of 63 rows, exactly
`[generationRepairs, institutions, isolationSupport, stress, stressTypes]`. `stress` is dragged in by
the runner's OWN partial-pin rule and by nothing this member chooses: `institutions`'s provider also
provides `generationRepairs`; its other provider also provides `isolationSupport`; ITS other provider
also provides `stress`; and `stress`'s providers also provide `stressTypes`.

**RULED (233a): YES, with the consequence DECLARED.** Design §22.1 correction 5 (*`stress` is
RE-DERIVED, never held*) binds what a DM may HOLD, not a closure's transitive pin; §22.1's own final
ruling makes the consequence of an edit the DIFFERENCE between two re-derivations, so a key held
identically in `R0` and `R1` cancels in the merge. **Measured: `record.stress` MOVED in 0 of 63 rows**
under the institution edit. §12 carries the figure; §11 item 4 STOPs a lane that re-opens the ruling.

### §0.3 · ⭐⭐ THE TWO UNITS TOGETHER LIGHT THE CARD END TO END — AND EITHER ALONE IS UNFALSIFIABLE

Unit 1 ALONE moves the institution card from `step_not_pinnable` 63/63 to `unknown_key` 63/63 and
applies 0 of 63, because `pinsFrom` PASS 4 joins the edited entity by `.id` and **0 of 2,428**
generated institutions carry one (2,124 carry `catalogId`, 2,428 carry `name`). Design §22.4 RULES
this: an institution has *"no `id`"* and *"the merge keys by `name`"*. With BOTH units planted, over
the 63-row corpus through the REAL `rederive`, asserting **the VALUE at its declared leaf** and never
`unapplied === []`:

| card | applied | the value IN THE BAG at its leaf | the value ON THE RE-DERIVED RECORD | a stray `[].` key |
|---|---|---|---|---|
| `institution.state` | **63/63** (tip: 0/63) | **63/63** (tip: 0/63) | **63/63** (tip: 0/63) | 0/63 |
| `institution.name` | **63/63** (tip: 0/63) | **63/63** (tip: 0/63) | **63/63** (tip: 0/63) | 0/63 |
| `institution.category` | **63/63** (tip: 0/63) | **63/63** (tip: 0/63) | **63/63** (tip: 0/63) | 0/63 |
| `faction.power` | 63/63 | **63/63** (tip: **0/63**) | **63/63** (tip: **0/63**) | **0/63** (tip: **63/63**) |
| `npc.status` | 63/63 (unmoved) | 63/63 (unmoved) | 63/63 (unmoved) | 0/63 |
| `powerSeat.holder` | 63/63 (unmoved) | 63/63 (unmoved) | 63/63 (unmoved) | 0/63 |

and the DORMANCY control — an EMPTY layer re-derives the record byte-identically in **63 of 63** rows,
at the tip and under both units alike, which is the goldens' protection by construction.

⭐ **ALL THREE INSTITUTION ROOT FIELDS LIGHT, NOT `state` ALONE (re-measured at the pre-proof, whole
corpus, through the REAL `rederive`).** `name`, `category` and `state` each report applied 0/63 with
`step_not_pinnable` 63/63 at the tip and each CARRIES its value at
`record.institutions[<the named one>].<field>` in **63 of 63** rows under both units, with a stray
`[].` key in 0/63 throughout. `institution.name` is `kind: 'free-cascade'` and therefore RENAMES the
join field itself: the root key names the OLD name and the write sets the new one, which the pass
resolves in ONE hop — measured 63/63, exactly as `faction.faction` does (§0.6). A4 asserts all three
at the VALUE; the case count is unchanged at eight.

### §0.4 · ⛔ THE QUIET LIE UNIT 2 REMOVES, MEASURED (judgment 233d(ii))

At the tip a faction-card edit reports **applied 63/63 while the DM's share reaches no faction**:
`splitOutputKey('powerStructure.factions[].power')` returns the leaf `factions[].power`, PASS 4 takes
its `isPlainObject(bag)` branch and writes `bag['factions[].power'] = 40`, leaving
`factions[0].power` at the record's own value with `unapplied` EMPTY — a literal key nothing reads, in
63 of 63 rows. That is the estate's cardinal class, and it is the SAME defect as the institution
join: **a DM's edit must land at the DECLARED RECORD PATH of the NAMED entity.** Unit 2 owns the
write's target; EM-R7's ratchet gains the row that would have convicted it (judgment 233d).

### §0.5 · THE JOIN IS THE REGISTER'S, AND THE ONE COLLECTION IT LEAVES OPEN IS MEASURED

`src/domain/edit/recordRegister.js :: KEYED_COLLECTIONS` — EM-R0a's LANDED register — already declares
the ruled join for two of the three editable collections, and design §22.4 rules the third:

| the card writes into | `KEYED_COLLECTIONS` | measured over the corpus |
|---|---|---|
| `institutions` | **`'name'`** | `name` present on every entry of every row; UNIQUE in 63/63 (2,428 entries) |
| `npcs` | **`'id'`** | `id` present and UNIQUE in 63/63 (611 entries) — today's behaviour, preserved |
| `powerStructure.factions` | *(none — `ATOMIC_COLLECTIONS`)* | `faction` present on every entry of every row; UNIQUE in 63/63 (408 entries) |

⭐ And the register itself says WHY that row is atomic, in its own words: `CROSS_ENTRY_TOTALS` declares
`'powerStructure.factions': { field: 'power', total: 100, mootUnder: 'HELD' }` — atomic for a
CROSS-ENTRY TOTAL, not for want of a key (the list's own header reads "keyless, key-broken, or under a
cross-entry total"). So this member declares ONE row, `_ATOMIC_EDIT_JOINS`, cited to §22.4 and held by
the walker against the register in both directions. ⛔ **The share TOTAL is NOT this member's**: a DM
who sets one share to 40 leaves the hundred unbalanced, and `renormalizeFactionPower` as the `fulfil`
writer is EM-C3's totality rule. §11 item 5 STOPs a lane that reaches for it; §12 records it.

### §0.6 · ⭐ JUDGMENT 237 R1 FOLDED IN: ALL THREE FACTION FIELDS, ASSERTED AT THE VALUE

EM-R7's compile seat measured the faction card's lie at the declared leaf and the chair gave the cure
to this member's unit 2. Re-measured here over the 63-row corpus through the REAL `rederive`, with the
root key built on the RULED join (a faction's display name):

| declaration | reports APPLIED | in the BAG at its leaf | the declared leaf CARRIES | a junk `[].` key |
|---|---|---|---|---|
| `faction.faction` | 63/63 → 63/63 | **0/63 → 63/63** | **0/63 → 63/63** | **63/63 → 0/63** |
| `faction.category` | 63/63 → 63/63 | **0/63 → 63/63** | **0/63 → 63/63** | **63/63 → 0/63** |
| `faction.power` | 63/63 → 63/63 | **0/63 → 63/63** | **0/63 → 63/63** | **63/63 → 0/63** |

⇒ EM-R7's A2 (the declared-leaf value arm) is RED-FIRST at the tip on all three rows and goes GREEN at
this member's landing; EM-R7 carries the `Depends-on` and §12 carries the cross-reference. ⚠ A
`faction.faction` edit RENAMES the join field itself: the root key names the OLD display name and the
write sets the new one, which the pass resolves in one hop — measured 63/63, not argued.

### §0.7 · ⛔⛔ JUDGMENT 237 R2 IS REFUTED BY MEASUREMENT — THE ROSTER CURE IS NOT THIS MEMBER'S UNITS

EM-R7's figure reproduces EXACTLY at this tip: over 126 power-card trials (63 rows × {the share, the
seat}) through the real `rederive` and the real `mergeConsequence`, **5 escalate**, every one
`V-EVIDENCE-ROSTER@generationCoherenceReceipt.judgments[].evidence`. ⛔ **With BOTH units planted the
figure is UNCHANGED at 5 of 126, and the power card's bag is `[powerStructure]` before and after.**
Unit 1 is the nested-`recordPath` READ; the power closure never reaches `npcs`, because `npcs` is
provided by `generatePopulation`, which the closure's provider rule does not visit.

The cure as worded — take `npcs` from the first re-derivation inside the pin closure — WAS BUILT AND
EXECUTED here, as the closure following a held key's READERS (`generatePopulation` declares
`reads: […, powerStructure, …]` and provides `npcs, relationships, factions, conflicts`), in two
variants:

| variant | escalations | the power card's bag | R1's roster differs from the record's |
|---|---|---|---|
| the tip | 5/126 | `[powerStructure]` | 115/126 |
| **units 1 + 2 as compiled** | **5/126** | `[powerStructure]` | 115/126 |
| the readers law | **0/126** | ten keys, `settlement` among them | **0/126** |
| the readers law, minus any chooser whose record path IS `record` | **0/126** | nine keys | **0/126** |

⛔ **Both curing variants reach 0 by FREEZING THE ROSTER**: `npcs`, `factions`, `conflicts`,
`relationships`, `history`, `settlementReason`, `resourceAnalysis` and `economicViability` stop
following a power edit at all (115/126 → 0/126). That is the horn design §22.1 examined and REFUSED in
its own words — *hold the final roster at EVERY writer … an edit never reaches the economy … **RULED:
none of the three*** — and the first variant additionally pins `settlement`, whose `recordPath` is
`record` itself, handing the runner the whole previous record as one chooser's value.

⇒ **OWED TO THE CHAIR, and not adjudicated here:** either the roster cure is a DESIGN amendment to
§22.1 (a new member of its own, with the frozen readings declared), or the escalation is the ladder's
to absorb, or `V-EVIDENCE-ROSTER`'s cross-key scope is the thing to narrow. This member asserts
NOTHING about it, and §11 item 7 STOPs a lane that reaches for it. ⭐ Both variants are preserved as
`planned3/` and `planned4/` in the compile scratch so the chair's chosen shape can be re-measured in
minutes rather than rebuilt.

---

## §1 · Reconciled authority

| Check | Verdict at the read tip `1b073009f` |
|---|---|
| judgment 233 a–d | the ruling this member is compiled to; 233a, 233b and 233c each carried in §0.2, §0.3/§0.5 and §6 |
| judgment 237 R1 (the faction card's three fields) | FOLDED IN and measured at the VALUE — §0.6, §9's A5/A6/A7; EM-R7's A2 goes green at this member's landing |
| judgment 237 R2 (the roster flicker) | ⛔ REFUTED BY MEASUREMENT as this member's — §0.7 — and **WITHDRAWN by judgment 240**: the cure is EM-R8's (the ladder's held-key law), not this member's; asserted nowhere here, STOPped by §11 item 7 |
| the charter | carries no EM-R1c row at the tip; the slot is the ledger charter's "SLOTS THIS TRAIN OPENED: EM-R1c (the nested-recordPath bag builder; judgment 206h-4)" — chartered at the next amendment |
| EM-R1b §11 item 8 | DISCHARGED: that STOP reserved this widening for a new member, and this is it |
| design §12 | governs; nothing here reopens it |
| design §22.1 correction 5 | ruled by 233a, declared in §0.2 and §12 — NOT reopened by the lane |
| design §22.4 | the identity this member joins on; quoted, not re-decided |
| THE PROMISE | untouched: no seed, no lived history, no tuning numeral, no golden |
| the pre-proof at train EM-T17's base `a545899ff` | the J-T1 window `1b073009f` → `a545899ff` is **EMPTY** over every change-manifest path and every `requiredSymbols` path; 15 of 15 required symbols re-found verbatim (`grep -c -F` = 1 each); the CREATE target ABSENT; 0 hits on the three retired symbols across 61 waiting capsules and 230 registry packets; NO READY packet in the registry, so the placement's duplicate-path reservation has nothing to collide with |

⭐ **THE REVALIDATION SENTENCE THE CHAIR STAMPS** (the Verified base and Last revalidated
placeholders both take `a545899ff55b7e4c3f219b3ca58cd86ddcf0f3cd`): *"Re-validated at `a545899ff` by an Opus PRE-PROOF seat
on 2026-09-23: the J-T1 window from the compile's read tip `1b073009f` is EMPTY over every
change-manifest and `requiredSymbols` path, so no verified fact moved; the preamble hash
re-measured identical; every required symbol found verbatim at count 1 and the CREATE target absent;
the packet's own instrument figures re-executed at this base."*

## §2 · Outcome

`pinsFrom` stops assuming a chooser's value lives at `record[<ctxKey>]` and reads it at the record
path the Tier-1 register names, through the same injected `getStepMeta` channel EM-R1b opened for
`transient` (**unit 1**); and the DM's value stops being written at a literal leaf name and lands at
the DECLARED RECORD PATH of the entity the root key NAMES, joined by the identity the record register
declares (**unit 2**). The institution card lights end to end, the faction card stops lying, and the
npc and power-seat cards are unmoved.

## §3 · Hard scope budget (measured, not estimated)

| bound | this packet | limit |
|---|---|---|
| handwritten files | 4 (2 MODIFY, 1 CREATE, 1 TEST) + 1 REGISTER row | ≤12 |
| new/changed effective production lines | **+68 AS BUILT** (`pipeline.js` 157→176, `dmLayer.js` 207→256; eslint's own `Linter` under `max-lines` with `skipBlankLines`+`skipComments`, executed on the BUILT tree. Version 3 declared +67 against `dmLayer.js` 207→255; the ONE extra effective line is the type-floor cure's two-line ternary in `rosterOf`, restated here rather than silently carried) | ≤400 |
| each NEW production leaf | n/a — the two `src/` rows are EXISTING files; the CREATE is a `tests/lint` walker | ≤250 |
| shared / hot-file delta | **does not bind**: neither `src/generators/pipeline.js` nor `src/domain/edit/dmLayer.js` is on `PACKET_STANDARD.md`'s hot-file standing list, and neither carries a `scripts/.size-baseline.json` row (both measured at `a545899ff`); the eslint ceiling governing both trees is **800** effective lines against 176 and 256 | ≤15 (hot/shared only) |
| new logic leaves | 0 (the CREATE is a `tests/lint` walker) | ≤2 |
| existing logic files modified | 2 | ≤3 |
| acceptance cases | 8 | ≤8 |

## §4 · Sealed dispatch and preflight

Standard sealed dispatch. ⛔ The lane's FIRST act, before any edit: re-run `probe-7-value.mjs` AND
the pre-proof's `probe-B-institution-fields.mjs` at the lane's own HEAD and quote the tip column of
§0.3's table (`institution.state`, `.category` and `.name` each applied 0/63 with `step_not_pinnable`
63/63; `faction.power` value in the bag 0/63 with a stray `[].` key 63/63). Then run
`probe-A-allcards.mjs` at the tip alone and quote its seventeen-row signature (§5.3a's left column).
If any of the three has moved, the premise is dead and the lane STOPS with that measurement rather
than building (EM-R2b's precedent). The lane also re-runs `EM-R1c.remeasure.sh` — the pre-proof's
one-command re-measurement of the window, the symbols, the line addresses and the register idiom.

## §5 · Verified tree contract

### §5.1 · Required symbols, found verbatim at `1b073009f` (`grep -c -F` = 1 on every row)

| path | symbol |
|---|---|
| src/domain/edit/dmLayer.js | `export function pinsFrom(record, layer, declarations, engine) {` |
| src/domain/edit/dmLayer.js | `  const held = (/** @type {string} */ key) => Object.hasOwn(source, key);` |
| src/domain/edit/dmLayer.js | `  for (const key of [...pinKeys].sort(byCodepoint)) pins[key] = structuredClone(source[key]);` |
| src/domain/edit/dmLayer.js | `        .find((e) => /** @type {{ id: unknown }} */ (e).id === row.entityId);` |
| src/domain/edit/dmLayer.js | `      /** @type {Record<string, unknown>} */ (entity)[row.leaf] = row.value;` |
| src/domain/edit/dmLayer.js | `function rosterOf(engine) {` |
| src/domain/edit/dmLayer.js | `export const REDERIVE_UNAPPLIED_REASONS = Object.freeze(['step_not_pinnable', 'unknown_key']);` |
| src/domain/edit/dmLayer.js | `import { partitionTrace } from './tracePartition.js';` |
| src/generators/pipeline.js | `const _TRANSIENT_CHOOSERS = new Set([` |
| src/generators/pipeline.js | `export function getStepMeta() {` |
| src/generators/pipeline.js | `      transient: (step.provides || []).filter((k) => _TRANSIENT_CHOOSERS.has(k)),` |
| src/domain/generation/generationForkRegistry.js | `export const GENERATION_TIER1 = Object.freeze(` |
| src/domain/edit/recordRegister.js | `export const KEYED_COLLECTIONS = Object.freeze({` |
| src/domain/edit/recordRegister.js | `  institutions: 'name',` |
| src/domain/edit/recordRegister.js | `  npcs: 'id',` |

### §5.2 · The post-edit simulation: `retiredSymbols` is exactly FOUR

⛔ Four required symbols this member REWRITES at their own path carry `retiredBy` in the capsule at
placement (§731.3's idiom, judgment 227), spelled here VERBATIM as the capsule spells them:

```
  const held = (/** @type {string} */ key) => Object.hasOwn(source, key);
  for (const key of [...pinKeys].sort(byCodepoint)) pins[key] = structuredClone(source[key]);
        .find((e) => /** @type {{ id: unknown }} */ (e).id === row.entityId);
      /** @type {Record<string, unknown>} */ (entity)[row.leaf] = row.value;
```

No OTHER packet names any of the four — measured with `grep -c -F` over
`docs/implementation/PACKET_MANIFEST.json`: **0 hits each** (the fourth row re-measured at the
lane's own HEAD against the 231 packets other than this one). Every other required symbol is
PRESERVED verbatim — re-audited against the BUILT tree at the lane's HEAD, eleven rows at
`grep -c -F` = 1 — including `REDERIVE_UNAPPLIED_REASONS` (this member removes a CAUSE of
`step_not_pinnable` and adds no reason) and the two `KEYED_COLLECTIONS` rows it reads.

### §5.3 · THE LANDED ARMS OVER THESE TWO FILES, EACH EXECUTED OVER THE PLANTED TEXT (judgment 148's law)

Each arm's own logic was transcribed into plain `node` and run against the tip and against the planted
text (`probe-8-landed-arms.mjs`). **ONE arm moves, and it is EM-R1b's own:**

| arm | at the tip | over the planted text | verdict |
|---|---|---|---|
| `tests/lint/transientChooserRegistry.walker.test.js :: A3` (the injected stub roster) | `unapplied=[] pins=["alpha"] leaf="THE DM VALUE"`; control `pins=[] reasons=["step_not_pinnable"]` | byte-identical | **UNMOVED** — a stub roster carries no `recordPaths`, which reads as `{}` and falls back to today's read |
| `dmLayerGoldenIsolation :: A1` (the closure is the roster's own) | `pinned=["conflicts","factions","npcs","relationships"]` === expected | identical | **UNMOVED** |
| `dmLayerGoldenIsolation :: A2` (six absence shapes give ONE result) | dormant re-derivation byte-identical 63/63 | identical 63/63 | **UNMOVED** |
| `dmLayerGoldenIsolation :: A4` (the closed reason set; two overrides land) | `reasons=["unknown_key"] role="Warden" status="retired"` | identical | **UNMOVED** |
| ⛔ `dmLayerGoldenIsolation :: A8 — EM-R1b: the institution card is a SECOND defect, still refused, blocked by a nested record path` | `reasons.length=63 ["step_not_pinnable"]`, `blockers=["catalogForTier,generationRepairs"]`, the DM's state on the record 0/63 | **`reasons.length=0`**, `blockers=["catalogForTier"]`, the DM's state on the record **63/63** | **MOVED — §7's TEST row, widened by the exact row** |

⭐ And the arm's own root key must be re-spelled: it builds `rootKey('institution', institution.id, …)`
and an institution's `.id` is `undefined`, so under unit 2 it refuses `unknown_key` 63/63. The widened
row takes the RULED join (`institutions: 'name'`), asserts `measured = 63` with `reasons.length === 0`,
asserts the DM's state AT `record.institutions[<the named one>].state` in 63/63 — never
`unapplied === []` — and keeps the register claims (`repairsRows`, the closed set) byte-identical.
Its title carries the count, as judgment 148's law requires of a re-title.

⛔ `getStepMeta()` gains a `recordPaths` member on every row: no landed arm asserts a meta row's exact
SHAPE — the two arms that read one read a single NAMED member (`dataFlowContract.test.js :: the step
contract` reads `.reads`; `pipelinePinnedMode.test.js :: A8` reads `.provides`), measured by `git grep`.

### §5.3a · ⭐ THE GENERAL FORM OF JUDGMENT 148'S LAW, MEASURED AT `a545899ff` (the pre-proof)

Five arms transcribed by hand is not the population. SIX test files IMPORT from `dmLayer.js`
(`tests/domain/dmLayer.test.js`, `tests/generators/pipelinePinnedMode.test.js`,
`tests/lint/editMutationPath.walker.test.js`, `tests/lint/transientChooserRegistry.walker.test.js`,
`tests/property/dmLayerGoldenIsolation.test.js`, `tests/store/editSlice.test.js`) and
`pipelinePinnedMode.test.js` ALONE holds seventeen `it`s over `pinsFrom`/`rederive`. Every one of
them reaches `pinsFrom` through a (card, field) pair `FIELD_DECLARATIONS` declares, so the general
fact was measured instead: ALL SEVENTEEN declared pairs, over a stride of the census corpus, tip
versus plant, comparing the bag's KEYS and the refusal REASONS.

| the declared pair | at the tip | over the planted text |
|---|---|---|
| `institution.name` · `.category` · `.state` | `pins=[] reasons=[step_not_pinnable]` | `pins=[generationRepairs,institutions,isolationSupport,stress,stressTypes] reasons=[]` |
| `institution.note` | `pins=[] reasons=[unknown_key]` | **byte-identical** |
| `npc.name` · `.role` · `.status` | `pins=[conflicts,factions,npcs,relationships] reasons=[]` | **byte-identical** |
| `npc.note` | `pins=[] reasons=[unknown_key]` | **byte-identical** |
| `faction.faction` · `.category` · `.power` | `pins=[powerStructure] reasons=[]` | **byte-identical** |
| `powerSeat.holder` | `pins=[powerStructure] reasons=[]` | **byte-identical** |
| `worldFact.terrain` · `.culture` · `.monsterThreat` · `.stressors` · `.resources` | `pins=[] reasons=[unknown_key]` | **byte-identical** |

Dormant re-derivation byte-identical at the tip and under the plant alike. ⇒ **ONLY the institution
card's three ROOT fields move.** An arm anywhere in `tests/` that exercises a pair whose signature is
byte-identical cannot move, which is why §11 item 2's claim is now a MEASURED population rather than a
transcribed sample — and why `pipelinePinnedMode.test.js`'s partial-pin, draw-count and one-stream
arms (all on the npc and power cards) are unmoved by construction.

### §5.4 · The other instruments, each measured over the planted text as a DELTA

| instrument | delta | how |
|---|---|---|
| `tests/copy/voiceMechanics.test.js` | **em +0, bang +0** on both files | the walker's own question (comments blanked, marks counted inside string literals): `pipeline.js` `{em:2,bang:0}` → `{em:2,bang:0}`, reproducing the generators baseline's own row for that file exactly; `dmLayer.js` `{em:0}` → `{em:0}`. ⛔ The `src/generators` em budget stays at 28 of 28 (the lane law of 206h-3) |
| `tests/lint/entropyRootCensus.walker.test.js` | **+0** both arms | no `WORLD_ROOTS` read added; the walker's own SITE COUNT pins read `src/domain` **36** (over 1,076 files) and `src` WHOLE **47** (over 2,278 files) at `a545899ff`, and `createPRNG(` is unmoved at 1 in `pipeline.js`, 0 in `dmLayer.js` |
| `tests/lint/ruinFilterRoster.walker.test.js` | **+0** | the planted text adds no `.institutions` read: 0 occurrences in `dmLayer.js`, and `pipeline.js`'s single occurrence is the pre-existing COMMENT the walker blanks |
| `scripts/lib/tuning-inventory.mjs` · `tuningRegister.walker.test.js` (P2/P3) | **+0** | no module-top-level `const UPPER_SNAKE = <number>` added; this member writes no numeral at all |
| `tests/lint/proseNumerics.test.js` | **+0** | no rendered float; MEASURED at `a545899ff`: `tests/lint/.prose-numerics-baseline.json` carries **NO row** for either modified file, so nothing sits above a baselined address (§5.5) |
| `tests/build/vendorPdfLazy.test.js` ST-2 roster | **+0** | no goods identity half-table imported |
| the lighting census | **files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +2** DECLARED | ⭐ MEASURED at the pre-proof with the walker's OWN classifier (`parkReasonsFor`, `liveTitlesIn`, `liveSuiteTitlesIn`, run in plain `node` over the tip): the committed register reads `files 2697 · parked 359 · credited 2338 · titles 25820 · suiteTitles 6855` and the LIVE estate measures exactly the same tuple, and **`tests/property/dmLayerGoldenIsolation.test.js` is CREDITED** (`parkReasonsFor` → `[]`) with **10 titles and 2 suiteTitles**, so its new arms DO count. The delta is therefore: the CREATE contributes `files +1 · credited +1 · titles +3` (three straight-line literal `it`s, one per homed case A1–A3) `· suiteTitles +1` (one literal `describe`); the TEST file contributes `titles +5` (A4–A8) `· suiteTitles +1` (this member's own `describe`, the third in that file); the landed A8's RE-TITLE is `titles +0`. The CREATE binds `it`/`test`/`describe` exactly once each and names no variable or parameter `it` (`OPENER_UNRESOLVED`). The lane re-runs `parkReasonsFor` over both files before its seal and quotes it. Never regenerated here; the refreeze is the train's terminal |
| ⛔ `tests/lint/negativeAssertionAnchor.walker.test.js` (judgment 219) | **+0, and it is a CONSTRAINT, not a free row** | MEASURED: `BARE_NEGATIVE_RE` is `not.toContain` / `not.toMatch` / `not.toHaveProperty` ONLY; `GENERATION_FACING_ROOTS` includes **`tests/property`**, whose quarantine arm is EXACT IDENTITY, and `READMITTED_GENERATION_FACING` names three files of which `dmLayerGoldenIsolation.test.js` is NOT one — so that file's ceiling is **ZERO** and it measures **0** bare negatives today. ⇒ A7's stray-`[].`-key claim and A8's "unmoved" claim are spelled as COUNT EQUALITIES (`toBe(0)` / `toEqual([])`, which the walker's own arm proves are OUT of scope), or they carry `// anchored: <why this cannot go vacuous>` on the assertion line or the ONE line immediately above it — never wrapped across two comment lines. The CREATE under `tests/lint` takes the same rule under the corpus-wide "no NEW un-anchored negative" arm |
| `tests/lint/proseWiringCensus.walker.test.js` | `producerIndexFiles` **+0** | neither file is a `src/domain/prose` producer, and neither is named in the census's `stamp.files` |
| the negative-assertion anchor walker (judgment 219) | **+0** | every arm of the CREATE asserts positively; the one negative control (A3's blind roster) asserts an EQUALITY on the refusal, not an absence |
| the any-cast ratchet (judgment 213) | **+0** | MEASURED with the ratchet's OWN counter (`scripts/count-domain-any.mjs :: countText`): `pipeline.js` `{any:0,suppress:0}` → `{any:0,suppress:0}`, `dmLayer.js` `{any:0,suppress:0}` → `{any:0,suppress:0}`; neither file carries a `tests/lint/.domain-any-baseline.json` row or a `DECLARED_OVERRUNS` row. The lane re-measures before its seal |
| ⛔ the TYPE FLOOR, the TWO configs | **0 errors under each, AS BUILT** — ⛔ the compiled contract was RED here (4×TS7006 from the detached `@param` block, TS2339 on `StepRoster`, TS7053 on the one-row join table) and the cure is REAL TYPES inside §7 (judgments 248/253); estate-wide the built tree reads 167 under `tsconfig.full.json` and 1797 under the strict config, EXACTLY the base's figures, so this member adds zero type errors anywhere | `tsconfig.full.json` includes `src/domain/**/*.js` AND `src/generators/**/*.js`, so it governs BOTH paths and IS the gate's typecheck (`npm run typecheck`, zero errors). `tsconfig.domain-strict.json` declares only `extends` + `compilerOptions` and inherits `tsconfig.json`'s include `["src/domain/**/*.js","src/domain/**/*.ts"]`, so it governs **`src/domain/edit/dmLayer.js` ONLY, never `pipeline.js`** — `dmLayer.js` is the one path that must read ZERO under both, and a worsened `src/domain` file is a STOP |
| the reviewed-set pin (§P2.20) | **NOT OWED** | this member registers no application command and dispatches no store writer: `application/commands` and `editSlice` occur 0 times in `pipeline.js`'s planted text and once in `dmLayer.js`'s, in a COMMENT (`src/store` in the header's importability note) |

### §5.5 · Line-addressed registers, and the addresses this member shifts

⭐ **RE-MEASURED AT THE PRE-PROOF, AND THE ANSWER CHANGED: THIS MEMBER OWES NO RE-ADDRESS.**

```
$ git grep -n -E 'src/generators/pipeline\.js:[0-9]+' -- src docs tests scripts
docs/implementation/PACKET_MANIFEST.json      "... Verbatim at src/generators/pipeline.js:59 (count 1)."
docs/implementation/packets/settlement-editor/EM-B2a1.md   src/generators/pipeline.js:59   (x2)
docs/implementation/packets/settlement-editor/EM-B2a4.md   src/generators/pipeline.js:59
$ git grep -n -E 'src/domain/edit/dmLayer\.js:[0-9]+' -- src docs tests scripts
(no output)
```

EM-B2a2's `src/generators/pipeline.js:182 → :198` `_note` is **GONE** — the base commit `a545899ff`
re-spelled it by SYMBOL (`pipelinePinnedMode.test.js :: A7`), discharging judgment 210d's slot. The
ONE surviving line address on either of this member's files is `src/generators/pipeline.js:59`, the
`_PINS_KEY` declaration. MEASURED: `_PINS_KEY` is at line **59** and `_TRANSIENT_CHOOSERS` at line
**70**, and the contract plants `_CHOOSER_RECORD_PATHS` immediately AFTER `_TRANSIENT_CHOOSERS` —
**below** the cited line. The address does not move, `tests/lint/sourceCitationIntegrity.walker.test.js`
is unmoved, and no citation row enters this manifest. `dmLayer.js` is cited by line NOWHERE, and
`tests/lint/.prose-numerics-baseline.json` carries no row for either file. ⛔ THE LANE RE-RUNS BOTH
GREPS at its own HEAD before it edits: an insertion ABOVE line 59 would re-open this row.

### §5.6 · The CREATE's own construction rules

`tests/lint/chooserRecordPathRegistry.walker.test.js` IMPORTS both sets from their producers and
re-types neither (`contractTestAntiVacuity` Rule 2 convicts a hardcoded exhaustive claim): the
REGISTER through `import { GENERATION_TIER1 } from '../../src/domain/generation/generationForkRegistry.js'`,
and the RUNNER'S DECLARATION through `import { getStepMeta } from '../../src/generators/pipeline.js'`
with the side-effect import `import '../../src/generators/generateSettlementPipeline.js'` that
registers the steps — ⛔ `_CHOOSER_RECORD_PATHS` is module-private and is NEVER imported; the channel
is `getStepMeta()`'s `recordPaths`, exactly as EM-R1b's landed
`tests/lint/transientChooserRegistry.walker.test.js` reads `transient` (measured: without the
side-effect import `getStepMeta()` returns `[]` and both directions hold on nothing). It registers
**exactly THREE straight-line literal `it`s, one per homed case A1–A3**, under ONE literal
`describe`; writes no bare `for (const seed of …) { expect(…) }` (`seedLoopTotality` — it collects
and asserts once); binds `it`/`test`/`describe` exactly once each and names no variable or parameter
`it`; spells every absence as a COUNT EQUALITY or carries the `// anchored:` marker on the assertion
line or the one immediately above (§5.4's anchor row); and carries its
`scripts/mutation-coverage-manifest.json` row under `invariants['tests/lint/chooserRecordPathRegistry.walker.test.js']`
(`ENFORCER_DIRS` read at `a545899ff` contains `tests/lint`, so the row IS owed; the register holds 730
`invariants` rows and does not yet name this one). `tests/lint` whole is a §10 instrument of this member.

### §5.7 · The import edges, priced

Unit 2 adds ONE import edge, `src/domain/edit/dmLayer.js` → `./recordRegister.js`. ⭐ That module is
**already in `dmLayer.js`'s own static closure** — `dmLayer.js` imports `./tracePartition.js`, which
imports `RECORD_CLASSES` from `./recordRegister.js` (measured) — so the edge adds a named export from
a module every chunk holding `dmLayer.js` already carries: **0 new module bytes in any budget.**

⛔ **AND THE REGISTER IS NOT IMPORTED, WHICH IS WHY UNIT 1 DECLARES ITS MAP IN THE RUNNER.**
`src/domain/generation/generationForkRegistry.js` is **36,840 B** with **ZERO production importers** —
production-unreachable by its own law. An import of it from `dmLayer.js` would be +0 B in the worker
but would drag that whole frozen table into every chunk that holds `dmLayer.js`. The chair's ruled
channel (declare in the runner, expose through `getStepMeta`, hold equal with a walker — EM-R1b's
precedent) is therefore the cheaper shape by measurement, not only by precedent.

### §5.8 · Bytes

Re-measured at `a545899ff` by a plain-node BFS from `src/workers/generation.worker.js` that reads no
`dist` (**128** first-party files at this tip; the control `src/components/edit/CardEditorDialog.jsx`
correctly OUT —
⚠ closure SIZE differs by walker and edge form, so MEMBERSHIP, never size, is the fact asserted):

| path | the generation worker's closure | `EAGER_FIRST_PAINT_MODULES` (**270**, IMPORTED from the tree's own `vite.config.js`, never a list) | the five edge-shared metas |
|---|---|---|---|
| `src/generators/pipeline.js` | **IN** (ceiling `WORKER_BUNDLE_CEILING_BYTES` = **1,391,327 B** at `a545899ff`, re-read from the test's own literal; ZERO SLACK) | OUT | not named |
| `src/domain/edit/dmLayer.js` | OUT | OUT | not named |
| `src/domain/edit/recordRegister.js` | OUT | OUT | not named |

⇒ the edge-shared rebuild is **NOT OWED** (0 of 5 metas name any path). The planted text adds **+1,045
raw B** to `pipeline.js` (17,662 → 18,707 — UNMOVED by the type-floor cure, which is wholly inside
`dmLayer.js`) and, AS BUILT, **+3,760** to `dmLayer.js` (25,762 → 29,522; version 3 declared
+3,571 → 29,333, before that cure); only the
first lands in a budget. Most of `pipeline.js`'s addition is the declaration's comment block, which
minification removes; what survives is fourteen key/path string pairs and one `Object.fromEntries`
expression: **PREDICTED +550 to +900 B minified**, a RISE against a zero-slack, owner-signed ceiling.

⛔ **THIS MEMBER THEREFORE OWES A NAMED BUY-BACK** (§P11.2 — a rise is the owner's; the cure is the
kit's buy-back ledger, never a re-mint upward). It carries NO row on `tests/build/generationWorkerLazy.test.js`
and edits no ceiling literal: the train's ONE real build with per-module attribution is the judge, and
CI's `verify:dist` is the free per-member reading (judgment 214). A cheaper spelling exists and is
NOT taken without the chair: nine of the fourteen paths are `record.config.<the same key>`, so a
`config`-suffix SET plus a five-row map would save roughly 250 B at the cost of two shapes where the
register has one — offered here, not decided.

## §6 · Exact contracts

### Unit 1 — the bag is keyed by the CTX KEY and READ AT THE RECORD PATH

`src/generators/pipeline.js` declares `_CHOOSER_RECORD_PATHS`, a module-private frozen `Map` of the
FOURTEEN chooser keys whose value does not live at `record.<ctxKey>`, ctx key → record path, spelled
beside `_TRANSIENT_CHOOSERS` and for the same stated reason (the register is production-unreachable,
§5.7). `getStepMeta()` gains, per step, `recordPaths: Record<string, string>` — that step's own
provided keys that carry a path, `{}` otherwise; no other member of the meta row moves and no step is
re-registered. `src/domain/edit/dmLayer.js :: rosterOf` carries it through as `paths` (a non-object
reads as `{}`; the leaf THROWS NEVER), and the `StepRoster` typedef gains
`paths: Record<string, unknown>` — ⛔ **`unknown`, not `string`, and that is the HONEST carried
type**: the roster is read from an UNTRUSTED injected handle, so `rosterOf` narrows the row with
the file’s own landed idiom (`isPlainObject` guards at RUNTIME, a JSDoc type expression carries
the type — exactly as the landed line above it does for `engine`) and `pinsFrom` keeps a
`typeof p === 'string'` filter at the READ site. Measured: both type configs read ZERO on this
file under that spelling and +1 under any looser one. `pinsFrom` resolves `held(key)` and the bag's value through ONE
private reader: a key with NO declared path reads `source[key]` exactly as today; a key with one walks
the path's segments after the leading `record`, and a missing segment at any depth reads as NOT HELD.
The bag stays keyed by the CTX KEY (the runner consults `pins[ctxKey]`), stays ASCII-ascending, and
holds a `structuredClone` of the RESOLVED value. **Absence:** a declared path present with value
`undefined` is HELD (`Object.hasOwn` at every segment, never truthiness). **Determinism:** no draw, no
stream, no clock, no locale. **Lifecycle:** the bag is transient; nothing persists and the record is
never touched. **Declared behaviour change:** a closure that refused on a nested key now completes, so
a bag may carry keys it could not before — `stress` and `stressTypes` on the institution channel,
63/63 (§0.2's ruling, §12's figure).

### Unit 2 — the DM's value lands at the DECLARED RECORD PATH of the NAMED entity

`pinsFrom` imports `KEYED_COLLECTIONS` from `./recordRegister.js` and declares ONE row,
`_ATOMIC_EDIT_JOINS = Object.freeze({ 'powerStructure.factions': 'faction' })` (§0.5), carrying
`/** @type {Readonly<Record<string, string>>} */` — a REAL type, never a cast: without it the
one-row literal’s inferred type refuses the `string` index `joinFor` performs (TS7053, measured). `joinFor(collectionPath)`
returns the register's spec when it is a STRING, this member's row for the one atomic collection, and
`null` otherwise — a MULTI-FIELD spec (an array) resolves to `null`, and no declared card writes into
such a collection today. PASS 4 becomes a declared-path write: the leaf is walked segment by segment
inside the CLONE; a segment spelled `<name>[]` is an ARRAY HOP resolved by `soleEntry`, which returns
the SOLE entry whose join field, stringified, equals the root key's `entityId` — zero or several
matches resolve to `null`, the estate's own exactly-one-match guard (`editSlice.js :: soleIndexOf`'s
shape). A bag value that is itself an array is hopped at the collection path. **Any failure to reach
the declared leaf is `unknown_key`**, today's reason; the closed refusal set is UNMOVED at two.
**Absence:** a leaf whose final segment is itself an array hop is refused, never guessed.
⛔ The record is never touched, and `chooseOrPin` is untouched.

⭐ **WHERE THE THREE NEW FUNCTIONS SIT, AND WHY IT IS A CONTRACT FACT.** `joinFor`,
`soleEntry` and `writeDeclared` are declared **ABOVE** `pinsFrom`’s own JSDoc block, never
between it and `pinsFrom`. Measured: a declaration placed between them DETACHES all four
`@param` tags from the function they document and the strict domain config reports four TS7006
on a file whose floor is zero. Each carries its parameter types inline in the file’s landed
idiom (`/** @type {string} */ collectionPath`), and `writeDeclared`’s array hops read through
`/** @type {Record<string, unknown>} */` type expressions. ⛔ ZERO `any`, zero `@ts-` directive,
zero baseline row: the any-cast ratchet’s own `countText` reads `{any:0, suppress:0}` on the
built file, exactly as on the base.

### User-facing copy

NONE. This member spells no user-facing string and renders no number.

## §7 · Exact change manifest

| action | path | symbol |
|---|---|---|
| MODIFY | src/generators/pipeline.js | `_CHOOSER_RECORD_PATHS` (new), `getStepMeta` |
| MODIFY | src/domain/edit/dmLayer.js | the `KEYED_COLLECTIONS` import, `_ATOMIC_EDIT_JOINS` (new), `joinFor` · `soleEntry` · `writeDeclared` (new), `rosterOf`, `pinsFrom` |
| CREATE | tests/lint/chooserRecordPathRegistry.walker.test.js | adds 3 `it`s (one per homed case A1–A3) under ONE literal `describe` |
| TEST | tests/property/dmLayerGoldenIsolation.test.js | adds 5 `it`s (this member's cases A4–A8) under ONE new literal `describe`, and RE-TITLES the landed A8 by the exact row (§5.3) — a re-title adds no title |
| REGISTER | scripts/mutation-coverage-manifest.json | the CREATE's row (`rowKey` = the walker's path) |

## §8 · Ordered coding sequence

1. RED FIRST, both units: the corpus arm asserting the tip column of §0.3 (institution 0/63; the
   faction's stray `[].` key 63/63), and the walker's cross-direction arm with the map absent.
2. `pipeline.js`: `_CHOOSER_RECORD_PATHS` and `getStepMeta`'s `recordPaths`. 3. `rosterOf`'s carry.
4. `pinsFrom`'s path reader (unit 1). 5. `joinFor` · `soleEntry` · `writeDeclared` and PASS 4 (unit 2).
6. The CREATE's three arms green. 7. The landed A8 RE-TITLED by the exact row; A4–A8 added under
this member's own `describe`, every absence spelled as a COUNT EQUALITY or carrying its one-line
`// anchored:` marker (§5.4's anchor row). 8. §5.4's instruments, each alone, each declared, with
`parkReasonsFor` printed over BOTH new/edited files and the two line-address greps of §5.5 re-run at
the lane's own HEAD. 9. The byte reading and the named buy-back.

## §9 · Acceptance matrix

| id | case | home |
|---|---|---|
| A1 | `_CHOOSER_RECORD_PATHS` equals `GENERATION_TIER1`'s non-null, non-identity `recordPath` rows in BOTH directions — fourteen keys — each set IMPORTED from its producer and neither re-typed, and (key → recordPath) is a FUNCTION over the register (zero keys carry two paths) | `tests/lint/chooserRecordPathRegistry.walker.test.js` |
| A2 | `pinsFrom` reads a NESTED record path through an INJECTED stub roster, and is blind to that key without the map (the negative control), the record itself untouched | same |
| A3 | `joinFor` returns `KEYED_COLLECTIONS`' own spec for `institutions` and `npcs`, and this member's single declared row for `powerStructure.factions`, whose join field is measured PRESENT and UNIQUE on every entry of every corpus row | same |
| A4 | the INSTITUTION card's THREE ROOT fields through the REAL `rederive` over the corpus: `state`, `category` and `name` each applied 63/63 AND each PRESENT at `record.institutions[<the named one>].<field>` 63/63 — the value, never `unapplied === []` — red-first at 0/63 on all three (`name` is the free-cascade RENAME of the join field itself, resolved in one hop) | `tests/property/dmLayerGoldenIsolation.test.js` |
| A5 | `faction.faction` (the join field itself, renamed): the DM's name PRESENT at `record.powerStructure.factions[<the renamed one>].faction` 63/63 — red-first at 0/63 | same |
| A6 | `faction.category`: the DM's category PRESENT at `record.powerStructure.factions[<the named one>].category` 63/63 — red-first at 0/63 | same |
| A7 | `faction.power`: the DM's share PRESENT at `record.powerStructure.factions[<the named one>].power` 63/63 — red-first at 0/63 — and NO key matching `[].` anywhere in the bag, 0/63, red-first at 63/63 | same |
| A8 | the npc and power-seat cards UNMOVED (applied and the value at the declared leaf, 63/63 each), and a dormant layer re-derives the record byte-identically 63/63 | same |


## §10 · Verification commands

```
# PRE-SEAL, from the KIT PATH, never placed in the tree (§P9)
node EM-R1c.count-prover.mjs EM-R1c.md EM-R1c.manifest.json
# THE CREATE DIRECTORY, RUN WHOLE, minus the lighting walker (§P7)
npx vitest run --pool=threads --maxWorkers=2 tests/lint --exclude tests/lint/sovereigntyLightingContract.walker.test.js
# the leaf's own consumers, ONE directory per gated run
npx vitest run --pool=threads --maxWorkers=2 tests/property/dmLayerGoldenIsolation.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/generators
npx vitest run --pool=threads --maxWorkers=2 tests/domain/dmLayer.test.js tests/store/editSlice.test.js
# the goldens and the prose manifest: UNMOVED is the claim, so they are run
npx vitest run --pool=threads --maxWorkers=2 tests/property/generatorGoldenMaster.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/property/dossierProseManifest.test.js
# the lighting walker ALONE — the member's measured INTERIOR RED, message quoted, delta declared
npx vitest run --pool=threads --maxWorkers=2 tests/lint/sovereigntyLightingContract.walker.test.js
# PRE-REPORT, no gate slot: the TypeScript compiler API over THIS member's own src/ paths
```

## §11 · Mandatory STOP conditions

1. ⛔ The tip column of §0.3 does not reproduce at the lane's own HEAD (§4) — the premise is dead.
2. ⛔ **A landed arm other than A8 moves.** §5.3 measured five arms; four are UNMOVED and that is the
   claim. A second mover means the contract is not the one compiled: STOP with the arm's own message.
3. ⛔ `_CHOOSER_RECORD_PATHS` or `_ATOMIC_EDIT_JOINS` is completed BY HAND against anything but
   `GENERATION_TIER1` and `KEYED_COLLECTIONS` + design §22.4. The walker holds both, both directions.
4. ⛔ The lane re-opens judgment 233a (the dragged `stress`) or design §22.4's identity. Both are
   RULED; a lane that exempts `stress` from the closure is rewriting the runner's partial-pin rule.
5. ⛔ **The lane reaches for the faction SHARE TOTAL.** A DM's 40 leaves the hundred unbalanced;
   `renormalizeFactionPower` as the `fulfil` writer is EM-C3's totality rule (§0.5). Outside this
   manifest.
6. ⛔ **The lane reaches for either slot this member's compile opened and the chair placed elsewhere
   (judgment 233d):** the WORLD-FACT card's inverse defect (the declarations spell record paths and
   `tier1` is read by nothing) is EM-B2b's pre-proof; EM-R7's ratchet owns the leaf-value assertion.
   Both are outside this manifest and a lane that widens `splitOutputKey` for `config.*` STOPS.
7. ⛔⛔ **THE LANE REACHES FOR THE ROSTER FLICKER (judgment 237 R2).** §0.7 measured it: this member
   leaves it at 5 of 126, and every cure that reaches 0 does so by freezing eight readings against a
   power edit — the horn design §22.1 REFUSED. The ladder, the cross-key scope and §22.1 itself are
   the chair's; a lane that widens the pin closure to follow a held key's READERS STOPS.
8. ⛔ The worker bundle rises beyond the stated +550 to +900 B, or any signed ceiling would be
   re-minted upward (§P11.2 — the owner's). The buy-back is the kit's ledger, never a re-mint.
9. Any golden or the prose manifest moves; or the closed refusal set gains a third member.
10. A number lands under `src/domain`/`src/generators` that is a genuine simulation dial (§P2.17).
11. ⛔ A spec, a walker or a ratchet would be weakened, widened or skipped to make this member pass.

## §12 · Completion receipt

- Base SHA; dispatch bundle and seal identity:
- §4's pre-seal measurement, the tip column of §0.3 quoted with its counts:
- The institution card end to end, ALL THREE ROOT FIELDS: `state`, `category` and `name` each applied
  63/63 AND each at `record.institutions[<the named one>].<field>` 63/63 (red-first 0/63 on all three):
- The faction card: the share at `record.powerStructure.factions[].power` 63/63, stray `[].` keys 0/63:
- The npc and power-seat cards UNMOVED, and the dormant re-derivation byte-identical 63/63:
- ⛔ **THE DECLARED `stress` CONSEQUENCE (judgment 233a):** the institution closure now holds
  `stress` and `stressTypes` in 63 of 63 rows; measured, `record.stress` MOVED in **0 of 63**. The
  key is held identically in `R0` and `R1`, so it cancels in §22.1's merge — recorded here so no
  successor reads a held `stress` as a DM-editable fact:
- ⛔ **THE FACTION SHARE TOTAL, LEFT OPEN ON PURPOSE:** one share set to 40 leaves
  `CROSS_ENTRY_TOTALS`' hundred unbalanced; EM-C3's `renormalizeFactionPower` is the `fulfil` writer
  (deliberately deferred — documented, not a bug to re-find; **slotted: EM-C3's
  `renormalizeFactionPower` (the `fulfil` writer)**):
- A8's widened row, quoted, with its count and its new title:
- The worker bytes this member's `pipeline.js` change carries, measured on the build, and the
  buy-back row that pays for them (the ceiling follows a measurement DOWN, never up):
- EM-B2a2's `pipeline.js` `_note` re-addressed by symbol (§5.5), for the train's citations unit:
- ⭐ **EM-R7's arm A2 (the declared-leaf value arm) GOES GREEN AT THIS LANDING** — red-first at the
  tip on all three faction rows and on the institution row; EM-R7 carries the `Depends-on` and this
  receipt carries the cross-reference (judgment 237 R1):
- ⛔ **THE ROSTER FLICKER IS UNMOVED AND UNCLAIMED (judgment 237 R2, refuted at §0.7):** 5 of 126
  power-card trials escalate on `V-EVIDENCE-ROSTER` at the tip and 5 of 126 after this member. The
  measured cure freezes eight readings against a power edit (115/126 → 0/126), which design §22.1
  refused, so it is NOT in this manifest (deliberately deferred — documented, not a bug to re-find;
  **slotted: EM-R8 (the ladder's held-key law; judgment 240) — a measurement seat first, then a
  member placed on EM-R7's flip**; the two built variants are `planned3/`, `planned4/`):
- The lighting delta as DECLARED (`files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +2`),
  against the walker's own message, with `parkReasonsFor` printed over BOTH the CREATE and
  `tests/property/dmLayerGoldenIsolation.test.js`:
- §5.5's two line-address greps re-run at the lane's own HEAD, showing `src/generators/pipeline.js:59`
  still above the insertion point and `src/domain/edit/dmLayer.js` cited by line nowhere — this member
  owes NO citation re-address (the base commit discharged EM-B2a2's, judgment 210d):
- The pre-proof's general judgment-148 measurement re-run at the lane's HEAD (§5.3a): of the seventeen
  declared (card, field) pairs, ONLY the institution card's three root fields move:
