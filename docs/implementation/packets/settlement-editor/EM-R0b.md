# EM / EM-R0b — `recordInvariants`: THE TOWN'S OWN INVARIANTS AS A MODULE (the re-entry family)

**Preamble:** docs/implementation/preambles/EM-PREAMBLE.md (SHA-256
**`125c693214235a81bf4f2b38506b621859a35e0325ff2ea58d335b4e0681a99d`**, MEASURED by this pre-proof
with `shasum -a 256` at the read tip `64b75240c` — train EM-T15's flipped tip — and re-stamped by the
chair at promotion if the tip moves; §P2 row 5, §P3.5). ⭐ That text IS **THE SIXTH AMENDMENT**, landed
as train EM-T13's first commit `7bc14e003` and self-declaring it (`EM-PREAMBLE.md :: the Volume/Signed-by
header`, "THE SIXTH AMENDMENT (2026-09-22) IS WRITTEN IN PLACE"). ⭐ As executed HISTORY with as-of
marks: the version-3 compile read `b90a95b7…`, the 09-20 pre-proof `16dfb96f…3195` at `141a1d775`, and
ESTATE-REPAIR-3 `c9f33c8d…6675` at `bdbf7c89c` — four values under one waiting packet, which is why
§P2 row 5 forbids copying the hash from any brief or sibling.

⛔ **THE INTERIM COMPILE-RULES ROW IS DELETED AT THIS PRE-PROOF, NOT SILENTLY DROPPED.** Versions 6 and 7
carried a governing row naming `COMPILE-RULES.interim.md` and a clause saying which text won where the two
disagreed. The sixth amendment's own header retires both in terms — *"The interim compile-rules sheet
`COMPILE-RULES.interim.md` (the chair, 2026-09-20; seventeen rules) is SUPERSEDED BY THIS TEXT … so a packet
compiled after this amendment carries no interim row and no clause saying which of the two governs where
they disagree."* Measured at `64b75240c`. Every law the deleted row carried is in the preamble, in
`PACKET_STANDARD.md` or in `PACKET_TEMPLATE.md`; nothing this packet asserts rested on the sheet.

- **Status:** `LANDED`
  ⚠ The status value stands ALONE on its line because `parsePacketHeader` anchors the status row at end-of-line.

  ⭐ **READY-able at `64b75240c`** (train EM-T15's flipped tip) — RE-MEASURED WHOLE by the Opus
  pre-proof lane of train EM-T16 stage 1, 2026-09-23, under the SIXTH amendment. The 09-20 pre-proof at
  `141a1d775` was stale by eight trains; every verified fact is re-executed here (evidence T16-1…T16-14).
  FOUR contracts are open for the chair (§12 questions 1–4); nothing is refuted.
- **Landed at:** `ce696cf86658e135972ce3c39d95e5a25feb1f96` — recordInvariants — the town's own invariants as a module (version 8): three pure leaves under src/domain/edit with their own arms; the five pending symbols discharged (151); the famine label keyed by band key, the integer tolerance, the 65-row stride with a count guard (208 a–c); +0 bytes in both closures by measured membership; built at ce696cf86 on train EM-T16 stage 1
- **Packet version:** 8
  - ⭐⭐ **What version 8 changed and why (the Opus PRE-PROOF lane of train EM-T16, 2026-09-23, at
    `64b75240c`).** The 09-20 pre-proof was taken at `141a1d775`, under the SECOND amendment, **206
    commits and eight trains ago**; this is a FULL re-measurement. **No premise is refuted and no
    acceptance contract moved.** Seven things changed, every one measured.
    (1) ⭐⭐ **BOTH DEPENDENCIES HAVE LANDED, SO ALL FIVE `_pendingRequiredSymbols` ROWS ARE
    DISCHARGED into `requiredSymbols`, each re-measured VERBATIM at this tip** (`grep -cF` = 1 for
    every one; judgment 151's law that a contract's spelling is not the code's). `src/data/bandLadders.js`
    (EM-R0d, landed on train EM-T14) and `src/domain/edit/recordRegister.js` (EM-R0a) are both PRESENT.
    (2) ⭐⭐ **THREE INSTRUMENTS HOLD A NEW `src/domain` FILE AT ZERO AND THIS PACKET WAS SILENT ON ALL
    THREE** — the voice walker's em-dash count, the tuning register's P2/P3 populations, and the
    prose-numerics detector. Two of them would have RED at the build under a live seal on text §6
    prescribes: the famine label `Deficit + em dash + Active Famine` as a string literal, and the
    half-unit `0.5`. §6 and §7 now carry the measured cures; §12 questions 1 and 2 put both to the chair.
    (3) ⭐ **THE ROUNDING RULE IS RE-EXPRESSED IN INTEGER FORM AND PROVED EQUIVALENT** on 20,402
    `(famine, deficitPct, surplusPct)` triples with **0 differences** (T16-9), because both published
    percentages are `Math.round`ed (`src/generators/foodGenerator.js :: the foodSecurity return`) and all
    four cuts are integers (`src/data/bandLadders.js :: FOOD_SECURITY_CUTS`). §6's six-row table is
    reproduced EXACTLY. The contract is unchanged; only its arithmetic spelling is.
    (4) The sealed `checks` array is widened to the GOVERNING WALKER FILES the sixth amendment computes
    (§P2 row 19's "a sealed `checks` array names files"), minus the two that red by design.
    (5) The header is re-stamped to the measured sixth-amendment hash and the interim compile-rules row
    is deleted, per the amendment's own retirement sentence.
    (6) Every absolute that moved is re-marked with an as-of figure: the first-paint set holds **270**
    modules (268 at `141a1d775`), the generation worker's static closure **230** (220 at the compile tip),
    the pipeline's **226** (224), `stamp.producerIndexFiles` **1185** (1172). ⭐ **EVERY DELTA THIS PACKET
    CAUSES IS UNCHANGED**: first-paint 0, worker 0, producer count +3, lighting `titles +7`.
    (7) ⛔ **ONE STALE REASON IS CORRECTED, AND IT IS NOT A DELTA.** The 09-20 pre-proof established
    `+0 B` partly from *"nothing anywhere in `src/` imports that directory"*. That is FALSE at this tip:
    `src/store/editSlice.js`, `src/store/settlementRederiveAction.js` and
    `src/components/edit/CardEditorDialog.jsx` all import `src/domain/edit/**` (T16-6). The `+0` stands on
    the narrower, still-measured fact — **nothing imports these three leaves, and `EAGER_FIRST_PAINT_MODULES`
    and the worker closure hold ZERO `src/domain/edit/**` members** — and it is stated that way now.
  - ⭐ Version 7 is the DOCS repair only (ESTATE-REPAIR-4, 2026-09-20, read tip
    `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`): §10's account of `gate-mutex.sh` is corrected — it
    gives up after its poll budget and **exits 3** (`GAVE UP`), NOT 0. The chair retracted the
    "exits 0" claim on 2026-09-20 after checking three versions of the script; the 09-15 note that
    stated it was wrong. ⛔ **The operational instruction is UNCHANGED and still correct** — a gate
    line with no printed test count DID NOT RUN — so no command, budget, contract, acceptance case,
    required symbol or change row moved.
  - Version 6 is the DOCS repair only (ESTATE-REPAIR-3, 2026-09-20, read tip `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`),
    bringing this packet under `COMPILE-RULES.interim.md`: §7's **`Wiring census (stamped)` row
    said `NO — zero, no stamped file is modified`, which answers only the SHA-MAP arm** while
    this manifest CREATEs THREE `.js` leaves under `src/domain/edit/`; it is replaced by the
    DEFERRED ROW with the measured `+3` (interim rule 1). The live `268` first-paint absolutes
    become measurements with as-of marks (interim rule 9), and the header no longer carries a
    literal preamble hash. ⛔ **No contract, budget, acceptance case, required symbol, change
    row or non-goal moved.**
  - Version 5 is the DOCS repair only (ESTATE-REPAIR-2, 2026-09-20): the `_pending` block moved out
    of `requiredSymbols` to the sibling key `_pendingRequiredSymbols`, §7's
    `recordInvariants.test.js` row became `CREATE`, and the `validate:packets` row joined `checks`.
    ⛔ **No contract, budget, acceptance case, required symbol or non-goal moved.**

  **What version 3 changed and why.** The chair's ruling of 2026-09-20 on EM-R0c's compile report
  (questions 2 and 4) sent three things here, and a new tree `32602dc60` was measured.
  (1) ⭐ **`CHECK_META` SHIPS** — an exported, total, per-check table naming the record KEYS each
  check relates, and **`Violation` gains a `keys` field**. EM-R0c's escalation ladder requires BOTH
  keys of a cross-key violation at step 1 (design §22.2 item 3, §22.3 item 5) and version 2 exported
  nothing it could read; EM-R0c's STOP-4 is what this discharges. ⭐ The table's own derivation
  reproduces version 2's **seven** cross-key checks exactly, by a rule version 2 stated in prose but
  never encoded (V3-2b).
  (2) ⭐ **THREE NEW CHECKS, 30 → 33.** Measuring the chair's question 4 found a third shape neither
  document had named: **one fact stored at TWO record paths**, on 525 of 525 rows, which the merge
  can put out of step **63/63 with a clean control** (V3-3d). `V-DEFENSE-MAGICDEP`,
  `V-DEFENSE-TRADITIONS` and `V-HISTORY-AGE` close it. Each has its control (63/63 clean) and an
  isolating mutant (convicts 63/63, trips nothing else).
  (3) ⭐ **THE CHAIR'S QUESTION 4 IS ANSWERED, AND THE ANSWER IS NO, TWICE.** No invariant ties the
  leaves of `defenseProfile.scores` (every candidate identity holds 0/525; `disaster` is REFUTED as a
  function of the other five) and none ties the leaves of `history.founding` (`overcoming` is REFUTED
  as a function of both `initialChallenge` and `reason`). **Both of EM-R0c's MIXED readings are
  LAWFUL and no group is owed for either.**
  (4) **Version 2's rulings on the food card are UNTOUCHED** — the rounding tolerance and the
  DECLARED flag table stand exactly as ruled, because every fact that forced them re-measures
  identically at this tip (V3-4).
  (5) Every version-2 figure was RE-EXECUTED at `32602dc60` in the command that quotes it. ⚠ Version
  2's harness pointed at `read-tip-e5bdfd031`; nothing was carried.
  (6) A2 is RE-CUT, not added: the pin's both-directions arm folds into A1, which already asserted the
  violating-row SET equals `KNOWN_VIOLATIONS` in both directions. The cap of eight holds.

  **What version 4 changed and why (the Opus PRE-PROOF lane, 2026-09-20 07:0x EDT, at `141a1d775`).**
  No premise is refuted and no figure moved: every version-3 headline was RE-EXECUTED at the new tip
  and every one holds to the row (P-4). Four things changed.
  (1) ⭐ **THE LIGHTING DELTA IS CORRECTED, `titles +8` → `titles +7`.** Version 3 priced eight
  titles from its eight ACCEPTANCE CASES, but §9 homes **A8 in `tests/build/domainGeneratorsBoundary.test.js`
  — "run, not edited"** — so it lands no `it` in the new file. The census sums `liveTitlesIn` over
  CREDITED files, and an unedited file contributes nothing (P-6). Inherited from version 2, which
  made the same count.
  (2) ⭐⭐ **"THE 63-ROW STRIDE" IS UNDEFINED IN THE TREE** and no arithmetic stride over the 525-row
  corpus yields 63 (P-7). A1, A3, A6 and A7 all name it and A1's "62 rows clean" is its cardinality.
  It is §12 question 3 (question 1 at that pre-proof; RENUMBERED at the EM-T16 pre-proof, which added two
  instrument questions ahead of it), with a measured menu and a recommendation; the figure is a MARKED PLACEHOLDER
  until the chair rules.
  (3) The preamble hash is re-stamped and §P2 row 11's second amendment is priced (P-8); the §12
  receipt's bundle row now says HOW `+0` is established, because a skipped byte arm is no longer a pass.
  (4) The three references to "§12 question 1" are re-cut: the chair ALREADY RULED it at ODQ §934.47
  addendum 38 ruling 1 (**BOTH** — the check here, the `history` group at EM-R0a), and §12 carried no
  numbered questions at all (P-9).

- **Verified base:** `em-t16-r0b-2026-09-23` at `64b75240c98831693e4495efedeb2639cfa691f7`
  ⭐ **The chair's revalidation sentence:** *"Compiled at `32602dc60`, pre-proofed at `141a1d775`, and
  RE-MEASURED WHOLE by an Opus pre-proof lane at `64b75240c` — train EM-T15's flipped tip — under the
  SIXTH amendment (evidence T16-1…T16-14). The J-T1 window over every change-manifest path and every
  requiredSymbols path (the five discharged rows included) across the 206 commits from `141a1d775` moves
  FOUR files and NO fact: `src/data/bandLadders.js` and `src/domain/edit/recordRegister.js` are the two
  dependencies ARRIVING (+90 and +190 lines, both CREATEs — their five symbols are re-measured verbatim
  here and discharged), `src/generators/generateSettlementPipeline.js` moves ONE line (EM-R1 v3's pin
  forwarding, inert when `options.pins` is undefined) and `tests/build/domainGeneratorsBoundary.test.js`
  gains judgment 111's root-derivation arm while its frozen set stays FOUR files and FIVE specifiers,
  measured at this tip. `src/domain/activeConditions.js` and `tests/helpers/goldenMasterCorpus.js` do not
  move at all. The corpus is unchanged: 525 rows, and index 510 still keys
  `town|germanic|mountain|mountain_pass|civilized|golden-master-v3` — the pin (T16-8). The four CREATE
  targets are ABSENT (T16-2). The budgets are unmoved and their MEMBERSHIP facts re-measured by importing
  the sets: `EAGER_FIRST_PAINT_MODULES` holds **270** modules AS OF this tip and **0** `src/domain/edit/**`;
  the generation worker's static closure holds **230** modules and **0** `src/domain/edit/**`; this packet
  MODIFIES no `src/` file, so the line-addressed and stamped registers are untouched and the wiring census
  moves only its producer COUNT, +3. The lighting delta is `files +1 · credited +1 · parked +0 ·
  suiteTitles +1 · titles +7`. ⚠ Four instrument obligations the earlier versions did not price are
  priced in §7 and put to me in §12."*
- **Last revalidated:** `64b75240c98831693e4495efedeb2639cfa691f7` — stamped by the chair at promotion.
  ⚠ **THE BASE IS NOT FREE, AND AT THIS TIP IT IS ALSO NO LONGER HARD.** `assertAncestorAndSubstrate`
  (`scripts/implementation-session.mjs :: assertAncestorAndSubstrate`) builds its substrate from the
  non-CREATE change rows **UNION every `requiredSymbols` path UNION every `retiredSymbols` path**, refuses
  when `git diff <base>..<HEAD>` touches any of them, and then refuses a second time for any substrate path
  the dispatch capsule omits. The five discharged rows put `src/data/bandLadders.js` and
  `src/domain/edit/recordRegister.js` into that substrate — **both have now LANDED** (T16-2), so the base
  need only be stamped at or after EM-R0a's and EM-R0d's landing commits, which every tip from EM-T14
  onwards satisfies. ⭐ **And the function returns EARLY when `head === packet.verifiedBase`** — the
  literal guard `if (head === packet.verifiedBase) return;` sits above the substrate build — so stamping
  the base at the dispatch tip skips both arms outright. That is the cleanest promotion and is what this
  packet asks for. Re-read at this tip (T16-13).
- **Depends on:** ⭐⭐ **BOTH DEPENDENCIES ARE LANDED AT THE READ TIP, MEASURED.** **EM-R0d** exports the
  ladders this packet reads and `src/data/bandLadders.js` is PRESENT at `64b75240c` with
  `legitimacyBandOf`, `readinessBandOf`, `foodSecurityBandOf`, `FOOD_SECURITY_CUTS`, `FOOD_SECURITY_BANDS`
  and `LEGITIMACY_BANDS` all resolving verbatim; **EM-R0a**'s `src/domain/edit/recordRegister.js` is
  PRESENT with `RECORD_CLASSES` and `CONSISTENCY_GROUPS` (T16-2, T16-3). ⚠ The version-7 sentence
  *"EM-R0d's four ladder symbols are not yet in the tree"* is therefore RETIRED, and with it the
  `_pendingRequiredSymbols` block: **all five rows are DISCHARGED into `requiredSymbols`, re-measured
  verbatim rather than promoted as written** (judgment 151). The family's order stands
  `EM-R0a · EM-R0d · EM-R0b · EM-R0f · EM-R0c · R1–R5 · R6 · R7`; EM-R0f has landed too (train EM-T14).
  ⚠ **EM-R0c is the member that CALLS this one**, and its two expectations are re-measured at T16-12:
  `export function recordInvariants` and the re-export line **byte for byte** — both are what §6 spells.
- **Collision group:** **NONE.** No packet reserves any of this packet's four paths. ⚠ EM-A2a also
  CREATEs into `src/domain/edit/` (`pools.js`) and EM-R0c CREATEs two more there — all different
  files; CREATE targets are checked per FILE, not per directory.
- **Commit authority:** edits only; the chair commits
- **Baseline posture:** **measured, at `32602dc60`.** The control re-run and widened to 525; the
  `CHECK_META` totality and key-membership arms; the two MIXED objects' functional-dependence tests;
  the three-way split falsifier and its control; an isolating mutant for each new check; every
  version-2 headline figure re-executed. Every figure carries its command in `EM-R0b.evidence.md`.

---

## 1. Reconciled authority

1. **`docs/DESIGN_EDIT_MODE_AND_DECREES.md` §22.2 item 3** — the module's commission, AND the
   escalation ladder's shape: *"the enclosing declared group from `R1`, then its top-level key from
   `R1` — BOTH keys for a cross-key check"*. Version 3 is what makes that readable.
2. **§22.3 item 1** (the receipt split BY PATH, the five evidence checks LIVE), **item 5** (the
   cross-key checks SHIP; `stress ≡ stressors` joins), **item 6** (the ladders read by import; the
   flag data DECLARED and taught on the FULL 525 rows).
3. **The chair, 2026-09-20**, on EM-R0c's compile report: question 2 — *"EM-R0b goes to VERSION 3"*,
   exporting the per-check key table; question 4 — the two new MIXED objects are measured here.
4. **`docs/implementation/charters/EDIT-MODE-TRAIN.md`**, amendments of 2026-09-19 18:30 and 19:29.
5. `docs/implementation/PACKET_STANDARD.md`, `EM-PREAMBLE.md` §P1–§P9 (§P2 rows 10–12).
6. The measured ground, re-run at `32602dc60` — `EM-R0b.evidence.md`.

Resolved contradictions, each measured:

- ⭐⭐ **"SEVEN CROSS-KEY CHECKS" AND "EIGHT CHECKS NAMING TWO KEYS" ARE BOTH TRUE, AND THE
  DIFFERENCE IS THE CONTRACT.** A naive `keys.length > 1` reads **8**; version 2's §5.4 says
  **seven**; version 2 is right. `V-FLAGVEC` names `economicState` and `powerStructure` and relates
  **neither to the other** — it is two independent arms under one id. Escalating it at both keys
  would take `powerStructure` from `R1` because the FOOD card disagreed, the opposite of the minimal
  move item 3 requires. **RULED: a row may declare `arms`; a violation carries the keys of the arm
  that FIRED, never the union; and `CROSS_KEY_CHECKS` is derived as "no `arms` and more than one
  key" — a derivation that reproduces version 2's seven exactly** (V3-2b).
- ⭐⭐ **THE CHAIR'S QUESTION 4, ANSWERED NO TWICE — AND THE MEASUREMENT FOUND A THIRD SHAPE.** No
  invariant ties `defenseProfile.scores`' own leaves (every candidate arithmetic identity holds
  **0/525**; `disaster` REFUTED as a function of the other five, 4 ambiguous tuples) and none ties
  `history.founding`'s (`overcoming` REFUTED as a function of `initialChallenge`, 4 of 9, and of
  `reason`, 9 of 17). Both MIXED readings are LAWFUL. **But three leaves hold ONE fact at TWO paths
  on 525/525 — `defenseProfile.scores.magicDependency` ≡ `defenseProfile.magicDependency`,
  `scores.traditions` ≡ `defenseProfile.traditions`, `history.founding.age` ≡ `history.age` — and
  the merge splits all three 63/63 with a control that splits nothing** (V3-3d). They ship as checks.
- ⭐ **A GROUP CANNOT CLOSE TWO OF THE THREE.** EM-R0a v2's walker forbids a group root nested inside
  another's, and `G7 defense-readiness` is rooted at `defenseProfile.readiness`; a group rooted at
  `defenseProfile` would nest it. A group rooted at `history` WOULD be legal — and ⭐ **THE CHAIR HAS
  RULED IT: BOTH** (ODQ §934.47 addendum 38, ruling 1, 2026-09-20 02:47 EDT). `V-HISTORY-AGE` DETECTS
  here; the `history` consistency group PREVENTS the split and is **EM-R0a's row, added at EM-R0a's
  pre-proof with this measurement cited**. Nothing is open. The two defence pairs keep detection only,
  by the division §22.2 item 3 draws (V3-3f).
- ⭐ **THE READINESS PRODUCERS CANNOT DISAGREE** — `rulingStructure.js` contains **0 lines naming
  `readiness`** and `defenseProfile.readiness.label === readinessBandOf(score)` on **525 of 525**
  (V3-4 D2, D3). Version 2's finding, re-executed. Its `_provDefLabel` remains EM-R0d's row.
- ⭐ **THE TWO ROUNDING HAZARDS ARE UNCHANGED AT THIS TIP** — 36 rows publish `deficitPct ∈ (15,20]`,
  **12 carrying `isPressured` and 24 carrying `isDeficit`** (V3-4 D4), and the label→flags table
  reproduces version 2's to the row including `Import-Dependent → isDeficit(196) + isPressured(12)`.
  The tolerance rule of §6 and the DECLARED table stand exactly as ruled.
- The candidates the recon REJECTED and version 1 DROPPED stay dropped with their controls (§5.3).

The implementer does not read other documents to reinterpret this packet.

## 2. Outcome

**Observable result:** a PURE module `src/domain/edit/recordInvariants.js` exporting
`recordInvariants(settlement) → violations[]`, each `{ id, path, message, kind, keys }`, **and
`CHECK_META`** — a total, frozen, per-check table naming the record paths and top-level keys each
check relates, so EM-R0c's escalation ladder can take BOTH keys of a cross-key violation at step 1.
Clean on every plain generated record of the golden corpus except one dated, named, single-row KNOWN
pin; its four band checks read THE PRODUCERS' OWN TABLES by import; it declares **no threshold and no
learned envelope**. It lands in neither the generation worker nor the first-paint set.

**Definition of done:** thirty-three checks, each with its own control and a planted mutant that
trips it and nothing else; a `CHECK_META` row for every one of them, proved total in both directions,
with every key it names present in EM-R0a's register; the rejected and dropped candidates recorded
WITH their controls; the only declared data is the label→flags table (taught on 525, controlled on
63), the KNOWN pin and `CHECK_META`.

In scope: the instrument as a pure module; its per-check metadata; its control and mutant table.

Explicit non-goals:

- **the merge, the escalation and the delta model** — EM-R0c's. This packet ships the predicate AND
  the metadata the ladder reads; it does not walk the ladder.
- **the class register and the groups** — EM-R0a's. §5.4 hands it the table its register is checked
  against, and the `history` consistency group the chair RULED at ODQ §934.47 addendum 38 ruling 1 is
  **EM-R0a's row**, not this packet's.
- **the ladders** — EM-R0d's. This packet IMPORTS them and defines none.
- **the corpus ratchet** — EM-R7's.
- **FIX-G1**, and the food card's 15-versus-20 flag question (the owner's, ODQ §934.57).
- ⛔ This packet is a READER. It writes nothing and changes no generator.

## 3. Hard scope budget

| Limit | Packet budget | Measured |
|---|---:|---|
| Behavior families | `1` | — |
| New persisted record families · Named state writers · Feature flags · User-facing surfaces | `0` | — |
| Direct consumers | `0` at landing (EM-R0c is the first) | V3-6 |
| New logic-bearing production leaves | `1` — `recordInvariants.js`. The two data leaves are frozen tables that take no branch | cap 2 |
| Existing logic-bearing production files modified | `0` | — |
| Additional registration-only files | `0` | — |
| Handwritten files total | `4` | cap 12 |
| New/changed effective production lines | `≤ 340` | cap 400 |
| Effective lines per leaf | `≤ 230` `recordInvariants.js` · `≤ 40` `recordInvariantFlags.js` · `≤ 70` `recordInvariantMeta.js` (**measured 64**) | cap 250 |
| Delta in a shared/hot file | `n/a` — no hot file is named | — |
| Acceptance cases | `8` — the cap; A2 is RE-CUT, not added | §9 |
| ⭐ Generation-worker byte delta | **`0 B`** — the worker entry's static closure holds **0** `src/domain/edit/**` files, and nothing imports these three leaves. ⚠ The closure's SIZE is an as-of measurement, never a pin: **230** modules at `64b75240c` (220 at the compile tip). ⚠ `src/data/bandLadders.js` IS in that closure, so a future importer would price — this packet is imported by nothing | T16-7 |
| ⭐ First-paint delta | **`0`** — measured BY IMPORTING `EAGER_FIRST_PAINT_MODULES` from the tree's own `vite.config.js` (§P2 row 11: import the set, never read a list; the probe reads no `dist` and cannot skip): **0** `src/domain/edit/**` members and 0 of this packet's leaves. ⚠ The SIZE is an as-of measurement, never quoted as a pin — **270** at `64b75240c`, 269 at `bdbf7c89c`, 268 at `141a1d775`. ⭐ **The delta is 0 whatever the size is** | T16-7 |

Overrides approved before dispatch: `NONE`.

⚠ **IDENTITY POSTURE: byte-identical output.** This packet adds a reader and changes no generator.
Neither golden appears in §7 and neither may move.

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- EM-R0b
```

Expected: all four targets ABSENT; every `requiredSymbols` row resolving verbatim.

⭐⭐ **READ DRY AT `64b75240c` BY THIS PRE-PROOF, CHECK BY CHECK (T16-13), against
`scripts/implementation-session.mjs`:**

| the dispatch's check | at `64b75240c` |
|---|---|
| status supplied by promotion; `parsePacketHeader` reads a status at all | PASS — `- **Status:** \`DRAFT\`` stands ALONE on its line, whitespace only after the closing backtick; every stamp sits on the continuation lines beneath it |
| manifest validates: 4 change rows, actions in `PACKET_ACTIONS`, 8 acceptance cases as `{id, case}` objects, no duplicate change path | PASS — unchanged at version 8; the capsule and §7 stay SET-EQUAL and the count prover proves it |
| `merge-base --is-ancestor <base> HEAD` | PASS for any base the chair stamps at or after EM-R0a's and EM-R0d's landings; **FREE when base === HEAD**, on the literal early return |
| `assertAncestorAndSubstrate`: no diff across `base..HEAD` over the substrate | PASS — substrate is `src/domain/activeConditions.js`, `tests/helpers/goldenMasterCorpus.js`, `src/generators/generateSettlementPipeline.js`, `tests/build/domainGeneratorsBoundary.test.js`, `src/data/bandLadders.js`, `src/domain/edit/recordRegister.js` (CREATE rows excluded). Empty at base === HEAD by the early return; at an earlier base, two of those six moved in the 206-commit window, so an EARLIER base REFUSES — this is the one reason the base is not free |
| the capsule names every substrate path | PASS — the second arm (`capsule omitted declared substrate`) is skipped with the first |
| CREATE targets absent and Git-clean | PASS — all four ABSENT at `64b75240c` (T16-2); ⚠ the worktree's cleanliness is the dispatching lane's, not a fact a pre-proof can carry forward |

⭐ **THE TYPE FLOOR, TWO CONFIGS, EACH NAMED PER PATH (§P2 row 20; measured at `64b75240c`, T16-14).**
`tsconfig.full.json` includes `src/domain/**/*.js` and EXCLUDES `tests` and `src/data`; `tsconfig.domain-strict.json`
extends `tsconfig.json` and turns `strict` + `noImplicitAny` ON over the domain. So the **three
`src/domain/edit/**` CREATEs are governed by BOTH**, and `tests/domain/recordInvariants.test.js` by
**NEITHER** (tests are excluded from the full config and outside the strict config's include) — eslint is
its only static gate. ⛔ `scripts/.domain-strict-baseline.json` carries **no `src/domain/edit/` row**, so
each new leaf must be **strict-clean at zero**; `check-domain-strict.mjs` counts errors located in
`src/domain` only and ignores import-followed errors elsewhere, so `src/data/bandLadders.js` being outside
the full config's include list costs this packet nothing.

⚠ **`checks` ORDER:** **no declared command is a generator** — `npx eslint`, `npm run typecheck:*`
and `npx vitest run` only — so no step re-stamps a declared path mid-chain and `check:packet` reaches
every step. The boundary ratchet is a `checks` command and a `requiredSymbols` row, deliberately
**not** a manifest row: this packet must leave `BASELINE_EDGES` byte-identical.

## 5. Verified tree contract

### 5.1 ⭐ THE FOUR BAND LADDERS — all read by import, none copied

| reading | the table this packet imports | form | control | evidence |
|---|---|---|---|---|
| `activeConditions[].severityBand` | `src/domain/activeConditions.js#severityBand` — a pure `0..1 → band` function | **EXACT**, over EVERY entry | 525/525 clean | V3-1 |
| `powerStructure.publicLegitimacy.label` | `src/data/bandLadders.js#legitimacyBandOf` (EM-R0d) — cuts 75/60/45/30, CLOSED below | **EXACT** | 525/525 clean | V3-1 |
| `defenseProfile.readiness.label` | `src/data/bandLadders.js#readinessBandOf` (EM-R0d) — cuts 76/55/38/24/12, CLOSED below | **EXACT** | **525/525, 0 disagreements** | V3-4 D2 |
| `economicState.foodSecurity.label` | `src/data/bandLadders.js#foodSecurityBandOf` + `FOOD_SECURITY_CUTS` (EM-R0d) — 40/15/5 on the deficit, 40 on the surplus, **OPEN below**, famine outranking | **EXACT, TOLERANT OF ROUNDING** (§6) | 525/525 clean | V3-4 D4 |

⛔ **No envelope is learned and no threshold is written in this packet.** A4 asserts it by scanning
the module's own source for a numeric literal outside the declared tolerance constants.

### 5.2 The rest of the verified contract

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| The one domain producer | `src/domain/activeConditions.js` | `export function severityBand` | the module has ZERO imports of its own and is already in both budgeted graphs | IMPORT it. ⛔ Never copy its thresholds |
| The ladder leaf | `src/data/bandLadders.js` | `legitimacyBandOf` · `readinessBandOf` · `foodSecurityBandOf` · `FOOD_SECURITY_CUTS` · ⭐ **`FOOD_SECURITY_BANDS`** · ⭐ **`LEGITIMACY_BANDS`** | ⭐ **LANDED (EM-R0d, train EM-T14) and PRESENT at `64b75240c`**; all six resolve verbatim, `grep -cF` = 1 each (T16-3). Imported at the `src/data/` address, NOT a `src/domain/` re-export. ⚠ The two band TABLES are NEW rows at this pre-proof and they exist to keep a label OUT of this packet's source: see §6's flag table and §12 question 1 | IMPORT the `src/data/` address |
| The class register | `src/domain/edit/recordRegister.js` | `RECORD_CLASSES` | ⭐ **LANDED (EM-R0a) and PRESENT at `64b75240c`**, `grep -cF` = 1 (T16-3); the file also exports `CONSISTENCY_GROUPS`, which §5.4's "closed by" column names. **A2 reads it to prove every `CHECK_META` key is a classed key.** ⚠ No importer roster and no dormancy arm governs it — `tests/lint/recordRegisterTotality.walker.test.js` carries arms A1–A7 over the register's own CONTENT, and `tests/lint/heldKeyWriterCensus.walker.test.js` imports it; neither enumerates importers, so this packet's TEST becoming a new importer carries NO widened row (judgment 148, measured at T16-11) | IMPORT it in the TEST only; ⛔ the module imports it NOT |
| The layering ratchet | `tests/build/domainGeneratorsBoundary.test.js` | `const BASELINE_EDGES` | ⭐ **RE-MEASURED at `64b75240c`: FOUR importing files / FIVE specifiers** — `checkDraftEdit.js`, `neighbourBackLink.js`, `institutionLifecycle.js` and `resourceDynamicsKernel.js` (two specifiers). The file MOVED in the window (judgment 111's root-derivation arm was added and the cardinality guard re-titled to "the 4 files and 5 known edges"); the FROZEN SET did not (T16-4). It now holds FOUR `it`s | These leaves import **no** `src/generators` module. A8 asserts it. ⛔ RUN, NEVER EDITED |
| Worker ceiling | `tests/build/generationWorkerLazy.test.js` | `WORKER_BUNDLE_CEILING_BYTES` | these leaves are **not in the worker's 220-module closure** | ⛔ READ ONLY, not a manifest row |
| First-paint closure | `vite.config.js` | `EAGER_FIRST_PAINT_MODULES` | **IMPORTED at this pre-proof; its size is an as-of measurement, never quoted as a pin** (**270** at `64b75240c`; 269 at `bdbf7c89c`; 268 at `141a1d775`). The fact asserted is MEMBERSHIP: **0** `src/domain/edit/**` (T16-7). ⚠ The module also exports `ENGINE_SHARED_DOMAIN_EXCISIONS` — a replica seeded before it reads wrong (§P2 row 15's correction) | ⛔ READ ONLY. Delta 0 |
| Corpus helper | `tests/helpers/goldenMasterCorpus.js` | `goldenCorpus` · `keyOf` | the 525-row corpus, ONE spelling; it exports `goldenCorpus` and `keyOf` and **NOTHING ELSE** — ⛔ **no stride, sample or slice helper exists anywhere in `tests/`** (P-7) | The test's corpus. ⛔ Never a second spelling |
| ⭐⭐ The stride | — | `__STRIDE_RULE__` | ⛔ **OPEN — §12 question 3.** "The 63-row stride" that A1/A3/A6/A7 name is defined in NO tree artifact, and **no arithmetic stride over 525 yields 63** (measured k∈[2,12], P-7). It is an artifact of EM-R0c's kit prototype carried into a SHIPPING test's contract. The pin sits at corpus index **510** | The rule the chair picks is written HERE as one line of source, so the suite's own sample is reproducible from the tree alone |
| Generator entry | `src/generators/generateSettlementPipeline.js` | `generateSettlementPipeline` | the pipeline the control runs | The test calls it; the MODULE never does |
| Mutation-coverage set | `tests/lint/mutationCoverage.shared.mjs` | `ENFORCER_DIRS` · `NAME_PATTERN` | `recordInvariants.test.js` matches neither | §7's zero row, and the naming pin below |
| Test precedent (corpus) | `tests/property/generatorGoldenMaster.test.js` | its whole-corpus generate-and-assert loop | literal titles | Copy this shape for A1 |
| Test precedent (unit) | `tests/domain/activeConditions.test.js` | its `describe`/`it` shape | a plain domain unit test | Copy this shape for A2–A8 |
| Golden posture | `tests/property/generatorGoldenMaster.test.js` · `tests/property/dossierProseManifest.test.js` | — | neither appears in §7 | UNCHANGED; motion is a STOP |

Forbidden alternatives:

- ⛔⛔ **NO `src/generators/**` MODULE MAY IMPORT ANY OF THE THREE LEAVES.**
- ⛔⛔ **THE MODULE IMPORTS NOTHING FROM `src/generators/**`** — a new `src/domain → src/generators`
  edge against a shrink-only ratchet. That refusal is why EM-R0d exists.
- ⛔ **`recordInvariants.js` IMPORTS `recordRegister.js` NOT.** `CHECK_META` names keys as STRINGS;
  only the TEST reads EM-R0a's register, to prove the names resolve. A runtime import would couple
  the predicate to the register for nothing and enlarge EM-R0c's closure.
- ⛔ **NO THRESHOLD IS WRITTEN AND NO ENVELOPE IS LEARNED.** A4 scans the module's source.
- ⛔ **NO FLAG IS DERIVED FROM A PUBLISHED NUMBER** — measured, it convicts 24 of 525 honest rows.
- ⛔ **NO CHECK SHIPS WITHOUT A PLANTED MUTANT** that isolates it (`V-STRESS-NAME`'s class).
- ⛔ **NO CHECK SHIPS WITHOUT A `CHECK_META` ROW** — A2's totality arm is the red.
- ⛔ **NO LEARNING AT RUNTIME.** `recordInvariants(settlement)` takes ONE record. A second parameter
  is STOP-1.
- ⛔ **NO BLANKET TOLERANCE FOR THE KNOWN DEFECT** — one named row with its date and its cure's
  packet id, asserted STILL PRESENT.
- ⛔ **THE TEST FILE'S BASENAME MAY NOT CARRY A `NAME_PATTERN` TOKEN** (`…contract.test.js` and
  `…walker.test.js` both MATCH and would silently owe a mutation-coverage row).
- ⛔⛔ **THE TEST FILE NEVER BINDS `it`, `test` OR `describe` AS A VARIABLE OR PARAMETER.**
  Straight-line literal `it`s under ONE literal `describe` — no `.each`, no loops, no nesting, no
  conditionals — or the promised `credited +1` lands as `parked +1`.
- no new top-level key; no persisted shape; no flag; no PRNG; no clock; no store; no files outside
  the manifest.

### 5.3 ⭐ THE REJECTED AND THE DROPPED — recorded so nobody re-adds them

| candidate | why it may not convict | the control that refused it |
|---|---|---|
| `criticalIssueCount = issues.length` | counts only the CRITICAL issues | REJECTED by the recon: false on 6 of 63 plain records |
| `foodRatio = production / need` | the ratio counts imports and magic | REJECTED by the recon: false on plain thorps and hamlets |
| `V-STRESS-NAME` | **it cannot convict** — zero violation return sites | DROPPED at v1, re-confirmed |
| the recon's band pair 4 | the same number as pair 3 on 525/525 | DROPPED at v1 as redundant |
| ⭐ every LEARNED band envelope | narrower than the truth; they would convict a DM's honest edit | STRUCK by §22.3 item 6; replaced by EM-R0d's tables read by import |
| ⭐ deriving the food flags from the published `deficitPct` | the flags were computed UNROUNDED and cut at 20 where the label cuts at 15 | REFUSED by its own control: **24 of 525** honest rows (V3-4 D4/D5) |
| ⭐ **NEW at v3** — `disaster` as a function of the other five defence scores | the same five scores yield different `disaster` values | REFUTED: **4 ambiguous tuples of 82** (V3-3b) |
| ⭐ **NEW at v3** — `overcoming` as a function of `initialChallenge` or of `reason` | the founding prose is drawn independently | REFUTED: 4 of 9 and 9 of 17 ambiguous (V3-3c) |
| ⭐ **NEW at v3** — any arithmetic identity over `defenseProfile.scores` | four candidates tried | each holds **0 of 525** (V3-3b) |

### 5.4 ⭐ FOR EM-R0a — every check, and the group or keys that close it

⭐⭐ **RE-MEASURED AGAINST THE LANDED REGISTER AT `64b75240c`, NOT AGAINST EM-R0a's PREDICTED SHAPE**
(judgment 134). `src/domain/edit/recordRegister.js :: CONSISTENCY_GROUPS` declares **SEVEN** groups, by
id and root: `food-security` (`economicState.foodSecurity`) · `food-balance`
(`economicViability.metrics.foodBalance`) · `viability-counts` (`economicViability`) · `isolation-support`
(`isolationSupport`) · `condition-severity` (`activeConditions[]`) · `defense-readiness`
(`defenseProfile.readiness`) · ⭐ **`history-age` (`history`)** — the six this table has always named as
G1–G4, G6, G7, **PLUS the `history` group the chair ruled at ODQ §934.47 addendum 38 ruling 1, which has
now LANDED with EM-R0a**. Nothing is owed and nothing is open. ⭐ And `RECORD_CLASSES` classes **47**
top-level keys, among which **all fourteen** this table's `keys` column names are present, so A2's
key-membership arm holds against the LANDED register rather than a predicted one (T16-3). **This table is
what EM-R0a's register is checked against, and it is also the source of `CHECK_META`.**

| check | kind | record paths related | keys | closed by |
|---|---|---|---|---|
| `V-DEPCOUNT` · `V-WARNCOUNT` · `V-SUMMARY-DEPS` · `V-SUMMARY-HOOKS` | count / prose-count | `economicViability.{metrics.*,dependencies,warnings,summary,plotHooks}` | `economicViability` | **G3** |
| `V-FOOD-RAW` · `V-FOOD-DEF` · `V-FOOD-PCT` | arithmetic | `economicViability.metrics.foodBalance.*` | `economicViability` | **G2** |
| `V-FOODSEC-RAW` · `V-FOODSEC-DEF` · `V-FOODSEC-PCT` · `V-FOODSEC-CHAINS` · `V-FOODSEC-FLAGS` · `V-BAND-FOODSEC` | arithmetic / count / flag-vector / band | `economicState.foodSecurity.*` | `economicState` | **G1** |
| `V-ISOLATION` | arithmetic | `isolationSupport.{deficit,requiredCapacity,capacity}` | `isolationSupport` | **G4** |
| `V-COND-BAND` | band | `activeConditions[].{severity,severityBand}` | `activeConditions` | **G6** (per-entry) |
| `V-BAND-READINESS` | band | `defenseProfile.readiness.{score,label}` | `defenseProfile` | **G7** |
| `V-INCOME-100` | arithmetic | `economicState.incomeSources[].percentage` | `economicState` | **ATOMIC**, not a group |
| `V-POWER-100` | arithmetic | `powerStructure.factions[].power` | `powerStructure` | **ATOMIC** + HELD |
| `V-LEGIT-SUM` · `V-LEGIT-FLAGS` · `V-BAND-LEGITIMACY` · `V-GOVERNING` | arithmetic / flag-vector / band / count | `powerStructure.{publicLegitimacy.*,factions[],governingName}` | `powerStructure` | **HELD-internal** |
| `V-FLAGVEC` | flag-vector | `economicState.foodSecurity.label` · `powerStructure.publicLegitimacy.label` | `economicState` · `powerStructure` | ⭐ **TWO ARMS, each single-key — NOT cross-key** |
| ⛔ `V-EVIDENCE-ROSTER` | prose-count | `generationCoherenceReceipt.judgments[].evidence` · `npcs` · `relationships` | 3 keys | **CROSS-KEY** |
| ⛔ `V-EVIDENCE-EVENTS` · `V-EVIDENCE-TENSION` | prose-count / referential | `…judgments[].evidence` · `history.*` | 2 keys | **CROSS-KEY** |
| ⛔ `V-EVIDENCE-STRESS` | referential | `…judgments[].evidence` · `stress.label` | 2 keys | **CROSS-KEY** |
| ⛔ `V-EVIDENCE-CONFLICT` | referential | `…judgments[].evidence` · `conflicts` | 2 keys | **CROSS-KEY** |
| ⛔ `V-DEFENSE-INST` | referential | `defenseProfile.institutions` · `institutions` | 2 keys | **CROSS-KEY** (and EM-R6's removal sweep) |
| ⛔ `V-STRESS-IDENTITY` | referential | `stress` · `stressors` | 2 keys | **CROSS-KEY** — byte-identical 525/525 |
| ⭐ `V-DEFENSE-MAGICDEP` **NEW** | referential | `defenseProfile.scores.magicDependency` · `defenseProfile.magicDependency` | `defenseProfile` | **NOTHING** — no legal group (a root at `defenseProfile` would nest G7); the check IS the closure |
| ⭐ `V-DEFENSE-TRADITIONS` **NEW** | referential | `defenseProfile.scores.traditions` · `defenseProfile.traditions` | `defenseProfile` | **NOTHING** — same reason |
| ⭐ `V-HISTORY-AGE` **NEW** | referential | `history.founding.age` · `history.age` | `history` | the check **AND** the `history-age` group at EM-R0a — the chair RULED BOTH (ODQ §934.47 addendum 38 ruling 1), and ⭐ **the group has LANDED**, rooted at `history`, measured in `CONSISTENCY_GROUPS` at `64b75240c` (T16-3) |

**Thirty-three checks. SEVEN are cross-key** and ship under §22.3 item 5 — `V-FLAGVEC` is not among
them, by the `arms` rule of §6. Every group G1–G7 is exercised by at least one check, and no check
names a group EM-R0a does not declare.

## 6. Exact contracts

```js
// src/domain/edit/recordInvariants.js — PURE. No store, PRNG, clock, locale, I/O, no mutation
// of the argument. ⛔ NO src/generators IMPORT. ⛔ NO recordRegister.js IMPORT.
import { severityBand } from '../activeConditions.js';
import {
  legitimacyBandOf, readinessBandOf, foodSecurityBandOf,
  FOOD_SECURITY_BANDS, LEGITIMACY_BANDS,
} from '../../data/bandLadders.js';
import { FLAGS_EVER_TRUE, FLAG_FIELDS, KNOWN_VIOLATIONS, FLAG_CORPUS } from './recordInvariantFlags.js';

export { CHECK_META, CHECK_IDS, CROSS_KEY_CHECKS } from './recordInvariantMeta.js';

/**
 * @typedef {'arithmetic'|'count'|'prose-count'|'referential'|'band'|'flag-vector'} InvariantKind
 * @typedef {{ id: string, path: string, message: string, kind: InvariantKind,
 *             keys: string[] }} Violation
 * @param {Record<string, any>} settlement  ONE record. Never a corpus.
 * @returns {Violation[]}  EMPTY when consistent. Declared check order, then array index.
 *                         Never null, never undefined, never throws.
 */
export function recordInvariants(settlement) { /* … */ }
```

```js
// src/domain/edit/recordInvariantMeta.js — PURE FROZEN DATA. Imports NOTHING.
/**
 * @typedef {{ kind: InvariantKind, paths: string[], keys: string[],
 *             arms?: { id: string, keys: string[] }[] }} CheckMeta
 */
export const CHECK_IDS = Object.freeze([ /* the 33 of §5.4, in declared order */ ]);
export const CHECK_META = Object.freeze({ /* one row per id */ });
export const CROSS_KEY_CHECKS = Object.freeze(
  CHECK_IDS.filter(id => !CHECK_META[id].arms && CHECK_META[id].keys.length > 1),
);
```

- `paths` — the record paths the check relates, VERBATIM from §5.4. `keys` — the distinct top-level
  keys those paths head at, lexicographic. **Every `paths` entry must head at a key its own `keys[]`
  carries; A2 asserts it.**
- ⭐ **`arms`** — present ONLY where a check has INDEPENDENT arms. **A violation carries the keys of
  the ARM THAT FIRED, never the union.** `V-FLAGVEC` is the only such row today.
- ⭐ **`CROSS_KEY_CHECKS` is DERIVED, never hand-listed:** no `arms` and more than one key. The
  derivation reproduces §5.4's seven exactly; hand-listing would drift the first time a check is
  added.
- `violation.keys` is what EM-R0c's escalation ladder reads at step 1 (design §22.2 item 3): the
  enclosing declared group, or — for a cross-key violation — **both keys from `R1`**.
- The parameter is **`settlement`** — the estate's receiver prior.
- Three outcomes per check: a violation, `true`, or **ABSTAIN**. Absent or mistyped inputs ABSTAIN;
  abstention is never reported.
- `violation.path` is the record path of the **derived** leaf that disagrees.
- ⛔⭐ **`violation.message` RENDERS NO NUMBER.** `tests/lint/proseNumerics.test.js` walks every
  `.js`/`.jsx` file under `src/` (not only the ones a packet modifies) through
  `tests/helpers/proseNumericsWalk.js`, whose detectors fire on a percent beside a numeral or an
  interpolation, on `×` beside one, and on a two-decimal float near a score word; the register is
  per-file with a category ceiling and a NEW file is held at zero. A message names the two PATHS and the
  disagreement in words (`"defenseProfile.readiness.label disagrees with readinessBandOf(score)"`) and
  carries no `%`, no `×`, no `.toFixed(2)` and no interpolated figure. The figures belong to the test's
  own assertion output, which the walker does not read. Measured at T16-10.

### The band checks

Three are a plain call: `legitimacyBandOf(score).label === label`, `readinessBandOf(score).label ===
label`, `severityBand(c.severity) === c.severityBand` — for EVERY `activeConditions` entry.

⭐ **The fourth is TOLERANT OF ROUNDING, and this is a contract, not a slack** — UNCHANGED from
version 2, and the facts that forced it re-measure identically (V3-4).

⛔⛔ **THE RULE IS WRITTEN IN INTEGER ARITHMETIC, AND THAT IS A REGISTRATION OBLIGATION, NOT A
STYLE.** `scripts/lib/tuning-inventory.mjs` holds a NEW `src/domain` file at **P3 = 0 bare decimal
literals** and **P2 = 0 module-top-level `const UPPER_SNAKE = <numeric>`**, both SHRINK-ONLY with new
files at zero, and `tests/lint/tuningRegister.walker.test.js` is the arm (*"P3 is SHRINK-ONLY with new
files at zero"*, *"P2 is SHRINK-ONLY with new files at zero"*). A `0.5` written anywhere in
`recordInvariants.js` — inline or as a named tolerance constant — REDS that walker at the build.

```text
V-BAND-FOODSEC, given published deficitPct d, published surplusPct s, and the famine flag f:
  when d and s are BOTH integers (Number.isInteger):
    ADMISSIBLE = { foodSecurityBandOf(f, d, s), foodSecurityBandOf(f, d + 1, s),
                   foodSecurityBandOf(f, d, s + 1), foodSecurityBandOf(f, d + 1, s + 1) }
  otherwise (an unrounded value needs no forgiveness for a rounding that never happened):
    ADMISSIBLE = { foodSecurityBandOf(f, d, s) }
  CONVICT iff the record's label is not in ADMISSIBLE.
```

⭐ **THE TWO FORMS ARE THE SAME FUNCTION, EXECUTED.** For integer cuts and a strict `>`,
`d + 0.5 > C` is `d >= C` is `d + 1 > C`, and `d - 0.5 > C` is `d > C`. Measured over every
`(f, d, s)` in `{false,true} × 0..100 × 0..100`: **20,402 triples checked, 0 differences** against the
nine-point half-unit set (T16-9). The preconditions are measured, not assumed: all four cuts are
integers (`src/data/bandLadders.js :: FOOD_SECURITY_CUTS`) and both published percentages are
`Math.round`ed at generation (`src/generators/foodGenerator.js :: the foodSecurity record's
`deficitPct`/`surplusPct` fields`). The six-row table below is reproduced EXACTLY, every ✓ and every
"still reds" (T16-9). ⚠ **The non-integer branch is STRICTER than version 7's rule, never looser** —
a DM edit that writes an unrounded percentage is judged exactly, which is what the rounding tolerance
was never for. §12 question 2 puts the alternative (declare the tolerance as a P1 tuning table and
register it) to the chair.

| published | label | EXACT | TOLERANT | what it is |
|---|---|---|---|---|
| `d=15, s=0` | `Import-Dependent` | ⛔ CONVICTS | ✓ passes | honest: unrounded 15.4 |
| `d=15, s=0` | `Pressured` | ✓ | ✓ | honest: unrounded 14.6 |
| `d=15, s=0` | `Deficit` | ⛔ | ⛔ **still reds** | genuinely wrong |
| `d=5, s=0` | `Pressured` | ⛔ CONVICTS | ✓ passes | honest: unrounded 5.3 |
| `d=5, s=0` | `Secure` | ✓ | ✓ | honest: unrounded 4.8 |
| `d=5, s=0` | `Import-Dependent` | ⛔ | ⛔ **still reds** | genuinely wrong |

The tolerance is **exactly half a unit and never more**.

### The flag-vector check — UNCHANGED from version 2

```text
for each declared (objectPath, flags[]) and the object's own `label`:
  1. the label was NEVER observed  -> ABSTAIN ENTIRELY.
  2. a flag that is FALSE          -> NEVER judged.
  3. a TRUE flag in the label's observed set -> pass.
  4. a TRUE flag in NO observation of that label -> CONVICT.
```

⛔ **THE WHOLE-VECTOR FORM IS REFUSED.** ⭐ **THE TABLE IS WHAT THE GENERATOR DOES**, taught on the
FULL 525 rows and re-measured at the version-3 tip to the row (V3-4 D5).

⛔⛔ **THE TABLE IS KEYED BY THE LADDER'S BAND KEY, NEVER BY THE LABEL TEXT, AND THAT IS A
REGISTRATION OBLIGATION.** `tests/copy/voiceMechanics.test.js` counts every em dash inside every
string literal of every `.js` file under `src/data` and `src/domain` (Tier 2, AST-based, comments and
non-literal text excluded), matches its per-file baseline EXACTLY (*"per-file debt exactly matches the
baseline (grew ⇒ rewrite; fell ⇒ bank the win)"*), and holds a file with no baseline row at ZERO.
One of the six food labels is `Deficit — Active Famine`. **Spelling it in
`recordInvariantFlags.js` would red that walker at the build** — EM-B1c1 version 4 was stopped by
exactly one em dash. `src/data/bandLadders.js` carries the label lawfully because it holds an
exact-string row in that file's `AUTHORED_VOCABULARY_ALLOWLIST` (measured, T16-5); this packet does not.

So: **`FLAGS_EVER_TRUE` is keyed by the ladder's own band KEY** (`famine`, `deficit`,
`importDependent`, `pressured`, `secure`, `surplus`; and the legitimacy ladder's keys likewise), and
`recordInvariants.js` resolves a record's `label` to its key through the IMPORTED `FOOD_SECURITY_BANDS`
and `LEGITIMACY_BANDS` tables at runtime. **No label text is spelled in any of the three leaves**, the
declared content of the table is unchanged to the row, and the three leaves stay at em 0 / bang 0.
§12 question 1 puts the alternative (an exact-string allowlist row in `tests/copy/voiceMechanics.test.js`
as a TEST manifest row) to the chair. The table, by label for the reader and by key for the source:

| object | label → flags EVER true |
|---|---|
| `economicState.foodSecurity` | `Secure`→`isSecure`(117) · `Pressured`→`isPressured`(151) · **`Import-Dependent`→`isDeficit`(196)+`isPressured`(12)** · `Deficit`→`isDeficit`(13) · `Deficit — Active Famine`→`isDeficit`(36) |
| `powerStructure.publicLegitimacy` | `Endorsed`→`isEndorsed`+`isApproved` · `Approved`→`isApproved` · `Tolerated`→`isTolerated` · `Contested`→`isContested` · `Legitimacy Crisis`→`isLegitimacyCrisis` |

⚠ **Every label present at 525 is present in the 63-row stride** (V3-4 D6: 5 labels, 0 lost).

### ⭐ The three duplication checks (NEW at version 3)

```text
V-DEFENSE-MAGICDEP    defenseProfile.scores.magicDependency === defenseProfile.magicDependency
V-DEFENSE-TRADITIONS  defenseProfile.scores.traditions      ≡  defenseProfile.traditions   (deep)
V-HISTORY-AGE         history.founding.age                  === history.age
```

Each ABSTAINS unless both paths are present. Each holds on **525 of 525** plain records, and the
merge can put the two copies out of step on **63 of 63** constructed trials with a control that
splits nothing (V3-3d). They are the ONLY instrument available for the two defence rows: a
consistency group rooted at `defenseProfile` would NEST `G7 defense-readiness`, which EM-R0a's own
walker forbids.

### Absence, ordering, determinism, lifecycle

- absent reading/object, non-number score, non-string label, label outside the table: **ABSTAIN**.
- `violations`: an empty array. ⛔ Never `null`, never `undefined`.
- a check that throws: ⛔ forbidden — total over any input by construction. A7 is the proof.
- **Order:** declared check order (`CHECK_IDS`), then array index. No content sort.
- ⚠ **The arithmetic checks carry a `|difference| ≤ 1` tolerance**; `V-ISOLATION` carries none; the
  three duplication checks carry none (they are exact identities). A3's mutants clear each check's
  own tolerance.
- Hash/fork key `NONE`; no PRNG, clock, locale or `Intl`.
- **Lifecycle:** module load, frozen, read-only. Never persisted or projected; every column `n/a`.
- Flag `NONE`. Golden posture **UNCHANGED**. Alignment `DECLARED EMPTY`.
  Edit story: `ENGINE-ONLY — EM-R0c's delta card is where a violation becomes words.`
- Closed kinds: the six §22.2 item 3 names, and no seventh.

## 7. Exact change manifest

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `src/domain/edit/recordInvariants.js` | `recordInvariants` | `≤ 230` | The thirty-three checks of §5.4 in `CHECK_IDS` order, each violation carrying `kind` and `keys` from `CHECK_META` (the arm's keys where the row declares arms). ⛔ Import ONLY `../activeConditions.js`, `../../data/bandLadders.js`, `./recordInvariantFlags.js` and `./recordInvariantMeta.js`. ⛔ No `src/generators` import, no `src/components` import, **no `recordRegister.js` import**, no numeric threshold, no corpus. Parameter named `settlement`. Re-export `CHECK_META`, `CHECK_IDS` and `CROSS_KEY_CHECKS` so EM-R0c's compiled import is satisfied verbatim — the re-export LINE is what EM-R0c's `requiredSymbols` row quotes, so it is written byte for byte as §6 spells it. ⭐ **THE FOUR INSTRUMENT PINS, MEASURED AT `64b75240c`:** (i) **no bare decimal literal and no module-top-level `const UPPER_SNAKE = <numeric>`** — the tuning register holds a new `src/domain` file at P3 = 0 and P2 = 0, so §6's integer form of the rounding rule is the only lawful spelling; (ii) **no em dash inside any string literal** — the voice walker holds a new `src/data`/`src/domain` file at em 0; (iii) **no rendered number in any `message`** — prose-numerics holds a new `src/` file at 0 in every category; (iv) ⛔ **no `.institutions` TOKEN IN CODE** — `V-DEFENSE-INST` reads its two paths through the declared `CHECK_META.paths` strings, never as a property chain, so this leaf never enrols in `tests/lint/ruinFilterRoster.walker.test.js`'s discovery set (that walker scans `codeOnly(src)`, which blanks strings AND comments, so a path spelled as DATA is not a roster read — measured T16-5). |
| `CREATE` | `src/domain/edit/recordInvariantMeta.js` ⭐ NEW | `CHECK_IDS`, `CHECK_META`, `CROSS_KEY_CHECKS` | `≤ 70` | Pure frozen data, §5.4's table transcribed. **Import nothing.** `CROSS_KEY_CHECKS` is DERIVED from `CHECK_META`, never hand-listed. The header names the design sections the `keys` field serves (§22.2 item 3, §22.3 item 5) and states the `arms` rule. ⭐ **The `paths` strings are DATA and stay strings** — they are what keeps `V-DEFENSE-INST` out of the ruin-filter discovery set. ⛔ No em dash, no bare decimal, no `UPPER_SNAKE` numeric const. |
| `CREATE` | `src/domain/edit/recordInvariantFlags.js` | `FLAGS_EVER_TRUE`, `FLAG_FIELDS`, `KNOWN_VIOLATIONS`, `FLAG_CORPUS` | `≤ 40` | Pure frozen data, §6's table verbatim. **Import nothing.** The header names the corpus (525 rows, `goldenCorpus`), the measuring sha, and that the table records what the generator DOES across its two ladders. ⛔⭐ **KEYED BY THE LADDER'S BAND KEY, NEVER BY THE LABEL TEXT** (§6): one of the six food labels carries an em dash and the voice walker holds this new file at em 0. ⛔ No numeric literal at all. |
| `CREATE` | `tests/domain/recordInvariants.test.js` | A1–A8 | `n/a` | ⛔ **Straight-line literal `it`s under ONE literal `describe`; never bind `it`, `test` or `describe` as a variable or parameter.** No `.each`, loops, nesting or conditionals AT REGISTRATION (a loop INSIDE an `it` body is fine and is what A1 and A3 use). ⭐ **`// anchored:` on the ONE line immediately above every negative `expect`** — above the `expect` itself, never above a wrapped expect's first line (§P6, EM-B1c1 v5's build red); the marker's spelling is `// anchored: <why>` (measured in `tests/lint/chanceMeetingKindPools.walker.test.js`). ⭐ **A1 and A3 COLLECT then assert ONCE** — `tests/lint/seedLoopTotality.walker.test.js` scans `tests` WHOLE and convicts a bare `for (const seed of …) { expect(…) }`; no loop header names a seed. The basename may gain no `NAME_PATTERN` token (measured: `recordInvariants.test.js` matches none of the sixteen, and `tests/domain` is not an `ENFORCER_DIR` — T16-10). ⛔ Every title unique in `tests/` (judgment 196). |

Generated artifacts: `NONE`. No leaf is in an edge-shared bundle closure. **No declared `checks`
command writes any path.** No other file may be edited.

> ⛔ **ROW 4 IS `CREATE`, NOT `TEST` (ESTATE-REPAIR-2, 2026-09-20).** `TEST` is for an EXISTING test
> file; a new one is `CREATE`. Measured at `bdbf7c89c`: `tests/domain/recordInvariants.test.js` is
> ABSENT, and **no other packet in the waiting estate CREATEs it** — this packet is its own creator,
> as §10 already says in terms (*"This packet CREATEs `tests/domain/recordInvariants.test.js`"*).
> `implementation-packets.mjs` exempts only `CREATE` from the row-existence arm, so the old row
> refused with `path does not exist for TEST`. The §7 table and the JSON capsule stay set-equal.

**Predicted register moves, priced here rather than discovered at the terminal:**

| Register | Moves? | Predicted delta | Door |
|---|---|---|---|
| Lighting census | **YES** | ⭐ **`files +1` · `credited +1` · `parked +0` · `titles +7` · `suiteTitles +1`** — **CORRECTED AT PRE-PROOF from version 3's `titles +8`.** The census sums `liveTitlesIn` over CREDITED files only; §9 homes **A8 in `tests/build/domainGeneratorsBoundary.test.js`, run and NOT edited**, so it lands no `it` here and an unedited file contributes nothing. SEVEN arms (A1–A7) land in the new file under ONE literal `describe` (P-6). ⚠ `credited +1` is conditional on §5.2's registration pin. ⛔ **NO ABSOLUTE IS RESTATED** — the live tuple is the chair's to stamp at promotion from `tests/lint/.lighting-census-baseline.json` | interior red; re-derived at the terminal by the chair |
| Mutation-coverage | **NO** | zero — `tests/domain` is not an `ENFORCER_DIR` and the basename matches no `NAME_PATTERN` token. ⚠ Both halves matter |
| ⚠ Observed-shape readers | **PRICED, RED-FIRST AT THE BUILD** | a NEW file has ceiling 0. ⭐ v3 adds a THIRD leaf, so the scan covers three | §8 step 7: scan before and after; **STOP for the chair** on any identity. The lane never mints |
| ⭐ Prose-numerics (line-addressed) | **NO** | zero — this packet **MODIFIES no `src/` file** |
| ⭐ Wiring census — BOTH arms | ⛔ **YES, ON THE PRODUCER ARM — A DEFERRED ROW** | **The SHA-MAP arm: NO** — zero; no stamped file is modified, and `stamp.files` / `stamp.candidateLeaves` key on `src/domain/display/stateProse/` paths this packet does not touch. ⛔ **THE PRODUCER-COUNT ARM, WHICH THE OLD ROW WAS SILENT ON:** `docs/content/wiring-census.json` — **a named INTERIOR RED, delta only.** `stamp.producerIndexFiles` **+3**: this member's new `.js` leaves under the two counted roots, named one by one — `src/domain/edit/recordInvariants.js` · `src/domain/edit/recordInvariantMeta.js` · `src/domain/edit/recordInvariantFlags.js`. **Re-measured from this packet's own CREATE rows at `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`.** ⛔ `producerIndexFiles` **NAMES NOTHING — IT IS A NUMBER** (**1185** at `64b75240c`, read from the stamp at this pre-proof; 1172 at `bdbf7c89c` — an as-of measurement, never a pin, and the DELTA `+3` is what this packet asserts), counting every `.js` under `src/generators/**` plus every `.js` under `src/domain/**`; so the answer is a DELTA, and "the register does not name mine" would be a category error. `stamp.files` (**7** entries at this tip) and `stamp.candidateLeaves` (a path→sha MAP, not a list) **UNMOVED** — both key on `src/domain/display/stateProse/` paths this member does not touch. `totals.*` **UNMOVED** — this member produces no pool, variant or relation. ⛔ **Regenerated WHOLE at the train's terminal, BY THE CHAIR** — a member never runs `node scripts/wiring-census.mjs`, because N lanes each regenerating a global COUNT produce N conflicting blobs. ⛔ **The path is in NEITHER §7, NOR the `changeManifest`, NOR `checks`** — naming it reserves the estate's most contended path against every sibling. | terminal, by the chair |
| Writer-reach · edge-shared · decision-fork · bundle ceilings | **NO** | zero — no record key is written, no chunk delta; the two closure MEMBERSHIPS re-measured by import at T16-7 |
| ⭐⛔ **Voice mechanics (Tier 2, `src/data` + `src/domain`)** | **YES — THREE NEW SCANNED FILES, EACH HELD AT ZERO** | `tests/copy/voiceMechanics.test.js` walks every non-test `.js` under `src/data` and `src/domain`, counts em dashes and bangs inside STRING LITERALS by AST, and asserts per-file equality with `tests/copy/.voice-mechanics-baseline.json`; a file with no row is held at 0 and the total budget (`EM_BUDGET = 670`, `BANG_BUDGET = 15`) is shrink-only. **PREDICTED DELTA: `em +0 · bang +0` on all three leaves**, which is a CONTRACT of §6 (the flag table is keyed by band key) and of §7 (no em dash in any literal), not a hope. ⚠ The food ladder's famine label carries an em dash; `src/data/bandLadders.js` holds it under that walker's `AUTHORED_VOCABULARY_ALLOWLIST` and this packet does not (T16-5) | none — zero delta by construction |
| ⭐⛔ **Tuning register, P2 and P3** | **YES — ONE NEW `src/domain` LOGIC LEAF, HELD AT ZERO IN BOTH** | `scripts/lib/tuning-inventory.mjs` + `tests/lint/tuningRegister.walker.test.js`: P2 counts module-top-level `const UPPER_SNAKE = <numeric>;` and P3 counts bare DECIMAL literals over `TREES_P2P3 = ['src/domain','src/generators']`, both SHRINK-ONLY with **new files at zero** (the arms' own words). **PREDICTED DELTA: `P2 +0 · P3 +0`**, which §6's integer form of the rounding rule is what buys. ⚠ A `0.5` anywhere in these leaves reds it; P3 does not count integers, so `d + 1` is free (T16-5) | none — zero delta by construction |
| ⭐⛔ **Prose numerics** (`tests/lint/proseNumerics.test.js`) | **CORRECTED — THE OLD ROW ANSWERED ONLY THE LINE-ADDRESSED ARM** | The walker scans **all of `src/`**, not only modified files (`walkSourceFiles(join(ROOT,'src'))`), and its per-file baseline holds a new file at 0 against `REVIEWED_CATEGORY_CEILINGS` (`floatInterpolation 147 · percentToken 54 · multiplier 8 · twoDecimalScore 9 · pushIndirection 0`). **PREDICTED DELTA: `+0` in every category**, bought by §6's rule that a `message` renders no number. ⛔ **The LINE-ADDRESSED arm is still zero for the separate reason the old row gave** — this packet MODIFIES no `src/` file, so no baselined row moves beneath an insertion (T16-10) | none — zero delta by construction |
| ⭐ **Domain `any`-cast baseline** | **YES — THREE NEW `src/domain` FILES, HELD AT ZERO** | `tests/lint/domainAnyCastBaseline.test.js` over `tests/lint/.domain-any-baseline.json`: per-file shrink-only, *"no file exceeds its baseline (new files get 0)"*. **PREDICTED DELTA: `+0`** — no `/** @type {any} */` cast in any leaf; the strict domain config is what makes that affordable | none — zero delta by construction |
| ⭐ **Ruin-filter roster** | **NO — EXEMPT BY CONSTRUCTION, DECLARED RATHER THAN DISCOVERED** | `tests/lint/ruinFilterRoster.walker.test.js` enrols any `src/domain` file whose `codeOnly(src)` matches `/\.institutions\b/`. `V-DEFENSE-INST` relates `defenseProfile.institutions` and `institutions`, but it reads them through `CHECK_META.paths` STRINGS, and the walker's discovery scan blanks string and comment contents by design (its own header: *"a `.institutions` spelled only in a comment or inside a quoted data-table path is not an execution and must not enrol"*). **PREDICTED: this packet adds NO row to the discovery set and NO row to `RUIN_AGNOSTIC_EXEMPT`.** ⚠ If the implementer writes the read as a property chain instead, the leaf enrols and the packet owes an exempt row — that is a STOP, not a fix (T16-5) | none — declared EXEMPT-by-construction |
| ⭐ **Entropy root census** | **NO** | `tests/lint/entropyRootCensus.walker.test.js` pins the READ SITES (`READ_SITES` — 23 rows) and the `createPRNG(` site counts. Measured at `64b75240c`: **36** sites under `src/domain`, **47** under `src/` whole, **1** under `src/domain/edit` (`pools.js`). **PREDICTED DELTA: `+0` on both arms** — §6 forbids a PRNG and these leaves read no world root (T16-10) | none |
| ⭐ **Goods identity surface (WEAVE ST-2)** | **NO** | `tests/build/vendorPdfLazy.test.js`'s ST-2 law governs `src/data/goods/identity.js` and `src/domain/goods.schema.js`. These leaves import neither, and import nothing under `src/data` but `bandLadders.js` (T16-10) | none |
| ⭐ **Seed-loop totality** | **NO, AND THE CURE IS IN §7** | `tests/lint/seedLoopTotality.walker.test.js` scans `SCAN_ROOTS = ['tests']` WHOLE, so `tests/domain/recordInvariants.test.js` is governed. **PREDICTED DELTA: `+0`** — A1 and A3 COLLECT into a row array and assert ONCE, and no loop header names a seed | none |

## 8. Ordered coding sequence

0. Dispatch and seal. **Re-run the live-edge scan** and confirm EM-R0d is LANDED with its **six** symbols
   present (the four ladder symbols plus `FOOD_SECURITY_BANDS` and `LEGITIMACY_BANDS`), and EM-R0a LANDED
   with `RECORD_CLASSES` and a `CONSISTENCY_GROUPS` still carrying `history-age`. ⭐ All eight were
   measured PRESENT at `64b75240c` (T16-3); if any has moved, STOP.
1. **Capture the baseline, RED FIRST.** Run the observed-shape reader scan in report mode and record
   the three new leaves' absence. Record the live lighting tuple from a throwaway probe.
2. Add the failing arms for A1–A8 against modules that do not exist yet.
3. Create `src/domain/edit/recordInvariantMeta.js` — §5.4's table transcribed, `CROSS_KEY_CHECKS`
   derived. ⭐ **Build this leaf FIRST**: A2's totality arm is what keeps the other two honest.
4. Create `src/domain/edit/recordInvariantFlags.js` — the table transcribed from A6's own measurement.
5. Create `src/domain/edit/recordInvariants.js` — the checks in `CHECK_IDS` order, the four band
   checks calling the imported ladders, every violation carrying `kind` and `keys`.
6. No wiring. ⛔ **Import this module from nothing.** EM-R0c is the first importer.
7. Run focused verification (§10).
8. ⭐ **THE REGISTER STEP.** Re-run the observed-shape scan. Any new finding identity on any leaf is a
   **STOP for the chair** with the identities quoted — do not mint, do not rename a variable to dodge
   the heuristic, do not widen the inventory.
9. Write the completion receipt.

## 9. Acceptance matrix

| ID | Case | Fixture/input | Required observation | Test home |
|---|---|---|---|---|
| A1 | ⭐ THE CONTROL, AND THE PIN BOTH WAYS | the stride `__STRIDE_RULE__`, generated plainly | `recordInvariants` returns `[]` on `__STRIDE_N_MINUS_1__` rows; `town\|germanic\|mountain\|mountain_pass\|civilized\|golden-master-v3` (corpus index **510**) returns exactly the pinned `V-SUMMARY-DEPS` with its exact message. **The violating-row SET equals `KNOWN_VIOLATIONS`, both directions**, so curing the generator REDS this arm and FIX-G1 must remove the pin. ⛔ Not a skip, not a filter, not a widened tolerance. ⚠ **THE CARDINALITY IS A MARKED PLACEHOLDER** until §12 question 3 is ruled; the SHAPE — "all clean but the one pin, set-equal both ways" — is what this arm asserts and it was EXECUTED at `141a1d775` on the full 525 (1 of 525, and it is the pin) and on the recommended stride (1 of 65). ⭐ RE-MEASURED at `64b75240c`: the corpus is still **525** rows, index **510** still keys `town|germanic|mountain|mountain_pass|civilized|golden-master-v3`, and `rows.filter((_, i) => i % 8 === 6)` still yields **65** rows carrying index 510 (T16-8). `tests/helpers/goldenMasterCorpus.js` did not move in the window and still exports `goldenCorpus` and `keyOf` and nothing else. ⛔ A stride that MISSES index 510 makes this arm VACUOUS. ⚠ **CORRECTED AT THIS PRE-PROOF:** `tests/lint/contractTestAntiVacuity.walker.test.js` governs `tests/security/**`, `tests/**/*.contract.test.*` and `tests/lint/*.test.js` — **NOT `tests/domain/*.test.js`** (measured, T16-11). No walker would catch a vacuous stride here, which is precisely why §12 question 3 must be ruled rather than left to the implementer | `tests/domain/recordInvariants.test.js` |
| A2 | ⭐⭐ `CHECK_META` IS TOTAL, ITS KEYS RESOLVE, AND A VIOLATION CARRIES THEM | `CHECK_IDS`, `CHECK_META`, EM-R0a's `RECORD_CLASSES`, the stride | (i) every id in `CHECK_IDS` has a `CHECK_META` row and every row names a declared id — **both directions**; (ii) every key any row names is a key `RECORD_CLASSES` classes, and **every `paths` entry heads at a key its own `keys[]` carries**; (iii) `CROSS_KEY_CHECKS` is exactly the rows with no `arms` and >1 key, and equals the SEVEN of §5.4 — ⛔ asserted against the literal seven ids, so a hand-edit that adds an eighth reds; (iv) every violation the stride produces carries `kind` and non-empty `keys`, and `V-FLAGVEC`'s carries **one** key, the arm's | `tests/domain/recordInvariants.test.js` |
| A3 | ⭐ GUARD THE GUARD | for each of the 33 checks, the smallest mutation of a real record | each mutation returns **exactly that check's id and no other**, each step clearing its check's own tolerance. ⚠ `V-FLAGVEC`'s foodSecurity arm needs an `Import-Dependent` carrier. ⭐ The three duplication mutants are measured isolating (convict 63/63, trip nothing else) | `tests/domain/recordInvariants.test.js` |
| A4 | ⭐ NO THRESHOLD, NO ENVELOPE | the module's own source text | the four band checks call `legitimacyBandOf` / `readinessBandOf` / `foodSecurityBandOf` / `severityBand`, and `recordInvariants.js` contains no numeric literal outside its tolerance constants | `tests/domain/recordInvariants.test.js` |
| A5 | ⭐ THE ROUNDING RULE | constructed records at `deficitPct = 15` and `= 5` | both adjacent labels pass at each cut; a label two rungs away REDS. §6's six-row table, executed | `tests/domain/recordInvariants.test.js` |
| A6 | THE FLAG DATA IS DECLARED, NOT DERIVED | the 63-row stride | the shipped `FLAGS_EVER_TRUE` covers every label the stride shows, and a flag DERIVED from the published `deficitPct` is shown to disagree with the record on the `(15,20]` rows | `tests/domain/recordInvariants.test.js` |
| A7 | ⭐ ABSENCE, UNSEEN LABEL, PURITY, IDEMPOTENCY, **AND THE DUPLICATION PAIRS** | a record with `economicViability`, `isolationSupport` and `activeConditions` deleted; a record whose `foodSecurity.label` is unknown; one real record; and each duplication pair with ONE side deleted | `[]` for the first two; the record deep-equal to a pre-call clone; two calls equal; ⭐ **each duplication check ABSTAINS when either side is absent** — it may never convict a partial record | `tests/domain/recordInvariants.test.js` |
| A8 | THE LAYERING BOUNDARY HOLDS | the live `src/domain` → `src/generators` edge scan | the live set equals `BASELINE_EDGES` (RE-MEASURED at `64b75240c`: **4 importing files / 5 specifiers** — `checkDraftEdit.js`, `neighbourBackLink.js`, `institutionLifecycle.js`, `resourceDynamicsKernel.js`×2; T16-4); none of the three leaves appears | `tests/build/domainGeneratorsBoundary.test.js` ⛔ **RUN, NOT EDITED — so it lands NO `it` and contributes NOTHING to the lighting delta** (this is why the delta is `titles +7`, not `+8`). ⚠ The file itself MOVED in the J-T1 window and now holds FOUR `it`s (it held three); the delta is unaffected because an UNEDITED file contributes nothing whatever its own count (T16-4) |

```text
CANNOT-CATCH:
1.  Whether `readiness.score` is a function of the six defence scores. Not refuted over 86 distinct
    tuples, but no exact relation was found, so nothing may convict. -> EM-R7's corpus ratchet.
2.  Whether a THIRD duplicated leaf exists that the 525-row corpus never separates. The three were
    found by inspecting two objects the merge happened to mix; the sweep was not exhaustive.
3.  A DM edit outside the eight-edit corpus. The runtime guard is what covers it, by design.
4.  ~~Whether the `history` group is preferable to `V-HISTORY-AGE`.~~ ⭐ **CLOSED by the chair at
    ODQ §934.47 addendum 38 ruling 1: BOTH.** The check detects here; the group prevents, at EM-R0a.
7.  ⭐ **NEW at the pre-proof — TWO OF THE READINESS LADDER'S FIVE CUTS ARE NOT EXERCISED BY THE
    CORPUS.** `V-BAND-READINESS` holds 525/525 and the six observed label ranges do not overlap
    (P-4), but the corpus's 41 distinct readiness scores carry an **11-wide hole at 34..44** that
    swallows the declared `38` cut whole, and the `24` cut is witnessed by a single score (24) with
    nothing at 21–23 beneath it. So A1's green proves the ladder AGREES WITH THE RECORD on every
    score the corpus draws; it does NOT prove the 38 cut is at 38, and it would not distinguish any
    cut in `(33, 45]`. -> **EM-R0d's pre-proof owns the ladder's cuts** and EM-R7's corpus ratchet
    records the witnessed set. ⛔ Nobody may read this arm as proof of the cut positions.
8.  ⭐ **NEW at the 09-20 pre-proof — the stride's cardinality.** Until §12 question 3 is ruled, A1's row
    count is a placeholder; the arm's SHAPE is executed and holds (P-7).
5.  The escalation itself. This packet ships the metadata; EM-R0c walks the ladder and EM-R7 proves
    the family reaches zero escalations.
6.  ⛔ A VACUOUS SPLIT FALSIFIER. The first draft of V3-3d mutated R0 at BOTH paths, so both landed
    on the record's own value and the arm printed SPLIT=0 while proving nothing. The construction in
    §6 and V3-3d is the corrected one; any re-derivation must move ONE path and keep the other.
```

## 10. Verification commands

```sh
npx eslint src/domain/edit/recordInvariants.js src/domain/edit/recordInvariantMeta.js \
  src/domain/edit/recordInvariantFlags.js tests/domain/recordInvariants.test.js
npm run typecheck:ratchet          # tsc -p tsconfig.full.json, via scripts/check-full-typecheck.mjs
npm run typecheck:domain:strict    # tsc -p tsconfig.domain-strict.json, via scripts/check-domain-strict.mjs

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/domain/recordInvariants.test.js --maxWorkers=2
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/build/domainGeneratorsBoundary.test.js --maxWorkers=2
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/property/generatorGoldenMaster.test.js \
  tests/property/dossierProseManifest.test.js --maxWorkers=2

node scripts/check-observed-shape-readers.mjs --report   # report mode NEVER writes the baseline
node scripts/implementation-packets.mjs validate
npm run check:packet -- EM-R0b
npm run check:tail
```

⭐⭐ **THE GOVERNING WALKER FILES ARE SEALED `checks` ROWS AS OF VERSION 8 (§P2 row 19: "a sealed `checks`
array names files, never the `tests/lint` directory"; `scripts/governing-tests.mjs` has not landed, so
they are COMPUTED the way that row prescribes — `git grep -l -F` per changed basename plus every walker
governing a directory this packet CREATEs into).** Seven files, in two arrays, one directory each:

```sh
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/tuningRegister.walker.test.js tests/lint/proseNumerics.test.js \
  tests/lint/domainAnyCastBaseline.test.js tests/lint/ruinFilterRoster.walker.test.js \
  tests/lint/entropyRootCensus.walker.test.js tests/lint/seedLoopTotality.walker.test.js
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/copy/voiceMechanics.test.js
```

⛔ **TWO WALKERS ARE DELIBERATELY ABSENT FROM THE SEALED ARRAY AND RUN ONLY IN §10**, for the same
reason and by the same law: `tests/lint/sovereigntyLightingContract.walker.test.js` (excluded by §P2 row
19 in terms — it reds by design under a train) and `tests/lint/proseWiringCensus.walker.test.js` (whose
producer-count arm this packet's §7 DECLARES as an interior red, regenerated at the terminal by the
chair). A guaranteed-red suite inside `checks` makes `check:packet` unpassable.

⭐⭐ **AND `tests/lint` WHOLE — ADDED AT THE PRE-PROOF (the chair's addendum of 2026-09-20, after runs
17, 18 and 19: "a CREATE or RENAME of a test file under `tests/<dir>` opts into EVERY walker that
governs `tests/<dir>` … run that directory WHOLE once in your instruments; `tests/lint` whole
ALWAYS").** This packet CREATEs `tests/domain/recordInvariants.test.js`, and **nothing above reaches
the walkers that will read it**: `npm run check` runs `test:ratchet` (a counting script) and `lint`
(eslint), never the `tests/lint` vitest suite — measured from `package.json` at `141a1d775`. Runs 17,
18 and 19 were one family precisely because a lane's new test file was read by a walker the lane
never ran.

```sh
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 tests/lint
```

⚠ **THIS IS A §10 VERIFICATION COMMAND, NOT A SEALED `checks` ROW, AND THE DISTINCTION IS
DELIBERATE.** `tests/lint/sovereigntyLightingContract.walker.test.js` is EXPECTED to red on its
interior arm — the packet adds a credited file and the census is an exact tuple, re-derived at the
terminal by the chair (§7). A guaranteed-red suite inside the sealed `checks` would fail the packet's
own gate. The lane RUNS it, QUOTES the census red with its printed tuple, and confirms **every other
`tests/lint` arm is green** — that is what proves no OTHER walker was surprised by the new file.

⚠ **`gate-mutex.sh` GIVES UP AFTER 40 POLLS AND EXITS 3** (`GAVE UP`) — ⛔ **never 0**; the chair retracted the "exits 0" claim on 2026-09-20 with three versions of the script checked. A gate line with no printed test count
DID NOT RUN. The env prefix above is the long-poll spelling; the receipt must print each run's count.
⛔ **No command above is a generator**, so no declared path is re-stamped mid-chain.

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md`, stop if:

- the dispatch seal is missing, invalid, or belongs to another worktree state;
- resume reports authority, HEAD, foreign-work or receipt-integrity drift;
- **STOP-1 — a second argument.** Any pressure to make `recordInvariants` take a corpus, a second
  record or an observation set;
- **STOP-2 — EM-R0d is not LANDED, or its four symbols are spelled otherwise than §5.2 names them**;
- **STOP-3 — the food ladder's cuts or its OPEN-below operator change in EM-R0d.** §6's rounding rule
  is written against `>` at 40/15/5; a closed-below ladder needs the admissible set re-derived;
- **STOP-4** — the observed-shape scan gives any leaf a finding identity (§8 step 8);
- **STOP-5** — either golden moves, or `tests/build/domainGeneratorsBoundary.test.js` reds;
- **STOP-6** — any check ships without a planted mutant that isolates it;
- ⭐ **STOP-7 — any check ships without a `CHECK_META` row, or a row names a key EM-R0a does not
  class.** A2 is the red. ⛔ Never add the key to `CHECK_META` to make the arm pass — a key outside
  the register means either the register or the check is wrong, and both are the chair's;
- ⭐ **STOP-8 — `CROSS_KEY_CHECKS` is hand-listed rather than derived**, or its membership stops
  equalling §5.4's seven. Either is a silent change to EM-R0c's escalation;
- ⭐⛔ **STOP-9 — ANY OF THE FOUR ZERO-HELD INSTRUMENTS MOVES.** `tests/copy/voiceMechanics.test.js`
  (em/bang), `tests/lint/tuningRegister.walker.test.js` (P2/P3), `tests/lint/proseNumerics.test.js` and
  `tests/lint/domainAnyCastBaseline.test.js` each hold a new file at ZERO and §7 predicts `+0` on every
  one. A non-zero figure is a STOP for the chair with the arm's own message quoted — ⛔ **never a
  baseline row, never an allowlist entry, never a `--update`**, all four of which are the estate's
  documented ways to launder exactly this class;
- ⭐⛔ **STOP-10 — A LEAF ENROLS IN THE RUIN-FILTER DISCOVERY SET.** If
  `tests/lint/ruinFilterRoster.walker.test.js` names any of the three leaves, the implementer wrote
  `V-DEFENSE-INST`'s read as a property chain instead of through `CHECK_META.paths`. Re-write the read;
  ⛔ do not add a `RUIN_AGNOSTIC_EXEMPT` row, which is the chair's;
- ⭐ **STOP-11 — EM-R0c's two expectations stop resolving.** `export function recordInvariants` and the
  re-export line `export { CHECK_META, CHECK_IDS, CROSS_KEY_CHECKS } from './recordInvariantMeta.js';`
  are quoted BYTE FOR BYTE in EM-R0c's `requiredSymbols`; a reformat of either line reds that packet's
  validation at the composed tip (T16-12).

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into the
next wave. ⛔ **This packet may not cure the generator defect it pins**, and it may not reconcile the
food card's two ladders — that is the owner's decision point (ODQ §934.57).

## 12. Completion receipt

- Base SHA · dispatch bundle and seal identity · final commit or working-tree state:
- Exact changed files and effective-line deltas (four files):
- Acceptance cases; focused commands, exits and counts (⚠ each with its own printed test count):
- A1's row count and wall-clock; A2's totality and key-membership output; A3's per-check verdict
  table for all 33; A5's boundary table:
- Observed-shape reader scan, before and after, over THREE leaves:
- Lighting census tuple, measured in a throwaway probe; the delta restated as
  ⭐ `files +1 · credited +1 · parked +0 · suiteTitles +1 · titles +7` with NO absolute — and the
  file's `it` count QUOTED, so `+7` is a measurement and not a promise:
- Sealed per-step receipt and exact-state resume status; both typecheck configurations:
- Dormancy/golden result; bundle/first-paint result (predicted: worker +0, eager +0).
  ⭐ **SAY HOW `+0` IS ESTABLISHED** (§P2 row 11's second amendment, ODQ §934.47 addendum 39): it is
  established STRUCTURALLY, by a probe that imports the exported sets and reads no `dist` (so it
  CANNOT skip): `EAGER_FIRST_PAINT_MODULES` holds **0** `src/domain/edit/**` members and the generation
  worker entry's static closure holds **0**, while NOTHING IMPORTS THESE THREE LEAVES — EM-R0c is the
  first importer and lands later. ⛔ **THE OLD REASON IS RETIRED AND MUST NOT BE RE-QUOTED:** *"nothing
  anywhere in `src/` imports that directory"* was true at `141a1d775` and is FALSE at `64b75240c`
  (`src/store/editSlice.js`, `src/store/settlementRederiveAction.js`,
  `src/components/edit/CardEditorDialog.jsx` — T16-6). The delta is unchanged; only the argument for it is. ⛔ **A `skipIf(!requireDistRead)` byte arm that SKIPPED IS
  NOT A PASS** and may not be quoted as one; if such an arm runs, quote its executed figure and the
  dist it read, or say plainly that it skipped:
- Generated artifacts: `NONE` · Deviations: `NONE | STOP` · Judgment calls: `NONE`

## ⭐ §12 QUESTIONS FOR THE CHAIR (re-cut at the EM-T16 pre-proof; §5.2, §6, §7 and §9 cite this section by number)

**1. ⛔ THE FAMINE LABEL CARRIES AN EM DASH. KEY THE FLAG TABLE BY BAND KEY, OR BUY AN ALLOWLIST ROW?**
MEASURED (T16-5): `tests/copy/voiceMechanics.test.js` counts em dashes in every string literal of every
non-test `.js` under `src/data` and `src/domain`, asserts per-file equality with its baseline, and holds a
file with no row at ZERO; one of the six food labels is `Deficit — Active Famine`.
`src/data/bandLadders.js` carries it lawfully under that walker's exact-string
`AUTHORED_VOCABULARY_ALLOWLIST`; a new leaf that transcribes the label does not.
**RECOMMENDED: key `FLAGS_EVER_TRUE` by the ladder's BAND KEY** and resolve a record's label through the
imported `FOOD_SECURITY_BANDS` / `LEGITIMACY_BANDS` (§6, applied on this copy). It spells no label in any
leaf, touches no instrument file, adds two `requiredSymbols` rows on a file this packet already imports,
and leaves the declared table's content identical to the row. The alternative is an exact-string allowlist
row in `tests/copy/voiceMechanics.test.js` — a TEST manifest row on a contended instrument, whose own arms
demand a reason naming a producer line, for a label this packet never needs to spell.

**2. ⛔ THE HALF-UNIT `0.5` IS A TUNING-REGISTER RED. INTEGER FORM, OR A REGISTERED P1 TABLE?**
MEASURED (T16-5): `tests/lint/tuningRegister.walker.test.js` holds a new `src/domain` file at **P3 = 0**
(bare decimals) and **P2 = 0** (module-top-level `UPPER_SNAKE` numerics), both shrink-only, in the arms'
own words. §6's `d ± 0.5` reds either way it is written.
**RECOMMENDED: the INTEGER form written into §6 on this copy** — proved equivalent on **20,402** triples
with **0** differences (T16-9), reproducing §6's six-row table exactly, and resting on two measured
preconditions (all four cuts integral; both published percentages `Math.round`ed at generation). The
alternative is to declare the tolerance as a P1 `*_TUNING` table and register it by id in
`tests/lint/.tuning-register.json` — the pen's file, and a rounding artefact is not a simulation dial.

**3. ⛔⛔ WHAT IS "THE 63-ROW STRIDE"? (carried from the 09-20 pre-proof, re-executed here.)** It is named
by A1, A3, A6 and A7, and A1's "62 rows clean" is its cardinality — but nothing in the tree defines it.
`tests/helpers/goldenMasterCorpus.js` exports `goldenCorpus` and `keyOf` and nothing else, no stride helper
exists anywhere under `tests/`, and no arithmetic stride over 525 yields 63. ⚠ **And no walker would catch
a vacuous one**: `contractTestAntiVacuity` governs `tests/security`, `*.contract.test.*` and
`tests/lint/*.test.js`, not `tests/domain` (T16-11).
**RECOMMENDED: `rows.filter((_, i) => i % 8 === 6)` — 65 rows, carrying corpus index 510 by construction**,
re-measured true at `64b75240c` (T16-8). One line of source, reproducible from the tree alone. The only
other rule this lane would defend is the FULL 525.

**4. THE SEALED `checks` ARRAY IS WIDENED TO THE SEVEN GOVERNING WALKER FILES — CONFIRM.** §P2 row 19 says
a sealed array names the governing walker FILES, computed; version 7's array named none of them, so four
instruments that hold this member at zero would have been met for the first time at the landing.
**RECOMMENDED: seal the seven of §10** (`tuningRegister`, `proseNumerics`, `domainAnyCastBaseline`,
`ruinFilterRoster`, `entropyRootCensus`, `seedLoopTotality`, `voiceMechanics`) **and keep the lighting and
wiring-census walkers OUT**, both of which red by design and would make `check:packet` unpassable.

**Noticed, not touched — each slotted, nothing deferred (the owner's law of 2026-09-19 18:10):**

1. ⭐ `src/generators/power/rulingStructure.js` `_provDefLabel` is a **SIXTH, undeclared spelling of
   the readiness vocabulary** and omits `Fortress`. Re-confirmed at this tip: that file contains
   **0 lines naming `readiness`** (V3-4 D3). → **EM-R0d's second version**, already slotted.
2. `V-FOODSEC-FLAGS`'s own map covers **four of the six** food labels and silently ABSTAINS on
   `Import-Dependent` and `Deficit — Active Famine`, where `V-FLAGVEC` is the sole check. → written
   down in §6; no separate slot.
3. The `V-EVIDENCE-*` checks' TRUE carrier counts range from **242 to 525 of 525**. → **EM-R7's
   corpus ratchet**: it records the carrier count beside each check.
4. `judgments[id=confidence_and_provenance].evidence[path=simulationTrace]` carries an `evidence`
   value of **`undefined` on 525 of 525 rows** (V3-4 D7). No shipped check reads it. → EM-R0c's
   compile, which owns the receipt finding; **ANSWERED there** (the receipt is merged, never
   recomputed).
5. ⭐ **NEW at v3 — the duplication SWEEP was not exhaustive.** Three duplicated leaves were found by
   inspecting the two objects EM-R0c's merge happened to mix. A whole-record sweep for "one fact at
   two paths" over the 525-row corpus is a bounded, mechanical measurement nobody has run. →
   **SLOT: EM-R7's corpus ratchet carries it** — it already walks every record path, and a
   duplication census is one more pass over the same walk.
6. ⭐ **NEW at v3 — `readiness.score` is not refuted as a function of the six defence scores** (86
   distinct tuples, 0 ambiguous) but no exact relation was found, so no check may ship. If a relation
   exists it is a real invariant the guard is missing. → **SLOT: EM-R7's ratchet records the
   observation count**, so the evidence either strengthens into a check or is recorded as
   coincidence.
7. ⭐⭐ **NEW at the pre-proof — THE READINESS LADDER'S `38` CUT IS UNWITNESSED BY THE CORPUS.** The
   525 rows carry 41 distinct readiness scores spanning 11..81 with a hole at **34..44**, so no row
   exercises the `38` boundary and the `24` cut is witnessed only from above (a single score at 24,
   nothing at 21–23). `V-BAND-READINESS` is still EXACT and still 525/525 — this bounds what its
   green PROVES, not whether it passes. → **SLOT: EM-R0d's pre-proof**, which owns the four ladders,
   states each cut's witnessed/unwitnessed status beside it; **EM-R7's corpus ratchet** records the
   witnessed score set so a corpus change that starts exercising 34..44 is visible. §9 CANNOT-CATCH 7.
8. ⭐✅ **DISCHARGED AT THE EM-T16 PRE-PROOF — the two prose headers are EM-R0a's, and EM-R0a HAS
   LANDED.** `src/domain/edit/` now holds ten landed leaves at `64b75240c` (T16-2), so the slot named below
   has either been taken by its owner or is a live row against the landed tree; either way it is no longer
   this packet's to carry, and this packet still CREATEs nothing that changes the two headers' truth value.
   The original row, kept as the record: **TWO TEST HEADERS ASSERT IN PROSE THAT `src/domain/edit/**` DOES
   NOT EXIST.** `tests/store/decreeRegistryPersistence.test.js:17` ("which does not exist at this
   commit") and `tests/lib/editTravel.test.js:15`. Both are COMMENT-ONLY — neither carries a runtime
   assertion, so **nothing reds** (measured) — but both become false the moment the first packet
   CREATEs under that directory. Under the family order that is **EM-R0a**, not this packet. →
   **SLOT: EM-R0a's pre-proof carries the two-line prose re-address**, since it lands first; if
   EM-A2a's `pools.js` lands earlier, it inherits the row.
9. ⭐✅ **ARRIVED — EM-R0d HAS LANDED and the second `bandLadders.js` basename is LIVE at
   `64b75240c`.** Both files exist: `src/data/bandLadders.js` (the ladders, read by three `src/generators`
   modules and six test files, measured T16-11) and `src/domain/compendium/bandLadders.js`. This packet's
   §5.2 spells the `src/data/` address explicitly, so no import here is ambiguous; the basename-addressing
   instruments are EM-R0d's row and it landed with them. The original row, kept as the record: **a SECOND
   `bandLadders.js` basename arrives with EM-R0d.**
   `src/domain/compendium/bandLadders.js` already exists (exporting `buildBandLadders`), is read by
   `scripts/generate-compendium-data.mjs`, `src/domain/compendium/searchIndex.js` and two test files,
   and carries **three rows in `scripts/.observed-shape-readers-baseline.json`**. EM-R0d creates
   `src/data/bandLadders.js`. The addresses are distinct so no import is ambiguous and this packet's
   §5.2 spells the `src/data/` address explicitly — but any instrument that addresses by BASENAME
   will now see two. → **SLOT: EM-R0d's pre-proof names the collision and states which instruments
   address by basename**; this packet's §8 step 8 scan is already red-first over its own three leaves.
