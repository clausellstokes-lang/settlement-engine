# `settlement-editor / EM-R0c` — THE MERGE: the consequence of an edit is the difference of two re-derivations, applied to the record by consistency group

- **Status:** `READY`
  ⚠ The status value above stands ALONE on its line, and it must: `parsePacketHeader`
  (`scripts/implementation-packets.mjs`) anchors the status row at END-OF-LINE
  (`/^\s*(?:-\s*)?\*\*Status:\*\*\s*`?([A-Za-z]+)`?\s*$/gmi`) and takes `status` only when
  EXACTLY ONE row matches. Every stamp lives here, on the CONTINUATION lines.
  ⭐ **READY-able at `6a0178b7b`** (FULL pre-proof, 2026-09-23 00:3x-01:5x EDT, train EM-T16
  stage 2, session cce01f87), **on ONE condition that is the train's own order**: EM-R0b version 8
  is READY in this tree and LANDS AT STAGE 1's FLIP. Its two symbols
  (`src/domain/edit/recordInvariants.js :: export function recordInvariants` and the re-export line)
  are the LAST two `_pendingRequiredSymbols` rows; the other six are DISCHARGED here, measured
  verbatim at this tip (EM-R0a and EM-R0f have LANDED). No premise is refuted; nothing is BLOCKED.
  ⛔ Version 5's condition named FOUR unlanded packets. Three of them (EM-R0a, EM-R0d, EM-R0f)
  landed on trains EM-T13 and EM-T14 and are measured here as LANDED FENCES, never as predictions
  (judgment 134).
- **Packet version:** `6`
  - ⭐⭐ **Version 6 is the FULL PRE-PROOF at train EM-T16's stage-1 placement tip `6a0178b7b`**
    (Opus pre-proof seat, 2026-09-23; the packet's previous pre-proof was 2026-09-20 at `141a1d775`,
    EIGHT trains ago, under the SECOND amendment). ⛔ **NO CONTRACT, ACCEPTANCE CASE, BUDGET FIGURE,
    CHANGE ROW, STOP CONDITION OR NON-GOAL MOVED.** What moved is every fact that rested on an
    unlanded sibling or on an absolute: (1) the **J-T1 window is NOT empty** (4 files, 293
    insertions, 13 deletions over 207 commits) and each of the four movements is a sibling's LANDING,
    re-measured here; (2) **SIX of the eight `_pendingRequiredSymbols` are DISCHARGED**, each
    re-measured verbatim with `grep -cF` at its landed address (judgment 151), and the two EM-R0b
    rows are named as ARRIVING at stage 1's flip; (3) **V1 and V2 are REFUTED AS WRITTEN and re-cut**
    to the narrower forms the tip supports; (4) the `268` / `220` / `172` / `1172` / `2653 383 2270
    25052 6680` absolutes are retired for measurements-with-as-of-marks or for DELTAS (§P2 row 11);
    (5) **FIVE instruments that did not govern `src/domain/edit/` when this packet was compiled now
    do**, each EXECUTED over this packet's own planted text and each measured at ZERO (§5 V17-V21,
    §7's CREATE rows, §10's two new sealed arrays); (6) the preamble row is re-stamped from the
    FOURTH amendment's hash to the SIXTH's, measured here; (7) EM-R6 LANDED and **re-aimed its §3b
    door away from this packet to the apply point**, while ordering the membership probe to STAY in
    this packet's `checks` as the standing guard, which §7 and §10 now say in EM-R6's own words.
  - ⭐ Version 5 is the DOCS repair only (ESTATE-REPAIR-4, 2026-09-20, read tip
    `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`): §10's account of `gate-mutex.sh` is corrected — it
    gives up after its poll budget and **exits 3** (`GAVE UP`), NOT 0. The chair retracted the
    "exits 0" claim on 2026-09-20 after checking three versions of the script; the 09-15 note that
    stated it was wrong. ⛔ **The operational instruction is UNCHANGED and still correct** — a gate
    line with no printed test count DID NOT RUN — so no command, budget, contract, acceptance case,
    required symbol or change row moved.
  - Version 4 is the DOCS repair only (ESTATE-REPAIR-3, 2026-09-20, read tip `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`),
    bringing this packet under `COMPILE-RULES.interim.md`: §7's row
    *"`docs/content/wiring-census.json` `stamp.files` | **NO** | keys on a MODIFIED `src/` file
    — none"* answered ONLY the sha-map arm and was SILENT on `stamp.producerIndexFiles`, while
    this manifest CREATEs TWO `.js` leaves under `src/domain/edit/`; it is replaced by the
    DEFERRED ROW with the measured `+2` (interim rule 1). ⭐ EM-R6 §3b's binding note, which
    named this packet, is RE-AIMED at the APPLY POINT in EM-R6 version 5 — **the membership
    probe in `checks` STAYS as the standing guard.** ⛔ **No contract, budget, acceptance case,
    required symbol, change row or non-goal moved.**
  - Version 3 is the DOCS repair only (ESTATE-REPAIR + ESTATE-REPAIR-2, 2026-09-20): the appended
    Status stamp moved off the Status row, one `TEST` row became `CREATE`, the `_pending` block
    moved to the sibling key, EM-R6's first-paint door was priced with an always-executing
    membership probe, and the `validate:packets` row joined `checks`. ⛔ **No contract, budget,
    acceptance case, required symbol or non-goal moved**; version 2 is in
    `pre-estate-repair-2026-09-20/`.
  > *What version 2 changed and why.* The pre-proof re-measured every verified fact at
  > `141a1d775` and found the J-T1 window EMPTY — 24 commits moved and not one touched a path this
  > packet names — so no figure drifted. Six things changed anyway, all from rulings and
  > measurements made AFTER the compile: (1) **STOP-4 is DISCHARGED** — ODQ §934.47 addendum 38
  > accepted EM-R0b version 3, which ships `CHECK_META` and re-exports it from `recordInvariants.js`
  > so this packet's compiled import is satisfied verbatim; V8 is rewritten and `CHECK_META` becomes
  > an owed `requiredSymbols` row. (2) **EM-R0f is CHARTERED**, not proposed (addendum 35 ruling 1).
  > (3) **G8 `history-age` now exists** (addendum 70), and §6's group row and §5's new V14 record
  > THE HISTORY-ORDER TRIAL the chair ordered — executed here over 693 trials. (4) ⛔ **§6's
  > write-base absolute is SCOPED to the top level**, because at depth it is false: 245 measured
  > counterexamples, every one lawful under §22.1. (5) ⛔ **§7's TEST row gains a seed-loop
  > construction rule** and §10 gains `tests/lint` WHOLE as an instrument — a new `tests/domain`
  > test file opts into six walkers that walk `tests/` whole, and one of them convicts the idiom §7
  > prescribed (executed, red-first). (6) The preamble hash is re-stamped to its second amendment.
- **Verified base:** `em-t16-r0c-2026-09-23` at `88515782a8001d5e10aff2becf8775b4f61f9936`
  > *Revalidation sentence for the chair:* re-measured at **`6a0178b7b`** (train EM-T16's stage-1
  > placement tip; `rev-parse --short HEAD` confirmed and `status --porcelain` EMPTY at the start and
  > at the end of the pre-proof, 2026-09-23). `git diff --stat 141a1d775 6a0178b7b` over every
  > change-manifest path and every `requiredSymbols` path, present and pending, returns FOUR FILES
  > over 207 commits: `src/data/economyFingerprint.js` +53 (EM-R0f's CREATE, LANDED on EM-T14),
  > `src/domain/edit/recordRegister.js` +190 (EM-R0a's CREATE, LANDED), and two movements in files
  > this packet only READS, `src/generators/generateSettlementPipeline.js` (1 line, EM-R1's pin
  > forwarding) and `tests/build/domainGeneratorsBoundary.test.js` (55+/6-, judgment 111's root fix
  > plus EM-R0f's re-baseline). All three CREATE targets ABSENT (`git ls-files` -> 0). All four
  > present `requiredSymbols` rows proved by `grep -cF` -> 1 at lines 77 / 76 / 60 / **70**
  > (`const BASELINE_EDGES` moved from :60 to :70; its `_note` is re-addressed, judgment 124). SIX
  > pending symbols DISCHARGED verbatim; the two EM-R0b rows arrive at stage 1's flip. The
  > domain->generators ratchet's live set re-read at FOUR files / FIVE specifiers, its cardinality
  > guard now `<= 5`. The eager first-paint closure re-measured BY IMPORTING `vite.config.js`'s
  > `EAGER_FIRST_PAINT_MODULES`: **270 as of `6a0178b7b`**, with 0 `src/domain/edit/**` members and
  > 0 EM-R6 leaves. The budget re-counted with eslint's own `max-lines` in process at 166 + 128 =
  > **294**, byte-identical to the compile's figures.
- **Last revalidated:** `88515782a8001d5e10aff2becf8775b4f61f9936` (the chair stamps the date and sha at promotion)
- **Depends on:** `EM-R0a` **LANDED** (`src/domain/edit/recordRegister.js`, 190 lines; its
  `CONSISTENCY_GROUPS` carries **SEVEN** entries at this tip, the seventh being `history-age`, root
  `history`, members `['age','founding.age']` -- V14's whole subject, re-read at `:175`-`:190`) ·
  `EM-R0d` **LANDED** (`src/data/bandLadders.js`, which EM-R0b reads; this packet names no symbol of
  it) · `EM-R0f` **LANDED** (`src/data/economyFingerprint.js`, 53 lines, ONE export
  `fingerprintPowerEconomyInput` at `:42`; `economyReconciliation.js` now IMPORTS it at `:22` and
  keeps no forwarding definition, so the moved text lives at exactly one address) · `EM-R6`
  **LANDED** (the `OrphanKind` vocabulary the delta carries; its §3b door was RE-AIMED away from this
  packet to the apply point and its probe ORDERED to stay -- §7) · ⭐ `EM-R0b` **version 8, READY in
  this tree and LANDING AT STAGE 1's FLIP** (`recordInvariants` **and the `CHECK_META` re-export
  line**) -- the packet's ONE open dependency, and the whole content of its conditional status.
- **Collision group:** `src/domain/edit/**` -- serialize with **EM-R0b** (`recordInvariants.js`,
  `recordInvariantMeta.js`, `recordInvariantFlags.js`), which lands FIRST at stage 1's flip.
  ⭐ **RE-MEASURED at `6a0178b7b` against every placed and waiting packet: NO FILE IS SHARED.** The
  two CREATE paths and the one test CREATE are claimed by nothing in `PACKET_MANIFEST.json` (226
  packets) and by nothing in the kit's `packets-waiting/`. Train EM-T16's stage 2 is PAIRWISE
  DISJOINT by path: **EM-R1b** takes `src/generators/pipeline.js`, `src/domain/edit/dmLayer.js`,
  `tests/lint/transientChooserRegistry.walker.test.js`, `tests/generators/pipelinePinnedMode.test.js`,
  `tests/property/dmLayerGoldenIsolation.test.js` and a `scripts/mutation-coverage-manifest.json` row;
  **EM-R3** takes `src/generators/power/economyReconciliation.js`,
  `src/generators/steps/powerEconomyReconcilePass.js`, `src/generators/steps/assembleSettlement.js`
  and `tests/generators/powerEconomyFreshness.test.js`. Stage 1 (EM-R2 v5, EM-R0b v8) and stage 3
  (EM-R4) intersect this packet's manifest NOWHERE. ⛔ On the two ROW-KEYED registers the train
  shares (`scripts/mutation-coverage-manifest.json` and `heldKeyWriterCensus`'s `DISCOVERY_EXEMPT`)
  **this packet holds NO row at all**, so no `rowKey` can collide (TOOL-27).
- **Commit authority:** edits only; the chair commits
- **Baseline posture:** measured. The inertness control (evidence E-1), the identity arm (E-3), the
  504-trial corpus (E-4), the emitted-vs-measured equivalence (E-2), the budget under eslint's own
  `max-lines` (E-9) and the import graph (E-8) were all executed at
  `32602dc607b7423838249cf57d73baf08feb047d`.
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: TO BE STAMPED BY THE CHAIR)
  > ⛔ **MEASURED AT THIS PRE-PROOF'S TIP `6a0178b7b`:**
  > `125c693214235a81bf4f2b38506b621859a35e0325ff2ea58d335b4e0681a99d`
  > (`shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md`) -- **the SIXTH AMENDMENT**,
  > landed as train EM-T13's first commit `7bc14e003`, with cite-by-symbol. Version 5's header row
  > cited `c9f33c8d...` (the FOURTH) and its continuation `16dfb96f...` (the SECOND); **both are
  > STALE and are retired here.** The hash moves with every amendment, which is why the header line
  > is the CHAIR'S STAMP and never a lane's figure (§P2 row 11, and the three laws of 2026-09-20).
  > ⛔ **The `COMPILE-RULES.interim.md` authority row that version 4 added is REMOVED**: the interim
  > rules landed as the amendments this packet is now measured against, and a header that still
  > points at the interim file points at a superseded law.

---

## 1. Reconciled authority

1. **The owner**, ODQ §934.36–§934.42: build the settlement editor now.
2. **THE PROMISE** — a seed is a starting world forever; lived history is immutable. This packet
   never merges and never recomputes HISTORY.
3. **Design §22.1** — the consequence of an edit is the DIFFERENCE between two re-derivations;
   **§22.2** (twelve items — the merge's law); **§22.3** (the receipt split by path; groups as
   `{ id, root, members }`; a collection under a cross-entry total is ATOMIC; cross-key checks ship);
   **§22.4** (the identity table: `npcs` by `id`, `institutions` and `factions` by `name`).
4. **The chair, 2026-09-19 20:16 (ODQ §934.60), the contract added from the pulse's data-loss
   defect:** `record′` is built FROM THE RECORD, and nothing present in the record and untouched by
   the edit may be absent from it. This packet carries it as an EXECUTED acceptance arm (A1), not a
   sentence: 0 record keys lost over 63 identity rows and over 504 edit trials (E-3, E-4).
5. **The charter's EM-R row for EM-R0c** and the amendments of 2026-09-19 18:30, 19:15, 19:29, 19:48,
   20:03, 20:16 and 20:34.
6. **The siblings, IMPORTED and never re-declared** -- and at this pre-proof FOUR OF FIVE ARE
   LANDED FENCES, re-read in the tree rather than predicted (judgment 134):
   **EM-R0a LANDED** (`src/domain/edit/recordRegister.js`, 190 lines, TWELVE frozen exports; ⭐ its
   `CONSISTENCY_GROUPS` carries **SEVEN** entries, `history-age` seventh with members
   `['age','founding.age']`; `economicState.incomeSources` and `powerStructure.factions` are BOTH
   `ATOMIC_COLLECTIONS` members AND `CROSS_ENTRY_TOTALS` rows, so §6's "a cross-entry total is ONE
   value" rule is bought by the ATOMIC import alone and this packet imports no second register),
   **EM-R0d LANDED** (the ladder tables in `src/data/bandLadders.js`; EM-R0b reads them, this packet
   names none), **EM-R0f LANDED** (`src/data/economyFingerprint.js`, ONE export at `:42`),
   **EM-R6 LANDED** (`OrphanKind`), and **EM-R0b version 8 READY, landing at stage 1's flip**
   (`recordInvariants`, and `CHECK_META` / `CHECK_IDS` / `CROSS_KEY_CHECKS` re-exported from
   `recordInvariants.js` on the line this packet's pending row quotes byte for byte).
7. **The three rulings this packet was re-cut against**, each read from the ledger:
   **ODQ §934.47 addendum 35** (this packet compiled and ACCEPTED; EM-R0f chartered; the suite is
   SEAM-FREE; the family's order is R0a · R0d · R0b v3 · R0f · R0c · R1–R5 · R6 · R7;
   `availableServices` and `economicState` CLOSED as measured non-groups; **the record's KEY ORDER
   is a contract of the merge** and EM-R7 gains an arm for it); **addendum 38** (`CHECK_META` ships
   and is total — **STOP-4 discharged**; `defenseProfile.scores` and `history.founding` are LAWFUL
   MIXED objects, no group owed; a `history` GROUP prevents the age split while `V-HISTORY-AGE`
   detects it); **addendum 70** (G8 declared with its two-member list; ⭐ *"G8 is the first group
   whose root also holds ORDER-BEARING collections, where EM-R0c's order arm and its group arm
   meet → **EM-R0c's pre-proof** carries one measured trial"* — discharged as **V14**).
8. **The code and the receipts** — `EM-R0c.evidence.md`: E-1…E-13 at the compile's `32602dc60`,
   P-1…P-12 re-executed at `141a1d775`, every row a quoted command.

**Resolved contradictions**

- *§22.2 item 4: "if [the order rule's second arm] never [fires], it is built as a counted diagnostic
  … not a silent branch."* → **It FIRES: 52 of 504 trials, five collections, 67 firings** (E-4.4).
  It ships as a real branch AND as a counted row in `delta.orderMoved`.
- *EM-R0a v2 slotted two findings to this compile.* → **Both answered.** (a) The generation receipt
  is MERGED, never recomputed; the ONE declared RECEIPT is recomputed, and taking `R1`'s digest
  instead is WRONG for the merged record in 31 of 315 trials (E-6). (b) The two ORDER-BEARING history
  collections need no declaration: each fires the second arm exactly once in 504 trials, which the
  general three-way order rule already carries (E-4.4).
- *The recon justified the chain partly because the batch path produced the `isSecure` conviction.*
  → With the GROUP rule on, **neither path is convicted**; the chain stands on §22.2 item 6's own
  ground — an edit is an event in order — and on the measured 18-leaf divergence (E-5).
- *The shared brief: "a new `src/domain/**` file moves the lighting census's file count."* → The
  PREAMBLE governs (§P2.1, and the chair's correction of 2026-09-19): the census counts TEST files;
  **a new `src/` leaf moves nothing** (E-11).
- *Every preamble SHA-256 any brief, compile or earlier version of this packet quotes.*
  → ⛔ ALL are stale, including the `16dfb96f…` the 2026-09-20 pre-proof itself measured.
  **RE-MEASURED at `6a0178b7b`:**
  `125c693214235a81bf4f2b38506b621859a35e0325ff2ea58d335b4e0681a99d` — the SIXTH AMENDMENT
  (`shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md`, T16-0). The hash moves with every
  amendment, which is why the header's line is the CHAIR'S STAMP and never a lane's figure.
- *The compile's own STOP-4 (V8: EM-R0b exports no per-check metadata).* → ⛔ **SUPERSEDED BY A
  RULING AND A LANDING.** ODQ §934.47 addendum 38 accepted EM-R0b version 3, whose §7 row states in
  terms: *"Re-export `CHECK_META`, `CHECK_IDS` and `CROSS_KEY_CHECKS` so EM-R0c's compiled import is
  satisfied verbatim."* The `Violation` typedef gains `keys`. **No re-cut of §6 is owed** and this
  packet still re-declares nothing (P-8).
- *The compile's §6: "⛔ A key present in `record` is present in the result, whatever the two
  re-derivations carry."* → ⛔ **TRUE AT THE TOP LEVEL, FALSE AT DEPTH, and it is scoped below.**
  Executed over 693 trials: **0 top-level record keys lost**, but **245 pure-object paths lost at
  depth** (`resourceAnalysis.imports.reasons.*` ×231, `economicState.foodSecurity.prosperityMod`
  ×12, `economicViability.metrics.foodBalance.*` ×2, `history.legacyAnnotations` ×1) — and **0 of
  the 245** sits under a node where `R0 ≡ R1`, so every one is §22.1 working correctly (the edit
  touched it; `R1` governs; `R1` does not carry it). The chair's own contract (ODQ §934.60) already
  says it right — *"nothing present in the record **and untouched by the edit**"* — and §6 now says
  it that way (P-9).

The implementer does not read other documents to reinterpret this packet.

---

## 2. Outcome

**Observable result:** a PURE module family under `src/domain/edit/` whose single public function,
`mergeConsequence(record, R0, R1, { edit, base }) → { record, delta }`, turns one edit's two
re-derivations into the new saved record and the delta card's three-valued account of what moved —
built FROM the record, by consistency group, with mirrors and the receipt recomputed and the town's
own invariants standing guard.

**Definition of done:** over the 63-row structured sample and all eight edits on all six tiers
(504 trials): every edit sticks, the settlement's name is kept, no caller record is mutated, **no
record key is lost**, no untouched HELD or AUTHORED subtree moves except the two the register exempts
by CLASS, and `recordInvariants` is clean with **ZERO escalations** — each arm executed, not argued.

**In scope**

1. **The pure tree merge** (§22.2 items 1, 2, 4, 5): compare and short-circuit at every NODE with
   ABSENT as a value; a declared CONSISTENCY GROUP moves WHOLE; a keyed collection merges by its
   DECLARED key; a keyless collection, a key-broken one, a collection under a cross-entry total and
   an array of scalars are ONE value; **nothing is ever merged by position**; ORDER is a reading
   merged three-way, its second arm counted.
2. **The post-passes and the delta** (§22.2 items 3, 7, 8, 12): MIRRORS recomputed after the merge,
   never merged; the one declared RECEIPT recomputed LAST over the merged record; HISTORY and
   AUTHORED untouched by CLASS; the runtime guard with its deterministic three-step escalation; the
   delta carrying, per moved reading, what it read BEFORE, what it reads NOW and what THE EDIT ALONE
   accounts for, plus `orderMoved`, `escalations` and EM-R6's `orphaned` entries.
3. **The prevention guard: THE CHAIN IS THE SEMANTICS** (§22.2 item 6). The module exports ONE merge
   function, it takes ONE edit, an ARRAY THROWS, and the chain is expressed in the RETURN
   (`delta.nextBase` is `R1`, which is the next edit's `R0`). A batch call is not merely discouraged:
   there is no exported symbol that accepts more than one edit, and the one that looks like it
   might refuses with a message that names the law.

**Explicit non-goals**

- The re-derivation itself (EM-R1–EM-R5). This packet takes `R0` and `R1` as ARGUMENTS and calls
  nothing that generates.
- Any caller. Nothing in `src/` imports these leaves when this packet lands; **EM-B2a** is the first,
  and the dynamic-import obligation and the byte price fall on its pre-proof (E-8).
- The corpus ratchet over the real seam — **EM-R7**, the family's terminal proof.
- Renormalising the power card's total (§22.2 item 8 — the OP's, not the merge's), the removal
  cascade (EM-R6), the newcomer's sixteen fields (§22.2 item 9), the trace partition (§22.2 item 11),
  and the delta CARD's wording (EM-D*). This packet supplies the numbers; it renders nothing.
- ⛔ Moving the economy fingerprint down a layer — **EM-R0f**, the split this compile takes (§3).

---

## 3. Hard scope budget

| Limit | This packet | Measured |
|---|---:|---|
| one behaviour family | the merge | — |
| new persisted record family | **0** | persists nothing; a pure function |
| named writer per state | **0** | writes no state; returns a value |
| feature flag | **0** | wave 1 is headless |
| user-facing surface | **0** | renders nothing |
| direct production consumers | **0 at landing** (EM-B2a is the first) | E-8(c): 0 importers in `src/` |
| new logic-bearing leaves ≤ 2 | **2** | E-9 |
| existing logic files modified ≤ 3 | **0** | `changeManifest` carries no MODIFY row |
| registration-only files touched ≤ 3 | **0** | §7 |
| handwritten files ≤ 12 | **3** (2 production + 1 test) | §7 |
| new/changed effective production lines ≤ 400 | **294** | E-9 |
| each new leaf ≤ 250 effective | `recordMergeTree.js` **166** · `mergeConsequence.js` **128** | E-9 |
| shared / hot-file delta ≤ 15 effective | **n/a** — this packet names no hot file and modifies none | §7 |
| acceptance cases ≤ 8 | **8** | §9 |

Counted with eslint's own rule, in process: `max-lines` with `{ skipBlankLines: true, skipComments:
true }` — the rule every ceiling in `eslint.config.js` uses.

### ⛔ 3.1 THE SPLIT THIS COMPILE PROPOSED -- **TAKEN, CHARTERED, BUILT AND LANDED**

**The merge itself fits in two leaves and did not need splitting.** One thing did not fit, and it
was not the merge: `recomputeReceipts` must call `fingerprintPowerEconomyInput`, and taking `R1`'s
digest instead is measurably WRONG for the merged record in **31 of 315 trials** (E-6). At the
compile that symbol lived in `src/generators/power/economyReconciliation.js`, and
`tests/build/domainGeneratorsBoundary.test.js` freezes the `src/domain` -> `src/generators` edge set
SHRINK-ONLY, so a `src/domain/edit/` leaf importing it would have been a FIFTH file and the ratchet
would have red (E-7, STOP-2).

⭐⭐ **THAT IS HISTORY NOW. EM-R0f LANDED on train EM-T14** and the leaf sits one layer down, exactly
as this packet proposed and the chair chartered (ODQ §934.47 addendum 35 ruling 1). **Re-measured at
`6a0178b7b`:** `src/data/economyFingerprint.js` is 53 lines with ONE export,
`export function fingerprintPowerEconomyInput` at `:42`; its only import is `fnv1a32` from
`../kernel/proseHash.js`; `economyReconciliation.js` IMPORTS it at `:22` and keeps no forwarding
definition, so the moved text has exactly one address and no `retiredSymbols` row of this packet is
disturbed. The ratchet's own header now says why, in terms: *"EM-R0f is why the set will not grow:
the power/economy input fingerprint moved DOWN a layer, to `src/data/economyFingerprint.js`, so that
EM-R0c's `src/domain/edit` record merge reads it without becoming the fifth file here."* The live
set is FOUR files / FIVE specifiers and the cardinality guard reads `toBeLessThanOrEqual(5)`.
⛔ **The import this packet writes is therefore `../../data/economyFingerprint.js` and never the
generators address** (§8 step 3; STOP-2 survives as the landing-time guard).

⛔ EM-R0f, not EM-R0c, carried the prose-numerics line-addressing sweep, the `wiring-census.json`
`stamp.files` check and the `path:line` citation sweep for that neighbourhood, and it has discharged
them.

**Who waits on which**

| waits on | why |
|---|---|
| **EM-R0c waits on EM-R0f** | `recomputeReceipts` imports it; without it the boundary ratchet reds |
| **EM-R0c waits on EM-R0a, EM-R0b, EM-R0d** | it imports the register and the invariants and re-declares neither; EM-R0b reads EM-R0d's ladders |
| **EM-B2a waits on EM-R0c** | the store path that first calls the merge; it owes the dynamic import and the byte price |
| **EM-B2b waits on EM-R0c** | the isolation property reads the delta |
| **EM-R7 waits on EM-R0c + the seam (EM-R1–R5)** | the corpus ratchet runs the merge over the REAL re-derivation |
| **the delta card (EM-D\*) waits on EM-R0c** | `delta.readings`' three values are what it renders |
| **EM-R6 does NOT wait on EM-R0c** | the delta carries its `orphaned` rows verbatim and judges nothing |

---

## 4. Sealed dispatch and preflight

`scripts/implementation-session.mjs`, check by check, as it would read at `__BASE__`:

1. **Branch name** — the implementation branch the chair cuts; the worktree holds the verified
   branch (the sealed dispatch throws `branch mismatch` from a lane branch).
2. **Ancestry** — `__BASE__` is an ancestor of the branch tip.
3. **Substrate unchanged since the verified base** -- this packet's substrate is
   `src/domain/edit/recordRegister.js`, `src/domain/edit/recordInvariants.js` and
   `src/data/economyFingerprint.js`. ⭐ **MEASURED AT `6a0178b7b`: TWO OF THE THREE ARE PRESENT**
   (the register at 190 lines, the fingerprint at 53), and the third, `recordInvariants.js`, is
   EM-R0b version 8's CREATE, READY in this tree and landing at stage 1's flip. The chair therefore
   stamps `__BASE__` at or after that flip; at that base all three exist and the check passes. This
   is the family's declared build order (R0a · R0d · R0b · R0f · R0c), not a defect.
4. **CREATE targets absent** -- `src/domain/edit/mergeConsequence.js`,
   `src/domain/edit/recordMergeTree.js` and `tests/domain/recordMerge.test.js` are ABSENT at
   `6a0178b7b` (`git ls-files` -> 0 for each) and this packet is the SOLE claimant: 0 occurrences of
   any of the three across `docs/implementation/PACKET_MANIFEST.json`'s 226 packets, and exactly 1
   in the kit's `packets-waiting/*.json` -- its own. ⛔ Neither EM-R0b v8 (whose three CREATEs are
   `recordInvariants.js`, `recordInvariantMeta.js`, `recordInvariantFlags.js` and
   `tests/domain/recordInvariants.test.js`) nor any stage-2 or stage-3 member names one of them.
5. **git-clean** — the build lane's own worktree.

**Generators among `checks`: NONE.** No command in §10 writes a file, so §P2.12's "a generator goes
LAST" has nothing to order and no generated artifact is owed. Edge-shared INPUT MEMBERSHIP measured
against all five metas' own `inputs` lists: **none** (E-8).

---

## 5. Verified tree contract

Every row below was RE-FOUND BY SYMBOL at **`6a0178b7b`** (train EM-T16's stage-1 placement tip).
The `Proof` column carries the compile's receipt, then the 2026-09-20 pre-proof's re-execution, then
**T16-n**, this pre-proof's. A row version 5 stated as an ABSOLUTE is restated as a measurement with
its as-of mark or as a DELTA (§P2 row 11).

| # | Fact | Where | Proof |
|---|---|---|---|
| V1 | ⛔ **REFUTED AS WRITTEN AND RE-CUT.** Version 5 read *"`src/domain/edit/` does not exist"*. It EXISTS and holds **TEN** files at this tip (`dmLayer.js`, `fieldDeclarations.js`, `operations.js`, `operationsNpcRename.js`, `operationsOffStage.js`, `pools.js`, `recordRegister.js`, `tracePartition.js`, `types.js`, `worldConditions.js`). **The fact this packet needs is narrower and HOLDS:** neither CREATE target exists | `src/domain/edit/` | `git ls-files -- 'src/domain/edit/*'` -> 10; `git ls-files` -> **0** for `recordMergeTree.js` and for `mergeConsequence.js` (**T16-1**) |
| V2 | ⛔ **REFUTED AS WRITTEN AND RE-CUT.** Version 5 read *"nothing in `src/` imports `src/domain/edit/**`"*. **FIVE** `src/` files do now (`components/edit/CardEditorDialog.jsx`, `components/edit/FreeField.jsx`, `components/edit/PoolField.jsx`, `store/editSlice.js`, `store/settlementRederiveAction.js`). **The fact this packet needs HOLDS:** nothing in `src/` imports `recordMergeTree.js` or `mergeConsequence.js`, and this packet adds no importer, so **both leaves enter NO emitted chunk** | whole `src/` walk | `git grep -l` over `src` (**T16-2**); the byte consequence is a MEMBERSHIP fact handed to EM-R1b, the train's byte-arm holder (judgment 206d) |
| V3 | the eager first-paint closure holds **0** `src/domain/edit/**` files and **0** EM-R6 leaves; its size is **270 modules as of `6a0178b7b`**, ⭐ read by IMPORTING `vite.config.js`'s exported set, never a replica or a list (§P2 row 11) | `vite.config.js#EAGER_FIRST_PAINT_MODULES` | E-8(a) -> 268 at `141a1d775` -> **270, 0, 0** and the probe exits `0` (**T16-3**). ⛔ The SIZE is never an absolute of this packet; the PASS CONDITION is the two zeroes |
| V4 | ⛔ **THE `220` ABSOLUTE IS RETIRED.** The generation worker's real closure is measured off `dist` by `tests/build/generationWorkerLazy.test.js#entryClosure`, which this seat may not build. **What this packet asserts, and what it measured, is MEMBERSHIP:** no `src/` module imports either leaf, so neither can be in the worker chunk; `WORKER_BUNDLE_CEILING_BYTES` is `1392364` and this member's declared delta against it is **+0 B** | `src/workers/generation.worker.js`; `tests/build/generationWorkerLazy.test.js:219` | E-8(b) -> **T16-2 / T16-4**. A rise is the OWNER's (§P11.2); this member re-mints nothing and edits no budget test |
| V5 | ⛔ **SUPERSEDED BY A LANDING.** `fingerprintPowerEconomyInput` no longer has a definition in `src/generators/power/economyReconciliation.js`: EM-R0f moved it and that file now IMPORTS it | `economyReconciliation.js:22` | `grep -n` (**T16-5**) |
| V5' | ⭐ **DISCHARGED.** The symbol this packet IMPORTS, `export function fingerprintPowerEconomyInput` at `src/data/economyFingerprint.js`, EXISTS at this tip and is a `requiredSymbols` row rather than a pending one | `src/data/economyFingerprint.js:42` | `grep -cF` -> **1** (**T16-5**). The leaf is 53 lines and imports only `fnv1a32` from `../kernel/proseHash.js` |
| V6 | the domain->generators ratchet is SHRINK-ONLY and the live set is **FOUR files / FIVE specifiers** (`checkDraftEdit.js`, `neighbourBackLink.js`, `institutionLifecycle.js`, `resourceDynamicsKernel.js` x2); its cardinality guard reads `toBeLessThanOrEqual(5)` and its header names EM-R0f and EM-R0c by name | `tests/build/domainGeneratorsBoundary.test.js#BASELINE_EDGES` (**`:70`**, moved from `:60`) | E-7 -> **T16-6**; the file gained judgment 111's root fix and EM-R0f's re-baseline in the window, and its `repoRoot` line is now itself pinned as text |
| V7 | the fingerprint's three members are private to ONE file, and that file is now `src/data/economyFingerprint.js`: `ECONOMY_FINGERPRINT_VERSION` (`:25`) and `economyProjectionInput` (`:27`) are MODULE-PRIVATE, only `fingerprintPowerEconomyInput` (`:42`) is exported | `src/data/economyFingerprint.js` | `grep -n '^export'` -> exactly one row (**T16-5**). This packet imports the exported one and names neither other |
| V8 | ⛔ **STILL PENDING, AND IT IS THE PACKET'S ONE OPEN CONDITION.** EM-R0b **version 8** is READY in this tree; its §6 spells `export function recordInvariants(settlement)` and, byte for byte, `export { CHECK_META, CHECK_IDS, CROSS_KEY_CHECKS } from './recordInvariantMeta.js';` | `docs/implementation/packets/settlement-editor/EM-R0b.md` §6 `:440` / `:450`; its §7 row and its STOP-11 | **T16-7**: `git ls-files -- src/domain/edit/recordInvariants.js` -> **0** at this tip. Both rows stay in `_pendingRequiredSymbols`, are named as ARRIVING at stage 1's flip, and their verbatim greps are in `EM-R0c.remeasure.sh` |
| V9 | `tests/domain/` is not a mutation-coverage ENFORCER_DIR and `recordMerge` matches no `NAME_PATTERN` token | `tests/lint/mutationCoverage.shared.mjs` | E-11 -> **T16-8**: `ENFORCER_DIRS` is EIGHT trees (`tests/lint`, `design`, `docs`, `data`, `copy`, `security`, `edgeFunctions`, `generators`), none of them `tests/domain`; `NAME_PATTERN`'s 16 tokens match neither `recordMerge.test.js` nor `recordMerge`. ⛔ `recordMergeContract` WOULD match `contract` -- the file's NAME is a contract of §7 |
| V10 | the lighting census counts TEST files, not `src/` leaves | `EM-PREAMBLE.md` §P2.1 | E-11 -> P-7 -> **T16-9**: the census's `files` figure is `2692` and `walk(join(ROOT,'tests'))` is its whole population |
| V11 | ⛔ **RE-CUT, AND THE LIVE RULE IS STRICTER THAN VERSION 5 SAID.** `tsconfig.domain-strict.json` extends `./tsconfig.json` (whose `include` is `src/domain/**/*.js` + `.ts`) and flips `strict` + `noImplicitAny` only, so **both CREATEs are governed by it**; `tsconfig.full.json` includes `src/domain/**/*.js` too, so both are in the `npm run typecheck` gate as well. The strict burn-down is **COMPLETE and the ceiling is a HARD ZERO** -- not "a baseline that can only shrink" | `tsconfig.domain-strict.json`; `tsconfig.full.json`; `tests/lint/domainStrictBaseline.test.js` header | E-11 -> **T16-10** -- **STOP-3**, and it now means ZERO strict errors, not "no growth" |
| V12 | EP-0 (LANDED) holds ONE `requiredSymbols` row on `economyReconciliation.js`, for `rngSeed: stepRng.fork(POWER_STREAM).seed,`, which survived EM-R0f's move and now sits at `:103` | `PACKET_MANIFEST.json`; `economyReconciliation.js:103` | E-7, E-12 -> **T16-5**. Undisturbed by this packet, which touches that file not at all |
| V13 | the emitted leaves are byte-for-byte the measured function | 567 trials | E-2 (the compile). **CARRIED FORWARD**: the J-T1 window touches no path either module READS except the register and the fingerprint, and both are its declared substrate rather than behaviour it depends on. Re-proving it is EM-R7's |
| **V14** | ⭐⭐ **THE HISTORY-ORDER TRIAL** -- the group arm and §22.2 item 4's order arm MEET at `history` and DO NOT INTERFERE; `members: null` at `history` SILENCES the order arm on four collections (203 entries -> 0). **Its premise is now a LANDED FENCE:** `CONSISTENCY_GROUPS` at `src/domain/edit/recordRegister.js:175`-`:190` carries SEVEN entries and the seventh is exactly `{ id: 'history-age', root: 'history', members: Object.freeze(['age','founding.age']) }` | executed through the prototype seam at `141a1d775`; the declaration re-read at `6a0178b7b` | P-10 (693 trials, 0 errors) + **T16-11**. The register's own comment gives the same reason in EM-R0a's words |
| **V15** | ⛔ **A NEW `tests/domain/*.test.js` OPTS INTO THE `tests/lint` WALKERS THAT WALK `tests/` WHOLE**, and `seedLoopTotality.walker.test.js` CONVICTS the forbidden idiom. `tests/domain` holds **12** rows in its SHRINK-ONLY `FROZEN_BARE_SEED_LOOPS` habitat (version 5 said 13) | `tests/lint/seedLoopTotality.walker.test.js#scanBareSeedLoops`, `SCAN_ROOTS = ['tests']` | P-11 -> **T16-12, RED-FIRST RE-EXECUTED at this tip through the walker's own scanner**: the forbidden `for (const { _seed, ...cfg } of rows) {` is convicted at its loop line (`count 1`); §7's prescribed `for (const c of rows) {` with the collected assertion scans **CLEAN** |
| **V16** | the lighting delta is **`files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +1`**, and the file is **CREDITED** | `tests/lint/sovereigntyLightingContract.walker.test.js#parkReasonsFor` / `#classifySource` | **T16-13** -- the walker's OWN classifier, re-derived FRESH from the current source at this tip (the walker file is byte-unmoved across the window; only its baseline JSON moved) and driven over a candidate draft built to §7: `parkReasonsFor -> []`, `titles -> 8`, `suiteTitles -> 1`. ⭐ And **judgment 196**: all 9 titles are UNIQUE across the estate's 2,692 test files (0 collisions) |
| **V17** ⭐ NEW | **`tests/property/dmLayerGoldenIsolation.test.js :: A8` clause 2 walks `src/domain/edit` WHOLE** and asserts *"no module under src/domain/edit names a generators specifier, static or dynamic"* with the regex `/(?:from\s*['"]|import\(\s*['"])[^'"]*\/generators\//`. The walk is DYNAMIC, so no manifest row is owed -- but the arm governs both CREATEs from the moment they land | `tests/property/dmLayerGoldenIsolation.test.js:245`-`:251` | **T16-14**: the regex run over this packet's own planted text returns **false** on both leaves. ⛔ EM-R1b holds a TEST row on this same file at stage 2; this packet holds none and adds none |
| **V18** ⭐ NEW | **`tests/lint/domainAnyCastBaseline.test.js`** freezes the `@type {any}` / `@ts-*` suppression debt of `src/domain` per file and **FAILS a NEW FILE THAT CARRIES ANY DEBT**. `tests/lint/.domain-any-baseline.json` holds 167 rows and **none under `src/domain/edit/`** | `scripts/count-domain-any.mjs#countText`; the baseline | **T16-15**: `countText` run over both planted leaves -> `{ any: 0, suppress: 0 }` each. This is the instrument that would punish buying STOP-3's strict-clean with a cast |
| **V19** ⭐ NEW | **`tests/copy/voiceMechanics.test.js` Tier 2** scans `walkJs('src/data') + walkJs('src/domain')` and counts em dashes and `!` inside STRING LITERALS by AST, per file, SHRINK-ONLY, **a file with no baseline row held at zero**. Its baseline holds 55 rows and **none under `src/domain/edit/`**. Tier 5's `src/generators` em budget stands at **28 of 28** and this member touches no `src/generators` file | `tests/copy/voiceMechanics.test.js#countFile` / `#stringLiteralContents` / `SCANNED_FILES`; `tests/copy/.voice-mechanics-baseline.json`; `.voice-mechanics-generators-baseline.json` | **T16-16**: the walker's own AST literal collector run over both planted leaves -> `em 0 · bang 0` (26 and 22 literals read). ⛔ Both files carry em dashes in COMMENTS (2 and 9) and the walker does not see them -- the rule is about LITERALS, and §7 says so |
| **V20** ⭐ NEW | **`tests/lint/tuningRegister.walker.test.js`** holds P2 (module-top-level `const UPPER_SNAKE = <numeric>;`) and P3 (bare DECIMAL literals in code text) SHRINK-ONLY over `TREES_P2P3 = ['src/domain','src/generators']`, **new files at zero** in the arms' own words | `scripts/lib/tuning-inventory.mjs#NAMED_NUMERIC_CONST_RE` / `#BARE_DECIMAL_RE`; the walker `:448` / `:482` | **T16-17**: both regexes run over both planted leaves -> **P2 0 · P3 0**. P3 does not count integers, so the guard ladder's `1 / 2 / 3` steps are free |
| **V21** ⭐ NEW | **`tests/lint/ruinFilterRoster.walker.test.js`** pins `readers.length` at **94** and discovers a reader by `READER_RE = /\.institutions\b/` over `codeOnly(src)` (comments and string contents blanked) across `src/domain` WHOLE; **`tests/lint/entropyRootCensus.walker.test.js`** pins `createPRNG(` at **36** under `src/domain` and **47** under `src/` whole, and its `ENTROPY_CONSUMERS` are `['createPRNG','hash01','fnv1a32']` | the two walkers; `tests/helpers/codeOnlySource.js#codeOnly` | **T16-18**: `codeOnly` + `READER_RE` over both planted leaves -> **false** (0 raw mentions either); `createPRNG(` / `hash01(` / `fnv1a32(` -> **0 / 0 / 0** in both. Live counts re-measured at **36 / 47**, identical to the pins. ⛔ The leaf calls `fingerprintPowerEconomyInput`, never `fnv1a32` itself, so no consumer site is minted |
| **V22** ⭐ NEW | `stamp.producerIndexFiles` is a NUMBER and its rule is measured, not assumed: **1185 at this tip = 114 `.js` under `src/generators/**` + 1071 `.js` under `src/domain/**`**, counted independently and equal to the stamp | `docs/content/wiring-census.json`; a `.js` walk of both roots | **T16-19**. This packet's DELTA is **+2** (§7). `stamp.files` (7 entries) and `stamp.candidateLeaves` (6) are all `src/domain/display/stateProse/` paths -> **UNMOVED** |

## 6. Exact contracts

### Inputs and outputs

```js
// src/domain/edit/recordMergeTree.js — PURE. No store, PRNG, clock, locale, I/O.
// ⛔ NO src/generators import, NO src/components import.
import {
  RECORD_CLASSES, CLASS_EXCEPTIONS, KEYED_COLLECTIONS, ATOMIC_COLLECTIONS, CONSISTENCY_GROUPS,
} from './recordRegister.js';

export const collapse = (path) => path.replace(/\[\d+\]/g, '[]');
export function newReceipts();          // the accounting shape, below
export function mergeTree(record, R0, R1);  // -> { value, receipts }
export const __internals = { at, put, mergeNode, collapse };
```

```js
// src/domain/edit/mergeConsequence.js — PURE. The post-passes, the guard and the delta.
import { CLASS_EXCEPTIONS, RECORD_CLASSES, CONSISTENCY_GROUPS } from './recordRegister.js';
import { recordInvariants, CHECK_META } from './recordInvariants.js';
import { fingerprintPowerEconomyInput } from '../../data/economyFingerprint.js';
import { mergeTree, __internals } from './recordMergeTree.js';

export function recomputeMirrors(merged);      // -> number of members rewritten
export function recomputeReceipts(merged);     // -> boolean, whether the digest moved
export function enclosingGroup(path);          // -> the SMALLEST declared group, or null
export function guardMergedRecord(merged, R1, exempt);  // -> escalation rows
export function mergeConsequence(record, R0, R1, options);  // -> { record, delta }
```

- `record` — **THE WRITE BASE**: the saved record with the op's own writes already applied.
- `R0` — `rederive(base)`; `R1` — `rederive(record)`. Both are ARGUMENTS; this module generates
  nothing.
- `options.edit` — `{ id, opType, touched?, orphaned? }` or `null`. **An ARRAY throws `TypeError`.**
- `options.base` — the record before the edit (the store's snapshot); defaults to `record`.

### State schema

**No state, no persisted key, no new record family.** The module holds nothing between calls and
writes nothing. `delta` is a return value, never stored by this packet.

### Transition table — the node rule, in the order it is applied

| the node at path `p` | rule | source |
|---|---|---|
| `collapse(p)` is a `CLASS_EXCEPTIONS` HISTORY or AUTHORED path | keep the record's, whole | §22.2 item 7 |
| `R0` and `R1` are deep-equal | keep the RECORD's own value — **including ABSENT** | §22.2 item 1 |
| `collapse(p)` is a declared group root and either side is an object | the group moves WHOLE: `R1`'s if any member differs, else the record's; a `members` list moves as ONE value over a node-rule rebuild that preserves key order | §22.2 item 2, §22.3 item 2 |
| ⭐ *the same row, said for the case EM-R0a's G8 created* — a group with a MEMBER LIST rebuilds every OTHER key of its root **by the node rule**, so an order-bearing collection under a group root keeps BOTH order arms and still reports to `delta.orderMoved`. ⛔ The implementer writes the member overwrite AFTER the node-rule rebuild and NEVER as a whole-object take: `members: null` at `history` silences the order arm on 4 collections (V14, measured 203 → 0 entries) | — | §22.2 items 2 and 4; V14 |
| both sides are arrays and `collapse(p)` is a declared KEYED collection not also declared ATOMIC | merge by the DECLARED key; see the order rule | §22.2 item 5, §22.4 |
| both sides are arrays otherwise (keyless, ATOMIC, a cross-entry total, an array of scalars) | ONE value: `R1`'s | §22.2 item 5, §22.3 item 3 |
| a declared key is absent or non-unique in any of the three arrays | the collection degrades to ONE value and the refusal is recorded in `receipts.keyBroken` | §22.2 item 5 |
| both sides are objects | recurse over the union of keys, `R0`'s order first | §22.2 item 1 |
| otherwise (a differing leaf) | take `R1`'s, and record the path in `receipts.taken` | §22.1 |

At the TOP level, before the node rule: a key whose class is `WORLD` takes `R1`'s (it is the input);
a key whose class is **anything other than `READING`** — HELD, AUTHORED, HISTORY, CONSTANT, or
unknown — is taken from the RECORD untouched. ⛔ **A TOP-LEVEL key present in `record` is present in
the result, whatever the two re-derivations carry** (measured: 0 top-level keys lost over 693
trials, P-9).

⛔⛔ **AND THE SAME RULE AT DEPTH, SAID EXACTLY — the chair's contract (ODQ §934.60) IS the exact
rule, and the absolute is not.** *Nothing present in the record **AND UNTOUCHED BY THE EDIT** may be
absent from the result.* "Untouched by the edit" is `R0 ≡ R1` at the node, and the node rule already
delivers it: a node the two re-derivations agree on returns the record's subtree WHOLE, present or
absent. Where they DISAGREE, `R1` governs — **including when `R1` does not carry the key at all**,
which is how a reading the edit genuinely removed leaves the record. Measured over 693 trials: 245
deep pure-object paths left the record, **0 of them under a node where `R0 ≡ R1`** (P-9). ⛔ **The
implementer must not "fix" this by padding absent keys back in**: that would re-introduce the dead
`resourceAnalysis.imports.reasons.*` rows a terrain edit legitimately clears, and it would
contradict §22.2 item 1's "present or absent". ⛔ **And A1 asserts the TOP-LEVEL form**, which is
safe at any depth because the identity arm makes `R0 ≡ R1` everywhere.

### Ordering and precedence

1. `mergeTree` — the whole tree, once.
2. `recomputeMirrors` — every `CLASS_EXCEPTIONS` MIRROR path, from the merged roster. A member whose
   `id` the merged roster no longer carries is left exactly as the record has it (removing a person
   is the op's cascade, never the merge's).
3. `recomputeReceipts` — the declared RECEIPT, **LAST**, over the merged record.
4. `guardMergedRecord` — `recordInvariants(merged)` minus the EXEMPT set (every violation the
   pre-edit record, the edited record or `R1` already carries). Each surviving violation escalates:
   **step 1** the enclosing declared group from `R1` (or, for a cross-key check, BOTH its keys);
   **step 2** the violated path's top-level key(s) from `R1`; **step 3** every READING key from `R1`.
   The receipt is recomputed after every escalation step. It terminates because a generated world
   agrees with itself; an exhausted ladder is recorded as `step: 'EXHAUSTED'` and never loops.

**The order rule, both arms.** Let the COMMON entries be those keyed in all three. If their relative
order is the same in `R0` and `R1`, **the record's order stands**: an entry only the record carries
keeps its place, and an addition takes the place of its `R1` neighbour. If `R0`'s and `R1`'s common
order DIFFERS — the edit moved the order — **`R1`'s order governs**, entries only the record carries
are appended, and the collection's collapsed path is pushed to `delta.orderMoved`.

### Determinism

Hash / fork key **NONE**. No PRNG, no clock, no locale, no environment, no `Math.random`, no
`Date`, no `Intl`. Every iteration order is derived from the arguments' own key order and from the
register's declaration order; every set is built from those, never from a hash of unordered input.
`structuredClone` on entry and on every value written out: **0 of 504 caller records mutated** and
0 of 63 on the identity arm (E-3, E-4).

### Flag and dormancy

Flag **NONE**. Wave 1 is headless. Dormancy is structural, not conditional: nothing in `src/` imports
these leaves until EM-B2a does, so the code is unreachable rather than switched off (E-8).

### Lifecycle

| path | behaviour |
|---|---|
| create | the merge returns a value; the op and the store persist it |
| read | none; the module reads only its three arguments |
| persist | **nothing** — no new key, no schema change |
| reload | nothing; the merged record is an ordinary saved record |
| regenerate | untouched; fresh generation never calls this module |
| undo | **the store's snapshot, never an inverse merge** (§22.1) |
| import / fork / gallery | edits do not travel (HZ-TRAVEL); this module is not on those paths |
| migrate | none owed; no persisted shape moves |
| public veil | none; the module renders nothing and reads no save-time key |

### Receipts and privacy

`delta` carries: `appliedEdit` (a string or `null`, **never an array**); `readings[]` of
`{ path, before, now, editAlone: { from, to } }`; `orderMoved[]`; `escalations[]`;
`orphaned[]` (EM-R6's rows, carried VERBATIM — the writer reports, the guard engine judges);
`groupsTaken[]`; `groupsKept`; `honesty: { taken, rideAlong }`; `nextBase` (which is `R1`, the next
edit's `R0`). No PII, no seed, no prose. The delta is not persisted by this packet.

### Alignment and edit story

Alignment: `DECLARED EMPTY.` Edit story: `ENGINE-ONLY: this packet computes the consequence of an
edit; it exposes no DM verb and renders no card.`

---

## 7. Exact change manifest

| action | path | symbols | bound | note |
|---|---|---|---|---|
| `CREATE` | `src/domain/edit/recordMergeTree.js` | `mergeTree`, `newReceipts`, `collapse`, `__internals` | `≤ 250` effective | The pure tree merge: the node rule, groups, keyed collections, ATOMIC, order. **Import ONLY `./recordRegister.js`.** Declare no class, no key, no group. The planted draft measures **166** effective lines under eslint's own `max-lines` with `{ skipBlankLines, skipComments }`, re-measured at `6a0178b7b` (T16-20). ⭐ **THE FIVE INSTRUMENT PINS, EACH EXECUTED OVER THE PLANTED TEXT AT `6a0178b7b` AND EACH AT ZERO** (V17-V21): (i) **no em dash and no `!` inside any string literal** -- the voice walker's Tier 2 scans `src/domain` whole and holds a file with no baseline row at `em 0 · bang 0`; em dashes in COMMENTS are invisible to it and this leaf carries two; (ii) **no bare decimal literal and no module-top-level `const UPPER_SNAKE = <numeric>;`** -- the tuning register holds a new `src/domain` file at `P3 = 0` and `P2 = 0`; integers are not counted; (iii) **no `@type {any}` and no `@ts-ignore` / `@ts-expect-error`** -- `domainAnyCastBaseline` FAILS a new file carrying any debt, so STOP-3's strict-cleanliness is bought with real types, never with a cast; (iv) ⛔ **no `.institutions` TOKEN IN CODE** -- the ruin-filter roster discovers readers by `codeOnly(src)` and pins `readers.length` at 94; every path this leaf handles arrives as DATA from the register, never as a property chain; (v) **no `createPRNG(`, `hash01(` or `fnv1a32(` call site** -- the entropy census pins 36 / 47 and §6's determinism clause already forbids a draw. |
| `CREATE` | `src/domain/edit/mergeConsequence.js` | `mergeConsequence`, `recomputeMirrors`, `recomputeReceipts`, `enclosingGroup`, `guardMergedRecord` | `≤ 250` effective | The post-passes, the guard and the delta, plus the ONE public surface. Imports `./recordRegister.js`, `./recordInvariants.js`, `../../data/economyFingerprint.js`, `./recordMergeTree.js` and **nothing else** -- the four measured on the planted draft, in that spelling (T16-2). The planted draft measures **128** effective lines (T16-20). ⭐ **THE SAME FIVE INSTRUMENT PINS, all measured at ZERO on this leaf too** (V17-V21), and **a SIXTH that governs this directory**: (vi) ⛔ **NO `src/generators` SPECIFIER, STATIC OR DYNAMIC** -- `tests/property/dmLayerGoldenIsolation.test.js :: A8` walks `src/domain/edit` WHOLE and its regex `/(?:from\s*['"]\|import\(\s*['"])[^'"]*\/generators\//` must return `false` for every module in the directory; measured `false` here (T16-14). ⛔ This is exactly why the fingerprint import is `../../data/economyFingerprint.js`: the generators address would red BOTH that arm and the boundary ratchet. ⚠ The `TypeError` message A8 asserts is a STRING LITERAL, so it obeys pin (i): it names the law in plain words and spells no em dash, no `!` and no number. |
| `CREATE` | `tests/domain/recordMerge.test.js` | arms A1-A8 and the `CANNOT-CATCH:` header | `n/a` | ⚠ The action is `CREATE`, not `TEST`: §4 measures this path ABSENT at the base and `implementation-packets.mjs` asserts `row.action !== 'CREATE' && !fileExists(...)`. **It ADDS EXACTLY 8 literal `it`s and RE-PINS 0 existing `it`s**, in **ONE literal `describe`** -- no `.each`, no loop, no conditional, no nested describe (§P3.4). Derive the 63-row stride from `goldenCorpus()`; commit NO fixture; generate through `generateSettlementPipeline(cfg, null, { seed: _seed, customContent: {} })` as `tests/property/generatorGoldenMaster.test.js` does. ⛔ **Never bind `it`, `test` or `describe` as a variable or parameter.** ⛔ **The file is named `recordMerge.test.js`** and NOT `recordMergeContract.test.js` (matches the mutation-coverage `NAME_PATTERN` token `contract`, V9) and NOT `recordMerge.contract.test.js` (that spelling ALSO opts the file into `contractTestAntiVacuity.walker.test.js`, whose `inScope` otherwise excludes `tests/domain`, and whose Rule 2 would convict A8's enumerated export list). Every negative assertion carries `// anchored:` on the line immediately above. ⭐ **Its lighting posture is EXECUTED at `6a0178b7b`, not predicted**: the walker's own classifier, re-derived fresh from the current walker source, scored a candidate draft `parkReasonsFor -> []`, `titles -> 8`, `suiteTitles -> 1` (V16, T16-13), and all 9 titles are unique across 2,692 test files (judgment 196, T16-13). <br><br> ⛔⛔ **THE SEED-LOOP CONSTRUCTION RULE (V15, STOP-9).** `seedLoopTotality.walker.test.js` walks `tests/` whole and convicts any `for (` line carrying `seed`/`seeds`/`SEED` whose body holds a bare `expect(`. `tests/domain` sits in its SHRINK-ONLY `FROZEN_BARE_SEED_LOOPS` habitat (**12** rows) and that baseline is OUTSIDE this manifest, so a conviction cannot be absorbed. **Build it as the precedent does:** `tests/property/generatorGoldenMaster.test.js:837` writes `for (const c of rows) {`, destructures INSIDE the body, collects failures and asserts ONCE after the loop. ⛔ Never write `for (const { _seed, ...cfg } of rows) {`. **RE-EXECUTED RED-FIRST at this tip** (T16-12): the forbidden shape is convicted at its loop line; the prescribed shape scans clean. |

**Generated artifacts: `NONE`** -- no `checks` command runs a generator, and no `src/` path of this
packet is an input to any of the five edge-shared bundle metas (E-8).

### Registration obligations, priced

| obligation | owed? | measurement |
|---|---|---|
| sovereignty-lighting census | **YES -- an INTERIOR RED, delta only** | ⭐ **`files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +1`**, from the ONE new test file, **EXECUTED at `6a0178b7b` through the walker's own `parkReasonsFor` / `classifySource`** (V16, T16-13). ⛔ **No absolute tuple is this packet's own; the census is re-derived whole at the train's terminal, BY THE CHAIR** (§P2.1). For the chair's arithmetic only, the frozen tuple at this tip is `2692·359·2333·25777·6847` (`tests/lint/.lighting-census-baseline.json`, `measuredAtSha 3c7cda17c`, the twenty-first refreeze) -- quoted as the BASELINE the chair refreezes FROM, never as this packet's figure. ⚠ EM-R0b v8 lands one new test file at stage 1 and EM-R1b / EM-R3 land their own; the terminal's refreeze is the SUM of every stage's declared delta, and this row is only this member's addend |
| ⭐ `tests/lint` WHOLE, as a §10 INSTRUMENT | **YES** | A CREATE under `tests/<dir>` opts into EVERY walker governing `tests/<dir>` (the chair's addendum of 2026-09-20; runs 17/18/19 were one family). `tests/lint` holds **189** files at this tip (version 5 said 172). It is an INSTRUMENT, never a sealed `checks` entry: sealing the directory run would bake the lighting INTERIOR RED into `check:packet`. ⭐ The SIX governing files that are safe to seal ARE sealed -- §10's two new arrays |
| ⭐⛔ **Voice mechanics (Tier 2, `src/data` + `src/domain`)** | **YES -- TWO NEW SCANNED FILES, EACH HELD AT ZERO** | V19. **PREDICTED DELTA `em +0 · bang +0` on both leaves**, which is a CONTRACT of the CREATE rows' pin (i), not a hope -- **measured on the planted text** (T16-16). Tier 5's `src/generators` budget is at **28 of 28** and this member touches no `src/generators` file, so its Tier-5 delta is **0** |
| ⭐⛔ **Tuning register, P2 and P3** | **YES -- TWO NEW `src/domain` LOGIC LEAVES, HELD AT ZERO IN BOTH** | V20. **PREDICTED DELTA `P2 +0 · P3 +0`**, measured on the planted text (T16-17) |
| ⭐⛔ **`domainAnyCastBaseline`** | **YES -- TWO NEW `src/domain` FILES, HELD AT ZERO DEBT** | V18. **PREDICTED DELTA `any +0 · suppress +0`**, measured on the planted text (T16-15). ⛔ This is the instrument that makes STOP-3 honest: a strict-clean file bought with `@type {any}` reds HERE instead |
| ⭐ **Prose numerics** | **NO -- and BOTH arms are answered** | The walker scans all of `src/` and holds a new file at 0 in every category; measured `{}` on both planted leaves (T16-16). The LINE-ADDRESSED arm is separately zero because this packet MODIFIES no `src/` file, so no baselined row moves beneath an insertion (218 rows, none under `src/domain/edit/`) |
| ⭐ **Ruin-filter roster** | **NO -- OUT OF THE DISCOVERY SET BY CONSTRUCTION, DECLARED RATHER THAN DISCOVERED** | V21; `readers.length` is pinned at **94** and neither leaf's `codeOnly` text matches `/\.institutions\b/` (T16-18). ⚠ If the implementer writes a roster read as a property chain, the leaf enrols and the packet owes an exempt row -- that is a STOP, not a fix |
| ⭐ **Entropy root census** | **NO** | V21; `createPRNG(` pinned at 36 / 47 and re-measured at 36 / 47; this packet's sites are 0 (T16-18) |
| `scripts/mutation-coverage-manifest.json` | **NO** | V9. ⛔ EM-R1b, EM-R2 and EM-R4 each hold a row on this file in train EM-T16; **this packet holds none**, so no `rowKey` can collide (TOOL-27) |
| `tests/lint/heldKeyWriterCensus.walker.test.js` `DISCOVERY_EXEMPT` | **NO** | Its A3 discovery set is the ASSEMBLY STEP's own body, which this packet does not touch. EM-R2 v5 lands the one new row there at stage 1; **this packet adds none** |
| observed-shape `EXPLAINED_WRITER_EXEMPTIONS` | **NO** | no `dmLayer` and no `decrees` literal in either leaf (E-11) |
| `scripts/check-writer-reach.mjs` | **NO MOVEMENT** | seven literal settlement fields, every one with a live corpus writer (E-11) |
| `docs/content/wiring-census.json` -- BOTH arms | ⛔ **YES, ON THE PRODUCER ARM -- A DEFERRED ROW** | **The SHA-MAP arm: NO** -- `stamp.files` (7 entries) and `stamp.candidateLeaves` (6) are all `src/domain/display/stateProse/` paths and this packet modifies none. ⛔ **THE PRODUCER-COUNT ARM:** `stamp.producerIndexFiles` **+2** -- `src/domain/edit/recordMergeTree.js` and `src/domain/edit/mergeConsequence.js`. ⭐ **THE RULE IS MEASURED, NOT ASSUMED (V22, T16-19): 1185 at `6a0178b7b` = 114 `.js` under `src/generators/**` + 1071 under `src/domain/**`, counted independently and equal to the stamp.** `producerIndexFiles` NAMES NOTHING -- it is a number -- so the answer is a DELTA. `totals.*` UNMOVED. ⛔ **Regenerated WHOLE at the train's terminal, BY THE CHAIR**; the path is in NEITHER §7, NOR the `changeManifest`, NOR `checks`. ⚠ EM-R0b v8 declares **+3** on the same counter at stage 1; the terminal sums |
| `path:line` citations in other files | **NO** | this packet shifts no line in any existing file |
| `npm run build:edge-shared` | **NO** | input membership measured against all five metas: none (E-8) |
| generation-worker / lazy-engine / first-paint budgets | **ZERO BYTES in all four, as a MEMBERSHIP fact** | V2, V3, V4. No `src/` module imports either leaf and this packet adds no importer, so neither enters an emitted chunk; `EAGER_FIRST_PAINT_MODULES` holds 0 `src/domain/edit/**` members at 270 modules. ⛔ **The declared delta `+0 B` per closure is handed to EM-R1b, train EM-T16's BYTE-ARM HOLDER (judgment 206d), for its sum**; this member edits no budget test and re-mints nothing. The bytes become real at the apply point's import, priced there |
| golden posture | **UNCHANGED** | §P3.1; the inertness control is the standing proof (E-1) |

### ⭐ EM-R6's FIRST-PAINT DOOR -- **RE-AIMED AWAY FROM THIS MEMBER; THE PROBE STAYS**

EM-R6 is **LANDED**, and its version 5 re-aimed §3b's binding note on the chair's ruling over
ESTATE-REPAIR-2's NOTE-2. Its landed text says so in terms, and this row is a transcription of it
rather than a fresh judgment:

> *"THE FIRST PACKET THAT CAN HOLD A STATIC IMPORT EDGE TO EITHER LEAF IS THE APPLY POINT --
> `EM-C4a` / `EM-D0` per the charter -- AND THE DOOR BELONGS THERE."*
> *"AND THE GUARD STAYS WHERE IT IS. EM-B1a, EM-B1c and EM-R0c each keep the first-paint MEMBERSHIP
> probe in their `checks` as a STANDING guard: it imports the exported set, reads no `dist`,
> therefore cannot skip (§P2.11), costs nothing, and catches the edge the day someone adds it."*

⚠ **THIS MEMBER DECLARES NO IMPORT OF EITHER EM-R6 LEAF**, measured over its §5-§8 and its JSON
capsule and over the planted text's four import specifiers (T16-2). The probe is the STANDING GUARD
that a later edit does not add one. **EXECUTED at `6a0178b7b`:**
`EAGER_FIRST_PAINT_MODULES: 270 | EM-R6 leaves in the eager closure: NONE`, exit `0` (T16-3).
⛔ The figure this member states is the **DELTA across its own edit (`N -> N`)**, never an absolute;
the **PASS CONDITION is that both leaves stay OUT**. A first-paint raise is OWNER-SIGNED and this
member re-mints nothing.

## 8. Ordered coding sequence

1. **K-GATE, before the first edit.** Confirm the three substrate files exist at the stamped base:
   `src/domain/edit/recordRegister.js`, `src/domain/edit/recordInvariants.js`,
   `src/data/economyFingerprint.js`. ⭐ **At `6a0178b7b` TWO of the three are PRESENT** (the register,
   190 lines; the fingerprint, 53) and `recordInvariants.js` ARRIVES with EM-R0b version 8 at stage
   1's flip -- so the gate is real, not ceremonial. **If any is absent, STOP.** ⭐ **And confirm the
   eight symbols by name, not the files alone** (`grep -cF`): `RECORD_CLASSES`, `CLASS_EXCEPTIONS`,
   `KEYED_COLLECTIONS`, `ATOMIC_COLLECTIONS`, `CONSISTENCY_GROUPS` (EM-R0a, all five measured
   present at this tip at `:14 :62 :70 :122 :175`), `recordInvariants` **and the re-export line
   `export { CHECK_META, CHECK_IDS, CROSS_KEY_CHECKS } from './recordInvariantMeta.js';`** (EM-R0b
   version 8 -- an EM-R0b before version 3 has the file and not the symbol, which is STOP-4's
   surviving guard), and `fingerprintPowerEconomyInput` (EM-R0f, measured present at
   `src/data/economyFingerprint.js:42`). ⛔ Confirm `CONSISTENCY_GROUPS` carries **SEVEN** entries
   including `history-age` with the member list `['age','founding.age']` -- measured SEVEN at this
   tip; `members: null` there silences the order arm on four collections (V14).
2. Create `src/domain/edit/recordMergeTree.js` from §6's contract. Import only the register.
3. Create `src/domain/edit/mergeConsequence.js` from §6's contract. ⛔ The import of the fingerprint
   is `../../data/economyFingerprint.js` — **never** `../../generators/power/economyReconciliation.js`
   (V6: the boundary ratchet reds on a fifth file).
4. Run `npx eslint` on both leaves. Then `npm run typecheck:ratchet` and
   `npm run typecheck:domain:strict`. **Both leaves must be strict-clean, and the strict ceiling is a
   HARD ZERO** — the burn-down completed (Wave B, 2026-07-09: 4,649 → 0 across 133 files), so there is
   no baseline to absorb a new file into (STOP-3, V11). ⛔ **And strict-cleanliness is bought with
   real types, never with `@type {any}`**: `tests/lint/domainAnyCastBaseline.test.js` FAILS a new file
   carrying any suppression debt, and it is a sealed check of §10 (STOP-10, V18).
5. Create `tests/domain/recordMerge.test.js` with §9's eight arms — ONE literal `describe`, eight
   straight-line literal `it` titles, the `CANNOT-CATCH:` header verbatim.
6. Run the focused test slot, then the boundary ratchet, then the goldens (§10, in that order).
7. ⛔ **This packet's §8 schedules NO census refreeze.** An in-packet refreeze needs a commit, a
   commit drifts the sealed HEAD, and the sealed verbs then refuse (§P2.1, measured at EM-P0's build).
   The delta of §7 is reported to the chair and the chair re-derives the census at the terminal.

---

## 9. Acceptance matrix

Every arm below was EXECUTED at `32602dc60` against the emitted modules (E-10), and takes `R0` and
`R1` as INPUTS — so it runs at this packet's own base, with the re-derivation seam still unbuilt.
⭐ **THE HOME OF ALL EIGHT CASES IS `tests/domain/recordMerge.test.js`**, the single test-bearing row
of §7, which **adds exactly 8 literal `it`s and re-pins exactly 0**; the count law (§P9) therefore
reads `8 declared its === 8 cases homed === the lighting titles delta +8`, and `EM-R0c.count-prover.mjs`
executes it over this Markdown and the JSON capsule.

| id | case | executed |
|---|---|---|
| **A1** | ⭐ **IDENTITY AND THE WRITE BASE.** For every row of the 63-row stride, `mergeConsequence(record, X, X)` with `X` deep-equal on both sides returns a record BYTE-IDENTICAL to the record; the caller's record is unmutated; **no TOP-LEVEL key of `record` is absent from the result** (⛔ the arm is written at the TOP LEVEL, as the compile measured it — §6 says why the absolute is false at depth, and byte-identity already covers depth here because `R0 ≡ R1` makes every node short-circuit); `escalations` and `readings` are both empty. | 63/63 identical · 0 mutated · 0 keys lost · 0 escalations · 0 readings; re-measured 0 top-level keys lost over 693 trials (P-9) |
| **A2** | ⭐ **THE GROUP IS THE UNIT — a label never sits beside a flag that contradicts it.** With `economicState.foodSecurity.label` differing between `R0` and `R1` and `isSecure` identical in both, the merged group is `R1`'s WHOLE. `enclosingGroup('economicState.foodSecurity.label')` is `'food-security'`; `enclosingGroup('availableServices.legal')` is `null` (a measured NON-group, §22.3 item 4). **The paired negative:** with the group rule off the same inputs produce the contradictory pair. | 63/63 whole · 0 split |
| **A3** | **ABSENT IS A VALUE, AND NOTHING IS MERGED BY POSITION.** When `R0` and `R1` agree on a collection LONGER than the record's, the record's own array stands unpadded; a key both re-derivations carry and the record does not stays ABSENT from the result. | 63/63 · 63/63 |
| **A4** | **KEYED BY THE DECLARED KEY.** With `R1`'s `availableServices.food` permuted and one entry changed, the changed entry joins BY NAME, every other entry is byte-identical to the record's, and the order arm is COUNTED in `delta.orderMoved`. | 31/31 |
| **A5** | **A CROSS-ENTRY TOTAL IS ATOMIC** (§22.3 item 3). `economicState.incomeSources` — whose shares total 100 — comes from `R1` as ONE value, never entry by entry beside settled neighbours. | 61/61 |
| **A6** | ⭐ **THE ORDER RULE'S TWO ARMS**, on `economicState.activeChains` — the READING collection the corpus itself re-ordered 43 of 67 times. ⛔ Never on `institutions`: it is HELD, the tree merge never reaches it, and an arm written there measures 0 and proves nothing. Arm 1: `R0` and `R1` agree on the commons' order ⇒ the record's order stands and `delta.orderMoved` is silent. Arm 2: `R1` reverses them ⇒ `R1`'s order governs and `delta.orderMoved` names the collection. | 55/55 · 55/55 |
| **A7** | **MIRRORS RECOMPUTED, THE RECEIPT RECOMPUTED AND NEVER TAKEN.** `recomputeMirrors` is a no-op on a plain record; after a role change the member chip follows the merged roster. With `R1` carrying a planted digest and a changed prosperity, the merged record's `economyInputFingerprint` is the RECOMPUTED value, never `R1`'s. | 63/63 · 63/63 · 63/63 |
| **A8** | ⭐ **THE CHAIN IS THE SEMANTICS.** An ARRAY of edits throws `TypeError` with the law in the message; a single edit yields `delta.appliedEdit` as a STRING; `delta.nextBase` is `R1`; `delta.orphaned` is the op's EM-R6 rows verbatim and `[]` when the op has none; and the module's export list is enumerated in the assertion so **no exported symbol takes more than one edit**. | throws · string · true · verbatim · 5 exports enumerated |

```text
CANNOT-CATCH:
1.  The merge over the REAL re-derivation. A1–A8 take R0 and R1 as inputs; EM-R1–R5 are unbuilt.
    EM-R7's corpus ratchet is the only place the seam and the merge meet.
2.  The 504-trial corpus figures themselves — identity 63/63 through the seam, zero escalations,
    the MIXED census, the order arm's 52/504, the 36 % honesty figure. Measured at this compile
    (E-3, E-4) and re-measured by EM-R7; not assertable in this file.
3.  The cross-key escalation (step 1 taking BOTH keys). EM-R0b VERSION 3 now supplies `CHECK_META`
    and seven derived `CROSS_KEY_CHECKS` (STOP-4 discharged), but THE ARM IS STILL UNEXERCISED
    HERE: the prototype instrument is the recon's 25-check version with no cross-key entry, and the
    corpus produced ZERO escalations, so nothing measured in this packet depends on it. EM-R7 is
    where the ladder meets real cross-key data.
9.  ⭐ **THE `history-age` GROUP'S EFFECT ON THE MERGED VALUE.** V14 proves the group arm and the
    order arm do not interfere, and that `members: null` would silence the order arm. It does NOT
    prove the group changes any merged value here: `history` re-derives faithfully in this corpus
    (the record's order never settles away from `R0` — 0 of 693), so the merged `history` is
    byte-identical with the member list, with `members: null` in 674 of 693, and with no group at
    all in 693 of 693. ⛔ **G8 is PREVENTIVE and its value-population in the 63-row corpus is
    ZERO** — the split addendum 38 measured lives in EM-R0b's own instrument, and the corpus that
    would open the seam is EM-R7's.
10. Whether the 3 AGE-MOVING world-fact edits V14 runs are edits the editor will actually offer.
    They are the ONLY edits that move `history.age` (its sole producer is
    `resolveSettlementAge(tier, config)`, `src/generators/historyGenerator.js`, read from
    `config.settlementAgeMode` / `settlementAgeYears`), and the corpus's own eight move it 0 times
    in 504. The EM-A1 root-field surface decides whether a DM can reach them.
4.  Whether the four MIXED objects outside a declared group (E-4.3) are missing groups. None
    produced a conviction; the chair rules, and EM-R7 holds the family to zero.
5.  The byte price. Zero bytes land while nothing imports these leaves; EM-B2a's pre-proof prices
    them.
6.  Whether the record's key order matters downstream. This packet preserves it (E-2) but nothing
    in the estate asserts it.
7.  merge∘merge vs merge(both) beyond five pairs. Five were run (E-5), one disagrees; all 28
    ordered pairs are EM-R7's.
8.  A DM edit outside the eight-edit corpus. The runtime guard is what covers it, by design.
```

---

## 10. Verification commands

```bash
# Focused static checks
npx eslint src/domain/edit/mergeConsequence.js src/domain/edit/recordMergeTree.js tests/domain/recordMerge.test.js
npm run typecheck:ratchet          # tsconfig.full.json includes src/domain/**/*.js — both leaves are in the gate's own config
npm run typecheck:domain:strict    # STOP-3: the strict ceiling is a HARD ZERO, not a shrinking baseline (V11)

# Focused tests: acquire the one test slot in the same command
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/domain/recordMerge.test.js

# The boundary ratchet: this packet adds no domain -> generators edge
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/build/domainGeneratorsBoundary.test.js

# Named golden proof: the posture is UNCHANGED and motion is a STOP
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js

# ⭐ EM-R6's DEFERRED FIRST-PAINT DOOR (§7). A MEMBERSHIP probe, not a byte arm: it imports
# vite.config.js's own exported set, reads no dist, and therefore can never skip. Exit 1 if
# either EM-R6 leaf has entered the eager closure. Measured at bdbf7c89c: 269 modules, NONE.
node -e "const{pathToFileURL}=require('node:url');import(pathToFileURL('vite.config.js').href).then(m=>{const S=Array.from(m.EAGER_FIRST_PAINT_MODULES);const bad=['src/domain/institutionRename.js','src/domain/institutionRemoval.js'].filter(p=>S.some(x=>String(x).includes(p)));console.log('EAGER_FIRST_PAINT_MODULES:',S.length,'| EM-R6 leaves in the eager closure:',bad.length?bad.join(', '):'NONE');if(bad.length)process.exit(1);})"

```bash
# ⭐⭐ THE SIX GOVERNING LINT WALKERS, SEALED (added by the EM-T16 pre-proof; V17-V21). Each one
# governs a directory this packet CREATEs into, each is measured at ZERO on this packet's planted
# text, and none of the six can red by design under a train -- so sealing them is lawful where
# sealing the directory run is not. Two arrays, one test directory each.
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/tuningRegister.walker.test.js tests/lint/proseNumerics.test.js \
  tests/lint/domainAnyCastBaseline.test.js tests/lint/ruinFilterRoster.walker.test.js \
  tests/lint/entropyRootCensus.walker.test.js tests/lint/seedLoopTotality.walker.test.js
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/copy/voiceMechanics.test.js
```

```bash
# LAST in the sealed set, the position 37 of the 38 landed rows that name the script take.
# (Added by ESTATE-REPAIR-2 under the chair's slot, item E.)
node scripts/implementation-packets.mjs validate
```

⛔ **TWO WALKERS ARE DELIBERATELY ABSENT FROM THE SEALED SET AND RUN ONLY AS §10 INSTRUMENTS**, for
the same reason: `tests/lint/sovereigntyLightingContract.walker.test.js` (it reds BY DESIGN under a
train -- §7's interior red) and `tests/lint/proseWiringCensus.walker.test.js` (whose producer-count
arm §7 DECLARES as an interior red, regenerated at the terminal by the chair). A guaranteed-red
suite inside `checks` makes `check:packet` unpassable.

```bash
# ⭐ INSTRUMENT, NOT A SEALED CHECK (addendum 70 Q-D: sealing the 189-file run would bake the
# lighting INTERIOR RED into `check:packet`). Run it ONCE, read it by hand, and report.
# ⛔ OWED BECAUSE THIS PACKET CREATES A FILE UNDER tests/ (the chair's 2026-09-20 addendum: a CREATE
#    under tests/<dir> opts into EVERY walker governing tests/<dir>). SIX walkers walk tests/ WHOLE
#    and read tests/domain/recordMerge.test.js — seedLoopTotality (⛔ see §7's construction rule),
#    negativeAssertionAnchor, sovereigntyLightingContract, goldenFreeze, controlBytes and
#    worldGenerationClockSeam. Expect EXACTLY ONE red: the lighting census's interior tuple.
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 tests/lint
```

⛔ **No command in the SEALED set writes a file**, so no generated path is owed and §P2.12's
generator-last rule has nothing to order. ⛔ A gate line with no printed test count DID NOT RUN (`gate-mutex.sh` gives up
after its poll budget and **exits 3**, `GAVE UP` — ⛔ never 0; the chair's retraction of 2026-09-20). Never read a gate through a shell pipe; use `npm run check:tail`
or `sh scripts/gate-tail.sh`. The lane never runs `npm run check`; the full gate moves to the
train's terminal.

---

## 11. Mandatory STOP conditions

- **STOP-1 — a golden moves.** `generatorGoldenMaster` or `dossierProseManifest` shifting by one byte
  is a STOP, not a re-record (§P3.1, HZ-GOLDEN).
- **STOP-2 — the boundary ratchet reds.** If `tests/build/domainGeneratorsBoundary.test.js` reports a
  FIFTH domain→generators file, the fingerprint import took the generators address. Fix the import;
  **never widen the baseline** (V6, E-7).
- **STOP-3 — a leaf is not strict-clean.** `scripts/check-domain-strict.mjs`'s debt baseline can only
  shrink and refuses to absorb a new file. Annotate the leaf; never run `--update` (V11).
- **STOP-4 — ✅ DISCHARGED at the pre-proof, and KEPT as a landing-time guard.** EM-R0b **version 3**
  (ODQ §934.47 addendum 38) ships `CHECK_META` — 33 rows, total in both directions, `CROSS_KEY_CHECKS`
  DERIVED — and re-exports it from `recordInvariants.js` expressly so this packet's compiled import
  is satisfied verbatim (V8). §6's import line is unchanged and `CHECK_META` is now an owed
  `requiredSymbols` row. **The guard that remains:** if the implementer reaches
  `import { recordInvariants, CHECK_META } from './recordInvariants.js'` and `CHECK_META` is absent
  at the stamped base, **STOP for the chair** — EM-R0b landed at a version before 3. Do NOT
  re-declare the seven cross-key ids here: that would spell EM-R0b's table twice and drift the
  moment a check is added.
- ⭐ **STOP-9 — `seedLoopTotality.walker.test.js` convicts the new test file.** Its frozen habitat is
  SHRINK-ONLY and its baseline is outside this manifest, so a conviction cannot be absorbed. Rebuild
  the loop to §7's construction rule (the precedent's `for (const c of rows)` shape) — **never add a
  frozen row, and never raise one** (V15).
- **STOP-5 — an escalation fires on the corpus.** The family's contract is ZERO escalations; one that
  fires is a MISSING GROUP DECLARATION and belongs to EM-R0a, not to a local patch here.
- **STOP-6 — a MIXED object outside a declared group produces a CONVICTION.** Report it with its
  leaves to the chair. **Never invent a group** (§22.2 item 2; the launch file's instruction).
- **STOP-7 — the budget.** Any leaf above 250 effective lines, the packet above 400, or a THIRD logic
  leaf. Split; never renegotiate.
- **STOP-8 — a substrate file is absent at the stamped base** (§8 step 1).
- ⭐⛔ **STOP-10 — ANY OF THE FIVE ZERO-HELD INSTRUMENTS MOVES.** `tests/copy/voiceMechanics.test.js`
  (em / bang), `tests/lint/tuningRegister.walker.test.js` (P2 / P3),
  `tests/lint/domainAnyCastBaseline.test.js` (`any` / `suppress`),
  `tests/lint/proseNumerics.test.js` and `tests/lint/entropyRootCensus.walker.test.js` each hold a
  NEW `src/domain` file at ZERO, and §7's CREATE rows are what buy it. A move is a STOP for the
  chair, **never a baseline edit** -- every one of those baselines is outside this manifest. The
  same for `tests/lint/ruinFilterRoster.walker.test.js`: if its discovery set names either leaf, the
  implementer wrote a roster read as a property chain, and the cure is the read, not an exempt row.
- ⭐⛔ **STOP-11 — a `src/generators` specifier appears in either leaf.**
  `tests/property/dmLayerGoldenIsolation.test.js :: A8` walks `src/domain/edit` WHOLE and forbids it,
  static or dynamic (V17), and the boundary ratchet forbids the same edge from the other side
  (STOP-2). The fingerprint import is `../../data/economyFingerprint.js`; there is no second lawful
  spelling.

---

## 12. Completion receipt

The build lane records, each executed and quoted:

1. The two CREATE targets absent before the first edit, and `git status` clean.
2. `npx eslint` clean on both leaves and the test file; `npm run typecheck:domain:strict` green with
   the debt baseline UNMOVED (quote the printed total).
3. `tests/domain/recordMerge.test.js` — eight `it` titles, all green, with the printed test count.
4. `tests/build/domainGeneratorsBoundary.test.js` green: **the live edge set is still four files /
   five edges.**
5. `tests/property/generatorGoldenMaster.test.js` and `tests/property/dossierProseManifest.test.js`
   green and UNCHANGED.
6. The effective-line count of each landed leaf under eslint `max-lines`
   `{ skipBlankLines, skipComments }`, against 250 and 400.
7. The lighting-census DELTA this packet causes — `files +1 · parked +0 · credited +1 · titles +8 ·
   suiteTitles +1` — restated as an INTERIOR RED for the chair's terminal refreeze, **with no
   absolute tuple**, and CONFIRMED against the walker's own `parkReasonsFor` on the landed file
   (it must print `[]`).
7b. ⭐ **THE FIVE ZERO-HELD INSTRUMENTS, RE-MEASURED ON THE LANDED FILES** and quoted: voice
   `em 0 · bang 0` per leaf; tuning `P2 0 · P3 0` per leaf; any-cast `{ any: 0, suppress: 0 }` per
   leaf; prose-numerics `{}` per leaf; entropy `createPRNG(` still 36 / 47 and the ruin-filter's
   `readers.length` still 94. Each is a §10 sealed check, so each prints its own count.
7c. ⭐ **THE WIRING-CENSUS DELTA `stamp.producerIndexFiles +2`**, restated as an interior red for the
   chair's terminal re-take, with the two paths named and NO absolute claimed.
7d. ⭐ **THE MEMBERSHIP PROBE's line, quoted with its figure** (`EAGER_FIRST_PAINT_MODULES: <N> |
   EM-R6 leaves in the eager closure: NONE`, exit 0) and the **declared byte delta `+0 B` per
   closure**, handed to EM-R1b as train EM-T16's byte-arm holder.
8. ⭐ **`tests/lint` WHOLE, run once as an instrument** (§10), with the printed test count and the
   list of reds. **EXACTLY ONE red is expected — the lighting census's interior tuple.** ⛔ A red
   from `seedLoopTotality.walker.test.js` is STOP-9, not a baseline edit; a red from
   `negativeAssertionAnchor` is a missing `// anchored:`; any other red is reported to the chair
   before the seal.
9. `git show --stat HEAD` naming ONLY this packet's three files.
