# Settlement editor / EM-B2a3 — `assembleInstitutions` AND `generatePower` CONSULT PINS, `provides` WHOLE: the institution and power choosers taught the landed seam, with the battery that convicts a pin that does not reproduce

- **Status:** `LANDED`
  ⚠ The status value above stands ALONE on its line because `parsePacketHeader`
  (`scripts/implementation-packets.mjs`) anchors the status row at end-of-line and takes `status`
  only when exactly one row matches. Every stamp, caveat and date goes on these continuation
  lines, never on the row.
  ⭐ **NARROWED TO WHAT IS PROVABLE AT THIS MEMBER'S LANDING**, per the chair's correction of its
  own launch brief (2026-09-20, from the EM-B2a4 seat): the brief's blanket acceptance sentence
  *"pinned re-derive reproduces the record's provided keys byte for byte over a stride of the
  golden corpus"* is WITHDRAWN, and this packet SEALS only the survivals it measured as TOTAL over
  the declared 63-row census corpus. §0 carries the whole measurement, sealed and unsealed halves
  separated, each figure with its as-of mark.
  ⭐ MEMBER 3 OF 4 of the superseded **EM-B2a**, per the chair's ESTATE-REPAIR and
  COMPILE-EM-B2a1 rulings of 2026-09-20: **EM-B2a1** (the leaf, INSTALLED) → **EM-B2a2** (the
  seam + the heavy chooser) → **EM-B2a3** (this) → **EM-B2a4** (the store consumer + the
  orchestration). The four-member partition is `EM-B2a.partition.md`.
- **Landed at:** `2117054e816cdbe5ca86300fe044460dfd648687` — the twenty-ninth landing — train EM-T12 (version 3; its build 608b0d63d and its FIX-2 cure 2117054e8, judgment 137, the pass-1A verifier's finding: A3 now sees the nested setActiveRng(createPRNG) mint through a vi.hoisted pass-through recorder drained per step): assembleInstitutions and generatePower consult the pin seam for every key they provide (5 files, +550/−14 at the build; +75/−7 at the cure); §11's EM-R1 STOP discharged at placement (judgment 133b) and §12 written at this flip with the lane's two measured observations (34/63 and 62/63, as of 04ac6520e; both EM-R3's seam)
- **Packet version:** 3
  - ⭐ **Version 3 (the Opus pre-proof for train EM-T12, read tip `583f8f644`)** re-measures version 2
    against the live tip and makes NINE edits, none of them a contract change: the `chooseOrPin`
    dependency becomes a `requiredSymbols` row so the validator refuses this packet before EM-B2a2
    has landed (measured: ONE error before, ZERO after); the import is declared WIDENED rather than
    ADDED, which makes the six prose-numerics rows UNMOVED and cancels the terminal's re-address;
    the tuning inventory (interim rule 17, which did not exist when version 2 was compiled), the
    Tier-5 voice ratchet, the writer-reach census and the observed-shape manifest join the measured
    "registers that do not move"; the CREATE row's `it` count and the lighting `titles` delta are
    BOUND to the seven acceptance cases and a count prover ships beside the packet; §6's two
    cross-references to `A2` are re-pointed at `A4`, where §9 homes those two arms; and every
    spelling that placed the pin primitive at its OLD home is re-pointed at EM-B2a2 version 2's
    exported home. ⛔ No contract, budget, acceptance case, change row, sealed check, figure, BLOCK
    or non-goal moved.
  - Version 2 was the ONE-FIELD repair only (ESTATE-REPAIR-4b, 2026-09-20, read tip
    `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`), on the chair's ESTATE-REPAIR-4 item (d): the
    capsule's `REGISTER` row gains the **`rowKey`** TOOL-27 keys its row-level exemption on —
    `invariants['tests/generators/pipelinePinnedChoosers.test.js']`, the spelling this row's own
    `_note` and §7 already carry — as a SIBLING key of `action`/`path`, in the exact shape the
    chair installed on EM-B2a4. ⛔ **Nothing else in this packet is touched**: no contract, budget,
    acceptance case, required symbol, change row, sealed check, figure, BLOCK or non-goal moved,
    and §7 and the capsule remain SET-EQUAL on all five rows.
- **Verified base:** `em-t12-b2a3-2026-09-21` at `efd0eb4481669fce0fa00c356fef9399ab374d17`
  ⚠ Left for the chair's promotion stamp. ⭐ **RE-MEASURED AT `583f8f644` (train EM-T10's promotion
  commit) BY THIS PRE-PROOF:** the J-T1 window from `bdbf7c89c` over every change-manifest path and
  every `requiredSymbols` path is EMPTY but for `scripts/mutation-coverage-manifest.json`
  (`1 file changed, 14 insertions(+)` — three rows added by train EM-T9, none of them this
  member's `rowKey`); all EIGHT version-2 required symbols are present VERBATIM at the SAME line
  numbers §5 states (`:216`, `:34`, `:426`+`:492`, `:149`, `:171`, `:118`, `:154`, `:72`); the
  CREATE target is ABSENT and the four other targets PRESENT; `shasum -a 256` on the preamble
  re-reads `c9f33c8d…c46675`, UNMOVED. **The ninth required symbol (version 3's) is ABSENT and
  MUST BE**, until EM-B2a2 lands. **The revalidation sentence the chair will use:**
  *"Re-measured at `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d` (read tip `read-tip-em-t7`,
  detached, `git status --short` EMPTY): the CREATE target `tests/generators/pipelinePinnedChoosers.test.js`
  ABSENT; both MODIFY targets present (`assembleInstitutions.js` 755 raw / 571 effective,
  `generatePower.js` 161 raw / 100 effective); both REGISTER/TEST targets present; all eight
  `requiredSymbols` present VERBATIM at the paths and counts in §5; `retiredSymbols` empty; the
  preamble measured `c9f33c8d…c46675`."* ⛔ A `__BASE__` packet can NEVER pass
  `validate:packets`; validation is downstream of this stamp, never a precondition of it.
- **Last revalidated:** left for the chair, with the sentence above.
- **Depends on:** **EM-R1** (pins cloned on entry — a HARD dependency, measured in §0 W and §9 A6:
  without it an uncloned record-built pin mutates the caller's own record in 52/63 and 30/63 rows
  and throws in 2/63). ⛔⛔ **EM-R1 IS CHARTERED BUT UNWRITTEN — MEASURED AT THIS TIP:** it exists
  as three charter rows in `docs/implementation/charters/EDIT-MODE-TRAIN.md` (`:132`, `:137`,
  `:178`) and as NO packet: `docs/implementation/packets/settlement-editor/` holds 25 packets and
  none of them is an `EM-R1`, and the chair's `packets-waiting/` holds none either. So this
  member's hard dependency cannot be satisfied by any train now placeable, and **either the chair
  rules §4a's Q4 the other way (a LOCAL clone in this member's two consults, which EM-R1's landing
  retires) or this packet waits on a packet nobody has compiled.** · **EM-B2a2 VERSION 2**
  (INSTALLED in `packets-waiting/`, placed on train **EM-T11** by judgment 100: it exports the
  shared pin primitive this member imports from `src/generators/pipeline.js`; see §5 and §6) and, through it, the LANDED **EM-P0** (the runner's pins channel) and **EM-P2**
  (`GENERATION_TIER1`, the census this member's contract reads). **EM-B2a1** need not land first —
  this member imports no symbol of the leaf.
- **Collision group:** **EM-B2a2** at the *symbol* level only (this member imports a2's export;
  their FILE sets are disjoint — measured in §5a). **EM-B2a4** on
  `scripts/mutation-coverage-manifest.json`, which both carry a row on: under today's validator
  they cannot be non-terminal together, which the chair's a3 → a4 chain already imposes
  (question **Q5**). No other waiting packet names either of this member's two source files.
- **Commit authority:** edits only; the chair commits
- **Baseline posture:** measured at the read tip — **no `scripts/.size-baseline.json` row exists
  for either source file** (grep, exit 1), so the generator layer's `max-lines` 800 governs
  (`skipBlankLines` + `skipComments`); measured headroom **229** effective lines for
  `assembleInstitutions.js` (571) and **700** for `generatePower.js` (100). Neither file is in
  `docs/content/wiring-census.json`'s `stamp.files` (the seven `src/domain/display/stateProse/`
  producers), so no census re-take is owed. Every register figure this packet predicts is a
  DELTA; the live absolutes are the chair's to stamp.
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: TO BE STAMPED BY THE CHAIR)
  > **Interim compile rules:** `COMPILE-RULES.interim.md` (the chair, 2026-09-20) — the fifth
  > amendment's rules (**1–17** as of 2026-09-21), obeyed before they land. Where this packet and `EM-PREAMBLE.md`
  > (SHA-256 `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`, **re-measured at read tip
  > `583f8f644` with `shasum -a 256` and UNMOVED**) disagree, the interim rules govern and this row is the record
  > of it.
- **Evidence:** every VERIFIED row below is receipted in `EM-B2a3.evidence.md` by command and output.

---

## §0 · THE MEASUREMENT — what this member reproduces, what it does not, and whose seam the rest is

**The instrument, and its control first.** Every figure below is EXECUTED at `bdbf7c89c` over the
DECLARED 63-ROW CENSUS CORPUS (`tests/helpers/generationForkCensus.js#censusCorpus`), with the
whole-`provides` pin built FROM THE RECORD and honoured at the step boundary. ⭐ **THE CONTROL:
injecting each step's OWN PRODUCED values reproduces the whole settlement 63/63 and 63/63**, so
every figure short of total below is the pin's content and not the harness.

### §0.S · PER-KEY SURVIVAL, N/63, as of `bdbf7c89c`

| record key | `assembleInstitutions` pinned | `generatePower` pinned |
|---|---:|---:|
| **whole settlement** | **7/63** ⛔ | **63/63** ⭐ |
| `institutions` | 11/63 ⛔ | 63/63 |
| `economicState` | 10/63 ⛔ | 63/63 |
| `availableServices` | 10/63 ⛔ | 63/63 |
| `simulationTrace` | 7/63 ⛔ | 63/63 |
| `economicViability` | 25/63 ⛔ | 63/63 |
| `resourceAnalysis` | 32/63 ⛔ | 63/63 |
| `npcs` · `factions` | 38/63 ⛔ | 63/63 |
| `powerStructure` | 40/63 ⛔ | 63/63 |
| `generationCoherenceReceipt` · `defenseProfile` | 46/63 ⛔ | 63/63 |
| `relationships` · `conflicts` | 49/63 ⛔ | 63/63 |
| `history` | 54/63 ⛔ | 63/63 |
| `spatialLayout` | 57/63 ⛔ | 63/63 |
| `isolationSupport` | 61/63 ⛔ | 63/63 |
| `stress` · `name` · `population` | **63/63** | 63/63 |

⭐ **`generatePower`'s record-built whole-`provides` pin reproduces the settlement TOTALLY.** That
is this member's sealed claim (§9 A5a). It holds even though `record.powerStructure` carries an
extra `conflicts` key `assembleSettlement` adds and the step never wrote, and even though
`powerIntent` is off-record and had to be supplied as the produced value (question **Q2**).

⛔ **`assembleInstitutions`'s does not, and the later writers are why.** `record.institutions` is
the roster after five in-place mutators; `cascadePass` DRAWS (register `stepDraws 56`,
`keyMoves 56`) and added 9 entries on the probe row (44 produced → 53 on the record → 58 under the
pin). **`subsumptionPass`, `cascadePass` and `factionCorrelationPass` each declare `provides: []`
and are therefore UNPINNABLE by a mechanism keyed on `provides`**; `isolationPass` and
`coherenceRepairPass` mutate the roster while providing other keys. Those five are **EM-R1's
writer census**, not this member's, and design §22 ruling 1 is the model that reaches them:
*"'Pinned at a step boundary' is withdrawn as the model; 'final at every writer' replaces it."*
The later writers of `powerStructure` — `neighbourFactions`, `powerEconomyReconcilePass` (both
`provides: []`) and `assembleSettlement` — are **EM-R3's**, and the 63/63 above is the measurement
that this member's producer half does not need them.

### §0.W · THE PIN BAG IS WRITTEN THROUGH UNLESS IT IS CLONED, as of `bdbf7c89c`

Measured with the pin handed back BY REFERENCE, which is exactly what
`return chooseOrPin(pins, key, draw)` does:

| | uncloned | cloned (`structuredClone`) |
|---|---:|---:|
| `assembleInstitutions` — the caller's `record.institutions` MUTATED | **52/63** ⛔ | **0/63** |
| `generatePower` — the caller's `record.powerStructure` MUTATED | **30/63** ⛔ | **0/63** |
| rows that THREW | **2/63** ⛔ | **0/63** |

The throw, quoted: `TypeError: Cannot add property 1, object is not extensible` at
`src/generators/steps/coherenceRepairPass.js:134` — `record.generationCoherenceReceipt.repairs` is
FROZEN at `src/generators/generationCoherence.js:515`, and an uncloned pin hands that frozen array
straight into a `push`. This is the design's own measured defect (*"a record-built pin bag is
MUTATED IN PLACE"*) reproduced at this member's two steps. ⇒ **the clone is a PRECONDITION, and it
is EM-R1's** (charter row EM-R1, "pins cloned on entry"; design §22 ruling 6, `structuredClone`,
always). This member mints no second spelling of it — question **Q4**.

### §0.D · THE DRAW-SKIP'S COST, MEASURED, and the rule that defers it

The primitive does not advance the stream (quoted in §6; at `583f8f644` it is still at its OLD home,
`src/generators/steps/generatePopulation.js:41-44`, and at THIS packet's own base it is at EM-B2a2
version 2's EXPORTED home, `src/generators/pipeline.js`), so
a pinned step skips its body and emits none of the `recordTrace` calls that body makes. Measured
over 9 corpus rows: `assembleInstitutions` emits **236** trace entries, non-zero in **9 of 9** rows
(7 at thorp to 50 at metropolis); `generatePower` emits **56**, non-zero in **9 of 9**; strip them
and the record is not byte-identical (95 → 42 entries on the probe row). Design §22.2 ruling 11:
*"Draw-SKIP is legal only under ruling 8's partition. The family's first cut CONSUMES AND DISCARDS
at every held writer outside `enrichNpcCoherence`; the skip of 3,324 + 985 draws is a later,
measured optimisation that lands only after the trace partition."* ⇒ whether this member SKIPS or
CONSUMES-AND-DISCARDS is **question Q1**, and `simulationTrace`'s 7/63 above is the skip's price.

### §0.K · TWO OF THE FIVE CHOOSER KEYS ARE NOT ON THE RECORD

`catalogForTier` and `powerIntent` are ABSENT from the record in **13 of 13** sampled rows (the
record's whole top-level key list is quoted in the evidence), while `runPipeline`'s rule refuses a
pin set that omits either. Both are re-derivable without a draw — `catalogForTier` from
`record.tier` through the step's own expression, `powerIntent` from `record._seed`,
`record.config` and `tier` — so a caller CAN build the set, but it re-derives rather than reads.
Design §21.5.4 names both in its five-key absent list; §21.5.3 says `powerIntent` is *"not pinnable
from a record as the runner's rule stands"*; and §22 ruling 6 **withdrew EM-P0b**, the charter that
would have narrowed the refusal. ⇒ **question Q2**.

## §1 · Reconciled authority

1. ⭐ **`docs/DESIGN_EDIT_MODE_AND_DECREES.md` §14 as AMENDED by §21.5 and §22–§22.4** (the
   owner-ratified design, read whole at the ledger tip). §14 gives the engine —
   *"`rederive(record, config′, layer)` … every registered chooser whose output the record holds
   takes the record's value instead of drawing"*; §21.5 and §22 amend it by measurement, and where
   they and §14 disagree **the later section governs** (§14 carries its own amendment banner:
   *"⚠ AMENDED BY §21.5.3 (measured)"*).
2. **THE CHAIR'S RULING ON THE EM-B2a PARTITION AND EM-B2a1** (2026-09-20, judgment 30): the
   mechanism is the landed pin consult; a member that teaches a step to consult pins owes that
   step's `provides` WHOLE; `generatePower` JOINS this member's paths; the three power helpers are
   re-measured and the 3 → 4 override used only if the measurement needs it (**it does not** — §5a).
3. **`COMPILE-RULES.interim.md`** (the chair, 2026-09-20), which governs where it and the preamble
   disagree — see the header row.
4. **THE PROMISE** (constitutional): a seed is a STARTING world forever; lived history is immutable.
   Design §22.2 item 7 applies it to `generationCoherenceReceipt`, one of this member's own
   record paths.
5. `docs/implementation/PACKET_STANDARD.md`, `docs/implementation/preambles/EM-PREAMBLE.md`,
   `docs/implementation/charters/EDIT-MODE-TRAIN.md` wave 1 row **EM-B2** (as split).
6. **Live git state at the verified base — which outranks all of the above on what exists.**

### §1a · ⭐ WHERE THIS MEMBER SITS IN THE RE-ENTRY FAMILY

Design §22 item 3 states the gap in one sentence: *"The pin seam exists in ONE step of
twenty-two."* **This member makes it three**, and that is the whole of its ambition. It is the
PRODUCER half of EM-R1's writer census for two steps; it is not `rederive`, it is not the no-edit
identity, and it makes no claim about what the pipeline reproduces beyond §0.S.

| | what it needs LANDED first | what needs THIS member first |
|---|---|---|
| **EM-R1** — pins cloned on entry + the writer census walker (producer · mutator · enricher · replay · mint · MIRROR) | ⭐ **HARD DEPENDENCY.** §0.W: without the clone this member's own steps let a later pass mutate the caller's record 52/63 and 30/63, and throw 2/63 | EM-R1's census counts this member's two steps in its PRODUCER class; the walker's denominator is complete only once they consult |
| **EM-R2** — the roster final inside `enrichNpcCoherence`; `corruptionPass` leaves a held roster alone | not needed: this member's keys are not `npcs`/`factions` | no |
| **EM-R3** — the power structure final: the replay does not run under a held structure; the freshness assert pin-aware at its three call sites | ⚠ not needed for §0.S's 63/63, which was measured WITHOUT it — but EM-R3 is the member that makes a held power structure survive an EDIT, which is what the power card needs. **Question Q3** on the ordering | ⭐ **YES** — `generatePower` is `powerStructure`'s PRODUCER, and EM-R3 holds it at `reconcilePowerStructure`, `neighbourFactions` and `powerEconomyReconcilePass`. EM-R3's seam is downstream of this one |
| **EM-R4** — the settlement NAME held through assembly | no; `name` already survives 63/63 under this member's pins | no |
| **EM-R5** — the trace partitioned by every step ABOUT a held key | ⚠ **precedes any draw-SKIP** (charter row EM-R5, verbatim). §0.D is why: `simulationTrace` is 7/63 under a skipping pin. If Q1 rules CONSUME-AND-DISCARD this member does not wait on EM-R5; if it rules SKIP, it does | EM-R5's partition must name these two steps' entries |
| **EM-R0c** — the three-way merge, the no-edit identity | no | no — the merge consumes `rederive`, which is EM-B2a4's |

⛔ **THE CHARTER'S OWN ORDER** (`docs/implementation/charters/EDIT-MODE-TRAIN.md`, the
"To compile" row): *"EM-R1–R5, R7; … the later waves (EM-B2a, EM-C1, the tick hook, the
surfaces)."* EM-B2a follows the EM-R family, and this member is compiled to be correct under that
order rather than to pre-empt it.

**Resolved contradictions** (each settled by measurement, the losing text named so nobody
re-litigates it):

- **The partition's §0 table says `generatePower`'s `provides` is the pin set "the member owes
  WHOLE" at `:34`** → **CONFIRMED VERBATIM** through the live registry, not the comment:
  `getStepMeta()` returns `provides=[powerIntent, powerStructure]` for `generatePower` and
  `provides=[institutions, catalogForTier, generationRepairs]` for `assembleInstitutions`,
  `mutates=[]` and `scratch=[]` for both.
- **The partition's §1.5 row "a3 lighting `+0`"** → **REFUTED**: it was written on the assumption
  that this member creates no test file. It creates one, so the delta is
  `+1 file · +0 parked · +1 credited · +T titles · +1 suiteTitle` (§7).
- **The partition's §1.5 row "a3 mutation coverage: not owed"** → **REFUTED by measurement**:
  `tests/generators` is in `ENFORCER_DIRS` and `enumerateInvariants` picks EVERY `.test.js` under
  an enforcer dir regardless of `NAME_PATTERN`, so this member's CREATE owes an `invariants` row.
- **The partition's row 18 address `densityAscension.js:142`** → refreshed to **`:149`** by the
  partition itself and **re-confirmed here** (count 1).
- **The partition's row 17 count** → **re-executed: 2**, first at `:426`, again at `:492`.
- **The superseded parent's §7/capsule rows M-5, M-6, M-7** (`relationshipArchetypes.js`,
  `stressFactions.js`, `rulingStructure.js`) → **MEASURED OUT** (§5a). They are not registered
  steps and are reachable from this member's steps only through `projectPowerGenerationIntent`.

The implementer does not read other documents to reinterpret this packet.

## §2 · Outcome

**Observable result:** `assembleInstitutions` and `generatePower` consult the shared pin primitive
for every key they provide, so a caller that supplies a lawful pin set for either step gets that
step's declared values back without the step producing them, and a caller that supplies none gets
today's generation byte for byte. The pin seam goes from one step of twenty-two to three.

**Definition of done:** all seven acceptance cases of §9 pass;
`tests/property/generatorGoldenMaster.test.js` and `tests/property/dossierProseManifest.test.js`
are byte-unmoved; `GENERATION_TIER1` moves no row; and §0's measured figures are re-taken at the
landing and recorded in §12 with the landing sha as their new as-of mark.

**In scope**

1. the pin consult inside `src/generators/steps/assembleInstitutions.js`, for all three keys;
2. the pin consult inside `src/generators/steps/generatePower.js`, for both keys;
3. the battery `tests/generators/pipelinePinnedChoosers.test.js`;
4. the two register/citation obligations those three cause (§7).

**Explicit non-goals**

- ⛔ **the pin primitive itself** — EM-B2a2 lifts and exports it; this member imports it and mints
  no second spelling;
- ⛔ **`pinsFrom`, `rederive`, `regenerateWithLayer`, the unapplied-override list and the store
  seat** — EM-B2a4's;
- ⛔ **the DM layer's leaf** (`layerRead`, `applyEdit`, `mintDmId`) — EM-B2a1's, LANDED or waiting;
  this member imports nothing from `src/domain/edit/**` and the layer never enters
  `src/generators/**`;
- ⛔ **the runner's partial-pin rule** — `src/generators/pipeline.js` is EM-B2a2's file and design
  §22 ruling 6 keeps the rule exactly as EM-P0 landed it;
- ⛔ **the three power helpers** — measured unreachable on the pinned path (§5a);
- ⛔ **`EM-P1b`'s cure for the display-name-keyed hash site** at `assembleInstitutions.js:745`;
  this member re-addresses the citation and asserts nothing about the cure;
- Record adjacent discoveries in the receipt; do not investigate or repair them.

## §3 · Hard scope budget

| Limit | Packet budget | Standard |
|---|---:|---:|
| Behavior families | 1 | 1 |
| New persisted record families | **0** | ≤1 |
| Named state writers | 0 (it adds no writer; it gates two existing producers) | ≤1 |
| Feature flags | 0 | ≤1 |
| User-facing surfaces | 0 (headless) | ≤1 |
| Direct consumers | 0 at this member's landing (EM-B2a4 is the first caller) | ≤2 |
| New logic-bearing production leaves | **0** | ≤2 |
| Existing logic-bearing production files modified | **2** | ≤3 |
| Additional registration-only files | 2 (the mutation-coverage row, the census citation) | ≤3 |
| Handwritten files total | **5** | ≤12 |
| New/changed effective production lines | ≤60 | ≤400 |
| Effective lines per touched file after the edit | `assembleInstitutions.js` ≤620 · `generatePower.js` ≤140, both against the layer's **800** | ≤250 for a NEW leaf |
| Delta in a shared/baselined file | **0** — neither file carries a `scripts/.size-baseline.json` row | ≤15 |
| Acceptance cases | **7** | ≤8 |

Overrides approved before dispatch: **NONE, and none is needed.** ⛔ No budget is raised or
invented here. ⭐ The chair's ruling-2 override (3 → 4 files) is **NOT USED**: the measurement of
§5a puts the file count at **two** source files, below the three the parent broke.

## §4 · Sealed dispatch, preflight, and THE BLOCK

```sh
npm run implementation:dispatch -- EM-B2a3
```

Dry-read against `scripts/implementation-session.mjs`, check by check, at the read tip:

| dispatch check | verdict at `bdbf7c89c` once the chair stamps the base |
|---|---|
| branch name / ancestry | ⚠ the sealed dispatch wants the worktree ON the verified branch; one build lane holds the integration branch in its worktree while the chair's is detached at the tip |
| substrate unchanged since the verified base | **PASSES** once the base is the tip; this member's two MODIFY targets are the substrate and neither has moved |
| CREATE targets ABSENT | **PASSES** — `tests/generators/pipelinePinnedChoosers.test.js` is absent (measured) |
| MODIFY / TEST / REGISTER targets present and clean | **PASSES** — all four exist at the tip |
| every `requiredSymbols` row resolving | **PASSES** — all eight found VERBATIM with their counts (§5) |
| git-clean / foreign dirt fingerprinted without target overlap | no sibling writes either source file (§5a) |

### §4a · THE FOUR QUESTIONS ONLY THE CHAIR CAN ANSWER

Each is recorded with the measurement that raises it and a recommendation; none is decided here.

**Q1 — SKIP OR CONSUME-AND-DISCARD?** The landed primitive SKIPS the draw. §0.D measures the
price: `simulationTrace` survives 7/63 under a skipping pin because the two steps emit 236 and 56
trace entries over 9 rows, non-zero 9/9. Design §22.2 ruling 11 and charter row EM-R1 both rule
the family's first cut CONSUMES AND DISCARDS and defer the skip until EM-R5's trace partition.
**Recommend: CONSUME AND DISCARD**, which makes the member independent of EM-R5 and costs only
run time (a re-derivation is already cheaper than a generation — design §22.1). ⚠ It is NOT
`chooseOrPin`: that primitive returns before `draw()` runs, so **EM-B2a2 would owe a SECOND export
beside it** (`consumeOrPin(pins, key, draw)`), and that is a2's declaration to make, which is why
this is one ruling for all three seats. ⛔⛔ **MEASURED AT THIS PRE-PROOF, AND IT IS THE REASON Q1
CAN NO LONGER WAIT: EM-B2a2 VERSION 2 IS INSTALLED, PLACED ON TRAIN EM-T11 (judgment 100), AND
DECLARES EXACTLY ONE EXPORT — `export function chooseOrPin(pins, key, draw)`.** `consumeOrPin` is
spelled in the whole waiting estate ONLY in this packet's own prose (`grep -rn -F 'consumeOrPin'
packets-waiting/` returns this file and its pre-repair backup, nothing else). So a CONSUME-AND-DISCARD
ruling after EM-B2a2 has landed obliges a VERSION 3 of an already-landed member's contract or a
new prerequisite packet, and this member cannot mint the second spelling itself (§2's first
non-goal). ⇒ **Q1 is ruled BEFORE EM-B2a2 is built, or the answer costs a packet.** The ruled texts
disagree and the chair owns the reconciliation: design §22.2 ruling 11 and
`EDIT-MODE-TRAIN.md:178` say the first cut CONSUMES AND DISCARDS outside `enrichNpcCoherence`,
while `EDIT-MODE-TRAIN.md:137` says "a held writer on a SHARED stream consumes its draws and
discards, **only a named child stream is skipped**" — and every registered step's `rng` IS a named
child fork at position 0 (`src/kernel/prng.js`: `fork: (label) => createPRNG(...)`), so under
`:137` these two steps may lawfully SKIP and `chooseOrPin` alone suffices. ⭐ **This lane
recommends the chair rule `:137`'s exemption to govern** — it costs no second export, and §0.D's
`simulationTrace` 7/63 is EM-R5's partition either way — and, if it rules CONSUME-AND-DISCARD
instead, that it does so while EM-B2a2 is still unbuilt.

**Q2 — WHAT IS A PIN FOR AN OFF-RECORD CHOOSER?** §0.K: `catalogForTier` and `powerIntent` are
absent from the record 13/13, and the whole-`provides` rule refuses a set without them. Both are
re-derivable with zero draws. **Recommend: the CALLER re-derives them** — `catalogForTier` from
`record.tier`, `powerIntent` from `record._seed` + `record.config` + `tier` — and EM-B2a4's
`pinsFrom` owns both derivations, so no runner change is needed and EM-P0b stays withdrawn.
⚠ The alternative (narrowing the runner's rule to on-record choosers) is EM-P0b, which design §22
ruling 6 withdrew by name.

**Q3 — DOES THIS MEMBER LAND BEFORE OR AFTER EM-R3?** §0.S measures `generatePower`'s pin at
63/63 WITHOUT EM-R3, so it does not need it for the no-edit identity — but EM-R3 is what makes a
held power structure survive an EDIT, and EM-R3's seam is downstream of this one.
**Recommend: this member lands BEFORE EM-R3**, because EM-R3's arms need a producer that consults
a pin in order to be non-vacuous, and the two are path-disjoint (EM-R3's named files are
`neighbourFactions.js`, `powerEconomyReconcilePass.js`, `economyReconciliation.js` and
`assembleSettlement.js`; this member's are the two steps).

**Q4 — WHO CLONES?** §0.W: uncloned, this member's own steps let a later pass mutate the caller's
record 52/63 and 30/63, and throw 2/63. **Recommend: EM-R1 clones at the runner and this member
declares it a hard dependency**, rather than cloning locally — a local clone would be a second
spelling of EM-R1's rule and would go dead the day EM-R1 lands. ⚠ If the chair wants this member
to land first, it clones in its own two consults and EM-R1's landing retires that code.

**Q5 — THE MUTATION-COVERAGE REGISTER.** This member owes a row on
`scripts/mutation-coverage-manifest.json` (§7, measured), and **four waiting packets already carry
a REGISTER row on the same file** — EM-A1, EM-A3, EM-B1b, EM-B1h — as will EM-B2a4 (partition
M-12). Under today's validator none of them can be non-terminal together.
**Recommend: accept the serialization** (the chair's chain already serializes a3 → a4) and let
**TOOL-27**, which keys ROWS of a row-keyed register, cure the wider collision when it lands; the
row is declared with its `rowKey` in TOOL-27's shape so nothing has to be re-cut.

## §5 · Verified tree contract

Every row re-found BY SYMBOL at the read tip; no line number below is a coding instruction.

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| ⭐ The chooser list, WHOLE | `src/generators/steps/assembleInstitutions.js` | `provides: ['institutions', 'catalogForTier', 'generationRepairs'],` (`:216`, count 1) | the live registry agrees (`getStepMeta()`); `mutates` and `scratch` are both EMPTY, so `provides` is the entire declared write set | PRESERVE. It IS the member's contract: `runPipeline` throws unless the pins cover every key of it |
| ⭐ The second chooser list | `src/generators/steps/generatePower.js` | `provides: ['powerIntent', 'powerStructure'],` (`:34`, count 1) | joined to this member by the chair's ruling; the parent's capsule missed the file entirely | PRESERVE, same law |
| The name join (partition 17) | `src/generators/steps/assembleInstitutions.js` | `institutions.find(i => i.name === instName)` (**count 2**, `:426` and `:492`) | forced-institution resolution BY DISPLAY NAME — the estate's live institution identity (design §22.4: institutions carry no id) | PRESERVE. The member wraps the step's production; it rewrites no name-resolution rule |
| The faction id (partition 18) | `src/generators/density/densityAscension.js` | ``id: `faction.${slug(name)}` `` (`:149`, count 1; the parent's `:142` is stale) | the faction id minted from a display-name slug — the join a `powerStructure` pin must keep honest | PRESERVE, untouched: the member edits nothing under `src/generators/density/` |
| ⛔ The rule that blocks | `src/generators/pipeline.js` | `export function runPipeline` (`:171`, count 1) | the partial-pin refusal is applied to EVERY registered step over ONE flat key namespace; design §22 ruling 6 keeps it exactly as EM-P0 landed it and withdraws EM-P0b | PRESERVE. ⛔ NOT this member's file (EM-B2a2's, partition M-2); named so the constraint is executable |
| The `powerIntent` mint | `src/generators/power/economyReconciliation.js` | `export function createPowerGenerationIntent` (`:118`, count 1) | stores `rngSeed: stepRng.fork(POWER_STREAM).seed` — a SEED STRING, not a live stream; measured `golden-master-v3::generatePower::power-structure`, JSON-serializable, 3,400 B, round-trip identical | PRESERVE. A `powerIntent` pin short-circuits the CALL in the step, never this helper |
| The `powerStructure` mint | `src/generators/power/economyReconciliation.js` | `export function projectPowerGenerationIntent` (`:154`, count 1) | `setActiveRng(createPRNG(intent.rngSeed))` at `:160` — the door every power draw goes through, and the reason `generatePower` reads `stepDraws: 0` | PRESERVE. It is also the ONLY path to the three power helpers (§5a) |
| ⭐ The record-reach census | `src/domain/generation/generationForkRegistry.js` | `export const GENERATION_TIER1` (`:72`, count 1) | the LANDED census, held equal to a fresh execution by `tests/generators/generationForkCensus.test.js`; its verdicts for this member's five keys are quoted in §5b | PRESERVE and IMPORT. The battery reads its rows; it never re-types them (`contractTestAntiVacuity` Rule 2) |
| ⭐ The primitive THIS MEMBER IMPORTS | `src/generators/pipeline.js` | `export function chooseOrPin(pins, key, draw)` (**ABSENT at `583f8f644`; count 0**) | **EM-B2a2 VERSION 2's deliverable** — the primitive's ONE exported home, lifted verbatim out of `generatePopulation.js` into the runner that owns `_PINS_KEY`. It is a `requiredSymbols` row because this member's deliverable CALLS it and must find it unchanged (`LANE-EM-PREPROOF` step 2) | IMPORT. ⛔ This row is what makes the packet REFUSE placement on any train before EM-B2a2 has landed, BY THE VALIDATOR rather than by prose: measured on a copy-only estate, **exactly one error at `583f8f644` (`EM-B2a3.requiredSymbols[8].symbol is missing from src/generators/pipeline.js`) and ZERO with EM-B2a2 simulated as landed** |
| Test precedent (the seam) | `tests/generators/pipelinePinnedMode.test.js` | `describe('EM-P0 — the pipeline seam: pins are consulted per chooser, on one stream')` with A1–A7 | A1 hashes a 41-row golden stride against the committed fixture; A3 counts raw draws on the step's own stream; A4 asserts the partial-pin message verbatim; A7 scans the step source | Copy this proof shape arm for arm at the two new steps |
| ⛔ The line-addressed citation | `tests/generators/generationForkCensus.test.js` | `{ site: 'src/generators/steps/assembleInstitutions.js:745', channel: 'hash', marker: 'inst.name' }` (`:162`) | the census arm reads that LINE LIVE and asserts it still contains the marker, failing with `<site> no longer carries inst.name: re-measure the observed site` | RE-ADDRESS in the same commit. Addresses only — no channel, marker or count is touched |

### §5a · THE FILE LIST, MEASURED — two source files, not four

`relationshipArchetypes.js` exports exactly `computeRelTension`; `stressFactions.js` exports
exactly `applyStressEventFactions`; `rulingStructure.js` exports `generatePowerStructure`,
`normalizeAndAnnotateFactions` and `renormalizeFactionPower`. **None declares `provides:`** — the
`provides:` roster over `src/generators` returns 22 step rows and names none of the three — and all
three are reached from `generatePower` ONLY through `projectPowerGenerationIntent` →
`generatePowerStructure`. A pin that short-circuits that call makes all three unreachable on the
pinned path. ⇒ **the chair's 3 → 4 override is not needed and no fourth file is named.**

**Path-disjointness with EM-B2a2, MEASURED.** a2's paths (partition M-2, M-3, rows 21a/21b):
`src/generators/pipeline.js`, `src/generators/npcGenerator.js`,
`src/generators/steps/generatePopulation.js`. a3's: `src/generators/steps/assembleInstitutions.js`,
`src/generators/steps/generatePower.js`. **Intersection: EMPTY.** The chair's chain
`a1 → (a2 ∥ a3) → a4` holds at the FILE level — but a3 IMPORTS a2's export, so a3 may be COMPILED
in parallel with a2 and may not LAND before it.

### §5b · WHAT THE LANDED CENSUS SAYS ABOUT THIS MEMBER'S FIVE KEYS

| `(step, key)` | `class` | `onRecordClass` | `producedOnRecordClass` | `recordPath` |
|---|---|---|---|---|
| `assembleInstitutions \| institutions` | `drawn` | `same` (63/63) | **`varies`** (same 7 / transformed 56) | `record.institutions` |
| `assembleInstitutions \| catalogForTier` | `pure` | **`absent`** (63/63) | `absent` | **`null`** |
| `assembleInstitutions \| generationRepairs` | `pure` | `varies` (absent 43 / same 20) | `absent` | `record.generationCoherenceReceipt.repairs` |
| `generatePower \| powerIntent` | **`label`** | **`absent`** (63/63) | `absent` | **`null`** |
| `generatePower \| powerStructure` | `pure` | `transformed` (63/63) | `transformed` | `record.powerStructure` |

Re-executed at the read tip over 13 corpus rows: `catalogForTier`, `powerIntent`,
`generationRepairs` and `stressTypes` are absent from the record's own key set **13/13**;
`record.generationCoherenceReceipt.repairs` equals `ctx.generationRepairs` **13/13** and
`record.config.stressTypes` equals `ctx.stressTypes` **13/13**.

**Forbidden alternatives**

- no second pin primitive and no second spelling of the consult — the single-writer rule for the
  seam; EM-B2a2 owns the export;
- no import of `src/domain/edit/**`, `src/store/**` or `src/components/**` into either step — the
  layer never enters `src/generators/**` and `dmLayer.js` never enters the generation-worker
  closure;
- no edit to `src/generators/pipeline.js`, `src/generators/steps/generatePopulation.js`,
  `src/generators/npcGenerator.js` (EM-B2a2's) or to any of the three power helpers;
- no new registered step, no second registration of either step, and no stream minted inside
  either step beyond the `exclusiveCoexist` fork that already exists;
- no change to any `GENERATION_TIER1` row, to `provides`/`mutates`/`reads`/`readsVersion` on any
  step, or to the golden fixtures;
- no file outside the §7 manifest.

## §6 · Exact contracts

### The primitive this member CONSUMES (EM-B2a2's export, compiled to as the partition declares it)

```js
// ⭐ THE HOME THIS MEMBER IMPORTS FROM: src/generators/pipeline.js, exported by EM-B2a2 version 2
// (train EM-T11). Quoted verbatim from the body as it stands at 583f8f644, where it is still at its
// OLD home, src/generators/steps/generatePopulation.js:41; EM-B2a2 lifts that body unchanged.
export function chooseOrPin(pins, key, draw) {
  if (pins && Object.prototype.hasOwnProperty.call(pins, key)) return pins[key];
  return draw();
}
```

⛔ **It does NOT advance the stream on a pin.** That is the landed contract, and it is exactly what
makes §0's B-2 bite. Under **CURE α** this member needs a second primitive beside it —
`consumeOrPin(pins, key, draw)`, which always calls `draw()` and discards its result when a pin is
present — and that is EM-B2a2's export to declare, not this member's to mint. **REPORTED, NOT
CHANGED** (the launch brief's rule for the interface table).

### Inputs and outputs — `assembleInstitutions`

```js
/**
 * The step's three choosers, keyed by the RECORD PATH each writes (the runner's `Pins` typedef).
 * @param {?Record<string, unknown>} pins  ctx.__pins, or null
 * @returns {{ institutions: object[], catalogForTier: object, generationRepairs: object[] }}
 *   - `institutions`      CHOSEN. Pin ⇒ the step's catalog iteration, its forced/out-of-tier
 *                         passes, its custom injection and its collapse/subsumption passes are
 *                         short-circuited. ⛔ 66 own-stream draws on the probe row.
 *   - `catalogForTier`    COMPUTED, zero draws, a pure function of `tier`. Pin ⇒ the merge is
 *                         not run. ⛔ NOT ON THE RECORD (13/13): a caller cannot build this pin
 *                         from a record and the runner refuses a set without it.
 *   - `generationRepairs` COMPUTED, zero draws, from `threatDefensePlan`. Pin ⇒ the threat-defense
 *                         loop is not run. Reconstructible from
 *                         `record.generationCoherenceReceipt.repairs` (equal 13/13).
 */
```

### Inputs and outputs — `generatePower`

```js
/**
 * @returns {{ powerIntent: object, powerStructure: object }}
 *   - `powerIntent`     Zero step-stream draws; ONE inner fork, whose SEED STRING it stores.
 *                       Pin ⇒ `createPowerGenerationIntent` is not called. ⛔ NOT ON THE RECORD
 *                       (13/13); re-derivable from `record._seed` + `record.config` + `tier`.
 *   - `powerStructure`  Zero step-stream draws; every power draw is on a SEPARATELY MINTED
 *                       `createPRNG(intent.rngSeed)` stream. Pin ⇒ `projectPowerGenerationIntent`
 *                       is not called, and with it none of the three power helpers.
 *                       ⭐ REPRODUCIBLE FROM THE RECORD — executed, including the case where
 *                       `record.powerStructure` carries the extra `conflicts` key
 *                       `assembleSettlement` adds and this step never wrote.
 */
```

### Ordering and precedence

- **Pipeline position:** `assembleInstitutions` (phase `institutions`, deps
  `buildGenerationContext, resolveResources, resolveStress, resolveNeighbour`) and `generatePower`
  (phase `power`, deps `generateEconomy, resolveNeighbour`). Neither position moves.
- **THE PIN CLOSURE IS NOT THE STEP.** Computed from `getStepMeta()` and asserted in **A4** (§9 homes the closure arm there; version 2's text said A2):
  this member's institution keys drag in **6 keys across 5 steps** — `catalogForTier,
  generationRepairs, institutions, isolationSupport, stress, stressTypes` over
  `assembleInstitutions, coherenceRepairPass, isolationPass, resolveStress, stressConfirmPass`;
  its power keys drag in **nothing** (2 keys, 1 step).
- **Merge/replace:** a pin REPLACES the step's production for that key; nothing is merged.
- **Tie-break:** none. A key present in `pins` by own-property wins; `undefined` stored as an own
  property is a pin, exactly as the landed primitive reads it.

### Determinism

- **Hash/fork key:** unchanged. `generatePower`'s `POWER_STREAM` fork label and
  `assembleInstitutions`'s `exclusiveCoexist::<tier>::<category>::<name>` fork label are untouched.
- **No-draw behaviour:** ⛔ **THE OPEN QUESTION**, and §0's B-2. Under the landed primitive a pin
  SKIPS the draws; under design §22.2 ruling 11 the family's first cut CONSUMES AND DISCARDS them.
  The packet does not choose (§4).
- **Stable enumeration:** unchanged; the roster's order is the catalog's.

### Flag and dormancy

- **Flag:** `NONE`. Wave 1 is headless and mints no flag.
- **Golden posture:** **UNCHANGED.** With no pins the primitive calls `draw()`, so fresh generation
  is byte-identical BY CONSTRUCTION, and A1 proves it over a ≥40-row stride against the committed
  fixture. ⛔ `UPDATE_GOLDEN=1` is FORBIDDEN to this packet.

### Lifecycle

| Create | Read | Persist | Reload | Regenerate | Undo | Import/migrate | Public veil |
|---|---|---|---|---|---|---|---|
| unchanged — the steps still produce their keys when no pin is supplied | unchanged | ⛔ NOT THIS MEMBER — it writes no record key and persists nothing | ⛔ n/a | ⭐ THIS IS THE MEMBER: a pinned re-derive is the regenerate path, and §0 is the measurement of what it does and does not reproduce | ⛔ EM-C1's; the store's snapshot per design §22.2 ruling 6 | ⛔ unchanged; edits do not travel (design §11) | ⛔ unchanged; no new projected field |

### Receipts and privacy

- **Closed kinds:** none minted. The runner's partial-pin message is the only new-reaching
  receipt and it is EM-P0's text, asserted verbatim in **A4** (§9's home for the partial-pin arm; version 2's text said A2).
- **Numeric-to-word bands:** `NONE` — this member renders no figure. ⚠ But see §7's
  prose-numerics row: it SHIFTS six existing line-addressed rows.
- **Player/public projection:** `NONE`.

### Alignment and edit story

- **Alignment:** `DECLARED EMPTY` — this member touches no faction/deity alignment surface.
- **Edit story:** this member is the institution and power half of the engine §14 names. The DM's
  verb reaches it through EM-B2a4's `rederive`; nothing here is user-visible.

## §7 · Exact change manifest

⭐ **GENERATED FROM `EM-B2a3.manifest.json` AND PROVED SET-EQUAL WITH IT** (interim rule 6 / chair
ruling 8). The proof is quoted in §12 and in `EM-B2a3.evidence.md`.

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `MODIFY` | `src/generators/steps/assembleInstitutions.js` | the `registerStep('assembleInstitutions', …)` callback body | 40 eff | Read `ctx.__pins` once at the top of the callback, exactly as `generatePopulation.js` does at its own `const pins = ctx.__pins || null;` (count 1; `:235` at `583f8f644`, and it MOVES UP by the ten lines EM-B2a2 deletes — cited by SYMBOL, never by that line), then gate all three provided keys through the shared primitive. ⛔⛔ **WIDEN THE EXISTING IMPORT LINE, NEVER ADD ONE**: `:11` already reads `import { registerStep } from '../pipeline.js';` and becomes `import { chooseOrPin, registerStep } from '../pipeline.js';` — ZERO lines are added above the file's six prose-numerics rows at `:62` and `:69`, which is what keeps that line-addressed register UNMOVED (interim rule 13). Mint no second spelling. ⛔ Change no registration meta, no fork label and no name-resolution rule. |
| `MODIFY` | `src/generators/steps/generatePower.js` | the `registerStep('generatePower', …)` callback body | 20 eff | Gate `powerIntent` at the `createPowerGenerationIntent` call and `powerStructure` at the `projectPowerGenerationIntent` call. ⛔⛔ **WIDEN THE EXISTING IMPORT LINE, NEVER ADD ONE**: `:15` already reads `import { registerStep } from '../pipeline.js';` and becomes `import { chooseOrPin, registerStep } from '../pipeline.js';`. ⛔ Edit neither helper and neither of the three power files. |
| `CREATE` | `tests/generators/pipelinePinnedChoosers.test.js` | A1–A7 of §9 — **adds exactly 7 literal `it`s**, one per sealed acceptance case, under ONE literal `describe` | 7 `it`s | Copy `tests/generators/pipelinePinnedMode.test.js`'s proof shape arm for arm. Flat literal `it(...)` under ONE literal `describe`; `it`/`test`/`describe` each bound EXACTLY ONCE and never as a variable or arrow parameter; no bare seed loop; every negative assertion anchored or a both-directions set equality; every asserted set IMPORTED (`getStepMeta()` for the chooser lists, `GENERATION_TIER1` for the record verdicts), never re-typed. |
| `REGISTER` | `scripts/mutation-coverage-manifest.json` | `invariants['tests/generators/pipelinePinnedChoosers.test.js']` | +1 row | Add one row, `kind: 'rationale'`, `ref: 'generators-tree-admitted-subject-coupled-2026-08-30'`. ⛔ Touch no other row and no other key. |
| `TEST` | `tests/generators/generationForkCensus.test.js` | `DISPLAY_NAME_KEYED_SITES[0].site` | 1 line | Re-take the ADDRESS only: `src/generators/steps/assembleInstitutions.js:<the new line of the `pickVariant` call>`. ⛔ Change no channel, no marker and no count; EM-P1b owns the site's cure. |

**Generated artifacts: `NONE`.**

> ⛔ **THE LIGHTING CENSUS IS A DEFERRED ROW, NOT AN ARTIFACT OF THIS PACKET.**
> `tests/lint/.lighting-census-baseline.json` — **a named INTERIOR RED, delta only.** This member
> causes **`files +1 · parked +0 · credited +1 · titles +7 · suiteTitles +1`** (all five figures,
> in the register's own order, because the walker stops at its first moved figure). ⭐ **`titles +7`
> is BOUND to the CREATE row's declared seven literal `it`s** (judgment 77: the delta is bound to
> the declared count, never left symbolic; interim rule 12's count prover ships beside this packet
> and exits non-zero on any inequality between the row's words, §9's homes and this figure). The
> predicted red MESSAGE has the shape `expected <N+1> to be <N>`. ⚠ The five live absolutes at the
> read tip, for the chair's stamp and NOT restated as this packet's own: `2670 · 359 · 2311 ·
> 25589 · 6823` (the fifteenth refreeze, `measuredAtSha 391f04c0a`). ⛔ **`LIGHTING_CENSUS_REFREEZE` is NEVER set by this lane**;
> the refreeze is regenerated WHOLE at the train's terminal, BY THE CHAIR. ⚠ The `+1 credited` is
> CONDITIONAL on the file not parking: the build lane runs the walker's own `parkReasonsFor` over
> the new file and quotes its output. The commonest cause is a re-bound `it` — and this member's
> battery is at real risk of it, because the shape it copies iterates corpus rows; the file
> therefore never names a variable or parameter `it`, `test` or `describe`.

> ⛔ **THE WIRING CENSUS IS A DEFERRED PREDICTION AND THIS PACKET NAMES ITS PATH NOWHERE**
> (interim rule 1 — not in this table, not in the capsule, not in `checks`).
> `stamp.producerIndexFiles` **+0**, measured against BOTH producer roots: this member creates no
> `.js` under `src/generators/**` and none under `src/domain/**`. `stamp.files` **UNMOVED** — it
> holds exactly the seven `src/domain/display/stateProse/` producers and neither of this member's
> source files is among them, so `tests/lint/proseWiringCensus.walker.test.js`'s `stale-bytes` arm
> is NOT owed. `totals.*` **UNMOVED**. ⛔ Regenerated WHOLE at the train's terminal, BY THE CHAIR.

> ⭐ **THE PROSE-NUMERICS BASELINE IS A LINE-ADDRESSED REGISTER THIS MEMBER DOES *NOT* SHIFT —
> VERSION 2's PREDICTION IS REFUTED BY MEASUREMENT, IN THE PACKET'S FAVOUR** (interim rule 13,
> which governs over `LANE-EM-PREPROOF` step 13 where they disagree).
> `tests/lint/.prose-numerics-baseline.json` holds **SIX** rows addressed at
> `src/generators/steps/assembleInstitutions.js` — **line 62** (`floatInterpolation`,
> `percentToken`) and **line 69** (`floatInterpolation`, `multiplier`, `percentToken`,
> `twoDecimalScore`), both inside `chanceCause`, which sits ABOVE the `registerStep` call;
> `generatePower.js` carries **zero**. Version 2 predicted that the member's IMPORT LINE sits above
> them and shifts all six. **MEASURED AT `583f8f644`: both source files ALREADY import from
> `../pipeline.js`** — `assembleInstitutions.js:11` and `generatePower.js:15` each read
> `import { registerStep } from '../pipeline.js';` — so EM-B2a2 version 2's export is reached by
> WIDENING that one line (§7's two MODIFY rows now say so in the coding instruction), **ZERO lines
> are added above `:62`, and all six rows are UNMOVED.** ⇒ **`none found`**: this member owes the
> re-address in NO §7 row of its own, and the train's terminal owes it nothing either. The §12
> receipt re-reads the six addresses live and asserts them at 62 and 69.

**Registers that DO NOT move, measured rather than assumed** (⭐ the first four rows are this
pre-proof's, EXECUTED at `583f8f644` over a copy-only estate and a second copy carrying the
contract's planned text — interim rule 17 did not exist when version 2 was compiled):

- ⭐ **`tests/lint/.tuning-inventory.json` / `tests/lint/tuningRegister.walker.test.js` (interim
  rule 17) — UNMOVED, driven through the inventory's OWN library** (`scripts/lib/tuning-inventory.mjs`:
  `discoverTables` → `countUnregisteredNamed(root, tables, TREES_P2P3, sourceOf)` and
  `countBareDecimals`), with the rule's POSITIVE CONTROL reading exactly its stated value
  (`src/domain/activeConditions.js` **P2 = 3**). Pristine and post-plan alike:
  `assembleInstitutions.js` **P2 0 · P3 34**, `generatePower.js` **P2 0 · P3 0**, tree totals
  **P1 237 tables · P2 sum 535 · P3 sum 6,985** — IDENTICAL. The member adds no numeric literal at
  all, so it mints no simulation dial (no owner-signed tuning row), moves no counted line OUT of
  either file (no deferred refreeze row) and is no STOP under that rule.
- ⭐ **The Tier-5 voice ratchet — UNMOVED.** Counted with the estate's own espree over string
  literals and template quasis (a JSDoc is a COMMENT and cannot reach it): both source files read
  `{em: 0, bang: 0}` pristine and post-plan, and the `src/generators` totals stay **em 51 · bang 0**
  over 114 files.
- ⭐ **`scripts/check-writer-reach.mjs` — EXECUTED BOTH WAYS, exit 0, logs BYTE-IDENTICAL**
  (`judged 6537 · LIT 572 · LIT-NAME 4671 · DARK 1294 (reviewable 495)`). No new written identity
  and no new customer surface.
- ⭐ **`scripts/.observed-shape-readers-baseline.json` — NO ROW IS OWED, and its per-file
  `manifests` entries are PROVENANCE, NOT A BYTE GATE.** Both source files appear in
  `manifests.{sourceTree, scanTree, executionTree}` with a `sha256` and a `size`, which reads like
  an obligation and is not one: measured at the tip, **121 of 121 `src/` files changed since the
  register's `frozenAtSha` differ from their banked `sha256`, and `node scripts/check-observed-shape-readers.mjs`
  still exits 0.** Neither change path holds an inventory, identity, rowTag, digest or minRows row.
  The script stays in `checks` as the standing guard it is.
- `scripts/.size-baseline.json` — **no row exists for either source file** (grep, exit 1); the
  layer's 800-line `max-lines` governs and the measured headroom is 229 / 700 effective lines.
- `scripts/check-observed-shape-readers.mjs` — no `EXPLAINED_WRITER_EXEMPTIONS` row is owed or
  minted: no member of this split writes `dmLayer` onto a record. The script stays in `checks` as
  a standing GUARD that no NEW finding identity appears.
- `scripts/check-writer-reach.mjs` — no new written identity and no new customer surface.
- `docs/content/wiring-census.json`'s `stamp.files` — see the deferred row above.
- `tests/lint/testRatchet.test.js` — measured: its `CEILING` (17) caps `baseline.entries`,
  enforcement-walker rows rather than test files, and is monotone-down.
- ⭐⭐ **THE PROMISE, PROVED BY CONSTRUCTION AND NOT BY A DIGEST** (judgment 78: a packet names the
  freeze register's ROW, never a golden's digest). The two rows this member must leave unmoved are
  **`generator-golden-master`** (`tests/fixtures/generator-golden-master.json`) and
  **`dossier-prose-manifest`** (`tests/fixtures/dossier-prose-manifest-golden.json`) in
  `tests/fixtures/.golden-freeze-register.json` — 50 surfaces, `frozenAtSha d22ceff01`. Three
  measurements, each executed: **(i) NO NEW PRNG STREAM** — the `createPRNG(` + `.fork(` population
  is **2 · 0 · 2** in `assembleInstitutions.js`, `generatePower.js` and `pipeline.js` pristine and
  **identical** post-plan, and the one fork label in this member's files
  (`exclusiveCoexist::${tier}::${category}::${name}`) is untouched; **(ii) THE DRAW ORDER IS
  UNCHANGED WHEN NO PIN IS SET** — the primitive driven in plain node calls `draw()` exactly once
  per key, in declaration order, for `pins = null`, `pins = undefined` and `pins = {}` alike, and
  an INHERITED (prototype-chain) key still DRAWS; **(iii) ZERO DRAWS WHERE A PIN EXISTS** — all
  three keys pinned consumes none, and an own property whose value is `undefined` IS a pin. ⇒ a
  chooser with no pin behaves byte-identically, which is the landed pin-consult mechanism (judgment
  30) rather than a promise about it, and A1 is the arm that executes it over the corpus.
  ⛔ **The pool-membership rule of judgment 30 — an override outside a chooser's candidate set is
  NOT APPLIED AND IS REPORTED — is NOT this member's and is asserted nowhere in it** (grep: the
  words appear once, in the non-goal row that assigns the unapplied-override list to EM-B2a4). This
  member has no candidate set and no override channel: it receives a value through `ctx.__pins` and
  the caller owns membership. A build lane that adds a membership check here is a STOP.
- both goldens (`generatorGoldenMaster`, `dossierProseManifest`) — byte-unmoved, and A1 is the
  proof rather than the promise.
- ⛔ **Byte budgets: NO ARM, NO DIST READ** (interim rule 2), and **this member is NOT the train's
  byte-arm holder** (interim rule 14: the chair names one at placement; this member carries no row
  on `tests/build/vendorPdfLazy.test.js`). **MEASURED at `583f8f644`, both ways, source-only:**
  the generation worker's STATIC closure from `src/workers/generation.worker.js` is **196 modules**
  pristine and post-plan, and **both change paths are INSIDE it** (`pipeline.js` and
  `generatePopulation.js` too; `src/domain/edit/dmLayer.js` is NOT, which is the layer boundary
  holding); `EAGER_FIRST_PAINT_MODULES` is **269 → 269, the sorted list SHA-identical
  (`75664dce4b2641f2`)**, and neither change path is eager ⇒ **first paint +0 B, no
  owner-signed ceiling is approached.** ⛔ **THE WORKER CEILING IS THE ONE RISK AND IT IS EXACT AT
  ZERO SLACK** (`WORKER_BUNDLE_CEILING_BYTES = 1401208` at placement — 1396015 since train EM-T12's terminal BUY-BACK, judgment 160; monotone-down, no placement cure): the pin
  consults' minified price is **NOT computable from source**, so — exactly as EM-B2a2 version 2
  carries it — the terminal's ONE real build decides, and if it reads any rise the cure is a
  SOURCE-SIDE shave inside this member's own two consults (fewer emitted identifiers; no
  relocation), never a lane's re-mint of the ceiling, which is a STOP for the chair. [At the T12 terminal the rise measured +565 B — this addition's whole minified size — so the shave was infeasible; the chair bought the bytes back elsewhere in the closure (seedrandom's ARC4 core alone, judgment 160) and the ceiling followed the measurement down.] The membership probe is the import-the-set shape; the train's
  terminal takes ONE summed price from ONE real build. ⛔ The worker ceiling is EXACT at zero slack
  and has no placement cure, which is why the price is the chair's and not this packet's.

⛔ **THE BROWSER SUITE: NOT GOVERNING.** This member touches no `src/components/**` path, no route,
no `data-testid` and no accessible name, so **no `e2e/` spec is named** (interim rule 10, stated as
it requires).

No other file may be edited.

## §8 · Ordered coding sequence

0. Dispatch and seal the packet; stop on any preflight mismatch. ⛔ **Do not start until the chair
   has ruled §4a's Q1 (skip or consume-and-discard) and Q4 (who clones)** — steps 3 and 4 are
   written to them. (⭐ Ruled before the build and discharged at placement, judgment 133b — history at this flip.)
1. Capture the baseline: the five lighting figures, `scripts/mutation-coverage-manifest.json`'s
   `invariants` count and `uncoveredBaseline`, and the six prose-numerics row addresses, each read
   live and recorded as the BEFORE of a delta — never copied from this packet.
2. Write the failing tests for A1–A7 and RUN THEM: every one must RED, for its stated reason,
   before a production line exists. ⛔ A2's arm must red against TODAY's tree for the right reason
   (the steps ignore pins, so a pin is silently dropped, which is the negative control §9a names).
3. Gate `generatePower`'s two keys first — it is the reproducible half — and re-run.
4. Gate `assembleInstitutions`'s three keys under the ruled cure and re-run.
5. Run `parkReasonsFor` over the new test file and quote its output.
6. Re-take the census citation's address and add the mutation-coverage row; re-run both walkers.
7. Quote the six convicted prose-numerics rows in the receipt. ⛔ Do NOT edit the baseline; the
   terminal owes the re-address.
8. Run the focused verification of §10. ⛔ Do NOT regenerate the lighting baseline.
9. Under the train, the bare full gate and the boot smoke move to the terminal; write §12.

## §9 · Acceptance matrix

| ID | Case | Fixture/input | Required observation | Test home |
|---|---|---|---|---|
| A1 | The null change — the goldens cannot move | a ≥40-row stride of the golden master's 525-row corpus; then the runner with `pins: {}` | every hash equals the COMMITTED fixture; `pins: {}` yields a settlement JSON-identical to the plain path and the same ctx key set | `tests/generators/pipelinePinnedChoosers.test.js` |
| A2 | ⭐ The pin is TAKEN (anti-vacuity) | a DELIBERATELY ALTERED value pinned, one key at a time over all five | the step's own output IS the pinned value and the record carries it, for each of the five keys by name. ⛔ Against TODAY's tree this arm reds on all five, because both steps ignore `ctx.__pins` and a full pin set comes back byte-identical to the baseline — which is why no reproduction arm in this file stands alone (§9a) | same |
| A3 | A pinned key's draw is not consumed for it | a counting proxy over the step's own forked stream; and, for the power step, the fork count plus a mint census | `assembleInstitutions` 66 own-stream draws unpinned → 0 pinned. ⛔ `generatePower` is ZERO own-stream draws UNPINNED, so that instrument is VACUOUS there: its arm counts the inner fork (1 → 0) and asserts `createPRNG` is never called with `<seed>::generatePower::power-structure` under a pin — ⭐ **NARROWED AT THE FLIP (judgment 143; the pass-1C verifier's FIX-1, 2026-09-22): WITHIN THE STEP'S OWN DRAINED WINDOW.** Read as a whole-run claim the sentence is false of correct code: `powerEconomyReconcilePass.js:29-34` re-hands the intent to `projectPowerGenerationIntent`, which mints the SAME seed later in a pinned run by a step with `provides: []` (EM-R3's seam). The built arm records the mints through a pass-through recorder drained at every step boundary and asserts the seed absent in `generatePower`'s window alone, through `expectPresentThenAbsent` (the liveness half first) | same |
| A4 | The partial pin refuses BY NAME, and the name is not always this member's step | pins = `assembleInstitutions`'s three keys alone; then `generatePower`'s two alone | the first throws `Pipeline pins: step "coherenceRepairPass" has choosers [generationRepairs, isolationSupport] but pins supply only [generationRepairs]. Pin every chooser of a step or none of them.` and reports `{ step: 'coherenceRepairPass', kind: 'pin', keys: ['isolationSupport'] }` under `onStrictViolation`; the second does NOT throw. The CLOSURE (6 keys / 5 steps; 2 keys / 1 step) is computed from `getStepMeta()` and asserted both directions, never re-typed | same |
| A5 | ⭐ The survival, SEALED ONLY WHERE MEASURED TOTAL | the DECLARED 63-row census corpus; pins built from the record, cloned, honoured at the step boundary | **(a)** `generatePower`: the whole settlement reproduces **63/63**, all nineteen watched keys 63/63. **(b)** `assembleInstitutions`: `stress`, `name` and `population` reproduce 63/63 and nothing else totally. **(c)** THE CONTROL: each step's own PRODUCED values reproduce 63/63 and 63/63. ⛔ §0.S's other figures are RECORDED MEASUREMENT with an as-of mark, never sealed — asserting one is a STOP (§11) | same |
| A6 | ⛔ The pin bag is never written through | the 63-row corpus, pin handed back BY REFERENCE, cloned and uncloned | cloned: the caller's record is MUTATED in **0/63** for both steps and **0/63** rows throw. Uncloned, as the negative control that keeps the clone honest: **52/63** · **30/63** mutated and **2/63** throwing `TypeError: Cannot add property 1, object is not extensible` at `coherenceRepairPass.js:134` (the record's `repairs` is frozen at `generationCoherence.js:515`). The clone itself is EM-R1's | same |
| A7 | The file list is two, and the helpers are untouched | the `provides:` roster over `src/generators`; the three helpers' export sets; the call chain from `generatePower` | none of the three helpers is a registered step; all three are reachable only through `projectPowerGenerationIntent`; the packet names two source files and no fourth | same |

This table is the entire edge-case budget — **7 of 8**. Omit nothing; add nothing.

## §9a · ⭐ THE NEGATIVE CONTROL THIS BATTERY MUST CARRY

**The instrument can lie green, and here is exactly how.** At the read tip both steps IGNORE
`ctx.__pins`. Pass a full, lawful pin set for either today and the runner seeds the context with
it, the step overwrites it with its own patch, and the settlement comes back **byte-identical to
the baseline** — executed, for `generatePower`'s two keys and for the eight-key closure alike. A
naive "pinned run reproduces the record" arm is therefore **green before a line of this member
exists**, and would stay green over a member that did nothing.

⇒ every reproduction arm in this battery is paired with an ANTI-VACUITY arm that pins a
**DELIBERATELY ALTERED** value and asserts the record CHANGES in the way the pin dictates. Against
today's tree that arm reds (the pin is dropped); against a correct member it passes; against a
member that wires only one of the five keys it reds on the key that was missed. The same shape is
what `contractTestAntiVacuity` Rule 2 demands of every declared set, and it is why the chooser
lists come from `getStepMeta()` and the record verdicts from `GENERATION_TIER1`.

## §10 · Verification commands

```sh
# Focused static checks
npx eslint src/generators/steps/assembleInstitutions.js src/generators/steps/generatePower.js \
  tests/generators/pipelinePinnedChoosers.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict

# ⭐ THE CREATE DIRECTORY, RUN WHOLE (the chair's addendum after runs 17/18/19: a CREATE under
# tests/<dir> opts into EVERY walker that governs tests/<dir>)
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 tests/generators

# The golden totality — the standing proof that fresh generation did not move
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js

# The governing walkers and the moved registers, by FILE (these are the sealed checks)
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/contractTestAntiVacuity.walker.test.js tests/lint/goldenFreeze.walker.test.js \
  tests/lint/negativeAssertionAnchor.walker.test.js tests/lint/seedLoopTotality.walker.test.js \
  tests/lint/mutationCoverageManifest.test.js tests/lint/generationForkRegistry.contract.test.js \
  tests/lint/catalogTierGateParity.walker.test.js tests/lint/institutionLabelSeam.census.test.js \
  tests/lint/magicLicenceCensus.walker.test.js tests/lint/settlementMapSurfaceAllowlist.walker.test.js \
  tests/lint/proseNumerics.test.js
# …and the same form, one directory per run, for the tests/domain (13), tests/joins (5),
#    tests/data (2), tests/store (2), tests/components (2), tests/pdf (2) and tests/kernel (1)
#    files the sealed array names.

# ⛔ THE BUILD LANE'S INSTRUMENTS — NEVER SEALED CHECKS
#   (a) the excluded directory, which MUST EXIT 0:
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint --exclude=tests/lint/sovereigntyLightingContract.walker.test.js
#   (b) the lighting walker ALONE, as the measured, named INTERIOR RED:
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/sovereigntyLightingContract.walker.test.js
#   ⛔ DO NOT set LIGHTING_CENSUS_REFREEZE. The refreeze is the chair's, at the terminal.

# ⭐ THE COUNT PROVER — KIT FURNITURE (interim rule 12, judgment 81), run from the KIT PATH BEFORE
#   the seal. It is never a sealed check, never a §7 row and is never placed in the tree.
node <kit>/packets-waiting/EM-B2a3.count-prover.mjs \
  <kit>/packets-waiting/EM-B2a3.md <kit>/packets-waiting/EM-B2a3.manifest.json

# Registers that must NOT move
node scripts/check-observed-shape-readers.mjs
node scripts/implementation-packets.mjs validate

# Sealed receipt and exact-state handoff; neither is landing authority
npm run check:packet -- EM-B2a3
npm run implementation:resume -- EM-B2a3
```

Every command exits `0` except the lighting walker, which is the declared interior red. A lane
never runs `npm run check`; it pauses at a held gate. ⚠ `gate-mutex.sh --run` gives up after its
poll budget and **exits 3** (`GAVE UP`) — export the budgets above, capture the exit code, and
never pipe the gate: no printed count means it DID NOT RUN. Report actual counts; never copy a
historical count.

## §11 · Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and `EM-PREAMBLE.md` §P8, stop if:

- **the chair has not ruled §4a's Q1 and Q4** — the first decides whether the member skips or
  consumes-and-discards its draws, the second who clones; both change the code, so the member may
  not be dispatched before them;
- ⛔ **a build lane asserts, as an acceptance, ANY survival figure §0.S did not measure as TOTAL**
  — `assembleInstitutions`'s whole settlement (7/63), `institutions` (11/63), `economicState`
  (10/63), `availableServices` (10/63), `simulationTrace` (7/63) and the rest are RECORDED
  MEASUREMENT with an as-of mark, and they are the LATER WRITERS' to fix (EM-R1's census, EM-R3's
  replay). Asserting one here would seal a claim this member cannot keep;
- **EM-R1 has not landed and the chair has not ruled the local clone** (Q4): without a clone the
  member's own steps mutate the caller's record 52/63 and 30/63 and throw 2/63 — ⭐ **DISCHARGED AT PLACEMENT (judgment 133b, 2026-09-21 17:00; history at this flip):** Q4 was ruled the other way before the build (a LOCAL `structuredClone` in this member's two consults), the built code clones on entry, and A6 proves the pin bag is never written through;
- ⛔ **`src/generators/pipeline.js` does not export `chooseOrPin` at the verified base** — EM-B2a2
  has not landed, the ninth `requiredSymbols` row refuses, and the seal cannot be taken. This is the
  machine-checkable form of the `Depends on` row and it is deliberate (⭐ history at this flip: EM-B2a2 landed in train EM-T11 and the export resolved at this member's verified base `efd0eb448`; the seal was taken at `04ac6520e`);
- ⛔ **the count prover (`EM-B2a3.count-prover.mjs`, kit furniture beside this packet in the chair's
  `packets-waiting/`) exits non-zero** — run it BEFORE the seal, with both paths as arguments:
  `node EM-B2a3.count-prover.mjs EM-B2a3.md EM-B2a3.manifest.json`. It is never a `checks` entry,
  never a §7 row and is never placed in the tree (judgment 81);
- ⛔ **any edit that ADDS an import line to either source file instead of widening the existing
  `import { … } from '../pipeline.js';`** — six prose-numerics rows sit above it at `:62` and `:69`
  and the packet's own §7 says they are UNMOVED;
- `tests/generators/pipelinePinnedChoosers.test.js` already exists at dispatch;
- any of the nine `requiredSymbols` of §5 no longer resolves VERBATIM at its stated count (⭐ the flip, 2026-09-22: version 3 said "eight" and was not re-counted when the ninth, `chooseOrPin`, joined at the pre-proof — the pass-1A verifier's NOTE-4; all nine resolved at the landing);
- **any golden row moves** — `tests/property/generatorGoldenMaster.test.js` or
  `tests/property/dossierProseManifest.test.js` reds, or A1's stride finds a hash off the
  committed fixture. That is EM-P0 version 1's failure class and it is a STOP, never a re-record;
- **any `GENERATION_TIER1` row moves** — a census row that moved means this member changed
  generation, which the golden posture forbids;
- either step would need a second registration, a second stream object, or a fork label change;
- the member would need to edit `src/generators/pipeline.js`,
  `src/generators/steps/generatePopulation.js`, `src/generators/npcGenerator.js` or any of the
  three power helpers — those are EM-B2a2's and the helpers are measured unreachable;
- the member would need to mint a second pin primitive rather than import EM-B2a2's;
- either file would exceed the layer's 800 effective-line `max-lines` ceiling, or a
  `scripts/.size-baseline.json` row would have to be minted for it;
- `parkReasonsFor` reports a reason for the new test file that cannot be cured inside it;
- the lighting census moves by anything other than `+1 file / +0 parked / +1 credited / +T titles
  / +1 suiteTitle`, or `uncoveredBaseline` moves at all;
- the acceptance matrix would grow past eight cases.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into the
next member.

## §12 · Completion receipt

- Base SHA:
- Dispatch bundle and seal identity:
- The chair's rulings on §4a Q1–Q5, quoted, and what changed in this packet because of each:
- Final commit or working-tree state:
- Exact changed files and effective-line counts (eslint `Linter`, `skipBlankLines` + `skipComments`) for both source files against the 800 layer ceiling (before: 571 / 100):
- Acceptance cases A1–A7, executed, with A5's sealed and unsealed halves stated separately:
- ⭐ The §9a negative control: the anti-vacuity arm's RED against the pre-member tree, quoted:
- The golden stride: rows checked, rows moved (must be zero), and the committed fixture's identity:
- `GENERATION_TIER1`: the census test's verdict before and after (expected: no row moved):
- `parkReasonsFor('tests/generators/pipelinePinnedChoosers.test.js')` output, quoted:
- Lighting census DELTA, all five figures, attributed to the one new test file by execution (⛔ never an absolute tuple; ⛔ no refreeze run here):
- Mutation-coverage manifest: `invariants` count before and after (expected +1) and `uncoveredBaseline` (expected UNMOVED):
- ⛔ The SIX convicted prose-numerics rows, quoted with their old and new line addresses, and the terminal's re-address named:
- The census citation's old and new address for `assembleInstitutions.js:<line>`, with the arm re-run:
- Wiring census: `stamp.producerIndexFiles` before and after (expected UNMOVED, +0) and `stamp.files` (expected UNMOVED):
- `tests/generators` run WHOLE, and every sealed check array, with exits and counts:
- `tests/lint --exclude=tests/lint/sovereigntyLightingContract.walker.test.js` — exit (must be 0):
- The lighting walker alone — the named interior red, message quoted:
- Both typecheck configurations (`typecheck:ratchet`, `typecheck:domain:strict`):
- Sealed per-step receipt and exact-state resume status:
- Base-versus-wave failure identity diff:
- §7-versus-capsule set-equality proof, re-run at the landing:
- Generated artifacts: `NONE`
- Deviations: `NONE | STOP`
- Out-of-scope observations, without investigation:
- ⭐ §0's figures RE-TAKEN at the landing sha, with that sha as their new as-of mark (survival N/63 per key, both write-through arms, both trace counts):
- Judgment calls: `NONE` — §4a's five questions are the chair's and this packet records how each was ruled.

### §12 · EXECUTED — written at the flip (2026-09-22, the successor chair; the pass-1A verifier's FIX-1: the two measured observations were only in the lane's scratch receipt)

- **Base and seal:** verified base `efd0eb448` (train EM-T11's landed tip); placement `04ac6520e`; seal `2559535d…` at `04ac6520e` (the build and the cure both under it). **Commits:** the build `608b0d63d` (5 files, +550/−14: `assembleInstitutions.js`, `generatePower.js`, `pipelinePinnedChoosers.test.js` CREATE, `generationForkCensus.test.js`, `mutation-coverage-manifest.json`) and the FIX-2 cure `254af6b64` on the lane, composed on the train as `2117054e8` (+75/−7 on the test file alone; judgment 137 — A3's instrument now sees the nested `setActiveRng(createPRNG(…))` mint through a `vi.hoisted` pass-through recorder drained per step). The full executed receipt with every count line is the lane's `EM-B2a3.receipt.md` and the two commit bodies' QUEUE'S ANSWER blocks (window 35 job 1, exit 0 after 636 s; window 36 job 1, exit 0 after 697 s: the red-first names A2/A3/A6/A7 by title, the counterforce reds A3 alone).
- **Lighting delta, by execution:** `+1 file · +0 parked · +1 credited · +7 titles · +1 suiteTitle`; `parkReasonsFor` `[]`. **Mutation manifest:** `invariants` +1, `uncoveredBaseline` unmoved. **Goldens:** unmoved (`7177cd6e…` / `88983938…`). **Tuning inventory:** P1 237 · P2 535 · P3 6,985 identical before and after. **Wiring census:** +0.
- ⭐ **THE TWO OUT-OF-SCOPE OBSERVATIONS, as of `04ac6520e` (recorded, asserted nowhere — asserting either would be §11's own STOP):**
  1. **§0.S's merged row splits.** `generationCoherenceReceipt · defenseProfile | 46/63` is two figures at this tip: `defenseProfile` **46/63**, `generationCoherenceReceipt` **34/63**. Every other §0.S figure re-reads equal. SLOT: EM-R1's writer census re-takes §0.S as its own baseline (the row is EM-R1's).
  2. **Through the member's own consults, `generatePower`'s record-built pin reproduces 62/63, not 63/63** (`village|plains|golden-master-v3`): the step's trace loop (`generatePower.js:96-171`) runs below the consults and reads `powerStructure?.factions` — under a record-built pin that is the RECORD's roster, which `neighbourFactions` and `powerEconomyReconcilePass` move after the step. That is EM-R3's seam (§1a); the verifier CONFIRMED the diagnosis. SLOT: EM-R3's replay / EM-B2a4's `rederive` meets this figure first.
- **Verifier:** pass 1A 0 STOP · 2 FIX · 5 NOTE (FIX-1 = this block; FIX-2 = the cure above; NOTE-4 = §11's nine); pass 1C over the cure 0 STOP · 1 FIX · 3 NOTE (FIX-1 = §9 A3 narrowed to the step's own window, judgment 143; NOTE-2 = a shared mint-recorder helper, TOOL-4's slot; the files in the kit's `findings/VERIFY-EM-T12-2026-09-21/`).
- **Judgment calls at the landing:** 133b (the EM-R1 STOP discharged at placement), 137 (the cure under the existing seal), 141 (the cure's add/add composition, the cure's blob taken and proven byte-equal), 143 (the A3 row narrowed).
