# Settlement editor / train EM-T11 — EM-B3f: the veil reaches the PARKED SNAPSHOT — a paused campaign's pre-interval undo cursor carries a whole settlement per member, and the account export carries it out unstripped

- **Status:** `LANDED`
  - Compiled 2026-09-21 by an Opus COMPILE lane (lane COMPILE-EM-B3f) at the read tip `429141e2d1c21cb5ee9dd5005ed1a77bbc9b3cd8`, on the chair's ruling of judgment 97, and on the INTAKE RULE: TOOL-30's census found the shape and it is slotted the turn it arrived.
  - ⭐ **THIS PACKET IS THE NARROWED SURVIVOR OF A REFUTED BRIEF.** The launch brief said the parked snapshot is stripped *"on the way IN and on the way OUT."* This lane's first measurement, run before a line of packet text, proved **THERE IS NO WAY IN** (§1). The chair accepted the refutation (judgment 97) and re-scoped the packet to the way OUT. **EXPORT ONLY, ONE FILE.**
  - READY-able. The §3 budget is inside every cap, so no override is requested.
  - ⛔ **THIS PACKET MUST LAND AFTER `EM-B3e` AND BEFORE `EM-C4a`.** It CALLS EM-B3e's `withoutEditKeys`, and EM-C4a is the charter's first writer of `dmLayer`. Until EM-C4a lands nothing writes either key, so the hole is dormant; from its landing onward every paused campaign's backup export carries the DM's private layer out of the veil.
- **Landed at:** `5a6ce34988c77a3741fe8dbbfc580df9f5dae6b6` — the twenty-sixth landing — train EM-T11 (version 3, re-stamped to the fifth amendment): the export-only strip of a saved record's edit keys at the account-data seam, one changed line at the campaigns binding (+46 effective), five arms C1–C5 with two mutants convicting C2 and C3 alone; the census walker's one string re-spelled
- **Packet version:** `3`
  ⭐ **VERSION 3 (the chair at placement, 2026-09-21; judgment 102 Q2): re-stamped to the fifth amendment's preamble hash; the interim-rules row struck. No other byte moves. The prior version is 2.**
  - **What version 2 changed and why (version 1 was the compile's):** an independent Opus PRE-PROOF re-measured every fact
    against REAL CODE at `f00d44040539c4586e21ae2bafb4e10f64a3d2aa` — the read tip where **EM-B3e's build commit is landed code**, not a packet's promise.
    Version 1 was compiled at `429141e2d`, BEFORE EM-B3e, so every line hint in it is short by 30 (or 14) lines and one
    verified-fact quote was EM-B3a's pre-EM-B3e body. Version 2 (a) refreshes every line hint to the tip, (b) replaces the
    `withoutEditState` quote with the body that is actually there, (c) moves `withoutEditKeys` from PLAUSIBLE to CONFIRMED
    (real code, count 1, driven), (d) corrects `+33 eff` to the **+46 eff** a faithful spelling of §8 costs, measured with
    eslint's own `max-lines` semantics and cross-checked at 371/393, (e) discharges §10's CREDITED premise by printing
    `parkReasonsFor` from the walker's own classifier, and (f) re-states the train roster and the collision census as
    measured. No contract, algorithm, acceptance case, check, budget row or STOP condition changed.
- **Verified base:** `em-t11-b3f-2026-09-21` at `252d7ba4eaa7e531abd2ff2d82e3f34c322d3a08`
  - *The revalidation sentence the chair stamps, with the facts this compile measured:* "Re-measured at `<tip>` under J-T1: `git -C <tree> diff --stat <base> <tip> -- src/lib/accountData.js tests/lib/accountData.test.js tests/lint/snapshotPromotion.census.walker.test.js` accounted for; **`EM-B3e` is LANDED at this base and `withoutEditKeys` resolves at count 1** (the `_pendingRequiredSymbols` row is promoted into `requiredSymbols` at that flip); both standing `requiredSymbols` rows re-resolved verbatim at count 1 (`EM-B3f.evidence.md` E-SYM); `retiredSymbols` is empty; the four budgeted byte closures were re-derived and this packet reaches NONE (E-BUD); the observed-shape register moves by ZERO rows under the contracted spelling, measured against the detector's own rule with a failing control (E-SPELL); the lighting DELTA of §10 is applied to the live baseline at promotion, never to a tuple copied from this document."
- **Last revalidated:** `252d7ba4eaa7e531abd2ff2d82e3f34c322d3a08` — STAMPED BY THE CHAIR AT PLACEMENT. No gate was run by this compile lane and none is claimed.
- **Depends on:** **`EM-B3a` — ✅ LANDED at `668d87512f539ad1c866ec3d9327d6d41f3d8d2f`** (its destructure-drop is the operation this packet reuses) and **`EM-B3e` — READY, train EM-T10** (its module-private `withoutEditKeys` is the helper this packet calls; **HARD**). It depends on no writer and on no migration. ⭐ **`TOOL-30` — same train, lands BEFORE this member**: it CREATEs `tests/lint/snapshotPromotion.census.walker.test.js`, which is this packet's third §7 row (interim rule 4).
- **Depended on by:** `EM-C4a` — the first writer of `dmLayer`. See the ⛔ row under Status.
- **Collision group:** `NONE.` Measured at `429141e2d` over all three change paths against every `packets-waiting/*.manifest.json` change row and every non-terminal packet of `docs/implementation/PACKET_MANIFEST.json` (E-COLLIDE). **Zero intersections with train EM-T11's other PACKET members**, re-measured capsule-by-capsule by the pre-proof at `f00d44040539c4586e21ae2bafb4e10f64a3d2aa`: `EM-C4a` version 4 (9 paths), `EM-B1c1` version 2 (3), `EM-B2a2` version 2 (3) and `EM-A2a` version 7 (2) — **CHANGE ∩ CHANGE = NONE against all four**, and NONE against all seven of train EM-T10's members but `EM-B3e`, whose `src/lib/accountData.js` row is this packet's own declared ordering. ⚠ **`TOOL-30` shares ONE path with this packet by design** — `tests/lint/snapshotPromotion.census.walker.test.js`, which TOOL-30 CREATEs and this packet edits as a `TEST` row under interim rule 4. TOOL-30 is a TOOL and carries no row in `PACKET_MANIFEST.json` (207 entries at the tip; `TOOL-30` absent), so the validator raises no `duplicate change path across packets` for it — but the two cannot build in PARALLEL on separate lane branches: **TOOL-30's commit must be an ancestor of this member's dispatch base**, which §4 already requires. Two facts the chair must hold, both measured:
  - ⛔ **THIS IS WHY THE TEST HOME IS `tests/lib/accountData.test.js` AND NOT `tests/lib/editTravel.test.js`.** `editTravel.test.js` is claimed by **EM-C4a — a member of this same train** — and by `EM-B3d` (READY, which `reservesChangePaths` because `READY` is not terminal) and by `EM-B3e`. A row on it here would refuse at placement with `duplicate change path across packets` and could never be a parallel-train member. `tests/lib/accountData.test.js` is claimed by **NOBODY** (E-COLLIDE), exists at the base, and already holds `describe('buildAccountExport')` — the suite that drives this packet's exact seam.
  - ⛔ **A PLACEMENT PRECONDITION:** `EM-B3e` still claims `src/lib/accountData.js`. `reservesChangePaths = !TERMINAL_PACKET_STATUSES.has(status)` (`scripts/implementation-packets.mjs:828`), so this packet places cleanly only on a tree where **EM-B3e is LANDED** — which is the ordering its `Depends on` row already requires.
- **Commit authority:** `edits only; the chair commits`
- **Baseline posture:** `measured at the read tip 429141e2d.` `src/lib/accountData.js` read WHOLE and driven in plain node; `src/lib/accountImport.js` read whole (to prove the import half is not this packet's); the three promoters read at their write sites and **promoter 3 driven end to end** (E-PROMOTE); the eight-door census executed (E-DOORS); the contracted cure executed standalone over the real door with all five mutants (E-CONTRACT); the contracted spelling counted with the tree's own TypeScript against the detector's own rule, with a failing control (E-SPELL); the registers (E-REG); the budgets (E-BUD); the collision census (E-COLLIDE). **No gate was run and none is claimed.**
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: measured with `shasum -a 256` at `429141e2d` by the compile and **RE-MEASURED by the pre-proof at `f00d44040539c4586e21ae2bafb4e10f64a3d2aa` — UNMOVED** — `fdecd426828665cad2dfaf872871f29f46fa4816e1bd4c43c20ccd84dacc9e7c`; STAMPED BY THE CHAIR AT PROMOTION from the live file, never copied from this document.)
> **Compile rules:** the fifth amendment of `EM-PREAMBLE.md` (SHA-256 above) — the interim sheet is superseded; nothing in this packet needs it.

> ⚠ The status value above stands ALONE on its line because `parsePacketHeader`
> (`scripts/implementation-packets.mjs`) anchors the status row at end-of-line.
> Every stamp lives on the continuation lines beneath it.

---

## 1. Reconciled authority

1. **`docs/DESIGN_EDIT_MODE_AND_DECREES.md` §11 ("edits do not travel") and §12 item 4 GOVERN.** §12 item 4: *"the travel instrument is a RUNTIME test that a fork, an import and the gallery projection carry neither key, not a static walker."* This packet's charge is that the sentence is true of the whole ACCOUNT EXPORT — not only of the array of saved settlements inside it.
2. **`docs/implementation/packets/settlement-editor/EM-B3a.md` — LANDED.** Its docblock above `withoutEditState` says *"This is the one seam through which either key could leave the account at all … the omission is spelled here and nowhere else."* **That sentence is measurably FALSE from EM-C4a onward** and this packet is what makes it true again. A header that undercounts its own reach is the exact drift EM-B3d's answered Q2 refused.
3. **`docs/implementation/packets/settlement-editor/EM-B3e.md` — READY, train EM-T10.** Its `withoutEditKeys` (`src/lib/accountData.js`) is the settlement-level drop this packet calls on every snapshot member. ⚠ **EM-B3e's §2 declares the world-pulse snapshot out of scope** — *"store-only state, outside every persistence whitelist … `buildAccountExport` never reads them"* — which is true of `pulseUndoStack` and `proposalUndoStack` and **NOT true of the PARKED one**, which is durable campaign state (§5.1). That row is why this packet exists; it is not a defect of EM-B3e's contract, which never claimed the campaign record.
4. **TOOL-30's census — `findings/TOOL-30-2026-09-21/M2-POPULATION-ANALYSIS.md`, §2 and §6b.** Its three-promoter population is confirmed here by execution. ⛔ **Its §6b headline conclusion is REFUTED by this lane** (§1's measurement below); the chair has resumed TOOL-30 to re-word it, and this packet re-words only the roster sentence EM-B3f itself makes true.
5. `docs/implementation/preambles/EM-PREAMBLE.md` — §P2 rows 3, 10, 11, 13; §P3.1 (goldens); §P4; §P5 **HZ-TRAVEL**; §P6 (a written-contract arm is proved by its counterforce); §P8.
6. `docs/implementation/PACKET_STANDARD.md` — the standard itself. This packet is inside every cap of the default hard scope budget.
7. Live git state, quoted command by command in `EM-B3f.evidence.md`, all at `429141e2d`.

### ⭐ THE MEASUREMENT THAT REFUTED THE LAUNCH BRIEF, RUN FIRST

`EM-B3f.evidence.md` E-HOLE, executed at this read tip before any packet text:

```
1.export.campaignsParked   CARRIES(HOLE)    dmLayer=true decrees=true
1.export.byReference       SAME-REF         campaigns ride the payload literal verbatim
2.import.validateCampaigns CARRIES          (validateAccountImport returns campaigns verbatim, :259)
3.import.landedCampaign    CLEAN — REFUTES  dmLayer=false decrees=false
3.import.initialKeys       n/a              Phase 9 passes ONLY ["settlementIds","contentBinding","contentBindingHistory"]
```

**The way OUT is real; there is no way IN.** `src/store/accountImportBody.js:839-850` — the only campaign writer on the account-import path — passes `createImportedCampaign` exactly three fields, and `src/store/campaignImportedCreation.js:57` answers with `worldState: createNewCampaignWorldState({ id, name })`. `rawCampaign.worldState` is read by nothing. It is a **DECIDED** rule, not an oversight: `src/lib/importReconciliationAdmission.js:396` names `['worldState', 'world_state_not_imported', 'World simulation state']` and surfaces *"World simulation state remains in the source and is not applied…"* to the user.

⇒ **Promoter 3 is NOT foreign-reachable, and this packet does not claim it is.** It closes a one-directional veil breach: the owner's own private layer leaving the account inside a backup file. The implementer does not read other documents to reinterpret this packet.

## 2. Outcome

**Observable result:** an account export carries the editor's two private keys **nowhere** — not in a saved settlement, not in a saved entry's version history (EM-B3e), and not inside a paused campaign's parked pre-interval undo snapshot.

**Definition of done:** `preflightAccountExport` passes every campaign through a new module-private walk that applies EM-B3e's `withoutEditKeys` to each `worldState.pausedAdvance.preIntervalUndo.saves[i].settlement` and to `…preIntervalUndo.active.settlement`; the walk is **REFERENCE-IDENTICAL when there is nothing to drop** and **NEVER MUTATES ITS INPUT**; five acceptance cases pass; the `accountData`, `editTravel`, `accountImport`, `accountImportSlice` and `accountDataPrivacySection` suites stay green; every golden is UNCHANGED.

In scope:

1. **One primary behaviour** — the veil reaches the parked snapshot, on the way OUT.
2. **One required integration** — the existing seam `preflightAccountExport`'s `campaigns` binding (`accountData.js:272`), extended in place with the same `.map(...)` spelling its `settlements` sibling already uses at `:241`.
3. **One prevention guard** — the dormancy arm and ⛔ the **COPY-ON-WRITE arm** (§9 C5), which deep-freezes the live campaigns before the export and asserts the DM's running parked snapshot still carries its layer afterwards.

Explicit non-goals:

- **THE IMPORT HALF — REFUTED AND OUT OF CONTRACT.** No strip on `src/lib/accountImport.js`, no call to `scrubImportedEditState`, no row on any import path. §1's measurement is the receipt.
- **The round trip already discards every campaign world on import** (tick, stressors, proposals, pantheon, war ledger and any paused advance; the campaign returns fresh with remapped members). **This is coded and declared** (`accountImportBody.js:758-761`; `importReconciliationAdmission.js:396`) and is **LEFT AS IS** by the chair's ruling. This packet pins nothing new for it.
- **Any promoter action.** `restorePulseSnapshotOnDraft`, `runUndoLastPulse`, `runUndoLastProposalApply`, `runUndoLastNpcVerb` and `revertToSnapshotAction` are read and recorded; none is touched and none is a manifest row. **The strip is a DOOR act, never a promoter act** — the pulse's own undo restores the account's OWN settlement, layer included, and must keep doing so.
- **Any writer.** `src/domain/edit/**` is EM-C4a's / EM-B2a's / EM-C1's. This packet writes neither key and imports nothing from there.
- **`src/store/persistProjection.js`, `persistMerge.js`, `normalizeSettlement`, `saves.js`, `worldExport.js`, `src/lib/campaigns.js`** — unchanged. A drop in the campaign PERSISTENCE path would erase a DM's own parked snapshot from their own save, which is the mirror of the hazard C5 convicts.
- **Both gallery doors.** Measured incapable: `ShareToGallery.jsx` passes `owningCampaign.worldState` only to `buildRealmArcSummary` (a ≤600-char string, `realmArcSummary.js:151-155`) and `settlementWarStatus` / `computeAliveness`; `galleryImportMap.js` has **zero** `worldState` hits and both doors already call `scrubImportedEditState`. No edit, no row, no arm.
- **The device draft.** `persistProjection.js` has zero `campaign` hits; the campaign device cache is owner-fenced (`campaigns.js:265/:287/:291`, `scopedLocalKey(ownerId)`). FIX-P8's shape does not reach campaigns. Recorded, not claimed.
- **Recursion below the snapshot.** `saves[i].settlement.versionHistory[j].settlement` is out of contract for the same reason EM-B3e ruled (§13 Q2).
- Record adjacent discoveries in the receipt; do not investigate or repair them.

## 3. Hard scope budget

| Limit | Packet budget | Measured/priced here |
|---|---:|---:|
| Behavior families | `1` | 1 |
| New persisted record families | `0 or 1` | **0** |
| Named state writers | `0 or 1` | **0** — this packet drops, never writes |
| Feature flags | `0 or 1` | **0** |
| User-facing surfaces | `0 or 1` | **0** |
| Direct consumers | `<=2` | **0 new** — no new exported symbol; both helpers are module-private |
| New logic-bearing production leaves | `<=2` | **0** |
| Existing logic-bearing production files modified | `<=3` | **1** — `src/lib/accountData.js` |
| Additional registration-only files | `<=3` | **0** |
| Handwritten files total | `<=12` | **3** |
| New/changed effective production lines | `<=400` | **47** (`+46 eff` added, **1 changed / 0 added** at the `campaigns` binding) — ⭐ **RE-MEASURED BY THE PRE-PROOF**, which spelled §8's three algorithms out against the real file and counted them with eslint's own `max-lines` semantics (`skipBlankLines`, `skipComments`): `isPlainRecord` **3**, `withoutSnapshotEditState` **28**, `withoutParkedSnapshotEditState` **14**, the binding **+1**. Version 1's `+33` undercounted the walk by thirteen lines. Still **far inside** the 400 cap |
| Effective lines per new leaf | `<=250` | n/a — no new leaf |
| Delta in a shared/hot file | `<=15` | **0 hot files touched.** The standing list is `EconomicsTab.jsx`, `OutputContainer.jsx`, `convergence.js`, `institutionLifecycle.js`, `App.jsx`, `SettlementsPanel.jsx`, `peaceTerms.js`, `informationStatecraft.js`; none is here. `src/lib/**/*.{js,jsx}` carries `max-lines: ['error', { max: 800, skipBlankLines: true, skipComments: true }]` (`eslint.config.js:735-741`): `accountData.js` is **393 effective at `f00d44040539c4586e21ae2bafb4e10f64a3d2aa`** (EM-B3e landed; the pre-proof's counter returns **371** for the same file at `429141e2d`, the exact figure two compiles measured, which is what validates it) and becomes **439 / 800** with this packet's +46 (E-EFF, re-measured). It is not in `scripts/.size-baseline.json` and carries no per-file override |
| Acceptance cases | `<=8` | **5** |

**No override is requested.** Exceeding any limit is a STOP and split, not an invitation to renegotiate.

⚠ The effective-line figures are counted with a plain-node pass implementing eslint's `max-lines` semantics (`skipBlankLines: true, skipComments: true`), **not** with eslint's own `Linter` — neither the compile nor the pre-proof lane runs a gate. The pass is cross-checked twice: **371** at `429141e2d` (the figure the compile measured) and **393** at `f00d44040539c4586e21ae2bafb4e10f64a3d2aa` (the figure version 1 predicted for EM-B3e's landing). Headroom at the ceiling is **361 lines**; nothing here turns on the last digit.

## 4. Sealed dispatch and preflight

Run from the packet's worktree before any edit:

```sh
npm run implementation:dispatch -- EM-B3f
```

Expected: exact packet Markdown and structured capsule emitted; verified-base ancestry and unchanged declared substrate proved before the seal pins exact HEAD; the **three** non-CREATE targets clean, **zero** CREATE targets, and **all three** required symbols resolving at count 1 (the third, `withoutEditKeys`, only after EM-B3e's flip promotes it out of `_pendingRequiredSymbols`); Git-visible foreign dirt fingerprinted without target overlap.

⛔ **THE BASE MUST BE AT OR AFTER `EM-B3e`'s LANDED COMMIT.** `withoutEditKeys` is this packet's helper and EM-B3e creates it; a base older than that makes the sealed substrate check throw and leaves this packet with no drop to call. ⛔ **THE BASE MUST ALSO CARRY `TOOL-30`'s LANDED WALKER** — `tests/lint/snapshotPromotion.census.walker.test.js` is a `TEST` row, and `validate:packets` errors on any non-`CREATE` row whose path is absent.

⛔ **SEALED DISPATCH WANTS THE WORKTREE ON THE VERIFIED BRANCH.** `implementation:dispatch` throws `branch mismatch` from a lane branch; the lane branch for this member is `em-t11-b3f-2026-09-21`, cut at the promotion commit, and the chair composes the lane's commit onto the train branch at the terminal.

Any mismatch makes this packet STALE. Stop before coding.

Edit only exact-manifest paths. This lifecycle never creates worktrees, stages, commits, merges, cleans, infers affected tests, or rules on a semantic line budget.

## 5. Verified tree contract

Every row was resolved BY SYMBOL at `429141e2d`; the command and its output are in `EM-B3f.evidence.md` under the cited section. Line numbers are the read tip's and are HINTS — re-find by symbol.

### 5.1 · THE PROMOTER-SIDE CENSUS — enumerated from the promoter, not from the entry (E-HOLE, E-PROMOTE, E-RING, E-DOORS)

| # | Promoter | Source it reads | Source written by | Persisted? | Rides the EXPORT | Rides the IMPORT | Device draft | Gallery | Foreign-reachable |
|---|---|---|---|---|---|---|---|---|---|
| 1 | `revertToSnapshotAction` (`settlementVersionHistoryActions.js:133`) | the entry's `versionHistory[].settlement` | `recordSnapshotAction:46` | **YES** (`saves.js` `row.version_history`) | **yes** | **yes**, with `restoreLifecycle` | no | no | **YES — EM-B3e's charge, already contracted** |
| 2 | `runUndoLastNpcVerb` (`npcVerbsBody.js:293`) | `npcVerbUndoStack[].priorSettlement` | `commitVerbResult:120` — `cloneJson` of the account's **OWN live row** | **NO** — session only; **7 sites in `src`, none persistence** | **no** — not a payload key | **no** — no import path exists | no | no | **NO** |
| 3 | `restorePulseSnapshotOnDraft` (`campaignWorldPulseDeferred.js:522`), via `runUndoLastPulse` (`:614`) and `runUndoLastProposalApply` (`:714`) | `pulseUndoStack` / `proposalUndoStack` (session) **and `worldState.pausedAdvance.preIntervalUndo`** (durable), read by `parkedIntervalUndoSnapshot` (`campaignSliceShared.js:163-165`) | `capturePulseSnapshot:272-276` — `saves: memberSaves.map(s => ({ id, settlement: cloneJson(s.settlement), campaignState }))`, parked by `buildPausedAdvanceCursor` (`campaignAdvanceSession.js:578`) inside the Phase-2 atomic persist | **YES** for the parked one — `campaigns.js:183` admits `worldState` as a record field | ⛔ **YES — THIS PACKET'S CHARGE** | **NO — REFUTED** (§1) | no | no | **NO** |

⇒ **Exactly ONE unclosed member, and exactly ONE direction.** Promoter 3's promotion itself is confirmed executed (`E-PROMOTE`: the parked snapshot's member settlement lands on `savedSettlements[i].settlement`), which is what makes the exported blob load-bearing rather than inert decoration.

### 5.2 · THE ROW TABLE

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| **The export seam** | `src/lib/accountData.js` | `export function preflightAccountExport` (`:267`); its `campaigns` binding at `:272` | `const campaigns = Array.isArray(state.campaigns) ? state.campaigns : [];` — **no projection, no strip**; the value flows into `inspectAccountContentReferences` (`:340`), `preflight.counts.campaigns` (`:378`) and the payload literal (`:407`). Driven in plain node at this tip: a paused campaign's parked snapshot exports with both keys, **by the same reference** (`E-HOLE 1.export.byReference SAME-REF`) | **MODIFIED.** ONE changed line at `:272`, adding `.map(withoutParkedSnapshotEditState)` — the **same wrap-the-ternary-then-`.map`** spelling `settlements` already uses at `:268-271` (⚠ the pre-proof measured that the `settlements` binding is a THREE-line wrapped form, not a two-line one: the SHAPE is shared, the line count is not). The `export function` declaration line is byte-unmoved and is a `requiredSymbols` row |
| **The settlement-level drop this packet CALLS** | `src/lib/accountData.js` | `withoutEditKeys(settlement)` | ⭐ **CONFIRMED BY THE PRE-PROOF AGAINST REAL CODE.** EM-B3e's build is landed code at `f00d44040539c4586e21ae2bafb4e10f64a3d2aa`: `function withoutEditKeys(settlement)` resolves at `src/lib/accountData.js:191`, **count 1, verbatim**, and its contracted behaviour was DRIVEN, not read — `if (!settlement \|\| typeof settlement !== 'object' \|\| Array.isArray(settlement)) return settlement;` then the destructure-drop, returning `settlement` itself when `Object.keys(rest).length === Object.keys(settlement).length`. It STAYS a `_pendingRequiredSymbols` row (interim rule 3) only because EM-B3e's packet row still reads `READY`; the chair promotes it at the LANDED flip, and the pre-proof's validator round 4 proved the promoted capsule green | **CALLED, PRESERVED UNCHANGED.** ⛔ This packet **does not re-implement the drop** and does not re-spell the guard. If `withoutEditKeys` is absent at the dispatch base, **STOP** (§11) |
| **The landed drop it descends from** | `src/lib/accountData.js` | `function withoutEditState(entry)` (`:223`) | ⭐ **RE-QUOTED FROM THE TIP — version 1 quoted EM-B3a's PRE-EM-B3e body, which is gone.** What is there now is EM-B3e's: `const { settlement: liveSettlement, versionHistory: history } = (entry || {});` then `withoutEditKeys(liveSettlement)`, a `history.map` that re-uses the same drop per element, and `if (nextSettlement === liveSettlement && nextHistory === history) return entry;`. Count 1, verbatim (E-SYM). The reference identity is contracted by the docblock above it | **PRESERVE UNCHANGED.** ⛔ **THE DOCBLOCK ABOVE IT MUST GAIN ONE SENTENCE** naming the campaigns seam. ⚠ That docblock is EM-B3a's **as EM-B3e already amended it** — at the tip it runs to `:221` and its sentence reads *"the omission is spelled here and nowhere else, and it reaches EVERY settlement the entry carries: the live one, and every settlement its `versionHistory` nests"* (`:204-206`). It is false once a second seam exists **some forty lines below** (`:204` → the helper block at ≈`:247`). Amend that existing paragraph in place; at most **two** raw comment lines (0 effective) |
| **⭐ THE CAPTURE — why a campaign carries settlements at all** | `src/store/campaignPulseHelpers.js` | `export function capturePulseSnapshot` (`:263`) | `saves: memberSaves.map(s => ({ id, settlement: cloneJson(s.settlement), campaignState: cloneJson(s.campaignState) }))` plus `active.settlement` — a FULL settlement blob per member and one for the live view | **PRESERVE UNCHANGED, and NOT a manifest row.** Read and recorded. Deliberately not a `requiredSymbols` row: this packet does not call it, and a LANDED row would cost a future rename an undeserved `retiredSymbols` discharge (EM-B3e's precedent for `revertToSnapshotAction`) |
| **⭐ THE PARK — why it is durable rather than session state** | `src/store/campaignAdvanceSession.js`; `src/store/campaignSliceShared.js` | `buildPausedAdvanceCursor` (`:151`, parked at `:578`); `parkedIntervalUndoSnapshot` (`:163`) | the cursor is written inside the Phase-2 commit **before `cacheCampaignState`**, so it "rides the SAME atomic persist" (the file's own comment, `:585-589`); `campaigns.js:183` admits `worldState` as a persisted record field | **PRESERVE UNCHANGED, NOT TOUCHED, NOT A ROW.** Same reasoning as the capture |
| **⭐ THE PROMOTION — why the blob is load-bearing** | `src/store/campaignWorldPulseDeferred.js` | `restorePulseSnapshotOnDraft` (`:472`); `runUndoLastPulse` (`:614`) | `state.savedSettlements[savedIndex] = { ...row, settlement: restoredSettlement, campaignState, timestamp }` (`:522-527`) and `persistUpdates.push(...)` (`:528-532`). Driven end to end at this tip: a parked snapshot carrying both keys lands on the live saved record (`E-PROMOTE M5.post.liveRow PROMOTED`) | **PRESERVE UNCHANGED, NOT A ROW.** ⛔ **The pulse's own undo must keep restoring the account's own layer** — the strip is a door act, never a promoter act (§2) |
| **⛔ THE LIVE ARRAY — why copy-on-write is mandatory** | `src/lib/accountData.js` | the `campaigns` binding (`:272`) | `state.campaigns` is **the live store array, by reference** (`E-HOLE 1.export.byReference SAME-REF`). An in-place drop would erase a DM's RUNNING parked snapshot from the session and, at the next `cacheCampaignState`, from their save | **The contract is PURE and COPY-ON-WRITE** (§6). Executed: with the live campaigns deep-frozen the contracted walk does not throw and the store keeps its layer (`E-CONTRACT C5`); the in-place mutant erases it (`E-CONTRACT MUT.e`) |
| **Test home** | `tests/lib/accountData.test.js` | `describe('buildAccountExport')` (`:86`) | 419 raw lines, **3** literal `describe`s and **12** literal `it` titles at this tip; `import { afterEach, describe, expect, it, vi } from 'vitest'` (`:13`) — a plain named vitest import, no alias, no namespace, no `.each`, no loop-generated test, no rebinding of `it`/`test`/`describe`; local `load()` helper re-mocks `supabase.js` per test (`:29-33`) | C1–C5 join the EXISTING `describe('buildAccountExport')` as **5 new literal-titled `it`s**. **No existing arm is edited and no new `describe` is opened.** ⛔ **`FROZEN_UNANCHORED_NEGATIVES['tests/lib/accountData.test.js'] = 1`** (`negativeAssertionAnchor.walker.test.js:603`) is SHRINK-ONLY: **do not anchor the existing `expect(JSON.stringify(out)).not.toMatch(/credits\|"role"/)` at `:161`**, and add **no** new un-anchored negative — every absence is `expect(serialized.includes('"dmLayer"')).toBe(false)` beside a liveness anchor |
| **⭐ TOOL-30's census walker — the roster row this packet makes true** | `tests/lint/snapshotPromotion.census.walker.test.js` | promoter 3's declared `why` string | **DOES NOT EXIST at this read tip** (`ls` → No such file). **`TOOL-30` CREATEs it and lands in this train BEFORE this member**, so this is a `TEST` row naming its sibling (interim rule 4) | **MODIFIED — ONE DECLARED STRING, NO ARM.** Promoter 3's `why` gains the sentence this packet makes true: *the parked snapshot's member settlements leave the account STRIPPED, so the veil holds through the export door.* ⛔ **Nothing else**: no arm added, edited or removed; **0 new `it`s**; no assertion; and **not** the §6b foreign-reach sentence, which is TOOL-30's own lane's to correct (judgment 97 clause 2) |
| **The declared absence** | `src/domain/edit/recordRegister.js` | `NOT_YET_WRITTEN_KEYS` (`:59`) | `Object.freeze(['dmLayer', 'decrees', 'crossSettlementConflicts'])`; nothing in `src/` writes either key today | **PRESERVE UNCHANGED, and deliberately NOT a `requiredSymbols` row** — EM-B3d already carries it in this family, and a second holder would double the `retiredSymbols` discharge EM-C4a owes |
| **Registers that do NOT move** | `scripts/mutation-coverage-manifest.json`; `docs/content/wiring-census.json`; `tests/lint/.prose-numerics-baseline.json`; `scripts/.size-baseline.json`; `tests/lint/.lighting-census-baseline.json`; `scripts/.observed-shape-readers-baseline.json`; `scripts/.slugify-idiom-baseline.json`; `tests/lint/publicIdentitySingleRender.walker.test.js`; every golden | — | mutation-coverage: **zero rows owed** — this packet CREATEs nothing (`ENFORCER_DIRS` read at the tip is `tests/lint · design · docs · data · copy · security · edgeFunctions · generators`; a `TEST` row on an existing `tests/lint` file is not a CREATE); wiring census **`+0`** — no CREATE under `src/generators/**` or `src/domain/**`; observed-shape **zero rows** under the contracted spelling (E-SPELL); prose-numerics **zero rows**, nothing rendered; lighting: the DELTA is §10's and the refreeze is the train's terminal act | **PRESERVE UNCHANGED**, every one |

**Forbidden alternatives:**

- no strip anywhere on the account IMPORT path, no call to `scrubImportedEditState`, no edit to `src/lib/accountImport.js` — §1 refuted the premise;
- no strip in `src/lib/campaigns.js`, `persistCampaigns`, `localWrite`, `cacheCampaignState` or any campaign persistence path — that would erase a DM's own parked snapshot from their own save;
- no edit to any promoter action, to `capturePulseSnapshot`, to `buildPausedAdvanceCursor` or to `parkedIntervalUndoSnapshot`;
- **no PROPERTY READ, anywhere in this packet's production code, of `worldState`, `pausedAdvance`, `preIntervalUndo`, `saves`, `active`, `settlement`, `dmLayer` or `decrees`** — every one is read by DESTRUCTURE (§6, E-SPELL). A dotted read on a resolvable receiver mints an observed-shape row through a door only the chair may open;
- **no unconditional clone and no in-place mutation** — the first breaks the dormancy contract and moves the bytes of every export ever taken; the second erases live DM state (C5);
- no re-implementation of the settlement-level drop: `withoutEditKeys` is called, never re-spelled;
- no recursion into `saves[i].settlement.versionHistory[j].settlement`;
- no edit to `tests/lib/editTravel.test.js` (EM-C4a's, this train), to `src/lib/importScrub.js`, to either gallery door, to any migration, or to `WORLD_SNAPSHOT_HARD_DENY`, `PUBLIC_TOPLEVEL_KEYS`, `toPublicSafe`, `PRIVATE_KEY_RE`, `COVERT_KEY_RE`;
- no edit to any register named above; no new `describe`; no edit to `:87`–`:411`'s existing twelve `it`s in `tests/lib/accountData.test.js`;
- no re-serialisation of any manifest or baseline JSON; no files outside the manifest.

## 6. Exact contracts

### Inputs and outputs

```js
// ── src/lib/accountData.js — TWO new MODULE-PRIVATE pure helpers, placed
//    immediately BELOW `withoutEditState` and ABOVE `preflightAccountExport`,
//    plus ONE changed line inside `preflightAccountExport`'s `campaigns` binding.

/** @param {unknown} value @returns {value is Record<string, any>} */
function isPlainRecord(value) { /* exact algorithm in §8 */ }

/**
 * THE VEIL REACHES THE PARKED SNAPSHOT (EM-B3f, design §11 and §12 item 4).
 *
 * `capturePulseSnapshot` embeds a WHOLE settlement per campaign member
 * (`campaignPulseHelpers.js:272-276`), and a PAUSED advance parks that snapshot on
 * the campaign record itself, inside the same atomic persist as the campaign
 * (`campaignAdvanceSession.js:578`). `preflightAccountExport` then puts `campaigns`
 * into the payload unprojected — so a strip that reaches only `savedSettlements`
 * leaves a paused realm's export carrying the DM's private layer for every member,
 * while the live settlement beside it is clean.
 *
 * ⛔ WHY THE STRIP IS A DOOR ACT AND NEVER A PROMOTER ACT: `runUndoLastPulse`
 * restores the account's OWN settlement and must keep restoring the DM's own layer
 * with it. Only the value LEAVING the account is veiled.
 *
 * ⛔ COPY-ON-WRITE IS THE CONTRACT, NOT AN OPTIMISATION. `state.campaigns` is the
 * LIVE store array by reference; an in-place drop would erase a DM's running parked
 * snapshot from the session and, at the next cacheCampaignState, from their save.
 *
 * Pure. REFERENCE-IDENTICAL when nothing is dropped: the campaign that went in comes
 * back, and so does every snapshot member that did not move. Reads every field by
 * DESTRUCTURE, never by a property access, so the observed-shape register cannot move.
 *
 * @param {unknown} snapshot a capturePulseSnapshot payload
 * @returns {unknown} the same snapshot when nothing was dropped, else a new one
 */
function withoutSnapshotEditState(snapshot) { /* exact algorithm in §8 */ }

/**
 * One campaign's parked pre-interval undo cursor, veiled. See above.
 * @param {unknown} campaign @returns {unknown} the same campaign, or a copy
 */
function withoutParkedSnapshotEditState(campaign) { /* exact algorithm in §8 */ }

//    …and `preflightAccountExport`'s one changed line (`:272`):
  const campaigns = (Array.isArray(state.campaigns) ? state.campaigns : [])
    .map(withoutParkedSnapshotEditState);
```

**Return shapes, exactly.**

- `isPlainRecord(value)` returns `value != null && typeof value === 'object' && !Array.isArray(value)`. It is spelled here because `src/lib/accountData.js` has no such guard today; it is the byte-identical twin of `src/lib/accountImport.js:267`, deliberately not imported (that module is the import half and this packet declares no dependency on it).
- `withoutParkedSnapshotEditState` returns its input **by reference** when the campaign is not a plain record, when `worldState` is not a plain record, when `pausedAdvance` is not a plain record, when `preIntervalUndo` is not a plain record, and when the snapshot walk moved nothing. Otherwise it returns `{ ...campaign, worldState: { ...worldState, pausedAdvance: { ...pausedAdvance, preIntervalUndo: <next> } } }`, each replaced key keeping its ORIGINAL position, and every sibling — `id`, `name`, `settlementIds`, `mapState`, `regionalGraph`, `wizardNews`, `contentBinding`, the world's `tick`, the cursor's `interval` — **untouched and by reference**.
- `withoutSnapshotEditState` returns its input **by reference** when neither `saves` nor `active` moved. Otherwise a new snapshot object in which every unmoved member is **the very element that went in**, and a moved one is `{ ...member, settlement: <stripped> }` with the member's own `id`, `campaignState` and key order preserved. A non-Array `saves`, a non-record element, and a non-record `active` all ride through **by reference, unexamined**.
- Both helpers **throw on nothing**, consume no PRNG, read no clock, mint no id, and **mutate nothing** — the campaign object they are handed is the live store's own.
- `preflightAccountExport`'s signature, arity, return shape, error codes and the ORDER of its statements are unchanged. `campaigns.length` at `:274`/`:348`, `inspectAccountContentReferences(campaigns, …)` at `:310` and the payload literal at `:377` all receive the veiled array — behaviour-preserving, because the strip touches only `worldState.pausedAdvance.preIntervalUndo` and content references read `contentBinding`.

**Ordering and precedence.** The walk runs at `:272`, i.e. **before** `inspectAccountContentReferences`, before `preflight.counts`, and before `serializedAccountExportByteLength(payload)` at `:415` — so the bytes measured are the bytes written. `settlements` is stripped at `:268-271` and `campaigns` at `:272`; the two are independent and order between them is immaterial.

### THE SPELLING IS MEASURED, NOT PREFERRED

Executed with the tree's own TypeScript against `scanReaders`' own rule (`legacy-reader-shape-scan.mjs:541-545`: a PropertyAccessExpression whose `.name` is an identifier not in `BUILTIN_MEMBERS` and not a write target), with a failing control (`E-SPELL`, `M13.out`):

| Source | COUNTED property reads | Receivers | Register cost |
|---|---:|---|---|
| the contracted walk | **2** | `Array` (`Array.isArray` ×2) — resolves to no known shape | **ZERO rows** |
| the refuted dotted alternative (the control) | **10** | `campaign.worldState` ×3, `…pausedAdvance` ×2, `…preIntervalUndo`, `u.saves`, `m.settlement` ×2, `m.settlement.dmLayer` | would MINT new entries for a file the baseline records |

`Array.isArray` is the live control's own spelling: `withoutEditState` and `preflightAccountExport` already use it in this exact file. **Every read of `worldState`, `pausedAdvance`, `preIntervalUndo`, `saves`, `active`, `settlement`, `dmLayer` and `decrees` in this packet's production code is a DESTRUCTURE.** A dotted read of any of them is a STOP (§11).

### State schema

⛔ **BOTH VALUES STAY OPAQUE**, exactly as EM-B3a ruled and EM-B3d and EM-B3e kept. This packet drops by key NAME (inside `withoutEditKeys`, which it calls) and asserts by serialized-substring absence beside a liveness anchor. It names no field inside either value.

**Absence rules:**

- **no `pausedAdvance`** — every unpaused campaign, which is the overwhelming majority: returned **by reference**, zero work.
- **a cursor with no `preIntervalUndo`** — the OLD-SHAPE cursor a previous build wrote (`campaignAdvanceSession.js:138` documents it): returned **by reference**.
- **a snapshot whose members carry neither key** — returned **by reference**, elements included. This is the dormancy case and it is **100 % of real records at this landing**.
- **a malformed snapshot** (`saves` absent, `null`, a string, a number; an element that is `null`, a primitive or an array; an element with no `settlement`; `active` `null` or a primitive): every such value rides through **by reference**, nothing throws, and a well-formed member in the same array is still stripped. Executed: `C3.malformedSaves NO-THROW len=8 badElementsByRef=true lastStripped=true`.
- **a settlement nesting its OWN `versionHistory`** — **OUT OF CONTRACT**, inherited from EM-B3e §13 Q2 for the identical reason: `snapshotSettlement` deletes a settlement's own timeline on both the record and the restore, so no product writer makes one.

### Transition table

| Prior state | Input/event | Guard | Next state | Receipt |
|---|---|---|---|---|
| a paused campaign whose parked snapshot's members carry both keys | `buildAccountExport` → `preflightAccountExport` | `Object.hasOwn` inside `withoutEditKeys`, per member | the export's campaign keeps its cursor, its interval, its members and their order; no member's settlement carries either key | none |
| the same account | the DM resumes or abandons the advance in the app | this packet adds no arm here | unchanged — the LIVE parked snapshot still carries the DM's own layer, and `runUndoLastPulse` still restores it | none |
| a campaign with no `pausedAdvance` | either | no cursor | **the identical campaign reference** | none |
| a paused campaign whose parked snapshot carries neither key (every record today) | `buildAccountExport` | no member moved | **the identical campaign reference** | none |
| any campaign | the account IMPORT | — | **unreachable**: the import mints a fresh world and reads `rawCampaign.worldState` nowhere (§1) | the reconciliation door's `world_state_not_imported` notice, unchanged |
| the owner's OWN save carrying both keys | save / load / `normalizeSettlement` / any undo | this packet adds no arm here | both keys survive byte-exact | none |

### Determinism

- **Hash/fork key:** `NONE`. No random value, no clock, no id.
- **Stable enumeration:** the shallow copy after a rest-destructure preserves every surviving key's insertion order at all four levels. Executed: `C1.siblingsKept KEPT interval=one_year tick=4 name=R`, `C1.stripped saves=2 order=a,b activeKept=true`.
- **Rounding/clamping:** none.
- **No-draw behaviour:** on a campaign carrying no paused advance, or one whose snapshot carries neither key, the walk returns **the identical object reference**. ⭐ **At this landing that is EVERY campaign in EVERY export**, because nothing in `src/` writes either key. The reference-identity arms (C2) are therefore this packet's **dormancy proof**, not a nicety.

### Flag and dormancy

- **Flag:** `NONE`.
- **Dormancy:** the whole packet is dormant at its own landing and becomes load-bearing the moment EM-C4a writes the first `dmLayer`.
- ⛔ **HZ-PERSIST-UNGATED holds by construction:** neither helper sits inside a flag conditional, and no persistence path is touched.
- **Golden posture:** `UNCHANGED`. `tests/property/generatorGoldenMaster.test.js` and `tests/property/dossierProseManifest.test.js` must not move; nothing here reaches the generator. Motion is a STOP (§P3.1).

### Lifecycle

| Create | Read | Persist | Reload | Regenerate | Undo | Import/migrate | Public veil |
|---|---|---|---|---|---|---|---|
| never here; EM-C4a is the first writer and lands AFTER | adds **no domain reader** — every field is destructured (§6) | **untouched** — `campaigns.js:183` still admits `worldState` whole, and a DM's own parked snapshot keeps its layer in their own save | untouched — the cursor rehydrates with the campaign | untouched | ⭐ **untouched, and deliberately so** — `runUndoLastPulse` restores the account's OWN parked snapshot, layer included. The strip is a door act, never a promoter act | ⭐ **THE EXPORT IS THIS PACKET'S CHARGE.** The IMPORT is unreachable (§1) and the round trip's existing campaign-world loss is coded, declared and LEFT AS IS | untouched: EM-B3a's, EM-B3b's, EM-B3d's and EM-B3e's landed veils carry every other way OUT |

### Alignment and edit story

- **Alignment:** `DECLARED EMPTY:` no NPC, faction or deity state, no alignment axis.
- **Edit story:** `ENGINE-ONLY:` this packet exposes no verb. It is the last reach of EM-B3's travel seams, laid down before the writer that makes it matter.

## 7. Exact change manifest

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `MODIFY` | `src/lib/accountData.js` | three new module-private helpers `isPlainRecord`, `withoutSnapshotEditState`, `withoutParkedSnapshotEditState`, placed after `withoutEditState` and before `preflightAccountExport`'s docblock; and ONE changed line in `preflightAccountExport`'s `campaigns` binding (`:272`) | `+46 eff` new, **1 changed / 0 added** at the binding | **(a)** Write the three helpers with §6's JSDoc and §8's algorithms. Read every field by DESTRUCTURE (`const { worldState } = campaign \|\| {};`), never `campaign.worldState`. Call EM-B3e's `withoutEditKeys` — **do not re-implement the drop**. **(b)** Change `const campaigns = Array.isArray(state.campaigns) ? state.campaigns : [];` to the two-line `.map(withoutParkedSnapshotEditState)` form of §6 **in place** (one changed line becoming two; nothing above it moves). **(c)** ⛔ Amend EM-B3a's docblock above `withoutEditState` IN PLACE so its *"the one seam … spelled here and nowhere else"* sentence names the campaigns seam too: at most **two** raw comment lines, **0 effective**. **(d)** `withoutEditState`, `withoutEditKeys`, `preflightAccountExport`'s signature, arity, return shape and statement ORDER are untouched. |
| `TEST` | `tests/lib/accountData.test.js` | C1, C2, C3, C4, C5 | `n/a` | **5 literal-titled `it`s appended INSIDE the EXISTING `describe('buildAccountExport')`** (`:86`), after the last existing `it` (`:340`). **No existing arm is edited and no new `describe` is opened.** Reuse the file's own `load()` helper (`:29`); add one local `pausedCampaign(...)` fixture helper (not a test). No `.each`, no loop-generated test, no nesting, and **no variable, parameter or binding named `it`, `test` or `describe`**. ⛔ **`FROZEN_UNANCHORED_NEGATIVES['tests/lib/accountData.test.js'] = 1`** (`negativeAssertionAnchor.walker.test.js:603`) reds on a SHRINK: **do not anchor `:161`'s `expect(JSON.stringify(out)).not.toMatch(/credits\|"role"/)`**, and add **no** new un-anchored negative — every absence is `expect(serialized.includes('"dmLayer"')).toBe(false)` beside a liveness anchor. |
| `TEST` | `tests/lint/snapshotPromotion.census.walker.test.js` | promoter 3's declared `why` string | **0 new `it`s**, `+0 eff` | ⭐ **CREATED BY THE SIBLING `TOOL-30`, WHICH LANDS FIRST IN THIS TRAIN** (interim rule 4). Re-word **ONLY** promoter 3's declared `why` to add the sentence this packet makes true — the parked snapshot's member settlements leave the account STRIPPED, so the veil holds through the export door. ⛔ **NOT ONE OTHER BYTE**: no arm added, edited, removed or renamed; no `it`, `test` or `describe`; no assertion; no roster entry. ⛔ **The §6b foreign-reach sentence is NOT this packet's to correct** — the chair has resumed TOOL-30's own lane for it (judgment 97 clause 2). If promoter 3's `why` already carries a correct export sentence at the dispatch base, this row is a **no-op and the packet says so in the receipt** rather than inventing a change. |

⚠ **`TEST`, not `CREATE`, for both test rows** (interim rule 4): `tests/lib/accountData.test.js` exists at the verified base, and `tests/lint/snapshotPromotion.census.walker.test.js` is created by the NAMED sibling TOOL-30 before this member. **This packet CREATEs no file**, so it opts into no new directory walker.

**Generated artifacts:** `NONE`. No `checks` command of this packet runs a generator, so the ordering hazard of §P2 row 12 does not arise.

**Interim rule 1 — the wiring census:** `+0`. This packet CREATEs no `.js` under `src/generators/**` or `src/domain/**` — the two roots measured — and `src/lib/accountData.js` is not in the census's `stamp.files`. **`docs/content/wiring-census.json` appears in neither §7, nor the `changeManifest`, nor `checks`.**

**Interim rule 13 — line-addressed registers:** `none found`, measured at `429141e2d` for all three paths. `src/lib/accountData.js` appears only in `scripts/.observed-shape-readers-baseline.json` (three hits, CONTENT-keyed), `scripts/.slugify-idiom-baseline.json:33` (a file list) and `tests/lint/publicIdentitySingleRender.walker.test.js:56` (a file-keyed rationale map). `tests/lib/accountData.test.js` appears only at `tests/lint/negativeAssertionAnchor.walker.test.js:603`, a FILE-keyed ceiling this packet neither shrinks nor grows. `tests/lint/snapshotPromotion.census.walker.test.js` does not exist yet and is addressed nowhere. **No register addresses any of this packet's paths BY LINE, so no re-address is owed.**

**Interim rule 15 — a CREATE under an ENFORCER directory:** **none.** `ENFORCER_DIRS` read at this tip (`tests/lint/mutationCoverage.shared.mjs:36-45`) is `tests/lint · tests/design · tests/docs · tests/data · tests/copy · tests/security · tests/edgeFunctions · tests/generators`. This packet's `tests/lint` row is a `TEST` on a file its sibling creates, not a CREATE, so **no `scripts/mutation-coverage-manifest.json` row is owed** — TOOL-30 owes the row for the file it creates, and a second holder would collide on the `rowKey`.

**Interim rule 16 — a renamed or re-shaped binding:** this packet renames nothing and changes no exported call shape. All three new helpers are module-private; `preflightAccountExport`'s and `buildAccountExport`'s signatures, arities and return shapes are byte-identical. Command: `git grep -n -F 'preflightAccountExport' -- tests scripts docs/implementation` → `tests/lib/accountData.test.js` only, which asserts on the RESULT, not the call shape.

**Interim rule 17 — the tuning inventory:** **n/a.** `scripts/lib/tuning-inventory.mjs` counts files under `src/domain` and `src/generators`; `src/lib/accountData.js` is under neither, so `countUnregisteredNamed` and `countBareDecimals` are not computed for it and no count moves. This packet introduces no numeric literal of any kind.

### ⭐ THE BUNDLE BUDGETS, PRICED — AND THIS PACKET OWES NOTHING

| Budget | Instrument and bound | Does this packet land in it? |
|---|---|---|
| First-paint eager closure | `EAGER_FIRST_PAINT_MODULES` IMPORTED live from `vite.config.js` (E-BUD); as executed history with an as-of mark, a **Set of 269** at `429141e2d`. Bound: `CLOSURE_BUDGET_BYTES` `1_048_000` (`tests/build/vendorPdfLazy.test.js:565`) | **NO** — `src/lib/accountData.js` probes `false` against the imported set. Delta **+0 modules, +0 B** |
| Generation worker | `WORKER_BUNDLE_CEILING_BYTES` (`tests/build/generationWorkerLazy.test.js`), EXACT, zero slack | **NO** — zero hits for `accountData` under `src/workers` |
| Lazy engine chunk | `tests/build/vendorPdfLazy.test.js` | **NO** — the chunk rule keys on `/src/generators/` plus `/src/data/narrativeData.js`; no path of this packet matches |
| Edge-shared bundles | the five `supabase/functions/_shared/*.meta.json` `inputs` closures | **NO** — zero hits for `accountData` in any meta |

⇒ **No ceiling TEST row, no stated byte bound, no re-mint.** ⛔ **Interim rule 14: this packet is NOT its train's byte-arm holder and carries NO row on any budget path**, predicting **+0 B** in every closure. Train EM-T11's holder is the chair's to name at the pre-proof.

**No other file may be edited.**

## 8. Ordered coding sequence

0. Dispatch and seal the packet; stop on any preflight mismatch. **Confirm `withoutEditKeys` resolves at count 1 in `src/lib/accountData.js`** and that `tests/lint/snapshotPromotion.census.walker.test.js` exists — either absence is a STOP (§11).
1. **Capture the baseline**, in this order, before any edit: (a) `shasum -a 256 tests/fixtures/generator-golden-master.json tests/fixtures/dossier-prose-manifest-golden.json`; (b) the lighting tuple from `tests/lint/.lighting-census-baseline.json`; (c) `node scripts/check-observed-shape-readers.mjs > "$SCRATCH/osr-base.log" 2>&1; echo "exit $?"` — **never through a pipe**; (d) the suites this packet must not move, green: `accountData`, `editTravel`, `accountImport`, `accountImportSlice`, `accountDataPrivacySection`.
2. **Write C1, C4 and C5 as FAILING tests first** against the uncured tree and QUOTE the red — C1 and C4 red because the serialized export still contains `"dmLayer"`; C5 reds because the export still carries the key (its freeze half passes vacuously today, and that is stated rather than hidden). ⚠ **C2 and C3 cannot be red-first**: C2's reference identity holds vacuously before the walk exists and C3's no-throw holds trivially. Per §P6's EM amendment each is proved by its **counterforce**, planted and restored to the exact pre-mutant SHA-256:
   - **C2's counterforce** — replace `withoutParkedSnapshotEditState`'s final reference test with an unconditional `return { ...campaign }`: C2's named title reds.
   - **C3's counterforce** — remove the `isPlainRecord(member)` guard inside the `saves` walk: C3's named title reds with a `TypeError`.
   Both mutants live in **this packet's own file**, so nothing outside the manifest is planted.
3. `src/lib/accountData.js`: add the three helpers, change the `campaigns` binding, amend EM-B3a's docblock in place. `git diff -U0 -- src/lib/accountData.js` must show the helper block, **exactly one changed line becoming two** at the `campaigns` binding (`:272` at the pre-proof's tip — re-find it by the literal `const campaigns = Array.isArray(state.campaigns)`, never by the number), and at most two added comment lines.
4. `tests/lib/accountData.test.js`: C1–C5 appended inside the existing `describe('buildAccountExport')`.
5. `tests/lint/snapshotPromotion.census.walker.test.js`: promoter 3's `why` string only. `git diff -U0` must show **one changed string and nothing else** — or no diff at all, if the sentence is already correct (record which in the receipt).
6. **Run the COUNT PROVER before the seal** (interim rule 12), from the kit path: `node <kit>/packets-waiting/EM-B3f.count-prover.mjs <kit>/packets-waiting/EM-B3f.md <kit>/packets-waiting/EM-B3f.manifest.json`. Exit must be `0`.
7. Run focused verification (§10), then `tests/lint` WHOLE in its two halves, then the goldens plain.
8. `shasum -a 256` the two goldens again: identical to step 1(a). Any motion is a STOP.
9. Write the completion receipt.

**Bounded algorithm — `isPlainRecord(value)`:**

```text
1. Return value != null && typeof value === 'object' && !Array.isArray(value).
```

**Bounded algorithm — `withoutSnapshotEditState(snapshot)`:**

```text
1. Destructure { saves, active } = snapshot.
2. nextSaves := saves
3. If saves is an Array:
     moved := false
     mapped := saves.map(member =>
       a. if !isPlainRecord(member)          -> return member            (by reference)
       b. destructure { settlement } from member
       c. scrubbed := withoutEditKeys(settlement)
       d. if scrubbed === settlement         -> return member            (by reference)
       e. moved := true; return { ...member, settlement: scrubbed })
     if moved then nextSaves := mapped
4. nextActive := active
5. If isPlainRecord(active):
     destructure { settlement } from active
     scrubbed := withoutEditKeys(settlement)
     if scrubbed !== settlement then nextActive := { ...active, settlement: scrubbed }
6. If nextSaves === saves AND nextActive === active, return snapshot.     (REFERENCE-IDENTICAL)
7. Return { ...snapshot,
            ...(nextSaves  === saves  ? {} : { saves:  nextSaves }),
            ...(nextActive === active ? {} : { active: nextActive }) }.
```

**Bounded algorithm — `withoutParkedSnapshotEditState(campaign)`:**

```text
1. Destructure { worldState } = campaign || {}.
2. If !isPlainRecord(worldState)             -> return campaign.          (REFERENCE-IDENTICAL)
3. Destructure { pausedAdvance } = worldState.
4. If !isPlainRecord(pausedAdvance)          -> return campaign.          (REFERENCE-IDENTICAL)
5. Destructure { preIntervalUndo } = pausedAdvance.
6. If !isPlainRecord(preIntervalUndo)        -> return campaign.          (REFERENCE-IDENTICAL)
7. next := withoutSnapshotEditState(preIntervalUndo)
8. If next === preIntervalUndo               -> return campaign.          (REFERENCE-IDENTICAL)
9. Return { ...campaign,
            worldState: { ...worldState,
              pausedAdvance: { ...pausedAdvance, preIntervalUndo: next } } }.
```

⛔ Steps 1, 3, 5 and the walks' `{ settlement }` reads are DESTRUCTURES, never property reads (§6). Every one of these branches was executed standalone against this read tip before this packet was written (`E-CONTRACT`, `M12-contract.out`).

## 9. Acceptance matrix

| ID | Case | Fixture/input | Required observation | Test home |
|---|---|---|---|---|
| C1 | ⭐ **THE PARKED SNAPSHOT, STRIPPED IN BOTH POSITIONS** | a campaign whose `worldState.pausedAdvance.preIntervalUndo` carries **two** `saves[]` members AND an `active`, each settlement carrying both keys planted opaque, driven through `buildAccountExport` | the serialized export contains neither `"dmLayer"` nor `"decrees"` **at any depth**. Anchored: the cursor survives — `preIntervalUndo.saves` has length **2** in the order `a, b`, `active.saveId` is `'a'`, `pausedAdvance.interval` is `'one_year'`, `worldState.tick` and the campaign's `name` and `settlementIds` are intact, and each member's `campaignState.phase` and `settlement.population` survive. Executed standalone: `C1.stripped CLEAN`, `C1.siblingsKept KEPT` | `tests/lib/accountData.test.js` |
| C2 | ⭐ **DORMANCY — REFERENCE IDENTITY, the case 100 % of real records take** | (i) a paused campaign whose snapshot carries neither key; (ii) a campaign with no `pausedAdvance`; (iii) a campaign with no `worldState`; (iv) an OLD-SHAPE cursor whose `preIntervalUndo` is absent | in every case the payload's campaign is **the very object that went in** (`toBe`) — the sibling of `settlements`' landed dormancy pin. Executed standalone, all four `REF-IDENTICAL` | `tests/lib/accountData.test.js` |
| C3 | **A MALFORMED SNAPSHOT NEVER THROWS AND NEVER SHELTERS A REAL MEMBER** | a `saves` of eight elements — `null`, a string, a number, `[]`, `{ id }` with no settlement, `{ settlement: [] }`, `{ settlement: null }`, and one well-formed member carrying both keys — with `active: null`; plus `worldState: []`, `pausedAdvance: 'x'` and `preIntervalUndo: null` as three sibling inputs | nothing throws; the array's length and order are unchanged; the seven malformed elements come back **by reference**; the one well-formed member is still stripped; and the three sibling inputs each return **by reference**. Executed: `C3.malformedSaves NO-THROW len=8 badElementsByRef=true lastStripped=true` | `tests/lib/accountData.test.js` |
| C4 | ⭐ **THE WHOLE DOOR, AND THE LIVE SETTLEMENT'S PARITY** | a state carrying BOTH a saved settlement with both keys AND a paused campaign whose snapshot carries them, through `buildAccountExport` | the whole serialized payload contains neither key — `settlements` clean (EM-B3a/EM-B3e's landed work, re-asserted here as the parity anchor) **and** `campaigns` clean. Anchored: `preflight.counts.campaigns` and `counts.settlements` are unchanged from the pre-cure run, so the strip did not drop a record. Executed: `C4.today CARRIES(HOLE)` → `C4.cured CLEAN` | `tests/lib/accountData.test.js` |
| C5 | ⛔ **COPY-ON-WRITE — THE EXPORT MUST NOT ERASE THE DM's RUNNING SNAPSHOT** (the chair, judgment 97 clause 3) | a paused campaign carrying both keys, **DEEP-FROZEN** (`Object.freeze` applied recursively to the campaign, its `worldState`, its cursor, the snapshot, every member and every settlement) and passed as `state.campaigns[0]`, driven through `buildAccountExport` | the export **does not throw**; the exported campaign carries neither key; and **the frozen live campaign still carries both** — `expect(JSON.stringify(liveCampaign).includes('"dmLayer"')).toBe(true)` — proving the walk never wrote through its input. Executed: `C5.frozenLive NO-THROW`, `C5.storeIntact LAYER-SURVIVES` | `tests/lib/accountData.test.js` |

This table is the entire edge-case budget. **The observed-shape register is deliberately NOT an acceptance case** — §6's measurement is a compile-time fact about a spelling and `node scripts/check-observed-shape-readers.mjs` in §10 is its proof.

⭐ **THE MUTANT TABLE, EXECUTED AT THIS READ TIP** (`E-CONTRACT`, `M12-contract.out`). Every mutant the chair named, and the arm that convicts it:

| Mutant | Convicted by | Executed verdict |
|---|---|---|
| (a) strips the live settlement only — no campaign walk at all | **C1** (and C4) | `MUT.a CONVICTED by C1` |
| (b) strips `saves[]` but leaves `active.settlement` | **C1** | `MUT.b CONVICTED by C1` |
| (c) strips the FIRST member only | **C1** (the fixture carries **two** members) | `MUT.c CONVICTED by C1` |
| (d) copies always — the dormancy killer | **C2** | `MUT.d CONVICTED by C2` |
| (e) ⛔ **strips IN PLACE — erases the DM's running parked snapshot** | **C5** | `MUT.e CONVICTED by C5` — `store snapshot erased=true` on an unfrozen victim, and a `TypeError` against C5's deep-frozen fixture |

## 10. Verification commands

```sh
# Focused static checks — eslint runs BARE, never through the mutex
npx eslint src/lib/accountData.js tests/lib/accountData.test.js \
  tests/lint/snapshotPromotion.census.walker.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict

# The observed-shape register, plain and read-only — NEVER through a pipe
node scripts/check-observed-shape-readers.mjs > "$SCRATCH/osr-head.log" 2>&1; echo "exit $?"; tail -5 "$SCRATCH/osr-head.log"

# ⭐ THE COUNT PROVER (interim rule 12) — KIT furniture, run from the kit path, never placed
node <kit>/packets-waiting/EM-B3f.count-prover.mjs \
  <kit>/packets-waiting/EM-B3f.md <kit>/packets-waiting/EM-B3f.manifest.json

# Focused tests — ONE test directory per gated run
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lib/accountData.test.js tests/lib/editTravel.test.js tests/lib/accountImport.test.js \
  tests/lib/importScrub.test.js tests/lib/importReconciliation.test.js \
  tests/lib/importReconciliationRecovery.test.js tests/lib/accountSettlementContentPortability.test.js

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/store/accountImportSlice.test.js

# The GOVERNING LINT FILES — sealed in `checks` (interim rule 8), the lighting walker excluded
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/negativeAssertionAnchor.walker.test.js tests/lint/publicIdentitySingleRender.walker.test.js \
  tests/lint/contractTestAntiVacuity.walker.test.js tests/lint/snapshotPromotion.census.walker.test.js

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/ui/accountDataPrivacySection.test.jsx tests/ui/accountW4d.test.jsx

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/components/campaignImportPanel.test.jsx tests/components/covenantClaimsParity.test.js

# ⭐ THE BUILD LANE'S INSTRUMENTS — NOT sealed checks (interim rule 8). Run both before the commit.
#   (a) the EXCLUDED directory run — MUST EXIT 0
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint --exclude=tests/lint/sovereigntyLightingContract.walker.test.js
#   (b) the LIGHTING WALKER ALONE — reds BY DESIGN under a train
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/sovereigntyLightingContract.walker.test.js

# The goldens, plain
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js

node scripts/implementation-packets.mjs validate

# Sealed receipt and exact-state handoff; neither is landing authority
npm run check:packet -- EM-B3f
npm run implementation:resume -- EM-B3f

# Wave-end presentation-safe invocation; never pipe
npm run check:tail
```

**Expected:** every command exits `0`. ⛔ **A gate line with no printed test count DID NOT RUN** — `gate-mutex.sh --run` gives up after its poll budget and **exits 3** (`GAVE UP`), so never drop the two exports. Report actual counts; copy no historical count. No generator is among these commands, so no step invalidates a later one. ⛔ **This packet touches no `src/components/**`, no route, no `data-testid` and no accessible name, so interim rule 10 names NO `e2e/` spec.**

**The train's interior red (named before it exists):** `tests/lint/sovereigntyLightingContract.walker.test.js` reds on this member's commit, because the census tuple is an exact equality and this member adds test titles. ⛔ **The prediction is a DELTA, never a tuple.** The predicted message has the SHAPE `expected <baseline titles + 5> to be <baseline titles>`.

| Figure | Walker semantics | **EM-B3f's delta** | Derivation |
|---|---|---:|---|
| `files` | every `*.test.js(x)` under `tests/` | **+0** | this packet CREATEs no test file |
| `parked` | files with ≥1 park reason | **+0** | no file's park status changes: every addition is a straight-line literal `it` under an EXISTING literal `describe`, with no `.each`, no loop, no conditional and no rebinding of `it`/`test`/`describe` |
| `credited` | files with zero park reasons | **+0** | `tests/lib/accountData.test.js` is already credited or already parked; this packet does not move it either way |
| `titles` | literal test titles in CREDITED files | **+5** | 5 new `it`s in `tests/lib/accountData.test.js`; the `tests/lint` row adds **0** |
| `suiteTitles` | literal `describe` titles in CREDITED files | **+0** | no new `describe`; the additions join the existing `describe('buildAccountExport')` |

⭐ **THE CREDITED PREMISE IS NO LONGER PLAUSIBLE — THE PRE-PROOF EXECUTED THE WALKER'S OWN CLASSIFIER.** `parkReasonsFor` is module-private to `tests/lint/sovereigntyLightingContract.walker.test.js`, so the pre-proof sliced the walker's classifier chain (its imports through `const parkReasonsFor = (src) => classify(src).reasons`) into a plain-node module, pinned its `ROOT` to the read tip and ran it: **`parkReasonsFor('tests/lib/accountData.test.js') = [] → CREDITED`**, `liveTitlesIn = 12`, `liveSuiteTitlesIn = 3`. Re-run over the same file with five straight-line literal `it`s appended inside the existing `describe('buildAccountExport')`: still `[]` (CREDITED), **titles 12 → 17 (+5)**, **suiteTitles 3 → 3 (+0)**. The `+5` above is therefore **CONFIRMED**, and the `+0` fallback this row used to carry is discharged.

**The absolute is the chair's stamp at promotion**, read from the live `tests/lint/.lighting-census-baseline.json` and added to this delta. Re-derived whole at the TERMINAL, by the chair, never inside this packet (§P2.1, §P3.2). ⛔ The deferred lighting row names `tests/lint/.lighting-census-baseline.json`, never the walker.

**Registers that do NOT move, measured at the read tip:** the observed-shape register (**zero rows** — E-SPELL); `scripts/mutation-coverage-manifest.json` (**zero rows owed** — no CREATE; TOOL-30 owes the row for the file it creates); `docs/content/wiring-census.json` (**`+0`**, unnamed anywhere in this packet); `tests/lint/.prose-numerics-baseline.json` (zero rows, nothing rendered); `scripts/.size-baseline.json` (no entry, none owed); `negativeAssertionAnchor.walker.test.js`'s ceiling for `tests/lib/accountData.test.js` (stays at **1**); `scripts/check-writer-reach.mjs` (no `src/domain/edit/**` file in this manifest); and **no bundle ceiling** (this packet reaches none).

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and `EM-PREAMBLE.md` §P8, stop if:

- the dispatch seal is missing, invalid, or belongs to another worktree state; or resume reports authority, HEAD, foreign-work or receipt-integrity drift;
- **`withoutEditKeys` does not resolve at count 1 in `src/lib/accountData.js`** — EM-B3e has not landed, and this packet has no drop to call. ⛔ **Do NOT re-implement it**;
- **`tests/lint/snapshotPromotion.census.walker.test.js` is absent** — TOOL-30 has not landed and the `TEST` row would refuse at `validate:packets`;
- **a PROPERTY READ of `worldState`, `pausedAdvance`, `preIntervalUndo`, `saves`, `active`, `settlement`, `dmLayer` or `decrees` appears in this packet's production code** (`campaign.worldState`, `u.saves`, `m.settlement`, any dotted access). It mints observed-shape rows through a door only the chair may open (§6);
- ⛔ **the walk MUTATES its input**, anywhere, by any spelling — an assignment through the campaign, the world, the cursor, the snapshot, a member or a settlement. `state.campaigns` is the LIVE store array; an in-place drop erases a DM's running parked snapshot. This is C5's charge and it is the sharpest hazard in the packet;
- **the walk clones unconditionally**, or a reference-identity branch is removed — that is the dormancy contract and the bytes of every export ever taken;
- a strip is proposed on ANY import path, in `src/lib/campaigns.js`, in any campaign persistence writer, or inside a promoter action — each is a different packet and two are refuted premises;
- **a SECOND settlement-bearing position is discovered inside a campaign record** that this packet does not cover, or a way IN for a foreign campaign world is found — either is a finding to report, never to fold in silently;
- `tests/lib/accountData.test.js`'s existing un-anchored site at `:161` is anchored, or the file acquires a second un-anchored negative;
- any of the twelve existing `it`s in `tests/lib/accountData.test.js` is edited, or a new `describe` is opened;
- **anything but promoter 3's declared `why` string changes in `tests/lint/snapshotPromotion.census.walker.test.js`** — an arm, an assertion, a roster entry, a test title. If TOOL-30's landed `why` already carries the export sentence, make NO edit and record the no-op;
- `tests/lib/editTravel.test.js` is edited — it is **EM-C4a's**, a member of this same train;
- a golden, the prose manifest, or any register outside this manifest moves;
- an acceptance case needs a sixth sibling, or any §3 limit is exceeded;
- the count prover (§10) exits non-zero.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into the next wave.

## 12. Completion receipt

- Base SHA:
- Dispatch bundle and seal identity:
- Final commit or working-tree state:
- Exact changed files and effective-line deltas (predicted: `accountData.js +46 eff` with **1 changed / 0 added** at the `campaigns` binding, plus at most two added comment lines at `0 eff`; one test file; one walker string):
- The net-zero proof at the binding: `git diff -U0 -- src/lib/accountData.js` showing the helper block and exactly one changed line at `const campaigns =`:
- ⭐ **The walker row, proven:** `git diff -U0 -- tests/lint/snapshotPromotion.census.walker.test.js` showing **one changed string and nothing else** — or NO diff, with the sentence quoted from TOOL-30's landed text to prove the no-op:
- Acceptance cases C1–C5, and the TWO counterforces §8 step 2 requires, quoted beside C2 and C3 (the planted mutant, its red title, the restored SHA-256, the re-run green):
- ⛔ **C5's copy-on-write proof, quoted in full:** the deep-freeze helper, the `buildAccountExport` call that did not throw, and the assertion that the live campaign still carries both keys afterwards:
- The COUNT PROVER's output and exit code, run from the kit path with both arguments (interim rule 12):
- Focused commands, exits, and counts (every gate line carrying a printed test count):
- `tests/lint` in its two instrument halves (§10): the EXCLUDED directory run (exit 0) and the lighting walker ALONE (the named interior red). Exit, count, and wall-clock for each:
- ⭐ **`parkReasonsFor('tests/lib/accountData.test.js')`, printed** — the pre-proof already printed it (`[]`, CREDITED; titles 12 → 17) and §10 records it as CONFIRMED. Re-print it at the landing against the FINAL file, and STOP if it is non-empty:
- The observed-shape register: exit captured from the command itself, with its finding count before and after — predicted **identical, zero rows moved**:
- The lighting census delta, re-derived at the terminal against the DELTA predicted in §10 (`files +0 · parked +0 · credited +0 · titles +5 · suiteTitles +0`) applied to the baseline the chair stamped at promotion:
- Both typecheck configurations, by name:
- Dormancy/golden result (posture UNCHANGED): the two `shasum -a 256` values before the first edit and after the last, identical:
- Bundle/first-paint result: the four budgeted closures were priced at compile and this packet reaches **NONE** (§7); the train's byte-arm holder is the chair's to name and this packet carries no row on its path:
- Registers moved: observed-shape `0 rows` / mutation-coverage `none owed` / wiring census `+0, unnamed` / prose-numerics `n/a` / size-baseline `n/a` / anchor ceiling `unmoved at 1` / bundle ceilings `none touched`:
- Deviations: `NONE | STOP`
- Out-of-scope observations, without investigation — carried forward from the compile so the chair can slot each one (the owner's law: there is never deferred work):
  - ⛔ **TOOL-30's §6b headline and promoter-3 foreign-reach sentence are REFUTED** (§1). The chair has resumed TOOL-30's own lane to correct them (judgment 97 clause 2). This packet edits only the export sentence it makes true.
  - **The account export/import round trip discards every campaign world on import** — tick, stressors, proposals, pantheon, war ledger and any paused advance; the campaign returns fresh with remapped members. **Coded and declared** (`accountImportBody.js:758-761`; `importReconciliationAdmission.js:396`), LEFT AS IS by the chair's ruling. Recorded so a later lane does not re-discover it as a defect.
  - **`src/lib/accountData.js` now spells `isPlainRecord` a second time in the estate** (the first is `src/lib/accountImport.js:267`). Two four-line twins is not a defect, but it is the shape a third would make a habit. Recorded, not investigated.
  - ⭐ **EM-C4a's `Depends on` row owes `EM-B3f`** beside `EM-B3e` and `EM-B3d`: from EM-C4a's landing the parked snapshot's blob stops being inert. A compile lane does not edit another packet; this is the chair's one-line act.
- Judgment calls: `NONE` beyond the two questions of §13, each of which is the chair's.

## 13. Questions only the chair can answer

**Q1 — SHOULD THE ACCOUNT EXPORT PROJECT CAMPAIGNS AT ALL, RATHER THAN STRIP ONE SUBTREE?** Measured: `preflightAccountExport` puts `state.campaigns` into the payload **wholly unprojected** (`:272`, `:407`), which is why this hole existed and why a fourth one could. `savedSettlements` has a named projection (`withoutEditState`); `campaigns` has none. The lawful narrow cure is this packet's — strip the one measured settlement-bearing subtree. The structural cure is a `withoutCampaignPrivateState(campaign)` projection with an explicit allow/deny posture over the whole campaign record, which is a **new capability on a paid surface and a persistence-shape question** — the owner's class, not a compile lane's. **The lane recommends the narrow cure AS COMPILED**, and recommends the structural question be slotted as its own design item rather than folded here. ⚠ Until it is, the guarantee is *"the veil reaches the one place a campaign nests a settlement today"*, not *"a campaign cannot carry private state out"*.

**Q2 — SHOULD THE WALK RECURSE INTO A SNAPSHOT MEMBER'S OWN `versionHistory`?** The contract covers the two positions a snapshot defines (`saves[i].settlement`, `active.settlement`) and does not recurse into `saves[i].settlement.versionHistory[j].settlement`. Measured, and inherited from EM-B3e's ruled Q4: `snapshotSettlement` deletes a settlement's own `versionHistory` both when a snapshot is recorded and when one is restored, so **no product writer can create that nesting**, and `capturePulseSnapshot` clones whatever the live row holds — which is a settlement without its own timeline. A hostile file could still hand-craft one, but it cannot reach a campaign at all (§1: there is no way in). **The lane recommends NO recursion**, identical to EM-B3e's ruled answer, and records that it improvised no second shape.
