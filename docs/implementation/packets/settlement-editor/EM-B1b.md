# Settlement editor / EM-B1b — the seven OFF-STAGE ops under the phantom consequence rule, and the guard-coverage instrument

- **Status:** `LANDED`
  ⚠ The status value above stands ALONE on its line because `parsePacketHeader`
  (`scripts/implementation-packets.mjs`) anchors the status row at end-of-line (J-TEWF1B-1).
- **Landed at:** `3aba638578722ec9d5c062588f2ba4bbf2b5d200` — the op family's second slice (version 6): operationsOffStage.js spliced into the catalogue by position, C1–C8 arms, EM-B1c1's B1 literal and B6 widened inside this member's own TEST row (judgment 152c), A6's title re-worded to the widened roster (judgment 156); the opGuardCoverage walker; build 3aba63857 on the train
- **Packet version:** 6
  - ⭐⭐⭐ **Version 6 is the RE-PROOF at train EM-T12's FLIPPED tip `16f0bc71c`**
    (2026-09-22, Opus pre-proof lane, session cce01f87). Version 5 was measured against
    EM-B1a version 9 and EM-B1c1 version 2 **AS PACKETS**; both have since LANDED, and
    judgment 134's law is that a sibling's LANDED fence is the only one a member may measure.
    Eight facts moved, every one by execution; **nothing was added to scope and no op row,
    payload, relation or acceptance COUNT changed**. (1) The composed catalogue is
    **TWENTY-TWO**, not twenty-one: EM-B1c1 landed `set-npc-name` (not `rename-npc`), so the
    fifteenth row is real. (2) ⛔ **THE FOUR SPLICE RUNS AND THEIR ANCHORS ARE UNCHANGED, and
    version 5's warning that run 4's anchor becomes `rename-npc` is REFUTED** — `set-npc-name`
    sorts AFTER `set-field`, at index 18 of the composed twenty-two, so it anchors nothing;
    run 4 stays `remove-npc` before `set-field`. (3) `operations.js` measures **233** at the
    tip and **238 of 250** spliced (version 5 said 231 to 236, and its +2 projection for
    EM-B1c1 was exactly right). (4) ⛔ **THE SPLICE'S IMPORT MUST BE ONE LINE**: measured, a
    four-line import makes the edit **+8** and §11 makes that a STOP. (5) ⛔⛔ **SIX LANDED
    ARMS GO RED ACROSS TEN ASSERTION SITES, NOT FOUR ARMS** — version 5 knew A1, A3, A4 and
    A6; **EM-B1c1's own B1 and B6 are new**, and B1 carries the hard literal
    `[11, 'set-institution-state', 'set-npc-status']` this member's splice moves to **18**.
    (6) ⛔ **THE ACCEPTANCE IDS COLLIDE**: EM-B1c1 landed `B1` to `B6` in the SAME literal
    `describe`, so this member's cases are re-lettered **C1 to C8** (RAISED R10). (7) The
    leaf's `guardsStated` may NOT spell `consequenceFor`: measured, the pre-proof replica's
    prose did, and this member's own counterforce scan (A3's matcher over the
    comment-stripped source) convicts a sentence that writes nothing. (8) Every dependency
    has LANDED and **the registered estate holds ZERO non-terminal packets**, so both
    `duplicate change path` errors and RAISED R4 are DISSOLVED. Version 5's own text follows.
  - ⭐⭐ **Version 5 is the PRE-PROOF's version (2026-09-21, Opus pre-proof lane, read tip
    `583f8f644` + EM-P4's lane commit `6981d1eff`).** Six facts were REFUTED BY MEASUREMENT and
    are corrected here; nothing was added to scope. (1) ⛔ **THE SPLICE IS FOUR POSITIONS, NOT
    ONE** (judgment 92): run through the tree's own `compareCodepoint`, the seven off-stage keys
    form FOUR discontiguous runs inside EM-B1a's fourteen, so a single spread — or one map spread
    four times — reds EM-B1a's A1 order arm. The leaf therefore exports FOUR GROUPS and
    `operations.js` takes ONE import and FOUR spread lines: **+5 effective, not +2** (measured
    231 → 236 of 250 with ESLint's own `max-lines` — ⭐ version 6 re-measures this as **233 → 238**). (2) The row carries **ELEVEN** fields, not
    ten, and `requires` is the **`{ world, registry }` SPLIT** — which is where design §13's
    clause (d) now lives exactly: every off-stage row's `requires.world` is **EMPTY**. (3) The
    catalogue is **B1a's FOURTEEN plus these seven**, stated as a derivation; "eighteen" and
    "twenty-five" predate ruling R8. (4) ⛔ **FOUR LANDED EM-B1a ARMS GO RED** when these rows
    appear — A1, A3 (twice), A4 and A6 — and this packet now WIDENS each in place under its own
    seal, by its own `TEST` row. (5) Every row declares `target: 'phantom'` — one of the eleven
    fields version 4 never spelled (**RAISED R7**). (6) §14's eleven-row set is DEMOTED to a
    raised item: `sue-for-peace`'s `requires.world` contradicts this packet's own C3 (B3 at version 5).
  - ⭐ Version 4 is the DOCS repair only (ESTATE-REPAIR-4, 2026-09-20, read tip
    `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`; the chair's judgment 41, question 4): §7's `CREATE`
    instruction said *"Import the two closed vocabularies from `operations.js`"* while §7's own
    `MODIFY` row has `operations.js` import THIS leaf — **a module CYCLE whose `const` bindings sit
    in the temporal dead zone**. EM-B1c1's IMPORT-NOTHING shape is the ratified correction and is
    applied here, with the vocabulary membership asserted **from the TEST** instead. ⛔ **No op row,
    payload, budget, acceptance COUNT, required symbol or change row moved** — the leaf loses one
    import line, so the ≈83 estimate does not rise.
  - Version 3 is the DOCS repair only (ESTATE-REPAIR-3, 2026-09-20, read tip `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`),
    bringing this packet under `COMPILE-RULES.interim.md`: §7's registration table gains the
    **P2.13 DEFERRED WIRING-CENSUS ROW** it had OMITTED (interim rule 1), and the manifest's
    `REGISTER` row on `scripts/mutation-coverage-manifest.json` gains the **`rowKey`** field
    TOOL-27 keys its row-level exemption on, so this packet and EM-A1 can hold that path
    without colliding. Its `_note`'s `704 -> 705` becomes a delta (the live count is 716).
    ⛔ **No contract, budget, acceptance case, required symbol, change row or non-goal moved.**
  - Version 2 is the DOCS repair only (ESTATE-REPAIR + ESTATE-REPAIR-2, 2026-09-20): every
    mutation-coverage absolute became a delta or carries an as-of mark, the predicted red message
    lost its absolute, and §7's row for the lighting walker — a path this packet does not edit —
    was deleted so §7 and the capsule are set-equal. ⛔ **No contract, budget, acceptance case,
    required symbol or non-goal moved**; version 1 is in `pre-estate-repair-2026-09-20/`.
- **Verified base:** `em-t13-b1b-2026-09-22` at `e1750d7454c1dff7e81342efadd05e47719ca6aa`
  ⓘ **THE CHAIR STAMPS THIS ROW AT PROMOTION.** ⭐ The revalidation sentence, with the version-6
  window facts: *"Re-measured at `16f0bc71c` (train EM-T12's flipped tip): `d31af2cee` IS an
  ancestor; the J-T1 window `d31af2cee`→`16f0bc71c` over every change-manifest path and every
  `requiredSymbols` path moves FOUR files — `scripts/mutation-coverage-manifest.json` (+88/−1),
  `src/domain/edit/operations.js` (+405/−0, CREATED by EM-B1a), `tests/domain/editOperations.test.js`
  (+847/−0, CREATED by EM-B1a and extended by EM-B1c1) and
  `tests/lint/chooserTotality.walker.test.js` (+47/−5); both CREATE targets are ABSENT; all three
  required symbols are present verbatim (`grep -c` → 1 each); the preamble's SHA-256 is
  `fdecd426…dacc9e7c`."*
  ⚠ Ten commits moved the branch under this lane (11:01→11:33 EDT), all docs plus two unrelated
  files; `d31af2cee` **IS an ancestor** of `7aa769830` and **every measured path is blob-identical
  across the window** (evidence §0). Held at `d31af2cee` per the chair's ruling (6).
- **Last revalidated:** left for the chair. Pre-proved 2026-09-22 at `16f0bc71c` (version 6);
  version 5's row read *2026-09-19 11:33 EDT, `d31af2cee`*.
- **Depends on:** ⭐⭐ **EVERY DEPENDENCY HAS LANDED AT `16f0bc71c`, MEASURED** —
  `docs/implementation/PACKET_MANIFEST.json` holds **215 packets and ZERO non-terminal ones**, so
  no path in this member's manifest is reserved by anybody: `EM-B1a` (version 11, train EM-T11),
  `EM-A1`, `EM-P4` and **`EM-B1c1` (version 5, train EM-T12)** are all terminal. The two
  `duplicate change path` errors version 5 measured, and RAISED R4's live holder of
  `scripts/mutation-coverage-manifest.json`, are **DISSOLVED**; the train is **EM-T13**. The
  version-5 text stands below as history: **`EM-B1a` version 9 LANDED** (this packet modifies its `operations.js` and
  widens four of its landed arms), **`EM-A1`** (typedefs, LANDED train EM-T9) and **`EM-P4`
  LANDED** (EM-B1a version 9's `operations.js` reads `NPC_STATUS_VALUES` from
  `src/domain/entities/npcs.js`, so this member's composed catalogue cannot load without it).
  ⭐⭐ **THE TRAIN, MEASURED: EM-T13 AT THE EARLIEST.** EM-B1a v9 rides EM-T11; **EM-B1c1 v2
  rides EM-T12** and holds the SAME two paths (`src/domain/edit/operations.js`,
  `tests/domain/editOperations.test.js`), so the two cannot ride one train — proved on a
  copy-only estate with EM-B1a terminal: each placed ALONE validates with ZERO errors, and
  placed TOGETHER the validator prints *"duplicate change path across packets"* on BOTH paths.
  EM-B1c1 goes first: it is on the first door's road. Legacy note: **`EM-B1a`** — this packet CREATES its own leaf for the seven off-stage rows and
  spreads them into B1a's `OP_TYPES` (one import, one spread); it uses B1a's `makeOp` /
  `validateOp` and both closed vocabularies unchanged. **B1a lands first.** Also `EM-A1` (typedefs).
  ⓘ **PLACEMENT IS JUST-IN-TIME (the chair's ruling 3):** `scripts/mutation-coverage-manifest.json`
  is held by `EM-A1` at DRAFT, and DRAFT reserves as READY does, so **this packet is not placed in
  `PACKET_MANIFEST.json` until that path is free** — it waits in the chair kit. Nothing in the
  contract changes; only when the row enters the manifest does.
- **Collision group:** ⭐⭐ **EMPTY AT `16f0bc71c`, MEASURED — RAISED R4 IS CLOSED.** The two shared
  files `src/domain/edit/operations.js` (which this packet touches by **five effective lines**) and
  `tests/domain/editOperations.test.js` are held by **EM-B1a** and **EM-B1c1**, both LANDED, and
  `scripts/mutation-coverage-manifest.json` is held by no non-terminal packet: the registered
  estate carries **215 packets and ZERO non-terminal ones**. `validate:packets` therefore reports
  no `duplicate change path` for this member (version 5 measured two, against non-terminal
  siblings that have since landed), and TOOL-27 row-keys the register in any case
  (`rowKey: invariants['tests/lint/opGuardCoverage.walker.test.js']`, disjoint from every landed
  row). ⛔ This member's arms still re-assert BOTH siblings' totality, so a row dropped by either
  reds here.
- **Commit authority:** edits only; the chair commits.
- **Baseline posture:** measured. Executed at this base: a `rationale` row is
  `{ kind: 'rationale', rationale: '<the executed account>' }`; `tests/lint` is the first of the
  ENFORCER DIRS. ⚠ **AS OF `d31af2cee`, AND AS HISTORY ONLY** —
  `scripts/mutation-coverage-manifest.json` then held **704** `invariants` rows of kinds
  `rationale | mutation | uncovered`, ~472 kB, and the lighting census tuple was
  `2645 / 383 / 2262 / 25009 / 6670`. ⛔ **Both figures move with every landing and NEITHER is this
  packet's own** (measured at `bdbf7c89c`: `invariants` **719**, `tests/lint/` rows **179**,
  `uncoveredBaseline` **186** — RE-MEASURED at `583f8f644`; census `2665 · 359 · 2306 · 25550 · 6816`). This member states
  DELTAS; the chair stamps the live absolutes at promotion and at the terminal refreeze. No test
  was run.
- **Preamble:** docs/implementation/preambles/EM-PREAMBLE.md (SHA-256: TO BE STAMPED BY THE CHAIR;
  ⭐ **MEASURED at the read tip `16f0bc71c` with `shasum -a 256`:
  `fdecd426828665cad2dfaf872871f29f46fa4816e1bd4c43c20ccd84dacc9e7c`** — the FIFTH amendment,
  the tree's live text. Version 5 quoted `c9f33c8d…`, which the fifth amendment superseded.)
  ⭐⭐ **THIS PACKET IS WRITTEN TO THE SIXTH AMENDMENT** (the chair's selection of 2026-09-22,
  judgment 144), which lands as the FIRST commit of train EM-T13's branch before this member is
  placed; §7's instrument-list rows are its §P2.19 to §P2.21 obligations, priced here, and §11
  carries its new STOP. **The chair RE-STAMPS this row to the amended file's hash at placement.**
- **Interim compile rules:** ⭐ **SUPERSEDED AT VERSION 6 BY THE SIXTH AMENDMENT** (the chair's selection of 2026-09-22, judgment 144), whose §P2.19–§P2.21 instrument list this packet's §7 prices and whose new §P8 STOP §11 carries; the amendment lands as the FIRST commit of train EM-T13's branch. ⛔ The hash quoted below is version 5's and is STALE: the tree's live preamble at `16f0bc71c` measures `fdecd426828665cad2dfaf872871f29f46fa4816e1bd4c43c20ccd84dacc9e7c`. The version-5 row, as history: `COMPILE-RULES.interim.md` (the chair, 2026-09-20) — the fifth amendment's rules (1–17 as of 2026-09-21), obeyed before they land. Where this packet and `EM-PREAMBLE.md` (SHA-256 `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`, measured with `shasum -a 256` at the read tip `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d` on 2026-09-20 — the FOURTH amendment, still the tree's live text) disagree, the interim rules govern and this row is the record of it.
  ⓘ Per the chair's ruling (6) this lane does not stamp it.

---

## 1. Reconciled authority

1. **THE CHAIR'S RULINGS, 2026-09-19** (ODQ §934.36 addendum, §934.46): **(1)** EM-B1 **SPLITS** —
   this packet is **B1b**: *the seven OFF-STAGE ops under §13 + the coverage walker + its
   mutation-coverage row*, depending on B1a. **(6)** the base stays `d31af2cee`. **(8)** the
   census counts test files.
2. **ODQ §934.43 / design §13 — THE PHANTOM CONSEQUENCE RULE**, the owner's: *a phantom
   counterparty can absorb an act but never return one.* Carried verbatim into §6: (a) exactly
   seven types are `stage: 'off-stage'`; (b) each carries `consequence: 'by-target-reality'` and
   the resolver `consequenceFor(target) → 'home-procedures+record' | 'world'` is **EM-F1's**, so
   this packet declares the POLICY AS DATA and nothing more; (c) for a phantom target the only
   effects are the home's own existing procedures plus the chronicle line — **no war state,
   treaty, route, faction-power or legitimacy shift**; for a real save the decree is handed to the
   campaign's inter-settlement machinery; (d) **an off-stage op's relations must not assume
   phantom-side state exists** — `make-peace` requires a prior `declare-war` **ENTRY IN THE
   REGISTRY**, never a war state in the world; (e) **no acceptance case beyond eight** — the
   policy folds into the declaration and validation cases.
   ⚠ The muster / casualty / upkeep verified-fact rows are **NOT owed by this packet** (EM-F1,
   wave 5); they appear only as the forbidden alternative home — *no second force-return
   mechanism* (§5).
3. **design §12.8 GOVERNS the coverage instrument** — coverage is **executable rule FUNCTIONS per
   op type, never a string**, and *"the op-coverage walker refuses a declared rule that cannot
   fire."* **ARCH §5**: coverage is declared in `OP_TYPES[type].guards` and **"stated when empty."**
4. **THE PROMISE** — an applied decree reopens read-only; promotion never rewrites a record-only
   past (§13's closing clause, EM-F2's).
5. **`EM-PREAMBLE.md`** §P2 (the mutation-coverage obligation, §P2.2), §P5 (HZ-PHANTOM), §P6 (the
   EM amendment: a `tests/lint/` acceptance file DOES owe its manifest row), §P8 — cited by hash.
6. **ARCH §8 instrument 3** (`opGuardCoverage.walker`), **§9** (the amended op list).
7. Live code at `d31af2cee`.

**Resolved contradictions:**

- "per-type guard coverage as FUNCTIONS" (charter) **vs** nothing to point at in wave 1
  (`guardRules.js` is EM-C3's) → reconciled by **ARCH §5's own clause, "stated when empty"**:
  every type declares `guards: []` plus a non-empty `guardsStated` reason at this wave, and EM-C3
  populates. §12.8's *"refuses a declared rule that cannot fire"* bites only on a **declared** rule,
  of which there are zero here, so the instrument is honest rather than vacuous. No contradiction
  survives, and this is stated rather than assumed because a walker over an empty declaration set
  is exactly the kind of instrument that can be mistaken for a passing guard.

---

## 2. Outcome

**Observable result:** the op catalogue gains the seven off-stage acts, each carrying the owner's
consequence policy as declared data and prerequisites that name registry entries rather than
world state; and `tests/lint/opGuardCoverage.walker.test.js` makes every op type's guard coverage
VISIBLE, so unfinished coverage can never be silent.

**Definition of done:** ⭐ **STATED AS A DERIVATION, NEVER AS AN ABSOLUTE** — `OP_TYPES` holds
EM-B1a's home roster PLUS EM-B1c1's `set-npc-name` PLUS these seven, and nothing else; at this
member's landing that is **TWENTY-TWO, measured at `16f0bc71c`**, and the arms read the rosters
rather than the numeral. The seven carry `stage: 'off-stage'` and `consequence:
'by-target-reality'`, with the two partitions asserted set-equal; no off-stage relation names a
world predicate; the walker proves every type declares coverage; the manifest row is appended
surgically; the leaf lands DARK.

In scope: (1) the seven off-stage rows with the §13 policy; (2) no integration — headless; (3) the
prevention guard, `opGuardCoverage.walker.test.js`, with its mutation-coverage row.

Explicit non-goals: ⭐ **THE REAL-SAVE PATH OF THE THREE "MAKE THEM" DIRECTIVES — a pin on the
counterparty's own fork — which is EM-E4's** (ODQ §934.47 add. 10 confirms a packet may ship an op
whose real-save path belongs to a later packet). The **phantom** path is reachable here: a
declared, record-only offer that satisfies the home's predicate at the tick (§13). **No fork pin is
implemented in this packet.** Also: the guard RULES (EM-C3) and the folding engine (EM-C2); **`consequenceFor` and
every phantom mechanic** (EM-F1); the muster / casualty / upkeep path a returning force uses
(EM-F1) — this packet writes none and names none as a write target; the PHANTOM/REAL badge (a
surface, EM-D3); the registry (EM-C1), the layer (EM-B2), the persisted keys (EM-B3), the tick
(EM-E1); any golden, tuning or migration.

---

## 3. Hard scope budget

| Limit | Packet budget | Standard |
|---|---:|---:|
| Behavior families | `1` | 1 |
| New persisted record families / writers / flags / surfaces | `0` | ≤1 each |
| Direct production consumers | `0` — lands DARK | ≤2 |
| New logic-bearing production leaves | **`1`** (`src/domain/edit/operationsOffStage.js`) | ≤2 |
| Existing logic-bearing production files modified | **`1`** (`src/domain/edit/operations.js` — **one import + FOUR spread lines, +5 eff**) | ≤3 |
| Additional registration-only files | **`1`** (`scripts/mutation-coverage-manifest.json`) | ≤3 |
| Handwritten files total | `5` (+1 deferred census row) | ≤12 |
| New/changed effective production lines | **≈106 (101 in the leaf + 5 in `operations.js`), MEASURED** | ≤400 |
| Effective lines per new leaf | **101 of 250** (`operationsOffStage.js`), RE-MEASURED at `16f0bc71c` by ESLint's own `max-lines` with `skipBlankLines` + `skipComments` — unmoved from version 5 | ≤250 |
| **`operations.js` after B1a + B1c1 + B1b** | ⭐ **233 → 238 of 250, MEASURED AT `16f0bc71c`** with ESLint's own `Linter` under `max-lines` (`skipBlankLines` + `skipComments`): the LANDED file measures **233** (EM-B1a's 231 plus EM-B1c1's +2, exactly the version-5 projection) and this member's splice is **+5**. ⚠ The 250 cap is a FAMILY budget: with EM-B1c's remaining rows (+5 — also four runs) the projection is **243 of 250**, seven lines of margin | ≤250 |
| Delta in a shared/hot file | `0` — `operations.js` is a SIBLING's file, not a hot-list file | ≤15 |
| Acceptance cases | `8` | ≤8 |

Overrides approved before dispatch: `NONE`. **HOT FILES: none named** — `operations.js` is new in
this train, carries no `scripts/.size-baseline.json` entry and no ceiling of its own.

⭐⭐ **THE FILE CAP IS RESOLVED — OPTION O1, AS THE CHAIR SHAPED IT (ruling 2).** This packet
**CREATES its own leaf** `src/domain/edit/operationsOffStage.js` carrying the seven off-stage
rows (**101 effective of 250, MEASURED**) and **MODIFIES `src/domain/edit/operations.js` by
exactly FIVE effective lines** — ONE import line and FOUR spread lines into the catalogue:

⛔⛔ **THE COMPOSITION IS A FOUR-POSITION SPLICE, AND THAT IS MEASURED, NOT CHOSEN** (judgment 92;
EM-B1c1 version 2's ratified form, generalised). Sorted with the tree's own `compareCodepoint`
(`src/domain/deterministicSort.js`) the seven off-stage keys form **FOUR discontiguous runs** inside
EM-B1a's fourteen. ⛔ `HOME_OP_TYPES` **is minted by no packet and occurs nowhere in the tree**
(`git grep -c -F 'HOME_OP_TYPES' -- src tests scripts docs` → no hit): EM-B1a authors its fourteen
rows INLINE in the frozen `OP_TYPES` literal, so the splice edits that literal in place.

```js
import { OFF_1, OFF_2, OFF_3, OFF_4 } from './operationsOffStage.js';   // +1
// ...inside EM-B1a's existing frozen OP_TYPES literal, four inserted lines:
  ...OFF_1,   // after `add-npc`,         before `found-phantom`     — close-trade, declare-war
  ...OFF_2,   // after `found-phantom`,   before `promote-phantom`   — make-peace, open-trade
  ...OFF_3,   // after `rebalance-power`, before `remove-faction`    — recall-force
  ...OFF_4,   // after `remove-npc`,      before `set-field`         — resolve-outcome, send-force
```

⛔⛔ **THE IMPORT IS ONE LINE, AND THAT IS A CONTRACT TERM, NOT A STYLE NOTE (version 6, MEASURED
at `16f0bc71c`).** A FOUR-LINE import — the shape long descriptive handles force — measures
`operations.js` at **241**, which is **+8** and therefore a §11 STOP. The one-line form with the
four short handles measures **238**, which is **+5**. There is **no `max-len` rule in
`eslint.config.js`** (measured; the landed `operations.js` already carries a 351-character line),
so one line is lawful. ⛔ The four exported handles are spelled **`OFF_1`, `OFF_2`, `OFF_3`,
`OFF_4`** for exactly this reason and may not be lengthened.

⭐⭐ **THE FOUR ANCHORS ARE UNCHANGED BY EM-B1c1'S LANDING** (measured at `16f0bc71c` with the
tree's own `compareCodepoint` over the real composed map). EM-B1c1 landed **`set-npc-name`**, which
sorts AFTER `set-field` — index **18** of the composed twenty-two — so it anchors no run.
⛔ The landed `operations.js` ALREADY carries one such spread, `...NPC_RENAME_OP_TYPES,` between
`set-institution-state` and `set-npc-status`: the ratified precedent for this member's four.

⛔ **ONE SPREAD CANNOT DO THIS AND NEITHER CAN FOUR SPREADS OF ONE MAP** — executed in plain node:
object spread fixes a key's position at its FIRST insertion, so spreading the same map at four
points still lands all seven keys at the first one. A trailing spread puts all seven LAST and reds
EM-B1a's A1 order arm (*"a new row cannot be appended wherever"*). ⛔ **VERSION 5'S CLAUSE HERE IS
REFUTED AT `16f0bc71c`: EM-B1c1 landed `set-npc-name`, not `rename-npc`, so run 4's anchor stays
`remove-npc`.** The run count, the four anchors and therefore the +5 are unchanged.
⭐ **RE-EXECUTED OVER THE REAL LANDED CODE**: the composed result is **22 keys**;
`Object.keys(OP_TYPES)` equals its own `compareCodepoint` sort AND equals the landed fifteen plus
these seven re-sorted; the `stage:'off-stage'` set and the `consequence:'by-target-reality'` set are
IDENTICAL and are exactly the sorted seven; the `home` set is exactly the landed fifteen; every one
of the twenty-two rows carries the ELEVEN fields with a non-empty `guardsStated`; relational
integrity over the twenty-two reports ZERO unknown strings and ZERO asymmetries; `requires.world` is
`[]` on all seven and every `target` is `phantom`; `OP_TYPES`, every composed row and all five of
the leaf's exported handles are FROZEN; `makeOp('make-peace', …).requires` FLATTENS to
`['declare-war']` with `['off-stage','by-target-reality']` copied; and `validateOp` of a well-formed
off-stage op against a phantom target returns `{ok: true, errors: []}`.

⛔ **EM-B1a's manifest is UNCHANGED**, exactly as the chair directed: B1a authors
`HOME_OP_TYPES` and the two closed vocabularies, and the spread above is this packet's single
edit. Both files land inside the cap — `operations.js` **238 of 250** and this leaf **101 of 250**,
both MEASURED at `16f0bc71c` — and the earlier ≈314–332 overage is retired. The lane's O2 and O3 are withdrawn with it; **O3 remains recorded as
refused**, since it was the squeeze the chair forbade.

**Three honest options, costed; the chair rules (R1):**

| # | option | effect on `operations.js` | cost |
|---|---|---|---|
| **O1 — two leaves** | B1b writes `src/domain/edit/operationsOffStage.js` (≈83) and B1a's file re-exports its rows into `OP_TYPES` from that module | B1a ≈231–249, B1b ≈83, **both inside 250** | one extra production leaf — B1a would then hold 1 of ≤2 new leaves and B1b 1 of ≤2, each inside its own budget. ⚠ It makes `OP_TYPES` a composition of two frozen maps, which A1's totality arm must then assert over the composition |
| **O2 — take EM-B1c too** | the three `rename-*` ops + `set-world-fact` (≈36) leave B1a as B1a pre-declared, and the off-stage seven still land in the same file | ≈278–296 — **still over** | insufficient on its own; O2 only helps combined with O1 |
| **O3 — a data-only row form** | every row's ten fields expressed as one compact frozen literal per type (a declared shape rather than ten authored lines) | unmeasurable at compile without writing it | ⛔ **this is the squeeze the chair forbade**; recorded to be refused, not chosen |

⭐ **The lane's reading is O1**, because it is the only one that puts both halves inside the cap
without compressing a row, and because it follows the same boundary the chair already drew — home
acts in one module, off-stage acts in another. **But it changes B1a's manifest**, so it is the
chair's call, not this lane's. Until it is ruled, this packet's manifest names B1a's file as its
MODIFY target (the shape the chair ordered), and §11 makes crossing 250 a STOP.

Decomposition of the MEASURED 101: seven rows × ~11 effective ≈ 77; the two literal vocabulary
members, the shared `guardsStated`, the outcome vocabulary and the five payload helpers ≈ 12; the
leaf's own row freezer ≈ 12. ⛔ **The leaf still IMPORTS NOTHING at run time**, so the five payload
helpers and the row freezer are authored here rather than taken from `operations.js` — that is the
cost of the cycle rule, and it is inside the cap.

---

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- EM-B1b
```

Expected: capsule emitted; ancestry and substrate proven; **`CREATE` target ABSENT**
(`tests/lint/opGuardCoverage.walker.test.js` — measured absent); **non-CREATE targets
`src/domain/edit/operations.js` and `tests/domain/editOperations.test.js` PRESENT AND CLEAN** —
which is only true once **EM-B1a has landed**, so ancestry is also the dependency check; and
`scripts/mutation-coverage-manifest.json` present and **clean**.

⚠ **B1a and B1b share two files and must serialize.** Dispatching this one while B1a is
non-terminal reds `validate:packets` on the duplicate path.

⚠ **AS OF `d31af2cee`, AND AS HISTORY ONLY:** `scripts/mutation-coverage-manifest.json` was 704 rows
and ~472 kB. ⛔ **READ THE LIVE COUNT FROM THE FILE at dispatch** — it measured **716** at
`bdbf7c89c` and moves with every sibling landing. Preflight must confirm no sibling lane holds it
dirty: a concurrent whole-file re-serialisation would bury this packet's single row, which is
exactly what `PACKET_STANDARD.md` forbids.

---

## 5. Verified tree contract

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| **B1a's machinery** | `src/domain/edit/operations.js` | `OP_TYPES`, `OP_STAGES`, `OP_CONSEQUENCE_POLICIES`, `makeOp`, `validateOp` | ⚠ **Does not exist at this base** — EM-B1a creates it. Both closed vocabularies already carry BOTH members, so this packet appends rows without editing a frozen constant | Extend; never fork a second `OP_TYPES`, a second validator, or a second stage vocabulary. ⛔ **But do NOT import them into the leaf** — `operations.js` imports the leaf, so the back-edge is a module cycle (EM-B1c1's ratified shape; §7's `CREATE` row carries the rule). **PROVED, not reasoned** (ESTATE-REPAIR-4, plain `node` on scratch replicas of the two shapes at the read tip): the declared shape throws `ReferenceError: Cannot access 'OP_STAGES' before initialization` at exit **1**, and the import-nothing shape loads at exit **0** with the composed map intact — with the cured replica's import EDGE asserted still present, so the green is not the absence of a graph |
| **Register row shape** | `scripts/mutation-coverage-manifest.json` | `invariants` | Kinds `rationale \| mutation \| uncovered`; a `rationale` row is `{ kind: 'rationale', rationale: '<the executed account>' }`. ⚠ AS OF `d31af2cee`, AND AS HISTORY ONLY: **704** rows, ~472 kB. The population moves with every landing (measured **716** at `bdbf7c89c`); read the live count from the file and state this member's move as the DELTA in §7. | The exact shape of the ONE row this packet appends, surgically |
| **The obligation's source** | `tests/lint/mutationCoverage.shared.mjs` | `ENFORCER_DIRS` | `tests/lint` is the FIRST entry (`:36-37`); `tests/lint/mutationCoverageManifest.test.js` asserts every enumerated file owns an `invariants` entry | Why the row is owed AT COMPILE by this packet and not by B1a |
| **Walker precedent** | `tests/lint/chooserTotality.walker.test.js` | `const SCAN_ROOTS`; HB-1's A7/A8 ("classified equals discovered both directions", "symbolsWithTwoDispositions empty") | A register walker asserts its table SET-EQUAL to the live scan in BOTH directions and reports a full offender list | **Copy this shape** for the coverage walker |
| **Test precedent — domain** | `tests/domain/institutionFounding.test.js` | `describe('MF-T2Q …')` + seven straight-line `it` | One literal `describe`, no `.each`/`runIf`/nesting, positive control first | Copy for the appended arms (§P3.4) |
| **Stable order** | `src/domain/deterministicSort.js` | `compareCodepoint` | The estate's one sanctioned, locale-free string order | The appended rows' key order |

**Forbidden alternatives:**

- ⛔ **no second force-return mechanism** — a returning force resolves through the home's EXISTING
  muster / casualty / upkeep mechanics (EM-F1, wave 5). This packet writes none, names none as a
  write target, and must not grow one (design §13);
- ⛔ **no phantom-side state, ever** — no war state, treaty, trade route, envoy state,
  faction-power or legitimacy shift derived from a phantom (§P8);
- ⛔ **`consequenceFor` is authored, imported and referenced NOWHERE here** — it is EM-F1's;
- ⛔ **no whole-file re-serialisation** of `scripts/mutation-coverage-manifest.json`;
- no second `OP_TYPES`, validator, constructor or stage vocabulary; no adapter per op type
  (§12.13); no import of `src/kernel/prng.js`, `src/kernel/rngContext.js`, `src/store/**` or
  `src/components/**`;
- no edit to any existing file **other than B1a's two and the manifest**;
- no files outside the manifest.

---

## 6. Exact contracts

### The seven OFF-STAGE op types — exactly the ARCH §9 OFF-STAGE list

`declare-war` · `make-peace` · `open-trade` · `close-trade` · `send-force` · `recall-force` ·
`resolve-outcome`

Every one carries `stage: 'off-stage'` and `consequence: 'by-target-reality'`, and every row
carries all **ELEVEN** fields of B1a's `OpTypeDeclaration` — none optional, none omitted.
⭐ **ELEVEN, not ten** (EM-B1a version 9 §6): `target · payload · stage · consequence · requires ·
enables · relatedTo · conflictsWith · duration · guards · guardsStated`. Ten is EM-A1's `Op`.

⛔⛔ **EVERY ROW DECLARES `target: 'phantom'` — RAISED R7.** `target` is one of the eleven and
version 4 never spelled it. `'phantom'` is the reading this wave supports: `consequence:
'by-target-reality'` is resolved from the TARGET, §2's non-goals put the real-save half in
EM-E4, and `validateOp` deliberately does NOT enforce a target-kind match (EM-B1a §8 step 3 —
the match is closed at EM-C4a's door). The counterparty also rides the payload as
`counterparty: { kind: 'ref', required: true }`, the shape `set-relationship` already uses.

⛔ **EVERY RELATION ON THE SEVEN IS CLOSED OVER THE SEVEN.** `conflictsWith` and `relatedTo` are
SYMMETRIC and `requires`/`enables` are EXACT INVERSES (EM-B1a A4), so an off-stage row naming a
HOME type would force the inverse onto a B1a row — which §11 makes a STOP. The relations are
therefore: `declare-war` enables `make-peace` and `send-force`; `open-trade` enables
`close-trade`; `send-force` enables `recall-force` and `resolve-outcome`; `declare-war` conflicts
with `make-peace` and `open-trade`; `open-trade` with `close-trade`; `send-force` with
`recall-force`; every `relatedTo` is `[]`. ⭐ **Executed over the composed 21: zero offenders.**

### THE PHANTOM CONSEQUENCE POLICY — design §13, spelled with no discretion

| clause | contract |
|---|---|
| **`stage`, exactly** | `off-stage` for **exactly these seven**; every other type is `home` (B1a's). The partition is TOTAL over all twenty-five and asserted both directions (A3). |
| **`consequence`, exactly** | `'by-target-reality'` on exactly these seven. The two sets are the same partition, asserted **SET-EQUAL** to the `stage` partition so they can never drift apart. |
| **The resolver is NOT here** | Consequence is decided **at apply time** by `consequenceFor(target) → 'home-procedures+record' \| 'world'`, which **EM-F1 owns**. This packet declares the POLICY as data and nothing more. ⛔ A `consequenceFor` implementation appearing here is a STOP. |
| **What a phantom act may produce** | Only (a) the home's own EXISTING procedures on the home's own state — a force resolves won or lost and returns through the existing muster/casualty/upkeep mechanics; an envoy or caravan returns unchanged — and (b) the chronicle line. **No war state, treaty, trade route, envoy state, faction-power or legitimacy shift.** This packet encodes that by DECLARING it and by writing no such effect; EM-E1 applies it. |
| **A real save** | The decree is handed to the campaign's inter-settlement machinery. That path is the simulator's; the editor only hands it over. |
| ⛔ **Relations must not assume phantom-side state** | ⭐⭐ **THE `requires` SPLIT IS WHERE THIS CLAUSE NOW LIVES EXACTLY** (EM-B1a version 9 §6/§16.3): `requires` is the object `{ world: readonly string[], registry: readonly string[] }`, so `make-peace.requires` is **`{ world: [], registry: ['declare-war'] }`** — the prior ENTRY IN THE REGISTRY, and `world` EMPTY. The same binds `recall-force` (`{ world: [], registry: ['send-force'] }`), `close-trade` (`open-trade`) and `resolve-outcome` (`send-force`). ⛔ **`requires.world` is EMPTY on all seven and C3 asserts it by name.** `makeOp` FLATTENS the pair onto EM-A1's `Op.requires: readonly string[]` — executed: `makeOp('make-peace', …).requires` is `['declare-war']`. |
| **Consequence the DM wants is a separate decree** | A defeat that should shake the town is a typed op **on the town itself**, staged and guarded like any other; the registry may OFFER it as a follows-from suggestion (a connection guard with `fulfil`) and **never applies it on its own** (§13). Encoded here as `relatedTo`, never as `enables` of an automatic effect. |
| **Promotion** | Out of scope; §13's closing clause (record-only outcomes stay record-only) is EM-F2's. |

### Guard coverage at this wave — declared, empty, and honest

Every one of the seven declares `guards: []` **and** a non-empty `guardsStated` naming EM-C3 as
the owner of its rules. ⛔⛔ **AND THAT SENTENCE MAY NOT SPELL `consequenceFor` (version 6,
MEASURED).** §5 forbids the name here and C2's counterforce is A3's own matcher,
`/\bconsequenceFor\b/` over the comment-stripped source, which sees a STRING LITERAL: a
`guardsStated` that names the resolver convicts a sentence that writes nothing. The pre-proof
replica's prose did exactly that and was measured red. The sentence names **EM-C3** and **EM-F1**
by packet id only. ARCH §5's *"stated when empty"* is the authority; §12.8's *"refuses a
declared rule that cannot fire"* bites only on a declared rule, of which there are none.
⛔ **A rule FUNCTION authored in this packet is a STOP** — the rules are EM-C3's, and a function
here would be a guard rule living in the op catalogue.

### Inputs, outputs, state schema

Unchanged from B1a — this packet adds no export and changes no signature. `OP_TYPES` gains seven
rows; `makeOp` and `validateOp` are untouched. **No state is persisted, written or minted.**

Absence rules — **identical to B1a and deliberately not re-specified in a second place**.

### Transition table

Unchanged from B1a. ⚠ One clause is worth stating because it is the product law: a **well-formed**
off-stage op against a phantom target validates **`ok: true`**. `validateOp` reports malformation
only and **never refuses an act**, because at this table a god sits and magic is real (design
§2.7; §P8). A8 asserts it.

### Ordering, determinism, flag, lifecycle

Pipeline position **NONE**; stable enumeration `compareCodepoint` over the combined twenty-five;
**hash/fork key `NONE`** — this packet draws no random number and imports no PRNG; flag `NONE`;
**golden posture `UNCHANGED`** (structurally — nothing imports the leaf and it takes no draw;
motion is a STOP); nothing persisted, so no migration and no veil is owed here.

### Receipts and privacy

`NONE` — no receipt, no figure, no DM-only field, no projection.

### Alignment and edit story

- **Alignment:** `DECLARED EMPTY: a vocabulary of acts; it reads no alignment, law or temper axis.`
- **Edit story:** `ENGINE-ONLY: the DM's verbs arrive from EM-D2's dialog through EM-C4's single
  adapter.`

---

## 7. Exact change manifest

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `src/domain/edit/operationsOffStage.js` | `OFF_1`, `OFF_2`, `OFF_3`, `OFF_4` (the four splice groups) and `OFF_STAGE_OP_TYPES` (the seven as one handle, for the batteries) | **250 eff (cap); 101 MEASURED** | The seven rows of §6 as one frozen map, each with `stage:'off-stage'`, `consequence:'by-target-reality'`, `guards: []` + a non-empty `guardsStated`, and prerequisites naming REGISTRY ENTRIES only. ⛔⛔ **THIS MODULE IMPORTS NOTHING** (EM-B1c1's shape, ratified by the chair, judgment 41 q4). `operations.js` imports THIS module's rows (the `MODIFY` row below), so an import back for `OP_STAGES` / `OP_CONSEQUENCE_POLICIES` would be a **module CYCLE** whose `const` bindings sit in the temporal dead zone at module evaluation. Each row therefore spells `stage: 'off-stage'` and `consequence: 'by-target-reality'` as LITERALS, and **membership of the two closed vocabularies is asserted in the TEST**, which imports both freely (B1 below). ⭐⭐ **THE TYPING CONTRACT, PROVED (EM-B1a version 9 §6.0, which allows the leaf ZERO errors and ZERO `any`):** the two literals are written `/** @type {const} */ ('off-stage')` and `/** @type {const} */ ('by-target-reality')`, the five payload helpers are authored here with JSDoc `@returns {PayloadFieldSpec}` so each `kind` is contextually typed and never widens to `string`, and the row shape is reached by a **TYPE-ONLY** `/** @typedef {import('./operations.js').OpTypeDeclaration} OpTypeDeclaration */` — which erases at emit, creates **no runtime edge and no cycle**, and is invisible to EM-B1a's A6 importer matcher (`/from\s+'([^']+)'/`, which a JSDoc `import(...)` inside a comment cannot match). The leaf also carries its OWN row freezer, because `operations.js`'s `row()` is module-private and unreachable across the cycle rule; it freezes to the identical depth and mints no second validator, constructor or vocabulary. ⭐ **MEASURED with the TypeScript compiler API in plain node over a copy-only scratch estate, both configs transcribed from `tsconfig.full.json` and `tsconfig.domain-strict.json`: `operations.js` 0/0 · `worldConditions.js` 0/0 · `operationsOffStage.js` 0/0, with a planted error still seen in each of the three (liveness 1 · 1 · 1).** ⛔ Add no second validator, constructor or stage vocabulary. ⛔ Author `consequenceFor` nowhere; author no rule function; write no force-return path. |
| `MODIFY` | `src/domain/edit/operations.js` | the `OP_TYPES` literal — a FOUR-POSITION SPLICE | **+5 eff** | EXACTLY one import naming the leaf's FOUR groups and FOUR spread lines inserted INSIDE EM-B1a's frozen `OP_TYPES` literal at the four anchors of §3 (measured with the tree's own `compareCodepoint`). ⛔ Change no B1a row, no signature, no vocabulary, nothing else in the file. ⛔ A trailing spread, or one map spread four times, reds EM-B1a's A1 order arm — proved in plain node. |
| `TEST` | `tests/domain/editOperations.test.js` | arms C1–C4 appended **AND SIX LANDED ARMS WIDENED IN PLACE ACROSS TEN ASSERTION SITES** | `n/a; adds FOUR `it`s and ZERO `describe`` | ⭐ **`TEST`, not `MODIFY`** — interim rule 4: the file exists only because NAMED SIBLINGS create it, and they are **EM-B1a** (the file) and **EM-B1c1** (arms B1–B6). Append C1–C4 to the single literal `describe`; straight-line `it`, no `.each`/nesting (§P3.4); every negative carries `// anchored:` on the line IMMEDIATELY above the `.not.` itself. ⛔ **THE CASE IDS ARE C, NOT B (version 6): EM-B1c1 landed `B1`–`B6` in this very `describe`, so a second `B1` would be two different claims under one title (RAISED R10).** ⛔⛔ **AND WIDEN, BY ADDITION, THE SIX LANDED ARMS THESE ROWS PUT RED — MEASURED BY EXECUTION AT `16f0bc71c` OVER THE REAL COMPOSED CATALOGUE, not read off a sibling's packet** (judgment 134). Each widening lives INSIDE the arm it strengthens, so **no `it` and no `describe` is added**: **(a) A1, three sites** — `expect(Object.keys(OP_TYPES).length).toBe(15)` measures **22**; the set-equality against `THE_FIFTEEN` reports `undeclared` = this member's exact sorted seven; and `expect(live).toEqual(THE_FIFTEEN)` reds. A SECOND roster constant carries the composed catalogue (`THE_FIFTEEN` itself is NOT edited — A3 and B1 read it), derived as `[...THE_FIFTEEN, ...THE_SEVEN].sort(compareCodepoint)` so no roster is hand-spelled. The own-sort order clause and the row-shape loop stay GREEN, measured. **(b) A3, one site** — `keys.filter(stage === 'off-stage')` asserted `toEqual([])` reds UNCONDITIONALLY and becomes this member's exact sorted seven; A3's `home`/`homeConsequence` clauses stay GREEN against `THE_FIFTEEN`, measured, because all seven are off-stage. **(c) A4, one site** — `foreign`, filtered by `!THE_FOURTEEN.includes(named)` over ALL types, reports the seven's own relation strings; the closure roster becomes the composed catalogue and the ORIGINAL clause is KEPT as a second assertion over the HOME rows alone (measured empty). **(d) A6, one site** — `specifiers` is asserted EXACTLY and gains `'./operationsOffStage.js'` as the **seventh** entry, in source order after `'./operationsNpcRename.js'`; the fence arm stays GREEN (measured: the specifier matches none of `prng|rngContext|/store/|/components/|force|muster|casualty|upkeep`). ⚠ **A6's DORMANCY arm is ALREADY RED at `16f0bc71c` and NOT this member's — RAISED R9.** **(e) EM-B1c1's B1, three sites** — `live.length` `.toBe(15)`; the set-equality against `THE_FIFTEEN`; and ⛔ the hard literal `expect([at, live[at-1], live[at+1]]).toEqual([11, 'set-institution-state', 'set-npc-status'])`, where `at` MEASURES **18** once these seven land. The two neighbours are UNCHANGED; only the index moves, to **18**. **(f) EM-B1c1's B6, one site** — `minted = live.filter(t => !THE_FOURTEEN.includes(t))` is asserted `toEqual([B1C1_ROW])` and becomes the EIGHT: this member's sorted seven plus `set-npc-name`. B6's three `collide()` clauses stay GREEN, measured: none of the seven is an `EDIT_KINDS` member (`EDIT_KINDS` reads 19 at the tip), the banked pair is unmoved, and the planted `'rename-npc'` control still returns `['rename-npc']`. ⛔ **Delete no assertion, weaken none, and add no `it`.** |
| `CREATE` | `tests/lint/opGuardCoverage.walker.test.js` | arms C5–C8 | `n/a` | The coverage walker in the `chooserTotality` shape: every row of the COMPOSED catalogue declares `guards` (an array) AND a non-empty `guardsStated`; the declared set SET-EQUALS the live scan both directions with a full offender list; a declared entry that is not a function reds; the walk is asserted non-empty first. |
| `REGISTER` | `scripts/mutation-coverage-manifest.json` | ONE new `invariants` row keyed `tests/lint/opGuardCoverage.walker.test.js` | `+1 row` | ⛔ **SURGICALLY beside its siblings**, never by re-serialising the file. Kind `rationale`, carrying the EXECUTED mutant account of §8 step 6. ⛔ **RELATIVE, NEVER ABSOLUTE: `invariants` +1 row keyed by this packet's OWN test file; `tests/lint/` rows +1 (from whatever its siblings leave the file at); `uncoveredBaseline` UNMOVED.** Three packets cannot all be right about one absolute, and the `704 → 705` this row carried in version 1 was already stale by twelve rows at the integration tip (`invariants` measured **716**, `tests/lint/` rows **177**, `uncoveredBaseline` **186** at `bdbf7c89c`). The row is keyed by this member's own test file and is therefore row-disjoint from EM-A1's, EM-A3's, EM-B1h's and EM-B2a's. ⚠ **These four are WAITING DRAFTS, not registered packets:** at `bdbf7c89c` the registered estate holds zero non-terminal packets, so none of them reserves anything until it is PLACED. (EM-B2b's conditional row on the same path was removed on its own measurement — ESTATE-REPAIR-2 NOTE-1.) |

Generated artifacts: `NONE`.

> ⛔ **THE §7 ROW FOR `tests/lint/sovereigntyLightingContract.walker.test.js` IS DELETED
> (ESTATE-REPAIR-2, 2026-09-20).** This packet does not edit that path — the row said so itself
> (*"DEFERRED TO THE TERMINAL — no edit"*) — and the JSON capsule correctly omits it, so §7 and the
> capsule were not set-equal: the shape the chair's EM-B2a ruling 8 makes the placement script
> refuse, on the estate's most contended path. The deferred-row block below already says everything
> the row said, and it names the file the refreeze actually writes. ⛔ **Edge-shared closure NOT owed** — the only modified production
file is a leaf this train creates, which is in no bundle closure.

### The registration ledger

| # | Obligation | Verdict | Measurement |
|---|---|---|---|
| P2.1 | lighting census | **OWED — `+1 file`, INTERIOR RED** | This packet adds ONE new test file (`tests/lint/opGuardCoverage.walker.test.js`, ONE literal `describe` + FOUR straight-line `it`) and APPENDS four `it`s to the existing `tests/domain/editOperations.test.js`. DELTA, all five figures in the register's own order: **`+1 files / +0 parked / +1 credited / +8 titles / +1 suiteTitles`**. ⭐ The existing file is CREDITED, not parked, proved by the register's own note at `16f0bc71c` (the eighteenth refreeze credits *EM-B1c1 +6 titles* to it). ⛔ The live absolutes are the chair's to stamp at the terminal refreeze and are quoted in no prediction; as history with an as-of mark they read `2683 · 359 · 2324 · 25713 · 6837` at `16f0bc71c`. |
| P2.2 | mutation-coverage row | ⛔ **OWED — and it is THIS packet's, priced here** | `tests/lint` is the first ENFORCER DIR (`mutationCoverage.shared.mjs:36-37`, RE-MEASURED at `16f0bc71c`: the export opens at `:36` and `'tests/lint'` is `:37`, and the list is EIGHT dirs) and `mutationCoverageManifest.test.js` asserts every enumerated file owns an `invariants` entry. ⭐ The split moved the obligation cleanly **with the file that incurs it** — B1a owes nothing. DELTA: `invariants` **+1 row**, `tests/lint/` rows **+1**, `uncoveredBaseline` **UNMOVED**. As history with an as-of mark, at `16f0bc71c` the register held `invariants` **723** (rationale 445 / mutation 92 / uncovered 186), `tests/lint/` rows **182**, `uncoveredBaseline` **186**, and **no row for this walker** (measured). |
| P2.3 | observed-shape exemption | **NOT OWED** | No save-time key read; nothing persisted. |
| P2.4 | writer-reach | **CANNOT MOVE** | `SURFACE_CLOSURE_STOP` includes `'src/store/'` (`writer-reach-scan.mjs:115-117`). |
| P2.5 | decision-fork + mechanism-coverage | **NOT OWED** | No seeded chooser, no pool; this packet draws nothing at all. |
| P2.7 | prose-numerics | **NOT OWED** | No figure rendered. |
| ⭐ **P2.10 / P2.12** | edge-shared bundle closure | **NOT OWED — MEASURED, not assumed** | INPUT MEMBERSHIP tested against all FIVE `supabase/functions/_shared/*.meta.json` `inputs` lists at `16f0bc71c`: `aiCharterBundle` (114 inputs), `aiGroundingBundle` (74), `aiOutputSchemaBundle` (115), `analyticsEventsBundle` (2), `intentAtlasBundle` (2). **Neither `src/domain/edit/operations.js` nor `src/domain/edit/operationsOffStage.js` appears in any of the five.** No generator is in this member's `checks`, so no GENERATED row is owed. |
| ⭐ **P2.11** | byte budgets / first paint | **+0 B — and NOT the train's byte-arm holder** | Measured by IMPORTING `EAGER_FIRST_PAINT_MODULES` from the tree's own `vite.config.js` (never a `dist` read, never a hand list): the set reads **270** at `16f0bc71c` with `src/main.jsx` present (anti-vacuity), and **NO `src/domain/edit/` module is in it** — `operations.js` included. The leaf takes no importer outside `operations.js`, which is itself lazy. The worker closure, the lazy-engine budget and the edge metas are therefore all UNTOUCHED. ⛔ The byte-arm holder of train EM-T13 is named by the chair AT THE PLACEMENT (§P2.11 as the sixth amendment corrects it); this member is compiled as if it is NOT the holder and its predicted delta per closure is **+0 B**. |
| ⭐ **P2.15** | browser suite (`e2e/`) | **NOT OWED — no spec named** | This member touches no `src/components/**` file, no route, no `data-testid` and no accessible name. Re-derived here rather than inherited (judgment 93). |
| ⭐ **P2.17** | tuning inventory | **MEETS IT AT ZERO — MEASURED with the library's OWN counters** | `measureTree()` from `scripts/lib/tuning-inventory.mjs` run over the tip and over a copy-only estate carrying both of this member's files: **every per-file count is IDENTICAL and `FILES WHOSE COUNTS MOVED` is EMPTY**; `operations.js` and `operationsOffStage.js` each read `P2 0 · P3 0` on both sides. Totals unmoved (as history with an as-of mark, `unregisteredNamed 544 / bareDecimals 7010` at `16f0bc71c`). Guard-the-guard: `NAMED_NUMERIC_CONST_RE` fires on a planted `const OFF_STAGE_WEIGHT = 0.5;` and `BARE_DECIMAL_RE` on a planted `0.5`. ⛔ The leaf declares **no number at all**: `duration: null` on every row and no dial. |
| ⭐ **P2.19** | voice walker, Tier 2 | **HELD AT ZERO — MEASURED** | `tests/copy/voiceMechanics.test.js` scans `src/data` + `src/domain` and holds a CREATEd file at zero. Its own counter (espree `stringLiteralContents`, then `/—/g` and `/!/g` per literal) over this member's planted leaf reads **`{em: 0, bang: 0}`**; `operations.js` reads `{em: 0, bang: 0}` at the tip AND spliced, so the DELTA is **+0 / +0** and no per-file baseline rises. ⛔ Every `guardsStated` sentence and every op key is written with `: ` and a full stop, never an em dash or an exclamation point. |
| ⭐ **P2.20** | reviewed-set pin | **NOT OWED** | This member registers **no application command**: it creates no file under `src/application/commands/**`, touches `standardCommandRegistry.js` not at all, and dispatches to no store writer (wave 1 is HEADLESS). `tests/application/commands/commandRegistry.test.js` is named in neither §7 nor `checks`. |
| ⭐ **P2.21a** | entropy-root census, BOTH arms | **NOT OWED — MEASURED on both arms** | The walker's own detectors run over the planted leaf and the spliced `operations.js`: `readSitesIn` (its `READ_RE` filtered by `WORLD_ROOTS`) returns **`[]`** for both, and the `ENTROPY_CONSUMERS` site counts (`createPRNG`, `hash01`, `fnv1a32`) are **`0 · 0 · 0`** for both, identical to the tip. Guard-the-guard: the same detectors return `['worldState']` and `createPRNG 1` on planted lines. ⛔ The leaf mints no stream, so neither the read-site arms nor the SITE COUNT pins move. |
| ⭐ **P2.21b** | ruin-filter roster | **NOT OWED — the leaf is not in the discovery set** | The walker's own `READER_RE = /\.institutions\b/` over the COMMENT-STRIPPED source (`codeOnly`, the walker's own strip) returns **false** for the planted leaf and for `operations.js` at the tip and spliced. The live discovery set reads **102 files under `src/domain`** at `16f0bc71c`, of which the `edit/` members are `pools.js` and `recordRegister.js` — this member adds neither and the delta is **+0**. Guard-the-guard: the same matcher returns true on a planted `s.institutions.length`. |
| ⭐ **P2.21c** | goods roster (ST-2) | **NOT OWED** | `GOODS_IDENTITY_TABLES` is `['src/data/resourceData.js']` at `16f0bc71c`. The leaf **IMPORTS NOTHING AT ALL** (its import list is measured EXACTLY empty after the comment strip), and the spliced `operations.js` imports no identity half-table, so no row is owed in `src/domain/goods.schema.js`'s ST-2 roster and `tests/build/vendorPdfLazy.test.js` does not move. |
| ⭐ **P2.16 / judgment 124** | line-addressed registers, INCLUDING THIS PACKET'S OWN `requiredSymbols` `_note`s | **NONE FOUND — MEASURED** | `git grep -n -F '<path>' -- tests/lint scripts` at `16f0bc71c` for all five manifest paths: `operationsOffStage.js` 0, `editOperations.test.js` 0, `opGuardCoverage.walker.test.js` 0; `operations.js` 5 hits, **all in `tests/lint/editMutationPath.walker.test.js` and none of them a `path:line` address** (they are a WATCHED-symbol roster keyed on `makeOp`/`validateOp`/`OP_TYPES`, which this member does not touch). ⭐ AND THIS PACKET'S OWN ROWS RE-MEASURED: `mutationCoverage.shared.mjs:36-37` still names `ENFORCER_DIRS` and `'tests/lint'`; `chooserTotality.walker.test.js` still declares `const SCAN_ROOTS` at `:64` over `worldPulse/spatial/traditions/region`; `deterministicSort.js:25` still exports `compareCodepoint`. No `_note` address is stale. |
| ⭐ **step 17 (the governing set)** | every module the contract imports | **MEASURED GREEN** | `tests/lint/editMutationPath.walker.test.js` (EM-C4a's landed walker) scans `src/` for STATIC named imports of `makeOp`/`validateOp`/`OP_TYPES` outside its frozen `EDIT_PATH`. This member adds **no export** to `operations.js`, and the new leaf **imports nothing**, so `mutationPathOffenders` is unmoved. ⛔ Its `staticNamedEdges` matcher is `/^import\s*\{…\}\s*from\s*'\.…';$/m` — a ONE-LINE form, which is a second reason §3 pins the import to one line. |
| ⭐ **P2.13** | **wiring census — the PRODUCER COUNT** | ⛔ **OWED — A DEFERRED ROW, delta only (interim rule 1)** | `docs/content/wiring-census.json` — **a named INTERIOR RED, delta only.** `stamp.producerIndexFiles` **+1**: this member's new `.js` leaves under the two counted roots, named one by one — `src/domain/edit/operationsOffStage.js`. **Re-measured from this packet's own CREATE rows at `16f0bc71c`.** ⛔ `producerIndexFiles` **NAMES NOTHING — IT IS A NUMBER** (`1181` at that tip, read from the stamp; `1172` at version 5's base), counting every `.js` under `src/generators/**` plus every `.js` under `src/domain/**`; so the answer is a DELTA, and "the register does not name mine" would be a category error. `stamp.files` (7 entries) and `stamp.candidateLeaves` (6) **UNMOVED** — both key on `src/domain/display/stateProse/` paths this member does not touch. `totals.*` **UNMOVED** — this member produces no pool, variant or relation. ⛔ **Regenerated WHOLE at the train's terminal, BY THE CHAIR** — a member never runs `node scripts/wiring-census.mjs`, because N lanes each regenerating a global COUNT produce N conflicting blobs. ⛔ **The path is in NEITHER §7, NOR the `changeManifest`, NOR `checks`** — naming it reserves the estate's most contended path against every sibling. |

> ⛔ **THE CENSUS ROW IS DEFERRED (§417 shape)** — DRAFT reserves as READY does, so no edit is made
> and `EM-B1b.manifest.json` **omits** the path. ⭐ **THE DEFERRED PATH IS
> `tests/lint/.lighting-census-baseline.json`, NOT THE WALKER** (measured at `bdbf7c89c`: the
> walker's refreeze writes `CENSUS_BASELINE_REL = 'tests/lint/.lighting-census-baseline.json'` and
> the walker file holds no live tuple), so the chair's single terminal insert is
> `{ "action": "TEST", "path": "tests/lint/.lighting-census-baseline.json" }`. Predicted interior
> red, **SHAPE ONLY**:
> `the estate's file count moved — re-measure, do not re-word: expected <N+1> to be <N>`, where
> `<N>` is whatever the live baseline reads when this member commits. ⛔ **The two absolutes this
> line carried in version 1 (`expected 2647 to be 2646`) are withdrawn**: a predicted red message
> that spells an absolute is the same stale-numeral class as an absolute tuple, and both figures
> were already wrong by nineteen files at the integration tip (`files` measured **2665** at
> `bdbf7c89c`). The chair stamps the live baseline at the terminal refreeze.

---

## 8. Ordered coding sequence

0. Dispatch and seal — **only after EM-B1a has landed**; re-read `git rev-parse HEAD` in the same
   command; confirm the mutation-coverage manifest is clean.
1. Capture the baseline BY MEASUREMENT, never by copying a figure from this packet: the live
   census figures; `sha256` of `tests/fixtures/generator-golden-master.json`; the live
   `invariants` row count and `uncoveredBaseline`, read from the file at the dispatch tip.
2. Append C1–C4 to `tests/domain/editOperations.test.js` (and widen the SIX landed arms
   named in §7) and add `tests/lint/opGuardCoverage.walker.test.js` with C5–C8, all **failing**.
3. Implement the seven rows, off-stage policy first, then the relations.
4. Sole writer / lifecycle seam: **NOT APPLICABLE** — record as skipped.
5. Wire consumers: **NOT APPLICABLE** — lands DARK. Record as skipped.
6. Plant the mutants (§P6), convict them, restore digest-exact — **then** write the
   `scripts/mutation-coverage-manifest.json` row, because its `rationale` must carry the EXECUTED
   account and never a predicted one.
7. Run focused verification (§10).
8. Run the wave-end gate per the train's plan; write the completion receipt.

---

## 9. Acceptance matrix

Four arms appended to B1a's `describe`; four in the new walker.

| ID | Case | Required observation | Home |
|---|---|---|---|
| **C1** | **Main + B1a's totality re-asserted, and the ORDER** | ⛔ **STATED AS A DERIVATION, NEVER AS AN ABSOLUTE:** `Object.keys(OP_TYPES)` is set-equal, both directions with a full offender list, to *EM-B1a's home roster **plus** this member's own seven* — so a row dropped by either packet reds here and a sibling landing between the two does not make the arm wrong. (At this member's landing that is **22** — the landed fifteen plus these seven, MEASURED at `16f0bc71c`; the arm reads the rosters, never the numeral.) ⭐ **AND THE ORDER, WHICH IS THE ARM THE SPLICE EXISTS FOR:** `Object.keys(OP_TYPES)` equals its own `compareCodepoint` sort — the assertion a trailing spread fails. Every new row carries all **eleven** fields with `guardsStated` non-empty; `OP_TYPES`, every row, and each of the leaf's four source groups are asserted FROZEN (R2, answered by EM-B1c1's ratified form: the groups this member exports ARE nameable, so the hole is closed from the five declared exports). | `tests/domain/editOperations.test.js` |
| **C2** | **⛔ THE §13 PARTITION, TOTAL AND SET-EQUAL** | ⭐ This is also the arm that replaces EM-B1a A3's `toEqual([])` clause, widened in place by §7's `TEST` row. The `stage:'off-stage'` set is **exactly** `{declare-war, make-peace, open-trade, close-trade, send-force, recall-force, resolve-outcome}` — an exact sorted list, never a length — and every other type is `home`. The `consequence:'by-target-reality'` set **SET-EQUALS** the off-stage set, and `consequence:'home'` equals the home set. Counterforce: a source scan proves `consequenceFor` is defined **nowhere** in the module — the resolver is EM-F1's. | same |
| **C3** | **⛔ NO PHANTOM-SIDE PREREQUISITE (design §13's clause d), IN THE SPLIT ITSELF** | For **every** off-stage type, **`requires.world` is `[]`** — the clause made structural by the row shape rather than by a naming convention — and `requires.registry` holds only `OP_TYPES` keys, with `make-peace` pinned to exactly `{ world: [], registry: ['declare-war'] }` and the same for `recall-force` (`send-force`), `close-trade` (`open-trade`) and `resolve-outcome` (`send-force`). ⭐ **AND THE CLOSURE ARM: no off-stage row names a HOME type in any relation**, because symmetry and the requires/enables inverse would force the counterpart onto a B1a row (§11's STOP). Relational integrity is re-run over the whole composed catalogue: every relation string is an `OP_TYPES` key; `conflictsWith` and `relatedTo` symmetric; `requires.registry`/`enables` exact inverses; the two `requires` kinds kept apart. | same |
| **C4** | **The report-never-refuse law, and no force-return path** | A **well-formed** off-stage op against a phantom target validates **`ok: true`** — `validateOp` reports malformation only and never refuses an act (design §2.7; §P8). ⭐ Plus the forbidden-home arm: a source scan proves the module imports **no force, muster, casualty or upkeep module** and writes no such path, with the matcher proved live on a planted string — the home procedures stay EM-F1's. | same |
| **C5** | **THE COVERAGE WALKER — guard-the-guard, first** | The walk is asserted non-empty before any membership claim; **every type in the composed catalogue** (never a numeral) declares `guards` as an **array** and `guardsStated` as a **non-empty string**. | `tests/lint/opGuardCoverage.walker.test.js` |
| **C6** | **Declared equals discovered, both directions** | The declared coverage set SET-EQUALS the live scan in BOTH directions with a full offender list (the `chooserTotality` shape) — an unregistered type reds, a stale row reds. | same |
| **C7** | **A declared rule must be able to fire (§12.8)** | A declared `guards` entry that is **not a function** reds; a row whose `guards` is non-empty while `guardsStated` claims emptiness reds. ⭐ At this wave every row is legitimately `guards: []`, and **that is asserted as the current state with its reason**, so the instrument is honest rather than vacuous and EM-C3's population will move it visibly. | same |
| **C8** | **The register's own guard** | `tests/lint/opGuardCoverage.walker.test.js` owns an `invariants` entry in `scripts/mutation-coverage-manifest.json` (the obligation this packet prices), asserted from the test side so a dropped row reds here rather than at a terminal. | same |

**8 of ≤8** — and the §13 policy folds into C2/C3/C4 rather than adding a case, exactly as the
chair's amendment clause (5) directs.

---

## 10. Verification commands

```sh
npx eslint src/domain/edit/operations.js tests/domain/editOperations.test.js \
  tests/lint/opGuardCoverage.walker.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/domain/editOperations.test.js

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/opGuardCoverage.walker.test.js tests/lint/mutationCoverageManifest.test.js \
  tests/lint/negativeAssertionAnchor.walker.test.js \
  tests/lint/contractTestAntiVacuity.walker.test.js tests/lint/seedLoopTotality.walker.test.js \
  tests/lint/editMutationPath.walker.test.js tests/lint/ruinFilterRoster.walker.test.js \
  tests/lint/entropyRootCensus.walker.test.js tests/lint/tuningRegister.walker.test.js

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/copy/voiceMechanics.test.js

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js

node scripts/check-observed-shape-readers.mjs
node scripts/implementation-packets.mjs validate
npm run check:packet -- EM-B1b
npm run implementation:resume -- EM-B1b
```

Expected: every command exits `0`, **except** the named census interior red until the terminal.
⛔ Never read a gate through a shell pipe (§P7). A member never runs `npm run check`.

---

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and §P8, stop if: ⛔ **a fork pin appears necessary** — **no
fork pin is implemented here**; the three "make them" directives declare `consequence:
'by-target-reality'` and their real-save half is EM-E4's, so writing one is a different packet;
**EM-B1a has not landed** — this packet
modifies its files; the seal is missing or foreign; HEAD is not `d31af2cee` or a descendant proved
non-interfering; **either `operations.js` or `operationsOffStage.js` measures > 250 effective lines** under
eslint's own `Linter` with `skipBlankLines` and `skipComments`, or the `operations.js` edit exceeds
**+5 effective** (⭐ version 6: the splice's import is **ONE LINE**; a four-line import measures
**+8** and is this STOP), or the splice cannot be placed at the FOUR measured anchors, **or any
landed arm reds with a figure this packet did not predict** (the sixth amendment's §P8: the arm's
own message decides and a figure adopted by hand is the failure — the six named arms are A1, A3,
A4, A6 and EM-B1c1's B1 and B6, ten assertion sites, every one measured at `16f0bc71c`), **or the
leaf's `guardsStated` would spell `consequenceFor`**, → STOP, and **never squeeze a row to fit**; `scripts/mutation-coverage-manifest.json` would
need re-serialising whole, or is dirty from another lane; **any phantom-side world state appears
necessary** (a war state, a treaty, a route, an envoy state, a faction-power or legitimacy shift);
**a second force-return mechanism appears necessary**; `consequenceFor` would need authoring,
importing or referencing; **a guard RULE FUNCTION would need authoring** (EM-C3's); an off-stage
type's `requires` would need a world predicate rather than a registry ENTRY; an adapter per op type
appears necessary; a PRNG or `rngContext` import appears necessary; a golden or the prose manifest
moves by one byte; any B1a row would have to change.

---

## 12. Completion receipt

Base SHA · seal identity · final tree state · exact changed files and effective-line deltas, **with
the COMBINED `operations.js` figure measured by eslint's `Linter`** · acceptance C1–C8 plus B1a's
A1–A8 re-run green · the mutants planted, convicted and restored digest-exact (§P6), **with the
account copied verbatim into the manifest row** · `invariants` row count before and after,
**MEASURED at the landing tip and stated as `+1`** (⛔ no absolute is copied from this packet), and
proof the file was NOT re-serialised (a `git diff --stat` showing one changed hunk)
· focused commands, exits and counts · sealed per-step receipt and resume status · both typecheck
configurations · gate stages actually executed · base-versus-wave failure identity diff ·
dormancy/golden result · census tuple before and at the tip with the interior red quoted verbatim ·
generated artifacts `NONE` · deviations `NONE | STOP` · out-of-scope observations without
investigation · **judgment calls: `NONE`**.

---

## 13. RAISED — for the chair

| # | Item |
|---|---|
| **R1** | ✅ **CLOSED by the chair's ruling (2).** O1 is taken and shaped so B1a's manifest is unchanged: this packet CREATES `operationsOffStage.js` and touches `operations.js` by one import and one spread (+2 effective). Both files sit inside the 250 cap. O3 stays recorded as refused. |
| **R2** | ✅ **ANSWERED by EM-B1c1 version 2's ratified form (judgment 92 R7).** The hole is real — `Object.freeze({...A, ...B})` freezes the OUTER map only — and the cure is that C1/C6 assert **every composed ROW frozen** plus **each of this leaf's FOUR exported groups frozen**, which are nameable from the five declared exports. EM-B1a's home map is exported by nothing and cannot be named, so "both source maps frozen" stays unwritable and is not attempted. The finding, retained: **`OP_TYPES` is a composition of five frozen maps**, so B1a's A1 totality arm and this packet's B1/B6 assert over the composition rather than one literal. ⚠ One consequence worth ruling: `Object.freeze({...A, ...B})` freezes the OUTER map only, so B1/B6 assert each SOURCE map frozen as well — otherwise a row could be mutated through `OFF_STAGE_OP_TYPES` while `OP_TYPES` reports frozen. Confirm that reading. |
| **R7** | ⛔ **`target` ON THE SEVEN — the field version 4 never spelled.** This version declares `target: 'phantom'` on all seven, because `consequence: 'by-target-reality'` resolves from the TARGET and §2's non-goals put the real-save half in EM-E4. ⚠ The honest cost: `OpTypeDeclaration.target` is a SINGLE kind of the closed seven, so the declaration cannot say *"a phantom or a real neighbour"*. `validateOp` does not enforce the match (EM-B1a §8 step 3, ruled deliberate) and EM-C4a's door refuses by resolution, so nothing reds today — but when EM-E4 lands the real-save half, either `target` widens to a LIST on every row of the catalogue (an EM-B1a row-shape change, which §11 makes a STOP here) or EM-E4 carries its own rows. **The chair rules which; the lane recommends `'phantom'` now and the question chartered for EM-E4.** |
| **R8** | ⛔ **§14's ELEVEN-ROW SET CONTRADICTS THIS PACKET'S OWN C3.** `sue-for-peace` is spelled there with `requires.world = warInProgress(counterparty)` — a WORLD predicate on an off-stage type, which §6's clause (d) and C3 forbid on all seven. The chair's dispatch fixes this member at **SEVEN**, so §14 is retained as the record and its four rows ride a successor packet that rules the contradiction first. **The lane does not adjudicate it.** |
| **R4** | ✅ **CLOSED BY MEASUREMENT (version 6).** At `16f0bc71c` the registered estate holds **215 packets and ZERO non-terminal ones**, so `scripts/mutation-coverage-manifest.json` has no live holder and neither do this member's two shared files. TOOL-27 row-keys the register in any case. Nothing to sequence. |
| **R9** | ⛔⛔ **THE TIP'S OWN A6 DORMANCY ARM IS ALREADY RED, AND IT IS NOT THIS MEMBER'S — THE CHAIR MUST RULE IT BEFORE THIS MEMBER BUILDS.** Measured at `16f0bc71c` with the arm's own reader: A6 asserts `importers` `.toEqual([])` over every `src/` file that imports `operations.js` or `worldConditions.js`, and **`src/store/editSlice.js:31` imports `validateOp` from `../domain/edit/operations.js`** — EM-C4a's landed store leaf. EM-C4a's own change manifest names `tests/domain/editOperations.test.js` NOWHERE and its `checks` never run that file, so the arm was never widened: this is judgment 134's class again, one train later. The guard-the-guard passes (`scanned.length` 2263 > 400), so the arm fires. ⛔ **This member's `checks[0]` is exactly that file, so EM-B1b cannot go green until the arm is cured.** The lane does not adjudicate: the cure is a ONE-SITE widening of A6's dormancy roster to `['src/store/editSlice.js imports src/domain/edit/operations.js']`, or to EM-C4a's declared `EDIT_PATH`. **The lane recommends the chair cure it in the sixth amendment's own first commit on the EM-T13 branch, or in a one-line repair member ahead of this train**, because EM-B1b's landing does not NEED the widening (judgment 110's law: a landed arm is widened only by the packet whose landing needs it) and carrying another member's red under this seal would misattribute it. |
| **R10** | ⛔ **THE ACCEPTANCE IDS.** EM-B1c1 landed `B1`–`B6` inside the SAME literal `describe` this member appends to, so version 5's `B1`–`B8` would put two different claims under one title. Version 6 re-letters this member's cases **C1–C8**. **Confirm the lettering** (the alternative is `B7`–`B14`, which the lane refuses because the letter is the member's, not the file's). |
| **R3** | **Guard coverage is legitimately empty at this wave** (ARCH §5's "stated when empty"), so the walker asserts a set of `[]`s. C7 pins that as the current state **with its reason**, and EM-C3's population will move it visibly. Confirm that reading, or direct a stricter instrument now rather than after EM-C3 lands. |

---

## 14. ⛔ THE "MAKE THEM" DIRECTIVES JOIN THE OFF-STAGE SET (ODQ §934.50, design §18)

Design §18: *"When a seal is missing, the card says why in the herald's voice and names the act
that would create the condition."* Those acts are **off-stage directives** — they create a
world-state condition on the **counterparty's** side — so they belong here, with the seven.

| new off-stage op | what it creates | real save | phantom |
|---|---|---|---|
| `make-them-sue-for-peace` | a pending peace offer | **a pin on the counterparty's own peace fork — EM-E4's** | a declared, **record-only** offer (§13) that satisfies the home's predicate at the tick |
| `make-them-send-envoy` | an envoy arrived | the same pin form, EM-E4's | record-only, per §13 |
| `open-route` | an open route | the same pin form, EM-E4's | record-only, per §13 |

⛔ All three carry `stage: 'off-stage'` and `consequence: 'by-target-reality'` like the other
seven — **and they are the clearest case for that policy in the whole catalogue**: against a real
save the decree is handed to the campaign's machinery (a fork pin); against a phantom it is the
record and the home procedures and **nothing else**, because a phantom can absorb an act but never
return one (§13).

**Two more rows the ruling names, with their `requires.world` measured:**

| op | `requires.world` | measured |
|---|---|---|
| `sue-for-peace` | **`warInProgress(counterparty)` ONLY** — the ruling is explicit | `atWarWith(graph, worldState, a, b)` — `embassyHazard.js:74`. ✅ LIVE |
| `schedule-event` | ⭐ **NOTHING** — *"always (from the catalogue)"*; `when` at or after the next tick | a `home` op, not off-stage; it belongs to **EM-B1a**'s set |

⛔⛔ **DEMOTED AT VERSION 5 — RAISED R8, NOT COMPILED HERE.** This member is SEVEN by the chair's
dispatch, and §14's own `sue-for-peace` row carries a `requires.world` that §6's clause (d) and
C3 forbid on every off-stage type. The section is RETAINED as the record and refuted here rather
than deleted. ⭐ Measured for whoever compiles the successor: the eleven-row set splices at
**FIVE** runs, not four, and the leaf measures ≈145 of 250. The text as version 4 left it:

⇒ **the off-stage set goes from seven to eleven** (`declare-war`, `make-peace`, `open-trade`,
`close-trade`, `send-force`, `recall-force`, `resolve-outcome`, plus `sue-for-peace`,
`make-them-sue-for-peace`, `make-them-send-envoy`, `open-route`), and `schedule-event` is a
nineteenth **home** op for EM-B1a. ⚠ **RAISED R5** — `schedule-event` changes B1a's membership,
and B1a is already over its cap by §16.3.

**Budget:** eleven rows × ~9 effective ≈ **99**, plus the policy's shared constants ≈ 12 and
imports ≈ 8 ⇒ **≈119 of the 250 leaf cap** in `operationsOffStage.js` — still comfortably inside,
so O1's shape holds and **no further split is needed here**.

**Acceptance stays at 8.** The four new rows fold into **C2** (the partition, now eleven asserted
as an exact sorted list) and **C3** (no phantom-side prerequisite — the three "make them" rows are
the sharpest test of it, since they *create* the counterparty's condition and must still name only
registry entries in `requires.registry`). **No case is added.**

| **R5** | **`schedule-event` is a HOME op, not an off-stage one** (design §18: *"always (from the catalogue)"*, `requires` empty), so it belongs to **EM-B1a**'s set as a nineteenth row and is NOT compiled here. B1a is already over its cap by its own §16.3, so the two questions land together — raised in both packets so neither can be ruled in isolation. |
| **R6** | **The three "make them" directives depend on EM-E4 for their REAL-save half** (a pin on the counterparty's own fork). Their PHANTOM half is complete here under §13 (record-only). Confirm that a packet may ship an op whose real-save path is another packet's — the alternative is holding all three until EM-E4, which would leave the phantom path unreachable too. |
