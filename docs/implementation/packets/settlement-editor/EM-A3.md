# EM / EM-A3 — THE FLAVOR CENSUS: both halves, property reads and value joins (train `em-w1`, wave 1)

**Preamble:** docs/implementation/preambles/EM-PREAMBLE.md (SHA-256: TO BE STAMPED BY THE CHAIR)

- **Governing preamble:** `EM-PREAMBLE.md` SHA-256 `fdecd426828665cad2dfaf872871f29f46fa4816e1bd4c43c20ccd84dacc9e7c`, MEASURED with `shasum -a 256` at the read tip `bfca6b7d6aad3ef1955d3bc33119988d8a5d9989` on 2026-09-22 — the FIFTH amendment, the tree's live text, and BYTE-UNCHANGED from the version-8 measurement at `16f0bc71c`. **This packet is written to the SIXTH amendment** (`drafts/amendment-6/EM-PREAMBLE.amended.md`, judgment 144), which lands as the FIRST commit of train EM-T13's branch before this member is placed; the chair RE-STAMPS this line to the sixth's measured hash at that placement. The fifth's `COMPILE-RULES.interim.md` rules are subsumed by the sixth and are no longer cited separately.

- **Status:** `LANDED`
  ⚠ The status value stands ALONE on its line because `parsePacketHeader`
  (`scripts/implementation-packets.mjs`, cited BY SYMBOL — a line number moves with the next edit)
  anchors the status row at END-OF-LINE and takes `status` only when exactly one row matches
  (J-TEWF1B-1; the sixth amendment's §P9(c) restates it and extends it to `Verified base`).
  ⭐⭐ **VERSION 9, THE RE-COMPILE ON JUDGMENT 157 (2026-09-22, read tip `bfca6b7d6`): the four
  version-8 blocks are CLOSED.** BLOCK-E is ruled (157d, E-1: the receiver-shape predicate, three
  arms, §6); BLOCK-F is ruled (157b: three roots, one a FILE, the guard root dropped in writing);
  BLOCK-G's refuted control is REPLACED by the planted split probe (A4); BLOCK-H is CURED in the
  tree at `17bf34dd1`, measured an ancestor of this tip. Nothing in §11 is now a lane's judgment
  to make. The member is READY-able on the chair's four remaining questions (§11 THE CHAIR'S FOUR).
- **Landed at:** `0a88d2b41a05737b35ae0a4d08296a714a26ce93` — the flavor census (version 9): tests/lint/flavorFields.census.test.js proves every free field has zero grounded derivation readers through scanSurfaceReads with explicit rootShapes and a key-level roster of exactly the declared decoys, readersProof filled, the reach stated (judgments 157 a–d, 163 a–d); build 0a88d2b41 on the train
- **Packet version:** 9
  - ⭐⭐ **Version 9 is THE RE-COMPILE ON THE CHAIR'S RULING (judgment 157 a–d; 2026-09-22, read
    tip `bfca6b7d6` = train EM-T12's CURED tip).** What moved and why: **§6's property-read arm is
    re-spelled with a RECEIVER-SHAPE PREDICATE** — the census pairs each free key with its receiver
    shape through the estate's own `scanSurfaceReads`, keyed by `writerIdentity(key, shape)`, over a
    cheap `foldCorpus` corpus carrying an EXPLICIT `rootShapes: ['settlement']`, indexing the FULL
    `sourceFiles(ROOT)` and inspecting only the three roots' files — in THREE arms with a planted
    positive control, so the silent all-clear the version-8 pre-proof measured cannot happen;
    **the scan scope is 157b's three roots**, one of them a FILE, with the `src/domain/edit` guard
    root's absence written down rather than walked as an empty set; **the register row's `kind` is
    `rationale`** with an inline reason (157a); **A4's refuted positive control is replaced by the
    split probe**; **BLOCK-H is recorded CURED** with the cure's sha measured an ancestor of this
    tip; **the arm count is no longer withheld**, so the count law's `titles` term is the numeral 7
    and the count prover is re-pointed to version 9 and hardened to prove it; and every
    sixth-amendment instrument is re-run over the planted text at this tip and stated as a DELTA.
    `requiredSymbols` moves 12 → 19. ⛔ No budget was raised, no instrument narrowed, no baseline
    written, no ceiling spent, and the VALUE-JOIN arm is byte-unchanged from version 8.
  - ⭐⭐ **Version 8 is the TRAIN EM-T13 PRE-PROOF (2026-09-22, read tip `16f0bc71c` = train
    EM-T12's flipped tip).** What moved and why: **EM-A1 HAS LANDED** (train EM-T9), so every fact
    this packet took from EM-A1 AS A PACKET is re-measured AS REAL CODE and the stale `Depends on`
    is corrected; the `free` denominator is re-derived FROM THE LANDED TABLE and is **2**
    (`institution.note` `:97`, `npc.note` `:103`) — BLOCK-D's prediction confirmed by measurement,
    never inherited; the MODIFY's true SHAPE is corrected (both rows share ONE module-level const,
    `ANNOTATION_PROOF` `:60`, so the authorized edit is one string literal, not two row literals);
    the enforcer-directory obligations of the CREATE are priced against every walker that governs
    `tests/lint`; every register figure is restated as a DELTA against the tip's live values; the
    byte price is proved by a membership probe that imports `EAGER_FIRST_PAINT_MODULES`; and FOUR
    new blocks (E, F, G, H) record contradictions this pre-proof MEASURED. ⛔ BLOCK-A's cure (the
    live scan) is **not** withdrawn — BLOCK-E is the measurement that its PREDICATE, as §6 spells
    it, cannot carry. No budget was raised, no instrument narrowed, no baseline written.
  - ⭐ Version 7 is the DOCS repair only (ESTATE-REPAIR-4b, 2026-09-20, read tip
    `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`): §7's MODIFY row gains `provenance` and `tier1` in
    its UNTOUCHABLE list — EM-A1 version 5 added both to the `FieldDeclaration` row shape and both
    are EM-A1's, which EM-A1's own inbox item 5 slots here — and **BLOCK-D's condition is answered
    with its measurement: the `free` denominator is TWO** (`institution.note`, `npc.note`), which
    closes EM-A1's BLOCK-6 and makes A2 and A5 non-vacuous. ⛔ **No contract, budget, acceptance
    case, required symbol, change row, BLOCK or non-goal moved**, and no path joins §7, the capsule
    or `checks`.
  - Version 6 is the DOCS repair only (ESTATE-REPAIR-3, 2026-09-20, read tip `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`): the
    manifest's `REGISTER` row on `scripts/mutation-coverage-manifest.json` gains the **`rowKey`**
    field TOOL-27 keys its row-level exemption on, in the same spelling the row's existing
    `symbol` already carries, and the interim-rules header row is added. ⭐ **MEASURED, not
    assumed: this packet CREATEs NO `.js` under `src/generators/**` or `src/domain/**`, so
    `stamp.producerIndexFiles` moves by `+0` and interim rule 1 owes it no deferred row** — its
    one CREATE is `tests/lint/flavorFields.census.test.js`. ⛔ **No contract, budget, acceptance
    case, required symbol, change row or non-goal moved.**
  - Version 5 is the DOCS repair only (ESTATE-REPAIR + ESTATE-REPAIR-2, 2026-09-20): the running
    lighting tuple converted to a delta, §7's census row actioned `CREATE`, the sealed lint check's
    lighting-walker argument removed, and this citation re-pointed to a symbol. ⛔ **No contract,
    budget, acceptance case, required symbol, BLOCK or non-goal moved**; the pre-repair text is
    version 4, in `pre-estate-repair-2026-09-20/`. Where the body below says "in version 1", it
    means the packet's own version 1, not that copy.
- **Verified base:** `em-t13-a3-2026-09-22` at `e1750d7454c1dff7e81342efadd05e47719ca6aa`
  ⚠ The value stands ALONE on its line (§P9(c) extends the status-row law to this row). The chair
  stamps it at placement. **THE REVALIDATION SENTENCE, with its measured window facts:**
  *"Revalidated at `e1750d7454c1dff7e81342efadd05e47719ca6aa` on the EM-T13 branch: the J-T1 window `d31af2cee` → `bfca6b7d6` over
  all seventeen change-manifest and `requiredSymbols` paths moved exactly TWO —
  `scripts/mutation-coverage-manifest.json` (+88/−1) and `src/domain/edit/fieldDeclarations.js`
  (+144, EM-A1's CREATE) — and every other path is byte-unchanged; the inner window
  `16f0bc71c` → `bfca6b7d6` (EM-C4a's cure `17bf34dd1`) touched SIX files and NOT ONE of this
  member's, so every version-8 measurement holds unaltered; the CREATE target
  `tests/lint/flavorFields.census.test.js` is ABSENT; all nineteen `requiredSymbols` resolve
  verbatim at count 1."*
- **Last revalidated:** `e1750d7454c1dff7e81342efadd05e47719ca6aa` (re-compiled 2026-09-22 against `bfca6b7d6`)
- **Depends on:** `EM-A1` — **LANDED** in train EM-T9. ⛔ The version-7 text *"EM-A1 is DRAFT on
  `EM-P2` and `EM-P3`"* was STALE and is corrected here: `src/domain/edit/fieldDeclarations.js`
  exists at `16f0bc71c` (144 raw lines, 47 effective) and every fact below is measured against the
  LANDED table, never against EM-A1's packet (the sixth amendment's judgment 134: a sibling's
  LANDED fence, never its predicted one). No other member gates this packet.
- **Collision group:** `EM-A1` is LANDED, and `LANDED` is TERMINAL — it reserves no change path, so
  the collision is discharged. **Inside train EM-T13** the only shared path is the ROW-KEYED
  register `scripts/mutation-coverage-manifest.json`: EM-B1b holds
  `invariants['tests/lint/opGuardCoverage.walker.test.js']` and EM-B2a4 holds
  `invariants['tests/property/dmLayerGoldenIsolation.test.js']`; this member holds
  `invariants['tests/lint/flavorFields.census.test.js']`. Three distinct row keys, no collision
  (TOOL-27; §P10.8). ⛔ All three must avoid `kind: "uncovered"`, which would move the global scalar
  `uncoveredBaseline` and refuse at placement. Path-disjoint against EM-D0e v1.1 and EM-B1b and
  EM-B2a4 on every other row (§11's disjointness table).
- **Commit authority:** edits only; the chair commits
- **Baseline posture:** measured at `bfca6b7d6`. Version 8's twelve measurements are re-run or
  proven byte-unchanged by the inner window, and version 9 adds seven EXECUTED ones: **the SPLIT
  PROBE** (the ruled mechanism run end to end on planted files — `rGrades 4`, `R` exactly the
  grounded file, `N` polluted by the decoys) and **its negative control** (the same probe with
  `rootShapes` omitted — `rGrades 0`, `resolvedReceivers 0`, the silent all-clear); **the
  KEY-LEVEL SITE ROSTER** over the three ruled roots, taken with the census's own planned
  mechanism (the estate's parse through `buildIndex`, the estate's own `isWriteTarget`); **the
  three roots' file populations** (114 · 444 · 1) with the FILE root walked as a file; **the voice
  walker's OWN tokenizer**, extracted from its source and run over the leaf as it stands AND over
  the planned string; **the governing set of every module the new contract imports**
  (`git grep -l -F <basename> -- tests`, one search per import target), which finds NO importer
  roster, dormancy arm or enumerator that a new `tests/lint` importer moves; and **the estate's
  2,795 `requiredSymbols` rows** swept for a pin on the one literal this packet rewrites (zero).
  Every figure carries its command in `EM-A3.evidence-t13.md`.

---

## 1. Reconciled authority

1. ⭐⭐ **SECOND CHAIR AMENDMENT (ODQ §934.44–§934.45; design §14 FINAL and §15; the ARCH and charter at `7aa769830`).** Item 5 reads *"EM-A3's flavor census keeps its half (a `free` field has zero derivation readers) and adds nothing else; the root check is A1's walker."* **That is now a RULING**, so the call this lane had recorded vetoably is withdrawn as a judgment and kept only as its reasoning (§12). Item 6 keeps the base at `d31af2cee` with J-T1 blob identity over the moved window — **EXECUTED: 27 of 27 measured paths byte-identical to `7aa769830`** (`EM-A1.evidence.md` E-40), including both register baselines and both scanner sources this packet reads. Nothing else in the amendment reaches here: the fifth world-fact card, the two new root fields (`npc.status`, `institution.state`) and the chooser-registry predicate are all EM-A1's.
2. **FIRST CHAIR AMENDMENT, ODQ §934.44 — "edit at the source, never at the derivation."** Its first ROOT test (*"whose writer reads no other world fact"*) is SUPERSEDED by §934.45's "root means CHOSEN". ⚠ **This packet's §11 BLOCK-D was written under the superseded test and its arithmetic moves with it**: under "root means chosen", six of EM-A1's nine drops reverse, so the `free` population may NOT be empty after all. BLOCK-D is therefore CONDITIONAL on EM-A1's ruling and is re-stated that way below.
2. `docs/OWNER_DECISION_QUEUE.md` §934.42 (build the editor now) and the §934.36 addendum of 2026-09-19 ~18:3x — *"a field may be typed when nothing downstream reads its value as a fact … the set is a CENSUS with a proof per field from the writer-reach and observed-shape instruments, not a list by taste."*
3. `docs/DESIGN_EDIT_MODE_AND_DECREES.md` **§12.3 and §14 GOVERN** — the census classifies by PROPERTY READS **and** by VALUE JOINS; a join-key field can never be plain `free`. `EM-PREAMBLE.md` §P5 HZ-JOINKEY restates it.
4. `docs/ARCH_EDIT_MODE_AND_DECREES.md` §8 instrument 2 — the reader scopes and the per-row proof string. **Instrument 1's new root arm is EM-A1's** (§12).
5. `docs/implementation/charters/EDIT-MODE-TRAIN.md` row **EM-A3** (unamended by §934.44 — only EM-A1's and EM-F1's rows moved).
6. `docs/implementation/PACKET_STANDARD.md`, `EM-PREAMBLE.md` §P1–§P9.
7. Live code and executed evidence at `d31af2cee` — `EM-A3.evidence.md`.

Resolved contradictions:

- Design §6.2 says the census is measured *"from the writer-reach and observed-shape data"* and ARCH §8.2 adds *"AND by VALUE JOINS"*. → §12.3 governs: **both halves**, not either.
- ⭐ **Where does the §14 ROOT check live — here or in EM-A1's walker?** → **RULED by the chair's second amendment, item 5: EM-A1's walker owns it; this census keeps its half and adds nothing else; the arm is not duplicated.** The reasoning this lane had assembled independently is kept in §12 as a citation, not as a judgment.

7. ⭐⭐ **THE CHAIR'S RULING OF 2026-09-22, judgment 157 a–d** (`findings/PREPROOF-EM-T13-2026-09-22/CHAIR-RULING.md`, EM-A3's section), on the version-8 pre-proof's four blocks and on the receiver-shape recon's report (`EM-A3.recon-receiver-shape.md`). It is the authority for §6 whole, for §7 row 3's `kind`, and for the scan scope:
   - **157a** — the `scripts/mutation-coverage-manifest.json` row's `kind` is `rationale` with an inline reason of at least 40 characters, for a `tests/lint` census whose remover-proof IS the build's red-first; `mutation` only when the packet also ships the sweep label, the once-claimed LABEL JOIN and the `MUTATED_FILES` row.
   - **157b** — the scan scope is `['src/generators', 'src/domain/worldPulse', 'src/domain/causalState.js']`, the walk accepting a FILE root; the `src/domain/edit` guard root is DROPPED with its absence written down, never walked as an empty set; **the ARCH's §8 instrument-2 sentence is corrected in the same version** (§7 row 4).
   - **157c** — BLOCK-H is already CURED in the tree at `17bf34dd1`; `checks[1]` stays as sealed.
   - **157d** — the predicate is **E-1, as the recon spelled it**: `scanSurfaceReads` over `foldCorpus` with an explicit `rootShapes`, three arms and a planted positive control, the REACH stated in words. ⛔ The exact resolver (`reader-shape-scan.mjs`'s `inspectNodeRead`) is NOT used.

Contradictions this packet does **not** resolve: **NONE remain open as blocks.** Version 8's four (BLOCK-E · F · G · H) are closed by judgment 157 and by the tree's own cure; §11 keeps each with its measurement, struck through, because a later lane that re-simplifies the census needs to find the discharge rather than re-derive it. Four questions stay the chair's and none of them gates the build — they are listed at **§11 THE CHAIR'S FOUR**, each with this lane's recommendation and its measurement.

The implementer does not read other documents to reinterpret this packet.

## 2. Outcome

**Observable result:** `tests/lint/flavorFields.census.test.js` proves, for every row of `FIELD_DECLARATIONS`, that a `free` field has zero derivation readers by BOTH halves — property reads and value joins — and that any field the rename cascade joins on is declared `free-cascade`, never `free`; each `free` row's `readersProof` string in `fieldDeclarations.js` names the arm that proved it.

**Definition of done:** the census file exists and is green in all SEVEN arms, and every `kind: 'free'` row carries a `readersProof` the census itself verifies — a string that states the reach honestly and overclaims nothing (§6).

In scope:

1. **One primary behaviour:** the two-half census with a proof string per row.
2. **One required integration:** the `readersProof` values on the `free` rows of `fieldDeclarations.js`.
3. **One prevention guard:** the census IS the guard.
4. **One governing-document correction:** the ARCH §8 instrument-2 sentence, which names two roots that do not exist and two registers that cannot deliver the half (judgment 157b).

Explicit non-goals:

- the declarations themselves (`EM-A1`), the pool catalogue (`EM-A2`), ops, the layer, the registry;
- widening, shrinking or re-freezing either register — **this packet writes no baseline and runs no `--write`**;
- any change to `src/domain/factionRename.js`;
- Record adjacent discoveries in the receipt; do not investigate or repair them.

## 3. Hard scope budget

| Limit | Packet budget |
|---|---:|
| Behavior families | `1` |
| New persisted record families | `0` |
| Named state writers | `0` |
| Feature flags | `0` |
| User-facing surfaces | `0` |
| Direct consumers | `0` |
| New logic-bearing production leaves | `0` |
| Existing logic-bearing production files modified | `1` (`src/domain/edit/fieldDeclarations.js` — `readersProof` string values only) |
| Additional registration-only files | `1` (`scripts/mutation-coverage-manifest.json`) |
| Handwritten files total | `4` (the three §7 paths + the ARCH §8 sentence correction) |
| New/changed effective production lines | `≤ 20` — **MEASURED: exactly ONE.** The only `src/` edit is the content of the string literal on `src/domain/edit/fieldDeclarations.js :: ANNOTATION_PROOF`, and the file's line count is unchanged (145 → 145, executed) |
| Effective lines per new leaf | `n/a` — no new production leaf |
| Delta in a shared/hot file | `n/a` — **this packet names NO hot file.** None of the five standing rows appears in §7. |
| Acceptance cases | `7` |

Overrides approved before dispatch: `NONE`.

Exceeding any limit is a STOP and split, not an invitation to renegotiate.

⚠ **The budget FITS and nothing is blocked on size.** Version 9 adds TWO acceptance cases (A6, A7 — the receiver-shape arm and the key-level roster) over version 8's five, well inside the standard's cap of eight, and adds ONE handwritten file, the ARCH §8 sentence 157b orders corrected. The `src/` production delta is ONE LINE, measured, not estimated.

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- EM-A3
```

Expected, ALL FOUR RE-MEASURED AT `bfca6b7d6`: `tests/lint/flavorFields.census.test.js` ABSENT (`git ls-files` empty, `ls` reports no such file); `src/domain/edit/fieldDeclarations.js` PRESENT and clean; `scripts/mutation-coverage-manifest.json` and `docs/ARCH_EDIT_MODE_AND_DECREES.md` PRESENT; and all NINETEEN `requiredSymbols` rows resolving verbatim at `grep -c -F` = 1. Any mismatch makes this packet STALE.

⭐ **DISPATCH IS NO LONGER REFUSED.** Version 8 was refused twice — once at BLOCKED status and once on an unlanded EM-A1. EM-A1 LANDED in train EM-T9, and judgment 157 closes all four blocks, so the only bar left is the chair's placement.

## 5. Verified tree contract

Every row executed at `d31af2cee` and RE-EXECUTED at `bfca6b7d6`; the command is in
`EM-A3.evidence-t13.md` under the cited F- or V9- number. ⭐ **THE FIRST NINE ROWS BELOW ARE THE
RULED MECHANISM (version 9, judgment 157d)**; the `Register A` / `Register B` rows that follow are
the BLOCK-A RECORD — the measurement that forced the live scan — and this census READS NEITHER
BASELINE.

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| ⭐ The identity | `scripts/lib/writer-reach-scan.mjs` | `writerIdentity` | `export function writerIdentity(key, shape)` returns `` `${key} on ${shape}` ``, with the header *"The estate's ONE identity spelling, shared with OSR's `identityOf`, COVERAGE's `BandField.id`, CHARSET's `customContentFieldOsrIdentity` and READERREVIEW's rubric. Never the line number."* `grep -c -F` → 1 (V9-1) | §6 arm 1's key; the census never spells `${key} on ${shape}` itself |
| ⭐ The scan | `scripts/lib/writer-reach-scan.mjs` | `scanSurfaceReads` | `export function scanSurfaceReads({ index, corpus, files, root, minRows = MIN_ROWS, keysToShapes, relOverride = null })`, returning `{ reads: Map<string, {R: Set<string>, N: Set<string>}>, stats }`. `index` and `files` are SEPARATE parameters, so the FULL tree may be indexed while only the roots' files are inspected. `R` = the receiver was GROUNDED to that shape; `N` = a name-level read credited to every known shape carrying the key. `grep -c -F` → 1 (V9-2) | §6 arm 1, executed |
| ⭐ The key→shape map | `scripts/lib/writer-reach-scan.mjs` | `keysToShapesOf` | `export function keysToShapesOf(corpus, minRows = MIN_ROWS)`; it skips a shape whose `record.rows < minRows`. `grep -c -F` → 1 (V9-3) | §6's `keysToShapes` input |
| ⭐ The index and the floor | `scripts/lib/writer-reach-scan.mjs` | `export { buildIndex, MIN_ROWS }` | `:621` re-exports both from `legacy-reader-shape-scan.mjs` / `observed-shape-baseline.mjs`, so the census imports ONE module for the whole mechanism. `MIN_ROWS = 40`. `grep -c -F` → 1 (V9-4) | §6's `index`; the census's own `minRows` is stated, never defaulted silently |
| ⭐ The write-target test | `scripts/lib/writer-reach-scan.mjs` | `isWriteTarget` | `export function isWriteTarget(node)` — assignment, compound assignment, `delete`, prefix/postfix. `grep -c -F` → 1 (V9-5) | §6 arm 2 uses THE ESTATE'S test, never its own |
| ⭐ The cheap corpus | `scripts/lib/observed-shape-corpus.mjs` | `foldCorpus` | `export function foldCorpus(roots)` over `Array<{name, value}>`, returning `{ shapes, arrayShapes, singleHome, graph }`. ⛔ **IT EMITS NO `rootShapes`** — that key is added only by `buildObservedCorpus` (`:1001`) — and `makeResolver`'s sixth parameter defaults to `[]`, so an omitted `rootShapes` grounds NOTHING. **EXECUTED BOTH WAYS at this tip: with `rootShapes: ['settlement']` → `rGrades 4, resolvedReceivers 4`; without it → `rGrades 0, resolvedReceivers 0`, a silent all-clear** (V9-6, V9-7) | §6's corpus, with the explicit `rootShapes` and the `rGrades > 0` fence that catches its omission |
| ⭐ The index input | `scripts/check-observed-shape-readers.mjs` | `sourceFiles` | `export function sourceFiles(root = ROOT)` walks `src/` under `isObservedShapeScanPath`; MEASURED **2,250** files at this tip. `grep -c -F` → 1 (V9-8) | §6 indexes the FULL tree and inspects 559 files |
| ⭐ The scan roots | the tree | `src/generators` · `src/domain/worldPulse` · `src/domain/causalState.js` | MEASURED at this tip: **114** `.js` · **444** `.js` · **1** FILE (`git ls-files`; `src/domain/causalState` is NOT a directory, and `/(^\|\/)guard[A-Za-z]*\.js$/` matches ZERO files anywhere under `src/`). The walk accepts a FILE root (judgment 157b) (V9-9) | §6's `SCAN_ROOTS`; A4 asserts a non-empty set under each |
| ⭐ The neighbour's budget pin is MODULE-LOCAL | `tests/lint/writerReach.walker.test.js` | `scansRun` · `countedScan` | `let scansRun = 0; const countedScan = (args) => { scansRun += 1; return scanSurfaceReads(args); };` is a module-scope binding of THAT file, asserted `toBe(2)` in its own suite. A SECOND file that imports `scanSurfaceReads` directly cannot reach the counter: it is not exported and each test module has its own registry. **This census therefore does not move that pin** (V9-10) | §7's `scansRun +0`; the one arm a naive re-use would have redded |
| ⭐ The free denominator, from the LANDED table | `src/domain/edit/fieldDeclarations.js` | `FIELD_DECLARATIONS` · `ANNOTATION_PROOF` | Seventeen rows over five cards; **exactly TWO** `kind: 'free'` rows, both `provenance: 'annotation'` and both keyed `note` — `institution.note` (`:97`) and `npc.note` (`:103`) — and both read `readersProof: ANNOTATION_PROOF`, the ONE module-level const at `:60`. An annotation row carries NO `outputKey`, "because the absence IS the claim that nothing on the record reads it". Byte-unchanged across the inner window (V9-11) | §6's subject; the MODIFY's true shape |
| ⭐ No arm pins the proof STRING | `tests/domain/editDeclarations.test.js` | four `readersProof` assertions | RE-MEASURED at `bfca6b7d6` after EM-C4a's cure moved 47 lines of this file: absent on a root row (`:165`), absent on a world-fact row (`:172`), non-blank on an annotation row (`:179`), present iff `kind === 'free'` (`:190`). **No assertion anywhere in `tests/` or `src/` reads the VALUE**, and the estate's 2,795 `requiredSymbols` rows carry ZERO pins on it (V9-12, V9-13) | §7 row 2 may rewrite the string; §P2.18 is discharged by measurement |
| Register A — real path | `scripts/.writer-reach-baseline.json` | `"stopSet"`, `"surfaceReach"`, `"darkUnregistered"` | Exists; `schema 1`; `frozenAtSha 5123689dff88b48a9948ba4160736aa87ba2d3c3`; `surfaceReach` holds **6,537** identities; `darkUnregistered` holds **1,290**; `population` is `{judged 6537, lit 572, litName 4671, dark 1294, thinKeys 9144, thinShapes 962, knownShapes 337}` (F-1, F-2) | The property-read half's declared source |
| ⛔ Register A — the STOP set | `scripts/.writer-reach-baseline.json` | `"stopSet"` | Exactly `["src/generators/","src/store/","src/workers/","src/lib/instantWorld/"]` (F-2) | §11 BLOCK-A |
| ⛔ Register A — no per-file readers | `scripts/.writer-reach-baseline.json` | top-level keys | The 23 top-level keys carry **no `readers` map**; `surfaceReach`'s value is a per-surface-class grade STRING, e.g. `"campaign-pdf=R dossier-pdf=R foundry=R web-display=R web-transitive=R world-book=R"` (F-2, F-4) | §11 BLOCK-A |
| Register A — the scanner's law | `scripts/lib/writer-reach-scan.mjs` | `SURFACE_CLOSURE_STOP` | `Object.freeze(['src/generators/', 'src/store/', 'src/workers/', 'src/lib/instantWorld/'])` at `:115`, with the header *"the engine boundary every closure halts at. Load-bearing at the FILE level … the same read planted at a `src/generators/` path lights nothing"* (F-3) | §11 BLOCK-A |
| Register A — the display split | `scripts/lib/writer-reach-scan.mjs` | `WEB_DISPLAY_DIRS` · `REPORT_ONLY_CLASSES` | `WEB_DISPLAY_DIRS = ['src/components/','src/domain/display/','src/pdf/']` (`:90`); `REPORT_ONLY_CLASSES = ['web-transitive','json-export']`, `web-transitive` documented as *"reachable only through files the web root drags in, never through a component, a display read model or the PDF"* (`:87`) (F-3) | The ONE non-display signal the register does carry |
| Register A — grade grammar | `scripts/lib/writer-reach-scan.mjs` | `parseReach` · `judgeWriters` | Grades are exactly `R` (a grounded receiver), `N` (an ungrounded name), `A` (allowlisted json-export); a class appears in `reach` only if some closure file read it (`:444-484`, `:490-501`) (F-3) | Read the grade, never infer one |
| ⛔ Register B — the population | `scripts/.observed-shape-readers-baseline.json` | `"inventory"` | The READER-WITH-NO-WRITER inventory: `_doc[0]` = *"READER-WITH-NO-WRITER INVENTORY — per-file HEURISTIC-LEAF identities and governed ceilings."* `inventory` covers **387 files** (256 `src/domain`, 50 `src/components`, 26 `src/generators`, 19 `src/lib`, 17 `src/store`, 12 `src/pdf`, …); `identities 1390`; `frozenAtSha 31ab5d18bdd44ba0691c2e16f3ae8f3cfef36eb1` (F-5, F-6) | §11 BLOCK-A |
| ⛔ Register B — coverage of the subject | `scripts/.observed-shape-readers-baseline.json` | `"inventory"` | Over all 387 files, **exactly ONE** row matches `(name\|role\|category\|faction\|power) on (institutions\|npcs\|factions)` — `src/pdf/lib/viewModel.js :: faction on npcs = 3` (F-6) | §11 BLOCK-A |
| Value-join authority | `src/domain/factionRename.js` | `FACTION_RENAME_SURFACES` (32) · `NPC_RENAME_SURFACES` (5) | The two cascade denominators, enumerated by live import (F-7) | The value-join half reads exactly these |
| Value-join ruling ledger | `src/domain/factionRename.js` | `NON_CASCADED_SURFACES` (16) · `NPC_NON_CASCADED_SURFACES` (13) | The written exclusions; each row carries a `why` (F-7) | A path here is a RULING, not a miss |
| ⛔ Measured: no candidate field is display-only | `scripts/.writer-reach-baseline.json` | `"surfaceReach"` | `name on institutions`, `name on npcs`, `name on factions`, `faction on factions`, `category on institutions`, `category on factions`, `role on npcs`, `power on factions` — **every one carries `web-transitive=R`**, i.e. a grounded read outside the three display dirs. None is `DARK` (F-4) | §11 BLOCK-C |
| Test precedent | `tests/domain/factionRename.test.js` | `describe('faction rename — the INDEPENDENT denominator')` › `test('every stored path that carries a faction name is declared, or ruled out in writing')` | The independent-denominator shape: it walks the whole blob and never reads the module's own declarations (F-8) | Copy this proof shape — the census must not be its own answer sheet |
| Test precedent | `tests/lint/habitBandsReconciliation.walker.test.js` | `describe('HB — the Bands line reconciles against the tuning seam, both directions')` › `test('GUARD-THE-GUARD: a Bands line planted on a no-bands wave REDS')` | Literal titles; both-directions reconciliation; one guard-the-guard arm (F-8) | Copy this walker shape |
| Census instrument | `tests/lint/sovereigntyLightingContract.walker.test.js` | `TEST_FILES` · `CENSUS` | Counts TEST files only (`:515-518`, asserted `:7460`). ⚠ AS OF `d31af2cee`, AND AS HISTORY ONLY: `tests/lint/.lighting-census-baseline.json` then read `files 2645 · parked 383 · credited 2262 · titles 25009 · suiteTitles 6670` (F-9). Every landing moves it; the live tuple is read from the register, never from this row. | §7's predicted delta |
| Registration instrument | `tests/lint/mutationCoverage.shared.mjs` | `ENFORCER_DIRS` | `tests/lint` is the first of eight. ⚠ AS OF `d31af2cee`, AND AS HISTORY ONLY: the manifest then carried 704 `invariants` rows, 171 under `tests/lint/` (F-10); measured **716 / 177** at `bdbf7c89c`. The population moves with every landing; §7 row 3 states this member's move as a DELTA. | §7 row 3 |
| Golden posture | `tests/property/generatorGoldenMaster.test.js` · `tests/property/dossierProseManifest.test.js` | — | Neither appears in §7; this packet adds one test file and edits string values in one frozen table | UNCHANGED; motion is a STOP |

Forbidden alternatives:

- no second census, no second reader scanner, no second spelling of either register;
- **no `--write`, `--genesis`, `--rebank` or any other writing door on either register** — a compile or an implementer that re-freezes a baseline to make a census green has inverted the instrument;
- no edit to `src/domain/factionRename.js`, to either baseline JSON, or to `tests/lint/.lighting-census-baseline.json` by hand;
- no files outside the manifest.

## 6. Exact contracts

⭐ **NOTHING IS WITHHELD IN VERSION 9.** Both halves are spelled exactly: the VALUE-JOIN arm is byte-unchanged from version 8, and the PROPERTY-READ arm is judgment 157d's predicate in mechanism, with its three arms, its planted positive control and its REACH stated in words.

### The value-join arm — SETTLED

```js
// Read the four declared lists from the cascade module itself; never re-spell a path.
import {
  FACTION_RENAME_SURFACES, NPC_RENAME_SURFACES,
  NON_CASCADED_SURFACES, NPC_NON_CASCADED_SURFACES,
} from '../../src/domain/factionRename.js';
```

**The predicate, exactly.** For a declaration row `{ card, field, kind }`, its stored path family is `storedPathsOf(card, field)` — the set of declared paths under which that card's records live (`institutions[]`, `npcs[]` **and** `factions[].members[]` for an NPC, `powerStructure.factions[]` **and** `factions[]` for a faction). The row **JOINS** iff any member of that set, suffixed `.${field}`, appears as a `path` with `kind: 'key'` in `FACTION_RENAME_SURFACES` or `NPC_RENAME_SURFACES`.

- A row that JOINS and is declared `'free'` → **RED**, message naming the row and the exact cascade path.
- A row that JOINS and is declared `'free-cascade'` → **PASS**.
- A row that JOINS and is declared `'pool'` → **RED** (a pooled value that is also a join key needs the cascade too; the cascade is the point, not the typeability).
- A row that does NOT join and is declared `'free-cascade'` → **RED**, message naming the row: `free-cascade` asserts a cascade exists; a row without one is the BLOCK-1 class EM-A1 records.
- `kind: 'prose'` surfaces are **excluded from the join predicate** and stated as such: a prose surface is a substitution target, not an exact-key join, and the two carry different risks (`factionRename.js`'s own `kind` docblock).

**Anti-vacuity, mandatory.** The arm asserts in the same test that `FACTION_RENAME_SURFACES.length` and `NPC_RENAME_SURFACES.length` are both `> 0` and that at least one declaration row was classified as JOINING. A census over an empty denominator, or one that finds no join at all, is the vacuous pass this arm exists to refuse.

**Absence and ordering.** Declarations are read in `FIELD_DECLARATIONS` order; the surface lists are read in their authored order; nothing is sorted and no path is normalized beyond the exact `[]` spelling the module uses. A path spelled differently is a RED, never a near-match.

### ⭐⭐ The property-read arm — THE RECEIVER-SHAPE PREDICATE (judgment 157d, E-1 as the recon spelled it)

⛔ **The two register baselines are NOT read**, and neither is the EXACT resolver. `reader-shape-scan.mjs`'s `inspectNodeRead` is unexercised outside `scanReaders`, and a full-tree exact scan must declare `excludedReadScopes` because ONE unresolvable UI read throws `budgetFailure` and aborts the whole scan. The census uses the estate's own **`scanSurfaceReads`**, which is exported, takes an arbitrary `files` list, and keys every read site by the estate's ONE identity spelling, `writerIdentity(key, shape)`.

**THE MECHANISM, exactly.** One import block, from three modules the estate already owns:

```js
import {
  buildIndex, keysToShapesOf, scanSurfaceReads, writerIdentity, isWriteTarget, MIN_ROWS,
} from '../../scripts/lib/writer-reach-scan.mjs';
import { foldCorpus } from '../../scripts/lib/observed-shape-corpus.mjs';
import { sourceFiles } from '../../scripts/check-observed-shape-readers.mjs';
import { FIELD_DECLARATIONS } from '../../src/domain/edit/fieldDeclarations.js';
```

```js
// THE SCAN SCOPE — judgment 157b. THREE roots, and the third is a FILE.
const SCAN_ROOTS = Object.freeze([
  'src/generators',            // MEASURED at bfca6b7d6: 114 .js files
  'src/domain/worldPulse',     // MEASURED at bfca6b7d6: 444 .js files
  'src/domain/causalState.js', // MEASURED: a FILE, not a directory. The walk accepts one.
]);
```

⛔ **THE FOURTH ROOT IS DROPPED, AND ITS ABSENCE IS WRITTEN DOWN RATHER THAN WALKED.** The ARCH's §8 sentence named `src/domain/edit/guards*`. The pattern `/(^|\/)guard[A-Za-z]*\.js$/` matches **ZERO** files anywhere under `src/` — the eight leaves of `src/domain/edit` are `dmLayer` · `fieldDeclarations` · `operations` · `operationsNpcRename` · `pools` · `recordRegister` · `types` · `worldConditions`. A root that resolves to an empty set would make A4's fence red on its own contract, so the root is REMOVED from `SCAN_ROOTS` and the ARCH's sentence is corrected in the same version (§7 row 4). The census carries a one-line comment naming the drop and its reason, so a later lane finds the measurement rather than re-adding the root.

**THE CORPUS, and the one hazard that makes it sound.**

```js
const ROOT = join(dirname(fileURLToPath(import.meta.url)), '../..');  // never process.cwd()
const corpus = foldCorpus(OBSERVATIONS);        // the CHEAP builder; buildObservedCorpus() costs 251,002 ms
corpus.rootShapes = ['settlement'];             // ⛔ LOAD-BEARING: foldCorpus emits none
const keysToShapes = keysToShapesOf(corpus, MIN_ROWS_HERE);
const index = buildIndex(sourceFiles(ROOT));    // the FULL tree is INDEXED …
const { reads, stats } = scanSurfaceReads({     // … and only the roots' files are INSPECTED
  index, corpus, files: filesUnder(SCAN_ROOTS), root: ROOT, minRows: MIN_ROWS_HERE, keysToShapes,
});
```

⛔ **`rootShapes` IS THE DIFFERENCE BETWEEN A PROOF AND A SILENT ALL-CLEAR, AND THAT IS EXECUTED, NOT ARGUED.** `foldCorpus` does not emit the key; `makeResolver`'s sixth parameter defaults to `[]`; a receiver that cannot be grounded yields no finding, by the resolver's own design (*"Resolution is DELIBERATELY PARTIAL AND SILENT. A receiver that does not resolve yields no finding: this walker never guesses."*). Run at `bfca6b7d6` on the same two planted files: **with `rootShapes: ['settlement']` → `rGrades 4, resolvedReceivers 4`; without it → `rGrades 0, resolvedReceivers 0`** and both identities read `R: []`, which is indistinguishable from a clean tree. Arm 3's `rGrades > 0` assertion is what makes that failure impossible to pass.

⭐ **INDEX THE WHOLE TREE, INSPECT ONLY THE ROOTS.** `scanSurfaceReads` takes `index` and `files` as separate parameters for exactly this, and `reader-shape-scan.mjs` states the law: *"EXCLUDED READ SCOPES EXCLUDE READ SITES, NEVER INDEX INPUTS"* — a generator receiver grounds only through call sites that may live outside the roots. MEASURED at this tip: `sourceFiles(ROOT)` is **2,250** files indexed, **559** inspected.

**THE VERDICT, WITH NO DISCRETION — THREE ARMS.** For each `kind: 'free'` declaration row, its key `K` is the row's `field` and its shape `S` is the card's stored shape (`institution` → `institutions`, `npc` → `npcs`), derived from the DECLARED homes and never from a hand-written probe map.

| arm | case | the assertion | what reds it |
|---|---|---|---|
| **1** | A6 | `reads.get(writerIdentity(K, S))?.R` is EMPTY, for every `free` row. MEASURED subject at this tip: `note on institutions` and `note on npcs` | ANY grounded real read of that key on that card shape under the three roots |
| **2** | A7 | the census's OWN key-level site roster over the three roots equals the frozen DECOY TABLE below, row for row | a NEW receiver (a new row, red BY NAME) or a NEW site on a declared receiver (red by that row's occurrence count) |
| **3** | A4 | the planted SPLIT PROBE discriminates: decoys in one planted file, one grounded read in another; `R` holds exactly the grounded file, `N` is polluted by both, and `stats.rGrades > 0` | a resolver that grounds nothing — the silent all-clear |

⛔ **THE REACH THIS CENSUS MAY CLAIM, STATED IN ONE SENTENCE AND NEVER OVERSTATED.** `R` is the legacy resolver's grounding, and `writer-reach-scan.mjs`'s own header measures that resolver at *"7.67 % of reads"* — which is why the `N` grade exists at all and why `N` is *"Deliberately over-permissive: the conservative direction for a DARKNESS claim."* **An empty `R` therefore proves ZERO GROUNDED READS, never zero reads.** The second claim — that no ungrounded `.note` read under the roots is a read of an institution or an npc — is carried by ARM 2's roster and by the declaration beside each row, because no resolver can prove that negative for an ungrounded receiver. ⛔ `N` is a per-FILE set and CANNOT serve as that roster. This sentence is written in the census's docblock AND, in one sentence, in each `free` row's `readersProof` (§7 row 2).

**THE DECOY TABLE — a frozen literal, one `why` per row, keyed by FILE and RECEIVER NAME and never by line.** MEASURED at `bfca6b7d6` with the census's own mechanism (the estate's parse through `buildIndex`, the estate's own `isWriteTarget`): **5 rows · 6 distinct SITES · 8 read occurrences**.

| # | file | receiver | occurrences | why this receiver is not a settlement record |
|---:|---|---|---:|---|
| 1 | `src/domain/worldPulse/partyImpact.js` | `action` | 3 | a pulse ACTION record's own annotation, built and consumed inside the party-impact fold |
| 2 | `src/domain/worldPulse/warReasons.js` | `demoTerms` | 1 | a demand-terms value object assembled for a war reason, never a record on the settlement |
| 3 | `src/generators/economy/viability.js` | `water` | 1 | the water-access verdict's own note, interpolated into a viability description |
| 4 | `src/generators/npcGenerator.js` | `override` | 2 | a CALLER-SUPPLIED override bag read before an npc exists; the write target is `enriched.stressNote` |
| 5 | `src/generators/structuralValidator.js` | `entry` | 1 | a validator ENTRY being normalized, not an institution or an npc |

⛔ **THE ROSTER IS NOT LINE-ADDRESSED, DELIBERATELY** (§P2.16): a table keyed by `path:line` would make every future line shift in `src/generators` a re-address this member owes on files it does not own. Keyed by file + receiver + count, a seventh site reds BY NAME when its receiver is new and by COUNT when it is not, which is strictly stronger and carries no address debt. The census asserts the row set, the SITE total (6) and the OCCURRENCE total (8), so every figure judgment 157d names is proved and none of them is an address. ⚠ **THE ROW COUNT IS FIVE AND THE SITE COUNT IS SIX** — the ruling's word "six" is the SITE figure, and `partyImpact.js` carries two sites on ONE receiver (`:67` and `:88`, `action.note`). This is §11 THE CHAIR'S FOUR, question 1.

**THE READ-SITE KINDS ARE THE SCANNER'S OWN FOUR, and they are not re-typed.** `scanSurfaceReads` visits property access, string element access, object binding patterns and `'k' in x`; destructuring and `in` sites are `N` ONLY, by construction, *"neither carries a receiver the resolver can ground"*. ARM 2's roster counts the two RECEIVER-BEARING kinds (property access and string element access) and excludes an assignment target through the estate's own `isWriteTarget`. **A fifth kind is a STOP, not an edit.**

**THE SPLIT PROBE, exactly (arm 3, case A4).** The census writes two files into `mkdtempSync(join(tmpdir(), …))`, never into the tree: `decoys.js` carrying the five declared receivers, and `real.js` carrying ONE read grounded through the declared root shape. It builds the SAME corpus with the SAME explicit `rootShapes`, indexes the two planted files, scans them, and asserts: `stats.rGrades > 0`; `reads.get('note on institutions').R` and `...('note on npcs').R` each hold EXACTLY the real file; and `N` holds BOTH. EXECUTED at `bfca6b7d6` by this compile:

```
stats: {"files":2,"sites":11,"propRead":11,"elemRead":0,"destructure":0,"inOp":0,"rGrades":4,"nGrades":20,"resolvedReceivers":4}
note on institutions   => {"R":["real.js"],"N":["decoys.js","real.js"]}
note on npcs           => {"R":["real.js"],"N":["decoys.js","real.js"]}
tier on institutions   => ABSENT (zero reads)
SPLIT CONTROL: PASS — R holds exactly the real file; N is polluted by the decoys; rGrades > 0
```

⭐ **This is A4's positive control, and it REPLACES the one version 7 named.** `role` on `npcs` *"which `getUpgradeOpportunities` reads"* was refuted at the version-8 pre-proof: that function's one `.role` site is `result.push({ ...role, category });`, a SPREAD of a local binding matching none of the four kinds (§11 BLOCK-G). A planted control cannot rot the way a live one does, and it is the only control that exercises the discrimination the whole arm rests on.

**THE VALUE-JOIN HALF is unchanged** (above): the rename cascade's four declared lists, read by live import.

⚠ **ANTI-VACUITY IS MANDATORY AND IS CASE A4.** In the same test: both cascade lists are non-empty; at least one declaration row is classified JOINING; `filesUnder(root)` is non-empty for EVERY one of the three roots; `stats.files` equals the inspected count; and the split probe's `rGrades > 0`. A scan that finds nothing because it walked nothing is the failure this instrument exists to refuse, and **A4 is not weakened by one word.**

### `readersProof` — SETTLED in shape AND in value

```js
/** @type {string} present iff kind === 'free'; non-empty; names the arm that proved it. */
```

The census asserts, for every `kind: 'free'` row: `readersProof` is a non-empty string, and it names an arm this file actually ran (case A5). ⭐ **VERSION 9 SETTLES THE VALUE**: one sentence, on ONE line, in the shared `ANNOTATION_PROOF` const, stating the census file, both arms and the REACH honestly. The value the build lane writes is spelled in §7 row 2 and its four byte constraints were run against the planned text with each walker's OWN counter (§7's register table).

### Determinism, flag, lifecycle, receipts

- Hash/fork key: `NONE`. No PRNG, no clock, no locale, no environment read.
- Stable enumeration: declaration order, then surface-list order. No sort.
- Flag: `NONE`. Golden posture: **UNCHANGED**.
- Lifecycle: the census persists nothing and is read by nothing. `n/a` for every column.
- Receipts and privacy: `NONE`; no DM-only field; nothing travels.
- Alignment: `DECLARED EMPTY: a census takes no alignment position.`
- Edit story: `ENGINE-ONLY: the census constrains the declaration table; it exposes no DM verb.`

## 7. Exact change manifest

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `tests/lint/flavorFields.census.test.js` | cases A1–A7 | `n/a` | The two-half census. ⭐ **SEVEN literal `test`s under ONE literal `describe`** — A1 (value-join), A2 (dormant/absent), A3 (guard-the-guard), A4 (anti-vacuity + the SPLIT PROBE), A5 (`readersProof`), A6 (the receiver-shape arm), A7 (the key-level roster) — which is the `titles +7 · suiteTitles +1` this member's lighting row states and the count the prover checks against §9. ⭐ **THE FILE MUST CALL `readdirSync` ITSELF** (it walks the three roots, one of them a FILE): `contractTestAntiVacuity`'s Rule 2 is FILE-GATED on `DERIV_RE = /readFileSync\|readdirSync\|globSync\|matchAll\|…\|Object\.(keys\|entries\|values)\(/` and returns `[]` for a DERIVING file, so a title carrying `every` (A5's and A7's do) is convicted ONLY if that call is absent. ⭐ **THE DECOY TABLE IS A FROZEN LITERAL KEYED BY FILE + RECEIVER + COUNT, NEVER BY LINE** (§6); the FREE KEYS it scans for are DERIVED from `FIELD_DECLARATIONS`, never re-typed. ⭐ **THE SPLIT PROBE WRITES ONLY INTO `mkdtempSync(join(tmpdir(), …))`** and never into the tree. **THE ENFORCER-DIRECTORY FIXTURE LAW, measured walker by walker at `16f0bc71c` (§10a):** ONE literal `describe`, straight-line literal `test` titles, every callback declaring ZERO parameters, openers imported unrenamed from `'vitest'`, and the words `it` / `test` / `describe` never re-bound as a variable or arrow parameter. Loop INSIDE a named test; collect then assert ONCE (`seedLoopTotality` convicts a bare `expect(` inside a `for (` header matching `/\bseeds?\b\|SEED/i`, and its allowlist is CLOSED to new files). ⛔ **NO `process.cwd()` AT MODULE SCOPE** — `moduleScopeCwdRatchet` freezes `POPULATION_CEILING['tests/lint']` at **17** and forbids raising it; the cured spelling is `const ROOT = join(dirname(fileURLToPath(import.meta.url)), '../..');` and `fs.readdirSync` is then unrestricted. Every `not.toContain` / `not.toMatch` / `not.toHaveProperty` carries `// anchored:` (lower-case, exact) on the SAME line or the ONE line immediately above — including any that appears in PROSE, because the anchor walker strips no comments and the new file's ceiling is 0. Every exhaustive set the census iterates is IMPORTED from its producer, never re-typed (`contractTestAntiVacuity` Rule 2), and no bare `if (!x) continue;` stands immediately before the only `expect(` (Rule 1a, which reads RAW source, fixtures included). |
| `MODIFY` | `src/domain/edit/fieldDeclarations.js` | the `readersProof` string value carried by the shared `ANNOTATION_PROOF` const | `1` effective line | ⭐⭐ **THE EXACT NEW VALUE, SPELLED HERE SO NO BUILD LANE INVENTS USER-FACING WORDS (§P2.19):** `'tests/lint/flavorFields.census.test.js: the value-join arm finds no cascade key for this field, and the receiver-shape arm grounds zero reads of this key on this card shape under src/generators, src/domain/worldPulse and src/domain/causalState.js, its key-level site roster holding only the declared unrelated receivers; the resolver grounds part of the corpus, so the claim is zero GROUNDED reads.'` — 398 characters, ONE line, and **ALL FOUR BYTE CONSTRAINTS EXECUTED AGAINST IT AT `bfca6b7d6`** with each walker's own counter: the voice walker's `charScanStringContents`, EXTRACTED FROM ITS SOURCE and run over the leaf as it stands and with the plan, reads `literals 150 · emDash 0 · exclamation 0` BOTH TIMES (`Δ emDash +0 · exclamation +0 · literals +0`); `ruinFilterRoster`'s live `READER_RE = /\\.institutions\\b/` does NOT match it; it carries no relative `from '…'` / `import '…'` specifier; and the file's line count is **145 before and 145 after**. ⛔ No `eslint` `max-len` rule is configured (measured), so the single long line is lawful. ⭐ **THE SHAPE, RE-MEASURED AT `bfca6b7d6`:** both `kind: 'free'` rows (`:97` `institution.note`, `:103` `npc.note`) read `readersProof: ANNOTATION_PROOF`, and `:60` declares that ONE module-level const. **The authorized edit is that ONE string literal** (and, if reworded, its `:55-59` docblock) — not two row literals. ⛔ Touch no `kind`, no `pool`, no `field`, no `group`, no `label`, ⛔ **no `provenance` and no `tier1`**: all seven keys are EM-A1's and EM-A1 is LANDED. ⛔ **THREE MEASURED CONSTRAINTS ON THE NEW STRING'S BYTES:** (1) NO em dash and NO exclamation point — `tests/copy/voiceMechanics.test.js` Tier 2 walks `src/data` + `src/domain`, this leaf measures `emDash=0 exclamation=0` over 160 literals today, it carries NO baseline row, and a per-file count NEVER RISES (§P2.19); (2) the text `.institutions` must not appear — `ruinFilterRoster.walker.test.js` discovers on RAW bytes, so a STRING would join its set (today the leaf spells `'institutions[].name'`, which has no leading dot and does not match `/\.institutions\b/`); (3) no `from '…'` / `import '…'` with a relative specifier, which `tests/domain/editOperations.test.js :: A6` (its `importsOf` roster walk) would read as an import edge through `commentsOnly`, a strip that KEEPS string text. ⭐ **KEEP THE EDIT LINE-COUNT-NEUTRAL, AND THE MEASUREMENT IS RE-TAKEN AT `bfca6b7d6`:** four live `path:line` citations name this leaf — `PACKET_MANIFEST.json:35579` (→ `fieldDeclarations.js:37`), `PACKET_MANIFEST.json:37251` (→ `:142-144`), `EM-C4a.md:134` (→ `:94` `:100` `:100-103` `:106` `:108` `:130-136` `:142-144`) and `EM-D0d.md:56` (→ `:37`). ⚠ Version 8 wrote `:37241` for the second; EM-C4a's cure moved the manifest by ten lines and it is **`:37251`** at this tip. Every cited address is TRUE now, and all of them sit ABOVE or BELOW the one line this packet rewrites without moving, so **nothing is re-addressed** — provided the `:55-59` docblock is left at its current line count. ⛔ Three LANDED `requiredSymbols` rows name this path — EM-A2b's `export const FIELD_DECLARATIONS`, EM-C4a's `export function declarationsFor` and `export function isEditableCard` — and the edit touches none of the three declarations. |
| `REGISTER` | `scripts/mutation-coverage-manifest.json` | `invariants['tests/lint/flavorFields.census.test.js']` | `≤ 12` | Add ONE row surgically beside its `tests/lint/` siblings. ⛔ Never re-serialise the manifest whole. ⭐⭐ **THE ROW'S `kind` IS RULED, NOT CHOSEN: `rationale`, with an inline `rationale` of AT LEAST 40 CHARACTERS** (judgment 157a — for a `tests/lint` census whose remover-proof IS the build's red-first; `mutation` would additionally owe a label in `scripts/mutation-sweep.sh`, a LABEL JOIN claimed exactly once, and a `MUTATED_FILES` row, and the packet ships none of the three). The rationale states what a remover would have to delete and what convicts them. ⛔ `"uncovered"` is UNAVAILABLE — the manifest is SHRINK-ONLY and its uncovered count must equal `uncoveredBaseline` exactly (**186** at this tip; a new uncovered row makes 187 and also refuses at placement under §P10.8). MEASURED at `bfca6b7d6`: `invariants` **723** rows, **182** under `tests/lint/`, `uncoveredBaseline` **186**, and the kind histogram is `rationale 445 · mutation 92 · uncovered 186` (445 + 92 + 186 = 723), so `rationale` is the estate's ordinary kind for this class by a factor of nearly five. |
| `DOC` | `docs/ARCH_EDIT_MODE_AND_DECREES.md` | §8 instrument 2's sentence | `1` sentence | ⭐ **JUDGMENT 157b ORDERS THIS CORRECTION IN THE SAME VERSION.** The live sentence (`:94`) reads *"every `free` field has zero readers under `src/generators`, `src/domain/worldPulse`, `src/domain/causalState`, `src/domain/edit/guards*`, and the registers, measured by PROPERTY READS (the writer-reach and observed-shape data) AND by VALUE JOINS…"*. Three clauses are refuted by measurement: `src/domain/causalState` is a FILE, `src/domain/edit/guards*` names a habitat that matches ZERO files under `src/`, and the two named registers cannot deliver the property-read half (§11 BLOCK-A). The corrected sentence names the THREE ruled roots with the third spelled as a file, drops the guard root, and replaces "the writer-reach and observed-shape data" with the receiver-shape scan over `scanSurfaceReads`, stating the reach (`zero GROUNDED reads`). ⛔ **NOTHING ELSE IN THE ARCH MOVES**, and the edit adds and removes no line above any cited address. §P2.18 is DISCHARGED BY MEASUREMENT: the estate's 2,795 `requiredSymbols` rows carry ZERO quotes of this sentence, and the live manifest holds ZERO non-terminal change rows on this path, so it is unreserved. |

Generated artifacts: `NONE`.

No other file may be edited.

> ⛔ **ROW 1 IS `CREATE`, NOT `TEST` (ESTATE-REPAIR-2, 2026-09-20).** `TEST` is the action for an
> EXISTING test file a member edits; a new one is `CREATE`. §4 above already reads *"Expected:
> `tests/lint/flavorFields.census.test.js` ABSENT"*, the file IS absent at `bdbf7c89c` (measured),
> and the JSON capsule has actioned it `CREATE` since the first repair — so §7 was the one place
> still saying `TEST`, in the Markdown a build lane codes from. `implementation-packets.mjs`
> exempts only `CREATE` from the row-existence arm. §7 and the capsule are now set-equal on all
> three rows.

**Predicted register moves, priced here rather than discovered at the terminal:**

| Register | Moves? | Predicted delta | Door |
|---|---|---|---|
| Lighting census | **YES — INTERIOR RED at this member's commit** | **DELTA ONLY: `files +1` · `parked +0` · `credited +1` · `titles +7` · `suiteTitles +1`.** ⭐ **THE `titles` TERM IS A NUMERAL IN VERSION 9 AND WAS THE SYMBOL `T` IN VERSION 8**, because the arm count was withheld while BLOCK-E was open; judgment 157d rules the arms, §6 spells SEVEN, §9 homes seven cases and §7 row 1 declares seven `test`s, so the count law now has a number to check rather than a placeholder — and the count prover is re-pointed to refuse a DISAGREEMENT rather than to refuse a numeral. ⭐ **THE DELTA IS DERIVED FROM THE WALKER'S OWN CODE, re-read at `bfca6b7d6`:** `TEST_FILES` walks `tests/` and filters `/\.test\.(js|jsx)$/`, so `.census.test.js` enters automatically; `test(…)` and `it(…)` count identically (`TEST_WORDS = ['it','test']`); only CREDITED files contribute titles; the ONE literal `describe` contributes `suiteTitles +1`; and `parked +0` holds only if the file obeys §7 row 1's fixture law — the build lane runs `parkReasonsFor` over the real file before `+1 credited` is trusted, because a park would make the delta `parked +1 · credited +0 · titles +0 · suiteTitles +0` instead. ⛔ **No absolute is stated here.** For the chair's terminal arithmetic only, the LIVE tuple at `16f0bc71c` (the EIGHTEENTH refreeze, `measuredAtSha e7d9fb7c5`) is `files 2683 · parked 359 · credited 2324 · titles 25713 · suiteTitles 6837`, all five asserted by EXACT equality; **what composes is the SUM OF THE DELTAS**. | Re-derived WHOLE at the train terminal (`EM-PREAMBLE.md` §P3.2) |
| | | ⛔ The cause is the ONE new TEST file, not a `src/` file — `EM-PREAMBLE.md` §P2.1's cause is refuted; see `EM-A1.md` §11 BLOCK-5. | |
| Mutation-coverage manifest | **YES** | `invariants` **+1** (live **723** at `16f0bc71c`, enumeration 723/723 in exact sync); `tests/lint/` rows **+1** (live **182**); ⛔ `uncoveredBaseline` **UNMOVED at 186**, and the two guard-the-guard floors are `toBeGreaterThanOrEqual`, so neither needs an edit | §7 row 3, in this member's own commit; row-keyed, so EM-B1b's and EM-B2a4's rows on the same path do not collide |
| Voice walker, Tier 2 (`tests/copy/voiceMechanics.test.js`) | **NO** | `emDash +0` · `exclamation +0` on `src/domain/edit/fieldDeclarations.js`, which is in Tier 2's `SCANNED_FILES` (`src/data` + `src/domain` walk) and carries NO row in `tests/copy/.voice-mechanics-baseline.json`, i.e. is held at ZERO. ⭐ **RE-MEASURED AT `bfca6b7d6` WITH THE WALKER'S OWN TOKENIZER** — `charScanStringContents`, EXTRACTED FROM THE WALKER'S SOURCE rather than re-typed, run over the leaf as it stands AND over the planned text: `literals 150 · emDash 0 · exclamation 0` both times. ⚠ Version 8's "160 literals" came from a naive regular expression, not the walker's tokenizer; the corrected figure is **150**, and both em-dash and exclamation counts were and remain ZERO. §7 row 2 carries the constraint and the exact string | none — a per-file count never rises (§P2.19) |
| ⭐ **The writer-reach walker's SCAN BUDGET pin** (`tests/lint/writerReach.walker.test.js`) | **NO** | `scansRun +0`. The pin is `expect(scansRun, 'a mutant suite that scans per plant costs minutes per gate').toBe(2)` over a MODULE-SCOPE `let scansRun = 0` that only that file's own `countedScan` increments. It is not exported, and a second test module importing `scanSurfaceReads` gets its own registry. **This is the one arm a naive re-use of the estate's scanner would have redded, and it is measured rather than assumed** | — |
| ⭐ **The governing set of the three imported modules** (§P10.11(i), judgment 110) | **NO ARM MOVES** | `git grep -l -F '<basename>' -- tests` run once per import target: `writer-reach-scan` → 1 file; `observed-shape-corpus` → 6; `check-observed-shape-readers` → 11; `legacy-reader-shape-scan` → 6; `observed-shape-baseline` → 4; `factionRename` → 12; `fieldDeclarations` → 7. **NOT ONE of them holds an importer roster, a dormancy arm or an enumerator keyed on TEST-FILE importers** — `scannerToolFiles()` and `detectorDigestOf` freeze the SCANNER SOURCES' bytes (which this member does not edit, and reading a governed file is not an edit), and `writerReach`'s `consumer` rows address `src/` files inside the register, never a test importer. This is the measurement EM-B1a version 8 did not make and was CUT from its train for | — |
| Entropy-root census, BOTH arms (`tests/lint/entropyRootCensus.walker.test.js`) | **NO** | read-site arm (`WORLD_ROOTS`) `+0` and `createPRNG(` SITE COUNT `+0`: the MODIFY changes one string literal's content and the leaf contains ZERO `createPRNG(` and no world-root read; the CREATE is under `tests/`, outside the walker's `src/` corpus (§P2.21(a), judgment 151) | — |
| Ruin-filter roster (`tests/lint/ruinFilterRoster.walker.test.js`) | **NO** | discovery set `+0` — **EXEMPT by measurement, not by claim**: the leaf's only `institutions` text is `'institutions[].name'`-shaped, which carries no leading dot and does not match the walker's `/\.institutions\b/`; the walker discovers on RAW bytes, so §7 row 2 forbids the new string from spelling `.institutions` (§P2.21(b)) | — |
| Goods roster, ST-2 (`src/domain/goods.schema.js` via `tests/build/vendorPdfLazy.test.js`) | **NO** | `+0` — the modified leaf imports NOTHING at runtime, and the census imports only `factionRename.js`, `fieldDeclarations.js` and node builtins. No goods identity half-table is reached (§P2.21(c)) | — |
| Tuning register / inventory | **NO** | `+0` — `NAMED_NUMERIC_CONST_RE` is NUMERIC-only, and `ANNOTATION_PROOF` is a string; its P3 bare-decimal pass reads `codeOnly`-stripped text, in which the literal is blanked (§P2.17) | — |
| Reviewed-set pin (`tests/application/commands/commandRegistry.test.js`) | **NO** | this member registers NO application command and makes NO store dispatch, so §P2.20 does not reach it — stated because the sixth amendment requires the answer either way | — |
| Prose-numerics · wiring census · line-addressed registers | **NO** | `+0` on each, RE-MEASURED at `bfca6b7d6`: `tests/lint/.prose-numerics-baseline.json` holds ZERO rows naming this leaf (`grep -c` → 0); `docs/content/wiring-census.json`'s `stamp.files` holds SEVEN entries, every one under `src/domain/display/stateProse/`, so `stamp.files` and `stamp.candidateLeaves` are UNMOVED, `stamp.producerIndexFiles` is the NUMBER **1181** and moves **+0** because this member CREATEs no `.js` under `src/domain/**` or `src/generators/**`, and `totals.*` are UNMOVED because no pool, variant or relation is produced. ⚠ Version 8 named a `scripts/.source-citation-baseline.json`; **no such file exists at this tip** (measured) — the citation instrument is `tests/lint/sourceCitationIntegrity.walker.test.js`, which reads live addresses, and §7 row 2 carries its four. The edit inserts and removes no line, so no line-addressed row moves (§P2.16) | — |
| Byte budgets — all four | **NO** | **+0 B, and the proof imports the set rather than reading a list (§P2.11).** `EAGER_FIRST_PAINT_MODULES` measured **270** modules at this tip by importing `vite.config.js`; `src/domain/edit/fieldDeclarations.js` is NOT a member, nor are its two importers. And the cluster is DARK: `fieldDeclarations.js` is imported only by `src/domain/edit/operations.js` and `src/store/editSlice.js`; `operations.js` is imported only by `editSlice.js`; and **`editSlice.js` has ZERO importers under `src/`** — so no entry point reaches any of them and no chunk contains them. Ceilings at the tip, for the record and NOT to be spent: `WORKER_BUNDLE_CEILING_BYTES = 1401208` (exact), `CLOSURE_BUDGET_BYTES = 1_048_000`, lazy engine `< 679_000`. **This member is NOT the train's byte-arm holder**, carries no budget TEST row, and states its predicted per-closure delta as `+0 B`; the holder is named by the chair at the placement | — |
| **Writer-reach register** (`scripts/.writer-reach-baseline.json`) | **NO** | zero — **and this is the row `EM-PREAMBLE.md` §P2.4 requires stated, so it is stated with its measurement.** The census READS the baseline as a JSON fixture from a file under `tests/lint/`; the register's own scan is rooted at `SURFACE_ROOTS` (`src/main.jsx`, the PDF/world-book/foundry/export entry points) and never walks `tests/`, so a test file cannot enter any closure or add any reader. And the census dereferences no SETTLEMENT record: it reads declaration rows and two frozen JSON artifacts. **Neither the `--write` shrink nor a mint is owed.** (F-11) | — |
| Observed-shape register (`EXPLAINED_WRITER_EXEMPTIONS`) | **NO** | zero | No save-time key (`dmLayer`, `decrees`) is read; EM-B3 owns that door. |
| Prose-numerics · size-baseline · edge-shared bundles | **NO** | zero | Nothing is rendered, baselined, or inside a bundle closure. |

## 8. Ordered coding sequence

0. Dispatch and seal; stop on any preflight mismatch. **Re-read `src/domain/edit/fieldDeclarations.js` at this step** — EM-A1 is the collision-group sibling that wrote it.
1. Capture the baseline evidence: the three roots' file counts, `sourceFiles(ROOT)`'s size and `MIN_ROWS`, recorded in the receipt, so a later drift is visible. ⛔ Neither register baseline is read by this member; record their `frozenAtSha` only as a drift witness.
2. Add the failing tests for A1–A7, SEVEN literal `test`s under ONE literal `describe`.
3. Implement the value-join arm (§6) — byte-unchanged from version 8's contract.
4. Implement the property-read arm's three arms (§6): the corpus with its EXPLICIT `rootShapes`, the full-tree index with the three-root inspection, arm 1's empty `R`, arm 2's frozen decoy table, arm 3's planted split probe with `rGrades > 0`.
5. Fill the `readersProof` value — the exact string in §7 row 2, byte for byte.
6. Add the one `scripts/mutation-coverage-manifest.json` row, `kind: "rationale"`, its reason at least 40 characters.
7. Correct the ARCH §8 instrument-2 sentence (§7 row 4), one sentence, line-count-neutral.
8. Run focused verification (§10).
9. Write the completion receipt. The bare full gate and the boot smoke belong to the train terminal.

Bounded algorithm (the value-join arm):

```text
1. Import the four declared lists from src/domain/factionRename.js. Assert both
   cascade lists are non-empty, or STOP: an empty denominator is a vacuous pass.
2. For each declaration row, build storedPathsOf(card, field) from the DECLARED
   homes, never from a hand-written probe map.
3. JOINS := any built path appears as a `kind: 'key'` path in either cascade list.
4. Apply §6's four-way verdict. On any RED, name the row AND the exact cascade path.
5. Assert at least one row was classified JOINING, or STOP (anti-vacuity).
```

## 9. Acceptance matrix

| ID | Case | Fixture/input | Required observation | Test home |
|---|---|---|---|---|
| A1 | Main behaviour — the VALUE-JOIN half | the live `FIELD_DECLARATIONS` and the four live surface lists | every joining row is `free-cascade`; no joining row is `free` or `pool`; no `free-cascade` row is join-less, and each RED names the row and the exact cascade path | `tests/lint/flavorFields.census.test.js` |
| A2 | Absent/dormant | a card type with no `free` row — **MEASURED at `bfca6b7d6`: THREE of the five qualify** (`faction`, `powerSeat`, `worldFact`), and TWO carry one (`institution.note`, `npc.note`) | both arms run, the scan's denominator is non-empty, and zero `free` rows are reported for those three without passing vacuously | `tests/lint/flavorFields.census.test.js` |
| A3 | Counterforce — GUARD-THE-GUARD | a planted `kind: 'free'` on a row whose path IS in `FACTION_RENAME_SURFACES` (e.g. `npcs[].name` via `NPC_RENAME_SURFACES`) | A1 REDS, and the message names both the row and the exact cascade path | `tests/lint/flavorFields.census.test.js` |
| A4 | Boundary — THE ANTI-VACUITY FENCE AND THE SPLIT PROBE (the positive control) | the live lists, plus TWO files planted under `mkdtempSync(join(tmpdir(), …))`: `decoys.js` carrying the five declared receivers and `real.js` carrying one read grounded through the declared root shape | both cascade lists non-empty; at least one row classified JOINING; a non-empty file set under EVERY one of the three roots; `stats.files` equal to the inspected count; and the probe discriminates — `stats.rGrades > 0`, `R` holding EXACTLY the grounded file for both identities, `N` polluted by both. ⭐ **THIS REPLACES THE REFUTED CONTROL** version 7 named (`role` on `npcs` via `getUpgradeOpportunities`, §11 BLOCK-G): a PLANTED control cannot rot, and it exercises the discrimination the whole arm rests on. EXECUTED by the version-9 compile at `bfca6b7d6` (§6) | `tests/lint/flavorFields.census.test.js` |
| A5 | Real integration — every `free` row carries a live proof | the live table — **MEASURED: the denominator is TWO**, and both rows read the ONE shared const `ANNOTATION_PROOF` | `readersProof` on every `kind: 'free'` row is a non-empty string naming an arm this file actually ran, and no `pool`, `free-cascade` or `share` row carries one. ⚠ **A5 IS A WRITTEN-CONTRACT ARM** (§P6): its assertion already holds at the base, because EM-A1 landed a non-blank placeholder value — so it CANNOT be red-first, and it is proved by a planted mutant that reds the named title, restored digest-exact, then green, with the counterforce quoted in the receipt | `tests/lint/flavorFields.census.test.js` |
| A6 | ⭐ THE RECEIVER-SHAPE ARM — zero GROUNDED reads | the live tree at the member's base: `foldCorpus` with an EXPLICIT `rootShapes: ['settlement']`, `buildIndex(sourceFiles(ROOT))` (2,250 files indexed), and `files` = the three roots' 559 files | `reads.get(writerIdentity(K, S))?.R` is EMPTY for every `free` row — MEASURED subject `note on institutions` and `note on npcs`. ⛔ The claim is ZERO GROUNDED READS and is stated as such: the resolver reaches part of the corpus by its own header, so arm 2 carries the rest | `tests/lint/flavorFields.census.test.js` |
| A7 | ⭐ THE KEY-LEVEL SITE ROSTER — the declared decoys and nothing else | the same three roots, scanned with the estate's parse through `buildIndex` and the estate's own `isWriteTarget` | the roster equals the frozen decoy table row for row — **MEASURED at `bfca6b7d6`: 5 rows (file + receiver) · 6 distinct SITES · 8 read occurrences** — and the SITE and OCCURRENCE totals are asserted beside it. A NEW receiver reds BY NAME; a NEW site on a declared receiver reds by that row's count. ⛔ The table is keyed by file + receiver + count and NEVER by line (§P2.16) | `tests/lint/flavorFields.census.test.js` |

Rows for idempotency, lifecycle round trip and privacy are **omitted, not replaced**: nothing is written, persisted or projected.

## 10. Verification commands

```sh
npx eslint tests/lint/flavorFields.census.test.js src/domain/edit/fieldDeclarations.js
npm run typecheck:ratchet
npm run typecheck:domain:strict

# THE ENFORCER-DIRECTORY BATTERY — one directory, six named FILES, never the directory itself
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/lint/flavorFields.census.test.js \
  tests/lint/mutationCoverageManifest.test.js tests/lint/negativeAssertionAnchor.walker.test.js \
  tests/lint/contractTestAntiVacuity.walker.test.js tests/lint/seedLoopTotality.walker.test.js \
  tests/lint/moduleScopeCwdRatchet.test.js --maxWorkers=2
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/domain/factionRename.test.js \
  tests/domain/editDeclarations.test.js --maxWorkers=2

# ⛔ NEITHER COMMAND BELOW IS A SEALED `checks` ROW, and that is measured (chair, 2026-09-20).
# A sealed `checks` array names FILES, never the `tests/lint` DIRECTORY: the excluded directory run
# costs three to nine minutes and belongs to the build lane as an INSTRUMENT; the lighting walker
# reds BY DESIGN here (this member CREATEs a test file and the census tuple is an exact equality),
# so sealing it bakes a red into `check:packet` (addendum 80).
#
# tests/lint WHOLE, EXCLUDED — owed because this member CREATEs a file under tests/, which opts it
# into every walker governing that directory (the chair's directory-whole addendum of 2026-09-20).
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --maxWorkers=2 \
  tests/lint --exclude=tests/lint/sovereigntyLightingContract.walker.test.js

# The named interior red, run ALONE so its figure is RECORDED — never to refreeze it
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run tests/lint/sovereigntyLightingContract.walker.test.js --maxWorkers=2

node scripts/implementation-packets.mjs validate
```

⛔ **`tests/lint/writerReach.walker.test.js` IS NOT A SEALED CHECK, AND THAT IS A MEASUREMENT, NOT
A CONVENIENCE.** It is the one existing caller of `scanSurfaceReads`, so §P7's computed rule names
it as a candidate; but its `beforeAll` runs `buildObservedCorpus()`, MEASURED at **251,002 ms**, and
the only arm a second caller could plausibly move — its `scansRun` budget pin — is a MODULE-SCOPE
binding of that file alone (§5, §7). Sealing a four-minute run that guards nothing this member
touches would make every `check:packet` pay for it. It IS run ONCE by the build lane inside the
`tests/lint` WHOLE (excluded) instrument above, which is where its cost is already budgeted.

**⭐ THE PRE-SEAL INSTRUMENT, run from the KIT path and never from the tree (§P9's count-prover law):**

```sh
node <kit>/packets-waiting/EM-A3.count-prover.mjs <kit>/packets-waiting/EM-A3.md \
                                                 <kit>/packets-waiting/EM-A3.manifest.json
```

It reads the packet's Markdown and its capsule and no test runner, prints one line per file
(file · arms the row declares · cases the matrix homes there · titles delta) and exits non-zero on
any inequality. It is run by the pre-proof, by the chair's placement and by the build lane BEFORE
its seal; it is NEVER placed in the tree, never a `checks` entry and never a §7 row. ⭐ **VERSION 9
RE-POINTS IT AND HARDENS IT**: the `titles` term is now the NUMERAL 7 rather than the withheld
symbol `T`, and the prover checks that the numeral EQUALS the matrix size, the §3 budget row and
the arm count §7 row 1 declares — three independent statements of one number, any disagreement a
non-zero exit. Its `--self-test` failing controls were executed (§10b).

**⭐ §10a — THE ENFORCER-DIRECTORY INSTRUMENTS, named because this member CREATEs under `tests/lint`.**
Every walker below was READ WHOLE and its population predicate measured; each admits the new file
AUTOMATICALLY, with no roster to join:
`negativeAssertionAnchor.walker.test.js` (`SCAN_ROOTS=['tests']`; `ANNOTATION_RE = /\/\/\s*anchored:/`
on the assertion's own line or the ONE line above; a file absent from the record is held at 0) ·
`contractTestAntiVacuity.walker.test.js` (in scope via `/^tests\/lint\/[^/]*\.test\.(js|jsx)$/`; all
five rules; three EMPTY exemption maps; ⭐ **Rule 2 returns `[]` for a file whose skeleton matches
`DERIV_RE`, which this census does because it calls `readdirSync` itself** — §7 row 1) ·
`seedLoopTotality.walker.test.js` (`SCAN_ROOTS=['tests']`; allowlist closed; collect then assert
once) · `moduleScopeCwdRatchet.test.js` (`GUARDED_DIRS=['tests/build','tests/lint']`; ceiling 17,
never raised; the cured spelling is `join(dirname(fileURLToPath(import.meta.url)), '../..')`) ·
`mutationCoverageManifest.test.js` + `mutationCoverage.shared.mjs` (`ENFORCER_DIRS` is EIGHT dirs
with `tests/lint` FIRST, RE-READ at this tip and never recalled; TOTALITY reds a file with no row) ·
`sovereigntyLightingContract.walker.test.js` (`TEST_FILES` walks `tests/`; five exact equalities).
Their rules are §7 row 1's coding instruction. The `tests/lint` WHOLE run, EXCLUDING the lighting
walker, and the lighting walker run ALONE are the build lane's two instruments (§P7) — neither is a
sealed `checks` row, and that is measured rather than assumed.

**⭐ §10b — WHAT THE VERSION-9 COMPILE EXECUTED, so the build lane does not re-derive it.** Each of
these ran in plain `node` against `bfca6b7d6`, with the tree read from an environment variable and
printed, and each is reproducible from `$SP/lane-compile-EM-A3-v9-scratch/`:
`probe-split.mjs` (the ruled mechanism end to end; exit 0) · `probe-split-norootshapes.mjs` (the
NEGATIVE control, `rGrades 0`; exit 1, as it must) · `probe-roster.mjs` (arm 2's roster; 5 rows / 6
sites / 8 occurrences) · `probe-voice.mjs` (the voice walker's OWN tokenizer over the leaf and over
the planned string; exit 0 on all four byte constraints).

⛔ **`node scripts/check-writer-reach.mjs` and `node scripts/check-observed-shape-readers.mjs` are NOT in this list, deliberately.** This packet moves neither register (§7) and re-running a ratchet it does not touch invites a stale-artifact red that belongs to another lane. If the chair wants a control, the read-only `--report` form is the one, never `--write`.

Expected: every command exits `0` except `sovereigntyLightingContract.walker.test.js`, the ONE named interior red at §7's figures. A gate line with no printed test count DID NOT RUN.

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and `EM-PREAMBLE.md` §P8, stop if: either register baseline's `frozenAtSha` differs from the value recorded at step 1; any arm would pass over an empty denominator; the cure for a red would be a baseline re-freeze; or a `free` row would be admitted with a `readersProof` no arm produced.

---

### ⭐ THE BLOCKS — ALL CLOSED AT VERSION 9, each keeping its measurement verbatim

⛔ **NOT ONE BLOCK IS OPEN.** Version 8 handed up four (E · F · G · H) and judgment 157 a–d ruled
every one of them; BLOCK-H was cured in the tree by another lane. Each block below keeps its
ORIGINAL measurement word for word beneath a re-stated header, because a record is annotated and
never rewritten (§P3.5), and because a later lane that "simplifies" this census would otherwise
re-derive the same holes from scratch. What is still the chair's is four SPELLINGS, listed above
at THE CHAIR'S FOUR, and none of them gates the build.

**BLOCK-A — ⭐ CURED BY THE CHAIR'S RULING §934.47 ITEM 6, and the measurement that forced it is kept.**

The ruling replaces the baseline read with a LIVE SCAN this census owns (§6), which is exactly option (b) of the three this lane named. **The block is lifted and the packet is DRAFT.** The measurement stays on the record because it is why the cure was needed, and because a later lane that "simplifies" the census back to a baseline read would reintroduce the hole:

**Why the two named instruments could not do it —**

ARCH §8 instrument 2, restated by design §6.2 and the ODQ §934.36 addendum:

> every `free` field has zero readers under **`src/generators`**, `src/domain/worldPulse`, `src/domain/causalState`, `src/domain/edit/guards*`, and the registers, **measured by PROPERTY READS (the writer-reach and observed-shape data)**

Measured at `d31af2cee`:

1. **`src/generators/` is the writer-reach register's closure STOP set** — the first and most load-bearing of the five named scopes is the one directory the instrument halts at by construction.
   ```
   $ node -e '…' scripts/.writer-reach-baseline.json
   writer-reach stopSet: ["src/generators/","src/store/","src/workers/","src/lib/instantWorld/"]
   ```
   and the scanner's own header (`writer-reach-scan.mjs:107-118`): *"THE STOP SET — the engine boundary every closure halts at. Load-bearing at the FILE level (Car 0 control 3: **the same read planted at a `src/generators/` path lights nothing**)."* **No read under `src/generators/` can ever appear in this baseline**, so "zero readers under `src/generators`" is not a claim the baseline can support in either direction. (F-2, F-3)
2. **The baseline carries no per-file reader map at all.** Its 23 top-level keys hold `surfaceReach` — a per-surface-CLASS grade string per identity — and `darkUnregistered`. `judgeWriters` computes a `readers: {R, N}` file set per identity in memory (`writer-reach-scan.mjs:479`), and **it is not persisted**. A census reading the artifact can ask "which surface classes reach this key", never "which files read it". (F-2, F-4)
3. **The observed-shape baseline is the wrong population by construction.** `_doc[0]`: *"READER-WITH-NO-WRITER INVENTORY."* A declared editable field is, by definition, WRITTEN by the generator — so it cannot appear. Measured: across all 387 inventoried files, **exactly one** row touches the three card shapes at all (`src/pdf/lib/viewModel.js :: faction on npcs = 3`). (F-5, F-6)

So the premise *"measured by property reads (the writer-reach and observed-shape data)"* is refuted by those two artifacts' own scope and shape. **The cure is a ruling, not a lane's pick**, and the options are visible:

- (a) respell the reader scope in the grammar the register does carry — `web-transitive` is documented as *"reachable only through files the web root drags in, never through a component, a display read model or the PDF"* and is the register's one non-display signal — accepting that `src/generators/` and `src/store/` fall outside every closure and are therefore **unmeasured, not proven clean**; or
- (b) authorize the census to take a LIVE measurement by calling the scanner library directly — `scanSurfaceReads({ index, corpus, files, … })` (`writer-reach-scan.mjs:356`) returns a per-file `{R, N}` map and accepts an arbitrary `files` list, so a declared scope of `src/generators/**` + `src/domain/worldPulse/**` + `src/domain/causalState/**` + `src/domain/edit/guards*` **is** answerable — at the cost of building the observed corpus inside a lint test, which is a new mechanism and a new runtime cost neither the charter nor the preamble priced; or
- (c) narrow instrument 2 to the VALUE-JOIN half, which is fully executable today (§6), and re-aim the property-read half to a member that owns the scanner.

**Judgment calls: NONE.** The chair adjudicates.

**⛔ BLOCK-E — ⭐⭐ RULED AND CLOSED BY JUDGMENT 157d (2026-09-22, 14:15 EDT, on the receiver-shape
recon's report). The cure is E-1, and §6 now carries it whole.** The measurement stays on the
record because it is why the predicate was re-spelled, and because a later lane that "simplifies"
the census back to a key-only scan would reintroduce the hole. The version-8 finding, unedited:
§6's property-read predicate WAS RECEIVER-BLIND and convicted BOTH of its own `free` rows on its
first run.

Both `kind: 'free'` rows carry the same key — `note`. §6's verdict is *"any of the four kinds
matches ITS KEY anywhere under `SCAN_ROOTS`"*, with no receiver test. Measured at `16f0bc71c`:

```
$ git grep -n -P '(\.note\b|\[.note.\]|\{[^}]*\bnote\b[^}]*\}\s*=|.note.\s+in\s)' \
    -- src/generators src/domain/worldPulse src/domain/causalState.js
src/generators/economy/viability.js:267     ... ${water.note}`
src/generators/npcGenerator.js:1301         if (override.note) enriched.stressNote = override.note;
src/generators/structuralValidator.js:485   note:           entry.note || null,
src/domain/worldPulse/partyImpact.js:67     ...(action.note ? [action.note] : []),
src/domain/worldPulse/partyImpact.js:88     summary: action.note || fields.summary || '',
src/domain/worldPulse/warReasons.js:871     note: demoTerms.note,
```

SIX live CODE sites, SEVEN read occurrences, on `water` · `override` · `entry` · `action` · `action`
· `demoTerms` — not one of them an institution or an npc record. ⚠ **THE SAME GREP UNDER `-E`
RETURNS ZERO** (`git grep -E` silently drops `\b`), which is the false zero the estate's GREP LAW
exists to catch; both engines were run.

**Why this is a ruling and not an edit.** The estate's own scanner keys an identity by **(key,
SHAPE)**, never by key alone — `writerIdentity(key, shape)` returns `` `${key} on ${shape}` ``
(`scripts/lib/writer-reach-scan.mjs:182`), and §6 itself cites `scanSurfaceReads` as the source of
its four kinds while dropping the pairing that makes them mean anything. The three cures, with what
each costs, measured:

- **(E-1) PAIR THE KEY WITH A SHAPE, using the estate's own exported reader.** `scanSurfaceReads({ index, corpus, files, root, keysToShapes, … })` (`:356`) is exported, accepts an arbitrary `files` list, and returns per-identity `{ R, N }` — `R` = receiver GROUNDED to that shape. Two tests already call it with custom file lists (`tests/lint/writerReach.walker.test.js:1000-1010`, `tests/lint/readerShapeResolver.test.js:78`). An executed probe over this exact problem separated the decoys from real reads: `note on institutions => {R:["real.js"], N:["decoys.js","real.js"]}`. ⚠ **TWO COSTS, BOTH MEASURED:** the real corpus (`buildObservedCorpus()`) takes **251,002 ms** — the cheap path is `foldCorpus(roots)` over plain in-memory objects, which an existing test already uses; and `corpus.rootShapes` is LOAD-BEARING while `foldCorpus` does not emit it, so omitting it yields `rGrades: 0` — **a silent all-clear**, which is the exact failure mode this census exists to refuse.
- **(E-2) KEEP THE KEY-ONLY SCAN AND CARRY A WRITTEN EXEMPTION LEDGER** of the six sites with a `why` each, in `factionRename.js`'s `NON_CASCADED_SURFACES` idiom. Cheapest; but it is a roster that ages, and it says nothing about a SEVENTH site a future lane adds on a seventh receiver.
- **(E-3) NARROW INSTRUMENT 2 TO THE VALUE-JOIN HALF** (BLOCK-A's option (c)), which is fully executable and non-vacuous today, and re-aim the property-read half at a member that owns a resolver.

⛔ **A SOUNDNESS CAVEAT THE CHAIR MUST SEE BEFORE CHOOSING (E-1):** the scanner's own header records that its legacy resolver *reaches 7.67 % of reads*, which is why the `N` grade exists at all. A ZERO claim resting on an empty `R` is therefore **not sound** unless the census ALSO shows the key has no ungrounded read — and `N` is polluted by exactly the six decoys above. So the charter's words, *"zero derivation readers … measured by property reads"*, are not carried by either grade alone. **Judgment calls: NONE. The chair adjudicates**, and this lane does not narrow an instrument by itself.

**⛔ BLOCK-F — ⭐ RULED AND CLOSED BY JUDGMENT 157b.** The corrected scope this lane RECOMMENDED is
the scope the chair RATIFIED, and §6 adopts it: `['src/generators', 'src/domain/worldPulse',
'src/domain/causalState.js']` with the walk accepting a FILE root, the `src/domain/edit` guard root
DROPPED with its absence written down, and the ARCH's §8 sentence corrected in this same version
(§7 row 4). The measurement that forced it, unedited:

1. `src/domain/causalState` is **NOT A DIRECTORY**. `git ls-files` shows the LEAF
   `src/domain/causalState.js` (plus three test files). A `walk()` over the spelled root yields
   nothing, and A4 asserts a non-empty file set under EVERY root. The ARCH §8 instrument-2 sentence
   spells it the same way, so the ARCH's wording names a leaf as a root.
2. `GUARD_FILE = /(^|\/)guard[A-Za-z]*\.js$/` matches **ZERO files** — not under `src/domain/edit`
   (whose eight leaves are `dmLayer` · `fieldDeclarations` · `operations` · `operationsNpcRename` ·
   `pools` · `recordRegister` · `types` · `worldConditions`), and not anywhere under `src/`. The
   ARCH's `src/domain/edit/guards*` names a habitat that does not exist at this tip.

The two roots that DO hold: `src/generators` (114 `.js`) and `src/domain/worldPulse` (444 `.js`).
**The chair rules the corrected scope** — this lane recommends the roots become
`['src/generators', 'src/domain/worldPulse', 'src/domain/causalState.js']` with the walk accepting a
FILE root, and the `src/domain/edit` guard root DROPPED with its absence written down rather than
walked as an empty set. Neither correction is made here.

**⛔ BLOCK-G — ⭐ CLOSED: THE CONTROL IS REPLACED, NOT REPAIRED.** Judgment 157d's third arm makes
A4's positive control a PLANTED SPLIT PROBE (§6), which cannot rot the way a live control does, and
the `getUpgradeOpportunities` `requiredSymbols` row that existed only to name the refuted control is
removed in version 9 with its reason written here rather than dropped silently. The refutation,
unedited:

§6 and A4 name *"`role` on `npcs`, which `getUpgradeOpportunities` reads"*. Measured: the whole of
`src/generators/economy/upgradeOpportunities.js` carries ONE `.role` site —
`:55  result.push({ ...role, category });` — a SPREAD of a local binding, not a property read of an
npc record, so none of the four kinds matches it. `.role` has ZERO sites under
`src/domain/worldPulse` and 10 files under `src/generators`. A4 is not weakened; its control is
re-chosen by measurement once BLOCK-E is ruled.

**⛔ BLOCK-H — ⭐⭐ CURED IN THE TREE, MEASURED, NOT PREDICTED (judgment 157c).** The cure is
`17bf34dd1` (*"EM-C4a (cure, version 4.3): the two landed dormancy arms widened"*), and
`git merge-base --is-ancestor 17bf34dd1 HEAD` returns TRUE at `bfca6b7d6` — so the arm this
member's sealed `checks[1]` runs is GREEN at its own base. Read at the cured tip, both arms were
widened BY ADDITION and never by deletion: `EXPECTED_IMPORTERS` gains
`'src/store/editSlice.js imports src/domain/edit/fieldDeclarations.js'` and the second-order
expectation gains `'src/store/editSlice.js imports src/domain/edit/operations.js'`, each carrying
the sanctioned-lazy-edge sentence. **EM-A3 touches that file nowhere**, as it must. The finding,
unedited:

EM-A1's DORMANCY arm asserts its importer roster SET-EQUAL in both directions. EM-C4a has since
LANDED `src/store/editSlice.js`, whose lines 29-31 import `dmLayer.js`, `fieldDeclarations.js` and
`operations.js`. Replicated with the estate's OWN `commentsOnly` helper and the test's verbatim
`walkSources` / `dormancyImporters` (scanned population 2,263 files):

```
ACTUAL  : ["src/domain/edit/operations.js imports src/domain/edit/fieldDeclarations.js",
           "src/store/editSlice.js imports src/domain/edit/fieldDeclarations.js"]
EXPECTED: ["src/domain/edit/operations.js imports src/domain/edit/fieldDeclarations.js"]
ASSERTION 1 => RED
ACTUAL  : ["src/store/editSlice.js imports src/domain/edit/operations.js"]   (expected [])
ASSERTION 2 => RED
```

An independent refuter attacked seven avenues — a `src/store` exclusion, a comment strip that eats
code, a commented-out import, a `.skip`/`skipIf`, a vitest `exclude` or alias, a second
`EXPECTED_IMPORTERS` definition, and file drift — and every one closed: **NOT REFUTED**.
⭐ **THE UNDERLYING CLAIM STILL HOLDS, so the cure is one line, not a redesign:** the cluster is
still DARK — `editSlice.js` has ZERO importers under `src/` — so no chunk contains any of these
modules and EM-A1's `+0 B` price is still true. What aged is the ROSTER, not the dormancy.
**This is EM-C4a's landing and EM-A1's arm; both are LANDED, so the cure is the chair's at the
train terminal** (widen `EXPECTED_IMPORTERS` by the one entry and `B1A_LEAVES`'s expectation by the
one entry, each with the sanctioned-edge sentence the arm already carries). EM-A3 may not touch
`tests/domain/editDeclarations.test.js`: it is outside its §7 and is EM-B1a's and EM-C4a's
territory. ⛔ **Until it is cured, `check:packet` for EM-A3 can never exit 0**, because
`checks[1]` runs that file.

**BLOCK-B — DISCHARGED at the EM-T13 pre-proof.**

Version 7 read: *"`EM-A3` MODIFIES `src/domain/edit/fieldDeclarations.js`, which `EM-A1` CREATEs.
That file is ABSENT at the base, and EM-A1 is itself BLOCKED."* **Both clauses are now false by
measurement.** EM-A1 LANDED in train EM-T9; the file exists at `16f0bc71c` at 144 raw / 47 effective
lines with all seventeen rows; and `LANDED` is a TERMINAL status that reserves no change path, so
the collision is discharged and the ordering dependency is satisfied. The block is kept, struck
through, because a later lane that re-reads the charter's `A1` collision column needs to find the
discharge rather than re-derive it.

**BLOCK-D (CONDITIONAL, and its condition is EM-A1's ruling) — under the FIRST amendment's root test this census's `free` population was EMPTY; under §934.45's it may not be.**

⚠ **Read this block's condition before its arithmetic.** It was computed under §934.44's *"writer reads no other world fact"*, which §934.45 replaced with *"root means CHOSEN, not computed"*. Under the new test six of EM-A1's nine drops reverse, so a larger catalogue — possibly including `note`-style annotation fields, which design §14 FINAL now names as a kind of their own (*"**Annotation** — the free notes; the DM's layer only; the flavor census proves nothing reads them"*) — may carry real `free` rows. **§14 FINAL in fact hands this census a subject it did not have before: the annotation kind is defined by this instrument's own proof.** Whether that subject exists at wave 1 depends on EM-A1's §11 BLOCK-0, not on anything here. The arithmetic below is retained because it is still the correct reading of the case where EM-A1 lands a cascade-only catalogue.

⭐⭐ **THE CONDITION IS NOW ANSWERED, AND THE DENOMINATOR IS TWO (ESTATE-REPAIR-4b, 2026-09-20 —
the chair's ruling 3 and judgment 33, landed in `EM-A1.md` version 5's BLOCK-6, which this closes).**
MEASURED, not inherited. Two facts compose it: (1) §6.2's shape law makes `kind: 'free'` hold **iff**
`provenance: 'annotation'` (`EM-A1.md` §6.2's absence table — a root row is *"⛔ never `free`"* and a
world-fact row is *"exactly `'pool'`"*); and (2) ARCH §9's annotation set is **enumerated, not
assumed** — `note free` appears **twice and only twice**, both on ARCH line 104 (`EM-A1.md` W-6) —
so the set is exactly **`institution.note`** and **`npc.note`**, and faction, power seat and the
world-fact card carry none. EM-A1 version 5 therefore declares seventeen fields across five cards,
**two of them `annotation`**. ⇒ **this census's `free` denominator is 2, not 0.**
⭐ **A2 and A5 are both NON-VACUOUS on it:** three of the five cards carry no `free` row (A2's
subject) and two do (A5's), so neither arm passes over an empty set. ⛔ **The pre-proof RE-MEASURES
the denominator against the table EM-A1 actually lands** — this block states a measurement and never
a numeral to inherit — and **A4's anti-vacuity fence is not weakened by one word**: it still refuses
a census that walked nothing. ⚠ **BLOCK-C is NOT closed by this**: that the two `note` fields exist
as declarations says nothing about how a NEW, generator-unwritten field earns a `readersProof`,
which is still the third thing the chair must settle.

**The arithmetic, under the superseded test (HISTORY — the case this block was written for):**

`EM-A1.md` §5.1 measures the writer of every field the amendment names. **Nine of ten drop as DERIVED or as no-such-field; one survives:**

```
npc.name — kind: 'free-cascade', provenance: 'root',
           writer: 'src/generators/npcGenerator.js#pickFirst'
```

`npcs[].name` is `NPC_RENAME_SURFACES[0]` (F-7), so §12.3 makes it `free-cascade`, never `free`. A one-field table therefore holds **zero `kind: 'free'` rows**, and this packet's whole subject — *"every `free` field has zero derivation readers"* — is vacuously true over an empty set. Case **A4**, the anti-vacuity fence, exists precisely to refuse that, and it would red on its own contract.

⭐ **That is not a reason to weaken A4.** It is the signal that EM-A3's scheduling follows EM-A1's ruling: if the chair rules §14's ROOT test as the packet measured it, there is nothing for a flavor census to classify in wave 1 and this packet should be re-aimed to the first wave that declares a `free` field. If the chair instead reads §14 as "the VALUE is authored, not computed" (`EM-A1.md` §11 BLOCK-0.2), the catalogue survives, `note`-style free fields may return, and BLOCK-A's measurement question becomes live again. **The two packets are one ruling, not two.**

⚠ Note what does NOT rescue it: the value-join half (§6) stays fully executable and non-vacuous even with one row — it would classify `npc.name` as JOINING and assert it is `free-cascade`. A packet could be cut on that half alone. Whether a one-arm census is still instrument 2 is the chair's call; this lane does not narrow an instrument by itself.

**BLOCK-C — a corollary the chair should see before ruling: on the register's own evidence, NONE of the candidate fields is display-only.**

Measured from `surfaceReach` (F-4) — `R` means a grounded read by a file in that closure:

| identity | reach | display-only? |
|---|---|---|
| `name on institutions` | `campaign-pdf=R dossier-pdf=R foundry=R json-export=N news=N web-display=R web-transitive=R world-book=R` | **NO** — `web-transitive=R` |
| `name on npcs` | `campaign-pdf=R dossier-pdf=R foundry=R json-export=A news=R web-display=R web-transitive=R world-book=R` | **NO** |
| `name on factions` | `campaign-pdf=R dossier-pdf=R foundry=R json-export=N news=R web-display=R web-transitive=R world-book=R` | **NO** |
| `faction on factions` | `campaign-pdf=R dossier-pdf=R foundry=R news=R web-display=R web-transitive=R world-book=R` | **NO** |
| `category on institutions` | `campaign-pdf=R dossier-pdf=R foundry=R json-export=N web-display=R web-transitive=R world-book=R` | **NO** |
| `category on factions` | `campaign-pdf=R dossier-pdf=R foundry=R json-export=N web-display=R web-transitive=R world-book=R` | **NO** |
| `role on npcs` | `campaign-pdf=R dossier-pdf=R foundry=R json-export=A news=N web-display=R web-transitive=R world-book=R` | **NO** |
| `power on factions` | `campaign-pdf=R dossier-pdf=R foundry=R web-display=R web-transitive=R world-book=R` | **NO** |

None of the eight is in `darkUnregistered` either. **This is consistent with the design** — names are join keys, so `free-cascade` is their kind — but it also means **the wave declares no plain-`free` field that the register could prove clean.** The only fields ARCH §9 declares `free` are the two `note` fields, and those **do not exist on any record** (`EM-A1.md` §11 BLOCK-2), so they carry no `surfaceReach` row at all. A census that reads "no row in the register" as "zero readers" is asserting absence of evidence as evidence of absence — precisely the self-referential-pin class `EM-PREAMBLE.md` §P6 forbids and that `factionRename.test.js`'s own header records as the R-5 defect. **How a NEW, generator-unwritten field earns a `readersProof` is unruled**, and it is the third thing the chair must settle before this packet can be READY.

### ⭐⭐ THE CHAIR'S FOUR — what version 9 hands up, each with a recommendation and its measurement

None of these gates the build; each is a spelling the chair may prefer otherwise, and each is
vetoable. This lane decided none of them by itself.

**Q1 — THE ROSTER'S KEY, AND WHY ITS ROW COUNT IS FIVE WHERE THE RULING SAYS SIX.** Judgment 157d
names "the six declared decoys by file + receiver name" and then enumerates FIVE `·`-separated
items, because `partyImpact.js` carries TWO SITES on ONE receiver (`:67` and `:88`, both
`action.note`). Measured with the census's own mechanism at `bfca6b7d6`: **5 rows by (file,
receiver) · 6 distinct SITES · 8 read occurrences.** A six-row table can only be reached by keying
on LINE, which makes the roster a line-addressed register (§P2.16) and puts every future line shift
in `src/generators` on this member's bill, on files it does not own.
**RECOMMENDED:** key the frozen table by **file + receiver + occurrence count** (five rows), and
have the census assert the row set, the SITE total (6) and the OCCURRENCE total (8) beside it.
A seventh site then reds BY NAME when its receiver is new and by COUNT when it is not — strictly
stronger than the six-row form — and every figure the ruling names is proved. **VETO:** six rows
keyed by `path:line`, with the re-address obligation accepted as this member's standing §7 row.

**Q2 — THE `requiredSymbols` DELTA: 12 → 19, and the one REMOVAL.** The new mechanism CALLS eight
symbols that must be found unchanged, and all eight resolve verbatim at count 1 at this tip:
`scanSurfaceReads`, `keysToShapesOf`, `writerIdentity`, `isWriteTarget`, `export { buildIndex,
MIN_ROWS }` (the `:621` re-export, which is why the census imports ONE module for the whole
mechanism), `foldCorpus`, `sourceFiles` and `FIELD_DECLARATIONS`. One row is REMOVED:
`src/generators/economy/upgradeOpportunities.js :: export const getUpgradeOpportunities`, whose
version-8 `_requiredSymbolsNote` retained it "until the chair re-chooses the control by
measurement" — which judgment 157d has now done, by replacing the live control with the planted
probe. **RECOMMENDED:** add the eight, remove that one, and KEEP the six baseline-era rows
(`BASELINE_PATH`, `SURFACE_CLOSURE_STOP`, `WEB_DISPLAY_DIRS`, `parseReach`,
`EXPLAINED_WRITER_EXEMPTIONS`, `generateNPCs`) — they are the BLOCK-A record, they cost nothing,
and removing them erases why the live scan was ordered. **VETO:** keep `getUpgradeOpportunities`
too, at 20 rows, with its note re-pointed to say the control is now planted.

**Q3 — DOES THIS MEMBER CARRY THE ARCH §8 CORRECTION, OR DOES THE TERMINAL?** Judgment 157b says
"the ARCH's §8 sentence corrected in the same version", and version 9 carries it as §7 row 4, a
`DOC` action on `docs/ARCH_EDIT_MODE_AND_DECREES.md`. Measured: the path is held by ZERO
non-terminal packets in the live manifest (so it is unreserved), the estate's 2,795
`requiredSymbols` rows carry ZERO quotes of the sentence (so §P2.18 is discharged), and `DOC` is a
valid `PACKET_ACTIONS` member whose target must EXIST, which it does. It raises the handwritten
file count to FOUR against a cap of twelve and the logic-bearing count not at all.
**RECOMMENDED:** carry it here — the member that changes the instrument is the member that should
correct the sentence describing it, and no sibling contends for the path. **VETO:** drop §7 row 4,
return to three paths, and slot the ARCH sentence to the train's terminal as a chair act.

**Q4 — IS `tests/lint/writerReach.walker.test.js` A SEALED CHECK?** §P7's computed rule names every
walker governing a module whose call shape the member changes, and that file is the only other
caller of `scanSurfaceReads`. Measured against it: this member changes NO call shape (it is a new
CALLER), the file's `scansRun` budget pin is a module-scope binding it cannot reach, its
`beforeAll` costs **251,002 ms**, and the governing-set grep over all three imported modules found
no importer roster, dormancy arm or enumerator keyed on test-file importers.
**RECOMMENDED:** NOT sealed; run ONCE inside the build lane's `tests/lint` WHOLE (excluded)
instrument, where its cost is already budgeted, and quote its exit in the receipt.
**VETO:** seal it as `checks[2]`, accepting roughly four minutes on every `check:packet`.

### PATH-DISJOINTNESS INSIDE TRAIN EM-T13, measured from the siblings' own capsules (§P10.1)

| member | its §7 paths | intersection with EM-A3 |
|---|---|---|
| **EM-D0e v1.1** | `src/components/edit/CardEditorDialog.jsx` (C) · `src/copy/en.js` (M) · `tests/components/primitives/editorHalo.test.jsx` (T) · `tests/components/cardEditorDialog.test.jsx` (C) | **NONE** |
| **EM-B1b v5/6** | `src/domain/edit/operationsOffStage.js` (C) · `src/domain/edit/operations.js` (M) · `tests/domain/editOperations.test.js` (T) · `tests/lint/opGuardCoverage.walker.test.js` (C) · `scripts/mutation-coverage-manifest.json` (R) | **the ROW-KEYED register only**; its row key is `invariants['tests/lint/opGuardCoverage.walker.test.js']`, ours is `invariants['tests/lint/flavorFields.census.test.js']` — distinct, no collision (TOOL-27, §P10.8) |
| **EM-B2a4 v2** | `src/domain/edit/dmLayer.js` (M) · `src/store/settlementRederiveAction.js` (C) · `src/store/settlementSlice.js` (M) · `tests/property/dmLayerGoldenIsolation.test.js` (C) · `tests/build/vendorPdfLazy.test.js` (T) · `scripts/mutation-coverage-manifest.json` (R) | **the ROW-KEYED register only**; its row key is `invariants['tests/property/dmLayerGoldenIsolation.test.js']` — distinct. ⭐ EM-B2a4 carries the `vendorPdfLazy.test.js` TEST row; EM-A3 carries NONE, consistent with EM-A3 not being the byte-arm holder |

⚠ **ONE SHARED OBLIGATION ACROSS ALL THREE:** none of the three mutation-coverage rows may be
`kind: "uncovered"`, because `uncoveredBaseline` is a GLOBAL SCALAR (186 at the tip, RE-MEASURED)
and a member that would move it refuses at placement (§P10.8). Judgment 157a rules all three
`rationale`, which satisfies this by construction. Stated here so the chair can check all three at
once rather than at three separate builds.

### ⭐ PATH-DISJOINTNESS AGAINST TRAIN EM-T14, measured from the same capsules (version 9)

Measured by reading each waiting capsule's `changeManifest` and intersecting it with EM-A3's four
paths. ⛔ EM-A3's paths are `tests/lint/flavorFields.census.test.js` (C) ·
`src/domain/edit/fieldDeclarations.js` (M) · `scripts/mutation-coverage-manifest.json` (R) ·
`docs/ARCH_EDIT_MODE_AND_DECREES.md` (DOC).

| member | its §7 paths | intersection with EM-A3 |
|---|---|---|
| **EM-R0d v8** | `src/data/bandLadders.js` (C) · `src/generators/factionDynamics.js` · `defenseGenerator.js` · `foodGenerator.js` (M) · `tests/domain/bandLadders.test.js` (C) · five TEST rows incl. `tests/copy/voiceMechanics.test.js` and `tests/build/generationWorkerLazy.test.js` · `src/domain/prose/holderTable.js` (M) | **NONE** |
| **EM-R0f v3** | `src/data/economyFingerprint.js` (C) · `src/generators/power/economyReconciliation.js` (M) · `tests/generators/powerEconomyFreshness.test.js` (T) · `tests/build/domainGeneratorsBoundary.test.js` (M) | **NONE** |
| **EM-R6 v7** | `src/domain/institutionRename.js` · `src/domain/institutionRemoval.js` (C) · their two `tests/domain` files (C) · **`src/domain/factionRename.js` (M)** | **NONE on change paths.** ⚠ ONE NON-BLOCKING EXPOSURE, NAMED: `src/domain/factionRename.js` is the home of FOUR of EM-A3's `requiredSymbols`. MEASURED: EM-R6's capsule declares `retiredSymbols: []` and itself requires `export const FACTION_RENAME_SURFACES` on that path, so an EM-R6 landing retires nothing EM-A3 names and the four rows survive it in either order. Re-measure at EM-R6's own pre-proof if its `retiredSymbols` ever becomes non-empty |

⚠ **EM-R0d carries a TEST row on `tests/copy/voiceMechanics.test.js`**, the walker EM-A3 predicts
`+0` on. That is NOT an intersection — EM-A3 carries no row on that path and moves neither of its
counts (measured `emDash +0 · exclamation +0` with the walker's own tokenizer) — but the two members
must not both try to bank a voice figure in one train, and they are on different trains as placed.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into the next wave.

## 12. Completion receipt

- Base SHA: `e1750d7454c1dff7e81342efadd05e47719ca6aa` (re-compiled against `bfca6b7d6aad3ef1955d3bc33119988d8a5d9989`)
- Dispatch bundle and seal identity: **NOT DISPATCHED** — version 9 is a COMPILE, not a build.
- Final commit or working-tree state: **no edit made.** This lane wrote only under
  `$SP/lane-compile-EM-A3-v9-scratch/`, and `git -C $SP/read-tip-em-t12 status --short` printed
  NOTHING before and after.
- Exact changed files and effective-line deltas: `NONE` yet. The BUILD's will be
  `tests/lint/flavorFields.census.test.js` (CREATE, seven `test`s under one `describe`),
  `src/domain/edit/fieldDeclarations.js` (**+1 / −1 effective line**, the file at 145 lines before
  and after), `scripts/mutation-coverage-manifest.json` (ONE row, `kind: "rationale"`) and
  `docs/ARCH_EDIT_MODE_AND_DECREES.md` (ONE sentence).
- Acceptance cases: `0 of 7 executed` by this lane; **three of the seven had their MECHANISM
  executed** in plain `node` at `bfca6b7d6` — A4's split probe (exit 0, quoted in §6), its negative
  control (exit 1, as it must), and A7's roster (5 rows / 6 sites / 8 occurrences).
- Focused commands, exits, and counts: **no vitest, eslint, `npm run check`, or register script was
  run.** Every measurement command and its output is in `EM-A3.evidence-t13.md`.
- Sealed per-step receipt and exact-state resume status: `n/a`.
- Both typecheck configurations: `n/a` — the build lane measures `tsconfig.full.json`'s errors on
  `src/domain/edit/fieldDeclarations.js` BEFORE it reports (§P7's 2026-09-21 clause).
- Wave-end gate stages actually executed: `NONE`.
- Base-versus-wave failure identity diff: `n/a`.
- Dormancy/golden result: `n/a`. Golden posture DECLARED UNCHANGED. ⭐ EM-A1's dormancy arm, which
  version 8 recorded RED as BLOCK-H, is GREEN at this base: the cure `17bf34dd1` is an ancestor.
- Generated artifacts: `NONE`
- Deviations: **NONE OUTSTANDING.** Version 8's five open items are all closed:
  **BLOCK-E** ruled (157d, E-1; §6 carries the predicate whole), **BLOCK-F** ruled (157b; the three
  roots, the FILE root, the guard root dropped in writing and the ARCH sentence corrected),
  **BLOCK-G** closed by REPLACEMENT (the planted split probe is A4's control), **BLOCK-H** CURED in
  the tree at `17bf34dd1`, and **BLOCK-C** — how a generator-unwritten field earns a `readersProof`
  — is ANSWERED by §6's reach sentence: the proof is the pair of arms plus the honest statement
  that an empty `R` is zero GROUNDED reads, which is exactly the "absence of evidence" trap
  BLOCK-C named and exactly what the roster arm closes. BLOCK-A stands as the record of why the
  live scan was ordered; BLOCK-B and BLOCK-D were discharged by measurement at version 8 and are
  re-confirmed here.
- One-time behaviour shift, recorded rather than left to ride: **the `readersProof` VALUE on both
  `free` rows changes**, from EM-A1's placeholder to the census's own reach sentence. No arm pins
  the old value (measured: zero assertions in `tests/`, zero `requiredSymbols` rows across the
  estate's 2,795), so nothing reds; it is recorded because the record is what a later reader has.
- Out-of-scope observations, without investigation:
  1. Both register baselines are frozen at shas that are NOT the packet base: writer-reach
     `5123689dff88b48a9948ba4160736aa87ba2d3c3`, observed-shape
     `31ab5d18bdd44ba0691c2e16f3ae8f3cfef36eb1`. Version 9 READS NEITHER, so the inherited-freeze
     question this row raised at version 8 no longer reaches this member; it is left standing for
     whichever member does read one.
  2. `powerStructure.factions[].category` is derived from the faction's display NAME by
     `inferFactionCategory` (`src/generators/power/factionCategories.js`), and
     `src/generators/factionRoles.js` infers a faction's archetype by name pattern. These are
     **name-valued derivations inside `src/generators/`** — the exact family BLOCK-A says neither
     register can see, and the family version 9's live scan CAN now see for any key it is pointed
     at. Named, not investigated.
  3. `src/domain/factionRename.js`'s `NPC_HANDLE_KEYS_NOT_CASCADED` records `faction` as an
     *"open question"* — a name-shaped affiliation key the membership predicate accepts and the
     cascade deliberately does not rewrite. A later census widening would meet that recorded
     latency.
  4. ⭐ NEW, noticed and not touched: `tests/copy/voiceMechanics.test.js`'s Tier-2 counter is a
     hand-written character tokenizer whose own header records that a PARSER found 39 exclamation
     points where the tokenizer found a different number. This member is held at ZERO by it either
     way, so nothing here turns on the discrepancy; it belongs to whichever lane owns that walker.
- Judgment calls: **NONE.** Version 8 handed up four blocks and the chair ruled all four (judgment
  157 a–d). Version 9 hands up FOUR SPELLINGS, each with a measurement and a recommendation and
  none of them decided by this lane: §11 **THE CHAIR'S FOUR** — the roster's key (five rows by
  file + receiver + count, or six by `path:line`), the `requiredSymbols` delta 12 → 19 and its one
  removal, whether this member or the terminal carries the ARCH §8 correction, and whether
  `writerReach.walker.test.js` is a sealed check or a build-lane instrument.
