# `settlement-editor / tests-only mode` — EM-E5: DIRECTION AND INFORMATION (RECORD)

- **Status:** `LANDED`
- **Packet version:** `record-1` — written after the fact by the HISTORIAN seat (U3), 2026-09-23
- **Landed at:** `78a54b255ca8a60ec962f134fd628255b89f317e` — EM-E5: the realm's fourteen verbs surfaced as seals with their own predicates and prose, the survey's ruled-in directions bound by import, and the information half declared at its sources
- **Verified base:** `feat/em-e5-direction` at `ad9321796533647899e4f39d70028e5eb636b317`
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: TO BE STAMPED BY THE CHAIR)

⛔ **A RECORD, NOT A DISPATCHABLE CONTRACT.** This member was built under TESTS-ONLY MODE — the owner, 2026-09-23 ~09:1x, *"Can we just have it where we only have the testing apparatus? As we did so many months ago?"* → *"Then do it and start building! Thank you"* (`docs/OPERATING_STANDARD.md` rule 39; stasis row S11). Under that word the estate's own ritual — packets, seals, placements, picks, flips, count provers, pre-proofs — is PARKED, never deleted, and the record is the branch's commit receipts, the progress board and this file. It pins no symbol, reserves no path, and may not be dispatched; the authority that governed the build is §1's brief and the design section that brief cites. The unfreeze train restores the ritual (U1, U3).

## §1 — The brief this member was built from

**The brief:** `briefs/launch/BUILD-EM-E5-tests-only.md` (the charter's Wave 3 row; design §17).

**The charge, in one sentence.** The realm's fourteen verbs surfaced as seals with their own predicates and prose, and the survey's ruled-in directions bound BY IMPORT; the rows reach `resolveDecree` as opTypes rather than being spliced into `OP_TYPES`.

## §7 — What moved (the commits, and their files)

| commit (on the train) | on its branch | subject |
|---|---|---|
| `78a54b255` | `ad7d7ff3f` | EM-E5: the realm's fourteen verbs surfaced as seals with their own predicates and prose, the survey's ruled-in directions bound by import, and the information half declared at its sources |

| action | path |
|---|---|
| CREATE | `src/domain/edit/directions.js` |
| MODIFY | `src/domain/worldPulse/informationStatecraft.js` |
| CREATE | `tests/domain/editDirections.test.js` |

## §9 — The acceptance cases, by title, as the commits record them

- **E5-1** — the surfaced set EQUALS the manifest's proposal lane BOTH DIRECTIONS, and it is the chartered fourteen (14 of the manifest's 16, measured).
- **E5-2** — the `direct-realm` row's vocabulary IS that surfaced set.
- **E5-3** — all sixteen render as seals in the manifest's order, and `offered` equals the predicate's own verdict verb for verb, with both halves non-empty.
- **E5-4** — a grayed seal's reason is the manifest's own sentence, verbatim, at a dark wave gate AND at a seal-level refusal; an offered seal carries none.
- **E5-5** — REINFORCE/INTERCEPT are the deferred pair, never offered, each covering its own deferral code, grayed with the manifest's reason and `realmVetoProse`'s prose.
- **E5-6** — the veto prose a card speaks is only for a code THAT verb covers.
- **E5-7** — the seal reader is total on a malformed world and a malformed seal.
- **E5-8** — the roster is the twelve, authored in codepoint order.
- **E5-9** — every enum is its own module's live list (nine vocabularies).
- **E5-9b** — and the rows SPELL those symbols: eleven picks, zero inline lists.
- **E5-10** — every `requires.world` id is a LIVE `WORLD_CONDITIONS` row (ruling 6's four).
- **E5-11** — each direction stages through EM-C1's `stage`: pending, `dm`, index 0 then 1.
- **E5-12** — ten rows each refuse a word no module owns and accept their list's first member, every refusal carrying EM-C1's own `pool-value` and naming the word.
- **E5-13** — the leaf never imports the refusal kinds at all.
- **E5-14** — the two suspension words are the treasury's own, siege outranking occupation.
- **E5-15** — no record, no type, a foreign type and a bare string all leave the levy free.
- **E5-16** — every declared source resolves to a live function of the module it claims.
- **E5-17** — sight is withheld naming ruling 4; secrecy is withheld as measurement owed.
- **E5-18** — the belief acts sit under `beliefExists`, correct and twist contradicting.
- **E5-19** — the bands are the writer's own classifier's answers.
- **E5-20** — the credibility kinds are the four the fold really branches on (4 of 4).
- **E5-21** — the leaf reaches no store, no application layer and neither op producer.
- **E5-22** — the rows are not spliced into the composed catalogue.
- **E5-23** — ZERO `src` modules import the leaf, over a walk of the whole tree.
- **E5-24** — all eleven declaration fields on all twelve rows, guards stated when empty.

## §10 — The checks the builder ran

- `npx vitest run --pool=threads --maxWorkers=2 tests/domain/editDirections.test.js`
- `node scripts/implementation-packets.mjs validate`

## §12 — How it reached the train

Branch `feat/em-e5-direction`, built at `ad7d7ff3f`; pushed as TRANSPORT (rule 24a — a lane push opens no pull request and merges nothing) and read on GitHub Actions (runs `35882470065`, `35888099156`). The chair read the run against the DECLARED reds — the two census cars under the `CENSUS_STASIS` switch (stasis S6), the lighting walker's title count, the observed-shape family's level at the branch's base (U37, closed by the schema-23 rung at `8cc2926d0` + `3b21ba791`) and the declared phone-floor rows (U25, re-recorded once at the tip by `56b085fc5`) — and MERGED it into the train branch `em-train-17-2026-09-23` by pick: `78a54b255`. Nothing was pushed as a landing; the pull request, the merge to `master`, the deploy and every migration remain the OWNER's.

ITS OWN REFREEZE WAS SUPERSEDED, NEVER HAND-MERGED. `ea8baaaf0` refroze `tests/lint/.tuning-inventory.json` on the branch and CONFLICTED with EM-E4b's refreeze of the same register; the pick was ABORTED and the register refrozen ONCE at the train tip by the walker's own ritual (`8e8f70182`, judgment 278), the chair's commit naming both causes. The direction leaf's UI half waits on EM-D1b (U30), and `directions.js` owes the editorTrain roster row (U29).
