# Settlement editor / EM-B1i — a STANDING institution stops reading as DESTROYED: `institutionDestroyed` tests the fate's DECLARED KIND, not its truthiness

- **Status:** `READY`
  ⚠ The status value above stands ALONE on its line because `parsePacketHeader`
  (`scripts/implementation-packets.mjs`) anchors the status row at end-of-line (J-TEWF1B-1).
- **Packet version:** 4
  ⭐ **VERSION 4 (the chair, judgment 101, 2026-09-21; the build lane's STOP at `583f8f644`, `lane-em-b1i-t10-scratch/EM-B1i.STOP.md`) — A7 RE-AIMED AT THE LEAF'S OWN RULE, NO COUNT MOVED.** Version 3's A7 asserted that the three non-`closure` write-site literals carry no `status:` key; `upswingKernel.js:763` founds an Academy with `status: 'active'` — a true `rise`, exactly as §5 and EM-B1h's leaf (`worldPulseFates.js:53`) say. Version 4's A7 asserts NO NON-ACTIVE `status` (an absent key or `'active'` both pass), which is strictly stronger on the `closure` negation the arm exists for and non-vacuous over three real literals. Three citations ride the same version: §5's `calamityKernel.js:309` → `:311` (the demote branch `:294-311` → `:296-313`, re-found at `583f8f644`); §8 step 0's `…KIND` grep gains its `=` so the `…KINDS` line stops matching; the EM-B1h arm this packet called "A6/T4" lives in the walker's T4 (`worldPulseFateTotality.walker.test.js:407-408`). Cases stay 7 of 7 (+8 titles); §7, the `changeManifest`, the `checks` and the lighting delta are untouched. The prior version is 3.
  - Version 3 is the EM-T10 PRE-PROOF revision, measured whole at `e80a6a3f4` (the integration
    tip merged to master). ⛔ **ONE DEFECT THAT WOULD HAVE FAILED PLACEMENT:** the `Verified base`
    row carried a trailing clause on its own line, so `parsePacketHeader` returned
    `verifiedBase: null` EVEN WITH A REAL SHA STAMPED (four-cell control, interim rule 5) — the row
    now carries its value alone. ⛔ **ONE STALE ABSOLUTE:** the `921c51cf…` dossier-manifest digest
    moved at CURE-J `df661cb43` (a SIGNED re-record, external to this packet); every digest literal
    is withdrawn in favour of `tests/fixtures/.golden-freeze-register.json`, which is the shape
    CURE-L `85cd9f4a8` landed for exactly this reason. Three drifted line ADDRESSES refreshed
    (`ruinInstitution.test.js` A4 and A6, `pulseKernel.js`), A5 re-worded to name EM-B1h's KINDS
    (`closure` 15 / `rise` 2 / `standing` 1) rather than "3 standing", and the header's second,
    unreproducible preamble hash withdrawn. ⛔ **No contract, budget, acceptance case, required
    symbol, change row or non-goal moved.**
  - Version 2 was the DOCS repair only (ESTATE-REPAIR-3, 2026-09-20, read tip `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`): the
    live `268` first-paint absolute in §3.2's bundle table becomes a measurement with an as-of
    mark (interim rule 9), and the interim-rules header row is added. ⭐ **MEASURED: this packet
    CREATEs no `.js` under `src/generators/**` or `src/domain/**`, so `stamp.producerIndexFiles`
    moves by `+0`** and interim rule 1 owes it no deferred row. ⛔ **No contract, budget,
    acceptance case, required symbol, change row or non-goal moved.**
- **Verified base:** `em-t10-b1i-2026-09-21` at `e348d59b609e2c62c957a4cd78653849792043b7`
  ⛔ Stamped by the chair at promotion; the value stands ALONE on the row above, because
  `parsePacketHeader` anchors `Verified base` at end-of-line exactly as it does `Status`
  (interim rule 5). ⭐ **MEASURED BY THE EM-T10 PRE-PROOF WITH A FOUR-CELL CONTROL through the
  tip own parser:** value alone + a real sha → the sha RESOLVES; the earlier form (a trailing
  `— ⛔ stamped by the chair…` clause on the same line) + the SAME real sha → **`verifiedBase: null`**,
  which fails validation at placement for a reason unrelated to content. Compiled and measured
  whole at `32602dc607b7423838249cf57d73baf08feb047d` (short `32602dc60`), `git status --short`
  **EMPTY** before and after. ⓘ **THE REVALIDATION SENTENCE THE CHAIR USES AT PROMOTION:*
  *Compiled at `32602dc60` by the Opus COMPILE lane (session a9df403c, 2026-09-20) against a tip
  where EM-B1h had NOT yet landed. Over every change-manifest and `requiredSymbols` path, re-run
  `git diff --stat <base> HEAD` in the SAME command as the stamp; the CREATE target
  `tests/domain/institutionDestroyedByKind.test.js` must be absent on disk and unknown to git;
  all TEN `requiredSymbols` rows must resolve verbatim (`grep -cF` → 1 each, evidence §16); and
  ⛔ **EM-B1h MUST BE LANDED** — `src/domain/worldPulse/worldPulseFates.js` must exist and export
  `WORLD_PULSE_FATE_KIND`, `WORLD_PULSE_FATE_KINDS` and `isWorldPulseFate`, at which point the
  chair ADDS those three `requiredSymbols` rows with the three `grep -cF` commands printed in
  evidence §16.*
- **Last revalidated:** `e348d59b609e2c62c957a4cd78653849792043b7` — the chair's stamp.
- **Depends on:** ⛔ **EM-B1h — a HARD dependency, not a sequencing preference.** This packet's
  cure imports `WORLD_PULSE_FATE_KIND` and `isWorldPulseFate` from EM-B1h's CREATE target
  `src/domain/worldPulse/worldPulseFates.js`, **which does not exist at this packet's compile
  tip** (`ls` → *No such file or directory*, evidence §16). EM-B1h's §6 is treated as APPLIED and
  the import is contracted exactly (§6.1). The charter's train places EM-B1i at EM-T6, directly
  after EM-B1h and before EM-B1a (*Amendments of 2026-09-19 19:10 EDT*, §934.47 addendum 28).
  EM-B1e is LANDED at `8f714cf3ceabef10c735f23b05783c7cddfcba34` and supplies the A3 oracle.
- **Collision group:** ⭐ **`tests/lint/worldPulseFateTotality.walker.test.js` — EM-B1h's CREATE,
  edited here.** EM-B1h built its A6 arm to REFUSE the first reader of the kind and **named this
  packet in the message a violation prints** (evidence §14); this packet is that reader, so it
  carries the arm's NARROWING as a declared row. Serialized by the train (EM-B1h lands first);
  no other packet names that path. `tests/lint/vocabularyTotality.walker.test.js` — checked FREE
  against the live manifest at compile; confirm again at placement.
  ⭐ **`scripts/mutation-coverage-manifest.json` IS NOT IN THIS PACKET'S GROUP** — the CREATE
  target sits outside every `ENFORCER_DIR` and matches no `NAME_PATTERN` token (proved by
  importing the enforcer's own module, evidence §11.1) — so **EM-B1d's READY reservation of that
  path does not gate this dispatch.**
  ⚠ One shared path with whichever train this joins: `tests/lint/.lighting-census-baseline.json`
  (that train's terminal act; ⛔ never this packet's — §7).
- **Commit authority:** edits only; the chair commits.
- **Baseline posture:** **measured**, not inherited, at `32602dc60`. Executed: the reader and its
  ONE caller found by symbol over the whole tree; the defect REPRODUCED and the cure PROVED
  through the real `advanceCauseLifecycle` (six fixtures, three divergences, two controls);
  the estate's own preset-lighting witness driven THREE TIMES (unpatched, probed, cured) with all
  eight rows compared field by field; 2,080 pulse ticks soaked across two presets and twenty
  in-world years; bundle membership from the repo's own `EAGER_FIRST_PAINT_MODULES` export and a
  verbatim copy of vite's static-edge rule; effective lines with eslint's own `Linter` against a
  control reproducing a frozen `.size-baseline` value exactly; the prototype hazard of an
  `in`-based membership test executed; both register scripts run in READER mode with the tree
  quoted clean on both sides; the lighting walker's OWN `parkReasonsFor` run over every host file.
  ⛔ **NO CENSUS ABSOLUTE IS QUOTED** — only the DELTA (§7). **No test was run and no gate.**
- **Preamble:** docs/implementation/preambles/EM-PREAMBLE.md (SHA-256: TO BE STAMPED BY THE CHAIR)
- **Interim compile rules:** `COMPILE-RULES.interim.md` (the chair, 2026-09-20) — the fifth amendment's eleven rules, obeyed before they land. Where this packet and `EM-PREAMBLE.md` (SHA-256 `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`, measured with `shasum -a 256` at the read tip `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d` on 2026-09-20 — the FOURTH amendment, still the tree's live text) disagree, the interim rules govern and this row is the record of it.
  > ⓘ **THE SECOND HASH IS WITHDRAWN** (EM-T10 pre-proof; EM-B1h version 6 withdrew its equivalent).
  > The preamble hash above is the one MEASURED at the read tip with `shasum -a 256`; it re-measures
  > **identical at `e80a6a3f4`**. A second, unexplained digest in the header is a figure no reader can
  > reproduce from the tree.

---

## 0. The headline the chair asked for: DOES A GOLDEN MOVE?

⭐ **NO GOLDEN MOVES, AND THE ANSWER IS EXECUTED, NOT ARGUED.**

| the question | the executed answer |
|---|---|
| does the **preset witness** move? | ⛔ **NO — 0 of 104 fields, 0 of 8 records.** The instrument was driven three times (unpatched / probed / cured) and compared field by field; the unpatched run first reproduced the committed golden with **0** field diffs, so the harness is proved to drive the estate's own single writer (evidence §6.1–§6.2) |
| do the two **estate goldens** move? | ⛔ **NO.** `generator-golden-master.json` and `dossier-prose-manifest-golden.json` are generation-time; `causeLifecycle.js` is **not in the generation worker's 220-module closure** (evidence §9). Their digests at this tip equal EM-B1e's LANDED A4 pins exactly (§6.4) |
| do any **dormancy goldens** move? | ⛔ **NO, AND NONE CAN.** `worldPulseFate` appears in **0** committed `.json` files under `tests/`, `src/` and `public/` — no golden or dormancy fixture spells a fate at all (evidence §6.3) |
| is the **owner's signed door** owed? | ⛔ **NO.** `tests/helpers/goldenRecordDoor.js` is not touched, no `predictedRows` is owed, and no shift record is written. §12's receipt re-proves it by execution rather than by repeating this claim |

⚠ **THE LIMIT OF THE WITNESS, NAMED RATHER THAN HIDDEN.** The witness realm carries no
`corrupt: true` NPC, so `institutionDestroyed` is never reached inside it (`calls=0`, all eight
rows). The witness proves the golden does not move; it does not prove the cure is inert where the
reader runs. That half is §0.1 and §0.2.

### §0.1 · What DOES change, in a DM's words

Today, through the REAL `advanceCauseLifecycle` (evidence §4), a wizard's tower that a disaster
merely knocked down a rung — renamed in place, `status: 'active'`, no inactive flag, its own
writer's comment reading *"rename in place, keeping the slot standing"* — produces this:

> **"Aldra Vane is cut loose"**
> *Sustaining institution "Wizard's Tower" destroyed; the compromise resolved (no live patron
> remained).*

The tower is standing. The DM is told its criminal patron was destroyed, the compromised reeve is
cleared (`corrupt: false`), and a notable news row goes into the chronicle. After this packet, the
same tick produces **nothing at all** — the arrangement continues, because the paymaster is alive.
A genuinely destroyed paymaster still cuts the leash, in the same words.

### §0.2 · How often it fires today — 2,080 ticks, and the honest number

| measure | measured |
|---|---:|
| pulse ticks driven (2 presets × 20 in-world years × 52) | **2,080** |
| non-`closure` fate stamps observed | **16** |
| `institutionDestroyed` calls | **804** |
| calls that DIVERGE under the cure | ⭐ **0** |

⇒ **the coincidence is RARE and the mechanics are why:** every compromise resolved inside years
1–2; every rise fate landed in years 2–4; `endCorruptNpcs: 0`. ⛔ **But the mis-read is PERMANENT,
not momentary** — sixteen **standing, active** academies ended each twenty-year world carrying a
fate that `institutionDestroyed` reads as *destroyed*, for the rest of that world's life. The
first compromise ever leashed to one of them severs instantly, with the words above. This packet
is a correctness fix whose measured lived-behaviour motion today is **zero**, and whose latent
population is **sixteen records per twenty-year world**.

---

## 1. Reconciled authority

1. ⭐ **THE CHARTER'S ROW THAT CREATED THIS PACKET** (`docs/implementation/charters/EDIT-MODE-TRAIN.md`,
   *Amendments of 2026-09-19 19:10 EDT*, ODQ §934.47 addendum 28): *"EM-B1i (NEW; train EM-T6,
   right after EM-B1h, before EM-B1a): that reader tests the `closure` KIND EM-B1h declares;
   measured first over the pulse goldens and the preset witness — if a golden moves, it is a
   lived-behaviour change and goes through the OWNER's signed door with the before/after in plain
   words."*
2. ⭐ **THE WIDENING** (same charter, *EM-B1h VERSION 2 ACCEPTED*, ODQ §934.47 addendum 30):
   *"EM-B1i's population is THREE, not two (a demoted wizard's tower joins the flourishing academy
   and the reconstructed guild in reading as destroyed) — its row is widened."* Re-confirmed here
   by reading each write site's record literal at this tip (evidence §3.2).
3. ⭐ **EM-B1h's §6.2 KIND MAP AND §6.3**, treated as APPLIED. EM-B1h declares the kind as data
   **no one reads**; this packet is its first and only reader, and EM-B1h's §2 names it:
   *"EM-B1i is the packet that makes `institutionDestroyed` read it, and that packet — not this
   one — owns the golden and preset-witness measurement."*
4. **`EM-PREAMBLE.md`** §P2 (rows 1, 3, 4, 7, 10, 11, 12), §P3 (rows 1, 4), §P6, §P7, §P8 — cited
   by hash. §P3.1 is the governing census law here: the golden master and the prose manifest do
   not move on any EM packet, and motion is a STOP. §0 discharges it by execution.
5. **THE PROMISE** — a seed is a starting world forever; lived history is immutable. ⛔ **No saved
   record's bytes move and no stored value becomes invalid.** The cure changes how an existing
   value is READ, and §6.2's unknown-fate contract is what keeps an older save's meaning from
   changing silently.
6. **`docs/DESIGN_EDIT_MODE_AND_DECREES.md` §12 GOVERNS**; nothing in §12 bears on a pulse-side
   reader. §15's *Ops* bullet — *"the vocabularies are FINITE-SEMANTICS pools … and the readers
   that consume them are named in the packet that adds each field"* — is discharged by §7's
   REGISTER row, which names this reader in the estate's producer→consumer register.
7. **EM-B1e (LANDED, `8f714cf3c`)** — its A4 pins the three golden digests and its A6 drives this
   very reader through `leashSevered()`. Both are simulated against the cure in evidence §6.4–§6.5
   and both stay green; A3 of this packet executes them.
8. Live code at the tip, which supplies every figure in §0, §3, §5, §6 and §7.

**Resolved contradictions:**

- EM-B1h §8 N9 — *"`vocabularyTotality.walker.test.js` will owe a row when the first consumer
  appears"* → ⭐ **a row IS owed, but not the one predicted.** A row over the eighteen FATE WORDS
  would be self-referential and therefore vacuous under §P6 (the consumer derives its reading from
  the producer by import). The non-vacuous contract is one level up — over the KINDS — and it is
  exactly this walker's declared class (evidence §13). §7 row 4 carries it.
- The charter's *"`LIFECYCLE_CLOSE_FATES` is 3 of the 15 closure fates"* → ⚠ **the framing crosses
  two vocabularies.** That set reads `institutionHistory[].fate`, and its sibling
  `LIFECYCLE_BUILD_FATES` holds `built`/`reopened`, which are not `worldPulseFate` members at all.
  Measured and put to the chair in §15 R4/Q4; ⛔ **not touched here.**

---

## 2. Outcome

**Observable result:** `institutionDestroyed` (`src/domain/worldPulse/causeLifecycle.js`) returns
`true` for a fate whose **declared kind is `closure`**, and for a truthy fate the vocabulary does
not know; it returns the flag/status reading for a fate whose declared kind is `standing` or
`rise`. A **flourishing academy, a reconstructed guild and a demoted-but-standing wizard's tower
stop reading as destroyed**, and a criminal leash on any of them stops severing.

**Definition of done:** the reader imports `WORLD_PULSE_FATE_KIND` and `isWorldPulseFate` from
EM-B1h's leaf and branches on a **total** kind→verdict map; the three flag and status arms are
byte-for-byte what they are today and still decide in the same ORDER; an unknown truthy fate keeps
today's verdict; EM-B1h's no-reader arm is NARROWED to a one-file roster rather than deleted; the
producer→consumer register gains its row; and **no golden, no dormancy golden and no byte of the
preset witness moves** — proved by execution, not asserted.

In scope: (1) **one behaviour family** — the fate's declared kind decides whether an institution
reads as gone; (2) the one required integration — EM-B1h's walker learns that exactly one reader
now exists; (3) the prevention guard — the `vocabularyTotality` row that convicts a future FOURTH
kind.

Explicit non-goals: ⛔ **`LIFECYCLE_CLOSE_FATES`** (§15 R4 — a different vocabulary, a
lived-behaviour change, and an exact both-ways pin in a landed walker); ⛔ **the
`institutionHistory[].fate` vocabulary** (EM-B1j's); ⛔ **any change to a WRITER** — the three
non-`closure` write sites are read here and never edited; ⛔ the op that produces
`ruined_by_decree` (EM-B1a's); ⛔ re-kinding any member or adding a fourth kind (EM-B1h's, and a
STOP here); any golden, tuning, migration, surface or paid-surface behaviour.

---

## 3. Hard scope budget

| Limit | Packet budget | Standard |
|---|---:|---:|
| Behavior families | `1` | 1 |
| New persisted record families | `0` — no key is created, read or written anew | ≤1 |
| Named state writers | `0` — this packet touches no writer at all | ≤1 |
| Feature flags / user-facing surfaces | `0` each | ≤1 each |
| Direct production consumers | `1` — `causeLifecycle.js:397`, the only call site | ≤2 |
| **New logic-bearing production leaves** | **`0`** | ≤2 |
| **Existing logic-bearing production files modified** | **`1`** — `causeLifecycle.js` only | ≤3 |
| New TEST leaves | `1` | n/a |
| **Additional registration-only files** | **`2`** — EM-B1h's walker (narrowed) and the vocabulary register | ≤3 |
| Handwritten files total | **`4`** | ≤12 |
| Effective lines in the new test leaf | **≈170 projected** (EM-B1d's executed 232 and EM-B1h's ≈240 are the yardsticks) | ≤250 |
| **New/changed effective production lines** | **`+5`; cap ≤10** (1 import + 1 `const` + 3 in the body, replacing 1) | ≤400 |
| Delta in a shared/hot file | `0` — **no edited file is hot** (§3.1) | ≤15 |
| Acceptance cases | `7` | ≤8 |

Overrides approved before dispatch: `NONE` — the packet fits the default with room to spare.

### §3.1 · Hot files — MEASURED, and none is edited

`PACKET_STANDARD.md`'s standing hot list names five files; **`causeLifecycle.js` is not among
them** (evidence §8). Measured with eslint's own `Linter` (`skipBlankLines`, `skipComments`)
against the `src/domain/**` ceiling of **800** (`eslint.config.js`, the *code-quality-7 domain
size ratchet* block):

```
causeLifecycle.js     376 / 800   HEADROOM 424   ← the ONLY edited production file (+5 eff)
CONTROL roadsKernel.js  838  —  EXACTLY its frozen scripts/.size-baseline.json value of 838
```

⇒ the control proves the counter reproduces eslint's own arithmetic. `causeLifecycle.js` carries
**no** per-file override and **no** `.size-baseline.json` entry, so the plain 800 ceiling applies
and 381/800 is the post-edit figure §12 must report.

### §3.2 · Bundles — ⭐ THE NEW IMPORT EDGE COSTS ZERO MODULES

| | generation worker (**1,401,208, EXACT, zero slack**) | eager first paint (size MEASURED at the base, never quoted — **268** at this packet's compile tip, **269** at `bdbf7c89c`) | lazy `engine` | `advanceInterval.worker` | edge-shared inputs (307) |
|---|---|---|---|---|---|
| `causeLifecycle.js` | ⭐ **NO — not in the 220-module closure** | **NO** | **NO** — the rule is `id.includes('/src/generators/')` (`vite.config.js:862`) | YES (549-module closure) | **NONE** |

⭐ **The leaf is already there.** `causeLifecycle.js` has exactly ONE static importer in `src/` —
`pulseKernel.js` — which also imports `calamityKernel.js`, EM-B1h's importer of the leaf. So every
closure that contains this reader already contains `worldPulseFates.js` once EM-B1h lands, and the
new edge adds **no module to any chunk**. The packet's whole byte cost is its own **≤194 B**
(esbuild `--minify`, per-module, a stated upper bound), into `advanceInterval.worker`, which has
**no byte ceiling today**; with EM-B1e's +437 B and EM-B1h's ≤1,008 B that is **≈1.64 KB of
TOOL-3's declared 4 KB per-train headroom**. ⇒ **no budget obligation, no TEST row for a budget
file, no build step.** **R2.**

---

## 4. Sealed dispatch and preflight

```sh
git rev-parse HEAD && npm run implementation:dispatch -- EM-B1i
```

| # | check | verdict at `32602dc60` |
|---|---|---|
| 1 | `dispatch branch mismatch` | the worktree must sit on the verified branch — the build lane's own act |
| 2 | `verified base is not an ancestor of HEAD` | passes while the stamped base is the tip or a proved-non-interfering ancestor |
| 3 | `verified-base descendant changed declared substrate` | passes when the base is the tip |
| 4 | `capsule omitted declared substrate` | passes; the capsule is built from the same list |
| 5 | `CREATE target must be absent and Git-clean` | ✅ `tests/domain/institutionDestroyedByKind.test.js` **absent on disk and unknown to git** (evidence §15) |
| 6 | `non-CREATE target must be Git-clean` | passes on a clean tree |

⚠ **THE WORKTREE IS SHARED.** Re-read `git rev-parse HEAD` in the same command as the dispatch.
⛔ **THE ONE PREFLIGHT THAT CAN REFUSE THIS PACKET: EM-B1h MUST HAVE LANDED.**
`src/domain/worldPulse/worldPulseFates.js` must exist and export all three named symbols, or the
first line of the cure cannot be written. ⓘ Unlike EM-B1h, this packet does **not** reserve
`scripts/mutation-coverage-manifest.json`, so EM-B1d's hold on that path is irrelevant here
(evidence §11.1).

---

## 5. Verified tree contract

Every row found **by symbol** at `32602dc60`; `grep -cF` counts in evidence §16.

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| ⭐ **THE READER — THE SUBJECT** | `src/domain/worldPulse/causeLifecycle.js` | `function institutionDestroyed(inst)` (`:136`) | `:138` decides on the two flags; **`:139` `if (inst.worldPulseFate) return true;` decides on the KEY's PRESENCE, never on the value**; `:140` decides on `NONSTANDING_STATUS`. ⛔ **module-private — not exported** | ⭐ **The whole cure. `:139` is replaced by two kind-aware lines; `:137`, `:138` and `:140` are byte-identical and keep their ORDER** |
| ⭐ **ITS ONLY CALLER** | `src/domain/worldPulse/causeLifecycle.js` | `:397` `const localSustainerGone = !!(sustainer && institutionDestroyed(sustainer));` | The ONLY call in the whole tree (`grep -rn` over `src`, `tests`, `scripts` → one definition, one call). On `true` it opens **TERMINAL 2 — PAYMASTER DEATH → RE-ADJUDICATE** (`:404`), which either re-patrons the compromise or **drops the record and clears the NPC's `corrupt` flag** | ⛔ **Untouched.** Its behaviour on `true` is unchanged; the packet changes only WHEN `true` happens |
| **The consequence of `true`** | `src/domain/worldPulse/causeLifecycle.js` | `:440` `reforms.push(…)` and `:566-570` in `projectCauseLifecycleOntoSettlement` | The reform clears the tag: `{ ...rest, corrupt: false, corruptionVector: null }` — one-way, undo-clean | ⛔ **Untouched** — the price of a wrong `true`, quoted in §0.1 |
| **The DM-visible receipt** | `src/domain/worldPulse/causeLifecycle.js` | `causeLifecycleNewsEntries` (`:608`), `NEWSWORTHY` (`:609`) | `re-adjudicated` is newsworthy: `significance: 'notable'`, `score: 48` | ⛔ **Untouched.** A wrong `true` puts a lie in the chronicle; this is why the packet exists |
| **The name resolver** | `src/domain/worldPulse/causeLifecycle.js` | `function sustainingInstitution(settlement, npc)` (`:146`) | Tolerant name match (`xn === n \|\| xn.includes(n) \|\| n.includes(xn)`); returns `null` for a foreign leash | ⛔ **Untouched.** Named because A1's fixture depends on its exact matching rule |
| **The flag/status arms** | `src/domain/worldPulse/causeLifecycle.js` | `const NONSTANDING_STATUS` (`:134`) | `Set(['removed','destroyed','remnant','ruined','defunct','closed','disbanded','abolished'])` — a **STATUS** vocabulary holding two FATE words | ⛔ **Untouched, and it still decides LAST.** A3 pins the order |
| ⭐ **WRITER — kind `standing`** | `src/domain/worldPulse/calamityKernel.js` | `worldPulseFate: 'demoted_by_disaster',` (`:311`, in the demote branch `:296-313`; re-found at `583f8f644`, version 4) | The record literal carries **NO `status:`, NO `_worldPulseInactive`, NO `_worldPulseMorallyAbolished`**; the branch renames in place and its own comment reads *"rename in place, keeping the slot standing"* | ⛔ **Untouched.** A7 re-derives the kind from this literal |
| ⭐ **WRITER ×2 — kind `rise`** | `src/domain/worldPulse/upswingKernel.js` | `worldPulseFate: 'upgraded_by_reconstruction' }` (`:516`), `worldPulseFate: 'founded_by_flourishing' }` (`:763`) | One promotes in place with no status change and no flag; one pushes a **new `status: 'active'`** record | ⛔ **Untouched.** A7 re-derives both |
| ⛔ **THE LANDED ORACLE THIS PACKET MUST NOT RED** | `tests/domain/ruinInstitution.test.js` | `function leashSevered(sustainer)` (`:156`), the A6 arms (`:359-381`) and A4 (`:321-345`) | A6 drives **this reader** through the real `advanceCauseLifecycle`; A4 pins the three golden digests. Both were simulated against the cure and stay green — the two fated records are `closure`-kinded **and** carry `_worldPulseInactive` and `status:'ruined'`, so `:138` decides them first | ⛔ **Untouched. A3 of this packet EXECUTES it** rather than reasoning about it. **This file is CREDITED at this tip** (7 titles) since CURE-C `145acdb75` |
| ⭐⭐ **EM-B1h's ARM THAT REFUSES THIS PACKET** | `tests/lint/worldPulseFateTotality.walker.test.js` | its A6/T4 arm — *"a source scan proves NO file under `src/` imports `WORLD_PULSE_FATE_KIND`"*, whose failure message **names EM-B1i** | ⛔ **A LANDED ARM THAT REDS THE MOMENT THIS CURE IS WRITTEN.** EM-B1h §11 lists *"any `src/` file would have to READ `WORLD_PULSE_FATE_KIND`"* as its own STOP, *"that is EM-B1i's"* | ⭐ **NARROWED, NEVER DELETED** (§7 row 3): the scan must be **SET-EQUAL both ways** to `['src/domain/worldPulse/causeLifecycle.js']`, so a SECOND reader still reds |
| ⭐ **THE PRODUCER→CONSUMER REGISTER** | `tests/lint/vocabularyTotality.walker.test.js` | `objectLiteralKeys` (`:88`); the four `'src/domain/worldPulse/<file>.js\|<CONST>'` rows (`:242-262`) | *"the consumer's recognized set must equal the producer's emitted set EXACTLY, both ways … no consumer entry without a producer … no producer value without a consumer entry"*. The file is **CREDITED** (26 titles, 6 suite titles) | ⭐ **Gains ONE `it` over the KINDS** — the non-vacuous contract (§7 row 4). ⛔ Not a row over the eighteen words: that would be self-referential |
| **Test precedent — the driver** | `tests/domain/ruinInstitution.test.js` | `leashSevered` (`:156`), `fixedRng` (`:154`) | Drives the private reader through its real consequence with a seeded fork and a negative control first | **Copy this shape** for A1–A3 |
| **Test precedent — the proof form** | `tests/domain/institutionFounding.test.js` | `describe('MF-T2Q — the institution founding year')` (`:113`) | ONE literal `describe`, straight-line `it`, positive control first; **CREDITED** (7 titles, 1 suite title) | **Copy this shape** (§P3.4) |
| **The second vocabulary — NOT TOUCHED** | `src/domain/worldPulse/institutionLifecycle.js` | `const LIFECYCLE_CLOSE_FATES = new Set(` (`:634`) | `Set(['shuttered','bankrupt','closed_for_want_of_custom'])`, feeding `priorLifecycleCounts` (`:641`), which reads `institutionHistory[].fate` | ⛔ **UNTOUCHED — and §15 R4 measures why.** Its sibling `LIFECYCLE_BUILD_FATES` holds words that are not fates at all |
| **The freeze that forbids widening it** | `tests/lint/provenanceStampSingleWriter.walker.test.js` | `test('THE FREEZE: the two fate vocabularies are exactly the frozen sets')` (`:293`, inside the `describe` at `:267`) | `expect(setOf('LIFECYCLE_CLOSE_FATES')).toEqual(['bankrupt','closed_for_want_of_custom','shuttered'])` — an **EXACT both-ways pin**, landed `831ba5c0c` (2026-07-27) | ⛔ **Untouched**, and in `checks` so a stray widening is caught |

**Forbidden alternatives:** ⛔ **no edit to any WRITER** — the three non-`closure` sites are read,
never changed; ⛔ **no change to the ORDER of the reader's arms** — the two flags decide FIRST and
the status decides LAST, exactly as today; ⛔ **no `in`-operator membership test** against the kind
map (evidence §7.1 — it reads `constructor`, `toString`, `valueOf` and `__proto__` as members);
⛔ **no second copy of the closure filter** anywhere in `src/`; ⛔ **no derivation of
`LIFECYCLE_CLOSE_FATES` from the kind**; ⛔ **no new export from `causeLifecycle.js`** — the
reader stays module-private; ⛔ **no fourth kind and no re-kinding** (EM-B1h's); ⛔ **no DELETION
of EM-B1h's no-reader arm** — it is narrowed to a roster; ⛔ no hand-edit of
`tests/lint/.lighting-census-baseline.json`; ⛔ no edit to either coupling baseline JSON; no files
outside the manifest.

---

## 6. Exact contracts

### §6.1 · THE CURE, VERBATIM

⛔ **The import is contracted exactly; EM-B1h's leaf is treated as APPLIED.**

```js
// at the top of src/domain/worldPulse/causeLifecycle.js, beside the existing sibling imports:
import { WORLD_PULSE_FATE_KIND, isWorldPulseFate } from './worldPulseFates.js';
```

```js
// immediately above institutionDestroyed, replacing nothing:
/**
 * What each DECLARED fate kind means for "is this institution gone?". TOTAL over
 * WORLD_PULSE_FATE_KINDS — a fourth kind declared by the leaf and not spelled here reds
 * tests/lint/vocabularyTotality.walker.test.js rather than defaulting silently to "standing".
 */
const DESTROYED_BY_FATE_KIND = Object.freeze({ closure: true, rise: false, standing: false });

function institutionDestroyed(inst) {
  if (!inst) return false;
  if (inst._worldPulseInactive === true || inst._worldPulseMorallyAbolished === true) return true;
  const fate = inst.worldPulseFate;
  // An UNKNOWN truthy fate — a word an older save may carry that this vocabulary does not
  // know — keeps TODAY'S reading, so no saved world changes meaning silently. `isWorldPulseFate`
  // is a Set test, never `in`: `in` walks Object.prototype and would read 'constructor',
  // 'toString', 'valueOf' and '__proto__' as members.
  if (fate && !isWorldPulseFate(fate)) return true;
  if (fate && DESTROYED_BY_FATE_KIND[WORLD_PULSE_FATE_KIND[fate]] === true) return true;
  return NONSTANDING_STATUS.has(norm(inst.status));
}
```

⛔ **`:137`, `:138` and the final `NONSTANDING_STATUS` line are BYTE-IDENTICAL to today and keep
their positions.** Only `:139` is replaced, by two lines, and one `const` and one `import` are
added. ⇒ **+5 effective lines.**

### §6.2 · The absence and unknown-value rules, stated exactly

| input | verdict today | verdict after | why |
|---|---|---|---|
| `inst` absent / null | `false` | `false` | unchanged, decided at `:137` |
| `_worldPulseInactive === true` or `_worldPulseMorallyAbolished === true` | `true` | `true` | ⛔ **decided FIRST, before any fate is read** — a rise-fated record that was LATER deactivated still reads destroyed |
| no `worldPulseFate` key, or `null`/`''`/`0`/`false` | falls to status | falls to status | unchanged — `null` is EM-B1h's CLEAR sentinel and is not a member |
| a member whose kind is `closure` (15 of 18) | `true` | `true` | ⭐ **unchanged** |
| a member whose kind is `standing` or `rise` (3 of 18) | `true` | ⭐ **falls to status** | ⭐ **THE CURE** |
| a truthy fate outside the vocabulary (an older save) | `true` | `true` | ⭐ **THE CONSERVATIVE CONTRACT** |
| a non-string truthy fate (an object, `true`, a number) | `true` | `true` | `isWorldPulseFate` returns `false` for a non-string, so the unknown arm catches it |
| `'constructor'`, `'toString'`, `'valueOf'`, `'__proto__'` | `true` | `true` | ⭐ **BECAUSE the test is a `Set`.** A2 pins all four |

⭐ **WHAT THE CONSERVATIVE CONTRACT COSTS, STATED.** An institution carrying a retired spelling
keeps reading as destroyed forever, even if that word once meant "standing". Measured: EM-B1h
swept every revision of all three derivers and found **no retired spelling in the repo's history**,
and this compile re-confirms that `worldPulseFate` appears in **0 committed `.json` files**
(evidence §6.3). A user's saved world is not in this repo, so the cost is a bounded, one-sided
risk: an unknown word can only ever read *too destroyed*, which is today's behaviour, never a new
one. Closing the READ side would need a migration and is **not this packet's** (§15 R5).

### §6.3 · Ordering, determinism, lifecycle

- **Pipeline position:** unchanged — `advanceCauseLifecycle` runs post-apply from
  `pulseKernel.js:1759`, and the reader is called at `:397`, inside TERMINAL 2, after the exposure
  terminal has returned.
- **Determinism:** ⛔ **no PRNG, no clock, no locale, no draw.** The cure adds a pure predicate
  over data already in hand. `Object.freeze` at module load; one `Set` membership test per call.
- **Same-tick visibility, merge, tie-break:** not applicable — the predicate is pure.

| Create | Read | Persist | Reload | Regenerate | Undo | Import/migrate | Public veil |
|---|---|---|---|---|---|---|---|
| one frozen object at load | the reader, at one call site | ⛔ **nothing new is persisted** | unchanged | unchanged | unchanged — the reform clear is untouched | ⭐ **NO MIGRATION OWED** — the cure changes a READING, never a stored byte; §6.2 is what makes an unknown stored word safe | unchanged — the reader is not a display surface |

**Receipts** `NONE` (the existing `re-adjudicated` receipt is untouched; this packet changes only
whether it is emitted). **Flag:** `NONE`. **Golden posture:** ⛔ **UNCHANGED**, proved by
execution in §0 and re-proved by A4. **Alignment:** `DECLARED EMPTY`. **Edit story:**
`ENGINE-ONLY — no DM verb reads or writes a fate; EM-B1a's op is the only future writer.`

### §6.4 · The narrowing of EM-B1h's arm, stated exactly

> The walker scans every `.js`/`.jsx` under `src/` excluding `**/*.test.*` for an import of
> `WORLD_PULSE_FATE_KIND` and REDS unless the hit list is **SET-EQUAL, IN BOTH DIRECTIONS**, to
> the declared reader roster `['src/domain/worldPulse/causeLifecycle.js']`. The failure message
> names EM-B1i as the packet that authorised the one reader and states that a SECOND reader needs
> its own packet and its own golden measurement.

⛔ **The arm is NARROWED, never deleted**: a zero-hit result now reds too (the cure went missing),
and a two-hit result reds (a second, unmeasured reader appeared).

### §6.5 · The `vocabularyTotality` row, stated exactly

> Using the walker's own `objectLiteralKeys(read('src/domain/worldPulse/causeLifecycle.js'),
> 'DESTROYED_BY_FATE_KIND')` and `WORLD_PULSE_FATE_KINDS` imported from the leaf, assert
> `setDiff(keys, KINDS)` and `setDiff(KINDS, keys)` are both `[]` — **no dead verdict arm, no
> kind without a verdict** — and that every value in the literal is a boolean.

⛔ **NOT a row over the eighteen fate words.** The consumer derives its reading from the producer's
own map by import, so such a row would be self-referential and vacuous (§P6).

---

## 7. Exact change manifest

⚠ **ONE FULL REPO-RELATIVE PATH PER ROW; the `action` word is a member of `PACKET_ACTIONS`
(`CREATE`, `DOC`, `MODIFY`, `REGISTER`, `TEST`) and is IDENTICAL to that path's `action` in
`EM-B1i.manifest.json`'s `changeManifest`.** The (action, path) pairs below and the JSON's are
asserted SET-EQUAL in both directions — executed, report §5.

| Action | File | Symbol/region | Max delta | Coding instruction |
|---|---|---|---:|---|
| `MODIFY` | `src/domain/worldPulse/causeLifecycle.js` | `function institutionDestroyed` (`:136`); the import clause | **≤5 eff** | §6.1 **verbatim**. One import from `./worldPulseFates.js`; one frozen `DESTROYED_BY_FATE_KIND` above the function; `:139` replaced by the two kind-aware lines. ⛔ **`:137`, `:138` and the `NONSTANDING_STATUS` line are byte-identical and keep their order.** ⛔ No `in` membership test. ⛔ No new export; the reader stays module-private. ⛔ Nothing else in the file. |
| `CREATE` | `tests/domain/institutionDestroyedByKind.test.js` | acceptance **A1–A7** | **≤250 eff** | ONE literal `describe`, **SEVEN straight-line `it`**. ⛔ `.each`, looped, conditional and nested registration forbidden (§P3.4). ⛔ **Never name a variable, parameter or import `it`, `test` or `describe`** — that is the `OPENER_UNRESOLVED:it` cause CURE-C (`145acdb75`) had to repair. Copy `leashSevered`/`fixedRng` from `tests/domain/ruinInstitution.test.js:154-185` as the driver (the REAL `advanceCauseLifecycle`, never a re-implementation) and the proof form from `tests/domain/institutionFounding.test.js:113`. Every negative carries `// anchored:` on the line above. Positive control BEFORE every negative. |
| ⭐ `TEST` | `tests/lint/worldPulseFateTotality.walker.test.js` | EM-B1h's A6/T4 no-reader arm | **≤8 eff** | §6.4 **verbatim** — narrow the arm to a **both-ways set-equality** against the one-file reader roster. ⛔ **Do NOT delete the arm** and do NOT weaken it to "at most one". ⛔ **Add NO `describe` and NO `it`** — the edit re-spells an existing arm's assertion and message; the lighting census must not move on this file. Restate EM-B1h's own `CANNOT-CATCH:` sentence that the kind can go stale, now that a reader exists. |
| ⭐ `REGISTER` | `tests/lint/vocabularyTotality.walker.test.js` | the bound-consumers `describe` (`:117`) | **+10 eff** | §6.5 **verbatim** — **ONE new straight-line `it`** inside the EXISTING `describe('vocabularyTotality — bound consumers (exact set, both ways)')`, in the shape of the `THREAT_DISPLAY`/`DEFENSE_STRESS_STATUS` rows. Import `WORLD_PULSE_FATE_KINDS` from the leaf at the top with the other producer imports. Use the file's own `objectLiteralKeys` (`:88`) and `setDiff`. ⛔ **Add NO second `describe`** — one `it`, one title. |

**Four rows, all handwritten (of ≤12). Generated artifacts: `NONE`** — a MEASUREMENT: edge-shared
INPUT membership is **0 of 307** (evidence §10), so `npm run build:edge-shared` is absent from
`checks` and none of §P2 row 12's seven generated paths is owed. ⭐ **No generator appears in
`checks` at all**, so step 14(a)'s generator-goes-LAST rule is satisfied vacuously.

### The registration ledger — every row priced against the tip

| # | Obligation | Verdict | Measurement |
|---|---|---|---|
| P2.1 | lighting census | **OWED — `+1 file`, INTERIOR RED** | ⛔ **NO ABSOLUTE IS QUOTED.** The DELTA: the CREATE contributes `+1 files / +0 parked / +1 credited / +7 titles / +1 suiteTitles`; row 4 adds **one `it`** to `tests/lint/vocabularyTotality.walker.test.js`, measured **CREDITED** by the walker's own `parkReasonsFor` at this tip (26 titles, 6 suite titles), so it contributes `+1 title`; row 3 adds **no title**. ⇒ ⭐ **`+1 files / +0 parked / +1 credited / +8 titles / +1 suiteTitles`**. ⚠ **This delta is stated against a tree in which EM-B1h HAS LANDED** (EM-B1h's own `+1 files / +4 titles / +1 suiteTitles` is separate and prior). The absolute is the chair's at the train's terminal. |
| P2.2 | mutation-coverage row | ⭐ **NOT OWED** | `enumerateInvariants` admits a file iff it is under an `ENFORCER_DIR` **or** its basename matches `NAME_PATTERN`. Proved by importing the enforcer's own module: `institutionDestroyedByKind.test.js` → `NAME_PATTERN: false`; `tests/domain/…` → `in ENFORCER_DIRS: false`. ⇒ ⛔ **no row, and NO contention with EM-B1d.** |
| P2.2b | coupling-inclusion census | **NOT OWED** | The census reds on a NEW unlayered `src/domain/{worldPulse,spatial}` module; this packet creates none. `causeLifecycle.js` is already a bare-path row in `.coupling-unlayered-baseline.json:39` with no declared `reads` to move. Both ends of the new edge are unlayered, so `scanCrossLayerPairs` mints no pair. ⛔ **Neither baseline JSON moves and `ARGUED_ROSTER_CEILING` does not move.** |
| P2.3 | observed-shape exemption | **NOT OWED** | No new save-time-key reader — `inst.worldPulseFate` is read at this exact site today. Baseline EXECUTED in reader mode: **1,964 findings, exactly matching the frozen inventory**, exit 0, tree clean. The scanner covers every `.js` under `src/`, so `check-observed-shape-readers.mjs` is in `checks` and **motion is a STOP**. |
| P2.4 | writer-reach | ⚠ **MEASURE AND RECORD — never write** | No new reader of a settlement field under `src/domain/edit/**`. Baseline EXECUTED: `judged 6537 · LIT 572 · LIT-NAME 4671 · DARK 1294 (reviewable 495)`. **A shrink is the chair's to bank; GROWTH is a STOP.** |
| P2.7 | prose-numerics | **NOT OWED, and no line-addressed row is disturbed** | The packet renders no figure, and `causeLifecycle.js` carries **zero** rows in `tests/lint/.prose-numerics-baseline.json` (the baseline names six `worldPulse` files; this is not one), so the insertion cannot sit above a baselined row (step 13). |
| — | stamped producer bytes | **NOT OWED** | `docs/content/wiring-census.json`'s `stamp.files` holds **7** entries; **none** is `causeLifecycle.js` ⇒ `proseWiringCensus.walker.test.js`'s `stale-bytes` arm cannot red (step 15). |
| — | citations by line | **NOT OWED** (one already-stale comment, slotted) | `git grep -n "causeLifecycle\.js:[0-9]"` over `src`, `docs/content`, `tests` finds **one** hit: a prose comment in `tests/domain/causeConjunctionContent.test.js:29` citing `:165` for `bearerSituation`, which lives at **`:176`** — **already wrong by 11 lines before this packet moves anything**, and read by no walker. ⇒ not a row here; §15 N5. |
| P2.5 · P2.6 · P2.8 · P2.9 | chooser/pool · persisted-key denylists · surface walkers · the tick hook | **NOT OWED** | No draw, neither persisted key, headless, no tick hook. |
| ⭐ **P2.10** | **edge-shared closure** | ⭐ **NOT OWED — MEASURED** | INPUT membership against all five committed metas (**307 inputs**): **no path of this packet appears in any**. ⇒ no rebuild, no generated path, no `_shared` row. |
| ⭐ **P2.11** | **byte budgets** | ⭐ **NO BUDGETED CHUNK** | §3.2. Generation worker: **NOT REACHABLE**. First paint: **NO**. Lazy `engine`: **NO** (the routing rule is `/src/generators/`). `advanceInterval.worker`: **YES, no ceiling today.** Priced **≤194 B** for TOOL-3's per-train attribution, and **zero new modules in any chunk**. **R2.** |
| ⭐ | **declared-command write set (§P2 row 12)** | **CLEAN** | Every command in `checks` read for what it WRITES. **No generator is invoked**; both `check-*` scripts run as readers (their write doors are `--write`, quoted); eslint has no `--fix`. ⇒ **no undeclared path is written by this packet's own seal.** |
| ⭐ | **`tests/` sweep (§P2 row 12, step 12)** | ⭐ **ONE TEST PATH OWED, AND IT IS ROW 3** | §11. |

> ⛔ **`tests/lint/.lighting-census-baseline.json` IS NOT A ROW** — §P2 row 1 forbids a member from
> editing it; the re-derivation is the train's terminal act and the CHAIR's. Predicted interior
> red, SHAPE ONLY: `the estate's file count moved — re-measure, do not re-word: expected <N> to be <N-1>`.

---

## 8. Ordered coding sequence

0. Dispatch and seal; re-read `git rev-parse HEAD` in the same command. ⛔ **Confirm EM-B1h has
   LANDED**: `src/domain/worldPulse/worldPulseFates.js` exists and
   `grep -cF "export const WORLD_PULSE_FATE_KIND ="` (the `=` keeps the `…KINDS` line out of the count), `grep -cF "export const WORLD_PULSE_FATE_KINDS"` and `grep -cF "export function isWorldPulseFate"`
   each print `1`. If not, **STOP**.
1. **Capture the baselines:** `sha256` of `generator-golden-master.json`,
   `dossier-prose-manifest-golden.json` and `preset-lighting-witness-golden.json` (expect
   the three rows of `tests/fixtures/.golden-freeze-register.json` — ⛔ **READ THE PIN FROM THE
   REGISTER, NEVER FROM A LITERAL** (CURE-L `85cd9f4a8` removed exactly those three literals from
   `ruinInstitution.test.js`; a literal quoted here went stale within a day when CURE-J `df661cb43`
   re-recorded the dossier manifest through the signed door)); `node scripts/check-observed-shape-readers.mjs`
   (expect **1964**, exit 0); `node scripts/check-writer-reach.mjs` (expect `judged 6537 · LIT 572
   · LIT-NAME 4671 · DARK 1294`); the `max-lines` figure of `causeLifecycle.js` (**expect 376 of
   800**).
2. Add `tests/domain/institutionDestroyedByKind.test.js` with A1–A7 **failing**; quote the red.
   ⛔ Run it BEFORE step 3 so A1's divergence arms are seen red against today's truthiness reader —
   that red is the packet's own reproduction of the defect and §12 must quote it.
3. Apply §6.1 to `src/domain/worldPulse/causeLifecycle.js`. ⛔ Nothing else in the file.
4. ⭐ **Immediately** run `tests/lint/worldPulseFateTotality.walker.test.js`: it REDS on EM-B1h's
   no-reader arm the moment step 3 lands — quote that red, then apply §6.4 and watch it green.
   That sequence is the arm's own proof that it was live.
5. Apply §6.5 to `tests/lint/vocabularyTotality.walker.test.js` and run it.
6. Wire consumers: **NOT APPLICABLE** — the one caller is unchanged. Skip and record.
7. Plant the mutants (§9), convict, restore digest-exact.
8. Run focused verification (§10), including ⭐ `tests/domain/ruinInstitution.test.js`, whose
   LANDED A4 and A6 this cure could red (§11), and ⭐ `tests/simulation/presetLightingWitness.test.js`.
9. Run the wave-end gate per the host train's plan; write the completion receipt. ⛔ **Never
   refreeze the census.**

```text
institutionDestroyed(inst), the bounded algorithm:
  1. inst is absent            -> false                                   (unchanged)
  2. _worldPulseInactive===true OR _worldPulseMorallyAbolished===true -> true   (unchanged, FIRST)
  3. fate := inst.worldPulseFate
  4. fate is falsy             -> go to 7                                 (unchanged; null is CLEAR)
  5. fate is not a member of the vocabulary (Set test, never `in`) -> true (the conservative reading)
  6. DESTROYED_BY_FATE_KIND[ WORLD_PULSE_FATE_KIND[fate] ] === true -> true (THE CURE)
  7. NONSTANDING_STATUS.has(norm(inst.status))                            (unchanged, LAST)
```

---

## 9. Acceptance matrix

`tests/domain/institutionDestroyedByKind.test.js` — ONE literal `describe`, SEVEN straight-line
`it` — plus the focused suites of the files it guards.

| ID | Case | Required observation |
|---|---|---|
| **A1** | ⭐ **THE CURE AT ITS ONE CONSEQUENCE, THROUGH THE REAL PULSE** | Driven through the REAL `advanceCauseLifecycle` with a seeded fork, six fixtures identical but for the fate on the paymaster. ⛔ **POSITIVE CONTROL FIRST:** a `closure`-kinded paymaster (`destroyed_by_disaster`) **still severs** — `reforms` non-empty, a `re-adjudicated` event, the record dropped. Then the three: `demoted_by_disaster`, `upgraded_by_reconstruction` and `founded_by_flourishing` each **emit NO event, push NO reform and KEEP the record**. ⛔ **NEGATIVE CONTROL:** an institution with no fate and no non-standing status never severs in either direction, so the driver cannot pass vacuously. |
| **A2** | ⭐ **THE UNKNOWN WORD KEEPS TODAY'S READING, AND `in` IS FORBIDDEN** | A truthy fate outside the vocabulary (`'razed_by_the_gods'`) **still severs** — no saved world changes meaning silently. ⭐ **AND THE FOUR PROTOTYPE WORDS**: `'constructor'`, `'toString'`, `'valueOf'` and `'__proto__'` each **still sever**, which they would NOT if membership were tested with `in`. A non-string truthy fate (an object) severs too. Each negative carries `// anchored:`. |
| **A3** | **THE UNTOUCHED ARMS, AND THEIR ORDER, PINNED BY CONSEQUENCE** | `_worldPulseInactive: true` **beside** a `standing` fate still severs, and so does `_worldPulseMorallyAbolished: true` — proving the flags decide BEFORE the fate. A non-standing `status` with **no** fate still severs, proving the status arm still decides LAST. ⭐ **AND THE LANDED ORACLE IS EXECUTED, NOT REASONED ABOUT:** `tests/domain/ruinInstitution.test.js` is in `checks` and its A6 (`leashSevered` ×4) and A4 (three digests) must be green. |
| **A4** | ⛔ **NOTHING MOVES** | The three goldens are bytewise unchanged **against the pin each carries in `tests/fixtures/.golden-freeze-register.json`** — ⛔ **no digest literal appears in this packet or its acceptance file** (CURE-L `85cd9f4a8`), and ⭐ **`tests/simulation/presetLightingWitness.test.js` is in `checks`** so the witness is re-driven for real rather than trusted. §12's receipt quotes both. |
| **A5** | ⭐ **THE KIND LAW IS TOTAL, AND A FOURTH KIND CANNOT ARRIVE QUIETLY** | `DESTROYED_BY_FATE_KIND`'s keys are **SET-EQUAL both ways** to `WORLD_PULSE_FATE_KINDS` and every value is a boolean (the `vocabularyTotality` row, asserted from the walker too). ⭐ **AND THE PARTITION IS COUNTED:** driving the predicate over all **eighteen** members yields exactly **15 destroyed / 3 NOT destroyed**, and the three are named with their KINDS as EM-B1h declares them (**`closure` 15 / `rise` 2 / `standing` 1** — `upgraded_by_reconstruction` and `founded_by_flourishing` are `rise`; `demoted_by_disaster` alone is `standing`) — so a re-kinding upstream reds here as well as in EM-B1h's walker. |
| **A6** | ⭐ **EXACTLY ONE READER OF THE KIND, DECLARED** | A source scan over every non-test `.js`/`.jsx` under `src/` finds the importers of `WORLD_PULSE_FATE_KIND` **SET-EQUAL both ways** to `['src/domain/worldPulse/causeLifecycle.js']`. ⛔ **BOTH FAILURE DIRECTIONS ARE REQUIRED**: a planted second importer reds, and an emptied hit list reds (the cure went missing). The scan is asserted **non-empty FIRST** (guard-the-guard). This is the same fact EM-B1h's narrowed arm asserts, stated from the consumer's side. |
| **A7** | ⭐ **THE KIND IS RE-DERIVED FROM THE RECORD, NOT TRUSTED** (EM-B1h R11) | Over a comment-only strip that KEEPS string contents (⛔ copy `commentsOnly` from `tests/domain/ruinInstitution.test.js:192`, **never** `tests/helpers/codeOnlySource.js`), the three non-`closure` write-site record literals are re-read and asserted to carry **no NON-ACTIVE `status` (an absent key or `status: 'active'` both pass: the two `rise` records are raised or founded ACTIVE and the `standing` record changes standing alone — version 4, judgment 101), no `_worldPulseInactive` and no `_worldPulseMorallyAbolished`** — so the `standing`/`rise` kinds this reader now BRANCHES on are still true of the writers. ⛔ **BOTH CONTROLS:** the matcher fires on a planted `_worldPulseInactive` beside one of the three, and ignores a commented one. This is the arm EM-B1h could not have: its kind had no reader, so a stale kind was uncatchable. |

**7 of ≤8.** One slot deliberately unspent.

⚠ **THE STRICT-TYPECHECK EXHAUSTIVENESS CASE IS DECLARED EMPTY:** the kinds are runtime data, not
a union type, and no `switch` branches over one. Both typechecks are in §10 regardless; the edited
file must stay strict-clean and `any`-free (it carries an allowance of 0 in `domainStrictBaseline`
/ `domainAnyCastBaseline`).

### The mutants (§P6) — SIX, each with its named target

| # | plant | must red |
|---|---|---|
| **M1** | `DESTROYED_BY_FATE_KIND.standing` flipped `false` → `true` | **A1**'s `demoted_by_disaster` arm **by name** — the arm that proves the cure is live rather than incidental |
| **M2** | the unknown-fate line deleted (`if (fate && !isWorldPulseFate(fate)) return true;`) | **A2**'s unknown-word arm AND its four prototype arms |
| **M3** | ⭐ `isWorldPulseFate(fate)` replaced by `(fate in WORLD_PULSE_FATE_KIND)` | **A2**'s prototype arms ONLY — the mutant that proves §7.1's hazard is guarded and not merely described |
| **M4** | the two flag checks moved BELOW the fate branch | **A3**'s ordering arm (a `standing` fate on an inactive record) — a reorder that every other arm would survive |
| **M5** | a fourth kind (`'razed'`) added to the leaf's `WORLD_PULSE_FATE_KINDS` without a verdict row | **A5** and the `vocabularyTotality` row, both ways |
| **M6** | ⭐ a planted `import { WORLD_PULSE_FATE_KIND }` in a second `src/` file | **A6** and EM-B1h's narrowed arm — the roster's one-file equality |

Each: prove the bytes moved (a no-op plant is a STOP), require a nonzero exit AND the named
acceptance title red, restore the exact pre-mutant SHA-256 **by `cp` from a backup taken before
the plant**, rerun focused green.

---

## 10. Verification commands

```sh
npx eslint src/domain/worldPulse/causeLifecycle.js \
  tests/domain/institutionDestroyedByKind.test.js \
  tests/lint/worldPulseFateTotality.walker.test.js \
  tests/lint/vocabularyTotality.walker.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/domain/institutionDestroyedByKind.test.js tests/domain/causeResolutionLifecycle.test.js \
  tests/domain/ruinInstitution.test.js tests/domain/corruptionWebPins.test.js \
  tests/domain/envoyChanceMeetingLedger.test.js tests/domain/roadsParticipation.test.js \
  tests/domain/upswingKernel.test.js tests/domain/calamity.test.js

GATE_MUTEX_TIER=shared … -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/worldPulseFateTotality.walker.test.js tests/lint/vocabularyTotality.walker.test.js \
  tests/lint/couplingInclusion.walker.test.js tests/lint/negativeAssertionAnchor.walker.test.js \
  tests/lint/contractTestAntiVacuity.walker.test.js \
  tests/lint/provenanceStampSingleWriter.walker.test.js tests/lint/proseNumerics.test.js \
  tests/lint/sizeBaseline.test.js tests/lint/domainStrictBaseline.test.js \
  tests/lint/domainAnyCastBaseline.test.js

GATE_MUTEX_TIER=shared … -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js

# ⭐ THE INSTRUMENT THAT COVERS THIS PATH — re-driven, never trusted.
GATE_MUTEX_TIER=shared … -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/simulation/presetLightingWitness.test.js

# ONCE, separately: EXPECTED RED until the train's terminal, with the predicted delta of §7.
GATE_MUTEX_TIER=shared … -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/sovereigntyLightingContract.walker.test.js

node scripts/check-observed-shape-readers.mjs   # expect 1964; motion is a STOP
node scripts/check-writer-reach.mjs             # compare against step 1; growth is a STOP
node scripts/implementation-packets.mjs validate
npm run check:packet -- EM-B1i
npm run implementation:resume -- EM-B1i
```

Expected: every command exits `0`, **except** the named census interior red until the host train's
terminal. ⛔ Never read a gate through a shell pipe (§P7). ⛔ A member never runs `npm run check`.

---

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and §P8, stop if: the seal is missing or foreign; HEAD is not
the stamped base or a descendant proved non-interfering; ⛔ **`src/domain/worldPulse/worldPulseFates.js`
does not exist or does not export all three named symbols** (EM-B1h has not landed); ⛔ **a golden,
the prose manifest or the preset witness moves by one byte** — §0 measured all three unmoved, so
motion means the cure is not the cure this packet compiled; ⛔ **`tests/domain/ruinInstitution.test.js`
reds** (its LANDED A4 digests or its A6 `leashSevered` arms — §5 and evidence §6.5 measured both
clean); ⛔ **a SECOND production file appears to need editing**; ⛔ **the reader would have to be
EXPORTED**, or a second copy of the closure filter would have to exist; ⛔ **an `in`-operator
membership test appears acceptable** (evidence §7.1); ⛔ **the ORDER of the reader's arms would
have to change**; ⛔ **`LIFECYCLE_CLOSE_FATES` appears to need deriving from the kind** (R4) or
`tests/lint/provenanceStampSingleWriter.walker.test.js`'s FREEZE arm reds; ⛔ **a FOURTH kind or a
re-kinding appears necessary** — that is EM-B1h's and it re-opens this packet's measurement;
⛔ **EM-B1h's no-reader arm would have to be DELETED rather than narrowed**; ⛔ **a SECOND `src/`
reader of `WORLD_PULSE_FATE_KIND` appears necessary**; ⛔ the `vocabularyTotality` row comes out
vacuous (both `setDiff`s trivially empty because the consumer re-spells the producer);
`check-writer-reach` shows **growth**; `check-observed-shape-readers` moves off 1,964;
a measured `max-lines` figure for `causeLifecycle.js` above 780;
`tests/lint/.lighting-census-baseline.json` would have to be edited here.

---

## 12. The `tests/` sweep and the LANDED-suite check (§P2 row 12, step 12) — EXECUTED

Swept by LITERAL and by VALUE (evidence §6.3, §6.5, §14).

| what | measured | verdict |
|---|---|---|
| ⛔ **EM-B1h's A6 no-reader arm** | *"a source scan proves NO file under `src/` imports `WORLD_PULSE_FATE_KIND`"*, and EM-B1h §11 makes a `src/` reader its own STOP, *"that is EM-B1i's"* | ⛔ **A DECLARED `TEST` ROW (§7 row 3).** This is the one sweep result that decides whether the packet can build at all, and it is the row EM-B1h deliberately left for it |
| ⛔ EM-B1e A6's `leashSevered` ×4 | `decreed` (`ruined_by_decree`) and `disastered` (`destroyed_by_disaster`) are `closure`-kinded **and** carry `_worldPulseInactive` + `status:'ruined'`; the bare institution has neither; `noFate` is decided by the flag | ✅ **all four unchanged — decided at `:138`/`:140`, both untouched.** A3 executes them |
| ⛔ EM-B1e A4's three digests | ⭐ **RE-MEASURED at `e80a6a3f4`: that arm no longer carries literals.** CURE-L `85cd9f4a8` re-pointed it at `tests/fixtures/.golden-freeze-register.json`; the dossier golden then moved at CURE-J `df661cb43` (a SIGNED re-record, zero rows moved), so the literal this packet compiled against is stale while the arm itself is green | ✅ **green, and §0 proves the cure does not move the witness** |
| ⛔ **the three non-`closure` words anywhere in `tests/`** | `demoted_by_disaster` **0 files** · `upgraded_by_reconstruction` **0 files** · `founded_by_flourishing` **1 file**, `tests/domain/upswingKernel.test.js:491`, asserting what the WRITER stamps | ✅ **no fixture, no pin and no golden spells a non-`closure` fate**; the one assertion is about a writer this packet never touches |
| ⛔ `worldPulseFate` in committed test DATA | **0 of every `.json` under `tests/`, `src/`, `public/`** | ✅ **no golden or dormancy fixture can move** |
| exact pins on the reader's verdict | `tests/domain/ruinInstitution.test.js` is the only suite that drives it, through `leashSevered` | ✅ measured above |
| the second vocabulary's freeze | `tests/lint/provenanceStampSingleWriter.walker.test.js:293-301` pins both sets EXACTLY, both ways | ✅ untouched, and **in `checks`** so a stray widening is caught |

---

## 13. Completion receipt

Base SHA · seal identity · final tree state · **the pre- and post-edit `max-lines` figures of
`causeLifecycle.js`** (expect **376 → 381** of 800) · exact changed files and effective-line
deltas · acceptance A1–A7 executed · ⭐ **the step-2 RED quoted verbatim** (A1's three divergence
arms failing against today's truthiness reader — the packet's own reproduction of the defect) ·
⭐ **the step-4 RED quoted verbatim** (EM-B1h's no-reader arm firing the moment the cure lands,
with its message naming EM-B1i) then green after §6.4 · **the eighteen members driven through the
predicate with the 15-destroyed / 3-not partition counted and the three named with their kinds (`closure` 15 / `rise` 2 / `standing` 1)** · ⭐ **the four
prototype words' verdicts quoted** · A6's reader-roster scan quoted whole with both controls ·
A7's re-derived record literals quoted · the six mutants planted, convicted and restored
digest-exact (§P6) · focused commands, exits and counts · ⭐ **`tests/domain/ruinInstitution.test.js`
green, with its A4 and A6 named** · ⭐ **`tests/simulation/presetLightingWitness.test.js` green** ·
the three golden digests before and at the tip · the writer-reach and observed-shape comparisons
(**recorded, never written**) · sealed `check:packet` and `implementation:resume` statuses · both
typecheck configurations · gate stages actually executed · base-versus-wave failure identity diff ·
census tuple before and at the tip with the interior red quoted verbatim · ⭐ **the measured
minified byte delta into `advanceInterval.worker` for TOOL-3 (predicted ≤194 B)** · edge-shared
verdict: **NOT OWED** · generated artifacts `NONE` · deviations `NONE | STOP` · out-of-scope
observations without investigation · **judgment calls.**

---

## 14. Noticed and NOT touched — nothing is deferred, each item carries its fate

| # | item | fate |
|---|---|---|
| **N1** | ⛔ **EM-B1h's A6 arm reds the moment this cure is written** — it was built to, and it names EM-B1i in its message. | **IN SCOPE — §7 row 3.** Narrowed to a one-file roster, never deleted. |
| **N2** | ⛔ **`LIFECYCLE_CLOSE_FATES` is NOT deliberate as a semantic choice, on the measurement** (evidence §12): born 2026-06-10 in the same hunk as `closureFateForInstitution` and equal to its three returns; frozen 2026-07-27 by an EXACT both-ways pin whose stated reason is seeded-advance drift, not completeness; and it reads `institutionHistory[].fate`, a different vocabulary, whose sibling set holds words that are not fates at all. | **OWNER/CHAIR DECISION POINT — Q4/R4.** Widening it is a lived-behaviour change (3 counted words → 15 begins damping rebuild odds where nothing damps them) AND reds a landed walker. ⛔ Not this packet's. |
| **N3** | ⚠ **The `vocabularyTotality` row EM-B1h predicted would have been vacuous.** A producer→consumer equality over the eighteen WORDS is self-referential once the consumer imports the producer. | **IN SCOPE, RESHAPED — §7 row 4** binds the KINDS instead, which is non-vacuous and convicts a future fourth kind. |
| **N4** | ⭐ **The `in`-operator prototype hazard** (evidence §7.1): `'constructor'`, `'toString'`, `'valueOf'` and `'__proto__'` all answer `true` to `in` against a frozen object literal. | **IN SCOPE — §6.1's contract forbids `in`; A2 pins all four; M3 convicts the mutant.** Worth stating estate-wide: **SLOT: the chair may want a lint arm** over `X in <frozen literal>` where `X` is unvalidated input. |
| **N5** | ⚠ **`tests/domain/causeConjunctionContent.test.js:29` cites `causeLifecycle.js:165` for `bearerSituation`, which is at `:176`** — the address is already wrong by 11 lines at this tip, before this packet moves anything. No walker reads it. | **SLOT: the chair's citation sweep** (FIX-D1 is already measuring 7,399 names), or CLOSE as prose. ⛔ Not a row here — fixing an already-stale address inside this packet would mix causes. |
| **N6** | ⚠ **The reader can be reached at most once per compromise.** A record that reaches `exposed-public` (`:386`) or `readjudicated` (`:404`) returns before TERMINAL 2 forever after. That is why the soak measured 0 divergences in 2,080 ticks even with every NPC leashed at a rise-fated institution. | **CLOSED — not work, but the reason §0.2's number is small.** Recorded so a later lane does not read "0 divergences" as "no defect". |
| **N7** | ⭐ **A demotion RENAMES the institution** (`calamityKernel.js:300`, `name: plan.demotedTo`), so a leash naming the GREATER stops resolving at all, while a leash naming the LESSER starts resolving to a `demoted_by_disaster` record. The defect's `standing` arm therefore fires through the tolerant name match, not through identity. | **CLOSED — measured, and A1 drives the resolver's real matching rule** rather than constructing the institution by identity. |
| **N8** | ⚠ **A `rise`/`standing` fate is never cleared.** The four CLEAR sites write `worldPulseFate: null` on reopen/re-raise/reactivation; nothing clears a fate from a standing institution, so the stamp — and, today, the mis-read — is permanent (§0.2's sixteen records). | **SLOT: an owner decision point if "a rise fate should age out" is ever wanted.** ⛔ Not a defect: the fate is provenance, and after this packet a standing institution's provenance no longer implies death. |
| **N9** | ⓘ **TOOL-3 sequencing**: EM-B1e's +437 B, EM-B1h's ≤1,008 B and this packet's ≤194 B all land in the uncapped `advanceInterval.worker` — **≈1.64 KB of the declared 4 KB per-train headroom.** | **SLOT: TOOL-3's first per-train attribution**, naming all three. **R2.** |
| **N10** | ⓘ `scripts/audit/cause-lifecycle-soak.mjs:171` hand-builds `worldPulseFate: 'captured_by_local_powers'` — a `closure` member, so its soak's verdicts are unchanged by this cure. | **CLOSED — not work.** Recorded so the audit script's numbers are not read as a regression after this lands. |
| **N11** | ⚠ **The preset witness is a vacuous control for THIS cure** — its realm has no corrupt NPC, so the reader is never called in it (0 calls across 8 rows × 52 ticks). It proves the golden unmoved; it does not exercise the change. | **SLOT: the chair may want a witness row with a compromised NPC** — a genuinely new instrument, not this packet's. Recorded so "the witness was green" is never read as "the cure was covered". |

---

## 15. RAISED — for the chair (every question only the chair can answer)

| # | Item |
|---|---|
| **R1** | ⭐ **CONFIRM THE SHAPE: a TOTAL kind→verdict map, not a closure-fate Set.** Measured (evidence §7.2): a `CLOSURE_FATES` Set is 9 B *more* expensive **and** hides a fourth kind behind a silent default; the frozen `DESTROYED_BY_FATE_KIND` literal is readable by `vocabularyTotality`'s own `objectLiteralKeys`, so a fourth kind REDS. If the chair prefers the Set, §6.1, §6.5, A5 and §7 row 4 change together. |
| **R2** | ⭐ **SEQUENCING vs TOOL-3.** ≤194 B into the uncapped `advanceInterval.worker`, **zero new modules in any chunk**. With EM-B1e's +437 B and EM-B1h's ≤1,008 B that is ≈1.64 KB of the declared 4 KB per-train headroom. **RECOMMEND: name all three in TOOL-3's first attribution.** Not adjudicated here. |
| **R3** | ⭐⭐ **Q4 ANSWERED, AND IT NEEDS A RULING. `LIFECYCLE_CLOSE_FATES`'s 3-of-15 restriction is nowhere argued as deliberate** (evidence §12): it was born equal to one deriver's three returns, in the same hunk, on 2026-06-10; the commit message and the only nearby comment are silent about membership; it was frozen on 2026-07-27 by an exact both-ways pin whose own prose gives seeded-advance drift — not semantic completeness — as the reason; and it belongs to `institutionHistory[].fate`, a different vocabulary (its sibling holds `built`/`reopened`, which are not fates). ⇒ **the honest reading is "a mirror, never re-examined", not "a decision".** Widening it is a lived-behaviour change AND reds `provenanceStampSingleWriter.walker.test.js`. **SLOT IT: EM-B1j (which already owns that vocabulary), or a named follow-on with an owner-signed shift.** ⛔ Not derived here. |
| **R4** | ⭐ **CONFIRM THE `vocabularyTotality` ROW'S SUBJECT.** EM-B1h's N9 predicted a row over the eighteen WORDS; measured, that row would be self-referential and vacuous (§P6). This packet binds the KINDS instead. Confirm — it is the difference between a guard and a tautology. |
| **R5** | ⚠ **THE CONSERVATIVE UNKNOWN-FATE CONTRACT, AND WHAT IT COSTS.** An unknown truthy fate keeps reading as destroyed forever. No retired spelling exists in this repo's history and `worldPulseFate` appears in 0 committed JSON files — but **a user's saved world is not in this repo.** The cost is one-sided (an unknown word can only read *too destroyed*, which is today's behaviour). Closing the READ side needs a migration and is a different packet. **Confirm the contract, or slot the migration.** |
| **R6** | ⭐ **THE LIVED-BEHAVIOUR PRICE, FOR THE OWNER'S EYES.** No golden moves and no measured pulse run diverged in 2,080 ticks — but the change IS lived: a DM who would have been told *"Aldra Vane is cut loose — Sustaining institution 'Wizard's Tower' destroyed"* about a standing tower will now be told nothing, and the reeve stays compromised. **That is the fix, and it is also a change to what the chronicle says.** No signed door is owed by the machinery; the chair may still want the owner to see §0.1 before the train closes. |
| **R7** | ⚠ **A6'S ARM AND EM-B1h's ARM ASSERT THE SAME FACT FROM TWO SIDES.** That is deliberate (the walker guards the estate; A6 guards the packet), but it is duplication. **Confirm, or drop A6 and spend the eighth acceptance slot elsewhere.** |
| **R8** | ⚠ **N4's estate-wide shape:** `X in <frozen object literal>` with unvalidated `X` is a live hazard class beyond this packet. **Does the chair want a lint arm for it?** Measured here, not adjudicated. |
| **R9** | ⚠ **N11: the preset witness never reaches this reader.** Its green is a real proof that the golden is unmoved and **not** a proof of coverage. **Does the chair want a witness row carrying a compromised NPC** (a new instrument, not this packet's), so the pulse's corruption path is byte-witnessed at all? |
