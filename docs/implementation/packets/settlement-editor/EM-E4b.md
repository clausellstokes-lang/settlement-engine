# `settlement-editor / tests-only mode` — EM-E4b: THE FORK SITES CONSULT THE PINS (RECORD)

- **Status:** `LANDED`
- **Packet version:** `record-1` — written after the fact by the HISTORIAN seat (U3), 2026-09-23
- **Landed at:** `d238afd86fe99b815ef94b2590399cb354965974` — EM-E4b (cure): the tuning register refrozen — pulseKernel.js joins TRAD_TUNING's dependents, no dial moved
- **Verified base:** `feat/em-e4b-fork-sites` at `ad9321796533647899e4f39d70028e5eb636b317`
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: TO BE STAMPED BY THE CHAIR)

⛔ **A RECORD, NOT A DISPATCHABLE CONTRACT.** This member was built under TESTS-ONLY MODE — the owner, 2026-09-23 ~09:1x, *"Can we just have it where we only have the testing apparatus? As we did so many months ago?"* → *"Then do it and start building! Thank you"* (`docs/OPERATING_STANDARD.md` rule 39; stasis row S11). Under that word the estate's own ritual — packets, seals, placements, picks, flips, count provers, pre-proofs — is PARKED, never deleted, and the record is the branch's commit receipts, the progress board and this file. It pins no symbol, reserves no path, and may not be dispatched; the authority that governed the build is §1's brief and the design section that brief cites. The unfreeze train restores the ritual (U1, U3).

## §1 — The brief this member was built from

**The brief:** `briefs/launch/BUILD-EM-E4b-tests-only.md` (judgment 270).

**The charge, in one sentence.** The pulse's head composes ONE pin bag and the registered fork sites consult it, so a pinned fork returns its outcome without ever consuming the stream.

## §7 — What moved (the commits, and their files)

| commit (on the train) | on its branch | subject |
|---|---|---|
| `8ea9c3a32` | `0b8df9f90` | EM-E4b: the pulse's head composes one pin bag and the registered fork sites consult it, a pinned fork returning its outcome without ever composing its draw |
| `d238afd86` | `c0ef2cffe` | EM-E4b (cure): the tuning register refrozen — pulseKernel.js joins TRAD_TUNING's dependents, no dial moved |

| action | path |
|---|---|
| MODIFY | `src/domain/worldPulse/pulseKernel.js` |
| MODIFY | `src/domain/worldPulse/traditionsKernel.js` |
| MODIFY | `src/domain/worldPulse/warSiegeVerdict.js` |
| REGISTER | `tests/lint/.tuning-inventory.json` |

Superseded inside this member (created and then renamed or removed by its own later commit, so the manifest carries only what stands in the tree): `tests/simulation/forkSitesConsultPins.test.js`.


## §9 — The acceptance cases, by title, as the commits record them

- **E4b-1** — a staged pin on HBF-85 yields the pinned outcome AT THE TICK through the real `simulateCampaignWorldPulse`, the year root is never composed, the unpinned control composes exactly one, and a second word proves the site is not constant.
- **E4b-2** — the siege verdict consults its pin over all four bands: the band is the pin's, `falls` is the band's, `roll` is 0, the fork count is 0, and the unpinned verdict is object-equal to today's on every fixture roll.
- **E4b-3** — zero pins ⇒ the pulse record is byte-identical to a world holding no decree, and two dormant ticks hand back ONE frozen bag by reference.
- **E4b-4** — a word outside the vocabulary never reaches the site — the festival answers the DRAWN word (a positive equality), the entry still lands as a cause, and the siege's four READING words are refused at the fold while a band is admitted.
- **E4b-5** — the fold precedes the apply on ONE shared line, and the kernel's effective line count equals `scripts/.size-baseline.json`'s banked 1,581.
- **E4b-6** — each fork's pin words are its own draw's, collected from the live `outcomeForDraw` and the live `resolveSiegeVerdict` rather than transcribed.
- **E4b-7** — the bag rides the growth chain's argument bag through the three leaves this member does not own, executed through the DENSITY head the kernel calls.
- **E4b-8** — the declared gap above, asserted.

## §10 — The checks the builder ran

- `node scripts/implementation-packets.mjs validate`

## §12 — How it reached the train

Branch `feat/em-e4b-fork-sites`, built at `0b8df9f90`, `c0ef2cffe`; pushed as TRANSPORT (rule 24a — a lane push opens no pull request and merges nothing) and read on GitHub Actions (runs `35883383624`, `35887956452`). The chair read the run against the DECLARED reds — the two census cars under the `CENSUS_STASIS` switch (stasis S6), the lighting walker's title count, the observed-shape family's level at the branch's base (U37, closed by the schema-23 rung at `8cc2926d0` + `3b21ba791`) and the declared phone-floor rows (U25, re-recorded once at the tip by `56b085fc5`) — and MERGED it into the train branch `em-train-17-2026-09-23` by pick: `8ea9c3a32`, `d238afd86`. Nothing was pushed as a landing; the pull request, the merge to `master`, the deploy and every migration remain the OWNER's.

A THIRD commit at the TIP belongs to this member's story: `76ae8bce8`, the chair's cure renaming `forkSitesConsultPins.test.js` off the mutation manifest's enrolment word. That red had been MASKED on every one of this branch's runs by EM-E4's inherited red in the SAME test file — hazard 56 and judgment 279 (b): a red is attributed by the assertion's offending NAMES, never by the failing test file. HBF-86's last hop stays blocked in `warDeployment.js` (U35).
