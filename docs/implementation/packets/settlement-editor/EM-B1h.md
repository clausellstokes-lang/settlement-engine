# Settlement editor / EM-B1h — `worldPulseFate` becomes a CLOSED vocabulary with a declared KIND per member, the one guarded writer refuses a foreign word, and a totality walker holds the six kernels to it

- **Status:** `LANDED`
  ⚠ The status value above stands ALONE on its line because `parsePacketHeader`
  (`scripts/implementation-packets.mjs`) anchors the status row at end-of-line (J-TEWF1B-1).
- **Landed at:** `45f57c6cf635ea513f45a529f5c45781e0565ced` — the seventeenth landing — 6 files: `src/domain/worldPulse/worldPulseFates.js` (the closed, kinded fate vocabulary), the one guarded writer in `src/domain/worldPulse/calamityKernel.js`, the totality walker over the six kernels, its own prose-numerics re-address (745 → 747), its coupling-inclusion row and a mutation-coverage row. TRAIN EM-T9: sealed and built on `em-t9-b1h-2026-09-21` (`aad3499af`), surviving a usage-limit cut mid-build with its worktree intact; six mutants each convicted by its named arm; sealed proofs green (window 16); the wiring census moved by its declared +1
- **Packet version:** 6
  - ⭐ **What version 6 changed, and why (the EM-T9 PRE-PROOF, 2026-09-21, read tip
    `e80a6a3f443e2f6a36e28fbebb72e98e46869fac`):** the J-T1 window over all fifteen
    change-manifest and `requiredSymbols` paths is **EMPTY** and all eighteen required symbols
    resolve verbatim, so no fact moved — but three things were added. ⭐ **§7 gains ROW 6,
    `tests/lint/.prose-numerics-baseline.json`** (P2.7(b)): the walker freezes debt by exact
    path + line + snippet and carries ONE row on `calamityKernel.js` at `:745`, above which this
    packet inserts two lines, so the row re-addresses 745 → 747 — measured path-disjoint from
    every other EM-T9 member. **§10 gains interim rule 8's EXCLUDED DIRECTORY RUN**, the
    instrument that surfaces that red before the landing. **The header's quoted preamble hash is
    WITHDRAWN** for the measured one. Fifteen line addresses are refreshed to the tip and the
    2,246-file population gains its as-of mark. ⛔ **No contract, budget, acceptance case,
    required symbol, check, non-goal or register DELTA moved**, and no existing change row moved.
  - ⭐ **What version 5 changed, and why (ESTATE-REPAIR-6, 2026-09-21, read tip
    `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`; judgment 33): the THREE sentences that counted on
    the UNSPLIT `EM-B2a` as a contender for `scripts/mutation-coverage-manifest.json` — the
    collision group, §4's preflight INSTRUCTION and §7's P2.2 ledger — now name the MEASURED holder
    set (**EM-A1, EM-A3, EM-B1b, EM-B2a3, EM-B2a4, EM-C4a**) and instruct the build lane to
    re-measure FROM THE CAPSULES rather than from a list.** ⛔ **No contract, budget, acceptance
    case, required symbol, change row, check or non-goal moved, and the capsule is UNTOUCHED.**
  - Version 4 is the DOCS repair only (ESTATE-REPAIR-3, 2026-09-20, read tip `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`),
    bringing this packet under `COMPILE-RULES.interim.md`: §7's registration table gains the
    **P2.13 DEFERRED WIRING-CENSUS ROW** it had OMITTED (interim rule 1); the live `268`
    first-paint absolute becomes a measurement with an as-of mark (interim rule 9); and the
    manifest's `REGISTER` row on `scripts/mutation-coverage-manifest.json` gains the
    **`rowKey`** field TOOL-27 keys its row-level exemption on. ⛔ **No contract, budget,
    acceptance case, required symbol, change row or non-goal moved.**
  > **WHAT VERSION 3 CHANGED AND WHY.** The DOCS repair only (ESTATE-REPAIR + ESTATE-REPAIR-2,
  > 2026-09-20): every mutation-coverage absolute became a delta, and the stale
  > *"EM-B1d [READY] reserves that exact path"* premise was retired at all four places it lived
  > (this header's revalidation sentence, the collision-group row, §4's preflight and §7 P2.2) —
  > EM-B1d is LANDED and `LANDED` reserves nothing. ⛔ **No contract, budget, acceptance case,
  > required symbol or non-goal moved.** Version 2 is in `pre-estate-repair-2026-09-20/`.
  >
  > **WHAT VERSION 2 CHANGED AND WHY.** Re-compiled at the same tip under the chair's rulings
  > (ODQ §934.47 addendum 28; charter, *Amendments of 2026-09-19 19:10 EDT*). **Q2** fixed the
  > home as the zero-import leaf. **Q3** fixed `null` as the CLEAR sentinel with four declared
  > `clear` roster rows. **Q4** added a declared KIND per member. Four things then came out of
  > the measurement that version 1 did not have:
  > ⭐ **THERE ARE THREE KINDS, NOT TWO — and the third one is not where the chair expected.**
  > The six names the chair flagged for a second look (`downsized`, `privatized`,
  > `captured_by_local_powers`, `reduced_to_watch_post`, `hollowed_out`, `survives_as_remnant`)
  > are **plain `closure`**: their single writer, `deactivateForDemotion`, sets
  > `_worldPulseInactive: true` and a non-active status for **all seven**, however gentle the
  > word. The member that needs the third kind is **`demoted_by_disaster`**, which the chair did
  > not flag: its writer renames the institution in place, changes **no status** and sets **no
  > flag** — its own comment says *"keeping the slot standing"* (§6.1, measured by execution).
  > ⇒ ⛔ **EM-B1i's defect population is THREE, not two.**
  > ⭐ **A REGISTRATION ROW VERSION 1 MISSED, FOUND BY SWEEPING THE REGISTERS FOR A NEW
  > `src/domain/worldPulse/` FILE:** `tests/lint/couplingInclusion.walker.test.js` is a census
  > **TOTAL over `src/domain/{worldPulse,spatial}`** and *"a NEW unlayered module REDS"*. The new
  > leaf owes an `ARGUED_UNLAYERED` entry and **`ARGUED_ROSTER_CEILING` 28 → 29 in the same
  > diff** (an EXACT equality). That is a FIFTH change-manifest row (§7, R9).
  > ⭐ **THE GUARD MOVES INTO THE LEAF** (R3's second question): `assertWorldPulseFate` measured
  > **98 B** in the kernel against **204 B** for the inline predicate, and **+2 effective lines**
  > against +4 — so the kernel is touched less and the message is spelled once (§3.3).
  > ⭐ **THE SHAPE IS THE KIND MAP AS THE SINGLE AUTHORED SOURCE**, the flat list derived from
  > it: **910 B against 1,248 B** for two literals side by side, and "a member with no kind"
  > becomes *unrepresentable* rather than *detected* (§6.2).
  > Also: the second vocabulary is measured member by member (§13) — `LIFECYCLE_CLOSE_FATES` is a
  > **strict subset, 3 of 15**, and `institutionHistory[].fate` is a **different, overlapping**
  > vocabulary (17 values; 12 shared, 5 its own, 6 ours) — and the leaf's `status:`-freedom is now
  > an acceptance arm of this packet (A7) rather than only a coding instruction.
- **Verified base:** `em-t9-b1h-2026-09-21` at `7f0621fdb72f769a285502ff5481247c1f07b04b`
  ⛔ Stamped by the chair at promotion; the value stands ALONE on the row above, because
  `parsePacketHeader` anchors `Verified base` at end-of-line exactly as it does `Status`
  (interim rule 5). ⭐ **MEASURED BY THE EM-T9 PRE-PROOF WITH A FOUR-CELL CONTROL, through the
  tip's OWN parser:** value alone + a real sha → the sha RESOLVES; the earlier form (a trailing
  `— ⛔ stamped by the chair…` clause on the same line) + the SAME real sha → **`verifiedBase: null`**;
  value alone + the `__BASE__` placeholder → `null` (the placeholder is not a sha, which is why
  the chair's stamp is mandatory); the chair's placement script → resolves, because it replaces
  the WHOLE line. ⇒ the defect was real and was MASKED by the stamping script; a stamp that
  rewrote only the backticked value would have parsed `null` at placement for a reason unrelated
  to content. `Status` was already clean and stays clean. Compiled and measured
  whole at `ad7ddf2c9` (EM-B1e LANDED; EM-B1d version 5 PLACED and building). ⓘ **THE
  REVALIDATION SENTENCE THE CHAIR USES AT PROMOTION:** *Compiled at `ad7ddf2c9` by the Opus
  COMPILE lane (session 7d3418f8, 2026-09-19), version 2 under ODQ §934.47 addendum 28. Over
  every change-manifest and `requiredSymbols` path, re-run `git diff --stat <base> HEAD` in the
  SAME command as the stamp; both CREATE targets must be absent on disk and unknown to git; all
  EIGHTEEN `requiredSymbols` rows resolve verbatim (`grep -cF` → 1 each, evidence §9);
  `scripts/mutation-coverage-manifest.json` must be UNRESERVED by any non-terminal packet
  (⚠ **EM-B1d is LANDED**, and `LANDED` is terminal and reserves no change path
  — `implementation-packets.mjs`'s `TERMINAL_PACKET_STATUSES`; re-measure at dispatch, because the
  live contenders are the WAITING DRAFTS, not the registered estate); and
  `ARGUED_ROSTER_CEILING` must still read **28** with
  `ARGUED_UNLAYERED` holding exactly 28 keys (§7 row 5). ⭐ **THE POST-EDIT SIMULATION CUT ONE
  ROW BEFORE IT WAS WRITTEN:** `const ARGUED_ROSTER_CEILING = 28` is NOT a `requiredSymbols`
  row, because row 5 RE-SPELLS that line (28 → 29) and `requiredSymbols` is asserted verbatim at
  every status — the EM-P3 lesson, and the row the chair cut from EM-B1d version 5. The stable
  prefix `const ARGUED_ROSTER_CEILING` is named instead, and it survives the edit.*
- **Last revalidated:** `7f0621fdb72f769a285502ff5481247c1f07b04b` — the chair's stamp.
- **Depends on:** **EM-B1e — LANDED at `8f714cf3ceabef10c735f23b05783c7cddfcba34`.** A real
  dependency: this packet's guard lives inside `ruinInstitution`, which EM-B1e created. Charter,
  *Amendments of 2026-09-19 18:10 EDT* (§934.56): *"SLOT: train EM-T6, after EM-B1f and BEFORE
  EM-B1a … compiled when EM-B1e has landed."* EM-B1f touches `src/domain/roads/state.js` and
  shares no path with this packet.
  ⛔ **EM-B1a depends on this packet** (its `ruined` arm passes `'ruined_by_decree'`, which the
  guard would otherwise REFUSE) and ⛔ **EM-B1i depends on it** — EM-B1i's cure is *"that reader
  tests the `closure` KIND EM-B1h declares"* (§934.47 addendum 28).
- **Collision group:** ⛔ **`scripts/mutation-coverage-manifest.json` — CONTENDED.**
  ⚠ **EM-B1d is LANDED**, and `LANDED` is a TERMINAL status that reserves NO change path
  (`implementation-packets.mjs`'s `TERMINAL_PACKET_STATUSES`), so the version-2 premise
  ("EM-B1d [READY] reserves that exact path") is withdrawn. **Re-measured at dispatch — at
  `bdbf7c89c` the registered estate holds no non-terminal packet at all.** The live contenders are
  the WAITING DRAFTS whose `changeManifest` HOLDS the same path — **EM-A1, EM-A3, EM-B1b,
  EM-B2a3, EM-B2a4 and EM-C4a**, re-measured over all 29 waiting capsules at `bdbf7c89c`
  (ESTATE-REPAIR-6). ⛔ **The unsplit `EM-B2a` no longer exists** — judgment 33 split it into
  a1–a4 behind the EM-R family, and **a1 and a2 hold NO row on this path**; **EM-C4a** is the
  contender the earlier list did not name. None is
  registered, so none reserves anything until it is placed, and the cure is ORDER exactly as
  before. This packet's row is anchored at the file's TAIL, far from EM-B1d's head-of-file insert.
  **`tests/lint/couplingInclusion.walker.test.js` is FREE** — ten packets name it, **all LANDED**
  (evidence §17). `src/domain/worldPulse/calamityKernel.js` is held only by **EM-B1e [LANDED]**.
  ⚠ One shared path with whichever train this joins: `tests/lint/.lighting-census-baseline.json`
  (that train's terminal act; ⛔ never this packet's — §7).
- **Commit authority:** edits only; the chair commits.
- **Baseline posture:** **measured**, not inherited, at `ad7ddf2c9`. Executed: the thirteen write
  sites and two read sites by a comment-only strip over 2,246 non-test `src/` files; ⭐ **each
  write site CLASSIFIED by the sibling keys of its own record literal** (§6.1); every value traced
  to a literal or a named deriver, and each deriver's returns enumerated over its WHOLE file
  history; the homonym table and trigger derived twice by independent readings that agree; the
  FLAGGED set proved set-equal to the six writer files both ways; effective lines with eslint's
  own `Linter` against a control reproducing a frozen `.size-baseline` value exactly; bundle
  membership from the repo's own `EAGER_FIRST_PAINT_MODULES` export and a verbatim copy of
  `computeEagerModuleGraph`'s walker; edge-shared INPUT membership against all five metas (307
  inputs); ⭐ the coupling-inclusion census read for what a NEW `worldPulse` module owes; the
  second vocabulary tabulated member by member; and every byte figure priced with esbuild on both
  candidate shapes. ⛔ **NO CENSUS ABSOLUTE IS QUOTED** — only the DELTA (§7). **No test was run.**
- **Preamble:** docs/implementation/preambles/EM-PREAMBLE.md (SHA-256: TO BE STAMPED BY THE CHAIR)
- **Interim compile rules:** `COMPILE-RULES.interim.md` (the chair, 2026-09-20) — the fifth amendment's eleven rules, obeyed before they land. Where this packet and `EM-PREAMBLE.md` (SHA-256 `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`, measured with `shasum -a 256` at the read tip `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d` on 2026-09-20 — the FOURTH amendment, still the tree's live text) disagree, the interim rules govern and this row is the record of it.
  > ⓘ **MEASURED, NEVER QUOTED (interim rule 9; THREE LAWS rule 2).** At `e80a6a3f4`
  > `shasum -a 256 docs/implementation/preambles/EM-PREAMBLE.md` →
  > `c9f33c8d2940372bde8a6d931e3d389b79516a90ed45405cbbbf3e698cc46675`, which EQUALS the value
  > the interim-rules row above already carries, so the §4 preamble STOP does not fire. ⛔ The
  > earlier sub-note quoting `b90a95b7…caa5e1` (a value a dispatch message carried) is WITHDRAWN:
  > it is not this tree's text. Measured twice at this tip — by this pre-proof and, independently,
  > by the EM-T8 probe lane (`EM-B3d.STOP.md` §4). Left unstamped by design.

---

## 0. What the measurement corrected, in both versions

The charter's premise — `worldPulseFate` is an OPEN vocabulary and FINITE-SEMANTICS forbids it —
is **CONFIRMED**. Its figures were corrected by version 1 and the charter has been amended:
**THIRTEEN write sites in SIX files** (five literal, four computed through three derivers, four
writing `null` to CLEAR), **SEVENTEEN live values**, and `ruined_by_decree` is the **eighteenth**.

Version 2 adds one more correction, and it is the chair's own second-look list turned around:

| the chair expected | MEASURED (§6.1) |
|---|---|
| `downsized`, `privatized`, `captured_by_local_powers`, `reduced_to_watch_post`, `hollowed_out`, `survives_as_remnant` might be *"diminished but standing"* | ⛔ **All six are plain `closure`.** Their one writer, `deactivateForDemotion` (`tierOutcomeApply.js:156`), sets `_worldPulseInactive: true` and `status: fate.status` (`remnant` or `removed`) for **all seven** demotion fates. The gentle words are PROSE; the record is a deactivation. |
| two kinds would do | ⭐ **THREE.** `demoted_by_disaster` — not on the chair's list — is written by a branch that changes **no status** and sets **no flag**, renaming the institution in place. Its own comment: *"rename in place, keeping the slot standing."* |

⇒ ⛔ **EM-B1i's defect population is THREE members, not two**: a demoted wizard's tower, a
reconstructed guild and a flourishing academy all read as *destroyed* at
`causeLifecycle.institutionDestroyed` today, and all three sever a criminal leash. **R10.**

---

## 1. Reconciled authority

1. ⭐ **THE CHAIR'S RULINGS ON THIS COMPILE (ODQ §934.47 addendum 28; charter, *Amendments of
   2026-09-19 19:10 EDT*):** the HOME is the zero-import leaf `src/domain/worldPulse/worldPulseFates.js`
   *("the kernel's own static closure is 327 modules against one — the placement before the
   ceiling")*; `null` is the CLEAR sentinel, never a member, and the four clear sites are declared
   `clear` roster rows; the eighteen are ONE vocabulary carrying a declared KIND per member,
   *"declared as DATA in this packet and read by nobody yet, so the packet stays golden-neutral
   (it freezes, it does not cure)"*; and the second vocabulary is measured, not assumed.
2. ⭐ **THE CHAIR'S ANSWER TO EM-B1e R2 (ODQ §934.47 addendum 24), which created this packet:**
   *"EM-B1h (the fate vocabulary closed: one frozen `WORLD_PULSE_FATES` and a totality walker over
   its writers), chartered to land BEFORE EM-B1a makes `ruined_by_decree` producible —
   FINITE-SEMANTICS is the estate's law and an open vocabulary is where a ninth spelling lands
   unnoticed."*
3. **`docs/DESIGN_EDIT_MODE_AND_DECREES.md` §15, the *Ops* bullet** — *"The vocabularies are
   FINITE-SEMANTICS pools, never free text, and the readers that consume them are named in the
   packet that adds each field."* §12 GOVERNS; nothing in §12 bears on a pulse-side vocabulary.
4. **The estate's own precedent for the FORM:** `src/domain/worldPulse/envoyErrandVocabulary.js` —
   *"THE CLOSED WORDS — … One spelling, one file: two spellings of a journey would let an import
   forge a state the writer never authored (**FINITE-SEMANTICS LAW**). … this leaf has **ZERO
   imports**. A module that reaches nothing cannot pass truth along."*
   And for the coupling ROW: `src/domain/worldPulse/bandFamilies.js`'s `ARGUED_UNLAYERED` entry —
   *"SP substrate — the shared band/severity vocabulary, spelled by every layer"*, `reads: []`.
5. **EM-B1e (LANDED)** §6's contract for `ruinInstitution`. This packet **adds one call and
   changes nothing else about the writer.**
6. **THE PROMISE** — lived history is immutable. ⛔ The vocabulary is derived FROM what the pulse
   already writes; **no saved record becomes invalid and no record's bytes move.**
7. **`EM-PREAMBLE.md`** §P2 (rows 1–4, 10, 11, 12), §P3, §P6, §P7, §P8 — cited by hash.
8. Live code at the tip, which supplies every figure in §0, §5, §6.1 and §13.

**Resolved contradictions:** none outstanding. Version 1's four (the charter's figures, the
compile brief's census sentence, EM-B1e's `ruined_by_decree` absence claim, DESIGN §15's stale
`EntityStatus` line) are **all CLOSED by the chair** — the charter is amended, the brief and the
design are corrected, and EM-B1e's figure is recorded as superseded by its own landing.

---

## 2. Outcome

**Observable result:** `WORLD_PULSE_FATES` is a frozen, exported, zero-import vocabulary of the
**eighteen** words the world pulse may stamp on `worldPulseFate`, **each carrying exactly one
declared KIND** — `closure`, `standing` or `rise`; `ruinInstitution` **refuses a fate outside it**
through the leaf's own `assertWorldPulseFate`; and a totality walker asserts that every site in
`src/` that writes `worldPulseFate` spells a member, reads the vocabulary, or is a declared CLEAR
site, and that no reader enumerates the value without joining the roster.

**Definition of done:** the leaf exists with eighteen members in codepoint order and one kind
each; `ruinInstitution` throws on a non-member; the walker's four arms are green; the
mutation-coverage row and the `ARGUED_UNLAYERED` row are appended, the latter with its ceiling
raised in the same diff; `ruined_by_decree` is a DECLARED member no writer can produce until
EM-B1a; and **no golden, no byte of any produced record, and no line of the five unedited writer
files moves.**

⛔ **NOBODY READS THE KIND IN THIS PACKET.** It is frozen DATA. The walker asserts its shape; no
production code branches on it. **EM-B1i is the packet that makes `institutionDestroyed` read it**,
and that packet — not this one — owns the golden and preset-witness measurement and, if lived
behaviour moves, the owner's signed door.

In scope: (1) **one behaviour family** — the pulse's fate words become a closed, kinded set;
(2) the one required integration — the guarded writer refuses a foreign word;
(3) the prevention guard — `tests/lint/worldPulseFateTotality.walker.test.js`.

Explicit non-goals: the op that will produce `ruined_by_decree` (**EM-B1a's**); ⛔ **making any
reader test the kind (EM-B1i's)**; ⛔ **deriving `LIFECYCLE_CLOSE_FATES` from the `closure` kind —
that would be a READER of the kind, and this packet has none** (§13, R6); re-pointing the five
unedited writer files (they already spell members, and one of them is at 798/800); the
`institutionHistory[].fate` vocabulary (§13, R4); the `EntityStatus` / `NpcStatus` unions; any
golden, tuning, migration, surface or paid-surface behaviour.

---

## 3. Hard scope budget

| Limit | Packet budget | Standard |
|---|---:|---:|
| Behavior families | `1` | 1 |
| New persisted record families | `0` — the key already exists on saved records | ≤1 |
| Named state writers | `0` — this packet adds no writer; it CONSTRAINS the existing one | ≤1 |
| Feature flags / user-facing surfaces | `0` each | ≤1 each |
| Direct production consumers | `1` (`calamityKernel.js`; EM-B1a and EM-B1i are later) | ≤2 |
| **New logic-bearing production leaves** | **`1`** — the vocabulary leaf (§3.2) | ≤2 |
| **Existing logic-bearing production files modified** | **`1`** — `calamityKernel.js` only | ≤3 |
| New TEST leaves | `1` walker | n/a |
| **Additional registration-only files** | **`3`** — the mutation-coverage manifest, the coupling-inclusion register and (v6) the prose-numerics baseline | ≤3 |
| Handwritten files total | **`6`** | ≤12 |
| Effective lines in the new leaf | **≈55 projected** (18 members + 18 kind rows + the two-part gloss) | ≤250 |
| Effective lines in the walker leaf | **≈240 projected** (EM-B1d's executed 232 is the yardstick) | ≤250 |
| **New/changed effective production lines** | **≈57; cap ≤75** (≈55 leaf + **2** in `calamityKernel.js`) | ≤400 |
| Delta in a shared/hot file | `0` — **no edited file is hot** (§3.1) | ≤15 |
| Acceptance cases | `7` | ≤8 |

Overrides approved before dispatch: `NONE` — the packet fits the default.

### §3.1 · Hot files — MEASURED, and the hot one is NOT edited

`PACKET_STANDARD.md`'s standing hot list names **none** of the six writer files (`grep -c` → 0 for
every basename). Measured with eslint's own `Linter` (`skipBlankLines`, `skipComments`) against
the `src/domain/**` ceiling of **800** (`eslint.config.js:687`):

```
calamityKernel.js                 457 / 800   HEADROOM 343   ← the ONLY edited file (+2 eff)
institutionLifecycle.js           798 / 800   HEADROOM   2   ⛔⛔ HOT, AND NOT ON THE LIST (R3)
upswingKernel.js                  640 / 800 · settlementLifecycleFirstClass.js 462 / 800
tierOutcomeApply.js               228 / 800 · magicRegimeLifecycle.js          200 / 800
CONTROL roadsKernel.js            838  —  EXACTLY its frozen .size-baseline value of 838
```

⇒ The control proves the counter reproduces eslint's own arithmetic. **`institutionLifecycle.js`
is a hot file by the standard's own definition and is absent from its list** — the chair adds the
row at its next sitting (R3); **this packet does not touch that file**, and §6's derivation rule is
what buys that.

### §3.2 · The home — ⭐ RULED BY THE CHAIR (Q2), on the compile's measurement

`src/domain/worldPulse/worldPulseFates.js`, a **zero-import leaf**. Measured at `ad7ddf2c9`:

| | generation worker (**1,401,208, EXACT, zero slack**) | eager first paint | lazy engine | `advanceInterval.worker` | edge-shared inputs | closure a consumer inherits |
|---|---|---|---|---|---|---:|
| ⭐ **the leaf** | **NO** | **NO** | **NO** | YES (via `calamityKernel.js`) | **NONE** | ⭐ **1 module** |
| `calamityKernel.js` | **NO** | **NO** | **NO** | YES | **NONE** | ⛔ **327 modules · 7,093,875 raw bytes** |
| `src/domain/entities/status.js` | **NO** | ⛔ YES | **NO** | YES | ⛔ **TWO bundles** | — |

**The zero-slack budget is untouchable by any candidate** — a membership fact, not an estimate.
The tree-shaking question is **moot by placement**.

### §3.3 · ⭐ THE GUARD LIVES IN THE LEAF (R3's second question), and the bytes say so

| option | the kernel's edit | kernel bytes (minified) | kernel eff lines |
|---|---|---:|---:|
| **A** (version 1) — the predicate inline: `import { WORLD_PULSE_FATES }` + a local `Set` + the throw | 3 statements | **204 B** | **+4** |
| ⭐ **B (CHOSEN)** — the leaf exports `assertWorldPulseFate(fate, caller)`; the kernel calls it | 1 import + 1 call | ⭐ **98 B** | ⭐ **+2** |

⇒ **B is 106 B cheaper in the kernel and touches two lines instead of four**, and the error
message is spelled ONCE, in the leaf, where **EM-B1a and EM-B1i will reuse it** instead of
re-spelling a third and fourth copy (which is the very defect this packet exists to end).

**The bytes, final.** esbuild `--minify` on the exact drafted sources (evidence §6): the leaf is
**910 B** and the kernel fragment **98 B** ⇒ ⭐ **≤1,008 B, a stated UPPER BOUND** (esbuild
preserves export names per module; rollup scope-hoists and mangles them inside the worker chunk).
Version 1's figure was 739 B; **the +269 B is the KIND data the chair ruled in.** It lands in
`advanceInterval.worker`, which has **no byte ceiling today**; TOOL-3's ruled style gives it a
measured ceiling with a **declared 4 KB headroom ratcheted per TRAIN**. ⇒ **no budget obligation,
no TEST row for a budget file, no build step.** **R2.**

---

## 4. Sealed dispatch and preflight

```sh
git rev-parse HEAD && npm run implementation:dispatch -- EM-B1h
```

| # | check | verdict at `ad7ddf2c9` |
|---|---|---|
| 1 | `dispatch branch mismatch` | the worktree must sit on the verified branch — the build lane's own act |
| 2 | `verified base is not an ancestor of HEAD` | passes while the stamped base is the tip or a proved-non-interfering ancestor |
| 3 | `verified-base descendant changed declared substrate` | passes when the base is the tip |
| 4 | `capsule omitted declared substrate` | passes; the capsule is built from the same list |
| 5 | `CREATE target must be absent and Git-clean` | ✅ **both absent on disk and unknown to git** |
| 6 | `non-CREATE target must be Git-clean` | passes on a clean tree |

⚠ **THE WORKTREE IS SHARED.** Re-read `git rev-parse HEAD` in the same command as the dispatch.
⛔ **TWO PREFLIGHTS CAN REFUSE THIS PACKET:** (a) `scripts/mutation-coverage-manifest.json`
reserved by a non-terminal packet — ⚠ **EM-B1d is LANDED and reserves nothing**
(`TERMINAL_PACKET_STATUSES`); ⭐ **re-measure at dispatch by READING THE CAPSULES, never by reading
this list** — every waiting manifest whose `changeManifest` holds that path, of which only the ones
PLACED by then reserve anything, since a waiting draft reserves nothing until it is registered. As
of `bdbf7c89c` that set is **EM-A1, EM-A3, EM-B1b, EM-B2a3, EM-B2a4 and EM-C4a** (ESTATE-REPAIR-6,
measured over all 29 waiting capsules). ⛔ **The unsplit `EM-B2a` the earlier list named NO LONGER
EXISTS** — judgment 33 split it into a1–a4 and sequences the family AFTER the EM-R members, and
**a1 and a2 declare NO row on this path** (each says so in its own §7), so only a3 and a4 can ever
contend here and both land far behind this packet; **EM-C4a** is the contender the earlier list did
not name; (b)
`ARGUED_ROSTER_CEILING` no longer reading **28** with exactly 28 `ARGUED_UNLAYERED` keys, which
would mean another lane moved the register under this packet (§7 row 5, §11).

---

## 5. Verified tree contract

Every row found **by symbol** at `ad7ddf2c9`; `grep -cF` counts in evidence §9.

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| ⭐ **THE GUARDED WRITER** | `src/domain/worldPulse/calamityKernel.js` | `export function ruinInstitution` (`:242`) | `:243` `if (typeof fate !== 'string' \|\| fate === '') throw new TypeError(…)`; `:244` the same for `reason`; returns five keys in a fixed order | ⭐ **Gains ONE CALL** to the leaf's assert, **between** the two existing checks. ⛔ The signature, the five keys and their order are byte-identical |
| **Its two call sites** | `src/domain/worldPulse/calamityKernel.js` | `:317`, `:321` | Both pass `fate: 'destroyed_by_disaster'` — a MEMBER | ⛔ **Untouched**; the guard is inert for them (A1) |
| **Writer — LITERAL, kind `standing`** | `src/domain/worldPulse/calamityKernel.js` | `:309`, inside the demote branch (`:304-311`) | `worldPulseFate: 'demoted_by_disaster'` — ⭐ **and the record literal carries NO `status:`, NO `_worldPulseInactive`, NO `_worldPulseEconomyClosed`** (measured, §6.1); the selection at `:290` requires `status === 'active'`; the branch's own comment reads *"rename in place, keeping the slot standing"* | ⛔ **Untouched** — already a member. ⭐ **Its kind is `standing`, and that is the packet's sharpest measured fact** |
| **Writer — DERIVED ×2 + CLEAR ×2** | `src/domain/worldPulse/institutionLifecycle.js` | `:1019`, `:1061`; `:959`, `:1103` | `:1013` `const fate = patch.fate \|\| closureFateForInstitution(target);` · `:1055` `const fate = patch.fate \|\| 'abolished';` · two sites write `worldPulseFate: null` **beside `status: 'active'` and the three flags set to `false`** | ⛔ **UNTOUCHED — LOAD-BEARING: this file is 798/800** (§3.1, R3) |
| ⭐ **Deriver 1** | `src/domain/worldPulse/institutionLifecycle.js` | `function closureFateForInstitution` (`:651`) | Returns exactly `'shuttered'` \| `'bankrupt'` \| `'closed_for_want_of_custom'` — **and exactly those three across ALL 30 revisions of the file** | All three are `closure` members |
| ⭐ **The second `fate` vocabulary's two sets** | `src/domain/worldPulse/institutionLifecycle.js` | `LIFECYCLE_BUILD_FATES` / `LIFECYCLE_CLOSE_FATES` (`:633-634`) | `Set(['built','reopened'])` and `Set(['shuttered','bankrupt','closed_for_want_of_custom'])`; the second feeds `priorLifecycleCounts`'s damping counter | ⛔ **UNTOUCHED.** §13 tabulates the relation: a **strict subset, 3 of 15**. ⛔ Deriving it would be a READER of the kind — **EM-B1i's or a follow-on's, never this packet's** (R6) |
| **Writer — DERIVED + CLEAR** | `src/domain/worldPulse/magicRegimeLifecycle.js` | `:357`, `:374` | `magicClosureFate(patch.form)` beside `status:'remnant'` + two flags; one `null` clear beside `status:'active'` | ⛔ **Untouched** |
| ⭐ **Deriver 2** | `src/domain/worldPulse/magicRegimeLifecycle.js` | `export function magicClosureFate` (`:129`) | Returns `'bankrupt'` \| `'closed_for_want_of_custom'`; its docblock: *"DRAWN FROM institutionLifecycle's EXISTING three-word fate vocabulary … rather than adding a fourth"* | ⭐ The estate already made this packet's argument once |
| **Writer — LITERAL, kind `closure`** | `src/domain/worldPulse/settlementLifecycleFirstClass.js` | `:601`, in `applySettlementLifecycleOutcomeToSettlement` (`:578`) | `worldPulseFate: 'abandoned_with_the_settlement'` beside `status: 'removed'` + `_worldPulseInactive: true` | ⛔ **Untouched** |
| **Writer — DERIVED + CLEAR** | `src/domain/worldPulse/tierOutcomeApply.js` | `:162`, `:265` | `worldPulseFate: fate.fate` inside `deactivateForDemotion` (`:156`), **beside `status: fate.status` AND `_worldPulseInactive: true`** | ⛔ **Untouched.** ⚠ Also in the eager first-paint closure. ⭐ **All seven demotion fates are `closure`** |
| ⭐ **Deriver 3** | `src/domain/worldPulse/tierOutcomeApply.js` | `function demotionFateForInstitution` (`:138`) | Seven `{fate, status}` pairs (`:142-148`); `status` is `'remnant'` ×6 and `'removed'` ×1 — **never `'active'`** | All seven are `closure` members |
| **Writer — LITERAL ×2, kind `rise`** | `src/domain/worldPulse/upswingKernel.js` | `:516`, `:763` (in `advanceUpswing`, `:366`) | `:516` renames in place with **no `status:` and no flag** (selection at `:514` requires `'active'`); `:763` pushes a **new `status: 'active'`** record | ⛔ **Untouched** — members, kind `rise` |
| ⭐ **The only producer of two members** | `src/domain/worldPulse/moralInstitutionPressure.js` | `:348` `const fate = best.martial ? 'disbanded' : 'abolished';` | Feeds `institutionPatch.fate` (`:379`), consumed by `institutionLifecycle.js:1055` | ⛔ **Untouched.** ⭐ **It writes no `worldPulseFate` itself** — why a write-site scan misses `disbanded` |
| **Reader 1 — FORWARDS** | `src/domain/provenance/rosterProvenance.js` | `function institutionLastLifecycle` (`:227`), read at `:258` | `textOrNull(inst.worldPulseFate)` — free text; branches on no value | ⛔ **Untouched.** Declared NON-ENUMERATING |
| ⭐ **Reader 2 — TRUTHINESS, AND EM-B1i'S SUBJECT** | `src/domain/worldPulse/causeLifecycle.js` | `function institutionDestroyed` (`:136`), read at `:139` | `if (inst.worldPulseFate) return true` — **decides on the KEY's presence, never the value** | ⛔ **Untouched by this packet.** ⭐ **EM-B1i makes this test the `closure` kind**; §6.3 records the three members it mis-reads today |
| ⭐ **NO READER IS A DISPLAY SURFACE** | `src/components/new/tabs/OverviewTab.jsx` | `institutionBadge` (`:56`), the call at `:57` | `const { created } = institutionProvenanceOf(inst);` — `created` ONLY; `lastLifecycle` is read **nowhere in `src/`** outside its producer | ⇒ ⛔ the fate is **not** reader-facing; the voice law does not apply. A6 asserts it |
| ⭐ **THE HOMONYM HOME** | `src/domain/worldPulse/causeLifecycle.js` | `const NONSTANDING_STATUS` (`:134`) | `new Set(['removed','destroyed','remnant','ruined','defunct','closed','disbanded','abolished'])` — a **STATUS** vocabulary holding two FATE words | ⛔ **Untouched.** The trigger excludes both because of it |
| ⭐⭐ **THE REGISTER A NEW `worldPulse` FILE OWES** | `tests/lint/couplingInclusion.walker.test.js` | `const ARGUED_UNLAYERED` (`:456`), `const ARGUED_ROSTER_CEILING = 28` (`:1051`), `const CENSUS_SCOPE_RE` (`:1054`) | The census is **TOTAL over `src/domain/{worldPulse,spatial}`**; its own arm is titled *"a NEW unlayered module REDS — it must get a family, an argument, or the baseline"*; the roster has **exactly 28** keys and the ceiling is asserted by **EXACT equality**; a `substrate` row declares `kind`, a `reason` of >20 chars and `reads` — its exact set of layered imports — checked against the live scan | ⭐ **The leaf takes an `ARGUED_UNLAYERED` row, `kind: 'substrate'`, `reads: Object.freeze([])`, and the ceiling goes 28 → 29 IN THE SAME DIFF** (§7 row 5). The baseline door is closed: it is *"only for pre-program debt"* and *"only shrinks"* |
| ⭐ **AND NO CROSS-LAYER PAIR IS MINTED** | `tests/lint/couplingInclusion.walker.test.js` | `function scanCrossLayerPairs` (`:1164`) | `for (const [rel, layer] of [...LAYER_OF])` … `if (!depLayer \|\| depLayer === layer) continue;` — it iterates **layered** modules and skips **unlayered** deps. Measured: `calamityKernel.js` matches **no** `LAYER_PATTERNS` family and sits in `.coupling-unlayered-baseline.json` (`:36` of 179) | ⇒ ⛔ **`tests/lint/.coupling-inclusion-baseline.json` and `.coupling-unlayered-baseline.json` are NOT rows of this packet and must not move.** Asserted by the walker's own suite in `checks` |
| **The `substrate` precedent** | `tests/lint/couplingInclusion.walker.test.js` | the `bandFamilies.js` row (`:473`) | `kind: 'substrate'`, *"SP substrate — the shared band/severity vocabulary, spelled by every layer"*, `reads: Object.freeze([])` | **Copy this row's exact shape** |
| **Test precedent — the walker** | `tests/lint/chooserTotality.walker.test.js` | `const SCAN_ROOTS` (`:64`), `describe('HB-1 — …')` (`:221`) | A register walker asserts its declared table SET-EQUAL to a live scan in BOTH directions | **Copy this shape** |
| **Test precedent — the proof shape** | `tests/domain/institutionFounding.test.js` | `describe('MF-T2Q — the institution founding year')` (`:113`) | One literal `describe`, straight-line `it`, positive control first | **Copy this shape** (§P3.4) |
| ⭐ **THE MATCHER PRECEDENT, AND ITS TRAP** | `tests/domain/ruinInstitution.test.js` | `function commentsOnly` (`:192`) | A comment-only strip that **KEEPS string contents**; the shared `tests/helpers/codeOnlySource.js` blanks them and returns a vacuous zero | ⛔ **Copy `commentsOnly`, never the shared helper** |
| ⛔ **A LANDED ARM THAT SCANS THE NEW LEAF** | `tests/domain/ruinInstitution.test.js` | A7 (`:383`) | Asserts the `status: 'ruined'` hit list is EXACTLY `['src/domain/worldPulse/calamityKernel.js']` over a comment-only strip of every non-test `src/` file | ⛔ **The leaf must contain no `status:` token in code and no `status: 'ruined'` anywhere.** ⭐ **A7 of THIS packet asserts it**, so a later editor of the leaf is told why |
| **Enforcer-dir law** | `tests/lint/mutationCoverage.shared.mjs` | `export const ENFORCER_DIRS` (`:36`) | `tests/lint` is the FIRST entry | The row this packet appends |
| **Register shape** | `scripts/mutation-coverage-manifest.json` | `invariants` | ⚠ AS OF `ad7ddf2c9`, AND AS HISTORY ONLY: **705** rows. The population moves with every landing (measured **716** at `bdbf7c89c`); read the live count from the file | ⛔ **RELATIVE: `invariants` +1 row keyed by this packet's OWN walker; `uncoveredBaseline` UNMOVED** |

**Forbidden alternatives:** ⛔ **no edit to any of the five unedited writer files** (a second
production file, or any edit to the 798/800 `institutionLifecycle.js`, is a STOP); ⛔ **no reader
of the KIND anywhere in `src/`** — that is EM-B1i's, and a reader here would forfeit the
golden-neutrality this packet is built on; ⛔ **no derivation of `LIFECYCLE_CLOSE_FATES` from the
kind** (R6); ⛔ no change to `ruinInstitution`'s signature, its five keys or their ORDER; ⛔ no
member added that no writer produces, except the chartered `ruined_by_decree`; ⛔ no member
removed; ⛔ **`null` is never a member**; ⛔ **no fourth kind and no re-kinding of a member without
the chair**; ⛔ no edit to either coupling baseline JSON; ⛔ no hand-edit of
`tests/lint/.lighting-census-baseline.json`; no files outside the manifest.

---

## 6. Exact contracts

### §6.1 · ⭐ THE KINDS, DERIVED BY EXECUTION FROM WHAT EACH WRITER DOES TO THE RECORD

Each write site was classified by the **sibling keys of its own record literal** (evidence §5.1),
not by the fate's English. The rule is the record's, and it is total:

> **`closure`** — the writer sets `_worldPulseInactive: true` **and** a non-`active` status.
> **`standing`** — the writer changes **no** status and sets **no** inactive flag: the institution
> is diminished but still stands, active, in place.
> **`rise`** — the institution is raised or founded and is active: an in-place promotion (no
> status change) or a **new** `status: 'active'` record.

```
calamityKernel.js:246   ruinInstitution     status:'ruined'  _worldPulseInactive  _worldPulseEconomyClosed   → closure
calamityKernel.js:309   calamity DEMOTE     ⭐ NONE — no status change, no inactive flag                      → standing
institutionLifecycle:1019  CLOSE            status:'remnant' _worldPulseInactive  _worldPulseEconomyClosed   → closure
institutionLifecycle:1061  ABOLISH          status:'remnant' _worldPulseInactive  _worldPulseMorallyAbolished→ closure
magicRegimeLifecycle:357   magic CLOSE      status:'remnant' _worldPulseInactive  _worldPulseEconomyClosed   → closure
settlementLifecycleFirstClass:601  DEATH    status:'removed' _worldPulseInactive                             → closure
tierOutcomeApply:162    tier DEMOTION       status: fate.status  _worldPulseInactive                         → closure ×7
upswingKernel.js:516    UPGRADE             ⭐ NONE — no status change, no inactive flag                      → rise
upswingKernel.js:763    FOUNDING            status:'active'                                                  → rise
— the four CLEAR sites all write worldPulseFate: null beside status:'active' and the flags set FALSE —
```

⇒ ⭐ **`closure` 15 · `standing` 1 · `rise` 2 = 18.** ⛔ **The chair's six suspected
"diminished but standing" names are all `closure`**: `deactivateForDemotion` deactivates for all
seven, whatever the word says. ⛔ **The one member that needed the third kind is
`demoted_by_disaster`.**

### §6.2 · ⭐ THE SHAPE — THE KIND MAP IS THE SINGLE AUTHORED SOURCE; THE FLAT LIST IS DERIVED

```js
/** The three answers to "what did this writer do to the institution". */
export const WORLD_PULSE_FATE_KINDS = Object.freeze(['closure', 'rise', 'standing']);

/** Every word the world pulse may stamp on `worldPulseFate`, with its kind. */
export const WORLD_PULSE_FATE_KIND = Object.freeze({
  abandoned: 'closure',                     // tierOutcomeApply demotionFateForInstitution :143 (status 'removed')
  abandoned_with_the_settlement: 'closure', // settlementLifecycleFirstClass :601
  abolished: 'closure',                     // institutionLifecycle :1055 ← moralInstitutionPressure :348
  bankrupt: 'closure',                      // closureFateForInstitution :654 ; magicClosureFate :130
  captured_by_local_powers: 'closure',      // tierOutcomeApply :147
  closed_for_want_of_custom: 'closure',     // closureFateForInstitution :655 ; magicClosureFate :130
  demoted_by_disaster: 'standing',          // ⭐ calamityKernel :309 — renamed in place, the slot stays standing
  destroyed_by_disaster: 'closure',         // calamityKernel :317/:321 through ruinInstitution
  disbanded: 'closure',                     // institutionLifecycle :1061 ← moralInstitutionPressure :348
  downsized: 'closure',                     // tierOutcomeApply :146
  founded_by_flourishing: 'rise',           // ⭐ upswingKernel :763 — a NEW active institution
  hollowed_out: 'closure',                  // tierOutcomeApply :148
  privatized: 'closure',                    // tierOutcomeApply :144
  reduced_to_watch_post: 'closure',         // tierOutcomeApply :142
  ruined_by_decree: 'closure',              // ⭐ DECLARED, UNPRODUCIBLE until EM-B1a
  shuttered: 'closure',                     // closureFateForInstitution :653
  survives_as_remnant: 'closure',           // tierOutcomeApply :145
  upgraded_by_reconstruction: 'rise',       // ⭐ upswingKernel :516 — promoted in place, stays active
});

/** The members, in codepoint order — DERIVED, so a member can never lack a kind. */
export const WORLD_PULSE_FATES = Object.freeze(Object.keys(WORLD_PULSE_FATE_KIND));

const FATE_SET = new Set(WORLD_PULSE_FATES);

/** @param {unknown} value @returns {boolean} */
export function isWorldPulseFate(value) {
  return typeof value === 'string' && FATE_SET.has(value);
}

/** @param {unknown} fate @param {string} caller @returns {void} throws on a non-member */
export function assertWorldPulseFate(fate, caller) {
  if (!FATE_SET.has(fate)) {
    throw new TypeError(`${caller}: worldPulseFate '${fate}' is not a member of WORLD_PULSE_FATES.`);
  }
}
```

⭐ **WHY THIS SHAPE, MEASURED** (the chair offered two; this is the cheaper hybrid):

| shape | minified | "a member with no kind" | the walker's totality arm |
|---|---:|---|---|
| a frozen list **beside** a frozen kind map (two authored literals) | **1,248 B** | *detected* by a set-equality between two literals three lines apart | a both-ways equality that can only catch a typo made in the same edit |
| ⭐ **the kind map as source, the list derived** | ⭐ **910 B** | ⭐ **UNREPRESENTABLE** — an object key has exactly one value | three arms that are all **live**: every kind ∈ `WORLD_PULSE_FATE_KINDS`, the keys are codepoint-sorted, and the kind populations are exactly 15/1/2 |

⇒ **338 B cheaper, the eighteen strings spelled ONCE, and the structural guarantee the estate
prefers over a detected one.** The membership test is identical in both (`new Set` once at load).
⛔ `Object.keys` preserves insertion order for these string keys, and the walker **asserts the
codepoint order** so it cannot rot.

⛔ **ZERO IMPORTS** (§3.2 and the `ARGUED_UNLAYERED` row's `reads: []`).
⛔ **THE LEAF CONTAINS NO `status:` TOKEN IN CODE AND NO `status: 'ruined'` ANYWHERE** — A7.

### ⛔ `null` IS THE CLEAR SENTINEL AND IS NOT A MEMBER (the chair's Q3)

The leaf's gloss must say both sentences:

> *These are the words the world pulse may stamp. `null` is not one of them: four sites write
> `worldPulseFate: null` to CLEAR a fate when an institution stands again — the reopen
> (`institutionLifecycle:959`), the re-raise (`:1103`), the magic reopen
> (`magicRegimeLifecycle:374`) and the tier reactivation (`tierOutcomeApply:265`), each beside
> `status: 'active'` and its flags set FALSE. A cleared fate is the ABSENCE of a fate, and
> `causeLifecycle.institutionDestroyed` reads exactly that distinction through truthiness. Adding
> `null` here would make "no fate" a fate.*
> **`ruined_by_decree` is DECLARED AND UNPRODUCIBLE** until EM-B1a's `set-institution-state` calls
> `ruinInstitution` with it; it is here so that op lands against a CLOSED set.
> ⛔ **NOTHING READS `WORLD_PULSE_FATE_KIND` YET. EM-B1i is the packet that makes
> `causeLifecycle.institutionDestroyed` test the `closure` kind** — see §6.3.

### §6.3 · ⭐ WHAT THE KIND IS FOR, RECORDED BUT NOT ACTED ON

`institutionDestroyed` (`causeLifecycle.js:136`) returns `true` at `:139` **on the key's
presence**, so today the **three non-`closure` members all read as destroyed** and sever an NPC's
criminal leash: a **demoted** wizard's tower, a **reconstructed** guild and a **flourishing**
academy. ⛔ **This packet changes nothing about that** — it only gives EM-B1i the vocabulary to
say it. EM-B1i owns the golden and preset-witness measurement and the owner's door.

### The guard — ONE call, EM-B1e's contract untouched

```js
export function ruinInstitution(inst, { reason, fate }) {
  if (typeof fate !== 'string' || fate === '') throw new TypeError(/* unchanged */);
  assertWorldPulseFate(fate, 'ruinInstitution');                       // ⭐ the only added line
  if (typeof reason !== 'string' || reason === '') throw new TypeError(/* unchanged */);
  return { /* the five keys, byte-identical, in the existing order */ };
}
```

⛔ **THE CALL GOES AFTER THE EXISTING `fate` TYPE CHECK AND BEFORE THE `reason` CHECK**, so every
verdict EM-B1e's LANDED A3 asserts is unchanged: `fate: ''`, a non-string `fate` and a missing
`fate` each still throw on the **same** check. ⛔ **The returned record does not change by one
byte** — the guard either throws or falls through.

**Absence rules.** absent / empty / non-string `fate` → throw (unchanged, on the TYPE check).
**a string outside the vocabulary → THROW (new).** `null` → throw (a non-string; unchanged) —
⛔ this writer never clears a fate, and the four clear sites do not call it.

**Determinism, flag, dormancy.** No PRNG, no clock, no locale. Flag `NONE`. `ruined_by_decree` is
unreachable until EM-B1a. **Golden posture: ⛔ UNCHANGED** — the packet validates and never
re-spells; A1 proves it by execution and A5 by digest.

### ⭐ THE WALKER'S MATCHER, STATED EXACTLY — NO DISCRETIONARY WORD

> **The walker strips block and line comments from every `.js`/`.jsx` file under `src/` excluding
> `**/*.test.*` with a COMMENT-ONLY strip that KEEPS string contents, flags each file whose
> remaining source contains any of the quoted literals of the TRIGGER SET — being the members of
> the live `WORLD_PULSE_FATES` that belong to no other declared vocabulary in the tree, where a
> declared vocabulary is a `const SCREAMING_SNAKE = <collection>` initialiser in a file that is not
> a row of the writer roster — and REDS unless the FLAGGED set is SET-EQUAL, IN BOTH DIRECTIONS,
> to the roster's `literal` rows; and REDS unless every file that writes `worldPulseFate` is a
> roster row whose declared `spelling` matches the tree.**

⭐ **THE `spelling` FIELD IS CONTRACT** (EM-B1d's accepted judgment call). Every roster row
declares `spelling: 'literal' | 'derived' | 'argument' | 'clear'`, and the walker CHECKS it:

- a **`literal`** row must be FLAGGED, and every fate literal it spells must be a member;
- a **`derived`** row must name a deriver that resolves live at path+symbol, and **every string
  literal that deriver returns must be a member** (the arm that catches `disbanded`);
- an **`argument`** row must import `assertWorldPulseFate` and call it;
- a **`clear`** row must write `worldPulseFate: null` at the declared site and spell no fate.

⛔ **THE DECLARED `CANNOT-CATCH:` HEADER:** *a writer that builds a fate from a runtime value no
deriver returns — a template, a concatenation, or a `patch.fate` a future producer sets to an
unknown word — is invisible to a source scan. **Measured: no such site exists today** — every
`patch.fate` producer in `src/` is enumerated (`institutionLifecycle:851`, the same deriver;
`moralInstitutionPressure:379`, the `:348` ternary). The guarded writer is the one site where the
check runs at RUNTIME, which is why EM-B1a's op routes through it. **And the KIND is data with no
reader, so no walker arm can prove a kind CORRECT — only present, single and drawn from the
declared three.** The kinds were derived from the writers' record literals at compile (§6.1); a
writer that later changes what it does to the record without changing its fate word would leave
the kind stale, and nothing here would red. EM-B1i, the first reader, is where that becomes
catchable.*

### ⭐ THE HOMONYM TABLE AND THE TRIGGER — DERIVED, MEASURED TWICE

Over the non-test `src/` corpus — **2,246 files AS OF the compile tip `ad7ddf2c9` (executed history, interim rule 9); re-measured 2,250 at `e80a6a3f4`, and the table below was RE-DERIVED over all 2,250 with an identical result** — excluding the seven roster files, by two independent readings
that **agree exactly** (evidence §12):

| member | foreign declared vocabularies (name × file) | verdict |
|---|---:|---|
| `abandoned` | **3** — `FOSSIL_KINDS` × `undercity/colonization.js`, `DEAD_ENDPOINT_STATUS` × `worldPulse/corruptionWeb.js`, `PLAN_STATES` × `worldPulse/demographicsPlans.js` | ⛔ HOMONYM |
| `abolished` · `disbanded` | **1** each — `NONSTANDING_STATUS` × `worldPulse/causeLifecycle.js` | ⛔ HOMONYM |
| the other **fourteen** | **0** each | ⭐ **TRIGGER** |

⭐ `abolished` and `disbanded` are simultaneously a *fate* and a *status* — **in the very file that
reads `worldPulseFate`.** Triggering on them would convict the reader. ⓘ `ruined_by_decree` is
excluded from the trigger by construction (unproducible).

⭐ **THE SETS, MEASURED (evidence §13):** FLAGGED **6** · writer-roster rows **6** · ⭐ **set-equal
both directions, no offender either way** · declared write SITES **13** (5 literal, 1 argument, 3
derived, 4 clear) · exemption register rows **0** · readers **2**, both non-enumerating.

### Lifecycle

| Create | Read | Persist | Reload | Regenerate | Undo | Import/migrate | Public veil |
|---|---|---|---|---|---|---|---|
| Two frozen objects at load | the walker, and `ruinInstitution`'s guard | ⛔ **nothing new is persisted** — the key and all seventeen live values already exist on saved records | unchanged | unchanged | unchanged | ⭐ **NO MIGRATION OWED, MEASURED:** the vocabulary is derived FROM the writers' own history (§12) | unchanged |

**Receipts** `NONE`. **Alignment:** `DECLARED EMPTY`. **Edit story:** `ENGINE-ONLY — the DM's verb
that writes ruined_by_decree is EM-B1a's op.`

---

## 7. Exact change manifest

⚠ **ONE FULL REPO-RELATIVE PATH PER ROW; the `action` word is a member of `PACKET_ACTIONS`
(`CREATE`, `DOC`, `MODIFY`, `REGISTER`, `TEST`) and is IDENTICAL to that path's `action` in
`EM-B1h.manifest.json`'s `changeManifest`.** The (action, path) pairs below and the JSON's are
asserted SET-EQUAL in both directions — executed, evidence §14.

| Action | File | Symbol/region | Max delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `src/domain/worldPulse/worldPulseFates.js` | `WORLD_PULSE_FATE_KINDS`, `WORLD_PULSE_FATE_KIND`, `WORLD_PULSE_FATES`, `isWorldPulseFate`, `assertWorldPulseFate` | **≤70 eff** | §6.2 verbatim: the kind map as the single authored source with a per-member writer comment, the derived frozen list, and §6's TWO-sentence `null` gloss plus the `ruined_by_decree` dormancy sentence and the §6.3 pointer to EM-B1i. ⛔ **ZERO IMPORTS.** ⛔ **No `status:` token in code and no `status: 'ruined'` anywhere** (A7; the LANDED `ruinInstitution.test.js` A7 scans this file). |
| `MODIFY` | `src/domain/worldPulse/calamityKernel.js` | `export function ruinInstitution` (`:242`); the import clause | **≤2 eff** | Import `assertWorldPulseFate` from `./worldPulseFates.js` and call it **between** the existing `fate` type check and the `reason` check (§6). ⛔ Nothing else: not `:309`'s demote branch, not the two call sites, not the five keys or their order. |
| `CREATE` | `tests/lint/worldPulseFateTotality.walker.test.js` | arms **T1–T4** | **≤250 eff** | §6's matcher verbatim, the DERIVED trigger, the derived homonym table, the **6-row writer roster with its checked `spelling` field**, the 2-row reader roster, the empty inline exemption register, the `CANNOT-CATCH:` header **including its kind-staleness paragraph**. ONE literal `describe`, **FOUR straight-line `it`**. ⛔ `.each`, looped, conditional and nested registration forbidden (§P3.4). ⛔ Copy `commentsOnly` from `tests/domain/ruinInstitution.test.js:192`, **never** `tests/helpers/codeOnlySource.js`. Negatives carry `// anchored:` on the line above. |
| `REGISTER` | `scripts/mutation-coverage-manifest.json` | one `invariants` row keyed the walker | `+1 row` | ⛔ **SURGICALLY, never re-serialised. RELATIVE, NEVER ABSOLUTE: `invariants` +1 row keyed by this packet's OWN walker (from whatever its siblings leave the file at); `uncoveredBaseline` UNMOVED.** The `705 → 706` this row carried in version 2 is withdrawn — it was already stale by eleven rows at the integration tip (`invariants` measured **716** at `bdbf7c89c`), and three packets cannot all be right about one absolute. Insert immediately **before** `"tests/components/dossierRawKeys.census.test.jsx"` and immediately **after** the close of `"tests/copy/landingProseGrounding.test.js"` — a pure **+N / −0** diff at the TAIL, far from EM-B1d's head-of-file insert. Kind `rationale` (⛔ `uncovered` is unavailable to an enforcer dir), ≥40 chars, carrying the EXECUTED mutant account. Anchor on key NAMES. |
| ⭐ `REGISTER` | `tests/lint/couplingInclusion.walker.test.js` | `ARGUED_UNLAYERED` (`:456`) and `ARGUED_ROSTER_CEILING` (`:1051`) | **+6 eff** | ⛔ **TWO EDITS, ONE DIFF.** Add one `ARGUED_UNLAYERED` row for `'src/domain/worldPulse/worldPulseFates.js'` in the shape of the `bandFamilies.js` row (`:473`): `kind: 'substrate'`, a `reason` of >20 chars naming it the pulse's closed fate vocabulary spoken by every layer, and `reads: Object.freeze([])` — **which is exact, because the leaf has zero imports**. Then `ARGUED_ROSTER_CEILING` **28 → 29**. ⛔ The ceiling is asserted by **EXACT equality**, so the row without the bump (or the bump without the row) REDS. ⛔ **Add NO `describe` and NO `it`** — the lighting census must not move on this file. ⛔ **Do not touch either coupling baseline JSON** (§5: no cross-layer pair is minted). |

| ⭐ `REGISTER` | `tests/lint/.prose-numerics-baseline.json` | the ONE row keyed `src/domain/worldPulse/calamityKernel.js` | `+0 / −0 rows; ONE "line" value` | ⛔ **A PURE RE-ADDRESS, NOT A BANK.** `tests/lint/proseNumerics.test.js` freezes live debt by **exact path + line + snippet identity** (its own header) and this file carries EXACTLY ONE row on the file this packet modifies: `{"path":"src/domain/worldPulse/calamityKernel.js","line":745,"category":"floatInterpolation","snippet":"Math.round(exposure.factor * 10000)"}`. The packet inserts **2 effective lines at `:243-244`, five hundred lines ABOVE it**, so the row re-addresses **745 → 747**. ⛔ **Change the `"line"` value and NOTHING else** — `path`, `category` and `snippet` stay byte-identical, no row is added or removed, the census count does not move, and the whole diff is **two lines, one −/+ pair** (the walker's own idiom: "88 pure line moves", "re-addresses 603 → 615"). Verify by grepping the diff for any changed line that is not a `"line"` value. ⓘ Re-measure the destination at dispatch: `745 + (lines this packet inserts above it)`. |

**Six rows, all handwritten (of ≤12). Generated artifacts: `NONE`** — a MEASUREMENT, see below.

### The registration ledger — every row priced against the tip

| # | Obligation | Verdict | Measurement |
|---|---|---|---|
| P2.1 | lighting census | **OWED — `+1 file`, INTERIOR RED** | ⛔ **NO ABSOLUTE IS QUOTED.** The DELTA — ONE new `tests/` file with one literal `describe` and four straight-line `it`; the `src/` leaf moves nothing; row 5 edits an existing test file and adds **no title** — is **`+1 files / +0 parked / +1 credited / +4 titles / +1 suiteTitles`**. ⭐ `files` counts TEST files only: `TEST_FILES = walk(join(ROOT,'tests'))` (`:515`), `figures.files = TEST_FILES.length` (`:612`). The absolute is the chair's at promotion. |
| P2.2 | mutation-coverage row | ⛔ **OWED, AND CONTENDED** | `tests/lint` is the FIRST `ENFORCER_DIR`. ⛔ **RELATIVE: `invariants` +1 row keyed by this packet's own walker; `uncoveredBaseline` UNMOVED** (the `705 → 706` is withdrawn; live is **716** at `bdbf7c89c`). ⚠ **THE CONTENTION HAS MOVED: EM-B1d is LANDED** at the integration tip (`PACKET_MANIFEST.json`, status `LANDED`), and `LANDED` is a TERMINAL status that reserves NO change path (`implementation-packets.mjs:44`, `TERMINAL_PACKET_STATUSES`), so B1d is no longer a preflight STOP. ⭐ **AND "live holders among NON-TERMINAL packets" IS THE WRONG FRAME (ESTATE-REPAIR-2, measured):** at `bdbf7c89c` the registered estate holds **zero** non-terminal packets, so nothing reserves this path today. The packets that will contend once placed are the WAITING DRAFTS whose `changeManifest` HOLDS it — **EM-A1, EM-A3, EM-B1b, EM-B2a3, EM-B2a4 and EM-C4a**, re-measured over all 29 waiting capsules at `bdbf7c89c` (ESTATE-REPAIR-6; EM-B2b's conditional row was removed on its own measurement, ESTATE-REPAIR-2 NOTE-1, and EM-B2a1 · EM-B2a2 · EM-B1i · EM-B4 · EM-D0b name the path in prose while declaring NO row on it). ⛔ **The unsplit `EM-B2a` no longer exists** — judgment 33 split it into a1–a4 and sequences the family AFTER the EM-R members, so only a3 and a4 can ever contend and both land far behind this packet; **EM-C4a** is the contender the earlier list did not name. Their rows are row-DISJOINT from this one, each keyed by its own test file (`editDeclarations.walker` · `flavorFields.census` · `opGuardCoverage.walker` · `pipelinePinnedChoosers` · `dmLayerGoldenIsolation` · `editMutationPath.walker`, MEASURED), and the contention is a placement-order question, not a content one. ⛔ Re-measure at dispatch FROM THE CAPSULES, never from this list. |
| ⭐ **P2.2b** | **coupling-inclusion census** | ⭐⭐ **OWED — THE ROW VERSION 1 MISSED** | `CENSUS_SCOPE_RE = /^src\/domain\/(?:worldPulse\|spatial)\//` and the census is TOTAL over it; its arm is titled *"a NEW unlayered module REDS — it must get a family, an argument, or the baseline"*. Three doors; the baseline is *"only for pre-program debt"* and *"only shrinks"*, so the leaf takes the **argument**: `ARGUED_UNLAYERED` +1 row and `ARGUED_ROSTER_CEILING` **28 → 29** (exact equality, measured: the roster holds exactly 28 keys today, 20 of them `substrate` with `reads: []`). ⭐ **No pair is minted and no baseline moves**: `scanCrossLayerPairs` skips unlayered deps, and `calamityKernel.js` is itself unlayered (`.coupling-unlayered-baseline.json:36` of 179). |
| P2.3 | observed-shape exemption | **NOT OWED** | No save-time key read; no new stored key. The guard reads its own ARGUMENT, never `inst.worldPulseFate`. ⚠ The scanner covers *"every `.js`/`.jsx` under `src/`"* and this packet creates one, so `check-observed-shape-readers.mjs` is in `checks` and **motion is a STOP**. |
| P2.4 | writer-reach | ⚠ **MEASURE AND RECORD — never write** | No new reader of a settlement field under `src/domain/edit/**`. Baseline at §8 step 1; a **shrink is the chair's to bank, GROWTH is a STOP.** |
| P2.5 · P2.6 · P2.8 | fork/mechanism · persisted-key denylists · surface walkers | **NOT OWED** | No chooser, pool or draw; neither persisted key; headless. ⭐ **THE BROWSER SUITE IS NOT OWED EITHER (interim rule 10), AND THIS PACKET SAYS SO AND NAMES NO SPEC:** it touches no `src/components/**` path, no route, no `data-testid` and no accessible name — its two CREATEs and its one MODIFY are all under `src/domain/worldPulse/` and `tests/lint/`. |
| ⭐ **P2.7** | **prose-numerics — TWO QUESTIONS, AND ONLY THE FIRST WAS ASKED** | ⭐ **(a) DETECTOR: NOT OWED. (b) LINE-ADDRESSED BASELINE: ⛔ OWED**, row 6 | **(a)** This packet renders **no figure and no word** (§5, A6), so it authors no prose numeric — unchanged. **(b)** ⛔ **A SECOND OBLIGATION THE DETECTOR QUESTION HIDES (the FIX-F class, 2026-09-19):** `tests/lint/.prose-numerics-baseline.json` addresses its frozen debt by **exact path + line + snippet**, and it carries **ONE row on `src/domain/worldPulse/calamityKernel.js`, at line 745** — MEASURED at `e80a6a3f4`. This packet's `+2` lands at `:243-244`, **above it**, so the row re-addresses **745 → 747** or `tests/lint/proseNumerics.test.js` reds its exact-identity arm at the landing. ⭐ **MEASURED, NOT ASSUMED, THAT THE MEMBER MAY CARRY IT:** of the 29 waiting capsules **ZERO declare this path**, and of this train's other three members (EM-A1, EM-D0a, EM-D0b) **NONE modifies a file carrying a baselined row** — so row 6 is path-disjoint inside EM-T9 and needs no terminal. ⚠ **ONE ESTATE-WIDE CONTENDER, OUTSIDE THIS TRAIN: `EM-B1j` also modifies `calamityKernel.js` and would shift the same row** (and `EM-B2a3` shifts six rows in `src/generators/steps/assembleInstitutions.js`); whichever lands second re-addresses from whatever the first leaves. ⛔ Re-measure the destination at dispatch; never copy `747` from this packet. |
| ⭐ **P2.10** | **edge-shared closure** | ⭐ **NOT OWED — MEASURED** | INPUT membership against the five committed metas (**307 inputs**): **no writer file and neither CREATE target appears in any**. Only `src/domain/entities/status.js` does — one reason §3.2 refuses it as a home. ⇒ no rebuild, no generated path, no `_shared` row. |
| ⭐ **P2.11** | **byte budgets** | ⭐ **NO BUDGETED CHUNK** | §3.2's table. Generation worker: **NOT REACHABLE**. Lazy engine: **NO**. First paint (the config's own export, **IMPORTED and its size MEASURED AT THE BASE, never quoted** — interim rule 9; history with an as-of mark: **268** at this packet's compile tip, **269** at `bdbf7c89c`, and the MEMBERSHIP answer does not move with the count): **NO**. `advanceInterval.worker`: **YES, no ceiling today.** Priced **≤1,008 B** (910 leaf + 98 guard) for TOOL-3's per-train attribution. **R2.** |
| ⭐ | **declared-command write set (§P2 row 12)** | **CLEAN** | Every command in `checks` read for what it WRITES. **No generator is invoked** — `build:edge-shared` is absent because P2.10 is not owed; both `check-*` scripts run as readers with no `--write`; eslint has no `--fix`. ⇒ **no undeclared path is written by this packet's own seal.** |
| ⭐ | **`tests/` sweep (§P2 row 12, step 12)** | ⭐ **NO TEST PATH OWED** | §12. |
| ⭐ **P2.13** | **wiring census — the PRODUCER COUNT** | ⛔ **OWED — A DEFERRED ROW, delta only (interim rule 1)** | `docs/content/wiring-census.json` — **a named INTERIOR RED, delta only.** `stamp.producerIndexFiles` **+1**: this member's new `.js` leaves under the two counted roots, named one by one — `src/domain/worldPulse/worldPulseFates.js`. **Re-measured from this packet's own CREATE rows at `bdbf7c89c2569679bcccdb188d5d831ccfaa0e6d`.** ⛔ `producerIndexFiles` **NAMES NOTHING — IT IS A NUMBER** (`1172` at that tip, read from the stamp), counting every `.js` under `src/generators/**` plus every `.js` under `src/domain/**`; so the answer is a DELTA, and "the register does not name mine" would be a category error. `stamp.files` (7 entries) and `stamp.candidateLeaves` (6) **UNMOVED** — both key on `src/domain/display/stateProse/` paths this member does not touch. `totals.*` **UNMOVED** — this member produces no pool, variant or relation. ⛔ **Regenerated WHOLE at the train's terminal, BY THE CHAIR** — a member never runs `node scripts/wiring-census.mjs`, because N lanes each regenerating a global COUNT produce N conflicting blobs. ⛔ **The path is in NEITHER §7, NOR the `changeManifest`, NOR `checks`** — naming it reserves the estate's most contended path against every sibling. |

> ⛔ **`tests/lint/.lighting-census-baseline.json` IS NOT A ROW** — §P2 row 1 forbids a member from
> editing it; the re-derivation is the train's terminal act and the CHAIR's. Predicted interior
> red, SHAPE ONLY: `the estate's file count moved — re-measure, do not re-word: expected <N> to be <N-1>`.

---

## 8. Ordered coding sequence

0. Dispatch and seal; re-read `git rev-parse HEAD` in the same command. ⛔ Confirm
   `scripts/mutation-coverage-manifest.json` is unreserved (EM-B1d LANDED) **and** that
   `ARGUED_ROSTER_CEILING` still reads **28** with exactly 28 roster keys.
1. **Capture the baselines:** `sha256` of `generator-golden-master.json`,
   `dossier-prose-manifest-golden.json` and `preset-lighting-witness-golden.json`; the
   the live `invariants` row count and `uncoveredBaseline`, READ FROM THE FILE at the dispatch tip
   and never copied from this packet; the `ARGUED_UNLAYERED` key count (**28**);
   `node scripts/check-writer-reach.mjs`; `node scripts/check-observed-shape-readers.mjs`; the
   `max-lines` figure of `calamityKernel.js` (**expect 457 of 800**).
2. Add `tests/lint/worldPulseFateTotality.walker.test.js` with T1–T4 **failing**; quote the red.
3. Create `src/domain/worldPulse/worldPulseFates.js` — §6.2 verbatim, zero imports.
4. ⭐ **Immediately add row 5** (`ARGUED_UNLAYERED` + the ceiling bump) and run
   `tests/lint/couplingInclusion.walker.test.js`: it reds the moment step 3 lands and greens on
   this step, which is its own proof. ⛔ Confirm **no coupling baseline JSON moved.**
5. Add the ONE call to `ruinInstitution` and extend the import clause. ⛔ Rename nothing.
6. Wire consumers: **NOT APPLICABLE** — EM-B1a and EM-B1i are later. Skip and record.
7. Plant the mutants (§9), convict, restore digest-exact — **then** write the
   `mutation-coverage-manifest.json` row carrying the executed account.
8. Run focused verification (§10), including ⭐ `tests/domain/ruinInstitution.test.js`, whose
   LANDED A3 and A7 this packet's guard and new leaf could red (§12).
9. Run the wave-end gate per the host train's plan; write the completion receipt. ⛔ **Never
   refreeze the census.**

```text
The walker, four arms:
T1  THE VOCABULARY IS EXACT, CLOSED AND KINDED:
      the scan is asserted non-empty FIRST (guard-the-guard);
      WORLD_PULSE_FATES parses to exactly 18 distinct members in CODEPOINT order, frozen;
      null is NOT a member;
      every member's kind is one of WORLD_PULSE_FATE_KINDS, and the three populations
        are exactly closure 15 / standing 1 / rise 2;
      WORLD_PULSE_FATE_KINDS is exactly {closure, rise, standing}.
T2  DECLARED EQUALS DISCOVERED, both directions:
      every roster row resolves live at path+symbol;
      the files that WRITE worldPulseFate equal the roster's six, both ways;
      every `literal` row's fate literals are members;
      every `derived` row's named deriver returns ONLY members;
      every `clear` row writes null at its site and spells no fate;
      the `argument` row imports assertWorldPulseFate and calls it;
      report a FULL offender list, never a first failure.
T3  NO FOREIGN SPELLING, tree-wide, AND THE LEAF IS CLEAN:
      derive the trigger from the live vocabulary minus the measured homonyms;
      assert it is exactly the 14 and red if it empties or halves;
      each of the 3 homonyms must name >= 1 foreign declared vocabulary;
      FLAGGED set-equal, both directions, to the literal roster rows u the register;
      the two readers are asserted NON-ENUMERATING and UNFLAGGED;
      the LEAF's comment-stripped source contains NO `status:` token, and its raw
        source contains no `status: 'ruined'` (the LANDED A7's exact hit list);
      a fate literal outside the vocabulary REDS with its file:line.
T4  THE GUARD IS LIVE, through the REAL function:
      ruinInstitution accepts every one of the 18 members (positive control first);
      it REFUSES a well-formed non-member with a throw;
      EM-B1e's four absence verdicts are re-asserted UNCHANGED, their ORDER pinned by message;
      the record for a member call is byte-identical to EM-B1e's frozen oracle shape.
```

---

## 9. Acceptance matrix

`tests/lint/worldPulseFateTotality.walker.test.js` — ONE literal `describe`, FOUR straight-line
`it` — plus the focused suites of the files it guards.

| ID | Case | Required observation |
|---|---|---|
| **A1** | **⭐ THE GUARD IS LIVE AND THE HISTORY DOES NOT MOVE** (T4) | Through the REAL `ruinInstitution`: **all eighteen members are ACCEPTED** (positive control BEFORE the refusal, so the arm cannot pass on a broken import), and a well-formed non-member (`'razed_by_the_gods'`) **THROWS**. ⭐ The byte arm: the record produced for `'destroyed_by_disaster'` is `JSON.stringify`-identical to EM-B1e's frozen oracle shape — five keys, same values, same ORDER — so **a validating guard is proved byte-transparent on every call it admits.** |
| **A2** | **EM-B1e's ABSENCE VERDICTS ARE UNCHANGED, AND THE CHECK ORDER IS PINNED** (T4) | A missing `fate`, `fate: ''`, a non-string `fate` and a missing `reason` each still throw — ⛔ **asserted BY MESSAGE, not merely by the fact of a throw**: `fate: ''` must fail the TYPE check and never the membership check, because §6 places the call after it. A reordering would leave the LANDED A3 green while silently changing which contract refused. Every negative carries `// anchored:`. |
| **A3** | **⭐ DECLARED EQUALS DISCOVERED, BOTH DIRECTIONS** (T2) | Every roster row resolves live at path+symbol. The set of `src/` files that WRITE `worldPulseFate` is **SET-EQUAL to the six roster rows both ways**. The `spelling` field is CHECKED, never trusted: `literal` rows spell only members; ⭐ **every `derived` row's deriver returns ONLY members** — the arm that catches `disbanded`, which no write site spells; `clear` rows write `null` and spell no fate; the `argument` row imports and calls `assertWorldPulseFate`. Full offender lists. |
| **A4** | **⭐ THE TRIGGER, THE HOMONYMS AND THE SET-EQUALITY** (T3) | The trigger is **DERIVED** and asserted to be exactly the **fourteen**, with a floor arm that reds if it empties or halves. Each of the **three** homonyms names ≥1 foreign declared vocabulary **by name and file**. FLAGGED is **SET-EQUAL both directions** to the literal rows ∪ the register; the register is **empty** and its checker is proved on a planted row. ⛔ **BOTH MATCHER CONTROLS ARE REQUIRED**: it FIRES on a planted literal before any negative, IGNORES a commented one, and the shared `codeOnly` helper is shown to return **zero**. |
| **A5** | **⛔ NOTHING MOVES; THE VOCABULARY IS DERIVED; THE KINDS ARE TOTAL** (T1) | The three goldens are bytewise unchanged (digests). The scan is asserted non-empty FIRST. `WORLD_PULSE_FATES` parses to exactly **eighteen** distinct members **in codepoint order**, frozen; **`null` is asserted NOT a member**; **every member's kind is one of the declared three and the populations are exactly `closure` 15 / `standing` 1 / `rise` 2**. ⭐ THE HONESTY ARM: seventeen of the eighteen are proved to have a live writer or deriver in `src/`, with **`ruined_by_decree` asserted BY NAME as the single declared-dormant member** — so a nineteenth word cannot arrive quietly and the dormant one is visible as such. |
| **A6** | **⭐ NO READER ENUMERATES, NO READER IS A SURFACE, AND NOTHING READS THE KIND** (T3) | Both readers are asserted UNFLAGGED and non-enumerating, driven through the REAL functions with a standing-institution negative control first. ⭐ The surface claim is ASSERTED: `lastLifecycle` is read nowhere in `src/` outside its producer, and `OverviewTab.jsx`'s `institutionBadge` destructures `created` ONLY. ⭐⭐ **AND THE GOLDEN-NEUTRALITY CLAIM IS MACHINERY: a source scan proves NO file under `src/` imports `WORLD_PULSE_FATE_KIND`** — the kind is data with no reader, which is what makes this packet a freeze rather than a cure. EM-B1i is named in the message a violation prints. |
| **A7** | **⭐ THE LEAF IS CLEAN, AND A LATER EDITOR IS TOLD WHY** (T3) | The leaf's **comment-stripped** source contains **no `status:` token at all**, and its **raw** source contains **no `status: 'ruined'`**. Both directions are asserted with the reason in the failure message: `tests/domain/ruinInstitution.test.js`'s LANDED A7 asserts the comment-only-stripped `status: 'ruined'` hit list over every non-test `src/` file is **exactly** `['src/domain/worldPulse/calamityKernel.js']`, and this leaf is inside that scan. |

**7 of ≤8.** One slot deliberately unspent.

⚠ **THE STRICT-TYPECHECK EXHAUSTIVENESS CASE IS DECLARED EMPTY:** the vocabulary is runtime data,
not a union type, and no `switch` branches over a fate. Both typechecks are in `checks` regardless,
and the new leaf must be strict-clean and `any`-free (a new file gets an allowance of 0 in
`domainStrictBaseline` / `domainAnyCastBaseline`).

### The mutants (§P6) — SIX, each with its named target

| # | plant | must red |
|---|---|---|
| **M1** | `WORLD_PULSE_FATE_KIND` loses `disbanded` | A3's `derived`-row arm **by name** (`moralInstitutionPressure:348`), proving the deriver walk is live and not a literal-scan replica |
| **M2** | `closureFateForInstitution` gains a fourth return `'liquidated'` | A3's `derived`-row arm **with its file:line** |
| **M3** | the `assertWorldPulseFate` call is deleted from `ruinInstitution` | A1's refusal arm |
| **M4** | a seventh writer planted as a new `src/` file spelling a trigger token | A4's FLAGGED set-equality **and** A3's writer-set equality |
| **M5** | `demoted_by_disaster`'s kind flipped `standing` → `closure` | A5's population arm (15/1/2 → 16/0/2) |
| **M6** | ⭐ a planted `import { WORLD_PULSE_FATE_KIND }` in any `src/` file | A6's no-reader arm — the machinery that keeps this packet a freeze |

Each: prove the bytes moved (a no-op plant is a STOP), require a nonzero exit AND the named title
red, restore the exact pre-mutant SHA-256 **by `cp` from a backup taken before the plant**, rerun
focused green.

---

## 10. Verification commands

```sh
npx eslint src/domain/worldPulse/worldPulseFates.js \
  src/domain/worldPulse/calamityKernel.js \
  tests/lint/worldPulseFateTotality.walker.test.js \
  tests/lint/couplingInclusion.walker.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/worldPulseFateTotality.walker.test.js tests/lint/couplingInclusion.walker.test.js \
  tests/lint/couplingDesk.walker.test.js tests/lint/mutationCoverageManifest.test.js \
  tests/lint/negativeAssertionAnchor.walker.test.js tests/lint/contractTestAntiVacuity.walker.test.js \
  tests/lint/ruinFilterRoster.walker.test.js tests/lint/domainStrictBaseline.test.js \
  tests/lint/domainAnyCastBaseline.test.js

# ⭐ THE LANDED SUITE THIS PACKET COULD RED — EM-B1e's A3 (the guard's check ORDER) and its
# A7 (an EXACT one-file hit list that scans the new leaf). §12 measured both clean; execute them.
GATE_MUTEX_TIER=shared … -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/domain/ruinInstitution.test.js tests/domain/calamity.test.js \
  tests/domain/calamity.kernel.integration.test.js tests/domain/ruinFilter.probe.test.js \
  tests/domain/institutionLifecycle.test.js tests/domain/evaluateInstitutionLifecycle.test.js \
  tests/domain/magicRegimeLifecycle.test.js tests/domain/settlementLifecycleFirstClass.test.js \
  tests/domain/upswingKernel.test.js tests/domain/shiftTier.test.js \
  tests/domain/causeResolutionLifecycle.test.js tests/domain/institutionStatusModel.test.js \
  tests/domain/martialMoralWF8.test.js tests/domain/moralInstitutionFounding.test.js \
  tests/domain/institutionFounding.test.js tests/domain/worldPulseExpansion.test.js

GATE_MUTEX_TIER=shared … -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/domain/provenance/rosterProvenance.test.js

GATE_MUTEX_TIER=shared … -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js

GATE_MUTEX_TIER=shared … -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/simulation/presetLightingWitness.test.js

# ONCE, separately: EXPECTED RED until the terminal, with the predicted delta of §7.
GATE_MUTEX_TIER=shared … -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/sovereigntyLightingContract.walker.test.js

# ⭐ THE EXCLUDED DIRECTORY RUN — interim rule 8's named INSTRUMENT, and the run that surfaces
# row 6's re-address before the landing rather than after it. It must EXIT 0.
GATE_MUTEX_TIER=shared … -- npx vitest run --pool=threads --maxWorkers=2 tests/lint \
  --exclude=tests/lint/sovereigntyLightingContract.walker.test.js

node scripts/check-observed-shape-readers.mjs   # motion is a STOP
node scripts/check-writer-reach.mjs             # compare against step 1; growth is a STOP
node scripts/implementation-packets.mjs validate
npm run check:packet -- EM-B1h
npm run implementation:resume -- EM-B1h
```

Expected: every command exits `0`, **except** the named census interior red until the host train's
terminal. ⛔ Never read a gate through a shell pipe (§P7). ⛔ A member never runs `npm run check`.

---

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and §P8, stop if: the seal is missing or foreign; HEAD is not
the stamped base or a descendant proved non-interfering; ⛔ **`scripts/mutation-coverage-manifest.json`
is reserved by a non-terminal packet at dispatch** (EM-B1d today) **or is dirty from another
lane**; ⛔ **`ARGUED_ROSTER_CEILING` does not read 28 with exactly 28 roster keys at dispatch**, or
**either coupling baseline JSON would have to move**; ⛔ **a SECOND production file appears to need
editing** — above all `institutionLifecycle.js` at **798/800**; ⛔ **any `src/` file would have to
READ `WORLD_PULSE_FATE_KIND`** — that is EM-B1i's and it forfeits this packet's golden-neutrality;
⛔ **`LIFECYCLE_CLOSE_FATES` appears to need deriving from the kind** (R6); ⛔ **a FOURTH kind
appears necessary, or a member's kind would have to change** from §6.1's measured classification;
⛔ **any member would have to be added** that no writer produces, other than the chartered
`ruined_by_decree`; ⛔ **any member would have to be REMOVED** — that is a value a saved world may
carry; ⛔ **`null` appears to need to be a member**; ⛔ **the trigger comes out EMPTY, a single
token, or below ten**; ⛔ **the exemption register would have to gain a row**; ⛔ **a FLAGGED file
would have to be EDITED to satisfy the roster**; ⛔ a golden, the prose manifest or **the preset
witness** moves by one byte; ⛔ `tests/domain/ruinInstitution.test.js` reds (its LANDED A3's check
ORDER or A7's exact hit list — §12); `check-writer-reach` shows **growth**;
`check-observed-shape-readers` moves; `ruinInstitution`'s signature, its five keys or **their
order** would have to change; `institutionHistory[].fate` or its two sets appear to need editing
(R4); a measured `max-lines` figure for `calamityKernel.js` above 780;
`tests/lint/.lighting-census-baseline.json` would have to be edited here.

---

## 12. The `tests/` sweep and the LANDED-suite check (§P2 row 12, step 12) — EXECUTED

⭐ This is the step that convicted EM-B1d version 4, so it is a section rather than a line. Swept
by LITERAL and by VALUE (evidence §15).

| what | measured | verdict |
|---|---|---|
| ⛔ **every `fate` value passed to `ruinInstitution` anywhere in `tests/`** | **`DECREE_FATE = 'ruined_by_decree'` and `DISASTER_FATE = 'destroyed_by_disaster'` — and nothing else.** Eleven call sites, enumerated | ✅ **BOTH ARE MEMBERS ⇒ NO ARM OF THE LANDED SUITE REDS, AND NO `TEST` ROW IS OWED.** The one measurement that decides whether this packet owes a TEST row, and it is clean |
| ⛔ EM-B1e A3's **check ORDER** | Its arms assert `fate: ''`, a non-string and a missing `fate` all throw. §6 places the assert AFTER the type check, so each still throws on the SAME check | ✅ Unchanged — and **A2 asserts the order by message** |
| ⛔ EM-B1e A7's **exact hit list** | It asserts `status: 'ruined'` is written in exactly one `src/` file; the strip keeps string contents | ⚠ **A CONSTRAINT AND NOW AN ACCEPTANCE ARM (A7 of this packet)**, not merely a coding note |
| `ruined_by_decree` in the tree | **2 occurrences, both in `tests/domain/ruinInstitution.test.js`** (`:52`, `:373`) — EM-B1e's own landing put them there | ✅ a MEMBER; every use stays legal |
| exact pins on a fate value | **none.** No test asserts a fate list in either direction; no fixture spells a value outside the eighteen | ✅ no TEST row owed |
| fate values in committed test DATA | `captured_by_local_powers`, `shuttered`, `bankrupt`, `abandoned_with_the_settlement`, `founded_by_flourishing`, `destroyed_by_disaster` | ✅ **all six are members** |
| ⭐ **a RETIRED spelling on an old save** | **NONE.** `worldPulseFate` appears in **0 of 97** `.json` files under `tests/`, `src/` and `public/`; the three derivers returned exactly their current words across **every** revision (30 · 7 · all) | ✅ **no migration owed**, and the claim is historical rather than a snapshot. ⚠ **R5** — a user's saved world is not in this repo |
| ⭐ **a LINE-ADDRESSED register on the file this packet MODIFIES** | `tests/lint/.prose-numerics-baseline.json` carries **ONE** row on `calamityKernel.js`, at **:745**; this packet's `+2` at `:243-244` is ABOVE it | ⛔ **A ROW IS OWED — §7 row 6** (re-address 745 → 747, the `"line"` value only) |
| ⭐ **CITATIONS BY LINE into the modified file** (shared brief step 16) | `git grep -nE "calamityKernel\.js:[0-9]+" -- src docs/content tests` → **6 hits**, all in `src/domain/certification/subsystemRowsPeople.js` and `subsystemRowsPlace.js`, addressing `:409 :423 :522 :679 :742` — every one BELOW the insertion point, so each rots by +2 | ✅ **NO ROW OWED, MEASURED:** `tests/lint/sourceCitationIntegrity.walker.test.js`'s only GATING arm over `src/` is **ARM 1, the EOF arm** — and the file GROWS, so past-EOF is impossible. ARMs 3, 4 and 5 are **REPORT-ONLY** by their own headers. ⚠ The six addresses become stale by two and are reported, not touched; the walker will not convict them |
| the audit script | `scripts/audit/cause-lifecycle-soak.mjs:171` hand-builds `worldPulseFate: 'captured_by_local_powers'` | ✅ a member; not a declared command; no row owed |

---

## 13. ⭐ THE SECOND VOCABULARY, TABULATED — the chair's N2/R4 measurement

**Question 1 — is `LIFECYCLE_CLOSE_FATES` exactly the `closure` kind's members?** ⛔ **No: a
STRICT SUBSET, 3 of 15** (evidence §16).

| `closure` member | in `LIFECYCLE_CLOSE_FATES`? | | `closure` member | in? |
|---|---|---|---|---|
| `bankrupt` | ✅ | | `abandoned` | — |
| `closed_for_want_of_custom` | ✅ | | `abandoned_with_the_settlement` | — |
| `shuttered` | ✅ | | `abolished` · `disbanded` | — |
| | | | `captured_by_local_powers` · `downsized` · `hollowed_out` | — |
| | | | `privatized` · `reduced_to_watch_post` · `survives_as_remnant` | — |
| | | | `destroyed_by_disaster` · `ruined_by_decree` | — |

**Reverse: all 3 of 3 are `closure` members.** And `LIFECYCLE_BUILD_FATES` (`built`, `reopened`)
has **0 of 2** in `WORLD_PULSE_FATES`.

⇒ ⚠ **The damping counter `priorLifecycleCounts` (`:643-649`) counts only the three ECONOMIC
closures** — an abolition, a tier demotion, a disaster ruin and a settlement death all leave a
history entry the counter does NOT count as a close. Whether that is intended or a second defect
is **not this packet's to judge**. ⛔ **Deriving `LIFECYCLE_CLOSE_FATES` from the kind would make
`institutionLifecycle.js` a READER of the kind, and this packet has none** — it would forfeit the
golden-neutrality the chair ruled on, and it would also **change the counter's behaviour** (3 → 15
counted words), which is a lived-behaviour change needing the goldens measured first. **R6:
docket it to EM-B1i (which is already measuring that reader against the goldens) or to a
follow-on.**

**Question 2 — is `institutionHistory[].fate` the same vocabulary under a second key?**
⛔ **No: a DIFFERENT, OVERLAPPING vocabulary.** Measured over all ten of its write sites:

| | count | members |
|---|---:|---|
| `institutionHistory[].fate` values | **17** | — |
| ⭐ **SHARED** with `WORLD_PULSE_FATES` | **12** | `shuttered`, `bankrupt`, `closed_for_want_of_custom`, `abolished`, `disbanded`, and all seven demotion fates |
| **ONLY** `institutionHistory[].fate` | **5** | `built`, `reopened`, `founded`, `reactivated`, `added` |
| **ONLY** `WORLD_PULSE_FATES` | **6** | `abandoned_with_the_settlement`, `demoted_by_disaster`, `destroyed_by_disaster`, `founded_by_flourishing`, `ruined_by_decree`, `upgraded_by_reconstruction` |

⇒ **The walker's roster does NOT cover those write sites, and must not pretend to**: the key is
different, five of its words are not fates at all, and six fates never reach it. ⚠ The hazard R4
named is real and now exact: at `institutionLifecycle:1019`+`:1031`, `:1061`+`:1073` and
`tierOutcomeApply:162`+`:290`, **one variable is written to both keys in the same statement**, so
a later packet that adds a word to one silently adds it to the other. **That vocabulary owes its
own closure, and it is a different behaviour family. R4 stands, sharpened.**

---

## 14. Completion receipt

Base SHA · seal identity · final tree state · **the pre- and post-edit `max-lines` figures of
`calamityKernel.js`** (expect ≈457 → ≈459 of 800) · exact changed files and effective-line deltas ·
**the eighteen members quoted whole with the writer or deriver that produces each, ITS KIND, and
the three populations counted (15/1/2)** · acceptance A1–A7 executed · ⭐ **T3's FLAGGED hit list
quoted whole, the matcher named, the trigger's derivation printed, and BOTH controls' results** ·
**the derived homonym table as executed** · ⭐ **the leaf's `status:`-freedom proved in both
directions** · ⭐ **the no-reader-of-the-kind scan quoted** · the six mutants planted, convicted
and restored digest-exact (§P6) · focused commands, exits and counts · ⭐
**`tests/domain/ruinInstitution.test.js` green, with its A3 and A7 named** · ⭐ **the
`ARGUED_UNLAYERED` count and `ARGUED_ROSTER_CEILING` before and after (28 → 29), with PROOF that
neither coupling baseline JSON moved** · `invariants` count before and after, MEASURED at the
landing tip and stated as `+1` (no absolute is copied from this packet), with the
`+N / −0` diff shape quoted · the writer-reach and observed-shape comparisons (**recorded, never
written**) · sealed `check:packet` and `implementation:resume` statuses · both typecheck
configurations · gate stages actually executed · base-versus-wave failure identity diff · the
three golden digests before and at the tip · census tuple before and at the tip with the interior
red quoted verbatim · ⭐ **the measured minified byte delta into `advanceInterval.worker` for
TOOL-3 (predicted ≤1,008 B)** · edge-shared verdict: **NOT OWED** · generated artifacts `NONE` ·
deviations `NONE | STOP` · out-of-scope observations without investigation · **judgment calls.**

---

## 15. RAISED — for the chair

| # | Item |
|---|---|
| **R1** | ✅ **CLOSED.** The corrected figures are confirmed and the charter is amended (§934.47 addendum 28). |
| **R2** | ⭐ **SEQUENCING vs TOOL-3 — still open, now with a bigger figure.** This packet adds **≤1,008 B** to the uncapped `advanceInterval.worker` (up from version 1's 739 B: the KIND data the chair ruled in costs +269 B). With EM-B1e's +437 B that is **≈1.45 KB of the declared 4 KB per-train headroom**. **RECOMMEND: name both figures in TOOL-3's first attribution**, and note that EM-B1i will add a third. Not adjudicated here. |
| **R3** | ✅ **ACCEPTED by the chair** (`institutionLifecycle.js` 798/800 joins the standing hot list at its next sitting). **Confirmed: this packet's one MODIFY is `calamityKernel.js`**, and the second half of the question is answered by measurement — ⭐ **the guard belongs in the LEAF**: `assertWorldPulseFate` costs **98 B and +2 effective lines** in the kernel against **204 B and +4** for the inline predicate, and the message is spelled once where EM-B1a and EM-B1i will reuse it (§3.3). |
| **R4** | ⚠ **MEASURED AND SHARPENED (§13).** `institutionHistory[].fate` is a **different, overlapping** vocabulary — 17 values, 12 shared, 5 its own, 6 ours — so the walker's roster does not and must not cover it. The hazard is exact: **one variable is written to both keys in the same statement** at three sites. **It owes its own closure, in its own behaviour family.** |
| **R5** | ⚠ **UNCHANGED.** The retired-spelling proof covers the repo, not production. The guard validates a WRITE, never a READ, so an unknown stored value is still forwarded and still reads truthy. Closing the READ side is a different packet and needs a migration. |
| **R6** | ⛔ **NEW — `LIFECYCLE_CLOSE_FATES` MUST NOT BE DERIVED HERE, AND THE REASON IS TWO-FOLD.** (a) It would make `institutionLifecycle.js` a READER of the kind, and this packet has none — golden-neutrality is the chair's own condition. (b) ⭐ **It would CHANGE BEHAVIOUR**: the counter today counts **3** words and the `closure` kind holds **15**, so an abolition, a tier demotion, a disaster ruin and a settlement death would begin damping a settlement's rebuild odds where they do not today. **That is a lived-behaviour change and needs the goldens measured first. Docket to EM-B1i** (already measuring that file's reader against the goldens) **or to a follow-on.** |
| **R7** | ✅ **CLOSED** — DESIGN §15's stale sentence corrected by the chair. |
| **R8** | ⚠ **UNCHANGED.** `scripts/mutation-coverage-manifest.json` is reserved by EM-B1d at READY; the train order resolves it and the tail anchoring makes a same-day landing textually safe. |
| **R9** | ⭐⭐ **NEW — THE REGISTER VERSION 1 MISSED, AND THE ONLY NEW WORK v2 ADDS.** `tests/lint/couplingInclusion.walker.test.js` is a census **TOTAL over `src/domain/{worldPulse,spatial}`** whose own arm is *"a NEW unlayered module REDS."* The new leaf takes an `ARGUED_UNLAYERED` row (`kind: 'substrate'`, `reads: Object.freeze([])` — exact, the leaf has zero imports) and **`ARGUED_ROSTER_CEILING` 28 → 29 in the same diff** (an EXACT equality, deliberately so: *"a number that only has to be 'not exceeded' is not a ratchet, it is a budget"*). ⭐ **No cross-layer pair is minted and no baseline moves** — `scanCrossLayerPairs` skips unlayered deps and `calamityKernel.js` is itself unlayered. **Confirm the `reason` text at promotion** — it is the one piece of prose in this packet that enters a frozen register. |
| **R10** | ⛔ **NEW — EM-B1i'S DEFECT POPULATION IS THREE, NOT TWO.** The chair's fate for N8 named the two upswing fates. Measured: **`demoted_by_disaster` is the third** — its writer renames in place, changes no status and sets no flag (*"keeping the slot standing"*), so a demoted wizard's tower also reads as destroyed at `institutionDestroyed` and also severs a criminal leash. ⇒ **EM-B1i's before/after must cover all three `standing`/`rise` members**, and its golden measurement is correspondingly wider. **Amend EM-B1i's charter row.** |
| **R11** | ⚠ **NEW — THE KIND IS DATA WITH NO READER, SO NO ARM CAN PROVE A KIND *CORRECT*.** The walker proves each kind is present, single and drawn from the declared three; it cannot prove that `privatized` really deactivates. The kinds were derived at compile from the writers' record literals (§6.1), and **a writer that later changes what it does to the record without changing its fate word would leave the kind stale with nothing red.** This is stated in the walker's `CANNOT-CATCH:` header. ⭐ **EM-B1i, the first reader, is where it becomes catchable** — recommend EM-B1i's walker arm re-derive the kinds from the record literals and assert them against this packet's declaration. **Flagged, not adjudicated.** |
