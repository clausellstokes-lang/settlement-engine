# `settlement editor / wave 1` — EM-B1f: status-based absence at THE ONE PARTICIPATION CHOKEPOINT

- **Status:** `LANDED`
  READY-able at `141a1d775` (pre-proof version 4); version 2 waited on EM-B1k; version 3 lifted the wait by modelling EM-B1k's kernel at `32602dc60`. **Version 4 is the
  re-run the charter owed after EM-B1k2's landing** (ODQ §934.47 addenda 39, 46, 61): both
  dependencies are now REAL in the tree, the promotion measurement is re-executed against them
  at `141a1d775`, and the walker machinery the chair's Q5 ruling needs is measured and carried.
- **Landed at:** `6b57180ef51ed06ce8ed9048be7602351b497f1d` — the eleventh landing — 13 files (+206/−24): the chokepoint reads the NPC's status beside stasis and hostage; the union-totality walker's ninth roster row spelled `derived` with the machinery that makes the kind real; the undispositioned ceiling burned by one; two edge bundles and five metas re-stamped; sealed at c127cdfb2 (digest `4fe12b75…295c`, the capsule re-taken after the chair rotated the first seal aside), 17 steps in both sealed verbs with ONE named pre-existing red (check 7, the prose manifest's provenance title — the owner's door, §934.71) and the third amendment's sealed `tests/lint` step green on its first execution (173 files, 2757/2757); §6's contract and §13.0's base table re-cut at this flip; the chair's build + `verify:dist` at the landing in flight
- **Packet version:** `4`
- **Verified base:** `fixes-2026-09-18-consist` at `0cea60c7a78ac66184468db743c240cfa62f05cf`
- **Last revalidated:** 2026-09-20 at `0cea60c7a78ac66184468db743c240cfa62f05cf` — stamped by the chair (session a9df403c) at placement, the window re-run in the same command as the stamp: over every path of this packet except the expected movers (none) `git diff --stat 141a1d775..HEAD` printed NOTHING; every `requiredSymbols` row resolves; the CREATE targets are absent and untracked; no non-terminal packet reserves a change path. THE CHAIR'S CUTS AT PLACEMENT: none beyond the stamp. The Status value stands alone on its line (the parser's law). Its gated batches PAUSE-AND-RESUME.
- **Depends on:** **EM-B1d, LANDED at `95e494bdb`** (version 5) · **EM-B1k, LANDED at
  `19c4cb853`** (flipped `63e40fe57`) · ⭐ **EM-B1k2, LANDED at `e96a1c33e`** (flipped
  `bf0db7a7e`) — the charter's ordering constraint *"EM-B1k2 lands BEFORE EM-B1f"* (addendum 46
  Q3/Q4) is **DISCHARGED**. All three dependencies are terminal.
- **Collision group:** ⭐ **NONE at `141a1d775`** — measured: **193 register entries, 190 `LANDED`,
  2 `SUPERSEDED`, 1 `READY` (EM-P2)**, whose five change paths share none of this packet's
  thirteen. `tests/domain/roadsParticipation.test.js` is held by **no non-terminal packet**
  (EM-B1k2's claim retired at its flip). **PLACEMENT IS FREE** (§7.1).
- **Commit authority:** edits only; the chair commits
- **Baseline posture:** measured at `141a1d775` — the chokepoint's body; the ten required symbols
  by `grep -cF` at their exact lines; ⭐ **the sole-member-house cells RE-EXECUTED at this tip,
  arm ON and arm OFF, through the shipped `simulateCampaignWorldPulse({commit:true})` against
  BOTH landed cures** (§13); the ninth roster row and the `derived` spelling simulated **through
  the walker's own `offencesOf`, `FLAGGED` and `DECLARED_FLAGGABLE` derivations**; the five test
  homes through the lighting walker's own `parkReasonsFor`; the three forms' minified bytes under
  the repo's own esbuild; the worker closures and the eager closure through `vite.config.js`'s own
  export; the edge-shared write set by generator AND by precedent; every `.status` writer in
  `src/`; the line-addressed and stamped registers; effective lines before and after under
  eslint's own `Linter`
- **Retired symbols — WITHDRAWN at the seal (the chair, 2026-09-20 08:5x; ODQ §934.47 addendum 84):** the packet's one `retiredSymbols` row (EM-B1k2's `const UNDISPOSITIONED_CEILING = 7`) was moot before the seal — EM-B1k2's `requiredSymbols` row was re-spelled figure-free at `fff247009`, so nothing cross-packet names the figure this packet burns — and the validator's rule that a non-terminal packet's retiree must still be PRESENT (`scripts/implementation-packets.mjs:844-848`) reds the build's own §7 burn (7 → 6) at its `validate` step: state 2 EXIT 1, state 3 (row removed) EXIT 0, proven by the build lane. The row is removed from the manifest; the packet's §7.2 burn stands. The validator's third trap shape is TOOL-19's.
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: ce516004a5d8e680f4160b03ce660c66d78261988b78a82239664c596600af4b — stamped by the chair at placement)
  — ⓘ measured live at `141a1d775`:
  `16dfb96fa79320abc7dcfbdd8f5152b6aa66998c269287a951079df6c8223195` (the SECOND amendment,
  landed `e5f53ae95`), identical to the dispatch stamp. The v3 figure `b90a95b7…caa5e1` is
  superseded.

> **THE CHAIR'S REVALIDATION SENTENCE, ready to use at promotion:**
> *"Revalidated at `<tip>`: `git diff --stat 141a1d775 <tip>` over all thirteen change paths and
> all nine `requiredSymbols` paths is EMPTY; all ten `requiredSymbols` rows resolve `grep -cF` = 1
> at their recorded lines (157 · 151 · 105 · 92 · 618 · 94 · 113 · 124 · 178 · 671);
> `isOffStage` still reads no `.status`; `NPC_UNAVAILABLE_STATUSES` is still
> `['dead','exiled','jailed','removed']` and frozen; the derived TRIGGER is still
> `['dead','exiled','retired']`, `FLAGGED === DECLARED_FLAGGABLE` holds at six files, and
> `src/domain/roads/state.js` is absent from both; `pulseKernel.js`'s two raw-base seams
> (`:184`, `:579`) and `factionDensityKernel.js`'s raw `tickStart` (`:719`) are unmoved; the
> register holds ONE non-terminal packet sharing none of the thirteen; the preamble hashes to
> `16dfb96f…3195` at the pre-proof (and to `ce516004…af4b` after the third amendment, stamped at placement); and §13's arm-on/arm-off tables were re-executed at `<tip>` and are identical
> row for row."*

> ⚠ **THE WINDOW THIS PRE-PROOF MEASURED, quoted.**
> `git diff --stat 19c4cb853 141a1d775` over all twelve v3 change paths and all seven v3
> `requiredSymbols` paths moved **ONE file**:
> `tests/domain/roadsParticipation.test.js | 92 +++++++++++++++-- · 1 file changed, 89
> insertions(+), 3 deletions(-)` (EM-B1k2's census widening).
> ⛔ **AND THE WINDOW WAS BLIND TO THE TWO FILES §13's PREMISE RESTS ON**, which is why they
> become `requiredSymbols` rows in this version: `pulseKernel.js | 2 +-` (CURE-F, `96036427f`)
> and `factionDensityKernel.js | 22 +++++++-` (EM-B1k2, `e96a1c33e`) both moved inside the same
> window and neither was in it.

## 0. WHAT VERSION 4 CHANGED, AND WHY

Version 4 is the **pre-proof re-run the charter owed after EM-B1k2's landing** (ODQ §934.47
addendum 61's fate 5; addendum 39's debt; addendum 46's ordering). Version 3 answered the gating
measurement by MODELLING EM-B1k's kernel as an in-memory override at `32602dc60`. Both cures are
now LANDED, so the question is answered by the tree, and version 4 re-measures every fact at
`141a1d775`. Six things changed:

1. ⛔ **THE CHAIR'S Q5 RULING CANNOT BE CARRIED BY A ROW — IT NEEDS MACHINERY, MEASURED.**
   Addendum 46 (Q5) ruled *"the union-totality walker's roster rows gain `spelling: 'derived'`
   (FORM B's honest third kind), in EM-B1f's own packet since it adds the ninth row"*, and v3's
   §7 carried `spelling: 'literals'` with Q4 calling `derived` *"a tooling question, not this
   packet's"*. **EXECUTED through the walker's own derivations: a ninth row spelled `derived`
   REDS A3** — `DECLARED_FLAGGABLE` is built from `LITERAL_ROWS` (`spelling === 'literals'`
   only), so the row does not enter it while post-edit `state.js` DOES enter `FLAGGED`, and the
   set-equality convicts `src/domain/roads/state.js` by name. **And `offencesOf` has no `derived`
   branch at all**, so the value would be decorative: measured, a `derived` row and a `literals`
   row produce the identical (empty) offence list. Version 4 carries the minimal machinery that
   makes the kind real, measured green in every direction (§7, evidence §17).
2. ⭐ **§13 IS RE-EXECUTED AT THIS TIP AGAINST BOTH LANDED CURES, NOT AGAINST A MODEL.** EM-B1k
   (`19c4cb853`) made the tick's WRITE base raw; **EM-B1k2 (`e96a1c33e`) made the density lane's
   IRREVERSIBLE READ raw by construction** (`factionDensityKernel.js:719`) and left
   `tests/domain/irreversibleRawRoster.contract.test.js` as the standing guard. The arm-on and
   arm-off tables at `141a1d775` are **identical row for row**, with a liveness anchor proving
   the arm was really present in one run and absent in the other (§13).
3. ⛔ **v3's §13 EXECUTION BASE IS STALE AND THE WINDOW DID NOT SEE IT.** CURE-F (`96036427f`)
   re-spelled `pulseKernel.js:579`'s comparand after v3's probes ran, and EM-B1k2 rewrote
   `factionDensityKernel.js:719`. Neither file was a change path or a `requiredSymbols` path, so
   v3's J-T1 window reported an empty move over a premise that had changed twice. **Both become
   `requiredSymbols` rows** so the next revalidation cannot be blind to them (§5).
4. ⭐ **THE CENSUS QUARANTINE IS BANKED AND THE CEILING BURNS 7 → 6.**
   `src/domain/worldPulse/npcVerdictPulse.js` sits undispositioned in
   `UNDISPOSITIONED_NPCS_READERS` while being permanent roster writer 2's direct caller —
   EM-B1k2's lane measured its disposition and could not bank it (addendum 48's fate; addendum 61
   item 5). This packet writes the disposition, moves the row into `EXPECTED`, and burns
   `UNDISPOSITIONED_CEILING` to **6** (§7, §7.2).
5. **PLACEMENT IS FREE AND Q3 IS CLOSED.** The register holds 193 entries with ONE non-terminal
   packet (EM-P2), which shares none of this packet's thirteen paths. v3's §7.1 figures (191
   entries, EM-B3c the only non-terminal, EM-B1k2 not yet placed) are all superseded.
6. **Two v3 claims corrected and one confirmed by re-measurement:** the `docs/**` line citations
   into `state.js` number **TEN**, not nine (§7.2, §12.1); `status: 'remnant'` is written in
   **THREE** `src/` files (nine sites), not one (§12.1); and `entities/npcs.js` IS in the eager
   first-paint closure — re-derived through `vite.config.js`'s own export at **268 modules**,
   together with `src/domain/density/factionLifecycle.js`, which is the preamble's second
   amendment made good (§3.2).

⛔ **VERSION 3's OWN CORRECTIONS, ALL RE-CONFIRMED AT `141a1d775` AND STILL BINDING:**

1. ⛔ **v2's chunk row "eager first paint — NO (`roads/state.js` absent; BOTH IMPORTERS absent)"
   is REFUTED in its second half.** `src/domain/entities/npcs.js` **IS** in the eager first-paint
   closure (`main.jsx → store/index.js → settlementSlice.js → events/mutateEntities.js →
   entities/npcs.js`). It does not change this packet's price — `roads/state.js` is still absent
   from that closure and FORM B adds **zero** module edges — but the sentence was wrong and is
   struck (§3.2).
2. ⛔ **v2's "no writer in `src/` can assign `exiled` or `removed` to `.status`" needed one
   qualifier.** Two writers DO write `status: 'removed'` — `settlementLifecycleFirstClass.js:601`
   and `tierOutcomeApply.js:143` — and **both write an INSTITUTION**, never an NPC
   (`demotionFateForInstitution`; `EntityStatus` carries `removed` too). The claim now reads *no
   writer assigns any of the three to an **NPC's** `.status`*, which is what the arm is about (§6).
3. ⭐ **The ninth roster row's shape is no longer argued — it is executed.** The walker's own
   `offencesOf` was run over the post-edit source: `omits: ALL_BUT_DEAD` is LAWFUL, and **the
   charter's own ruling text (an omission naming `active`, `dead`, `missing`, `retired`) is
   CONVICTED TWICE** (§7, Q1).
4. The four test homes are CREDITED by the lighting walker's own `parkReasonsFor` (executed) —
   the `+4 titles` delta is measured, not assumed.
5. `checks` gains `tests/property/roadsDormancyGolden.test.js`, copying EM-B1k's own landed
   `checks` row: A5's dormancy claim is precisely what that golden pins. **Confirmed by addendum
   46 (Q6): the row stays.**
6. ⛔ **SUPERSEDED BY VERSION 4.** v3's placement paragraph measured `19c4cb853 → 96036427f` and
   named EM-B3c as the register's only non-terminal packet. Both are stale: EM-B3c LANDED
   (`ae4a643f7`), EM-B1k2 LANDED (`e96a1c33e`), EM-P2 v4 was placed READY, and the tip is
   `141a1d775`. The re-measured placement is §7.1.

---

## 1. Reconciled authority

1. **The owner, design §15 (ODQ §934.46):** *"A jailed or exiled holder cannot keep a seat."*
2. **The charter, amendments of 2026-09-19 17:36** (EM-B1f's six rulings; ODQ §934.47 addendum 22)
   — Q1 the arm is `exiled · jailed · removed`, never `missing`/`retired` (R18); Q2 the lazy
   engine's ~700 B margin is a raise-time convention and the literal assertion is the law; Q3
   price `advanceInterval.worker` as a DELTA, no per-packet re-mint; Q4 **FORM B, derived from
   `NPC_UNAVAILABLE_STATUSES` minus `dead`**; Q5 the ninth roster row is this packet's; Q6
   discharged; Q7 `envoyCasting`'s double refusal is the settled idiom.
3. **The charter, amendments of 2026-09-19 19:42 and 20:03–20:34** (ODQ §934.60 and its two
   addenda) — the two live defects; **EM-B1k cures them FIRST; EM-B1f lands after it WITH ALL
   THREE MEMBERS and does not narrow.**
4. **The charter, amendments of 2026-09-20 02:0x and 02:53** (ODQ §934.62, §934.47 addendum 39) —
   EM-B1k version 2 placed and built; **EM-B1k2** follows it as defence in depth and owns the
   participation ratchet's widening to `src/domain/density`.
4a. ⭐ **The charter, amendment of 2026-09-20 03:35 (ODQ §934.47 addendum 46) — FOUR RULINGS THIS
   PACKET CARRIES.** (Q1) the ninth row's omission is `ALL_BUT_DEAD`, the charter's earlier
   `active · dead · missing · retired` text corrected there. (Q3/Q4) **EM-B1k2 lands BEFORE
   EM-B1f** — DISCHARGED at `e96a1c33e`. (Q5) ⛔ **the roster rows gain `spelling: 'derived'`, IN
   THIS PACKET, since it adds the ninth row** — §7 carries it with the machinery the walker needs
   (v3's Q4 deferral is withdrawn as refuted by execution). (Q6) `roadsDormancyGolden` stays in
   `checks`. Plus the FATE: `tests/property/npcs.property.test.js:40`'s vacuous five-status arm
   → *"EM-B1f's pre-proof adds the TEST row if it fits the budget, else a FIX-T2 lane"* — it
   fits, and §7 carries it (Q2 below).
4b. ⭐ **The charter, amendments of 2026-09-20 03:39 and 05:27 (ODQ §934.47 addenda 48 and 61) —
   THE BANKABLE WIN IS THIS PACKET'S.** *"`npcVerdictPulse.js` sits in the census's quarantine
   while this packet proves its disposition — a bankable win → SLOT: EM-B1f's pre-proof (the
   next packet that touches the census)"*, restated at the landing as *"banks its disposition row
   and burns the ceiling 7 → 6"*. §7 carries both.
5. **The charter's train table:** **EM-T6** — EM-B1k2 landed as its opener; EM-B1f is next,
   because it ⛔ **BLOCKS EM-B1a's promotion** (B1a's `set-npc-status` is what makes `jailed`
   producible).
6. **EM-B1d version 5's own law, inherited verbatim** (`entities/npcs.js` header above
   `NPC_UNAVAILABLE_STATUSES`): *"`jailed` is UNAVAILABLE but still ON the roster… **Participation
   is a THIRD question, cured once at `isOffStage` (roads/state.js §8) by EM-B1f.**"*
7. **`DESIGN_THE_ROADS.md` §8** and **§1 law 5** — travel is narrative, captivity is mechanical.
8. **`EM-PREAMBLE.md` §P2 rows 10–11**; the pre-proof brief's steps 11–16.

### §1.1 · Resolved contradictions — five, each measured

- v1's *"`ROSTER_ABSENT_STATUSES` gains `jailed`"* → **withdrawn**: EM-B1d v5 left that constant
  untouched (`['dead','exiled','removed']`, `factionLifecycle.js:87`) and minted
  `NPC_UNAVAILABLE_STATUSES` instead. Evidence §4.
- v1's four `REGENERATE` rows → **invalid**: `PACKET_ACTIONS` is
  `['CREATE','DOC','MODIFY','REGISTER','TEST']` (`implementation-packets.mjs:30-36`). Seven
  `MODIFY` rows. Evidence §11.
- ⛔ **the charter's ruling (3) text — an omission naming `active`, `dead`, `missing`, `retired` —
  is REFUTED BY THE WALKER.** Executed: `'active'` is convicted as *"omits a word that is not a
  member of the live union"* (the check is against `NON_ACTIVE`, not `NpcStatus`), and dropping
  `exiled`/`jailed`/`removed` from `omits` is convicted as *"enumerates the union without exiled,
  jailed, removed"*. The lawful row is `omits: ALL_BUT_DEAD`. Evidence §7. **Measured, not chosen.**
- ⛔ **v2's "both importers absent from eager first paint"** → refuted; see §0 item 1. Evidence §9.
- ⛔ **v2's "no writer can assign `removed`"** → qualified; see §0 item 2. Evidence §13.
- ⛔ **v3's Q4 — *"should the walker gain a `spelling: 'derived'`? A tooling question, not this
  packet's"*** → **WITHDRAWN TWICE**: by the charter (addendum 46 Q5 assigns it here) and by
  execution (a `derived` row without machinery REDS the walker's A3 and means nothing to
  `offencesOf`). Version 4 carries the kind and its guard. Evidence §17.
- ⛔ **v3's "the ninth row is +2 effective lines and nothing else in the walker is touched"** →
  **superseded**: the ruled `derived` spelling costs the roster row plus `FLAGGABLE_SPELLINGS`,
  one changed `LITERAL_ROWS` line and a six-line `derived` branch in `offencesOf`, with two
  planted probes inside the EXISTING A2/A4 arm (so the lighting delta stays +0 titles there).
  Evidence §17.
- ⛔ **v3's "nine `docs/**` citations"** → **TEN**, re-counted at `141a1d775`. Evidence §19.
- ⛔ **v3's §13 "how it was driven" paragraph** (an in-memory override of `19c4cb853`'s
  `pulseKernel.js` at `32602dc60`) → **superseded by a real tree**: both cures are landed and the
  cells are re-executed against them, no overrides at all. Evidence §20.

The implementer does not read other documents to reinterpret this packet.

---

## 2. Outcome

**Observable result:** an NPC whose `status` is `exiled`, `jailed` or `removed` is OFF-STAGE for
every participation read, so a jailed or exiled holder cannot keep a seat.

**Definition of done:** `isOffStage` returns `true` for those three; the seven existing consumers
inherit it unchanged; `readWarSeatBooks` reads `unseated` for a jailed ruler exactly as it already
does for a dead, shelved or hostage one; and **no irreversible consumer acts on the widening**,
which §13 now proves by execution at the cured tree rather than assumes.

In scope: ONE arm on ONE predicate; integration by *inheritance* (no consumer is edited); the
prevention guard is EM-B1d's walker, which this packet joins as its ninth roster row.

Explicit non-goals: ⛔ `missing` and `retired` never join the arm (Q1; A2 pins both directions).
⛔ No writer — `set-npc-status` is EM-B1a's. ⛔ No holder guard, op, store surface, component or
flag. ⛔ No edit to `NPC_UNAVAILABLE_STATUSES`, `ROSTER_ABSENT_STATUSES` or any EM-B1d file.
⛔ No edit to `pulseKernel.js` or anything else EM-B1k or EM-B1k2 owns.

---

## 3. Hard scope budget

| Limit | Packet budget | This packet |
|---|---:|---:|
| Behavior families | `1` | **1** |
| New persisted record families · Named state writers · Feature flags · User-facing surfaces | `0 or 1` | **0 · 0 · 0 · 0** |
| Direct consumers | `<=2` | **0 new** (seven inherit) |
| New logic-bearing production leaves | `<=2` | **0** |
| Existing logic-bearing production files modified | `<=3` | **1** |
| Additional registration-only files | `<=3` | **1** |
| Handwritten files total | `<=12` | **6** |
| New/changed effective production lines | `<=400` | **+5 MEASURED** (≤6) |
| Delta in a shared/hot file | `<=15` | n/a — §3.1 |
| Acceptance cases | `<=8` | **7** |

Overrides approved before dispatch: `NONE`.

⭐ **HANDWRITTEN 5 → 6** because §7 gains `tests/property/npcs.property.test.js` (addendum 46's
fate). Acceptance cases stay at **7 of ≤8**: the property arm's observation folds into **A2**,
which already owns *"the arm, exact and derived"*, rather than minting an eighth case that would
leave the matrix with zero slack. **The chair may prefer an eighth case — Q2.**

### §3.1 · Hot files — RE-MEASURED at `141a1d775`; the one file edited is NOT hot

`src/domain/roads/state.js` = **288 effective lines** (596 raw) under eslint's own `Linter`
(`configType: 'flat'`, `max-lines` with `skipBlankLines` and `skipComments`) against the
`src/domain/**` ceiling of **800** (`eslint.config.js:712`): **512 lines of headroom**, no per-file
override, **no `scripts/.size-baseline.json` entry** (`grep -c` → 0), and absent from
`PACKET_STANDARD.md`'s standing hot list.

⭐ **AND THE POST-EDIT FIGURE IS MEASURED, NOT ESTIMATED.** The same `Linter`, over §6's exact
post-edit text written to a file: **293 effective lines (607 raw) — a delta of +5 effective, +11
raw**, leaving **507 lines of headroom**. The `≤6` budget row is therefore a measurement with one
line of slack, not a forecast. Evidence §16.

### §3.2 · ⭐ THE BUNDLE BUDGETS, PRICED (§P2 row 11) — ALL RE-MEASURED AT `141a1d775`

| budget | ceiling | member? | cost | obligation |
|---|---:|---|---:|---|
| generation worker `WORKER_BUNDLE_CEILING_BYTES` | `1401208` — ZERO slack (`generationWorkerLazy.test.js:159`, re-confirmed) | **NO** — measured twice, two methods | **0 B** | none |
| eager first paint `EAGER_FIRST_PAINT_MODULES` | closure budget | **NO** — `roads/state.js` absent from the **268-module** set ⚠ **`entities/npcs.js` AND `density/factionLifecycle.js` ARE eager** | **0 B** | none — the edge points the other way |
| lazy `engine` chunk | `< 679_000` (`vendorPdfLazy.test.js:787`, re-confirmed) | ⭐ **YES** | **+184 B measured**, bound **≤370 B** | §8's byte step |
| `advanceInterval.worker` | ⛔ **none today — re-confirmed: `grep -rn advanceInterval tests/build/` returns NOTHING**; **TOOL-3 mints one with a 4 KB per-TRAIN headroom** | ⭐ **YES** | **a +184 B DELTA against that headroom** | ⛔ **no per-packet re-mint (addendum 22's Q3 ruling)** |
| `townSceneExport` · `townScene` · `customContentPreview` workers | — | **NO** | **0 B** | none |
| edge-shared bundles | INPUT membership | ⭐ **YES — input of both** | 7 paths | §3.3 |

⭐ **HOW THE CLOSURES WERE MEASURED, so the figures can be refused.** The eager set is
`vite.config.js`'s OWN exported derivation (`EAGER_FIRST_PAINT_MODULES`, `:309`), imported and
enumerated — **268 modules**, matching the preamble amendment's own re-execution — never a
replica. The worker closures are a static walk from each worker entry following only static
`from '…'` edges (a dynamic `import()` is a chunk boundary): `advanceInterval.worker.js` **544
modules, `roads/state.js` a MEMBER**; `generation.worker.js` **145, NOT a member**;
`townSceneExport.worker.js` **125, NOT a member**. ⚠ **HONEST LABEL:** the MEMBERSHIP verdicts are
**CONFIRMED** and agree with v3's independent measurement in every row; the module COUNTS differ
from v3's (549 / 220) because the two walks treat dynamic imports differently — `townSceneExport`
agrees exactly at 125. Nothing in this packet's price depends on a count.

**The bytes, RE-MEASURED at `141a1d775`** (each form written over the real file, minified with the
repo's own esbuild 0.28.1, `--minify --format=esm --target=es2022`): base **7,727 B**;
**FORM A** (three literals spelled here) **7,885 B = +158 B**; ⭐ **FORM B as a SPECIFIER on the
import `state.js` already has** **7,911 B = +184 B — CONTRACTED**; FORM B as a second import
statement was measured at v3 as 7,946 B = +219 B. The base file is byte-identical to versions 2
and 3 (SHA-256 `23ffc63bb6f39944defe147241f7f9ddbc75966509efb1bcaa4176b789676481` all three
times), so every figure reproduces exactly.

⭐ **THE IMPORT IS NOT A NEW EDGE.** `state.js:36` already reads
`import { importanceWeight } from '../entities/npcs.js';`. Measured: `entities/npcs.js` is already
in the `advanceInterval.worker` closure; **its own closure is 2 modules** (itself and
`src/kernel/slugify.js`) and **does not reach** `roads/state.js` — no cycle; **0 modules are added
to any closure.** FORM B's whole cost over FORM A is **+26 B of code**, and it buys a single
source of truth for the vocabulary.

⚠ **HONEST LABEL:** `+184 B` is **CONFIRMED** as a minified-source delta, **PLAUSIBLE** as the
rendered-chunk delta (esbuild is not Rollup's renderer). Hence the ×2 bound.

Under **Q2** the lazy engine's `< 679_000` literal is the law between re-mints; the build lane
measures at this packet's own base and asserts growth ≤370 B with only `roads/state.js` moved.

### §3.3 · ⛔ THE EDGE-SHARED OBLIGATION — SEVEN PATHS, re-established BOTH ways at `141a1d775`

**BY THE GENERATOR.** `src/domain/roads/state.js` is an INPUT of both `aiCharterBundle`
(**114** inputs) and `aiOutputSchemaBundle` (**115**) — membership, never entry-hood (§P2 row 10)
— and of neither `aiGroundingBundle` (**74**), `analyticsEventsBundle` (**2**) nor
`intentAtlasBundle` (**2**); every count and every membership re-read from the live
`*.meta.json` at the tip. `build:edge-shared` runs `scripts/build-edge-shared.mjs`, whose **five**
`ENTRIES` (`:33`) each write `generatedAt: new Date().toISOString()` (`:83`) inside one `for` loop
(`:108`), so a run that moves two bundles writes **7 paths**: 2 bundles + their 2 metas + the 3
siblings' re-stamped metas.

⭐ **BY PRECEDENT, exactly and twice.** `git show --stat 95e494bdb -- supabase/functions/_shared`
(EM-B1d's own landing) prints those SEVEN paths and no others — `aiCharterBundle.js | 5 ++++-`,
`aiCharterBundle.meta.json | 4 ++--`, `aiGroundingBundle.meta.json | 2 +-`,
`aiOutputSchemaBundle.js | 5 ++++-`, `aiOutputSchemaBundle.meta.json | 4 ++--`,
`analyticsEventsBundle.meta.json | 2 +-`, `intentAtlasBundle.meta.json | 2 +-`. `ee8ac6c3c` shows
the same seven. ⛔ **Both methods were used, as pre-proof step 11 requires, and neither was run in
a read tip** — the generator is gated and was read, never executed.

---

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- EM-B1f
```
Check by check, at a base set to the tip: **branch/ancestry** — `head === verifiedBase`, the
substrate arm returns early; **substrate** — no `REGISTER` row is declared, so no declared-substrate
file sits in the window; **CREATE targets absent** — there is **no CREATE row**, so the check is
vacuous; **non-CREATE targets clean** — thirteen pre-existing paths, every one present at
`141a1d775` and git-clean; **required symbols resolve** — the **ten** rows of §5, each
`grep -cF` = 1 at `141a1d775` at the recorded line (executed, evidence §15).

⭐ **`node scripts/implementation-packets.mjs validate` PASSES ON PLACEMENT at `141a1d775`.**
`reservesChangePaths = !TERMINAL_PACKET_STATUSES.has(status)` (`implementation-packets.mjs:676`)
with `TERMINAL = {LANDED, SUPERSEDED}` (`:43`), and a duplicate path across two non-terminal
packets is an ERROR (`:694`). Measured over `docs/implementation/PACKET_MANIFEST.json` at the
tip: **193 entries — 190 `LANDED`, 2 `SUPERSEDED`, 1 `READY`.** The one `READY` is **EM-P2**,
whose five change paths are `src/domain/generation/generationForkRegistry.js`,
`tests/lint/generationForkRegistry.contract.test.js`, `tests/generators/generationForkCensus.test.js`,
`tests/helpers/generationForkCensus.js` and `scripts/mutation-coverage-manifest.json` — **not one
of this packet's thirteen.** Re-measured — §7.1.

---

## 5. Verified tree contract — every row re-found BY SYMBOL at `141a1d775`

⭐ **TEN ROWS, not eight.** The two new rows are the substrate §13's premise rests on. They are
required symbols under the standard's own test — a fact the deliverable must find unchanged —
and their absence from v3's window is exactly how CURE-F and EM-B1k2 moved §13's base without the
revalidation noticing (§0 item 3).

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| **The chokepoint** | `src/domain/roads/state.js` | `export function isOffStage` (`:157`) | `isInStasis(npc) \|\| whereabouts.state === 'hostage'`; **never reads `.status`**. Pure, total, absence-safe (`isOffStage(null)` and `isOffStage(undefined)` both `false`, executed). | THE one place the arm is added. |
| **Inherited predicate** | `src/domain/npc/npcOps.js` | `export function isInStasis` (`:151`) | its vocabulary is `STASIS_REASONS`, a per-layer fact, **not** `NpcStatus`. | Preserve; build beside it. |
| **The availability vocabulary** | `src/domain/entities/npcs.js` | `export const NPC_UNAVAILABLE_STATUSES` (`:105`) | `Object.freeze(['dead','exiled','jailed','removed'])`; its header names EM-B1f as the cure for the participation question. | ⭐ **The arm DERIVES from it** (FORM B). Never re-spelled. |
| **Sole seat read** | `src/domain/worldPulse/warSeatBooks.js` | `function rosterNpcById` (`:92`) — ⛔ **module-private** | `:102` pairs `=== 'dead'` with `isOffStage`. | Preserve UNEDITED — the widening ARRIVES here. |
| **Exported route to it** | `src/domain/worldPulse/warSeatBooks.js` | `export function readWarSeatBooks` (`:618`) | calls it at `:633`; `!rulerId \|\| !ruler` returns `securityBand:'unseated'`, `seatWeight01:0`, omits `rulerId`. | A1 observes through it. |
| **Master gate** | `src/domain/worldPulse/worldSnapshot.js` | `export function buildWorldSnapshot` (`:94`) | `:127-129` filters off-stage NPCs out of the settlement every pulse kernel reads; the RAW save roster is untouched. | A4 observes through it. ⭐ **And EM-B1k now makes the tick's write base the RAW roster, so this is a READ PROJECTION and nothing else** (§13). |
| **The (formerly) irreversible consumer** | `src/domain/density/factionLifecycle.js` | `export function factionRosterOf` (`:113`) | `:117` filters `settlement.npcs` by `isOnRoster` (`:96-99`), whose vocabulary is `ROSTER_ABSENT_STATUSES = ['dead','exiled','removed']` (`:87`); `factionLifecycleStateOf` (`:129`) returns `crewed` on a non-empty roster (`:130`) and `dissolved` otherwise (`:131`). ⭐ **EM-B1k2 makes BOTH its production callers hand it a RAW roster by construction.** | ⛔ Named so §13 cannot be lost. Not edited. |
| ⭐ **THE RAW WRITE BASE (new in v4)** | `src/domain/worldPulse/pulseKernel.js` | `function buildSettlementMap` (`:178`) — ⛔ **module-private** | `:184` prefers `item.save?.settlement` over the filtered `item.settlement`; `:579` restores the raw roster onto the tick's settlement. ⚠ **`:579` was re-spelled by CURE-F (`96036427f`) AFTER v3's §13 probes ran** — its comparand is now `item.settlement?.npcs`, not `vaulted.settlement?.npcs`. | ⛔ **NOT EDITED.** §13's first premise. In the window so a third re-spelling cannot pass unseen. |
| ⭐ **THE RAW IRREVERSIBLE READ (new in v4)** | `src/domain/worldPulse/factionDensityKernel.js` | `export function advanceFactionDensity` (`:671`) | `:719` reads `tickStart = asObject(asObject(item.save).settlement \|\| item.settlement)` — the RAW save roster BY CONSTRUCTION (EM-B1k2). The R18 law is no longer computed from a projection and rescued downstream. | ⛔ **NOT EDITED.** §13's second premise, and the reason this packet's widening cannot reach a dissolution. Guarded by `tests/domain/irreversibleRawRoster.contract.test.js`. |
| **Prevention guard** | `tests/lint/statusUnionTotality.walker.test.js` | `const CONSUMER_ROSTER` (`:124`) | eight rows; `spelling` is CHECKED, not trusted; its `CHOKEPOINT` constant (`:121`) already says *"status-based absence joins that chokepoint in EM-B1f, not here"*. ⛔ **MEASURED: `DECLARED_FLAGGABLE` (`:264`) is built from `LITERAL_ROWS` (`:263`), which selects `spelling === 'literals'` ONLY, and `offencesOf` (`:275`) branches on `'union-read'` and nothing else** — so the ruled third kind needs machinery, not just a row. | ⛔ Gains this packet's ninth row, **APPENDED**, plus the `derived` kind's machinery and its two planted probes (§7). |
| **The set-equality that convicts** | `tests/lint/statusUnionTotality.walker.test.js` | `const DECLARED_FLAGGABLE` (`:264`) | A3 asserts `FLAGGED` set-equal to it, both directions. Executed at the tip: both are the same **six** files and `src/domain/roads/state.js` is in neither. Post-edit, `state.js` enters `FLAGGED` (it spells `'dead'` at the `filter`) — so the ninth row is what keeps A3 green, and deleting it REDS by name. | ⛔ The ONE line the `derived` kind must change; A7 is its guard. |
| **Test precedent** | `tests/domain/warSeatBooks.test.js` | `it('never lets a dead roster holder keep deciding through a stale top rung')` | asserts `interestKind:'realm'`, `settlementWeight01:1`, `seatWeight01:0`, `securityBand:'unseated'`, `rulerId` undefined. | ⭐ A1 copies this shape verbatim with `'jailed'`. |
| **Test precedent** | `tests/domain/roadsState.test.js` | `describe('roads state — isOffStage …')` (`:30`) | four straight-line `it`s. | A2/A3 extend it; no new `describe`. |

Forbidden alternatives: no second participation predicate or status vocabulary; ⛔ **no edit to any
of the seven consumers**; ⛔ no edit to any EM-B1d, EM-B1k or EM-B1k2 file; no new `worldState` key,
PRNG stream, time source or writer; no files outside the manifest.

---

## 6. Exact contracts

> **RE-CUT AT THE FLIP (the chair, 2026-09-20; ODQ §934.47 addenda 84, 86):** the verbatim contract as pre-proofed did not typecheck — `NPC_UNAVAILABLE_STATUSES` is `readonly NpcStatus[]`, so `.includes(<string>)` is `TS2345` on BOTH sealed configs (+1 against a baseline of 0). The sealed build cured it with the file's adjacent idiom, widening the HAYSTACK (`/** @type {readonly string[]} */ (OFF_STAGE_STATUSES)`), never casting the untrusted runtime string into `NpcStatus`; nothing measured moved (293 effective, +184 B, the declaration line byte-identical; both typechecks at their ceilings). The two lines above now read as the tree does.

### The predicate, after this packet — the whole diff, verbatim (FORM B, the chair's)

```js
// at :36 — a SPECIFIER on the import this file already has; not a new module edge.
// ⭐ PRECEDENT, BYTE FOR BYTE: src/domain/worldPulse/envoyCasting.js:50 already reads
//    `import { NPC_UNAVAILABLE_STATUSES, importanceWeight } from '../entities/npcs.js';`
import { NPC_UNAVAILABLE_STATUSES, importanceWeight } from '../entities/npcs.js';
```
```js
/**
 * The statuses that put a person OFF-STAGE, DERIVED from the availability vocabulary so this
 * file never spells the union a third time (entities/npcs.js owns it; EM-B1d v5's law).
 * `dead` is subtracted because the three seat/ladder consumers pair `=== 'dead'` themselves.
 */
export const OFF_STAGE_STATUSES = Object.freeze(
  NPC_UNAVAILABLE_STATUSES.filter((s) => s !== 'dead'),
);

export function isOffStage(npc) {
  if (isInStasis(/** @type {Parameters<typeof isInStasis>[0]} */ (npc))) return true;
  const o = npc && typeof npc === 'object' ? /** @type {Record<string, unknown>} */ (npc) : null;
  if (o && /** @type {readonly string[]} */ (OFF_STAGE_STATUSES).includes(String(o.status || '').toLowerCase())) return true;
  const w = o ? o.whereabouts : null;
  return !!(w && typeof w === 'object' && /** @type {Record<string, unknown>} */ (w).state === 'hostage');
}
```
⛔ **The declaration line `export function isOffStage(npc) {` is unchanged byte-for-byte.**
⛔ **`OFF_STAGE_STATUSES` evaluates to `['exiled','jailed','removed']` and is frozen — executed
in seven probe cells at the cured tree.** A member added to `NPC_UNAVAILABLE_STATUSES` later joins
the arm automatically; that is the point of deriving, and A2 pins the current value so the widening
is never silent.

### Inputs, outputs, absence — every row below EXECUTED in §13's probes

- **absent `status` key** ⇒ `''` ⇒ **on-stage** — the case the corpus made load-bearing (4,884 of
  5,171 generated NPCs carry no `status` key);
- **`null` / `undefined` npc** ⇒ `false`, exactly as today;
- **non-string `status`** ⇒ `String(...)` coerces; `0` ⇒ `''` ⇒ on-stage (executed);
- **`'JAILED'`** ⇒ lowercased ⇒ off-stage, matching `warSeatBooks:102` / `npcLadderState:205` /
  `npcLadderKernel:660` (executed);
- **unknown token (`'sabbatical'`) / empty string** ⇒ on-stage (executed). The predicate never
  guesses.

### Ordering · determinism · flag · dormancy · golden posture

Stasis first (unchanged), then status, then hostage — a disjunction, so the order is not
observable; fixed so a future arm is one block. No hash/fork key, no draw, no rounding. **Flag:
NONE** — the arm is deliberately outside `roadsActive`, as `isInStasis` already is. **Dormancy:
presence-driven** — a world with no such NPC evaluates one `includes` on a 3-member frozen array
and behaves identically, including `buildWorldSnapshot` returning the SAME settlement reference
(A5), which `tests/property/roadsDormancyGolden.test.js` pins across the dormant corpus.
**Golden posture: UNCHANGED**, re-measured by execution at `19c4cb853`: across the FULL 525-row
corpus, 5,171 generated NPCs carry only `<ABSENT>` (4,884) or `'active'` (287) — **zero** of the
arm's three members.

### Lifecycle

| Create | Read | Persist | Reload | Regenerate | Undo | Import/migrate | Public veil |
|---|---|---|---|---|---|---|---|
| nothing | a READ of an existing union member | nothing | unchanged | unchanged — generation writes none (525/525 measured) | unchanged | ⛔ unchanged: an imported or forked save reads identically, the predicate being pure over the record | ⛔ unchanged — the veil reads the SAVE, not the participation view, so the jailed person still appears in the dossier as jailed |

### Receipts, privacy, alignment, edit story

No receipt; no rendered figure (⇒ prose-numerics NOT owed, and the baseline does not name this file
— measured); no DM-only field; projection unchanged. Alignment **DECLARED EMPTY**. Edit story
**ENGINE-ONLY** — the DM's route is EM-B1a's.

### ⛔ The behaviour change, stated so it can be refused

A world holding an NPC stored as `exiled` or `removed` changes its **participation** reading the
moment this lands. **Measured at `19c4cb853`: no writer in `src/` assigns any of the three to an
NPC's `.status`.** The NPC writers are `killNpc` (`npcs.js:186`, `'dead'`), `createNpc`
(`npcs.js:277`, `'active'`), `mutateEntities.js:230/241/410`, `undoEvent.js:459`,
`magicFormsPractitioner.js:220`, `factionRoles.js:251`, `lineageMemberBirth.js:303`,
`densityAscension.js:145` and `factionDensityKernel.js:610` (all `'active'`), and `addNpc`
enumerates its fields without forwarding `event.payload.status`. ⛔ **THE ONE CORRECTION TO
VERSION 2:** two `src/` sites DO write `status: 'removed'` —
`settlementLifecycleFirstClass.js:601` (`{ ...inst, … }`) and `tierOutcomeApply.js:143`
(inside `demotionFateForInstitution`) — and **both write an INSTITUTION**, whose `EntityStatus`
union (`entities/status.js:23`) carries `removed` as a homonym. Neither can reach an NPC record.
⇒ **inert by construction on every world that exists.** `jailed` becomes producible only with
EM-B1a, which this packet blocks.

⭐ **AND THE EXCEPTION THAT BLOCKED VERSION 2 IS GONE — §13 proves it by execution.**

---

## 7. Exact change manifest — THIRTEEN paths, set-equal with the JSON

| Action | File | Symbol/region | Max delta | Coding instruction |
|---|---|---|---:|---|
| `MODIFY` | `src/domain/roads/state.js` | `isOffStage` (`:157`), import (`:36`) | **+5 eff MEASURED** (≤6) | §6 verbatim. Declaration line byte-identical. ⛔ Stales two edge bundles — see the seven rows. |
| `MODIFY` | `tests/lint/statusUnionTotality.walker.test.js` | `CONSUMER_ROSTER` (`:124`), `LITERAL_ROWS` (`:263`), `offencesOf` (`:275`), the A2/A4 arm (`:337`) | **+12 eff** | ⭐ **FOUR EDITS, ALL MEASURED GREEN TOGETHER (evidence §17); the chair's Q5 ruling needs all four.** ⑴ **THE NINTH ROW, APPENDED AT THE END** (never inserted: A3 drives three synthetic probes off `CONSUMER_ROSTER[3]`, the `envoyCasting` row): `{ file: 'src/domain/roads/state.js', symbol: 'isOffStage', spelling: 'derived', enumerator: false, omits: ALL_BUT_DEAD, why: <≥40 chars> }`. ⑵ **`FLAGGABLE_SPELLINGS = Object.freeze(['literals', 'derived'])`** above `LITERAL_ROWS`, and `LITERAL_ROWS` selects `FLAGGABLE_SPELLINGS.includes(r.spelling)` — ⛔ **without this the row REDS A3 by name** (measured). Rename `LITERAL_ROWS` → `FLAGGABLE_ROWS` (module-private; a name is a claim). ⑶ **the `derived` branch in `offencesOf`, ABOVE the `union-read` branch**: a `derived` row is convicted when the file spells a `RETIRED_FOREIGN_SPELLINGS` word (with its `file:line`) or does not read `AVAILABILITY_VOCABULARY` — ⛔ without it the kind checks NOTHING and a `derived` row is indistinguishable from a `literals` one (measured). ⑷ **two planted probes INSIDE the existing A2/A4 `test`** (never a new arm — a new `it`/`test` is a lighting title): an honest FORM B source passes, a source that does not read the vocabulary is convicted by name. The `why` must say: *"THE PARTICIPATION CHOKEPOINT ITSELF. It spells only `'dead'`, and only to SUBTRACT it: the members it acts on — exiled, jailed, removed — are DERIVED from NPC_UNAVAILABLE_STATUSES and never spelled here, so `omits` lists words this file does not WRITE rather than words it ignores. `missing` and `retired` are genuinely excluded: a reversible absence keeps the place (R18)."* ⛔ Nothing else in the walker is touched; `TRIGGER`, `FOREIGN` and the exemption register are unmoved (measured). |
| `TEST` | `tests/domain/roadsState.test.js` | the `isOffStage` describe (`:30`) | `n/a` | A2 + A3 — **one** new straight-line `it`. No new `describe`. ⛔ Never name a variable or parameter `it`, `test` or `describe`. |
| `TEST` | `tests/domain/warSeatBooks.test.js` | beside the dead-holder case (`:111`) | `n/a` | A1 — **one** new straight-line `it`, copying the dead-holder shape verbatim with `'jailed'`. |
| `TEST` | `tests/domain/roadsParticipation.test.js` | the master-gate describe (`:33`); ⭐ **the census block (`:187`)** | `n/a` | A4 + A5 + A7 — **two** new straight-line `it` in the master-gate describe. ⭐ **AND THE BANKING (addendum 61 item 5), which REVERSES v3's "do not touch" on this block:** move `'src/domain/worldPulse/npcVerdictPulse.js'` out of `UNDISPOSITIONED_NPCS_READERS` (`:480`) into `EXPECTED` in sorted position (between `npcVerdictApply.js` `:395` and `partyImpact.js` `:396`) with the written disposition §7.3 supplies, and burn `UNDISPOSITIONED_CEILING` (`:493`) **7 → 6**. ⛔ Both edits sit inside EXISTING arms, so **+0 titles**. ⛔ Do not touch the `EXPECTED` rows EM-B1k2 wrote, the roots or the `-e` flags in `foundReaders` (`:512`). ⛔ Do not disturb EM-B1k's two `it`s at `:65` and `:108`. |
| `TEST` | `tests/property/npcs.property.test.js` | the `createNpc` shape arm (`:40`) | `n/a` | ⭐ **NEW IN v4 — addendum 46's fate, and it is a REAL defect, not a tidy-up.** MEASURED: `createNpc` **does** forward a caller's status (`entities/npcs.js:160`: `status: (input.status \|\| 'active')`), and the arbitrary (`:20-31`) declares no `status` key — so the arm asserts membership of a FIVE-word list (`active, dead, missing, exiled, retired`) that omits `jailed` and `removed`, over a value that is always `'active'`. Cure, two lines and no new title: give the arbitrary a `status` drawn from the SEVEN-member union, and widen the assertion to the same seven. ⛔ **No new `it`/`test`** — the widening lands inside the existing `test`. ⛔ This file is under `tests/property/` but is NOT a golden; STOP condition 4 is unaffected. |
| `MODIFY` | `supabase/functions/_shared/aiCharterBundle.js` | whole artifact | `n/a` | ⛔ Written by `npm run build:edge-shared`, **LAST** in `checks`. Never hand-edited. |
| `MODIFY` | `supabase/functions/_shared/aiCharterBundle.meta.json` | whole artifact | `n/a` | same run |
| `MODIFY` | `supabase/functions/_shared/aiOutputSchemaBundle.js` | whole artifact | `n/a` | same run |
| `MODIFY` | `supabase/functions/_shared/aiOutputSchemaBundle.meta.json` | whole artifact | `n/a` | same run |
| `MODIFY` | `supabase/functions/_shared/aiGroundingBundle.meta.json` | `generatedAt` | `n/a` | ⛔ A SIBLING re-stamped by the same window (step 11). Declared so the seal does not drift. |
| `MODIFY` | `supabase/functions/_shared/analyticsEventsBundle.meta.json` | `generatedAt` | `n/a` | same |
| `MODIFY` | `supabase/functions/_shared/intentAtlasBundle.meta.json` | `generatedAt` | `n/a` | same |

**Generated artifacts:** ⛔ **SEVEN**, all by `npm run build:edge-shared`, never by hand.
⛔ **No CREATE row. No REGISTER row. Exactly four new `it` and no new `describe`.**
⛔ **`REGENERATE` is not a `PACKET_ACTIONS` member** (`implementation-packets.mjs:30`) — every
generated row is `MODIFY`.
⛔ **A measured NEGATIVE never enters this table** — the four are in §7.2 (the chair's rule of
2026-09-20, from EM-B1k v2's fifth cut).

### §7.1 · Placement — RE-MEASURED at `141a1d775`; Q3 is CLOSED

`TERMINAL_PACKET_STATUSES = {LANDED, SUPERSEDED}` (`implementation-packets.mjs:43`);
`reservesChangePaths = !TERMINAL.has(status)` (`:676`); a duplicate path across two non-terminal
packets is an ERROR (`:694`).

- ⭐ **At `141a1d775`: 193 entries — 190 `LANDED`, 2 `SUPERSEDED`, 1 `READY`.** The single
  non-terminal packet is **EM-P2**, whose five change paths share **none** of this packet's
  thirteen. **PLACEMENT IS FREE.** ✓
- ⭐ **`tests/domain/roadsParticipation.test.js` is held by NO non-terminal packet.** EM-B1k's
  claim retired at `63e40fe57` and EM-B1k2's at `bf0db7a7e`. **v3's Q3 — the two-way placement
  order — is CLOSED by the charter's own ruling (addendum 46: EM-B1k2 first) and by its landing.**
- ⚠ **EM-P2 reserves `scripts/mutation-coverage-manifest.json`.** This packet declares no row
  there (§7.2 P2.2, re-measured), so the two do not collide — but a later revision of this packet
  that CREATEs under an enforcer dir would, and would have to wait for EM-P2 to flip.
- **No packet in the register declares `src/domain/roads/state.js` as a change path.** ✓
- All ten `requiredSymbols` re-confirmed `grep -cF` = 1 at `141a1d775`, each at its recorded line
  (157 · 151 · 105 · 92 · 618 · 94 · 113 · 124 · 178 · 671). Evidence §15.

### §7.3 · ⭐ THE DISPOSITION TO BE WRITTEN — `npcVerdictPulse.js`, measured not asserted

The row moves into `EXPECTED` carrying this judgement, whose three claims are each executed in
evidence §18. The implementer writes it as a comment above the path, in the shape EM-B1k2's own
dispositions model:

> ⭐ **EM-B1f — PERMANENT ROSTER WRITER 2's DIRECT CALLER, and the row that closes the gap
> EM-B1k2 left.** `successorNpc.js`'s row above is ABOUT `replaceOustedNpcs`, and names this file
> as the binder of its base — yet this file itself sat in the quarantine, undispositioned. **NOT
> via-snapshot, and RAW BY ITS CALLER.** `applyOrganicNpcVerdicts` reads `.npcs` once
> (`:109`) off `verdictSettlement`, bound at `:100` from the `settlement` parameter, and
> `pulseKernel.js:662` hands it `s` — the tick's settlement, made RAW by EM-B1k at `:184`/`:579`.
> Its second roster use is `replacementSource` (`:99`), handed to `replaceOustedNpcs` at `:133`:
> the same raw object, which is why writer 2's contract holds. **PARTICIPATION-INDEPENDENT AND
> REQUIRED TO BE**, for the `npcVerdictApply` / `stripNpcInfluence` reason: an ouster verdict is a
> MUTATION read, and it lands on a person whether or not they were on stage this tick — read
> through the participation view, a shelved or jailed figure could not be sentenced at all, and
> `replaceOustedNpcs`, which RETURNS A WHOLE ROSTER, would drop everyone the filter removed. ⚠ And
> one honest residual, recorded rather than hidden: `npcId` (`npcAgency.js:194`) falls back to
> `npc_${index}` for a record carrying no `id`, `name` or `label`, so on a FILTERED base such a
> record could be misaddressed; EM-B1k2's A6 measured 0 of 5,171 corpus NPCs without a display
> name, so the fallback is unreachable today and the raw base keeps it that way.

**And the ceiling burns.** `UNDISPOSITIONED_NPCS_READERS` (`:480`) goes from seven rows to six
(`envoyCasting · oathHolder · sovereigntyNews · warDeployment · warRulingsNews · warSeatBooks`),
and `UNDISPOSITIONED_CEILING` (`:493`) from `7` to **`6`** — a shrink-only ratchet, monotone down,
burned never padded. ⛔ **The new figure is `6`, stated here so the implementer does not derive it
from the array it is meant to cap.**

### §7.4 · ⛔⛔ THE BURN IS TRAPPED BY A LANDED `requiredSymbols` ROW — A CHAIR ACT IS OWED BEFORE DISPATCH

⭐ **This is the pre-proof's step-10 finding, and it would have STOPPED the build at its own seal.**

**EM-B1k2 is `LANDED` and its manifest requires the VERBATIM TEXT**
`const UNDISPOSITIONED_CEILING = 7` **at `tests/domain/roadsParticipation.test.js`**, with the
`_note`: *"⭐ ADDED in version 2 (nothing removed): the ceiling at :422, quoted WITH its value so
the no-raise is provable verbatim rather than by inspection."*

⛔ **Burning the ceiling to `6` deletes that exact text.** `scripts/implementation-packets.mjs`
runs a **total, status-blind existence check** on every `requiredSymbols` row of every packet
(`:801`: `if (!source.includes(row.symbol)) addError(…is missing from…)`), and its §379.2 discharge
has an explicit fence, quoted from the source: *"**ONLY LANDED discharges. A DRAFT or READY
retirement is a promise, and a promise may not silence a live guard.**"*

⇒ **A `retiredSymbols` row in THIS packet does not unblock the build.** It discharges only once
EM-B1f is itself flipped to `LANDED`. Between the edit and the flip,
`node scripts/implementation-packets.mjs validate` — **which is in this packet's own `checks`** —
reds with `EM-B1k2.requiredSymbols[n].symbol is missing from tests/domain/roadsParticipation.test.js:
const UNDISPOSITIONED_CEILING = 7`, so **the packet cannot pass its own seal.** That is EM-B1d's
build STOP in a new costume, caught here instead of there.

⚠ **AND THE TRAPPED ROW BREAKS A STANDING LAW.** `implementation-packets.mjs:94` records it
verbatim: *"the estate already carries the general law (**"never put a re-recorded FIGURE in
requiredSymbols"**, ODQ TE-26)"*. The validator machine-enforces that law only for migration
filenames and migration head figures; a **ratchet figure** slips through, and this is its first
bite. A row written to prove a ceiling never RISES has made the only permitted direction — the
BURN the charter itself ordered in the same breath (addendum 61 item 5) — impossible.

**THREE ROUTES, and the recommendation is the second (Q1).**

| route | what it is | verdict |
|---|---|---|
| **A** | EM-B1f carries `retiredSymbols: [{ path, symbol: 'const UNDISPOSITIONED_CEILING = 7' }]` and nothing else changes | ⛔ **INSUFFICIENT ALONE** — correct bookkeeping, discharges permanently at the flip, but leaves `validate` red for the whole build. Carried anyway, in the manifest. |
| ⭐ **B** | **The chair re-spells EM-B1k2's landed row as the figure-free `const UNDISPOSITIONED_CEILING`**, in the sitting that promotes this packet | ⭐ **RECOMMENDED.** Smallest edit; the guard stays LIVE (the constant must still exist); it cures the TE-26 breach rather than routing around it; and no `retiredBy` is needed, so nothing is silenced. `retirementOf` (`:205`) would also accept `"retiredBy": "§934.47"` on that row (`RETIRED_BY_REFERENCE = /§\d+/`, `:188`) — but that DISCHARGES the guard instead of fixing it, so it is the worse of the two spellings. |
| **C** | Bank the disposition, leave the ceiling at `7` | **FALLBACK ONLY.** Lawful (six rows under a ceiling of seven), but it disobeys addendum 61's explicit *"burns the ceiling 7 → 6"* and leaves the ceiling padded above its population — which the block's own comment forbids in spirit: *"You may burn it; you may never pad it."* |

⚠ **Route B is surgery on a LANDED packet's manifest row and is therefore outside a lane's
authority.** It is put to the chair as **Q1** and must be done **before dispatch**, not during the
build. ⓘ EM-B1k2's three `_note` addresses for this file (`:409`, `:422`, `:425`) are already
stale at `141a1d775` — the live addresses are `:480`, `:493` and `:511` — which is a second, softer
instance of the same habit; the rows themselves are symbol-pinned and unaffected.

### §7.2 · The registration ledger — measured negatives live HERE, never in the table

| # | Obligation | Verdict | Measurement |
|---|---|---|---|
| P2.1 | lighting census | **OWED — titles only** | ⭐ RE-EXECUTED through the walker's own `parkReasonsFor` at `141a1d775`: **all FIVE homes return `[]` (CREDITED)** — `roadsState` · `warSeatBooks` · `roadsParticipation` · `statusUnionTotality.walker` · `npcs.property`. The walker's own `TEST_FILES` length is **2653**, matching the frozen register's `files` figure exactly, so the classifier was run against the live estate. ⛔ NO ABSOLUTE QUOTED. **DELTA: `+0 files / +0 parked / +0 credited / +4 titles / +0 suiteTitles`** — the four are A1 (warSeatBooks ×1), A2 (roadsState ×1) and A4/A5/A7 (roadsParticipation ×2). ⛔ **The walker edit, the census banking and the property widening all land inside EXISTING arms and are worth ZERO titles** — `TEST_WORDS = ['it','test']` (`:781`), so a new `test` in the walker would have cost one. The chair stamps the absolute against the frozen `2653·383·2270·25052·6680`. |
| P2.2 | mutation-coverage row | **NOT OWED** | The packet CREATEs nothing under `ENFORCER_DIRS`; the walker's row is EM-B1d's and already landed. ⚠ `scripts/mutation-coverage-manifest.json` is RESERVED by EM-P2 (READY) — a later revision that owed a row would have to wait for its flip. |
| P2.3 | observed-shape exemption | **NOT OWED** | No save-time key read. ⚠ the scanner is in `checks`; **motion is a STOP.** |
| P2.4 | writer-reach | ⚠ **MEASURE, DO NOT ASSUME** | Baseline captured at step 1; a shrink is `--write`, **growth is a mint and a CHAIR act**. |
| P2.5 · P2.7 | fork / prose-numerics | **NOT OWED** | No draw; no figure. `tests/lint/.prose-numerics-baseline.json` does **not** name `src/domain/roads/state.js` (`grep -c` → **0**, re-executed at `141a1d775`), so the line-addressed-baseline class (pre-proof step 13) cannot fire on this edit. |
| P2.10 | edge-shared freshness | ⛔ **OWED — §3.3** | Seven paths, by generator AND by precedent, both re-executed at `141a1d775`. |
| P2.11 | byte budgets | **PRICED — §3.2** | +184 B into the lazy engine; a DELTA against TOOL-3's 4 KB per-train headroom for `advanceInterval.worker`; zero into the generation worker, the three other workers and first paint. ⚠ **§P2 row 11's second amendment binds the receipt:** a `skipIf(!requireDistRead)` byte arm that SKIPS is not a pass — §12's receipt quotes the executed figure and the dist it read, or says the arm skipped. |
| **step 13** | line-addressed registers | **MEASURED NEGATIVE — not a row** | `tests/lint/.tuning-inventory.json:967` carries `"src/domain/roads/state.js#ROADS_TUNING": { "exported": true, "idiom": "literal", "keys": 76, "line": 222, … }` — ⭐ **`:222` re-confirmed as the live address of `export const ROADS_TUNING`** — and this edit sits ABOVE it, so the LINE goes stale. But `tuningRegister.walker.test.js:406` keys on **`spanDigest`** (`if (was.spanDigest !== now.spanDigest)`) and `:428` explicitly pins *"a comment inserted above the table must leave the digest where it was"*. No manifest row; §12.1 records it. ⭐ And the register's other two touches are inert by construction: `TABLE_DEF_RE` only matches `*_TUNING`, `NAMED_NUMERIC_CONST_RE` only `= <number>;`, `BARE_DECIMAL_RE` only `\d+\.\d+` — `OFF_STAGE_STATUSES` matches none. |
| **step 15** | stamped files | **MEASURED NEGATIVE — not a row** | `docs/content/wiring-census.json` `stamp.files` has **7** entries, all under `src/domain/display/stateProse/` (`general · power · economy · defense · stressors · warFaith · dossierMounts`), and **does not name this file**. No re-take. |
| **step 16** | citations by line | **MEASURED NEGATIVE — not a row** | `git grep -nE "src/domain/roads/state\.js:[0-9]+"` over `src docs tests` finds ⛔ **TEN hits (v3 said nine), ALL in `docs/**` prose** — `DESIGN_FP_ARCHITECTURE.md:335`, `DESIGN_FP_ARCH_GR.md:145`, `DESIGN_FP_ARCH_SP.md:144`, `DESIGN_FP_ARCH_WF.md:173` and `:770`, `SETTLEMENT_CAPABILITY_ATLAS.md:7374`, `ES-5B.md:97` and `:366`, `ES-5C.md:435`, `WF-SUBSTRATE.md:344`. **ZERO hits under `src/` or `tests/`.** ⛔ **None is read live by a walker** — the census's live `cite:` reads are `src/domain/prose/holderTable.js`'s, and `state.js` carries **0** `cite:` tokens (`grep -c` → 0). Stale after this edit: the six `:319` `riskToleranceOf` addresses (live address `:320`, already off by one BEFORE this packet) and the Atlas's `:145-149` `isOffStage`. `:122` `WHEREABOUTS_STATES` (live address `:123`) sits ABOVE the insertion and does not move. §12.1 records it; the fate is CLOSED to FIX-C2's docs-live baseline (addendum 46). |
| **step 12** | `tests/` sweep for changed symbols and literals | ⭐ **ONE REAL HIT — now a manifest row** | The sweep that EM-B1d's build STOP demanded, run over every word this packet touches. `OFF_STAGE_STATUSES` and `isOffStage`: no exact pin outside the declared homes. `NPC_UNAVAILABLE_STATUSES`: exact-pinned in the walker's A1 (`:328`) and consumed in `envoyCasting.js` — **not widened by this packet, so neither moves**. `ROSTER_ABSENT_STATUSES`: exact-pinned BOTH directions in `tests/generators/densityLaw.test.js:1005` and `:1028` — **untouched, and in `checks` so a drift reds**. ⛔ **The hit:** `tests/property/npcs.property.test.js:40` asserts the status is one of FIVE, omitting `jailed` and `removed`, over an arbitrary that never sets one while `createNpc` **does** forward it. That is the set-meaning question EM-B1d's packet failed to ask, one level down — a named set whose consumer disagrees with it. It is a `TEST` row in §7. |
| **step 14b** | title pricing on EXISTING files | **PRICED, not assumed** | `parkReasonsFor` run over every home BEFORE pricing (P2.1). No new test file is created or renamed, so the 2026-09-20 addendum's *"a CREATE or RENAME opts into every walker governing `tests/<dir>`"* does not fire — but `tests/lint` whole is still an instrument step (§10). |

---

## 8. Ordered coding sequence

0. Dispatch and seal; stop on any preflight mismatch. ⓘ At `141a1d775` all three dependencies are
   `LANDED` and the register's one non-terminal packet (EM-P2) shares nothing — but re-check
   before dispatch, because the branch moves under a lane (§4, §7.1).
1. **Capture the baselines before the first edit:** `check-writer-reach`,
   `check-observed-shape-readers`, `state.js`'s effective-line reading under eslint's own `Linter`
   (expect **288**), the engine chunk's size from a real build through the exclusive mutex, and
   the kit's per-module attribution.
2. **Reproduce and add the failing tests.** ⭐ Run A1's fixture FIRST and observe
   `securityBand:'holding'`, `seatWeight01:0.3744`, `rulerId:'a:ruler'` for a jailed ruler — this
   lane executed it at `19c4cb853` (evidence §6). If it does not reproduce, **STOP**.
   ⭐ **Then run §13.0's five cells with the arm OFF** and keep the table: it is the control half
   of step 5a's identity claim.
3. **Implement the arm** — §6 exactly, in `src/domain/roads/state.js`, and nothing else.
   Re-measure effective lines (expect **293**, a delta of +5).
4. **The walker, in FOUR edits and in this order** (§7): `FLAGGABLE_SPELLINGS` + the widened
   `FLAGGABLE_ROWS`; the `derived` branch in `offencesOf`; the two planted probes inside the
   existing A2/A4 `test`; then **APPEND the ninth row** with `spelling: 'derived'` and its reasoned
   omission. Confirm green; **delete the row locally and confirm A3's set-equality REDS naming
   `src/domain/roads/state.js`**; restore. ⛔ Confirm `TRIGGER` still reads
   `['dead','exiled','retired']` — if it moved, STOP (a homonym lost its foreign claim).
4a. **Bank the census row** (§7.3): move `npcVerdictPulse.js` into `EXPECTED` with its written
   disposition, burn `UNDISPOSITIONED_CEILING` to **6**, and confirm BOTH census arms green — the
   exactness arm and the quarantine-honesty arm.
4b. **Widen the property arm** (`tests/property/npcs.property.test.js`), two lines, no new title.
5. **Run focused verification** (§10) including `tests/lint` WHOLE once, then the byte step: a real
   build through the mutex, per-module attribution showing **only `src/domain/roads/state.js`
   moved**, growth ≤370 B, the engine chunk `< 679_000`. ⚠ If the dist-read arm SKIPPED, say so in
   the receipt — a skipped byte arm is not a pass.
5a. ⭐ **Re-run §13.0's five cells with the arm ON** and assert the table is identical to step 2's
   control, row for row, with the liveness anchor printed in both. A difference is a **STOP**.
6. ⛔ **LAST: `npm run build:edge-shared`**, then confirm exactly SEVEN `_shared` paths moved and
   record both `sourceHash` transitions. It re-stamps declared paths, so nothing may follow it.
7. Write the completion receipt.

```text
1. isInStasis(npc)                                            ⇒ off-stage
2. o := (npc is a non-null object) ? npc : null
3. /** @type {readonly string[]} */ (OFF_STAGE_STATUSES).includes(String(o?.status||'').toLowerCase())  ⇒ off-stage
4. w := o ? o.whereabouts : null ; w?.state === 'hostage'      ⇒ off-stage
5. otherwise                                                   ⇒ on-stage
```

---

## 9. Acceptance matrix

| ID | Case | Required observation | Test home |
|---|---|---|---|
| **A1** | ⭐ **THE SEAT LAW, EXECUTED (design §15).** | A jailed ruler through `readWarSeatBooks` → `interestKind:'realm'`, `settlementWeight01:1`, `seatWeight01:0`, `securityBand:'unseated'`, `rulerId` undefined — identical to the landed `dead` case. ⛔ **Anchored:** an `active` ruler in the same arm reads `holding`, `0.3744`, `a:ruler`. Both halves already executed at `19c4cb853` (evidence §6). | `tests/domain/warSeatBooks.test.js` |
| **A2** | **THE ARM, EXACT AND DERIVED.** | `OFF_STAGE_STATUSES` equals `['exiled','jailed','removed']` and `Object.isFrozen` is true — so a widening of `NPC_UNAVAILABLE_STATUSES` reds here rather than changing behaviour silently. `isOffStage` TRUE for the three and for `'JAILED'`; ⛔ **FALSE for `active`, `dead`, `missing`, `retired`, `'sabbatical'`, `''`, a non-string (`0`), and a record with NO `status` key.** The `missing`/`retired` rows are what pin Q1. ⭐ **ALL SEVENTEEN CELLS ALREADY EXECUTED at `141a1d775` against §6's exact post-edit source** (evidence §14) — the implementer is transcribing a measurement. ⭐ **AND ITS SECOND HALF, in `tests/property/npcs.property.test.js`:** `createNpc`'s shape arm admits the full SEVEN-member union over an arbitrary that actually draws one, so a status the caller can now supply is no longer silently outside the pin. | `tests/domain/roadsState.test.js` · `tests/property/npcs.property.test.js` |
| **A3** | **REGRESSION ANCHOR** — travel is still narrative. | The four existing `it`s read as before. **No new title.** | `tests/domain/roadsState.test.js` |
| **A4** | ⛔ **ABSENCE FROM PARTICIPATION IS NOT ABSENCE FROM THE RECORD.** | `buildWorldSnapshot` excludes a jailed NPC from the participation view **and** `save.settlement.npcs` still contains them by id. | `tests/domain/roadsParticipation.test.js` |
| **A5** | **DORMANCY** — zero allocation. | The existing `plain` fixture still yields the **same settlement reference**. **No new title**; `tests/property/roadsDormancyGolden.test.js` is the corpus-wide half, run via `checks`. | `tests/domain/roadsParticipation.test.js` |
| **A6** | ⛔ **NOTHING MOVES.** | Both golden suites bytewise unchanged. Proved, not argued: **0 of 5,171 corpus NPCs across all 525 rows** carry any of the three (executed at `19c4cb853`), and no `src/` writer can assign one to an NPC. Run via `checks`; no new title. | `tests/property/…` |
| **A7** | ⭐ **THE WALKER'S NINTH ROW AND THE `derived` KIND, BOTH GUARDED.** | ⑴ The walker is green with the appended `derived` row and the machinery — **EXECUTED at `141a1d775` through the walker's own `FLAGGED`, `DECLARED_FLAGGABLE`, `TRIGGER` and `offencesOf`** (evidence §17). ⑵ **Deleting the row REDS by name**: post-edit `state.js` is FLAGGED (it spells `'dead'` at the `filter`) and drops out of `DECLARED_FLAGGABLE`, so A3's set-equality convicts `src/domain/roads/state.js` — executed, with and without the machinery. ⑶ **The `derived` kind is REAL WORK in both directions**, proved on planted sources inside the existing A2/A4 arm: an honest FORM B shape passes; a source that does not read `NPC_UNAVAILABLE_STATUSES` is convicted by name; a source spelling a retired foreign word is convicted with its `file:line`. ⑷ **`TRIGGER` is unmoved at `['dead','exiled','retired']`** although `state.js` joins `ROSTER_FILES` — measured, because none of its four SCREAMING_SNAKE vocabularies spells a union member, so no homonym loses its foreign claim. | `tests/domain/roadsParticipation.test.js` drives it; the walker's own suite proves the red |

Seven of ≤8. ⚠ **The property-arm observation is folded into A2 rather than minting an eighth
case** — see §3 and Q2; the chair may split it out if the matrix should read that way.

---

## 10. Verification commands

```sh
npx eslint src/domain/roads/state.js tests/domain/roadsState.test.js \
  tests/domain/warSeatBooks.test.js tests/domain/roadsParticipation.test.js \
  tests/property/npcs.property.test.js tests/lint/statusUnionTotality.walker.test.js
npm run typecheck:ratchet && npm run typecheck:domain:strict

npx vitest run --pool=threads --maxWorkers=2 \
  tests/domain/roadsState.test.js tests/domain/roadsParticipation.test.js \
  tests/domain/warSeatBooks.test.js tests/domain/warSeatTermination.test.js
npx vitest run --pool=threads --maxWorkers=2 tests/lint/statusUnionTotality.walker.test.js
# ⭐ NEW IN v4 — the census's own landed contract, and the property file this packet now edits
npx vitest run --pool=threads --maxWorkers=2 \
  tests/domain/irreversibleRawRoster.contract.test.js tests/property/npcs.property.test.js
# ⭐ INSTRUMENT (not a sealed check while the lighting walker reds by design — addendum 61 item 7):
#    tests/lint WHOLE, once, and the ONLY permitted red is the lighting census.
npx vitest run --pool=threads --maxWorkers=2 tests/lint
# ⛔ GOLDEN POSTURE — must not move (roadsDormancyGolden copies EM-B1k's own landed row)
npx vitest run --pool=threads --maxWorkers=2 \
  tests/property/generatorGoldenMaster.test.js tests/property/dossierProseManifest.test.js \
  tests/property/roadsDormancyGolden.test.js
# ⛔ THE R18 FAMILY — the density lane's own pins, run because this packet feeds it
npx vitest run --pool=threads --maxWorkers=2 \
  tests/generators/densityLaw.test.js tests/domain/advanceWorkerByteIdentity.test.js

node scripts/check-observed-shape-readers.mjs      # motion is a STOP
node scripts/check-writer-reach.mjs                # shrink = --write; growth = a chair act
node scripts/implementation-packets.mjs validate

# ⛔ ABSOLUTELY LAST — it re-stamps declared paths; nothing may follow it
npm run build:edge-shared
```

---

## 11. Mandatory STOP conditions

1. ⛔ Any non-terminal packet holds any of the thirteen change paths at the dispatch tip —
   `validate` refuses (§4, §7.1). ⚠ Watch `scripts/mutation-coverage-manifest.json`'s owner EM-P2.
2. ⛔ The defect does not reproduce at step 2 (a jailed ruler already reads `unseated`).
3. ⛔ `NPC_UNAVAILABLE_STATUSES` is not `['dead','exiled','jailed','removed']` at the base.
4. ⛔ Either golden suite moves, or `roadsDormancyGolden` or `tests/generators/densityLaw.test.js`
   reds.
5. ⛔ `check-writer-reach` or `check-observed-shape-readers` **grows**.
6. ⛔ Engine growth exceeds 370 B, any module other than `src/domain/roads/state.js` moved, the
   engine lands ≥ `679_000`, or any worker ceiling reds.
7. ⛔ The walker demands anything beyond the four edits §7 names, or the derived `TRIGGER` is no
   longer `['dead','exiled','retired']`, or `FLAGGED` gains a file other than
   `src/domain/roads/state.js`.
8. ⛔ Any consumer needs an edit — the whole premise is inheritance through one chokepoint.
9. ⛔ Fewer or more than SEVEN `_shared` paths move.
10. ⛔ The delta in `state.js` exceeds 6 effective lines (measured +5), or a file outside the
    manifest must change.
11. ⛔ **§13's arm-on and arm-off tables are not identical at the build's own tip** — re-run
    §13's cells before the first edit. ⛔ **The v3 probes in the compile scratch are NOT the
    instrument any more**: they drive in-memory overrides of a kernel that CURE-F has since
    re-spelled. §13's cells run against the real tree with the arm applied to a copy.
12. ⛔ **`pulseKernel.js:184`/`:579` or `factionDensityKernel.js:719` have moved since this
    packet's verified base** — §13's premise is those three lines, and they are `requiredSymbols`
    rows precisely so the dispatch refuses rather than the lane discovering it.
13. ⛔ **The census banking does not shrink**: `UNDISPOSITIONED_NPCS_READERS` must end at six rows
    with `UNDISPOSITIONED_CEILING = 6`. A ceiling that rises, or a row moved without a written
    disposition, is a STOP — never pad it to go green.
14. ⛔⛔ **EM-B1k2's landed `requiredSymbols` row still reads `const UNDISPOSITIONED_CEILING = 7`
    at dispatch.** §7.4's chair act (route B) is a **PRECONDITION OF DISPATCH**, not a build step:
    without it `validate` — a sealed check of this packet — reds from the first edit of the
    ceiling until the chair flips EM-B1f, and the packet cannot pass its own seal. The build lane
    verifies the row's text BEFORE step 4a and STOPS if it still carries the figure.

---

## 12. Completion receipt

Record, with executed output: the step-2 reproduction both ways; the seven consumer sites
re-confirmed **unedited** by diff; both golden suites, the roads dormancy golden and the density
suite; the seven `_shared` paths and both `sourceHash` transitions; writer-reach and observed-shape
before/after; the byte step (engine before/after, per-module attribution, worker readings) — ⚠ **if
a `skipIf(!requireDistRead)` arm SKIPPED, say so; a skipped byte arm is not a pass** (§P2 row 11's
second amendment); the lighting DELTA actually caused (`+4 titles` predicted) **as a delta**; the
walker's red when the ninth row is removed **and the `derived` kind's two planted convictions**;
the effective-line reading for `state.js` before and after under eslint's own `Linter` (288 → 293
predicted); ⭐ **the census banking — the quarantine at six rows, `UNDISPOSITIONED_CEILING = 6`,
and both census arms green**; ⭐ **and §13.0's five cells re-executed at the built tip, arm ON and
arm OFF, with the liveness anchor printed in both runs.**

### §12.1 · NOTICED, NOT TOUCHED — each specific enough to slot

1. ✓ **CLOSED BY THE CHAIR** (addendum 46, Q1): the charter's ninth-row omission text was
   corrected to `ALL_BUT_DEAD` in the ledger. Recorded so it is not re-found.
2. ✓ **CLOSED** (addendum 46, Q3/Q4 + `bf0db7a7e`): the two-way placement question on
   `tests/domain/roadsParticipation.test.js` is discharged — EM-B1k2 landed first.
3. ✓ **CLOSED, owned elsewhere and now LANDED:** the participation ratchet's roots are
   `src/domain/worldPulse`, `src/domain/spatial`, `src/domain/density` and
   `src/generators/density`, with a `factionRosterOf` reader token (EM-B1k2, `e96a1c33e`). 41 → 44
   readers. Not this packet's.
4. ⛔ **WIDER THAN v3 RECORDED — `status: 'remnant'` is written in THREE `src/` files, not one,
   and `'remnant'` is NOT a member of the `EntityStatus` typedef** (`entities/status.js:23` =
   `active|impaired|removed|destroyed|vacant`), which `statusUnionTotality.walker.test.js` A1 pins
   at exactly five. Measured at `141a1d775`: `tierOutcomeApply.js:142,143,144,145,146,147,148`
   (six of `demotionFateForInstitution`'s seven branches), **`institutionLifecycle.js:1016` and
   `:1058`**, **`magicRegimeLifecycle.js:354`** — nine write sites across three files, plus a
   documenting comment at `institutionStatusModel.js:22`. Slot: **EM-B1j's compile** (addendum
   46's fate) — and the measurement it must start from is nine sites in three files, not six in
   one; if `'remnant'` is a separate vocabulary, **EM-B1l** is chartered from it.
5. ⚠ **`tests/lint/.tuning-inventory.json:967`'s `line: 222` for `ROADS_TUNING` goes stale** with
   this edit. Not asserted (`tuningRegister.walker.test.js:406` keys on `spanDigest`; `:428` pins
   that a comment above a table must not move it), so it re-takes with that file's next
   regeneration. No action owed.
6. ✓ **CLOSED, owned elsewhere — but the COUNT was wrong.** **TEN** `docs/**` prose citations
   address `src/domain/roads/state.js` by line, not nine (§7.2 step 16 lists all ten). Six of them
   spell `:319` for `riskToleranceOf`, whose live address is **`:320`** — ⚠ **they are already
   stale by one BEFORE this packet touches anything.** Fate: **FIX-C2's docs-live baseline carries
   them** (addendum 46). Recorded with the corrected count so FIX-C2 does not inherit "nine".
7. ✓ **CLOSED and LANDED:** `src/generators/density/titularSuccession.js`'s dark
   `factionRosterOf` reads carry EM-B1k2's written disposition in the census. Not re-slotted.
8. ⭐ **PROMOTED TO A MANIFEST ROW** (addendum 46's fate, and it is worse than v3 thought):
   `tests/property/npcs.property.test.js:40` asserts five statuses, and **`createNpc` DOES forward
   a caller's status** (`entities/npcs.js:160`), so the arm is not merely vacuous — it is a pin
   that would fail to catch a real `jailed` or `removed` NPC the day one is constructible. §7
   carries the two-line cure.
9. ⛔ **A TOOLING HAZARD THAT PRODUCED A SILENT FALSE NEGATIVE IN THIS VERY LANE.** `git grep -E`
   does **not** honour PCRE `\s`: `git grep -nE "status:\s*'removed'" -- src` returns **zero
   hits** and exit 1, while `git grep -nE "status:[[:space:]]*'removed'" -- src` returns the two
   real writers. A census built on the first form would have "refuted" a true claim with a clean
   green. Same family as EM-B1k2's BSD-vs-GNU `-e`-flags ruling. Slot: **a line in the compile
   brief's measurement rules** — POSIX classes in `git grep -E`, never `\s`/`\d`/`\w`; or `-P`.
10. ⚠ **`UNDISPOSITIONED_NPCS_READERS` still holds six rows after this packet's banking**
    (`envoyCasting · oathHolder · sovereigntyNews · warDeployment · warRulingsNews ·
    warSeatBooks`). ⭐ **Three of them — `envoyCasting.js`, `warSeatBooks.js` and
    `sovereigntyNews.js` — are files this packet's own §5 contract reads or names**, so the next
    packet in this family is unusually well placed to bank more. Slot: **EM-B1a's compile** (it
    edits the verdict/status surface and will be in `warSeatBooks.js`'s neighbourhood) —
    recommend it disposition `warSeatBooks.js` and `envoyCasting.js` and burn the ceiling again.
11. ⚠ **`tests/lint` whole is ~2 minutes and catches reds the sealed `checks` cannot**, yet cannot
    join `checks` while the lighting walker reds by design. §10 carries it as an INSTRUMENT step.
    ✓ Already slotted: **the third preamble/standard amendment, owed at this packet's placement
    sitting** (addendum 61 item 7) — a `tests/lint` step that excludes the lighting walker, or
    reads TOOL-9's print mode, joins the sealed `checks`.

---

## 13. ⭐⭐ THE PROMOTION MEASUREMENT — RE-EXECUTED AT `141a1d775` AGAINST BOTH LANDED CURES

> **RE-CUT AT THE FLIP (the chair, 2026-09-20; ODQ §934.47 addendum 84):** the sealed build re-executed this table against the real tree at `c127cdfb2` and REFUTED its base claim for `exiled`/`removed`: the house IS swept from `powerStructure.factions` (`[The Crown]`) and a `faction_dissolved` beat DOES fire — seed-stable, the premise files unmoved. It is ARM-INDEPENDENT and the person is kept in every cell, so it does not touch the promotion verdict (both arms identical row for row; the liveness anchor `false`/`true`); v3's 'dissolved, person kept' is what reproduces. The prose above stands as the pre-proof wrote it, with this correction beside it — a landed packet's record is not rewritten, it is annotated.

Version 2's block was: *the participation filter is the base the tick's writes are built from, so
adding `jailed` to it would erase a person from the save and dissolve their house.* Version 3
lifted the block by MODELLING the cure. **Version 4 does not model anything: both cures are in
the tree.**

- **EM-B1k (`19c4cb853`) made the WRITE base raw.** `pulseKernel.js:184`
  (`buildSettlementMap` prefers `item.save?.settlement`) and `:579` (the `localSettlements` seed
  restores the raw roster). ⚠ **`:579` was re-spelled by CURE-F (`96036427f`) after v3's probes
  ran** — it now compares against `item.settlement?.npcs`, the roster the tick was HANDED, rather
  than the clock chain's return. That is a correction in the same direction, and it is why the
  line is now a `requiredSymbols` row.
- ⭐ **EM-B1k2 (`e96a1c33e`) made the IRREVERSIBLE READ raw BY CONSTRUCTION.**
  `factionDensityKernel.js:719`'s `tickStart` is
  `asObject(asObject(item.save).settlement || item.settlement)`, so the §810.4 R18 law is no
  longer computed from a projection and rescued by a downstream `stillEmpty` guard — the habitat
  is gone, not merely fenced. `tests/domain/irreversibleRawRoster.contract.test.js` (386 lines,
  five arms, every arm driving a SHIPPED entry) is the standing guard, and
  `roadsParticipation.test.js`'s widened census now dispositions `factionLifecycle.js` in writing
  as *"PARTICIPATION-INDEPENDENT BY CONSTRUCTION AND REQUIRED TO BE"*.

⇒ **The packet's standing question — "does an IRREVERSIBLE consumer read the FILTERED
participation view?" — is ANSWERED BY THE TREE.** Both production callers of `factionRosterOf`
hand it a raw roster; the census asserts it; the contract suite proves it red-first.

**How version 4's cells were driven, stated so they can be refused.** No loader and no override.
An overlay root symlinks the read tip module-for-module with ONE real file substituted —
`src/domain/roads/state.js`, holding §6's exact post-edit text, built from the live file by a
transform whose two anchors (the `:36` import line and the whole `isOffStage` body) **throw if
they stop matching** — and Node runs it under `--preserve-symlinks` so the overlay is what every
importer resolves. The control run is the same driver against the unmodified tree. **The liveness
anchor is printed by both runs**: `isOffStage({status:'jailed'})` reads `true` in the arm run and
`false` in the control, so the identity below is a measurement and not a vacuous pass.

### §13.0 · THE CELLS, at `141a1d775`, through `simulateCampaignWorldPulse({ commit: true })`

Ashford: two houses, one member each, the Weaver's status varied; one committed tick.

| sole Weaver | arm | houses after the tick | roster | lifecycle reader | `faction_dissolved` beat |
|---|---|---|---|---|---|
| `active` (CONTROL) | off / **on** | `[Crown, Weavers]` / **identical** | 2 of 2 / **2 of 2** | crewed / **crewed** | no / **no** |
| **`jailed`** | off / ⭐ **on** | `[Crown, Weavers]` / ⭐ **identical** | 2 of 2 / ⭐ **2 of 2** | crewed / ⭐ **crewed** | no / **no** |
| `exiled` | off / **on** | `[Crown, Weavers]` / **identical** | 2 of 2 / **2 of 2** | dissolved / **dissolved** | no / **no** |
| `removed` | off / **on** | `[Crown, Weavers]` / **identical** | 2 of 2 / **2 of 2** | dissolved / **dissolved** | no / **no** |
| shelved (`stasis`) | off / **on** | `[Crown, Weavers]` / **identical** | 2 of 2 / **2 of 2** | crewed / **crewed** | no / **no** |

⭐⭐ **THE ARM-ON AND ARM-OFF TABLES ARE IDENTICAL, ROW FOR ROW.** Nobody is erased from the roster
in any cell (2 of 2 everywhere), **no house leaves `powerStructure.factions` in any cell** *(REFUTED AT THE BUILD — see the re-cut below: for `exiled`/`removed` the house IS swept)*, and no
dissolution beat fires in any cell.

⛔ **And the `exiled`/`removed` `dissolved` reading is NOT this packet's and NOT a sweep.** It is
`factionLifecycleStateOf`'s verdict over `ROSTER_ABSENT_STATUSES = ['dead','exiled','removed']`
(`factionLifecycle.js:87`, read by `isOnRoster` `:96-99`) — a **pre-existing** R18 reading that
fires identically with the arm absent, leaves the house in the settlement, and keeps the person in
the record. `jailed` is the reversible one, and it changes nothing at all.

⚠ **THIS IS A STRONGER RESULT THAN v3's §13.2 REPORTED**, and the improvement is EM-B1k2's, not
this packet's: at v3's modelled tree the `exiled`/`removed` rows read *"dissolved, person kept"*;
at `141a1d775` the house is not swept from `powerStructure.factions` at all and only the lifecycle
READER reports `dissolved`. v3's table is superseded by the rows above.

### §13.1 · ⓘ VERSION 3's TABLES, KEPT AS HISTORY — the pre-cure cells are what they are for

⛔ **These two tables were measured at a MODELLED tree (`32602dc60` + an in-memory `19c4cb853`
kernel) and are superseded by §13.0 for every "landed" claim.** They are kept because their
PRE-CURE cells are the only executed record of the harm, and cell 7 against cell 4 is still the
sentence that justifies the whole ordering: *the same arm, the same fixture, the same tick —
erasure before EM-B1k, no erasure after.*

#### §13.1a · The DM's own reproduction (v3) — a generated seven-person town, the shipped shelve writer, one tick with `commit: true`, the real store commit

| cell | kernel | arm | subject | store roster | DB roster | `return-npc` | verdict |
|---|---|---|---|---|---|---|---|
| 1 | **pre-cure** | off | DM-shelved | **6 of 7** | **6 of 7** | `npc_target_missing` | ⛔ **ERASED** (v1's finding, reproduced) |
| 7 | **pre-cure** | ⭐ **ON** | `status:'jailed'` | **6 of 7** | **6 of 7** | `npc_target_missing` | ⛔ **ERASED** — *exactly the harm version 2 refused to ship* |
| 2 | **landed** | off | DM-shelved | 7 of 7 | 7 of 7 | `{"ok":true}` | ✓ SURVIVES |
| 3 | **landed** | off | `status:'jailed'` | 7 of 7 | 7 of 7 | `npc_not_in_stasis` | ✓ SURVIVES (arm absent ⇒ inert) |
| **4** | **landed** | ⭐ **ON** | `status:'jailed'` | **7 of 7** | **7 of 7** | `npc_not_in_stasis` | ⭐ **SURVIVES** |
| **5** | **landed** | ⭐ **ON** | `status:'exiled'` | **7 of 7** | **7 of 7** | — | ⭐ **SURVIVES** |
| **6** | **landed** | ⭐ **ON** | `status:'removed'` | **7 of 7** | **7 of 7** | — | ⭐ **SURVIVES** |

⭐ **Cell 7 against cell 4 is the whole answer:** the same arm, the same fixture, the same tick —
erasure before EM-B1k, no erasure after. **Adding `jailed`, `exiled` or `removed` to the filter can
no longer erase a person from the save.**

#### §13.2a · The sole-member house (v3) — the R18 reproduction, eight statuses, four cells

At the **landed** kernel, `ARM_MODE=off` and `ARM_MODE=on` produce **byte-identical rows for all
eight statuses**:

| sole Weaver | pre-cure, arm off | pre-cure, ⭐arm on | **landed, arm off** | **landed, ⭐arm on** |
|---|---|---|---|---|
| `active` | stands | stands | stands | stands |
| `stasis` | ⛔ dissolved, person gone | ⛔ dissolved | ✓ stands, person kept | ✓ stands, person kept |
| `hostage` | ⛔ dissolved, person gone | ⛔ dissolved | ✓ stands, person kept | ✓ stands, person kept |
| **`jailed`** | stands | ⛔ **dissolved, person gone** | stands | ⭐ **stands, person kept** |
| `exiled` | dissolved, **person kept** | dissolved, person kept | dissolved, person kept | **dissolved, person kept — UNCHANGED** |
| `removed` | dissolved, person kept | dissolved, person kept | dissolved, person kept | **dissolved, person kept — UNCHANGED** |
| `missing` | stands | stands | stands | stands |
| `retired` | stands | stands | stands | stands |

⭐ **THE ARM CHANGES NOTHING IN THIS CONSEQUENCE SPACE AT THE CURED TREE** — the `landed, arm off`
and `landed, arm on` columns are identical, row for row.

⛔ **And the reason `exiled` and `removed` dissolve at all is NOT this packet.** It is
`ROSTER_ABSENT_STATUSES = Object.freeze(['dead','exiled','removed'])`
(`factionLifecycle.js:87`) read by `isOnRoster` (`:96-99`) — a **pre-existing** R18 reading that
fires identically with the arm absent, and that keeps the person in the record. The charter's
sentence *"every reading in the estate already counts `removed` absent"* is confirmed by
execution. `jailed` is the reversible one, and it now dissolves nothing.

### §13.3 · What this does NOT claim

- ✓ **v3's Q2 residual is CLOSED.** The `fresh`-fallback dependency
  (`factionDensityKernel.js:721-723`) was EM-B1k2's finding; **EM-B1k2 landed at `e96a1c33e`** and
  removed it — its A4 arm pins the fallback door directly. The defence in depth IS in the tree.
- §13.0's cells drive ONE two-house fixture, not the corpus. The corpus-wide claim this packet
  makes is the golden one (§6): across all 525 rows, 5,171 generated NPCs carry only `<ABSENT>`
  (4,884) or `'active'` (287) — **zero** of the arm's three members. That figure is v3's
  measurement, inherited; it is re-proved at the build by the two golden suites in `checks`.
- ⚠ **HONEST LABEL on §13.0:** the five rows are **CONFIRMED** (executed, both arms, with a
  liveness anchor). The claim that they generalise to every world is **PLAUSIBLE**, and it rests
  on the golden posture and on the writer census (§6), not on the fixture.

### §13.4 · The questions that remain — numbered for the chair, not waited on

- ⛔⛔ **Q1 (NEW, AND A PRECONDITION OF DISPATCH) — EM-B1k2's LANDED `requiredSymbols` row traps
  the ceiling burn the charter ordered.** The row pins the verbatim text
  `const UNDISPOSITIONED_CEILING = 7`; only a LANDED retirement discharges such a row; `validate`
  is one of this packet's own sealed `checks`. **Recommendation: route B — the chair re-spells
  EM-B1k2's row as the figure-free `const UNDISPOSITIONED_CEILING`** in the sitting that promotes
  this packet, which also cures the TE-26 breach (*"never put a re-recorded FIGURE in
  requiredSymbols"*, quoted at `implementation-packets.mjs:94`). Full working: §7.4.
- **Q1a — ✓ CLOSED BY THE CHAIR.** Addendum 46 corrected the charter's ninth-row text: only
  `omits: ALL_BUT_DEAD` is lawful, and the packet carries it. No further act is owed.
- ⛔ **Q2 (NEW, and the one that matters) — the `derived` spelling's machinery.** Addendum 46 (Q5)
  ruled the kind into THIS packet. **Measured: the row alone REDS the walker's A3, and the kind
  alone checks nothing.** §7 therefore carries four edits to the walker instead of one, at +12
  effective lines instead of +2. **Recommendation: carry it** — a hollow `derived` value would be
  worse than either alternative, and the machinery is measured green in every direction with A7 as
  its guard. **If the chair prefers the smaller packet**, `spelling: 'literals'` is measured green
  today and the kind becomes a TOOL lane; that reverses a ruling, so it is the chair's.
- **Q3 — ✓ CLOSED.** The placement order was ruled (EM-B1k2 first) and discharged by its landing.
  `tests/domain/roadsParticipation.test.js` is held by no non-terminal packet at `141a1d775`.
- **Q4 (was v3's Q4) — the walker's `omits` reads backwards under FORM B**, listing the words the
  arm ACTS ON because they are derived rather than spelled. The `derived` kind this version adds
  makes that legible but does not change the semantics of `omits`. **Recommendation: leave it** —
  renaming `omits` touches all nine rows and belongs to a TOOL lane, not to the packet that adds
  the ninth.
- **Q5 (NEW) — `tests/property/npcs.property.test.js` is a sixth handwritten file.** Addendum 46
  left the call here. It fits the budget and the defect is real (§7). **Recommendation: keep the
  row**, with its observation folded into A2 rather than minting an eighth acceptance case.
