# Settlement editor / wave 1 — EM-B3c: the gallery scanner's client-mirror totality — a walker that holds the NET-CURRENT server scanner to every key the client hard-denies, landing red on the three it misses, and the migration that closes them

- **Status:** `READY`
- **Packet version:** `2`
- **Verified base:** `fixes-2026-09-18-consist` at `21b991118ce3b4f9d7015783b9bd955547dd4815`
- **Last revalidated:** 2026-09-20 at `21b991118ce3b4f9d7015783b9bd955547dd4815` — RE-PINNED by the chair (session a9df403c) before dispatch, the window re-run in the same command as the stamp: since `21b991118` the branch gained CURE-A/A2/B/C/D, EM-B1k (placed, LANDED `19c4cb853`, flipped), the lighting refreeze, CURE-E/F/G and the chair's docs — over every change-manifest and `requiredSymbols` path `git diff --stat` printed NOTHING except the shared `scripts/mutation-coverage-manifest.json` (`5 0 scripts/mutation-coverage-manifest.json`: CURE-G's row for EM-B1k's renamed suite, inserted elsewhere in the file) — this packet's two insertion anchors (`gallerySanitizer.pglite` → `gallerySeedLeak.pglite`) are still adjacent in that order; both CREATE targets absent; migration number 203 still FREE; all 21 required symbols resolve verbatim. The sealed dispatch's substrate arm reads the window from THIS base. ⛔ APPLYING 201–203 stays the OWNER's hand. The previous stamp, kept as history: 2026-09-19 at `21b991118b7e1eebf76f30d91ea3705b01798644` — stamped by the chair at promotion, the window re-run in the same command as the stamp: since the compile tip `58fcfe614` the branch gained the EM preamble's §P2 row 12, two docs folds, EM-B1e (placed, LANDED) and EM-B1d version 5 (placed, LANDED `95e494bdb`); over every change-manifest and `requiredSymbols` path `git diff --stat` printed NOTHING except the shared `scripts/mutation-coverage-manifest.json` (`4 0 scripts/mutation-coverage-manifest.json`: EM-B1d's landed row, inserted elsewhere in the file) — this packet's two insertion anchors (`gallerySanitizer.pglite` → `gallerySeedLeak.pglite`) are still adjacent in that order; both CREATE targets are absent and untracked; migration number 203 is FREE; all required symbols resolve verbatim. ⛔ APPLYING migrations 201–203 (`supabase db push` + the applied-head bump) is the OWNER's hand; until then production's scanner is the pre-202 definition and the gap this packet closes is OPEN IN PRODUCTION (ODQ §934.55). The compile lane's sentence: *the chair's revalidation sentence, ready to stamp:* "Re-measured at `21b991118` under **J-T1**: `git diff --stat 58fcfe61458b784b0470b854caf916b7c2961edf 21b991118 -- <the twenty-one substrate paths and the eight change paths>` printed NOTHING; both CREATE targets (`tests/security/galleryScannerMirrorTotality.test.js`, `supabase/migrations/203_gallery_scanner_client_mirror_totality.sql`) are absent and untracked and **203 is still the free number**; all twenty-one `requiredSymbols` rows re-resolved verbatim (`grep -cF` → 1 each, E20) and none is refused by rule HK-5's migration-head-figure trap; the net-current gallery scanner is still `202_edit_registry_public_denylist.sql` under BOTH a lexical and a numeric prefix sort, and the subset claim is still RED for EXACTLY `factionPairStates`, `envoyErrands`, `concludedWars` (E13.1 — a fourth name is a STOP); the four budgeted bundle closures and the five edge-shared metas were priced and this packet reaches none of them, so it carries no ceiling TEST row and owes no `build:edge-shared` (E7); the lighting absolute at the promotion tip is `<files · parked · credited · titles · suiteTitles>` and after THIS packet alone (`files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +1`) the walker reads `<…>` — the refreeze is the train's terminal act and the chair's, never this packet's."
- **Depends on:** `EM-B3a` — ✅ LANDED at `668d87512f539ad1c866ec3d9327d6d41f3d8d2f` (the client veil). `EM-B3b` — ✅ LANDED at `ac46d2daffb8e5a43d4dea429fe0202444eb64ae` (migration 202, the SQL mirror, and its rehearsal-train registration — this packet's 203 is built from 202's body and copies B3b's registration shape field for field). Both are ancestors of the compile tip. **That is the whole dependency list.**
- **Collision group:** `scripts/mutation-coverage-manifest.json` — **EM-B1d (READY, closing train EM-T3) is the ONLY non-terminal reserver of any of this packet's eight change paths**, measured over the live manifest (188 packets; histogram `LANDED 185 · SUPERSEDED 2 · READY 1`). `supabase/migrations/**`, `scripts/ops/migrationRehearsalCore.mjs`, `tests/ops/migrationRehearsal.test.js` and the three DOCs are reserved by **nobody** non-terminal (WEB-1/2/3, EM-B3b, DOM-1, DOM-2, IA-1, WEB-8 all LANDED; a terminal status reserves nothing). ⚠ **EM-P1 and EM-P2 are ABSENT from the live manifest** — they reserve nothing today and their reservation is prospective, beginning when the chair re-admits them (charter: EM-P2 v3 at EM-T3.5, EM-P1 at EM-T4). **Under R5's FLOAT this resolves with no re-ordering: dispatch immediately AFTER train EM-T3's terminal and the single contention has ended by construction** (E19). Dispatching *into* EM-T3 alongside EM-B1d would fail the "non-CREATE target must be Git-clean" check on that one path.
- **Commit authority:** `edits only; the chair commits`
- **Baseline posture:** `measured, then re-measured under the chair's rulings.` Version 1's twelve measurements stand (E0–E12) and were re-executed inside the version-2 script (E13.1). Version 2 adds: migration 203 constructed and proved by mechanical diff (E14); the six mutants re-run against the post-203 text (E15); the full census of every site that names 202, classified (E16); the structural replacement for the owner pin, as a finding (E17); the budget for both halves (E18); the collision census over all eight paths (E19); twenty-one `requiredSymbols` rows and the HK-5 trap check (E20). **No gate was run by this lane and none is claimed.**
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: b90a95b7af484137ecf70bd15cde5054edf66b5db0d9f6e90c974d97b7caa5e1 — stamped by the chair at promotion (the preamble gained §P2 row 12 at `e68d913e6`) — verified at this compile base as `1cf5442719f2236320068afb6b4bab2b4ea49f3b08457c04ccf5eaae6a11faf6`, E0)
- ⛔⛔ **THE OWNER'S HAND:** **APPLYING migrations 201–203 (`supabase db push` + the applied-head bump) is the OWNER's hand; until then production's scanner is the pre-202 definition and the gap this packet closes is OPEN IN PRODUCTION.** `supabase/applied-head.json` stays at `200` and this packet does not touch it.

### What version 2 changed, and why

Version 1 was compiled BLOCKED on three questions. The chair has ruled all three and version 2
carries the rulings; nothing in it awaits a further decision.

**R1 — the re-cut is ACCEPTED.** Version 1 measured that the charter's stated direction (*every
client denylist token has a covering SQL alternative*) is already asserted by
`snapshotDenylistDrift.test.js:78`, green for 36 of 36 tokens. The packet's content is the
direction nothing asserts — **every member of the client `WORLD_SNAPSHOT_HARD_DENY` is refused
by the net-current scanner** — plus the reconciliation arm, the numeric-sort arm, and the
`CANNOT-CATCH:` list exactly as measured. Version 2 states the re-cut as settled law in §1
rather than as a question in §12.

**R2 — the gap is CLOSED, not exempted, and BOTH halves ride ONE landing.** Version 1 proposed
an exemption list as one of three options; the chair ruled that extending a denylist to mirror
what the client already refuses strengthens the posture and is within its delegation, that
authoring the migration FILE is the program's established pattern (EM-B3b authored 202), and
that applying it stays the owner's hand. Version 2 therefore gains a **second CREATE row** —
`supabase/migrations/203_gallery_scanner_client_mirror_totality.sql` — and everything a new
scanner migration owes in this estate (§7, thirteen classified sites in E16.2), and **no
exemption list is built**. The red-first proof and its cure are one landing: the walker is
written first and quoted RED on exactly three names, then 203 makes it green.

**R3 — the local copy of the latest-wins walk is ACCEPTED**; consolidation is the chair's
TOOL-4, outside this packet. §2's non-goals say so.

**R4 — the mutation-coverage row is `"rationale"`**, its text citing the executed mutants.

**R5 — the packet is FLOATED with priority.** Every register prediction is a DELTA, the verified
base is `21b991118`, and the collision census is stated per path with the dispatch window named.

Version 2 also records two things version 1 could not know, both measured: **203's effective SQL
cost is 58 lines — identical to 202's** (the three members ride the existing `'politicsLedgers'`
line and the new explanation is all comment), so the two-half packet totals **≤96 of 400
effective production lines and 8 of 12 files — no split is needed** (E18); and **the
`dmLayer`/`decrees` literal lines in the 202-wave arm are NOT subsumed by the subset claim and
must be kept unchanged** (E15.2), which answers the chair's question in R2(c).

---

## 1. Reconciled authority

1. **The chair's rulings R1–R5 of 2026-09-19 17:3x** — the re-cut; the gap closed by migration
   and not by exemption, in one landing; the local extractor copy; the `"rationale"` row; the
   floated placement. These govern every earlier statement of this packet's shape.
2. **The owner's order of 2026-09-19 ~03:1x** — the editor programme continues; the push chain
   (ODQ §934.42) is PAUSED and migrations 201–203 ride the owner's hand.
3. **THE PROMISE** and the product scope (constitutional). Untouched: this packet adds no
   behaviour a player or DM can observe.
4. **`docs/implementation/PACKET_STANDARD.md`** and **`PACKET_TEMPLATE.md`**.
5. **`docs/implementation/preambles/EM-PREAMBLE.md`** — §P2.1, §P2.2, §P2.10, §P2.11, §P3.1,
   §P3.4, §P6, §P7, §P8.
6. **`docs/DESIGN_EDIT_MODE_AND_DECREES.md` §12.4** — two persisted keys, both DM-private; the
   veil precedes the writer.
7. **`docs/implementation/charters/EDIT-MODE-TRAIN.md`** — the EM-B3c row (amendments of
   2026-09-19 16:0x) as amended by R1, and the train plan (amendments of 17:0x).
8. **`docs/implementation/packets/settlement-editor/EM-B3b.md`** (LANDED) — the estate's proven
   shape for a scanner migration: its change manifest, its wave row, its `requiredSymbols`.
9. **The tree, at `58fcfe614`** — and where the tree refutes a document, the tree governs.

### Resolved contradictions

- **"the direction `snapshotDenylistDrift.test.js` does NOT assert"** (charter; EM-B3a §12) →
  **REFUTED AND SUPERSEDED BY R1.** `:78` is `test.each(JS_TOKENS)` over `sqlDenies` at `:64` —
  the chartered direction, asserted, 36/36 green. This packet does not re-assert it (§P6).
- **"a NEW migration re-creating the scanner without the editor's alternatives is invisible"** →
  **REFUTED IN PART.** A hypothetical 203 re-creating 136's body reds three existing assertions
  (E4, E15). What is true is narrower: such a file is invisible to the **sealed dispatch's
  path-based substrate check**, not to the gate.
- **Migration 136's and 202's array comment — *"every CONDITIONAL_LEDGER_KEY but pantheon"*** →
  **FALSE at this tip, and 203 corrects it by naming the rule instead of a snapshot.** Three
  conditional ledgers landed on the client after 136 froze the array: `factionPairStates`
  (2026-07-20), `envoyErrands` (2026-08-03), `concludedWars` (2026-08-31). 202 copied 136's
  array verbatim. `worldSnapshotDenyCensus.test.js` forces a new ledger onto the CLIENT list;
  nothing forced the SQL to follow — until this packet's walker.
- **"the `hard_deny` array and the alternation are interchangeable"** → **FALSE, measured
  (E15.2).** After 203, striking `'dmLayer'` from the array leaves the key refused by
  `.*dm.*`, so the subset claim is correctly green. The 202-wave arm's two literal lines
  therefore assert something strictly stronger and are **kept unchanged**.

The implementer does not read other documents to reinterpret this packet.

---

## 2. Outcome

**Observable result:** a single security walker under `tests/security/` reds whenever the
repository's **net-current** `public._gallery_world_snapshot_is_safe` stops refusing a key the
client hard-denies, or whenever a migration **declares** a re-creation of that scanner in a
shape the latest-wins extractor cannot read — and migration **203** makes that walker green by
mirroring the three members the server has been missing since 2026-07-20.

**Definition of done:** the walker is written first and is **RED at the base for exactly three
names** — `factionPairStates`, `envoyErrands`, `concludedWars` — with that red quoted in the
receipt; migration 203 is added; the walker is **GREEN**; and each of the walker's claims is
proved discriminating by an in-file negative control. The walker guards **the repository's
net-current definition of the scanner, never production's**: it reads committed migration files
and makes no statement about the deployed database.

**In scope:**

1. **The subset claim** (the primary behaviour): every member of `WORLD_SNAPSHOT_HARD_DENY` is
   refused by the net-current scanner — as a `hard_deny` array member under exact,
   case-insensitive equality (Postgres's own `lower(key) = any (select lower(x) …)` semantics,
   `202:93`) **or** by a covering alternation alternative under the drift test's `^alt$` rule.
2. **The reconciliation claim** (the true cure for the 203+ class): every migration carrying a
   line-anchored `create [or replace] function public._gallery_world_snapshot_is_safe` is
   matched by the latest-wins extractor, and the extractor's owner is the **highest-numbered**
   such migration under a NUMERIC prefix sort.
3. **The cure**: migration 203 re-creates the scanner with three members added to `hard_deny`
   and **every other byte of the function body unchanged from 202**, under a contracted
   mechanical diff, and with a header that corrects 136's and 202's stale claim by naming the
   rule rather than a snapshot.
4. **The registrations a new scanner migration owes** in this estate (§7, from E16.2).

**Explicit non-goals:**

- **Re-asserting the drift test's direction** (§P6) — the walker cites it and does not copy it.
- **Building an exemption list.** R2 closed the gap; nothing is exempted.
- **Applying any migration.** `supabase/applied-head.json` is not touched; the push is the
  owner's.
- **Consolidating the three copies of the latest-wins walk** — the chair's TOOL-4 (R3).
- **Replacing the `scanner.owner` pin structurally** — recorded as a finding in §12 and E17;
  re-pointed here as every previous scanner migration has re-pointed it.
- **Editing `snapshotDenylistDrift.test.js`, `sourceContract.js`, the dossier sanitizer
  `_gallery_sanitize_public_json`, or `_gallery_dm_full_json`.**
- Any `src/` change, any surface, any flag, any golden. Record adjacent discoveries in the
  receipt; do not investigate or repair them.

---

## 3. Hard scope budget

| Limit | Packet budget | EM-B3c v2 (measured, E18) |
|---|---:|---:|
| Behavior families | `1` | **1** |
| New persisted record families | `0` | **0** — no table, no column, no row |
| Named state writers | `0` | **0** |
| Feature flags | `0` | **0** |
| User-facing surfaces | `0` | **0** |
| Direct consumers | `<=2` | **0** |
| New logic-bearing production leaves | `<=2` | **0** |
| Existing logic-bearing production files modified | `<=3` | **1** — `scripts/ops/migrationRehearsalCore.mjs` |
| Additional registration-only files | `<=3` | **2** — the 203 migration, the mutation register |
| Handwritten files total | `<=12` | **8** (3 of them DOC, which cost no production lines) |
| New/changed effective production lines | `<=400` | **<=96** — `58` SQL + `<=34` the wave row + `4` the JSON register; `+0` for the head constant (one line in place) |
| Effective lines per new leaf | `<=250` | **n/a** — no production leaf; the walker is ≤190 raw test lines |
| Delta in a shared/hot file | `<=15` | **+4**; no hot file named, and `migrationRehearsalCore.mjs` carries no `scripts/.size-baseline.json` entry (EM-B3b measured the same file) |
| Acceptance cases | `<=8` | **8** |

Overrides approved before dispatch: `NONE`.

⭐ **NO SPLIT IS NEEDED AND NONE IS PROPOSED.** The measured total is 24% of the line budget and
67% of the file budget. 203's SQL costs **exactly what 202's cost — 58 effective lines** — because
the three members ride the existing `'politicsLedgers'` line and the new explanation is `--`
comment. The red-first proof and its cure stay in ONE landing, as ruled.

---

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- EM-B3c
```

Expected: the packet Markdown and capsule are emitted; verified-base ancestry and unchanged
declared substrate are proven before the seal pins exact HEAD; **both CREATE targets are absent
and Git-clean**, every non-CREATE target is Git-clean, and all twenty-one required symbols
resolve.

**Dry read (E10, E19, E20): every check PASSES with the base stamped to the promotion tip and
the packet dispatched AFTER train EM-T3's terminal.** Two conditions, both measured:

1. `scripts/mutation-coverage-manifest.json` must be Git-clean — it is **EM-B1d's change path**,
   and EM-B1d closes EM-T3. Dispatching after that terminal ends the contention by construction.
2. **No `requiredSymbols` row may pin a migration-head FIGURE** in `docs/DEPLOY.md`,
   `ARCHITECTURE.md`, `docs/CURRENT_STATE.md`, `scripts/ops/migrationRehearsalCore.mjs` or
   `docs/ops/MIGRATION_REHEARSAL_RUNBOOK.md` — `scripts/implementation-packets.mjs:775–783`
   (rule HK-5) refuses them outright. This packet's four rows in those paths were run through
   HK-5's own predicate and all four are ACCEPTED (E20).

⚠ **A limit the packet names rather than hides:** the substrate check is **path-based**, so a
future `204_*.sql` remains invisible to `implementation:dispatch` even with EM-B3c landed.
EM-B3c closes that class **at the gate**, which is where a new file first becomes visible.

Any mismatch makes this packet STALE. Stop before coding. Edit only exact-manifest paths.

---

## 5. Verified tree contract

| Role | File | Symbol | Verified fact (E-ref) | Required use |
|---|---|---|---|---|
| **The subject** | `supabase/migrations/202_edit_registry_public_denylist.sql` | `create or replace function public._gallery_world_snapshot_is_safe` | the NET-CURRENT scanner at the base: sixth and last of six definitions, identical owner under a lexical and a numeric sort (E1.1, E1.2, E13.1) | **203's body is built FROM it**; 202 itself is never edited |
| **The array being extended** | `supabase/migrations/202_edit_registry_public_denylist.sql` | `hard_deny constant text[] := array[` | 25 members; invisible to `sqlRegexAlternation`, which reads only `key ~* (…) then` (E2.4, E3.2) | the ONE region 203 changes |
| **The comparison that fixes the spelling** | `supabase/migrations/202_edit_registry_public_denylist.sql` | `if lower(key) = any (select lower(x) from unnest(hard_deny) as t(x)) then` | **both sides lowered at comparison time**; the literal is written in the client's camelCase (E14.1) | 203's three members are spelled `'factionPairStates','envoyErrands','concludedWars'` — the client's spelling, the array's convention |
| **The alternation that must not move** | `supabase/migrations/202_edit_registry_public_denylist.sql` | `if key ~* ('^('` | 34 alternatives (E2.3) | **UNCHANGED in 203**, asserted by the mechanical diff (A8) |
| **State authority (client)** | `src/domain/display/worldSnapshotPublic.js` | `export const WORLD_SNAPSHOT_HARD_DENY` | 28 frozen members; three of them named by no migration (E3.2) | the claim's left side; imported, never re-typed |
| **Reader** | `src/domain/display/worldSnapshotPublic.js` | `export const COVERT_KEY_RE` | 11 tokens through `jsRegexTokens` (E2.3) | A3's control only |
| **Reader** | `src/domain/display/publicSafe.js` | `export const PRIVATE_KEY_RE` | 26 tokens; `\bdm` → `dm` is what covers `dmLayer` (E2.3, E3.1) | A3's control only |
| **Normalizer/extractor** | `tests/helpers/sourceContract.js` | `export function sqlRegexAlternation` | parses ONLY `~* (…) then`; strips `\m \M \y \Y`; THROWS on absence (`:195–212`) | Reuse; no hand-rolled parser |
| **Normalizer/extractor** | `tests/helpers/sourceContract.js` | `export function jsRegexTokens` | THROWS on zero tokens (`:168–179`) | Reuse |
| **Test precedent (the extraction idiom)** | `tests/security/snapshotDenylistDrift.test.js` | `function netCurrentScannerSql` | the file-local latest-wins walk (`:30–45`), already copied once into `migrationRehearsal.test.js:38–56` | **Copy this proof shape** (R3); do not import, do not edit |
| **Test precedent (the direction already held)** | `tests/security/snapshotDenylistDrift.test.js` | *the membership test at `:78`* | every CLIENT REGEX TOKEN ⊆ the SQL alternation; 36/36 green (E2.4, E3.1) | Cited in the header as the claim this walker does NOT duplicate |
| **Test precedent (the two-key pin, KEPT)** | `tests/ops/migrationRehearsal.test.js` | `:383` `hardDenyMembers ⊇ ['dmlayer','decrees']`; `:387–388` `denies('decrees')`, `denies('dmLayer')` | ⭐ **asserts the ARRAY mechanism specifically — strictly stronger than the subset claim, and NOT subsumed** (E15.2) | **KEPT UNCHANGED. No 203 twin.** They keep passing because 203 removes nothing |
| **Registration authority** | `scripts/ops/migrationRehearsalCore.mjs` | `export const MIGRATION_TRAIN_REPO_HEAD` | `202` at `:19` — a **hard-pinned constant**, not derived | `202` → `203`, +0 eff |
| **Registration authority** | `scripts/ops/migrationRehearsalCore.mjs` | `export const MIGRATION_WAVES` | the `edit-registry-public-denylist` row at `:551–584` is the shape a 203 row copies field for field | one row APPENDED; the array declaration untouched |
| **Registration authority** | `scripts/ops/migrationRehearsalCore.mjs` | `export function buildMigrationRehearsalPlan` | derives the plan from the waves; `:746` reds if `repoHead !== MIGRATION_TRAIN_REPO_HEAD` | **not touched** (EM-B3b: "every other function is NOT touched") |
| **Registration authority** | `tests/ops/migrationRehearsal.test.js` | `covers the exact applied-head to repository-head gap in semantic waves` | ten hard-pinned figures inside this one `it` (E16.2 row 3) | figures move INSIDE it; the TITLE is untouched; **no new `describe`, no new `it`** |
| **Derived doc pin** | `tests/docs/deployRunbookFreshness.test.js` | `names the current migration head file` | derives the head from `supabase/migrations/` and reds if `docs/DEPLOY.md` drifts (`:121–127`) | makes the DEPLOY.md edit FORCED |
| **Derived doc pin** | `tests/docs/architectureFreshness.test.js` | `migrations? ahead` | parses CURRENT_STATE's head, gap and contiguity figures against disk (`:161–180`) | makes the CURRENT_STATE edits FORCED |
| **Derived doc pin** | `tests/docs/docCounts.test.js` | `MIGRATION_COUNT` | reads the directory (`:30–33`) and asserts the count stated in DEPLOY.md / ARCHITECTURE.md | makes the ARCHITECTURE.md edit FORCED |
| **Lifecycle / honesty** | `supabase/applied-head.json` | `"appliedHead"` | `200`; `migrationAppliedHead.test.js:39` asserts only `appliedHead <= repoHead`, which `200 <= 203` satisfies (E8, E16.2 row 9) | **NOT edited.** The bump is the owner's, in the same act as `supabase db push` |
| **Registration rule** | `tests/lint/mutationCoverage.shared.mjs` | `export const ENFORCER_DIRS` | `'tests/security'` is a member (`:42`) (E6.1) | the REGISTER row's authority |
| **Registration rule** | `scripts/mutation-coverage-manifest.json` | `"uncoveredBaseline"` | `186`, asserted EXACTLY (`:242–258`) | the new row is `"rationale"`, never `"uncovered"` (R4) |
| **Registration rule** | `tests/lint/sovereigntyLightingContract.walker.test.js` | `const CENSUS_FIGURE_KEYS` | `titles` = `it` titles, `suiteTitles` = `describe` titles (`:1380`) (E6.3) | fixes the DELTA in §7 |
| **Registration rule** | `tests/lint/negativeAssertionAnchor.walker.test.js` | `const BARE_NEGATIVE_RE` | three matchers; `SCAN_ROOTS = ['tests']`; unlisted ceiling **0** (E6.4) | forbids those three matchers in the new file |

### Forbidden alternatives

- **no change to 203's function body outside the `hard_deny` array literal and its comment** —
  contracted as a mechanical diff (A8) and executed at §8 step 4;
- **no member REMOVED from the array, ever** — the denylist only grows; a removal is the owner's
  (R2) and is a STOP here;
- **no second spelling of the client denylist** — `WORLD_SNAPSHOT_HARD_DENY` is imported;
- **no hand-rolled alternation or token parser** — both route through `sourceContract`;
- **no edit** to `snapshotDenylistDrift.test.js`, `sourceContract.js`, migration 202, migration
  136, `_gallery_sanitize_public_json`, `_gallery_dm_full_json`, or `supabase/applied-head.json`;
- **no edit to the two literal `dmLayer`/`decrees` lines** at `migrationRehearsal.test.js:383`
  and `:387–388` — they are kept, by measurement (E15.2);
- no `.not.toContain` / `.not.toMatch` / `.not.toHaveProperty` in the new walker file;
- no `.each`, no loop-registered or conditionally-registered test, no nested `describe`;
- no `src/` file, no component, no store surface, no flag;
- no files outside the manifest.

---

## 6. Exact contracts

### Inputs and outputs

```js
/**
 * Latest-wins extraction of the net-current scanner across the migrations directory.
 * Re-implements tests/security/snapshotDenylistDrift.test.js:30-45 (a deliberate THIRD copy,
 * chair ruling R3; arm A4 is what keeps it honest). Files are visited in NUMERIC prefix order.
 * @returns {{ sql: string, owner: string|null, declarers: string[], matchedIn: string[] }}
 *   sql        — the LAST matched definition, LOWERCASED, or '' when none matched
 *   owner      — the filename that produced `sql`, or null
 *   declarers  — every migration carrying a LINE-ANCHORED declaration of the scanner,
 *                whether or not the extractor could match it  (A4's left side)
 *   matchedIn  — the subset the extractor actually matched    (A4's right side)
 */
function netCurrentScanner() { … }

/**
 * The scanner's `hard_deny` array members, lowercased with the SQL.
 * THROWS when the array literal is absent — never returns [].
 * @param {string} sql @returns {string[]}
 */
function hardDenyMembers(sql) { … }

/**
 * THE CLAIM'S PREDICATE. A key is refused iff it is an EXACT, case-insensitive member of the
 * hard_deny array (Postgres: `lower(key) = any (select lower(x) from unnest(hard_deny))`), OR
 * some alternation alternative matches it as a WHOLE key (the drift test's `^alt$` rule).
 * The array is checked first and the mechanism is RECORDED, never constrained.
 * @param {string} key @returns {{ refused: boolean, by: 'array'|'alternation'|null, alts?: string[] }}
 */
function refusalOf(key) { … }
```

### State schema

Two data changes, both exactly specified.

**(1) `supabase/migrations/203_…sql` — the ONLY region that differs from 202's function body:**

```sql
-- BEFORE (202, verbatim)
    -- conditional ledgers (135): every CONDITIONAL_LEDGER_KEY but pantheon
    'religionStates','warPosture','occupations','martialReadiness','conquestFeeds',
    'mercenaryMarket','rulesetLog','spatialDigest','spatialLedgers','narrativeTempo',
    'politicsLedgers'
  ];

-- AFTER (203)
    -- conditional ledgers — THE RULE, NOT A SNAPSHOT (203): every member of the client
    -- WORLD_SNAPSHOT_HARD_DENY except the public-allowlisted `pantheon`. 136 and 202 both
    -- wrote "every CONDITIONAL_LEDGER_KEY but pantheon", which was true when written and
    -- went stale three times: factionPairStates (2026-07-20), envoyErrands (2026-08-03)
    -- and concludedWars (2026-08-31) landed on the client afterwards and nothing carried
    -- them here. tests/security/galleryScannerMirrorTotality.test.js now holds this array
    -- to the client list on every gate run, so the claim cannot go stale again.
    'religionStates','warPosture','occupations','martialReadiness','conquestFeeds',
    'mercenaryMarket','rulesetLog','spatialDigest','spatialLedgers','narrativeTempo',
    'politicsLedgers','factionPairStates','envoyErrands','concludedWars'
  ];
```

**THE MECHANICAL-DIFF CONTRACT (executed at §8 step 4, and asserted by A8):**

```js
const mask = (b) => b.replace(/hard_deny\s+constant\s+text\[\]\s*:=\s*array\[[\s\S]*?\];/, '<<ARRAY>>');
assert(mask(body203) === mask(body202));                       // 3,582 chars, byte-identical
assert(JSON.stringify(alts203) === JSON.stringify(alts202));   // 34 alternatives, same order
assert(added.length === 3 && removed.length === 0);            // three in, none out
```

Measured in advance (E14.3): **true, 3,582 identical residual characters, 34 alternatives both
sides, `hard_deny` 25 → 28, added `[factionpairstates, envoyerrands, concludedwars]`, removed
`[]`.** Everything else 203 carries — the `@rollback:` annotation, the `-- ⚠️ WRITTEN, NOT
APPLIED` block, the `Depends on:` line, the `Guarded by` list — is header comment in 202's own
idiom and costs zero effective lines.

**(2) `scripts/mutation-coverage-manifest.json` — one object under `"invariants"`:**

```jsonc
"tests/security/galleryScannerMirrorTotality.test.js": {
  "kind": "rationale",
  "rationale": "<see §7's coding instruction; it cites the executed mutants>",
  "kindNote": "rationale rather than a planted sweep mutation: a plant additionally requires a MUTATED_FILES row in scripts/mutation-sweep.sh, and this walker's arms are counterfactual-bearing by construction. Upgrading to a plant later is a strict improvement needing no baseline movement."
}
```

**Absence rules:** the extractor matched nothing → **RED at A2**, never a silent pass.
`hard_deny` array absent → `hardDenyMembers` **THROWS**. The alternation expression absent →
`sqlRegexAlternation` **THROWS** (`sourceContract.js:199`). A migration declares the scanner but
the extractor cannot match it → **RED at A4**, naming the file. A key refused by BOTH mechanisms
→ legal and expected; `by` is recorded, never constrained.

### Transition table

| Prior state | Input/event | Guard | Next state | Receipt |
|---|---|---|---|---|
| the scanner refuses every client hard-deny member | a new conditional ledger joins `WORLD_SNAPSHOT_HARD_DENY` with no SQL mirror | A1 | **RED**, naming the member | the message names the member and both ways to satisfy it |
| net-current scanner = migration N | a migration N+k re-creates it without a member's refusal | A1 | **RED**, naming the member and the new owner | as above |
| net-current scanner = migration N | a migration N+k re-creates it in a shape the extractor cannot read | A4 | **RED**, naming the unextractable file | "declared but unextractable — the guard would go on reading migration N" |
| owner = the highest-numbered declarer | a four-digit migration prefix lands | A5 | **RED** if lexical and numeric disagree | names both orders |
| 202 is net-current, three members unrefused | **this packet's 203 lands** | A1 | **GREEN**, 28 of 28 refused, every one `by='array'` | E14.4 |
| any | a key refused by the array only, or the alternation only | A1 | **GREEN** | `by` recorded, not constrained |

### Ordering and precedence

- **Latest-wins is by NUMERIC migration prefix**, not lexical. Measured identical at the base
  (every filename is three-digit zero-padded) and asserted by A5 so it stays so.
- **Within one file**, the LAST line-anchored definition wins.
- The two refusal mechanisms are **disjunctive, unordered**; the array is *checked* first only so
  `by` has a stable value.
- **203 must be the highest-numbered migration when it lands** — see §11's STOP on a contended
  number.
- Client members are walked in `WORLD_SNAPSHOT_HARD_DENY`'s frozen order; failures report in it.

### Determinism

- Hash/fork key `NONE`; PRNG `NONE`; time `NONE`; locale `NONE` — every comparison is
  ASCII-lowercased on both sides, as 202 does in SQL (`:93`) and the drift test does in JS.
- The walker is a pure function of the committed tree: same tree ⇒ same verdict.
- 203 is `immutable`, `set search_path = public, pg_temp`, and re-runnable, exactly as 202 is —
  those lines are inside the byte-identical residual.

### Flag and dormancy

- Flag `NONE`. Gate `NONE`. **Golden posture: UNCHANGED** — `generatorGoldenMaster.test.js`
  (525 rows) and `dossierProseManifest.test.js` cannot move: the packet adds no `src/` byte
  (§P3.1; motion is a STOP).
- **Dormancy of the migration itself:** 203 is INERT until the owner applies it. Its new
  refusals change nothing observable in production today.

### Lifecycle

| Create | Read | Persist | Reload | Regenerate | Undo | Import/migrate | Public veil |
|---|---|---|---|---|---|---|---|
| 203 creates no table, column, policy or row | the walker reads COMMITTED files, never a database | nothing persisted | n/a | n/a | n/a | ⭐ **`@rollback:` — recreate 202's body verbatim, i.e. this same function with the three members removed. Nothing is destroyed: the function holds no state and mints no row; the only consequence is that a snapshot carrying those three ledgers is accepted server-side again, exactly as today.** `create or replace` re-points `publish_map` (089), the `saved_maps` guard (091) and the campaign-tiles RPC (148) by name, with no signature change and no re-grant | this packet IS a public-veil guard and its cure; it adds no surface |

### Receipts and privacy

- Closed kinds: none minted. DM-only fields: none read — the walker and the migration touch key
  **NAMES** only, never a settlement, a record or a value.
- ⚠ Failure text names denied key names; those are already public in `worldSnapshotPublic.js`,
  in 202's SQL and in the drift test's messages. No new information is disclosed.

### Alignment and edit story

- Alignment: `DECLARED EMPTY: a prevention guard and its denylist have no alignment surface.`
- Edit story: `ENGINE-ONLY: a DM never sees either half.`

---

## 7. Exact change manifest

| Action | File | Symbol/region | Maximum delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `tests/security/galleryScannerMirrorTotality.test.js` | `netCurrentScanner`, `hardDenyMembers`, `refusalOf`; ONE top-level `describe`; EIGHT straight-line `it`s | `<=190 raw`, **0 eff production** | ⭐ **WRITTEN FIRST, BEFORE THE MIGRATION** (§8 step 2) and quoted RED on exactly three names. Header first: the claim; the drift-test direction it deliberately does NOT duplicate, cited by path and test title (§P6); and a six-item `CANNOT-CATCH:` list from E5.1 — a `DO`-block re-creation, a non-`$$` tag with a later `$$;` (over-run), an `alter`/`comment on` touch, the stripped Postgres `\m`/`\y` boundaries, the database, and a key registered in no client list. Import `jsRegexTokens`/`sqlRegexAlternation` from `../helpers/sourceContract.js`; import `WORLD_SNAPSHOT_HARD_DENY` and `COVERT_KEY_RE` from `../../src/domain/display/worldSnapshotPublic.js`, `PRIVATE_KEY_RE` from `../../src/domain/display/publicSafe.js`. RE-IMPLEMENT the latest-wins walk locally (R3), visiting files in NUMERIC prefix order, returning `{sql, owner, declarers, matchedIn}`; say in a comment that A4 is what keeps this third copy honest and that TOOL-4 is the consolidation. ⛔ Exactly one `describe`, exactly eight `it`s; no `.each`, no loop, no conditional registration, no nested describe. ⛔ No `.not.toContain`/`.not.toMatch`/`.not.toHaveProperty`. ⛔ No bare `if (!x) continue/return;` immediately before an `expect(`. Every negative is anchored by A2 in the same file. |
| `CREATE` | `supabase/migrations/203_gallery_scanner_client_mirror_totality.sql` | `public._gallery_world_snapshot_is_safe` | **58 eff SQL** | Copy migration 202's file and change **only** the `hard_deny` array's tail region, exactly as §6 spells it. Members spelled in the client's camelCase, appended to the existing `'politicsLedgers'` line (E14.1). ⛔ **The alternation, the recursion, `immutable`, `set search_path`, the two `jsonb_typeof` branches and the scalar return are byte-identical to 202** — §8 step 4 executes the mechanical diff and A8 asserts it. Write 202's header idiom: the design citation; what changed and what did NOT; the `-- ⚠️ WRITTEN, NOT APPLIED` block naming `appliedHead` 200 and the owner's `supabase db push`; `Depends on: 202 (the net-current body), 089 (publish_map), 091 (the saved_maps guard), 148 (the campaign-tiles RPC)`; `Re-runnable.`; the `@rollback:` annotation from §6's Lifecycle row; and a `Guarded by` list naming `tests/security/galleryScannerMirrorTotality.test.js`, `tests/ops/migrationRehearsal.test.js`, `tests/security/snapshotDenylistDrift.test.js` and `tests/security/galleryWorldSnapshotScanner.pglite.test.js`. ⛔ Re-state the sibling-sanitizer reason 202 records: `_gallery_sanitize_public_json` and `_gallery_dm_full_json` are deliberately left net-current. ⛔ **No member is ever REMOVED.** |
| `MODIFY` | `scripts/ops/migrationRehearsalCore.mjs` | `MIGRATION_TRAIN_REPO_HEAD` (`:19`); one row appended to `MIGRATION_WAVES` | `+0` and `<=34 eff` | `202` → `203` on the existing line. Append ONE frozen wave row copying the `edit-registry-public-denylist` row (`:551–584`) field for field: `id: 'gallery-scanner-client-mirror-totality'`, `from: 203`, `to: 203`, a `purpose` naming the three members and the stale-claim correction, `rollback` forward-only with the `@rollback` reversal spelled (so it classifies documented-manual-reversal like 201/202), `expectedObjects: [{ kind: 'function', name: '_gallery_world_snapshot_is_safe' }]`. ⛔ `buildMigrationRehearsalPlan` and every other function are NOT touched. |
| `TEST` | `tests/ops/migrationRehearsal.test.js` | ten figures + one `toMatchObject` | `n/a` | `:108` `repoHead` `202`→`203`; `:109` `pendingCount` `81`→`82`; `:131` append `[203, 203]`; `:138` `Array.from({length: 81})`→`82`; `:382` **re-point** `scanner.owner` to `'203_gallery_scanner_client_mirror_totality.sql'`; `:496` → `{ appliedHead: 200, repoHead: 203, pendingCount: 3 }`; `:499` append `['gallery-scanner-client-mirror-totality', 203, 203]`; `:511` the throw regex gains `203`; `:521` `repoHead`→`203`; `:522` `migrationCount`→`203`; `:644` `repoHead`→`203`; and ONE `plan.waves.find(w => w.id === 'gallery-scanner-client-mirror-totality')` `toMatchObject` block added INSIDE the existing wave `it`, in the 202 block's voice. ⛔ **NO new `describe`, NO new `it`** — this file contributes 0 to the lighting title delta. ⛔ **DO NOT TOUCH `:383` or `:387–388`** — the `dmLayer`/`decrees` literal lines assert the ARRAY mechanism specifically, are NOT subsumed by the walker's subset claim (E15.2), and keep passing unchanged because 203 removes nothing. |
| `REGISTER` | `scripts/mutation-coverage-manifest.json` | one object under `"invariants"`, alphabetically between `"tests/security/gallerySanitizer.pglite.test.js"` and `"tests/security/gallerySeedLeak.pglite.test.js"` | `+4` | `"kind": "rationale"` (R4) — **never `"uncovered"`**, which takes the count to 187 against `uncoveredBaseline: 186` and REDS the SHRINK-ONLY arm. The reason (≥40 chars) **cites the executed mutants**: the walker derives both sides from source, and each arm carries its own in-file counterfactual driving the LIVE predicate — A3 plants a hard-deny mutant that this predicate refuses and the drift test's predicate accepts; A4 plants a `$fn$`-quoted declaration and a `drop`+plain-`create`; A5 plants a four-digit prefix; A6 plants a substring near-miss; A8 plants the mechanical diff. A planted source regression would prove only that a guard tests its own subject. Add the `kindNote` from §6. ⛔ Do NOT re-serialise the file; edit the one region. ⛔ Do NOT touch `uncoveredBaseline`. |
| `DOC` | `docs/DEPLOY.md` | `:198` | one line | `**Current migration head: \`202_edit_registry_public_denylist.sql\`**` → `203_gallery_scanner_client_mirror_totality.sql`. FORCED by `tests/docs/deployRunbookFreshness.test.js:121`, which derives the head from disk. |
| `DOC` | `ARCHITECTURE.md` | `:269` | one line | `- **migrations/** (202)` → `(203)`. FORCED by `tests/docs/docCounts.test.js:30–33`. |
| `DOC` | `docs/CURRENT_STATE.md` | `:5`, `:79–83` | two lines + one clause | `contiguous to 202` → `203`; `head 202 is 2 migrations ahead` → `head 203 is 3 migrations ahead`; the sentence that names 201's and 202's purposes gains 203's (the client-mirror totality of the gallery scanner). FORCED by `tests/docs/architectureFreshness.test.js:161–180`, which parses those exact figures against disk. |

**Generated artifacts:** `NONE`.

⛔ **NOT in this manifest, by measurement:** `supabase/applied-head.json` (the bump is the
owner's; `appliedHead <= repoHead` still holds at `200 <= 203`); `supabase/rollback/` (202 has
no `.down.sql`; 203 uses the in-file `@rollback:` idiom); `docs/ops/MIGRATION_REHEARSAL_RUNBOOK.md`
(names no migration number); `scripts/check-migration-head.mjs`, `scripts/vercel-ignore-build.mjs`,
`scripts/backup-restore-drill.mjs`, `scripts/ops/migration-rehearsal.mjs`,
`tests/ops/postDeployVerify.test.js`, `tests/lint/testRatchet.test.js`,
`tests/docs/migrationContiguity.test.js` (all DERIVED from disk or from
`MIGRATION_TRAIN_REPO_HEAD`); `tests/scripts/implementationPackets.test.js` (a synthetic
fixture, not a claim about the tree). Thirteen sites classified in E16.2.

⛔ **`tests/lint/.lighting-census-baseline.json` is NOT a generated artifact of this packet and
§8 schedules no refreeze** (§P2.1: an in-packet refreeze needs a commit, the commit drifts the
sealed HEAD, and the sealed verbs then refuse — measured at EM-P0's build). The predicted
INTERIOR RED is a **DELTA**:

> **lighting census delta: `files +1 · parked +0 · credited +1 · titles +8 · suiteTitles +1`**
> — from this manifest's ONE new test file with exactly one `describe` and eight `it`s,
> statically registered and importing from `'vitest'` ⇒ credited, not parked. The edits to
> `tests/ops/migrationRehearsal.test.js` add **no title**. The ABSOLUTE is stamped by the chair
> at promotion and the re-derivation is the train's terminal act. Precedent: EM-P0 §7.

No other file may be edited.

---

## 8. Ordered coding sequence

0. **Dispatch and seal**; stop on any preflight mismatch (§4). Confirm
   `scripts/mutation-coverage-manifest.json` is Git-clean (EM-B1d shares it) and that **203 is
   still the free number**.
1. **Capture the baseline, executed, before the first edit** (the K-gate):
   (a) the net-current scanner's owner under BOTH a lexical and a numeric prefix sort — they
   must agree, and the owner must be the highest-numbered declarer (`202_…`);
   (b) the `hard_deny` member list (25) and the alternation list (34), with their sizes;
   (c) the per-member refusal verdict for all 28 of `WORLD_SNAPSHOT_HARD_DENY`.
2. ⭐ **WRITE THE WALKER AND QUOTE ITS RED.** Run it. It MUST red on A1 naming **exactly**
   `factionPairStates`, `envoyErrands`, `concludedWars`. **Quote that output verbatim in the
   receipt — it is the packet's red-first proof.** ⛔ **A fourth name, or a missing one, is a
   STOP**: the client list moved under the packet and every figure here must be re-measured.
3. **Author migration 203** from 202's body, changing only the array tail (§6, §7).
4. ⭐ **EXECUTE THE MECHANICAL DIFF** in a scratch script: mask both bodies' `hard_deny` array
   literal and assert the remainder is byte-identical (expected: 3,582 chars); assert the
   alternation is unchanged (34 both sides, same order); assert three added, zero removed.
   ⛔ **Any other difference is a STOP.**
5. **Re-run the walker. It MUST be GREEN, 28 of 28, every member `by='array'`.** Quote it.
6. **Add the registrations**: the wave row and the head constant
   (`scripts/ops/migrationRehearsalCore.mjs`); the ten figures and one `toMatchObject` in
   `tests/ops/migrationRehearsal.test.js` — **leaving `:383` and `:387–388` untouched**; the
   `"rationale"` row; the three DOC lines.
7. **Run focused verification** (§10), one gate at a time through the mutex.
8. **Write the completion receipt** (§12), including the owner-hand line. The lighting refreeze
   and the wave-end gate are the TRAIN TERMINAL's and the chair's, not this packet's.

### Bounded algorithm (the walker)

```text
1. declarers := every supabase/migrations file matching /^\d.*\.sql$/ whose text matches
   /^create\s+(or\s+replace\s+)?function\s+public\._gallery_world_snapshot_is_safe\b/im
2. for each such file in NUMERIC prefix order, take the LAST hit of
   /^create\s+or\s+replace\s+function\s+public\._gallery_world_snapshot_is_safe\b[\s\S]*?\$\$;/igm
   -> {sql (lowercased), owner}; record the file in matchedIn
3. A4: declarers \ matchedIn MUST be []            ; else RED naming each file
4. A5: owner MUST equal max(declarers by numeric prefix); else RED naming both orders
5. A2: sql non-empty; alternation >= 30; hard_deny >= 20; WORLD_SNAPSHOT_HARD_DENY >= 20
6. A1: for each member m of WORLD_SNAPSHOT_HARD_DENY:
        refused(m) := hard_deny.includes(lower(m))          -- by 'array'
                   OR alternation.some(a => /^a$/i.test(m)) -- by 'alternation'
      unrefused := members where refused(m) is false  ->  MUST be []
7. A3/A6/A7/A8: the in-file controls, the two named editor keys, and the mechanical diff,
   all against the SAME predicate the live arms use
```

---

## 9. Acceptance matrix

| ID | Case | Fixture/input | Required observation | Test home |
|---|---|---|---|---|
| **A1** | **Main behavior — the subset claim** | the live `WORLD_SNAPSHOT_HARD_DENY` (28) against the live net-current scanner | `unrefused` is `[]`. ⭐ **RED-FIRST at step 2 on exactly `["factionPairStates","envoyErrands","concludedWars"]`; GREEN at step 5, 28 of 28, every member `by='array'`.** The failure text names each member and both ways to satisfy it | `tests/security/galleryScannerMirrorTotality.test.js` |
| **A2** | **Dormant/absent — non-vacuity, and the liveness anchor for every negative in the file** | the live tree | the extractor located a scanner and names its owner; `SQL_ALTS.length >= 30`; `hardDenyMembers.length >= 20`; `WORLD_SNAPSHOT_HARD_DENY.length >= 20`. A parse regression that empties any set reds HERE (the drift test's `:71` idiom) | same |
| **A3** | **Counterforce — the planted mutant, and the proof the direction is NEW** | a synthetic copy of the net-current body with the three members struck from `hard_deny`, held in the test body | (i) this predicate REFUSES to call them refused — it discriminates; (ii) the drift test's own `sqlDenies` rule ACCEPTS the same mutant — the claim is genuinely unasserted elsewhere. Measured in advance: mutant M-e′ is GREEN on the drift test and RED here (E15.1) | same |
| **A4** | **Boundary — the reconciliation claim (the 204+ cure)** | the live migrations, plus two synthetic sources: one `$fn$`-quoted definition, one `drop` + plain `create function` | live: `declarers \ matchedIn` is `[]`. Synthetic: both shapes are DECLARED and NOT MATCHED, so the arm would red on them — it is not a tautology over a corpus that happens to use `$$` everywhere (E5, E5.1) | same |
| **A5** | **Idempotency/ordering — latest-wins is NUMERIC** | the live filenames, plus a synthetic list containing a four-digit prefix | live: the owner equals the highest-numbered declarer, and lexical and numeric agree (every prefix is three digits). Synthetic: with a `1000_` member the two orders DISAGREE, so the arm has a distinguishing case (E1.2) | same |
| **A6** | **Sparse/near-miss — the array arm is EXACT, not substring** | synthetic keys `npcStatesCount`, `xdm` | `npcStatesCount` is NOT refused *by the array* (Postgres's `lower(key) = any(…)` is exact equality); `by` is recorded per member, so a member migrating between mechanisms is visible in the output rather than invisible in a boolean | same |
| **A7** | **Privacy/regression — the two editor keys, named** | `'dmLayer'`, `'decrees'` | both refused, with `by` recorded. The generalisation of `migrationRehearsal.test.js:381–388` — which is KEPT, because it asserts the array mechanism specifically and is not subsumed (E15.2) | same |
| **A8** | **Real integration — the mechanical-diff contract on migration 203** | migration 202's function body and migration 203's, both read from disk | with each body's `hard_deny` array literal masked, the remainders are **byte-identical**; `sqlRegexAlternation` returns the **same 34 alternatives in the same order** for both; `hard_deny` grows 25 → 28 with `["factionPairStates","envoyErrands","concludedWars"]` added and **nothing removed**. Measured in advance: true, 3,582 identical residual chars (E14.3) | same |

This table is the entire edge-case budget (8 of 8). **Title arithmetic:** eight `it` titles +
one `describe` title = the lighting delta `titles +8 · suiteTitles +1` in §7.

---

## 10. Verification commands

```sh
# Focused static checks
npx eslint tests/security/galleryScannerMirrorTotality.test.js scripts/ops/migrationRehearsalCore.mjs tests/ops/migrationRehearsal.test.js

# Focused tests — ONE test directory per invocation, the slot acquired in the same command
GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/security/galleryScannerMirrorTotality.test.js \
  tests/security/snapshotDenylistDrift.test.js \
  tests/security/worldSnapshotDenyCensus.test.js \
  tests/security/galleryWorldSnapshotScanner.pglite.test.js \
  tests/security/gallerySanitize.pglite.test.js

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/ops/migrationRehearsal.test.js

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/docs/architectureFreshness.test.js \
  tests/docs/deployRunbookFreshness.test.js \
  tests/docs/docCounts.test.js \
  tests/docs/migrationAppliedHead.test.js

GATE_MUTEX_TIER=shared GATE_MUTEX_MAX_POLLS=100000 GATE_MUTEX_POLL_SECONDS=20 \
  sh scripts/gate-mutex.sh --run -- npx vitest run --pool=threads --maxWorkers=2 \
  tests/lint/mutationCoverageManifest.test.js \
  tests/lint/contractTestAntiVacuity.walker.test.js \
  tests/lint/negativeAssertionAnchor.walker.test.js \
  tests/lint/sovereigntyLightingContract.walker.test.js

npm run validate:migration-head
node scripts/implementation-packets.mjs validate

# Sealed receipt and exact-state handoff; neither is landing authority
npm run check:packet -- EM-B3c
npm run implementation:resume -- EM-B3c
```

**Expected:** every command exits `0`, EXCEPT the named INTERIOR RED —
`tests/lint/sovereigntyLightingContract.walker.test.js` reds on the census delta of §7 until the
chair re-derives the register at the train's terminal — and except **`validate:migration-head`,
which will surface THREE pending migrations (201, 202, 203) instead of two: visible, not fatal,
by design.** Report actual counts; do not copy historical counts.

⛔ **A gate line with no printed test count DID NOT RUN** (`gate-mutex.sh` gives up after its
poll ceiling and exits 0 — a live false green). Never read a gate through a shell pipe; use
`npm run check:tail` or `sh scripts/gate-tail.sh`.

No golden or prose-manifest run is listed: the packet adds no `src/` byte and cannot move either
(§P3.1). No typecheck configuration: no typed production surface. No `build:edge-shared`: no
change path is an input of any of the five bundle metas (E7.2). No bundle-ceiling run: no
budgeted chunk is reached (E7.1).

---

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and EM-PREAMBLE §P8, stop if:

- the dispatch seal is missing or invalid, or resume reports authority, HEAD, foreign-work or
  receipt-integrity drift;
- ⭐ **step 2's red names anything other than exactly `factionPairStates`, `envoyErrands`,
  `concludedWars`** — a fourth name, or a missing one, means the client list moved under the
  packet and every figure here must be re-measured before a single edit;
- ⭐ **step 4's mechanical diff shows ANY difference between 202's and 203's function bodies
  outside the `hard_deny` array literal and its comment** — including one alternation
  alternative, one whitespace character, or a changed `search_path`;
- **the walker is not GREEN at step 5**, or any member is refused by neither mechanism;
- **`203` is taken** when the packet dispatches — another lane landed a migration first. The
  packet is STALE: re-number to the next free value and re-measure every figure that moves with
  it (the filename in DEPLOY.md, `MIGRATION_TRAIN_REPO_HEAD`, the wave row's `from`/`to`, the
  ten figures in `migrationRehearsal.test.js`, the `scanner.owner` pin, the CURRENT_STATE gap).
  ⛔ Do not squeeze in beside another migration at the same number;
- **the net-current scanner's owner is no longer `202_edit_registry_public_denylist.sql`** at
  dispatch, or `sqlDenies('decrees')` is false — the premise moved;
- a lexical and a numeric prefix sort of the migrations disagree at the base — A5's precondition
  is gone and the extraction idiom this packet copies is already wrong estate-wide;
- **any member would be REMOVED from the `hard_deny` array, or the alternation would be
  narrowed** — the denylist only grows; weakening it is the OWNER's and is never this packet's;
- **`supabase/applied-head.json` would be edited** — the bump is the owner's hand, in the same
  act as `supabase db push`;
- the fix appears to require editing `snapshotDenylistDrift.test.js`, `sourceContract.js`,
  migration 202, migration 136, or the two sibling sanitizers;
- **`tests/ops/migrationRehearsal.test.js:383` or `:387–388` would be edited** — those two
  literal lines are KEPT by measurement (E15.2);
- a `requiredSymbols` row would pin a migration-head FIGURE in one of the five
  `MIGRATION_HEAD_FIGURE_PATHS` — rule HK-5 refuses it and the dispatch fails;
- `scripts/mutation-coverage-manifest.json` is not Git-clean at dispatch (EM-B1d shares it), or
  the new row would need `"kind": "uncovered"`, or `uncoveredBaseline` would need to move;
- the walker would use `.not.toContain`/`.not.toMatch`/`.not.toHaveProperty`, a `.each`, a
  loop-registered test or a nested `describe`;
- a golden or the prose manifest moves.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into
the next wave.

---

## 12. Completion receipt

- Base SHA:
- Dispatch bundle and seal identity:
- Final commit or working-tree state:
- Exact changed files and effective-line deltas:
- ⭐ **THE RED-FIRST PROOF (§8 step 2), quoted verbatim — the walker's A1 failure naming exactly three members:**
- ⭐ **THE MECHANICAL DIFF (§8 step 4), quoted — residual chars identical, alternation unchanged, three added / zero removed:**
- ⭐ **THE GREEN (§8 step 5), quoted — 28 of 28 refused:**
- Acceptance cases:
- Focused commands, exits, and counts:
- Sealed per-step receipt and exact-state resume status:
- Both typecheck configurations: `n/a — no typed production surface`
- Wave-end gate stages actually executed: `n/a — the terminal is the train's`
- Base-versus-wave failure identity diff:
- Dormancy/golden result: `UNCHANGED (asserted by construction: no src/ byte)`
- Bundle/first-paint result: `NO BUDGETED CHUNK REACHED (E7.1); no edge-shared bundle staled (E7.2)`
- `validate:migration-head`: `expected — THREE pending (201, 202, 203); visible, not fatal`
- Generated artifacts: `NONE`
- Deviations: `NONE | STOP`
- Out-of-scope observations, without investigation:
- Judgment calls: `NONE`
- ⛔⛔ **THE OWNER'S HAND — carried verbatim into the receipt:** **"APPLYING migrations 201–203
  (`supabase db push` + the applied-head bump) is the OWNER's hand; until then production's
  scanner is the pre-202 definition and the gap this packet closes is OPEN IN PRODUCTION."**

### Findings recorded for the chair (not built here)

- ⭐ **THE STRUCTURAL REPLACEMENT FOR THE `scanner.owner` PIN (E17).** `migrationRehearsal.test.js:382`
  conflates *the extraction works* with *this migration is newest*; the second rots on every
  scanner migration, and this packet re-points it as every previous one has. The non-rotting form
  asserts an EXISTENCE claim plus EM-B3c's arm A5:
  `expect(scanner.declarers).toContain('<this wave's migration file>')` and
  `expect(scanner.owner).toBe(highestNumberedDeclarer(scanner.declarers))`, with the wave's
  substance claims (`hardDenyMembers ⊇ {…}`, `denies(…)`) unchanged. It would edit a LANDED
  packet's `checks` member, so it belongs in the tooling window beside **TOOL-4**, not inside a
  security landing.
- **TOOL-4 (the chair's, R3):** three copies of the latest-wins directory walk now exist —
  `snapshotDenylistDrift.test.js:30–45`, `migrationRehearsal.test.js:38–56`, and this walker's.
  Consolidating them onto `tests/helpers/sourceContract.js` would edit two shipped security
  guards that are `checks` members of LANDED packets (EM-B3a `checks[2]`, EM-B3b `checks[0]`
  and `checks[2]`). Arm A4 keeps all three honest until then.
- **The sealed dispatch's substrate check stays path-based.** EM-B3c does not make a future
  `204_*.sql` visible to `implementation:dispatch`; it makes it visible **at the gate**.
  EM-B3a's K2 deferral is discharged in that narrower sense.
- **The two sibling sanitizers were re-measured and remain out of scope** (E1.3):
  `_gallery_sanitize_public_json` (net-current 189) and `_gallery_dm_full_json` (129) are
  projections, not predicates, and their top-level allowlist already drops settlement-root keys.
