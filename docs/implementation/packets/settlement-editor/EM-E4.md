# `settlement-editor / tests-only mode` — EM-E4: THE DIRECTOR'S PINS (RECORD)

- **Status:** `LANDED`
- **Packet version:** `record-1` — written after the fact by the HISTORIAN seat (U3), 2026-09-23
- **Landed at:** `837a995d39b824f1761997f438ac8f348164f497` — EM-E4 (cure): the fork-pin suite renamed off the manifest's enrolment words and its observed-shape reads brought under the walker's law
- **Verified base:** `feat/em-e4-director-pins` at `ec09c2e47020bc6fd0e3f24e972e7915515c9964`
- **Preamble:** `docs/implementation/preambles/EM-PREAMBLE.md` (SHA-256: TO BE STAMPED BY THE CHAIR)

⛔ **A RECORD, NOT A DISPATCHABLE CONTRACT.** This member was built under TESTS-ONLY MODE — the owner, 2026-09-23 ~09:1x, *"Can we just have it where we only have the testing apparatus? As we did so many months ago?"* → *"Then do it and start building! Thank you"* (`docs/OPERATING_STANDARD.md` rule 39; stasis row S11). Under that word the estate's own ritual — packets, seals, placements, picks, flips, count provers, pre-proofs — is PARKED, never deleted, and the record is the branch's commit receipts, the progress board and this file. It pins no symbol, reserves no path, and may not be dispatched; the authority that governed the build is §1's brief and the design section that brief cites. The unfreeze train restores the ritual (U1, U3).

## §1 — The brief this member was built from

**The brief:** `briefs/launch/BUILD-EM-E4-tests-only.md` version 2, after judgment 265 (the first cut narrowed to registered + declared forks).

**The charge, in one sentence.** The director's pins over the forks that DECLARE a vocabulary, resolving in `chooseOrPin`'s own shape so a pinned fork yields its outcome without advancing the stream; the siege band exported and the pending offer put on the record.

## §7 — What moved (the commits, and their files)

| commit (on the train) | on its branch | subject |
|---|---|---|
| `1ac56429b` | `ad9321796` | EM-E4: the director's pins over the two forks that declare a vocabulary, a pin that returns the outcome without advancing the stream, and the standing peace offer |
| `837a995d3` | `2d55007ef` | EM-E4 (cure): the fork-pin suite renamed off the manifest's enrolment words and its observed-shape reads brought under the walker's law |

| action | path |
|---|---|
| CREATE | `src/domain/edit/directives.js` |
| MODIFY | `src/domain/worldPulse/decreeHook.js` |
| MODIFY | `src/domain/worldPulse/peaceTermsDrafting.js` |
| MODIFY | `src/domain/worldPulse/warSiegeVerdict.js` |
| CREATE | `tests/simulation/directorFork.test.js` |

Superseded inside this member (created and then renamed or removed by its own later commit, so the manifest carries only what stands in the tree): `tests/simulation/pinFork.test.js`.


## §9 — The acceptance cases, by title, as the commits record them

- **E4-1** — ruling 1 is live: both first-cut forks carry a registered row WITH a vocabulary, and the event lottery carries a row WITHOUT one (the negative half).
- **E4-2** — the four bands the REAL `resolveSiegeVerdict` rolls are exactly `SIEGE_VERDICT_BANDS`, and each one's `falls` agrees with the verdict's own.
- **E4-3** — the festival's pin words are the DRAW's own; `cancelled` is the deterministic skip arm's and is not among them (six typed, five drawn — measured, not assumed).
- **E4-4** — a pin yields the pinned outcome and the stream is where it was — the festival, against a control trace that never reaches the fork, with the unpinned negative.
- **E4-5** — the same for the siege band, and the band carries `falls` with it.
- **E4-6** — an unpinned fork draws exactly as today, and a rotted bag entry draws too.
- **E4-7** — an out-of-vocabulary outcome and a fork with no vocabulary are BOTH refused before any tick; the siege's READING words are refused as pin words; a non-pin op is silence.
- **E4-8** — a refused pin never enters the bag, so the fork is never reached by it at the tick.
- **E4-9** — the bag at the tick: due now, due at this tick, a future pin untouched, the later of two on one fork wins, and zero pins hands back ONE shared frozen bag.
- **E4-10** — the bag is read at the head, before the apply, and the two readings of `isDue` cannot drift (the due ids equal the causes the apply names).
- **E4-11** — every pin's write takes the route `authorityFor` gives it, over the real `ACTOR_INITIATED_MAJOR_TYPES`; an unroutable pin is a proposal, never an auto.
- **E4-12** — the drafting table's own terms become a standing offer and the pending-offer read finds it; a second suing replaces rather than duplicates.
- **E4-13** — the offer key is never minted without an offer, and it rides the saved blob whole.
- **E4-14** — the structural fence: the directive leaf imports ONE module, the hook still imports EXACTLY TWO, and neither reaches a catalogue.

## §10 — The checks the builder ran

- `npx vitest run --pool=threads --maxWorkers=2 tests/simulation/directorFork.test.js`
- `node scripts/implementation-packets.mjs validate`

## §12 — How it reached the train

Branch `feat/em-e4-director-pins`, built at `ad9321796`, `2d55007ef`; pushed as TRANSPORT (rule 24a — a lane push opens no pull request and merges nothing) and read on GitHub Actions (runs `35878071414`, `35885657684`). The chair read the run against the DECLARED reds — the two census cars under the `CENSUS_STASIS` switch (stasis S6), the lighting walker's title count, the observed-shape family's level at the branch's base (U37, closed by the schema-23 rung at `8cc2926d0` + `3b21ba791`) and the declared phone-floor rows (U25, re-recorded once at the tip by `56b085fc5`) — and MERGED it into the train branch `em-train-17-2026-09-23` by pick: `1ac56429b`, `837a995d3`. Nothing was pushed as a landing; the pull request, the merge to `master`, the deploy and every migration remain the OWNER's.

Its cure did TWO things beyond the rename: the fork-pin suite was renamed off the mutation manifest's enrolment word, and its own two observed-shape reads were cured STRUCTURALLY (the standing offers keyed by counterparty, design §18) rather than bought with a register row. HBF-86's row still declares `SIEGE_FALL_ODDS_WORDS` where the pin's vocabulary is the exported band (U18), `TRADITION_OUTCOME` types six words where the draw makes five (U19), and ruling 7's inbound-envoy read is unlanded (U20).
