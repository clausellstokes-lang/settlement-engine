# Corpus Coverage / AO-2+3 — the Wizard News voice contract and its first cure

- **Status:** LANDED
- **Packet version:** `1`
- **Verified base:** `claude/composite-r4` at `481c7e0aaa46bad968d1d0ad4b984294d15c7bf6`
- **Last revalidated:** 2026-08-13 at zero-bank child
  `f0c272e894f3f4d4edbd7fd1dac4c65580f87e52`.
- **Landed:** 2026-08-13 — contract C1
  `c825af928729f0738f6fe97cc1246d2558fe5627`, cure C2
  `d4f213d02fa2bc0e11c71245ad8d5cfb30468837`, and zero-bank C3
  `f0c272e894f3f4d4edbd7fd1dac4c65580f87e52`; do not redispatch.
- **Depends on:** AO-0 schema-8 genesis `e71beb84355666fe5508f61c0f9acdc516a97b79`, terminal flip `481c7e0aaa46bad968d1d0ad4b984294d15c7bf6`, and CR-AO-3/4/7/11 in ledger `e7d66ccecf9d514870489493d4815f2a3aa05f56`.
- **Commit authority:** the coding agent edits only the eight manifest paths and does not commit. The coordinator alone constructs, proves, and old-value-CAS lands the three immutable commits in §8.
- **Census receipt:** the one new test file added exactly eight `it` titles and one
  `describe` title; the whole re-derived five-tuple is
  `2410/365/2045/19968/5636`. AO-2+3 is terminal and the reservation is free.
- **Declared behavior shift:** applied/outcome Wizard News summaries matching `can advance through` become indicative at the view-time curation seam, and that changed summary is retained in the campaign's persisted Wizard News feed. Queued summaries, reasons, underlying outcomes/proposals, state shape, every non-summary feed field, and every unrelated home remain unchanged.

## 1. Reconciled authority

CR-AO-7 requires the contract to assert before the cure. The ordinary landing law forbids
exposing a red branch tip. CR-AO-11 resolves that lifecycle narrowly: AO-2 and AO-3 are one
coordinator-owned, three-commit train and one branch CAS. The semantic order remains visible;
neither deliberate-red ancestor becomes a handoff or shared branch tip.

The live scalar corpus corrects the charter's older shorthand. Across 272 introduced Wizard
News entries and 53 observed `(kind|impactKind)` homes there are **48 distinct prospective
summary values and 53 occurrences at seven applied NPC summary addresses**:

| Address | Distinct | Occurrences |
|---|---:|---:|
| `applied|npc_bargain|summary` | 5 | 5 |
| `applied|npc_exploit|summary` | 3 | 3 |
| `applied|npc_expose|summary` | 1 | 1 |
| `applied|npc_mobilize|summary` | 5 | 5 |
| `applied|npc_protect|summary` | 8 | 8 |
| `applied|npc_reform|summary` | 21 | 23 |
| `applied|npc_suppress|summary` | 5 | 8 |
| **Total** | **48** | **53** |

The five duplicate occurrences are real debt, not a counting error. `reasons` participate in
entry reconstruction so two otherwise similar entries are not collapsed, but are neither
classified nor rewritten. CR-AO-3 ruled summary reuse defective; it did not adopt the charter's
broader reasons suggestion.

## 2. Outcome and explicit non-goals

The packet lands one finite-semantics contract and its first production cure:

1. Reconstruct the 272 introduced Wizard News entries from AO-0's opt-in scalar stream.
2. Classify the exact seven applied-summary addresses against the closed vocabulary
   `prospective | indicative` and freeze the measured debt identity-by-identity.
3. Prove that debt exists before changing production.
4. At `newsEntryForOutcome`, change applied summary projection from `can advance through` to
   `advances through` only for the exact seven ruled NPC impact kinds; proposal projection stays
   byte-identical.
5. Bank the seven vanished debt identities to zero without erasing the immutable historical
   denominator or weakening either-direction comparison.
6. Register `HZ-CROSSHOME` as live MACHINERY, route a derived pre-mortem predicate to it, and
   satisfy the new test file's mutation-coverage and lighting-census obligations.

Explicitly out:

- `reasons`, headlines, `npcAgency.js`, underlying outcome/proposal or state shape, IDs, times, feed order,
  metronome suppression, significance, routing, or any other prose family;
- AO-4 totality, inert rewrite quarantine, and the uncovered faction challenge headline;
- AO-5 timeline, pulse-history, regional-log, chronicle, or `narrativeSummary` coverage;
- AO-6's standing source-mutant battery;
- any golden re-record, dependency, flag, tuning value, global timeout/config edit, floor, ceiling, observed-shape
  scanner/baseline, test-ratchet baseline, mutation sweep, or ninth implementation path.

## 3. Contract and reconstruction law

Create `scripts/lib/news-voice-contract.mjs` as pure infrastructure. It may import no product
producer. The walker invokes `buildObservedCorpus({ scalarFields: OBSERVED_SCALAR_FIELDS })`
and gives the returned rows to this helper.

### 3.1 Introduced-entry reconstruction

Use only scalar rows rooted at each of the twelve `pulseResult` roots and beneath
`wizardNews.entries[index]`. Rehydrate each visible entry from its relative typed paths.
Canonical entry identity is the deterministic serialization of all selected scalar leaves
beneath the entry, with root ordinal and array index removed; `reasons` and tags therefore remain
part of identity while volatile IDs/times remain unavailable by AO-0 law.

Each pulse root is a newest-first accumulated, capped snapshot. For every canonical signature,
take only the positive multiset-count delta from the immediately preceding snapshot. Negative raw
deltas are expected cap evictions, not corruption: exclude them from introductions, retain their
absolute magnitude as removal telemetry, and require the current arithmetic to close at exactly
272 introductions - 32 removals = 240 final rows. This removes snapshot carry-forward while
preserving repeated visible entries as occurrences. For a signature whose positive delta is `N`,
select the **lowest `N` current entry indexes** in that newest-first snapshot as its introduced
occurrences; then stable-sort all introductions by pulse ordinal, selected source entry index, and
signature. No other duplicate-to-index assignment is permitted.

Build the AO-0 scalar corpus once in one `beforeAll`; invoke the pure reconstruction twice over
those same frozen rows and require byte-identical results, exactly 272 introductions, 32 removals,
240 final rows, and 53 homes. An empty root set, missing required field, malformed typed path,
noninteger/impossible multiset count, unclosed balance, or denominator drift fails closed.

### 3.2 Closed address and voice vocabulary

The contract owns exactly the seven table rows in §1, field `summary`, expected class
`indicative`. A summary containing the token-bounded phrase `can advance through` is
`prospective`; a nonblank summary at one of those addresses is otherwise `indicative`. Blank or
non-string values at an owned address are malformed, never silently indicative. No AI/model,
fuzzy score, broad modal vocabulary, or source scan is permitted.

The observed debt identity is:

```text
(kind|impactKind|field|observedVoiceClass) -> { distinctValues, occurrences }
```

The JSON baseline has this exact closed envelope and no additional keys. Row containers are
canonical codepoint-sorted **arrays**, never JSON objects keyed by identity: duplicate object keys
are erased during `JSON.parse`, so a keyed map cannot prove duplicate rejection.

```json
{
  "schemaVersion": 1,
  "corpus": {
    "pulseRoots": 12,
    "introductions": 272,
    "retirements": 32,
    "finalEntries": 240,
    "homes": 53
  },
  "retroDetection": {
    "rows": [
      { "home": "applied|npc_bargain", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 5, "occurrences": 5 },
      { "home": "applied|npc_exploit", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 3, "occurrences": 3 },
      { "home": "applied|npc_expose", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 1, "occurrences": 1 },
      { "home": "applied|npc_mobilize", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 5, "occurrences": 5 },
      { "home": "applied|npc_protect", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 8, "occurrences": 8 },
      { "home": "applied|npc_reform", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 21, "occurrences": 23 },
      { "home": "applied|npc_suppress", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 5, "occurrences": 8 }
    ],
    "totals": { "identities": 7, "distinctValues": 48, "occurrences": 53 }
  },
  "current": {
    "outstanding": [
      { "home": "applied|npc_bargain", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 5, "occurrences": 5 },
      { "home": "applied|npc_exploit", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 3, "occurrences": 3 },
      { "home": "applied|npc_expose", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 1, "occurrences": 1 },
      { "home": "applied|npc_mobilize", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 5, "occurrences": 5 },
      { "home": "applied|npc_protect", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 8, "occurrences": 8 },
      { "home": "applied|npc_reform", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 21, "occurrences": 23 },
      { "home": "applied|npc_suppress", "field": "summary", "expectedVoiceClass": "indicative", "observedVoiceClass": "prospective", "distinctValues": 5, "occurrences": 8 }
    ],
    "totals": { "identities": 7, "distinctValues": 48, "occurrences": 53 }
  }
}
```

This is the literal C1 envelope; at C3 `current.outstanding` is `[]`. Every envelope object has
exactly the displayed keys. Every count is a non-negative safe integer; `schemaVersion` is exactly
`1`; all row strings are nonblank and equal their trim; each row object has exactly the six
displayed keys. Rows sort by the codepoint identity
`home|field|observedVoiceClass`, and duplicate identities fail before totals are compared.
`current.outstanding` must be an exact object-deep-equal subset of immutable
`retroDetection.rows`; it may not alter a historical row's counts or vocabulary. Both totals
objects are derived from their arrays and must equal them exactly. The five `corpus` keys and
values are exact and conservation `introductions - retirements === finalEntries` is mandatory.

Commit 1 freezes all seven current rows. Commit 3 changes only `current.outstanding` to `[]` and
`current.totals` to exact zeros `{ "identities": 0, "distinctValues": 0, "occurrences": 0 }`;
`schemaVersion`, `corpus`, and `retroDetection` remain byte-identical.

Comparison is only-shrinks in both directions: a new identity, grown count, shrunk count, or
vanished identity all fail with their address and both values. The only legal bank is an explicit
same-train baseline edit after the cure. Malformed JSON/envelope, duplicated identity, inconsistent
totals, zero/empty corpus, or historical-denominator movement fails before comparison.

### 3.3 One ordered completion test across all three commits

Exactly one `it` identity owns the deliberate red, with the same title and ordered body at C1,
C2, and C3. Name it `contract -> cure -> bank completion is aligned and debt-free`. It performs
these steps in this order, with ordinary fail-fast assertions:

1. compare live debt to `current.outstanding` using the bidirectional comparator;
2. validate production alignment for the baseline phase — a nonzero current array requires the
   exact seven prospective pre-cure addresses, while an empty current array requires all seven
   address-gated applied fixtures indicative and their paired proposals unchanged; then
3. require `current.totals` to equal exact zero totals.

Therefore C1 passes steps 1–2 and fails **only step 3**; C2 fails **at step 1** with exactly seven
vanished identities and reaches neither later assertion; C3 passes all three. The other seven
`it` registrations are phase-stable helper/envelope/reconstruction controls. There is no separate
unconditional cure/completion test: it would create a second intermediate red and destroy the
auditable red cardinality.

### 3.4 Production cure

Modify only `src/domain/worldPulse/worldPulseFeedCuration.js` in commit 2. Add the summary
counterpart to the existing headline projection and apply it only when `status !== 'proposal'`,
`outcome.candidateType` is one of the seven §1 NPC impact kinds, and the exact source grammar
`/\bcan advance through\b/` matches:

```text
can advance through  ->  advances through
```

The seven-kind allow-set is closed and exact. A generic applied-summary rewrite is forbidden:
AO-2+3 owns only the seven observed addresses, while AO-4 owns totality. The replacement is
token-bounded, first-match deterministic, and number-invariant. An applied summary at an unowned
impact kind or with no matching grammar passes through unchanged. A proposal summary always
remains the original `outcome.summary`. `outcome`, `reasons`, and every returned key other than
the affected applied `summary` remain untouched. The curated summary is a deliberately changed
persisted Wizard News projection; no packet claim may call the whole campaign feed byte-identical.

## 4. Hard scope and line budget

| Limit | AO-2+3 budget |
|---|---:|
| Behavior families | 1 — Wizard News cross-home summary voice |
| Unique implementation paths | exactly 8 |
| New logic-bearing leaves | 1 helper, at most 250 effective lines |
| Existing production files modified | exactly 1 |
| `worldPulseFeedCuration.js` delta | at most 15 effective lines at `newsEntryForOutcome` |
| New test files / registrations | exactly 1 / exactly 1 `describe` + 8 `it` |
| Persisted record families or keys | 0 |
| Acceptance cases | exactly 8 |
| Shared branch ref moves | exactly 1, sealed parent → green bank child |

The walker may give its single corpus-building `beforeAll` the same explicit `900_000 ms` local
hook budget as the existing AO-0 consumer, whose recorded contended build cost required it. This
is not authority to edit global test timeout/configuration, add a timeout path, or raise any floor
or ceiling. Build once; a second expensive corpus build is forbidden.

## 5. Hazard, pre-mortem, mutation, and census obligations

Add one ten-key `HZ-CROSSHOME` registry class with status `MACHINERY`, `acceptedReason: null`,
and enforcer paths consisting of the contract walker, helper, and baseline. `inChain: true` is a
derived claim and must agree with `validate:hazard-registry`; do not move `classFloor`,
`machineryFloor`, `documentBaseline`, or `owedBaseline`.

Its instance evidence names the executed §1 denominator and the mechanism: one curation seam
placed the same prospective string under queued and applied homes while correcting only the
headline. Its triggers cover edits to a multi-home prose router, the scalar home/address fields,
or the contract baseline/helper.

Add one `PREDICATES` row in `scripts/lib/premortem-triggers.mjs`, classed only to
`HZ-CROSSHOME`. Derive the protected substrate from the baseline/contract artifact rather than
duplicating the seven addresses. It must have a non-empty population and a synthetic changeset
that fires its own message under `npm run validate:premortem`. The instrument stays advisory.

Add exactly one `scripts/mutation-coverage-manifest.json` row for the new walker. Use
`kind: "rationale"`: its eight ordinary tests inject the four baseline polarities, malformed and
empty corpora, duplicate-preserving reconstruction, and the pre-/post-cure result. Do not add a
standing `mutation-sweep.sh` plant; AO-6 owns that battery.

Re-derive the lighting census whole in the contract commit. The expected isolated movement from
`2409/365/2044/19960/5635` is one file, one credited file, eight test titles, and one suite title:
`2410/365/2045/19968/5636`. Never forward-add if the executed walker reports something else.

## 6. Exact eight-path manifest

| Commit | Action | Path | Symbol / region | Max delta | Instruction |
|---:|---|---|---|---:|---|
| 1 | CREATE | `scripts/lib/news-voice-contract.mjs` | reconstruction, classification, envelope validator, comparator exports | 250 effective | Pure infrastructure; no product-producer import. |
| 1 | CREATE | `tests/lint/newsVoiceContract.walker.test.js` | one `describe`, eight `it`, one corpus `beforeAll` | 8 tests | Seven phase-stable controls plus the one ordered completion identity in §3.3; no second cure assertion. |
| 1, 3 | CREATE, then MODIFY | `tests/lint/.news-voice-baseline.json` | immutable retro denominator + bankable `outstanding` | one baseline | C1 freezes seven debt rows; C3 banks only current debt to zero. |
| 1 | MODIFY | `scripts/hazard-registry.json` | new `HZ-CROSSHOME` class | one class | Add MACHINERY; no floor/baseline movement. |
| 1 | MODIFY | `scripts/lib/premortem-triggers.mjs` | one `PREDICATES` row | one predicate | Add its derived population and firing synthetic. |
| 1 | MODIFY | `scripts/mutation-coverage-manifest.json` | `newsVoiceContract.walker.test.js` | one row | One rationale row; surgical raw-text edit, no global reserialization. |
| 1 | MODIFY | `tests/lint/sovereigntyLightingContract.walker.test.js` | `CENSUS` + dated cause | five values | Whole five-tuple re-derive. |
| 2 | MODIFY | `src/domain/worldPulse/worldPulseFeedCuration.js` | `newsEntryForOutcome` | 15 effective | Exact seven-address applied summary projection only; zero reasons/headline/outcome/non-summary-field movement. |

Commit 1 changes exactly the first seven unique paths. Commit 2 changes only the eighth. Commit 3
changes only the baseline. There are exactly eight unique paths across the train. No generated
artifact is committed.

## 7. Preflight and focused checks

Before any edit, prove HEAD/branch, exact target cleanliness, file absence for all three CREATE
rows, AO-0 scalar topology, 272/53 reconstruction inputs, §1's seven-row denominator, the 25-row
headline rewrite table, and the live census `2409/365/2044/19960/5635`.

Run focused Vitest only through the mutex:

```sh
npm run validate:packets
npm run validate:hazard-registry
npm run validate:premortem
sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/lint/newsVoiceContract.walker.test.js \
  tests/domain/worldPulseNewsCuration.test.js \
  tests/lint/hazardRegistryFailClosed.test.js \
  tests/lint/mutationCoverageManifest.test.js \
  tests/lint/sovereigntyLightingContract.walker.test.js
npx eslint \
  scripts/lib/news-voice-contract.mjs \
  scripts/lib/premortem-triggers.mjs \
  src/domain/worldPulse/worldPulseFeedCuration.js \
  tests/lint/newsVoiceContract.walker.test.js \
  tests/lint/sovereigntyLightingContract.walker.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict
npm run check:observed-shape-readers
npm run check:tail
```

`npm run check:tail` is always bare, never mutex-wrapped. At commit 1 and commit 2 use detached
proof worktrees and capture the exact deliberate focused/full-gate red set; only the final bank
child must run the entire list at exit zero.

## 8. Atomic three-commit lifecycle

1. Dispatch and seal at a clean green descendant whose eight substrate blobs match this base.
2. Build private-index **contract commit C1** with exactly seven paths. In a detached worktree,
   re-execute the scalar corpus and prove exactly one deliberate red identity: the ordered
   completion test passes debt comparison and pre-cure production alignment, then fails only its
   final exact-zero assertion at `48 distinct / 53 occurrences`; every other test is green.
3. Build direct **cure commit C2** changing only `worldPulseFeedCuration.js`. Detached proof must
   show the same completion test identity fails first at its comparison, naming all seven frozen
   outstanding identities as vanished/unbanked wins. It must not reach alignment/zero assertions;
   any second failing identity or other red stops.
4. Build direct **bank commit C3** changing only `.news-voice-baseline.json`; preserve its corpus
   and retro-detection sections byte-for-byte and set current outstanding debt to zero.
5. In a fresh detached C3 worktree run all focused checks, both typecheck ratchets, plain OSR, and
   bare full gate. Every command exits zero; census is the executed whole tuple.
6. Copy the exact eight path bytes into the shared worktree and move the shared branch once, by
   old-value CAS from the sealed green parent directly to C3. Never squash or expose C1/C2 as tip.
7. If HEAD moved, a target blob changed, or CAS fails, discard the train and recompile against the
   actual parent. After success, neutralize only these eight stale shared-index paths.

## 9. Acceptance matrix

| ID | Required observation |
|---|---|
| A1 | One AO-0 scalar build feeds two pure reconstructions that are byte-identical and close at 272 introductions - 32 cap removals = 240 final rows / 53 homes; the seven debt rows equal §1 at 48 distinct / 53 occurrences. |
| A2 | The same single ordered completion `it` owns every intermediate red: C1 passes live/current comparison and pre-cure alignment then fails only final zero; malformed/empty corpus and malformed baseline fixtures fail closed in phase-stable fixture tests. |
| A3 | C2 changes only the exact seven applied summary addresses from `can advance through` to `advances through`; paired proposal rows, reasons, headlines, underlying outcome/proposal inputs, keys, order, IDs, and every non-summary feed field remain unchanged, while the changed summary is intentionally retained in persisted Wizard News. |
| A4 | Multiset snapshot differencing preserves the five duplicate occurrences and assigns every positive delta's lowest current indexes deterministically; changing reasons changes entry identity but reasons are never voice-classified or rewritten. |
| A5 | Canonical row arrays reject duplicate identities and new/grown/shrunk/vanished fixture debt by identity/count; C2's same completion `it` fails first on exactly seven vanished rows, and C3's zero-array bank passes without changing corpus or retro evidence. |
| A6 | All seven owned addresses are closed and indicative after C2; an unowned home and an applied nonmatching summary pass through, while blank/malformed values at owned addresses fail closed. |
| A7 | `HZ-CROSSHOME` validates as MACHINERY with existing in-chain enforcers; its derived nonempty pre-mortem population and synthetic fire; the mutation rationale and whole census join are valid. |
| A8 | Exact 7-path C1 → 1-path C2 → 1-path C3 lineage passes focused checks and the bare full gate at C3 before one old-value CAS; no golden, OSR inventory, default corpus, flag, tuning, dependency, ceiling, or ninth path moves. |

## 10. Mandatory STOPs

Stop on any denominator other than 272 introductions / 53 homes / 48 distinct / 53 occurrences
or any per-address split unlike §1; failure to close 272 introductions - 32 removals = 240 final;
inability to reconstruct without volatile ID/time fields;
classification or rewriting of `reasons`; queued-summary, headline, producer, persistence, state,
ordering, ID, significance, suppression, unrelated-home movement, or a generic/non-address-gated
summary rewrite; more or fewer than eight test titles or one suite title; a second scalar corpus
build; a hook budget other than the explicit local 900,000 ms or any global timeout/config edit;
any census delta not wholly explained by the one new test file;
an HZ floor/baseline move; a pre-mortem predicate that is empty, authored when derivable, or does
not fire synthetically; mutation-manifest global reserialization; a golden or OSR/default-corpus
movement; C1 reds beyond the outstanding-debt test; C2 reds beyond the seven vanished identities;
different C1/C2 failing test identities, a second intermediate failing test, or wrong assertion
order; any C3 red; a path outside the exact eight; a commit with the wrong per-commit path set; shared ref
movement before proof; CAS failure; or final full-gate exit nonzero. Do not rebaseline, widen the
contract, add a skip, tune prose, or repair an adjacent defect.

## 11. Executed landing receipt — 2026-08-13

- **Authority and immutable train:** dispatch HEAD
  `c5fd3638586c39d50a10f4cf64b7fa1408fbaaf1` was sealed with digest
  `0c54ad5d89522eb9ff8b7081e2cf47190e6866b7e5574ec22cd74a562d8b56ed`.
  The exact direct-parent lineage is contract C1
  `c825af928729f0738f6fe97cc1246d2558fe5627` → cure C2
  `d4f213d02fa2bc0e11c71245ad8d5cfb30468837` → zero-bank C3
  `f0c272e894f3f4d4edbd7fd1dac4c65580f87e52`. C1 changed exactly the seven
  contract paths, C2 changed only `worldPulseFeedCuration.js`, and C3 changed
  only `.news-voice-baseline.json`; one old-value CAS exposed only green C3.
- **Ordered red proof:** C1 produced the required **7/8** result: the sole failing
  completion identity reached only its final-zero assertion with the exact seven
  rows / 48 distinct values / 53 occurrences still outstanding. C2 again produced
  **7/8** with the same sole failing identity, now failing first on exactly the seven
  vanished, unbanked identities. No second test identity failed at either ancestor.
- **Green closure:** after C3 banked only current outstanding debt, the focused
  proof passed **97/97**. The hazard registry validated at **29** classes,
  **M12/P11/D6**, and the pre-mortem registry validated at **28** predicates.
- **Estate receipts:** both typecheck ratchets passed at **173/173** and
  **1,134/1,134**; plain observed-shape validation stayed green at **1,998** reads;
  and the lighting census re-derived whole at
  **2410/365/2045/19968/5636**.
- **Full landing gate:** bare `npm run check:tail` ran to exit 0. Strict dist
  discovered and reported the same **50** files and passed **403/403** tests.
  No golden, default corpus, observed-shape artifact, flag, tuning, dependency,
  ceiling, persisted shape, unrelated baseline, or ninth implementation path moved.
  Deviations: **NONE**. Judgment calls: **NONE**.
