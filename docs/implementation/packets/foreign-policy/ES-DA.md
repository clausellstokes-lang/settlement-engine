# Espionage / ES-Da — the composite rider: covert cargo on an already-accepted peace envoy

- **Status:** LANDED
- **Landed:** at `6dbb76c2`, 2026-08-12; flipped by the chair after verification (12 files by diff-tree, dark-path byte-identity proven BY EXECUTION against the pre-feature commit with the hash frozen as PRE_ESDA_DARK_LEDGER_HASH, census folded whole, gate TRUE_EXIT=0 at HEAD). Do not redispatch. CR-ESDA-6 (letter-wave id shape) and CR-ESDA-7 (the JSDoc cure) executed; two further ratified deviations: the barrel re-export and the ES-5 orphan-pin narrowing (the lesson: a "fully orphaned" file's emptiness is pinned somewhere — grep before wiring).
- **Status note:** ✅ **PROMOTED READY 2026-08-11 by the Fable chair (session `c42c8924`,
  Lane U), compiled from Lane Q's read-only draft and the five rulings CR-ESDA-1..5 (§12).**
  ⭐ **ES-Da is the ONLY dispatchable espionage slice at this HEAD.** ES-7 was REFUSED (five of
  six Herald kinds have no reachable producer, because the dispatcher was never chartered) and
  ES-D was REFUSED after it (`envoyErrandRecords.js` forces a covert row's endpoints to the
  peace offer's own, so no autonomous per-tick stage can mint a covert mission). **ES-Da is the
  argued split, not a re-opening of either refusal.**
  ⭐ **Lane Q's one sequencing blocker (its D7) is DISCHARGED:** the lighting-census walker was
  TC-5b-i's reserved path, TC-5b-i **LANDED at `9183d52c`**, and a terminal packet reserves
  nothing (`scripts/implementation-packets.mjs:43`). The census obligation is now sequenced by
  the chair's census-holder rule rather than blocked (§7b).
- **Packet version:** `1`
- **Drafted by:** Lane Q (read-only compile lane), 2026-08-11, Opus-era — see §12c.
  Compiled to packet standard and promoted by Lane U under the Fable chair, 2026-08-11.
- **Verified base:** `claude/composite-r4` at `33487c77b0d9290db157f08330840558e17902bf`
- **Base note:** CONFIRMED at `33487c77` — *"The observed-shape baseline shrinks by exactly the
  two rows the repair retired"*, the fully-green-gate commit. Lane Q measured at `63c62822`;
  five commits have landed since (`9183d52c`, `e7774ff2`, `da31d170`, `b0912f7f`, `33487c77`).
  ⛔ **The base is restamped here and every figure Lane Q quoted is re-derived at preflight
  rather than inherited** — in particular the `envoyDiplomacy.js` effective-line count (§5b B3)
  and the lighting-census row, which `da31d170` re-recorded (§5b B4).
- **⚠ The tree is a LIVE SHARED WORKTREE.** At this promotion a sibling lane holds the OSR
  instrument files (`scripts/check-observed-shape-readers.mjs`,
  `scripts/lib/observed-shape-baseline.mjs`, `scripts/migrate-observed-shape-readers.mjs`,
  `scripts/.observed-shape-readers-baseline.json`). **CONFIRMED disjoint from all seven paths in
  §7**, and all four are forbidden edits here regardless. ⛔ Reserved, never staged, never
  reverted, never attributed. The preflight re-measures.
- **Authority for the behavior:** `docs/OWNER_DECISION_QUEUE.md` §17 disposition 5 — *"§16 ES-Da
  — AUTHORIZED UNDER THE GRANT, with conditions"* — under the owner's 2026-08-11 refreshed
  grant; §16 on the same branch is the refusal record that defines the slice. ⚠⚠ **BOTH LIVE ON
  THE LEDGER BRANCH `review-fixes-2026-07-08` AND ARE NOT PRESENT ON THIS BUILD BRANCH.** A
  build-branch reader cannot open them; that is expected, and it is why the conditions are
  restated in full at §12a rather than cited.
- **Depends on:** ES-1's covert door, ES-2/ES-3's gauntlet and product stages, ES-5a's doctrine
  leaf, and the errand spine — **all landed and all dark**. ⭐ **TC-5b-i at `9183d52c` is a
  RESERVATION dependency only**; there is no code dependency between the lanes.
- **Collision group:** `espionage-and-envoy-errand` — serialize against any lane touching
  `envoyDiplomacy.js`, the errand mint family, or `src/domain/worldPulse/espionage/`.
- ⚠⚠ **CENSUS-HOLDER RULE (chair, 2026-08-11).** `GR-4a`, `SCW-0` and `TC-5b-ii` were promoted
  READY in the same documentation change, and all four fold the one estate-wide lighting census.
  **Only ONE of the four may have an implementer in flight at a time; the chair sequences.**
  ⛔ **GR-4a — not this packet — holds
  `tests/lint/sovereigntyLightingContract.walker.test.js` as a manifest reservation at
  promotion.** The chair MOVES that row into ES-Da's manifest, as a one-line edit, when it
  dispatches ES-Da. **Until it does, touching the walker is out of manifest and is a STOP**
  (§7b, §7c-2).
- **Commit authority:** to be stated by the chair in the dispatch message. Absent explicit
  authority, the coding agent leaves its changes unstaged and uncommitted.

---

## 0. Outcome first — what lands, and what the measurement says the prose does not

**One pure leaf that answers a single question** — *does this court slip a watcher into the
embassy it is already sending, and what is it sending him to settle?* — **and one composition at
the existing accepted-peace dispatch**, so a lawful `covert` sub-record rides the errand row the
war lane was already about to mint.

**Nothing new travels. Nothing new is scheduled. No stage is added.** That is the whole point of
the composite-rider shape: the traveller, the route, the speed, the concurrency cap, the transit
refusals and the persistence are all the peace embassy's, unchanged.

**The §16 sizing is CONFIRMED accurate, with one correction and one addition:**

- **CONFIRMED — one new leaf**, under `src/domain/worldPulse/espionage/`.
- **CONFIRMED — ≤15 effective lines into the diplomacy file.** The composition point is
  `envoyDiplomacy.js#dispatchAcceptedPeaceEnvoy`; the edit is one import, one call, and one
  conditional spread into an argument list that already exists. **Lane Q measured
  `envoyDiplomacy.js` at 753 effective lines against its 800 layer ceiling — 47 lines of
  headroom — and it is NOT in `scripts/.size-baseline.json`, so no frozen number governs it.**
  Measured with eslint's own `Linter` under `max-lines{skipBlankLines,skipComments}`, the same
  engine `tests/lint/sizeBaseline.test.js` uses.
- **CONFIRMED — zero new tuning values are ACHIEVABLE**, under CR-ESDA-4. Every threshold, band
  and weight the rider needs already exists in `ESPIONAGE_DOCTRINE_TUNING`, `DISPATCH_TUNING`
  and `ESPIONAGE_TUNING`.
- ⚠ **CORRECTION to the §16 sizing — it is one leaf plus FOUR registration/pin obligations, not
  one leaf alone.** Three exact-equality pins and one certification row go stale the moment the
  rider lands, and each is a **same-commit** obligation rather than follow-up work: the `gated`
  census in `tests/property/espionageProductsDormancyFence.test.js`, the espionage certification
  row's prose in `subsystemRowsVirtual.js`, a coupling row in `couplingRegistryEspionage.js`,
  and the estate-wide lighting census. None is large; all are mandatory. **§16's "one new leaf,
  ≤15 lines" measured the BEHAVIOR correctly and under-counted the registration surface.**
- ⭐ **ADDITION the §16 record did not have.** ES-Da is not only the first covert-row producer —
  it is the **first production caller of `espionageDoctrineStage.js`, today a fully orphaned
  file**. The rider lights ES-5a's doctrine half by being its caller: a second landed-dark wave
  reached at zero extra cost.

**The volume's prose has been wrong five compiles running, so every claim below is labelled
CONFIRMED (executed read at a named sha) or PLAUSIBLE (reasoning only).** Three of §16's own
sentences are re-measured at §11 and two of them needed narrowing.

---

## 1. Reconciled authority

Reconciled per `PACKET_STANDARD.md` §"Authority order".

1. **Live git state at `33487c77` decides what exists.** Where design prose and code disagree,
   **the code wins and §11 says so.**
2. **The newest owner ruling** — the 2026-08-11 refreshed grant, chair disposition §17.5.
   Product behavior authorized: *an accepted peace envoy can carry a spy*, **built DARK**, with
   the behavior manifesting only when flags light at the endgame.
3. **The chair's rulings CR-ESDA-1..5 (§12a) are BINDING**, together with the serialization law
   ruled at `73f5be96`.
4. **`CONTRIBUTING.md` / `ARCHITECTURE.md` / the certification contracts** supply the
   invariants: the CQ5 virtual-flag law, the one-writer-per-state law (R-BLD-4), J-SP-2 (exactly
   one purposeful-travel substrate), the SP-D one-reader law, and the coupling same-commit
   obligation.
5. **`DESIGN_FP_ARCH_ES.md`** supplies intent after reconciliation — §1 (the canonical model),
   §3.4 (the composite mission), §3.5 (the tainted journey), §3.9 (the doctrine read), §3.12
   (the dispatch cadence).
6. **`SOL_QUEUE.md` and `INDEX.md` never prove work is open.** ⚠ `docs/implementation/INDEX.md`
   at the draft's HEAD still recorded ES-Da as owner-gated and unreadable from the build branch.
   **That cell is corrected by the chair in the same documentation change that promotes this
   packet** — a coordinator act, not a change-manifest row (CR-ESDA-5, §7b).

**No unresolved higher-authority disagreement.** ES-7 remains refused; ES-D-as-a-whole remains
refused; this packet is the argued split.

---

## 2. Outcome and non-goals

### 2.1 Why a rider is not the ES-7 anti-pattern

ES-7 was refused because five of six Herald kinds had **no reachable producer** — consumers
built, producer never chartered. **ES-Da is the exact inverse: it builds the PRODUCER for
consumers that are already mounted on a live pulse.** `advanceEspionageGauntlet`
(`envoyPulse.js:276`) and `advanceEspionageProducts` (`envoyPulse.js:415`) are called every tick
today and walk an empty ledger. **This packet gives them their first row. It adds no consumer at
all.**

### 2.2 Non-goals — everything the ES-7 and ES-D refusals parked STAYS parked

Each is an explicit exclusion, and reaching any of them is a **STOP**:

- ⛔ **NO DISPATCHER.** No autonomous per-tick stage that decides a court wants a mission and
  sends somebody. The ES-7 refusal stands untouched.
- ⛔ **NO HERALD GROWTH.** Zero new Herald kinds, zero routing rows, zero news. The six kinds
  remain ES-7's, and five of them remain producerless. This packet mints **no receipt of its
  own** and names **no Herald kind** — the ES-5b/5c/5d/6a precedent, held.
- ⛔ **NO VETTING BAND.** `vetVolunteerEnvoy` is not called and no volunteer band is derived. The
  vetting band has zero producers and this packet does not become one. **ES-6b is NOT released
  by this landing.**
- ⛔ **NO MULTI-STOP ROUTES.** The itinerary is exactly ONE stop, and it is the embassy's own
  destination. There is no route producer for multi-stop covert itineraries and this packet does
  not build one.
- ⛔ **NO CASTING HELPER REVIVAL.** `castCovertOperative` is NOT called and stays dead. The site
  already casts by diplomatic law (`envoyCandidate`), and the whole rider premise is that **the
  SAME person carries the cargo.** A second cast would be a second traveller.
- ⛔ **`mintCovertMission` STAYS A DEAD END.** ES-Da does not call it and does not revive it. The
  row rides `mintEnvoyErrand` → `mintErrandSpine`, the one path that actually writes. **§11.2 —
  this is the single most important thing a coding agent could get wrong.**
- ⛔ **NO FREE-STANDING SPY ROW.** R-ES1-1's residual is untouched: a covert mission that becomes
  a row still rides a diplomatic errand wearing its face. ES-Da does not deepen that weld and
  does not resolve it.
- ⛔ **NO NEW LEDGER, NO NEW PERSISTED KEY.** The `covert` sub-record and every one of its seven
  `COVERT_KEYS` already exist and are already validated on both sides. **Zero new keys.**
- ⛔ **NO FLAG LIT.** `espionageEnabled` stays absent from every preset. Lighting is the
  endgame's, under the owner's walk and both soaks.

---

## 3. Hard scope budget

| Limit | Budget | This packet | |
|---|---:|---|---|
| Behavior families | 1 | 1 — covert cargo on an accepted peace envoy | ✓ |
| New persisted record families | ≤1 | **0** — the `covert` sub-record already exists | ✓ |
| Named writer per changed state | exactly 1 | 1 — `writeErrands`, via `mintEnvoyErrand`, **unchanged** | ✓ |
| Feature flags | ≤1 | **0 new** — reads the existing `espionageEnabled` through `espionageActive` | ✓ |
| User-facing surfaces | ≤1 | **0** | ✓ |
| Direct production consumers | ≤2 | 1 — `envoyDiplomacy.js#dispatchAcceptedPeaceEnvoy` | ✓ |
| New logic-bearing production leaves | ≤2 | 1 | ✓ |
| Existing logic-bearing production files modified | ≤3 | 1 — `envoyDiplomacy.js` | ✓ |
| Registration-only production files touched | ≤3 | 2 — `couplingRegistryEspionage.js`, `subsystemRowsVirtual.js` | ✓ |
| Handwritten files total | ≤12 | 7 reserved + 1 sequenced census row (§7) | ✓ |
| New/changed effective production lines | ≤400 | ≈150 projected | ✓ |
| Each new leaf | ≤250 effective | ≈110 projected | ✓ |
| **Each shared/hot-file delta** | **≤15 effective** | **≤15 into `envoyDiplomacy.js` — the §16 sizing, and the binding constraint** | ⚠ |
| Named acceptance cases | ≤8 | 8 (§9) | ⚠ at cap |

⛔ **If the `envoyDiplomacy.js` delta cannot be held at 15 effective lines, the agent STOPS** and
proposes the smallest split. **It must not renegotiate this number: it is the number the
owner-facing record sized the slice at.**

---

## 4. Verified tree contract

- **State authority:** `worldState.envoyErrands` (key `ENVOY_ERRAND_LEDGER_KEY`). **Unchanged.**
- **Sole writer:** `envoyErrandLedger.js#writeErrands`, reached through
  `envoyErrand.js#mintEnvoyErrand`. **ES-Da adds no writer.**
- **The field this packet causes to be written:** the conditional `covert` sub-record on the
  errand row, plus the conditional `purposeClass` word. Both are constructed by the ONE
  constructor, `envoyErrandRecords.js#errandSpineBlock`, exactly as today.
- **Direct readers that begin to fire:** `espionageGauntlet.js#covertDwellRead` and
  `espionageProductStage.js#advanceEspionageProducts` — both already mounted, both already
  walking the ledger every tick. §4b.
- **Normalizer / absence rule:** `normalizeCovertMission` — malformed cargo **HEALS TO ABSENT**
  on the persist side and is **REFUSED BY NAME** at the mint. Unchanged.
- **Lifecycle seams:** persistence, reload, regeneration, undo, import and migration all run
  through `normalizeErrand`, which already validates `covert` on every path. **ES-Da opens no new
  lifecycle path — which is why it needs no migration.**
- **Receipt / audience projection:** **NONE.** This packet mints no receipt, no news, no Herald
  kind and no home-visible field.
- **Tests whose shape to copy:** `tests/property/espionageMissionDormancyFence.test.js` (the
  three-door dormancy shape), `tests/domain/espionageMission.test.js` (the mint-refusal battery).

**Live symbols this packet consumes — re-verify each BY SYMBOL, never by line number:**

| Symbol | Home | What it must still do |
|---|---|---|
| `dispatchAcceptedPeaceEnvoy` | `src/domain/worldPulse/envoyDiplomacy.js` | convert an accepted decision into a row; call `mintEnvoyErrand` exactly once |
| `mintEnvoyErrand` | `src/domain/worldPulse/envoyErrand.js` | still accept `purposeClass` and `covert` params and forward them **unread** to `mintErrandSpine` |
| `mintErrandSpine` | `src/domain/worldPulse/errandMint.js` | still return `{ok, fields, plan, reason}`; still return `fields:{}` when `errandSpineActive` is false |
| `covertMissionRefusal` | `src/domain/worldPulse/errandMint.js` | still name each refusal reason |
| `normalizeCovertMission` | `src/domain/worldPulse/envoyErrandRecords.js` | still total-on-garbage; still the ONE covert validation |
| `errandSpineBlock` | `src/domain/worldPulse/envoyErrandRecords.js` | still write `covert` only when the RESOLVED class is covert |
| `espionageActive` | `src/domain/worldPulse/espionage/espionageGate.js` | still the three-part conjunction, still the ONLY by-name read of the flag |
| `espionageDoctrineFor` | `src/domain/worldPulse/espionage/espionageDoctrineStage.js` | still return `null` when dark and a `known:false` doctrine when unreadable |
| `dispatchDemandFor` | `src/domain/worldPulse/espionage/espionageDoctrineStage.js` | still return a member of `DEMAND_BANDS` |
| `hash01` | `src/domain/region/contestMath.js` | still the estate's keyed hash (L1 — no new PRNG stream) |
| `covertDwellRead` | `src/domain/worldPulse/espionage/espionageGauntlet.js` | still admit `travelling`/`parlaying` and test the SUB-RECORD, not the class word |
| `COVERT_KEYS`, `ENVOY_COVERT_PRODUCTS` / `_DEMANDS` / `_FACES`, `MAX_COVERT_ITINERARY_STOPS`, `COVERT_STOP_KEYS` | `src/domain/worldPulse/envoyErrandVocabulary.js` | the closed vocabulary the rider must satisfy |

**Forbidden alternative homes:** a new `*Errand.js` module (that is the second substrate J-SP-2
forbids); a private covert-travel path; a second gate on `espionageEnabled`; any new file under
`src/domain/worldPulse/` outside `espionage/`.

**Forbidden files:** `scripts/.size-baseline.json`, `eslint.config.js`,
`src/domain/worldPulse/pulseKernel.js`, `src/domain/worldPulse/applyWorldPulse.js`,
`src/domain/worldPulse/envoyPulse.js`, `src/domain/worldPulse/espionage/espionageMissions.js`,
`src/domain/worldPulse/envoyErrandVocabulary.js`, `src/domain/worldPulse/errandMint.js`,
`src/domain/worldPulse/envoyErrandRecords.js`, `scripts/check-observed-shape-readers.mjs`,
`scripts/lib/observed-shape-*`, `scripts/.observed-shape-readers-baseline.json`,
`docs/implementation/INDEX.md` and `docs/implementation/PACKET_MANIFEST.json` (the last two are
RESERVED by IA-2 — §7b), and every file outside §7.

### 4b. THE CONSUMER-REACHABILITY MAP — what actually begins to fire, and from what state

§16 said ES-Da *"lights five landed-dark waves at once"*. **Re-measured, that is close but not
exact, and the correction matters to the owner-facing claim.** Measured across all 14 espionage
files and their importers:

| Wave / entry point | Mounted? | What ES-Da changes | Label |
|---|---|---|---|
| **ES-5a** `espionageDoctrineStage.js#espionageDoctrineFor`, `dispatchDemandFor` | **NO — fully orphaned file** | ES-Da becomes its **FIRST production caller**. The whole doctrine half wakes. | CONFIRMED |
| **ES-5a** `espionageWariness.js#overdueForeignNotables` | via gauntlet | wired but **unreachable** — its caller only runs per row clearing `covertDwellRead`. ES-Da supplies the first such row. | CONFIRMED |
| **ES-2/ES-3** `advanceEspionageGauntlet` (`envoyPulse.js:276`) | YES, every tick | walks an empty ledger today; begins detecting on the embassy standing at its destination. Writes nothing (`custodyWritten:false`). | CONFIRMED |
| **ES-3** `advanceEspionageProducts` (`envoyPulse.js:415`) | YES, every tick | begins accruing `gathered` onto the row (through `writeErrands`) and landing belief records (through `reconcileBelief`). | CONFIRMED |
| **ES-5d** `depositMissionCredits` → `readMissionCreditEvents` (`npcLadderKernel.js:469`) | YES, every tick | reader returns an empty Map today; begins returning real credits once a landing grades above `empty`. Gated additionally on `npcLadderEnabled`. | CONFIRMED |
| **ES-6a** `deliverMissionLeaks` (`espionageProductStage.js:760`) | YES, every tick | walks an empty `landings` today; begins walking real ones. Gated additionally on `corruptionWebActive` and a foreign leash. | CONFIRMED |
| **ES-5b** `presenceSharesFor` (`settlementPolitics.js:565`, `factionCompetition.js:202`) | YES, every tick | **NOTHING.** Reads `factionStates` + roster `whereabouts`. **Not covert-gated at all** — it lights on `espionageEnabled` alone, with or without ES-Da. | CONFIRMED |
| **ES-5c** `careerRiskFor` (`npcLadderChallenge.js:330`) | YES, every tick | **NOTHING.** Reads `npc.whereabouts` + `npcStates`. **Not covert-gated at all** — an ordinary peace errand already trips it. | CONFIRMED |

⭐ **The corrected owner-facing sentence: ES-Da reaches ES-5a (both halves), ES-2, ES-3, ES-5d
and ES-6a. It does NOT unlock ES-5b or ES-5c** — those two were never covert-gated and will move
bloc math and ladder defense scores the moment `espionageEnabled` lights whether or not ES-Da
exists. §11.1 records this as a correction to §16's own wording, and it slightly **REDUCES** the
scope of what the owner is authorizing at the endgame lighting.

**How the traffic reaches the consumers — the exact state.** `covertDwellRead` admits a row whose
`state` is `travelling` or `parlaying`, whose scheduled cursor reads `progressBand === 'arrived'`,
and whose arrival settlement is **NAMED IN THE ITINERARY**. An accepted peace envoy, on completing
its outbound route, is transitioned by `advanceEnvoyErrands` to `parlaying` with the final
outbound cursor at `to`. ⇒ **An itinerary naming `to` is exactly what makes the embassy a dwelling
covert traveller** — no new travel, no new schedule, one stop the embassy was already going to
make. CONFIRMED by reading `envoyErrand.js#advanceEnvoyErrands` and
`espionageGauntlet.js#covertDwellRead`.

---

## 5. Preflight

Run before the first edit. **Every check is a STOP on failure.**

```sh
git rev-parse --abbrev-ref HEAD          # expect: claude/composite-r4
git rev-parse HEAD
git merge-base --is-ancestor 33487c77 HEAD && echo ANCESTRY_OK

# ⛔ THE CENSUS-HOLDER GATE. The chair must have MOVED the walker row into ES-Da's
#    PACKET_MANIFEST.json entry before dispatch; GR-4a holds it at promotion.
git status --porcelain tests/lint/sovereigntyLightingContract.walker.test.js   # expect EMPTY

# Target files clean. ⚠ A sibling lane's OSR-instrument dirt is FOREIGN and RESERVED —
# never stage it, never revert it, never attribute it.
git status --porcelain -- \
  src/domain/worldPulse/envoyDiplomacy.js \
  src/domain/certification/couplingRegistryEspionage.js \
  src/domain/certification/subsystemRowsVirtual.js \
  tests/property/espionageProductsDormancyFence.test.js
# expect: EMPTY. Anything here is a STOP.

# New files absent.
test ! -e src/domain/worldPulse/espionage/espionageRider.js && echo NEW_LEAF_ABSENT
test ! -e tests/domain/espionageRider.test.js
test ! -e tests/property/espionageRiderDormancyFence.test.js

# Live symbols — by symbol, never by line number (the §4 table is the contract).
rg -n 'dispatchAcceptedPeaceEnvoy' src/domain/worldPulse/envoyDiplomacy.js
rg -n 'export function mintEnvoyErrand' src/domain/worldPulse/envoyErrand.js
rg -n 'mintErrandSpine|covertMissionRefusal' src/domain/worldPulse/errandMint.js
rg -n 'normalizeCovertMission|errandSpineBlock' src/domain/worldPulse/envoyErrandRecords.js
rg -n 'export function espionageActive' src/domain/worldPulse/espionage/espionageGate.js
rg -n 'espionageDoctrineFor|dispatchDemandFor' src/domain/worldPulse/espionage/espionageDoctrineStage.js
rg -n 'covertDwellRead' src/domain/worldPulse/espionage/espionageGauntlet.js
rg -n 'COVERT_KEYS|MAX_COVERT_ITINERARY_STOPS' src/domain/worldPulse/envoyErrandVocabulary.js
rg -n 'hash01' src/domain/region/contestMath.js
```

### 5b. Baselines to capture BEFORE the first edit

| # | Premise | Exact command | Expected / recorded |
|---|---|---|---|
| B1 | ⭐ **THE DARK-PATH GOLDEN — the packet's central receipt; capture it FIRST** | `sh scripts/gate-mutex.sh --run -- npx vitest run tests/property/espionageProductsDormancyFence.test.js tests/property/espionageDormancyFence.test.js tests/property/errandSpineDormancyFence.test.js tests/domain/envoyErrand.test.js tests/domain/envoyErrandSpineLifecycle.test.js` | **UNMEASURED.** Record pass count **and the exact test titles**. This is the base to diff against |
| B2 | The walkers ES-Da can break — **all EXACT-EQUALITY pins** | `sh scripts/gate-mutex.sh --run -- npx vitest run tests/lint/errandConsumerRegistry.walker.test.js tests/lint/couplingInclusion.walker.test.js tests/lint/sizeBaseline.test.js tests/domain/subsystemRowsVirtual.test.js` | **UNMEASURED** — exit 0 |
| B3 | ⛔ `envoyDiplomacy.js` effective lines, BEFORE the edit | eslint's own `Linter` under `max-lines{skipBlankLines,skipComments}` | **MEASURED BY LANE Q AT `63c62822`: 753 against a ceiling of 800 — 47 lines of headroom, unbaselined.** ⛔ **Re-measure at preflight; never `wc -l`** |
| B4 | ⭐ Lighting census row | read `tests/lint/sovereigntyLightingContract.walker.test.js` at a **committed** sha | **MEASURED BY READ at the verified base `33487c77`, line `:3731`: `files: 2397, parked: 365, credited: 2032, titles: 19763, suiteTitles: 5578`.** ⛔ A named-committed-sha snapshot per serialization-law rule 4 — **re-derive from the FILE at preflight; never fold onto this row** |
| B5 | Test-ratchet headroom | read `scripts/.test-ratchet-baseline.json` + `tests/lint/testRatchet.test.js:177` | **MEASURED BY READ: 17 entries against `const CEILING = 17` — ZERO headroom.** Land with no new baselined failure |
| B6 | Typecheck posture | `npm run typecheck:ratchet` then `npm run typecheck:domain:strict` | **UNMEASURED** — exit 0 both, named separately with their windows |
| B7 | Pre-existing gate reds | committed-base run, capturing `$?` yourself | **UNMEASURED.** ⚠ The **only** legitimate pre-existing red to attribute is the owner-approved `generatorGoldenMaster` golden |

### 5c. Corrections carried forward from Lane Q — do not re-discover these

Four measurements cost real time to establish. They are recorded so the compile does not repeat
them:

1. ⭐ **`envoyDiplomacy.js` has 47 lines of headroom, not zero.** The *"both pulse mouths sit at
   EXACT zero headroom"* finding is CONFIRMED but applies to `applyWorldPulse.js` (941 measured
   / 941 frozen) and `pulseKernel.js` (1580 / 1580) — **not** to the composition point.
   **ES-Da touches neither pulse mouth** (§11.4).
2. **The one-reader law's escape is razor-thin and was verified empirically.** §6.3 — and it is
   the subject of CR-ESDA-2.
3. **`deliberationRead` is NOT a dead arm here, but it asks the wrong question.** §11.5 / §12b D1.
4. ⚠ **The main tree `/Users/cstokes/Desktop/settlement-engine` is NOT a usable source** — it has
   all of `docs/implementation/` deleted in its working tree. **Read everything from the
   worktree.**

---

## 6. Exact contracts

### 6.1 The new leaf — `src/domain/worldPulse/espionage/espionageRider.js`

**Purpose.** Decide whether the embassy this court is already sending also carries a watcher, and
compose the lawful `covert` sub-record if it does. **Nothing else.**

**Export:**

```
covertRiderFor({ worldState, item, fromId, toId, errandId, tick }) -> {
  covert: Record<string, unknown> | null,
  reason: string,
}
```

**Contract, door by door — each door its own return with its own reason, because a conjunction
whose arms cannot be dropped one at a time is a conjunction nobody has proven:**

1. **DARK ⇒ `{covert: null, reason: 'dark'}`.** `espionageActive(worldState) !== true` is read
   **FIRST, before any world object is touched.** ⭐ **This is the byte-identity arm and it is
   the packet's central invariant (CR-ESDA-1).** The leaf gates here and reads second, exactly as
   `espionageCareer.js`, `espionagePresence.js` and `espionageCareerCredit.js` do.
2. **INVALID ENDPOINTS ⇒ `reason: 'invalid_endpoints'`.** `fromId`, `toId` and `errandId` must all
   be non-empty and `fromId !== toId`.
3. **NO READABLE DOCTRINE ⇒ `reason: 'no_doctrine'`.** `espionageDoctrineFor({worldState, item,
   courtId: fromId})` returns `null` (dark, already covered) or a doctrine with `known !== true`.
   **A court whose axes did not resolve slips nobody aboard, and says so in the leaf's own word
   rather than defaulting to a temperament.**
4. **THE DRAW ⇒ `reason: 'cadence_declined'`** when `hash01(riderKey(errandId)) >=
   doctrine.frequency01`. Lawless-malicious courts slip a watcher aboard often; lawful-benevolent
   ones rarely. **Zero new tuning:** `frequency01` is the doctrine leaf's own composed weight over
   `ESPIONAGE_DOCTRINE_TUNING`.
5. **OTHERWISE ⇒ `{covert, reason: 'rides'}`** with the sub-record below.

**The keyed stream (L1 — zero new PRNG streams).** One spelling, in this leaf:

```
riderKey(errandId) -> `es.rider.${errandId}`
```

Keyed on the **ERRAND ID** rather than on `(courtId, tick)` because a court may dispatch two
episodes on one tick and they must not share a byte. `errandId` derives from the offer
(`envoyErrandIdForOffer`) and is stable across replays of the same world, so the draw is
deterministic and replay-identical. §12b D6 records why this deliberately does not reuse
`dispatchCadenceKey`.

**The composed sub-record — exactly the shape `normalizeCovertMission` accepts:**

```
{
  demand:     dispatchDemandFor({ frequency01: doctrine.frequency01, urgent: false }),
  itinerary:  [ { face: 'declared', settlementId: toId, stayTicks: 1 } ],
  product:    'confirm',
  subjectId:  toId,
}
```

Field by field, with the law each satisfies:

- **`demand`** — a member of `COVERT_DEMAND_SET`, produced by the ONE bar function so the
  mission's bar, the early-resolve threshold and the grade at close cannot disagree.
  `urgent: false` is passed **explicitly**: the decision this embassy carries is already MADE and
  accepted, so there is no siege-at-the-gate urgency to lower the bar. **Stated, never left to a
  default.**
- **`itinerary`** — EXACTLY ONE stop, the embassy's own destination. `face: 'declared'` because
  the stop IS the public itinerary — the embassy is openly going to `toId` and the cover story is
  true about *where* he goes and false only about *why* (§12b D4). **`stayTicks: 1` is DERIVED,
  not authored — CR-ESDA-4.**
- **`product: 'confirm'`** — a member of `COVERT_PRODUCT_SET`. `confirm` crosses no observed slot
  and needs no `legRefs`, which is what keeps this packet at zero new surface (§12b D3).
- **`subjectId: toId`** — the court being sued. ⭐ **This is the ES-D refusal's constraint turned
  into the product semantics rather than worked around:** `envoyErrandRecords.js` forces
  `payload.offererId === from && payload.targetId === to`, so a court can only spy on the court it
  is suing. **ES-Da does not fight that; it IS that.** An embassy suing for peace carries a man
  confirming what his court believes about the court across the table.
- **`legRefs`, `gathered`, `standoff`** — **ABSENT.** All three are conditional,
  drop-when-absent (T4: a key is a byte). `gathered` and `standoff` are the ES-3 amender's,
  written later by `advanceEspionageProducts`, **never at the mint**.

**Purity.** No `Date`, no `Math.random`, no store, no React, no I/O, no mutation. **The one
stochastic choice is a keyed hash.**

⚠⚠ **THE `.npcs` TRAP.** A new leaf under `src/domain/worldPulse/` must not contain the dotted
token `.npcs` **anywhere, including in a comment** — a raw grep with exact set equality convicts
even a header line. The rider has no reason to spell it; **if a comment wants to, reword the
comment, never widen the scan.**

### 6.2 The composition — `envoyDiplomacy.js#dispatchAcceptedPeaceEnvoy`

**Where.** Immediately before the existing `mintEnvoyErrand({...})` call, **after `graduated`
resolves**, so `errandId` and the endpoints are already in hand and nothing is recomputed.

**The edit, in full — this is the ≤15-line budget:**

1. one import of `covertRiderFor` from `./espionage/espionageRider.js`;
2. one call, passing values the function already holds — `worldState: graduated.worldState`,
   `item: fromItem` (already computed for `envoyCandidate`), `fromId`, `toId`, `errandId`
   (already computed at the `envoyErrandIdForOffer` line), `tick: atTick`;
3. one conditional spread into the existing `mintEnvoyErrand` argument list.

**The spread — and its exact spelling is load-bearing (§6.3):**

```
...(rider.covert ? { purposeClass: 'covert', covert: rider.covert } : {}),
```

**Absence semantics.** When the rider returns `covert: null` the spread contributes `{}`, so
`mintEnvoyErrand` destructures `purposeClass = null, covert = null` exactly as it does today.
**The argument object is byte-identical to the one built before this packet existed. The dark
path is byte-identical BY CONSTRUCTION, not by care** — which is the shape §17's mandatory
condition asks for, and it is CR-ESDA-1.

⛔ **No other change to this function.** The route plan, the pictures, the purpose selection, the
graduation, the NPC move and every refusal path are untouched.

### 6.3 ⚠⚠ THE ONE-READER LAW — the razor edge, verified empirically (CR-ESDA-2)

`tests/lint/errandConsumerRegistry.walker.test.js` reds any module in `src/` **outside the
five-file errand FAMILY** that spells `purposeClass`, `declaredPurpose` or `truePurpose` in any of
three forms. **`envoyDiplomacy.js` and the new rider leaf are BOTH outside the family.** Lane Q
drove the walker's own three regexes against candidate spellings:

| Spelling | `readsAField` | Verdict |
|---|---|---|
| `...(rider.covert ? { purposeClass: 'covert', covert: rider.covert } : {})` | **false** | ✅ **LAWFUL — use exactly this** |
| `mintEnvoyErrand({ ..., purposeClass: 'covert', covert: rider.covert })` | **false** | ✅ lawful |
| `{ purposeClass: written, covert }` | **true** | ⛔ **REDS THE WALKER** |
| `{ purposeClass, covert }` (shorthand) | **true** | ⛔ **REDS THE WALKER** |

**Why.** `PATTERN_ELEMENT_RE` matches an element that IS the field name, optionally renamed to an
**identifier** or defaulted. A **string-literal** value (`purposeClass: 'covert'`) fails the
identifier branch and fails the trailing anchor, so it does not match. **The escape is one
character wide.**

⛔⛔ **RULED (CR-ESDA-2) — THE INSTRUCTION IS EXACT AND NOT A PREFERENCE:**

> **The value of `purposeClass` MUST be the string literal `'covert'`. Never a variable, never
> shorthand, never a computed value — in either file. If the compile finds itself wanting
> `{ purposeClass }`, THAT IS A STOP, not a refactor.**

⭐ **And the belt-and-braces form is REQUIRED, not merely recommended: the RIDER LEAF must never
spell `purposeClass` at all.** The single string-literal spelling lives **only** at the
`envoyDiplomacy.js` call site, so the leaf cannot drift into the walker's jurisdiction later.

### 6.4 ⚠⚠ THE MINTER CENSUS — do not become a fourth minter

The same walker pins the minter set at **EXACT EQUALITY**:

```
expect(minters).toEqual([
  'src/domain/worldPulse/envoyErrand.js',
  'src/domain/worldPulse/espionage/espionageMissions.js',
  'src/domain/worldPulse/pactProposals.js',
]);
```

A module "mints" if it CALLS `mintErrandSpine` under **any** local name — the detector resolves
aliases, namespaces and re-export hops to a fixed point, so **renaming the import does not dodge
it.** ⛔ **Neither the rider leaf nor `envoyDiplomacy.js` may call `mintErrandSpine`, directly or
through `envoyErrand.js`'s re-export.** The rider composes CARGO; the war lane's existing
`mintEnvoyErrand` call does the minting, and `mintEnvoyErrand` is **not** `mintErrandSpine`.
⇒ **ES-Da needs NO new `ERRAND_CONSUMERS` row and must not add one** — adding one would red
DIRECTION 2 (`built:true` whose module does not mint).

### 6.5 The prevention guard — the dark-path byte-identity fence

One new property test, `tests/property/espionageRiderDormancyFence.test.js`, in the shape of
`espionageMissionDormancyFence.test.js`. It proves what CR-ESDA-1 demands, and it proves it
**three ways, because one way is not a proof**:

1. **THE BYTE-IDENTITY GOLDEN.** A real multi-tick run that dispatches an accepted peace envoy,
   advances it to its destination and closes it, executed in a DARK world and in a world with the
   rider module present. The resulting `worldState.envoyErrands` must be **byte-identical**,
   asserted by serialized comparison. Then the same run in **every non-true spelling of the
   flag** — absent, `false`, `0`, `'true'` — all one world.
2. **THE PER-DOOR CENSUS.** Each of the rider's five doors is driven ALONE and returns its own
   reason. **A conjunction nobody can drop an arm of is a conjunction nobody has proven.**
3. **THE LIT MUTANT — the fence must be able to SEE.** A LIT world with a drawing court must
   produce a row whose `covert` sub-record is present and lawful, and `covertDwellRead` must
   return `dwelling:true` for it at the destination. ⭐ **Without this half the fence would pass
   by measuring nothing — the recorded vacuity class.**

---

## 7. Exact change manifest

| # | Action | Path | Symbol / region | Max Δ | Instruction |
|---:|---|---|---|---:|---|
| 1 | `CREATE` | `src/domain/worldPulse/espionage/espionageRider.js` | `covertRiderFor`, `riderKey` | `110` eff. | The five-door leaf of §6.1. **Gates on `espionageActive` FIRST.** ⛔ No `.npcs` token anywhere, including comments. ⛔ Must never spell `purposeClass` (CR-ESDA-2). |
| 2 | `MODIFY` | `src/domain/worldPulse/envoyDiplomacy.js` | `dispatchAcceptedPeaceEnvoy` + one import | **`15`** eff. | One import, one call, one conditional spread — §6.2. ⛔ `purposeClass` value **must be the string literal** `'covert'`. |
| 3 | `REGISTER` | `src/domain/certification/couplingRegistryEspionage.js` | new `ESDA_COVERT_RIDER_COUPLING`, appended to `ES_ESPIONAGE_COUPLINGS` | `35` eff. | The same-commit coupling licence — §7a. ⛔ **`direction: 'INFO→GRAMMAR'`**, derived at §7a. |
| 4 | `REGISTER` | `src/domain/certification/subsystemRowsVirtual.js` | the `espionageEnabled` row | `10` eff. | Add the rider leaf to `module`; **retire the now-false sentence** *"nothing DISPATCHES yet … the stage walks an empty ledger"*. §7c-3. |
| 5 | `CREATE` | `tests/property/espionageRiderDormancyFence.test.js` | the three-part fence | `n/a` | §6.5. Straight-line registration only — §7c-1. |
| 6 | `CREATE` | `tests/domain/espionageRider.test.js` | the five doors, the composed shape, vocabulary conformance | `n/a` | §6.1. Straight-line registration only. |
| 7 | `TEST` | `tests/property/espionageProductsDormancyFence.test.js` | the `gated` exact-equality array | `6` eff. | Add `'espionageRider.js'` in codepoint order, with a `⏱ ES-Da ADDS ONE MORE` comment in the established idiom. §7c-1. |
| ⏸ | `TEST` | `tests/lint/sovereigntyLightingContract.walker.test.js` | the lighting-census row | `~20` | §7c-2. Re-derive all five WHOLE, re-record whole, cause stated. ⛔⛔ **SEQUENCED, NOT RESERVED AT PROMOTION — GR-4a holds the row. The chair moves it into this packet's `PACKET_MANIFEST.json` entry at dispatch; until it does, touching this file is out of manifest and is a STOP.** |

⭐ **These eight rows are the packet's complete obligation set; the SEVEN non-sequenced ones are
its reserved change paths and `PACKET_MANIFEST.json` carries exactly those seven.** The
discrepancy is deliberate, mechanical, and explained at §7b.
⚠ **The three `CREATE` spellings become existence-checked at the LANDED flip**
(`scripts/implementation-packets.mjs:466-468`), so a rename during implementation must move the
manifest row in the same change.

⛔ **NOT in the manifest and NOT to be edited:** `docs/implementation/INDEX.md` and
`docs/implementation/PACKET_MANIFEST.json` (§7b), `scripts/.size-baseline.json`,
`eslint.config.js`, `pulseKernel.js`, `applyWorldPulse.js`, `envoyPulse.js`,
`espionageMissions.js`, `envoyErrandVocabulary.js`, `errandMint.js`, `envoyErrandRecords.js`,
and the sibling lane's OSR instrument files. **A target outside this manifest is out of scope
even when the full gate finds an adjacent defect.** Generated artifacts: `NONE`.

### 7a. The coupling row — and its DIRECTION, which nothing cross-checks (CR-ESDA-3)

```
couplingId:   'CPL-19.INFO_TO_GRAMMAR.ES-Da.covert_rider'
pairId:       'CPL-19'
direction:    'INFO→GRAMMAR'
read:         'src/domain/worldPulse/envoyDiplomacy.js#dispatchAcceptedPeaceEnvoy'
receiptField: 'worldState.envoyErrands[].covert.{demand,itinerary,product,subjectId}'
counterforce: 'src/domain/worldPulse/errandMint.js#covertMissionRefusal'
flags:        ['errandSpineEnabled', 'espionageEnabled']
owningVolume: 'ESPIONAGE'
owningWave:   'ES-Da'
intendedDesk: 'war'
```

⭐⭐ **THE DIRECTION IS DERIVED, NOT GUESSED, AND IT IS THE OPPOSITE OF EVERY OTHER ES ROW.**
`direction = depLayer→importerLayer`. Measured against `LAYER_PATTERNS` in
`tests/lint/couplingInclusion.walker.test.js`:

- dependency = `src/domain/worldPulse/espionage/espionageRider.js` → matches
  `/^src\/domain\/worldPulse\/espionage\//` → **INFO**;
- importer = `src/domain/worldPulse/envoyDiplomacy.js` → matches
  `/^src\/domain\/worldPulse\/(?:treaty|peaceTerms|peaceReasons|peaceEngine|negotiation[A-Z]|envoy)/`
  → **GRAMMAR**.

⇒ **`INFO→GRAMMAR`.** Every ES row before this one reads `GRAMMAR→INFO` because espionage was
always the *importer*; **ES-Da is the first row where the espionage family is the DEPENDENCY, so
the arrow flips.** `read` names the **IMPORTER** — confirmed against
`ES1_HIDDEN_FRANCHISE_COUPLING`, whose `read` is `envoyDiplomacy.js` under `TRADE→GRAMMAR`.
`pairId` stays `CPL-19`, the volume's GRAMMAR × INFO anchor, on the CPL-20 precedent where both
directions share one anchor.

⚠⚠ **NOTHING CROSS-CHECKS THE ID's ARROW AGAINST `direction`, SO A WRONG ONE SHIPS GREEN.** The
row is spelled from the measurement above, and **the derivation is recorded here so a reviewer can
re-run it rather than trust it.** ⛔ **The coupling-direction hazard has bitten this program
before — the direction inverts with the import direction, and one lane had it backwards.**

### 7b. Reservations and collisions

| Packet | Status | Paths | Disjoint from ES-Da? |
|---|---|---|---|
| **TC-5B-I** | LANDED | — | ✅ **NONE reserved — a terminal packet reserves nothing** (`implementation-packets.mjs:43`). **This is what discharged Lane Q's D7.** |
| **TC-5B-II** | READY | 9, under `src/components/townMap/`, `src/lib/`, `src/design/`, `tests/ui/`, `tests/lib/`, `tests/build/` | ✅ **ZERO — all nine.** |
| **GR-4A** | READY | 10, under `src/domain/worldPulse/`, `tests/domain/`, `tests/property/`, plus the census walker | ✅ **ZERO on files.** ⚠ Both packets live under `src/domain/worldPulse/` — the near-miss is real and the file sets are disjoint (GR-4a is the treaty/breach family; ES-Da is the envoy/espionage family). ⏸ The census walker is the one shared artifact — sequenced below. |
| **SCW-0** | READY | 5, under `tests/lint/` and `scripts/` | ✅ **ZERO.** |
| **IA-2** | STALE | 12, incl. `docs/implementation/INDEX.md` and `docs/implementation/PACKET_MANIFEST.json` | ✅ **Disjoint ONLY BECAUSE ES-Da does not claim those two.** See ⛔ below. |

⛔ **THE INDEX/MANIFEST COLLISION, AND WHY THIS PACKET SIDESTEPS IT.** IA-2 [STALE] holds
`docs/implementation/INDEX.md` and `docs/implementation/PACKET_MANIFEST.json` under exclusive
reservation — **CONFIRMED STALE still reserves**, since `TERMINAL_PACKET_STATUSES` is
`{LANDED, SUPERSEDED}` only. `scripts/implementation-packets.mjs` fails closed on `duplicate
change path across packets`, so listing either in ES-Da's manifest makes the packet
**non-dispatchable**. TC-5B-I and TC-5B-II both sidestep this by not listing their own
index/manifest rows; **ES-Da follows that precedent — the coordinator updates `INDEX.md` and the
manifest as packet maintenance, outside the change manifest** (CR-ESDA-5, and it is what
discharges §1 item 6's stale-cell note).

⛔⛔ **THE CENSUS WALKER — SEQUENCED, NOT RESERVED.** ES-Da adds two test FILES and many titles,
so all five census figures move; serialization-law rule 1 re-derives the census WHOLE in the
change that moves any figure, and rule 5 forecloses the "no new file, no census move" escape. But
**four non-terminal packets now need that one file**, and `validate:packets` — **step 3 of the
17-step chain** — fails closed on a duplicate path, blacking out fourteen later steps. **RULED
(chair, 2026-08-11): GR-4a holds the reservation at promotion; the chair MOVES the row into
whichever packet it dispatches next; only ONE implementer is in flight across the four.**
⛔ Rule 4 also binds: **ES-Da may not quantify another lane's uncommitted delta**, and must
attribute its own delta in isolation against a named committed sha.

### 7c. Landing discipline this manifest incurs — THREE obligations, all measured

1. ⚠⚠ **THE `gated` EXACT-EQUALITY CENSUS, AND THE PARKED-SUITE TRAP.**
   `tests/property/espionageProductsDormancyFence.test.js` pins the set of `espionage/*.js` files
   that spell `espionageActive` at **exact equality** — nine members today. **The rider gates, so
   it MUST be added in the same commit**, in codepoint order, with a comment row in the file's
   established `⏱ ES-Xx ADDS ONE MORE` idiom stating **why** it gates (it has a production
   importer outside the family and can inherit nobody's dormancy). ⛔ **The sibling pin —
   `espionageEnabled` read by name in EXACTLY ONE module, `espionageGate.js` — must stay true:
   the rider must never spell `espionageEnabled`, only `espionageActive`.**
   ⚠⚠ **And both new test files must register straight-line.** A `test(`/`it(` inside a loop is
   `TEST_UNREGISTERED` and **the whole file parks**, losing every title in it; a non-straight-line
   `describe` parks the same way; `.each()` parks via `TEST_TABLE_UNPROVEN`. **Verify `credited`
   moved, not just `files`.**
2. ⭐ **THE LIGHTING CENSUS.** Under the serialization law ruled at `73f5be96`: *"A shared census
   is re-derived WHOLE in the same change that moves any figure"*, and *"File-placement does not
   exempt: `titles`/`suiteTitles` pin estate-wide."* All five arms are `.toBe()` exact equality
   plus a `parked + credited === files` cross-check. ⚠ **The sequence hazard is live:** while any
   arm is red the census **stops measuring**, so later arms may read anything. ⭐ Probe with a
   temporary `console.log` **inside the existing census test, before its first assertion**, so it
   mints no title and cannot move what it measures. ⛔ **STOP** if a foreign lane holds
   uncommitted test titles at re-record time, or if the chair has not moved the reservation into
   this packet (§7b). **A red shared walker is never landed as debt.**
3. ⚠ **THE CERTIFICATION ROW'S PROSE.** The `espionageEnabled` row in `subsystemRowsVirtual.js`
   currently asserts *"ES-1 built the door and ES-3 built the traffic, but nothing DISPATCHES yet
   … so no covert mission is minted in a running world and the stage walks an empty ledger."*
   **ES-Da makes that FALSE.** The estate's own recorded rule — *"a certification row asserting a
   false invariant is worse than no row, because the walkers treat it as the authority"* — makes
   this a **same-commit obligation. Narrow the sentence; do not delete the row.** Two constraints:
   `tests/domain/subsystemRowsVirtual.test.js` requires `prose.length > 2000` (an edit must not
   shrink it under), and its importer scan is `toContain`, not exact-set, so `envoyDiplomacy.js`
   joining the importers reds nothing.

---

## 8. Ordered coding sequence

Per `PACKET_STANDARD.md` §"Mandatory implementation order":

0. **Preflight** (§5) — branch, ancestry, the census-holder gate, clean targets, absent new
   files, live symbols. **Stop on mismatch.**
1. **Capture B1–B7** (§5b). ⭐ **The dark-path golden is captured BEFORE the rider exists.**
2. **Write the smallest failing focused test** — `tests/domain/espionageRider.test.js`, door 1
   (dark returns `{covert: null, reason: 'dark'}`) plus the composed-shape case.
3. **Implement the pure leaf** (§6.1). **Gate first, read second.**
4. **No writer or lifecycle seam to extend** — ES-Da adds neither. Skip, per this note.
5. **Wire the ONE named consumer** — `envoyDiplomacy.js` (§6.2), with the string-literal
   `purposeClass` spelling of §6.3. **Re-measure the effective-line delta immediately.**
6. **Registrations and the prevention guard** — the coupling row (§7a), the certification-row
   narrowing (§7c-3), the `gated` census member (§7c-1), and the dormancy fence (§6.5).
7. **Focused verification** (§10), including the four exact-equality walkers.
8. **The census re-derivation** (§7c-2, only if the reservation has been moved here), **then the
   wave-end gate and the completion receipt** (§14).

⛔ **The agent must not start by changing a golden, a baseline, a budget or a persisted shape.**

---

## 9. Acceptance matrix — the complete, closed denominator (8 cases)

| # | Case | Assertion |
|---|---|---|
| **C1** | **The main reachable behavior** | A lit world, an accepted peace offer, a drawing court ⇒ the minted row carries `purposeClass:'covert'` and a lawful one-stop `covert` sub-record whose `subjectId` and `itinerary[0].settlementId` are **both** the target court. |
| **C2** | **Absent / disabled — THE MANDATORY BYTE-IDENTITY RECEIPT** | A multi-tick run over a full errand lifecycle in a DARK world is **byte-identical** to the same run before this packet, asserted on serialized `worldState.envoyErrands`. Repeated for **absent, `false`, `0` and `'true'`** spellings — one world. |
| **C3** | **Counterforce / negative** | The draw declining (`hash01 >= frequency01`) and an unreadable doctrine (`known !== true`) each yield `covert: null` **and their own reason**; the minted row is byte-identical to a no-rider row. |
| **C4** | **Sparse / malformed-but-supported** | A court with no resolvable settlement item, **and** a world where `errandSpineEnabled` is dark but `espionageEnabled` is lit (the invalid lighting order) ⇒ the layer stays dark SAFELY at door 2 of `espionageActive`; no covert field is read, refused, or written. |
| **C5** | **Duplicate / idempotent** | A second dispatch for the same offer hits `duplicate_episode` **before the rider is consulted**; the rider's draw for one `errandId` is stable across repeated evaluation (keyed hash, no PRNG). |
| **C6** | **Lifecycle round trip** | A minted covert row survives persist → reload → `normalizeErrand` byte-identically; a forged import with corrupt covert cargo **HEALS TO ABSENT and keeps the traveller**; undo (`restoreEnvoyErrands`) restores the row with its cargo intact. |
| **C7** | ⭐ **Real writer-to-reader integration — the "traffic flows" proof** | With the row minted and the embassy arrived at its destination, `covertDwellRead` returns `dwelling:true` at the named stop with `intervalIdx: 0` (the minted stay), and `advanceEspionageProducts` accrues at least one `gathered` partial onto the row. |
| **C8** | **Named historical regression — the one-reader law and the minter census** | `tests/lint/errandConsumerRegistry.walker.test.js` stays green: the offender set is **empty** (no new direct field read) AND `minters` still equals **exactly** the three registered modules (the rider is not a fourth minter). |

⛔ **Cases that do not apply are omitted, not replaced. A newly discovered edge case is recorded
in the completion receipt without investigation** unless it disproves a packet premise — in which
case the packet STOPS.

### 9b. OUTPUT POSTURE — dark-built, and the dark path is the assertion

> **ES-Da ships DARK. `espionageEnabled` stays absent from every preset, so no existing world
> changes at all** — and CR-ESDA-1 makes that byte-identity a matter of **construction, not
> care**: when the rider declines, the spread contributes `{}` and the `mintEnvoyErrand` argument
> object is the object that was built before this packet existed.
>
> ⛔ **The dark-path byte-identity golden moving AT ALL is a STOP, never a re-record.** It is the
> packet's central invariant and the owner's condition.
> ⚠ **Figures permitted to move, each re-recorded whole with the cause stated:** the `gated`
> census array (+1 member), the five lighting-census numbers, and the certification row's prose.
> ⛔ **Everything else moving is a STOP.**
> ⭐ **What lights at the endgame, restated as measured (§4b): ES-5a (both halves), ES-2, ES-3,
> ES-5d and ES-6a are reached. ES-5b and ES-5c are NOT** — they were never covert-gated.

---

## 10. Verification commands

```sh
# Baseline B1/B2 — the test slot is held for the COMPLETE process, not observed and released.
sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/property/espionageProductsDormancyFence.test.js \
  tests/property/espionageDormancyFence.test.js \
  tests/property/errandSpineDormancyFence.test.js \
  tests/lint/errandConsumerRegistry.walker.test.js

# Focused behavior — C1..C8.
sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/domain/espionageRider.test.js \
  tests/property/espionageRiderDormancyFence.test.js \
  tests/domain/envoyErrand.test.js \
  tests/domain/envoyErrandSpineLifecycle.test.js \
  tests/domain/espionageMission.test.js

# The four walkers this packet can break — ALL exact-equality pins.
sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/lint/errandConsumerRegistry.walker.test.js \
  tests/lint/couplingInclusion.walker.test.js \
  tests/lint/sizeBaseline.test.js \
  tests/domain/subsystemRowsVirtual.test.js

npx eslint src/domain/worldPulse/espionage/espionageRider.js \
  src/domain/worldPulse/envoyDiplomacy.js \
  src/domain/certification/couplingRegistryEspionage.js \
  src/domain/certification/subsystemRowsVirtual.js

npm run typecheck:ratchet          # tsconfig.full.json
npm run typecheck:domain:strict    # tsconfig.domain-strict.json

# Census re-derivation (§7c-2), then the landing gate.
npm run check:tail
```

⚠ **Never read a gate through a pipe.** `npm run check | tail` reports the PIPE's status and has
greenwashed a red gate twice. Use `npm run check:tail` or `sh scripts/gate-tail.sh <command...>`.
`npm run check` is a **17-step `&&` chain** — a red step blacks out every later step, so the
receipt must say **which steps actually ran**. **Trust no exit status you did not capture
yourself.**

---

## 11. Recorded deviations — where the design prose and the code disagree, the code wins

**11.1 — §16's "lights five landed-dark waves at once" is NARROWED.** CONFIRMED by per-file
importer census: ES-5b (`espionagePresence.js`) and ES-5c (`espionageCareer.js`) are **not
covert-gated at all.** Both read `whereabouts` and faction/npc state, both are mounted on live
per-tick paths, and both begin moving bloc math and ladder defense scores the moment
`espionageEnabled` lights — with or without ES-Da. What ES-Da actually reaches is **ES-5a (both
halves), ES-2, ES-3, ES-5d and ES-6a.** ⭐ **This slightly REDUCES what the owner is authorizing,
and it should be carried back to §16 rather than left in a packet.**

**11.2 — ⛔⛔ THE MINT THAT "WOULD CREATE THE ROW" IS NOT THE MINT ES-Da USES.**
`mintCovertMission` has **no production caller** and its returned `fields` have **no writer**;
that is what made ES-1's door a validated dead end. **The design volume reads as though covert
rows arrive through it. THEY DO NOT, and ES-Da does not change that.** The row arrives through
`mintEnvoyErrand` → `mintErrandSpine`, which spreads `spine.fields` into the row.
**`mintCovertMission` remains dead after this packet, deliberately.** CONFIRMED. ⭐ **This is the
single most important thing a coding agent could get wrong.**

**11.3 — the `ERRAND_CONSUMERS` row will be true-but-misleading after ES-Da.** The registry
credits `espionageMissions.js` as the built `covert` consumer, and DIRECTION 2 only asks whether
that module reaches `mintErrandSpine` — which it still does, so nothing reds. **But after ES-Da
the module that actually causes covert rows to exist is `envoyDiplomacy.js`. No walker can see
this**, which is exactly the fail-open-invisibility class CR-ES5B-4 says to **record rather than
design around**. **Disposition (§12b D5): record it in the rider leaf's header and in the
completion receipt; do NOT restructure the registry** — a row change would red DIRECTION 2 both
ways.

**11.4 — §16's "both pulse mouths sit at EXACT zero line headroom" is CONFIRMED but does not bind
ES-Da.** `applyWorldPulse.js` measures 941 against a frozen 941 and `pulseKernel.js` 1580 against
a frozen 1580 — zero headroom in **both** directions (growth reds eslint, shrink reds
`sizeBaseline.test.js`). **ES-Da touches neither**, because the composition point sits one level
below the mouth, inside a function the mouth already calls (`applyWorldPulse.js:627`). **No stage
is added and no pulse line moves.**

**11.5 — `deliberationRead` is reachable but semantically wrong here.** Measured:
`deliberationRead({castable:true})` with no confidence supplied returns `'dispatch_and_wait'` (a
non-finite confidence counts as stale), so composing `dispatchCadenceFor` whole would **not** be
a dead arm. **But its door 3 asks "should this court defer its decision pending a spy?" and on
the ES-Da path the decision is already made and accepted.** Using it would mean supplying a
fabricated `castable` / `decidingConfidence01` to make a question fit that the situation does not
pose. §6.1 therefore composes the doctrine and the draw directly and skips the deliberation read
— recorded here rather than left to be inferred (§12b D1).

---

## 12. Rulings and recorded decisions

### 12a. The chair's rulings — CR-ESDA-1..5, BINDING

- **CR-ESDA-1 — DARK-BUILT, AND BYTE-IDENTITY BY CONSTRUCTION. RULED.** ES-Da ships with
  `espionageEnabled` absent from every preset; lighting is the endgame's, under the owner's walk
  and both soaks. ⭐ **The dark-path byte identity is not a property to be tested for after the
  fact — it is a CONSTRUCTION:** the leaf gates on `espionageActive` before touching any world
  object (door 1, §6.1), and a declining rider contributes `{}` to the argument spread, so the
  object handed to `mintEnvoyErrand` is the object that existed before this packet (§6.2). The
  three-part fence at §6.5 proves it three ways, **including the LIT MUTANT, without which the
  fence would pass by measuring nothing.** ⛔ **Any motion in the dark-path golden is a STOP,
  never a re-record.** This discharges the owner's mandatory condition 1.
- **CR-ESDA-2 — THE STRING LITERAL `purposeClass: 'covert'` IS A STOP INSTRUCTION. RULED.**
  `errandConsumerRegistry.walker.test.js` reds any `src/` module outside the five-file errand
  family that reads `purposeClass` as a field, and **both** files ES-Da touches are outside it.
  Lane Q drove the walker's own regexes: a **string-literal value** escapes
  `PATTERN_ELEMENT_RE`'s identifier branch; a shorthand or variable **does not**. **The escape is
  one character wide.** ⇒ The value must be the literal `'covert'`, never a variable, never
  shorthand, never computed, **in either file** — and ⭐ **the rider leaf must not spell
  `purposeClass` at all**, so the single spelling lives only at the call site. **Wanting
  `{ purposeClass }` is a STOP, not a refactor.**
- **CR-ESDA-3 — THE COUPLING ARROW IS `INFO→GRAMMAR`, WITH ITS DERIVATION RECORDED. RULED.**
  §7a. The direction is `depLayer→importerLayer`; the dependency is the espionage leaf (**INFO**)
  and the importer is `envoyDiplomacy.js` (**GRAMMAR**), so the arrow is `INFO→GRAMMAR` — **the
  opposite of every prior ES row**, because ES-Da is the first row where the espionage family is
  the *dependency* rather than the importer. ⛔ **Nothing cross-checks the id's arrow against
  `direction`, so a wrong one ships green** — which is why the derivation is written into the
  packet for a reviewer to re-run rather than trust. `pairId` stays `CPL-19` on the CPL-20
  both-directions-share-one-anchor precedent.
- **CR-ESDA-4 — `stayTicks: 1` IS A DERIVED CONSTANT, NOT A TUNING VALUE. RULED.** The owner's
  condition 2 prefers zero new tuning keys, and this is the packet's only arguable brush with the
  tuning carve-out, so it is ruled explicitly rather than assumed. **`beginEnvoyReturn` refuses
  before `parlayTick + 1`, so one tick is exactly the stay the embassy's own schedule already
  reserves** — the value is *read off the errand family's own law*, not chosen. ⭐ **Recorded as
  DERIVED in the leaf, with that one-sentence justification beside it**, so a later tuning pass
  does not mistake it for a knob. **Rejected:** `0` — measurably wrong, it makes `intervalIdx` 1
  at the arrival tick, pricing every embassy as an immediately-rooted overstayer. ⇒ **ES-Da adds
  ZERO new tuning keys**, discharging the owner's condition 2.
- **CR-ESDA-5 — THE WALKER IS A TEST RESERVATION, AND THE SERIALIZE-BEHIND NOTE IS DISCHARGED.
  RULED.** Lane Q's D7 held ES-Da behind TC-5b-i because the census walker was that packet's
  fifth reserved path. **TC-5b-i LANDED at `9183d52c`; a terminal packet reserves nothing; the
  serialize-behind condition is DISCHARGED.** ⛔ **It is replaced, not deleted:** three sibling
  packets promoted in the same change also fold the census, and the validator forbids duplicate
  non-terminal paths, so **GR-4a holds the reservation at promotion and the chair MOVES the row
  at dispatch — one implementer in flight across the four** (§7b). ⭐ Also ruled here (Lane Q's
  D8): **the stale `INDEX.md` cell recording ES-Da as owner-gated and unreadable from the build
  branch is corrected by the chair in this same documentation change** — coordinator maintenance,
  outside this packet's change manifest, on the TC-5B precedent.

### 12b. Lane Q decisions the chair ADOPTS at promotion — ✅ FABLE-VALIDATED 2026-08-11

Each was a recommendation in the read-only draft; each is ratified here so the packet carries
**zero** open items. Validated by the Fable chair in session `c42c8924`; **no `⏳` marker lands.**

- **D1 — WHICH DECISION FUNCTION GATES THE RIDER: `espionageDoctrineFor` + an own draw, NOT
  `dispatchCadenceFor` whole.** Ground: `dispatchCadenceFor`'s deliberation door asks whether to
  **DEFER** a decision, and ES-Da's decision is already accepted; composing it whole would require
  **fabricating** `castable` and `decidingConfidence01` to make a question fit that the situation
  does not pose (§11.5). ⭐ Both paths make ES-Da the doctrine stage's first production caller, so
  **ES-5a wakes either way** — the choice costs nothing and buys honesty.
- **D3 — THE PRODUCT WORD IS `confirm`.** It crosses no observed slot and needs no `legRefs`,
  keeping the slice at zero new surface. **Rejected:** `acquire` (would require a `legRefs` list
  and widen the packet); `refute` (crosses everything readable and is a louder product behavior
  than a first re-entry should carry).
- **D4 — THE ITINERARY FACE IS `'declared'`.** The stop **is** the embassy's public destination;
  the cover story is true about *where* the man goes and false only about *why*. **Rejected:**
  `'covert'` — it would assert a hidden stop that the peace embassy demonstrably does not make.
- **D5 — THE `ERRAND_CONSUMERS` TRUTHFULNESS GAP: RECORD, DO NOT RESTRUCTURE.** A registry edit
  reds DIRECTION 2 in both directions. Document it in the leaf header and the completion receipt,
  on the CR-ES5B-4 precedent (§11.3).
- **D6 — THE KEYED STREAM IS A NEW SPELLING, `es.rider.${errandId}`.** `dispatchCadenceKey` is
  keyed `(courtId, tick)`, so a court dispatching two episodes on one tick would get **identical
  rolls**, and a future ES-7 dispatcher reusing the key would **correlate** with this one. Still
  **zero new PRNG streams** — `hash01` is the estate's keyed hash (L1).

---

## 13. Mandatory STOP conditions

The agent stops — without expanding or repairing — when:

- ⛔ packet status is not READY, or HEAD is not the verified base or an explicitly admitted
  descendant;
- ⛔ **the `envoyDiplomacy.js` delta would exceed 15 effective lines** (measured with eslint's own
  `Linter`, never `wc -l`);
- ⛔ a target file is dirty, changed concurrently, or outside §7's manifest;
- ⛔ a required symbol from §4 is missing or materially different;
- ⛔ **`minters` would gain a fourth member**, or an `ERRAND_CONSUMERS` row seems needed (§6.4);
- ⛔ **the one-reader walker reds** — a `purposeClass` spelling drifted off the string literal;
  fix the spelling, and if that is not possible, **STOP** (CR-ESDA-2);
- ⛔ **the dark-path byte-identity golden moves AT ALL** — the packet's central invariant and the
  owner's condition; **any motion is a STOP, never a re-record** (CR-ESDA-1);
- ⛔ **`mintCovertMission` or `castCovertOperative` would need to be called** (§2.2, §11.2);
- ⛔ **a second itinerary stop, a `legRefs` list, a vetting band, a Herald kind, or any dispatcher
  behavior would be needed** — each is a parked refusal;
- ⛔ **a new tuning value would be needed**, including a fourth field on the composed sub-record;
- ⛔ a new persisted key, a second writer, a new flag, a new ledger or a new stage is needed;
- ⛔ **the chair has not moved the lighting-census walker's manifest row into this packet** and
  the census nonetheless needs folding, or a foreign lane holds uncommitted test titles at
  re-record time (§7b, §7c-2);
- ⛔ a ratchet, baseline, budget, timeout or ceiling would need raising;
- ⛔ a new gate failure lies outside §9's acceptance surface;
- ⛔ an additional edge case requires new product behavior;
- **any packet premise here is refuted by live code. The code wins; the packet stops.**

The STOP report contains the smallest measured contradiction, the evidence, and a proposed split.
It contains **no speculative repair.**

---

## 14. Completion receipt requirements

Beyond `PACKET_STANDARD.md`'s standard fields, ES-Da's receipt must carry:

- ⭐ **the dark-path byte-identity evidence, QUOTED** — the serialized-ledger comparison, both
  flag states, with the run length;
- **the measured effective-line delta on `envoyDiplomacy.js`, before and after, from eslint's own
  `Linter`** (⛔ **never `wc -l`**);
- the `gated` census before/after, and **the lighting census's five figures re-derived WHOLE
  against a named committed sha, with ES-Da's own delta attributed IN ISOLATION**;
- **both typecheck configurations named with their windows** (`typecheck:ratchet` /
  `tsconfig.full.json`; `typecheck:domain:strict` / `tsconfig.domain-strict.json`);
- **which of the 17 gate steps actually ran**;
- the coupling row's `direction` derivation **re-run and quoted**, not inherited (§7a);
- ⭐ **the consumer-reachability claim re-stated as measured: ES-5a, ES-2, ES-3, ES-5d and ES-6a
  reached; ES-5b and ES-5c unaffected** (§4b, §11.1);
- the `ERRAND_CONSUMERS` truthfulness gap recorded as an out-of-scope observation (§11.3);
- **judgment calls: `NONE`** — every one is pre-ruled at §12. **Any non-none architectural
  judgment invalidates READY and returns the packet to the coordinator.**
