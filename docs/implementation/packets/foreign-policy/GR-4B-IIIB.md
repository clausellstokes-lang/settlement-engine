# Foreign Policy / GR-4B-IIIB — the open-question dossier line

- **Status:** READY
- **Packet version:** `1`
- **Verified base:** `claude/composite-r4` at `2dd09360e63fcca2b278dcf4d3e9c7f949434e55`
- **Base measured:** 2026-08-14 ("docs: finish A-22 succession exemplar cure")
  in the minifold worktree with a clean `git status --porcelain`.
- **Depends on:** GR-4d at implementation `a17d0a02` for the durable pending
  proposal and its validator; GR-4b-iii-a at implementation `7e7d5205` for the
  landed fallen-holder composer idiom; annex chair acts A-22 / CR-GR4B-12 at
  `c39d2413` and `2dd09360` for the corrected corpus.
- **Collision group:** `pact-grammar-voice`, `treaty-display-read-model`, and
  `sovereignty-lighting-census`. Serialize against every lane touching any of
  the ten manifest paths.
- **Commit authority:** edits only. The coding agent may edit exactly the ten
  implementation paths in section 7 and must leave them unstaged and
  uncommitted. The coordinator owns proof, immutable commit construction,
  terminal promotion, and old-value CAS.
- **Baseline posture:** the verified tree is clean. Both CREATE targets are
  absent. The receipt annex is already corrected and immutable at SHA-256
  `0beee51479b0ffa22f4448506dca6cb85ab7f37b957a42acf1827c81e961520e`.
  **No hot file enters this manifest** — the three standing hot files were
  measured at this base and none is touched (section 4.3).

- **Promoted READY:** 2026-08-14 by the Fable chair after independent
  re-verification of the drafting receipts (validate:packets 34/0 at base,
  `TITLE_BASELINE = 485` at `guidanceRegistry.walker.test.js:345`, the four
  banked voice-mechanics arms in `scripts/.test-ratchet-baseline.json`, the
  hot-file figures, and the base census tuple). Chair rulings CR-GR4B-13,
  CR-GR4B-14, and CR-GR4B-15 are SIGNED as written in section 1; the drafting
  judgments 4-6 (DISPLAY-layer leaf, no flag read, always-present key) are
  RATIFIED; the zero-PDF-edit asymmetry is deliberate per the corpus block's
  own address.

## 1. Reconciled authority and chair rulings

This is the successor authority for GR-4b-iii-b. Terminal `GR-4B.md`,
`GR-4D.md`, and `GR-4B-IIIA.md` are history and must not be amended or
redispatched. `GR-4B-IIIA.md` §1 (CR-GR4B-9) named this slice as explicitly
deferred and its §12 closed without it; this packet is the release.

### CR-GR4B-12 — the annex chair act is discharged and immutable

The authored dossier pool repeated the successor / grouped-oath defect already
cured for the opening beat and asserted envoys, prepared letters, a town-wide
wait, market activity and universal compliance that neither the pending row nor
the selected treaty carries. The chair act landed at `c39d2413` and closed at
`2dd09360`; it is complete governance substrate, not an implementation path.
The implementer consumes it verbatim and may not edit it.

The eight required-slot vectors are exactly, in authored order — **derived by
execution through `tests/helpers/receiptAnnex.js` at this base, not
transcribed**:

```text
[]
[npc, settlement]
[counterpart, settlement]
[settlement, counterpart]
[]
[settlement, counterpart]
[npc, settlement]
[]
```

`{npc}` means only the fallen holder recorded on the selected treaty's oath
stamp. No successor or roster lookup is permitted. `{band}` is absent. The pool
remains eight families, `n/a` significance, `public` audience, no Herald desk,
in the authored order. Annex SHA-256 mismatch is a pre-dispatch STOP.

### CR-GR4B-13 — user-facing surface override `1 → 2` (OWNER/CHAIR GATE)

The default hard scope budget allows **at most one user-facing surface**. The
governed corpus block addresses two by name in its own heading —
`### succession_question_open (GR-4) — dossier: WarFaithTab / TreatyPanel` — and
the landed sibling dossier line `treaty_age_line` already renders one read-model
key at both mounts. Rendering only one of the two would refuse half the corpus's
own declared address.

This override applies **only** to the user-facing-surface row. Every other
default limit binds unchanged: the handwritten total is **10** and receives no
override, the registration-only count is **2** and receives no override, and
GR-4b-alpha's historical `12 → 14` override does not transfer. An eleventh path,
a third registration file, or an attempt to invoke an inherited total override is
a STOP.

**The documented alternative, rejected:** render only into `TreatyPanel.jsx` and
defer `WarFaithTab.jsx` to a further slice. Rejected because it splits one
authored corpus block across two waves for a budget row rather than a fact, and
leaves the dossier — the surface the block is named for — silent.

### CR-GR4B-14 — eligibility is a stamped, live-term treaty

A-22 rules that every line is entailed by "the currently eligible treaty's
parties, live written terms and oath stamp". Variant 6 says the treaty "remains
live under the terms already written" and variant 4 says the parchment "still
joins" the two courts, so a treaty carrying no written term does not entail its
own pool. Eligibility is therefore: the question's `treatyKey` resolves in the
live ledger, the document read-model carries at least one term, the treaty's
orientation resolves both persisted court names, and the acting court's `sworn`
stamp names the exact `npcId` the question carries. Any miss yields **zero
lines for that row** and never a partial line.

### CR-GR4B-15 — `{settlement}` is the acting court, for every viewer

`{settlement}` binds to `question.settlementId` (the court that must answer) and
`{counterpart}` to `question.otherId`, identically on both mounts and for both
parties' dossiers. The binding is **not** viewer-relative: variant 3 is authored
from the counterpart's vantage in its own words ("From {counterpart}, the treaty
still reads as standing"), so re-binding the slots per viewer would invert that
family's meaning. This is the exact binding
`treatySuccessionOpeningVoice.js` already uses, and it deliberately does not
route through `grammarSlotRoles` — the obligee/obligor axis is not this pool's
axis.

## 2. Outcome and boundary

**Observable result:** every treaty document rendered by the display read-model
carries one canonically ordered dossier line per validated pending succession
question standing against that instrument, and both the realm `TreatyPanel` and
the dossier's `WarFaithTab` render those lines beneath the longevity line. The
line says the question is open and the parchment still stands; it claims no
answer, no successor name, no breach, and no per-treaty uniqueness.

The pending proposal is mechanics and the line is presentation. Missing
presentation data suppresses the line and must never veto, alter, or write any
proposal, treaty, or ledger.

**Definition of done:** the exact ten paths pass B1-B8; no hot file is touched;
the receipt-pool registry moves by exactly one row while the Herald, routing,
and impact-kind censuses do not move at all; the runtime denominator moves by
exactly eleven; the whole lighting census becomes exactly
`2415/365/2050/20008/5641`; the `title=` census stays exactly `485`; all four
deep-craft kill-list ceilings stay exact; eight disposable mutants fail by their
intended B-title and restore digest-exact; the sealed checks, independent bare
full gate, and separate boot smoke exit zero; no soak starts.

In scope:

1. one pure DISPLAY-owned dossier-line composer leaf;
2. one always-present read-model key threaded through the three existing render
   entry points;
3. one eight-family annex-verbatim `n/a` / no-desk grammar kind and its **two**
   mechanical registration homes;
4. two one-block mounts, one per named surface;
5. one eight-case prevention file and the three already-governed walkers it
   necessarily updates.

Explicit non-goals:

- no new state, state writer, persisted family, schema, migration, flag, tuning
  key, dependency, golden, baseline, ceiling, timeout, preset, icon, or route;
- no edit to `treatySuccessionDecision.js`, `treatySuccessionOpeningVoice.js`,
  `treatySuccessionVoice.js`, `treatyBreach.js`, `actorMajorApproval.js`,
  `peaceTerms.js`, `peaceTermsDocument.js`, `applyWorldPulse.js`,
  `heraldRouting.js`, `chroniclersLetter.js`, `settlementRumors.js`,
  `src/pdf/lib/liveWorld.js`, any terminal packet, or the receipt annex;
- no mutation-manifest, hazard-registry, coupling-registry, observed-shape
  baseline, size-baseline, test-ratchet, voice-mechanics-baseline,
  kill-list-ceiling, title-baseline, ledger-ownership, or pulse-stage edit;
- no `impactKind` mint, no Herald desk, no `WHAT_PHRASES` row, no
  `EXPECTED_VOICE` row, no chronicler section;
- no diagnostic, weekly, release, or research soak.

## 3. Hard scope and line budgets

| Limit | Packet budget |
|---|---:|
| Behavior families | `1` |
| New persisted record families | `0` |
| New state writers | `0` |
| New feature flags / tuning keys | `0 / 0` |
| New public feed kinds | `0` — a dossier line mints no `impactKind` |
| New grammar registry rows | `1` |
| User-facing surfaces | `2` under CR-GR4B-13 |
| Direct production consumers | `2` — the two named mounts |
| New logic-bearing production leaves | `1` |
| Existing logic-bearing production files modified | `3` |
| Registration-only production files | `2` — within the default `3`, no override |
| Handwritten implementation files total | exactly `10`; no override |
| New/changed effective production lines | `<=152` |
| `treatySuccessionDossier.js` | `<=110` effective and `<=170` raw |
| `treatyDocument.js` delta | `<=10` effective (`180 → <=190`) |
| `grammarReceiptPools.js` delta | `<=14` effective (`93 → <=107`) |
| `grammarNews.js` delta | `<=8` effective (`110 → <=118`) |
| `TreatyPanel.jsx` delta | `<=5` effective (`129 → <=134`) |
| `WarFaithTab.jsx` delta | `<=5` effective (`168 → <=173`) |
| Acceptance cases | exactly `8` literal `it` registrations |

Comments that repair now-stale local counts or deferral prose inside one of the
ten paths are permitted and required for coherence. They do not authorize a new
path or an effective-line overrun.

## 4. Verified tree contract and fingerprints

### 4.1 Existing target seals

Both CREATE paths are absent (executed check, section 12a). Every existing
implementation target is clean at this exact raw/effective count and SHA-256.
Effective counts were taken with eslint's own `Linter` under `max-lines`
`{ skipBlankLines: true, skipComments: true }` at this base — never `wc -l`,
never an inherited figure.

| Path | Raw / effective | SHA-256 |
|---|---:|---|
| `src/domain/display/treatyDocument.js` | `339 / 180` | `7c68f54d22f45dfe658649cef2fa97dbbc5460cc51ee0fa7808267088be71369` |
| `src/domain/worldPulse/grammarReceiptPools.js` | `152 / 93` | `81e654348b8ae0a10fab654d2bae6f2074d110eac1dbc11bd3dba9200c64eac1` |
| `src/domain/worldPulse/grammarNews.js` | `244 / 110` | `e9dd32b2144ef3205b33552e14c7c9a7a7ae849ae054ee58ea4b392c972adea9` |
| `src/components/map/TreatyPanel.jsx` | `176 / 129` | `8c2f19fb60011e3fbf7b9f4e73412f120948edf45dc223ca3aa27e9ff4a25b17` |
| `src/components/new/tabs/WarFaithTab.jsx` | `232 / 168` | `3c80b6574ff286f677f18bd3ac4dab58f6dac12152d8942011ddc545fffda853` |
| `tests/lint/grammarLifecycleKindPools.walker.test.js` | `338 / 210` | `5b4229415ee817d995f1bf4963cd5f1e66029ab824e1b2f9a4b194ffb9e58baa` |
| `tests/lint/kindPoolFloors.walker.test.js` | `318 / 183` | `d90285db4f571810462be16594f64a9e66489c8927d7b28b37143272616eb64f` |
| `tests/lint/sovereigntyLightingContract.walker.test.js` | `4395 / 1336` | `b912230b033f409bd1f933344de645259f9c3eaf6ba23b4d018a222eb6aca324` |

Anchor seals — read, never edited:

```text
0beee51479b0ffa22f4448506dca6cb85ab7f37b957a42acf1827c81e961520e  docs/content/RECEIPT_POOLS_GRAMMAR.md
c9cad6ffda48aec04ab3966bad085783ed21fb8f9f75f73b27353a60d468556d  src/domain/worldPulse/treatySuccessionDecision.js
67a38dd4a289a79dc4355f956828a25f6a1ce37a80cdcfb4de93727a30ee5450  src/domain/worldPulse/treatySuccessionOpeningVoice.js
3e7af9f1da57ef6ec8ff4edb82bec37e1369bca117b3488d6e055f36448d8b24  src/domain/worldPulse/oathHolder.js
5d48a8b434a78739ce5954468f2446cd0d4f4be034d44d18978781016b58567e  src/domain/worldPulse/treatyOrientation.js
087ed97c432e2e4f8d832fff9c4b351af46064c03cd0b978e7d943447f63d084  src/domain/worldPulse/treatyEnforcement.js
90b5d21636151764e2141f399628498f3a4436ef09857a3cb2f68a556d5bc1d3  src/pdf/lib/liveWorld.js
e9ec5f248df9002d7481a7b63419bc862fe4eee6de177e24dbbe9c9d7088a982  tests/property/treatyLifecycleVoiceDormancyFence.test.js
```

Any mismatch, existing CREATE target, target dirt, or new foreign movement is a
dispatch STOP.

### 4.2 Required live symbols

| Symbol | Home | Role |
|---|---|---|
| `successionQuestionPayloadTuple` | `worldPulse/treatySuccessionDecision.js` | the closed outcome validator — the only admissible projection |
| `swornPartiesOf` | `worldPulse/oathHolder.js` | the treaty's total oath-stamp reader — the only source of `{npc}` |
| `treatyOrientationOf` | `worldPulse/treatyOrientation.js` | the one orientation reader, for both persisted court names |
| `treatyLedgerOf` | `worldPulse/treatyEnforcement.js` | the canonical treaty ledger reader |
| `grammarReceipt` | `worldPulse/grammarNews.js` | the seeded picker |
| `treatyDocument` / `treatyDocumentsForSettlement` | `worldPulse/peaceTerms.js` | the existing document read-model, unchanged |

Required created production symbol: `successionQuestionOpenLines`.
Retired symbols: **NONE**.

### 4.3 Hot files — measured, and none is in the manifest

The standing hot-file list was measured at this exact base with eslint's own
`Linter`, because the standard requires an executed figure whenever a hot file
could enter a manifest:

| File | Effective | Ceiling | Headroom | In this manifest |
|---|---:|---:|---:|---|
| `src/components/OutputContainer.jsx` | `599` | `600` | `1` | no |
| `src/domain/worldPulse/peaceTerms.js` | `797` | `800` | `3` | no |
| `src/domain/worldPulse/informationStatecraft.js` | `780` | `800` | `20` | no |

No manifest path carries a `scripts/.size-baseline.json` entry, and none is
added. `treatyDocument.js` and the new leaf sit under the `src/domain/**/*.js`
layer ceiling of `800`; both `.jsx` mounts sit under the `src/components/**`
ceiling of `600`.

### 4.4 Live and post-change census

| Census | Live | Required post-change |
|---|---:|---:|
| receipt-pool registry rows (`GRAMMAR_KIND_REGISTRY`) | `10` | `11` |
| grammar Herald rows (`GRAMMAR_HERALD_KINDS`) | `5` | `5` |
| registered impact kinds (`impactKindWalkers`) | `109` | `109` |
| explicitly routed kinds (`EXACT_SECTION`) | `377` | `377` |
| routed and registered | `103` | `103` |
| estate registered kinds (`kindPoolFloors` `ALL_ROWS`) | `109` | `110` |
| registered minus routed-and-registered | `6` | `7` |
| deliberately unvoiced tokens | `274` | `274` |
| underfloor kinds (frozen backlog) | `28` | `28` |
| `title=` census (`guidanceRegistry` `TITLE_BASELINE`) | `485` | `485` |
| observed-shape reader findings | `1998` | `1998` |

The three existing `test.each(GRAMMAR_KIND_REGISTRY)` sites in
`grammarLifecycleKindPools.walker.test.js` add three runtime cases when the
registry gains one row — measured at this base as `39` tests over `10` rows,
becoming `42` over `11`. The new acceptance file adds eight literal cases. The
full runtime denominator therefore moves exactly `28010 → 28021`; the frozen
known-failure ceiling remains `16`.

The whole lighting tuple moves exactly:

```text
files / parked / credited / test titles / suite titles
2414 / 365 / 2049 / 20000 / 5640
2415 / 365 / 2050 / 20008 / 5641
```

That is exactly `+1` file, `+0` parked, `+1` credited, `+8` test titles, and
`+1` suite title. The new file has one literal `describe` and eight literal
top-level `it` calls. `.each`, looped or conditional registration, nested
describes, `skip`, and `todo` are forbidden. The registry-row edit contributes
zero census titles — the GR-4b-iii-a landing moved the tuple by exactly
`+1/0/+1/+8/+1` while making the identical registry edit.

## 5. Corpus and registration contract

Add `succession_question_open` to `GRAMMAR_RECEIPTS` with the eight annex
families byte-verbatim except for interpolation syntax and the stripped
editorial `[exemplar, R-28]` tag. Add one grammar registry row with exactly:

```text
kind         succession_question_open
significance n/a
audience     public
section      null
pool depth   8
contexts     none
required     [], [npc,settlement], [counterpart,settlement],
             [settlement,counterpart], [], [settlement,counterpart],
             [npc,settlement], []
```

`n/a` carries a declared floor of six in both governing walkers; a depth of
eight clears it.

**Register the kind in NO other home.** This is the load-bearing difference from
GR-4b-iii-a and it is a derivation, not a preference:

- `heraldRouting.js` `EXACT_SECTION` / `KIND_SECTION_DIVERGENCES`: **no row.**
  `section` is `null`, so the kind never enters `GRAMMAR_HERALD_KINDS` and the
  five-join arm never reaches it.
- `chroniclersLetter.js` `KIND_SECTION`: **no row**, same reason.
- `settlementRumors.js` `WHAT_PHRASES`: **no row, and a row would red.**
  `grammarLifecycleKindPools.walker.test.js` asserts that every registry row
  with `section === null` has an undefined `WHAT_PHRASES` entry — a phrase for a
  clause rendered into another surface would be vocabulary with no address.
- `impactKindWalkers.test.js` `EXPECTED_VOICE`: **no row.** That walker's
  denominator is the set of minted `impactKind` literals; a dossier line mints
  none, exactly as `treaty_age_line`, `treaty_true_state_chip`,
  `treaty_lapsed.road_open` and `ran_its_term` mint none and appear nowhere in
  `EXPECTED_VOICE`.

Do not add a grammar party-role override. Like the landed alpha and opening
voices, this family binds the direct acting-court/counterpart axis, not the
obligee/obligor axis, and the composer therefore never calls `grammarSlotRoles`.

Repair these stale comments/titles inside already authorized paths:

- `grammarReceiptPools.js`: the header's "ten governed pools wired through
  GR-4b-iii-a" statement;
- `grammarNews.js`: the module header's "Six of the seven pools" party-binding
  count and the `section` non-null commentary if it states a pool total;
- `grammarLifecycleKindPools.walker.test.js`: every exact pool count and any
  title or assertion still saying ten pools.

- `kindPoolFloors.walker.test.js`: the "move together" warning comment beside
  the registered-minus-routed identity is reworded to name the no-desk class (a
  `section: null` row registers without routing) instead of restating an
  arithmetic this change legitimately and deliberately breaks.

No comment repair may change a test-title cardinality beyond the one new suite's
declared `+8/+1`.

## 6. Exact behavior and identity contract

Create the DISPLAY-owned leaf `src/domain/display/treatySuccessionDossier.js`.
It exports:

```js
successionQuestionOpenLines(worldState, pairKey, terms)
```

returning `string[]` — **always an array, never null and never absent**.

The function follows this exact order:

1. Read `worldState.proposals` when it is an array; otherwise return `[]`.
   **Never call `resolveSuccessionQuestions` or any other writer.** This leaf is
   a reader; the pending rows it narrates were written by the late treaty stage
   in an earlier tick.
2. Return `[]` immediately when `terms` is not a non-empty array (CR-GR4B-14:
   a treaty with no written term does not entail this pool).
3. Read the exact treaty from `treatyLedgerOf(worldState)[pairKey]`. Absent or
   non-record ⇒ `[]`.
4. Walk `worldState.proposals` **in its existing order**, adding no sort of its
   own. The landed `successionQuestionsForTick` codepoint-sorts seat-transition
   rows and treaty keys before insertion, so the persisted array order is
   already canonical; a second sort here would be a divergent authority.
5. For each row, require `proposal.status === 'pending'` exactly, then validate
   the complete `proposal.outcome` through `successionQuestionPayloadTuple`.
   Do not hand-walk or partially trust `proposalPayload`.
6. Require `question.treatyKey === pairKey` and
   `question.settlementId !== question.otherId`. Two distinct questions may
   lawfully share one treaty, so **no per-treaty uniqueness, dedupe, or
   court-pair pairing may be applied** — each surviving row renders its own
   line.
7. Resolve both persisted court names through `treatyOrientationOf(treaty)`.
   An id echoed as a name is unresolved and yields no line for that row.
8. Resolve the fallen holder only through the selected treaty's total reader:

   ```js
   swornPartiesOf(treaty).find(
     (stamp) => stamp.settlementId === question.settlementId
       && stamp.npcId === question.npcId,
   )
   ```

   No successor, ruler, standings, or roster lookup is permitted. The new leaf
   must not contain the dotted roster token even in comments, because an
   existing raw-source census reads comments.
9. Compose through `grammarReceipt('succession_question_open', outcomeId, {
   settlement, counterpart, npc })` with no `context` argument. The validated
   outcome ID is the seed; `grammarReceipt` namespaces every seed with its own
   kind, so this pool's pick is independent of the opening beat's pick on the
   same identity. Court-pair, NPC, question-key truncation, tick-only, or
   array-index seeds are forbidden.
10. Append `receipt.line` only. A null receipt yields no line for that row.
    Use no locale operation, draw, clock, `Date`, or `Math.random`. The leaf
    mutates nothing: not `worldState`, not the ledger, not the proposals, not
    the treaty, not the document.

### 6.1 The read-model key

`treatyDocument.js` threads `worldState` into `decorate` and adds exactly one
key to every dressed document:

```js
successionLines: successionQuestionOpenLines(worldState, doc.pairKey, doc.terms)
```

**The key is ALWAYS PRESENT and is `[]` when nothing qualifies.** This is not a
style choice and a drop-when-absent spelling is a STOP:
`tests/property/treatyLifecycleVoiceDormancyFence.test.js` — a file deliberately
outside this manifest — asserts that the lit read-model grows exactly the key
set `['ageYears']` over the dark one and that every other key is
`JSON.stringify`-equal across the flag. An always-present, always-`[]`-when-dark
array keeps that fence green with no edit; a conditional key reds a file this
packet has no authority to touch.

All three existing entry points thread `worldState`:
`renderTreatyDocument`, `renderTreatiesForSettlement`, `renderAllTreaties`.
`decorate`'s existing `doc` / `treaty` parameters, the `ageLine` call, and every
other key keep their exact current behavior.

### 6.2 The two mounts

Both mounts render the lines immediately **beneath** the existing longevity
line, in array order, as plain italic prose in the muted register — the exact
`ageLine` idiom at each surface:

- `src/components/map/TreatyPanel.jsx`, inside `TreatyCard`, after the
  `doc.ageLine` block, `data-testid="treaty-succession-question-line"`;
- `src/components/new/tabs/WarFaithTab.jsx`, inside `TreatyBlock`, after the
  `doc.ageLine` block, the same `data-testid`.

Three spellings are forbidden in both blocks, and each is a STOP:

1. **No `title=` prop.** The `title=` census sits at exactly `485` against a
   shrink-only baseline of `485` — measured at this base — so a single new
   native tooltip reds the walker.
2. **No tinted wash and no rounded/shadowed chrome.** The deep-craft kill-list
   ratchets are EXACT (`toBe`, not `<=`) over `src/components/**`. No new line
   may spell `borderRadius`, `boxShadow`, `rgba(`, or any of
   `SLATE_BG|AMBER_BG|GREEN_BG|RED_BG|BLUE_BG|GOLD_BG|successBg|dangerBg|infoBg|warningBg`.
3. **No em dash and no exclamation point in JSX text**, and no new `.jsx` file.
   The blocks render `{line}` and author no literal sentence of their own.

`src/pdf/lib/liveWorld.js` consumes the same read-model through an **explicit
field projection**, so the new key is inert for the war-room PDF export and no
PDF edit is authorized — which is exactly what the corpus block's two-mount
address requires (the sibling `treaty_age_line` block names the PDF; this one
does not).

## 7. Exact ten-path implementation manifest

| Action | Path | Region | Maximum effective delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `src/domain/display/treatySuccessionDossier.js` | composer and pure helpers | `<=110` | Read pending validated succession questions for one exact treaty and return zero-or-more canonically ordered authored lines; mutate nothing |
| `MODIFY` | `src/domain/display/treatyDocument.js` | three render entries and `decorate` | `<=10` | Thread `worldState` to `decorate` and add the always-present `successionLines` key |
| `REGISTER` | `src/domain/worldPulse/grammarReceiptPools.js` | `GRAMMAR_RECEIPTS` | `<=14` | Add eight annex-verbatim families and correct the stale local pool count |
| `REGISTER` | `src/domain/worldPulse/grammarNews.js` | `GRAMMAR_KIND_REGISTRY` | `<=8` | Add one exact `n/a`/`public`/`null`-section row, no contexts and no party-role override |
| `MODIFY` | `src/components/map/TreatyPanel.jsx` | `TreatyCard`, after the age line | `<=5` | Render the lines in order in the muted italic register; no `title=`, no wash |
| `MODIFY` | `src/components/new/tabs/WarFaithTab.jsx` | `TreatyBlock`, after the age line | `<=5` | Same block, same register, same prohibitions |
| `CREATE` | `tests/domain/treatySuccessionDossier.test.js` | one describe, B1-B8 | `n/a` | Exactly eight literal top-level `it` cases; no registration indirection |
| `TEST` | `tests/lint/grammarLifecycleKindPools.walker.test.js` | `EXPECTED`, `GR4_KINDS`, exact counts | `n/a` | Move the pool census `10 → 11`, add the exact registry tuple and slot vectors, leave the Herald five untouched, add no title |
| `TEST` | `tests/lint/kindPoolFloors.walker.test.js` | `REGISTERED_KIND_COUNT` and the routing identity | `n/a` | Move `109 → 110` and the registered-minus-routed identity `6 → 7`; `ROUTED_TOKENS`, unvoiced `274` and underfloor `28` do not move; no title motion |
| `TEST` | `tests/lint/sovereigntyLightingContract.walker.test.js` | whole `CENSUS` | `n/a` | Re-derive whole and record exactly `2415/365/2050/20008/5641` |

Generated artifacts: **NONE**. The dossier-state prose generator is driven by
`docs/content/RECEIPT_POOLS_DOSSIER_STATE.md` `DS-` blocks and this slice adds
none.

## 8. Ordered coding sequence and preflight

Before editing, the coordinator dispatches the sealed packet:

```sh
npm run implementation:dispatch -- GR-4B-IIIB
```

The capsule must prove exact authority bytes, branch/base ancestry, both CREATE
targets absent, every pre-existing target hash, required-symbol evidence, clean
targets, and the annex hash. Then:

1. Capture the three hot-file effective counts, the `title=` census, the four
   kill-list counts, both TypeScript ratchets, the observed-shape count, the
   focused green predecessor tests, and the whole census.
2. Create the one test file with the exact eight titles below and establish the
   intended missing-behavior reds without registering a ninth title.
3. Add the eight-family corpus and the one grammar row.
4. Implement the pure composer leaf.
5. Thread `worldState` through the three render entries and add the
   always-present read-model key.
6. Mount both surfaces, in the order `TreatyPanel.jsx` then `WarFaithTab.jsx`.
7. Update the two governed walkers, re-derive the whole census once, then run
   focused checks.
8. Execute the eight disposable mutants outside the shared worktree, restore
   every target digest exactly, and rerun focused green.
9. Run the attached sealed runner once. Then obtain an independent bare
   `check:tail` receipt and a separate boot smoke. Do not start soak or
   adjacent work.

## 9. Acceptance matrix — exactly eight titles

All cases live under one literal describe title:
`GR-4b-iii-b — the open-question dossier line`.

| ID | Exact `it` title | Required observation |
|---|---|---|
| B1 | `a validated pending question renders exactly one fully addressed dossier line` | One valid pending row against a stamped, live-term treaty yields exactly one line on the document read-model; the line is an exact member of the authored eight; the proposal, treaty, ledger and oath stamp are byte-identical before and after the read. |
| B2 | `all eight annex-verbatim families and slot vectors are reachable` | Actual composer calls with real outcome identities deterministically reach all eight exact corrected lines and exact slot vectors; `{npc}` resolves to the fallen holder named on the acting court's `sworn` stamp; no successor name, no `{band}`, no digit, no residual token, no em dash and no exclamation point appears in any rendered line. |
| B3 | `a dark mechanism leaves both mounts and the read-model byte-identical` | With routine-major approval absent, with it declared false, and with the oath holder dark, no pending succession row exists; `successionLines` is present and `[]` on every document; `JSON.stringify` of the whole read-model and the rendered markup of both mounts are byte-identical to the same world built at base. |
| B4 | `terminal, unvalidated and foreign rows render no line` | Applied, dismissed and expired statuses, a malformed or truncated payload, a mismatched outcome identity, a foreign `proposalPayload.kind`, and a duplicate id all render nothing, and none of them throws. |
| B5 | `an unresolvable address renders nothing, never a partial line` | A missing ledger entry, an unresolved court name, an id echoed as a name, an absent or half-written oath stamp, a stamp naming a different `npcId`, and a treaty with zero written terms each yield zero lines while the pending row stays untouched. |
| B6 | `two questions on one treaty render two lines in canonical order` | Two distinct validated pending questions against the same treaty key render two distinct lines in the persisted proposal order; reversing the input array's insertion order produces identical bytes in identical order; no per-treaty uniqueness or court-pair dedupe is applied. |
| B7 | `the same world is byte-stable, draws nothing, and writes nothing` | Repeated and JSON-round-tripped worlds produce identical bytes; draw and clock sentinels remain zero; the composer returns `[]` rather than throwing on a null, empty or non-record world; and no call mutates any input. |
| B8 | `a real late-stage succession reaches both mounts and no other surface` | A real GR-4d run through the late treaty stage inserts one pending proposal, and both `TreatyPanel` and `WarFaithTab` render that exact line beneath the age line; the `src/pdf/lib/liveWorld.js` projection of the same world carries no succession key or line. |

Assertions may loop inside a case. Test registration may not.

## 10. Mutation proof — eight controlled mutants

No standing mutation manifest or sweep row is owed: the new test filename is
outside the enforced mutation-test nomenclature and directories. Test trust is
proved with eight disposable source mutants in an isolated immutable candidate.
For every mutant, prove source bytes changed, require nonzero test exit and the
named B-title red, restore the exact pre-mutant SHA-256, and rerun focused green.

| Mutant | Controlled change | Required conviction |
|---|---|---|
| M1 | suppress the line append | B1 and/or B8 |
| M2 | replace the receipt seed with one constant | B2 |
| M3 | accept any status instead of exactly `pending`, or skip `successionQuestionPayloadTuple` | B4 |
| M4 | drop the `question.treatyKey === pairKey` guard so every question renders on every treaty | B6 |
| M5 | resolve `{npc}` from the first `sworn` stamp instead of the exact `settlementId`+`npcId` match | B2 and/or B5 |
| M6 | add a sort, or make array insertion order the authority | B6 |
| M7 | make `successionLines` drop-when-absent instead of always present | B3 |
| M8 | revert the `WarFaithTab.jsx` mount so only one surface renders | B8 |

A no-op plant, zero exit, wrong-title red, broad crash, dirty-tree override, or
non-exact restoration is a STOP. Do not run the estate-wide shared-tree sweep.

## 11. Coupling, OSR, hazard, and gate obligations

### Coupling

The `couplingInclusion` layer table classifies `src/domain/worldPulse/**` and
`src/domain/spatial/**` only; `src/domain/display/**` is unlayered and outside
that census, so a DISPLAY-owned leaf mints no layer pair and moves no layer
floor. **Measured at this base: every module the composer imports is already in
`treatyDocument.js`'s transitive closure (222 modules), and the landed
`treatySuccessionOpeningVoice.js` closure adds exactly zero modules that
closure does not already carry.** Run `couplingInclusion.walker.test.js`. Do not
edit a coupling registry or baseline. Any successor-name INTERIOR import or
measured new pair is a source-repair STOP.

### Observed-shape reader law

Use `successionQuestionPayloadTuple` for the closed outcome projection,
`treatyLedgerOf` for the canonical ledger, `swornPartiesOf` for the oath stamp,
and `treatyOrientationOf` for the court names. Do not manually traverse
arbitrary proposal payload shapes or copy any reader. Run
`check:observed-shape-readers` before and after; it stands at exactly `1998`
findings at this base. A new file carries a per-identity ceiling of zero, so
**any** new finding must be repaired in the leaf or the packet stops; the
observed-shape baseline is forbidden.

### Existing hazard dispositions

- **HZ-SIZECEILING:** no hot file is in the manifest; the three standing hot
  files were measured at this base and are untouched. The new leaf and every
  modified file stay under both the packet and repository ceilings, and no
  `scripts/.size-baseline.json` entry is added or moved.
- **HZ-NEWFILECEILING:** both TypeScript ratchets remain zero-error for both new
  paths; no baseline raise.
- **HZ-READERNOWRITER:** canonical readers only; the leaf holds no writer and
  calls no resolver.
- **HZ-TITLEBASELINE:** the `title=` census is at `485` against a shrink-only
  baseline of `485` — **no headroom**. Neither mount may add a `title=` prop.
- **HZ-KILLLIST:** the four deep-craft ceilings assert `toBe`, not `<=`. Both
  mount blocks must leave `borderRadius`, `boxShadow`, `rgba(` and every
  `*_BG`/`*Bg` token count unmoved.
- **HZ-BANKEDVOICE:** all four `tests/copy/voiceMechanics.test.js` arms are
  **banked known failures** at this base, so the estate-wide em-dash and
  exclamation-point walker will **not** red on a new tell in a
  `src/domain` string literal — the banked per-test identity absorbs it. B2
  therefore asserts the eight rendered lines carry zero U+2014 and zero `!`
  directly, and the corpus was verified clean of both at this base.
- **HZ-DORMANCYFENCE:** `successionLines` must be always present and `[]` when
  empty, or `treatyLifecycleVoiceDormancyFence.test.js` — outside this manifest
  — reds on its exact `added` key-set assertion.
- **HZ-PDFPROJECTION:** `src/pdf/lib/liveWorld.js` projects an explicit field
  list; do not add the key there, and do not widen the projection.
- **HZ-CROSSHOME:** run the news-voice, headline and prose-family contracts; no
  baseline moves.
- **HZ-MUTANTNOOP / HZ-TESTVACUITY:** controlled-byte and exact-title mutation
  receipts above.
- **HZ-DISTBOOT:** the new static import edge requires a separate boot smoke
  even though the module closure does not grow.
- **HZ-GATEMUTEX:** every Vitest invocation runs through the governed mutex; no
  parallel Vitest owner.
- **HZ-SINGLEWRITER:** one coding writer and one coordinator; no shared-target
  edits from another lane.
- **HZ-PIPEEXIT:** final full-gate evidence is bare `check:tail` with true exit.

Run both hazard-registry and pre-mortem validators. A new working warning or
trigger requiring an eleventh path is a STOP and chair return.

### Sealed checks

The structured manifest owns exactly ten executable children:

1. packet validation;
2. hazard-registry validation;
3. pre-mortem validation;
4. scoped ESLint over the exact ten paths;
5. the mutexed direct/predecessor/contract battery, which must include
   `treatyDossierTab.test.jsx`, `treatyLifecycleVoiceDormancyFence.test.js`,
   `sovereigntyWaveCloseIntegration.test.js`, `successionQuestion.test.js`,
   `peaceTermsWave3.test.js`, `deepCraftKillList.test.js`,
   `guidanceRegistry.walker.test.js`, `impactKindWalkers.test.js` and
   `treatySuccessionOpeningVoice.test.js` alongside the four manifest test
   files;
6. full TypeScript ratchet;
7. strict-domain TypeScript ratchet;
8. observed-shape reader validation;
9. bare full `check:tail`;
10. separate `smoke:boot`.

The sealed runner is not one of its own children. After the attached sealed
receipt, the coordinator independently repeats the bare full gate and boot
smoke for landing evidence.

## 12. Completion receipt and STOP conditions

The implementer returns:

- exact base, candidate SHA, and manifest-ordered target SHA-256 list;
- exact changed-path set and `+/-` diff statistic;
- raw/effective line counts and per-file deltas, taken with eslint's own
  `Linter` and never `wc -l`;
- B1-B8 direct result and the wider battery result;
- all live/post registry counts, the `title=` census, the four kill-list counts,
  and the whole census derivation;
- eight mutant commands, changed-byte proof, exact failing titles/exits, and
  post-restore digests;
- both TypeScript ratchets and the observed-shape result;
- capsule digest, session/seal IDs, and complete attached runner receipt;
- independent `check:tail` exit, log path/SHA-256/lines/bytes, total tests,
  frozen known failures, build/prerender total, and strict-dist count;
- independent boot-smoke exit, chunk/edge/shell figures and any classified
  benign diagnostic;
- explicit statement: **no soak started**.

Stop and return to the chair on any of:

- authority, base, branch, annex, required-symbol, fingerprint, or CREATE
  absence mismatch;
- an eleventh path, a third registration file, a ninth acceptance title, or any
  inherited `12 → 14` claim;
- any movement in the `title=` census, any kill-list ceiling, any hot file, or
  any `scripts/.size-baseline.json` entry;
- a conditional or drop-when-absent `successionLines` key;
- an `impactKind` mint, a Herald desk, a `WHAT_PHRASES` row, an `EXPECTED_VOICE`
  row, a chronicler section, or a PDF projection edit;
- any new flag, tuning key, state family, migration, golden, baseline, ceiling,
  dependency, terminal-packet, annex, mutation-manifest, hazard-registry,
  coupling-registry, voice-mechanics-baseline, or observed-shape artifact edit;
- a registry/census/runtime denominator different from the exact figures above;
- a successor or roster lookup, a grouped-instrument or per-treaty-uniqueness
  claim, a fabricated slot, a partial address, a terminal voice, a duplicate
  line, a sort of the composer's own, or a presentation failure that touches
  mechanics;
- ungoverned Vitest concurrency, a recursive sealed runner, a failed or
  ambiguous mutant, an incomplete restore, a nonzero validator/gate/smoke, or
  foreign dirt;
- any soak work.

This packet ends after the dossier line lands and its receipts are attached. It
does not continue into GR-4b-ii, GR-5, or soak.

### 12a. Executed preflight receipts — 2026-08-14, verified base `2dd09360`

Every figure in this packet was measured in the drafting run. Commands and
outputs are quoted in the drafting report beside this file.

- `git log -1` at the minifold worktree →
  `2dd09360e63fcca2b278dcf4d3e9c7f949434e55 (HEAD -> claude/composite-r4)`;
  `git status --porcelain` printed nothing.
- `npm run validate:packets` → `valid: 34 packets (0 READY)`, true exit `0`.
- eslint `Linter` under `max-lines` `{skipBlankLines,skipComments}` →
  `peaceTerms.js 797`, `informationStatecraft.js 780`,
  `OutputContainer.jsx 599`, `treatyDocument.js 180`,
  `grammarReceiptPools.js 93`, `grammarNews.js 110`, `TreatyPanel.jsx 129`,
  `WarFaithTab.jsx 168`, true exit `0`.
- `countTitles()` re-executed over `src/` → `title= census = 485`, against
  `TITLE_BASELINE = 485`.
- `npx vitest run tests/lint/sovereigntyLightingContract.walker.test.js` →
  `1 passed (1) / 33 passed (33)`, true exit `0`, census
  `2414/365/2049/20000/5640`.
- `npx vitest run tests/lint/grammarLifecycleKindPools.walker.test.js` →
  `39 passed (39)`; `tests/lint/kindPoolFloors.walker.test.js` →
  `12 passed (12)`.
- `npm run check:observed-shape-readers` →
  `observed-shape readers: 1998 finding(s), exactly matching the frozen
  inventory`, true exit `0`.
- The eight authored lines and their eight slot vectors were extracted through
  `tests/helpers/receiptAnnex.js` at this base and match section 1 exactly;
  both `# GR-4` and `# GR-5` anchors matched exactly once, and the
  `### succession_question_open` heading matched exactly once without colliding
  with `### succession_question_opened`.
- Both CREATE targets were checked and are absent.
- The nine-file base battery ran `8 passed / 1 failed (9)` and
  `129 passed / 4 failed (133)`; all four failures are
  `tests/copy/voiceMechanics.test.js` arms already present by name in
  `scripts/.test-ratchet-baseline.json`.
