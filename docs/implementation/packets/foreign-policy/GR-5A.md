# Foreign Policy / GR-5A — the monotone memory (`treatyRenewalEnabled`)

- **Status:** READY
- **Train:** `refs/trains/gr-5a`, **ONE member**. Plan: `laneTC11-TRAIN-PLAN.md`.
- **Chair authority:** `OWNER_DECISION_QUEUE.md` **§47** (2026-08-15) — all six open questions
  signed and `J-TC11-1..5` ratified. **O-5** accepts the P1 exit-1 with exactly four named
  `requiredSymbols` rows; **O-6** records that this member authors **ZERO** numbers, which is why it
  lands without a chair value signature.
  …and **§49** (2026-08-15) — the first compile's I1 STOP RATIFIED and the **SEVENTH PATH SIGNED**
  (`{TEST, tests/domain/subsystemRowsVirtual.test.js}`, three module-scope literal edits, zero new
  titles, census-neutral). This packet is the re-authored **P1′**; the parked P1 stays as evidence
  at `refs/trains/gr-5a-p1-parked`. ⭐ §49 ruling 3 makes the flag-mint seventh path STANDING LAW
  for every volume. Every §47 tripwire is unchanged.
- **Volume:** GR — Foreign Policy / the pact-grammar voice family.
- **Family preamble, cited BY SHA-256** (the §P-header citation law):
  `docs/implementation/preambles/GR-PREAMBLE.md`
  sha256 `d2715a7b0a0cfe6859127fb69339019ceccdc860dd24602faf2bb07bc02095d9`
  ⭐ **RE-COMPUTED at P1 by the executing lane and identical.** The git blob id `ce583f02…` is a
  different digest and is **not** what the citation law demands.
- **Branch:** `claude/composite-r4`
- **Verified base:** `claude/composite-r4` at `6cd18ad3e9b8be768d0e69ce1edc641cb5b626db`
- **Base-state capsule:** `de214782`; HEAD is its **docs-only child** (executed: four paths, all
  under `docs/implementation/`, **0** non-docs paths), so the capsule is citable as executed under
  its `consumptionLaw` / chair judgment **J-T1** — and every row this manifest touches is
  re-executed in §4 regardless.

---

## 1. Reconciled authority

| Source | What it binds |
|---|---|
| `docs/implementation/preambles/GR-PREAMBLE.md` @ the sha above | §P1–§P9 in full: census law, registration cost, hazard dispositions, mutant hygiene, gate law, the twenty-one STOP conditions, the §28 delete-clause law, the §31 anchor preflight |
| `docs/implementation/PACKET_STANDARD.md` | statuses, dispatch lifecycle, scope budget, hot-file law, edge-case budget, STOP conditions, completion receipt, train landings |
| `docs/DESIGN_FP_ARCH_GR.md` §1–§8 (GR-5 at `:576`) | the compiled implementation layer; L1–L9; §3's flag law and its 2026-08-05 ERRATUM; §4's lifecycle-paths clause; V-7 |
| `docs/DESIGN_FP_GRAMMAR.md` §4 + §5 GR-5 (`:1154`) | the design narrative and the `worstObservedEver` contract |
| `OWNER_DECISION_QUEUE.md` §41–§45 | §42/§43 the value-derivation law · §44 `R35` path reservation · §45.2 the mandatory validator simulation |

⛔ **Where this packet and the volumes disagree, the EXECUTED measurement in §3 wins and the volume
is docketed.** Five volume claims are refuted below; each carries its corrected fact.

---

## 2. Outcome and boundary

### 2.1 What lands

**A treaty remembers the worst it was ever seen to do.** Today `treaty.complianceState` is
overwritten every advance from the current tick's observation, so a pact that was strained for a
decade and honoured last week is, on the parchment, a pact that was never strained. GR-5A adds one
conditional, monotone, drop-when-absent field — `worstObservedEver` — written at the one site that
already computes the tick's worst observation, and **only** when `treatyRenewalEnabled === true`.

It is the substrate the rest of GR-5 stands on: the conversion gate reads it BY NAME (*"a
`compelled_alliance` reaching its window with `worstObservedEver === 'honored'`"*), and GR-6's
fraying occasion reads it BY NAME (`worstObservedEver ?? 'honored'`). Neither can be written until
the field exists, and the field cannot exist without the flag.

### 2.2 What it deliberately does NOT build — the non-goals, named

⛔ No renewal window · no `renewal` trigger producer · no proposal open/answer · no mid-term
renegotiation demand · no ask cap · no conversion · no relationship-label proposal · no lineage act
· no vocabulary widening of any kind · **no news kind, no Herald desk, no receipt pool, no
`WHAT_PHRASES` row, no chronicler row** · no dossier line, no `treatyDocument.js` edit, no PDF edit,
no read-model key · no tuning key, band, threshold or number · no soak · no migration · no golden
re-record.

⭐ **GR-5A is DARK-COMPLETE.** With the flag absent or `false`, no byte of any world changes.

### 2.3 ⛔ GR-5 AS CHARTERED IS REFUSED IN PART

The volume's GR-5 charters six behaviours and five bands. Five successors are named in the train
plan §11 (GR-5b window · GR-5c renegotiation · GR-5d conversion · GR-5e voice · GR-5f dossier), each
with the measured reason it cannot compile at this base — a chair-signed band, or a producer that
does not exist. **This packet is slice (a) alone and its landing releases none of them.**

---

## 3. ⭐⭐ THE FIVE REFUTED PREMISES THIS PACKET IS BUILT ON

Every claim re-executed at `6cd18ad3`. Full ten-row SPV table in the train plan §3.

### R-GR5-5 (STOP-CLASS) — "the one-line monotone fold" is not expressible as one line

ARCH §5 GR-5 budgets *"the one-line monotone fold at the V-7 site"*. `peaceTerms.js` measures
**797 of 800** with no `scripts/.size-baseline.json` entry and no door, and §P8 stop 6 makes **any**
movement of a hot file's measured count a STOP; §P5 requires **net zero**. ⇒ **A one-line fold is a
STOP, not a budget.** THE CURE, and it is the file's own landed idiom: **three ONE-FOR-ONE
replacements** (§6.1) for a net of **zero effective and zero raw**, numstat `3/3`. Precedent
executed: GR-4b `dd457b9a` did exactly this at numstat `2/2`, `797 → 797`, and this file already
carries two-statement lines at `:349` and `:784`. `eslint.config.js` declares no `max-len` and no
`max-statements-per-line` rule.

### R-GR5-1 — V-7's addresses have rotted

The claim's mechanism is intact; its hand-keyed line numbers are wrong. `let worstObserved` is
minted at **`:694`** (not `:634`), folded at **`:741`**, written at **`:759`** (not `:676`).
⛔ **Navigate by symbol.** The standing hand-keyed-address law.

### R-GR5-2 — the lineage vocabulary is not what §4 says

Executed: `PACT_LINEAGE_ACTS = ['amended','broken_by_war','disavowed_by_succession','formed','war_ended']`.
The volume's §4 set is `{formed, amended, renewed, renegotiated, converted}` — **three absent, three
unnamed present**. Pinned EXACTLY (`toEqual`) at `tests/domain/pactAmendment.test.js:50` and
asserted codepoint-sorted at `tests/domain/successionQuestion.test.js:427`. **GR-5A widens nothing**,
so both pins stay green.

### R-GR5-3 — GR-5 mints no provenance value, and two members are producer-less

Executed: `PACT_PROVENANCE = ['converted','dictated','negotiated','renewed']` — GR-2 landed all
four. ⚠ AND the only provenance write anywhere in `src/` is `pactFormation.js:495
provenance: 'negotiated'`, so **`renewed` and `converted` are orphan vocabulary members today**.
Recorded so GR-5b/5d do not re-mint what exists (**O-4**).

### R-GR5-4 — "endings entries … join pact endings" names neither of the two homes

`PACT_ENDINGS` is **two distinct exported symbols**: `pactAmendment.js` (6 formation/closure members)
and `treatyLifecycleVoice.js` (3 observation members: `ran_its_term`, `hollowed_detected`,
`hollowed_quiet`). `pactAmendment.js`'s own header acknowledges the collision. The charter sentence
is unresolvable as written; **chair question O-2**, and out of GR-5A's scope either way.

---

## 4. Verified tree contract and executed preflight receipts

⭐ **Every figure below was executed by this lane at `6cd18ad3`, bare, in-shell, unpiped, with the
exit captured. Nothing is inherited or delegated.**

### 4.1 Base state

| # | Instrument | Result |
|---|---|---|
| 1 | `git rev-parse HEAD` / `git status --porcelain` | `6cd18ad3…` / **0 lines**, at open and at close |
| 2 | `node scripts/implementation-packets.mjs validate` | `valid: 44 packets (0 READY)`, **TRUE_EXIT=0** |
| 3 | `node scripts/check-observed-shape-readers.mjs` | `1998 finding(s), exactly matching the frozen inventory`, **TRUE_EXIT=0** |
| 4 | `node scripts/check-tuning-bands.mjs` | `manifest v2 OK: 9 ratified soak bands, all valid`, **TRUE_EXIT=0** |
| 5 | `npx vitest run tests/lint/negativeAssertionAnchor.walker.test.js` | **9 passed, TRUE_EXIT=0** |
| 6 | `shasum -a 256 docs/implementation/preambles/GR-PREAMBLE.md` | `d2715a7b0a0cfe6859127fb69339019ceccdc860dd24602faf2bb07bc02095d9` |

⚠ **ONE CAPSULE ROW IS STALE AND IS RE-EXECUTED RATHER THAN CITED.** `BASE_STATE.json` records
`validatePackets: "44 packets / 1 READY"`; the docs-only window `de214782..6cd18ad3` flipped HB-2
LANDED. Receipt 2 above is the citable figure: **44 / 0 READY**.

### 4.2 Hot files — measured with eslint's own `Linter`, `max-lines` `{ skipBlankLines: true, skipComments: true }`

| File | Effective | Ceiling | Headroom | In manifest |
|---|---:|---:|---:|---|
| `src/domain/worldPulse/peaceTerms.js` | **797** | 800 | **3** | ⛔ **YES — pays NET ZERO** |
| `src/domain/worldPulse/convergence.js` | 798 | 800 | 2 | no |
| `src/domain/worldPulse/informationStatecraft.js` | 780 | 800 | 20 | no |
| `src/components/OutputContainer.jsx` | **n/a** | 600 | — | no (⚠ does not reproduce — train plan §2.2, **O-3**) |
| `src/domain/worldPulse/pactAmendment.js` | **127** | 800 | 673 | YES |
| `src/domain/worldPulse/simulationRules.js` | 305 | 800 | 495 | YES |
| `src/domain/certification/subsystemRowsVirtual.js` | 573 | 800 | 227 | YES |

Executed: `scripts/.size-baseline.json` carries **15** keys and `peaceTerms.js` is not one of them.

### 4.3 Required live symbols (navigate by symbol, never by line)

| Path | Symbol | State |
|---|---|---|
| `src/domain/worldPulse/peaceTerms.js` | `export function advanceTreaties(` | EXISTS — the mover |
| `src/domain/worldPulse/peaceTerms.js` | `let worstObserved = 'honored';` | EXISTS `:694` — the fold's source |
| `src/domain/worldPulse/peaceTerms.js` | `treaty.complianceState = worstObserved;` | EXISTS `:759` — the edit site |
| `src/domain/worldPulse/peaceTerms.js` | `import { absorbWarEndIntoStandingPact } from './pactAmendment.js';` | EXISTS `:151` — the import site |
| `src/domain/worldPulse/peaceTerms.js` | `const lifecycleVoiceLit = treatyLifecycleVoiceActive(worldState);` | EXISTS `:350` — the hoist site |
| `src/domain/worldPulse/peaceTermsPrimitives.js` | `export function rankState(s)` | EXISTS `:96` — the comparator, **already imported by peaceTerms.js at `:157`** |
| `src/domain/worldPulse/pactAmendment.js` | `export const PACT_PROVENANCE` | EXISTS — the home's vocabulary neighbour |
| `src/domain/worldPulse/pactAmendment.js` | `function recordOf(value)` | EXISTS — the OSR-safe read idiom this wave must copy |
| `src/domain/worldPulse/simulationRules.js` | `export const ENGINE_GATED_VIRTUAL_RULE_KEYS` | EXISTS `:185`, **19 members**, codepoint-sorted |
| `src/domain/worldPulse/pactProposals.js` | `pactFormationActive` | EXISTS — the gate-in-a-pact-leaf precedent |

**CREATED by this packet** (each is a P1 interior red, §7 of the plan):
`pactAmendment.js :: export function treatyRenewalActive(worldState)` ·
`pactAmendment.js :: export function worstObservedEverAfter(` ·
`pactAmendment.js :: export function worstObservedEverOf(treaty)` ·
`simulationRules.js :: 'treatyRenewalEnabled'`.

### 4.4 Live and post-change censuses

| Figure | Live (executed) | Post-change | Instrument |
|---|---|---|---|
| lighting census | `2428/366/2062/20107/5653` | **`2429/366/2063/20115/5654`** | `sovereigntyLightingContract.walker.test.js:4195` |
| runtime tests / frozen | 28130 / 16 | **28138** (FLOOR) / **16** | `test:ratchet --runtime-tests` |
| `ENGINE_GATED_VIRTUAL_RULE_KEYS` | **19** | **20** | executed import |
| `GRAMMAR_KIND_REGISTRY` / `GRAMMAR_RECEIPTS` / `GRAMMAR_HERALD_KINDS` | 12 / 12 / 6 | **12 / 12 / 6** | executed import — ⭐ no kind minted |
| `ROUTED_TOKENS` / `REGISTERED_KIND_COUNT` / registered-minus-routed / unvoiced / `REGISTRIES` | 378 / 111 / 7 / 274 / **9** | **all unmoved** | `kindPoolFloors.walker` — ⛔ `REGISTRIES` off 9 is a STOP |
| `PACT_LINEAGE_ACTS` / `PACT_ENDINGS` / `PACT_PROVENANCE` / `PACT_TRIGGERS` / `PACT_TRIGGERS_PRODUCED` | 5 / 6 / 4 / 5 / 4 | **unmoved** | executed import |
| observed-shape findings | **1998** | **1998** | `check-observed-shape-readers.mjs` |
| `typecheck:ratchet` / `typecheck:domain:strict` | `173/173` / `1134/1134` | EXACT floors | named with config AND window |
| `ARGUED_ROSTER_CEILING` / `UNLAYERED_BASELINE_CEILING` / `LAYER_FLOORS.GRAMMAR` | 19 / 179 / 42 | **unmoved** | `couplingInclusion.walker` |
| `title=` census / kill-list / soak bands | 485 / `85/69/167/163` / 9 | **unmoved** | no mount, no token, no number |

⭐ **CENSUS-HOLDER: THE ROW IS FREE.** Executed at `6cd18ad3`: 44 rows, status histogram
`{LANDED: 43, SUPERSEDED: 1}`, **zero non-terminal rows**, so no packet reserves the census walker
or any other path this manifest names. GR-5A takes the reservation at P1 and is the sole holder.

---

## 5. Exact state, behaviour and lifecycle contract

### 5.1 The field

```
treaty.worstObservedEver : 'honored' | 'strained' | 'defaulted'
```

- **MONOTONE.** It only ever moves up the `rankState` ordering (`honored 0 < strained 1 < defaulted 2`).
  It never moves down, and no code path clears it.
- **OBSERVED, not true.** Its source is `comp.observedState` as already folded into the local
  `worstObserved` — the fogged register. ⛔ It is not `trueState`, and it carries no DM-only truth,
  so it needs no `includeGroundTruth` projection and adds no key to any public payload.
- **CONDITIONAL, drop-when-absent (T4).** Absent, never `null`, never backfilled — the
  `sellerId`/`buyerId`/`swapId`/`assetId` precedent byte-for-byte, and the `treatyTicksPerYear`
  provenance discipline (V-5) verbatim.
- **READ `?? 'honored'`.** It records history **since lighting**, and that is DECLARED, not implied.
- **ONE WRITER.** Executed: `treaty.complianceState` is assigned at exactly two treaty-level sites,
  both inside `peaceTerms.js` (`:674` the repudiation shell, `:759` the PASS-2 fold). The
  peaceTerms **writer family** is the one writer of treaty records, and `advanceTreaties` PASS 2 is
  the one writer of this field.

### 5.2 Exact transitions

| prior `worstObservedEver` | this tick's `worstObserved` | next | receipt |
|---|---|---|---|
| absent | `honored` | `'honored'` | first lit observation materializes the key |
| absent | `strained` | `'strained'` | |
| `'strained'` | `'honored'` | **`'strained'`** | ⭐ THE WHOLE POINT — the record remembers |
| `'strained'` | `'defaulted'` | `'defaulted'` | |
| `'defaulted'` | anything | `'defaulted'` | terminal |
| any | any | **unwritten** when the flag is absent or `false` | dark ⇒ byte-identical |

**Two branches are excluded BY CONSTRUCTION and both are DECLARED, not discovered:**
- **The lapse/prune branch** (`:745`–`:757`): when every term has expired, `worstObserved` is
  vacuously `'honored'` (the file's own comment at `:751` records this), the record is `delete`d
  from the ledger and the loop `continue`s at `:756` — **before** the fold at `:759`. Nothing is
  written, and nothing survives to be written to. Pinned by **A7**.
- **The repudiation shell** (`:670`–`:684`): sets `complianceState = 'defaulted'` directly and
  `continue`s at `:683`, again before `:759`. A torn-up treaty's shell is spent history whose grade
  `scoreTreatyDefault` already reads; the fold does not reach it. Pinned by **A7**.

### 5.3 Lifecycle paths (L4, written before the writer builds)

- **create** — only at the V-7 fold, only under the flag.
- **read** — only through `worstObservedEverOf(treaty)`, which resolves absence to `'honored'`. ⛔ No
  consumer touches the raw field. The reader is **exported and UNCONSUMED in `src/`** at this wave —
  the GR-0 `treatiesPricedDuring` handoff idiom — and **A8** pins the absence, so the pin reds the
  day GR-5d wires the conversion gate. That red is the handoff signal.
- **persist** — rides treaty serialization inside `spatialLedgers.treaties` as-is. No new
  top-level key, no new ledger, no `spatialUsage` entry. **A8** JSON-round-trips a multi-tick record.
- **regenerate** — the field regenerates with the treaty through the ledger's existing pins; it is
  derived from observations the same seed reproduces, so THE PROMISE is kept without a delta.
- **undo** — restores byte-true with the record.
- **migrate / import** — ⛔ **NOTHING MIGRATES.** Legacy records carry no key and every reader
  tolerates absence forever.
- **veil** — no DM-only truth; no projection change; no public-payload key.

---

## 6. Exact SEVEN-path implementation manifest

| # | Action | Path | Symbol / region | Max Δ eff | Instruction |
|---|---|---|---|---:|---|
| 1 | MODIFY | `src/domain/worldPulse/pactAmendment.js` | after `PACT_PROVENANCE` / `provenanceOf` | **≤ 24** | Add `treatyRenewalActive(worldState)` — the ONE by-name strict `=== true` gate — plus the pure `worstObservedEverAfter(treaty, observed)` and `worstObservedEverOf(treaty)`. ⛔ Read the record through the file's own `recordOf(value)` idiom, NEVER a bare `treaty.worstObservedEver` chain (§11 OSR). Order by `rankState` imported from `peaceTermsPrimitives.js`; author no second ordering |
| 2 | MODIFY | `src/domain/worldPulse/peaceTerms.js` | `:151`, `:350`, `:759` | **0 — NET ZERO** | THREE one-for-one replacements, §6.1. ⛔ numstat must be exactly `3/3`; effective 797 → 797, raw 1264 → 1264 |
| 3 | REGISTER | `src/domain/worldPulse/simulationRules.js` | `ENGINE_GATED_VIRTUAL_RULE_KEYS` | ≤ 2 | Insert `'treatyRenewalEnabled'` in codepoint order, **between `'treatyLifecycleVoiceEnabled'` and `'underwaysOrganicFoundingEnabled'`**, with the dated joining comment in the file's own idiom. ⛔ ABSENT from `DEFAULT_SIMULATION_RULES` and from EVERY preset |
| 4 | REGISTER | `src/domain/certification/subsystemRowsVirtual.js` | virtual rows | ≤ 22 | One `rule: 'treatyRenewalEnabled'` row in the landed GR idiom. ⛔ Never a `wizard_news.*`-fed identity on a certification row |
| 5 | CREATE | `tests/domain/treatyRenewalMemory.test.js` | — | n/a | ONE literal `describe`, EIGHT literal top-level `it` calls (§8). ⛔ No `.each`, no `runIf`, no nested describe, no `skip`, no `todo` |
| 6 | TEST | `tests/lint/sovereigntyLightingContract.walker.test.js` | the recorded tuple | n/a | Re-derive the WHOLE tuple in this same commit to `2429/366/2063/20115/5654`. ⛔ Never patch one figure — the census is SEQUENCED and stops measuring at its first red |
| 7 | TEST | `tests/domain/subsystemRowsVirtual.test.js` | `VIRTUAL_RULES`, `LANE_LEAVES` | n/a | ⭐ **THE FLAG-MINT SEVENTH PATH (§49 ruling 2, and ruling 3 makes it STANDING LAW for every volume).** THREE module-scope literal edits and **ZERO new test titles**: one `const TREATY_RENEWAL = 'treatyRenewalEnabled';` in the rule-const block with its dated joining comment; `TREATY_RENEWAL` appended to `VIRTUAL_RULES` (AUTHORING order — the row is appended last in `subsystemRowsVirtual.js`, so it appends last here); and `[TREATY_RENEWAL]: ['src/domain/worldPulse/pactAmendment.js']` in `LANE_LEAVES`. ⛔ Add no `it`, `test` or `describe` — this path is CENSUS-NEUTRAL and path 6's tuple depends on it staying so |

⛔ **AN EIGHTH PATH IS A STOP.** ⛔ **A third registration-only production file is a STOP** (the
default budget is 3 and this packet spends 2; **no override is recorded and none may be inherited —
§P2: AN OVERRIDE NEVER TRANSFERS**).

### 6.2 ⭐⭐ WHY PATH 7 EXISTS — MEASURED AT `6cd18ad3`, NOT ARGUED

The first compile of this packet reserved SIX paths and stopped at I1 on the seventh. The
contradiction, and the reason no six-path shape exists:

1. The CQ5 one-commit flag law needs key + by-name gate read + certification row in ONE commit.
   `tests/lint/engineGatedRuleKeys.walker.test.js`'s `manifestWithoutRow` arm reds if a manifest
   key carries no row; its `unaccountedReads` arm reds if a gated key is unmanifested. **Executed
   with all three present: `TRUE_EXIT=0`, green.** The row is mandatory, not optional.
2. `tests/domain/subsystemRowsVirtual.test.js:384` is an **exact ORDERED equality** between
   `VIRTUAL_SUBSYSTEM_ROWS.map(row => row.rule)` and its own frozen `VIRTUAL_RULES` literal, so
   authoring the twentieth row reds it BY CONSTRUCTION. **Executed: `TRUE_EXIT=1`,
   `1 failed | 13 passed`, one assertion, `+ "treatyRenewalEnabled"`.**
3. `VIRTUAL_RULES` cannot gain the member alone: `:563` iterates `LANE_LEAVES[rule]` for every
   member and throws on `undefined`.

⇒ **the certification row costs this path and there is no spelling that avoids it.** The
immediately preceding train already knew: HB-2's changeManifest names this exact file, and
HB-2B's terminal records it as deviation **D2**. ⭐ §49 ruling 3 converts three independent
discoveries into standing law — every packet minting an engine-gated flag now names this path
and prices its three literal edits at compile time.

### 6.1 The three replacements in `peaceTerms.js`, verbatim

```
:151  BEFORE  import { absorbWarEndIntoStandingPact } from './pactAmendment.js';
      AFTER   import { absorbWarEndIntoStandingPact, treatyRenewalActive, worstObservedEverAfter } from './pactAmendment.js';

:350  BEFORE  const lifecycleVoiceLit = treatyLifecycleVoiceActive(worldState);
      AFTER   const lifecycleVoiceLit = treatyLifecycleVoiceActive(worldState); const renewalMemoryLit = treatyRenewalActive(worldState);

:759  BEFORE  treaty.complianceState = worstObserved;
      AFTER   treaty.complianceState = worstObserved; if (renewalMemoryLit) treaty.worstObservedEver = worstObservedEverAfter(treaty, worstObserved);
```

⭐ Two statements on one line is **this file's own landed idiom**, measured at `:349` (the flag-hoist
line immediately above the site) and `:784`. `eslint.config.js` declares no `max-len` and no
`max-statements-per-line` rule. ⛔ **Do not reformat, re-wrap, or "tidy" any other line of this
file** — an unrelated whitespace change moves the effective count and is a STOP.

---

## 7. Ordered coding sequence

0. **Preflight.** Prove branch, HEAD ancestry, `git status --porcelain`, clean targets, and every
   §4.3 symbol. ⛔ CHECK-GIT-FIRST on `simulationRules.js` and `subsystemRowsVirtual.js` immediately
   before staging (CQ5 — train plan §10).
1. **Capture the dark golden** — the own-footprint baseline for A2/A3 **before** any source edit.
2. **Write the failing acceptance file** (§8), all eight titles.
3. **Implement the leaf** — path 1. Pure; no writer, no resolver (HZ-READERNOWRITER).
4. **Extend the writer** — path 2, the three one-for-one replacements.
5. **Register** — paths 3 and 4, **in this same commit** (the one-commit flag law).
6. **Re-derive the census WHOLE** — path 6.
7. **Focused verification** (§10) + the §31 anchor preflight + the eight mutants (§9).
8. **Terminal:** one bare `npm run check:tail`, then a **separate** `npm run smoke:boot`.

⛔ The agent must not start by changing a golden, baseline, budget or persisted shape.

---

## 8. Acceptance matrix — exactly eight titles, ONE file, ONE describe

| # | Title (the census key — exact) | Proves |
|---|---|---|
| **A1** | `lit: a treaty observed strained records worstObservedEver 'strained' at the compliance write` | main behaviour; the lit-mutant control's anchor |
| **A2** | `absent: the flag unset writes no worstObservedEver key and the ledger is byte-identical` | FENCE 1 — own-footprint golden |
| **A3** | `false behaves exactly as absent: the two ledgers are byte-identical to each other` | FENCE 2 — absent-vs-false differential |
| **A4** | `dark: worstObservedEverAfter is never called on any path through advanceTreaties` | FENCE 3 — call-path spy |
| **A5** | `exactly three src files name treatyRenewalEnabled and the only gate is a strict === true read` | FENCE 4 — gate-polarity census |
| **A6** | `monotone: a treaty that strains and then recovers keeps worstObservedEver 'strained'` | ⭐ the counterforce, and GR-5's reason to exist |
| **A7** | `neither the all-terms-lapsed prune nor the repudiation shell writes worstObservedEver` | the two excluded branches (§5.2) |
| **A8** | `a multi-tick record JSON round-trips, a legacy record reads honored, and no src consumer exists` | lifecycle + writer-to-reader + the handoff tripwire |

**A5's expected file list**, modelled on the landed GR-0 fence (`treatyLifecycleVoiceDormancyFence.test.js:214`):
`src/domain/certification/subsystemRowsVirtual.js` · `src/domain/worldPulse/pactAmendment.js` ·
`src/domain/worldPulse/simulationRules.js`. **A fourth file reds until somebody classifies it** —
which is the point.

⛔ **A ninth title is a STOP** (§P8.5), and §P3 makes it a census move the packet did not predict.
⭐ Renaming a title or widening an existing case's assertions is census-neutral; **registering a
ninth is not.**

⚠ **§P5 HZ-BANKEDVOICE:** all four `tests/copy/voiceMechanics.test.js` arms are banked known
failures, so the estate-wide em-dash / exclamation walker will **not** red on a new tell. GR-5A
authors no rendered prose at all, so no case is owed here — **and that absence is stated
affirmatively rather than assumed.**

---

## 9. Mutation proof — eight disposable mutants, isolated immutable candidate

§P6 order for each: (1) prove the **source bytes changed**; (2) require a **nonzero exit** AND the
**named title** red — a red under a different title is an ambiguous mutant and a STOP; (3) restore
the **exact pre-mutant sha256**; (4) rerun focused green.

| # | Plant | Must red |
|---|---|---|
| **M1** | `=== true` → `!== false` in `treatyRenewalActive` | **A5** |
| **M2** | delete the `if (renewalMemoryLit)` guard at `:759` (always write) | **A2** and **A3** |
| **M3** | invert the comparator in `worstObservedEverAfter` (`>` → `<`) | **A6** |
| **M4** | drop the `?? 'honored'` legacy default in `worstObservedEverOf` | **A8** |
| **M5** | write `worstObserved` directly instead of the folded value | **A6** |
| **M6** | move the fold ABOVE the `liveTerms.length === 0` branch | **A7** |
| **M7** | add the fold inside the repudiation branch (`:674` region) | **A7** |
| **M8** | replace `rankState` with a raw string `>` comparison | **A6** (alphabetical order puts `defaulted < honored < strained`, a real ordering bug) |

⛔ **Do not run the estate-wide shared-tree sweep** — `src/` carries other lanes' uncommitted work
and a sweep plant's `git checkout --` revert is the parallel-session hazard the dirty-tree guard
refuses. ⛔ **No `scripts/mutation-coverage-manifest.json` row is owed**: executed against
`enumerateInvariants`, `tests/domain/treatyRenewalMemory.test.js` sits outside `ENFORCER_DIRS` and
its basename matches none of the enforced-nomenclature tokens. ⛔ Never re-serialize that file.

**Anti-vacuity, each having bitten this volume:** ⛔ a mutant that passes 7/7 means a **redundant
second guard**, not safe code — M2 is the exposed one and must be checked in both directions.
⛔ A **count mutant on a literal** goes vacuous. ⛔ A **fixture mirroring the deriver** can never see
a dead arm — A6's fixture must drive the REAL `advanceTreaties`, not a hand-built record.
⛔ A **rendered-surface negative** passes when the surface never rendered — A2/A3 must first assert
the lit path DOES write, then assert the dark path does not. ⛔ A **self-referential pin** proves
nothing.

---

## 10. Focused verification — exact argv, exact expectations

```
npx vitest run tests/domain/treatyRenewalMemory.test.js                       ; echo TRUE_EXIT=$?   # 8 passed, exit 0
npx vitest run tests/domain/pactAmendment.test.js                             ; echo TRUE_EXIT=$?   # green: no vocabulary widened
npx vitest run tests/domain/peaceTerms.test.js                                ; echo TRUE_EXIT=$?   # green: the mover is unchanged in behaviour
npx vitest run tests/property/treatyLifecycleVoiceDormancyFence.test.js       ; echo TRUE_EXIT=$?   # green: renewal is dark in every fixture
npx vitest run tests/property/pactFormationDormancyFence.test.js              ; echo TRUE_EXIT=$?   # green
npx vitest run tests/lint/engineGatedRuleKeys.walker.test.js                  ; echo TRUE_EXIT=$?   # green: key + read + row all in this commit
npx vitest run tests/domain/subsystemRowsVirtual.test.js                      ; echo TRUE_EXIT=$?   # green ONLY with path 7 (§6.2) — the ordered-equality pin at :384
npx vitest run tests/lint/couplingInclusion.walker.test.js                    ; echo TRUE_EXIT=$?   # green: no leaf minted, no row, no ceiling move
npx vitest run tests/lint/negativeAssertionAnchor.walker.test.js              ; echo TRUE_EXIT=$?   # 9 passed — the §31 preflight, BEFORE green is declared
npx vitest run tests/lint/sovereigntyLightingContract.walker.test.js          ; echo TRUE_EXIT=$?   # green at 2429/366/2063/20115/5654
node scripts/check-observed-shape-readers.mjs                                 ; echo TRUE_EXIT=$?   # 1998 — ANY movement is a STOP
node scripts/implementation-packets.mjs validate                              ; echo TRUE_EXIT=$?   # 45 packets (1 READY), exit 0 at I1
```

⛔⛔ **Never wrap `npm run check*` in `gate-mutex.sh --run`** — `test:ratchet` re-acquires and
self-deadlocks, and **exit 3 is the mutex giving up, not a red**. ⛔ Never read a gate through a
pipe. Trust no exit status you did not capture in-shell. **Outlast the gate in your own turn.**

---

## 11. Wave-specific hazards, coupling, and OSR

- **HZ-SIZECEILING** — `peaceTerms.js` **797/800**, re-measured at this base with eslint's own
  `Linter`. ⛔ The edit is net zero or the packet STOPS. Never add a baseline entry, never raise a
  ceiling, never decompose the file.
- **OSR (§P8 stop 13)** — baseline **1998**, exit 0. Executed against the frozen inventory:
  `pactAmendment.js`, `pactProposals.js`, `pactTriggers.js`, `treatyLifecycleVoice.js`,
  `peaceTerms.js`, `oathHolder.js` and `treatyEnforcement.js` each carry **ZERO** rows; only
  `pactFormation.js` (1) and `peaceTermsAppraisal.js` (1) appear in the cohort. The detector grounds
  by receiver (`RECEIVER_ROOT`) and a **CallExpression receiver resolves to nothing**, which is why
  `recordOf(treaty).worstObservedEver` costs zero and `treaty.worstObservedEver` in a new function
  may not. ⛔ A movement off 1998 is a STOP — report, never mint, never baseline.
- **§P4 COUPLING** — `pactAmendment.js` is claimed by `LAYER_PATTERNS.GRAMMAR`'s `/pact[A-Z]/` arm;
  no leaf is minted, so no layer pair, no `ARGUED_UNLAYERED` row, no floor move.
  `ARGUED_ROSTER_CEILING` **19** and `UNLAYERED_BASELINE_CEILING` **179** stay put. ⛔ Never edit a
  coupling registry, a baseline, or the ARGUED roster. ⚠ The new import in `peaceTerms.js` is a
  **widened existing edge** to a module it already imports, so no new module edge exists at all —
  which also removes the barrel-hop class by construction.
- **HZ-DORMANCYFENCE** — `treatyLifecycleVoiceDormancyFence.test.js` asserts the lit read-model adds
  exactly `['ageYears']`. GR-5A adds a **record** field, not a read-model key, and is dark in every
  one of that fence's fixtures. Unmoved.
- **HZ-READERNOWRITER** — the three new functions hold no writer and call no resolver; the write
  statement stays in the writer family's own mover.
- **HZ-WIZARDNEWSAUTHORING / HZ-TITLEBASELINE / HZ-KILLLIST** — no news site, no mount, no `title=`,
  no `borderRadius`/`boxShadow`/`rgba(`/`*_BG` token. All unmoved. ⛔ The 19-row ledger and the 485
  `title=` ceiling are never raised.
- **HZ-OATHSTAMP / HZ-NPCSTOKEN** — ⛔ do not name the treaty-ledger write, and do not write the
  `.npcs` dotted token, **even in a comment**; both convict from raw source.
- **HZ-DISTBOOT** — no new static import edge (see coupling above); the terminal's separate
  `smoke:boot` is owed by the ordinary terminal rule.
- **HZ-GATEMUTEX / HZ-PIPEEXIT / HZ-SINGLEWRITER** — §P5/§P7 verbatim.

Run **both** the hazard-registry and pre-mortem validators. **A GR member owes no new hazard row.**
A new working premortem warning whose cure lies outside this manifest is a STOP and a chair return.

---

## 12. Completion receipt and STOP conditions

**The receipt reports:** verified base sha and final state · exact changed files with effective-line
deltas (⛔ including `peaceTerms.js` **797 → 797** and the numstat `3/3`) · the eight acceptance
cases executed and passed · commands, exits, test counts, both named ratchets, OSR before/after ·
base-versus-wave failure-identity diff · the dark/own-footprint golden result · generated artifact
delta or `NONE` · deviations `NONE` or a STOP · out-of-scope observations without investigation ·
judgment calls `NONE`.

**STOP — the preamble's §P8 twenty-one conditions bind in full. Wave-specific additions:**

1. `peaceTerms.js` measures anything but **797** after the edit, or the numstat is not `3/3`.
2. The observed-shape count moves off **1998**.
3. `ENGINE_GATED_VIRTUAL_RULE_KEYS` would take the key in a commit that does not ALSO carry the
   by-name gate read and the certification row.
4. Any preset or `DEFAULT_SIMULATION_RULES` would declare the key.
5. Any of `PACT_LINEAGE_ACTS` / `PACT_ENDINGS` / `PACT_PROVENANCE` / `PACT_TRIGGERS_PRODUCED` would
   need a member.
6. A news kind, receipt pool, `WHAT_PHRASES` row, `EXACT_SECTION` key or chronicler row appears
   necessary — that is GR-5e, and it carries a five-file cost this packet has not bought.
7. A band, threshold, window, cap, tuning key or **any authored number** appears necessary — that is
   a §42/§43 chair act, never an implementer's.
8. `kindPoolFloors`' `REGISTRIES` moves off **9**, or any numeral moves in a direction not predicted
   in §4.4.
9. An EIGHTH path, a third registration-only production file, or a ninth acceptance title.
   ⚠ The seventh — `tests/domain/subsystemRowsVirtual.test.js` — is SIGNED into §6 by §49 ruling
   2 and is no longer a STOP; adding a TEST TITLE to it still is, because path 6's tuple is
   derived on the premise that this path is census-neutral.
10. The census walker is dirty at dispatch, or a foreign lane holds uncommitted test titles at
    re-record time.
11. Any soak, tuning, deploy, push, migration, marketplace, paid-policy or legal work would begin.
    **None of these is ever implied by an implementation packet.**

**The STOP report contains the smallest measured contradiction, the evidence, and a proposed packet
split. It contains no speculative repair.**
