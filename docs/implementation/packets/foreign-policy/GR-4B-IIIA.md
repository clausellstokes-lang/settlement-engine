# Foreign Policy / GR-4B-IIIA — the question-opening voice

- **Status:** READY
- **Packet version:** `1`
- **Verified base:** `claude/composite-r4` at `955acd7ad2aafc9fc64ad71ade64c54349219495`
- **Last revalidated:** 2026-08-13, after the annex-only chair correction
  `955acd7ad2aafc9fc64ad71ade64c54349219495`, whose direct parent is the GR-4d
  terminal `7a589733a8bcfae68c3768c035e0670e3331e493`.
- **Depends on:** GR-4b-alpha at `dd457b9a` for the landed succession-voice
  grammar idiom and GR-4d at implementation `a17d0a02`, version-2 authority
  `fcf1efbd`, and terminal receipt `7a589733` for the durable pending question.
- **Collision group:** `world-pulse-proposal-mouths`, `pact-grammar-voice`, and
  `sovereignty-lighting-census`. Serialize against every lane touching any of
  the twelve manifest paths.
- **Commit authority:** edits only. The coding agent may edit exactly the twelve
  implementation paths in section 7 and must leave them unstaged and
  uncommitted. The coordinator owns proof, immutable commit construction,
  terminal promotion, and old-value CAS.
- **Baseline posture:** the verified tree is clean. The receipt annex is already
  corrected and immutable at SHA-256
  `91f4920117ded93d822dedb8b67b5f6972f6fa9fc93857a31d9aee0614209fd7`.
  `peaceTerms.js` is exactly **1264 raw / 797 effective** against a ceiling of
  800 with no baseline door. The free whole lighting census is
  **2413/365/2048/19992/5639**.

## 1. Reconciled authority and chair rulings

This is the successor authority for GR-4b-iii-a. Terminal `GR-4B.md` and
`GR-4D.md` are history and must not be amended or redispatched.

### CR-GR4B-9 — scope release after GR-4d

GR-4d's landed pending proposal makes exactly one previously dishonest voice
surface honest: `succession_question_opened`, spoken when a new durable pending
question has actually entered the docket. This packet releases only that
opening Herald/Chronicle beat.

Still deferred and explicitly excluded:

- GR-4b-iii-b's dossier line `succession_question_open`;
- GR-4b-ii's `honored_by_silence`, `reaffirmed`, and every honor/terminal voice;
- any voice on Apply, Dismiss, expiry, ring overflow, undo, or replay;
- GR-5, credibility tuning, public repudiation widening, and any new surface.

### CR-GR4B-10 — A-21 annex chair act discharged before dispatch

The authored annex used `{npc}` as though the successor's name were readable,
used `{band}` and plural claims for a producer that opens one question per exact
instrument, and marked a slotless exemplar as slotted. The chair act landed
alone at `955acd7a`; it is complete governance substrate, not an implementation
path. The implementer consumes it verbatim and may not edit it.

The seven required-slot vectors are exactly, in authored order:

```text
[]
[npc, settlement]
[counterpart, settlement]
[npc]
[]
[settlement]
[npc]
```

The four materially corrected variants are exactly:

```text
{npc} left an oath standing at {settlement}; the new seat has taken the chair while the parchment remains unanswered.
The oath {npc} swore is a question this week, and the clerks have set its parchment before the new seat.
The clerks in {settlement} have laid the unanswered oath on the council table, with room beneath it for one word.
Whatever the new seat decides, the oath {npc} swore will be remembered longer than its terms.
```

`{npc}` means only the fallen holder recorded on the selected treaty's oath
stamp. No successor or roster lookup is permitted. `{band}` is absent. The pool
remains seven families, `notable`, `public`, Herald/chronicle, in the authored
order. Annex SHA-256 mismatch is a pre-dispatch STOP.

### CR-GR4B-11 — one fresh registration-only override

The standard registration-only production-file budget widens **3 → 5** because
one Herald kind mechanically requires five distinct existing registries:

1. receipt pool;
2. grammar-kind registry;
3. Herald exact route plus its explicit letter divergence;
4. Chronicler letter section;
5. settlement-rumor phrase.

This override applies only to that row. The handwritten implementation total is
already exactly **12** and receives **no override**. GR-4b-alpha's historical
`12 → 14` override does not transfer. A thirteenth path, sixth registration
file, or attempt to invoke the inherited total override is a STOP.

## 2. Outcome and boundary

**Observable result:** when GR-4d inserts a new valid pending succession
question, the same late treaty-stage call returns exactly one fully addressed
public `succession_question_opened` beat for that instrument. The beat says the
question is open; it does not claim an answer, breach, honor, successor name, or
grouped treaty count.

The queue write is mechanics and the beat is presentation. Missing presentation
data may suppress the beat but must never veto, undo, or alter the pending
proposal.

**Definition of done:** the exact twelve paths pass A1-A8; the one hot-file
mount stays exactly 1264 raw / 797 effective; the registry and routing counts
move by exactly one; the runtime test denominator moves by exactly eleven; the
whole census becomes exactly `2414/365/2049/20000/5640`; eight disposable
mutants fail by their intended A-title and restore digest-exact; the sealed
checks, independent bare full gate, and separate boot smoke exit zero; no soak
starts.

In scope:

1. one pure GRAMMAR-owned opening-voice wrapper;
2. one one-for-one treaty-stage import/call replacement;
3. one seven-family annex-verbatim public grammar kind and its five mechanical
   registration homes;
4. one eight-case prevention file and the four already-governed walkers it
   necessarily updates.

Explicit non-goals:

- no new state, state writer, persisted family, schema, migration, flag, tuning,
  dependency, golden, baseline, ceiling, timeout, preset, or UI;
- no edit to `treatySuccessionDecision.js`,
  `treatySuccessionProposalApply.js`, `treatySuccessionVoice.js`,
  `treatyBreach.js`, `actorMajorApproval.js`, `applyWorldPulse.js`, any dossier
  file, terminal packet, or receipt annex;
- no mutation-manifest, hazard-registry, coupling-registry, observed-shape
  baseline, size-baseline, test-ratchet, ledger-ownership, or pulse-stage edit;
- no diagnostic, weekly, release, or research soak.

## 3. Hard scope and line budgets

| Limit | Packet budget |
|---|---:|
| Behavior families | `1` |
| New persisted record families | `0` |
| New state writers | `0` |
| New feature flags / tuning keys | `0 / 0` |
| New public feed kinds | `1` |
| Direct production mounts | `1` — existing treaty stage |
| New logic-bearing production leaves | `1` |
| Existing logic-bearing production files modified | `1` |
| Registration-only production files | `5` under CR-GR4B-11 |
| Handwritten implementation files total | exactly `12`; no override |
| New/changed effective production lines | `<=228` |
| `treatySuccessionOpeningVoice.js` | `<=200` effective and `<=250` raw |
| `grammarReceiptPools.js` delta | `<=12` effective |
| `grammarNews.js` delta | `<=8` effective |
| `heraldRouting.js` delta | `<=4` effective |
| `chroniclersLetter.js` delta | `<=2` effective |
| `settlementRumors.js` delta | `<=2` effective |
| `peaceTerms.js` delta | exactly `0` raw and `0` effective |
| Acceptance cases | exactly `8` literal `it` registrations |

Comments that repair now-stale local counts or deferral prose inside one of the
twelve paths are permitted and required for coherence. They do not authorize a
new path or effective-line overrun.

## 4. Verified tree contract and fingerprints

### 4.1 Existing target seals

The two CREATE paths are absent. Every existing implementation target is clean
at this exact raw/effective count and SHA-256:

| Path | Raw / effective | SHA-256 |
|---|---:|---|
| `src/domain/worldPulse/peaceTerms.js` | `1264 / 797` | `ad0ddd8e93a3685b870c3359880c602e8464dcf6dae25bef5c2427fb028552ba` |
| `src/domain/worldPulse/grammarReceiptPools.js` | `142 / 84` | `0dc5a834b631a8240d7f19fd205ff721e272f62abb2b36830d9462991607a0e1` |
| `src/domain/worldPulse/grammarNews.js` | `237 / 106` | `fcc8f0c0fa6fd072ad1b6269c2bad94a630f4f8cfe4b11d730e9314b541fcf08` |
| `src/domain/realm/heraldRouting.js` | `620 / 264` | `f318d2f67e50b0983dd89d834d01210a85803576d9f558786830b63b23a36421` |
| `src/domain/display/chroniclersLetter.js` | `414 / 218` | `4fcf5340580e90e00089447ddc91955402665f56b9f510dae35693b531d82bc9` |
| `src/domain/display/settlementRumors.js` | `879 / 506` | `05cbca1c8840043e50f0612cae333a9c37a59d46579671cfd902a452e6be2eab` |
| `tests/lint/grammarLifecycleKindPools.walker.test.js` | `336 / 209` | `e41cc2729f87de04fd47be16d87b637c4932a4d7e8540aa86bf85eb39f219268` |
| `tests/lint/kindPoolFloors.walker.test.js` | `316 / 183` | `1a06520f05b9021b05aba13a0843c90acd2de8f238573d6e7835c3fc2ed3d846` |
| `tests/domain/impactKindWalkers.test.js` | `199 / 86` | `220cdc951ae53eee5181d5447cb3b1de4075625c72a71dfdc4f90dcd432d1569` |
| `tests/lint/sovereigntyLightingContract.walker.test.js` | `4390 / 1336` | `97a51402946e38f2ba1ce97588b2a11fd26eb0bca7e25f066f3813b1eae0dde6` |

Anchor seals:

```text
c9cad6ffda48aec04ab3966bad085783ed21fb8f9f75f73b27353a60d468556d  treatySuccessionDecision.js
fbc916dc5890b673ea2f6f3c7f58f451554759eef80f296151f9b220a1a9eef0  treatySuccessionProposalApply.js
1cf685802bedb8128adc57b37d84c1332922f15585bf56e0624321f4320649ff  treatySuccessionVoice.js
91f4920117ded93d822dedb8b67b5f6972f6fa9fc93857a31d9aee0614209fd7  RECEIPT_POOLS_GRAMMAR.md
```

Any mismatch, existing CREATE target, target dirt, or new foreign movement is a
dispatch STOP. A verified-base descendant is admissible only through the sealed
ancestry/substrate proof.

### 4.2 Live and post-change census

| Census | Live | Required post-change |
|---|---:|---:|
| receipt-pool registry rows | `9` | `10` |
| grammar Herald rows | `4` | `5` |
| registered impact kinds | `108` | `109` |
| explicitly routed kinds | `376` | `377` |
| routed and registered | `102` | `103` |
| registered minus routed | `6` | `6` |
| deliberately unvoiced | `274` | `274` |
| underfloor kinds | `28` | `28` |

The three existing `test.each(GRAMMAR_KIND_REGISTRY)` sites add three runtime
cases when the registry gains one row. The new test file adds eight literal
cases. The full runtime denominator therefore moves exactly
`27999 → 28010`; the frozen known-failure ceiling remains `16`.

The whole lighting tuple moves exactly:

```text
files / parked / credited / test titles / suite titles
2413 / 365 / 2048 / 19992 / 5639
2414 / 365 / 2049 / 20000 / 5640
```

That is exactly `+1` file, `+0` parked, `+1` credited, `+8` test titles, and
`+1` suite title. The new file has one literal `describe` and eight literal
top-level `it` calls. `.each`, looped or conditional registration, nested
describes, `skip`, and `todo` are forbidden.

## 5. Corpus and registration contract

Add `succession_question_opened` to `GRAMMAR_RECEIPTS` with the seven annex
families byte-verbatim except for interpolation syntax and stripped editorial
tags. Add one grammar registry row with exactly:

```text
kind         succession_question_opened
significance notable
audience     public
section      trade
pool depth   7
contexts     none
required     [], [npc,settlement], [counterpart,settlement], [npc],
             [], [settlement], [npc]
```

Register the same literal kind in exactly these other homes:

- `heraldRouting.js` `EXACT_SECTION`: `trade`;
- `heraldRouting.js` `KIND_SECTION_DIVERGENCES`: `trade`, with the explicit
  judgment that the letter treats a succession question as a court act while
  the Herald files the treaty cohort under trade;
- `chroniclersLetter.js` `KIND_SECTION`: `courts`;
- `settlementRumors.js` `WHAT_PHRASES`:
  `a new seat weighing the oath the old one swore`;
- `impactKindWalkers.test.js` `EXPECTED_VOICE`: `null` — an authored pact
  headline/summary must not borrow a generic trade crier merely because the
  Herald desk is trade;
- `grammarLifecycleKindPools.walker.test.js` `GR4_KINDS` and exact registry
  tuple: the new kind, `notable`, `public`, `trade`, depth `7`.

Do not add a grammar party-role override. Like the landed alpha succession
voice, this family binds the direct acting-court/counterpart axis, not the
obligee/obligor axis.

Repair these stale comments/titles inside already authorized paths:

- `grammarReceiptPools.js`: the header's seven-pool statement and the comment
  saying all opening siblings remain unwired;
- `grammarNews.js`: the two-Herald-row statement;
- `grammarLifecycleKindPools.walker.test.js`: exact pool/Herald counts and any
  title or assertion still saying eight/nine pools, three/four Herald kinds,
  or seven registry rows.

No comment repair may change a test-title cardinality beyond the one new suite's
declared `+8/+1`.

## 6. Exact behavior and identity contract

Create the GRAMMAR-owned leaf
`treatySuccessionOpeningVoice.js`. It exports:

```js
resolveSuccessionQuestionsWithOpeningVoice(worldState, tick, now)
```

The function follows this exact order:

1. Snapshot the IDs of every proposal present before the call.
2. Call landed `resolveSuccessionQuestions(worldState, tick, now)` exactly once.
3. Preserve its returned `worldState` and every returned `newsEntries` item.
   This is load-bearing: when routine-major approval is absent or false, the
   landed direct road may emit the existing alpha `disavowed_by_succession`
   beat. The wrapper must neither suppress nor rewrite it.
4. Inspect only proposals in the returned final state whose IDs were absent
   from the snapshot and whose status is exactly `pending`.
5. Validate each row's complete `outcome` through
   `successionQuestionPayloadTuple`. Do not hand-walk or partially trust
   `proposalPayload`.
6. Read the exact selected treaty from
   `treatyLedgerOf(finalState)[question.treatyKey]`. Never pair or dedupe by
   court pair; two instruments between the same courts are distinct questions.
7. Resolve both persisted court names through `treatyOrientationOf(treaty)`.
   An id echoed as a name is unresolved.
8. Resolve the fallen holder only through the selected treaty's total reader:

   ```js
   swornPartiesOf(treaty).find(
     (stamp) => stamp.settlementId === question.settlementId
       && stamp.npcId === question.npcId,
   )
   ```

   No successor, ruler, standings, or roster lookup is permitted. The new leaf
   must not contain the dotted roster token even in comments because an
   existing raw-source census reads comments.
9. Compose the grammar receipt only after the queue write. An unresolved
   treaty, court name, fallen-holder name, or receipt returns zero opening
   beats for that row but never vetoes or rolls back the proposal.
10. Preserve proposal enumeration order. Use no sort, locale operation, draw,
    clock, `Date`, or `Math.random`.

### 6.1 Per-instrument identity

Use the validated outcome ID from `successionQuestionPayloadTuple` as both the
receipt seed and `sourceEventId`. The public ID is exactly:

```text
wizard_news.<openedTick>.succession_question_opened.<stablePart(outcomeId)>
```

Court-pair, NPC, question-key truncation, tick-only, or array-index identity is
forbidden. The outcome ID already binds the exact instrument and complete
question.

### 6.2 Exact beat shape

The beat mints literal, raw-scanner-visible fields:

```js
kind: 'succession_question_opened'
impactKind: 'succession_question_opened'
```

Never mint either through a constant. The remaining fields are:

- `significance: receipt.significance` (`notable`);
- `severity: 0.56`, `score: 58`;
- `tick: openedTick`, `scope: 'regional'`;
- headline:
  `` `${settlementName}'s new seat weighs the oath sworn to ${counterpartName}` ``;
- `summary: receipt.line`;
- a copied, nonempty `reasons` array from the validated outcome;
- `settlementIds: [question.settlementId, question.otherId]`;
- names in the same order;
- a copied exact `treaty.parties` array;
- `sourceEventId: outcomeId`;
- `familyId`, `audience`, and `section` from the receipt;
- `tags: ['world_pulse', 'pact_grammar', 'lifecycle']`.

There is no `ending`: an open question is not a pact ending. A failure to
produce this complete address yields no beat, never a partial one.

### 6.3 Treaty-stage mount

`peaceTerms.js` changes only the existing succession-decision import and its
existing late treaty-stage call:

```text
resolveSuccessionQuestions → resolveSuccessionQuestionsWithOpeningVoice
```

Both replacements are one line for one line. Program order, arguments, return
shape, and every neighboring stage remain unchanged. The file must finish at
exactly **1264 raw / 797 effective**. Any raw or effective motion is STOP.

## 7. Exact twelve-path implementation manifest

| Action | Path | Region | Maximum effective delta | Coding instruction |
|---|---|---|---:|---|
| `CREATE` | `src/domain/worldPulse/treatySuccessionOpeningVoice.js` | wrapper and pure composer helpers | `<=200` | Call the landed decision once, select only newly inserted valid pending rows, resolve exact instrument/address, and append zero-or-one opening beat per row |
| `MODIFY` | `src/domain/worldPulse/peaceTerms.js` | succession import and existing call | exactly `0` raw/effective | One-for-one symbol/path replacement only; remain 1264/797 |
| `REGISTER` | `src/domain/worldPulse/grammarReceiptPools.js` | `GRAMMAR_RECEIPTS` | `<=12` | Add seven annex-verbatim families and correct stale local prose |
| `REGISTER` | `src/domain/worldPulse/grammarNews.js` | `GRAMMAR_KIND_REGISTRY` | `<=8` | Add one exact notable/public/trade row, no contexts or party-role override |
| `REGISTER` | `src/domain/realm/heraldRouting.js` | `EXACT_SECTION`, `KIND_SECTION_DIVERGENCES` | `<=4` | Add exact trade route and explicit courts-to-trade divergence |
| `REGISTER` | `src/domain/display/chroniclersLetter.js` | `KIND_SECTION` | `<=2` | File the kind under `courts` |
| `REGISTER` | `src/domain/display/settlementRumors.js` | `WHAT_PHRASES` | `<=2` | Add the exact governed phrase |
| `CREATE` | `tests/domain/treatySuccessionOpeningVoice.test.js` | one describe, A1-A8 | `n/a` | Exactly eight literal top-level `it` cases; no registration indirection |
| `TEST` | `tests/lint/grammarLifecycleKindPools.walker.test.js` | exact registry, GR4 cohort, annex and routing pins | `n/a` | Move exact counts 9→10 and Herald 4→5; add kind/vector/corpus pins without extra titles |
| `TEST` | `tests/lint/kindPoolFloors.walker.test.js` | existing registry-driven floor proof | `n/a` | Admit the new governed row; underfloor count stays 28; no title motion |
| `TEST` | `tests/domain/impactKindWalkers.test.js` | `EXPECTED_VOICE` | `n/a` | Add the exact literal kind with `null`; no title motion |
| `TEST` | `tests/lint/sovereigntyLightingContract.walker.test.js` | whole `CENSUS` | `n/a` | Re-derive whole and record exactly 2414/365/2049/20000/5640 |

Generated artifacts: **NONE**. Required created production symbol:
`resolveSuccessionQuestionsWithOpeningVoice`. Retired symbols: **NONE**.

## 8. Ordered coding sequence and preflight

Before editing, the coordinator dispatches the sealed packet:

```sh
npm run implementation:dispatch -- GR-4B-IIIA
```

The capsule must prove exact authority bytes, branch/base ancestry, both CREATE
targets absent, every pre-existing target hash, required-symbol evidence, clean
targets, and the annex hash. Then:

1. Capture the hot-file raw/effective counts, current registry counts, both
   TypeScript ratchets, OSR count, focused green predecessor tests, and census.
2. Create the one test file with the exact eight titles below and establish the
   intended missing-behavior reds without registering a ninth title.
3. Add the seven-family corpus and grammar row.
4. Implement the pure wrapper/composer.
5. Make the one-for-one 1264/797 treaty-stage replacement.
6. Add the four remaining production registrations and the three existing
   walker registrations.
7. Re-derive the whole census once, then run focused checks.
8. Execute the eight disposable mutants outside the shared worktree, restore
   every target digest exactly, and rerun focused green.
9. Run the attached sealed runner once. Then obtain an independent bare
   `check:tail` receipt and separate boot smoke. Do not start soak or adjacent
   work.

## 9. Acceptance matrix — exactly eight titles

All cases live under one literal describe title:
`GR-4b-iii-a — the question-opening voice`.

| ID | Exact `it` title | Required observation |
|---|---|---|
| A1 | `a newly inserted proposal emits exactly one opening beat` | A valid newly inserted pending row remains exact; the oath is untouched; one fully addressed notable/public beat carries the outcome reason, identity, instrument parties, and no ending. A deliberately unresolvable presentation fixture still queues but yields no beat. |
| A2 | `all seven annex-verbatim families and slot vectors are reachable` | Actual wrapper/composer calls with real outcome identities deterministically reach all seven exact corrected lines and exact slot vectors; `{npc}` is the fallen holder, `{band}` and successor names never appear. |
| A3 | `absent or false gates, direct handling, and HONOR emit no opening beat` | Both non-true gate states preserve landed direct behavior byte-for-byte; a direct DISAVOW keeps its existing alpha beat unchanged; direct or lit HONOR mints no opening beat. |
| A4 | `duplicates, retained terminals, and collisions emit no second opening beat` | Same-input replay, pre-existing pending/applied/dismissed/expired rows, ID/key collisions, and malformed rows never reopen or double-voice. |
| A5 | `two instruments get distinct identities and reverse enumeration stays stable` | Two treaties between the same courts produce distinct per-outcome IDs; reversing input insertion order produces identical canonical proposal and beat bytes in identical order. Landed `successionQuestionsForTick` codepoint-sorts seat-transition rows and treaty keys, and the wrapper preserves that final canonical proposal order without adding a sort. |
| A6 | `the same input is byte-stable and draws no randomness` | Repeated and JSON-round-tripped inputs are byte-stable; draw/clock sentinels remain zero; insertion order does not become authority. |
| A7 | `apply, dismiss, expiry, overflow, undo, and replay never mint terminal or duplicate opening voice` | Opening voice occurs only on first pending insertion; every terminal route, overflow, undo, regenerated pending row, and replay mints no terminal or duplicate opening line while existing alpha apply behavior remains separately owned. |
| A8 | `a real organic ladder succession reaches the late treaty stage and emits one fully addressed public beat` | The real ladder transition reaches the real `advanceTreaties` mount in the same tick, inserts one pending proposal, leaves the treaty intact, and returns one exact public opening beat. |

Assertions may loop inside a case. Test registration may not.

## 10. Mutation proof — eight controlled mutants

No standing mutation manifest or sweep row is owed: the new test filename is
outside the enforced mutation-test nomenclature and directories. Test trust is
proved with eight disposable source mutants in an isolated immutable candidate.
For every mutant, prove source bytes changed, require nonzero test exit and the
named A-title red, restore exact pre-mutant SHA-256, and rerun focused green.

| Mutant | Controlled change | Required conviction |
|---|---|---|
| M1 | suppress the opening-beat append | A1 or A8 |
| M2 | replace the receipt seed with one constant | A2 |
| M3 | allow composition in dark/direct/HONOR mode | A3 |
| M4 | remove the pre/post-new or exact-pending filter | A4 and/or A7 |
| M5 | use court-pair/tick identity instead of validated outcome ID | A5 |
| M6 | introduce a draw or unstable seed/order | A6 |
| M7 | compose during terminal transition or replay | A7 |
| M8 | revert the `peaceTerms.js` mount to raw `resolveSuccessionQuestions` | A8 |

A no-op plant, zero exit, wrong-title red, broad crash, dirty-tree override, or
non-exact restoration is a STOP. Do not run the estate-wide shared-tree sweep.

## 11. Coupling, OSR, hazard, and gate obligations

### Coupling

The `treaty*.js` filename places the new source leaf in the existing GRAMMAR
layer. Its decision, grammar, stable-id, oath, orientation, and treaty-ledger
imports are existing GRAMMAR/shared substrate and mint no new layer pair. Run
`couplingInclusion.walker.test.js`. Do not edit a coupling registry or baseline.
Any successor-name INTERIOR import or measured new pair is a source-repair STOP.

### Observed-shape reader law

Use `successionQuestionPayloadTuple` for the closed outcome projection and
`treatyLedgerOf` for the canonical treaty ledger. Do not manually traverse
arbitrary proposal payload shapes or copy the ledger reader. Run
`check:observed-shape-readers` before and after. Any new finding must be repaired
in the leaf or STOP; the observed-shape baseline is forbidden.

### Existing hazard dispositions

- **HZ-SIZECEILING:** `peaceTerms.js` closes at exact 1264/797; new leaf stays
  under both packet and repository ceilings.
- **HZ-NEWFILECEILING:** both TypeScript ratchets remain zero-error for both new
  paths; no baseline raise.
- **HZ-READERNOWRITER:** only canonical payload/ledger/oath readers; no writer.
- **HZ-CROSSHOME:** run news-voice, headline, and prose-family contracts; all
  authoring fields are complete and no baseline moves.
- **HZ-MUTANTNOOP / HZ-TESTVACUITY:** controlled-byte and exact-title mutation
  receipts above.
- **HZ-DISTBOOT:** the new static import edge requires a separate boot smoke.
- **HZ-GATEMUTEX:** every Vitest invocation runs through the governed mutex; no
  parallel Vitest owner.
- **HZ-SINGLEWRITER:** one coding writer and one coordinator; no shared-target
  edits from another lane.
- **HZ-PIPEEXIT:** final full-gate evidence is bare `check:tail` with true exit.

Run both hazard-registry and pre-mortem validators. The existing registry
already names these classes; no new hazard row is owed. A new working warning
or trigger requiring a thirteenth path is STOP and chair return.

### Sealed checks

The structured manifest owns exactly ten executable children:

1. packet validation;
2. hazard-registry validation;
3. pre-mortem validation;
4. scoped ESLint;
5. the mutexed direct/predecessor/contract battery;
6. full TypeScript ratchet;
7. strict-domain TypeScript ratchet;
8. observed-shape reader validation;
9. bare full `check:tail`;
10. separate `smoke:boot`.

The sealed runner is not one of its own children. After the attached sealed
receipt, the coordinator independently repeats the bare full gate and boot
smoke for landing evidence.

## 12. Completion receipt and STOP conditions

The implementer returns:

- exact base, candidate SHA, and manifest-ordered target SHA-256 list;
- exact changed-path set and `+/-` diff statistic;
- raw/effective line counts and per-file deltas;
- A1-A8 direct result and the wider battery result;
- all live/post registry counts and the whole census derivation;
- eight mutant commands, changed-byte proof, exact failing titles/exits, and
  post-restore digests;
- both TypeScript ratchets and OSR result;
- capsule digest, session/seal IDs, and complete attached runner receipt;
- independent `check:tail` exit, log path/SHA-256/lines/bytes, total tests,
  frozen known failures, build/prerender total, and strict-dist count;
- independent boot-smoke exit, chunk/edge/shell figures and any classified
  benign diagnostic;
- explicit statement: **no soak started**.

Stop and return to the chair on any of:

- authority, base, branch, annex, required-symbol, fingerprint, or CREATE
  absence mismatch;
- a thirteenth path, sixth registration file, ninth acceptance title, or any
  inherited `12 → 14` claim;
- any `peaceTerms.js` raw/effective motion;
- any new flag, tuning, state family, migration, golden, baseline, ceiling,
  dependency, dossier, terminal-packet, annex, mutation-manifest,
  hazard-registry, coupling-registry, or observed-shape artifact edit;
- registry/census/runtime denominator different from the exact figures above;
- a successor or roster lookup, grouped-instrument claim, fabricated slot,
  partial address, terminal voice, duplicate opening voice, or presentation
  failure that vetoes mechanics;
- ungoverned Vitest concurrency, recursive sealed runner, failed/ambiguous
  mutant, incomplete restore, nonzero validator/gate/smoke, or foreign dirt;
- any soak work.

This packet ends after the opening beat lands and its receipts are attached.
It does not continue into GR-4b-ii, GR-4b-iii-b, GR-5, or soak.
