# Town cartography / MF-T2G — the §288-nonconforming light profile STRIPPED from every published projection (§299.3(b) discharged): a record-only deletion inside a dormant module, with the twelve-pin digest re-record declared, captured and bounded

- **Status:** READY
- **Packet version:** 1
- **Verified base:** `claude/composite-r4` at `cb369d2b0b63422db6f91af6c0f4feabcf5ff2dc`
  (the MF-T2F landing), **read from git at this lane's start and never extended from a quoted
  prefix** (§381's fabricated-SHA law). Every preflight row below was re-executed at THIS
  base; nothing is inherited from the compile, from the capsule, or from a sibling. The
  compile (TC-T2G) was pinned to `a09138d7` and marked every figure `STOP: RE-DERIVE AT
  BASE`; each re-derivation is recorded in §4 with its instrument.
  ⚠ **This member was first built and fully proven at `a09138d7`, then carried forward across
  THREE landings — WF-8A, the OSR schema-9 mint, and MF-T2F.** The overlap was measured file
  by file with `comm`: **none of the five code/test hosts collides**; exactly the three shared
  meta paths do (`PACKET_MANIFEST.json`, `INDEX.md`, the census walker), and each was
  **byte-restored from the new base and this lane's appends re-run on top**, never merged. The
  mint is disjoint from this member entirely. `package.json`/`package-lock.json` did not move
  in the window, so no schema-mint trigger fired.
- **Depends on:** nothing by symbol. This member consumes no export of MF-T2B/C/D/E/F. Its
  substrate is the T1V/T1S/T1X-landed codex projection stack (`fabric/projection.js`, the
  barrel), all landed long before this wave. The order E → F → G is TRAIN order and
  census-path staged promotion, not dependency. **MF-T2Bf (§372) binds MF-T2H, not this
  member** — re-proven at this base in §4 row 6 and §4.3.
- **Family preamble:** `docs/implementation/preambles/MF-PREAMBLE.md`, SHA-256
  `0706aad6a84e4f74ec47602e9e3222fc398c4c8864bb38c79ac480c2a11db4ed` — **recomputed from the
  file at this base by the implementing lane**, identical to the value the compile carried, so
  no re-stamp occurred in the window. Its §P1 refutations, §P2 hazard dispositions, §P3 anchor
  preflight, §P5 census law, §P6 mutant hygiene, §P7 STOP set and §P8 capsule law bind this
  packet and are not restated.
- **Collision group:** `d3a-port` — this member reserves the shared census path
  `tests/lint/sovereigntyLightingContract.walker.test.js` AND the barrel
  `src/domain/townMap/fabric/index.js`, plus `fabric/projection.js` and three domain test
  files (§7). Staged promotion (`CR-HB2B-SPLITP`; preamble §P7.11): this packet is not
  promoted while any other D3a member is non-terminal on a shared path.
- **Commit authority:** the executing lane commits on its own detached ref; the chair moves
  the branch.

> **`censusAuthorization`:** this packet moves the test census by **two titles and nothing
> else** — `+0 files / +0 parked / +0 credited / +2 titles / +0 suiteTitles`. **Base tuple,
> convicted at this base: `2497 / 364 / 2133 / 20712 / 5785`. After tuple, convicted at this
> base: `2497 / 364 / 2133 / 20714 / 5785`. The DELTA is what this member owns; the tuple is
> a reading of a moving estate and was re-derived from scratch at the carried-forward base.** Its authorizing decisions are **ODQ §312** (the
> D3a dispatch whose §312.1 roster carries the §299.3 obligations as a named track) and **ODQ
> §375** (the chair's ruling of this member, which ratified the record-vs-shadows boundary and
> accepted the declared one-time digest shift), under the wave charter at **§310.4** and
> §299.4's binding-forward rule. The family's stamp is **GRANTED** at ODQ §312.2b.

> ⛔ **SOURCE PROVENANCE — not a port member.** There is no sandbox source and no port-source
> hash: the strip deletes app-side code that MF-T1V landed. The provenance rows are the EDIT
> HOSTS, hashed at THIS base by the implementing lane; all five match the values the compile
> recorded, so the projection surface did not move in the window:
>
> | file | SHA-256 at the verified base |
> |---|---|
> | `src/domain/townMap/fabric/projection.js` | `769254042f48a7977086ffeef064d431490c33a9691e42cdb837ec5139564c67` |
> | `src/domain/townMap/fabric/index.js` | `fc7286e23287811e70cbc00db9da4d578ea0cc29af6b8c07da65bb2a33a6819e` |
> | `tests/domain/townMapMassingProjection.test.js` | `3f51dfe854569b4e54fd19e5dd8096376ff02dcf1c77d21ff60f923891b4816a` |
> | `tests/domain/townMapMassingPersistence.test.js` | `f0488ae6c7a6c6a2ee643ef89704c0e63d1ebf9391e00550aab35a6e31360d9a` |
> | `tests/domain/townMapFantasyConstruction.test.js` | `3b3cbc7bd3d542f8665cf00cb05bfb010961a51c2ebcc0f052b5ac8fff3f86a3` |
>
> Law: ODQ §288 (§288.2 one `GLOBAL_CELESTIAL` source with a registered azimuth/elevation
> pair; §288.3 the record carries direction, elevation, receiver separation, unclamped/applied
> lengths, mapping/clamp law, provenance; §288.8 implementation dormant, fixed survey after
> D4) · §299.3(b) (the debit) · §374.1 (the compile dispatch) · §375 (this member's ruling).

---

## 1. Reconciled authority

1. **The offense, measured at this base:** `FIXED_SURVEY_LIGHT_V1` was a sealed artifact of
   kind `PROJECTION_LIGHT_PROFILE` wearing the §288.4 authority name `mode: 'FIXED_SURVEY'`,
   whose key roster was `[artifactId, artifactKind, contentHash, directionQ, mode,
   profileVersion, shadowRunDenominatorQ]` — **none of §288.2/§288.3's required content
   existed** (no source, no azimuth/elevation, no receiver separation, no clamp law, no
   provenance). Every published `FIRST_SLICE_PROJECTION` embedded it through the single seal
   site, so every DM and PUBLIC projection published a dimensional light profile that violates
   the law's shape for one.
2. **The cure is STRIP, not CONFORM** — §299.3(b) offers both; §288.8's safe order places the
   conforming fixed survey after D4; §290.4 forbids enlarging the tranche. **ODQ §375 ratified
   the record-only boundary.** Three deletions, nothing built.
3. **The declared shift, bounded by execution:** removing the field moves the published
   artifact's `contentHash` and NOTHING else. Proven at this base as a WHOLE-BODY claim on all
   six published surfaces rather than field by field: strip `{contentHash, lightProfile}` from
   both sides and the entire remaining body is byte-identical. These are **artifact bytes in a
   dormant module, never seed bytes**, and the motion was captured as goldens before the edit.
4. **Preamble §P2.8 — no incumbent is convicted.** The shadow arithmetic
   (`FIXED_DIRECTION_Q`, `FIXED_SHADOW_DENOMINATOR_Q`, `shadowPolygon`, `convexHull`) is not
   edited and its two constants STAY as module-internal values — after the strip they are
   unexported, unpublished rendering internals making no §288 claim (RAISED-3). The preserved
   region was proved byte-identical by `cmp`, and the SHADOW draw geometry itself was proved
   byte-identical through the strip.
5. **§310.3(9) / §P2.5:** no tuning constant is read, chosen or moved.
6. **BigInt exactness is kernel law and is not implicated:** this member touches no arithmetic.
7. **Live code decides.** Every load-bearing claim was EXECUTED at this base before any pin was
   written: two complete probe trees built from BASE blobs by `git archive` (pristine and
   stripped), the strip applied by exact-text match and `diff -ru` verified to be exactly the
   three regions, the projector driven on the LANDED fixture helpers through the LANDED barrel
   in both trees. **119 checks, zero failures.**

## 2. Outcome

**Observable result:** no published projection carries a light profile. The
`PROJECTION_LIGHT_PROFILE` record, its `light:fixed-survey:v1` identity, its `FIXED_SURVEY`
mode string and the `lightProfile` field are absent from every `FIRST_SLICE_PROJECTION`
artifact, from the module, from the barrel's export surface and from every emitted chunk.
§299.3(b) is discharged by deletion, restoring §288.8's stated dormancy. Everything else about
the projection — artifactId, audience law, primitives, shadows, draw ops, warnings,
determinism, frozen-ness — is byte-for-byte what it was.

**The IN/OUT boundary, named affirmatively:**

| capability | verdict | why |
|---|---|---|
| delete `FIXED_SURVEY_LIGHT_V1` (record + export + barrel row + publication field) | **IN** | the member's charter; §299.3(b); §375 |
| re-record the twelve projection digest pins with declared cause | **IN** | the strip's declared, bounded consequence; captured as goldens first; §375 accepted it |
| rewrite the one `lightProfile` identity arm as an anchored absence pin; add two titles | **IN** | the proof surface; §9 |
| building any §288-conforming profile (source/azimuth/elevation/clamp law) | **OUT — forbidden** | D4 scope; §288.8's safe order; §290.4's stop |
| stripping or editing the SHADOW machinery, `FIXED_DIRECTION_Q`, `FIXED_SHADOW_DENOMINATOR_Q`, `shadowPolygon`, `convexHull` | **OUT — forbidden** | not the debit; §P2.8; §375 ratified RAISED-1 |
| editing `massingProjection.js`, `content.js`, `coordinateAbi.js`, `foundation.js`, any other production file | **OUT — forbidden** | the manifest is closed; T2Bf owns `coordinateAbi.js` |
| editing any landed packet doc quoting the old digests | **OUT — forbidden** | never-re-serialize; landed records are historical (RAISED-4, ratified) |
| any `artifactId`, draw-op, primitive, save-bytes or seed motion | **OUT — a STOP if observed** | the probes proved none occurs at this base |
| any consumer wiring, any new record, any stamp | **OUT** | this member mints nothing |

**Definition of done:** the three deletion regions are applied; the acceptance matrix passes
with every pin written from base captures; the census is re-recorded (+2 titles only); every
digest pin OUTSIDE the declared twelve, every other fabric-touching test result, both typecheck
ceilings and the entry closure are unchanged.

**Explicit non-goals, named affirmatively:** no D4 light of any kind; no shadow-model opinion;
no `heightQ()` motion; no frontage/parcel claim (§P7.14); no rung, no tuning, no dependency
bump, no version-string motion; no edit to the sweep-plant anchor host (`dcelEmbedding.js` is
not opened).

## 3. Hard scope budget

| Limit | Packet budget | Standard |
|---|---:|---:|
| Behavior families | `1` | 1 |
| New persisted record families | `0` | ≤1 |
| Named state writers | `0` | ≤1 |
| Feature flags | `0` | ≤1 |
| User-facing surfaces | `0` | ≤1 |
| Direct production consumers | `0` | ≤2 |
| New logic-bearing production leaves | `0` | ≤2 |
| Existing logic-bearing production files modified | `2` — `fabric/projection.js` · `fabric/index.js` | ≤3 |
| Additional registration-only files | `0` | ≤3 |
| Handwritten files total | `7` (2 src · 3 test edits · 1 census re-record · this packet) | ≤12 |
| New/changed effective production lines | **`10` changed, net −10 (measured)** | ≤400 |
| Effective lines per new leaf | n/a — no new leaf | ≤250 |
| Delta in a shared/hot file | `0` — no manifest path is on the hot-file list (re-read at this base) | ≤15 |
| Acceptance cases | `8` | ≤8 |

Overrides approved before dispatch: `NONE`.

## 4. Preflight, executed at this base

| # | Check | Result at the verified base |
|---|---|---|
| 0 | `shasum -a 256 docs/implementation/preambles/MF-PREAMBLE.md` | `0706aad6…1db4ed` — MATCH |
| 1 | edit-host re-hash, five rows | ALL FIVE MATCH the compile's recorded values |
| 2 | fidelity: the three domain test files run green, the twelve pins and the identity arm intact | 4 files / 23 tests passed, exit 0 |
| 3 | `git grep -n 'FIXED_SURVEY_LIGHT_V1' -- src tests` | exactly FIVE lines (projection 20/362 · index 99 · massingProjection 7/119) — no new consumer |
| 4 | the single-declaration walker, focused | green; fabric declared names **150 → 149** across 24 modules (MF-T2F added ten and two files), against a ≥94 floor and an 18-file floor — the floor is cleared by 55 |
| 5 | the fabric-touching battery, per-file counts BEFORE the first edit | **40** files under `tests/` import `townMap/fabric` (38 test files + 2 fixtures; MF-T2F added two); the before-arm was run in a SEPARATE worktree at this base |
| 6 | `git grep -n 'canonicalBytes' -- projection.js foundation.js massingProjection.js` | **exit 1, zero hits** — the T2Bf preimage boundary holds |
| 7 | the census tuple, by conviction | `2497 / 364 / 2133 / 20712 / 5785` — green by equality AND by a -1 sentinel reading `expected 20712 to be -1` |
| 8 | both typecheck configurations | `typecheck:ratchet` **173 / ceiling 173**; `typecheck:domain:strict` **1134 / ceiling 1134** — both at ceiling, zero headroom |
| 9 | §P2.9 unedited-substrate captures | §4.2, executed on trees built from BASE blobs |

### 4.2 · The pristine-vs-stripped comparison, executed at this base

Two complete trees extracted by `git archive` from the base blobs — never the working tree —
with the strip applied to the stripped copy by exact-text match and `diff -ru` proving the
whole-tree difference is exactly the three regions in two files. **119 checks PASS, 0 FAIL.**

```
P0  the offense, measured: zero of the five §288.2/3 content classes present on the record
P1  FIDELITY — the pristine tree reproduces all four DRIVEN landed pins figure-for-figure
P2  THE DECLARED MOTION, four driven surfaces (legacy doc + direct massing × PUBLIC/DM):
      artifactId UNCHANGED · contentHash MOVED · semanticPrimitives, drawOps, sourceAuthority,
      warnings, audience and artifactKind each byte-identical · and the WHOLE BODY minus
      {contentHash, lightProfile} byte-identical, which is the claim as a claim rather than a
      list of fields · the stripped contentHash reseals its own body (no stale seal)
P3  key roster exact: pristine minus exactly lightProfile, on every surface
P4  byte scan with the §372.3 found-the-symbol control: each forbidden string FOUND in the
    pristine bytes first, ABSENT in the stripped bytes; control found in both
P5  shadows preserved: {GROUND 4, FRONTAGE 8, SHADOW 2, BUILDING 3, ROOF_RIDGE 1} identical,
    18/18 primitives and draw ops, and the SHADOW draw GEOMETRY byte-identical — the [2,1]/4
    residue demonstrably still fires
P6  stripped replay byte-identical; result deep-frozen
P7  barrel: the record exported pristine, absent stripped; the projector still exported; the
    export count falls by exactly one
P8  single-declaration floor 150 → 149; zero duplicates both trees; EXACTLY ONE name
    disappeared and it is the record's
P9  effective delta: projection.js 374 → 365 · index.js 112 → 111
P10 ⭐ THE SAVED PATH, which the compile could not execute and left to this lane. Both saved
    pins reproduced pristine first, then measured stripped; whole-body-minus-two byte-identical;
    and the NEGATIVE CONTROL: the saved massing DOCUMENT digest does NOT move, because
    documents never embedded the profile.
```

**The six distinct values behind the twelve literals, measured at this base:**

| surface | before | after |
|---|---|---|
| legacy PUBLIC | `scene-v1-9820c3f2273a313f22ff9246c65e1891` | `scene-v1-f9b571a76668a7eee533f1eb0d091db0` |
| legacy DM | `scene-v1-1945e8a01df6c24788c3393c4e3c452d` | `scene-v1-d143b6efee808314e8dde7ab5ea9f42a` |
| direct PUBLIC | `scene-v1-2f65d0d84551c889da48e08ceb1eb27f` | `scene-v1-1321ec6f9b44ec8e1b4e303334e8b964` |
| direct DM | `scene-v1-bc2930de2297900d8715aa32b1a884c3` | `scene-v1-bbd197b33ba3de7c61b76ac7f4e7f352` |
| saved PUBLIC | `scene-v1-b90080500e9e0a99b6ee469444832743` | `scene-v1-41f2d7037e51306297e632379fc2664c` |
| saved DM | `scene-v1-47ec385c5ea913fbad576ef8cc2d2219` | `scene-v1-9880b235ec01b4162a05d711a2d3e7bc` |

Each after-value was captured TWICE independently and agreed: once by the probe, once from the
live interior-red failure messages of the three suites.

### 4.3 · The MF-T2Bf independence, re-proven at this base

Two arms, because the grep alone would only show that one spelling is absent:

1. **The import chain, read whole.** `projection.js` publishes through
   `sealCanonicalArtifact` in `foundation.js`, which digests through `sceneDigest` in
   `townScene/stableScene.js`. `stableScene.js` imports NOTHING — it is a self-contained
   FNV-1a serializer — and `foundation.js` imports only `MAX_WORLD_UNITS` from
   `coordinateAbi.js`. `canonicalBytes` is nowhere in that chain, and the grep over the three
   fabric modules returns exit 1.
2. **The digest-pin reproduction.** The pristine probe tree reproduced ALL SIX landed pins
   figure-for-figure through exactly this path. A preimage this member's motion depended on
   could not have reproduced the landed values while the separator defect stood.

### 4.4 · The effective-line basis

Measured with **eslint's own `Linter`** under `max-lines {skipBlankLines, skipComments}`, never
`wc -l`: `projection.js` **374 → 365 (−9)**, `index.js` **112 → 111 (−1)**. **Net −10**, ten
changed effective lines, inside the §3 cap. The two-line authority comment costs zero.

⚠ **A measurement hazard found and closed in passing:** eslint's flat config matches its
`files` patterns against the CWD, so running the `Linter` on a path outside the tree returns
*No matching configuration found* and NO `max-lines` message — which the first cut of the
instrument read as **zero effective lines**. The instrument now aborts on both shapes rather
than printing a silent zero. Recorded because the same shape would misprice any packet.

## 5. Verified tree contract

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| The strip host | `src/domain/townMap/fabric/projection.js` | `FIXED_SURVEY_LIGHT_V1` · the `lightProfile:` field | the §288 nonconformance measured; the field is the ONLY publication (one seal site); `FIXED_DIRECTION_Q`/`FIXED_SHADOW_DENOMINATOR_Q` are separately declared and CONSUMED by `shadowPolygon` — they stay | delete the record block and the field; touch nothing else except the authority comment |
| The barrel | `src/domain/townMap/fabric/index.js` | the `FIXED_SURVEY_LIGHT_V1,` row | one row in the projection export block; the other three rows stay | delete the one row |
| The digest seam | `fabric/foundation.js` → `townScene/stableScene.js` | `sealCanonicalArtifact` / `sceneDigest` | the preimage is the artifact body; `canonicalBytes` is NOT in this chain (§4.3) | READ-ONLY; never edited |
| The identity-arm host | `tests/domain/townMapMassingProjection.test.js` | the import row · the identity arm · the legacy digest pair | the only test reference to the profile | remove the import; rewrite the arm as A1; re-record the two pins; add the two new titles |
| The four-pin host | `tests/domain/townMapMassingPersistence.test.js` | A6 | direct PUBLIC/DM + legacy PUBLIC/DM literals | re-record with declared cause |
| The six-pin host | `tests/domain/townMapFantasyConstruction.test.js` | A6 | saved + direct + legacy pairs; ⚠ `LEGACY_HASH` is a saved-DOCUMENT digest and must NOT move | re-record the six; `LEGACY_HASH` untouched |
| Census | `tests/lint/sovereigntyLightingContract.walker.test.js` | `const CENSUS = Object.freeze({` | SEQUENCED, stops at its first red figure | re-record all five together with cause + authorization |
| ⛔ The sweep-plant anchor | `scripts/mutation-sweep.sh` | entry 28a | plants on `dcelEmbedding.js`'s exported signature line; no plant targets `projection.js` or the barrel | not touched |
| The standing walker | `tests/lint/townMapFabricSingleDeclaration.walker.test.js` | name floor ≥94 | a removal member's exposure is the FLOOR; 150 → 149 | runs focusedly; never edited |
| Anchor helper | `tests/helpers/anchoredNegatives.js` | `expectAbsentWithAnchor` | already imported by the identity-arm host | by name on the same line, per §P3 |

**Forbidden alternatives:** no edit to any production file beyond the two hosts; no edit to any
landed packet record; no `test.each`, no `describe.runIf`, no loop-registered tests; no new
test file; no new fixture ids; no files outside the manifest.

## 6. Exact contracts

### The production edit — three deletion regions, one comment

The `FIXED_SURVEY_LIGHT_V1` export block is deleted and replaced by a two-line authority
comment naming this member, ODQ §299.3b and §288.8. The `lightProfile:` line is deleted from
the sealed publication. The barrel row is deleted. `FIXED_DIRECTION_Q` and
`FIXED_SHADOW_DENOMINATOR_Q` stay, and the `sealCanonicalArtifact` import stays (still
consumed at the publication site). The published `FIRST_SLICE_PROJECTION` roster afterwards,
pinned exactly by A3: `[artifactId, artifactKind, audience, contentHash, drawOps,
semanticPrimitives, sourceAuthority, warnings]`.

### The re-record contract (all twelve pins, one law)

Each re-recorded block carries a cause comment naming this member and its authorization, and
every new value is this lane's OWN capture at this base. ⛔ The re-record is proven by a
**key-by-key diff**: exactly twelve occurrences move, zero pins added, zero removed,
`LEGACY_HASH` and every non-projection literal byte-identical.

### Determinism

Unchanged by construction — the projector's arithmetic, ordering and freezing are untouched;
replay identity was executed on the stripped tree. A8 stays green untouched.

### Flag, dormancy, lifecycle, receipts

- Flag: `NONE`. Golden posture: **the twelve declared pins move with cause; every other golden
  unchanged and PROVEN unchanged** by the untouched-pin control (§10), the post-build fence and
  the forensic zoom.
- Lifecycle: no persisted state is touched; the projection is a pure derivation; no
  reload/regen/undo/migration seam exists for a record that no longer exists. Save bytes are
  pinned unchanged by the existing arms — documents never embedded the profile, and that was
  executed as a negative control rather than asserted.
- Records UNSTAMPED trivially: this member mints no record of any kind.
- Receipts/privacy: closed kinds `NONE`; DM-only fields `NONE`. Audience law untouched — the
  strip was executed on both audiences.
- Alignment: `DECLARED EMPTY: a deletion has no alignment surface.`
- Edit story: `ENGINE-ONLY: nothing is DM-editable and nothing is proposed.`

## 7. Exact change manifest

| Action | File | Symbol/region | Max delta | Coding instruction |
|---|---|---|---|---|
| `MODIFY` | `src/domain/townMap/fabric/projection.js` | delete the record block + the publication field; add the two-line comment | **≤ 10 changed eff, net −9** | §6; nothing else in the file moves |
| `MODIFY` | `src/domain/townMap/fabric/index.js` | delete the one export row | **≤ 2 changed eff, net −1** | §6 |
| `TEST` | `tests/domain/townMapMassingProjection.test.js` | remove the import; rewrite the identity arm (A1); re-record two pins; add two `it()` titles (A2, A3) | `n/a` | straight-line `it()` calls, string-literal titles prefixed `MF-T2G`, inside the existing describe; anchored per §P3 |
| `TEST` | `tests/domain/townMapMassingPersistence.test.js` | re-record the four A6 pins | `n/a` | the §6 re-record contract |
| `TEST` | `tests/domain/townMapFantasyConstruction.test.js` | re-record the six A6 pins | `n/a` | the §6 re-record contract; `LEGACY_HASH` untouched |
| `TEST` | `tests/lint/sovereigntyLightingContract.walker.test.js` | the `CENSUS` object | one re-record block | ONE block naming its cause and the `censusAuthorization` refs; all five figures re-derived together; ⛔ scoped BY UNIQUE KEY, never a blanket substitution |
| `DOC` | `docs/implementation/packets/town-cartography/MF-T2G.md` | this packet | `n/a` | the packet, its status transitions, and its §12 landing record |

Generated artifacts: `NONE`. ⚠ No `tests/lint/**` file is minted, so §P3b's mutation-coverage
row does not attach. §P4's registration template is satisfied by its "reason none exists" arm:
nothing new is registered anywhere — this member only removes. **Symbols the deliverable
creates:** the two new `it` titles join `requiredSymbols` at the flip to LANDED, never at
READY. ⛔ **No re-recorded digest FIGURE enters `requiredSymbols`.** `retiredSymbols`:
**`FIXED_SURVEY_LIGHT_V1`**, with its §299.3(b) authority; no successor exists and none is
named.

## 8. Ordered coding sequence

0. Run every §4 preflight. STOP on any mismatch per the rows' own terms.
1. Capture the golden/dormancy evidence BEFORE the first edit: the battery's before-arm in a
   separate worktree at this base; the census tuple by conviction; the twelve pins driven
   green; the §4.2 probes on trees built from BASE blobs; and the emitted-chunk baseline for
   the forensic zoom, with a found-the-symbol control and both denominators named.
2. Rewrite the A1 arm to the anchored absence form — red against the unedited module.
3. Apply the three deletion regions + the comment. A1 goes green.
4. — (no writer, no lifecycle seam) · 5. — (no production consumer) · 6. — (no registration)
7. Add A2/A3; re-record the twelve pins FROM the step-1-after captures; re-record the census.
8. Focused verification (§10) including the anchor preflight and the untouched-pin control; the
   removing-power sweep; the post-build dormancy discharge with the forensic zoom; then the
   wave-end gate and the completion receipt.

## 9. Acceptance matrix

| ID | Case |
|---|---|
| **A1** | The rewritten identity arm — the published projection carries no light profile. Positive control FIRST (already in the arm): the artifact kind, the 18-primitive census with `SHADOW: 2`, the shadow hull shapes; then the anchored absence, routed through `expectAbsentWithAnchor` by name with a sibling key as the liveness anchor. |
| **A2** | ⭐ NEW — the §299.3(b) strip pin, all four driven surfaces + the barrel. The §372.3 found-the-symbol control FIRST: each surface's canonical bytes are proved to carry a known-present symbol; then each of the four forbidden strings is absent from every surface's bytes through the anchored helper. The barrel arm asserts the record is gone from the namespace with a sibling export as the anchor and the projector's presence as the positive control. |
| **A3** | ⭐ NEW — the exact key roster, on all four surfaces, as the prevention guard in BOTH directions: the profile cannot silently return AND no second field can silently vanish. |
| **A4** | The six-pin re-record (fantasyConstruction A6): the six literals equal this lane's base captures; `LEGACY_HASH` byte-identical; cause comment present. |
| **A5** | The four-pin re-record (massingPersistence A6): the four literals equal the base captures; the save-bytes pins in the same arm unchanged. |
| **A6** | The two-pin re-record (massingProjection legacy pair). |
| **A7** | No-op controls — nothing else moved. The full fabric-touching battery, per-file counts and title sets identical before and after EXCEPT the one named file with exactly the declared delta. |
| **A8** | Determinism/replay of the stripped projector — the existing first-slice determinism property suite, green untouched. |

**Anchor law (§P3):** A1, A2 and A3 assert absences; each routes through
`tests/helpers/anchoredNegatives.js` by name on the same line. A2's vacuity is additionally
closed by the found-the-symbol control: every scan proves it can find a known-present symbol
before any absence is trusted.

**Census motion:** `+0 files · +0 parked · +0 credited · +2 titles (A2, A3) · +0 suites`.
⛔ `parked` stays at its recorded value. ⚠ **NAMED INTERIOR REDS, before they exist:** (1) the
census arm reds at this member's own implementation commit until the tuple is re-recorded, and
because the census is SEQUENCED the red arrives at `titles` — **a red at `files` means a
sibling landed mid-edit, which is a STOP and a resync**; (2) the twelve digest pins are red
between the strip and the re-record, a two-commit window interior to the member.

## 10. Verification commands — the gate skeleton

```sh
# Anchor preflight — MANDATORY, BEFORE the member proof is declared green
npx vitest run tests/lint/negativeAssertionAnchor.walker.test.js ; echo TRUE_EXIT=$?

# Focused static + both typecheck configurations, by name and window
npx eslint src/domain/townMap/fabric/projection.js src/domain/townMap/fabric/index.js \
           tests/domain/townMapMassingProjection.test.js \
           tests/domain/townMapMassingPersistence.test.js \
           tests/domain/townMapFantasyConstruction.test.js \
           tests/lint/sovereigntyLightingContract.walker.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict

# Focused tests — one slot, one command
sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/domain/townMapMassingProjection.test.js \
  tests/domain/townMapMassingPersistence.test.js \
  tests/domain/townMapFantasyConstruction.test.js \
  tests/property/townMapFirstSliceDeterminism.test.js \
  tests/lint/townMapFabricSingleDeclaration.walker.test.js \
  tests/lint/sovereigntyLightingContract.walker.test.js

# THE UNTOUCHED-PIN CONTROL — the full fabric-touching battery, per-file counts and TITLE SETS
# identical before and after, EXCEPT the one named file with exactly the declared delta. The
# before-arm runs in a separate worktree at the verified base, never a working-tree copy.

# THE REMOVING-POWER SWEEP — four mutants + control, each convicting a NAMED branch, each
# restored by byte copy verified with cmp and followed by a clean re-run:
#   M0 pristine-green control
#   M1 re-publish a locally-declared profile at the seal site -> A1, A2, A3 and all 12 pins red
#   M2 re-declare the record and restore the barrel row      -> A2's barrel arm reds, alone
#   M3 delete `warnings` from the sealed publication          -> A3 reds (removal direction)
#   M4 revert ONE re-recorded digest pin                      -> that suite reds, alone

# ⛔ Dormancy: POST-BUILD ONLY (§P2.2 post-§324.5). Build first, then:
npm run build
VERIFY_DIST=1 npx vitest run tests/build/townMapLazy.test.js ; echo TRUE_EXIT=$?
# …and the forensic zoom: the stripped literals appear in ZERO emitted chunks AFTER, against
# the step-1 measured baseline, with a found-the-symbol control asserted first and BOTH
# denominators NAMED. The entry static closure is walked, not assumed.

# Sealed receipt and exact-state handoff; neither is landing authority
npm run check:packet -- MF-T2G
npm run implementation:resume -- MF-T2G

# Wave-end — BARE, fresh shell, never piped, never wrapped, and OUTLAST it in your own turn
npm run check:tail ; echo TRUE_EXIT=$?
```

⛔⛔ **NEVER wrap `npm run check*` in `gate-mutex.sh --run`** — `test:ratchet` re-acquires and
self-deadlocks; **exit 3 is the mutex giving up, not a red.** ⚠ Trust no exit status you did
not capture. ⚠ **Stray protocol (§358.2):** on any non-census stray, locate the run's
`results.json` in TMPDIR by mtime and READ THE ROW — a duration past `testTimeout` is a timeout
kill wearing a red's identity; the frozen census is ELEVEN (§356.3), read from
`scripts/.test-ratchet-baseline.json`, never widened, never inferred from a brief.

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and preamble §P7:

- a live **seed** byte moves — the twelve declared pins are artifact bytes in a dormant module
  and are this member's expected result; anything beyond them is not;
- any pin outside the declared set moves: any `artifactId`, any draw-op or primitive count, any
  save-bytes pin, `LEGACY_HASH`, any non-projection `scene-v1-` literal, the T2C A1 alignment
  pin, or any fabric-touching test file not named in §7;
- a THIRTEENTH moving literal appears, or any movement beyond `contentHash`;
- the §4 row-2 fidelity run is RED at the base;
- the §4 row-3 reference census finds a sixth `FIXED_SURVEY_LIGHT_V1` site;
- the §4 row-6 preimage-independence grep finds a hit;
- any production file outside the two hosts would need editing, or the shadow machinery would
  need touching — STOP-and-report, never a manifest widening;
- any §288-conforming profile content would be BUILT — that is D4's charter;
- any landed packet record would need editing;
- the `export function derivePlanarDcelEmbedding(input) {` anchor line changes in ANY byte, or
  `CURRENT_MAP_TRADITION_ID` becomes imported or declared in any edited file;
- the family preamble's hash does not match a fresh computation at the base;
- the single-declaration walker reds, or its name floor is approached within 5;
- the census moves by other than +2 titles, or `parked` moves at all; a delta smaller than the
  titles added is attributed by reverting one test file at a time, never accepted;
- `tests/build/townMapLazy.test.js` reds POST-BUILD, or the forensic zoom finds a stripped
  literal in ANY emitted chunk after the edit, or the entry static closure grows;
- another D3a member is simultaneously non-terminal on a shared path — wait it out;
- any ratchet, baseline, budget or ceiling would need raising; any tuning constant would be
  touched; either typecheck count would move off its recorded ceiling pair.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into
the next wave.

## 12. Completion receipt

*(filled at the flip to LANDED, and MUST include the twelve-pin before/after digest table —
the declared shift's one-time record, §72.3-form: key-by-key, zero added, zero removed)*

## 13. RAISED — for the chair

| # | the call | disposition |
|---|---|---|
| **1** | The strip's boundary: the RECORD goes, the SHADOWS stay. | **RATIFIED at ODQ §375.** Executed here: the primitive census AND the shadow draw geometry are byte-identical through the strip. |
| **2** | The twelve-pin re-record includes six pins whose arm is titled "preserves predecessor pins". Their semantics survive; the recorded values move once, by declared cause. | **ACCEPTED at ODQ §375.** Titles unchanged. |
| **3** | The residue: `[2,1]/4` survives as two UNPUBLISHED module-internal constants feeding the SHADOW draw-ops — an anonymous rendering convention making no §288 claim. | Ratified; carried into the D4 charter's inputs as a known carry rather than a later discovery. |
| **4** | The old digests remain quoted in landed packet docs and manifest case text. Those are historical landing records; never-re-serialize. | **Left untouched**, per §375. A reader tracing an old digest finds the census walker's MF-T2G cause comment. |
| **5** | The +2 titles land inside the existing MF-T1V describe, title-prefixed `MF-T2G`. | Adopted — the only shape that keeps the census at +2/+0 without minting a file the ceiling law cannot absorb. |
| **6** | `retiredSymbols` names `FIXED_SURVEY_LIGHT_V1` with NO successor. | Adopted — an empty successor is the honest record of a strip. |
| **7** | ⭐ NEW, raised by this lane: **the emitted-chunk MEMBERSHIP of the projection module shifts, benignly.** At the base, `FIRST_SLICE_PROJECTION` lived in the `scenePortraitExport` lazy chunk; after the strip it lives in the fabric barrel's own lazy chunk. Both are lazy chunks; the ENTRY static closure is **8 chunks before and 8 after**, with all five probe literals **0 of 8** in both, and the post-build dormancy fence discharges on 3 EXECUTED tests. | Recorded, not chased. Removing an export changed rollup's module-grouping decision; no dormancy or entry-closure property moved. Named here so a successor reading a chunk-name diff does not re-derive it as a surprise. |
