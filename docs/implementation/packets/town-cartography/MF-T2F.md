# Town cartography / MF-T2F — exact solid legality, extended from area to VOLUME: the §287.5 predicate home ported onto the landed massing vocabulary, with the R-MF-2 volume-arm conviction CURED in BigInt (§299.3(a) discharged app-side)

- **Status:** LANDED
- **Packet version:** 1
- **Verified base:** `claude/composite-r4` at `42e12a76f1dd35e09a8938ad34a08795c33a4f6f`
  (the OSR schema-9 re-freeze, C2). ⚠ **This member was BUILT and fully proven at
  `a09138d7`** — the MF-T2E landing — and then re-based here after WF-8A (`47ea9c9b`) and the
  OSR mint (`158d99cd`, `42e12a76`) took the track first. Both bases are recorded, and §4.5
  carries the re-derivation: **every figure that could move was re-derived at `42e12a76`, and
  one of them did.** Nothing is inherited from the compile, from the capsule, or from a sibling.
  The compile was pinned to `5d18b4a0` and marked every figure `STOP: RE-DERIVE AT BASE`; each
  re-derivation is recorded in §4 with its instrument, and the ONE shear the first re-derivation
  found (§4 row 3) is named rather than absorbed.
- **Chair review:** ODQ §371 ruled all seven RAISED items (§13). The rulings are folded into the
  body below and re-stated in §13 beside the question each answered.
- **Depends on:** `MF-T2E` (**LANDED** — `massPart.js`, the `solidPartQ`/`massPartQ` records whose
  discriminant this member's predicate reads; confirmed present at this base and hashing
  `d52c19fa…`, §4 row 2) and
  `MF-T2B` (the ABI wall, the threaded validators, `deepFreezeCanonical`,
  `COORDINATE_ABI_VERSION`, `bounds` — LANDED). **No symbol of MF-T2C or MF-T2D is consumed**;
  those bind only through train order and the census-path staged promotion. MF-T2K is this
  member's named future consumer (the `DIFFERENT_SUPPORT_SURFACE` refusal is K's substrate), not
  a dependency.
- **Family preamble:** `docs/implementation/preambles/MF-PREAMBLE.md`, SHA-256
  `0706aad6a84e4f74ec47602e9e3222fc398c4c8864bb38c79ac480c2a11db4ed` — **recomputed at this base
  by the implementing lane** (`git show a09138d7:… | shasum -a 256`) and matching the value
  MF-T2E verified at both of its bases, so no further re-stamp has occurred. Its §P1 refutations
  (R-MF-2 governs this member), §P2 hazard dispositions (§P2.8 protects the incumbent), §P3
  anchor preflight, §P3b, §P4 registration template, §P5 census law, §P6 mutant hygiene, §P7
  STOP set and §P8 capsule law bind this packet and are not restated.
- **Collision group:** `d3a-port` — this member reserves ONE shared D3a change path,
  `tests/lint/sovereigntyLightingContract.walker.test.js`. It reserves NO path on
  `src/domain/townMap/fabric/index.js` and NO path on any existing production file (§7).
  Staged promotion (`CR-HB2B-SPLITP`; preamble §P7.11): this packet is not promoted while any
  other D3a member is non-terminal on the census path. ⭐ Measured at this base: MF-T2A/B/C/D/E
  are ALL `LANDED`, so the shared path is uncontended and the promotion is clean.
- **Commit authority:** the executing lane commits on its own detached ref; the chair moves the
  branch.
- **Baseline posture, all re-derived at this base:** no `townMap` path carries a
  `scripts/.size-baseline.json` entry and none is on the hot-file list; the plain `src/domain/**`
  ceiling is 800 effective under `max-lines {skipBlankLines, skipComments}`; the fabric
  single-declaration walker is green and its floors only strengthen; both typecheck
  configurations sit EXACTLY at their ceilings here (`typecheck:ratchet` 173/173,
  `typecheck:domain:strict` 1134/1134), so this member's allowance was ZERO new type errors and
  it spent none; the frozen test-ratchet census is **ELEVEN** failing tests, read from
  `scripts/.test-ratchet-baseline.json` at this base and enumerated in the lane receipt;
  `BASE_STATE.json` remains inadmissible per §P8.

> **`censusAuthorization`:** this packet moves the test census by two credited files, eight
> titles and two suite titles (delta derived in §9; base tuple `STOP: RE-DERIVE AT BASE`). Its
> authorizing decisions are **ODQ §312** (the D3a dispatch whose §312.1 roster carries the
> §299.3 obligations as a named track), **ODQ §365** (the chair's dispatch of this compile:
> *"member F's scope from the §312.1 roster, discharging §299.3(a), carrying the §348.2
> float-volume conviction and its executed fixture as the cure's arm"*), plus a slot for the
> executing seat's own dispatch §, under the wave charter at **§310.4** and §299.4's
> binding-forward rule. The family's stamp is **GRANTED** at ODQ §312.2b; this member continues
> the OPEN eight-cap engine train (A → B → C → D → E → **F**).

> ⭐ **SCOPE DETERMINATION (the boundary argument the dispatch orders).** §365 names member F
> directly, so the determination here is a verification, not a choice — executed against the
> roster and the standing rulings:
>
> 1. **F is the plan's member 5 successor and the only unblocked next member of the open
>    train.** `draft-D3A-PLAN.md` §9.1 hangs F off E (`MF-T2E ── MF-T2F`); E is landed
>    (pending CAS); F's other substrate (MF-T2B) is landed. G is independent but the plan's own
>    order runs the E→F pair first; H/J sit behind D in the plan's graph and are not yet
>    compiled; K explicitly depends on F (*"the order is F → K, not K → F"*); M/P/S are
>    chair-blocked (§299.3c). Nothing in §313–§365 re-scoped the roster (checked section by
>    section at the ledger tip; the §349–§364 rows are OSR/WF/process/content acts).
> 2. **F discharges §299.3(a) — and the discharge boundary is stated exactly.** §299.3(a)'s
>    debit: *"volume legality is collision-by-identity, not the §287.5 exact solid
>    intersection."* The identity site is live at the pin —
>    `src/domain/townMap/fabric/operations.js` (~line 258) refuses on `buildingId`/`plotId`/
>    `constructionOperationId` equality under the message *"fantasy construction collides with
>    the base massing"* — a uniqueness constraint wearing a geometry predicate's name, exactly
>    as the D1 module's header describes. **This member lands the §287.5 predicate home
>    app-side, on real vertical intervals, which is the obligation's substance.** It does NOT
>    edit `operations.js`: the identity check is a legitimate uniqueness constraint and keeps
>    its uniqueness role; wiring the operation path to CONSULT the exact predicate is live
>    engine behavior owing a soak, sits behind the §299.3c vocabulary ruling (MF-T2S), and is
>    recorded as the named successor step, not smuggled in. RAISED-2 asks the chair to confirm
>    this discharge reading.
> 3. **The §348.2 conviction is this member's to cure, by order.** *"The carried T2F conviction
>    (the D1 VOLUME arm multiplies in Number over a float area — R-MF-2 fires at F's port; the
>    executed fixture travels with the seed)."* §4.2 carries the fixture EXECUTED at this
>    compile, both arms.

> ⛔ **SOURCE PROVENANCE — a PORT-BY-HASH member (preamble §P1 R-MF-4), with six recorded
> divergences.** The port source and its context, each cited exactly:
>
> | source | citation | what it supplies |
> |---|---|---|
> | sealed sandbox `fabric/solidLegality.js` (MF-D1) | SHA-256 `b2f59501f1902cd74b470238996c5bd875a84a8934aa8124517d2159bdb7f645` — verified at extraction by this compile from `refs/preserve/map-sandbox-w3f-sealed` blob `0c46df0729b3da609c4fa1c09e67520dd1a4ecdf`; the executor re-hashes; a mismatch is a STOP | THE PORT SOURCE: the result discriminant (`REFUSED`/`PLANAR_ONLY`/`VOLUME`), the refusal order and reason strings, `PLAN_ERA_VERTICAL`, the zero floor and its no-tuning rationale, `intervalOverlapVerdict` as the faithful negative control, `footprintIsAnswerable`, `dualRunLegality` |
> | sealed sandbox `fabric/fabricGeometry.js` | SHA-256 `c40c75b1fff4dd7393677f18e5b755dfb2a7fe17f4bc94a2a30f97621c78ed6e` (extraction-verified) | CONTEXT ONLY — the float arithmetic the source predicate rests on; nothing is ported from it (the landed `exactGeometry.js` is its already-ported closure, and the exact core replaces its role for legality) |
> | landed `massPart.js` (MF-T2E, at `a09138d7`) | the record vocabulary this predicate reads; `solidPartQ` records are admitted DIRECTLY (executed §4.2 P5) | the input shape: `{partId, supportSurfaceId, footprint, vertical:{kind:'INTERVAL', baseQ, topQ}, abiVersion}` |
> | ODQ §287.5 / §299.3(a) / §348.2 / §365 | ledger `docs/OWNER_DECISION_QUEUE.md` @ `6b933f52` | the law (*"exact solid intersection, not XY/Z interval overlap, owns volume legality"*), the obligation, the conviction order, the dispatch |
>
> **The six divergences from the sealed source, each ordered or forced, each executed (§4.2):**
>
> | # | divergence | authority | executed at |
> |---|---|---|---|
> | D-1 | identity key `solidId` → `partId`; the record read is the landed T2E shape, admitted with zero adaptation; the refusal reason string `SAME_SOLID_ID` is retained verbatim | T2E is the landed app vocabulary and F is its named reader; two id spellings would be a second truth | P5, P7 |
> | D-2 | the float area imports (`absArea`, `polygonIntersectionArea`, `triangulateSimple`, `triangulationIsSound`) are NOT taken; an exact BigInt-rational core decides and measures instead; only `bounds` (exact integer compares) is imported from `exactGeometry.js` | §348.2's ordered cure; preamble §P1 R-MF-2; the kernel's exactness precedent (MF-T2C) | P1, P2 |
> | D-3 | published magnitudes become exact: `sharedAreaQ`/`sharedVolumeQ` are frozen `{numQ, denQ}` BigInt rationals in lowest terms (`denQ ≥ 1n`), `sharedHeightQ` is BigInt, `OVERLAP_FLOOR_Q` is `0n`; field NAMES, discriminant kinds, verdicts, refusal reasons and refusal order are unchanged | a Number cannot carry the exact value the cure computes; at toy scale every figure is numerically identical to the sealed predicate's (executed, all five shapes) | P0, P5 |
> | D-4 | `solidOverlap` on a footprint the exact core cannot prove simple THROWS a typed TypeError instead of silently answering from a partial triangulation — the sealed spelling, executed, answers `VOLUME … OVERLAPPING` from garbage on a bowtie | closing an executed silent-garbage path; same class as the plan §1 divergence 4 (throw → typed) | probe 2 |
> | D-5 | `planEraSolid` validates through the landed wall (`requireCanonicalId`/`requireCanonicalInt`) instead of `String()` coercion | the family's one-validator-spelling law | P7 |
> | D-6 | answerability strengthens from a float tolerance (`1e-6`) to an exact identity (Σ|2A(tri)| must equal |2A(ring)| exactly); refusal is conservative-exact and typed | exactness makes the tolerance meaningless; the float spelling REFUSES valid ground at ABI scale (executed: the sliver) | P4, probes 3/4 |

---

## 1. Reconciled authority

1. **ODQ §287.5, verbatim:** *"exact solid intersection, not XY/Z interval overlap, owns volume
   legality."* This member is that law's app-side home. The D1 header's own frame binds the
   port: the contract does not change when the third dimension arrives; only the discriminant
   does — and E supplied `[baseQ, topQ)`, so the VOLUME arm is now real.
2. **ODQ §299.3(a):** the codex slice's volume legality is collision-by-identity — a named
   obligation. Discharged here by landing the predicate home (scope determination item 2; the
   boundary is exact and RAISED-2 confirms it).
3. **ODQ §348.2 / §365 — the carried conviction, now the cure's charter:** the D1 VOLUME arm
   computes `sharedVolumeQ = areaQ * h` in Number over a float `polygonIntersectionArea`.
   ⭐⭐ **EXECUTED at this compile, both arms (§4.2 P1/P2):**
   - **the verdict flips**: on coincident R-MF-2 slivers (`m = 1286630001`) at legal ground the
     sealed predicate answers `VOLUME {sharedAreaQ: 0, sharedVolumeQ: 0, DISJOINT_OR_ABUTTING}`
     where the exact answer is area `1/2`, volume `1500`, `OVERLAPPING` — a legality answer
     inverted on valid ground;
   - **the product drifts at ordinary ground**: coincident squares of side `s = 94906267`
     (≈1.05% of the wall) read sealed `sharedAreaQ = 9007199515875288` (exact − 1) and
     `sharedVolumeQ = 27021598547625865000` (exact − 1784).
   The cure is the exact core of §6, per preamble §P1 R-MF-2: every decision AND every
   published magnitude in the legality arms is BigInt-exact.
4. **Preamble §P2.8 — the incumbent is NOT convicted.** `groundLaw.overlapping` (sandbox §17)
   is unquantified, not wrong (D1 measured `legacyFalsePositive = 0`); it is not ported, not
   edited, not reported as a defect. Likewise the codex `operations.js` identity check keeps
   its uniqueness role; only its claim to the NAME of volume legality ends, and it ends by the
   predicate home existing, not by an edit.
5. **The D1 vertical rule, inherited through E:** half-open `[baseQ, topQ)` on a NAMED support
   surface; different supports refuse rather than answer. Executed: the stacked pair shares
   quantum 5000 and encloses zero volume (§4.2 P0/P5) — the half-open law as behavior.
6. **ODQ §310.3(9) / preamble §P2.5:** no tuning constant. `OVERLAP_FLOOR_Q` stays EXACTLY
   ZERO (typed `0n`); the sealed source's own comment — a tolerance here would be a tuning
   dial on a law — ports with it. No quantum rung is read or chosen.
7. **The one-predicate-home law (§287.12 precedent):** `solidOverlap` is the ONE legality
   spelling. The exact-area core is a separate leaf because the measured basis (§4.4) does not
   fit one leaf under the 250 cap — but its exports are consumed only inside the fabric, the
   float `polygonIntersectionArea` keeps its fixture-scale geometry role untouched, and the
   legality question routes through `solidOverlap` alone. RAISED-3 records the second-leaf
   argument for the chair.
8. **Live code decides.** Every load-bearing figure below was EXECUTED before any pin was
   written (preamble §P2.9): the sealed predicate driven at both scales, the prototype core
   driven on the same fixtures, T2E's landed constructors driven for the interop arm, the
   refusal arms identified per fixture. Captures in §4.2; logs in `laneTCT2F-probe/`
   (`driver.log`, probes 2–4), `TRUE_EXIT=0`.

**Resolved contradictions:**

- *Plan §4's F row prices "existing logic modified: 1 — `fabric/exactGeometry.js`,
  `polygonIntersectionArea` re-exported"* — vs the pin: it is ALREADY public (T2B landed it
  so), and the cure means the legality path no longer consumes it at all. **RESOLUTION: zero
  existing production files modified** (the T2C/T2D/T2E measured precedent). Plan staleness
  §13.1.
- *Plan §4's F row prices ~110 eff, one leaf* — vs the measured basis: the ported contract
  alone is ~137 effective and the ordered BigInt cure adds an exact core of ~145 executed
  effective lines. One leaf breaches the 250 cap; `PACKET_STANDARD` forbids quiet
  renegotiation. **RESOLUTION: two leaves** — `solidLegality.js` (the contract) +
  `exactIntersectionArea.js` (the core) — the exact allowance of two, each under its cap, and
  the same subject split the sandbox itself uses. RAISED-3.
- *The sealed result publishes Number magnitudes* — vs the conviction: a Number cannot carry
  `27021598547625867000` and the cure exists precisely because the Number spelling drifts.
  **RESOLUTION: D-3** — exact BigInt forms under the same field names; verdicts, kinds,
  reasons unchanged; toy-scale figures numerically identical (executed). RAISED-1.
- *The stacked-pair pin (plan §4: "two solids stacked `[0,5)` and `[5,9)` share 100 units of
  area and ZERO volume")* — T2E deliberately left it to this member so the predicate and its
  pin land together. Taken: §9 A3.

The implementer does not read other documents to reinterpret this packet.

## 2. Outcome

**Observable result:** the app-side fabric owns volume legality by exact solid intersection.
`solidOverlap` reads the landed T2E records directly and answers with the D1 discriminant —
`REFUSED` (same id / different support), `PLANAR_ONLY` (either vertical ABSENT, exact shared
area), `VOLUME` (exact shared area, height and volume with the half-open law) — every decision
and every published magnitude exact in BigInt, so the R-MF-2 class cannot produce a wrong
verdict or a drifted figure on any legal ground. The forbidden interval predicate rides along
as a faithful negative control; the dual-run harness turns its wrongness into numbers. Nothing
calls any of it in production; live seeds stay byte-identical; every existing digest, pin and
test result stays exactly where it is.

**The IN/OUT boundary, named affirmatively:**

| capability | verdict | why |
|---|---|---|
| `solidOverlap` — the §287.5 predicate home on T2E records, exact decisions AND magnitudes | **IN** | the member's charter; §299.3(a) |
| the exact core: BigInt ear-clip triangulation with the exactness identity, homogeneous-coordinate clipping, rational shared area | **IN** | the §348.2 cure; without it the predicate inverts verdicts on valid ground (executed) |
| `planEraSolid`, `PLAN_ERA_VERTICAL`, `OVERLAP_FLOOR_Q` (`0n`) | **IN** | the D1 plan-era contract, ported through the landed wall |
| `intervalOverlapVerdict` — the forbidden predicate as faithful negative control | **IN** | D1's own conviction machinery; A4 pins its wrongness |
| `footprintIsAnswerable` + typed refusal of unanswerable footprints inside the predicate | **IN** | D-4/D-6; the sealed silent-garbage path is closed |
| `dualRunLegality` | **IN** | D1's migration shape; A7 proves counts close |
| any consumer wiring — operations, rosters, generation, projection | **OUT — forbidden** | dormant; the operation-path consultation is the named successor (post-T2S, soak-owing) |
| editing `operations.js`, `groundLaw`, any existing production file | **OUT — forbidden** | §P2.8; the discharge boundary (scope determination 2) |
| any edit to `exactGeometry.js` or its float spellings | **OUT** | fixture-scale geometry keeps its role; the cure lives in the new core |
| `SupportSurfaceRef` typing, support connections/registries | **OUT** | MF-T2K (*"F → K, not K → F"*) |
| `heightQ()` motion, world-unit conversion, height publication | **OUT** | §P2.7; intervals stay explicit-input quanta |
| `artifactKind`/`schemaVersion` stamps, persistence seams | **OUT** | results are in-memory verdicts; the T2D/T2E unstamped precedent |
| barrel export, any production consumer | **OUT** | lands DORMANT; the acceptance battery is the only caller |

**Definition of done:** the two leaves export the ten §6 symbols; the acceptance matrix passes
with every pin written from base captures; the census is re-recorded; every digest pin and
every fabric-touching test result is unchanged.

**In scope:**

1. **One primary behavior** — exact solid legality with the volume arm (the contract, the core,
   the negative control and the dual-run are ONE behavior: the predicate without the exact core
   answers wrongly, and without the control its superiority is an assertion).
2. **One necessary integration path** — the T2E→F reader seam: `solidPartQ` records admitted
   directly (executed §4.2 P5); this is the LIVE interop arm T2E's packet promised to this
   member. No production path; the tests are the caller.
3. **One prevention guard** — the answerability refusal (D-4/D-6): a footprint the exact core
   cannot prove simple is refused typed, with the exactness identity as its guard-the-guard
   (the identity CONVICTS completed-but-unsound triangulations — executed, three fixtures).

**Explicit non-goals, named affirmatively:**

- ⛔ **No existing production file moves.** Not the barrel, not `exactGeometry.js`, not
  `massPart.js`, not `operations.js`, not any codex module. The MF-T2A sweep-plant anchor line
  (`export function derivePlanarDcelEmbedding(input) {` at column zero in `dcelEmbedding.js`)
  and the `CURRENT_MAP_TRADITION_ID` non-import are preserved BY CONSTRUCTION — no host that
  carries them is opened — and §11 carries both as STOPs regardless.
- ⛔ **No legality cutover.** Nothing in the generation path calls the exact predicate (§P2.8's
  clause); the cutover is a future wave's declared act.
- ⛔ **No incumbent conviction.** Neither `groundLaw.overlapping` nor the `operations.js`
  identity check is reported as a defect; the first is unquantified-not-wrong, the second is a
  uniqueness constraint that stays.
- ⛔ **No frontage/parcel equivalence claim** (§P7.14); no rung, no tuning constant, no
  dependency bump, no version-string motion, no y-flip.
- Record adjacent discoveries in the receipt; do not investigate or repair them.

## 3. Hard scope budget

| Limit | Packet budget | Standard |
|---|---:|---:|
| Behavior families | `1` | 1 |
| New persisted record families | `0` — in-memory verdicts and vocabulary (T2D/T2E precedent) | ≤1 |
| Named state writers | `0` | ≤1 |
| Feature flags | `0` | ≤1 |
| User-facing surfaces | `0` | ≤1 |
| Direct production consumers | `0` | ≤2 |
| New logic-bearing production leaves | `2` — `fabric/solidLegality.js` · `fabric/exactIntersectionArea.js` | ≤2 |
| Existing logic-bearing production files modified | `0` | ≤3 |
| Additional registration-only files | `0` — deliberately no barrel edit | ≤3 |
| Handwritten files total | `6` (2 src · 2 acceptance · 1 census re-record · this packet) | ≤12 |
| New/changed effective production lines | **≤ `340`** (measured basis §4.4: executed prototype 282 + split-seam glue) | ≤400 |
| Effective lines per new leaf | **≤ `190`** (core) · **≤ `175`** (contract), measured with eslint's own `Linter` at implementation | ≤250 |
| Delta in a shared/hot file | `0` — no manifest path is on the hot-file list (`STOP: RE-DERIVE AT BASE`) | ≤15 |
| Acceptance cases | `8` | ≤8 |

Overrides approved before dispatch: `NONE`. The two-leaf shape IS the cap-respecting split
(`PACKET_STANDARD`: the agent proposes the smallest split rather than renegotiating the
budget); no split MEMBER was seeded — the split is intra-member, two leaves inside the
standard's own allowance.

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- MF-T2F
```

Then, before any edit — every row re-executed at the dispatch base; nothing inherited from this
compile, from the capsule, or from a sibling packet:

| # | Check | Result EXECUTED at the build base `a09138d7` AND re-executed at the landing base `42e12a76` (§4.5) |
|---|---|---|
| 0 | `git show a09138d7:docs/implementation/preambles/MF-PREAMBLE.md \| shasum -a 256` | `0706aad6a84e4f74ec47602e9e3222fc398c4c8864bb38c79ac480c2a11db4ed` — **MATCH**, no further re-stamp |
| 1 | port-source re-hash: `git cat-file blob 0c46df0729b3da609c4fa1c09e67520dd1a4ecdf \| shasum -a 256` | `b2f59501f1902cd74b470238996c5bd875a84a8934aa8124517d2159bdb7f645` — **MATCH** (§P1 R-MF-4 satisfied) |
| 2 | substrate existence: `git show a09138d7:src/domain/townMap/fabric/massPart.js \| shasum -a 256` | PRESENT, `d52c19fadbe6c06bb906d1dc0c93e1a76114f189078943da099c59c07985e76f` — the compile's value unchanged; the §4.2 P5 interop probes reproduce |
| 3 | name-collision scan over `src tests` for the ten arriving names plus the two leaf basenames | ⚠ **SHEAR — ONE HIT, and the compile's cross-check was WRONG.** `grep` exit 0, one line: `src/domain/townMap/fabric/massPart.js:9`, a DOCBLOCK PROSE mention (*"MF-D1's `solidLegality.js` declared it in…"*). Per-name breakdown executed: every one of the ten export names has **zero** files; only the string `solidLegality` matches, and only in a comment. A prose reference is not a live declaration, so this is NOT the STOP condition (§11 names *"any of the ten colliding with a live declaration"*), but the compile asserted "T2E's leaf names none of these" and that assertion was false. Recorded rather than absorbed |
| 4 | `npx vitest run tests/lint/townMapFabricSingleDeclaration.walker.test.js` | green at base (5/5) and green after the leaves land (5/5) — the floors are lower bounds, so the ten arriving exports only strengthen them |
| 5 | the untouched-pin denominator: an EXPLICIT list enumerated at base, the union of MF-T2E's 57-path control set and every `tests/**/townMap*.test.js` basename match | **67 files** at BOTH bases — the list was re-derived at `42e12a76` and is byte-identical, because WF-8A and the mint added no `townMap` suite. The list is fixed at base, so this member's two new files (which match `townMap*`) cannot enter it and cannot move the denominator. BEFORE arm: 67 files / 444 tests / 0 non-passed |
| 6 | the census tuple, by −1 sentinel | build base `2494/364/2130/20684/5782`; **landing base `2495/364/2131/20704/5783`** — WF-8A's one new test file carries twenty titles. Both are proven rather than read: the walker ran GREEN at each base, and every one of its five assertions is an exact equality, so a green run convicts all five figures at once. The landing-base arm was run BARE in a pristine worktree at `42e12a76` |
| 7 | `npm run typecheck:ratchet` · `npm run typecheck:domain:strict` | `173 error(s), ceiling 173` and `1134 errors, ceiling 1134` — both AT ceiling, zero headroom, both `TRUE_EXIT=0`, and unchanged at the landing base |
| 8 | §P2.9 unedited-substrate captures — the §4.2 probes re-executed at base BEFORE any pin was written | ALL REPRODUCE. Sealed blobs re-extracted from `refs/preserve/map-sandbox-w3f-sealed` and re-hashed; app modules re-extracted at `a09138d7` and hashing exactly the compile's values (`foundation 1ada51b6…`, `coordinateAbi 424500a8…`, `exactGeometry dcd16370…`, `settlementFoundation 01bc29b6…`). Driver `TRUE_EXIT=0`, zero FAIL lines; probes 2–4 `TRUE_EXIT=0`. ⛔ The sealed tree is imported by NOTHING in `src/` or `tests/` |

### 4.2 · The captures this packet's pins are written FROM — RE-EXECUTED AT THIS BASE

Re-executed in full by the implementing lane at `a09138d7` (probe tree `laneTET2F-probe/`:
sealed blobs re-extracted from the preserve ref and re-hashed, app modules re-extracted at this
base; `laneTET2F-driver.log` `TRUE_EXIT=0` with zero FAIL lines, `laneTET2F-probe2/3/4.log` each
`TRUE_EXIT=0`). Every figure below reproduced identically to the compile's:

```
P0  port fidelity, toy scale — sealed (float) vs prototype (exact), five shapes, key-adapted
    for the sealed drive only (solidId), DIRECT records for the prototype:
      stacked  [0,5000)x[5000,9000) same support+footprint (10000-square):
               both: VOLUME sharedAreaQ 100000000 · height 0 · volume 0 · DISJOINT_OR_ABUTTING
      overlap  [0,5000)x[4250,9000): both: VOLUME · height 750 · volume 75000000000 · OVERLAPPING
      support  differing ids: both REFUSED / DIFFERENT_SUPPORT_SURFACE, detail string verbatim
      ABSENT   planEra vs INTERVAL: both PLANAR_ONLY · sharedAreaQ 100000000 · verticalStatus ABSENT
      same id  both REFUSED / SAME_SOLID_ID
    every magnitude numerically identical; exact denominators all 1n at toy scale
P1  CONVICTION ARM 1 — the verdict flip (the carried R-MF-2 sliver, m = 1286630001):
      Number shoelace 2A = 0 · BigInt 2A = -1 · landed float area()/absArea() = 0
      landed float polygonIntersectionArea(sliver, sliver) = 0
      sealed:  VOLUME {sharedAreaQ: 0, sharedHeightQ: 3000, sharedVolumeQ: 0,
               verdict: DISJOINT_OR_ABUTTING}          *** wrong on valid ground ***
      proto:   VOLUME {sharedAreaQ: 1/2, sharedHeightQ: 3000n, sharedVolumeQ: 1500,
               verdict: OVERLAPPING}
      sliver-in-containing-square: same flip, same exact answer
      control: ordinary triangle — sealed and proto agree (area 12000000, OVERLAPPING)
      answerability face: sealed footprintIsAnswerable(sliver) = false (refuses valid ground);
      proto = true
P2  CONVICTION ARM 2 — the Number product at ordinary ground (s = 94906267, coincident squares,
    h = 3000):
      exact s^2 = 9007199515875289 · Number s*s = 9007199515875288 (drift -1)
      sealed:  sharedAreaQ 9007199515875288 · sharedVolumeQ 27021598547625865000
      exact:   sharedAreaQ 9007199515875289 · sharedVolumeQ 27021598547625867000
      volume drift: -1784 quanta³ on two ordinary coincident buildings at ~1% of the wall
P3  the L-and-notch conviction of the FORBIDDEN interval predicate (D1's, reproduced):
      interval predicate: OVERLAPPING · exact (sealed AND proto): sharedAreaQ 0,
      DISJOINT_OR_ABUTTING
P4  answerability: bowtie (2A = 0) refused by both spellings; square answerable in both;
      the sliver splits them (P1)
P5  THE LIVE INTEROP ARM — T2E solidPartQ records (built by the LANDED massPart.js through the
    LANDED validators) fed to the prototype DIRECTLY, zero adaptation:
      stacked → VOLUME height 0n volume 0 DISJOINT_OR_ABUTTING
      overlapping → volume 75000000000 exact, OVERLAPPING
      vs planEraSolid → PLANAR_ONLY, sharedAreaQ 100000000, verticalStatus ABSENT
P6  dualRunLegality closes: roster of 4 (one bowtie) → pairs 3 · unanswerable 1 ·
      intervalFalsePositive 1 · legacyFalsePositive 1 with sample carrying sharedAreaQ 0
P7  refusal shapes through the landed validators: uppercase id → 'must be a canonical id';
      float coordinate → 'must be an integer in -9007199254..9007199254'; two-point ring typed;
      SAME_SOLID_ID reads partId on app records
probe2  the sealed predicate on an UNANSWERABLE footprint silently answers:
      solidOverlap(bowtie, square) → VOLUME sharedAreaQ 100000000 OVERLAPPING (garbage from a
      partial float triangulation) — the D-4 divergence's executed justification; the prototype
      throws typed. deepFreezeCanonical accepts BigInt fields (the result-freezing spelling).
probe3/4  three self-crossing rings with NONZERO 2A (48000000 / 42000000 / 72000000) all
      complete ear-clipping and are convicted by the EXACTNESS IDENTITY (e.g. sum 208000000 ≠
      want 48000000) — M8's convicting fixtures, executed; bowtie and collinear fire the
      degenerate arm instead
```

### 4.5 · ⭐⭐ THE RE-BASE ONTO `42e12a76`, AND THE FIGURE THE CARRY WOULD HAVE GOT WRONG

This member was built, swept and fully proven at `a09138d7`. WF-8A and the OSR mint's two commits
then took the track. The four commits were cherry-picked onto the new tip in a FRESH worktree with
its own `npm ci` — never a rebase in place — one at a time, with conflicts reported rather than
resolved.

**What actually conflicted:** exactly one path, `tests/lint/sovereigntyLightingContract.walker.test.js`,
at the `feat` commit. `PACKET_MANIFEST.json` and `INDEX.md` auto-merged (WF-8A's rows and this
member's are disjoint within each file). Resolution was by **byte-restore from the NEW base**
(`git show 42e12a76:… `, `cmp`-verified) followed by re-applying this member's block **below**
WF-8A's, in landing order — never by accepting a merge of the two blocks.

**Overlap, measured by `comm` rather than assumed:**

| against | shared paths |
|---|---|
| the OSR mint (C1 + C2, 8 files) | **NONE — disjoint** |
| WF-8A (22 files) | exactly 3: `INDEX.md`, `PACKET_MANIFEST.json`, the census walker |

The mint's own two C2 files (`scripts/.observed-shape-readers-baseline.json` and
`tests/lint/observedShapeReaders.walker.test.js`) are untouched by this member: `git diff 42e12a76
HEAD` over both is empty.

⭐⭐ **THE CARRY WOULD HAVE BEEN WRONG, AND THIS IS THE FIFTH FIRING OF THE COINCIDENCE LAW.**
At `a09138d7` the measured ends were `2494/364/2130/20684/5782 → 2496/364/2132/20692/5784`. WF-8A
landed ONE new test file carrying **twenty** titles, so at `42e12a76` the true ends are
`2495/364/2131/20704/5783 → 2497/364/2133/20712/5785`. Carrying the earlier AFTER tuple would have
published **`titles: 20692` against a true `20712`** — off by exactly WF-8A's twenty, in the one
figure a reader is least likely to recompute by hand, while the other four stayed self-consistent.
The BASE arm was re-proven by running the walker BARE in a pristine worktree at `42e12a76`, where
it passed 33/33 (which is what says WF-8A and the mint left the census consistent); only then was
the AFTER arm convicted afresh by the cumulative sentinel.

**What did NOT move, verified rather than assumed:** the preamble hash (`0706aad6…`), the sealed
port-source hash (`b2f59501…`), `massPart.js` (`d52c19fa…`), and `foundation.js` /
`coordinateAbi.js` / `exactGeometry.js` / `massPart.js` all byte-identical between the two bases;
the name-collision scan returning the same single prose hit; both typecheck ceilings still exactly
173/173 and 1134/1134; the 67-path untouched-pin denominator re-derived at the new base and
**identical** (WF-8A and the mint added no `townMap` suite); both leaves and both acceptance files
`cmp`-identical to the bytes the nine-mutant sweep ran against, so the sweep transfers with proof
— and it was re-run at the new base anyway, convicting the same arms.

### 4.3 · ⭐ The LIVE captures — the implemented leaves driven before any pin was written

The §4.2 battery proves the PROTOTYPE reproduces at this base. The pins in §9 are written from a
second, separate drive of the **implemented modules themselves** (`laneTET2F-live.log`,
`TRUE_EXIT=0`, ALL CHECKS PASS), because a pin written from a prototype pins the prototype:

```
L0  port fidelity, sealed (float) vs the LANDED leaves, five shapes — every kind, verdict,
    magnitude and refusal string agrees; the DIFFERENT_SUPPORT_SURFACE detail is byte-verbatim
L1  the sliver: Number shoelace 2A = 0 · BigInt 2A = -1n · landed float area() = 0 ·
    polygonIntersectionArea(sliver, sliver) = 0
    sealed VOLUME {sharedAreaQ 0, sharedVolumeQ 0, DISJOINT_OR_ABUTTING}
    live   VOLUME {sharedAreaQ {1n,2n}, sharedHeightQ 3000n, sharedVolumeQ {1500n,1n}, OVERLAPPING}
    sealed footprintIsAnswerable(sliver) = false · live = true
    control: ordinary triangle agrees in both spellings, area {12000000n,1n}
L2  s = 94906267 · exact s² = 9007199515875289n · Number s*s = 9007199515875288
    sealed sharedAreaQ 9007199515875288 · sharedVolumeQ 27021598547625865000
    live   sharedAreaQ {9007199515875289n,1n} · sharedVolumeQ {27021598547625867000n,1n}
    drift: area -1n · volume -1784n
L3  L-and-notch: intervalOverlapVerdict OVERLAPPING · live exact sharedAreaQ {0n,1n},
    DISJOINT_OR_ABUTTING; control (nested squares) both OVERLAPPING
L4  answerability arms, per fixture — bowtie AND collinear fire the DEGENERATE arm (2A = 0);
    cross-A (2A = 48000000) fires the EXACTNESS-IDENTITY arm; square and sliver answerable.
    live solidOverlap(bowtie, square) THROWS TypeError; the sealed spelling ANSWERS
    VOLUME sharedAreaQ 100000000 sharedVolumeQ 300000000000 OVERLAPPING — D-4's justification,
    re-executed here against the real leaves
L5  T2E solidPartQ records straight in, zero adaptation: stacked {0n,1n} volume · overlapping
    {75000000000n,1n} · vs planEraSolid PLANAR_ONLY {100000000n,1n} verticalStatus ABSENT ·
    different support REFUSED · same id REFUSED; every result and every magnitude deep-frozen.
    ghost.vertical === PLAN_ERA_VERTICAL (identity) · abiVersion 1 read live ·
    OVERLAP_FLOOR_Q = 0n, typeof 'bigint'
L6  dualRunLegality: pairs 3 · exactOverlapping 2 · intervalOverlapping 3 · legacyOverlapping 3 ·
    intervalFalsePositive 1 · intervalFalseNegative 0 · legacyFalsePositive 1 · unanswerable 1 ·
    one sample carrying sharedAreaQ {0n,1n}
L7  refusals through the landed wall, verbatim: 'planEraSolid.partId must be a canonical id' ·
    'planEraSolid.footprint[1][0] must be an integer in -9007199254..9007199254' ·
    'planEraSolid.footprint must be a ring of at least three [xQ, zQ] points' ·
    'solidOverlap.a must be an object' · the two exactRatioQ refusals.
    exactRatioQ(-4n,-6n) = {2n,3n} · (4n,-6n) = {-2n,3n} · (0n,7n) = {0n,1n}
L8  replay deep-equal; comment-stripped source of both leaves reaches no clock/float token
```

### 4.4 · The effective-line basis — MEASURED on the landed leaves

Measured with eslint's own `Linter` under the estate's `max-lines {skipBlankLines, skipComments}`
options, never `wc -l`:

| leaf | effective | cap | headroom |
|---|---:|---:|---:|
| `src/domain/townMap/fabric/exactIntersectionArea.js` | **160** | 190 | 30 |
| `src/domain/townMap/fabric/solidLegality.js` | **150** | 175 | 25 |
| packet production total | **310** | 340 | 30 |

The compile's basis was the 282-line executed prototype (core ≈ 145, contract ≈ 137); the landed
split measures 310, the +28 being the seam the split itself costs — the core's exported surface,
its JSDoc type contracts, and the result typedefs the strict configuration requires. Every figure
sits inside the cap the chair fixed at §371, and no cap was renegotiated.

⚠ **This table was re-measured on the COMMITTED bytes, and the first reading was stale.** An
earlier measurement recorded the contract leaf at 147; three further type-annotation edits landed
after it (the result-shape typedefs and the two verdict locals the strict configuration needs),
and the figure moved to 150 without anything re-measuring it. Caught by re-running the instrument
against the committed file rather than trusting the number already written down — the same law as
the census: a figure is a measurement of one tree at one moment, and carrying it forward is a
stale read that happens to look sound.

## 5. Verified tree contract

| Role | File | Symbol | Verified fact | Required use |
|---|---|---|---|---|
| The record vocabulary read | `src/domain/townMap/fabric/massPart.js` (at `a09138d7`) | `solidPartQ` records | `{partId, supportSurfaceId, footprint, vertical:{kind:'INTERVAL', baseQ, topQ}, abiVersion}`; admitted by the prototype DIRECTLY (§4.2 P5) | the predicate's input shape; ⛔ `massPart.js` is NOT imported by either leaf (records arrive as arguments) and NOT edited |
| The integer wall | `fabric/foundation.js` | `requireCanonicalInt`, `requireCanonicalId`, `requireCanonicalRecord`, `deepFreezeCanonical` | wall `±9007199254`; id grammar lowercase-only; refusal messages captured §4.2 P7; `deepFreezeCanonical` accepts BigInt fields (executed, probe 2) | every input refusal and every freeze; ⛔ no second validator spelling |
| The ABI version read | `fabric/coordinateAbi.js` | `COORDINATE_ABI_VERSION` (= 1), `MAX_WORLD_UNITS` | `heightQ()` THROWS and stays throwing | `abiVersion` stamped on `planEraSolid` records — READ, never moved |
| ⚠ The geometry home, imported NARROWLY | `fabric/exactGeometry.js` | `bounds` ONLY | integer bbox compares are exact in Number (values < 2^53) | the fast-reject and the faithful negative control; ⛔ `area`/`absArea`/`polygonIntersectionArea`/`triangulateSimple`/`triangulationIsSound` are NOT imported by either leaf — the R-MF-2 reason, with the P1/P2 figures, lives in both docblocks |
| The port source | sealed `solidLegality.js`, blob `0c46df07…` | the D1 contract | hash `b2f59501…`; §4.2 P0 fidelity | ⛔ NOT imported by any app or test file, ever; base probes extract to scratch |
| ⛔ The sweep-plant anchor | `scripts/mutation-sweep.sh` | entry 28a | plants on `dcelEmbedding.js`'s exported signature line at column zero | not touched; §11 keeps the guard |
| The standing law | `tests/lint/townMapFabricSingleDeclaration.walker.test.js` | single-declaration | green at tip; §4 row 3's scan shows the ten arriving names collide with nothing | binds both leaves' exports |
| Census | `tests/lint/sovereigntyLightingContract.walker.test.js` | `const CENSUS = Object.freeze({` | five-figure tuple, SEQUENCED | re-record all five together with cause + authorization |
| Test precedent | `tests/domain/townMapMassPart.test.js` + `tests/property/townMapMassPartDeterminism.test.js` (T2E) | the two-file proof shape | domain matrix + determinism companion | **copy this proof shape** |
| Anchor helper | `tests/helpers/anchoredNegatives.js` | `expectAbsentWithAnchor`, `expectPresentThenAbsent` | the negative-assertion route | by name on the same line, per §P3 |

**Forbidden alternatives:** no edit to any existing production file; no import of the sealed
sandbox tree from any app or test file; no import of `massPart.js` by either leaf; no
`Math.sqrt`/trig/clock/RNG/locale in either leaf (integer and BigInt arithmetic only — stricter
than `exactGeometry.js`, which needs `Math.sqrt`); no `test.each`, no `describe.runIf`, no
loop-registered tests; no files outside the manifest; no `export *`.
⛔ **Name reservation discharged:** the seven names MF-T2E reserved for this member —
`PLAN_ERA_VERTICAL`, `OVERLAP_FLOOR_Q`, `planEraSolid`, `solidOverlap`,
`intervalOverlapVerdict`, `footprintIsAnswerable`, `dualRunLegality` — are exactly
`solidLegality.js`'s export set; this member reserves NO name forward (MF-T2K types
`supportSurfaceId` in place and renames nothing).

## 6. Exact contracts

### Leaf 1 · the exact core — `src/domain/townMap/fabric/exactIntersectionArea.js`

```js
export function exactRatioQ(numQ, denQ)
// -> Object.freeze({ numQ, denQ })  — BigInt rational in lowest terms, denQ >= 1n; sign on numQ;
//    {0n, 1n} for zero. TypeError on denQ === 0n or non-BigInt input. The ONE mint of the
//    published exact-quantity shape.

export function assertAnswerableFootprintQ(ring, label)
// -> void, or a typed TypeError naming label and the arm:
//    · 'has zero exact signed area (degenerate ring)'      — BigInt 2A === 0n
//    · 'is not an answerable footprint (no admissible ear…' — exact ear search stalls
//    · 'is not an answerable footprint (exact triangulation identity failed)'
//      — ear-clipping completed but Σ|2A(tri)| ≠ |2A(ring)| (the completed-but-unsound class,
//        convicted by executed fixtures: §4.2 probe 3/4)
//    Internally: exact BigInt ear-clip triangulation — orientation by BigInt sign, strict
//    convexity by BigInt sign, containment by three strict BigInt orients, and the exactness
//    IDENTITY (equality, no tolerance) as the closing guard. The docblock carries the R-MF-2
//    reason with the P1 figures (Number 2A = 0 · BigInt 2A = -1 · landed float area() = 0).

export function sharedFootprintAreaExactQ(ringA, ringB, labelA, labelB)
// -> exactRatioQ record: the EXACT shared area of two integer rings, in quanta².
//    · integer bbox fast-reject (via the imported `bounds`; exact compares) -> {0n, 1n}
//    · exact triangulation of both rings (throws typed per assertAnswerableFootprintQ)
//    · per triangle pair: Sutherland-Hodgman against the CCW edges in HOMOGENEOUS BigInt
//      coordinates [X, Y, W], W > 0n — side sign  (b−a) × (p/W − a)  scaled by W stays a pure
//      BigInt sign; the crossing point is [dp·Xq − dq·Xp, dp·Yq − dq·Yp, dp·Wq − dq·Wp],
//      negated as a triple if W < 0n
//    · rational twice-area accumulated with gcd reduction; final area = twice/2, reduced
//    An abutment (shared edge or vertex) contributes exactly zero — no epsilon exists anywhere
//    in this file.
```

Imports ONLY: `bounds` from `./exactGeometry.js`. Everything else is internal. PURITY: pure;
BigInt and integer arithmetic only.

### Leaf 2 · the ported contract — `src/domain/townMap/fabric/solidLegality.js`

The seven reserved exports, D1's shapes with divergences D-1/D-3/D-4/D-5 (header table):

```js
export const PLAN_ERA_VERTICAL   // Object.freeze({kind:'ABSENT', reason: <the D1 string verbatim>})
export const OVERLAP_FLOOR_Q     // 0n — EXACTLY ZERO, typed BigInt; the no-tuning comment ports

export function planEraSolid(partId, supportSurfaceId, footprint)
// -> deepFreezeCanonical({ partId, supportSurfaceId, footprint, vertical: PLAN_ERA_VERTICAL,
//    abiVersion: COORDINATE_ABI_VERSION })
//    ids through requireCanonicalId; each coordinate through requireCanonicalInt; ring of >= 3
//    [xQ, zQ] points. ⛔ No degeneracy/simplicity check HERE — answerability is the predicate's
//    seam, exactly as in the source (D-5 hardens only the input wall).

export function intervalOverlapVerdict(a, b)
// -> 'OVERLAPPING' | 'DISJOINT' — the predicate §287.5 FORBIDS, ported faithfully (bbox arms
//    via `bounds`; closed-interval Number compares on the vertical) as the negative control.
//    Exported ONLY so a test and a census can quote what it gets wrong; nothing may decide
//    with it. Reads partId-shaped records (D-1).

export function footprintIsAnswerable(ring)
// -> boolean: false for a non-array/short ring, and false when assertAnswerableFootprintQ
//    throws; true otherwise. The D0-simplicity seam, stated out loud (D1's framing kept).

export function solidOverlap(a, b)
// Records: {partId, supportSurfaceId, footprint, vertical, …} — T2E solidPartQ records and
// planEraSolid records alike (D-1). Both through requireCanonicalRecord.
// Refusal order IS the source's:
//  1. a.partId === b.partId          -> freeze {kind:'REFUSED', reason:'SAME_SOLID_ID', verdict:null}
//  2. supportSurfaceId differs       -> freeze {kind:'REFUSED', reason:'DIFFERENT_SUPPORT_SURFACE',
//                                        detail: <the SPEC §10.5 bridge sentence verbatim>, verdict:null}
//  3. areaQ = sharedFootprintAreaExactQ(a.footprint, b.footprint, …)   // throws typed on an
//     unanswerable footprint (D-4 — the sealed spelling's silent-garbage path, closed)
//  4. either vertical ABSENT         -> deepFreezeCanonical({kind:'PLANAR_ONLY',
//                                        sharedAreaQ: areaQ, verdict: areaQ.numQ > OVERLAP_FLOOR_Q
//                                        ? 'OVERLAPPING' : 'DISJOINT_OR_ABUTTING',
//                                        verticalStatus: 'ABSENT'})
//  5. both INTERVAL: lo = max(baseQ), hi = min(topQ) in BigInt; h = hi − lo;
//     sharedHeightQ = h > 0n ? h : 0n;
//     sharedVolumeQ = (planar && h > 0n) ? exactRatioQ(areaQ.numQ * h, areaQ.denQ) : {0n,1n};
//     -> deepFreezeCanonical({kind:'VOLUME', sharedAreaQ, sharedHeightQ, sharedVolumeQ,
//        verdict: sharedVolumeQ.numQ > OVERLAP_FLOOR_Q ? 'OVERLAPPING' : 'DISJOINT_OR_ABUTTING'})
// The half-open law is arithmetic, not a branch: touching intervals give h = 0 and zero volume.

export function dualRunLegality(solids, legacyBoolean)
// D1's shape verbatim: counters {pairs, exactOverlapping, intervalOverlapping, legacyOverlapping,
// intervalFalsePositive, intervalFalseNegative, legacyFalsePositive, legacyFalseNegative,
// unanswerable, samples<=6}; unanswerable footprints skipped via footprintIsAnswerable;
// REFUSED pairs counted in pairs but excluded from verdict comparison; samples carry partId
// pairs and the exact sharedAreaQ record.
```

Imports ONLY: the four foundation validators, `COORDINATE_ABI_VERSION`, `bounds`, and the three
leaf-1 exports. PURITY: pure; BigInt and integer arithmetic only.

### Absence and refusal rules

- Non-record inputs, mis-grammared ids, non-integer or out-of-wall quanta, short or malformed
  rings, unanswerable footprints: **typed TypeError through the landed validators or a message
  naming the offending label and law** — never `NaN`, never a silent skip, never a partial
  answer (D-4).
- `REFUSED` results carry `verdict: null` — refusal is not a verdict, exactly as in the source.

### Determinism

- Pure over arguments; integer and BigInt arithmetic only; no clock, no randomness, no locale
  ordering, no iteration-order dependence; fields written in fixed literal order; no spread of
  caller input reaches a published result.
- Same inputs twice yield deep-equal results (§9 A8 is the credited proof).

### Flag, dormancy, lifecycle, receipts

- Flag: `NONE`. Golden posture: **unchanged and PROVEN unchanged** — the untouched-pin control
  (§10) plus the post-build fence and forensic zoom (§P2.2, post-§324.5 form).
- Lifecycle: pure computation, in-memory; ⛔ nothing persisted, no reload/regen/undo/migration
  seam exists to declare; no `artifactKind`/`schemaVersion` (the T2D/T2E precedent). Public
  veil: n/a — no runtime surface.
- Receipts/privacy: closed kinds `NONE`; DM-only fields `NONE`.
- Alignment: `DECLARED EMPTY: a legality predicate has no alignment surface.`
- Edit story: `ENGINE-ONLY: explicit-input predicate; nothing is DM-editable and nothing is proposed.`

## 7. Exact change manifest

| Action | File | Symbol/region | Max delta | Coding instruction |
|---|---|---|---|---|
| `CREATE` | `src/domain/townMap/fabric/exactIntersectionArea.js` | the three leaf-1 exports + internal triangulation/clip/rational helpers | **≤190 eff** | the §6 leaf-1 contract; docblock carries the R-MF-2 reason with the P1/P2 figures and the §348.2 conviction line; imports `bounds` only |
| `CREATE` | `src/domain/townMap/fabric/solidLegality.js` | the seven reserved exports | **≤175 eff** | the §6 leaf-2 contract; docblock carries the D1 header's three-answers frame (with §P2.8's protection restated), the six divergences by number, and the §299.3(a) discharge boundary |
| `CREATE` | `tests/domain/townMapSolidLegality.test.js` | `describe('MF-T2F solid legality')`, A1–A7 | `n/a` | ONE literal `describe`, seven straight-line `test()` calls, string-literal titles; fixtures built inside named tests (SP-D idiom); ids fit the lowercase grammar |
| `CREATE` | `tests/property/townMapSolidLegalityDeterminism.test.js` | `describe('MF-T2F solid legality determinism')`, A8 | `n/a` | replay + purity companion, the T2E property-file shape |
| `TEST` | `tests/lint/sovereigntyLightingContract.walker.test.js` | the `CENSUS` object | measured **`+34/−1`** (inside the `+41/−1` J-TET2E-1 ratified class figure) | ONE re-record block naming its cause and the `censusAuthorization` refs; all five figures re-derived together at base by the −1-sentinel instrument; ⛔ any manifest edit scoped to this member's own row BY UNIQUE KEY, never a blanket substitution (the T2E §6j near-miss) |
| `DOC` | `docs/implementation/packets/town-cartography/MF-T2F.md` | this packet | `n/a` | the packet, its status transitions, and its §12 landing record |

Generated artifacts: `NONE`. No other file may be edited. ⚠ The two acceptance files are
`CREATE` rows (the family precedent). ⚠ No `tests/lint/**` file is minted, so §P3b's
mutation-coverage row does not attach. §P4's four-name template is satisfied by its
"reason none exists" arm (T2C/T2D/T2E precedent): the leaves are directory-internal, the barrel
deliberately does not export them, and the registry tests that bind this member are the
single-declaration walker and the entry-closure fence. **Symbols the deliverable CREATES** (the
ten exports, the two describe titles) join `requiredSymbols` **at the flip to LANDED, never at
READY**. `retiredSymbols`: **EMPTY** — nothing app-side is superseded (the sealed source
retires with the sandbox at the port's completion, not per member).

## 8. Ordered coding sequence

0. Dispatch and seal; run every §4 preflight. STOP on any mismatch, including the
   preamble-hash, port-source-hash and massPart-existence rows.
1. **Capture the golden/dormancy evidence BEFORE the first edit:** the full fabric-touching
   battery counts (explicit list, per file); the census tuple by −1 sentinel; the §4.2 probes
   re-executed at base (P0–P7 + probes 2–4; the sealed blob extracted to scratch, never
   imported) — the pins of A1–A8 are written FROM these captures and from the implemented
   modules' own printed output, never from this packet's historical figures.
2. Add the smallest failing focused test (A1's first arm against the not-yet-existing leaves).
3. Implement leaf 1 (`exactIntersectionArea.js`) per §6 — triangulation, clip, rational,
   the three exports; then leaf 2 (`solidLegality.js`) — constants, `planEraSolid`, the
   negative control, answerability, the predicate, the dual-run.
4. — (no writer, no lifecycle seam)
5. — (no production consumer; the tests are the named reader until the cutover wave)
6. — (no registration; §7's "reason none exists" arm)
7. Focused verification (§10), including the anchor preflight and the untouched-pin control.
8. The removing-power sweep (§10's mutants), the census re-record, the post-build dormancy
   discharge with forensic zoom, then the wave-end gate and the completion receipt (§12).

## 9. Acceptance matrix

Every negative carries a positive control in the same test. The **capture** column is behavior
executed at compile time (§4.2); the executor re-captures at its base.

| ID | Case | Fixture/input | Capture | Required observation | Home |
|---|---|---|---|---|---|
| **A1** | Main behavior — the port-fidelity quintet on T2E records | the five P0 shapes, built via the LANDED `solidPartQ` (10000-square, the four interval/support/id variations) + `planEraSolid` for the ABSENT arm | P0 + P5 | positive control FIRST: stacked → `VOLUME` height `0n` volume `{0n,1n}` `DISJOINT_OR_ABUTTING` (⭐ the half-open adjacency law); overlapping → volume `{75000000000n,1n}` `OVERLAPPING`; different support → `REFUSED` with the SPEC §10.5 detail; ABSENT → `PLANAR_ONLY` area `{100000000n,1n}` `verticalStatus 'ABSENT'`; same id → `REFUSED/SAME_SOLID_ID` `verdict null`; every result deep-frozen | domain |
| **A2** | Typed refusals + answerability | non-record input; uppercase support id; float coordinate; two-point ring (via `planEraSolid`); the collinear ring; the bowtie; cross-A `[[0,0],[8000,0],[8000,8000],[4000,-2000],[0,8000]]` (2A = 48000000, completes-but-unsound) | P7, P4, probes 3/4 | each refusal a TypeError naming its label (wall messages are the landed validators' own); `footprintIsAnswerable` false on bowtie AND cross-A, true on the square (positive control); `solidOverlap` on an unanswerable footprint THROWS typed rather than answering (D-4) | domain |
| **A3** | ⭐ The stacked-pair pinned negative (the plan's named pin, left to this member by T2E) | `[0,5000)` and `[5000,9000)`, same support, same footprint | P0/P5 | the two solids share `{100000000n,1n}` of area and volume `{0n,1n}` — `DISJOINT_OR_ABUTTING`; the anchored absence: no positive volume on a touching pair | domain |
| **A4** | ⭐ The pinned conviction of the FORBIDDEN interval predicate | the L-and-notch pair (§4.2 P3 fixtures) | P3 | `intervalOverlapVerdict` says `'OVERLAPPING'` where `solidOverlap` returns `sharedAreaQ {0n,1n}` and `DISJOINT_OR_ABUTTING` — the §287.5 gap as a measurement; positive control: on genuinely overlapping squares both say overlapping. ⛔ the incumbent `groundLaw.overlapping` is NOT mentioned, per §P2.8 | domain |
| **A5** | ⭐⭐ The carried §348.2 conviction — BOTH arms, with in-test float witnesses | arm 1: coincident R-MF-2 slivers (`m = 1286630001`, intervals `[0,3000)`); arm 2: coincident squares of side `s = 94906267`, `h = 3000` | P1, P2 | arm 1: the test's own Number shoelace reads `0` and the live `exactGeometry.area()` reads `0` on the sliver (the divergence witnessed in-test); `solidOverlap` answers `OVERLAPPING` with `sharedAreaQ {1n,2n}` and `sharedVolumeQ {1500n,1n}`; arm 2: the test computes `BigInt(s)**2n * 3000n = 27021598547625867000n` and pins `sharedVolumeQ` to exactly that while witnessing `Number(s*s) !== s²-exact`; positive control: the ordinary triangle agrees in both spellings (area `{12000000n,1n}`) | domain |
| **A6** | Constants + `planEraSolid` pins with guard-the-guard openers | the two exported constants; a planEraSolid record | P0/P7 + the source bytes | opener: `PLAN_ERA_VERTICAL` is frozen, kind `'ABSENT'`, reason nonempty (guard-the-guard); `OVERLAP_FLOOR_Q === 0n` exactly (⛔ the no-tuning pin); `planEraSolid` mints `vertical: PLAN_ERA_VERTICAL` (identity), `abiVersion === COORDINATE_ABI_VERSION` read live; a caller cannot smuggle a vertical (the input has no vertical parameter — property-absence with anchor) | domain |
| **A7** | `dualRunLegality` closes | the P6 roster (L-shape, notch, square, bowtie) + a bbox legacy stub | P6 | `pairs 3 · unanswerable 1 · intervalFalsePositive 1 · legacyFalsePositive 1`; the sample carries the pair's partIds and `sharedAreaQ {0n,1n}`; opener: the roster is nonempty and the stub returns true at least once (guard-the-guard) | domain |
| **A8** | Determinism / replay + purity | every A1/A5 fixture computed twice; both leaves' raw text | §4.2 replay + the source | deep-equal results across two computations (BigInt fields compared by value); the source scan finds no clock/randomness/locale/`Math.` identifier in either leaf, with a positive control proving the scan sees a planted occurrence | property |

**Anchor law (§P3):** A2, A3, A6 and A8 assert absences (refusals that must not vanish, no
positive volume on a touching pair, no vertical parameter, no forbidden identifier). Each
routes through `tests/helpers/anchoredNegatives.js` by name on the same line, or carries
`// anchored: <why this cannot go vacuous>` on the assertion line or the line immediately
above.

**Census motion — derived from the case count, re-derived at base:**

```
DELTA: +2 files · +0 parked · +2 credited · +8 titles (7 domain + 1 property) · +2 suites
⭐ THE LANDING BASE (42e12a76), which is what the walker carries:
BASE  (walker GREEN there, BARE, in a pristine worktree — 33/33):    2495/364/2131/20704/5783
AFTER (each figure CONVICTED by its own -1 sentinel arm):            2497/364/2133/20712/5785
  files       "expected 2497 to be -1"
  parked      "expected 364 to be -1"      ⭐ UNCHANGED — both new files are CREDITED
  credited    "expected 2133 to be -1"
  titles      "expected 20712 to be -1"
  suiteTitles "expected 5785 to be -1"

The build base (a09138d7) measured 2494/364/2130/20684/5782 -> 2496/364/2132/20692/5784 by the
same method. Both ends were re-derived rather than carried, and §4.5 records why that mattered:
WF-8A's single new test file carries TWENTY titles, so the carried AFTER tuple would have
published titles 20692 against a true 20712.
The suite layer is proven SEPARATELY as well: `describe(` appears exactly once per new file, and
the two titles grep as `MF-T2F solid legality` and `MF-T2F solid legality determinism`; the
evidence layer greps as exactly 7 + 1 straight-line `test(` calls.
```

⚠⚠ **THE CENSUS IS SEQUENCED, AND THAT BROKE THE OBVIOUS INSTRUMENT.** A one-run-per-key sentinel
sweep reddened `files` on every single run — the first assertion aborts the test and the other
four are never evaluated — so four of the five arms returned "NO SENTINEL MESSAGE" and the honest
answer was to abort them rather than read a number off a different assertion. The walk was made
**cumulative**: each already-convicted key restored to its measured value so the next key could be
reached, with exactly one `-1` in the line at a time, the walker file rebuilt from its pristine
`git show HEAD:` bytes before every run and byte-restored with `cmp` after the last. This is the
same defect class MF-T2E's first instrument fell into (a true figure written under the wrong name)
approached from the other side: here the instrument stayed silent instead of lying, because the
sentinel makes an arm say WHICH question its number answers.

⛔ `parked` stays at its recorded value. One literal `describe` per file, straight-line
`test()` calls, string-literal titles; loops live INSIDE named tests. ⚠ **NAMED INTERIOR RED:**
the census arm is an exact equality against a recorded constant, so this member reds at its own
implementation commit until the tuple is re-recorded; the census is SEQUENCED, so the `files`
red is itself proof the later figures had not yet been read.

## 10. Verification commands — the gate skeleton

```sh
# Anchor preflight — MANDATORY per new test file, BEFORE the member proof is declared green
npx vitest run tests/lint/negativeAssertionAnchor.walker.test.js ; echo TRUE_EXIT=$?

# Focused static + both typecheck configurations, by name and window
npx eslint src/domain/townMap/fabric/exactIntersectionArea.js \
           src/domain/townMap/fabric/solidLegality.js \
           tests/domain/townMapSolidLegality.test.js \
           tests/property/townMapSolidLegalityDeterminism.test.js
npm run typecheck:ratchet          # AT ceiling at T2E's landing — zero headroom, report the pair
npm run typecheck:domain:strict    # AT ceiling — zero headroom, report the pair

# Effective-line ledger against §3 — eslint's own Linter, never wc -l

# Focused tests — one slot, one command
sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/domain/townMapSolidLegality.test.js \
  tests/property/townMapSolidLegalityDeterminism.test.js \
  tests/lint/townMapFabricSingleDeclaration.walker.test.js \
  tests/lint/sovereigntyLightingContract.walker.test.js

# ⭐ THE UNTOUCHED-PIN CONTROL — every fabric-touching test file (explicit list re-derived at
# base; 59 expected after T2E, including the twelve digest-pin suites and the T2C A1 alignment
# pin in townMapDcelEmbeddingExtension.test.js), identical per-file counts before the first
# edit and after the last. This member adds two leaves; it must move NOTHING that already
# exists. Goldens compare against a `git archive` extraction of the base, never a working-tree
# copy a hook may have touched.

# ⭐ THE REMOVING-POWER SWEEP — nine mutants, planted in a scratch copy, each convicting a
# NAMED branch, each restored by byte copy verified with cmp (never the `git checkout --`
# family). M0 pristine-green control first; clean re-run after; every conviction quotes its
# failing arm:
#   M1 core ring twice-area BigInt -> Number shoelace          -> A5 arm 1 (the sliver's
#        OVERLAPPING pin vanishes) and A2 (the sliver is refused as degenerate)
#   M2 interval intersection misread closed: h = hi - lo + 1n  -> A3 (the stacked pair gains
#        phantom volume; the DISJOINT_OR_ABUTTING pin reds)
#   M3 SAME_SOLID_ID refusal deleted                           -> A1 (same-id arm expects REFUSED)
#   M4 DIFFERENT_SUPPORT_SURFACE refusal deleted               -> A1 (support arm)
#   M5 PLANAR_ONLY arm deleted (ABSENT treated as interval)    -> A1 (ABSENT arm expects
#        PLANAR_ONLY kind)
#   M6 verdict floor > OVERLAP_FLOOR_Q loosened to >=          -> A3/A4 (zero-shared-volume and
#        zero-shared-area pairs read OVERLAPPING)
#   M7 intervalOverlapVerdict's XY arm deleted                 -> A4 (the forbidden predicate's
#        expected OVERLAPPING on the notch vanishes — the negative control kept falsifiable in
#        both directions)
#   M8 the exact triangulation IDENTITY check deleted          -> A2 (cross-A is silently
#        ANSWERED with an inflated area instead of refused — executed fixture: sum 208000000 ≠
#        want 48000000)
#   M9 rational gcd reduction broken (unreduced return)        -> A5 arm 1 (the {1n,2n} pin
#        reds on an unreduced denominator)

# ⛔ Dormancy: POST-BUILD ONLY (§P2.2 post-§324.5). Build first, then:
npm run build
VERIFY_DIST=1 npx vitest run tests/build/townMapLazy.test.js ; echo TRUE_EXIT=$?
# …and the forensic zoom: string literals unique to the leaves — 'is not an answerable
# footprint' and the SPEC §10.5 bridge detail sentence — appear in ZERO entry-closure chunks
# and (since nothing imports these leaves, matching the T2E honest shape) in NO emitted chunk
# at all, with scanner controls asserted PRESENT first and both denominators NAMED (§P2.12).

# Sealed receipt and exact-state handoff; neither is landing authority
npm run check:packet -- MF-T2F
npm run implementation:resume -- MF-T2F

# Wave-end — BARE, fresh shell, never piped, never wrapped, and OUTLAST it in your own turn
npm run check:tail ; echo TRUE_EXIT=$?
```

⛔⛔ **NEVER wrap `npm run check*` in `gate-mutex.sh --run`** — `test:ratchet` re-acquires and
self-deadlocks; **exit 3 is the mutex giving up, not a red.** ⚠ Trust no exit status you did
not capture. ⚠ Report both typecheck configurations by name and window. ⚠ **Stray protocol
(§358.2, the hunt's law):** on ANY non-census stray, locate the run's `results.json` in TMPDIR
by mtime and READ THE ROW — duration > testTimeout is a timeout kill wearing a red's identity,
settled on the spot; the frozen census is ELEVEN (§356.3) and is read from
`scripts/.test-ratchet-baseline.json`, never widened, never inferred from a brief. Expected:
every command exits `0`; report actual counts, never this packet's historical ones.

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and preamble §P7:

- any digest pin, exact-key pin or pinned roster in the fabric-touching suites moves, or any
  such test file changes its result between the §8-step-1 and §10 captures — this member is
  purely additive and a moved pin means it was not;
- any existing production file would need editing — including `exactGeometry.js`,
  `massPart.js`, `foundation.js`, `coordinateAbi.js`, `operations.js`, the codex massing
  modules and the barrel; the cure for any such need is a STOP-and-report, never a manifest
  widening;
- `src/domain/townMap/fabric/massPart.js` is absent at the dispatch base (MF-T2E not landed);
- the `export function derivePlanarDcelEmbedding(input) {` line changes in ANY byte, or
  `CURRENT_MAP_TRADITION_ID` becomes imported or declared in any created file;
- the family preamble's hash or the port-source blob hash does not match a fresh computation
  at the base (§4 rows 0–1);
- any export beyond the ten §6 names, or any of the ten colliding with a live declaration;
- the toy-scale port-fidelity captures (P0/P5) fail to reproduce at base — the predicate must
  agree with the sealed source figure-for-figure at fixture scale, or the port is not a port;
- `Math.` (any member), `Date`, RNG or locale enters either leaf;
- an epsilon, tolerance or quantum of any kind enters either leaf — the exactness contract has
  no dial, and a tolerance here is a tuning constant (§P2.5);
- `groundLaw.overlapping` or the `operations.js` identity check is reported as a defect
  (§P2.8; the discharge boundary);
- a `heightQ()` edit, a world-unit conversion, an `artifactKind`/`schemaVersion` mint, or any
  stamped version-string motion;
- the census moves by other than the §9 delta against the tuple captured at §8 step 1, or
  `parked` moves at all; a delta smaller than the titles added is attributed by reverting one
  test file at a time, never accepted as arithmetic;
- `tests/build/townMapLazy.test.js` reds POST-BUILD, or the forensic zoom finds a
  member-unique literal in an entry chunk;
- another D3a member is simultaneously non-terminal on the census walker path — wait it out
  (staged promotion, §P7.11);
- any ratchet, baseline, budget or ceiling would need raising; any tuning constant of any kind
  would be touched.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into
the next wave.

## 12. Completion receipt

*(filled at the flip to LANDED)*

**Landing base:** `42e12a76f1dd35e09a8938ad34a08795c33a4f6f` (build base `a09138d7`; see §4.5).
**Lane:** TE-T2F, own detached worktree with its own `node_modules` from `npm ci`
(`NPMCI_TRUE_EXIT=0`), plus a second pristine base-proof worktree at the landing tip for the BARE
arms. No ref moved by this lane. Every row below was re-executed at the LANDING base.

**Delivered:** the two leaves (`fabric/exactIntersectionArea.js` 160 eff, `fabric/solidLegality.js`
150 eff), the two acceptance files (A1–A7 domain, A8 property), the census re-record, this packet.
**Zero existing production files modified**, exactly as §3 priced.

| gate row | result |
|---|---|
| `npx eslint` over all four new files | `ESLINT_ALL_TRUE_EXIT=0` |
| `npm run typecheck:ratchet` | `173 error(s), ceiling 173` · `TRUE_EXIT=0` — zero new type errors against a zero-headroom ceiling |
| `npm run typecheck:domain:strict` | `1134 errors, ceiling 1134` · `TRUE_EXIT=0` — same |
| effective-line ledger (eslint `Linter`), re-measured on the COMMITTED bytes | 160/190 · 150/175 · packet 310/340 |
| `tests/domain/townMapSolidLegality.test.js` + the property companion | 8 passed · `TRUE_EXIT=0` |
| `tests/lint/negativeAssertionAnchor.walker.test.js` | 9 passed · `TRUE_EXIT=0` |
| `tests/lint/townMapFabricSingleDeclaration.walker.test.js` | 5 passed · `TRUE_EXIT=0` |
| `tests/lint/sovereigntyLightingContract.walker.test.js` | 33 passed BARE at the landing base before the re-record, and 33 passed after it · `TRUE_EXIT=0` |
| untouched-pin control (67 explicit paths, list re-derived at the landing base) | before 67/444/0 · after 67/444/0 · **per-file diff EMPTY** |
| `npm run build` | `TRUE_EXIT=0` |
| post-build `VERIFY_DIST=1 … tests/build/townMapLazy.test.js` | 3 passed · `TRUE_EXIT=0` |
| post-build `VERIFY_DIST=1 npx vitest run tests/build` | 52 files / 433 tests passed · `TRUE_EXIT=0` |
| `npx vitest run tests/lint tests/docs` | 6 failed / 1828 passed — **all six are members of the ELEVEN banked set**, zero strays. The same six, by name, at both bases |

**The removing-power sweep — nine mutants, each planted from pristine bytes, each convicted by a
NAMED arm, each restored by byte copy verified with `cmp` (never the `git checkout --` family).**
M0 pristine-green control passed 8/8 first; the clean re-run after the sweep passed 8/8 again.
⭐ The sweep was run at BOTH bases and convicted the identical arms at each. Both leaves and both
acceptance files are `cmp`-identical across the re-base, so the first sweep transfers with proof
rather than by assumption — and it was re-run anyway, because an equivalence pin stays green over
a deleted cure and only re-execution can tell the two apart.

| mutant | predicted arm | arm that ACTUALLY convicted, with its message |
|---|---|---|
| M1 core ring twice-area → Number shoelace | A5 arm 1, A2 | **A5 and A8.** ⚠ A2 did NOT fire: the sliver's answerability assertion lives in A5, not A2, so the packet's prediction split one test's coverage across two |
| M2 interval read closed (`h = hi − lo + 1n`) | A3 | **A1, A3, A5, A8** — `expected 2n to be 1n` on the stacked pair's height |
| M3 `SAME_SOLID_ID` refusal deleted | A1 | **A1** — `expected 'VOLUME' to be 'REFUSED'` |
| M4 `DIFFERENT_SUPPORT_SURFACE` refusal deleted | A1 | **A1** — `expected 'VOLUME' to be 'REFUSED'` |
| M5 `PLANAR_ONLY` arm deleted | A1 | **A1 and A8** |
| M6 verdict floor loosened `>` → `>=` | A3/A4 | **A1, A3, A4, A7** — `expected 'OVERLAPPING' to be 'DISJOINT_OR_ABUTTING'` |
| M7 `intervalOverlapVerdict` XY arm deleted | A4 (the notch's OVERLAPPING vanishes) | **A4, by the OTHER direction** — `expected 'OVERLAPPING' to be 'DISJOINT'` on the separated pair. Deleting the arm cannot remove an OVERLAPPING answer; what it removes is the control's ability to say DISJOINT, and A4 pins both directions, which is why it still convicts |
| M8 exactness IDENTITY check deleted | A2 | **A2** — `expected true to be false`: cross-A becomes "answerable" and is silently answered |
| M9 rational gcd reduction broken | A5 arm 1 | **A1, A3, A5, A8** — `expected { numQ: 3000n, denQ: 2n } to deeply equal { numQ: 1500n, denQ: 1n }` |

**Dormancy, discharged POST-BUILD with the forensic zoom and live scanner controls, both
denominators named:** 710 emitted JS chunks scanned, 8 entry-closure chunks. Controls asserted
PRESENT first — `'must be an object'` in 1 entry-closure chunk, and the landed fabric wall's own
`'must be a canonical id'` in 1 emitted chunk (proving the reader sees this family's text where it
ships). All five member-unique literals: **entry 0, any 0**.
⭐ **Stated honestly rather than glossed:** nothing imports either leaf, so they appear in NO
emitted chunk at all — a weaker claim than "excluded from the entry closure". The same is true of
MF-T2E's landed `massPart.js` one commit down, whose own unique literal also scans to zero, so
this member's zero is the sibling precedent's zero.

**Anchors preserved, grepped before and after at BOTH bases:**
`export function derivePlanarDcelEmbedding(input) {` stands at `dcelEmbedding.js:140` at column
zero with an EMPTY diff on that file, and `CURRENT_MAP_TRADITION_ID` appears zero times in all four
created files.

**Merge integrity, checked because a conflict resolution can delete a landed cure while its
equivalence pins stay green:** all twelve created `requiredSymbols` rows resolve against the LIVE
tree (12 of 12, zero unresolved); WF-8A's packet row survives in both `PACKET_MANIFEST.json` and
`INDEX.md`; the mint's two C2 files are untouched by this member; and `git diff --name-only
42e12a76 HEAD` returns exactly this member's eight paths and nothing else.

**Behaviour shift: NONE.** This member adds two leaves nothing calls. Every fabric-touching suite
in the 67-path control returned identical per-file counts and statuses; no digest, golden or
exact-key pin moved.

---

## 13. Scope determination detail, plan staleness and RAISED — for the chair at review

**Plan staleness ledger** (`draft-D3A-PLAN.md` was compiled at `9d851fae`, before members 1–5
landed; each divergence named, none silent):

1. **Plan §4's F row "existing logic modified: 1 — `exactGeometry.js`,
   `polygonIntersectionArea` re-exported (+0 eff, already public)" is doubly stale:** the
   symbol was public from T2B's landing, and the §348.2 conviction (found at T2E's compile,
   after the plan) means the legality path deliberately does NOT consume it. F modifies zero
   existing files.
2. **Plan §4's ~110-eff one-leaf price is superseded by the measured basis** (§4.4: 282
   executed effective lines) — the conviction cure is the difference; the two-leaf split is
   the cap-respecting answer.
3. **Plan §4's census shape for F (`+2/0/+2/+6/+2`) is superseded by the derived
   `+2/0/+2/+8/+2`** — the same count-from-the-actual-cases correction every landed member of
   this wave recorded.
4. **Plan §9.3's "every member touches `fabric/index.js`" does not bind this member** — no
   barrel edit (the T2C/T2D/T2E precedent); only the census walker is reserved.
5. **The plan's F row predates the result-shape question entirely** — D-3 (exact BigInt
   magnitudes) exists because of the §348.2 conviction; RAISED-1 puts it to the chair.
6. **Stamp/preamble rows are discharged** (stamp §312.2b; preamble landed; this draft cites
   the live hash and orders a recompute).

**RAISED — ALL SEVEN RULED BY THE CHAIR AT ODQ §371.1, and the rulings are what this member was
built to.** The rulings, restated: exact `{numQ, denQ}` rationals under the D1 field names
(RAISED-1 adopted); the discharge boundary confirmed — the `operations.js` collision-by-identity
site is NOT edited and NOT convicted, consumer wiring is the successor's (RAISED-2); TWO leaves
at caps ≤190/≤175 effective with the packet at ≤340 (RAISED-3 adopted, caps tightened);
`OVERLAP_FLOOR_Q = 0n` as drafted, structural (RAISED-4); `dualRunLegality` ports in this member,
dormant (RAISED-5); D-4's typed refusal replaces the silent-garbage answer, and all six
divergences ship DECLARED with their executed witnesses (RAISED-6); the §358.2 stray posture
binds this member's gate and the banked set is ELEVEN (RAISED-7). The chair additionally directed
that the conviction fixtures use the drafted values exactly — the odd `s = 94906267` is kept,
because the even square is the near-vacuous trap the compile caught.

| # | the call | this lane's recommendation (as ruled) |
|---|---|---|
| **1** | ⭐⭐ **The published result types (D-3).** The sealed source publishes Number magnitudes; the ordered BigInt cure cannot express its answers in Number (`27021598547625867000` is the executed counterexample). This draft publishes frozen `{numQ, denQ}` exact rationals under the SAME field names, with kinds/verdicts/reasons unchanged and toy-scale figures numerically identical (executed, §4.2 P0). | **Adopt.** Alternatives rejected: Number-with-rounding re-mints the convicted defect under a cure's name; a dual Number+exact publication is two truths in one record. Nothing app-side pins the result field types (T2E's committed tests deliberately never drove the predicate — verified), so the divergence lands free of re-records. |
| **2** | ⭐ **The §299.3(a) discharge boundary.** This member lands the §287.5 predicate home on real intervals — the obligation's substance — and does NOT edit the `operations.js` identity site or wire any consumer (live behavior, soak-owing, and behind the §299.3c/T2S ruling). | **Confirm the reading: predicate-home landed = (a) discharged; the operation-path consultation is the named successor step.** If the chair reads (a) as requiring the consumer wiring too, F still lands as compiled and the wiring becomes a named follow-on member — the packet text survives either ruling. |
| **3** | ⭐ **The second leaf (`exactIntersectionArea.js`) exports exact-area machinery** — three names, consumed today only by `solidLegality.js`. The one-predicate-home concern is answered in §1.7: the legality QUESTION has one home (`solidOverlap`); the float `polygonIntersectionArea` keeps its fixture-scale geometry role; the exact spelling is a different contract (exact rational, ABI-scale), not a second spelling of the same one. | **Adopt the two-leaf split** — forced by the 250 cap against a 282-line executed basis, and it mirrors the sandbox's own geometry/law split. Alternative (one leaf, cap raised) is a §P7.13 STOP by definition. If the chair prefers the core UNEXPORTED-in-spirit, a walker arm asserting "no importer of `exactIntersectionArea.js` outside `fabric/`" can ride a later housekeeping act — noted, not compiled. |
| **4** | **`OVERLAP_FLOOR_Q` becomes `0n`** (BigInt zero, compared against exact numerators). The value is unchanged — exactly zero, the no-tuning rationale ported verbatim. | Adopt; a Number `0` beside BigInt comparisons would be a type seam inviting exactly the coercion the cure removes. |
| **5** | **`dualRunLegality` ports now** rather than deferring to the cutover wave: it is one of the seven reserved names, it is D1's migration shape, and A7 keeps it honest. Its `legacyBoolean` hook stays caller-supplied (no `groundLaw` exists app-side to bind). | Adopt. Deferring it would leave a reserved name undeclared and the reservation dangling. |
| **6** | **The answerability refusal inside `solidOverlap` (D-4)** closes an executed silent-garbage path (the sealed spelling answers `VOLUME … OVERLAPPING` from a partial triangulation on a bowtie). The refusal is conservative-exact: a ring the exact core cannot PROVE simple is refused typed. | Adopt. The alternative — port the silent answer faithfully — ships a known-garbage path into the one predicate home. |
| **7** | **The stray-classification posture for this member's gate:** the §358.2 protocol (read the results.json row; duration > budget = timeout kill) governs; the frozen census is ELEVEN; the quiet-track law from T2E's precedent applies if the chair's GO requires it. | Recorded so the executor inherits the protocol rather than re-deriving it mid-red. |
