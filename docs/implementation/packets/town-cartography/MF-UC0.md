# Town cartography / MF-UC0 — the UNDERGROUND EXISTENCE GATE landed engine-side: `deriveStrataExistence` (ODQ §311.1 compiled as a pure domain-root deriver), the §3 license-home audit re-executed field-level, the inference-only `institutionSubstructure` facet kind proven golden-inert, and the closed joint vocabulary minted as data for every later car

- **Status:** LANDED
- **Packet version:** 1
- **Verified base:** `claude/composite-r4` at `f32c548ea720fc10ae895b0d542a837cce7d63aa`
  ⚠⚠ **AS AUTHORED — the row above now names the LANDING SLOT (see the Landing note below); the
  BUILD base was `84e06412` (MF-T2H's landing).** The continuation that follows was written at that base:
  — MF-T2H's landing (ODQ §442.1), read with `git rev-parse` at the lane's opening
  (2026-08-23T01:11Z) and re-read at the member tip. Every figure below was executed at THIS
  base by the implementing lane TE-UC0; nothing is inherited from the charter
  (`draft-UNDERCITY-PLAN.md`, re-walked at `b10ed1a1`) except where a row says so and names
  the re-derivation. ⚠ The status value above stands ALONE on its line because
  `parsePacketHeader` (`scripts/implementation-packets.mjs`) anchors the status row at
  end-of-line.
- **Landing note (TE-UCSTACK-LANDING, 2026-08-23, ODQ §472.4 — THE UNDERCITY TRAIN's FIRST TWO CARS, a lawful green prefix of T-UC1):** authored at BUILD
  base `84e06412` as holding tip `a5c6c4fa` (pinned `refs/preserve/holding-uc0`); landed as ONE stacked landing with its sibling
  (MF-UC0 → MF-UC3, UC-0 first because UC-3 imports its `jointVocabulary.js`; each rebased in sequence onto the moving tip) at slot `f32c548e`
  (the PRODUCER TRAIN's §469 follow-on, landings 36–38). Rebased implementation commits `adb2bb8e → 12a60e54` (authored `e67aacf0 → a5c6c4fa`);
  this member is the FIRST of the two. Entered at **LANDED** directly — the §410 form, as the MAP STACK and the PRODUCER TRAIN did:
  MF-T2H's READY reservation on the census walker stands until HK-A lands, so the §417 deferred row could only enter the manifest at a
  terminal status, and did, at this act. Census: slot `2508 / 365 / 2143 / 20801 / 5800`; this member's position `2509 / 365 / 2144 / 20809 / 5801`; the stack's ONE live tuple
  `2510 / 365 / 2145 / 20817 / 5802` convicted at the stack tip (33/33) with the negative controls *"the estate's file count moved — re-measure, do not re-word: expected 2510 to be 2508"* (control A, the summed delta +2)
  and *"… expected 2510 to be 2509"* (control B, MF-UC0's position put back — MF-UC3's +1 alone, the proof that no sibling's delta was dropped by the merge).
  Every delivered-file digest this packet cites (the hash table, the preamble `0706aad6…`, the generator-golden fixture `29c6cc8f…`) re-verified at the
  landed tree — none moved, so no RE-HASHED table note is owed; the census note's walker digest `6fa706ac…` is the BUILD-base pristine blob
  (restoration history), and the walker at the landed tree reads `9b277324eacca30c377efb1d2caa2f9f4373ae845ed17100f661486719cc8f7a` after this act's re-record. The jointVocabulary.js this car mints is the ONE copy on the branch: MF-UC3's identical carried blob (sha256 f9445c98…, `cmp` exit 0 both ways) resolved to it at the rebase, so the path is owned by this packet alone. The §6 mutant section's test digest `b02df4f0…` is the pre-totality-rewrite working copy (BUILD history); the committed acceptance blob is the hash table's `863cd5f9…`, which matches the landed tree.
- **Landing follow-on (ODQ §474, TE-UCSTACK-LANDING, 2026-08-23):** at the stack tip 006c07be this packet's own acceptance arm A6 redded with the received-list diff `+ "src/domain/undercity/staticComponents.js"` — the arm's importer census is a TEXT-TOKEN scan (`includes('domain/undercity/')` over `src/`) pinned by exact equality to this leaf alone, and MF-UC3's leaf carries the same FIRST-PAINT CLOSURE prose line (`staticComponents.js:48`), so a later train car tripped a pin that contradicted this packet's own "Binds forward: UC-3". Neither build lane could see it (UC-0 built alone; UC-3's tree never held this acceptance file). RULED cure in-train: A6 now pins the INTENT — a POSITIVE control (`toContain` this leaf: the scanner sees the token) plus the closure arm (every `src/` file mentioning the token that is NOT under `src/domain/undercity/` → `[]`); a later undercity car is not an outsider. Same `it` title, so NO census motion (the lighting walker 33/33 at `2510/365/2145/20817/5802`, tuple line unchanged). Re-proved: the acceptance 8/8 + anchor walker + seedLoopTotality + the lighting walker (4 files / 59 tests, exit 0); eslint 0; C0 0; a planted outside importer (`src/_ucst_probe/probe.js`) convicts the closure arm (`expected [ 'src/_ucst_probe/probe.js' ] to deeply equal []`) and was removed, the file restored digest-exact. Test file 241 → 242 effective; sha256 `863cd5f9…` → `a9317d83…` (the hash table row RE-HASHED). Forward law for UC-1/UC-2/UC-4/UC-5: the same closure prose line is lawful; the arm no longer forecloses the train.
- **Landing follow-on (ODQ §475, TE-UCSTACK-LANDING, 2026-08-23):** the terminal at 0ccd5794 redded six `tests/edgeFunctions` tests in ONE class — `src/domain/spatial/cohesionWeave.js` is a transitive INPUT of the aiCharter / aiGrounding / aiOutputSchema edge-shared bundles (each `.meta.json` `inputs` roster names it), and this member's seven facet rows changed its hash (three `*.freshness` reds *"Bundle is stale"* + three `edgeSharedBundleReproducibility` reds *"built from content that is neither committed nor staged"*). The build swept `tests/lint tests/build tests/ops tests/docs` and never `tests/edgeFunctions`; the producers' hosts are in no roster, so their terminal was green. RULED cure in-train: `npm run build:edge-shared` at the committed tree, the eight-file set committed with hooks ON — the three bundle `.js` by exactly this member's rows + the `Source hash` header (0 added lines outside the hunk, counted), five metas as one build window (aiGrounding `cafa393b140605f3 → 5ccae1a97b7a9c45`, aiCharter `39077ffae6b87673 → bbd4c9974b23c09c`, aiOutputSchema `28065de02fe98479 → 10327ef5afcba63c`; analyticsEvents / intentAtlas by `generatedAt` only). The four edge test files 87/87 with the set; the S0 sha unchanged (the bundles sit outside the OSR envelope). No census motion (no test file moved). The §7 "Generated artifacts: NONE" line is corrected below, and the manifest row gains eight `DOC` rows for the artifacts (the WEB-3 form — the validator's action vocabulary has no GENERATED kind; a `DOC` row is not a change-path reservation at a terminal status). Forward law: grep every MODIFY host against `supabase/functions/_shared/*.meta.json` before the sweep; a hit means `tests/edgeFunctions` joins the sweep and the regen rides the member commit.
- **Charter and rulings:** `draft-UNDERCITY-PLAN.md` §4 UC-0 (this car), §1 (the binding law,
  quoted), §3 (the license-home audit this car re-executes), §7 F1 (this fact's shape), §8–§11
  (the raised split, deferred rows incl. D-8, execution laws, judgments). Ruled at **ODQ §441**
  (§441.1 one truth for sewers · §441.5(a) the facet kind priced · §441.5(d) the walls home owed
  here · §441.5(e) the census shape · §441.5(k) the anchor key · §441.7 the MF family), applied
  in place by TC-UNDERCITY-R2 and **ratified at ODQ §445.2** (J-R2-1 D-8 recorded not cured ·
  J-R2-2 `jointVocabulary.js` minted here · J-R2-3 the §443 addendum). Doctrine citation:
  **ODQ §311–§311.9** (the owner's undercity directive and the chair's refinements).
- **Depends on:** nothing — this car compiles NOW (§441.7). It reads LANDED symbols only:
  `facetOf` (`src/domain/spatial/cohesionWeave.js`), `resolveSettlementTerrain`
  (`src/domain/resolveTerrain.js`), `nativeSemanticResourceKeys`
  (`src/domain/content/customContentSemanticAuthority.js`), `compareCodepoint`
  (`src/domain/deterministicSort.js`) and `institutionalCatalog` / `catalogIdForName`
  (`src/data/institutionalCatalog.js`). No producer symbol is named (§441.5(f)).
- **Binds forward:** UC-3 (imports `jointVocabulary.js`), UC-1 (imports `sanitationRosterOf`
  and `SANITATION_LADDER` — the one-truth read, §441.1 — and feeds the derived rung through the
  optional second argument), UC-4 (reads the walls accessor this packet NAMES, §1 row 15), UC-5
  (the joint vocabulary is its law), CT-4 (the §7 F1 consumer: `strataExistence` is the fact
  its "why this town has an underground at all" sentences cite), the D5 strata fabric wave
  (consumes the same fact through §287.4's StrataExistencePlan — one producer, two consumers).
- **Family preamble:** `docs/implementation/preambles/MF-PREAMBLE.md`, SHA-256
  `0706aad6a84e4f74ec47602e9e3222fc398c4c8864bb38c79ac480c2a11db4ed` — recomputed from the
  file at `84e06412` by this lane (`shasum -a 256`), identical to the charter's expectation
  and to the value every MF packet carries. Its §P3 anchor preflight, §P5 census law, §P6
  mutant hygiene, §P7 STOP set and §P8 capsule law bind this packet and are not restated.
  **Stamp: GRANTED at ODQ §312.2b** — the stamped column applies (eight members per engine
  train; T-UC1 carries three).
- **Collision group:** `undercity` (T-UC1 = MF-UC0 → MF-UC3 → MF-UC1). At this base the
  149-row manifest holds exactly ONE non-terminal packet, `MF-T2H` (READY, the landed member
  awaiting its status flip); the four delivered/modified paths and this packet's own path are
  held by NO packet (executed §417 probe, §4 row 5). The shared census path
  `tests/lint/sovereigntyLightingContract.walker.test.js` is held by 68 packets (67 LANDED +
  MF-T2H) and is NOT reserved here (§417's T2J shape: the row rides this packet verbatim for
  the chair's landing act; see the census note).
- **Commit authority:** the executing lane commits on its own detached HEAD with hooks ON; the
  chair moves the branch. This lane moved no ref.

> **`censusAuthorization`:** this packet moves the test census by
> **`+1 files / +0 parked / +1 credited / +8 titles / +1 suiteTitles`** — exactly the charter's
> predicted shape with N = 8 (`it` count). **Base tuple at `84e06412`, the walker GREEN 33/33
> at base: `2499 / 364 / 2135 / 20729 / 5787`** (MF-T2H's re-record). **After tuple, read
> from the walker's own failure message figure by figure in the SEQUENCED walk and then
> CONFIRMED by a full green pass (33/33) with the row applied:
> `2500 / 364 / 2136 / 20737 / 5788`** (`files` redded first — "expected 2500 to be 2499" —
> then `parked` PASSED at 364 without redding, then `credited` "2136 to be 2135", `titles`
> "20737 to be 20729", `suiteTitles` "5788 to be 5787"). ONE new acceptance file,
> `tests/domain/undercityStrataExistence.test.js`, credited (one literal `describe`, eight
> straight-line `it`, string-literal titles, every loop INSIDE a named test; no
> `.each`/`runIf`/nesting). Its authorizing decisions are **ODQ §311** (the undercity
> doctrine), **§359.3** (the OB-6 fold into this charter), **§431** (the train's dispatch)
> and **§441** (the ruling whose §441.5(e) gives this block its shape), ratified at §445.2.
> The family's stamp is **GRANTED** at ODQ §312.2b.
> ⭐⭐ **STACKED LANDING (ODQ §472.4; §457/§469 laws) — convicted at the stack tip, never carried.** Slot tuple
> (`claude/composite-r4` = `f32c548e`, the PRODUCER TRAIN's follow-on): `2508 / 365 / 2143 / 20801 / 5800`; this member's position in the
> stack re-derives to `2509 / 365 / 2144 / 20809 / 5801`; the stack's ONE live tuple `2510 / 365 / 2145 / 20817 / 5802` was
> convicted at the stack tip (33/33) with two negative controls — the slot's own tuple put back reds at `files`:
> *"the estate's file count moved — re-measure, do not re-word: expected 2510 to be 2508"* (the summed delta +2), and
> MF-UC0's position tuple put back reds *"… expected 2510 to be 2509"* (MF-UC3's delta alone). The DELTA `+1/+0/+1/+8/+1` crossed the
> rebase; the authored tuples above are the BUILD-base history.
>
> ⛔ **THE ROW IS DEFERRED TO THE LANDING ACT (ODQ §417, the T2J/T2Q/T2R shape).** The walker
> edit was made, proved green, and then REVERTED ON PURPOSE at the member commit (the working
> file is byte-identical to HEAD's blob `6fa706ac…`): the row is inserted by the chair's
> landing act at the train terminal, so the census arm of `sovereigntyLightingContract.walker`
> REDS at this member's tip — **that is the ONE NAMED interior red (charter §4 "interior reds,
> named now"; preamble §P7.12), not a defect.** No other red stands at this tip (§10). The
> exact text to paste, replacing the `files: … suiteTitles: …` line inside the `CENSUS`
> object (at `:5277` at this base), is:
>
> ```
>     // ── RE-RECORDED 2026-08-23 BY TE-UC0 (MF-UC0), CAUSE ATTRIBUTED ──────────────
>     // `files` 2499 → 2500, `credited` 2135 → 2136, `titles` 20,729 → 20,737,
>     // `suiteTitles` 5,787 → 5,788. `parked` is UNCHANGED at 364 (it PASSED without redding).
>     // ONE new acceptance file, tests/domain/undercityStrataExistence.test.js, credited (one
>     // literal `describe`, straight-line `it`, string-literal titles, every loop INSIDE a named
>     // test; no `.each`/`runIf`/nesting) carrying EIGHT arms —
>     //   ⭐ A1 guard-the-guard: the `institutionSubstructure` rows are LIVE against the REAL
>     //      catalog and keyed right (exact name sets per value) before any arm below runs
>     //   ⭐ A2 no qualifying seed → no sheet (a cesspit rung contributes none; total on garbage)
>     //   ⭐ A3 one modest seed → the sheet exists (the two-level reading's first level)
>     //   ⭐ A4 a facet-DECLARED custom institution counts, incl. the sanitation/drains facet
>     //   ⭐ A5 the catalog's marsh/floodplain forbiddance honoured (refused, visible, never counted)
>     //   ⭐ A6 deterministic · closed vocabularies as literals · pure · dormant (no townMap import)
>     //   ⭐ A7 G-43: remove the last qualifying institution → exists flips false
>     //   ⭐ A8 the POSITIVE KEY arm: the real 'Underground network' row resolves `subterranean`
>     //      through facetOf; a roster carrying 'Sewage system' seeds the full-web class (UC-1's read)
>     // `censusAuthorization`: ODQ §311 (the undercity doctrine), §359.3 (the OB-6 fold), §431
>     // (the train's dispatch), §441 (the ruling; §441.5(e) is this bullet's shape), ratified §445.2.
>     // ⛔ ATTRIBUTED BY THE SEQUENCED WALK, NOT BY ARITHMETIC, at THIS base. `files` redded first
>     //   ("expected 2500 to be 2499"), `parked` PASSED at 364, then `credited` ("2136 to be
>     //   2135"), `titles` ("20737 to be 20729"), `suiteTitles` ("5788 to be 5787"), then GREEN
>     //   33/33 — each figure read from its own arm's failure message, never computed; the member
>     //   adds exactly one test file and no other lane's file moved.
>     files: 2500, parked: 364, credited: 2136, titles: 20737, suiteTitles: 5788,
> ```
>
> ⚠ The chair re-derives the BASE tuple at the terminal before pasting: sibling members on the
> same train (UC-3, UC-1) and any landing between now and then each move it by their own
> declared delta — the DELTA `+1/+0/+1/+8/+1` is what crosses a rebase, never the tuple.

> **Delivered files, hashed from their COMMITTED blobs at the member tip** (`git show
> HEAD:<path> | shasum -a 256`), and their effective lines under eslint `max-lines`
> `{ skipBlankLines, skipComments }` measured with eslint's own `Linter` (`/tmp/uc0/efflines.mjs`,
> the TET2K/TET2J instrument):
>
> | file | action | SHA-256 (committed blob) | effective lines |
> |---|---|---|---:|
> | `src/domain/undercity/strataExistence.js` | CREATE | `94f146b2d87c2da8e3acef208ca7defe35ca11aba2be1b8303df9db28a6393d2` | **110** of 250 (charter estimated ~180) |
> | `src/domain/undercity/jointVocabulary.js` | CREATE | `f9445c9868122a41d33d984700ffc19693cc9c6168fec440df0f64d31f7ed13f` | **8** of 20 (J-R2-2's cap) |
> | `src/domain/spatial/cohesionWeave.js` | MODIFY | `dc9946ea7e830ef939eccc4a863aa949aa06e3edc62e4463e5e6f16cbf7b9764` | **141 → 148, +7** of the ≤15 shared-file delta (measured BEFORE the edit at 141; no hot-file row, no size-baseline row; the 800 domain ceiling is far) |
> | `tests/domain/undercityStrataExistence.test.js` | CREATE | `a9317d836dc31354ce88d0c896d943b9ec23610f53850d37dc87eafbd1260a1d` (RE-HASHED at the §474 follow-on; authored `863cd5f9…`) | 242 (test; not a production budget; 241 as authored) |
> | `tests/lint/ruinFilterRoster.walker.test.js` | TEST (MODIFY) | `bef87e395ece7c11509918888b13b733fd76dea71697c6fb9f68076db08c4228` | +1 exempt row (5 comment lines) and the reader count re-measured 89 → 90 (§10.1) |
>
> None of the three created paths exists at `84e06412` (`git ls-tree` empty; `src/domain/undercity/`
> does not exist at base), so the member collides with nothing it creates. **Existing logic
> modified: 1 file, +7 effective, zero imports added.** Production effective total **125** of 400.
> The member commit is `e67aacf0` (hooks ON; lint-staged's `eslint --fix` changed NOTHING —
> every committed blob equals the proved working-tree blob by digest).

---

## §0 · WHAT THIS CAR IS, AND WHAT IT REFUSES

Three leaves and one acceptance file, every one golden-inert by construction (charter §0's
central inheritance: the generator golden hashes the WHOLE settlement, so nothing here touches
the generation path — and the S0 golden run is the PROOF, §4 row 7):

1. **`src/domain/undercity/strataExistence.js` — `deriveStrataExistence(settlement, { sanitationRung? })`
   → `{ exists, seeds[], refused[], derivation: 'STRATA_EXISTENCE_V1' }`.** ODQ §311.1 compiled:
   the SHEET exists iff at least one lawful seed; seed classes are the closed vocabulary
   `sanitation | crypt | cellar | mine | subterranean` (the doctrine's four typed classes plus the
   declared-subterranean kin — §8 J-TEUC0-1); every seed row carries its class, its licensing
   fact (`licence`, `facetKind`, `facetValue`), its dossier home (`home`), its canonical anchor key
   and the settlement's resolved terrain class as a receipt. Institutions resolve through
   `facetOf` ONLY under the catalog's OWN key spellings (`subterranean`; the new
   `institutionSubstructure`), never a name string (custom-content parity; the clandestineFacet
   CHOKEPOINT, never its KEY — D-8). The catalog's own forbiddance ("tunnels flood") is honoured
   and made VISIBLE: a seed whose row forbids a resource the settlement's native roster carries
   lands in `refused[]` and never counts. It is §287.4's fact-licensed StrataExistencePlan at the
   engine, and §7 F1's producer.
2. **`src/domain/undercity/jointVocabulary.js` — frozen closed data (J-R2-2):** `JOINT_KINDS`
   (`grate | stair | sealed_door | sluice | breach`, §311.9.2(iii)) and `TEMPERAMENTS`
   (`STATIC | MONOTONE | SURFACE_COUPLED | DEMAND_DRIVEN`, §311.8.2 / §441.5(b)), with two
   membership predicates. Minted by the FIRST car so every later component row imports one truth
   (§8 J-TEUC0-3 for the temperament co-mint).
3. **`src/domain/spatial/cohesionWeave.js` — ONE new INFERENCE-ONLY facet kind
   `institutionSubstructure`** (`crypt | cellar | sewer | mine | none`) as five regex rows in
   `FACET_INFERENCE`, FIXED against the real catalog at this base (§3). Domain-side, never a
   catalog `facets` addition (R-2 golden shift refused).
4. **`tests/domain/undercityStrataExistence.test.js`** — eight arms (§5), the guard-the-guard
   arm first.

**The §3 license-home audit was RE-EXECUTED field-level at this base** (§1) — including the
walls accessor walk the charter owed here (§441.5(d)): the engine-side home is NAMED,
`defenseProfileHasWalls` in `src/domain/causalState.js`, with the name-list idiom it rests on
recorded. No row STOPPED.

**IT REFUSES:** any undercity-proper predicate (UC-4's: criminal drivers, colonization), any
geometry or sheet rendering (D5's), any catalog edit (R-2), any generation-path write, any
registry verb or flag, any tuning-flavoured scalar (every constant here is a closed vocabulary),
any cure of the sibling defect D-8 (`clandestineFacet.js:50` — HK-1's micro-fix car, §445.3),
any `townMap/**` import (the first-paint closure law), any read of `inst.id` (no writer exists —
§2.4), and any consumer wiring (the member is DORMANT: nothing in `src/` imports
`src/domain/undercity/**` — proved by A6).

## §1 · THE §3 LICENSE-HOME AUDIT, RE-EXECUTED FIELD-LEVEL AT `84e06412` (§311.8.5's order)

Every row was walked at the line named, in THIS tree, by this lane (the charter's `b10ed1a1`
walk holds: T2H's landing touched none of these paths). Verdicts: **CONFIRMED** = read at the
line; **HOME NAMED** = the accessor this car names for a later car; **NO TYPED HOME** = the
doctrine named a field that does not exist (ruled, not a STOP); **PRODUCER-TRAIN DEPENDENCY**
= the unlanded producer. **No row reads STOP.**

| # | license input | home at `84e06412` (executed) | verdict |
|---|---|---|---|
| 1 | subterranean/clandestine institutions | `facets: { clandestine: 'clandestine', subterranean: 'subterranean' }` at `institutionalCatalog.js:886 / :1446 / :1965` — exactly three `facets:` lines in the file (`grep -n 'facets:'`); resolved through `facetOf` (`cohesionWeave.js:315`; `declaredFacet` :281 requires `facetKind in facets`). **The key this car reads is `facetOf(i, 'subterranean')`** — pinned by A8 against the real row at all three tiers; `facetOf(row, 'institutionFunction')` on the same row reads `null` (D-8 observed in `/tmp/uc0/probe-leaf.mjs`, NOT cured) | CONFIRMED (§441.5(a)) |
| 2 | crypt / cellar / sanitation / mine seed classes | NO facet kind existed: `FACET_INFERENCE` (`cohesionWeave.js:258`) carried `institutionNature` + `institutionFunction` only. **MINTED here**: `institutionSubstructure` rows fixed against the real catalog (§3) | HOME MINTED (§441.5(a)) |
| 3 | 'Sewage system' | `institutionalCatalog.js:2225-2231` — `required: false, baseChance: 0.4, tags: ['sanitation'], priorityCategory: 'infrastructure'`, city block (`city: {` :1539, `metropolis: {` :2249); rolled at metropolis too via `assembleInstitutions.js:243` `mergeCatalogs(city, metropolis)`. Resolves `institutionSubstructure → 'sewer'` by inference (the ONLY catalog row that does) — the full-web class, one truth with UC-1 (A8) | CONFIRMED (§441.1) |
| 4 | 'Aqueduct or water system' | `:2239-2245` — `required: true, baseChance: 1, exclusiveGroup: 'waterSupply', tags: ['essential', 'water']`, city block; the WATER side of §311.8.2(c) — resolves NO substructure (A8 pins it does not seed) | CONFIRMED (§441.1) |
| 5 | 'Public bathhouse' | `:978-984` — `baseChance: 0.3, tags: ['trade', 'sanitation'], priorityCategory: 'government'`, town block (`town: {` :891); a sanitation-TAGGED cause for UC-1, not the full-web class — resolves NO substructure (A8) | CONFIRMED (§441.1) |
| 6 | founding kind (planned/charter/military) | `grep -rn 'founding\.kind\|foundingKind' src` → **0 hits** at this base; `genArrivalDetail` (`narrativeGenerator.js`) yields prose picks only; `historyGenerator.js:800` documents the `founding` object's shape as prose. Prose-regex REFUSED by name | NO TYPED HOME (§441.2 — UC-1's zero-weight slot, R-7) |
| 7 | prosperity band | `corruption.js:318-323` `spawnCorruptionChance({ crime, security, prosperity })` reads `n01(prosperity)` beside crime/security — the band UC-1 reads | CONFIRMED |
| 8 | civic capacity | **57** catalog rows with `priorityCategory: 'government'` at this base (executed over the live import, `/tmp/uc0/catalog-flat.tsv`); `institutionNature` 'civic' inference row at `cohesionWeave.js:267` (`/\bhall\b\|court\|assembly\|council\|magistrat/`). This car pins the READ: facet-first through `facetOf` (the clandestineFacet law) | CONFIRMED |
| 9 | gradient + outfall / terrain | `resolveSettlementTerrain` (`src/domain/resolveTerrain.js:58` — "the ONE terrain read": `config.terrainType` → `terrainOverride` → `terrain` → top-level → `geography.terrain`); vocabulary `plains \| hills \| forest \| riverside \| coastal \| mountain \| desert`; the `[D6 THE UNDERWAYS]` affinity comments at `geographyData.js:268` (mountain) and `:577` (hills). This car reads the class through that accessor and carries it on every seed row as a receipt | CONFIRMED (the engine reads terrain CLASS; relief is fabric-side) |
| 10 | plague/fire REBUILD events | `calamityKernel.js:73` typedef `calamityHistory?: CalStamp[]`, read `:203`, appended `:459-460` (ring of 8); `buildCalamityLedger` at `display/calamityLedger.js:62`. Not read by this car; the §443 law binds UC-1/UC-2/UC-4 (through the ledger, never raw) | CONFIRMED (via calamityLedger, §443) |
| 11 | high-water population | `src/domain/highWater.js` **ABSENT** at `84e06412` (`ls` → No such file); T2R HOLDING at `f65b3ff3` (§444.1), READY, unlanded | PRODUCER-TRAIN DEPENDENCY (T2R) |
| 12 | burial demand × institution age | `src/domain/institutionFounding.js` **ABSENT** at this base; T2Q HOLDING at `cc9ef856` (§445.1), unlanded | PRODUCER-TRAIN DEPENDENCY (T2Q) |
| 13 | quarter trade volume | `urbanFabricKernel.js:283` (the header row) and `:405-406` `// 5. TRADE VOLUME — the M6d windowed throughput (dark ⇒ 0)` / `getSpatialLedger(worldState, 'tradeFlow')` | CONFIRMED (dark ⇒ 0) |
| 14 | resource sites (mines/quarries) | `src/domain/resourceSites.js` **ABSENT**; T2N dispatched at this base (§444.2), no commit yet — the path stays PREDICTED. This car's `mine` seed reads the ROSTER's workings ('Mine', 'Mine (open cast)', 'Stone quarry' by inference) — the roster is the gate's licence; T2N's sites are UC-2's extent/anchor | PRODUCER-TRAIN DEPENDENCY (T2N) for UC-2; the GATE needs none |
| 15 | criminal share of the economy | `safetyProfile.js:619` `const blackMarketCapture = Math.min(80, baseShadowPercent + stressShadowBonus);` · `corruption.js:539-541` the `compound.criminalEffective` precedence read, verbatim at this base. ⚠ NOTE for UC-4 (executed, not this car's): the observed-shape corpus probe (`/tmp/uc0/osr-keys.log`) lists `institutions`, `tier`, `config`, `defenseProfile` on the `settlement` shape but NOT `safetyProfile` — UC-4 must probe the corpus before reading `blackMarketCapture` on a grounded receiver (the §443 class) | CONFIRMED (§441.3) |
| 16 | syndicate power standings | `factionArchetypes.js:40` `CRIMINAL: 'criminal'`; the classifying regex `:69`; `corruption.js:418` reuses `criminalCaptureState` | CONFIRMED |
| 17 | **walls/tolls (smuggler license) — HOME OWED HERE (§441.5(d))** | **WALLS — HOME NAMED: `defenseProfileHasWalls(settlement.defenseProfile)` in `src/domain/causalState.js:306`** ("True when the defense profile carries REAL walls — an explicit hasWalls flag, a non-empty classified walls group (defenseGenerator's institutions.walls), or a legacy non-empty walls descriptor. Deliberately reads the DATA, never a regex over JSON.stringify"). Consumers today: `causalState.js:947`, `mapProfile.js:180`, `townMap/townLayoutV2.js:262`, `townMap/townMapModel.js:36` — engine-side and map-side alike, so UC-4 reads the SAME wall fact the map draws. **The idiom it rests on:** `defenseProfile.institutions.walls` is written at GENERATION by `defenseGenerator.js:32` — a name-list idiom over `nativeSemanticName(inst)` (`'wall', 'citadel', 'palisade', 'earthwork', 'inner citadel', 'massive walls'`); `defenseProfile` is an observed generation-written key (`/tmp/uc0/osr-keys.log`), so a UC-4 reader of it is lawful under §443. The candidates the charter listed are DISPOSED: `demographicsObservation.js:106` `{ granary: 0, walls: 0 }` is a CAPACITY-BINDING label (which ceiling binds), not a wall fact; `warDeployment.js` carries siege prose and `fortificationStrength` (attrition), not a wall predicate; `priorityHelpers.js:52` `hasWalls` and `historyGenerator.js:128` are `src/generators/**` idioms a domain leaf may not import (`tests/build/domainGeneratorsBoundary.test.js`). **TOLLS — rows NAMED, accessor OWED AT UC-4:** no engine accessor exists; the roster carries 'Toll bridge' (village/Economy, `tags: ['trade']`), 'Customs house' (town/Economy, `tags: ['guild', 'law_enforcement']`), 'Gates (if walled)' (town/Defense) and 'Harbour master's office' (city/Economy, port + law_enforcement). UC-4's packet prices the toll read (facet-resolved, never a name string) beside the wall read; the §311.8.2(d) licence is "a wall OR toll", so the wall accessor alone resolves the licence and this row is NOT a STOP | HOME NAMED (walls) · TOLL ROWS NAMED (accessor owed at UC-4, deferred row D-UC0-2) |

## §2 · THE GATE'S CONTRACT

### §2.1 Inputs, outputs, totality

`deriveStrataExistence(settlement, context = {})`. TOTAL: any input — `null`, a string, an
institutions list holding `null`/numbers/empty objects — yields a typed result; nothing throws,
nothing is invented (A2). `settlement.institutions` → array or empty; `settlement.config` →
object or null (the native-resource projection of a null config is `[]`); the terrain class is
`resolveSettlementTerrain(settlement)` or `null`. `context.sanitationRung` is UC-1's derived
rung once it lands (one of `SANITATION_LADDER`); absent or unknown ⇒ the roster read alone.

| field | meaning |
|---|---|
| `exists` | `seeds.length > 0` — the SHEET exists (§311.1's gate; the two-level reading's FIRST level) |
| `seeds[]` | every lawful seed row, ordered by anchor key then class (`compareCodepoint`); the ladder seed (anchor `null`) sorts first |
| `refused[]` | seed rows the catalog's own forbiddance refuses — `refusal: 'FORBIDDEN_RESOURCE'`, `resources: [...]` the native resources that fired — visible, never counted (the honesty contract, §311.9.2(vii), applied to the gate) |
| `derivation` | `'STRATA_EXISTENCE_V1'` — FROZEN; changing any rule is a declared shift by construction |

A seed row: `{ class, licence, home, anchor, name, facetKind, facetValue, terrain }` —
`class ∈ SEED_CLASSES`, `licence ∈ SEED_LICENCES` (`INSTITUTION_FACET` — the institution's
facet, declared or inferred, through `facetOf`; `DERIVED_RUNG` — UC-1's ladder), `home ∈
{ institutions, sanitation_ladder }` (the dossier home CT-4 cites), `anchor` the canonical
institution key (§2.4; `null` for the ladder seed), `name` the display string as a RECEIPT
(never the anchor), `facetKind`/`facetValue` the exact chokepoint read, `terrain` the resolved
class or `null`.

### §2.2 The class ← facet mapping (closed, one truth)

| facet read (through `facetOf`) | seed class | catalog rows that resolve it at this base |
|---|---|---|
| `subterranean === 'subterranean'` (declared only — no inference table for this kind) | `subterranean` | the three 'Underground network' rows (village/town/city); 'Underground city' (metropolis) declares NO facet and resolves nothing — A8 pins that rather than inferring it |
| `institutionSubstructure === 'sewer'` | `sanitation` | 'Sewage system' |
| `institutionSubstructure === 'crypt'` | `crypt` | the ten church / cathedral / monastery / graveyard rows (§3) |
| `institutionSubstructure === 'cellar'` | `cellar` | the nine granary / warehouse / root-cellar / vintner / brewer rows (§3) |
| `institutionSubstructure === 'mine'` | `mine` | 'Mine', 'Mine (open cast)', 'Stone quarry' |
| `institutionSubstructure === 'none'`, `null`, or any value outside `SUBSTRUCTURE_VALUES` | — (licenses nothing) | the two 'Access to …' rows infer `none`; a declared `none` overrides an inference (A4); an unknown declared value (`'vault'`) licenses nothing — closed vocabulary, never a guess |

One institution may license two rows (a custom row declaring `subterranean` AND inferring
`cellar` yields both — two honest facts, one anchor).

### §2.3 The sanitation seed — one truth with UC-1 (§441.1)

`sanitationRosterOf(institutions)` returns every institution resolving `institutionSubstructure
→ 'sewer'` (the catalog's 'Sewage system' by inference; a custom institution declaring the
sanitation/drains facet `institutionSubstructure: 'sewer'`), ordered by anchor. **UC-1's FULL
WEB rung exists exactly where this list is non-empty** — UC-1 imports this function and never
re-derives the read, so the seed and the rung can never disagree about whether the sewer
institution exists. §311.7.1's "cesspits contribute no sheet" is `sanitationRungSeeds(rung)`:
the rung's index in `SANITATION_LADDER = ['cesspits', 'culvert', 'quarter_network', 'full_web']`
must be ≥ 1. UC-1 imports the ladder from here (§8 J-TEUC0-2) and supplies its rung through
`context.sanitationRung`; the resulting seed row is `{ class: 'sanitation', licence:
'DERIVED_RUNG', home: 'sanitation_ladder', anchor: null, name: null, facetKind:
'sanitationRung', facetValue: <rung> }` (A3). Today (UC-1 unbuilt) the roster read stands alone
— honest, not understated: a city without the institution has quarter drains, and UC-1 is the
car that says so.

### §2.4 The anchor key (§441.5(k)) and the `id` deferral

`institutionAnchorKey(inst)` = the generation-stamped `catalogId` when present (it LEADS even
when a DM renamed the display name — A6, mutant m5), else `catalogIdForName(name)` (an
unstamped legacy row of a catalog name slugs to the same key as a stamped one), else the name
itself (a custom row's stable name key — the registry's own fallback, `institutionalCatalog.js`
"every id-first join falls back to the legacy name matcher"), else `null`. **`inst.id` is NOT
read.** Executed reason: the observed-shape corpus (`/tmp/uc0/osr-keys.log`) shows the
`institutions` shape WITHOUT `id`, and `scripts/.observed-shape-readers-baseline.json` banks
`id on institutions` as a reader-with-no-writer identity in 18 files — a new reader in a new
file mints a row at ceiling 0 and reds `observedShapeReaders.walker` (T2R's second red, §443's
class). Since no writer produces `id` on any institution today, behaviour on every live world is
IDENTICAL to the charter's order; the `id`-precedence rung is DEFERRED (D-UC0-1) to the
entity-registry migration that mints the writer (`settlement.schema.js:744` "required after
entity-registry migration"), at which point the walker itself will admit the read.

### §2.5 The catalog's forbiddance, honoured and visible

`forbiddanceOf(inst, anchor, facetKind)` unions three spellings of ONE truth: the row's own
`forbiddenResources` (observed on the `institutions` shape — the generator spreads it), the
catalog row's list by stable key (an unstamped legacy row), and the CLASS union over every
catalog row declaring the same facet kind (derived at module load from the catalog: today
`subterranean → ['fertile_floodplain', 'marshlands']`), so a CUSTOM subterranean institution
that wrote no forbiddance of its own still inherits "tunnels flood" (A5). The resources
compared are `nativeSemanticResourceKeys(config)` — the SAME projection `assembleInstitutions.js:
337-339` refuses with at generation, so a custom-labelled "marshlands" (`nearbyResourcesCustom`)
grants no native physics and does not refuse (A5). On a generated world this re-check is a
no-op for catalog rows (the generator already refused); it bites on edited rosters, custom
content and legacy saves — and it is reported, not hidden.

### §2.6 The mine seed and "where terrain licenses them"

The roster IS the licence: the generator rolled the workings under its terrain and resource
multipliers (`getResourceMultiplier`, `geographyData` affinities), so a 'Mine' in the roster is
terrain-licensed by construction; the engine re-checks only the catalog's own forbiddance (none
on the mine rows today) and carries the terrain CLASS on the row as the dossier receipt. T2N's
resource sites are UC-2's extent/anchor inputs, not the gate's.

## §3 · THE FACET KIND, PRICED AND PROVEN (§441.5(a))

**Headroom, measured BEFORE the edit with the validated instrument** (eslint `Linter`,
`max-lines { skipBlankLines: true, skipComments: true }`, `/tmp/uc0/efflines.mjs`, working tree
and `git show 84e06412:…` agreeing): `src/domain/spatial/cohesionWeave.js` = **141** effective.
The shared/hot-file cap of ≤15 puts the ceiling at 156; the edit lands at **148 (+7)**. The file
has no `scripts/.size-baseline.json` row and no `tests/lint/.domain-any-baseline.json` row (0
debt, unchanged), is not on PACKET_STANDARD's hot-file list, and gains NO import (the coupling
census, `CENSUS_SCOPE_RE` over `worldPulse|spatial` importers, is unmoved — 16/16).

**The rows, FIXED against the real catalog by an executed probe** (`/tmp/uc0/rows-probe.mjs` →
`rows-probe.log`; the chokepoint's text is `name + ' ' + type + ' ' + category`, FIRST match
wins, type absent on catalog rows; zero matches against any bare category string):

| order | value | regex | REAL catalog rows it resolves (311 rows walked) |
|---|---|---|---|
| 1 | `none` | `/^access to /i` | 'Access to parish church' (thorp, hamlet), 'Access to external mill' (thorp, hamlet) — the church/mill stands ELSEWHERE; ordered FIRST so "church" cannot infer a crypt for a building 2–5 km away (A1 pins the order) |
| 2 | `sewer` | `/sewage\|sewer\|drain/i` | 'Sewage system' — the only one |
| 3 | `mine` | `/\bmine\b\|quarry/i` | 'Mine', 'Mine (open cast)', 'Stone quarry' (not 'Smelter', 'Salt works', 'Peat cutter', 'Mint' — surface works) |
| 4 | `crypt` | `/church\|cathedral\|monaster\|friary\|abbey\|graveyard\|cemetery\|ossuary\|crypt\|temple/i` | 'Parish church', 'Parish churches (2-5)', 'Parish churches (10-30)', 'Parish churches (50-100+)', 'Cathedral (10,000+ only)', 'Great cathedral', 'Monastery or friary', 'Multiple monasteries', 'Major monasteries (5-10)', 'Graveyard' (not 'Wayside shrine', 'Priest (resident)', 'Small hospital', 'Almshouse', 'Druid Circle' — no burying house) |
| 5 | `cellar` | `/warehouse\|cellar\|granar\|storehouse\|undercroft\|vintner\|brewer/i` | 'Communal root cellar', 'Town granary', 'City granaries', 'State granary complex', 'Merchant warehouses', 'Warehouse district', 'Vintner', 'Brewer', 'Brewery' (the charter's example row carried bare `merchant`, REFUSED: it would have matched 'Merchant oligarchy', a government, and the three 'Merchant guilds' rows, which store nothing) |
| — | (null) | — | the remaining 253 distinct rows |

`none` is a lawful DECLARED value as well (`facets: { institutionSubstructure: 'none' }` on a
"Temple of the Open Sky" overrides the crypt inference it would otherwise earn — A4), which is
what makes DECLARED-over-INFERRED meaningful for this kind. Declared values outside the closed
set resolve through `facetOf` (which does not validate) and are REFUSED by the deriver's
mapping (they license nothing) — closed vocabulary at the consumer.

**Golden-inert, PROVEN (constraint 2):** `tests/property/generatorGoldenMaster.test.js` 3/3
green BEFORE the edit at `84e06412` and AFTER it with every member file in place;
`tests/fixtures/generator-golden-master.json` **bytewise identical at
`29c6cc8fd0573a37a8e4042b8f98db92fbe49ea36e24f61355c806f79bc9e0a8` (67,779 bytes)** both times
(`/tmp/uc0/s0-base-verbose.log`, `/tmp/uc0/tip-battery.log`). Generation never reads facet
kinds: `FACET_INFERENCE` is reached only through `facetOf` at classification time, by domain
consumers (`generosityKernel`, `clandestineFacet`, `institutionLifecycle`, …), none of which is
on the generation path. No catalog `facets` key moved (constraint 3: `assembleInstitutions.js:
296-299/:355` spread the entry onto the hashed settlement — REFUSED). `tests/domain/cohesionWeave.test.js`
17/17 unchanged (constraint 5); the new kind is a third table key.

## §4 · PREFLIGHT, EXECUTED AT `84e06412` AND RE-EXECUTED AT THE MEMBER TIP

| # | row | result |
|---|---|---|
| 1 | `git rev-parse claude/composite-r4` at opening | `84e06412f6fdca91020197f2a332ba7f98642f64`; the worktree was added detached at it; `refs/preserve/holding-t2r` = `f65b3ff3`, `holding-t2q` = `cc9ef856` unmoved by this lane |
| 2 | deps | `package-lock.json` byte-identical to the chair's `chair-baseproof-b10ed1a1` (`cmp` exit 0) → its `node_modules` APFS-cloned (`cp -Rc`, not a symlink); `npm ls` exit 0; `npx husky` exit 0; `core.bare=false`, `hooksPath=.husky/_`; disk 8,153,876 KB free at start |
| 3 | preamble SHA-256 at base and at tip | `0706aad6…` both — no re-stamp in the window |
| 4 | the three created paths and this packet's path at base | ABSENT (`git ls-tree` empty; `src/domain/undercity/` does not exist) |
| 5 | §417 duplicate-path probe (executed over the 149-row manifest) | `cohesionWeave.js` held by 0 packets; the three created paths by 0; the census path by 68 (67 LANDED + MF-T2H READY) — NOT reserved here; exactly ONE non-terminal packet in the manifest (MF-T2H) |
| 6 | coupling census scope | `CENSUS_SCOPE_RE = /^src\/domain\/(?:worldPulse\|spatial)\//` — cohesionWeave.js is IN scope but gains no import; `src/domain/undercity/**` is OUT of scope as an importer; `couplingInclusion.walker` 16/16 at base and tip |
| 7 | generator golden, BEFORE and AFTER (`tests/property/generatorGoldenMaster.test.js`) | 3/3 green both; manifest `29c6cc8f…` bytewise identical (§3) |
| 8 | anchors (§P3) | `tests/lint/negativeAssertionAnchor.walker.test.js` 9/9 at base and with the member; every scanned negative carries `// anchored:` on its line or the line above, or routes through `expectAbsentWithAnchor` BY NAME |
| 9 | census tuple at base, walker GREEN 33/33 | `2499 / 364 / 2135 / 20729 / 5787` |
| 10 | census tuple with the member — the SEQUENCED walk, each figure read from its own failure message, then confirmed GREEN 33/33 | `2500 / 364 / 2136 / 20737 / 5788` = `+1 / 0 / +1 / +8 / +1`; then REVERTED per §417 (the named interior red) |
| 11 | effective lines (eslint `Linter`) | `strataExistence.js` **110** of 250 · `jointVocabulary.js` **8** of 20 · `cohesionWeave.js` **141 → 148 (+7)** of +15 · test 241 |
| 12 | `package.json` / `package-lock.json` motion | none — no §349.2 mint trigger |
| 13 | the two typecheck configurations, by name, bare, one at a time, at the member | `typecheck:ratchet` → `[typecheck-ratchet] OK — no type regressions (173 error(s), ceiling 173).` exit 0 · `typecheck:domain:strict` → `[domain-strict] ✓ no strict-type regressions (1134 errors, ceiling 1134).` exit 0 |
| 14 | `tests/lint/domainAnyCastBaseline.test.js` (monotone-down) | 13/13 at base and with the member; `count-domain-any` reads `{any:0, suppress:0}` on all three production files — the new leaves carry ZERO debt and cohesionWeave.js stays at 0 |
| 15 | `tests/lint/observedShapeReaders.walker.test.js` with the member | **27/27 GREEN; the A1/A7 literals `reads 1995 / identities 1409 / files 387` UNMOVED, violations 0** — the leaf reads only generation-written keys (`institutions`, `config.nearbyResources*`, `name`, `catalogId`, `facets`, `forbiddenResources`; terrain through `resolveTerrain.js`'s banked read) and never `inst.id` (§2.4) |
| 16 | `tests/lint/sizeBaseline.test.js` · `domainGeneratorsBoundary` · `entropyRootCensus.walker` · `underwaysInstitutionParity` | 3/3 · 3/3 · 31/31 · 9/9 at base and tip (UNMOVED, as predicted in the lane's instrument table) |
| 17 | `tests/build/townMapLazy.test.js` | SKIPPED at base and tip (3 dist-gated arms; `dist/` absent in the worktree) — the first-paint claim is proved by A6 instead (no `townMap` import in either leaf; nothing in `src/` imports `src/domain/undercity/`) |
| 18 | raw C0 control bytes in every authored file (`LC_ALL=C grep -c $'[\x01-\x08\x0b\x0c\x0e-\x1f]'`) | 0 in both leaves, the test, cohesionWeave.js, this packet, the manifest row and the index row |
| 19 | the enforcement-claims `CLAIM_RE` (`tests/docs/enforcement-claims.test.js:40`) over this packet and the leaves | 0 hits |
| 20 | nondeterminism tokens over the raw text of both leaves (`Date`, `Math.random`, `Intl`, `toLocale`, `performance`, `crypto`, `localeCompare`) | 0 — A6 with its planted positive control; ⚠ the scan counts PROSE, so the leaf headers name the vocabulary only in the abstract (the T2H B2 lesson, which convicted this lane's first header) |
| 21 | `tests/lint/seedLoopTotality.walker.test.js` (ceiling 0 per new test file — the anchor walker's class) | convicted the first authoring of the acceptance file ONCE (3 bare seed loops, header-matched on `SEED_*`/`…Seeds`); CURED by totality rewrites (a rung table, an empty-offender list, a mapped frozen check; the helper's and A8's unscanned loops made total as well) — 9/9 after |
| 22 | `tests/lint/ruinFilterRoster.walker.test.js` | RED with the member (2 arms) → DISPOSITIONED (§10.1) → 12/12 |

## §5 · ACCEPTANCE — EIGHT ARMS, ONE LITERAL `describe`, STRAIGHT-LINE `it` (8/8 green, exit 0)

`npx vitest run tests/domain/undercityStrataExistence.test.js` —
`describe('MF-UC0 — the underground existence gate, the substructure facet, and the joint vocabulary')`.
Fixtures are REAL catalog rows spread exactly as the generator spreads them (`{ category, name,
...entry, source, catalogId }`), never a fixture spelling.

| # | arm | what it proves |
|---|---|---|
| A1 | **guard-the-guard** (the arm every MF walker opens with) | the `institutionSubstructure` rows are LIVE against the REAL catalog and KEYED RIGHT: 300+ rows walked through `facetOf`, the exact sorted name set per value (`sewer` 1 · `mine` 3 · `crypt` 10 · `cellar` 9 · `none` 2), every resolved value inside the closed set, the first-match order pinned ('Access to parish church' → `none`, 'Parish church' → `crypt`), a misspelled kind resolves nothing, every vocabulary non-empty — before any arm below can pass on nothing |
| A2 | **no qualifying seed → no sheet** (charter acceptance 1) | a real seedless roster reads `{ exists: false, seeds: [], refused: [] }`; a `cesspits` rung contributes no sheet (§311.7.1); unknown rungs do not seed; `null`/`undefined`/garbage rosters and configs never throw and never invent |
| A3 | **one modest seed → the sheet exists** (acceptance 2, the two-level reading's first level) | a thorp with only a 'Communal root cellar' reads `exists: true` with exactly one `cellar` row (anchor `communal_root_cellar`, terrain `hills`); a seedless roster with a `culvert` rung reads exactly one `DERIVED_RUNG` sanitation row; every rung above cesspits seeds; the result's key set is exactly the four contract fields |
| A4 | **facet-DECLARED custom institutions count** (acceptance 3, incl. a custom sanitation/drains facet) | a `facets` declaration and a `facet:<kind>:<value>` tag both count whatever their English; the custom drains facet seeds `sanitation` AND appears in `sanitationRosterOf` (one truth with UC-1); a declared `none` overrides the crypt inference "Temple" earns (DECLARED over INFERRED, both readings asserted); a value outside the closed set licenses nothing — both absences anchored by present siblings |
| A5 | **marsh/floodplain forbiddance honoured** (acceptance 4) | the real 'Underground network' row carries `['marshlands', 'fertile_floodplain']`; on a native-marsh settlement it is REFUSED (`refused[0].resources === ['marshlands']`, `exists: false`), on dry hills it seeds; a custom `subterranean` row with no forbiddance of its own inherits the class rule on a floodplain; a custom-LABELLED marsh (`nearbyResourcesCustom`) does not refuse (the native projection, as the generator); refusal is per row — a parish church beside the refused network still seeds |
| A6 | **deterministic · closed · pure · dormant** (acceptance 5 + the seed-class enumeration pin + the S0 entry-closure arm) | same roster in reversed order → `toEqual` and byte-identical JSON; seeds ordered by anchor; every row's class/licence in the closed sets; `institutionAnchorKey`'s four rungs incl. the renamed-stamp case (m5's line); all six vocabularies as LITERALS and `Object.isFrozen`; the predicates; a raw-text nondeterminism scan with a planted positive control; the leaves import no `townMap` module (import list proven non-empty first); nothing in `src/` mentions `domain/undercity/` except the leaf's own header |
| A7 | **G-43: remove the last qualifying institution → exists flips false** (acceptance 7) | a village with 'Parish church' + 'Stone quarry' reads `['crypt', 'mine']`; remove the quarry → still exists on the crypt; remove the church → `exists: false`, `seeds: []`, with the two non-seed rows proven still present (the flip is the SEED's, not roster size); re-add the church → true |
| A8 | **the POSITIVE KEY arm** (acceptance 8; §441.5(a) constraint 4) | at village, town AND city the real 'Underground network' row's `facets` is exactly `{ clandestine, subterranean }`, `facetOf(row, 'subterranean') === 'subterranean'`, `facetOf(row, 'clandestine') === 'clandestine'`, and the gate seeds `['subterranean', 'underground_network']`; 'Underground city' (metropolis) declares no facet and resolves `null` (pinned, not inferred); a city roster carrying the real 'Sewage system' seeds exactly the sanitation row of the full-web class (anchor `sewage_system`) and `sanitationRosterOf` returns exactly it; 'Public bathhouse' (sanitation-TAGGED, a UC-1 cause) and 'Aqueduct or water system' (the water side) do NOT seed — anchored by the sewer row — and the roster of the two alone is empty |

Cases omitted, not replaced: no persistence round-trip and no idempotent-write case (nothing
is written), no privacy boundary (no rendered surface), no lit-flag case (no flag).

## §6 · MUTANTS — m1–m5 ALL CONVICT, planted and restored digest-exact with two clean controls

Harness `/tmp/uc0/mutants.sh` (+ `mutants-m5b.sh`), every vitest run mutexed; pristine digests
leaf `94f146b2…` / cohesionWeave `dc9946ea…` / test `b02df4f0…` reproduced after every restore
(`RESTORED digest-exact` ×6):

| # | mutant (a BRANCH, never a count) | predicted | executed |
|---|---|---|---|
| m1 | the `none` row deleted from `FACET_INFERENCE.institutionSubstructure` | A1 red | **RED A1** — 'Access to parish church' infers `crypt` and the `none` set empties; the guard-the-guard arm convicts the table LIVE against the real catalog |
| m2 | the forbiddance branch made dead (`hit.length > 99`) | A5 red | **RED A5** — the marsh network seeds instead of refusing |
| m3 | cesspits made a seed (`i >= 0`) | A2 red | **RED A2** — the bottom rung contributes a sheet |
| m4 | **the D-8 defect RE-PLANTED** — the subterranean read under the precedent's wrong key (`facetOf(inst, 'institutionFunction')`) | A8 red (A4/A5 too) | **RED A4, A5, A6, A8** — the real network row resolves nothing under the wrong key; the positive key arm is exactly the pin §441.5(a) ordered |
| m5 | the `catalogId` precedence line DELETED | A6 red | **RED A6** — `expected 'The Drains' to be 'sewage_system'` (the renamed-stamp property) |
| control ×2 | the clean tree | green | **PASS 8/8** both (`mutant-control.log`, `mutant-control2.log`) |

⚠ **One mutant candidate was EQUIVALENT on its first spelling and was RE-PLANTED (§P6):** m5
as a `!== ''` → `=== ''` flip convicted only A6's empty-string edge, because for every catalog
name the stamped `catalogId` equals `catalogIdForName(name)` — the precedence branch is
indistinguishable from the slug fallback on catalog rows. The property that separates them (a
DM-renamed display name on a stamped row) was PINNED in A6, and the mutant re-planted as a
deletion convicts on exactly that line. An equivalent mutant is banked as a property, never
left as luck.

## §7 · EXACT CHANGE MANIFEST

| Action | File | Region | Delta | Instruction |
|---|---|---|---|---|
| `CREATE` | `src/domain/undercity/strataExistence.js` | new domain-root leaf (new directory) | **110** effective of 250 | `deriveStrataExistence`, `institutionAnchorKey`, `sanitationRosterOf`, `sanitationRungSeeds`, `SEED_CLASSES`, `SANITATION_LADDER`, `SEED_LICENCES`, `SUBSTRUCTURE_FACET_KIND`, `SUBSTRUCTURE_VALUES`, `SUBTERRANEAN_FACET_KIND`, `STRATA_EXISTENCE_DERIVATION`; imports cohesionWeave / resolveTerrain / customContentSemanticAuthority / deterministicSort / institutionalCatalog only; pure; dormant |
| `CREATE` | `src/domain/undercity/jointVocabulary.js` | new frozen data leaf | **8** effective of 20 | `JOINT_KINDS`, `TEMPERAMENTS`, `isJointKind`, `isTemperament`; zero-import |
| `MODIFY` | `src/domain/spatial/cohesionWeave.js` | `FACET_INFERENCE.institutionSubstructure` (five rows after `institutionFunction`) | **+7** effective of ≤15 (141 → 148) | the inference-only kind; no import, no export, no other line |
| `CREATE` | `tests/domain/undercityStrataExistence.test.js` | new acceptance file | 241 (test) | one literal `describe`, eight straight-line `it`; anchored negatives by `// anchored:` and `expectAbsentWithAnchor` by name; every multi-case check TOTAL (no early-exit loop) |
| `TEST` | `tests/lint/ruinFilterRoster.walker.test.js` | `RUIN_AGNOSTIC_EXEMPT` (one row in the existence-gate section) + the exact reader count | +1 row; 89 → 90 | the §311.3 disposition of the gate's raw roster read (§10.1, J-TEUC0-8); read the count from the arm's own message, never compute it |
| `DOC` | `docs/implementation/packets/town-cartography/MF-UC0.md` | new | — | this packet |
| `DOC` | `docs/implementation/PACKET_MANIFEST.json` | one packet row appended by TEXT surgery (parsed to verify, never re-serialised) | — | registration |

`docs/implementation/INDEX.md` receives one table row as the coordination-ledger surface it is
(ODQ §331.4: packets do not list it). ⛔ The shared census path is NOT a row here (§417; the
row text rides the census note). Generated artifacts — AS AUTHORED `NONE`; CORRECTED at the landing act (ODQ §475, the eight-file edge-shared set the MODIFY host owed — the WEB-3 form, committed together and none hand-edited):

| generator | artifacts |
|---|---|
| `npm run build:edge-shared` | `supabase/functions/_shared/aiCharterBundle.js` + `aiCharterBundle.meta.json`, `aiGroundingBundle.js` + `aiGroundingBundle.meta.json`, `aiOutputSchemaBundle.js` + `aiOutputSchemaBundle.meta.json` (each bundles `cohesionWeave.js` transitively — the three `.js` move by exactly this member's seven rows + the `Source hash` header), and the two sibling `analyticsEventsBundle.meta.json` / `intentAtlasBundle.meta.json` (by `generatedAt` only — one build window, the CR-EB-2 (b) coherence rule) |

No `tests/lint/**` file is minted,
so §P3b's mutation-coverage row does not attach. No registration file is minted (no flag, no
spatial-ledger row, no coupling row; the facet rows are inference data in an existing table —
`tests/domain/cohesionWeave.test.js` is the registry test and is unmoved). **Symbols the
deliverable CREATES** join `requiredSymbols` **at the flip to LANDED, never at READY**
(PACKET_STANDARD §"Change manifest"). At READY the rows name only what the deliverable
PRESERVES: the facet chokepoint, the terrain read, the native-resource projection, the
comparator, the catalog's id index, the walls accessor this packet NAMES for UC-4 (so its
disappearance is detectable before UC-4 compiles), and the anchored helper.
`retiredSymbols`: **EMPTY**.

## §8 · JUDGMENT CALLS, VETOABLE BY A WORD

- **J-TEUC0-1 — the closed seed-class vocabulary is the doctrine's four PLUS `subterranean`.**
  §311.1 reads "sewers, crypts, cellars and their kin"; the catalog's 'Underground network'
  ("Dug smuggling passages beneath the village") is the most literally subterranean row in the
  catalog, §3 row 1 confirms its declared facet as a LICENSE INPUT, and §441.5(a) orders the key
  pinned — a village whose only underground is its dug passages plainly has a sheet. Chosen over
  filing the kin under `cellar` (a stretch on "untaxed storage") or refusing it (a sheet-less
  warren). Say "veto" to drop the class; A8's key pin stands either way.
- **J-TEUC0-2 — `SANITATION_LADDER` (the §311.7.1 rung names) is minted HERE, and the derived
  rung enters through an optional second argument.** The charter's sanitation seed reads "or
  where UC-1's derived rung is ≥ culvert"; with UC-1 unbuilt the three honest shapes were (a) a
  MODIFY row on this leaf at UC-1 (a second edit into a landed leaf), (b) this leaf importing
  UC-1 later (a cycle: UC-1 imports `sanitationRosterOf` from here), or (c) the rung as a typed
  optional input over a ladder minted by the first car — J-R2-2's own logic. (c) chosen; the
  ladder is data, not tuning (the doctrine's four words), and UC-1 imports one truth.
- **J-TEUC0-3 — `TEMPERAMENTS` co-minted in `jointVocabulary.js`.** §441.5(b) makes every
  component row from UC-3 on carry both a joint kind and a temperament; the reason J-R2-2 gave
  for minting the joint data first applies verbatim to the temperament enum (UC-3 would
  otherwise mint it and UC-1/2/4/5 import from a middle car). Two lines, 8 effective total.
- **J-TEUC0-4 — `refused[]` is reported beside `seeds[]`.** §7 F1 names `{ exists, seeds[] }`;
  the refused rows are ADDITIVE (CT-4's F1 reads are unchanged) and make the forbiddance
  VISIBLE rather than silently dropped — the honesty contract applied at the gate. Say "veto"
  to drop the field; A5 then pins `exists`/`seeds` only.
- **J-TEUC0-5 — the anchor key omits the `id` rung today (§2.4).** Chosen over reading
  `inst.id` and eating a reader-with-no-writer red, and over reading it through an indirection
  the walker cannot ground (gaming the instrument). Identical behaviour on every live world;
  deferred row D-UC0-1 names the wake condition.
- **J-TEUC0-6 — `vintner` and `brewer` are cellar-bearing commerce; bare `merchant` is not.**
  Wine and beer are stored and fermented below ground; the charter's example `merchant` token
  would have seeded a cellar under 'Merchant oligarchy' (a government) and the 'Merchant guilds'
  rows. Each row's match set is executed and pinned in A1, so a veto is a one-row edit plus the
  re-pinned list.
- **J-TEUC0-8 — the existence gate reads the RAW roster and is DISPOSITIONED as ruin-agnostic,
  not routed through `liveInstitutions()`.** The §408 sweep's one member-caused red
  (`ruinFilterRoster.walker`, §10.1) offered two cures; the doctrine decides: §311.3 "the
  HIGH-WATER LAW runs underground — dug is forever", §311.8.2(b) "ABANDONED not erased" — a
  ruined church's crypt is a FOSSIL (UC-4's recession vocabulary), never an absence, so a
  gate that filtered ruins would erase exactly the seeds the doctrine says cannot recede. The
  exemption row names the reason in the walker's own existence-gate category (the
  `mapProfile.js` "a ruined citadel still reads walled" precedent) and the reader count is
  re-measured from the arm's message. ⚠ This is the SECOND-RED line the brief draws
  (`seedLoopTotality` is classified as an authoring ratchet over the lane's own new file, the
  anchor walker's class — §4 row 21 — not as a member-caused red; if the chair reads it as
  one, this packet flips to BLOCKED by a word). Reverse: delete the exempt row, read
  `liveInstitutions(s)` in the leaf, re-measure the count at 89, and add a `standing` receipt
  so CT-4 can still speak of the crypt beneath the ruin.
- **J-TEUC0-7 — the walls home is NAMED as `defenseProfileHasWalls`, and the toll accessor is
  OWED at UC-4 rather than minted here.** The §311.8.2(d) licence is "a wall OR toll"; the wall
  accessor resolves it engine-side with a recorded idiom; a toll facet kind minted here without
  its consumer would be an unconsumed vocabulary (the FINITE-SEMANTICS law prefers the consumer
  to mint it). Recorded as D-UC0-2.

## §9 · RAISED — split OWNER / CHAIR / DEFERRED

### Owner (nothing new; the charter's docket stands)

No owner item is raised by this car. R-2 (generation-path motion) did NOT fire: the golden is
bytewise identical. R-3 (tuning constants): NONE in this car — every constant is a closed
vocabulary. R-4 (new surface): NONE — the member is dormant.

### Chair (recorded; nothing here blocks the landing)

1. **RAISED-1 — the `institutionSubstructure` rows are a domain-side table the §I facet law's
   coverage walker (W-COMPOSER-1's mint-time UX) does not yet surface to custom-content
   authors.** A DM who wants a custom crypt declares `facets: { institutionSubstructure: 'crypt' }`
   or a `facet:institutionSubstructure:crypt` tag today; the authoring surface that offers the
   kind is that walker's, not this car's.
2. **RAISED-2 — the `safetyProfile` key is absent from the observed `settlement` shape in the
   corpus probe** (§1 row 15). UC-4's `criminalShare` read must be probed against the corpus
   before it is written, or routed through `corruption.js`'s own banked read by import (the
   charter's preferred shape already).
3. **RAISED-3 — HK-1 (D-8) can now cite an executed witness:** `/tmp/uc0/probe-leaf.mjs` shows
   `facetOf(<the :886 row>, 'institutionFunction') === null` while `facetOf(row, 'clandestine')
   === 'clandestine'`. A8 pins the RIGHT keys; it deliberately does not pin the wrong one, so
   HK-1's cure cannot red this member.

### Deferred, with the wake condition

| # | item | wakes when |
|---|---|---|
| D-UC0-1 | the `id`-precedence rung of `institutionAnchorKey` (§441.5(k)'s first clause) | an institution writer of `id` lands (the entity-registry migration, `settlement.schema.js:744`) — the observed corpus then carries the key and the walker admits the read |
| D-UC0-2 | the TOLL accessor for the smuggler licence (§311.8.2(d)'s "or toll") | UC-4's packet, beside the wall read — facet-resolved over the named rows ('Toll bridge', 'Customs house', 'Gates (if walled)', 'Harbour master's office'), never a name string |
| D-UC0-3 | the DERIVED_RUNG sanitation seed's live feed (`context.sanitationRung`) | UC-1 lands `deriveSewerLadder` and its consumers pass the rung; until then the roster read stands alone (honest: a city without the institution has quarter drains, which UC-1 will say) |
| D-8 (charter) | `clandestineFacet.js:50` reads `'institutionFunction'` for `'clandestine'` | HK-1, its own micro-fix car (§445.3) — NOT cured here (J-R2-1) |

**The §7 F1 consumer note (CT-4; D5):** `strataExistence: { exists, seeds[] }` is produced
here exactly as §7 F1 names it, with `refused[]` and `derivation` ADDITIVE. CT-4's "why this
town has an underground at all" sentence cites `seeds[].name`/`anchor` and `home`; where the seed
is the sewer institution it cites THAT institution (the dossier already lists it). The D5 strata
fabric consumes the SAME object through §287.4's StrataExistencePlan. A later UC executor
changing this shape after CT-4 compiles owes CT-4 a re-compile flag (contract term 2).

## §10 · THE GATES, AND THE ATTRIBUTION OF EVERY RED

**Executed by TE-UC0 at `84e06412` + the member, every exit captured in-shell, every vitest
invocation under `scripts/gate-mutex.sh --run` on the shared lock, short `TMPDIR=/tmp/uc0`,
logs under `/tmp/uc0`:**

| instrument | at `84e06412` (this worktree, before any edit) | with the member | verdict |
|---|---|---|---|
| `tests/domain/undercityStrataExistence.test.js` | — | **8/8** (`acc1.log`, `mutant-control2.log`) | GREEN |
| `npm run typecheck:ratchet` | — | `[typecheck-ratchet] OK — no type regressions (173 error(s), ceiling 173).` exit 0 | GREEN at the exact ceiling |
| `npm run typecheck:domain:strict` | — | `[domain-strict] ✓ no strict-type regressions (1134 errors, ceiling 1134).` exit 0 | GREEN at the exact ceiling |
| `tests/property/generatorGoldenMaster.test.js` | 3/3, manifest `29c6cc8f…` | 3/3, manifest `29c6cc8f…` bytewise | UNMOVED — golden-inert PROVEN |
| `tests/lint/observedShapeReaders.walker.test.js` | 27/27 (1995/1409/387) | 27/27 (1995/1409/387) | UNMOVED — no new reader row |
| `tests/lint/sovereigntyLightingContract.walker.test.js` (census arm) | 33/33 | RED by construction ("expected 2500 to be 2499"); 33/33 with the deferred row applied, then reverted | the ONE NAMED interior red |
| `negativeAssertionAnchor` · `cohesionWeave.test` · `domainAnyCastBaseline` · `sizeBaseline` · `domainGeneratorsBoundary` · `couplingInclusion` · `entropyRootCensus` · `underwaysInstitutionParity` | 9 · 17 · 13 · 3 · 3 · 16 · 31 · 9 | identical counts | UNMOVED |
| `tests/lint/seedLoopTotality.walker.test.js` | 9/9 | RED once on the first authoring (3 bare loops in the new file) → CURED in the file → 9/9 | authoring ratchet, ceiling 0 per new file (§4 row 21) |
| `tests/lint/ruinFilterRoster.walker.test.js` | 12/12 | RED (2 arms) → DISPOSITIONED → 12/12 | **the ONE member-caused red — cured under the walker's own prescribed shape, J-TEUC0-8** |
| `tests/build/townMapLazy.test.js` | skipped (dist absent) | skipped (dist absent) | the closure claim proved by A6 |
| the §408 sweep `vitest run tests/lint tests/build tests/ops`, mutexed and quiet | (classified against the b10ed1a1 baseproof, read-only) | see §10.1 | — |

### §10.1 The §408 sweep, classified

`sh scripts/gate-mutex.sh --run -- npx vitest run --reporter=verbose tests/lint tests/build
tests/ops` with every member file in place (before the two cures), `/tmp/uc0/sweep.log`,
**SWEEP_TRUE_EXIT=1**, 131 s, 0 timeouts: **6 failed files / 9 failed tests / 2,110 passed /
114 skipped (190 files)**. Every red attributed:

| failing file | arms | verdict |
|---|---|---|
| `tests/lint/clampPrimitiveBaseline.test.js` | 1 | PRE-EXISTING, banked (brief; the b10ed1a1 baseproof reads the same arm red — `/tmp/uc0/baseproof-known.log`) |
| `tests/lint/warCostKindPools.walker.test.js` | 3 | PRE-EXISTING, banked (same) |
| `tests/lint/warRulingKindPools.walker.test.js` | 1 | PRE-EXISTING, banked (same) |
| `tests/lint/sovereigntyLightingContract.walker.test.js` | 1 | the ONE NAMED interior red (`expected 2500 to be 2499`; census note) |
| `tests/lint/seedLoopTotality.walker.test.js` | 1 | MINE — an authoring ratchet at ceiling 0 over the lane's NEW test file ("3 bare seed loop(s) at line(s) 134, 207, 225"); the header regex matches `SEED_CLASSES` / `sanitationRungSeeds` spellings; CURED by totality rewrites (§4 row 21) — the anchor walker's class, not a member-caused red |
| `tests/lint/ruinFilterRoster.walker.test.js` | 2 | **MINE — the ONE member-caused red.** Verbatim: "src/domain/undercity/strataExistence.js: reads the raw .institutions roster but neither routes through the ruin filter nor is exempted. If it aggregates institutions as LIVE providers, route it through liveInstitutions()/isLiveInstitution() … If it is ruin-agnostic (display/mutation/name-lookup/existence/…), add it to RUIN_AGNOSTIC_EXEMPT in this file with a reason." and `expected 90 to be 89` on the exact reader count. **DISPOSITIONED** (J-TEUC0-8): the gate is an EXISTENCE GATE and is ruin-agnostic BY DOCTRINE (§311.3 "dug is forever"; §311.8.2(b) "ABANDONED not erased") — the exempt row lands in the walker's existence-gate section with that reason, and the count is re-measured 89 → 90 from the arm's own message. Re-proved 12/12 (`/tmp/uc0/reprove-1.log`). The alternative (route through `liveInstitutions()`) was REFUSED: it would make a ruined church's crypt vanish from the sheet — the opposite of the doctrine — and is recorded as the reversal |

No load stray occurred (0 timeouts; every other file green at 2,110 passed). The sweep ran
BEFORE the two cures; the cured set was re-proved mutexed (`reprove-1.log`, 51/51) and the
whole sweep is re-run at the committed tip in §10.3.

### §10.3 The re-proof at the committed tip

Member commit `e67aacf0` (hooks ON; `eslint --fix` changed nothing — every committed blob
equals the proved blob by digest). Re-executed AT that tip, every vitest run mutexed:

| proof | result |
|---|---|
| the §408 sweep again (`/tmp/uc0/sweep2.log`, 101 s, 0 timeouts) | `4 failed files / 6 failed tests / 2,113 passed / 114 skipped` — EXACTLY the three banked pre-existing reds (`clampPrimitiveBaseline` 1 · `warCostKindPools.walker` 3 · `warRulingKindPools.walker` 1; the b10ed1a1 baseproof reds the same arms, `/tmp/uc0/baseproof-known.log`) plus the ONE named interior red (the census arm). `ruinFilterRoster` and `seedLoopTotality` GREEN |
| the baseproof control for the two cured walkers | `ruinFilterRoster.walker` 12/12 and `seedLoopTotality.walker` 9/9 GREEN at `b10ed1a1` — both reds were the member's by construction, and both are cured |
| mutants m1–m5 (+ m5b) against the COMMITTED blobs (`/tmp/uc0/tipmutants.log`, `tipmutants-m5b.log`) | m1 → A1 · m2 → A5 · m3 → A2 · m4 → A4/A5/A6/A8 · m5 → A6 · m5b → A6 (`expected 'The Drains' to be 'sewage_system'`); every restore digest-exact to leaf `94f146b2…` / test `863cd5f9…`; clean controls 8/8 ×2 |
| `tests/docs` (20 files) + `tests/scripts/implementationPackets.test.js` with the packet, manifest row and index row in place (`/tmp/uc0/docs-sweep.log`) | `enforcement-claims` RED on the SAME pre-existing six naked claims (`FABLE_VALIDATION_QUEUE.md` ×4, `GOLDEN_SHIFT_LEDGER.md`, `IN-0C.md`) and nothing else — this packet adds no claim (`CLAIM_RE` 0 hits) |
| `npm run validate:packets` | `valid: 150 packets (1 READY)` at DRAFT (`validate-draft.log`) → `valid: 150 packets (2 READY)` at READY (`validate-ready.log`; the other READY is MF-T2H, the landed member awaiting its flip) |

### §10.2 What this lane did NOT do

It did not run the full gate or `check:tail` (the landing terminal's act). It did not edit the
walker, its baseline, the exemption bank, the catalog or the sibling precedent. It moved no ref
and never touched the main checkout.
