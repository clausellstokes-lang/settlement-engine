# Town cartography / MF-T2E — `MassPartQ` / `SolidPartQ`: the typed functional-volume vocabulary (the quantized vertical truth D3a mints, in the exact shape the D1 legality predicate already admits)

- **Status:** DRAFT
- **Packet version:** 1
- **Verified base:** `claude/composite-r4` at `5d18b4a0c5515baa795c6fc763258a17d607d1ed`
  (the MF-T2D landing). Every preflight row below was re-executed at THIS base by the implementing
  lane; nothing is inherited from the compile, from the capsule, or from a sibling. ⚠ The compile
  was pinned to `3ac279db` and every figure it carried was marked `STOP: RE-DERIVE AT BASE`; each
  re-derivation is recorded in §4 with its instrument, and the census shear T2D's landing caused
  is named rather than absorbed.
- **Depends on:** `MF-T2A` (the single-declaration law — LANDED at `37fb6916`) and `MF-T2B` (the
  ABI wall, `requireCanonicalInt`'s threaded bounds, `deepFreezeCanonical`,
  `COORDINATE_ABI_VERSION` — LANDED at `cdfe5a96`). **No symbol of MF-T2C or MF-T2D is consumed** —
  those bind this member only through train order and the census-walker staged promotion (§7).
  MF-T2F is this member's named future READER, not a dependency.
- **Family preamble:** `docs/implementation/preambles/MF-PREAMBLE.md`, SHA-256
  `0706aad6a84e4f74ec47602e9e3222fc398c4c8864bb38c79ac480c2a11db4ed` — **recomputed at this base by
  the implementing lane** and matching the compile's value, so no third re-stamp occurred. Its §P1
  refutations, §P2 hazard dispositions, §P3 anchor preflight, §P3b, §P4 registration template, §P5
  census law, §P6 mutant hygiene, §P7 STOP set and §P8 capsule law bind this packet and are not
  restated.
- **Collision group:** `d3a-port` — this member reserves ONE shared D3a change path,
  `tests/lint/sovereigntyLightingContract.walker.test.js`. ⭐ It deliberately reserves NO path on
  `src/domain/townMap/fabric/index.js` and NO path on any existing production file (§7). Staged
  promotion: MF-T2D reached LANDED before this packet was promoted, and the manifest was measured
  to hold **no non-terminal packet at all** at this base, so the path is uncontended.
- **Commit authority:** the executing lane commits on its own detached ref; the chair moves the
  branch.

> **`censusAuthorization`:** this packet moves the test census by two credited files, seven titles
> and two suite titles (§9, measured). Its authorizing decisions are **ODQ §312** (the D3a port
> dispatch whose §312.1 roster names the §287.16 massing foundations as a D3a track), **ODQ §344.1**
> (the chair's spawn of the compile) and **ODQ §348** (the chair's review, which ratified member 5
> as drafted and TIGHTENED the caps), under the wave charter at **§310.4** and §299.4's
> binding-forward rule. The family's stamp is **GRANTED** at ODQ §312.2b, so `town-cartography`
> sits in the eight-member engine-train column and this member continues the OPEN train
> (A → B → C → D → **E**) rather than opening a new one.

> ⛔ **SOURCE PROVENANCE — this is a DESIGN-IMPLEMENTATION member, not a port-by-hash member.**
> `MassPartQ`/`SolidPartQ` have NO sandbox source module: the sealed sandbox's own
> `coordinateAbi.js` and `solidLegality.js` each state *"§7's `MassPartQ`/`SolidPartQ` are
> design-only (SPEC §10.16's status override)."* The sources this member implements from:
>
> | source | citation | what it supplies |
> |---|---|---|
> | `map-corpus/docs/GENERATION-SPEC.md` §7.1.2 · §10.5 · §10.16 | the blob the landed preamble cites — see §4 row 1 for the executed hash finding | the `FunctionalVolume`, `MorphologyRole` and unknown-reason vocabularies (closed, verbatim); the no-inference law; the support-relative vertical design |
> | sealed sandbox `fabric/solidLegality.js` (MF-D1) | SHA-256 `b2f59501f1902cd74b470238996c5bd875a84a8934aa8124517d2159bdb7f645`, **re-verified at this base** from `refs/preserve/map-sandbox-w3f-sealed` blob `0c46df0729b3da609c4fa1c09e67520dd1a4ecdf` | ⭐ the vertical rule D1 declared IN ADVANCE so D3a inherits it: half-open `[baseQ, topQ)` on a NAMED support surface, and the exact `{kind:'INTERVAL', baseQ, topQ}` discriminant its `solidOverlap` VOLUME arm reads — **re-executed at this base, §4.2 P4** |
> | ODQ §287.16 / §287.5 | the D3a charter (*"explicit-input support-relative solids, typed functional volumes…"*) and §287.5's geometry repair | the chartering text |

---

## 1. Reconciled authority

1. **ODQ §287.16, the chartering text, verbatim:** *"D3a may land explicit-input support-relative
   solids, typed functional volumes, component slots, architectural-history operations and
   deterministic fixtures while leaving live seeds byte-identical."* This member is the **typed
   functional volumes** item, and ONLY that item: component slots are MF-T2K, vegetation MF-T2L,
   operations MF-T2M (⛔ blocked behind MF-T2S).
2. **SPEC §10.16(3), the D1 row:** *"The volume law's attachment vocabulary needs
   `MassPartQ`/`SolidPartQ`, which D3a mints — the typed contract lands here, the third dimension
   there."* The typed contract landed with D1's predicate; this member is "there".
3. **The D1 vertical rule, inherited rather than re-minted.** ⭐⭐ **RE-EXECUTED AT THIS BASE
   (§4.2 P4):** the D1 `solidOverlap` driven read-only on records built by a prototype of §6
   returned `VOLUME {sharedAreaQ:100000000, sharedHeightQ:0, sharedVolumeQ:0,
   verdict:'DISJOINT_OR_ABUTTING'}` on the stacked `[0,5000) × [5000,9000)` pair — the half-open
   law as behavior, not prose — plus `REFUSED/DIFFERENT_SUPPORT_SURFACE` across supports and
   `PLANAR_ONLY` against a plan-era `ABSENT` vertical. The discriminant this member publishes is
   admitted by the predicate MF-T2F ports, verbatim, with zero adaptation.
4. **Preamble §P1 R-MF-2 — BigInt at the one arithmetic decision.** ⭐⭐ **RE-EXECUTED AT THIS BASE
   (§4.2 P1):** on the triple `a=[0,0] b=[m−1,m] c=[m,m+1]`, `m = 1286630001`, the Number shoelace
   reads `2A = 0` and the landed float `area()`/`absArea()` read `0` — while BigInt reads
   `2A = −1`. A Number-based check would REFUSE that valid sliver as degenerate. The check is
   BigInt, internal, with the reason and these figures in the file.
5. **Preamble §P2.7 — the height door.** `heightQ()` throws and no height is published in the plan
   era; *"the member that opens that door does so deliberately, in its own packet body."* **This
   packet body is that deliberate act, for the CONTRACT only:** it mints the vertical-interval
   record vocabulary with explicit integer quanta validated through the landed wall. It does NOT
   edit `heightQ()` (re-verified still throwing at this base, §4.2 P2), does NOT convert world
   units, and does NOT publish height into any artifact.
6. **ODQ §303.5 record-shapes-in:** the record family coheres with the landed codex explicit-mass
   shapes by convention — the landed validators, `deepFreezeCanonical`, typed refusals naming
   their label — without editing any of them.
7. **ODQ §310.3(9) / preamble §P2.5:** no tuning constant of any kind. The vocabularies are closed
   SPEC-verbatim lists pinned by equality; no numeric constant beyond the ABI wall is read, and
   none is minted.
8. **Live code decides.** Every load-bearing fixture was EXECUTED at this base before any pin was
   written (preamble §P2.9): the landed validators' refusal shapes, the canonical-id grammar, the
   prototype constructors, the R-MF-2 sliver, and the D1 predicate on the published discriminant.
   Captures in §4.2; log `laneTET2E-probe/driver.log`, `PROBE_TRUE_EXIT=0`.

**Resolved contradictions:**

- *The plan's MF-T2E row says "one new persisted record family"* — vs the landed MF-T2D precedent
  (J-TCT2D-2, accepted at ODQ §334.4). **RESOLUTION, ratified at §348: the records are VALIDATED
  IN-MEMORY VOCABULARY — frozen, constructor-built, and UNSTAMPED. The persisted-family budget row
  is 0.** The kind/version stamp and every persistence seam arrive with the member that first
  persists a massing phase, and that member owns the mint.
- *The SPEC's full `MassPartQ`/`SolidPartQ` interfaces carry roofs, materials, floor programs,
  origins, closed shells, upper envelopes, provenance and content hashes* — vs the budget and
  §290.4's fence. **RESOLUTION: this member mints the D3a-CORE slice** — the three closed
  vocabularies, the knowledge axis, the half-open vertical interval, and the two record
  constructors in the shape the D1 predicate admits. Everything else is named OUT in §2 with its
  owning member.
- *`exactGeometry.area()` is the tree's area spelling* — vs R-MF-2 at ABI scale. **RESOLUTION: the
  degeneracy check is an INTERNAL BigInt sign-of-area, not exported**, with the reason in the file.
  Extending `exactGeometry.js` would edit an existing production leaf for one internal predicate;
  refused, recorded.
- *SPEC §10.5's `SupportSurfaceRef` is a discriminated five-branch owner-resolved reference* — vs
  this member's budget and the absence of any registry to resolve against. **RESOLUTION:
  `supportSurfaceId` is a canonical id string** — the D1 predicate's own shape (it compares ids and
  refuses across them; executed §4.2 P4). The typed machinery is MF-T2K's subject, and the
  id-string field is upward-compatible: K types the reference, it does not rename the field.
- *SPEC §7.1.2's `MassingKnowledge<T>` carries a REQUIRED `provenanceRef` and an optional
  `coverageRef`.* **RESOLUTION: neither is minted** — no `ProvenanceRef`/`EvidenceCoverageRef`
  substrate exists in this era, and an unvalidatable required field is worse than an absent one
  (the landed ABI record's own "named ABSENT rather than faked" posture). Recorded as a named
  slice boundary in the leaf's docblock, never a silent omission.

The implementer does not read other documents to reinterpret this packet.

## 2. Outcome

**Observable result:** the app-side fabric gains the typed functional-volume vocabulary and the
quantized vertical truth §287.16 charters: closed SPEC-verbatim vocabularies for functional volume
and morphology role, a total KNOWN/UNKNOWN knowledge axis that cannot carry a value while unknown,
a half-open `[baseQ, topQ)` vertical interval on a named support surface refused through the landed
ABI wall, and frozen `SolidPartQ`/`MassPartQ` records whose discriminant the D1 legality predicate
(MF-T2F's port source) admits verbatim. Nothing calls it in production; live seeds stay
byte-identical; every existing digest, pin and test result stays exactly where it is.

**The IN/OUT boundary, named affirmatively:**

| capability | verdict | why |
|---|---|---|
| `FUNCTIONAL_VOLUME_KINDS` (6), `MORPHOLOGY_ROLE_KINDS` (17), `MASSING_UNKNOWN_REASONS` (4) — closed, frozen, SPEC-verbatim | **IN** | the typed vocabulary §287.16 charters; pinned by deep equality (§9 A5) |
| the knowledge axis: `KNOWN{value}` / `UNKNOWN{reason}`, total, no value while unknown | **IN** | the SPEC's no-inference law made structural — an UNKNOWN cannot feed a value-conditioned resolver because it HAS no value |
| `verticalIntervalQ(baseQ, topQ)` — half-open, strictly positive measure, integers through the landed wall | **IN** | the D1-declared rule inherited, not re-minted; executed §4.2 P3/P4 |
| `solidPartQ` — `{partId, supportSurfaceId, footprint, vertical}` + `abiVersion` | **IN** | the exact admission shape of the D1 predicate; executed §4.2 P4 |
| `massPartQ` — `{partId, parentBodyId, morphologyRole, functionalVolume, solid}` with `partId === solid.partId` | **IN** | the part/solid identity law (SPEC §10.5) |
| BigInt footprint-degeneracy check (internal) | **IN** | R-MF-2; §1.4's executed divergence |
| any overlap, interval-intersection or legality predicate | **OUT — forbidden** | MF-T2F ports the ONE predicate home; a second spelling here is the §287.12 defect |
| `SupportSurfaceRef` typing, component/material slots | **OUT** | MF-T2K |
| vegetation trunk/canopy records | **OUT** | MF-T2L |
| architectural-history operations | **OUT** | MF-T2M — ⛔ blocked behind MF-T2S |
| roofs, floor programs, functional PROGRAM axis, origins, closed shells, upper envelopes, provenance/content hashes | **OUT** | arrive with K/L/M and the first persisting member |
| `artifactKind`/`schemaVersion` stamps, any persistence seam | **OUT** | resolved contradiction 1; the first persisting member owns the mint |
| `heightQ()` motion, world-unit conversion, height publication | **OUT** | §1.5; the door stays deliberately un-opened beyond the contract |
| barrel export, any production consumer, any wiring | **OUT** | lands DORMANT; the acceptance battery is the only caller |

**Definition of done:** `massPart.js` exports the eight §6 symbols; the acceptance matrix passes
with every refusal typed and every pin executed from base captures; the census is re-recorded;
every digest pin and every fabric-touching test result is unchanged.

**Explicit non-goals:**

- ⛔ **No existing production file moves.** Not the barrel, not `exactGeometry.js`, not
  `foundation.js`, not `coordinateAbi.js`, not any codex massing module. The MF-T2A sweep-plant
  anchor line and the `CURRENT_MAP_TRADITION_ID` non-import are preserved BY CONSTRUCTION — this
  member does not open any host that carries them — and both were verified by grep at this base
  before and after the edit (§12).
- ⛔ **No second predicate spelling.** No overlap, no interval intersection, no shared-volume
  arithmetic.
- ⛔ **No fixture-era vocabulary semantics.** The lists are identifiers with membership law only.
- ⛔ **No dependency bump, no `FABRIC_COORDINATE_ABI`/version-string motion, no y-flip, no rung,
  no tuning constant.**

## 3. Hard scope budget

Caps TIGHTENED by the chair at ODQ §348 (leaf ≤160 effective, packet ≤180) and treated as hard.

| Limit | Packet budget | Measured | Standard |
|---|---:|---:|---:|
| Behavior families | `1` | 1 | 1 |
| New persisted record families | `0` — in-memory vocabulary | 0 | ≤1 |
| Named state writers | `0` | 0 | ≤1 |
| Feature flags | `0` | 0 | ≤1 |
| User-facing surfaces | `0` | 0 | ≤1 |
| Direct production consumers | `0` | 0 | ≤2 |
| New logic-bearing production leaves | `1` — `fabric/massPart.js` | 1 | ≤2 |
| Existing logic-bearing production files modified | `0` | 0 | ≤3 |
| Additional registration-only files | `0` — deliberately no barrel edit | 0 | ≤3 |
| Handwritten files total | `5` | 5 | ≤12 |
| New/changed effective production lines | **≤ `180`** | **118** | ≤400 |
| Effective lines per new leaf | **≤ `160`** | **118**, measured with eslint's own `Linter` | ≤250 |
| Delta in a shared/hot file | `0` — no manifest path is on the hot-file list | 0 | ≤15 |
| Acceptance cases | `7` | 7 | ≤8 |

Overrides approved before dispatch: `NONE`. No split member was seeded.

## 4. Sealed dispatch and preflight — every row RE-EXECUTED at this base

| # | Check | Result at `5d18b4a0` |
|---|---|---|
| 0 | `shasum -a 256 docs/implementation/preambles/MF-PREAMBLE.md` | `0706aad6…1db4ed` — **MATCH**; no third re-stamp |
| 1 | source re-hash | the preserved D1 `solidLegality.js` re-hashed to `b2f59501…b645` — **MATCH**. ⚠ **The spec-blob row diverged and was WORKED, not waved through** — see the finding below |
| 2 | name-collision scan over `src tests`, grep's OWN exit captured | `GREP_EXIT=1`, zero hits — the eight arriving names collide with nothing |
| 3 | `npx vitest run tests/lint/townMapFabricSingleDeclaration.walker.test.js` | 5 passed, `TRUE_EXIT=0`. Its guard-the-guard floors are `≥18` files and `≥94` names, so an arriving leaf can only strengthen them — **there is no exact count pin this member moves** |
| 4 | the fabric-touching battery, counted BEFORE the first edit | 57 files / 387 tests, all passed, `TRUE_EXIT=0` (§10's untouched-pin control) |
| 5 | the census tuple, by placeholder-and-convict | **`2492 / 364 / 2128 / 20677 / 5780`** — §9 |
| 6 | `npm run typecheck:ratchet` and `npm run typecheck:domain:strict` | `173 errors, ceiling 173` and `1134 errors, ceiling 1134` — **both sit exactly AT their ceilings, so this member's allowance is ZERO new type errors in either configuration** |
| 7 | §P2.9 unedited-substrate captures | §4.2, all reproduced |

⚠ **THE SPEC-BLOB FINDING (preflight row 1), recorded because it is a live trap for every later
member of this family.** The packet's required spec hash is `aa613cb8…1698`. Resolving the blob
through git gives `7dcd70ab…` — at the current ledger head AND at the head the compile itself read,
so the committed object never moved. Hashing the ledger worktree's **working file** gives
`aa613cb8…1698` exactly. The cited anchor is therefore an **uncommitted working-tree state**, not a
committed object: the compile hashed the working file, and that file is dirty relative to its
branch. Proved harmless for THIS member rather than assumed — the committed blob was extracted and
diffed against the working file, and the entire 32-line difference is one `⟦FOLD §303⟧` progress
block plus one status-table row, both about MF-W3F. **§7.1.2, §10.5 and §10.16 are byte-identical
under either reading**, so every vocabulary pinned below is the same text. Carried to the chair as
RAISED-A (§13): the anchor is not reproducible from git history by a later checkout.

### 4.2 · The captures this packet's pins are written FROM (all re-executed at THIS base)

Sandbox modules extracted fresh from `refs/preserve/map-sandbox-w3f-sealed` into the lane
scratchpad — never into the app or test tree. `solidLegality.js` hashed `b2f59501…b645` (the
packet's required value) and the sealed `coordinateAbi.js` hashed `123f3c17…c8404`, which is
exactly the port-source hash the LANDED `coordinateAbi.js` docblock cites — a cross-verification
the compile did not have.

```
P1  R-MF-2 at the ring-area decision      -> m=1286630001; tri a=[0,0] b=[m-1,m] c=[m,m+1]:
                                             Number 2A = 0 · BigInt 2A = -1 · landed float
                                             area()/absArea() = 0; ordinary-triangle control
                                             agrees in both spellings (12000000); NEW ARM: a
                                             genuinely collinear ring reads 0 in BOTH spellings
P2  landed validator refusal shapes       -> non-integer / past-wall: TypeError '<label> must be
                                             an integer in -9007199254..9007199254' · empty id:
                                             '<label> must be a canonical id' · non-record:
                                             '<label> must be an object' · negative-in-wall
                                             coordinate ACCEPTED · heightQ() STILL THROWS
P2b the canonical-id grammar, measured    -> /^[a-z0-9][a-z0-9:._-]{0,95}$/ — lowercase only;
                                             'support:wallTop:w9' REFUSED, 'support:wall-top:w9'
                                             admitted; every fixture is spelled to the grammar
P3  prototype constructors                -> records frozen (record, footprint, each point);
                                             vertical {kind:'INTERVAL',baseQ,topQ}; abiVersion 1;
                                             replay JSON-identical; refusals typed on [5000,5000)
                                             empty, [9000,5000) inverted, float baseQ; the
                                             wall-scale sliver CONSTRUCTS while absArea() reads 0
P4  the D1 predicate on the discriminant  -> stacked [0,5000)x[5000,9000), same support, same
    (sealed solidOverlap, read-only)         footprint: VOLUME sharedAreaQ=100000000
                                             sharedHeightQ=0 sharedVolumeQ=0 DISJOINT_OR_ABUTTING
                                             · overlapping: VOLUME sharedVolumeQ=75000000000
                                             OVERLAPPING · different support: REFUSED/
                                             DIFFERENT_SUPPORT_SURFACE · ABSENT vs INTERVAL:
                                             PLANAR_ONLY · same id: REFUSED/SAME_SOLID_ID —
                                             adaptation by KEY, never position
```

⭐⭐ **P4 is the member's charter executed:** the record this packet publishes is admissible by the
D1 predicate VERBATIM — the half-open law, the support refusal and the plan-era interop are the
predicate's own behavior on these records, captured before any pin was written.

## 5. Verified tree contract

| Role | File | Symbol | Verified fact at `5d18b4a0` | Required use |
|---|---|---|---|---|
| The integer wall | `src/domain/townMap/fabric/foundation.js` | `requireCanonicalInt`, `requireCanonicalId`, `requireCanonicalRecord`, `deepFreezeCanonical` | wall defaults `±MAX_WORLD_UNITS` = `±9007199254`; id grammar `/^[a-z0-9][a-z0-9:._-]{0,95}$/`; refusal messages captured §4.2 P2 | every input refusal and every freeze; ⛔ no second validator spelling |
| The ABI version read | `src/domain/townMap/fabric/coordinateAbi.js` | `COORDINATE_ABI_VERSION` (=1), `MAX_WORLD_UNITS` | `heightQ()` THROWS and stays throwing (re-executed §4.2 P2) | `abiVersion` stamped on `solidPartQ` records — READ, never moved |
| ⛔ The geometry home NOT extended | `src/domain/townMap/fabric/exactGeometry.js` | `area`, `absArea` | float shoelace; reads 0 on the valid wall-scale sliver (§4.2 P1) | **NOT imported by the leaf.** The internal BigInt sign check exists precisely because this spelling cannot decide degeneracy at ABI scale; the docblock says so with the P1 figures |
| ⛔ The sweep-plant anchor | `src/domain/townMap/fabric/dcelEmbedding.js` | `export function derivePlanarDcelEmbedding(input) {` at column zero | present and byte-identical, grepped before and after the edit | not touched by this member |
| The future reader, named | sealed `solidLegality.js` → MF-T2F's port | `solidOverlap` VOLUME arm | reads exactly `{kind:'INTERVAL', baseQ, topQ}` + `supportSurfaceId` (§4.2 P4) | ⛔ NOT imported from the sandbox by any app or test file, ever |
| The record-shape family joined | `src/domain/townMap/fabric/shapes.js`, `building.js`, `massingRoster.js` | the codex explicit-mass shapes | landed dark; §303.5 record-shapes-in | ⛔ **forbidden files** — coherence by convention, zero bytes moved |
| The standing law | `tests/lint/townMapFabricSingleDeclaration.walker.test.js` | the fabric single-declaration law | green at base; floors are lower bounds, not exact counts | binds the eight arriving exports |
| Census | `tests/lint/sovereigntyLightingContract.walker.test.js` | `const CENSUS = Object.freeze({` | the five-figure tuple, SEQUENCED | re-record all five together with cause + authorization |
| Test precedent | `tests/domain/townMapCoordinateAbi.test.js` + `tests/property/townMapCoordinateAbiDeterminism.test.js` | MF-T2B's two-file proof shape | the family's domain-matrix + determinism-companion form | **copied** |
| Anchor helper | `tests/helpers/anchoredNegatives.js` | `expectAbsentWithAnchor` | the negative-assertion route | by name on the same line, per §P3 |

**Forbidden alternatives:** no edit to any existing production file; no export of any area, overlap,
intersection or legality helper; no `artifactKind`/`schemaVersion`/`lawVersion` mint; no barrel
edit; no `export *`; no import of the sealed sandbox tree from any app or test file; no
`test.each`, no `describe.runIf`, no loop-generated tests; no files outside the manifest.
⛔ **Name reservation:** this member must NOT declare `PLAN_ERA_VERTICAL`, `OVERLAP_FLOOR_Q`,
`planEraSolid`, `solidOverlap`, `intervalOverlapVerdict`, `footprintIsAnswerable` or
`dualRunLegality` — those are MF-T2F's arriving exports, and minting any of them here would red the
single-declaration walker at T2F's landing (the exact habitat MF-T2A closed).

## 6. Exact contracts — `src/domain/townMap/fabric/massPart.js`

```js
export const FUNCTIONAL_VOLUME_KINDS  // SPEC §7.1.2 FunctionalVolume, verbatim order (6)
export const MORPHOLOGY_ROLE_KINDS    // SPEC §7.1.2 MorphologyRole, verbatim order (17)
export const MASSING_UNKNOWN_REASONS  // SPEC §7.1.2 MassingUnknownReason, verbatim order (4)

export function knownMassingFact(value, vocabulary, label)
// -> Object.freeze({ status: 'KNOWN', value })
//    TypeError naming label when value is not a member of the (nonempty, frozen) vocabulary.

export function unknownMassingFact(reason, label)
// -> Object.freeze({ status: 'UNKNOWN', reason })
//    ⛔ The UNKNOWN arm carries NO value field — the no-inference law is structural.

export function verticalIntervalQ(baseQ, topQ)
// -> frozen { kind: 'INTERVAL', baseQ, topQ }; integers through requireCanonicalInt (negatives
//    legal — a keel sits below its support datum). ⛔ HALF-OPEN, STRICTLY POSITIVE MEASURE.

export function solidPartQ(input)
// input : { partId, supportSurfaceId, footprint: [[xQ, zQ]…], vertical: { baseQ, topQ } }
// -> frozen { partId, supportSurfaceId, footprint, vertical, abiVersion }
//    footprint: ≥3 points; refused when the BigInt-exact signed area is zero. ⛔ No
//    simplicity/self-crossing check: ring simplicity is the kernel's guarantee.

export function massPartQ(input)
// input : { partId, parentBodyId, morphologyRole, functionalVolume, solid }
// -> frozen { partId, parentBodyId, morphologyRole, functionalVolume, solid }
//    ⛔ THE IDENTITY LAW: input.solid.partId must equal input.partId; a mismatch names BOTH ids.
```

Internal, unexported: `ringAreaSign2x` (the BigInt decision) and `requireVocabularyMember`
(membership with the guard-the-guard arm first).

### Absence and refusal rules

- non-record input, missing/mis-grammared ids, non-integer or out-of-wall quanta, empty or
  inverted interval, degenerate footprint, non-member vocabulary value, non-member unknown reason,
  UNKNOWN-with-value, KNOWN-without-value, a third status shape, part/solid id mismatch: **typed
  `TypeError` through the landed validators or a message naming the offending label and law** —
  never `NaN`, never a silent skip, never a defaulted field.
- There is no empty-collection arm: a solid without a vertical interval is not constructible here
  (plan-era `ABSENT` verticals belong to MF-T2F), and a mass part without a solid is refused.

### Determinism

- Pure over arguments; integer and BigInt arithmetic only; no clock, randomness, locale ordering
  or iteration-order dependence; fields written in fixed literal order; no spread of caller input
  reaches a published record, and A7 proves it by mutating the caller's array after construction.
- Same input twice constructs JSON-identical records (§9 A7).

### Flag, dormancy, lifecycle, receipts

- Flag: `NONE`. Golden posture: **unchanged and PROVEN unchanged** — the untouched-pin control
  (§10) plus the post-build dormancy fence and forensic zoom.
- Lifecycle: pure construction, in-memory; ⛔ nothing persisted, so no reload/regen/undo/migration
  seam exists to declare. Public veil: n/a — no runtime surface.
- Receipts/privacy: closed kinds `NONE`; DM-only fields `NONE`.
- Alignment: `DECLARED EMPTY: a record vocabulary has no alignment surface.`
- Edit story: `ENGINE-ONLY: explicit-input vocabulary; nothing is DM-editable and nothing is proposed.`

## 7. Exact change manifest

| Action | File | Symbol/region | Max delta | Coding instruction |
|---|---|---|---|---|
| `CREATE` | `src/domain/townMap/fabric/massPart.js` | the eight §6 exports + internal `ringAreaSign2x`, `requireVocabularyMember` | **≤160 eff** (measured **118**) | The §6 contract. Imports ONLY the four foundation validators and `COORDINATE_ABI_VERSION`. ⛔ The BigInt check is internal; the docblock carries the R-MF-2 reason with the P1 figures and the §10.16(3) charter line |
| `CREATE` | `tests/domain/townMapMassPart.test.js` | `describe('MF-T2E mass-part vocabulary')`, A1–A6 | `n/a` | ONE literal `describe`, six straight-line `test()` calls, string-literal titles; fixtures built inside named tests; id fixtures fit the §4.2 P2b grammar |
| `CREATE` | `tests/property/townMapMassPartDeterminism.test.js` | `describe('MF-T2E mass-part determinism')`, A7 | `n/a` | The replay + purity companion, matching MF-T2B's property-file shape |
| `TEST` | `tests/lint/sovereigntyLightingContract.walker.test.js` | the `CENSUS` object | `+13/−1` | ONE re-record block naming its cause and the `censusAuthorization` refs; all five figures re-derived together at the base by placeholder-and-convict |
| `DOC` | `docs/implementation/packets/town-cartography/MF-T2E.md` | this packet | `n/a` | The packet, its status transitions, and its §12 landing record |

Generated artifacts: `NONE`. No other file may be edited. ⚠ The two acceptance files are `CREATE`
rows, not `TEST` rows (the MF-T2B/T2C/T2D measured precedent: the validator asserts a TEST path
exists at EVERY status, and existence for a CREATE row is assertable only at LANDED). ⚠ No
`tests/lint/**` file is minted, so §P3b's mutation-coverage row does not attach. §P4's four-name
template is satisfied by its "reason none exists" arm: the leaf is directory-internal, the barrel
deliberately does not export it, and the registry tests that bind this member are the
single-declaration walker and the entry-closure fence. **Symbols the deliverable CREATES** join
`requiredSymbols` **at the flip to LANDED, never at READY**. `retiredSymbols`: **EMPTY**.

## 8. Ordered coding sequence

0. Dispatch and seal; run every §4 preflight. STOP on any mismatch.
1. **Capture the golden/dormancy evidence BEFORE the first edit:** the full fabric-touching test
   battery counts; the census tuple by conviction; and the §4.2 probes re-executed at the base.
2. Add the smallest failing focused test.
3. Implement `massPart.js` per §6 — vocabularies, knowledge axis, interval, then the two
   constructors.
4. — (no writer, no lifecycle seam)
5. — (no production consumer; the tests are the named reader until MF-T2F)
6. — (no registration; §7's "reason none exists" arm)
7. Focused verification (§10), including the anchor preflight and the untouched-pin control.
8. The removing-power sweep, the census re-record, the post-build dormancy discharge with forensic
   zoom, then the wave-end gate and the completion receipt (§12).

## 9. Acceptance matrix

Every negative carries a positive control in the same test. The **capture** column is behavior
executed at THIS base (§4.2).

| ID | Case | Required observation | Home |
|---|---|---|---|
| **A1** | Main behavior — a two-part body's records construct and wear the D1-admissible shape | positive control FIRST: both records construct; `vertical` deep-equals `{kind:'INTERVAL', baseQ, topQ}` exactly and its key list is exactly `baseQ/kind/topQ` (the discriminant MF-T2F's predicate reads); frozen deep on record, footprint, each point and interval; `abiVersion` read from the LIVE import, never transcribed; the two intervals share the boundary quantum 5000 and BOTH construct (half-open adjacency is legal by design) | domain |
| **A2** | Refusals — typed, never floats, never silent | positive control first (the unmodified fixture constructs, so each refusal is attributable to the one field it changes); float `baseQ`; out-of-wall `topQ`; `[5000,5000)` empty and `[9000,5000)` inverted both naming the half-open law; uppercase support id refused by the measured grammar while its lowercase respelling is ADMITTED; degenerate footprint; a two-point ring; non-record input; missing `parentBodyId`. A negative in-wall interval is accepted — a keel below its datum is legal ground | domain |
| **A3** | The knowledge axis — total, no value while unknown | KNOWN publishes `{status:'KNOWN', value}`; non-member value refused; UNKNOWN publishes `{status:'UNKNOWN', reason}` with **no `value` property**, asserted through `expectAbsentWithAnchor` with `reason` as the anchor; non-member reason refused; KNOWN with no value refused; an UNKNOWN input smuggling a value refused naming the no-inference law; a third status shape refused rather than defaulted | domain |
| **A4** | The identity law | the mismatch throws naming BOTH ids and the law; the agreeing pair constructs with `part.solid.partId === part.partId` | domain |
| **A5** | Vocabulary closure — the ported-value pins with the guard-the-guard opener | opener: each list is NONEMPTY and frozen; the three lists deep-equal the SPEC values in order (6/17/4); each constructor refuses a non-member of ITS list; and the guard itself convicts an EMPTY vocabulary rather than admitting everything | domain |
| **A6** | ⭐⭐ The named historical regression — R-MF-2 at the ring-area decision | the divergence witnessed IN-TEST: the test's own Number shoelace reads `0` on the sliver and its own BigInt shoelace reads `-1`; ARM 1 — `solidPartQ` with that footprint CONSTRUCTS; ARM 2 — a genuinely collinear ring (BigInt `2A = 0` exactly, asserted) is REFUSED as degenerate. Both arms in one test, so deleting the check outright cannot pass by satisfying only ARM 1 | domain |
| **A7** | Determinism / replay + purity | ten published behaviours replay JSON-identically; pinned answers sit beside the replay so a leaf that replays a WRONG shape cannot pass by being consistently wrong; two constructions are not the same object (no shared singleton); mutating the caller's footprint after construction does not reach the record; the source scan finds no clock/randomness/locale identifier in `massPart.js`, with a planted occurrence asserted SEEN first | property |

**Anchor law (§P3):** A3 and A7 assert absences and each routes through
`tests/helpers/anchoredNegatives.js` by name on the same line, or carries an inline
`anchored:` reason on the assertion line or the line immediately above.

**Census motion — MEASURED, not predicted:**

```
BASE  (placeholder-and-convict, all five, at 27c250f9):
  files: 2492, parked: 364, credited: 2128, titles: 20677, suiteTitles: 5780
DELTA: +2 files · +0 parked · +2 credited · +7 titles (6 domain + 1 property) · +2 suites
AFTER (re-convicted after the edit):
  files: 2494, parked: 364, credited: 2130, titles: 20684, suiteTitles: 5782
```

⛔ `parked` stays at its recorded value. One literal `describe` per file, straight-line `test()`
calls, string-literal titles; loops live INSIDE named tests, so both files are CREDITED rather than
parked. ⚠ **NAMED INTERIOR RED:** the census arm is an exact equality against a recorded constant,
so this member reds at its own implementation commit until the tuple is re-recorded.

## 10. Verification commands

```sh
npx vitest run tests/lint/negativeAssertionAnchor.walker.test.js ; echo TRUE_EXIT=$?

npx eslint src/domain/townMap/fabric/massPart.js \
           tests/domain/townMapMassPart.test.js \
           tests/property/townMapMassPartDeterminism.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict

sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/domain/townMapMassPart.test.js \
  tests/property/townMapMassPartDeterminism.test.js \
  tests/lint/townMapFabricSingleDeclaration.walker.test.js \
  tests/lint/sovereigntyLightingContract.walker.test.js

# ⭐ THE UNTOUCHED-PIN CONTROL — 57 fabric-touching files (the digest-pin and golden suites
# included), the SAME explicit denominator at both ends, run before the first edit and after the
# last. This member adds a leaf; it must move NOTHING that already exists.

# ⭐ THE REMOVING-POWER SWEEP — seven mutants, planted in a scratch copy, each convicting a NAMED
# branch, each restored by byte copy verified with cmp (never the git checkout -- family).

# ⛔ Dormancy: POST-BUILD ONLY. Build first, then:
npm run build
VERIFY_DIST=1 npx vitest run tests/build/townMapLazy.test.js ; echo TRUE_EXIT=$?
# …and the forensic zoom over a NAMED denominator, with scanner controls asserted present.

npm run validate:packets
npm run check:tail ; echo TRUE_EXIT=$?
```

⛔⛔ **NEVER wrap `npm run check*` in `gate-mutex.sh --run`** — `test:ratchet` re-acquires and
self-deadlocks; **exit 3 is the mutex giving up, not a red.** ⚠ Trust no exit status you did not
capture. ⚠ Report both typecheck configurations by name and window.

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and preamble §P7:

- any digest pin, exact-key pin or pinned roster in the fabric-touching suites moves, or any such
  test file changes its result between the §8-step-1 and §10 captures;
- any existing production file would need editing — including `foundation.js`, `coordinateAbi.js`,
  `exactGeometry.js`, the codex massing modules and the barrel;
- the `export function derivePlanarDcelEmbedding(input) {` line changes in ANY byte, or
  `CURRENT_MAP_TRADITION_ID` becomes imported or declared in any file this member creates;
- the family preamble's hash or the preserved `solidLegality.js` hash does not match a fresh
  computation at the base;
- any of MF-T2F's reserved names (§5) would be declared or exported here;
- any overlap, interval-intersection, shared-volume or legality computation would be exported;
- a vocabulary value would differ in spelling, count or order from the SPEC's §7.1.2 lists;
- `heightQ()` would be edited, or any world-unit-to-quantum conversion would enter the leaf;
- an `artifactKind`, `schemaVersion` or law-version string would be minted;
- a published record would carry a non-integer, an out-of-wall quantum, or an empty/inverted
  interval;
- the single-declaration walker reds, or an arriving export name collides with a live one;
- the census moves by other than the §9 delta against the tuple captured at §8 step 1, or `parked`
  moves at all; a delta smaller than the titles added is attributed by reverting one test file at
  a time, never accepted as arithmetic;
- `tests/build/townMapLazy.test.js` reds POST-BUILD, or the forensic zoom finds a member-unique
  literal in an entry chunk;
- another D3a member is simultaneously non-terminal on
  `tests/lint/sovereigntyLightingContract.walker.test.js`;
- any ratchet, baseline, budget or ceiling would need raising; any tuning constant of any kind
  would be touched.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into the
next wave.

## 12. Completion receipt

*(filled at the flip to LANDED)*

## 13. RAISED for the chair

| # | the call | this lane's recommendation |
|---|---|---|
| **A** | ⭐⭐ **NEW, found at this base.** The family's cited GENERATION-SPEC anchor `aa613cb8…1698` is an **uncommitted working-tree state** in the main worktree, not a committed git object (the committed blob is `7dcd70ab…` at every ledger commit checked). Every packet in this family cites it, and it is not reproducible from git history by any later checkout — it evaporates if the file is ever committed, reverted or stashed. Proved harmless for THIS member by diffing the two versions (the whole difference is an MF-W3F progress fold; §7.1.2/§10.5/§10.16 are byte-identical). | **Commit the spec file, or re-cite the family's packets to the committed blob.** Outside this member's manifest, so recorded rather than fixed. Until then, a preflight row that says "MATCH REQUIRED" against `aa613cb8…` can only be satisfied by hashing the working file, and a lane that resolves the blob through git will read a STOP that is not real. |
| **B** | **The §P2.7 height door.** This member mints the vertical-interval CONTRACT (explicit integer quanta through the landed wall) and declares that act deliberately in §1.5; `heightQ()` stays throwing (re-verified at this base); height's first PUBLICATION into any artifact stays owed. | Confirm the reading. Either reading leaves the executor's instructions identical. |
| **C** | **The T2F carry, inherited from the compile:** the D1 predicate's VOLUME arm computes `sharedVolumeQ = areaQ * h` in Number over a float `polygonIntersectionArea` — at ABI scale that product exceeds exact float range (the P4 fixture already yields 7.5e10 at toy scale). | **Carry as a NAMED note to MF-T2F's compile** — its BigInt-conversion decision, not this member's to fix or pin. |
| **D** | **The knowledge axis omits `provenanceRef`/`coverageRef`** (SPEC §7.1.2 makes the first REQUIRED). No such substrate exists in this era. | Confirm. Alternative — mint a placeholder ref — refused: a required field carrying a placeholder is a worse lie than a missing one, which is the landed ABI record's own recorded posture. |
