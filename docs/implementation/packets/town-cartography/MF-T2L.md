# MF-T2L — vegetation: the durable trunk/canopy mass and the deterministic nonpersistent instance field

- **Status:** READY
- **Packet version:** 1
- **Ruling:** implemented by lane TE-T2L against the chair ruling at ODQ §425, which took all five
  of the compile's questions as settled: both halves discharged as RECORD + DERIVATION in the one
  leaf; the `fieldKey` ACCEPTED through the wall this era, not derived; the caps ratified at leaf
  ≤230 / packet ≤240; the era-law stamp ships so the recipe member's cutover is a declared shift;
  and the ODQ §419.2 rename plus its severable title word-swap ride this member's landing act.
- **Family preamble:** `docs/implementation/preambles/MF-PREAMBLE.md`, SHA-256
  `0706aad6a84e4f74ec47602e9e3222fc398c4c8864bb38c79ac480c2a11db4ed` — recomputed by this lane at
  its own base and identical to the value MF-T2K recorded, so no re-stamp occurred in the stack
  window. Its §P1 refutations (R-MF-2 and R-MF-4/5 govern this member), §P2 hazard dispositions,
  §P3 anchor preflight, §P4 registration template, §P5 census law, §P6 mutant hygiene, §P7 STOP set
  and §P8 capsule law bind this packet and are not restated.
- **Verified base:** `claude/composite-r4` at `4d2d17f86f173686d142a97e3fd356d6b7be97e6`
  ⚠⚠ **READ THE NEXT LINES BEFORE TRUSTING THAT ROW.** The SHA is MF-T2K's **HOLDING (unlanded)**
  commit, itself stacked on MF-T2J's `f7ba314505703b96097b8aba568cc80e3ec30023`, MF-T2H's
  `05e7f9d56eec119102f540e316bd8e1061f81a92` and MF-T2Bf's landed commit, so this member is **FIVE
  DEEP**. The branch token names the branch of record this member is verified FOR and will land on;
  the row is **re-stamped at the landing slot** once MF-T2H, MF-T2J and MF-T2K land and this member
  is rebased. ⭐ The base was DERIVED at lane start rather than inherited:
  `git merge-base --is-ancestor` reports
  MF-T2H's `05e7f9d5` an ancestor of it and reports it an ancestor of NEITHER
  `claude/composite-r4` nor `review-fixes-2026-07-08`, so the five-deep stack is unrebased and
  unlanded and branches at `9bfae712`. ⚠ The stack is rebased at the landing slot; the census DELTA
  is what crosses, never the tuple.
- **Depends on:** MF-T2K (`supportSurface.js`, SHA-256 `9d0e162b…2ea7`, one commit), MF-T2E
  (`massPart.js`, `d52c19fa…e76f`, one commit) and MF-T2H (`fabricRng.js`, `4094ecb8…6e95a`, one
  commit). ⭐ This is the family's first STACKED member that consumes its predecessor's export by
  symbol rather than binding only through train order: every mass is keyed by a `supportSurfaceRef`
  record, re-derived through MF-T2K's own constructor.
- **Collision group:** `d3a-port`. ⛔⛔ Two shared paths are edited at this member's tip and NEITHER
  appears in its `changeManifest`, per the ODQ §417 standing law ratified at MF-T2J's RAISED-1a: the
  code edit is proved at the tip and the shared row is deferred verbatim to the landing act. They
  are `tests/lint/sovereigntyLightingContract.walker.test.js` (held by MF-T2H at READY) and
  `tests/lint/townMapStageManifest.walker.test.js` (held by MF-T2J at READY). The duplicate-path
  probe was EXECUTED at this base rather than trusted — §4 row 19 quotes the refusal. Every other
  path this member touches carries zero reservations at any status.

> **`censusAuthorization`:** this packet moves the test census by
> **`+1 file / +0 parked / +1 credited / +6 titles / +1 suiteTitle`** — the fixed single-behaviour
> shape `draft-D3A-PLAN.md` §4 assigns this member. Base tuple re-derived at this base:
> `2502 / 364 / 2138 / 20737 / 5790`; after tuple **`2503 / 364 / 2139 / 20743 / 5791`**, each
> figure read from its own arm's failure message in a sequenced walk (§6). Authorizing decisions:
> **ODQ §276**, **ODQ §304.4**, **ODQ §310.4**, **ODQ §421** and **ODQ §425**, under §299.4's
> binding-forward rule. The family's stamp is **GRANTED** at ODQ §312.2b.

> ⛔ **PORT SOURCE PROVENANCE (preamble §P1 R-MF-4) — A DESIGN-IMPLEMENTATION MEMBER, MEASURED WITH
> A LIVE INSTRUMENT.** The sealed W3 fabric holds NO vegetation machinery, and the zeros were proved
> against a firing control rather than believed:
>
> | grep over sealed tip `ee0db96d` (case-insensitive, `-l`) | scope | files |
> |---|---|---|
> | `VegetationMass\|CanopyVolume\|VegetationInstanceField\|phenolog` | `src/domain/townMap` | **0** |
> | `vegetat` | `src/domain/townMap` | **0** |
> | `species` | `src/domain/townMap` | **0** |
> | `trunk` (CONTROL — expected to fire) | `src/domain/townMap` | **3** — `substrate.js` / `waterMode.js` (river-trunk prose) and `groundDress.js` (tree-stipple presentation) |
>
> The sealed estate draws trees only as PRESENTATION, which is the volume's own two-contracts law
> (§10.6: land-use texture *"cannot mint population, density, species, phenology or a
> `VegetationInstanceField`"*) already lived by the sandbox. **There is no sandbox module to port;
> the sources are SPEC §10.6's contract and the landed substrate.** Every consulted source was
> re-hashed at this base before any edit and every value matched the compile's (§4 row 4).

## §1 · WHAT THIS MEMBER IS, AND WHAT IT REFUSES

### §1.1 One leaf, two halves

One dormant production leaf, `src/domain/townMap/fabric/vegetation.js`, carrying BOTH halves of ODQ
§287.5's vegetation sentence. The plan's law for this member is its spine (`draft-D3A-PLAN.md` §4):
*"§287.5 requires **both** halves: a durable selectable trunk/canopy mass **and** a separate
deterministic nonpersistent instance field. A member that lands only the durable mass has landed
half a contract and made the other half harder; both or neither."*

The governing design law is MF-T2E's era-slice posture, inherited whole: **an unvalidatable required
field is worse than an absent one.** SPEC §10.6's full contract resolves through registries this era
does not have, so this member mints the D3a-core slice and names every absent piece OUT with its
owning successor (§1.6). The field names are the SPEC's, so the registry era ADDS fields and renames
nothing.

### §1.2 THE MASS — trunk and canopy on ONE derived ground authority

SPEC §10.6: *"`VegetationMass` has no second support: all placement reads `trunk.support`… Support
truth rejects any consumer or serialized cache that gives trunk and canopy different ground
authority."* And §10.5's identity law: *"A `VegetationMass.trunk.partId` instead equals its
`canopyAnchor.trunkPartId` and is uniquely owned by that vegetation body."* Three structural
decisions carry those laws as impossibilities rather than as checks:

1. **ONE typed support in, one derived key threaded everywhere.** The constructor strips
   `surfaceId` from the caller's record, re-executes MF-T2K's `supportSurfaceRef` over the branch
   fields, and refuses if the re-derived key differs from the record's — naming BOTH keys. A
   hand-built alias wearing K's record shape cannot pass, because the derivation is K's own. The
   blocked seventh kind (`MAINTAINED_FREE_SPACE`) and the segment grammar arrive FREE from the same
   call and are not re-spelled here. Neither the trunk input nor the canopy input may carry a
   `supportSurfaceId` at all, and the refusal names the one-key law rather than merely the field.
2. **Trunk and canopy are landed `solidPartQ` volumes, constructed internally** — massPart's own
   one-construction-route idiom, so no half-validated solid can circulate. The canopy's era volume
   rides the `solid`-field idiom rather than minting `closedShell`/`SolidQ` (J-TCT2L-4, §1.6).
3. **The anchor's laws are derivation wherever the era can execute them.**
   `canopyAnchor.trunkPartId === trunk.partId` is refused naming both ids. The canopy volume's
   vertical base **is** the anchor's `baseHeightAboveSupportQ` — the canopy input carries `topQ`
   only — so §10.6's *"validated against the canopy shell"* is satisfied by construction on the one
   axis the era owns, and `verticalIntervalQ`'s strictly-positive-measure law then refuses a canopy
   closing below its own anchor, free, from the landed spelling. `canopy.ownerBodyId` is WRITTEN as
   `bodyId` and `kind` is WRITTEN as `'VEGETATION'`; a field with one writer cannot mismatch.

`phenologyClass` is validated against the closed four, SPEC-verbatim and in SPEC order.
`speciesClassId` is a canonical id through the landed wall, unresolved this era — the same posture
MF-T2K published for `materialId`.

### §1.3 THE FIELD — the region law the foundation already published

§10.6's materialisation law: *"The field may materialise only when that aggregate supplies a known
count or a known density plus deterministic rounding law and one known species class and one known
phenology class."* No aggregate, density law or rounding registry exists this era, so the era field
is the KNOWN-COUNT arm: `{fieldKey, supportRegion, instanceCount, speciesClassId, phenologyClass}`.

- `fieldKey` — a canonical id through the landed wall, **accepted this era, not derived** (ODQ §425
  ruling 2, on the MF-T2E precedent): the SPEC's `DerivationKey` derives from aggregate and recipe
  refs that do not exist, and a coordinate-derived stand-in would be determinism theatre that also
  embeds signed quanta into id grammar. The aggregate member owns the derivation flip, and because
  the field name is the SPEC's the migration ADDS nothing that renames.
- `supportRegion` — a canonical axis-aligned rectangle ring through the landed `canonicalRectBounds`,
  ⭐ whose own docblock is this era's region law, published before this member existed: *"The first
  tranche deliberately admits a canonical axis-aligned rectangle only. Broader planar-face support
  belongs to the later W3 corpus packet."* It also refuses zero measure, so a degenerate field
  cannot exist — inherited, not re-spelled.
- `instanceCount` — a canonical integer ≥ 0; a felled stand may hold a known zero, and every index
  against it is refused by name (§1.4).
- The SPEC's aggregate-conservation, subdivision, mixed-stand and `SINGLE_FIELD_EXACT_REGION` laws
  all bind field↔aggregate PAIRS; with the aggregate OUT they are named OUT with it, never
  half-minted.

### §1.4 THE INSTANCES — nonpersistence as a structural fact

SPEC §10.6: *"individual instances are regenerated from `(fieldKey, instanceIndex)`, carry no entity
id, are not individually persisted or selectable, and cannot be named by a domain receipt."*

- **Deterministic and parameter-free.** The basis is
  `` `${VEGETATION_FIELD_LAW_VERSION}:${fieldKey}:${instanceIndex}` ``, and each axis is the landed
  `hash32` of that basis with an axis suffix, folded by integer modulo:
  `xQ = minX + (hash32(basis + ':x') % widthQ)`. Containment in the half-open
  `[minX, maxX) × [minZ, maxZ)` rect holds BY CONSTRUCTION, not by test alone. ⭐ There is no
  spacing, density, jitter or shape constant anywhere in the derivation — zero free parameters —
  which is what keeps this member off THE PROMISE's owner-signature surface (§P2.5, checked by
  enumeration: the leaf declares no numeric constant at all).
- **R-MF-2, head-on.** The leaf multiplies NO quanta. Its only coordinate arithmetic is
  `minX + (uint32 % widthQ)`: `hash32` is a pure `xmur3` returning `h >>> 0`, so the modulo cannot
  go negative; both other operands are wall-validated integers and the sum is bounded by `maxX`.
  Every intermediate is a safe integer, and the leaf says so in its own docblock — the refutation's
  "says why in the file" duty discharged in the direction that applies. ⛔ `hashUnit`/`hashInt` are
  deliberately NOT used: both route through a float divide by 2³², and the integer-modulo spelling
  makes the question unaskable.
- **Nonpersistent structurally.** The instance record HAS no id field of any kind — no `bodyId`, no
  `entityId`, no `instanceId`, no `artifactId` — so it cannot be selected, persisted or named by a
  receipt even by a confused consumer. `instanceIndex` is refused unless it is a canonical integer
  in `[0, instanceCount)`, and a zero-count field refuses every index naming the emptiness.
- **The era law is VERSIONED so its successor is a declared act.**
  `VEGETATION_FIELD_LAW_VERSION = 'mf-vegfield-era-uniform-rect-v1'`, on the
  `FABRIC_FOUNDATION_LAW_VERSION` precedent. When the recipe registry arrives, real recipes
  supersede this law and instance positions MOVE — a declared shift priced in THAT member's packet.
  Naming it now is what makes it a declaration instead of a surprise (RAISED-4, ODQ §425 ruling 4).
  Until then the leaf is dormant vocabulary and no rendered surface consumes these positions.

### §1.5 THE CONSUMER SEAMS — executed, not narrated

- **L consumes K:** every mass is keyed by a `supportSurfaceRef` record re-derived through K's
  export. And K's DAG law consumes L's shape: A6 runs the landed `assertSupportAcyclicity` over a
  building part on a terrain face and a tree trunk on that part's `ROOF_FACE` — §10.5's own example
  of vegetation anchored to architecture — and pins the order supports-first.
- **L feeds F:** the landed `solidOverlap` runs over `solidPartQ` records built BY this member, with
  every magnitude pinned exact (§5, A6). ⛔ **The refusal STAYS a refusal**: this member mints no
  connection vocabulary and licenses nothing MF-T2F refuses (ODQ §419.1, inherited whole).

### §1.6 What it refuses, named affirmatively

No production file moves and no barrel row is added (`fabric/index.js` carries zero rows for all
five dormant leaves; this member continues the precedent). No `CanonicalVegetationAggregate`, no
EXPLICIT_CANON/RSLP authority arms, no population disposition, density law, rounding law,
subdivision/conservation/mixed-stand law or `SINGLE_FIELD_EXACT_REGION` enforcement. No recipe
registry, no derived `fieldKey`, no density arm. No `recipeId`/`lawVersion`/`effectiveAt`/
`artifactId`/`contentHash`/`provenanceRef` stamp on any record — the in-memory-vocabulary posture
MF-T2E ratified at ODQ §348. No `CanonicalVegetationPhase`, no `VegetationObservation`, no opacity
bands, no phenology MOTION. No promotion path and no operation vocabulary (§10.6 routes promotion
through a declared operation, and that seam sits behind ODQ §299.3c). No epoch fields. No canopy
shell machinery — no `closedShell`/`SolidQ`, no `receiverSurfacePatchIds`, no `bounds` cache, no
`offsetXYQ` (its shell-agreement law needs a ring reference point the era cannot define). No
polygonal region and no point-in-polygon geometry. No second spelling of anything landed. No new RNG
definition — the leaf matches the entropy census's definition regex zero times (§4 row 12). No
`heightQ()` motion, no y-flip, no arrangement-quantum rung, no tuning constant, no dependency bump,
and no new `tests/lint/**` file, so §P3b is priced at zero. No live seed byte moves and no declared
shift exists this era.

### §1.7 ⛔⛔ THE DEFERRED ROWS AND THE §419.2 RENAME

Both walker edits are MADE and PROVED at this member's tip; **neither path enters this
`changeManifest` until the reserving packets are terminal.** The landing act inserts, verbatim:

**Row A — the census re-record** (`tests/lint/sovereigntyLightingContract.walker.test.js`):

```json
{
  "_note": "THE CENSUS RE-RECORD, all five figures re-derived together with the cause stated: ONE NEW TEST FILE and nothing else. 2502/364/2138/20737/5790 -> 2503/364/2139/20743/5791 (tuples re-derived at the landing base per the carry law; the DELTA +1/0/+1/+6/+1 is what crosses a rebase). The walk was SEQUENCED and each figure read from its own arm's failure message; attribution BY ISOLATION, with the one file hidden convicting the base tuple exactly. parked holds at 364 because every title is a string literal in one describe. censusAuthorization: ODQ 276, ODQ 304.4, ODQ 310.4, ODQ 421, ODQ 425.",
  "action": "TEST",
  "path": "tests/lint/sovereigntyLightingContract.walker.test.js"
}
```

**Row B — the roster row PLUS the §419.2 rename** (`tests/lint/townMapStageManifest.walker.test.js`):

```json
{
  "_note": "ONE ROSTER ROW AND THE RULED RENAME, one edit: 'vegetation.js' appended in alphabetical position (after 'supportSurface.js') to the roster formerly named CODEX_SLICE_MODULES, and that constant renamed to UNASSIGNED_LANDED_MODULES per ODQ 419.2 ('the roster rename rides MF-T2L's landing act - one honest word over a third roster'). The why for the row, from packet section 1.8: the leaf is a D3a design-implementation mint measured against the sealed tree (zero sandbox vegetation modules), so nodeOfModule() rightly resolves it to no node and the record's 49-module lead is untouched. FOUNDATION_READERS is unmoved: the leaf imports foundation.js, massPart.js, supportSurface.js and fabricRng.js, none of which that roster tracks.",
  "action": "TEST",
  "path": "tests/lint/townMapStageManifest.walker.test.js"
}
```

**THE RENAME, AS EXECUTED.** The occurrence census was re-run at this base and confirms the
compile's figure: the identifier appears at exactly FOUR sites in the walker (the declaration, the
failure message, the accounting pin and the planted-arrival arm), all four swapped. Item 5 replaced
MF-T2K's RAISED sentence with the rename record; item 6 swapped the two roster-referent prose sites
(`or on the frozen codex roster` in the file header and `neither the S0-S23 record nor the codex
roster` in the failure message) to `unassigned-landed roster`; item 7, the accounting test's TITLE
word-swap, was approved at ODQ §425 ruling 5 and executed. Prose describing the codex SLICE itself —
its landing history, its module counts, its readers of the ABI — is true provenance and was NOT
touched, and the one surviving mention of the old identifier is the rename record's own comment.

⭐ **ONE DEVIATION FROM THE DRAFT'S EDIT TEXT, TAKEN AND RECORDED (J-TET2L-1).** The draft's item 6
named two prose sites; the executed census found a THIRD roster-referent site the compile did not
list — the roster's own declaration docblock, titled `⛔ THE CODEX-SLICE ROSTER`, immediately above
the renamed constant. It was swapped to `⛔ THE UNASSIGNED-LANDED ROSTER`, because a docblock still
titled for the old name directly above `const UNASSIGNED_LANDED_MODULES` is exactly the stale word
§419.2 exists to retire, and the next reader would re-find it. It is severable exactly as item 7 is.

⛔ **THE ATOMICITY LAW: the leaf, the roster row and the rename land in ONE commit.** Any commit
carrying the leaf without the row is a tree whose lint battery is red (the named interior red,
§P7.12); any commit renaming without all four identifier sites is a `ReferenceError`.

### §1.8 THE STAGE-MANIFEST ACCOUNTING — measured at the base, pin by pin

A new fabric leaf reds MF-T2J's walker by construction: the accounting arm is exact in both
directions over the whole directory, and `vegetation.js` is neither assigned by the ported record
nor on the frozen roster. The cure is the ONE roster row. Every neighbouring pin was checked against
the base spelling rather than assumed, and the walker then RUN with leaf, row and rename all present
(3 passed, §4 row 10):

1. The `toEqual([...ROSTER])` pin compares the directory-derived unassigned list against the roster;
   `'vegetation.js'` sorts after `'supportSurface.js'` by codepoint, so appending at the tail keeps
   the derived order.
2. The arithmetic pin is computed on both sides, so it self-adjusts: 5 + 24 = 29 at the after state.
   **The assigned-set literal (5 names) is untouched.**
3. The planted-arrival arm self-adjusts too — it pins the `nodeOfModule === null` count to
   `ROSTER.length + 1` with an in-memory plant, and both sides grow by one with the row.
4. The record-lead pin stays a literal `.toBe(49)`: an on-disk mint moves neither `manifestModules()`
   nor its not-on-disk filter.
5. ⛔ **`FOUNDATION_READERS` FORCED THE IMPORT DESIGN.** Its keys are exactly `coordinateAbi.js`,
   `solidLegality.js`, `spatialReceipt.js` and `stageManifest.js`. The leaf imports
   `foundation.js`, `massPart.js`, `supportSurface.js` and `fabricRng.js` — none tracked — and does
   NOT import `coordinateAbi.js` (there is no ABI stamp to mint; `solidPartQ` stamps `abiVersion`
   itself) and does NOT import `solidLegality.js` (the acceptance file's consumer; tests sit outside
   the roster's directory denominator). A design importing either would owe a roster edit inside
   another member's reserved file for zero behaviour. The transitive closure adds NO new module to
   any import graph.
6. The agreement arm parses only landed ∧ ASSIGNED modules, so `vegetation.js` is not parsed and its
   `hash32(` call sites are invisible to that arm's fork census by both scope and spelling.

### §1.9 The registration template, at full strength (§P4, ODQ §35.3)

| the four | the census register | the stage-manifest accounting register |
|---|---|---|
| **THE ROW** | the five-figure tuple, re-recorded whole with its cause (§6; deferred text §1.7 Row A) | `'vegetation.js'` in the roster + the §419.2 rename (deferred text §1.7 Row B) |
| **THE HEAD RE-EXPORT** | **NONE, AND THAT IS THE ANSWER** — `fabric/index.js` is not opened; all five dormant leaves carry zero barrel rows and this member continues the precedent | **NONE** — a test-side roster has no composing head; the walker consumes the row directly |
| **THE EXACT-LIST / EXACT-COUNT PIN** | the census arm's five equalities plus `parked + credited === files`, exact both directions | the accounting `toEqual` both ways, the assigned-set literal, the computed 5 + 24 = 29 arithmetic, the `length + 1` plant arm and the 49-lead literal |
| **THE REGISTRY TEST PATH** | `tests/lint/sovereigntyLightingContract.walker.test.js` | `tests/lint/townMapStageManifest.walker.test.js` |

The other §P4 registers: anchor walker (ceiling zero, executed §4 row 14), single-declaration walker
(floors only strengthen; five arriving export names collide with nothing), entry-closure fence
(§10), coupling census / size ratchet / hot-file law (all zero in-directory, R-MF-5).

## §2 · EXACT CONTRACTS — `src/domain/townMap/fabric/vegetation.js`

```js
export const VEGETATION_PHENOLOGY_CLASSES
// SPEC §10.6's phenology union, verbatim and in SPEC order (4), frozen and closed:
// ['EVERGREEN', 'DECIDUOUS', 'DROUGHT_DECIDUOUS', 'BARREN']

export const VEGETATION_FIELD_LAW_VERSION
// 'mf-vegfield-era-uniform-rect-v1' — the era instance-derivation law's version stamp.

export function vegetationMass(input)
// input: { bodyId, support,                       // support: a supportSurfaceRef record, RE-DERIVED
//          trunk: {partId, footprint, vertical},  // NO supportSurfaceId (refused, naming the law)
//          canopy: {canopyId, speciesClassId, footprint, topQ},   // NO supportSurfaceId, NO baseQ
//          canopyAnchor: {trunkPartId, baseHeightAboveSupportQ},  // trunkPartId === trunk.partId
//          phenologyClass }
// -> deepFreezeCanonical({ bodyId, kind: 'VEGETATION', trunk,
//      canopy: {canopyId, ownerBodyId, speciesClassId, solid},
//      canopyAnchor: {trunkPartId, baseHeightAboveSupportQ}, phenologyClass })
// Both volumes constructed INTERNALLY through massPart.solidPartQ — one construction route.

export function vegetationInstanceField(input)
// { fieldKey, supportRegion, instanceCount, speciesClassId, phenologyClass } -> a frozen record of
// the same five fields. Region through canonicalRectBounds (canonical rect only; zero measure
// refused there); instanceCount a canonical int >= 0; ids through the wall; phenology closed.
// Idempotent over its own output, which is what lets the instance derivation re-check a
// caller-held field without a second spelling.

export function vegetationFieldInstance(field, instanceIndex)
// -> deepFreezeCanonical({ fieldKey, instanceIndex, positionQ: [xQ, zQ], speciesClassId,
//                          phenologyClass })   // NO id field of any kind exists on the record.
// field re-validated through vegetationInstanceField; instanceIndex a canonical int in
// [0, instanceCount), and a zero-count field refuses every index naming the emptiness.
```

**PURITY:** pure over arguments; string, array and integer handling plus the landed calls. No clock,
no randomness beyond the keyed hash of the caller's own strings, no locale ordering, no
iteration-order dependence; fields written in fixed literal order; no caller spread reaches a
published record.

**MEASURED SIZE: 149 effective lines** under `max-lines {skipBlankLines, skipComments}`, against the
ODQ §425-ratified caps of ≤230 per leaf and ≤240 for the packet — measured with eslint's own
`Linter`, never `wc -l`, and the instrument validated on four independent knowns first (§4 row 7).
The compile predicted ≈195; the honest spelling came in under it because the anchor equalities are
derivations rather than validators and both volumes route through one landed constructor.

**Name reservations honored:** none of MF-T2K's exports, no `canopyVolumeQ` (the shell-era member's
name, deliberately left free), no aggregate/phase/observation names, no operation vocabulary, and no
exported membership helper.

## §3 · BLAST RADIUS AND DORMANCY EVIDENCE — executed

Tree-wide grep for the five arriving export names, excluding `node_modules`, `.git` and `dist`:
hits are the leaf's own declarations, the acceptance file and packet/index prose, and **nothing
else**. ZERO landed production consumers, ZERO barrel rows, ZERO persistence seams. The leaf's edges
are OUTBOUND only (`foundation.js`, `massPart.js`, `supportSurface.js`, `fabricRng.js`), so it is a
SINK in the module graph. The pre-edit collision scan returned 0 files for each of the five names
with a firing control (`solidPartQ` → 7 files) proving the instrument live. ⚠ The LIVE application's
`townScene` vocabulary includes presentation-plane `vegetation` template kinds (lowercase
scene-manifest strings in `compileTownSceneGeometry.js`); they share no identifier and no code path
with this member — noted so a future grep reader does not mistake the scene's paint vocabulary for a
consumer of this leaf.

## §4 · PREFLIGHT — every row EXECUTED by the lane at its own base `4d2d17f8`

| # | row | executed result |
|---|---|---|
| 1 | the stack + sealed refs re-resolved | `ec8f3359` (`composite-r4`, observed) · `05e7f9d5` (t2h) · `f7ba3145` (t2j) · **`4d2d17f8` (t2k — the base)** · `ee0db96d` (sealed) · `62d89307` (ledger). Exits read, never the echo |
| 2 | stack state | t2h ancestor-of t2k YES; t2k ancestor of NEITHER moving branch; branch point `9bfae712` ⇒ unrebased, unlanded. §416.1 identity arm applied to `composite-r4`'s move (`1a437bca` → `ec8f3359`): not my stack's tip, base unchanged |
| 3 | preamble SHA-256 | `0706aad6…1db4ed` — identical to MF-T2K's recorded value |
| 4 | consulted-source hashes | `supportSurface.js` `9d0e162b…2ea7` (1 commit) · `massPart.js` `d52c19fa…e76f` (1 commit) · `foundation.js` `1ada51b6…93e3` · `fabricRng.js` `4094ecb8…6e95a` (1 commit) · `solidLegality.js` `950680dc…391f` · spec blob `aa613cb8…1698` resolved as a committed object at the ledger branch. **ALL MATCH** the compile's values |
| 5 | packet-manifest reservations | MF-T2H/T2J/T2K **READY**, every other `MF-T2*` LANDED; the two shared paths are theirs; the three CREATE paths carry **zero** reservations at any status (0 manifest hits for `vegetation\|townMapVegetation\|MF-T2L`) |
| 6 | the sealed-fabric negative | four greps WITH a firing control — zero sealed vegetation modules; nothing to port |
| 7 | effective-line instrument validation | eslint's own `Linter` reproduced FOUR independent knowns exactly: `massPart` **118**, `solidLegality` **150**, `stageManifest` **219**, `supportSurface` **182** — only then was the leaf priced |
| 8 | name-collision scan, five arriving exports + reserved `canopyVolumeQ` | **0 files each**; control `solidPartQ` → **7 files** (instrument live) |
| 9 | the K-seam captures, executed live | `supportSurfaceRef({kind:'TERRAIN_FACE', patchId:'p1'})` derives `support:terrain-face:p1`; a caller-supplied `surfaceId` is refused naming the free-alias law; `MAINTAINED_FREE_SPACE` is refused naming the §299.3c-blocked substrate. All three premises §1.2 rests on verified rather than inherited |
| 10 | stage-manifest walker, with leaf + row + rename present | **3 passed**, and the neighbouring pins read as §1.8 states: assigned-set literal untouched, arithmetic and plant arms self-adjusting, record lead 49 unmoved |
| 11 | `FOUNDATION_READERS` unmoved | roster keys executed as exactly the four; the leaf's imports avoid all four by design; the walker green with the leaf present IS the executed proof |
| 12 | entropy-root census | the definition regex `function (fnv1a32\|hash01\|hashUnit\|hash32\|fnv1a)` matches the leaf **0** times (it imports `hash32`); the census walker is green |
| 13 | `package.json` / `package-lock.json` | **byte-unmoved**; no dependency arrives |
| 14 | anchor preflight (§P3), bare and in-shell | **9 passed, exit 0**; ceiling zero holds — the acceptance file's negatives are throw-assertions and `in`-membership reads, so zero scanned matchers exist in it |
| 15 | `CLAIM_RE` over this packet | **0 hits**, executed with the exact regex read from `tests/docs/enforcement-claims.test.js` at this base |
| 16 | barrel zero-row precedent | `fabric/index.js` carries **0** mentions of the five dormant leaves; this member adds none |
| 17 | both typecheck configurations | `typecheck:domain:strict` (`tsconfig.domain-strict.json`) **1134 errors / ceiling 1134, exit 0**; `typecheck:ratchet` (`tsconfig.full.json`) **173 / ceiling 173, exit 0**. ZERO new errors in either, and no cast row and no widening were taken |
| 18 | forensic-zoom probe precheck | `DROUGHT_DECIDUOUS` and `mf-vegfield` → **0 files** over `src tests`; both are string LITERALS in the leaf, so both survive minification |
| 19 | the duplicate-path probe | EXECUTED with the two deferred rows present — the validator refuses exactly as predicted; the quote is in §11. The rows were then REMOVED and `validate:packets` re-run green |

⭐ **ONE DEVIATION FROM THE COMPILE, REPORTED RATHER THAN SMOOTHED (row 8):** the compile published
`4` files for the `solidPartQ` control; it re-executes to **7** at this base over `src tests`. The
conclusion is unchanged and strengthened — the control fires, so the zeros beside it are real.

## §5 · ACCEPTANCE CASES — six, in ONE file, one literal `describe`

`tests/domain/townMapVegetation.test.js` — one `describe`, six straight-line `test` calls,
string-literal titles, every loop INSIDE a named test (the SP-D idiom). **6 passed (6), exit 0.**

| id | case |
|---|---|
| **A1** | GUARD-THE-GUARD, first: `VEGETATION_PHENOLOGY_CLASSES` nonempty, frozen and pinned by order-sensitive deep equality against the SPEC-verbatim four; `VEGETATION_FIELD_LAW_VERSION` pinned to its exact literal (the derivation basis is load-bearing — a drifted stamp silently moves every position). Then the happy path: a full mass constructs, frozen at the record AND nested, `kind` reads `'VEGETATION'`, the key list is exact, and a replay is JSON-identical. |
| **A2** | THE ONE-KEY LAWS. Positive control first, then: the trunk's `supportSurfaceId` and the canopy `solid.supportSurfaceId` BOTH read `support:terrain-face:p1`; `canopy.ownerBodyId === bodyId`; `canopy.solid.vertical.baseQ === canopyAnchor.baseHeightAboveSupportQ`; the canopy solid's `partId` is the `canopyId`. Refusals: a hand-built alias support record refused naming BOTH keys; `supportSurfaceId` inside the trunk input refused naming the one-key law; inside the canopy input refused; `canopyAnchor.trunkPartId` mismatch refused naming both ids. |
| **A3** | MASS REFUSALS, the inherited walls arriving intact: unknown `phenologyClass`; a `MAINTAINED_FREE_SPACE` support record (K's own blocked-kind message surfaces through the re-derivation); canopy `topQ` equal to the anchor base (`verticalIntervalQ [3000, 3000) is empty or inverted`); a degenerate trunk footprint (massPart's zero-exact-signed-area refusal); missing `canopy`; an extra top-level field; non-record input. |
| **A4** | THE FIELD: constructs frozen with the five fields in order and replays JSON-identical; the region law — a non-rectangle ring, a zero-measure rect and a 3-point ring each refused by `canonicalRectBounds`' own messages; `instanceCount` −1 refused, `2.5` refused, `0` ADMITTED (the felled stand); unknown phenology refused; an extra field refused. Idempotence: feeding the returned record back constructs an equal record. |
| **A5** | THE INSTANCES. THE GOLDEN — `vegetationFieldInstance(field, 0).positionQ` is exactly `[169, 376]` and `(field, 7)` is `[165, 402]`, computed by executing the leaf at authoring over the fixture rect `[100,200) × [300,450)`, count 10, key `'orchard-east'`; the rect deliberately starts at 100/300 so a dropped region base cannot pass by luck. Determinism: index 3 twice, JSON-identical. Containment: a loop over ALL ten indices asserts `minX ≤ xQ < maxX`, `minZ ≤ zQ < maxZ` and integer-exactness (`Number.isSafeInteger` on both axes). NONPERSISTENCE, STRUCTURAL: `'instanceId'`, `'entityId'`, `'bodyId'` and `'artifactId'` all read `false` under `in`, and the record's exact key list is pinned. Refusals: index −1, index `count`, index `1.5`, and every index against a count-0 field, the message naming the emptiness. |
| **A6** | THE CONSUMER SEAMS, landed code with zero adaptation. (i) L→F: a trunk `[0,50)` against a building `solidPartQ` `[30,80)` on ONE derived `support:terrain-face:p1` key → `VOLUME`, `OVERLAPPING`, `sharedAreaQ {numQ: 10000n, denQ: 1n}`, `sharedHeightQ 20n`, `sharedVolumeQ {numQ: 200000n, denQ: 1n}` — every magnitude pinned EXACT. (ii) the same tree's trunk `[0,3000)` against its own canopy `[3000,8000)` → `sharedVolumeQ 0n`, `DISJOINT_OR_ABUTTING` (the half-open law through this member's records). (iii) a `WALL_TOP`-keyed trunk against a terrain-keyed solid → `REFUSED`, `DIFFERENT_SUPPORT_SURFACE`, `verdict: null`, the detail quoted — the refusal STAYS a refusal. (iv) K consumes L's shape: `assertSupportAcyclicity` over a building part on a terrain face and a rooftop tree on that part's `ROOF_FACE` returns `['bldg.1.range', 'veg.roof.1.trunk']`, the building strictly first. |

## §6 · THE CENSUS WALK — sequenced, isolation-attributed

⚠ The interior red was NAMED before it existed (§P7.12): one new test file forces the five-equality
census arm red by construction. The walk was RUN rather than short-circuited, each figure read from
its own arm's failure message:

| step | arm | reading |
|---|---|---|
| 1 | file count | `expected 2503 to be 2502` |
| 2 | credited | `expected 2139 to be 2138` — `parked` PASSED at 364 without ever redding |
| 3 | test titles | `expected 20743 to be 20737` |
| 4 | suite titles | `expected 5791 to be 5790` |
| 5 | — | **GREEN, 33 passed (33), exit 0** at `2503 / 364 / 2139 / 20743 / 5791` |

⛔ **ATTRIBUTION BY ISOLATION.** With the walker pinned at the after tuple, hiding
`townMapVegetation.test.js` alone convicted `expected 2502 to be 2503` — the base tuple EXACTLY,
which is the proof that nothing else moved this census under the lane. The hidden file was restored
digest-exact (`b447b6c6…73fb`). The re-record's cause paragraph lands in a counted file, so the
walker was re-run AFTER the comment landed. The manifest ROW is deferred (§1.7 Row A).

## §7 · MUTANTS — seven, plus the guard-the-guard arm, clean-control FIRST

⛔ The clean-tree control ran FIRST and read **exit 0 with 6 passed (6)** — an all-red sweep with no
clean-green control is a broken runner. Every mutant was planted by exact single-anchor substitution
(the harness refuses to fire on 0 or 2 matches), convicted, and restored digest-exact against
`52ab2efd…8971`, with the final restore re-verified and `git diff` over the tracked fabric directory
reading empty.

| # | branch deleted or weakened | conviction, by named arm |
|---|---|---|
| GUARD | `VEGETATION_PHENOLOGY_CLASSES` emptied | A1 first, then A2/A3/A4 — the nonempty and order-sensitive pins red BEFORE any membership test could pass on nothing |
| M1 | the support re-derivation equality deleted (an alias key accepted as given) | **A2 alone** — the naming-both-keys arm. A3's blocked-kind arm still passes because that refusal fires inside K's constructor, before the equality |
| M2 | the `canopyAnchor.trunkPartId === trunk.partId` refusal deleted | **A2 alone**, on a different assertion from M1's and with a message naming both ids |
| M3 | the canopy solid threaded with a second, independently spelled ground key | **A2 and A6** — the canopy-key equality and, downstream, the landed predicate's own `DIFFERENT_SUPPORT_SURFACE` refusal on the stacked pair |
| M4 | the mass phenology membership check deleted | **A3 alone** |
| M5 | the `canonicalRectBounds` route bypassed (raw region stored unvalidated) | **A4 alone** — the three region refusals construct where refusal is expected |
| M6 | the `instanceIndex` range refusal deleted | **A5 alone** — index −1, `count` and `1.5` all stop refusing; the count-0 arm still refuses, by design, through its own named branch |
| M7 | the modulo fold's region base dropped | **A5 alone** — the GOLDEN's exact integers red and the containment sweep reds with it |

⭐ **M3 WAS RE-SPELLED FROM THE COMPILE'S TEXT, AND WHY (J-TET2L-2).** The compile proposed
"weakened to pass a caller-supplied canopy `supportSurfaceId` through when present". Under the
implemented design that mutant is EQUIVALENT: the exact-field door refuses such an input before the
threading is ever reached, so the added pass-through is dead code and nothing reds. Per the
J-TET2J-8 rule the enabling condition was diagnosed and the mutant re-planted in a convicting
spelling that attacks the SAME branch — the second door to a second ground authority — by threading
a different key into the canopy. It convicts two arms rather than one.

## §8 · JUDGMENTS — every call, vetoable

| id | the call | chosen over | why |
|---|---|---|---|
| J-TET2L-1 | the §419.2 rename ALSO swaps the roster's own declaration-docblock title, a third prose site the compile did not list | executing only the draft's two named prose sites | a docblock titled `THE CODEX-SLICE ROSTER` directly above `const UNASSIGNED_LANDED_MODULES` is precisely the stale word §419.2 retires, and the next reader re-finds it; severable exactly as item 7 is, and neither the ratchet nor the sweep keys on it |
| J-TET2L-2 | M3 re-planted in a convicting spelling that attacks the same branch | reporting the compile's M3 as fired when it is equivalent | §P6 — a mutant that cannot red proves nothing; the J-TET2J-8 rule is explicit that the cure is diagnose, re-plant, disclose |
| J-TET2L-3 | ONE exact-field door owns the `supportSurfaceId` refusal, with a message that names the one-key law | a dedicated guard beside the field check | two guards each independently holding the invariant is the redundant-second-guard defect §P6 names; one door with a branching message keeps the conviction honest and the message teaching |
| J-TET2L-4 | the count-0 field carries its own named refusal branch ahead of the range check | letting `[0, -1]` refuse it with an arithmetic message | a felled stand is a real state and its refusal should say so; the range check still covers every other index, which is what keeps M6 convicting |
| J-TET2L-5 | the acceptance file's refusal helper THROWS when nothing threw | returning an empty string on a silent pass | MF-T2K's own mid-lane lesson: a deleted refusal must red as behaviour, never pass as a matcher artifact |
| J-TET2L-6 | the leaf came in at 149 effective, well under the ratified 230, and no scope was added to fill the room | spending the headroom | the cap is a ceiling, not a target; every absent piece has a named owning successor (§1.6) |

The compile's own judgments J-TCT2L-1 through J-TCT2L-13 were implemented as written and are not
re-argued here; ODQ §425 ruled the five questions they raised.

## §9 · RAISED

**CHAIR:** RAISED-1 through RAISED-5 were all ruled at ODQ §425 and are implemented as settled.
Two new items, both severable and neither blocking:

1. **J-TET2L-1's third prose site** — approve or strike the docblock-title swap.
2. **The measured size came in at 149 against a ratified 230.** No action is owed; it is recorded so
   the next member of this family prices its own prediction against a measured member rather than
   against the compile's estimate.

**OWNER:** none. This member mints no tuning constant (the derivation is parameter-free by
enumeration), no persisted schema, no paid or user-visible surface, and moves no seed byte; nothing
on the carve-out list is touched, and no quantum is chosen anywhere in it.

## §10 · THE ENTRY-CLOSURE FENCE AND THE FORENSIC ZOOM

§P2.2 as amended by ODQ §324.5: the fence discharges POST-BUILD only. `dist/` was built, then
`VERIFY_DIST=1 npx vitest run tests/build/townMapLazy.test.js` reported **3 passed (3), exit 0** —
EXECUTED counts, not a pre-build skip over zero tests. The forensic zoom then walked the built
chunks under the §422 withdrawn-probe discipline (every probe a string LITERAL present in the leaf,
each verified unique across `src/` at this base first): the leaf's probes appear in **no chunk
whatsoever**, and the three positive controls owned by landed fabric modules
(`plan-q1-0-1000-v1`, `CANONICAL_SPATIAL_OPERATION`, `ORTHOGONAL_CROSS_PLAN`) each appear, so the
denominator provably contains the fabric surface. Figures are in §11.

## §11 · EXECUTED RECEIPTS

- **Acceptance:** 6 passed (6), exit 0. Combined focused battery over the acceptance file and the
  three walkers this member touches or is measured by: green.
- **Effective lines:** 149 (cap 230 leaf / 240 packet), instrument validated on four knowns first.
- **Census:** `2502/364/2138/20737/5790` → `2503/364/2139/20743/5791`; sequenced walk, GREEN 33/33;
  isolation attribution convicted the base tuple exactly.
- **Typechecks:** strict 1134/1134 exit 0; ratchet 173/173 exit 0. Zero new errors in either.
- **Mutants:** 7/7 plus the guard-the-guard arm convicted; clean control exit 0 with 6/6 FIRST;
  every restore digest-exact.
- **Anchor walker:** 9 passed, exit 0, ceiling zero. **Single-declaration + entropy-root walkers:**
  36 passed, exit 0. **Stage-manifest walker:** 3 passed with leaf, row and rename present.
- **`validate:packets`:** green at DRAFT and again at READY. **Duplicate-path probe:** EXECUTED with
  the deferred rows present, refusal quoted; the rows were removed and the validator re-run green.
- **Fence:** post-build, `VERIFY_DIST=1` → 3 passed (3), exit 0.
- **§408 pre-gate sweep** over `tests/lint tests/build tests/ops`, run TWICE and reported as it
  happened rather than as it was predicted. The STABLE red set is **five tests in three files**, and
  all five join `scripts/.test-ratchet-baseline.json`'s frozen eleven by executed title-prefix
  match at `4deb4f02` — `clampPrimitiveBaseline` (entry 7), `warCostKindPools` ×3 (entries 8–10),
  `warRulingKindPools` (entry 11). That is the identical set MF-T2J and MF-T2K reported at this same
  base. ⚠ **BOTH RUNS ALSO CARRIED STRAYS, AND THE TWO STRAY SETS ARE DISJOINT** — run 1:
  `townScene3dLazy` ×5, `observedShapeSentinel`, `postureNameCollision`,
  `sovereigntyLightingContract` (13 failed / 7 files); run 2: `townSceneLocalMatrixAudit` ×2 and
  `postureNameCollision` (8 failed / 5 files). **Every stray file from both runs passes GREEN when
  run focused at this tip** (3 files / 86 passed and 2 files / 22 passed, both exit 0), and run 1's
  strays each carried 24–63 second durations because that sweep was run while `npm run build` held
  the machine. This is the estate's known varying-cast intermittent contamination, not this
  member's: nothing in the stray cast reads this member's leaf, its acceptance file or either
  walker it edits, and the member's own file
  (`tests/lint/sovereigntyLightingContract.walker.test.js`) is green focused at 33/33 on every
  execution. Per the varying-cast protocol the run was classified and re-run ONCE; a second stray is
  a STOP, so the hunt is left to its chartered owner (§355) and is recorded here rather than chased.
- **S0, two-part, at this base:** exit 1 WITH the printed schema-9 envelope verdict — a real
  reading rather than the `ERR_MODULE_NOT_FOUND` trap. This member writes no governed file.
- The full gate is deferred to the landing slot under the ratified rule: this lane's base is
  provably not an ancestor of the branch, so a gate receipt taken now would certify a tree that is
  not the one that lands.

### §11.1 · DELIVERABLE DIGESTS, read from the COMMITTED tip with `git show HEAD:`

⚠ Read from the committed tip rather than from the working tree, because the pre-commit hook can
re-stage — and it was checked: `git diff HEAD` over the four code deliverables reads empty, so the
hook changed nothing under this lane.

| deliverable | SHA-256 at the committed tip |
|---|---|
| `src/domain/townMap/fabric/vegetation.js` | `52ab2efda906190b08ae16fc4031569b67c43a42f8900af6755ca51173838971` |
| `tests/domain/townMapVegetation.test.js` | `b447b6c6782af03e89b675383b95c95153db64019fba6ca03ed8ee5adc4473fb` |
| `tests/lint/townMapStageManifest.walker.test.js` | `d11ece38aa7d9257422b525dc27ce881fd56f3832d6a72c84aa4e8096eda1a17` |
| `tests/lint/sovereigntyLightingContract.walker.test.js` | `9bdf53c055ebadb49248fee6e4508c543a6775a39d4d67d8283f715e28f588b3` |

**PINNED UNMOVED at the tip** — every consulted source still hashes to its base value:
`supportSurface.js` `9d0e162b…2ea7` · `massPart.js` `d52c19fa…e76f` · `fabricRng.js`
`4094ecb8…6e95a` · `foundation.js` `1ada51b6…93e3` · `solidLegality.js` `950680dc…391f`.
`package.json` and `package-lock.json` are byte-unmoved against the base (executed
`git diff --name-only` over both paths reads empty), so no mint trigger fires.

**Diff against the base:** 7 files, 1322 insertions, 11 deletions; production surface is ONE file.

**Forensic zoom, executed over the built tree:** 710 chunks in the denominator. Four leaf probes,
each verified a string LITERAL present in the leaf and unique across `src/` beforehand
(`mf-vegfield-era-uniform-rect-v1`, `DROUGHT_DECIDUOUS`, the count-0 refusal fragment, and the
`VEGETATION` kind) → **0 chunks each**; ZERO probes withdrawn. Three positive controls owned by
landed fabric modules → **1 chunk each**, so the denominator provably contains the fabric surface
and the absence is measured against something rather than against nothing.

## §12 · HARD SCOPE BUDGET (ODQ §425-ratified)

| Limit | This packet | Standard |
|---|---:|---:|
| Behavior families | `1` | 1 |
| New persisted record families | `0` | ≤1 |
| Named state writers / feature flags / user-facing surfaces | `0` | ≤1 each |
| Direct production consumers | `0` | ≤2 |
| New logic-bearing production leaves | `1` — `fabric/vegetation.js` | ≤2 |
| Existing logic-bearing production files modified | `0` | ≤3 |
| Registration-only edits | `2` — the two walker edits, both deferred-manifest (§1.7) | ≤3 |
| Handwritten files total | `6` — leaf · acceptance file · 2 walker edits · packet · manifest+INDEX rows | ≤12 |
| New/changed effective production lines | **149** (cap 240) | ≤400 |
| Effective lines per new leaf | **149** (cap 230) | ≤250 |
| Delta in a shared/hot file | `0` — no manifest path is on the hot-file list | ≤15 |
| Acceptance cases | `6` | ≤8 |

Overrides taken: `NONE`.
