# Town cartography / MF-T2M — architectural-history operations and deterministic fixtures: the §287.16 massing-foundation member that lands the SPEC's twelve-kind dated-operation payload grammar as a domain member of the fabric's ONE sealed-artifact grammar, and executes the §423 Shape-B strip that demotes `fabric/operations.js` from door-claimant to payload compiler

- **Status:** DRAFT
- **Packet version:** 1
- **Implemented by:** lane TE-T2M, against the chair-ruled compile `draft-MF-T2M.md`
  (ODQ §426; compiled by TC-T2M under ODQ §423.1).
- **Ruling of record:** **ODQ §423** (the domain door; Option B + the §423.1 Option-D law) as
  ruled at **ODQ §426**. This member executes consequences (1) and (2); the store side is
  UNTOUCHED — consequence (3) keeps the door verb docketed with the executor tranche.
- **Verified base:** `4d2d17f86f173686d142a97e3fd356d6b7be97e6`
- **Base provenance:** `refs/preserve/holding-t2k`,
  MF-T2K's holding tip. ⭐ **DERIVED WITH EXECUTED EVIDENCE, not assumption:** TE-T2L had
  committed no holding tip when this lane derived its base (its worktree HEAD read `4d2d17f8`
  with uncommitted work, and `refs/preserve/holding-t2l` did not exist), and the cascade had NOT
  rebased the D3a stack — `git merge-base --is-ancestor` reports `05e7f9d5` (T2H), `f7ba3145`
  (T2J) and `4d2d17f8` (T2K) are none of them ancestors of `claude/composite-r4` (`ec8f3359`) or
  of the ledger branch (`0982b530`), and `git merge-base 4d2d17f8 claude/composite-r4` is
  `9bfae712`. So `4d2d17f8` IS the D3a stack's then-current tip.
- **Depends on:** **MF-VS1** (LANDED — created `fabric/operations.js`; its three
  `requiredSymbols` compiler functions ALL SURVIVE this strip) and **MF-T1X** (LANDED — extended
  it; its two GATE `requiredSymbols` rows are retired here and discharged per §379.2 at the
  LANDED flip). Positionally stacked on MF-T2K: **no symbol of MF-T2H, MF-T2J, MF-T2K or MF-T2L
  is consumed** — the stack binds through train order and the two deferred shared rows.
- **Family preamble:** `docs/implementation/preambles/MF-PREAMBLE.md`, SHA-256
  `0706aad6a84e4f74ec47602e9e3222fc398c4c8864bb38c79ac480c2a11db4ed` — **recomputed by this lane
  at its own base**, identical to the compile's and to T2K's/T2L's recorded value.
- **Collision group:** `d3a-port`. ⛔⛔ **TWO shared paths are edited and NEITHER appears in the
  `changeManifest`** (the §417 standing law): `tests/lint/sovereigntyLightingContract.walker.test.js`
  is reserved by **MF-T2H (READY)** and `tests/lint/townMapStageManifest.walker.test.js` by
  **MF-T2J (READY)** — both executed at this base. Every DIRECT path this member touches carries
  **zero NON-TERMINAL reservations** (executed sweep over `PACKET_MANIFEST.json`; the LANDED rows
  on `operations.js`/`index.js`/the fantasy test are numerous and lawful).

> **`censusAuthorization`:** this packet moves the test census by
> **`+1 file / +0 parked / +1 credited / +7 titles / +1 suiteTitle`**.
> **Base tuple, executed at `4d2d17f8`: `2502 / 364 / 2138 / 20737 / 5790`.
> After tuple, executed at this lane's tip: `2503 / 364 / 2139 / 20744 / 5791`.**
> The **+7** shape (six acceptance titles plus the §423.1 prevention arm) was RAISED as a
> deviation from the plan's fixed +6 single-behaviour shape and **RATIFIED at ODQ §426(3)**.
> ⚠ The DELTA is what crosses a rebase (the carry law); the tuples are re-derived from the hash
> after the last edit at the landing slot. Authorizing decisions: **ODQ §276** (the build sheet),
> **ODQ §304.4** (the D3a port charter), **ODQ §310.4** (the wave charter), **ODQ §423/§423.1**
> (the door ruling and the structural law) and **ODQ §426** (this member's compile ruling), under
> §299.4's binding-forward rule. Family stamp: GRANTED at **ODQ §312.2b**.

---

## §1 · WHAT THIS MEMBER IS

### §1.0 The ruling it executes

ODQ §423: *"the store registry (src/store/operationRegistry.js) is THE census of DOOR VERBS; one
domain artifact grammar owns operation PAYLOADS … (1) fabric/operations.js DEMOTES from
door-claimant to payload compiler — the TC-D3A Shape-B strip, executed inside MF-T2M's scope; its
artifacts are proposal-grade until a registered store verb accepts them. (2) MF-T2M UNBLOCKS as a
domain member: architectural-history artifacts + deterministic fixtures joining the fabric's ONE
sealed-artifact grammar, dormant, zero store rows, live seeds byte-identical."* And §423.1:
*"an operation VOCABULARY may exist in exactly two places — the store registry's verb rows and the
domain's sealed-artifact payload grammar; a module claiming both levels, or a third vocabulary at
either level, is refused at review as a second truth."*

### §1.1 The leaf — `fabric/architecturalHistory.js`, **210 effective lines**

Measured with the validated instrument (eslint's own `Linter`, `max-lines
{skipBlankLines, skipComments}`), against the ODQ §426-carried caps of **220 per new leaf** and
**260 new/changed effective production lines**. The instrument was validated FIRST on five
published knowns and read every one exactly: massPart **118**, solidLegality **150**,
`operations.js` **278** (the §P9 figure), stageManifest **219**, supportSurface **182**.

- **The kind vocabulary, ported whole (12, SPEC order)** — byte-verified against blob
  `aa613cb8d9ea8b895236304e9e72b08940ba1698` lines 11408–11411. A1 constructs **every one** of
  the twelve this era: they are records, not executions, so no kind needs an era refusal.
- **The operation record** — sealed through `foundation.sealCanonicalArtifact` with
  `artifactKind: 'ARCHITECTURAL_HISTORY_OPERATION'`, the spelling the SPEC's ONE
  `CoreArtifactKind` union already carries (line 10089, executed). The member JOINS the grammar
  rather than minting beside it.
- **The one-way law, structural** — the record carries `beforeMassingRef` and **no after-ref
  field exists to misuse** (SPEC lines 11444–11446; the MF-T2H unrepresentable-not-forbidden
  idiom).
- **Kind-specific endpoints, exactly the two the SPEC names** — `SUBDIVIDE_BODY` requires
  `newBodyIds` (≥2, distinct, none equal to `bodyId`); `AMALGAMATE_BODY` requires
  `sourceBodyIds` symmetrically; **every other kind REFUSES both fields** (the exact-record law).
- **`replayArchitecturalHistoryOperation`** — re-derives through the sole creator and refuses a
  divergence, the landed `requireReplay` idiom; what makes a tampered proposal detectable.
- **`architecturalHistoryFixture()`** — the §287.16 second deliverable, a zero-argument
  PRODUCTION export: three dated operations over one synthetic body, strictly ascending
  (1204 / 1288 / 1341), both cause arms and one endpoint kind, every record replay-verified at
  construction.

**Imports EXACTLY `./foundation.js` and `../../townScene/stableScene.js`** — both untracked by
`FOUNDATION_READERS`, and both already inside the fabric's transitive closure, so **no new module
enters any import graph**.

### §1.2 ⭐ The derived artifact id — the free-alias law carried forward

`artifactId` is DERIVED: `arch-history-op:<sceneDigest({domain: <lawVersion>, …content})>`, in the
exact idiom of the landed `stateArtifactId`/`receiptArtifactId` derivations. Measured: 57
characters, inside the landed wall's 96. **Caller-supplied `artifactId` or `contentHash` is
REFUSED BY NAME** (MF-T2K's ratified free-alias law). `operationId` stays the caller's EntityId —
the SPEC carries both fields and they mean different things.

### §1.3 ⚠⚠ The four-token cause-unknown list, and the compile prediction it CORRECTED

`CAUSE_UNKNOWN_REASONS` is declared LOCALLY, not imported from `massPart.js`. Both sources were
read at this base:

| list | ordered members |
|---|---|
| SPEC `CauseDisposition` UNKNOWN (blob `aa613cb8`, lines 5866–5867) | `SOURCE_SILENT, CONFLICTING_EVIDENCE, OUTSIDE_COVERAGE, WITHHELD` |
| landed `massPart.js` `MASSING_UNKNOWN_REASONS` (lines 82–84) | `NOT_OBSERVED, OUTSIDE_COVERAGE, CONFLICTING_EVIDENCE, WITHHELD` |

⛔ **THE COMPILE'S A1 PREDICTION WAS WRONG AND THE PIN SHIPS STRONGER FOR IT.** The draft
predicted the two lists *"differ at EXACTLY index 0 … and agree on the other three"*. Executed,
that is false POSITIONALLY: index 0 differs, **indices 1 and 2 are SWAPPED**, and only index 3
agrees. As SETS they differ by exactly one member each way. A1 therefore pins BOTH facts — the
symmetric difference (`['SOURCE_SILENT']` / `['NOT_OBSERVED']`), the three-member intersection,
AND the order swap — so neither a merge, a "deduplication", nor a reorder can silently fuse two
vocabularies the SPEC keeps distinct. MF-T2K's identity-import was justified by byte-identity;
that justification fails here, which is exactly why a local declaration is the honest spelling.

### §1.4 ⭐⭐ The Shape-B strip, executed line by line

`fabric/operations.js` at base: sha256 `fd8c64b20ca6271052eb9a01d1a5d76d42ddfe348c6105e072ff44ec3e65a108`,
15303 bytes, **278 effective**. After the strip: **276 effective** (−2). `fabric/index.js`:
**111 → 110** (−1). ⚠ Note against §P9: index.js measured **111** at this base, not the published
101 — §P9's figure is a stamped fact about `9d851fae` and the file has since grown. Not a STOP.

1. **The gate export died** — `export const FANTASY_CANON_GATE = 'EXPLICIT_FANTASY_CANON'` and
   **all six** use sites: the v1-legacy mechanism seal, the v2 mechanism seal, the operation seal,
   and the gate-equality legs of both the `replayOperation` check and the executor's mechanism
   check. **No refusal class was lost** — A6 proves each survivor positively.
2. **The artifact kind demoted** — `'CANONICAL_SPATIAL_OPERATION'` → `'PROPOSED_SPATIAL_OPERATION'`
   (ODQ §426(1)). Executed census at base: the old string existed in **exactly one** `src`+`tests`
   site and **zero** `requiredSymbols` rows.
3. **Two law versions moved with the two shapes that moved** (ODQ §426(2)):
   `MECHANISM_LAW_VERSION` → `mf-t2m-registered-fantasy-construction-mechanism-v3`,
   `OPERATION_LAW_VERSION` → `mf-t2m-proposed-fantasy-construction-operation-v1`.
   `FIRST_SLICE_MASSING_CONSTRUCTION_STATE_LAW_VERSION` and the state/receipt shapes are
   UNCHANGED — they never carried a gate.
4. **The barrel lost one row** — `fabric/index.js`'s operations block drops `FANTASY_CANON_GATE,`;
   the other six re-exports stay.
5. **⚠ ONE test literal moved, with its cause beside it.** `LEGACY_HASH` in
   `tests/domain/townMapFantasyConstruction.test.js`:
   `scene-v1-56e0d430be735fbbab2f7aa4b4b50281` → **`scene-v1-5994c3ce8cf9d4d9b1ed8ffa2d6e4d1f`**.
   ⭐ **EXACTLY ONE, PROVED BY EXECUTION rather than predicted:** the fantasy battery was run with
   the strip in place and read `1 failed | 5 passed (6)` — the single failure being that literal.
   The six `scene-v1-*` projection pins in that file's A6 ride non-fantasy projections that embed
   no operation artifact and passed UNMOVED in the same run. A second moved literal was a STOP.
6. **What the strip did NOT touch:** `massingProjection.js` (zero gate references — it imports
   `executeFantasyConstruction` directly and keeps working untouched) · the three compiler
   functions and their exports · `FANTASY_CONSTRUCTION_OPERATION_KIND` · the two STATE constants ·
   the stage-manifest roster's treatment of `operations.js` · `FOUNDATION_READERS`.

⚠ **A PREMISE CORRECTION THE COMPILE ALREADY CARRIED, RESTATED:** the door brief's "zero
importers" was a grep blind spot. `operations.js` IS bundled (via the barrel and
`massingProjection.js`), so the strip has REAL dist consequences — proven in the forensic zoom.

### §1.5 ⛔⛔ The retirement, and the FOUR-RED validator window — BLESSED at ODQ §426(4)

This packet carries `retiredSymbols`. **The §379.3 binding preflight was re-executed at this
base**: the two retired pairs are pinned by **exactly MF-T1X** and by no other packet; **zero**
landed `requiredSymbols` row anywhere pins `CANONICAL_SPATIAL_OPERATION`, `EXPLICIT_FANTASY_CANON`
or either moving law-version string; and every kept export's rows (MF-VS1's three functions,
MF-T1X's kind/state/index rows) still resolve post-strip, checked string by string.

The validator asserts `requiredSymbols` existence at EVERY status, **only LANDED discharges**, and
a non-terminal packet's own `retiredSymbols` demand the retiree STILL PRESENT. With the strip on
this lane's tip the window is therefore **exactly four errors**, and it was quoted VERBATIM at
both DRAFT and READY — see §5.

### §1.6 ⛔⛔ The two deferred rows (§417) — texts for the landing act

Both walker edits are MADE and PROVED at this member's tip; neither path enters the
`changeManifest` until its reserving packet is terminal.

**Row A — the census re-record** (`tests/lint/sovereigntyLightingContract.walker.test.js`):

```json
{
  "_note": "THE CENSUS RE-RECORD, all five figures re-derived together with the cause stated: ONE NEW TEST FILE (+1 file, +6 titles, +1 suite title) plus ONE arm added to the already-credited single-declaration walker (+1 title) — the ODQ 423.1 Option-D prevention arm, whose +7 shape was RAISED and RATIFIED at ODQ 426(3). 2502/364/2138/20737/5790 -> 2503/364/2139/20744/5791 (tuples re-derived at the landing base per the carry law; the DELTA +1/0/+1/+7/+1 is what crosses a rebase). The walk was SEQUENCED and each figure read from its own arm's failure message. ATTRIBUTED BY ISOLATION IN TWO STEPS because the motion has two sources: hiding townMapArchitecturalHistory.test.js alone read 2502/364/2138/20738/5790 GREEN (base plus only the walker arm's title), and reverting the walker arm as well read 2502/364/2138/20737/5790 GREEN (the base tuple exactly); both restores digest-exact. parked holds at 364 because every title is a string literal in one describe. censusAuthorization: ODQ 276, ODQ 304.4, ODQ 310.4, ODQ 423.1, ODQ 426.",
  "action": "TEST",
  "path": "tests/lint/sovereigntyLightingContract.walker.test.js"
}
```

**Row B — the unassigned-roster row** (`tests/lint/townMapStageManifest.walker.test.js`):

```json
{
  "_note": "ONE ROSTER ROW: 'architecturalHistory.js' appended in FIRST alphabetical position to the unassigned-landed roster (CODEX_SLICE_MODULES at this base; re-check the constant name against the post-MF-T2L walker at the landing slot — the walker's own failure message, not the constant name, is the contract). The why, from packet section 1.1: the leaf is a D3a design-implementation mint against the SPEC contract, so the sealed record measured a tree it was never in and nodeOfModule() rightly resolves it to no node; the record's 49-module lead pin and the assigned-set literal are untouched, and the accounting arithmetic pin is computed, so it self-adjusts. FOUNDATION_READERS is unmoved: the leaf imports foundation.js and townScene/stableScene.js, neither of which that roster tracks; operations.js is EDITED rather than created, is already on the roster, and reads no tracked module.",
  "action": "TEST",
  "path": "tests/lint/townMapStageManifest.walker.test.js"
}
```

### §1.7 ⭐ The §423.1 prevention arm

Home: `tests/lint/townMapFabricSingleDeclaration.walker.test.js` — MF-T2A's walker, the §310.3(7)
D1 walker family scan over exactly this directory, LANDED and UNRESERVED at this base. One added
`test`. The arm asserts **exactly zero** line-anchored matches of the export-declaration grammar
for an acceptance gate across `src/domain/townMap/fabric/**` — a victory assertion with no
allow-list, because a door is a place to bank the first offender.

⚠ **The anchor is the EXPORT-DECLARATION grammar, and that was measured rather than reasoned:**
`stageManifest.js` lawfully carries `'gate'` as a §10.14 manifest FIELD NAME (line 271) **and**
`'*|watergate|*|*'` as a random namespace (line 214). A plain `gate` text scan convicts both. The
test itself proves both shapes INERT against the anchored matcher, and proves the matcher live on
a planted declaration, before the victory assertion runs.

**Its removing power is proven IN-LANE** (§7, M8), not by a sweep row: `scripts/mutation-sweep.sh`
is reserved by MF-T2J at READY, so the standing sweep row **DEFERS per §417** — ODQ §426(5),
RAISED-5 option (b): it rides the next member that lawfully touches the sweep. The file's
`mutation-coverage-manifest` row already exists (per-file enumeration); no manifest byte moves.

### §1.8 What it refuses, named affirmatively

No store motion of any kind · no executor, no after-state, no receipt, no `DerivationAuthorityRef`
· no TimeKey structure, no epoch derivation, no chronology validator · no provenance ledger and no
registry resolution · **the SPEC's optional `coverageRef` on the UNKNOWN cause arm is deliberately
ABSENT** (judgment J-TET2M-1: it names an `EvidenceCoverageRef` into a registry this era cannot
resolve, and T2E's law says an unvalidatable required field is worse than an absent one) · no
`RURAL_CHANGE` family, no `CoreOperationKind` rows, no AMP-promoted operation · no
mapEdits/document-inverse path (§10.13) · **no barrel row for the new leaf** (the five-dormant-
leaves zero-row precedent; the barrel is opened ONLY to delete the gate row) · no quanta
arithmetic, no BigInt, no geometry (R-MF-2 has no surface) · **no new dependency and no
`package.json` byte** (executed: `git diff --name-only HEAD --` over both is empty) · **no new
`tests/lint/**` FILE** (the prevention arm is an edit to an enumerated file with a standing row) ·
no second spelling of anything landed · **no live seed byte moves.**

## §2 · BLAST RADIUS AND DORMANCY EVIDENCE

Two stories, kept apart:

- **The new leaf is a SINK.** Outbound edges only (foundation, stableScene), zero importers, zero
  barrel rows, zero persistence seams — MF-T2K's shape verbatim. The forensic zoom proves it
  enters no chunk.
- **The strip edits a BUNDLED dormant module.** `operations.js` reaches one lazy chunk; nothing
  eager. Its bytes in that chunk move; **no live surface calls any fantasy-construction path** (the
  fabric has no input producer, §P2.1) and live seeds are byte-identical by construction. The dist
  motion is DECLARED — the §P7.1 parenthetical: a declared artifact shift inside a dormant module,
  named in its own packet.

## §2b · ⚠⚠ THE OSR ENVELOPE — a §384.2 record, and a PREMISE CORRECTION

`operations.js` sits in all three schema-10 subject-tree manifests (`sourceTree`/`scanTree`/
`executionTree`, keyed by sha256 `fd8c64b2…5a108` + size 15303, executed at base), so this
member's edits move those manifests by construction.

⛔ **THE COMPILE'S §3b PREDICTION DOES NOT HOLD AT THIS BASE, AND THE HONEST READING IS RECORDED
INSTEAD.** The compile predicted this member "flips the bare gate 0→1 by construction" — a
prediction written for a **WHOLE** envelope (exit 0). The §397 two-part S0 executed at this base
reads **exit 1** with the printed verdict *"observed-shape detector or unscanned execution input
changed since the schema-9 instrument was governed; an ordinary gate/write cannot migrate the
instrument"* — byte-identical to what TE-T2K and TE-T2L read at this same base. **The envelope is
ALREADY BROKEN before this member touches anything**, so there is no 0→1 flip available to
attribute. The recorded form is BROKEN→BROKEN with the same verdict line, plus the positive
attribution in §5. The cure remains the next mint's re-freeze, never this lane's write.

## §3 · ACCEPTANCE CASES — six in one file, plus the walker arm

`tests/domain/townMapArchitecturalHistory.test.js` — ONE literal `describe`, six straight-line
`test` calls, string-literal titles, every loop INSIDE a named test (the SP-D idiom). No `.each`,
no `runIf`, no nesting.

| id | case |
|---|---|
| **A1** | Guard-the-guard first: the three vocabularies nonempty, frozen, deep-equal to SPEC-verbatim literals (12/5/4). Then THE ANTI-DRIFT PIN against `massPart.MASSING_UNKNOWN_REASONS` in both directions (symmetric difference, intersection, and the order swap). Then the happy path — one operation per constructible class, each frozen deep, each replaying JSON-identical — and **all twelve kinds constructed**. |
| **A2** | The derived id spells `arch-history-op:scene-v1-…` and is inside the wall; same input replays byte-identical including `contentHash`; six single-field perturbations give six distinct ids; caller-supplied `artifactId`/`contentHash` refused naming the free-alias law; and the TAMPER pin — a record resealed after one payload byte moved has a self-consistent `contentHash` and is caught only by the replay through the sole creator. |
| **A3** | Thirteen typed refusals with the unmutated input constructing FIRST so each is attributable to its one perturbation; plus the NO-INFERENCE law in both directions with DISTINCT message pins, so neither arm can cover for the other. |
| **A4** | The endpoint law: both kinds construct as positive controls; missing / short / duplicated / self-naming refused symmetrically; a plain kind carrying either field refused, and each split kind carrying the OTHER kind's field refused; and the two arms' refusal messages proved DIFFERENT. |
| **A5** | The fixture: two calls byte-identical; three operations with strictly ascending dates; every record re-derived through the sole creator; the declared kind/cause coverage present; and the terminal record's `contentHash` pinned to the recorded golden **`scene-v1-c75cab40b6e086edaa26f04c3c4593ff`**. |
| **A6** | THE STRIP CONTRACT. Anchored absences via `expectAbsentWithAnchor` BY NAME: the gate is gone from the barrel, and no `gate` key survives on a freshly-sealed v2 mechanism, v1-legacy mechanism, operation, construction state or receipt — each absence anchored by a sibling key riding the same seal. The kind reads `PROPOSED_SPATIAL_OPERATION`. Then the survivors POSITIVELY: the full execute path still yields `{constructionState, receipt}` with three masses and a `BUILDING_ADDED` effect; the v1-legacy mechanism still refuses on the schemaVersion arm; a resealed non-replaying operation is still refused. |

**The walker arm** (in `townMapFabricSingleDeclaration.walker.test.js`): *"THE ODQ 423.1 TWO-LEVEL
LAW: no fabric module declares an acceptance gate"* — guard-the-guard floors, the matcher proved
live on a plant and INERT on both lawful `gate` shapes, then the victory assertion.

## §4 · MUTANTS — eight, clean-tree control FIRST

⛔ **CONTROL: exit 0, `Tests 18 passed (18)`** over the acceptance file, the single-declaration
walker and the fantasy battery. Every mutant was planted by exact single-anchor substitution
(refused on 0 or 2 matches), convicted by an executed run, and **restored digest-exact** (sha256
before/after, every one YES).

| # | branch deleted or weakened | convicted |
|---|---|---|
| M1 | the kind-membership refusal (unknown kind admitted) | **A3**, single-arm |
| M2 | the distinctness refusal on id lists | **A3 and A4** — ⚠ stronger than the compile predicted (A3 alone), because one shared helper serves both the affected set and the endpoint lists |
| M3 | the UNKNOWN-carrying-`causeRef` structural refusal | **A3** (the no-inference message pin) |
| M4 | the caller-supplied `artifactId`/`contentHash` refusal | **A2** |
| M5 | the replay divergence check (tampered record admitted) | **A2**, on an arm M4's message cannot produce — the two messages are distinct by construction |
| M6 | the split/merge endpoint validation branch | **A4**, single-arm |
| M7 | the fixture's ascending-date construction perturbed | **A5** (the ascending arm; the golden stayed green, so the conviction attributes to the right arm) |
| M8 | **GUARD-THE-GUARD:** `export const PROBE_CANON_GATE = 'X'` planted in a fabric module | the §423.1 arm, **RED, quoted verbatim in §5** |

## §5 · EXECUTED RECEIPTS

**§397 two-part S0 at base** — `node scripts/check-observed-shape-readers.mjs`, bare:
`S0_TRUE_EXIT=1` with the printed line *"observed-shape detector or unscanned execution input
changed since the schema-9 instrument was governed; an ordinary gate/write cannot migrate the
instrument"*. A real reading, not the `ERR_MODULE_NOT_FOUND` trap.

**Typechecks** — both configurations by name and window, at base and at tip:
`typecheck:domain:strict` exit 0, `[domain-strict] ✓ no strict-type regressions (1134 errors,
ceiling 1134).` · `typecheck:ratchet` exit 0, `[typecheck-ratchet] OK — no type regressions
(173 error(s), ceiling 173).` **ZERO new errors in either, and ZERO `any` casts in the new leaf.**

**Census walk, sequenced** — `files` 2503, then `parked` PASSED at 364 without ever redding, then
`credited` 2139, then `titles` **20744**, then `suiteTitles` 5791, then **GREEN at 33/33**, and
green again AFTER the cause comment landed in the counted file.

**⛔⛔ THE FOUR-RED WINDOW, QUOTED VERBATIM** (ODQ §426(4)). Decomposed in three executed readings:

1. **Strip landed, this packet NOT yet in the manifest** — exactly TWO errors, the MF-T1X half:
```
[implementation-packets] MF-T1X.requiredSymbols[145].symbol is missing from src/domain/townMap/fabric/operations.js: export const FANTASY_CANON_GATE
[implementation-packets] MF-T1X.requiredSymbols[149].symbol is missing from src/domain/townMap/fabric/index.js: FANTASY_CANON_GATE,
```
2. **At DRAFT and at READY** — exactly FOUR, the named window and nothing else:
```
[implementation-packets] MF-T1X.requiredSymbols[145].symbol is missing from src/domain/townMap/fabric/operations.js: export const FANTASY_CANON_GATE
[implementation-packets] MF-T1X.requiredSymbols[149].symbol is missing from src/domain/townMap/fabric/index.js: FANTASY_CANON_GATE,
[implementation-packets] MF-T2M.retiredSymbols[0].symbol is already absent from src/domain/townMap/fabric/operations.js before READY: export const FANTASY_CANON_GATE
[implementation-packets] MF-T2M.retiredSymbols[1].symbol is already absent from src/domain/townMap/fabric/index.js before READY: FANTASY_CANON_GATE,
```
3. **THE CONTROL** — the strip reverted in a scratch worktree with this packet present: **FULL
   GREEN**, which is what proves the four reds are the window rather than a defect.

All four clear atomically at the LANDED flip: this packet's LANDED `retiredSymbols` discharge
MF-T1X's pair rows (§379.2's H9 semantics, in the validator with its own plants), and the LANDED
arm demands exactly the absence the strip produced.

**M8's red, verbatim — the arm's minting proof:**
```
An ACCEPTANCE GATE is exported from src/domain/townMap/fabric/. ODQ 423.1 puts an operation
vocabulary in exactly two places — the store registry's verb rows and the domain's
sealed-artifact payload grammar — and refuses a module that claims both levels as a second
truth. This directory is the payload level. acceptance lives at the STORE DOOR, never in a
payload grammar (ODQ §423.1). A door verb is a row in src/store/operationRegistry.js — not a
fabric export. If this constant is payload VOCABULARY rather than an acceptance gate, name it
for what it is: the offending suffix is what makes it read as a gate:
export const PROBE_CANON_GATE =  <-  operations.js
: expected [ Array(1) ] to deeply equal []
```

## §6 · SCOPE BUDGET — measured against the ODQ §426-carried caps

| Limit | Measured | Budget |
|---|---:|---:|
| Behavior families | 1 | 1 |
| New persisted record families | 0 | ≤1 |
| Named state writers / feature flags / user-facing surfaces | 0 | ≤1 each |
| Direct production consumers | 0 | ≤2 |
| New logic-bearing production leaves | 1 | ≤2 |
| Existing logic-bearing production files modified | 2 | ≤3 |
| Registration-only edits | 3 | ≤3 |
| Handwritten files total | 9 | ≤12 |
| New/changed effective production lines | **207** (leaf 210, `operations.js` −2, `index.js` −1) | ≤260 |
| Effective lines per new leaf | **210** | ≤220 |
| Delta in a shared/hot file | 0 — no touched path is on the hot-file list | ≤15 |
| Acceptance cases | 6 (+1 standing walker arm) | ≤8 |

## §7 · DEVIATIONS FROM THE COMPILE — reported, not smoothed

1. **The A1 anti-drift prediction was wrong positionally** (§1.3). The pin ships stronger.
2. **The §3b envelope prediction assumed a WHOLE envelope** (§2b). At this base the bare gate is
   already broken; the recorded form is BROKEN→BROKEN with positive attribution.
3. **M2 convicts two arms, not one** (§4) — one shared helper serves two callers.
4. **`fabric/index.js` measures 111 effective, not §P9's published 101** (§1.4) — §P9 measured a
   different base.
5. **`operations.js` sheds 2 effective lines, not the compile's ≈10** — the removed gate legs sat
   on shared multi-key lines rather than on their own.
6. **The compile's "zero reservations on DIRECT paths" should read "zero NON-TERMINAL
   reservations"** — LANDED rows on those paths are numerous and lawful.
7. **Commit order:** A6 and the §423.1 arm land in the STRIP commit rather than the acceptance
   commit, and the census re-record lands with them. An acceptance pin for a strip that has not
   happened is red at its own commit boundary, and a census re-record before all title motion is
   complete would have to be patched twice — §P5 forbids that.
