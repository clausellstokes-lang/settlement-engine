# GR-ARCHITECTURE — THE PACT GRAMMAR, COMPILED FOR BUILD

## Fable 5 chair compile, 2026-08-04, executed READ-ONLY against
## claude/composite-r4 @ e564e135 (the minifold tree, mid-build-wave; every
## receipt below was grepped or read on this HEAD, none inherited from the
## volume). SOURCE VOLUME: docs/DESIGN_FP_GRAMMAR.md (2026-08-02, @ 38f81d05).
## TEMPLATE + BINDING PROTOCOL: docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md (§10
## binds verbatim). The tree has moved ~2 days and one whole war program since
## the volume's census; §2 below is the corrected ground truth. Where this
## document and the volume disagree, THIS document's §2 receipts win on
## substrate facts and the volume wins on design law — a conflict in design law
## is a STOP-and-report, never an implementer pick.

**Status: COMPILED IMPLEMENTATION LAYER. Nothing here is scheduled until the
owner sequences it (spine §5: GRAMMAR first among the six FP programs, behind
the sim-proof path). Every judgment below is labeled JUDGMENT and is vetoable.
Live code outranks every table in this file — re-verify anything you build on.**

---

## §1 THE LAWS BOUND TO THIS PROGRAM (WR-era laws L1–L9, GR-specific bindings)

- **L1 DETERMINISM:** every stochastic choice in GR-2/GR-6 is keyed `hash01`
  (home: `src/domain/region/contestMath.js:45`) with keys
  `gr.<facet>.<realmId>.<a>.<b>...` codepoint-ordered; weighted races are
  `w * hash01` products (the `sovereigntyMarketStage.js` header documents the
  house race idiom verbatim — both sides of every weight proven live, the
  dead-band law). `pulseKernel.js` (BANKED at 1580, scripts/.size-baseline.json)
  and `applyWorldPulse.js` receive ZERO edits — see §2 R-3 for what this does to
  the volume's mount premise.
- **L2 FLAGS:** the five GR flags take the LIVE idiom (§3), which is richer than
  the volume's statement: absent from DEFAULT_SIMULATION_RULES and every preset,
  DECLARED `false` on the full_simulation ceiling with a dated comment (the
  WR-1/2/3/6/7 precedent, `simulationRules.js:445-472`), joining
  `ENGINE_GATED_VIRTUAL_RULE_KEYS` (`simulationRules.js:185`) in the SAME commit
  as the first real `=== true` gate read — `tests/lint/
  engineGatedRuleKeys.walker.test.js` proves the manifest BOTH WAYS. Four-fence
  dormancy set per flag + lit-mutant control; one by-name read per flag (the
  conjunction-gate hole).
- **L3 EPISTEMICS:** the K3 pin set gains `pactTriggers.js`, `pactProposals.js`,
  and the renewal evaluator (import-pinned + token-scanned + guard-the-guard
  against a legitimate truth reader OUTSIDE the negotiation set — never
  `peaceTerms.js`). The `sovereigntyMarketStage.js` header records the split
  this program copies: zero-import leaves for appraisal-class math, pinned
  import lists for composers.
- **L4 PERSISTENCE:** ONE new top-level key in the whole program
  (`spatialLedgers.pactProposals`); every treaty-record extension is a
  conditional drop-when-absent field riding the `sellerId`/`buyerId`/`swapId`/
  `assetId` precedent (T4: absent, never null). §4 carries the full
  Lifecycle-paths clause, written BEFORE any writer builds.
- **L5 RECEIPTS:** banded words only in prose; comparisons in words (banding is
  lossy); scalars stay on returned reads; runtime no-decimal pins on composed
  output.
- **L6 HERALD:** every new kind lands the five joins (annex pool row · registry
  row with requiredSlots + slotless fallback · WHAT_PHRASES
  (`src/domain/display/settlementRumors.js`) · section authority
  (`src/domain/realm/heraldRouting.js`, 525 lines) · full address chain), plus
  `chroniclersLetter.js` rows, in the kind's own commit; own walker file per
  kind.
- **L7 VERIFICATION:** violation-row attribution against a git-archived base
  (node_modules symlinked); executed mutants with cp/cmp restore (NEVER
  checkout-family) + mutationCoverageManifest entries; `// anchored:` on new
  negatives; exactly-once assertions on doc-reading pins; analytic pins that
  recompute prove nothing — pin the rendered/executed artifact. THE
  MOLD-CONFORMANCE WARNING (the volume's own 89-percent finding): substring
  pins cannot see grammar — every house-voice surface this program lands gets a
  mold walker over the CLOSED template set, not a token grep; budget the
  verifier half of every wave at parity with the implementer half.
- **L8 SIZE:** measured current sizes in every wave spec below; new logic =
  lazy leaves; `scripts/.size-baseline.json` is shrink-only tolerance-0; measure
  with the enforcer Linter AT the publishing commit — the raw and effective
  counts below are chair approximations for BUDGETING only, never inheritable.
- **L9 PROCESS:** pathspec commits; python3 byte-scan every authored file (the
  NUL class, bit six times); one wave = one commit; per-wave implementer +
  adversarial verifier with reject gates; STOP-and-report on measured blockers
  is a SUCCESS mode; spine requirements 13 (Alignment) and 14 (Edit-verb story)
  discharged per wave; seam pre-contracts pinned from BOTH sides with a
  tripwire (the TR-5 / `catalogGrewSinceWr10` pattern, §6).

---

## §2 SUBSTRATE CLAIMS — RE-MEASURED 2026-08-04 @ e564e135
## (live code outranks this table; the census STOP rule binds: an implementer
## finding this table overstating the tree STOPS and reports)

### 2a REFUTED premises (the volume built on these; each carries its cure)

- **R-1 — "peaceTerms.js sits at 794 of its 800 ceiling; ALL capability in lazy
  leaves; peaceTerms gains only net-zero seam lines."** REFUTED AS ARITHMETIC.
  The file was DECOMPOSED since the census into an eleven-module writer family:
  `peaceTerms.js` (head, 1181 raw / ~766 effective by chair approximation) plus
  `peaceTermsCatalog.js` (235), `peaceTermsPrimitives.js` (108),
  `peaceTermsAppraisal.js` (291), `peaceTermsDrafting.js` (109),
  `peaceTermsCarriedSheet.js`, `peaceTermsGraph.js` (145),
  `peaceTermsCoalition.js`, `peaceTermsOverlay.js`, `peaceTermsDocument.js`
  (297), `peaceTermsSale.js` (278). peaceTerms.js has NO
  `scripts/.size-baseline.json` entry, so the layer ceiling (800 effective,
  `eslint.config.js` max-lines with skipBlankLines/skipComments) governs, with
  ≈34 effective lines of measured headroom. CURE: the writer-family law STANDS
  (one family, leaves consume the family's modules), but the "each seam line
  bought net-zero" arithmetic is RELAXED to: state the effective delta in the
  commit, stay under 800 measured at the publishing commit, prefer net-zero.
  The volume's leaf names survive; their import targets are the FAMILY modules
  (catalog/primitives/clock), not the head.
- **R-2 — "Sole mint path: war-exit PASS 1; the one-seam fact this program
  widens."** REFUTED SINCE THE CENSUS — WR-10 already widened it.
  `peaceTermsSale.js` header, verbatim: "the THIRD transport into the one
  instrument, beside the live-appraisal mint and the carried-sheet mint" — a
  VICTOR-FREE peacetime mint (`mintSovereigntySaleTreaties`), writer-family
  member, called by `sovereigntyMarketStage.js` (486 lines), mounted in
  `settlementLifecycleKernel.js:90`. CONSEQUENCES FOR GR: (a) the thesis
  sentence narrows — GR-2 adds the FOURTH transport, not the second door;
  (b) the sale is the PROVEN PATTERN GR-2 copies: a family-member leaf that
  composes `TERM_CATALOG` + primitives + `CURRENT_TREATY_TICKS_PER_YEAR`
  directly, writes through `treatyLedgerOf`/`setSpatialLedger`, is NOT
  re-exported by peaceTerms.js (deliberately — re-export would drag the caller
  into the head's measured 35-module import cycle, the dist chunk-cycle TDZ
  class; see `satellitesLedger.js` header), and whose caller lives OUTSIDE the
  cycle; (c) the volume's J-GR-1 "two net-zero exported seams on peaceTerms"
  is SUPERSEDED for the MINT side (see J-GRC-1 below); the AMENDMENT side
  still needs its seam ruling.
- **R-3 — "advancePeacetimePacts called from pulseKernel adjacent to
  advanceTreaties, flag-gated at the call site."** REFUTED TWICE. (a)
  `pulseKernel.js` is BANKED (1580 in scripts/.size-baseline.json; the standing
  R-BLD-10 law: zero edits ever). (b) The treaty advance is no longer a bare
  call: `pulseKernel.js:2580` calls `advanceTreatiesWithDisposition` (the WR-2
  wrapper in `dispositionChannels.js`, 88 lines). CURE (J-GRC-2): the peacetime
  pass mounts in `settlementLifecycleKernel.js` — the lifecycle host — exactly
  as `advanceSovereigntyMarket` does (own-flag-before-host-gate; dark ⇒ the
  stage body is never entered ⇒ byte-identical). The kernel measures 1194 raw;
  its ceiling headroom is measured at build, and the mount is a ~3-line gated
  stage call in the host's existing stage sequence.
- **R-4 — "`negotiationPictures.js`: ABSENT; BUILD-PRECONDITION for
  GR-2/5."** REFUTED IN GR'S FAVOR — it EXISTS (618 raw / ~512 effective,
  `src/domain/worldPulse/negotiationPictures.js`) with the full two-picture
  API: `createNegotiationPicture` :164, `evaluateNegotiationPicture` :396,
  `compareOfferToResponderDraft` :464, `negotiateFromPictures` :523,
  `validateTermSheetAgainstPictures` :597. The §9 build-precondition on WR-7c
  is SATISFIED; GR-2/GR-5 consume, never fork.
- **R-5 — "SP spine reads (SP-2 believed subjects, SP-4
  postureOf/riskToleranceOf) — VERIFY-AT-BUILD their landed shapes."**
  MEASURED ABSENT, plus one live collision. Zero hits for `postureOf`,
  `believedScarcity`, `believedSubjects` anywhere in src/domain. AND:
  `riskToleranceOf` ALREADY EXISTS as an NPC-level read
  (`src/domain/roads/state.js:319`, consumed by `roadsKernel.js:982`) — SP-4's
  settlement-court read MUST NOT take that name (collision would be silent at
  import-rename time — the concurrent-lane rename hazard). GR-0 and GR-1 read
  no spine surface and can build now; GR-2..GR-6 have HARD spine
  preconditions (§7 Q2).
- **R-6 — "`warIntents` ledger (W1) — GR-6's pre-war mediation surface."**
  EXISTS but the shape refutes the occasion as drafted:
  `warIntent.js:79` `WAR_INTENT_LEDGER_KEY = 'warIntents'`, record =
  `{ targetId, tick }` (typedef :98), TTL = 2 ticks
  (`WAR_INTENT_TTL_TICKS = 2`, :90 — deliberately: one retry, no more). There
  is no intent "scoring" to pressure and only a 1–2 tick window. CURE
  (J-GRC-3): occasion 1 lands as a bounded multiplier consumed at the OPENER's
  soft-gate read — the `embassyLedger.js` deposit-and-consume idiom pointed the
  other way (its header is the design law: ×1 exactly when dark/no pass ⇒
  byte-identical; a dependency-free leaf the opener imports; roads never
  writes war state, mediation never blocks a gate).
- **R-7 — "NO soak script observes the treaties ledger at all."** STALE. WR-9's
  `scripts/audit/war-convergence-collector.mjs` now reads the treaty ledger
  (`treatyPairKey` import :74; `treatyPairKeys` observation :288;
  `treatyWritten` per war :454-455). STILL TRUE and still GR-7's charter:
  compliance-state distributions, formation-vs-dictation ratio, endings mix,
  term lifetimes, stream grain moved, refusal rates — all unmeasured. GR-7's
  section extends the collector's own idiom rather than minting a rival
  script.

### 2b VERIFIED premises (receipts by symbol; line numbers WILL rot — navigate
### by symbol, the standing hand-keyed-line-address law)

| # | Claim | Receipt | State |
|---|---|---|---|
| V-1 | One treaty per unordered pair is enforced at the war door | `peaceTerms.js:348` `mintedThisPair` + comment "one treaty per unordered pair"; pair key minted by `treatyPairKey` (`peaceTermsPrimitives.js`) | VERIFIED — and the SALE door honors it by REFUSAL: `peaceTermsSale.js:246` refuses when either directed spelling of the slot is occupied (measured; see §7 Q4) |
| V-2 | TERM_CATALOG: now **12 terms / 8 families**, executor kinds `transfer/overlay/readiness_cap/war_block/occupation_hold/seam`; codepoint-frozen TERM_TYPES/TERM_FAMILIES | `peaceTermsCatalog.js:141-192` | VERIFIED with growth: `sovereignty_transfer` joined (own family, forced by §13 stacking); the volume's "extract TERM_CATALOG into a pure-data sibling" item (GR-3) is ALREADY DONE under the name `peaceTermsCatalog.js` — GR-3's catalog-mechanics slice is VOID |
| V-3 | Every term REQUIRES `expiresTick` | TermRecord typedef, `peaceTermsCatalog.js` ("REQUIRED... unrepresentable") | VERIFIED |
| V-4 | `non_intervention` catalogued, ZERO producers (the D4 tombstone) | catalog comment: "no non_intervention asset producer yet ⇒ never drafted ⇒ byte-identical" | VERIFIED — the no-orphan-vocabulary law's anchor, AND the executor:'seam' idiom GR-3's deferred-consumer rows reuse |
| V-5 | Treaty clock: 52-week current, legacy-12 by explicit marker | `treatyClock.js` `CURRENT_TREATY_TICKS_PER_YEAR`; persisted `treatyTicksPerYear` at `peaceTerms.js:230,:863` and `peaceTermsSale.js:184` | VERIFIED — GR-1's provenance discipline template |
| V-6 | Compliance under fog: observed vs true, monitor reach | `evolveCompliance` called `peaceTerms.js:642` with `{loserCapacity01, monitorReach01}`; `victorMonitorReach` :629 | VERIFIED — ONE call site, so the volume's role-generalized respelling is a real net-zero param rename; it MUST route names through `treatyOrientationOf` (`treatyOrientation.js`, 178 lines — CR-WR10-G's one-reader law; the measured failure it prevents: `String(treaty.loserId)` === `"undefined"` addressing a court nobody can find) |
| V-7 | `worstObserved` is a per-tick LOCAL overwritten onto `treaty.complianceState`; no history persists | `peaceTerms.js:634` (`let worstObserved = 'honored'`), :676 write | VERIFIED — GR-5's `worstObservedEver` is NEW WORK with a natural one-line monotone fold at exactly this site, gated `treatyRenewalEnabled` |
| V-8 | Enforcement reads bite from one dependency-free leaf; expiry lifts same tick | `treatyEnforcement.js` (179 raw; `treatyBlocksWar` :117); expiry drop `peaceTerms.js:637-641` | VERIFIED |
| V-9 | Conserved grain streams + accumulators | `computeTreatyGrainDraw` (`treatyTransfer.js`), `extractedFromLoser`/`deliveredToVictor` `peaceTerms.js:670-672` | VERIFIED |
| V-10 | Expiry/prune are SILENT in every feed | spent treaty ⇒ `delete nextLedger[key]` `peaceTerms.js:686` — no beat, no kind | VERIFIED, one addendum: WR-2 disposition learning now OBSERVES these moments (`treatyDispositionDeltas` outcome `'held'` :684, `'defaulted'` :700). GR-0's beats COEXIST with (never replace, never feed) that learning — §5 GR-0 collision map |
| V-11 | Fraying legibility + house-voice totality table | `frayingTermOf`/`treatyFrayingSummary`/`treatyDocument` in `peaceTermsDocument.js` (297); `TREATY_COMPLIANCE_VOICE` family×state at `display/treatyDocument.js:35` with FLOOR fallback :102 | VERIFIED — GR-3 adds family rows to BOTH the domain and display tables, walker-guarded |
| V-12 | Repudiation gate requires a LIVE `non_aggression` term; composer surface `repudiableTreatyPairs`; verb registered | `treatyBreach.js:47-51` `isRepudiableTreaty`; :62 `repudiableTreatyPairs`; `realmManifest.js:288` `REPUDIATE_TREATY` (src/domain/events/), offerability :301; `changeAuthorityPolicy.js` (src/domain/worldPulse/) | VERIFIED — J-GR-16's factoring premise holds exactly as ruled; treatyBreach.js is 175 raw / ~127 effective, room for the split |
| V-13 | Broken-oath cascade tellable-now | `scoreTreatyDefault` `warReasons.js:553`; war-block lift `treatyEnforcement.js`; `defaultedBy` write with CR-WR10-G needs-guard `peaceTerms.js:694-699` | VERIFIED |
| V-14 | Mediation single-source, war-exit occasions only | `findCrossPressuredMediator` `peaceTermsGraph.js:103`; invoked in `mintTreaty` `peaceTerms.js:783`; `accrueMediationTrust` `peaceTermsOverlay.js:84`, called :963 | VERIFIED — GR-6 adds occasions, never a second finder |
| V-15 | Three-lane DM proposal machinery | severity-gated drift `relationshipRulesCore.js:776` (`applyMode: severity >= 0.72 ? "proposal" : "auto"`); `actorMajorApproval.js` opt-in `routineMajorApproval === true` :75-76, EXPIRES TO DECLINE :27,:103; realm verbs `realmManifest.js` | VERIFIED — GR-4's two-mode declaration and HONOR-terminal divergence stand as ruled |
| V-16 | Relationship vocabularies + turning-point archive cap 24 | `relationshipState.js:136` `RELATIONSHIP_TURNING_POINT_CAP = 24`, append :295-298, normalize :414-419 | VERIFIED |
| V-17 | Turning-point RENDERING absent from components + pdf | grep zero in src/components, src/pdf | VERIFIED — GR-2's "the courts remember" WarFaithTab line premise holds |
| V-18 | `defensive_pact` edges: readers only, NO writer | readers now FIVE families: `warHomeCosts.js:79` (levy), `warCapacityReads.js:60` (ally relief), `thirdPartyRansom.js:70`, `warAllianceRisk.js:41`, certification notes | VERIFIED and STRONGER than the census — GR-3's `mutual_defense` found-writer value grew |
| V-19 | Generosity `credit` obligations with maturity/default | `generosityKernel.js:38,:89,:123` | VERIFIED — GR-1's obligation stamp target |
| V-20 | Embassy = war-exit channel only; bounded-multiplier idiom | `embassyLedger.js` header (deposit-and-consume; ×1 exactly when dark) | VERIFIED — the idiom GR-6's occasion-1 cure reuses (R-6) |
| V-21 | `believedMarginAtSignature` persisted; credibility ledger + fracture deltas exist; NOTHING ties a lie to a treaty | `peaceTerms.js:860`; `spatialLedgers.credibility` (`informationStatecraft.js:120`), `fractureCredibilityDeltas` :378 (consumed :1364); zero treaty tokens in the credibility region | VERIFIED — GR-0's read and GR-4's charge both NEW, both have real homes |
| V-22 | Succession-repudiation, renewal/renegotiation, standalone NAP, faith/population terms, `sworn`, `provenance:'negotiated'`, `lineage`, `oathHolderOf`, `pactProposals`, all five GR flag names | grep ZERO across src/ and tests/ | VERIFIED ABSENT — every GR symbol is virgin namespace |
| V-23 | Ruler-person read absent; the compose path exists | no `rulerOf`/`seatHolder`; `governingFactionOf` `rulingPower.js:189`, `npcInFaction` `npcLadderState.js:153`, `durableIdForRoster` `npcLedger.js:586` | VERIFIED — GR-1 composes exactly these sanctioned helpers |
| V-24 | Dossier surfaces | `WarFaithTab.jsx` TreatyBlock :109-116 (230 raw); `TreatyPanel.jsx` :18 renderAllTreaties (152 raw); `HeraldBody.jsx` (294); `pdf/lib/liveWorld.js` treaty chapter :83,:274,:344 | VERIFIED — all landing pads live; sizes small enough for line additions without leaf extraction |
| V-25 | WR-6 alliance-web read exists with a CLOSED consumer census | `readAllianceWebRisk` (`warAllianceRisk.js`, 57 raw); `tests/lint/allianceWebRiskConsumers.walker.test.js` pins EXACTLY TWO named consumers and forbids rebuilt walks | VERIFIED — GR-2's `shared_threat` trigger is a THIRD consumer: the walker reds at GR-2 and the census update must be ARGUED in the wave commit (§6 seam 4) |
| V-26 | The WR-10 catalog tripwire is live and will fire at GR-3 | `catalogGrewSinceWr10` `sovereigntyBundle.js:166`; asserted `false` at `tests/domain/sovereigntyBundleWr10.test.js:321,:388`; `sovereigntyBundle.js` derives components from TERM_FAMILIES at call time (no literals — comment-stripped scan pinned both directions) | VERIFIED — the red is RECORDED as the instruction: re-read WAR §5's degradation note, confirm the bundle widened mechanically, update/delete the tripwire pins and the WAR §3 row, all in GR-3's commit (§6 seam 1) |
| V-27 | Transit leg physics read | `hopWeeks` (`spatial/distanceRead.js`), consumed by roads/seaRoads | VERIFIED — GR-2's dark-transport `answerDueTick` source. ⚠ STANDING HAZARD: hopWeeks is CALIBRATED PER DIGEST — a whole realm spans ~2..8 march weeks, so any dwell/reach band ≥ 9 weeks refuses nothing; GR-2/GR-6 bands state their ranges against the measured digest spectrum |
| V-28 | Common-rite scorer exists for GR-3's `shared_rite` to point at | `peaceReasons.js:587` (`common_rite` off the closed faith×alignment quadrant, :55) | VERIFIED |
| V-29 | Conditional-field precedent on treaty records | `assetId` (term), `sellerId`/`buyerId`/`swapId` (treaty) — all drop-when-absent with T4 comments (`peaceTermsSale.js:172-190`, catalog typedef) | VERIFIED — GR's `sworn`/`provenance`/`lineage` fields copy this discipline byte-for-byte |
| V-30 | Herald plumbing homes | WHAT_PHRASES in `settlementRumors.js` (843); `heraldRouting.js` in src/domain/realm (525); `chroniclersLetter.js` (405); beat id idiom `wizard_news.{tick}.{kind}.{...stableParts}` (`peaceTermsSale.js:192`) | VERIFIED |
| V-31 | Flag walker + certification machinery | `ENGINE_GATED_VIRTUAL_RULE_KEYS` (5 members today incl. `sovereigntyTradeEnabled`); `engineGatedRuleKeys.walker.test.js` two-way proof incl. the JSDoc-cast spelling; certification rows in `subsystemRowsVirtual.js` | VERIFIED — §3's manifest timing follows this exactly |
| V-32 | Mount + gate idiom for peacetime treaty machinery | `advanceSovereigntyMarket` imported at `settlementLifecycleKernel.js:90`; the stage header documents crossing-not-level, treaty-as-cooldown, zero-PRNG race, pinned import list | VERIFIED — GR-2's `advancePeacetimePacts` is this stage's sibling in every structural respect |

**Tally: 32 rows VERIFIED (several with material corrections folded in), 7
premises REFUTED (R-1..R-7). The refutations are the payload: R-1/R-2/R-3
change WHERE GR builds, R-5 changes WHEN, R-6 changes one occasion's shape,
R-4 removes a precondition, R-7 trims a wave's charter.**

---

## §3 THE FLAG FAMILY (five flags; the LIVE idiom, upgraded from the volume)

| Flag | Gates | Waves | Manifest timing |
|---|---|---|---|
| `treatyLifecycleVoiceEnabled` | lapse beat, detection beat, longevity voice, lie-revealed tie, DM true-state chip | GR-0 | joins ENGINE_GATED_VIRTUAL_RULE_KEYS + ceiling-preset `false` declaration + certification row in GR-0's commit (first gate read lands there) |
| `oathHolderEnabled` | sworn stamps (treaties + credit obligations), succession question, `succession_repudiation`, repudiation credibility charge | GR-1, GR-4 | GR-1's commit |
| `pactFormationEnabled` | proposal ledger, four triggers, two-sided drafting, standalone NAP, war-overtaken closure, new term families' producers, war-end draft-lens extension (J-GR-14, disclosed) | GR-2, GR-3 | GR-2's commit |
| `treatyRenewalEnabled` | renewal window, renegotiation-from-strength, conversion, the `worstObservedEver` writer | GR-5 | GR-5's commit |
| `mediationGeneralizedEnabled` | intent-stage pressure, fraying-pact pass, temple arm | GR-6 | GR-6's commit |

Shape per flag (all five, no exceptions):
1. ABSENT from `DEFAULT_SIMULATION_RULES` **and from every preset override,
   with NO exception — manifest-only is the law.**

   > **⚠️ ERRATUM 2026-08-05 (recorded in place at the ES + WY
   > owner-amendment fold; REPORTED, NOT SILENT; vetoable in one owner
   > clause).** This item previously read: "ABSENT from
   > DEFAULT_SIMULATION_RULES and every preset override EXCEPT a dated
   > `false` declaration on the full_simulation ceiling (the WR precedent,
   > `simulationRules.js:445-472` comments — certification visibility without
   > lighting or preset-identity change)." **That exception is STRUCK.** It
   > contradicted item 3 of this same list, and the tree sides with item 3.
   >
   > **THE CONTRADICTION, EXECUTED.** Item 3 requires every flag here to join
   > `ENGINE_GATED_VIRTUAL_RULE_KEYS`.
   > `tests/lint/engineGatedRuleKeys.walker.test.js` asserts of EVERY manifest
   > member that no preset declares it, failing with "`<key>` is declared in
   > DEFAULT_SIMULATION_RULES or a preset spread, so it is no longer VIRTUAL —
   > delete it from ENGINE_GATED_VIRTUAL_RULE_KEYS and move its row to the
   > subject lane". A flag taking BOTH arms reds that walker on arrival, so
   > the struck clause and item 3 could never both be obeyed.
   >
   > **CR-WR10-C SUPERSEDES.** `src/domain/worldPulse/simulationRules.js`'s
   > own manifest docblock records the WR-9a fork as exactly two cures —
   > declare each key `false` in the `full_simulation` spread (+32 serialized
   > bytes per key on every NEW campaign, a moved new-campaign state hash), or
   > teach the census to enumerate the keys the engine actually gates on (zero
   > bytes on every path) — and states: "CR-WR10-C ruled the second." The
   > struck clause pointed a GR implementer at the arm the ruling REJECTED.
   >
   > **THE WR PRECEDENT IS REAL, BUT IT IS NOT PRECEDENT FOR THIS.** The
   > comments the struck clause cited do exist (`warTerminationEnabled`,
   > `dispositionChannelsEnabled`, `lineageClaimEnabled`, each `false` on the
   > ceiling), but those are PRE-MANIFEST HISTORY — CQ5 recorded the five WR
   > flags as exactly that, "not doctrine". MEASURED at `32cc17f7`: all three
   > carry ZERO membership in `ENGINE_GATED_VIRTUAL_RULE_KEYS`, which is
   > precisely what keeps them lawful. The conjunction the struck clause
   > licensed exists nowhere in the tree.
   >
   > **BOTH GR WAVES INDEPENDENTLY RE-DERIVED THIS AND BUILT MANIFEST-ONLY.**
   > `treatyLifecycleVoiceEnabled` (GR-0) and `oathHolderEnabled` (GR-1) sit
   > in `ENGINE_GATED_VIRTUAL_RULE_KEYS` and in NO preset — measured at
   > `32cc17f7`, exactly two occurrences of those names in
   > `simulationRules.js`, both inside the manifest. **The built code was
   > already right; only this document was wrong.** This erratum corrects the
   > document and re-rules nothing.
2. Every gate reads strict `=== true` (JSDoc-cast spelling is walker-legal).
3. Joins `ENGINE_GATED_VIRTUAL_RULE_KEYS` in the SAME commit as the first gate
   read; `engineGatedRuleKeys.walker` and the certification totality walker
   red otherwise — this is now MEASURED machinery, not doctrine.
4. FOUR-FENCE dormancy set per flag (own-footprint golden · absent-vs-false
   differential · call-path spy · gate-polarity census) + the lit-mutant
   control proving the fences see; one by-name gate read (the frozen-list
   `.every()` hole).
5. Dependencies (unchanged from the volume, re-affirmed): `treatyRenewalEnabled`
   ⇒ `pactFormationEnabled`; lighting order = build order; the GR-7
   certification walker reds out-of-order lighting. Cross-program interactions
   carried verbatim: `envoyDiplomacyEnabled` transport shift (disclosed,
   transport-mode field on every formation receipt), `oathHolderEnabled` ×
   `pactFormationEnabled` (reserve reads the charge band, clean-by-absence when
   dark), `seatBooksEnabled` (INT-1, degraded arm declared), and
   `routineMajorApproval` (opt-in rule, not a program flag; GR-4 two-mode).

---

## §4 CANONICAL MODEL — new state, deliberately small (fought to ONE new
## top-level key)

```
worldState.spatialLedgers.pactProposals    — GR-2; writer pactProposals.js (ONE
  [ { id, from, to, trigger,               //   writer; the volume's §4 shape
      sheet: { terms: [...] },             //   verbatim: five closed triggers
      openedTick, answerDueTick, state,    //   incl. 'renewal'; states open |
      transport } ]                        //   signed | refused | expired;
                                           //   transport: 'abstract'|'envoy'
                                           //   (§3 disclosure, on the RECORD
                                           //   because receipts are built from
                                           //   it). Cap MAX_OPEN_PROPOSALS
                                           //   (band, default 2). A queue, not
                                           //   an archive: settled rows prune
                                           //   after their receipt lands.

treaty record extensions (conditional, drop-when-absent — the sellerId/buyerId
precedent verbatim; NO new top-level key):
  sworn: { [settlementId]: { npcId, name, swornTick } }     — GR-1
  provenance: 'dictated'|'negotiated'|'renewed'|'converted' — GR-2/5; legacy
                                           //   unmarked ⇒ 'dictated' AT READ,
                                           //   never rewritten (V-5 discipline)
  lineage: [ { act, tick, termIds, ending? } ]              — GR-2/5 (R1 law)
  worstObservedEver: 'honored'|'strained'|'defaulted'       — GR-5; MONOTONE,
                                           //   written ONLY under
                                           //   treatyRenewalEnabled at the V-7
                                           //   fold; read `?? 'honored'`
  TermRecord.beneficiary: settlementId|'both'               — GR-2/3; §13
                                           //   stacking keys family ×
                                           //   beneficiary for negotiated
                                           //   lineage; war door unchanged
  breachType value 'succession_repudiation'                 — GR-4 (a vocabulary
                                           //   member, not a key)

succession questions — NO new ledger: dark mode scores directly at the event;
lit mode rides worldState.proposals (actor-major shape, V-15), terminal
HONOR (declared divergence from expire-to-DECLINE, pinned both ways).
```

**Lifecycle-paths clause (L4, written before any writer builds):**
- **create:** proposals only via `pactProposals.js` (trigger crossing or the
  `PROPOSE_PACT` verb handler); treaty extensions only via the writer family at
  mint/lineage-act time; `worstObservedEver` only at the V-7 fold.
- **read:** proposals through the module's own reader (writer/reader spelling
  pinned by booting the REAL writer); provenance/lineage/sworn through
  dedicated read helpers that resolve legacy-absent to the declared defaults
  (`'dictated'`, single-act, seat-voice) — no consumer touches raw fields.
- **persist:** all fields ride `spatialLedgers`/treaty serialization as-is;
  JSON-round-trip pins on every new field; the `sworn.npcId` stamp
  JSON-round-trips against the ALIAS TRAP (roster objects).
- **regenerate (THE PROMISE):** open proposals are RE-DERIVED from re-scored
  triggers (J-GR-8 — not precious state); treaties regen through their
  existing pins with extensions riding the record; GR-1 re-stamps the
  regenerated world's seat-holder (THE SAME-SEAT pin); settled succession
  questions never re-open.
- **undo:** ledger and treaty extensions restore byte-true with the record;
  an open question restores with `worldState.proposals`.
- **migrate/import:** NOTHING migrates — legacy records carry NO new field and
  every reader tolerates absence forever (the treatyTicksPerYear provenance
  discipline, V-5); import validates proposal trigger tokens against the
  closed five and `sworn.npcId` against the roster, degrading to seat-voice on
  a dangling ref (never a crash, never an invented name).
- **veil:** proposals and treaty extensions carry no DM-only truth EXCEPT the
  GR-0 true-state chip (`includeGroundTruth` projection) and `hollowed_quiet`
  (DM-only ending); every public payload builder already returns through
  veilPublicPayload (standing law) — the new fields join the audited
  projection, pinned.

**Deliberately NOT modeled** (volume list re-affirmed): no negotiation-session
state, no counter-offer object, no peacetime margin budget, no new confidence
stock, no multilateral anything, no anniversary scheduler, no second mediation
finder, no new relationship vocabulary.

---

## §5 THE WAVES (dependency order; one wave = one commit; every flagged wave
## DARK per §3; per wave: files + measured sizes, leaves + budgets, pins,
## mutants, dormancy, req 13/14, collision map. The volume's §5 prose remains
## the design narrative; THIS section is what changed under re-measurement.)

### GR-0 — THE LIFECYCLE VOICE (`treatyLifecycleVoiceEnabled`)
**Builds NOW (no spine reads — R-5).**
- **Files touched:** `peaceTerms.js` (~766 eff/800 — the lapse/detection beat
  hooks are ~4-6 gated lines at the V-10 prune site and the V-6 compliance
  crossing; state the delta, prefer net-zero via moving a read-model line into
  `peaceTermsDocument.js`); `display/treatyDocument.js` (208 raw — age line);
  `WarFaithTab.jsx` (230) / `TreatyPanel.jsx` (152 — DM chip behind
  `includeGroundTruth`) / `pdf/lib/liveWorld.js` (age line); herald joins in
  `settlementRumors.js` + `heraldRouting.js` + `chroniclersLetter.js`.
- **New leaf:** `treatyLifecycleVoice.js` (budget ≤200 eff): beat composition
  for `treaty_lapsed` / `treaty_default_detected` + `ageYears` read +
  `treatiesPricedDuring(pair, window)` (the GRAMMAR×INFO read — landed here,
  consumed by FP-INFORMATION later; §6 seam 5).
- **Pins:** the volume's set verbatim (fog negative hardest: undetected cheat
  ⇒ NO beat; ending-totality walker; twenty-year eulogy; lie-window negative;
  id + address walker) PLUS: the DETECTION CROSSING pin must key on the
  OBSERVED transition (first crossing out of honored), which at V-6's one call
  site means comparing against the term's PRIOR observedState — seed a
  two-tick fixture, never a single-tick harness (vacuous-absence law).
  Four-fence set + lit-mutant for the flag. Mold walker on the eulogy voice
  (L7's mold-conformance warning — this program's own 89-percent lesson).
- **Mutants:** the prune-site hook (delete the gated push ⇒ ending-totality
  walker reds); the DETECT_FLOOR conjunction.
- **Dormancy:** dark ⇒ zero new news kinds in the stream golden; the V-10
  disposition deltas are PRE-EXISTING lit-path behavior of
  `dispositionChannelsEnabled` and must be excluded from GR-0's own-footprint
  fence (collision below).
- **Req 13/14:** Alignment — engagement: the eulogy names the age; Edit-verb —
  none (no DM-editable surface; the chip is read-only projection). Declared.
- **Collision map:** WR-2's `treatyDispositionDeltas` fires at the SAME two
  sites (V-10) — GR-0's beats never feed disposition learning and the
  own-footprint golden runs with `dispositionChannelsEnabled` dark to isolate;
  pacing governor: all four kinds register significance (SP-6 family
  assignments only).

### GR-1 — THE OATH-HOLDER IDENTITY (`oathHolderEnabled`)
**Builds NOW (composes existing helpers — V-23).**
- **Files touched:** the writer family's THREE stamp sites (war mint
  `mintTreaty`, carried-sheet mint, sale mint `peaceTermsSale.js` — the volume
  said "both doors"; the tree now has THREE, and the sale door stamps too or
  the walker that asserts stamp totality across mint paths reds);
  `generosityKernel.js` credit mint (V-19).
- **New leaf:** `oathHolder.js` (budget ≤120 eff): `oathHolderOf(worldState,
  settlementId) -> { npcId, name } | null` composed EXACTLY from
  `governingFactionOf` → `npcInFaction` → codepoint-stable pick →
  `durableIdForRoster` (V-23 receipts). Null ⇒ no stamp ⇒ seat-voice.
- **Pins:** volume set verbatim (legacy-unstamped negative; JSON alias
  round-trip; death changes NOTHING; SAME-SEAT determinism + regen re-stamp;
  null contract) PLUS the THIRD-DOOR pin: a sovereignty sale minted under the
  flag carries stamps for buyer and seller (fixture through
  `mintSovereigntySaleTreaties`).
- **Mutants:** the codepoint tie-break (reverse it ⇒ SAME-SEAT pin reds); the
  null guard.
- **Dormancy:** dark ⇒ no `sworn` key ever written (absent-key byte golden).
- **Req 13/14:** Alignment — engagement (the signature line); Edit-verb — none,
  declared with reason (stamps are engine history, never DM-edited).
- **Collision map:** `peaceTermsSale.js` is WR-10's file — coordinate the stamp
  hook as a small gated addition with the war chair's queue (FABLE_VALIDATION_
  QUEUE.md row); the alias trap (roster objects) is the recorded hazard class.

### GR-2 — PEACETIME FORMATION + THE STANDALONE NAP (`pactFormationEnabled`) — **LANDED 2026-08-06 @ `a18fdcfa`, repaired @ `91075d45`**
<!-- Build state is not maintained per-file; FP §5's PROGRESS block is the one home.
     The "BLOCKED on spine SP-2/SP-4" line immediately below is DISCHARGED, not live:
     SP-2/SP-4 are the pre-fold spellings of SP-B (`4c0f2f38`) and SP-C (`cbd348a5`),
     both landed before this wave. It is left standing because the sentence carries the
     wave's dependency reasoning, which is still the right reasoning. -->

**BLOCKED on spine SP-2/SP-4 (R-5; §7 Q2). Compiled deltas vs the volume:**
- **Mount (R-3 cure, J-GRC-2):** `advancePeacetimePacts` mounts as a stage in
  `settlementLifecycleKernel.js` beside `advanceSovereigntyMarket` (:90),
  own-flag-before-host-gate; NEVER a pulseKernel edit.
- **Mint/amend (R-2 cure, J-GRC-1):** `pactFormation.js` is a writer-FAMILY
  member on the `peaceTermsSale.js` pattern: composes
  `TERM_CATALOG`/primitives/clock directly, writes through
  `treatyLedgerOf`/`setSpatialLedger`, NOT re-exported by peaceTerms.js, caller
  outside the head's import cycle. The AMENDMENT act (standing instrument ⇒
  lineage append) lives in `pactAmendment.js` (family member, same pattern);
  the war door's amendment awareness (J-GR-14b) is the ONE peaceTerms.js edit
  this wave makes (the PASS 1 pair-slot guard's gated arm), delta stated.
- **New modules + budgets:** `pactTriggers.js` (pure, ≤220 eff),
  `pactProposals.js` (one writer, ≤250 eff), `pactFormation.js` (≤200 eff),
  `pactAmendment.js` (≤180 eff).
- **Transport:** dark ⇒ `answerDueTick` from the `hopWeeks` leg read (V-27 —
  dwell bands stated against the measured 2..8-week digest spectrum, the
  calibrated-weeks hazard); lit ⇒ SP-1/WR-7 envoy, purpose `diplomatic`,
  transport-mode on every receipt.
- **Pins:** the volume's full set (refusal / no-overlap / dependency-fear /
  posture / spam / four-trigger reachability / NAP symmetry / stacking-refusal
  / two-sided compliance / serial-disavower / DM parity / war-overtaken /
  transport-mode / dormancy) with these compiled additions: the K3 pin set
  gains the two new modules with guard-the-guard against a truth-reader
  OUTSIDE the set; the writer/reader payload pin boots the REAL
  `pactProposals.js` writer; the SALE-COEXISTENCE pin — a pair holding a live
  sale instrument receives a pact proposal and the acceptance AMENDS that
  instrument (one record at the pair key, V-1) while the sale door's refusal
  behavior is asserted UNCHANGED (§7 Q4's measured baseline).
- **Mutants:** the two-sided conjunction (drop one side ⇒ no-overlap pin
  reds); the cap; the stacking-refusal receipt.
- **Dormancy:** dark ⇒ the kernel stage body never entered ⇒ byte-identical
  (the sovereigntyMarketStage fence pattern verbatim).
- **Req 13/14:** Alignment — engagement (posture prices proposals); Edit-verb —
  `PROPOSE_PACT` with the full §10.7 registration set (manifest row + dials +
  typed veto codes + authority row + decisionTier + handler + parity pin).
- **Collision map:** `allianceWebRiskConsumers.walker` — GR-2 is the THIRD
  consumer of `readAllianceWebRisk`; the census update is ARGUED in-commit
  (V-25, §6 seam 4). `sovereigntyMarketStage` — two peacetime stages now run
  in the host: stage ORDER is fixed and pinned (market first, pacts second —
  JUDGMENT, vetoable; rationale: a court that just sold is a court whose
  believed books changed, and the pact trigger should read the post-sale
  world). SP-4 court reads must NOT be spelled `riskToleranceOf` (R-5's
  collision).

> **Current implementation handoff (reconciled 2026-08-09):** GR-3a is landed;
> GR-3b is owed but **BLOCKED**, not dispatchable from this design. The only
> coding contract is
> [`implementation/packets/foreign-policy/GR-3B.md`](./implementation/packets/foreign-policy/GR-3B.md).
> Do not infer its unresolved producer-selection rules from the prose below.

### GR-3 — THE NEW TERM FAMILIES (second slice of `pactFormationEnabled`) — **GR-3a LANDED 2026-08-06; GR-3b OWED/BLOCKED**
<!-- Build state is not maintained per-file; docs/implementation/INDEX.md controls current
     dispatch and FP §5 retains dated corroboration only. What
     follows is the SCOPE SPLIT, which is not build state but a change to what this wave IS.

     GR-3a (landed): the twelve catalog rows, the seventh `grant` executor and its seven
     named reads in treatyEnforcement.js, the three house-voice family rows, the three
     seam-6 coupling rows, and THE WR-10 TRIPWIRE DISCHARGE — which was this wave's
     scheduling reason and is now done, so every later catalog-touching wave (TR-5, WF-6,
     POP-5b) inherits a fired-and-discharged wire instead of a live one.

     GR-3b (owed): THE PRODUCER SIDE. The nine faith/population/security rows have no
     producer, because their producer is `PACT_DRAFT_LENS` in pactFormation.js and a
     second build lane held that file under uncommitted strict-typing work for the whole
     of GR-3a's build window; editing it would have meant committing another lane's
     unfinished change to land this one. That is a MEASURED blocker, reported rather than
     raced. The ladder primitive ships (`orderTermsByAsk`, exported and unconsumed, the
     GR-0 handoff idiom) and the debt is held by a FROZEN shrink-only register in
     tests/domain/peaceTermsGrantTerms.test.js, so it cannot be lost.

     ALSO DEFERRED BY NAME: J-GR-14a, the war-end appraisal lens extension. Giving the new
     families asset classes is a lit-path behaviour shift on golden-pinned war-end
     drafting and deserves its own commit and its own disclosed-shift pin; J-GR-14b (the
     war door's amendment awareness) already landed with GR-2. The struck premise in
     peaceTermsAppraisal.js's D4 seam note is corrected in place.

     FOUND WHILE BUILDING, OWED TO THE CHAIR: `reparations` is a PRODUCER-LESS TERM and
     pre-dates this program — working executor, stream-physics home, label and drafting
     line, and no CLASS_TERM asset class maps to it, so draftTerms can never select it.
     `non_intervention` by accident rather than by declaration. Both cures (an asset
     class, or a seam declaration) move war-end goldens, so it is RECORDED in the owed
     register under CHAIR rather than repaired by an implementer. -->

- **Catalog mechanics VOID (V-2):** the extraction is done; the new rows land
  directly in `peaceTermsCatalog.js` (235 raw — headroom fine). Rows: five
  faith, three population, `mutual_defense`, each with the volume's executor
  and the `grant` (7th) executor kind consolidated in `treatyEnforcement.js`
  (J-GR-5; 179 raw — the reads fit).
- **THE TRIPWIRE DISCHARGE (V-26, §6 seam 1):** GR-3's commit updates
  `tests/domain/sovereigntyBundleWr10.test.js:321,:388` and the WAR §3 row per
  the tripwire's own recorded instruction; `sovereigntyBundle.js` widens
  MECHANICALLY (derives from TERM_FAMILIES at call time) — the new pin asserts
  the bundle now composes the new families and that `catalogGrewSinceWr10()`
  returns the post-GR-3 truth.
- **Trade-rights rows:** per §7 Q1's recommendation, GR-3 ALSO mints
  `exclusivity` / `market_access` / `toll_exemption` as executor:'seam' rows
  (the V-4 non_intervention idiom: no producer ⇒ never drafted ⇒
  byte-identical) under GRAMMAR spellings; TR-5 lands executors + producers.
- **Pins:** volume set (no-producer-less-term walker with the seam-row
  exemption EXPLICIT; grant lift; Law One negatives; freeze walker
  same-commit; found-writer pin on the V-18 readers; J-GR-14 lighting pin) +
  the war-door stacking pin against family × beneficiary.
- **Dormancy:** catalog rows with no producer are byte-identical by the V-4
  mechanism (measured precedent, not hope); the J-GR-14 lens extension is
  flag-gated with the disclosed-shift pin.
- **Req 13/14:** Alignment — declared-empty with reason (catalog data);
  Edit-verb — none. **Collision:** TREATY_COMPLIANCE_VOICE gains rows in BOTH
  tables (V-11), walkers same-commit; `informationStatecraft.js:1416` notes a
  planned share-sell TERM_CATALOG row (FP-INFORMATION) — spellings coordinate
  through the canonical-list contract (J-GR-15).

### GR-4 — THE SUCCESSION QUESTION (second slice of `oathHolderEnabled`)
- Volume spec stands whole; compiled deltas: the charge writes through the
  MEASURED credibility home (`spatialLedgers.credibility`,
  `informationStatecraft.js:120`; the `fractureCredibilityDeltas` idiom :378);
  `treatyBreach.js` factoring per J-GR-16 fits in the file (V-12);
  the succession event list is WR-5's D re-read surface — VERIFY-AT-BUILD its
  landed symbol (the war program built through WR-10; navigate by symbol).
- **Pins:** volume set verbatim (silence-honors hardest; tribute-only
  disavowal with `repudiableTreatyPairs` asserted UNCHANGED; dark-mode
  silence-equivalence; HONOR-terminal divergence; composition fixture;
  price differential; both-paths charge + dark negative).
- **Req 13/14:** Alignment — engagement (posture colours the answer);
  Edit-verb — none new (the question rides the existing actor-major surface).
- **Collision:** `worldState.proposals` payload shape VERIFY-AT-BUILD;
  the charge is a disclosed lit-path addition to WR-0c's verb (J-GR-13).

### GR-5 — RENEWAL, RENEGOTIATION, CONVERSION (`treatyRenewalEnabled`)
- Volume spec stands; compiled deltas: `worstObservedEver` lands as the
  one-line monotone fold at the V-7 site (gated, dark ⇒ never written);
  renewal proposals ride `pactProposals` (trigger `renewal`); lineage acts
  execute through `pactAmendment.js` (GR-2's leaf — no new writer);
  re-stamping via GR-1 at every act.
- **Pins:** volume set (refusal-of-renewal ⇒ clean lapse hardest; ask-cap;
  conversion negative on the RECORDED history; K3 extension; multi-act
  JSON-round-trip; dormancy).
- **Req 13/14:** Alignment — engagement; Edit-verb — none. **Collision:** the
  V-7 fold sits beside WR-2's disposition deltas — the fence isolates as in
  GR-0.

### GR-6 — MEDIATION GENERALIZED (`mediationGeneralizedEnabled`)
- **Occasion 1 recompiled (R-6 cure, J-GRC-3):** a dependency-free leaf
  (`mediationPressure.js`, ≤120 eff) the opener consumes — the embassyLedger
  idiom mirrored: reads a live `warIntents` row + the single finder
  (`peaceTermsGraph.js:103`), lands a bounded multiplier on the opener's soft
  gate, ×1 exactly when dark/no-broker/no-intent ⇒ byte-identical. The 2-tick
  TTL is DECLARED in the wave (the window IS one deliberation), and the
  "war that did not happen" receipt mints when a pressured intent expires
  unopened.
- Occasions 2 and 3 per the volume (fraying read + `worstObservedEver ??
  'honored'`; temple arm through `cohesionWeave` facet resolution).
- **Pins:** volume set (never-forces hardest; interested-broker dual-face;
  distant-broker vs the V-27 calibrated reach band; single-finder source-scan
  walker; both-edge trust). **Req 13/14:** Alignment — engagement; Edit-verb —
  none. **Collision:** the opener consumer census (whatever walker guards
  `warIntent.js` consumers) re-measured at build; `accrueMediationTrust`
  reused, never forked.

### GR-7 — MEASUREMENT + CERTIFICATION (no flag)
- Extends `war-convergence-collector.mjs`'s OWN idiom (R-7) with the treaty
  section: compliance distributions, formation-vs-dictation THESIS METRIC,
  endings mix (closed vocabulary with every producer named, the volume's §GR-7
  list), term lifetimes on the V-5 clock, stream grain moved, formation +
  mediation mixes with their floors; certification rows for all five flags
  (subsystemRowsVirtual pattern, V-31); the out-of-order-lighting red;
  ⚠ never a `wizard_news.*`-fed identity on a certification row (standing
  moverFamily hazard). Succession-mix envelope declares its
  `routineMajorApproval: true` profile precondition. Every envelope carries an
  executed mutant negative control.
- **This wave closes the program: DONE when the envelopes hold on an
  owner-ordered soak, and not before.**

---

## §6 SEAM CONTRACTS (pre-pinned both ways, each with its tripwire)

1. **GR-3 ↔ WR-10 (the catalog tripwire — ALREADY PINNED FROM THE OTHER
   SIDE):** `catalogGrewSinceWr10()` (`sovereigntyBundle.js:166`) reds at GR-3
   by RECORDED DESIGN; GR-3's commit executes the tripwire's instruction
   (re-read WAR §5's degradation note, assert the mechanical bundle widening,
   update the two test assertions and the WAR §3 row). Honored, not fought.
2. **GR-3 → TR-5 (trade-rights):** GR-3 mints the three rows as
   executor:'seam' (GRAMMAR spellings canonical, J-GR-15/CR-WR10-B); TR-5
   lands executors + producers. Tripwire: a GR-3 pin asserts the three rows
   remain producer-less seams (reds the day TR-5 lands — the instruction to
   move the reachability obligation to TR-5's commit).
3. **GR-2/5 ↔ WR-7 (transport + pictures):** consume `negotiationPictures.js`
   (R-4) and the envoy errand (purpose `diplomatic`); transport-mode field on
   every formation receipt; dark dwell = `hopWeeks`. Tripwire: the
   transport-agreement band pin (dark clock vs lit clock agree within band).
4. **GR-2 ↔ WR-6:** `readAllianceWebRisk` gains its THIRD consumer; the
   closed-census walker (`allianceWebRiskConsumers.walker.test.js`) is the
   tripwire and the update is argued in GR-2's commit — never absorbed.
5. **GR-0 → FP-INFORMATION:** `treatiesPricedDuring` lands in GR-0 with
   find/miss pins; INFO wires the exposure-path consumer (coupling row
   GRAMMAR×INFO). Tripwire: the GR-0 pin asserting the read is exported and
   UNCONSUMED in src (reds when INFO wires it — the handoff signal).
6. **GR-3 → FAITH (WF-6) / POP / TRADE consumer-dark arms:** grant reads
   (`missionaryAccessFor` etc.) land with producer-side pins; consumer-side
   reachability DEFERRED BY NAME in each coupling-registry row
   (`couplingRegistry.js` per §10.6 — the walker asserts the row lands with
   the read's commit).
7. **GR-4 ↔ WR-0c:** the repudiation credibility charge is a disclosed
   lit-path addition under `oathHolderEnabled` (J-GR-13); pinned both-paths +
   dark-negative.
8. **GR-* ↔ WR-2 (disposition learning):** GR endings mint NO new disposition
   outcomes — `treatyDisposition.js`'s outcome vocabulary is WR-2's surface; a
   GR wave wanting a new outcome is a STOP-and-report to the war chair.
   Tripwire: the outcome-vocabulary freeze pin (if one exists at build;
   otherwise GR-0 lands it — one line, both programs safer).
9. **GR-2 ↔ INT-1 (`seatBooksEnabled`):** the acceptance reserve names
   `booksOf` behind both flags; dark ⇒ posture+risk only, receipt names its
   inputs (declared degraded arm — the §3 disclosure).

---

## §7 OPEN CHAIR QUESTIONS (max 4, each with the drafting chair's
## recommendation — JUDGMENT, vetoable)

- **Q1 — Trade-rights rows: GR-3's commit or TR-5's?** The WAR volume
  (CR-WR10-B, 2026-08-04) says GR-3 "must mint the trade-rights catalog rows";
  the GRAMMAR volume's GR-3 lists faith/population/security only.
  RECOMMENDATION: GR-3 mints the three rows as executor:'seam'
  (byte-identical by the V-4 mechanism), TR-5 lands executors/producers —
  seam 2's tripwire keeps both honest. Cost of waiting instead: WR-10's
  bundle stays unnameable on trade rights for another program's duration.
- **Q2 — The spine gap.** GR-2..GR-6 read SP-2/SP-4 surfaces that are grep-zero
  in the tree (R-5). RECOMMENDATION: sequence GR-0 and GR-1 immediately (no
  spine reads; both are pure density wins), hold GR-2+ behind SP-2/SP-4, and
  rule NOW that SP-4's court reads are spelled `courtPostureOf` /
  `courtRiskAppetiteOf` — never `riskToleranceOf`, which is TAKEN by an
  NPC-level read at `roads/state.js:319` (a silent-collision class this estate
  has been bitten by).
- **Q3 — GR-6 occasion 1's mount.** The intent ledger is `{targetId, tick}`
  with TTL 2 (R-6). RECOMMENDATION: the bounded-multiplier leaf at the
  opener's soft gate (J-GRC-3, the embassy idiom mirrored); the alternative —
  extending the intent record with a scoring surface — touches a war-owned
  ledger shape for one consumer and is the worse trade.
- **Q4 — The sale door vs the one-instrument law.** MEASURED: a standing
  instrument at the pair key makes `mintSovereigntySaleTreaties` REFUSE
  (`peaceTermsSale.js:246`). Once GR-2's amendment seam exists, should a sale
  against a standing instrument become a lineage act instead of a refusal?
  RECOMMENDATION: not in GR's build — record it as a post-GR-2 harmonization
  candidate owned jointly with the war chair; GR-2 pins the sale door's
  refusal behavior UNCHANGED so the later ruling starts from a fenced
  baseline.

---

## §8 IMPLEMENTER PROTOCOL
DESIGN_WAR_RULINGS_ARCHITECTURE.md §10 binds verbatim; the GRAMMAR volume's
§10 items 1–7 bind with these compiled corrections: item 1's "never mint a
treaty outside peaceTerms.js" now reads "outside the peaceTerms WRITER FAMILY"
(R-2 — the sale precedent is the family's own definition); item 5's ceiling
obligation uses the MEASURED ~766/800 figure re-measured at each publishing
commit, never inherited — including from this document.
