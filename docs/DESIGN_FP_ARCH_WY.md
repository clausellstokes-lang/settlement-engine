# DESIGN — THE WAYFARE PROGRAM (WY: the inhabited roads, the physical flows, the mirror law)

## docs/DESIGN_FP_ARCH_WY.md — A LANDED MEMBER OF THE docs/DESIGN_FP_ARCH_*
## FAMILY; WY is the ELEVENTH chartered volume prefix (admitted at §5b item 7,
## in the same commit as this program's first cross-layer registry row).
## Master architect, 2026-08-05; PROMOTED TO REPO FORM AT THE QUEUE FOLD,
## 32cc17f7 (stamped by the landing lane). THE OWNER-AMENDMENT VOLUME that refines and
## coheres the full directive set of 2026-08-05 (memory/
## rumor-durability-and-absolute-distance-directive.md — sections 1, 2, 2b, 2c,
## 2d, 2e, 2f, the mid-run additions 2g, 2h, 2i, 2j, and the post-r2 additions
## 2k (MILITARY SUPPLY) and 2l (OCCUPATION IS BOTH-SIDED PROSPERITY), absorbed
## here per 2g's own instruction — DIRECTIVE SECTIONS 1 THROUGH 2l ARE ALL
## ABSORBED AS OF THIS PASS: 2k lands in WY-8 (the army-facets wave, slices
## 8a/8b, field F9) and 2l routes as the §5b item 10 VERIFY-THEN-PIN fold
## obligation on seam 1) into ONE program: the physics charter (absolute
## distance, severity-durable rumor truth, the regime-flip house idiom), the
## canonical mover model, the surface charter (four map layers, the popup
## family, the single-projection mirror law), the closed encounter-pairs table,
## twelve dependency-ordered waves in two lanes, and the queue amendment the
## chair lands (§5b — written to land VERBATIM).
##
## COMPILE PROVENANCE. Compiled from FIVE substrate censuses
## (CENSUS-{MAP,DOSSIER,ARMY,MOVERS,FLOWS}.md) authored as SESSION-SCRATCHPAD
## COMPILE INPUTS — they are NOT repo files, they do not land with this volume,
## and every premise they carry is RE-MEASURED AT BUILD by the wave that
## consumes it. Every load-bearing premise below was re-measured against the
## live minifold worktree (branch claude/composite-r4; censuses at HEAD
## e30770bd, drift-checked to c7933e84; the r1 compile at c7933e84; the r2
## REVISION at d7ea69a4 — the first cohesion round's fifteen findings CLOSED in
## place, every corrected premise re-measured at d7ea69a4, and the five chair
## rulings BAKED as §7's vetoable record; drift-checked to b441bca5 (three
## GR/TR-lane commits landed mid-revision; zero premise files touched, the
## WarFaithSection twelve re-confirmed); the r3 AMENDMENT (second-architect
## pass) absorbing directive sections 2k and 2l, which the directive gained
## AFTER the r2 read — 2k into WY-8's two slices + the F9 sign-off row + the
## `armySupplyEnabled` flag; 2l into §5b item 10 + seam 1 + §10 — with the §0
## spine, §3f facet grounding, §7 Q2 supply note, §8 tuning rows, and §9
## sentences updated to match). THIS PROMOTION carries the volume into its repo
## home unchanged in substance: header provenance, the §7 ruling record's
## promotion note, and the self-reference are the ONLY transformations — no
## wave spec, no section number, no ruling text, and no seam row moved.
##
## THE CHAIR RULINGS ARE RECORDED, NOT OPEN (Q1-Q5; §7 is the authoritative
## record — ruled in the FIRST ruling round by the chair, 2026-08-05, baked at
## r2 and carried here unchanged; every one VETOABLE, the owner's veto window
## is the queue fold, and the fold rows all five into
## docs/FABLE_VALIDATION_QUEUE.md): **Q1 faith-cargo conservation — the drafted
## BLEND shape AS DRAFTED** (the column carries the banded composition; arrival
## pulls the destination mix by arrivals/(destPop+arrivals), renormed, through
## religionState's own writer family; the popup says "faiths carried", never
## "faith conserved"; the HEADS re-founding recorded owner-elective). **Q2 home
## units — DECLARE-ABSENT** (the same `armyProjection` with the completeness
## map declaring the structurally absent field-army facets; NO owner-gated
## home-unit record is minted). **Q3 the WarFaithMapOverlay deployment arrows —
## DELETED under the ambiguity law** (movement telling on the canvas;
## wave-assigned to WY-0, both halves together, owner-visible with the veto
## window open until the wave lands). **Q4 the popup primitive — PortablePopup
## MODAL v1** (clicking is reading; canvas occlusion while reading is ACCEPTED;
## the anchored popover is RECORDED-NOT-BUILT until the modal measurably
## fails). **Q5 the caravan layer — SHIPS ON WY-3 against the M6a NON-FOOD
## shipments, with an HONEST LEGEND** (the legend says what the layer shows
## now; grain caravans join AUTOMATICALLY when TR-4 lights, because the layer
## binds to the LEDGER, not the flag).
##
## ⚠ THE HEADER HAZARD BLOCK — read before building any wave:
## - **LIVE CODE OUTRANKS EVERY TABLE IN THIS DOCUMENT.** The tree is LIVE with
##   concurrent build lanes: navigate by SYMBOL, never by any line number, file
##   list, or count quoted here, and CHECK-GIT-FIRST before every wave (§5's
##   collision law; WY-7/WY-8 land only in a quiet window for the war-surface
##   files).
## - **THE r3 CHAIR TRANSLATION, RESTATED (the seam-1 contract; binding on
##   WY-8 slice 8a):** low supply drags the ARMY'S OWN `accumulatedAttrition`
##   — NEVER the realm's `warExhaustion`. §3f's three-exhaustions labeling IS
##   the contract: the army's wear is "condition", the realm's is "the home
##   front", the road's is "march fatigue". The owner's ONE-CLAUSE VETO SURFACE
##   is the WY-8a ledger row.
## - **Nothing measured here is inherited.** The five censuses, every count,
##   and every effective-line budget (L8) are re-measured at the publishing
##   commit — including from this document.
##
## The binding canon: docs/DESIGN_FP_ARCHITECTURE.md §1 laws L1-L9, §3 flag
## law, §5 wave order, §10 protocol — verbatim; docs/
## DESIGN_WAR_RULINGS_ARCHITECTURE.md §1/§10 behind it; the espionage volume
## (docs/DESIGN_FP_ARCH_ES.md — promoted to the same family in this same fold
## pass, compiled from DESIGN_ESPIONAGE_ARCHITECTURE-DRAFT.md, session
## scratchpad; navigate by its ES prefix) whose
## covert law the Wayfare covert rendering RIDES, and whose queue fold ORDERS
## AHEAD OF THIS ONE — this volume's §5b insertion is written to land AFTER the
## ES fold and composes with it. The owner directive's verbatim-intent sections
## are LAW here; chair mechanics accepted in conversation are restated as the
## rulings they were; everything else below labeled JUDGMENT is vetoable.
##
## PERSISTENCE SIGN-OFF, STATED ONCE AND PROMINENTLY: this program requests
## NINE new conditional fields on EXISTING records (§2 table; zero new
## top-level worldState keys, zero new spatialLedgers keys, zero new mapState
## keys). Persisted-record shape is an owner-gated class (judgment-ledger §3).
## The §2 table IS the batch sign-off request; no wave that mints a field
## builds until its row is signed. Every field is drop-when-absent and
## flag-gated, so an unsigned row stalls exactly one wave, never the program.

---

## §0 THE PREMISE — thirteen directive sections, one program, and the seven census corrections that reshape it

The owner's directive set, compressed to its spine: **distance is absolute**
(the map's declared scale, not a normalized 2-8 week span — J-D11(b)
activated); **truth is severity-durable in transit** (big events survive
hops; only deliberate manipulation beats the floor, and big lies cost more by
construction); **the roads are inhabited at freeze** (movers mid-route are
the common case, and the map owes them a render); **the map tells positions
only** (armies, caravans, persons, populations at their road positions — no
direction, no destination, no intention on the canvas); **clicking is
reading** (every marker opens a popup in the legibility grammar; facets
render where records carry them and are declared-absent where not);
**everything on the map is real and everything real is conserved** (physical
trade with dispatch debits and arrival credits; physical migration carrying
faith; seizure transfers; quantization never a starvation artifact);
**one projection per entity kind** (the popup, the dossier tab, and the PDF
read the same builder, and a walker proves the facet sets identical);
**movers carry news** (arrival injects tidings; a town whose caravans stop
coming goes dark); **caravan density scales with a floor** (trade is the
lifeblood; the floor is economic, never physical); **caravans follow the
flow of goods** (a caravan is the quantized commodity flow, never an
illustration); **caravans read the relationship** (partnership thickens a
road, war thins it — the ambient read no label states); **armies eat** (2k:
supply is a banded army facet over the SAME closed goods vocabulary —
food = grain class, armor = crafted class, no new resource type; army supply
is stock-in-transit like caravan cargo, conserved; consumption couples to
condition and readiness, replenishment arrives as reinforcement cargo per
the 2i grounding chain or as rest-debits on real host stores — the army
eats, the town resents, and war cost gains a geography); **occupation is
both-sided prosperity** (2l: holding costs the governor as surely as the
holding yields — an occupation is viable only while BOTH prosperities
sustain it, and the sovereignty market's firesale is the named exit).

The five censuses measured the tree this lands in, and seven corrections
reshape the build. Each is CONFIRMED in its census; each changes scope:

- **C1 — A wayfare layer already ships, and it breaks the new law.**
  `src/components/map/TravelersLayer.jsx` renders armies, migrants, and
  road-mission envoys at interpolated road positions, store-wired and
  DM-gated — and it draws a direction CHEVRON on every marker, flips it 180
  degrees for retreats and returns, and states destination/ETA/purpose in
  canvas tooltips. `WarFaithMapOverlay.jsx` draws home-to-target deployment
  arrows. **Wayfare SUPERSEDES this surface; it does not extend it.** The
  first surface wave is a deletion, owner-visible, recorded (§5 WY-0; the
  deployment-arrow half RULED at §7 Q3, vetoable).
- **C2 — Non-food goods are ALREADY physical; food is not built at all.**
  `commodityFlow.js` runs finite origin stocks, caravans with `carried`,
  en-route taps, banditry, destination credit, conscience-gated seizure
  TRANSFER, and an asserted integer conservation identity. Food/grain has NO
  per-tick import term to replace — coverage is netted into the generation
  stash, and TR-4's grain machinery is zero-built (grep-confirmed). So 2d is
  not one physicalization wave: the goods half is a BODY-AND-CADENCE repair
  (§5 WY-3), the grain half stays TR-4's build, and this volume amends TR-4's
  charter rather than duplicating it.
- **C3 — Migration is ALREADY physical on two conserved lanes, and a third
  netted lane is unfenced.** M4 crisis columns and P2 homeostat columns both
  ride `spatialLedgers.migration` with executed conservation identities. But
  `flows.js` `populationFlows` teleports refugees same-tick with no
  spatialActive check — a netted regime running concurrently with the
  physical one in any spatial world with `migrationFlowsEnabled` lit. The
  exactly-one law is violated in the tree TODAY; the fence lands in the same
  program that writes the law (§5 WY-4).
- **C4 — The caravan is the ONE mover that cannot be positioned.**
  `ShipmentRecord` carries no `path` and no `departTick`; the route is known
  at dispatch and thrown away, then re-solved at arrival against LIVE
  embattlement. A render-time re-solve can put the marker on a road the
  caravan never took — a marker that lies. The cure is two fields at the one
  dispatch write (§2, §5 WY-3), and it is the highest-leverage engine edit in
  the program.
- **C5 — The mirror law's screen half is a phantom.** `WarFaithSection` does
  not exist as a component; TWELVE files spell the name at this HEAD (r2
  re-census: comment anchors and claims across components/pdf/domain plus
  four test files — the retirement sweeps the FULL grep census at build,
  never a fixed count), and two of the four B-track selectors have zero
  screen consumers tree-wide — a green pin guarding nothing. No projection-builder layer
  exists for any entity kind; the two working prototypes (`projectNpcPool`
  with `includeCovert`; `parityContract` as data) are the shapes to copy
  (§3e, §5 WY-7/WY-8).
- **C6 — There is no route-position id space, and 2b's obligation inverts.**
  `whereabouts.placeId` is ALWAYS a settlement id ("On the road to X" names
  the destination). Nothing is broken today; the ~130 location reads become
  mis-targets only if a program mints a route-position id. The obligation is
  a GUARD (route positions stay derived; the typed `{kind:'settlement'|
  'route_node'}` discriminator is the only lawful widening), not a consumer
  rewrite (§2d).
- **C7 — Faith has no conserved quantity.** Shares are integer percentage
  points summing to 100; nothing multiplies share by population anywhere in
  src. "Arrival credits the mix, conserved" requires a model ruling — HEADS
  (true conservation, re-founds every share write) vs BLEND (the
  occupationFaithPull shape, cheap, not conservation). RULED at §7 Q1:
  BLEND for v1 (vetoable; no cohesion finding refuted the shape).

**What Wayfare is NOT.** The map never creates mechanics: encounters happen
in existing kernels, and the map shows the record (2c verbatim).
`spatialArrivals` (queued consequences) and `interventions` (aspatial
commitments whose physical column is already an armyTransit record) are
RULED OUT of the layer family — rendering either would put a non-conserved
thing on the map. No commander is ever named on an army facet
(armyTransit.js's owner boundary + the product-scope law). No wave here
resolves a named character's fate. And WY builds NOTHING that reads
`pulseKernel.js` or `applyWorldPulse.js` — every engine mount is an existing
seam or a lifecycle-host stage per L1.

---

## §1 THE PHYSICS CHARTER

### 1a THE ABSOLUTE-DISTANCE LAW (J-D11(b) activated) — distance is the map's, not the normalizer's

**The law (owner verbatim intent):** realm distance is ABSOLUTE, derived from
the map's declared scale. Tens of miles and thousands of miles are different
worlds; settlements price by literal distance; the 2-8 march-week
normalization dies for scaled worlds.

**The socket, measured:** the war volume §7 parks "the map km-scale for the
transit kernel's mode table (J-D11(b); does not exist in the tree —
owner-signed when the mode table lands)." Grep confirms zero hits for
`kmPerWeek|MODE_SPEED|milesPerWeek|scaleKm` across src. The transit kernel
(`namedPersonTransit.js`) already exposes the injection point: its
`gradeMultiplier` parameter is documented in-file as the seam "a later
mode-speed table can supply another multiplier without creating a second
floor or position model." `hopWeeks`/`calibration` (`distanceRead.js`) is
the per-digest normalizer every consumer rides.

**THE CHARTER SUPERSESSION, RECORDED (chair-ruled, vetoable; the r2
flag-home resolution):** two binding volumes spelled this seam differently.
The war volume's WR-7a correction (DESIGN_WAR_RULINGS_ARCHITECTURE, the
2026-08-02 self-audit note) cites J-D11(b)'s mode-speed table "under
`portOpportunityEnabled`"; measured at the origin, DESIGN_REALM_DIRECTIVES'
J-D11 item (b) itself carries no flag clause — the "dark behind
`portOpportunityEnabled`" sentence closes item (a)'s port-institution
paragraph, plausibly scoping the whole J-D11 program — and the war volume's
§7 parking row ("owner-signed when the mode table lands") names no flag
either. The conflict is real, not refutable by measurement, so it is RULED
HERE: the owner's 2026-08-05 absolute-distance directive ACTIVATES J-D11(b)
as the Wayfare scale charter, and the mode-speed table lands DATA-GATED by
`spatialDigest.kmScale` (absent = legacy, forever), DECOUPLED from
`portOpportunityEnabled` — which remains the port/water-corridor program's
flag (J-D11 (a)/(c)), unbuilt and unbound today (zero src hits). Owner-
directive activation outranks the port-opportunity charter; the mode
table's owner-signing obligation from the war §7 row is PRESERVED verbatim
(the §8 tuning rows). §5b amends the war volume's two sites by name so the
charter is superseded on the record, never silently contradicted (seam 1).

**The mechanism (WY-1):**

1. **The declared scale is a per-world datum, not a flag — and it SURVIVES
   THE REBUILD.** At spatial canonization, the digest builder
   (`buildSpatialDigest`) stamps `spatialDigest.kmScale` (km per
   map-coordinate unit; the creation flow supplies it on FIRST canonize —
   from the FMG map's own distance scale where the bridge exposes one, else
   the owner-signed default). The digest is deepFrozen per canon VERSION,
   but it is NOT written once: `runSpatialCanonize`
   (src/store/campaignSpatialCanonize.js) documents a §V.1 RECEIPTED
   RE-CANONIZE that rebuilds the whole digest on founding events (ports and
   teleport blocs re-derived, `spatialCanonVersion` bumps). THE
   CARRY-FORWARD CLAUSE (the r2 HIGH fix — the ghost-write class, THE
   PROMISE): on every re-canonize path the builder RE-RECEIVES the prior
   digest's `kmScale` from the campaign's worldState — the same
   prior-worldState read the body already performs for `biomeTruthEnabled`
   — so a scaled world hitting a receipted re-canonize keeps its scale and
   every mover clock keeps its denomination; a datum that survived the
   first canonize can never ghost the rebuild. Lifecycle-pinned in WY-1:
   canonize → founding edit → re-canonize → `kmScale` identical, week
   spectra identical, version bumped; plus the drop-mutant (a rebuild that
   omits the carry-forward reds the pin). **Absent scale = legacy
   normalized behavior, byte-identical, forever** — carry-forward of
   absence is absence, so an already-canonized world never shifts (THE
   PROMISE holds with zero disclosure debt for existing campaigns).
   Re-canonization of a LEGACY world onto a scale (minting a datum where
   none existed) is owner-elective future work, not built here.
2. **One mode-speed table.** NEW leaf `src/domain/spatial/modeSpeeds.js`:
   `MODE_SPEEDS` km/week for the closed mode set `{foot, cart, river,
   coastal, sea}` (J-D11(b)'s own list), consumed by `hopWeeks` when
   `kmScale` is present: weeks = km / speed, composed with the existing
   grade multiplier, floored at law M's one week (`MIN_LEG_TICKS` — the
   floor binds in every denomination). Every mover inherits through the one
   kernel: armies (`armyMarchWeeks` composes its `ARMY_SPEED_FACTOR` on top),
   envoys, wanderers, columns, caravans. The constants are OWNER-SIGNED
   tuning rows (§5 WY-1's table) per J-D11(b)'s stipulation.
3. **THE PER-MAP SPECTRUM LAW (the dead-band law applied per map).** The
   standing hazard is proven: `hopWeeks` is calibrated per digest — a whole
   realm spans 2..8 march weeks today, so any authored band >= 9 refuses
   nothing, and under kmScale a bigger map buys LONGER weeks, not more of
   them, until it buys both. Therefore: `calibration(digest)` publishes the
   world's own MEASURED spectrum (min/median/max leg weeks), and EVERY
   distance-derived band in the mover estate — reinforcement windows
   (MAX_REINFORCEMENT_WEEKS), patience timeouts, interception windows,
   staleness saturations — derives from that spectrum or carries an
   in-file justification. WY-1 lands the spectrum surface plus a shrink-only
   lint census over the named consumer set (an authored week-literal in a
   mover module reds unless derived-or-justified). The
   `MAX_MARCH_WEEKS 52 < SIEGE_MAX_AGE` invariant is preserved by CLAMPING
   the march at 52 weeks with a receipt ("the far shore is beyond a year's
   march") — the clamp is pinned, the invariant never silently re-derived.
4. **The consequences ride, correctly, and are named:** war duration scales
   with theater size; fog scales with real distance (big realms foggier —
   spies and magic worth more, the espionage economy sharpens); large maps
   regionalize (the tyranny of distance); tiny maps become near-transparent
   worlds — CORRECT simulation, disclosed. **The creation flow owes the
   scale-vs-mystery disclosure** (a small-scale map is an open book; a
   continental one is a fog bank — the choice is the player's, stated
   plainly, never mechanically distorted). That copy lands with WY-1's Lane
   B slice.
5. **The map-side coupling, recorded:** mover marker positions derive from
   hopWeeks-shaped clocks, so for NEWLY canonized scaled worlds the same
   geometry renders different positions than a legacy world would — by
   design, not drift. For legacy worlds nothing moves (arm 1). The
   TravelersLayer migration-t defect (reconstructing departure from
   season-sensitive hopWeeks while `rec.departTick` sits unread) is cured in
   WY-9 independently of scale, so position honesty does not wait for WY-1.

### 1b THE SEVERITY-DURABILITY LAW — big truths survive the road

**The law (owner verbatim intent):** truth in rumors becomes MORE DURABLE
through transit and hopping, scaling with the event's severity, UNLESS
deliberately manipulated.

**The substrate, measured (r2 re-measure):** `spatial/rumorNetwork.js`
already runs per-hop ORGANIC weathering in BOTH distorting modes —
`'unreliable'` AND `'full'` (the in-file M10a note: full is the unreliable
SUPERSET; hiFi teleport carriers never weather in either) — via
`degradeTelling` on a keyed fork minted per (event, carrier, edge, hop),
mutating fields tracked by `completeness01`/`accuracy01`, with a rare
perfect-hop arm. The mutations are CONDITIONAL DISCRETE DRAWS (a 30%
band-drift chance with a nested 50% direction draw; a 12% name-swap chance
with two randInt draws) — which is exactly why the r1 "scale the magnitude,
same draws in the same order" spec was self-contradictory and is REPLACED
below. SP-A's ONE severity ladder is MINTED and live: `SEVERITY_LADDER =
['glancing', 'telling', 'grave', 'ruinous']` (`bandFamilies.js:85` at this
HEAD).

**The mechanism (WY-2) — one multiplier, zero new keys:**

1. NEW leaf `severityDurability.js`: rung → drift multiplier, authored
   descending — glancing 1.0 (full organic drift), telling ~0.75, grave
   ~0.4, ruinous ~0.1 (near-lossless organic transit). Owner tuning row;
   the exact values are signed at the soak.
2. **THE MECHANISM, REBUILT ON THE L1 DISCIPLINE (r2 HIGH fix — keyed
   hash01, zero conditional draws).** The dark arm is today's code path
   VERBATIM: the weathering site mints its keyed fork and calls
   `degradeTelling` unchanged — byte-identity. The lit arm mints NO fork
   and draws NOTHING sequential: it calls the new severity-aware degrade in
   `severityDurability.js`, which makes ONE keyed roll `u =
   hash01('rumor-weather:' + eventRef + ':' + carrier + ':' + edgeId + ':'
   + hop)` — the exported, avalanche-finalized `hash01`
   (`region/contestMath.js`; the FNV-1a low-bit parity hazard is already
   cured inside that helper) over the SAME tuple the fork key spells — and
   maps u through a CLOSED OUTCOME TABLE: `{perfect | plain-wear |
   band-drift-up | band-drift-down | name-swap | severe-garble}`. Row
   widths are today's aggregate organic rates SCALED by the rung's
   multiplier (ruinous compresses every mutating row toward zero and grows
   `perfect`; glancing keeps the full organic widths); the name-swap
   TARGET index derives from the same u by sub-interval rescale within its
   row (deterministic, zero further draws); wear scales are authored row
   constants. BOTH arms are fully deterministic; both distorting modes
   ('unreliable' AND 'full') take the same table when lit; hiFi carriers
   still never weather. **Stream identity for every other consumer rests
   on FORK ISOLATION, not draw counts** — the organic fork is keyed
   per-(event,carrier,edge,hop) and single-consumer (the in-file fork
   law), so the lit arm's non-minting shifts no other stream in either
   flag state. The pins are therefore a FORK-KEY-SET CENSUS (the module's
   rng touches enumerate to exactly the one dark-arm `rumor-organic:` key
   family, both flag states) plus an OTHER-CONSUMER STREAM GOLDEN (a
   non-rumor consumer's same-seed stream byte-identical across flag
   states) — NEVER a draw-count pin. The ruinous-≈-near-lossless multi-hop
   fixture stays as the owner-intent pin, now satisfiable because discrete
   mutations are genuinely suppressed by the table, not "damped". THE RUNG
   DERIVATION IS TABLE-MAPPED, NEVER WORD-MAPPED: the event's rung derives
   from its own severity where the mint carries one, else by INDEX from
   the existing magnitude bands — and note the collision the pin must
   survive: rumor magnitude 0..3 spells faint/notable/serious/GRAVE
   (`magnitudeBandOf`) while `SEVERITY_LADDER` spells
   glancing/telling/GRAVE/ruinous — 'grave' sits at different rungs in the
   two vocabularies, so the pinned derivation maps by index/table and an
   anchored negative reds any word-keyed join.
3. **Manipulation bypasses the floor.** A deliberately planted telling (the
   `disinfo:` lineage — the only plant marker, per the Herald slot-class
   law) weathers per the planter's design, not the durability floor. And
   the cost asymmetry is EMERGENT, not tuned: severe events mint more
   independent hop-0 roots (one per event-witness — the independence ROOT
   machinery already in the module), so a sustained big lie contradicts
   more corroborating streams and raises exposure odds BY CONSTRUCTION.
   WY-2 pins this with an executed fixture (a grave-severity plant
   accumulates at least the contradiction pressure of a glancing one), and
   the espionage program's covert manipulation (ES amendment I) becomes the
   law's AGENT — "unless deliberately manipulated" now has a face, and the
   severity-durability pricing composes at the plant site (ES seam, §6).
4. **The espionage economy sharpens correctly, and the receipts say so:**
   spies matter most for drift-prone small facts and for auditing
   suspicious severe claims; catastrophes stay commonly known. Fog belongs
   on details, states, and intentions — never on burned cities. That
   sentence is the wave's tellable.

Flag: `severityDurableRumorsEnabled`, virtual per L2, dark = multiplier
absent = byte-identical. Rumor lanes are live machinery; the flag keeps the
same-seed shift owner-held.

### 1c THE REGIME-FLIP HOUSE IDIOM — formalized once, instantiated three times

**THE IDIOM (ruled by the owner across 2d/2e; stated here once as law):**

> For every conserved unit family (a good class, a population, a mover), a
> settlement is served by EXACTLY ONE regime per tick: NETTED (a rate or a
> same-tick delta pair) XOR PHYSICAL (dispatch debits the origin, the unit
> rides a mover record in transit, arrival credits the destination). A
> walker asserts exactly-one in BOTH flag states, forever. Conservation
> holds across all three places — origin + transit + destination — with
> seizure a TRANSFER, never a vanish. Cadence derives from flow bands (the
> QUANTIZATION LAW: half a caravan per month is one caravan per two months
> with doubled credit — never a starvation artifact). And THE HOUSE IDIOM
> binds the surface to the physics: if it is on the map it is REAL, and if
> it is real it is CONSERVED — a marker is never a lie, and a flow with no
> marker is either declared-netted or dark.

**Instantiation I — TRADE (generalizing TR-4 per good class).** The
measured truth (C2): M6a non-food goods are ALREADY the physical regime,
conservation asserted; food is netted with no per-tick term; the
`tradeFlowsEnabled` candidate lane is a SIGNAL, not a flow (it moves
nothing and the caravan layer must never bind to it). What WY adds:

- **The per-good-class regime contract** (WY-3): NEW
  `flowRegimeContract.js` declaring, per good class, `physical | netted |
  dark`, with the bidirectional exhaustive test of 2i — every caravan
  manifest traces to a real surplus-deficit pair in the closed goods
  vocabulary, and every computed flow is realized by caravans or explicitly
  declared netted. v1 rows: the M6a good classes physical; grain pre-pinned
  toward TR-4's T7 walker (which remains TR-4's to land — this contract is
  the SHAPE generalized, growing one class at a time, exactly the SP-A
  reconciliation-walker pattern).
- **The quantization cure** (WY-3): M6a's `MIN_SHIP` floor simply refuses
  sub-floor needs today — the starvation artifact 2d(e) forbids, live for
  goods. Lit, persistent sub-floor deficits accrue a dispatch debt and emit
  a MIN_SHIP caravan every ceil(MIN_SHIP/need) ticks with the accumulated
  credit — cadence from flow bands, conservation unchanged.
- **The measured correction to T7, REPORTED CENSUS-SHAPED (r2; not
  re-ruled, and never a count):** the routed TR queue row reads "T7 fences
  the grep-derived `importDependency` consumer census of the food ledger,
  SHRINK-ONLY" — a count-of-N row would be stale the day a fourth reader
  lands. Members at THIS head, for the record: `foodStockpile.js`
  blockadePct (one of T7's two named sites); `demographicsRates.js`
  `foodCapacityOf` (unfenced — feeds K_food, the demographic carrying
  capacity that actually bounds population); AND `magicSubstitution.js`
  `substitutedImportDependency` (unfenced — "one number, read twice," with
  its own downstream consumer set). Excluded BY SYMBOL-HOME, named so the
  grep-prefix trap cannot re-add it: `tierResourceDynamics.js` declares a
  LOCAL `importDependency` boolean derived from trade lists — a different
  quantity that merely shares the spelling. A TR-4 that neutralizes only
  the two originally-named sites leaves the capacity and substitution
  models believing in netted imports; the census row fences whatever the
  build-day grep finds.
- **2i's grounding chain, bound at build:** manifest goods from the closed
  vocabulary (the scarcity families / term-catalog `good` precedent /
  bandedStock classes — verify the one vocabulary at build and pin the
  spelling); production truth (resourceDynamics yields); demand truth (the
  needs model the dispatch composer prices); flow topology (surplus at A +
  deficit at B + profitable route = a caravan). Nothing on the road the
  economy did not send; nothing sent that fails to appear.

**Instantiation II — MIGRATION (the POP wave, with faith cargo).** The
measured truth (C3): the physical lanes exist with executed conservation;
the netted `flow_migration` arm is unfenced. What WY adds (WY-4):

- **The fence:** under `flowMigrationPhysicalEnabled` AND spatialActive,
  the `populationFlows` producer mints COLUMNS through the one M4 writer
  path (travelClass ABSENT so M4 lands them — the P2 mutex is untouched;
  RF-2 holds) instead of same-tick delta pairs. Dark, the netted arm runs
  as today (documented). **THE PROPOSAL ARM IS COVERED (r2):**
  `flow_migration` candidates whose fraction clears
  `MIGRATION_PROPOSAL_FRACTION` carry `applyMode: 'proposal'` and apply
  their populationDeltas at DM ACCEPTANCE, not at mint — under the lit
  fork the ACCEPT path mints the column too, through the same M4 writer
  (acceptance IS the dispatch tick; receipted; the candidate's copy stays
  "toward", never "arrived"), so BOTH arms — auto and proposal-accept —
  are physical when lit, and a proposal-accept pin executes the accept
  path in both flag states. The exactly-one walker asserts netted XOR
  physical per settlement per tick in BOTH flag states — and the SAME
  walker finally covers the bare `populationDynamics` spatial fork (D1),
  which has run unwalked since it landed. **THE DOUBLE-SERVICE BLIND SPOT,
  NAMED AND FENCED (r2):** after the fence, flow columns and M4's own
  crisis columns are both PHYSICAL and travelClass-absent, so netted-XOR-
  physical alone cannot see one stressor served twice — and the producers
  are NOT disjoint by construction (M4's trigger is the mass-emigration
  shed; `flow_migration` triggers on the DISPLACEMENT_STRESSORS set at
  severity ≥ 0.6 — famine/siege/plague/occupation sit in both reach
  chains, and the overlap predates the fence: C3's concurrency). The WY-4
  census MEASURES trigger disjointness at build, and regardless of its
  answer the ATTRIBUTION discipline lands: every physical column carries
  exactly one provenance (fence columns the flow ruleId receipt via F5;
  M4 columns the shed receipt), and a DEDUP PIN beside the walker asserts
  no settlement-tick is served by both producers for one stressor family
  — the fence YIELDS to a same-tick M4 dispatch (deterministic precedence,
  receipted), with an executed double-send mutant.
- **The cargo (2e, owner refinement):** columns gain `faithCargo` (banded
  composition derived deterministically from the origin's observance mix at
  departure — `projectReligionStateOntoSettlement` is a pure read away,
  including the unaffiliated bucket) and `causeReceipt` (the launching
  stressor/exodus receipt, frozen at dispatch — a render-time read of
  current stressors would lie once the stressor clears). Both ride
  `migrationCargoEnabled`; both enter `columnOf`'s conditional
  carry-through IN THE SAME COMMIT as their writer with the
  round-trip-THROUGH-ENQUEUE pin (RF-1 — the whitelist normalizer rebuilds
  the whole ledger every M4 dispatch tick and silently deletes unknown
  fields; POP-1 adds its own fields to the same normalizer, so the two
  amendments compose by order, never fork).
- **The WF×POP seam (owner: migration is the PHYSICAL VECTOR of faith
  spread):** arrival credits the destination's observance mix per the Q1
  RULING (BLEND — §7 Q1, vetoable), through
  religionState's OWN writer family (single-writer law; the
  occupationFaithPull arm is the shape precedent), additionally gated on
  the faith-spread flag pair — and the legacy-mirror hazard binds: a wave
  lighting only `faithSpreadEnabled` while the inherited
  `religionDynamicsEnabled` default-false mirror is present will silently
  stay dark; the writer must handle both spellings exactly as the UI does.
  The tellable is the owner's own sentence: a large column of one faith
  marching toward a town of another is next year's religious tension,
  visible and clickable.
- **The uncredited-exit note:** `settlementLifecycleKernel`'s documented
  ORIGIN-LOSS (population shed uncredited from the world sum, deliberately
  deferred) is a standing conservation hole the house idiom now names.
  WY-4 does NOT fix it (it is POP's estate, deliberately deferred there)
  but records it beside the walker so the exactly-one law's honesty is not
  overstated.

**Instantiation III — TRAVELERS (already physical; the walker keeps it
so).** Armies, envoys, missions, wanderers, and columns already move
physically through persisted records — there is no netted arm to fence.
The idiom's obligation here is PRESERVATION: the encounter-pairs walker
(§4) and the flow-regime contract carry traveler rows asserting that no
future wave mints a netted teleport for a mover kind (a spatial arrival
that skips transit reds unless it is the documented `spatialArrivals`
consequence queue, which is not a mover). The travelers' regime work in
this program is the BODY repairs: the caravan's missing clock and road
(WY-3), the mission's missing per-leg departure (WY-3), and the migration
departure read (WY-9).

---

## §2 THE CANONICAL MODEL — the L4 fight, won at zero new keys

**ZERO new top-level worldState keys. ZERO new spatialLedgers keys. ZERO new
mapState keys.** Every addition is a conditional field on an EXISTING record,
drop-when-absent, flag-gated, exactly one writer each. Positions are NEVER
stored for rendering — they interpolate at read time from the fields below
plus the frozen digest. Projections (§3e) are pure derived reads. The news
cargo (2g) is fully derived (§5 WY-5) — a mover carries what its origin's
ledger held at departTick, reconstructed from arrival records, zero fields.

### 2a The nine conditional fields (THE SIGN-OFF TABLE)

| # | Field | Record (existing) | Writer (one) | Flag gate | Wave |
|---|---|---|---|---|---|
| F1 | `departTick` | `spatialLedgers.supplyShipments[]` (CommodityShipment) | commodityFlow dispatch write | `caravanBodiesEnabled` | WY-3 |
| F2 | `path` (settlement-id route, frozen at dispatch — `dispatchPick.route.path`, already in hand) | same record | same write | same | WY-3 |
| F3 | `legDepartTick` | `spatialLedgers.roads.missions[]` (MissionRec) | re-stamped at every `legArrivalTick` write — NINE sites in TWO modules at this HEAD (eight in `worldPulse/roadsKernel.js` + `consumeMissionRecall` in `roads/state.js`); re-censused by symbol at build, tree-wide | same (the stamp is inert bookkeeping; readers prefer-it-when-present) | WY-3 |
| F4 | `faithCargo` (banded composition, closed shape: top entries + unaffiliated, band words only) | `spatialLedgers.migration[]` (MigrationColumn) | the column mints (M4 dispatch + the WY-4 fence path) | `migrationCargoEnabled` | WY-4 |
| F5 | `causeReceipt` ({kind, atTick} — the launching stressor/exodus token, frozen) | same record | same mints | same | WY-4 |
| F6 | `experience` | `worldState.deployments[homeId]` | warArmyRecord seed/refresh (beside `readiness`, same conditional idiom — omitted when no martial record) | none needed: conditional beside an existing conditional; byte-identical for every world without a martial record | WY-8 |
| F7 | `experience` | `spatialLedgers.armyTransit[]` | armyTransitKernel's existing copy site | same | WY-8 |
| F8 | `prizeCargo` ({good: units} integer map, conserved) | `spatialLedgers.armyTransit[]` | armyCaravanSeizure (this program's new encounter arm, E14) | `caravanSeizureEnabled` | WY-6 |
| F9 | `supplyCargo` ({goodClass: units} integer map over the CLOSED goods vocabulary — food = grain class, armor = crafted class, no new resource type; conserved, F8's shape precedent) | `spatialLedgers.armyTransit[]` | NEW `armySupply.js` — the ONE writer module; every mint (march dispatch from origin stores, debited), decrement (consumption), and credit (reinforcement cargo, rest replenishment) routes through it | `armySupplyEnabled` | WY-8 (slice 8a) |

Plus ONE frozen-digest datum, not a record field: `spatialDigest.kmScale`
(WY-1; stamped at canonization by the digest builder AND CARRIED FORWARD
through every receipted re-canonize — the rebuild-on-founding-events path
re-receives the prior digest's value, lifecycle-pinned, so the datum can
never ghost the rebuild (§1a.1); absent on every existing world forever —
the legacy arm is the world's identity, not a migration, and carry-forward
of absence is absence).

**Rejected as fields (derived instead):** column size band (derives from
`arrivals`); travel fatigue (derives from `position01`); army condition band
(derives from `accumulatedAttrition` through the WY-8 ladder); going-dark
(derives from arrival ledgers); carried news (derived at arrival, §5 WY-5);
home-garrison supply (derives FRESH from the home settlement's own stores —
2k's rest arm at home; consistent with Q2's no-record ruling, no field);
any route-position id (C6 — refused as a class, see 2d).

### 2b Lifecycle clauses (L4, per family — landing BEFORE the writers build)

- **Shipment body (F1/F2).** CREATE: stamped lit-only at the one dispatch
  write; both values already computed there — no new solve. READ: the
  caravan layer and popup interpolate `pointAlongPath(pts(path),
  progress01(tick, departTick, arrivalTick))`; the seizure arm (WY-6) reads
  `path` for co-location; NOTHING may re-solve a route at render (source
  scan — C4's lie prevention). PERSIST (r2 attribution fix): the field
  teaching aims at commodityFlow's OWN ARRIVE-LOOP RECONSTRUCTION — the
  whitelist rebuild that re-mints every in-transit record each tick and
  whose in-file comment documents exactly this loss class biting before
  ("a multi-tick smuggle run loses smuggle:true on reconstruction");
  `departTick`/`path` are carried across in-transit ticks precisely as
  `smuggle` is, in the SAME COMMIT as the dispatch write (RF-1) — plus any
  load-time spatialLedgers normalizer the build-day census finds
  (`supplyShipments.js` exports no record normalizer at this HEAD; it is
  the supply-LINK module, not the shipment-record keeper). The
  serialize/regen/undo/import round-trip pin lands here (measured absent
  for this family — not inherited from TR-4's promise) and it round-trips
  THROUGH AT LEAST ONE IN-TRANSIT TICK (dispatch → advance → fields
  survive the arrive-loop rebuild → arrival), never merely save/load.
  REGENERATE
  (THE PROMISE): same-seed replay re-derives identical stamps (the dispatch
  is deterministic today; the stamps add no draw). UNDO: pulse ring restores
  wholesale. MIGRATE: none — old records lack the fields, and every reader
  prefers-present/falls-back (dark worlds and old saves render no caravan
  markers rather than lying ones). VEIL: shipment records never reach public
  payloads today; the map-only enforcement (§3e) pins that they never start.
- **Mission leg clock (F3).** CREATE: re-stamped beside every
  `legArrivalTick` write — outbound dispatch, phase flips, sea delays,
  re-plans, AND the recall's return leg (NINE sites in TWO modules at this
  HEAD: eight in `worldPulse/roadsKernel.js` plus `consumeMissionRecall`
  in `roads/state.js` — the r2 census correction; a kernel-only sweep
  would leave every recalled mission's return leg without its clock). The
  WY-3 pairing walker source-scans the TREE by symbol, never a file list,
  so a tenth `legArrivalTick` write anywhere without its twin reds. READ:
  `currentHopOf` and the persons layer prefer `legDepartTick` when present,
  reconstructing (today's behavior) when absent — the season-sensitivity
  defect class dies where the field exists, and old records degrade to
  exactly today's render. PERSIST/REGENERATE/UNDO/MIGRATE: as F1/F2
  (drop-when-absent; no migration). VEIL: n/a (engine bookkeeping).
- **Column cargo (F4/F5).** CREATE: frozen at dispatch from the origin's
  live mix and the launching cause; the WY-4 fence path stamps both on its
  columns. READ: populations popup + the WF credit arm (F4); the popup's
  "why" row (F5). PERSIST: THROUGH-ENQUEUE — `columnOf` gains the
  conditional carry-through in the same commit (RF-1; the pin round-trips
  enqueueColumns' whole-ledger rebuild + releaseArrivals + the P2 stage-0
  read, not merely save/load). REGENERATE: same-seed identical (pure reads
  at dispatch). UNDO: ring. MIGRATE: none. VEIL: band words only; the raw
  origin mix never rides the column; `faithCargo` never reaches a public
  payload (the map-only class + publicSafe contract row).
- **Army experience (F6/F7).** CREATE: conditional stamp beside `readiness`
  at seed/refresh and the kernel copy — two lines, byte-identical for every
  world with no martial record (the M1 shape from the army census). READ:
  the army projection ONLY (H2/H3 discipline: presence-checked, never
  defaulted — the absent arm renders declared-absent, and `experienceOf`'s
  absent-⇒-1 inversion — an absent record reads FULLY SEASONED, the
  documented byte-identity default; r2 spelling fix: the default is 1,
  never -1, so a builder hunts no negative — can no longer reach a
  surface). PERSIST: rides the
  deployment/transit records' existing serialization. REGENERATE/UNDO:
  ring-restored with their parents. MIGRATE: none. VEIL: banded word only.
- **Prize cargo (F8).** CREATE: the seizure arm only. READ: the army popup
  (a prize row); the home-credit release. PERSIST: on the transit record.
  CONSERVATION: carried = prize + lost at seizure; the prize credits the
  HOME settlement's stocks through commodityFlow's one stock-write seam at
  the column's arrival home (never a same-tick teleport — the timing pin);
  a destroyed or dissolved column books its prize to `lost`. REGENERATE/
  UNDO: ring. MIGRATE: none. VEIL: DM-truth surfaces only until the Herald
  tells the seizure (the kind is public news — L6 five joins in WY-6).
- **Supply cargo (F9 — 2k).** CREATE: stamped lit-only at march dispatch by
  `armySupply.js`, debiting the ORIGIN's real stores through commodityFlow's
  one stock-write seam (a bled home sends empty wagons — the manifest shows
  it, per 2i's grounding chain; no stock is conjured). READ: the army
  projection's supply band ONLY (presence-checked; declared-absent on old
  records and dark worlds — H2/H3 discipline). MUTATE (all through the one
  writer): consumption decrements with time/march and books to `consumed`
  (never silently vanishes — the conservation identity is
  debited = carried + consumed at every tick); reinforcement columns carry
  supply AS CARGO from origin surplus (surplus-gated — no surplus, no
  cargo); RESTING debits the HOST settlement's real stores and credits
  `supplyCargo` — the OCCUPIED arm mints a requisition receipt that feeds
  occupation resistance (WR-10's durable fragility gains a feeding
  schedule), the FRIENDLY arm books an ally-hosting burden (coalition
  strain with physical roots). PERSIST: rides the armyTransit record's
  existing serialization; the armyTransit normalizer (taught F8 at WY-6)
  is taught F9 in the SAME commit as the first stamp (RF-1), and the
  round-trip pin runs THROUGH at least one consumption tick. REGENERATE:
  same-seed identical (deterministic debits). UNDO: ring. MIGRATE: none —
  recordless armies render supply declared-absent, never full. VEIL:
  banded word only; the raw units never leave DM-truth surfaces.

### 2c The mapState surface (client-side, per-campaign persisted)

**No new layer keys.** The shipped `travelers` key and its
`travelersFilter` are KEPT and widened: the sub-layer set becomes the four
owner layers `['armies', 'caravans', 'persons', 'populations']`
(JUDGMENT, vetoable: reusing the key preserves every old campaign's toggle
state and avoids four new DEFAULT_LAYERS keys; the label rename is
presentation). Lifecycle: the persisted-filter values migrate by a
load-time normalize (old `'migrants'` → `'populations'`, `'envoys'` →
`'persons'`) in the mapSlice merge — pinned, because `mapState.layers` is
persisted per campaign and merged on load (the census-1 hazard: verify the
spread order gives new sub-layer defaults their default, and pin it).

### 2d THE ROUTE-POSITION GUARD (2b's obligation, corrected by C6)

Route positions remain DERIVED, never stored as place ids. The law:
`whereabouts.placeId` and every location field in the mover estate holds a
settlement id or the empty string; the ONLY lawful widening is the existing
typed discriminator `{kind:'settlement'|'route_node'}` (hasExactKeys-
validated in foreignGuestHold.js), and any program minting a route-position
id owes the full ~130-read consumer census FIRST. WY lands the guard as a
lint census (the writer sites of `placeId` and the mover location fields
assert settlement-id-or-empty), which is cheap today precisely because the
premise was corrected — nothing needs widening yet, and the trap is fenced
before it exists.

---

## §3 THE SURFACE CHARTER

### 3a THE FOUR LAYERS — riding the shipped pattern, superseding the shipped layer

The four owner layers are sub-layers of ONE Wayfare family occupying the
existing `travelers` registration (§2c), mounted in `MapOverlay.jsx`'s
transformed `<g>` at the shipped z-position, reading the freeze exactly as
today (campaigns → activeCampaign.worldState → spatialLedgers + the frozen
spatialDigest, `geometryVersion` in the memo deps):

| Sub-layer | Records (all existing) | Notes |
|---|---|---|
| ARMIES | `armyTransit` + `navalTransit` (same record shape, `mode:'sea'`) | force-BANDED marker: the glyph's SIZE encodes the strength band (ARMY_STRENGTH_PHRASES ladders) — magnitude is presence, not intention; sea markers carry the naval glyph variant; the naval record's TWO affiliations (ownerId port + cargoId army) both link in the popup |
| CARAVANS | `supplyShipments` WITH F1/F2 present (else not drawn — never a re-solve) | grain caravans join automatically when TR-4's records land in the same ledger family; treaty streams likewise (TR-4 slice 2) |
| PERSONS | `roads.missions` + `envoyErrands` + `npcLedger.{roamers,placed}[].transit` (+ DM-assigned legs) | the two-envoy-ledgers hazard is named in-file: "envoys" today rendered only missions; WY renders BOTH ledgers; one-hop legs draw on the fromId→toId chord (JUDGMENT: chord geometry for one-hop movers is honest — the leg IS the geometry; multi-node routeRef resolution is deferred until a surface needs it) |
| POPULATIONS | `migration` (BOTH writer provenances — M4 crisis and P2 homeostat) | positioned from `rec.departTick` (the persisted clock — the census-4 defect cure), road-borne filter preserved verbatim |

**The registration walker (the dead-checkbox cure):** NEW
`tests/ui/mapLayerManifest.walker.test.js` — a render-free source-scan
asserting DEFAULT_LAYERS keys ⇄ LayersPanel rows ⇄ MapOverlay mounts ⇄
MapLegend entries, both directions. **The census domain is the BOOLEAN row
set with a declared CONFIG-KEY EXEMPTION CLASS (r2):** DEFAULT_LAYERS
carries ~20 keys at this HEAD, and the non-boolean ones (the `*Filter`
family, `regionalMinSeverity`) are panel CONTROLS, not layer rows — the
walker exempts them structurally (non-boolean value ⇒ config class) rather
than by name, and its anti-vacuity floor is MEASURED AT BUILD from that
boolean row set, never inherited from this document (the r1 "thirteen" was
wrong on both axes). `toggleLayer`'s silent no-op on undeclared keys makes
this mandatory before any layer work. The legend sources every Wayfare colour
from the ONE shared style module the layers draw from (the MapLegend law).

**Performance posture, recorded:** no virtualization exists anywhere in the
SVG layers, and mover counts under 2b scale with map size × route density,
not settlement count. The proven mitigation (RoadsLayer's batched-path
concatenation) CONFLICTS with per-marker clickability — flagged, not used.
The memo-dep discipline binds (minimum store slice + geometryVersion; never
read `transformRef.current` during render). If a soak shows a busy scaled
world past a few hundred mover nodes, the escape hatch is a zoom-gated LOD
(markers below a scale threshold render as the batched dot field,
non-interactive, with the popup path via the layer panel's list) — DEFERRED,
recorded, not built.

### 3b THE AMBIGUITY LAW (pinned)

> A marker is a presence, never an intention. No direction arrows, no
> destination lines, no path highlights, no heading flips, no
> destination/ETA/purpose in canvas tooltips. Heading, purpose, and ETA
> live in READING surfaces — the popup, the Herald, the dossier.

Application to the shipped estate (WY-0, all owner-visible, all recorded):
delete `chevronPoints` rendering and both `angleDeg + 180` flips from the
travelers layer; reduce tooltips to presence-only ("An army column" /
"A migrant column"); the geometry leaf keeps returning `angleDeg` (pure
API, other consumers unharmed). `WarFaithMapOverlay`'s home→target
deployment arrows are MOVEMENT-telling and fall under the law — RULED at
§7 Q3 (delete; vetoable): the army's position IS the deployment's
presence; siege rings, occupation badges, and prize glyphs are
FRONTS/STATES and stay. The deletion is WAVE-ASSIGNED to WY-0 (both
halves land together). The banded marker SIZE (3a) is magnitude, not
intention — lawful. Enforcement: an anchored negative in the layer tests
(no polygon/arrow/marker-end in the wayfare group; no destination string in
a canvas `<title>`), so the chevron cannot quietly return.

### 3c THE COVERT LAW (pinned; the espionage seam)

> Covert travelers render ONLY in the DM's true view, marked covert, and
> are omitted FAIL-CLOSED from every player-facing surface and export.

The chokepoints exist and are BOUND, not re-invented: engine-side,
`hidden: true` on a leg (five fail-closed filters already consume it) and
the covert envoy/errand records; render-side, `viewerSeesDmSecrets(ctx)` —
with the census-1 hole CLOSED in WY-9: every map call site supplies
`ownerUserId`/`viewerUserId` (today's `{isOwner: !!auth?.user}` proves a
session exists, not that this viewer owns THIS campaign; the fail-closed
predicate already supports the id pair and it is simply unsupplied);
projection-side, the `includeCovert`/audience parameter decided ONCE at the
projection (the heraldWanderers one-gate law verbatim: the presence of the
field IS the permission); export-side, the roadScene THROW pattern (a
player-audience assembly that throws on a non-player-safe source) is the
shape for the popup renderers' player arm, and `publicSafe`'s allowlist
plus its contract test enforce that no Wayfare projection is reachable from
a public payload. The DM popup shows the declared face AND the true purpose
marked covert (envoyIntent private goals; ES covert sub-records when the
espionage program lands); the player surface omits the ENTITY, not the
field — no redacted stubs (the CausalityPopup precedent). The espionage
volume's covert law is the parent; WY adds no second spelling (§6 seam 7).

### 3d THE POPUP GRAMMAR (the legibility law embodied)

**The canvas stays direction-silent; clicking is reading.** Every wayfare
marker is clickable and opens ONE popup family:

- **Glance** — the marker itself: kind glyph, banded size (armies), covert
  ring (DM), encounter link glyph when an encounter record anchors here.
- **Sentence** — one house-voiced state line from the projection ("Five
  weeks on the road to Aldmere, the column's strength telling." /
  "Grain out of Byrne, expected by first thaw.").
- **TABLE** — the exact-figures register. JUDGMENT (vetoable), resolving
  the armyStrength no-internals law against 2e's "the third register
  permits numbers": the table shows numbers that are FACTS IN THE FICTION —
  headcounts (`formatCount`), tick-dated departures, ETA weeks, carried
  units, prize units — and NEVER internal scores: the 0..100 capacity axis,
  scalar readiness/experience/attrition, and every tuning-shaped float stay
  BANDED words. The decoy fields (`objective`, `returnCondition`) are
  banned from every facet set by anchored negative (H5).
- **LINKED RECEIPTS** — the entity's recent Herald lines via a NEW
  entity-keyed feed read (`heraldFeed` gains `linesForEntity(ids, n)` —
  the marker→Herald query that census 1 found missing), plus
  `useRealmEntityNav` links for every named settlement (home, origin,
  destination, captor).
- **DECLARED-ABSENT, never invented:** every facet renders through the
  `{band, source, complete}` provenance shape (the warCoalitionExpenditure
  pattern, adopted verbatim) — an absent record renders the absence row
  ("No muster rolls survive for this column" / "Readiness: unknown to this
  world — no martial tradition"), never a zero, never a guess, and never
  `experienceOf`'s inverted default (H3).

**The primitive (RULED at §7 Q4, vetoable):** `PortablePopup` —
the centered, focus-trapped, outside-click-closing modal WITH scrim — IS
the Wayfare popup v1. Clicking is reading; occluding the canvas while
reading is acceptable and consistent with the one existing popup
convention (no acknowledge control, structural). An anchored popover with
viewport flipping and a leader line is real new primitive work with real
a11y surface; it is DEFERRED until the modal measurably fails, recorded
here so the deferral is found.

**Reach:** markers take the PlacementsLayer pointer seam
(pointerEvents:'auto' per marker group inside the inert wrapper — pan
preserved between glyphs), hit-slop under counter-scale, drag-vs-click
dead-zone, `tabIndex`/`role="button"`/Enter-opens per marker, and the
sr-only "world map in words" summary widens to enumerate mover counts per
sub-layer (today it names placed settlements only — a lie by omission once
the roads are inhabited).

### 3e THE SINGLE-PROJECTION MIRROR LAW (pinned)

> ONE projection builder per entity kind, with an audience parameter,
> consumed by the map popup AND the dossier tab AND the future PDF. A
> totality walker proves the facet sets identical across surfaces, with an
> exact-in-both-directions exemption census. Populations and caravans are
> map-only BY OWNER EXCEPTION — transient movers, not dossier residents;
> their traces live in ledgers and the Herald.

The builders, their seeds, and their consumers:

| Kind | Builder (home: src/domain/display/) | Grown from (never beside) | Surfaces |
|---|---|---|---|
| treaty | `treatyProjection.js` | `treatyDocument.decorate()` (90% built) | war tab + PDF FaithWar + TreatyPanel |
| person | `personProjection.js` | `heraldWanderers` RELOCATED out of components/map + `projectNpcPool` (+ `whereaboutsDisplay` as the ONE whereabouts vocabulary — D7's two-truths cure) | NPC tab + Unaffiliates + persons popup + PDF NPC slices (identity now, facets as the PDF migrates) |
| army | `armyProjection.js` | `armyStrength.js` grows the audience parameter | war tab (ALL units, home + field) + army popup + PDF liveWorld (B-track re-anchored) |
| caravan | `caravanProjection.js` | new (greenfield — no second spelling exists; the mirror law is cheap BEFORE first drift) | caravan popup ONLY (map-only class) |
| population | `populationProjection.js` | new | populations popup ONLY (map-only class) |

Binding rules, all proven in-tree before this volume named them: pure (no
store, no React, no clock); audience decided AT the projection and nowhere
downstream; dormant ⇒ null/[] so the off-state is byte-identical;
worker-safe returns (data only — the nameById lesson); field-level
conditional facets (present only when the record carries them — the
declared-absent grammar at the field level). The kind registry is
`entityLinks.KIND_PREFIX` EXTENDED (army/caravan/population/treaty join
npc/faction/institution) so identity and facet-set share one registry and
the popup↔dossier deep-link gets id prefixes for free.

**The walker:** NEW `tests/lint/wayfareMirror.walker.test.js`, copied from
`premiumGateSingleSource`'s exact-in-both-directions census (NOT
viewModelParity's value-equality pin — totality is the property): for each
kind, `FACETS[kind]` is the frozen declared set; every facet is consumed by
the popup renderer AND the dossier renderer, or carries a `SURFACE_EXEMPT`
row with a reason class from the closed set `{map-only, print-only,
dm-only, home-absent}`; a facet on one surface and silently absent from the
other reds IN BOTH DIRECTIONS; anti-vacuity floors. **THE CONSUMPTION-
EVIDENCE CONVENTION IS BOUND (r2 — a registry-driven generic renderer
would blind a bare literal scan, silently green or all-red):** the DEFAULT
evidence is the facet KEY LITERAL spelled at the per-facet render site on
both surfaces; where a kind's two surfaces deliberately share ONE renderer
module, the walker instead asserts SHARED-RENDERER IMPORT IDENTITY (both
surfaces import the same module — the premiumGateSingleSource discipline),
and the choice is DECLARED PER KIND in the walker's own manifest — a kind
with zero literals on a surface and no declared shared-renderer row reds;
so does a declared shared-renderer row whose two surfaces do not both
import the module; the army walker enumerates units by the frozen UNIT_SOURCES
registry `['deployments','armyTransit','navalTransit','interventions',
'defenderSiegeLedger']` (H10 — never one ledger's keys); and the decoy
anchored negatives (`objective`/`returnCondition` in no facet set, H5).

**The debts this law retires in its first two waves (all CONFIRMED in
census 2):** the WarFaithSection phantom — TWELVE files spell the name at
this HEAD (the r2 re-census across components/pdf/domain + four test
files); the retirement re-anchors or SURFACE_EXEMPT's the FULL grep census
at build, never a fixed count — in the SAME commit as the first war
projection; the treaty role word
(CR-WR10-G landed screen-only — the PDF still prints "under X's terms" on
sale conveyances), treaty scope (settlement-scoped vs realm-wide under one
heading), and treaty term facets (fraying/lapsing/mediator/coalition
dropped in print) die at `treatyProjection`; the PDF's double occupation
render collapses to the live arm when both fire (JUDGMENT, disclosed print
shift); the five campaign-eligibility spellings and four nameFor spellings
collapse into `resolveOwningCampaign(store, saveId, {require})` +
`settlementNameFor` — each surface keeps its CURRENT eligibility, now
declared through one resolver, so the popup and the tab it links to can no
longer disagree silently (D9/D10). The three inline premium spellings (D14)
are NOT converged here — any new Wayfare surface lands in the premium
census's assertion-B rows or reds it.

**Map-only, ENFORCED:** caravan/population projections live in the map
component chunk's import graph only; a contract test asserts no pdf/, no
export, no publicSafe-reachable module imports them, and
`PUBLIC_TOPLEVEL_KEYS` is untouched (the fail-closed seam already mirrored
to SQL). The owner exception is a structural fact, not a comment.

**Home units (H1, the biggest architectural question):** a home garrison is
a fresh per-tick computation by DESIGN, with no record — so roughly
two-thirds of the army facet set is structurally absent for home units.
RULED at §7 Q2 (vetoable): v1 renders home units through the SAME
`armyProjection` with the completeness map declaring the absent facets
(readiness/experience/mobilization/exhaustion-scar are per-SETTLEMENT
reads and render fine; wear, casus, destination, position are
field-army facts and declare absent; supply, r3 per 2k, is the ONE facet
that moved columns — the home arm derives FRESH from the town's own
stores, "provisioned from stores", no record needed, consistent with this
ruling's no-record spine). Minting a home-unit record is
owner-gated new persisted shape, recorded as elective future work, NOT
built here.

### 3f THE FACET SETS (concrete, the walker's seed data)

**ARMY** (war tab ⇄ popup ⇄ PDF): identity/home (link) · role word
(march/reinforcement/retreat/convoy/blockade — home arm: "garrison") ·
strength BAND (sized glyph + word; never the 0..100 number) · condition
BAND (accumulatedAttrition through the WY-8 ladder — the army's OWN wear) ·
readiness BAND (martial scalar where `hasMartialRecord`; declared-absent
otherwise; the 0-vs-0.5 transit fork is cured at the projection by
presence-checking the deployment record first, H2) · experience BAND
(F6/F7; declared-absent; never the inverted default) · mobilization word
(the ungated warPosture ladder — a SECOND readiness-flavoured facet,
deliberately distinct and labeled "posture") · troop population
(`formatCount` of deployedPopulation where the war-economy flags lit;
declared-absent otherwise — the 0..100 axis is NEVER labeled people, H7) ·
destination (destId/targetId JOIN across both records so a retreat still
tells "had marched on X, falling back" — H8) · departed/ETA (tick date +
weeks — table-register numbers) · supply BAND (2k, F9 through the WY-8
ladder — stock-in-transit over the closed goods vocabulary, food = grain
class, armor = crafted class; home arm derives from the town's own stores,
"provisioned from stores"; declared-absent where `armySupplyEnabled` is
dark or the record predates the field) · the home front
(warExhaustionBand, explicitly labeled the REALM's scar, not the
column's — the three-exhaustions ambiguity resolved by labeling: the
army's wear is "condition", the realm's is "the home front", the road's is
"march fatigue" derived from position01, shown only mid-route) · casus
(typed reasons + receipts where peaceCausalActive) · recall (typed cause
where recalled) · prize (F8 units where present) — DM-only: envoyIntent
declared face + private goals marked covert; commandPicture in a clearly
labeled "what its commanders believe" belief block (H9 — fog shown as fog,
JUDGMENT: shown, because the two-books texture is the product, but never
un-labeled). NEVER: a commander's name (H11, binding), `objective`,
`returnCondition`.

**CARAVAN** (popup only): cargo (good + carried units + band word) ·
origin/destination (links) · departed/ETA · flags (starving-origin cause;
ranDanger) — DM-only: smuggle disposition. **POPULATION** (popup only):
size (formatCount + band) · faiths carried (F4 band composition) ·
origin/destination · why (F5 receipt + travelClass provenance word) ·
expected arrival. **PERSON** (NPC tab ⇄ popup): name/title · the ONE
whereabouts sentence + badge · purpose word (mission/errand class) · phase ·
expected return · escort band · held/ransom state — DM-only: covert mark,
hidden legs, dmLine. **TREATY** (war tab ⇄ PDF): decorate()'s full output —
title (instrument-correct per CR-WR10-G) · both role words ·
termLines with fraying/lapsing/family/good · complianceState ·
mediatorLine · coalitionLine · scope = THIS settlement's documents on both
surfaces (the realm-wide table becomes a labeled realm chapter arm —
JUDGMENT, disclosed).

---

## §4 THE ENCOUNTER-PAIRS TABLE (closed; the map shows encounters, never creates them)

The closed vocabulary, compiled from census 4 (every existing arm CONFIRMED
at its resolver) plus the TWO new arms — E14 (this program's, at WY-6) and
E15 (the espionage program's, admitted at the 2026-08-05 fold by chair ruling
CR-ES-6 and MOUNTED at ES-2, a phase EARLIER than WY-6). The walker
(`tests/lint/encounterPairs.walker.test.js`, WY-6) freezes this table as a
manifest {pair, resolver module, co-location law, outcomes} and
source-scans the encounter/collision resolver set: a resolver outside the
table reds; a table row without a resolver reds; **anti-vacuity at fifteen**.

⛔⛔ **ERRATUM REPORTED AT THE 2026-08-07 (HB + WC + EP) FOLD — THE STRUCK
TEXT ABOVE AND BELOW IS PRESERVED, NOT REWRITTEN, AND THIS NOTE IS THE
AMENDMENT.** ~~"anti-vacuity at fifteen"~~ and ~~"this slice mints no
sixteenth"~~ (§5, F9) and ~~WY-6's "sixteenth-arm plant (walker reds — the
count moved with CR-ES-6)"~~ are all TRUE AS OF THIS FOLD and all go STALE
the day the war-circulation volume builds. **THE TABLE GROWS BY TWO:**
docs/DESIGN_FP_ARCH_WC.md SECTION 8 row 7 claims **E16 (brigand ×
settlement)** and **E17 (column × host)** as WC's arms, landing at WC-3 and
WC-12 — "the table is WY's; the rows are this volume's; every new co-location
resolver adds its row IN ITS OWN COMMIT, moving the closed table's
anti-vacuity count."
⚠⚠ **THE ROWS ARE NOT ADMITTED HERE, AND THAT IS DELIBERATE — THE CR-ES-6
SHAPE DOES NOT TRANSFER.** E15 could be admitted at its fold because ES-2
lands a PHASE EARLIER than WY-6, so the resolver existed by the time the
walker ran. **WC-12 lands AFTER WY-6** (the WC family sits at the PHASE 4
tail; see docs/DESIGN_FP_ARCHITECTURE.md §5), so an E16/E17 row authored now
would sit resolver-less across WY-6's landing and RED this walker's own
`a table row without a resolver reds` arm. The rows therefore land WITH their
resolvers, in WC-3's and WC-12's own commits, exactly as WC row 7 states.
⭐ **WHAT WY-6 MUST DO INSTEAD, so that neither volume reds the other.** The
count arm is the only growth-hostile one, and its mutant is the part that goes
vacuous rather than red: a SIXTEENTH-ARM PLANT tests a literal, and a literal
designed to grow cannot be tested that way twice. **DERIVE, DON'T RESTATE:**
the anti-vacuity assertion reads the table's OWN length and pins it EQUAL to
the source-scanned resolver set, both directions, so it stays true at fifteen,
at sixteen and at seventeen; and the reddenability proof becomes the
**UNTABLED-RESOLVER PLANT** (plant a resolver, do not table it — reds
forever), which is growth-proof, rather than a count plant that stops meaning
anything the moment the count is allowed to move. A WY-6 walker authored
against the literal fifteen would red the two resolvers this estate has now
consciously admitted — the CR-ES-6 failure shape, one level up, and this
wave's own tripwire firing on its own author for the second time.

**⭐ THE CONSOLIDATION BOUNDARY (CR-ES-6, recorded in this volume and in
docs/DESIGN_FP_ARCH_ES.md §7 — the two ladders are ONE registry and TWO
mathematics).** The espionage volume's catch/tap ladder and this table
duplicate a SHAPE — per-stop encounter, graded outcome — and the fold ruled
where the line runs rather than leaving it to be re-derived at WY-6:

- **The encounter-resolver REGISTRY is ONE: this table.** Every module that
  resolves a co-location between two moving or one moving and one fixed body
  carries a row here, closed and walker-scanned, one row per resolver. This
  is why E15 exists instead of an exemption: `espionageGauntlet.js` IS such a
  resolver, and a scan narrowed to let it through would be a hole nobody
  watches. The ORDER makes it urgent — ES-2 lands in PHASE 3 and WY-6 in
  PHASE 4, so the row lands with the RESOLVER (ES-2's own commit, its charter
  binds it), and WY-6's walker is BORN SEEING FIFTEEN and VERIFIES the row it
  finds. WY-6 never re-closes the table around a resolver already admitted.
- **The catch/probability MATH stays PER-RESOLVER.** The espionage catch
  model (`catchChance01` × `legStack(k)` × `dwellRamp`, ES §3.3) and the tap
  ladder (`TAP_LEVELS`, `tapLevelFor`) are MATHEMATICS and stay in their own
  volume; `seizureTake`, the roads gauntlet's rolls and this table's other
  resolvers keep theirs. A manifest whose job is TOTALITY does not become a
  home for probability models, and this volume mints no shared graded-outcome
  helper — the third arm the cohesion pass offered is declined for that
  reason.
- **Consequence for a future arm:** a new resolver adds a ROW (and moves
  anti-vacuity by one) in the commit that mounts it; it does NOT need this
  volume's permission, its author's chair, or a WY wave. The table is a
  registry of what exists, kept closed so nothing exists unlisted.

| # | Pair | Co-location law | Resolver (existing unless marked) | Outcomes |
|---|---|---|---|---|
| E1 | army × army | remaining-path region sharing | armyTransit detectCollisions + resolveFieldBattle | field battle; beaten column retreats |
| E2 | army × envoy errand | EXACT node equality | envoyEncounter + envoyInterceptionStage | field parlay / war_continue / private goals |
| E3 | army × roads mission | currentRegion === hop | roadsKernel + embassyHazard | hostage / delayed / road parley |
| E4 | siege × mission at rest | besieger at destination | roadsKernel | hostage / trapped |
| E5 | embattled road × mission | hop embattlement | roadsKernel | hostage / robbed |
| E6 | host court × mission | arrival at destId | roadsKernel | hostage / expelled / received |
| E7 | sea hazard × mission | currentSeaHop over sea | seaRoads | hostage / delayed / robbed |
| E8 | gate × caravan | route crosses gate node | smuggle + commodityFlow | seizure / confiscation / smuggled / toll |
| E9 | banditry × caravan | route danger remainder | commodityFlow | fractional loss |
| E10 | en-route tap × caravan | intermediary consumes good | commodityFlow | partial transfer |
| E11 | interdiction × artery | edge usage (hidden hops exempt) | routeNetworkConsumersInterdiction | severance reading |
| E12 | third party × ransom | half-term checkpoint | thirdPartyRansom | pay / refuse / compromise |
| E13 | parlay continuation | prefix-extended legIndex | envoyErrandParlay | route re-plan |
| **E14** | **army × caravan (NEW — this program's arm)** | army `currentRegion` node === caravan node at tick, BOTH via the nearest-node (Math.round) snap, documented | **NEW armyCaravanSeizure.js** (WY-6) | seizure (conserved transfer to `prizeCargo`) / passage |
| **E15** | **covert operative × host settlement watch (NEW — the espionage program's arm; admitted by CR-ES-6, 2026-08-05)** | per STOP while dwelling — one resolution per dwell interval, only where the host↔home relationship rung is hostile-class (a friendly host rolls nothing); ES §3.3(b) | **NEW espionageGauntlet.js** (ES-2 — mounted a PHASE before WY-6; the row lands in ES-2's commit) | capture → `foreignGuestHold` cause `caught_spying` through THE ONE hold writer (the same hold family E3-E7's `hostage` outcome names) / uncaught passage / retroactive exposure (ES §3.5's taint road — an exposure, not a hold) |

**E14's economics are REUSED, not minted (census 4's verdict, binding):**
`seizureTake(carried, conscience)` + the stock-credit conservation pattern
from the gate arm, with the seizer's conscience read from the army's HOME
settlement; the prize rides F8 and credits home stocks at the column's own
arrival — never a same-tick teleport. Hostility derives from the existing
war-state reads (the hostilePairFor family); allied and neutral caravans
PASS (the passage arm is the negative control). supplyWebWarfare's
interdiction remains a PRESSURE instrument — its documented deferral is
untouched; E14 is physical taking on physical bodies, a different
instrument, and the twin-note lands in both files.

**The co-location anchor rule (pre-existing divergence, RECORDED not
unified):** three co-location laws coexist (region-sharing; exact-node;
currentRegion-vs-hop with a Math.round/Math.floor disagreement). WY does
NOT unify engine co-location — that is a behavior change in three kernels'
outcomes and belongs to no surface program. WY rules the SURFACE: the
encounter glyph anchors to the ENCOUNTER RECORD's own node, never to the
continuously-interpolated marker position, so the map never draws two
markers apart while the kernel calls them met (the house idiom kept honest
under divergent snaps). The divergence itself is filed as a measured
report to the war/roads chairs.

---

## §5 THE WAVES — twelve, two lanes, dependency-ordered

House discipline per wave (FP §10, binding): one wave = one commit (slices
where marked), DARK per §1c/§2 gates, focused gates per slice + full gate at
wave end through check:tail / gate-tail.sh, pathspec commits, python3
byte-scan on every authored file, CW-0 registry rows same-commit for every
cross-layer read, mutationCoverageManifest entry per executed mutant, spine
requirements 13/14 per wave, ledger row per wave. Effective-line budgets are
targets measured at the publishing commit (L8 — never inherited, including
from this document). ⚠ COLLISION LAW for the whole program: the FP build
lanes are LIVE in this tree (GR/TR lanes had WarFaithTab.jsx,
treatyDocument.js, TreatyPanel.jsx, liveWorld.js, tradeWar.js DIRTY at this
compile) — CHECK-GIT-FIRST is mandatory before every wave, and WY-7/WY-8
land only in a quiet window for those files.

### LANE E — THE ENGINE (slots into FP §5; positions in §5b)

**WY-1 — THE SCALE CHARTER** (J-D11(b) activated; no flag — data-gated by
`spatialDigest.kmScale`, absent on every existing world).
Files: NEW `src/domain/spatial/modeSpeeds.js` (~60 eff); `distanceRead.js`
hopWeeks/calibration gain the kmScale arm + the spectrum surface (budget
the leaf split if the file is near ceiling — measure first); the digest
builder (`buildSpatialDigest`, src/domain/spatial) stamps the datum and
`src/store/campaignSpatialCanonize.js` supplies it — creation-supplied on
first canonize, PRIOR-DIGEST CARRY-FORWARD on every receipted re-canonize
(the §1a.1 clause; the same prior-worldState read the body already
performs for the biome flag); creation-flow disclosure copy (Lane B
slice, one screen). Pins: legacy-absent byte-identity golden (a canonized
world without the datum renders and simulates byte-identical); THE
RE-CANONIZE ROUND-TRIP (r2 HIGH pin — THE PROMISE class: canonize a
scaled world → founding edit → receipted re-canonize → `kmScale`
identical, week spectra identical, `spatialCanonVersion` bumped); the
absolute-arm fixture (two scales, one geometry ⇒ different week spectra;
law-M floor holds at both); the spectrum-derivation lint census
(shrink-only, over the named mover-estate consumers); the MAX_MARCH_WEEKS
clamp receipt pin; the invariant pin (march cap < siege max age, both
worlds). Mutants: kmScale-ignored plant (the absolute fixture reds);
carry-forward-severed plant (a re-canonize that rebuilds without
re-receiving the datum ⇒ the round-trip pin reds — the ghost-write
mutant); floor-severed plant (a sub-week leg mints ⇒ law-M pin reds).
Dormancy: absent-datum worlds are the dark arm by construction.
Owner-signed: the MODE_SPEEDS table + the default scale (the §8 tuning
rows); the wave BUILDS dark and the datum starts flowing only when the
creation flow ships it. Charter note: the flag-home supersession (§1a —
decoupled from `portOpportunityEnabled`, chair-ruled, vetoable) rides
this wave's ledger row.

**WY-2 — THE DURABLE TRUTH** (`severityDurableRumorsEnabled`; mechanism
REBUILT in r2 on the keyed-hash01 discipline — §1b.2 is the spec).
Files: NEW `src/domain/spatial/severityDurability.js` (~90 eff — the rung
multipliers AND the closed-outcome-table degrade: ONE `hash01` roll per
hop over the fork-key tuple, zero conditional draws, the name-swap index
by sub-interval rescale); `rumorNetwork.js` weathering site gains the
two-arm gate (dark: fork + `degradeTelling` VERBATIM; lit: no fork, the
table — both distorting modes, hiFi never weathers); the score→rung
derivation pinned BY INDEX (the grave-collision anchored negative —
magnitude faint/notable/serious/grave vs SEVERITY_LADDER
glancing/telling/grave/ruinous, never joined by word). Pins: four-fence +
lit-mutant (L2); ruinous-near-lossless multi-hop fixture (the owner-intent
pin, now genuinely satisfiable); glancing-drifts fixture; plant-bypass pin
(`disinfo` lineage weathers unfloored); the big-lie emergent-exposure
fixture (grave plant vs glancing plant contradiction pressure, executed);
THE FORK-KEY-SET CENSUS (the module's rng touches enumerate to exactly
the one dark-arm `rumor-organic:` family in BOTH flag states — fork
isolation is the stream guarantee, r2: the draw-count pin is DELETED as
wrong-mechanism); the OTHER-CONSUMER STREAM GOLDEN (a non-rumor
consumer's same-seed stream byte-identical across flag states). Mutants:
multiplier-inverted plant (glancing durable ⇒ fixture reds); a
sequential-draw plant in the lit arm (the fork-key census reds). Seam: ES
composes at the plant site when it lands (§6 seam 7).

**WY-3 — THE MOVER BODIES** (`caravanBodiesEnabled`; owner sign-off rows
F1/F2/F3; slices: 3a fields + normalizers, 3b cadence + contract).
Files: `spatial/commodityFlow.js` dispatch write stamps F1/F2 lit-only
(values already in hand — no new solve) AND its OWN ARRIVE-LOOP
RECONSTRUCTION is taught the two fields in the same commit (r2
attribution fix: the whitelist rebuild that re-mints in-transit records
each tick — the in-file `smuggle:true` precedent is the exact loss class;
`supplyShipments.js` exports no record normalizer, it is the supply-LINK
module; any load-time spatialLedgers normalizer found at build joins the
same commit); F3 re-stamped beside ALL NINE `legArrivalTick` writes in
TWO modules (eight in `worldPulse/roadsKernel.js` + `consumeMissionRecall`
in `roads/state.js` — r2 census; re-census by symbol at build) with the
TREE-WIDE pairing walker; the QUANTIZATION CADENCE (3b): sub-MIN_SHIP
persistent deficits accrue dispatch debt ⇒ MIN_SHIP caravan every ceil
interval, doubled credit, lit-only; NEW `flowRegimeContract.js` (~120 eff
— the per-good-class regime declaration + 2i's bidirectional test shape;
v1 rows M6a-physical; grain rows pre-pinned toward TR-4's T7, tripwired).
Pins: four-fence + lit-mutant; the serialize/regen/undo/import round-trip
for the shipment family, THROUGH at least one in-transit tick so the
arrive-loop rebuild is inside the loop (measured absent — landed here,
not inherited; §5b hands FP #26 the one-line delta: TR-4 VERIFIES and
extends, never re-lands); conservation
identity extended-unweakened (before/after sums with the debt bookkeeping
proven outside the stock identity); THE MARKER-TRUTH SOURCE SCAN (no
render-path module calls a route solver — C4's lie fence, guarding WY-10
from birth); the cadence pin (a persistent half-caravan need receives one
caravan per two months with doubled credit — never zero). Mutants:
re-solve-at-render plant (scan reds); cadence-starved plant (the sub-floor
fixture reds); a TENTH legArrivalTick write without its twin (walker
reds); a field-dropping arrive-loop plant (the through-transit round-trip
reds). Collision: commodityFlow is TRADE's estate — CHECK-GIT-FIRST; land
before TR-4 (§5b) so grain inherits the body.

**WY-4 — THE POPULATION CARGO + THE FENCE** (`flowMigrationPhysicalEnabled`
+ `migrationCargoEnabled`; owner sign-off rows F4/F5).
Files: `flows.js` populationFlows gains the lit physical fork (columns
through the M4 writer path, travelClass ABSENT so M4 lands them — RF-2
mutex untouched) COVERING BOTH APPLY ARMS (r2: the auto arm at mint AND
the proposal-accept arm — a candidate over MIGRATION_PROPOSAL_FRACTION
applies at DM acceptance, which becomes the dispatch tick under the lit
fork, receipted); `migration.js` columnOf carry-through for F4/F5 SAME
COMMIT (composing with POP-1's own carry-through by order); the faith
credit arm in the religiousContest writer family per the Q1 RULING
(BLEND — §7, vetoable), additionally gated on the faith-spread flag pair
WITH the legacy-mirror handling (both spellings, exactly as the UI
writes); NEW exactly-one rows in `flowRegimeContract.js` (netted
flow_migration XOR physical columns per settlement per tick, BOTH flag
states — and the same walker finally covers populationDynamics' bare
spatial fork); THE PRODUCER-OVERLAP CENSUS (r2 — measure M4-shed vs
DISPLACEMENT_STRESSORS trigger disjointness at build; they are NOT
disjoint by construction, §1c). Pins: four-fence ×2 + lit-mutants;
THROUGH-ENQUEUE round-trip for F4/F5 (RF-1 — enqueue rebuild + release +
stage-0 read); the PROPOSAL-ACCEPT pin (the accept path executed lit and
dark — the fence covers both apply arms, never just auto); the DEDUP pin
(no settlement-tick served by both producers for one stressor family —
the fence yields to a same-tick M4 dispatch, deterministic precedence,
receipted); M4 conservation extended-unweakened (fence columns enter the
identity); the WF×POP tellable executed (one-faith column → destination
mix moves at arrival, receipted; dark arm byte-identical); the
omniscience collapse golden. Mutants: both-arms-live plant (walker reds);
double-send plant (one famine dispatched by both producers ⇒ the dedup
pin reds); cargo-stripped plant (columnOf reverted ⇒ THROUGH-ENQUEUE
reds); wrong-lane plant (a fence column minted WITH a demographic class ⇒
attribution pin reds). Order: after POP-1 (shared normalizer; §5b). The
uncredited-exit hole is RECORDED beside the walker, not fixed here (POP's
documented deferral).

**WY-5 — THE CARRIED WORD** (`moversCarryNewsEnabled`).
Files: NEW `moverTidings.js` (~150 eff): at caravan arrival and column
release, reconstruct the origin's telling set as-at departTick (arrival
records with arrivalTick <= departTick — fully derived, zero fields),
offer to the destination through pickBetterTelling at hopCount+1 with ONE
mover-hop weathering roll — the WY-2 closed-table degrade under its own
key namespace (`mover-tidings:`), keyed hash01, zero sequential draws (r2:
one mechanism, two namespaces — never a second weathering spelling); THE
TAP-ORDER LAW binds (2g/ES-H): movers carry
what places TOLD them — the outbound performance fidelity — except events
the origin itself witnessed (the hop-0 root), which carry at the root's
fidelity; severity-durability composes (WY-2's multiplier applies to the
mover hop). Mounts: the two arrival sites, own-flag-before-host-gate.
THE GOING-DARK READ: `lastWordFrom(dest, origin)` over rumor arrivals +
the M6d tally ⇒ the computed fact "no word has come from Alderford since
the thaw" — a Herald kind (`no_word_from`) with the FULL FIVE JOINS (L6)
+ the dossier/popup line. Pins: four-fence + lit-mutant; the
fidelity-injection pin (a mover-carried telling arrives fresher/more
complete than ambient at equal hop depth); THE BLACKOUT TELLABLE (one cut
route starves grain AND news on the same interruption — executed across
WY-3's physical lane); arrival-order determinism (the merge is total-
ordered). Mutants: tap-order inversion plant (a performance-taps mover
delivering root fidelity ⇒ pin reds). Order: after WY-3 (caravan bodies)
and POP-1/WY-4 (column estate settled).

**WY-6 — THE PRIZE ARM + THE PAIRS TABLE** (`caravanSeizureEnabled`;
owner sign-off row F8).
Files: NEW `armyCaravanSeizure.js` (~120 eff — E14 as specced in §4);
armyTransit normalizer taught F8; the home-credit release at column
arrival through commodityFlow's one stock-write seam; NEW
`tests/lint/encounterPairs.walker.test.js` (the closed table, §4) — **BORN
SEEING FIFTEEN: E15 (`espionageGauntlet.js`) was admitted by CR-ES-6 and
MOUNTED at ES-2, a phase earlier, so this walker VERIFIES that row and never
re-closes the table at fourteen; a walker authored against fourteen reds a
resolver the estate consciously admitted, which is this wave's own
tripwire firing on its own author**;
Herald kind `caravan_seized` with the five joins. Pins: four-fence +
lit-mutant; conservation extended (carried = prize + lost; prize credits
home at arrival — the timing pin executed); both conscience arms executed
(lawful loots little / evil near-all); the passage negative (allied
caravan passes untouched); the co-location snap pin (both sides
Math.round, documented); the E15-PRESENT pin (the manifest carries the
espionage resolver and its `caught_spying` outcome — if ES-2 has not landed
when this wave builds, the row is still authored and the resolver-side scan
is what waits, never the row). Mutants: sixteenth-arm plant (walker reds —
the count moved with CR-ES-6);
prize-teleport plant (home credited at seizure tick ⇒ timing pin reds).
Order: after WY-3.

**WY-11 — THE LIFEBLOOD ENVELOPE** (`caravanFloorEnabled` + instrument;
the trade tail).
Files: THE FLOOR LAW (2h): per-region subsistence circulation — a
peddler-grade minimum dispatch eligibility derived from settlement
quantity × tier diversity, owner-signed band; ECONOMIC never physical
(the floor raises dispatch EV/eligibility floors; it NEVER conjures
arrivals through interdiction — cut routes still go dark, and the
executed pin proves an interdicted region's floor delivers zero); THE
DENSITY ENVELOPE soak instrument (caravan density vs activity × tier
diversity × war state — lively AND lean regions reachable; the dead-band
law applied to an emergent quantity); 2j VERIFIED RIDING (war suppression
via seizure-EV; partnership via risk/cost/information — the
market_access/toll_exemption term families' route-cost effect measured
once TR-5 is landed; the payoff named: pair-wise caravan density is the
ambient relationship read, no label, consistent with the ambiguity law);
certification rows for the eight WY flags. Pins: floor-never-beats-
interdiction (executed); envelope reachability both ends; flag-census
rows. Mutants: envelope-collapse plant. Order: after TR-9 (the EV/pact
substrate complete); degraded arms declared if built earlier.

### LANE S — THE SURFACES (SOL_QUEUE Lane B; beside the LD ladder)

**WY-0 — THE LAWFUL CANVAS** (no flag; buildable NOW; the program's first
commit in either lane).
Files: TravelersLayer chevron/flip/tooltip deletion (C1);
WarFaithMapOverlay arrow deletion per the Q3 RULING (r2: RULED, vetoable
— the deletion is wave-assigned HERE; both halves land together, the
owner's veto window is open until this wave lands and the change is
ledger-rowed as owner-visible); NEW
`tests/ui/mapLayerManifest.walker.test.js` (§3a); the persisted-layers
merge pin (a new default key reaches old campaigns with its default —
trace and pin the spread order). The two shipped test files
(travelersLayer, roadsTravelersGeometry) move — a LEGITIMATE one-time
owner-visible shift, recorded against directive 2c in the commit and the
ledger row, never silent. Mutants: dead-checkbox plant (a LayersPanel row
without a store key ⇒ walker reds). Req 13/14: declared-empty (repair +
instrument).

**WY-7 — THE PROJECTION SPINE** (no flag; behavior-preserving except
declared unifications).
Files: entityLinks KIND_PREFIX extension; NEW `treatyProjection.js`
(decorate consumed whole; both surfaces stop post-shaping — D3/D4/D5 die;
the PDF scope/role/term shifts are DISCLOSED print changes, each named in
the commit); `personProjection.js` (heraldWanderers relocated pure-move +
ONE whereabouts vocabulary — D7's unification is a visible dossier wording
shift, disclosed); NEW `resolveOwningCampaign` + `settlementNameFor`
(D9/D10 — each surface keeps its current eligibility, now declared); NEW
`tests/lint/wayfareMirror.walker.test.js` (§3e) seeded with treaty +
person rows; PHANTOM RETIREMENT (C5, r2 census): the retirement clause is
THE FULL `WarFaithSection` GREP CENSUS AT BUILD, never a fixed count —
TWELVE files spell it at this HEAD (comment anchors + claims across
components/pdf/domain incl. dossierLazyTabs, entityLinks, faithPanelModel,
FaithWar.jsx, livingWorldSignals, LivingWorldTab, WarFaithTab, liveWorld
+ four test files incl. summaryTabPhase2 and entityIndex) — every member
re-anchored to the real surface or SURFACE_EXEMPT'd; liveWorldBTrack's
screen half re-pointed or SURFACE_EXEMPT'd print-only until WY-8; a
retirement-completeness grep pin (zero `WarFaithSection` spellings
tree-wide after the wave, or each survivor carries its declared reason)
lands in the same commit. Pins: before/after parity goldens
on every touched surface, with the declared unifications listed and
re-recorded under this ruling. Budget: OutputContainer and WarFaithTab are
near their ceilings — every consumption lands in tab-body leaves, never
the host (the useFogLayer hook-bundling trick where a host must grow).
Collision: ⚠⚠ the dirtiest files in the live tree — quiet window
MANDATORY.

**WY-8 — THE ARMY MIRROR + THE SUPPLY TRAIN** (F6/F7/F9 rows; the
army-facets wave — 2k lands HERE, r3; slices: 8a the supply train
[engine, `armySupplyEnabled`], 8b the mirror [surface; the wave's original
body plus the supply facet]).
**Slice 8a — THE SUPPLY TRAIN (2k; engine; flag `armySupplyEnabled`; F9).**
Files: NEW `src/domain/war/armySupply.js` (~140 eff — the ONE writer for
every F9 mutation, §2b's clause is the spec): march-dispatch mint debiting
origin stores through commodityFlow's one stock-write seam (2i grounding —
a bled home sends empty wagons, surplus-gated, visible in the manifest);
per-tick CONSUMPTION with time/march booking to `consumed`; the coupling
arm — low supply accrues `accumulatedAttrition` (the army's OWN wear, the
condition ladder's quantity) and drags the readiness band down, the
FIELD-side twin of WR-4's home-front drain (the three-exhaustions labeling
of §3f holds: this touches "condition", never "the home front"); the REST
arm — resting in a town debits the HOST's real stores and credits
`supplyCargo`: OCCUPIED requisition mints the receipt that feeds occupation
resistance (the WR-10 fragility coupling, receipted), FRIENDLY hosting
books the ally-burden (coalition strain with physical roots); REINFORCEMENT
columns carry supply as cargo from origin surplus. EMERGENT, ZERO NEW ARMS:
supply lines become strategy by COMPOSITION — cut the road behind an army
and its reinforcement cargo stops arriving (interdiction + the existing
encounter table; §4 stays CLOSED — at FIFTEEN since the 2026-08-05 fold
admitted the espionage gauntlet as E15 per CR-ES-6 — stated as a pin, and
this slice mints no sixteenth) — the
supply-web layer's army-side half; the twin-note lands in
supplyWebWarfare.js and armySupply.js both (its pressure-instrument
deferral stays untouched). Pins: four-fence + lit-mutant; the conservation
identity (debited = carried + consumed, every tick, executed); the
empty-wagons pin (a surplus-less origin dispatches zero cargo — the
manifest says so); the requisition-resistance coupling executed (an
occupied town hosting a resting army gains resistance pressure, receipted;
dark arm byte-identical); the F9 round-trip THROUGH a consumption tick;
the starvation tellable (cut road ⇒ supply falls ⇒ condition degrades in
weeks, executed). Mutants: conjured-supply plant (a credit without a debit
⇒ conservation reds); free-lunch plant (a rest that debits no host store ⇒
requisition pin reds); a second-writer plant (an F9 write outside
armySupply.js ⇒ the single-writer source scan reds). Collision:
commodityFlow stores are TRADE's estate — CHECK-GIT-FIRST; 8a lands after
engine WY-3 (the stock-write seam and cargo idiom it reuses).
**Slice 8b — THE MIRROR (surface; the original wave body).**
Files: `armyProjection.js` grown from armyStrength (audience parameter;
UNIT_SOURCES registry; the declared-absent provenance shape; the retreat
JOIN; the belief block; the commander-name anchored negative); M1's two
conditional stamp lines (warArmyRecord + armyTransitKernel); NEW
`hasMartialRecord` absence predicate (M2); the ARMY CONDITION band ladder
(M4 — new vocabulary, owner tuning row); the SUPPLY band (2k: F9 through
the WY-8 band ladder for field armies; home arm derives fresh from the
town's own stores — "provisioned from stores"; declared-absent where the
flag is dark or the record is old) joins the army popup facet table AND
the mirror-law war tab in the same commit — the mirror walker's army row
gains the facet on both surfaces at once, never one; WarFaithTab consumes
the projection for ALL units (home + field per the Q2 ruling); the B-track
pin re-anchored (deployedArmyStatus gains its first screen consumer — the
vacuity dies). Pins: mirror-walker army rows (now including supply); the
deity-free inversion negative (experience declared-absent, never veteran —
H3 executed); the 0-vs-0.5 fork pin (a recordless world's column renders
absent, never half-ready — H2); the home-unit completeness map pin; the
supply declared-absent pin (dark flag ⇒ the absence row, never a full
band). Mutants: decoy facet plant (`objective` enters a facet set ⇒
anchored negative reds). Order: after WY-7 (8b); 8a additionally after
engine WY-3.

**WY-9 — THE CLICKABLE CANVAS** (store-gated; the four sub-layers live).
Files: the Wayfare layer family restructure (§3a — four sub-layers;
armies absorb navalTransit; persons widen to all three person ledgers;
populations positioned from `rec.departTick` — the census-4 defect cure,
one line; the filter-value migration normalize per §2c); the pointer seam
+ hit-slop + drag-vs-click + keyboard reach + sr-only widening (§3d); the
popup family over PortablePopup with the projection renderers
(armies/persons live; caravans/populations render "no bodies to draw"
until WY-10's bindings); the marker→Herald query (`linesForEntity`); THE
COVERT TIGHTENING (ownerUserId/viewerUserId supplied at every map call
site — §3c); covert markers DM-only with the covert ring, fail-closed.
Pins: declared-absent rendering pin (an absent facet renders the absence
row); the ambiguity anchored negatives (§3b); pan-preserved pointer test;
the a11y row (keyboard open/close, focus trap, sr-only counts). Mutants:
covert-leak plant (a hidden leg rendered without DM ctx ⇒ fail-closed pin
reds). Order: after WY-0 (lawful canvas) and WY-8 (army projection);
persons need WY-7.

**WY-10 — THE FLOW LAYERS** (store-gated; blocks on WY-3 + WY-4).
Files: caravan sub-layer binds ONLY to F1/F2-bearing shipments (the WY-3
source scan already fences re-solves; recordless shipments simply do not
draw — a marker is never a lie); populations popup facets read F4/F5;
caravan/population projections land with their map-only enforcement
(§3e's import census + publicSafe contract rows); the legend rows note
what the layer shows (non-food goods now; grain joins when TR-4 lights —
the layer reads the LEDGER, not the flag, so new shipment classes appear
without a surface edit). Pins: mirror-walker rows with map-only
exemptions; the lie-negative (a bodiless shipment renders nothing);
map-only import census executed. Order: last surface wave.

### §5b THE QUEUE INSERTION (the amendment the chair lands; written to land verbatim, AFTER the pending ES fold)

1. **FP §3 THE FLAG FAMILY — eight rows (numbering continues from the ES
   fold's row 44):** `| 45 | severityDurableRumorsEnabled | WY-2 | severity-
   durable rumor fidelity: the one-multiplier amendment on organic per-hop
   weathering |` · `| 46 | caravanBodiesEnabled | WY-3 | shipment
   departTick+path stamps, the mission leg clock, the quantization cadence |`
   · `| 47 | flowMigrationPhysicalEnabled | WY-4 | the flow_migration
   exactly-one fence: columns, not teleports, under the spatial canon |` ·
   `| 48 | migrationCargoEnabled | WY-4 | faith cargo + launching receipt on
   migration columns |` · `| 49 | moversCarryNewsEnabled | WY-5 | arrival
   fidelity injections + the going-dark read |` · `| 50 |
   caravanSeizureEnabled | WY-6 | the army×caravan prize arm |` · `| 51 |
   caravanFloorEnabled | WY-11 | the per-region subsistence circulation
   floor |` · `| 52 | armySupplyEnabled | WY-8 (slice 8a) | military supply:
   the F9 stock-in-transit, consumption/condition coupling, rest-debits +
   requisition-resistance (2k) |`. The existing-flags paragraph gains: "the WY flags gate
   conditional FIELDS on existing records (§4's WY entry); none mints a key."
2. **FP §4 CANONICAL MODELS — the WY entry:** "WY: nine conditional fields
   on existing records (supplyShipments departTick/path; missions
   legDepartTick; migration faithCargo/causeReceipt; deployments+armyTransit
   experience; armyTransit prizeCargo + supplyCargo) + the frozen-digest datum
   spatialDigest.kmScale (CARRIED FORWARD through every receipted
   re-canonize — the rebuild path re-receives it, lifecycle-pinned); ZERO
   new top-level keys, ZERO new ledger keys; normalizers taught
   same-commit (columnOf THROUGH-ENQUEUE; the commodityFlow arrive-loop
   reconstruction taught departTick/path; supplyShipments round-trip
   landed at WY-3); route positions stay DERIVED — the route-position-id
   guard census. Owner sign-off table: docs/DESIGN_FP_ARCH_WY.md §2a."
3. **FP §5 THE WAVES — insertions (engine lane):** WY-1 lands at the PHASE 1
   tail (after #7 SP-E — spine infrastructure, data-gated dark; the
   J-D11(b) cross-reference DISCHARGED-AND-AMENDED: the mode table lands
   data-gated by `spatialDigest.kmScale`, decoupled from
   `portOpportunityEnabled` per the WY §1a supersession — the war volume's
   §7 row is amended by item 9 below, not contradicted). WY-2 lands in
   PHASE 3 immediately before ES-1 (the manipulation seam composes; if the
   ES fold has not landed, WY-2 slots after #19 IN-3). WY-3 lands in PHASE
   4 immediately BEFORE #26 TR-4 (grain inherits the caravan body; the
   flowRegimeContract pre-pins T7's rows — and TR-4's T7 row is ROUTED
   CENSUS-SHAPED per the r2 correction: "T7 fences the grep-derived
   `importDependency` consumer census of the food ledger, shrink-only" —
   members at this HEAD incl. foodCapacityOf and
   substitutedImportDependency, the tierResourceDynamics same-name local
   excluded by symbol-home; never a count-of-N row). ONE-LINE DELTA OWED
   TO #26 (r2): TR-4 slice 1's "commodityStocks/supplyShipments
   serialize→regen→undo→import round-trip pin (measured absent — do not
   inherit)" sentence gains "— LANDED AT WY-3, ordered immediately before;
   TR-4 VERIFIES and extends it to the grain rows, never re-lands it."
   WY-6 lands after #26 TR-4; WY-11 lands after #31 TR-9. WY-4 lands in
   PHASE 6 immediately after #42 POP-1 (shared columnOf amendments compose
   by order); WY-5 lands after WY-4. The wave count amends 68 → 75
   (post-ES; 60 → 67 if WY lands first — land ES first, it is the older
   debt).
4. **SOL_QUEUE §2 LANE B — the WY surface block** joins beside the LD
   ladder as its own named sub-block: "WY surfaces: WY-0 (early opener —
   independent, buildable now) → WY-7 → WY-8 → WY-9 → WY-10 (serial;
   WY-10 additionally waits on engine WY-3/WY-4; WY-7/WY-8 require a quiet
   window on WarFaithTab/treatyDocument/liveWorld — CHECK-GIT-FIRST)." The
   LD ladder's own size-ratchet gate binds: no WY surface item grows a
   host file at ceiling; every consumption is a lazy tab-body leaf. r3:
   the WY-8 row gains "(slice 8a is ENGINE — the 2k supply train, F9,
   `armySupplyEnabled`; it additionally waits on engine WY-3 and takes the
   commodityFlow quiet window; 8b is the surface mirror as before)."
5. **FP §9 THE SEAM MATRIX:** the eight WY seam rows (§6 below) join under
   prefix WY; the matrix count amends accordingly (58 → 66 post-ES).
6. **FP §11:** the chair-question list stays capped at ten; §11 gains one
   pointer line: "The WY owner-amendment volume carries five WY-prefixed
   chair RULINGS (Q1-Q5, each the volume's own recommendation AFFIRMED,
   each vetoable) — its §7 is the authoritative record."
7. **THE PREFIX ADMISSION (the NF-1-shaped fold obligation):** WY joins
   CHARTERED_VOLUME_PREFIXES as the ELEVENTH prefix (after ES's tenth) in
   the SAME commit as WY's first cross-layer registry row —
   couplingRegistry.test.js pins the closed set TWICE and the admission
   commit edits BOTH sites (r2: the `CHARTERED_VOLUME_PREFIXES` const the
   id-shape regex is built from, AND the literal `toEqual([...])` list
   inside the shape describe — navigate by symbol; editing only the const
   leaves the toEqual red, editing only the toEqual leaves the regex
   rejecting WY ids); every WY wave minting a cross-layer import pair owes
   its CW-0 row same-commit.
8. **The severity-durability cross-reference:** DESIGN_FP_ARCH_IN gains one
   line at its rumor-adjacent waves naming WY-2 as the fidelity amendment's
   home; the espionage volume's §3 manipulation mechanics cite WY-2's
   multiplier as the durability floor they bypass.
9. **THE WAR-VOLUME AMENDMENT (the J-D11(b) supersession, recorded with
   the war chair — r2):** DESIGN_WAR_RULINGS_ARCHITECTURE gains one
   pointer sentence at EACH of its two J-D11(b) sites — the §7 km-scale
   parking row ("owner-signed when the mode table lands") and the WR-7a
   2026-08-02 correction (the "under `portOpportunityEnabled`" clause):
   "SUPERSEDED TOWARD DATA-GATING (WY §1a, chair-ruled, vetoable): the
   mode table lands gated by `spatialDigest.kmScale`, decoupled from
   `portOpportunityEnabled`, which stays the J-D11 (a)/(c) port program's
   flag; the owner-signing obligation is unchanged." The amendment lands
   in the same queue-fold commit as this volume's §5b so the two binding
   volumes never disagree for a single commit.
10. **THE OCCUPATION BOTH-SIDED REGISTER (2l — r3; a VERIFY-THEN-PIN fold
    obligation, not a WY wave):** at the queue fold, CENSUS the existing
    occupation economics by symbol (the occupied-side `benefitYield` to the
    occupier; the war-economy drains; ANY existing occupier-side holding
    cost — the owner believes this mostly built, so the census is the
    truth-finder, multi-spelling greps, live code outranking this row) and
    where the OCCUPIER half is missing, route a WAR-chair queue row pinning
    the occupier-side holding cost (garrison/administration drain on the
    GOVERNING town) so occupation is viable only while BOTH prosperities
    sustain it — a poor occupier cannot afford to hold; a drained holding
    yields nothing and resists more (2k's requisition-resistance arm is
    the feeding schedule already landing at WY-8a). The both-sided
    register feeds the HOLD/RELEASE/SELL strategy calculus and BOTH
    dossiers' risk registers (occupier and occupied), and THE EXIT IS
    NAMED: a soured occupation is the sovereignty market's firesale story
    (WR-10's dark instrument is the sell door) — seize, hold while it
    pays, sell before it ruins you, or hold too long out of pride
    (temperament's business). The pin lands war-side with seam 1's
    tripwire; WY's only build-time contribution is 2k's requisition
    receipts, already specced.

---

## §6 SEAM CONTRACTS (every neighbor, both sides pinned, a tripwire each)

| # | Neighbor | The contract | Tripwire |
|---|---|---|---|
| 1 | WAR | armyProjection READS the war estate (deployments/armyTransit/naval/interventions/defenderSiege) through the frozen UNIT_SOURCES registry; F6/F7/F8 ride war-owned records with the war lane's normalizers taught same-commit; the commander-name boundary and the one-army law bind every facet; the deployment-arrow deletion is RULED (§7 Q3, vetoable) and lands as a war-surface change with the war chair's nod; the J-D11(b) flag-home supersession (§1a) is RECORDED WITH THE WAR CHAIR via §5b item 9's two pointer sentences in the war volume, same queue-fold commit; the co-location divergence (three snap laws) is FILED to the war/roads chairs, not unified here; r3 — F9 supply rides the war-owned armyTransit record with its normalizer taught same-commit, the consumption coupling writes ONLY the army's own `accumulatedAttrition` (never the realm's exhaustion — the three-exhaustions labeling is the contract), WY-8a's requisition receipts FEED the war program's occupation-resistance quantity through its existing writer, and the 2l both-sided occupation register is a VERIFY-THEN-PIN fold obligation routed WAR-side (§5b item 10: census benefitYield/war-economy drains/any occupier-side cost; pin the occupier holding cost where missing; feeds hold/release/sell + both dossiers' risk registers; the sovereignty-market firesale is the named exit) | the UNIT_SOURCES census reds a sixth army-bearing ledger; the commander anchored negative; the Q3 ruling + veto window recorded in WY-0's ledger row before the wave lands; the war volume's two J-D11(b) sites amended or the fold commit is incomplete; the F9 single-writer scan + conservation identity; the §5b item 10 census executed at the fold or the fold commit is incomplete |
| 2 | TR (trade) | WY-3 lands BEFORE TR-4 and hands it the caravan body + the flowRegimeContract grain pre-pins + the FP #26 one-line delta (the shipment round-trip pin landed at WY-3 — TR-4 verifies, never re-lands); the T7 correction is REPORTED CENSUS-SHAPED as a TR queue row ("fence the grep-derived importDependency consumer census of the food ledger, shrink-only" — foodCapacityOf and substitutedImportDependency live at this HEAD; the tierResourceDynamics same-name local excluded by symbol-home), never re-ruled here and never a count; commodityFlow edits are serial with the TR lanes (CHECK-GIT-FIRST); the quantization cadence composes with TR-4's authored grain constant; WY-11's floor is EV-side only; r3 — WY-8a's supply mints, rest-debits, and requisitions move REAL stores exclusively through commodityFlow's one stock-write seam (the same door F8's prize credit uses; supply uses the CLOSED goods vocabulary — food = grain class, armor = crafted class, no new resource type), serial with the TR lanes, CHECK-GIT-FIRST | the grain pre-pin rows red when TR-4 lands without consuming them; the T7 walker's census pin is shrink-only (a consumer leaving the census without a fence receipt reds); a store write outside the one seam reds the stock-write source scan |
| 3 | POP | WY-4 composes with POP-1's columnOf carry-through by ORDER (POP-1 first); the fence's columns land through M4's writer with travelClass absent (RF-2 mutex untouched) and COVER BOTH APPLY ARMS (auto + proposal-accept — r2); the producer overlap with M4's own crisis lane is MEASURED at build and fenced by attribution + the dedup precedence (the fence yields to a same-tick M4 dispatch); conservation identities extended-unweakened; the uncredited-exit hole recorded beside the walker as POP's documented deferral | the THROUGH-ENQUEUE pin; the proposal-accept pin; the double-send mutant against the dedup pin; the wrong-lane mutant; a columnOf edit that drops either program's fields reds both programs' pins |
| 4 | WF (faith) | the F4 credit writes ONLY through religionState's writer family (single-writer law; occupationFaithPull is the shape precedent); gated on the faith-spread pair with the legacy-mirror handling (both spellings); the HEADS/BLEND ruling (Q1) gates the arm's arithmetic; the WF×POP tellable is the shared fixture | a share write outside the writer family reds the single-writer census; the both-spellings lighting pin |
| 5 | IN (information) | WY-2 amends the rumor estate IN's waves ride — the lit arm on the keyed-hash01 closed table, the dark arm verbatim (r2 mechanism); WY-5's tap-order law is the mover instance of ES-H's ruling; the going-dark Herald kind lands with the five joins and IN-5's desk routing honored | the fork-key-set census + the other-consumer stream golden (fork isolation is the stream guarantee — stream theft dies at birth); the kind's own walker file |
| 6 | SP (spine) | WY-1 consumes the transit kernel's documented gradeMultiplier socket — ONE position model, no second floor (law M binds in every denomination); the movement-site manifest is NOT widened here, but the armyTransit exemption (the persisted-position stepper outside MOVEMENT_SITES) is FILED as a structural-prevention candidate — ⭐ **ANSWERED AT THE 2026-08-07 FOLD, AND NOT BY AN EDIT TO THIS VOLUME'S GUARD (CR-WC-10):** docs/DESIGN_FP_ARCH_WC.md SECTION 8 row 10 mints a SECOND, COUNTS-MOVER manifest at WC-11 with its own discovery signature, because `MOVEMENT_SITES` is a NAMED-PERSON leg-physics census by its own header and signature and `armyTransit` is OUT OF ITS SCOPE rather than exempt from it; **law M keeps its scope, its header and its signature UNTOUCHED**, and the second manifest carries a DISJOINTNESS PROOF (a planted named-person token does NOT enter it) so it cannot quietly become a fork of the first; the per-map spectrum surface is SP-consumable | the totality walker's existing rows stay green; a second speed floor anywhere reds law M's walker |
| 7 | ES (espionage) | Wayfare covert rendering RIDES the espionage covert law — one covert predicate chain (hidden legs, covert errand sub-records, viewerSeesDmSecrets, includeCovert-at-the-projection), zero second spellings; the persons layer renders covert operatives DM-only marked covert the day ES lands, with no ES-side surface work; WY-2's durability floor is the thing ES-I's manipulation bypasses, priced at the plant site; the missing-NPC tell composes with the persons layer (absence is visible on the map the way it is in the ledger); **AND THE ENCOUNTER REGISTRY (added at the 2026-08-05 fold, chair ruling CR-ES-6 — this seam row gains the clause; the seam COUNT is unchanged at eight):** ES-2's `espionageGauntlet.js` is an ENCOUNTER RESOLVER, so it carries §4 row **E15** (covert operative × host settlement watch) in ES-2's OWN commit, a phase before WY-6 — the registry is ONE (this volume's §4 table, closed, walker-scanned), while the catch/probability MATH stays ES-side (§3.3) and no shared graded-outcome helper is minted; WY-6's walker VERIFIES E15 rather than re-closing the table, and anti-vacuity is FIFTEEN | the fail-closed export mutant (a covert mover in a player render reds); the plant-site pricing pin lands ES-side and cites WY-2; the encounter walker's resolver-set scan is the tripwire in BOTH directions (an untabled resolver reds; a table row without a resolver reds) and the sixteenth-arm mutant proves the count moved |
| 8 | SURFACES (map/dossier/PDF estate) | the mirror walker is the standing gate for every new facet on any kind; populations/caravans map-only is enforced structurally (import census + publicSafe rows); the premium-gate census (D14) gains a row for any priced Wayfare affordance (the mapChains three-site pattern, gate the affordance never the derivation); host files at ceiling never grow (lazy leaves; the hook-bundling trick); the sr-only summary and keyboard reach are part of every layer wave's done bar | the mirror walker's both-direction reds; the map-only import census; the size ratchet itself |

---

## §7 THE FIVE CHAIR RULINGS (r2: RULED, no longer open — each is the volume's own recommendation AFFIRMED by the chair, 2026-08-05; every one VETOABLE, the owner's veto window is the queue fold and each ruling's ledger row names it; PROMOTED UNCHANGED at the fold — all five row into docs/FABLE_VALIDATION_QUEUE.md, the header restates them, and an implementer NEVER re-rules one silently)

**Q1 — Faith cargo conservation: HEADS or BLEND? RULED: BLEND.** (Gated WY-4's credit
arm; the cargo fields land either way.) Faith today is a proportion field —
integer shares summing to 100, no adherent headcount anywhere in src (C7).
TRUE conservation (HEADS: adherents = share/100 × population, debited at
departure, credited at arrival) makes every existing share write a derived
quantity and collides with the unaffiliated sink, niche capacity, and the
patron seat — a re-founding of the faith model. BLEND (the column carries
the banded composition; arrival pulls destination shares by
arrivals/(destPop+arrivals), renormed — the occupationFaithPull shape)
is cheap, deterministic, and matches every existing precedent, but it is
NOT conservation: the origin loses no faith by sending people away.
**RULING (chair, vetoable — the drafted conservation shape AFFIRMED; no
cohesion finding refuted it): BLEND for v1, with the conservation limit
stated in the volume and the popup honest ("faiths carried", never "faith
conserved"); the HEADS re-founding recorded as owner-elective future
work.** The house idiom bends here knowingly rather than silently — the
column's PEOPLE are conserved (the migration identity); the faith MIX is
an influence, and the owner signs which story the engine tells.

**Q2 — Home units: declare-absent or mint a record? RULED:
DECLARE-ABSENT.** (Gated WY-8's war tab shape; H1.) A home garrison is a
fresh per-tick computation by design — no record, so
wear/casus/destination/position are structurally absent (supply, r3 note
per 2k: derives fresh from the home settlement's stores — the rest arm at
home; no record minted, the ruling's spine untouched). **RULING
(chair, vetoable): declare-absent v1 through the completeness map (the
per-settlement facets — readiness, experience, posture, the home front —
render fine; the field-army facets declare absent with provenance); NO
new owner-gated record is minted — the home-unit record stays owner-gated
new persisted shape, recorded as elective, not built.** The mirror law is
satisfied honestly: the same facet SET, with absence declared rather than
invented.

**Q3 — The deployment arrow: front or movement? RULED: MOVEMENT —
DELETE.** (Gated WY-0's second half; owner-visible change on a shipped,
default-on surface.) WarFaithMapOverlay draws home→target arrows with
arrowheads — direction AND destination on the canvas. **RULING (chair,
vetoable): the arrows are MOVEMENT telling and the ambiguity law deletes
them; the marching army's marker at its road position IS the deployment's
presence, and the siege ring at the target is the front.
Siege/occupation/prize/mobilization glyphs stay (states, not intentions).
THE DELETION IS WAVE-ASSIGNED TO WY-0 — both halves land together,
ledger-rowed as owner-visible with the veto window open until the wave
lands.**

**Q4 — The popup primitive: modal or anchored popover? RULED: MODAL.**
(Gated WY-9's popup family.) Nothing today anchors to a marker;
PortablePopup is the one focus-trapped primitive and it occludes the
canvas. **RULING (chair, vetoable): PortablePopup modal v1 — clicking is
reading, and the map behind a scrim is a reading posture; canvas occlusion
while reading is ACCEPTED; the anchored popover (flip logic, leader line,
its own a11y surface) is RECORDED as a later enhancement, not built,
until the modal measurably fails.**

**Q5 — Does the caravan layer wait for grain? RULED: NO — SHIP ON WY-3.**
(Gated WY-10's timing; 2d(f)'s marker-honesty vs 2d's "the caravans ARE
the economy".) **RULING (chair, vetoable): ship against M6a non-food
shipments the moment WY-3 lands — the markers are honest (real caravans,
real cargo, persisted bodies), and the legend SAYS SO HONESTLY (what the
layer shows now; grain joins when TR-4 lights); grain caravans join
AUTOMATICALLY because the layer binds to the ledger, not the flag.**
Waiting for TR-4 buys nothing but a darker map.

---

## §8 TUNING SURFACE (owner-signed at the soak; raw-authored until then; none in proposedSoakBands until ratified)

MODE_SPEEDS km/week per mode + the default kmScale (WY-1; J-D11(b)'s
owner-signed constant, per the war volume §7 parking) · the
severityDurability rung multipliers (WY-2) · the quantization cadence
bounds + dispatch-debt cap (WY-3) · the faith-credit pull cap (WY-4, per
Q1's ruling) · the mover-hop weathering magnitude (WY-5) · seizure
conscience bands ride the EXISTING seizureTake table (WY-6 — no new bands;
reuse) · the ARMY CONDITION ladder edges (WY-8) · the supply consumption
rates per march/rest tick + the supply band edges + the rest-debit and
requisition rates (WY-8a, per 2k) · the floor law's
per-region minimum band + the density envelope edges (WY-11). Every band
derived from hopWeeks spectra carries the PER-MAP derivation obligation
(§1a.3) — the dead-band law applied per map, forever.

## §9 HERALD + LEGIBILITY (the sentences this program must be able to say)

- "Still on the road, expected by first thaw." (the in-transit render, 2b)
- "An army column." — and nothing more, on the canvas (2c)
- "Five weeks out of Byrne, the column's strength telling." (the popup sentence)
- "Readiness: unknown to this world — no martial tradition." (declared-absent)
- "The grain that never came: Aldmere's caravans stopped three weeks ago." (2d lag)
- "They took the wagons at the ford; the grain feeds the besiegers now." (E14)
- "A column of the Lakefaith marches toward a town of the Flame." (WF×POP)
- "No word has come from Alderford since the thaw." (2g, computed)
- "The road between them was thick with wagons once." (2j, ambient)
- "The far shore is beyond a year's march." (WY-1's clamp receipt)
- "Three weeks of bread in the wagons, and the road home is cut." (2k, the supply band)
- "The column ate Aldmere bare, and Aldmere remembers." (2k, requisition → resistance)
- "The garrison costs more than the holding yields." (2l, the both-sided register — the firesale's opening line)

Every one carries id + full address chain + typed action + named
settlements + recorded reasons per the news address law; every new kind
lands the five joins in its mint commit (L6).

## §10 WHAT THIS VOLUME DID NOT DECIDE (routed, not dropped)

The T7 consumer-census correction, routed census-shaped (→ TR chair queue
row, seam 2). The
co-location snap divergence (→ war/roads chairs, seam 1). The
movement-site manifest's armyTransit exemption (→ structural-prevention
candidate, seam 6). The uncredited population exit (→ POP's documented
deferral, recorded beside the WY-4 walker). The home-unit record and the
HEADS faith re-founding (→ owner-elective, Q1/Q2). Re-canonization of
legacy worlds onto kmScale (→ owner-elective; absent = legacy forever).
The anchored popover (→ deferred until the modal fails, Q4). LOD/
virtualization for scaled-world mover counts (→ deferred with the escape
hatch named, §3a). The five dark dossier prose corpora (warFaith
et al. — a THIRD authored war spelling sits unconsumed; wiring them is
the dossier estate's own debt, named here because the mirror walker will
see them the day they wire). The occupier-side holding cost (2l — r3):
routed VERIFY-THEN-PIN at the queue fold (§5b item 10 → seam 1's war-chair
queue row; the census of benefitYield/war-economy drains decides what is
missing, and the pin lands war-side — WY builds only 2k's requisition
receipts that feed it).
