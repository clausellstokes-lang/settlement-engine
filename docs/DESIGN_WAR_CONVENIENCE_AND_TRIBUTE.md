# DESIGN — COALITIONS OF CONVENIENCE + THE TRIBUTE FAMILY + ECONOMIC STATECRAFT (the war amendments of 2026-08-02)

## Fable 5 architecture, 2026-08-02 (night), under full owner delegation —
## compiled from the owner's design dialogue of this session (allies of
## convenience · multi-coalition wars · the back-out · betrayal under
## allyship · tribute in five bases · the three-goal demand doctrine · the
## justice-scaled resentment law · the healing lattice · trade as war's
## sibling · economic statecraft · the olive branch · the pragmatic
## override · bad deals and great deals). BOUND BY
## DESIGN_WAR_RULINGS_ARCHITECTURE.md and DESIGN_FP_SPINE.md — where this
## document conflicts with either, they win and the conflict is a bug to
## report. IMPLEMENTATION IS ASSIGNED TO THE EXTERNAL IMPLEMENTER (Sol);
## the war volume's §10 implementer protocol binds verbatim. Substrate
## claims cite the landed volumes plus session-verified tree facts;
## anything beyond is marked VERIFY-AT-BUILD with its fallback named.
## COHESION: this document was adversarially checked (2026-08-02/03, six
## slices) against GRAMMAR (catalog closure, trigger closure, TermRecord
## shape, endings vocabulary), TRADE (T-laws, mirrors, built substrate),
## WAR (E3/ally-bar, WR-6 law, the live tree), INTERIOR + POPULATIONS
## (memory clocks, burial, flow laws), COUPLINGS + SPINE (declared-empties,
## requirements 6/10/13/14), and against HEAD for completeness. THE CHECK'S
## FINDINGS ARE FOLDED IN under chair rulings R-A..R-L (2026-08-03). AN
## ADVERSARIAL RE-CHECK then traced all 127 conflicts and returned 14
## RESIDUALS; ALL FOURTEEN ARE REPAIRED IN PLACE (2026-08-03), the
## judgment-shaped ones recorded as J-CT-31..J-CT-34 and the rest inline
## where they sit. This paragraph is a record of two checks performed, NOT
## a clearance: §10's STOP rule governs anything they missed.

---

## §0 THE THESIS

Two findings organize everything here. FIRST: the pairwise law (I — no war
object, no coalition object, membership derived from edges + ties) means
allies of convenience are the engine's ONLY kind of ally, multi-coalition
wars are a graph phenomenon resolving as a CASCADE of pairwise
terminations, and trade blocs are webs exactly as war coalitions are. What
is missing is RECORDS, not machinery: exits, side-switches, betrayal
pricing, deal quality, and the alignment coloring none of it yet carries.
SECOND: the unequal peace and the economic war are incomplete — the victor
cannot demand a SHARE of what the loser is; the demand has no doctrine; the
resentment has no honest arithmetic, no healing lattice, and no deal-quality
read. The cure is ONE term family (five bases, of which only three are
genuinely new work — TB-1), ONE drafting layer, TWO restraint terms, TWO
deliberate lanes (the olive branch and the pragmatic override — the branch
joining the burial, remission, and honest settlement as the healing
lattice's FOUR deliberate doors), ONE derived read, and — by owner addendum
of 2026-08-03 — a FAITH WING of two rows (TB-1b). **AND — by owner order of
2026-08-02/03, out of the exhaustive dynamics sweep — EIGHT EXPANSION WAVES
(§4-EXP), which are part of this cure and not an appendix to it:** XW-1 the
secret protocol · XW-2 the wedge · XW-3 preventive pressure · XW-4 the
subsidy · XW-5 the loan · XW-6 the arbitration compact + the ultimatum
stance · XW-7 scorched earth (specced complete, OWNER-GATED to build) ·
XW-8 the guarantee + the third-party term class. All of it rides the
one-instrument law. Nothing below — **§4-EXP INCLUDED, and the claim was
re-checked over those eight row by row** — adds an actor class, an N-party
object, a timer, a clamp, or a coin (XW-5's loan phases and XW-6's
`expired_unanswered` are both reads against recorded ticks and explicitly
refuse a stored countdown; XW-8's concert is a web plus an SP-8 era read,
never an object). ALIGNMENT (spine requirement 13) is ENGAGED
THROUGHOUT, read-side only, via the derived two-axis settlement read — §6
is the consolidated engagement map AND THE AUTHORITY on which mechanisms
alignment is wired into; no wave's own line claims closure over the map.
POSTURE (requirement 10) is consumed wherever this document opens a
decision surface: `postureOf`/`riskToleranceOf` colour the demand doctrine,
the override bar, the exit choice, the compact's keeping, and the justice
discount — named per wave, read-side, never a new stock.

## §1 THE LAWS THAT BIND EVERY WAVE

Constitutional (war §1a verbatim): same-seed byte identity · dormancy
behind virtual flags, golden-fenced before wiring · seeded purity ·
monotone ratchets (all new logic in lazy leaves) · receipts carry id + full
address chain + typed action + settlements BY NAME + recorded reason ·
premium isolation · finite semantics. K3 (nobody is ever current), E3
(threshold never selector), I (pairwise only), L (no timers, no clamps),
M (the speed floor), T2 (no coin) bind throughout.

- **CT-LAW-1 — NO COALITION OBJECT, EVER.** War coalitions AND trade blocs
  are webs of pairwise records. "How is the quitter treated?" and "who is
  in the bloc?" have no global answers BY DESIGN: each observer computes
  its own from its own ledger.
- **CT-LAW-2 — THE ALLY-BAR EROSION LAW.** Betrayal is reachable ONLY
  through receipted relationship decay: WR-2's ally-bar stands, so the
  knife requires the tie to sour first through named forces. No dice-roll
  treachery, ever — the Herald must be able to narrate the souring before
  the blow.
- **CT-LAW-3 — SURPRISE IS BELIEF LAG.** No surprise flag: a surprise
  attack under allyship is the victim's stale warm picture (K3), sharpened
  by the traitor's use of ally intel channels and the covert seam. The
  counterplay is IN-3's vetting — a paranoid court buys warning with
  posture.
- **CT-LAW-4 — THE BETRAYAL MARKET.** Every treachery reprices the traitor
  in every future join read, in TWO coins: the lawful pay in credibility;
  the chaotic-evil pay materially and coalitionally (severed trade, revenge
  grievances, rising shared-threat scores federating into
  counter-coalitions). Alignment colors WILLINGNESS, never consequences.
- **TB-LAW-1 — TRIBUTE IS A TERM, NEVER A RELATIONSHIP.** The family rides
  the one instrument per pair (R1); occupation and vassalage remain edges;
  "client" is a DERIVED dossier label. The same family serves the war door
  (dictated) and the peacetime door (the weak settlement's offer).
- **TB-LAW-2 — BASES ARE BANDED, MEDIA ARE REAL.** The share ladder is
  CLOSED {tithe, quarter, half, lions_share, all}; payment always moves
  real conserved goods, receipted acts, or real migration columns.
  Prosperity is an INDEX, never a medium (never-transfer-a-derivable).
- **TB-LAW-3 — THE EXTREMITY GATE ON TOTALITY.** The `all` band in ANY
  basis is economic razing: it demands the extremity read exactly as R's
  razing does — WHICH MEANS R's DOUBLE GATE WHOLE, both arms, cited and
  never re-minted: the extremity read AND R2's evil-exclusive boundary at
  initiation. WR-8 states the exception in its own words — the razing is
  "evil-exclusive at initiation (hard alignment gate — the one place
  alignment is a boundary, not a weight)" — and that one place is
  INHERITED here, not opened here: J-CT-8's colors-never-selects law binds
  everywhere else in this document, and the `all` band is the single
  declared exception, owned by WR-8. Its negative case pins hardest
  (clearly-winning-but-not-extreme drafts the lion's share at most; and a
  non-evil victor at full extremity still cannot reach `all`).
- **TB-LAW-4 — NO VIABILITY CLAMP; THE BUILT FLOOR IS PHYSICS, NOT A
  CLAMP** (re-titled 2026-08-03: "above the built floor" implied a
  drafting-side bound that this law explicitly does not create). Ruin-by-
  extraction is legal and priced (law L + moral pricing): famine arcs,
  flight, the commons, source collapse, and the watchers answer it. The
  prosperity index is the structural mercy — the take falls with the
  tributary's health by arithmetic, not by rule. **AMENDED (R-J, chair,
  2026-08-03): THE BUILT RESERVE FLOOR STANDS.** `treatyTransfer.js` is
  landed law — "A treaty may impoverish a court; it may not starve it out
  of" existence (`:35`), `RESERVE_MONTHS: 1.5` (`:56`), and at the floor
  "the term defaults, nothing moves" (`:107-108`); TRADE's lean-year clause
  is the same mercy at the seller's end. **RE-RULED (chair, 2026-08-03),
  because the previous phrasing forbade a drafting clamp in one clause and
  imposed one in the next: THERE IS NO DRAFTING-SIDE FLOOR TEST AT ALL, AND
  NONE IS ADDED.** A sheet MAY promise beyond the tributary's reserve —
  courts overpromise, that is what courts do, and a treaty that cannot be
  paid is a real and tellable thing rather than a bug. **The built floor is
  PHYSICS AT PAYMENT TIME, not a rule about drafting:** the reserve check
  fires inside `treatyTransfer` when the installment is actually moved, so
  a term drafted above reserve simply PAYS SHORT when the year is lean —
  the shortfall is RECEIPTED and reads as STRAINED compliance, with the
  built path's own outcome ("the term defaults, nothing moves") owning what
  a sustained shortfall becomes on the compliance union's terms. Nothing in
  this program reads that floor at drafting time, and nothing needs to.
  **AND THE FLOOR IS NOT A CLAMP — IT IS CONSERVATION**, which is why it
  sits above the no-clamp law rather than in tension with it. The no-clamp
  law therefore binds with no exception: **NO SECOND FLOOR, NO VIABILITY
  TEST, NO DRAFTING CLAMP, AND NO DRAFTING-TIME READ OF THE BUILT FLOOR
  LANDS ANYWHERE IN THIS DOCUMENT'S WAVES** (source-scan pinned at TB-1 and
  TB-3: the drafting path imports no reserve read).
  Consequence, stated so the soak is not surprised: `source_collapsed` is
  reachable through the tributary's own physics (famine, flight, the
  commons, the collapse of the yield the share cuts against), NOT through a
  treaty stream draining a court to zero — the floor makes the stream
  default first. TB-7's goose envelope measures the former.
- **TB-LAW-5 — THE JUSTICE-SCALED STRAIN LAW.** Resentment against the
  charging settlement(s) = ask magnitude × believed injustice. Magnitude
  is physics (share band × duration × basis cruelty × capacity context);
  injustice is BELIEF — the loser's believed war outcome against the ask.
  **CORRECTED (R-K, 2026-08-03): INT-4's discount is generalized, not
  inverted, and it keeps BOTH its named inputs.** INT-4 discounts the
  commons' tribute term "by the believed severity of the war the treaty
  closed", reading "the war's closing receipts and the town's own
  exhaustion scar", with the floor consuming `postureOf` (a defiant-posture
  town forgives tribute more slowly than a weary one). believedInjustice
  reads the same three: closing receipts, exhaustion scar, posture — and
  generalizes them from the paying seat's own commons to the pair. Belief-
  side, K3-pinned, and manipulable by design — BUT ONLY THROUGH THE FOREIGN
  LANES: rumor, envoys, and the information program's disinfo machinery
  across the court boundary. THE SEAT NEVER LIES TO ITS OWN COMMONS
  (CPL-20's declared-empty stands, J-CPL-9): a seat cannot work its own
  crowd's justice belief to shed strain, and no wave here opens that lane.
- **TB-LAW-6 — FOG GOVERNS COMPLIANCE.** Proportional and negative
  obligations enforce against MONITORED reality only — "a poorly-watched
  cheat ghosts as honored" (CPL-5's law): concealment is the obligated
  party's lane (the hidden vein, the under-reported harvest, the smugglers'
  road out the back gate), monitoring reach is the beneficiary's, and the
  whole contest rides believed-vs-true. THE FOG DECIDES DETECTION, NEVER
  QUANTITY (CPL-8/CPL-16, absolute — "belief never enters the conserved
  goods arithmetic anywhere"): the share that moves is the TRUE share of
  the true yield; what monitoring reach decides is whether a shortfall is
  SEEN, and therefore whether the term reads `honored` or `defaulted`.
- **TB-LAW-7 — THE HEALING LATTICE + THE SATURATION BAND.**
  **THE WRITER QUESTION, RULED FIRST (R-H, 2026-08-03) — it decides
  everything below.** The six reducers are READ-SIDE MODIFIERS ON EXISTING
  READS. They mint NO stock, register NO new writer, and take NO decay
  clock of their own. Resentment already has exactly ONE decay owner —
  `relationshipEvolution.js:128-132`'s per-tick mean-reversion toward the
  relationship type's baseline — and INTERIOR §1b's ONE WRITER PER STOCK
  law keeps it. INT-6 chose the read side for precisely this reason
  ("Suppression is a READ over this ledger; no incident row is ever
  deleted"); the lattice follows it exactly. What the lattice supplies is
  a banded, receipted MODIFIER on the grievance read, never a second hand
  on the stock.
  **THE CLOCK QUESTION, RULED SECOND: every clock in this lattice — and
  every resentment clock this document touches, CV-4's betrayal history
  included — ADOPTS THE D5 LIFESPAN-BAND FAMILY (INT-5's law; the seam
  adopts, never invents).** Both `halfLifeTicks` and `maxLookbackTicks`
  scale by the same D5 band multiplier the mean-reversion uses, and INT-5's
  hardest pin binds here in full: BOTH SIGNS SCALE BY THE SAME BAND — the
  band lengthens bright-lane memory exactly as it lengthens dark-lane
  memory. Six healing clocks running human while the wound runs undying
  (or the reverse) IS the anti-ratchet failure that pin exists to catch.
  For an undying court there is no time-based forgetting at all
  (`relationshipEvolution.js:214` — "Infinity (undying) ⇒ 0 (no time
  reversion)"): erosion becomes event-driven, a century-old treachery IS
  still a price, and the deliberate doors below become the ONLY road home.
  **THE SIX REDUCERS.** MUTUALLY BENEFICIAL TRADE — and only that: healing
  requires BOTH sides' own-picture valuations positive (the two-picture
  discipline applied to commerce). NOTE, so no one builds on a
  misattribution: the BUILT drift ladder neutral→trade_partner→allied
  (`relationshipEvolution.js:45-63` — `RELATIONSHIP_RULE_MATRIX` at :45,
  `neutral_to_trade_partner` :47, `trade_to_allied` :53, the allied bucket
  :58-63; the old 38-50 cite pointed at a re-export block) is DRIFT-FORMED
  off candidateDirection/disposition/salience and carries NO own-picture
  valuation input — the two-picture gate is NEW WORK this wave lands ON TOP
  of the built ladder, not a property of it. A TRIBUTE flow in any basis is
  not trade and never heals, and coerced exclusivity or dependency past the
  fear band reads through `dependency_fear` and CAN INFLAME on the same
  flow evidence (conditional, on top of coercion — never unconditional):
  the grain that feeds both mends; the grain that feeds one reminds. THE
  SALIENCE FENCE, stated because the built dampener does not know about
  tribute: `tradeSalience` feeds the hostility dampener off value-of-tie,
  and a one-sided tribute caravan to a food-insecure victor would raise
  pair salience and quietly dampen hostility — i.e. heal. The tribute
  executors therefore EXCLUDE tribute-borne flows from the salience read
  (one fence, one pin, named at TB-3; a fixture asserts a pure-tribute pair
  gains no salience credit). CO-BELLIGERENCE — allying in a NEW war against
  a common enemy: honored obligations write the bright lane
  (`obligation_discharged`, `coalition_debt_paid`), and SHARED BLOOD IS THE
  STRONGEST SINGLE REDUCER ON THE FASTEST CLOCK — convenience's own
  redemption arc, and how allies of convenience become something more (with
  its own risk: a defaulted co-belligerence wounds DEEPER than distance
  ever did — the healing lane and the betrayal market read the same
  record). CHARITY — unilateral relief on the BUILT generosity lane (the
  relief applicator, gratitude bonds, famine_relief as profiteering's own
  casus mirror; `relief_grant` is TR-2-NEW, a house act verb TRADE mints,
  NOT built today): the enemy who fed us is hard to hate — with THREE
  edges, the third of which is a built gate that changes the reducer's
  shape. (i) Above the saturation band the gift reads through BELIEF (the
  mistrusted gift is a receipt, not a heal). (ii) The corruption web
  already prices sustained generosity as cheap recruitment — alms and
  angling are one ledger read two ways, split only by declared-vs-true
  purpose. (iii) **THE gratitudeBonds LAW (tree receipt,
  `gratitudeBonds.js:23`: "NO friendship rides leverage — credit verdicts
  and predatory-weighted gifts deposit" nothing): A GIFT THAT RIDES
  LEVERAGE DEPOSITS NOTHING. A victor charging tribute IS leverage by
  definition, so — stated as the reducer's own precondition — THE VICTOR'S
  ALMS ARE DEAD WHILE ITS TRIBUTE STANDS. Remission first, then charity
  mends. That ordering is the arc, not a bug: mercy has to be paid for
  before it can be believed.** (And the built reframe lane runs the other
  way over the same substrate with a negativity bias — DARK_ENTER 0.35 <
  BRIGHT_ENTER 0.55, gift→debt_unpaid→tribute_extracted — so an extractor's
  gift is MORE likely to read as extraction than as grace; INT-6's
  organic/deliberate split governs, and the lattice is a READ over both
  lanes, never a third writer.) MIGRATION KIN TIES — departure memory's
  bright lane, POP-3's `kinBand` (**CORRECTED: `kinship_bond` is WR-3's
  `lineage_claim` peace mirror over a founding parent/child edge — a live,
  certified, Herald-routed kind — and is NOT the migration token; two
  substrates, two names, never merged**), and ASSIMILATION — **HOMED
  (chair, 2026-08-03; it was sitting in this reducer with no owning volume
  and no producer). `assimilation` is NOT POP's: it resolves to the
  TRADITIONS-ADOPTION machinery, whose declared pair is CPL-12's
  (FAITH/COUPLINGS-owned), and it is cited AS SUCH here — a POINTER to that
  pair's existing read. This volume mints no assimilation producer, no
  assimilation ledger, and no assimilation clock. If CPL-12's pair is dark
  at build, the assimilation arm reads ABSENT, NOT ZERO (SP-4's
  discipline), and the reducer's receipt names which arms it actually
  had.** THE CAUSECLASS
  GATE, ruled: a kin band minted by an EXPULSION column — including this
  document's own levy — does not heal the wound that minted it; only
  `flight`/`opportunity`-classed bands feed the reducer (otherwise
  extraction mints its own cure, and POP-3's declared consumer is an
  AMPLIFIER, not a reducer). SIMILAR FAITH (`common_rite` — the quadrant
  table's bright pole — and `shared_rite`). ALIGNMENT AFFINITY
  (near-quadrant pairs heal faster — the same derived axes, read-side; and
  the affinity term reads BOTH axes symmetrically, which is why §6's
  healing-rate row now colours both columns).
  **THE SATURATION GATE + ITS COMPOSITION ORDER.** Healing is
  SATURATION-GATED and the healing RATE SCALES INVERSELY WITH THE STANDING
  RESENTMENT BAND (the continuous law — §7 tunes this curve, and it must
  exist for that row to mean anything). Below the saturation band, contact
  heals — the grain road quietly mends what the war broke. At or above it,
  ambient healing SUSPENDS and contact can even inflame (trading with the
  hated reads as collaboration to the commons). **COMPOSITION ORDER, RULED
  (R-H): THE BURIAL DISCOUNT APPLIES BEFORE THE SATURATION GATE.** The
  saturation test reads the DISCOUNTED grievance, so a burial CAN
  de-saturate a pair — which is the whole point of a deliberate door. Two
  hysteresis mechanisms now sit on one edge (INT-6's suppression phase-in
  and hold-dwell band, then the saturation band's own width): the order
  above is the tie-break, and J-INT-14 binds the discount itself —
  "a BANDED, RECEIPTED DISCOUNT on the whole edge, family named for
  LEGIBILITY only — never a zero". A saturated wound is reachable ONLY by
  the generational (D5-scaled) clocks and by DELIBERATE, PRICED acts: the
  burial (INT-6), remission (TB-3), honest settlement (`debt_forgiven`),
  and the olive branch (TB-5) — four doors. This is the two-timescale echo
  applied to the heart: contact heals quickly what runs shallow, and only
  time and choice heal what runs deep. The band carries hysteresis (a
  width, never a point) and is NEVER permanent — reversal pinned at fixture
  grade: EVERY saturated pair on a fixture must be healable through the
  deliberate lane.
- **TB-LAW-8 — THE SELF-PRICING LAW.** Every refusal manufactures the
  refused party's grievance: the blockade of the hated town is also the
  minting of its war reason. No embargo is free, ever. **CORRECTED (R-J,
  2026-08-03) — the mirror axis was misnamed.** TR-1's mirrors are
  SEVERANCE↔PARTNERSHIP pairs, two signs off the SAME evidence, and
  `market_exclusion↔market_opened` is the NO_TRADE/access-state pair on
  that axis — NOT an embargoer↔embargoed pair. Reading it as the latter
  would red TR-1's totality+bijection walker. The law therefore lands
  through TR-1's own severance evidence: an exclusion that reaches
  NO_TRADE/access state produces `market_exclusion` on the excluded party's
  ledger through TR-1's existing mirror, and the exclusions that DO NOT
  reach that state (the three built organic embargo levers, floored at
  `EMBARGO_FLOOR 0.15` — a curtailment, never a cutoff) mint their typed
  reason through TR-1's receipt lane instead. Both arms are TR-1's to
  build; this document consumes them and adds no mirror of its own.
- **TB-LAW-9 — THE PRAGMATIC OVERRIDE.** Economic benefit weighs NO MATTER
  WHO OFFERS IT: grievance and resentment COLOR formation and acceptance
  thresholds; they never veto a sufficiently beneficial deal (the E3 shape
  applied to commerce). The against-the-grudge deal is receipted and
  priced on the other ledgers — the commons judge the seat that sells to
  the butchers of the next valley; conscience reads the counterparty;
  legitimacy pays where the crowd cares. Gold has no grudge; the town
  that takes it still does. **THE SEAM, STATED (R-J, 2026-08-03): the
  override operates at TREATY FORMATION AND ACCEPTANCE ONLY.** It does not
  touch — and no wave here edits — the generation-time trade-link gate,
  where `hostile ⇒ NO_TRADE` is a hard rule and not a threshold
  (`region/tradeLinks.js:22-26`, `NO_TRADE_RELATIONSHIPS = new Set(
  ['hostile'])`, applied at `:48`). A hostile pair may therefore SIGN
  against the grudge while its caravans still have no link to run on; the
  instrument that opens the link is TRADE's `market_access` term, landed at
  TR-5. Until TR-5, the override's reachable product is the SIGNATURE and
  its priced consequences, not the trade. Sol builds to that boundary and
  the fixture asserts it.
- **TB-LAW-10 — TRIGGERS DECIDE WHEN, THE CATALOG DECIDES WHAT (R-A,
  chair, 2026-08-03; the closure law this document previously broke in
  four places).** GRAMMAR's formation-trigger set is CLOSED and GR-2 has
  already struck one would-be member for exactly the defect this document
  was committing: a phrase naming a trigger "with no token, no scorer, and
  no reachability pin", returnable only "if and when a `rivalry_cooling`
  trigger is ruled into the closed set with its own producer machinery".
  Under that same return condition, and satisfying it in full, THE CLOSED
  SET WIDENS BY EXACTLY ONE TOKEN: **`rapprochement`** (TB-5's olive
  branch). Its scorer is named — the healing calculus (standing resentment
  band × the deliberate-door reachability) crossed with the proposer's own
  books; its reachability pins are named at TB-5; it validates on import
  like every other token, and it takes its cell in GR-7's trigger-mix
  envelope. ONE token, minted by GRAMMAR, adopted here by pointer.
  EVERYTHING ELSE STAYS INSIDE THE EXISTING FIVE. The economic
  shared-threat read is a WIDENING OF `shared_threat`'s EVIDENCE, not a
  sixth token: the corner-monopolist and the rising trade power are common
  enemies scored by the trigger that already exists (disclosed as a spec
  change to that trigger's product — a shared_threat crossing may now
  propose a commercial sheet as well as `mutual_defense` + `non_aggression`
  — with its own GR-7 envelope note so two unlike phenomena do not blend
  in one cell). And the weak settlement's tribute offer, XW-4's subsidy,
  and XW-5's loan are SHEET COMPOSITION under existing triggers, not new
  triggers at all: `trade_demand`'s believed-scarcity crossing and
  `shared_threat` already say WHEN a seat proposes; what the sheet CARRIES
  is the catalog's business, and the catalog is where those rows land.
  REMISSION IS NOT A TRIGGER AND NOT AN ENDING: it is an `amended` lineage
  act through R1's amendment door, drafted by the victor and running the
  standard accepted-proposal path — one act token, already closed, already
  counted.
- **Requirement 10 (posture): ENGAGED.** Every decision surface this
  document opens consumes the state-derived, outcome-learned, banded
  `postureOf`/`riskToleranceOf` composite — one character, not seven dice.
  Named per wave (CV-1's exit choice, CV-2's vetting, TB-2's doctrine,
  TB-3's remission, TB-4's compact keeping, TB-5's override bar,
  TB-LAW-5's justice floor), read-side, capped, reversal-pinned (no courage
  ratchets). Out-of-posture acts are priced and receipted news, never
  forbidden.
- **Requirement 13 (alignment): ENGAGED — §6 is the map AND the
  authority.** Both axes enter as READ-SIDE WEIGHTS from the derived
  `alignmentOf`, capped, colors-never-selects pinned by the E3 import
  discipline in every consuming evaluator. No stored axis, no writer.
  **CITATION CORRECTED:** `beliefMap.js:915` is the JSDoc of
  `applyAllyIntelSharing`, which takes `alignmentOf` as an INJECTED
  parameter; the producer is `pulseKernel.js:1938` and `:1985`. Cite the
  producer. (Line-anchored citations in this document drift — peaceTerms is
  currently dirty — so Sol re-derives any anchor it builds on and prefers
  symbol anchors; a census overstatement is a STOP-and-report.)
- **Requirement 14 (edit verb):** treaty terms ride the EXISTING treaty
  surface (repudiation, term edits via userEdits, approval lanes); exits,
  switches, doctrine, and the inequity read are engine-derived records
  with no new DM verb (a DM-proposals lane over the demand doctrine is
  DEFERRED, §9); every new receipt kind registers per the standing rules.
  **14(b) LIFECYCLE SURVIVAL — the estate's most-bitten class — is paid
  PER WAVE, not here:** every wave that persists state carries a Lifecycle
  paths line naming JSON-round-trip, regen, undo, and import behaviour, and
  a DM-edited row is NEVER machine-dropped. Waves that persist nothing say
  so explicitly.

## §2 SUBSTRATE (what this extends — volume law + session-verified facts)

**LIVE CODE OUTRANKS THIS TABLE.** It was re-derived 2026-08-03 against the
minifold tree and is two waves fresher than the version Sol may have seen;
any implementer finding it OVERSTATING the tree STOPS and reports rather
than building on it.

| Surface | Where | State |
|---|---|---|
| Four-term termination; cause dissolution; cost-to-stop incl. face | WR-1 | **COMMITTED** (f13cef0f) |
| Ally-bar law; four disposition channels incl. mercantile | WR-2 | **COMMITTED** (7796954e) |
| lineage_claim + its `kinship_bond` peace mirror (warTermination.js:140) | WR-3 | **COMMITTED** (526c5e31) |
| WR-4 belief-side evaluators · WR-5 | WR-4 / WR-5 | **COMMITTED** (0b363cea, e1654184) |
| Join/spend/exit/settle first-class; apportionment weights; adequacy-of-payment band; ingratitude_debt ↔ debt_forgiven; `coalition_debt_paid` vs `coalition_reimbursement_forgiven` (warCoalitionSettlement.js:717,:758); the E3 alliance-web risk read (NEW WORK homed at WR-6) | war volume WR-6 | **IN-TREE / MID-BUILD** (warAllianceRisk, warCoalition{Decision,Expenditure,Graph,Ledger,Settlement}.js + tests; coalitionLedgerEnabled certification row already in subsystemRowsWar.js) — builds in **Lane A** |
| ⚠️ WR-6 BUILD-vs-SPEC DRIFT, recorded: warAllianceRisk prices the ENEMY's web only ("The enemy itself is priced elsewhere and is therefore excluded here"; observer excluded too), and its topology arm is truth-sourced while WR-6 specs a pure belief-side read | tree vs WR-6 | ⚠️ DRIFT — CV-4 must not build on the spec text |
| The unexpected door (a member with no live cause parleys ITSELF — kind (c)); interceptor kind (d) terms-shopping (the third-party plant) | WR-7b | SPECCED |
| Own-picture coalition votes; ratification; no merged estimate | WR-7c / K4 | SPECCED |
| Formation triggers (CLOSED five: trade_demand · faith_communion · migration_pressure · shared_threat · renewal — widened by exactly one, `rapprochement`, per TB-LAW-10); §13 stacking (one term per family × beneficiary, `term_refused_stacking`, frozen COMPOSABLE list); dependency fear; two-picture drafting | GRAMMAR GR-2 / GR-3 / SP-3 | SPECCED |
| GR-7 pact endings (CLOSED: ran_its_term · renewed · converted · renegotiated · repudiated · disavowed_by_succession · hollowed_quiet · hollowed_detected · broken_by_war) + the endings-mix envelope; lineage acts (CLOSED: formed · amended · renewed · renegotiated · converted); per-term COMPLIANCE STATES (honored/strained/defaulted/expired — peaceTerms' own, a DIFFERENT vocabulary) | GRAMMAR GR-7 / §4 | SPECCED/LAW |
| Renegotiation-from-strength; succession-repudiation; conversion | GR-5 / GR-4 | SPECCED |
| One instrument per pair; amendment-shaped formation; directional terms **with beneficiary** | R1 (constitutional) | LAW |
| Catalog canonicality corpus-wide (one list, one spelling, one closure contract); the lazy-leaf writer family; no producer-less term (the non_intervention tombstone); J-GR-15 as the WORKED WIDENING INSTRUMENT (a GRAMMAR judgment block ADOPTS rows into GR-3; the requesting volume becomes a CONSUMER — "pointers, never rows") | R3 / R2 / GRAMMAR §1d / J-GR-15 | LAW |
| ⚠️ `non_intervention` was NOT removed from the tree: peaceTerms.js:207 carries it with `executor:'seam'` — "recorded-not-enforced this wave". The real precedent is LAND-AS-SEAM, not CUT; GRAMMAR §1d owns the tombstone's reading | tree | ⚠️ CORPUS FACT (GRAMMAR adjudicates) |
| TERM_CATALOG's BUILT economic family — EXACTLY FOUR conserved sink-only grain-month streams {tribute, reparations, restitution, resource_share} (peaceTerms.js:185-194, all `family:'economic', stream:true, executor:'transfer'`); CLASS_TERM maps export_flows→resource_share, treasury→tribute (:226-227) with the :376 fallback | tree / TRADE §2 | **BUILT** |
| **THE TREATY RESERVE FLOOR** — "A treaty may impoverish a court; it may not starve it out of" (treatyTransfer.js:35); RESERVE_MONTHS 1.5 (:56); at the floor "the term defaults, nothing moves" (:107-108) | tree | **BUILT — LANDED LAW (TB-LAW-4)** |
| Streams/stores + tribute physics; **tribute_strain minted in `accrueStrainResentment` at peaceTerms.js:1337-1353** (the old :1023-1044 cite is STALE — that range is `advanceTreaties`' return path); installments on the 52-week treaty clock; the margin→duration derivation `0.5 + margin + margin², capped` (LANDED) | war volume + tree | BUILT/LANDED |
| Treaty clock provenance (52 current / legacy 12, never backfilled) — **WR-0c item (4)**; the margin→duration derivation is a SEPARATE landed fact and does NOT live at that citation | GRAMMAR §2 / WR-0c(4) | LANDED |
| The extremity read; **the overwhelming gate**; razing rarity discipline; R2's evil-exclusive initiation boundary (the one place alignment is a boundary, not a weight) | WR-8 / N / R / R2 | SPECCED |
| labor_compact (banded flow read `laborCompactFor(pair)`); migration_right; settlement_provision | GR-3 population family | SPECCED |
| Casus commercii mirrors on the SEVERANCE↔PARTNERSHIP axis incl. market_exclusion↔market_opened (NO_TRADE/access state), famine_profiteering↔famine_relief, dependency_fear↔dependency_comfort, contraband_injury↔honest_gates; walker totality+bijection over the 8 pairs | TRADE TR-1 | SPECCED |
| tradeWar (truth-scored, belief-fenced per T9); the corner; **the intent deposit — commerce's ONE channel to war (J-TR-2), no gate relief** | TRADE + WR-0c(3) + J-TR-2 | SPECCED/LANDED |
| Generation-time trade links: **`hostile ⇒ NO_TRADE` is a HARD GATE**, not a threshold (region/tradeLinks.js:22-26, applied :48); `market_access` is the instrument that opens it | tree / TR-5 | **BUILT (gate) / SPECCED (opener)** |
| The three ORGANIC embargo levers — conscience embargo with `EMBARGO_FLOOR 0.15` ("a curtailment, never a total cutoff — a trickle of trade persists", institutionTolerance.js:59,:135), the weaponized-dependency collapse rule, the merchant lever (±0.05, mean-reverting) — hysteresis-guarded; no typed grievance record names WHY (TR-1 adds it) | tree | **BUILT** (was mis-marked SPECCED) |
| `tradeSalience` per-commodity value-of-tie feeding the hostility dampener (trade-as-peace) | tree (tradeSalience.js) | **BUILT** — TB-LAW-7's salience fence bites here |
| J route lifecycle / route chartering through the docket | routeNetworkCharter*.js | **BUILT + TESTED, DARK** (`routeLifecycleEnabled: false`) |
| dispatchEV = a per-(origin,destination) GO/NO-GO gate with a REFUSE/RESUME deadband; destination CHOICE among candidate markets ("WHERE") lands at TR-3 behind `believedMarketsEnabled` | tree / TR-3 | **BUILT (whether) / SPECCED (where)** |
| The relationship drift ladder neutral→trade_partner→allied — RELATIONSHIP_RULE_MATRIX at relationshipEvolution.js:**45-63** (:47 neutral_to_trade_partner, :53 trade_to_allied, :58-63 allied); drift-formed, never decided; **no own-picture valuation input exists** | tree | BUILT (session-verified) |
| Per-tick mean-reversion of trust/resentment/fear toward baseline (relationshipEvolution.js:128-132) — **the ONE decay owner of the resentment stock**; D5 band scaling both signs (:214 — undying ⇒ 0, no time reversion) | tree / INT-5 | **BUILT — one-writer law** |
| Built tribute EXTRACTION on patron/client/vassal edges: `patron_extracts_tribute` (:65), `vassal_tribute_extraction` (:77), implemented relationshipRulesCore.js:411,617, certified subsystemRowsPeople.js:129,153, Herald-routed heraldRouting.js:141,455 (`vassal_extraction`) | tree | **BUILT, CERTIFIED, ROUTED** |
| Migration columns; causeClasses **{flight, opportunity, EXPULSION, DISPERSAL}** (CLOSED, stamped from evidence the flow carries); `rememberedAs` **{mourned, sent_forth, cast_out, scattered}** (CLOSED, one per causeClass); departure memory ONE ledger ONE writer (destination grammar is projection-side); THE ARRIVAL CLEARING + refused_at_the_wall; J-POP-15's REAL return column; the 300-tick conservation identity (departures === arrivals + returned + lost + inTransit) with its leak-detecting negative control; J-POP-10 (columns never kill — scoped to P2; M4 keeps its two mortality sinks) | POPULATIONS POP-1/3/5/5b | SPECCED |
| The commons tribute term DISCOUNTED by believed war severity, reading the war's closing receipts + the town's own exhaustion scar, floor consuming `postureOf` (the justice discount's seed); the king-who-paid arc; **J-INT-3: the commons term rides the EXISTING composite under a cap — NEVER a new direct write** | INTERIOR INT-4 | SPECCED |
| The burial + the dig-up (pair-keyed; closes the DECREEING side's reads only — unilateral burial is half a peace); the D5 memory clocks (both-signs scaling, INT-5's hardest pin); the reframe lane (dark gift→debt_unpaid→tribute_extracted / bright debt_unpaid→gift_forgiven, DARK_ENTER 0.35 < BRIGHT_ENTER 0.55) | INT-6 / INT-5 / INT §2 row 14 | SPECCED (INT-6/5) / BUILT-DARK (reframe) |
| The generosity lane (relief applicator, gratitude bonds — **`gratitudeBonds.js:23`: NO friendship rides leverage**); the corruption web's generosity-affordability read. `relief_grant` is **TR-2-NEW**, not built | tree; TR-2 | BUILT (lane) / SPECCED (relief_grant) |
| M6a **integer** conservation (EXACT five-term balance, kernel refuses to persist imbalance); the ONE-DELTA-APPLICATOR discipline (generosityUpdates.applyFoodDeltasToUpdates); **T7's exactly-one-arm walker is FOOD-ONLY and two-sited** — it buys no coverage for arbitrary resource classes | TRADE TR-4 / T7 / J-TR-8 | SPECCED |
| `alignmentOf(id) → {lawfulness01, malice01}`, derived, no writer — **producer at pulseKernel.js:1938,:1985** (beliefMap.js:915 is `applyAllyIntelSharing`'s JSDoc; alignmentOf is injected there) | tree | BUILT (session-verified) |
| `climbDownConsequence({actorId, lawfulness01, exitKind})` → price01, folded into stop01 (warTermination.js:762-766; momentum.js:1054) — **the lawfulness oath-price weight is ALREADY LIVE**; `FACE_SAVING_EXITS = {white_peace 0.35, non_aggression 0.3, mediation 0.2, declared_resolution 0.6}` in momentum.js, a **FOREIGN read-only file** | tree / C2's foreign-file law | **BUILT** |
| Grievance ledgers; revanchism; the loud-fact exemption (extraction is public once news latency is paid) | INT / C-LAW-5 | SPECCED/LAW |
| CPL-21: tribute_strain points OUTWARD at the victor, never inward at the paying seat; INT-4 owns the inward arc, INERT without `economicCoupReadEnabled` | COUPLINGS CPL-21 / INT-4 | LAW / INERT |
| CPL-17's declared-empty on population-transfer — **RE-RULED BY OWNER OVERRIDE 2026-08-02** (the levy is anonymous columns only; the hostage-surety half of the empty STANDS) | COUPLINGS CPL-17 | OWNER-OVERRIDDEN (recorded both sides) |
| The conscience embargo's moral drift; the observer's-axis judgment | CPL-7 / TR-6 / WR-10 | SPECCED (the embargo itself is BUILT, above) |
| VERIFY-AT-BUILD (the ledger — §10's resolve list is IDENTICAL to this row by construction): (1) stream-executor shapes in peaceTerms' leaf family; (2) the warReasons grievance-feed spelling for debt-fed mass (resolves at CV-1 start); (3) the per-resource YIELD read **plus the non-food conservation arm** (both resolve at TB-3 start — report which arm; fallback: TB-3 lands its own monitored-yield read and its own non-food conservation arm, each with its own pins); (4) dispatch's exclusion-consumption surface for embargo_compact (resolves at TB-4 start); (5) the two-picture signature persistence (resolves at TB-6 start; fallback: TB-6 lands it as slice 1, disclosed) | tree | VERIFY-AT-BUILD |

## §3 THE CONVENIENCE WAVES (CV-1..CV-4; flag `coalitionConvenienceEnabled`
## + a WR-9-style CERTIFICATION ROW of its own; lit-preconditions: WR-6 LIT
## and WR-7 LIT — not merely landed; the lighting order IS the build order
## and a flag lit out of order is an invalid config the certification walker
## reds; one commit per slice)

**THESE WAVES EXTEND LANDED CODE.** WR-1..WR-5 are COMMITTED and WR-6's
module family is in the working tree with its certification row already
written. No CV slice mints what exists: each brief must open by naming the
landed module it extends, and any brief that reads as a fresh mint is
wrong and is a STOP-and-report.

### CV-1 — THE EXIT VOCABULARY (the back-out gets its records)

A closed exit set, minted at the moment a member stops discharging:
- **`exit_settled`** — the quitter squares its coalition debt (the WR-6
  adequacy band met) and, where its own edge carries live causes, sues its
  separate peace through the normal envoy lane. **Records
  `coalition_debt_paid`, NOT `debt_forgiven` (R-I, corrected).** WR-6 is
  explicit and the built code agrees: "Payment is a conserved transfer +
  `coalition_debt_paid` fact and consumes/reduces the obligation. It does
  NOT fabricate `debt_forgiven`: that mirror remains the bright
  interpretation of a still-live obligation (forgiveness, not payment)" —
  `warCoalitionSettlement.js:717` mints `coalition_debt_paid` /
  `coalition_debt_unpaid`, and `:758` keeps forgiveness a separate act
  (`coalition_reimbursement_forgiven`). Forgiveness of a LIVE obligation
  remains available and remains a different, brighter story. Also records:
  the treaty.
- **`exit_defaulted`** — it just stops. alliance_obligation defaults at a
  severity banded by the GAP between the obligation and the EVIDENCE-BOUNDED
  contribution derivation for the ACTIVE episode (apportionment weights) —
  the realm that bled two years and leaves is read differently from the
  token-levy quitter. **WR-6/J-WR-5 bind: NO STORED EXPENDITURE TOTALS and
  no parallel journal; evicted or healed evidence is SILENCE, never an
  invented lifetime sum.** The gap is therefore banded from what the
  episode's surviving evidence supports, and a quitter whose evidence has
  healed away bands LOW — which is itself the honest story (the world
  forgot what it cost you). Records: ingratitude_debt + a grievance ENTRY
  on each ally's pair ledger. THE RESENTMENT CHAIN IS HEREBY NAMED:
  unsettled ingratitude_debt feeds grievance mass that the war-reasons
  layer READS — the betrayal casus is derivable, never implied (one clause,
  one pin; VERIFY-AT-BUILD the feed spelling, resolving at CV-1 start).
- **`exit_cold`** — withdrawal with NO separate peace: the quitter's own
  edge keeps its live causes at zero deployments — THE COLD EDGE, a named
  dormant state with its receipt ("The war was not ended; it was only set
  down."). Re-ignition is the counterparty's choice through the NORMAL
  opener gates (no new opener, no new casus mint — the old causes stand).
  **THE COLD EDGE IS A STATE CENSUS, NOT AN ENDING (R-I, ruled).** WR-9's
  closed endings mix is UNTOUCHED and gains no cold slot; a zero-deployment
  live-cause pair is COUNTED by TB-7's instrumentation as a census cell,
  not graded as an outcome. And WR-1's four-term termination read not
  firing at zero deployments IS THE DESIGN, stated so nobody patches it:
  the read is per surviving deployment per pulse, so a set-down war simply
  has nothing to decide until deployments return — re-ignition is exactly
  that return. No stored per-pair "cold" state exists (source-scan pinned);
  the condition is derived from live causes × zero deployments, which is
  also why it cannot collide with CPL-5's declared-empty on ceasefire
  objects.
- **PARTIAL DISCHARGE IS NOT A FOURTH EXIT (folded, tree-driven).** The
  built settlement produces three payment statuses (`paid`/`partial`/
  `unpaid` — warCoalitionSettlement.js:502,:691). The exit vocabulary stays
  CLOSED AT THREE (J-CT-6): a partial settler exits `exit_defaulted` with
  its adequacy gap banded by what it DID pay. The partiality lives in the
  gap band, never in a new act token.
- **THE FACE-SAVING SEAM, ruled (R-I).** `momentum.js` is FOREIGN and
  read-only (C2's law; §10.1's "never touch momentum.js"), and it owns the
  only exit vocabulary the engine PRICES: `FACE_SAVING_EXITS =
  {white_peace 0.35, non_aggression 0.3, mediation 0.2,
  declared_resolution 0.6}`. CV-1's three kinds therefore MAP READ-ONLY
  onto those existing inputs at the call site — `exit_settled` → the peace
  it actually signs (`white_peace`/`non_aggression` as the sheet says),
  `exit_cold` → `declared_resolution` ("declared victory and went home",
  which is the cold edge's semantics almost exactly), `exit_defaulted` →
  no face-save (the un-face-saved price it deserves). NOTHING IS EVER
  REGISTERED INTO momentum.js. The mapping is a lookup in CV-1's own leaf,
  source-scan pinned against any import-side edit.
Per-observer treatment then COMPUTES (CT-LAW-1), and here is WHO READS
WHAT: the ENEMY reads isolation + believed weakness (opportunism), gated by
whatever treaty exists; EACH ALLY reads its own debt + its own grievance,
from its own ledger, with no global answer anywhere; the reckoning arrives
via GR-5 renegotiation-from-strength (the winner reopens standing
arrangements against the quitter's evidence-bounded damage) or debt
settlement. NOTHING NEW — the wave is the vocabulary, the receipts, the
read-only face mapping, and the debt→grievance clause.
- **Force/counterforce (same evidence):** the exit's relief (the drain
  stops, the books breathe) ↔ the exit's bill (debts, grievances, the web's
  repricing) — one record set, two signs.
- **Alignment:** lawfulness01 colors exit-choice price sensitivity (the
  lawful pay for settlement; the chaotic default cheap in their own eyes —
  and only their own). Colors, never selects.
- **Posture (req 10):** the exit CHOICE consumes `postureOf`/
  `riskToleranceOf` — a defiant court sets the war down cold where a weary
  one buys its way out; out-of-posture exits are priced and receipted,
  never forbidden.
- **Clock (C-LAW-4):** fast = the exit act itself (same-tick, own court);
  SLOW = every cross-court consequence, which propagates at NEWS SPEED with
  its carrier named (rumorNetwork `arrivalTick += hopWeeks`, distance-priced
  surcharge) — an ally learns of the default when the word arrives, never
  the tick it happened. All durations denominated against `INTERVAL_WEEKS`.
- **Lifecycle paths:** the exit receipts and the ingratitude_debt/grievance
  entries persist on the existing coalition + grievance ledgers —
  JSON-round-trip pinned, regen and undo carry them with their records
  (never re-derived from the chronicle), import validates the act token
  against the closed three and drops invalid rows with a logged receipt. A
  DM-edited row is never machine-dropped. No new ledger.
- **Endings: none new** (fragmentation stands; exits are ACTS on the way,
  and the cold edge is a census cell — WR-9's mix is untouched).
- **Bands:** the adequacy-gap severity ladder and the exit-receipt
  significance classes are **SP-6b family REFERENCES with SP-6a class
  assignments — this wave authors no scale** (SP-6's R5: volumes reference
  a family and assign their kinds to its classes, never author one). The
  adequacy band itself is WR-6's; CV-1 assigns against it and mints no
  second band family.
- **Couplings:** CW-0 rows war×interior (the quitter's commons) and
  war×grammar (the separate peace), each carrying the full key set —
  pairId, direction, read, receiptField, counterforce (module.export),
  flags, owningVolume, owningWave, intendedDesk — in the SAME commit as the
  read. Cascade registration: a multi-ally exit cascade registers with CW-1
  per C-LAW-6 (depth never becomes wallpaper).
- **Dossier round-trip:** exits, standing debts, and cold edges render on
  the war/treaty panels with the cause walk intact — open the town, find
  the trace where a DM would look. Herald: pool per receipt kind under the
  content-depth floor (≥4 variants each, the annex discipline).
- **Pins (negative hardest):** the three exits distinguishable on one
  seeded coalition fixture; a cold edge re-ignites WITHOUT new casus; a
  settled quitter with a NAP is NOT attackable through the treaty block
  (WR-0c item 1's machinery, LANDED); a defaulted quitter's ally holds the
  gap-banded grievance; a PARTIAL payer lands exit_defaulted at a smaller
  gap than a zero payer; `exit_settled` writes `coalition_debt_paid` and
  NEVER `debt_forgiven` (source scan); no `debt_forgiven` is ever minted by
  a payment path; the face mapping reads momentum.js and writes nothing
  into it (import + source scan); NO global "neutral" state anywhere
  (source scan: no neutrality field); regen/undo round-trip preserves every
  exit record.

### CV-2 — THE SIDE-SWITCH (the turncoat gets a shape)

alliance_obligation defaulted at BETRAYAL severity (the top band of the
SP-6b severity family — referenced, not authored) + entry into the opposing
web through the NORMAL join read (no teleported war entry; the new allies
price the turncoat with CV-4's history term already) + THE TURNCOAT RECEIPT
naming both movements in one sentence. Reachability (CT-LAW-2): only across
an eroded tie — the negative pin asserts a healthy-tie switch is
UNREACHABLE — or through the covert pre-arranged defection. **THE COVERT
DOOR IS WR-7b KIND (c), CORRECTED:** kind (c) is "a coalition member of the
target — who may PARLAY THEMSELVES if their own edge carries no live cause
(peace entering through the unexpected door)", which is exactly an ally
arranging its own switch. Kind (d) (terms-shopping) requires intercepting
somebody ELSE's travelling envoy by position co-location, and homing the
switch there would make treachery hostage to an interception event; kind
(d) remains available as the third-party plant, never as the defector's own
route. The covert lane keeps the tie LOOKING healthy while the true purpose
rots it — and that is CT-LAW-3's surprise, priced by the victim's vetting
posture. A NEW coalition entering a war is NOT a switch (§5).
- **Force/counterforce (same evidence):** the knife's prize (the opposing
  web's terms, the exhausted friend's weakness) ↔ the betrayal market's
  bill, minted off the SAME switch record — every gain is entered twice,
  once as profit and once as price.
- **Alignment:** malice01 colors the willingness read on betrayal-shaped
  opportunities; lawfulness01 the oath-price. Colors, never selects.
- **Posture (req 10):** the VICTIM's `postureOf` prices the vetting (a
  paranoid court buys warning); the defector's own risk tolerance colors
  whether it moves at all.
- **Clock (C-LAW-4):** the default and the entry are same-tick own-court
  acts; the world's REPRICING of the turncoat travels at news speed with
  its carrier named — a distant court prices a betrayal it has not yet
  heard of at zero, which is the story. INTERVAL_WEEKS denominated.
- **Lifecycle paths:** the switch record persists on the existing coalition
  ledger + the betrayal history CV-4 reads — JSON-round-trip pinned; regen
  and undo carry the record (never re-derived from the chronicle); import
  validates it; the covert chain's envoy records ride their own existing
  lifecycle. No new ledger.
- **Endings: none new.** **Bands:** BETRAYAL severity is an SP-6b class
  assignment; the turncoat receipt class is an SP-6a assignment.
- **Couplings:** CW-0 row war×war (the switch as the web's own repricing)
  and war×information (the covert lane's declared/true purposes), full key
  set, same commit. Cascade: a switch that re-shapes two webs registers
  with CW-1.
- **Dossier round-trip:** the switch renders on both pairs' war/treaty
  panels with the full backward walk (envoy records, true purposes) intact
  — the post-hoc "how did we not see it" page.
- **Pins:** healthy-tie switch UNREACHABLE (the hardest negative); the
  covert defection leaves the full backward-walkable chain; the self-parlay
  route is kind (c) and requires no interception event (fixture); the
  turncoat's next join is priced (CV-4 fixture); double-default (switch
  then switch back) compounds, never resets; regen/undo round-trip.

### CV-3 — THE ALIGNMENT WEIGHTS (requirement 13 paid, war edition)

Two capped read-side weights from the derived `alignmentOf`. **SCOPE
CORRECTED (R-K): this wave WIRES two weights; it does not close the
document's alignment engagement. §6 IS THE AUTHORITY on where alignment is
read, and §6 lists more rows than this wave builds — the rest are wired by
their own waves under §6's discipline.** The two here:
- (a) **malice01 scales opportunism's read of a FRIEND'S weakness** — the
  good realm discounts the exhausted ally as prey; the malicious one savors
  it. **LANDING CORRECTED (R-I): the weight lands THRESHOLD-SIDE, never on
  the casus score.** E3 is verbatim — "no disposition read may pick a
  target, manufacture a grievance, or override a relationship. No ally
  exemption anywhere — the relationship IS the bar" — and the built house
  idiom is the same shape (`warCoalitionDecision.js`: the RELATIONSHIP
  makes the score — `obligation01 = pactStrength*0.5 + trust*0.3 +
  dependency*0.2` — while DISPOSITION makes the bar). So malice01 modulates
  the THRESHOLD an opportunistic move must clear where the pair carries a
  live or recent alliance tie; it never touches the relationship term and
  never mints a casus. **WR-2's passed-over-ally pin is CARRIED HERE, both
  arms:** a lowered threshold does not clear a strong friendly tie —
  ordinary case, allies passed over; pressured case, betrayal reachable and
  proportionally priced. (Chair note, recorded rather than assumed: E3 is
  literally scoped to "disposition read". This wave RULES that an alignment
  read inherits E3 whole — same shape, same discipline, same pins — because
  the alternative is a second, unfenced selector class. Vetoable.)
- (b) **lawfulness01 scales the PRICE SENSITIVITY to oath-breach** in the
  exit, switch, and termination reads — the lawful court weighs the
  credibility and face bill heavily; the chaotic court shrugs it. **BUILT-
  PATH CORRECTED (R-I): this weight is ALREADY LIVE and is EXTENDED, never
  re-minted.** `warTermination.js:762-766` already computes `const face01 =
  climbDownConsequence({ actorId, lawfulness01: threshold.lawfulness01,
  exitKind: '' }).price01` and folds it into `stop01` via `STOP_FACE_W`;
  `momentum.js:1054` owns the function. CV-3 therefore EXTENDS that call's
  INPUTS (supplying the real `exitKind` from CV-1's read-only mapping, and
  the exit-choice scoring's own use of the same `price01`) and adds NO
  second face term. **J-WR-11's double-count law is cited and binds:** WR-2
  refused to run two history multipliers on one appetite for exactly this
  reason; two face terms on one price would be the same defect. The named
  wiring points are the cost-to-stop face term and the exit-choice scoring
  — those two, no others.
Colors, never selects: the E3 import discipline pins both, **and the
operative clause is stated because it is the testable part — NO ALIGNMENT
READ MAY NAME A TARGET**; the guard-the-guard control proves the scan
bites.
- **Force/counterforce (same evidence):** the lawful court's high face
  price is its own restraint AND its own rigidity — the same weight that
  keeps it honest keeps it in a losing war; the chaotic court's cheap exit
  is its agility AND the market's memory of it (CV-4 reads the same acts).
- **Alignment:** this wave IS one of §6's engagements (rows "Exit choice",
  "Betrayal willingness"); it claims no others.
- **Posture (req 10):** the face price composes with `postureOf` at the
  same call site — alignment colors HOW MUCH the bill stings, posture
  colors whether this court is in a mood to pay it.
- **Clock:** no new clock — the weights ride the reads they colour, at
  those reads' existing cadences (INTERVAL_WEEKS denominated).
- **Lifecycle paths:** NONE — no stored axis, no writer, no record. The
  weights are pure reads over a derived value; there is nothing to
  round-trip, and the source scan asserting no stored axis IS the pin.
- **Flag + preconditions:** `coalitionConvenienceEnabled`, with its own
  certification row. **`warTerminationEnabled` is declared a LIT-
  precondition of CV-3** (this weight reaches WR-1's termination read, and
  the WAR flags are independent dark switches — the dependency must be
  declared or the lighting order is invalid). **WR-1's §1b-B receipt field
  is EXTENDED to name the alignment state that moved the score**, so a
  termination receipt never silently changes which term is DECIDING.
- **Couplings:** CW-0 row war×belief (alignment → price sensitivity), full
  key set, same commit. **Dossier round-trip:** DECLARED NONE, with reason
  — a read-side weight has no state to render; what renders is the RECEIPT
  of the read it coloured (the termination and exit receipts, which name
  their inputs), and those are CV-1's and WR-1's panels.
- **Bands:** both weight curves, capped, §7 table.
- **Pins:** colors-never-selects (E3 import discipline, both weights); the
  guard-the-guard control reds when the scan is neutered; the malice weight
  moves the THRESHOLD and never the relationship term (source scan);
  WR-2's passed-over-ally pin, BOTH arms; exactly ONE face term exists in
  `stop01` after this wave (source scan — the J-WR-11 double-count
  negative); no stored axis anywhere; a termination receipt names the
  alignment state that moved it.

### CV-4 — THE BETRAYAL MARKET (the join read learns history)

**HOMING CORRECTED (R-I): the betrayal history term lands on THE JOIN
DECISION's bar/refusal side — WR-6's join read — NOT on the alliance-web
risk read.** The tree is unambiguous about what that read is for:
`warAllianceRisk.js` prices "the enemy's alliance web to depth two… The
enemy itself is priced elsewhere and is therefore excluded here" (observer
and enemy both in `excluded`), and `warCoalitionDecision.js:110` folds it
as `(1 - risk.risk01) * 0.15`. Raising `risk01` for a serial betrayer would
therefore LOWER *that betrayer's own* join score — discouraging the traitor
from joining instead of repricing the traitor for everyone else, the exact
inversion of CT-LAW-4. The trustworthiness term belongs on the CALLER's
side: it discounts `obligation01`'s trust component and raises the join
BAR/refusal cost that a candidate with a betrayal record must clear when
OTHERS weigh taking it in. (The recorded WR-6 build-vs-spec drift makes
this sharper, not softer: `warAllianceRisk`'s topology arm is truth-sourced
today while WR-6 specs a pure belief-side read — grafting a raw betrayal
record onto it would WIDEN the K3 gap this document declares binding.)
THE TERM ITSELF: recorded exits_defaulted, switches, and cold exits —
severity-sized against the SP-6b family, **decaying on a D5-BAND-SCALED
generational half-life (R-H, corrected — SP-5b's shared shape supplies the
idiom, INT-5's D5 table supplies the BAND, and both `halfLifeTicks` and
`maxLookbackTicks` scale together, both signs)**. For a fleeting-lifespan
court a century-old treachery is a legend, not a price; **for an UNDYING
court there is no time-based forgetting at all** — erosion becomes
event-driven and the burial becomes the only road (INT-5's forty-year pin
asserts exactly this). A flat human-clock half-life here would re-open the
seam INT-5 exists to close.
**BURIAL SCOPE, RULED (R-K/corpus deference): the burial is PAIR-KEYED and
closes the DECREEING side's reads only** — "a burial the counterpart never
answers leaves their casus lanes untouched (unilateral burial is half a
peace)". A market-wide history term has no single decreeing side, so:
a burial between A and B discounts the entry IN A's OWN reads of B, and in
B's once B answers; it does NOT discount that betrayal in third parties'
join reads or in shared-threat scoring. The world's memory is not one
court's to close — which is the better story anyway, and is why the
dig-up matters.
The same history feeds SHARED-THREAT SCORING — the serial betrayer draws
the counter-coalition (CT-LAW-4's coalitional coin). **THE COMMERCE FENCE,
stated (R-J): the embargo web's defections (TB-4) write into the SAME
history — one traitor's ledger, military and commercial — but that history
reaches COALITION FORMATION (GR-2's shared_threat scoring) and the JOIN
BAR, and it NEVER reaches the war taxonomy directly.** J-TR-2 binds:
"commerce reaches war ONLY through tradeWar's existing intent deposit
reading severance magnitude", and this wave mints no commercial casus and
merges no taxonomy. A purely commercial betrayer becomes hard to ally with
and easy to gang up on; it does not thereby acquire a war reason.
- **Force/counterforce (same evidence):** the record that prices the
  traitor UP for every future partner is the same record that prices its
  ENEMIES' coalition against it — and the same record the burial, the
  D5 clock, and honest discharge work DOWN. One ledger, three signs.
- **Alignment:** lawfulness01 colors how heavily priors weigh for the
  lawful joiner; malice01 colors nothing here — **consequences never
  discount (CT-LAW-4/J-CT-8)**, and that is a declared alignment-empty on
  the malice axis with its reason.
- **Posture (req 10):** the joining court's `riskToleranceOf` colors how
  much recorded treachery it will tolerate for a needed ally — the
  desperate take the knife-hand.
- **Clock (C-LAW-4):** the record is written at the act; every OTHER
  court's read of it arrives at NEWS SPEED (rumorNetwork hop latency,
  distance-priced) — a betrayal three realms away is not priced until the
  word lands. Decay on the D5-scaled half-life. INTERVAL_WEEKS denominated.
- **Lifecycle paths:** the betrayal history persists (the spatialLedgers
  family idiom, per WR-8's precedent) — JSON-round-trip pinned; **regen and
  undo carry the record AND its burial state together** (never re-derived
  from the chronicle — this is precisely the regen/undo class WR-7 calls
  the lifecycle bug this program is most bitten by); import validates act
  tokens against CV-1's closed three and drops invalid rows with a logged
  receipt; expiry is a read against recorded ticks, never a stored
  countdown; a DM-edited entry is never machine-dropped.
- **Bands:** severity sizing = SP-6b family reference; the decay band is
  drawn from the D5 table (no new band family authored). §7 tunes the
  assignment, not the scale.
- **Couplings:** CW-0 rows war×interior (the burial's discount) and
  war×trade (the commercial defection feeding one ledger), full key set,
  same commit. Cascade: a counter-coalition federating against a serial
  betrayer registers with CW-1.
- **Dossier round-trip:** the traitor's record renders on the war/treaty
  panel and on the pair page — including buried entries shown AS buried on
  the decreeing side — cause-walkable from any join refusal back to the act.
- **Pins:** join price monotone in priors (0/1/2 fixture); the term moves
  the JOIN BAR and never `risk01` (source scan — the mis-homing negative);
  burial discounts on the decreeing side AND leaves third-party join reads
  untouched (the hardest negative — unilateral burial is half a peace); the
  dig-up restores; the D5 band scales both the half-life and the lookback,
  and an UNDYING pair shows NO time decay (the INT-5 parity fixture); the
  shared-threat feed reaches formation (a three-betrayal actor draws a
  counter-pact on a fixture) while minting NO war casus (source scan, the
  J-TR-2 negative); regen/undo carry record + burial state; mutant control
  (zero the term, prove the join-price pin reds).

## §4 THE TRIBUTE + STATECRAFT WAVES (TB-1, TB-1b, TB-2..TB-7)

**THE FLAGS, ATTRIBUTED CORRECTLY (chair, 2026-08-03 — the earlier header
listed a FOREIGN flag among this section's own, which would have registered
a duplicate certification row against a flag another volume owns).** This
section mints exactly TWO flags and therefore exactly two certification
rows: **`tributeFamilyEnabled`** (the section flag, TB-1's commit) and
**`embargoCompactsEnabled`** (TB-4's own, its commit). **`faithTermsEnabled`
IS NOT OURS: it is FAITH's, minted at WF-6** (`DESIGN_FP_FAITH.md` — the
faith-side term executors, formation triggers, and the communion trigger),
and TB-1b consumes it as a **LIT-PRECONDITION, never as an own-flag** —
TB-1b registers NO certification row against it, and TB-1b's own switch is
`tributeFamilyEnabled` (it is a wing of this family, not a third flag).
§10 states it that way and §10 is the authority. **Lit-preconditions per
wave are listed in §10**, and a flag lit out of that order is an invalid
config the certification walker reds.

**THE CATALOG INSTRUMENT, NAMED ONCE FOR THE WHOLE SECTION (R-A/R-B).**
This document does not mint catalog rows. GRAMMAR's TERM_CATALOG is
CANONICAL corpus-wide — "one list, one spelling, one closure contract" —
and the WORKED lawful widening instrument is **J-GR-15**: a GRAMMAR
judgment block ADOPTS the requested rows into GR-3 under GRAMMAR's
spellings, the spine and COUPLINGS carry the matching amendments as a
cross-volume edit owned by the cohesion pass, and the requesting volume
becomes a CONSUMER wave — "pointers, never rows". Every new row below is
therefore a **CATALOG-ADDITION REQUEST to GR-3 carrying its war-side
rationale**, exactly as WF-6's two faith rows were. (R3's consumer
enumeration reads FAITH/TRADE/POPULATIONS; **WAR is a fourth consumer** and
the adopting ruling should say so — R3's "canonical for ALL term families,
corpus-wide" plainly covers it, and making the pointer explicit costs one
clause.) Each request must carry, or it does not close: **a family, an
executor kind, a producer, a biting consumer, and `expiresTick` REQUIRED**
(the perpetual-extraction impossibility holds by construction). And
GRAMMAR's §10 obligations ride every adoption commit: **the TERM_CATALOG
freeze, its families walker, and `TREATY_COMPLIANCE_VOICE`'s totality table
(family × state, `display/treatyDocument.js:36-60`) update in the SAME
commit as GR-3 or the walkers red** — and every new news kind registers in
WHAT_PHRASES + heraldRouting + rumor voice + chroniclersLetter rows in the
kind's own commit.

**THE CLOSURE CONTRACT, DISCHARGED — FAMILY + EXECUTOR KIND FOR EVERY ROW
THIS DOCUMENT REQUESTS (chair, 2026-08-03; the contract above was being
STATED and not PAID, which would have sent Sol to GRAMMAR with rows that
cannot be adopted). This table covers §4 AND §4-EXP; producers, biting
consumers and `expiresTick` are carried in each wave's own body.**

| Requested row | Family | Executor kind | Wave |
|---|---|---|---|
| `tribute_goods` (IS the BUILT `tribute` row — no request) | economic | `transfer` (BUILT) | TB-1 / TB-3 |
| `tribute_resource_share` (a BASIS on the built `resource_share` row — no request) | economic | `transfer` (BUILT) | TB-1 / TB-3 |
| `tribute_prosperity_indexed` | economic | `transfer` — an INDEXED RE-SIZE of a conserved stream, not a new executor: the obligation re-derives per period and the built transfer moves the result | TB-1 / TB-3 |
| `tribute_service` | **population** | the **RECEIPTED-ACT kind, REQUESTED** (below) — labour happens, it does not conserve, so no transfer kind fits | TB-1 / TB-3 |
| `tribute_population` | **population** | the **FLOW-MINTING kind, REQUESTED** (below) — its product is a banded anonymous column on POP's existing ledger, under POP's own writer; it moves no stock | TB-1 / TB-3 |
| `cult_establishment` | **faith** | `grant` — J-GR-5's standing-RIGHTS kind, which is exactly what an establishment is. Its EFFECT machinery is the BUILT cult/foothold substrate (`IMPOSE_CULT`/`SET_PRIMARY_DEITY`), reached **CONSUMER-SIDE**; the executor grants the right and never pokes that substrate directly | TB-1b |
| `patron_imposition` | **faith** | `grant`, same shape, same consumer-side reach into the built flip substrate | TB-1b |
| `embargo_compact` | security | `forbearance`, **REQUESTED** (below) | TB-4 |
| `route_restriction` | sovereignty | `forbearance`, **REQUESTED** (below) | TB-4 |
| `subsidy` | **economic — CORRECTED**: XW-4's header read "security family row", but a subsidy is a conserved stream pointed at a friend and it rides TB-3's stream shapes whole | `transfer` — a CONDITIONED stream (the condition is the subsidized war's life; expiry rides the EDGE's termination, never a timer) | XW-4 |
| `loan` | economic | `transfer` — TWO-PHASE on one row (disbursement stream, then repayment stream), the phase derived from recorded ticks and balances, never a stored countdown | XW-5 |
| XW-6's arbitration pre-commitment row (spelling GRAMMAR's to set at adoption) | security | `forbearance` — the binding CONTENT is negative (not to repudiate an accepted finding); the acceptance itself is GR-6's occasion, not an executor's product | XW-6 |
| `treaty_guarantee` | security | **the kind the BUILT `mutual_defense` row already carries** (a pledged response, discharge read at the event) — a POINTER to a built kind, not a fourth request; Sol names it off the catalog at slice start and the brief records the name it found | XW-8 |
| `neutralization_compact` | security | `forbearance` — a joint pledge of RESTRAINT toward a named buffer is a negative obligation, the same requested kind | XW-8 |

**THE EXECUTOR-KIND REQUESTS ARE THREE, AND THEY TRAVEL AS ONE INSTRUMENT.**
J-GR-15's adoption block is a single instrument, so the three kinds this
document needs are requested TOGETHER inside it, never as three trickling
asks: (1) **`forbearance`** — negative obligations and prohibitions, which
no built kind covers (the 7th kind `grant` is scoped by J-GR-5 to standing
RIGHTS); (2) **the RECEIPTED-ACT kind** — rendered service, countable
receipts, no conserved stock moved; (3) **the FLOW-MINTING kind** — an
executor whose product is a banded anonymous migration column on POP's
existing ledger under POP's own writer. Spellings are GRAMMAR's to set.
**DECLARED FALLBACKS, so a decline is never silence:** if `forbearance` is
declined, TB-4's two rows do not build and TB-4 reduces to its composition
half (as TB-4 already states), and XW-6's and XW-8's forbearance rows fall
with it; if the receipted-act kind is declined, `tribute_service` does not
build and the family ships FOUR bases with the basis-cruelty ladder
re-declared over four; if the flow-minting kind is declined,
`tribute_population` does not build and CPL-17's declared-empty stands
undisturbed in BOTH halves.

**THE STACKING RULING (R-B, chair, 2026-08-03) — without it, half of §4 is
unrepresentable.** GRAMMAR §13's law is that within one **family ×
beneficiary** cell a SECOND term is REFUSED WITH A RECEIPT
(`term_refused_stacking`) "unless the pair is on the frozen COMPOSABLE
list — seed member: {non_aggression, mutual_defense} (walker-enforced, same
freeze discipline as TERM_CATALOG)". TB-2's whole demand doctrine assumes a
victor can draft a MIX of bases onto one instrument, and under the law as
it stands the second basis is refused. Ruled: **THE TRIBUTE BASES JOIN THE
FROZEN COMPOSABLE LIST**, as one declared composable set, adopted by
GRAMMAR in the same block that adopts the rows — **and the set is declared
ACROSS families, deliberately: per §4's assignment table the five bases do
NOT all share a family (three are economic, `tribute_service` and
`tribute_population` are POPULATION family, beside `labor_compact` and
`migration_right` where they belong). Cross-family bases never collided
under §13 in the first place — the refusal is a within-family-cell rule —
so the COMPOSABLE entry is what makes a MULTI-ECONOMIC-BASIS sheet legal,
and the population bases compose by family separation. Stating both roads
matters: the walker must not be written as if one mechanism carried all
five, and the stacking pin's positive arm must exercise a two-ECONOMIC-basis
sheet specifically, or it proves nothing.** The adoption block carries the
set — **and the COMPOSABLE
walker is updated in that commit; say so in the brief, because a frozen
list that gains members silently is exactly what the freeze discipline
exists to prevent.** Separately, and because it is the same decision:
**third-party terms key their stacking cell by family × beneficiary ×
TARGET** (see R-D below), which is what lets one pair hold several embargo
compacts against several targets without colliding. One schema decision
closes both the stacking question and the third-party question; they are
solved as ONE change, not two.

### TB-1 — THE FAMILY (catalog work; GRAMMAR adopts per R3/J-GR-15)

**THE FAMILY SUBSUMES THE BUILT ECONOMIC FAMILY — it does not duplicate it
(R-J, corrected; this is the difference between three new rows and five).**
`peaceTerms.js` already carries the economic family as EXACTLY FOUR
conserved sink-only grain-month streams `{tribute, reparations,
restitution, resource_share}` (`:185-194`), all `executor:'transfer'`, all
under the treaty reserve floor, with `CLASS_TERM` mapping
`export_flows → resource_share` and `treasury → tribute` (`:226-227`) and a
`:376` fallback. Therefore:
- **`tribute_goods` IS THE BUILT `tribute` ROW** — banded stream, cheap,
  taken AS IS. Its executor is NAMED and already exists: the built transfer
  executor, with J-TR-8's ONE delta applicator
  (`generosityUpdates.applyFoodDeltasToUpdates`) doing the conversion —
  no sixth writer of storageMonths lands with this program (T3).
- **`tribute_resource_share` EXTENDS THE BUILT `resource_share` ROW** —
  the share basis is a new BASIS on an existing row, not a new row beside
  it. Extending, not re-minting, is what keeps the catalog closed and keeps
  `CLASS_TERM`'s existing mapping honest. **AND THE SPELLING IS NOT A ROW
  — the same cure `tribute_goods` got, applied here (chair, 2026-08-03).**
  `tribute_resource_share` is THIS DOCUMENT'S PROSE NAME FOR A BASIS, not a
  catalog token: the adoption block requests no row and no spelling under
  it, the basis rides `resource_share`'s existing token, and a SOURCE SCAN
  asserts that no second row and no second spelling is minted — the
  duplication negative, twin of the goods one. If a distinct token is ever
  wanted it is a fresh catalog request under R3, never a silent second
  spelling (R3: one list, one spelling, one closure contract).
- **GENUINELY NEW ROWS ARE ONLY THREE:** `tribute_prosperity_indexed`
  (obligation re-sized per period from the loser's prosperity band; PAID in
  goods — the index basis; **economic family, `transfer` executor — an
  indexed re-size of a conserved stream**) · `tribute_service` (receipted
  acts; the `labor_compact(pair)` banded flow read — `laborCompactFor(pair)`
  — extended; **population family, the REQUESTED receipted-act executor
  kind**) · `tribute_population` (banded anonymous columns over the term's
  life; see TB-3's fences and the CPL-17 owner override; **population
  family, the REQUESTED flow-minting executor kind**). The families and
  executor kinds for all three, and for every other row this document
  requests, are carried in **§4's assignment table**, which is where the
  closure contract is actually paid.
All of them are directional terms with a beneficiary on the ONE instrument
(R1), composable under the COMPOSABLE ruling above, executors in LAZY LEAF
SIBLINGS of peaceTerms (R2). (Note on the R2 rationale: the lazy-leaf
discipline stands on its own merits; the "794/800 lines" ceiling figure the
corpus quotes for peaceTerms is STALE — the file is materially larger — so
Sol re-derives the ceiling at slice start rather than reading a budget off
a stale number.)
**DURATION HAS TWO SOURCES, ONE PER DOOR (chair, 2026-08-03 — the earlier
text named exactly one and then applied the family to both doors, which
GRAMMAR forbids; a drafter at the peacetime door had no lawful source at
all).**
**WAR DOOR — the LANDED margin derivation `0.5 + margin + margin², capped`
— UNMODIFIED.** It is landed, it is not this program's to touch, and the
"unmodified" is an explicit don't-touch, not a stylistic note. (Its
citation is its own; **WR-0c item (4) is the TREATY-CLOCK PROVENANCE rule**
— 52 current / legacy 12, never backfilled — and GRAMMAR binds that
citation to the clock in all four of its references. Do not follow the
WR-0c(4) pointer expecting the duration formula.) **This derivation applies
at the WAR DOOR ONLY**, because a margin is what a war produced.
**PEACETIME DOOR — THE DEMAND'S MEASURE, and never a margin.** GRAMMAR §1c
is binding and is quoted so nobody re-derives it: **NO MARGIN IN PEACE** —
"a peacetime pact has no victor and no margin. The budget analogue is THE
DEMAND'S MEASURE — what the trigger read says each side needs." So the weak
settlement's offer takes its duration, and its whole term budget, from THE
TRIGGER'S OWN READ — the `trade_demand` believed-scarcity crossing or the
`shared_threat` crossing that produced the sheet — exactly as every other
peacetime term does, on the same surface, through GRAMMAR's own peacetime
budget path. **A SYNTHETIC MARGIN IS THE RULED-AGAINST OPTION, not a
shortcut:** J-GR-2 names it as the VETO branch by name ("VETO reuses
`termBudgetFor` with a synthetic margin"), so manufacturing a margin to
reach the war-door formula in peace is the thing GRAMMAR already decided
against. **Pin, both doors, one fixture:** a war-end tribute term's
duration traces to the margin derivation; a peacetime tribute offer's
duration traces to the trigger's measure; and NO synthetic margin is
computed anywhere on the peacetime path (source scan — the negative).
**BOTH DOORS.** War-end: the war-end term sheet — TB-2's drafting layer
extending WR-8's bargaining range (see TB-2), with **WR-10's sovereignty-
market bundle GROWING the family later as composable components, a
COMPOSITION rather than a precondition** (WR-10 is the sovereignty market
and closes the war program; sequencing TB-1 — the foundational catalog wave
for all of §4 — behind the last war wave was an ordering error, recorded
and corrected in §10). Peacetime: the weak settlement OFFERS a share for
protection/access, priced two-sided under dependency fear — and per
TB-LAW-10 that offer is **SHEET COMPOSITION UNDER EXISTING TRIGGERS**
(`trade_demand`'s believed-scarcity crossing, or `shared_threat`), never a
new trigger token; **its duration and budget come from that trigger's own
DEMAND'S MEASURE, per the two-door ruling above, never from a margin.**
- **Force/counterforce (same evidence):** the tribute's revenue ↔ the
  tribute's strain — TB-7's arithmetic reads the SAME term record that
  pays, and TB-2's dependency inversion prices the victor's own growth
  around the inputs.
- **Alignment:** DECLARED ALIGNMENT-EMPTY, with reason — TB-1 is catalog
  and schema work; it opens no decision surface. The appetite for what the
  rows CARRY is coloured at TB-2 (doctrine), TB-3 (`all`-band extremity)
  and TB-6 (extractive drafting), which are §6's rows.
- **Posture (req 10):** DECLARED NONE at TB-1, same reason — the drafting
  surfaces that consume posture are TB-2 and TB-5.
- **Clock:** installments on the treaty's OWN clock (52-week year,
  `treatyTicksPerYear`, identity-pinned to `INTERVAL_WEEKS.one_year`;
  legacy 12 by explicit marker, never backfilled). Durations denominated
  there.
- **Lifecycle paths:** term rows persist on the pair's ONE instrument via
  the existing TermRecord lifecycle — JSON-round-trip pinned (including the
  new `target?` field, R-D); regen and undo carry terms with the
  instrument; import validates term tokens against the canonical catalog
  and drops invalid rows with a logged receipt; **a DM-edited term is never
  machine-dropped**; `expiresTick` is a read against recorded ticks, never
  a stored countdown.
- **Bands:** the share ladder `{tithe, quarter, half, lions_share, all}` —
  a volume-local closed vocabulary, **declared volume-local in its §7 row**
  per SP-6's standing rule (2). Severity/significance anywhere near it are
  SP-6b family references.
- **Couplings:** CW-0 rows war×grammar (the catalog request + the stacking
  amendment) and war×trade (the economic family's subsumption), full key
  set, same commit. **Dossier round-trip:** the terms render on the treaty
  document surface, and `TREATY_COMPLIANCE_VOICE` gains the family × state
  rows in the adoption commit (walker-guarded) — open the treaty, read what
  is owed and how it is going.
- **Pins:** every new row carries family + executor kind + producer +
  consumer + `expiresTick` (the closure-contract pin — **and §4's
  assignment table is where those four are actually carried; a walker
  asserts every requested row in this document appears in that table, so
  the contract cannot be stated-but-unpaid again**); `tribute_goods`
  resolves to the BUILT `tribute` row and mints no second row (source
  scan — the duplication negative); **the share basis mints no second row
  AND no second spelling — `tribute_resource_share` appears nowhere in the
  catalog, the executors, or `CLASS_TERM` (source scan, the twin
  duplication negative)** — while the basis itself extends
  `resource_share` and `CLASS_TERM`'s mapping still holds; **duration
  traces to the margin derivation at the war door and to the trigger's
  demand measure at the peacetime door, with NO synthetic margin on the
  peacetime path (one fixture, both doors, plus the source-scan
  negative)**; a multi-basis
  tribute sheet COMPOSES rather than refusing (the stacking pin, positive —
  **and its fixture uses TWO ECONOMIC bases, since a mixed economic +
  population sheet composes by family separation and would prove nothing
  about the COMPOSABLE entry**)
  while a non-composable second same-family term still REFUSES with
  `term_refused_stacking` (the same pin, negative — the freeze did not
  become a free-for-all); the peacetime and war-end doors AGREE on stacking
  (one fixture, both doors); JSON-round-trip over all three new rows.

### TB-1b — THE FAITH WING (owner addendum 2026-08-03: treaties may carry
### religious consideration; **own flag: `tributeFamilyEnabled`, this
### section's — TB-1b mints NO flag and NO certification row of its own.
### `faithTermsEnabled` is FAITH's, minted at WF-6, and is consumed here as
### a LIT-PRECONDITION only**; WF-6 is the OWNING-VOLUME DOOR and this wave
### rules only the war/tribute-side drafting)

**THE PROHIBITION IS RE-AFFIRMED FIRST, because everything else depends on
it: NO TERM EVER DELIVERS BELIEVER SHARE OR CONVICTION.** The
conversion-mandate prohibition (J-GR-12 / J-CPL-6) stands untouched — a
creed is never the deliverable, a table never moves a soul, and belief
moves only through the built spread/contest machinery on its own evidence.
What a treaty CAN deliver is INSTITUTION and PRESENCE. Two catalog rows,
requested of GR-3 per R3. **BOTH ARE FAITH-FAMILY ROWS WITH THE `grant`
EXECUTOR KIND (chair, 2026-08-03; carried in §4's assignment table):**
`grant` is J-GR-5's standing-RIGHTS kind and an establishment IS a standing
right of presence, so no new executor kind is requested for the faith wing.
Their EFFECT machinery is the BUILT cult/foothold substrate —
`IMPOSE_CULT`/`SET_PRIMARY_DEITY` at `mutateEntities.js:832/:877` and the
foothold machinery — reached **CONSUMER-SIDE**: the granted right is what
the consumer reads, and the executor never pokes that substrate directly:
- **`cult_establishment`** — an embedded cult/temple in the OBLIGOR
  settlement at a BANDED standing `{minor, major}`. "Depending on the deal"
  is exactly the standing band × institution tier DRAFTED: a minor
  establishment is a permitted shrine and a resident priest; a major one is
  a temple with an institution tier and a seat at the table. Deliverable =
  INSTITUTION + PRESENCE. Believer share then moves ONLY through the built
  spread/contest machinery, on its own carriers and its own clock — the
  term buys a door, never a congregation.
- **`patron_imposition`** — the treaty-door reach of the occupation flip.
  The built precedent is exact: occupation imposes patrons, and WF-1
  classifies that fall `imposed` ("the creed followed the garrison").
  This row lets the same outcome be DEMANDED at the table instead of taken
  by garrison. **Gated hard: demandable at the WAR DOOR only under high
  margin — extremity-adjacent, gated BESIDE the lion's share** (not at
  `all`'s razing gate, but in its neighbourhood: a god is not a tithe).
  At PEACETIME it is offerable only as the WEAK PARTY'S OWN SUBMISSION,
  drafted from its side and priced two-sided — never demandable in peace.
**THE COUNTERFORCES ARE ALL BUILT — that is why this wave is affordable.**
The underground congregation (WF-5b: on an imposed flip the old faith goes
to the cellars, `pin:kept-alive-in-cellars`); the sacred-tension and schism
reads; tolerance backlash (WF-6's own force/counterforce — "the term's
demand ↔ the host's backlash"); and the endings vocabulary already carries
both outcomes — `imposed_held` versus `resurgent`. Nothing here needs a new
opposing mechanism; it needs the existing ones POINTED at the term.
**STRAIN AT FAITH GRADE.** An imposed god reads through TB-LAW-5's justice
lens at MAXIMUM severity to the faithful — believedInjustice at faith grade
is the sharpest reading in the document — and it feeds the counter-crusade
directly through the SHARED-FAITH web: co-religionists elsewhere read the
imposition as their own injury, and `shared_threat` scores it. The victor
who takes a god takes a war with it, later.
- **Force/counterforce (same evidence):** the establishment's reach (a
  foothold, a tier, a voice at the table) ↔ the backlash, the underground,
  and the shared-faith web's federation — every arm scoring off the same
  imposition record.
- **Alignment:** lawfulness01 colours HONOURING the establishment term (the
  lawful keep the shrine open even when the crowd wants it burned); the
  FAITH-SIDE colouring is the QUADRANT TABLES' own lane (sacred_claim ↔
  common_rite, the closed four-quadrant table) and is NOT re-authored here
  — one read, one owner.
- **Posture (req 10):** the demanding court's `postureOf` colours whether
  it reaches for the patron at all; the host's posture prices its
  tolerance-vs-backlash response.
- **Clock:** the term on the treaty clock; the CONGREGATION moves on the
  spread machinery's own clock (slow, carrier-bound); the counter-crusade's
  federation propagates at news speed (C-LAW-4), INTERVAL_WEEKS denominated.
- **Lifecycle paths:** the term persists on the instrument (TermRecord
  round-trip); the INSTITUTION it plants persists through the existing
  religion-state lifecycle and is NOT re-derived from the term (regen/undo
  carry both, independently — a repudiated term does not un-build a temple;
  it removes the protection, which is the interesting case).
- **Bands:** standing `{minor, major}` — a volume-local closed pair,
  declared volume-local in §7; severity at faith grade is an SP-6b family
  reference.
- **Couplings:** CW-0 rows war×faith (the imposition and its strain) and
  war×interior (the host's backlash into the commons), full key set, same
  commit. Cascade: imposition → underground → resurgence registers with
  CW-1.
- **Dossier round-trip:** the imposed cult renders through the EXISTING
  FaithSection / pantheon surfaces — open the town, see whose god sits in
  the new temple and by whose signature.
- **Pins:** no term ever moves believer share directly (source scan, the
  hardest negative — J-GR-12/J-CPL-6 held); `patron_imposition`
  UNREACHABLE in peacetime as a demand (reachable only as the weak party's
  own offer) and UNREACHABLE at the war door below the high-margin gate;
  an imposed flip mints the underground congregation on a fixture;
  `imposed_held` and `resurgent` both reachable; the faith-grade strain
  feeds a shared-faith counter-coalition on a fixture; the establishment
  survives repudiation of the term (the lifecycle negative).

### TB-2 — THE DEMAND DOCTRINE (why this ask)

The victor's goal mix — FOUR goals: **{punitive, containment, acquisitive,
reconstructive}** — is DERIVED,
capped, receipted — never a menu. **EVERY INPUT BELOW IS BELIEF-SOURCED
(K3, structurally enforced).** K3 enumerates the negotiation family by name
— "no negotiation path (**terms**, vote, interceptor judgment, close-vote
comparison, feasibility) may read true world state" — and WR-8 routes even
the actor's OWN home-front through belief. The doctrine module therefore
imports no true-state read; its import list is pinned and its source
scanned for true-state tokens, exactly as the rest of the negotiation
family is. The goals: punitive ← believed grievance × severity × extremity
(skews totality, population; counterforce: CPL-5's own archetype — the
dictated peace pre-pays the next war) · containment ← the threat read —
**THE LOSER'S BELIEVED TRAJECTORY**, which is what makes containment a K3
read rather than a truth read (skews strategic shares, growth-skimming
index, labor, route_restriction; counterforce: the cornered rat, a fenced
realm with nothing to lose, plus the monitoring bill) · acquisitive ← the
victor's own believed deficit reads (seatBooks settlement-position +
supply gaps; counterforce: DEPENDENCY INVERTS — the victor grows around
tribute inputs and the term's end is the winner's shock, receipted at
lapse). **THE seatBooks DEGRADED ARM, DECLARED (R-K; SP-4's precedent,
verbatim in shape):** the ruler's books land LAST (INTERIOR INT-1,
`seatBooksEnabled`) while consumers arrive earlier. Until seatBooks lands,
the acquisitive read composes from SUPPLY GAPS ONLY — **the books term is
ABSENT, NOT ZERO, and the doctrine receipt NAMES WHICH INPUTS IT HAD.**
`seatBooksEnabled` is declared a lit-precondition of the books arm alone
(COUPLINGS register item 8's reservation form: behind the flag,
colour-only, receipt naming seatBooks, absent-not-zero until INT-1 lands);
no registry row exists for it until this consuming read lands. ·
**reconstructive** ← the healing calculus (the Marshall stance: draft
BELOW capacity + olive branches + remission-forward, betting on TB-LAW-7
and the prosperity index to convert enemy into market into ally;
weighted up by low malice, long lawfulness, and a containment read that
says the fence WON'T hold — mend what you cannot cage; counterforce:
Weimar's shadow — the revanchist takes the aid and rearms, priced by the
mistrusted-gift arm and the saturation band; the Herald's line: "The
terms were drawn as a bridge."). CV-3's
weights fold in (the lawful discount the punitive weight they cannot
stomach; the malicious relish it — alignment coloring the DOCTRINE, the
map's richest row). THE HERALD NAMES THE DOCTRINE — **three authored
receipt families under the content-depth floor** (≥4 variants each): "The
terms were drawn to bleed them." / "…as a fence." / "…as a larder." —
each entailed by the drafting record (headline honesty at treaty scale);
the reconstructive goal adds a FOURTH family, "…as a bridge."
**COMPOSITION WITH WR-8, STATED (folded): the goal mix sets COMPOSITION
INSIDE WR-8's bargaining range, never beside it.** WR-8's terms consumer
already defines that range — "what I could take by force vs what I must
give to survive — both ends beliefs, ranges may not overlap, and
non-overlap IS the grind receipted". The doctrine decides WHAT the sheet is
made of within the range WR-8's feasibility read has already drawn; it
never widens the range, and **it inherits the non-overlap grind receipt
unchanged** (a doctrine cannot draft a sheet the range does not admit; when
it cannot, the grind is the receipt). Two inputs, one draftTerms surface,
one stated order.
Extends the war-end drafting lens: J-GR-14's disclosed-shift discipline
applies (a lit-path drafting change on a landed feature — disclosed,
flag-fenced, receipted, golden plan reported before the commit).
- **Force/counterforce (same evidence):** each goal ships its own, named
  above and scored off the SAME drafting record — the punitive draft's next
  war, the containment draft's cornered rat and monitoring bill, the
  acquisitive draft's dependency inversion, the reconstructive draft's
  Weimar shadow.
- **Alignment:** §6's richest row — lawfulness01 colours punitive-weight
  TOLERANCE, malice01 colours punitive-weight APPETITE, and both colour the
  reconstructive weight (long lawfulness and low malice raise it). Capped,
  read-side, colors-never-selects.
- **Posture (req 10):** the drafting court's `postureOf`/
  `riskToleranceOf` colours the mix — a weary court drafts to be done with
  it, a defiant one drafts to be feared; out-of-posture drafts are priced
  and receipted, never forbidden.
- **Clock:** the doctrine is derived AT DRAFTING (one read, one receipt, no
  standing cadence); the world's reading of the doctrine travels at news
  speed (C-LAW-4). INTERVAL_WEEKS denominated.
- **Lifecycle paths:** the derived mix is NEVER STORED (never-store-a-
  derivable); what persists is the DOCTRINE RECEIPT naming the weights and
  their inputs, on the existing chronicle/receipt lifecycle —
  JSON-round-trip pinned, regen and undo carry the receipt, import
  validates its kind. A DM-edited receipt is never machine-dropped.
- **Bands:** goal-weight caps + derivation curves, §7; receipt significance
  is an SP-6a class assignment against the SP-6b family.
- **Couplings:** CW-0 rows war×interior (seatBooks, when lit) and
  war×belief (the believed-trajectory read), full key set, same commit.
- **Dossier round-trip:** the doctrine renders on the treaty document — the
  DM opens the peace and reads WHY it was drawn this way, walkable back to
  the grievance, the threat read, or the deficit that produced it.
- **Pins:** the mix is derived and never a menu (no DM picker, source
  scan); no true-state import in the doctrine module (the K3 import pin +
  token scan — the hardest negative); the books arm reads ABSENT not zero
  when `seatBooksEnabled` is dark, and the receipt says so; the doctrine
  never widens WR-8's range and the non-overlap grind still receipts;
  three (four with reconstructive) receipt families each carry ≥4 variants;
  the disclosed golden shift is reported before the commit.

### TB-3 — THE TRIBUTE EXECUTORS (the new work)

**THE GOODS basis** (`tribute_goods` = the BUILT `tribute` row): no new
executor — the built transfer executor moves it, months convert at J-TR-8's
ONE delta applicator (`generosityUpdates.applyFoodDeltasToUpdates`), M6a's
EXACT five-term INTEGER conservation holds with the kernel refusing to
persist an imbalance, and no sixth writer of storageMonths lands (T3).
Cheap, as-is, named here so the row is not orphaned between waves.
**The SHARE basis**: banded share × MONITORED YIELD, cut at the source
chokepoint, conserved into the existing goods physics — **INTEGER, ONE
WRITER FAMILY**, the extracted share visibly OUT of the loser's available
supply. **T7 SCOPE, CORRECTED (R-J):** T7's exactly-one-arm law and its
walker are FOOD-ONLY and two-sited; citing "T7's idiom per resource class"
buys coverage that does not exist. Food rides T7 whole. **A NON-FOOD
resource class lands its OWN conservation arm with its OWN pins** in this
slice, declared as such (VERIFY-AT-BUILD alongside the yield read; fallback
named at §2). Compliance measures monitored yield only (TB-LAW-6) and the
fog decides DETECTION, never quantity; concealment rides believed-vs-true
(the under-report is a BELIEF write the exposure machinery can catch — a
caught cheat is a defaulted term with the standard shell).
**The SERVICE levy**: the receipted-act executor — rendered services mint
countable receipts (labor doesn't conserve; it HAPPENS); the half-rendered
grudging levy is compliance-under-fog in its most human form. Extends
`laborCompactFor(pair)`'s banded flow read.
**The PROSPERITY index**: each period the obligation re-derives from the
loser's CURRENT prosperity band (a read, never a transfer — TB-LAW-2); the
incentive cure lands with it — the take rises with the tributary's health,
so extraction-to-death is self-defeating by arithmetic. Pin: a collapsing
tributary's indexed obligation FALLS, same seed, no clamp anywhere (and the
built reserve floor is below it, TB-LAW-4).
**THE POPULATION LEVY — the owner-ordered basis, fenced (R-F).** The owner
ordered population as a tribute basis in the design dialogue of 2026-08-02;
COUPLINGS CPL-17's declared-empty ("people are never the PAYMENT") is
RE-RULED by owner authority for this direction ONLY, and the override is
recorded on BOTH sides (CPL-17 carries the amendment line; §2 carries the
row). **CPL-17's hostage-surety half is UNTOUCHED and still stands.**
**AND CPL-12'S GENERAL CLAUSE IS RECONCILED HERE TOO (chair, 2026-08-03 —
it had never been addressed anywhere in this document, and under C-LAW-8
("DECLARED-EMPTY IS BINDING… building one is a STOP-and-report") an
unaddressed declared-empty stops the implementer cold).** CPL-12's
declared-empty is FAITH-SCOPED, but it states its law generally:
"population moves on push/pull/flight physics only". A treaty-compelled
column read literally is a FOURTH cause, and Sol reading that clause
literally would stop. **Ruled: the levy does not widen the causes of
movement — it supplies COMPULSION as the EVIDENCE CONTEXT of an EXISTING
cause.** POP-3's closed causeClass set stays four, the column is stamped
`expulsion` from evidence the flow already carries, and `rememberedAs`
takes that class's existing word `cast_out`. CPL-12's clause is READ AS
SCOPED to its own faith lane; and even read generally, the levy sits
INSIDE `expulsion`'s physics rather than beside them. **The reconciliation
is recorded on BOTH sides exactly as the CPL-17 override was — CPL-12
carries the scoping line, this wave carries the pointer — and it is a
COUPLINGS-owned cross-volume edit the cohesion pass lands, never the
implementer's to assume.** Declared fallback: if COUPLINGS declines the
scoping, `tribute_population` does not build and the family ships FOUR
bases (the same fallback the flow-minting executor request carries). The
fences, all of them, stated:
- **ANONYMOUS POPULATION ONLY.** POP's fence is carried verbatim:
  "MIGRATION IS A FLOW, NEVER AN ERRAND (spine SP-1's fence, verbatim)…
  Named persons who travel (a returning elder, a commons emissary) are SP-1
  consumers under the GRAMMAR/INTERIOR programs", and "THE NAMED-CAST
  MERCY (already wired, now law): engine deaths draw against the anonymous
  pool only". A levy column draws from the anonymous pool and NEVER names
  a person; INTERIOR's NEVER-KILL, NEVER-RESOLVE-FATES law is absolute and
  untouched.
- **THE COLUMN LANE IS DECLARED: P2, zero mortality — M4 IS NOT TOUCHED.**
  J-POP-10 is quoted in full so the scope is not over-read: "road drama
  never kills on P2: P2 column events are time/provision/direction costs;
  M4's existing sinks gain narration only". M4 keeps its two mortality
  sinks (originDeaths, roadDeaths) and this levy never rides it. That —
  not a blanket "columns never kill" — is why §9's "no forced-march
  mortality" holds.
- **causeClass is `expulsion`**, stamped with THE TREATY'S COMPULSION as
  its evidence-rule context. POP-3's rule is that the class comes from
  evidence the flow already carries (travelClass + refusal/violence
  context); a compliance column carries a COMPULSION context instead —
  which is the same kind of evidence, supplied by the instrument. **POP-3's
  closed sets are UNTOUCHED**: `{flight, opportunity, expulsion,
  dispersal}` stays four, and `rememberedAs` takes the existing word for
  that class — **`cast_out`** — with no tribute word invented (an
  unenumerated addition would be an implementer invention under POP's
  finite-semantics law). The taken generation feeds later revanchism.
- **ONE LEDGER, ONE WRITER.** The ORIGIN writes departure memory; the
  destination end is a PROJECTION, not a second write (POP-3's law;
  `rememberedAs` carries the origin's word, the destination's grammar is
  projection-side). "Writes both ends" was wrong and is corrected here.
- **THE 300-TICK CONSERVATION IDENTITY IS EXTENDED BY THE LEVY ARM — a
  stated obligation of this slice, not an assumption.** `departures ===
  arrivals + returned + lost + inTransit` is re-derived over the levy arm
  and RE-EXECUTED at 300 ticks with its leak-detecting negative control,
  exactly as POP-1 re-executes it for the clearing and return arms. Every
  POP wave extends the estate's best instrument; none weakens it.
- **THE IN-ROAD IS LICENSED, NOT DICTATED.** The arrival clearing gates
  landing on the victor's TRUE capacity, **and POP-5b's permit table is
  consumed on the destination side with `migration_right` as its FOURTH
  permit input** — a licensed pair's in-road stands open one banded step
  deeper than the table alone allows, receipted, the depth step banded; and
  J-POP-11's reconciliation holds, **the table WINS**. A levy column that
  bypassed both would be a second, ungated in-road beside the licensed one;
  it does not exist.
- **THE WALL REFUSAL MINTS THE REAL RETURN COLUMN (J-POP-15).** The
  over-greedy victor's own wall refuses its tribute —
  `refused_at_the_wall` REACHABLE on a fixture, the humiliation pin — and
  the turn-back "mints a REAL return column on the existing ledger
  (inTransit homeward, `returned` stamped at the home landing)"; it walks
  home, conserved, and lands through the same clearing. A same-tick shift
  has no road home. **Compliance ruling: a beneficiary-side refusal is
  NEITHER concealment nor cheating** — the obligor rendered — so the
  installment reads DISCHARGED at the wall, the return column is the
  victor's own problem, and the humiliation is the story.
**The EXTREMITY GATE**: the `all` band in ANY basis demands R's double gate
whole (TB-LAW-3, both arms including R2's evil-exclusive initiation
boundary); negative pin hardest.
**REMISSION**: the victor amends the tribute DOWN mid-term through R1's
amendment machinery — an **`amended` LINEAGE ACT** (the closed act set,
already counted; not a trigger, not an ending) running the standard
accepted-proposal path. Mercy as receipted policy: strain relief and a
credibility credit at the price of revenue; the taper is tellable. It is
also the PRECONDITION of the charity reducer (TB-LAW-7): remission first,
then charity mends.
**THE DOUBLE-EXTRACTION RULING (folded).** The tree already carries built,
certified, Herald-routed tribute extraction on patron/client/vassal edges
(`patron_extracts_tribute`, `vassal_tribute_extraction`). TB-LAW-1 keeps
occupation and vassalage as EDGES and tribute as a TERM — so a pair can
carry both. Ruled: **the term and the edge are one drain, not two.** Where
a tribute term is live on a pair that also carries a vassal/patron
extraction edge, the term's executor is the paying arm and the edge's
extraction reads as ATTRIBUTION over the same flow (the receipt names both
faces); it never bills twice. This is J-INT-3's double-count discipline
applied at the war door.
**THE SALIENCE FENCE (TB-LAW-7's, executed here):** tribute-borne flows are
EXCLUDED from `tradeSalience`'s value-of-tie read, so a one-sided levy
cannot quietly dampen hostility through the built trade-as-peace lane.
- **Force/counterforce (same evidence):** the extraction's yield ↔
  concealment, the wall, the collapse of the source, and TB-7's strain —
  all scored off the same compliance record.
- **Alignment:** malice01 colours the `all`-band ask appetite (still
  extremity-gated, and still inside R2's evil-exclusive boundary at
  initiation). **WRITE-SIDE ENGAGEMENT, DECLARED (req 13):** the population
  levy MOVES a town's soul at both ends — it therefore moves the derived
  axes through the CONSTITUENTS, and it does so THROUGH THEIR EXISTING
  WRITERS ONLY (the migration ledger and the roster/institution writers POP
  owns). No settlement-alignment writer is minted here or anywhere.
- **Posture (req 10):** REMISSION is a decision surface and consumes
  `postureOf` — a defiant victor remits late, a weary or reconstructive one
  early; the receipt names the posture it had.
- **Clock:** installments on the treaty's own 52-week clock; columns at
  column speed (P2 travel time); the wall refusal and its return column on
  the ledger's own cadence. INTERVAL_WEEKS denominated.
- **Lifecycle paths:** the levy's columns and departure-memory rows persist
  on the EXISTING migration ledger — JSON-round-trip pinned; regen and undo
  carry columns, `rememberedAs`, and the return arm together; import
  validates causeClass and `rememberedAs` against POP's closed sets and
  drops invalid rows with a logged receipt; the conservation identity is
  re-executed after a round-trip (a lifecycle path that leaks is a leak).
- **Bands:** share ladder (volume-local, §7-declared); basis cruelty
  multipliers (volume-local, §7-declared); the permit depth step is POP's
  band, referenced not authored; severity/significance are SP-6b family
  references.
- **Couplings:** CW-0 rows war×populations (the levy arm, the clearing, the
  return column), war×trade (the conserved share and the salience fence),
  war×interior (remission's strain relief), full key set, same commit.
- **Dossier round-trip:** the levy renders on the population panel (columns
  out, columns refused, columns home) AND on the treaty document (what is
  owed, what was rendered) — one cause walk from the wall back to the
  signature.
- **Pins (negative hardest):** no named person ever appears in a levy
  column (source scan + fixture — the hardest negative); the levy never
  touches M4 and mints ZERO mortality (source scan); the 300-tick identity
  holds with the levy arm lit AND after a regen round-trip, with its
  leak-detecting negative control; `refused_at_the_wall` REACHABLE and its
  return column lands `returned` at home; the obligation reads discharged
  at a beneficiary-side wall refusal; `migration_right` deepens the in-road
  by exactly one banded step and the table still WINS; a collapsing
  tributary's indexed obligation falls, same seed, no clamp; the share
  basis' non-food arm carries its own conservation pin (T7 not cited for
  coverage it lacks); tribute flows contribute NO salience (the fence
  negative); a vassal edge plus a tribute term bills ONCE (the
  double-extraction negative).

### TB-4 — THE RESTRAINT TERMS + THE EMBARGO WEB (economic statecraft)

Covered BY COMPOSITION, with pointers: growth limitation (TB-2 containment
+ `trade_exclusivity`'s primacy lock — **which lands at TR-5, not before**)
· supply REDIRECTION (provision toward allies + exclusion toward enemies —
two pairwise instruments; a "redirect" verb would violate law I and is not
built) · the RESENTMENT-LED REFUSAL (the grievance/strain read feeding the
refusal, named and receipted: "No grain of ours crosses their gates";
TB-LAW-8 prices it — the refusal mints the refused party's casus).
**TWO CORRECTIONS TO THE COMPOSITION CLAIM (R-J), because "no new
machinery" was not true as written.** (1) **`severance` is TRADE's
OWNER-SETTLED ENDING KEY, never a drafting verb here.** `TRADE_ENDING_KEYS
= {fortune, ruin, monopoly, collapse, severance, cornered}` is CLOSED and
owner-settled, minted at TR-1/TR-5 with its casus named, and it carries a
policed share envelope at TR-9. This document uses the word ONLY as that
ending key; it drafts no severance and adds no unaccounted mass to TR-9's
envelope. (2) **This wave adds NO INPUT to the three BUILT organic embargo
levers.** Those levers are hysteresis-guarded and mean-reverting, floored
at `EMBARGO_FLOOR 0.15` — "a curtailment, never a total cutoff, a trickle
of trade persists" — and TR-1 already owns that seam (it adds the typed
REASON to the existing paths). Wiring a war-side grievance read INTO them
from the other direction would be two volumes writing one surface, and
would be new machinery besides. The organic levers are left exactly as they
are; the compact below is a SEPARATE, DECLARED instrument with its own read.
NEW ROWS (**catalog-addition requests to GR-3 per R3/J-GR-15**; the
no-producer-less-term law satisfied with named producers AND biting
consumers; each carrying family, executor kind, and `expiresTick`):
- **`embargo_compact`** — the commercial mutual_defense: a PAIRWISE pledge
  of joint refusal toward a NAMED third party. **Family:** security.
  **Executor kind:** these two rows are NEGATIVE OBLIGATIONS, and no
  existing kind covers a prohibition — the 7th kind `grant` is scoped by
  J-GR-5 to standing RIGHTS, not forbearances. **A `forbearance` executor
  kind is therefore REQUESTED OF GRAMMAR through the same J-GR-15 adoption
  block; it is not minted here.** If GRAMMAR declines it, these two rows do
  not build and TB-4 reduces to its composition half (declared fallback,
  not silence). Producers: the ECONOMIC SHARED-THREAT READ — **`shared_
  threat`'s existing token with widened evidence, NOT a sixth trigger**
  (TB-LAW-10; the corner-monopolist or rising trade power as common enemy,
  with the trigger's widened product disclosed and its GR-7 envelope note
  carried) — plus grievance-led drafting. **Consumer: the compact's OWN
  READ, exposed by its own executor and CONSUMED AT DISPATCH (R-J).** The
  built `dispatchEV` is a per-(origin,destination) GO/NO-GO gate with a
  REFUSE/RESUME deadband — there is no candidate list to filter until TR-3
  lands destination CHOICE ("WHERE") behind `believedMarketsEnabled` — so
  the pledge bites as a declared refusal the dispatch gate consults, never
  as a read built inside TRADE's module (C-LAW-2: no read is built in the
  seam between volumes). **T4 fence, stated:** that read returns the
  compact's OWN declared exclusion, never a target list. VERIFY-AT-BUILD
  the exact consumption point at slice start. **Lit-preconditions gain TR-3
  (the dispatch WHERE surface) and TR-5** (`trade_exclusivity`/
  `market_access` — the primacy lock and the access instrument the
  composition half names). **AND TR-6, SCOPED (chair, 2026-08-03 — the
  producer named evidence that does not exist at TB-4's build time).**
  TRADE gates corner and monopoly POSITIONS behind `corneringEnabled` at
  TR-6 (whose own lit-preconditions are TR-2 + TR-3 + TR-4 +
  `commonsVoiceEnabled`), so the CORNER-MONOPOLIST arm of the economic
  shared-threat read **requires TR-6 LIT** and is a lit-precondition of
  that arm alone. **THE DEGRADED ARM IS DECLARED, not left to discovery:
  with TR-6 dark, the economic shared-threat read composes from DEPENDENCY
  AND EXCLUSIVITY EVIDENCE ONLY** — the rising trade power seen through
  dependency fear and exclusivity locks, which need no corner position —
  **and the trigger's receipt NAMES WHICH EVIDENCE IT HAD (absent, not
  zero — SP-4's discipline).** The compact remains fully draftable and
  fully biting on the degraded arm; what TR-6 adds is one more way to see a
  common enemy, never the only way. TB-4 therefore does NOT wait on TR-6.
  THE BLOC IS A WEB — no object; it forms, frays,
  and fragments as war coalitions do. **VOCABULARY FENCE (R-C): CV-1's
  exit tokens apply to COALITION OBLIGATIONS AND COMPACTS — a bloc quitter
  is exit_settled/defaulted/cold ON ITS COMPACTS — and they NEVER re-label
  a treaty's endings or a term's compliance state, which are GR-7's and
  peaceTerms' closed vocabularies respectively.** Compliance under fog: THE
  SMUGGLERS' ROAD IS THE DEFECTION LANE — the ally secretly trading with
  the target is a defaulted term + a CV-4 betrayal-history entry when
  caught, the catching riding the believed-smuggling/exposure machinery
  (which has a real built producer: M7's spill-on-refusal warrant, the
  relational CONTRABAND_TABLE, conscience-gated seizures, the criminal
  rumor carrier). **THE OBLIGOR SEAM, ruled:** the compact's party is the
  SETTLEMENT (CPL-14 — parties are settlements, ever), while the dispatching
  actor is TR-2's house, whose act set is closed — and §5's law holds that
  THE CROWN CANNOT COMMAND THE HOUSE. So a house's caravan crossing the
  line does not become a crown verb: **the settlement's MONITORED
  COMPLIANCE falls, and the term reads defaulted, exactly as any other
  poorly-kept obligation does.** Deniability by emergence, liability by
  monitoring — which is also the better story.
- **`route_restriction`** — the economic demilitarization clause: a
  negative obligation against forming/chartering NEW routes with named
  parties. **Family:** sovereignty. **Executor kind:** the same requested
  `forbearance`. Producer: TB-2's containment drafting + peacetime demands.
  **Consumer: the route-charter estate — CORRECTED (R-J): its teeth LIGHT
  WITH `routeLifecycleEnabled`, not "from day one".** The J route lifecycle
  (demand-then-event charters through the docket, `routeNetworkCharter*.js`)
  is BUILT + TESTED but DARK by default. TRADE's own idiom for this is the
  declared degradation, and it is adopted here: **dark J ⇒ the term is
  DRAFTABLE and RECORDED but its charter-blocking arm is INERT, and TB-7's
  envelope records that arm as an HONEST PERMANENT ZERO until the flag
  lights** — declared, never a silent no-op.
- **`trade_cap` is CUT** by the non_intervention tombstone: **it returns
  only when a wave names its producer AND its biting consumer in one
  stroke.** A decision, not an omission (source-scan pinned). *(Two record
  notes, neither of which reopens the ruling: the flat absolute "no
  consumer bites a volume cap" is too strong — the tree carries quantity
  surfaces today, integer caravan loads and warehouse ceilings, and TR-5
  lands a treaty-grade one in `grain_provision`; and in the tree
  `non_intervention` was NOT removed but landed AS SEAM with
  `executor:'seam'`, recorded-not-enforced. GRAMMAR §1d owns the tombstone's
  reading. J-CT-11's CONDITIONAL phrasing is the one that binds.)*
- **Force/counterforce (same evidence):** the compact's bite (the target
  starves of markets) ↔ the leak (exclusion is pairwise, so the embargoed
  buys through a third market), the smugglers' road, the watchers' judgment
  of a grievance-thin embargo, and TB-LAW-8's minted grievance — every arm
  scoring off the same refusal record.
- **Alignment:** lawfulness01 colours compact-KEEPING (the lawful honour
  the embargo they resent; the chaotic defect when the dear band tempts);
  malice01 colours the punitive-refusal weight. The observer's axis judges
  the embargo of the innocent — a grievance-thin embargo reads as
  aggression to the watchers, feeding THEIR threat reads.
- **Posture (req 10):** compact keeping under temptation consumes
  `riskToleranceOf` — a court with appetite for risk runs the road.
- **Clock (C-LAW-4):** the refusal bites at dispatch cadence; DETECTION of
  a defection travels at news/exposure speed with its carrier named (the
  criminal rumor carrier), so a defection is priced when it is LEARNED, not
  when it happens. INTERVAL_WEEKS denominated.
- **Lifecycle paths:** compacts and restrictions persist as TermRecords on
  the pair's ONE instrument, **including the new `target` field (R-D)** —
  JSON-round-trip pinned over the target id; regen and undo carry the term
  with its target; import validates the target resolves and drops invalid
  rows with a logged receipt; **a DESTROYED OR MERGED TARGET ⇒ THE TERM
  LAPSES WITH A RECEIPT** (never a dangling reference, never a silent
  survival); a DM-edited compact is never machine-dropped.
- **THE THIRD-PARTY FIELD (R-D, chair, 2026-08-03 — the schema amendment,
  declared rather than assumed).** GRAMMAR's TermRecord gains
  **`target?: settlementId`, FOR THE THIRD-PARTY TERM CLASS ONLY**,
  declared in the EXACT shape of the beneficiary precedent: its own
  `[AMENDED]` block in GRAMMAR §4 (as `beneficiary` got its `[CORRECTED …
  R1]` block), its own JSON-round-trip pin, and the destroyed-target lapse
  rule above. It is the same axis that keys the stacking cell (family ×
  beneficiary × target, R-B), which is why one decision closes both.
  **MONITORING IS UNCHANGED AND STAYS TWO-ID:** compliance reads the
  OBLIGOR's monitored lanes through the existing `observerReach(observerId,
  obligorId)` — B watches A, as always. **The target id is CLASSIFICATION,
  never a reach requirement**; nobody gains a read into a third party's
  traffic, and law I's pairwise discipline is untouched (a three-realm
  arrangement is still three bilateral instruments that happen to rhyme).
- **Bands:** the compact's refusal significance is an SP-6a class
  assignment against the SP-6b family; no scale authored.
- **Couplings:** CW-0 rows war×trade (the refusal at dispatch), war×war
  (the defection into CV-4's ledger), war×grammar (the target field + the
  stacking key), full key set, same commit. Cascade: a bloc forming or
  fraying across several pairs registers with CW-1.
- **Dossier round-trip:** compacts and restrictions render on the treaty
  document with their named target, and the refusals render on the trade
  panel — open the town, see who it will not sell to and by whose promise.
- **Pins:** the web forms on an economic shared-threat fixture (with
  `shared_threat`'s own token, no sixth trigger — source scan); **the web
  ALSO forms with `corneringEnabled` DARK, on dependency/exclusivity
  evidence alone, and the trigger receipt names which evidence it had (the
  degraded-arm pin, both arms — and the negative: no corner-monopolist
  evidence is read when TR-6 is dark)**; a caught
  defection defaults + prices into CV-4; TB-LAW-8 asserted (the embargoed
  party's grievance minted through TR-1's severance evidence);
  `route_restriction` blocks a charter on a fixture WITH
  `routeLifecycleEnabled` lit, and records an honest permanent zero
  without it (both arms); one pair holds TWO compacts against TWO targets
  without `term_refused_stacking` firing, and a SECOND compact against the
  SAME target still refuses (the stacking-key pin, both signs); a destroyed
  target lapses the term with a receipt (the hardest lifecycle negative);
  the compact's read returns its own exclusion and never a target list (T4
  source scan); nothing is written into the three organic embargo levers
  (source scan); the cut cap stays cut.

### TB-5 — THE DELIBERATE LANES (the olive branch + the pragmatic override)

THE OLIVE BRANCH: **the closed formation-trigger set gains EXACTLY ONE
token — `rapprochement` (TB-LAW-10, minted by GRAMMAR, adopted here by
pointer)** — a seat may PROPOSE mutually-beneficial trade specifically to
mend a wounded pair. **Its scorer, named as the closure contract requires:
the healing calculus (the pair's standing resentment band × the deliberate
door's reachability) crossed with the proposer's own books** — i.e. a court
proposes rapprochement when the wound is real, the door is open, and it can
afford the gesture. It validates on import against the closed set like
every other token, and it takes its own cell in GR-7's trigger-mix
envelope. The branch is priced (posture spent; refusable with the standing
grievance as the recorded reason — the spurned branch is itself a receipt,
minting NO grievance, per J-GR-7's refusal asymmetry: peacetime refusal =
turning-point + banded trust delta, no grievance), and effective even AT
the saturation band because it is a DELIBERATE act — **the healing
lattice's FOURTH deliberate door beside the burial, remission, and honest
settlement.** Acceptance still requires both pictures positive (you cannot
mend with a deal the other side reads as tribute).
THE PRAGMATIC OVERRIDE (TB-LAW-9 executed): the formation/acceptance
scoring weighs economic benefit against the grievance colour — a sufficient
gain clears a hostile threshold, receipted as the against-the-grudge deal,
and PRICED elsewhere: the commons' judgment (legitimacy where the crowd
cares — INT coupling; and CPL-18 holds, the crowd's veto is priced delivery
failure, never a signature at the table), conscience reading the
counterparty (the tolerance machinery), the watchers' axis. **THE SEAM,
CARRIED FROM TB-LAW-9: the override operates at FORMATION AND ACCEPTANCE
ONLY and does not touch the generation-time `hostile ⇒ NO_TRADE` link
gate.** Until TR-5's `market_access` lands, the override's reachable
product is the SIGNATURE and its priced consequences; the caravans wait on
TRADE. Sol builds to that boundary and the fixture asserts it.
- **Force/counterforce (same evidence):** the mending pull of gain ↔ the
  crowd that remembers what gold forgets — same deal, two ledgers.
- **Alignment:** the good court resists profiting from the butcher
  (malice01 low RAISES the override bar); the pragmatic chaotic court
  clears it cheap. **lawfulness01 colours the FORM: whether the override is
  drafted as a formal term or left as an UNWRITTEN UNDERSTANDING — and the
  understanding is explicitly NOT a second instrument (corrected).** It
  carries no term, no catalog row, no executor, and no compliance surface;
  it is a disposition between courts, and if either wants it enforceable it
  must become a term on the ONE instrument (R1). A binding deal outside the
  instrument does not exist in this model, and alignment colours the
  threshold, never the structure (E3).
- **Posture (req 10):** the branch SPENDS posture to be offered and the
  override's bar reads `postureOf`/`riskToleranceOf` — a proud court pays
  more to bend, a desperate one bends cheap; out-of-posture acts are priced
  and receipted news, never forbidden.
- **Clock (C-LAW-4):** the proposal and its answer ride the envoy lane's
  own latency (a branch travels; it does not teleport); the world's reading
  of an against-the-grudge deal arrives at news speed. INTERVAL_WEEKS
  denominated.
- **Lifecycle paths:** an ACCEPTED branch becomes ordinary terms on the one
  instrument and rides TB-1's lifecycle; the OVERRIDE RECEIPT persists on
  the chronicle (JSON-round-trip, regen/undo carry it, import validates the
  kind); the spurned-branch receipt likewise. **The unwritten understanding
  persists NOTHING, by construction** — which is the point of naming it.
- **Bands:** override gain-threshold curve + commons-price bands, §7;
  branch receipt significance is an SP-6a class assignment.
- **Couplings:** CW-0 rows war×grammar (the new trigger + the override at
  acceptance), war×interior (the commons price), war×trade (the gain read),
  full key set, same commit.
- **Dossier round-trip:** the branch, its refusal, and the against-the-
  grudge deal render on the treaty/diplomacy panel and on the commons page
  where the price landed — cause-walkable from the grumbling to the
  signature.
- **Pins:** `rapprochement` REACHABLE on a real generated corpus (the
  reachability pin the closure contract demands — a trigger nobody can fire
  is orphan vocabulary); the olive branch reaches a SATURATED pair (the
  deliberate-lane fixture) where ambient contact cannot; the spurned branch
  receipts and mints NO grievance (the refusal-asymmetry negative); the
  override clears a hostile threshold on sufficient gain AND pays the
  commons price (both asserted on one fixture); no override ever fires
  without its receipt; **the override never opens a NO_TRADE link (the
  seam negative — source scan: no wave here imports or edits
  `tradeLinks.js`)**; the unwritten understanding creates no term and no
  compliance record (source scan).

### TB-6 — THE INEQUITY READ (bad deals and great deals exist)

**THE READ IS PER-OBSERVER (R-E, chair, 2026-08-03 — the K4 cure, and the
one change that makes this wave lawful).** A single per-instrument band
computed by comparing BOTH parties' private valuations is a MERGED ESTIMATE
across a court boundary: it reads the counterparty's own valuation, which
is an intentions/books secret, and C-LAW-5 routes every such read through
belief while K4 forbids merging two pictures into one number. So the read
is redesigned, and it costs nothing but honesty:
**EACH SIDE DERIVES ITS OWN FELT-EQUITY BAND, from its OWN valuation and
its BELIEVED counterpart surplus** — belief-routed, K3-pinned, sourced from
what this court thinks the other got. Banded CLOSED **{boon, fair, lean,
extractive}**, one band PER SIDE, never merged, never stored (re-derived
from the persisted signature valuations). Two sides can and often will
disagree about the same treaty, and that disagreement is the drama: the
victor who believes it was generous, the loser who knows better.
**THE HEALING RATE CONSUMES EACH SIDE'S OWN FELT BAND** (TB-LAW-7
sharpened — a boon mends fastest for the side that feels it; a fair deal
mends; a lean one mends nothing; an extractive one ACCRUES strain through
TB-LAW-5's justice lens even while honoured — the unequal treaty is a slow
wound wearing a seal). **THE OBJECTIVE TWO-SIDED COMPARISON STILL EXISTS —
but ONLY in TB-7's GATE-SIDE INSTRUMENTATION**, where a soak harness may
lawfully look at both pictures at once because it is measuring the world,
not playing in it. No engine effect ever reads it.
Bad deals arise two honest ways, both lawful: BELIEF ERROR (a picture was
wrong — the wrong-market tragedy at treaty scale; the deal was fair in the
mind and lean on the ground) and COMPULSION (the least-bad acceptance under
dependency or weakness — signed, valid, and resented).
Other consumers: RENEGOTIATION (GR-5's from-strength machinery is exactly
the lean deal's exit; the read names which instruments are ripe — from the
ripe side's own picture) · THE HERALD ("The wool went cheap, and both sides
knew it" — pools under the floor; the band CROSSING on re-read is the
tellable moment: the deal the world turned sour).
**THE PERSISTENCE, DECLARED AS AN EXCEPTION (R-E).** The band is derived,
but the SIGNATURE-TIME VALUATIONS it re-derives from must persist — and by
this wave's own design ("RE-READ as pictures shift") they are NOT
re-derivable after the fact. TRADE numbers its exceptions explicitly
(J-TR-3: books are "the second disclosed exception to never-store-a-
derivable", J-WR-3's idiom); this is therefore declared **THE THIRD
DISCLOSED EXCEPTION TO NEVER-STORE-A-DERIVABLE: the per-side signature
valuations, event-accrued at signature, one writer, with round-trip pins.**
It lands as **TB-6's FIRST SLICE** (SP-3's two-picture persistence is
VERIFY-AT-BUILD; if SP-3 has not landed it, TB-6 lands the persistence
itself, disclosed). The persistence lives where the two-picture machinery
lives — NOT inside `peaceTerms.js` or `pactFormation.js`, whose
`negotiationPictures` import a source-scan walker forbids; TB-6's read is
homed in its own leaf that may import it. (Note the shape it must survive:
GRAMMAR's pactProposals record PRUNES at signing — "the ledger is a queue,
not an archive" — so the valuation cannot be left there; and the prior-band
comparison a re-read crossing needs follows `worstObservedEver`'s
precedent: an explicit monotone-or-not declaration, one writer, written
only under the wave's own flag so dark ⇒ never written ⇒ byte-identical.)
- **Force/counterforce (same evidence):** the extractive draft's revenue ↔
  the felt-extractive band's strain and the ripening renegotiation — the
  same signature read twice, once as gain and once as grievance.
- **Alignment:** malice01 colours the extractive-drafting appetite; the
  observer's axis judges the extractive deal as it judges extraction (loud
  once news latency is paid). lawfulness01 colours nothing here — declared
  alignment-empty on that axis, with reason: drafting greed is an appetite,
  and the oath-price axis has no purchase on a deal freely signed.
- **Posture (req 10):** the ACCEPTANCE decision consumes `postureOf` — a
  defiant court refuses a lean deal a weary one signs, and that is how
  compulsion-lean becomes reachable at all.
- **Clock (C-LAW-4):** the signature read is same-tick and own-court; **the
  RE-READ propagates at NEWS SPEED with its carrier named** — a court's
  picture of the counterparty's surplus updates when word arrives (rumor
  hop latency, distance-priced), never the tick the world moved. Band
  crossings are therefore late by construction, which is the story.
  INTERVAL_WEEKS denominated.
- **Lifecycle paths:** the persisted signature valuations (the third
  disclosed exception) round-trip through JSON, survive regen and undo with
  their instrument (never re-derived from the chronicle — they cannot be),
  and import validates them; a missing valuation reads ABSENT, not zero,
  and the band read declares itself unavailable rather than guessing. The
  BAND itself persists nothing. A DM-edited instrument keeps its
  valuations.
- **Bands:** inequity band edges {boon, fair, lean, extractive} — a
  volume-local closed family, declared volume-local in §7; the crossing
  receipt's significance is an SP-6a class assignment against SP-6b.
- **Couplings:** CW-0 rows war×belief (the believed-surplus read) and
  war×grammar (the signature persistence), full key set, same commit.
- **Dossier round-trip:** each side's felt band renders on ITS OWN treaty
  page — the same instrument reading `boon` on one panel and `lean` on the
  other is the feature, not a bug — with the crossing receipts on the
  timeline.
- **Pins:** the four bands reachable on real fixtures (incl. compulsion-lean
  — a dependency-priced acceptance); **the two sides' bands DIVERGE on a
  fixture (the K4 pin — one instrument, two honest answers)**; no engine
  effect reads both pictures (source scan — the hardest negative; the only
  two-sided comparison in the codebase lives in TB-7's harness); the
  believed-surplus read routes through belief machinery (K3 import pin);
  the re-read crossing receipts, and does so only after news latency is
  paid; the extractive-while-honoured strain accrual asserted (the
  negative: a FAIR honoured deal accrues NOTHING); the band itself
  derived-never-stored (source scan); the valuations round-trip through
  regen/undo/import; dark flag ⇒ nothing written ⇒ byte-identical.

### TB-7 — THE STRAIN + THE MEASURE (the acceptance harness)

The justice-scaled strain law implemented: strainDelta = ask magnitude
(share band × duration × capacity context) × basis cruelty (population >
resource_share > prosperity_indexed > goods > service) × believedInjustice
(the loser's believed outcome vs the ask, reading the war's closing
receipts, the exhaustion scar, and `postureOf` — TB-LAW-5).
**THE STRAIN LANDS ON TWO LEDGERS, NOT THREE (R-G, chair, 2026-08-03).**
The inward write is DELETED. CPL-21 is the law — "tribute_strain resentment
points OUTWARD at the victor, never inward at the paying seat… the
instrument reaches the interior only as strain, unattributed" — and J-INT-3
forbids the parallel write by name: "the narrated middle is attribution +
the commons tribute term through the existing composite — NEVER a new
direct legitimacy write from treaty state; the grain already bills through
economic_capacity and the pressure lanes, and a parallel write
double-counts the same drain." So:
1. **THE PAIR GRIEVANCE (revanchism)** — outward, at the victor:
   `tribute_strain`, minted in `accrueStrainResentment`
   (`peaceTerms.js:1337-1353`; the old :1023-1044 citation was stale).
2. **THE WATCHERS** — extraction is a loud public fact (C-LAW-5, once news
   latency is paid), read into every observer's threat scoring and
   alignment-lens judgment.
**THE KING-WHO-PAID ARC IS NOT LOST — IT IS POINTED AT ITS OWNER.** TB-7
POINTS AT INT-4's COMMONS COMPOSITE as the owning lane: INT-4's
TREATY-BURDEN term, under INT-4's own cap ("NO new state, NO new writes"),
INERT without `economicCoupReadEnabled`, lit in the owner's own lighting
order. The punitive victor still destabilizes its own tributary — which
containment doctrine should still fear — but the write is INT-4's and the
cap is INT-4's, and this document consumes the arc rather than forking it.
**THE NON-STREAM SEAM, COORDINATED (folded).** INT-4's treaty-burden term
reads LIVE OUTBOUND-STREAM state — "the payer's book of the same conserved
grain peaceTerms already moves" — and two of the five bases MOVE NO STREAM:
the SERVICE levy (receipted acts) and the POPULATION levy (columns). Left
alone, the arc would go silent for exactly the two cruellest bases.
Ruled: **the non-stream bases coordinate through INT-4's OWN RECEIPTS PATH**
— the service levy's rendered-act receipts and the levy's column receipts
are surfaced to INT-4's existing read as compliance evidence of the same
burden, through INT-4's writer, under INT-4's cap. One lane, one writer,
five bases. (An INT-4-side amendment; recorded here as this document's
request, and INT-4's to accept — the cross-volume edit is the cohesion
pass's, not the implementer's.)
The HEALING terms (TB-LAW-7) and the INEQUITY modifiers (TB-6, each side's
own felt band) enter the same arithmetic with opposite sign.
**MEASURE (WR-9 discipline) — RESTATED IN LAWFUL VOCABULARIES (R-C, chair,
2026-08-03; the previous envelope mixed three closed sets and coined
spellings, the exact confusion GR-7 legislated against by name).** Three
vocabularies exist over these objects and they are never blended:
- **GR-7's PACT ENDINGS (closed, per-instrument/per-lineage-act):**
  `ran_its_term` · `renewed` · `converted` · `renegotiated` · `repudiated`
  · `disavowed_by_succession` · `hollowed_quiet` (DM-only) ·
  `hollowed_detected` · `broken_by_war`. TB-7 REFERENCES GR-7's existing
  endings-mix envelope over these — it does not restate a parallel one.
  (`honored_to_term` was a new spelling for `ran_its_term` and is retired.)
- **THE COMPLIANCE-STATE UNION, CITED BY NAME (peaceTerms' own, per-term,
  existing):** `honored` / `strained` / `defaulted` / `expired`. **The
  TRIBUTE LIFECYCLE MIX is an envelope over THIS union**, per basis — how
  tribute is actually LIVED, which is what this wave wanted to measure.
  **`source_collapsed` IS NOT AN ENDING: it is `defaulted` with a reason
  field the envelope reads** (the goose envelope is therefore a
  defaulted-by-source-collapse SHARE within the union, rare-but-reachable —
  and per TB-LAW-4 it is reached through the tributary's own physics, since
  the built reserve floor makes the stream default before it starves a
  court).
- **`amended`-ACT COUNTS (the lineage-act set, closed):** REMISSION is
  measured as a count of `amended` acts drafting downward — not as a
  `remitted` ending, which never existed.
The rest of the harness: **NO PERMANENT TRIBUTE** — and stated so it is not
vacuous: a SINGLE term cannot be permanent by construction (`expiresTick`
is REQUIRED on every stream/status term; perpetual extraction is
unrepresentable), so what this envelope actually falsifies is **an
unbounded RENEWAL LINEAGE CHAIN** — a pair whose instrument is renewed and
re-renewed past a generational horizon band. That is the anti-ratchet, and
that is the cell the soak can red · **RECONCILIATION REACHABLE** (a healed
pair on a real fixture — the counterforce audit's row) + **SATURATION
rare-but-reachable**, both **REFERENCING INT-7's already-authored burial
endings envelope** rather than duplicating it over the same fixtures
(INT-7 owns `{buried_and_blessed, buried_and_borne, buried_and_answered,
dug_up, lapsed_with_the_pair}` with its own floors; TB-7 adds the tribute
pair's reachability cell and cites INT-7 for the rest) · the **EMBARGO-WEB
envelope** (blocs form AND fray on the soak; a permanent universal embargo
reds; and `route_restriction`'s charter arm records an **HONEST PERMANENT
ZERO** while `routeLifecycleEnabled` is dark, declared rather than silent)
· the **INEQUITY mix** — measured **GATE-SIDE, where the objective
two-sided comparison lawfully lives (R-E)**: all four bands live per side,
extractive rare, and the DIVERGENCE between the two sides' felt bands is
itself a measured cell · the **COLD-EDGE STATE CENSUS** (zero-deployment
live-cause pairs counted as a census cell — **WR-9's endings mix is
UNTOUCHED and gains no cold slot**) · the **three-coalition + tribute soak
case** (a triangle war ending in asymmetric tributes, the full cascade
narrated end to end on the cause walk). Mutant controls on every envelope.
- **Force/counterforce (same evidence):** strain accrues and the healing
  lattice discharges — one arithmetic, opposite signs, off the same term
  record; the watchers' judgment is itself counter-weighted by the
  reconstructive doctrine's visible mercy.
- **Alignment:** the WATCHERS row of §6 — every observer judges the
  extraction on ITS OWN axes, per-observer, never a global verdict. The
  strain arithmetic itself is alignment-empty by design (it is physics ×
  belief), declared with that reason.
- **Posture (req 10):** believedInjustice's floor consumes `postureOf` (the
  defiant town forgives slowly, the weary one fast) — INT-4's input,
  generalized, not re-minted.
- **Clock (C-LAW-4):** strain accrues on the treaty's own clock
  (`STRAIN_RESENTMENT_PER_YEAR` on `treatyTicksPerYear`); **the WATCHERS'
  read is NEWS-CARRIED** — extraction is loud only once latency is paid,
  hop-priced by distance, so a far court's judgment lags by construction.
  Decay of what strain deposits rides the D5-scaled clocks (TB-LAW-7).
  INTERVAL_WEEKS denominated throughout.
- **Lifecycle paths:** strain writes through the EXISTING incident/
  grievance writers (no new stock, no new writer); the envelopes are
  HARNESS state, not world state, and persist nothing into the save. The
  gate-side two-sided instrumentation is soak-only and is source-scan
  pinned OUT of the engine build.
- **Bands:** basis-cruelty multipliers and the generational horizon band
  are volume-local, declared volume-local in §7; every envelope's grading
  axis is an **SP-6b family reference** (WR-9's share-envelope discipline),
  never an authored scale.
- **Couplings:** CW-0 rows war×interior (the pointer to INT-4's lane, with
  INT-4 named as owningVolume/owningWave), war×belief (the watchers), full
  key set, same commit. **Cascade: the triangle-war cascade and the
  bloc-fray cascade REGISTER WITH CW-1 (C-LAW-6)** — history at full
  coupling can flood the Herald, and depth never becomes wallpaper.
- **Dossier round-trip:** the strain renders on the pair page (what this
  peace is costing, and to whom) and the tributary's burden renders through
  INT-4's own commons surface — not a second panel here.
- **Pins:** NO inward legitimacy write from treaty state exists anywhere
  (source scan — the hardest negative, J-INT-3's own shape); the service
  and population bases reach INT-4's read through its receipts path
  (fixture: the king-who-paid arc fires for a service-only levy); the
  envelope names only GR-7 endings, compliance-union states, and `amended`
  counts (a vocabulary walker asserts no coined spelling); `source_
  collapsed` appears as a defaulted REASON and never as an ending token;
  WR-9's endings mix is byte-unchanged by this wave; the renewal-chain
  anti-ratchet cell reds on a seeded perpetual-renewal fixture; the
  gate-side two-sided read is absent from the engine build (source scan).

## §4-EXP THE EXPANSION WAVES (XW-1..XW-8; from the exhaustive dynamics
## sweep, owner-ordered 2026-08-02/03; INTEGRATE-FIRST is this section's own
## law — every wave names what it rides before what it mints, and a wave
## that could be composition instead of mechanism was demoted to §5)

**SECTION-WIDE TOTALITY (the standing slots; each wave states its DELTAS
below and inherits the rest).** Every XW wave: mints catalog rows only as
**CATALOG-ADDITION REQUESTS to GR-3 per R3/J-GR-15** (pointers, never rows;
family + executor kind + producer + consumer + `expiresTick` or it does not
close) · adds no formation trigger (**TB-LAW-10: triggers decide WHEN, the
catalog decides WHAT** — the subsidy, the loan, the guarantee, and the
arbitration compact are SHEET COMPOSITION under the existing five plus
`rapprochement`) · carries **its own flag AND a WR-9-style certification
row**, with lit-preconditions stated as LIT (not merely landed) and the
lighting order enforced by the certification walker · denominates every
clock against `INTERVAL_WEEKS` and names a **CARRIER for every cross-court
read (C-LAW-4)** — nothing lands in a foreign court the tick it was caused
· registers **CW-0 rows with the full key set** (pairId, direction, read,
receiptField, counterforce, flags, owningVolume, owningWave, intendedDesk)
in the same commit as the read, and **registers multi-layer cascades with
CW-1 (C-LAW-6)** · **references SP-6b severity families and assigns SP-6a
classes — no wave authors a scale** · ships **≥4-variant receipt pools**
for every new kind, registered in WHAT_PHRASES + heraldRouting + rumor
voice + chroniclersLetter in the kind's own commit · and pays
**requirement 14(b)**: persisted state round-trips through JSON, survives
regen and undo carried WITH its record (never re-derived from the
chronicle), validates on import with a logged receipt for dropped rows, and
a DM-edited row is never machine-dropped.

### XW-1 — THE SECRET PROTOCOL (owning volume: GRAMMAR; flag
### `covertTermsEnabled`; the largest missing twist-generator)

RIDES: includeCovert (the estate's standing audience projection), the envoy
lane's declared/true purposes (negotiation in the dark), the exposure stack
(IN-0b interception, IN-3's sweep, the compromised envoy), GR-4's
succession question (the heir finds the protocol in the father's papers),
the grievance ledgers. MINTS: a `covert: true` state on individual
TermRecords of the ONE public instrument (never a second secret instrument
— R1 holds; the public NAP carries the secret partition protocol as covert
directional terms). THE LOUD-FACT AMENDMENT, ruled: the INSTRUMENT'S
EXISTENCE stays loud (C-LAW-5 untouched); its covert TERMS are not —
scoped visibility rides the same projection discipline as every covert
surface. COMPLIANCE SCOPING: a covert term binds and enforces between its
signers only; monitoring is signer-side by construction. EXPOSURE: any
standing exposure path may surface a covert term — the receipt fires the
betrayed third parties' grievances AT ONCE ("you promised THEM what?"),
with the strain read through TB-LAW-5's justice lens at maximum (a secret
promise broken in the dark reads maximally unjust), and the exposed
signers' credibility pays the lie-fall rate. The Zimmermann arc composes:
an intercepted covert proposal TO a third party pushes the discoverer
toward the opposing web. Alignment: lawfulness01 colors willingness to
sign covert terms (the lawful prefer the open instrument — a capped
drafting weight); every observer judges an exposed protocol on its own
axes. **Slots (deltas):** force/counterforce — the secret's advantage ↔ the
exposure stack + the betrayed third parties' grievances, same record.
Posture: the signing court's `riskToleranceOf` prices signing in the dark.
Clock: covert terms enforce at treaty cadence; EXPOSURE travels the
interception/rumor carriers at hop latency — the "you promised THEM what?"
tick is the ARRIVAL tick, never the signing tick. Lifecycle: the
`covert: true` flag persists ON the TermRecord (no second record) —
round-trip, regen, undo and import carry covert scope with the term, and
the audience projection is re-derived, never persisted per-viewer. Dossier
round-trip: the instrument renders to all; its covert terms render only to
signer-scoped audiences (`includeCovert`) — the DM sees both, and the
projection pin is the test. Couplings: CW-0 war×information; cascade
(exposure → multi-party grievance) registers with CW-1. Pins: a covert term
invisible to non-signer projections (audience
pin, the hardest); exposure fires ALL betrayed-party grievances in one
tick; the succession-discovery path reachable; unexposed covert terms
still enforce between signers; mutant control (expose with no third-party
reference ⇒ no betrayal grievance).

### XW-2 — THE WEDGE (owning volume: INFORMATION, as an IN-2 amendment;
### rides `infoLureEnabled`)

RIDES: IN-2's axis-typed disinfo record (the correction pass just
generalized it), reconcileBelief's contradiction machinery, source
credibility, the corroboration ladder, the blowback triple. MINTS: one new
subjectKind — `intent`, pair-referenced: a plant asserting a THIRD party's
hostile intent toward the mark (make A believe B plots against A — divide
et impera; Iago's whisper; the Ems Dispatch was a wedge-EDIT). The planted
belief feeds the mark's EXISTING threat/hostility reads (colors bars,
never mints casus — J-INF-9 holds absolutely). COUNTERFORCES, same
evidence: B's actual conduct contradicts the planted intent over time (the
contradiction comparator on the intent axis — lived peace is the wedge's
solvent); corroboration; and on exposure the deception_betrayal grievance
re-aims at the commissioner from BOTH wedged parties at once — the exposed
wedge UNITES its victims (the twist's own twist, and the reason wedging is
self-limiting exactly as betrayal is). Alignment: malice01 colors the
commissioning appetite; the observers judge an exposed wedge-maker as they
judge a betrayer. **Slots (deltas):** counterforces are already named above
and all score off the SAME plant record. Posture: the commissioner's
`riskToleranceOf` prices the plant; the mark's vetting posture prices the
catch. Clock: the plant propagates on the rumor carrier at hop latency, and
the contradiction comparator decays it on the D5-scaled memory band —
`INTERVAL_WEEKS` denominated. Lifecycle: the `intent` subjectKind record
persists on IN-2's existing disinfo ledger (no new ledger) —
round-trip/regen/undo/import per its owning volume's rules. Dossier
round-trip: the planted belief renders on the mark's belief panel and, once
exposed, on both victims' grievance pages — the war-over-an-authored-
grievance walk. Couplings: CW-0 war×information; the two-victim re-aim is a
cascade and registers with CW-1. Pins: a wedge never mints or strengthens a casus
directly (E3/J-INF-9 import pin); the two-victim re-aim on exposure; lived
contradiction decays the plant (fixture: a wedged pair that keeps trading
sheds the wedge); the war-over-an-authored-grievance story renders on the
cause walk end to end.

### XW-3 — PREVENTIVE PRESSURE (owning volume: WAR, a WR-1/reasons-layer
### amendment; rides `warTerminationEnabled`'s substrate)

RIDES: the SAME believed-trajectory/threat read TB-2's containment doctrine
already consumes (one read, two consumers — no second trajectory
evaluator), the mirror (IN-1 — the riser can SEE the fear forming), the
reassurance instruments that already exist (the NAP offered from strength,
the olive branch downhill, disclosure terms). MINTS: the preventive WEIGHT
— a rising neighbor's trajectory band lowers the war-motive bar (capped,
colored, E3-compliant: it never selects, never mints a casus; the Thucydides
trap as a threshold color). The dilemma is playable on existing reads:
deterrence raises the riser's own threat read of YOU (the spiral);
reassurance spends position to damp it (the NAP from strength is the
de-escalation verb, already in the catalog). Alignment: lawfulness01
damps preventive appetite (the lawful need the grievance, not the
forecast); malice01 feeds it. **Slots (deltas):** force/counterforce — the
preventive weight ↔ the reassurance instruments and the spiral's own cost,
both scored off the SAME believed-trajectory read (one read, two
consumers — no second trajectory evaluator). Posture: `postureOf` composes
with the weight at the motive bar; a weary court does not start a war
against a forecast. Clock: the trajectory read is belief-side and updates
at news speed (the riser is seen late), `INTERVAL_WEEKS` denominated.
Lifecycle: NONE — a capped read-side weight, no stored state, no writer;
the source scan asserting no new evaluator IS the pin. Dossier round-trip:
DECLARED NONE for the weight itself, with reason; what renders is the
motive receipt naming the trajectory band that coloured it, on the existing
war panel. Couplings: CW-0 war×belief. Pins: preventive weight capped below any
single live casus (fear alone cannot clear a bar a real grievance
couldn't); the reassurance arm measurably damps (fixture: NAP signed ⇒
preventive weight falls); the spiral reachable AND breakable on one
seed-family.

### XW-4 — THE SUBSIDY (owning volume: GRAMMAR GR-3, **ECONOMIC family row
### — CORRECTED 2026-08-03: this header read "security family"; a subsidy
### is a conserved stream pointed at a friend and it rides TB-3's stream
### shapes whole, so it lands in the economic family with the `transfer`
### executor as a CONDITIONED stream. §4's assignment table is the
### authority**; rides `tributeFamilyEnabled` + `coalitionConvenienceEnabled`)

RIDES: the stream executor family (TB-3's goods/prosperity-indexed shapes —
a subsidy IS a tribute stream pointed at a friend), WR-6's join decision
(the read that NAMES the price the subsidy PAYS — the missing payment
instrument for a priced join), CV-1's adequacy band REUSED WHOLE as the
subsidy's compliance read (the theatrically-fighting client is an
inadequate discharge — fog's war-obligation form), the war's own lifecycle
(the term lapses at the subsidized war's end — expiry riding the edge's
termination, no timer). MINTS: the `subsidy` catalog row + the
conditioned-on-war lifecycle hook. Twists carried free: the paymaster war
(the mercantile-disposition realm fighting with its treasury); the subsidy
withdrawn mid-war as leverage (an amendment-down with coalition-scale
consequences — remission's dark twin); the client that outgrows its
paymaster. Alignment: lawfulness01 colors honoring the subsidy through a
losing war; malice01 colours nothing — declared empty, with reason (paying
a friend is not an appetite the malice axis reads). **Slots (deltas):**
force/counterforce — the subsidy buys the join ↔ the paymaster's own drain
and the client that outgrows it, same stream record. Posture: the
paymaster's `postureOf` prices paying rather than marching (the mercantile
seat's own character). Clock: disbursement on the treaty's 52-week clock;
the war's end lapses the term through the EDGE's termination, no timer;
the client's reading of a withdrawn subsidy travels at news speed.
Lifecycle: the `subsidy` TermRecord + its conditioned-on-war hook
round-trip, survive regen/undo with the instrument, validate on import;
**an orphan stream after the war's end is the negative pin**. Dossier
round-trip: the subsidy renders on the treaty document and on the client's
war panel (who is paying for this war). Couplings: CW-0 war×grammar,
war×war (the priced join). Bands: adequacy is CV-1's/WR-6's, REUSED whole —
no second read, no second band family. Pins: join price payable (fixture: a
priced join clears on the subsidy's signature); theatrical discharge
detected at the adequacy band; lapse-at-peace (no orphan streams).

### XW-5 — THE LOAN (owning volume: GRAMMAR GR-3, economic family row;
### rides `tributeFamilyEnabled`)

RIDES: stream shapes (disbursement), the I2 reparations-claim shape
(default→claim conversion — the built idiom, new subject), GR-5's
renegotiation-from-strength (the creditor's leverage lane), house credit
grammar (E1b's precedent at settlement grain). MINTS: the `loan` row — a
two-phase term (disbursement stream, then repayment stream on the 52-week
clock) whose DEFAULT converts the balance to a standing claim; the claim
feeds the creditor's drafting position. THE DEBT-TRAP ARC composes, never
scripted: lend big in the lean year → the default → the claim → the
renegotiation that takes the port (Egypt 1876; Suez). Counterforces:
repudiation (GR-4's succession door — the heir disavows the father's
debts: Brest-Litovsk's OTHER lesson), the creditor's own dependency
(TB-2's inversion — the lender grows around the repayment), and the
watchers pricing predatory lending on their own axes. Alignment:
malice01 colors predatory-terms drafting; lawfulness01 colors repayment
fidelity. **Slots (deltas):** counterforces are named above (repudiation,
the creditor's own dependency, the watchers) and all read the SAME loan
record. Posture: the borrower's `postureOf` prices taking the lean-year
loan at all; the creditor's risk tolerance prices lending it. Clock: both
phases on the treaty's 52-week clock; the default→claim conversion is an
act, and the WORLD's pricing of a predatory lender arrives at news speed.
Lifecycle: the two-phase term and its standing claim persist on the
instrument — round-trip pinned across the PHASE BOUNDARY (a loan
mid-conversion must survive regen/undo without losing its balance),
import-validated; **the phase state is derived from recorded ticks and
balances, never a stored countdown**. Dossier round-trip: the loan, its
phase, and any standing claim render on the treaty document and the seat's
obligations page — the debt-trap arc walkable end to end. Couplings: CW-0
war×grammar, war×trade (the creditor's leverage). Pins: the full two-phase
lifecycle on one fixture; default→claim→leverage walkable;
repudiation-by-succession reachable; the phase boundary survives
regen/undo; T2 holds (banded streams and claims, no coin anywhere).

### XW-6 — THE ARBITRATION COMPACT + THE ULTIMATUM STANCE (owning volume:
### GRAMMAR; composition-plus-naming, the lightest wave)

ARBITRATION rides GR-6 whole (the single-finder law untouched — arbitration
is an OCCASION): a pre-commitment compact term — **SECURITY family, the
REQUESTED `forbearance` executor kind, spelling GRAMMAR's to set at
adoption (§4's assignment table): the binding CONTENT is negative, not to
repudiate an accepted finding, while the acceptance itself is GR-6's
occasion and not an executor's product** — both parties bind to
accept the mediator's sheet; repudiating an ACCEPTED arbitration is a
breach through the standard shell at treaty-grade severity; the
mediator-turned-guarantor pipeline completes (brokered → bound →
guaranteed). THE ULTIMATUM is pure naming over built machinery: a
formation proposal drafted under war-shadow (a live deposited intent on
the pair), where `expired_unanswered`/refusal feeds the intent's weight —
one receipt family marks the stance ("The demand was delivered with the
seal still wet on the muster rolls"). **Slots (deltas):** force/counterforce
— the compact's binding force ↔ the cost of repudiating an accepted
finding, and the ultimatum's pressure ↔ the refusal's own dignity price,
each off one record. Alignment: lawfulness01 colours honouring an accepted
arbitration (declared: malice01 empty here — submitting to a finder is not
an appetite). Posture: the ultimatum's DELIVERY consumes `postureOf` (a
proud court delivers demands it cannot back). Clock: arbitration on the
mediation lane's own cadence; `expired_unanswered` is a read against the
proposal's `answerDueTick`, never a timer; news-carried both ways.
Lifecycle: the arbitration compact is an ordinary TermRecord (TB-1's
lifecycle); the ultimatum persists NOTHING beyond the existing proposal
record and its receipt — declared, because a stance that quietly became
state would be the defect. Dossier round-trip: the compact renders on the
treaty document; the ultimatum renders as its receipt family on the
diplomacy timeline. Couplings: CW-0 war×grammar. Pins: repudiated
arbitration prices at treaty grade; the ultimatum's refusal-to-war chain
walkable; the single-finder law untouched (GR-6 byte-unchanged); neither
adds an evaluator (source scan).

### XW-7 — SCORCHED EARTH (owning volume: WAR, defense-side;
### ⚠️ OWNER-GATED ACTIVATION — specced complete, BUILDS ONLY on explicit
### owner sign-off, recorded here per the chair's flag)

RIDES: WR-8's destruction arithmetic POINTED INWARD (the razing's conserved
accounting, self-directed — no new destruction physics), the
feasibility/threat read (the believed-incoming conquest as trigger
context), posture + alignment coloring, the commons machinery (the town
watching its own granary burn). MINTS: one defense-doctrine act — the
seat destroys own stores/infrastructure ahead of a believed conquest,
denying the invader supply (the invader's K_food inheritance arrives
ruined — 1812's answer to conquest's inheritance counterforce). Priced
brutally and honestly: the commons' grievance at the seat (their bread
burned by their own lord), the interior strain, the recovery bill — a
desperation verb, not a routine one (the trigger bar sits at
believed-overwhelming threat). Alignment: lawfulness01 raises the bar
(order resists burning its own); the desperate chaotic seat reaches it
first; malice01 declared empty — this is desperation, not cruelty, and
reading it as appetite would cheapen both. **Slots (deltas):**
force/counterforce — the denial of supply ↔ the commons' grievance at their
own lord, the interior strain, and the recovery bill, all off the same
destruction record. Posture: the bar reads `postureOf`/`riskToleranceOf`
directly — this IS a posture verb. Clock: the act is same-tick and own-
court; the invader's ruined inheritance is discovered at ARRIVAL, and the
world hears of it at news speed. Lifecycle: the destruction lands through
WR-8's EXISTING conserved destruction accounting (no new physics, no new
ledger); the commons grievance rides the existing incident writer;
round-trip/regen/undo carry both with their owners. Dossier round-trip: the
burned granary renders on the settlement panel and the act on the war
timeline — the town can read what its own lord did. Couplings: CW-0
war×interior (the commons at the fire), war×war (the invader's
inheritance). Pins: conservation holds through self-destruction (the WR-8
balance, inward); the commons grievance mints; the invader's inheritance
measurably ruined; UNREACHABLE below the desperation bar (negative
hardest).

### XW-8 — THE GUARANTEE + THE THIRD-PARTY COMPACT CLASS (owning volume:
### GRAMMAR; rides `covertTermsEnabled`'s sibling flag `thirdPartyTermsEnabled`)

RIDES: embargo_compact's third-party-reference SHAPE (TB-4 mints it first —
this wave generalizes the shape into a CLOSED class, one reference
discipline, one walker), GR-6's mediation (the broker who GUARANTEES what
they brokered — the natural producer), the breach shell, the join/threat
reads. MINTS: the third-party term class, closed at THREE rows in v1. **ALL THREE
ARE SECURITY-FAMILY ROWS AND THEIR EXECUTOR KINDS ARE ASSIGNED IN §4's
TABLE (chair, 2026-08-03):** `embargo_compact` and `neutralization_compact`
take the REQUESTED `forbearance` kind (both are pledges of RESTRAINT —
negative obligations), while `treaty_guarantee` takes **the kind the BUILT
`mutual_defense` row already carries** — a pledged response whose discharge
is read at the event — which is a POINTER to a built kind, not a fourth
executor-kind request; Sol names it off the catalog at slice start and the
brief records the name it found. The rows:
`embargo_compact` (TB-4's, adopted into the class) · `treaty_guarantee`
(the guarantor pledges response to the breach of ANOTHER pair's instrument
— producer: the mediator-guarantor pipeline + the peacetime offer;
consumer: the breach event fires the guarantor's obligation, whose
discharge or default rides CV-1's machinery whole — Belgium 1914, "the
scrap of paper": the guarantor dragged by a half-forgotten signature, or
defaulting and paying the betrayal market) · `neutralization_compact` (two
rivals jointly pledge restraint toward a named buffer — producer: the
mutual shared-threat read of EACH OTHER; consumer: the restrained verbs
read it; the partition understanding is its dark composition with WR-10
swaps). THE CONCERT EMERGES: a dense post-war web of guarantees + NAPs
damping wars for a generation — measurable as a peace era (SP-8 reads the
climate; no concert object, ever). MFN stays DEFERRED by the tombstone
(no treaty-comparison consumer exists; recorded). Alignment: lawfulness01
colors guarantee fidelity (the lawful march for the scrap of paper);
the watchers judge the defaulted guarantor hardest of all (a guarantee is
the loudest promise). **THE THIRD-PARTY FIELD IS TB-4's, ADOPTED WHOLE
(R-D):** this class rides `target?: settlementId` and the family ×
beneficiary × target stacking key — one reference discipline, one walker,
no second schema; the destroyed-target lapse rule and the two-id
`observerReach` monitoring rule apply unchanged (the target id is
classification, never a reach requirement). **Slots (deltas):**
force/counterforce — the guarantee's deterrence ↔ the dragged guarantor's
own war and the defaulter's market price, one record. Posture: the
guarantor's `postureOf` prices marching for the scrap of paper. Clock: the
obligation fires on the BREACH EVENT as learned — news-carried, hop-priced
(a guarantor honours what it has heard of), `INTERVAL_WEEKS` denominated;
the concert's peace era is an SP-8 CLIMATE read over time, not an object.
Lifecycle: all three rows are TermRecords with `target` — round-trip,
regen/undo, import-validated, destroyed-target lapse with receipt; the
discharge/default of a fired guarantee rides CV-1's machinery whole and
inherits its lifecycle. Dossier round-trip: guarantees render on BOTH the
guarantor's and the referenced pair's treaty pages — the "who is on the
hook for this peace" view. Couplings: CW-0 war×grammar; the concert (a
dense web damping wars) is a cascade and registers with CW-1. Pins: a
guarantee fires on its referenced pair's
breach (fixture); the dragged-guarantor arc walkable; the defaulted
guarantor prices into CV-4 at top band; neutralization restrains on a
fixture; the class walker asserts closure at three.

## §5 EMERGENT DYNAMICS — DOCUMENTED, NOT BUILT (implementing one is a STOP-and-report)

**The following are NOT built; they FALL OUT, and THE MEASURE WAVE PROVES
THEM** (TB-7 is obligated to cover them — "documented" here is a promise of
measurement, not a shrug): the separate peace that re-concentrates the war
(freed resources re-read remaining edges); the jackal coalition
(opportunism against both exhausted sides + `shared_threat` binding the
jackals — GR-2 running in wartime); the counter-coalition against the
over-winner (the winner's own rising shared-threat score); convergence
(multi-front resource contention accelerates every termination read; **and
WR-9's duration envelope reds a pathological stable cycle AS A TUNING
FAILURE** — that is what the red MEANS, and it is never a trigger for an
in-engine clamp, J-TR-11's discipline). A NEW coalition entering is NOT a
side-switch and needs no record beyond its own formations — CV-2's shape
applies only to a member crossing between existing webs.
TRADE AS THE PROXY FRONT — confirmed integrated end to end: TR-1's mirrored
casus commercii as the reasons layer, tradeWar + the corner as the contest
engines, **{severance, cornered, collapse}** as the war-shaped endings, ONE
disciplined crossing (the intent deposit, no gate relief, per WR-0c(3) +
J-TR-2) — **and the PROXY SPECTRUM (the mercantile disposition channel, the
merchant seat's war-averse levers, the blockade↔embargo continuum, the
containment demand doctrine, and belief-lane economic deception) RUNS
TARIFF-TO-SIEGE WITH NO SEAM.** That enumeration is a ruled finding of the
owner question of 2026-08-02, not decoration: it is the evidence for the
no-seam claim, and deleting it deletes the proof. TWO DELIBERATE ABSENCES:
(1) NO ECONOMIC-CAMPAIGN OBJECT — the sustained squeeze EMERGES from
standing grievances driving individually priced verbs, exactly as
coalitions emerge from edges; (2) THE CROWN CANNOT COMMAND THE HOUSE —
proxy war through houses happens by INTEREST ALIGNMENT (the crown's war
makes the corner profitable; the house follows the dear bands) —
deniability by emergence, Law One's grain separation absolute.
Implementing either "absence" is a STOP-and-report.

**THE WAR-PARALLEL KIT IS HEREBY COMPLETE AND AUDITABLE IN ONE LINE** (the
document's own closure claim for the economic-statecraft half — restored
because two of its members exist nowhere else in this volume): reasons
(TR-1) · contests (tradeWar, the corner) · coalitions (embargo webs) ·
**envoys (factors)** · **intelligence (believed markets, the lure)** ·
tribute (TB-1, TB-3) · termination (`severance` as TRADE's ending key,
renegotiation) · healing (TB-LAW-7 + the olive branch) · the deal itself
(the inequity read) · proxy discipline (this section).

**THE STORY CANON (named-emergent — each becomes a fixture + a Herald pool
at build; no mechanism, all composition):** THE DANEGELD LOOP (tribute
paid to the extorter teaches extortion — the payee's outcome-learned
appetite reads the payment as a win and returns; VERIFY-AT-BUILD the
learning wire, then pin it) · THE LEAGUE DRIFT (defensive contributions
renegotiated-from-strength by the hegemon, ossifying into tribute — the
Delian League into the Athenian Empire) · THE RECOGNITION GAMBIT (a third
party's pact with a contested satellite IS recognition — France 1778; the
parent's grievance mints off the pairwise act itself) · THE NEUTRAL'S
CARGO (a blockader's seizure of a third party's caravan mints the
neutral's grievance — the war spreads through trade; 1812) · THE LEAKY
EMBARGO (exclusion is pairwise, so the embargoed buys through a third
market; the Continental System's death by re-export, the smugglers' road
its retail arm) · THE PRETENDER'S WAR (the harbored émigré as legitimacy
cover for the host's invasion; the restored puppet who repudiates the
restorer) · THE OPIUM CYCLE (contraband dispute → trade war → real war →
extractive treaty → a century of memory → revanchism — the whole coin,
one arc) · THE INTERDICT (the temple's mandate-withdrawal as domestic
sanction — divine mandate machinery pointed at the seat) · THE
STAB-IN-THE-BACK (post-defeat blame redirection riding the witch-hunt and
legitimacy lanes) · THE CRUSADE (sacred claims aligning across many pairs
— a web that feels called) · THE MELIAN REFUSAL (the pragmatic override
declined — strength demands, the weak choose pride, the price is paid).

**THE HISTORY SHELF (archetype → machinery, for Sol's fixtures and the
Herald's registers):** Belgium 1914 → the guarantee ("the scrap of paper")
· the continental subsidies → XW-4 · Egypt 1876/Suez → XW-5's debt trap ·
Molotov-Ribbentrop / the Tsar's published treaties / Zimmermann → XW-1 ·
the Ems Dispatch → XW-2 · Sparta-and-Athens → XW-3 · 1812 Russia → XW-7 ·
the Marshall Plan → the reconstructive doctrine (TB-2's fourth goal:
{punitive, containment, acquisitive, RECONSTRUCTIVE} — the victor drafts
BELOW capacity and invests, betting on the healing lattice and the
prosperity index to convert enemy into market into ally; counterforce:
Weimar's shadow — the revanchist who takes the aid and rearms, priced by
the mistrusted-gift arm) · the Congress of Vienna → the guarantee-web
concert as a measurable peace era (SP-8's climate reads it) · the
Anglo-Portuguese alliance → co-belligerence healing compounding across
centuries.

## §6 THE ALIGNMENT ENGAGEMENT MAP (requirement 13, consolidated — every
## row read-side, capped, colors-never-selects, from the derived
## alignmentOf; no row stores or writes an axis)

| Mechanism | lawfulness01 colors | malice01 colors |
|---|---|---|
| Exit choice (CV-1) | settlement-vs-default price sensitivity | — (declared empty WITH reason: quitting a coalition is a PRICE decision, not an appetite one — malice reads targets and opportunities, and there is no target in walking away; the malice engagement over a quitter's weakness is the NEXT row's, where it belongs) |
| Betrayal willingness (CV-2/3) | oath-breach price (face/credibility weight) | opportunism's read of a friend's weakness |
| Join pricing (CV-4) | how heavily priors weigh for the lawful joiner | — (consequences never discount — CT-LAW-4) |
| Demand doctrine (TB-2) | punitive-weight tolerance (the lawful cannot stomach what they cannot justify) | punitive-weight appetite |
| The family + schema (TB-1) | — | — (declared alignment-empty: catalog and schema work opens no decision surface; the appetite for what the rows carry is coloured at TB-2/TB-3/TB-6) |
| Faith terms (TB-1b) | honouring the establishment term | — (the faith-side colouring is the QUADRANT TABLES' own lane, not re-authored here) |
| Extraction extremity (TB-3) | — (declared empty WITH reason — swept in with the six, same defect class: the oath-price axis has no purchase on how MUCH a victor asks; what the lawful court's restraint touches is the punitive weight at TB-2 and the honouring of terms once signed, both of which are their own rows) | the `all`-band ask appetite — still extremity-gated, AND still inside R2's evil-exclusive initiation boundary (TB-LAW-3: WR-8 owns the one place alignment is a boundary; this row is the weight, not the boundary) |
| Population levy, WRITE side (TB-3) | — | — (write-side engagement DECLARED: the levy moves both towns' constituents, and therefore their derived axes, THROUGH THE EXISTING POP/roster writers only — no settlement-alignment writer anywhere) |
| Embargo keeping (TB-4) | compact fidelity under temptation | punitive-refusal weight |
| The override bar (TB-5) | formal-instrument preference | low malice RAISES the bar against profiting from the butcher |
| Extractive drafting (TB-6) | — (declared empty: a deal freely signed gives the oath-price axis no purchase) | the extractive-band appetite |
| Healing rate (TB-LAW-7) | the affinity term reads BOTH axes symmetrically — near-quadrant pairs heal faster | the same affinity term, same read (one term, two axes — this row is symmetric BY CONSTRUCTION, which is why neither column is dashed) |
| Strain + measure (TB-7) | — | — (declared alignment-empty WITH reason: the strain arithmetic is physics × belief; the alignment engagement over extraction is THE WATCHERS' row below, which is per-observer) |
| Covert terms (XW-1) | willingness to sign in the dark (the lawful prefer the open instrument) | — (declared empty WITH reason: secrecy is a PROCEDURE, not a cruelty — what the covert term CARRIES is coloured on the row that owns it, and colouring the wrapper too would double-count one appetite) |
| The wedge (XW-2) | — (declared empty WITH reason: the oath-price axis has no purchase on a lie told to a third party — no oath is broken by planting a belief, and the lawful court's restraint here is already priced as the commissioning appetite it does not have) | commissioning appetite |
| Preventive pressure (XW-3) | damps it (the lawful need the grievance, not the forecast) | feeds it |
| Subsidy fidelity (XW-4) | honoring through a losing war | — (declared empty WITH reason, and XW-4's own body says the same: paying a friend is not an appetite the malice axis reads; the predatory version of paying is XW-5's loan, which IS coloured) |
| Loan drafting/repayment (XW-5) | repayment fidelity | predatory-terms appetite |
| Scorched earth (XW-7) | raises the desperation bar | — (declared empty WITH reason, and XW-7's own body says the same: this is DESPERATION, not cruelty, and reading it as appetite would cheapen both) |
| Guarantee fidelity (XW-8) | the lawful march for the scrap of paper | — (declared empty WITH reason: honouring or abandoning a guarantee is an oath question end to end; the malice engagement over what a guarantee ENABLES is priced on the join and threat rows, and a second colouring here would double-count it) |
| Arbitration fidelity + the ultimatum stance (XW-6) | honouring an ACCEPTED arbitration (the lawful keep the finding they lost) | — (declared empty WITH reason, and XW-6's own body says the same: submitting to a finder is not an appetite; the ultimatum's DELIVERY is priced by posture, not by malice) |
| Reconstructive weight (TB-2) | long lawfulness raises it | low malice raises it |
| The watchers (throughout) | every observer judges acts on ITS OWN axes — the world's judgment is per-observer, per the standing law | same |

## §7 TUNING SURFACE (owner-signed at the soak; no bare floats)

**READ THE COLUMNS LITERALLY.** *Wave* is SCHEDULABLE — every row names the
commit that lands it, never a section or a discipline. *Kind* declares
whether the row tunes a VOLUME-LOCAL band (this document's own closed
vocabulary, declared local per SP-6's standing rule 2), an ASSIGNMENT of
kinds to a spine family's classes (SP-6a/SP-6b — **this document authors NO
severity or significance SCALE**, per SP-6's ruling R5), or a CURVE/CAP on
a read-side weight.

| Band | Kind | Wave |
|---|---|---|
| Share ladder thresholds {tithe, quarter, half, lions_share, all} | volume-local (declared) | TB-1 |
| Basis cruelty multipliers | volume-local (declared) | TB-3 |
| Goal-weight caps + derivation curves (incl. the reconstructive weight) | curve/cap | TB-2 |
| believedInjustice curve | curve | TB-7 |
| Healing-rate curve (the INVERSE scaling against the standing resentment band) + per-reducer strengths, with co-belligerence ranked strongest | curve | TB-LAW-7, landing with TB-7 |
| Saturation band + hysteresis width; the burial-before-saturation composition order's phase-in | volume-local (declared) | TB-LAW-7, landing with TB-7 |
| D5 lifespan-band multipliers for every clock in this document (healing, resentment, betrayal history) | **REFERENCE ONLY — the EXISTING D5 table; no band is authored here** | TB-7 / CV-4 |
| Inequity band edges {boon, fair, lean, extractive} | volume-local (declared) | TB-6 |
| Override gain-threshold curve + commons-price bands | curve | TB-5 |
| `rapprochement` scorer thresholds (the healing calculus × books) | curve | TB-5 |
| Adequacy-gap ladder (assignments against WR-6's adequacy band — no second band family) | SP-6b assignment | CV-1 |
| Betrayal severity classes | SP-6b assignment | CV-2 |
| Betrayal decay half-life BAND (drawn from the D5 table) | reference + assignment | CV-4 |
| malice01 weight curve (capped) — threshold-side | curve/cap | CV-3 |
| lawfulness01 weight curve (capped) — extending the built `climbDownConsequence` face path | curve/cap | CV-3 |
| Cold-edge, exit, and turncoat receipt significance classes | SP-6a assignment | CV-1, CV-2 |
| Doctrine receipt significance classes (four families incl. "as a bridge") | SP-6a assignment | TB-2 |
| **Embargo compact, named-refusal, and caught-defection receipt significance classes** | SP-6a assignment | TB-4 |
| **Route-restriction refusal + charter-blocked receipt classes** | SP-6a assignment | TB-4 |
| Olive-branch and spurned-branch receipt significance classes | SP-6a assignment | TB-5 |
| Inequity band-crossing receipt significance classes | SP-6a assignment | TB-6 |
| **Faith wing: `cult_establishment` standing band {minor, major} + institution tier** | volume-local (declared) | TB-1b |
| **Faith wing: `patron_imposition` margin gate (extremity-adjacent, beside the lion's share)** | curve/cap | TB-1b |
| **Faith wing: establishment, imposition, backlash, and resurgence receipt classes** | SP-6a assignment | TB-1b |
| Subsidy adequacy assignment (CV-1's band REUSED, never a second read) | SP-6b assignment | XW-4 |
| Loan phase durations + predatory-terms drafting caps | curve/cap | XW-5 |
| Preventive-pressure weight cap (below any single live casus) | curve/cap | XW-3 |
| **Covert-signing drafting weight cap (lawfulness01 colouring willingness to sign in the dark) + exposed-protocol receipt classes** | curve/cap + SP-6a assignment | XW-1 |
| **Wedge commissioning-appetite weight cap + the plant's decay on the D5-scaled memory band (BAND REFERENCED from the D5 table, never authored) + plant/exposure receipt classes** | curve/cap + reference + SP-6a assignment | XW-2 |
| **Ultimatum-stance receipt class + arbitration-repudiation severity at treaty grade** | SP-6a / SP-6b assignment | XW-6 |
| Scorched-earth desperation bar | curve/cap | XW-7 (owner-gated) |
| Guarantee/neutralization fidelity + default severity classes | SP-6a/6b assignment | XW-8 |
| Tribute lifecycle mix + goose + renewal-chain horizon + embargo-web + inequity-mix envelopes (each with its mutant control) | envelope (SP-6b grading axis referenced) | TB-7 |

## §8 JUDGMENT BLOCKS (the chair's rulings under delegation — each vetoable)

**EVERY RULING CARRIES ITS VETO CONSEQUENCE.** A delegated ruling is only
vetoable with informed consent if the owner can see what accepting the veto
COSTS; the consequence clause is that price, and it is restored on every
row (R-K).

- **J-CT-1** Tribute is a term family, never a client-state edge — VETO
  builds the patron/client relationship object and accepts the second
  relationship plane.
- **J-CT-2** No viability clamp — extraction may kill the goose; the
  consequences answer — VETO adds a floor and accepts the clamp against
  law L. *(Amended by J-CT-25: the BUILT reserve floor is not this
  ruling's to remove.)*
- **J-CT-3** Totality demands extremity, in any basis — VETO frees the
  `all` band and accepts routine economic razing.
- **J-CT-4** Prosperity is an index, never a medium — VETO mints a
  transferable prosperity stock (and violates never-store-a-derivable).
- **J-CT-5** The population levy is a flow, never a sink — J-POP-10
  absolute on its declared P2 lane — VETO is owner-only (it prices deaths).
- **J-CT-6** The exit vocabulary is closed at three — VETO widens it (each
  new exit class needs its own records, receipts, and pins, and a partial
  settler already bands inside `exit_defaulted`).
- **J-CT-7** No dice-roll betrayal: the erosion law stands — VETO permits
  healthy-tie treachery and accepts unnarratable knives.
- **J-CT-8** Alignment colors willingness, never consequences — VETO lets
  malice discount the material bill (and mints a free-evil ratchet).
- **J-CT-9** The demand doctrine is derived, never player-chosen in v1 — a
  DM-proposals lane over the goal mix is DEFERRED, recorded (§9) — VETO
  ships the picker and accepts a menu where a character should be.
- **J-CT-10** The justice discount is belief-side and manipulable through
  FOREIGN lanes only — VETO makes it a truth read (and deletes the
  information-war stories) or opens the seat-to-crowd lie CPL-20 forbids.
- **J-CT-11** `trade_cap` stays cut until a wave names its producer AND its
  biting consumer in one stroke — VETO ships an unbitten token and accepts
  orphan vocabulary in the canonical catalog.
- **J-CT-12** The embargo bloc is a web, never an object (CT-LAW-1's
  commercial twin) — VETO builds the bloc object and accepts the
  coalition-object mistake in commercial dress.
- **J-CT-13** The override never vetoes and is never free (a gain-cleared
  hostile deal ALWAYS receipts and ALWAYS prices the other ledgers) — VETO
  makes resentment a hard veto and deletes the pragmatism stories.
- **J-CT-14** The inequity read is derived, never stored — **AMENDED by
  J-CT-26 to PER-OBSERVER** — VETO restores a single merged per-instrument
  band and accepts a merged estimate across a court boundary (K4/C-LAW-5).
- **J-CT-15** Covert terms ride the ONE public instrument (existence loud,
  terms scoped) — VETO permits a second secret instrument and breaks R1.
- **J-CT-16** The wedge colors, never mints (J-INF-9 absolute) — VETO
  grants lies a casus and breaks the taxonomy's honesty.
- **J-CT-17** Preventive weight caps below any single live casus — VETO
  frees it and accepts forecast wars.
- **J-CT-18** The subsidy's compliance is CV-1's adequacy band REUSED,
  never a second read — VETO mints a parallel compliance band and accepts
  two ladders measuring one discharge.
- **J-CT-19** Scorched earth is specced-complete but OWNER-GATED to build
  (it destroys own state) — VETO here is the owner's SIGN-OFF, which is the
  only thing that starts the build; declining costs nothing but the wave.
- **J-CT-20** The third-party term class is closed at three; MFN deferred
  by the tombstone — VETO widens the class (each member needs its own
  reference discipline and walker) or ships MFN with no comparison consumer.
- **J-CT-21** The reconstructive goal joins the doctrine as DERIVED weight,
  never a menu option — VETO makes the four goals a picker and accepts the
  menu J-CT-9 refused.
- **J-CT-22 (R-A)** The closed formation-trigger set widens by EXACTLY ONE
  token, `rapprochement`, with scorer, reachability pin, import validation,
  and its GR-7 envelope cell; everything else rides existing tokens
  (economic threats widen `shared_threat`'s EVIDENCE; the weak offer, the
  subsidy, and the loan are SHEET COMPOSITION; remission is an `amended`
  act) — VETO either re-opens the closed set with unscored arms (the exact
  defect GR-2 struck "post-rivalry détente" for) or refuses the token and
  deletes the olive branch's producer, leaving the deliberate door with no
  way to open.
- **J-CT-23 (R-B)** The tribute bases join GRAMMAR's frozen COMPOSABLE list
  by ruling, with the walker updated in the adopting commit; third-party
  terms key their stacking cell by family × beneficiary × target — VETO
  leaves the second basis of every mixed sheet REFUSED with
  `term_refused_stacking`, which unbuilds TB-2's whole demand doctrine, the
  multi-target embargo web, and multi-party route restriction at once.
- **J-CT-24 (R-D)** TermRecord gains `target?: settlementId` for the
  third-party class ONLY, declared in the beneficiary precedent's exact
  shape, with a JSON-round-trip pin and a destroyed-target lapse receipt;
  monitoring stays two-id through `observerReach` and the target id is
  classification, never reach — VETO makes both TB-4 rows unrepresentable
  (or, worse, smuggles a third-party reach read past law I).
- **J-CT-25 (R-J)** TB-1 SUBSUMES the built economic family (`tribute_goods`
  IS the built `tribute` row; the share basis EXTENDS `resource_share`;
  only three rows are new), and THE BUILT RESERVE FLOOR STANDS above the
  no-clamp law — **re-ruled 2026-08-03: it stands as PHYSICS AT PAYMENT
  TIME, and there is NO drafting-side floor test anywhere (TB-LAW-4); a
  sheet may promise beyond reserve and pay short, receipted** — VETO mints
  duplicate catalog rows over live ones and/or
  removes a landed mercy the estate already ships, and takes the goose
  envelope's reachability with it.
- **J-CT-26 (R-E)** The inequity read is PER-OBSERVER — each side's own
  felt band from its own valuation and its BELIEVED counterpart surplus;
  the objective comparison lives only in TB-7's gate-side instrumentation;
  the persisted signature valuations are the THIRD disclosed exception to
  never-store-a-derivable — VETO keeps the merged estimate and accepts a
  K4/C-LAW-5 violation at the heart of the healing rate.
- **J-CT-27 (R-G)** Tribute strain lands OUTWARD and on the watchers only;
  the paying seat's commons are INT-4's lane, consumed by pointer under
  INT-4's cap, with the non-stream bases coordinated through INT-4's own
  receipts path — VETO writes a second tribute→commons term and accepts
  J-INT-3's named double-count on one drain.
- **J-CT-28 (R-H)** Every clock here adopts the D5 lifespan-band family
  (both signs, same multiplier); the six reducers are READ-SIDE modifiers
  with no new stock and no new writer; the burial discount composes BEFORE
  the saturation gate; charity deposits nothing while tribute stands
  (`gratitudeBonds.js:23`) — VETO runs healing on a human clock over a
  band-scaled wound (INT-5's anti-ratchet pin's exact failure), or mints a
  second writer on a single-writer stock, or leaves a saturated pair
  unreachable by the burial the design promises.
- **J-CT-29 (R-I)** `exit_settled` records `coalition_debt_paid` and never
  fabricates `debt_forgiven`; CV-1's exits map READ-ONLY onto momentum.js's
  existing inputs; the cold edge is a STATE CENSUS and WR-9's endings mix is
  untouched; CV-4's history lands on the JOIN BAR, not on the enemy-web
  risk read; CV-3's lawfulness weight EXTENDS the built
  `climbDownConsequence` path — VETO fabricates forgiveness from payment,
  or edits a FOREIGN file, or grades a non-ending in a closed mix, or
  inverts CT-LAW-4 by discouraging the traitor instead of repricing it, or
  double-counts the face term J-WR-11 already warned about.
- **J-CT-30 (R-L, owner addendum)** Treaties may carry RELIGIOUS
  consideration as INSTITUTION and PRESENCE only — `cult_establishment` at
  a banded standing and `patron_imposition` gated at high margin (war door)
  or offered as the weak party's submission (peacetime); the
  conversion-mandate prohibition is re-affirmed and no term ever delivers
  believer share or conviction — VETO either closes the faith door the
  owner opened, or opens it too far and lets a table move a soul, which
  J-GR-12/J-CPL-6 forbid absolutely.
- **J-CT-31 (residual repair, 2026-08-03)** DURATION HAS TWO SOURCES, ONE
  PER DOOR: the landed margin derivation at the WAR door, and THE DEMAND'S
  MEASURE — the trigger's own read — at the PEACETIME door, per GRAMMAR
  §1c's no-margin-in-peace law — VETO either leaves the weak settlement's
  offer with NO lawful duration source at all (the state the document was
  in), or reaches the war-door formula through a synthetic margin, which is
  the branch J-GR-2 already named as its VETO case.
- **J-CT-32 (residual repair, 2026-08-03)** TB-LAW-4 RE-RULED: there is NO
  drafting-side floor test anywhere — a sheet may promise beyond reserve —
  and the built reserve floor is PHYSICS AT PAYMENT TIME (shortfalls pay
  short, receipted, read as strained compliance), which is conservation and
  not a clamp — VETO restores the self-contradiction (forbidding a drafting
  clamp and imposing a drafting-side bound in one law), which forces Sol to
  either build the forbidden clamp or treat a stated rule as unenforceable
  prose; and building it would put a viability test back on the drafting
  path that law L and J-CT-2 both refuse.
- **J-CT-33 (residual repair, 2026-08-03)** THE CLOSURE CONTRACT IS PAID,
  NOT MERELY STATED: every row this document requests carries a family and
  an executor kind in §4's assignment table (`tribute_service` and
  `tribute_population` are POPULATION-family rows, the faith wing takes
  `grant`, the subsidy is ECONOMIC not security), and the THREE executor
  kinds this needs — `forbearance`, the receipted-act kind, the
  flow-minting kind — travel as ONE J-GR-15 instrument with a declared
  fallback each — VETO sends Sol to GRAMMAR with rows that cannot be
  adopted (a request missing a family or an executor kind does not close
  by the document's own contract), or splits one adoption instrument into
  three trickling asks and re-opens the frozen catalog three times.
- **J-CT-34 (residual repair, 2026-08-03)** THE SCOPED PRECONDITIONS AND
  ATTRIBUTIONS: TR-6 (`corneringEnabled`) lights ONE ARM of TB-4's
  producer, with the degraded arm declared and pinned rather than
  discovered; CPL-12's general "push/pull/flight physics only" clause is
  SCOPED so the levy sits inside `expulsion`'s physics rather than beside
  them (recorded on both sides, as CPL-17's override was); and
  `faithTermsEnabled` is FAITH's WF-6 flag consumed as a LIT-PRECONDITION,
  never one of this section's own — VETO either blocks TB-4 behind a TRADE
  wave it does not need, or ships a producer whose named evidence does not
  exist at build time; leaves an unaddressed declared-empty that C-LAW-8
  makes a STOP-and-report; and registers a duplicate certification row
  against a flag another volume owns (or leaves TB-1b with no flag at all).

## §9 WHAT IS DELIBERATELY NOT BUILT

**EACH ITEM CARRIES THE LAW OR RULING THAT FORBIDS IT** — so a reader can
tell constitutional absences from this document's own chair rulings (R-K).

No coalition/bloc object (CT-LAW-1) · no N-party table (law I) · no
client-state edge (J-CT-1) · no timers or clamps anywhere (law L,
TB-LAW-4 — and no SECOND floor beside the built one, J-CT-25, **and no
DRAFTING-TIME read of the built one either: the drafting path imports no
reserve read, source-scan pinned**) · no coin
(T2) · **no hostage-surety term (CPL-17's declared-empty stands — the half
of that empty the owner did NOT override, and it is still binding)** · no
forced-march mortality (J-CT-5, on the declared P2 lane; M4's own sinks are
untouched and not ours) · no DM doctrine picker in v1 (J-CT-9) · no new
betrayal casus (J-WR-2's compilation discipline: debt-fed grievance mass +
the existing taxonomy suffice — a `betrayal` pair would double-count the
grievance lane and cost the walker's bijection) · no economic-campaign
object (§5's first deliberate absence, ruled at the owner question of
2026-08-02) · no crown-commands-house verb (§5's second; Law One's grain
separation) · no `trade_cap` (J-CT-11, the tombstone applied forward) ·
no stored deal-quality field (never-store-a-derivable; J-CT-14/J-CT-26 —
the SIGNATURE VALUATIONS are stored, disclosed as the third exception, and
the BAND is not) · **no engine read that compares both parties' pictures
(K4/C-LAW-5, J-CT-26 — the only two-sided comparison lives in TB-7's
soak-side harness)** · **no seat-to-crowd deception lane (CPL-20's
declared-empty, J-CPL-9 — the seat never lies to its own commons)** ·
**no read built in the seam between volumes (C-LAW-2 — every new read is
built in its owning volume's wave with its own pins)** · **no edit to
`momentum.js` (C2's foreign-file law; §10.1)** · **no second tribute→
commons write (J-INT-3, J-CT-27)** · **no term that delivers believer share
or conviction (J-GR-12/J-CPL-6, re-affirmed at TB-1b)** · **no binding deal
outside the one instrument (R1 — the "quiet arrangement" is a disposition,
never an instrument)** · **no authored severity or significance SCALE
(SP-6's ruling R5 — this document assigns classes to spine families and
never mints a ladder)** · no "redirect supplies" verb (law I) · no mercenary
companies and no chartered-company war (a house never owns a war lane —
Law One's grain; the subsidy is hired fighting at realm grain, the only
lawful form) · no MFN until a treaty-comparison consumer exists · no
forged events (the wedge plants beliefs about intent, never records of
deeds) · no congress/concert object (the concert is a web + an era read).

## §10 SEQUENCING + CHAIR CHECKPOINTS

**LIT-PRECONDITIONS LIVE HERE (the §4 header points at this section, not at
§9 — the earlier pointer was broken). Stated as LIT, not merely landed: a
flag lit out of this order is an invalid config the certification walker
reds, and every new flag ships its own certification row.**

Order: CV-1→2→3→4 after WR-6 + WR-7 LIT (CV-3 additionally requires
`warTerminationEnabled` LIT — it reaches WR-1's read); **TB-1→2 after GR-3
LIT (+ WR-8's drafting lens for the war-end door) — CORRECTED: WR-10 is NO
LONGER A PRECONDITION.** WR-10 is the sovereignty market and closes the war
program; sequencing the foundational catalog wave behind the last war wave
was an ordering error. WR-10's bundle GROWS the family later, as
composition. TB-1b after TB-1 + WF-6's door (`faithTermsEnabled` LIT, GR-3's
faith rows landed). TB-3 after TR-4 (the conservation seam) + POP-1/POP-3
for the population basis, with **POP-5b LIT for the permit in-road** and
`departureMemoryEnabled` + `believedMigrationEnabled` LIT for the memory
ledger and the arrival clearing; TB-5's deliberate-door parity additionally
needs `deliberateForgivenessEnabled` LIT (the burial verb, suppression
reads, the dig-up) and the reconciliation machinery's `reframeEnabled`
(+ `peaceCausalActive` for the casus consumers). TB-4 after TR-1 + GR-3,
**and now additionally after TR-3 (the dispatch WHERE surface) and TR-5
(`trade_exclusivity`/`market_access`)** — all four LIT — with its
consumption-surface VERIFY-AT-BUILD resolving at slice start and
`routeLifecycleEnabled`'s state declared (dark ⇒ the charter arm records an
honest permanent zero); **and TR-6 (`corneringEnabled`) is a lit-
precondition OF ONE ARM ONLY — the corner-monopolist evidence of the
economic shared-threat read. TB-4 does NOT wait on TR-6: with TR-6 dark the
read runs its DECLARED DEGRADED ARM on dependency and exclusivity evidence
alone, and the trigger receipt names which evidence it had (absent, not
zero). Both arms pinned at TB-4.**
TB-5 after GR-2 (carrying `rapprochement`) + the healing substrate; TB-6
after SP-3's two-picture persistence (VERIFY-AT-BUILD; fallback: TB-6 lands
the persistence as its FIRST SLICE, disclosed as the third exception to
never-store-a-derivable); TB-7 with the WR-9 harness (and INT-4 lit before
the king-who-paid arc can be read through its lane;
`economicCoupReadEnabled` is the owner's lighting order, not ours).
THE EXPANSION WAVES — **STATED AS LIT, NOT MERELY LANDED (chair,
2026-08-03). §4-EXP's own section-wide totality obliges every XW wave to
carry "lit-preconditions stated as LIT (not merely landed) and the lighting
order enforced by the certification walker"; this block stated ORDERING
while the CV and TB blocks above stated LIT, so the section was holding
itself to two standards in one place. The order below is enforced by the
certification walker exactly as the CV/TB order is, and a flag lit out of
it is an invalid config the walker reds:**
XW-4 (subsidy) after TB-1's catalog work LANDED with `tributeFamilyEnabled`
and `coalitionConvenienceEnabled` LIT (it reuses CV-1's adequacy band and
WR-6's join read, and neither reads anything while dark) · XW-5 (loan)
after TB-1's catalog work LANDED with `tributeFamilyEnabled` LIT · XW-6
after GR-6 LIT (nothing can bind to a finder's sheet before the mediation
lane is lit) · XW-3 after WR-1 LANDED and `warTerminationEnabled` LIT (it
colours the motive read that lives behind that flag) · XW-2 with IN-2's
build and `infoLureEnabled` LIT (its owning volume's wave and its owning
volume's flag) · XW-1 after GR-2 LIT + IN-0b LIT, carrying its own
`covertTermsEnabled` and its own certification row (the exposure paths must
be LIT, not merely landed — a covert term nobody can expose is
unfalsifiable) · XW-8 LAST of the grammar tail, after TB-4 LANDED and with
`thirdPartyTermsEnabled` LIT (it generalizes TB-4's third-party shape and
rides the mediation pipeline) · XW-7 specced now, BUILT only on the owner's
sign-off (J-CT-19) — its flag never lights without that signature. All dark, one commit
per slice, **THE WAR VOLUME's §10 IMPLEMENTER PROTOCOL VERBATIM** (the
header says so; this section is THIS document's sequencing and is not what
that phrase points at — the two are never the same §10), ledger rows, CW-0
registry rows with the full key set in the same commit as any cross-layer
read, and CW-1 cascade registrations per C-LAW-6.
CHECKPOINTS: TB-2's drafting-lens extension is a disclosed lit-path shift
(J-GR-14 precedent — golden plan reported before the commit) · **the five
VERIFY-AT-BUILD items are exactly §2's row, not a different list** — the
stream-executor shapes, the warReasons debt-feed spelling (CV-1 start), the
per-resource yield read plus the non-food conservation arm (TB-3 start —
report which arm), the dispatch consumption surface (TB-4 start), and the
two-picture signature persistence (TB-6 start) · every catalog request goes
through **J-GR-15's adoption instrument**, and its commit carries the
TERM_CATALOG freeze, the families walker, `TREATY_COMPLIANCE_VOICE`'s
totality rows, and the COMPOSABLE-list walker update together, or the
walkers red · every new receipt kind lands with its ≥4-variant pool (the
SP-6 floor) AND its WHAT_PHRASES + heraldRouting + rumor-voice +
chroniclersLetter registrations in the kind's own commit (the receipt-pool
corpus gains these kinds at build — the annexes do NOT yet carry them) ·
**the cross-volume amendments this document REQUESTS rather than makes are
THREE (recounted 2026-08-03), and all three are the cohesion pass's to
land, never the implementer's to assume — if any is declined, the declared
fallback in its wave stands: (1) INT-4's receipts-path arm for the
non-stream bases (R-G); (2) GRAMMAR's THREE EXECUTOR KINDS travelling as
ONE J-GR-15 instrument — `forbearance` for the restraint/compact rows, the
RECEIPTED-ACT kind for `tribute_service`, and the FLOW-MINTING kind for
`tribute_population` (§4's assignment table carries the fallbacks); (3)
COUPLINGS' CPL-12 SCOPING LINE, reconciling that declared-empty's general
"push/pull/flight physics only" clause with the levy's compulsion-context
column (TB-3), recorded on both sides exactly as the CPL-17 override was**
· **the substrate
census in §2 is re-derived from the tree before Sol picks up the CV/TB
block, and any line-anchored citation is re-verified or replaced with a
symbol anchor** · any conflict with the war volume, GRAMMAR's catalog
closure, TRADE's T-laws, INTERIOR/POPULATIONS' flow and memory laws,
COUPLINGS' declared-empties, or the spine is a STOP-and-report — and that
rule outranks this document's own cohesion paragraph.
