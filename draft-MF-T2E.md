# Town cartography / MF-T2E — `MassPartQ` / `SolidPartQ`: the typed functional-volume vocabulary (the quantized vertical truth D3a mints, in the exact shape the D1 legality predicate already admits)

- **Status:** DRAFT — compiled by lane TC-T2E (chair-tier COMPILE seat, ODQ §344.1, the §343.1(a)
  criterion named in the dispatching row: the scope determination is itself a boundary argument).
  ⛔ Nothing lands before the chair reviews this draft; compiling is preparation, not landing.
- **Packet version:** 1
- **Compile tip (pinned):** `claude/composite-r4` at `3ac279db4232cc65abb57c4fa016578efd0f695d`
  (the WF-1E landing; MF-T2C landed two commits down). Every repo read in this draft was taken by
  `git show`/`git grep` against that SHA. ⚠ **The DISPATCH base is unknowable from here** — TE-T2D
  is landing the boundary noder as this compiles, so the branch tip moves before any executor
  starts. Every base figure below is marked `STOP: RE-DERIVE AT BASE`.
- **Depends on:** `MF-T2A` (the single-declaration law — LANDED) and `MF-T2B` (the ABI wall,
  `requireCanonicalInt`'s threaded bounds, `deepFreezeCanonical`, `COORDINATE_ABI_VERSION` — LANDED
  at `cdfe5a96`). **No symbol of MF-T2C or MF-T2D is consumed** — those bind this member only
  through train order and the census-walker staged promotion (§7). MF-T2F is this member's named
  future READER, not a dependency.
- **Family preamble:** `docs/implementation/preambles/MF-PREAMBLE.md`, SHA-256
  `0706aad6a84e4f74ec47602e9e3222fc398c4c8864bb38c79ac480c2a11db4ed` — **computed from the file at
  the pinned tip by this compile** (`git show 3ac279db…:… | shasum -a 256`), matching the
  post-§324.5 stamp MF-T2C and the MF-T2D draft both record. ⛔ **The executor recomputes at ITS
  base before any edit** — the file was re-stamped twice on 2026-08-21 and a further re-stamp is
  possible; a mismatch is a STOP, not a shrug. Its §P1 refutations, §P2 hazard dispositions, §P3
  anchor preflight, §P3b, §P4 registration template, §P5 census law, §P6 mutant hygiene, §P7 STOP
  set and §P8 capsule law bind this packet and are not restated.
- **Collision group:** `d3a-port` — this member reserves ONE shared D3a change path,
  `tests/lint/sovereigntyLightingContract.walker.test.js`. ⭐ It deliberately reserves NO path on
  `src/domain/townMap/fabric/index.js` and NO path on any existing production file (§7). Staged
  promotion, never simultaneous with another non-terminal D3a member on the census path
  (`CR-HB2B-SPLITP`; preamble §P7.11). ⚠ At compile time MF-T2D is non-terminal on that same path;
  this packet is not promoted until MF-T2D reaches a terminal status.
- **Commit authority:** the executing lane commits on its own detached ref; the chair moves the branch.
- **Baseline posture** (all `STOP: RE-DERIVE AT BASE`; figures below are compile-tip measurements,
  stated so the executor can see a shear): no `townMap` path carries a `scripts/.size-baseline.json`
  entry and none is on the hot-file list; the plain `src/domain/**` ceiling is 800 effective under
  `max-lines {skipBlankLines, skipComments}`; the fabric single-declaration walker is green and the
  arriving export names collide with nothing (§4); the census tuple and both typecheck floors are
  re-derived at base, never inherited (`BASE_STATE.json` remains inadmissible per §P8 unless
  re-stamped at the dispatch base).

> **`censusAuthorization`:** this packet moves the test census by two credited files, seven titles
> and two suite titles (delta derived in §9; base tuple `STOP: RE-DERIVE AT BASE`). Its authorizing
> decisions are **ODQ §312** (the D3a port dispatch whose §312.1 roster names the §287.16 massing
> foundations as a D3a track) and **ODQ §344.1** (the chair's spawn of this compile: *"TC-T2E
> (chair-tier compile of the NEXT map member…)"*), plus a slot for the executing seat's own dispatch
> §, under the wave charter at **§310.4** and §299.4's binding-forward rule that a packet moving any
> census or ratchet names its authorizing decision in the packet body. The family's stamp is
> **GRANTED** at ODQ §312.2b, so `town-cartography` sits in the eight-member engine-train column and
> this member continues the OPEN train (A → B → C → D → **E**) rather than opening a new one.

> ⭐ **SCOPE DETERMINATION (the §344.1 question, answered).** The dispatching row framed the choice
> as *"between the fabric-extraction producer and the §312.2c dual-run retirement."* This compile
> examined both against the plan and the standing rulings and REFUSED BOTH as the next member,
> choosing the plan's own member 5 instead — full argument and evidence in `laneTCT2E-receipt.md`
> §2, compressed here because the packet must stand alone:
>
> 1. **The fabric-extraction producer is not a D3a member.** MF-T2D's accepted resolution
>    (J-TCT2D-3; ODQ §334.4) already places it: *"the fabric-extraction half does NOT port here —
>    those record kinds have no app-side producer and belong to the generator tranches."* Its input
>    is the sandbox fabric (channels/walls/water records) that only the ~9,900-effective-line
>    generator half produces, and `draft-D3A-PLAN.md` §2.2 fences that half OUT of D3a by name.
>    §290.4 forbids a compiler enlarging the tranche.
> 2. **The §312.2c dual-run retirement act cannot fire yet, and it is not a port member.** §312.2c's
>    own text: retirement fires *"when the DUAL-RUN EQUIVALENCE proves the app-side generator
>    subsumes the sandbox"* — and the app-side generator does not exist until the generator tranches
>    port. An act compiled now is a gate without its subject. It is also a different SHAPE: a
>    read-only program measurement plus, later, a retirement packet with `retiredSymbols` rows —
>    not a `PACKET_STANDARD` port member with a production leaf. RAISED-2 recommends the valuable
>    half (an arrangement-stage dual-run INSTRUMENT over `refs/preserve/map-sandbox-w3f-sealed`) as
>    a parallel chair measurement lane, which serves the impulse behind BOTH named candidates
>    without minting production code whose inputs do not exist.
> 3. **The plan's roster stands and names the next member.** ODQ §312.1 collected the 16-member
>    roster with the §287.16 massing foundations as a track; nothing in §313–§344 re-scoped it.
>    §312.2b's stamp caps map trains at eight, so the current train continues past T2D and its next
>    member is **MF-T2E: `MassPartQ`/`SolidPartQ`, the typed functional-volume vocabulary** —
>    unblocked (its substrate is MF-T2B, landed), chartered verbatim by §287.16 (*"typed functional
>    volumes"*), and the direct unblocker of MF-T2F, which discharges §299.3(a), one of the three
>    recorded obligations.

> ⛔ **SOURCE PROVENANCE — this is a DESIGN-IMPLEMENTATION member, not a port-by-hash member.**
> `MassPartQ`/`SolidPartQ` have NO sandbox source module: the sealed sandbox's own
> `coordinateAbi.js` and `solidLegality.js` state *"§7's `MassPartQ`/`SolidPartQ` are design-only
> (SPEC §10.16's status override)."* The sources this member implements from, each cited exactly:
>
> | source | citation | what it supplies |
> |---|---|---|
> | `map-corpus/docs/GENERATION-SPEC.md` §7.1.2 · §10.5 · §10.16 | ledger-worktree blob `aa613cb8d9ea8b895236304e9e72b08940ba1698` — the SAME blob the landed preamble cites; re-hash before reading (`git hash-object`), a mismatch is a STOP | the `FunctionalVolume`, `MorphologyRole` and unknown-reason vocabularies (closed, verbatim); the no-inference knowledge law; the support-relative vertical design |
> | sealed sandbox `fabric/solidLegality.js` (MF-D1) | SHA-256 `b2f59501f1902cd74b470238996c5bd875a84a8934aa8124517d2159bdb7f645` — verified IDENTICAL at the sealed-tip path AND at `refs/preserve/map-sandbox-w3f-sealed` blob `0c46df0729b3da609c4fa1c09e67520dd1a4ecdf`; the executor re-hashes either copy | ⭐ the vertical rule D1 declared IN ADVANCE so D3a inherits it: half-open `[baseQ, topQ)` on a NAMED support surface, and the exact `{kind:'INTERVAL', baseQ, topQ}` discriminant its `solidOverlap` VOLUME arm reads — **executed by this compile, §4.2 P4** |
> | ODQ §287.16 / §287.5 | ledger `docs/OWNER_DECISION_QUEUE.md` @ `83227384` | the D3a charter (*"explicit-input support-relative solids, typed functional volumes…"*) and §287.5's geometry repair |
>
> Nothing is ported by code region; no sandbox module hash gates the edit itself, but BOTH cited
> hashes are re-verified at dispatch (§4) because the pins of §9 are written from them.

---

## 1. Reconciled authority

1. **ODQ §287.16, the chartering text, verbatim:** *"D3a may land explicit-input support-relative
   solids, typed functional volumes, component slots, architectural-history operations and
   deterministic fixtures while leaving live seeds byte-identical."* This member is the **typed
   functional volumes** item, and ONLY that item: component slots are MF-T2K, vegetation MF-T2L,
   operations MF-T2M (⛔ blocked behind MF-T2S per the plan §6).
2. **SPEC §10.16(3), the D1 row:** *"The volume law's attachment vocabulary needs
   `MassPartQ`/`SolidPartQ`, which D3a mints — the typed contract lands here, the third dimension
   there."* The typed contract landed with D1's predicate; this member is "there": the third
   dimension's RECORD vocabulary, explicit-input, dormant.
3. **The D1 vertical rule, declared in advance precisely so this member inherits it rather than
   minting a second one** (`solidLegality.js` docblock, sealed source): *"the vertical extents are
   half-open intervals `[baseQ, topQ)` on a NAMED SUPPORT SURFACE, and two solids on DIFFERENT
   supports do not intersect merely because their intervals do."* ⭐⭐ **EXECUTED at this compile
   (§4.2 P4):** the D1 `solidOverlap` driven read-only on records built by a prototype of §6
   returned `VOLUME { sharedHeightQ: 0, verdict: 'DISJOINT_OR_ABUTTING' }` on the stacked
   `[0,5000) × [5000,9000)` pair — the half-open law as behavior, not prose — and
   `REFUSED/DIFFERENT_SUPPORT_SURFACE` across supports, and `PLANAR_ONLY` against a plan-era
   `ABSENT` vertical. The discriminant this member publishes is admitted by the predicate MF-T2F
   ports, verbatim, with zero adaptation.
4. **Preamble §P1 R-MF-2 — BigInt at the one arithmetic decision.** The leaf's only arithmetic is
   the footprint degeneracy check (zero signed area), and it is a product of ABI quanta. ⭐⭐
   **EXECUTED at the pinned tip (§4.2 P1):** on the R-MF-2 triple
   `a=[0,0] b=[m−1,m] c=[m,m+1]`, `m = 1286630001`, the Number shoelace reads `2A = 0` and the
   landed float `area()`/`absArea()` read `0` — while BigInt reads `2A = −1`. A Number-based check
   would REFUSE that valid sliver as degenerate. The check is BigInt, internal, with the reason and
   these figures in the file.
5. **Preamble §P2.7 — the height door.** `heightQ()` throws and no height is published in the plan
   era; *"the member that opens that door does so deliberately, in its own packet body."* **This
   packet body is that deliberate act, for the CONTRACT only:** it mints the vertical-interval
   record vocabulary with explicit integer quanta validated through the landed wall. It does NOT
   edit `heightQ()`, does NOT convert world units, and does NOT publish height into any artifact —
   height's first PUBLICATION stays owed by a later member and is carried, not taken (RAISED-3).
6. **ODQ §303.5 record-shapes-in, applied at the identity seam:** the record family coheres with
   the landed codex explicit-mass shapes (`shapes.js`, `building.js`, `massingRoster.js` — the
   shapes §287.16 extends) by convention — the landed validators, `deepFreezeCanonical`, typed
   refusals naming their label — without editing any of them.
7. **ODQ §310.3(9) / preamble §P2.5:** no tuning constant of any kind. The vocabularies are closed
   SPEC-verbatim lists pinned by equality; no numeric constant beyond the ABI wall is read, and
   none is minted.
8. **Live code decides.** Every load-bearing fixture below was EXECUTED before any pin was written
   (preamble §P2.9): the landed validators' refusal shapes, the prototype constructors, the R-MF-2
   sliver, and the D1 predicate on the published discriminant. Captures in §4.2 and
   `laneTCT2E-receipt.md` §1; log `laneTCT2E-probe/driver.log`.

**Resolved contradictions:**

- *The plan's MF-T2E row says "One new persisted record family — the budget's exact allowance of
  one"* — vs the landed MF-T2D precedent (J-TCT2D-2, accepted at ODQ §334.4): no
  `artifactKind`/`schemaVersion` on a record nothing persists, because a stamped shape that later
  grows (T2K adds slots, T2L vegetation, T2M operations) puts two shapes under one name.
  **RESOLUTION: the records are VALIDATED IN-MEMORY VOCABULARY — frozen, constructor-built, and
  UNSTAMPED. The persisted-family budget row is 0.** The kind/version stamp and every persistence
  seam (reload, regen, undo, migration) arrive with the member that first persists a massing
  phase, and that member owns the mint. (Plan staleness — §13.2; RAISED-4.)
- *The SPEC's full `MassPartQ`/`SolidPartQ` interfaces carry roofs, materials, floor programs,
  origins, closed shells, upper envelopes, provenance and content hashes* — vs the plan's ~140-line
  pricing and §290.4's fence. **RESOLUTION: this member mints the D3a-CORE slice** — the three
  closed vocabularies, the knowledge axis, the half-open vertical interval, and the two record
  constructors in the shape the D1 predicate admits (`partId`/`supportSurfaceId`/`footprint`/
  `vertical`). Everything else is named OUT in §2 with its owning member. (RAISED-5, RAISED-7.)
- *`exactGeometry.area()` is the tree's area spelling* — vs R-MF-2 at ABI scale (§1.4's executed
  divergence). **RESOLUTION: the degeneracy check is an INTERNAL BigInt sign-of-area, not
  exported**, with the reason in the file. Extending `exactGeometry.js` instead would edit an
  existing production leaf for one internal predicate; refused, recorded (RAISED-6 carries the
  adjacent T2F question).
- *The support surface: SPEC §10.5's full `SupportSurfaceRef` is a discriminated five-branch
  owner-resolved reference with an acyclic-DAG law* — vs this member's budget and the absence of
  any registry to resolve against. **RESOLUTION: `supportSurfaceId` is a canonical id string**
  (the D1 predicate's own shape — it compares ids and refuses across them; executed §4.2 P4).
  The typed `SupportSurfaceRef` machinery is MF-T2K's subject (*"support-relative solids and
  component slots"*), and the id-string field is upward-compatible with it: K types the reference,
  it does not rename the field.

The implementer does not read other documents to reinterpret this packet.

## 2. Outcome

**Observable result:** the app-side fabric gains the typed functional-volume vocabulary and the
quantized vertical truth §287.16 charters: closed SPEC-verbatim vocabularies for functional volume
and morphology role, a total KNOWN/UNKNOWN knowledge axis that cannot carry a value while unknown,
a half-open `[baseQ, topQ)` vertical interval on a named support surface refused through the landed
ABI wall, and frozen `SolidPartQ`/`MassPartQ` records whose discriminant the D1 legality predicate
(MF-T2F's port source) admits verbatim. Nothing calls it in production; live seeds stay
byte-identical; every existing digest, pin and test result stays exactly where it is.

**The IN/OUT boundary, named affirmatively:**

| capability | verdict | why |
|---|---|---|
| `FUNCTIONAL_VOLUME_KINDS` (6), `MORPHOLOGY_ROLE_KINDS` (17), `MASSING_UNKNOWN_REASONS` (4) — closed, frozen, SPEC-verbatim | **IN** | the typed vocabulary §287.16 charters; pinned by deep equality (§9 A5) |
| the knowledge axis: `KNOWN{value}` / `UNKNOWN{reason}`, total, no value while unknown | **IN** | the SPEC's no-inference law made structural — an UNKNOWN cannot feed a value-conditioned resolver because it HAS no value |
| `verticalIntervalQ(baseQ, topQ)` — half-open, strictly positive measure, integers through the landed wall | **IN** | the D1-declared rule inherited, not re-minted; executed §4.2 P3/P4 |
| `solidPartQ` — `{partId, supportSurfaceId, footprint, vertical}` + `abiVersion: COORDINATE_ABI_VERSION` | **IN** | the exact admission shape of the D1 predicate; executed §4.2 P4 |
| `massPartQ` — `{partId, parentBodyId, morphologyRole, functionalVolume, solid}` with `partId === solid.partId` | **IN** | the part/solid identity law (SPEC §10.5: *"part.solid.partId === part.partId"*) |
| BigInt footprint-degeneracy check (internal) | **IN** | R-MF-2; §1.4's executed divergence |
| any overlap, interval-intersection or legality predicate | **OUT — forbidden** | MF-T2F ports the ONE predicate home (`solidOverlap`); a second spelling here is the §287.12 defect |
| `SupportSurfaceRef` typing, component/material slots | **OUT** | MF-T2K |
| vegetation trunk/canopy records | **OUT** | MF-T2L (⛔ both halves or neither, per the plan) |
| architectural-history operations | **OUT** | MF-T2M — ⛔ blocked behind MF-T2S (§299.3c) |
| roofs, floor programs, functional PROGRAM axis, origins, closed shells, upper envelopes, provenance/content hashes | **OUT** | the SPEC's full interfaces; arrive with K/L/M and the first persisting member |
| `artifactKind`/`schemaVersion` stamps, any persistence seam | **OUT** | resolved contradiction 1; the first persisting member owns the mint |
| `heightQ()` motion, world-unit conversion, height publication | **OUT** | §1.5; the door stays deliberately un-opened beyond the contract |
| barrel export, any production consumer, any wiring | **OUT** | lands DORMANT; the acceptance battery is the only caller |

**Definition of done:** `massPart.js` exports the eight §6 symbols; the acceptance matrix passes
with every refusal typed and every pin executed from base captures; the census is re-recorded; every
digest pin and every fabric-touching test result is unchanged.

**In scope:**

1. **One primary behavior** — the typed vocabulary and its constructors (the lists, the knowledge
   axis, the interval, the two records are ONE behavior: a record missing any of them is not the
   §287.16 vocabulary).
2. **One necessary integration path** — none in production; the writer-to-reader proof is split by
   necessity: the compile executed the D1 predicate on the published discriminant (§4.2 P4), and
   the LIVE interop arm lands with MF-T2F, the named reader — because ⛔ no app file, test files
   included, may import the sealed sandbox tree.
3. **One prevention guard** — the vocabulary-closure arm with its guard-the-guard opener (§9 A5):
   the lists are nonempty, frozen, exactly the SPEC values, and the constructors refuse non-members
   — so a later "helpful" widening reds on arrival.

**Explicit non-goals, named affirmatively:**

- ⛔ **No existing production file moves.** Not the barrel, not `exactGeometry.js`, not
  `foundation.js`, not `coordinateAbi.js`, not any codex massing module. The MF-T2A sweep-plant
  anchor line (`export function derivePlanarDcelEmbedding(input) {` at column zero in
  `dcelEmbedding.js`) and the `CURRENT_MAP_TRADITION_ID` non-import are preserved BY CONSTRUCTION —
  this member does not open any host that carries them — and §11 still carries both as STOPs in
  case a later revision of this packet does.
- ⛔ **No second predicate spelling.** No overlap, no interval intersection, no shared-volume
  arithmetic. The stacked-pair zero-volume pin is MF-T2F's acceptance case, not this member's.
- ⛔ **No fixture-era vocabulary semantics.** The lists are identifiers with membership law only;
  no constructor attaches behavior to any particular kind (LAND_CAP does not imply floating-land
  machinery; that is the SPEC's later design).
- ⛔ **No dependency bump, no `FABRIC_COORDINATE_ABI`/version-string motion, no y-flip, no rung,
  no tuning constant.**
- Record adjacent discoveries in the receipt; do not investigate or repair them.

## 3. Hard scope budget

| Limit | Packet budget | Standard |
|---|---:|---:|
| Behavior families | `1` | 1 |
| New persisted record families | `0` — in-memory vocabulary (resolved contradiction 1) | ≤1 |
| Named state writers | `0` | ≤1 |
| Feature flags | `0` | ≤1 |
| User-facing surfaces | `0` | ≤1 |
| Direct production consumers | `0` | ≤2 |
| New logic-bearing production leaves | `1` — `fabric/massPart.js` | ≤2 |
| Existing logic-bearing production files modified | `0` | ≤3 |
| Additional registration-only files | `0` — deliberately no barrel edit | ≤3 |
| Handwritten files total | `5` (1 src · 2 acceptance · 1 census re-record · this packet) | ≤12 |
| New/changed effective production lines | **≤ `180`** (measured basis §4.4: prototype core 52 + priced additions ≈ 85–120 estimated) | ≤400 |
| Effective lines per new leaf | **≤ `160`**, measured with eslint's own `Linter` at implementation | ≤250 |
| Delta in a shared/hot file | `0` — no manifest path is on the hot-file list (`STOP: RE-DERIVE AT BASE`) | ≤15 |
| Acceptance cases | `7` | ≤8 |

Overrides approved before dispatch: `NONE`. The member fits its caps with room; **no split member
was seeded** — MF-T2F already exists as the plan's next member and this packet neither absorbs nor
re-shapes it.

## 4. Sealed dispatch and preflight

```sh
npm run implementation:dispatch -- MF-T2E
```

Then, before any edit — every row re-executed at the dispatch base; nothing inherited from this
compile, from the capsule, or from a sibling packet:

| # | Check | Compile-tip result (executor re-derives) |
|---|---|---|
| 0 | `shasum -a 256 docs/implementation/preambles/MF-PREAMBLE.md` | `0706aad6…1db4ed` at `3ac279db`; **recompute — a further re-stamp is possible** |
| 1 | source re-hash: `git cat-file blob 0c46df0729b3da609c4fa1c09e67520dd1a4ecdf \| shasum -a 256` (the preserved D1 `solidLegality.js`) and `git hash-object map-corpus/docs/GENERATION-SPEC.md` in the ledger worktree | `b2f59501…b645` and `aa613cb8…1698` — MATCH REQUIRED; a moved spec blob means the vocabularies must be re-read before the pins are written |
| 2 | name-collision scan: `git grep -cE 'FUNCTIONAL_VOLUME_KINDS\|MORPHOLOGY_ROLE_KINDS\|MASSING_UNKNOWN_REASONS\|knownMassingFact\|unknownMassingFact\|verticalIntervalQ\|solidPartQ\|massPartQ\|townMapMassPart' -- src tests ; echo GREP_EXIT=$?` | **exit 1, zero hits** at `3ac279db` (grep's own exit captured in-shell, never a pipe tail's); the arriving names collide with nothing |
| 3 | `npx vitest run tests/lint/townMapFabricSingleDeclaration.walker.test.js ; echo TRUE_EXIT=$?` | green at the tip; `STOP: RE-DERIVE AT BASE` (T2D's landing moves the fabric denominators) |
| 4 | every fabric-touching test file (re-derive the list at base by grep over `tests/` for `townMap` — 15+ files at the tip incl. the twelve digest-pin suites), counts captured BEFORE the first edit | the untouched-pin control's before-arm; per-file counts in the lane receipt |
| 5 | the census tuple | `STOP: RE-DERIVE AT BASE` by placeholder-and-convict (§325.2); compile-tip value in §9 for shear visibility only |
| 6 | `npm run typecheck:ratchet` and `npm run typecheck:domain:strict`, floors by name and window | `STOP: RE-DERIVE AT BASE`; no `townMap` key in either baseline means zero allowed debt for the new leaf |
| 7 | §P2.9 unedited-substrate captures — the §4.2 probes re-executed at base BEFORE pins are written | see §4.2 |

### 4.2 · The compile-tip captures this packet's pins are written FROM (re-execute all at base)

Executed by this compile at `3ac279db` (probe tree assembled from `git show` blobs of the LANDED
modules plus the hash-verified sealed `solidLegality.js`; driver and log in
`laneTCT2E-probe/`, `TRUE_EXIT=0`):

```
P1  R-MF-2 at the ring-area decision      -> m=1286630001; tri a=[0,0] b=[m-1,m] c=[m,m+1]:
                                             Number 2A = 0 · BigInt 2A = -1 · landed float
                                             area()/absArea() = 0; ordinary-triangle control
                                             agrees in both spellings (12000000)
P2  landed validator refusal shapes       -> non-integer / past-wall: TypeError '<label> must be
                                             an integer in -9007199254..9007199254' · empty id:
                                             '<label> must be a canonical id' · non-record:
                                             '<label> must be an object' · negative-in-wall
                                             coordinate ACCEPTED (a keel below datum is legal)
P2b the canonical-id grammar, measured    -> /^[a-z0-9][a-z0-9:._-]{0,95}$/ — lowercase only;
                                             'support:wallTop:w9' REFUSED, 'support:wall-top:w9'
                                             admitted; fixture ids must fit the grammar
P3  prototype constructors                -> records frozen; vertical {kind:'INTERVAL',baseQ,topQ};
                                             abiVersion 1; replay JSON-identical; refusals typed
                                             on [5000,5000) empty, [9000,5000) inverted, float
                                             baseQ, non-member KNOWN value, non-member UNKNOWN
                                             reason; the wall-scale sliver CONSTRUCTS (BigInt)
                                             while absArea() reads 0 on it (Number control)
P4  the D1 predicate on the discriminant  -> stacked [0,5000)x[5000,9000), same support, same
    (sealed solidOverlap, read-only)         footprint: VOLUME sharedAreaQ=100000000
                                             sharedHeightQ=0 sharedVolumeQ=0 DISJOINT_OR_ABUTTING
                                             · overlapping intervals+footprints: VOLUME
                                             sharedVolumeQ=75000000000 OVERLAPPING · different
                                             support: REFUSED/DIFFERENT_SUPPORT_SURFACE · ABSENT
                                             vs INTERVAL: PLANAR_ONLY · same id: REFUSED/
                                             SAME_SOLID_ID — adaptation by KEY, never position
```

⭐⭐ **P4 is the member's charter executed:** the record this packet publishes is admissible by the
D1 predicate VERBATIM — the half-open law, the support refusal and the plan-era interop are the
predicate's own behavior on these records, captured before any pin was written.

### 4.4 · The effective-line basis (compile-tip measurement; executor re-measures)

The prototype of the §6 core (`laneTCT2E-probe/driver.mjs`, the constructors region) measures
**52 nonblank/noncomment lines** executed end-to-end. The full leaf prices at roughly +35–65 over
it: the 17-value morphology list, the `massPartQ` assembly with the identity law, nested-input
validation, and refusal-message polish. The leaf budget is a hard **160** effective, measured with
eslint's own `Linter` at implementation — never `wc -l`, never this table.

## 5. Verified tree contract

| Role | File | Symbol | Verified fact at `3ac279db` | Required use |
|---|---|---|---|---|
| The integer wall | `src/domain/townMap/fabric/foundation.js` | `requireCanonicalInt`, `requireCanonicalId`, `requireCanonicalRecord`, `deepFreezeCanonical` | wall defaults `±MAX_WORLD_UNITS` (MF-T2B threading); id grammar `/^[a-z0-9][a-z0-9:._-]{0,95}$/`; refusal messages captured §4.2 P2 | every input refusal and every freeze; ⛔ no second validator spelling |
| The ABI version read | `src/domain/townMap/fabric/coordinateAbi.js` | `COORDINATE_ABI_VERSION` (=1 at tip), `MAX_WORLD_UNITS` | the D1-ported ABI leaf; `heightQ()` THROWS and stays throwing | `abiVersion` stamped on `solidPartQ` records — READ, never moved |
| ⛔ The geometry home NOT extended | `src/domain/townMap/fabric/exactGeometry.js` | `area`, `absArea` | float shoelace; reads 0 on the valid wall-scale sliver (§4.2 P1) | **NOT imported by the leaf.** The internal BigInt sign check exists precisely because this spelling cannot decide degeneracy at ABI scale; the docblock says so with the P1 figures |
| ⛔ The sweep-plant anchor | `scripts/mutation-sweep.sh` | entry 28a | plants on `dcelEmbedding.js`'s exported signature line at column zero | not touched by this member; §11 keeps the guard in case a revision opens that host |
| The future reader, named | sealed `solidLegality.js` → MF-T2F's port | `solidOverlap` VOLUME arm | reads exactly `{kind:'INTERVAL', baseQ, topQ}` + `supportSurfaceId` (§4.2 P4) | ⛔ NOT imported from the sandbox by any app or test file, ever; the live interop arm is MF-T2F's acceptance surface |
| The record-shape family joined | `src/domain/townMap/fabric/shapes.js`, `building.js`, `massingRoster.js` | the codex explicit-mass shapes | landed dark; §303.5 record-shapes-in | ⛔ **forbidden files** — coherence by convention, zero bytes moved |
| The standing law | `tests/lint/townMapFabricSingleDeclaration.walker.test.js` | the fabric single-declaration law | green at tip; §4 row 2's scan shows the arriving names collide with nothing | binds the eight arriving exports |
| Census | `tests/lint/sovereigntyLightingContract.walker.test.js` | `const CENSUS = Object.freeze({` | the five-figure tuple, SEQUENCED | re-record all five together with cause + authorization |
| Test precedent | `tests/domain/townMapCoordinateAbi.test.js` + `tests/property/townMapCoordinateAbiDeterminism.test.js` | MF-T2B's two-file proof shape | the family's domain-matrix + determinism-companion form | **copy this proof shape** |
| Anchor helper | `tests/helpers/anchoredNegatives.js` | `expectAbsentWithAnchor`, `expectPresentThenAbsent` | the negative-assertion route | by name on the same line, per §P3 |

**Forbidden alternatives:** no edit to any existing production file; no export of any area,
overlap, intersection or legality helper; no `artifactKind`/`schemaVersion`/`lawVersion` mint; no
barrel edit; no `export *`; no import of the sealed sandbox tree from any app or test file; no
`test.each`, no `describe.runIf`, no loop-generated tests; no files outside the manifest.
⛔ **Name reservation:** this member must NOT declare `PLAN_ERA_VERTICAL`, `OVERLAP_FLOOR_Q`,
`planEraSolid`, `solidOverlap`, `intervalOverlapVerdict`, `footprintIsAnswerable` or
`dualRunLegality` — those are MF-T2F's arriving exports, and minting any of them here reds the
single-declaration walker at T2F's landing (the exact habitat MF-T2A closed).

## 6. Exact contracts

### The vocabulary and the records — `src/domain/townMap/fabric/massPart.js`

```js
export const FUNCTIONAL_VOLUME_KINDS = Object.freeze([
  'OPEN_CLEAR', 'DOMESTIC_STACK', 'STORAGE_STACK', 'PARTIAL_LOFT',
  'OCCUPIED_ATTIC', 'UNINHABITED_ATTIC',
]);                                    // SPEC §7.1.2 FunctionalVolume, verbatim order
export const MORPHOLOGY_ROLE_KINDS = Object.freeze([
  'MAIN_RANGE', 'CROSS_WING', 'REAR_RANGE', 'ANNEX',
  'STAIR_TOWER', 'GALLERY', 'PASSAGE', 'LEAN_TO',
  'CURTAIN_RUN', 'WALL_TOWER', 'GATEHOUSE', 'KEEP', 'BELFRY',
  'SPIRE', 'COVERED_PASSAGE', 'LAND_CAP', 'LAND_KEEL',
]);                                    // SPEC §7.1.2 MorphologyRole, verbatim order (17)
export const MASSING_UNKNOWN_REASONS = Object.freeze([
  'NOT_OBSERVED', 'OUTSIDE_COVERAGE', 'CONFLICTING_EVIDENCE', 'WITHHELD',
]);                                    // SPEC §7.1.2 MassingUnknownReason, verbatim order

export function knownMassingFact(value, vocabulary, label)
// -> Object.freeze({ status: 'KNOWN', value })
//    TypeError naming label when value is not a member of the (nonempty, frozen) vocabulary.
export function unknownMassingFact(reason, label)
// -> Object.freeze({ status: 'UNKNOWN', reason })
//    TypeError naming label when reason is not a member of MASSING_UNKNOWN_REASONS.
//    ⛔ The UNKNOWN arm carries NO value field — the no-inference law is structural: an unknown
//    fact cannot feed a value-conditioned resolver because the value does not exist on the record.

export function verticalIntervalQ(baseQ, topQ)
// -> deepFreezeCanonical({ kind: 'INTERVAL', baseQ, topQ })
//    baseQ/topQ integers through requireCanonicalInt (the landed ±MAX_WORLD_UNITS wall; negatives
//    legal — a keel sits below its support datum, §4.2 P2). ⛔ HALF-OPEN, STRICTLY POSITIVE
//    MEASURE: topQ > baseQ or a TypeError naming the half-open interval law; [b, b) is refused.

export function solidPartQ(input)
// input : { partId, supportSurfaceId, footprint: [[xQ, zQ]…], vertical: { baseQ, topQ } }
// -> deepFreezeCanonical({ partId, supportSurfaceId, footprint, vertical, abiVersion })
//    · partId / supportSurfaceId through requireCanonicalId (grammar §4.2 P2b, lowercase only)
//    · footprint: ≥3 points, each coordinate through requireCanonicalInt; refused when the
//      BigInt-exact signed area is zero (degenerate ring) — the INTERNAL check of §1.4, with the
//      R-MF-2 reason and the P1 figures in the docblock. ⛔ No simplicity/self-crossing check:
//      ring simplicity is the D0 kernel's guarantee and a second spelling here is forbidden.
//    · vertical through verticalIntervalQ · abiVersion: COORDINATE_ABI_VERSION, read never moved.

export function massPartQ(input)
// input : { partId, parentBodyId, morphologyRole, functionalVolume, solid }
//   · morphologyRole: member of MORPHOLOGY_ROLE_KINDS (plain closed value — arrangement, not use)
//   · functionalVolume: { status:'KNOWN', value } | { status:'UNKNOWN', reason } routed through
//     knownMassingFact/unknownMassingFact against FUNCTIONAL_VOLUME_KINDS (total axis; any other
//     status shape is a TypeError)
//   · solid: the solidPartQ INPUT shape, constructed internally — ONE construction route, no
//     half-validated record circulates
// -> deepFreezeCanonical({ partId, parentBodyId, morphologyRole, functionalVolume, solid })
//    ⛔ THE IDENTITY LAW: input.solid.partId must equal input.partId (SPEC §10.5:
//    part.solid.partId === part.partId); a mismatch is a TypeError naming both ids.
```

### Absence and refusal rules

- non-record input, missing/mis-grammared ids, non-integer or out-of-wall quanta, empty or
  inverted interval, degenerate footprint, non-member vocabulary value, non-member unknown reason,
  UNKNOWN-with-value, KNOWN-without-value, part/solid id mismatch: **typed TypeError through the
  landed validators or a message naming the offending label and law** — the family's refusal
  convention; never `NaN`, never a silent skip, never a defaulted field.
- There is no empty-collection arm: a solid without a vertical interval is not constructible here
  (plan-era `ABSENT` verticals belong to MF-T2F's ported `planEraSolid`), and a mass part without
  a solid is refused — the SPEC's `parts` non-emptiness lives at the body level, which is not this
  member's record.

### Determinism

- Pure over arguments; integer and BigInt arithmetic only; no clock, no randomness, no locale
  ordering, no iteration-order dependence (fields are written in fixed literal order; no object
  spread of caller input reaches the published record).
- Same input twice constructs JSON-identical records (§4.2 P3; §9 A7 is the credited proof).

### Flag, dormancy, lifecycle, receipts

- Flag: `NONE`. Golden posture: **unchanged and PROVEN unchanged** — the untouched-pin control
  (§10) plus the post-build fence and forensic zoom (§P2.2, post-§324.5 form).
- Lifecycle: pure construction, in-memory; ⛔ nothing persisted, so no reload/regen/undo/migration
  seam exists to declare (resolved contradiction 1 places the mint with the first persisting
  member). Public veil: n/a — no runtime surface.
- Receipts/privacy: closed kinds `NONE`; DM-only fields `NONE`.
- Alignment: `DECLARED EMPTY: a record vocabulary has no alignment surface.`
- Edit story: `ENGINE-ONLY: explicit-input vocabulary; nothing is DM-editable and nothing is proposed.`

## 7. Exact change manifest

| Action | File | Symbol/region | Max delta | Coding instruction |
|---|---|---|---|---|
| `CREATE` | `src/domain/townMap/fabric/massPart.js` | the eight §6 exports + internal `ringAreaSign2x` | **≤160 eff** | The §6 contract. Imports ONLY: `requireCanonicalInt`, `requireCanonicalId`, `requireCanonicalRecord`, `deepFreezeCanonical` (foundation) and `COORDINATE_ABI_VERSION` (coordinateAbi). ⛔ The BigInt check is internal; the module docblock carries the R-MF-2 reason with the P1 figures and the §10.16(3) charter line |
| `CREATE` | `tests/domain/townMapMassPart.test.js` | `describe('MF-T2E mass-part vocabulary')`, A1–A6 | `n/a` | ONE literal `describe`, six straight-line `test()` calls, string-literal titles; fixtures built inside named tests (SP-D idiom); id fixtures fit the §4.2 P2b grammar |
| `CREATE` | `tests/property/townMapMassPartDeterminism.test.js` | `describe('MF-T2E mass-part determinism')`, A7 | `n/a` | The replay + purity companion, matching MF-T2B's property-file shape |
| `TEST` | `tests/lint/sovereigntyLightingContract.walker.test.js` | the `CENSUS` object | `+12/−1` | ONE re-record block naming its cause and the `censusAuthorization` refs; all five figures re-derived together at the base by placeholder-and-convict |
| `DOC` | `docs/implementation/packets/town-cartography/MF-T2E.md` | this packet | `n/a` | The packet, its status transitions, and its §12 landing record |

Generated artifacts: `NONE`. No other file may be edited. ⚠ The two acceptance files are `CREATE`
rows, not `TEST` rows (the MF-T2B measured precedent). ⚠ No `tests/lint/**` file is minted, so
§P3b's mutation-coverage row does not attach. §P4's four-name template is satisfied by its "reason
none exists" arm, copied from the MF-T2C/T2D precedent: the leaf is directory-internal, the barrel
deliberately does not export it, and the registry tests that bind this member are the
single-declaration walker and the entry-closure fence. **Symbols the deliverable CREATES** (the
eight exports, the two describe titles) join `requiredSymbols` **at the flip to LANDED, never at
READY** — the validator resolves rows against the live tree at every status. `retiredSymbols`:
**EMPTY**.

## 8. Ordered coding sequence

0. Dispatch and seal; run every §4 preflight. STOP on any mismatch, including the preamble-hash,
   spec-blob or `solidLegality` hash rows.
1. **Capture the golden/dormancy evidence BEFORE the first edit:** the full fabric-touching test
   battery counts; the census tuple; and the §4.2 probes re-executed at the base (P1, P2, P2b, P3
   shapes — P4 re-runs against the PRESERVED blob only, extracted to scratch, never imported) —
   the pins of A1–A7 are written FROM these captures and from the implemented module's own printed
   output, never from this packet's historical figures.
2. Add the smallest failing focused test (A1's main arm against the not-yet-existing module).
3. Implement `massPart.js` per §6 — vocabularies first, then the knowledge axis, then the
   interval, then the two constructors.
4. — (no writer, no lifecycle seam)
5. — (no production consumer; the tests are the named reader until MF-T2F)
6. — (no registration; §7's "reason none exists" arm)
7. Focused verification (§10), including the anchor preflight and the untouched-pin control.
8. The removing-power sweep (§10's mutants), the census re-record, the post-build dormancy
   discharge with forensic zoom, then the wave-end gate and the completion receipt (§12).

## 9. Acceptance matrix

Every negative carries a positive control in the same test. The **capture** column is behavior
executed at the pinned tip by this compile (§4.2); the executor re-captures at its base.

| ID | Case | Fixture/input | Capture | Required observation | Home |
|---|---|---|---|---|---|
| **A1** | Main behavior — a two-part body's records construct and wear the D1-admissible shape | hall `[0,5000)` + solar `[5000,9000)` on `support:terrain:t1`, square footprints (P3's fixture) | P3: frozen, `{kind:'INTERVAL',…}`, `abiVersion` 1 | positive control FIRST: both records construct; `Object.isFrozen` deep on record, footprint and vertical; `vertical` deep-equals `{kind:'INTERVAL', baseQ, topQ}` exactly (the discriminant MF-T2F's predicate reads — §4.2 P4 is the executed admission proof); `abiVersion === COORDINATE_ABI_VERSION` read from the live import; the two intervals share the boundary quantum 5000 and BOTH construct (half-open adjacency is legal by design) | domain |
| **A2** | Refusals — typed, never floats, never silent | float `baseQ`; out-of-wall `topQ`; `[5000,5000)` empty; `[9000,5000)` inverted; uppercase support id; degenerate (zero-area) footprint; missing `parentBodyId` | P2/P2b/P3: each refusal message shape captured verbatim | each bad input throws a TypeError naming its label (wall messages are the landed validators' own, so the range text is the wall's); the empty and inverted intervals name the half-open interval law; the uppercase id is refused by the measured grammar | domain |
| **A3** | The knowledge axis — total, no value while unknown | KNOWN `OPEN_CLEAR`; KNOWN `CATHEDRAL_VOID` (non-member); UNKNOWN `NOT_OBSERVED`; UNKNOWN `DID_NOT_LOOK` (non-member); UNKNOWN carrying a `value` field | P3: member/non-member behavior captured | KNOWN publishes `{status:'KNOWN', value}`; non-member value refused naming the label; UNKNOWN publishes `{status:'UNKNOWN', reason}` with **no `value` property** (asserted by property absence with anchor); non-member reason refused; an UNKNOWN input that also carries a value is refused — the no-inference law is structural | domain |
| **A4** | The identity law | `massPartQ` with `input.partId ≠ input.solid.partId`; then the agreeing pair | prototype law of §6 | the mismatch throws a TypeError naming BOTH ids; the agreeing pair constructs with `part.solid.partId === part.partId` | domain |
| **A5** | Vocabulary closure — the ported-value pins with the guard-the-guard opener | the three exported lists + a non-member probe per constructor | SPEC blob `aa613cb8…` §7.1.2 (re-hash at base, §4 row 1) | opener: each list is NONEMPTY and frozen (guard-the-guard, §P6); `FUNCTIONAL_VOLUME_KINDS` deep-equals the six SPEC values in order, `MORPHOLOGY_ROLE_KINDS` the seventeen, `MASSING_UNKNOWN_REASONS` the four (the MF-T2B `ANGLE_TABLE_SIZE` / MF-T2D ladder precedent); constructors refuse a non-member of each | domain |
| **A6** | ⭐⭐ The named historical regression — R-MF-2 at the ring-area decision | the sliver `a=[0,0] b=[m−1,m] c=[m,m+1]`, `m = 1286630001` | P1: Number 2A = 0 · BigInt 2A = −1 · landed float `area()` = 0 | the counterforce with its control: the test's own Number shoelace reads 0 on the sliver (executed in-test, the divergence witnessed); `solidPartQ` with that footprint CONSTRUCTS (the BigInt check sees the nonzero area); a genuinely collapsed ring (three collinear points, BigInt 2A = 0 exactly) is REFUSED as degenerate — both arms in one test | domain |
| **A7** | Determinism / replay + purity | every A1/A5 fixture constructed twice; the leaf's raw text | P3: replay JSON-identical | deep-equal records across two constructions; the source scan finds no clock/randomness/locale identifier in `massPart.js`, with a positive control proving the scan sees a planted occurrence | property |

**Anchor law (§P3):** A2, A3, A5 and A7 assert absences (no value on UNKNOWN, no forbidden
identifier, refusals that must not silently vanish). Each routes through
`tests/helpers/anchoredNegatives.js` by name on the same line, or carries
`// anchored: <why this cannot go vacuous>` on the assertion line or the line immediately above.

**Census motion — derived from the case count, re-derived at base:**

```
DELTA: +2 files · +0 parked · +2 credited · +7 titles (6 domain + 1 property) · +2 suites
BASE and AFTER tuples: STOP: RE-DERIVE AT BASE by placeholder-and-convict, all five together.
Compile-tip tuple for shear visibility ONLY (pre-T2D-landing):
  files: 2490, parked: 364, credited: 2126, titles: 20669, suiteTitles: 5778
```

⛔ `parked` stays at its recorded value. One literal `describe` per file, straight-line `test()`
calls, string-literal titles; loops live INSIDE named tests. ⚠ **NAMED INTERIOR RED:** the census
arm is an exact equality against a recorded constant, so this member reds at its own
implementation commit until the tuple is re-recorded; the census is SEQUENCED, so the `files` red
is itself proof the later figures had not yet been read.

## 10. Verification commands — the gate skeleton

```sh
# Anchor preflight — MANDATORY per new test file, BEFORE the member proof is declared green
npx vitest run tests/lint/negativeAssertionAnchor.walker.test.js ; echo TRUE_EXIT=$?

# Focused static + both typecheck configurations, by name and window
npx eslint src/domain/townMap/fabric/massPart.js \
           tests/domain/townMapMassPart.test.js \
           tests/property/townMapMassPartDeterminism.test.js
npm run typecheck:ratchet
npm run typecheck:domain:strict

# Effective-line ledger against §3 — eslint's own Linter, never wc -l

# Focused tests — one slot, one command
sh scripts/gate-mutex.sh --run -- npx vitest run \
  tests/domain/townMapMassPart.test.js \
  tests/property/townMapMassPartDeterminism.test.js \
  tests/lint/townMapFabricSingleDeclaration.walker.test.js \
  tests/lint/sovereigntyLightingContract.walker.test.js

# ⭐ THE UNTOUCHED-PIN CONTROL — the twelve digest-pin suites PLUS every other fabric-touching
# test file (list re-derived at base), identical counts before the first edit and after the last.
# This member adds a leaf; it must move NOTHING that already exists. Goldens are compared against
# a `git archive` extraction of the base, never against a working-tree copy a hook may have touched.

# ⭐ THE REMOVING-POWER SWEEP — seven mutants, planted in a scratch copy, each convicting a NAMED
# branch, each restored by byte copy verified with cmp (never the git checkout -- family):
#   M1 ringAreaSign2x := Number shoelace                  -> A6 (the valid sliver is refused;
#                                                            the expected construction vanishes)
#   M2 half-open validation topQ > baseQ := topQ >= baseQ -> A2 (the empty interval [b, b) is
#                                                            accepted; the expected throw vanishes)
#   M3 UNKNOWN arm keeps a caller-supplied value field    -> A3 (the value-absence pin reds)
#   M4 the partId === solid.partId law deleted            -> A4 (the mismatch constructs)
#   M5 vocabulary membership check deleted                -> A3/A5 (a non-member is accepted)
#   M6 deepFreezeCanonical dropped on the published record-> A1 (Object.isFrozen reads false)
#   M7 footprint coordinate validation bypassed           -> A2 (a float coordinate is accepted;
#                                                            an out-of-wall coordinate is emitted)
# Pristine-green control before, clean re-run after; every conviction quotes its failing arm.

# ⛔ Dormancy: POST-BUILD ONLY (§P2.2 post-§324.5). Build first, then:
npm run build
VERIFY_DIST=1 npx vitest run tests/build/townMapLazy.test.js ; echo TRUE_EXIT=$?
# …and the forensic zoom: a string literal unique to the leaf (the half-open interval law's
# refusal text, or 'MASSING_UNKNOWN_REASONS') appears in ZERO entry-closure chunks and in a lazy
# chunk elsewhere in dist/, with scanner controls asserted present. An absence over a denominator
# that does not contain the surface proves nothing (§P2.12); name the denominator.

# Sealed receipt and exact-state handoff; neither is landing authority
npm run check:packet -- MF-T2E
npm run implementation:resume -- MF-T2E

# Wave-end — BARE, fresh shell, never piped, never wrapped, and OUTLAST it in your own turn
npm run check:tail ; echo TRUE_EXIT=$?
```

⛔⛔ **NEVER wrap `npm run check*` in `gate-mutex.sh --run`** — `test:ratchet` re-acquires and
self-deadlocks; **exit 3 is the mutex giving up, not a red.** ⚠ Trust no exit status you did not
capture. ⚠ Report both typecheck configurations by name and window; a ratchet being green is
evidence only about that ratchet. Expected: every command exits `0`; report actual counts, never
this packet's historical ones.

## 11. Mandatory STOP conditions

In addition to `PACKET_STANDARD.md` and preamble §P7:

- any digest pin, exact-key pin or pinned roster in the fabric-touching suites moves, or any such
  test file changes its result between the §8-step-1 and §10 captures — this member is purely
  additive and a moved pin means it was not;
- any existing production file would need editing — including `foundation.js`,
  `coordinateAbi.js`, `exactGeometry.js`, the codex massing modules and the barrel; the cure for
  any such need is a STOP-and-report, never a manifest widening;
- the `export function derivePlanarDcelEmbedding(input) {` line changes in ANY byte, or
  `CURRENT_MAP_TRADITION_ID` becomes imported or declared in any file this member creates;
- the family preamble's hash, the spec blob or the preserved `solidLegality.js` hash does not
  match a fresh computation at the base (§4 rows 0–1);
- any of MF-T2F's reserved names (§5's name-reservation list) would be declared or exported here;
- any overlap, interval-intersection, shared-volume or legality computation would be exported;
- a vocabulary value would differ in spelling, count or order from the SPEC blob's §7.1.2 lists;
- `heightQ()` would be edited, or any world-unit-to-quantum conversion would enter the leaf;
- an `artifactKind`, `schemaVersion` or law-version string would be minted, or any stamped version
  string would move;
- a published record would carry a non-integer, an out-of-wall quantum, or an empty/inverted
  interval;
- the single-declaration walker reds, or an arriving export name collides with a live one;
- the census moves by other than the §9 delta against the tuple captured at §8 step 1, or
  `parked` moves at all; a delta smaller than the titles added is attributed by reverting one
  test file at a time, never accepted as arithmetic;
- `tests/build/townMapLazy.test.js` reds POST-BUILD, or the forensic zoom finds a member-unique
  literal in an entry chunk;
- another D3a member is simultaneously non-terminal on
  `tests/lint/sovereigntyLightingContract.walker.test.js` (MF-T2D at compile time — wait it out);
- any ratchet, baseline, budget or ceiling would need raising; any tuning constant of any kind
  would be touched.

Do not edit the packet, broaden the manifest, repair unrelated gate failures, or continue into
the next wave.

## 12. Completion receipt

*(filled at the flip to LANDED)*

---

## 13. Scope determination detail, plan staleness and RAISED — for the chair at review

**Plan staleness ledger** (`draft-D3A-PLAN.md` was compiled at `9d851fae`, before members 1–4
landed; each divergence named, none silent):

1. **The §344.1 candidate frame is answered rather than followed.** The dispatch named the
   fabric-extraction producer and the §312.2c dual-run as the candidates; this compile refused
   both under the standing rulings (header block; receipt §2) and compiled the plan's own next
   member. If the chair intended a re-scope of D3a, that is a veto of RAISED-1, not a staleness
   here.
2. **Plan §4's "one new persisted record family — the budget's exact allowance of one" is
   superseded** by the J-TCT2D-2 / §334.4 precedent: the records are unstamped in-memory
   vocabulary; the persisted-family row reads 0; the first persisting member owns the mint and the
   seams (resolved contradiction 1).
3. **Plan §4's census shape for T2E (`+1/0/+1/+6/+1`) is superseded by the derived
   `+2/0/+2/+7/+2`** — the same count-from-the-actual-tests correction MF-T2B (+7) and
   MF-T2C/T2D (+8) each recorded, plus the family's domain-matrix + determinism-companion
   precedent for a member that mints canonical records.
4. **Plan §9.3's "every member touches `fabric/index.js`" does not bind this member** — no barrel
   edit (the T2C/T2D precedent), so only the census walker is reserved.
5. **The plan's ~140-effective estimate is replaced by a measured basis** (§4.4: executed
   prototype core 52 + priced additions; hard cap 160).
6. **Stamp/preamble rows are already discharged** (stamp §312.2b; preamble landed and twice
   re-stamped; this draft cites the live hash and orders a recompute).
7. **The plan's §9.1 graph hangs T2E off T2D.** Measured: no T2D symbol is consumed; the true
   dependency is MF-T2B. The edge is TRAIN order (census-path staged promotion), and this packet
   records it as such — T2E's implementation can build while T2D is at terminal, but is not
   PROMOTED until T2D is terminal (§P7.11).

**RAISED:**

| # | the call | this lane's recommendation |
|---|---|---|
| **1** | ⭐⭐ **The member determination itself.** The dispatch framed T2E as a choice between the fabric-extraction producer and the §312.2c dual-run retirement; this compile refused both (extraction → generator tranches per J-TCT2D-3/§334.4 and plan §2.2 under §290.4; dual-run → its own firing condition needs an app-side generator that does not exist, and it is a program act, not a port member) and compiled the plan's member 5, `MassPartQ`/`SolidPartQ`. | **Adopt.** The §312.1 roster stands un-re-scoped through §344; the stamped train continues A→B→C→D→E; E unblocks F, which discharges §299.3(a). Veto = a D3a re-scope, which is a chair act this lane may not take. |
| **2** | ⭐ **The impulse behind both named candidates is real and servable NOW — as an INSTRUMENT, not a member:** an arrangement-stage dual-run measurement over `refs/preserve/map-sandbox-w3f-sealed` (harness-side extraction of the sealed corpus's real-leaf fixtures → the landed noder+kernel → compare against the sandbox's own arrangement/DCEL results, aligned BY KEY). Read-only, MEAS-class (the §338.1 minKeys shape), zero repo bytes, dispatchable at any free slot. It would convert T2D's RAISED-6 carry ("real-leaf noding stays OUT pending the §312.2c gate") from a deferral into early evidence, and it de-risks every later tranche. | **Charter it as a parallel chair measurement lane.** Not compiled here: chartering a measurement is a chair act, and its deliverable is evidence, not a packet. If the chair prefers it as this seat's next compile instead, that is a lawful re-aim — but it still is not "the next D3a member". |
| **3** | **The height door (§P2.7).** This member mints the vertical-interval CONTRACT (explicit integer quanta through the landed wall) and declares that act deliberately in §1.5; `heightQ()` stays throwing; height's first PUBLICATION into any artifact stays owed. | Confirm the reading. If the chair reads any interval record as "height published", then this packet body is the deliberate §P2.7 act and already owns it — either reading leaves the executor's instructions identical. |
| **4** | **No kind/version stamp on the records** (the J-TCT2D-2 precedent extended to a family DESIGNED for eventual persistence): stamping now, then growing the shape through K/L/M, puts two shapes under one name — the exact second-truth defect twice recorded. | Adopt. Alternative — mint `schemaVersion: 1` now and bump per member — refused as version churn on an unpersisted shape; the first persisting member mints once, against the then-complete shape. |
| **5** | **Vocabulary scope: `morphologyRole` (17) and the unknown-reasons (4) ride with `functionalVolume` (6); `functionalProgram`, `floorProgram`, materials, roofs, origins stay OUT.** The three included lists are what `massPartQ`'s own fields consume; the excluded axes belong to K/M and would be dead vocabulary here. | Adopt; veto = widen (cheap, ~4 lines per list, but dead until its consumer) or narrow (forces K to edit this leaf, breaking the zero-modified-files pattern). |
| **6** | **The D1 predicate's VOLUME arm computes `sharedVolumeQ = areaQ * h` in Number, over a float `polygonIntersectionArea`** — at ABI scale that product exceeds exact float range (the §4.2 P4 fixture already yields 7.5e10 at toy scale). This is **MF-T2F's port conviction to make**, under R-MF-2, when it ports the predicate — not this member's to fix or pin. | **Carry as a NAMED note to MF-T2F's compile** (its BigInt-conversion decision, the T2C precedent). Recorded here so the next compiler inherits the finding with its executed fixture instead of re-deriving it. |
| **7** | **`SolidPartQ` here omits `closedShell`/`upperEnvelope`/`lowerOffset`** (SPEC §10.5's full shape): the D3a-core slice matches the D1 predicate's admission shape exactly, and the shell machinery arrives with MF-T2K. The field NAMES chosen (`partId`, `supportSurfaceId`, `footprint`, `vertical`) are upward-compatible — K types the support reference and adds fields; it renames nothing. | Confirm. Alternative — mint the full SPEC interface now — refused: it cannot be validated without the support-registry and hash substrate, and unvalidatable required fields are worse than absent ones. |
