───────────────────────────────────────────────────────────────
PLANT #78  target=src/domain/institutions/institutionTable.js
  cmd: perl -0pi -e "s/        closed: false,\n        values: Object\.freeze\(band \? \[band\] : \[\]\),/        closed: true,\n        values: Object.freeze(band ? [band] : []),/" src/domain/institutions/institutionTable.js
  PLANTED:       Tests  4 failed | 15 passed (19)
    RED:      × `whoIsCounted` is OPEN on every tier, and holds a BAND rather than a roll 14ms
    RED:      × derives every `closed` flag from the source roster, so a closed column with an unread source cannot exist 30ms
    RED:      × holds the three partially-filled columns OPEN, and names what is unread on each 26ms
    RED:      × gives the `whoIsExempt` arm a WORLD, so its emptiness stops being asserted about nothing 27ms
    RED:  FAIL  tests/lint/institutionTable.walker.test.js > the one thing the table can never say > `whoIsCounted` is OPEN on every tier, and holds a BAND rather than a roll
    RED:  FAIL  tests/lint/institutionTable.walker.test.js > THE HONESTY RULE — a column is `closed` only where every source the spec names is read > derives every `closed` flag from the source roster, so a closed column with an unread source cannot exist
    RED:  FAIL  tests/lint/institutionTable.walker.test.js > THE HONESTY RULE — a column is `closed` only where every source the spec names is read > holds the three partially-filled columns OPEN, and names what is unread on each
    RED:  FAIL  tests/lint/institutionTable.walker.test.js > THE HONESTY RULE — a column is `closed` only where every source the spec names is read > gives the `whoIsExempt` arm a WORLD, so its emptiness stops being asserted about nothing
  RESTORED cmp-exact: yes
───────────────────────────────────────────────────────────────
PLANT #81  target=src/domain/institutions/institutionTable.js
  cmd: perl -0pi -e "s/        closed: sourcesAllRead\('whatItDoes'\),/        closed: true,/" src/domain/institutions/institutionTable.js
  PLANTED:       Tests  2 failed | 17 passed (19)
    RED:      × derives every `closed` flag from the source roster, so a closed column with an unread source cannot exist 34ms
    RED:      × holds the three partially-filled columns OPEN, and names what is unread on each 26ms
    RED:  FAIL  tests/lint/institutionTable.walker.test.js > THE HONESTY RULE — a column is `closed` only where every source the spec names is read > derives every `closed` flag from the source roster, so a closed column with an unread source cannot exist
    RED:  FAIL  tests/lint/institutionTable.walker.test.js > THE HONESTY RULE — a column is `closed` only where every source the spec names is read > holds the three partially-filled columns OPEN, and names what is unread on each
  RESTORED cmp-exact: yes
───────────────────────────────────────────────────────────────
PLANT #82  target=src/domain/institutions/institutionTable.js
  cmd: perl -0pi -e "s/\.\.\.dutyRows\.map\(\(s\) => s\.name\), \.\.\.incomeDuties/...dutyRows.map((s) => s.name)/" src/domain/institutions/institutionTable.js
  PLANTED:       Tests  1 failed | 18 passed (19)
    RED:      × CLOSES `whatItCounts` because it now reads the FIRED INCOME ROW — a positive twin, not a flag 6ms
    RED:  FAIL  tests/lint/institutionTable.walker.test.js > THE HONESTY RULE — a column is `closed` only where every source the spec names is read > CLOSES `whatItCounts` because it now reads the FIRED INCOME ROW — a positive twin, not a flag
  RESTORED cmp-exact: yes
───────────────────────────────────────────────────────────────
PLANT #83  target=src/domain/institutions/institutionTable.js
  cmd: perl -0pi -e "s/  const services = allServices\.filter\(\(row\) => !row\.institution \|\| liveNames\.has\(row\.institution\)\);/  const services = allServices;/" src/domain/institutions/institutionTable.js
  PLANTED:       Tests  1 failed | 18 passed (19)
    RED:      × routes the RUIN FILTER through the service COLUMNS, and the filter is measured doing work 544ms
    RED:  FAIL  tests/lint/institutionTable.walker.test.js > THE HONESTY RULE — a column is `closed` only where every source the spec names is read > routes the RUIN FILTER through the service COLUMNS, and the filter is measured doing work
  RESTORED cmp-exact: yes
───────────────────────────────────────────────────────────────
PLANT #85  target=src/domain/institutions/institutionTable.js
  cmd: perl -0pi -e "s/  const taxedCoin = Number\(settlement\?\.economicState\?\.treasury\?\.coinFlows\?\.taxed\);/  const taxedCoin = NaN;/" src/domain/institutions/institutionTable.js
  PLANTED:       Tests  2 failed | 17 passed (19)
    RED:      × READS THE THIRD SOURCE: `coinFlows.taxed` reaches the basis, present-then-absent 5ms
    RED:      × AND IT READS THE PRODUCED PATH: `economicState.treasury`, never `settlement.treasury` 1ms
    RED:  FAIL  tests/lint/institutionTable.walker.test.js > THE HONESTY RULE — a column is `closed` only where every source the spec names is read > READS THE THIRD SOURCE: `coinFlows.taxed` reaches the basis, present-then-absent
    RED:  FAIL  tests/lint/institutionTable.walker.test.js > THE HONESTY RULE — a column is `closed` only where every source the spec names is read > AND IT READS THE PRODUCED PATH: `economicState.treasury`, never `settlement.treasury`
  RESTORED cmp-exact: yes
───────────────────────────────────────────────────────────────
PLANT #80  target=src/domain/prose/wiringCensus.js
  cmd: perl -0pi -e 's/    predicate: \[\],\n    fieldsRead: \[\],\n    status: WIRING_STATUS\.UNRESOLVED,/    predicate: [],\n    fieldsRead: [],\n    status: WIRING_STATUS.RESOLVED,/' src/domain/prose/wiringCensus.js
  PLANTED:       Tests  7 failed | 19 passed (26)
    RED:      × ANTI-VACUITY: the corpus census carries BOTH statuses, in measured integers 6ms
    RED:      × (c1) a pool whose key function has no branch selecting it reads WIRING-UNRESOLVED 2ms
    RED:      × (c1e) a PAIR-ARRAY key table is rung three, and a key it does not name is not 1ms
    RED:      × (c1f) a DOUBLE-quoted key is a key, and its apostrophe eats nothing after it 1ms
    RED:      × (c1c) rungs TWO and THREE recover a predicate the literal rung cannot see 1ms
    RED:      × (d) the ANTI-VACUITY arm reds on a fixture built to hold both, and can tell them apart 0ms
    RED:      × the GRAMMAR walker's arm D changes its verdict when the census is passed 3ms
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > the wiring census — TOTALITY over the corpus > ANTI-VACUITY: the corpus census carries BOTH statuses, in measured integers
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > THE CONTROLS — each must fire, and each cure must stop it firing > (c1) a pool whose key function has no branch selecting it reads WIRING-UNRESOLVED
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > THE CONTROLS — each must fire, and each cure must stop it firing > (c1e) a PAIR-ARRAY key table is rung three, and a key it does not name is not
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > THE CONTROLS — each must fire, and each cure must stop it firing > (c1f) a DOUBLE-quoted key is a key, and its apostrophe eats nothing after it
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > THE CONTROLS — each must fire, and each cure must stop it firing > (c1c) rungs TWO and THREE recover a predicate the literal rung cannot see
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > THE CONTROLS — each must fire, and each cure must stop it firing > (d) the ANTI-VACUITY arm reds on a fixture built to hold both, and can tell them apart
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > THE CENSUS AS THE WALKERS' SOURCE OF TRUTH > the GRAMMAR walker's arm D changes its verdict when the census is passed
  RESTORED cmp-exact: yes
───────────────────────────────────────────────────────────────
PLANT #84  target=src/domain/prose/wiringCensus.js
  cmd: perl -0pi -e 's/        predicate: qualify\(\[\.\.\.guardRows, \.\.\.extra\], fn, callArgs\),/        predicate: [],/' src/domain/prose/wiringCensus.js
  PLANTED:       Tests  9 failed | 17 passed (26)
    RED:      × "RESOLVED" IS NOT "RECOVERED": the two narrower integers ship beside it 7ms
    RED:      × one table, and the join between them is total 2ms
    RED:      × (c1b) the predicate that IS recovered is the branch's own field, op and value 1ms
    RED:      × (c1b2) FOUR branches before the literal — each recovers ITS OWN guard, not the first 1ms
    RED:      × (c1d) a fact spoken to by a DEEPER path, or by a guard that did not parse, is not MISSING 1ms
    RED:      × (c1c) rungs TWO and THREE recover a predicate the literal rung cannot see 1ms
    RED:      × (c3) a predicate over a field no composer holds is a finding 0ms
    RED:      × the fact index inverts the census without losing a pool 1ms
    RED:      × CO-OCCURRENCE has no floor of its own, and refuses to invent one 1ms
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > the wiring census — TOTALITY over the corpus > "RESOLVED" IS NOT "RECOVERED": the two narrower integers ship beside it
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > the census printed BESIDE the composed-fill census (report-only) > one table, and the join between them is total
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > THE CONTROLS — each must fire, and each cure must stop it firing > (c1b) the predicate that IS recovered is the branch's own field, op and value
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > THE CONTROLS — each must fire, and each cure must stop it firing > (c1b2) FOUR branches before the literal — each recovers ITS OWN guard, not the first
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > THE CONTROLS — each must fire, and each cure must stop it firing > (c1d) a fact spoken to by a DEEPER path, or by a guard that did not parse, is not MISSING
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > THE CONTROLS — each must fire, and each cure must stop it firing > (c1c) rungs TWO and THREE recover a predicate the literal rung cannot see
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > THE CONTROLS — each must fire, and each cure must stop it firing > (c3) a predicate over a field no composer holds is a finding
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > THE MAP READ THE OTHER WAY — fact → text, and the three tiers > the fact index inverts the census without losing a pool
    RED:  FAIL  tests/lint/proseWiringCensus.walker.test.js > THE MAP READ THE OTHER WAY — fact → text, and the three tiers > CO-OCCURRENCE has no floor of its own, and refuses to invent one
  RESTORED cmp-exact: yes
───────────────────────────────────────────────────────────────
porcelain after all plants:        5
 M scripts/mutation-sweep.sh
 M src/domain/institutions/institutionTable.js
 M src/domain/prose/wiringCensus.js
 M tests/lint/institutionTable.walker.test.js
 M tests/lint/proseWiringCensus.walker.test.js
