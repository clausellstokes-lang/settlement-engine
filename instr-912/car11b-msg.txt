INSTR-912 car 11b: the lighting census refrozen at car 11's tip — titles 23778 -> 23779, and nothing else moved

Car 11 adds ONE title to `tests/lint/institutionTable.walker.test.js` — the produced-path arm
that pins `whatItCounts`'s third source to `economicState.treasury`, the path the pulse writer
actually writes. The file is CREDITED by this contract (the two parked walkers are
proseMoveGrammar and proseEntryContradiction), so a credited title moved and the door had to be
re-run. The new title is an `it()` inside an existing `describe`, which is why `suiteTitles`
does not move with it.

THE DOOR'S OWN FIGURES, from its own refreeze run and no other source:

  census REFROZEN at b2d617611c615404f5beae23a78e9437c1b98b99 by INSTR-912 car 11 (Opus 5):
  files 2551 -> 2551, parked 375 -> 375, credited 2176 -> 2176,
  titles 23778 -> 23779, suiteTitles 6361 -> 6361.
  This run fails BY DESIGN so a refreeze can never be mistaken for a passing gate.
   Test Files  1 failed (1)
        Tests  1 failed | 33 passed (34)

and the plain re-run the ritual requires as its proof:

   Test Files  1 passed (1)
        Tests  34 passed (34)

The baseline diff is four lines and no more: `measuredAtSha`, `measuredBy`, `note`, and
`titles`. This is the ONLY `--write`/`--update` this car ran against any register: the
observed-shape inventory was NOT refrozen (car 11 cleared it by re-pointing the read, not by
banking it) and the prose-numerics baseline was NOT rewritten (it reads 225 exact, 0 new).

Seat: Opus 5 — Fable-unvalidated
Lane: INSTR-912
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
