# SKEPTIC — lane LIGHT — LENS: THE FENCES, THE REGISTERS AND THE RECEIPT (the consist)

Seat: Opus 5 — Fable-unvalidated (verifier). Dock read: `$SC/skepLIGHT`, HEAD `990a7860a`, base `8961388ce`.
Porcelain BEFORE `0` · AFTER `0`. Nothing was written inside any dock; no build, no `--write`, no golden re-record,
no `git checkout/stash/reset`, no whole-suite run. Every figure below is from a command executed here.

---

## SUMMARY

The consist is clean and the registers reproduce. Six of the receipt's quoted outputs were re-run and matched,
two of them verbatim to the character. **One receipt sentence is REFUTED** (the voice-E2 "every added src line is a
comment except two" leg — it is 47 lines, not 2), and **two are PARTLY true** (the persisted delta is two keys, not
one; the §917 writer-reach `--write` moves `frozenAtSha` as well as the digest). None of the three moves a
generated byte, and none changes the lane's disposition. One FENCE hazard was observed and is not the lane's.

---

## (a) THE CONSIST — `git diff --name-status 8961388ce..990a7860a`

**38 files: 15 `src/`, 3 `scripts/`, 20 `tests/` (one of them new).** Nothing under
`src/domain/display/stateProse/`, `src/domain/prose/` or `src/data/dossierStateProse/`. The only file matching
"census" is `tests/lint/.lighting-census-baseline.json` — the one census ritual the brief permits. The prose/wiring
censuses (`tests/lint/proseWiringCensus.walker.test.js` and siblings) are untouched. **CONFIRMED.**

**The src delta, measured:** 339 added lines, of which **47 are non-comment and non-blank** — the dial line, the
loader `loadGenerationLawPayloads()` and its seven await sites, six `payloadAwaitedBy` roster rows, one re-export,
two `self.onmessage = async`, and five lines of one `GENERATION_LAWS` `why` string. No em dash entered any added
`src/domain` or `src/data` string literal (checked; the Tier-2 voice ratchet's scope).

**Persisted shape — PARTLY.** The birth-time delta is **two** keys, not "the one config marker":
`config._livingContentLawVersion` on every classified BIRTH, and `settlement.customContentRoster` at
`generateSettlementPipeline.js:190` whenever the run's reviewed environment yields a row. The second is the law's
own point and the receipt states it (car 1c); no seed input moved. Recorded as a correction to the lens's premise,
not as a defect.

**Commits — CONFIRMED.** Eight commits, each carrying all three trailers (`Seat: Opus 5 — Fable-unvalidated`,
`Lane: LIGHT`, `Co-Authored-By: Claude Opus 5 …`). Staged paths are disjoint per car and match the receipt's table;
car 1a is exactly 16 files, which is the "sixteen changed files" its eslint line names.

---

## (b) THE ONE RED — the writer-reach register digest

Re-run at the tip: `tests/lint/writerReach.walker.test.js` → **1 failed / 55 passed (56)**.
The failure is `registerDigestOf()` vs `baseline.registerDigest`, printing exactly the receipt's two values:
expected `ac4734954a…`, received `14a3b59541…`. **CONFIRMED.**
`tests/lint` holds **149** test files, matching the receipt's "148 passed / 1 failed (149)". The 2490/2491 test
totals are **UNTESTED** — running the whole directory is outside my fence.

**Is the strike lawful, or a chair ruling?** **Lawful — pre-ruled by the instrument itself.**
`scripts/lib/writer-dark-register.mjs:383` is clause D-dial's own failure text, and it ends with the instruction to
retire the row rather than let it outlive its premise; the retired row's own `lighting` prose said the same before
the day came. The strike also creates **no growth violation**, which matters because `--write` is shrink-only and
refuses growth: `customContentRoster on settlement` appears in neither the baseline's `darkUnregistered` (1322 rows)
nor its `surfaceReach` (6532 rows), because the walker corpus generates under `customContent: {}` and so never
writes the key. `node scripts/check-writer-reach.mjs` (plain) **exits 0** and prints
`judged 6532 · LIT 560 · LIT-NAME 4646 · DARK 1326 (reviewable 526)` — identical to the banked population — and
`git status --porcelain scripts/.writer-reach-baseline.json` is **0 lines**.
The re-cut arms in the walker were **widened, not weakened**: the lane added
`expect(gated.has('customContentRoster on settlement')).toBe(true)`, so retiring the row did not drop the
dial-gated guarantee, and the three plants moved their DOOR to the density dial only because the living-content
dial now dies at D-dial before reaching clause W/S.
**The `--write` remains the chair's, correctly.**

---

## (c) THE LIGHTING CENSUS RE-FREEZE

Plain walker at the tip: `tests/lint/sovereigntyLightingContract.walker.test.js` → **34 passed**, so the frozen
tuple **is** the live measurement. **CONFIRMED.**

Reproduced independently of the walker:
* `files 2556 → 2557` — `git ls-tree -r` at each commit, filtered to `*.test.js|jsx`: **2556** and **2557**.
* `titles +15` and `suiteTitles +3` — counted at source over every touched test file:
  `livingContentLawWiring` 12→19, `densityCreateBoundary.walker` 14→16, `livingContentRosterPublicDrop` 6→7,
  `livingContentPromiseBytes` 0→5 (**net +15**); describes +2 and +1 (**net +3**). No other touched file moved
  either count, and none of the fifteen is `.skip`/`.runIf`/`.todo`.

⚠ **LOW — one provenance blemish the census exists to refuse.** The refrozen record carries `"date": "2026-09-09"`
while `measuredBy`, the commit body and the receipt all place the refreeze on 2026-09-08 (the machine's clock read
2026-09-08 22:1x throughout my pass). A stale-or-wrong numeral in a provenance note is the exact class the commit
message itself apologises for one paragraph earlier. Chair's call whether it is worth a byte.

---

## (d) THE FOUR REGISTERS — every one reproduced

| gate | re-run here | receipt | verdict |
|---|---|---|---|
| `check-observed-shape-readers.mjs` | exit 0 — `1972 finding(s), exactly matching the frozen inventory`; porcelain 0 | 1972, no write | CONFIRMED |
| `$SC/prose-numerics-rekey.mjs <dock>` (dry) | `baseline=225 live=225 parseErrors=0 · exact=225 rekeyed=0 relocated=0 FELL=0 NEW=0` | identical, verbatim | CONFIRMED |
| `check-domain-strict.mjs` | exit 0 — `no strict-type regressions (1120 errors, ceiling 1120)` | 1120/1120 | CONFIRMED |
| `check-full-typecheck.mjs` | exit 0 — `no type regressions (173 error(s), ceiling 173)` | 173/173 | CONFIRMED |
| `eslint src/ tests/ scripts/` | exit 0 — `31 problems (0 errors, 31 warnings)`; 19 files carry messages, **overlap with the 38 touched files is empty** | 0 errors, 31 warnings, none touched | CONFIRMED |
| `tests/lint/proseNumerics.test.js` | 29 passed; baseline file holds exactly 225 entries | — | CONFIRMED |
| `tests/domain/livingContentPromiseBytes.test.js` | **5 passed, 44.22 s** | 5 tests, 44.16 s | CONFIRMED |

---

## (e) THE FOUR REFUSALS

**R1 — the register digest.** Correctly chair-gated; see (b). The strike is the instrument's own pre-ruled
disposition and the baseline write is the register door. **CONFIRMED.**

**R2 — the empty-record roster shape.** Correctly owner-gated. `src/domain/content/livingContentRoster.js:261` is
`if (rows === 0) return null;`, so law 2 writes **no key at all** on a run whose reviewed environment yields
nothing. Emitting a frozen empty record instead would widen the persisted settlement shape on every world the
product mints — a persistence decision, not a lane's. The brief's expectation was wrong and the receipt corrects it
with the measurement. **CONFIRMED.**

**R3 — the `_gallery_dm_full_json` SQL twin.** The finding is real and the tripwire is sound.
* The net-current body **is** migration 129: it is the last case-insensitive `create or replace function
  public._gallery_dm_full_json` in `supabase/migrations/`. (130/142/189 only *call* it, and their own comment
  strings still say "net-current at 121" — stale, and not this lane's to fix.)
* That body strips `aiData · aiDailyLife · aiSettlement · dossierNotes · dmNotes · notes · narrativeNotes ·
  _seed · _regenSeed · _config`, and on `config` additionally `_seed` and `latentPantheon`. **Neither
  `customContentRoster` nor `customContentProvenance` is stripped.** The exposure is real.
* "Widened by one key, not opened" is CONFIRMED at the source: `customContentProvenance` is written at
  `generateSettlementPipeline.js:153` with no reference to any dial; the roster is written 37 lines later behind
  the world's own config.
* **The re-cut arm is not a gate that cannot fail.** It resolves the net-current body with the *same* extractor
  `tests/security/galleryDmFull.pglite.test.js` already uses (byte-identical regex and latest-wins sort, so the two
  files cannot disagree), anchors non-vacuity on two strings I verified are really in that body (`- 'dmNotes'`,
  `- 'latentPantheon'`), and reds the moment either key appears in the SQL — which is the cure arriving. Its only
  brittleness is the three-digit migration sort, which breaks at migration 1000.
* ⚠ **MEDIUM, for the chair, not against the lane:** the file named `livingContentRosterPublicDrop` is now green
  end to end while a DM-full share read back from the server still carries the author's unadopted homebrew library.
  The green is honest and the header says so, but the *shipping* decision — landing the dial with that door open —
  is owner-gated and still open. **CONFIRMED as correctly refused.**

**R4 — the test-totals census.** The prediction is right. Counted independently at source: **+1 test file, +15
test blocks**, with the per-file split matching the receipt's hand count exactly and no conditional among them.
Today's banked totals are `totalFiles 2502 · totalTests 32436`, so §917 should land `2503 / 32451`. **CONFIRMED.**

---

## (f) ARE THE QUOTED OUTPUTS THE COMMANDS' OWN?

Six were re-run; all six matched, two of them character-for-character (the prose-numerics re-key line and the two
writer-reach digests). **CONFIRMED for everything I could execute.** One sentence does not survive:

⛔ **REFUTED — the voice-E2 three-way proof, leg 3.** The receipt asserts "Every line this lane added to a src file
is a COMMENT, except two". Measured: **339 added src lines, 47 of them non-comment and non-blank.** The
conclusion still stands on the other two legs, both of which I confirmed: `git diff --stat 8961388ce..990a7860a` on
`src/domain/display/labelBands.js` and `src/domain/display/stateProse/generalStateProse.js` is **empty**, and no
added `src/domain`/`src/data` string literal carries an em dash. Severity LOW — a false figure inside a true
finding, which is the class this estate refuses in others' work.

---

## (g) WHAT THE §917 REGISTER DOORS WILL MOVE

Read from `$SC/run-registers-917.sh` and `$SC/run-ratchet-917.sh` (the latter already points at `laneLIGHT`).

| door | prediction | ground |
|---|---|---|
| **writer-reach `--write`** | `registerDigest` `ac473495…` → `14a3b595…` **and `frozenAtSha`** `455ec96a4…` → the §917 sha. Nothing else. | `baselineOf()` rebuilds the whole object and stamps `sha: headSha(root)`; every other measured figure (population, closureSizes, scanStats, the three digests, `pendingSurfaceCeiling` 3, `reviewableDarkCount` 526, `darkUnregistered` 1322) is identical to the banked one on my plain run. The dry run exits 0, so the shrink-only refusal will not fire. **This corrects the receipt's "ONE frozen value and nothing else" — PARTLY.** After the write, `writerReach.walker.test.js` goes green. |
| **lighting census** | **nothing** — the door is not run at §917; the plain walker is the receipt. | 34 passed at the tip; car 1g after the refreeze moved no test file. |
| **OSR** | **nothing** — no new scanned path, no drift. | `check-observed-shape-readers.mjs` exits 0 at exactly 1972 with zero drift lines. |
| **prose-numerics `--write`** | **nothing** — the dry run is already exact. | `exact=225 rekeyed=0 relocated=0 FELL=0 NEW=0`. |
| **mounts baseline, tuning register** | **nothing** — the lane touched neither surface. | Not in the 38-file consist. |
| **test-census ratchet `--update`** | **+1 file, +15 tests** → `totalFiles 2503 · totalTests 32451`, plus `measuredAtSha`. | Counted at source; none of the fifteen is skipped or conditional. |

---

## ⚠ ONE FENCE HAZARD, AND IT IS NOT THE LANE'S

At ~22:23 a `git status --porcelain` in `skepLIGHT` returned **1** — ` M src/domain/content/livingContentLaw.js`,
whose diff was exactly car 1g's `@type {number}` annotation removed. Within ~30 seconds the file was back to the
committed blob (md5 `c123bdf7…` on both sides), with its mtime **unchanged** at 22:11:15 throughout, and the state
did not reproduce on a second `check-full-typecheck.mjs` run. It was not mine — I never wrote in the dock — and it
was not the gate: `check-full-typecheck.mjs`'s only write is its own baseline under `--update`, which I did not
pass. The shape (strip the cure, run, restore by md5, preserve mtime) is a **mutation plant on car 1g by another
lens working the same dock**, which the fence forbids while a lens could be reading it. Sibling vitest runner
counts of 41 and 56 were observed in the same window. Recorded so the chair knows two lenses were plant-testing
one tree; my own porcelain is 0 before and 0 after, verified repeatedly.
