# SKEPTIC — lane LIGHT, lens: THE CALLER, THE DIAL AND THE PROMISE (cars 1a, 1b, 1c)

Seat: Opus 5 — Fable-unvalidated (verifier). Read-only on every tree.
Dock read: `skepLIGHT` at `990a7860a`. Base control: `laneSEAM` at `8961388ce` (porcelain 0).
Porcelain `skepLIGHT`: **0 before, 0 after**. Vitest runners at return: **0**.
Every figure below is from a command executed here whose output I saw.

---

## VERDICT SUMMARY

| # | claim (receipt) | verdict | severity |
|---|---|---|---|
| a1 | the loader's caller exists on the create boundary and keeps the dynamic form | CONFIRMED | NONE |
| a2 | every path that can reach the pipeline with a lit config awaits the loader (the receipt's seven) | CONFIRMED (enumerated independently) | NONE |
| b | plants A and B red by name | CONFIRMED (exact strings) | NONE |
| c1 | the dial line reads `ROSTER_LIVING_CONTENT_LAW_VERSION` and the mint returns the marker | CONFIRMED | NONE |
| c2 | the mint fires on a classified BIRTH and on no PREVIEW / re-derivation | CONFIRMED | NONE |
| c3 | "the dormant branch still exercised through the leaf's closed test" | **PARTLY** — the mint's dormant branch is exercised by nothing | LOW |
| d1 | THE PROMISE: 0 movers on 525 + 768 | CONFIRMED, and extended | NONE |
| d2 | the receipt's per-corpus table ("roster under law 1 = 0", "wrong law = 0" on RATE) | **PARTLY** — the RATE arm measures neither | LOW |
| e1 | ABSENT, not an empty record (the brief's correction) | CONFIRMED | NONE |
| e2 | anti-vacuity: a lit birth carries a roster; marker+roster removed ⇒ byte-identical | CONFIRMED | NONE |
| f | `readLivingContentLawVersion` still fails closed after the 1g annotation | CONFIRMED | NONE |
| — | R3's framing: lighting "widened it by one key", both keys equally conditional | **PARTLY** — the roster fires 3× as often as provenance | LOW |

**No HIGH finding.** No lit config reaches the pipeline unarmed; no generated byte moved.

---

## (a) THE CALLER — CONFIRMED

`loadGenerationLawPayloads()` is an async export of `src/domain/density/densityCreateBoundary.js:334`;
it awaits `loadLivingContentRoster()`. The seam's edge is intact and lazy:
`livingContentSeam.js` still spells `await import('./livingContentRoster.js')` — no space, no `from`.

**The reacher set, enumerated by me and not from the manifest.** Scanning all **2,211** `src/`
`.js`/`.jsx` files, exactly eight name the pipeline module path; two of those
(`domain/locksPreservation.js`, `domain/regenerationPreservation.js`) name it only inside doc
comments. The real importers are six:

| importer | in `PIPELINE_REACHERS`? | reaches `livingContentRosterFor`? |
|---|---|---|
| `components/surveyor/ConstructionPanel.jsx` | yes (PREVIEW) | yes |
| `lib/instantWorld/composeInstantWorld.js` | yes (BIRTH) | yes |
| `workers/generationRequest.js` | yes (EXECUTOR) | yes |
| `workers/customContentPreview.worker.js` | yes (PREVIEW) | yes |
| `store/campaignContentBindingSession.js` | yes (PREVIEW) | yes |
| `store/settlementSlice.js` | **no** | **no** — it destructures only `regenNPCsPipeline` / `regenHistoryPipeline` |

`settlementSlice.js` is the one importer off the manifest, and it is off it correctly: the seam's
only call site in `src/` is `generateSettlementPipeline.js:185`, inside `generateSettlementPipeline`
itself, and neither regen entry point passes through it. (Executed check: exactly two non-comment
references to `livingContentRosterFor` in `src/`, at `livingContentSeam.js:123` and
`generateSettlementPipeline.js:185`.) `store/settlementGenerateAction.js` reaches the pipeline
through `generationRequest.js`, which the manifest records as `reachesVia`.

I read each of the seven awaiters: in every one the `await` precedes the pipeline call **inside the
same async function**, never in a skippable branch. `ConstructionPanel.jsx` awaits once at line 86
above BOTH branches (realm at 89, settlement at 94). Both worker shells await inside their existing
`try`, so a load failure answers on the shell's own error channel.

Scripts: no script under `scripts/` calls `birthConfig` or `composeInstantWorld` except
`observe-genesis-war-ramp.mjs`, which carries the top-level await. Every other script hands the
pipeline a raw (markerless) config and so takes the dark branch. No unarmed script path.

**Eager closure, re-derived by me at BOTH trees** (`EAGER_FIRST_PAINT_MODULES` imported from each
tree's `vite.config.js`): base `laneSEAM` **263**, tip `skepLIGHT` **263**. None of
`densityCreateBoundary`, `livingContentSeam`, `livingContentLaw`, `livingContentRoster`,
`livingContentLawVersion` or any of the seven awaiters is in the eager set at either state.
`tests/build/livingContentSeamLazy.test.js`: **6 passed**.

**The outage is real and reproduces.** My first probe run — before I armed the seam — died with
`Error: [livingContentSeam] v2 world, roster payload not loaded` at
`livingContentSeam.js:126` out of `generateSettlementPipeline.js:185`, on a lit config with
`customContent: {}`. So the loader is required on EVERY lit generation, not only on ones with
homebrew.

## (b) THE TWO PLANTS — CONFIRMED, exact

Both executed inside one command that backed up, planted, ran, restored, checked md5 + `cmp`, and
printed porcelain before and after. `git checkout` was never used.

* **PLANT A** (the await deleted from `generation.worker.js`): `densityCreateBoundary.walker`
  **1 failed / 15 passed**, offender exactly
  `src/workers/generationRequest.js: src/workers/generation.worker.js no longer awaits the payload`.
  Porcelain 0 → 0, md5 MATCH, `cmp` OK.
* **PLANT B** (the await deleted from all seven; verified 0 files left awaiting):
  **2 failed / 14 passed** — nine offenders listed, and the outage arm's
  `expected 0 to be greater than 1`. Porcelain 0 → 0, all seven md5 MATCH + `cmp` OK.
* Clean run before and after: **16 passed**.

*(A first attempt at PLANT B silently no-opped on zsh word-splitting; nothing was written, the
walker ran 16 passed on the clean tree, porcelain stayed 0. Recorded so the retry is not read as a
second measurement of the same tree state.)*

## (c) THE DIAL — CONFIRMED, with one correction

Executed at the tip: `NEW_SETTLEMENT_LIVING_CONTENT_LAW_VERSION = 2`,
`ROSTER_LIVING_CONTENT_LAW_VERSION = 2`,
`newSettlementLivingContentLaw() = {"_livingContentLawVersion":2}`. The dial line is literally
`= ROSTER_LIVING_CONTENT_LAW_VERSION`, so the flip stays one line.

`tests/domain/livingContentMaterialization.test.js` **11 passed**;
`tests/domain/livingContentLawWiring.test.js` **19 passed** — including the CREATE arms (birth mints
the marker and nothing else), the two CLAMP arms in both directions, and
`⛔ THE STOP: a section regen never stamps a law onto an existing world`. The PREVIEW half is held
statically by the walker's `every BIRTH mints the law, and nothing else does` arm (green), not by an
executed preview run; that is the same shape it had before the lane.

**PARTLY (LOW) — c3.** The receipt (car 1b) says the inverted arm keeps "the dormant branch still
exercised through the leaf's closed test". It does not. The real
`newSettlementLivingContentLaw()` is called in exactly two places in the estate
(`livingContentLawWiring.test.js:235`, `livingContentMaterialization.test.js:327`) and both now take
the LIT branch; both dark drives (`boundaryWithDarkLaw`, `boundaryWithLitLaw`) replace the function
wholesale with `vi.doMock`. What the materialization arm actually exercises is
`readLivingContentLawVersion(DEFAULT)` and `materializesLivingContent({})` — the leaf, not the
mint's `{}` branch, which is executed by nothing at this tip. Nothing about the product moves; the
revert is still one line and `check-domain-strict` proves the branch type-checks. It is the
sentence that is wrong, not the code.

## (d) THE PROMISE — CONFIRMED, and strengthened beyond the receipt

`tests/domain/livingContentPromiseBytes.test.js`: **5 passed** (78.40 s here; the receipt's 44.16 s
is a timing, not a claim). The file prints no figures — its table is carried by assertions
(`moved` empty, `v2RosterCount === 0`), all of which passed.

**My own independent measurement**, not using that file:

1. Three seeds of my own — `GOLDEN[0]` (`golden-master-v3`), `RATE[0]` (`rate-0-0`),
   `RATE[400]` (`rate-100-0`) — generated under law 1 and law 2 and deep-diffed **path by path**
   over the whole settlement object. Every one returns exactly two differing paths:
   `config._livingContentLawVersion` and `_config._livingContentLawVersion`. Nothing else.
   `dark law = 1`, `lit law = 2` on each, so it is two laws and not one law twice.
2. **Cross-tree control, full corpora.** The dark path generated at the BASE commit and at the LANE
   TIP, hashed:
   `golden 525 → adb022663d579c4fa7a4ceffd2391a376961d0d109471d76cfa73eab566d7596` and
   `rate 768 → 8445dd42da0c2e57cea89d2884f95d9abc43b7ab9f10406ba0eed55879ef307f` — **identical at
   both trees**. All 1,293 pre-lighting worlds are byte-unmoved by the eight cars.
3. **With homebrew loaded**, which the receipt's corpora runs never exercise (they pass
   `customContent: {}`): 96 RATE towns with the reference pack, law 1 vs law 2, marker and roster
   subtracted — **0 of 96** moved any other byte. This is the arm the receipt's own table could not
   make, and it holds.

**PARTLY (LOW) — d2.** The receipt's 1c table prints, for the RATE row, `roster under law 1 = 0` and
`wrong law = 0`. Reading the file: the RATE arm counts only `v2RosterCount` and asserts only
`moved`; it never asserts `verdict.v1` / `verdict.v2` per row and never counts `v1Roster`. Those two
RATE cells are stated, not measured, by that file. (Both are true on the seeds I measured; the
correction is to the receipt's provenance, not to the fact.)

## (e) ABSENT, NOT EMPTY, AND NOT VACUOUS — CONFIRMED

* With `customContent: {}` under law 2: `Object.hasOwn(settlement, 'customContentRoster') === false`
  and the value is `undefined` on all three of my seeds. Not an empty record — **no key at all**.
  The receipt's refusal R2 reproduces exactly.
* With the reference pack under law 2, on all three seeds: roster PRESENT, `schemaVersion` 1,
  buckets exactly `deities, factions, stressors, traditions`; the dark twin carries none; and after
  removing the marker and the roster the two worlds are **byte-identical** (`true` on each).
* Through the shipped dial (`birthConfig`), a birth carries `_livingContentLawVersion = 2`; with the
  pack it carries a roster, with `customContent: {}` it carries neither roster nor provenance.

**PARTLY (LOW) — the R3 framing.** R3 says lighting "widened it by one key" and that
`customContentRoster` and `customContentProvenance` are alike "conditional on the run's reviewed
environment holding custom content". Measured on 96 RATE towns with the reference pack loaded:
**roster 96/96, provenance 32/96.** The provenance receipt is written only when the generated town
actually adopted a definition; the roster is written whenever the environment holds one. So on the
`_gallery_dm_full_json` surface the lit law adds a key that rides **three times as many** DM-shared
dossiers as the key already riding it. The refusal itself (owner-gated migration, not written) is
right; the sentence understates the delta.

## (f) THE STRICT ANNOTATION — CONFIRMED, no reader widened

`node scripts/check-domain-strict.mjs` at the tip: **exit 0**,
`no strict-type regressions (1120 errors, ceiling 1120)`. No baseline file appears in
`git diff --stat 8961388ce..HEAD` (only `tests/lint/.lighting-census-baseline.json` moved), so the
baseline was not widened.

**PLANT C (mine, not in the receipt):** deleting the `@type {number}` line reproduces the 1g red
exactly — `src/domain/content/livingContentLaw.js: 1 strict errors (baseline 0) — +1`. Restored,
md5 MATCH, `cmp` OK, porcelain 0 → 0. The annotation is the cure, and it is load-bearing.

**Does the widened declared type let a reader accept a version outside the space?** No.
`readLivingContentLawVersion` is a runtime closed-membership test and is untouched by the lane.
Executed over `undefined, null, 0, 1, 2, 3, 99, '2', '3', true, 2.0, NaN, Infinity, '2abc'`: every
result lands inside `LIVING_CONTENT_LAW_VERSIONS`; `readLivingContentLawVersion(3) === 1`. The
annotation is on the DIAL, not on any reader, and a dial set outside the space would still red
`livingContentMaterialization`'s exact pin (`toBe(ROSTER_LIVING_CONTENT_LAW_VERSION)`).

---

## EXECUTED, IN ORDER

`densityCreateBoundary.walker` 16 · PLANT A 1F/15P · PLANT B 2F/14P (+ one no-op attempt) ·
`livingContentPromiseBytes` 5 · `livingContentLawWiring` 19 · `livingContentMaterialization` 11 ·
`livingContentSeamLazy` 6 · `check-domain-strict` exit 0 (1120/1120) · PLANT C strict red reproduced ·
own probes: field-path diff on 3 seeds, 96-town reference-pack sweep, cross-tree eager derivation
(263/263), cross-tree full-corpora dark hashes (525 + 768, identical).

Scripts written (only under `skeptic-light/`): `probe.mjs`, `probe2.mjs`, `eager.mjs`,
`crosstree.mjs`, `crosstree-full.mjs`, `reachers.mjs`.
