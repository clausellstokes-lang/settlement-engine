# FOLD — LANE LIGHT, the skeptic pass over `990a7860a` / base `8961388ce`

**CONFIRMED 32 · REFUTED 2 · PARTLY 7 · UNTESTED 8 · NEW findings 1 (MEDIUM) — porcelain: lens A (caller/dial/promise) skepLIGHT 0 -> 0 · lens B (lifecycle/holes) skepLIGHT 0 -> 0, laneSEAM 0 -> 0 · lens C (fences/registers/receipt) skepLIGHT 0 -> 0 · folder at fold time: skepLIGHT `990a7860a` 0, laneSEAM `8961388ce` 0.**

Folder: Opus 5 — Fable-unvalidated. Read-only on every tree; the only file written by this pass is
this one. Lens files: `caller-dial-promise.md` (A) · `lifecycle-holes.md` (B) ·
`fences-registers-receipt.md` (C). Figures below come from a lens file or from a command I ran in
`skepLIGHT` at the tip (marked *folder*).

**No HIGH finding in three lenses.** No lit config reaches the pipeline unarmed, no generated byte
moved on 1,293 configurations, no read path stamps a persisted world, and the one red at the tip is
the refused register digest the lane left for the chair on purpose. The lane's disposition is
unchanged by this pass: it lands. What it owes is nineteen cures, none of them to the dial.

---

## 2 · EVERY REFUTED AND PARTLY VERDICT (and the one NEW finding)

| # | claim | verdict | figure | sev | lens |
|---|---|---|---|---|---|
| 1 | receipt, voice-E2 leg 3: "Every line this lane added to a src file is a COMMENT, except two" | **REFUTED** | **339 added src lines, 47 of them non-comment and non-blank** (dial line, the loader + its 7 await sites, 6 `payloadAwaitedBy` rows, 1 re-export, 2 `self.onmessage = async`, 5 lines of one `GENERATION_LAWS` `why` string). The E2 conclusion survives on its other two legs, both confirmed. | LOW | C |
| 2 | the refrozen lighting census record's provenance date | **REFUTED** | `.lighting-census-baseline.json:29` reads `"date": "2026-09-09"`; `measuredBy`, the commit body and the receipt all place the refreeze on **2026-09-08** (*folder*, line-verified). A stale provenance numeral is the exact class this census exists to refuse. | LOW | C |
| 3 | receipt 1b: "the dormant branch still exercised through the leaf's closed test" | **PARTLY** | The mint's `{}` branch is **executed by nothing** at this tip. `newSettlementLivingContentLaw()` has exactly two real call sites (`livingContentLawWiring.test.js:235`, `livingContentMaterialization.test.js:327`), both now on the LIT branch; both dark drives replace the function wholesale with `vi.doMock` (*folder*: call sites and the ternary at `livingContentLaw.js:204-212` read). The materialization arm exercises `readLivingContentLawVersion(DEFAULT)` and `materializesLivingContent({})` — the leaf, not the mint. Sentence wrong, code right; revert still one line, branch still type-checked. | LOW | A |
| 4 | receipt 1c table, RATE row: "roster under law 1 = 0", "wrong law = 0" | **PARTLY** | The RATE arm of `livingContentPromiseBytes.test.js` counts only `v2RosterCount` and asserts only `moved`; it never asserts `verdict.v1`/`verdict.v2` per row and never counts `v1Roster`. Both cells are **stated, not measured** by that file (both true on the three seeds lens A measured independently). | LOW | A |
| 5 | receipt R3 / test header: roster and provenance are alike "conditional on the run's reviewed environment holding custom content"; lighting "widened it by one key" | **PARTLY** (two lenses, one sentence) | Measured on 96 RATE towns with the reference pack: **roster 96/96, provenance 32/96** — the added key rides **3x** as many DM-shared dossiers. It is also a strict **superset** (`publicSafe.js:283-287`: roster = every reviewed definition in scope, adopted or not; provenance = which materialized), and before car 1a the roster's server exposure was **structurally unreachable** (no caller; a lit config threw out of the pipeline). Literally true, materially understated. The refusal itself stands. | MEDIUM (sizing) | A + B |
| 6 | receipt 1e headline: "a world minted by the LIT PRODUCT … carried into the boundaries" | **PARTLY** | At `livingContentLawWiring.test.js:699-767` the lit-minted world reaches **three arms only** — the gallery ingest and the two public projections. The reconciliation drop and the account remap / `versionHistory` hole are still driven by hand-built rosters. The receipt's own body says so; the headline overstates. | LOW | B |
| 7 | receipt 1a: the eager-closure proof stands in for the missing dist | **PARTLY** | It is a **MODULE-GRAPH proof and no byte claim in this lane was executed.** `vendorPdfLazy` = 27 passed / **27 skipped** (`dist` absent — *folder*: `ls dist` → no such directory); `sizeBaseline.test.js` contains **0** occurrences of `dist` (*folder*), so the dry ratchets do not stand in. Both worker bundles gained `densityCreateBoundary.js` and `livingContentLaw.js` as genuinely new static edges (`generationWorkerLazy`'s exact list 2 -> 3); emitted delta unmeasured. | MEDIUM | B |
| 8 | receipt R1: the strike "moves ONE frozen value and nothing else" | **PARTLY** | The `--write` also restamps **`frozenAtSha`** — `455ec96a4abace0ae7eb68d09e419820bacd5b5d` (*folder*, `.writer-reach-baseline.json:11`) -> the §917 sha, because `baselineOf()` rebuilds the object and stamps `sha: headSha(root)`. Every other measured figure is identical to the banked one. | LOW | C |
| 9 | receipt 1f, car title "the lit walkers" | **PARTLY** | The car's entire diff to `mechanismLitCoverage.test.js` is an **18-line header comment**; zero executable change, denominator unmoved. The receipt's own text is honest ("green and BLIND … and now says so"); the title buys documentation, not coverage. Nothing in the estate enumerates VERSIONED GENERATION LAWS and demands a lit proof of each. | LOW | B |
| — | ⭐ **NEW — the reconciliation boundary admits a FOREIGN birth law** | **NEW FINDING, uncovered** | `dropReconciledSettlementContentRecords` (`importReconciliationAdmission.js:475-499`) deletes `customContentProvenance` and `customContentRoster` and never touches `settlement.config`; the module names the law key **0** times (*folder*, `grep -c` = 0). `tests/lib/importReconciliation.test.js` carries **0** marker assertions (*folder*). The sibling archive-less importer's own rule (`importScrub.js:115-121`) forbids exactly this state — "a world that says it was born under the roster law while carrying no roster … lies about its own scope, permanently". **Lighting is what makes it bite:** before the flip a marked world could only arrive from a hand-lit build; after it, **every** reconciled foreign import lands marker-carrying and roster-less. Not a leak, not a byte-mover, nothing reds. | MEDIUM | B |

Also recorded, not against the lane: **clamp direction** (PARTLY, severity NONE — at the lit dial the
destructure is inert; the lit-direction guarantee comes from spread ORDER at
`densityCreateBoundary.js:272-276`, which is what the receipt already says) and **the persisted
birth-time delta is TWO keys, not one** (`config._livingContentLawVersion` on every BIRTH plus
`settlement.customContentRoster` at `generateSettlementPipeline.js:190` when the environment yields a
row) — a correction to the lens premise, stated correctly in car 1c.

**⚠ FENCE HAZARD, not the lane's.** Lens C observed `skepLIGHT` porcelain briefly = 1 at ~22:23 —
` M src/domain/content/livingContentLaw.js`, the car 1g `@type {number}` annotation stripped, restored
within ~30 s by md5 with mtime preserved: **a mutation plant by another lens on the same dock**, which
the fence forbids while a lens could be reading it. Sibling vitest runner counts of 41 and 56 seen in
the same window. Every lens's own porcelain is 0 before and 0 after; no figure in this fold is known to
be affected, but the two dial gates (`check-full-typecheck`, `check-domain-strict`) were run in that
window by more than one seat.

---

## 3 · THE CURES OWED BEFORE §917 — nineteen (4 code · 8 receipt · 7 rulings)

### (a) CODE — four

**A1. `src/lib/importReconciliationAdmission.js`** — inside `dropReconciledSettlementContentRecords`,
strip `LIVING_CONTENT_LAW_CONFIG_KEY` from `entry.settlement.config` alongside the two records (one
destructure, the shape `importScrub.js:158-166` already ships). **Gated on ruling C1** — if the chair
rules the other way, A1 becomes a written correction to `importScrub.js:115-121`'s general-case
paragraph instead. *Arm that proves it:* a new case in `tests/lib/importReconciliation.test.js` (today
**0** marker assertions) driving a LIT-product-minted world through admission and asserting the marker
absent, plus a plant that removes the strip and reds it. Blast-radius re-run: `importReconciliation`
18, `accountImportSlice` 26, `accountSettlementContentPortability` 17.

**A2. `tests/lint/.lighting-census-baseline.json:29`** — `"date": "2026-09-09"` -> `"2026-09-08"`.
*Arm:* `tests/lint/sovereigntyLightingContract.walker.test.js` re-run stays **34 passed** and the diff
is one numeral (the walker does not read the date; the proof is that no measured figure moves).

**A3. `tests/lint/densityCreateBoundary.walker.test.js`** — the payload arm asserts only that the
literal `await loadGenerationLawPayloads(` appears **somewhere in the file** of each named awaiter. An
await moved BELOW `runGeneration`, or into a skipped branch, keeps it green. Strengthen to an
ordering/live-branch check. *Arm:* a plant that MOVES (not deletes) the await in
`settlementGenerateAction.js` below `runGeneration` and must red. All seven are currently correct
(`settlementGenerateAction.js:158`, `generation.worker.js:54`, `customContentPreview.worker.js:27`,
`instantWorldBody.js:151`, `WorldPage.jsx:49`, `ConstructionPanel.jsx:86`,
`campaignContentBindingSession.js:89`) — this is prevention, so it may be deferred-and-recorded rather
than landed, at the chair's word (C6).

**A4. `tests/domain/livingContentPromiseBytes.test.js`** — widen the RATE arm to assert `verdict.v1` /
`verdict.v2` per row and to count `v1Roster`, so the two RATE cells the receipt prints are measured by
the file that prints them. *Arm:* the file's own count going 5 -> 5 with two new assertions (or 5 -> 6);
if it lands, the §917 test-census prediction moves (see §5).

### (b) RECEIPT — eight corrections to `$SC/receipt-light.md`

**B1** voice-E2 leg 3: "COMMENT, except two" -> **339 added src lines, 47 non-comment/non-blank**; the
conclusion stands on legs 1 and 2 (both files empty in `git diff --stat 8961388ce..990a7860a`; no em
dash in any added `src/domain`/`src/data` literal).
**B2** car 1b: delete "with the dormant branch still exercised through the leaf's closed test"; state
that the mint's `{}` branch is executed by nothing at this tip and what the leaf's arm actually covers.
**B3** car 1c table: mark the RATE row's "roster under law 1" and "wrong law" cells as **stated, not
asserted by that file** (or land A4 and delete this correction).
**B4** R1: "ONE frozen value and nothing else" -> **digest AND `frozenAtSha`**.
**B5** R3: re-price — roster **96/96** vs provenance **32/96** with the pack loaded; roster a strict
superset of provenance; roster exposure went **unreachable -> live** at car 1a. The refusal is unchanged.
**B6** car 1e: the ⭐ headline scope -> the lit-minted world reaches **three** arms (gallery ingest +
both public projections); the other boundaries remain hand-built rosters.
**B7** car 1a: label the eager-closure proof **module-graph only**; add that no byte claim in this lane
was executed, that `sizeBaseline.test.js` reads no dist, and that the two worker bundles' new static
modules are unmeasured in bytes.
**B8** car 1f: retitle/reframe — an 18-line header comment, zero executable change; name the gap
(nothing enumerates versioned generation laws for lit coverage) as deferred-and-recorded.

### (c) RULINGS FOR THE CHAIR — seven

**C1** The reconciliation marker: strip it (A1), or rule that a reconciled world keeps its foreign
birth law and `importScrub.js`'s paragraph is wrong about the general case. Note for the ruling: the
strip reclassifies an imported v2 world as v1 on a persisted shape, which is why `importScrub` carves
the ACCOUNT path out — but reconciliation is **archive-less and cross-account**, the gallery shape, and
the precedent is already landed, so this reads chair-level rather than owner-gated. Say which.
**C2** The writer-reach `--write` at §917 — the lane correctly refused it; the chair's door. Expect
digest + `frozenAtSha`, nothing else.
**C3** The census date numeral (A2) — worth a byte or not.
**C4** The R3 server twin: land the lit dial with `_gallery_dm_full_json` still passing both records, or
hold §917 on an owner-gated migration. **The re-priced sizing in B5 must reach the owner with the ask** —
the previous framing ("one more key on an already-open door") reads smaller than the measurement.
**C5** Does §917 owe a BUILD? No byte claim in LIGHT was executed. If yes, it needs a dock with
**cloned** `node_modules` (a symlinked dock poisons the edge bundle), then `vendorPdfLazy` with its 27
dist contracts live, `sizeBaseline`, the first-paint total against its ceiling, and a chunk-membership
read for `livingContentRoster.js` + `customContentManifest.js`.
**C6** A3 now, or deferred-and-recorded with the ordering gap named in the walker's header.
**C7** The fence breach — two lenses plant-testing one dock (the car 1g strip/restore at ~22:23). A
ruling for the next sitting on one-writer-per-dock during a skeptic pass.

---

## 4 · THE FIGURES THE LEDGER WILL CARRY

1. **The caller's reacher set — CONFIRMED, independently enumerated.** Over **2,211** `src/`
   `.js`/`.jsx` files, exactly **8** name the pipeline module path; two (`domain/locksPreservation.js`,
   `domain/regenerationPreservation.js`) only in doc comments, so **6 real importers**. Five are on
   `PIPELINE_REACHERS`; `store/settlementSlice.js` is off it **correctly** (it destructures only
   `regenNPCsPipeline`/`regenHistoryPipeline`, and the seam's single `src/` call site is
   `generateSettlementPipeline.js:185` inside `generateSettlementPipeline` itself — exactly two
   non-comment references to `livingContentRosterFor`, at `livingContentSeam.js:123` and
   `:185`). `store/settlementGenerateAction.js` reaches via `generationRequest.js` (`reachesVia`).
   **Seven await sites**, each preceding the pipeline call **inside the same async function**, none in a
   skippable branch. No unarmed script path (`observe-genesis-war-ramp.mjs` carries a top-level await;
   every other script hands the pipeline a markerless config). The outage is real and reproduced:
   `Error: [livingContentSeam] v2 world, roster payload not loaded` on a lit config with
   `customContent: {}` — so the loader is required on **every** lit generation, not only homebrew ones.
   PLANT A 1F/15P naming the exact offender; PLANT B 2F/14P; clean 16 passed either side.
2. **THE PROMISE, 525 and 768 — CONFIRMED and extended: 0 movers and 0 movers.** Not one byte other
   than the law's own declaration moved on **1,293** configurations (`livingContentPromiseBytes` 5
   passed). Independently: three seeds deep-diffed path by path return exactly **two** differing paths
   (`config._livingContentLawVersion`, `_config._livingContentLawVersion`), with dark law 1 / lit law 2
   on each; the dark corpora hashed at BOTH trees are **identical** — golden 525
   `adb022663d579c4fa7a4ceffd2391a376961d0d109471d76cfa73eab566d7596`, rate 768
   `8445dd42da0c2e57cea89d2884f95d9abc43b7ab9f10406ba0eed55879ef307f`; and **0 of 96** RATE towns with
   the reference pack loaded moved any byte once marker and roster were subtracted — the arm the
   receipt's own corpora runs (which pass `customContent: {}`) could not make. **Corrected:** two RATE
   cells in the receipt's table are stated, not measured by the file (row 4).
3. **ABSENT, not empty — CONFIRMED: 0 empty records of 525 and 0 of 768.**
   `Object.hasOwn(settlement, 'customContentRoster') === false` and the value `undefined` on every
   measured lit run with `customContent: {}` — no key at all, not an empty record —
   `livingContentRoster.js:261` being `if (rows === 0) return null;`. With the reference pack: roster
   present, `schemaVersion` 1, buckets exactly `deities · factions · stressors · traditions`, and the
   two worlds **byte-identical** once marker and roster are removed. R2's refusal (a frozen empty record
   widens the persisted shape on every world the product mints) is CONFIRMED as correctly owner-gated.
4. **The no-migration arm — CONFIRMED and non-vacuous: three hops, all clean.** Structurally there is no
   read-path writer to find: `LIVING_CONTENT_LAW_CONFIG_KEY` has exactly three uses outside its leaf
   (`livingContentLaw.js:209`, the only write, inside the mint; the clamp destructure at
   `densityCreateBoundary.js:269`; the gallery strip at `importScrub.js:163`), and the mint has exactly
   two callers, both classified BIRTH. The arm's hops are the LOAD round trip, `regenSection('npcs')`
   and `deepClone`; all three leave the marker absent, the resolved law at 1 and the roster absent.
   **Plant on `src/domain/clone.js` convicts: 2 failed / 17 passed**, both the UNDO/CLONE and the
   NO MIGRATION arms, restored `cmp` IDENTICAL. Recorded weaknesses: the LOAD hop is a test-local
   `JSON.parse(JSON.stringify(...))`, not a production loader, and `regenSection` structurally cannot
   write `config`, so that hop is a future guard rather than a live measurement.
5. **The three holes' arms lit — CONFIRMED, seven suites reproduce figure-for-figure:** `importScrub`
   **13** · `campaignSlice.galleryImport` **6** · `importReconciliation` **18** · `accountImportSlice`
   **26** · `accountSettlementContentPortability` **17** · `livingContentRosterPublicDrop` **7** ·
   `livingContentLawWiring` **19**. The lit-product-minted world reaches **three** of those arms (row 6).
   **And the hole none of them closes:** reconciliation keeps a foreign birth-law marker while dropping
   its roster (the NEW MEDIUM row) — 0 key occurrences in the module, 0 marker assertions in its test.
6. **The eager closure 263 -> 263 — CONFIRMED, re-derived at both trees by two lenses.**
   `EAGER_FIRST_PAINT_MODULES` from each tree's own `vite.config.js`: base `8961388ce` **263**, tip
   `990a7860a` **263**. `densityCreateBoundary.js`, `livingContentSeam.js`, `livingContentLaw.js`,
   `livingContentRoster.js`, `livingContentLawVersion.js` and all seven awaiters are **absent from the
   eager set in both states**. The excision inference checks out: `ENGINE_SHARED_DOMAIN_EXCISIONS` has
   18 entries, the roster is not among them, and `computeEagerModuleGraph()` seeds from
   `[...ENGINE_SHARED_DOMAIN]` **after** excisions (`vite.config.js:223` deletes, `:290` seeds, `:300`
   computes) — so the roster's absence also proves it never entered `ENGINE_SHARED_DOMAIN`.
   `livingContentSeamLazy` **6 passed**. **This is a module-graph proof; no byte was executed** (row 7).
7. **The gates at the tip `990a7860a` — CONFIRMED as quoted, six re-run and matched, two verbatim.**
   `writerReach.walker` **1 failed / 55 passed (56)** — the digest arm only, expected
   `ac4734954adbaee04755427934180b8f129c0b0df21072ec00f49b7e797bd1b0`, received
   `14a3b59541fe2288628da0a815192e34c8138d55f73e98ffefae63e902076501`, and the strike is **lawful, not a
   chair ruling** (clause D-dial's own failure text at `writer-dark-register.mjs:383` instructs the
   retirement; the retired row's own `lighting` prose said the same), with **no growth violation** and
   the arms **widened, not weakened**. `tests/lint` holds **149** test files (the 2490/2491 totals are
   UNTESTED). `check-domain-strict` **exit 0, 1120 errors / ceiling 1120**, baseline not widened, and
   PLANT C (deleting the `@type {number}`) reproduces the 1g red exactly. `check-full-typecheck`
   **exit 0, 173/173**. OSR **exit 0, 1972 findings**, drift 0, no write. prose-numerics dry
   **`baseline=225 live=225 parseErrors=0 · exact=225 rekeyed=0 relocated=0 FELL=0 NEW=0`**.
   writer-reach plain **exit 0 — judged 6532 · LIT 560 · LIT-NAME 4646 · DARK 1326 (reviewable 526)**,
   identical to the banked population, baseline file untouched. lighting census walker **34 passed**,
   and its tuple reproduces independently (`files 2556 -> 2557` by `git ls-tree`; `titles +15`,
   `suiteTitles +3` counted at source, none `.skip`/`.runIf`/`.todo`). eslint **exit 0, 31 warnings**
   over 19 files, **overlap with the 38 touched files EMPTY**. `vendorPdfLazy` **27 passed / 27
   skipped** (no dist). voice E2 **1 failed / 18 passed, inherited** — both named files empty in the
   diff, chair-predicted at §916 — with leg 3 of its argument REFUTED (row 1). The consist is
   **38 files: 15 `src/`, 3 `scripts/`, 20 `tests/`** (*folder*-verified), nothing under
   `stateProse`/`prose`/`dossierStateProse`, and the only census file touched is the one the brief
   permits. Eight commits, each carrying all three trailers. porcelain **0**, vitest runners **0**.

---

## 5 · WHAT THE §917 REGISTER DOORS WILL MOVE

| door | moves | ground |
|---|---|---|
| **writer-reach `--write`** | `registerDigest` `ac473495…` -> `14a3b595…` **and `frozenAtSha` `455ec96a4abace0ae7eb68d09e419820bacd5b5d` -> the §917 sha**. Nothing else. `writerReach.walker` then goes green. | `baselineOf()` rebuilds the object and stamps `sha: headSha(root)`; population, closureSizes, scanStats, the three digests, `pendingSurfaceCeiling` 3, `reviewableDarkCount` 526, `darkUnregistered` 1322 are identical on the plain run. The dry run exits 0, so the shrink-only refusal will not fire. |
| **test-census ratchet `--update`** | `totalFiles` **2502 -> 2503**, `totalTests` **32436 -> 32451**, plus `measuredAtSha` (banked today `0c9384e186…`). | +1 test file, +15 test blocks, counted at source in two lenses with the per-file split matching the receipt's hand count (`livingContentLawWiring` 12->19, `densityCreateBoundary.walker` 14->16, `livingContentRosterPublicDrop` 6->7, `livingContentPromiseBytes` 0->5); none skipped or conditional. **⚠ Run it AFTER cures A1/A4 — each adds tests and moves this prediction.** |
| **lighting census** | **nothing** — the door is not run at §917 (the plain walker is the receipt, 34 passed). If cure A2 lands, exactly one numeral moves and no measured figure does. | Car 1g after the refreeze moved no test file. |
| **OSR** | **nothing** | exit 0 at exactly 1972, zero drift lines. |
| **prose-numerics `--write`** | **nothing** | the dry run is already exact: `exact=225 rekeyed=0 relocated=0 FELL=0 NEW=0`. |
| **mounts baseline · tuning register** | **nothing** | neither surface is in the 38-file consist. |

---

## 6 · UNTESTED, AND WHAT WOULD TEST EACH

| # | untested | what would test it |
|---|---|---|
| U1 | the whole-`tests/lint` totals **2490 passed / 1 failed (2491)** and the **148/1** file split — only the 149 file count was re-counted; running the directory was outside every lens fence | the chair runs whole `tests/lint` once at the tip; expect the single writer-reach digest red |
| U2 | **every byte claim** — chunk membership for `livingContentRoster.js` + `customContentManifest.js`, the first-paint total against its ceiling (the seam's header cites 1,045,910 -> 1,095,584 vs a 1,047,000 ceiling), the engine chunk, and the two worker bundles' emitted delta from their two new static modules | a vite build in a dock with **cloned** `node_modules`, then `vendorPdfLazy` with its 27 dist contracts live, `sizeBaseline`, and a chunk-membership read (ruling C5) |
| U3 | the mint's dormant `{}` branch — executed by nothing at this tip | an arm that calls the **real** `newSettlementLivingContentLaw()` with the dial forced to DEFAULT by mocking the version leaf rather than replacing the function, asserting `{}` |
| U4 | the PREVIEW half of "every BIRTH mints the law, and nothing else does" — held statically by the walker, not by an executed preview | an executed preview through `customContentPreview.worker.js` / `ConstructionPanel.jsx` asserting no marker on the produced config (same shape it had before the lane) |
| U5 | "`generatorGoldenMaster` is red at the base commit exactly as here (525/525 drifting)" — receipt-only; the three corpus md5s are confirmed, the base run is not | run `generatorGoldenMaster` once in `laneSEAM` at `8961388ce` |
| U6 | whether the walker catches an await **moved** rather than deleted — believed green-through (PLAUSIBLE, unexecuted) | the relocation plant in cure A3 |
| U7 | `check-test-ratchet.mjs --update`'s real output against the +1/+15 prediction | the chair's ratchet ritual at §917, after A1/A4 |
| U8 | the blast radius of cure A1 (stripping the reconciled marker) | `importReconciliation`, `accountImportSlice`, `accountSettlementContentPortability`, `importScrub` re-run after the change, plus the removal plant |
