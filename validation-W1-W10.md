# FABLE VALIDATION SEAT — SLICE W1–W10 (R-i, R-ii, R-iii, R-iv, R-vi, R-viii′, W1–W10, APPENDIX NF-1…NF-6)

**Seat.** Fable 5.1, retrospective, 2026-09-12. Read-only on the repo; this file is the one output.
**Record read whole, in order:** `RETRO-VALIDATION.md` · `fidelity-W1-W10.md` · `items-W1-W10.md` · `evidence/convert-W1-W10.md` · `evidence/CONTRADICTION-TABLE.md` (head, §F1/F2/F4 rows cited, §R, appendices) · `verbatim/BRIEF-pre-recut-ADDENDA-1-to-13.md` (ADDENDUM 12, its amendment, ADDENDUM 13A) · `verbatim/rulings-DEF2.as-run.txt` · `evidence/measure-prose.py` + `measurement-output.txt`.
**Every citation resolved in the dock** `/private/tmp/…/scratchpad/dock-f2da5a3ee` (HEAD = `f2da5a3ee REWRITE car 8b-W-5-lighting`), never on the ledger branch.

**Labels.** CONFIRMED = I opened the file / ran the command and quote it. PLAUSIBLE = reasoning only.

---

## 0. PROTOCOL STEP 1 — THE MEASUREMENT RE-RUN (CONFIRMED)

Both invocations from the script header, run against the dock (which holds all four revisions as commits):

```
$ python3 measure-prose.py <dock> docs/content/RECEIPT_POOLS_DOSSIER_STATE.md '### DS-DEF-2:' '### DS-DEF-3:' f2da5a3ee 471ce894a 1900
SHIPPED  f2da5a3ee: units 78 | sample 78u/1905tok | DISTINCT 519 | content types 489 | mean words 24.4 | ',and' joins 63%
AFTER    471ce894a: units 312 | sample 108u/1902tok | DISTINCT 168 | content types 143 | mean words 17.4 | ',and' joins 74%
$ python3 measure-prose.py <dock> docs/content/RECEIPT_POOLS_DOSSIER_STATE.md '### DS-DEF-11 ' '### ' f73bdbf16 5cfc02000 1200
SHIPPED  f73bdbf16: units 12 | sample 12u/244tok | DISTINCT 124 | content types 103 | mean words 20.3 | ',and' joins 58%
AFTER    5cfc02000: units 48 | sample 48u/735tok | DISTINCT 125 | content types 104 | mean words 15.3 | ',and' joins 69%
```

Identical to `measurement-output.txt` in every figure and every top-word list. **The warrant reproduces.** (Note for a successor: the script's `<block-heading>` must be the literal prefix `### DS-DEF-2:` — with the colon; the record does not say so.)

---

## 1. THE FACTS OF THE DOCK THAT THIS SLICE TURNS ON (all CONFIRMED by reading the code)

These five facts decide most of the verdicts below, so they are stated once.

| # | fact | where |
|---|---|---|
| D1 | **`WALLED-STRAINED` fires on a walls-only town.** The key reads `walls` and `militaryGate < 1` and NO force flag (`defenseStateProse.js:1055-1062`). `economicGates.military` is recorded whenever `hasAnyDefense` (`defenseGenerator.js:468`), and `hasAnyDefense = inst.hasWalls \|\| inst.hasGarrison \|\| …` (`:177-178`) — walls alone satisfy it. So a walled town with no garrison, militia, watch, mercenary or charter and `econOutput < 50` renders STRAINED. | `defenseStateProse.js:1055-1062`; `defenseGenerator.js:177-178`, `:189-191`, `:468` |
| D2 | On that town the SAME tab prints **"Walls present but no organized military force to man them."** (`Invasion & War` row, `threatAssessment.js:119`) and, under `plagued`, **"Walls exist but no organized force to sustain a watch rotation"** (`:66`); on the Unsafe band the safety panel prints **"There is no meaningful guard presence."** (`safetyProfile.js:300`). | `threatAssessment.js:66`, `:119`; `safetyProfile.js:300` |
| D3 | **`passageShapes.js` is NOT a composer.** Its header says so ("at car 8a nothing ships a second shape, and `composeStateProse.js` is not a caller"); `grep -rln passageShapes src scripts` returns only `passageShapes.js`, `composedWalker.js` (which merely exports the stop list TO it, `:473`), `scripts/prose-shape-report.mjs` and mutation manifests. `composeStateProse.js` has no import of it. And its own `lawfulPassageShapes` answers `applicable:false` for a unit with no modifier ("A UNIT WITH NO MODIFIER HAS NO SHAPE QUESTION"). | `passageShapes.js:9-13`, `:118-125`; `composeStateProse.js` (no import) |
| D4 | **The instrument's own floor-2 rows refuse, by name, three phrases this slice's permission cells license:** F2-05 lists *"thinner than it was"* and *"the durative"* as an ELAPSED COURSE; F2-06 withdraws *"the gate stands open more often than not"* as a RATE ("⛔ The chair's own ADDENDUM 14 example is withdrawn in its rate form"); F2-03 refuses *"a raising … narrated"*; F2-09 says *"No state-prose pool key reads a history field, so a historical allusion desyncs on a button press"* (grounded: `historyPreservation.js:1-5` — "Reroll history replaces `settlement.history` wholesale"; `ageBands.js:52` `HISTORICIZE_BAND = 'years-past'`). | `CONTRADICTION-TABLE.md:255-263`; dock files as cited |
| D5 | **The holder table is a record-keeping enumeration whose citations must RESOLVE.** `HOLDER_KINDS` at `holderTable.js:78-79`; the muster entries at `:188-195`; and the table's own F1-24 quotes `composedWalker.js:1071-1073`: "the holder kind resolves to no institution in this town's roster, so the cited record has nobody keeping it". R-8 keeps that: "a roll CITED as a record needs the holder". | `holderTable.js:78-79`, `:188-195`; `CONTRADICTION-TABLE.md:103`, `:319` |

---

## 2. THE ROW-BY-ROW TABLE — 16 items (3 SPLITs both halves, 13 STRUCK) + 6 APPENDIX rows

Columns: (a) verbatim cell vs source · (b) ground: real field, line reads so in the dock, engine behaves so · (c) permission safe? — with the WORST sentence composed for every STRUCK item and the hunt for a denier.

| id | recorded | (a) verbatim | (b) ground | (c) permission — worst sentence → denier | MARK |
|---|---|---|---|---|---|
| **R-i** | STRUCK | OK — matches ADDENDUM 12 R-i character for character (CONFIRMED by eye against `BRIEF…:113`). | **FALSE.** `passageShapes.js:22-30` is a header comment; the mechanical half (`nounCarry`, `lawfulPassageShapes`) governs shape 2 only, answers `applicable:false` at k=0 (D3), **and is not wired to the composer at all** (D3). "None exists" is added beyond the packet (fidelity F2). | Worst: a spine whose second sentence opens "It has not been tested since the road moved" with no antecedent. Hunt: no field, flag, band or roster row is made false by a dangling referent — it is incoherence, not contradiction. So the STRUCK-as-lawfulness verdict survives. But the cell's promise "The composer will simply not draw the shape that would break" is FALSE at the dock, and the residual (a face-grain incoherence) has no instrument, because the CRAFT verdict is pool-grain. | **CHALLENGE** — verdict stands; ground is false on both legs (inapplicable at k=0; unwired at k≥1); the permission's safety promise is not true. CONFIRMED. |
| **R-ii** | STRUCK | OK (CONFIRMED). | Partly. `moveGrammar.js` exists and is "the classifier that CHECKS a variant's tag … PURE, HEADLESS. Nothing here runs at the draw"; it types a face's moves. It cannot count negated openers per variant, so the archive's added clause "the cap added nothing an instrument was not already doing" is unsupported (fidelity F2, **unrepaired** — still present). The packet's real ground ("names no field") holds. | Worst: every variant of a pool opens "No one has …/Nothing has …". Hunt: no field denies a negated opener; F1-34 and F2 bind the content, not the opener. Safe. | **OK on verdict · CHALLENGE on the ground's unsourced clause.** CONFIRMED. |
| **R-iii** | STRUCK | OK (CONFIRMED). | Correct: no field; "a standpoint the card refuses" is the abolished objection. | Worst: "{settlement} asks nothing of its {defwork}" on WALLED-QUIET. Hunt: QUIET fires only on the `settled` family (`defenseStateProse.js:1061-1062`), beside which the Beasts row prints "Safe heartland…" — no denial; the totality edge is F1-34 and the cell names it. Safe. | **OK.** CONFIRMED. |
| **R-iv** | STRUCK | OK (CONFIRMED). | Correct: angle tags are row metadata; `grep angle DefenseTab.jsx` → nothing rendered (CONFIRMED for the tab; PLAUSIBLE for the PDF). | Worst: an `[unfolding]` variant that never realises OPEN. Hunt: nothing rendered carries the tag. Safe. | **OK.** |
| **R-vi** | STRUCK | OK (CONFIRMED). | Half-true. `holderTable.js:78-79` (the record cites `:71`, which lands in the doc comment three lines above `HOLDER_KINDS`; imprecise, not wrong) and `:188-195` do file the wall and force fields under `muster`. But the same layer REFUSES a citation whose holder resolves to no institution (D5), and the instrument keeps that as F1-24 / R-8. So "positively supported" is true only where the muster kind resolves. | **Worst: "The roll shows the muster carried standing at {settlement}."** on a walls-only STRAINED town (D1). Denied on the same tab by `threatAssessment.js:119` "Walls present but no organized military force to man them" and, on the Unsafe band, `safetyProfile.js:300`. The row's permission cell is unconditional ("Say 'the roll shows'… The one bar left is floor 2") — it is wider than the instrument the fold itself produced (R-8: "a roll CITED as a record needs the holder"). | **REVERSE → SPLIT.** Struck half: the citation-budget bar. Kept half: a roll CITED needs a resolving holder — floor 1 — `threatAssessment.js:119`, `composedWalker.js:1071-1073` (F1-24 / R-8). CONFIRMED. |
| **R-viii′ KEPT half** | SPLIT-kept, floor 4 | OK; the quote stops one sentence short of the cure sentence (fidelity F6, **unrepaired**), substance intact. | **TRUE.** `defenseGenerator.js:184-191` verbatim: "Floor 0.6: built walls keep standing and unpaid soldiers desert slowly" (`:186-187`); `milUpkeepMult = Math.min(1, 0.6 + (econOutput / 50) * 0.4)` (`:189`) applied to the funded portion above `communityMilBase` (`:191`). Landed as F4-02. | "May NOT write a wall that pays for itself" — safe and correct. | **OK.** CONFIRMED. |
| **R-viii′ STRUCK half (no headcount)** | SPLIT-struck | as above | The strike's reason (R-DST-A was a cell-overlap rule, not a truth) is arguable and I do not reverse it on that leg. But the engine does not RUN desertion at birth — the multiplier is a static reduction; every birth condition sits at `elapsedTicks: 0`; `HISTORICIZE_BAND` refuses temporal register below `years-past` (D4). ET-2's D-31 (MODEL) explicitly keeps "never a thinning muster, never desertion" and R-9 overrides it — the fold contradicts itself on this half. | **Worst (and it is the cell's own example): "the roll thinner than it was", "the men drifting off".** Denied by the instrument's own **F2-05**, which lists *"thinner than it was"* and *the durative* verbatim as refused ELAPSED COURSES (`ageBands.js:52`; `candidateEvents.js:279` et al. `elapsedTicks: 0`). Second denier: on a walls-only STRAINED town (D1) "the men", "posts unfilled" are floor 1 against `threatAssessment.js:119`. R-9 ("men drifting off slowly is licensed") and F2-05 cannot both stand. | **REVERSE — the permission, not the strike.** Should read: the cell-overlap bar is struck; what replaces it is a **standing state only** ("the muster is thin", "posts stand unfilled") — never a course, never a comparative against a past, and never on a key where no force row resolves. Fields: `ageBands.js:52` / F2-05 (floor 2); `threatAssessment.js:119` (floor 1). CONFIRMED. |
| **W1** | STRUCK | OK (CONFIRMED). | `holderTable.js:188-193` reads as cited. But a holder row says which RECORD keeps a field; it models no possession between a structure and a force. The ratified entailment law (13A item 1: "what the word MEANS, never what the thing typically HAS") forbids reading it as one. ADDENDUM 12 recorded this construction as the vector to "the shipped watch by another route". | **Worst: "The palisade at {hamlet} and the gate's watch…"** — the cell licenses *"the gate's watch"* by name. Denied on every town where `inst.hasWatch` is false (`priorityHelpers.js:48`; catalogue seats a watch town-plus; DS-DEF-11 WALLED-* fires at every tier) by `safetyProfile.js:300`/`:309` — the instrument KEEPS this as F1-01. Second: "its muster" on a walls-only STRAINED town → `threatAssessment.js:119`. | **REVERSE → SPLIT.** Struck half: the grammar-binding rule (which possessor a pronoun takes). Kept half: a possessive may not SEAT a body the roster denies — the permission's "the gate's watch" is withdrawn. `priorityHelpers.js:48`, `safetyProfile.js:300`; floor 1. CONFIRMED. |
| **W2 KEPT half** | SPLIT-kept, floor 1 | OK (CONFIRMED). | **TRUE.** `threatAssessment.js:113-130` builds `Invasion & War` for every town (`:117` "inadequate against any professional force", `:125` "Cannot resist organized military aggression", label `:128`); consumed at `DefenseTab.jsx:183` `buildThreatAssessment(r)`. `stressGenerator.js:121` `heartland → ×0.3`, `:168` `×0.4` (occupied), `:231` `×0.4` (wartime) — exact. Landed as F1-34. | "nothing threatens {settlement}" refused — correct. | **OK.** CONFIRMED. |
| **W2 STRUCK half** | SPLIT-struck | as above | Correct: phrasing over the country made nothing true or false. | **The cell's own example "the gate stands open more often than not" is a RATE that the instrument's F2-06 withdraws by name** ("'The gate stands open' is lawful; 'the gate stands open more often than not' is not"). "children play on the earthwork" names a material row — fine only on a palisade/earthwork row (F1-32). | **CHALLENGE** — verdict right; the permission cell carries an example its own instrument refuses (floor 2). CONFIRMED. |
| **W3 KEPT half** | SPLIT-kept, floor 1 | OK (CONFIRMED). | TRUE on the strip: `OverviewTab.jsx:247` `{r.tier}` capitalised beside name and population. **Citation error:** `RECEIPT_POOLS_DOSSIER_STATE.md:6200` is DS-GEN-17; the `UNWALLED-SMALL`/`-LARGE` key line is `:5966` and the pools `:5985-5990`. Ground true, line wrong. Landed as F1-31. | Safe. **Open gap the row does not see:** is "town" a TIER word or the estate's generic? The shipped `UNWALLED-LARGE` v2 ("A town of {settlement}'s weight") fires on city/metropolis, and every DS-DEF-11 face says "the town" on a thorp. F1-31 as written refutes all of them; nothing in the table rules "town" generic. | **CHALLENGE** — citation off by 234 lines; F1-31 needs a ruling on generic "town" or it refutes the shipped corpus. CONFIRMED. |
| **W3 STRUCK half** | SPLIT-struck | as above | Correct: spelling the band is receipt language. | Safe. | **OK.** |
| **W4** | STRUCK | OK (CONFIRMED). | No field — correct. But the replacement (pool-grain CRAFT/DULL) had not run at record time, and the grains differ: W4 named WHICH variant moved; a DULL verdict cannot. The exposure is systemic, not one-off: W6, R-ii/W10 rest on the same never-run arm (fidelity F4, **unrepaired**). I cannot verify the CRAFT verdict's status from the ledger checkout (`rewrite/` is absent here) — PLAUSIBLE that it now exists in the lane. | Worst: every variant of a pool rewritten onto one construction. Hunt: no field; it is dullness. Safe as lawfulness. | **CHALLENGE** — the honest form is SUSPENDED pending the CRAFT verdict's first firing, and the same applies to W6 and R-ii/W10. PLAUSIBLE on the instrument's status; CONFIRMED on the grains. |
| **W5** | STRUCK | OK (CONFIRMED). | Half-right: the maxim-frame half is tone. But the ground itself concedes "the one true edge inside it is the tier word", then the permission cell licenses that edge **by name**. | **Worst: "A town of {Thorpname}'s weight has never needed a wall."** on a Thorp — beside "Thorp" on the identity strip. Denied by `OverviewTab.jsx:247` (F1-31, W3's kept half). The trailing "Only the tier the strip prints binds" does not undo a licence stated in the same breath (fidelity F5, **unrepaired**). | **REVERSE → SPLIT.** Struck half: the maxim-frame tone bar. Kept half: a tier word inside a generic frame = F1-31. `OverviewTab.jsx:247`; floor 1. CONFIRMED. |
| **W6** | STRUCK | OK (CONFIRMED). | Tone — correct. Same CRAFT dependency as W4. | Worst: a beat landed twice in one face. Hunt: nothing false. Safe. | **OK** (with W4's systemic note). |
| **W7** | STRUCK | OK — "W7 THE ROLL — R-vi." is exact; the flagged parenthetical is exact (CONFIRMED). | As R-vi. | As R-vi — the same unconditional "the roll may be shown, cited" on a key that can fire with no force (D1, D2). | **REVERSE → SPLIT** (with R-vi; two rows, so the cost is doubled exactly as the record warned). CONFIRMED. |
| **W8** | STRUCK | OK (CONFIRMED). | Three stacked tone rules — correct as far as it goes; "let the ledger cite its own source" inherits R-vi's defect. | Worst: "The books at {settlement} show the muster paid" on a walls-only STRAINED town → `threatAssessment.js:119` (and F1-24). "Let the visitor see the town spending on its wall" is safe (F4-02 binds only a SPLIT purse, which the cell does not license). | **CHALLENGE** — the citation permission must carry F1-24/R-8. CONFIRMED by inheritance. |
| **W9** | STRUCK | OK (CONFIRMED). | Correct: a counting meta-rule; the entailment table is now the refuters' label-trap instrument and compels nothing. | Worst: a face that names no attribute at all. Hunt: nothing false. Safe. | **OK.** |
| **W10** | STRUCK | OK (CONFIRMED). | As R-ii (the unsourced clause is here too: "did not red"). | Safe. | **OK on verdict · CHALLENGE on ground** (with R-ii). |
| **NF-1** | NEW FINDING (routed) | n/a (packet words quoted exactly). | **Path wrong:** `src/domain/prose/composeStateProse.js` does not exist; the file is `src/domain/display/stateProse/composeStateProse.js`, and `:679` is inside the FACT BUDGET doc comment (`:678-690`, "`k <= 3 - \|spine.reads\|`") — substance holds. **"As `passageShapes.js` already does" is FALSE** (D3): nothing refuses any shape at composition; the shipped shape always seats the added sentence after a spine that may be another pool's. | Nothing handed back — correct. But W1's cell leans on this cure ("the composer will…"), and the cure is unbuilt. | **CHALLENGE** — fix the path; strike "already"; the composer refuses nothing today. CONFIRMED. |
| **NF-2** | ⚠ NEW BAR, "floor 1" | exact. | `OverviewTab.jsx:251` `{hist.age} years old`, `:259` `"{hist.historicalCharacter}"` — CONFIRMED. **But the table files this as F2-07, floor 2**, not floor 1 as the appendix says. And the table's F2-09 + `historyPreservation.js:1-5` ("Reroll history replaces `settlement.history` wholesale") + F2-02 (`hist.age` is user-settable and rerollable) mean the printed age is a MOVING target for a pool whose key reads no history field — which DS-DEF-11's key does not (`defenseStateProse.js:1055-1065` reads walls, threat, gate, tier). | **Worst (the cell's own example): "raised by someone's grandfather."** Denied by **F2-03** (a raising narrated) and **F2-05** (a two-generation elapsed course), and by **F2-09** the moment the DM presses Reroll history: the four-hundred-year town becomes a forty-year one and the face has no key that moves with it. "MAY now write age-flavour freely" is false on this desk. | **REVERSE.** The bar as rowed is under-cut: on any pool whose key reads no history field, age-flavour is refused outright (F2-09), and a raising/founding is refused everywhere (F2-03); only a comparative against a same-page permanent feature ("older than the road") survives. `historyPreservation.js:1-5`, `OverviewTab.jsx:251`; floor 2. CONFIRMED. |
| **NF-3** | ⚠ NEW BAR, floor 4 | exact. | **TRUE.** `defenseGenerator.js:189` (0.6), `:223` (0.7), `:251` (0.65), `:614` (`disasterGate = Math.min(1, 0.55 + (_econOut / 50) * 0.45)`); all monotone in one input. Landed as F4-03. Note: `economicGates.military` exists only when `hasAnyDefense`, `internal` only when `hasLawInfra` (`:468-470`), so on a hamlet with no paid stack the bar is vacuous, not wrong. | Narrows R-viii′ correctly. | **OK.** CONFIRMED. |
| **NF-4** | ⚠ NEW BAR, floor 4 | exact. | TRUE: floors at `:189/:223/:251/:614`; `communityMilBase` exempt (`:191`). ET-2's D-31 adds the nuance the row lacks: at town+ the community base is 0, so the FLOOR alone does the bounding there. Landed as F4-04. | "there is no watch left to pay" — refused here as MODEL, and also F1-01 where `hasWatch` is false; both hold. | **OK.** CONFIRMED. |
| **NF-5** | ⚠ NEW BAR, floor 1 | exact. | `contradictions.js:134-153` `detectOversizedInstitutions` — classification `interesting_tension` with consequences — CONFIRMED. Rendered: `contradictions` consumed by `ViabilityTab.jsx` (`:288`, `:313`, `:334` print `description`) and `pdf/sections/ViabilityAssessment.jsx` — rendering of the DESCRIPTION confirmed, of the classification word PLAUSIBLE. Reach is narrow: `if (tier !== 'village' && tier !== 'hamlet') return []` (`:138`) and only `OVERSIZED_PATTERN` rows. Landed as F1-112 with the right condition ("where the module has classified one"). | Safe. | **OK.** CONFIRMED (reach caveat noted). |
| **NF-6** | NEW FINDING (engine defect, to owner) | exact. | `threatAssessment.js:59`, `:66`, `:80` hardcode "Palisade…" — CONFIRMED; `hasWalls = (inst.walls \|\| []).length > 0` (`:40`) so any wall-class row fires them. **Path wrong:** the buckets file is `src/domain/institutions/defenseInstitutionBuckets.js:84-87`, not `src/domain/prose/…`. | Nothing handed back — correct. | **OK on substance · CHALLENGE on the path.** CONFIRMED. |

### R-v — the §6.2 ruling

`grep -n 'R-v[^i]' items-W1-W10.md` → **no hits.** R-v is not rowed in `items-W1-W10.md`; it has no `old law` cell, no ground cell and no VALIDATION cell anywhere in the record. Its only disposition is the prose of `RETRO-VALIDATION.md §6.2` ("R-v IS STRUCK … A writer may state more than one fact in a unit"). The same is true of R-vii (`grep 'R-vii[^i]'` → 0) and of the unprimed R-viii (appears only inside R-viii′'s quoted parenthetical). **The ruling is therefore un-validatable by this protocol as it stands.** On the merits, PLAUSIBLE OK: two facts in one unit make nothing false; the residual (a fused agent asserting a relation no field computes, referent-law rule 3) is a different rule and lives in the W20–W27 slice. It needs a row.

---

## 3. FIDELITY-REPAIR CHECK (`fidelity-W1-W10.md`, seven findings)

| # | finding | repaired? | evidence (CONFIRMED by grep/read of `items-W1-W10.md` as it stands) |
|---|---|---|---|
| 1 | NF 2–6 absent; seven citations vanished | **YES, with two defects** | APPENDIX rows NF-1…NF-6 present; all seven dropped citations present (`OverviewTab.jsx:251`, `defenseGenerator.js:189`/`:186-192`, `contradictions.js:136-153`, `threatAssessment.js:59`, `defenseInstitutionBuckets.js:84-86`, `composeStateProse.js:679`). Defects introduced by the repair: two file paths are wrong (`src/domain/prose/composeStateProse.js`, `src/domain/prose/defenseInstitutionBuckets.js`); NF-2's floor label (1) disagrees with the table's F2-07 (2). |
| 2 | R-ii ground strengthened beyond the packet; R-i "and none exists" | **NO** | `grep -c 'added nothing an instrument'` → 1; `grep -c 'and none exists'` → 1. Both stand verbatim. |
| 3 | packet's positive floor-3 clearance dropped | **NO** | `grep -c 'floor 3'` → 0; `grep -c deity` → 0; the only "scope" hits are the tally's `SCOPE 0`. |
| 4 | unbuilt-CRAFT exposure named for W4 only | **NO** | Closing §5 still reads "This is the one strike in the slice"; W6 and R-ii/W10 rows unchanged; the only `SUSPENDED` hit is the closing section's own remedy sentence. |
| 5 | W5 licenses the construction W3's kept half refuses | **NO** | `grep -c collision` → 0; W5's cell still licenses "a town of {settlement}'s weight" by name. (And it is worse than the packet said — see W5 REVERSE.) |
| 6 | R-viii′ quote stops before its cure sentence; R-viii's prior licence unrecorded | **NO** | The only `keeps itself` hit is inside the quoted R-viii′ text; the cure sentence and the 01:0x reversal history are absent. |
| 7 | R-v / R-vii / R-viii have no disposition line | **NO in the items file · PARTIAL in the record** | Items file: 0 hits for each. `RETRO-VALIDATION.md §6.2` disposes R-v (STRUCK) and R-vii (wiring row) in prose; R-viii still nowhere; none has a VALIDATION cell. |

The chair's §6.1 claim that "every such cell is now prefixed `[archivist's reading]`" holds for this file: 3 prefixes (R-ii, R-iii, NF-3's floor attribution). No `[corrected]` cell exists in this slice.

---

## 4. COVERAGE

22 rows validated / 22 in the file (16 verdict rows — 3 SPLITs both halves, 13 STRUCK — plus 6 APPENDIX rows), plus the un-rowed R-v.

| class | items | OK | CHALLENGE | REVERSE |
|---|---|---|---|---|
| SPLIT kept halves (R-viii′, W2, W3) | 3 | 2 | 1 (W3: citation + generic "town") | 0 |
| SPLIT struck halves | 3 | 1 (W3) | 1 (W2: rate example) | 1 (R-viii′: the permission licenses an elapsed course) |
| STRUCK | 13 | 5 (R-iii, R-iv, W6, W9, W10*) | 4 (R-i, R-ii*, W4, W8) | 4 (R-vi, W7, W1, W5) |
| APPENDIX | 6 | 3 (NF-3, NF-4, NF-5) | 2 (NF-1, NF-6 paths) | 1 (NF-2) |
| un-rowed | R-v | — | not validatable (no row) | — |

\* R-ii/W10: verdict OK, ground challenged.

---

## 5. REVERSALS

| id | recorded | should be | field:line (dock f2da5a3ee) · floor | the false sentence the permission would license |
|---|---|---|---|---|
| **R-vi** | STRUCK | SPLIT — the citation-budget half struck; **a roll CITED needs a resolving holder** kept (already F1-24 / R-8 in the table; the row and its cell are wider than the instrument) | `threatAssessment.js:119`; `composedWalker.js:1071-1073`; `safetyProfile.js:300` · floor 1 | "The roll shows the muster carried standing at {settlement}." — on a walls-only WALLED-STRAINED town (`defenseStateProse.js:1055-1058` reads no force flag; `defenseGenerator.js:177-178`, `:468`), beside "Walls present but no organized military force to man them." |
| **W7** | STRUCK | SPLIT, as R-vi | as R-vi | as R-vi (two rows; the record's own warning that a wrong strike here costs double is borne out) |
| **W1** | STRUCK | SPLIT — the grammar-binding half struck; **a possessive may not seat a body the roster denies** kept (F1-01's field) | `priorityHelpers.js:48`; `safetyProfile.js:300`, `:309` · floor 1 | "…and the gate's watch." — licensed by name in the cell — on any town with `hasWatch` false (the catalogue seats a watch town-plus; WALLED-* fires at every tier). Also "its muster" on a walls-only STRAINED town → `threatAssessment.js:119`. |
| **W5** | STRUCK | SPLIT — the maxim-frame tone half struck; **a tier word inside a generic frame** kept (= F1-31, W3's kept half) | `OverviewTab.jsx:247` · floor 1 | "A town of {Thorpname}'s weight has never needed a wall." — licensed by name — beside "Thorp" on the identity strip. |
| **R-viii′ (struck half)** | SPLIT-struck, permission "the men drifting off, the roll thinner than it was, posts unfilled" | the strike of the cell-overlap bar may stand; **the permission must be a standing state only** — "the muster is thin", "posts stand unfilled" — never a course, never a comparative against a past, never on a key with no force row | `ageBands.js:52` (`HISTORICIZE_BAND`); `candidateEvents.js:279` et al. (`elapsedTicks: 0`); the table's own F2-05 · floor 2. And `threatAssessment.js:119` · floor 1 on a walls-only STRAINED town | "The roll is thinner than it was, and the men are drifting off." — F2-05 lists *"thinner than it was"* and *the durative* verbatim as refused; R-9 and F2-05 contradict each other inside one instrument, and ET-2's D-31 (MODEL) still reads "never a thinning muster, never desertion". |
| **NF-2** | ⚠ NEW BAR floor 1, "age-flavour is now free … 'raised by someone's grandfather'" | NEW BAR floor 2, **narrower**: a raising/founding narrated is refused everywhere (F2-03); on any pool whose key reads no history field, ALL age-flavour is refused (F2-09) — DS-DEF-11's key reads none; only a comparative against a same-page permanent feature survives | `historyPreservation.js:1-5` ("Reroll history replaces `settlement.history` wholesale"); `OverviewTab.jsx:251` (`hist.age`, user-settable per F2-02); `defenseStateProse.js:1055-1065` · floor 2 | "The {defwork} was raised by someone's grandfather." — a raising narrated with a two-generation date; and after the DM presses Reroll history the printed age moves and the face cannot. |

---

## 6. CHALLENGES (strongest first)

1. **R-i** — the ground is false on both legs: `lawfulPassageShapes` answers `applicable:false` for a bare spine (k=0), and it is not a composer caller at all (`composeStateProse.js` never imports it; the header says so). "The composer will simply not draw the shape that would break" is untrue at the dock. Verdict may stand; ground and promise cannot.
2. **NF-1** — path wrong (`src/domain/display/stateProse/composeStateProse.js`), and "as `passageShapes.js` already does" is false: nothing refuses any shape at composition today, so W1's residue has no cure in the engine.
3. **W2 struck half** — the cell's example "the gate stands open more often than not" is the exact phrase the table's F2-06 withdraws as a RATE; a validator who trusts the cell ships a floor-2 sentence.
4. **W3 kept half** — citation `RECEIPT_POOLS…:6200` is DS-GEN-17; the key is at `:5966`. Larger: F1-31 has no ruling on "town" as the estate's generic, so it refutes the shipped `UNWALLED-LARGE` v2 on every city and every "the town" on every thorp.
5. **W4 / W6 / R-ii+W10** — four rows discharged into a pool-grain CRAFT verdict that had never fired at record time; W4 alone caught a real regression at the FACE grain, which a DULL verdict cannot address. Honest form: SUSPENDED pending first firing, for all four.
6. **R-ii / W10** — "the cap added nothing an instrument was not already doing" is invented; `moveGrammar.js` types a face's moves and cannot count openers across variants (fidelity F2, unrepaired).
7. **W8** — "let the ledger cite its own source" inherits R-vi's unconditional citation; must carry F1-24/R-8.
8. **NF-6** — path wrong (`src/domain/institutions/defenseInstitutionBuckets.js:84-87`); substance confirmed.
9. **NF-2 floor label** — appendix says floor 1; the table files it F2-07 floor 2. A refuter with the appendix cites the wrong token.
10. **R-v** — struck in `RETRO-VALIDATION §6.2` but rowed nowhere with a VALIDATION cell; R-vii and R-viii likewise (fidelity F7 unrepaired in the items file).
11. **R-viii′ kept half's quote** — stops before the cure sentence and never records that R-viii LICENSED the opposite until 09-10 01:0x (fidelity F6 unrepaired); the one item that reversed itself before ADDENDUM 14 carries no history.
12. **Floor-3 clearance** — the packet's positive "nothing here touches floor 3" is still absent from the items file (fidelity F3 unrepaired).
13. **R-vi's holder citation** — `holderTable.js:71` lands in the doc comment; `HOLDER_KINDS` is at `:78-79` and the muster entries at `:188-195`. Imprecise, not wrong.
14. **NF-5's reach** — `detectOversizedInstitutions` returns `[]` for every tier but village and hamlet (`contradictions.js:138`); F1-112's condition is right but the appendix reads wider than the module.

---

## 7. SLICE VERDICT

The re-cut's premise holds — both measurements reproduce to the digit — and the three kept halves (one purse, the `Invasion & War` row, the printed tier) stand on real fields at lines that read exactly as cited. Nine of the thirteen strikes are sound as lawfulness verdicts. But the slice fails at the column the protocol tells a reviewer to argue with: **six permission cells put a false sentence on the page**, and in four of them the denier is a row the same fold wrote into its own instrument (F1-01, F1-24/R-8, F1-31, F2-03/05/06/09). The single largest risk is structural and this slice did not see it: **`WALLED-STRAINED` is keyed on walls and the gate alone and never reads a force row**, so every muster, roll and watch permission handed back here — "its muster", "the roll shows", "the men drifting off", "the gate's watch" — is unconditional on a key that fires beside "Walls present but no organized military force to man them." on the same tab. The shipped STRAINED faces ("the watch that should man it", "the muster behind it is thinning") already breach it; the re-cut, as rowed, licenses more of the same. A DS-DEF-11 row of F4-18's shape ("naming any force bucket on a walls-only STRAINED town") is missing from the table, and it should be the first row a re-refutation of DEF-11 runs.

## 8. THE ONE THING

The instrument contradicts the archive on the slice's own most-challenged item: `items-W1-W10.md` licenses "the roll thinner than it was" and "the men drifting off" under R-viii′, while `CONTRADICTION-TABLE.md` F2-05 lists *"thinner than it was"* and the durative verbatim as refused elapsed courses and F2-06 withdraws the W2 cell's "more often than not" by name. Until R-9 is re-cut to a standing state and the two example phrases are struck from the cells, a writer reading the archive and a refuter reading the table will disagree on the first STRAINED face they meet.
