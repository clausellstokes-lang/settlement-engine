Desk composition (chair car): the generation worker bundle takes its measured byte ceiling — HORIZON-B2's refused arm, paid by the landing that built

HORIZON-B2 declined to write WORKER_BUNDLE_CEILING_BYTES without a build (a pin with no receipt is the false-instrument class). The chair built at the composed tip (build-900-chair.log): dist/assets/generation.worker-*.js measures 1,404,242 bytes. The constant and a dist arm land in generationWorkerLazy.test.js — monotone-down, the figure read from the failure never computed from the tree, a rise a chair ruling. Proof: VERIFY_DIST=1 run green with the built dist; the plain run green; eslint 0.

Seat: Fable 5.1 — validated

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
