1. `[ledger]` {settlement} keeps grain against a failed harvest and keeps nothing against disease.
   - `[face]` The granary at {settlement} is kept for a bad year, and no such provision is kept for the sick.
   - `[face]` Food is stored at {settlement} for a failed harvest, and the town stores nothing against sickness.
   - `[face]` {settlement} has its granary and no care for the sick.
2. `[unfolding]` The stores hold against a hungry year. Against a sick one the town holds nothing.
   - `[face]` What the store holds meets a failed harvest. A sickness it does not meet.
   - `[face]` Grain is put by against hunger, and for sickness nothing is put by.
   - `[face]` The town has stores against hunger. For sickness it has no provision at all.
3. `[street]` The town can outlast a hungry year at {settlement} and has no answer to a sick one.
   - `[face]` For hunger the town has a store at {settlement}; for sickness it has none.
   - `[face]` A bad harvest {settlement} can sit out. Against a sickness the town is unprovided.
   - `[face]` The store carries {settlement} through a hungry year, and nothing carries it through a sickness.

--- NOTES

**The card this pool is written against** (printed this session, `node scripts/prose-licence-card.mjs DS-DEF-2 'Disasters & Famine: granary, NO medical provision'`): role SPINE, no relation, no attach; reads `disasterRowSituation(granary, hospital, church)`; predicate `=== granary, no medical provision`; bag `{band: RESERVED, route: proper, settlement: proper}`, FILLED at this block's call sites `{settlement}`; form sentence; angle `ledger street unfolding`; source `(none) · standing SOURCE-UNRESOLVED`; **may claim** that the predicate holds as a STANDING fact of the record; **may NOT** a count, a cause, a season, a future, a standpoint, a second fact, another civic object of the class `care`; refused columns: a totality over persons, an exemption, a named character's fate, a theological claim.

**The claim set every one of the twelve wordings carries, and nothing else.**
- **C1 — the store stands.** The granary limb of the predicate value. Licensed by *may claim*: the predicate `disasterRowSituation(...) === granary, no medical provision` holds as a STANDING fact. Move PRESENT (MOVE-GRAMMAR §1.2 row 1, licensed by the block's STATE-KEY).
- **C2 — no medical provision stands.** The second limb of the same predicate value, so the same *may claim* clause; realised as ABSENCE class (a) LACK (§1.2 row 11: a `none-exists` world field — `hospital` and `church` both false inside this predicate value), written flat, never as the opener (wall 3), with no completing "but" (R-DA-02).
- **The slot.** `{settlement}` in variants 1 and 3 only, from the card's bag as FILLED at this block's call sites. `{band}` is RESERVED and `{route}` is unfilled here, so neither is written. Every face of a variant carries exactly its parent's slot set (ARCH §2.5's face row refusal).
- **No citation anywhere.** The card's `source: (none) · standing SOURCE-UNRESOLVED` licenses no holder, so no face names one (arm A13; Part B §24's ceiling never engaged).

**Per face, clause by clause.** In each row below, `C1` and `C2` are the two clauses above; no face asserts a third thing.

| row | C1 (the store) | C2 (the lack) | slot | other claims |
|---|---|---|---|---|
| 1 line | `keeps grain against a failed harvest` | `keeps nothing against disease` | `{settlement}` (bag) | none |
| 1 face a | `the granary ... is kept for a bad year` | `no such provision is kept for the sick` | `{settlement}` | none |
| 1 face b | `Food is stored ... for a failed harvest` | `the town stores nothing against sickness` | `{settlement}` | none |
| 1 face c | `has its granary` | `no care for the sick` | `{settlement}` | none |
| 2 line | `The stores hold against a hungry year` | `the town holds nothing` against a sick one | none | none |
| 2 face a | `What the store holds meets a failed harvest` | `A sickness it does not meet` | none | none |
| 2 face b | `Grain is put by against hunger` | `for sickness nothing is put by` | none | none |
| 2 face c | `The town has stores against hunger` | `it has no provision at all` for sickness | none | none |
| 3 line | `can outlast a hungry year` | `has no answer to a sick one` | `{settlement}` | none |
| 3 face a | `the town has a store` for hunger | `for sickness it has none` | `{settlement}` | none |
| 3 face b | `A bad harvest ... can sit out` | `the town is unprovided` against a sickness | `{settlement}` | none |
| 3 face c | `The store carries ... through a hungry year` | `nothing carries it through a sickness` | `{settlement}` | none |

**What the shipped sentences claimed and this rewrite DROPS** (the card refuses each; dropping is the rewrite's purpose, and the shipped breach is the corpus's known state).
- Variant 1: *"a sickness here spreads until it stops of its own accord"* — a FUTURE and a SECOND FACT (both on the card's may-NOT list), and an outcome no field of this read holds. Dropped. The two licensed limbs are kept.
- Variant 2: *"will carry" / "will carry it"* — a bare future indicative, a FATE breach under A2 / R-DA-07 / THE PROMISE, corrected to the present; the CLAIM behind it (the store answers hunger) is kept, so no claim moved, only the modality that was never licensed. *"and the town has not built anything that would"* — a HISTORICAL claim (what was never built) with no event-provenance field on this read (§1.2 row 2, R-DA-19), and a second fact. Dropped.
- Variant 3: *"and knows which of the two it fears"* — a STANDPOINT and a belief frame (card may-NOT), a FEELING (the non-move of §1.3), and a totality over the town's persons (refused column). Dropped.
- Nothing is added anywhere: the twelve wordings assert C1 and C2 and no third proposition.

**The thread (§1.4.1) inside each variant.** Variant 1's wordings are single sentences, so the wall does not bite; each lands on the civic noun (granary, food, care). Variant 2's second sentence carries the noun forward every time — line: *year → a sick one* with *hold/holds*; face a: *meets → meet*; face c: *stores → it* (the town). Variant 3 face b carries *{settlement} → the town*. As a SPINE this row is first in the passage and hands a modifier a concrete civic noun to pick up (granary · store · stores · grain), never an abstraction.

**Composition (the spine reads first, and modifiers of any salience follow).** No wording depends on a preceding sentence, opens on a connective, or closes on a set-up; each closes on a standing fact of a varied kind (R-DA-04): an absence (1 line, 2 line, 2 face b), a person class (1 face a), a civic object (1 face c: *care*), a condition (3 face b: *unprovided*), a thing carried (3 face c).

**Form checks run over all twelve.** No em dash · no exclamation · no question · no digit or percent · no `which`-clause · no "I" or "you" · no citation · no figure, sense-verb on an abstraction or inanimate intent · no cause word · no future indicative · one semicolon in the pool (3 face a) against R-DA-06's ration · settlement-token openers confined to ONE variant (variant 1, two of its four wordings) per R-DA-17 · no two variants share their first two words (A11): *{settlement} keeps* · *The stores* · *The town can*. Sentence lengths run 10 to 19 words for spread (R-DA-05); noun varied across the pool: granary · food · grain · store · stores (R-DA-10).

**REFUSALS — two POOL-LEVEL laws this pool cannot meet under this card. No variant is refused; all three are written and lawful in themselves.**
1. **R-DA-02's "CONTRAST is never the closing move of more than one variant per pool" cannot be met.** The predicate has exactly two limbs and wall 3 (MOVE-GRAMMAR §1.4.3) bars the ABSENCE from opening, so the lack is terminal in every lawful variant of this pool. Recorded as a declared exceedance rather than smuggled past: the closing KINDS are varied instead (the list above), which is what R-DA-04 actually measures.
2. **§2.1's "a pool of k variants carries min(k, 8) DISTINCT level-1 grammars" cannot be met at k = 3.** The card licenses one read, one predicate, no second typed field, no event provenance, no `not-held` record field and no unresolved state value, so V2, V6, V7 and V8 are unlicensable here and V4/V5 collapse into the same PRESENT → LACK order. All three variants are V3 (PRESENT → LACK); they are separated by head (state token · the object · the town's capability), by sentence count (one · two · one) and by close kind. A third distinct grammar would need a claim the card does not license, and adding one is barred.
