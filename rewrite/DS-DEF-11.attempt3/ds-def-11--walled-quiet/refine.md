Seat: Opus 5 — Fable-unvalidated (REFINER, a different author than the drafter; DS-DEF-11 · pool `WALLED-QUIET` · the refinement round, Part B §21 phase 2 / §21.1 / §21.2 / §21.3 / §21.4)

# DS-DEF-11 · `WALLED-QUIET` — the refined variant rows (paste under the pool's bold heading)

3 variants, same vids, same order, same angle tags, none added, none removed, none merged.
Twelve faces refined one for one against the draft at `draft-round-2.md`: none added, none dropped, no claim added, none lost.

1. `[visitor]` {settlement} keeps a {defwork}, and the {defwork} stands.
   - `[face]` The {defwork} at {settlement} is a work that stands.
   - `[face]` At {settlement} stands a {defwork}.
   - `[face]` A {defwork} stands at {settlement}, and it goes on standing.
2. `[elder]` The {defwork} stands; {settlement} has it up.
   - `[face]` A {defwork} at {settlement} is up.
   - `[face]` What {settlement} has is a {defwork}, up and standing.
   - `[face]` The {defwork} {settlement} has is standing.
3. `[ledger]` Set down as standing, the {defwork} stands.
   - `[face]` The {defwork} stands as entered.
   - `[face]` A {defwork} is entered, and entered as it stands.
   - `[face]` The {defwork} is carried as standing.

--- NOTES

## Per face: what changed, and the law or target behind it

**v1 `[visitor]` — the field is what STANDS, seen plainly.**

1. **v1 line** (was `{settlement} keeps a {defwork}, and it stands.`) — the resumptive `it` becomes the slot noun again, `and the {defwork} stands`. THE THREAD (MOVE-GRAMMAR §1.4.1, owner 2026-09-08 ~21:4x): "a deliberate noun echo for the thread is lawful", and the echo leaves the wall-class noun as the nearest antecedent for whichever modifier the salience order mounts next, where the pronoun left a two-noun ambiguity a modifier had to resolve. The register card licenses the recurrence itself ("the WORD may recur; the FACT must not"). Same claim, same slots, same shape.
2. **v1 face a** (was `A {defwork} stands at {settlement}.`) — rewritten as a predicate nominal, `The {defwork} at {settlement} is a work that stands.` Two targets. First, the draft's own round-2 finding: the old face closed on the `proper` slot and `settlement` ends in `ment`, so `closers.abstractNounRate` read it as an abstract-noun closer at the AUTHORED grain (`proseFingerprint.js:147`) — carried as an instrument artefact and now simply gone, with no row of the pool closing on `{settlement}`. Second, §21.1's ceiling: the pool had no nominal predication at all, and a clerk's definition ("the X at Y is a work that stands") is a shape the register owns and this pool was not spending.
3. **v1 face b** (was `The {defwork} that {settlement} keeps stands.`) — becomes the locative inversion `At {settlement} stands a {defwork}.` The relative was the fourth row of the pool built on the frame `The {defwork} … {settlement} …`, and A11's spread plus §21.1's "widest sibling distance" want the shapes apart, not the words. The inversion also closes on the slot noun, which is the strongest hand-forward in the pool for THE THREAD: a modifier mounting behind it picks up `it` with exactly one candidate.
4. **v1 face c** (was `At {settlement} a {defwork} is up, and up it remains.`) — becomes `A {defwork} stands at {settlement}, and it goes on standing.` Three reasons. `up` is the ELDER's word and was leaking into the visitor's variant (a cross-angle tic the brief names); `and up it remains` was the pool's second fronted/inverted tail, so the fronted-place habit now fires once in twelve rows instead of twice; and the durative carries the card's STANDING modality in the present, not a future (R-DA-07: the present stands, no forecast — this is the same durative round 1 shipped through the gate, restored deliberately).

**v2 `[elder]` — the field is the town's own holding: has, up.**

5. **v2 line** (`The {defwork} stands; {settlement} has it up.`) — KEPT VERBATIM, deliberately. It is the pool's one semicolon (so the punctuation profile does not move from the gate the draft passed), its two beats are the elder's plain speech at the ceiling already, and §21.4's density law forbids a change with no law behind it: every rewrite I tried either bought a word ("has it up and whole" — an unlicensed second fact about condition) or traded density for plainness.
6. **v2 face a** (was `A {defwork} is up at {settlement}, and it holds.`) — becomes the plain declarative `A {defwork} at {settlement} is up.` Two laws. `holds` of a wall imports RESISTANCE, and this is the pool whose key is *no live threat* — a refuter reading `holds` against `WALLED-THREATENED` would name a claim the card does not license ("may NOT: a second fact"). And the coordinate tail was the fifth of six in the draft; the pool needed one flat short line that says the thing and stops (the register card: "The short line exists").
7. **v2 face b** (was `The {defwork} {settlement} has, it has standing.`) — becomes the pseudo-cleft `What {settlement} has is a {defwork}, up and standing.` The old row was a topic-plus-resumptive comma splice that reads as costume rather than as the office's third person (register card: no persona; the antique air lives in the noun, never in the syntax). The tail is an appositive, not the draft's `and it is up` clause — one joint fewer for the same claim, which is §21.4's compression, and the appositive is a shape the pool did not otherwise carry. `up and standing` clears `shapes.doubledAdjectiveRate` (`up` matches none of the regex's suffixes) and the triad regex (one comma, not two).
8. **v2 face c** (was `What {settlement} has is a {defwork}, and it is up.`) — becomes the contact relative `The {defwork} {settlement} has is standing.` The draft carried TWO pseudo-clefts in one pool and the block's `UNWALLED-SMALL` carries a third ("What stands at {settlement} is …"); MOVE-GRAMMAR §3.4's spread and the brief's zero-tics target allow the frame once. It now fires once in this pool, in face b, and the cleft that stayed is the one whose tail earns its length.

**v3 `[ledger]` — the field is the record's own diction: set down, entered, carried. `{defwork}` alone, exactly as the shipped parent.**

9. **v3 line** (was `Entered as standing, the {defwork} stands.`) — becomes `Set down as standing, the {defwork} stands.` The block's `UNWALLED-LARGE` already opens a face with `Entered among the towns, {settlement} has no walls.` — the identical fronted-participle-plus-PP opener two pools away, which is a cross-pool signature and a refuter's finding under A11/§3.4. `Set down` is the same office's diction at the same register and puts the pool's three ledger verbs on three different words (set down · entered · carried).
10. **v3 face a** (was `A {defwork} is carried, and carried as standing.`) — becomes `The {defwork} stands as entered.` The pool's shortest line and its densest: `as entered` carries the record's modality and the standing in one complement, which is §21.4's compression that rewards the reader rather than the draft's third `and`-coordinate. The coordinate-with-repetition shape is not lost — it moves to face b, so the pool keeps every shape it had and gains one.
11. **v3 face b** (was `The {defwork} is entered standing.`) — becomes `A {defwork} is entered, and entered as it stands.` This is the office's own formula repeated (register card: "Repeat the office's formula because it is the office's"), and `as it stands` is the idiom doing two jobs at once — as the record has it, and standing. The draft's bare passive was the plainest row in the pool with no law asking for plainness (§21.4's named regression); the passive is kept, in face c, where it is not the only thing the row does.
12. **v3 face c** (was `What is entered is a {defwork}, and it is entered standing.`) — becomes the plain passive `The {defwork} is carried as standing.` It removes the pool's second pseudo-cleft (see face 8) and its longest padding — the old row spent eleven words to assert one claim twice — and it keeps `carried`, the ledger verb the draft's face a owned, so no vocabulary is lost from the variant.

## The claim set, unchanged and unmoved

All twelve rows assert exactly ONE claim, the same claim, and it is the card's: **that `forces.walls.present` holds, as a STANDING fact of the record** (`node scripts/prose-licence-card.mjs DS-DEF-11 'WALLED-QUIET'`, re-run this session). Every row carries the presence AND the standing modality, so arm A6 reads the four faces of each variant as claim-equal to each other: `stands` · `is a work that stands` · `stands` · `goes on standing` · `stands / has it up` · `is up` · `up and standing` · `is standing` · `set down as standing / stands` · `stands as entered` · `entered as it stands` · `carried as standing`.

Nothing was added: no count, no cause, no season, no future, no standpoint, no second fact, no other civic object of the class `wall`; no totality over persons, no exemption, no named character, no theological claim. Nothing licensed was dropped — the draft's dropped-claim ledger (`draft-round-2.md` §3, against the shipped rows) stands untouched, because a refinement is a one-for-one substitution over the draft, not a second pass over the shipped corpus (§22(e)).

**No citation, still.** The card's `move:` is `(none declared)` and none of S3's three reasons is present (no two accounts disagree, no count from an interested party, no record whose keeper is a power), so the provenance ceiling of one per unit (§24; MOVE-GRAMMAR §4.4.3) is spent at zero. `set down` · `entered` · `carried` are the office's own formula for its own entry and name no holder — naming one here would be the office citing its own books, which §4.4.3 makes a finding.

**Slots, per ARCH §2.5's face-row refusals.** v1 and v2 carry `{settlement}` and `{defwork}` on all four rows; v3 carries `{defwork}` alone on all four and introduces no `{settlement}`, exactly as each shipped parent did. No face opens on the `proper`-typed slot (T-F8) and none opens on the lower-case bare-common slot; the pool's one settlement-token opener is v1's numbered line, the one R-DA-17 order constraint 10 allows.

## What the refinement does to the measures (reasoned from the shipped regexes, NOT executed — the fences allow only the licence card to run here)

| measure | draft round 2 | refined | note |
|---|---|---|---|
| `shapes.participialOpenerRate` | 0 of 12 | 0 of 12 | first words: settlement · the · at · a · the · a · what · the · set · the · a · the — none ends in `ing` |
| `openers.sameOpenerAsPreviousRate` | 0 | 0 | no two adjacent rows share a first word |
| `closers.abstractNounRate` (authored grain) | 1 of 12 | **0 of 12** | the draft's declared instrument artefact is CURED: no row closes on `{settlement}` |
| `closers.pronounRate` | 0 | 0 | no row closes on it/that/there |
| `punctuation.semicolonRate` | 1 of 12 (v2 line) | 1 of 12 (v2 line) | the same row, deliberately |
| colon · em dash · digit · question · exclamation · parenthesis · quote | 0 | 0 | |
| `shapes.whichTailRate` · `thereIsOpenerRate` · `antithesisRate` · `triadRate` · `doubledAdjectiveRate` · `adverbsPerSentence` | 0 | 0 | `up and standing` clears the doubled-adjective regex (`up` matches no suffix in the list) and the triad regex (one comma) |
| `wordsPerSentence.shareUnder8` | 0.5833 (7 of 12) | 0.5833 (7 of 12) | held exactly |
| `runsOfThreeSameLengthBand` | 0.0833 (1) | 0.0833 (1) | held exactly; the face order inside each variant was chosen to hold it |
| `wordsPerSentence.neighbourVariation` | 0.384 | 0.397 | the one measure that moves, and it moves UP, away from the exemplars' floor (the gate's own probe records a face-grain depth of 1.597 *under* on this metric, so the floor is the side that bites) |
| `shareOver30` | 0 | 0 | lengths 5 to 10 against the draft's 5 to 11 |

Word counts in row order (whitespace tokens, the fingerprint's unit): 8 · 9 · 5 · 10 · 7 · 6 · 10 · 6 · 7 · 5 · 9 · 6.

## Tics, counted before and after (the brief's zero-tics target)

| habit | draft round 2 | refined |
|---|---|---|
| `and …` coordinate tail | 6 of 12 | 3 of 12, each with a different second clause (full NP · pronoun with a durative · ellipted repetition) |
| pseudo-cleft `What … is …` | 2 of 12 (3 in the block) | 1 of 12 (2 in the block) |
| fronted place / inversion | 2 of 12 | 1 of 12 |
| `up` outside the `[elder]` variant | 1 | 0 |
| `Entered …,` opener shared with `UNWALLED-LARGE` | 1 | 0 |
| distinct rhythms across the twelve rows | 9 | 12 |

The twelve rhythms, one per row: coordinate with a noun echo · predicate nominal · locative inversion · coordinate with a durative · semicolon · plain declarative · pseudo-cleft with an appositive · contact relative · fronted participial absolute · as-complement · coordinate with the office's repetition · passive.

## Sibling distance (arms A1 / A11), re-read this session in the block's annex

Avoided in full from `WALLED-THREATENED`: stands to {settlement}'s name · possessed of · holds its place · an item of the town's own defensive work · a fixture of · with the town at · belongs to the town · the fortification of · walled by · fixed defense · the walled place. From `WALLED-STRAINED`: is the town's · is {settlement}'s own · held by the town · walled with · owns · in the town's hands. From `UNWALLED-SMALL`: the `What stands at {settlement} is …` frame. From `UNWALLED-LARGE`: `Entered …,` as an opener · carries. No tier noun (`town`, `village`) and no baked wall noun (`wall`, `stone`, `gate`, `circuit`, `rampart`) appears on any row, so nothing here contradicts a sibling in structural fact and nothing names a second civic object of the class.

## THE THREAD

These are SPINE rows: each is read first and must hand a noun forward. Every row closes on the standing predicate or on the slot noun itself, so whatever the salience order mounts next has `the {defwork}` or `it` with one candidate to pick up; `At {settlement} stands a {defwork}.` is the strongest of the twelve on this test and the shortest. Not one row changes subject in its middle and hands nothing back.

## Verdict, per §21.2

The set was LAWFUL at the draft commit and is lawful after the refinement: no wall is broken, no measure that passed is newly failed, one measure is cured (`closers.abstractNounRate` at the authored grain), one moves further inside its band (`neighbourVariation`), and the rest are held to the digit. **KEEP** is the recommendation; the revert-to-lawful arm is not needed. Nothing is banked, so the pool's banked count stays at zero.

## Findings carried forward for the chair (both the draft's, both still true after the refinement — neither is a wording row)

1. **The pool key's discriminating claim is still not licensed by its own card.** `WALLED-QUIET` is "walls, no live threat"; the card's may-claim names only `present`, its predicate line reads "(none recovered: the pool has no key-function branch)", and a second fact is refused outright. So the quiet is absent from all twelve rows and these spines carry the same typed claim a rewritten `WALLED-THREATENED` spine would carry — MOVE-GRAMMAR §3.4 / U9's line about dropping the clause that carries the pool key's discriminating claim. Two ways out, both above a writer's seat: recover the pool's predicate into the card, or rule that a state key may select without being said. A refinement round cannot cure it, because curing it means adding a claim.
2. **`{defwork}` is NAMED BUT NEVER FILLED at this block's call sites** (the card's own bag line). All twelve rows need it and the SLOTS line forbids a baked noun, so an unfilled slot renders a raw token or nothing. A wiring row, not a wording row.
3. **Band distance this set knowingly keeps** (unchanged in kind from the draft, and not curable by an author): within-pool words-per-sentence sd is about 1.8 against R-DA-05's 4.0 floor, and R-DA-04's close-kind spread runs to three kinds. Both follow from a one-claim licence — there is not enough licensed material for a long sentence or a wider close set, and §21.4 forbids buying either with padding. Under §16.2's ENTRY grain (BUDGET two thirds, DEPTH 1.75) the set sits inside the budget.

**Walls checked on every one of the twelve rows:** no em dash · no exclamation · no digit or percent · no `which`-clause · no question · no first or second person · no expletive opener · no future indicative · no forecast · no citation · no figure · no maxim · no tier noun · no material noun · no second wall-class object · no `-ly` adverb · one sentence per row (A1) · one bracketed angle tag per numbered line, `[plain]` nowhere.
