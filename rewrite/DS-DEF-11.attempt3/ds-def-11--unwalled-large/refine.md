Seat: Opus 5 — Fable-unvalidated (the REFINER, a different author than the drafter). Block DS-DEF-11 · pool `UNWALLED-LARGE` · role spine · REFINEMENT (arm A).
Input: `draft-round-1.md` (no `draft-round-2.md` exists for this pool — the pool went lawful at round 1 and no second draft round was run; the refinement is taken off the round-1 commit, which is also what stands in the dock's `RECEIPT_POOLS_DOSSIER_STATE.md` today).
Two variants in, two variants out; same vids, same order, same angle tags; four wording faces each. One face refined for one face; none added, none dropped; no claim added, none dropped.
Paste target: under the bold pool line `**`UNWALLED-LARGE`**` in `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`, replacing rows 1 and 2 and their face rows.

1. `[counterforce]` Past village weight, {settlement} carries no wall.
   - `[face]` No wall encloses {settlement}, a town at the least.
   - `[face]` Larger than any village, {settlement} lies unwalled.
   - `[face]` Ranked a town and above, {settlement} is not walled.
2. `[ledger]` By weight {settlement} is a town or larger, and unwalled.
   - `[face]` The rank of {settlement} is a town's or more, and no wall stands about the town.
   - `[face]` No circuit of wall runs about {settlement}, and the town is counted above the villages.
   - `[face]` Town weight attaches to {settlement}, and no wall does.

--- NOTES

**The claim set is unmoved.** Every one of the eight rows asserts exactly the draft's two licensed claims and nothing else — **C1** `forces.walls.present` is false, as a STANDING fact of the record (the card's `may claim`), and **C2** `settlement.tier` stands at town and above (the second `reads` row, and the key's discriminator against `UNWALLED-SMALL`). No cause, no history, no count, no forecast, no standpoint, no second civic object, no holder cited. Slots are `{settlement}` alone on every row, one occurrence, equal to the parent's (`{defwork}` stays unused: this pool fires only where the settlement has no wall-class row). Arm A6 reads across the four faces of each variant: they are claim-equal to each other and to their spine.

**Per row — what changed and the law behind it.**

1. spine `[counterforce]` — `Past village size` → `Past village weight`. The sibling pool `UNWALLED-SMALL` opens a face on `Village size is as far as {settlement} goes` and closes another on `the size {settlement} keeps`: **size is the small pool's measure word**, and the draft's opener shared two words with it. Splitting the measure vocabulary (small keeps *size*, large takes *weight*) is the widest sibling distance available without touching the claim (A5 / A11 sibling distance; R-DA-22's one term for one thing applied to the measure, not to the wall). `carries no wall` is kept: it is the pool's sharpest landing on the civic noun the field names (R-DA-04) and no sibling row holds it.
1a. `The town is no village, and nothing walls {settlement}.` → `No wall encloses {settlement}, a town at the least.` Two cures in one line. `nothing walls` is **verbatim the small pool's** `Nothing walls the place` (`:6011`) — a shipped collision the draft's own note missed, because it checked the pre-rewrite sibling rows rather than the ones now in the file. And `The town is no village` is the bare `X, not Y` shape R-DA-02 refuses outside a named sibling band. The refined line states the LACK flat and first (R-DA-02's licence for the civic lack as the first half, no completing "but"), and carries the band as an apposition, which drops the row from eleven words to nine and buys the pool spread at the short end.
1b. `Larger than any village, {settlement} stands open.` → `… {settlement} lies unwalled.` `stands open` is **DS-POP-3's phrase for a different fact** (`{settlement} stands open to its approaches`, `:6086`, the trade-approach block): a face that must stand alone anywhere cannot carry a predicate whose other owner in the estate means road access. `lies unwalled` states C1 in the field's own term with no cross-block reading, keeps the seven-word short line R-DA-06 wants at a rate (< 8 words ≥ 0.030), and frees `stands` for one use elsewhere in the pool.
1c. `Ranked with the towns` → `Ranked a town and above`. The sharper licensed fact: the STATE-KEY is *town and above*, and `with the towns` leaves the band's open top unsaid where the key states it. Predicate unchanged; the participial fronting is kept because it is the only one in the pool after 2b was recast.
2. spine `[ledger]` — `and it holds no wall` → `and unwalled`. `holds no wall` is **verbatim the small pool's** `The place holds no wall` (`:6013`). The clipped copular tail is the ledger's own cadence, parses cleanly off `is` (`is a town or larger, and [is] unwalled`), and takes the spine from thirteen words to ten so the two spines no longer sit at the same length with the same fronted-measure shape. `By weight` is kept as the opener: the row may not open on `{settlement}` (T-F8 refuses a proper-typed slot at the head of a sentence face, and §2.5's sentence-row rule reads the same way on a spine row — the draft kept both spines off the slot and the refinement does not spend that risk for a shape).
2a. `No circuit runs about {settlement}, a town at least in weight.` → `The rank of {settlement} is a town's or more, and no wall stands about the town.` The draft had **two faces opening on the lack** inside a pool of eight rows; the pool keeps two (1a and 2b) rather than three, and this row is the one converted to a common-noun subject, which is the shape the pool most lacked — five of the draft's eight rows fronted an adjunct before the subject. At sixteen words it is the pool's long end and lifts the within-pool word-count sd from about 2.2 to about 3.2 (R-DA-05's within-pool floor is 4.0; the remaining distance is reported, not padded away — two claims and no cause is the whole of what this pool may say, §21.4).
2b. `Entered among the towns, {settlement} has no walls.` → `No circuit of wall runs about {settlement}, and the town is counted above the villages.` Three cures. `Entered` is **the same block's `WALLED-QUIET` ledger idiom** (`[ledger] Entered as standing, the {defwork} stands`, `:5994`) and reads as that pool's signature; `has no walls` is the small pool's `has no wall` (`:6014`); and the participial fronting duplicated 1c's. The refined row takes the draft's own best noun — the wall's *circuit* — out of variant 2's tail and puts it at the head, with `of wall` added so the noun cannot be read as a watch's round, and states the band as a full clause rather than a second apposition, so 1a and 2b do not open on the lack in the same architecture.
2c. `Weight enough for a town stands to {settlement}, and no wall does.` → `Town weight attaches to {settlement}, and no wall does.` `stands to {settlement}` is **the same block's `WALLED-THREATENED` spine** (`A {defwork} stands to {settlement}'s name`, `:5972`) — the strongest sibling collision in the draft, and inside DS-DEF-11 rather than across blocks. `attaches to` is the clerk's word for a standing that attaches to a place, and it takes the ellipsis cleanly (`no wall does` = no wall attaches to it), where a wall that *stands* or *holds* would have carried the defence sense the upkeep pools own. The line drops from twelve words to nine and keeps the pool's one elliptical close.

**Declared judgments — a refuter should look here first.**
1. **Two faces open on the LACK (1a, 2b), as in the draft.** MOVE-GRAMMAR wall 3 (ABSENCE never opens) is read as governing the GAP class (R-DA-08, a fact of the record); R-DA-02 licenses the world LACK as the first half with no completing "but". Neither row carries a completing "but". The count is held at the draft's two, not raised; if the chair reads wall 3 as covering the LACK, 2b is the one to revert, since 1a is the shorter and the pool's only apposition.
2. **No row opens on `{settlement}`, spine or face.** T-F8 refuses it for a face; §2.5's sentence-row rule is read the same way for the spine, and R-DA-17 keeps the settlement token off the default opener. The cost is four fronted adjuncts across the eight rows, but of four different kinds — a bare measure NP (1), a comparative AP (1b), a participial (1c), a measure PP (2) — against the draft's five, two of them participial.
3. **No citation, deliberately.** The card licenses the muster and §24 caps the provenance move at one citation per unit for one of S3's three reasons only (two accounts disagreeing, a count from an interested party, a keeper who is a power). None obtains: one standing fact and its band, no count, no second account. The exemplar registers with raw text cite at zero per 786 sentences, so a citation here would be the habit §24 names as a refuter's finding. The refusal is re-recorded, not re-decided.
4. **`Ranked` / `counted` / `attaches` frame the tier as the compiling office's reading.** They assert the measured band and no record act beyond the office's own compiling; they are the vetoable idiom of this refinement, exactly as `entered` was of the draft. If the chair reads any of them as a second fact about the record, the cure is the bare copula (`is a town or larger`), which the pool already carries at 2.
5. **`circuit of wall` is the wall, not a second civic object.** The card refuses another civic object of the class `wall`; `circuit of wall` is a periphrasis for the same object, inside its negation, licensed by the same `none-exists` reading as `no wall`.
6. **Sibling distance, measured against the file as it stands.** No row of this pool now shares a phrase with `UNWALLED-SMALL` (*size*, *the place*, *nothing walls*, *holds/keeps/has no wall*, *without a wall*, the two-sentence shape), with `WALLED-THREATENED` (*stands to {settlement}'s name*, *The town of/at {settlement}*, *belongs to the town*), with `WALLED-QUIET` (*entered*, *carried*, *is up*, *What {settlement} has*), with `WALLED-STRAINED` (*The town at {settlement}*, *the town's own*), or with DS-POP-3 (*stands open*). The one residual root shared with a sibling is *carries* (spine 1 against `WALLED-QUIET`'s `A {defwork} is carried`): different pool, different construction, and the office's word is licensed to recur.
7. **Rhythm and spread.** Word counts 7, 9, 7, 9, 10, 16, 15, 9 (mean 10.25, sd about 3.2, against the draft's 2.2). No two rows share their first two words. Closers vary in kind: the wall, the band, the state, the town, the villages, and one elliptical verb; none is a pronoun. Joints are a comma or a single `and`; no semicolon anywhere, against the shipped rows' rate of 1.0 (R-DA-06's ceiling 0.130). No digit, no percent, no em dash, no `which`, no question, no exclamation; one sentence per row, so a composed unit with one modifier stays inside two sentences.
8. **THE THREAD.** Six of eight rows leave the wall or its absence in the last phrase, and the two that close on the band (1a, 2b) leave the town noun there, so whichever DEF-11 modifier the composer seats next has a noun to carry forward, and no row ends on a pronoun or a bare abstraction.

**REFUSALS: none.** The set was lawful at the draft commit and is lawful after the refinement; nothing is banked, no face is withdrawn, the variant count stays at two and the face count at four per variant.
