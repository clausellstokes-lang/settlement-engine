# RE-REFUTE · DS-DEF-2 · pool `Disasters & Famine: granary AND hospital` · seat Opus (re-refuter)
<!-- Instruments read whole: card.md (all 1058 lines, sections (1)-(9) including (0), (2b), (2b'),
     (2c), (2d), (5), (6), (7), (8), (9)); speakers.md; draft.md; refute.md; cure.md.
     Code facts re-executed READ-ONLY against the dock (nothing entered, nothing written there,
     no test run): src/data/institutionalCatalog.js, src/domain/display/stateProse/faceSources.js,
     docs/content/RECEIPT_POOLS_DOSSIER_STATE.md at HEAD db54f803b.
     The pool FINGERPRINT (ruling 35) was re-measured by running rewrite/measure-block.py
     read-only; its figures are quoted at §3 and the figure that moved the craft vote is named.
     FENCES HELD: this file is the only thing written, anywhere. -->

THE TEST APPLIED: a face is lawful unless it CONTRADICTS the record. Silence is permission.
"The card does not license it" names no fault. I hunted contradictions in the ONE cured line,
re-read the twenty-six untouched ones for anything the cure could have disturbed, and re-took
the craft verdict at the pool grain.

## 0. WHAT THE CURE ACTUALLY DID — VERIFIED, NOT TAKEN ON ITS WORD

cure.md line 4 claims "Every face other than the cured one is carried BYTE-IDENTICAL from
draft.md". EXECUTED: the row blocks of draft.md and cure.md were extracted and diffed.

```
diff <(sed -n '/^\*\*`Disasters/,/^--- NOTES/p' draft.md) <(… cure.md)
29c29
<    - `[face]` `[register]` The register holds that whoever takes the sick in answers …
---
>    - `[face]` `[register]` A local priest holds that whoever takes the sick in answers …
```

ONE line differs, and it is the charged one. The claim is CONFIRMED. The dock at HEAD
db54f803b carries the cured line verbatim (RECEIPT_POOLS_DOSSIER_STATE.md:2914), so the rows
under judgment and the rows in the annex are the same rows.

## 1. THE CURED FACE — variant 3, face 4, source `register`

> "A local priest holds that whoever takes the sick in answers somewhere other than the hall,
> and does not say where."

**VERDICT: PASS.** No field, row, label or machine sentence on this tab denies it at any value
the keys admit. The tests taken, each one named:

**(a) FLOOR 1 — THE ASSERTING AGENT NOW RESOLVES ON ALL THREE PREIMAGE TIERS.** The charge that
was upheld against the draft was F1-24: `the register` made an unresolved record-keeper the
sentence's own subject, and at CITY `Burial grounds and charnel house` carries `Burial` and
`Lifting and ossuary` and no register at all (card.md:795-799 and card.md:954-966); 68 of the
preimage's 272 towns are city. The cured subject cites NO record. The body behind it is
required on every tier of the preimage: `Parish churches (2-5)` at town (card.md:39),
`Parish churches (10-30)` at city (card.md:38), and the same row at metropolis by the merge
correction the marker executed against `assembleInstitutions.js:243-245` (card.md:717-723). The
charge is answered at its own ground.

**(b) AND THE ROLE WORD IS THE SOURCE'S OWN SEATING ROW, WHICH IS STRONGER THAN THE CARD SAYS.**
Executed read-only against the dock: `faceSources.js:90-91` seats the `register` source on
`REGISTER_NAMES = ['church', 'cathedral', 'temple', 'abbey', 'monastery', 'friary']`, excluding
only the thorp's `access to …` walk — **a burial or registry row seats nothing.** So the source
token this face carries is seated by a CHURCH row on every preimage town, and "a local priest"
is the role that seating row itself licenses. Of the pool's three register role words it is the
only one that leans on the row the seater actually reads (see §4, W-2).

**(c) FLOOR 1 — NOTHING DENIED, NOTHING INFERRED INTO A SILENCE.** The face denies no required
row and no service at or above the bar (card (2b)); it infers no body into the key's silence;
it does not touch the watch, which section (0) corrects to CLOSED TRUE on the whole preimage
and which is the sitting's own named failure mode. No same-page field is denied at any value
the keys admit.

**(d) THE CLAIM ITSELF, RE-EXECUTED AGAINST THE CATALOGUE RATHER THAN THE CARD.** The face says
the house that takes the sick "answers somewhere other than the hall". I did not take card
(7) speaker 7's reading on trust — a hall-run hospital row anywhere on the preimage would make
this a speaker asserting a field's opposite, which IS a fail. All six rows `hasHospital` can
seat were read: `Monastery or friary` (institutionalCatalog.js:1267, tags religious/monastery),
`Small hospital` (:1275, "Usually religious-run", tags religious/healing), `Multiple
monasteries` (:1805), `Major hospital` (:1814, tags religious/healing), `Major monasteries
(5-10)` (:2378), `Hospital network` (:2385, tags healing/**religious**). **Every one of the six
carries the `religious` tag and not one is described as hall-run, municipal or civic.** The
claim contradicts no row on any tier, and it is carried as an ACCOUNT ("holds that") rather
than as a bare fact, which is the right handling of a tendency.

**(e) FLOOR 3, THE NAMED-OFFICE BAR — THE CLOSEST CALL, AND IT IS CLEAR.** `Parish Priest` is
minted under `plague_onset` and `religious_conversion` (card.md:886-891), and this key reads no
stress, so such a town can draw this face. The bar is on the SINGULAR office the tier names as
an NPC. "A local priest" is indefinite and the roster prints two to five parishes at town and
ten to thirty at city, so it names no singular office; the card, having weighed exactly this
bar in exactly this place, prints "a local priest" among the three safe words and bars "the
priest", "the parish priest", "the high priest" (card.md:800-802). Nothing is predicated of a
deity; the speaker leans on no standing band, so deity rows D-04 to D-10 stay clear.

**(f) FLOOR 2.** No count, no share, no date, no season, no rate, no event the record did not
run. "answers somewhere other than the hall" is the simple habitual present, lawful everywhere;
"does not say where" is a withholding reported in the archiver's hand, not a claim. No elapsed
course over a live field.

**(g) FLOOR 4.** Nothing on the one military purse, nothing on the gates' direction, no
permanence and no decay clock, no covert fact — and section (2c) records that no covert field
marks this pool at all, so nothing here could be a concealment in any case.

**(h) RULING 35, THE SIBLING RUNGS — TESTED AND REFUSED AS A FINDING.** The rung to fear is
`granary AND parish care only` (hospital = false), whose own sentence gives the care to the
clergy. This face does not: the provider is withheld ("whoever takes the sick in") and the
priest is the SOURCE reporting on it, which is precisely what card (8) instructs (write the
function, let the fill name the row). On the sibling rung the body is not withheld — it is the
parish — so the sentence does not read as that rung's state. The `NO reserves` rung is
untouched. Recorded because a priest speaking about the sick is the one place on this pool
where a reader could slide into the neighbouring rung, and the sentence does not.

**(i) THE MECHANICAL AND VOICE SCANS.** One sentence; no em dash, no exclamation mark, no
digit, no contraction; no `{settlement}`; no notebook device on a player face. Run through the
kernel's own classifiers (re-implemented in measure-block.py from
`stateProseKernel.js openerClassOf` / `closeClassOf`): opener class `subject` — **identical to
the draft line it replaces** — close class `plain`, attributed under both the loose and the
STRICT arm, self-citation 0 (ruling 40), forecast 0 (ruling 33). None of the tells is present:
no which-clause closer, no summarising close, no antithesis pair, no reassurance, no clever
last beat. The line stops on the withholding.

## 2. THE TWENTY-SIX UNTOUCHED FACES — RE-READ FOR CURE DAMAGE

The cure changed a speaker word, and a speaker word is a pool-level object, so the two sibling
faces of the same source were re-taken:

- **v1 f3** "The sexton's view is that the house for the sick sends fewer to the ground than
  the town supposes…" — PASS stands. `Burial` is p 1 on all three tiers (card (2b)); no book is
  cited; the comparison carries no band word and no number, and it is the source's view, which
  is never a finding.
- **v2 f5** "Those who bury say the dead that come from the house for the sick come washed…" —
  PASS stands. Same ground; the role word is a plural trade, not an office.

The three now read "the sexton's view" · "those who bury" · "a local priest", which is three
role words for one source across three variants. That is a by-product of the cure and it
improves the pool; it damages nothing, since no variant is read beside another.

Nothing else in the pool references the changed face, and the diff proves nothing else moved.
The twenty-four remaining faces and three spines stand as refute.md judged them; I re-read
them for the sitting's named failure mode (floor 1 BY INFERENCE — a body inferred into the
key's silence that a required row denies) and found none: no face says nobody is set up to
respond, that no one is charged with it, or that there is nobody to send.

## 3. CRAFT — POOL GRAIN: **PASS**

THE FINGERPRINT FIRST (ruling 35), re-measured read-only, `measure-block.py <dock>
docs/content/RECEIPT_POOLS_DOSSIER_STATE.md '### DS-DEF-2'`, pool [P22]:

```
TREE     units 30 · sentences 32 · words 682 | wps 21.34 ±5.3 | same-opener 0.103 (3)
         | pet 0.59/100w (4) | sensory 4.25/100w (29) | attrib/sent 0.656 (21, strict 0.656)
         | open-share 0.0 | self-cite 0 | forecast 0 | varied
         openers subject 22 · place 6 · attributed 2   closes plain 29 · summary 1
SHIPPED  units 3 · sentences 3 · words 76 | wps 26 ±4.08 | same-opener 0.0 | pet 1.32/100w
         | sensory 1.32/100w | attrib/sent 0.0 (strict 0.0) | openers subject 3 | closes plain 3
```

**THE FIGURE THAT MOVED ME: `attrib/sent 0.656 strict` against a shipped `0.0` and an exemplar
baseline of 0 in 786 sentences.** Two thirds of this pool's sentences carry a named source with
a stake, where the rows it replaces carry none at all — that is ADDENDUM 18's whole design
landing on the page, and it is the difference between a dossier and a camera. Beside it,
`sensory 4.25/100w (29)` against the shipped `1.32` says the pool is made of concrete nouns
(the door, the store, the carts, the dead, the bar) rather than abstractions, and
`pet 0.59/100w` — the LOWEST of any re-cut pool in the block's tree summary, against a tree
median of 1.61 — says it is not leaning on the corpus's tics to get there. `same-opener 0.103`
and `wps ±5.3` refuse the "one sentence twelve times" reading outright.

Nine distinct sources speak (hall · market · register · tavern · watch · stranger · gate ·
guild · garrison), five pairs in three kinds, and the pool has a genuine dispute with two
honest sides — whose word opens the granary door — which no face settles. Nobody is
reassured into agreement. The default to DULL is for a pool that reads as a camera with no
speaker; this one is the opposite case.

COLLAPSE NAMED (a risk carried forward, not a verdict): **THE HOSPITAL HALF IS UNDER-SERVED —
7 faces of 27 touch the sick, and variant 1 carries exactly one of nine.** The key fixes two
states and card (9) puts the pool's second dispute in the house that takes the sick; a town
drawing variant 1 shows a single line about the half of its key that is about people. Lawful
(the re-cut struck "the same claim set on every face" by name and rules that a face need not
restate its key at all), and so it is a risk for the chair and not a finding. refute.md's two
other risks — the granary door as variant 1's repeated landing noun, and the gate's interest
turning between variants — stand as that seat recorded them and the cure did not touch them.

## 4. WIRING / INSTRUMENT ROWS (no face charged on any of these)

**W-1 (carried, RE-CONFIRMED AS STILL LIVE) — THE CARD CONTRADICTS ITSELF ON "the register".**
card.md:800 still prints "the register" among speaker 3's safe words while card.md:795-799 and
all of (8) rule that it resolves on two tiers of three and fails F1-24. The cure followed (8),
the section written to name falsehoods, and that is right. The chair should strike "the
register" from (7)'s safe list and leave "the sexton" and "a local priest", or the next writer
on this pool walks into the same wall.

**W-2 (NEW, and it outlives this pool) — WHEN CAR 18l LANDS, THE ROLE TABLE FOR THE `register`
SOURCE MUST NOT EMIT A RECORD-KEEPER AT CITY.** The cured face's tag is still `[register]`.
Ruling 25 says the page will print a ROLE drawn from the town's own rows in place of the class
word. `faceSources.js:90-91` seats this source on a CHURCH-family row and never on a burial or
registry row, so a role table that reaches for "the register", "the sexton's book" or any
record-keeper on a CITY town would re-introduce exactly the F1-24 defect the cure just removed,
under the renderer's hand instead of the writer's. The safe role set for `register` on this
preimage is the church's own people and the burial trade: a local priest · the sexton · those
who bury. This seat cannot fix it without entering the dock, which its fences forbid.

**W-3 (INSTRUMENT) — THE FINGERPRINT'S "closes … summary 1" IS A CLASSIFIER ARTIFACT ON THIS
POOL, NOT A TELL.** The unit is v2's tavern face, "…and walks home from, **and that is** said
with some pride", which matches `SUMMARY_RE`'s `,\s*and that is` arm. It is a reported
attitude, not a summarising close, and the chair should not read the figure as one. Recorded
because ruling 35 puts this number in front of a refuter's eye and the eye should know what it
is looking at.

**W-4 (carried) — refute.md's W-b and W-c are UNRESOLVED by the cure and outside this seat.**
W-b: the frozen/live seam under the garrison and watch faces (`defenseProfile.institutions.*`
is a generation-time snapshot at 0 writers while `institutions[bucket=*]` carries 38 pulse
writers, card (6)); per §1.4's tie-break the faces stand and the wave-2 page refuter, which can
see the render, should take it. W-c: the pool carries NINE faces per variant against the
brief's mechanical four, so either the annex grammar selects at render or the build reds on
this pool. Neither is a floor matter and neither is the writers' fault.

## 5. SUMMARY

1 cured face judged: **1 PASS · 0 FAIL · 0 WITHHELD.** The cure's own claims were tested rather
than accepted: the byte-identity claim CONFIRMED by diff, the tier-resolution claim CONFIRMED
against the catalogue and the merge correction, the opener-class claim CONFIRMED against the
kernel's classifier, and the "answers somewhere other than the hall" claim CONFIRMED against
all six rows `hasHospital` can seat rather than against the card's paraphrase of them.
2 sibling faces of the same source re-taken, both PASS. Craft at the pool grain: **PASS**, on
`attrib/sent 0.656 strict` against a shipped 0.0. Four wiring rows, one of them new.
