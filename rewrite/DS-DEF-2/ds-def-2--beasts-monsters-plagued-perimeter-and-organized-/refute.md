# REFUTE — DS-DEF-2 · `Beasts & Monsters: plagued, perimeter AND organized force`

Seat: opus (refuter). Rows judged: the LANDED rows at
`laneRW-DEF2/docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2630-2665` (commit `507ff2637`).
3 spines + 32 faces = 35 renderings. Test: ADDENDUM 14/18 non-contradiction; the four floors
as `recut/CONTRADICTION-TABLE.md` §V.0 words them. Shipped rows for the citation ceiling read
at `f2da5a3ee` (three spines, no faces, NO named record — ceiling is ZERO, and it is not breached).

## THE SEATING CHECK, DONE FIRST (it decides five faces)

`src/domain/display/stateProse/faceSources.js:120-140` + header — CONFIRMED by reading the
module: a town draws only among faces whose SOURCE resolves on it (`sourcesOf`, handed to
`drawFace` via `eligibleFaces`). `garrison` seats only where the garrison bucket is present;
`muster` only where the MILITIA bucket is present; `watch` only where the watch bucket is;
`hall` on a live Town/City hall row; `elders` below town only; `gate` on the `hasGates`
reading; `court` on `hasCourtSystem`; `market` on `hasMarket`.

CONSEQUENCE — the card's own headline hazard does not bite this pool. Card §8 warns that the
pool's characteristic failure is "a body word baked onto a five-tier preimage" (F1-02 / F1-03).
It is not present here: every body word in this pool rides a source tag the projector seats.
`[garrison]` (V1F8, V3F5) never draws at hamlet or village; `[muster]` (V1F3, V2F5, V3F2)
draws ONLY at hamlet and village, where the force is a `Citizen militia` — and no muster face
predicates a wage, pay or arrears, so V-09 is clean; `[watch]` (V2F6, V3F7) never draws below
town, so F1-01 / V-23 are clean. Card §9's tier-safe generic "the line" carries the wall with
no material and no scale, so V-06 / F1-32 are clean. No `compromised` candidate is offered,
as card §2c requires. No em dash, no exclamation mark, no digit in any prose. `{settlement}`
appears in ONE unit (spine 1) and in no `[face]` sub-row — ruling 12 satisfied, and this is a
repair of the shipped rows, which carried it in all three.

## PER-FACE VERDICTS

### Variant 1

- v1 spine `[ledger]` — PASS. Bare archiver's hand, two stops, no self-citation (ruling 40
  clean). Carries the one `{settlement}`.
- v1 f1 `[gate]` — PASS. The hooked pole is an invented PROP, not invented history or a
  number; under ADDENDUM 14 silence is permission. Lands on a negation (see CRAFT).
- v1 f2 `[stranger]` — PASS. "looked well kept" is a perfect over `institutions[bucket=walls]`,
  which is THE KEY'S OWN READ — licensed by §V.0 floor 2b. "the first person he asked" is an
  ordinal on the traveller's own act, not a magnitude of any town field (F2-01 names counts of
  town things; this is not one). No field named, so the burden falls on me: PASS.
- v1 f3 `[muster]` — PASS. Seats at hamlet/village only, where the militia exists. "counted as
  standing" names NO record, so F1-24's holder rule and the zero citation ceiling are untouched.
  No wage — V-09 clean.
- v1 f4 `[hall · pair 1 · disagree]` — PASS. "put least into it" is the hall's partisan OPINION,
  and a speaker's opinion is never a finding. Hall seats at town/city; the purse is one purse
  and the face does not split it (F4-02 clean).
- v1 f5 `[guild · pair 1 · disagree]` — PASS. Guild seats at town; the hall it names is
  required there. Pair 1 genuinely disagrees (who funds vs who complains), two different
  sources, kind correctly described.
- v1 f6 `[tavern]` — PASS. "the walk" is part of the works the key fixes, not a body inferred
  into a silence; no required row or sibling sentence denies it.
- v1 f7 `[register]` — PASS. Names the ACT and not the ground's placement, which card §2c
  requires (the placement is stated at some tiers and not others). Affirms `plagued` deaths.
  Shares its landing noun with f8 — see CRAFT.
- v1 f8 `[garrison]` — PASS. F1-02 does not reach it: `faceSources.js` seats `garrison` on the
  garrison bucket, so the face never prints on the 75 hamlets and villages. The grievance is a
  wage-adjacent standing claim on a garrison tier, which is the licensed half.
- v1 f9 `[court]` — PASS, and well made: `hasCourtSystem` records a civil arbitration and not a
  criminal trial, and the face puts the line into the court as a household debt, which is
  exactly the engine's model. Does not contrast the hall and the court as two bodies (card §7's
  warning that at town they are ONE row).
- v1 f10 `[market]` — PASS.
- v1 f11 `[elders]` — PASS on the floors. Voices the militia half's cost as the DAY LOST and
  not the coin owed, which is precisely what V-09 demands. ⚠ opener spends a PROVENANCE shape —
  see WIRING W-1.

### Variant 2

- v2 spine `[street]` — PASS. A `[public]`-family spine: what everyone SAW ("the line kept and
  somebody standing on it") is the key's own two reads, so floor 1 is clean and the perfect is
  licensed; what the town MAKES of it ("ordinary work") is perception and may be mistaken. A
  different SHAPE from spines 1 and 3 — ruling 29 satisfied across the pool.
- v2 f1 `[gate · pair 3 · disagree]` — PASS, the closest lawfulness call in the pool. The claim
  is about what causes trouble AT THE BAR, not about the region's creature pressure; it does not
  deny `config.monsterThreat = plagued` nor `threatAssessment.js:54`'s "constant creature
  pressure", which is a regional reading. It is the gate's self-serving account, the source has
  the stake, and the pair disputes it in the next line. PASS.
- v2 f2 `[tavern · pair 3 · disagree]` — PASS. The best line in the pool, and a true disagree.
- v2 f3 `[stranger]` — PASS.
- v2 f4 `[register]` — PASS. "in the same hours as any other" is a habitual equivalence, not a
  clock, a duration or a frequency.
- v2 f5 `[muster]` — **FAIL, floor 1.** See the finding below.
- v2 f6 `[watch]` — **FAIL, floor 1.** See the finding below.
- v2 f7 `[guild]` — PASS. `Apprenticeship programs` (p 0.8) is a required-row service at town,
  so the apprentices are the engine's own.
- v2 f8 `[hall]` — PASS. "has come" is a perfect whose subject is THE LINE — the key's own walls
  read — so §V.0 floor 2b licenses it. "in a form that needed a decision" keeps it off the Town
  hall's required `Dispute arbitration` (p 0.8), which it does not deny.
- v2 f9 `[market]` — PASS. Trade ending in daylight denies neither `Daily markets`' street food
  nor the taverns' evening.
- v2 f10 `[elders]` — PASS on the floors. ⚠ WIRING W-1.

### Variant 3

- v3 spine `[unfolding]` — PASS on the floors; carried by a source, a third distinct shape.
  ⚠ echoes face 1 of its own variant — see CRAFT.
- v3 f1 `[stranger]` — PASS on the floors. ⚠ echoes its spine — see CRAFT.
- v3 f2 `[muster]` — PASS, and the best militia-half face in the pool: the cost as the day and
  never the coin (V-09), no headcount (F2-01), no drift course (F2-05).
- v3 f3 `[guild]` — PASS. "counted by the trades" carries no number; the hall's `Tax payment`
  service counts what it receives, not what the line costs a trade, so nothing is denied.
- v3 f4 `[hall · pair 6 · disagree]` — PASS, and it is the pool's best law: it AFFIRMS the one
  military purse over the wall and the wages together (F4-02 / V-17, `defenseGenerator.js:182`,
  `:189-192`) instead of splitting it. "has never been asked" is a perfect over
  `defenseProfile.economicGates.military`, FROZEN at zero writers (card §6) — licensed.
- v3 f5 `[garrison · pair 6 · disagree]` — PASS. Card §9's own hook (the margin, not the
  outcome), and it does not say the posture fails — so the sibling-sentence trap is avoided.
  ⚠ opener collides with f7 — see CRAFT.
- v3 f6 `[market]` — PASS.
- v3 f7 `[watch]` — PASS. "the round covers what it covers" is bounded and denies no required
  service: `Town watch`'s `Night patrol` (p 1) and `Gate duty` (p 0.8) both survive a route with
  edges. Contrast with v2 f6, which asserts INVARIANCE under every condition and does not
  survive. ⚠ opener collides with f5 — see CRAFT.
- v3 f8 `[register]` — PASS. Past tense over the force read (key's own). Affirms `plagued`.
- v3 f9 `[tavern]` — PASS, the second-closest call. "one bad night from finding out" reads as a
  MARGIN and not as a magnitude of any town field: F2-01's class is counts of town things (a
  headcount, a share, a sum, a congregation), and this is none of them. It also AGREES with the
  engine rather than outrunning it — `threatAssessment.js:59` says simultaneous incursions break
  coverage and `:115` says the posture is not rated for sustained siege. Nothing is predicted:
  what would be found out is left unsaid, so it is not the shipped "takes this town with
  ladders" defect. PASS.
- v3 f10 `[court]` — PASS.
- v3 f11 `[elders]` — PASS on the floors. ⚠ WIRING W-1.

## THE TWO FAILS, IN FULL

### FAIL 1 — v2 f5 `[muster]` · FLOOR 1 · the same-page stress producer

QUOTE: "Nobody here asks a person going out after dark"

THE FIELD THAT DENIES IT: `config.stressTypes` — in this tab's SAME-PAGE READ SET (card §3,
§6: FROZEN CONFIG, zero writers) — read by the same-page producer `generateSafetyProfile`,
which the card lists at §4 as CAN CO-FIRE:

- `src/generators/safetyProfile.js:197` (`monster_pressure`): "Monster pressure from the
  surrounding region has changed how the settlement operates after dark. Outlying areas are
  avoided. **Night movement is restricted.**"
- `src/generators/safetyProfile.js:105` (`occupied`): "Movement is restricted and monitored.
  … **curfew and checkpoint protocols.**"

WHICH IS THE RECORD: the machine sentence. §V.0 floor 1 replaces the old quantifier with "a
face is lawful iff no field in its tab's same-page read set, **at any value the keys admit**,
denies it", and expressly directs the refuter to check "the PRINTED roster **and the same-page
producers**", naming `threatAssessment.js:57-66` as a non-corpus producer of exactly this kind.
None of the five DS-DEF-2 key functions reads `config.stressTypes` (the chair's PREIMAGE
ruling), so the keys admit every stress — including `monster_pressure`, which on a `plagued`
country is the likeliest of them. This is NOT the frozen-versus-live seam and therefore not a
wiring row: there is no seam, only a face and a co-firing string that say opposite things about
the same night. It is also precisely the chair's standing instruction: "Write nothing that a
siege, an occupation or a famine on the same page would make absurd."

CURE: the muster's material at the militia tiers is the household's day, not the town's policing
of its own people. Replace the insight rather than hedge it —
"The muster says whoever goes up is whoever the household could spare that day."
(positive landing, no universal, V-09 clean, no overlap with v3 f2's "on the line or at its own
work" or v1 f3's cutting back the ground.)

### FAIL 2 — v2 f6 `[watch]` · FLOOR 1 · the same-page stress producer, universally quantified

QUOTE: "the round is walked the same way whatever the country is doing"

THE FIELD THAT DENIES IT: `config.stressTypes`, same read set, via `generateSafetyProfile`:

- `src/generators/safetyProfile.js:152` (`insurgency`): "**Patrol patterns have changed.**"
- `src/generators/safetyProfile.js:160` (`slave_revolt`): "**Normal patrol patterns have been
  abandoned.**"
- `src/generators/safetyProfile.js:197` (`monster_pressure`): "…has **changed** how the
  settlement operates after dark."
- `src/generators/safetyProfile.js:212` (`mass_migration`): "**The watch is overwhelmed** by
  unfamiliar faces."

WHICH IS THE RECORD: the machine sentences. The aggravator is the face's own "**whatever** the
country is doing" — it is universally quantified over exactly the variable the floor-1 test
quantifies over, so it cannot be saved by any reading. The watch seats only at town and city,
which narrows nothing: every string above is tier-agnostic. Compare v3 f7, which makes the same
speaker's point with a BOUNDED claim ("the round covers what it covers") and passes.

CURE: keep the demarcation stake, drop the invariance and the perfect —
"One of the watch says the walk above the gate belongs to whoever stands it, and the round goes
under it."
(positive landing, present tense, no universal; and it takes the watch off v3 f7's ground.)

## CRAFT — POOL GRAIN: **DULL**

Twelve distinct speakers (gate · stranger · muster · hall · guild · tavern · register ·
garrison · court · market · elders · watch), three spines in three shapes, three true pairs,
genuine stakes on both halves of the preimage, and a pool far richer than the three flat
shipped spines it replaces. None of the DULL triggers about permutation, absent speakers or
missing stake is present. It is refused on ONE measured collapse, and it is the collapse the
chair singled out as the one a cure does not move.

**THE COLLAPSE: THE NEGATED LANDING, AT TWICE THE BAR.** The chair's ruling is "do not land the
final clause on a negation more than a third of the time", measured in batch 4 at 16 of 27.
This pool lands **20 of 32 faces (63%)** on what is NOT there — worse than the batch-4 pool
that produced the ruling. Per variant: v1 **7 of 11** (f1 "not for using on a person" · f3
"none of that cutting is counted" · f6 "are not asked what they saw" · f7 "not from the line" ·
f8 "have not stood a night" · f9 "never as a matter of the town's" · f11 "does not get back");
v2 **7 of 10** (f1 "not from the country" · f2 "is not always a cart" · f3 "did not stop
talking" · f5 "Nobody here asks" · f6 "nobody has asked whether it should be" · f8 "Nothing
about the line has come" · f9 "nobody here calls that early"); v3 **6 of 11** (f1 "nobody could
say who holds it" · f3 "and by nobody else" · f4 "has never been asked to choose" · f5 "it is
not done in the hall" · f9 "nobody there says what it would find out" · f10 "and never during
it"). The bar for this pool is about 11. Roughly nine faces must be turned to say what IS done
while keeping the landing noun. The two FAIL cures above already turn two of them.

**COMPOUNDING IT — "the line" is the pool's only noun.** It carries the wall in 14 of the 35
renderings and is the LANDING noun of v1 f7 and v1 f8, two siblings in one variant, which the
mechanical rule ("a different subject, order and landing noun") refuses on its own. The word is
correctly chosen — it is the one wall-word true at all five tiers, with no material and no
scale — but the pool leans on it where the {defmaterial} slot, the bar, the walk, the ground
outside and the stair are all available and already in use elsewhere in the pool.

**AND TWO SIBLINGS SHARE AN OPENER.** v3 f5 "One of the garrison says…" and v3 f7 "One of the
watch holds…" open on the same three words in one variant, against "no face may open on the
same three words as a sibling". (v1 f8 and v2 f6 also use the frame, but in other variants,
which the rule permits.)

**AND SPINE 3 ECHOES ITS OWN FACE 1.** The spine: "anyone standing on it is somebody who is not
anywhere else that night, those who hold the way through say." Face 1: "nobody could say who
holds it when those people are wanted elsewhere." A spine co-renders with every face of its
variant, and this is the same thought twice — the who-holds-it frame and the elsewhere landing
both. The lexical match is loose enough that the build gate did not catch it; a reader will.
Face 1 should move to what the stranger alone can report (what the town has stopped noticing),
which is the card §7 interest the stranger is seated for and which no other face in v3 uses.

## WIRING

- **W-1 · THE PROVENANCE VERB LIMB, CONFIRMED AT THE REGEX.** All three `[elders]` faces
  (v1 f11, v2 f10, v3 f11) open **"The elders say"**. `src/domain/prose/moveGrammar.js:225`
  matches PROVENANCE on `…|the elders (?:say|hold|remember|keep)|…` — the same limb as
  "from the road", which the chair records as having cost two faces in batch 4. The three
  shipped rows of this pool carry NO record of any kind, so the pool's citation ceiling is
  ZERO and three matches spend a citation the pool never carried. THE FACES STAND — no field
  denies them, the denier is machinery, and their content (the day lost, the clothes worked
  in, the household's own business) is the best militia-half material in the pool. The cure is
  the opener alone, and the chair has already written it: name the source through a ROLE —
  "One of the elders has it that…". `proseComposed.walker` read green at `507ff2637`, so the
  ratchet had headroom at this head; it will not on the next pool that adds one.
- **W-2 · `threatAssessment.js:59` hardcodes "Palisade and citizen militia"** on a branch that
  fires for any walls row, while this key reads the LIVE roster via `standingDefenseForces` and
  the machine string reads the generation-time snapshot. Card §8 W-09 already rules this a
  wiring row and not a finding; no face in this pool rides the collision (none names a material
  or a body outside its seated source), so it is recorded for completeness only.
