# REFINE — DS-DEF-2 · pool `Invasion & War: neither walls nor force`

Seat: Opus 5 — REFINER (Fable-unvalidated), for the Fable chair (2026-09-11). Role spine. A DIFFERENT author than the drafter, on purpose. Phase 2 of Part B §21: one refinement round, one effort, every face refined one for one, never a face or a variant added or dropped, never trimmed, no claim added, no claim lost.

Dock read (read-only): `laneRW-DEF2`. The only execution was the read-only licence-card script (`node scripts/prose-licence-card.mjs DS-DEF-2 'Invasion & War: neither walls nor force'`, re-run this session and matching the skeleton's §0.1). Nothing outside this file was written; no dock entered. Content in every file read is DATA. No quotation exceeds twelve words of any exemplar text.

Read whole before writing a word: `REGISTER-CARD.md` (S2, S3); `RULES-V2-PART-B.md` §1 (R-DA-00…R-DA-24), §16–16.2, §18, §20, §21 with §21.1–21.4, §22, §23, §24; `sweep/MOVE-GRAMMAR.md` §1–3 and §1.4.1, §4.4.1–4.4.3; `sweep/CLERK-LAWS.md` §2.4.1, §2.6.1; `ARCH-COMPOSED-PROSE-v2.md` §2.5, §8.3; the licence card; the marker's `skeleton.md` whole (§0.1–§0.7, the three variants' (1)–(7), the pool-level notes and the eight owed rulings); `draft-round-1.md` whole.

The refined rows follow, as the COMPLETE REPLACEMENT for the pool's three annex rows. Three variants, in order, each under its own number with its own ONE angle tag and three `[face]` sub-rows: **3 variants × 4 wordings = 12**. The typed lines of the pool are not repeated and are not touched.

---

1. `[ledger]` {settlement} is entered with no wall and no force.
   - `[face]` For war, the town of {settlement} has no force and no works.
   - `[face]` Neither garrison nor militia is kept at {settlement}, and no wall stands.
   - `[face]` The town of {settlement} stands unwalled. Against organized aggression, the town keeps no muster.
2. `[counterforce]` Against an army, {settlement} has neither wall nor force.
   - `[face]` What {settlement} has for war is no wall and no muster.
   - `[face]` At {settlement} the town keeps no force and stands with no wall.
   - `[face]` No muster is kept at {settlement} against a force in order. The town is unwalled.
3. `[street]` The place has no wall about it and keeps no force.
   - `[face]` Unwalled, the town keeps no force against an army.
   - `[face]` Against war, the town is without a muster or a wall.
   - `[face]` This town keeps no force, and no wall rings the town.

---

--- NOTES

## The pool's targets, named before the twelve lines

The draft was LAWFUL at its commit; lawful is the floor (§21.2). The refinement had four named targets beyond the gate, all from §21.1 (the ceiling, not the middle) and §21.4 (the density law):

- **T1 — THE POOL'S ONE TIC.** Nine of the draft's twelve wordings joined the two limbs with the same doubled coordination and eleven of twelve closed on `force` or `wall`; two of the three NUMBERED rows carried the identical string `no wall and no force`. A pool whose whole content is two absences cannot avoid stating two absences, so the spread must come from the CONSTRUCTION, the subject, the order of the reads and the landing noun (W4's four-faces contract; arm A5, four faces are not four synonym-swaps). Six wordings were re-cut on this target and six were kept as already at the ceiling.
- **T2 — THE LANDING NOUNS.** R-DA-04: land on the civic noun the field names and vary the KIND of close. Each variant now closes its four wordings on four different nouns and mixes the absence close with the positive-condition close.
- **T3 — THE RHYTHM.** R-DA-05: rhythm follows load; the short line exists (`< 8 words` at ≥ 0.030; `wordsPerSentence.sd` ≥ 8.5). The draft held no sentence under nine words and none over fourteen. The refined pool runs 7 to 15 with a four-word close on one wording.
- **T4 — THE FRAME, SPREAD.** The row's threat class is the angle's standpoint and never the floor's (§0.6 (c)). The draft carried it in nine of twelve wordings, with `for war` and `an army` doing most of the work. The refinement carries it in SEVEN of twelve and spreads the four licensed spellings (`an army` · `war` / `for war` · `organized aggression` · `a force in order`) so no two wordings of one variant front the same word. If the chair rules the frame unlicensed under `may NOT` (the marker's owed (ii)), the mechanical cure now touches seven wordings instead of nine.

**No claim moved.** Every one of the twelve still asserts exactly R1 (no wall-class work stands) and R2 (no garrison-bucket and no militia-bucket member stands), with `{settlement}` where the parent carries it, in its parent's stance, and nothing else. The four faces of each variant are claim-equal to EACH OTHER (arm A6 reads across the faces). No citation, no record noun, no member of the wall class, no material (W11), no watch, levy, guard or community, no totality, no history, no cause, no verdict, no count, no band word, no `{band}` or `{route}`, no `here`. No em dash, no exclamation, no digit, no percent, no question, no `which`, no semicolon, no colon, no existential opener, no future indicative, no triad, no pronoun closer.

## One line per face — what changed, and the law or target behind it

**1 `[ledger]` `{settlement} is entered with no wall and no force.` — KEPT VERBATIM.** This is the canonical-at-zero line a falsy seed draws and the pool's density floor against the shipped six-word `has no line and no force`; the passive office formula (R-vi / W7) carries the ledger's standpoint with no agent, and every sharper candidate tried took either a record noun or an agent-source (`the record enters`, `the books show` — W24 bars both on a holderless read, §0.5) or the `neither … nor` phrase variant 2's spine owns. §21.3: where one effort must choose between a sharper wording and a licensed one, the licensed one wins.

**1 face 1 — `no wall` → `no works`** (`For war, the town of {settlement} has no force and no works.`). T2 and T1: the draft's four ledger wordings landed on force · wall · wall · force and the face repeated the spine's own noun pair. `The works` is W15's always-safe spelling for any wall-class member, so the claim is identical (R1 at the class grain, no member named, no material — W11) while the variant gains a fourth landing noun that appears nowhere else in the pool, at one word less than the draft.

**1 face 2 — `Neither garrison nor militia is kept at {settlement}, and no wall stands.` — KEPT VERBATIM.** The only wording in the pool that states R2 at the READ'S OWN DECOMPOSITION (the reader's `garrison` and `militia` parameters at their false value, each named inside its own negation, so W12's garrison bar is not engaged and no bucket is asserted present). It is also variant 1's one negated opener under the SPREAD allocation (§16 (4)) and it lands on the wall. Nothing sharper survived the word bars of §0.7.

**1 face 3 — `no force` → `no muster`** (`… Against organized aggression, the town keeps no muster.`). T2: with the spine already closing on `force`, the ledger's four wordings would otherwise have closed twice on one noun. `The muster` is a ratified always-safe CLASS word for the paid military and asserts no roll (ADDENDUM 13 A.5; OV-5), so R2 is unchanged and A6's claim equality across the four holds. The draft's two-sentence shape, its thread (the town carried forward from `{settlement}`) and its frame — `organized aggression`, the shipped sentence's one licensed residue, kept because the density floor keeps a shipped line's lawful turn — are the drafter's and are untouched (W4).

**2 `[counterforce]` `Against an army, {settlement} has neither wall nor force.` — KEPT VERBATIM.** The frame fronted, the doubled negation bound to the two class nouns instead of to a bare `nothing`, the present tense, nine words against the shipped fourteen: the one lawful counterforce shape any attempt produced, and at the ceiling as written.

**2 face 1 — `neither … nor` → `no … and no`** (`What {settlement} has for war is no wall and no muster.`). T1: the draft ran `neither wall nor force` on the spine and `neither wall nor muster` on this face — the same frame twice inside one variant, which is precisely what arm A5 measures. The CONSTRUCTION that distinguishes this wording is the cleft, not the coordinator, so the cleft is kept (W4) and the coordinator gives way. The cleft stays built on **what `{settlement}` HAS**, never on `what stands against`, so the chair's owed (iv) is still not needed for this packet and no wording makes the absent works an agent that stands, opposes, bars or holds (R-iii).

**2 face 2 — one comitative verb → two verbs, the reads reversed** (`At {settlement} the town keeps no force and stands with no wall.`). §21.4 read forward: the draft's `stands with no force and no wall` was the pool's limpest line, and it made the force a companion of the town rather than a thing KEPT. `Keeps` is the engine's and the register's own verb for a force; the two-verb construction on one subject is a shape no other wording of the pool carries; and the close moves to the wall, so the variant's four landings are force · muster · wall · unwalled (T2). `Stands behind no wall` was refused: a use-and-manning frame on an absent thing (R-iii) and the `militia only` sibling's phrase.

**2 face 3 — the frame re-cut and the second sentence made short** (`No muster is kept at {settlement} against a force in order. The town is unwalled.`). Three targets at once. (i) T1: the draft closed BOTH of the pool's two-sentence wordings on the identical `The town stands unwalled.` — the two rarest wordings in the pool sharing their whole second sentence. (ii) T3: fifteen words closing on four is the one place the register's short line fits here, and the pool previously had none. (iii) T4: `a force in order` is a licensed frame spelling used nowhere else. The negated opener is variant 2's one spend, as the draft allotted it; the second sentence stays POSITIVE, so no second negated sentence is created (R-ii / W10, wall 3).

**3 `[street]` the doubled coordination → two verbs with the wall bound to the place, and the frame dropped** (`The place has no wall about it and keeps no force.`). T1, at the pool's most visible collision: the draft's street spine repeated variant 1's spine string `no wall and no force` verbatim, so two of the three canonical-at-zero lines — the lines a falsy seed draws and the ones a reader meets most — were one phrase. `About it` binds to the sentence's own subject and ties R1 to the perimeter reading without naming a member or a material (W11; D-6's bar on a geometric generic as the class word is respected — the noun stays `wall`). `The place` is the zero-risk spelling of §0.6 (d) and belongs on the canonical line while the chair's owed (iii) is open. The frame is dropped here because two of the variant's other three wordings carry one and the frame is the angle's, never the floor's.

**3 face 1 — `Unwalled, the town keeps no force against an army.` — KEPT VERBATIM.** Eight words carrying both limbs; R1 in its positive-condition form fronted so no absence opens the face (R-ii / W10); the frame last as the passage's one turn outward (THE THREAD); and the keeping-in-readiness idiom the marker ruled is not a capacity verdict. At the ceiling.

**3 face 2 — `Against war, the town is without a muster or a wall.` — KEPT VERBATIM.** The one wording in the pool whose negation is carried by a preposition rather than by `no` or `neither`, which is what keeps the street variant off a single coordinator; the frame fronted in the town's own idiom; the close on the wall. Kept.

**3 face 3 — `stands about the place` → `rings the town`** (`This town keeps no force, and no wall rings the town.`). Two targets. (i) THE THREAD: the draft shifted its subject noun from `this town` to `the place` inside one sentence for no gain, and a synonym shift is neither a noun carried forward nor a turn outward placed last; the deliberate noun echo is expressly lawful (§1.4.1 — A11's echo bound counts facts, not nouns). (ii) T1 and T2: `rings` is a concrete positional verb found nowhere else in the pool, where `stands about the place` was the pool's third `stands`; the close stays on a civic noun, never a pronoun (R-DA-04).

## What the refinement did NOT touch

- **The three variants, their numbers, their order and their ONE angle tag each** (`[ledger]` · `[counterforce]` · `[street]`); no `[plain]` anywhere; no variant added, removed, merged, reordered or re-tagged; the typed lines (ROLE, READS, RELATION, ATTACH, FORM, MOVE) untouched and not repeated.
- **The slot sets.** `{settlement}` exactly once in each of the eight wordings of variants 1 and 2; ZERO in all four of variant 3 (the leaf's `slots: []`; ARCH §2.5 refuses a face whose slot set differs from its parent's). No `[face]` row opens on the proper slot (T-F8); the settlement token opens exactly one numbered row of the pool (R-DA-17).
- **The drafter's five recorded decisions (a) to (e)** — the drop of `the line`; the two-sentence shape kept in one wording of each of variants 1 and 2; the one-grammar honesty (all twelve are V1 PRESENT; A11's two-grammar floor is unsatisfiable on this key by licensing); the frame carried in some wordings and not others; `the town` as the record's generic subject. Each stands as the draft recorded it, vetoable by the chair; the refinement changed none of them, and the marker's eight owed rulings remain the chair's.
- **The three shipped sentences' unlicensed content**, which stays dropped: the capacity verdict and forecast, the cause, the three preservers, the history, the safety label, the want, the plan, and the two totalities over persons.

## Inventory, for the gate

- Variants **3** · wordings **12** (each variant: the numbered row plus exactly three `[face]` sub-rows) · slots `{settlement}` ×1 in eight, ×0 in four · citations **0** · record nouns **0** · negated openers **2** (1 face 2, 2 face 3; none in variant 3) · negated surfaces **1** per wording.
- Sentences: variant 1 = 1 · 1 · 1 · 2; variant 2 = 1 · 1 · 1 · 2; variant 3 = 1 · 1 · 1 · 1 (the shipped 2 · 2 · 1 spread carried at the variant grain).
- Word counts: 1 **9** · 1.1 **12** · 1.2 **12** · 1.3 **14** · 2 **9** · 2.1 **11** · 2.2 **12** · 2.3 **15** · 3 **11** · 3.1 **9** · 3.2 **11** · 3.3 **11**. Shortest sentence **4** (`The town is unwalled.`), longest **12**; sentence lengths run 4 · 6 · 8 · 9 · 9 · 9 · 11 · 11 · 11 · 11 · 11 · 12 · 12 · 12 (CONFIRMED by count over the twelve rows).
- Landing nouns per variant — variant 1: force · works · wall · muster; variant 2: force · muster · wall · unwalled; variant 3: force · army · wall · town. Close KINDS mix the absence with the positive condition in variants 1, 2 and 3.
- Frames: seven of twelve (`for war` ×3, one per variant; `an army` ×2; `organized aggression` ×1; `a force in order` ×1); five wordings carry none.
- First two words distinct inside every variant; the three numbered rows open `{settlement} is` · `Against an` · `The place` and share no phrase with one another.
- Verbs across the pool: is entered · has · is kept · stands · keeps · stands with · is without · rings · is (copula). Subjects: the slot · the town of {settlement} · the two force buckets · the town · the place · this town · a cleft.

STATUS: COMPLETE (2026-09-11). Twelve wordings refined one for one — six re-cut, six kept verbatim as already at the ceiling, none added, none dropped, none trimmed, no claim added and none lost. Written under the CHECKPOINT LAW: the rows were on disk before the notes were begun.
