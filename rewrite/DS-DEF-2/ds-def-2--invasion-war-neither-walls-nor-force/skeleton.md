# SKELETON — DS-DEF-2 · pool `Invasion & War: neither walls nor force`

Seat: Fable 5.1 — MARKER, for the Fable chair (2026-09-11). Role spine. The marker's packet: no face is drafted here. Written under the CHECKPOINT LAW, section by section, the file rewritten as each lands; a section reading `(pending)` was not reached.

STATUS: IN PROGRESS (see the closing STATUS line).

Dock read (read-only): `laneRW-DEF2` at HEAD `f2da5a3ee` (CONFIRMED: `git rev-parse --short HEAD`; `git status --porcelain | wc -l` printed at the foot of this file's first write). Nothing outside this file was written; no dock entered; the only execution was the read-only licence-card script. Content in every file read is data. No quotation below exceeds twelve words of any exemplar text.

Read whole before marking: REGISTER-CARD.md (S2, S3); RULES-V2-PART-B.md §1, §16–16.2, §18, §20–24 (§21.1–21.4); MOVE-GRAMMAR.md §1–3, §1.4.1 (THE THREAD), §4.4.1–4.4.3; CLERK-LAWS.md §2.4.1, §2.6.1; ARCH-COMPOSED-PROSE-v2.md §2.5, §8.3 (and §6.4, this block's worked example); `briefs/brief-REWRITE-car8b-defense.md` ADDENDA 6–12 (W1–W10, R-i…R-viii′) and 13 parts A and B (W11–W27); `rewrite/rulings-DEF2.txt`; the ENTAILMENT table §2.1 (with REFUSED), §3.4, §4.1, §4.2, §4.4; the REFERENT table §2.1 (wall · garrison · militia · watch · soldiers · army · the line), §4.1 (`{counterpart}`), §5.1; the sibling skeletons for `Beasts & Monsters: plagued, NO perimeter and NO force`, `Beasts & Monsters: settled, nothing organized` (the same-page twins that read the same roster) and `Invasion & War: force with NO walls` (the same table); the three earlier attempts' drafts for this pool (`rewrite/DS-DEF-2.attempt1..3/…/draft-round-1.md`) as DATA for hazards, never as a draft to continue.

## 0. The card, the key, the census and the block header, as read

### 0.1 The licence card (`node scripts/prose-licence-card.mjs DS-DEF-2 'Invasion & War: neither walls nor force'` in laneRW-DEF2, verbatim in the lines that bind)

- `reads: invasionRowSituation(walls, garrison, militia) (via INVASION_ROW_POOL in defenseStateProse.js)` (absent ⇒ no candidate; a modifier is silent, never "false")
- `predicate: … === no walls, no force`
- `bag: {band: RESERVED, route: proper, settlement: proper}` · `FILLED at this block's call sites: {settlement}`
- `relation: (a spine takes no relation)` · `seat/form: (not a seat-taker) / sentence` · `move: (none declared)` · **`angle: counterforce ledger street`**
- `attach: (empty)` · `echo: spine mounts 1 (tabs: defense) · modifier mounts 0` — the echo key is the WHOLE table-rung reading, shared by every pool that selects a row of `INVASION_ROW_POOL`
- `covert: no` · `audience: player (no mark)`
- `source: muster · standing LICENSED — a citation of this holder is licensed where the provenance budget allows` (see §0.5: on every town this pool fires on the holder resolves to nothing, so the lawful citation count is zero)
- `may claim: that the reader invasionRowSituation(walls, garrison, militia) selects the row no walls, no force of INVASION_ROW_POOL in defenseStateProse.js, as a STANDING fact of the record`
- `may NOT: a count, a cause, a season, a future, a standpoint, a second fact` (no `another civic object of the class …` line prints for this pool: the census row's `objectClass` is null, `objectClasses` empty)
- `REFUSED COLUMNS, always: a totality over persons; an exemption from a duty (whoIsExempt is null everywhere); a named character and that character's fate (product scope); a theological claim about a deity (the deity doctrine)`

### 0.2 The census row (`docs/content/wiring-census.json`, CONFIRMED)

`status: RESOLVED` · `rung: table` · `keyFunction: INVASION_ROW_POOL` · `readsGrain: branch` · `narrowed: false` · `reads = branchReads = fieldsRead = [invasionRowSituation(walls, garrison, militia) …]` · `objectClass: null` · `sites: [defense.threatAssessment]` · `k: 2` · `covert: false` · `slotsNamed = slotsFilled = [settlement]` · `slotsWithoutProvider: []` · `variants: 3` · `grammars: 1` · tier THIN. `source.kind: muster`, `source.fields: {walls: muster, garrison: muster, militia: muster}`, `holder: null` (town-resolved by `holdersOf`), `standing: LICENSED`, `twoSource: false`. `departure: null`.

**Rate: 218 of 768 towns (2839 bp; lo 2531 · hi 3168; wizard-weighted 3229).** By tier: **thorp 68 of 128 · hamlet 72 of 128 · village 64 of 128 · town 14 of 128 · city 0 · metropolis 0.** The city and metropolis silences are censused LAWFUL (value-class). So this is the small-tier pool of its table: seven firings in eight are on a thorp, hamlet or village, and every wording must be true at once on a thorp with a `Household levy`, a village with nothing, and a town with its required `Town watch` (§0.4).

### 0.3 The generated leaf and the shipped rows

`src/data/dossierStateProse/defense.generated.js:674-697`: vids 1 · 2 · 3, angles `ledger` · `counterforce` · `street`, faceCounts 1 · 1 · 1; **variant 3's slot set is EMPTY** (`slots: []`), variants 1 and 2 carry `{settlement}` once. The annex: block header at `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2568`, the bold pool line at `:2655`, the rows at `:2656-2658`:

1. `[ledger]` {settlement} has no line and no force. Organized aggression cannot be resisted here; what preserves the town is distance, diplomacy, or being beneath notice.
2. `[counterforce]` Nothing has come for {settlement} and nothing about the town would stop it. The safety here is entirely a matter of nobody having wanted to.
3. `[street]` The town's plan for an army is to not be interesting to one, and everybody here can state the plan.

### 0.4 What the read is, from the code (CONFIRMED, read-only)

`defenseThreatProse` (`src/domain/display/stateProse/defenseStateProse.js:611-660`): `forces = standingDefenseForces(settlement)` (`src/domain/institutions/defenseInstitutionBuckets.js:169-182` — the LIVE, ruin-filtered roster through `liveInstitutions`, `institutionRoster.js:30-42`, never the `defenseProfile.institutions` snapshot); `walls = forces.walls.present`, `garrison = forces.garrison.present`, `militia = forces.militia.present` (`:623-626`); `invasion: rung(invasionRowPoolKey(walls, garrison, militia))` (`:656`). `invasionRowSituation` (`:476-483`) returns `'no walls, no force'` only when `walls` is false AND `garrison` is false AND `militia` is false (a garrison outranks a militia, so on this row BOTH force buckets are consulted and both are empty). `INVASION_ROW_POOL` (`:458-465`) maps that row to this pool key; the table is TOTAL (the lens is never silent).

The ONE keyed condition this pool carries (R-v: the reads of a key are one keyed condition, never a second fact), all limbs STANDING on the live roster:

- **R1 — no wall-class work stands.** The walls bucket (`wall · citadel · palisade · earthwork · inner citadel · massive walls`, `:84-87`, substring-matched) is empty: no `Palisade` (thorp), no `Palisade or earthworks` (hamlet · village), no `Town walls`, no `Gates (if walled)` (caught by `wall` inside "walled": so no gate row either), no `Citadel`, no `City walls and gates`, no `Massive walls and fortifications`. A LACK (MOVE-GRAMMAR §1.2 row 11 class (a), a `none-exists` world fact). **Layer of the read: BODY, negated** (the walls bucket, empty).
- **R2 — no garrison-bucket member and no militia-bucket member stands.** The garrison bucket (`garrison · barracks · professional guard · professional city watch · multiple garrison`, `:88-91`) and the militia bucket (`citizen militia · militia`, `:92-94`) are both empty: no `Barracks`, no `Garrison`, no `Professional city watch`, no `Citizen militia`. A LACK. **Layer of the read: BODY, negated** (two buckets, empty). The engine's own class word for what is absent is `force` (the reader's own doc: "walls against a professional garrison or a militia"; the pool key's own spelling "neither walls nor force"); the ratified class words for the paid military are "the muster" and "the town's force" (ADDENDUM 13 A.5; PART B rule 5).
- **The frame.** The STATE-KEY fixes the row label `Invasion & War`, and the reader is the invasion reader: the two absences are read AGAINST the class of threat the row assesses. See §0.6 (c) for what the frame licenses and what it does not.

**What the read does NOT consult, and so no face may assert or deny:** the WATCH bucket (`town watch · city watch`; `Town watch` is REQUIRED at town, `institutionalCatalog.js:1348-1355`, so EVERY town-tier firing of this pool holds a standing watch — 14 of 128 towns), the MERCENARY bucket, the CHARTER bucket, the `magicDef` bucket, the thorp's `Household levy` (`:99-104`: "One able-bodied adult from each household musters …" — in NO bucket, though the threat plan's force regex admits `levy`, `threatDefensePolicy.js:49-50`), the readiness badge, the safety label, the pay gate, the country's monster tier, the terrain, the route, the hour, any count. Contrast the DS-DEF-5 `NO organized force at all` selector, which consults six buckets: THIS selector consults three, so "nothing organized", "no defense", "nobody under arms", "nothing armed", "undefended" are FALSE on a thorp with a levy or a town with its watch and are refused as totalities (W20; CLERK-LAWS C2; the D-F3 pattern). The licensed absences are of the works and of the muster (garrison-or-militia), and nothing wider.

**Where the same-page twin sits.** The Beasts row of this block reads the SAME roster (`perimeter = walls`, `force = garrison || militia`, `:655`). On a town that fires this pool the Beasts rung prints `settled, nothing organized` (the low tier), `plagued, NO perimeter and NO force` (rare: the threat plan pushes a fortification and a force onto every plagued roster, `threatDefensePolicy.js:70-93`, so only a ruin stamp or an import reaches it), or nothing at all (`frontier` with neither is a corpus silence, `:437`). So the two absences this pool states are, on most of its towns, stated a second time by the Beasts spine on the same mount: that echo is the TABLE'S structure (ARCH §6.4: five rungs at one mount), not a writer's fault, and the writer differentiates by FRAME (the army against the creatures) and by construction, never by inventing a third fact. Recorded for the chair, not ruled.

### 0.5 The source: no citation and no record noun on any face of this pool

The card prints `source: muster · standing LICENSED` at the KIND grain. The muster kind's ONLY roster holder is a `Citizen militia` with an instantiated `Muster training` service (`holderTable.js:279-288`: "A town with a Garrison and no militia has men under arms and no roll of them"; OV-5; ADDENDUM 13 A.5). On this row `militia === false`, so on every town this pool fires on the holder resolves to NOTHING (the census row prints `holder: null`), and none of S3's three reasons obtains (no two accounts; no count; no keeper who is a power). Under W24 (a read that resolves NO holder carries no record word) and PART B rule 5, every face: cites nothing; uses no record noun (roll · rolls · muster roll · books · count · register); states the fact on the read's own nouns (the wall, the works, the force, the muster as a CLASS word, the town). The `[ledger]` tag is a STANDPOINT and licenses no citation (W24, W8); "entered as standing", "set down as", "stands on the record as" is the office's own formula and is lawful (R-vi/W7). **Provenance budget for the pool: zero.** The exemplar registers cite at 0 per 786 sentences (§24).

### 0.6 The block header, the register, the layers, and four readings recorded for the chair's veto

**Block header (annex `:2568-2593`).** STATE-KEY: five fixed rows, each with a `scoreBand` badge, read against `config.monsterThreat`, the institution presence flags and `compound.inst`. SLOTS `{settlement}` `{band}` `{route}` — this pool names and fills `{settlement}` only on variants 1 and 2 and NO slot on variant 3; `{band}` is RESERVED and `{route}` unfilled: a face naming either drops (ARCH §2.5, a face whose slot set differs from its parent's is refused). SECTION-TARGET `defense`. PROVENANCE + FENCE: the shape EXTENDS the `buildThreatAssessment` lattice into a pool (each branch holds ONE string today); the walls read rides the predicate, never a presence check on `institutions.walls`; institution presence is a STANDING fact with no recorded history; "the causal clauses here are capability clauses … never historical ones". **The card outranks the header on capability:** `may NOT: a second fact, a standpoint, a future`, and ADDENDUM 8 (2) rules that the block's title is its name and not a licence; the header's "capability clauses" is the older law and licenses no verdict on what an attacker could do (reading (b) below). PDF PARITY: parity.

**The register.** The register card's six one-line registers were read; this pool is THE DOSSIER (the record itself; the clerk's third person; the six shapes of its closed set; the town's name is not the default opener). The three tags are STANCES inside that register (W27), from the block's own §0b palette (annex `:118-126`): `[ledger]` "the clerk's view — what the books, rolls and counts show" (cut by W24 to a standpoint that licenses no record noun); `[counterforce]` "the thing that did NOT happen — the cap, the prop, the restraint, plainly stated"; `[street]` "the town's own talk about its condition".

**The referent layers this pool's read carries (ADDENDUM 13 B rule 1; tagged beside every claim in §1–§3).** walls → BODY, negated. garrison + militia → BODY, negated (two buckets; the muster CLASS word). `{settlement}` and "the town" → NONE (the record's subject; a lawful possessor of what it keeps and does not keep; never an agent that decides, plans, answers or hopes — W22/W23). The row's threat class (an army, a force in order, organized aggression) → EXTERNAL BODY, HYPOTHETICAL: no possessor, no name (`{counterpart}` is declared on other blocks of this desk and filled on none, REFERENT §4.1), never arrived, never acting. No HOLDER, ORGAN, POWER, ROLE, AGGREGATE or TRADITION layer is reached; `{band}` is RESERVED and its word is never written.

**Four readings recorded for the chair's veto, each re-derived from the card and the rulings.**
(a) THE TWO ABSENCES ARE ONE KEYED CONDITION, and every wording states both (ADDENDUM 8 (1); R-v; ADDENDUM 7's SKELETON RULE). They may share ONE negated surface ("has no line and no force"; "neither wall nor force") or a positive frame ("stands open, with no force kept"); two ABSENCE moves side by side ("No wall. No force.") is wall 3, and an ABSENCE never opens a face (R-ii/W10 lets ONE negated surface on the present read stand per variant, and it is not an absence opener; a second negated sentence is). A face stating one absence is a lost read.
(b) NO CAPABILITY VERDICT IS LICENSED. The lattice's own string for this cell (`threatAssessment.js:143-144`: no walls or garrison; cannot resist organized aggression; survival by distance, diplomacy or irrelevance) is product code outside the REWRITE's corpus (OW-20) and the shipped `[ledger]` row copies it; the card prints no capability read, and "cannot be resisted", "would not be stopped", "takes the town", "would find nothing against it" are each a CAPACITY fact the fields do not hold (MOVE-GRAMMAR §1.2 row 1's may-NOT column), an outcome VERDICT (§1.3: no VERDICT move), and a FORECAST in the modal (A2; `may NOT: a future`). `rulings-DEF2.txt` already lists this block's `walls with NO force` "takes this town with ladders" as a breach of the same class. The subjunctive is a lawful MODALITY (A2), but a lawful mood does not license an unlicensed content.
(c) THE ROW'S THREAT CLASS IS A LAWFUL FRAME, NOT A CLAIM. The STATE-KEY's row label `Invasion & War` and the reader's own doc name the class of threat the row assesses; a face may state the two absences AGAINST that class ("against an army", "against a force in order", "for war") as the standpoint of the reading — the row's own subject, not a second fact — with the layer EXTERNAL BODY (hypothetical). What the frame may NOT do: say one has come or not come (HISTORY without provenance; the block's own fence); say what one would do or meet (reading (b)); give it a possessor or a name (W25; `{counterpart}` unfilled on this block); call it "raiders" (the Beasts row's and the stress record's word — C7), "the besieger" (W17: a siege entails a besieger; no siege is read here), "the enemy" (a relation no field computes). A face that states the condition with NO frame ("neither wall nor force stands at {settlement}") is equally lawful; the frame is the angle's, never the floor's. Vetoable.
(d) "THE TOWN" IS THE RECORD'S GENERIC SUBJECT, LICENSED AT EVERY TIER; "A TOWN" IS A BAND. Variant 3 carries NO slot, so its four wordings must name the settlement generically or not at all. The ratified always-safe spellings of W15 themselves use "the town" at every tier ("the town's force", "what the town has built", "what the town holds back"), the engine's `fieldSynonyms` says "the town's pay", and the shipped rows of every small-tier pool on this desk use it; so "the town" as the subject noun is licensed as the record's generic and carries no tier value. What IS a value claim (W3, W5; CLERK-LAWS C2 against the head record on the same page): "a town" with the generic article as a band, "the village", "the hamlet", "a place this size", "a town of X's weight". The plagued twin's marker read "the town" as a value claim on thorp/hamlet/village and the settled twin's marker read it as the naming form's alternative; this packet takes the settled reading and flags the split for the chair. "The place" and `{settlement}` are the zero-risk spellings; "the community" is the engine's below-town generic and needs a tier read this key lacks, so it is refused.

### 0.7 The words, ruled for this pool (each with its bar)

- **the wall · no wall · unwalled · no works · nothing built:** the walls class in its negation, safe of every member (A-7; REFERENT: "the wall · the perimeter" HOLD). "No perimeter" is true here (no member at all stands) but the class has no always-safe geometric generic (D-6); prefer "no wall" / "no works". Never a member ("no palisade", "no ditch", "no gate", "no earthwork" — a second civic object of the wall class, and a material by W11), never "never walled" (history).
- **the line:** ONE spelling, TWO layers on this desk (REFERENT row 43): the walls BODY on DS-DEF-2/5, the MANNED FRONT on DS-DEF-7; "never 'the line' where the pool reads a force" — this pool reads both. The shipped "no line" is the CLAIM of R1 and is lawful as a claim; the WORD is refuter-exposed; "no wall" is the same claim at zero risk. A writer who keeps it keeps it at the pool's risk (the `force with NO walls` sibling's skeleton ceded "no line" to this pool for word allocation; the referent table's bar outranks that allocation).
- **the force · no force · the muster · the town's force:** the engine's own class token and the ratified class words (A.5; rule 5). "No force" is the pool key's own spelling and the always-safe negation of R2. "The muster" as a CLASS word asserts no roll; "the muster roll" / "the roll" is a record word and is barred (§0.5).
- **no garrison and no militia:** the two buckets the reader consults, named at their false value — LICENSED as the read's own decomposition (W20: a body word where the pool's read reaches the row; the reader's own parameters are `garrison` and `militia`, and the sibling keys of this table spell them). Not the W12 `Garrison` row asserted present, not "the militia" asserted present. Refuter-exposed only if a face reads "the garrison" as a present body.
- **soldiers · soldiery · armed · under arms:** REFUSED as the negated thing. A-6's one always-safe person-plural spelling is "the town's force"; "no soldiers" is a spelling the watch's part-time guards (town) and the levy's adults (thorp) fall under, so the negation is FALSE on those rosters; "nothing armed", "nobody under arms", "neither walled nor armed", "nothing in it is armed" are totalities over the persons the read does not consult (attempts 2 and 3 used every one of these — a recorded hazard, §4).
- **the watch · the guard · the levy · the community · specialists · hired men · the charter hall:** REFUSED, present or denied (W13; A.5 struck "the guard"; W20; §0.4). At town the watch STANDS on every firing.
- **the town · the place · {settlement}:** the record's subject (§0.6 (d)). Never a thinking, planning, hoping, deciding, answering agent (W23; R-DA-13's belief-frame floor; the D-F16/D-F18 pattern).
- **an army · a force in order · organized aggression · war:** the row's threat class as a FRAME only (§0.6 (c)). Never "raiders", "the besieger", "the enemy", "a siege", "an attack" as an event, never with a possessor, never arrived or repelled.
- **safety · survival · danger · exposed · helpless · at the mercy of:** REFUSED — fate and verdict nouns (A2; THE PROMISE; the UNWALLED-SMALL precedent refused "safety"); "safety" also collides with the SAFETY LABEL, an AGGREGATE of DS-DEF-3 on the same desk (W20; label family 3.3).
- **distance · diplomacy · terms · notice · interest · appetite · the road · the miles · a march · the prize:** REFUSED — second facts no field on this card holds (no geography read: `{route}` is unfilled; no treaty read: the treaty pools are DS-WAR's; no salience field exists) and a cause (`may NOT`); attempt 1 carried all of them as "inherited debt" and refused every variant — the debt is dropped, never carried (ADDENDUM 6: only the unlicensed part of a shipped sentence is dropped; ADDENDUM 13 A: a known breach is never kept as a floor).
- **undefended · open on every side · nothing between · no defense at all:** REFUSED as totalities (§0.4); "undefended" also matches the readiness label `Undefended` (L-54), an AGGREGATE this key does not read (W20).
- **stands open · lies open:** "open" as the unwalled condition is a condition word on R1 (attempt 3's "lies open") and is lawful as R1's positive frame so long as it is bound to the wall in the same clause and is not made a route or a terrain; "open on every side" is a totality.
- **the way in · the approach · the country outside:** A-12's safe region spellings; a bare "the way into {settlement}" asserts no geography beyond the settlement (attempt 3) but "the road", "the pass", "the ford" do; use only as the frame's direction, never as an observable.

## Variant 1 `[ledger]`

### (1) Number and angle
`1.` `[ledger]` — index 0, canonical-at-zero (the line a falsy seed draws). Slot set `{settlement}` (one occurrence). Two sentences as shipped.

### (2) The shipped sentence, verbatim
{settlement} has no line and no force. Organized aggression cannot be resisted here; what preserves the town is distance, diplomacy, or being beneath notice.

### (3) Every claim it makes, one per line (licence · referent layer beside the read's layer)
- "{settlement} has no line" — no wall-class member stands at the town — **LICENSED** R1 (`walls === false` on the live roster; the walls bucket empty). The CLAIM is the read itself; the WORD "the line" is refuter-exposed (REFERENT §2.1 row "the line": ONE spelling, TWO layers on this desk — walls-BODY in DS-DEF-2/5, the manned front in DS-DEF-7 — with the bar "never 'the line' where the pool reads a force", and this pool reads a force). "No wall" is the same claim at zero risk. "has" binds the absence to the town as possessor (W1's lawful direction). Layer **BODY, negated** (walls) beside a BODY read — match.
- "and no force" — no garrison-bucket member and no militia-bucket member stands — **LICENSED** R2 (`garrison === false` AND `militia === false`; "force" is the engine's own class token, the pool key's own spelling and the ratified class word's root, ADDENDUM 13 A.5). One negated surface carrying both limbs (R-ii/W10). Layer **BODY, negated** (the two force buckets) beside a BODY read — match.
- "Organized aggression cannot be resisted here" — **UNLICENSED**: a CAPACITY verdict on the arrangement (MOVE-GRAMMAR §1.2 row 1's may-NOT column: PRESENT never carries a capacity fact the field does not hold; §1.3: no VERDICT move); a FORECAST of an outcome in the modal (`may NOT: a future`; A2 state never fate); a second fact; the lattice's own cell string copied into the corpus (`threatAssessment.js:143-144`; OW-20: product code outside the REWRITE's corpus, licensing nothing); "here" a deictic standpoint (`may NOT: a standpoint`). The one licensed residue is the FRAME WORD — "organized aggression" names the row's threat class (§0.6 (c)) — never the verdict on it. Layer **EXTERNAL BODY, hypothetical**, unpossessed and made the agent of an outcome — refused (W25; W23).
- "what preserves the town is" — **UNLICENSED**: a CAUSE (`may NOT: a cause`, named outright as the sentence's subject in a cleft); "preserves" a fate noun's verb (THE PROMISE; A2); a second fact.
- "distance" — **UNLICENSED**: a second fact no field on this card holds (no geography read; `{route}` unfilled; the GEOGRAPHY move needs a route/terrain field, MOVE-GRAMMAR §1.2 row 8). Layer **NONE** (a terrain/route token) on a BODY read — a wrong-layer noun (W20).
- "diplomacy" — **UNLICENSED**: a second fact (no treaty read; the treaty pools are DS-WAR's); a relation with an unnamed outside power no field computes (W23; W25). Layer **POWER, unnamed** — refused on this read.
- "or being beneath notice" — **UNLICENSED**: a second fact (no salience field exists anywhere in the engine); a BELIEF frame on unnamed others' attention (R-DA-13's belief-frame floor; the FEELING non-move); a standpoint. Layer: an EXTERNAL BODY's interior — refused.
- Form findings, not claims: the numbered row opens on `{settlement}` (lawful for at most ONE variant of the pool, R-DA-17; never for a `[face]` sub-row, T-F8); the second sentence is the lattice's verdict and cause in the summarising position (R-DA-03 / R-DA-12 measure the summarising second sentence at zero); the semicolon carries a gloss; three preservers by habit (the triad, R-DA-10's floor ≤ 0.020); "the town" in the second sentence carries the thread forward from `{settlement}` — that thread is lawful in itself.

### (4) The reads the rewrite must state
Every face of this variant states BOTH limbs of the one keyed condition (§0.4; ADDENDUM 8 (1); R-v) and nothing else:
- **R1** no wall-class work stands at `{settlement}` — inside its own negation or as the positive condition bound to the wall (no wall · no works · nothing built · unwalled · stands open of any wall); never a member ("no palisade", "no gate", "no ditch"), never a material (W11), never a history ("never walled").
- **R2** no force of the garrison class and none of the militia class stands — at the class grain (no force · the town's force, none · no garrison and no militia as the read's own decomposition · the muster, none kept); never the watch, the levy, the guard, "soldiers", "armed", "under arms", "nothing organized", "undefended" (§0.4; §0.7).
The LICENSED claims of the shipped sentence are exactly R1 ("has no line") and R2 ("no force"); both are carried. The frame ("organized aggression", an army, a force in order, war) is lawful as the reading's standpoint only (§0.6 (c)) and is optional; the verdict, the cause and the three preservers are not carried.

### (5) The angle's stance, in one sentence
`[ledger]` is "the clerk's view — what the books, rolls and counts show" (§0b), cut by W24 to a STANDPOINT: here it may set the two absences down as the record's standing entry against the row's own class of threat — no wall and no force entered against `{settlement}` for war, in the office's own formula (entered as standing; stands on the record; carried as none — R-vi's lawful surface) — and cite nothing, since on every town this pool fires on the muster kind resolves NO holder (§0.5; W24; the budget is zero); it may NOT use a record noun (roll · books · count · register), cite, say what an army could or could not do (reading (b)), name a cause or a preserver (distance · terms · treaty · notice · the miles · the prize), give a geography or a season, say what has or has not come (history), count, name the watch or the levy or the guard, use the readiness or safety label's word, or write `{band}` or `{route}`.

### (6) The turns worth keeping (the density floor)
- "{settlement} has no line and no force." — six words, both limbs on ONE verb with ONE joint and ONE negated surface: the pool's density floor and the register's short line (R-DA-05: the short line exists; ADDENDUM 6: a shipped clause lawful under the card may stand verbatim as ONE of the four faces). It is lawful as a CLAIM as it stands. Two cautions the chair rules, not the drafter (Owed (i) and (vii) below): the word "line" is the referent table's exposed spelling on a pool that reads a force — "has no wall and no force" is the same six-word compression at zero risk; and the verbatim form opens on `{settlement}`, which only the NUMBERED row may do (T-F8 refuses it on a `[face]` sub-row) while R-DA-17 says the town's name is not the default opener — so the verbatim clause can live only at index 0, the canonical line, or be carried inside a sentence.
- The SHAPE "has no X and no Y" — the possessive verb with the doubled negation inside one surface — is the lawful shape for stating both limbs at once (§0.6 (a)); a face may carry it in its own vocabulary at the same compression (keeps · holds · carries · is entered with).
- The second sentence has no lawful clause to keep; its one lawful WORD is the frame ("organized aggression" → an army · a force in order · war), which a face may carry as the standpoint of the reading, never as a subject that acts.

### (7) What would make the rewrite a regression here
- INVENTORY: variant 1 stays at index 0 with the `[ledger]` tag — ONE tag, no `[plain]` marker (attempt 1 wrote `[plain]` on every spine row and is the hazard; ADDENDUM 3 (1)) — and grows to four wording faces (the numbered line plus three `[face]` sub-rows), every face carrying `{settlement}` exactly once as the parent does (ARCH §2.5: a face whose slot set differs from its parent's is refused); a fall, a merge, a reorder, a second bracketed tag, or a `{band}` / `{route}` written in is a trim or a malformed row.
- A LOST LICENSED READ: a face stating one absence only (a face without R2 reads as `walls with NO force`'s or DS-DEF-11's UNWALLED cell; a face without R1 reads as `militia only`'s or `force with NO walls`'s negative); a face that states R2 as a totality wider than the two buckets ("nothing organized", "no defense", "nobody under arms", "nothing armed", "undefended") is the same loss by another door, because it is FALSE on a thorp with its levy and on every town with its required watch (§0.4).
- A LOST LAWFUL TURN: giving up the one-verb, one-surface compression of both limbs for two flat negated sentences with no law behind the change (§21.4 the density law; ADDENDUM 6 the floor against the SHIPPED sentence; ADDENDUM 7: an inventory line is not a rewrite) — attempt 2's "{settlement} is an unwalled town. It keeps no garrison and no militia." is the inventory pair AND a second negated sentence (an absence opener, R-ii/W10, wall 3); attempt 3's "Works and soldiers are absent at {settlement}." is the compression kept with a barred word.
- A DROPPED ANGLE: a ledger face that reads as the street's talk ("the town says", "here") or the counterforce's restraint under the `[ledger]` tag; a ledger face that CITES or carries a record noun — "the roll shows", "on the muster roll", "the books", "the count" (W24; R-vi; §24's ceiling is one citation per unit on a licensed holder for an S3 reason, and no holder resolves on any town of this pool, §0.5).
- AN ADDED CLAIM (the walker's C-pair arm; the refuters' grounds): any of the unlicensed claims above returning in new words — "cannot be resisted", "cannot be defended", "would find nothing against it", "nothing arranged against it", "would not have to be fought", "would meet nothing", "takes", "holds"; "preserves", "keeps the town", "safety", "survival"; "distance", "the miles", "a march", "the crossing", "terms", "agreements", "the prize", "a poor return", "a campaign", "notice", "interest", "appetite" (attempt 1 carried all of these as "inherited debt" and refused every variant; the debt is DROPPED, never carried — ADDENDUM 6, ADDENDUM 13 A); a history ("never walled", "nothing has come", "has never had to"); a cause word ("because", "so", "which is why"); the watch, the levy, the guard, "soldiers", "armed", "under arms", "a company", "an armed body", "enrolled" (attempt 1 — a roll word); a count; a season; the country's tier ("frontier", "settled" — the Beasts row's read, W2); the terrain; the readiness or safety label's word.
- FORM: a `[face]` row opening on `{settlement}` (T-F8); a `which`; a colon or semicolon carrying a gloss; an em dash; a digit; a question; an existential opener ("There is no wall at …"); a future indicative; "here" as the ledger's deictic (the record's third person names the town); a maxim frame (W5); a triad; a second negated sentence (R-ii/W10 — one negated surface per variant; a second is an absence opener, wall 3); the absence opening a `[face]` as its whole architecture.
- THE THREAD (MOVE-GRAMMAR §1.4.1; R-i at k = 0): this line is the spine and is read first; each face closes on a civic noun of the key — the wall, the force, the town, or the frame noun as the one turn outward placed last — never on "here", "it", "notice", an abstraction or a pronoun; a two-sentence face carries the town or the force forward into its second sentence. No modifier attaches at this dock (`attach: empty`), so the thread is the face's own second sentence where it has one — and see Owed (vii): a lawful second sentence is hard to fill on a one-read key.
- SIBLING DISTANCE on the SAME PAGE (A11; C7): the Beasts spine states the SAME two absences on the same mount on most of this pool's towns (`settled, nothing organized` at the low tier; `plagued, NO perimeter and NO force` rarely; §0.4) — this pool differentiates by FRAME (the army, never the creatures or "raiders") and by construction, never by a third fact; its ledger row must not read as the settled twin's ledger row with the noun swapped. DS-DEF-11's UNWALLED cells (the `[counterforce]` arm that says the wall did NOT get built; the why is that cell's) compose beside on every firing: nothing here says why there is no wall. DS-DEF-5's force-surface pools compose on the same tab: nothing here says "nothing organized" or names a bucket the read does not consult. The five sibling rows of `INVASION_ROW_POOL` a reader meets across towns: `force with NO walls` owns "keeps a … force and no perimeter" and "no line" was ceded to THIS pool by that skeleton (§0.7: the referent bar outranks the allocation); `walls with NO force` owns the wall-without-people shape; `militia only` owns "armed citizens on their own ground".
- WORD BARS on this read (§0.7 whole): "the line" only at the pool's risk; "no perimeter" true but not the safe generic; "soldiers" / "soldiery" / "armed" / "under arms" never as the negated thing (a totality over the persons the read does not consult); "the watch" / "the guard" / "the levy" / "the community" never; "the garrison" only inside its negation as the read's own parameter, never asserted; "the muster roll" / "the roll" never; "safety", "survival", "helpless", "exposed", "undefended", "open on every side" never; "distance", "diplomacy", "terms", "notice", "interest", "the road", "the prize" never; "raiders", "the besieger", "the enemy", "a siege", "an attack" never; `{band}` and `{route}` never written; "a town" / "the village" / "the hamlet" / "a place this size" never as a band (§0.6 (d)).
- FOUR FACES, NOT FOUR PARAPHRASES (W4; the owner's four-faces rule): the four ledger faces differ in CONSTRUCTION — the entry with both limbs on one possessive verb (the shipped shape); the office's formula with the record as subject ("entered against {settlement} … neither wall nor force"); the frame fronted as the reading's standpoint ("For war, {settlement} …" / "Against an army …") with the doubled negation inside one surface; the positive form of R1 bound to the wall with R2 as the one negation ("{settlement} stands unwalled and keeps no force") — never a synonym in one frame.
- EAR HAZARDS recorded from the three earlier attempts (data, not findings): attempt 1 kept the verdict, the cause and the three preservers in every face ("terms", "the miles", "a poor return for a campaign", "would not pay for the crossing") and wrote "no armed body is enrolled" (a roll word on a holderless read); attempt 2 wrote inventory pairs and "no levy is called" (the levy is unread and STANDS on every thorp with a household levy) and "No soldiers are kept" (a totality); attempt 3's "No wall rings {settlement}, and the town keeps no force under arms" is close to lawful in shape (one sentence, the wall then the force, the town carried) with "under arms" the barred word; its "The town does without a perimeter at {settlement} and without arms" is a USE frame ("does without") on the absent things (R-iii) and "arms" the barred totality.
