Seat: MARKER (opus), DS-DEF-2 · pool `Beasts & Monsters: settled, defenses beyond the need` · the skeleton the writer drafts from; nothing here is a face.

# DS-DEF-2 · `Beasts & Monsters: settled, defenses beyond the need` · SKELETON

Three shipped variants, vids 1 to 3, angles `[counterforce]` `[ledger]` `[visitor]` in that order. The rows below are the annex's at the dock tip (`docs/content/RECEIPT_POOLS_DOSSIER_STATE.md` lines 2620 to 2623) and the generated leaf carries the same three texts with the same vids, angles and slot sets (`src/data/dossierStateProse/defense.generated.js` lines 504 to 529; the pool's manifest row at `:1098-1110` reads `role: spine`, `variantCount: 3`, `faceCounts: [1,1,1]`, `vids: [1,2,3]`). Every row's slot set is `{settlement}` alone. The writer rewrites these three, one for one, and gives each its four `[face]` sub-rows.

**THE TEST THIS PACKET IS MARKED UNDER (ADDENDUM 14, the owner 2026-09-12).** A face is LAWFUL unless it CONTRADICTS the record. Silence is permission. "The card does not license it" is not a finding and the tag `unlicensed` does not appear anywhere below. Claims are tagged SAFE, CONTRADICTED (with the field, file and line, and which of the two is the record) or FLOOR-2.

---

## 0. What the writer reads before the first word

### 0.1 The licence card, printed this session in the dock (`node scripts/prose-licence-card.mjs DS-DEF-2 'Beasts & Monsters: settled, defenses beyond the need'`)

```
LICENCE (block DS-DEF-2 · role spine · key `Beasts & Monsters: settled, defenses beyond the need`)
  reads:      beastsRowSituation(family, perimeter, force) (via BEASTS_ROW_POOL in defenseStateProse.js)
              (absent ⇒ no candidate; a modifier is silent, never "false")
  predicate:  beastsRowSituation(family, perimeter, force) === settled country, perimeter
  bag:        {band: RESERVED, route: proper, settlement: proper}
              FILLED at this block's call sites: {settlement}
  relation:   (a spine takes no relation)
  seat/form:  (not a seat-taker) / sentence      move: (none declared)     angle: counterforce ledger visitor
  attach:     (empty: a spine takes no attach set)
  echo:       spine mounts 1 (tabs: defense) · modifier mounts 0 (none)
              the echo table is keyed on this pool's WHOLE table-rung reading, so every pool
              that selects a row of `BEASTS_ROW_POOL` shares ONE echo key
  covert:     no
  source:     muster · standing LICENSED
  may claim:  that the reader selects the row `settled country, perimeter` of `BEASTS_ROW_POOL`,
              as a STANDING fact of the record
  may NOT:    a magnitude outside the read's own band word (floor 2a), an elapsed course, a dated
              cause or a season (floor 2b), a prediction the pulse adjudicates (floor 2b)
  audience:   player (no mark)
  REFUSED COLUMNS, always: a totality over persons; an exemption from a duty; a named character
              and that character's fate; a theological claim about a deity
```

⚠ **One card line is narrower than the code, and the writer should know it.** The card prints `source: muster` as a single-source row. The predicate is a join of TWO holders: the perimeter and the force are the MUSTER's records (`holderTable.js:188`, `:189`, `:190`), and the country's threat tier is the ROAD's (`holderTable.js:234`, "the look of the country, which anyone travelling it can see"). That is a wiring row for the chair, not a wording row. It matters to the writer only in this: the tier is a thing anybody on the road can see, and the wall is a thing on a roll.

### 0.2 The block's header lines (annex lines 2568 to 2593, the parts that bind this pool)

- **STATE-KEY:** five fixed rows (`Beasts & Monsters` · `Invasion & War` · `Internal Security` · `Economic Survival` · `Disasters & Famine`), each with a `scoreBand` badge (`STRONG` / `ADEQUATE` / `WEAK` / `CRITICAL`) rendered beside the prose, read against `config.monsterThreat` (`plagued` / `frontier` / `settled`), the institution presence flags and `compound.inst`. This pool is row 1's `settled country, perimeter` branch.
- **SLOTS:** the block declares `{settlement}` `{band}` `{route}`. This pool's three parents carry `{settlement}` only, and a face's slot set must equal its parent's (ARCH §2.5's face-row refusals), so every face here carries `{settlement}` and nothing else.
- **SECTION-TARGET:** `defense`.
- **PROVENANCE + FENCE (the block's own, quoted in substance):** the `buildThreatAssessment` lattice is dossier-native and this shape EXTENDS it rather than replacing it; the corpus's job is that each branch currently holds exactly ONE string, so every settlement in a branch says the same words. Two standing defects must not be reintroduced: the `plagued`+nothing branch's lowercase sentence lead, and a presence check on `institutions.walls` in place of the predicate. **Institution presence is a STANDING fact with no recorded history; the causal clauses here are CAPABILITY clauses and never HISTORICAL ones** unless the history surface supplies the ancestry. It does not here.
- **Composition fences:** a SPINE, sentence form, no relation and no attach set, ONE spine mount on the defense tab and zero modifier mounts today. The spine is therefore FIRST in its composed unit and chooses nothing that follows it. The echo key is the whole table rung, so a mount counted against this pool may be a SIBLING ROW of `BEASTS_ROW_POOL` (one of the other six situations) and never a second print of this one.

### 0.3 The register card's six one-line registers (the dossier line is this pool's)

- The dossier: the record itself; the clerk's third person; the six shapes of its closed set; the town's name is not the default opener.
- The NPC ladder: read aloud to the players; role-bound; never a named interior; the stage licenses the claim, never the shape.
- The Herald: the estate's one quoted in-world voice; report mode; flattest where hottest; the bill lands apart from the deed.
- The chronicle: a borrowed body of headlines; its own prose is frames and dressings; the quiet year is one sentence of varied shape.
- The DM page: candid; the why only from a typed field; second person to the referee alone; it grades, never hedges.
- Chrome and the docent: never the archivist; the product speaking to the person who runs it; mechanics first, one term per thing, the label as the only emphasis.

### 0.4 The owner's rules that bind every face, restated once

Four wording faces per semantic variant, each a different vocabulary or rhythm inside the voice, never a paraphrase of its sibling. Never trim (Part B §22: the counts only rise; a face that fails the gate stays in the annex as a refusal row with its measurement). An unweighted seeded roll picks the face at render, so every face must stand alone. The exemplar, not the practical. No em dash, no exclamation mark, no digit or percent in a connective, no which-clause. The clerk who was there, compiling from records, citing a holder only where the card licenses a source and the budget allows.

**THE THREAD (MOVE-GRAMMAR §1.4.1).** This pool is a SPINE and sits first. Every face must hand a noun forward that a later modifier can pick up (the perimeter, the country, the gate, the purse, the roll) and must close on a standing fact rather than a set-up. There are zero modifier mounts on this pool today, so no sibling text is guaranteed to follow; the face must read as a complete unit alone AND as an opener.

**THE DENSITY LAW (Part B §21.4).** Compression and idiom that reward the reader are part of the ceiling. "Unclear" is not a finding unless a law is broken. Do not trade density for plainness.

### 0.5 The reads this pool reaches, resolved — MATERIAL A WRITER MAY USE, never a bound on what may be written

The predicate is one function of three inputs (`src/domain/display/stateProse/defenseStateProse.js:429-441`):

```
function beastsRowSituation(family, perimeter, force) {
  ...
  if (perimeter) return 'settled country, perimeter';
  return force ? '' : 'settled country, neither';
}
```

| read | what it holds on this key | holder | what it puts in a writer's hand | what the record can deny about it |
|---|---|---|---|---|
| `measuredMonsterFamily(config.monsterThreat)` → `settled` | the country's monster tier is the calm baseline. The producer's word is `heartland`; the corpus word is `settled`; `low` is a legacy alias (`monsterThreat.js:55-61`, `defenseStateProse.js:279-284`). MEASURED, never defaulted: an absent tier returns null and the desk says nothing (`:312-334`) | the ROAD (`holderTable.js:234`) — the look of the country, which anyone travelling it can see | the country is the quiet end of the ladder; creature activity is the lowest of three bands; the tier is a thing visible from the road | **a TOTALITY of safety.** `settled`/`heartland` only MULTIPLIES threat DOWN (monster ×0.3, occupation ×0.4, war ×0.4: `stressGenerator.js:117-125`, `:168`, `:231`) and `monsterThreat.js:20-28` calls it "the calm baseline". A `monster_pressure` or `under_siege` stress can and does stand on a heartland town |
| `standingDefenseForces(settlement).walls.present` === true | at least one STANDING member of the walls bucket is on the town's LIVE roster. The bucket keywords are `wall` · `citadel` · `palisade` · `earthwork` · `inner citadel` · `massive walls` (`defenseInstitutionBuckets.js:84-89`), matched as SUBSTRINGS over the institution's native semantic name, and the projection reads `liveInstitutions(settlement)` so a ruined wall is not counted (`:169-182`) | the MUSTER (`holderTable.js:188`) | a perimeter work stands, by its ROSTER NAME and its own catalogue description; it is standing TODAY, ruin-filtered | **the WORD.** A `Citadel` is inner and `Gates (if walled)` is a point: neither is a line around the town (F1-07). The row's own printed description fixes the material: `Town walls` = stone walls and gates ringing a town; `City walls and gates` = masonry; `Palisade` = a ring of sharpened stakes; `Palisade or earthworks` = wooden or earthen (F1-32, `institutionVocabulary.js:155`, `:157`, `:162`, `:278`) |
| `garrison.present \|\| militia.present` (the `force` argument) | **NOT CONSULTED ON THIS KEY.** The branch returns `settled country, perimeter` before it looks at `force`, by the function's own documented intent at `defenseStateProse.js:423-425`, which states that this key "deliberately does not consult the force at all" because a wall in a quiet country is the more specific fact | the MUSTER (`holderTable.js:189`, `:190`) | nothing. This is the single most important line in the packet | **any manning claim.** The pool fires with `garrison.present === false && militia.present === false`, and on such a town `safetyProfile.js:300` prints "There is no meaningful guard presence." on the same page |

**The reads the desk performs and this key does NOT reach, listed so the writer knows the page around the sentence:** `scoreBand(scores.monster)` renders the badge beside this very prose (`DefenseTab.jsx:322-341`) and is computed from different inputs than the key (§1.4 W-10 of the contradiction table: the card must print the band spread over the key's domain before any intensity word is refutable); the other four threat rows each carry their own badge from their own arm; `config.stressTypes` is read by NO key function of this block.

### 0.6 The provenance move, priced for this pool

The card licenses a citation of the muster where the budget allows. The ceiling is ONE per unit (Part B §24) and only for one of S3's three reasons: two accounts that disagree; a count from an interested party; a record whose keeper is a power. None obtains here. The muster and the road hold two DIFFERENT facts (the wall, the country), not two accounts of one; no count is stated; the muster is not marked interested (`holderTable.js:110-111`). The exemplar registers with raw text cite at zero per 786 sentences. **Recommendation: zero citations in this pool.** A13's blanket citation ban is struck and the record-word bar W24 is struck entire, so the `[ledger]` face may reach for accounts, returns, duties, the roll, the entry; that is vocabulary, not a citation, and it is free.

---

## VARIANT 1 · vid 1 · `[counterforce]`

### 1.1 The shipped sentence, verbatim

> There is very little in the country around {settlement} and there are substantial works facing it; whatever the walls here are for, it is not the creatures.

### 1.2 Every claim it makes, on the new test

- **The country around the town holds very little.** — **CONTRADICTED** `config.monsterThreat` read at its English sense. `settled`/`heartland` is the engine's "calm baseline" (`src/data/monsterThreat.js:20-28`) and multiplies the stress roll DOWN and never to zero: `monster_pressure` and `under_siege` are ×0.3 on a heartland town (`src/generators/stressGenerator.js:117-125`), and `config.stressTypes` can force either outright (`:110` onward). A `monster_pressure` stress deducts up to twenty from the monster score on this very page (`src/generators/defenseGenerator.js:432-441`). **The record is the stress roster and the tier band; the face is the thing that must move.** Contradiction-table row **F1-34** in terms: a TOTALITY of safety over the town. The row's own note: ease at the town's own grain is FREE, so the cure is to move the claim from the country to the town.
- **Works stand, and they face the country.** — **SAFE.** `standingDefenseForces(settlement).walls.present` is true by the predicate, ruin-filtered through the live roster (`src/domain/institutions/defenseInstitutionBuckets.js:169-182`).
- **The works are SUBSTANTIAL.** — **FLOOR-2** (a magnitude outside the read's own band word). The bucket admits a `Palisade` — "a ring of sharpened stakes" (`src/data/institutionVocabulary.js:278`) — on exactly the same footing as `Massive walls and fortifications`, so the size word is the writer's and no field hands it over. Row **F2-01**. Named explicitly in the block's own contradiction set (`rewrite/rulings-DEF2-v14.txt`) as one of the four shipped clauses that are still false and must not be carried forward.
- **The works are WALLS.** — **CONTRADICTED** on part of the key's range. `Citadel` is inner and `Gates (if walled)` is a point; neither is a line around the town (**F1-07**), and the roster row's own description fixes the material (**F1-32**). The record is the roster row. The safe generic on this row is *the works* or the recorded name; the specific words (stakes, an earth bank, masonry, a gate) are free wherever the face does not fix a material the row denies.
- **The walls are FOR something.** — **SAFE** as written, because the face refuses to say what. `whatever ... are for` states no cause and buys none; the record is silent on why any institution stands and silence is permission.
- **What the walls are for is NOT the creatures.** — **CONTRADICTED** on the same ground as the first claim: where a `monster_pressure` stress stands on a heartland town, the creatures are exactly what presses it. **F1-34**, and **F4-06** in its neighbourhood (the readiness band moves on tier and country while no works move, so no face may explain the works by the band or the band by the works).
- **The relation between the two halves is a mismatch (much work, little threat).** — **SAFE as the key's own reading.** The pool key IS `defenses beyond the need`; the STATE-KEY is the record for that comparison and the face is entitled to it. What is not entitled is the magnitude on either side of it.

### 1.3 The reads this pool reaches (material for the rewrite of vid 1)

- `measuredMonsterFamily(config.monsterThreat)` → `settled`: the calm END OF A LADDER OF THREE, stated as a band and never as an emptiness.
- `forces.walls.present`: a standing perimeter work, by its roster name or by a generic the row does not deny.
- The key's own comparison: a perimeter in the quiet band. The mismatch is the pool, not an inference.
- NOT reached and deliberately unread: the force. This face must survive an unmanned wall.

### 1.4 ⭐ WHAT WOULD BE FALSE HERE

**Contradiction-table rows this pool's key can actually walk into:**

| row | the claim that would be false | the field that denies it |
|---|---|---|
| **F1-34** | a totality of safety — "nothing threatens {settlement}", "no danger here", "there is very little in the country" | `stressGenerator.js:117-125`, `:168`, `:231` (heartland multiplies down, never to zero); `monsterThreat.js:20-28`; `threatAssessment.js:113-130` builds an `Invasion & War` row for EVERY town and `DefenseTab.jsx:150-183` renders it beside this sentence |
| **F1-07** | walls, a circuit, a perimeter, a LINE around the town, where the standing member is a `Citadel` (inner) or `Gates` (a point) | `defenseInstitutionBuckets.js:84-89`, `:169-182`; DS-DEF-5 prints the works on the same desk |
| **F1-32** | a wall MATERIAL the row's own printed description fixes otherwise (stone on a palisade town, timber on a masonry city) | `institutionVocabulary.js:155`, `:157`, `:162`, `:278` |
| **F1-33** | a material SOURCE ("cut from its own woods", "the stone of the country") where a fortification chain runs | `supplyChainData.js:873-884` feeds every wall row `rawInputs: ['Quarried stone','Building materials']`. Where no chain is instantiated, the source is free |
| **F1-01 / F1-02 / F1-03 / F1-04 / F1-05 / F1-06** | a watch, a garrison, a militia, "the guard", a mercenary company or a charter hall as a standing body | `priorityHelpers.js:45-54`; the force buckets `defenseInstitutionBuckets.js:84-107`; `safetyProfile.js:300` prints "There is no meaningful guard presence." on the denial side |
| **F1-26 / F1-27 / F1-28 / F1-29** | a militia AND a watch as two bodies (`exclusiveGroup: 'civilianDefense'`, `institutionalCatalog.js:1340-1354`); a town watch called professional, full-time or soldiers (it is "Part-time guards. Night patrol and gate duty."); a CITY without soldiers of its own (`Garrison` is `required: true` at city, `:1925-1930`); at city, the watch and the garrison contrasted as two bodies |
| **F1-40** | outrunning the readiness BADGE printed beside the prose, or borrowing another arm's | `defenseGenerator.js:487-522`; `defenseScoreBands.js:33-39`. ⚠ §1.4 **W-10**: this pool's key and the badge are computed from DIFFERENT inputs, so an intensity word here is INSTRUMENT-HELD and not yet refutable either way |
| **F4-02 / F4-03 / F4-04** | two purses split at birth (the wall kept and the muster not); the four gates split in DIRECTION; a TOTAL collapse of pay | ONE multiplier over "garrison wages, wall maintenance" together (`defenseGenerator.js:182`, `:189-192`); all four gates are `min(1, floor + econOutput/50 × (1−floor))` on one input and differ in degree only; every gate has a floor and the community baseline is exempt, so the licensed extreme is short, late or thin and NEVER none |
| **F4-06** | the readiness band explained by the works | `defenseGenerator.js:510`: the band moves on tier and country while every wall and roster row is unchanged; `:491` gives a bonus for being cheap to defend |
| **F4-07** | the river or the coast doing nothing for the town's defence | `defenseGenerator.js:129-135` — riverside and coastal carry a small POSITIVE multiplier |
| **F1-126** | a minted PROPER NAME borne by the face (a person, a tavern, a lane, a family) | a pooled face is authored once and drawn by every town whose key matches, so the name prints identically across a region. The town's NPC roster and the `{npc}` slots are the name authority; this pool has neither |
| **F2-01 … F2-09** | any magnitude, date, season, duration, founding, elapsed course, rate, trend, or dependency on a field the key cannot see | the band vocabularies are closed; `history.age` is frozen at birth and rerollable; `ageBands.js`'s `HISTORICIZE_BAND = 'years-past'`; NO state-prose pool key reads a history field |

**THE CLOSED ROSTERS THIS POOL TOUCHES (floor 1).** Five rosters are closed, and a body, building, record-keeper, force or faith-house they do not carry may not be asserted: the institution roster over the LIVE roster (`institutionRoster.js`, `liveInstitutions`); **the force buckets — `garrison`, `militia`, `watch`, `mercenary`, `charter`** (`defenseInstitutionBuckets.js:84-107`, plus `magicDef`); the faction list; the faith entries; the NPC office roster. Everything else is silence, and silence is permission. In particular: the walls bucket's closed keyword set is `wall · citadel · palisade · earthwork · inner citadel · massive walls`, and a perimeter thing outside it is not on this town's roll.

**Not a faith pool.** The deity axes, the derived temper, the pantheon rank, the settlement standing and the suppressed flag do not arise here and no face may reach for them.

### 1.5 ⭐ THE PREIMAGE — the range of towns this key selects

`settled country, perimeter` fires on **every town whose measured monster family is `settled` and whose walls bucket has at least one standing member**, and on NOTHING ELSE about the town. That is:

- **Every tier** from thorp to metropolis. A thorp's `Palisade` and a metropolis's `Massive walls and fortifications` select the same three sentences. So do `Town walls`, `City walls and gates`, `Citadel`, `Gates (if walled)`, `Palisade or earthworks`, and any custom row whose native semantic name contains one of the six keywords.
- **With a force and without one.** The branch returns before it reads `force` (`defenseStateProse.js:438-440`). A heartland town with a wall, no garrison, no militia and no watch reads this pool, with "There is no meaningful guard presence." printed on the same page (`safetyProfile.js:300`).
- **Every stress state.** No key function of this block reads `config.stressTypes`. This pool prints under `monster_pressure`, `under_siege`, `occupied`, `famine`, `plague_onset`, `wartime` and the rest, each of which renders its own banner on the same dossier. **A face about a quiet country is absurd under an ACTIVE SIEGE banner, and the siege is the record.**
- **Every route, culture, prosperity rung and population band**, none of which the key reads.
- **Both clocks.** DS-DEF-2's box is two clocks inside one `<div>`: rows 1 and 2 are live, rows 3 to 5 are the generation-time snapshot (`DefenseTab.jsx:317-321`, §1.4 W-11). This row is a LIVE one.

A face must contradict no state in that range, not merely the town on this skeleton.

### 1.6 The angle's stance in one sentence

`[counterforce]` states the thing that did NOT happen — the cap, the prop, the restraint, plainly stated — so here it may name what the quiet band has spared the works, or what the works have never been asked to do, or the pressure that never came to the wall, always as a STANDING condition and never as an event that ran, a course that elapsed or a prediction the pulse adjudicates.

### 1.7 The turns worth keeping

- *whatever the walls here are for, it is not the creatures* — the SHAPE is the pool's best asset and it is reusable: a refusal to name a cause, which opens a question instead of closing one. The second half must move (F1-34); the first half's move, declining to state a purpose the record does not hold, is exactly the register's "leave one matter standing open".
- *There is very little in the country around {settlement} and there are substantial works facing it* — the MISMATCH is the pool's whole claim and must survive into every face. The two magnitudes on either side of it must not.
- The semicolon joint between a condition and its counterweight is lawful and rationed; keep at most one face on it so the pool does not collapse onto one punctuation.

### 1.8 ⭐ WHERE THE FLAVOUR IS (vid 1)

- **A heartland town's perimeter was never planted for the beasts, and the engine says so structurally.** `threatDefensePolicy.js:73-86` plants a fortification only where the threat is `plagued`, or `frontier` at town-plus; a heartland town is not in that branch at all. So the wall on this key came from the tier's own fabric — a city has walls because it is a city. That is not a cause a face may assert, but it IS the reason the mismatch is real, and it points the writer at what a perimeter in a quiet country actually IS on the ground: civic furniture. A boundary, a customs line, a thing the town is inside of, rather than a thing holding something out.
- **The gate is the concrete particular nobody has used.** Every walls row above thorp carries gates in its own description (`Town walls` = "Stone walls and gates ringing a town"; `City walls and gates`), and `Town watch` carries `Gate duty: {on: true}` as a live service row (`institutionServices.js:1324`). What a stranger meets is a gate with nothing to decide: leaves that are not closed, a bar that is not dropped, an arch used for arriving. The contradiction table names exactly this as free — "the bar has not been dropped in years" is ease at the town's own grain, which F1-34 licenses.
- **The absence has a shape and it is not emptiness.** The counterforce angle wants the thing that did not happen; what did not happen here is that the works were never put to the question. An earth bank nobody has had to stand on. A walk whose only traffic is people taking the short way. Stakes whose points face a country that sends nothing at them. None of that is a count, a date or an elapsed course; all of it is a standing condition, and the conversion the contradiction table recommends in terms — *a date closes a question; a standing condition opens one*.

### 1.9 What would make the rewrite of vid 1 a regression

Vid 1 not first, or not `[counterforce]`, or its slot set not `{settlement}` alone, or fewer than four faces, or no longer the pool's canonical index-zero line. Any face that loses the mismatch (the works against the quiet band) — that is the pool's discriminating claim, and without it a face is claim-identical to `settled, nothing organized`'s or to a frontier row's. Any face carrying `substantial`, `very little in the country`, or a re-spelled totality of safety. A face that fixes a material or a shape the roster row denies. A face that opens on the `{settlement}` proper slot (T-F8) or closes on a set-up rather than a standing fact.

---

## VARIANT 2 · vid 2 · `[ledger]`

### 2.1 The shipped sentence, verbatim

> {settlement} is comfortably over-provided against beasts. The pressures that matter to this town are internal, and its defensive spending does not reflect that.

### 2.2 Every claim it makes, on the new test

- **The town is over-provided against beasts.** — **SAFE.** This is the pool key's own reading: `settled country, perimeter` is spelled `Beasts & Monsters: settled, defenses beyond the need` in `BEASTS_ROW_POOL` (`defenseStateProse.js:411-412`), and the STATE-KEY is the record for the comparison. Nothing denies it.
- **COMFORTABLY — an intensity on the over-provision.** — **HELD, not a finding either way.** The badge rendered beside this prose is `scoreBand(scores.monster)` (`DefenseTab.jsx:322-341`), computed from different inputs than the key (`defenseGenerator.js:195-227`: the tier base, the four institution bonuses, a magic bonus and the monster upkeep multiplier). §1.4 **W-10** rules this an INSTRUMENT row: the card must print the band spread over the key's domain before any intensity word on this pool is refutable. The writer should know the badge can read `ADEQUATE` beside a face that says `comfortably`, and should prefer an intensity that comes from the ARRANGEMENT rather than from a score.
- **"beasts" as the row's subject.** — **SAFE.** `Beasts & Monsters` is the row's own printed label (`threatAssessment.js:109`). ⚠ `plagued` is MONSTER ACTIVITY and never disease; that trap is on the sibling rows, not this one.
- **The pressures that MATTER to this town are internal.** — **CONTRADICTED** on two separate grounds. (a) **F1-40**, borrowing another arm's badge: each of the five arms carries its OWN band from its own score (`defenseGenerator.js:487-522`; `defenseScoreBands.js:33-39`), and `scores.internal` can read STRONG on the same page where `scores.economic` or `scores.disaster` reads CRITICAL — in which case the pressures that matter are not internal. (b) The key reads no stress field at all, so this face prints under `famine`, `under_siege`, `occupied`, `wartime` and `plague_onset` banners, every one of which names an EXTERNAL pressure on the same dossier (`stressGenerator.js`; the banner renders above). **The record is the other arms' bands and the stress roster.**
- **The town HAS defensive spending.** — **SAFE**, and it is the engine's own model. One military purse runs over "garrison wages, wall maintenance" together (`defenseGenerator.js:182`, `:189-192`), and there are FOUR upkeep gates on this page, all `min(1, floor + econOutput/50 × (1−floor))`: military 0.6 (`:189`), monster 0.7 (`:223`), internal/order 0.65 (`:251`), disaster 0.55 (`:614`).
- **That spending DOES NOT REFLECT the internal pressure — an allocation judged wrong.** — **CONTRADICTED** by the engine's own positive model, **F4-03**. The four gates are monotone in ONE input, `econOutput`, and differ in degree only, never in direction; nothing in the engine allocates a purse BETWEEN threats, so there is no allocation for the face to call mismatched. The order purse that funds "watch wages, court and gaol funding" (`fieldSynonyms.js:244-253`) rises and falls with the same number as the wall's. **The record is the gate formulae.**
- **A verdict on the town's arrangement ("does not reflect that").** — the verdict half is otherwise free: the record rates nothing, but the RATING here is not borrowed from a rating field, it is an inference from the previous claim, and it falls with it.

### 2.3 The reads this pool reaches (material for the rewrite of vid 2)

- The key's own comparison (`defenses beyond the need`): the ledger's proper subject. A provision entered against a need the country does not press.
- `forces.walls.present`: a standing work on the roll, by its roster name.
- `measuredMonsterFamily` → `settled`: the calm band of a three-rung ladder, as a band word.
- Free to the `[ledger]` stance and NOT reached by the key, therefore silence and therefore permission: the record vocabulary struck free by the recut (W24 entire) — accounts, returns, duties, ledgers, manifests, minutes, the writ, a licence, the entry, the roll. The `[ledger]` stance may cite its own record.
- NOT reached: the force, the other four arms' bands, the stress roster, the purse's arithmetic.

### 2.4 ⭐ WHAT WOULD BE FALSE HERE

The table at §1.4 binds this variant unchanged. The rows this `[ledger]` variant walks into HARDEST, because a clerk's vocabulary invites them:

| row | the trap this variant specifically invites |
|---|---|
| **F1-40** | ranking the five arms against each other, or importing another arm's band. Each badge is its own; the block's own contradiction set states it flatly: *a badge on one arm says nothing of another*. |
| **F4-02** | splitting the two purses — the wall kept and the muster not, the wall paying for itself. ONE multiplier covers garrison wages and wall maintenance together (`defenseGenerator.js:182`, `:189-192`). A face never pays the wall and not the muster. |
| **F4-03** | splitting the DIRECTION of the four gates (patrols provisioned while the gaol starves; the watch paid and the muster not). They differ in degree only. |
| **F4-04** | a TOTAL collapse of pay — "nothing has been paid", "there is no watch left to pay". Every gate has a floor and the unpaid community baseline is exempt; the licensed extreme is SHORT, LATE or THIN and never none. A slowly thinning muster IS the model's own word (`:186-187`, "unpaid soldiers desert slowly") and is no longer barred — but a HEADCOUNT is DS-DEF-5's cell and stating one here collides with that surface. |
| **F2-01** | a sum, a share, a rate or a price on the spending. The gate is a multiplier the page never prints as a number in prose. |
| **F2-06** | a RATE. "The entry is made most years", "seldom questioned" is not lawful; "the entry is made" is. |
| **F2-02 / F2-05 / F2-08** | arrears with a duration ("three months' arrears"), a comparative against a past ("thinner than it was"), a trend ("it has grown"). `history.age` is frozen at birth and rerollable; `ageBands.js` pins `HISTORICIZE_BAND = 'years-past'`. |
| **F1-24** | a citation to a holder the card prints SOURCE-UNRESOLVED. This card resolves the muster, so a muster citation is available; the budget recommendation is still zero (§0.6). |
| **F1-03** | "the muster ROLL" as a CITED RECORD where no `Citizen militia` row stands — the muster kind's only holder is `Citizen militia` (`holderTable.js:279-288`). **"The muster" as the class word is free everywhere.** |

**THE CLOSED ROSTERS**, as at §1.4: the live institution roster; the force buckets `garrison · militia · watch · mercenary · charter` (and `magicDef`); the faction list; the faith entries; the NPC office roster. Not a faith pool: no deity axis, derived temper, pantheon rank, settlement standing or suppressed flag arises.

### 2.5 ⭐ THE PREIMAGE

As §1.5, unchanged: every tier, every country-consistent town with a standing walls-bucket member and the `settled` family, with or without a force, under every stress state, on both sides of the route and prosperity axes. The `[ledger]` angle does not narrow it. A face that reasons about the town's OTHER pressures is reasoning about a page it cannot see.

### 2.6 The angle's stance in one sentence

`[ledger]` is the clerk's view — what the books, rolls and counts show — so here it may enter the work as a standing charge against a country that does not press it, in the office's own record vocabulary, and it may NOT rank the five arms, price the purse, date the entry, or grade the arrangement from a rating field this key does not read.

### 2.7 The turns worth keeping

- *over-provided* — the pool key's own claim in one word, and the sharpest compression in the three shipped rows. Keep the CLAIM in every face; vary the word.
- The two-sentence shape (the standing fact, then the qualification in its own sentence) is the register's own and is lawful under S2 if the second sentence is a second FACT and not a gloss of the first.
- *comfortably* is worth keeping as EVIDENCE of what the pool wants — an intensity — but it should come from the arrangement (nothing is asked of the works) and not from a score.

### 2.8 ⭐ WHERE THE FLAVOUR IS (vid 2)

- **The purse is the ledger angle's own material and nobody has spent it.** One military multiplier over wall maintenance and wages together; four gates that move together on `econOutput`; a floor under every one and a community baseline that is never charged at all. What that looks like on a page: the wall is a standing charge that recurs whether or not anything comes at it, entered every time beside the wages of men who have nothing to stand against. The clerk's true subject here is a line item that never argues for itself.
- **The recut handed back the whole record vocabulary and the office who keeps it.** W24 is struck entire and W22's person bar is struck by name: an UNNAMED person may appear, act, keep a key, chase, refuse, be avoided, be resented. The engine's own `officesOf` comment says the office column "is OPEN and STAYS OPEN". So a reeve slow with the returns, a collector who has never had to explain this entry, a clerk who copies it forward because it was copied forward — all licensed plot hooks, all unused by the shipped row.
- **The over-provision has a concrete civic face: the thing the works are used FOR instead.** Nothing in the record says a wall must be defensive furniture. A gate is where a road narrows and where somebody stands; a wall is where a town's inside stops. What a quiet town's works actually do — mark a boundary, shelter a market's edge, hold a bar nobody drops — is silence in the record and therefore the writer's, and it is the plainest way to say `beyond the need` without saying `over-provided` a second time.

### 2.9 What would make the rewrite of vid 2 a regression

Vid 2 not second, or not `[ledger]`, or its slot set not `{settlement}` alone, or fewer than four faces. A face that loses the over-provision (the pool's discriminating claim). A face that carries the internal-pressures ranking or the spending verdict forward in any spelling. A face that splits the purses or the gates' direction, asserts a total non-payment, or states a headcount. A face that reaches for a sum, a rate or a duration. A pool whose four faces per row all open on `{settlement}` or all take the two-sentence shape — order constraint 10 allows the settlement token to open at most one variant per pool.

---

## VARIANT 3 · vid 3 · `[visitor]`

### 3.1 The shipped sentence, verbatim

> A stranger notices the perimeter at {settlement} chiefly for how relaxed the people on it are.

### 3.2 Every claim it makes, on the new test

- **A perimeter stands at the town.** — **SAFE** on most of the range: `forces.walls.present` is true by the predicate. ⚠ **CONTRADICTED** in the WORD on part of it: a `Citadel` is inner and `Gates (if walled)` is a point; neither is a *perimeter* (**F1-07**, `defenseInstitutionBuckets.js:84-89`, `:169-182`). The record is the roster row.
- **There are PEOPLE ON the perimeter — it is manned.** — **CONTRADICTED**, and this is the variant's central fault. The key returns `settled country, perimeter` BEFORE it consults `force` (`defenseStateProse.js:438-440`), by the function's own documented intent at `:423-425`. So the pool fires on a heartland walled town with `garrison.present === false` AND `militia.present === false`; where no law body stands at all, `safetyProfile.js:300` prints "There is no meaningful guard presence." on the same page. Rows **F1-01 / F1-02 / F1-03 / F1-04**. The block's own contradiction set names this clause in terms as one of the four shipped clauses that are still false: *'how relaxed the people on it are' (manning asserted from a summary whose own fence says it asserts nothing about any arm)*. **The record is the force buckets and the safety profile's printed denial.**
- **Those people are RELAXED.** — **SAFE IN ITSELF**, and it is the useful half. F1-34's own note licenses it: *ease at the town's own grain is free*. W22a/W22b's person bar is struck by name, so an unnamed person may act, refuse, be idle, be resented. The claim falls here only because it is predicated on the manning claim above it, not because ease is barred.
- **A stranger notices the perimeter CHIEFLY for that — a ranking of what is noticed first.** — **SAFE.** W27's stance rules are struck; a stance may reach for its own nouns and rhythm, and a stranger may act, be turned away, be told the wrong thing, pay twice. Nothing in the record denies what a stranger notices first.
- **The stranger is unnamed and bears no proper noun.** — **SAFE**, and it must stay that way: **F1-126**, a minted proper name borne by the face rather than a slot prints identically across a region. This pool's only slot is `{settlement}`.

### 3.3 The reads this pool reaches (material for the rewrite of vid 3)

- `forces.walls.present`: the work itself, standing today, ruin-filtered, with its roster name and its catalogue description available (stakes, an earth bank, stone walls and gates, masonry, an inner citadel, a point of gates).
- `measuredMonsterFamily` → `settled`: the country as a band, not as an emptiness. The ROAD is its holder, which is exactly a visitor's own vantage (`holderTable.js:234`).
- The key's comparison: works against a country that does not press them.
- NOT reached, and the trap: the force. **A `[visitor]` face may describe the WORK and the COUNTRY and the TOWN's ease; it may not put a body on the wall.**

### 3.4 ⭐ WHAT WOULD BE FALSE HERE

The table at §1.4 binds unchanged. The rows this `[visitor]` variant walks into HARDEST:

| row | the trap this variant specifically invites |
|---|---|
| **F1-01** | a WATCH as a standing body (`inst.hasWatch` false, `priorityHelpers.js:48`; `safetyProfile.js:300`, `:309` print the denial). ⚠ **"keeps watch", "the night is watched" and PATROLS are free anywhere** — `patrol` is a `force`-class token, not a body. The bar is on the BODY, not on the activity. |
| **F1-02 / F1-03 / F1-04** | a garrison, a militia, or "the guard" as a body. Where ANY law body stands, "the guard" is the engine's own word (`safetyProfile.js:271`, `:336`) — so the test is the FLAG on this town, never the tier. |
| **F1-26 / F1-27** | a militia AND a watch together (`exclusiveGroup: 'civilianDefense'`, `institutionalCatalog.js:1340-1354`); a Town watch called professional, full-time or soldiers — its own row reads "Part-time guards. Night patrol and gate duty." |
| **F1-07 / F1-32** | *perimeter* / *a line around the town* on a citadel or a gate row; a material the row's own description fixes otherwise. |
| **F1-34** | the stranger's ease widened into a totality of safety over the country. Keep it at the town's grain. |
| **F1-126** | a minted proper name — a person, a tavern, a lane, a family. The stranger stays unnamed and meets no named thing. |
| **F2-05 / F2-07** | an elapsed course in the noticing ("has stood", "no longer", "as ever", "again"); an AGE of fabric stated as an age. ⚠ age-flavour is now FREE where it does not contradict the printed age (`OverviewTab.jsx:251`, `:259` print `{hist.age} years old` on the same dossier) — "older work than the arrangement that pays for it" is lawful; "the old wall" on a four-hundred-year town is a contradiction only if the number denies it. |
| **F2-06** | a RATE in the noticing ("most days the gate stands open"). "The gate stands open" is lawful; the rate form is not. |

**THE CLOSED ROSTERS**, as at §1.4. Not a faith pool.

### 3.5 ⭐ THE PREIMAGE

As §1.5, with the one line that matters most to this variant restated: **the key does not consult the force.** The same sentence prints on a walled heartland town with a full city garrison and on a walled heartland town with nobody under arms at all, and on the second of those the page beside it says there is no meaningful guard presence. A face that survives only the first is a face that fails on the range.

### 3.6 The angle's stance in one sentence

`[visitor]` is what a stranger notices first, without being told, so here it may report the standing work as a thing met on the road and the country as a thing crossed to reach it, and it may NOT put a body on that work, name anything, date it, count it, or carry its noticing forward into a course.

### 3.7 The turns worth keeping

- *A stranger notices the perimeter at {settlement} chiefly for* — the RANKING move (what is noticed first, and what it is noticed for) is the angle's best instrument and is entirely lawful. Keep the move; change what is noticed.
- The single-sentence, single-clause shape. It is the only short line in the pool and the register says the short line exists; if all four of this row's faces run long, the pool loses its one change of pace.
- The ease itself. F1-34 licenses ease at the town's grain by name, and the `[visitor]` row is where it belongs; it needs a subject the roster carries.

### 3.8 ⭐ WHERE THE FLAVOUR IS (vid 3)

- **What a stranger actually meets is a THING, and the record names it.** Not "the perimeter" three times, but a ring of sharpened stakes, an earth bank with a ditch under it, stone walls and gates ringing a town, masonry, an inner citadel that is not a ring at all. `institutionVocabulary.js:155`, `:157`, `:162`, `:278` are the descriptions; the walls bucket's six keywords are the closed set. The recut struck W15's always-safe spelling list entire and named it *the measured cause of the flavour collapse* — 168 distinct words against the shipped corpus's 519, `country` once every twenty-one words. **Choose the vivid word.**
- **The gate is where a stranger's day actually happens, and nothing is asked of him.** `Town watch` carries `Gate duty: {on: true}` as a live service row (`institutionServices.js:1324`) and the engine's own surfaces argue about whether there are gates to bribe (§1.4 W-08 rules that a writer agreeing with either surface cannot be failed). What the record leaves silent — and therefore hands over — is the ARRIVAL: a bar that is not dropped, leaves that are stood open against the bank, an arch used for coming in rather than for keeping out, nobody who wants to know his business. Not one of the three shipped rows has a gate in it.
- **Absence on the ground has a shape, and it is USE, not emptiness.** A walk on top of a bank that is only ever a short way across town. Stakes facing a country that sends nothing at them. A ditch with the wrong things in it. Under the new test the record is silent about all of this and silence is permission; what the record denies is only a BODY the roster does not carry, and none of these needs one.

### 3.9 What would make the rewrite of vid 3 a regression

Vid 3 not third, or not `[visitor]`, or its slot set not `{settlement}` alone, or fewer than four faces. Any face that puts a person ON the works, or names a watch, garrison, militia, guard, mercenary company or charter hall as a body. Any face that fixes a material or a shape the roster row denies, or that says *perimeter* where a citadel or a gate is the standing member. A face that mints a proper name. A face that widens the town's ease into a safe country. A pool in which all four faces of this row run long, since this variant carries the pool's only short line.

---

## 4. THE POOL AS A WHOLE — what the writer must not lose

- **The discriminating claim is the MISMATCH**: a standing perimeter work in the calm band of the monster ladder. Every one of the twelve faces must carry it, or the face is claim-identical to a sibling pool's (`settled, nothing organized`, or either frontier row) and the pool has failed the only job its key gives it.
- **Three angles, three different things to be over-provided ABOUT**: `[counterforce]` the pressure that never came to the works; `[ledger]` the charge that recurs against a need nobody presses; `[visitor]` the thing met on the road and what is not asked of anyone at it. If two of the three faces could swap angles without anybody noticing, the pool is DULL at the pool grain, which is the one craft verdict the refuter may return.
- **The four shipped clauses this block's contradiction set names as still false**, two of which are this pool's: `substantial works` (vid 1) and `how relaxed the people on it are` (vid 3). They may not be carried forward in any spelling. **Everything else the shipped rows say is available again** — the old law struck many clauses for being unlicensed, and unlicensed is no longer a fault.
- **The pool-grain spread rules**: no two variants of one pool share their first two words (A11); the settlement token opens at most one variant per pool and never two adjacent (order constraint 10); a pool of three carries three DISTINCT level-1 grammars; a pool whose renderings collapse onto one construction or one vocabulary is DULL.

