# REFINEMENT ROUND — DS-DEF-2 · `Economic Survival`: `WEAK` (Opus REFINER seat, a different author than the drafter, 2026-09-11)

Seat: Opus 5 — Fable-unvalidated, REFINER for the REWRITE, working for the Fable chair. Refined against the printed licence card, the marker's `skeleton.md`, the writer's `draft-round-1.md`, the register card (with S2 and S3), Part B §1, §16–§16.2, §18, §20, §21–§23 (§21.1 the ceiling · §21.2 every set · §21.3 one effort · §21.4 the density law), MOVE-GRAMMAR §1–§3, §4.4.1–§4.4.3 and §1.4.1 THE THREAD, CLERK-LAWS §2.4.1 and §2.6.1, ARCH v2 §2.5 and §8.3, and brief ADDENDUM 12's W1–W10 with ADDENDUM 13's W11–W27.

THREE variants in, THREE out — same vids, same order, same one bracketed angle tag each, none added, removed or merged. Four faces per variant — TWELVE faces, one-for-one with the draft. Zero claims added, zero claims dropped. The typed lines of the pool (ROLE, READS, RELATION, ATTACH, FORM, MOVE) are untouched and are not repeated here.

---

## THE ROWS, READY TO PASTE UNDER THE POOL'S HEADING

1. `[ledger]` Against a crisis the town of {settlement} is entered thinly covered, and a real one would strain the cover.
   - `[face]` The holding the town of {settlement} keeps against a crisis is entered thin. A real crisis would strain that holding.
   - `[face]` The thin cover entered for {settlement} limits what the town can do in an emergency; a real crisis would strain that cover.
   - `[face]` A real crisis would strain the cover the town of {settlement} keeps against one, and that cover is entered weak.
2. `[street]` Thin cover is what the town of {settlement} would have to meet a crisis with, and a real one would strain that cover.
   - `[face]` What cover the town of {settlement} has against a crisis is thin. A real one would strain that cover.
   - `[face]` The capacity {settlement} has to hold through a crisis runs thin, and a real one would strain that capacity.
   - `[face]` Under a real crisis the cover the town of {settlement} has would strain, and that cover is weak.
3. `[unfolding]` The cover {settlement} keeps against a crisis stands thin, and a real one would strain that cover as things stand.
   - `[face]` What the town of {settlement} could do about a crisis is thin, and a real one would strain that capacity as it stands.
   - `[face]` The town of {settlement} is thinly covered against a crisis. A real one would strain that cover where it stands.
   - `[face]` In a real crisis the cover kept at {settlement} would strain, and that cover now stands thin.

--- NOTES

## N.0 THE FOUR TARGETS THIS ROUND WAS PUSHED AGAINST (§21.1 the ceiling, not the middle; §21.2 a lawful face that sits just above the floor is pushed toward the ceiling)

The draft is LAWFUL and no face of it broke a wall; every change below is a ceiling move, not a cure. The four named targets beyond the gate (§21 phase 2: the band's interior · sibling distance · the sharper fact · the read-aloud ear):

1. **THE POOL'S ONE TIC, KILLED.** All twelve draft faces closed on the SAME edge in the SAME grammar — `and a real crisis would strain that cover / that capacity / what the town holds`, the pressure as subject, the clause last, the joint `, and` twelve times over. That is the machine signature §16 item 6 names and the "same rule broken the same way everywhere" of §16's SPREAD. The edge is a WALL of the claim set (D-22: the band entails exactly the ladder's reading, and the edge is its second half), so it stays in all twelve; its GRAMMAR is now spread four ways — the pressure as subject and the edge last (six faces), the edge fronted as the whole first clause with the band landing last (V1 f4, V2 f4, V3 f4), the intransitive edge under a fronted conditional adjunct (V2 f4, V3 f4), and the edge in its own short second sentence (V1 f2, V2 f2, V3 f3). Eleven joints are `, and` or a full stop and ONE is a semicolon (V1 f3), so the pool no longer spends a single joint twelve times (R-DA-06 THE JOINT IS CHOSEN; the shipped v1's own joint was a semicolon, so the form is the register's).
2. **TWELVE DISTINCT OPENERS.** The draft repeated `What the town` three times, `The town` three times and `Against a crisis` twice across the pool's faces. All twelve faces now open on twelve distinct first-two-word pairs (`Against a` · `The holding` · `The thin` · `A real` · `Thin cover` · `What cover` · `The capacity` · `Under a` · `The cover` · `What the` · `The town` · `In a`), and the three VARIANT openers (`Against a` · `Thin cover` · `The cover`) are distinct at the first word (A11; MOVE-GRAMMAR §1.4 wall 10; the shipped pool's one-word gap between "Chronic shortfall" and "The shortfall" is not reproduced anywhere). No face opens on the settlement token (T-F8) and none opens on a negated surface (W10: the pool carries ZERO negated surfaces, against a ceiling of one per variant).
3. **THE SLOT IS SEATED SIX WAYS, NOT PARKED.** The draft parked `at {settlement}` as a locative tail in seven of twelve faces. The slot now sits in six seats — the naming form `the town of {settlement}` (W5's own licensed form, and the sibling ADEQUATE and CRITICAL pools' seat), `entered for {settlement}`, `at {settlement}` (twice, where the corpus's own STRONG row seats it), `kept at {settlement}`, `{settlement} has`, `{settlement} keeps`. Every face carries it exactly once and no face carries a second slot; `{band}` (RESERVED) and `{route}` (a COMPONENT of the score, refused twice over) are named nowhere (ARCH §2.5: each face's `{slot}` set equals its parent's).
4. **THE BAND IS SPELLED FOUR WAYS INSIDE EACH VARIANT, AND THE FACES DIFFER IN CONSTRUCTION.** Each variant's four faces now differ in SUBJECT, in the ORDER of the three components, in the band's spelling and in the landing noun — never in vocabulary alone (W4; the brief's "the four faces differ in construction (the subject, the order of the reads, the landing noun)"). The draft's V2 f2 and f3 were one construction with two nouns swapped (`X is thin at {settlement}, and a real crisis would strain X`); that pair is gone. Lengths now run 17 to 23 words (draft 15 to 23) with three two-sentence faces carrying a seven-to-nine-word second sentence as R-DA-06's short relief, so rhythm follows load rather than a metronome (R-DA-05).

**Claims: unchanged, face for face.** Every face carries exactly the draft's three claims — the ARM at its engine meaning (W14: the economic-resilience axis, never coin, revenue or prosperity), the BAND's condition (thin cover), and the BAND's subjunctive EDGE (a real one would strain it) — plus the one slot. Nothing was added and nothing was dropped; the four faces of each variant stay claim-equal to each other (arm A6 reads across the faces). The two shipped clauses the draft kept under the density floor are still kept, verbatim and in the same faces: **`limits what the town can do in an emergency`** (V1 f3) and **`what the town could do about a crisis`** (V3 f2).

## N.1 ONE LINE PER FACE — WHAT CHANGED AND THE LAW OR TARGET BEHIND IT

### Variant 1 · `[ledger]` — the construction contract KEPT (W4): the office's own formula is the surface of all four faces (`entered` · `is entered` · `entered for` · `is entered`), the ledger cites nothing and names no record (W24; A13; the card's SOURCE-UNRESOLVED)

1. **The numbered line** — `{settlement}` re-seated from the bare token into the naming form `the town of {settlement}` (W5: the naming form; it also puts four words between the opener and the slot, where the draft had two), and the draft's comma after the fronted `Against a crisis` struck so the line runs on one breath. Claims, order, band spelling (`thinly covered`, the ladder's own) and close (`the cover`) unchanged. Target: the read-aloud ear (§21.4: the line lands when read), not plainness.
2. **Face 2** — the draft's `What the town holds against a crisis at {settlement} is entered thin. A real crisis would strain what the town holds.` repeated its own five-word subject as its object, which is the summarising beat in miniature (W6) and cost the face its landing. Rebuilt on the noun `holding` — the corpus's own noun for this arm on this very table (the sibling STRONG row: "holding is entered strong at {settlement}") — so the second sentence lands on `that holding` and the thread is carried by a real noun, not by a repeated clause (R-i THE THREAD at k = 0; R-DA-04 land on the civic noun). Opener moved off `What the` to free that pair for V3 f2. Band spelling `thin`. Same three claims.
3. **Face 3** — the density-floor face, and the shipped clause `limits what the town can do in an emergency` stands verbatim as it must (§21.4: the owner's lawful clauses were written to be kept, not smoothed). Two changes: the joint is now a SEMICOLON, the pool's only one, which is the shipped v1's own joint recovered at a lawful site and breaks the twelve-fold `, and` (R-DA-06); and the face is one word shorter so the longest line in the pool is no longer this one. Subject (`the thin cover`), order (band → arm → edge) and close (`that cover`) unchanged.
4. **Face 4** — the draft's `The town is entered weak at {settlement} on what it could do about a crisis …` duplicated V3 f2's kept clause word for word, which is a cross-variant echo the refuters read as one wording in two places, and its `it` sat downstream of two nouns. Rebuilt with the EDGE as the whole first clause and the BAND as the landing (`and that cover is entered weak`) — the pool's one edge-first ledger face, so the office's verdict word arrives last, where the register puts a standing fact (R-DA-04's varied close kind; MOVE-GRAMMAR §1.4 wall 7). The band word is still the row key's own (`weak`), the possessive pronoun is gone entirely (W1 is satisfied by construction, not by binding), and `the record enters` was refused in drafting as a citation (W24: the office does not cite its own books) in favour of the passive `is entered`.

### Variant 2 · `[street]` — the construction contract KEPT (W4): the town's plain idiom (`would have to meet … with` · `has` · `has to hold through` · `has`), no office formula anywhere, no standing marker anywhere, nothing anyone knows, says or is owed

1. **The numbered line** — the subjunctive capability frame the shipped row owed this variant is now IN the canonical line: `would have to meet a crisis with` replaces the draft's flat `has to meet a crisis with`, which asserted the town meeting a crisis as a present indicative and edged toward an event the record does not hold (R-DA-07 the edge is subjunctive; state never fate). `{settlement}` re-seated into the naming form. Same claims, same order, same close.
2. **Face 2** — the draft's `What the town can do in an emergency is thin at {settlement}, and a real crisis would strain what the town can do.` closed by restating its own subject (W6) and was one construction with face 3. Rebuilt on the street's plainest possession, `What cover the town of {settlement} has against a crisis is thin`, and split into two sentences so the edge lands as a seven-word second sentence — the pool's short relief in this variant (R-DA-06; the register card's "The short line exists"). Thread carried by `cover` (R-i).
3. **Face 3** — the draft's `The town's capacity to hold through a crisis is thin at {settlement}` was the same construction as its sibling face 2 with two nouns swapped. The capability frame is kept (it is this variant's licensed shape), but the subject is rebuilt as a relative on the slot itself (`The capacity {settlement} has to hold through a crisis`), which removes the parked locative tail, removes the possessive (W1 by construction) and changes the opener; the band is spelled `runs thin`, the marker's own licensed spelling that no draft face spent (§0.2's lawful-noun column). Same three claims.
4. **Face 4** — the draft's `Against a crisis the town has thin cover at {settlement}. A real one would strain that cover.` repeated the numbered line's fronted `Against a crisis` (V1's opener) and its band spelling. Rebuilt as the pool's shortest face (eighteen words) with the edge fronted under a conditional adjunct and the band landing last (`and that cover is weak`) — a third order for this variant, the row key's own band word, and a close on a standing condition rather than on the cover a third time.

### Variant 3 · `[unfolding]` — the construction contract KEPT (W4): V6 PRESENT → OPEN, the OPEN realised LAST and by a standing marker only (`as things stand` · `as it stands` · `where it stands` · `now stands`), no office formula, no street `has`

1. **The numbered line** — the draft's close `would strain it where it stands` spent two pronouns in five words and left the face landing on an anaphor a modifier cannot pick up. Rebuilt to `would strain that cover as things stand`: the noun is handed forward for the two modifier positions the spine's `k 2` allows (THE THREAD, MOVE-GRAMMAR §1.4.1), the OPEN marker still sits last, and `stands thin` keeps the unfolding's signature verb. Claims and order unchanged.
2. **Face 2** — the shipped clause `what the town could do about a crisis` is kept verbatim, as the density floor requires. Changed: the draft's band verb `stands thin` collided with the numbered line's, so the band is spelled `is thin` and the whole standing weight is carried by the OPEN marker where R-iv puts it (last, after the presence); `at {settlement}` is absorbed into the naming form so the slot is not parked. Same three claims, same order.
3. **Face 3** — rebuilt from the draft's face 4 (`The town stands thinly covered at {settlement} against a crisis. A real crisis would strain that cover as things stand.`), which spent `stands` twice and `as things stand` a second time in the same variant. Now `is thinly covered` with the OPEN carried once, by `where it stands`, at the end of the second sentence; the two-sentence shape and the thread (`covered` → `that cover`, R-i) are kept. The ladder's own spelling of the band is preserved.
4. **Face 4** — the draft's `A real crisis would strain what {settlement} keeps against one, and what the town keeps now stands thin.` opened on the same two words as V1 f4 and repeated `keeps` inside itself. Rebuilt as the pool's shortest unfolding face (seventeen words) with the edge fronted under `In a real crisis`, the cover seated by a participle (`the cover kept at {settlement}`) and the band landing on the OPEN marker (`that cover now stands thin`) — the edge → band + OPEN order the draft wanted, with the repetition gone. `now` stays the present marker of R-DA-07 and is never a duration.

## N.2 THE WALLS, RE-CHECKED OVER THE REFINED TWELVE (the refiner's own receipt, scanned not asserted)

- **Slots:** twelve faces, `{settlement}` exactly once each, no second slot, no face opening on the token (scanned).
- **Hard walls:** no em dash, no exclamation, no question, no digit, no percent, no which-clause, no first or second person, no figure, no sense verb on an abstraction, no intent for an inanimate thing, no future indicative (scanned).
- **No body, no component, no pay word, no prosperity word, no person, no belief, no record noun, no citation:** scanned over all twelve against the marker's §8 bar lists — zero hits on granary, stores, grain, food, months, market, route, port, road, hospital, medicine, garrison, muster, militia, watch, soldiers, force, people, treasury, purse, chest, books, roll, revenue, tax, coin, money, funds, poor, hall, seat, council, pay, paid, wages, owed, arrears, upkeep, keeping, shortfall, morale, know, forget.
- **No duration, history, trend, count, cause, comparison or non-sibling contrast:** zero hits on chronic, already, still, each season, so far, yet, sustained, worsening, eroding, because, the reason, rather than, more than.
- **The edge, unhardened:** `strain` is the ladder's verb in all twelve and is always subjunctive; no `will strain`, `exhaust`, `overwhelm`, `break`, `collapse`, `run out`, `would fail`, `would not hold`, `for long` or `when one comes`.
- **The pressure is the CLASS only** (`a crisis`, `a real crisis`, `a real one`, `an emergency`); `threat` is spelled nowhere, for the reason the draft recorded (on this tab it is the Beasts row's word — rule 3 across the tab; the skeleton's §0.7 fence 2).
- **W10:** zero negated surfaces in the pool, against a ceiling of one per variant; no face can be read as an absence opener.
- **W1:** the pool now carries no possessive pronoun at all and one possessive noun phrase fewer than the draft; every possessor is the town or the slot, stated.
- **W2, W3, W11–W13, W15, W17–W19, W21–W23, W25–W27:** unreachable by construction, as in the draft — no body, material, garrison, watch, external force, stress record, creed, power, person or holder is named. W20 is the live bar on this AGGREGATE read and is obeyed: the only nouns spent are the band's own, the arm's, the pressure class and the town.
- **THE THREAD:** every face hands forward a noun an unknown modifier can pick up (`the cover` · `that holding` · `that capacity` · `what the town can do in an emergency`), none closes on a bare pronoun, and each of the three two-sentence faces carries a noun from its first sentence into its second (R-i at k = 0). Each face reads as well immediately after nothing as immediately before a sibling modifier, since it does not choose its place.
- **The close, varied in kind:** a condition, an object, the band as a standing verdict, the OPEN marker — never a maxim, a cause, an outcome, a summary or a verdict beyond the band's reading (R-DA-04; R-DA-12).

## N.3 REVERSIONS

**NONE.** All twelve faces were refined one for one; no face reverted to its draft wording, no face or variant was added, dropped or merged, and no claim moved. If the gate finds any refined face unlawful, its draft sibling in `draft-round-1.md` is the lawful fallback for that face alone (§21.2: a lawful set keeps its refinement only if it stays lawful).

## N.4 ROWS THE REFINER CARRIES UP TO THE CHAIR (recorded, not decided; the draft's four open rows stand unchanged and are not restated)

1. **The semicolon in V1 f3 is the pool's only one** and is spent deliberately, to break a twelve-fold `, and` habit at the one face long enough to carry it (R-DA-06). The shipped v1 of this pool used a semicolon, so the form is the register's own and not an import. If the chair reads the connectives floor as pinning the pool to comma joints, the change is one character and the face is otherwise untouched.
2. **`the town of {settlement}` is spent in seven of twelve faces.** It is W5's declared naming form and the seat the sibling ADEQUATE and CRITICAL pools already use on this table, so it is the corpus's own spelling; but this pool's band is reachable at village tier and below, and the naming form carries the word "town". The bare "the town" the draft used carries it equally, so nothing is worsened here — recorded because a tier-safe generic (W15's "the community" is the military's, not the settlement's) does not exist for this desk, and if the register car mints one the seat is a one-word substitution in seven faces.
3. **`runs thin` and `the holding` are spent for the first time in this pool.** Both are the marker's own licensed spellings (§0.2's lawful-noun column; the sibling STRONG row's `holding`), taken up to widen the band's interior and the sibling distance. Neither adds a claim: `runs thin` is the band's condition stated as a state, not a trend, and carries no direction of travel; `the holding` is the arm, not a stock, a stores word or a possession.
