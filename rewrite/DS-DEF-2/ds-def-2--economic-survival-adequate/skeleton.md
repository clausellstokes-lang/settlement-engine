# SKELETON — DS-DEF-2 · `Economic Survival: ADEQUATE` (the marker's packet; Fable, 2026-09-11)

Seat: Fable 5.1 — marker for the REWRITE, working for the Fable chair. This file is the only thing this seat wrote. Everything below is judged against the printed licence card, the census row it was printed from, the reader function the card names and the producer behind it, the annex block's header lines, the register card (with S2 and S3), Part B §1 and §16–§24, MOVE-GRAMMAR §1–§3, §1.4.1 and §4.4.1–§4.4.3, CLERK-LAWS §2.4.1 and §2.6.1, ARCH v2 §2.5 and §8.3, brief ADDENDA 6–9, 12 and 13 parts A and B (the entailment law, the referent law, the writer bars W1–W27), the two ratified tables' rows that name this key table, and `rewrite/rulings-DEF2.txt`; where a shipped clause reads well but no field licenses it, it is UNLICENSED here and the law is named. This pool's sibling `Economic Survival: STRONG` was marked earlier today on the same card shape; where the two pools share a fence this packet says so, so the chair can read the four ECONOMIC pools as one table.

Status: IN PROGRESS — section 0 written; variants 1 to 3, sections 8 and 9 follow (the checkpoint law: each section lands on disk as it is finished).

## 0. THE CARD, THE KEY, THE ONE READ AND ITS LAYER (what the faces are wired to)

### 0.1 The licence card as printed (`node scripts/prose-licence-card.mjs DS-DEF-2 'Economic Survival: ADEQUATE'` in laneRW-DEF2, read-only)

```
LICENCE (block DS-DEF-2 · role spine · key `Economic Survival: ADEQUATE`)
  reads:      scoreBand(economicScore) (via ECONOMIC_ROW_POOL in defenseStateProse.js)
              (absent ⇒ no candidate; a modifier is silent, never "false")
  predicate:  scoreBand(economicScore) (via ECONOMIC_ROW_POOL in defenseStateProse.js) === ADEQUATE
  bag:        {band: RESERVED, route: proper, settlement: proper}
              FILLED at this block's call sites: {settlement}
  relation:   (a spine takes no relation)   ← a spine IS the seat and carries no relation; the relation is the MODIFIER's property, fixed at its freeze (ARCH §4.5)
  seat/form:  (not a seat-taker) / sentence      move: (none declared)     angle: ledger threshold unfolding
  attach:     (empty: a spine takes no attach set)
              n/a
  echo:       spine mounts 1 (tabs: defense) · modifier mounts 0 (none)
              the echo table is keyed on this pool's WHOLE table-rung reading (truncated at the file's first dot) and NOT on a producer-token root, so every pool that selects a row of `ECONOMIC_ROW_POOL` shares ONE echo key: a mount counted there may be a sibling ROW of the same table
  covert:     no
  source:     (none) · standing SOURCE-UNRESOLVED
              NO citation is licensed: a face naming a record holder here is refused by arm A13
  may claim:  that the reader `scoreBand(economicScore)` selects the row `ADEQUATE` of `ECONOMIC_ROW_POOL` in `defenseStateProse.js`, as a STANDING fact of the record
  may NOT:    a count, a cause, a season, a future, a standpoint, a second fact
  audience:   player (no mark)
  REFUSED COLUMNS, always: a totality over persons; an exemption from a duty (whoIsExempt is null everywhere); a named character and that character's fate (product scope); a theological claim about a deity (the deity doctrine)
```

The census row behind the card (`docs/content/wiring-census.json`, block DS-DEF-2, pool `Economic Survival: ADEQUATE`): `status RESOLVED` · `rung table` · `keyFunction ECONOMIC_ROW_POOL` · `readsGrain branch` · `reads` = the one synthetic label above (`fieldsRead` and `branchReads` the same) · `narrowed false` · **`objectClasses []`, `objectClass null`** (no civic class is spent by this pool; a face that NAMES one imports a body or a stock the read does not reach, W20 — and `reserves`, the shipped v1's noun, is a member of the census's `store` class, `wiringCensus.js:1309`: `['stores', 'reserve', 'reserves', 'stock', 'larder', 'harvest']`) · `sites ["defense.threatAssessment"]` · `k 2` · `rateBp 1250` on the 768-town RATE sample (96 towns; by tier: thorp 0 of 128 · hamlet 9 · village 46 · town 23 · city 11 · metropolis 7 — the band is reachable at every tier but the thorp in the sample; NO tier read is on the card, so no tier word is baked) · **`source {kind: "", kinds: [], fields: {economicScore: ""}, holder: null, holderReason: "no mapping row resolves any field this pool reads", standing: SOURCE-UNRESOLVED, twoSource: false}`** · `variants 3` · **`grammars 1`** (the shipped three share one grammar — a defect the rewrite cures by construction, MOVE-GRAMMAR §2.1: a pool of three carries three distinct level-1 grammars) · `slotsFilled ["settlement"]` · `slotsNamed ["settlement"]` · `slotsWithoutProvider []` · `covert false` · `absent {}` · `attach []`. No `dm-only` mark on any of the three rows. The `fieldSynonyms` table has NO entry for `scores.economic` (verified by grep at the dock: the only economic mention in `wiringCensus.js` is the docblock at `:1402` on `economicGates.military`); the pay words (`muster · pay · purse · roll · rolls · wage · wages`) are the synonyms of a DIFFERENT field, the pay gate, which this pool does not read.

### 0.2 The one read, unpacked to its producer (`defenseStateProse.js:529-543` the table and its reader; `defenseScoreBands.js:38-39` the band; `defenseGenerator.js:257-290` the producer, `:59` the axis definition; `bandLadders.js:200-205` the ladder's authored readings) — read for what the band MEANS, never as a licence of its own

The reader returns `ECONOMIC_ROW_POOL[scoreBand(economicScore)]`; `scoreBand` is the frozen four (`n >= 65 ? 'STRONG' : n >= 40 ? 'ADEQUATE' : n >= 20 ? 'WEAK' : 'CRITICAL'`). So the key is ONE KEYED CONDITION of ONE input: `defenseProfile.scores.economic` at or above forty and below sixty-five. An absent or non-finite score is silence, never CRITICAL. (The threat-assessment chrome uses a DIFFERENT threshold set for its colour and its string, `threatAssessment.js:160-173`: its string band is 40–64 as well, but its colour band is 35–59 — a wiring fact; the pool follows `scoreBand`.)

What the score is made of (the producer, for the marker's judgment of every "fund", "pay", "reserves", "months" and "emergencies" word; NOT a licence — entailment D-22 refuses "what the score is made of"): the PRIMARY driver is `foodSecurity.storageMonths` (0 months = 0, 1 = 10, 3 = 25, 6 = 45, 12 = 70, capped at 70; the generator's own comment: "Primary driver: food storage months"; the axis is defined at `:59` as "logistical resilience (food, medicine, trade access)"); then flat bonuses for `hasMarket` (+10), `hasHospital` (+10), a `port` route in a maritime world (+10), a `crossroads` route (+8), `econOutput × 0.2`, alchemy with a granary (+8); then the whole is MULTIPLIED by an economic-health gate `0.45 … 1.0` by `econOutput` ("a struggling economy cannot fund crisis logistics"); then stress penalties and the food-chain modifier. The band's authored READING (`bandLadders.js:202`, `Adequate`): **"At or above 40. Covered, but with little margin."** That reading is the ONE licensed content of this key beyond the band word, and it is richer than STRONG's: it carries an EDGE ("little margin") as authored text. The chair's ruling in `rulings-DEF2.txt` for this block: **"the readiness band word is exactly the ladder's reading and a STRONG badge on one arm says nothing of another; the economic-survival arm is FOOD-STORAGE-driven resilience, never prosperity."** Entailment L-52: the badge is "exactly the ladder's reading; frozen at four rungs; a badge on one arm says nothing about another"; L-53: "economic is driven primarily by FOOD STORAGE MONTHS and is not prosperity"; D-22: NOT "a fifth gradation; a trend; a cause; a comparison; what the score is made of"; G-19: NOT "a cause; a history; a count; a trajectory; self-sufficiency; survival".

| input | how the caller derives it | value at this key | REFERENT LAYER of the read (ADDENDUM 13 B A.1) | the lawful nouns | the unlawful nouns |
|---|---|---|---|---|---|
| `scores.economic` banded | `scoreBand(dp.scores.economic)` | `ADEQUATE` (the score at or above forty and below sixty-five) | **AGGREGATE** — "a band, score, label or ladder rung computed over bodies — it names NO body: the band's own word only" (the referent table §2.1 "per-arm badge" row lists `DS-DEF-2 Economic ×4` under AGGREGATE; the defense referent survey `:225`: "aggregate — no referent body"; W20). HOLDER: none (SOURCE-UNRESOLVED; no record word at all, W24) | the ARM by its label at its ENGINE meaning (W14): "economic survival" as the pressure's name; in plain words "the town's capacity to hold through a crisis", "the town's capacity to meet a crisis" (the generator's own noun for the axis is "crisis logistics", `:283`, and "the capacity to respond", `threatAssessment.js:173`; "emergency" is the chrome's word for the same class, `:167`; either "crisis" or "emergency" is the pressure CLASS, never a particular crisis); the BAND by its own word ("adequate") or by the ladder's authored reading ("covered, but with little margin" — "covered" and "little margin" are the two licensed halves, and the margin is a margin of COVER, never a sum, a stock or a span of time); the town as the possessor of the reading ("{settlement}'s …", "the town's …") | any BODY (a granary, the stores, the reserves, a market, a port, a hospital, a garrison, the muster, the soldiers, the watch, a treasury, a purse, the books, a hall) — W20, and D-F25 on the sibling STRONG pool; **`reserves` in particular** — a `store`-class stock word (A-13; `wiringCensus.js:1309`) on a pool whose `objectClass` is null, AND the score's primary component (food storage months) named by its stock; any COMPONENT of the score (food, months, grain, coin, revenue, tax, trade, a route, medicine) — D-22; PROSPERITY and COIN words ("fund", "money", "revenue", "wealth", "funds") — L-53, rulings-DEF2; the pay-gate words ("pay", "paid", "wages", "upkeep", "the keeping", "purse") — W16 and rule 5 license them on a pay-gate read ONLY, and this card reads no gate; a PARTICULAR crisis (a siege, a famine, a bad harvest, a season, a war, an occupation) — the card's "a season", W17; a DURATION or LENGTH ("short", "long", "a few months", "within months", "of the ordinary length", "every season") — the card's "a season" and "a count", R-DA-16, D-22 "a trend"; a TREND or TRAJECTORY ("begins eating", "moves closer", "each season of pressure") — D-22, G-19, and R-DST-B (a standing field licenses no history); a CAUSE — the card, D-22; a FUTURE or an outcome ("will strain", "would run out", "would not hold") — state never fate; a COMPARISON to another arm or town, or to a NORM ("ordinary", "enough") — D-22; a PERSON or persons, or a totality over them ("anybody", "nobody") — W22, the card's REFUSED COLUMN; a belief or knowledge frame ("hide that from", "find out", "know") — R-DA-13, W23; a FIGURE (a crisis that "eats", a "finite part" that "moves closer") — R-DA-11 |

**Where the key fires (for the writer's ear, not as a licence).** The village tier most (46 of 128 in the RATE sample), then the town; never a thorp in the sample; a city or metropolis reads ADEQUATE only where a stress or a poor economy pulls a granaried, marketed roster down. No tier read is on the card, so no tier word may be baked; no institution read is on the card, so no body may be named; no stress read is on the card, so no pressure may be said to be LIVE and none may be said to be absent (the arm measures resilience against a crisis; a siege may be on the same page through DS-STR, and its penalty is already inside this score).

**What the KEY'S NAME licenses: only its engine meaning.** "Economic Survival: ADEQUATE" is the row label; under W14 a face renders the ENGINE meaning — the economic-resilience axis, food-storage-driven, banded ADEQUATE, read as "covered, but with little margin" — never the dictionary sense of "economic" (coin, funding, revenue) and never "survival" as an outcome (no field records the town surviving anything). The engine's own chrome for this branch (`threatAssessment.js:169`: "Adequate economic resilience for a short-term crisis. A prolonged siege will begin straining reserves within months.") is PRODUCT CODE outside the REWRITE's corpus (ADDENDUM 13 B D, OW-20) and is not a licence for the same words in a face; it is where the shipped v1 got "short", "long", "reserves" and "within a few months", and where v3 got "the ordinary length" and "a few months". The DS-DEF-6 Economic Backing cell (`defenseDisplay.js:219-220`: "Adequate" / "Adequate upkeep, some shortfalls.") bands the SAME score on the same tab and is likewise chrome; "upkeep" and "shortfalls" there are the gate's words on a cell that is not a gate read either, and the desk's own docblock (`defenseStateProse.js:1473-1475`) files the pay half of this row as that lens's whole subject.

### 0.3 The block's header lines (annex `RECEIPT_POOLS_DOSSIER_STATE.md`, `### DS-DEF-2`, lines 2568-2596 of the laneRW-DEF2 copy; this pool's rows at 2685-2688)

- **STATE-KEY:** five fixed rows (`Beasts & Monsters` · `Invasion & War` · `Internal Security` · `Economic Survival` · `Disasters & Famine`), each with a `scoreBand` badge, read against `config.monsterThreat`, the institution presence flags, and `compound.inst`. This pool is the `Economic Survival` row's spine at its second rung; its badge IS its read (the only DS-DEF-2 row whose key is the badge itself), so the face's whole licence is the AGGREGATE and its authored reading, and nothing beneath it.
- **SLOTS:** `{settlement}` `{band}` `{route}` — the card: only `{settlement}` is FILLED at this block's call sites; `{band}` RESERVED (a face writes the band word itself, never the slot); `{route}` proper but unfilled here (and a route is a COMPONENT of this score, refused twice over). A face names neither `{band}` nor `{route}`.
- **SECTION-TARGET:** `defense`.
- **PROVENANCE + FENCE:** `buildThreatAssessment` is dossier-native and this shape EXTENDS it into a pool (each branch held ONE string); two defects must not be reintroduced (neither touches this row); **"institution presence is a STANDING fact with no recorded history; the causal clauses here are CAPABILITY clauses … and never HISTORICAL ones unless the history surface supplies the ancestry"** — on this row there is no institution read at all, so the capability clause the header licenses elsewhere has no bodies to join here; the band's reading ("covered, but with little margin") is the only capability statement on the card, and it is a STANDING one: no "begins", no "moves", no "has not been asked".
- **RECEIPT:** `threatAssessment.js:28-195` (the economic branch `:159-176`); rendered `DefenseTab.jsx:150-183`. **PDF PARITY:** parity.
- **Same page (C7; W2; A1/A11; the referent law's rule 3 across the tab):** DS-DEF-2 mounts the other four rows on the SAME tab — the Invasion row names the works and the force (a face here names NEITHER), the Disasters row names the granary and the stores (a face here says NOTHING of reserves or stores though the score is made of them — D-22; the `store` class is that row's, A-13), the Internal Security row the court and the gaol. DS-DEF-6's Economic Backing cell bands this same score as chrome beside the face (`:1473`); DS-DEF-11's pay-gate pools carry the wages and the upkeep (a face here says nothing of pay). An ADEQUATE badge on this arm says nothing of another arm (L-52; D-22's REFUSED column) — so no face says the town is safe, provisioned, defended or sound, or unsafe, in general (W2: no totality over the town).

### 0.4 The register card's six one-line registers (read whole; the dossier binds here)

The dossier: the record itself; the clerk's third person; the six shapes of its closed set; the town's name is not the default opener. The NPC ladder, the Herald, the chronicle, the DM page, chrome and the docent do not reach this pool. Amendment S2 (a computed CONSEQUENCE of the sentence's own fact may ride as one clause — here NO relation row exists on a spine and the producer's components are refused, so NO consequence clause is available; "little margin" is not a consequence, it is the reading itself) and S3 (every fact has a holder; the office cites one only for an S3 reason; here the holder is UNRESOLVED, so no citation at all — A13, W24) bind every face. The three questions before any sentence ships: which field licenses it (one: the band, with its authored reading); which claims does it carry and are they the same claims as before (one claim in two halves: the pressure covered, the margin little — in the angle's stance); what does it leave standing open (the margin: the record says it is little and says nothing of what lies past it — that is the one lawful open matter, stated as a standing edge, never as a forecast of its crossing).

### 0.5 The pool's shipped rows, verbatim (the annex, bold line `**\`Economic Survival\`: \`ADEQUATE\`**`; leaf vids 1-3)

1. `[ledger]` {settlement} can fund a short crisis. A long one begins eating reserves within a few months, and the reserves are not deep enough to hide that from anybody.
2. `[unfolding]` The town's capacity to pay for its own emergencies is real and finite, and every season of pressure moves the finite part closer.
3. `[threshold]` {settlement} can pay for a crisis of the ordinary length; the edge of what it can fund lies a few months past the beginning of one, and the town has not been asked to find out where.

Variant count: THREE shipped variants (ledger · unfolding · threshold), matching the card's `angle: ledger threshold unfolding`. Slot sets per parent (a face whose `{slot}` set differs from its parent's is refused, ARCH §2.5): vid 1 `{settlement}` · vid 2 `{}` (NO slot — every face of variant 2 carries no `{settlement}`) · vid 3 `{settlement}`. Sentence counts: vid 1 TWO sentences; vid 2 ONE sentence (an "and" joint); vid 3 ONE sentence (a semicolon joint and an ", and" joint — two joints in one sentence, R-DA-06's ration). A11's "where they differed in sentence count they keep differing" binds vid 1 against vids 2 and 3: vid 1 stays two sentences, vids 2 and 3 stay one each. Openers: vid 1 opens on `{settlement}` and vid 3 opens on `{settlement}` — TWO shipped breaches at once: T-F8 (a sentence face never opens on a `proper`-typed slot), MOVE-GRAMMAR wall 10 (the settlement token opens at most one variant per pool, never two adjacent — here two of three), and A11 (no two variants of one pool share their first two words: "{settlement} can" opens both v1 and v3). Vid 2 opens on "The town's". All three are cures, none a floor.

### 0.6 Findings of the two ratified tables that touch this key table (none names this pool's rows by line; the sibling rows' findings bind by shape)

- **REFERENT-TABLE §2.1 "per-arm badge" row:** `DS-DEF-2 Economic ×4` filed AGGREGATE — "the band's own word; never an institution".
- **REFERENT-TABLE §5.1 D-F25 (HOLDS, the sibling STRONG v1):** "the garrison can be kept paid while they last" — "a named BODY on an economic band read that consults no bucket". By the same shape, the shipped ADEQUATE v1's "reserves" is a named STOCK on the same read (A-13's `store` class), and is dropped, never kept as a floor.
- **REFERENT-TABLE §5.1 D-F15 (CONDITIONAL, the sibling WEAK v2):** "the people who would have to hold {settlement} … are already owed" — "unindividuated PERSONS with a debt relation on a band read"; the refuter let the DEBT stand as the producer's own reading of the WEAK band (DS-DEF-6's cell, `defenseDisplay.js:221`) and named the PERSONS as the fault. By the same shape, the shipped ADEQUATE v1's "not deep enough to hide that from anybody" carries persons AND a totality over them (the card's REFUSED COLUMN) AND a knowledge frame; nothing of it survives. This seat does NOT extend the CONDITIONAL to a pay word on this pool (the card reads no gate; W16; the desk's docblock files the pay half as DS-DEF-6's subject) — the same position the STRONG marker took, recorded there as open row 2 and here as open row 2, so the chair rules once for the four pools.
- **ENTAILMENT-TABLE D-22 (HOLDS):** the readiness band word entails "exactly the ladder's reading"; NOT "a fifth gradation; a trend; a cause; a comparison; what the score is made of"; REFUSED "a badge on one arm says anything about another". **L-52, L-53, G-19** as §0.2. **A-13:** "the stores" / "the granary" are two classes on purpose — `store` (`stores, reserve, reserves, stock, larder, harvest`) is the STOCK; the shipped v1's "reserves" belongs to it.
- **`rulings-DEF2.txt`:** "the readiness band word is exactly the ladder's reading and a STRONG badge on one arm says nothing of another; the economic-survival arm is FOOD-STORAGE-driven resilience, never prosperity."
- No row of the ENTAILMENT table's defense section 7 (the "more knowledgeable than the simulation" list) and no row of REFERENT §5.1 names annex lines 2685-2688; the pool is judged from the card, the producer and the laws. (The defense packets were grepped for the pool key and for its line numbers: no hit.)

### 0.7 Fences this seat records for the writer and the chair, not decided here (shared with the STRONG packet where marked ★, so one ruling serves four pools)

1. ★ **The arm's NOUN.** The band's "own word" is ADEQUATE; the ladder's reading is "covered, but with little margin". A face must name WHAT is covered, and the only licensed referent is the arm itself at its engine meaning. This seat licenses "the town's capacity to hold through a crisis", "the town's capacity to meet a crisis", "economic survival" (the label, as a name), and refuses any spelling that names a component or a body. Hazard noted for the drafter: "hold" grazed condition-word findings on DEF-11's wall faces (ADDENDUM 9 (d)); here no wall is on the read, and "hold through" is a capability idiom on the town, but "meet" is the spelling with no such neighbour.
2. ★ **"crisis" / "emergency".** The pressure CLASS the arm is measured against, in the engine's own vocabulary ("crisis logistics", "crisis response", "emergency measures"). This seat licenses the class word and refuses every particular (a siege, a famine, a bad season, a war, an occupation, a blockade) and every LENGTH on it ("short", "long", "of the ordinary length", "sustained"). If "crisis" is read as importing a live event, "the pressure" (the ladder's own word) is the safe spelling.
3. **"little margin" — the authored edge, and what it may NOT become.** The reading's second half is licensed verbatim and in the writer's own words at the same claim ("with little to spare" — the chair may strike it as a coin idiom; "and not much past it"; "the cover is thin at its edge"). It may NOT become a QUANTITY (months, coin, a stock's depth: "not deep enough", "a few months"), a COMPONENT ("the reserves"), a TREND ("moves closer", "begins eating"), a HISTORY ("has not been asked"), or an OUTCOME past the edge ("would run out", "would not hold"). "Margin" is a margin of COVER; it is not "reserves" by another word.
4. **The [threshold] angle's "what would tip it".** The annex minted `[threshold]` for "the band's own edge — the state is what it is and what would tip it, stated without predicting that it will"; MOVE-GRAMMAR wall 2 makes every edge subjunctive. On this key the edge is AUTHORED (little margin), so the threshold face has a licensed edge without inventing one. What may sit past the edge: this seat licenses the band's FLOOR stated as a floor in words ("the reading sits near the bottom of its band", "the cover has its floor close under it") and records as **CONDITIONAL, open row 3** whether the NEXT RUNG'S own authored reading ("thinly covered", the ladder's `Weak` row) may be named subjunctively as what lies past the edge ("past the margin the pressure would read thinly covered") — the ladder is a frozen public record and the annex's shipped `[threshold]` rows state edges this way, but D-22 refuses "a comparison" and "a fifth gradation", and a refuter could read the next rung's word as either. Until ruled, the safe threshold face states the edge as the margin's littleness and the floor's nearness, subjunctive where it reaches, and names no other rung.
5. ★ **The pay and coin words on a band that is partly economic health.** `econHealthMult` and `econOutput × 0.2` are inside the score; D-22 still refuses "what the score is made of", and "fund", "pay", "purse", "wages", "upkeep" are the ratified nouns of a DIFFERENT read (or coin words on a food-storage band, L-53). This seat refuses them here (open row 2).
6. **The [unfolding] angle on a resolved band (R-iv).** R-iv rules that the unfolding realises the OPEN move after the presence (PRESENT then OPEN; V6), never the ledger's measure; the OPEN move needs a matter standing open NOW in the record. On this key the matter standing open is the MARGIN: the record says the cover is real and the margin is little, and says nothing of what lies past it — a standing edge, declarative, not a trend and not a season. This seat licenses the unfolding as "the covering stands; the margin stands little, and it is the part the record leaves open", and refuses "every season", "moves closer", "each thing that goes wrong" (a trend is R-DST-W4-b's length-of-time claim wearing a label). If the chair reads OPEN as needing an `unresolved`-valued field and finds none here, the unfolding falls back to the presence in its running order (the cover stated, then its margin) with no OPEN move, and the angle is carried by the order alone — which R-iv says is NOT enough; recorded as open row 4.

---
