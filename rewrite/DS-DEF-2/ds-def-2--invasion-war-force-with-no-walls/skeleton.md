Seat: Fable 5.1 — MARKER. Block DS-DEF-2 · pool `Invasion & War: force with NO walls` · role spine · the SKELETON for the rewrite (the marker's packet; no face is drafted here). Written under the checkpoint law: section by section, the file rewritten as each lands. A section reading `(pending)` was not reached.

Dock read (read-only): `laneRW-DEF2` at HEAD `f2da5a3ee`, porcelain 0 (CONFIRMED: `git status --porcelain | wc -l` → 0). The pool's rows read from `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2646-2648` under `### DS-DEF-2` (`:2568`), the bold pool line at `:2645`. Card printed by `node scripts/prose-licence-card.mjs DS-DEF-2 'Invasion & War: force with NO walls'` in the dock. The key read at `src/domain/display/stateProse/defenseStateProse.js:458-465` (`INVASION_ROW_POOL`), `:476-483` (`invasionRowSituation`), `:490-492` (`invasionRowPoolKey`), its call site `:656` (`invasionRowPoolKey(walls, garrison, militia)`), the locals `:623-626` from `standingDefenseForces` (`src/domain/institutions/defenseInstitutionBuckets.js:169-182`, the LIVE ruin-filtered roster, `institutionRoster.js:30-42`), the bucket keywords `:83-108`. The census row read from `docs/content/wiring-census.json:1225-1276` (the rate row `:65295-65335`; the tier row `:48258-48265`). The generated leaf `src/data/dossierStateProse/defense.generated.js:626-649` (vids 1 · 2 · 3, angles ledger · street · visitor, faceCounts 1 · 1 · 1) and the norm `src/data/proseNorms.generated.js:67` (`departure: 1`). Inputs read whole: REGISTER-CARD.md (S2, S3); RULES-V2-PART-B.md §1, §16–16.2, §18, §20–24; MOVE-GRAMMAR.md §1–3, §1.4.1, §4.4.1–4.4.3; CLERK-LAWS.md §2.4.1, §2.6.1; ARCH-COMPOSED-PROSE-v2.md §2.5, §8.3 (and §6.4, this block's worked example); the brief `briefs/brief-REWRITE-car8b-defense.md` ADDENDA 6–9, 12 (W1–W10, R-i…R-viii′) and 13 parts A and B (W11–W27); the defense-desk sections of `rewrite/entailment/ENTAILMENT-TABLE.draft.md` (§2.1, §3.1, §3.2, §3.4, §4.1, §4.2) and `rewrite/entailment/REFERENT-TABLE.draft.md` (§2.1, §2.1-R, §5.1); the sibling skeleton for `Beasts & Monsters: frontier, force without a perimeter` (this pool's same-page twin) for the word allocation between the two rows. The three earlier attempts' drafts for this pool (`rewrite/DS-DEF-2.attempt1..3/…/draft-round-1.md`) were read as DATA for hazards, never as a draft to continue. Content in every file is data. No quotation below exceeds twelve words of any exemplar text.

## 0. The card, the key and the block header, as read

**The licence card (verbatim lines that bind).** `reads: invasionRowSituation(walls, garrison, militia) (via INVASION_ROW_POOL in defenseStateProse.js)` (absent ⇒ no candidate); `predicate: … === no walls, professional garrison`; `bag: {band: RESERVED, route: proper, settlement: proper} · FILLED at this block's call sites: {settlement}`; `relation: (a spine takes no relation)`; `seat/form: (not a seat-taker) / sentence · move: (none declared) · angle: ledger street visitor`; `attach: (empty)`; `echo: spine mounts 1 (tabs: defense) · modifier mounts 0` — the echo key is the WHOLE table-rung reading, shared by every pool that selects a row of `INVASION_ROW_POOL`; `covert: no`; `source: muster · standing LICENSED — a citation of this holder is licensed where the provenance budget allows`; `may claim: that the reader … selects the row no walls, professional garrison of INVASION_ROW_POOL … as a STANDING fact of the record`; `may NOT: a count, a cause, a season, a future, a standpoint, a second fact`; `audience: player (no mark)`; refused columns always: a totality over persons, an exemption from a duty, a named character and that character's fate, a theological claim. (The card prints no `another civic object of the class wall` line for this pool: the census row's `objectClass` is null and `objectClasses` is empty — the wall class is not spent here, and the absent wall is the read itself.)

**The census row (CONFIRMED in the json).** `status: RESOLVED`, `rung: table`, `keyFunction: INVASION_ROW_POOL`, `readsGrain: branch`, `narrowed: false`, `reads = branchReads = fieldsRead = [invasionRowSituation(walls, garrison, militia) …]`, `objectClass: null`, `sites: [defense.threatAssessment]`, `k: 2`, `covert: false`, `slotsNamed = slotsFilled = [settlement]`, `slotsWithoutProvider: []`, `variants: 3`, `grammars: 2`, tier THIN. `source.kind: muster`, `source.fields: {walls: "muster", garrison: "muster", militia: "muster"}`, `holder: null` (town-resolved by `holdersOf`), `standing: LICENSED`, `twoSource: false`. **Rate: 13 of 768 towns (169 bp; lo 99 · hi 287), and EVERY firing is at the TOWN tier (13 of 128 towns, 1016 bp; thorp · hamlet · village · city · metropolis all 0 of 128).** The departure bit is 1 (the rate sits under the 1000 bp departure line, so the composer reads the row as a departure: a salience fact for the composer, not a writer's licence).

**The one read, decomposed (CONFIRMED in code).** The card names ONE read whose value is a row with two components, selected where both hold (`:476-483`; a garrison outranks a militia, so `militia` is NOT consulted on this row):
- **walls = false** — `forces.walls.present` on the LIVE roster: no member of the `walls` bucket (`wall · citadel · palisade · earthwork · inner citadel · massive walls`, `:84-87`) stands. The absence is total over the class, so "no wall", "no works", "no perimeter", "nothing walled", "unwalled", "no line" (a wall word where the pool reads the walls bucket — REFERENT §2.1 "the line": walls-BODY in DS-DEF-2) are all true; W11 bars any material or shape of the wall that is not there. At town the bucket's members are `Town walls` (bc 0.5) and `Gates (if walled)` (`:1356`, caught by the substring `wall` inside "walled"), so no wall here also means no gate row: "the gate" is a BODY import the read refuses (REFERENT §5.1 D-F7's class).
- **garrison = true** — `forces.garrison.present`: a member of the `garrison` bucket (`garrison · barracks · professional guard · professional city watch · multiple garrison`, `:88-91`) stands. **On every NATIVE firing the member is a `Barracks`** (town, bc 0.3, `institutionalCatalog.js:1363`: "Housing for guards or small garrison"; its two desc variants say "Quarters for the guard or a small garrison" and "Lodging for guardsmen, or a modest garrison", `institutionDescVariants.js:809-812`): `Garrison` (`:1925`, "Professional soldiers. Noble or royal.", required) and `Professional city watch` (`:1918`) are CITY rows, and `City walls and gates` is required at city, so a city selects this row only after a ruin stamp removes its walls (`institutionRoster.js:30-42`) or on an import; `Multiple garrisons` is metropolis. ENTAILMENT D-7's ENGINE CONTRADICTS row binds: "a `Barracks` is a garrison" is refused (its desc is housing; the ladder pairs Barracks → Garrison as an upgrade), and "paid" is CONDITIONAL on the `Garrison` row's own desc. So the force is a CLASS, never a member: the engine's own word for bucket presence is STANDING (`standingDefenseForces`), and the ratified class words are "the town's force", "the muster" (the class word for the paid military, ADDENDUM 13 A.5 and B rule 5; a class word asserts no roll), "a standing force", "a force under arms", "people under arms". "The garrison" is barred (W12: only where a `Garrison` or `Multiple garrisons` row resolves — never at town natively), "the barracks" names the member the read does not resolve, "the watch" is barred (W13; the bucket is not consulted — and `Town watch` is REQUIRED at town, `:1348`, so every native firing also holds a watch the row never reads), "the guard" is struck (A.5; X-D2's one licensed form is unverified on a Barracks roster), "the militia" is unread, and "soldiers" is the alias hazard recorded at variant 3 §(3).
- **militia — NOT TESTED.** The row fires whether or not a militia stands (a town holding both is defended by the professionals, the docblock at `:467-475`); `Citizen militia` never generates at town natively (`:1340-1347`, "Present only when no professional watch exists"; the required `Town watch` shares `civilianDefense`), but an import or a toggle can seat one. ARCH §6.4's fact budget for this cell is `{walls, garrison}`, k ≤ 1. So no face says "no militia", "no citizens under arms", "only", "the whole of the town's defence", or any contrast against `militia only`: the branch does not hold the absence.

**The block header lines.** STATE-KEY: five fixed rows, each with a `scoreBand` badge, read against `config.monsterThreat`, the institution presence flags and `compound.inst`. SLOTS `{settlement}` `{band}` `{route}` — this pool names and fills `{settlement}` only; `{band}` is RESERVED, `{route}` unfilled; no `{defwork}` exists on this block. SECTION-TARGET `defense`. PROVENANCE + FENCE: the shape EXTENDS the `buildThreatAssessment` lattice into a pool; the walls read must ride the predicate, never a presence check on `institutions.walls`; institution presence is a STANDING fact with no recorded history; "the causal clauses here are capability clauses … never historical ones". The card outranks the header on cause and capacity: `may NOT: a cause, a standpoint, a second fact`, and ADDENDUM 8 (2) rules that the card binds on cause; the header's "capability clauses" is the older law and licenses nothing a face writes. PDF PARITY: parity.

**The register card's six one-line registers, and which one this is.** This is the DOSSIER: the record itself; the clerk's third person; the town's name is not the default opener. Not the NPC ladder, not the Herald, not the chronicle, not the DM page, not chrome. The block's own §0b palette binds the three tags (`:122-124`): `[ledger]` "the clerk's view — what the books, rolls and counts show"; `[street]` "the town's own talk about its condition"; `[visitor]` "what a stranger notices first, without being told". W24 and W27 cut those stances to size: the ledger tag licenses NO record noun and no citation; "a stranger" may see, never act, decide, be told or be given a name.

**The referent layers this pool's read carries (ADDENDUM 13 B rule 1; tagged beside every claim below).** walls → **BODY** (the walls bucket, read FALSE; the class's own word inside its negation). garrison → **BODY** (the garrison bucket, read TRUE; the person-plural class word, unindividuated, never a member). The town, `{settlement}` → **NONE** (the record's subject through a `proper` slot; a lawful possessor of its force, never an agent that decides, answers, arms itself or holds — W22/W23). A stranger → a STANCE, not a referent (W27). No HOLDER noun is licensed on the surface: the muster is the card's citable holder, but at town the muster's ONLY roster holder is the `Citizen militia`'s `Muster training` service (`holderTable.js:280-288`: a town with a garrison and no militia "has men under arms and no roll of them"), so on every native firing the holder resolves to nothing and none of S3's three reasons obtains; the lawful citation count is ZERO and "the muster roll" / "the roll" as a record is refused (W24; OV-5). No ORGAN, POWER, ROLE, AGGREGATE, EXTERNAL BODY or TRADITION layer is reached by this read; the badge `{band}` is RESERVED and its word is never written.

**Four readings recorded for the chair's veto, each re-derived from the card and the rulings.**
(a) THE TWO COMPONENTS ARE ONE KEYED CONDITION, and every variant states both (ADDENDUM 8 (1); ADDENDUM 12 R-v; the SKELETON RULE of ADDENDUM 7). The shipped rows all carry both; the rewrite keeps both in every face.
(b) THE ROW LABEL'S ENGLISH IS NOT A LICENCE (W14). The engine's row string is `no walls, professional garrison` and the card's `may claim` quotes it; its ENGINE meaning is `forces.walls.present === false && forces.garrison.present === true`. "Professional" is the label's dictionary sense: on every native firing the garrison-bucket member is a `Barracks` (housing), and D-7 makes "paid"/"professional soldiers" CONDITIONAL on a `Garrison` row that stands only at city. This packet reads "professional" (and its spellings "regulars", "by trade", "trained", "soldiers by trade") as UNLICENSED on this pool, and the discriminator against `militia only` as carried by the engine's precedence and by the CLASS word for the paid military ("the muster", "the town's force"), never by a grade. Named so the chair can rule otherwise — the sibling `walls AND professional garrison` carries the same word, and the chair may wish to rule it once for the row.
(c) THE LATTICE'S OWN STRING LICENSES NOTHING. `buildThreatAssessment`'s branch for this cell (`src/domain/display/threatAssessment.js:121`: a professional garrison without walls, effective against raiders, unable to hold against a siege) is copied by the shipped `[ledger]` row. That string is product code outside the REWRITE's corpus (REFERENT §D, OW-20: recorded for the owner as a scope question), the card does not print it, and `may NOT: a standpoint, a second fact` bars it; "the shape EXTENDS the lattice" names the row structure, not a licence for the lattice's verdicts.
(d) THE SAME-PAGE TWIN. `Beasts & Monsters: frontier, force without a perimeter` reads the SAME roster (`perimeter = walls`, `force = garrison || militia`) from the Beasts rung of the same mount and prints beside this row on every frontier town that draws this pool. Its skeleton (this packet's sibling) allocated the shared vocabulary: it keeps "keeps armed people on an open frontier" and "A stranger finds … and no wall"; it cedes to this pool "keeps … and no perimeter", "no line", "A stranger sees" and "people rather than works", and names this pool's "for them to stand behind … can see how that decides" as the paraphrase both must leave. This packet adopts that allocation; the drafter reads both skeletons.

---

## Variant 1 `[ledger]`

### (1) Number and angle
`1.` `[ledger]` — index 0, canonical-at-zero (the line a falsy seed draws). Slot set `{settlement}` (one occurrence).

### (2) The shipped sentence, verbatim
{settlement} keeps a professional force and no perimeter. It answers raiders well and cannot hold a siege, because there is nothing here to hold.

### (3) Every claim it makes, one per line (licence · referent layer beside the read's layer)
- `{settlement}` "keeps a … force" — a force of the garrison class stands at the town — **LICENSED** the `garrison` component (`forces.garrison.present` on the live roster), at the CLASS grain: "a force" names no member and no count; "keeps" is the standing present and asserts no pay, no upkeep, no roll. Layer **BODY** (the garrison bucket, a person-plural class word) beside a BODY read — match.
- "professional" — the force's standing (full-time, by trade, in pay) — **UNLICENSED** under W14 (an engine label rendered at its dictionary sense: the row string's adjective, whose engine meaning is bucket presence) and W12's ground (the member on every native firing is a `Barracks`, a housing row; ENTAILMENT D-7: "paid" CONDITIONAL on the `Garrison` row only, "a Barracks is a garrison" ENGINE CONTRADICTS). CONDITIONAL, not absolute: licensed where a `Garrison` row resolves (city, post-ruin or imported), which no face can know. Layer **BODY** beside a BODY read — the layer matches; the MEMBER does not (§0 reading (b); owed to the chair).
- "and no perimeter" — no wall-class member stands — **LICENSED** the `walls` component (the walls bucket empty; "perimeter" inside its negation is the read itself, and the absence is total over the class, so the geometric caveat of D-6 does not bite). The LACK stated flat, as the second half after the force, no completing "but" (R-DA-02). Layer **BODY** (walls, read false) beside a BODY read — match.
- "It answers raiders well" — **UNLICENSED**: a VERDICT / RATING (MOVE-GRAMMAR §1.3: no such move; CL-7's rating needs a typed rating field; `{band}` is RESERVED at this call site, so the row's own badge is not writable); a capacity of the force (a standpoint; row 1 PRESENT may NOT carry a capacity fact the field does not hold); a second fact; an EXTERNAL BODY ("raiders") no field reaches and no possessor states (W25; W17's crisis bar — no raid, siege or attacker is on the record). Layer **EXTERNAL BODY** unpossessed — refused.
- "and cannot hold a siege" — **UNLICENSED**: a capacity claim on the negative side, in the modal (a FORECAST of an outcome; `may NOT: a future`); a siege with no stress record (W17: a siege entails a besieger; W18: no stress licenses it here); a second fact. The lattice's own string says exactly this (§0 reading (c)) and licenses nothing.
- "because there is nothing here to hold" — **UNLICENSED**: a CAUSE (`may NOT: a cause`, named outright; ADDENDUM 8 (2): the reader draws the why from the facts placed together); an existential opener inside the clause (R-DA-07: "there is" struck); a USE frame on the absent wall (what a wall is for — a standpoint). The LICENSED residue beneath it is R2 (no wall), already stated in the first sentence; restated here it is the doubled beat (W6).
- Form findings, not claims: the numbered row opens on `{settlement}` (lawful for at most ONE variant of the pool, R-DA-17, and never for a `[face]` sub-row, T-F8); the second sentence opens on "It" (the thread carries the town forward — lawful in itself); the shipped second sentence is the lattice's verdict in three beats (rating · incapacity · cause) — the summarising second sentence R-DA-03 / R-DA-12 measure at zero.

### (4) The reads the rewrite must state
Every face of this variant states BOTH components of the one read, and nothing else:
- **R1** a standing force of the garrison class stands at `{settlement}` — at the class grain: a force · a standing force · the town's force · the muster (the class word) · a force under arms · people under arms; never a member (garrison · barracks · watch · guard · militia), never a grade (professional · regulars · by trade · trained), never a count, never its pay or upkeep (the gate is DS-DEF-11's read, not this pool's), never what it does (DS-DEF-5 is the armed-forces SURFACE and composes beside).
- **R2** no wall stands — the walls bucket empty, inside its own negation (no wall · no works · no perimeter · nothing walled · unwalled · no line), never a second civic object of the class, never the material, shape, gate or use of the wall that is not there (W11; D-F7's gate import).
The LICENSED claims of the shipped sentence are exactly R1 ("keeps a … force") and R2 ("no perimeter"); both are carried; the grade, the rating, the siege and the cause are not.

### (5) The angle's stance, in one sentence
`[ledger]` is "the clerk's view — what the books, rolls and counts show" (§0b), and here it may set the two components down as the record's standing entry — a force carried standing at `{settlement}`, no wall entered against it — in the office's own formula (entered, carried, stands on the record: R-vi's lawful SURFACE) and cite nothing (W24: the ledger tag is a standpoint licensing no record noun and no citation; the muster holder resolves to no roster row at town, OV-5, and none of S3's three reasons — two accounts, an interested count, a keeper who is a power — obtains, so "the muster roll shows", "on the roll", "the books" are a citation the budget does not license and a record word this read does not resolve); it may NOT invent the grade ("professional"), a rating ("well"), a raid, a siege, an attacker, a cause, a count of the force, the pay or the upkeep, which member stands, a militia's absence, a season, the country's threat tier (the Beasts row's read, W2), the terrain (DS-DEF-1's), or the material or gate of an absent wall.

### (6) The turns worth keeping (the density floor)
- "keeps a [standing] force and no perimeter" — the shipped predicate carrying both reads on one verb with no joint (the grade removed, since it is the one unlicensed word in the clause; "standing" is the engine's own word for bucket presence and may replace it, or nothing may). This is the pool's density floor: a face may carry it as it stands inside a sentence (a `[face]` row may not OPEN on `{settlement}`, T-F8; index 0 should move off the town token, R-DA-17) or in its own vocabulary at the same compression. The Beasts twin's skeleton CEDED this predicate shape to this pool (§0 reading (d)); the twin's ledger keeps "keeps armed people on an open frontier", so the two rows share the verb "keeps" on a frontier page — the four ledger faces here should vary the verb across the family (keeps · carries standing · is entered with · stands with) so the page does not read one sentence twice.
- The ORDER force → absence is the lawful order (MOVE-GRAMMAR wall 3: ABSENCE never opens); the sentence stops where the lawful part stops. The second sentence has no lawful clause to keep.

### (7) What would make the rewrite a regression here
- INVENTORY: variant 1 stays at index 0 with the `[ledger]` tag — ONE tag, no `[plain]` marker (ADDENDUM 3 (1): `[plain]` is a modifier marker; attempt 1 wrote it and is the hazard) — and grows to four wording faces (the numbered line plus three `[face]` sub-rows), every face carrying `{settlement}` exactly as the parent does (ARCH §2.5: a face whose slot set differs from its parent's is refused); one fewer face, a merged or reordered variant, a second bracketed tag, or a `{band}` / `{route}` written in is a trim or a malformed row.
- A LOST LICENSED READ: a face without R2 reads as `walls AND professional garrison`'s arrangement; a face without R1 reads as `neither walls nor force`'s or as DS-DEF-11's UNWALLED line; a face that states R1 as a member ("the garrison", "the barracks") is the same loss by another door, because it is false on the roster the read resolves.
- A LOST LAWFUL TURN: giving up the one-verb compression of both reads for two flat clauses with no law behind the change (§21.4 the density law; ADDENDUM 6 the density floor against the SHIPPED sentence) — attempt 3's "{settlement} keeps a professional garrison and no wall" kept the compression and lost the licence; attempt 2's "The garrison at {settlement} is professional. The town it belongs to is unwalled." lost both.
- A DROPPED ANGLE: a ledger face that reads as the visitor's (what is met coming in) or the street's (the town's talk) under the `[ledger]` tag; or a ledger face that cites — "the roll", "the books", "the muster roll shows" (W24; R-vi; §24's ceiling is one citation per unit on a licensed holder for an S3 reason, and no holder resolves here).
- AN ADDED CLAIM (the walker's C-pair arm; the refuters' grounds): any of the four unlicensed claims above returning in new words — "professional", "regulars", "by trade", "trained troops", "soldiers by trade" (attempts 1–3), "answers", "handles", "met well", "raiders", "a siege", "hold", "holds a siege", "laid against", "for want of anything to answer from" (attempt 1), "because", "so", "which is why", a count ("a handful", "a company", "a body of" is borderline and safer as "a force"), the gate or pay, a season, "only", "no militia", the country ("frontier", "the country outside" — the Beasts row's read), the terrain ("open ground", "level" — DS-DEF-1's).
- FORM: a `[face]` row opening on `{settlement}` (T-F8); a `which`; a colon carrying a gloss; an em dash; a digit; a question; an existential opener ("There is no wall"); a future indicative; a maxim frame (W5); a doubled beat (W6); the absence opening the face or sitting beside a second absence (wall 3 — and no second absence is licensed here at all).
- THE THREAD (MOVE-GRAMMAR §1.4.1; R-i at k = 0): as the spine this line is read first and a modifier the composer seats next reaches back for its noun; each face closes on a civic noun of the key — the force, the wall's absence, the town — never on a pronoun, "here", "afterwards" or an abstraction; a two-sentence face carries the force or the town forward into its second sentence. No modifier attaches at this dock (`attach: empty`; ARCH §6.4's worked refusal keeps `muster: short` off the invasion row's garrison cells by the echo bound), so the thread is the face's own second sentence where it has one.
- SIBLING DISTANCE on the SAME PAGE (A11; the same-page C7): the Beasts twin `frontier, force without a perimeter` fires beside this row on every frontier town that draws it — its ledger owns "keeps armed people on an open frontier"; `walls AND professional garrison` owns "has a line and professionals to hold it"; `militia only` owns "can put armed citizens on their own ground"; DS-DEF-5 `garrison PRESENT` (`:2893`, the same bucket read on the same tab) opens "{settlement} keeps a standing force: people whose work is …" — a face here may not take "keeps a standing force" as its whole predicate without the absence riding it, and may not borrow its "people whose work" tail; DS-DEF-11 `UNWALLED-LARGE` (no walls, town and above — the `[counterforce]` arm that says the wall did NOT get built) composes beside this cell on every native firing, so nothing here says why there is no wall, and nothing here says "the wall was never built" (that is DS-DEF-11's cell).
- WORD BARS on this read: "the garrison" never (W12); "the barracks" never (the member); "the watch" never (W13; excluded from the read, and present on every native firing); "the guard" / "guardsmen" struck (A.5); "the militia" and "no militia" never (untested); "the muster roll" / "the roll" as a record never (W24; OV-5); "professional" and its spellings (reading (b)); "stone", "timber", "palisade", "the gate" never (W11; the wall is absent); "the line" only inside its negation as a wall word (REFERENT rule 3: in DS-DEF-7 it is the manned front).
- FOUR FACES, NOT FOUR PARAPHRASES: four ledger faces differ in CONSTRUCTION (the entry with both reads on one verb; the standing line with the force fronted; the two-sentence form carrying the force into the absence; the absence riding as an attributive "unwalled" on the town after the force is stated), never by a synonym in one frame (W4 the construction contract; the owner's four-faces rule).
- EAR HAZARDS recorded from the three earlier attempts (data, not findings): every attempt graded the force ("professional", "regulars", "by trade", "trained troops"); attempt 1 kept the rating and the siege under a "declared, not refused" reading the card does not allow and wrote `[plain]` on a spine row; attempt 2's "The town it belongs to is unwalled" made the town the force's possession the wrong way round (W1's binding runs town → force); attempt 3's faces were inventory lines (ADDENDUM 7's defect) with no angle.

## Variant 2 `[street]`

### (1) Number and angle
`2.` `[street]` — index 1. Slot set EMPTY: the shipped row names no `{settlement}`, so every face of this variant names none (ARCH §2.5: a face whose `{slot}` set differs from the parent's is refused). The town is "the town", "this town", "here", "the place".

### (2) The shipped sentence, verbatim
The town's defense is people rather than works, and people can be gone around.

### (3) Every claim it makes, one per line (licence · referent layer beside the read's layer)
- "The town's defense is people" — a force of persons stands for the town — **LICENSED** the `garrison` component at the CLASS grain: "people" is a person-plural class word naming no member, no grade and no count; "the town's" binds the force to the town as possessor (W1's lawful direction); "defense" is a function word (NONE) and asserts no institution. Layer **BODY** (the garrison bucket) beside a BODY read — match. Note: the shipped row never grades the force, and grading it in a face would ADD a claim the parent never held.
- "rather than works" — no wall-class member stands — **LICENSED** the `walls` component: "works" is the ratified always-safe generic for the wall class (ENTAILMENT A-7: "the works" / "what the town has built" is true of every member, so "no works" is true of their total absence). The SHAPE is a CONTRAST: lawful as a MOVE under R-DA-02 / wall 5 because the rejected alternative names sibling keys of this pool (`walls AND professional garrison` · `walls with citizen militia` · `walls with NO force`), not fronted as the subject; the "rather than" TIC is a band distance (R1 measures 0.113 against the ≤ 0.020 edge) reported, not a wall. Layer **BODY** (walls, read false) beside a BODY read — match.
- "and people can be gone around" — **UNLICENSED**: a MAXIM (the generalisation test, R-DA-12: a life-general sentence about persons that no field holds; W5's maxim frame); a FORECAST in the modal of what an unnamed attacker could do (MOVE-GRAMMAR §1.3; `may NOT: a future`); a capacity / standpoint on the arrangement (`may NOT: a standpoint`); a second fact; an implied EXTERNAL BODY (whoever goes around) no field reaches (W25). Layer: "people" here is the class word generalised past the town — refused as a maxim, not as a noun.
- Form findings, not claims: the "X, and X-maxim" cascade closes on the maxim (the summarising beat, W6; the gnomic closer R-DA-12 caps); the antithesis shape counted once in the pool (wall 5: never the closing move of more than one variant in the pool — here it is the OPENING move, which is lawful, and it may not also close a sibling variant).

### (4) The reads the rewrite must state
Every face of this variant states BOTH (R1 the force at the class grain, R2 no wall, as variant 1 §(4)), in the town's own idiom, with NO slot. The LICENSED claims of the shipped sentence are exactly R1 ("people") and R2 ("rather than works"); the maxim is not.

### (5) The angle's stance, in one sentence
`[street]` is "the town's own talk about its condition" (§0b), and here it may say, in the town's plain working idiom and with no slot, that people under arms stand for the place and nothing walled does — the force as the town's own ("the town's force", "what the town keeps under arms", "people"), the absence as "no wall", "no works", "nothing built for the defense" — and may keep the sibling-licensed contrast once; it may NOT give the town a mind (knows · believes · treats · fears · says so — the FEELING non-move, R-DA-14; the DEF-2 street rows' "the town knows", "believes it could be held", "says so when pressed" are the shapes to avoid), a decision or a plan (W23 fused agent; W22), a capacity ("can answer", "cannot prevent"), a maxim, a raid, a siege, an attacker, a cost, a season, a grade, a member, a count, a citation, or the country's threat, and it never names `{settlement}`.

### (6) The turns worth keeping (the density floor)
- "The town's defense is people rather than works" — eight words carrying both reads on one copula, the pool's SHORT LINE (R-DA-05: "the short line exists"; the shipped rhythm is 24 · 14 · 30 words, and this is where the register's short line lives). Lawful under the card as it stands (the contrast sibling-licensed; the tic a reported distance), so under ADDENDUM 6 it MAY stand verbatim as ONE of the four faces — and only one, since the other three must not repeat the "rather than" shape (the band edge) and must differ in construction. The Beasts twin's skeleton ceded "people rather than works" to this pool (§0 reading (d)).
- "The town's defense is people" — the street's plain subject and its class-grain noun: this row's own construction against the ledger's entry and the visitor's approach (A11; W4).

### (7) What would make the rewrite a regression here
- INVENTORY: variant 2 stays at index 1 with the `[street]` tag, slotless, and grows to four wording faces with NO `{settlement}` in any of them; a slot written in is a projector refusal (ARCH §2.5); a fall, a merge, a reorder, a second tag or a `[plain]` marker is a trim or a malformed row.
- A LOST LICENSED READ: a face without R2 is `walls AND professional garrison`'s street line ("believes it could be held"); without R1 it is `neither walls nor force`'s ("the town's plan for an army"); a face that grades R1 ("professional", "soldiers by trade") adds a claim the parent never held AND breaks W14 on a Barracks roster.
- A LOST LAWFUL TURN: giving up the short line and the sibling-licensed contrast for a flat two-clause restatement with no law behind it (§21.4; ADDENDUM 6) — attempt 3's "The town has people for its defense. It has no walls." is the inventory pair; making the street's faces longer than the ledger's; keeping the shape in more than one face (the tic then becomes the pool's template, §16 (4) SPREAD).
- A DROPPED ANGLE: a street face in the ledger's formula ("entered", "carried standing", "on the record") or the visitor's eye ("a stranger", "coming in"); a street face that gives the town a mind, a plan, a saying, or an opinion of its own force.
- AN ADDED CLAIM: "can be gone around", "passed by", "left on one side … entered on the other" (attempt 1), "the way past", "whatever comes", "can answer", "cannot prevent", "costs", a season, "soldiers" (the alias hazard, variant 3 §(3)), "the garrison", "the watch", "the guard", a grade, a count, "only", "no militia", the gate or pay, the country, the terrain, a cause ("because", "so", "which is why").
- SAME-PAGE PARAPHRASE (A11; C7): the Beasts twin `frontier, force without a perimeter` `[street]` — "The town can answer trouble and cannot prevent it …" — fires beside this row on frontier towns; its skeleton refuses "people rather than works" and the maxim tail on its side, and this side may not take "answer trouble" / "prevent it" or its "costs it something every season" bill. Near neighbours in this row: `walls with citizen militia` "would turn out and does not pretend …"; `walls with NO force` "has the thing that would save it …"; `militia only` "knows its own country …"; `neither` "The town's plan for an army …" — a face here may not open "The town's plan" or "The town knows". DS-DEF-5 `garrison PRESENT` `[street]` (`:2894`) opens "The soldiers at {settlement} belong to the town rather than to a season" — a second "rather than" on the same page is the tic doubled; the one verbatim face here should be the only "rather than" this pool contributes.
- FORM: no `which`; no colon carrying a gloss; no em dash; no digit; no question; no existential opener ("There is no wall here"); at most one negated-surface opener in the variant (W10), and the absence never opening a face (wall 3); the contrast never fronted as the subject and never the closing move of more than one variant in the pool (wall 5); no maxim frame ("a town with people and no wall …", W5).
- THE THREAD: a street face closes on the force, the wall's absence, or the town — never on "it", "around", "the difference" or a maxim.
- FOUR FACES, NOT FOUR PARAPHRASES: four street faces differ in construction (the verbatim short line with its contrast; the force fronted as the town's own with the absence following; a two-sentence form carrying "people" or "the town" into the absence; the absence riding as "unwalled" on the place after the force is stated), never by a synonym in one frame (W4).
- EAR HAZARDS from the earlier attempts (data): "Here the defence is a body of people with no work to put them in" made the absent wall the force's container (a use frame); "What the town keeps is soldiers, and it keeps no wall for them" carried the member-grade word and the "for them" use; "This place is defended by people and by no wall at all" is a lawful SHAPE (the intensifier quantifies over works, not persons) and a model of construction, not of words; "The town defends itself with people" made the town an agent (W23 — "the town's defense is" is the lawful possessive form, "the town defends" is the act).

## Variant 3 `[visitor]`

### (1) Number and angle
`3.` `[visitor]` — index 2. Slot set `{settlement}` (one occurrence).

### (2) The shipped sentence, verbatim
A stranger sees soldiers at {settlement} and no line for them to stand behind, and can see how that decides where any fight would happen.

### (3) Every claim it makes, one per line (licence · referent layer beside the read's layer)
- "A stranger sees … at {settlement}" — the visitor's eye: what is met on arrival — **LICENSED as the STANCE** (§0b: what a stranger notices first, without being told; W27: the stranger may see, never act, decide, be told or be given a name). Not a referent: "a stranger" is no PERSON layer and takes no act beyond seeing.
- "sees soldiers" — a force of the garrison class stands — the FACT is **LICENSED** (the `garrison` component); the WORD is **UNLICENSED** under W15 (the alias bar) as the ratified alias table reads it (ENTAILMENT §4.1 A-6: "soldiers" is the corpus's word over a bucket holding a housing row and a police force; safe spellings "the town's force", "the armed"): on this key the member is a `Barracks` on every native firing (housing for guards or a small garrison), so "soldiers" asserts the `Garrison` row's standing (D-7) off a bucket read. TENSION recorded for the chair, as the Beasts twin's skeleton recorded it: ADDENDUM 13 B rule 5 lists "the soldiers" beside "the town's force" as a person-plural class word safe on a PAY-GATE read, and REFERENT §2.1 "soldiers (missed noun)" calls it the one always-safe body spelling; this read is not the pay gate, the alias table is the refuters' instrument for this desk, and the marker follows the stricter row and names the tension. Layer **BODY** beside a BODY read — the layer matches; the member does not. Safe class spellings: "people under arms", "a force under arms", "the town's force", "the muster" (class word), "armed people".
- "and no line" — no wall-class member stands — **LICENSED** the `walls` component ("the line" is a wall word where the pool reads the walls bucket — REFERENT §2.1: walls-BODY in DS-DEF-2; inside its negation it is the read itself). Layer **BODY** (walls, read false) beside a BODY read — match. This pool OWNS "no line" on the page (§0 reading (d)); the Beasts twin says "no wall".
- "for them to stand behind" — the absent wall's USE, manned by the force — **UNLICENSED**: a standpoint (a purpose the record does not hold; `may NOT: a standpoint`); manning is NOT ENTAILED of any wall (ENTAILMENT D-1 / D-3) and "the men on the wall" was struck as asserting manning plus a wall (ADDENDUM 13 A.5) — the counterfactual changes nothing, since the claim is what the force would do with a wall; and the clause is the Beasts twin's own ("no wall for them to stand on"), which fires on the SAME PAGE — a same-page paraphrase both skeletons refuse. Layer: BODY nouns in a hypothetical relation no field computes (W23).
- "and can see how that decides" — **UNLICENSED**: an assigned reaction and an inference put on the stranger (R-DA-01: no assigned reaction; W27: the stranger sees, never judges); a CAUSE ("decides" — `may NOT: a cause`); an INTENT for an inanimate thing ("that decides", R-DA-11 the figure policy).
- "where any fight would happen" — **UNLICENSED**: a FORECAST in the conditional (`may NOT: a future`; A2 state never fate); a totality ("any fight"); an event with no provenance and an EXTERNAL BODY unnamed and unpossessed (W17, W25); a second fact. The lattice's own string has no such clause; the shipped row invented it.
- Form findings, not claims: the "and … and" cascade with the inference as its third beat (the summarising beat, W6); the close on "happen" (a hollow verb handing nothing forward); the shipped row is a near-duplicate of the Beasts twin's `[visitor]` row (same verb class, same object pair, same "for them" tail, same inference) — the C7 same-page class the DEF-11 refuters named.

### (4) The reads the rewrite must state
Every face of this variant states BOTH (R1 the force at the class grain — the word replaced — and R2 no wall, as variant 1 §(4)), in the visitor's stance, and nothing else. The LICENSED claims of the shipped sentence are R1 (a force met; the word "soldiers" replaced) and R2 ("no line"); the use, the inference and the fight are not.

### (5) The angle's stance, in one sentence
`[visitor]` is "what a stranger notices first, without being told" (§0b), and here it may state what is met coming in — a force under arms met at `{settlement}` and no line passed, no works about the place, the town open on every side (of the town's want of works only, never of the ground) — with the stranger seeing and nothing more; it may NOT let the stranger be told anything, judge, foresee a fight, count, see a wall's use ("to stand behind", "to hold", "to man"), name a member (soldiers · garrison · barracks · militia · watch · guard), grade the force, act, decide, or take a name; the visitor carries no record noun, no citation, no gate (W8, W24, W27), no country and no terrain.

### (6) The turns worth keeping (the density floor)
- "A stranger sees [the force] at {settlement} and no line" — the visitor's frame carrying R1 and R2 on one verb in eight words (with the class-grain noun in the force's place). This is the variant's density floor: a face may keep the frame and the compression, never the member word and never the "for them" tail. This pool OWNS "sees" and "no line" on the page (the Beasts twin keeps "finds … and no wall").
- "A stranger sees" — the visitor's lawful opener (sees · meets · comes upon · passes), kept as this row's own construction so the ledger's entry and the street's idiom stay apart (A11; R-DA-10). Note for the drafter: "A stranger" opens the visitor row of twelve pools in this block and the Beasts twin's visitor row fires beside this one; one face may keep it and the others should vary the eye (a newcomer, an arrival, whoever comes in, "coming in one meets …") without making the stranger act (W27).

### (7) What would make the rewrite a regression here
- INVENTORY: variant 3 stays at index 2 with the `[visitor]` tag and grows to four wording faces, every face carrying `{settlement}` as the parent does; a fall, a merge, a reorder, a second tag or a `[plain]` marker is a trim or a malformed row.
- A LOST LICENSED READ: a face without R2 is `walls AND professional garrison`'s stranger ("sees the two things that matter together"); without R1 it is `walls with NO force`'s ("a serious absence of anyone standing in it") read backwards, or `neither`'s; a face that states R1 as "soldiers", "the garrison" or "a garrison" (attempt 3's "sees a garrison and an unwalled town") is the same loss under W12/W15.
- A LOST LAWFUL TURN: giving up the one-verb compression of force and absence for two flat clauses with no law behind the change (§21.4; ADDENDUM 6); dropping the observation frame altogether so the face reads as the ledger's entry under the `[visitor]` tag — attempt 2's "Soldiers stand at {settlement}, and no line runs about the town" and "The armed force at {settlement} is posted in a town that has no wall at any point" are the hazard: the stance must stay legible as the arrival's eye (what is met, seen, passed), with or without the word "stranger".
- A DROPPED ANGLE: a visitor face that enters the record's formula ("entered", "carried standing"), cites, or gives the town's talk ("here", "this town says"); a visitor who is TOLD (W27), who HEARS (the street's ear), who JUDGES ("can see how"), or who acts ("walks the perimeter" — there is none).
- AN ADDED CLAIM: "soldiers" (W15/A-6, above), "the garrison", "a garrison", "the militia", "the watch", "the guard", a grade, a use of the wall ("to stand behind", "to stand on", "to hold", "to set them on", "to man"), "decides", "would happen", "would fall", "the attacker", "whoever comes", "a fight", "an attack", a count, a season, the gate or pay, "open ground" / "level ground" as a terrain fact (DS-DEF-1's; "open" only of the town's want of works), the country's threat tier (the Beasts row's).
- SAME-PAGE PARAPHRASE (A11; C7): the Beasts twin `frontier, force without a perimeter` `[visitor]` — "A stranger finds soldiers at {settlement} and no wall for them to stand on, and can see how that would go …" — shares the shipped V3's verb class, object pair, tail and inference and fires beside this row on frontier towns; the rewritten faces here must not share its first two words where avoidable, its "finds … and no wall" pair, or its "can see how" tail — this pool keeps "sees" and "no line". Near neighbours in this row: `militia only` `[visitor]` "meets armed townspeople who are entirely competent …" (never "armed townspeople" here — it asserts the militia member); `walls with NO force` "sees a serious perimeter …"; `walls AND professional garrison` "sizing {settlement} up sees …". DS-DEF-5 `garrison PRESENT` `[visitor]` (`:2895`) "is counted at the gate by somebody whose job that is" — no gate, no counting, no somebody here (D-F7).
- FORM: no `[face]` row opens on `{settlement}` (T-F8); no `which`; no em dash; no digit; no question; no perfect aspect on the arrival ("has come", "arrived") that makes the visit a history; no subjunctive edge; the absence never opening the face (wall 3).
- THE THREAD: a visitor face closes on the force, the open place, or the wall's absence — a civic noun the composer's next modifier can carry — never on "them", "behind", "happen", or the stranger.
- FOUR FACES, NOT FOUR PARAPHRASES: four visitor faces differ in construction (the approach fronted; the force met first; the absence met first — after the force is named, wall 3; the two-sentence form with the town carried by the thread), never by a synonym for "stranger" in one frame (W4).
- EAR HAZARDS from the earlier attempts (data): attempt 1's every face carried "soldiers", a "line to hold/set them on" use, and a fight "where the attacker chose" (three unlicensed claims per face); attempt 2 dropped the stranger and wrote fabric lines under the visitor tag; attempt 3's "A traveller finds soldiers at {settlement} and no wall around the town" borrowed the Beasts twin's verb and object pair verbatim to the word class; "A newcomer at {settlement} sees soldiers. The town shows no wall." is a lawful two-sentence SHAPE (the thread carries the town) with the member word to replace.

## Pool-level notes

### Closing inventory (the marker's count)
- Shipped variants: **3** (`[ledger]` at index 0 with `{settlement}`; `[street]` at index 1, slotless; `[visitor]` at index 2 with `{settlement}`). None is trimmed, merged or reordered; the rewrite delivers 3 variants × 4 faces = 12 wordings, the counts only rising; every face keeps its parent's slot set exactly; every numbered row keeps its ONE angle tag and carries no `[plain]` marker.
- The ONE read, two components, stated in EVERY face: **R1** a standing force of the garrison class, at the class grain (a force · a standing force · the town's force · the muster as class word · a force under arms · people under arms · people) · **R2** no wall (the walls bucket empty, inside its negation: no wall · no works · no perimeter · nothing walled · unwalled · no line). No predicate beyond the row; no relation; no attach; no S2 clause seat (no engine-computed consequence of this row is read; ARCH §6.4's worked refusal keeps `muster: short` off the invasion row's garrison cells); no covert twin; `{band}` and `{route}` never written; no `{defwork}` on this block.
- The tier fact that binds every face: the pool fires ONLY at town on the 768-town sample (13 of 128 towns; 0 elsewhere), where the garrison-bucket member is always a `Barracks` (housing), the `Town watch` is always present and unread, and `Citizen militia` never generates natively. So the member is never named, the force is never graded, the watch is never mentioned, and no absence beyond the wall's is stated.
- Licensed holder: the muster, for the force fact; at town the muster's only roster holder (`Muster training` on a `Citizen militia`) resolves on no native firing; no S3 reason obtains; the lawful citation count on every unit of this pool is ZERO; the `[ledger]` tag licenses no record noun (W24).
- Claims of the shipped rows that survive the card: variant 1 — two of six (R1 "keeps a … force" with the grade struck, R2 "no perimeter"); variant 2 — two of three (R1 "people", R2 "rather than works" as a sibling-licensed contrast); variant 3 — two of six (R1 with "soldiers" replaced, R2 "no line") plus the stance. Everything else was bought with an effect: a grade, a rating, a raid, a siege, a cause, a maxim, a use, an inference and a forecast.
- Word bars binding every face (the desk's tables): "the garrison" / "a garrison" (W12) · "the barracks" (the member) · "the watch" (W13) · "the guard" / "guardsmen" (struck, A.5) · "the militia" / "no militia" / "only" (untested at this cell) · "professional" · "regulars" · "by trade" · "trained" (W14; §0 reading (b)) · "soldiers" (W15/A-6, with the rule-5 tension named) · "the muster roll" / "the roll" as a record (W24; OV-5) · "raiders" · "a siege" · "an army" · "the attacker" · "a fight" (W17, W25; a second fact) · any material, gate or use of the absent wall (W11; D-F7) · "the line" only inside its negation as a wall word · the country and its threat tier (the Beasts row's read; W2) · the terrain (DS-DEF-1's) · the gate's nouns, pay and upkeep (DS-DEF-11's).
- Same-page siblings to keep distance from (A11; C7), all reading the SAME roster on the SAME mount or tab: the Beasts twin `frontier, force without a perimeter` (its skeleton and this one allocate the shared words — §0 reading (d)); on a plagued or a settled country NO Beasts row prints beside this pool at all (`beastsRowSituation` returns the empty key for force-without-perimeter on both families, `defenseStateProse.js:429-440`), so the twin is a frontier-only neighbour; DS-DEF-5 `garrison PRESENT` (`:2892-2895`, the same bucket read: "keeps a standing force: people whose work …", "belong to the town rather than to a season", "counted at the gate"); DS-DEF-11 `UNWALLED-LARGE` (no walls, town and above; the `[counterforce]` arm that says the wall did NOT get built — the why is that cell's, never this one's); the five sibling rows of `INVASION_ROW_POOL` a reader meets across towns.
- The lawful moves for this pool (MOVE-GRAMMAR §2.1 level 1, filtered by the block's fields): V1 PRESENT (the row as one standing fact, the absence riding attributively) and V3 PRESENT → LACK (the force, then the wall's absence stated flat); no V2 (no structural-consequence field is read), no V4/V5 (no named object, no institution row is read — a member name would be a claim), no V6/V8 (no unresolved or `not-held` field). The census reads `grammars: 2` for the shipped pool; the rewrite keeps at least two distinct grammars across the three variants (A11 generalised) and reports the count.
- The band position is information only (§21.1); the drafter aims at the ceiling — the sharpest licensed fact, the strongest rhythm, the widest sibling distance, zero tics — and never trades density for plainness (§21.4).

### Owed to the chair, named so a drafter does not decide them silently
(i) §0 reading (b) — "professional" (the row label's own adjective, quoted on the card's `may claim`) read as UNLICENSED under W14/W12 because every native firing resolves the bucket to a `Barracks`; the sibling pool `walls AND professional garrison` carries the same word; a single ruling for the row is cheaper than two.
(ii) Variant 3 §(3) — "soldiers" refused under alias A-6 on a Barracks-resolving read while rule 5 lists it as a safe person-plural class word on the pay gate and REFERENT §2.1 calls it the always-safe body spelling; the same tension the Beasts twin's skeleton named.
(iii) §0 reading (c) — the lattice's own string for this cell (`threatAssessment.js:121`) licenses no rating, siege or capacity word (OW-20); the block's PROVENANCE "capability clauses" line is read as superseded by the card's `may NOT: a standpoint, a cause` (ADDENDUM 8 (2)).
(iv) Variant 2 §(3) and §(6) — the "rather than" CONTRAST read as sibling-licensed (R-DA-02 / wall 5) and its shipped line kept verbatim as ONE face under ADDENDUM 6, the tic reported as a band distance once in the pool.
(v) §0 — the militia component is NOT TESTED at this cell (k ≤ 1, `{walls, garrison}`), so no face states or denies a militia and no "only" is written; the key's discrimination against `militia only` lives in the engine's precedence, not in any face's text (ADDENDUM 7's honest limit).
(vi) §0 reading (d) — the word allocation with the Beasts twin on the same page (this pool: "keeps … and no perimeter", "no line", "A stranger sees", "people rather than works"; the twin: "keeps armed people on an open frontier", "finds … and no wall"); both skeletons refuse the shared "for them to stand behind/on … can see how" tail.
