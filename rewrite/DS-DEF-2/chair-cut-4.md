# THE CHAIR'S CUT 4 — batch 3's two open items (the OPUS chair, next day ~11:4x; the owner's word "Continue the rest with opus")

The chair cuts by name under OPUS_CHAIR_MANUAL §7.1 (cure targets and banked refusal rows are the chair's). Both cuts are MEASURED, not reasoned: `provenanceCount` was run on every line below.

## (A) THE FRONTIER POOL — apply the cure packet, minus ONE line
`ds-def-2--beasts-monsters-frontier-credible-deterrence/cure.md` was refused WHOLE by the cure gate because ONE cured line, variant 3's spine, spends the literal "from the road", which `moveGrammar.js`'s PROVENANCE alternative matches; that would raise `proseComposed.walker`'s SHRINK-ONLY citation ceiling from 7 to 8 corpus-wide. The gate was RIGHT and its measurement holds (executed by the chair: the head line reads 0, the cured line reads 1). But six lawful cures were dropped for one line, so the chair applies the packet with that one line re-cut.

APPLY `cure.md` VERBATIM, with this single replacement for variant 3's spine:

    3. `[counterforce]` A traveller says the line is there to be seen, and that he takes it what is in the country sees it too.

GROUND: the refuter's finding on v3f0 was that "a long way" is a magnitude stated in a word (F2-01's "a handful" shape) and asserts a prominence the village row does not hold; it said the stranger's conjecture in the second half is "lawful and worth keeping". This cut removes the magnitude WITHOUT spending the provenance literal: `provenanceCount` = 0 (executed), no distance word, the conjecture kept, the pool's own subject (a line meant to be seen) intact. The seven refusal rows the workflow banked for this pool are therefore DISCHARGED by this cut, except any the re-refute still finds.

## (B) THE FULL-LEGAL-CHAIN POOL — the one FAIL the cure introduced
At HEAD, `ds-def-2--internal-security-full-legal-chain-court-and-pri` variant 2 face 8 reads:

    - `[face]` `[garrison]` A soldier of the garrison says the town's quarrels are not the garrison's until one of them comes to the wall.

The re-refuter found it floor 1: a town-tier `Barracks` row (baseChance 0.3) seats the `garrison` source, while walls are an independent 0.5 coin, so an ordinary preimage town carries a garrison and NO wall and the face presupposes a body the roster does not hold. REPLACE with:

    - `[face]` `[garrison]` A soldier of the garrison says the town's quarrels are not the garrison's until somebody brings one to the garrison.

GROUND: the sense is kept (the garrison stays out of civil matters until drawn in), no wall is presupposed, no building is named beyond the source's own seat, `provenanceCount` = 0 (executed).

## AFTER
Census → generator → census → both `--check`; the FULL per-commit test list (`git show 95760e212` names the suites) plus `tests/lint/proseComposed.walker.test.js` (the ratchet that refused the packet) and `tests/lint/proseMoveGrammar.walker.test.js`; the wave gate `--section defense --arm cure --round 1`. If the face-count register moves, re-freeze it by hand and DECLARE the grow. Commit: `REWRITE 8b DS-DEF-2 cure 4 (the chair's cut by name): the frontier packet lands minus one citation, and the garrison stops presupposing a wall`.
