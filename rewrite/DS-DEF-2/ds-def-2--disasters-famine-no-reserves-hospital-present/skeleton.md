# SKELETON — DS-DEF-2 · `Disasters & Famine: NO reserves, hospital present` (the marker's packet; Opus seat, 2026-09-11)

Seat: MARKER (opus) for the REWRITE, working for the Fable chair. This file is the only thing this seat wrote, anywhere. Files in `laneRW-DEF2` were READ only; nothing in any dock was edited, committed or tested, and the only thing executed there was the read-only licence-card script the brief names. Everything below is judged against the printed licence card, the census row it was printed from, the reader function the card names and the producer flags behind it, the annex block's header lines, the register card (with amendments S2 and S3), RULES-V2-PART-B §1 and §16–§24, MOVE-GRAMMAR §1–§3 with §1.4.1 (the thread) and §4.4.1–§4.4.3, CLERK-LAWS §2.4.1 and §2.6.1, ARCH v2 §2.5 (the annex grammar) and §8.3 (the licence card), brief ADDENDUM 13 PART A and PART B (the entailment law, the referent law, the writer bars W11–W27), the two ratified tables' rows that name this pool's reads, and `rewrite/rulings-DEF2.txt`. Where a shipped clause reads well but no field licenses it, it is UNLICENSED here and the law is named.

Status: COMPLETE — section 0 (the card, the key unpacked to its producer, the tier arithmetic, the header lines, the register card, the shipped rows, the tables' findings, the seat's fences), the three shipped variants each with items (1) to (7), section 8 (the pool-level fences, whole) and section 9 (the open rows for the chair). **Variant count: THREE** (`[ledger]` · `[street]` · `[unfolding]`); twelve faces owed at four per variant.

---

## 0. THE CARD, THE KEY, ITS TWO HALVES AND THEIR LAYERS

### 0.1 The licence card as printed

`node scripts/prose-licence-card.mjs DS-DEF-2 'Disasters & Famine: NO reserves, hospital present'`, run read-only in `laneRW-DEF2`:

```
LICENCE (block DS-DEF-2 · role spine · key `Disasters & Famine: NO reserves, hospital present`)
  reads:      disasterRowSituation(granary, hospital, church) (via DISASTER_ROW_POOL in defenseStateProse.js)
              (absent ⇒ no candidate; a modifier is silent, never "false")
  predicate:  disasterRowSituation(granary, hospital, church) (via DISASTER_ROW_POOL in defenseStateProse.js) === no reserves, hospital
  bag:        {band: RESERVED, route: proper, settlement: proper}
              FILLED at this block's call sites: {settlement}
  relation:   (a spine takes no relation)   ← a spine IS the seat and carries no relation; the relation is the MODIFIER's property, fixed at its freeze (ARCH §4.5)
  seat/form:  (not a seat-taker) / sentence      move: (none declared)     angle: ledger street unfolding
  attach:     (empty: a spine takes no attach set)
              n/a
  echo:       spine mounts 1 (tabs: defense) · modifier mounts 0 (none)
              the echo table is keyed on this pool's WHOLE table-rung reading (truncated at the file's first dot) and NOT on a producer-token root, so every pool that selects a row of `DISASTER_ROW_POOL` shares ONE echo key: a mount counted there may be a sibling ROW of the same table
  covert:     no
  source:     (none) · standing SOURCE-UNRESOLVED
              NO citation is licensed: a face naming a record holder here is refused by arm A13
  may claim:  that the reader `disasterRowSituation(granary, hospital, church)` selects the row `no reserves, hospital` of `DISASTER_ROW_POOL` in `defenseStateProse.js`, as a STANDING fact of the record
  may NOT:    a count, a cause, a season, a future, a standpoint, a second fact, another civic object of the class `store`
  audience:   player (no mark)
  REFUSED COLUMNS, always: a totality over persons; an exemption from a duty (whoIsExempt is null everywhere); a named character and that character's fate (product scope); a theological claim about a deity (the deity doctrine)
```

The census row behind the card (`docs/content/wiring-census.json`, block DS-DEF-2, pool `Disasters & Famine: NO reserves, hospital present`): `status RESOLVED` · `rung table` · `keyFunction DISASTER_ROW_POOL` · `readsGrain branch` · `reads`/`fieldsRead`/`branchReads` = the one synthetic label above · `narrowed false` · **`objectClasses ["store","care"]`, `objectClass "store"`** · `sites ["defense.threatAssessment"]` · `k 2` · **`rateBp 404`** (the key fires on about four towns in a hundred of the RATE sample — a NINTH of the sibling `granary AND hospital` key's 3542) · **`source {kind:"", kinds:[], fields:{granary:"", hospital:"", church:""}, holder:null, holderReason:"no mapping row resolves any field this pool reads", standing:"SOURCE-UNRESOLVED", twoSource:false}`** · `variants 3` · **`grammars 2`** · `slotsFilled ["settlement"]` · `slotsNamed ["settlement"]` · `slotsWithoutProvider []` · `hasBag true` · `covert false` · `absent {}` · `attach []`. No `dm-only` mark on any of the three rows.

**`grammars 2`, read correctly.** The census computes `grammars` as `orders.size` over `orderIdOf(classifyMoves(v.text))` per variant (`src/domain/prose/wiringCensus.js:652-662`) — it is the count of DISTINCT TYPED MOVE ORDERS across the pool, **not** a sentence count. So two of this pool's three variants realise the SAME level-1 order and one differs; which two share was not measured by this seat (running the classifier is outside the marker's fence). Against MOVE-GRAMMAR §2.1's rule that a pool of k variants carries min(k, 8) DISTINCT level-1 grammars, a pool of 3 owes THREE and holds TWO. That is a spread debt the rewrite may repay, and it is one better than the sibling pool's `grammars 1`.

### 0.2 The key unpacked to its producer — read for what the key MEANS, never as a licence of its own

The caller (`src/domain/display/stateProse/defenseStateProse.js`, the `disaster:` rung of the threat-assessment builder) reads
`disasterRowPoolKey(civicFlag(compound.hasGranary), civicFlag(compound.hasHospital), civicFlag(compound.hasChurch))`;
`civicFlag(v)` is `v === true` (a strict boolean test, never a coercion). `disasterRowSituation(granary, hospital, church)` is:

```js
function disasterRowSituation(granary, hospital, church) {
  if (granary) {
    if (hospital) return 'granary, hospital';
    return church ? 'granary, parish care' : 'granary, no medical provision';
  }
  return hospital ? 'no reserves, hospital' : 'no reserves, no medical provision';
}
```

So this key is reached on `granary === false && hospital === true`, and **the `church` argument is NOT CONSULTED on this branch** (it is read only inside the `granary` limb). The key is TWO conditions of two producer booleans — one FALSE and one TRUE — and nothing else.

| half | how the producer derives it (`src/generators/priorityHelpers.js:63-64`) | what the flag records | REFERENT LAYER (ADDENDUM 13 B, A.1) | the lawful nouns | the unlawful nouns |
|---|---|---|---|---|---|
| `compound.inst.hasGranary === **false**` | `hasAny(names, ['granar'])` over the live roster's institution names is FALSE — no `granar`-stemmed row is on this town's roster | that NO GRANARY BUILDING stands on the record. **NOT that the town holds no food** (label trap L-20, ratified): the stem is `granar`, so the flag is about the BUILDING and never the stock; and it is a TIER PROXY — `Town granary` is `required:true, baseChance:1` at town (`institutionalCatalog.js:925`) and `City granaries` the same at city (`:1590`), and NO village/hamlet/thorp row matches the stem, so **every `no reserves …` pool is village-and-below** | the ABSENCE of a **BODY** (the `storehouse`-class row) at the ROSTER grain. Under A.1's vocabulary an absence is also listed at **NONE** — the one live layer conflict of this pool, ruled by this seat at §0.7 fence 1 and recorded as open row 1 | the BUILDING's absence stated at the roster grain: "no granary stands here"; "the town keeps no granary"; "there is no granary on the record" — the body word negated, the record's own grain, placed SECOND (an absence never opens: MOVE-GRAMMAR §1.4 wall 3; R-DA-08) | **every stock word**: "no reserves", "no food", "keeps no food", "nothing stored", "no stores", "holds nothing back", "no buffer", "empty larder", "no harvest kept" (the `store` class: `stores · reserve · reserves · stock · larder · harvest`, `wiringCensus.js` `CIVIC_OBJECT_CLASSES`; A-10: say the building or what is in it, never both in one word) — **and each of them is ENGINE-CONTRADICTED besides**: see §0.3 |
| `compound.inst.hasHospital === **true**` | `hasAny(names, ['hospital','monastery','healer','friary'])` | that SOME care-class row is on the roster. **Below town the ONLY row that can satisfy it is `Healer (divine, 1st level)`** (`institutionalCatalog.js:845`, a VILLAGE row, bc 0.4, magicLicense `low`, desc "Basic healing spells…") — a **PERSON**. `Small hospital` (`:1275`) and `Monastery or friary` (`:1267`) are TOWN rows; `Major hospital` city; `Hospital network` metropolis; and the cascade reaches at most ONE tier below a settlement's own (`cascadeGenerator.js:138-142`), so no sub-town roster can seat them | **BODY** (the referent table §2.1's hospital row: `BODY: flag hasHospital — catches monastery, healer, friary`); HOLDER none (SOURCE-UNRESOLVED) | the always-safe spelling of the alias bar (A-15; W15; `rulings-DEF2`): **"those tending the sick"** — and nothing else. "a house that takes in the sick" is licensed ONLY on a hospital-NAMED row, which on THIS key is unreachable on a native roster (§0.3) | "the hospital", "the infirmary", "the sick-house", "a place for the ill", "somewhere to put the sick" (a BUILDING the flag does not hold, and cannot hold here); "the healer", "a healer", "the physician" (a PERSON — W22 bars a person as a referent outright); beds, physicians, a count, funding; **"can treat", "can contain", "outbreak containment", "casualty treatment"** (D-13 CONDITIONAL on a hospital-named row, UNREACHABLE here, and its own NOT-ENTAILED row is "that it can contain the outbreak it faces") |
| `compound.inst.hasChurch` | not reached on this branch | NOTHING on this key | — | — | any church, parish, clergy, temple, shrine or faith word: the argument is not read, and `Monastery or friary` sets `hasChurch` and `hasHospital` from ONE row, so a clergy word beside the care body would name one row twice (rule 3, W20). A face here also names no deity and no spell (the `Healer` row is `divine`, `magicLicense: low`; the DEITY DOCTRINE is constitutional) |

### 0.3 THE TIER ARITHMETIC, AND THE TWO ENGINE CONTRADICTIONS THIS POOL SITS ON

**Where this key can fire.** `Town granary` and `City granaries` are `required: true, baseChance: 1`; `assembleInstitutions.js:279-282` seats a required row unless a DM toggle sets `forceExclude`. No sub-town catalogue row matches `granar`. Therefore, on a natively generated roster, `hasGranary === false` ⇒ **village, hamlet or thorp**. Of those three tiers only the VILLAGE holds any row matching `hasHospital`'s keywords, and only one — `Healer (divine, 1st level)`, bc 0.4. **So this pool's shipped sentences print, on the generated path, almost exclusively on a VILLAGE whose entire medical record is one first-level divine healer**, which the measured `rateBp 404` is consistent with. (The defense refuter's own row records the city end of the same arithmetic: "every `no reserves` pool … unreachable at city on a native roster".) Two escapes exist and are recorded, not ruled: a DM's `forceExclude` toggle on a town's required granary, and custom content naming a matching row. Recorded as open row 2.

**ENGINE CONTRADICTION 1 — the key's own name is false of the world.** `hasGranary === false` does NOT mean the town holds no food. `foodGenerator.js:161-165` gives a granary-less settlement `1.5` months at thorp/hamlet and `1.0` at village-and-up; `foodStockpile.js:190-193` gives the same settlement `1.5` at thorp/hamlet and `2.0` otherwise (the two producers **disagree**, which is itself recorded here). Either way the figure is NOT ZERO. And `isolationSupport.js:47`'s `RESERVE_INSTITUTION` regex counts `root cellar`, `warehouse`, `storehouse`, `silo`, `cistern`, `cold storage` and `preserv` as RESERVES — so the thorp `Communal root cellar` (`institutionalCatalog.js:111`, described in the catalogue as a store for grain and preserved food and a buffer against a bad harvest) is a reserve to one system and invisible to this flag. The ratified entailment table says this in its own words at L-20, and the defense refuter names the SIBLING cell's shipped line "holds no food buffer at all" (annex `:3046-3049`) as the breach this creates. **This pool's v1 "keeps no food against a bad harvest" is that same breach, one cell over.** A face asserting an empty store is not merely unlicensed; it is contradicted by the engine on the very towns that draw it.

**ENGINE CONTRADICTION 2 — the care capability cannot resolve here at all.** D-13's entailment "somewhere to put the sick; casualty treatment and outbreak containment" is CONDITIONAL on a `hospital`-named row. On this key that condition is not merely unresolved on the card (the sibling pool's situation) but **unreachable on a native roster**, because every hospital-named row sits at a tier whose granary is required. ADDENDUM 13 A item 6 is explicit that a CONDITIONAL used where its condition does not resolve FAILS. So v1's "can treat and contain an outbreak" is refused absolutely, not conditionally.

**What the ROW'S LABEL licenses (W14).** "Disasters & Famine" is the fifth readiness row's label (`threatAssessment.js:190`). On this pool the key reads two building flags and NOTHING of the badge: the row's BADGE printed beside every face is `scores.disaster` (`foodSecurity.resilienceScore × disasterGate`, `defenseGenerator.js:608-618`, rendered `DefenseTab.jsx:150-185`), a DIFFERENT read this card does not carry. So **no face grades this arm** — not "worse off", not "better prepared", not "exposed", not "thin", not "unready": the badge two inches away may say the opposite. The label's two pressures may be named as CLASSES — hunger and sickness — and never as events, seasons, outcomes or grades.

### 0.4 The block's header lines (annex `RECEIPT_POOLS_DOSSIER_STATE.md`, `### DS-DEF-2` at `:2568`; this pool's bold line at `:2715`, rows `:2716-2718`)

- **STATE-KEY:** five fixed rows, each with a `scoreBand` badge, read against `config.monsterThreat`, the institution presence flags and `compound.inst`. This pool is the `Disasters & Famine` row's spine; its key is TWO of the `compound.inst` presence flags, one negated (the badge is a separate read no face may speak, §0.3).
- **SLOTS:** `{settlement}` `{band}` `{route}` — the card FILLS only `{settlement}`; `{band}` is RESERVED and `{route}` unfilled here. **No face names `{band}` or `{route}`, and no face speaks a band word.**
- **SECTION-TARGET:** `defense`. **PDF PARITY:** parity (`viewModel.js` defense slice). **RECEIPT:** `threatAssessment.js:28-195`; rendered `DefenseTab.jsx:150-183`.
- **PROVENANCE + FENCE (the block's own, and it bites this pool hardest):** `buildThreatAssessment` is dossier-native and this shape EXTENDS it into a pool. Two standing defects must not be reintroduced (neither is on this row). And: **"Institution presence is a STANDING fact with no recorded history; the causal clauses here are *capability* clauses (walls without people cannot be held) and never *historical* ones (walls built after a siege) unless the history surface supplies the ancestry."** On this row the capability half is bounded to nothing by §0.3's contradiction 2 (the care capability cannot resolve) and by the absence half carrying no capability at all; and the HISTORICAL half is breached verbatim by shipped v3 ("the sickness it has seen", "the hunger it has not"). This line is the single most load-bearing sentence in the header for this pool.
- **Same page (C7; W2; A1/A11):** DS-DEF-2 mounts the other four readiness rows on the SAME tab — Beasts and Invasion name the works and the force, Internal Security the court and the gaol, Economic Survival its band. A face here names none of them. DS-DEF-6's Medical Readiness and Logistics & Supply cells (`defenseDisplay.js:235-245`) band the SAME two flags as chrome beside the face, and the desk's own docblock records that this row "speaks BOTH the reserve and the medical halves" while DS-DEF-6's medical lens "is the second half of that sentence on its own" — recorded so no drafter imports that cell's material and no reviewer reads the overlap as this pool's fault.
- **The composed neighbourhood (`k 2`, and the one thing that is CERTAIN about it):** the census gives this spine `k 2`, so up to two modifiers follow it in a composed unit, ordered by salience, and the writer does not choose the order. **The `stores: short` modifier — the neighbour that governs the sibling pool — is HELD OFF THIS SPINE.** ARCH §8.3's own worked card says so verbatim in its `attach` line ("the two NO-reserves cells HELD — T-F12"), and the census's `CIVIC_OBJECT_CLASSES` docblock says why in code: this key's string carries the stock word *reserves*, so `objectClassesOf` reads `store` on it and T-F12 refuses a `store`-class modifier beside a `store`-class key. By the same guard **no `care`-class modifier attaches either**. So whatever the two modifiers are, neither speaks the stock and neither speaks the care body: a face must hand forward a noun a modifier of some OTHER class can pick up (the town; the pressure class), and must not rely on a stock or care continuation. This seat did not enumerate the attachable set (outside the fence) — open row 5.

### 0.5 The register card's six one-line registers, applied (read whole; the dossier binds here)

The dossier: the record itself; the clerk's third person; the six shapes of its closed set; the town's name is not the default opener. The NPC ladder, the Herald, the chronicle, the DM page, and chrome and the docent do not reach this pool. **Amendment S2** (a computed CONSEQUENCE of the sentence's own fact may ride as ONE clause) — **not available here**: a spine takes no relation, and no relation row joins these two presence flags to any computed consequence; so every second fact takes its own sentence and no consequence clause may ride. **Amendment S3** (every fact has a holder; the office cites one only for one of three reasons) — the holder is UNRESOLVED, so the citation count for this pool is **ZERO** and any naming face is refused by arm A13 and by W24 ("on a read that resolves no holder, NO record word at all"). The register card's absence clause binds every face: an absence is written **in the record, never in the world; flat; never as the opener; replacing a sentence, never added** — which fixes the ORDER of this pool's two halves for all twelve faces (§8). The three questions: which field licenses it — two, `hasGranary` false and `hasHospital` true; which claims does it carry — two, and they must be the same two in every face; what does it leave standing open — the stock the town actually holds, which no field on this card reads and no face may guess at.

### 0.6 The pool's shipped rows, verbatim (annex `:2715-2718`)

1. `[ledger]` {settlement} can treat and contain an outbreak and keeps no food against a bad harvest; a crop failure here becomes hardship the same season it happens.
2. `[street]` The town is better prepared for the sickness than for the hunger, which is an unusual way round and does not comfort anyone.
3. `[unfolding]` {settlement} is arranged against the sickness it has seen and not against the hunger it has not, and nothing in hand is correcting the imbalance.

Variant count: **THREE**, matching the card's `angle: ledger street unfolding`. Slot sets per parent (ARCH §2.5: a face whose `{slot}` set differs from its parent's is REFUSED): vid 1 `{settlement}` · **vid 2 `{}` — no slot in this variant or in any of its faces** · vid 3 `{settlement}`. Sentence counts: all three are ONE sentence (vid 1 a semicolon joint, vid 2 a which-tail, vid 3 a comma-and tail). Openers: vid 1 on `{settlement}` (a T-F8 breach), vid 2 on "The town", vid 3 on `{settlement}` (a second T-F8 breach). **Two of the three shipped variants open on the settlement token; wall 10 permits at most ONE per pool, never two adjacent. That is a pool-level breach on top of the two per-face ones, and it is a cure, never a floor.**

### 0.7 The seat's fences, recorded for the writer and the chair (this seat's reading; §9 carries the chair's rows)

1. **THE ABSENCE'S NOUN — the pool's one live law conflict.** ADDENDUM 13 B A.1 files "an absence" under layer **NONE** ("no institution noun of any layer"), which read strictly would refuse even "no granary". MOVE-GRAMMAR §1.2 row 11 class (a) LACK is licensed by "a `none-exists` world field (**no walls, no temple, no king**)" — the move's own definition names the body in the negative — and R-DA-02 licenses the LACK as the first half only with no completing "but". **This seat rules:** the NONE layer governs what a noun may REFER TO when the read is a stock, a status or a condition — it stops a band being called "the watch" — and an absence read whose negated predicate IS the body flag is the one place where naming the body IS the read. So **"no granary" (the body word, negated, at the roster grain) is LICENSED, and every stock word is REFUSED**, both by A-10's class split and by §0.3's contradiction 1. If the chair rules the NONE layer strictly instead, the absence must be carried with no noun at all ("nothing of the kind is on the record"), every face loses its sharpest licensed word, and the pool's density floor drops to the care body alone. Open row 1.
2. **THE CARE BODY'S NOUN.** **"those tending the sick"** on every face, and nothing else — the alias bar's always-safe spelling (A-15, W15, `rulings-DEF2`), true of a hospital, a monastery and a healer alike. Here the underlying row is, on the generated path, a single PERSON, which W22 bars as a referent; the plural class phrase is what keeps the person off the page. This seat REFUSES every building noun and every capability verb on it. The referent table's row 25 lists "the infirmary · somewhere for the sick" in its layer-safe column; this seat rules the entailment table's CONDITIONAL binding over it (ADDENDUM 13 A item 6), and §0.3's contradiction 2 makes that ruling stronger here than at the sibling pool, because the condition cannot resolve at this key at all. Open row 3.
3. **THE PRESSURE CLASSES.** "hunger" and "sickness" as the CLASSES the row is keyed against, at the label's engine meaning (W14; S-8 permits only "illness", "sickness", "disease"), **bare and generic**. Refused: "the plague", "famine" as an event, "an outbreak", "a crop failure", "a failed harvest", "a bad year", "a bad harvest", "a hungry year", "a sick season", and any DEFINITE-PARTICULAR reading of the class ("the sickness it has seen") that makes the class a live or past event. Open row 4.
4. **NO GRADE, EVER.** The row's own badge is a separate read (§0.3). No face says better, worse, unusual, exposed, thin, short, ready, unready, adequate, or the other way round; no face compares the two halves of its own key as a ranking. Both shipped v2 and v3 do this, in different words, and it is the pool's most repeated shipped fault.

---

## VARIANT 1 · `[ledger]` (vid 1; slot set `{settlement}`; ONE sentence, a semicolon joint, an internal "and")

**(1) Number and angle tag:** variant 1, `[ledger]`.

**(2) Shipped, verbatim:**
> {settlement} can treat and contain an outbreak and keeps no food against a bad harvest; a crop failure here becomes hardship the same season it happens.

**(3) Every claim, one per line — LICENSED `<card field or read>` / UNLICENSED `<the law it breaks>`, each with its REFERENT LAYER beside the READ's layer (the read is BODY-present for the care half and BODY-ABSENT — NONE under a strict reading, §0.7 fence 1 — for the granary half):**

- `{settlement}` OPENS the sentence — **a pool-level fence, not a claim.** A sentence face may not open on a `proper`-typed slot of the block's bag (ARCH §2.5's face row, T-F8; MOVE-GRAMMAR §1.4 wall 10). A shipped breach; and this pool breaches wall 10 TWICE (vid 3 also opens on it), where the wall allows one per pool. Moving the opener off the token is a CURE, and **no face of this variant may carry it**.
- CAN TREAT — **UNLICENSED: an observable the fields do not hold, and a capability whose licensing condition is unreachable at this key.** D-13 entails "casualty treatment" only ON A `hospital`-NAMED ROW; on this key no hospital-named row can sit on a native roster (§0.3 contradiction 2), so the CONDITIONAL fails (ADDENDUM 13 A item 6). What the flag actually records below town is one `Healer (divine, 1st level)` — a PERSON with "Basic healing spells" — so the clause also reaches for a **PERSON as the load-bearing referent** behind an impersonal "can", which W22 bars outright. Layer: an asserted capability of a body the record does not hold → effectively **NONE**, against a **BODY** read. UNLICENSED.
- AND CONTAIN — **UNLICENSED: an observable the fields do not hold.** D-13's NOT-ENTAILED column names almost this phrase: "that it can contain the outbreak it faces". Layer NONE against BODY. UNLICENSED.
- AN OUTBREAK — **UNLICENSED: a second fact and an EVENT the card does not hold.** W17 (a disease outbreak is "illness" or "sickness", never "the plague" — and no outbreak is on this card at all); W18 (a stress is licensed by its own record; no stress record is on this card); the card's may-NOT "a second fact". S-8 records that the engine's two records on this event contradict each other, so only "illness", "sickness", "disease" is ever safe, and then only as a CLASS. Layer **NONE** against BODY. UNLICENSED.
- KEEPS NO FOOD — **UNLICENSED three times over, and this is the pool's ratified shipped breach.** (a) **The wrong object**: the read is `hasGranary === false`, a BUILDING flag (stem `granar`); "food" is the `store` class — the STOCK — and A-10 splits the two on purpose at 8a-8 ("say the building or what is in it, never both in one word"). (b) **An observable the fields do not hold**: D-12's NOT-ENTAILED column carries "how full", "how many months", "drawn on". (c) **ENGINE-CONTRADICTED**: a granary-less village holds 1.0 months to `foodGenerator.js:161-165` and 2.0 to `foodStockpile.js:190-193`, and `isolationSupport.js:47` counts a `root cellar` as a reserve — the ratified table says so at L-20 and the defense refuter names the identical shipped wording one cell over ("holds no food buffer at all", annex `:3046-3049`) as the breach it creates. Layer **NONE** (a stock) against a read whose object is a **BODY**. **Dropped by the rewrite, never kept as a floor.**
- AGAINST A BAD HARVEST — **UNLICENSED: a season, and an EVENT.** The card's may-NOT carries "a season" verbatim (R-DA-16); W17: a famine never entails a harvest failure or a season. D-12 licenses "it buffers a harvest" as the granary's entailed PURPOSE — and there is no granary here to have a purpose. Layer NONE against BODY-absent. UNLICENSED.
- THE SEMICOLON and everything after it — **UNLICENSED as a joint: a second FACT carried on the first sentence.** R-DA-03: a qualification takes its own sentence, never a tail, and MOVE-GRAMMAR wall 6 repeats it. S2's clause seat is NOT available (a spine takes no relation; no relation row joins these flags to any computed consequence — §0.5). Nor is the content a consequence the engine computed: it is a forecast the writer computed.
- A CROP FAILURE — **UNLICENSED: an EVENT, and a cause introduced.** W17 (a famine never entails a harvest failure); W18 (no stress record on this card); the card's may-NOT "a cause". Layer NONE against BODY-absent. UNLICENSED.
- HERE BECOMES HARDSHIP — **UNLICENSED: a forecast stated as a standing general truth, and an outcome no field holds.** The register card: nothing takes the future, an edge is subjunctive, and no edge field is here; MOVE-GRAMMAR §1.3's FORECAST non-move ("no field says what comes next"); STATE never FATE, which is constitutional under THE PROMISE. "Hardship" is held by no field, and the row's own badge beside the face may read STRONG under it. Layer NONE against BODY-absent. UNLICENSED.
- THE SAME SEASON IT HAPPENS — **UNLICENSED twice: a season (the card's may-NOT verbatim; R-DA-16) and a RATE** (how fast a thing the record does not hold arrives). Layer NONE. UNLICENSED.
- the AND joining "can treat and contain … and keeps no food" — **LAWFUL in its work, unlawful in its cargo.** The key IS the conjunction of the two conditions (`granary === false && hospital === true`), so naming both halves in one unit is not "a second fact" in the card's sense: it is the one read stated whole. The card's may-NOT bars a fact BEYOND the two, and "another civic object of the class `store`" bars a second stock object beside the first. **The joint's SHAPE — presence, then absence — is the lawful order (§8) and is worth keeping; both of the clauses it joins are refused.**
- **CITATION COUNT: zero, and lawful** — no holder is named. The card is SOURCE-UNRESOLVED; A13 refuses any naming face; W24 refuses any record word at all on a holder-less read, and the `[ledger]` tag is a STANDPOINT that licenses none.
- the sentence's SHAPE — one sentence, a semicolon, an internal "and": punctuation a writer may spend freely (the register card). What this one joins is unlicensed on both sides of the semicolon.
- **PROVENANCE of the whole:** the capability half is the engine's own chrome rewritten (`threatAssessment.js`'s disaster branch and `defenseDisplay.js:235-243`'s "Hospital present · Casualty treatment, outbreak containment, recovery capacity"). Product code is outside the REWRITE's corpus (OW-20) and is **never** a licence for the same claims in a face.

**(4) THE READS THE REWRITE MUST STATE — every read the card names, plus every LICENSED claim above:**

- **Read A — `hasHospital === true`:** a CARE-class row stands on this town's record, stated as **"those tending the sick"** and never as a building, a person or a capability. Layer BODY.
- **Read B — `hasGranary === false`:** **NO GRANARY** stands on this town's record — the `storehouse`-class body absent at the roster grain, written as a fact OF THE RECORD and never of the world, flat, and never first. Layer: the negated BODY (NONE under the strict reading; §0.7 fence 1, open row 1).
- **The conjunction itself:** both, TOGETHER, as the one standing fact the key selects — a care row present and no granary. A face stating one half has dropped half the read; and because the two halves pull opposite ways, a face stating only the presence reads as the sibling `granary AND hospital` cell and a face stating only the absence reads as `NO reserves, NO medical provision`. **On this pool a half-read is not terseness; it prints another cell's fact.**
- **The pressure classes** (optional, licensed at the label's engine meaning, W14): hunger and sickness as what the row is keyed against — bare, generic, never an event, a season, an outcome or a grade.
- **the shipped licensed material carried forward: the ORDER alone** — the care presence first, the granary absence second, in one unit. **No clause of this variant survives verbatim.**
- `{settlement}` in every face (the parent's slot set), **never as the opener**.
- SILENCE on: any stock, level, quantity, month or sufficiency; beds, physicians, a healer, a count; treating, containing, quarantine, recovery; any event, season, harvest, year or outbreak; any outcome, hardship, grade or badge word; the parish, the church, any clergy, any deity, any spell; the other four readiness rows; any holder or record word.

**(5) THE ANGLE'S STANCE — `[ledger]` on this pool, in one sentence:** the ledger is the clerk's own way of setting a fact down (W24: a STANDPOINT that licenses NO record noun and NO citation; R-vi: the office's own formula — "entered", "carried on the record", "set down as standing" — is the ledger's lawful surface, while "the books show" is a citation the budget does not license), so this face enters the two conditions as the compiled record holds them — a care row standing, and no granary on the record — both as STANDING facts with no history behind them (the block's PROVENANCE line), landing on the civic thing the fact names; it may NOT invent a stock, a level, a count, a date, a cause, a season, an event, an outcome, a capability, a grade, a badge reading, a holder or a record word, and it may NOT explain what the pairing MEANS.

**(6) THE TURNS WORTH KEEPING (lawful under the card, verbatim; the density floor):**
- **None verbatim.** This variant contains no clause the card licenses whole. What survives is the SHAPE: the two halves in one unit with the PRESENCE first and the ABSENCE second — which is not merely lawful but mandatory (an absence never opens: MOVE-GRAMMAR §1.4 wall 3; R-DA-08), and which the shipped line gets right while getting both of its nouns wrong.
- **Not kept:** "can treat and contain an outbreak"; "keeps no food"; "against a bad harvest"; "a crop failure here becomes hardship"; "the same season it happens"; the `{settlement}` opener.
- The density floor for this variant is therefore the two reads in the office's formula with a lawful joint between them, the pressure class where the face wants it, and the varied landing noun. **The honest limit (ADDENDUM 7) is stark here:** the shipped line's apparent richness — a capability, an outbreak, an empty store, a failed crop, a hardship, a season — is six inventions, two of which the engine actively contradicts, and a faithful rewrite is plainer in every one of those places. It is more knowledgeable in none, and it is right where the shipped line is wrong.

**(7) WHAT WOULD MAKE THE REWRITE A REGRESSION HERE:**
- **the ratified breach re-kept:** any face saying "no food", "no reserves", "nothing stored", "no stores", "an empty store", "no buffer", "nothing held back", "no harvest kept" — the `store`-class stock word on a BUILDING flag, engine-contradicted at L-20 and named by the defense refuter one cell over; dropped, never a floor;
- **the care capability re-kept:** "can treat", "can contain", "treatment", "containment", "quarantine", "recovery", "sees the sick through" — D-13's CONDITIONAL, unreachable at this key;
- **an inventory line:** a face that names the care row and the missing granary and stops, with no stance, no formula and no landing (ADDENDUM 7 rule 3): the read being a conjunction is not a licence to write a list;
- **a lost licensed read:** a face naming ONE half — which on this pool prints a SIBLING CELL'S fact, not a terse version of this one — or naming the granary's absence by the stock again, or replacing the care body with a parish, clergy or church word (the `church` argument is not read here, and one `Monastery or friary` row sets both flags);
- **a lost lawful turn:** the mandatory ORDER inverted, so a face opens on the absence (wall 3, R-DA-08) — the one structural thing the shipped line does right;
- **a dropped angle:** a face that reads as the town's talk or as a standing-open matter under a `[ledger]` tag; a face that cites a holder or reaches for a record word ("the books", "the rolls", "the register", "according to") — A13, W24;
- **an added unlicensed claim:** any of "food", "grain", "months", "enough", "full", "thin"; "the hospital", "the infirmary", "a healer", "beds", "physicians"; "an outbreak", "the plague", "a failed harvest", "a bad year", "a season", "a crop failure"; "hardship", "catastrophe", "collapse", "suffers", "goes hungry"; "because", "the reason", "which is why"; "better", "worse", "unusual", "exposed", "unready", or any badge word; a which-clause, an em dash, a digit, a percent, an exclamation, a question; the settlement token as opener.

---

## VARIANT 2 · `[street]` (vid 2; slot set `{}` — NO `{settlement}` in this variant or any of its faces; ONE sentence, a comparative frame and a `which`-tail)

**(1) Number and angle tag:** variant 2, `[street]`.

**(2) Shipped, verbatim:**
> The town is better prepared for the sickness than for the hunger, which is an unusual way round and does not comfort anyone.

**(3) Every claim, one per line — LICENSED / UNLICENSED with the law named, each with its REFERENT LAYER beside the READ's:**

- THE TOWN as subject and opener — **LAWFUL as a frame and as an opener.** The town is the possessor of the roster this key reads, so "the town is…" states the record in the street's naming form (W5), and this variant carries no slot to breach T-F8 with. What the town may not do is know, believe, decide, value, feel, judge, be comforted, or be compared with other towns.
- IS BETTER PREPARED … THAN … — **UNLICENSED: a GRADE, and a fronted CONTRAST.** (a) The card reads two booleans; it computes no readiness, and the row's own BADGE (`scores.disaster`) is a separate read this card does not carry (§0.3), so a face grading preparedness can print directly beneath a badge saying otherwise. "Prepared" is also the capability claim of v1 in adjectival dress — D-13's CONDITIONAL is unreachable here, so nothing on this card says the town is prepared for sickness at all. (b) A CONTRAST is licensed only where the rejected alternative names a SIBLING POOL KEY or a sibling BAND (R-DA-02; MOVE-GRAMMAR §1.4 wall 5); here the rejected alternative is **the other half of this same key**, not a sibling, and the contrast is **FRONTED as the sentence's subject**, which wall 5 bars in terms. Layer: "the sickness"/"the hunger" are pressure CLASS words at layer **NONE**, made the objects of a ranking, against a read whose two objects are a **BODY** and a **BODY's absence**. UNLICENSED.
- **NEITHER READ IS STATED.** The care row is never named; the missing granary is never named. This variant's whole content is a RANKING of two pressures, so it states ZERO of the card's two reads in licensed form — the same structural failure the sibling pool's `[counterforce]` variant carries, and the strictest regression risk in this pool is a rewrite that keeps the comparative frame and keeps the silence about the bodies.
- THE SICKNESS / THE HUNGER (definite, particular) — **borderline, ruled UNLICENSED here.** The bare class words are licensed at the label's engine meaning (W14; S-8 allows "illness", "sickness", "disease"). The DEFINITE article with a comparative frame reads them as two particular pressures this town faces, which slides the class toward an EVENT — the thing W17 and W18 refuse. Layer NONE. §0.7 fence 3; the cure is the bare class, not the definite one.
- `, WHICH IS …` — **UNLICENSED: a HARD WALL breached.** No which-clause (the brief's hard walls; R-DA-03's `, which` band 0.066 → ≤ 0.010 per variant; MOVE-GRAMMAR §1.4 wall 6: QUALIFY never as a "which" tail). This is a wall, not a band: it FAILS rather than reporting a distance (Part B §16 (1) and (5)).
- AN UNUSUAL WAY ROUND — **UNLICENSED twice.** (a) **A claim about OTHER TOWNS**: "unusual" is a rarity over the population, and no field on this card holds the distribution. That the key's measured `rateBp` is 404 does not license it — the record speaks of THIS town, and a face that knows how rare it is knows the corpus, which is the machine's own tell. (b) **A MEANING move**: the clause glosses what the preceding fact amounts to, and MOVE-GRAMMAR §1.3 records that no field holds what a fact means (R-DA-03's summarising second half; the register card's bar on explaining a fact). Layer NONE. UNLICENSED.
- AND DOES NOT COMFORT ANYONE — **UNLICENSED three times, and this is the sharpest single breach in the pool because it reaches a REFUSED COLUMN.** (a) **A TOTALITY OVER PERSONS** — "anyone" — which the card's REFUSED COLUMNS bar *always*, not by band. (b) **A FEELING**: comfort is an interior state assigned to the town's people; MOVE-GRAMMAR §1.3's FEELING non-move ("no field carries motive, belief or mood"); R-DA-14; NL-5. (c) **A PERSON as a load-bearing referent** (W22: no "somebody", "whoever", "anyone" as the thing a clause turns on). (d) It is also **the kicker** — the sentence buying its effect with a closing evaluation (the register card's bar on closing on a moral; fault 15). Layer NONE against BODY. UNLICENSED.
- the COMMA-JOINED TAIL entire — **UNLICENSED as a joint**: a second fact (in fact a second and a third) carried as a tail on the first sentence (R-DA-03; wall 6). S2's clause seat is not available (§0.5).
- **NO SLOT** — the parent's slot set is empty; a face of this variant carrying `{settlement}` is **refused outright** (ARCH §2.5). The town is "the town", never by its name.
- **CITATION COUNT: zero, and lawful** (A13; W24; SOURCE-UNRESOLVED).

**(4) THE READS THE REWRITE MUST STATE — every read the card names, plus every LICENSED claim above:**

- **Read A — `hasHospital === true`:** a care-class row stands, in the street's plain idiom — **"those tending the sick"**, never a building, never a person, never a capability. Layer BODY.
- **Read B — `hasGranary === false`:** no granary stands on the record, in the street's plain idiom, as a fact of the record, flat, and second. Layer: the negated BODY (open row 1).
- **The conjunction:** both together as the one standing fact; in this variant's register the natural form is the town as the subject that HAS the one and HAS NOT the other — which is the read stated whole in the plainest words the pool allows.
- **The pressure classes** (optional, W14): hunger and sickness, bare and generic, as what the row is keyed against — never definite-particular, never ranked against each other.
- the shipped licensed material carried forward: **"The town"** as the street's lawful subject and opener, and nothing else. **Nothing of "is better prepared … than", "which is an unusual way round", or "does not comfort anyone" survives.**
- **No `{settlement}` in any face.** One or two sentences per face (A1 allows either; R-DA-03 refuses a third).
- SILENCE on: any grade or comparison between the two halves; any stock or quantity; any capability; any event, season or outbreak; anything the town knows, feels, is comforted by, expects, has been told or thinks; any totality ("anyone", "everyone", "nobody", "all of them"); any claim about other towns or about how common this is; the badge; the parish; any holder or record word.

**(5) THE ANGLE'S STANCE — `[street]` on this pool, in one sentence:** the street is the town's own plain talk about its condition, so this face states the two conditions in the town's ordinary idiom — the short clause, the everyday noun where the ledger would use the office's formula, the town as the subject that has the one thing and not the other — and it may leave one matter standing open (stated, never asked); but NO field here holds anything the town does, says, believes, feels or values about either half, so the street may NOT report what the town knows, minds, is comforted by, is used to or thinks unusual (W22, W23, R-DA-13, R-DA-14, and the FEELING non-move), may NOT total the town ("anyone", "everybody", "nobody here"), may NOT rank its own two halves or speak of other towns, may NOT name a stock, a count, an event, a season or an outcome, and may NOT cite a record.

**(6) THE TURNS WORTH KEEPING (lawful under the card, verbatim; the density floor):**
- `The town` — the street's lawful subject and the variant's lawful opener; the pool's one genuinely reusable shipped asset, and the only clause-initial material in the whole pool that survives.
- **Not kept:** "is better prepared for the sickness than for the hunger"; ", which is an unusual way round"; "and does not comfort anyone" — each refused, the totality being the one that reaches a REFUSED COLUMN.
- The density floor for this variant is therefore the two reads in the street's idiom with the town as subject, the ORDER fixed (presence, then absence), and the matter left standing open by silence. Because the shipped line names NEITHER body, the floor here is the stance plus one lawful word — the thinnest floor of the three, and the honest one.

**(7) WHAT WOULD MAKE THE REWRITE A REGRESSION HERE:**
- **the refused column re-kept:** any face carrying "anyone", "everyone", "nobody", "no one here", "all of them", or any other totality over persons — refused *always*, by the card's own last line;
- **the grade re-kept:** "better prepared", "worse off", "further along for one than the other", "readier for", "the wrong way round", "an odd balance", "uneven", "lopsided", "an imbalance" — the comparison of the key's own two halves, which is a reading of the badge the card does not carry;
- **the interior recovered by a softer route:** "the town does not pretend", "nobody here is easy about it", "they are used to it", "the town would rather it were the other way" — these are the SAME breach as "does not comfort anyone", not a cure for it;
- **an inventory line:** the two facts named with no street idiom and no landing (ADDENDUM 7 rule 3);
- **a lost licensed read:** dropping "The town" as the subject from every face when it is the one shipped word the card licenses; naming one half only (which prints a sibling cell's fact, §8); slipping the granary's absence back into the stock ("the town has nothing put by", "there is nothing to eat through a bad year");
- **a slot breach:** any face carrying `{settlement}`, which this parent does not have;
- **a dropped angle:** a face in the ledger's office formula or in the unfolding's standing-open shape under a `[street]` tag;
- **an added unlicensed claim:** "the hospital", "the infirmary", "a healer", "a place for the ill"; "food", "stores", "reserves", "put by", "months", "enough"; "an outbreak", "the plague", "a bad harvest", "a failed crop", "a season"; "unusual", "rare", "most towns", "other towns", "you would expect"; "comfort", "worry", "fear", "knows", "minds", "is glad"; a which-clause, an em dash, a digit, a percent, an exclamation, a question.

---

## VARIANT 3 · `[unfolding]` (vid 3; slot set `{settlement}`, carried as the OPENER; ONE sentence, a paired "against X … and not against Y" frame and a comma-and tail)

**(1) Number and angle tag:** variant 3, `[unfolding]`.

**(2) Shipped, verbatim:**
> {settlement} is arranged against the sickness it has seen and not against the hunger it has not, and nothing in hand is correcting the imbalance.

**(3) Every claim, one per line — LICENSED / UNLICENSED with the law named, each with its REFERENT LAYER beside the READ's:**

- `{settlement}` OPENS the sentence — **a pool-level fence, not a claim**: the second T-F8 breach in this pool (ARCH §2.5's face row; MOVE-GRAMMAR §1.4 wall 10, which allows ONE settlement-opener per pool and is here breached twice). A cure, never a floor; no face of this variant may carry it.
- IS ARRANGED AGAINST … AND NOT AGAINST … — **the frame is the read's SHAPE and is lawful in form; its cargo is not.** The paired construction does carry both halves of the key (something standing against sickness, nothing standing against hunger), which is more of the read than either sibling variant states. But it states them **without naming either body**: the care row is never called anything, and the missing granary is never called anything, so what the reader is given is two pressures and an arrangement. Under the density law a face may compress; it may not vanish the referents the card licenses. **Kept as a SHAPE, refused as a statement of the reads.**
- THE SICKNESS (definite) / THE HUNGER (definite) — **borderline, ruled UNLICENSED in this construction** (as in v2): the definite article plus the relative clauses below turn the two CLASSES into two particular pressures with histories. The bare class is the cure (§0.7 fence 3). Layer NONE.
- IT HAS SEEN — **UNLICENSED: a HISTORICAL claim on a standing-configuration read, and the block's own PROVENANCE line breached verbatim.** R-DST-B / A6 is the licensing law under every move: "a standing configuration field licenses a STRUCTURAL clause and never a HISTORICAL one; only an event-provenance field licenses history" (MOVE-GRAMMAR §1.1), and the HISTORY move (§1.2 row 2) is licensed by an event-provenance field only — none is on this card. The block's header says it in its own words: *institution presence is a STANDING fact with no recorded history; the causal clauses here are capability clauses and never historical ones unless the history surface supplies the ancestry.* W18 repeats it: a stress is licensed by its own record, and no stress record is on this card. Layer **NONE** (an event) against a **BODY** read. UNLICENSED.
- AND NOT AGAINST THE HUNGER IT HAS NOT — **UNLICENSED twice.** (a) The same HISTORICAL claim in the negative: it asserts that this town has had no hunger, which no field holds. (b) **An absence written in the WORLD rather than in the record** — R-DA-08 and the register card are explicit that a declared gap is a fact OF THE RECORD, never of the world; "the hunger it has not [seen]" is a claim about the town's lived past, not about what the roster holds. Layer NONE against BODY-absent. UNLICENSED.
- the IMPLIED CAUSE of the whole frame — **UNLICENSED: a cause, and the header's named refusal.** "Arranged against what it has seen and not against what it has not" asserts that the care row exists BECAUSE of past sickness and the granary is missing BECAUSE there was no hunger. That is the "walls built after a siege" clause the block's PROVENANCE line refuses by name, and the card's may-NOT carries "a cause" verbatim; MOVE-GRAMMAR §1.3's MEANING non-move covers the gloss half. Layer NONE. UNLICENSED.
- NOTHING IN HAND — **UNLICENSED: a totality over the town's affairs that no field holds.** The card reads two booleans; it holds nothing about works, plans, projects, funds or intentions, so "nothing in hand" asserts the emptiness of a set the record never enumerates. Layer NONE. UNLICENSED.
- IS CORRECTING — **UNLICENSED: a FORECAST in continuous dress, and a trajectory.** MOVE-GRAMMAR §1.3: FORECAST does not exist anywhere in the estate; the register card: nothing takes the future and an edge is subjunctive; STATE never FATE under THE PROMISE. A present continuous asserting that no change is under way is a claim about what comes next wearing the present tense. Layer NONE. UNLICENSED.
- THE IMBALANCE — **UNLICENSED: a GRADE and a VALUATION**, the same fault as v2's "better prepared" in a different word: it reads the two halves of the key against each other and judges the result, while the row's own BADGE — a read this card does not carry — prints beside it. Layer NONE against BODY. UNLICENSED.
- the COMMA-AND TAIL entire — **UNLICENSED as a joint**: a second fact on the first sentence (R-DA-03; wall 6); S2's clause seat unavailable (§0.5).
- **THE `[unfolding]` TAG IS MISREAD BY THIS VARIANT — and this is the finding that governs the rewrite of vid 3.** R-iv (the chair's ruling that binds DS-DEF-2): *an `[unfolding]` variant realises OPEN — the matter standing open NOW — after the presence, never the ledger's measure.* The OPEN QUESTION move (MOVE-GRAMMAR §1.2 row 10) is a STANDING-OPEN specialisation of PRESENT, licensed by a state field whose value is unresolved / contested / pending / unmet, **declarative and present**. The shipped line instead realises a TRAJECTORY — a past that explains, and a future that is not coming — which is HISTORY plus FORECAST, the two moves this card licenses least. `[unfolding]` names the standing-open present; it does not name a direction of travel.
- **CITATION COUNT: zero, and lawful** (A13; W24; SOURCE-UNRESOLVED). "In hand" is not a holder and breaches no citation rule; it breaches the totality bar.

**(4) THE READS THE REWRITE MUST STATE — every read the card names, plus every LICENSED claim above:**

- **Read A — `hasHospital === true`:** a care-class row stands — **"those tending the sick"** — named, in this stance, as what the town has standing against sickness. Layer BODY.
- **Read B — `hasGranary === false`:** no granary stands on the record — named, in this stance, as the thing that is not there against hunger; written as a fact of the record, flat, and second. Layer: the negated BODY (open row 1).
- **The conjunction:** both together; this variant's paired frame is the pool's most natural place to state the conjunction whole, and it is the one variant whose shipped shape already reaches for it.
- **The pressure classes** (W14, and this stance's own material): hunger and sickness, bare and generic — the one variant where naming both pressures is most at home, and still never as events, never with histories, never ranked.
- **A MATTER LEFT STANDING OPEN, in the present, by silence** — the `[unfolding]`'s actual job under R-iv. No typed field on this card carries an unresolved/contested/pending value, so the OPEN QUESTION move is NOT licensed as a sentence; what remains lawful is the register card's own permission to *leave one matter standing open — stated, never asked* — realised by what the face declines to say (what the town does hold back, which the card never reads), never by "the record does not say", which would be an absence the record does not declare.
- the shipped licensed material carried forward: **the paired SHAPE "against … and not against …"** (with both halves re-nouned and both relative clauses struck), and nothing else. **No clause of this variant survives verbatim.**
- `{settlement}` in every face, **never as the opener** — vid 3 currently breaches this, and with vid 1 puts the pool two over wall 10's allowance of one.
- SILENCE on: anything the town has seen, suffered, escaped or been spared; any cause for either half; any plan, work, fund or remedy, and the absence of one; any trajectory, direction, drift or worsening; any grade, imbalance or comparison; any stock, capability, event, season, outcome or badge word; the parish; any holder or record word.

**(5) THE ANGLE'S STANCE — `[unfolding]` on this pool, in one sentence:** the unfolding is a STANCE within the dossier (W27, never a body and never a voice) and, under R-iv, it realises the matter STANDING OPEN NOW after the presence — so on this pool it is the record stated with the pair set out as it currently stands (a care row against sickness, no granary against hunger) and one matter left open in the present; it may NOT reach into the past for a reason (no event-provenance field is on this card and the block's PROVENANCE line refuses the historical clause by name), may NOT reach into the future for a direction ("is correcting", "is getting worse", "will tell", "so far"), may NOT grade the pair or name an imbalance, may NOT assert what the town has or has not in hand, and may NOT turn the two pressure classes into two events with histories.

**(6) THE TURNS WORTH KEEPING (lawful under the card, verbatim; the density floor):**
- **None verbatim.** What survives is the **paired frame** — something standing against the one pressure and nothing standing against the other, in one unit, presence first — which is the only shipped construction in this pool that reaches for both halves of the read at once. It is kept as a shape; every noun and every relative clause inside it is refused.
- **Not kept:** "is arranged against the sickness it has seen"; "and not against the hunger it has not"; "and nothing in hand is correcting the imbalance"; the `{settlement}` opener.
- The density floor for this variant is therefore the pair stated in the present with both bodies named at their own layer, the ORDER fixed, and one matter left standing open by silence. **The honest limit:** this variant's whole apparent depth — a remembered sickness, an unexperienced hunger, a reason for the arrangement, a drift nobody is arresting — is four inventions across two refused move classes (HISTORY and FORECAST), and a faithful rewrite is plainer in every one of them while being the only one of the three that genuinely holds both halves of the key in view.

**(7) WHAT WOULD MAKE THE REWRITE A REGRESSION HERE:**
- **the history re-kept:** "it has seen", "has known", "remembers", "has never had", "was spared", "since the last", "after", "once", "used to" — any past on a card that holds no event provenance, and the block's PROVENANCE line breached by name;
- **the forecast re-kept:** "is correcting", "is not being corrected", "nothing is changing", "will tell", "so far", "yet", "not yet", "in time", "before long", "the next one" — the FORECAST non-move, STATE never FATE;
- **the grade re-kept:** "the imbalance", "lopsided", "uneven", "the wrong way round", "one and not the other" **as a judgment** rather than as the two facts;
- **the totality re-kept:** "nothing in hand", "nothing is being done", "no one has", "there is no plan" — a set the record never enumerates;
- **an inventory line:** the pair named with no stance and nothing standing open, which is the ledger's face under an `[unfolding]` tag (ADDENDUM 7 rule 3);
- **a lost licensed read:** keeping this variant's habit of naming neither body — gesturing at "what the town is arranged against" while calling neither the care row nor the granary anything — or naming one half only, which prints a sibling cell's fact;
- **a lost lawful turn:** the paired frame abandoned in all four faces, so the conjunction is never heard as a pair; or the pair inverted so the absence opens (wall 3, R-DA-08);
- **a dropped angle:** an `[unfolding]` face that states the pair flatly with nothing standing open (the ledger's job) or that reports the town's talk (the street's);
- **an added unlicensed claim:** "the hospital", "the infirmary", "a healer"; "food", "stores", "reserves", "put by", "a buffer", "months"; "an outbreak", "the plague", "a failed harvest", "a bad year", "a season"; "because", "the reason", "which is why", "on account of"; "hardship", "hunger will", "it will go badly"; a which-clause, an em dash, a digit, a percent, an exclamation, a question; the settlement token as opener.

---

## 8. POOL-LEVEL FENCES THE TWELVE FACES MUST OBEY TOGETHER (the marker's inventory of the walls)

- **THE READ IS A CONJUNCTION OF A PRESENCE AND AN ABSENCE, AND EVERY FACE STATES BOTH HALVES.** `disasterRowSituation` returns `no reserves, hospital` only where `hasGranary` is FALSE **and** `hasHospital` is TRUE. A face naming one half is not a terse face: it **prints a sibling cell's fact** — presence alone reads as `granary AND hospital`, absence alone as `NO reserves, NO medical provision`. This is the pool's first wall, and it is sharper here than at any sibling because the two halves have opposite polarity.
- **THE ORDER IS FIXED FOR ALL TWELVE FACES: THE PRESENCE FIRST, THE ABSENCE SECOND.** An ABSENCE never opens and never sits beside another ABSENCE (MOVE-GRAMMAR §1.4 wall 3; R-DA-08; the register card). The shipped rows get this right; a rewrite that leads with the missing granary breaks a wall, not a band.
- **THE TWO BODY SPELLINGS, fixed for the whole pool.** The care row: **"those tending the sick"** and nothing else (A-15, W15, `rulings-DEF2`) — never "the hospital", "the infirmary", "the sick-house", "a place for the ill", "somewhere to put the sick", "a healer", "a physician" (a building the flag cannot hold at this key, or a PERSON W22 bars). The absent body: **the granary named and negated at the roster grain** — "no granary stands here", "the town keeps no granary", "there is no granary on the record" — never a stock word (§0.7 fence 1; open row 1 if the chair rules the strict NONE layer).
- **THE STOCK IS NEVER SPOKEN, AND SAYING IT IS A FACTUAL ERROR, NOT ONLY A BREACH.** No "food", "reserves", "stores", "stock", "larder", "harvest", "put by", "held back", "buffer", "months", "enough", "empty", "bare". The `store` class is a different civic object from the `storehouse` class (A-10; `CIVIC_OBJECT_CLASSES`), the read is the BUILDING flag, and the engine gives a granary-less settlement 1.0–2.0 months of food and counts a root cellar as a reserve (§0.3 contradiction 1; L-20; the defense refuter's own row on the identical shipped wording one cell over). **This is the pool's ratified shipped breach and the rewrite drops it.**
- **NO CAPABILITY ON THE CARE BODY.** No "can treat", "can contain", "containment", "quarantine", "casualty treatment", "recovery", "sees the sick through", "copes", "is equipped". D-13's entailment is CONDITIONAL on a hospital-named row and that condition **cannot resolve at this key on a native roster** (§0.3 contradiction 2). The lawful statement is that the row STANDS, and nothing about what it can do.
- **NO PERSON, ANYWHERE (W22; the REFUSED COLUMNS).** The row behind the care flag is, below town, one `Healer (divine, 1st level)` — a person. No face says "a healer", "the physician", "whoever", "somebody", "anyone", "everyone", "nobody", "the people here". The plural class phrase is precisely what keeps the person off the page, and a totality over persons is refused *always* by the card's last line (shipped v2 breaches it).
- **NO FAITH, NO DEITY, NO SPELL.** The care row is `divine` with `magicLicense: low`, and a `Monastery or friary` at the tiers above sets `hasChurch` from the same row; the `church` argument is not read here at all. No face names a church, a parish, clergy, a temple, a shrine, a god, a blessing or a spell (the DEITY DOCTRINE is constitutional; W19).
- **NO EVENT, NO SEASON, NO DURATION (W17, W18, R-DA-16, the card's "a season").** Never "an outbreak", "the plague", "a sickness that is spreading", "a famine", "a failed harvest", "a crop failure", "a bad year", "a bad harvest", "a hungry season", "a winter", "when it comes", "so far", "yet", "this year". The two pressures are CLASSES — hunger, sickness — **bare and generic**, never definite-particular, and nothing more.
- **NO HISTORY (R-DST-B / A6; the block's PROVENANCE line; MOVE-GRAMMAR §1.2 row 2).** No past tense about this town, no "has seen", "has never", "was spared", "since", "after", "used to", "still". Institution presence is a standing fact with no recorded history and this card carries no event provenance.
- **NO FORECAST, NO TRAJECTORY (MOVE-GRAMMAR §1.3; STATE never FATE; THE PROMISE).** No "becomes", "will", "is getting", "is correcting", "is not being corrected", "goes on", "the next one", "before long". An edge, if one were licensed, would be subjunctive; no edge field is here.
- **NO GRADE AND NO COMPARISON OF THE KEY'S OWN TWO HALVES.** No "better", "worse", "readier", "more prepared for one than the other", "the wrong way round", "unusual", "odd", "uneven", "lopsided", "an imbalance", "exposed", "thin", "short", and no band word. The row's own BADGE (`scores.disaster`) is a separate read this card does not carry and may print STRONG or CRITICAL directly beside any face (§0.3). **Both shipped v2 and v3 breach this in different words; it is the pool's most repeated fault.**
- **NO CAUSE, NO COUNTERFACTUAL, NO MEANING (the card's may-NOT; MOVE-GRAMMAR §1.3).** No "because", "the reason", "thanks to", "which is why", "on account of", "rather than", and no clause explaining what the pairing amounts to.
- **NO CLAIM ABOUT OTHER TOWNS OR ABOUT HOW COMMON THIS IS.** "Unusual", "rare", "most towns", "you would expect", "not the usual way round" are claims about the corpus, not about this town; that the key's `rateBp` is 404 licenses none of them.
- **NO SECOND CIVIC OBJECT (the card's may-NOT "another civic object of the class `store`"; `objectClasses ["store","care"]`; T-F12).** ONE care object and ONE storehouse absence, and nothing else civic — no parish, no church, no hall, no market, no mill, no walls, no muster, no watch, no court, no gaol. The other four readiness rows sit on the SAME tab and a face here names none of them (C7, W2, A1/A11).
- **NO CITATION AND NO RECORD WORD (A13; W24; SOURCE-UNRESOLVED).** The recommended citation count for this pool is **ZERO**. The `[ledger]` angle is a STANDPOINT licensing no record noun, so "the books", "the rolls", "the register", "according to", "the town's own count" are each a finding. The office's own formula ("entered", "carried on the record", "set down as standing") is the ledger's lawful surface (R-vi).
- **THE SLOT SETS ARE THE PARENTS' EXACTLY** (ARCH §2.5): vid 1 `{settlement}` · **vid 2 `{}`** · vid 3 `{settlement}`. `{band}` is RESERVED and `{route}` unfilled: no face names either, and no face speaks a band word.
- **THE SETTLEMENT TOKEN NEVER OPENS A SENTENCE FACE, AND THE POOL IS TWO OVER TODAY** (T-F8; MOVE-GRAMMAR §1.4 wall 10 — at most ONE settlement-opener per pool, never two adjacent). Vids 1 and 3 both open on it. The cure is structural, not cosmetic: the token rides inside the sentence.
- **THE SPREAD (census `grammars 2` over three variants).** MOVE-GRAMMAR §2.1: a pool of k variants carries min(k, 8) DISTINCT level-1 grammars. Three variants owe THREE orders and hold TWO; two of the three share an order (which two was not measured under this seat's fence). **This seat's recommendation, vetoable (open row 6):** the rewrite repays the debt by giving the three variants three distinct level-1 orders — the plainest available set being the presence-led PRESENT→LACK (V3) for `[ledger]`, the object-led V4 for `[street]`, and V3 with a standing-open close for `[unfolding]` — and by letting ONE variant run two sentences (A1 allows one or two; R-DA-03 refuses a third), where its second sentence carries a noun forward from its first (R-i).
- **THE THREAD (MOVE-GRAMMAR §1.4.1; the owner, 2026-09-08).** This pool is the SPINE and opens the passage; `k 2`, so up to two modifiers follow, ordered by salience, and the writer does not choose the order. Every face must hand a NOUN forward that a modifier can carry and must read well both alone and immediately before an unknown sibling. **The one thing certain about those siblings:** neither is a `store`-class nor a `care`-class modifier, because T-F12 refuses a modifier whose key names a class the spine's key names, and this key names both (`reserves` → `store`, `hospital` → `care`); ARCH §8.3's own worked card records the consequence in terms — the `stores: short` modifier is HELD off the two NO-reserves cells. **So a face must not lean on a stock or care continuation, and the safest nouns to hand forward are the town itself and the pressure class.**
- **THE FOUR FACES DIFFER IN CONSTRUCTION, NEVER IN VOCABULARY ALONE** (the owner's four-faces rule; ADDENDUM 7 rule 4; W4). The axes this pool actually offers: the SUBJECT (the town · those tending the sick · the record's own formula · the pressure); the GRAMMAR of the absence (a negated body clause, a bare "and none against hunger", a second sentence, a lack-first-half with no completing "but"); the JOINT (a plain "and", a paired against/not-against, a colon, a second sentence); and the LANDING noun. The ORDER of the two halves is NOT an axis — it is fixed by wall 3. Each face states the conjunction WHOLE; an unweighted seeded roll picks it, so none may lean on a sibling.
- **NEVER TRIM** (Part B §22): four faces per variant, TWELVE for the pool; a variant short of four is refused, and a face that is a paraphrase of its sibling is refused with it. A face that cannot be made lawful is banked as a refusal row with its measurement, never removed.
- **Hard walls, every face:** no em dash, no exclamation, no digit, no percent, **no which-clause** (shipped v2 carries one), no question, no "I" or "you", no figure, no sense verb on an abstraction, no intent or feeling for the town or for a building, no forecast, no totality over persons (shipped v2 carries one), no maxim, no kicker, no hook, no opener on the settlement token (shipped v1 and v3 both do), no three-of-anything by habit.
- **The label walls (W14).** "Disasters & Famine" is the ROW'S LABEL and this pool reads only the two building flags beneath it — never the badge, never a live famine, never "disaster" as an event. `hasGranary` is a TIER PROXY about the BUILDING, saying nothing of the stock and false on every village-and-below whatever it stores; `hasHospital` fires below town on one first-level divine healer. **The pool KEY's own words "NO reserves" are a key, not a licence: the key never changes (A17) and no face repeats its stock word as a claim.**
- **The bars reachable here, so a drafter reaching to "enrich" finds the bar rather than the gap:** W14 (both labels), W15 (the alias bar, on the care body — the pool's live one), W17 and W18 (the events), W20 (a body word at the wrong layer, and the stock for the building), W22 (the healer as a person; "anyone" as a referent), W24 (a record word on a holder-less read), W19 (the divine row's faith), W27 (the stances). W11, W12, W13, W16, W21, W25 and W26 are unreachable by a lawful face here because no wall, garrison, watch, pay-gate, power, external-body or covert read is on this card.
- **The echo:** every pool selecting a row of `DISASTER_ROW_POOL` shares ONE echo key (the card's own note), so a mount counted against this pool may be a sibling ROW of the same table; the faces neither rely on nor fear it.
- **The density floor for the pool (ADDENDUM 6; Part B §21.4).** The lawful purse is: the care body in its one safe spelling; the storehouse body named and negated at the roster grain; the two pressure classes bare; the office's formula or the street's idiom or the standing-open close, per angle; the fixed order; the varied landing noun; and the matter left standing open by silence. **That is the whole purse and it is spent in full in every face.** The honest limit: of the three shipped sentences, **not one clause is lawful whole** — the single surviving word is "The town" (v2's subject) — and what is lost with the rest is a capability that cannot resolve, an empty store the engine refutes, two events, a past, a future, a rarity, a grade and a totality over persons. The rewrite is plainer in exactly those places by design, more knowledgeable in none, and CORRECT where the shipped rows are factually wrong. **This plainness must not be read as a regression at review** (Part B §21.4: a refinement that made a lawful line plainer with no law behind the change is the regression — here every change has a law behind it).

## 9. THE MARKER'S OPEN ROWS FOR THE CHAIR (recorded, not decided; the drafter writes under this seat's reading until ruled)

1. **THE ABSENCE'S NOUN — the pool's one live law conflict, and the row that decides its density.** ADDENDUM 13 B A.1 lists "an absence" under layer **NONE** ("no institution noun of any layer"); MOVE-GRAMMAR §1.2 row 11 class (a) LACK is licensed by a `none-exists` field and its own examples are body words negated ("no walls, no temple, no king"). **This seat rules the body word licensed in the negative** ("no granary"), on the ground that the NONE layer governs what a noun may REFER TO on a stock/status/condition read and that an absence read whose negated predicate IS the body flag is the one place where the body word IS the read. **If the chair rules the strict NONE layer instead:** no face may name the granary at all, the absence must be carried nounlessly, and the pool's density floor falls to the care body plus the pressure classes. Nothing else in this packet moves.
2. **WHERE THIS KEY CAN FIRE — the tier arithmetic, and its two escapes.** `Town granary`/`City granaries` are `required: true, baseChance: 1` and `assembleInstitutions.js:279-282` seats a required row unless a DM toggle sets `forceExclude`; no sub-town row matches `granar`; the cascade reaches at most one tier below. So on the generated path this key is **village-and-below**, and below town the ONLY `hasHospital` row is `Healer (divine, 1st level)` at village (bc 0.4) — consistent with `rateBp 404`. The two escapes — a DM `forceExclude` on a town's required granary, and custom content naming a matching row — are **recorded, not ruled**; the faces are written to be true under all three, which is why no face may name a tier, a size or a kind of settlement.
3. **THE CARE BODY'S NOUN, and whether the plural is honest.** This seat licenses **"those tending the sick"** only (A-15, W15, `rulings-DEF2`), refusing every building noun and every capability. Two things are recorded for the chair. (a) The referent table's row 25 lists "the infirmary · somewhere for the sick" in its layer-safe column; this seat rules the entailment table's CONDITIONAL binding over it, and §0.3's contradiction 2 makes the ruling stronger here than at the sibling pool, because the condition cannot resolve at this key at all. (b) On the generated path the plural phrase stands over what is, on the record, **one person** — the plural is what keeps W22 satisfied, and this seat judges the class phrase honest (it asserts a kind, not a count) and records the tension rather than inventing a singular that would name the person.
4. **THE PRESSURE CLASS WORDS, and the definite article.** This seat licenses "hunger" and "sickness" **bare** at the label's engine meaning (W14; S-8's three safe words) and refuses "famine", "a failed harvest", "an outbreak", "the plague", "disaster", "catastrophe" — and also refuses the DEFINITE-PARTICULAR reading the shipped rows use twice ("the sickness it has seen", "better prepared for the sickness"), which slides a class into an event. If the chair reads even the bare class words as importing a live pressure, all three variants lose their stance material and fall back to the two bodies stated with nothing named against them — recorded because that would cut the `[unfolding]` and `[street]` floors hardest.
5. **THE COMPOSED NEIGHBOURHOOD IS NOT PRINTED ON THE CARD.** `k 2` says two modifiers may follow this spine; the card prints no list, and enumerating the attachable set is outside this seat's fence. What IS established from the census's own code and ARCH §8.3's worked card is the exclusion: **no `store`-class and no `care`-class modifier attaches**, because this key names both classes and T-F12 refuses the collision — ARCH §8.3 states it as "the two NO-reserves cells HELD". Recorded as a wiring question for the register car (should a spine's card print its live attach set?) and as a thread fact for the writer (§8).
6. **THE SPREAD PIN.** The census reads `grammars 2` over three variants against MOVE-GRAMMAR §2.1's min(k, 8) = 3. This seat recommends the rewrite repay the debt to three distinct level-1 orders and let ONE variant run two sentences; the pin is the sitting's to set, not the marker's, and A1 permits either. Which two variants currently share an order was not measured (the classifier is outside the fence).
7. **THE TWO FOOD PRODUCERS DISAGREE.** `foodGenerator.js:161-165` gives a granary-less village **1.0** months and `foodStockpile.js:190-193` gives the same settlement **2.0** (thorp and hamlet agree at 1.5). Not a writer's task and not this packet's business beyond the finding it grounds — recorded for the owner and the wiring car, because the disagreement is the second engine fact that makes "no food" false, and because a later card printing a stock read must be read as a change and not as a licence this packet missed.
8. **THE BADGE BESIDE THE FACE.** `scores.disaster` prints as the row's band directly beside every face of this pool and is a read this card does not carry, so a lawful face can sit under a CRITICAL or a STRONG badge. Recorded as a COMPOSITION fact for the chair and a wiring question for the register car: whether a presence spine should ever be lit without its own row's band on the card. Not a writer's task, and no face grades the arm.
9. **THE SAME-PAGE CHROME SAYS WHAT THE FACE MAY NOT.** DS-DEF-6's Medical Readiness and Logistics & Supply cells (`defenseDisplay.js:235-245`) band the same two flags beside the face, and the desk's own docblock records that this row "speaks BOTH the reserve and the medical halves" while DS-DEF-6's medical lens "is the second half of that sentence on its own". The engine's own strings are product code outside the REWRITE's corpus (OW-20, recorded for the owner as a scope question, not acted); the face follows the card and will therefore read plainer than the chrome two inches away. Recorded so the plainness is not mistaken for a regression at review.
10. **THE HOLDER IS UNRESOLVED AND LIKELY TO STAY SO** (`holderReason`: "no mapping row resolves any field this pool reads"; a granary holder row was drafted and WITHDRAWN per the referent table §2.1). Citation count ZERO stands for this pool until a register car mints one (OW-23's shape); recorded so a later card printing a holder is read as a change, not as a licence this packet missed.
11. **THE SHIPPED ROWS OF THIS POOL ARE, ON BALANCE, FACTUALLY WRONG — not merely unlicensed.** Two of the three assert an empty store the engine refutes or a capability the roster cannot supply. This is worth the chair's eye because it changes what the rewrite is: at most sibling pools the rewrite is a voice correction that loses invented richness; **here it is also a correction of record**, and the argument for the plainer wording does not rest on the voice alone.
