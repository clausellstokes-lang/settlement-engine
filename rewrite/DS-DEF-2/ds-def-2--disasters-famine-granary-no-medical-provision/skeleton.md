# SKELETON — DS-DEF-2 · pool `Disasters & Famine: granary, NO medical provision`
Seat: MARKER (opus). Status: COMPLETE (all three variants marked; §0 and §4 written).
Variants: 3 shipped (vids 1, 2, 3). Nothing in this file is a face.

---

## 0. What the writer reads before the first word

### 0.1 The licence card, printed this session in the dock (`node scripts/prose-licence-card.mjs DS-DEF-2 'Disasters & Famine: granary, NO medical provision'`)

```
LICENCE (block DS-DEF-2 · role spine · key `Disasters & Famine: granary, NO medical provision`)
  reads:      disasterRowSituation(granary, hospital, church) (via DISASTER_ROW_POOL in defenseStateProse.js)
              (absent ⇒ no candidate; a modifier is silent, never "false")
  predicate:  disasterRowSituation(granary, hospital, church) === granary, no medical provision
  bag:        {band: RESERVED, route: proper, settlement: proper}
              FILLED at this block's call sites: {settlement}
  relation:   (a spine takes no relation)
  seat/form:  (not a seat-taker) / sentence      move: (none declared)     angle: ledger street unfolding
  attach:     (empty: a spine takes no attach set)
  echo:       spine mounts 1 (tabs: defense) · modifier mounts 0 (none)
              the echo table is keyed on this pool's WHOLE table-rung reading and NOT on a
              producer-token root, so every pool that selects a row of `DISASTER_ROW_POOL`
              shares ONE echo key: a mount counted there may be a sibling ROW of the table
  covert:     no
  source:     (none) · standing SOURCE-UNRESOLVED
              NO citation is licensed: a face naming a record holder here is refused by arm A13
  THE TEST (ADDENDUM 14, the owner 2026-09-12): a face is LAWFUL unless it CONTRADICTS the record.
              SILENCE IN THE RECORD IS PERMISSION. "The card does not license it" is NOT a finding.
              This card says what the read REACHES, never the bounds of what may be written.
  may claim:  that the reader `disasterRowSituation(granary, hospital, church)` selects the row
              `granary, no medical provision` of `DISASTER_ROW_POOL` in `defenseStateProse.js`,
              as a STANDING fact of the record
  may NOT:    a magnitude outside the read's own band word (floor 2a), an elapsed course, a dated
              cause or a season (floor 2b), a prediction the pulse adjudicates (floor 2b),
              another civic object of the class `care`
  audience:   player (no mark)
  REFUSED COLUMNS, always: a totality over persons; an exemption from a duty (whoIsExempt is null
              everywhere); a named character and that character's fate (product scope); a
              theological claim about a deity (the deity doctrine)
```

⚠ **The `may NOT` line's last limb is this pool's own, and it is not boilerplate.** *Another civic object of the class `care`* means: the face may not put a lazaret, an infirmary, a sick-house, a poorhouse, an almshouse, a physician, a midwife, a wise-woman or a herbalist into this town in ANY form — not as present, not as "the nearest one", not as a thing the town once had. The whole discriminating content of this key is that the class is EMPTY.

### 0.2 The block's header lines (annex `RECEIPT_POOLS_DOSSIER_STATE.md` §DS-DEF-2, the parts that bind this pool)

- **RECEIPT:** `src/domain/display/threatAssessment.js:28-195` (`buildThreatAssessment`) · rendered `src/components/new/tabs/DefenseTab.jsx:150-183` · the walls predicate `src/domain/causalState.js:300-315`.
- **STATE-KEY:** five fixed rows, each with a `scoreBand` badge, read against `config.monsterThreat`, the institution presence flags, and `compound.inst`. **This pool is row 5, and row 5 reads ONLY the three institution flags** — not the country, not the band.
- **SLOTS:** `{settlement}` `{band}` `{route}` · **SECTION-TARGET:** `defense` · **PDF PARITY:** parity (`viewModel.js` defense slice).
- ⛔ **FENCE (verbatim force, the biggest in this cluster):** *Institution presence is a STANDING fact with no recorded history; the causal clauses here are* **capability** *clauses (walls without people cannot be held) and never* **historical** *ones (walls built after a siege) unless the history surface supplies the ancestry.* Read it twice before writing vid 2: two of the three shipped rows break it.
- **The block's own corpus complaint, which this pool is the cure for:** each branch currently holds exactly ONE string, so every settlement in a given branch says the same words.

### 0.3 The register card's six one-line registers (the dossier line is this pool's)

- **The dossier** — the record itself; the clerk's third person; the six shapes of its closed set; the town's name is not the default opener. ← **this pool**
- The NPC ladder — read aloud to the players; role-bound; never a named interior; the stage licenses the claim, never the shape.
- The Herald — the estate's one quoted in-world voice; report mode; flattest where hottest; the bill lands apart from the deed.
- The chronicle — a borrowed body of headlines; its own prose is frames and dressings; the quiet year is one sentence of varied shape.
- The DM page — candid; the why only from a typed field; second person to the referee alone; it grades, never hedges; every answerable plant answered, and some left open.
- Chrome and the docent — never the archivist; the product speaking to the person who runs it; mechanics first, one term per thing, the label as the only emphasis.

### 0.4 The owner's rules that bind every face, restated once

- **FOUR wording faces per semantic variant.** Twelve faces for this pool. Each a different vocabulary or rhythm inside the voice; never a paraphrase of its sibling.
- **NEVER TRIM** (Part B §22). Three variants stay three variants; each keeps its slot forever; a face that fails the gate is banked as a refusal row with its measurement, never deleted. Shortening a sentence inside its band is editing and is allowed; cutting a sentence is trimming and is not.
- **An unweighted seeded roll picks the face at render**, so every face must stand alone and must be the best of the twelve, not the safest.
- **The walls:** no em dash · no exclamation mark · no digit or percent anywhere in prose or in a connective · no `which`-clause · no question · no second person · no future indicative (the edge is subjunctive) · no figure, no sense verb on an abstraction, no intent for an inanimate thing · no gloss, no summarising second sentence, no moral, no uplift, no hook.
- **THE THREAD** (owner 2026-09-08 ~21:4x, a wall of every composed order): a composed unit is read as one passage. An added sentence carries a noun forward from the sentence before it, or its change of subject is the passage's one turn outward and sits last. Write each face so it reads well immediately after the spine AND after any sibling modifier — you do not choose its place.
- **THE CEILING, NOT THE MIDDLE** (§21.1) and **THE DENSITY LAW** (§21.4): compression that rewards the reader is part of the ceiling; a lawful line made plainer with no law behind the change is the regression.
- **THE PLAINER FLOOR** (owner 2026-09-09): the most memorable sentence WITHOUT increasing the claim set. The prose may be more elegant than the data and never more knowledgeable.
- **The `[slot]` contract (T-F8):** a face's `{slot}` set must equal its parent variant's. **vid 1 and vid 3 carry `{settlement}`; vid 2 carries NO SLOT and all four of its faces must carry none.**

### 0.5 ⭐ THE READS THIS POOL REACHES — MATERIAL a writer may use, never a bound on what may be written

The predicate is THREE BOOLEANS through one branch (`src/domain/display/stateProse/defenseStateProse.js:576-583`):

```
function disasterRowSituation(granary, hospital, church) {
  if (granary) {
    if (hospital) return 'granary, hospital';
    return church ? 'granary, parish care' : 'granary, no medical provision';
  }
  return hospital ? 'no reserves, hospital' : 'no reserves, no medical provision';
}
```

The caller hands it `civicFlag(compound.hasGranary), civicFlag(compound.hasHospital), civicFlag(compound.hasChurch)` (`:660-663`), where `compound` is `settlement.economicState.compound.inst`. **The lens is TOTAL — this row is never silent.**

⚠ **The desk's own docblock records why the situation set is five and not six:** a church counts as medical provision ONLY in the granary branch, because the corpus wrote `granary AND parish care only` and no matching "no reserves, parish care" pool. **So this pool is the one cell where the church is consulted and answers no.**

| read | what it holds on this key | holder | what it puts in a writer's hand | what the record can deny about it |
|---|---|---|---|---|
| `compound.inst.hasGranary` = **true** | `hasAny(names, ['granar'])` over the settlement's own institution roster (`priorityHelpers.js:63`) | **NONE. `SOURCE-UNRESOLVED`** — `holderTable.js` carries no row for `compound.inst` | exactly three catalog rows can satisfy it, and all three are named, administered objects: **`Town granary`** ("Communal grain storage. Buffers harvests, prevents famine."), **`City granaries`** ("Multiple large grain stores distribute food across the city. State managed."), **`State granary complex`** ("State-administered granary network holding strategic reserves."). The store is COMMUNAL or STATE-MANAGED, and at the two upper tiers it is PLURAL and DISTRIBUTED | **the stock inside it.** The block's own set: *hasGranary is a TIER PROXY (every town-plus, no village-and-below) and says nothing whatever about the stock inside it.* And `economicState.foodSecurity.storageMonths` holds the duration separately — see the contradiction table |
| `compound.inst.hasHospital` = **false** | `hasAny(names, ['hospital','monastery','healer','friary'])` is FALSE | none | the roster carries no hospital, no monastery, no friary and no healer of any kind. **Four keywords, all absent** | nothing in the record denies the absence; it is the key's own branch |
| `compound.inst.hasChurch` = **false** | `hasAny(names, ['church','cathedral','temple','monastery','friary','shrine','priest','abbey'])` is FALSE | none | **eight keywords, all absent.** No church, no cathedral, no temple, no shrine, no abbey, no monastery, no friary, no resident priest. The roster holds no house of faith and no clergy at all | ⚠ **it does NOT deny that the town has a faith.** `config.faithProfile.piety` and the deity record live outside the institution roster (`causalState.js:90`; `deityAxes.js`). **A face may say the roster holds no house; it may not say the town has no god, no belief and nobody who prays.** That is the deity doctrine's line and a refuter will take it |

**WHAT THE THREE FLAGS ARE BUILT FROM (`src/generators/priorityHelpers.js:45-80`) — read before writing a word:**

1. They are a **NAME MATCH over the settlement's institution roster**, lowercased. Nothing else. No count, no size, no siting, no condition, no keeper.
2. They are **NOT ruin-filtered.** The desk's own docblock says the FORCE rows read the live roster through `standingDefenseForces` precisely because `defenseProfile.institutions` is a generation-time snapshot that a ruin stamp never reaches. **This row does not take that cure.** It reads `economicState.compound.inst`, which is the name-match set. ⛔ **So a flattened granary still reads `hasGranary: true` here.** A face that asserts the building is sound, kept, guarded, full or in use is asserting a condition the flag does not hold and the ruin path can contradict.
3. **`hasHospital` fires on `monastery` and `friary`, and so does `hasChurch`.** So the two falses are not independent: a monastery would have satisfied both. The pool's real shape is *a granary, and not one of twelve keywords anywhere in the roster.*

**The reads the desk performs and this key does NOT reach, listed so the writer knows the page around the sentence:**

- **No band, no score.** This row's key reads no number at all. `scores.disaster` sits in the badge beside it and is a different fact (§0.7.2).
- **No country.** `config.monsterThreat` is row 1's; `plagued` is MONSTER ACTIVITY and never disease (the block's set), so neither the word nor its reading belongs here in any form.
- **No walls, no garrison, no militia, no watch, no court, no prison, no port, no route, no tier, no terrain, no population, no culture.**
- **No stress.** No key function of this block reads `config.stressTypes`. This pool prints under `famine`, `plague_onset`, `under_siege`, `occupied`, `indebted`, `monster_pressure` and `wartime`, each of which renders its own banner on the same tab (`DefenseTab.jsx:240-245`).
- **No live stockpile.** `economicState.foodSecurity.stockpile` is the campaign's live grain ledger and this key never asks for it.

### 0.6 The provenance move, priced for this pool: ZERO, and it is a wall rather than a recommendation

The card prints `source: (none) · standing SOURCE-UNRESOLVED` and says in its own line that **no citation is licensed and a face naming a record holder here is refused by arm A13.** The holder table has no row for `compound.inst`.

- **No named keeper, no named record, in any face.** Not the granary's own book, not a store roll, not a tally, not the elders, not the road, not a sexton's register.
- ⚠ **This pool's own temptation is sharper than most.** A granary is a thing with a keeper, and the `[ledger]` angle will reach for one. **The `[ledger]` face must sound like the record without naming one** — flat order, the fact first, the qualification in its own sentence.
- The exemplar registers with raw text cite at zero per 786 sentences, so zero is also the norm (§24).

### 0.7 ⭐ THE PAGE AROUND THE SENTENCE

#### 0.7.1 This paragraph is ALWAYS LAST, and its neighbour above is always the same row

The block renders `['beasts','invasion','internal','economic','disaster']` in that order, filtering out silent rows (`DefenseTab.jsx:112-114`). Of the five keys:

- `beastsRowPoolKey` **can be silent** (the corpus's own preserved silences, `defenseStateProse.js:429-440`).
- `invasionRowPoolKey` is TOTAL over six situations; `internalRowPoolKey` TOTAL over four; `disasterRowPoolKey` TOTAL over five.
- `economicRowPoolKey` is silent only on an absent or non-finite score.

**Therefore this paragraph is NEVER FIRST and ALWAYS LAST**, and the sentence directly above it is, on every town in the range, an `Economic Survival` paragraph — one of STRONG, ADEQUATE, WEAK or CRITICAL. That is the most precise thread fact this pool has, and the writer should use it:

- **The sentence before this one closes on money, funding, reserves, pay, a crisis the town can or cannot fund, or a shortfall.** Its four shipped forms end on *"the garrison can be kept paid while they last"*, *"the reserves are not deep enough to hide that from anybody"*, *"irregular pay shows up as morale exactly when it matters"*, *"exhausts the town's capacity almost immediately and then continues"*.
- **A face here that opens on money, reserves, funding or what the town can afford is picking up the previous paragraph's subject rather than starting its own.** The thread is served by carrying a NOUN forward, not the theme: the town, the season, the store.
- **Being last has a second consequence: this paragraph closes the whole threat block.** Under R-DA-04 the close is a KIND drawn per seed from `{condition · prohibition · absence · object · a name not given}` — and this pool is an ABSENCE pool, so the absence close is the one kind it must NOT take four times. The block's last word should not be the same shape in all twelve faces.

#### 0.7.2 ⛔ THE SHARPEST HAZARD IN THE PACKET: the badge beside this sentence MOVES and the sentence does not

The bar and band word rendered on this row are **`scores.disaster`** (`DefenseTab.jsx:185`, `defenseDisplay.js:311-313`), with a two-step fallback to `economicState.foodSecurity.resilienceScore` and then to a local composite. And **`scores.disaster` is the ONE score in this block that the world pulse re-grades:**

- `defenseGenerator.js:609-617` persists it as the gated resilience: `disasterGate = min(1, 0.55 + (econOut / 50) * 0.45)`, floor `0.55`, then `disaster = resilience × disasterGate`.
- `worldPulse/foodStockpile.js:385-402` re-grades the storage slice **from the CURRENT granary** each tick, recomputes `resilienceScore`, re-applies the persisted gate, and **writes `defenseProfile.scores.disaster` back immutably** when it moves (`:468-473`). Its own comment says so plainly: a siege eats the granary.
- The tab's caption says it out loud, one line above the bars: the bars are as judged at the first survey, **and Disasters & Famine is re-judged as the campaign advances** (`DefenseTab.jsx:313`).

⛔ **So this row's key is FROZEN AT BIRTH (a name in a roster) while this row's BADGE IS LIVE (the grain in the store).** They can end up saying opposite things six pixels apart, and the reader sees both at once. A face written as *the town is provided for* prints under a CRITICAL badge on a besieged town whose granary has been eaten. **The lawful subject of a face here is THE BUILDING AND THE ARRANGEMENT — what this town has put up and what it has not — never the quantity inside the building and never the town's current safety.**

#### 0.7.3 On the SAME TAB, DS-DEF-6's `Logistics & Supply` speaks the granary again, and one of its shipped lines contradicts this pool's shipped lines

`supplyLogisticsPoolKey(hasGranary, hasPort, tradeRouteAccess)` survives the DS-DEF-6 C3 block (it is one of only two lenses that do) and renders on the same tab at the SUPPORTING mount. Every town in this pool has `hasGranary: true`, so **one of its three granary branches always prints beside this paragraph:**

> `Granary + port` — *"…the hardest posture there is to starve out…"*
> `Granary in isolation` — *"…endurance here is exactly the size of what is in the granary and not a day longer."*
> `Granary with road supply` — *"…grain put by and knows it is put by against the {route} closing rather than against the harvest failing."*

⛔ **Read that last one against this pool's shipped vid 1.** DS-DEF-6 says the grain is put by *against the route closing rather than against the harvest failing*; DS-DEF-2 vid 1 says the town *can feed itself through a failed harvest*. **The two sentences are on one screen and they deny each other.** The lane's own fence for DS-DEF-6 is that LOGISTICS is *the granary against the SUPPLY ROUTE*, which is the reading this pool must leave alone.

**This row's discriminating job against that neighbour:** DS-DEF-6 owns the granary-against-the-route; **this row owns the granary against the SICKNESS** — the pairing of a reserve with an empty care class. A face that reaches for roads, the port, isolation, supply, a caravan, a cut road or endurance-in-days is writing the neighbour's sentence.

#### 0.7.4 On the Overview tab, DS-GEN-17 says the opposite of this pool in its own voice

`DS-GEN-17` resolves `compound.inst` to one of five keys in written order and stops at the first that holds. A town in this pool has `hasGranary: true`, so unless `ADMINISTERED`, `GARRISONED` or `LETTERED` fires first, **it resolves to `PROVISIONED`** — whose shipped variant 1 reads:

> *"{settlement} keeps stores against the bad season **and care for the bad year**; the town has met scarcity before and built its answer into the roster."*

⛔ **There is no care here.** `PROVISIONED` fires on `hasGranary OR hasHospital`, so it cannot tell this pool's town from a town with a hospital, and it asserts both halves. **This is an estate-internal contradiction the reader can see by changing tabs, and it is DS-GEN-17's fault rather than this pool's.** Recorded here for the chair rather than left to be re-found: DS-GEN-17's `PROVISIONED` faces are owed a disjunct-safe wording when that block's lane runs. Nothing this pool writes cures it, and no face here should try to answer it.

#### 0.7.5 The funding note that can print one click below this sentence

`READINESS_GATE_FOR['Disasters & Famine']` is `['disaster', 'relief funding']` (`defenseDisplay.js:283`). When `economicGates.disaster < 1` the expanded row renders **"Upkeep underfunded: relief funding at NN%"** (`:317-321`, rendered `DefenseTab.jsx:343`). The gate's floor is `0.55` and it is identity only at `economyOutput >= 50` (`defenseGenerator.js:614`).

⭐ **Read the expense name.** The estate's own word for what this row funds is **RELIEF**. So the record holds, on one row: a store, no house of care, and a relief budget that may be running at fifty-five per cent. A face that says the town wants for nothing, or that its answer to a bad year is complete, is contradicted by a note one click away.

### 0.8 ⭐ WHAT WOULD BE FALSE HERE — the contradiction rows THIS key can walk into

The block's own set is `rewrite/rulings-DEF2-v14.txt`. The rows below are the ones this key can actually reach.

| the claim that would be false | the field that denies it, and which of the two is the record |
|---|---|
| ⛔ **A DURATION FOR THE STORE: "through a failed harvest", "a hungry year", "a bad year", "a season", "months", "long enough"** | two denials. (a) The block's own set, verbatim: *hasGranary is a TIER PROXY and says nothing whatever about the stock inside it* — the flag is a NAME MATCH and carries no quantity, so any duration is floor 2a. (b) **The record holds the duration separately and it is under a year for most of the range.** `foodGenerator.js:161-165`: a state granary gives **8 months (12 at metropolis)**, a city granary **5 or 7**, a plain granary **5 at town, 3.5 at village, 2.5 otherwise**, with a mill multiplying by 1.25. **The generator is the record, and on a plain-granary town it says five months, not a year.** A failed harvest is answered by the next harvest; five months does not reach it |
| ⛔ **A FUTURE INDICATIVE: "will carry", "will hold", "will last", "is going to"** | a WALL, not a band (R-DA-07; A2; MOVE-GRAMMAR §1.3 FORECAST; THE PROMISE — state never fate). The edge is subjunctive: *would*. Shipped vid 2 carries two of them in one variant |
| ⛔ **AN ELAPSED COURSE OR A HISTORY: "has not built", "never built", "was never built", "the town chose", "since the last outbreak", "has met scarcity before"** | the block's own FENCE verbatim: *institution presence is a STANDING fact with no recorded history; the causal clauses here are capability clauses and never historical ones unless the history surface supplies the ancestry.* And floor 2b. There is NO event provenance on this key, so the HISTORY move does not exist here (MOVE-GRAMMAR §1.2 row 2: it may not exist in R1 STATE at all) |
| ⛔ **A PREDICTION THE PULSE ADJUDICATES: "a sickness spreads until it stops", "plague runs until it burns out", "a crop failure becomes hardship", "would take the town"** | floor 2b. The world pulse adjudicates the food ledger, the blockade and the stockpile (`worldPulse/foodStockpile.js`), and `plague_onset` is a typed stress with its own banner. ⚠ **The CLAIM itself is the estate's own** — `threatAssessment.js:188` prints *"No medical infrastructure. Plague spreads until it burns out."* directly under this row when the reader expands it — so the fact is available; it is the INDICATIVE MOOD that is the breach. The lawful form is the capability clause in the present, or the subjunctive edge |
| ⛔ **THE TOWN HAS NO FAITH, NO GOD, NOBODY WHO PRAYS, NOTHING SACRED** | `hasChurch` is a NAME MATCH over the institution roster. The faith record lives elsewhere — `config.faithProfile.piety` (`causalState.js:90`), the deity axes and the derived temper (`deityAxes.js`). **The roster is the record for the BUILDING; it is not the record for the belief.** And the DEITY DOCTRINE is a wall: faith is culture, never theology. The lawful statement is about the house and the clergy, never about what anyone believes |
| ⛔ **ANY OBJECT OF THE CLASS `care`, IN ANY MOOD: an infirmary, a lazaret, a sick-house, a ward, a poorhouse, an almshouse, a physician, a healer, a midwife, a herbalist, a wise-woman, "the nearest hospital", "a day's ride to a monastery"** | the card's own `may NOT` limb, and floor 1 (the closed roster). The class is EMPTY on this key. **A neighbouring settlement's provision is also refused** — this block reads no neighbour, and the address law's named-neighbour licence does not reach here |
| ⛔ **THE STORE IS FULL, SOUND, KEPT, GUARDED, KEPT DRY, IN USE, WELL RUN** | the flag is a name match and is **not ruin-filtered** (§0.5 row 2, against the desk's own `standingDefenseForces` cure for the force rows). A flattened granary still reads true. **The ruin path is the record**; the flag says a row of that name is on the roster and nothing else |
| ⛔ **THE TOWN IS SAFE, PROVIDED FOR, IN NO DANGER OF HUNGER, PAST THE WORST, UNTROUBLED** | three at once. (a) The live badge: `scores.disaster` is re-graded by the pulse and can read CRITICAL beside this sentence (§0.7.2). (b) The stress preimage: `famine` is REACHABLE with a granary — `stressGenerator.js:134` multiplies its probability by 0.5 and never to zero — so **a famine banner can print on this tab above this paragraph.** So can `plague_onset`, `under_siege`, `occupied` and `indebted`. (c) The funding note: *relief funding at NN%* (§0.7.5). **All three are the record** |
| ⛔ **A COUNT, A FIGURE, A SPELLED NUMBER, A NUMBER OF MONTHS, "one building", "two things"** | no digits in prose (A4); and the months belong to `foodSecurity.storageMonths`, which this key does not read |
| ⛔ **A TOTALITY OVER PERSONS: "everyone here", "nobody is treated", "the whole town", "no one is spared"** | the REFUSED COLUMNS line; `whoIsExempt` is null everywhere; a quantifier is licensed only by `closed` on the quantified column (CLERK-LAWS §1.3), and persons are never closed. ⚠ **A totality over the CIVIC CLASS is a different thing and IS licensed** — "there is no house for the sick" quantifies the roster, which is closed, not the people |
| ⛔ **A FEELING, A FEAR, A KNOWING, A BELIEF, A PREFERENCE HELD BY THE TOWN** | the FEELING move does not exist anywhere in the estate (MOVE-GRAMMAR §1.3): no field carries motive, belief or mood; the reaction is an ACT. Shipped vid 3 closes on one |
| ⛔ **A CITATION, A KEEPER, A RECORD NAMED** | `SOURCE-UNRESOLVED`; arm A13 refuses it (§0.6) |
| ⛔ **`plagued` READ AS DISEASE** | the engine meaning from the block's set: `plagued` is MONSTER ACTIVITY and never disease. This key does not read the country at all, so the word does not belong here in any reading. ⚠ **"plague" as the English noun for an outbreak is a different word and is NOT barred** — the estate's own string uses it — but a face must not let it read as the country tier |
| ⛔ **THE SUPPLY ROUTE, THE ROADS, THE PORT, ISOLATION, ENDURANCE IN DAYS** | DS-DEF-6's `Logistics & Supply` owns that reading and prints on the same tab (§0.7.3) |
| ⛔ **A MINTED PROPER NAME** (a person, a family, a lane, an inn, a road) | a pooled face is authored once and drawn by every town whose key matches, so the name prints identically across a region. This pool has no name authority and no `{npc}` slot |
| ⛔ **EXPLAINING THE BADGE, OR A GLOSS OF THE ROW** | the MEANING move does not exist. Saying the town rates poorly against disasters is the label read aloud |

**THE CLOSED ROSTERS THIS POOL TOUCHES (floor 1).** A body, building, record-keeper, force or faith-house these do not carry may not be asserted: **the institution roster** (`institutionalCatalog.js` per tier, resolved through `priorityHelpers.getInstitutionNames`), read here as the NAME-MATCH set and **not** through `liveInstitutions`; the seven defence buckets (`defenseInstitutionBuckets.js`); the faction list; the faith entries; the NPC office roster; **and the holder table (`holderTable.js`), which for this pool is EMPTY.** Everything else is silence, and silence is permission.

**Not a faith pool — but it is the one pool in the block that stands next to the faith doctrine.** The deity's four axes, the DERIVED temper (`deityTemper()`, never the inert stored `temperamentAxis`), the PANTHEON rank and the SETTLEMENT standing kept apart, and the suppressed flag beside the standing: **none of them is read here and no face may reach for any of them.** The absence of a church building is a ROSTER fact. It is not a fact about a deity, a pantheon, a standing or a suppression, and a face that lets it read as one has walked into the doctrine.

### 0.9 ⭐ THE PREIMAGE — the range of towns this key selects

`Disasters & Famine: granary, NO medical provision` fires on **every settlement whose roster carries a name matching `granar` and NO name matching any of `hospital · monastery · healer · friary · church · cathedral · temple · shrine · priest · abbey`.** Three booleans, nothing else. A boolean key fires across every tier, country and stress state it admits — so the range, read off the catalog:

- **THE TIER FLOOR IS REAL AND IT IS `town`.** Only three catalog rows match `granar`: `Town granary` (town, `required: true`), `City granaries` (city, `required: true`), `State granary complex` (metropolis, `required: false`, `baseChance: 0.7`). **No thorp, hamlet or village can reach this pool**, which matches the block's set: *hasGranary is a TIER PROXY (every town-plus, no village-and-below)*.
- ⭐⭐ **AND THE SECOND CONDITION IS HARD TO MEET, WHICH MAKES THIS POOL'S RANGE UNUSUALLY NARROW AND UNUSUALLY SHARP.** At **town**, `Parish churches (2-5)` is `required: true, baseChance: 1`; at **city**, `Parish churches (10-30)` is `required: true, baseChance: 1`. `assembleInstitutions.js:279-281` admits a required row before it consults the category toggle (`(inst.required && !forceExclude) || (catEnabled && toggle.require)`), so **disabling the Religious category does NOT remove them.** Only an explicit per-institution `toggle.forceExclude`, a custom roster, or an imported world can. **At METROPOLIS every church-family row is optional** — `Parish churches (50-100+)` 0.8, `Great cathedral` 0.6, `Major monasteries (5-10)` 0.55, `Hospital network` 0.55 — and the granary is 0.7.
  **So the unforced generated home of this pool is the METROPOLIS**, at roughly one roll in ninety by the catalog's own chances (0.7 × 0.2 × 0.4 × 0.45 × 0.45 ≈ 0.011 before priority weighting — **this is arithmetic from the cited baseChances, not a measured census**, and the wave's own corpus sample should settle it). A town or a city reaches this pool only where a creator deliberately struck the parish rows out.
  ⭐ **The writer should take this as a gift rather than a warning.** The towns in this pool are not towns that happen to lack a hospital; they are places whose record shows a granary and not one house of faith or care in the whole roster. That is a deliberate-looking arrangement, and the face may be written as though the arrangement means something, **without saying what it means or how it came about.**
- **EVERY COUNTRY TIER, EVERY ROUTE, EVERY TERRAIN, EVERY CULTURE, EVERY POPULATION BAND within town-plus.** None is read.
- **EVERY STRESS STATE.** No key function of this block reads `config.stressTypes`. `famine` is reachable (its probability is halved by a granary, never zeroed — `stressGenerator.js:134`); so are `plague_onset`, `under_siege`, `occupied`, `indebted`, `monster_pressure` and `wartime`. **A face here prints beneath a famine banner and beneath a plague banner.** Write nothing that a famine on the same page would make absurd.
- **EVERY BADGE.** The bar beside the sentence is `scores.disaster`, which can read STRONG on a metropolis with a full state granary and CRITICAL on the same town after a siege has eaten it. **The same face prints against both.**
- ⚠ **TWO CLOCKS, AND THEY DISAGREE.** The key is a birth fact (a name in a roster, never ruin-filtered). The badge is a live fact (the pulse re-grades it every tick). A face written as a report of what this town HAS BUILT is safe against both; a face written as a report of how the town IS DOING is safe against neither.

A face must contradict no state in that range, not merely the town on this skeleton.

---

<!-- VARIANTS 1-3 FOLLOW; written as they are settled -->
## VARIANT 1 · vid 1 · `[ledger]` · slots `{settlement}` · canonical at index zero

### 1.1 The shipped sentence, verbatim

> `[ledger]` {settlement} can feed itself through a failed harvest and has nothing at all against disease; a sickness here spreads until it stops of its own accord.

**Shape:** one sentence, two joints (an `and` coordination, then a semicolon). Two segments past the joint. Opens on the settlement token. Closes on a manner phrase.

### 1.2 Every claim it makes, on the new test

| # | the claim | verdict |
|---|---|---|
| 1 | a granary stands on this settlement's roster | **SAFE** — the key's own predicate; the one thing the card names as a standing fact of the record |
| 2 | the settlement **can feed itself** — the store is the town's own subsistence answer | **SAFE** as a capability of the arrangement. The catalog's own desc for `Town granary` is *"Communal grain storage. Buffers harvests, prevents famine."*, so buffering a harvest is the estate's own framing of the object |
| 3 | the store carries the town **through a failed harvest** — i.e. for the span from one harvest to the next | **CONTRADICTED** — `economicState.foodSecurity.storageMonths`, `src/generators/foodGenerator.js:161-165`: a plain granary gives **5 months at town**, a city granary **5 or 7**, and only a state granary at metropolis reaches **12**. A failed harvest is answered by the next harvest and five months does not reach it. **The generator is the record.** Compounded by floor 2a: the key is a NAME MATCH and holds no quantity at all (the block's set: *says nothing whatever about the stock inside it*) |
| 4 | the settlement **has nothing at all against disease** | **SAFE** — the key's own branch, and a totality over the CIVIC CLASS rather than over persons. Twelve keywords are absent from the roster; the roster is the closed column the quantifier needs |
| 5 | **a sickness here spreads** — the indicative, as a standing behaviour of this town | **FLOOR-2** *a prediction the pulse adjudicates*. `plague_onset` is a typed stress the world pulse and the stress generator adjudicate, and the row's own badge is re-graded live. ⚠ The CLAIM is the estate's own — `threatAssessment.js:188` prints *"No medical infrastructure. Plague spreads until it burns out."* one click below — so the fact is available and only the MOOD is the breach |
| 6 | it spreads **until it stops of its own accord** — an elapsed course with a terminus | **FLOOR-2** *an elapsed course*. It narrates a run to an end state, which is the HISTORY move's shape on a key that holds no event provenance, and it is the block's FENCE in the future tense rather than the past |
| 7 | the town takes no action against a sickness (implied by 5 and 6 together) | **SAFE** — entailed by claim 4 at the civic level. ⚠ It must NOT be allowed to read as *nobody does anything*, which is a totality over persons |

### 1.3 The reads this pool reaches (material for the rewrite of vid 1)

The three flags of §0.5, and nothing else. For the `[ledger]` angle in particular: the granary's three catalog identities are the concrete material — **communal** storage at town, **distributed and state-managed** stores at city, a **state-administered network of strategic reserves** at metropolis. A ledger face may reach for what kind of object the store is, because the roster names it. It may not reach for what is in it.

### 1.4 ⭐ WHAT WOULD BE FALSE HERE (vid 1)

Every row of §0.8 applies. The four this variant is standing on:

1. **The duration.** Any restatement of "through a failed harvest" — "through a bad year", "a season of failure", "until the next crop" — repeats the contradiction. **The lawful form names the KIND of thing the store answers, never how long it answers for.**
2. **The indicative forecast.** "spreads", "runs", "burns out", "takes hold", "goes through the town" in the present or future indicative. **The lawful form is the capability clause in the present ("there is nothing here that answers it") or the subjunctive edge ("would meet nothing").**
3. **The elapsed course.** "until it stops", "until it burns out", "until it has finished". A terminus is a history the key does not hold.
4. **The ledger angle's own trap: a keeper.** `SOURCE-UNRESOLVED`. No store roll, no tally, no book, no keeper, no "the record shows".

### 1.5 The preimage, as it bites vid 1

This face prints on a metropolis with a state granary network and on a town whose creator struck the parishes out; under a famine banner and under a plague banner; beside a STRONG badge and beside a CRITICAL one. **"Can feed itself" beside a CRITICAL disaster badge on a besieged town is the failure mode to design against**, and it is the reason claim 3 is the one to lose rather than claim 2.

### 1.6 The angle's stance in one sentence

`[ledger]` is the record stating the arrangement in the order a clerk would set it down — **what stands, then what does not** — flat, landing on the civic thing the fact names, with the qualification in its own sentence and no keeper cited.

### 1.7 The turns worth keeping

- **"has nothing at all against disease"** — the strongest clause in the whole pool. Flat, exact, a civic totality over a closed roster, and the absence written in the record rather than in the world. **Carry the fact; the wording itself is a candidate for one face verbatim.**
- **the two-part shape "X, and not Y"** — the LACK stated as the first half with no completing "but" is exactly R-DA-02's licensed form, and this variant already has it.
- **"of its own accord"** — a good phrase attached to an unlawful claim. The idiom is worth salvaging onto a lawful subject.

### 1.8 What would make the rewrite of vid 1 a regression

Trading the density for plainness (§21.4). Making it a two-fact inventory of the form *there is a granary and there is no hospital* — that is the shipped estate string with the nouns swapped, and it is what the whole rewrite exists to stop. Losing the asymmetry, which is this pool's entire content: **one thing built, one thing not, on one roster.**

### 1.9 ⭐ WHERE THE FLAVOUR IS (vid 1)

- **The store is an ADMINISTERED object, and the record says so in three different sizes.** *Communal* grain storage at town; *multiple large stores distributed across the city, state managed*; a *state-administered network holding strategic reserves* at metropolis. So the granary is not a barn — it is the one piece of civic machinery this town operates against the bad case. A ledger face can stand on the fact that the town runs a system for one kind of trouble and operates none for the other. Nobody has used the word "administered", or any of its plain English, anywhere in this pool.
- **The asymmetry is PHYSICAL and a stranger would see it without being told.** The grain store is among the largest and best-kept structures of its class on the roster, and there is no building at all on the other side of the ledger. Not a small one, not an old one — none. The shipped rows say "nothing at all" as an abstraction; nobody has noticed that it is a fact about the skyline.
- **Every one of the twelve missing keywords is a building or a person the catalog carries at this tier and this town did not take.** `Small hospital`, `Almshouse`, `Foundling home`, `Monastery or friary`, `Major hospital`, `Hospital network`, `Parish churches`, `Great cathedral`. The absence has a shape: it is the whole Religious-and-healing column of the roster, empty, on a town rich enough to build a state granary. **That is the particular a stranger would notice and nobody has written.**

---

## VARIANT 2 · vid 2 · `[unfolding]` · **NO SLOT** — this variant never names the town · **the most damaged of the three**

### 2.1 The shipped sentence, verbatim

> `[unfolding]` The stores will carry the town through hunger. Nothing here will carry it through a plague, and the town has not built anything that would.

**Shape:** two sentences. The first is four words past its subject and is a bare future indicative. The second repeats the first's verb ("will carry") and closes on a perfect ("has not built").

### 2.2 Every claim it makes, on the new test

| # | the claim | verdict |
|---|---|---|
| 1 | there are **stores** — a stock, not merely a building | **FLOOR-2** *a dependence on an unobserved field*. The key is a name match on `granar`; the stock lives in `foodSecurity.storageMonths` and this key never asks for it. ⚠ The NOUN is recoverable — a granary is a store by definition — but the sentence uses it as a QUANTITY |
| 2 | the stores **will carry** the town | **FLOOR-2** *a prediction the pulse adjudicates*, and a WALL breach besides: a bare future indicative is refused estate-wide (R-DA-07, A2, THE PROMISE). The pulse literally adjudicates this — `foodStockpile.js` spends the stores down tick by tick |
| 3 | they carry it **through hunger** — an unbounded span of the whole condition | **CONTRADICTED** on the same ground as vid 1 claim 3: `foodGenerator.js:161-165` holds the span and it is five months at town. Also floor 2a |
| 4 | **nothing here will carry it through a plague** | the FACT is **SAFE** (the key's branch); the FORM is **FLOOR-2** — the second bare future indicative in a two-sentence variant |
| 5 | **the town has not built anything that would** | **FLOOR-2** *an elapsed course*, and the block's FENCE named exactly: *institution presence is a STANDING fact with no recorded history; the causal clauses here are capability clauses and never historical ones.* "Has not built" is a historical clause on a key with no event provenance |
| 6 | the town had the OPTION and did not take it (entailed by "has not built") | **FLOOR-2** *a dependence on an unobserved field*. Nothing in the record holds what this town could have built or chose not to. ⚠ §0.9's preimage makes the entailment tempting and it is still not a licensed claim — the writer may FEEL the deliberateness and may not ASSERT it |

**This variant carries two wall breaches, one contradiction and three floor-2 rows in twenty-nine words.** It is the one to rebuild from the fact up rather than to repair.

### 2.3 The reads this pool reaches (material for the rewrite of vid 2)

As §0.5. ⛔ **And one hard constraint of its own: NO SLOT.** All four faces of vid 2 must name the town by a common noun — the town, the place, this settlement — and never by `{settlement}`. A face whose `{slot}` set differs from its parent's is refused by the annex grammar (T-F8).

### 2.4 ⭐ WHAT WOULD BE FALSE HERE (vid 2)

1. **Any future indicative.** This is the variant that reaches for one; it did so twice. The `[unfolding]` angle is the one that most wants a verb of motion through time, and the wall does not bend for the angle.
2. **Any perfect of building, deciding, choosing, neglecting, letting, allowing.** "Has not built", "never built", "has let", "has gone without" — every one is the FENCE.
3. **Any duration for the store.**
4. **Any account of WHY the care class is empty** — poverty, distance, a decision, a founding, a preference. The record holds no reason and §0.9's reachability arithmetic is not a licence.

### 2.5 The preimage, as it bites vid 2

`[unfolding]` reads as a process, and **the only process on this row belongs to the badge, not to the key.** The key is frozen at birth; the pulse is what unfolds. So the lawful unfolding here is NOT the grain going down and NOT the plague running — both are the pulse's. **What unfolds lawfully is the town's own STANDING asymmetry, stated as a condition that holds rather than as a course that runs.** That is the hardest of the three angles on this pool and the packet says so plainly.

### 2.6 The angle's stance in one sentence

`[unfolding]` may state a condition that is CONTINUING rather than momentary — what is the case now and goes on being the case — **without a verb of motion, without a terminus, and without a future**; its licensed tense is the present, and its licensed edge is the subjunctive.

### 2.7 The turns worth keeping

- **The bare contrast across two sentences** — the first sentence for the thing that stands, the second for the thing that does not. That is the shape R-DA-03 asks for (the qualification in its own sentence, never a tail) and this variant already has it.
- **"Nothing here"** — three words, a civic totality over a closed roster, no persons quantified. Clean and worth carrying.

### 2.8 What would make the rewrite of vid 2 a regression

Curing the two futures by flattening the variant into a present-tense inventory. The owner's density law is explicit: a lawful line made plainer with no law behind the change is the regression. **The cure for "will carry" is a sharper present, not a duller one.**

### 2.9 ⭐ WHERE THE FLAVOUR IS (vid 2)

- **The town keeps its dead better than its sick.** Every tier in this pool's range carries a REQUIRED burial row and no tier requires a house of care: `Parish burial grounds` at town, `Burial grounds and charnel house` at city, `Cemetery network` at metropolis. Their catalog descs are among the richest in the file — a sexton's register that is the town's longest unbroken record, carts going out at dusk, grounds beyond every gate with their own gatekeepers and a trade in plots the revenue office watches. ⚠ **ROSTER-BOUND:** a face naming the burial ground is licensed where that row resolves, which is every generated tier of the preimage but not necessarily a custom roster. **This is the single sharpest unused fact available to this pool**, and the writer should reach for it knowing the refuters will ask which row licenses it.
- **The ground is still called the PARISH's, and there is no parish.** On a town or a city that reached this pool by having its churches struck out, the required burial row keeps its name. A stranger reads *parish* on the one civic ground and finds no parish anywhere behind it. That is a genuine, typed, on-the-roster oddity and nobody has written it.
- **What the absence looks like on the ground:** there is nowhere to take a sick person, so a sick person stays in the house they fell ill in, and containment is a matter of doors rather than of a ward. A traveller who falls ill on this road becomes a householder's problem rather than an institution's. **Note the line:** that is a description of an arrangement (SAFE) and becomes a forecast the moment it says what happens next.

---

## VARIANT 3 · vid 3 · `[street]` · slots `{settlement}`

### 3.1 The shipped sentence, verbatim

> `[street]` The town can outlast a hungry year at {settlement} and has no answer at all to a sick one, and knows which of the two it fears.

**Shape:** one sentence, three segments, two `and` joints in a row. The slot is stranded mid-clause ("The town … at {settlement}"), so the town is named twice in eight words by two different terms.

### 3.2 Every claim it makes, on the new test

| # | the claim | verdict |
|---|---|---|
| 1 | a granary stands on this settlement's roster | **SAFE** — the key's own predicate |
| 2 | the town **can outlast a hungry year** | **CONTRADICTED** — the same row as vid 1 claim 3, in its sharpest form: *a year* is named outright. `foodGenerator.js:161-165` holds **5 months at town**, 3.5 at village, 5 or 7 at city; only the metropolis state-granary branch reaches twelve. **The generator is the record.** Also floor 2a on a boolean key |
| 3 | the town **has no answer at all to a sick one** | **SAFE** — the key's own branch; a civic totality over the closed roster. The parallelism *a hungry year … a sick one* is the best turn in the variant |
| 4 | the town **knows** something | **FLOOR-2** *a dependence on an unobserved field*, and a WALL besides: the FEELING move does not exist anywhere in the estate (MOVE-GRAMMAR §1.3 — no field carries motive, belief or mood; the reaction is an ACT). Nothing holds what this town knows |
| 5 | the town **fears** one of the two more than the other | **FLOOR-2** on the same ground, doubled: it is an interior AND a comparison of two interiors. No field holds either term |
| 6 | the town's people hold that fear collectively ("the town … fears") | **FLOOR-2**, and it edges the REFUSED COLUMNS line — a totality over persons wearing a civic noun |
| 7 | (implied) the sickness is the worse of the two here | **SAFE as a civic reading** — it follows from claims 1 and 3 at the level of what the town has built, and it is the pool's actual content. ⚠ **It is safe as an ARRANGEMENT and unsafe as a SENTIMENT.** The rewrite's whole job on this variant is to move that reading from the second form into the first |

### 3.3 The reads this pool reaches (material for the rewrite of vid 3)

As §0.5. For `[street]` in particular: the material is what a person in the town would DO, not what they would feel — where a sick person is taken, what door is closed, which building the grain goes into and which building the sick do not go into. The register card's line is exact: **the reaction is an ACT.**

### 3.4 ⭐ WHAT WOULD BE FALSE HERE (vid 3)

1. **Every verb of interior state:** knows, fears, worries, expects, trusts, prefers, understands, remembers, has learned, has come to rely on, takes seriously, does not think about. The `[street]` angle is the one that reaches for these and the wall does not bend for it.
2. **Any totality over persons:** "everybody here", "nobody", "the whole town", "anyone can tell you", "they all know".
3. **The year.** Any duration for the store.
4. **A collective preference or priority** — "the town has spent its thinking on the grain", "cares more about", "would rather". That is claim 7 in its unsafe form.
5. **A named voice, a saying, a thing people say.** No saying row exists in a typed table (the SAYING non-move), and no citation is licensed.

### 3.5 The preimage, as it bites vid 3

`[street]` on a metropolis is a different sentence from `[street]` on a struck-out market town, and the same face prints on both. **So the street face may not describe a street.** No lane, no square, no market day, no crowd size, no district — the key reads no tier, no population and no terrain. What survives across the whole range is the CIVIC ARRANGEMENT seen from ground level: the thing you can walk to, and the thing you cannot.

### 3.6 The angle's stance in one sentence

`[street]` may state the arrangement as it is met by a person standing in the town — **what is done, where a thing is taken, what is available at the point of need** — in the clerk's third person, as an act or a standing practice, never as a mood, a knowledge or a collective opinion.

### 3.7 The turns worth keeping

- ⭐ **"a hungry year … a sick one"** — the pool's best turn by a distance. One adjective pair carrying the entire two-sided key, with the second half compressed to a pronoun. **This is the density law's kind of compression and it should survive into at least one face, in some form.** ⚠ It carries the contradicted *year*; the parallelism has to be rebuilt on a noun the record holds.
- **"no answer at all to"** — the same flat totality as vid 1's "nothing at all against", in a different rhythm. Two lawful forms of one claim, and the pool has room for both.

### 3.8 What would make the rewrite of vid 3 a regression

Losing the parallel construction in the process of curing the fear clause. The parallelism IS the variant; the interior is a tail bolted to it. **Cut the tail, keep the spine, and let the last move be an act or a standing condition rather than a state of mind** (R-DA-04's close kinds: a condition · a prohibition · an absence · an object · a name not given).

Also: fix the double naming. "The town … at {settlement}" names the same thing twice by two terms in one clause, which is R-DA-22's one-term-for-one-thing breach dressed as a slot placement.

### 3.9 ⭐ WHERE THE FLAVOUR IS (vid 3)

- **There is a place to take grain and no place to take a person.** That is the whole pool in one concrete, ground-level asymmetry, and it is an ACT rather than a feeling: the grain goes somewhere; the sick stay where they are. Nobody has written the pool from the point of view of *where a thing is carried*.
- **The care the town does get is domestic, and the record is silent about it, which is permission.** With no hospital, no monastery, no healer and no priest on the roster, the tending that happens happens in houses, by whoever is there. **A face may describe that arrangement** — a sick person is a household's business — **and may not turn it into a count, a named person, a virtue or an outcome.** The silence here is the widest in the pool and it is the writer's to use.
- **A stranger falling ill in this town has nowhere to be taken, and a stranger is exactly who the `[street]` and `[visitor]` material is for.** The catalog carries `Travelers' inn` and its kin at these tiers; the traveller arrives, and the one civic answer to a body is the one the town has not built. That is a concrete, licensed, wholly unused situation.
- **What someone would complain about:** not the absence in the abstract, but the practical consequence of it — that containment here is a matter of a closed door rather than of a ward, and that the closed door is a neighbour's door. ⚠ Write the arrangement, never the outcome; the outcome is the pulse's.

---

## 4. THE PACKET'S OWN VERDICT, for the writer's first pass

**What this pool actually says, stripped to the record:** *a name matching `granar` is on this town's roster, and not one of twelve care-and-faith keywords is.* One thing built against the bad harvest; the whole other column empty. Everything else in the twelve faces has to come from the shape of that asymmetry, from what the catalog names the granary, and from the silence around the sick — never from a quantity, a duration, a course, a history, a forecast or an interior.

**The three faults all three shipped variants share, and which the rewrite must not carry forward:**

1. **The duration.** All three assert that the store answers a YEAR or a HARVEST. `foodSecurity.storageMonths` says five months on a plain-granary town. **This is the pool's one true contradiction and it is in every shipped row.**
2. **The course.** Two of three narrate the sickness running to an end — an elapsed course on a key with no event provenance, in the indicative, on a matter the pulse adjudicates.
3. **The interior.** One of three closes on what the town knows and fears; the FEELING move does not exist.

**What is SAFE and under-used, and where the twelve faces should be spent:** the granary as an ADMINISTERED, COMMUNAL or STATE-MANAGED object named three ways by the catalog; the absence as a SHAPE rather than an abstraction (the whole care column, empty, on a roster rich enough to hold a state granary); the burial provision this range always carries against the care provision it never does; the domestic tending the record is silent about; and the stranger who falls ill with nowhere to be taken.

**The thread to write against:** this paragraph is always LAST and always follows an `Economic Survival` sentence about money, pay or funding. It closes the whole threat block. Carry a noun forward rather than a theme, and do not let all four faces of a variant close on the same kind.

**Twelve faces, three variants, no slot on vid 2.** Nothing in this file is a face.
