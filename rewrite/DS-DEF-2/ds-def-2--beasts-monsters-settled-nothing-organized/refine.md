# DS-DEF-2 · pool `Beasts & Monsters: settled, nothing organized` · REFINEMENT (phase 2, §21.2)

Refiner: Opus 5 (Fable-unvalidated seat), a DIFFERENT author from the drafter, under the Fable chair. Block DS-DEF-2, role **spine**, three variants, four rows each (the numbered line plus three `[face]` sub-rows).
Input: `draft-round-2.md` in this directory (the lawful-but-failing set), the marker's `skeleton.md`, the licence card printed in `laneRW-DEF2`, the pool's current rows in `laneRW-DEF2/docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2679-2691`.
Paste target: under the annex heading whose bold text reads **`Beasts & Monsters`: `settled`, nothing organized**.
The pool's typed lines (ROLE / READS / RELATION / ATTACH / FORM / MOVE and the block's STATE-KEY, SLOTS, SECTION-TARGET, PROVENANCE + FENCE) are untouched and are not repeated. No variant added, removed, merged or reordered; every vid and every angle tag stands exactly as it stands; no `[plain]` marker anywhere; twelve rows in, twelve rows out.

**THE GATE'S NAMED FAILURE, AND THE ONE STATE REMOVED.** The draft was refused on ONE measure: `band depth · shapes.participialOpenerRate (over) = 28.24 band-widths on 1 of 12 face(s)` — vid 3, face index 1, *"Coming on {settlement} out of a quiet country, a stranger passes no works and finds no muster in the town."* The pool's only sentence-initial participial phrase is gone (§N.1 below); **the refined set carries ZERO participial openers on all twelve rows**, and no post-nominal participle opens any sentence either. No failing state is added: the openers, the fronted-PP count, the negated-surface-opener count and the length spread are all held at or inside the draft's own profile (§N.4).

Written under the CHECKPOINT LAW: the paste block landed first, then the notes, face by face.

---

## THE PASTE BLOCK (the complete replacement for the pool's variant rows)

1. `[ledger]` {settlement} keeps no works and no muster against the country, and that country runs low in beasts.
   - `[face]` The beasts about {settlement} are few, and neither works nor muster stands in the town.
   - `[face]` The town of {settlement} is entered with no works and no muster, and the country beyond is entered as quiet.
   - `[face]` Little comes out of the country about {settlement}. Against that country the town holds no muster, and no works stand.
2. `[street]` The town stands without works or muster, and what is outside it is quiet.
   - `[face]` The country outside carries few beasts, and the town facing that country keeps neither works nor muster.
   - `[face]` Beasts are few out in the country. No works stand against that country, and the town keeps no muster.
   - `[face]` What comes out of the country is little. The town holds no works and no muster against that country.
3. `[visitor]` A stranger walks out of {settlement} past no works and no muster, and meets few creatures in the country.
   - `[face]` Out of a country low in beasts a stranger comes on {settlement} and finds no works and no muster in the town.
   - `[face]` No works and no muster meet a stranger at {settlement}, and beyond the town the country shows little.
   - `[face]` Few creatures meet a stranger in the country about {settlement}. The town in that country has no works and keeps no muster.

---

## --- NOTES

One line per face: what changed, and the law or target behind it. The claim set is untouched everywhere — every row still states the whole keyed condition (LOW beast tier over the country · no works · no muster) and nothing wider; no claim is added, none dropped (A6, read across the faces).

### N.0 The three refinement targets this pass worked to (§21.1, §21.2, §21.4)

1. **The gate's one measure removed** — the participial opener (the only failing state; §N.1 v3 face 1).
2. **The pool's four tics cured** — (a) the duplicated fronted frame *"Against that country"* on two rows of two different variants; (b) FIVE of twelve rows landing on the word *muster*, now three; (c) *"about"* three times inside variant 1, now twice; (d) the band word *quiet* on four rows, now two.
3. **The ceiling, not the middle** — the ledger's office formula made the ledger's signature (v1 face 2), the correlative rationed to two rows of twelve, the four faces of each variant pushed further apart in SUBJECT, in DEVICE and in LANDING NOUN, not only in vocabulary (the brief's "the four faces differ in construction").

Nothing in the density floor was traded for plainness (§21.4). Every lawful shipped turn the draft kept is still kept, verbatim: `against the country` and `keeps no` and the band word `quiet` from variant 1; `The town` and `what is outside it` from variant 2; `A stranger`, `walks out of {settlement}` and the verb `meets` from variant 3. **Variant 2's numbered line and variant 3's numbered line stand UNCHANGED** — each is at its ceiling and every candidate rewrite traded density for nothing (§N.2, §N.3); a shipped-lawful clause standing verbatim is the density law's own permission, and the same permission covers a draft clause already at the ceiling.

---

### N.1 THE TWELVE FACES, ONE LINE EACH

**VARIANT 1 · `[ledger]` — the office enters the condition and measures it; it cites nothing and uses no record noun (W24; §0.3 of the skeleton: the muster holder resolves to null on every town this pool fires on).**

| row | change | law / target |
|---|---|---|
| **1 · numbered line** | `and the country runs low in beasts` → **`and that country runs low in beasts`** | the read-aloud ear: `against the country, and the country` stutters at the joint. `that country` carries the noun forward by name instead of restating it (THE THREAD at k = 0, R-i / §1.4.1: a deliberate noun echo is lawful, a stutter is not). The slot opener, `keeps no`, `against the country` and the tier rendering `runs low in beasts` all stand — the shipped line's whole lawful residue (§1.6) and the sharpest LOW rendering the skeleton rules (§0.4). |
| **1 · face 1** | `and the town keeps neither works nor muster` → **`and neither works nor muster stands in the town`** | the landing noun. Five of the draft's twelve rows landed on *muster*; this row now lands on **the town**, the civic noun a later modifier can pick up (R-DA-04, vary the KIND of close; THE THREAD's "leave a civic noun standing"). The correlative is kept — it is the variant's declared device (W4) and the one construction that carries both absences on ONE negated surface (W10) — but it moves from the predicate into the SUBJECT, which is a real construction change, not a synonym swap. |
| **1 · face 2** | `holds no works and keeps no muster, and the country about it is entered as quiet` → **`is entered with no works and no muster, and the country beyond is entered as quiet`** | three things at once. (i) The office's own formula now carries BOTH halves — the register card's explicit licence ("Repeat the office's formula because it is the office's; the WORD may recur, the FACT must not"), and it is the one row in the pool that sounds unmistakably like the office rather than like a description: the ceiling for this angle (R-vi / W7; `entered` is a FORMULA, never a citation and never a record noun). (ii) Two negated verbs collapse to ONE negated surface (W10). (iii) `the country about it` → `the country beyond`: it removes the third `about` in the variant (a tic) AND removes the pool's one W1 exposure — a bare `it` standing after the objects *works* and *muster*, which a refuter applying W1 mechanically binds to the nearer object. No pronoun now stands anywhere in this variant. |
| **1 · face 3** | `the town keeps no muster, and no works stand` → **`the town holds no muster, and no works stand`**; the fronted frame kept here and struck from its twin | the pool's worst tic: `Against that country` opened the second sentence of BOTH v1 face 3 and v2 face 3, verbatim. The frame stays HERE, where it belongs — it is variant 1's own shipped turn (`against the country`) carried into the thread — and v2 face 3 gives it up (see below). `keeps` → `holds` so that this row and v2 face 2 no longer close on the same five words; the row lands on **the works**, the fourth distinct landing noun in the variant. |

**VARIANT 2 · `[street]` — the town's own condition in the town's plain speech; slot-free on all four rows (the round-1 gate refusal, cured in the draft and held here: parent and faces carry the same fill set, A6 / ARCH §2.5).**

| row | change | law / target |
|---|---|---|
| **2 · numbered line** | **UNCHANGED** | it is at its ceiling and every candidate traded density for nothing: 14 words, the pool's shortest row (the street's brevity contract, W4); the privative `without`, the one device no other row in the pool uses; and `what is outside it` standing VERBATIM from the shipped sentence — the only clause of that sentence the card licenses, and the density law's own case for keeping it (§21.4: a lawful shipped clause may stand as one of the four faces). Verified, not neglected. |
| **2 · face 1** | `and the town facing it has neither works nor muster` → **`and the town facing that country keeps neither works nor muster`**; two sentences folded to one | the pronoun goes (W1: `it` stood after the object *few beasts*), the noun is carried forward by NAME, and `has` → `keeps` puts the street's own verb on the muster. The row is one sentence again so that variant 2 runs 1 · 1 · 2 · 2 in sentence count rather than 1 · 2 · 2 · 2 — rhythm follows load across the pool (R-DA-05), and the short line stays the street's. |
| **2 · face 2** | `The town has no works against that country and keeps no muster` → **`No works stand against that country, and the town keeps no muster`** | the second sentence no longer opens on *The town* (four rows of the draft's twelve opened a sentence that way). It opens instead on a negated surface — which W10 expressly rules is NOT an absence opener, so MOVE-GRAMMAR wall 3 does not bite — and this is variant 2's ONE such opener, inside W10's per-variant cap. Two negated verbs collapse to one compound subject plus one predicate. |
| **2 · face 3** | `Against that country the town holds no works and no muster` → **`The town holds no works and no muster against that country`** | the tic cure: the duplicated fronted frame is struck here, leaving variant 1 face 3 as the pool's only fronted `Against that country`. The region token moves to the CLOSE, where it is both the landing noun and the thread's carried noun (R-i) — the row now ends on the civic thing the read is over rather than on the absence, which is the fourth distinct close in the variant. The cleft (`What comes out of the country is little`) is untouched: it is the variant's declared construction (W4) and the pool's only cleft. |

**VARIANT 3 · `[visitor]` — the eye of a stranger at the approach; it sees and finds, and never acts on the town, decides, is told, is named or is counted (W27).**

| row | change | law / target |
|---|---|---|
| **3 · numbered line** | **UNCHANGED** | it is at its ceiling and carries THREE shipped turns verbatim — `A stranger`, `walks out of {settlement}` (the slot mid-sentence, so this is not a second settlement opener, wall 10) and the verb `meets`, completed lawfully with `few creatures` where the shipped line had the unlicensed `nothing`. Every rewrite tried here spent one of those three to gain nothing the card licenses; the density floor refuses that trade (§21.4). Verified, not neglected. |
| **3 · face 1** | `Coming on {settlement} out of a quiet country, a stranger passes no works and finds no muster in the town.` → **`Out of a country low in beasts a stranger comes on {settlement} and finds no works and no muster in the town.`** | **THE GATE'S ONE FAILING STATE, REMOVED.** The sentence-initial participle (`Coming on …`) becomes a fronted PREPOSITIONAL frame — no participle anywhere in the row, and none anywhere in the pool. The pool's fronted-PP count is held at TWO (this row and v1 face 3), exactly the draft's count, so nothing is traded for the cure. Three further gains ride with it: the verb count drops from three to two (no habitual triad, R-DA-10); the band word `quiet` drops out here, taking the pool's count from four rows to two; and the tier is rendered as `a country low in beasts`, the skeleton's `low` reading (§0.4), which no other row uses. Lands on **the town**. |
| **3 · face 2** | `Neither works nor muster meets a stranger at {settlement}, and beyond the town the country carries few beasts.` → **`No works and no muster meet a stranger at {settlement}, and beyond the town the country shows little.`** | the correlative is rationed: with v1 face 1 and v2 face 1 both carrying `neither … nor`, a third made it the pool's habit (R-DA-10: never reach for the same device by habit). `No works and no muster` as the subject keeps the construction — the absences as what the eye does not meet — while changing the device. `carries few beasts` → `shows little`: `carries` moved to v2 face 1, and `shows` is the visitor's own verb for what is and is not there to be seen; the country is the subject of a plain non-intentional verb, not a sense verb on an abstraction and not an inanimate acting with intent (R-DA-11). Lands on **little**, a close no other row takes. |
| **3 · face 3** | `The town in that country shows no works, and keeps no muster.` → **`The town in that country has no works and keeps no muster.`** | `shows` moves to face 2 where the visitor's eye is the subject; here the town simply stands as it stands, which keeps the town out of any presentational act toward the stranger (W27: the stranger may see; it is never shown, told or admitted). The first sentence is untouched — it inverts the numbered line's clause (the creatures as subject, not the stranger), which is the variant's fourth construction, and it carries `country` forward by name into the second sentence (R-i). |

---

### N.2 THE TWO UNCHANGED ROWS, RECORDED AS A DECISION AND NOT AN OMISSION

§21.2 says a lawful face that sits just above the floor is pushed toward the ceiling, and §21.4 says a refinement that made a lawful line plainer with no law behind the change is the regression. Variant 2's numbered line and variant 3's numbered line were each worked and each returned to their draft wording, for the same reason: every candidate spent a clause the SHIPPED sentence gave the corpus and the card licenses — `what is outside it` on one, `A stranger` / `walks out of {settlement}` / `meets` on the other — and bought nothing licensed with it. The candidates are recorded so the chair can veto the judgment:

- v2 numbered line: `and the country outside it is quiet` (spends the shipped `what is outside it`); `and what is outside it is a quiet country` (adds three words to the pool's one short row and loses the brevity contract, W4).
- v3 numbered line: `past no works and no one under arms` (a totality over persons — a REFUSED COLUMN on the card, and W22); `in the country beyond the town` (closes on an adverbial and duplicates face 2's `beyond the town`); splitting the row into two sentences (spends the shipped one-sentence movement and puts variant 3 at three two-sentence rows).

A face that is already at the ceiling keeps its wording; that is the refinement's answer, not its absence.

---

### N.3 CLAIM EQUALITY, VERIFIED ROW BY ROW (arm A6, read ACROSS the faces)

Each of the twelve rows asserts exactly three things and no fourth: the country's beast tier is LOW · no works stand · no muster stands. The renderings, one per row, all distinct:

| row | READ 1 (the LOW tier, country-scoped, W2) | READ 2 (no works, A-7's class word) | READ 3 (no muster, item 5's class word) |
|---|---|---|---|
| 1 parent | `that country runs low in beasts` | `keeps no works … against the country` | `no muster` |
| 1 f1 | `The beasts about {settlement} are few` | `neither works …` | `… nor muster stands in the town` |
| 1 f2 | `the country beyond is entered as quiet` | `entered with no works` | `and no muster` |
| 1 f3 | `Little comes out of the country about {settlement}` | `no works stand` | `the town holds no muster` |
| 2 parent | `what is outside it is quiet` | `stands without works …` | `… or muster` |
| 2 f1 | `The country outside carries few beasts` | `keeps neither works …` | `… nor muster` |
| 2 f2 | `Beasts are few out in the country` | `No works stand against that country` | `the town keeps no muster` |
| 2 f3 | `What comes out of the country is little` | `holds no works …` | `… and no muster` |
| 3 parent | `meets few creatures in the country` | `past no works …` | `… and no muster` |
| 3 f1 | `Out of a country low in beasts` | `finds no works …` | `… and no muster in the town` |
| 3 f2 | `the country shows little` | `No works …` | `… and no muster meet a stranger` |
| 3 f3 | `Few creatures meet a stranger in the country about {settlement}` | `has no works` | `keeps no muster` |

Fills: variant 1 `{settlement}` on all four rows · variant 2 NONE on all four rows · variant 3 `{settlement}` on all four rows. Every face's slot set equals its parent's (ARCH §2.5; the round-1 refusal stays cured). `{band}` and `{route}` are never named. Only variant 1's numbered line opens on the slot (wall 10; R-DA-17); no `[face]` sub-row opens on a `proper`-typed slot (T-F8).

---

### N.4 WHAT THE REFINEMENT DID **NOT** MOVE (no new failing state — §21.2, "as many failing states removed as one effort allows, never a new one added")

- **Participial openers: 1 → 0.** The one sentence-initial participle is gone. No row and no second sentence opens on a participle; the two post-nominal participles (`the town facing that country`) sit mid-sentence, after a capitalised article, where an opener metric cannot reach them.
- **Fronted prepositional openers: 2 → 2.** Draft: v1 f3 s2 and v2 f3 s2. Refined: v1 f3 s2 and v3 f1. The count is held exactly; the DUPLICATE (`Against that country` twice, verbatim) is what went.
- **Negated-surface sentence-openers: 1 → 2, one per variant.** Draft: v3 f2 only. Refined: v2 f2 s2 and v3 f2. W10 caps these at one per VARIANT and licenses them outright (a negated surface on a present read is not the ABSENCE move, so MOVE-GRAMMAR wall 3 does not apply); v1 carries none.
- **Length.** Draft 14 · 15 · 19 · 20 | 14 · 15 · 19 · 19 | 19 · 20 · 18 · 22, mean 18.1. Refined 17 · 15 · 20 · 20 | 14 · 17 · 19 · 19 | 19 · 22 · 18 · 22, mean 18.5. Range 14 to 22 both times; the street still carries the short row and the visitor the long ones (R-DA-05, rhythm follows load).
- **Sentence count.** Draft 8 one-sentence rows and 4 two-sentence; refined 8 and 4. The mix moved variants (v2 f1 folded to one, nothing folded out), the total held.
- **Opener words, first two.** Numbered lines `{settlement} keeps` · `The town` · `A stranger` — distinct (A11), unchanged. Within each variant: v1 `{settlement} keeps / The beasts / The town / Little comes`; v2 `The town / The country / Beasts are / What comes`; v3 `A stranger / Out of / No works / Few creatures`. The consecutive-row first-word profile is identical to the draft's (rows 2–3 and 5–6 share only the article `The`, exactly as before).
- **The walls, all of them, re-checked on all twelve rows.** No em dash · no exclamation · no question · no digit and no percent in the prose · no `, which` · no colon · no semicolon · no fragment (FORM is `sentence`) · no second person · no persona · no future indicative · no perfect tense · no expletive (`there is` / `it is`) · no `[plain]` marker · exactly one bracketed angle tag per numbered line, carried over unchanged · exactly three `[face]` sub-rows per variant.
- **Landing nouns spread.** Draft: *muster* closed five of twelve rows. Refined: *muster* ×3, *town* ×2, *quiet* ×2, *country* ×2, *beasts* ×1, *works* ×1, *little* ×1. Close KINDS: condition ×6, absence ×4, object ×2. No row closes on a pronoun (`closers.pronounRate` = 0).
- **The band word.** `quiet` fell from four rows to two (v1 f2, the office's entry; v2 parent, the shipped street turn). It is still the corpus's own word for this tier and is never widened to *safe*, *empty*, *peaceful* or *at peace*.
- **Pronouns.** Three in the pool, every one binding to the subject of its OWN clause, none crossing a sentence boundary: `what is outside it` (v2 parent, `it` = the town, the shipped phrase), and that is now the only bare `it` in the set — the draft's `the country about it` (v1 f2) and `the town facing it` (v2 f1) both took the noun by name instead (W1).

---

### N.5 THE BARS, RE-WALKED ON THE REFINED TEXT (W1–W27; a breach is a FAIL with the bar named, so each is named here as clear)

- **W1 possessor/pronoun binding** — one pronoun left in the pool (above); no possessive anywhere; every cross-sentence reference is by name (`that country`).
- **W2 country-scope** — every tier claim is stated over the country (`that country`, `the country outside`, `out in the country`, `the country about {settlement}`, `a country low in beasts`, `beyond the town the country`); no row states the tier as a totality over the town. War and the other pressures on this page (C7) are never touched.
- **W3 band marker** — no tier band word is rendered here (this key is not a band read); `{band}` is RESERVED and unnamed.
- **W4 construction contract** — variant 1 is the office entering and measuring; variant 2 is the town's plain speech at the pool's shortest lengths; variant 3 is the stranger's eye. No variant moved onto a sibling's construction; the devices that were swapped (the correlative, `shows`, `carries`) moved BETWEEN faces of the pool, never across an angle's stance.
- **W5 naming form** — `The town of {settlement}` on v1 f2 is the naming form; `the town` is the plain form elsewhere; no generic-article maxim frame anywhere (`a country low in beasts` is a fronted PP naming the specific country the stranger comes out of, not a band carried on a generic article).
- **W6 no doubled beat** — no clause restates the read it follows; the draft's summarising risk was already out.
- **W7 / R-vi the roll** — `is entered` / `is entered as` twice on v1 f2 is the OFFICE'S OWN FORMULA, and it is the only formula in the pool. No row makes a record an agent-source (`the roll shows`, `the books set down`) and no row cites anything.
- **W8 the angles** — the ledger enters and measures and cites nothing; the street states the standing plainly (no field on this block holds what the town says or does); the visitor reports what is seen and never an accounts fact or a town act.
- **W9 entailments are may** — the density floor is counted on the CARD'S THREE READS, all three present on all twelve rows; nothing is entailed from a noun.
- **W10 negated surface** — see §N.4; one per variant at most, and none is the ABSENCE move.
- **W11 the material bar** — no material and no material source anywhere: no stone, no timber, no `{defmaterial}`, no wall-source clause. The class word `the works` / `what the town has built` is the only wall-bucket spelling used, and no row's grammar breaks on either `{defwork}` literal.
- **W12 garrison / W13 watch** — neither word appears, present or denied, on any of the twelve rows. Every town-tier instance of this pool carries a required `Town watch` standing (skeleton §0.2), so the shipped `would justify a watch` was a breach on the record; the refined set does not reintroduce it and does not deny a watch either.
- **W14 labels at their engine meaning** — `settled` is rendered as the LOW beast tier of the country and never as *heartland*, *founded*, *the centre of a realm*, *safe* or *no threat*; `plagued` is a different row and is not touched.
- **W15 aliases** — `the works` (A-7), `the muster` (item 5), `the country` / `the country outside` (A-12), `the town`, `beasts` / `creatures` (the row's own class). Never *the wall*, *the palisade*, *the line*, *the gate*, *the militia*, *the guard*, *the community*, *the soldiers*, *the rolls*.
- **W16 the gate's word** — no upkeep, pay, wage or purse word: this pool reads no economic gate.
- **W17 crisis / W18 stress / W19 faith** — no siege, occupation, war, famine, illness, stressor or creed word anywhere.
- **W20 the layer bar** — the country and the town are NONE-layer tokens; `the works` and `the muster` are BODY-ABSENCES of exactly the three buckets the read consults (walls · garrison · militia); no body word from a bucket the read never consults enters; nothing is named on an aggregate or a label.
- **W21 baked power** — no hall, council, chamber or seat.
- **W22 the person bar** — no person as agent, decider, holder or referent; no *somebody*, *whoever*, *nobody*, *no one*, *the clerks*; the force absence is written on the CLASS WORD (`no muster`) and never on people. `a stranger` is the visitor's eye and is never named, told, admitted, questioned, counted or given an act on the town.
- **W23 the fused-agent bar** — the town never decides, needs, thinks, knows, fears or remembers; `facing` and `in that country` are standing spatial relations, not postures; no relation is asserted between two referents no field joins.
- **W24 the record-word bar** — NO record noun on any row (no roll, rolls, books, register, count, accounts, entry) and no citation, because the card's `muster` holder resolves to null on every town this pool fires on (skeleton §0.3). `[ledger]` is treated as a STANDPOINT that licenses no citation; `entered` is the office's formula, which the bar expressly permits.
- **W25 external body** — none.
- **W26 visibility** — `covert: no`, `audience: player (no mark)`; no row carries a dm-only mark; no state organ is cited on any face.
- **W27 the angle-tag bar** — `[ledger]`, `[street]` and `[visitor]` are stances; none is turned into a body, a holder or a record.

---

### N.6 SIBLING DISTANCE (A1 / A11 — the neighbouring pools on this block, re-checked after the edits)

- `Beasts & Monsters: settled, defenses beyond the need` (`:2669-2677`) states the works PRESENT against the SAME tier. The refined set borrows none of its wordings: not `the country around the town`, not `little troubles the country`, not `a quiet one`, not `carrying little in the way of beasts`, not `What a stranger sees at {settlement} is …`. Two edits of this pass were made specifically to hold that distance (v3 f2's `shows little` rather than the sibling's `carrying little`; v3 f1's fronted PP rather than the sibling's cleft).
- `Invasion & War: neither walls nor force` reads the same two buckets against a different pressure: every row here names the beast class or the country's quiet, so no row of this pool can be read as the war row's sentence.
- `DS-DEF-5`'s six-bucket key owns *no contracted, chartered or arcane body stands*; this key consults three buckets, so no row says *nothing organized*, *no defense*, *nobody armed* — the shipped variant 1's `organized defense` stays dropped.

---

### N.7 REFUSALS

**None.** All twelve rows are refined in place and all twelve are lawful under the card, the skeleton, the two ratified law tables (ADDENDUM 13 parts A and B, W1 to W27) and the voice. Nothing is banked.

Two matters carried forward unchanged from the draft and recorded again so they are not re-found as bugs:

1. **The provenance move is a deliberate ZERO on this pool**, not an omission: the card licenses a citation of the `muster` holder "where the provenance budget allows", but this row fires only on `force === false`, so no `Citizen militia` with a `Muster training` service stands, the census row prints `holder: null`, and W24 forbids a record word on a read that resolves no holder. This is the card's own arithmetic (§24's ceiling never binds because the budget is zero here).
2. **Variant 1's shipped contrast (`rather than a gap`) is gone and is not replaced**: it named no sibling pool key and no sibling band, so R-DA-02 removes it; the sibling that would license a contrast (`plagued`) is a different value of `config.monsterThreat`, and naming it would assert as a second fact that this town's country is not plagued.

---

**STATUS: COMPLETE.** Twelve rows refined one for one (three numbered lines, nine `[face]` sub-rows); ten rows re-worded, two returned to their draft wording as a recorded decision (§N.2); every vid and angle tag unchanged; no variant or face added, removed, merged or reordered; the gate's one named failing state removed and no new one added. Nothing outside this file was written; no dock was entered, edited, committed or tested; the only command run in `laneRW-DEF2` was the read-only licence-card script.
