# DRAFT ROUND 2 — DS-DEF-2 · pool `Internal Security: court without detention`

Writer: Opus 5 (WRITER seat, Fable-unvalidated), for the Fable chair. REWRITE car 8b, block DS-DEF-2, draft round 2.
Dock `laneRW-DEF2` read only; the only execution was `node scripts/prose-licence-card.mjs DS-DEF-2 'Internal Security: court without detention'`.
Written under the checkpoint law; nothing outside this packet file was written anywhere.

Three variants rewritten IN PLACE under their own numbers and their own angle tags (vid 1 `[ledger]` · vid 2 `[street]` · vid 3 `[unfolding]`); no variant added, removed, merged or reordered. Four faces per variant (the numbered row is face 1; the three `- [face]` sub-rows are faces 2 to 4), twelve faces in all. The typed lines of the pool (ROLE, READS, RELATION, ATTACH, FORM, MOVE) are untouched and not repeated here.

**Row form.** The angle tag and the `[face]` marker are backticked, matching the pool's rows as they now stand in the dock's annex (`RECEIPT_POOLS_DOSSIER_STATE.md:2782-2794`) and ARCH §2.5's face-row example. Strip the backticks if the projector's `FACE_ROW_RE` wants bare brackets; nothing else in the rows changes.

**WHAT MOVED FROM ROUND 1 (the gate's one failing owned measure, answered).** The gate's feedback on round 1 was a single measure: `band depth · shapes.participialOpenerRate (over) = 13.62 band-widths on 1 of 12 face(s)`, against an ENTRY-grain depth ceiling of 1.75 band-widths (Part B §16.2) and R-DA-18's floor (participial 0.0117, band edge ≤ 0.020). The one offending face is **variant 1, face 2**, which opened on the past participle "Entered" (`Entered at {settlement} is formal law, …`); it is the only face-initial participle in the pool, which is why the instrument counted one face and not two. It is rewritten below on a subject-first entry formula. **After the change the pool carries ZERO face-initial participles, so the measure reads 0 of 12 faces and the depth on it is 0 band-widths.** Every other face is byte-identical to round 1: no lawful face is touched, because §21.2 keeps a lawful set only while it stays lawful, and nothing else was reported failing. One round-1 arithmetic error in the notes is corrected (§N.4: three two-sentence faces, not four) and one refuter-facing fence is recorded (§N.7 row 6).

---

## THE ROWS — ready to paste under the pool's heading

**`Internal Security`: court without detention**
1. `[ledger]` Formal law stands entered at {settlement}, and the town holds no place of confinement.
   - `[face]` The town of {settlement} is entered with formal law, and has no gaol under that law.
   - `[face]` Law at {settlement} is formal in its standing. Beside that law the town carries nothing to hold under.
   - `[face]` What {settlement} keeps is formal law, and it keeps no gaol.
2. `[street]` The town's law is formal, and the town keeps no gaol.
   - `[face]` Here the law is formal, and there is nowhere in the town to hold under that law.
   - `[face]` This town's law is formal. Beneath that law the town carries no place of confinement.
   - `[face]` Law here is formal, and no gaol stands under that law.
3. `[unfolding]` Law is formal at {settlement}, and its holding half stands open.
   - `[face]` Under formal law at {settlement}, detention stands unmet.
   - `[face]` At {settlement} the law is formal. Holding under that law is the half not in place.
   - `[face]` The law {settlement} keeps is formal, and a place of confinement under it is wanting.

---

## --- NOTES

### N.0 The claim set, once, for all twelve faces

Every face of every variant carries EXACTLY these two claims and no third:

| claim | the card clause that licenses it |
|---|---|
| C1 — the town keeps FORMAL law | `reads: court` · `predicate: court truthy (no literal)` · `may claim: that court (truthy) holds, as a STANDING fact of the record`. The word "formal" is the ENGINE'S OWN adjective for this flag (`const hasFormatLaw = inst.hasCourtSystem \|\| inst.hasPrison`, `safetyProfile.js:65-66`); on this pool the court limb alone satisfies it, so "formal" is read off `court` and off nothing else. The referent table (row 20, ratified) gives "the town's law" / "the law" as the read's own safe FUNCTION word, never an agent. |
| C2 — the town has NO place of confinement | `reads: prison` (the card's `reads:` line carries it; the branch is `else if (f.hasCourtSystem)` after `hasCourtSystem && hasPrison` failed, `threatAssessment.js:144-147`) · the ABSENCE move class (a) LACK, a `none-exists` world field (MOVE-GRAMMAR §1.2 row 11) · the card's `absent ⇒ no candidate` line. Lawful nouns per A-17 / referent row 21: "a place of confinement", "a gaol", "a prison". |

The two reads are ONE keyed condition (R-v / ADDENDUM 8 ruling 1), which is what licenses the single joint that carries both in one sentence, or the two-sentence form with the thread carried on "law". No face adds a cause, a second fact, a count, a standpoint, a forecast, a totality or a citation.

**Dropped from every shipped sentence** (unlicensed under the card's `may NOT`, and under the ratified tables; each is the corpus's known state, never a floor): a running process ("tries", "can name a wrong", "judgment") — D-15 makes it CONDITIONAL on a `Courthouse` row this flag read cannot see, W14 names `hasCourtSystem` as every town's meeting hall; a totality over persons ("anyone", "the person who did it") — a REFUSED COLUMN and W22; sentencing kinds ("money and exile", "the purse or the road") — a second fact, NOT ENTAILED by D-15, and "the purse" and "the road" are HOLDER words on a read that resolves no holder (W24, referent row 49 and the road kind); a cause ("so") — the card's `may NOT`; the law as an agent ("reaches for") — R-DA-11 and W23, the very clause the referent table names as the fault; a verdict ("fall unevenly") — a standpoint; a history and a forecast ("Each judgment … costs the next one", "spending down a reputation they cannot replace") — R-DST-B, D-F21, STATE NEVER FATE; "the town's courts" as a body — D-F21 HOLDS, A-16, W20.

**Density floor honoured.** The one shipped noun phrase the ratified tables call safe, "**The town's law**", stands VERBATIM as the opener of variant 2's face 1 (the canonical-at-zero row). The shipped verb "keep" survives the re-seating in v2 ("keeps no gaol") — re-seated on the THING, because "cannot keep" as an inability reading would add a capability claim the card does not license, and "cannot keep anyone" is the totality the column refuses. Every other shipped clause carries "tries" / "anyone" / the sentences / the verdict / the decline and is dropped by construction, not smoothed: ADDENDUM 6 makes only a clause LAWFUL under the card a floor. **Round 2 adds no plainness:** the one face it re-cuts keeps the office's entry formula in its main verb and trades a fronted participle only for a subject-first naming form (W5), so the change is licensed by a measured band and is not a density trade (§21.4).

### N.1 Variant 1 `[ledger]` — face by face

Angle: the office ENTERS the two flags as it sets them down, in its own formula ("entered", "stands", "carried standing" — R-vi / W7), and cites NOTHING (`source: (none) · SOURCE-UNRESOLVED`; a citing face is refused by arm A13; W24 bars "roll", "record", "books" outright, and the `[ledger]` tag is a STANDPOINT that licenses no record noun).

| face | words | construction (subject · order · landing) | C1 licensed by | C2 licensed by |
|---|---|---|---|---|
| 1 (numbered) | 14 | subject "formal law"; law → lack; lands on the absence noun "confinement"; office formula "stands entered" | `predicate: court truthy`; "formal" from `hasFormatLaw` | `reads: prison` falsy, stated flat on the thing after the presence (wall 3) |
| 2 (RE-CUT this round) | 16 | subject is the TOWN in its naming form ("The town of {settlement}", W5); passive entry formula on the law, active lack on the town; lands on "law" | as above; "is entered with formal law" is the office's own entry, no record noun and no source | as above; "has no gaol under that law" states the LACK on the thing as a world fact, not as a silence of the record (ABSENCE class (a), never class (b)'s GAP, which would need a `not-held` provenance field the card does not carry) |
| 3 | 18 | TWO sentences, thread carried on the noun "law" (R-i); subject shifts to "the town" in the turn outward, placed last; lands on an ABSENCE close | as above | as above; "nothing to hold under" — the object elided so no person is a referent (W22) |
| 4 | 11 | pseudo-cleft ("What {settlement} keeps is …"); the short line; lands on "gaol" | as above | as above |

Slots: `{settlement}` exactly once in each face, per the leaf's `slots: ["settlement"]` for vid 1; `{band}` and `{route}` named nowhere.
Four distinct constructions inside the variant (ADDENDUM 7 rule 4; W4 keeps the variant on the ledger's grammar): the law as subject · the town as subject · two sentences with the turn last · the cleft. Face 2's verbs ("is entered with" · "has no") are the two the variant's other faces do not use ("stands entered"/"holds" · "is formal in its standing"/"carries" · "keeps"/"keeps"), so the re-cut face is not a paraphrase of a sibling in vocabulary or in rhythm.
Regression guards cleared (skeleton §1.7): not an inventory line (the entry formula and the engine's own adjective carry the angle, never "has a court and no prison"); both reads present in every face; the angle is the office's formula and not a citation; no restored breach; the lack never opens and is never "but"-completed; no digit, em dash, which-tail or third sentence.

### N.2 Variant 2 `[street]` — face by face (unchanged from round 1)

Angle: the same two flags as the town meets them in use, in the town's own plain terms and the present tense, WITHOUT a slot. The street invents nothing the town does instead, no person the law acts on, no reliance, no habit, no belief frame, no cause, and never the law as an agent.

| face | words | construction (subject · order · landing) | notes |
|---|---|---|---|
| 1 (numbered) | 11 | subject "the town's law", the shipped noun phrase verbatim; the town as the second subject; lands on "gaol" | the density floor: the one noun phrase the referent table calls safe (row 20 / A-16) kept as written |
| 2 | 17 | existential second clause ("there is nowhere …"); lands on "law" | the pool's ONE expletive construction, and it is NOT an opener, so `thereIsOpenerRate` (R-DA-07, band ≤ 0.020) reads zero across the pool |
| 3 | 15 | TWO sentences; the second fronts the adverbial ("Beneath that law …"), carrying the noun forward (R-i); lands on "confinement" | "This town's" seats the possessor on the town, never on the law (W1) |
| 4 | 11 | subject of the second clause is the ABSENT THING itself ("no gaol stands under that law"); lands on "law" | the only face whose second clause takes the lack as its grammatical subject; it still does not OPEN on it (wall 3) |

Slots: NONE in any face, per the leaf's `slots: []` for vid 2 (ARCH §2.5: a face's slot set must equal its parent's). The town is "the town" / "this town" / "here".
Construction contract (W4) kept: the street's grammar is the town's own plain statement, never variant 1's entry formula and never variant 3's OPEN move.

### N.3 Variant 3 `[unfolding]` — face by face (unchanged from round 1)

Angle: PRESENT → OPEN (V6). The presence is stated first, then the matter that stands open in this town NOW, declarative, never asked, never narrated as a trend. No "each time", no "goes on", no widening, no cost to a next judgment, no reputation, no future.

| face | words | construction (subject · order · landing) | the OPEN realisation |
|---|---|---|---|
| 1 (numbered) | 11 | subject "law"; possessive seated on the law ("its holding half"); lands on the condition "open" | "its holding half stands open" |
| 2 | 8 | the presence fronted as a prepositional adverbial (not a participle); the short line; subject is the engine's own word "detention" | "detention stands unmet" — `threatAssessment.js:147`'s own branch reading is "limited detention" |
| 3 | 16 | TWO sentences; the second is an inverted predicate nominal on a GERUND SUBJECT, thread carried on "law"; lands on "place" | "the half not in place" |
| 4 | 15 | relative-clause subject ("The law {settlement} keeps"); lands on the condition "wanting" | "a place of confinement under it is wanting" |

Slots: `{settlement}` exactly once in each face, per the leaf's `slots: ["settlement"]` for vid 3.
No face narrates a decline, a habit, a cause or a future; no "the court(s)", no person, no reputation, no weight, no belief frame.

### N.4 The pool-level checks (re-run on the round-2 rows)

- **Sibling distance (A11 / A5).** The three numbered rows open on "Formal law" · "The town's" · "Law is" — no two share their first two words. Across all twelve faces the first-two-word openers are: `Formal law` · `The town` · `Law at` · `What {settlement}` · `The town's` · `Here the` · `This town's` · `Law here` · `Law is` · `Under formal` · `At {settlement}` · `The law` — twelve distinct pairs (`The town` · `The town's` · `The law` differ in their second token).
- **THE PARTICIPIAL OPENER (the gate's one failing measure).** Face-initial tokens after the re-cut: `Formal` (adjective) · `The` · `Law` · `What` · `The` · `Here` · `This` · `Law` · `Law` · `Under` · `At` · `The`. **Zero participles; the measure reads 0 of 12 faces.** The pool's only sentence-initial `-ing` word anywhere is the GERUND SUBJECT "Holding" in the SECOND sentence of variant 3 face 3, which the instrument does not count (see §N.7 row 6, recorded for the refuters and the chair).
- **The settlement token as opener (wall 10 / R-DA-17).** No face opens on the bare token. One face (variant 3, face 3) opens on the adverbial "At {settlement}"; variant 1 face 2 names the token inside the licensed naming form "the town of {settlement}" (W5) and does not open on it; no two token-fronted faces sit adjacent.
- **W10 (a negated surface).** Exactly one negation per face, and in every case it is the prison read — a genuine `none-exists` LACK, never a negated surface on a present read.
- **Pronoun closers (R-DA-04, band ≤ 0.055).** Zero faces close on a pronoun; the re-cut face closes on "law", as the face it replaces did. The close kinds vary across the pool: an object (`gaol`, `confinement`, `law`), a condition (`open`, `unmet`, `wanting`), an absence (`nothing to hold under`), and the civic noun of the read (`place`).
- **Length spread (R-DA-05).** 8 to 16 words per face, 14 · 16 · 18 · 11 / 11 · 17 · 15 · 11 / 11 · 8 · 16 · 15; the short line exists (v3 face 2 at 8; v1 face 4 and v2 faces 1 and 4 at 11). **THREE** of the twelve are two-sentence units and nine are one sentence — round 1's note said four, which was an arithmetic error in the note only; the rows were and are three (v1 face 3, v2 face 3, v3 face 3).
- **Form walls.** No em dash, no exclamation mark, no question mark, no digit or percent, no `which`-clause, no third sentence in any face, no citation, no second angle tag, no `[plain]` marker anywhere.
- **The thread (R-i / §1.4.1).** Every two-sentence face carries the noun "law" forward into its second sentence; every one-sentence face whose second clause changes subject places that shift LAST and hands back a civic noun of the read. The re-cut face 2 carries "law" across its own joint ("formal law … under that law"), which §1.4.1 makes lawful outright (a deliberate noun echo for the thread). Every face lands on a standing fact of a civic thing, so it reads as well immediately after the spine's own opening as it does before the one unknown modifier this site's `k: 1` permits.
- **Same-page siblings (C7).** No face says anything about order, crime, the watch, the seat, the tier, the safety label or a capture rung, so nothing here can contradict DS-DEF-3, DS-DEF-4 or DS-DEF-5 on the same page; and no face restates the full-legal-chain sibling ("the town's law … and a place of confinement") or the detention-without-process sibling. The contrast with detention is never fronted as a subject and never closes more than one variant; in fact no face states it as a contrast at all — the lack is stated flat on the thing (R-DA-02, wall 5). The re-cut face's nouns ("formal law", "a gaol") are the same alias-safe family the neighbouring Internal Security pools already use in the dock's annex, so the four rows of the block read as one vocabulary.

### N.5 Writer bars, checked one by one (re-run over the round-2 rows)

W1 possessor binding — every possessive is seated where it belongs: "The town's" / "This town's" on the town; "its holding half" / "its standing" on the law, in each case immediately after a clause whose subject IS that thing; the re-cut face 2 carries no possessive pronoun at all and names its referent ("that law") rather than leaning on "it". W2 country-scoped threat — no threat read on this pool. W3 band marker — no band read; no tier word anywhere (the pool fires only at town and up, but tier is not a read on this card). W4 construction contract — three distinct grammars, one per variant, kept through all four faces of each; the re-cut face stays on the ledger's entry formula and does not borrow the street's plain statement or the unfolding's OPEN move. W5 naming-form article — "the town of {settlement}" is the licensed naming form and is the re-cut face's subject; no maxim frame; no generic article carries a band. W6 no doubled beat — no clause restates the read it follows. W7 the roll — the office's formula ("entered", "stands", "is entered with") is used; "the roll", "the books", "the record" appear nowhere, and no face makes a record an agent-source. W8 the angles — the unfolding realises OPEN after the presence; the ledger enters and cites nothing; there is no `[visitor]` variant in this pool. W9 entailments are may — the density floor is counted on the card's two reads, not on an entailment. W10 — one negation per face, on the LACK. W11 material bar — no material word (no wall read on this pool). W12 garrison bar — no force word anywhere. W13 watch bar — "the watch" appears in no face; D-F4 is the shipped breach on this pool's full-chain sibling and is not imported here. W14 label bar — `hasCourtSystem` is rendered at its ENGINE meaning (formal law from the flag), never as "a court"; `hasPrison` falsy is rendered as the absence of a place of confinement, never as "no cells" or "no stocks". W15 alias bar — "the town's law" / "the law" is the read's own safe function word (A-16, referent row 20); "a place of confinement" / "a gaol" is A-17's always-safe spelling; no "the court", "the hall", "the council", "the magistrate", "justice", "an assize". W16 the gate's word — no pay-gate read on this pool. W17 crisis bar — no crisis read. W18 stress-record bar — no stress read. W19 faith bar — no faith read. W20 layer bar — every noun sits at the layer of its read: the READ is BODY at the FLAG grain, so the law is named through its FUNCTION word and the missing thing through the class's own noun; no AGGREGATE or NONE read is given a body word. W21 baked-power bar — no "hall", "council", "chamber", "seat". W22 person bar — no person as agent, decider, permission-holder or referent; every object of "hold" is elided or is the THING. W23 fused-agent bar — one referent per unit; the only relation asserted between the two reads is the KEY's own (`internalRowPoolKey(court, prison)`), which R-v makes one keyed condition. W24 record-word bar — SOURCE-UNRESOLVED, so no record word at all; the re-cut face's "is entered with" is the office's own formula and names no record noun; the `[ledger]` angle is a standpoint and licenses none. W25 external-body bar — no foreign force. W26 visibility bar — `covert: no`, `audience: player (no mark)`; no face names anything a captured organ would be needed to admit, and no face cites anything. W27 angle-tag bar — the three tags are STANCES and none of them is read as a body; "the elders", "a stranger" appear nowhere.

### N.6 Refusals

**NONE.** All three variants are written, lawfully, under their own numbers and their own angle tags; no variant is banked as a refusal row, and no face is written that a law refuses. The round moved the gate's one failing owned measure (participial opener: 1 of 12 faces at 13.62 band-widths → 0 of 12 at 0), so it is not a dry round.

### N.7 Fences carried forward (recorded vetoably, not decided here)

1. **The prison limb is a READ.** The card's one-row `predicate:` line prints `court truthy` alone, but `prison` is on the card's `reads:` line and the key function's branch is reached only after `hasCourtSystem && hasPrison` fails (`threatAssessment.js:144-147`). Every face states BOTH reads on that reading. If the chair rules the prison limb is NOT a read of this pool, the twelve faces lose their second clause and the pool becomes a one-read pool; the rewrite would then need re-drafting, not trimming.
2. **"The town's law" is not "another civic object of the class `law`".** The card's `may NOT` bars another civic object of the class, whose token list is `court · prison · gaol · law · justice · magistrate · assize` (`wiringCensus.js:1311`). The marker reads the bar as naming objects OTHER than the reads, and the referent table lists "the town's law" as the court read's own safe function word. Every face in this packet rests on that reading. A refuter who reads the class list literally would strike the only lawful noun the ratified tables give this pool, and the pool would have no writable surface at all — the chair is asked to say which reading binds.
3. **The OPEN move on a `none-exists` lack.** MOVE-GRAMMAR row 10 licenses OPEN on a state field whose value is unresolved / contested / pending / UNMET. The marker reads the missing second half of the chain as "unmet" in the engine's own sense ("limited detention", `threatAssessment.js:147`; `Court only` against `Court + Prison`). Variant 3's four faces are written on that reading. If the chair refuses it, variant 3 realises V3 (PRESENT → LACK) instead and its four faces need their OPEN realisations ("stands open", "stands unmet", "not in place", "is wanting") re-cut to the flat lack — a re-draft of four faces, no change to variants 1 and 2.
4. **A derived `{courtname}` fill would widen this pool.** "The court" as a body noun is CONDITIONAL on a `Courthouse` / `Multiple courthouses` / `Multiple court buildings` row, which this flag read cannot see (A-16, referent row 20, W20). A derived fill on the `{watchname}` pattern (OW-1) computed from the resolved court-class row would make the body noun safe and give this pool a second civic noun to land on. Recorded for CAR 8b-W-2's list; not acted, and no face in this packet anticipates it.
5. **DS-DEF-6 `Legal Infrastructure: Court only`** (annex `:2996-2999`) says "fines and exile" on a card that recovered NO reading. That pool is WIRING-UNRESOLVED and not this block's, but the two rows sit in one dossier and this rewrite drops the sentencing claim here. If DS-DEF-6 keeps it, the two surfaces will disagree about what this town can do.
6. **NEW THIS ROUND — the gerund subject in variant 3 face 3.** "Holding under that law is the half not in place" opens its SECOND sentence on a gerund functioning as the subject of a predicate nominal, not on a participial clause. The gate's instrument does not count it (it reported one face, and the only face-initial participle was variant 1 face 2), so no measure is moved by changing it, and §21.2 keeps a lawful face lawful rather than trading it for an echo. But the reconstruction rule in `sweep/section-ai.md` is written flatly ("No sentence opens on a participle"), so a refuter reading the ban at the sentence grain rather than the face grain could name it. The chair is asked whether the ban is face-initial (the instrument's reading, which this packet follows) or sentence-initial. If sentence-initial binds, the lawful substitution that keeps the OPEN move, the thread and the word count is: `At {settlement} the law is formal. Under that law the holding half is not in place.` — its only cost is that "holding half" then echoes face 1's noun phrase, which is why it is recorded here rather than taken.

### N.8 The count

Three variants rewritten in place (vids 1, 2, 3; tags `[ledger]`, `[street]`, `[unfolding]` exactly as they stand) · twelve faces (four per variant) · word counts 14 / 16 / 18 / 11 · 11 / 17 / 15 / 11 · 11 / 8 / 16 / 15 · slots: eight faces name `{settlement}` once each (variants 1 and 3), four name no slot (variant 2) · one face re-cut this round (variant 1, face 2) · eleven faces byte-identical to round 1 · refusals: none.
