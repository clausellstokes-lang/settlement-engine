# REFUTE ROUND 2 — DS-DEF-2 · `Economic Survival: STRONG` (REFUTER, seat Opus 5, Fable-unvalidated; 2026-09-11)

Judged: the rows standing in the annex after the refinement (`kept.md`), face by face, against the printed licence card
(`node scripts/prose-licence-card.mjs DS-DEF-2 'Economic Survival: STRONG'`, read-only in laneRW-DEF2), `skeleton.md`,
`draft-round-2.md` (the lawful draft the density law is read against beside the shipped sentence), the shipped rows at
`402d592dfc1d82d0dee8e7d3354398ab775b2721~3:docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`, the REGISTER CARD with S2 and S3,
RULES-V2-PART-B §1 / §16–16.2 / §18 / §20–§24, MOVE-GRAMMAR §1–§3 and §4.4.1–§4.4.3, CLERK-LAWS §2.4.1 / §2.6.1,
ARCH-COMPOSED-PROSE-v2 §2.5 and §8.3, brief ADDENDUM 12 (W1–W10, R-i…R-viii′) and ADDENDUM 13 A+B (W11–W27).
Face 0 = the numbered tagged line; faces 1–3 = the `[face]` sub-rows, in order.
Status: COMPLETE — all twelve faces judged; read-aloud notes written.

## 0. WHAT I CONFIRMED BEFORE JUDGING (so the verdicts are not argued from the packets' own prose)

- The card prints ONE read (`scoreBand(economicScore) === STRONG`), bag `{band: RESERVED, route: proper, settlement: proper}`
  FILLED `{settlement}`, `source (none) · SOURCE-UNRESOLVED`, `may NOT: a count, a cause, a season, a future, a standpoint,
  a second fact`, angles `counterforce ledger street`. Verified by running the script in the dock.
- `shapes.participialOpenerRate` at `src/domain/prose/proseFingerprint.js:141` filters `/ing$/` on the FIRST WORD only —
  so the refiner's claim that `Entered` leaves the gate's owned measure at 0.0000 is **CONFIRMED at the instrument grain**.
  The finding I raise against that face below is therefore NOT the mechanical measure; it is named on its own law.
- Inventory walls all hold: 3 variants, the same vids in the same order, one bracketed angle tag each
  (`[ledger]` · `[street]` · `[counterforce]`, no `[plain]` on a spine row), 4 faces each, 12 of 12.
- Slot sets are the parents' exactly: vid 1 `{settlement}` ×4 · vid 2 `{}` ×4 · vid 3 `{settlement}` ×4. `{band}` and
  `{route}` named by no face. Sentence counts pinned 1 · 1 · 2 (A11). No face opens on the settlement token (T-F8, wall 10).
- No em dash, no digit, no exclamation, no question, no which-clause, no colon anywhere. No body noun, no component of the
  score, no pay word, no record noun, no citation, no person, no role word, no cause, no duration, no particular crisis,
  no maxim, no comparison to another arm, no totality over the town. W11–W19 and W20–W27 are unreachable by construction
  on eleven of twelve faces and are not breached on the twelfth.
- The three lawful shipped turns the skeleton listed are all kept: `can absorb a … crisis` (vid 1 face 3, with its modal
  restored this round), `The town could go through` (vid 2 face 0, verbatim), `a crisis` (vid 3, every face).
  No unlicensed shipped claim was re-imported. **No face is an inventory line.**
- The four faces of each variant are claim-equal to each other (A6) EXCEPT where a verdict below says otherwise.

## 1. VERDICTS — one line per face (variant · face · verdict · law · quote ≤ 12 words · finding · cure)

### VARIANT 1 · `[ledger]` · vid 1

- **v1 · face 0 · FAIL** · law: the REGISTER CARD's costume wall ("the antique air, if any, lives in the noun, never in the
  syntax"; R-DA-18) read with the ambiguity that opens a claim after the spine (W22 by ambiguity: a person as agent) ·
  quote: `Entered at {settlement}, holding through a crisis is strong.` ·
  **finding:** this pool is the SPINE and opens the composed passage, so its first two words are read cold. A fronted bare
  past participle followed by `at <a proper place name>` takes a motion reading on the first pass — somebody entered at the
  town — and the record sense of `entered` is only recovered at the main clause. That first reading imports an unnamed
  human agent onto a read whose layer is AGGREGATE and whose card licenses no person (W22); and the lift that makes the
  line land is carried entirely by the inversion, which is where the register card forbids it. The refinement's own stated
  cause was that the draft's `holding` stood without its complement; but the draft's `holding` was not bare — the fronted
  `Through a crisis,` bound it verbally — so the defect cured was smaller than the one introduced, and the round's other
  stated law (W15/W20, the holdfast reading) is not reached either way. A regression against the draft on a named target.
  · **cure:** revert to the draft line `Through a crisis, holding is entered strong at {settlement}.` — the refiner banked
  exactly this fallback (refine.md §6 row 1) and records that nothing else in the pool moves with it. No claim changes.

- **v1 · face 1 · PASS** · quote: `What {settlement} would bear in a crisis is entered as well covered.` ·
  the free relative names the pressure, which is what the ladder's authored reading predicates `well covered` of; `would`
  frames a capacity and not an edge (R-DA-07); `is entered as` is the office's own formula and names no record and no
  holder (R-vi / W7 / W24); the slot is carried, not the opener; the construction (free relative subject) is distinct from
  every sibling's. Nothing is added and no licensed claim is lost. The formula recurring across the variant's faces is
  expressly lawful ("Repeat the office's formula because it is the office's. The WORD may recur; the FACT must not.").

- **v1 · face 2 · FAIL** · law: R-DA-07 (THE PRESENT STANDS; a standing capacity, never an event), read with the card's
  `may NOT: a season` and skeleton §8's NO EVENT wall (W18: no stress read is on this card) ·
  quote: `a pressure the town meets well` ·
  **finding:** `meets` is an eventive verb in the bare present, so the clause reads habitually — whenever such pressure
  comes, the town meets it — which is a recurrence over events the record does not hold. The card reads a standing
  capacity at or above the band threshold and nothing about crises arriving. This is the very fault the same round named
  when it restored the modal at face 3 of this variant ("the bare present indicative `absorbs` also reads as a habitual …
  a frequency the record does not hold"); the round applied that law at face 3 and broke it at face 2 by replacing the
  durative `carries` with a strictly more eventive verb. The fault is inherited in kind from the draft's `carries` and
  worsened in degree by the substitution, so the finding stands against the face and the regression against the round.
  · **cure:** `Economic survival at {settlement} is set down as a pressure the town can meet well.` — one word added, the
  capability modal the card licenses (skeleton §0.7 fence 3), which keeps the round's verb-spread gain and adds no claim.

- **v1 · face 3 · PASS** · quote: `That {settlement} can absorb a crisis well is carried as standing.` ·
  the shipped line's one lawful turn `can absorb a … crisis` stands with its modal, which is the density floor honoured at
  §21.4 (the round restored the modal the draft had dropped — a genuine gain, not a regression); the that-clause subject is
  distinct from every sibling's; `carried as standing` is the office's formula (R-vi / W7) and no record noun (W24).

### VARIANT 2 · `[street]` · vid 2

- **v2 · face 0 · PASS** · quote: `The town could go through a crisis well.` ·
  the shipped clause `The town could go through` standing verbatim as one of the four, which §21.4 expressly permits and
  the skeleton names as this variant's density frame; `a crisis` is the pressure CLASS; `could` frames a capacity
  (R-DA-07); no slot, matching the parent's empty set; the street's own subject and idiom. Not an inventory line: the arm,
  the pressure and the band are all present in the angle's stance.

- **v2 · face 1 · FAIL** · law: R-DA-07 (the present stands; no perfect aspect on a standing fact; C3 STATE never FATE)
  read with D-22 (the band word entails exactly the ladder's reading) · quote: `a pressure the town has well covered` ·
  **finding:** `has … covered` is read first as a present perfect, which puts a completed act of covering behind the band
  — a history on a read whose card licenses one standing value and refuses a cause and a second fact. On the alternative
  reading it is the colloquial resultative idiom "have it covered", which is modern spoken register and not the compiled
  record's third person. Either reading also relocates the ladder's predication: `bandLadders.js:203` predicates
  `well covered` of the PRESSURE, agentless, and this makes the TOWN the agent that covered it — the same category slip
  this round cured at v1 face 1 and then reintroduced here. The draft's `the town is well covered for` carried the ladder's
  own predication with no aspect at all, so this is a regression against the draft with R-DA-04 (a stranded close) as its
  only stated warrant, and a close-kind gain does not buy an aspect.
  · **cure:** revert to `A crisis is a pressure the town is well covered for.` If the stranded preposition must go, the
  smallest lawful alternative that keeps the ladder's predication and adds no claim is
  `A crisis is a pressure well covered at {settlement}` — REFUSED here, because vid 2's parent slot set is empty
  (ARCH §2.5) — so the revert is the cure.

- **v2 · face 2 · PASS** · quote: `Strong is how the town carries a crisis.` ·
  the band's own word fronted is word order, which the register card makes spendable, and the claim is the card's
  predicate unchanged; `carries` is durative and states the manner the band reads, not an event; the manner clause carries
  no second claim; the OBJECT close is the variant's one object landing; no slot, matching the parent. The fronted-band
  proximity with the CRITICAL and ADEQUATE sibling pools is a reported sibling distance (A1/A5), not a wall, and only one
  band's pool renders for any town.

- **v2 · face 3 · PASS** · quote: `Where a crisis would have to be borne, the town is strong.` ·
  the fronted relative SCOPES the band to this arm alone, which is what D-22's refused column and W2 both require, so
  `the town is strong` is not a totality; `would have to be borne` is a subjunctive capacity frame with no agent, so no
  person is imported (W22); `borne` is plain modern English and the lift is not in the syntax (R-DA-18). One-word
  substitution from the draft for verb spread, with no claim moved.

### VARIANT 3 · `[counterforce]` · vid 3

- **v3 · face 0 · PASS** · quote: `What stands at {settlement} against a crisis is the town's carrying.` ·
  the cleft names the only counterforce the card licenses — the band itself (skeleton §V3(5)) — with the pressure as a
  CLASS and never as live (W18); the possessive is seated on the town (W1); the second sentence states the band, so it is
  a second CONSTRUCTION of the one read and not a gloss or a maxim (R-DA-03, R-DA-12); `carrying` is handed forward
  between the two sentences (R-i at k = 0). Unchanged from the draft.

- **v3 · face 1 · WITHHELD** · law under test: D-22 (exactly the ladder's reading) · quote:
  `The town is well covered for that pressure.` ·
  **finding, unproven:** the ladder predicates `well covered` of the pressure; this sentence predicates it of the TOWN.
  By the strict reading of D-22 that this same round used to justify rewriting v1 face 1, that is a predication the card
  licenses in a different shape; by the ordinary reading the two are equivalent, the scope is explicit (`for that
  pressure`) so no totality over the town arises (W2), and no claim is added. I cannot prove a claim changed, so the
  channel is WITHHELD rather than FAIL. Everything else in the face is clean: `is measured against` is a verb and not a
  record noun, so no holder is cited on a SOURCE-UNRESOLVED card (A13, W24); the thread runs `pressure` → `pressure`; the
  two sentences differ in construction. · **cure if the chair reads D-22 strictly:** `That pressure is well covered at
  {settlement}.` — the ladder's own predication, the slot already in the parent's set, no claim added.

- **v3 · face 2 · PASS** · quote: `The town of {settlement} can hold through a crisis. Against that pressure the holding
  is strong.` · W5's naming form carries the subject without the slot opening the sentence; `can hold through a crisis`
  is the capability verb on the pressure class; `the holding` is anaphoric to `can hold` in the sentence before, so the
  holdfast reading the draft's fence 7 guards against is closed inside the face itself (W15/W20); both `pressure` and
  `holding` are handed forward (R-i). Unchanged from the draft.

- **v3 · face 3 · FAIL** · law: W4 THE CONSTRUCTION CONTRACT ("moving a variant onto its sibling's construction is a
  regression") read at the grain the rewrite rules state the four-faces law ("THE FOUR FACES DIFFER IN CONSTRUCTION — the
  subject, the order of the reads, the landing noun"), with A5 (a sibling paraphrase past a synonym swap) ·
  quote: `What stands under that pressure is the town's carrying.` ·
  **finding:** this second sentence is now the same pseudo-cleft as the PARENT face's first sentence — `What stands …
  is the town's carrying.` — sharing the cleft, the order of the reads and the landing noun phrase word for word, with
  only the prepositional phrase differing. The draft had the two inverted against each other (`The town's carrying is
  what stands under that pressure.`), which is what kept face 3's construction its own. The round bought a close-kind
  spread under R-DA-04, a soft band rule, by spending a construction distinction the four-faces rule and W4 hold as the
  rewrite's own wall; that trade is the regression, and it lands inside one variant where both faces are siblings of the
  same parent. · **cure:** revert the second sentence to `The town's carrying is what stands under that pressure.` — the
  smallest change that restores the construction distinction, keeps the thread (`pressure` carried forward), and adds no
  claim. If the chair wants the close-kind spread as well, the lawful alternative that keeps both is
  `The town's carrying is what stands there.` — REFUSED here, because the demonstrative adverb closes on nothing the
  composer can pick up and R-DA-04's close list does not hold it; so the revert is the cure.

## 2. THE READ-ALOUD NOTES (information only, one per variant)

- **Variant 1 `[ledger]`.** Read aloud in order the four faces do not sound like one office: face 0 opens on a participle
  that the ear parses as somebody walking into the town before the sentence corrects it, and faces 1, 2 and 3 all land
  their formula in the same passive slot (`is entered as` · `is set down as` · `is carried as`), so the recurrence that
  the register card licenses as the office's own begins to sound like a rota rather than a habit. With the face 0 cure
  applied the variant reads evenly, because the fronted `Through a crisis,` gives the ear a different entry and leaves
  the three passives spread across three different subjects.
- **Variant 2 `[street]`.** The strongest-sounding variant of the three, and the only one whose parent needs nothing:
  `The town could go through a crisis well.` lands in eight plain words and stops. Face 1's `has well covered` is the one
  place the variant slips out of the record's third person into something a person would say over a counter, and the ear
  hears it before the law does — which is usually the sign the law is there.
- **Variant 3 `[counterforce]`.** The two-sentence shape carries well and the short second sentences (`That carrying is
  strong.`, `Against that pressure the holding is strong.`) are the pool's best relief. Read faces 0 and 3 back to back,
  though, and the ear hears the same sentence twice — `What stands … is the town's carrying` — which is exactly what the
  cure at face 3 removes. The variant's two `strong` closes are reported by the refiner and are a card consequence (the
  card licenses two band spellings), not a writing fault.

## 3. TALLY

12 faces judged: **4 FAIL** (v1 f0, v1 f2, v2 f1, v3 f3) · **1 WITHHELD** (v3 f1) · **7 PASS**.
All four FAILs are regressions the REFINEMENT round introduced against the lawful draft; none is a breach the draft
carried and none is a re-imported shipped claim. Every cure named is a revert or a one-word addition of a modal the card
already licenses, and no cure adds a claim, moves a slot set, moves a sentence count or touches an angle tag.
