# DRAFT ROUND 2 — DS-DEF-2 · pool `Invasion & War: walls with NO force`

Writer: Opus 5 (Fable-unvalidated seat), for the Fable chair. REWRITE car 8b, block DS-DEF-2, draft round 2.
Written against `skeleton.md`, the licence card printed in `laneRW-DEF2`, the pool's CURRENT rows in that dock's annex (`docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`, the round-1 text as landed), and the block's sibling pools read whole for arms A1 and A11.
Three variants in, three out, in order, under their own vids and their own angle tags; none added, none removed, none merged.
The typed lines of the pool (ROLE · READS · RELATION · ATTACH · FORM · MOVE) are untouched and are not repeated here.

STATUS: COMPLETE (three variants, twelve faces, zero refusals).

**The gate's one failing measure, answered.** `band depth · punctuation.colonRate (over) = 6.987 band-widths on 1 of 12 face(s)` — the single colon in the corpus was variant 1's second face (`Set down at {settlement}: the works standing, …`). It is gone; **zero colons stand in the twelve faces**, so the measure moves from 6.987 band-widths outside to 0.000 (inside the band, `colonRate = 0`, which is `martin-narrative`'s own value). No round-2 face carries a colon, a semicolon, an em dash, a question mark, a digit or a percent. This round is not dry.

**Two further changes, each on a law and not on taste** (§21.3: an unlawful or at-risk face is pushed toward the ceiling in the same effort):
- **Variant 2 face 2** repeated its own verb (`finds … finds`) — the sameness tic. One word changed (`finds the works` → `sees the works`); the line's length, claims, order and landing are untouched.
- **Variant 3 is returned to the SHIPPED slot set (slotless)** and its absence limb is re-spelt. Two laws: (i) *never ADD a claim* — the shipped v3 (`The town has the thing that would save it and not the people who would use it…`) names no settlement, and round 1 added `{settlement}` to all four faces; (ii) **arm A1 / A11 on the page-set** — the Beasts row of this same block, which renders on the SAME defense tab, now reads at its variant 1 `The town of {settlement} has its works and no force to hold them.` Round 1's v3 head was `The town of {settlement} has the works and not the force to hold them.` — a five-word identical opener and the same closing clause, on one page, for every plagued walled town with no force. The round-2 head keeps the contrast shape and the compression and names the two force classes instead (`and neither a garrison nor a militia to hold them`), which is the skeleton's own stricter spelling (§4.1) and is strictly MORE specific, never plainer.

---

## THE REWRITTEN ROWS (paste under the pool's heading, replacing the three variant rows)

1. `[ledger]` The works at {settlement} are entered as standing, and to hold them the town has neither garrison nor militia.
   - `[face]` No force stands at {settlement}, and what stands is the works.
   - `[face]` Set down at {settlement} are the works standing, and no force in the town.
   - `[face]` What the town has built at {settlement} stands entered. To hold the works the town has no force.
2. `[visitor]` A stranger at {settlement} sees the works standing and no force at them.
   - `[face]` What a stranger finds at {settlement} is the works, and neither garrison nor militia.
   - `[face]` A stranger finds no force at {settlement}, and sees the works.
   - `[face]` A stranger comes to {settlement} and sees the works. No garrison and no militia stand at the works.
3. `[street]` The town has the works and neither a garrison nor a militia to hold them.
   - `[face]` What the town has built stands, and no force is kept in the town.
   - `[face]` The town is without a garrison and without a militia, and what it has is the works.
   - `[face]` The town has the works. The town keeps no garrison and no militia.

--- NOTES

### N.0 The card's clauses, named once (every licence below cites one of these)

| tag | the card's own line |
|---|---|
| **C-READ(a)** | `predicate: invasionRowSituation(walls, garrison, militia) === walls, no force` — the `walls` argument TRUE (a `walls`-bucket member stands on the live roster) |
| **C-READ(b)** | the same predicate — the `garrison` argument FALSE (no `Garrison`, `Multiple garrisons`, `Barracks` or `Professional city watch`) |
| **C-READ(c)** | the same predicate — the `militia` argument FALSE (no `Citizen militia`) |
| **C-STAND** | `may claim: that the reader … selects the row walls, no force … as a STANDING fact of the record` — licenses the present tense and the standing aspect (`stands`, `entered as standing`, `kept`) |
| **C-BAG** | `bag: {band: RESERVED, route: proper, settlement: proper}` · `FILLED at this block's call sites: {settlement}` — licenses the one slot and no other; it LICENSES the slot, it does not require it (the shipped v3 spends none) |
| **C-ANGLE** | `angle: ledger street visitor` — licenses each variant's stance and no fourth |
| **C-SOURCE** | `source: muster · standing LICENSED` — SPENT BY NOBODY here (skeleton §0.4 / §4.4: no muster record resolves on a key whose militia is absent by construction; zero citations on this pool) |

(b) and (c) together are the key's own word **"no force"**; the three legs are ONE keyed condition (R-v), so stating all three is not "a second fact" (skeleton §1.4, §4.1).

### N.1 Variant 1 `[ledger]` — the licences, face by face

Construction contract (W4), unchanged from round 1: THE OFFICE'S ENTRY — the fact set down in the office's own formula, no eye, no contrast close, no citation.

| face | claim | licence |
|---|---|---|
| 1 (the numbered line) | the works stand at {settlement} | C-READ(a) + C-STAND (`are entered as standing` is the office's formula, R-vi/W7; not a record noun, W24) |
| | {settlement} | C-BAG |
| | no garrison | C-READ(b) |
| | no militia | C-READ(c) |
| | the ledger's stance | C-ANGLE |
| 2 | no force (garrison and militia together) | C-READ(b) + C-READ(c), in the key's own word |
| | the works stand | C-READ(a) + C-STAND (`what stands is the works`) |
| | {settlement} | C-BAG |
| 3 **(CHANGED — the colon cure)** | the works stand | C-READ(a) + C-STAND (`Set down at {settlement} are the works standing`; `set down` is the office's own formula, skeleton §1.5, and is an entry, never an agent-source, W24) |
| | no force | C-READ(b) + C-READ(c) (the second conjunct rides the same `set down`: the office enters the works standing, and enters no force in the town) |
| | {settlement} | C-BAG |
| 4 | the works stand | C-READ(a) + C-STAND (`what the town has built … stands entered`; `what the town has built` is the alias table's always-safe spelling of any wall-class member, A-7 / W15) |
| | no force | C-READ(b) + C-READ(c) |
| | {settlement} | C-BAG |

**The colon cure, stated exactly.** Round 1's face 3 read `Set down at {settlement}: the works standing, and no force in the town.` The colon was the corpus's only one and was the gate's only failing measure. It carried no licence: MOVE-GRAMMAR's only colon-bearing grammar is **V7** (`HISTORY → PRESENT with the joint chosen — a colon where the cause is named`), and V7 is **R2 CAUSAL only**; this pool is R1 STATE, which has no provenance field and therefore no V7 (R-DA-19). The cure is the same claim set in the same construction with the colon replaced by the inversion the formula already wanted: `Set down at {settlement} are the works standing, and no force in the town.` Nothing else in the face moved — the same office formula, the same three reads, the same order (works → absence), the same landing noun (`the town`), 13 words to 14.

Dropped from the shipped v1, each with the law that drops it (unchanged from round 1, kept here so the packet stands alone): "nobody to put on them" (REFUSED COLUMN, a totality over persons; W22; manning NOT ENTAILED, ENTAILMENT item 1; CONTRADICTED by the required `Town watch` on every native town of this key) · "A determined attacker" (an EXTERNAL BODY no field holds; W25) · "takes this town" (a forecast, STATE never FATE; a deictic standpoint, the card's `may NOT`) · "with ladders and patience" (a tactical particular; a duration; hollow specificity, fault 24; the ENTAILMENT refuter's own finding on this row) · "and requires nothing else" (a totality, a verdict close, the doubled beat W6, a third clause). The weight is replaced with licensed specificity, never with nothing: the two force classes NAMED where the shipped folded them into one person word.

Density floor: the pool's one memorable compression, "has walls and nobody to put on them", survives in its licensed form — face 1 carries the stricter "to hold them the town has neither garrison nor militia", faces 2 to 4 carry "no force" at the same weight; the exact unlicensed part, and only that, is gone (skeleton §1.6). The colon cure did not make its face plainer: the same fact is carried by the same formula in the same number of clauses.

### N.2 Variant 2 `[visitor]` — the licences, face by face

Construction contract (W4), unchanged: ONE PERCEPTION — a stranger's eye placed at {settlement}, which may see and find and may not walk, act, judge, measure, be told or be named (W27).

| face | claim | licence |
|---|---|---|
| 1 (the numbered line) | a stranger is the eye at {settlement} | C-ANGLE (the `[visitor]` stance; W27: the stranger may see) + C-BAG for the placement |
| | the works stand, seen | C-READ(a) + C-STAND |
| | no force at them | C-READ(b) + C-READ(c) |
| 2 | a stranger is the eye | C-ANGLE + W27 |
| | the works stand, found | C-READ(a) + C-STAND |
| | no garrison, no militia | C-READ(b), C-READ(c) — named apart, as the licensed specificity |
| | {settlement} | C-BAG |
| 3 **(CHANGED — the repeated verb)** | a stranger is the eye | C-ANGLE + W27 |
| | no force | C-READ(b) + C-READ(c) |
| | the works stand | C-READ(a) + C-STAND |
| | {settlement} | C-BAG |
| 4 **(CHANGED — the thread)** | a stranger arrives and sees | C-ANGLE + W27 (arrival is the visitor's placement; walking a circuit is not) |
| | the works stand | C-READ(a) + C-STAND |
| | no garrison, no militia | C-READ(b), C-READ(c) |
| | {settlement} | C-BAG |

**Face 3, one word.** Round 1 read `A stranger finds no force at {settlement}, and finds the works.` — the same verb twice in eleven words is best-ai fault 10 (sameness) at the smallest grain, and the second `finds` is the limp beat W6 watches for. `finds the works` → `sees the works`: the claims, the order (absence → works), the length (11 words) and the landing noun (`the works`) are all unchanged, and the variant now spends three distinct perception verbs across its four faces (`sees` · `finds` · `finds`/`sees` · `comes … sees`).

**Face 4, the thread at k = 0 (R-i).** Round 1 read `A stranger comes to {settlement} and sees the works. Neither garrison nor militia stands in the town.` — the second sentence handed back `the town`, which picks up `{settlement}` by reference rather than by carrying a NOUN forward from the first sentence, which is what R-i asks of a spine's own second sentence. Round 2 reads `… No garrison and no militia stand at the works.` — the noun `the works` is carried forward literally, the face lands on it, and the absence sits `at the works` in the same licensed spelling the numbered line uses (`no force at them`). This is the only negated-surface opener in the variant (W10: at most one), and it is a PRESENT read negated, never an absence opener.

Dropped from the shipped v2 (unchanged): "serious" twice (an evaluative on a condition and extent the record does not rate; VERDICT does not exist; NOT ENTAILED: condition, height, extent, D-3) · "anyone standing in it" (a totality over persons; W22; manning; and "in it" makes the works a line one stands in, which D-6 refuses) · "perimeter" (W15 / A-7: FALSE of a `Gates (if walled)`-only town and of a `Citadel`; the class has no always-safe geometric generic, so "the works" is the spelling).

Density floor: the licensed form of the shipped compression, `sees the works and no force at them`, stands verbatim as face 1 (skeleton §2.6 names it as the same weight) and is untouched by this round; the lawful turn `A stranger at {settlement} sees` is kept as the variant's opener, as §4.5 requires.

### N.3 Variant 3 `[street]` — the licences, face by face

Construction contract (W4), unchanged in KIND: THE TOWN'S PLAIN ACCOUNT — what the town has and has not, in the commonest civic words, with the sibling-licensed contrast; no office formula, no stranger, no voice, no belief, no interlocutor. **The slot set is returned to the shipped one: all four faces are SLOTLESS** (A6's slot-equality holds across the four; arm D is satisfied by naming no slot at all).

| face | claim | licence |
|---|---|---|
| 1 (the numbered line) | the town has the works | C-READ(a) + C-STAND (the shipped opener's own lawful fact and its own opener `The town has`, kept — skeleton §3.6) |
| | and neither a garrison nor a militia to hold them | C-READ(b) + C-READ(c); the CONTRAST is licensed by the sibling keys `walls with citizen militia` and `walls AND professional garrison` (wall 5 / R-DA-02), is not fronted, and is the closing move of this ONE variant of the pool; the two classes are the skeleton's own stricter spelling (§4.1) |
| 2 | the works stand | C-READ(a) + C-STAND (`what the town has built stands`) |
| | no force kept | C-READ(b) + C-READ(c) |
| 3 **(re-spelt)** | no garrison, no militia | C-READ(b), C-READ(c) (`without a garrison and without a militia`) |
| | the works stand, as what the town has | C-READ(a) + C-STAND |
| 4 | the works | C-READ(a) + C-STAND |
| | no garrison, no militia | C-READ(b), C-READ(c) |
| | (the thread) | R-i: the second sentence carries the noun `the town` forward from the first |

**Why the slot went, and why that is the shipped claim set.** The shipped v3 is `The town has the thing that would save it and not the people who would use it, and says so when pressed.` — it names NO settlement. `C-BAG` licenses the slot; it does not oblige a variant to spend it, and the pool already proves that (v1 and v2 spend it, v3 does not). Round 1 put `{settlement}` into all four faces of v3. Under **"never ADD a claim"** the naming of the town is a claim the shipped sentence did not make, so round 2 hands it back. Nothing else about the variant's licence changes: the street's plain account is the record's own subject either way, and read (a) is a fact about THIS town whether or not the town is named in the unit.

**Arm A1 / A11 on the page-set — the second reason, and the one that matters more.** DS-DEF-2 renders FIVE fixed rows on one defense tab, so the `Beasts & Monsters` row and the `Invasion & War` row are read together on every town. On a plagued, walled, force-less town the Beasts branch that fires is `plagued, perimeter but NO force to hold it`, whose variant 1 now reads **`The town of {settlement} has its works and no force to hold them.`** Round 1's v3 head read `The town of {settlement} has the works and not the force to hold them.` — five identical opening words and an identical closing clause, on one page, differing by `its`/`the` and `no`/`not the`. That is the restatement arm A1 exists to catch, and it is a page-level fault no reader would forgive. The round-2 head keeps the contrast SHAPE (wall 5), keeps the compression, keeps the shipped opener `The town has`, drops the slot, and names the two force classes: `The town has the works and neither a garrison nor a militia to hold them.` The distance is bought with MORE licensed specificity, not with plainness (§21.4): where the Beasts partner says `no force`, this pool now says which two rows are absent.

Dropped from the shipped v3 (unchanged): "that would save it" (a capability of the works the presence flag does not hold, D-3 "whether they have held"; a subjunctive edge with no typed threshold; the maxim shape, R-DA-12) · "the people who would use it" (a totality over persons; W22; manning; CONTRADICTED by the required `Town watch`) · "and says so when pressed" (the town as a speaking agent with an admission — a belief frame and a fused collective agent, W23, the referent findings D-F16/D-F18; an implied presser, W22; a third clause, R-DA-03). The CONTRAST SHAPE itself is kept, because the rejected alternative names a sibling key; only its filling was refused.

Density floor: `has the thing that would save it and not the people who would use it` reaches its licensed form at the same compression in face 1; the street's plain register is kept (`has`, `keeps`, `is without`, `the works`, `what the town has built`) and no face is plainer than the shipped where a licensed form of the compression exists.

### N.4 The bars, checked across all twelve faces (re-run on the round-2 text, by script for the token classes)

- **W11 material bar** — no material and no material source anywhere; `{defmaterial}` is unminted, so no face names timber or stone. Scanned: zero hits.
- **W12 garrison bar / W13 watch bar / W15 alias bar** — "garrison" and "militia" appear only as the ABSENT class words the predicate's own arguments name (C-READ(b), C-READ(c)); no garrison is asserted present; the WATCH is never named, in either polarity, because this key excludes the watch bucket and the required `Town watch` stands on every native town of the key (skeleton §0.4, the town-tier trap). The wall-class noun is "the works" / "what the town has built" (W15's always-safe spelling) in every face; "the line", "walls", "the perimeter", a gate, a tower, a walkway and a citadel appear nowhere (scanned: zero hits on `wall`, `perimeter`, `line`, `watch`).
- **W14 label bar** — no engine label is rendered at its dictionary sense; `{band}` is RESERVED and unnamed, `{route}` unfilled and unnamed, no readiness word, no country word, no pay word, no gate read.
- **W16 the gate's word** — not engaged: this key reads no `economicGates`; no face says upkeep, keeping, wages, pay, a sum, a rate or a share.
- **W17 crisis bar / W18 stress-record bar / W19 faith bar** — not engaged: no siege, occupation, besieger, war, famine, illness, insurgency, stressor, creed or deity is named.
- **W20 layer bar** — every noun sits at its read's layer: "the works" / "what the town has built" is BODY on a BODY read (the `walls` bucket, C-READ(a)); "no force" / "no garrison" / "no militia" is NONE on an absence of two bodies (C-READ(b), C-READ(c)); "the town" is NONE (a possessor, never a body); "a stranger" is the visitor's stance noun and is never a referent that acts.
- **W21 baked-power bar / W22 person bar / W23 fused-agent bar** — no seat, hall, council or chamber; no person as agent, decider, holder or counterfactual (scanned: zero hits on `nobody`, `anyone`, `no one`, `people`, `men`, `unmanned`); no relation between two referents that no field computes; no town that decides, says, knows, believes or pretends.
- **W24 record-word bar** — zero record nouns and zero citations on this pool (the skeleton's §4.4 ruling, taken; scanned: zero hits on `roll`, `books`, `record`, `accounts`). The `[ledger]` tag is treated as a STANDPOINT throughout; "entered as standing", "set down … are", "stands entered" are the office's own formula (W7), never an agent-source. **The colon cure was written to stay inside this bar**: `Set down at {settlement} are the works standing` is the office entering a fact, not a record showing one.
- **W25 external-body bar** — no foreign force at all; the shipped attacker is gone.
- **W26 visibility bar** — `covert: no` on the card; `audience: player (no mark)`; no face carries a dm-only mark or names anything the player's page would not hold.
- **W27 angle-tag bar** — three stances, three constructions: the office's entry · the stranger's one perception · the town's plain account. No elders' body, no stranger who acts or is named, no stance noun used as a holder's record.
- **W1 possessor binding** — no possessive pronoun is used in any face. The one pronoun subject is v3 face 3's `what it has`, whose antecedent is `the town`, the clause's own subject and the lawful possessor; the round-1 `its works` shape appears nowhere (it is the Beasts partner's, not this pool's).
- **W2 country-scoped threat** — not engaged: this key reads no `config.monsterThreat`, and no face says what the country is (the Beasts row on the same page carries that read; C7 refuses importing it here).
- **W3 band marker** — not engaged: `{band}` is RESERVED and is named nowhere.
- **W4 the construction contract** — kept per variant, and the round-2 edits were written INSIDE each contract: the colon cure stays the office's entry; v2 face 3 changes one verb inside the one-perception shape; v3's head keeps the contrast shape and the shipped opener. No variant moved onto a sibling's construction.
- **W5 naming-form article** — `the town of {settlement}` is the naming form and is now used nowhere (v1 and v2 place `{settlement}` adverbially; v3 is slotless). No maxim frame: every `the town` is the record's own subject, and no face states a life-general claim about towns (R-DA-12).
- **W6 no doubled beat / W10 one negated opener** — no clause restates the read it follows. Negated-surface openers: v1 face 2 (`No force stands …`), v2 face 4's second sentence (`No garrison and no militia stand …`), v3 none. One per variant at most, and each is a PRESENT read negated, never an absence opener.
- **THE THREAD (R-i, §1.4.1)** — the three two-sentence faces each carry a NOUN forward: v1 face 4 `the works` ← `what the town has built`; v2 face 4 `the works` ← `the works` (round 2's change); v3 face 4 `the town` ← `the town`. No face changes subject in the middle and hands nothing back.
- **The composition fence** — every face reads as a passage OPENING (this is a spine, mounted first) and lands on a noun a modifier can pick up: `militia` · `the works` · `the town` · `force` · `them` (the works). No face opens on `{settlement}` (T-F8), and the settlement token opens no variant of the pool (wall 10, spent zero times).
- **The walls** — scanned by script over the twelve faces: **0 colons** (round 1: 1), 0 semicolons, 0 em dashes, 0 exclamation marks, 0 question marks, 0 digits, 0 percents, 0 `which`-clauses, 0 expletive openers, 0 second-person. No figure, no simile, no sense verb on an abstraction, no tricolon (no face carries three of anything), no evaluative adjective, no count, no cause, no season, no future, no standpoint.

### N.5 The measurements (reported, not gating)

| vid | angle | face | words | sentences | opener (first two words) | landing noun | changed in round 2 |
|---|---|---|---|---|---|---|---|
| 1 | ledger | 1 | 19 | 1 | The works | militia | — |
| 1 | ledger | 2 | 11 | 1 | No force | the works | — |
| 1 | ledger | 3 | 14 | 1 | Set down | the town | **yes — the colon cure** |
| 1 | ledger | 4 | 18 | 2 | What the | force | — |
| 2 | visitor | 1 | 13 | 1 | A stranger | them (the works) | — |
| 2 | visitor | 2 | 14 | 1 | What a | militia | — |
| 2 | visitor | 3 | 11 | 1 | A stranger | the works | **yes — the repeated verb** |
| 2 | visitor | 4 | 18 | 2 | A stranger | the works | **yes — the thread noun** |
| 3 | street | 1 | 15 | 1 | The town | them (the force classes) | **yes — slot, A1 distance** |
| 3 | street | 2 | 14 | 1 | What the | the town | **yes — slot** |
| 3 | street | 3 | 17 | 1 | The town | the works | **yes — slot, re-spelt** |
| 3 | street | 4 | 13 | 2 | The town | militia | **yes — slot** |

- Twelve faces; mean 14.75 words; range 11 to 19; sd 2.59; three of twelve are two-sentence faces. (Round 1: mean 14.2, sd 2.6 — the shape of the set is held while the failing measure moved.)
- **`punctuation.colonRate` — the gate's measure: 1 of 12 faces over by 6.987 band-widths in round 1; 0 of 12 in round 2.** The exemplar band runs 0 (`martin-narrative`) to 0.1252 (`tolkien-elevated`); every face now reads 0, inside the band at its floor. `semicolonRate` 0 on all twelve (band 0.0098 to 0.122); `questionRate` 0; no other punctuation measure moved, because only one face's punctuation changed and it lost a mark rather than gaining one.
- The three VARIANTS' first two words are distinct (`The works` · `A stranger` · `The town`), so A11 holds at the grain it is measured on; only one face of one variant ever renders, so face-level opener repetition inside a variant costs nothing at the draw.
- Pronoun closers: 2 of 12 (v2 face 1 `at them`; v3 face 1 `to hold them`) — unchanged from round 1. Both are the density floor's own licensed forms of the shipped compressions, which the skeleton names at §2.6 and §3.6 as the same weight as the shipped. R-DA-04's 0.055 is a REGISTER rate over 2,914 sentences, not a per-pool wall; the local rate is reported here rather than cured by making the two floor-lines plainer (§21.4).
- Within-pool word-count sd is 2.59 against R-DA-05's ≥ 4.0 direction, held at round 1's value. The floor on face length is structural: every face of v1 and v2 must carry `{settlement}`, and the shortest lawful wording that states all three legs plus the slot runs to eleven words. Reported, not padded — adding words without a claim is the tic the band exists to catch.
- Grammars: two distinct level-1 shapes across the three variants (a PRESENT stated as one keyed condition in the visitor's single perception; PRESENT-then-the-absence in the ledger's entry and the street's contrast), so the census's `grammars: 2` does not fall.
- Citations: 0 of 12, against Part B §24's exemplar rate of 0 per 786 sentences.
- Reads stated per face: 3 of 3 (the works · no garrison · no militia) in every one of the twelve, against the shipped rows' 1 stated and 2 folded into a person word.
- Slots: v1 four of four carry `{settlement}`; v2 four of four carry it; v3 zero of four (the shipped v3's own set). A6's slot-equality holds inside every variant.

### N.6 Refusals

**None.** All three variants are written lawfully; no variant of this pool needed a refusal row. The gate's one failing measure is cured, not banked.

### N.7 Carried for the chair (vetoable, four items)

1. **The slot returned on variant 3.** Round 1 added `{settlement}` to a variant the shipped corpus wrote slotless; round 2 hands it back, on "never ADD a claim" and on the A1 page-level collision at N.3. If the chair prefers the name kept, the lawful form that still clears the Beasts partner is `At {settlement} the town has the works and neither a garrison nor a militia to hold them.` (the skeleton's own §1.6 placement of `has` in a non-opening position), and every face of v3 then takes `{settlement}` back in the same adverbial position.
2. **A page-level finding the wording cannot cure, for the chair not for the writer.** On a plagued, walled, force-less town this pool's claim set is a strict SUBSET of the `Beasts & Monsters: plagued, perimeter but NO force to hold it` pool's, and both render on one defense tab: both state the works standing and both state the same two force rows absent, the Beasts pool adding only the country read. Round 2 buys what construction and vocabulary can buy (a different opener, a different contrast, the two classes named where the partner says "no force"), but BOTH pools are licensed to the `[visitor]` angle and both write a stranger who sees the works and finds no force, so a reader of that town meets the same fact twice however either pool is worded. This is an arm A1 / C7 finding against the ROW SET, not against either pool's text, and it belongs to the composition, not to this packet.
3. **"no garrison" and "no militia" as the class words of an ABSENCE.** W12 bars "the garrison" as a BODY word where no garrison row resolves. Here the row's non-resolution IS the read (C-READ(b)), and the skeleton licenses exactly these spellings at §4.1. The draft takes the skeleton's licensing: the bar is read as barring an asserted PRESENCE, never the predicate's own negative argument. Round 2 uses the indefinite article on v3's head (`neither a garrison nor a militia`) so that no face carries a definite body noun for an unresolved row. If the chair reads W12 the other way, every face collapses to the key's own word "no force", which four of the twelve faces already carry unaided.
4. **Zero citations.** The card prints `source: muster · standing LICENSED`, but no muster record exists on a key whose militia is absent by construction (holder null, `holderReason: town-resolved`). The skeleton's §4.4 ruling — cite nothing — is taken, and the colon cure was written to stay on the office's formula rather than reach for the record noun a colon invites.

Seat: Opus 5 (Fable-unvalidated) — writer packet, draft round 2; three variants, twelve faces, zero refusals; the gate's one failing measure moved from 6.987 band-widths outside to inside the band; the packet is whole.
