# SKELETON — DS-DEF-2 · `Economic Survival: CRITICAL` (the marker's packet; Fable, 2026-09-11)

Seat: Fable 5.1 — marker for the REWRITE, working for the Fable chair. This file is the only thing this seat wrote. Everything below is judged against the printed licence card, the census row it was printed from, the reader function the card names and the producer behind it, the annex block's header lines, the register card (with S2 and S3), Part B §1 and §16 to §24, MOVE-GRAMMAR §1 to §3, §1.4.1 and §4.4.1 to §4.4.3, CLERK-LAWS §2.4.1 and §2.6.1, ARCH v2 §2.5 and §8.3, brief ADDENDUM 13 parts A and B (the entailment law, the referent law, the writer bars W11 to W27), the two ratified tables' rows that name this pool or its arm, and `rewrite/rulings-DEF2.txt`; where a shipped clause reads well but no field licenses it, it is UNLICENSED here and the law is named.

Status: IN PROGRESS — section 0 written; variants 1 to 3, section 8 and section 9 follow (the checkpoint law: this file is rewritten section by section as the work lands).

## 0. THE CARD, THE KEY, THE ONE READ AND ITS LAYER (what the faces are wired to)

### 0.1 The licence card as printed (`node scripts/prose-licence-card.mjs DS-DEF-2 'Economic Survival: CRITICAL'` in laneRW-DEF2, read-only)

```
LICENCE (block DS-DEF-2 · role spine · key `Economic Survival: CRITICAL`)
  reads:      scoreBand(economicScore) (via ECONOMIC_ROW_POOL in defenseStateProse.js)
              (absent ⇒ no candidate; a modifier is silent, never "false")
  predicate:  scoreBand(economicScore) (via ECONOMIC_ROW_POOL in defenseStateProse.js) === CRITICAL
  bag:        {band: RESERVED, route: proper, settlement: proper}
              FILLED at this block's call sites: {settlement}
  relation:   (a spine takes no relation)   ← a spine IS the seat and carries no relation; the relation is the MODIFIER's property, fixed at its freeze (ARCH §4.5)
  seat/form:  (not a seat-taker) / sentence      move: (none declared)     angle: ledger street unfolding
  attach:     (empty: a spine takes no attach set)
              n/a
  echo:       spine mounts 1 (tabs: defense) · modifier mounts 0 (none)
              the echo table is keyed on this pool's WHOLE table-rung reading (truncated at the file's first dot) and NOT on a producer-token root, so every pool that selects a row of `ECONOMIC_ROW_POOL` shares ONE echo key: a mount counted there may be a sibling ROW of the same table
  covert:     no
  source:     (none) · standing SOURCE-UNRESOLVED
              NO citation is licensed: a face naming a record holder here is refused by arm A13
  may claim:  that the reader `scoreBand(economicScore)` selects the row `CRITICAL` of `ECONOMIC_ROW_POOL` in `defenseStateProse.js`, as a STANDING fact of the record
  may NOT:    a count, a cause, a season, a future, a standpoint, a second fact
  audience:   player (no mark)
  REFUSED COLUMNS, always: a totality over persons; an exemption from a duty (whoIsExempt is null everywhere); a named character and that character's fate (product scope); a theological claim about a deity (the deity doctrine)
```

The census row behind the card (`docs/content/wiring-census.json`, `rows[]`, block DS-DEF-2, pool `Economic Survival: CRITICAL`, printed by this seat with a read-only `node -e` over the JSON): `status RESOLVED` · `rung table` · `keyFunction ECONOMIC_ROW_POOL` · `readsGrain branch` · `reads` = the one synthetic label above (`fieldsRead` and `branchReads` the same) · `narrowed false` · **`objectClasses []`, `objectClass null`** (no civic class is spent by this pool; a face that NAMES one imports a body the read does not reach, W20) · `sites ["defense.threatAssessment"]` · `k 2` · **`rateBp 938`** (the CRITICAL band fires on about nine towns in a hundred of the RATE sample; the rarest of the four economic rows) · **`source {kind: "", kinds: [], fields: {economicScore: ""}, holder: null, holderReason: "no mapping row resolves any field this pool reads", standing: SOURCE-UNRESOLVED, twoSource: false}`** · `variants 3` · **`grammars 1`** (the census classifier reads one grammar across the three; the STRONG sibling reads 2) · `slotsFilled ["settlement"]` · `slotsNamed ["settlement"]` · `slotsWithoutProvider []` · `covert false` · `absent {}` · `attach []`. No `dm-only` mark on any of the three rows. The `fieldSynonyms` table (`src/domain/prose/fieldSynonyms.js:49-51`) has NO entry for `scores.economic`; the pay words (`wages · wage · pay · purse`) are the synonyms of a DIFFERENT field, `defenseProfile.economicGates.military` (the pay gate), which this pool does not read.

### 0.2 The one read, unpacked to its producer (`defenseStateProse.js:529-543` the table and its reader; `:660` the caller; `defenseScoreBands.js:38-39` the band; `defenseGenerator.js:257-290`, `:340-426`, `:603-624` the producer; `src/domain/compendium/bandLadders.js:202-207` the ladder's reading) — read for what the band MEANS, never as a licence of its own

The caller reads `economicRowPoolKey(dp.scores?.economic)`; the reader returns `ECONOMIC_ROW_POOL[scoreBand(economicScore)]`; `scoreBand` is the frozen four (`n >= 65 ? 'STRONG' : n >= 40 ? 'ADEQUATE' : n >= 20 ? 'WEAK' : 'CRITICAL'`). So the key is ONE KEYED CONDITION of ONE input: `defenseProfile.scores.economic < 20`. An absent or non-finite score is SILENCE, never CRITICAL (the reader's own docblock: "An absent or non-finite score is SILENCE, never CRITICAL"; `avgScore` returns null on no numbers "so a consumer that banded 0 would report CRITICAL for a settlement that simply has no defence profile" is the hazard the reader refuses). The band's authored READING (`bandLadders.js:206`, `Critical`): **"Below 20. Effectively uncovered against this pressure."**

What the score is made of (the producer, for the marker's judgment of every "fund", "spend", "pay", "nothing to spend" and "the people who would be paid" word; NOT a licence — entailment D-22 refuses "what the score is made of"): the PRIMARY driver is `foodSecurity.storageMonths` (0 months = 0, 1 = 10, 3 = 25, 6 = 45, 12 = 70, capped at 70; the generator's own comment: "Primary driver: food storage months"; the axis is defined at `:59` as "logistical resilience (food, medicine, trade access)"); then flat bonuses for `hasMarket` (+10), `hasHospital` (+10), a `port` route in a maritime world (+10), a `crossroads` route (+8), `econOutput × 0.2`, alchemy with a granary (+8); then the whole is MULTIPLIED by an economic-health gate `0.45 … 1.0` by `econOutput` ("a struggling economy cannot fund crisis logistics"; "physical grain in a granary retains value even when the treasury is empty — never zero it"); then stress penalties (siege up to 25, famine 20, plague 15, `indebted` 15, with their magic offsets); then the food-chain modifier (+5 healthy / −8 impaired). The chair's ruling in `rulings-DEF2.txt` states it for this block: **"the economic-survival arm is FOOD-STORAGE-driven resilience, never prosperity"**; the entailment table's L-53: "economic is driven primarily by FOOD STORAGE MONTHS and is not prosperity"; L-52: "exactly the ladder's reading; frozen at four rungs; a badge on one arm says nothing about another"; G-19 refuses "a cause; a history; a count; a trajectory; self-sufficiency; survival".

**What CRITICAL is reachable WITH (the engine's own model; the marker's evidence against the shipped coin claims).** The band is a position below twenty on that stack, and the stack reaches it by several roads none of which a face may name: (a) a thorp or hamlet with no stores at all (storage 0, no market, no hospital, a land route) reads `0 + round(econOutput × 0.2)`, which is CRITICAL at any `econOutput` below forty even at the health gate's identity, so **a CRITICAL town can have a healthy economy and a full purse**; (b) a walled town with six months in its granary (45) under a destitute economy (× 0.45 = 20) and a famine stress (−20) reads CRITICAL, so **a CRITICAL town can hold months of grain**; (c) a market town under siege (−25) and plague (−15) falls from ADEQUATE to CRITICAL with no change in its purse or its stores. The health gate's floor is 0.45, never zero, on the generator's stated principle that grain keeps its value when the treasury is empty. So "nothing to spend", "cannot fund", "could not pay" are not merely unlicensed spellings of the band: on road (a) they are FALSE on the engine's own model (entailment law item 3: the engine's model bounds every entailment). And because roads (b) and (c) run through a LIVE stress penalty while the card reads no stress, a face may not say a crisis is present ("trouble") any more than it may say one is absent.

| input | how the caller derives it | value at this key | REFERENT LAYER of the read (ADDENDUM 13 B A.1) | the lawful nouns | the unlawful nouns |
|---|---|---|---|---|---|
| `scores.economic` banded | `scoreBand(dp.scores.economic)` | `CRITICAL` (the score below twenty) | **AGGREGATE** — "a band, score, label or ladder rung computed over bodies — it names NO body: the band's own word only" (the referent table's own row for this desk lists `DS-DEF-2 Economic ×4` under AGGREGATE, §2.1 row "per-arm badge"; W20). HOLDER: none (SOURCE-UNRESOLVED; no record word at all, W24) | the ARM by its label at its ENGINE meaning (W14): "economic survival" as the pressure's name; in plain words "the town's capacity to hold through a crisis", "what the town has against a crisis", "the town's cover against a crisis" (the generator's own noun for the axis is "crisis logistics", `:283`; the chrome's is "crisis response", `threatAssessment.js:173`; either names the pressure CLASS, never a particular crisis); the BAND by its own word ("critical") or by the ladder's authored reading ("effectively uncovered against this pressure"; "uncovered", "no cover", with the ladder's own hedge "effectively" carried or dropped, never a second hedge); "the pressure" (the ladder's own word for the class); the town as the possessor of the reading ("{settlement}'s …", "the town's …"); a NEGATED CAPABILITY as the band's reading in other words ("cannot hold through a crisis", "could not meet a crisis") with nothing narrated after it | any BODY (a granary, the stores, a market, a port, a hospital, a garrison, the muster, the soldiers, the watch, the town's force, a treasury, a purse, the books, a hall) — W20, D-F25 on the sibling STRONG; any COMPONENT of the score (food, months, grain, coin, revenue, tax, trade, a route, medicine) — D-22 "what the score is made of"; PROSPERITY and COIN words as the fact ("fund", "spend", "money", "revenue", "nothing to spend", "no coin") — L-53, `rulings-DEF2`, and §0.2 road (a) where they are false; the pay-gate words ("pay", "paid", "wages", "upkeep", "the keeping", "purse") — W16 and rule 5 license them on a pay-gate read ONLY, and this card reads no gate; a PARTICULAR crisis (a siege, a famine, a bad harvest, a bad month, a season, a war, an occupation) — the card's "a season", W17; a DURATION or RATE ("sustained", "almost immediately", "a bad month", "for months", "then continues") — the card's "a season", R-DA-16, D-22 "a trend"; a LIVE pressure ("trouble", "the crisis now on") — W18, the card reads no stress; a CAUSE ("because", "the reason") — the card, D-22; a TRAJECTORY or FUTURE ("exhausts", "continues", "makes the next thing cheaper to happen", "would collapse") — state never fate, G-19; a TOTALITY over pressures or over the town ("anything", "any pressure", "nothing") — W2, D-22's refused column (a CRITICAL badge on this arm says nothing of the Beasts or Invasion rows on the same page); a COMPARISON to another arm or town — D-22; a PERSON or persons ("the people who would have to be paid") — W22, D-F15's shape on the sibling WEAK; a belief or knowledge frame ("know it") — R-DA-13, R-DA-14, W23; a MAXIM ("each thing that goes wrong makes the next thing cheaper to happen") — R-DA-12; a FIGURE ("spending its way out", "cheaper to happen") — R-DA-11 |

**Where the key fires (for the writer's ear, not as a licence).** Any tier: a thorp with no root cellar and a thin economy reads CRITICAL; a town with market, granary and walls reads CRITICAL under siege and famine in a destitute year; `rateBp 938`. No tier read is on the card, so no tier word may be baked; no institution read is on the card, so no body may be named; no stress read is on the card, so no pressure may be said to be LIVE or ABSENT (the arm measures resilience against a crisis, not the presence of one); no economy read is on the card, so no purse may be said to be empty or full.

**What the KEY'S NAME licenses: only its engine meaning.** "Economic Survival: CRITICAL" is the row label; under W14 a face renders the ENGINE meaning — the economic-resilience axis, food-storage-driven, banded CRITICAL, read as "effectively uncovered against this pressure" — never the dictionary sense of "economic" (coin, revenue, prosperity) and never "survival" as an outcome (no field records the town surviving or failing to survive anything). The engine's own chrome for this branch (`threatAssessment.js:173`: "Economic base cannot support crisis response. Any sustained threat quickly overwhelms the capacity to respond.") is PRODUCT CODE outside the REWRITE's corpus (ADDENDUM 13 B D, OW-20) and is not a licence for the same words in a face; it is where the shipped v1 got every one of its clauses ("cannot fund a response", "Any sustained pressure", "the town's capacity"). The DS-DEF-6 Economic Backing cell (`defenseDisplay.js:216-221`) bands the SAME score on the same tab as chrome, and its lowest cell is "Critical" / "Cannot sustain forces. Systemic breakdown." — but that cell's threshold is TWENTY-FIVE (`econScore >= 25 ? 'Underfunded' : 'Critical'`), not scoreBand's twenty, so DS-DEF-6's "Critical" is NOT this key (a score of twenty to twenty-four reads WEAK here and Critical there); the CONDITIONAL the referent refuter allowed on the sibling WEAK v2 (D-F15: the debt as "the producer's own reading of the band" at `:221`) cannot be mapped onto this key even in principle, and "cannot sustain forces" would in any case be a BODY claim on an AGGREGATE read (W20) that is false on every CRITICAL thorp with no force to sustain. The desk's own docblock (`defenseStateProse.js:1473-1475`) files the pay half of this row as DS-DEF-6's "whole subject".

### 0.3 The block's header lines (annex `RECEIPT_POOLS_DOSSIER_STATE.md`, `### DS-DEF-2`, lines 2568-2596 of the laneRW-DEF2 copy; this pool's rows at 2695-2698)

- **STATE-KEY:** five fixed rows (`Beasts & Monsters` · `Invasion & War` · `Internal Security` · `Economic Survival` · `Disasters & Famine`), each with a `scoreBand` badge, read against `config.monsterThreat`, the institution presence flags, and `compound.inst`. This pool is the `Economic Survival` row's spine at its lowest rung; its badge IS its read (the only DS-DEF-2 row whose key is the badge itself), so the face's whole licence is the AGGREGATE and nothing beneath it.
- **SLOTS:** `{settlement}` `{band}` `{route}` — the card: only `{settlement}` is FILLED at this block's call sites; `{band}` RESERVED (a face writes the band word itself, never the slot); `{route}` proper but unfilled here (and a route is a COMPONENT of this score, refused twice over). A face names neither `{band}` nor `{route}`.
- **SECTION-TARGET:** `defense`.
- **PROVENANCE + FENCE:** `buildThreatAssessment` is dossier-native and this shape EXTENDS it into a pool (each branch held ONE string); two defects must not be reintroduced (neither touches this row); **"institution presence is a STANDING fact with no recorded history; the causal clauses here are CAPABILITY clauses … and never HISTORICAL ones unless the history surface supplies the ancestry"** — on this row there is no institution read at all, so the capability clause the header licenses elsewhere has no bodies to join here; the band's reading ("effectively uncovered against this pressure") is the only capability statement on the card, and it is a NEGATED one, which R-ii / W10 lets a face carry once per variant.
- **RECEIPT:** `threatAssessment.js:28-195` (the economic branch `:159-176`); rendered `DefenseTab.jsx:150-183`. **PDF PARITY:** parity.
- **Same page (C7; W2; A1/A11; the referent law's rule 3 across the tab):** DS-DEF-2 mounts the other four rows on the SAME tab — the Beasts row may read `plagued, perimeter AND organized force` and the Invasion row `walls and professional garrison` beside a CRITICAL economic arm (a full granary under a destitute year and a famine is road (b) of §0.2), so a face here that says the town "cannot fund a response to anything" or cannot "pay" its force contradicts the tab (C7) as well as the card; the Disasters row names the granary and the stores (a face here says nothing of stores though the score is made of them — D-22; T-F12's `store` class is that row's); the Internal Security row the court and the gaol. DS-DEF-6's Economic Backing cell bands this same score as chrome beside the face at a different threshold (`:1473`; §0.2); DS-DEF-11's pay-gate pools carry the wages and the upkeep (a face here says nothing of pay). A CRITICAL badge on this arm says nothing of another arm (D-22's REFUSED column) — so no face says the town is defenceless, helpless, exposed or broken in general (W2: no totality over the town).

### 0.4 The register card's six one-line registers (read whole; the dossier binds here)

The dossier: the record itself; the clerk's third person; the six shapes of its closed set; the town's name is not the default opener. The NPC ladder, the Herald, the chronicle, the DM page, chrome and the docent do not reach this pool. Amendment S2 (a computed CONSEQUENCE of the sentence's own fact may ride as one clause — here NO relation row exists on a spine and the producer's components are refused, so NO consequence clause is available) and S3 (every fact has a holder; the office cites one only for an S3 reason; here the holder is UNRESOLVED, so no citation at all — A13, W24) bind every face. The three questions before any sentence ships: which field licenses it (one: the band); which claims does it carry and are they the same claims as before (one claim: the band CRITICAL on the economic-survival arm, in the angle's stance); what does it leave standing open (the pressure this arm measures stands uncovered and has not come; that is the one lawful open matter, stated as a standing condition, never as a forecast of what it would do when it comes).

### 0.5 The pool's shipped rows, verbatim (the annex, bold line `**\`Economic Survival\`: \`CRITICAL\`**`; leaf vids 1-3)

1. `[ledger]` {settlement} cannot fund a response to anything. Any sustained pressure exhausts the town's capacity almost immediately and then continues.
2. `[unfolding]` The town is not spending its way out of trouble because there is nothing to spend, and each thing that goes wrong makes the next thing cheaper to happen.
3. `[street]` The town could not pay for a bad month at {settlement}, and the people who would have to be paid know it.

Variant count: THREE shipped variants (ledger · unfolding · street), matching the card's `angle: ledger street unfolding`. Slot sets per parent (a face whose `{slot}` set differs from its parent's is refused, ARCH §2.5): vid 1 `{settlement}` · vid 2 `{}` (NO slot — every face of variant 2 carries no `{settlement}`) · vid 3 `{settlement}`. Sentence counts: vid 1 TWO sentences (a full stop after "anything"); vid 2 ONE sentence (a "because" and an "and" joint); vid 3 ONE sentence (an "and" joint). A11's "where they differed in sentence count they keep differing" binds vid 1 against vids 2 and 3: vid 1 stays two sentences, vids 2 and 3 stay one each (the census reads `grammars 1` on the shipped rows, so the pool's grammar spread is a floor the rewrite may raise, never lower). Openers: vid 1 opens on `{settlement}` (a T-F8 breach; a cure, not a floor), vid 2 on "The town", vid 3 on "The town" (two of three share their first two words — an A11 spread breach the rewrite cures; no two faces of one pool's reading share their first two words). Negated surfaces: vid 1 one ("cannot"), vid 2 one ("is not spending"), vid 3 one ("could not") — each variant already sits at R-ii / W10's ceiling of one, and no face may add a second.

### 0.6 Findings of the two ratified tables that touch this pool's rows or its arm

- **REFERENT-TABLE §2.1 "per-arm badge" row:** `DS-DEF-2 Economic ×4` filed AGGREGATE — "the band's own word; never an institution". This pool is one of the four.
- **REFERENT-TABLE §5.1 D-F15 (CONDITIONAL, the sibling WEAK v2):** "the people who would have to hold {settlement} … are already owed" — "unindividuated PERSONS with a debt relation on a band read"; the refuter let the DEBT stand as the producer's own reading of the WEAK band (`defenseDisplay.js:221`) and named the PERSONS as the fault. By the same shape, this pool's vid 3 "the people who would have to be paid know it" carries the persons (the fault) and a knowledge frame the producer's reading holds for no band; and for CRITICAL the producer's DS-DEF-6 reading sits at a DIFFERENT threshold (twenty-five, §0.2), so the CONDITIONAL cannot reach this key at all. This seat does NOT extend the CONDITIONAL to a pay word on this pool. RECORDED as open row 2 for the chair.
- **REFERENT-TABLE §5.1 D-F25 (HOLDS, the sibling STRONG v1):** "the garrison can be kept paid while they last" — a named BODY on an economic band read that consults no bucket. The same ruling bars "forces" or any force word here.
- **ENTAILMENT-TABLE D-22 (HOLDS):** the readiness band word entails "exactly the ladder's reading"; NOT "a fifth gradation; a trend; a cause; a comparison; what the score is made of"; REFUSED "a badge on one arm says anything about another". **L-52:** frozen at four rungs; an absent score is null, never banded 0. **L-53:** "economic is driven primarily by FOOD STORAGE MONTHS and is not prosperity". **G-19:** the band word = the axis's number in that band; NOT "a cause; a history; a count; a trajectory; self-sufficiency; survival".
- **ENTAILMENT-TABLE A-25 (the crisis words):** "the crisis / the trouble / this" names TWO record classes with different vocabularies and the DS-STR-2 selector binds neither to a banner; on this pool no stress is read at all, so "trouble" as a live event (vid 2) has no record behind it of either class.
- **`rulings-DEF2.txt`:** "the readiness band word is exactly the ladder's reading and a STRONG badge on one arm says nothing of another; the economic-survival arm is FOOD-STORAGE-driven resilience, never prosperity"; R-ii (one negated surface per variant on a PRESENT read); R-iv (an `[unfolding]` variant realises OPEN after the presence, never the ledger's measure); R-vi (the office's formula is the ledger's surface; no roll as a source).
- No row of the ENTAILMENT table's defense section 7 (the "more knowledgeable than the simulation" list) names this pool's lines; the pool is judged from the card, the producer and the laws.

### 0.7 Fences this seat records for the writer and the chair, not decided here

1. **The arm's NOUN.** The band's "own word" is CRITICAL; the ladder's reading is "effectively uncovered against this pressure". A face must name WHAT is uncovered, and the only licensed referent is the arm itself at its engine meaning. This seat licenses "the town's capacity to hold through a crisis", "what the town has against a crisis", "the town's cover against a crisis", "economic survival" (the label, as a name) as spellings of the ARM, and refuses any spelling that names a component or a body. If the chair rules the arm may be spoken ONLY by its label word, the plain-word spellings fall and nothing else moves.
2. **"crisis" / "the pressure".** The pressure CLASS the arm is measured against, in the engine's own vocabulary ("crisis logistics", "crisis response") and the ladder's ("this pressure"). This seat licenses the class word and refuses every particular (a siege, a famine, a bad month, a bad season, a war, an occupation, a blockade) and every duration on it. RECORDED for the chair: if "crisis" is read as importing an event, the safest spelling is "the pressure" (the ladder's own word).
3. **The negated capability.** "cannot hold through a crisis", "could not meet a crisis", "has no cover against a crisis", "stands uncovered against a crisis" are read here as the band's reading ("effectively uncovered") in other words: a standing capacity stated in the negative, the one negated surface R-ii / W10 allows a variant. This seat refuses a CONSEQUENCE narrated after it ("exhausts", "continues", "cheaper to happen", "would collapse") and a DURATION or RATE on it ("almost immediately", "a bad month"). RECORDED for the chair: whether "uncovered" (the ladder's own word) counts as the variant's one negated surface; this seat counts it, so a face uses EITHER "uncovered" OR "cannot", never both.
4. **The coin and pay words on a band that is partly economic health.** `econHealthMult` and `econOutput × 0.2` are inside the score, and the sibling WEAK v2's debt clause was let stand as CONDITIONAL on the producer's DS-DEF-6 reading. This seat refuses "fund", "spend", "pay", "paid", "wages", "upkeep", "purse", "money", "coin", "nothing to spend" on this pool: the card reads no gate, D-22 refuses "what the score is made of", W16 and rule 5 license the gate's words on a gate read only, DS-DEF-6's lowest cell sits at a different threshold, and §0.2's road (a) makes every empty-purse claim FALSE on a CRITICAL thorp with a healthy economy. RECORDED as open row 2.
5. **The live pressure.** CRITICAL is often the product of a stress penalty (siege, famine, plague, debt), and a writer will reach for "trouble" or "the crisis now on" to give the band a reason. The card reads no stress; W18 licenses a stress only by its own record; so the face states the band as a standing condition against a pressure that is neither said to be present nor said to be absent. RECORDED as open row 11.

