# REFUTATION ROUND 1 — DS-DEF-2 · pool `Disasters & Famine: NO reserves, NO medical provision`

Seat: opus (REFUTER), for the Fable chair. Judged over `kept.md` (the rows standing in the annex
after the refinement round), against `skeleton.md` (the marker's licensed skeleton), the licence
card as `scripts/prose-licence-card.mjs` prints it in `laneRW-DEF2`, ADDENDUM 13 PART A (W11-W19)
and PART B (W20-W27), ADDENDUM 12 (W1-W10, R-i..R-viii'), the REGISTER CARD with S2/S3,
RULES-V2-PART-B §1 / §16-16.2 / §18 / §20 / §21-§23, MOVE-GRAMMAR §1-§3 / §1.4.1 / §4.4.1-4.4.3,
CLERK-LAWS §2.4.1 / §2.6.1, ARCH-COMPOSED-PROSE-v2 §2.5 and §8.3, the ENTAILMENT-TABLE and
REFERENT-TABLE defense sections, and `draft-round-1.md` (the draft the refinement came from).

FACE NUMBERING: the numbered tagged line is FACE 0; the three `- [face]` sub-rows are FACES 1, 2, 3.
Nothing was written outside this file. No dock was entered; the only command run in `laneRW-DEF2`
was the read-only licence-card script.

COUNT: 12 faces judged — **7 FAIL · 0 WITHHELD · 5 PASS**.

---

## THE POOL-LEVEL FINDINGS THAT STAND BEHIND THE PER-FACE ROWS

**P1 — THE DENSITY THE SKELETON PUT ON THE TABLE IS LARGELY UNSPENT (skeleton §2.4, §5.1 item 4:
"never with nothing").** The rewrite correctly drops seventeen unlicensed shipped claims. The
skeleton names six licensed places to spend the freed weight. Measured over the twelve faces:
item 1 (the SIBLING CONTRAST, R-DA-02, against a granary-holding or hospital-holding sibling key)
is spent **0 of 12**; item 3 (the FUNCTION of the absent thing, D-12 / D-13) **3 of 12**;
item 4 (the office's own formula, R-vi/W7) **1 of 12**; item 6 (the close varied in KIND,
R-DA-04) **0 of 12 — every one of the twelve faces closes on an absence**. The refinement notes
refuse the sibling contrast on the ground that "a contrast carried by one face and not its
siblings would break claim-equality under arm A6"; that reason does not reach a contrast carried
by all four faces of one variant, which is what R-DA-02 and skeleton §6.1 licence, so the largest
licensed spend in the pool was declined without a law.

**P2 — SKELETON §6.1 WAS NOT OBEYED ANYWHERE.** §6.1 rules that the second constituent takes the
LACK move "placed not-first and not adjacent to another absence", and names the sibling contrast
as "the lawful thing to put between them". All twelve faces place the two absences immediately
side by side with nothing between. Because §1's row 1.9 licenses the pair AS ONE KEYED CONDITION
(R-v), I do NOT fail every face on MOVE-GRAMMAR §1.4 constraint 3 for that alone — the marker's
own packet is in two minds about it — but where the second constituent is a bare elliptical noun
phrase with no predicate the face reads as a LIST of two absences and is failed as the inventory
line (skeleton §5.1 item 1).

**P3 — THE FOUR-FACES RULE IS BREACHED IN THREE PAIRS.** The rule is that the faces differ in
CONSTRUCTION — the subject, the order of the reads, the landing noun — not in vocabulary alone.
Three pairs move NONE of the three: v1 faces 1 and 3 (subject the care house, order C2 then C1,
landing the storehouse class), v2 faces 0 and 2 (subject the granary, order C1 then C2, landing
the care class), v3 faces 0 and 3 (subject the stranger, order C1 then C2, landing the sick).
In each pair the weaker member is failed; in each pair the failed member is also the PLAINER one,
so §21.4's regression is the same finding seen from the other side.

**P4 — THE [ledger] STANCE IS CARRIED BY ONE FACE OF FOUR.** Skeleton §3.1 gives the ledger the
office's own formula as its surface; only face 3 ("carried standing") has it. Faces 1 and 2 hold
nothing of the office and are interchangeable with the [street] variant's faces — the dropped
angle of §5.1 item 5, which W4's construction contract names. The [visitor] stance, by contrast,
is realised in all four of its faces, and that variant is the pool's strongest.

**P5 — WHAT THE REWRITE GOT RIGHT, recorded so the cure round does not undo it.** The stock trap
is clean: no face uses a `store`-class word and no face asserts an emptiness, so the engine's own
1.5-2.0 months of storage on a granary-less roster is nowhere contradicted (L-20; D-12's ENGINE
CONTRADICTS column). No record word, no holder, no citation anywhere (W24; A13). No tier word.
No person acts, decides, directs or is told (W22, W27). The density floor's T1, T2, T3 and T5 are
carried verbatim. Four of the draft's universal-negative sweeps past the care class are cured.
No digit, no em dash, no exclamation, no which-clause, no semicolon, no expletive, no forecast.

---

## PER-FACE VERDICTS

### VARIANT 1 — `[ledger]` (grammar V3, declared at the draft)

**v1 f0 — PASS.**
Quote: "keeps no granary to hold grain back, and no infirmary stands".
Finding: none proven. The pool's one licensed opener (`{settlement}`, order constraint 10) heads a
negated PRESENT surface (R-ii, W10) and the care class takes the LACK on its own predicate, which
is exactly the shape skeleton §6.1 prescribes. Both constituents carry the absent thing's FUNCTION
in the engine's own terms (D-12 "grain is stored / it buffers a harvest"; D-13 "somewhere to put
the sick", the shipped T5 verbatim), so it is one of only three faces in the pool that spends any
of skeleton §2.4's licensed density. Both nouns sit at the layer of their read (BODY-absence,
`storehouse` and `care`). No record word, no tier word, no person, no totality.
Cure: none.

**v1 f1 — FAIL.**
Law: **W4 / skeleton §5.1 item 5 (the dropped angle)**, with **skeleton §5.1 item 1** (the
inventory shape) on the tail.
Quote: "No house that takes in the sick stands at {settlement}, nor any storehouse".
Finding: nothing in the face is the compiling office. Skeleton §3.1 makes the `[ledger]`'s surface
the office's own formula; this face has no formula, no measure, no record standing — it states two
absences flat and is interchangeable with the `[street]` variant's own face 1, with which it
shares the care-class noun verbatim ("a house that takes in the sick"), the verb ("stands") and
the read order (care, then storehouse). A `[ledger]` face built as a second `[street]` line is the
construction-collapse the skeleton names and W4 forbids. The second constituent is a bare
elliptical noun phrase with no predicate, so the pair reads as a list rather than as the one keyed
condition R-v licenses, and the absences sit adjacent with nothing between them (skeleton §6.1).
Cure: front a present ground and give the storehouse its own office-formula predicate, keeping
both claims and adding none — e.g. open on the locative at `{settlement}` and close the second
constituent on "is entered" (R-vi/W7), which restores the angle and removes the ellipsis in one
change.

**v1 f2 — FAIL.**
Law: **W4 / skeleton §5.1 item 5 (the dropped angle)**, with **skeleton §6.2** (V4 is not a
licensed member on this card).
Quote: "The granary that would store grain is not built at {settlement}".
Finding: the refinement notes describe this face as "the object-first subject with the subjunctive
relative". Object-first is level-1 member V4, and skeleton §6.2 rules V4 NOT licensed here because
V4 wants a named-object field and the granary is a class word read off a flag; the variant's
declared grammar is V3 and W4 makes the draft's declared grammar the refiner's to keep. The face
also carries nothing of the office, so with face 1 it leaves the `[ledger]` variant with one
ledger face in four. The trailing "and neither is a house for the sick" is an elliptical inversion
with no predicate of its own, adjacent to the first absence (skeleton §6.1).
Cure: keep the subjunctive relative as the granary's function but seat the clause on the office's
formula rather than on the object — state the pair as what is and is not ENTERED at `{settlement}`,
which returns the face to V3 and to its angle without touching a claim.

**v1 f3 — FAIL.**
Law: **skeleton §5.1 item 1 (the inventory line)** and **the four-faces rule / A5** (P3 above).
Quote: "No infirmary is carried standing at {settlement}, and no granary."
Finding: ten words, of which the last three are a bare negated noun with no predicate; the face
states the pool's key fact and nothing else, in the shape skeleton §5.1 item 1 refuses by name
("{settlement} has no granary and no infirmary"). Against its sibling face 1 it moves none of the
three axes the four-faces rule counts: the subject is the same referent (the care-class row), the
read order is the same (care, then storehouse), the landing noun is the same referent (the
storehouse class), and only the spellings and the verb change — so it is face 1 in a second coat,
and the plainer of the two, which is §21.4's regression read from the other side. "carried
standing" is correctly the office's formula and not a record noun (R-vi/W7, W24); that is the one
thing to keep.
Cure: keep "carried standing" and give the storehouse constituent its own predicate with the
licensed sibling contrast between the two (R-DA-02 against the `granary AND hospital` or
`granary, NO medical provision` key), which separates the absences, moves the landing noun and
adds no claim.

### VARIANT 2 — `[street]` (grammar V1, declared at the draft)

**v2 f0 — FAIL.**
Law: **skeleton §5.1 item 1 (the inventory line)**, with **§21.4 (the density regression)** and the
four-faces rule (P3 above).
Quote: "No granary stands in the place, and no infirmary."
Finding: nine words, two negated nouns under one predicate with an elliptical tail; it states the
pool's key fact and nothing else — no street stance beyond the bare locative, no licensed read
past the pair, none of skeleton §2.4's density. It is its own sibling face 2 made plainer: the same
subject referent (the granary), the same read order, the same locative ("the place"), the same
landing class, and it drops face 2's one turn — the absence stated at the BUILDING layer ("among
the buildings of the place") — with no law behind the change, which is §21.4's regression. It
matters more than a sibling row because index 0 is the line a falsy seed draws and the line the
canonical projection keeps live (MOVE-GRAMMAR §3.2).
Cure: leave face 2's building-layer turn where it is and re-cut this face on a different
construction — a present ground first (where a storehouse would stand and does not) with the care
class taking its own predicate — so the subject, the order and the landing noun all move off
face 2's.

**v2 f1 — FAIL.**
Law: **W14 (the label bar) and W15 (the alias bar)**, with the referent law's **rule 3** (same word,
same referent, same row) and **C7** (the same-page fence).
Quote: "The community stands with neither a house that takes in the sick".
Finding: "The community" is not a neutral word for a settlement on this page. CONFIRMED in the
dock: `deriveDefenseGroupLabel` returns the literal string `'The community'` as the small-tier
fallback name for the town's armed body (`src/generators/power/governanceNarrative.js:506`), and
`SMALL_SETTLEMENT_TIERS` is `{thorp, hamlet, village}` (`:15`) — exactly the tier range this pool
can fire at (skeleton §0.5: `Town granary` and `City granaries` are `required: true`, so a native
roster puts this row at village and below and nowhere else). ENTAILMENT-TABLE P-19 carries the same
row. So on every cell this face can reach, the engine's own defense prose may name the town's force
with the identical two words on the identical defense page, and this face makes those two words the
possessor of a storehouse and a care house instead — an engine label read at its dictionary sense
(W14), a generic word used where the row it names is a different row (W15), and one word carrying
two referents on one page (rule 3, C7). ADDENDUM 13 PART A item 5, which the skeleton cites for
this word, gives it as the engine's generic for THE PAID MILITARY below town, not for the place.
Cure: replace "The community" with "The place", the skeleton's own safe subject, and let the cure
round re-cut face 3's opener so the two faces do not share it. No claim moves.

**v2 f2 — PASS.**
Quote: "A granary is not among the buildings of the place, nor a house".
Finding: none proven. This is the pool's one face that states the absence at the layer the flag
actually occupies — the BUILDING, not the stock — which is the cure for the trap the pool key's own
word (`NO reserves`, a `store`-class word against a `hasGranary` BUILDING read, skeleton §0.7)
invites every writer of this pool to walk into. It is plainest civic idiom with no maxim, no
practice, no custom, no season and no count, which is what skeleton §3.2 asks the `[street]` stance
for, and §21.4 makes it a floor rather than something to smooth.
Cure: none.

**v2 f3 — FAIL.**
Law: **the licence card's `may claim`** (the row, not the world) and **W14** (the `hasChurch` label
trap), with the card's REFUSED COLUMN **"a totality over persons"**.
Quote: "and without anywhere for the sick".
Finding: "anywhere" scopes the negation over every place in the settlement, not over the care-class
row the read reaches. CONFIRMED in the dock: `disasterRowSituation` returns on the no-granary side
before the `church` argument is ever consulted (`defenseStateProse.js:580-586`; the `church` argument is read only inside the `granary` branch at `:583`), and the block's own
docblock says so in its own words — a church counts as medical provision only in the granary branch,
"so for a town with no reserves the split is hospital-or-nothing, and the church is not consulted
there"; meanwhile `Access to parish church` is `required: true, baseChance: 1`
(`institutionalCatalog.js:300`), so a parish stands on a large share of this pool's own hamlet
cells, and the corpus's sibling row calls its clergy the people who tend the sick. A face saying the
place has NOWHERE for the sick denies a record the read never looked at. This is precisely the
universal-negative sweep the refinement round cured in four other faces (its notes call it "the
round's largest correction"); it survives here because the notes count "anywhere for the sick" as
one of the four spellings that NAME the class, and it does not name the class — it names every
location.
Cure: put the care-class noun back in the clause — "and without a house for the sick", or the
ratified positive in its negative on its own predicate — the cure round choosing the spelling that
keeps this face's distance from face 2. Nothing else in the face moves.

### VARIANT 3 — `[visitor]` (grammar V3, declared at the draft)

**v3 f0 — PASS.**
Quote: "A stranger looking for the granary at {settlement} finds no such building".
Finding: none proven. The density floor's T1, T2 and T3 are carried verbatim as the skeleton asked
(§4), the stranger does only what W27 licenses him to do (look, find nothing — he is not directed,
not told, not named, and draws no conclusion, which is exactly where the shipped v3 broke), and
"finds no such building" states the absence at the BUILDING layer, the pool's one cure for the
stock trap. Both constituents are named on their ratified class nouns (REFERENT-TABLE lines 62 and
63), and the coined "sick-house" of the shipped row is gone.
Cure: none.

**v3 f1 — PASS.**
Quote: "passes no infirmary, and sees no storehouse for grain".
Finding: none proven. Two perception verbs, each constituent on its own predicate, and the read
order reversed against face 0 with the landing noun moved to the storehouse class — the construction
moves, not only the vocabulary. "crossing", "passes" and "sees" are all inside skeleton §3.3's list
of what the stance licenses. No person acts, nothing is told, nothing is concluded.
Cure: none.

**v3 f2 — PASS.**
Quote: "The infirmary a stranger looks for at {settlement} is not built".
Finding: none proven. The stranger is demoted into a relative clause and the absent care house
takes the subject position, which is a construction change and not a synonym swap against faces 0,
1 and 3; the eye is still in the sentence, so the `[visitor]` stance holds rather than collapsing
into a record statement. "is not built" is narrower than the read rather than wider — `hasHospital`
false is the absence of the hospital, monastery, healer and friary rows together, and naming the
infirmary claims less than that, which is the safe direction.
Cure: none.

**v3 f3 — FAIL.**
Law: **the four-faces rule / A5** (the faces differ in CONSTRUCTION — the subject, the order of the
reads, the landing noun), with **§21.4 (the density regression)**.
Quote: "A stranger arrives at {settlement} and comes upon no granary".
Finding: against face 0 this face moves none of the three axes. Subject: "A stranger" in both. Read
order: the storehouse then the care house in both. Landing noun: the sick in both. What changes is
vocabulary alone — "looking for"/"arrives", "finds no such building"/"comes upon no granary",
"takes in the sick"/"for the sick" — which is the refusal the owner's four-faces rule names and
which the unweighted seeded roll makes worse, since the two are drawn with equal weight and a
reader who meets both meets one sentence twice. It is also the plainer of the pair and it is plainer
exactly where it matters: face 0's "finds no such building" is the pool's cure for the stock trap
and this face drops it for the bare noun, with no law behind the change (§21.4, read against the
shipped v3 as well, whose licensed core is the same frame). The trailing "and no house for the sick"
is a bare elliptical absence with no predicate.
Cure: reverse the reads and land on the storehouse, giving the care constituent its own perception
predicate, so the order and the landing noun both move off face 0's while the subject stays the
stranger; face 2 already holds the object-first opener, so this face keeps the stranger as subject
and differs from face 1 by its verbs.

---

## READ-ALOUD NOTES (information only; no verdict rides on these)

**Variant 1 `[ledger]`.** Read aloud in order, the four faces land as one sentence spoken four
times: three of them end on a bare negated noun, and two of the three ("nor any storehouse for
grain", "and no granary") tail off rather than close. Face 0 is the only one with a spoken shape —
the two purpose clauses give the voice somewhere to go and the line finishes on "the sick" rather
than trailing. Face 3's ten words are the shortest in the pool and the short line is a real
permission (NL-2, R-DA-05), but a short line still has to close, and this one stops.

**Variant 2 `[street]`.** The four open on four different words, which reads well, but three of the
four run the same rhythm — a short subject, one negative verb, then a trailing second negative —
so the ear hears a frame rather than four sentences. Face 2 is the only one with a phrase in it a
person would say out loud ("not among the buildings of the place"); the other three are list
intonation. "without a granary, and without anywhere" also puts two stressed "without"s three words
apart, which is the beat the ear catches first and the beat the pool least wants.

**Variant 3 `[visitor]`.** The best variant to hear. The stranger gives the voice a place to stand
and the perception verbs carry the line without any of them reaching for an act. Faces 0 and 3 read
as the same sentence spoken twice with different words, and heard back to back the second one
sounds like a worse take of the first; face 2's inversion is the one that most rewards a second
reading, because the absent thing arrives before the denial does.
