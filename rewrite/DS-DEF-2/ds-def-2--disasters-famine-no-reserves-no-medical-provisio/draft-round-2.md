1. `[ledger]` No food is set aside at {settlement} against a bad year, and no one in the town treats the sick. The year that fails is hardship at once, and the plague burns itself out.
   - `[face]` Against a bad year {settlement} keeps no reserve of food, and against a sickness nobody at all. Hardship comes with the bad year, and the sickness is waited out.
   - `[face]` At {settlement} neither a store against the bad year nor a hand for the sick is held anywhere. The sickness runs its course, and the failed harvest is hardship with no interval.
   - `[face]` Nothing is laid up at {settlement} against a bad year, and nobody here nurses the sick. A failed harvest is want at once, and the sickness ends unaided.
2. `[street]` The town's answer to a sickness is the answer it gives a bad season. Both are endured, and the counting comes after.
   - `[face]` A season gone bad and a sickness come to one answer here, and that answer is to bear the thing. The count is taken when the thing has gone by.
   - `[face]` Hunger and the sickness are met in one way here, lived through while they last and counted when they are done.
   - `[face]` Between a bad season and a sickness the town makes no difference of answer, and it holds on through each. A reckoning follows when each is over.
3. `[visitor]` A stranger asking after the granary or the sick-house at {settlement} is sent to neither. The town keeps no such place.
   - `[face]` A newcomer at {settlement} asking after the store of food or the house of the sick is given no direction, because the town holds neither.
   - `[face]` Whoever comes to {settlement} looking for a grain store or a sickroom is directed to neither. Neither building stands in the town.
   - `[face]` Strangers at {settlement} ask where the grain is kept and where the sick are taken, and get no answer to either. Nothing in the town goes by either name.

--- NOTES

**Packet.** Block DS-DEF-2 · pool key `Disasters & Famine: NO reserves, NO medical provision` · draft round 2 · Opus WRITER seat (Fable-unvalidated). Three existing variants rewritten in place under their own numbers and their own bracketed tags; none added, removed, merged or reordered. Four wordings per variant (the numbered row's own text plus three `[face]` sub-rows), twelve wordings in the pool. The typed lines of the pool are untouched and not repeated here.

**THE ROUND-1 REFUSAL, CURED — the grammar.** Round 1 wrote the numbered row as ``1. `[ledger]` `[plain]` …``. `VARIANT_RE` (`scripts/generate-dossier-state-prose.mjs:228`) accepts an optional SECOND backticked bracket and `foldTag` (`:346-357`) folds its first part into `marks`, so `plain` entered the closed mark vocabulary and `dossierStateProseProjection.contract.test.js:715-725` red with `unclassified: [ 'plain' ]`. The lawful row carries ONE bracket — the angle, per the docblock's own form ``N. `[angle · tag]` text`` (`:26`) — and the numbered row's text IS the plain wording; `faceCounts` is `1 + wordings.length` (`:829`), so parent plus three faces is the four the owner's rule asks for. Every landed sibling in this block is written that way (`Beasts & Monsters: plagued, perimeter AND organized force`; `frontier, force without a perimeter`). No `[plain]` tag appears anywhere in this packet, and no numbered row carries a second bracket.

**THE FAILING OWNED MEASURE, ANSWERED — `shapes.whichTailRate`.**
- **The reading.** 26.027 band-widths over on 1 of 3 faces. The band over the fourteen primary fingerprints is 0.0000–0.0370 (`leguin-nonfiction-spoken` 0.0370 at the top; `martin-chronicle` 0.0000 at the floor), width 0.0370. `(1.0000 − 0.0370) / 0.0370 = 26.027`, so the reported face measured at exactly **1.0000** — one sentence, one `, which`.
- **The face it was.** The pool kept its PREVIOUS rows when round 1 was refused, so the gate measured the shipped three, and the shipped variant 2 is a single sentence carrying `, which is to endure it and count afterwards` (`docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`, the `NO reserves, NO medical provision` annex). `whichTailRate`'s detector is `rate(count(/, which\b/))` (`prose-research/fingerprint.mjs`), and one hit in a one-sentence unit is 1.0000. The other two shipped variants carry no `, which` — hence 1 of 3.
- **The cure.** Zero occurrences of `, which` in any of the twelve wordings of this packet. K5, K6 and K7 are carried by a coordinate clause or by their own sentence in all four wordings of variant 2. With the grammar refusal cured the rows land, and the measure moves **1.0000 → 0.0000**, inside the band, depth 0 band-widths, on every face. This is the round's moved measure; the round is not dry.

**THE OTHER TWENTY INSTRUMENTS, WRITTEN TO DELIBERATELY (bands from the fourteen `primary/*.fingerprint.json`; grain ENTRY, depth ceiling 1.75, budget two thirds — Part B §16.2).** A one- or two-sentence unit can only score 0, 0.5 or 1.0 on a per-sentence rate, so a single occurrence of a rare shape is a deep exceedance. The four that cost most were priced and refused outright:

| instrument | band | one hit in a two-sentence face | this packet |
|---|---|---|---|
| `punctuation.semicolonRate` | 0.0085–0.1220 | 0.5 ⇒ **3.4** band-widths over | zero semicolons (round 1 spent one; withdrawn) |
| `closers.abstractNounRate` | 0.0122–0.1151 | 0.5 ⇒ **3.7** over | no sentence closes on a `ness/tion/sion/ity/ment/ance/ence/ship/hood/dom` word — `sickness` and `hardship` both match the detector and neither is ever final |
| `closers.pronounRate` | 0.0122–0.1083 | 0.5 ⇒ **4.1** over | no sentence closes on `here/it/them/that/this/there` (round 1's `Neither of them stands here.` is withdrawn) |
| `shapes.doubledAdjectiveRate` | 0.0057–0.0556 | 0.5 ⇒ **8.9** over | no `X-suffix and Y-suffix` adjacency; every `and` in the pool is either comma-preceded or preceded by a word outside the suffix set |
| `shapes.antithesisRate` | 0.0082–0.0401 | 0.5 ⇒ **14.4** over | round 1's `rather than after it` is withdrawn; zero regex hits (zero is 0.26 band-widths UNDER, twelve times cheaper) |
| `openers.sameOpenerAsPreviousRate` | 0.0334–0.1667 | 0.5 ⇒ **2.5** over | the two sentences of every face open on different words |
| `shapes.participialOpenerRate` | 0.0000–0.0342 | 0.5 ⇒ **13.6** over | no wording opens on an `-ing` word |
| `wordsPerSentence.shareOver30` | 0.0489–0.3382 | 1.0 on a one-sentence face ⇒ **2.1** over | the longest single sentence in the pool is twenty-five words |

`wordsPerSentence.neighbourVariation` (band 0.495–0.805) was written to directly: the two sentences of each face are sized so `2|a−b|/(a+b)` lands in or near the band. Measured per face — v1: 0.353 · 0.345 · 0.250 · 0.286; v2: 0.545 · 0.667 · (one sentence) · 0.963; v3: 0.857 · (one sentence) · 0.909 · 0.897. Two of the ten two-sentence faces sit inside the band and the other eight within 0.79 band-widths of it; v1 face B is the deepest at 0.79 under and is declared. `adverbsPerSentence` is 0 in every face (the detector excludes `only`, and no other `-ly` word is used); `triadRate`, `thereIsOpenerRate`, `dialogueShare`, `colonRate`, `emDashRate`, `questionRate`, `exclamationRate`, `parenthesisRate` are 0. `runsOfThreeSameLengthBand` is 0 on every face and cannot be otherwise on a unit A1 caps at two sentences — 0.60 band-widths under, structural, declared. **The two single-sentence faces (v2 face 2, v3 face 1) score `neighbourVariation` 0 by construction, which is 1.60 band-widths under — inside the 1.75 entry ceiling, and the deepest reading in the packet.** They are kept for the pool's rhythm spread (R-DA-05) and for the density law (§21.4): they are the compressed faces, and making them two sentences would be trading density for a number.

**The claim set carried, one id per claim (the BEFORE's, unchanged).**
- K1 no food is held at the settlement against a bad year · K2 there is nobody at the settlement to treat the sick · K3 a failed harvest is hardship with no interval between the failure and the hardship · K4 a sickness runs on until it stops of its own accord · K5 the answer to a bad season and the answer to a sickness are one answer · K6 that answer is to endure the thing · K7 the counting comes after the enduring · K8 a stranger asks after a store of food or a house for the sick · K9 the stranger is sent to neither · K10 the town holds neither.

**Variant 1 `[ledger]` — the licence per claim.** K1 by the card's `may claim:` on the `no reserves` term of the predicate `disasterRowSituation(granary, hospital, church) === no reserves, no medical provision`, as R-DA-02's flat LACK. K2 by the same clause on the `no medical provision` term; the read's `church` term is FALSE under this predicate, so no wording names a church, a parish, clergy or prayer anywhere in the pool. K3 and K4 are NOT licensed by any card clause (see R1 and R4). The slot `{settlement}` by `bag: {band, route, settlement}`, FILLED at this block's call sites as `{settlement}`, used once per wording and never as the opener. The angle `ledger` by `angle: ledger street visitor`. The four faces are claim-equal clause for clause (arm A6): set aside/treats/burns itself out · reserve/nobody at all/waited out · a store and a hand/held anywhere/runs its course · laid up/nurses/ends unaided.

**Variant 2 `[street]` — the licence per claim.** K5 NOT licensed (see R5) — an identity between two responses, where the predicate holds two absences and says nothing about what the town does with them. K6 NOT licensed as an act of the town; written as a passive or as a third-person act in every wording so no wording assigns the town a motive, a resolve or a mood (the FEELING non-move, MOVE-GRAMMAR §1.3). K7 NOT licensed — an ordering of two acts no field holds; no number is written and none is implied. The parent carries no slot and neither does any face (ARCH §2.5: a face whose `{slot}` set differs from the parent's is refused). The angle `street` by the card. Four vocabularies: answer given/endured/the counting comes after · come to one answer/bear the thing/the count is taken · met in one way/lived through/counted · no difference of answer/holds on/a reckoning follows.

**Variant 3 `[visitor]` — the licence per claim.** K8 NOT licensed — what a stranger asks after is a standpoint, which the card's `may NOT` bars while the card's own `angle:` names it (see R2). K9 NOT licensed — a direction given by somebody, where no field holds the person directing; the act is left agentless in three wordings (`is sent to`, `is directed to`, `get no answer`) and takes a bare passive in the fourth (`is given no direction`). K10's LACK half IS licensed by `may claim:` — both terms of the predicate are false, so `the town keeps no such place` is the predicate restated as a flat lack; its CAUSAL half is convicted by `may NOT: a cause` (see R6). Slot `{settlement}` once per wording, never as the opener. Four vocabularies: granary/sick-house/sent to neither/no such place · store of food/house of the sick/no direction/holds neither · grain store/sickroom/directed to neither/neither building · where the grain is kept/where the sick are taken/no answer to either/goes by either name.

**REFUSALS — six, unchanged in substance from round 1 and re-declared. Every wording is written and none is trimmed (§22).**

1. **R1 · POOL-WIDE: `may NOT: a second fact`.** The card licenses exactly one claim, that the predicate holds. The predicate is itself a conjunction of two absences, so K1 and K2 alone are two facts on the card's plain words, and every variant carries one to two more beyond them. Nothing can be dropped: arm A6 requires the four faces to be claim-equal to the variant as it stood, and §22 (a)/(e) forbids removing what a sentence stood for. The same card/register conflict is on record from the `granary AND parish care only` and `Invasion & War: walls AND professional garrison` packets: the register card (amendment S2) permits two sentences and a second fact in its own sentence; the licence card forbids a second fact outright. On the card's plain words no wording in this pool is lawful; on the register's, all twelve are. The chair's ruling is owed and this packet decides nothing.

2. **R2 · The card contradicts itself on the standpoint, and variant 3 is the one it convicts.** `angle:` names `ledger street visitor`; `may NOT:` bars `a standpoint`. Variant 3 is a visitor standpoint by its tag, which is not mine to touch, and its only claims are the stranger's asking, the stranger's being sent on, and the lack behind both. If `may NOT` governs, variant 3 cannot be made lawful in any wording and the pool collapses to two variants, which is a trim by another route, so the ruling matters. Written under the `angle` clause.

3. **R3 · Wall 3 is UNSATISFIABLE for this pool, and this is the pool's structural refusal.** `ABSENCE never opens and never sits beside another ABSENCE` (MOVE-GRAMMAR §1.4 wall 3; R-DA-08; CL-12). This pool's predicate IS two absences, so every wording that carries K1 and K2 places one ABSENCE beside another, and claim-equality requires every wording of variant 1 to carry both; and the pool's first licensed fact is a lack, so a wording that does not open on an absence must open on the thing the town does not have, which is what all four of variant 1's wordings do (`No food`, `Against a bad year`, `neither a store` after the slot, `Nothing is laid up`). No rewording cures either limb; a pool whose state key is a double negative cannot obey a wall written for a record that mostly holds facts. The honest dispositions are a scoped exemption for double-negative state keys or a walker channel that reports rather than fails on them; the choice is the chair's.

4. **R4 · Variant 1's K3 and K4 are consequences with no consequence field.** The card refuses `a cause`; K3 asserts the absence of an interval between a failed harvest and hardship, and K4 asserts the course a sickness runs where nothing stops it. Both are the parent's own claims, both are structural rather than historical (R-DST-B holds: no wording says a harvest failed, a plague came, or a town suffered — every wording is a standing conditional of the arrangement), and both are kept in all four wordings. **Held, not refused:** no wording takes the future indicative. K3 and K4 are written as present generals or edges (`is hardship at once`, `comes with`, `runs its course`, `is want at once`, `is waited out`, `ends unaided`, `burns itself out`), so STATE never FATE holds and `may NOT: a future` is not breached.

5. **R5 · Variant 2 touches `a season` and the near edge of `a count`.** K5's unit is a bad season and the card refuses a season; the parent's own noun is `season` and it is kept in three of the four wordings, with the fourth carrying it as `Hunger` so the annex holds both. K7's counting is an act with no number written and no number implied, which is the nearest a wording in this pool comes to `may NOT: a count`; it is the parent's claim and is kept. **Held, not refused:** what the counting counts is left unstated in every wording, exactly as the parent left it, so nothing is invented and no totality over persons is asserted.

6. **R6 · A stated cause in variant 3, kept once.** The parent's `because there is neither` asserts the lack as the reason for the refused direction, and the card refuses a cause. The claim is kept in all four wordings; the causal JOINT is written in exactly one (`because the town holds neither`) so the parent's construction survives in the annex, and the other three set the lack beside the refusal without asserting the link (`The town keeps no such place`, `Neither building stands in the town`, `Nothing in the town goes by either name`). That is the same claim with the causal assertion unstated, not a claim removed. If the chair rules the joint out, three wordings already stand without it.

**Laws tested and held across all twelve wordings.**
- No em dash, no exclamation, no question mark, no digit, no percent, no colon, no semicolon, no parenthesis, no `which` anywhere in the pool.
- No first or second person, no persona, no address to the reader, no imperative. Variant 2's answer is never written as an instruction, which would have read as second person.
- No future indicative and no modal `will`; every edge is a present general.
- No citation of any kind. `source: (none) · SOURCE-UNRESOLVED` licenses none, so no wording names a keeper, a roll, a register, an elder or an authority; arm A13 has nothing to fire on, and the provenance ceiling of §24 (one citation per unit, on a licensed holder) is spent at zero. Nobody is named as giving the stranger a direction, which also keeps R2's agentless reading.
- No named character and no fate of one; no theological claim; the `church` term of the read is FALSE, so no church, parish, clergy, prayer or deity appears — the DEITY DOCTRINE is held by omission, and the adjacent `granary AND parish care only` pool's prayer language appears nowhere here.
- No totality over persons and no exemption from a duty. `no one in the town treats the sick` and `nobody here nurses the sick` are the predicate's own lack of provision, not a claim about every person in the town.
- No figure, no simile, no sense verb on an abstraction, no inanimate thing acting with intent. The parent's `burns until it burns out` idiom survives in exactly one wording (variant 1's plain, as `the plague burns itself out`) and the other three render K4 in different words. Declared, inherited, not added.
- `may NOT: another civic object of the class store` — at most one store-class noun per wording: `reserve of food` (v1 face 1), `a store` (v1 face 2), `granary` (v3 plain), `store of food` (v3 face 1), `grain store` (v3 face 2); the remaining seven wordings name none.
- One term for one thing WITHIN each wording (R-DA-22): no wording says both `store` and `granary`, or both `sickness` and `plague`. `the sick` beside `the sickness` is the thread echo §1.4.1 licenses (a noun carried forward) — the first is the people, the second the illness.
- Slot discipline: every wording of variants 1 and 3 carries `{settlement}`, every wording of variant 2 carries none, and each face's slot SET matches its parent's exactly (ARCH §2.5).
- T-F8 and wall 10: no wording opens on the `proper`-typed slot. The BEFORE's variant 1 opened `{settlement} holds no food…`; the settlement-opener count for this pool falls from one to zero. Deliberate, declared, not a trim.
- A1 and R-DA-03: every wording is one or two sentences; no third sentence, no qualification carried as a tail.
- THE THREAD (§1.4.1): in every two-sentence wording the second sentence carries a noun forward — `year` and `sick`/`sickness` (all four of variant 1), `Both` for the season and the sickness (v2 plain), `the thing` (v2 face 1), `each` (v2 face 3), `neither` (v3 plain and v3 face 2), `either` (v3 face 3). No wording changes subject mid-passage and hands nothing back. As this pool is the SPINE, every wording closes on a civic noun or a present condition of one that a modifier can pick up after it: `burns itself out` · `waited out` · `no interval` · `ends unaided` · `comes after` · `gone by` · `are done` · `each is over` · `no such place` · `holds neither` · `stands in the town` · `either name`.
- A11 spread: the twelve wordings carry twelve distinct first-two-word pairs — `No food` · `Against a` · `At {settlement}` · `Nothing is` · `The town's` · `A season` · `Hunger and` · `Between a` · `A stranger` · `A newcomer` · `Whoever comes` · `Strangers at`.
- R-DA-05 rhythm: word counts with the slot as one word run 34 / 29 / 32 / 28 for variant 1, 22 / 30 / 21 / 27 for variant 2, 21 / 25 / 22 / 29 for variant 3. Longest wording thirty-four words, shortest twenty-one; longest sentence twenty-five words, shortest six (`The town keeps no such place.`).
- R-DA-04 close kinds vary inside each variant: variant 1 closes on a course, a condition, a place and a measure, a condition; variant 2 on a sequence, a completed span, a completed act, a completed span; variant 3 on an absence, an absence, an absence, a name not given. Variant 3's four closes are the narrowest set in the pool, because K10 fixes the lack as the last claim in a claim-equal family; declared, not hidden.
- Sibling distance (arms A1, A11): no wording restates or contradicts `granary AND hospital`, `granary AND parish care only`, `granary, NO medical provision` or `NO reserves, hospital present`. The siblings' phrases are avoided throughout — `somewhere to put the sick`, `a place for grain`, `a place for the ill`, `a full store`, `a modest infirmary`, `clergy who tend the sick`, `outlast a hungry year`, `a sick one`, `nothing at all against disease`, `spreads until it stops of its own accord`, `hardship the same season it happens`, `better prepared for the sickness than for the hunger`, `catastrophe`, and every word of the parish family. `against a bad year` is this pool's own parent's phrase and is kept.

**Declared consequences of the rewrite (not refusals).**
- **The which-tail is gone**, and it is the round's moved measure: `shapes.whichTailRate` 1.0000 → 0.0000 on every face once the rows land.
- **The semicolon is gone.** Round 1 spent one (v1 face 1); at 3.4 band-widths over on a two-sentence unit it is not affordable at the entry grain, and the two facts it joined now stand in their own clauses.
- **The antithesis is gone.** Round 1's `rather than after it` cost 14.4 band-widths; K3's denial of sequence is now carried by `at once`, `comes with`, `with no interval` and `at once` in the four wordings.
- **The pronoun closer is gone.** Round 1's `Neither of them stands here.` closed on `here`; variant 3's plain now closes on `place`.
- **Zero settlement-openers, down from one.** Wall 10 is satisfied at zero rather than at one, and the settlement token's register-wide share falls by one occurrence from this pool.
- **Two single-sentence faces, down from four.** The compressed faces are kept (v2 face 2, v3 face 1) because the density law makes compression part of the ceiling; the other two were made two-sentence to bring `neighbourVariation` into its band rather than to make them plainer.
- **The pool is banked as unlawful on the card's plain words** (R1, R2, R3) and lawful on the register's. All twelve wordings are written to the ceiling within that failure and no new failing state is added (§21.2, §21.3). The banked count for this pool is three variants and twelve wordings; nothing here should be read as evidence that the pool is lawful.
