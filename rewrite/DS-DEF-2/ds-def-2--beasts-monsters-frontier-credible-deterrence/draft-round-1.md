Seat: Opus 5 — Fable-unvalidated. Block `DS-DEF-2` · pool `Beasts & Monsters: frontier, credible deterrence` · REWRITE draft round 1.
Written to the marker's `skeleton.md` in this directory, the printed licence card, the register card (S2, S3), RULES-V2 Part B §1 / §16–§16.2 / §18 / §20–§23, MOVE-GRAMMAR §1–§3 and §4.4.1–§4.4.3, CLERK-LAWS §2.4.1 / §2.6.1, ARCH-COMPOSED-PROSE-v2 §2.5 and §8.3, `rulings-DEF2.txt`, and brief ADDENDUM 13 parts A and B (bars W1–W27).
Three variants in, three variants out: the same vids, the same order, the same angle tags, none added, none removed, none merged. One bracketed tag per row; no `[plain]` anywhere.
Paste the block below under the pool's bold heading, in place of the pool's three numbered rows.

**`Beasts & Monsters`: `frontier`, credible deterrence**
1. `[ledger]` Around {settlement} lies frontier country, and the town's works stand in it. A force stands behind the works.
   - `[face]` The works at {settlement} stand on an active frontier. Standing with the works is the town's force.
   - `[face]` At {settlement} the works are carried as standing, and a force with them. The country the works stand in is frontier country.
   - `[face]` A force stands at {settlement}, and the country it stands in is frontier country. What the town has built stands with the force.
2. `[street]` Frontier country lies outside the town, and what stands inside it are the works and a force.
   - `[face]` The works are up and a force stands with them in frontier country.
   - `[face]` In frontier country the town has a force, and that force stands behind the works.
   - `[face]` The town's force stands behind what the town has built, and the country outside is frontier country.
3. `[counterforce]` Against frontier country {settlement} has the works, and behind the works the town's force.
   - `[face]` At {settlement} the works are not without a force, and the country outside them is frontier country.
   - `[face]` Outside {settlement} the country is frontier country, and inside the town a force stands with the works.
   - `[face]` In the frontier country around {settlement} the town's force stands with what the town has built.
--- NOTES

## A. THE CARD, AND THE THREE CLAUSES EVERY CLAIM IS KEYED TO

Printed read-only in `laneRW-DEF2`: `node scripts/prose-licence-card.mjs DS-DEF-2 'Beasts & Monsters: frontier, credible deterrence'`.

- **MAY CLAIM** (verbatim): *that the reader `beastsRowSituation(family, perimeter, force)` selects the row `frontier country, perimeter and force` of `BEASTS_ROW_POOL` in `defenseStateProse.js`, as a STANDING fact of the record.* The selected row has three limbs, and this packet keys every claim in every face to one of them:
  - **C1** — the `family` limb: the country is **frontier country**. Layer NONE (a country-tier token; it names no body). Stated over the COUNTRY, never as a totality over the town (W2; C7: the war, crime, economy and disaster rows sit on the same tab).
  - **C2** — the `perimeter` limb: **the works stand** (at least one `walls`-bucket member). Layer BODY. Spoken only by the always-safe class words "the works" / "what the town has built" (W15; entailment D-6: the class has no safe geometric generic).
  - **C3** — the `force` limb: **a force stands** (`garrison || militia`). Layer BODY. Spoken only by the class words "a force" / "the town's force" (ADDENDUM 13 A.5; W12/W13/W15 bar "the garrison", "the militia", "the watch", "the guard", "soldiers", "townspeople" on a key that reads garrison OR militia and never the watch).
- **THE KEYED CONDITION (R-v, `rulings-DEF2.txt`).** The reader returns this row only when all three limbs hold, so C1 + C2 + C3 are ONE state, not three facts. A face stating all three states the state the key names and spends no "second fact"; the card's may-NOT "a second fact" refuses a fact OUTSIDE the three (a pay gate, a stock, a gate, a watch, a charter, a count, a badge).
- **MAY NOT** (verbatim): a count, a cause, a season, a future, a standpoint, a second fact. **REFUSED COLUMNS:** a totality over persons; an exemption from a duty; a named character and that character's fate; a theological claim.
- **BAG:** `{band: RESERVED, route: proper, settlement: proper}`, FILLED at this block's call sites `{settlement}`. Every face carries exactly its parent's slot set: vid 1 `{settlement}`, vid 2 `{}` (no slot in any face, as the shipped row), vid 3 `{settlement}`. `{band}` and `{route}` are named by no face; `{defwork}` is not in this block's bag and is named by no face.
- **FORM:** sentence. **ROLE:** spine (no relation, no attach, no consequence clause: S2's clause seat has no RELATION row to sit on here).
- **SOURCE:** `muster · standing LICENSED`, spent at ZERO — see refusal 5.

## B. PER FACE — WHICH CARD CLAUSE LICENSES EACH CLAIM

Every face of every variant states all three limbs. No face makes a claim outside C1, C2, C3 and the bag. Order is written as the sequence of limbs; the close names the landing noun and its KIND (R-DA-04).

### Variant 1 · `[ledger]` (vid 1 · slot set `{settlement}` · two sentences · grammar V1 PRESENT)

The ledger is a STANDPOINT and licenses no record noun and no citation (W24); its lawful surface is the office's own formula (R-vi/W7: *entered*, *carried standing*), which one face uses and three do not.

- **1 (parent)** — "Around {settlement} lies frontier country, and the town's works stand in it. A force stands behind the works."
  - `frontier country` = **C1**, stated as the country's own condition. `the town's works` = **C2**; the possessive is seated on the TOWN (W1), never on the works. `A force` = **C3**. `{settlement}` = **CARD-BAG**, and it does not open the face (ARCH §2.5 T-F8).
  - `behind the works` is the key's own conjunction read together — the licensed idiom of backing, not a spatial fact and not a manning (the skeleton's claim row: it becomes unlicensed the moment it is sharpened to "on", "in" or "manning"). Order C1 → C2 → C3. Thread (R-i): sentence two carries the noun *the works* forward. Close: *the works*, an OBJECT.
- **1-a** — "The works at {settlement} stand on an active frontier. Standing with the works is the town's force."
  - `The works` = **C2**. `an active frontier` = **C1** at the label's own engine rendering (W14; the skeleton's fence 1 — the engine prints exactly this phrase for this value). This is the pool's one turn kept from the shipped line (§D below). `the town's force` = **C3**, possessor seated on the town.
  - Order C2 → C1 → C3. Thread: *the works* carried forward. Close: *the town's force*, an OBJECT. Construction: inverted second sentence, the face's difference from its siblings.
- **1-b** — "At {settlement} the works are carried as standing, and a force with them. The country the works stand in is frontier country."
  - `the works` = **C2**; `carried as standing` = **CARD-MAYCLAIM** (the standing fact of the record) in the office's own formula (R-vi) — it cites nothing and names no record noun. `a force with them` = **C3** (the second conjunct elliptical; "them" binds to the works, W1). `frontier country` = **C1**.
  - Order C2 → C3 → C1. Thread: *the works* carried forward. Close: *frontier country*, a CONDITION.
- **1-c** — "A force stands at {settlement}, and the country it stands in is frontier country. What the town has built stands with the force."
  - `A force` = **C3**. `frontier country` = **C1**. `What the town has built` = **C2** in the second always-safe spelling of the wall class (W15).
  - Order C3 → C1 → C2. Thread: *the force* carried forward. Close: *the force*, an OBJECT.

### Variant 2 · `[street]` (vid 2 · slot set `{}` · one sentence · grammar V1 PRESENT)

The street states the standing plainly, because no field holds what the town says or thinks about it (R-iii; R-DA-13's belief-frame floor; W23's fused agent). Its difference from the ledger is the plain idiom and the absence of the office's formula, and no face carries a slot.

- **2 (parent)** — "Frontier country lies outside the town, and what stands inside it are the works and a force."
  - `Frontier country` = **C1**, the country's condition. `the works` = **C2**. `a force` = **C3**. `inside it` binds to *the town* (W1). No slot, as the parent's set requires.
  - Order C1 → C2 → C3. Close: *a force*, an OBJECT.
- **2-a** — "The works are up and a force stands with them in frontier country."
  - `The works are up` = **C2** in the street's plain idiom (presence, never a height, a material, a gate or a manning). `a force` = **C3**. `frontier country` = **C1**.
  - Order C2 → C3 → C1. Close: *frontier country*, a CONDITION. The pool's shortest face.
- **2-b** — "In frontier country the town has a force, and that force stands behind the works."
  - `frontier country` = **C1**. `the town has a force` = **C3** (possession as a standing fact; the town is not an agent that decides, W23). `the works` = **C2**.
  - Order C1 → C3 → C2. Close: *the works*, an OBJECT. `that force` carries the noun across the joint.
- **2-c** — "The town's force stands behind what the town has built, and the country outside is frontier country."
  - `The town's force` = **C3**. `what the town has built` = **C2**. `frontier country` = **C1**.
  - Order C3 → C2 → C1. Close: *frontier country*, a CONDITION.

### Variant 3 · `[counterforce]` (vid 3 · slot set `{settlement}` · one sentence · grammar V1 PRESENT)

The counterforce sets the PRESSURE beside the THING STANDING AGAINST IT and settles nothing (the skeleton's fence 3, from the block's own subtitle: *the five pressures, and what the town actually has standing against each*). It states no deterrence, no cause, no forecast, no frequency of arrivals, no mechanism and no badge word; the reader draws what the key is named for.

- **3 (parent)** — "Against frontier country {settlement} has the works, and behind the works the town's force."
  - `frontier country` = **C1** (the pressure side). `the works` = **C2**. `the town's force` = **C3**. `Against` is the block's own frame word for what stands against a pressure, an orientation and not an effect.
  - Order C1 → C2 → C3. Close: *the town's force*, an OBJECT. The second conjunct is elliptical, the face's construction.
- **3-a** — "At {settlement} the works are not without a force, and the country outside them is frontier country."
  - `the works` = **C2**; `not without a force` = **C3** as the pool's ONE negated surface on a present read (W10: never the opener, at most one). `frontier country` = **C1**; `outside them` binds to the works (W1).
  - Order C2 → C3 → C1. Close: *frontier country*, a CONDITION.
- **3-b** — "Outside {settlement} the country is frontier country, and inside the town a force stands with the works."
  - `frontier country` = **C1**. `a force` = **C3**. `the works` = **C2**. The outside/inside pair is the counterforce's two sides stated as position, never as a line, a circuit or an enclosure (entailment D-6).
  - Order C1 → C3 → C2. Close: *the works*, an OBJECT.
- **3-c** — "In the frontier country around {settlement} the town's force stands with what the town has built."
  - `the frontier country around {settlement}` = **C1**. `the town's force` = **C3**. `what the town has built` = **C2**.
  - Order C1 → C3 → C2. Close: *what the town has built*, an OBJECT (a name not given in its own noun — the wall class spoken by its safe generic). The pool's one face that states the counterforce as containment rather than opposition; recorded as a reading judgment at refusal 3.
