1. `[ledger]` A citizen militia at {settlement} answers a raid on its own ground, in place of a professional garrison. Around the town nothing is built for that muster to stand behind.
   - `[face]` At {settlement} the muster entered against a raid is townspeople, not a professional garrison. That muster turns out on ground of its own, and no wall stands around the town.
   - `[face]` Against a raid the citizens of {settlement} turn out on their own ground, and no professional garrison is kept. Nothing the town has built stands around them.
   - `[face]` Citizens of {settlement}, not a professional garrison, make the muster that answers a raid on their own ground. The town has built no wall around itself.
2. `[street]` The town's own people make the muster at {settlement}, part-time and not a professional garrison. They come to that muster off their own work, and nothing is built around the town for them to hold.
   - `[face]` Part-time and off their own work, the townspeople of {settlement} make the muster instead of a professional garrison. They have no wall to hold.
   - `[face]` The muster at {settlement} is part-time and the town's own, not a professional garrison. The people who turn out come off their own work, and the town has built nothing for that muster to hold.
   - `[face]` Townspeople are what turns out at {settlement}, part-time and not a professional garrison. Mustering takes them off their own work, and no wall around the town is theirs to hold.
3. `[visitor]` A stranger at {settlement} finds the town's own people at their work and no wall around the town. Those people are a part-time citizen militia in place of a professional garrison.
   - `[face]` Townspeople at their ordinary work are what a stranger sees at {settlement}, and no wall stands around the town. The muster they make is part-time and not a professional garrison.
   - `[face]` What a stranger comes upon at {settlement} is the town's own people at their work, and no wall around the town. They muster part-time, instead of a professional garrison.
   - `[face]` The people a stranger passes at {settlement} are at their own work behind no wall. Their muster is part-time, not a professional garrison.

--- NOTES

CURER: Opus 5 (CURER seat, Fable-unvalidated; a different author from the drafter and the refiner), for the Fable chair, 2026-09-11. Cure round 1, pool `Invasion & War: militia only`, block DS-DEF-2. Nine targets, nine cured, none refused. The three untargeted faces (v3 f0, f1, f3) are byte-identical to the annex at HEAD `471ce894a` (`diff` run against `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2750-2761`: lines 9, 10 and 12 identical, lines 1 to 8 and 11 the nine targets). Grammars kept per W4 as the draft declared them: v1 V3 (PRESENT then LACK, the sibling contrast riding on the PRESENT move), v2 V2 (PRESENT then CONSEQUENCE(structural)), v3 V3 closing on the sibling contrast (the one variant of the pool licensed to close on it, wall 5). Every face still carries all three reads of the card (R1 walls ABSENT, R2 militia PRESENT, R3 garrison ABSENT) plus the shipped sentences' licensed residue, and the four faces of each variant stay claim-equal to each other (arm A6). No claim added; `{settlement}` is the only slot in all twelve rows; no digit, no em dash, no exclamation, no which-clause, no citation, no record noun.

v1 f0 — CURED. The judge's named cure taken as written: the three adjuncts hanging off `answers` are broken at the second, R1 becomes its own flat sentence, and `for that muster to stand behind` replaces the `it` that could bind to `the town` (R-DA-03; W1). Claims: the militia present and raised, R2, the recorded row `Citizen militia` through the bucket; `a raid` the row's recorded purpose (D-8, the `Emergency defense` service at p 1.0 and the village desc); `on its own ground` on the LOCATION reading only, the skeleton's floor turn kept verbatim; `in place of a professional garrison` the sibling-licensed contrast, the key's own rejected alternative `no walls, professional garrison` (R-DA-02); R1 flat, not the unit's opener, not adjacent to R3's contrast, in the exact `what the town has built` negation A-7 rules exact. Thread: `that muster` co-refers with `A citizen militia` and `the town` with `{settlement}` (R-i, J-1).

v1 f1 — CURED, and the variant's `[ledger]` stance realised here (the second half of this target's finding: the stance was realised in none of the four faces). R1 no longer rides as a circumstantial on the capability clause, so the lack cannot read as the completing half of a contrast (R-DA-02 with R-DA-03), and the `it` that sat one word after `a professional garrison` is gone (W1). `entered` is the office's own formula and the ledger's lawful surface, with no record noun and no citation beside it (R-vi, W7, W8, W24; the provenance budget on this key is zero, S3's three reasons none of them holding). `is townspeople` is the bare plural, not the definite class: the co-extension the judge convicted four times in the sibling `walls with CITIZEN MILITIA` pool is not installed here. Claims: R2 at the person-plural grain (D-8 HOLDS: townspeople); the raid; R3 as contrast; `on ground of its own` seated on `turns out`, the skeleton's own safe surface for the location reading, so the advantage sense D-8 refuses as competence is not reachable; R1 flat. Thread: `That muster` and `the town` carried forward (R-i, J-1). Construction distinct from its three siblings in subject, read order and landing.

v1 f2 — CURED. The restating clause `Those citizens stand` is gone, so the beat that existed only to carry R1 is gone with it (W6), and the lack now carries the thread inside itself as the judge's cure directs. The judge's model sentence is FOLLOWED IN SHAPE, NOT COPIED: copying it would land f2 and f0 on the same ten words, which is the A5 / R-DA-05 ground this same judgment convicts at v2 f2. Claims: R2 and the raid and the ground unchanged from the annex's first sentence (`turn out on their own ground` is the one surface the refuter's read-aloud note found lands as a place); R3 as the flat LACK form (`no professional garrison is kept`), separated from R1 by the whole of the second sentence (wall 3); R1 in the `Nothing the town has built` negation (A-7, exact over every wall-bucket member; W11 cannot bite, there being no wall to name a material of). Thread: `them` binds to `the citizens`, the only plural in the first sentence (W1).

v1 f3 — CURED. The contrast is re-seated off `answers a raid` and onto the muster's makers, so the engagement claim the rewrite exists to drop cannot be restored by attachment (the card's may NOT: another civic object of the class `force`, a future; W25; R-DA-02), and the face is re-cut on a construction face 0 does not use (a fronted appositive contrast on the person-plural subject; A5). Claims: `Citizens` the recorded name's own word (`Citizen militia` on all three tiers, constant across the class); the muster made by them, R2; the raid; the ground on the location reading; R3 through the sibling key's own word; R1 in the `has built no wall around itself` form the skeleton lists as safe. Thread: `The town` co-refers with `{settlement}` (R-i, J-1). The face does not close on the contrast, which wall 5 reserves to variant 3 alone.

v2 f0 — CURED. `It` is replaced by the named noun, so the licensed entailment no longer hangs on `a professional garrison`, the nearest antecedent (W1); `They` binds to `The town's own people`, the only plural before it, and `them` to the same. Claims: R2 as the town's own people making the muster (D-8 HOLDS: townspeople); part-time (HOLDS, constant across the three recorded rows, bakeable); the work they come to it off (D-8 HOLDS: raising them takes them off other work) as the variant's declared CONSEQUENCE(structural) move, a standing fact and never an event (R-DST-B); R3 as contrast; R1 with the capability clause the block's PROVENANCE fence licenses (`nothing built for them to hold`), never a historical or outcome clause.

v2 f1 — CURED. Re-cut on the construction the judge named, which face 0 does not use: the two entailments fronted, the muster and the contrast in the main clause, the lack in its own short sentence. The judge's model landing (`The town has built nothing for them to hold`) is NOT copied, because it would collide with this judgment's own cure for f2 and reinstate the A5 ground; the lack is instead stated on the people, in a surface no sibling face carries. Claims: part-time; off their own work (both D-8, MAY-entailments carried by every face of this variant so the four stay claim-equal); R2; R3 through the sibling key's word `instead of a professional garrison`; R1 as the flat LACK with the capability reading the fence licenses (`no wall to hold`). Thread: `They` binds to `the townspeople of {settlement}` (W1, R-i).

v2 f2 — CURED. R1's surface is varied on this face only, exactly as the cure directs, so the pool no longer lands two faces of one variant on the same ten words (A5; R-DA-05, the pool as the unit of spread). The possessive `Its people`, whose nearest antecedent was `a professional garrison`, is replaced by a plural class noun with a restrictive relative, which is the person-plural grain D-8 licenses and not a person as a referent (W22 bars `the person who`, `whoever`, a singular agent; a plural class word is safe). Claims: R2 as the muster being the town's own; part-time; the work; R3; R1 in the `the town has built nothing` form, landing on `that muster`, which also carries the thread forward from the first sentence (R-i, J-1).

v2 f3 — CURED. The one sentence carrying four stacked adjuncts is broken in two (R-DA-03: never a tail, never a third), and the cleft's copula now agrees with its plural complement (`Townspeople are what turns out`), with the bare plural keeping the class and the force from reading as co-extensive (the judgment's own ruling on the sibling militia pool). The judge's second sentence is followed in shape, not copied: `They come to it off their own work` is this judgment's cure for f0 and would restore both the A5 collision and the `it` binding it exists to remove, so the work entailment is stated on the act of mustering and the lack on the wall. Claims: R2; part-time; R3; the work (D-8); R1 flat. Thread: `them` and `theirs` bind to `Townspeople` (W1, R-i).

v3 f2 — CURED. The demonstrative that pointed at no antecedent is gone: the second sentence now takes `They`, which binds to `the town's own people` standing in the face's own first sentence (THE THREAD, MOVE-GRAMMAR 1.4.1, R-i, J-1), and the clause is re-cut as a verbal predication so it is no longer face 1's second sentence with its subject swapped (A5). Claims: part-time (D-8 HOLDS) and R3 through the sibling key's word; R1 and R2 stand unchanged in the face's first sentence, which is byte-identical to the annex. The face still closes on the sibling contrast, which is variant 3's licensed close and no other variant's (wall 5).

POOL-LEVEL, carried for the re-refutation (information, not verdicts):
- `answers a raid on its own ground` stands in v1 f0 and f3 because both are the judge's named cures, written after the refuter recorded (its section 3) that the idiom pulls toward the advantage sense the skeleton licenses only as a place. The hazard is therefore a chair-level ruling carried forward, not an uncured finding; in the two v1 faces the curer was free to re-seat (f1, f2) the phrase sits on `turns out`, the skeleton's own safe surface.
- R1's surface pool is law-bounded, as the refuter recorded: `no perimeter` and `no line` are false of a citadel and are the force word on this desk (E-6, A-7), `unwalled` and `no works stand about the town` are the neighbouring `force with NO walls` pool's discriminating words, and no material word is available until `{defmaterial}` is minted (W11). The four surfaces now standing in variant 1 (an existential passive, a copular `stands`, a relative-clause subject, a transitive perfect) and the four in variant 2 are as far apart as those bounds allow; two of them share the four words `the town has built`, in different syntactic roles, which is recorded rather than cured.
- F-1 (`armed` on a `Citizen militia`) is still unruled by the chair and no face says it; nothing in this round turns on the ruling.
- No target was refused.
