# SKELETON — DS-DEF-2 · `Invasion & War: walls AND professional garrison` (the marker's packet; Fable, 2026-09-11)

Seat: Fable 5.1 — marker for the REWRITE, working for the Fable chair. This file is the only thing this seat wrote. Everything below is judged against the printed licence card, the census row it was printed from, the reader function the card names, the annex block's header lines, the register card (with S2 and S3), Part B §1 and §16–§24, MOVE-GRAMMAR §1–§3 and §4.4.1–§4.4.3, CLERK-LAWS §2.4.1 and §2.6.1, ARCH v2 §2.5 and §8.3, brief ADDENDA 6–13 (the density floor, the skeleton phase, the entailment law, the referent law, the writer bars W1–W27) and `rewrite/rulings-DEF2.txt`; where a shipped clause reads well but no field licenses it, it is UNLICENSED here and the law is named.

Status: COMPLETE — sections 0, variants 1 to 3, 8 and 9 all written (the checkpoint law was followed section by section; nothing is pending).

## 0. THE CARD, THE KEY, THE THREE INPUTS AND THEIR LAYERS (what the faces are wired to)

### 0.1 The licence card as printed (`node scripts/prose-licence-card.mjs DS-DEF-2 'Invasion & War: walls AND professional garrison'` in laneRW-DEF2, read-only)

```
LICENCE (block DS-DEF-2 · role spine · key `Invasion & War: walls AND professional garrison`)
  reads:      invasionRowSituation(walls, garrison, militia) (via INVASION_ROW_POOL in defenseStateProse.js)
              (absent ⇒ no candidate; a modifier is silent, never "false")
  predicate:  invasionRowSituation(walls, garrison, militia) (via INVASION_ROW_POOL in defenseStateProse.js) === walls, professional garrison
  bag:        {band: RESERVED, route: proper, settlement: proper}
              FILLED at this block's call sites: {settlement}
  relation:   (a spine takes no relation)   ← a spine IS the seat and carries no relation; the relation is the MODIFIER's property, fixed at its freeze (ARCH §4.5)
  seat/form:  (not a seat-taker) / sentence      move: (none declared)     angle: ledger street visitor
  attach:     (empty: a spine takes no attach set)
              n/a
  echo:       spine mounts 1 (tabs: defense) · modifier mounts 0 (none)
              the echo table is keyed on this pool's WHOLE table-rung reading (truncated at the file's first dot) and NOT on a producer-token root, so every pool that selects a row of `INVASION_ROW_POOL` shares ONE echo key: a mount counted there may be a sibling ROW of the same table
  covert:     no
  source:     muster · standing LICENSED
              a citation of this holder is licensed where the provenance budget allows
  may claim:  that the reader `invasionRowSituation(walls, garrison, militia)` selects the row `walls, professional garrison` of `INVASION_ROW_POOL` in `defenseStateProse.js`, as a STANDING fact of the record
  may NOT:    a count, a cause, a season, a future, a standpoint, a second fact, another civic object of the class `force`
  audience:   player (no mark)
  REFUSED COLUMNS, always: a totality over persons; an exemption from a duty (whoIsExempt is null everywhere); a named character and that character's fate (product scope); a theological claim about a deity (the deity doctrine)
```

The census row behind the card (`docs/content/wiring-census.json`, block DS-DEF-2, this pool): `status RESOLVED` · `rung table` · `keyFunction INVASION_ROW_POOL` · `readsGrain branch` · `reads` = the one synthetic label above (`fieldsRead` and `branchReads` the same) · `narrowed false` · **`objectClasses ["force"]`, `objectClass "force"`** (this card, unlike the Beasts siblings', REFUSES "another civic object of the class force" — see §0.3 on what that fences) · `sites ["defense.threatAssessment"]` · `k 2` · `rateBp 3984` · `source {kind: muster, fields: {walls: "muster", garrison: "muster", militia: "muster"}, holder: null, holderReason: "town-resolved: the register is not a town, so the holder is named by holdersOf(kind, settlement)", standing: LICENSED, twoSource: false}` · `variants 3` · `grammars 2` · `slotsFilled ["settlement"]` · `slotsNamed ["settlement"]` · `slotsWithoutProvider []` · `covert false` · `absent {}` · `attach []`. No `dm-only` mark on any of the three rows.

### 0.2 The one read, unpacked to its three inputs (`defenseStateProse.js:476-483` the reader; `:490-492` the key; `:623` and `:656` the caller; `defenseInstitutionBuckets.js:83-104` the buckets) — read for the VALUES at this key, not as a licence of its own

The caller reads `const forces = standingDefenseForces(settlement)` — the LIVE roster, ruin-filtered (the file's own ⛔ at `:604-611`: a flattened citadel does not read here as standing walls) — and passes `walls`, `garrison`, `militia` as the three `.present` booleans to `invasionRowPoolKey(walls, garrison, militia)`. The reader:

```
if (walls) {
  if (garrison) return 'walls, professional garrison';
  return militia ? 'walls, citizen militia' : 'walls, no force';
}
```

So (chair, rulings-DEF2 R-v, from ADDENDUM 8 ruling 1) the key is ONE KEYED CONDITION of TWO inputs: `walls === true` AND `garrison === true`. **The third input, `militia`, is NEVER CONSULTED on this branch** (the reader's own docblock: "A GARRISON OUTRANKS A MILITIA … `garrison` is read before `militia` on both sides of the wall"). A face therefore states two bodies and says NOTHING of a militia — neither "no militia" (false on an imported or toggle-forced roster holding both; the key does not deny it) nor "the militia" (a second force object the card refuses). The card's "a second fact" refuses facts OUTSIDE the two (a pay gate, a stock, a watch, a charter, a count, a badge).

| input | how the caller derives it | value at this key | REFERENT LAYER of the read (ADDENDUM 13 B A.1) | the lawful nouns | the unlawful nouns |
|---|---|---|---|---|---|
| `walls` | `forces.walls.present` — the `walls` bucket = `wall · citadel · palisade · earthwork · inner citadel · massive walls` (`:84-87`); shipped members that can resolve here: `Town walls` (town, bc 0.5), `Gates (if walled)` (town, bc 0.5 — caught through the substring `wall` inside "walled"; a GATE row with no wall row still sets the bucket, entailment D-5), `City walls and gates` (city, REQUIRED), `Citadel` (city; an INNER fortress) | `true` | **BODY** — the walls bucket (an institution row the read reaches by the bucket); HOLDER kind muster on the census's `fields.walls`, resolving `holder: null` here (the wall's record is the muster ROLL, whose only roster holder is a `Citizen militia` — referent table 2.1 walls row; OV-5) | "the works", "what the town has built" (always safe: entailment D-6, W15, rulings-DEF2); "the wall" as the CLASS word (referent table 2.1: "the wall · the perimeter (HOLDS)" as class words) — accepted here only where the writer accepts that a `Gates (if walled)` town reads a gate and a post-ruin citadel city an inner fortress under that word; this seat recommends "the works" | "a line", "a perimeter", "a circuit", "the enclosure" AS GEOMETRY (continuity NOT ENTAILED; false of `Citadel` and `Gates`, D-6, D-4, W15); a material (W11: "stone", "timber", "masonry" until `{defmaterial}` is minted; and the shipped chrome for THIS branch calls every member a palisade, C-11); height, thickness, a gate, a walkway, towers, a moat (D-3); "held", "manned", "defended", "people on it" (manning NOT ENTAILED; "the men on the wall" struck at ADDENDUM 13 A.5); age, condition, who built it, whether it has held (D-3; R-DST-B: no history off a presence field); `{defwork}` (NOT in this block's bag, ARCH §2.5: a face whose slot set differs from its parent's is refused) |
| `garrison` | `forces.garrison.present` — the `garrison` bucket = `garrison · barracks · professional guard · professional city watch · multiple garrison` (`:88-91`); shipped members that can resolve: `Garrison` (city, REQUIRED — "professional soldiers answering to a noble or royal authority", D-7), `Professional city watch` (city, REQUIRED — the SAME row sits in the `watch` bucket too, OV-1), `Barracks` (town, bc 0.3 — a HOUSING row, D-7), `Multiple garrisons` (metropolis) | `true` | **BODY** — the garrison bucket; HOLDER kind muster (the card's `source`), `holder: null` on this row (a garrison town "has men under arms and no roll of them", `holderTable.js:280-288`, OV-5) | **"the soldiers"** (the person-plural class word the ratified law names safe: ADDENDUM 13 B A.5 "the person-plural class word ('the town's force', 'the soldiers')", W12's own "or uses 'the soldiers'", W25; referent table 2.1 row `soldiers`: "the soldiers · the men — the one always-safe body spelling the refuter finds") — and it CARRIES THE KEY'S DISCRIMINATOR by the word's meaning (a soldier is a man whose work is arms; a militiaman is a townsman turned out — entailment law item 1); **"the town's force"** (always safe, ADDENDUM 13 A.5; loses the discriminator); **"the muster"** as the CLASS word for the paid military (the chair's ruling, A.5; OV-34: "survives as a BODY word on a body read with the possessor seated" — "the town's muster"; a class word asserts no roll) | **"the garrison" BAKED** (W12 THE GARRISON BAR: only where a `Garrison` / `Multiple garrisons` row resolves — every city, yes; a `Barracks` town fires THIS KEY with no such row, and a face is written once for every town the key fires on, so a baked "the garrison" is a breach on the Barracks towns until a derived force-name fill lands — §0.7 fence 1); "professional(s)" / "professional garrison" sharpened past the label's ENGINE meaning (W14: the label means "the garrison BUCKET resolved, not the militia bucket"; the dictionary sense — career, drilled, in pay, full-time — is entailed of the `Garrison` row (D-7: "paid" CONDITIONAL from "in noble or royal pay") and NOT of a `Barracks` (housing)); "paid", "wages", "upkeep", "the keeping" (a pay-gate read this card does not carry — DS-DEF-11's cell; W16 licenses the gate's words only on a gate read); "the militia" (unread); **"the watch"** (W13; and at city it is the same ROW as a garrison keyword, OV-1 — naming it names a SECOND force object, which the card's `objectClass force` refuses; rule 3 SAME ROW: a body read and an organ read on one row need the layer change stated, which no face here may do); "the guard" (struck, ADDENDUM 13 A.5, X-D2); "the men on the wall" (struck); "the muster roll" / "the roll" as a RECORD (OV-5: only with a resolved `Citizen militia` + `Muster training` — never on this key's holder); a count, a strength, "thinning", "at full strength" (the military SCORE, DS-DEF-5's cell; R-DST-A); tenure, a commander, loyalty, whose pay (D-7 NOT ENTAILED); "the men" — accepted by the referent table as a safe spelling, but a gendered word with no sex-ratio field (alias A-6 on WARTIME v4); this seat prefers "the soldiers" |
| `militia` | `forces.militia.present` — passed, NEVER READ on this branch | (unread) | — | (nothing: the face is silent on a militia) | "the militia", "townspeople under arms", "no militia", "not the townspeople" (a contrast naming the sibling key `walls with citizen militia` is licensed by R-DA-02 only as a rejected alternative the key DENIES — and this key does not deny a militia, it does not read one; the negation is a claim no field holds) |

**Where the key fires (for the writer's ear, not as a licence).** Every natively generated CITY (`City walls and gates` and `Garrison` are both REQUIRED at city, R-A) and every TOWN holding a walls-bucket row (`Town walls` or `Gates (if walled)`, each bc 0.5) with a `Barracks` (bc 0.3); `rateBp 3984` on the RATE sample. No tier read is on the card, so W3's band marker is not owed and no tier word may be baked ("a town or larger" would be a claim the card lacks).

**What the KEY'S NAME licenses: only its engine meaning.** "walls, professional garrison" is the reader's own row label; under W14 a face renders the ENGINE meaning of a label — the walls bucket AND the garrison bucket resolved — never its dictionary sense (a drilled, paid, career force). ADDENDUM 8 ruling 2 ("the block's title is its name, not a licence") applies to everything the corpus glossed onto the label: deterrence, raiding, a conventional assault, a siege, stores, a rating. The engine's own chrome for this branch (`threatAssessment.js:115`: "Walls and professional garrison provide meaningful deterrence against raiding and conventional assault. Not rated for sustained siege without significant supply stockpiles.") is PRODUCT CODE outside the REWRITE's corpus (ADDENDUM 13 B D, OW-20) and is not a licence for the same words in a face; it is where the shipped v1 got both its clauses.

**`objectClass force` — what the card's last may-NOT fences.** The census files this pool's key under the civic class `force` (`wiringCensus.js:1303` flattens `garrison · militia · muster · watch · guard · soldier · patrol · armed`, OV-25). The card refuses "another civic object of the class `force`": a face names ONE force body — the garrison bucket's, by a lawful spelling above — and never a second (the militia, the watch, the guard, a levy, a patrol, mercenaries, the charter hall, "the men on the wall" beside "the soldiers"). The projector's class guard cannot tell one force noun from another (OV-25), so a face that spells the one body two ways in one sentence ("the soldiers … the muster") is at risk of reading as two objects; one spelling per face is the safe form.

### 0.3 The block's header lines (annex `RECEIPT_POOLS_DOSSIER_STATE.md`, `### DS-DEF-2`, lines 2568-2596 of the laneRW-DEF2 copy; this pool's rows at 2630-2633)

- **STATE-KEY:** five fixed rows (`Beasts & Monsters` · `Invasion & War` · `Internal Security` · `Economic Survival` · `Disasters & Famine`), each with a `scoreBand` badge, read against `config.monsterThreat`, the institution presence flags, and `compound.inst`. This pool is the `Invasion & War` row's spine; its badge (`scores.military` banded) is an AGGREGATE read this pool does NOT carry — no face names a band word, a rating, or a posture.
- **SLOTS:** `{settlement}` `{band}` `{route}` — the card: only `{settlement}` is FILLED at this block's call sites; `{band}` RESERVED; `{route}` proper but unfilled here. A face names neither `{band}` nor `{route}`.
- **SECTION-TARGET:** `defense`.
- **PROVENANCE + FENCE:** `buildThreatAssessment` is dossier-native and this shape EXTENDS it into a pool (each branch held ONE string); two defects must not be reintroduced (the `plagued`+nothing lowercase lead; the walls read rides the predicate, never a presence check on `institutions.walls` — the desk's live-roster read at `:623` is the cure's shape); **"institution presence is a STANDING fact with no recorded history; the causal clauses here are CAPABILITY clauses (walls without people cannot be held) and never HISTORICAL ones"** — see §0.7 fence 2 on what a capability clause may say under the card's "may NOT a cause / a second fact".
- **RECEIPT:** `threatAssessment.js:28-195`; rendered `DefenseTab.jsx:150-183`; the walls predicate `causalState.js:300-315`. **PDF PARITY:** parity.
- **Same page (C7; W2; A1/A11; the referent law's rule 3 across the tab):** DS-DEF-2 mounts the Beasts row on the SAME tab, and the Beasts row names the SAME TWO BODIES (its `perimeter` is this pool's `walls`; its `force` is `garrison || militia`, which on this town resolves through the same garrison row). So the two spines must neither restate nor contradict: the works are the works on both rows, and the force is one body (a face here that calls it "the garrison" while the Beasts face calls it "a force" is a spelling drift the reader will read as two things). The Economic Survival row carries the pay and morale readings (a face here says nothing of pay); the Disasters row carries the stores (a face here says nothing of stores — the shipped v1's "without stores behind it" would contradict a `granary AND hospital` row beside it); the Internal Security row carries the court and the gaol (and, on the shipped corpus, an imported "watch" — D-F4). A war pressure on this row beside a quiet Beasts row is the design (W2: no totality over the town's safety on either).

### 0.4 The register card's six one-line registers (read whole; the dossier binds here)

The dossier: the record itself; the clerk's third person; the six shapes of its closed set; the town's name is not the default opener. The NPC ladder, the Herald, the chronicle, the DM page, chrome and the docent do not reach this pool. Amendment S2 (a computed CONSEQUENCE of the sentence's own fact may ride as one clause, the joint's own comma and word from the connectives list, never "which", two sentences at most — and here NO relation row exists on a spine, so NO consequence clause is available) and S3 (every fact has a holder; the office cites one only for an S3 reason; never "according to" by habit) bind every face. The three questions before any sentence ships: which field licenses it; which claims does it carry and are they the same claims as before; what does it leave standing open.

### 0.5 The pool's shipped rows, verbatim (the annex, bold line `**\`Invasion & War\`: walls AND professional garrison**`; leaf vids 1-3)

1. `[ledger]` {settlement} has a line and professionals to hold it, which is real deterrence against raiding and against a conventional assault; it is not a posture rated for a long siege without stores behind it.
2. `[visitor]` A stranger sizing {settlement} up sees the two things that matter together (the wall and the men who belong to it) and revises what an attempt would cost.
3. `[street]` The town believes it could be held, and the belief is founded on something rather than on hope.

Variant count: THREE shipped variants (ledger · visitor · street), matching the card's `angle: ledger street visitor`. Slot sets per parent (a face whose `{slot}` set differs from its parent's is refused, ARCH §2.5): vid 1 `{settlement}` · vid 2 `{settlement}` · vid 3 `{}` (NO slot — every face of variant 3 carries no `{settlement}`). Sentence counts: all three ONE sentence each (vid 1 is one sentence joined by a semicolon and carrying a `which` tail); A11's "where they differed in sentence count they keep differing" binds nothing here, so each variant may be one or two sentences under A1, and the pool's spread is carried by first-two-words, construction and order of the reads. Openers: vid 1 opens on `{settlement}` (a T-F8 breach; a cure, not a floor), vid 2 on "A stranger", vid 3 on "The town".

### 0.6 Findings of the two ratified tables that touch this pool's rows

- No row of REFERENT-TABLE §5.1 (D-F1…D-F28) cites annex lines 2631-2633; the `soldiers` row's `:2617` and `:2648` are the Beasts `frontier, force without a perimeter` and the Invasion `force with NO walls` siblings, not this pool. No row of the ENTAILMENT table's defense section 7 (the "more knowledgeable than the simulation" list) names this pool. The pool is therefore judged here from the card and the laws alone; the sibling findings that bind by shape are D-F25 (a named body on a band read — the reverse here: a band or rating word on a body read, W20), D-F16/D-F17/D-F18 (the town as a deciding, thinking agent — vid 3's "The town believes"), and A-24 (a siege entails a besieger and never walls or an army at the walls — vid 1's "a long siege").

### 0.7 Fences this seat records for the writer and the chair, not decided here

1. **The force's NAME.** W12 licenses "the garrison" only where a `Garrison` / `Multiple garrisons` row resolves; this key fires on every city (where one does) and on Barracks towns (where none does). A face is one string for both. This seat reads the ratified law as it stands and licenses the person-plural class word "the soldiers" (and "the town's force", "the town's muster") for every face, refusing a baked "the garrison". If the register car mints a derived force-name fill from the resolved garrison-bucket row (the `{watchname}` shape of CAR 8b-W-2, OW-1), the specific noun becomes lawful through the fill and nothing else moves. RECORDED for CAR 8b-W-2 as a wiring row, not acted.
2. **The PROVENANCE capability clause against the card's "may NOT a cause / a second fact".** The block's header says the causal clauses here ARE capability clauses; the card prints "a cause" and "a second fact" under may-NOT, and the taste's ruling (ADDENDUM 8 ruling 2) is that a conjunction key is not a computed relation. This seat judges by the card: a clause naming ONLY the key's two bodies in the key's own conjunction ("the works, and the soldiers with them") is the keyed state and lawful; a clause that PREDICATES holding, manning, deterrence or an attacker's cost ("to hold it", "who belong to it", "could be held", "what an attempt would cost") adds a manning, a relation or a forecast no field holds and is UNLICENSED. If the chair rules the capability joint lawful for this block, the rows marked "a capability the fields do not hold" below flip; the deterrence, siege, rating, stores and belief rows do not.
3. **"professional(s)".** The label's engine meaning is the garrison bucket; the dictionary sense is entailed of the `Garrison` row and not of a `Barracks`. This seat treats the word as CONDITIONAL and names "the soldiers" as the licensed carrier of the discriminator. If the chair rules the label's own adjective lawful at its engine meaning (as "active" was read for `frontier` on the Beasts sibling), "professionals" returns to the turns worth keeping on vid 1 and nothing else moves.
4. **"the men".** The referent table's `soldiers` row lists "the men" beside "the soldiers" as the safe spelling; alias A-6 records "the men" as a gendered word with no sex-ratio field on the stressors desk. This seat licenses it on vid 2 as the table prints it and recommends "the soldiers".

---

## VARIANT 1 · `[ledger]` (vid 1; slot set `{settlement}`; one sentence, a semicolon and a `which` tail)

**(2) Shipped, verbatim:**
> {settlement} has a line and professionals to hold it, which is real deterrence against raiding and against a conventional assault; it is not a posture rated for a long siege without stores behind it.

**(3) Every claim, one per line (licence · law · referent layer beside the read's layer):**
- {settlement} opens the sentence — **a pool-level fence, not a claim**: a sentence face may not open on a `proper`-typed slot of the block's bag (ARCH §2.5, T-F8; MOVE-GRAMMAR §1.4 wall 10). This opener cannot be carried into any face; moving it is a cure, not a regression.
- the town HAS the works ("has a line") — **LICENSED in substance** the `walls` input (a walls-bucket member stands; layer **BODY** = the read's layer), **UNLICENSED in its noun**: "a line" asserts a CONTINUOUS enclosure the bucket does not entail (entailment D-6: continuity NOT ENTAILED; false of `Gates (if walled)`, which fires this key at town with no wall row, and of a post-ruin `Citadel`; W15 the alias bar). The lawful spelling: "the works" / "what the town has built"; "the wall" as the class word with §0.2's caveat.
- professionals stand ("professionals") — **LICENSED in substance** the `garrison` input (a garrison-bucket member stands; layer **BODY** = the read's layer). **The WORD is CONDITIONAL** (§0.7 fence 3): the label's engine meaning is "the garrison bucket, not the militia bucket" (W14), and the dictionary sense (career, drilled, in pay) is entailed of the `Garrison` row (D-7) and NOT of a `Barracks` (a housing row) on the town-tier firings. The licensed carrier of the discriminator is "the soldiers" (§0.2).
- the professionals are there TO HOLD the works ("to hold it") — **UNLICENSED: a manning and a capability the fields do not hold** (manning NOT ENTAILED, D-3 and D-7; "the men on the wall" struck at ADDENDUM 13 A.5; §0.7 fence 2) and **a purpose** given to a body (an intent no field records). It predicates a RELATION between the two BODY reads that no field computes (W23 by shape: the key is a conjunction of two presences, not a computed relation — ADDENDUM 8 ruling 2).
- the arrangement IS real deterrence ("which is real deterrence") — **UNLICENSED: a VERDICT** (MOVE-GRAMMAR §1.3: VERDICT does not exist; the register rates nothing; R-DA-12's generalisation), **a cause** (the two bodies as the reason attackers are deterred; the card's may-NOT; the key's name is not a licence, §0.2), and **a `which`-clause** (R-DA-03; the hard wall of the owner's rules: no which-clause). Layer **NONE** (an effect on unrecorded actors) against BODY reads — a wrong-layer predicate (W20).
- deterrence AGAINST RAIDING — **UNLICENSED: an event class no field records** (no raid field exists on this key or the desk; the stress roll is a generation-time probability, never a record of what came; W17's crisis bar by shape). Layer **NONE**. This is the engine's chrome (`threatAssessment.js:115`) copied into the record; the chrome is not a licence (OW-20).
- and AGAINST A CONVENTIONAL ASSAULT — **UNLICENSED: the same event class** and **a classification the engine does not model** ("conventional" against what; a standpoint). Layer **NONE**.
- it is not a POSTURE — **UNLICENSED: an AGGREGATE word on a BODY read** (W20; "posture" is the military posture vocabulary, chrome not prose — entailment D-26, L-57; the arm's badge is an AGGREGATE read this pool does not carry).
- RATED for a long siege — **UNLICENSED: a rating** (a verdict; the badge's business, an AGGREGATE this pool does not read — D-22, W20), **a hypothetical siege stated in the present indicative** (alias A-24 / W17: a siege entails a besieger and never walls or an army at the walls; no siege field at this key; an edge is subjunctive, never "is not rated"), **a duration** ("long"; the card's "a season"; R-DA-16), and **a negated surface on a claim that is not a PRESENT read** (R-ii / W10 license one negated surface per variant only on a PRESENT read).
- WITHOUT STORES behind it — **UNLICENSED: a second fact of another cell** (the `store` civic class; the Disasters row on this same tab and DS-DEF-6's cell; T-F12 keeps the stock apart) and **a same-page contradiction risk** (C7: beside a `granary AND …` row the town HAS stores); "behind it" binds the possessor to the posture (W1).
- the sentence's SHAPE — a semicolon joining two verdicts: R-DA-06's semicolon is a rationed permission (≤ 0.130 per variant), lawful as punctuation; what it joins here is not.

**(4) The reads the rewrite must state (all of them) plus the licensed shipped claims:**
- `walls === true` — the works stand at {settlement} (the walls-bucket class word); never "a line", never a material, a height, a gate, a manning, a condition.
- `garrison === true` — the soldiers stand at {settlement} (the garrison bucket's body by the person-plural class word; "the town's force" / "the town's muster" as the alternatives); never "the garrison" baked (W12; §0.7 fence 1), never the militia (unread), the watch (W13; a second force object), the guard (struck), a count, pay, a strength.
- the KEY'S CONJUNCTION — the two together on one row (the works, and the soldiers with them): the keyed condition, stated as two presences placed together, never as one holding, manning or explaining the other.
- the shipped licensed claims carried forward: the town has the works; professionals stand (as "the soldiers"). Nothing of "which is real deterrence … without stores behind it" survives.
- SILENCE on a militia (§0.2): neither asserted nor denied.

**(5) The angle's stance:** the ledger is the clerk's own way of stating the fact (W24: a STANDPOINT that licenses NO record noun and no citation), so this face enters the two reads as the compiled record holds them — the works, the soldiers, together — in the office's formula ("entered as standing", "carried on the record", "set down") if it wants one, with the wall-class and force-class nouns at their read's layer; it may cite a holder at most once and only for an S3 reason (two accounts disagreeing, a count from an interested party, a keeper who is a power), none of which two presence reads present, and the muster holder resolves `null` on this row (OW-23: a kind with no holder is cited by the class word or not at all; OV-5: a garrison town keeps no roll), so the recommended citation count is ZERO and "the roll shows" / "the muster roll" is a finding (R-vi, W7); it may NOT invent a deterrence, a rating, a raid, an assault, a siege, a store, a posture word, a badge word, a manning, a purpose, a pay fact, a count, a season, or a "because".

**(6) Turns worth keeping (verbatim, lawful as they stand):**
- none survives whole. The lawful residue is the CONJUNCTION SHAPE `has … and …` (the town has the works and the soldiers) — a rhythm, not a verbatim clause, because "a line" (D-6) and "to hold it" (manning) both fall; and `professionals` under §0.7 fence 3 only. The doubled preposition `against … and against …` is a rhythm the writer may re-use on licensed content, never on raiding and assault.
- Not kept: "a line"; "to hold it"; "which is real deterrence"; "against raiding and against a conventional assault"; "a posture rated"; "a long siege"; "without stores behind it".
- The density floor for this variant is therefore the two reads in the ledger's formula with the conjunction stated — richer in truth than the shipped line, whose second half was the chrome's invention (ADDENDUM 7's honest limit applies: the faithful rewrite is plainer in those places by design; the richness comes from the two bodies named at their layer and from the office's formula).

**(7) What would make the rewrite a regression here:**
- inventory: vid 1 loses its slot set `{settlement}`, its `[ledger]` tag, or drops below four faces; a face stating one body only ("the works stand at {settlement}") with no second read and no stance (ADDENDUM 7 rule 3); a face that paraphrases a sibling face rather than differing in construction (W4);
- a lost licensed read: a face without the works, or without the soldiers, or with the two in two disconnected sentences and no noun carried forward (R-i, the thread at k = 0 inside the variant's own sentences);
- a lost lawful turn: the conjunction shape gone from every face (two facts listed flat is an inventory line); the shipped "has … and …" traded for a passive that names neither body;
- a dropped angle: a face that reads as a stranger's eye or the town's talk under a ledger tag; a face that cites a holder or a roll;
- an added unlicensed claim: "a line", "perimeter"; "the garrison" baked; "the militia", "the watch", "the guard", "the men on the wall"; "hold", "man", "defend"; "deterrence", "deter", "raid", "assault", "siege", "besieged"; "rated", "posture", a badge word; "stores", "supply", "provisioned"; "professional" sharpened into pay or drill; "wages", "upkeep", "paid"; "because", "the reason"; a count, a season, "long"; a which-clause, an em dash, a digit, an exclamation; the settlement token as opener.

---

## VARIANT 2 · `[visitor]` (vid 2; slot set `{settlement}`; one sentence with a parenthesis)

**(2) Shipped, verbatim:**
> A stranger sizing {settlement} up sees the two things that matter together (the wall and the men who belong to it) and revises what an attempt would cost.

**(3) Every claim, one per line (licence · law · referent layer beside the read's layer):**
- a stranger SEES — **the visitor's frame, lawful** (W27: "a stranger" is the visitor's eye; it may see). A stance word, not a referent of any layer; it names no person of the record (W22 is not engaged by the eye itself).
- the stranger is SIZING {settlement} UP — **UNLICENSED: the stranger ACTS and JUDGES** (W27: the eye "may see, never act, decide"); an interior weighing (R-DA-14 seen, not meant; the FEELING/verdict non-moves). The eye may look at the works and the soldiers; it may not appraise the town.
- the wall stands ("the wall") — **LICENSED in substance** the `walls` input (layer **BODY** = the read's layer). The noun: the class word the referent table prints as HOLDS, with §0.2's caveat (a `Gates (if walled)` town, a post-ruin citadel); "the works" is the recommended spelling and "the wall" is accepted as the class word.
- the men stand ("the men") — **LICENSED in substance** the `garrison` input (layer **BODY** = the read's layer). The noun: the referent table's `soldiers` row prints "the soldiers · the men" as the safe person-plural spelling; §0.7 fence 4 records the gendered-word caution; "the soldiers" is preferred and carries the key's discriminator.
- the men BELONG TO the wall ("who belong to it") — **UNLICENSED: a relation between the two BODY reads that no field computes** (W23 the fused-agent bar by shape; ADDENDUM 8 ruling 2: a conjunction key is not a computed relation), **a manning by another route** (NOT ENTAILED; "the men on the wall" struck), and **a possessive binding to the wall** (W1: "it" after the wall binds the men to the wall). Layer: a cross-body relation — no read licenses it.
- the two are seen TOGETHER ("together") — **LICENSED**: the key's own conjunction (both presences on one row). Layer BODY × BODY as the key reads them.
- they are the two things THAT MATTER — **UNLICENSED: a standpoint and a verdict** (the register never explains what a fact means or rates it; "matter" ranks the two bodies above whatever else the tab holds — the arm's badge is the AGGREGATE this pool does not read, W20). "the two things": a count in words of the key's own two reads — lawful as words (R-DA-16) and not a count over persons; it is the "that matter" that falls.
- the stranger REVISES what an attempt would cost — **UNLICENSED: the stranger DECIDES** (W27; an interior, R-DA-14), **a hypothetical assault** ("an attempt": no field; W17's crisis bar by shape), and **a deterrence claim by another route** ("would cost": an attacker's calculation the engine does not model; the key's name is not a licence, §0.2). The subjunctive "would" is a lawful modality; the claim under it is not. Layer **NONE** (an unrecorded actor's reckoning) against BODY reads (W20).
- the parenthesis — punctuation the writer may spend (the register card: "spend word order, punctuation … freely"); it is not itself a claim. A sentence face must open on a capital that is not a `proper`-typed slot (ARCH §2.5, T-F8) — "A stranger" satisfies it.

**(4) The reads the rewrite must state (all of them) plus the licensed shipped claims:**
- `walls === true` — the works are what the stranger sees first or second: the wall-class noun at BODY; never a geometry, a material, a height, a manning.
- `garrison === true` — the soldiers are the other thing seen: the person-plural class word at BODY; never "the garrison" baked, never the militia, the watch, the guard, a count, a pay fact.
- the KEY'S CONJUNCTION — seen TOGETHER: the shipped "together" is the licensed carrier of it and stays.
- the shipped licensed claims carried forward: a stranger sees the wall and the men, together. Nothing of "sizing up", "that matter", "who belong to it", "revises what an attempt would cost" survives.
- `{settlement}` in every face (the parent's slot set), never as the opener.
- SILENCE on a militia.

**(5) The angle's stance:** the visitor reports what is seen on arrival (the RULES' own line: "the visitor reports what is seen on arrival"; W27: the eye may see, never act, decide, be told or be given a name; W8: the visitor carries a gate only as a standing state — and this card carries no gate), so this face sets the works and the soldiers before a stranger's eye as two things standing together at {settlement}, in the order the eye meets them, and stops there; the PROVENANCE line licenses structural reads only, so it may NOT appraise, size up, revise, reckon a cost, infer deterrence, name what matters, put the men on the wall or make them the wall's, count them, grade them, or say what the stranger concludes; the stranger is never given a name, a nation, an errand or a feeling.

**(6) Turns worth keeping (verbatim, lawful as they stand):**
- `A stranger … sees` — the visitor's frame, lawful once "sizing {settlement} up" is struck from between its two words; the shape "A stranger at {settlement} sees …" is the density floor's frame.
- `the wall and the men` — the two bodies named as a pair, lawful as the referent table prints both class words; the recommended spelling is "the works and the soldiers", and either pair may stand verbatim as ONE face's nouns.
- `together` — the key's conjunction in one word; keep it.
- Not kept: "sizing {settlement} up"; "the two things that matter"; "who belong to it"; "revises what an attempt would cost".

**(7) What would make the rewrite a regression here:**
- inventory: vid 2 loses its slot set `{settlement}`, its `[visitor]` tag, or drops below four faces; a face that names one body only; a face that paraphrases a sibling face rather than differing in construction (W4);
- a lost licensed read: a face without the works or without the soldiers; a face that drops "together" (or its equivalent) so the two read as a list rather than the key's conjunction;
- a lost lawful turn: the stranger's frame gone from every face (a visitor face with no eye is the ledger's entry under the wrong tag — a dropped angle); "the wall and the men" / "the works and the soldiers" gone from every face while the replacement is plainer;
- a dropped angle: a face in which the stranger acts, judges, sizes up, revises, is told, is named, or feels; a face that cites a holder;
- an added unlicensed claim: "belong to", "on the wall", "manning", "holds"; "that matter"; "an attempt", "an attack", "a siege", "raiders"; "would cost", "deters", "thinks twice"; "the garrison" baked; "the militia", "the watch", "the guard"; a count of men; "paid", "professional" sharpened; a which-clause, an em dash, a digit, an exclamation; the settlement token as opener.

---

## VARIANT 3 · `[street]` (vid 3; slot set `{}` — NO `{settlement}` in any face; one sentence)

**(2) Shipped, verbatim:**
> The town believes it could be held, and the belief is founded on something rather than on hope.

**(3) Every claim, one per line (licence · law · referent layer beside the read's layer):**
- the town BELIEVES — **UNLICENSED: a belief frame** (R-DA-13: never on a belief; the register card: no assigned reaction; the FEELING non-move) and **a fused collective agent** (W23: the "the town has decided" class — D-F16 and D-F18 by shape; W22: no person-plural as believer or decider). Layer: the town as a believing agent is **NONE** (a collective person) against two BODY reads — a wrong-layer subject.
- the town COULD BE HELD — **UNLICENSED: a manning and a capability the fields do not hold** (NOT ENTAILED; §0.7 fence 2), **a hypothetical siege outcome** (alias A-24 / W17 by shape: nothing on this key records an attacker, and "held" against nothing is the deterrence claim by another door; the key's name is not a licence, §0.2). "could" is a lawful subjunctive modality; the claim under it is not. Layer **NONE**.
- the belief is FOUNDED ON something — **UNLICENSED: a cause** (the card's may-NOT; a second clause that explains the first — the MEANING non-move, R-DA-03 and R-DA-12: the record never sums up the sentence before) and **a hedged non-statement** ("something" in place of the two bodies — fault 29's class of the pretext; R-DA-13's vague-authority floor). The works and the soldiers are **NOT STATED**.
- rather than on HOPE — **UNLICENSED: a contrast whose rejected alternative names no sibling key or band** (R-DA-02: a bare "X, not Y"; "hope" is no pool key of this table) and **a FEELING** (the FEELING non-move; R-DA-11: no feeling for a collective or inanimate thing).
- the town as SUBJECT ("The town") — **lawful as a frame**: the town is the holder of its own roster (the read is `settlement.institutions[]` through the live filter), so "the town has …", "the town keeps …" states the presence read in the street's naming form (W5: the naming form; the taste kept "{settlement} keeps its {defwork}" as a turn). What the town may not do is believe, think, decide, know, hope or judge.
- **No slot** — the parent's slot set is empty; no face may carry `{settlement}` (ARCH §2.5: a face whose slot set differs from its parent's is refused).

**(4) The reads the rewrite must state (all of them) plus the licensed shipped claims:**
- `walls === true` — the works stand: the plain noun in the town's own words ("the works", "what the town has built", "the wall" as the class word); never "the line", never a material, a gate, a manning.
- `garrison === true` — the soldiers stand: "the soldiers", "the town's force", "the town's muster"; never "the garrison" baked, never the militia, the watch, the guard, the pay.
- the KEY'S CONJUNCTION — the two together, in the street's short clause.
- the shipped licensed claims carried forward: only the frame "The town" as the street's subject. Nothing else in this row survives as licensed; this variant's density floor is the two reads at the street's stance, which is a HIGHER floor than the shipped line (which names neither body).
- **No `{settlement}` in any face.** SILENCE on a militia.

**(5) The angle's stance:** the street is the town's own talk about its condition (the RULES' own line: "the street reports what the town does or says about it only where a field holds that and otherwise states the standing plainly"), so this face states the works and the soldiers as the town's standing condition in the town's idiom — the short clause, the thing named rather than judged, the plain noun where the ledger would use the formula, the town as the subject that HAS its works and its soldiers — and may leave one matter standing open (stated, never asked); the PROVENANCE line licenses only structural reads and no field holds an attitude, so the street may NOT say what the town believes, thinks, knows, hopes, minds or has decided, may NOT personify or quantify the town ("everybody", "nobody", "the town knows"), may NOT say the town could be held or would fall, may NOT explain one read by another or found one on the other, may NOT grade the arrangement (sound, enough, real, founded), and may NOT contrast it against a feeling or against anything that is not a sibling key.

**(6) Turns worth keeping (verbatim, lawful as they stand):**
- `The town` — the street's subject in the naming form; the one lawful residue.
- Nothing else survives. The two verbs of the shipped line (believes, is founded) and its two nouns (belief, hope) all fall. The density floor is the two reads in the street's plainness with a sharper licensed fact in place of the belief frame (the works and the soldiers named apart, together) — richer in truth than the shipped line.

**(7) What would make the rewrite a regression here:**
- inventory: vid 3 gains a `{settlement}` slot (its parent has none), loses its `[street]` tag, or drops below four faces; a face stating one body only; a face that paraphrases a sibling face rather than differing in construction (W4);
- a lost licensed read: a face without the works or without the soldiers — the shipped line names neither, so returning to a face that names one is still the regression against the floor this skeleton sets;
- a lost lawful turn: none to lose beyond the street's subject; but a face that trades the street's idiom for the ledger's formula or a stranger's eye is a dropped angle;
- an added unlicensed claim: a belief or attitude verb (believes, thinks, knows, hopes, trusts, counts on); "could be held", "would hold", "would fall"; "founded on", "rests on", "because"; a contrast naming no sibling ("rather than hope"); a totality ("everybody", "nobody"); "the garrison" baked; "the militia", "the watch", "the guard"; a siege, a raid, an attacker; a pay word; a which-clause, an em dash, a digit, an exclamation; any `{settlement}`.

---

## 8. POOL-LEVEL FENCES THE FACES MUST OBEY TOGETHER (the marker's inventory of the walls)

- **The settlement token never opens a sentence face** (ARCH §2.5, T-F8; MOVE-GRAMMAR §1.4 wall 10). Today vid 1 opens on `{settlement}` — a shipped breach; moving the opener is a cure. Lawful openers: the works, the soldiers, the office's formula, the stranger (vid 2), the town (vid 3), a plain civic noun.
- **The slot sets are the parents' exactly:** vid 1 `{settlement}` · vid 2 `{settlement}` · vid 3 `{}`. `{band}` (RESERVED) and `{route}` (unfilled here) are named by no face. `{defwork}` is NOT in this block's bag and is named by no face; the works are the class word.
- **ONE force object per face** (the card: "another civic object of the class `force`" refused; `objectClass force`): the garrison bucket's body by ONE spelling per face — "the soldiers" (carries the discriminator), "the town's force", "the town's muster" — never a second force noun beside it (the militia, the watch, the guard, a levy, a patrol, mercenaries, the charter hall, "the men on the wall"); the projector's class guard cannot tell one force noun from another (OV-25), so two spellings in one sentence read as two objects.
- **SILENCE on a militia** (§0.2): the reader never consults `militia` on this branch; no face asserts or denies one.
- **A11's spread:** no two variants of one pool share their first two words; the four faces of a variant differ in CONSTRUCTION — the subject, the order of the two reads, the landing noun — not vocabulary alone (ADDENDUM 7 rule 4; W4).
- **Level-1 grammars this block can license:** V1 PRESENT (the keyed condition, its two reads in either order — works first or soldiers first) is the only member whose licensing field this card prints; V4 OBJECT → PRESENT needs a named-object FIELD (the bag carries no `{defwork}`; a class word does not make V4); V2 needs a structural-consequence field (none; S2's clause seat is closed on a spine with no relation row); V3/V8 need a `none-exists` or `not-held` field (none: both reads are present); V5 needs an institution table row (none on the card); V6 an unresolved value (none). The census reads `grammars: 2`, so the writer varies the ORDER of the two reads and the sentence count (one or two under A1) and declares the grammar per variant.
- **The thread (MOVE-GRAMMAR §1.4.1; R-i at k = 0):** this pool is the SPINE, so its face opens the passage and must hand a noun forward that a modifier can carry — the works, the soldiers — and should not close on a pronoun (R-DA-04's pronoun-closer ceiling); inside a two-sentence face the second carries a noun forward from the first regardless of k. The census reads `k 2`: two modifier positions may follow this spine, ordered by salience, so every face is written to read well before an unknown modifier as well as alone.
- **The close:** a standing fact of varied KIND across the faces (a condition, an object, a name not given …); never a verdict ("deterrence", "rated", "enough"), a maxim, a hook, a cause.
- **Same word, same referent across the tab (the referent law rule 3; C7; A1/A11):** the Beasts row on this tab names the same works and, on this town, the same force body; a face here spells them as the Beasts faces do (the works; a force / the soldiers) and never as a different thing ("the garrison" here, "a force" there). No face contradicts the Economic Survival row (no pay, no morale) or the Disasters row (no stores) beside it.
- **The referent walls (W20–W27) on this pool:** the works at BODY (the walls bucket) by the class word, never a geometry, material, height, gate or manning; the soldiers at BODY (the garrison bucket) by the class word, never "the garrison" baked (W12), "the militia", "the watch" (W13), "the guard", "the men on the wall"; no AGGREGATE word on these body reads (no badge, rating, posture, "deterrence" — W20); no person as agent, believer or decider (W22); no fused agent ("the town believes", "the men who belong to it" — W23); no record word (W24: the muster holder resolves `null` here; the ledger is a standpoint that licenses no citation; "the muster roll" is a record only with a `Citizen militia` and `Muster training`, OV-5 — never on this key); no external body (no attacker, besieger, raider — W25 by shape, and no field); the stance's nouns never a holder's record and the stranger never an actor (W27).
- **The label walls (W14) on this pool:** "walls, professional garrison" at its ENGINE meaning — the walls bucket AND the garrison bucket resolved — never the dictionary sense (career, paid, drilled; a continuous stone circuit); `Barracks` is a housing row and `Gates (if walled)` a gate row, and both fire this key.
- **The crisis walls (W17; alias A-24):** no siege, raid, assault, attack, attempt, besieger, ladders, stores, provisions; nothing on this key records an attacker or a stock.
- **The material bar (W11):** no "stone", "timber", "masonry", "earth"; no material source. `{defmaterial}` is not minted and is not in this bag in any case.
- **Hard walls, every face:** no em dash, no exclamation, no digit, no which-clause, no question, no "I"/"you", no figure, no sense verb on an abstraction, no intent or feeling for an inanimate or collective thing, no forecast (the edge, if any, subjunctive — and no edge is licensed here), no totality over persons or over the town's safety, no belief frame, no count of men, no season or duration, no cause, no badge or posture word, no pay, wages, upkeep or keeping word (DS-DEF-11's cell), no strength or thinning of the force (DS-DEF-5's cell), no watch, charter, mercenary or arcane body (the read excludes them and the card refuses a second force object).
- **Provenance ceiling:** at most one citation per unit on a licensed holder and only for an S3 reason (Part B §24); the muster kind resolves `holder: null` on this row and a garrison town keeps no roll (OV-5, OW-23), so the recommended citation count for this pool is ZERO; "the muster" is the force's class word, never a record.
- **The echo:** every pool that selects a row of `INVASION_ROW_POOL` shares ONE echo key, so a mount counted against this pool may be a sibling row; the faces neither rely on nor fear it.
- **The density floor (ADDENDUM 6, §21.4):** the shipped v1 states both reads (a line; professionals) and v2 states both (the wall; the men); v3 states neither. Every face of every variant states BOTH reads and their conjunction; a face stating one read with no angle is an inventory line and is refused (ADDENDUM 7 rule 3). The honest limit (ADDENDUM 7): this pool's shipped richness (deterrence, raiding, assault, siege, stores, the stranger's reckoning, the town's belief) was unlicensed invention copied from or bred by the engine's chrome, so a faithful rewrite is plainer in those places by design; the richness comes from the two bodies named at their layer, the conjunction stated as the key states it, and each stance's own shape.

## 9. THE MARKER'S OPEN ROWS FOR THE CHAIR (recorded, not decided)

1. **Fence 1 — the force's name.** W12 licenses "the garrison" only on a resolved `Garrison` / `Multiple garrisons` row (every city), while this key also fires on `Barracks` towns; a baked "the garrison" is a breach there, so this seat licenses "the soldiers" / "the town's force" / "the town's muster" for every face. A derived force-name fill from the resolved garrison-bucket row (the `{watchname}` shape, OW-1) is the wiring that would license the specific noun — a row for CAR 8b-W-2, not a writer's task.
2. **Fence 2 — the PROVENANCE capability clause.** Whether "the causal clauses here are capability clauses" licenses "to hold it" / "could be held" against the card's "may NOT a cause / a second fact". This seat licensed the bare conjunction and refused the predicate (ADDENDUM 8 ruling 2 generalised); the rows so marked flip if the chair rules otherwise. Sibling pools of this table ("walls with NO force", "force with NO walls") carry the same question with the opposite sign.
3. **Fence 3 — "professional(s)".** The label's own adjective at its engine meaning (the garrison bucket) versus its dictionary sense (not entailed of a `Barracks`). This seat treats it as CONDITIONAL and carries the discriminator through "the soldiers"; if the chair reads the adjective as the label's engine rendering (as "active" for `frontier` on the Beasts sibling), vid 1's "professionals" returns to the turns worth keeping.
4. **Fence 4 — "the men".** Printed safe by the referent table's `soldiers` row; a gendered word with no sex-ratio field by alias A-6. Licensed on vid 2 as the table prints it; "the soldiers" recommended.
5. **The militia is unread at this key.** `invasionRowSituation` returns on `garrison` before consulting `militia`; a face is silent on a militia. Recorded so no drafter negates it ("not the townspeople") as a sibling-key contrast — the key does not deny it.
6. **The key's name is not a licence** ("professional garrison"; the chrome at `threatAssessment.js:115` supplied the shipped v1's deterrence, raiding, assault, siege and stores). ADDENDUM 8 ruling 2 generalised; recorded so the drafter does not read the row label as permission.
7. **The census reads line prints ONE synthetic read at branch grain** while the reader's inputs are `forces.walls.present`, `forces.garrison.present` and an unread `forces.militia.present`; the census `source.fields` maps all three to `muster` with `holder: null`. This seat stated the two consulted inputs as the keyed condition under R-v; a citation is not licensed on this row.
8. **Every natively generated city fires this key** (`City walls and gates` and `Garrison` both required), so the tab's own `buildThreatAssessment` string ("… Not rated for sustained siege without significant supply stockpiles.") sits beside every city's face saying what the face may not. The engine's own strings are product code outside the corpus (OW-20, recorded for the owner as a scope question); a wiring fact, not a writer's.
9. **The live roster against the snapshot** (entailment C-10): this desk reads `standingDefenseForces` (ruin-filtered) while `buildThreatAssessment` reads `defenseProfile.institutions`; a flattened citadel is `walls ABSENT` here and `hasWalls` to the chrome beside it. A wiring fact for the register car; the faces follow the card's read.
10. **`Gates (if walled)` sets the walls bucket with no wall row** (entailment D-5; the substring `wall` inside "walled"), and `Barracks` sets the garrison bucket as a housing row (D-7): both are town-tier firings of this key on which "the wall" and "the garrison" are each false. "the works" and "the soldiers" are the spellings true on every firing; the two residuals are CAR 8b-W-2 rows (O-1's `{defwork}` residuals; the force-name fill of open row 1).
