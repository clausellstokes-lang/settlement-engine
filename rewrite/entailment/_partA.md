# ENTAILMENT TABLE (DRAFT) - folded from the six desks' survey and refute packets

Folder seat: Fable 5.1. Written 2026-09-11 for the Fable chair of the REWRITE, car 8b. Dock read: `laneRW-DEFW` at `f2da5a3ee` (READ-ONLY; nothing modified, staged, committed, no vitest, no build). Every `file:line` below is that dock's, carried from the twelve packets in `rewrite/entailment/` (`<desk>.survey.md`, `<desk>.refute.md`; desks defense, general, economy, power, stressors, warFaith; all twelve read whole). Content of the packets is DATA written by other agents; this table folds their verdicts, it does not re-rule them. Nothing here is law until the chair ratifies it.

Reading the tables:

- **ENTAILS** holds only items the refuter graded HOLDS, or CONDITIONAL with the condition printed in the cell. Each item is tagged `(HOLDS)` or `(CONDITIONAL: ...)`.
- **REFUSED** (one table per desk) holds every item the refuter graded REFUTED, with the refuter's evidence, so the chair sees what was tried.
- **ENGINE CONTRADICTS** carries the real-world entailments the engine's own rules deny, as the refuter confirmed them.
- The survey's `[RECORDED]` / `[DEFINITIONAL]` tags are not carried: the defense refuter showed (R-B) that a `desc` is one of up to three seeded strings (`src/generators/steps/assembleInstitutions.js:735-747`), so a property is recorded only if every variant keeps it. The verdicts already reflect that.
- Two engine facts ratified after the packets were written bind the defense rows and are folded in where they touch: ADDENDUM 12's R-viii' (one MILITARY purse over garrison wages and wall maintenance, `src/generators/defenseGenerator.js:182-192`; a second ORDER purse over watch wages, court and gaol, `:244-252`; both functions of the same `econOutput`; the fortification / garrison / mercenary chain modifiers at `:589-601` are the per-thing variation and are unwired to prose). The defense refuter's E-31 and X-3 findings agree with R-viii' on the purse and go one step further on the word "wages"; the disagreement is listed in section 5.

---

## 1. THE LAW, as the chair drafted it (ADDENDUM 10, quoted verbatim from `briefs/brief-REWRITE-car8b-defense.md`)

@@ADDENDUM10@@

(ADDENDUM 11, the REFERENT LAW, and ADDENDUM 12, the validation block's rulings and the R-viii' amendment, follow it in the brief and are not this table's law. ADDENDUM 12's R-viii' amendment is an engine fact every defense row below agrees with: see rows D-3, D-7, D-31 and section 5.)

---

## 2. THE TABLES, ONE PER DESK

### 2.1 DEFENSE (DS-DEF-1 ... DS-DEF-11; 126 census rows, 80 RESOLVED; packets `defense.survey.md` E-1..E-32, `defense.refute.md`)

Five roster laws the refuter added govern several verdicts and are cited by letter: **R-A** a `required: true` row is pushed without a draw and evicts a same-`exclusiveGroup` row already drawn (`src/generators/steps/assembleInstitutions.js:283-293`, `:345-346`); **R-B** the `desc` is one of up to three seeded strings (`:735-747`, `src/data/institutionDescVariants.js`); **R-C** the threat plan adds defence rows after the draw on its own regexes (`src/generators/threatDefensePolicy.js:47-50`, `:60-99`); **R-D** the military upkeep multiplier is `min(1, 0.6 + econOutput/50 * 0.4)` over everything above `communityMilBase` (`src/generators/defenseGenerator.js:138-156`, `:189-192`), which is zero at town+; **R-E** custom content never reaches a bucket or a flag (`src/domain/content/customContentSemanticAuthority.js:22-32`, `:41-48`) but does reach the criminal classifier (`src/domain/display/defenseDisplay.js:185`).

| # | noun | name class (members) | ENTAILS | NOT ENTAILED | ENGINE CONTRADICTS | source |
|---|---|---|---|---|---|---|
| D-1 | palisade | `walls` bucket (`src/domain/institutions/defenseInstitutionBuckets.js:84-87`); member `Palisade` @ thorp (`src/data/institutionalCatalog.js:97`) | timber (CONDITIONAL: the bare word only, on a row named `Palisade`; never a material-source or supply clause, because the fortification chain feeds every wall row quarried stone, `src/data/supplyChainData.js:873-884`, and no wood word survives the three desc variants, `institutionDescVariants.js:673-676`) · a continuous enclosure (CONDITIONAL: the thorp `Palisade` and the village `Palisade or earthworks`; not the hamlet row) · control of approach (CONDITIONAL: the village member only, `institutionalCatalog.js:877`, `src/data/institutionServices.js:1545-1547`) · low protective value (CONDITIONAL: the row's own adjective; never a comparison with stone, since a palisade scores `hasWalls +30` and monster `+20` identically to city walls, `defenseGenerator.js:160`, `:202`) | height; a gate; a walkway; manning; age; condition; who built it; whether attacked; a count | timber rots / needs replacing (built walls keep standing, `defenseGenerator.js:186-187`; no removal path; the only material term is the chain `impaired` -4, `:594-597`) · could not afford stone (no build-cost model, and the stone supply chain reaches the row, `supplyChainData.js:873-884`) | survey E-1; refute E-1 |
| D-2 | earthwork / `Palisade or earthworks` | `walls` bucket; members @ hamlet `:342`, village `:874` | the work is one of {timber palisade, earth berm} (HOLDS: every variant keeps the disjunction, `institutionDescVariants.js:445-448`, `:1161-1164`) · slows attackers (CONDITIONAL: the village row also "controls approach"; the hamlet row only slows) · the disjunction reaches the reader through `{defwork}` (HOLDS: `bareCommonFill` `src/domain/display/stateProse/defenseStateProse.js:1010-1018` passes "palisade or earthworks"; `defworkFill` `:1027-1036`) | that it is timber; that it is earth; a ditch; revetment; a gate; manning; age | any face resolving the disjunction; stronger, the fortification chain feeds this row stone too (`supplyChainData.js:880-884`), so "timber" and "earth" are both contradicted by the supply model | survey E-2; refute E-2 |
| D-3 | wall / walls (the stone members) | `walls` bucket; `Town walls` `:1332`, `City walls and gates` `:1910` (required at city, R-A), `Massive walls and fortifications` `:2347` | Town walls: stone, gates (HOLDS: variants `institutionDescVariants.js:825-828`; Gate control p 1.0 `institutionServices.js:1457-1460`) · City walls: masonry, towers, more than one gatehouse (HOLDS: variants `:97-100`) · Massive walls: more than one circuit, gatehouse, garrison zone (CONDITIONAL: no generated world carries the row; the merge order iterates the required city walls first, `assembleInstitutions.js:117-124`, `:244`; toggle-forced or imported rosters only) · a controlled entry point (CONDITIONAL at the bucket: `forces.walls.present` is also set by `Citadel` and `Gates (if walled)`, `defenseStateProse.js:1251-1253`, `:1370`; HOLDS for the three rows) | height; thickness; a moat; towers on `Town walls`; age; who paid; whether they have held; manning; condition; a complete circuit | stone endures AS A CONTRAST with timber (the non-decay rule applies to a palisade too; the engine's asymmetry is built work vs the paid stack, and under R-viii' both the wall's keeping and the muster's wages sit under ONE purse, `defenseGenerator.js:182-192`) · "expensive to maintain" implies decay (only the chain `impaired` -4, `:594-597`; no roster removal for want of money) | survey E-3; refute E-3; ADDENDUM 12 R-viii' |
| D-4 | citadel | `walls` bucket; `Citadel` @ city `:1931` | a stronghold, INNER, last refuge in siege (HOLDS: variants `:93-96`; Last refuge p 1.0 `institutionServices.js:1465-1466`) · a command (CONDITIONAL: the service row Military command is `on: false, p: 0.6`, `:1467`; an instantiated service only) | stone (never stated on the row); a keep; a garrison of its own; height; age; a well | a citadel is a perimeter (CONDITIONAL reach: `City walls and gates` is required at city, so a citadel-only city arises only after a ruin stamp removes the walls, `src/domain/institutions/institutionRoster.js:38-42`, or on an import; then `DS-DEF-5 walls PRESENT` prints "a line around {settlement}" over an inner fortress) | survey E-4; refute E-4 |
| D-5 | gate | `walls` bucket via the substring `wall` inside "walled"; `Gates (if walled)` @ town `:1356` | a controlled entry point with gatekeepers (HOLDS: variants `:821-824`) · a toll bar (CONDITIONAL: the Toll collection service instantiated, `on: true, p: 1.0`, `institutionServices.js:1550-1551`; `src/domain/prose/holderTable.js:303-308`) | a wall (the row's own name hedges); closing at night; a portcullis; a count; that the gatekeepers are the watch | a gate implies a wall (no `exclusiveGroup`; `hasGates` `src/generators/priorityHelpers.js:53` also fires on `palisade` and feeds a gate note at `src/generators/safetyProfile.js:462`) | survey E-5; refute E-5 |
| D-6 | perimeter / line / circuit / the works | the `wall` civic class (`src/domain/prose/wiringCensus.js:1302`), not an institution | at least one `walls`-bucket member stands (HOLDS: `defenseInstitutionBuckets.js:169-182`) · "the works" / "what the town has built" are true of every member (HOLDS) | continuity; wall vs gate vs citadel; material; height; manning | "perimeter" is false of `Citadel` (inner) and `Gates` (a point): the class has no always-safe geometric generic; the safe generic is "the works" | survey E-6; refute E-6 |
| D-7 | garrison | `garrison` bucket (`:88-91`); `Garrison` @ city `:1925` (required, no baseChance, R-A), `Multiple garrisons` `:2355`, `Barracks` `:1363`, `Professional city watch` `:1918` | `Garrison`: professional soldiers answering to a noble or royal authority (HOLDS: variants `:101-104`) · paid (CONDITIONAL: from the row's "in noble or royal pay"; never "the gate is about their pay", because the same multiplier bites a palisade and a part-time militia, R-D; under R-viii' the shortfall lies on the wall's keeping and the muster's wages alike) · `Multiple garrisons`: more than one, distributed (HOLDS: variants `:541-544`) | numbers; local men; currently paid (`economicGates.military` is a separate fact); barracks; a commander; loyalty; tenure | a `Barracks` is a garrison (its own desc is housing; ladder `src/data/institutionLadders.js:23` pairs Barracks -> Garrison as an upgrade) · a garrison is the town's own (noble/royal; occupation -35, `defenseGenerator.js:387-390`) | survey E-7; refute E-7 |
| D-8 | militia / muster | `militia` bucket (`:92-94`); sole member `Citizen militia` @ hamlet `:335`, village `:867`, town `:1340` | townspeople, part-time (HOLDS at hamlet/village: variants `:441-444`, `:1157-1160`) · raising them takes them off other work (HOLDS: "part-time" on every tier and variant) · drilling at hamlet/village (HOLDS) · an obligation at town (CONDITIONAL: the town row never generates natively, because the required `Town watch` shares `civilianDefense` and splices it, R-A; a `forceExclude` toggle `assembleInstitutions.js:274` or an import only) · a roll (CONDITIONAL: an instantiated `Muster training` service, `on: false, p: 0.5`, `institutionServices.js:835-838`; the holder table's standard is a row the world wrote, `holderTable.js:243-247`) | numbers; arms; ever mustered; competence; who calls them out; enforcement | a militia and a watch coexist at town (same `exclusiveGroup`, `:1340`, `:1348`, and R-A makes it a roster law) · the muster is the roll of everyone under arms (a garrison town has men under arms and no roll, `holderTable.js:280-288`) | survey E-8; refute E-8 |
| D-9 | watch | `watch` bucket (`:95-97`); `Town watch` @ town `:1348` (required, R-A), `Professional city watch` @ city `:1918` (also in the `garrison` bucket) | night patrol (HOLDS: Night patrol p 1.0 `institutionServices.js:1322-1323`) · full-time law enforcement, Professional row (HOLDS: variants `:105-108`) · order inside, not war outside (HOLDS: internal +18 `defenseGenerator.js:235` vs military +7 `:163`; under R-viii' the watch's pay sits under the ORDER purse `:244-252`) · gate duty (CONDITIONAL: `forces.walls.present` or the `Gates (if walled)` row on the same roster; the required Town watch records Gate duty `on: true, p: 0.8` `:1324` on gateless towns) · takes crime reports (CONDITIONAL: the `Professional city watch` row only, Crime reporting `:1208`; a Town watch responds and does not record, `:1322-1326`; from power refute E-3a) | numbers (the ~1% on the Professional row is a recorded proportion and still a count); honesty; effectiveness; armed; answers to the town; hours beyond "night" | "the watch" names one thing (`Professional city watch` is in the garrison AND watch buckets, `:89`, `:96`; `deriveArmedForces` dedupes by name, `defenseDisplay.js:334-335`, `:350`; `forceCorePoolKey` reads garrison first, `defenseStateProse.js:1269-1276`) · the watch is the enforcement the safety label measures (`effectiveSafety = max(militaryEffective / max(8, criminalEffective), communityOrderBonus, courtOrderFloor)`, `safetyProfile.js:44`, `:56-74`) | survey E-9; refute E-9; power refute E-3 |
| D-10 | mercenary company / contracted force | `mercenary` bucket (`:98-100`); sole shipped member `Mercenary quarter` @ city `:2182` | paid, and a contract (HOLDS: annex `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2874-2880`; Mercenary hire p 1.0 `institutionServices.js:1074-1075`) · organized companies, hundreds to thousands (HOLDS: variants `:21-24`; the one recorded magnitude on the desk, still a count on the card's may-NOT line) | a term length; loyalty; who pays; foreign; whether the contract is honoured | the contracted force can be read from `hasMercenary` (the flag holds four rows, the bucket one, `priorityHelpers.js:49` vs `:98-100`; the mercenary chain gives a `Free company hall` town +4, `supplyChainData.js:893-897`, `defenseGenerator.js:598-600`, while DS-DEF-5's lens is silent, `defenseStateProse.js:1283-1285`) | survey E-10; refute E-10 |
| D-11 | charter hall | `charter` bucket (`:101-104`); `Adventurers' charter hall` @ hamlet `:325`, village `:837`, town `:1443`; `Multiple adventurers' guilds` @ city `:2175` | operating under a charter / licence (HOLDS for the three charter-hall rows: variants `:501-504`, `:689-692`, `:1229-1232`) · coordinating monster response (HOLDS on all three) · posting bounties (CONDITIONAL: hamlet and village rows; the town row says contracts) · work soldiers are the wrong instrument for (CONDITIONAL: "the things soldiers are wrong for"; never "the garrison's" or "the militia's" by name, since no garrison row exists at hamlet and the militia never generates at town) | a count of hands; specialists present now; the grantor; fees; busy; tenure | `Multiple adventurers' guilds` is a charter hall (no charter word, `:2175`; bucket `:103`) · the absence of a charter hall is a finding only where `countryWarrantsCharter` (frontier / plagued, `defenseStateProse.js:1241-1243`, `:1294-1297`) | survey E-11; refute E-11 |
| D-12 | granary | `hasGranary` keyword `granar` (`priorityHelpers.js:63`); `Town granary` `:925` (required), `City granaries` `:1590` (required), `State granary complex` `:2505` | grain is stored; it buffers a harvest; communal at town (HOLDS: services `institutionServices.js:1429-1431`) | how full; how many months (`storageMonths` defaults 4 when absent, `defenseGenerator.js:265`; town 5 / village 3.5 / thorp-hamlet-without-granary 1.5, `src/generators/foodGenerator.js:161-165`); who holds the key; drawn on; a count of buildings | a granary means the town eats through a bad year (`econHealthMult` to x0.45 `defenseGenerator.js:281-290`; disaster gate floor 0.55 `:608-618`) · `hasGranary` is a TIER PROXY: true on every town+ (required rows), false on every village-and-below whatever it stores; the thorp `Communal root cellar` `:111` is a reserve to `isolationSupport.js:47` and not to this flag | survey E-12; refute E-12 |
| D-13 | hospital / infirmary | `hasHospital` keywords hospital, monastery, healer, friary (`priorityHelpers.js:64`) | somewhere to put the sick; casualty treatment and outbreak containment (CONDITIONAL: a `hospital`-named row on the roster; `Healer (divine, 1st level)` `:845` and `Monastery or friary` `:1267` "MAY operate hospital/school" also set the flag) | beds; physicians; a count; that it can contain the outbreak; funding | "Hospital present" means a hospital (`Monastery or friary` sets `hasHospital`, `hasChurch`, `hasMonastery` and the `hasDivine` tradition at once, `priorityHelpers.js:64-67`, `defenseGenerator.js:104-107`) | survey E-13; refute E-13 |
| D-14 | church / parish / clergy | `hasChurch` keywords church, cathedral, temple, monastery, friary, shrine, priest, abbey (`:65`); ten rows incl. `Wayside shrine` `:42`, `:292` and `Access to parish church` `:50`, `:300` (required at hamlet, R-A) | consecrated ground, a place of observance (CONDITIONAL: a `Parish church` / `Parish churches` / cathedral / monastery row; `Access to parish church` records a church 2-5 km away in another settlement, `Wayside shrine` a marker with no clergy) | resident clergy; a priest; medical capability; wealth; age; a bell | Clergy care from `hasChurch` (universal at hamlet from the required access row; at town `Parish churches (2-5)` is required `:1260` and about half of towns print "clergy who tend the sick" annex `:2706` off a row recording parishes) | survey E-14; refute E-14 |
| D-15 | court / courthouse / the law | `hasCourtSystem` keywords courthouse, court buildings, democratic assembly, city hall, town hall (`:55`); `Town hall` `:1550` and `City hall` `:2268` are required | a process that runs (CONDITIONAL: "try" / "arrest, try and hold" only where a `Courthouse` / `Multiple courthouses` / `Multiple court buildings` row stands; "a dispute goes before a magistrate" where the Town hall's Dispute arbitration service is instantiated, `on: true, p: 0.8`, `institutionServices.js:1623-1625`) | judges; a code of law; sentencing; enforcement (the prison branch); impartiality | `hasCourtSystem` means a court (every town and city sets it from a meeting hall; `no legal infrastructure` and `detention without process` are effectively unreachable at town+, softened only by the arbitration service) | survey E-15; refute E-15 |
| D-16 | prison / gaol / detention | `hasPrison` keywords prison, stocks, large prison, massive prison (`:54`); `Small prison/stocks` `:1564`, `Large prison` `:2282`, `Massive prison` `:2514` | people can be held; public punishment at town (HOLDS: Holding cells p 1.0, Public punishment p 0.8, `institutionServices.js:1635-1638`) | capacity; conditions; sentences; a gaoler; anyone currently held | detention from a stocks-only row (hypothetical: the keyword `stocks` would set the flag from a pillory; note the general refuter R-L7: the shipped row DOES hold cells, so "stocks are not a prison" is itself a label read and must not become a rule) | survey E-16; refute E-16; general refute R-L7 |
| D-17 | port / docks / harbour | `hasPort` regex `\b(?:port\|docks?\|harbou?r\|shipyard\|navy)\b` (`priorityHelpers.js:32`, `:61`); `Docks/port facilities` `:987`, `:1652`; `Shipyard` `:1025`, `:1676`; `Harbour master's office` `:1726` | water access for bulk trade (HOLDS) · supply can arrive by water (CONDITIONAL: `config.tradeRouteAccess === 'port'` in a world where `worldLaw.supportsMaritime()`, `defenseGenerator.js:273-275`; not `hasPort`, since Docks admit a `river` route) · the sea (CONDITIONAL: off a `Shipyard` row only, `forbiddenTradeRoutes` includes river and the desc says ocean-going, `:1025`; never off `hasPort`) | the sea from `hasPort`; a fleet | a shipyard is a port (the regex says yes; the city `Shipyard` `:1676` records war vessels while `hasNavy` stays false; the DS-DEF-6 note speaks of sea approaches off a flag a river shipyard sets, `defenseDisplay.js:268-269`) | survey E-17; refute E-17 |
| D-18 | navy / fleet | `hasNavy` keywords navy, major port (`:62`) | no member: nothing to carry an entailment (HOLDS: zero of 316 catalogue rows match; `Naval Defense: Naval force` is unreachable on a native roster, `defenseStateProse.js:1608-1612`) | everything | (a pool with no producer) | survey E-18; refute E-18 |
| D-19 | arcane defense / wards | `magicDef` bucket (`:105-108`): wizard, mages' guild, mage, academy of magic, golem workforce, alchemist | practitioners of the named kind (CONDITIONAL: not the `Golem workforce` row `:2223`, which records no practitioner) | wards, detection, counterspells (the engine has one detection-shaped internal term the bucket does not see, `defenseGenerator.js:239-242`); that they work for the town; a count | an alchemist is an arcane defence (`:107`; the generator treats alchemy as an amplifier tradition with no military or monster term, `:117-119`) · `arcane defense ABSENT` on druid and divine towns the engine scores as defended (`Druid Circle` `:829`, `Warden's Lodge` `:1409`, `Elder Grove Council` `:1389` set `hasDruid` `defenseGenerator.js:112-114`; parish church / monastery / cathedral set `hasDivine` `:104-107`; none is in `magicDef`) · silence, not falsity, where `magicWorksAt` is false (`src/domain/worldPulse/magicWorksAt.js:49-53`) | survey E-19; refute E-19 |
| D-20 | terrain (the ground) | `TERRAIN_DATA[k].name`, seven (`src/data/geographyData.js:50`) | exactly the `strategicValue` sentence, for the terrains the desk maps (HOLDS: `geographyData.js:126`, `:208`, `:310`, `:418`, `:523`, `:621`, `:720`; maps `defenseStateProse.js:693-703`, `:719-723`, which leave Coastal and Riverside out of both pools) | elevation figures; a named feature; weather; that anyone chose the site; a cause of the town's location (annex `:2513-2515`) | the ground decides the wall's material (`stoneAvailability` / `timberAvailability` are institution and resource weights; `Town walls` is stone on Plains; the fortification chain's stone upstream applies on Plains as on Mountain) | survey E-20; refute E-20 |
| D-21 | terrain multiplier | tuning table `defenseGenerator.js:129-135` | the same works are worth more on a mountain than on a plain (HOLDS; the corpus's reading at annex `:2548`) | that riverside or coastal ground is neutral (both carry a small positive multiplier while the desk's lens is silent on them) | - | survey E-21; refute E-21 |
| D-22 | the readiness band word | `scoreBand` STRONG / ADEQUATE / WEAK / CRITICAL (`src/domain/display/defenseScoreBands.js:38-39`; readings `src/domain/compendium/bandLadders.js:202-207`) | exactly the ladder's reading (HOLDS) | a fifth gradation (annex `:3246-3248`); a trend; a cause; a comparison; what the score is made of | a badge on one arm says anything about another (annex `:2510-2513`: a STRONG header over a CRITICAL food arm is common) | survey E-22; refute E-22 |
| D-23 | the overall readiness label | `Fortress` ... `Undefended` (`defenseGenerator.js:515-521`) | the mean of the arms plus a tier bonus minus a threat penalty (HOLDS: `:490-521`; in a no-magic world the mean is over FOUR arms, `:502-505`) · not walls, not the badge's number (HOLDS: `posturePoolKey` reads `readiness.score`, `defenseStateProse.js:759-771`) | a wall row; a garrison; a wall material; a count | a thorp reading well-defended is well defended (+12 for being cheap to defend, `:491`, is a statement about scale) | survey E-23; refute E-23 |
| D-24 | the live band (DS-DEF-7, dark) | `surplus` ... `collapsed` (`src/domain/causalState.js:423-427`, `:444-449`) | a position on a 0-100 causal score whose neutral value is 50 (HOLDS: `:1034-1053`) | that `collapsed` means nobody is left (corpus `:3114`); `adequate` is the no-information reading (`:436`) | - | survey E-24; refute E-24 |
| D-25 | the safety label | `Very Safe` ... `Dangerous` (`safetyProfile.js:253-305`; readings `bandLadders.js:188-195`) | a RATIO of enforcement to criminal presence, floored two ways (HOLDS: `effectiveSafety = max(militaryEffective / max(8, criminalEffective), communityOrderBonus, courtOrderFloor)`, `safetyProfile.js:44`, `:56-74`) | absolute crime; contentment; freedom | `Very Safe` means a happy town (the ladder's own reading: "Fortress towns and occupations sit here", `bandLadders.js:189`) | survey E-25; refute E-25 |
| D-26 | the military posture words (fifteen) | `src/domain/display/defenseDisplay.js:25-41` over `STRESS_TYPE_MAP` `:61-68` | the stress type's own recorded consequences (HOLDS against `src/data/stressTypes.js:53-62`, `:64-72`, `:84-92`, `:104-113`, `:159-168`) | anything the English of the posture word adds: see label traps L-14 .. L-18 | the posture word is chrome, not prose (annex `:3159-3162`) | survey E-26; refute E-26 |
| D-27 | criminal structure words | `CRIM_STRUCTURE_DATA` `defenseDisplay.js:160-173`; classifier `:183-195` | a hierarchy, permission by somebody, suppression of what draws enforcement (CONDITIONAL: from a `Thieves' guild` row only; see REFUSED for the class-wide claim) · no numbers, no boss, no capture (HOLDS: capture is a separate ladder, annex `:2805-2807`) | numbers; a named boss; territory; tenure; that it reached the seat | `null` means no organized crime (`Rookery` `:1503`, `Assassins' guild` `:2459`, `Street gang` `:1466`, `Underground network` fall to `null`; the same file's `criminalOpNote` `:142-149` recognises them; `hasCriminalInst` `priorityHelpers.js:72` sees `assassins`; the Rookery's own desc says a standing organization backs it) | survey E-27; refute E-27 |
| D-28 | the capture rungs | `none` ... `capture` (`defenseStateProse.js:908-915`; producer `src/generators/power/rulingStructure.js:797`) | exactly the rung; `none` = nothing criminal reached the seat, not no criminal interest (HOLDS: `:928-945`) | who was bought; a price; a date; that the town knows | an absent capture state is `none` (the tab styles it so; the desk refuses it for a sentence, `:860-866`) | survey E-28; refute E-28 |
| D-29 | magic dependency | `defenseProfile.magicDependency` (`defenseGenerator.js:449-454`) | a qualifying STRESS is active: the binding term is the stress, not the magic (HOLDS: TRUE on 0 of 200 default worlds, 24 of 600 high-magic worlds all carrying a stress, `defenseStateProse.js:1668-1686`) | that the town's ordinary living runs on magic (corpus `:3211` states a standing dependency off a crisis flag: the desk's widest corpus-vs-producer gap) | - | survey E-29; refute E-29 |
| D-30 | blockade | `stockpile.blockaded` / `blockadeBypass` (`src/domain/worldPulse/foodStockpile.js:417-418`); holder `toll-bar` | the bypass, if any, is `teleport` or `airship` (HOLDS: `foodStockpile.js:313-316`; channels `foodGenerator.js:139-144`, forced by `isolationGenerator.js:88-90`) · a hostile fleet on the sea approaches, the port choked (CONDITIONAL: the naval layer's own writer of `blockade` at `foodStockpile.js:307` was not traced; "fleet" is the display note's word, `defenseDisplay.js:263-266`) | a besieger by name; duration; a cause beyond the stamp | live only in a campaign world (`defenseStateProse.js:1638-1648`) | survey E-30; refute E-30 |
| D-31 | the economic-upkeep gate | `economicGates.{military, monster, internal, economic, disaster}` (`defenseGenerator.js:467-472`, `:647-650`; floors 0.6 / 0.7 / 0.65 / 0.45 / 0.55) | the unpaid community baseline is exempt (CONDITIONAL: thorp / hamlet / village only, `:138-156`; at town+ the base is 0 and nothing is exempt) · no desertion, arrears or season (HOLDS) · the desk's `< 1` threshold, any shortfall at all (HOLDS: `defenseStateProse.js:1056-1058`; `Math.round(x*100)/100` at `:468` lets 0.99 fire it) · under R-viii': the shortfall lies on the wall's keeping AND the muster's wages alike, one purse, and the wall STANDS THROUGH it (ADDENDUM 12) | that anyone has deserted; arrears; a season; late rather than low pay; that the town chose this; a per-institution split of the purse at birth (R-viii') | an under-funded defence is a decaying defence (`:186-187`) · the gate is a wage model (see REFUSED D-R4: R-D) | survey E-31; refute E-31, X-3; ADDENDUM 12 |
| D-32 | stress / crisis (DS-DEF-8) | `STRESS_TYPE_MAP` fifteen types (`stressTypes.js:10-174`) | the type's own `viabilityNote` consequences (HOLDS; `slave_revolt` carries `requiresTier: "town"` `:141`, the only tier-gated stress) | duration; who caused it; whether it will end | the posture word is chrome (annex `:3159-3162`) | survey E-32; refute E-32 |

The defense survey's thirteen ENGINE CONTRADICTS rows (C-1 .. C-13) all HOLD per the refuter; C-2 and C-3 are CONDITIONAL on a post-ruin or imported roster at city (R-A); C-7 is stronger (the chain feeds the row stone); C-11 confirmed at `src/domain/display/threatAssessment.js:59`, `:66` (the engine's own defence prose calls every wall-bucket member a palisade, including a metropolis's). C-10 (two rosters on one tab: `threatAssessment.js:40-43` reads the generation snapshot, `defenseStateProse.js:604-609` the live ruin-filtered roster) and C-12 (a custom-content wall is `walls ABSENT` to this desk) stand.

#### REFUSED (defense)

| # | item tried | verdict | refuter's evidence |
|---|---|---|---|
| D-R1 | `charter hall` entails a charter for `Multiple adventurers' guilds` | REFUTED | the row's desc is "Competing organizations", no charter word (`institutionalCatalog.js:2175`); the bucket catches it on `multiple adventurers'` (`defenseInstitutionBuckets.js:103`) |
| D-R2 | `organized` entails a hierarchy, permission by somebody, suppression of what draws enforcement (class-wide) | REFUTED for two members | `hasSyndicate` (`defenseDisplay.js:187`) classifies `Front businesses` (`institutionalCatalog.js:1480`, `:2040`, a laundering cover with no hierarchy) and `Multiple criminal factions` (`:2006`, "Competing gangs. Turf disputes") as `organized`, against the classifier's own note (`:167`) and the corpus's "stable enough to negotiate with" (annex `:2815`) |
| D-R3 | `semi-organized` entails routes and territories | REFUTED for one member | `hasSemiOrg` (`:188`) fires on `gambling`: a town `Gambling den` (`institutionalCatalog.js:1520`, Entertainment) makes the town "coordinated enough to hold routes and territories" (annex `:2821`) |
| D-R4 | the economic-upkeep gate means the FUNDED portion is under-funded in the proportion stated (as a wage claim; "the muster's wages") | REFUTED as a wage claim | R-D: `milUpkeepMult = min(1, 0.6 + econOutput/50 * 0.4)` (`defenseGenerator.js:189`) is a function of economic output, not of any payroll; it is applied to `hasWalls +30`, `hasMilitia +10`, `hasWatch +7`, `hasCharterHall +10` exactly as to `hasGarrison +28` (`:160-165`, `:190-192`), and recorded whenever `hasAnyDefense` (`:467-470`), which a lone palisade satisfies. A village with `Palisade or earthworks` + a part-time `Citizen militia` (no wage recorded anywhere) reports `military: 0.8` at `econOutput 25` and DS-DEF-11 prints WALLED-STRAINED. The chair's R-viii' agrees on the ONE PURSE and keeps the word "wages"; the refuter says the word is a label read on a wage-less roster: see section 5, item O-1 |
| D-R5 | the survey's `[RECORDED]` tag on "timber" for `Palisade` | tag rejected (the entailment survives as DEFINITIONAL, conditional) | no wood word on the thorp row or its variants (`institutionalCatalog.js:97-99`, `institutionDescVariants.js:673-676`); the "wooden" cite is the hamlet's disjunctive row (`:345`) |

Live corpus faces the defense refuter found more knowledgeable than the simulation (for the rewrite, not ruled here; `defense.refute.md` section 7): DS-DEF-5 `watch PRESENT` v1 "inside the walls" (annex `:2903`; Town watch required, walls bc 0.5) · `garrison PRESENT` v3 "counted at the gate" (`:2895`; fires on a Barracks in a gateless town) · `charter hall ABSENT` v1/v3 "the garrison's problem", "sends soldiers" (`:2923`, `:2925`; no garrison row at hamlet/village) · DS-DEF-2 `settled, defenses beyond the need` v1 "substantial works", v3 "how relaxed the people on it are" (`:2621`, `:2623`) · DS-DEF-11 `WALLED-STRAINED` v1 "stone keeps itself, and wages do not" (`:5982`; `{defwork}` fills palisade; the gate fires on a wage-less village; also failed under R-viii') · DS-DEF-2 `walls with NO force` v1 "takes this town with ladders" (`:2641`; an earth berm needs none) · `granary AND parish care only` v1 "clergy who tend the sick" (`:2706`) · DS-DEF-5 `arcane defense ABSENT` all three (`:2933-2935`; druid and divine towns) · DS-DEF-4 `structure organized` all four (`:2815-2818`) · `structure semi-organized` v1 (`:2821`; a gambling den) · DS-DEF-6 `No reserves, landlocked` v1 "holds no food buffer at all" (`:3047`; a thorp root cellar and 1.5 months) · `Naval Defense: Port only` v3 "A stranger sails into" (`:3059`; a river dock).

### 2.2 GENERAL (DS-GEN-*, DS-REL-*, DS-POP-*, DS-HK-1; 193 census rows, 119 RESOLVED; packets `general.survey.md`, `general.refute.md`)

| # | noun | name class (members) | ENTAILS | NOT ENTAILED | ENGINE CONTRADICTS | source |
|---|---|---|---|---|---|---|
| G-1 | `{settlement}` | the page's own town (`src/domain/display/stateProse/generalStateProse.js:1712`) | this is the town whose page this is (HOLDS; a name failing `properFill` `:157-165` drops every variant, so the entailment can only be silent) | size; age; tier; walls; prosperity | - | survey §1; refute §1 |
| G-2 | `{counterpart}` | `neighbourNetwork[].neighbourName` (`:1877`) | a DIRECTED relation exists to a second settlement (CONDITIONAL: `localRelationshipRole` resolves; direction lives only in `relationshipFrom/To` + the role, stamped by `applyWorldPulseRelationshipGraph.js:196-216`; a legacy link with `relationshipType` alone is undirected and the desk withholds asymmetric types, `:1487-1492`) | that the counterpart is a town rather than a hold; its size; distance; standing | - | survey §1; refute §1 |
| G-3 | `{faction}` / `{faction2}` | `conflicts[].parties` (`src/generators/power/conflicts.js:240`) | a NAMED political actor is a party to this record (HOLDS on DS-GEN-2; DS-GEN-1 is DARK: `severity` is an array on 100% of tensions, `:388-420`) | its power; size; legitimacy; whether it governs | - | survey §1; refute §1 |
| G-4 | `{govFaction}` · `{governing}` · `{controller}` · `{npc}` | `narrativeGenerator.js:630-640`; `hookEscalation.js:495-506`; `hookEscalation.js:466`, `:479`; `npcConnections[].primaryNPCName` (`:1536`) | govFaction holds the seat on THIS note (CONDITIONAL: the temple-economy note, measured unreached in 384 configs, `:1020-1022`) · governing = the power as the clock names it (CONDITIONAL: a non-empty `governingName`; the lowercase fallback never reaches the desk) · controller = the hook names a holder (CONDITIONAL, vacuous: the desk declares no `controller` slot, `:102-137`) · npc = a cast person on a member receipt, this town's end (CONDITIONAL: no writer of `primaryNPCName` found outside the desk and components) | how the seat was held; tenure; hostility; any fate (product scope) | - | survey §1; refute §1 |
| G-5 | `{issue}` / `{stakes}` | `conflicts.js:237-242` template rows | the record states a matter in dispute (HOLDS: a template row picked from a table, gated by world law `:222-235`) · something FORWARD stands to change hands (HOLDS: annex `:5266`) | that either side is right; that it is old; that it will change hands; never synthesised from `issue` | - | survey §1; refute §1 |
| G-6 | `{steading}` / `{ruin}` / `{event}` | `SteadingsSection.jsx:26-33`; `historyGenerator.js:865-880`; `:1808` | a satellite is on this town's ledger (HOLDS) · a named fallen settlement stands nearby, with a duration (HOLDS; opt-in `config.ancientRuinsEnabled`, `:865`) · the record names this event (HOLDS) | a head count (`{band}` RESERVED, `:125-131`); who fell the ruin; that the event is still felt | absence of a ruin is "the flag is off", never "no ruin here" (`:1414-1416`) | survey §1; refute §1 |
| G-7 | `{founder}` / `{challenge}` | `founding.foundedBy`, `founding.initialChallenge` | a lowercase descriptive phrase with its own determiner (HOLDS, never rendered: 26 of 26 fail `proper`, annex `:236-259`) · the record names a first trial (CONDITIONAL, unfilled by design `:1787-1789`) | a founder's NAME; that the trial was overcome | - | survey §1; refute §1 |
| G-8 | `{timeband_since}` / `{timeband_age}` | `TIME_BANDS` (`src/domain/display/heraldCausalGrammar.js:333-340`) | an elapsed span as a band (HOLDS up to 60 years; `since: null` on the sixth band, so 89 of 108 events return `undefined`, `:454-465`) · an age as a band, total over the ladder (HOLDS: predicate form "older than its bearers") | a year; a date; a construction date (the engine holds none, annex `:127`) | - | survey §1; refute §1 |
| G-9 | `{institution}` / `{resource}` / `{good}` | `activeChains[].processingInstitutions[]` (`:1259-1268`); `rawResource` / `resource` (`:1348-1354`); `primaryImports[]` (`:1372-1379`) | institution = the chain row names a processing house (CONDITIONAL: the value is a CATEGORY label, e.g. `Merchant guilds (3-8)`, `Glassmakers`, matched to some roster row whose own name is never used; not "a named house") · resource = a raw input is named (CONDITIONAL: a snake_case token is refused, `:1212`; on STALLED the slot is deliberately unfilled `:1832-1843`) · good = a trade good is named (CONDITIONAL: a conforming import row exists; parenthetical rows refused `:1213`) | a named building; that the resource is worked or plentiful; price; volume | - | survey §1; refute §1 |
| G-10 | terrain family word | WATER-EDGE {coastal, riverside} · HIGH-GROUND {mountain, hills} · WOODLAND · OPEN-GROUND · DRY-GROUND (`:1073-1081`) | coastal / riverside: standing water at the edge of the site (HOLDS: `src/domain/resourceTerrainCompatibility.js:36`; `defenseGenerator.js:133-134`) · forest: standing timber (HOLDS; on most towns `forest` is the DEFAULT for route `isolated`, `src/generators/terrainHelpers.js:28`) · plains: open ground, no natural chokepoint (HOLDS: `defenseGenerator.js:135`; also the catch-all for an unknown route `terrainHelpers.js:32`) · desert: aridity (HOLDS: `foodGenerator.js:300`) · mountain / hills: relief (CONDITIONAL: relief only; "an approach that climbs" is a ROUTE fact, and `hills` with route `river` has a river approach) | a compass direction; a named river; a distance; a harbour (`hasPort` is a separate read); a defensible height; soil; climate; a season | terrain is DERIVED from the route where no override is set (`terrainHelpers.js:23-33`): WOODLAND and OPEN-GROUND are route defaults, not observations | survey §3.1; refute §2.1 |
| G-11 | approach word (`tradeRouteAccess`) | eight members (`terrainHelpers.js:23-33`) | river: a river (HOLDS `:26`) · crossroads: more than one road meeting (HOLDS; and every crossroads town is `isEntrepot`, `tradeGoods.js:94`) · mountain_pass / mountain_road: a route over relief (HOLDS; the pass carries a winter closure `foodGenerator.js:302-306`) · desert_road: a route across dry ground (HOLDS `:31`) · port: a water approach that carries cargo (CONDITIONAL: not the sea, since `port` + `riverside` is a river port, `narrativeGenerator.js:908-912`; and a port route with a non-water terrain override is not refused, `terrainHelpers.js:24`, so "a water approach" only where terrain is a water value) · road: a road (CONDITIONAL: a real value on DS-GEN-5 / 13 / POP-3; on DS-GEN-6 the `road` pool is the ELSE-ARM, see REFUSED) · isolated: no maintained through-route (CONDITIONAL: "no trade road" holds; "no route at all" does not, since the food model gives a `_minorRouteRate` trickle, `foodGenerator.js:307-309`) | traffic volume; toll income; road condition; a named road; age; who maintains it | DS-GEN-5 folds `mountain_pass` onto the isolated scene and `mountain_road` / `desert_road` onto `ordinary` (`:820-842`) while DS-GEN-13 / DS-POP-3 call `mountain_road` NARROW (`:1102`): one town is "ordinary" and "narrow" on one desk | survey §3.1; refute §2.1 |
| G-12 | market-class work | the NAME class `/\b(market\|bazaar\|exchange\|shambles\|stalls)\b/i` (`:1109`, from `src/domain/townMap/glyphAssign.js:100`) over `liveInstitutions` | entrepôt: goods pass through rather than originate here (CONDITIONAL: `isEntrepot` is a ROUTE fact on every crossroads town, `tradeGoods.js:90-97`; `transit` is at most four goods `:162`; the quantity claim "half of what changes hands" is unlicensed) · a flattened bazaar credits no market (HOLDS: the ruin filter `:1139-1141`; `impaired` stays live) · shambles = a meat market (CONDITIONAL, vacuous: no catalog key contains `shambles` or `stalls`) | a market day; a charter; a square; a frontage; a gate; prices; volume; stalls | (see REFUSED G-R4: the class-wide "goods are bought and sold" fails on four rows) | survey §3.2; refute §2.2 |
| G-13 | the DS-GEN-17 roster words | `ADMINISTERED` · `GARRISONED` · `LETTERED` · `PROVISIONED` · `BARE`, first antecedent wins (`:1178-1187`) over `compound.inst` (`priorityHelpers.js:41-79`) | ADMINISTERED: a civic seat AND a place of confinement (HOLDS as phrased: `hasCourtSystem` matches halls and an assembly, `:55`; `hasPrison` matches `Small prison/stocks`, which holds cells, `institutionalCatalog.js:1567`) · LETTERED: a practitioner-class row (HOLDS: `:68`; one `Hedge wizard` `:824` or a `Scroll scribe` `:1982` is the whole licence) · BARE: none of the four antecedents holds (HOLDS of `compound.inst`; as a statement about the PRESENT roster CONDITIONAL, since the booleans are minted at generation over the raw roster and never re-read, `economicState.js:51`, `:872`) · GARRISONED: a military-class row (CONDITIONAL: only if a wall counts as one, since `hasMilitaryInst` `:45` includes `walls` and `citadel`; `hasNavy` is dead on native rosters) · PROVISIONED: a store of grain (CONDITIONAL: only where `hasGranary` `:63`; the care arm fails, see REFUSED; the key cannot tell which arm fired) | that justice is done; staffing; honesty; a count; a school; that a store is full; that a house "still stands" after a played calamity (frozen booleans) | the booleans are FROZEN at generation and blind to a later calamity, while the sibling DS-GEN-13 filters (`institutionRoster.js:29-53`); `hasAny` is a substring test (`priorityHelpers.js:24-25`) | survey §3.3; refute §2.3 |
| G-14 | `{calamity}` / the event word | `EVENT_TYPE_NAMES` (`src/data/historyData.js:1547-1582`), lowercased by `calamityFill` (`:509-517`) | the great fire / the flood / the plague years = fire / water / disease (HOLDS `:1570-1572`) · the crash / the trade collapse = a market failure (HOLDS `:1567-1568`) · the occupation = an occupier who came and went (HOLDS `occupation_legacy` `:1556`) · the coarse `type` = the record's category (HOLDS `historyGenerator.js:588`; three categories are one-to-one with a differently named fine event, see label traps) | how many died; who did it; which quarter; a year; a season; a count; that it is still felt | (see REFUSED G-R8, G-R9: "the siege" and the BLOW reading of the fill) | survey §3.4; refute §2.4 |
| G-15 | `anchored` | `historicalEvents[].anchored` | the anchor pass REPLACED the event with a settlement-appropriate template on a 0.6 coin (HOLDS as the engine meaning: `historyGenerator.js:600-604`, `:630-640`; resource events `anchored: false` `:672`, `:691`, `:710`, `:741`) | that the event still bears on the present (see REFUSED G-R10; DS-GEN-16's "what remains" reading rides on `lastingEffects.length > 0`, `:646-648`) | - | survey §3.4; refute §2.4 |
| G-16 | recency label · `historicalCharacter` · founding record · ancient ruin | `:599-610`; `historyGenerator.js:843-853`; `founding{...}` + `history.age`; `history.ancientRuin` | recency = an elapsed span in years, banded (HOLDS; two ladders coexist, TIME_BANDS cuts 10/25/60y vs recency 10/30/80/200y, never pair a word from one with a cut from the other) · the five `historicalCharacter` branches = a COUNT over the timeline (HOLDS; `newly founded` needs `age <= 0`, reachable via `settlementAgeMode: 'new'` or `'custom'`, `:44-49`) · founding = a founding row, an age as a band, no construction date (HOLDS: annex `:127`; `settlementAgeMode: 'custom'` gives an exact age figure, which the prose still bands) · ancient ruin = a named fallen settlement nearby (HOLDS, opt-in) | a date; a generation count; whether anyone alive remembers; that an older town is larger (`AGE_BY_TIER` `historyData.js:1111-1118`) | (see REFUSED G-R11 for `stable and prosperous`) | survey §3.4; refute §2.4 |
| G-17 | fabric scar kind (DS-GEN-15, WIRING-UNRESOLVED) | eight kinds (`src/domain/worldPulse/urbanFabricKernel.js:305-314`) | burn_lots / plague_quarter / rubble_field = fire / disease / collapse (HOLDS `:319-323`) · siege_repairs / occupation_marks / lean_years = a lifted siege / a lifted occupation / famine (HOLDS `:672-676`) · scars DECAY on a kind-specific half-life (HOLDS `:644-647`: the one decay clock over built fabric in the estate, keyed on the calamity kind, not on any material) · flood_line = water (CONDITIONAL: the rule matches `/flood\|deluge\|storm\|tide/` `:322`, so a wind storm mints one) | where the scar sits; its shape; a district; a direction (annex `:141`) | an absent mirror yields no sentence, never a fallback | survey §3.4; refute §2.4 |
| G-18 | population words | `QUANTITY_BANDS` (`demographicsHerald.js:72-80`); `POP_READING` (`trendLens.js:24-30`) | `nobody` = a nonpositive count (HOLDS `:127-128`) · the trend verb = a RETROSPECTIVE reading over the window (HOLDS where window >= 2; band 0 is also the empty-ring answer `:47` and only DS-POP-3's gate `:1578-1586` separates them) · no forecast, no cause (HOLDS: annex `:4845`, `:4953`) · the band word = a head count inside the rung's ceiling (CONDITIONAL: the CEILING holds; the word's English does not, 41 souls reads "a hundred or so"; not a fill of this desk) | a digit; a ratio; who they were; why | the bands are owner-retunable prose (`:70-72`); the believed trend on a belief record is a different thing (`:1568-1570`) | survey §3.5; refute §2.5 |
| G-19 | standing, verdicts, scores | `scoreBand` (`defenseScoreBands.js:37-38`); readiness `defenseGenerator.js:515-521`; prosperity `prosperityRank.js:62-92`; food `foodGenerator.js:340-358`; safety `SAFETY_POOL_OF` `:240-249`; `viability.js:557-558` | the band word = the axis's number in that band (HOLDS; absent score -> null `:291-296`) · `scores.monster` = monster DEFENCE (HOLDS; `plagued` subtracts 15, `defenseGenerator.js:227`) · `scores.economic` = economic RESILIENCE, food-storage-driven (HOLDS `:257-290`) · `scores.magical` = 0 when magic is off or a small tier without a magic row (HOLDS `:296-307`; a cathedral lifts the gate) · readiness = the composite in band, with a tier bonus and threat penalty (HOLDS) · prosperity = the measured rung; `Poverty` unreachable natively (HOLDS `:187-217`) · food thresholds (HOLDS `foodGenerator.js:340-358`; both percentages are 0 when `dailyNeed` is 0) · `Deficit - Active Famine` = the famine STRESS is live (HOLDS: a config stress, can sit on a surplus town) · safety head word = the strain/base rung (HOLDS for the routed nine) · `viable` = no CRITICAL issue at the first survey (HOLDS; never re-run, `ViabilityTab.jsx:142-143`) · MARGINAL fires on an ABSENT verdict (HOLDS `:681-687`) | a cause; a history; a count; a trajectory; self-sufficiency; survival; a crime rate; a watch | ten temporal tokens are banned from every DS-GEN-3 / DS-GEN-11 variant (annex `:34`, `:106`) | survey §3.6; refute §2.6 |
| G-20 | relationship type word | `REL_DYNAMICS` (`src/generators/neighbourGenerator.js:48-95`); role `localRelationshipRole` (`:1483-1495`) | allied = a standing alliance (HOLDS `relationshipRulesCore.js:323-364`) · rival = competition (HOLDS `economyMode: 'compete'`) · cold_war = hostility short of war (HOLDS) · neutral = a recorded tie with no charge (HOLDS) · STATELESS: no origin, no since-when (HOLDS annex `:5546`) · overlord / vassal render nothing (HOLDS `:1477-1481`) · `prominentRelationship` / `flagDriven` (HOLDS `:757-798`; `flagDriven` was false on all 48, reached only in a stressed world) · trade_partner = a trade tie is recorded (CONDITIONAL: "goods move" is consistent but unmodelled; no goods ledger between settlements exists; "prices move together" is a quantity claim nothing holds) · patron / client = THIS town is the senior / junior end (CONDITIONAL: only where the role resolves; at generation both carry `economyMode: 'dependent'`, `:66-75`, and direction lives in the role alone) · hostile = open hostility (CONDITIONAL: not WAR, which is a separate condition/cost model) | when it began; a treaty; a distance; a road; that a tie "became" anything | a legacy row with `relationshipType: 'patron'` alone does not say which end this town is, and both asymmetric pools go silent (`:1471-1476`) | survey §3.7; refute §2.7 |
| G-21 | tensions, conflicts, warnings | `TENSION_LABELS` (`plotHooks.js:7-18`); `conflicts.js:238`; `narrativeGenerator.js:575-670`; `structuralValidator.js:416-441` | `occupation_legacy` / `outside_debt` = an occupation happened and ended / an obligation outward (HOLDS by the word; the block is dark) · a coherence note = a STANDING relation between two live fields (HOLDS; five of six pools unreached, `:1002-1030`) · `structuralSuggestions[]` = something is MISSING (HOLDS `:428-434`) · `structuralViolations[]` = the record disagrees with itself (CONDITIONAL: only `dependency_violation` `:436-441`; `tier_violation` and `by_design` rows are design-rule findings) | for eight of ten tension types, that anything HAPPENED (no event id, no `yearsAgo`, no cause); an origin for a conflict; the sum of a debt | (see REFUSED G-R12, G-R13) | survey §3.8; refute §2.8 |
| G-22 | steadings and lifecycle | `provenance` (`:1436-1440`); `charterPending` (`satellitesLedger.js:79`); `lifecycleStatus` (`SteadingsSection.jsx:18-21`) | `charterPending` = the charter has not yet been granted, and village scale reached (HOLDS `settlementLifecycleKernel.js:870-882`) · `lifecycleStatus` = the town has DIED (HOLDS `settlementLifecycleFirstClass.js:113-117`, `:612-624`; 0 of 48 on a fresh settlement, a lawful dark mount) · `forced` = a decree happened (CONDITIONAL: minted at `settlementLifecycleKernel.js:1207`, `:1216`; the refuter read the mint site, not its caller) · the organic arm = close enough to be counted, far enough to be its own (CONDITIONAL: the fold hides `resource_strike` and `resettlement`, two typed provenances the organic prose speaks over) | a head count; a distance; a road; a name for the decree; any named person's fate | - | survey §3.9; refute §2.9 |
| G-23 | hook category / clock id | `hookEscalation.js:387-436`; `plotHooks.js:31-39` | `legitimacy_crisis` = Contested or worse (HOLDS `:487-498`) · the stages are a TEMPLATE, not a record (HOLDS) · the pool joins on the id segment, never the label (HOLDS `:729-745`) · hook category (HOLDS) · `faction_split` = two factions within 8 power with differing archetypes (CONDITIONAL: "conflicting wants" = differing archetype, "overlapping" = within 8, `:507-520`) | that any stage has happened; a historical clause | (see REFUSED G-R14, G-R15: `bread_riot` / `smuggling_rise` fire on a superset of their trigger text) | survey §3.10; refute §2.10 |

The general survey's seventeen ENGINE CONTRADICTS rows (C-1 .. C-17) stand as the refuter graded the underlying rows; C-6 (stone endures / timber rots: the only decay clock over built fabric is the scar half-life, keyed on the calamity kind, `urbanFabricKernel.js:305-314`) is the general desk's counterpart of the chair's walls-never-decay finding.

#### REFUSED (general)

| # | item tried | verdict | refuter's evidence |
|---|---|---|---|
| G-R1 | `{reason}` = the record TYPED a reason | REFUTED | the only candidate, `history.founding.reason`, is a PREDICATE CLAUSE ("was founded by foresters managing the woodland under charter"), not a typed value; the desk refuses to fill it (`generalStateProse.js:1790-1795`) |
| G-R2 | `{calamity}` = a typed BLOW of that name is on the record | REFUTED | `calamityFill` (`:509-517`) accepts ANY `The` + Title Case event name; `recordCalamityEvent` (`:660-667`) selects any row with `lastingEffects`; resource events (`historyGenerator.js:661-676`) yield "the great harvest compact", "the iron dispute"; `EVENT_TYPE_NAMES` also yields "The Pilgrimage", "The Return", "The Influx". The fill's class is EVENT, not BLOW |
| G-R3 | `road` (DS-GEN-6) = a road | REFUTED on DS-GEN-6 | `ORIGIN_POOL_OF_ROUTE[...] \|\| 'road'` over a four-key table (`:887-892`, `:964-967`): `mountain_pass`, `mountain_road`, `desert_road` and an ABSENT route all read "{settlement} is on a road and lives off the road" |
| G-R4 | market-class work = a place where goods are bought and sold | REFUTED | the NAME test credits `Whisper market` (`institutionalCatalog.js:2086`, brokers of knowledge), "Chroniclers' exchange" (`:1759`, paid correspondents), `Slave market` (`:1036`, persons), `Black market` / `Black market bazaar` (the CRIMINAL roster, `priorityHelpers.js:76-77`); MARKET-OPEN's "Market day is {settlement} at its truest" over a black-market-only town is a false face |
| G-R5 | `ADMINISTERED` = "keeps a court" (corpus text 1) | REFUTED | a `Town hall` + stocks town resolves it; "court" only where the row is `Multiple courthouses` / `Multiple court buildings` |
| G-R6 | `GARRISONED` corpus faces: "keeps professionals for its safety", "pays for its own defense in wages, not only in stone" | REFUTED | `hasMilitaryInst` (`priorityHelpers.js:45`) includes `'walls'`, so a walls-only town resolves GARRISONED; and "in stone" asserts a wall material the defense desk owns through `{defwork}` (a palisade is a wall-class member). The annex's own gloss "a force the town PAYS for" (`RECEIPT_POOLS_DOSSIER_STATE.md:152`) is contradicted by the code |
| G-R7 | `PROVISIONED` care arm = a house of care | REFUTED | `hasHospital` (`:64`) matches `Healer (divine, 1st level)` (`institutionalCatalog.js:845-849`, a PERSON, licence held at `low` for the deity doctrine) and `Monastery or friary` ("MAY operate hospital/school") |
| G-R8 | "the siege" = a besieging force | REFUTED | 'The Siege' names `external_threat` (`historyData.js:1560`), whose template is "News of approaching danger divided the community on how to respond", factions Prepare for war / Seek diplomacy / Flee (`:1182-1184`): a DIVIDED RESPONSE, not a siege that happened |
| G-R9 | mountain / hills = an approach that climbs | REFUTED as a terrain fact | the APPROACH is `tradeRouteAccess`; `hills` with route `river` (an override, `terrainHelpers.js:24`) has a river approach; license relief only |
| G-R10 | `anchored: true` = the event still bears on the present | REFUTED | a narrative-fit stamp on a `_rng() < 0.6` coin (`historyGenerator.js:600-604`, `:630-640`); the ancient-ruin event is `anchored: true` (`:877`); DS-GEN-16's "what remains" reads `lastingEffects.length > 0` (`:646-648`), not the flag |
| G-R11 | `stable and prosperous` = a prosperity reading | REFUTED | the ELSE-ARM of a timeline COUNT (`historyGenerator.js:848`): a `Struggling` town with a quiet timeline reads "stable and prosperous" |
| G-R12 | tension `type` is closed at 10 | REFUTED | `TENSION_LABELS` is a DISPLAY list; the producer can write more than twenty types and fourteen would reach no pool (`generalStateProse.js:415-418`); closed in the CORPUS, open in the PRODUCER |
| G-R13 | conflict `intensity` = how close the parties are to violence | REFUTED | `intensity = rivalries.length > 1 ? 'high' : rivalries.length === 1 ? 'moderate' : 'low'` (`conflicts.js:238`): a COUNT of recorded rivalry relationships |
| G-R14 | clock `bread_riot` = "Food supply chain is strained or worse", and only that | REFUTED for "only that" | `if (chain.status === 'stable') continue;` (`hookEscalation.js:463-466`): the clock fires on `substituted` (a magically sustained chain) and `captured` too (`supplyChainState.js:178`, `:186`) |
| G-R15 | clock `smuggling_rise` = "Trade chain is strained or worse" | REFUTED (same shape) | `:470-481`, any non-stable `trade_entrepot` chain |

Corrections the general refuter made to the survey's instrument reads: custom-content rows never reach the substring matchers (`customContentSemanticAuthority.js:46-60`), and 'Small prison/stocks' DOES hold cells (`institutionalCatalog.js:1567`), so the survey's "stocks are not a prison" (A-6, L-12) is itself a label read; FOUNDED-YOUNG / `newly founded` are reachable by `settlementAgeMode` (`historyGenerator.js:44-49`), the "youngest town 81y" figure being the `auto` draw only.

