---

## 3. LABEL TRAPS, MERGED ACROSS THE SIX DESKS

An engine value or label whose English meaning is not its engine meaning. Every row: the label as a reader meets it · the field · the engine meaning quoted or paraphrased from code · the source. Rows the surveyors and refuters raised on more than one desk are merged once, with every desk's citation kept; the desk tags in brackets name the packets that raised the row. Grouped by the family the label belongs to.

### 3.1 The country and the ground

| # | label | field | engine meaning | source |
|---|---|---|---|---|
| L-1 | `plagued` | `config.monsterThreat` | "The surrounding region is plagued by MONSTER activity." Never disease. On the monster arm it adds +8 then subtracts 15 after the gate, so the tier word LOWERS the monster score it seems to raise; on the stress roll it multiplies `under_siege` and `monster_pressure` by 2.5 and `wartime` by 1.3 and never touches `plague_onset`; on the power desk it mints the stability label `Tense — regional monster threat` or appends `; monster threat active`; the structural validator spells the same value "Embattled region". The disease facts of the estate are the `plague_onset` stress, the `plague_quarter` scar and the `Quarantined` head word, and `monsterThreat` is none of them. [defense L-1, X-9; general L-9; power L-7; stressors §5; warFaith §5] | `src/components/new/SummaryTab.jsx:28`, `:37`; `src/data/monsterThreat.js:28`; `src/domain/display/stateProse/defenseStateProse.js:279-284`; `src/generators/defenseGenerator.js:194-198`, `:227`, `:498`; `src/generators/stressGenerator.js:119`, `:230`, `:172-180`; `src/generators/power/governanceNarrative.js:329-338`; `src/generators/structuralValidator.js:551-572` |
| L-2 | `heartland` ⇄ `settled`; `frontier`; `civilized` | `config.monsterThreat` | The producer says `heartland` and the corpus says `settled`; the desk keys on the producer token ("KEY ON THE CANONICAL PRODUCER TOKEN, NEVER ON THE CORPUS WORD"). `heartland` is LOW raider / monster exposure (the stress roll multiplies siege / monster by 0.3 and occupation by 0.4), not "the centre of a realm". `frontier` is the DEFAULT: `normalizeMonsterThreat(undefined) === 'frontier'`, so a face reading `frontier` off an unmeasured town describes a default (the desk refuses it, `measuredMonsterFamily` requires the raw value). `civilized` is forwarded UNCHANGED though not a canonical tier. [defense L-2, L-3, L-4; stressors refute §4.13] | `defenseStateProse.js:254-284`, `:271-276`, `:316-334`; `src/data/monsterThreat.js:55-56`; `stressGenerator.js:121`, `:168` |
| L-3 | `forest` / `plains` (terrain) | `config.terrainType` | Derived from the ROUTE when no override is set: `isolated → forest`, `road → plains`, `crossroads → plains`; `plains` is also the catch-all for an unknown route. WOODLAND and OPEN-GROUND are route defaults on most towns, not observations. [general L-11, C-1] | `src/generators/terrainHelpers.js:23-33` |
| L-4 | `port` (route) on non-water terrain; `port` on `riverside` | `config.tradeRouteAccess` × `terrainType` | `getTerrainType` honours ANY override, so `port` + `hills` is representable and DS-GEN-5 prints the `port` scene ("runs on the tide", "wharves") beside HIGH-GROUND. On `riverside` terrain a port is a RIVER port: `maritimeSupported = terrainType === 'coastal' \|\| (tradeRoute === 'port' && terrainType !== 'riverside')`, `portKind` may be `river`. "The sea" is licensed only where `maritimeSupported`. [general R-L13; economy A.3, §C] | `terrainHelpers.js:24`; `src/domain/display/stateProse/generalStateProse.js:862-866`; `src/generators/narrativeGenerator.js:908-912`; `src/generators/generationContext.js:195-205` |
| L-5 | `river` (access) | `hasSeaPort` | Counts as a SEA modality in the arrivals tally (`access === 'port' \|\| 'sea' \|\| 'river' \|\| 'coastal'`). A river town is a sea-modality town to the throughput model. [economy A.4, §C] | `src/domain/spatial/tradeFlow.js:112-115`, `:142` |
| L-6 | `sea` / `coastal` (access) | `ROUTE_TIER`, `hasSeaPort` | Accepted readers' tokens with NO producer; `tradeRouteSemantics.js:89` calls `coastal` a "real generated value", but no pool ever rolls them. [economy §C] | `src/generators/steps/resolveConfig.js:29-37`, `:132-133`; `src/domain/tradeRouteSemantics.js:83-97` |
| L-7 | `mountain_pass` | route tier `seasonal` | An ANNUALISED 0.4 share of a road ("snowed shut in winter"); never a live closure; and no terrain route pool produces it on a generated town. [economy 3.1, §C; general L-10] | `tradeRouteSemantics.js:34-47`, `:76`, `:90`; `src/generators/foodGenerator.js:304-317`; `resolveConfig.js:29-37` |
| L-8 | `isolated` | three models | "cannot import anything" to the trade model; a minor-route food trickle for town+ / village to the food model; "nothing reaches it" to the desk. The only safe spelling is no approach to name. [economy A.8, §C; general §2.1 `isolated`] | `src/generators/economy/tradeGoods.js:53-56`; `foodGenerator.js:168-176`, `:293-296`, `:307-309`; `src/domain/display/stateProse/economyStateProse.js:167-170` |
| L-9 | `road` (DS-GEN-6 pool key) | `config.tradeRouteAccess` | The ELSE-ARM: `ORIGIN_POOL_OF_ROUTE[...] \|\| 'road'` over a four-key table, so `road`, `mountain_pass`, `mountain_road`, `desert_road` and an absent value all read "on a road". [general L-10, G-R3] | `generalStateProse.js:887-892`, `:964-967`; `src/generators/narrative/settlementOriginProse.js:160-172` |
| L-10 | `Riverside` / `Mountain` / `Desert/Arid` | `resourceAnalysis.terrain` | The DISPLAY name, which differs from the corpus pool word for three terrains of seven; a route on the display name would darken three terrains without an error anywhere ("THE LABEL TRAP, MEASURED, AND IT IS LIVE IN THE TREE ALREADY"). [economy §4] | `economyStateProse.js:660-668`; `src/data/geographyData.js:129`, `:211`, `:624` |
| L-11 | `agricultureCapacity` vs `TERRAIN_AGRI` | two fertility tables | They disagree on five of seven terrains (hills vs coastal invert); the comment "(matches TERRAIN_DATA)" is false; the canonical food model reads `TERRAIN_AGRI`. [economy A.6, §C] | `foodGenerator.js:37-41`, `:205`; `geographyData.js:68`, `:144`, `:227`, `:329`, `:437`, `:542`, `:638`; `src/generators/economy/foodBalance.js:46` |
| L-12 | `strategicValue`, `situationDesc`, `economicDragDesc`, `dependency.impact`, `entrepotNote`, `magicNote` | engine-authored strings | Printed by the surface already; the prose FRAMES them and never restates them. [economy §4; defense E-20] | `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:797-798`, `:1569`, `:1099-1101`, `:994-997` |

### 3.2 The roster flags (substring matches over institution names)

| # | label | field | engine meaning | source |
|---|---|---|---|---|
| L-13 | `hasAny` (every `inst.*` flag) | `priorityHelpers.js:24-25` | A SUBSTRING test, not a word test: `keywords.some(k => nameList.some(n => n.includes(k)))`. The one word-bounded exception is `PORT_INFRA_RE`, added because `Teleportation circle` read as a harbour. Custom-content rows never reach any flag (`nativeSemanticName` returns `''`), while name tests over `liveInstitutions` and the criminal classifier DO reach them. [general §3.3, R-A1; defense R-E, C-12] | `src/generators/priorityHelpers.js:24-25`, `:32`; `src/domain/content/customContentSemanticAuthority.js:22-32`, `:41-60`; `src/domain/display/defenseDisplay.js:185` |
| L-14 | `Hospital present` / `hasHospital` / PROVISIONED's care arm | `compound.inst.hasHospital` | Keywords `hospital`, `monastery`, `healer`, `friary`: a village's `Healer (divine, 1st level)` ("Basic healing spells. A closed wound costs 10 in gold", a PERSON, licence held at `low` for the deity doctrine) and a `Monastery or friary` ("MAY operate hospital/school") both set it; the monastery also sets `hasChurch`, `hasMonastery` and the generator's `hasDivine` tradition, one row and four flags. [defense L-8, X-12; general L-12, R-L9; economy D.8; warFaith F2] | `priorityHelpers.js:64-67`; `src/data/institutionalCatalog.js:845-849`, `:1267-1271`; `defenseGenerator.js:104-107`, `:300-301` |
| L-15 | `Clergy care` / "clergy who tend the sick" / `hasChurch` | `compound.inst.hasChurch` | Keywords include `shrine`, `priest`, `abbey`: `Wayside shrine` ("Simple prayer marker. No resident clergy."), `Access to parish church` ("Walk 2-5km to village church", a church in ANOTHER settlement, `required: true` at hamlet so the flag is TRUE ON EVERY HAMLET), `Priest (resident)` with no building. At town `Parish churches (2-5)` is required and records parishes, not care. [defense L-9, X-2, C-4; general L-12, A-3; warFaith §5] | `priorityHelpers.js:65`; `institutionalCatalog.js:42`, `:50`, `:292`, `:300`, `:1260` |
| L-16 | `Court only` / `Court + Prison` / "can arrest, try and hold" / `hasCourtSystem` / ADMINISTERED | `compound.inst.hasCourtSystem` | Keywords include `town hall`, `city hall`, `democratic assembly`; `Town hall` and `City hall` are `required: true, baseChance: 1`, so every generated town and city sets the flag from "Meeting place and administrative center". Softened only by the Town hall's `Dispute arbitration` service ("before a magistrate", `on: true, p: 0.8`): a civil PROCEDURE is recorded on most town halls, a criminal trial is not. [defense L-10, E-15; general L-1 (ADMINISTERED), L-12, G-R5] | `priorityHelpers.js:55`; `institutionalCatalog.js:1550`, `:2268`; `src/data/institutionServices.js:1623-1625` |
| L-17 | `Prison only` / "detention" / `hasPrison` / `stocks` | `compound.inst.hasPrison` | Keyword `stocks`; the shipped town row is `Small prison/stocks`, "Holding cells AND public punishment", so it DOES hold cells: the survey's own "stocks are not a prison" is itself a label read of the key's tail and must not become a rule; a stocks-only row would set the flag from a pillory, but no such row ships. [defense L-11, E-16; general L-12, R-L7] | `priorityHelpers.js:54`; `institutionalCatalog.js:1564-1567` |
| L-18 | `Naval force` / `hasNavy` | `compound.inst.hasNavy` | Keywords `navy`, `major port`: NO shipped catalogue row matches (316 rows dumped; the 230 catalog keys grepped). `DS-DEF-6 Naval Defense: Naval force` and GARRISONED's `\|\| hasNavy` arm are unreachable on a native roster; and `major port` would make a PORT a NAVY if the row existed. [defense L-12, E-18; general R-L8] | `priorityHelpers.js:62`; `defenseStateProse.js:1608-1612` |
| L-19 | `hasPort` | `compound.inst.hasPort` | The word-bounded regex `\b(?:port\|docks?\|harbou?r\|shipyard\|navy)\b` fires on a `Shipyard` (a building yard), a `Harbour master's office` (an OFFICE) and `Docks/port facilities` ("River or coastal dock", `tradeRouteRequired: ['port','river']`), so `hasPort` never entails the sea; the sea is a `Shipyard` fact (`forbiddenTradeRoutes` includes `river`, desc "ocean-going") or a `maritimeSupported` fact. [defense L-24 note, X-10, E-17; general L-12, R-L12; economy D.1] | `priorityHelpers.js:32`, `:61`; `institutionalCatalog.js:987`, `:1025`, `:1676`, `:1726-1730`; `generationContext.js:195-205` |
| L-20 | `Granary present` / `No reserves` / `hasGranary` / `granar` | `compound.inst.hasGranary` | The stem `granar`, so the flag is about the BUILDING and never the stock; and a TIER PROXY: `Town granary` and `City granaries` are required, no village-and-below row matches, so every `granary ...` pool is town+ and every `no reserves ...` pool is village-and-below whatever it stores (the thorp `Communal root cellar` is a reserve to `isolationSupport.js` and not to this flag; a town with NO granary building still has a granary reading at 1.5 to 2.0 months). The shipped terrain data carries a truncated row `name: "granar"` the substring matches. [defense L-21, X-1, E-12; economy §4 `granar`, 3.8, 5.6; general A-8] | `priorityHelpers.js:63`; `institutionalCatalog.js:111`, `:925`, `:1590`; `src/domain/worldPulse/isolationSupport.js:47`; `foodGenerator.js:161-165`; `src/domain/worldPulse/foodStockpile.js:190-193`; `geographyData.js:460` |
| L-21 | `hasMarket` / `Market Taxes` / `Criminal Syndicate Revenue` | `compound.inst.hasMarket`; income labels | `hasMarket` matches `market`, `bazaar`, `fair`, `trade center`, `exchange`: a `Black market`, a `Slave market`, a `Horse market`, an `Annual fair` (PERIODIC) all set the standing-market flag that feeds `scores.economic` +10, `marketInstMult` and the food import model; `Market Taxes` is also earned from an `annual fair` alone; `Criminal Syndicate Revenue` fires because `hasMarket` matches `underground` (Underground City). [economy 5.1, §C; general A-9, R-A8] | `priorityHelpers.js:56`, `:394`, `:403-410`; `defenseGenerator.js:268`; `foodGenerator.js:130`, `:153`, `:190`; `src/generators/economy/economicState.js:90-97`, `:326-334`; `institutionalCatalog.js:950` |
| L-22 | `hasGuild` / `Guild Fees` / `Thieves' Guild Revenue` | `compound.inst.hasGuild`; income labels | The bare token `guild` fires on `Thieves' guild`, `Assassins' Guild`, `Mages' guild`; `deriveEconomicComplexity` reads `hasInst('market','trading','merchant','guild')`, so a Thieves' guild raises the complexity reading and pays "Guild Fees" to the town; `Thieves' Guild Revenue` fires on `crimInsts` containing `guild` OR `thieves`, so an `Assassins' Guild` alone yields it. [economy 5.7, §C; general A-10] | `priorityHelpers.js:57`, `:69`, `:72-73`; `economicState.js:107-113`, `:323-336`, `:882`; `safetyProfile.js:596` |
| L-23 | `hasMercenary` / `hasCharterHall` / `hireling hall` | `compound.inst.*` | `hasMercenary` holds four rows (`Mercenary quarter`, `Free company hall`, `Veteran's lodge`, `Hireling hall`) while the DEFENCE bucket holds one; `hasCharterHall` also matches `hireling hall` ("Job board for torchbearers ... porters"), so one job board is both a contracted force and a monster-response charter for scoring while the bucket catches neither. [defense A-5, A-6, E-10; warFaith §6] | `priorityHelpers.js:49`, `:51`; `src/domain/institutions/defenseInstitutionBuckets.js:98-104`; `institutionalCatalog.js:881`, `:1370`, `:1436`, `:2182`; `defenseGenerator.js:164` |
| L-24 | `hasMilitaryInst` / GARRISONED | `compound.inst.hasMilitaryInst`; DS-GEN-17 key | The list `['garrison','barracks','guard','watch','citadel','walls','militia','mercenary','navy','charter hall']` INCLUDES `walls` and `citadel` and EXCLUDES `Palisade`, `Palisade or earthworks` and `Gates (if walled)`: a walls-only town resolves GARRISONED (the annex's gloss "a force the town PAYS for rather than merely a wall it built" is contradicted by the code) and a palisade village gets no guard sentence at all (`guardEffectivenessDesc` is gated on it). [general L-1, C-2, G-R6; defense A-18] | `priorityHelpers.js:45`; `generalStateProse.js:1179-1184`; `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:152`; `safetyProfile.js:317-318` |
| L-25 | `hasGates` | `compound.inst.hasGates` | Fires on `palisade` as well as `gates`, `town walls`, `city walls`, `massive walls`, and feeds a gate note in the safety profile, so a palisade town gets gate prose. [defense X-6, E-5] | `priorityHelpers.js:53`; `safetyProfile.js:462` |
| L-26 | `hasWatch` / `hasGarrison` (one row, two flags) | `compound.inst.*` | `Professional city watch` is in BOTH the `garrison` and the `watch` lists and buckets; `deriveArmedForces` de-duplicates by name for this reason; `forceCorePoolKey` reads garrison FIRST, so a city whose only force is the watch renders `garrison PRESENT`; `safetyProfile` renders "garrison and city watch" for the one row while the power generator's label ladder calls the same town "The watch". `hasWatch` is town-and-up only (`Town watch` is required at town, so every town has one). [defense A-4, E-9, R-A; power A-α; economy 5.3; stressors A2; warFaith §6] | `priorityHelpers.js:46-48`; `defenseInstitutionBuckets.js:89`, `:96`; `defenseDisplay.js:334-335`, `:350`; `defenseStateProse.js:1269-1276`; `safetyProfile.js:263-268`, `:321-322`; `governanceNarrative.js:500-502`; `institutionalCatalog.js:1348-1355` |
| L-27 | `hasMagicInst` / `Arcane support` / LETTERED / `magicDef` | `compound.inst.hasMagicInst`; the `magicDef` bucket | Keywords include `alchemist`, `scroll scribe`, `enchant`, `hedge wizard`: one hedge wizard or a scroll shop makes a town LETTERED; the DS-DEF-6 status text fills from `magicDef.slice(0,2)`, a different bucket again; the bucket is name-matched arcane / alchemy while the generator's magical defence is FOUR traditions (arcane, divine, druid, alchemy), so `arcane defense ABSENT` prints on druid and divine towns the engine scores as defended, and an `Alchemist shop` (an amplifier tradition with no military term) reads as an arcane defence. [defense L-22, X-14, E-19; general L-12 (LETTERED)] | `priorityHelpers.js:68`; `defenseDisplay.js:227`; `defenseInstitutionBuckets.js:105-108`; `defenseGenerator.js:93-119`, `:208-212`; `institutionalCatalog.js:824`, `:829`, `:1389`, `:1409`, `:1982`, `:2223` |
| L-28 | `Access to external mill` / `Access to parish church` | roster rows named after the thing | ABSENCE rows named after the thing they lack: `Access to external mill` ("Must travel to manor/village mill") matches the glyph class `wheelhouse` and the civic class `craft` (`mill`), and is a `processingInstitutions` member of the grain chain and a `TRADE_DEPENDENCY_NEEDS` key that passes `properFill`; `Access to parish church` sets `hasChurch`. The owner's "a mill grinds" meets a roster row that is the town NOT having a mill. [general R-L10; economy 3.7] | `institutionalCatalog.js:123`, `:50`; `src/domain/townMap/glyphAssign.js:93`; `src/domain/prose/wiringCensus.js:1316`; `src/data/economicData.js:9-20`; `economyStateProse.js:333-340` |
| L-29 | `Mill` (terrain modifier, storage, flour) | `institutionModifiers`; `has('mill')`; `localProduction` | Every reader is a substring: the riverside terrain row `Mill` is matched by `includes()`, so a Sawmill / Fulling mill / Windmill takes the water-milling boost; `storageCapacityMonths` does `has('mill')` and multiplies granary capacity by 1.25 for any of them; `tradeGoods.js:29` makes any name containing `mill` or `baker` a `flour` producer; the FOOD/AGRICULTURE tag backfill on `mill` makes every mill a GRAIN processor. [economy 5.2, D.6, §C; general R-A3] | `geographyData.js:170`, `:258-261`; `foodStockpile.js:194`; `foodGenerator.js:122`; `tradeGoods.js:29`; `src/lib/entities.js:171` |
| L-30 | `food` (the institution TAG) / `fullyExploited` / `{institution}` on the exploitation lens | processing-tag matcher; `processingInstitutions[0]` | The `food` tag is declared on Taverns, Ale house, Toll bridge, Water source, Caravaneer's post and backfilled on any name matching `/mill\|...\|field\|.../`, so a town with a Tavern and an Ale house has its grain chain FULLY EXPLOITED and `{institution}` fills `Mill` from the STATIC catalog row, a house the town does not have. [economy A.1, §C] | `institutionalCatalog.js` (tags scan); `entities.js:171`, `:228-232`; `src/data/resourceChains.js:56`, `:58`; `src/generators/resourceGenerator.js:140`, `:155-181` |
| L-31 | `Fishing & water` / `salt` (local commodity) / `Agricultural Rents` / `Property Rents` / `Toll Revenue` / `Entrepôt Trade` | food chains; `localProduction`; `incomeSources[]` | `hasInst('fish')` makes a Fishmonger a fishery; any name containing `dock`, `port` or `fishmonger` produces `salt`; `Agricultural Rents` is a TIER default at thorp / hamlet / village with no farm required; `Property Rents` "within the walls" is gated on tier only, walls never checked; `Toll Revenue` "roads and bridges" is `route === crossroads` with no bridge or toll institution; `Entrepôt Trade` "warehouse fees" is a crossroads route at town+ with no warehouse checked. [economy §C] | `foodGenerator.js:126`, `:151`; `tradeGoods.js:35-36`, `:139-144`; `economicState.js:55-60`, `:156-163`, `:174-179`; `src/generators/economy/helpers.js:41` |
| L-32 | `Watchtower` | a SERVICES row with no catalogue row | `Watchtower` ("Signal fire ... Night watch") exists in `institutionServices.js` and nowhere in the catalogue, a dead vocabulary a writer could cite; and the power generator's `includesInstitution('watch')` matches it, so "The watch" is minted on a masonry tower. [defense §6.7; power E-19a] | `institutionServices.js:1561-1565`; `governanceNarrative.js:74-75` |
| L-33 | "fortified" / "walled" inside a desc | `Waystation`, `Caravanserai`, `Burial ground` descs | "A fortified overnight stop", "walled waystation", "A walled plot" carry wall words in the DESC and are in no wall class; a writer reading rosters will find "walled" on unwalled towns. [defense Y-7] | `institutionalCatalog.js:405`, `:531`, `:538`; the hamlet `Burial ground` row |
| L-34 | the recorded `desc` | `inst.desc` | One of up to THREE seeded strings (`pickVariant([inst.desc, ...variants])`): a property is recorded only if every variant keeps it (the thorp `Palisade`'s "wooden" is in none; its variants say "sharpened stakes" / "Pointed stakes"). [defense R-B, X-8, D-R5] | `src/generators/steps/assembleInstitutions.js:735-747`; `src/data/institutionDescVariants.js:673-676` |
| L-35 | `Citizen militia` @ town; `Massive walls and fortifications` @ metropolis | catalogue rows | Rows no generated world carries: the required `Town watch` shares `civilianDefense` and splices the militia; the merged city+metropolis catalogue iterates the required `City walls and gates` before `Massive walls` in the same `defenseLevel` group. Every entailment written for them licenses nothing natively. [defense R-A, X-7] | `assembleInstitutions.js:117-124`, `:244`, `:283-293`, `:345-346`; `institutionalCatalog.js:1340-1355`, `:1910`, `:2347` |
| L-36 | "fortification" / "force" (the threat plan's regexes) | `FORTIFICATION_NAME`; `MILITARY_FORCE_NAME` | `barracks`, `garrison` and `fortress` COUNT AS A FORTIFICATION, so a frontier town with a Barracks is not given walls and its receipt reason "requires a defensible perimeter" is written for a billet; the plan's force regex admits `levy` and `guard` while the buckets admit neither, so a thorp `Household levy` is a force to the plan and "NO organized force at all" to DS-DEF-5. [defense R-C, X-4, X-5, Y-1] | `src/generators/threatDefensePolicy.js:13`, `:47-50`, `:73-80`; `defenseInstitutionBuckets.js:88-100`; `institutionalCatalog.js:104-110` |

### 3.3 Safety, order and crime

| # | label | field | engine meaning | source |
|---|---|---|---|---|
| L-37 | `Very Safe` and every base safety label | `safetyProfile.safetyLabel` | `effectiveSafety = max(militaryEffective / max(8, criminalEffective), communityOrderBonus, courtOrderFloor)`: a RATIO floored two ways, not a measurement of the watch; a thorp with no criminal roster gets a flat 1.25 bonus and reads `Moderate` with zero enforcement; a town with a court gets a 1.20 floor; `Very Safe` reads "Fortress towns AND OCCUPATIONS sit here". [defense L-5, L-6, E-25; general §3.6] | `safetyProfile.js:44`, `:56-74`, `:253-305`; `src/domain/compendium/bandLadders.js:188-195` |
| L-38 | `Controlled` / `Controlled — Authoritarian` | `safetyProfile.safetyLabel`; the legitimacy safety term | NOT "well-policed": two writers only, the occupation curfew strain and the authoritarian state-crime label, "Residents face little risk from thieves and considerably more from informers and occupation officials" / "from the authorities themselves. Unofficial disappearances are not discussed openly." The corpus pools it with `Secure`; the legitimacy safety contribution subtracts 5 for it, so a "SAFETY, adverse" pool can be drawn for an over-policed town. [defense L-7; general L-3; power L-I] | `safetyProfile.js:103-108`, `:240-246`; `generalStateProse.js:241-242`; `src/generators/factionDynamics.js:33-40` |
| L-39 | `Quarantined` / `Restricted` | `safetyProfile.safetyLabel` | Both are PLAGUE strains under `plague_onset` (`safetyRatio >= 2 ? 'Quarantined' : >= 1 ? 'Restricted' : 'Dangerous — Plague Unrest'`), pooled with `Secure` / `Controlled` and with `Tense` / `Strained` / `Unsafe`; the quarantine sentence is written at EVERY tier with an informal-enforcement fallback below town; `Quarantined` also feeds the legitimacy safety term (-8). [general L-3b; stressors refute R8; power L-I] | `safetyProfile.js:133-143`, `:138`; `generalStateProse.js:243`, `:246`; `factionDynamics.js:37` |
| L-40 | `Secure` (safety) vs `Secure` (food) | two ladders | No branch of `safetyProfile.js` writes `Secure` at all, so the DS-GEN-11 pool speaks only over `Controlled` and `Quarantined` towns; food `Secure` is the ELSE-ARM of the deficit / surplus cascade (residual ≤ 5 percent, surplus ≤ 40) and is "is covered", never "covers itself" (the residual is AFTER imports and magic). [general L-3c, L-15; economy A.2, E-R7] | `generalStateProse.js:230-238`, `:260-267`; `foodGenerator.js:318-330`, `:355-358` |
| L-41 | `unknown` (safety severity) | `safetySeverityOf(...).key` | The LOUD fallback, deliberately not a quiet neutral; it draws no prose ("a defect to surface, not a state to narrate"). [defense L-20] | `src/domain/display/safetySeverity.js:39-45`; annex `:2745-2747` |
| L-42 | `orderStatus` (DS-DEF-3) | `scores.internal` banded | A FOURTH safety vocabulary on the same banner (`Strong / Adequate / Weak Public Order`, `Critical: Order Failing`): the INTERNAL SCORE (court +20, prison +15, garrison +15, watch +18, minus crime), not the ratio the label reads. [defense X-15] | annex `:2731-2736`; `defenseGenerator.js:231-255` |
| L-43 | `organized` / `semi-organized` / `diffuse` / `null` (criminal structure) | `deriveCriminalStructure` | `organized` fires on `Front businesses` (a laundering cover with no hierarchy) and `Multiple criminal factions` ("Competing gangs. Turf disputes", against the classifier's own note); `semi-organized` fires on `gambling`, so a `Gambling den` makes a town "coordinated enough to hold routes and territories"; `null` swallows `Assassins' guild`, `Rookery` (whose own desc says a standing organization backs it), `Street gang`, `Underground network`; `criminalOpNote` in the SAME FILE recognises gang / street / underground / assassin, and `hasCriminalInst` sees `assassins`: two crime classifiers in two files disagree about one row. [defense E-27, D-R2, D-R3, C-5, X-13; power A-13] | `defenseDisplay.js:142-149`, `:160-195`; `institutionalCatalog.js:1466`, `:1480`, `:1503`, `:1520`, `:2006`, `:2040`, `:2459`; `priorityHelpers.js:72` |
| L-44 | `none` / `adversarial` / `equilibrium` / `corrupted` / `capture` (the capture ladder) | `powerStructure.criminalCaptureState` | `none` = nothing criminal reached the seat, not no criminal interest; an ABSENT state is styled `none` on the tab and refused for a sentence; `equilibrium` renders "Criminal: Tolerated" (a tacit tolerance, not balance); `adversarial` renders "Criminal: Adversarial" (crime present and OPPOSED, better than equilibrium); at play the settlement value is the ROLLUP of the WORST faction, governing or not, while DS-POW-6 fills `{faction}` with `governingName`; AGENT / LEADER is a declared judgment on a faction-level rung and no person-level captured-role record exists. [defense E-28; power L-2, L-3, L-H, E-14] | `defenseStateProse.js:860-866`, `:908-945`; `src/components/new/tabs/PowerTab.jsx:163-169`; `src/domain/corruption.js:474-480`; `src/domain/worldPulse/factionCapture.js:136-152`; `src/domain/worldPulse/pulseKernel.js:645-648`; `src/domain/display/stateProse/powerStateProse.js:396-404`, `:962` |
| L-45 | `blackMarketCapture` and its three tiers | `safetyProfile.blackMarketCapture` | `(criminalEffective - 25) / 3` plus per-stress bonuses, tier-scaled, capped at 80: an ESTIMATE off the criminal slider, not a measurement of trade; below 3 is no surface, missing is null; the off-book INCOME LINE exists only above 10, so the minor tier (3-10) has a shadow reading and no criminal income line. [economy 3.15, §4] | `safetyProfile.js:619-638`; `economicState.js:319`; `economyStateProse.js:545-565` |
| L-46 | `Criminal Syndicate Revenue` · `Thieves' Guild Revenue` · `Smuggling Network Revenue` · `Shadow Economy (untaxed)` · `Black Market Revenue` | `incomeSources[].source` | Five spellings of one line, all `isCriminal: true`; `criminalIncomePoolKey` is keyed on the FLAG and never the label ("a label route would drop four of five and do it without an error anywhere"). [economy 5.9, §2.16] | `economicState.js:334-341`, `:349`; `economyStateProse.js:462-469` |
| L-47 | the seven criminal operation roles; `criminal revenue stream` | `criminalOpRole.js` | Substring on the operation NAME; only the FIRST classifiable operation is narrated; the fallback row is WIDE (`Slave market`, `Whisper market`, `Human trafficking network`, `Kidnapping ring`, `Contract killer`, `Street gang`, `Bandit affiliate`, `Outlaw shelter`, `Underground network`, `Underground city`, `Multiple criminal factions`, `Assassins Guild` all miss the six substrings): "no clearer description of it than that" is true of the classifier, not the town; `Slave market` and `Whisper market` do NOT classify as `parallel marketplace` (only `black market` does). [power E-15, A-10] | `src/domain/criminalOpRole.js:38-46`, `:56-64`; `powerStateProse.js:962-968`, `:985-987` |
| L-48 | `Thieves' Guild` (the FACTION row) | `powerStructure.factions[]` | Minted whenever `criminalPower > 5`, independent of a thieves'-guild INSTITUTION; `computeCriminalCaptureState`'s `isCrim` matches the substring `thiev` on the NAME. A faction called Thieves' Guild is not the `Thieves' guild chapter` row and licenses no building. [power L-C] | `src/generators/power/rulingStructure.js:658-674`; `factionDynamics.js:221`; `priorityHelpers.js:74` |
| L-49 | `Military/Guard` (the FACTION row) | `powerStructure.factions[]` | An aggregate of "the soldiers and watchmen, not the officers who govern", minted whenever `militaryPower > 5` (thorp: priority > 60) with NO institution test, so the row exists on towns with no garrison, watch or militia; not the `watch` holder kind and not `inst.hasWatch`; doubled under `under_siege`. [power L-B, E-16b] | `rulingStructure.js:570-600`, `:590-593`; `src/generators/power/stressFactions.js:13-16` |

### 3.4 Defense scores, gates and postures

| # | label | field | engine meaning | source |
|---|---|---|---|---|
| L-50 | `economicGates.military < 1` ("underfunded", "wages"); the gate absent | `defenseProfile.economicGates.military` | `milUpkeepMult = min(1, 0.6 + econOutput/50 * 0.4)`: a function of economic output, not of any payroll, applied to `hasWalls +30`, `hasMilitia +10`, `hasWatch +7`, `hasCharterHall +10` exactly as to `hasGarrison +28`, and recorded whenever `hasAnyDefense` (a lone palisade satisfies it); `econOutput < 50` on any roster with any defence row bites a palisade and a part-time militia exactly as a garrison. ABSENT means no paid stack to gate, not fully funded ("a hamlet with no paid defenses reports no military gate at all"); the desk's `< 1` threshold fires on any shortfall (`Math.round(x*100)/100` lets 0.99 fire it). Under ADDENDUM 12 the gate is ONE purse over garrison wages and wall maintenance; a second ORDER purse gates watch wages, court and gaol (`internalUpkeepMult`, floor 0.65); both are functions of the same `econOutput`. [defense E-31, R-D, X-3, L-23; ADDENDUM 12; warFaith §6 "the pay gate itself"] | `defenseGenerator.js:138-156`, `:160-165`, `:177-192`, `:244-252`, `:462-470`; `defenseStateProse.js:1056-1058` |
| L-51 | the chain modifiers (`chainModifiers.{military,economic}`) | `defenseProfile` | The FORTIFICATION chain (healthy +6 "walls maintained", impaired -4), the GARRISON chain (healthy +5, vulnerable / impaired -5 "no provisions") and the MERCENARY chain (+4): the engine's only maintenance-and-supply model for walls and soldiers, and the per-thing variation the owner reached for. NOT WIRED to prose: no census row reads them (the DS-DEF-11 cards read `economicGates.military` alone). The fortification chain also feeds EVERY wall row quarried stone, a palisade included. [ADDENDUM 12; defense §6.14, E-1, E-2, D-R5] | `defenseGenerator.js:585-605`, `:594-600`; `src/data/supplyChainData.js:873-884`, `:893-897` |
| L-52 | `STRONG` / `ADEQUATE` / `WEAK` / `CRITICAL` (the arm badge) | `scoreBand` | Exactly the ladder's reading; frozen at four rungs; a badge on one arm says nothing about another ("a STRONG header over a CRITICAL food arm is a real and common state"); an absent score is null, never banded 0. [defense L-19 sibling, E-22; general §3.6] | `src/domain/display/defenseScoreBands.js:37-39`; `bandLadders.js:202-207`; annex `:2510-2513`, `:3246-3248`; `generalStateProse.js:291-296` |
| L-53 | `scores.monster` / `scores.economic` / `scores.magical` | the five axes | The page labels them "Monster Defense", "Economic Resilience", "Magical Capability": monster is a CAPABILITY score and a `plagued` region SUBTRACTS 15; economic is driven primarily by FOOD STORAGE MONTHS and is not prosperity; magical is 0 when magic is off world-wide and 0 at thorp / hamlet / village with no magic row (a cathedral or monastery lifts the gate), so `scores.magical: CRITICAL` reads a WORLD, not a town; in a no-magic world the readiness mean is over FOUR arms. [general L-2, L-2b, L-2c, C-14, C-15; defense E-23] | `src/components/new/tabs/OverviewTab.jsx:372-376`; `defenseGenerator.js:196-227`, `:257-290`, `:296-307`, `:502-505` |
| L-54 | `Fortress` ... `Undefended` (the readiness label) | `defenseGenerator.js:515-521` | The mean of the arms PLUS a tier bonus (thorp +12, "cheap to defend") MINUS a threat penalty (plagued -15): scale and country move the word without any works moving; `posturePoolKey` reads `readiness.score`, not the badge's number. [defense E-23, C-8; general §3.6] | `defenseGenerator.js:487-521`; `defenseStateProse.js:759-771` |
| L-55 | `ADEQUATE` / `collapsed` (the live band, DS-DEF-7) | `causalState` band | 50 is "the neutral / no-information score and lands in 'adequate'"; `collapsed` is `score < 15` on a score that STARTS at 50, not "nobody is left". [defense L-19, E-24] | `src/domain/causalState.js:423-427`, `:436`, `:444-449`, `:1034-1053` |
| L-56 | `{reason}` (DS-DEF-7/10, dark) | `causalState` contributor `.reason` | Not a cause phrase but a full SENTENCE ending in a period (`Defense readiness score: 62.`, `Defensive walls in place.`, `<label> taxes defense readiness.`); slotting it into "has lost to {reason}" prints nonsense. [defense L-13] | `causalState.js:486-488`, `:1044-1053`, `:359-364` |
| L-57 | `QUARANTINE ACTIVE` · `MIGRATION SURGE` · `UNDER TRIBUTE` · `BEAST PRESSURE` · `SECURITY COMPROMISED` (the fifteen postures) | `DEFENSE_STRESS_STATUS[type].posture` | Display posture words derived per stress type for the Defense tab and the PDF, "chrome, not prose": the stress's own label is `Disease Outbreak` ("a quarantine that is not being enforced consistently"); `Mass Migration` covers BOTH directions (immigration and emigration); `Indebted to Outside Power` is a CREDITOR model, not a conqueror's tribute; `Beast & Raider Threat` is "not only creatures, and not necessarily unthinking" ("Someone is directing this"); `Recently Betrayed` records a betrayal, not a leak of arrangements. [defense L-14 to L-18, E-26, E-32; stressors §5] | `defenseDisplay.js:25-42`, `:61-68`; `src/data/stressTypes.js:53-62`, `:64-72`, `:84-92`, `:104-113`, `:159-168`; annex `:3159-3162` |
| L-58 | `magicDependency: true` | `defenseProfile.magicDependency` | A qualifying STRESS is active (`magicOn && ((under_siege && ...) \|\| (famine && ...) \|\| (plague_onset && ...) \|\| ...)`): the binding term is the stress, not the magic; TRUE on 0 of 200 default worlds; the corpus's "{settlement} runs on arcane work" states a standing dependency off a crisis flag. [defense E-29, C-13] | `defenseGenerator.js:449-454`; `defenseStateProse.js:1668-1686`; annex `:3211` |
| L-59 | `Under blockade` / `blockaded` / "fleet" | `stockpile.blockaded` | Written only by the world pulse while the naval layer is lit; `economicState.foodSecurity.stockpile` is null on every settlement the pipeline builds; "a hostile fleet" is the DISPLAY note's word, and the writer of `blockade` was not traced; the bypass, if any, is `teleport` or `airship`; on the war desk a blockade IMPAIRS AIRSHIPS ONLY and never a road or a harbour. [defense L-24, E-30; warFaith WF-12] | `defenseDisplay.js:250-256`, `:262-266`; `defenseStateProse.js:1638-1648`; `foodStockpile.js:307`, `:313-316`, `:417-418`; `src/domain/worldPulse/blockadeTransport.js:19-28` |
| L-60 | `no reserves, port open` vs `granary, port` | mixed | The producer's own asymmetry, mirrored on purpose: the reserve branch reads the INSTITUTION flag `hasPort`; the empty branch reads the CONFIG value `tradeRouteAccess === 'port'`. Two different questions behind one row. [defense L-25] | `defenseDisplay.js:245`; `defenseStateProse.js:1555-1588` |
| L-61 | `Town watch` "gate duty" | the row's desc and service | Recorded on EVERY town, including the half with no wall and no gate row (`Town watch` required; `Town walls` and `Gates (if walled)` bc 0.5). [defense X-11, E-9] | `institutionalCatalog.js:1348`; `institutionServices.js:1324` |

### 3.5 History and the record

| # | label | field | engine meaning | source |
|---|---|---|---|---|
| L-62 | `anchored: true` | `historicalEvents[]` | NOT "still bears on the present": stamped only where the anchor pass REPLACED the event with a settlement-appropriate template on a `_rng() < 0.6` coin; resource events `anchored: false`; the ancient-ruin event `anchored: true`; DS-GEN-16's "what remains" rides on `lastingEffects.length > 0`. [general L-4, G-R10] | `src/generators/historyGenerator.js:600-604`, `:621-640`, `:672`, `:691`, `:710`, `:741`, `:877`; `generalStateProse.js:572-579`, `:646-656` |
| L-63 | `stable and prosperous` | `history.historicalCharacter` | The ELSE-ARM of a timeline COUNT, not a prosperity reading: a `Struggling` town with a quiet timeline reads it. [general L-5, G-R11] | `historyGenerator.js:848-853` |
| L-64 | `The Purge` / `The Migration` / `The Mandate` / `The Siege` (the `{calamity}` word) | `EVENT_TYPE_NAMES[type]` | `The Purge` is `infiltration_fear`, "a paranoia/denunciation arc ... NOT an occupation"; `The Migration` is `population_friction`, friction not movement; `The Mandate` is `legitimacy_crisis`; `The Siege` is `external_threat`, "News of approaching danger divided the community on how to respond" (factions Prepare for war / Seek diplomacy / Flee), a DIVIDED RESPONSE, not a siege that happened. And the fill class is EVENT, not BLOW: `calamityFill` accepts any `The` + Title Case name, so 'The Great Harvest Compact', 'The Pilgrimage', 'The Return', 'The Influx' pass. [general L-6, R-L1, R-L2, G-R2, G-R8] | `src/data/historyData.js:1182-1184`, `:1547-1582`; `generalStateProse.js:509-517`, `:660-667`; `historyGenerator.js:661-676` |
| L-65 | `exile_return` / `occupation_infiltration` / `demographic` / `disaster` (the coarse event type) | `historicalEvents[].type` | Three categories are one-to-one with a differently-named fine event (`exile_return` = "The Occupation", `occupation_infiltration` = "The Purge", `demographic` = "The Migration"); `disaster` includes `external_threat` ("The Siege"), not a natural disaster. [general L-7] | `historyGenerator.js:588`; `src/generators/history/historyPolicyData.js:48-65` |
| L-66 | `viable` | `economicViability.viable` | `criticalIssues.length === 0` at the first survey, never re-run ("later events and edits do not re-run this check"); not self-sufficiency; MARGINAL fires on an ABSENT verdict; `criticalIssueCount` lives at `metrics.criticalIssueCount`. [general L-8, C-7] | `src/generators/economy/viability.js:557-558`, `:565`; `src/components/new/tabs/ViabilityTab.jsx:142-143`; `generalStateProse.js:681-698` |
| L-67 | `severity` | four records on one desk; the world stressor | `currentTensions[].severity` and `historicalEvents[].severity` = `minor \| major \| catastrophic`; `coherenceNotes[].severity` = `notable \| contradiction \| context`; `structuralViolations[].severity` = `error \| warning \| by_design`; on a world stressor `severity` is the ORIGIN's and a spread target's felt severity is a separate never-re-aged stamp (`severityBySettlement`); DS-GEN-1's tension `severity` is an ARRAY on 100 percent of tensions, so the block is dark. [general L-16, §1; stressors refute F4] | `src/components/new/tabConstants.js:20`; `narrativeGenerator.js:586`, `:599`, `:614`, `:631`, `:645`, `:663`; `structuralValidator.js:421`, `:435-440`; `generalStateProse.js:388-420`; `src/domain/worldPulse/stressorsCore.js:285-291`, `:336-339` |
| L-68 | `intensity` (conflicts) | `conflicts[].intensity` | A COUNT of recorded rivalry relationships (`rivalries.length > 1 ? 'high' : === 1 ? 'moderate' : 'low'`), not heat. [general R-L5, G-R13] | `src/generators/power/conflicts.js:238` |
| L-69 | tension `type` "closed at 10" | `TENSION_LABELS` | A DISPLAY list; the producer can write more than twenty types and fourteen would reach no pool; closed in the corpus, open in the producer. [general G-R12] | `src/domain/dossier/plotHooks.js:7-18`; `generalStateProse.js:415-418` |
| L-70 | `LEVEL` (DS-POP-3) | `populationTrendBand().band === 0` | Band 0 is ALSO the no-data answer for a ring with fewer than two readings; only DS-POP-3's `window < 2 ⇒ null` gate makes LEVEL a reading; the believed trend on a belief record is a different thing sharing the name. [general L-14, C-5] | `src/domain/display/trendLens.js:47-49`; `generalStateProse.js:1547-1586`, `:1568-1570` |
| L-71 | `QUANTITY_BANDS` word | `demographicsHerald.js:72-80` | The CEILING holds; the word's English does not: 41 souls reads "a hundred or so", 1,201 reads "thousands"; the bands are owner-retunable prose; `nobody` is a nonpositive count. [general G-18] | `src/domain/worldPulse/demographicsHerald.js:70-80`, `:124-134` |
| L-72 | `label` on an escalation clock; `bread_riot` / `smuggling_rise` trigger text | `deriveEscalationClocks` | `id: 'clock.bread_riot.<trigger>'` vs `label: 'Bread Riot Clock'`: the id is the join key, the label a display string; "strained or worse" is the description but the code fires on any non-`stable` status including `substituted` (magically sustained) and `captured`. [general L-13, R-L11, G-R14, G-R15] | `generalStateProse.js:729-745`; `src/domain/hookEscalation.js:388`, `:463-481`; `src/domain/supplyChainState.js:178`, `:186` |
| L-73 | `market (route crossroads)` (DS-GEN-5) | `SCENE_OF_ROUTE` | An ARRIVAL-SCENE label keyed on ROUTE alone whose texts assert "all of them end in the same square", while DS-GEN-13 can resolve `NO-MARKET` on the same town; the square is the map engine's fact. [general R-L4, R-A11] | `generalStateProse.js:820-826`, `:834`, `:1139-1147`; annex `:120` |
| L-74 | the market NAME class (DS-GEN-13) | `/\b(market\|bazaar\|exchange\|shambles\|stalls)\b/i` | Credits `Whisper market` (knowledge brokers), "Chroniclers' exchange" (paid correspondents), `Slave market` (persons), `Black market` (the CRIMINAL roster); no catalog key contains `shambles` or `stalls`. [general R-L3, G-R4] | `generalStateProse.js:1109`; `institutionalCatalog.js:1036`, `:1759`, `:2086`; `priorityHelpers.js:76-77` |
| L-75 | `settlementAgeMode: 'new'` / FOUNDED-YOUNG | `historyGenerator.js:44-49` | `age = 0` is REACHABLE by config (`'new'` or `'custom'` with 0), so `newly founded` and FOUNDED-YOUNG are live arms; the survey's "youngest town 81y" measured `auto` only; `'custom'` gives an exact age figure and no date, which the prose still bands. [general R-L14] | `historyGenerator.js:44-49`; `generalStateProse.js:620-621`, `:631-634`; `SummaryTab.jsx:33` |
| L-76 | `PROSPERITY_TONE` / `SAFETY_TONE` (PDF) | print-only tone keys | Keyed on words the producers do not emit (`SAFETY_TONE` has `safe, orderly, ...`; the producer writes `Very Safe`, `Moderate`, `Controlled — Authoritarian`), so they fall to `'muted'`; `PROSPERITY_TONE` has no row for `Comfortable` or `Prosperous`. [general L-17] | `src/pdf/lib/viewModel.js:85-101`, `:359-361` |
| L-77 | `Poverty` / `Impoverished` / `Affluent` | prosperity spellings | `Poverty` is not a `PROSPERITY_RANK` key and `Impoverished` is never emitted (the pool lights only on a stored record); `Affluent` is not a rung (the old Compendium prose drifted); an unrecognised spelling renders nothing. [general §3.6; economy §4, 3.9] | `src/domain/prosperityRank.js:62-92`; `generalStateProse.js:187-217`; annex `:1162-1164`; `economyStateProse.js:364-372` |
| L-78 | `provenance: 'forced'` / `charterPending` / `lifecycleStatus` | steadings | `forced` is minted at `settlementLifecycleKernel.js:1207`, `:1216` (the refuter read the mint site, not its caller); `charterPending` also entails village scale reached; `lifecycleStatus` is 0 of 48 on a fresh settlement, a lawful dark mount. [general G-22] | `src/domain/worldPulse/settlementLifecycleKernel.js:870-882`, `:1207`, `:1216`; `src/domain/spatial/satellitesLedger.js:79`; `generalStateProse.js:1388-1394` |

### 3.6 The economy desk's own labels

| # | label | field | engine meaning | source |
|---|---|---|---|---|
| L-79 | `shortage` / `adequate` / `surplus` (flow band); `strained` / `met` / `abundant`; `trade-dependent` | `flowDrift` | LOW / high measured road THROUGHPUT this tick (`inflow + outflow` against two thresholds), not a shortage or a pile of goods; the dependency word is a 1:1 re-labelling of the band, not a second measurement; `isTradeDependent` is "any import, export, `tradeDependency` or `tradeLink` recorded"; `SHORTAGE × not trade-dependent` can never key; an unrecognised band is null, not adequate; the tally is "as of the last tick it was advanced". [economy §4, 3.12] | `tradeFlow.js:186-192`, `:244-253`; `src/domain/display/tradeFlowEconomics.js:58-62`, `:65-81`, `:100-113`; `economyStateProse.js:583-611` |
| L-80 | `vulnerable` (export posture); `entrepot` / `isEntrepot`; `limited`; `import_dependent` | `exportPosture.status`; `economicState.isEntrepot` | `vulnerable` = `access === 'isolated'`, not raiders or a cut route; `isEntrepot` = `route === 'crossroads'` (or a port with an institution named `international trade` / `warehouse district`), so on most entrepôt towns the fact means "sits on a crossroads"; `transit` is a slice of at most four goods; `limited` counts STRINGS after `toArray`, which may be a service label or a `(transit)` good; `import_dependent` has NO producer (five arms, no sixth). [economy §4, 3.11, 3.14] | `src/domain/display/exportPosture.js:20-27`, `:33-36`, `:56-62`; `tradeGoods.js:90-97`, `:162`; `economyStateProse.js:622-630` |
| L-81 | `dear` / `steady` / `cheap`; the crier's `receipt` | `marketQuote.tag`; `highlight.crierLine` | A STOCK count on the `commodityStocks` ledger against a fixed target of 8 units (`< 0.35×8` shortage, `> 1.25×8` surplus), no demand term and no coin anywhere; services never move; `steady` is "unmeasured OR in band" (an absent ledger entry reads `adequate`); the crier's receipt INVENTS a seasonal cause ("for it comes scarce this season") whenever the road drift does not explain the band, and a steady good's phrase can read "a shade above its usual price" from the ROAD band. [economy A.5, §4, 3.20, E-R16, E-R17] | `src/domain/display/marketPrices.js:65-76`, `:129-133`, `:137`, `:143-151`, `:161-168`, `:182-184`, `:213-218`, `:228-241`, `:272-277`, `:318` |
| L-82 | `impaired` / `degraded` / `vulnerable` (service); `magicalInfra` | `computeChainSets` | `impaired` = `severity === 'critical'` OR the town is under siege OR the route is `isolated`, so on an isolated town every trade-dependent service reads impaired regardless of its own condition; `degraded` / `vulnerable` = `severity === 'high'` / everything else; `magicalInfra` is detected by a REGEX over prose (`/magical trade infrastructure/i`) and removes the service from all three sets. [economy §4] | `src/components/new/tabHelpers.js:30`, `:38-46`, `:55` |
| L-83 | `stable` / `blocked` / `entrepot` / `unexploited` / `collapsing` / `captured` (chain status) | `supplyChainState` | An UNKNOWN status silently canonicalises to `stable`, and `entrepot` remaps to `stable` with no local input at all; `blocked` is written only by the isolated-subsistence shutdown of entrepot chains ("shut off for want of a route", not an upstream failure) and the chain STATUS `unexploited` (that same shutdown, remapped to `blocked`) is a different record from the exploitation BUCKET `unexploited` ("no processor", never worked); `collapsing` HAS a producer and one arm fires on a single pressure (`scarce && maxSeverity >= 0.8`); only `captured` is unproduced. [economy A.7, §4, §C, 3.13, E-R10, E-R11] | `supplyChainState.js:22`, `:174-181`, `:185-187`, `:199-202`, `:443-447`; `src/generators/economy/customTradeEndpointIntegration.js:113-118`; `resourceGenerator.js:175` |
| L-84 | `Deficit — Active Famine` | `foodSecurity.label` | The famine STRESS alone (`stresses.includes('famine')`), whatever the arithmetic: production x0.35, imports still cover, it can sit on a surplus town, "the fields failed" is unlicensed; AND the producer's label carries an em dash and the word Deficit while the corpus pool is `ACTIVE FAMINE`, so `foodSecurityPoolKey('Deficit — Active Famine') → null`: the famine pool is unreachable from the shipped label (probed; not on the file's declared no-producer list). [economy A.10, §4, E-R9; general R-L6, §3.6] | `foodGenerator.js:236`, `:243`, `:318`, `:341-342`; `economyStateProse.js:394-401` |
| L-85 | `Import-Dependent` | `foodSecurity.label` | 15 to 40 percent of daily need met by NOTHING, imports and magic already subtracted (`deficit = max(0, rawDeficit - importCoverage - magicOffset)`); printable with zero imports on an isolated thorp; licensed: "a real share of its need is met by nothing on the record", never "bought". [economy A.2, §4, §C, E-R8] | `foodGenerator.js:293-296`, `:308-309`, `:318-330`, `:347` |
| L-86 | `well stocked` / `thin` (granary band); `season` | `granaryOutlook` | A FRACTION of this town's own derived capacity, never an absolute, and the capacity is 1.5 to 2.0 months on a town with no granary at all; `season` is a position in a 13-week quarter, present only under seasons-on, with weather a separate three-value `seasonalEvent` (`hard_winter`, `drought`, `bountiful`). [economy §4, 3.3, 3.8] | `src/domain/display/dossierViewModel.js:296-304`, `:333-336`, `:341-347`; `foodStockpile.js:69`, `:190-193`, `:330-332`, `:428-429` |
| L-87 | `economyOutput` / PDF `OUTPUT` | `compound.economyOutput` | The economy priority SLIDER multiplied by crime, magic, a market-institution flag, stress and neighbour multipliers; not production, not coin; never printed as a figure. [economy §4] | `priorityHelpers.js:403-410`; `src/pdf/sections/EconomicsTrade.jsx:68` |
| L-88 | `Subsistence` / `Diversified` (complexity band); `specialist trade`; `mix of field and market`; `surplus farm trade`; `subsistence living` | `COMPLEXITY_BAND_BY_LABEL`; `COMPLEXITY_NOUN` | The band is a CLASS (`Subsistence` names three producer labels, `Diversified` two); SPECIALIZED is "four or five income lines, nothing selects a speciality"; MIXED is `village && hasMarketInst` with farming never tested (every small tier carries `Agricultural Rents` by default); AGRICULTURAL / MINOR_SURPLUS / SURPLUS read tier, market flag and export count and never `foodSecurity` (a `Deficit` village with four exports prints "surplus farm trade"); SUBSISTENCE = thorp / hamlet && `exportCount < 3` (a `Surplus` hamlet prints "subsistence living"). [economy §4, E-R1 to E-R4] | `src/domain/display/labelBands.js:112-115`, `:130-142`, `:149-161`; `src/generators/economy/prosperity.js:296-315`; `economyStateProse.js:212-224` |
| L-89 | `unexploited` (bucket); `exportValue`; `resourceAnalysis.exports` vs `primaryExports` | exploitation row | The bucket means never worked, not stopped (no depletion field on that surface); `exportValue` is a FOUR-word class the corpus split two ways, and on a partial row the chain's static value rides the row while the partial export reports `'medium'`; exploitation exports and the trade column come from two pipelines, so "leaves as {good}" reads one list and the export column is the other. [economy §4, §C, 3.6, E-R6] | annex `:1491-1495`; `resourceGenerator.js:295-317`, `:307-314`; `economicState.js:741-751`, `:791-795`; `economyStateProse.js:733-735` |
| L-90 | `{good}` (the shared bag) | `leadingGoodNoun(primaryExports)` | DARK in practice: 0 of 48 fills, all 99 export labels capitalised; and the class includes SERVICES and PEOPLE (`'Maritime services (cargo, pilotage)'`, `'Mercenary services — ...'`, the slave-trade sentences), so a `{good}` is not "a made thing that leaves". [economy 3.4, E-R5] | `economyStateProse.js:26-27`, `:848-853`; `economicState.js:624-636`, `:654-663`, `:793-795`; `computeActiveChains.js:1021-1025` |
| L-91 | `ECONOMY_FRESHNESS_SENTENCES` (DS-ECO-7) | a frozen copy unit | A vetoable copy unit with a voice contract of its own: the detector proves an event landed, never that a figure is wrong. [economy §4] | annex `:1135-1141` |

### 3.7 The power desk's own labels

| # | label | field | engine meaning | source |
|---|---|---|---|---|
| L-92 | `governanceFractured: true` | `publicLegitimacy.governanceFractured` | Exactly `score < 30`, the same predicate as the `Legitimacy Crisis` band, at all three writers; not an independent fracture (the annex's fence at `:1927-1930` is contradicted by the code). [power L-1, C-1] | `factionDynamics.js:133`; `src/domain/timeProgression.js:179`; `src/domain/rulingPower.js:352`; `powerStateProse.js:249-251` |
| L-93 | `Stable` (first word) / the FLOOR pool / `Fractured — no stable governing authority` / `critical matched` / `Volatile` / `Desperate` / `Anxious` / `Tense (external threat)` / `Vulnerable` | `powerStructure.stability` | The classifier matches the FIRST WORD only: bare `Stable`, `Stable (theocratic governance)` and the play-time `Stable: a fresh mandate, still finding its footing` draw one pool; SEVENTEEN distinct labels (including `Fractured — no stable governing authority`, `Suppressed (under occupation)`, `Unsettled:`, `Subjugated:`, `Transitional:` ×2) fall to the FLOOR whose prose asserts "{faction} holds the hall"; `critical matched` is unreachable (the only `critical` label also carries `siege`, tested first); `Volatile` is `succession_void` ("power is available to whoever moves first", the seat VACANT) routed to `unstable matched` whose prose says "{faction} governs"; `Desperate` is hunger specifically; `Anxious` is disease and the ONE power label that licenses illness; `Tense (external threat)` is an adversarial NEIGHBOUR RELATIONSHIP TYPE; `Vulnerable` is two vocabularies (a stability label and the DEFENSE readiness label). [power L-4 to L-11, L-D, L-E, L-F, L-G, L-P, E-18] | `powerStateProse.js:275-284`, `:294-330`; `governanceNarrative.js:17`, `:249-338`; `rulingPower.js:414-420`, `:662`; `stressFactions.js:25-46`, `:109-111`; `factionDynamics.js:51-53`; `rulingStructure.js:740-741` |
| L-94 | `recentConflict` (birth) | `power.recentConflict` | An ONGOING present-tense dispute in every birth producer branch ("Neither side will back down", "is hearing the case"); resolved only by the play-time transfer's sentence, where under a coup the old house is REMOVED; "the losing side is still on the rolls" is true only under a lawful passage with `demoted` true. [power L-J, P-R6] | `governanceNarrative.js:372-487`; `rulingPower.js:552-565`, `:672-675` |
| L-95 | `govMultiplier` 1.15 and 0.80; "the hold" | `publicLegitimacy.govMultiplier` | Five bands into three pools: 1.15 ("public approval lends them weight" on the shipped page) draws "backing hardens their hold"; 0.80 ("public doubt loosens") draws "public rejection is breaking the hold". [power L-12, C-11, E-13] | `factionDynamics.js:113-118`; `src/components/dossier/EngineSections.jsx:208-214`; `powerStateProse.js:517-524` |
| L-96 | `Stable` (coup risk) / `weight` / `gated` | `riskLabel`; `challengers[].weight`; `incumbent.gated` | `Stable` means no non-governing, NON-CRIMINAL faction with power ≥ 5 (criminal factions are excluded by construction, the capture ladder is their path), so "Nobody is positioned to take the {seat}" is false of a town whose Thieves' Guild sits at `corrupted` / `capture`; `weight` is `power × COUP_COERCION[archetype]`, a coercion coefficient (a military house of 20 outweighs a merchant house of 25); `gated` is always true with fewer than three challengers; with a null governing faction every roster row is a challenger. [power L-13, L-14, L-15, E-10, C-4] | `src/domain/rulingPowerCoup.js:36-49`, `:51`, `:69-76`, `:81-125`, `:274-282`; `rulingPower.js:224-230` |
| L-97 | `mixed` (ruling power); `mixed` (economic base); `legitimacyNerve`; `hysteresisWiden` | `structuralLens` | `mixed` is the FAIL-SOFT default of `rulingPowerFromArchetype` for every unmapped category (`magic`, `economy`, `other`): 21 of 52 roster labels read it, `merchant_league` is unreachable on generated data, `Royal Authority` / `Noble Governorship` / `Ducal Governorship` / `Feudal Stewardship` / `Headman's Authority` read `council` (`government` is tested before `noble`), `Military Council` reads `autocrat`; and `structuralLensOf` reads the MAX-POWER faction, never `isGoverning` (a besieged council town reads `autocrat` off its doubled guard); the economic-base `mixed` is the ONLY value the shipped lens can produce (no base is passed), so the five pools are dark by declaration; the nerve is a constant of the lens table, a disposition of the KIND of seat; `hysteresisWiden` is a deadband multiplier whose consequence only may be spoken. [power L-16 to L-19, E-16, E-17, §2, C-6, C-7, C-12] | `src/domain/spatial/cohesionWeave.js:131-199`, `:275-287`; `src/generators/power/factionCategories.js:109-176`; `rulingStructure.js:699-701`; `powerStateProse.js:668-682` |
| L-98 | `consolidation = 0` ("a fully divided court"); `deferred` (receipt kind) | `coalitionConsolidation01`; politics receipt | 0 is returned for a DORMANT layer, a live layer with no ruling bloc and `totalPower <= 0`: not a measured full division; a ruling bloc is a 40 percent PLURALITY (`RULING_CONSOLIDATION_FLOOR: 0.4`), so "a working majority" (the corpus AND the engine's own concession detail string) overstates; `deferred` names three situations (the draw missed, the leaders' rivalry blocked it, the three-bloc cap). [power L-20, L-24, E-6, P-R1, C-8] | `src/domain/worldPulse/settlementPolitics.js:149`, `:454`, `:581`, `:588-590`, `:604-607`, `:979`, `:982`, `:986` |
| L-99 | `end: commerce` / `end: survival` / glue `threat` / glue `doctrine` / glue `compromise` / glue `patronage` | `deriveEnd`; `classifyGlue` | `commerce` is ALSO the fail-soft for a lead member with no `ARCHETYPE_END` row (`other`: `Town Mayor`, `Elder Consensus`, `Elected Reeve`), "organized around money" with no merchant in it; `survival` also comes from the goal `break_vassalage` with no threat; `threat`'s `settlementUnderThreat` is true for a besieging front OR the town's OWN `war_preparation` / `mobilized` / `deployed` posture OR six tolerant snapshot fields, so an army marching OUT forms a `threat` bloc whose detail says "a common danger at the walls" (and no wall row is read); `doctrine` fires on two ARCANE rows ("a shared creed" of two wizard colleges); `compromise` fires on a bare corrupt flag with no leash object; `patronage` dies at a succession PAST THE DWELL only where a prior leader id was recorded. [power E-7, E-8, P-R2 to P-R4, L-K, L-L, L-M, C-9] | `settlementPolitics.js:414-457`, `:461-496`, `:510-528`, `:800-806`; `src/domain/worldPulse/mobilization.js:117-121` |
| L-100 | `present: false` (the governance ledger) | `governanceLedger(settlement).present` | No legitimacy record at all (a legacy save or an ungenerated settlement), and the ledger still returns `legitimacyScore: 50`: the 50 is a neutral default, not a reading; "no reading", never a middling one. [power L-23] | `src/domain/governanceLedger.js:32-36`, `:55-66`; annex `:2292-2294` |
| L-101 | `Merchant Guilds (dominant)`; `powerLabel: Dominant` | faction rows; `powerLabel` | The parenthetical is the PROSPERITY flag and the row's power is CAPPED at 0.88 × the government's, so a "(dominant)" guild never outweighs the seat at birth; `Dominant` is ≥ 35 of the renormalised 100 (a plurality band) while the desk's `DOMINANT share` is `governing > sum(others)`. [power L-A, L-O] | `rulingStructure.js:497-507`; `factionDynamics.js:582-586`; `powerStateProse.js:353` |
| L-102 | `desc` on the governing row; `{seat}` after a transfer | `powerStructure.desc`; `governingName` | On the fallback path a label absent from `govDescByFaction` carries the `Mixed Council` gloss ("Power is distributed across multiple factions without a clear dominant authority"), the persisted `desc` for `Headman's Authority`, `Priestly Guidance`, `Town Mayor`, `Shadow Senate` and others; after a play-time transfer `governingName` is a `GOVERNMENT_PREFERENCES` / alt label (`Noble Regency`, `Garrison Command`, `Martial Administration`, `<label> Ascendant`) outside the governance-label class, and `properFill` accepts them. [power E-1e, L-N, L-Q] | `rulingStructure.js:255-338`, `:424-440`; `rulingPower.js:180-187`, `:214-217`, `:433-449`, `:657-658`; `powerStateProse.js:178-185` |
| L-103 | `modifiers: ['vacant']` / `['occupied']` / `['contested']` / `['contested legitimacy']` on the governing row | `powerStructure.factions[].modifiers` | The engine's own one-word rulings on the seat: `vacant` halves the governing power while `isGoverning` stays true and `governingName` still fills `{seat}` / `{faction}` ("the seat itself stands vacant" is what PowerTab prints); `occupied` keeps the pre-occupation body governing at 0.6 with an `Occupation Authority` row pushed NON-governing. [power L-D, L-E; stressors refute §4.2] | `stressFactions.js:25-46`, `:55`, `:109-111`, `:178`, `:308`, `:357`; `PowerTab.jsx:117` |
| L-104 | `previousGovernments` (`by`, `government`) | `powerStructure.previousGovernments[]` | No generation-time writer, absent on 48 of 48 generated towns; the row shape is `{label, cause, tick}`, `by` is never written and `government` is an annex spelling; demotion of the defeated house is entailed only by cause AND Register VII (`demotesIncumbent` is false on every v1 world). [power E-20] | `rulingPower.js:376`, `:400-404`, `:629-632`; `powerStateProse.js:527-546`, `:920-923`; `EngineSections.jsx:277` |
| L-105 | the ladder: "sorted top-first"; `instability` absent; `SHALLOW_RUNGS_BELOW` | `ladderRungsOf`; `npcLadderState` | The rung array is a SEAT order, first-lit from structural importance and changed only by a contest swap; `standing` is a separate stock projected onto each rung, so `standing[0] - standing[1]` can be NEGATIVE while the array still names the top rung (the desk folds a negative gap into "{npc} leads by very little"); depth is a TIER cap (thorp 1, hamlet 2, village / town 3, city 4, metropolis 5), so "the ladder is short because the faction is" is false on those towns; at first light the gaps are a pure function of rung count (three of five pools selected by tier alone); `ladderInstabilityOf` returns 0 when the field is absent, so "long-held" also spells "no churn recorded". [power E-9, P-R5, C-2, C-3] | `src/domain/townMap/ladderRead.js:95-107`, `:138-141`, `:211-217`; `src/domain/worldPulse/npcLadderState.js:44-56`, `:107-110`, `:217-224`, `:912-921`; `src/domain/worldPulse/npcLadderKernel.js:677-683`, `:828`, `:1181`; `powerStateProse.js:578-582`, `:610-611` |
| L-106 | "the dominant contributor" (prosperity / safety / defense / food) | `BREAKDOWN_WORD` | The four are not commensurable (prosperity ±20, safety ±20, defense ±10 × tier scale, food ±10 × tier scale): on a thorp defense ≤ 3 and food ≤ 4, so dominance is a coefficient artifact; only prosperity has a favourable pool. [power E-12, C-5] | `factionDynamics.js:20-27`, `:30-44`, `:51-59`, `:76-82`, `:155-159`; `powerStateProse.js:220-225`, `:255-266` |
| L-107 | `covert` (census column); `objectClass` (census column) | census row fields | `covert` keys on the READ PATH (`COVERT_SOURCES` includes `blocs.covert`, not `politics.blocs`), so every DS-POW row reads `covert: false` though DS-POW-6's four capture pools are 100 percent dm-only; `objectClass` is derived from the POOL-KEY STRING alone ("this column reads the key and nothing else reads the key"), never evidence about the town, and it mis-files `patron` as `temple`, `port` as `road`, `guild_conflict` as `craft` and DS-POW-7's political "court" as `law`. [power L-21, L-22, E-5; general A-13; stressors §3a] | `wiringCensus.js:1236-1240`, `:1252-1268`, `:1301-1325`, `:1358-1381` |

### 3.8 Stress and condition labels

| # | label | field | engine meaning | source |
|---|---|---|---|---|
| L-108 | `plague_onset` / `plague` (archetype) / `'Plague'` (label) | `stress[].type`; `condition.archetype` | The ONSET of a disease outbreak ("It is not yet a plague, but it will be if nothing changes"; label "Disease Outbreak") on the stress record, and the archetype `plague`, label 'Plague', "A virulent illness spreads", on the promoted condition of the SAME settlement: two records, one word, contradicting each other. [stressors §5, R8] | `stressNarrative.js:98-99`; `stressTypes.js:85`; `src/domain/conditionPromotion.js:29`; `src/domain/activeConditions.js:59-66` |
| L-109 | `monster_pressure` / "Beast & Raider Threat" | `stress[].type` | "Whether wolves, raiders, or worse"; the world twin is `monster_raider_pressure`; a `war_pressure` condition may carry no war (a siege, a blockade, a war OR `/monster\|raider/` promote to it) and prints the label 'Wartime pressure'. [stressors §5, refute §2] | `stressNarrative.js:105-106`; `stressorsCore.js:140`; `conditionPromotion.js:31`, `:34`, `:63`; `activeConditions.js:75-76` |
| L-110 | `religious_conversion` / "Religious Conversion" / "RELIGIOUS CRISIS" | `stress[].type` | Three unequal branches chosen by `name.length % 3` (a faith gaining ground, a fracture, an outside authority REQUIRING conversion), and the summary and the faction block choose branches INDEPENDENTLY (settlement name vs governing-faction name), so one record can carry two stories; the corpus pool is named "RELIGIOUS CRISIS" and the desk records the label / pool mismatch. [stressors §5, R12] | `stressNarrative.js:119-133`; `stressFactions.js:301`; `stressorsStateProse.js:111-112` |
| L-111 | `summary` (wartime); `summaryRoll`; `ye` | `stress[].summary` | The ONLY persisted trace of the 45 percent profit coin (`summaryRoll` is deleted at assembly); a SECOND independent profit coin in the faction block (`militaryEffective >= 55 && economyOutput >= 45`); the profit summary asserts "The garrison is reinforced" at any tier. [stressors R14, refute §2] | `stressNarrative.js:40-42`, `:145-152`; `assembleSettlement.js:176-188`; `stressFactions.js:250-251` |
| L-112 | `source: 'event'` / `source: 'forced'` / `forcedByConfig` / `config.stressType(s)` | `stress[]` entry shape; effectiveConfig | An `APPLY_STRESSOR` entry has `severity` and no `summary` / `viabilityNote` / `crisisHook` / `historyColour`; a `forced` entry bypassed `requiresTier`; `config.stressTypes` after the pipeline is the ROLLED types (threaded by `resolveStress`), `intendedStressTypes` is what the user asked for. [stressors refute F1, F2, §2] | `crisisLifecycle.js:272-281`; `resolveStress.js:37-47`, `:61-69`; `stressGenerator.js:300-325` |
| L-113 | the stress-minted faction rows: `'Grain Holders'` · `'Departure Committee'` · `'Occupation Authority'` · `'Monster Hunters / Adventurers'` · the insurgency opposition · `'Crown Creditors (Noble Coalition)'` | `powerStructure.factions[]` | The engine's richest per-type model and the strongest licence or refutation on the record: Grain Holders = PRIVATE control of reserves (against "issued by rule"); Departure Committee = emigration, minted beside a summary that always says arrivals; the Occupation Authority's desc explicitly declines to fix a garrison ("varies"); Monster Hunters are "Outside professionals brought in or passing through", always minted; the insurgency opposition is an elite non-cooperation faction or an unarmed commons movement, never armed; Crown Creditors make the creditors RESIDENT houses. [stressors refute §2, §4.1] | `stressFactions.js:11-390`, `:45`, `:76-92`, `:137-142`, `:162-166`, `:188-204`, `:230-241` |
| L-114 | `emerging` / `peaking` / `easing` (lifecycle); `severity`, `age` (world stressor); `memoryStrength` | `lifecycleStage` etc. | `emerging` = `age <= 1` tick, nothing about visibility; `peaking` = `severity >= 0.72`, a threshold not a turning point; `easing` = `severity <= 0.24 && age > 0`, a low level not a trend; all three ORIGIN-relative (a spread target feels a never-re-aged stamp); `memoryStrength` exists only on echoes (`status: 'residual'`). [stressors §5, R16, refute F4] | `stressorsCore.js:285-290`, `:314-322`, `:362-368`; `stressors.js:770-776`; `stressorSeverity.js:28-32` |
| L-115 | `worsening` / `easing` / `stable` (condition status) | `condition.status` | Declared drift directions (+0.04 / -0.06 per tick); `easing` is FORCED within 2 ticks of expiry regardless of the real direction; `worsening` on the display path is the archetype's DEFAULT for a raw record with no status (the engine's own comment names the invented-motion hazard and refuses it on its own path); every generated condition sits at `elapsedTicks: 0`. [stressors §5, R19, refute R19] | `activeConditions.js:644-646`, `:846-849`, `:859`, `:879-882`, `:894-902`; `stressorsStateProse.js:329-331` |
| L-116 | `GENERATION` / `source: 'generation'` | `condition.triggeredAt.sourceEventType`; `causes[].source` | "This settlement was born carrying this", not an in-world event, `detail` literally `Settlement generated under stressor "<label>"`; nonetheless the value that routes the condition into the TRACED-provenance pool. [stressors §5, R20] | `conditionPromotion.js:203-208`; `stressorsStateProse.js:243-257` |
| L-117 | `critical` (condition band) vs `SEVERITY.CRITICAL` (structural validation) | two ladders | `severity >= 0.75` on a condition; a DIFFERENT `SEVERITY.CRITICAL` with members `CRITICAL · IMPLAUSIBLE · INEFFICIENCY · DEPENDENCY` for structural validation; and for a LIFT (`reconstruction`, `boom`, `flourishing`) or `war_spoils` the condition's severity is a magnitude of gain. [stressors §5, R18] | `activeConditions.js:541-547`; `src/data/constants.js:16-21`; `src/domain/worldPulse/archetypeCatalog.js:28-31` |
| L-118 | `regional_*` (13 archetypes) and their labels; `war_pressure`; `vassal_extraction`; `dominant_npc_removed` / 'Leadership void'; `faction_challenge`; `war_spoils` | `condition.archetype`; `condition.label` | Purely LOCAL stressors promote to `regional_*` archetypes and print 'Regional ...' labels; `war_pressure` is what a siege, a blockade, a war or a monster promotes to; `vassal_extraction` is what an occupation promotes to; `dominant_npc_removed` is what a succession void promotes to (the archetype says "removed", the label says "void", the stress summary says "died"); `faction_challenge` is a betrayal AND a coup; `war_spoils` is a BENEFIT ("sustain the war effort", `affectedSystems: []`, `defaultStatus: 'easing'`) that the annex files under "occupation layer" whose prose reads "Holding this place costs more than it returns". [stressors §5, refute §2] | `conditionPromotion.js:31`, `:34-45`, `:47`, `:53-55`, `:60-63`, `:76`; `activeConditions.js:75-76`, `:203-204`, `:211-212`, `:254-255`, `:274-275`, `:282-283`, `:306-307`, `:454-461`; annex `:4489-4490` |
| L-119 | `foreign_sponsored` / `declared_war` / `unattributed` / `palace_coup` / `military` (archetype) | `originContext.variant`; `contenders[].archetype` | "foreign" = a hostile NEIGHBOUR SETTLEMENT edge, "declared" = the edge's rank label, no polity, border or declaration event exists; `unattributed` persists after the DM names the attacker and is written onto ANY type whose `originContext` was null; `palace_coup` is the FALLBACK (no qualifying challenger, or an unmapped leading archetype), not "noble-led"; `CATEGORY_MAP` folds `watch` and `law` categories into `military`, so a `barracks_coup` may be led by a town-watch or a law faction. [stressors refute F5, F6, §2] | `stressorDynamics.js:600`, `:742-752`, `:776-786`, `:877`; `stressors.js:616-623`; `rulingPowerCoup.js:51`, `:104`; `src/domain/factionArchetypes.js:42-47`, `:55` |
| L-120 | `originContext.reason`; `'Strong walls give besiegers pause.'`; `'The granaries hold a real reserve.'` | world stressor reason strings | Free prose stamped at birth: for `servile_uprising` it asserts bound labour the model lacks; the wall reason reads `causalScore(entry,'defense_readiness') >= 70`, no wall; the granary reason reads `storageMonths >= 2` and its sibling institution reason a class regex that includes a FISHERY and a MILL. [stressors §5, refute §2] | `stressorDynamics.js:48`, `:837`; `stressorGates.js:446-447`, `:459-467`; `stressorsCore.js:44-46`, `:175-180` |
| L-121 | `'local watch'`; `deprecated: true`; `blocksResolution`; `floorsMet: false`; `probability`; `STRESS_SEVERITY_WEIGHT`; `colour` / `historyColour`; `icon` | various | `'local watch'` is the engine's own generic force word below town tier (a different fact from `inst.hasWatch`); `deprecated` on `slave_revolt` means organic births are folded out ("no slavery substrate"); `blocksResolution` is reserved for hard causal dependencies and suppressed for an echo; `floorsMet: false` caps the counterforce score at neutral, never penalises, and means something different for `siege` (the only `requireAllFloors: true`) than for every other type; `probability` is a generator draw weight capped at 0.35; the severity WEIGHT is a narrative sort key; `historyColour` is a six-member timeline tint (`famine` is `economic`, `plague_onset` is `disaster`); `icon` is always `undefined` though the annex's STATE-KEY names it. [stressors §5, A10; refute §2] | `safetyProfile.js:30-35`; `stressorsCore.js:166-182`; `stressorDynamics.js:157`, `:171`, `:367-371`, `:429`, `:462-465`, `:583`; `stressTypes.js:13-14`, `:20`, `:30`, `:92`; `stressGenerator.js:51`, `:257`, `:263-279`; annex `:4049` |

### 3.9 War and faith labels

| # | label | field | engine meaning | source |
|---|---|---|---|---|
| L-122 | `contested` (four senses) | legitimacy band; `faith.contested`; occupation rung; the DS-FTH-4 key | (i) a legitimacy score below 0.25 (nobody need be contesting it); (ii) a rival within six share points (nothing to do with legitimacy); (iii) the LOWEST rung of the occupation ladder, "the occupier has not yet taken hold" (also the DEFAULT phrase for an unknown state); (iv) DS-FTH-4's disjunction "a rival niche occupied, OR legitimacy in its low band". [warFaith §5, F7, F8] | `faithPanelModel.js:39-44`; `religionState.js:56`, `:626`; `occupationStatus.js:40`, `:54-56`; `occupation.js:125`, `:132-141`; annex `:6270` |
| L-123 | `established` (legitimacy) vs `established` (standing); `cult` (standing) vs `cult` (rankAxis / pantheon tier) vs `cults[]` vs `PANTHEON RANK: Cult` | four faith fields | Legitimacy `established` is a score in `[0.5, 0.75)`; standing `established` is an adherent SHARE of 15 in this town (with hysteresis down to 11); standing `cult` is share < 15 AND the value forced onto a SUPPRESSED creed; `rankAxis` / tier `cult` is a realm-wide SEAT COUNT below 2 (and `rankAxis` is the AUTHORED embed rank, never synced to the ledger tier); `cults[]` is every non-patron embedded deity regardless of rank or standing. [warFaith §5, §6, F3, F4, F5, F12] | `faithPanelModel.js:41`, `:189`, `:244`; `religionState.js:51`, `:150-155`, `:276`; `pantheon.js:54-57`, `:78-79`, `:122`, `:383-388`; `deitySnapshot.js:61`; `deityCommitEmbed.js:135` |
| L-124 | `live` (false) | the `faithDark` lens | "No `faithProfile` projected", true for every standalone settlement and every un-pulsed campaign; not "newly founded". [warFaith refute F27] | `faithPanelModel.js:194`; `religionState.js:614-618`; `FaithTab.jsx:190`, `:218` |
| L-125 | `piety.trend`; `secular`; the sink's direction | `pietyTrend`; the devotion band; `unaffiliated` | The trend is the sign of `structuralTarget - local01`, where the reading is HEADING, not where it has been; `secular` is the floor band (`local01 < 0.2`), not the absence of faith (DS-FTH-2 is the no-patron pool); the engine does not project the sink's own direction and the corpus's growing / shrinking is a declared proxy off the piety trend. [warFaith §4.2, F16, F18] | `faithPanelModel.js:71-86`; `warFaithStateProse.js:531-533`, `:611-613`; `religionState.js:386-395`, `:640-644` |
| L-126 | `discredited` (in the `LEGITIMACY: contested` prose); `imposed`; `abandoned` | `PATRON_FALL_CAUSES` | `discredited` is a FALL cause token (the seat was LOST on a contest), which the legitimacy band records nothing of; `imposed` has two producers (a garrison flip and the DM's re-assign) and the precedence is first-match-wins over overlapping arms; `abandoned` is deliberately absent. [warFaith §4.2, F7, F20] | `patronFall.js:46-50`, `:62-71`, `:76`, `:86-90`, `:105-119` |
| L-127 | `mandate` propping = `null` | `divineMandateStatus` | `null` for a government whose `mandateGovWeight <= 0` ("a merchant council or a republic"): REFUSED-TO-READ, not "no mandate". [warFaith §5] | `religionState.js:786-794`; `warFaithStateProse.js:580-583` |
| L-128 | `honored` (compliance) | `treatyTermPoolKey` | (i) `normalizeState` fails to `honored`: anything unrecognised, including absent, renders `honored`; (ii) `complianceState` is the OBSERVED state under fog (`observedState`), the `trueState` kept off the read-model on purpose. [warFaith §5, W22, W23] | `treatyDocument.js:159-161`, `:218-223`, `:237-239`; `peaceTerms.js:705-707` |
| L-129 | `victorId` / `loserId`; `atWar`; `At war` (the pool key); `warBeat` | `treatyOrientation`; `warStatus`; the residue key | `victorId` is `receiverId` and `loserId` is `giverId` "on a WR-10 sale exactly as on a war settlement" (victor = buyer); `atWar` is a constant `true` whenever the object exists and `null` otherwise; the `At war` pool is the RESIDUE arm, and `warBeat` fires on a decaying scar, an `alert` posture, an army in transit and a holding of others. [warFaith §5, 0.3, W28] | `treatyOrientation.js:171-176`; `warStatus.js:296-297`; `warFaithStateProse.js:188-207`, `:420-426`; `WarTab.jsx:485-492`, `:523` |
| L-130 | `pays`; `burdened` | `settlementOccupation` | `pays` = the rung is `extractive` or above, no yield-vs-cost comparison exists and the engine's own scales invert at `extractive` (benefit 0.45, burden 0.55); `burdened: !pays \|\| resistance >= 0.35` is not the complement of `pays`. [warFaith §5, W9] | `occupationStatus.js:48-50`, `:102`; `occupation.js:135-141`, `:150-156` |
| L-131 | `largely quiescent`; `at peace` (fallback); `contested` (occupation default); `occupation authority` (fallback string) | phrase readers | `largely quiescent` is the `[0, 0.2)` band, not zero resistance; an UNKNOWN posture token reads "at peace"; an unknown occupation state reads `contested`; `occupiedSettlements(...).occupier` falls back to the literal string `'occupation authority'`, which will render as a name. [warFaith §5] | `occupationStatus.js:54-56`, `:59-64`; `mobilizationStatus.js:45-47`; `warStatus.js:333-335` |
| L-132 | `war_exhaustion` (three records); `rested`; `NON-REVERTING` | posture state; condition archetype; the scar ledger | A POSTURE state ("war-weary, its army spent"), a CONDITION archetype and a 0..1 SCAR ledger share one word; `rested` is exactly zero scar, which a decayed scar reaches (not "never fought"); the display's own "NON-REVERTING" comment is false, the scar decays at 0.03 per tick when the army is home, and the scar is also written onto a levied vassal that fielded nothing. [warFaith §5, W16, W17] | `mobilizationStatus.js:33`; `activeConditions.js:351`; `warStatus.js:343-352`, `:375-381`; `warHomeCosts.js:69`, `:142-143`, `:160`, `:432` |
| L-133 | `alert` under the `climbing the ramp` key; `deployed` under the `fully ready` key; `ticksToDeploy` | `mobilizationPoolKey` | `alert` is "on alert" with nothing raised, yet `RAMP_CLIMBING` includes it; at `deployed` the army HAS marched, yet the desk routes it into "fully ready"; `ticksToDeploy` is "Heuristic, presentation-only" and returns 0 at both ends of the ramp. [warFaith §5, W13, W14] | `mobilizationStatus.js:29`, `:50-53`, `:58-69`; `warFaithStateProse.js:49-55`, `:230`, `:238-239`, `:255-261`; `mobilization.js:311-320` |
| L-134 | `Even-handed` (aggression posture) | `aggressionPosture(mult)` | The fall-through arm covering `[0.96, 1.04]` including exactly 1.0; the exported chip reader returns `null` for the same range. [warFaith §5] | `liveWorld.js:89-95`; `livingWorldSignals.js:153`, `:161` |
| L-135 | `cults[]` (the `patronCults` lens); `rankAxis` | `config.cultDeitySnapshots`; the embed | The static embed list, never reconciled against suppression or eviction in the live state; `rankAxis` is the authored rank on the embed, defaulted to `minor`, not the pantheon's seat-count tier. [warFaith refute §4] | `faithPanelModel.js:192`, `:244`, `:249`; `religionState.js:270-278`; `deityCommitEmbed.js:135`; `pantheon.js:122`, `:383-388` |
