# REFERENT-TABLE.draft — THE REFERENT LAW, folded from six desks (twelve packets)

Folder: Fable 5.1 (the fold seat of the referent survey). Dock `laneRW-DEFW` at `f2da5a3ee`, READ-ONLY; every `file:line` below is the surveyor's or refuter's re-derivation in that tree, carried with its verdict. This is a research packet, not prose and not a ruling: the chair ratifies. Sources: `<desk>.referent.survey.md` (Opus surveyor) and `<desk>.referent.refute.md` (Fable refuter) for defense · general · economy · power · stressors · warFaith, all read WHOLE; the law from `briefs/brief-REWRITE-car8b-defense.md` ADDENDUM 11.

STATUS: IN PROGRESS — checkpointed section by section (the checkpoint law, owner 2026-09-11). Sections: 1 THE LAW · 2 PER-DESK TABLES (+ REFUSED) · 3 OVERLAPS · 4 POWER SLOTS + VISIBILITY · 5 WRONG-LAYER FINDINGS · 6 OWED TO THE CHAIR · 7 CLOSING COUNT.

---

## 1. THE LAW as the chair drafted it — ADDENDUM 11, quoted verbatim from `briefs/brief-REWRITE-car8b-defense.md` (lines 93-107 at the time of the fold)

```
## ADDENDUM 11 (chair, 2026-09-10 00:1x) — THE REFERENT LAW (draft; a sibling of ADDENDUM 10; the referent survey follows the entailment survey)
THE OWNER (00:0x): "words can also be related to both the power and/or faction or individual institution or both, and so laws must be made to decide which one it attributes and that situation."
THE ENGINE ALREADY TYPES THE LAYERS, so the law is a mapping and not an invention (all cites in laneRW-DEFW at f2da5a3ee):
- **BODY** — an institution row (`settlement.institutions[]` by recorded name), bucketed for defense by `src/domain/institutions/defenseInstitutionBuckets.js:83-104` (walls · garrison · militia · watch · mercenary · charter). The engine's own alias overlap is on the record: "professional city watch" sits in BOTH the garrison and the watch buckets.
- **HOLDER (the record's keeper)** — `src/domain/prose/holderTable.js:78` HOLDER_KINDS (treasury · muster · census · parish · toll-bar · market · watch · court · elders · tradition · road · office); the paid military the upkeep gate measures is the MUSTER kind (walls · garrison · militia · mercenary · charter, lines 188-192); the WATCH kind is a STATE ORGAN (line 115: office · court · treasury · watch) that keeps the order records (`criminalCaptureState`, `blackMarketCapture`, `safetyProfile`, lines 210-212). So in THIS engine "the watch" is two things by table: a defense BODY (`forces.watch.present`, DS-DEF-5) and the state's order ORGAN (a holder). The owner's "the watch is the guard/military power" is English; the engine splits it, and the chair's earlier sentence ("the watch is a class word for the paid military") is CORRECTED here: the class word for the paid military is THE MUSTER; "the guard" is a body word (garrison bucket: professional guard); "the watch" names the watch bucket or the order organ and never the muster as a whole.
- **POWER** — a typed standing over an organ: the settlement-wide capture ladder `powerStructure.criminalCaptureState` (none · adversarial · equilibrium · corrupted · capture) and the per-faction `captureState` (`holderTable.js:129-143`, `factionCapture.js:136`), which make the four state organs INTERESTED in their own records (SITTING §R c-22); a per-institution corruption impairment on a SECURITY body (`corruption.js:630` SECURITY_INSTITUTION_RE = watch · garrison · constab · guard · magistrate · court · barracks; `impairments[].type === 'corruption'`, `covert` true = the DM's hidden channel, else a public scandal, lines 655-680); a brokerage house's PATRON (`brokeragePatronage.js:60` genesis · captured). The power is a FACTION or the ruling structure, and a faction is a NAME the engine records (a `proper`-typed slot), never a baked noun.
- **PERSON** — NEVER a referent: `COLUMN_SOURCES.holderRole` is a measured null (no typed NPC-to-institution edge exists; `holderTable.js:32-36`), the DM face names the office and never the officer, and product scope forbids a named character's fate. An individual appears only as a ROLE word (the captain, the reeve) in a standing state.
THE LAW, as the chair drafts it (vetoable):
1. **A word's referent is fixed by the layer of the READ the sentence carries, never by the word.** A body read (presence · pay · upkeep · muster · service rows · walls) refers to the INSTITUTION AS A BODY and uses body words (the muster, the garrison, the militia, the watch as a bucket name, the men on the wall). A standing read (capture · corruption impairment · patronage · INTERESTED) refers to the ORGAN UNDER A POWER and names the power only through the typed slot the engine records (the faction's name; "the ruling structure" as the class word), with the institution as the OBJECT of the sentence ("the watch is bought", "the house has a patron"). A role read (an office that exists) refers to the ROLE and states a standing condition, never a fate.
2. **The marker's skeleton tags every claim's REFERENT LAYER beside its licence** (BODY / ORGAN-UNDER-POWER / ROLE), and the licence card prints the layer per read once ratified; a refuter fails a face whose noun's layer does not match its read's layer ("the bought watch" on a pay read; "the muster's wages" on a capture read; a captain doing anything on any read).
3. **SAME WORD, SAME REFERENT inside a composed unit.** The thread rule (the owner, 09-08: adjacent sentences carry a noun forward) carries the REFERENT forward, not the spelling: a unit may not say "the watch" as a body in one sentence and "the watch" as a captured organ in the next without the layer change being stated in words. Where a town holds BOTH (a corruption impairment on a security body AND a pay shortfall on the muster), they are TWO facts with two referents, seated as two units; a fused agent ("the bought watch is paid short") asserts a relation no field computes and is refused unless a relation row exists (S2's clause seat).
4. **Visibility follows the power layer.** A covert corruption impairment or a birth capture is the DM face (the pen line beside the passage, SITTING §T); the player face says what the state's organs would admit, which is the body read alone. A revealed impairment is a public scandal and both faces may name it.
5. **The alias overlap inside the engine is a wiring fact, not a writer's.** "professional city watch" in two buckets, and the WATCH holder kind that is not the watch bucket, go to the register car as a census/holder finding; until cured, a face never uses "the watch" for a pay-gate read (the muster kind does not contain the watch bucket).
WHAT THE REFERENT SURVEY OWES (queued to run after the entailment survey `wf_8ca05b10-e44` completes, same shape: one Opus surveyor + one Fable refuter per desk, a Fable folder → `rewrite/entailment/REFERENT-TABLE.draft.md`): for every institution-class noun each desk can render, its layers (body / holder kind / power / role), the reads that resolve to each layer, the engine's own overlaps, and the typed slot that names the power where one exists. The chair ratifies both tables together before DEF-2 / DEF-1 / DEF-5 launch.

```

---

## 2. THE TABLES PER DESK

Column key: **noun** · **layers** = the layer assignments that HOLD (or are CONDITIONAL, with the condition printed in the cell) — BODY row / HOLDER kind / POWER slot / ROLE · **reads per layer (pools)** · **safe class word** (the always-safe spelling for the layer, as the packets found it and as ADDENDUM 13 item 5 already rules where it reaches) · **source** (file:line at f2da5a3ee, re-derived by the surveyor and confirmed or corrected by the refuter). Verdict = the refuter's. Every assignment the refuter REFUTED is in the desk's REFUSED table with the evidence.

Cross-desk note on the layer vocabulary (raised by three refuters, ruled by none): the three-valued tag BODY / ORGAN-UNDER-POWER / ROLE is not exhaustive. The packets found reads that are **AGGREGATE** (a band, score or label computed over bodies that refers to none: defense §3.9, general §3.2), **NONE** (a stock band, a balance, a status, a terrain token, an absence — no institution row at all: economy refute §2), **HOLDER** (a citable record-keeper NOT under a power: economy refute §2, the market · toll-bar · road · elders · parish kinds are holders and not state organs), **EXTERNAL BODY** (the besieger's army, the occupier's garrison: defense M-13) and **PERSON-ROOTED STANDING** (the covert channel's NPC ground: defense M-10). See §6.

### 2.1 THE DEFENSE DESK (DS-DEF-1 … DS-DEF-11; 126 census rows · 383 variants · 80 RESOLVED / 46 WIRING-UNRESOLVED)

Sources: `defense.referent.survey.md` (Opus) · `defense.referent.refute.md` (Fable). Layer denominators the refuter confirmed: holder kinds over the 126 rows `(none) 91 · muster 26 · muster + watch 2 · toll-bar 3 · treasury 2 · muster + road 2`; POWER reads 5 (DS-DEF-4's capture ladder); ROLE reads 0 by field (but see `{seat}`); covert-by-path rows 0 of 126; `dm-only` marks 7 of 383.

| noun | layers (HOLDS / CONDITIONAL with condition) | reads per layer (pools) | safe class word | source |
|---|---|---|---|---|
| wall · walls | BODY: walls bucket; flag `hasWalls`. HOLDER: muster kind — CONDITIONAL: the wall's record is held by the muster ROLL, whose only roster holder is the `Citizen militia`; a garrison-only town resolves `holder: null` | BODY: `forces.walls.present` (DS-DEF-5 walls PRESENT/ABSENT; DS-DEF-11 ×5); `walls` arg of `invasionRowSituation` (DS-DEF-2 Invasion ×6); `perimeter` arg of `beastsRowSituation` (DS-DEF-2 Beasts ×7) | the wall · the perimeter (HOLDS); "the line" CONDITIONAL — a wall word only where the pool reads `forces.walls.present`; in DS-DEF-7 it is the MANNED front (a force word) | `defenseInstitutionBuckets.js:84-87`; `priorityHelpers.js:52`; `holderTable.js:188`, `:281-284`; `defenseStateProse.js:623-626`, `:655-656`, `:1089`, `:1385`; annex `:3092-3124` (the line as a force word) |
| citadel · palisade · earthwork · massive walls | BODY only (walls bucket) | as walls | the wall class (the `{defwork}` fill; W11 bars the material) | `defenseInstitutionBuckets.js:85-87`; catalogue `Citadel` (city), `Palisade` (thorp), `Palisade or earthworks` (hamlet/village), `Massive walls and fortifications` (metropolis) |
| gate · gates | BODY only — in the walls bucket through the substring "walled" (accident in one table, intent in the other: the flag list names `gates (if walled)` on purpose); `hasGates` fires on a palisade with no gate row; `Gates (if walled)` is ALSO a toll-bar roster row | no pool reads a gate directly; "the gate" recurs on non-walls pools (annex `:2895`, `:2904`, `:2929`) | the gate (body) — only where `forces.walls.present` is read | `defenseInstitutionBuckets.js:85`, `:138`; `priorityHelpers.js:52-53`; `holderTable.js:308` |
| `{defwork}` (the slot) | BODY only — the town's own wall by recorded name; refuses any name outside the four wall words and drops the variant | DS-DEF-11 ×3 name it | — (it IS the derived fill) | `defenseStateProse.js:1003`, `:1027-1035`, bag line `:1086` (the survey's `:1084` is `const slots = {`) |
| garrison | BODY: garrison bucket; flag `hasGarrison`. HOLDER: muster kind (the `garrison` token). POWER-eligible: a SECURITY body for a corruption impairment | BODY: `forces.garrison.present` (DS-DEF-5 garrison PRESENT); `garrison` arg (DS-DEF-2 Invasion ×6, garrison outranks militia); `garrison \|\| militia` as `force` (DS-DEF-2 Beasts ×7) | the garrison (HOLDS). At city tier the pool's roster ALWAYS includes the required `Professional city watch` (a garrison keyword) — O-1 | `defenseInstitutionBuckets.js:88-91`; `priorityHelpers.js:46`; `holderTable.js:189`; `corruption.js:630`; `defenseStateProse.js:655-656`, `:1270`, `:476-483` |
| barracks | BODY (garrison bucket); a SECURITY body | via `forces.garrison.present` | the garrison | `defenseInstitutionBuckets.js:89`; `corruption.js:630`; catalogue `Barracks` (town, 0.3) |
| professional guard | BODY keyword with NO shipped row | none | — (a keyword the world never builds) | `defenseInstitutionBuckets.js:89`; `threatDefensePolicy.js:38` (`Professional guard (hundreds)`, no catalogue row) |
| militia · citizen militia | BODY: militia bucket; flag `hasMilitia`. HOLDER: muster kind — the ONLY roster holder of the muster record (`Muster training`) | BODY: `forces.militia.present` (DS-DEF-5 militia PRESENT (no garrison)); `militia` arg (DS-DEF-2 Invasion); `force` disjunct (Beasts) | the militia (HOLDS). "the muster": CONDITIONAL — as a cited RECORD only where a live `Citizen militia` stands ("a town with a Garrison and no militia has men under arms and no roll of them"); as the CLASS WORD for the paid military it is the chair's ruling (ADDENDUM 13 item 5), and on a walled Town-watch-only town it names a body the town does not have (A-11) | `defenseInstitutionBuckets.js:92-94`; `priorityHelpers.js:47`; `holderTable.js:190`, `:279-288`; catalogue `Citizen militia` (hamlet/village/town) |
| mercenary company · mercenary quarter · hired muscle | BODY: mercenary bucket; flag `hasMercenary` (the flag catches THREE rows the bucket does not: `hireling hall`, `free company hall`, `veteran's lodge`). HOLDER: muster kind | `forces.mercenary.present` (DS-DEF-5) — fires only at CITY (`Mercenary quarter` is the only shipped bucket member) | the contracted force (the annex's own fence licenses "the contract is a contract") | `defenseInstitutionBuckets.js:98-100`; `priorityHelpers.js:49`; `holderTable.js:191`; `defenseStateProse.js:1284`; annex `:2877-2878` |
| adventurers' charter hall | BODY: charter bucket; flag `hasCharterHall` (`hireling hall` is in BOTH `hasMercenary` and `hasCharterHall`). HOLDER: muster kind (token `charter`) — no roster holder | `forces.charter.present` (DS-DEF-5 ×2) | the charter hall | `defenseInstitutionBuckets.js:101-104`; `priorityHelpers.js:49`, `:51`; `holderTable.js:192` |
| magicDef bodies (wizard · mages' guild · academy · alchemist · golem workforce) | BODY: the SEVENTH bucket (ADDENDUM 11 lists six); flag `hasMagicInst` | DS-DEF-5 `arcane defense PRESENT/ABSENT` reads `forces.magicDef.present` in CODE (`arcaneDefensePoolKey`); the CENSUS lists only `magicWorks` — a census under-report (M-12) | arcane provision | `defenseInstitutionBuckets.js:105-108`; `priorityHelpers.js:68`; `defenseStateProse.js:1319-1321`, `:1389` |
| `force` (the token) | HOLDER: muster kind; on this desk `force = garrison \|\| militia` — EXCLUDES the watch, the mercenary, the charter | the `force` arg of `beastsRowSituation` (DS-DEF-2 Beasts ×7) | the muster (garrison-or-militia only) | `holderTable.js:193`; `defenseStateProse.js:655`, `:429-440` |
| watch | BODY: watch bucket (`town watch · city watch · professional city watch`); flag `hasWatch` (same three spellings). HOLDER: the WATCH kind, a STATE ORGAN, keeping `blackMarketCapture · criminalCaptureState · safetyProfile`; its roster holders are `Professional city watch` / `Town watch` — THE SAME ROWS as the bucket (M-2: two tables over one row). POWER-eligible: a SECURITY body. CONDITIONAL on the pool: `watch PRESENT` fires only where garrison AND militia are absent (`forceCorePoolKey` garrison > militia > watch), so never at city and never for the `Professional city watch`; the flag `hasWatch` reaches the PAY GATE (`hasAnyDefense`) and so DS-DEF-11 WALLED-STRAINED and the two `muster + road` pools (A-11) | BODY: `forces.watch.present` (DS-DEF-5 watch PRESENT; NO organized force at all). HOLDER-ORGAN: the two-source rows (card `muster + watch · a STATE ORGAN`); the organ's records are read DERIVED by DS-DEF-3 (`publicOrderSituation(safetyLabel)`) and DS-DEF-4 (`text(captureState)`) and the census resolves neither back to the kind (W-1). SECURITY: no landed pool; the unlanded `watch: bought (revealed)/(covert)` taste pools | the watch AS A NAME only where the town resolves `Town watch` or `Professional city watch` (town and up); "keeps watch" as an activity; the engine's own tier-derived `watchLabel` (`town watch` / `city watch` / `local watch`); NEVER "the watch" for the pay-gate read by the TABLE (rule 5) — but see REFUSED: the gate's PRODUCER counts `hasWatch` | `defenseInstitutionBuckets.js:95-97`; `priorityHelpers.js:48`; `holderTable.js:78-81`, `:115`, `:202-212`, `:317-323`; `corruption.js:630`; `defenseStateProse.js:1269-1275`; `defenseGenerator.js:177-178`, `:189-191`; `fieldSynonyms.js:49-52`; `safetyProfile.js:30-35`; `institutionalCatalog.js` (`Town watch` REQUIRED at town; `Professional city watch` REQUIRED at city) |
| court · courthouse | BODY: flag `hasCourtSystem` — satisfied by `town hall` / `city hall`, both REQUIRED, so nearly every town+ is `hasCourtSystem` through its HALL. HOLDER: court kind, a STATE ORGAN; its field tokens are the SEAT's (`govMultiplier · blocs · stability · termLines …`). POWER-eligible: a SECURITY body (`Courthouse`, `Multiple courthouses`, `Multiple court buildings`) | DS-DEF-2 Internal Security ×4 (`court ; prison`, SOURCE-UNRESOLVED); DS-DEF-6 Legal Infrastructure ×4 (WIRING-UNRESOLVED) | the court (body) only where a courthouse row resolves; otherwise the function word ("the town's law") | `priorityHelpers.js:55`; `holderTable.js:78-81`, `:115`, `:215-223`; `corruption.js:630`; `defenseStateProse.js:657-659` |
| prison · gaol · stocks | BODY: flag `hasPrison` only — no holder kind | DS-DEF-2 Internal Security ×4; DS-DEF-6 ×4 | the gaol · a place of confinement | `priorityHelpers.js:54` |
| treasury | HOLDER: treasury kind, a STATE ORGAN — no defence body | DS-DEF-8 `viability threatened` / `intact` (card: `treasury · a STATE ORGAN`) | the purse · the treasury | `holderTable.js:78-81`, `:115`, `:166-169`; census (`economicViability.viable` → treasury) |
| office | HOLDER: office kind (the record itself), a STATE ORGAN; a citation on it is a FINDING, never a licence | none on this desk | the record — and no citation | `holderTable.js:84`, `:115`, `:237-239`, `:362-363`, `:488-490` |
| granary · the stores | BODY: flag `hasGranary` (substring `granar`); a `granary` holder row was drafted and WITHDRAWN | DS-DEF-2 Disasters ×5; DS-DEF-6 Logistics ×5 | the granary (the building) · the stores (the stock) — never both in one word | `priorityHelpers.js:63`; `holderTable.js:157-158` |
| hospital · infirmary | BODY: flag `hasHospital` — catches `monastery`, `healer`, `friary` (so "hospital present" can be a friary) | DS-DEF-2 Disasters ×5; DS-DEF-6 Medical ×3 | the infirmary · somewhere for the sick | `priorityHelpers.js:64`; shipped `Healer (divine, 1st level)`, `Monastery or friary`, `Multiple monasteries` |
| church · parish · clergy | BODY: flag `hasChurch`. HOLDER: parish kind (tokens `piety`, `unaffiliated`); a `church` holder row was drafted and WITHDRAWN | DS-DEF-2 Disasters ×5 (the `church` arg); DS-DEF-6 Clergy care | the parish (organ) · the clergy (body, plural, unindividuated) | `priorityHelpers.js:65`; `holderTable.js:78-81`, `:157-158`, `:227-228` |
| port · docks · harbour | BODY: flag `hasPort` (`PORT_INFRA_RE` also matches `navy`). HOLDER: toll-bar kind | DS-DEF-6 Naval Defense ×3 (card `toll-bar · LICENSED`); Logistics ×5 | the harbour · the toll bar (organ) | `priorityHelpers.js:32`, `:61`; `holderTable.js:183-185` |
| navy · fleet | BODY: flag `hasNavy` (`navy`, `major port`) — CONDITIONAL: the defense refuter's enumeration of `institutionalCatalog.js` finds NO shipped member; the general refuter's sweep of `institutionServices.js` finds a `Navy` row (X-D1, §6) | DS-DEF-6 Naval Defense reads `blockaded ; navy ; port` | the ships | `priorityHelpers.js:62`, `:45`; `foodStockpile.js:417` |
| road | HOLDER: road kind (tokens `terrainType`, `monsterThreat`) — CONDITIONAL: it resolves to institutions (`Listening post` / `Waystation`), so the survey's "a body word for nothing" is wrong as a holder and right as a defence bucket | DS-DEF-11 WALLED-THREATENED / QUIET (card: `muster + road`) | the road (organ) · the country | `holderTable.js:78-81`, `:233-234`, `:349-355` |
| `{seat}` (the slot) | POWER, typed — `proper`, filled from `power.government` — CONDITIONAL: the value is the governing faction's LABEL, whose vocabulary includes ROLE words (`Town Mayor`, `Elected Reeve`, `Feudal Appointee`) and standing-laden labels (`Corrupt Council`, `Corrupt City Council`, `Shadow Senate`) — so the desk's one typed power slot can render a ROLE as the organ, or a standing the capture read denies (M-7, A-16); the census reports the slot NAMED BUT NEVER FILLED (W-2) | DS-DEF-4 capture ×5 name it | `{seat}` itself; the class word **the ruling structure** / **the seat** where the fill is refused (the module's own comment accepts "the hall" as the fallback — a register finding) | `defenseStateProse.js:961` (fill; the survey's `:962` is the closing brace), `:866-871`; `rulingStructure.js:243-247`, `:259-292`, `:443-452`, `:787`, `:792`; census `slotsWithoutProvider: ["seat"]` |
| the ruling structure | POWER (class word) | DS-DEF-4 capture ×5 | the ruling structure · the seat | `rulingStructure.js:797` |
| `criminalCaptureState` (the ladder) | POWER — a standing over the state's organs; mapped to the WATCH kind — CONDITIONAL: the engine reads ONE field two ways (a capture of the STATE reaching four organs; a capture of the CITY WATCH by the underworld) and "captured" has two thresholds (birth capture from `adversarial`; world capture at `corrupted`/`capture` only) (M-8, M-9) | DS-DEF-4 capture ×5 (census read `text(captureState)`; card `(none) · SOURCE-UNRESOLVED`) | the capture rung | `rulingStructure.js:797`; `holderTable.js:107-110`, `:129-143`, `:141`, `:211`, `:661`; `factionCapture.js:129-150` (ladder literal `:137`) |
| per-faction `captureState` | POWER, typed | none on this desk | — | `holderTable.js:134-140`; `factionCapture.js:136-146` |
| `{faction}` (the slot) | POWER, typed `proper` — declared on DS-DEF-3/4/5, named by ZERO variants, filled by ZERO call sites | none | — | annex `:2737`, `:2800`, `:2870`; grep over `defenseStateProse.js`: 0 hits |
| `{npc}` (the slot) | PERSON — declared, forbidden by the block's own fence ("the leash is the state, not a fate") | none | — (never a referent) | annex `:2800`, `:2809-2810` |
| the criminal interest · the operators · the syndicate | POWER, UNTYPED (an authored noun) — CONDITIONAL for "syndicate": the engine's OWN label for the `organized` structure key (`Organized Syndicate`), licensed on a STRUCTURE read, never on a CAPTURE rung | DS-DEF-4 structure ×4, capture ×5 | the criminal interest (class) | `defenseDisplay.js:162`, `:183-195`; annex `:2807`; `defenseStateProse.js:975-976` |
| a corruption impairment | POWER over a SECURITY body (`covert` boolean) | none landed; the unlanded taste pools `watch: bought (revealed)/(covert)` | — | `corruption.js:630`, `:661-691`; `prose-licence-card.mjs:37-41` |
| a brokerage PATRON | POWER over a house — verified as engine fact, NOT a defense-desk read | none | — | `brokeragePatronage.js:60` |
| practitioner(s) | ROLE (plural, a standing condition) — under a MUSTER holder (`magicDependency`) | DS-DEF-9 ×6; DS-DEF-6 Magical Capability v2 | a practitioner · the people it calls on | `holderTable.js:194`; `defenseInstitutionBuckets.js:105-108`; annex `:3213`, `:2983` |
| `Town Mayor` · `Elected Reeve` · `Feudal Appointee` | ROLE words entering as the POWER through `{seat}` (the refuter's A-8) | DS-DEF-4 ×5 at render | — (the register car owes a `seatKind`, or the class word replaces the slot) | `rulingStructure.js:284`, `:452` |
| soldiers (missed noun) | BODY at the person-plural grain — the paid military; no table splits it | 14 occurrences across DS-DEF-2/5/6/8/10 | the soldiers · the men (the one always-safe body spelling the refuter finds) — ADDENDUM 13 struck "the men on the wall" (asserts manning + a wall) | annex `:2617`, `:2648`, `:2894`, `:2899`, `:2900`, `:2914`, `:2919`, `:2925`, `:2963`, `:2983`, `:3144`, `:3298` |
| army (the enemy's; the town's abroad) | EXTERNAL BODY (`besiegedBy`, muster-kind tokens) or a hypothetical; `{counterpart}` unfilled | 6 occurrences | the besieger (through `{counterpart}` once wired) | `warStatus.js:289-297`; `holderTable.js:196-197`; annex `:2562`, `:2652`, `:2658`, `:3138`, `:3144`, `:3287` |
| the line | ONE spelling, TWO layers: walls-BODY in DS-DEF-2/5; the MANNED FRONT (a force word) in DS-DEF-7 — rule 3's case inside one desk | 19 occurrences | the wall (DEF-2/5) — never "the line" where the pool reads a force | annex `:2601-2656`, `:2883`, `:3092-3124` |
| specialists · specialist recourse | charter-hall BODY at the person grain; asserted on the Beasts key which does NOT read `charter` | 5 occurrences | the charter hall | `defenseStateProse.js:655`; annex `:2606`, `:2918`, `:2923`, `:2924` |
| `Household levy` | BODY in NO table — a force only to `threatDefensePolicy.js` (`levy`); a thorp with a levy reads `NO organized force at all` (M-5) | 0 in prose | — | `institutionalCatalog.js:104`; `threatDefensePolicy.js:13`, `:47`; `defenseStateProse.js:1273-1274` |
| `{counterpart}` (the slot) | the besieger / occupier NAME (an EXTERNAL POWER) — `proper`, declared on DS-DEF-1/5/6/7/8/10, named by 3 rows, NOT filled at this tip | DS-DEF-10 UNDER OCCUPATION ("What stands on these walls answers to {counterpart}") | the occupier (class) | annex `:2508`, `:2870`, `:2950`, `:3297-3299`; `warStatus.js:283-298`; `occupationStatus.js:74-76` |
| `{institution}` (the slot) | a BODY-naming slot with no provider | DS-DEF-6 Clergy care v2 (`:3018`); DS-DEF-9 declares it | — | annex `:2950`, `:3197`; census `noProvider: ["institution"]` |
| the occupier · the people who put them here | an EXTERNAL POWER possessing the garrison (M-13): a BODY word with a foreign possessor; no row in the survey carries a possessor layer | DS-DEF-10 UNDER OCCUPATION | the occupier's men · `{counterpart}`'s garrison (W12) | annex `:3297-3299`; `occupationStatus.js:74-76` |
| the purse | treasury ORGAN word — on a court/prison read at `:2667` it names an organ the read does not reach | DS-DEF-2 `:2667`; DS-DEF-11 fence `:5969` | the purse (treasury reads only) | `holderTable.js:166-169` |
| the town's law · the law | a FUNCTION word (safe), sometimes made an agent ("reaches for the purse") | `:2661`, `:2667`, `:2763` | enforcement · the law (function, never agent) | `safetyProfile.js:281` |
| fences · bandits · small operators | criminal BODIES at the person grain on the `diffuse` structure key — the only three substrings the classifier reads | DS-DEF-4 `:2826` | — | `defenseDisplay.js:189` |
| the sick-house · infirmary · the parish | hospital flag (may be a friary) / parish ORGAN | `:2708`, `:2723`, `:3017` | as hospital / parish rows | `priorityHelpers.js:64`; `holderTable.js:296-302` |
| per-arm badge · overall readiness · safety label · criminal structure · military posture · live readiness band · supporting-capability status | AGGREGATE — computed FROM bodies, refers to NONE (27 of the desk's pools); the safety label is reachable with NO law body at all (a thorp reads `Moderate` from a community bonus) | DS-DEF-1 ×4, DS-DEF-2 Economic ×4, DS-DEF-10 ×21, DS-DEF-3 ×7, DS-DEF-4 structure ×4, DS-DEF-7 ×5, DS-DEF-6 ×21 | the band's own word; never an institution | `defenseScoreBands.js:38-39`; `defenseGenerator.js:515-521`; `safetyProfile.js:56-62`, `:74`, `:226-314`; `defenseDisplay.js:25-41`, `:183-195`, `:219-259`; `causalState.js:422-449` |

#### 2.1-R THE DEFENSE DESK — REFUSED (assignments the refuter REFUTED, with the evidence)

| assignment tried (survey) | refuter's verdict | evidence |
|---|---|---|
| "the wall family has NO organ, NO power and NO role layer anywhere in the engine" | REFUTED | (i) `threatDefensePolicy.js:44-45` files `garrison` and `barracks` under FORTIFICATION — a class where a force word is a wall word; (ii) `holderTable.js:188` puts `walls` under the MUSTER kind, whose holder is the Citizen militia's roll (`:281-284`); (iii) the power part holds (`corruption.js:630` omits wall words) |
| `hasWatch` "consumed by safetyProfile, never by a DS-DEF pool" | REFUTED | `defenseGenerator.js:177-178`: `hasAnyDefense = … \|\| inst.hasWatch …` gates `economicGates.military` (`:189-191`, `:468`) — the read of DS-DEF-11 WALLED-STRAINED and the two `muster + road` pools |
| "the engine's own prose never bakes 'the watch'" | REFUTED | `safetyProfile.js:274` bakes "The adventurers' charter hall handles threats the watch cannot." under `hasCharterHall` with no `hasWatch` guard; `:281`, `:288`, `:297`, `:307` bake `'The watch'` under `hasWatch` |
| DS-DEF-5 arcane pools "read `magicWorks`, not the bucket — a wiring row" | REFUTED | `defenseStateProse.js:1319-1321` `arcaneDefensePoolKey(forces, magicWorks)` returns on `forces.magicDef.present`; the CENSUS under-reports the read (M-12) |
| `NO organized force at all` reads "the same four" buckets | REFUTED | `defenseStateProse.js:1273` consults SIX (garrison · militia · watch · mercenary · charter · magicDef); the face is licensed to say no contracted, chartered or arcane body stands |
| O-2 "a pay/upkeep read does not reach the watch bucket" (as an engine fact) | REFUTED — the TABLE half holds | the gate's producer counts `hasWatch` (`defenseGenerator.js:177-178`); `fieldSynonyms.js:51` names the gate "the town's pay for its watch"; the `hasWatch` list (`priorityHelpers.js:48`) equals the watch bucket's keywords (`:95-97`). The read reaches the bucket; the table declines to name it |
| O-12 "four bucket keywords match no shipped row" | REFUTED as counted | `inner citadel`, `professional guard`, `hired muscle` match nothing; `massive walls` matches `Massive walls and fortifications`; `adventurers' guild hall` matches nothing but `Multiple adventurers' guilds` matches through a sibling keyword — three, not four |
| "a law expensive at the ROLE layer costs this desk nothing" | REFUTED | A-8: the desk's only power slot `{seat}` renders `Town Mayor` / `Elected Reeve` (`rulingStructure.js:284`, `:452`) — a role word reaches five DS-DEF-4 pools |
| §0 "`--section` handling does not exist at this tip" | REFUTED (the derived boundary `DS-DEF-*` holds anyway) | `scripts/prose-wave-gate.mjs:61`, `:65`, `:111-112`, `:343-378` — the file is grep-binary; `grep -a` finds it |
| "the guard · the garrison" as the always-safe body words (the guard = the garrison bucket, ADDENDUM 11) | CONDITIONAL — refused as garrison-anchored | `safetyProfile.js:273`, `:276`, `:282`, `:336` use "the guard" as a class word over WHICHEVER law body exists; the only garrison keyword carrying it (`professional guard`, `:89`) has no row; ADDENDUM 13 item 5 already struck "the guard" from the always-safe list |
| law item 5 as drafted ("until cured a face never uses 'the watch' for a pay-gate read") | REFUTED as the rule stands | follows the holder table against the gate's producer (M-1) and the estate's synonym instrument; on a walled Town-watch-only town (no Barracks 0.7 × no Citizen militia 0.4 ≈ a quarter of walled towns) it bars the only true body word and licenses "the muster" for a roll the town does not keep (M-3). The refuter's safe form until the `{watchname}` fill lands: the person-plural class word or the gate's own nouns (`wages · pay · purse`), never either institution name |
| "the desk names no role anywhere" (as a rendered fact) | CONDITIONAL — holds over the CORPUS only | `{seat}` injects role words at render (A-8) |
| "INTERESTED reaches the four organs" only | CONDITIONAL | `holderTable.js:707-708`: `interested` is ALSO true on `corrupt` / `corruptImpairment` for ANY kind — a muster row is interested where its Citizen militia carries a corruption impairment |
| 7.1 the `watch PRESENT` citation "licensed only while the town is not captured" | CONDITIONAL — stricter than stated | `capturedRulingStructure` counts `adversarial` and `equilibrium` as captured (`holderTable.js:141`): the citing player face FAILS from rung 1, not from `corrupted`; and fails where the holder carries any corruption impairment (`:707-708`) |

### 2.2 THE GENERAL DESK (DS-GEN-* · DS-REL-* · DS-POP-* · DS-HK-*; 23 blocks · 193 census rows · 119 RESOLVED / 74 WIRING-UNRESOLVED)

Sources: `general.referent.survey.md` (Opus) · `general.referent.refute.md` (Fable). Denominators the refuter re-measured exactly: holder kinds `(none) 162 · treasury 7 · elders 5 · road 5 · market + toll-bar 4 · market 4 · toll-bar 3 · office 3`; STATE-ORGAN rows 10 (7 treasury LICENSED, 3 office OFFICE); covert 0 of 193; standing reads NONE (no pool reads a capture state, an impairment or a patron); the one person-naming read `link.npcConnections`; word counts over the 496 variant-bearing lines: market 24 · rolls/roll 26 · hall 15 + seat 7 · books/book 15 · watch 13 · walls 11 · elder 0 (all 56 hits are the `[elder]` angle tag). **Not one pool on this desk resolves the `watch`, `muster`, `court`, `parish`, `census` or `tradition` kind.**

| noun | layers (HOLDS / CONDITIONAL with condition) | reads per layer (pools) | safe class word | source |
|---|---|---|---|---|
| the watch | BODY: `hasWatch` (three spellings); a substring member of `hasMilitaryInst`; the watch bucket; scores military +7 and internal +18. HOLDER: watch kind (STATE ORGAN) — CONDITIONAL: the record resolves only where a live roster row carries `Crime reporting` (`Professional city watch`, on: true, p 0.8) or `Crime response` (`Town watch`, on: false, p 0.7) or `Missing persons` (on: false, p 0.4) — a per-town ROLL even where a watch row stands. POWER-eligible: SECURITY body. The display label is TIER-derived (`'local watch'` below town whether or not any watch row exists) | NONE on this desk. The one adjacent read is `head` via `SAFETY_POOL_OF` (DS-GEN-3 safetyLabel ×3): the FIELD token `safetyProfile` maps to the watch kind but the pool's grain is the key-function argument, so the census resolves nothing (the cheapest wiring row on the desk: F-12) | presence only, never a duty: the watch kind's `dutyNamed` is **0** — "what the watch is FOR" states an unrecorded duty even where the body resolves | `priorityHelpers.js:45`, `:48`; `defenseInstitutionBuckets.js:95-97`; `defenseGenerator.js:163`, `:235`; `holderTable.js:212`, `:318-322`; `institutionServices.js:1208-1209`, `:1325`; `corruption.js:630`; `safetyProfile.js:30-35` |
| the garrison | BODY: `hasGarrison`; garrison bucket; scores +28 / +20 / +15. HOLDER: muster kind (the `garrison` token). POWER-eligible: SECURITY body. Shipped rows `Garrison`, `Multiple garrisons`, `Barracks` | none on this desk (DS-GEN-3's military band is an AGGREGATE) | the garrison (body) · the muster (the roll, CONDITIONAL as below) | `priorityHelpers.js:46`; `defenseInstitutionBuckets.js:88-91`; `defenseGenerator.js:161`, `:201`, `:234`; `holderTable.js:189`; `institutionServices.js:185`, `:1127`, `:1555` |
| the militia · the muster | BODY: `hasMilitia`; militia bucket. HOLDER: muster kind — CONDITIONAL: roster-backed by ONE institution (`Citizen militia`) whose `Muster training` is `on: false, p: 0.5`, so even the one carrier instantiates the record by roll | none | the muster (the chair's class word); a named militia only where the roster row is the Citizen militia | `priorityHelpers.js:47`; `defenseInstitutionBuckets.js:92-94`; `holderTable.js:280-287`; `institutionServices.js:835-837` |
| the mercenary company · free company | BODY: `hasMercenary` (SIX keywords) / `hasFreeCompany`; the mercenary bucket (THREE keywords) — CONDITIONAL: the boolean and the bucket disagree on three members; `Hireling hall` is both mercenary and charter. HOLDER: muster kind | none | hired men | `priorityHelpers.js:49-51`; `defenseInstitutionBuckets.js:98-100`; `holderTable.js:191` |
| the charter hall | BODY: `hasCharterHall`; charter bucket. HOLDER: muster kind | none | the charter hall | `priorityHelpers.js:51`; `defenseInstitutionBuckets.js:101-104`; `holderTable.js:192` |
| the walls · the gates | BODY: `hasWalls`, `hasGates` (fires on `town walls · city walls · massive walls · palisade` with NO gate row); walls bucket. HOLDER: `walls` → muster; `Gates (if walled)` is separately a TOLL-BAR roster row | none (DS-GEN-17's `readings.inst` resolves no holder) | the works | `priorityHelpers.js:52-53`; `defenseInstitutionBuckets.js:84-87`; `holderTable.js:188`, `:308`; `institutionServices.js:1551` |
| the court | BODY: `hasCourtSystem` = `courthouse · court buildings · democratic assembly · city hall · town hall`. HOLDER: court kind (services `Criminal trials · Civil disputes · Notary services …`, `dutyNamed: 0`); its FIELD tokens are the SEAT's (`govMultiplier · governanceFractured · breakdown · blocs · stability · recentConflict · termLines · fraying · yearsRemaining`). POWER-eligible: STATE ORGAN; SECURITY body | none (DS-GEN-17 ADMINISTERED reads the flag pair) | the seat where the row is a hall; the court only where the row is a courthouse | `priorityHelpers.js:55`; `defenseGenerator.js:232`; `holderTable.js:115`, `:215-223`, `:325-329`; `corruption.js:630` |
| the prison · the gaol | BODY: `hasPrison` (`prison · stocks · large prison · massive prison`); no holder kind. `City hall` carries `Small prison/stocks` and `Holding cells` as SERVICE names | DS-GEN-17 ADMINISTERED (`hasCourtSystem && hasPrison`) | a place of confinement | `priorityHelpers.js:54`; `defenseGenerator.js:233`; `institutionServices.js:1634-1635` |
| the magistrate · the constable | POWER-eligible only (members of `SECURITY_INSTITUTION_RE`) — ZERO shipped rows; unreachable on this desk | none | — | `corruption.js:630`; roster sweep (263 keys): 0 |
| the hall · the council · the seat | BODY + HOLDER (several kinds at once) + POWER — CONDITIONAL per row: `Town council` = ELDERS (`Record of custom`) + TOLL-BAR (`Market charter and tolls`); `Town hall` = TREASURY (`Tax payment`) + OFFICE (`Record filing`); `City administration` = TREASURY + OFFICE (`Public records`); `City hall` = OFFICE ONLY (`Public record access`; NOT a treasury row — the survey's row text is wrong for it) and a `hasCourtSystem` member. The slot `{seat}` is "the governing body by its own generated name — never a baked noun" | none of the general blocks declares `{seat}` | the seat, filled from `{seat}`; never "the hall" / "the council" baked | `institutionServices.js:32-37`, `:39-45`, `:1623-1636`; `holderTable.js:277`, `:308`, `:336`, `:361`; `priorityHelpers.js:55`; annex `:194`, `:1816` |
| the elders | HOLDER: elders kind (`Record of custom`; roster `Household elder · Village elder · Village headman · Town council`). BODY/ROLE: the same rows — CONDITIONAL: `hasInst('elder')` is a substring and also reaches `Elder Grove Council` (a druidic body) and `Informal elder consensus`, neither an elders holder | DS-GEN-9 recency framing ×5 (`marker.yearsAgo`, LICENSED) — the desk's one order-adjacent licensed holder, and it never uses the word | the elders (the kind) · the custom (the record) | `institutionServices.js:14`, `:17`, `:20`, `:24`, `:26`, `:30`, `:37`, `:497`, `:1448`; `holderTable.js:332-336`; `defenseGenerator.js:89-90`, `:151` |
| the reeve · the headman · the steward · the appointee | BODY (institution rows) that are ALSO holders: `Village reeve` → TREASURY (`Tax collection`, on: false, p 0.7); `Village headman` → ELDERS + TREASURY; `Lord's appointee` → TREASURY; `Lord's steward` in no holder cite — a role word here is a BODY word; there is no person behind it (CONDITIONAL as a general law: `officesOf` also mints role words off `npcs[].role/.title`) | DS-GEN-17 (indirectly, the booleans match the rows) | the body word itself | `institutionServices.js:20-24`, `:1030-1032`, `:1035`, `:1346-1349`; `holderTable.js:277`, `:336`; `institutionTable.js:377-396` |
| the treasury | HOLDER: treasury kind (STATE ORGAN); roster `Village headman · City administration · Weekly market · City-state government · Lord's appointee · Village reeve · Town hall` | DS-GEN-3 viable ×2; DS-GEN-11 ×5 (LICENSED, "a citation of this holder is licensed where the provenance budget allows") — CONDITIONAL flip to INTERESTED only where the town instantiates a treasury holder AND (`criminalCaptureState !== 'none'` or any faction's `captureState`) | the purse · the treasury | `holderTable.js:115`, `:166-169`, `:273-277`, `:724-756` |
| the office | HOLDER: office kind (the compiling record); standing ALWAYS `OFFICE`, never LICENSED — a citation "would be citing the speaker"; its `Public records` service registers "deeds, wills, births, and freemen" (a population register the census kind does not hold). STATE ORGAN — and an OFFICE row can NEVER flip to INTERESTED (`sourceOfForTown` returns early) | DS-GEN-7 structuralViolations/Suggestions ×2; DS-REL-2 prominentRelationship ×1 | the record — no citation | `holderTable.js:84`, `:115`, `:357-363`, `:489`, `:563-575`, `:724-729`; `institutionServices.js:45` |
| the toll-bar | HOLDER: toll-bar kind (`Toll collection · Customs brokerage · Market charter and tolls`; roster `Gates (if walled) · Major Port · Town council`) | DS-GEN-6 crossroads/isolated/road ×3; DS-GEN-13 ×4 (`market + toll-bar`) | the toll | `holderTable.js:183-185`, `:304-308`; `institutionServices.js:199`, `:1551` |
| the census | HOLDER: census kind (`Citizen registration · Noble registration`; roster `Democratic assembly · Royal seat`) — CONDITIONAL: both services are `on: false, p: 0.5` (a roll on the two carriers); `Democratic assembly` is ALSO a `hasCourtSystem` member | NO general-desk pool reads it — though DS-POP-1/-2/-3 are the population blocks; `HOLDER_SOURCES` carries no `population · migration · births · deaths` token at all | the roll (see the §8.3 question in §6) | `holderTable.js:164-240`, `:290-294`; `institutionServices.js:891-895`, `:1231-1233`; `priorityHelpers.js:55` |
| the tradition | HOLDER: tradition kind — `rosterBacked: false`, "THE ONE KIND WITH NO INSTITUTION ANYWHERE IN THE SHIPPED ROSTER" | none | nothing citable | `holderTable.js:339-346` |
| the road (holder) | HOLDER: road kind (`Road register` under `Listening post`, on: true; `Way-bill registration` under `Waystation`, on: false, p 0.6); tokens `terrainType`, `monsterThreat` | DS-GEN-12 ×5 (LICENSED) | the way | `holderTable.js:233-234`, `:350-354`; `institutionServices.js:405-408`, `:1652-1653` |
| the market · the stalls · the exchange | BODY: `MARKET_NAME` (`market · bazaar · exchange · shambles · stalls`, word-bounded) over `liveInstitutions` — CONDITIONAL on a LICIT member: the class ALSO admits `Black market`, `Black market bazaar`, `Whisper market`, `Slave market`, `Fish market`; `hasMarket` (substring) does the same. HOLDER: market kind (`Weekly market · Public auctions`; roster `Market square`); `Weekly market` is ALSO a treasury roster row | DS-GEN-13 MARKET-OPEN/NARROW/ENTREPOT/NO-MARKET ×4 (ruin-filtered); DS-GEN-18 ×4 (market kind, LICENSED) | the exchange · the stalls (the market kind's own noun); "keeps a market" only where the resolved row is licit | `generalStateProse.js:1109`, `:1138-1152`; `institutionRoster.js:30`, `:53`; `priorityHelpers.js:56`; `holderTable.js:277`, `:311-315`; `institutionServices.js:524-526`, `:1064-1065` |
| the church · the temple · the parish | BODY: `hasChurch` (eight house kinds; `Church/Temple` is ONE row carrying both words). HOLDER: parish kind (`Register of the dead · Central register · Records`; roster `Parish burial grounds · Cemetery network · Parish church`). ROLE: `Priest (resident)` is an INSTITUTION ROW. POWER-name overlap: a governing faction NAME containing `temple`/`clergy`/`order` is read as non-secular | none resolves the parish on this desk; DS-GEN-7 `temple economy under a secular seat` reads a `note.type\|tab` key | a house of the faith (body) · the parish (the record) | `priorityHelpers.js:65-67`; `holderTable.js:226-227`, `:297-301`; `institutionServices.js:1161`, `:1194`, `:1411-1413`; `narrativeGenerator.js:625-627` |
| the granary · the storehouse | BODY: `hasGranary` (substring `granar`); census civic classes split `store` (the STOCK) from `storehouse` (the BUILDING) | DS-GEN-17 PROVISIONED (`hasGranary \|\| hasHospital`, a DISJUNCTION) | the building or what is in it — never both in one word | `priorityHelpers.js:63`; `wiringCensus.js:1309`, `:1322` |
| the hospital · the healer | BODY: `hasHospital` (`hospital · monastery · healer · friary`; a monastery ALSO sets `hasChurch`) | DS-GEN-17 PROVISIONED | a house that takes in the sick | `priorityHelpers.js:64`; `defenseGenerator.js:204`; `wiringCensus.js:1315` |
| the port · the harbour · the navy | BODY: `hasPort` (`PORT_INFRA_RE`), `hasNavy` (`navy · major port`); `Navy` IS a shipped row of `institutionServices.js` (see X-D1). HOLDER: `Major Port` is a toll-bar row | DS-GEN-17 GARRISONED's second disjunct | the water approach | `priorityHelpers.js:32`, `:61-62`; `holderTable.js:308`; `institutionServices.js:194-199` |
| the guild · the trade | BODY: `hasGuild` (bare substring — fires on `Thieves' guild`, `Mages' guild`); `hasMerchantGuild`, `hasMagesGuild`, `hasThievesGuild` | none | the trade · the company | `priorityHelpers.js:57-58`, `:69`, `:73`; `wiringCensus.js:1316` |
| the magic house | BODY: `hasMagicInst`, `hasMagesGuild`, `hasWizardTower`, `hasAlchemist`; the `magicDef` bucket. ROLE: "a practitioner" | DS-GEN-17 LETTERED (`hasMagicInst`) | a practitioner (role) · the house (body) | `priorityHelpers.js:68-71`; `defenseInstitutionBuckets.js:105-108` |
| the criminal house | BODY: `hasCriminalInst`, `hasThievesGuild`, `hasBlackMarket`, `hasSmuggling`, `hasGangInfra`; `hasCrimOrg = hasInst('fence','smuggl','gang','outlaw')` feeds the community internal base. POWER: the criminal faction (`category === 'criminal'` OR a NAME containing `thiev`) drives the capture ladder | none | the underworld — and only through a faction NAME where one is recorded | `priorityHelpers.js:72-76`; `defenseGenerator.js:154`; `factionDynamics.js:220-232` |
| `{institution}` (a workshop) | BODY: `craftInstitutionFill` from `activeChains[].processingInstitutions[]` — the values are CATEGORY labels (`Merchant guilds (3-8)`, `Glassmakers`, `City walls and gates`), not roster names | DS-GEN-18 | the workshop · the craft | `generalStateProse.js:1259`; annex `:172` |
| `{steading}` · `{ruin}` | BODY (a satellite place, a fallen one); no holder | DS-GEN-8 ×4 | the named place itself | `generalStateProse.js:1417`, `:1442` |
| the watchtower (missed noun) | BODY: a shipped row; a `watch` for `hasMilitaryInst` (substring) — resolves DS-GEN-17 GARRISONED alone; a `watch` for `SECURITY_INSTITUTION_RE`; its own glyph. NOT `hasWatch`, NOT the watch bucket, NOT the watch kind (services `Signal fire · Approaching threat warning · Night watch`) | DS-GEN-17 GARRISONED | — (a body the watch word does not name) | `institutionServices.js:1561-1565`; `priorityHelpers.js:45`; `corruption.js:630`; `glyphAssign.js:99` |
| the assembly · the royal seat (missed) | `Democratic assembly`: a census carrier AND a `hasCourtSystem` member (DS-GEN-17 ADMINISTERED fires on an assembly + stocks); `Royal seat`: a census carrier (`Noble registration`, rolled) | DS-GEN-17 | the seat (`{seat}` is the typed slot) | `holderTable.js:294`; `priorityHelpers.js:55`; `institutionServices.js:891`, `:1231`; `hookEscalation.js:421` |
| the mayor · the lord · the sexton · the warden (missed) | ROLE words inside BODY rows and service descs (`Mayor and council` — NEITHER an elders nor a toll-bar holder; `Lord's appointee`; the sexton in `Register of the dead`'s desc; `Warden's Lodge`); none is an NPC record on this desk | none | — | `institutionServices.js:491`, `:1030`, `:1035`, `:1069`, `:1413` |
| `{faction}` · `{faction2}` | POWER, typed `proper` — a NAMED political actor party to a record (`currentTensions[].factions[]`, `conflicts[].parties[0..1]`) | DS-GEN-1, DS-GEN-2, DS-GEN-7 (NAMED BUT NEVER FILLED on DS-GEN-7 per the card), DS-REL-1, DS-HK-1 | the faction's name | annex `:171`, `:198`, `:5181`, `:5264` |
| `{govFaction}` | POWER, typed — the governing faction NAMED ON THE COHERENCE NOTE | DS-GEN-7 only | — | annex `:201`; `narrativeGenerator.js:633` (the survey's `:632` is one line off) |
| `{governing}` | POWER, typed — `powerStructure.governingName` or the literal fallback `'the governing faction'` | DS-HK-1 `legitimacy_crisis` | the governing faction (the engine's own fallback string) | annex `:202`; `hookEscalation.js:511`; `rulingStructure.js:787` |
| `{controller}` | NOT a power — filled from `chain.dependency.institution \|\| processingInstitutions[0] \|\| 'unattributed'` (a BODY name or a literal), though §0c-2 glosses it "the holder of a hook's controlling interest" and the engine's OWN stages render it as a rent-taking AGENT | DS-HK-1 `bread_riot`, `smuggling_rise` | — (wiring: the gloss or the fill is wrong) | annex `:203`; `hookEscalation.js:394`, `:405`, `:481`, `:493`; `supplyChainState.js:473-487` (`return 'unattributed'` `:486`) |
| `powerStructure.government` / `.governingName` · `criminalCaptureState` · per-faction `captureState` · a corruption impairment · a brokerage PATRON | POWER (typed) — read by NO general-desk pool; per-faction `captureState` CONDITIONAL: three thresholds of "captured" (`capturedRulingStructure` counts `adversarial`; the birth stamp writes only at `equilibrium`+ and only on the governing entry; `standingOf`'s world capture counts `corrupted`/`capture` only); a patron is covert iff `house.legality === 'illegal'`, whatever its source | none | — | `rulingStructure.js:755`, `:762-766`, `:787`, `:792`, `:797`; `holderTable.js:133-142`, `:211`, `:659`; `factionDynamics.js:220-232`; `corruption.js:630`, `:648-650`, `:674-688`; `brokeragePatronage.js:60`, `:295`, `:319-325` |
| `{npc}` | PERSON — "a cast person named on a member receipt; never minted, no fate resolved"; filled by `localNpcFill` from `npcConnections[].primaryNPCName`, THIS TOWN's end only | DS-REL-1 cross-settlement NPC contacts — the desk's ONE person-naming read (card: `link.npcConnections` not-produced) | — (the law's PERSON row vs this pool is an OWED question, §6) | annex `:173`, `:5560`; `generalStateProse.js:1502`, `:1526-1541` |
| `link` + `localRelationshipRole` (`patron` / `client`) | ROLE — the role THIS TOWN holds in an asymmetric tie, withheld where absent; `overlord`/`vassal` are real values with NO pool; "patron" is ALSO a brokerage POWER and a member of the census civic class `temple` (which is why the pool classes as a temple pool) | DS-REL-1 patron / client | the tie's own word | `generalStateProse.js:1477-1494`; `canonicalRelationship.js:54-57`, `:143`, `:302-307`; `brokeragePatronage.js:60`; `wiringCensus.js:1312` |
| DS-GEN-3's bands (military · internal · monster · economic · magical axes; readiness label; safety head; prosperity; foodSecurity) | AGGREGATE — a number or word computed OVER bodies, never a body (40 of DS-GEN-3's 42 rows; readiness averages FOUR axes where `magicExists` is false, five otherwise) | DS-GEN-3 ×40 | the band's own word; no noun of any layer | `defenseGenerator.js:159-191`, `:195-228`, `:231-254`, `:257-307`, `:503-522`; `generalStateProse.js:240-250`, `:320-324` |

#### 2.2-R THE GENERAL DESK — REFUSED

| assignment tried (survey) | refuter's verdict | evidence |
|---|---|---|
| "the guard" as the always-safe BODY word for the watch/garrison | REFUTED as a body-grounded word (CONDITIONAL as a class word where `hasGarrison` resolves) | roster sweep: `guard` 0 rows; `professional guard` (`defenseInstitutionBuckets.js:89`, `priorityHelpers.js:46`) is a keyword with no member; `'guard'` is read as a FACTION-NAME substring naming the enforcement POWER (`factionDynamics.js:232`) |
| the magistrate · the constable · **the barracks** "exist ONLY as members of `SECURITY_INSTITUTION_RE`; unreachable" | REFUTED for the barracks | `Barracks` is a shipped row (`institutionServices.js:1555`), a garrison-bucket keyword (`:89`), a `hasGarrison` member (`:46`) and a glyph member (`glyphAssign.js:98`) |
| "the market body" (MARKET_NAME resolves a market) | REFUTED as stated | the word-bounded class contains `Black market`, `Black market bazaar`, `Whisper market`, `Slave market`, `Fish market` (`generalStateProse.js:1109`; `priorityHelpers.js:56`): a town whose only market-class row is a Black market resolves MARKET-OPEN and DS-GEN-13 says it "keeps a market" |
| "For those ten [STATE-ORGAN] pools, `sourceOfForTown` will return INTERESTED on any town whose ladder is not `none` … roughly a third of towns" | REFUTED as stated; CONDITIONAL as corrected | (a) the three OFFICE rows can NEVER flip (`holderTable.js:724-729` returns early on a non-LICENSED base); (b) the seven treasury rows flip only where the town instantiates a treasury holder (`holdersOf` `:590-607`; else `absent: ['holder …']` `:746-756`); (c) the faction check is over ANY faction entry (`:133-137`) |
| "A ROLE WORD IN THIS ENGINE IS A BODY ROW, NOT A PERSON" (as a general law) | CONDITIONAL — holds for the seven named rows on the seven named reads only | `hasInst` is a substring (`defenseGenerator.js:89-90`) reaching `Elder Grove Council` and `Informal elder consensus`; `officesOf` mints role words from `npcs[].role/.title` (`institutionTable.js:377-396`, `COLUMN_SOURCES.office` `read: true` `:124-127`) |
| "no typed NPC→institution edge exists" (as "the engine never joins a person to an institution") | CONDITIONAL — true of the holder-ROLE column only | the NPC schema declares `institutionId` (`settlement.schema.js:444`, `:953`); `corruption.js:648-650`, `:685-688` name-match a corrupt NPC's home to a security institution; `officesOf` reads role words off persons |
| "the frozen covert list is `impairment.covert`, `mobilization.covert`, `blocs.covert`" | CONDITIONAL — SIX entries | `COVERT_SOURCES` `wiringCensus.js:1252-1259` also lists `compromisedSecurityInstitutions`, `npc.corrupt`, `corruptNpc` |
| readiness = "the AVERAGE of the five axes" | CONDITIONAL | four axes where `magicExists` is false (`defenseGenerator.js:503-505`) |
| the muster kind "= walls · garrison · militia · mercenary · charter plus `force`, `magicDependency`, `economicGates`" | CONDITIONAL — TWELVE tokens | `holderTable.js:188-199` also carries `besiegedBy`, `besiegingTargets`, `ticksToDeploy`, `stretchedThin` |
| "all four [purses] published side by side on `economicGates`" | CONDITIONAL | `military` is published only with `hasAnyDefense`, `internal` only with `hasLawInfra` (`defenseGenerator.js:462-472`) |
| `narrativeGenerator.js:632` for the `{govFaction}` note string | cite REFUTED, claim HOLDS | `:633` |

### 2.3 THE ECONOMY DESK (DS-ECO-1 … DS-ECO-12 · DS-SUP-1 … DS-SUP-3; 15 blocks · 105 census rows · 55 RESOLVED / 50 WIRING-UNRESOLVED)

Sources: `economy.referent.survey.md` (Opus) · `economy.referent.refute.md` (Fable). Denominators the refuter reproduced: `stateOrgan` on 8 rows (DS-ECO-6 ×3 watch, DS-ECO-12 ×5 treasury); covert 0 of 105; five blocks with no mount site (DS-ECO-4, -5, -7, DS-SUP-1, DS-SUP-2); 31 distinct reads, none matching `/npc/`; `fieldSynonyms` (the engine's ratified organ-noun table `HOLDER_KIND_NOUN_ROWS`) for 16 of 31. **The refuter's correction to the layer column: the honest values on this desk are FOUR — BODY (only the two `{institution}` fills) · HOLDER (market ×14, toll-bar ×2, watch ×1, treasury ×4: a record's keeper, citable by its ratified nouns, NOT under a power) · ORGAN-UNDER-POWER (the watch and treasury rows ONLY where the town is captured — and `standingOf` has no product caller) · NONE (the other 21 reads).**

| noun | layers (HOLDS / CONDITIONAL with condition) | reads per layer (pools) | safe class word | source |
|---|---|---|---|---|
| `{institution}` on DS-ECO-11 (exploitation lens) | BODY only — a chain house from `leading.row.processingInstitutions[0]` (16 distinct: Alchemist shop · Apothecary · Butcher · Caravanserai · Fishmonger · Glassblower · Market · Mill · Mine · Potter · Salt works · Sawmill · Stone quarry · Tannery · Vintner · Weavers' guild) — CONDITIONAL on `leadingExploitation` returning a row and `properFill` accepting it. Overlaps: `Alchemist shop` is a `magicDef` DEFENCE-bucket keyword; `Weavers' guild` fires `hasGuild` (which also fires on `Thieves' guild`); `Market` collides with the market KIND and civic class | DS-ECO-11 EXPLOITATION (the census reads `leading.bucket`, `leading.row`) | the workings · the house | `economyStateProse.js:746-767`, `:320-332`, `:991`; `resourceGenerator.js:175-177`; `resourceChains.js` (mineralHot); `defenseInstitutionBuckets.js:110`; `priorityHelpers.js:57`; `holderTable.js:311-317`; `wiringCensus.js:1310` |
| `{institution}` on DS-SUP-3 (impaired lens) | BODY + HOLDER + POWER-ELIGIBLE, CONDITIONAL ON THE MEMBER: any of the 65 `TRADE_DEPENDENCY_NEEDS` keys with a rendered service row that is `impaired` (severity `critical` \|\| siege \|\| isolated). `Town watch` / `Professional city watch` = watch bucket + the watch KIND's two roster holders + `SECURITY_INSTITUTION_RE` (all three); `Garrison` / `Barracks` / `Multiple garrisons` = garrison bucket + regex, holder of NO kind; `Citizen militia` = militia bucket + muster HOLDER; the four wall rows = walls bucket only; `Free company hall` / `Mercenary quarter` = mercenary bucket only. The five force houses are filed under `employment`, a category `EXPECTED_SERVICES_BY_TIER` never expects — so the ABSENCES lens can never name a force body and the IMPAIRED lens can | DS-SUP-3 `A CATEGORY PRESENT BUT ITS CHAIN IMPAIRED` (`readings.impairedInstitution`, not-produced per the card; SOURCE-UNRESOLVED; player-audible) | **NONE** — the slot renders a proper name and the writer cannot substitute (the wrong-layer noun arrives through the fill: OWED Q4) | `economicData.js:9`ff; `economicState.js:712-720`; `tabHelpers.js:38-45`; `serviceCategoryTables.js:40-46` (`src/generators/services/`); `ServicesTab.jsx:88`, `:95-100` (`src/components/new/tabs/`); `economyDeskRead.js:109`; `economyStateProse.js:999`; `servicesDisplay.js:15-21`; `holderTable.js:281-288`, `:318-324` |
| `{faction}` | POWER, typed `proper` — DELIBERATELY UNFILLED ("would name a revenue LINE where the sentence names a HOUSE: the label trap, one layer up"); anchored liveness drops the variant. CONDITIONAL, sharper: the CAPTOR is nowhere typed — `capturedRulingStructure().faction` is a ladder STRING; the governing entry's `captureState` is the settlement ladder MIRRORED; the criminal power is identified only by `category === 'criminal'` or a name containing `thiev` (O-R3) | named by 3 faces (DS-ECO-6 ≥30 v3; DS-ECO-12 criminal-present v3; DS-SUP-2 CAPTURED) — never rendered | the part of it nobody writes down (what the corpus already uses) | `economyStateProse.js:910-921`; `stateProseKernel.js:161-166`; `holderTable.js:133-137`; `rulingStructure.js:457`, `:764-766`; `factionDynamics.js:221` |
| `{access}` | not institution-class (`road · river · port · crossroads · pass`) — but `road` is ALSO the ROAD holder kind's name (resolving to `Listening post` / `Waystation`) and the `road` civic class; filled from `eco.tradeAccess`, an UNMAPPED alias of the toll-bar-kind field `tradeRouteAccess` (O-R1) | DS-ECO-1 ×5 (RESOLVED, `source.kind ''`) | the approach | `economyStateProse.js:173-179`, `:888`; `economicState.js:870`; `holderTable.js:182`, `:233-234`, `:350-356`; `wiringCensus.js:1313` |
| the granary | BODY: `hasGranary`; the chain house `Town granary` / `Granary`; the `storehouse` class — vs a READING that is not a body (the STOCK band; three capacity rows keyed on the name). A `granary` holder row was WITHDRAWN. The BUILDING is never read by the desk | DS-ECO-2's reads are `granary.band` etc — the STOCK (layer NONE) | the stores · what the town holds back (the `store` class) | `dossierViewModel.js:336`; `priorityHelpers.js:63`; `foodStockpile.js:185-195`; `wiringCensus.js:1309`, `:1322`; `holderTable.js:157-159` |
| the market | HOLDER: market kind (`Market square` by `Weekly market · Public auctions`) — NOT a state organ. BODY: `hasMarket` (incl. bazaar/fair/exchange; fires on `Black market`); the chain house `Market`; the `market` civic class. `Weekly market` the INSTITUTION is a TREASURY holder (`Tax collection`) while `Weekly market` the SERVICE licenses the market kind (O-R5) | DS-ECO-10 ×6, DS-ECO-12 ×5 + `analysis.*` ×3 (market kind, LICENSED); DS-ECO-3 / DS-ECO-4 unwired | the trade · what the town trades; the kind's ratified nouns **book · books · stall** | `holderTable.js:170-180`, `:272-279`, `:311-317`; `priorityHelpers.js:56`; `wiringCensus.js:1310`; `institutionServices.js:524-526`, `:1064-1065`; `fieldSynonyms.js:83` |
| its mill | BODY (grain `Mill`, `Fulling mill`, `Sawmill`; the terrain institution `Mill`); `river_mills` is a typed `infrastructure` RESOURCE; `has('mill')` is a SUBSTRING that raises food capacity ×1.25 | none — DS-ECO-11 River reads `text(terrainKey)` | the workings | `foodStockpile.js:194`; `resourceSemantics.js:71-72`; `geographyData.js:170`; `wiringCensus.js:1316` |
| the guilds · a guild | BODY (`hasGuild` on the bare token) AND POWER (the typed FACTIONS `Craft Guilds`, `Merchant Guilds`, `Thieves' Guild`) at once | none — DS-ECO-6 reads `blackMarketCapture` only | the trades · the craft houses; a POWER only through `{faction}` | `priorityHelpers.js:57-58`; `rulingStructure.js:506`, `:647`, `:671`; `wiringCensus.js:1316` |
| the hall · the halls | BODY (the `hall` civic class) + HOLDER (court kind's `Courthouse`; `Town hall` = treasury + office) + POWER (the seat, in "dictate to the hall") | DS-ECO-12 v3's read is `eco.incomeSources` → TREASURY, a different organ | the town's own reckoning (treasury); the ruling structure (the seat) | `wiringCensus.js:1314`; `holderTable.js:272-279`, `:325-331`, `:357-364` |
| the gates · the gate returns | BODY (the `wall` civic class) + HOLDER (`Gates (if walled)` is a toll-bar holder); `gate control` is a `Town watch` SERVICE | DS-ECO-3 `SHORTAGE × trade-dependent` has NO reads (WIRING-UNRESOLVED) | the toll-bar's ratified nouns **book · books · toll** — *returns* is not among them | `holderTable.js:304-310`; `wiringCensus.js:1302`; `economicData.js` (`'Town watch'.svcs`) |
| the walls · a wall | BODY (walls bucket; `wall` civic class) and a MUSTER field token | no economy read is a walls read | inside the town (a place phrase) | `defenseInstitutionBuckets.js:84-87`; `holderTable.js:188`; `wiringCensus.js:1302` |
| its watch | BODY (bucket; `hasWatch`; `hasMilitaryInst` on the bare token; the `force` civic class) + HOLDER (watch kind) + STATE ORGAN — the exact three-way word, on a terrain read whose ratified noun set is EMPTY | DS-ECO-11 TERRAIN: Forest v2 (`text(terrainKey)`, `source.kind ''`; mounted, player-audible) | whoever keeps the peace here; the class word for the paid military is the muster | `defenseInstitutionBuckets.js:95-97`; `holderTable.js:115`, `:202-212`; `priorityHelpers.js:45`, `:48`; `wiringCensus.js:1303`; annex `:1521` |
| the warehouses · the workshops · the yard · the stalls · the house · the quarter · the workings · the stores | BODY (`storehouse`, `craft`, `care` civic classes; chain houses) — `stall` is the MARKET kind's own ratified noun (HOLDER); `healing` is a CATEGORY key; `the workings` has no engine row (deliberately); `the stores` is the `store` class (a stock word) | DS-ECO-12 v3 / DS-ECO-10 (`stall`, market kind); DS-SUP-3 (`readings.notableAbsences` — CATEGORY keys, layer NONE); the rest unwired | the workings · the stores (already the desk's safe generics) | `wiringCensus.js:1309`, `:1315`, `:1316`, `:1322`; `fieldSynonyms.js:83`; `servicesDisplay.js:15-21`; `economyDeskRead.js:108` |
| the books · the accounts · the ledgers · the rolls · the returns · the duties · fees / a licence · the survey · the counts · the purse / the chest (record words) | HOLDER-layer SURFACES against the engine's own ratified table: **books** = treasury · parish · toll-bar · market · road · office; **rolls** = muster · census · watch · court (ON-ORGAN on DS-ECO-6 ≥15 v1, the watch kind's `roll · rolls · watch`); **stall** = market; **toll** = toll-bar; **counts** = census; **purse · chest** = treasury (UNUSED on the desk). NOT ratified for any kind: **accounts** (9 lines), **returns**, **ledgers** (`ledger` WITHDRAWN as a token), **duties** (NOT in `DUTY_SERVICE_KINDS`; the only catalog "Duties" is `Customs house`'s "Duties and tariffs", a house in no `HOLDER_RECORDS` cite — the survey's route REFUTED, the conclusion stands). **survey** = the office kind (a citation is a FINDING, arm A13's OFFICE limb). The `[ledger]` ANGLE names the same nouns as a STANDPOINT — two authorities over three nouns (OWED Q1) | DS-ECO-12 INCOME MIX (treasury) and TRADE PROFILE (market) ON-ORGAN; DS-ECO-6 ≥15 v1 ON-ORGAN; DS-ECO-9 organ right (toll-bar), noun (*returns*) not ratified; everywhere else a record named where the census gives no holder | the kind's ratified nouns and no others (the survey's row 1 for the law: mechanisable today) | `fieldSynonyms.js:42-45`, `:77-90`, `:96-102`, `:115-128`; `holderTable.js:84`, `:157-161`, `:304-310`, `:318-324`, `:357-364`; `institutionTable.js:106`; `institutionServices.js:529-532`; `composedWalker.js:1011`, `:1018-1020`, `:1045-1051`, `:1066-1069`; annex `:120-128` |
| a stranger (×42) · the crier (×5) · a clerk / the clerks (×3) · the men · Merchants · [elder] | ROLE-shaped words with no read behind them: *a stranger* is the `[visitor]` ANGLE's standpoint, not a role (OWED Q2); *the crier* is an authored COPY table (`CRIER_FRAMES`), not an office; *the clerks* the `[ledger]` persona (at `:1080`, `:1122`, `:1649` — the survey's DS-ECO-6 ≥30 v1 location is wrong); *the men* a force-register idiom on ratepayers; *Merchants* a FROZEN component literal (`EconomicsTab.jsx:664`, not `economicDragDesc`), unmarked, player-audible; `[elder]` an ANGLE and separately a holder kind. NONE of ADDENDUM 11's specimen role words appears in the desk's 355 variant lines | none — no economy read touches `settlement.npcs` | — | annex `:122`, `:124`, `:125`, `:1080`, `:1116`, `:1122`, `:1649`; `marketPrices.js:228-239`; `EconomicsTab.jsx:664`; `economy.generated.js:1595`; `institutionTable.js:129-138`, `:377-397` |
| the road · the roads (missed noun, 31 occurrences) | as a PLACE word no layer; but the ROAD holder kind's NAME (nouns `book · books`), the `road` civic class, and the `{access}` value one hop from a toll-bar field — on the seven TERRAIN pools (road-kind after the `terrainKey` repair) and DS-ECO-1, a refuter needs a ruling: route (safe) or record (a citation) | DS-ECO-11 TERRAIN ×7; DS-ECO-1 ×5 | the road as a route only | `holderTable.js:233-234`, `:350-356`; `fieldSynonyms.js:88`; `wiringCensus.js:1313` |
| the hands (as a POWER) · one name | POWER named through an idiom to dodge the dark `{faction}` | DS-SUP-2 CAPTURED v2 `:1337`, v3 `:1338` (NO PRODUCER) | — | annex `:1335-1338` |
| Customs house · Warden's Lodge · Workhouse · Hunter's lodge · Adventurers' charter hall (missed) | `Customs house` a catalog body keeping no ratified record; the others are more `employment` houses the impaired lens can name; the charter hall is a muster-kind TOKEN with no roster holder | DS-SUP-3 `{institution}` | — | `institutionServices.js:529-532`; `serviceCategoryTables.js:45-50`; `defenseInstitutionBuckets.js:101-104`; `holderTable.js:192` |
| `rank` · `foodBalance.*` · `granary.*` (band) · `readings.flowDrift.band` · `text(terrainKey)` · `readings.notableAbsences` · `leading.bucket` | layer NONE — no institution row, no holder kind (the survey's BODY tag on these is REFUSED); `text(terrainKey)` becomes HOLDER (road) after a one-token repair (`terrainKey` → `terrainType`) | DS-ECO-1 ×5; DS-ECO-2 ×7; DS-ECO-3; DS-ECO-11 ×7 + ×4; DS-SUP-3 ×6 | no institution-class noun of any layer | `economyStateProse.js:961`, `:976`; `holderTable.js:157-159`, `:233`; `economyDeskRead.js:108`; census `source.kind ''` |
| `eco.safetyProfile.blackMarketCapture` | HOLDER (watch, a STATE ORGAN) — ORGAN-UNDER-POWER only where `standingOf(…,'watch').interested` (birth capture from `adversarial`; no product caller); the read's VALUE is a shadow estimate computed with NO join to the capture ladder (O-R6) | DS-ECO-6 ×3 | the watch's own record (`roll · rolls · watch`), never "bought" / "under a hand" from this read | `holderTable.js:99-102`, `:115`, `:129-143`, `:210`; `safetyProfile.js:619-638`; `factionDynamics.js:220-245` |
| `eco.incomeSources` (+ `.length` · `.reduce` · `.filter`) | HOLDER (treasury, a STATE ORGAN) — ORGAN-UNDER-POWER only where the town is captured; the criminal LINE inside the mix is a treasury RECORD fact, not a standing over the treasury | DS-ECO-12 ×5 | the treasury's ratified nouns (`books · purse · chest`) | `holderTable.js:166`; `fieldSynonyms.js:78` |
| `eco.foodSecurity.stockpile.blockaded` · `.blockadeBypass` | HOLDER (toll-bar) — not a body, NOT an organ (`toll-bar ∉ STATE_ORGAN_KINDS`); CONDITIONAL rider: ANY holder can be INTERESTED world-run through a corruption impairment written on an NPC's home institution of any kind (O-R4) | DS-ECO-9 ×2 | `book · books · toll` | `foodStockpile.js:417-418`; `holderTable.js:115`, `:304-310`, `:660-661`, `:671`, `:685`; `corruptionImpair.js:111`; `mutateEntities.js:831` |
| `readings.exportPosture.status` · `analysis.economicStrengths` · `.strategicValue` · `eco.isEntrepot` · `.localProduction` · `.primaryExports` · `.primaryImports` | HOLDER (market) — not a body, not an organ; the same O-R4 rider | DS-ECO-10 ×6; DS-ECO-11 ×3; DS-ECO-12 ×5 | `book · books · stall` | `holderTable.js:170-180`, `:311-317` |

#### 2.3-R THE ECONOMY DESK — REFUSED

| assignment tried (survey) | refuter's verdict | evidence |
|---|---|---|
| LAYER = BODY on `rank`, `foodBalance.*`, `granary.*`, `readings.flowDrift.band`, `text(terrainKey)` ("the ground"), `readings.notableAbsences` | REFUTED — layer NONE (HOLDER road for the terrain token after repair) | the law's BODY is "an institution row"; none of these carries one (census `source.kind ''`); `deriveNotableAbsences` is over CATEGORY keys (`servicesDisplay.js:15-21`); `terrainKey` is a local const (`economyStateProse.js:976`) that loses the `road` row (`holderTable.js:233`) |
| LAYER = ORGAN-UNDER-POWER on `blackMarketCapture` and `incomeSources` (as if the power were always there) | CONDITIONAL — HOLDER; under-power only where captured, and never resolved at draw time | `standingOf` / `sourceOfForTown` have NO product caller (grep over `src/`); `capturedRulingStructure` `holderTable.js:129-143`; the shadow estimate has no join to the ladder (`safetyProfile.js:619-638`) |
| "BODY, cited to an organ" on `stockpile.blockaded/.blockadeBypass`, `exportPosture.status`, `economicStrengths`, `strategicValue`, `eco.*` | REFUTED on both words — HOLDER (toll-bar / market), which are NOT state organs | `holderTable.js:115`; `standingOf` prints "toll-bar is not one of the state's own organs" (`:685`) |
| *duties* = "the toll-bar/treasury duty vocabulary (`DUTY_SERVICE_KINDS`)" | REFUTED on the vocabulary; the wrong-word finding stands | `institutionTable.js:106` has no `duties`/`duty`; `Customs house` (`institutionServices.js:529-532`) is the only "Duties" and is in no holder cite |
| "the market body" / "toll-bar organ" as words | REFUTED as vocabulary | as above |
| *Merchants operating in the shadow economy…* = `economicDragDesc` scaleNote | attribution REFUTED; the finding (frozen, unmarked, player-audible) stands | `EconomicsTab.jsx:664`; `economy.generated.js:1595`; `economicDragDesc` is `safetyProfile.js:641-644` |
| *a clerk* at DS-ECO-6 ≥30 v1 | CONDITIONAL — count right, one location wrong | `:1080`, `:1122`, `:1649` |
| `properFill` "refused 1 of 25: `Bakers (5-15)`" as a DS-ECO-11 `{institution}` member | CONDITIONAL — that value is `[2]` of the grain chain and never fills the slot | `resourceChains.js:58`; the fill is `[0]` (`economyStateProse.js:991`) |
| the `watch` token row at `holderTable.js:203-209`; the census "`fieldSynonyms` column" | cite/wording corrections | `:202-209`; the census carries one document-level table `fieldSynonyms` (`wiring-census.json:47351`), not a per-row column |
| paths: `institutionTable.js`, `ServicesTab.jsx`, `serviceCategoryTables.js` | path corrections, lines exact | `src/domain/institutions/institutionTable.js`; `src/components/new/tabs/ServicesTab.jsx`; `src/generators/services/serviceCategoryTables.js` |

### 2.4 THE POWER DESK (DS-POW-1 … DS-POW-7; 79 pools · 256 variants · 29 dm-only · LICENSED 14 = court 10 · watch 2 · court + watch 2 · SOURCE-UNRESOLVED 65 · covert 0)

Sources: `power.referent.survey.md` (Opus; supersedes `power.referent.survey.SUPERSEDED-0533.md`) · `power.referent.refute.md` (Fable; 114 verdicts: HOLDS 93 · CONDITIONAL 12 · REFUTED 9, four substantive). **The structural fact: no pool of this desk reads `settlement.institutions[]`, any bucket, any impairment, any patron or any per-faction `captureState`** (zero hits over the 79 rows' reads); every body word in its prose is a baked noun with no row behind it (the hall ×50, the watch ×5, the walls ×4; the survey's "66 of 256 variants" reads 55 on the refuter's list). `SLOT_FILL_TABLES = {}`: every referent arrives as a generated proper string whose class the engine does not tag.

| noun | layers (HOLDS / CONDITIONAL with condition) | reads per layer (pools) | safe class word | source |
|---|---|---|---|---|
| the watch | BODY: watch bucket; roster `Professional city watch`, `Town watch`. HOLDER: watch kind, STATE ORGAN, records `Crime reporting / Crime response / Missing persons`. POWER-eligible: SECURITY body. The desk ALSO uses **watch as a verb** ×3 (the reverse alias trap, unrowed by the survey) | BODY: none · HOLDER: `power.criminalCaptureState` (DS-POW-6 capture reached an AGENT / a LEADER) and `breakdown*` + `criminalCaptureState` (DS-POW-6 ADVANCING / RECOVERING) — 4 rows, kind `watch` / `court + watch` · POWER: none | CONDITIONAL — "the guard" is REFUSED (a faction name, a role keyword, a mandated role name on the same page); the paid military as a class is the muster and the watch is not in it; on this desk the watch is a CITATION-layer word (the record's keeper), never a subject | `defenseInstitutionBuckets.js:95-97`; `institutionServices.js:1206`, `:1322`; `holderTable.js:78`, `:115`, `:318-323`; `corruption.js:630`, `:675-681`; census probe |
| the hall | BODY: `Town hall`, `City hall` (OFFICE-kind holders) + `Guild Hall`, `Adventurers' Charter Hall`, `Free company hall`, `Hireling hall`, `Gambling halls`; the civic class `hall` (`hall · council · charter · seat · office · chamber · moot`); used by the desk as a synonym for the `{seat}` fill | NONE at any layer — 50 occurrences (46 "the hall" + 4 forms), zero reads | the ruling structure (class word) or the `{seat}` slot; *the hall* is unlicensed as a body on this desk | `institutionServices.js:55`, `:320`, `:930`, `:941`, `:1002`, `:1623`, `:1629`; `holderTable.js:357-362`; `wiringCensus.js:1314` |
| the walls · the gates | BODY: walls bucket; the `walls` token maps to the MUSTER kind; `Gates (if walled)` is in the walls bucket AND a toll-bar holder (toll-bar holders measured: `Town council · Major Port · Gates (if walled)`) | NONE — 4 + 2 occurrences (DS-POW-2 siege v1/v2/v4; DS-POW-6 neutral v1, duty evasion v2; DS-POW-7 glue threat v1) | none licensed here (the `{defwork}` slot is the defense desk's) | `defenseInstitutionBuckets.js:84-87`; `holderTable.js:188`, `:304-308`; `wiringCensus.js:1302` |
| the granary | BODY: `City granaries` (`:839`, not `:838`), `Town granary` (`:1429`), `State granary complex` (`:1472`); class `storehouse`; a WITHDRAWN holder | NONE — DS-POW-1 FOOD, adverse v1 | none licensed here | `institutionServices.js:839`, `:1429`, `:1472`; `wiringCensus.js:1321`; `holderTable.js:157-159` |
| the customs | BODY: `Customs house`; HOLDER: toll-bar (`Customs brokerage`) | NONE — DS-POW-6 duty evasion v1/v3 read `name` | none licensed here | `institutionServices.js:530`; `holderTable.js:305` |
| the market | BODY: nine roster rows (lawful and illegal); HOLDER: market kind (holder measured = `Market square` only; `Weekly market` is a TREASURY holder — A-6); civic class `market` | the operation-role pools read `name` (the criminal operation's own label): DS-POW-6 parallel marketplace · stolen goods market · criminal revenue stream (census `objectClass: market`) | the operation by its role label (`criminalOpRole.js:38-57`), never *the market* | `institutionServices.js:48`, `:517`, `:524`, `:759`, `:879`, `:920`, `:1064`, `:1132`, `:1671`; `holderTable.js:277`, `:311-316`; `wiringCensus.js:1310` |
| the court | BODY: `Courthouse`, `Multiple court buildings`. HOLDER: court kind, STATE ORGAN (holders measured = those two). POWER-eligible: SECURITY body; civic class `law`. **The word means two things on this desk**: the law court (the holder) and the political ASSEMBLY (DS-POW-7 `a fully divided court`, `objectClass: law`) — the engine types only the law court | HOLDER: 12 of the desk's 14 LICENSED rows (court 10, court + watch 2) · BODY: none | at the assembly sense the `{seat}` / the hall's own combination; at the record sense a CITATION, never a subject | `institutionServices.js:89`, `:1117`; `holderTable.js:78`, `:115`, `:325-330`; `corruption.js:630`; `wiringCensus.js:1311` |
| the council | BODY: `Town council` (holds ELDERS + TOLL-BAR records), `Mayor and council`, `Elder Grove Council`, `Guild governance`. POWER (a value): one of six `RULING_POWERS`. A NAME: fourteen `governingName` values contain "Council". Civic class `hall`. ROLE (a fifth register the survey missed): a GOVERNMENT role keyword and `'Council Member'` a mandated NPC role. The annex's own fence: "prose that hard-codes the council will be wrong on most settlements" | POWER: `readings.structuralLens.rulingPower === 'council'` selects DS-POW-5 `council` — census WIRING-UNRESOLVED (reads `[]`) · BODY: none | the ruling structure; the six ruling-power words are lens VALUES, safe only as the pool they key | `institutionServices.js:32`, `:497`, `:985`, `:1069`; `cohesionWeave.js:133`, `:171-177`; `rulingStructure.js:161-176`, `:219-250`, `:259-290`; `wiringCensus.js:1314`; `roleCategory.js:33-34`; `npcGenerator.js:1525`, `:1530`, `:1534`; `powerStateProse.js:709-713`; annex `:2199-2201` |
| the workshops · the tables · the clearinghouse · the temple / `{institution}` · the prison / the gaol | the workshops: BODY (`Craft Guild District`; class `craft`) on pools DARK by declaration; the tables: BODY (`Gambling halls`) on a `name` read; the clearinghouse: NO ENGINE ROW ANYWHERE (an invention); the temple / `{institution}`: the §0c slot named by ONE variant of forty and never filled (`slotsWithoutProvider`); the prison: BODY (`Small prison/stocks`, `Large prison`; class `law`), not rendered — and `'Prison Warden'` is an emitted power ROLE | none (the economicBase pools read `[]`; `unlicensed revenue` v3 reads `name`; DS-POW-5 theocracy drops) | the role label; none for the rest | `institutionServices.js:134`, `:941`, `:1635`, `:1640`; `powerStateProse.js:665-701`; `wiringCensus.js:1311`, `:1316`; `historyData.js:148`; `npcGenerator.js:1465` |
| the muster | HOLDER-ORGAN ONLY: muster kind; exactly ONE roster holder (`Citizen militia`, "the sharpest wiring debt this table found"); its FIELDS are walls · garrison · militia · mercenary · charter · force · economicGates · besiegedBy · besiegingTargets · ticksToDeploy · stretchedThin. The desk's single occurrence is the VERB (DS-POW-4 "what {faction} can muster for itself") — a reverse alias trap | NONE (no muster-kind read on the desk) | the muster (as a noun, on a muster-kind read only) | `holderTable.js:78`, `:188-199`, `:280-288`; `institutionServices.js:835` |
| the rolls · the records · the accounts · the books · the returns · the manifests · the minutes · the writ | HOLDER-layer SURFACES (the office's own formula); `office` is "the compiling record itself" and a citation on it is a FINDING; the `ledger` token WITHDRAWN. *the writ* (DS-POW-1 Endorsed v3, Contested v2) is a court/office INSTRUMENT surface with no engine row | DS-POW-1 (rolls ×3, records, accounts ×3, returns), DS-POW-2 (rolls), DS-POW-4 (rolls ×3, records ×3), DS-POW-5 (books), DS-POW-6 (rolls, record, returns, books, manifests, minute) — leaf counts roll 8 · record 7 · account 6 · return 2 · book 2 · manifest 1 · minute 1 | the surface is lawful (R-vi / W7); the roll as an AGENT-SOURCE is not | `holderTable.js:74-77`, `:159-161`, `:271-364`, `:361-362` |
| the houses · a house | POWER: a faction is a recorded NAME (`factions[]{faction, power, powerLabel, isGoverning}`; world-run `factionStates` with `archetype`, `captureState`, `leaderNpcId`) — CONDITIONAL hole: *house* is also a BODY word (`Banking House`, brokerage houses) | `factions` / `factions.length` (DS-POW-2 DOMINANT / NARROW); `politics.blocs` (DS-POW-5 autocrat, DS-POW-7 presence) | the houses (the plural of the typed faction slot); NOT for a brokerage house or a bank | `rulingStructure.js:781`, `:787`; `settlementPolitics.js:246-258`; `institutionServices.js:96`; `brokeragePatronage.js:283-286` |
| the combination · the bloc · the majority | POWER: the `Bloc` typedef (`{id, members[], glue{type,detail}, end, strain, sinceTick, covert?}`); the glue `patronage` is PEOPLE-HELD and dies at succession — a ROLE-layer fact inside a POWER row | `politics.blocs` — DS-POW-7 `layer DORMANT` and `an opposition bloc forms COVERT…` (the only two RESOLVED of twenty) | the combination (the shipped word: names the typed bloc without claiming an institution) | `settlementPolitics.js:63-67`, `:423-457`, `:461`, `:549`, `:604`, `:792-803` |
| the criminal interest · the operator | POWER: `computeCriminalCaptureState` reads a criminal FACTION's power against the governing faction's and the safety ratio; its OUTPUT names no faction and no person — CONDITIONAL: the criminal party IS a typed, named faction (`category === 'criminal'` or a name containing `thiev`; born as `Thieves' Guild`; typed roles `Kingpin`, `Lieutenant`) — no slot of THIS desk carries it | `power.criminalCaptureState` (4 rows); `name` (7 operation-role rows) | the criminal interest (a standing, not a body) | `factionDynamics.js:220-273`, `:221-222`, `:237-243`, `:246-247`; `rulingStructure.js:671`; `factionRoles.js:52-53`; `criminalOpRole.js:38-57` |
| the office (missed noun, 5 uses) | FIVE registers: a HOLDER KIND; the `'OFFICE'` register STANDING; a civic-class member of `hall`; the seat-as-office in the desk's own prose (DS-POW-4 Contested v1, Critical v1; DS-POW-7 end seats v1, end doctrine v1); a faction-name word (`Conversion Enforcement Office`) | none rowed | — (the desk's most layer-loaded unrowed noun) | `holderTable.js:80`, `:87`; `wiringCensus.js:1314`; `stressFactions.js:148` |
| the clerks · the officials · an operator · a leader · the person who speaks for {faction} · the incumbent / the challenger · a ruler | ROLE words — the survey tested them against the roster and the holder kinds (the BODY/HOLDER instrument), not against `officesOf` (the ROLE instrument): the engine EMITS `'City Clerk'`, `'Corrupt Official'` (a role NAME that bakes a corruption STANDING into a public roster string), `'Kingpin'`/`'Lieutenant'`, and `leaderNpcId`/`leaderName` are typed and UNREAD by the desk; `contenders.incumbent{name, gated, govMultiplier}` / `challengers[]{name, archetype, power, weight}` arrive as a caller READING; *a ruler* is the generic of the `{seat}` fill (a maxim frame when stated as "a ruler with the town behind them can do things") | none by field: the ACT charges (record · name · chase · agreeable · speaks) stand under law 1 regardless | a ROLE states a standing condition, never an act | `historyData.js:103`; `npcGenerator.js:1524`, `:1532`; `roleCategory.js:35`; `factionRoles.js:52-53`; `settlementPolitics.js:252-253`; `rulingPowerCoup.js:77-104`; `PowerTab.jsx:198-201`; `institutionTable.js:395-397` |
| `{npc}` | PERSON — a recorded NPC name in subject position (6 of 16 DS-POW-3 variants) from `ladderRungsOf` → `{npcId, name, standing}`; the draw is GATED AT THE CALLER (`publicDossier ? null : drawnAtMount(...)`) — a visibility carrier outside the marks | DS-POW-3 (`reading.rungs*`) | — (OWED Q-1: a ROLE read or a PERSON referent) | `ladderRead.js:95-107`; `powerStateProse.js:639`; `PowerTab.jsx:477-480`; annex `:2084-2085` |
| the reeve · the headman · the mayor · the priest · elder · the captain | inside `{seat}` fills, never as bare nouns (`'village reeve' → 'Elected Reeve'`, `"Headman's Authority"`, `'Priestly Guidance'`, `'Town Mayor'`) — AND emitted NPC ROLES (`'Reeve'`, `'Mayor'`, `'Elder'` the thorp/hamlet mandated role); *elder* is ALSO an angle tag and a holder kind (four registers); **the captain**: zero occurrences on the desk BUT the engine EMITS `'Guard Captain'`, `'Watch Captain'` (with a typed institution link), `'City Watch Chief'`, `'Garrison Commander'` — ADDENDUM 11's specimen IS an engine role word; *the factor* is not emitted | `{seat}` on DS-POW-1/2/4/6/7 | — (a role word can arrive INSIDE the body slot) | `rulingStructure.js:164`, `:259-260`, `:284`; `historyData.js:21`, `:119`; `npcGenerator.js:1512-1536`; `factionRoles.js:45`, `:215-233`; `roleCategory.js:33`, `:38`; `holderTable.js:332-337` |
| `{seat}` | POWER slot = `powerStructure.governingName` (the same value as `government`) — CONDITIONAL: whatever class the generated name happens to be — a body (*Town Council*), an office (*Royal Authority*, *Feudal Stewardship*) or a role (*Town Mayor*, *Elected Reeve*, *Headman's Authority*, *Priestly Guidance*) | DS-POW-1 (38 of 41 variants), -2, -4, -6, -7 | — (OWED Q-6) | `powerStateProse.js:149-152`, `:155-162`, `:860`, `:875`; `rulingStructure.js:787`, `:792` |
| `{faction}` | POWER slot = THE SAME `governingName` STRING in DS-POW-2/-4/-6/-7 — one referent under two spellings (R-2); DS-POW-6's comment documents `{faction}` as the CAPTURED HOUSE and fills it with the GOVERNING body (R-3) | `:876`, `:909`, `:953`, `:962` | — | `powerStateProse.js:860`, `:876`, `:900-901` (the comment; the survey's `:957-958` is wrong), `:909`, `:953`, `:962` |
| `{counterpart}` | POWER slot = `readings.contenders.challengers[0].name` — CONDITIONAL: the name can be `'Military/Guard'` (a body word as a power), `'War Council'`, `'Conversion Enforcement Office'`, or `'Unknown Faction (hidden)'` (power 15, "its presence is not known to the settlement") seated on a DS-POW-4 Contested/Critical face that carries NO dm-only mark — a covert POWER on a player face (A-15) | DS-POW-4 (`:953`), DS-POW-7 (`:909`) | — | `powerStateProse.js:178-186`, `:994`; `rulingPowerCoup.js:51`, `:87-104`; `stressFactions.js:105-107` |
| the ruling structure (as a CLASS) · the patron | `RULING_POWERS` = `autocrat · council · theocracy · merchant_league · criminal · mixed`, total, failing soft to `mixed` (the POWER's KIND, never its name); the patron (`patronId · patronName · patronArchetype`, `genesis`/`captured`; player projection writes `patronName: 'unknown'`) is read by NO pool of this desk — DS-POW-7 `end patron` invents the relation from the bloc's `end` value | DS-POW-5 (lens); none (patron) | the ruling structure | `cohesionWeave.js:133`, `:171-177`, `:194-197`; `brokeragePatronage.js:60`, `:287-297`, `:319-328`; `settlementPolitics.js:461-465` |
| an army · the harvest · the government / the governing body (missed) | *an army* (dark pools) `force`-adjacent, no bucket keyword; *the harvest* (5) the `store` civic class — a STOCK word on a legitimacy read; *the government* = `powerStructure.government`, a typed field equal to `governingName` at birth (R-6: `governanceFractured` asserts a SECOND, UNNAMED power on a predicate COEXTENSIVE with the crisis band) | DS-POW-1 governanceFractured v3; DS-POW-4 previousGovernments v3 | — | `wiringCensus.js:1309`; `rulingStructure.js:792`; `factionDynamics.js:132-133`; `powerStateProse.js:53-55`; annex `:1928-1930` |

#### 2.4-R THE POWER DESK — REFUSED

| assignment tried (survey) | refuter's verdict | evidence |
|---|---|---|
| PERSON — "`holderRole` is a hardcoded null: no typed NPC→institution edge exists anywhere in the estate" | REFUTED as a measured absence (the RULE can still be ratified on product-scope grounds) | `npc.linkedInstitutionIds` is a schema field (`settlement.schema.js:459`), written by the generator (`factionRoles.js:215-233`: `role: 'Watch Captain', linkToInst: /watch\|garrison\|barracks\|militia/` `:45`; `'Lord Mayor'` → `/council\|court\|government\|…halls?/` `:56`) and by `magicFormsPractitioner.js:216`; read by `events/batch.js:149`, `magicFormsPractitioner.js:125`, `SuccessorPrompt.jsx:56`. `holderRole: null` (`institutionTable.js:457`) is the table's CHOICE not to read it |
| "the guard" — zero occurrences; the safe BODY word for the garrison bucket | REFUTED on both halves | the leaf carries `guards` as a verb (DS-POW-5 craft v3); `'Military/Guard'` is a FACTION name (`rulingStructure.js:596`), read by the engine as the military (`factionDynamics.js:233`); `guard` a MILITARY role keyword (`roleCategory.js:38`); `'Guard Captain'` a mandated role (`npcGenerator.js:1514-1516`) |
| "the captain · the factor — not part of this desk's vocabulary; captain matches no roster row and no holder kind" | REFUTED for the captain (the engine emits it) | `'Guard Captain'`, `'Watch Captain'` (typed link), `'City Watch Chief'`, `'Garrison Commander'` (`npcGenerator.js:1514-1536`; `factionRoles.js:45`); `captain` in `roleCategory.js:38` |
| Q-7 / "draft law 4 currently has NO mechanical carrier on this desk; visibility lives entirely in the annex's dm-only marks" | REFUTED — three CALLER-side carriers, none censused | `PowerTab.jsx:194` (`audience = playerView ? 'player' : 'dm'`), `:198-200`, `:215` (`publicDossier` nulls the whole desk), `:477` (DS-POW-3), `:203-206` (DS-POW-7 honours the secrets filter); the census `covert=false` on 79/79 HOLDS |
| POWER (INTERESTED) — "the four state organs become interested parties" | CONDITIONAL | birth-time INTERESTED fires on `adversarial` (`holderTable.js:141`), a rung whose producer says "enforcement is winning" (`factionDynamics.js:267-268`); the `corrupt` and `controlled` arms of `standingOf` are dead in the product (only `scripts/taste-holders.mjs:54, :102` call it, with `{}`) |
| a corruption impairment = the covert channel | CONDITIONAL | the covert set ALSO fills from an unexposed corrupt NPC (`corruption.js:683-689`; `npcHomeInstitution` `:651`; substring `nameMatches` `:640-645`); the regex tests RAW `inst.name` (`:665`) while buckets test `nativeSemanticName` — a custom "Night Watch" is a security body in NO bucket |
| "the ladder names no faction … none typed — the criminal party is never named by a slot" | CONDITIONAL | the criminal party IS typed and named (`factionDynamics.js:221-222`; `rulingStructure.js:671`; `factionRoles.js:52-53`); no slot of THIS desk carries it (holds) |
| "66 of 256 variants carry at least one body word" | CONDITIONAL on the word list | the refuter's list reads 55; the survey's list is not printed |
| cite corrections (claims hold) | cite REFUTED | `holderTable.js:751-760` → `:730-744` (the pair dedupe); `factionDynamics.js:245-246`/`:257-259` → `:246-247`; `powerStateProse.js:957-958` → `:900-901`; `:809-810` → `:800-801`; `institutionServices.js:838` → `:839`; annex `:2305-2307` → `:2293-2295` |

### 2.5 THE STRESSORS DESK (DS-STR-1 · DS-STR-2 · DS-CND-1; 67 pools · 49 RESOLVED / 18 WIRING-UNRESOLVED · variants 96 / 96 / 54 = 246 as the refuter re-counted them)

Sources: `stressors.referent.survey.md` (Opus) · `stressors.referent.refute.md` (Fable). **The one number the desk turns on (HOLDS at the census grain): rows carrying a HOLDER 0 · `SOURCE-UNRESOLVED` on 67 of 67 · `covert` 0 · `objectClasses` on 4 rows only (market ×2, temple, hall).** The desk has no ORGAN-UNDER-POWER read wired and no licensed institution referent of any kind; every institution-class noun in its 246 variants is rendered on a read whose RECORD holds no typed institution, holder, power or person — with the refuter's sharpening that the RECORDS do carry the engine's own body and person words in string fields (`counterforce.sourceBreakdown[].kind === 'institution'`, `originContext.hooks[]`, `reason`, the condition templates' `description`), and the coup's `contenders[].name` is a typed POWER name on the very record six ORIGIN pools read.

| noun | layers (HOLDS / CONDITIONAL with condition) | reads per layer (pools) | safe class word | source |
|---|---|---|---|---|
| walls · wall (13 uses) | BODY: walls bucket; `hasWalls`; the `wall` civic class; THREE wall spellings across `priorityHelpers.js`, the bucket and `stressGenerator.js`. HOLDER: the `walls` token → muster | NONE — no desk pool reads a wall field | the wall · the perimeter — and on this desk nothing, because no read reaches it | `defenseInstitutionBuckets.js:84-87`; `priorityHelpers.js:53`; `wiringCensus.js:1302`; `holderTable.js:189`; `stressGenerator.js:107`; annex 4071-4486 |
| gates · gate (3) | BODY (`wall` class; `hasGates`) AND HOLDER: `Gates (if walled)` backs the TOLL-BAR kind (missed by the survey, B-5) | NONE | the gate (class word) | `priorityHelpers.js:54`; `wiringCensus.js:1302`; `holderTable.js:308` |
| garrison · garrisons (7) | BODY: garrison bucket; `hasGarrison`; the `force` civic class; the world classes `defense` AND `security`. HOLDER: the token → muster. POWER: a SECURITY body; `garrison\s+rule` in a faction NAME reads as archetype OCCUPATION; under BOTH upkeep purses (`hasAnyDefense` and `hasLawInfra`); ALSO an NPC role word the engine matches (B-14) | NONE | "the guard" as the bucket's body word — the stressors refuter HOLDS this (7.7) while three other refuters refuse it (X-D2, §6); "the muster" as the class word | `defenseInstitutionBuckets.js:88-91`; `priorityHelpers.js:46`; `wiringCensus.js:1303`; `stressorDynamics.js:50-51`; `holderTable.js:190`; `corruption.js:630`; `factionArchetypes.js:68`; `defenseGenerator.js:177-178`, `:244`; `npcGenerator.js:1023` |
| soldiers · officers (1 face) | `soldier` a `force`-class member; no roster row spelled "soldier"; "officers" NO engine row (though `sergeant`, `commander`, `marshal` are role words the engine matches) | NONE | the muster | `wiringCensus.js:1303`; `npcGenerator.js:1018-1025` |
| watch (2: a verb at 4145, a body at 4155) | SEVEN rows + one: watch bucket; garrison bucket via `professional city watch`; the watch HOLDER kind; a STATE ORGAN; `SECURITY_INSTITUTION_RE`; faction archetype MILITARY by category AND name; world classes `defense` AND `security`; plus `Watchtower` (a `watch` for the regex and `hasMilitaryInst`, in no bucket and no kind — B-1) and the tier-derived label `'local watch'` printed below town with no roster test (B-10). CONDITIONAL on tier: `hasWatch` is a NAME list; the "town and up" fact is the catalog's seating and a custom roster can seat a watch anywhere. The `Watch Captain` role links to FOUR bodies. The pay read: the watch arms BOTH purses at the gate (B-4) | NONE | as a BODY the bucket's own name; as an ORGAN "the watch" of the order records; "never for a pay read" is a CHAIR'S CHOICE against the generator, not an engine impossibility (the refuter's 7.8) | `defenseInstitutionBuckets.js:90`, `:96`; `holderTable.js:78`, `:115`, `:202-209`; `corruption.js:630`; `factionArchetypes.js:55`, `:79`; `stressorDynamics.js:50-51`; `priorityHelpers.js:25-26`, `:48`; `institutionalCatalog.js:1348`, `:1918`; `institutionServices.js:1561`; `safetyProfile.js:30-35`; `factionRoles.js:45`; `defenseGenerator.js:177-178`, `:244-253` |
| army (2) | no institution row; `NAME_RULES` MILITARY includes `army`; the condition template `army_deployed` says "standing army … home garrison" — CONDITIONAL: the engine speaks of a standing army as a DESCRIPTION string, never a row | NONE | — (a campaign is DS-WAR-1's) | `factionArchetypes.js:79`; `activeConditions.js:342` |
| granary (2) · stores (2) | granary: the `storehouse` class (split from `store` at 8a-8; a LONE `granary` key still reads `store`); a WITHDRAWN holder; `hasGranary` a probability input; the world food class also credits a fishery. stores: the `store` class | NONE | the stores (the stock); the building needs a row | `wiringCensus.js:1304-1309`, `:1319-1322`; `holderTable.js:157-162`; `stressGenerator.js:109`; `stressorDynamics.js:48` |
| market (6) | BODY: `market` class; `hasMarket`. HOLDER: market kind (`dutyNamed: 0`). POWER: MERCHANT archetype by category and name | KEY-LEVEL ONLY, twice (`SYNERGY: market_shock × indebtedness`, `ORIGIN: merchant_cabal` carry `objectClasses: ['market']`); no market FIELD is read | the market as the class word only | `wiringCensus.js:1310`; `priorityHelpers.js:57`; `holderTable.js:310-316`; `factionArchetypes.js:56`, `:81` |
| warehouse · warehouses (2) | the `storehouse` class — CONDITIONAL: the survey's "no body row" is wrong; `hasWarehouse` is a flag and `Merchant warehouses` / `Warehouse district` are catalog rows | NONE | — | `wiringCensus.js:1322`; `priorityHelpers.js:60`; `institutionServices.js` keys |
| hall (4) · council (2) · chamber (1) · seat (12) · office / offices (5) | ONE civic class `hall` (hall · council · charter · seat · office · chamber · moot); the world `admin` class. `office` is a HOLDER KIND and a STATE ORGAN (a citation on it "would be citing the speaker"). `seat` is the POWER layer's own object (`governingFactionOf` / `governingName`). `Town council` backs toll-bar AND elders; `Village headman` backs treasury AND elders; `hasCourtSystem` contains the halls (B-6). Also `administration` (missed): `City administration`, a BODY row backing treasury and office — the roster's own spelling of the office kind | KEY-LEVEL ONCE: `ORIGIN: council_schism` → `objectClasses: ['hall']` — whose own card refuses "another civic object of the class `hall`", i.e. its own "seat", "office", "chamber" (used anyway: F6, F17) | the ruling structure (power) · the office (record) · the hall (building) — three layers wearing one class | `wiringCensus.js:1314`; `stressorDynamics.js:49`; `holderTable.js:78`, `:115`, `:277`, `:308`, `:336`, `:356-364`; `rulingPower.js:88`, `:224-229`; `priorityHelpers.js:56`; `institutionalCatalog.js:1900`; annex 4087-4361 |
| courthouse (1) | the `law` class; HOLDER: court kind, STATE ORGAN; POWER: SECURITY body; `CATEGORY_MAP.law → MILITARY` | NONE | the court (organ) | `wiringCensus.js:1311`; `holderTable.js:78`, `:115`, `:324-330`; `corruption.js:630`; `factionArchetypes.js:55` |
| treasury (2) | HOLDER: treasury kind, STATE ORGAN (fields `incomeSources · viable · criticalIssueCount · economicViability`) | NONE — rendered twice, once as an AGENT ("a drowning treasury", which is VERBATIM the record's own `reason` string) | the treasury (organ) — never as an agent | `holderTable.js:78`, `:115`, `:166-169`; `stressorDynamics.js:844` |
| altars · pulpit · sanctuary · observances · rite · creed · congregations (8) | the `temple` class (temple · shrine · church · parish · clergy · faith · patron — "pulpit", "altars", "sanctuary" are NOT tokens, so the card's class refusal would not see them); `parish` is a HOLDER kind, not an organ; RELIGIOUS archetype; the world `religious` class | KEY-LEVEL ONCE: `ORIGIN: temple_putsch` → `objectClasses: ['temple']` | the temple (class word); the deity doctrine sits on top | `wiringCensus.js:1312`; `stressorDynamics.js:52`; `holderTable.js:78`, `:296-302`; `factionArchetypes.js:57`, `:70` |
| roads · road (2) · routes · arteries (missed) | the `road` class (`route` a token); HOLDER: road kind (`Road register · Way-bill registration`) | NONE | the road | `wiringCensus.js:1313`; `holderTable.js:78`, `:234-235`, `:349-355`; annex 4143, 4280, 4451-4452 |
| workshops (1) · workroom (missed) | the `craft` class; a `Cartographer's workshop` row; the arcane BODY class is BUILDINGS (`sanctum · college · conclave …`) and the `magicDef` bucket | NONE | the workshops | `wiringCensus.js:1316`; `stressorDynamics.js:53`; `defenseInstitutionBuckets.js:105-108` |
| schools (1) · taverns (1) · camps (2) | no civic class for any; schools and taverns HAVE roster rows (`Gladiatorial school`, `Academy`, `Great library`; `Inn/Tavern`, `Taverns (5-20)`, `Coaching inn` …) — the survey's "no roster row" REFUTED; camps have none | NONE | none exists | `institutionalCatalog.js:981`, `:1002`, `:1527`; `institutionServices.js` keys |
| houses (merchant sense, 2) | `NAME_RULES` NOBLE (`\bhouse\s+[a-z]`) and MERCHANT (`bank`, `broker`, `guild`); a brokerage HOUSE has a typed PATRON — a different object | NONE | the merchant power (the archetype class word) | `factionArchetypes.js:81`, `:84`; `brokeragePatronage.js:60` |
| institutions · institution (4) | the generic for `settlement.institutions[]` | NONE | the institutions (asserts no row) | — |
| healers (1) | the `care` class names `healer` as a CIVIC OBJECT — AND `'healer'` is an NPC role token, `Healer (divine, 1st level)` a village catalog row, and `'Healer'` the stress-mandated role for famine / plague / migration (the survey's "no engine row" REFUTED) | NONE | the infirmary (body) | `wiringCensus.js:1315`; `npcGenerator.js:1035`, `:1523`, `:1529`, `:1533`; `institutionalCatalog.js:845` |
| barracks · militia · mercenary · charter · citadel · palisade · prison · gaol · muster (0 uses) | the buckets / classes / the muster kind; never rendered (the `ORIGIN: barracks_coup` pool never uses "barracks") | NONE | — | `defenseInstitutionBuckets.js:84-104`; `wiringCensus.js:1311`; `holderTable.js:191-193` |
| rolls (3) · registers (verb) · books (2) · accounts (2) · ledgers (1) · seal(s) (2) · writ (1) · licence / permission (2) · tax / levy / levies (6) · quarantine (1) · lists / tally / bill (missed) | RECORD words of organs the desk never resolves: rolls → census (`DUTY_SERVICE_KINDS` names `rolls?`); books / accounts → treasury / office; `ledger` a WITHDRAWN token; tax / levy → treasury AND toll-bar; `quarantine` no record at all (a Defense-tab display posture); lists → the watch's `Missing persons` / the office's `Record filing` | NONE (standing SOURCE-UNRESOLVED on every row) | the read's own noun, never a register | `holderTable.js:157-162`, `:274`, `:289-295`, `:305`, `:319`, `:358`; `institutionTable.js:106`; `defenseDisplay.js:25-42`; annex 4075-4455 |
| "another power" / "an outside power" (2) | POWER, class word only — the positive exemplar P1 | `token === occupied` (a condition of the town) | **"another power"** IS the safe class word | annex 4087, 4102 |
| authority (5) | POWER generally; `stability` → court kind HOLDS; `publicLegitimacy` → court REFUTED (not a holder token; a `PowerStructure` typedef property) | NONE | the ruling structure | `holderTable.js:215-224`; `rulingPower.js:90` |
| government / governments (7) · the ruling power (1) · the seat (12) | POWER: `powerStructure.government` / `governingName`; GOVERNMENT archetype; `governingFactionOf` — the NAME slot exists and is UNREAD by every desk pool | NONE | the ruling structure · the seat | `rulingPower.js:88`, `:131-133`, `:224-229`; `factionArchetypes.js:54`, `:87` |
| ruler / rulers (2) | POWER typed by NAME on the coup record (`incumbent.name`, stamped at birth) and UNREAD; the engine's own hook says "Officers loyal to the **seat**" — the face's "ruler" is the writer's; a PERSON-shaped word for a FACTION-typed object | `variant === barracks_coup` (`:4340`), `temple_putsch` (`:4349`) | the seat | `stressorDynamics.js:690`, `:894`; `rulingPowerCoup.js:118-123` |
| occupier (2) | POWER — CONDITIONAL BY RECORD: DS-STR-1 `occupied` is a BIRTH token and `worldState.occupations` (the only source of `occupierName`) is written ONLY by the world run — at birth there is NO name to wire (the survey's "one field away" REFUTED for DS-STR-1; CONDITIONAL for DS-CND-1's occupation family); the occupier is a SETTLEMENT id, not a faction (B-11) | `token === occupied`; `FAMILY: occupation layer` (dark) | "the occupier" is safe as a CLASS word even where the name is null | `stressGenerator.js:46-58`; `occupationStatus.js:87-96`; `pulseKernel.js:1150` |
| creditor / creditors (2) · sponsor (2) · faction / factions (2) · religious authority (1) · the houses that finance (1) · neighbour (missed) · conspiracy / conspirators / the plot (missed) · resistance / armed movement (missed) | POWER words: creditor — no typed field on the DESK's records (a world-run `creditorId` exists elsewhere); sponsor — `sponsorSettlementId` typed and UNREAD; factions — `powerStructure.factions[]` / `nameOf`, UNREAD; religious authority / the houses — the archetype reaching the variant through `COUP_VARIANT_BY_ARCHETYPE`, the NAME `contenders[0].name` UNREAD; neighbour — a SETTLEMENT (`hostileNeighborsOf`; `attackerSettlementId`), the `{counterpart}` shape; the conspiracy — the coup's challenger FACTION, typed as `originContext.contenders[]` (the engine's own hooks say "the conspirators"); the resistance — a SCALAR on the occupations ledger and the `occupation_resistance` archetype, NO institution row | the ORIGIN pools (`originContext.variant`) | "the sponsor", "the factions", "a religious power", "a merchant power", "the neighbour" as class words; the NAMES exist and are unwired | `generosityKernel.js:920`; `stressorDynamics.js:670`, `:743`, `:747`, `:777`, `:781`, `:783`, `:884`, `:891-894`, `:911-919`; `rulingPower.js:131-133`; `occupationStatus.js:94`; `activeConditions.js:433` |
| clerks (3) · collector(s) (3) · officers (1) · administrators (1) · scouts (1) · handler / asset (1) | ROLE words with NO engine row: not in `FACTION_ROLES`, `ROLE_FACTION_MAP`, `NAME_RULES` or any class list (`administrators` — the BODY `City administration` exists; scouts and the handler are the engine's own HOOK / REASON strings) | NONE | — (none safe on this desk) | `npcGenerator.js:1002-1060`; `factionRoles.js:40-60`; `npcProfile.js:158`; `stressorDynamics.js:679`, `:763` |
| overseers (2) · officials (1) · healers (1) · mages / casters / hedge wizards (4) · captain · reeve · elder · priest · factor (0 uses) | ROLE words the engine EMITS or MATCHES (the survey's "none" REFUTED): `'overseer'`, `'official'`, `'healer'`, `'factor'`, `'reeve'`, `'captain'`, `'priest'`, `'elder'`, `'wizard' · 'mage' · 'archmage' · 'sorcerer' · 'hedge wizard'` are all `ROLE_FACTION_MAP` tokens; `NAME_RULES` MILITARY carries `captain\|sheriff\|warden\|ranger\|sentinel`, GOVERNMENT `reeve\|steward\|elder`. None binds an institution (`holderRole` null stands); the AGENCY given them is the finding, not the word | NONE | — | `npcGenerator.js:64-72`, `:1004`, `:1006`, `:1015`, `:1017`, `:1027`, `:1035`, `:1040-1049`, `:1054-1055`; `factionArchetypes.js:79`, `:87` |
| heir (missed) · the person responsible · somebody it had reason to trust · Somebody … answers elsewhere (missed) | ROLE (heir: no row; nearest record `dominant_npc_removed` "succession is unresolved") and PERSON referents (the second and third explicit persons beside F18) | `token === succession_void`; `RECENTLY BETRAYED`; `INFILTRATED` | — | `activeConditions.js:306-308`; annex 4114, 4122, 4137 |
| collaborators / patriots · commons / households / population / people (23) · the men · hands · the street / the crowd · badge · the works | collective-person agents; "a totality over persons" is refused on every card; "The streets rose on their own" is the engine's own `popular_revolt` reason | NONE | — | annex 4079, 4091, 4151, 4160-4162, 4178, 4330, 4335-4336, 4356, 4364, 4369, 4374; `stressorDynamics.js:850` |
| `{counterpart}` | POWER slot, `proper` — named by `ORIGIN: declared_war` only; NEVER FILLED (`SLOT_FILL_TABLES` is `{}`); the record field EXISTS (`attackerSettlementId`) | `declared_war` | — | `dossier-slot-shapes.mjs:27`; `stressorsStateProse.js:82`; `stressorDynamics.js:781` |
| `attackerLabel` | THE DM'S PEN — "a goblin warband, a mercenary company — which only the DM can name"; NOT READ by any pool | — | the player face may say the town does not know | `stressors.js:610-627`; `stressorDynamics.js:594-598`, `:787-796` |
| `originContext.variant` (17 ORIGIN pools) | MIXED — the desk's one real layer question: FIVE pure FACTION-ARCHETYPE reads (`barracks_coup`, `merchant_cabal`, `temple_putsch`, `arcane_ascendancy`, `council_schism`) + `palace_coup` = archetype `noble` OR THE DEFAULT (a face may not say "a noble house" without `contenders[0].archetype === 'noble'` — B-12); THREE neighbour-relationship reads (`foreign_sponsored`, `abandoned_agent`, `declared_war`); `servile_uprising` a causal-score read; `tax_revolt` a CO-LOCATED-STRESSOR read (not a score — REFUTED); `popular_revolt`, `internal_conspiracy`, `unattributed` FALLBACKS (four fallbacks, not two); two `memoryStrength` reads; one co-located occupation. The coup half is ORGAN-UNDER-POWER-shaped with a typed NAME the desk does not use — CONDITIONAL: the snapshot is stamped "NARRATIVE only", a BIRTH-time fact | DS-STR-2 ORIGIN ×17 | — | `stressorDynamics.js:740-908` (`:766-773`, `:789-796`, `:829-836`, `:842`, `:848-852`, `:877`, `:886-893`, `:911-919`); `rulingPowerCoup.js:81-124` |
| `condition.archetype` (3 written of 46) | a KIND of the record; the 46 include `corruption_exposed` (an organ-under-power fact at the TOWN grain, beside the per-institution cause of the same name — B-7) AND `rebellion`, `faction_challenge`, `government_overthrown`, `coup_suppressed`, `dominant_npc_removed`, `army_deployed`, `occupation_resistance`, `occupation_burden` — power-, person- and body-layer archetypes the survey did not list (REFUTED "the only one") | DS-CND-1 ARCHETYPE ×3 (`reconstruction`, `boom`, `flourishing`) | — | `activeConditions.js:59-483` (`:99`, `:107`, `:115`, `:123`, `:290-296`, `:306`, `:340`, `:433`, `:443`); `institutionStatusModel.js:118-120`, `:166`, `:178` (`src/domain/worldPulse/`) |
| the other reads: `token (via CRISIS_POOL_OF)` ×15 · ARITY ×2 · `worldStressor.lifecycleStage` ×5 · COUNTERFORCE / SYNERGY (dark) ×10 · `condition.severity` ×4 · DIRECTION ×2 · PROVENANCE ×2 · DURATION ×1 · FAMILY (dark) ×6 | a CONDITION, a BAND, a STAGE, a WINDOW of a record — no institution, organ, power or role in the READ (CONDITIONAL: the record's `description` / `hooks` / `reason` strings carry the engine's own body and person words; `{reason}` on COUNTERFORCE will print `${key} institutions` in the world's `INSTITUTION_CLASSES` vocabulary once wired — B-9) | 47 pools | the condition's own word | `stressorsStateProse.js:98-117`, `:160`, `:170`, `:338-352`, `:388-397`; `stressorsCore.js:34-42`, `:314-322`; `activeConditions.js:493`, `:541-547`, `:636-643`, `:670`; `stressorDynamics.js:47-55`, `:386-391` |

#### 2.5-R THE STRESSORS DESK — REFUSED

| assignment tried (survey) | refuter's verdict | evidence |
|---|---|---|
| variants 99 · 96 · 51 | REFUTED (per block; the total 246 holds by coincidence of two errors) | the leaf `stressors.generated.js`: DS-STR-1 96 (15×6 + 3 + 3), DS-STR-2 96, DS-CND-1 54 (18×3) |
| "the desk's implementation `stressorsStateProse.js` — leaf map `prose-licence-card.mjs:60-61`" | CONDITIONAL | `:60-61` maps to `stressorsStateProseCandidates.js`; the module is real, the cite names the candidates file |
| "the addendum's `holderTable.js:129-143` CORRECTED: the function opens at `:127`" | REFUTED — the addendum was exact | `:127` is a JSDoc line; `:129` opens `capturedRulingStructure`, `:143` closes it |
| `hasWatch` "town tier and up only (`:48`)" | CONDITIONAL | `:48` is a NAME list; the tier fact is the catalog's seating (`institutionalCatalog.js:1348`, `:1918`); a custom roster can seat a watch at any tier |
| `FACTION_ROLES` "the ONLY typed role vocabulary" | CONDITIONAL | `ROLE_FACTION_MAP` (`npcGenerator.js:1002-1060`) and the importance list (`:64-72`) match role WORDS; `NAME_RULES` carry role words |
| `worldState.stressors[]` "institution ref: none"; `settlement.activeConditions[]` "person: none" | REFUTED in part / CONDITIONAL | `counterforce.sourceBreakdown[].kind === 'institution'` (`stressorDynamics.js:188-275`, `sourceLabel` `:389`); `originContext.hooks[]` (`:738`); templates' `description` strings (`activeConditions.js:342`, `:445`); `dominant_npc_removed` (`:306-308`); `triggeredAt.sourceEventTargetId` (`:632`) |
| "on this desk EVERY institution-class noun is rendered on a read whose RECORD holds no institution, holder, power or person" | CONDITIONAL — true of the READS, false of the RECORDS | `contenders[].name` (`stressorDynamics.js:891-893`) is a typed power name on the record the six ORIGIN pools read |
| warehouse "no body row" · schools / taverns "no roster row" · healers "no engine row" · overseers / officials / factor / mages "none" | REFUTED | `priorityHelpers.js:60`; `institutionalCatalog.js:845`, `:981`, `:1002`, `:1527`; `npcGenerator.js:1015`, `:1035`, `:1040-1049`, `:1054-1055`, `:1523-1533` |
| "authority" → `publicLegitimacy` → court kind | REFUTED (half) | `publicLegitimacy` is not a holder token (`rulingPower.js:90`); `stability` → court (`holderTable.js:219`) holds |
| "occupier — typed, by name, unread; DS-STR-1 UNDER OCCUPATION renders 'the occupier' with the name one field away" | REFUTED for DS-STR-1 | `worldState.occupations` is written only by the world run (`pulseKernel.js:1150`); `occupied` is a birth token — the name does not exist |
| `tax_revolt` "a CAUSAL SCORE read"; `internal_conspiracy` / `unattributed` "the fail-closed branches" | REFUTED in part / CONDITIONAL | `tax_revolt` reads co-located stressors (`:842`); `popular_revolt` (`:848`) and `palace_coup` (`:877`) are fallbacks too |
| `corruption_exposed` "the only organ-under-power archetype" | REFUTED | eight more power/person/body-layer archetypes (`activeConditions.js:99-443`) |
| O-1 "at thorp/hamlet/village the word names nothing at all" | CONDITIONAL — refuted as a totality | `safetyProfile.js:30-35` prints `'local watch'` below town whenever `hasWatch` resolves |
| O-8 `[elder]` "seven times" at the listed lines | REFUTED (count and lines) | eight faces: annex 4249, 4256, 4271, 4371, 4380, 4461, 4467, 4496 |
| safe word "the watch — only for an order read; never for a pay read" | CONDITIONAL | the watch arms BOTH purses (`defenseGenerator.js:177-178`, `:244-253` "watch wages, court and gaol funding") — the bar is a ruling, not an engine impossibility |
| safe word "the muster" | CONDITIONAL | safe as a class word; "the muster roll" exists on one roster institution only |
| F1, F5, F6, F12, F21, F22, F26 as "invented" body/role words | CONDITIONAL — the layer mismatch stands; the WORDS are the record's own hook / reason strings | `VARIANT_HOOKS` `stressorDynamics.js:657-729` (`:679`, `:689`, `:690`, `:701`, `:705`, `:706`, `:717`), rides on the record as `originContext.hooks[]` (`:738`); `reason` (`:844`) |
| F10 (c) "patrols is not a watch service" | HOLDS but incomplete | `patrol` IS a `force`-class civic token (`wiringCensus.js:1303`) — B-2 |
| `src/domain/institutionStatusModel.js` | path REFUTED (lines right) | `src/domain/worldPulse/institutionStatusModel.js:118-120`, `:166`, `:178` |

### 2.6 THE WARFAITH DESK (DS-WAR-1 … DS-WAR-5 · DS-FTH-1 … DS-FTH-4; 9 blocks · 138 pools · 418 variants · 14 dm-only · 27 RESOLVED / 111 WIRING-UNRESOLVED; DS-WAR-4, DS-WAR-5, DS-FTH-4 DARK)

Sources: `warFaith.referent.survey.md` (Opus) · `warFaith.referent.refute.md` (Fable; twenty-one line cites drift 1-6 lines, none changing a verdict). Source standing (HOLDS): `muster` 7 (DS-WAR-1) · `court` 4 (DS-WAR-2, `stateOrgan: true`) · `parish` 2 (DS-FTH-1 SINK) · SOURCE-UNRESOLVED 125. **No pool reads `settlement.institutions[]`; the desk has no BODY read of its own; its one institution-NAME slot `{institution}` never fills; the WATCH kind is read by no pool; the muster's whole roster is one row.** The `{creed}` object is NONE of the four layers (a TRADITION read, `MOVES.TRADITION`) — the law needs a fifth word or an explicit exclusion.

| noun | layers (HOLDS / CONDITIONAL with condition) | reads per layer (pools) | safe class word | source |
|---|---|---|---|---|
| the army · the field force | POWER-adjacent only (whose army); NO BODY row; `army` not in the `force` civic class | `war.status.besiegingTargets.length` → `statusLabel: On campaign` (holder muster) | the muster | `holderTable.js:197`; `wiringCensus.js:1303` |
| the soldiers · the men | a person-plural; the card's refused column "a totality over persons" | `war.occupation` (`occupation.pays *`), `war.occupierPosition` | the muster (ours); **"the occupier's men" / "the soldiers"** (theirs — W12); NOT "{counterpart}'s garrison" (REFUSED) | `occupationStatus.js:95-103`; ADDENDUM 13 W12 |
| the muster | HOLDER kind (roster `Citizen militia` alone, `Muster training` on: false p 0.5) HOLDS; the "posture-rung state word" layer is CORPUS usage, not an engine row (`POSTURE_PHRASE` never says "muster") — CONDITIONAL; as the always-safe class word CONDITIONAL: the engine's OWN voice gives the word TWO referents on this desk's block (`treatyDocument.js:58` "the compelled banner still answers the muster" = the COUNTERPART's call; `:63` "no muster gathers where none is allowed" = the TOWN's arms); on a `relational` clause the possessor must be seated and `{counterpart}` may be the WRONG power; the holder's second hop resolves on the WRONG TOWN for `occupierHoldings.*` (A-6) | `war.postureState` + `war.mobilization.ticksToDeploy` → the four `mobilization:` pools; `besiegedBy` / `besiegingTargets` → muster | the muster — as a BODY word on a `mobilization:` / `statusLabel:` read, with the possessor seated | `holderTable.js:79`, `:196-199`, `:279-288`, `:599-615`; `institutionServices.js:837`; `mobilizationStatus.js:27-35`; `treatyDocument.js:58`, `:63`; `warFaithStateProse.js:279-311` |
| the banner | the engine's own word for the `relational` clause (`'the compelled banner'`) — CONDITIONAL: the 24 `<family> · <state>` rows carry NO reads and are SOURCE-UNRESOLVED; the court licence sits on the four `fraying` / `document-level` rows ONLY | `relational · <state>` (unread) | the banner on a `relational` term — licensed by no read on the family pools | `treatyDocument.js:57-61`; the census dump |
| the garrison | THREE rows claimed, TWO hold: (b) the TOWN's own BODY — bucket, `hasGarrison`, roster `Garrison`; (c) the `territorial` treaty clause (`occupation_continuation`), whose voice `:68` "The garrison keeps the walls" bakes a wall the engine's own text supplies; (a) "the OCCUPIER's soldiers — `settlementOccupation`" REFUTED as an engine row (the occupation record carries NO garrison field; it is the corpus's wording at `:3451`). Overlaps: SECURITY body; `military` role keyword; `Watch Captain`'s `linkToInst` | this desk reads only `war.occupation`, `war.occupierPosition` — neither a garrison | "the occupier's men" — never a bare "the garrison", never "{counterpart}'s garrison" | `defenseInstitutionBuckets.js:88-91`; `priorityHelpers.js:46`; `institutionServices.js:185`; `peaceTermsCatalog.js:173`; `treatyDocument.js:67-71`; `occupationStatus.js:83-104`; `corruption.js:630`; `roleCategory.js:39`; `factionRoles.js:45` |
| the walls · the gate | BODY (bucket; `hasWalls`; `hasGates`; roster `Gates (if walled)`, ALSO the toll-bar holder's row); the `walls` token maps to muster for the DEFENSE desk | NONE | no safe word on this desk — a wall sentence here is another desk's read | `defenseInstitutionBuckets.js:84-87`; `priorityHelpers.js:52-53`; `institutionServices.js:1550`; `holderTable.js:188`, `:308`; `wiringCensus.js:1302` |
| the militia | BODY (bucket; `hasMilitia`; roster `Citizen militia`) AND the sole HOLDER roster row of the muster kind | none directly; what a licensed `muster` citation RESOLVES to | the muster (class) / the citizen militia (only if resolved) | `defenseInstitutionBuckets.js:92-94`; `priorityHelpers.js:47`; `institutionServices.js:835-838`; `holderTable.js:284` |
| a mercenary company · hireling hall · free company | BODY — REFUTED as rowed: `hireling hall` is NOT in the mercenary bucket (`mercenary company · mercenary quarter · hired muscle`) and not in the charter bucket; it sets `hasMercenary` AND `hasCharterHall` — two FLAGS, ZERO buckets; a Hireling-hall town reads `hasMercenary: true` to the generator and `forces.mercenary.present: false` to the projection | NONE | the hired companies | `defenseInstitutionBuckets.js:98-104`; `priorityHelpers.js:49`, `:51`; `institutionServices.js:1002` |
| the charter hall | BODY (bucket; `hasCharterHall`; catalog `Adventurers' charter hall`); `charter` also a `hall`-class word and a treaty-instrument word | NONE | the charter hall | `defenseInstitutionBuckets.js:101-104`; `priorityHelpers.js:51`; `institutionalCatalog.js:325`, `:837`, `:1443`; `wiringCensus.js:1314` |
| the watch | BODY (bucket) AND HOLDER (the watch kind, a STATE ORGAN; roster `Professional city watch` / `Town watch`) AND SECURITY body AND `force` class AND `military` role keyword (`watch chief`, `city watch`) AND a `FACTION_ROLES` key; `professional city watch` in both buckets | NONE — no warFaith row resolves the watch kind; the noun is ABSENT from the desk's corpus (the three "the watch" hits are "the watching") | no safe use on this desk | `defenseInstitutionBuckets.js:90`, `:95-97`; `holderTable.js:79`, `:115`, `:202-212`, `:322`; `corruption.js:630`; `wiringCensus.js:1303`; `roleCategory.js:39`; `factionRoles.js:44`; `warFaith.generated.js:2447`, `:2461` |
| the barracks | BODY (keyword; roster `Barracks`); SECURITY body; W12: a housing row that licenses no "garrison" | NONE | the barracks | `defenseInstitutionBuckets.js:89`; `institutionServices.js:1555`; `corruption.js:630` |
| the hall | HOLDER: office kind (`City administration` / `City hall` / `Town hall`), a STATE ORGAN; `Town hall` ALSO the treasury holder's roster and a `hasCourtSystem` keyword; the `hall` civic class | NONE — the leaf bakes "the hall" 15 times with no read | none on this desk (the hall is the power desk's organ) | `holderTable.js:115`, `:277`, `:356-361`; `institutionServices.js:39`, `:1623`, `:1629`; `priorityHelpers.js:55`; `wiringCensus.js:1314` |
| the court | HOLDER: court kind, STATE ORGAN (roster `Courthouse`, `Multiple court buildings`); `law` class; SECURITY body; `hasCourtSystem` matches halls too; the power slot `criminalCaptureState` reaches the organ by rule — CONDITIONAL on the threshold: the court's birth INTERESTED mark fires at `adversarial`, a rung the generator's own comment calls not-a-capture | the four DS-WAR-2 rows via `termLines` / `fraying` / `yearsRemaining` (card: `a STATE ORGAN (interested where the town is captured)`) | the court — for a treaty read, as the record's KEEPER, never as an agent (and never "bought" from a birth INTERESTED mark at `adversarial`/`equilibrium`) | `holderTable.js:79`, `:115`, `:141`, `:221-223`, `:324-330`, `:661`, `:677-698`; `institutionServices.js:89`, `:1117`; `wiringCensus.js:1311`; `corruption.js:630`; `rulingStructure.js:762-767` |
| the seat | FOUR rows: the FAITH seat (`state.patronRef`); the `political` treaty family's installed seat (`puppet_seat`, voice `:72-76`); the governing seat (`governingName`, the power desk's `{seat}`); the `hall` civic-class keyword — the first two are authored on THIS desk and both say "the seat"; ALSO `termLabel('puppet_seat') === 'installed seat'`, so `{term}` prints the word through a proper slot (A-4) | `term.family === political`; `faith.ranks[].isPatron` (STANDINGS / LEGITIMACY / FALL) | the installed seat (treaty) / the patron's place (faith) | `religionState.js:620`; `peaceTermsCatalog.js:174`, `:408`; `treatyDocument.js:72-76`; `rulingStructure.js:787`; `wiringCensus.js:1314` |
| the occupier · the occupation authority | POWER — CONDITIONAL: TWO READERS, TWO REFERENTS, not "two spellings of one referent": `occupationStatus.js:96` names the OCCUPIER SETTLEMENT; `warStatus.js:318-335` reads the OCCUPIED town's `powerStructure.governingName` (the conquest-installed faction); they coincide only where the conquest seated the occupier's own faction as `isGoverning`; the literal `'occupation authority'` renders as a name | `war.occupation` → `occupation.pays *`, `statusLabel: Occupied`; `war.occupierPosition` → `occupierHoldings.*` | `{counterpart}` (the named power) or "the occupier" | `occupationStatus.js:96`; `warStatus.js:318-335` |
| the ruling structure · the government | POWER — `mandateGovWeight`'s regex (theocracy · monarch / feudal / autocra / imperial / … / king / queen / emperor); the reader is GOVERNMENT-SCOPED and returns `null` for a merchant council or republic; the class word and the faction NAME are the same string | `faith.mandate.phrase` → the three `MANDATE:` pools | the ruling structure | `religionState.js:686-691`, `:789`; `rulingStructure.js:787`, `:792`; `warFaithStateProse.js:580-583` |
| the faction · the house | POWER (`factions[]{faction, isGoverning, captureState}`) — none on this desk (no `{faction}` slot); "house" IS on the corpus for FAITH FABRIC (`:4886`, `:4711`) while `brokeragePatronage.js` types `houseName` for a brokerage HOUSE under a patron (A-12) | NONE | none — a faction word here is another desk's read | `holderTable.js:64-66`, `:129-143`; `warFaithStateProse.js:139-145`; `brokeragePatronage.js:288-289` |
| the treasury | HOLDER: treasury kind, STATE ORGAN — used in FAITH prose as "the faith's treasury" (a state-organ kind word attached to a creed, on `templeWealth`, a field with NO WRITER anywhere) | NONE | none on this desk | `holderTable.js:79`, `:115`, `:272-278`; `warFaithStateProse.js:95-99`; annex `:4797` |
| the market | HOLDER: market kind (roster `Market square`); the `market` civic class | NONE | none on this desk | `holderTable.js:79`, `:315`; `institutionServices.js:1064`; `wiringCensus.js:1310` |
| the granary · the stores · the wagons | BODY (stock / building) with NO holder (the `granary` token WITHDRAWN); `storehouse` vs `store` classes | NONE — the DS-WAR-2 `economic` prose reaches for it | none on this desk | `holderTable.js:157-161`; `wiringCensus.js:1309`, `:1322`; annex `:3528`, `:3534` |
| the rolls · the books · the ledger · the records | the `[ledger]` ANGLE is a STANDPOINT ("the clerk's view — what the books, rolls and counts show"); `ledger` WITHDRAWN as a holder token; the angle palette's own names (`ledger`, `elder`) are HOLDER-kind vocabulary, which is how the shipped prose slides from the standpoint into the organ (F-7) | every pool carries an angle | the record / the books as a STANDPOINT only — never as a holder | annex `:122`; `holderTable.js:79-80`, `:157-161` |
| the elders | HOLDER: elders kind (`Record of custom`; roster `Household elder · Village elder · Village headman · Town council`); a `government` ROLE keyword; the `[elder]` ANGLE (20 variants on this desk); the engine EMITS "the village elders" / "the household heads" as a council label on small tiers (elsewhere) | NONE (the angle only) | the `[elder]` angle only — never "the elders" as a body | `holderTable.js:79`, `:333`, `:336`; `roleCategory.js:34`; annex `:125`; `governanceNarrative.js:101-105` |
| the parish | HOLDER: parish kind (`Register of the dead · Central register · Records`; roster `Parish burial grounds · Cemetery network · Parish church`); `parish` also a `temple`-class word; `Parish church`'s `Records` is on: false p 0.5 and `Parish churches (2-5)` offers `Record keeping`, which is DELIBERATELY in no kind's list — CONDITIONAL: "a town-tier town holds the parish record only through its burial grounds" is unverified per tier (PLAUSIBLE) | `faith.piety.trend` + `faith.unaffiliated` → the two `SINK:` pools (LICENSED) | the parish — for a piety / sink read only | `holderTable.js:79`, `:226-227`, `:257-259`, `:296-302`; `institutionServices.js:1161-1170`, `:1411-1428`; `wiringCensus.js:1312` |
| the toll-bar · the census · the road · the tradition | HOLDER kinds; `tradition` has no institution anywhere; `road` also the `road` civic class — and the pilgrim pools speak of "the pilgrim road" with no road read | NONE | none on this desk | `holderTable.js:79-80`, `:338-348`, `:350-355`; `wiringCensus.js:1313`; annex `:4818` |
| the treaty · the instrument · the document | an INSTRUMENT held by the court organ (`TreatyDocument`; sides `receiver · giver`); `victorId`/`loserId` are `receiverId`/`giverId` "on a WR-10 sale exactly as on a war settlement" — a defect the desk declares | `doc`, `doc.termLines` → the three `document-level:` pools | the instrument / the document | `treatyDocument.js:35-91`, `:359-361`; `treatyOrientation.js:166`, `:171-176`, `:200-207`; `warFaithStateProse.js:420-426` |
| the term · the clause (`{term}`) | an OBLIGATION on the instrument (catalog types labelled at `:398-430`; 13 families; the corpus authored 7 + a floor — CONDITIONAL: the count 26 not re-counted); the LABEL TABLE fills the proper slot with `installed seat`, `temple restitution`, `pilgrim's road`, `right of mission`, `shared rite` — a seat word, a temple word and a road word arriving THROUGH the slot (A-4) | `term`, `term.family`, `term.complianceState` → the 24 `<family> · <state>` pools | the clause / the term | `peaceTermsCatalog.js:159-322`, `:328`, `:398-430` (`:408`, `:416-418`, `:420`); `warFaithStateProse.js:323-326`, `:379-389`, `:798` |
| the creed · the faith · the rite (`{creed}`, `{rival_creed}`) | NONE of the four layers — a CULTURE object (`{deityRef, name, niche, share, standing, legitimacy, isPatron}`), `MOVES.TRADITION`'s licence; `patron` and `faith` are both `temple`-class words, so the observance and the building are ONE class to the projector | `faith.ranks`, `faith.patron.rankAxis`, `faith.piety.*`, `faith.contested` → DS-FTH-1/3 | the creed · the observance · the rite | `religionState.js:620`; `moveGrammar.js:47`; `wiringCensus.js:1312`; `warFaithStateProse.js:784-797` |
| the patron | THREE rows: (a) the seated creed (`state.patronRef`) — a faith standing; (b) a brokerage house's PATRON (`genesis · captured`) — POWER, whose visibility follows the HOUSE's legality (`covert: house.legality === 'illegal'`), not the power (A-7); (c) `rankAxis` a pantheon word; the holder table reads the brokerage patron as the `controlled` arm of a STANDING | (a) `faith.patron.rankAxis`, `faith.ranks[].isPatron`, `hasPatron`; (b) and (c) unread here | the patron creed (faith) — never bare "the patron" beside a power sentence | `religionState.js:620`, `:640`; `brokeragePatronage.js:60`, `:228`, `:293`, `:296`; `warFaithStateProse.js:476`; `holderTable.js:665-666` |
| the temple · church · shrine · cathedral · monastery (`{institution}`) | BODY — the six per-tier `Religious:` rosters (`Wayside shrine` · `Parish church` · `Priest (resident)` · `Graveyard` · `Parish churches (2-5)` · `Monastery or friary` · `Cathedral (10,000+ only)` · `Great cathedral` …); the parish rows are ALSO the holder-organ; `hasChurch` is a substring match TRUE for a resident priest or a wayside shrine with no church building | NONE — `{institution}` never fills ("no temple-NAME producer") | none on this desk — a temple BODY sentence here has no read behind it | `institutionalCatalog.js:41-42`, `:291-292`, `:762-785`, `:1259-1267`, `:1795-1828`, `:2363-2371`; `priorityHelpers.js:65`; `warFaithStateProse.js:131-132` |
| the parish church · the graveyard / burial ground / cemetery network | BODY and (coin-flip) ORGAN: `Parish church`'s `Records` on: false p 0.5; the burial rows are the RELIABLE parish holders (`Register of the dead` on: true p 0.8; `Central register` on: true p 0.9) | the resolution of a `parish` citation on the two SINK pools | the parish register | `institutionServices.js:1161-1166`, `:1411-1416`, `:1423-1428` |
| the clergy | ROLE vocabulary (`religious` keywords) and a `temple`-class word; no institution row, no field — CONDITIONAL: `governanceNarrative.js:128-130` EMITS `'the clergy'` as the healer label where a church / cathedral / parish row is on the roster (a roster-conditioned label the engine does use); the leaf uses it 12 times | NONE | none on this desk (no roster read) | `roleCategory.js:41-44`; `wiringCensus.js:1312`; `governanceNarrative.js:128-130` |
| the congregation · the faithful · the households | a person-plural (the card's refused column); banded by `faith.ranks[].share`; `whoIsCounted` is OPEN BY LAW ("the population is a NUMBER and no roll of persons exists anywhere in the engine") | `shareBandLabel` | the following · the share of the town (a band, never a body) | `faithPanelModel.js:60-65`; `institutionTable.js:211-212` |
| the house · the benches · the building · the roof · the stonework · the fabric | BODY (the fabric of `{institution}`, which never fills); no built-fabric field (`MOVES.OBJECT` licences one) | NONE | none | `warFaithStateProse.js:131-132`; `moveGrammar.js:42` |
| the faith's treasury · the coffer · the tithe · the councils (of a faith) | `templeWealth` has NO WRITER (7 hits, all comments or block titles); no faith-council field; `council` a `hall`-class word and a `government` role keyword | NONE (the `TEMPLE WEALTH:` and `ENDOWED` pools are unroutable) | none | `warFaithStateProse.js:95-99`; `warFaith.generated.js:4604`, `:5574`; `dossierMounts.js:504-505`; `wiringCensus.js:1314`; `roleCategory.js:34` |
| cults[] · the lesser rites | a faith standing (`config.cultDeitySnapshots` — every NON-PATRON embedded deity); `cult` also a local `standing` and a pantheon tier | `faith.cults.length > 0` → `CULTS:` | the lesser rites | `faithPanelModel.js:192`; `religionState.js:150-155`; `pantheon.js:79-80` |
| the ruler | the MANDATE phrases' own word, licensed ONLY where `mandateGovWeight > 0` — CONDITIONAL: the engine types nothing named "the ruler"; the regex admits PERSON-shaped words (monarch · king · queen · emperor), so the referent is the government CLASS; safe ONLY inside the authored phrase the block "surrounds, never rewrites"; a writer's own "the ruler" outside it is a role word the engine does not emit | `faith.mandate.phrase` → the three `MANDATE:` pools | the ruling structure | `religionState.js:686-691`, `:789`, `:791-793`; annex `:4517-4518` |
| the pilgrim road · the inns | no pilgrim-season record; `road` a holder kind (`Listening post` / `Waystation`); `{season}` never fills | NONE (`PILGRIM SEASON:` pools unroutable) | none | `holderTable.js:350-355`; `warFaithStateProse.js:131-132` |
| the captain · the priest / high priestess · the reeve · the elder · the factor · the magistrate · the mayor / lord mayor · the guildmaster · the clerk(s) · the sexton · the bearers · the shepherd / observers / watchers | ROLE words: `FACTION_ROLES` (`Watch Captain` `linkToInst: /watch\|garrison\|barracks\|militia/` — spans the MUSTER's roster row, A-8; `High Priestess`; `Guildmaster`; `Lord Mayor`); `ROLE_CATEGORY_KEYWORDS`; `Priest (resident)` and `Village reeve` are institution ROWS named by role words (the priest row keeps NONE of the parish services; the reeve row is a treasury holder); `magistrate` also a SECURITY body and a `law`-class word; the clerk a STANDPOINT word; the sexton AND the bearers appear only inside service DESCRIPTION strings (`Register of the dead`; `Funeral procession`) | NONE — no warFaith read reaches a role; the ANGLE `[elder]` only | none safe on this desk | `factionRoles.js:40-61` (`:42`, `:45`, `:48`, `:56`); `roleCategory.js:33-52` (`:34`, `:38`, `:43-44`, `:50`); `institutionalCatalog.js:778`; `institutionServices.js:1194-1199`, `:1346`, `:1413`, `:1427`; `holderTable.js:277`; `corruption.js:630`; `wiringCensus.js:1311` |
| the ranks · {counterpart}'s company · the harbour / the roads · the coffer · a clerical post · the hall (asked) · the impairment (a blockade) (missed) | the ranks: a person-plural of the banner (the voice's "the ranks mutter of desertion" is DS-DEF-5's cell, R-viii′); "company" a COALITION here and the mercenary bucket's keyword elsewhere (feasibility pools SOURCE-UNRESOLVED); the harbour / roads (DS-WAR-5, dark) the `road` kind / `hasPort` / `road` class; the coffer a treasury-class word on `{institution}`; a clerical post a ROLE placement on a field with no writer; the hall as an ADDRESSEE (DS-WAR-5, dark); "the impairment" borrows the engine's `impairments[]` word for a blockade (a toll-bar field) | as listed | — | `warFaith.generated.js:532`, `:541`, `:1072`, `:2461`, `:2941`, `:5027-5073`, `:5118`; `treatyDocument.js:59`; `defenseInstitutionBuckets.js:99`; `holderTable.js:80`, `:184-185`, `:354`; `priorityHelpers.js:61` |
| the reads by layer: `statusLabel: On campaign` (muster) · `Occupied` · `At war` · the two `mobilization: climbing` (muster) · `occupierHoldings.stretchedThin / .strengthened` (muster) · the four court rows · the two parish rows · the rest | BODY-adjacent (a force's state held by the muster) on `On campaign` and the two `climbing` pools; CONDITIONAL on `Occupied` and `At war` — their keyed branches are a POWER standing (`if (occupation)`) and a residue (`otherwiseAtWar`), muster-LICENSED only through sibling-branch tokens (`sourceOfRow` takes the STRONGEST field) — a citation incidental to the read; `occupierHoldings.*` is about ANOTHER power's position and the holder resolves on the OCCUPIED town's militia for the OCCUPIER's record (A-6); ORGAN-UNDER-POWER on the four court rows; ORGAN (no power) on the two parish rows; NO LAYER on the other 125 (arm A13: no record holder may be named) | as listed | — | `warFaithStateProse.js:197-207`, `:251-262`, `:279-311`, `:398-402`, `:436-448`, `:537-543`; `holderTable.js:199`, `:510-514`, `:599-615` |

#### 2.6-R THE WARFAITH DESK — REFUSED

| assignment tried (survey) | refuter's verdict | evidence |
|---|---|---|
| "{counterpart}'s garrison" as the safe word for the occupier's soldiers; the garrison row (a) "the OCCUPIER's soldiers — `settlementOccupation`" | REFUTED | `occupationStatus.js:95-103` returns `{occupierName, statePhrase, resistancePhrase, pays, burdened}` — NO garrison field; W12 "an occupation never entails a garrison present"; row (a) is the corpus's wording (`:3451`) |
| "the guard (a body word of the garrison bucket, as ADDENDUM 11 rules)" | REFUTED | `governanceNarrative.js:86-99` (`deriveGuardLabel` FALLBACK `'the guard'` at `:98`), `:134-149` (fallback `:148`); `safetyProfile.js:300` "There is no meaningful guard presence."; ADDENDUM 13 item 5 |
| `Hireling hall` as the mercenary bucket's roster | REFUTED | the bucket is `['mercenary company', 'mercenary quarter', 'hired muscle']` (`:98-100`); the row is in two FLAGS (`priorityHelpers.js:49`, `:51`) and NO bucket |
| `{counterpart}` on a treaty pool = "whose call it answers" (the document's other side) | REFUTED | `warFaithStateProse.js:795`, `:930-933` fill from the occupier, else the single siege counterpart (`:690-694`); `doc.victorName` / `loserName` (`treatyDocument.js:359-361`) are never read into the slot |
| the occupier "two spellings of one referent" | CONDITIONAL — two readers, two referents | `warStatus.js:318-335` reads the OCCUPIED town's `governingName`; `occupationStatus.js:96` the occupier settlement |
| O-6 "the muster's three rows are the same referent's layers, unlike the watch" | REFUTED | `treatyDocument.js:58` vs `:63` (two referents in the engine's own voice); A-6 (the wrong-town holder); the "state word" layer is corpus |
| PERSON — the GROUND "no typed NPC→institution edge exists anywhere in the estate" | REFUTED (the CONCLUSION holds on product scope) | `settlement.schema.js:444`, `:953` (`institutionId`); written `npcOps.js:218`, `:245`; `npcs.js:229`, `:239`; `successorNpc.js:39`; read `corruption.js:648-650`, `:683-689`; `corruptionImpair.js:149-157`; `npcAgency.js:766`, `:810`; `mutateEntities.js:827` — a WORLD-RUN seat outside the institution table |
| "captured" as one reading of the ladder | REFUTED — THREE thresholds | `holderTable.js:141` (`adversarial` counts at birth); `:661` (world capture at `corrupted`/`capture`); `rulingStructure.js:762-767` ("`adversarial` … asserts enforcement is WINNING") |
| the corruption impairment = the covert channel | CONDITIONAL | `corruption.js:683-689`: an unexposed corrupt NPC HOMED there makes the body covert with NO impairment |
| "the banner" licensed on the family pools (holder court) | CONDITIONAL | the 24 family rows carry no reads; the court licence is on the four doc/fraying rows only |
| "the muster" as a posture-rung STATE word | CONDITIONAL — corpus, not engine | `mobilizationStatus.js:27-35` never says "muster" |
| `occupierHoldings.*` "BODY-adjacent … holder muster" | HOLDS as referent · REFUTED as holder | `holderTable.js:199`, `:599-615`: the holder is the OCCUPIED town's militia for the OCCUPIER's record (A-6) |
| `statusLabel: Occupied` a muster-licensed BODY-adjacent read | CONDITIONAL — incidental licence | the keyed branch is `if (occupation)` (`:203`), a POWER standing; the muster tokens are the sibling branches' (`sourceOfRow` `:510-514`) |
| the clergy "no row, no field; asserts a body the engine does not count" | CONDITIONAL | `governanceNarrative.js:128-130` emits `'the clergy'` under a roster condition |
| "the ruler is the ruling STRUCTURE, not a person" | CONDITIONAL | the regex admits person-shaped words (`religionState.js:689`); safe only inside the authored phrase |
| F-1 row 10 `economic · honored` "(holder court)" | CONDITIONAL — the layer error stands; the court-held ground does not | the row is SOURCE-UNRESOLVED |
| "a town-tier town holds the parish record only through its burial grounds" | CONDITIONAL (PLAUSIBLE, unverified per tier) | `institutionalCatalog.js:1259-1267` |
| "26 catalog types" | CONDITIONAL (not re-counted) | `peaceTermsCatalog.js:159-322` |
| cites: `factionRoles.js:44-46/47-49/51/56-58` → `:41-43/:44-46/:47-50/:55-57`; `wiringCensus.js:1310-1315`, `:1323` → `:1309-1314`, `:1322`; `roleCategory.js:37-38/52` → `:38-39/:50`; annex `:4654/:4656/:4709/:4728/:4772/:4779/:4757/:4813` → `:4657/:4659/:4711/:4725/:4770/:4775/:4755/:4812` | cite REFUTED, content HOLDS | twenty-one drifts, re-derived |

