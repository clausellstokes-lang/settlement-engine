### 2.3 ECONOMY (DS-ECO-1..12, DS-SUP-1..3; 105 census rows; packets `economy.survey.md` §3.1..3.20, `economy.refute.md`)

Nine cross-cutting refutations (economy refute §A) move most verdicts and are cited by letter: **A.1** the exploitation lens's `{institution}` is the static catalog row's processor carried unchanged (`src/generators/resourceGenerator.js:140`, `:166-181`), so `Mill` fills over a town with no mill; **A.2** `Import-Dependent` and `Secure` read the RESIDUAL deficit after imports and magic are subtracted (`src/generators/foodGenerator.js:318-330`); **A.3** a route token does not fix the terrain and a terrain does not fix the route (`src/generators/terrainHelpers.js:24`; `src/generators/generationContext.js:195-205`); **A.4** `river` weighs like a seaport in the throughput tally (`src/domain/spatial/tradeFlow.js:112-115`, `:142`); **A.5** the shipped crier line INVENTS a seasonal cause (`src/domain/display/marketPrices.js:213-218`); **A.6** two terrain-fertility tables disagree (`foodGenerator.js:37-41` vs `src/data/geographyData.js:68`..`:638`); **A.7** `collapsing` HAS a producer (`src/domain/supplyChainState.js:443-447`) and `unexploited` names two records; **A.8** `isolated` is three different amounts of nothing; **A.9** the DS-ECO-2 deficit tile is the residual AFTER the road (`src/domain/display/dossierViewModel.js:113-114`); **A.10** the famine pool is unreachable from the shipped label (probe: `'Deficit — Active Famine' -> null`, `economyStateProse.js:394-401`).

| # | noun | name class (members) | ENTAILS | NOT ENTAILED | ENGINE CONTRADICTS | source |
|---|---|---|---|---|---|---|
| E-1 | `{access}` | `ACCESS_NOUN` road · river · port · crossroads · pass (`src/domain/display/stateProse/economyStateProse.js:173-179`); `isolated` has NO fill (`:167-170`); from `config.tradeRouteAccess` (`resolveConfig.js:195`) | road: an overland way (HOLDS `src/domain/tradeRouteSemantics.js:88`) · river: water, and a way along it (HOLDS `:87`; `generationContext.js:198-200`) · crossroads: more than one way, meeting (HOLDS `:85`; `tradeGoods.js:94`) · port: water (HOLDS) and a harbour edge / the sea (CONDITIONAL: the SEA only where `terrainType !== 'riverside'`, `generationContext.js:195-197`; a harbour only where `deep_harbour` is available or `hasPort` resolves, `resourceSemantics.js:57-62`) · pass: high ground on both sides (CONDITIONAL: only where `config.terrainType === 'mountain'`, since an explicit `plains` override survives, `resolveConfig.js:180`, `:204`; "closed part of the year" is an annualised rate, `tradeRouteSemantics.js:34-47`, `:76`, `:90`, never a live winter state) | width; surfacing; upkeep; patrols; present traffic (DS-ECO-3's ledger); a toll; a bridge or ferry (`naturalFeatures` is consumed only by the undercity anchor, `staticComponents.js:289`); age; who built it | `mountain_pass` cannot arise on a generated town (`resolveConfig.js:29-37`) · `isolated` contributes no fill by design · the access word is structural configuration, never historical (annex `:801-802`) · `isolated` is "cannot import anything" to `tradeGoods.js:53-56` and a minor-route trickle to the food model (`foodGenerator.js:168-176`, `:293-296`): the only safe spelling is no approach to name (A.8) | survey 3.1; refute B 3.1 |
| E-2 | `{complexity}` | eleven authored phrases `COMPLEXITY_NOUN` (`:212-224`) over `COMPLEXITY_LABEL` (`src/domain/display/labelBands.js:130-142`) | spread of trades / broad base: more than one earning line (HOLDS: `prosperity.js:296-303`, six or nine income lines) · handful / narrow: few (CONDITIONAL: "fewer than its rung expects", never an absolute count; CONCENTRATED is a city with fewer than six) · market trade: a market-mediated economy (CONDITIONAL: only where the matching institution is a LAWFUL market, since `hasMarketInst = hasInst('market','trading','merchant','guild')` `economicState.js:882` is satisfied by a `Black market` or a `Thieves' guild`) | prosperity (the label reads tier, income count, export count and a flag, `economicState.js:878-883`); guilds; a square; headcount; tenure | the band collapse is not injective (`Subsistence` names three labels, `Diversified` two, `labelBands.js:112-115`, `:149-161`); the display string is a key, never a fill (`:902-907`) | survey 3.2; refute B 3.2 |
| E-3 | `{season}` | spring · summer · autumn · winter (`dossierViewModel.js:304`); the raw token (`:908`) | a position in the year, one of four, thirteen weeks each, ordered (HOLDS: `:303-304`; `foodStockpile.js:69`, `:428-429`; present only under seasons-on, `:330-332`) · weather is a separate three-value `seasonalEvent` (HOLDS `:298-302`) | weather; temperature; a harvest; a sowing; hunger; a festival | "winter is hard here" is a `hard_winter` claim; a bad harvest is `drought`, not `autumn` | survey 3.3; refute B 3.3 |
| E-4 | `{good}` (the export column, shared bag) | chain OUTPUT labels in `primaryExports` (`economicState.js:791-795`, `:863`), transit-marked ones skipped (`:848-853`) | a transit-marked export only passed through (HOLDS: `src/generators/computeActiveChains.js:748-751`; the marker fires on crossroads / port / river routes while `isEntrepot` is crossroads or port + institution, and DS-ECO-12 requires both `:528`) · the slot is DARK in practice (HOLDS: 0 of 48 fills, all 99 export labels capitalised, `:26-27`) | volume; price; a buyer; a route; who makes it; that it is the largest export | (see REFUSED E-R5) | survey 3.4; refute B 3.4 |
| E-5 | `{good}` (the exploitation lens's own bag) | `finalProducts[0]` of the leading row (`:990`; `src/data/resourceChains.js`) | the finished article at the far end of ONE resource line (HOLDS as a CATALOG fact about the line, not a fact that the article is made here) | that the town exports it; that it is sold; a quantity | using the shared-bag `{good}` here is finding R-DST-ROLE (`:981-986`) | survey 3.5; refute B 3.5 |
| E-6 | `{resource}` | `rawResource` of `RESOURCE_CHAINS` (23, `resourceChains.js`); typed semantics `src/domain/resourceSemantics.js:43ff` | the material nature of each word (timber = wood; ore = metal-bearing rock; stone; clay = earth; fish; grain = a field crop; wool = from a living animal; hides = from a dead one; glass sand = sand; herbs = plants; gemstones = stone; grapes = fruit; livestock = living animals; flax = a plant fibre) (HOLDS: definitional; the commodity tokens agree, `resourceGenerator.js:22-34`, `:78-86`) · the resource is present and available (HOLDS: `:47`, `:55`, `:136-139`) · the processing verbs (milled, pressed, fired, cut, smelted...) (CONDITIONAL: what the material would NEED, never what this town does; "smelted here" needs a Smelter on the roster, which `fullyExploited` never guarantees, A.1) | quantity; quality; depth; tenure; owner; price; that anyone works it (the exploitation bucket) | a positional resource cannot deplete by the random roll (`resourceSemantics.js:57-62` etc.; the DEPLETE_RESOURCE event path still writes `depleted` for any key, `:342-356`, and a positional row's depleted description is "obstructed or unsafe", so "the harbour is obstructed" is licensable under `condition: depleted` and "silted" is not) · exhaustible = `recoveryMode: 'manual'`, never refills on its own · renewable = `'natural'`, never "worked out for good" · `unexploited` (bucket) = never worked, not stopped (annex `:1491-1495`) | survey 3.6; refute B 3.6 |
| E-7 | `{institution}` (the processing house / the impaired house) | `processingInstitutions[0]` of the leading row (`:991`) or `readings.impairedInstitution` (`:997-1000`; `src/components/new/tabHelpers.js:4-57`) | the definitional verbs (mill grinds; sawmill cuts timber; mine takes ore out; quarry cuts stone; smelter melts; tannery makes leather; weavers make cloth; potter fires clay; glassblower melts sand; apothecary compounds; fishmonger sells fish; vintner makes wine; butcher kills and cuts; salt works wins salt; caravanserai lodges a caravan; market is where goods change hands) (CONDITIONAL: only where the name is a ROSTER institution; on the impaired-service lens it is (`economicState.js:692-720` writes `institution: instName` from the roster) except `Access to external mill`, a `TRADE_DEPENDENCY_NEEDS` key (`src/data/economicData.js:9-20`) that passes `properFill`; on the exploitation lens only the ABSENCE reading of the `unexploited` pool, A.1) | size; staffing; condition; age; busy; owner; profit; a guild; a building | no condition, age or wear anywhere the desk reads (the economy analogue of built walls keep standing; the only place a house can be "gone" is the calamity ruin filter, not on this desk) · `Bakers (5-15)` is refused by `properFill` (`:326-329`) · a `{resource}` seam takes a singular verb (`:287-316`) | survey 3.7; refute B 3.7 |
| E-8 | the granary (DS-ECO-2) | `well stocked` / `stocked` / `thin` / `nearly empty` over `storageMonths / capacityMonths` (`dossierViewModel.js:336`); building flag `hasGranary` (`priorityHelpers.js:63`) | a store of food held against a later need (HOLDS: `foodGenerator.js:159-165`; capacity `foodStockpile.js:185-195`) · the band is a fraction of what THIS town can hold, never an absolute (HOLDS `:333-336`) | a building; a door; a keeper; a queue; sacks; months to last (`lastsUntil` `:341-347`); why the level is where it is (annex `:847-848`) | a town with NO granary building still has a granary reading: capacity falls through to 1.5 (thorp / hamlet) or 2.0 months (`foodStockpile.js:190-193`), so "the granary doors" asserts a building the roster may not carry; the `store` / `storehouse` civic split exists for this (`wiringCensus.js:1304-1308`) | survey 3.8; refute B 3.8 |
| E-9 | the prosperity rung | `PROSPERITY_TIERS` seven (`src/data/constants.js:92-100`); readings `bandLadders.js:52-60` | an ordered position on a closed ladder plus the authored reading (HOLDS; `Wealthy` only through `deriveProsperityLabel`, `prosperity.js:244-246`; `Subsistence` only on an isolated thorp / hamlet, `:280-286`) | a cause; a direction; a history; a comparison; coin; countable reserves | a derived output with no provenance trail: every variant is plain description (annex `:1166-1174`); `Affluent` is not a rung (`:1162-1164`); an unrecognised spelling renders nothing (`:364-372`) | survey 3.9; refute B 3.9 |
| E-10 | the food-security rung | six labels (`foodGenerator.js:341-359`) | Surplus: raises more than it eats (HOLDS: `surplusPct` from local production only, `:272`, `:331`, `> 40` `:353`) · Pressured: a small standing gap (HOLDS: residual 5-15%, `:350`) · Deficit: a large standing gap (HOLDS: residual > 40, `:344`) | a named cause (a route clause only where a route or chain field carries the break, annex `:1223-1226`); deaths; a season; a blockade | the label is a RATIO reading, not a stock reading (`:330`); the blockade states override the ladder (`economyStateProse.js:387-396`); (see REFUSED E-R7..E-R9 for Secure, Import-Dependent, Active Famine) | survey 3.10; refute B 3.10, A.2, A.10 |
| E-11 | the export posture word | six statuses (`src/domain/display/exportPosture.js:20-27`, `:56-62`) | none: zero exports (HOLDS `:58`) · established: two or more (HOLDS `:62`) · vulnerable: the access token is `isolated` (HOLDS `:60`) · limited: exactly one (CONDITIONAL: one export LINE, counted over strings after `toArray` `:33-36`, which may be a service label or a `(transit)` good; never "one good it makes") · entrepot: goods go out that came in (CONDITIONAL: only where a `(transit)`-suffixed export is present) | value; buyers; reliability; that a route was ever cut (annex `:1409-1414`: structural clauses only) | `import_dependent` has NO producer: five arms, no sixth (`:56-62`; `economyStateProse.js:622-630`) | survey 3.11; refute B 3.11 |
| E-12 | the live trade-flow band | `shortage` / `adequate` / `surplus`; magnitude; dependency (`tradeFlow.js:75-76`; `tradeFlowEconomics.js:65-81`) | a measured throughput on the spatial ledger (CONDITIONAL: "as of the last tick the tally was advanced": `tradeFlowEconomics.js:100-113` reads `rec.in` / `rec.out` raw and decay applies only when `advanceTradeFlowTally` runs, `tradeFlow.js:186-192`) | a reason (annex `:902-906`: drawing WHY would let the dossier invent a war); goods scarce in the market (a different ledger); a caravan; a season | `SHORTAGE x not trade-dependent` can never key (`tradeFlow.js:252`; measured 0 of 361 points, `:583-597`) · an unrecognised band is null, not adequate (`:605-611`) | survey 3.12; refute B 3.12 |
| E-13 | the canonical supply-chain status | seven (`supplyChainState.js:22`, `:185-187`); legacy remap `:174-181` | scarce: producing below normal (HOLDS: `impaired` -> scarce `:179`; pressure >= 0.65 `:455`) · substituted: running on a prop (HOLDS: `magically_sustained` -> substituted `:180`) · captured: a faction takes rents (HOLDS, vacuous: no producer anywhere; `:443` only propagates it; unlicensable on any shipped town) · strained: running with no slack (CONDITIONAL: "running, under a recorded pressure or a route dependency": `vulnerable` -> strained `:178`; regional pressure 0.25-0.65 `:456`) · collapsing: failing under compounding pressures (CONDITIONAL: "multiple" only on the `severeCount >= 2` arm `:444`; `:447` fires on `scarce && maxSeverity >= 0.8`, ONE pressure) | a named cause without its field (annex `:1297-1300`) | an unknown status silently canonicalises to `stable` (`:199-202`); (see REFUSED E-R10, E-R11 for `stable` and `blocked`) | survey 3.13; refute B 3.13, A.7 |
| E-14 | the entrepôt | `isEntrepot` and the `' (transit)'` suffix | goods that leave were not made here (CONDITIONAL: the `(transit)`-marked members only; `isEntrepot` alone is `route === 'crossroads'`, `tradeGoods.js:94`) | warehouses; a yard; a clerk; a merchant class; measured transit volume; a toll | `deriveIsEntrepot` is a crossroads route, or a port carrying an institution named `international trade` / `warehouse district` (`tradeGoods.js:90-97`); `transit` is a slice (`:162`); the `Entrepôt Trade` income desc asserts "warehouse fees" on every crossroads town+ (`:139-144`) | survey 3.14; refute B 3.14 |
| E-15 | the shadow-economy capture tier | three tiers >= 30 / >= 15 / >= 3 (`economyStateProse.js:558-564`) on `blackMarketCapture` | a share of trade is not on the rolls, banded (CONDITIONAL: the off-book INCOME LINE exists only when `blackMarketCapture > 10`, `economicState.js:319`; at the minor tier 3-10 there is a shadow reading and NO criminal income line, so a face must not point at the income mix there) | organisation; a guild; a named faction (`{faction}` unfilled on this desk `:910-921`); deaths; violence; who profits | an ESTIMATE off the criminal slider plus per-stress bonuses, capped at 80 (`safetyProfile.js:619-638`); below 3 is no surface; missing is null (`:545-554`) | survey 3.15; refute B 3.15 |
| E-16 | the service catalog and its absences | eleven category words (`src/domain/display/servicesDisplay.js:25-30`) against six tier expectation lists (`:15-22`) | an absence entails only that the roster does not fill that category at a tier that expects it (HOLDS `:48-53`; `magic` / `employment` / `criminal` are never expected) | that anything was lost ("the healer left", annex `:1696-1700`); the reason a category is impaired (DS-SUP-2's library) | the expectation is tier-indexed only | survey 3.16; refute B 3.16 |
| E-17 | the tier word | six (`constants.js:3`, ranges `:7-14`) | a population band (HOLDS: `resolveConfig.js:89-97`; boundaries agree with `:26-35`) · a list of expected categories (HOLDS `servicesDisplay.js:15-22`) | walls; a market; a garrison; a charter; a mayor | a `metropolis` pool exists because completeness at that rung is itself a fact (annex `:1743-1746`); it licenses "has everything" for no other tier | survey 3.17; refute B 3.17 |
| E-18 | the terrain word (DS-ECO-11) | seven producible (`geographyData.js:50`; route table `economyStateProse.js:647-655`) | Coastal: the sea (HOLDS `:53`; `generationContext.js:195`) · Riverside: fresh water (HOLDS `:130`) · Mountain: rock and height (HOLDS `:212`) · Forest: trees (HOLDS `:314`) · Plains: open, arable ground (HOLDS `:422`; capacity 1.5 / 1.0 in both tables) · Hills: slope (HOLDS `:527`) · Desert: little water (HOLDS `:625`) · salt and fish in the allowed roster (CONDITIONAL: present only where `nearbyResources` carries them and they are not depleted, `resourceGenerator.js:59-68`) · a comparative fertility clause between two terrains (CONDITIONAL: only where both fertility tables order the pair the same way; hills vs coastal disagree, A.6) | climate as such; a named river or sea; distances; a view; the town's shape; the `strategicValue` string (framed, never restated, annex `:1569`) | `TERRAIN: Swamp` / `Tundra` / `anything else` have no producer (`:670-681`) | survey 3.18; refute B 3.18 |
| E-19 | the market (as an object) | holder kind `market` -> `['Weekly market', 'Public auctions']` (`holderTable.js:310-316`) | a place where goods change hands, and as a holder it keeps the trade record (HOLDS; the holder needs an INSTANTIATED row, `:243-247`; `Weekly market` is `on: true, p: 1.0` on `Market square`, `institutionServices.js:1064-1066`, so `Market square` always resolves) | a square; a building; a day; stalls; a crier; a charter | exactly ONE shipped institution carries both services (`Market square`); a town with a `Weekly market` institution and no `Market square` has no market holder, and `Weekly market` is a treasury row (`holderTable.js:272-279`) | survey 3.19; refute B 3.19 |
| E-20 | the dear / cheap good | `dear` · `steady` · `cheap` (`marketPrices.js:182-184`) over five MOVEMENTS (`:70-76`) | a movement against this settlement's OWN usual price, never a coin (HOLDS `:65-76`, `:161-168`) | a reason; a season; a shortage of the raw input; demand; a merchant; a currency | a stock count on `commodityStocks` against a fixed target of 8 (`:143-151`); services never move (`:129-133`); (see REFUSED E-R16, E-R17) | survey 3.20; refute B 3.20, A.5 |

#### REFUSED (economy)

| # | item tried | verdict | refuter's evidence |
|---|---|---|---|
| E-R1 | `specialist trade` = one speciality | REFUTED | SPECIALIZED = town && `incomeSourceCount >= 4` && not (market && >= 6) (`prosperity.js:304-306`): four or five income lines; nothing selects a speciality |
| E-R2 | `mix of field and market` = both farming and market | REFUTED | MIXED = village && `hasMarketInst` (`:307-309`); farming is never tested; every thorp / hamlet / village carries `Agricultural Rents` by tier default with no farm (`economicState.js:55-60`) |
| E-R3 | `surplus farm trade` / `small farm surplus` / `farm surplus` = farming with something left over | REFUTED | AGRICULTURAL / MINOR_SURPLUS / SURPLUS read tier, market flag and export count (`:308-314`), never `foodSecurity`; a village labelled `Deficit` with four exports prints "surplus farm trade" |
| E-R4 | `subsistence living` = nothing left over | REFUTED | SUBSISTENCE = thorp / hamlet && `exportCount < 3` (`:315`); a hamlet labelled `Surplus` with two exports prints it |
| E-R5 | `{good}` (shared bag) = a made or handled THING that leaves the town | REFUTED | `economicState.js:793-795` pushes `deriveServiceExports` labels (`computeActiveChains.js:1021-1025`, e.g. `'Maritime services (cargo, pilotage)'`), `:624-636` pushes `'Mercenary services — trained companies available for hire'`, `:654-663` the slave-trade sentences: services and people are members of the class |
| E-R6 | (lens `{good}`, implied by variants `:3560`, `:1289`) it leaves the town / goes out of the gates | REFUTED | exploitation exports live in `resourceAnalysis.exports` (`resourceGenerator.js:295-317`), a separate list from `economicState.primaryExports` (`economicState.js:741-751`, `:791-795`); "gates" is a wall fact (`priorityHelpers.js:53`) |
| E-R7 | `Secure` = covers itself | REFUTED | A.2: `deficitPct` is the residual after `importCoverage` and `magicOffset` (`foodGenerator.js:318-330`); `Secure` = residual <= 5 and surplus <= 40 (`:356-358`); licensed: "is covered" |
| E-R8 | `Import-Dependent` = a substantial part of what it eats is bought | REFUTED | A.2: residual 15-40% UNCOVERED (`:347`); reachable on an isolated thorp with `importCoverageRate` 0 (`:293-296`, `:308-309`); licensed: "a real share of its need is met by nothing on the record" |
| E-R9 | `Active Famine` = not enough, and no arrangement bringing enough | REFUTED | the label is the famine STRESS alone (`:341-342`), production x0.35 (`:243`), imports still cover (`:318`); and the pool is unreachable from the shipped label (A.10); licensed: "a famine is recorded" |
| E-R10 | chain `stable` = all inputs present | REFUTED | unknown -> stable (`supplyChainState.js:199-202`); `entrepot` -> stable with no local input at all (`:177`) |
| E-R11 | chain `blocked` = offline after a hard upstream failure | REFUTED | the only writer of the legacy token is the isolated-subsistence shutdown of entrepot chains (`customTradeEndpointIntegration.js:113-118`): no route, not an upstream failure; licensed: "shut off for want of a route" |
| E-R12 | "mill grinds" on the exploitation lens | REFUTED | A.1: `Mill` fills over a town with Taverns + Ale house (the `food` tag makes them grain processors, `src/lib/entities.js:228-232`, `resourceChains.js:56`); and `hasMill` elsewhere is a substring over `mill` (`foodGenerator.js:122`, `foodStockpile.js:194`, `tradeGoods.js:29`), so a Sawmill town "has a mill", stores x1.25, and produces flour |
| E-R13 | Coastal = a harbour edge | REFUTED | `deep_harbour` may be absent or depleted; `hasPort` is separate (`priorityHelpers.js:32`, `:61`); an explicit `isolated` route on coastal terrain is constructible (A.3) |
| E-R14 | Riverside = a crossing | REFUTED | `geographyData.js:130` is a disjunction ("along a navigable river OR at a river crossing"); `naturalFeatures` is never recorded per town |
| E-R15 | Mountain = a short growing year | REFUTED | no season-length field; `agricultureCapacity 0.4` is a yield multiplier, and the canonical food model uses its own table (A.6); licensed: "the ground yields little" |
| E-R16 | `steady` (market tag) is a measurement | REFUTED | an absent ledger entry reads `adequate` (`marketPrices.js:318`, `:137`): `steady` = unmeasured OR in band |
| E-R17 | the crier's reason ("for it comes scarce this season") | REFUTED | A.5: `causalReceipt` invents a seasonal cause whenever the road drift does not explain the band (`:213-218`, `:228-241`, `:272-277`); never copy the receipt clause |

Corrections the economy refuter made to the survey's own citations (refute §F): `computeActiveChains.js` lives at `src/generators/`, not `src/generators/economy/`; `collapsing` is produced (`supplyChainState.js:444`, `:447`), only `captured` is not; the two `unexploited`s are different records (chain STATUS at `customTradeEndpointIntegration.js:116`, exploitation BUCKET at `resourceGenerator.js:175`); `TERRAIN_DATA.agricultureCapacity` is not the canonical food model's fertility (`foodGenerator.js:38-41` is); `naturalFeatures` is consumed only by the undercity anchor (`staticComponents.js:289`).

### 2.4 POWER (DS-POW-1 .. DS-POW-7; 79 census rows, 31 RESOLVED; packets `power.survey.md` E-1..E-20, `power.refute.md`)

The power refuter's lens probe (refute §2; print-only `lens-probe.mjs` over the 52 roster labels) is the instrument behind several rows: the generation-side `category` (`src/generators/power/factionCategories.js:146-176`) does not join `ARCHETYPE_TO_RULING` (`src/domain/spatial/cohesionWeave.js:171-177`), so 21 of 52 labels read `mixed`; `Royal Authority`, `Noble Governorship`, `Ducal Governorship`, `Feudal Stewardship`, `Headman's Authority` read `council` (`government` is tested before `noble`, `factionCategories.js:109-146`, `:151`); `Military Council` reads `autocrat`; `merchant_league` is unreachable on generated data.

| # | noun | name class (members) | ENTAILS | NOT ENTAILED | ENGINE CONTRADICTS | source |
|---|---|---|---|---|---|---|
| P-1 | `{seat}` / the governing body's name | GOVERNANCE LABELS: `governanceLabelMap` 22 rows (`src/generators/power/rulingStructure.js:161-176`), council modifiers `:196-233`, tier fallbacks `:244-320`; filled from `governingName` `:787` | `Elected Reeve` entails election (HOLDS: gloss `:366`, `govBody` key `:164`, `:440`) · `Feudal Appointee` entails delegation, revocable (HOLDS `:369-370`, key `:166`) · `Merchant oligarchy` entails office bought (HOLDS `:415-416`, key `:172`) · Council / Consensus / Assembly / Senate / Consortium = a body, more than one decides (CONDITIONAL: true as English for the 22 mapped rows; the DS-POW-5 lens word for the same town can say the opposite on the same page: `Military Council` -> `autocrat`, `Royal Authority` -> `council`) · the label's authored gloss is the engine's definition (CONDITIONAL: birth only, and only for `govBody`-matched labels; on the fallback path a label absent from `govDescByFaction` `:424-439` carries the `Mixed Council` gloss "Power is distributed across multiple factions", the persisted `desc` for `Headman's Authority`, `Town Mayor`, `Shadow Senate` and others; after a play-time transfer `governingName` is a `GOVERNMENT_PREFERENCES` / alt label (`Noble Regency`, `Garrison Command`, `<label> Ascendant`, `src/domain/rulingPower.js:180-187`, `:433-449`, `:657-658`) outside this class) | a building; a chamber; how many sit; how often it meets; age; tenure; whether loved; whether it convenes; a hall | `Democratic assembly`'s gloss denies the coup framing ("factions lobby for influence rather than seizing control", `:418-419`) while `coupContenders` never reads the label (`src/domain/rulingPowerCoup.js:81-106`) · prose that hard-codes "the council" is wrong on most settlements (annex `:1816`, `:2202-2204`) | survey E-1; refute E-1 |
| P-2 | "the hall" | NONE: the word is baked in the corpus; no hall-name producer exists (`powerStateProse.js:855-876`); the `hall` civic class is a key-string classifier (`wiringCensus.js:1236-1240`, `:1313`) | entails nothing about a building (HOLDS; `hasCourtSystem` matches `town hall` / `city hall` and the `office` holder kind is instantiated by `Town hall` / `City hall` / `City administration`, `holderTable.js:357-366`, and nothing joins either to the corpus word) | that the settlement has a `Town hall` row; a chamber; a door; a place a stranger walks into | on a thorp / hamlet the governing institution is `Head-of-household consensus` or `Informal elder consensus` (`institutionServices.js:1448-1453`; `rulingStructure.js:162-163`), a consensus, not a place; DS-POW-2's floor and DS-POW-7 assert a hall on every tier | survey E-2; refute E-2 |
| P-3 | "the watch" | holder kind `watch` -> `Town watch`, `Professional city watch` (`holderTable.js:318-324`); the power generator's label ladder (`governanceNarrative.js:499-506`) | keeps watch (CONDITIONAL, per row: `Town watch` services are Night patrol, Gate duty, Crime response, `institutionServices.js:1322-1326`; `Professional city watch` are Law enforcement, Crime reporting, Missing persons, Escort service, `:1206-1211`; "takes crime reports" is the Professional row only; a "crime returns" face follows the row) | numbers; pay; competence; night patrol (Professional row); the only armed body; answerable to the seat | five corpus places assert a watch off pools that read no watch row: DS-POW-1 `Endorsed` v4, DS-POW-6 `capture pressure RECOVERING` v2, DS-POW-2 `siege matched` v2 ("the watch rota"), DS-POW-6 `neutral baseline` v1, DS-POW-6 `stolen goods market` v2 (census reads: `legitimacy`, `breakdown.*`, `power.criminalCaptureState`, `power.stability`) | survey E-3; refute E-3 |
| P-4 | "the rolls" / "the clerks" / "the record" | holder kind `office` -> Public records / Public record access / Record filing, instantiated by `City administration`, `City hall`, `Town hall` only (`holderTable.js:357-366`) | a record exists only where such an institution does (HOLDS; the office keeps its own books, so a citation there is "citing the speaker", `:363-365`) | clerks as persons; a chase for arrears; a ledger book; literacy; (the survey's "small tiers have no office-kind institution" is PLAUSIBLE, unverified by the refuter) | DS-POW-1's `[ledger]` angle asserts rolls and clerks in all five bands; cards 3 / 4 / 7 print SOURCE-UNRESOLVED (`holderTable.js:36-45`, the three-standing law) | survey E-4; refute E-4 |
| P-5 | "the court" (political sense) | (i) holder kind `court` -> `Courthouse`, `Multiple court buildings` (`holderTable.js:325-333`); (ii) DS-POW-7's political court, an unfilled English noun | two disjoint senses; the political sense entails nothing about adjudication (HOLDS) | a room; an audience; a monarch; a bench; that the two senses coincide | the census read DS-POW-7's `consolidation 0: a fully divided court` as `objectClass: law` because `court` is a `law` token (`wiringCensus.js:1311`) | survey E-5; refute E-5 |
| P-6 | "a bloc" / "the combination" | `Bloc` typedef (`src/domain/worldPulse/settlementPolitics.js:64-67`) | at least two members (HOLDS `MIN_BLOC_MEMBERS: 2` `:96`; a bloc below it fractures `:882-885`) · a primary glue and an end (HOLDS `:952-970`) · at most three per settlement (HOLDS `:93`, `:985`) · 8 ticks before a non-triggering dissolution (HOLDS `:120`; tested at `:803`, `:848`, `:854`, `:864`; exposure is not dwelled) | that it governs (a separate derivation: governing faction inside AND >= 0.4 of settlement power, `rulingBlocOf` `:549-592`); a name; a leader; a meeting; a count beyond two; that outsiders know | `coalitionConsolidation01` returns 0 for a dormant layer AND a live layer with no ruling bloc, AND for `totalPower <= 0` (`:581`, `:604-607`): 0 is not a measured full division; (see REFUSED P-R1 for "a working majority") | survey E-6; refute E-6 |
| P-7 | the glue words | `classifyGlue` (`settlementPolitics.js:414-457`): compromise · threat · doctrine · patronage · concession | concession: SEAT-HELD, outbiddable, survives leader changes (HOLDS `:452-455`; outbid market `:858-889`) · doctrine: two or more members whose archetype is `religious` OR `arcane` (HOLDS `:443-446`; "creed" fires on two wizard colleges) · patronage: PEOPLE-HELD, dies at succession (CONDITIONAL: dies at a succession PAST THE DWELL, `:803`, and only where a prior leader id was recorded `:800`; "will die with its bearers' tenure" is true in the limit, "the moment either changes" is not) · compromise: a seat-holder carries a corruption leash (CONDITIONAL: leash OR a bare corrupt flag, `:428`) | kinship; affection; money; a debt; a deity; a temple; a rite; orthodoxy; a siege; an army; walls; a named enemy; who holds the other end | the `threat` detail string says "at the walls" and `settlementUnderThreat` never reads a wall row (`:438-440`); (see REFUSED P-R2 for "external") | survey E-7; refute E-7 |
| P-8 | the end words | `ARCHETYPE_END` `:461-466`, `GOAL_END_HINT` `:469-473`, `deriveEnd` `:484-496` | seats: lead archetype noble / military / government / civic, or a lead-leader goal in {secure_office, seek_promotion, consolidate_power} (HOLDS; the goal hint is read on the LEAD member's leader only and overrides the archetype) · doctrine: archetype religious / arcane, or goal spread_faith / defend_doctrine (HOLDS) · patron: archetype criminal OR outsider, or goal serve_patron (HOLDS `:464`, `:473`; a political sponsor, never a deity) | ambition; a named target seat; a good; a route; a sum; a creed's content; a deity; a temple | (see REFUSED P-R3, P-R4 for `commerce` and `survival`) | survey E-8; refute E-8 |
| P-9 | "the ladder" / "a rung" / "standing" | `ladderRungsOf` (`src/domain/townMap/ladderRead.js:95-107`); `npcLadderState.js:44-56`, `:107-110` | standing = stock / STAND_MAX (10) (HOLDS `:46`, `:920`) · decays toward baseline over a 156-week half-life (HOLDS `:47-51`, `:228-234`) · only office-holders hold a rung, floor 0.4 (HOLDS `:57`, `:208-209`) · high instability erodes effective power (HOLDS `ladderRead.js:211-217`; consumed at `rulingPowerCoup.js:93-94`, `:108-109`) · at first light the pool is selected by rung count alone (CONDITIONAL: the 3-rung cell sits ON the `SETTLED_MIN_GAP` boundary; `FIRST_SIGHT_WEEKS: 1` may shrink the gap by ~0.44% and land it in "clear top rung" instead; either way the selection is a function of tier) | a reason for any rise or fall; a title; an office; an age; a tenure; a death; a count of members; that anyone is climbing | depth is a TIER cap (thorp 1, hamlet 2, village / town 3, city 4, metropolis 5, `npcLadderState.js:108-110`; `SHALLOW_RUNGS_BELOW = 3` `powerStateProse.js:579`), so "the ladder is short because the faction is" is false on those towns · `ladderInstabilityOf` returns 0 when the field is absent (`ladderRead.js:138-141`), so "long-held" also spells "no churn recorded"; (see REFUSED P-R5) | survey E-9; refute E-9 |
| P-10 | "a challenger" / "outweighs" / "the seat could fall" | `coupContenders` (`rulingPowerCoup.js:81-125`): top 3 non-governing, non-criminal factions with power >= 5, `weight = power x COUP_COERCION[archetype]` (`:36-49`, `:99`); `coupRiskLabel` (`:274-282`) | `Critical` entails the incumbent is UNGATED, its amplified weight below the weakest of a full field of three (HOLDS `:111-112`, `:278`) · a rival exists on the roster with the stated relative weight (CONDITIONAL: in the engine's sense only, coercion on the challenger vs `govMultiplier` on the incumbent, `:87-112`) | an intention; a plan; a claim; a grievance; a date; that anybody has moved | `Stable` means no non-criminal challenger with power >= 5: criminal factions are excluded by construction (`:102-104`), so "Nobody is positioned to take the {seat}" is false of a town whose Thieves' Guild sits at `corrupted` / `capture` · with a null governing faction every roster row is a challenger (`rulingPower.js:224-230`) | survey E-10; refute E-10 |
| P-11 | the legitimacy band words | `legitimacyBandFor` (`src/generators/factionDynamics.js:106-140`); rungs `bandLadders.js:175-181` | a score position: 50 + four bounded signed contributions, clamped (HOLDS `:161-166`) · `governanceFractured` is exactly `score < 30` at all three writers (HOLDS `factionDynamics.js:133`; `timeProgression.js:179`; `rulingPower.js:352`; desk note `powerStateProse.js:53-62`) | a cause; a trend; a history; that anyone was asked; a season; a person's opinion; a SECOND independent fracture fact (the annex's fence at `:1927-1930` is contradicted by the code) | - | survey E-11; refute E-11 |
| P-12 | "the dominant contributor" | `BREAKDOWN_WORD` (`powerStateProse.js:220-225`), max absolute value (`:255-260`) | which of the four signed numbers has the largest magnitude (HOLDS) | that the town cares about it most; a cause; a direction | the four are not commensurable (prosperity ±20, safety ±20, defense ±10 x tier scale, food ±10 x tier scale, `factionDynamics.js:20-27`, `:30-44`, `:51-59`, `:76-81`, `:155-159`): on a thorp defense <= 3 and food <= 4, so dominance is a coefficient artifact · the SAFETY word is fed by non-crime strains (`famine` -> -20, `quarantined` -> -8, `controlled` -> -5, `:33-40`) · only prosperity has a favourable pool (`:262-266`) | survey E-12; refute E-12 |
| P-13 | "the hold" (`govMultiplier`) | five bands 1.30 / 1.15 / 1.00 / 0.80 / 0.60 (`factionDynamics.js:113-118`) | the direction and magnitude of legitimacy's effect on the incumbent's coup defence (HOLDS `rulingPowerCoup.js:85`, `:110`) | a public act; a vote; a protest; a rival's move | five bands into three pools (`powerStateProse.js:519-524` vs `EngineSections.jsx:208-214`): 1.15 ("lends them weight") draws "backing hardens", 0.80 ("doubt loosens") draws "rejection is breaking" | survey E-13; refute E-13 |
| P-14 | the capture rungs | `CAPTURE_LADDER` (`src/domain/corruption.js:474`); birth classifier `factionDynamics.js:220-255`; display `PowerTab.jsx:163-169` | a FACTION-level position on the ladder (HOLDS `factionCapture.js:136-146`) · `capture` at birth entails crimP > govP x 1.5, crimP >= 24, govP >= 15, safetyRatio < 0.35 (CONDITIONAL: birth only; at play `advanceCaptureState` steps the ladder per faction and the settlement value is the ROLLUP of the WORST faction, `corruption.js:476-480`, `factionCapture.js:136-146`, `pulseKernel.js:645-648`; the face cannot tell birth from play) | which person; which seat; a sum; a date; a method (no person-level captured-role record exists, `powerStateProse.js:396-404`) | AGENT / LEADER is a declared judgment on a faction-level rung (`:399-404`); worse, the DS-POW-6 `{faction}` fill is `governingName` (`:962`) while the rollup may sit on `Military/Guard` or `Merchant Guilds` (the code's own example: "the underworld could capture the City Watch", `factionCapture.js:150-152`); the annex's "deputy" matches no engine seat (`npcAgency.js:413` is `lieutenant_operator`) | survey E-14; refute E-14 |
| P-15 | the criminal operation roles | seven roles, substring on the operation NAME (`src/domain/criminalOpRole.js:38-46`) | what the operation IS (HOLDS as a label definition; the annex declares this the role's own entailment `:2299-2301`) · "the landing points are unofficial" (`duty evasion` v1) (CONDITIONAL on `hasPort`: smuggling rows are minted at city / hamlet / town with no port gate, `institutionDescVariants.js:81`, `:437`, `:797`) | who runs it; size; age; profit; headcount; whether the watch knows | only the FIRST classifiable operation is narrated (`powerStateProse.js:985-987`); the fallback row `criminal revenue stream` is wide (`Slave market`, `Whisper market`, `Assassins Guild`, `Street gang`... all miss the six substrings) | survey E-15; refute E-15 |
| P-16 | the ruling-power words | `RULING_POWERS` autocrat · council · theocracy · merchant_league · criminal · mixed (`cohesionWeave.js:134`); lens `RULING_POWER_LENS` `:161-168` | the five authored lens rows (autocrat: one decider, nerve coup; council: averaged, sticky, nerve unrest; theocracy: doctrine-first, nerve heresy; merchant_league: nerve insolvency; criminal: nerve betrayal) (CONDITIONAL: true of the LENS ROW, not of the town the word is drawn for; the `fear` / `warStyle` strings are authored vocabulary the annex permits lifting, `:2206-2209`) | that the feared thing is happening (the nerve is a disposition, annex `:2210-2211`); a number of deciders; a building; a tenure | `structuralLensOf` reads the MAX-POWER faction, never `isGoverning` (`:279-285`; under `under_siege` the `Military/Guard` row is doubled, `stressFactions.js:13-16`, so a besieged council town reads `autocrat`) · the vocabularies do not join (probe above; `merchant_league` unreachable; a Royal Authority is a `council`) · `mixed` is the fail-soft default dressed as a reading (`:196`) | survey E-16; refute E-16, §2 |
| P-17 | the economic-base words | `ECONOMIC_BASES` + `ECONOMIC_BASE_LENS` (`cohesionWeave.js:131`, `:141-147`) | each base's authored `fear` / `warStyle` (HOLDS as table rows) · all five pools DARK by declaration (HOLDS: `structuralLensOf` passes no base, `:286`; `powerStateProse.js:668-679`) | a mine; a field; a road; a workshop; any institution | every world reads `mixed` | survey E-17; refute E-17 |
| P-18 | the stability words | 21 baseline / stress / annotation labels (`governanceNarrative.js:249-338`) + 5 transfer labels (`rulingPower.js:414-420`), classified by FIRST WORD (`powerStateProse.js:305-330`) | only the token the classifier matched (HOLDS; annex `:2010-2013`: a `stable` match may not assert no rivals) | duration; a cause; a rival; a trend; a future | `critical matched` is unreachable (the only label with `critical` also carries `siege`, tested first, `:294-302`) · the FLOOR pool absorbs seventeen distinct labels including `Fractured — no stable governing authority`, and its prose asserts "{faction} holds the hall" (`governanceNarrative.js:295`; floor `:313`); (see REFUSED P-R6 for `recentConflict`) | survey E-18; refute E-18 |
| P-19 | "the militia" / "the garrison" / "the guard" / "the community" | `deriveDefenseGroupLabel`, minted in the POWER generator: garrison > militia > watch > mercenary company > (small tier) the community / the guard (`governanceNarrative.js:499-506`) | `The community` / `The guard` are fallbacks and entail no institution (HOLDS `:506`) · a first-match precedence, one winner, a selection not an inventory (HOLDS `:500-506`) · each named row entails the institution it matched on (CONDITIONAL: substring match, `includesInstitution` `:74-75`; `'watch'` matches the roster row `Watchtower` `institutionServices.js:1561`, so "The watch" is minted on a masonry tower) | pay; numbers; training; that they are the same body as the watch | "the guard" is NOT always safe: `The guard` is the non-small-tier fallback when NO force institution matches, and on that same town `safetyProfile.js:300`, `:309` says "no meaningful guard presence" / "effectively no law enforcement"; safe only where `hasMilitaryInst`; on small tiers the engine's own generic is `The community` (`:506`), backed by `communityMilBase` (`defenseGenerator.js:140-156`) | survey E-19; refute E-19, A-beta |
| P-20 | "previous government" / "the cause" | `RULING_POWER_CAUSES` coup · election · succession · conquest · appointment (`rulingPower.js:356`); lawful passage `:376`; row shape `{label, cause, tick}` `:631` | no generation-time writer; play-only (HOLDS `:629-632`; absent on 48 of 48 generated towns, `powerStateProse.js:527-546`) · a RECORDED cause licenses a causal clause; a lawful passage leaves the defeated house demoted (CONDITIONAL: demotion only under Register VII, `demotesIncumbent` returns `rollsRegisterVii(config)`, false on every v1 world, `:400-404`; cause AND Register VII, never the cause word alone) | a death; a date in years; a name; a battle; `by` and `government` (annex spellings no writer produces) | the `previousGovernments empty` pool's "Whatever came earlier was not the kind of thing the rolls kept" would be drawn for every generated town | survey E-20; refute E-20 |

#### REFUSED (power)

| # | item tried | verdict | refuter's evidence |
|---|---|---|---|
| P-R1 | a ruling bloc = "a working majority" (corpus `a RULING bloc, consolidated` v1; the engine's own concession detail `:454`) | REFUTED | `RULING_CONSOLIDATION_FLOOR: 0.4` (`settlementPolitics.js:149`); `rulingBlocOf` returns at `share >= 0.4` (`:588-590`): a 40% plurality; lifting the detail string verbatim carries the overstatement; safe: "a working plurality" / "the largest combination" |
| P-R2 | glue `threat` = "an EXTERNAL danger is live" | REFUTED as "external" | `settlementUnderThreat` (`:510-528`) is true for a besieging front INTO the town (`:513`) OR the town's OWN posture at `war_preparation` / `mobilized` / `deployed` (`:516-518`; `mobilization.js:117-121`) OR six tolerant snapshot fields (`:521-527`): a town whose army is deployed abroad forms a `threat` bloc with the detail "a common danger at the walls" |
| P-R3 | end `commerce` = archetype merchant / craft / labor, or a commerce goal | REFUTED | `return base \|\| 'commerce'` (`:495`): `commerce` is also the FAIL-SOFT for a lead member whose archetype has no `ARCHETYPE_END` row (`other`, e.g. `Town Mayor`, `Elder Consensus`, `Elected Reeve`): "organized around money" with no merchant in it |
| P-R4 | end `survival` = an external threat override, and only that | REFUTED as the only source | `GOAL_END_HINT.break_vassalage = 'survival'` (`:470`) with no threat at all; and the threat itself includes the town's own mobilization (P-R2) |
| P-R5 | the ladder array is "sorted top-first" by standing | REFUTED | the rung array is a SEAT order, first-lit from structural importance (`npcLadderKernel.js:677-683`), thereafter changed only by a contest swap (`:828`, `:1181`); `standing` is a separate stock projected onto each rung (`npcLadderState.js:912-921`); `standing[0] - standing[1]` can be NEGATIVE while the array still names the top rung; the desk's `<= CROWDED_TOP_GAP` (`powerStateProse.js:610`) folds a negative gap into "{npc} leads by very little" |
| P-R6 | `recentConflict present` = "the losing side is still on the rolls" (v2) / "the quarrel it came through" (v1) | REFUTED | every birth producer string is an UNRESOLVED present-tense dispute (`governanceNarrative.js:372-487`: "Neither side will back down", "is hearing the case"); the only resolved form is the transfer's own sentence (`rulingPower.js:672-675`), where under a coup the old house is REMOVED (`:552-565`); `recentConflict present` entails "a recorded dispute exists" and nothing about its outcome |
| P-R7 | corpus nouns no survey row covers, graded REFUTED by the refuter (§4): "the walls" (DS-POW-2 `siege matched` v1 / v4, DS-POW-6 `neutral baseline` v1, DS-POW-7 `glue threat` v1; `inst.hasWalls` read by NO power pool, `under_siege` has no wall gate `defenseGenerator.js:336`) · "the gates" / "the watch rota" (DS-POW-2 `siege matched` v2; `hasGates`, `hasWatch` unread) · "the granary (door)" (DS-POW-1 `FOOD, adverse` v1; the contribution reads `foodSecurity.label` only, `factionDynamics.js:149`, `:164`) · "the streets after dark" / "policing" (DS-POW-1 `SAFETY` v1 / v3; the word is fed by `famine` / `quarantined` / `controlled` labels) · "the season" / "each season" (eight pools; a trend needs a persisted band with a CHANGE flag, `powerStateProseCandidates.js:47-51`; the card refuses a season and a future) · "remembers being hungry longer than being fed" (DS-POW-1 `FOOD` v3; `computePublicLegitimacy` has no memory, play-time deltas are symmetric, `timeProgression.js:165`) · "the minute of any meeting" (no meeting record exists) · "a person who speaks for {faction}" (DS-POW-6 `LEADER` v1; no person-level capture record, and the wrong faction) · "spend goodwill" (no goodwill stock; the card refuses a future) | REFUTED (nine corpus nouns) | `power.refute.md` §4 |

Corrections the power refuter made to the survey (refute §6): `{faction}` in DS-POW-6 is `governingName` (`:962`), which makes the capture pools name the seat's faction; E-20's row shape is `{label, cause, tick}`; "the guard" is not always safe; card 1 (`governanceFractured true`) prints `source: court · LICENSED` and the census confirms `stateOrgan: true`; every DS-POW row reads `covert: false` (confirmed by direct print).

