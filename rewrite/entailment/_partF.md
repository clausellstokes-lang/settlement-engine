---

## 5. OWED TO THE CHAIR

Two lists. 5.1 is every CONDITIONAL entailment whose condition needs a WIRING DECISION (a derived slot computed from a name class, a holder-row spelling, a producer change) rather than a writer's care; a CONDITIONAL whose condition a writer can meet by reading the record as it stands (a tier, a route, a faction row, a resolved `term.type`) is left in the desk tables and not repeated here. 5.2 is every disagreement between a surveyor and its refuter, and the three places where the packets and the chair's draft law disagree. Nothing here is ruled by the folder.

### 5.1 CONDITIONAL entailments that need a wiring decision

| # | the entailment and its condition | what the decision is | source rows |
|---|---|---|---|
| O-1 | **The wall's MATERIAL.** "timber" on `Palisade` (bare word only; the chain feeds every wall row quarried stone, `src/data/supplyChainData.js:873-884`, and no wood word survives the three desc variants); "stone" / "masonry" on `Town walls` and `City walls and gates`; NO material on `Citadel`; NEITHER material on `Palisade or earthworks` (a disjunction the engine never resolves, which reaches the page as the literal `{defwork}` fill "palisade or earthworks"); `Gates (if walled)` passes `bareCommonFill` with its parentheses. ADDENDUM 10 item 2 names the `{defmaterial}` derived fill as the register car's wiring. | Mint `{defmaterial}` from the recorded name class with a value table: `Palisade` → timber; `Town walls`, `City walls and gates`, `Massive walls` → stone; `Citadel`, `Gates (if walled)`, `Palisade or earthworks` → NO FILL (the variant naming the slot drops). And decide whether "cut from its own woods" / "the stone of the country" (a material SOURCE) is ever licensed: the refuter says never, since the fortification chain's stone upstream applies on Plains as on Mountain and a palisade too. Also decide whether `{defwork}` may render "palisade or earthworks" and "gates (if walled)" at all (the survey's residuals 2 and 3). | D-1, D-2, D-3, D-4, D-5, D-R5; defense C-7, C-11; A-7 |
| O-2 | **"The guard" as an always-safe spelling.** ADDENDUM 10 item 5 adopts "the guard", "the muster", "the men on the wall" as always-safe spellings of the paid military the gate measures. The power refuter (A-β) shows `The guard` is the engine's FALLBACK label when NO force institution matches (`src/generators/power/governanceNarrative.js:506`), on which town `safetyProfile.js:300`, `:309` print "no meaningful guard presence" / "effectively no law enforcement": safe only where `hasMilitaryInst`, and on small tiers the engine's own generic is `The community`. "The men on the wall" asserts MANNING, which the defense packets place in NOT ENTAILED (walls-without-people is a live and common state, `DS-DEF-2 walls with NO force`; D-3), and a wall, which only the WALLED pools carry. "The muster" is safe as the CLASS word, but "the muster roll" as a citation is backed by a roster row only where a `Citizen militia` stands with an instantiated `Muster training` service (`src/domain/prose/holderTable.js:280-288`; `src/data/institutionServices.js:835-838`; D-8). | Re-cut item 5's always-safe list: "the muster" (class word, never a roll citation), "the town's force", "the community" on small tiers; "the guard" only where `hasMilitaryInst`; "the men on the wall" struck or gated on `forces.walls.present` AND a force row. | A-3, A-4; power A-β, E-19; defense D-3, D-8, A-15; ADDENDUM 10 item 5 |
| O-3 | **"The watch" as the institution's NAME.** ADDENDUM 10 item 5 makes it CONDITIONAL on `hasWatch` with a derived holder fill as the wiring. The packets add: `hasWatch` is town-and-up only (`src/generators/priorityHelpers.js:48`), `Town watch` is REQUIRED at town so every town has one, `Professional city watch` sets `hasGarrison` too and `forceCorePoolKey` reads garrison first (so the `watch PRESENT` pool never fires at city), the power generator's substring match mints "The watch" on a `Watchtower`, and "takes crime reports" is the Professional row's service only. On the economy desk the watch is the LICENSED source holder of DS-ECO-6 and a STATE ORGAN. | Decide the holder-row spelling: a `{watchname}` (or the holder table's own institution name) filled from the resolved `watch` holder row, tier-scaled as `safetyProfile.js:30-35` already does (`'town watch' / 'city watch' / 'local watch'`); and decide whether the `garrison PRESENT` pool at city may say "the watch" for a `Professional city watch` row. | A-1; D-9; power E-3, E-19a; economy 5.3; general A-1 |
| O-4 | **The occupier's garrison vs the town's.** WF-3 (an occupation does NOT entail an occupier army present, `src/domain/worldPulse/occupation.js:873-893`), S-3 (the occupier's soldier count "varies", `src/generators/power/stressFactions.js:45`), and D-7 (the town's `garrison` bucket holds a housing row and a police force) mean "the garrison" is unresolvable from the record without WHOSE. | Decide whether a face may ever say "the garrison" bare, or must fill "{counterpart}'s" (the occupier's name, which the war desk already fills) or "the town's" (resolved from the roster row name). The war refuter's condition "the garrison here" only where `occupierStillPresent` needs that read wired into the desk (it is a separate function the display does not call). | WF-3, WF-4, WF-R2, S-3, S-R3, D-7, A-2 |
| O-5 | **The `{reason}` fill on the stressors desk.** S-20: ruling a `{reason}` fill vocabulary lights the TRACED-provenance pool, whose only birth-time trace is the sentinel `'GENERATION'` (`src/domain/conditionPromotion.js:203-208`), and prints "The town knows exactly what did this" on 100 percent of birth conditions. The honest split is `causes.some(c => c.source === 'event')` (`stressorsStateProse.js:226-238`), which the desk deliberately does not use. | Either re-key the provenance pools on `isEventSourcedCondition` before any `{reason}` vocabulary is ruled, or leave `{reason}` unfilled on this desk by decision (the desk's current state, held silent by anchored liveness). The chair's brief names `{reason}` "the RECORDED reason, typed" (annex `:182`); the general refuter found no TYPED founding reason on its desk either (G-R1). | S-20, S-R19; general G-R1; stressors §8.6 |
| O-6 | **`{institution}` on the economy desk's exploitation lens.** E-7 / L-30: the fill is the STATIC catalog's `processingInstitutions[0]` carried unchanged, so `Mill` fills over a town whose grain chain is "fully exploited" by a Tavern and an Ale house; "a mill grinds" is not a licence there. On the impaired-service lens the fill IS the roster's, except the `TRADE_DEPENDENCY_NEEDS` key `Access to external mill`. | Decide whether the exploitation lens's `{institution}` is re-wired to a resolved ROSTER row (a derived fill from `institutionSupportsChain`'s actual supporters) or restricted to the ABSENCE reading of the `unexploited` pool ("the catalog's processor is not here"), with `properFill` refusing `Access to external mill` on the impaired lens. | E-7, E-R12; economy A.1; general R-L10 |
| O-7 | **The market holder.** E-19 / A-20: the holder kind `market` resolves to `Market square` ALONE; `Weekly market` is a treasury row; `hasMarket` and the DS-GEN-13 name class credit a black market, a slave market and a knowledge broker. | Decide whether "the market" as an OBJECT requires the `Market square` holder row (a holder-row spelling) and which name test the general and economy desks key on; the two desks currently answer differently for the same custom or criminal row. | E-19, E-2, G-12, G-R4; economy 5.1; general A-9, R-A8 |
| O-8 | **The port and the sea.** D-17, E-1, G-11, A-11: "the sea" is licensed off a `Shipyard` row or `maritimeSupported` (`port` route off non-`riverside` terrain), never off `hasPort`; "a harbour" only where `deep_harbour` is available or `hasPort` resolves; the DS-DEF-6 logistics pools read the institution flag on one branch and the config enum on the other. | Decide the derived fill or read for the water words (a `portKind` read, `src/generators/generationContext.js:201-205`, already exists and is unread by prose), and whether `DS-DEF-6 Naval Defense: Port only` v3 "A stranger sails into" is re-keyed on it. | D-17, E-1, G-11, E-R13; defense A-10, X-10; economy D.1 |
| O-9 | **The DS-WAR-2 side arm and the treaty party.** Refute 0.1 and 0.2: the term pools carry no side while every directional cell is authored from the payer's chair, and `{counterpart}` is the martial counterpart. Every directional term entailment (WF-9, WF-10, WF-16) is CONDITIONAL on `termObligationOf(record, term).obligorId === settlementId`, a reading the desk does not hold. | A producer change: add a side arm to `treatyTermPoolKey` (or key the directional families on `documentSideOf`) and fill `{counterpart}` on DS-WAR-2 from `doc.victorName` / `doc.loserName`; until then only the side-neutral `generic floor` cells are licensed and `{term}` is the safe spelling. | WF-8, WF-9, WF-10, WF-16, WF-35, WF-R5, WF-R14 |
| O-10 | **The `At war` residue and the covert pool.** Refute 0.3 and 0.4: `At war` fires on a decaying scar, an `alert` posture, an army in transit and a holding of others, so the residue has no licensed member (WF-R6); the `mobilization: COVERT` pool is dark because the tab excludes covert readings. | Decide whether the residue key is re-cut (a scar-only key, an alert-only key) or declared dark; and whether the covert pool stays dark or the tab passes `includeCovert` for the DM audience. | WF-6, WF-R6, warFaith refute 0.3, 0.4 |
| O-11 | **DS-STR-2's anaphora and the spread target.** A-25: the lifecycle and origin lines name no crisis, the selector is `.find()` and includes echoes, and a spread target feels the origin's clock (F4). S-16's `peaking` / `emerging` are CONDITIONAL on `settlementId === originSettlementId`, a read the desk does not make. | Decide whether the desk owes a naming clause, a same-type guard against the banner, and an origin-vs-spread-target read (`severityBySettlement`), before the lifecycle pools are rewritten. | S-16, S-17, A-25; stressors §8.7, refute F4 |
| O-12 | **The condition direction lens and the wind-down constant.** S-19: the display-path status substitution (`activeConditions.js:644-646`) bites raw / legacy partials only; S-21: the desk's 25 percent wind-down (declared vetoable, `stressorsStateProse.js:259-278`) disagrees with the engine's fixed 2 ticks (`activeConditions.js:859`) in both directions. | Decide whether the direction lens reads the RAW condition or drops the motion claims, and move the wind-down cut to the engine's 2 ticks or keep the 25 percent by veto. | S-19, S-21; stressors §8.4, §8.8 |
| O-13 | **The FAMILY door and the counterforce pools.** A-31: if a `family` field is added to `CONDITION_ARCHETYPE_TEMPLATES` the five family pools light with membership the engine's own groupings contradict; the ten dark COUNTERFORCE / SYNERGY pools have a PERSISTED reading (`stressor.counterforce`, `stressor.synergy`) the desk's dark-pool reason does not cover. | Re-cut the family pools against `archetypeCatalog.js:22-45` BEFORE any `family` field lands; decide whether the counterforce / synergy pools read the persisted snapshots, with the `siege`-only conjunctive reading gated on `requireAllFloors`. | A-31; stressors A9, A10, §8.9, refute §2, §4.7 |
| O-14 | **The instrument's own word lists.** A-39: `CIVIC_OBJECT_CLASSES` mis-files `patron` (DS-REL-1, the bloc end) as `temple`, `port` as `road`, `guild_conflict` as `craft`, DS-POW-7's political "court" as `law`; the guard sees pool keys only, so every civic object living in prose is unguarded; `covert` keys on the read path so every DS-POW row reads `covert: false` though four capture pools are dm-only. | A wiring decision on the census: split the collisions in the token lists (or add a per-pool override), and decide whether the restatement guard should walk the variants' nouns as well as the key. | A-39; general A-13, §7.5; power L-21, L-22, E-5; stressors §3a, §8.2 |
| O-15 | **The pay gate's per-thing variation.** D-31 / L-50 / L-51: under R-viii' the military gate is ONE purse and no face splits it at birth; the FORTIFICATION / GARRISON / MERCENARY chain modifiers (`defenseGenerator.js:585-605`) are the engine's per-thing variation and are unwired to prose. ADDENDUM 12 records the wiring row (add the chain statuses as reads on WALLED-STRAINED and DS-DEF-5) as vetoable. | Land or refuse the chain-status reads; until wired, no face says the wall's keeping is provisioned while the muster's wages are short or the reverse. | D-31, D-3, D-7; ADDENDUM 12 amendment |
| O-16 | **The general desk's frozen booleans and the custom-content asymmetry.** G-13: DS-GEN-17's `compound.inst` is minted at generation over the RAW roster and never re-read, while DS-GEN-13 filters through `liveInstitutions`; A-40: custom rows reach name tests and not flags. A "still stands" claim after a played calamity, or any claim over a custom row, differs by block within one desk. | Decide whether DS-GEN-17 is re-keyed on a live read (the ruin filter) and which of the two answers the licence adopts for custom content on every roster-reading block. | G-13, G-R6, G-R7; general C-3, R-A1; defense C-12 |
| O-17 | **The power desk's joins.** P-16 / L-97: `structuralLensOf` reads the max-power faction and the two vocabularies do not join (`merchant_league` unreachable, `Royal Authority` a `council`, `Military Council` an `autocrat`); P-14 / L-44: DS-POW-6's `{faction}` is `governingName` while the capture rollup is the worst faction's; P-1: `{seat}` after a transfer is an alt label outside the class; P-20: `previousGovernments` has no birth writer. | Either join the vocabularies (a wiring act: `isGoverning` in `structuralLensOf`, a category → archetype map) or declare the affected pools dark as the desk already does for `economicBase`; fill DS-POW-6's `{faction}` from the captured faction, not the seat; the annex's DS-POW-1 fence at `:1927-1930` amended or the pool re-scoped (C-1). | P-1, P-14, P-16, P-20; power §10 |
| O-18 | **The faith desk's producers.** WF-19: `rankAxis` is the authored embed rank and the pantheon tier is never written back; WF-24: `cults[]` is a static list never reconciled; WF-32: `deities[ref].covert`, `templeWealth` and the pilgrim season have NO writer (three pools of DS-FTH-3 unmarked); WF-31: `tenure` is not projected; WF-33: `live: false` is a projection status the tab reads without a campaign gate. | Decide which of these are wired (write the tier back, reconcile `cults[]`, project `tenure`), which pools are declared dark, and whether the faith tab is campaign-gated. | WF-19, WF-24, WF-31, WF-32, WF-33; warFaith §7 |
| O-19 | **The `{good}` and `{chain}` slots on the economy desk.** E-4: the shared-bag `{good}` fills 0 of 48 (all 99 export labels capitalised) and its class includes services and people; `{chain}` is never filled (0 of 318 conformant). | Decide whether the export labels are lowercased into a fill vocabulary (a producer change) or the slots stay dark by declaration. | E-4, E-R5; economy §1 |
| O-20 | **`settled` in the draft law.** ADDENDUM 10 item 4 glosses `settled` as "no live threat, never founded". The packets read the producer token `heartland` as LOW raider / monster exposure (the stress roll multiplies siege / monster by 0.3 and occupation by 0.4, `stressGenerator.js:121`, `:168`), and W2 of ADDENDUM 12 already bars a totality over the town because DS-DEF-2 carries war and other pressures on the same page. | Re-word item 4's gloss: `settled` (producer `heartland`) = the country's monster-and-raider tier is the LOW one; never "no live threat", never founded. | L-2; defense L-2; stressors refute §4.13; ADDENDUM 12 W2 |

### 5.2 Disagreements between a surveyor and its refuter (and between the packets and the draft law)

| # | desk | the surveyor said | the refuter said | where it stands in this table |
|---|---|---|---|---|
| X-1 | defense | E-31: the economic-upkeep gate means the FUNDED portion is under-funded in the proportion stated; the corpus's "wages" | REFUTED as a wage claim: the multiplier is a function of `econOutput` alone and gates a palisade and a part-time militia exactly as a garrison; a wage-less village prints WALLED-STRAINED (R-D, X-3). The chair's R-viii' (ADDENDUM 12) agrees on the ONE PURSE and keeps the word "wages"; the refuter says the word is a label read on a wage-less roster | D-31, D-R4; the chair decides whether "wages" survives on a roster with no paid row |
| X-2 | defense | E-1: "timber" on `Palisade` tagged [RECORDED] | the tag is wrong (no wood word on the thorp row or its variants; the "wooden" cite is the hamlet's disjunctive row); the entailment survives as DEFINITIONAL and CONDITIONAL, and the supply chain feeds the row stone | D-1, D-R5; O-1 |
| X-3 | defense | E-30: a hostile fleet on the sea approaches [RECORDED] | CONDITIONAL: "fleet" is the display note's word; the writer of `blockade` was not traced | D-30 |
| X-4 | defense | E-8: a roll [RECORDED] via the holder table | CONDITIONAL: the holder record is a service row at `on: false, p: 0.5` | D-8; O-2 |
| X-5 | general | A-6 / L-12: "stocks are not a prison" | REFUTED (R-L7): the shipped row holds cells; the survey's trap is itself a label read | L-17, A-17 |
| X-6 | general | FOUNDED-YOUNG "effectively unreached" (youngest town 81y) | reachable by `settlementAgeMode: 'new'` / `'custom'`; the figure measured `auto` only | L-75 |
| X-7 | general | `{calamity}` = a typed BLOW; "the siege" = a besieging force; `anchored` = still bears on the present; `intensity` = closeness to violence; tension type closed at 10; `mountain` / `hills` = an approach that climbs; market-class work = goods bought and sold | all REFUTED with code (event not blow; a divided response; a narrative-fit coin; a rivalry count; open in the producer; the approach is the route; the name class credits knowledge, persons and the criminal roster) | G-R2, G-R8, G-R10, G-R13, G-R12, G-R9, G-R4 |
| X-8 | general | custom-content rows reach the substring matchers | they never do (`nativeSemanticName` is `''`); name tests over `liveInstitutions` reach them | A-40; O-16 |
| X-9 | economy | `captured` and `collapsing` have no producer; `unexploited` remaps to `blocked` (one word for never-worked and shut-off) | `collapsing` HAS a producer (two arms, one on a single pressure); only `captured` is unproduced; the two `unexploited`s are DIFFERENT records (a chain status vs the exploitation bucket) | E-13, L-83 |
| X-10 | economy | `Import-Dependent` = a substantial part of what it eats is bought; `Secure` = covers itself; `Active Famine` = not enough and no arrangement | all REFUTED: the residual after imports and magic (printable with zero imports); covered, not by itself; the famine STRESS alone, and the pool unreachable from the shipped label | E-10, E-R7, E-R8, E-R9, L-84, L-85 |
| X-11 | economy | "mill grinds" as a definitional licence; Coastal = a harbour edge; Riverside = a crossing; Mountain = a short growing year; `naturalFeatures` include a ford, a bridge, a ferry | REFUTED on the exploitation lens (the catalog's word); `deep_harbour` may be absent; the desc is a disjunction and `naturalFeatures` is consumed only by the undercity anchor; no season-length field; `computeActiveChains.js` lives at `src/generators/`, not `src/generators/economy/`; `TERRAIN_DATA.agricultureCapacity` is not the canonical fertility | E-R12 to E-R15, E-18, L-11; O-6 |
| X-12 | power | E-9: the ladder array is sorted top-first by standing | REFUTED: a SEAT order; `standing` is a separate stock; a negative gap is folded into "leads by very little" | P-R5, L-105 |
| X-13 | power | E-16 (b): `merchant_league` unreachable, `mixed` fail-soft | HOLDS and UNDERSTATED: 21 of 52 labels read `mixed`; `Royal Authority`, `Noble Governorship`, `Ducal Governorship`, `Feudal Stewardship`, `Headman's Authority` read `council`; `Military Council` reads `autocrat` (the lens probe) | P-16, L-97; O-17 |
| X-14 | power | A-4: "the guard" is an always-safe generic | NOT always safe (A-β); the engine's small-tier generic is `The community` | A-3; O-2 |
| X-15 | power | E-7 `threat` = an EXTERNAL danger; E-8 `commerce` / `survival` from archetype or goal only; E-20 a lawful passage leaves the defeated house demoted; `{faction}` in DS-POW-6 is "the acting faction"; the row shape `{government, cause, tick, by}` | REFUTED as "external" (the town's own mobilization counts); `commerce` is the fail-soft, `survival` also from `break_vassalage`; demotion only under Register VII; `{faction}` is `governingName`; the shape is `{label, cause, tick}` | P-7, P-8, P-20, P-14, P-R2 to P-R4; L-99, L-104 |
| X-16 | power | E-4: small tiers have no office-kind institution | PLAUSIBLE, not verified (no tier table found for `Town hall`); the three-standing law makes the point without it | P-4 |
| X-17 | stressors | R3 resistance, R5 the collector, R9 the death: NOT ENTAILED | ON THE RECORD for generation-built towns (F1): CONDITIONAL on `stress[].source` | S-3, S-5, S-9 |
| X-18 | stressors | R5: the creditor is OUTSIDE (ENTAILS) | CONDITIONAL: under `Royal Authority` with a noble institution the creditors include RESIDENT houses | S-5 |
| X-19 | stressors | R8: the quarantine variants CONTRADICT the engine (the quarantine sentence is gated on `hasWatch`) | wrong: `safetyProfile.js:134-143` writes a quarantine sentence at EVERY tier with an informal fallback; CONDITIONAL on a generation-built record | S-8 |
| X-20 | stressors | R14: conscription appears only in the loss branch (NOT ENTAILED) | HOLDS: the TYPE definition asserts it for every wartime town (`stressTypes.js:156`) | S-14 |
| X-21 | stressors | R6: the world record "flatly denies" from-within | overstated: the act is local in every variant, the motive is not | S-6 |
| X-22 | stressors | R17: `unattributed` entails no attacker and is bound to siege / wartime / occupation; `palace_coup` is noble-led | REFUTED (F5: written onto any type and survives naming; F6: the fallback) | S-17, S-R16 |
| X-23 | stressors | R19: the E-3 protection is defeated at the call site for every condition | narrower: persisted conditions carry the canonical status; only raw / legacy partials are defaulted at display | S-19; O-12 |
| X-24 | stressors | R23: "several" is always two at birth | REFUTED: Mode 3 only; Modes 0 and 2 and `APPLY_STRESSOR` exceed it | S-23, S-R21 |
| X-25 | stressors | R13: the settlement is town-tier or larger | REFUTED: only the probabilistic Mode 3 checks `requiresTier` | S-13, S-R12 |
| X-26 | warFaith | siege: an army physically present (ENTAILS); occupation: a garrison present as the holder's instrument (ENTAILS) | both REFUTED (a marching army counts on the display; `occupierStillPresent` is a separate read) | WF-1, WF-3, WF-R1, WF-R2; O-4 |
| X-27 | warFaith | rank = a seat count (`major` ≥ 4 seats) | REFUTED for the routed pool (`rankAxis` is the authored embed rank); the seat-count entailment belongs to the unrouted PANTHEON RANK pools | WF-19, WF-R7; O-18 |
| X-28 | warFaith | `{rival_creed}` is the first non-patron in share order, "not the nearest rival" | HOLDS as the nearest rival (ranks are sorted share-desc; the survey's caveat was wrong) | WF-23 |
| X-29 | warFaith | compliance = the ledger classified the clause | REFUTED as truth: the OBSERVED state under fog | WF-11, WF-R5 |
| X-30 | warFaith | (no row) `live: false`; `At war`; `cults[]` present; the `STANDINGS` keys | added by the refuter: `live` is a projection status (REFUTED as "newly founded"); `At war` has no licensed member; `cults[]` is static; dominant is a 6-point margin | WF-33, WF-R6, WF-R10, WF-23 |
| X-31 | the draft law | ADDENDUM 10 item 3: "the {defwork} keeps itself, and the muster's wages do not" LICENSED on WALLED-STRAINED as the engine's own asymmetry | superseded by the chair's own ADDENDUM 12 R-viii' (one purse; the wall STANDS THROUGH the shortfall; never "keeps itself"); the defense refuter goes further (X-1 above: "wages" is a label read on a wage-less roster). The verbatim law in section 1 still carries the superseded sentence | D-31, D-3; section 1 |
| X-32 | the draft law | ADDENDUM 10 item 5: "the guard", "the muster", "the men on the wall" always safe | disputed by the power refuter and the defense packets (O-2) | A-3, A-4; O-2 |
| X-33 | the draft law | ADDENDUM 10 item 4: `settled` = "no live threat, never founded" | the producer token is `heartland` = the LOW monster / raider tier (O-20) | L-2; O-20 |
