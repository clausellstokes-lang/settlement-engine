# REFERENT REFUTE — THE GENERAL DESK (overview / relations / population / hooks)

Refuter: Fable 5.1. Dock `laneRW-DEFW` at `f2da5a3ee`, READ-ONLY (nothing modified, staged or committed; no vitest, no npm, no build; the only node runs were print-only scripts over `docs/content/wiring-census.json` and `src/data/institutionServices.js`). Surveyor packet: `general.referent.survey.md` (483 lines, read whole). Every cite below re-derived by `sed -n` / `grep -an` in the dock at f2da5a3ee. Verdicts: **HOLDS** (true for every member and consistent with the engine) · **CONDITIONAL** (true only for named members or only where a named field resolves; the condition is stated) · **REFUTED** (false for some member or contradicted by the engine; the code is quoted). Default to CONDITIONAL/REFUTED when uncertain.

STATUS: COMPLETE (attempt 2; attempt 1 was cut off after its header; all nine sections written). Sections: 0 scope+cites · 1 the addendum cites and §1.1 · 2 the noun table · 3 reads by layer · 4 the overlaps · 5 the findings F-1…F-48 · 6 §§6–9 (summary, raised items, record words, precedent) · 7 ADDED OVERLAPS · 8 MISSED NOUNS · 9 the refuter's summary for the chair.

---

## 0. THE SURVEY'S SCOPE FIGURES AND CITES, RE-MEASURED

| # | surveyor's claim | re-derivation | verdict |
|---|---|---|---|
| S-1 | desk key `general` at `prose-wave-gate.mjs:294` (`SECTION_LEAVES`), row `:297`; projector title/prefixes at `generate-dossier-state-prose.mjs:114` | `sed -an 294,300p`: `export const SECTION_LEAVES = Object.freeze({` at 294, `general: DOSSIER_STATE_PROSE_GENERAL,` at 297; `DESKS` at `:108`, the general row (`prefixes: ['DS-GEN-', 'DS-REL-', 'DS-POP-', 'DS-HK-']`) at `:114` | HOLDS |
| S-2 | census: 193 rows · RESOLVED 119 · WIRING-UNRESOLVED 74 · covert 0 · 23 blocks | node over `docs/content/wiring-census.json`: desk rows 193; status `{WIRING-UNRESOLVED: 74, RESOLVED: 119}`; covert `{false: 193}`; 23 blocks (DS-POP-1/2/3, DS-GEN-1,2,3,5…18, DS-REL-1/2, DS-HK-1) | HOLDS |
| S-3 | holder-kind distribution (none 162 · treasury 7 · elders 5 · road 5 · market+toll-bar 4 · market 4 · toll-bar 3 · office 3); ten STATE-ORGAN rows | node: `source.kind {(none): 162, treasury: 7, toll-bar: 3, office: 3, elders: 5, road: 5, 'market + toll-bar': 4, market: 4}`; `stateOrgan {false: 183, true: 10}`; the ten are DS-GEN-3 viable ×2, DS-GEN-11 ×5 (treasury, LICENSED) and DS-GEN-7 ×2 + DS-REL-2 ×1 (office, OFFICE) | HOLDS |
| S-4 | not one pool resolves the `watch`, `muster`, `court`, `parish`, `census` or `tradition` kind | the seven kinds present are exactly those in S-3; the desk's reads matching `/captur\|impair\|patron\|watch\|corrupt\|safety\|crim/` are ONE: `head (via SAFETY_POOL_OF in generalStateProse.js)` | HOLDS |
| S-5 | the word table over 496 variant-bearing lines (watch 13 · rolls 18 + roll 8 · hall 15 · books 11 + book 4 · seat 7 · muster 3 · market 24 · walls 11 · elder 0 · guard 0 · garrison 2 · court 2 · officers 1 · levy 1 · patron 3 · temple 2) | re-counted by the surveyor's own method (annex ranges 4831–5177, 5178–5962, 6039–6265; lines carrying `` `[ `` ; angle tags stripped; `grep -oiw`): 496 lines; **every figure reproduced exactly** | HOLDS |
| S-6 | `DS-GEN-4` folded into `DS-STR-1` (`RECEIPT_POOLS_DOSSIER_STATE.md:5366`) | `:5366` = `### DS-GEN-4: Overview › Active crisis banners (stressors): **FOLDED INTO DS-STR-1**` | HOLDS |
| S-7 | the card's only referent line is `· a STATE ORGAN (interested where the town is captured)` (`scripts/lib/prose-licence-card.mjs:333`) | `sourceText` at `:329`; the organ string at `:333`; no body/role/power column anywhere in the function (`:329-340`) | HOLDS |
| S-8 | `grep` on `scripts/prose-wave-gate.mjs` returns nothing silently without `-a` | `grep -n "SECTION_LEAVES" scripts/prose-wave-gate.mjs` printed NOTHING, rc 0; `file` reports `a /usr/bin/env node script executable (binary data)`; `grep -an` finds `:294`, `:297` | HOLDS (CONFIRMED by execution) |
| S-9 | the survey's own cite corrections (§10.1) | spot-checked the ten that matter for the law: `corruption.js:663` ✓ · `holderTable.js:683` ✓ (`absent.push(captured-at-birth (${kind} is not one of…`) · `:707` ✓ · `:770` ✓ · `:563-570` ✓ · `defenseScoreBands.js:39` ✓ · `hookEscalation.js:396/407/419/420/421` ✓ · `supplyChainState.js:486` ✓ (`return 'unattributed'`) · `rulingStructure.js:787/792/797` ✓ · `narrativeGenerator.js:632` ✗ — the note string carrying `${govFaction.faction}` is at **`:633`** (`:629` `notes.push({`, `:630` type, `:631` severity, `:632` tab, `:633` note) | HOLDS except one line-drift (633, not 632) |

---

## 1. THE ADDENDUM CITES (§1) AND THE THREE THINGS THE ADDENDUM DOES NOT SAY (§1.1)

| # | surveyor's claim | re-derivation | verdict |
|---|---|---|---|
| A-1 | BODY buckets at `defenseInstitutionBuckets.js:83-104` plus a seventh `magicDef` at `:105` | `DEFENSE_BUCKET_KEYWORDS` `:83`; walls `:84-87`; garrison `:88-91` (`'garrison', 'barracks', 'professional guard', 'professional city watch', 'multiple garrison'`); militia `:92-94`; watch `:95-97` (`'town watch', 'city watch', 'professional city watch'`); mercenary `:98-100`; charter `:101-104`; `magicDef` `:105-108` | HOLDS |
| A-2 | "professional city watch" in BOTH garrison (`:90`) and watch (`:96`) | quoted above | HOLDS |
| A-3 | `HOLDER_KINDS` twelve at `:78-81` | `export const HOLDER_KINDS = Object.freeze([` `:78`; the twelve at `:79-80` | HOLDS |
| A-4 | the MUSTER kind = walls · garrison · militia · mercenary · charter (`:188-192`) "plus `force` `:193`, `magicDependency` `:194`, `economicGates` `:195` — all kind muster" | the muster kind carries **TWELVE** tokens, `:188-199`: the eight the surveyor names AND `besiegedBy` `:196`, `besiegingTargets` `:197`, `ticksToDeploy` `:198`, `stretchedThin` `:199` (siege, mobilization and occupation records are muster-kind). Nothing on this desk reads them, but the roster of the kind is misstated | CONDITIONAL — the list is a subset; the four war/occupation tokens are muster-kind too |
| A-5 | the WATCH kind is a STATE ORGAN (`:115`) keeping `blackMarketCapture` `:210`, `criminalCaptureState` `:211`, `safetyProfile` `:212`; the `watch` token's note `:203-209` | all four quoted verbatim; `STATE_ORGAN_KINDS = Object.freeze(['office', 'court', 'treasury', 'watch'])` `:115` | HOLDS |
| A-6 | POWER = `capturedRulingStructure` `:129-143` + `settlementCaptureState` `factionCapture.js:136` | both at the lines named; note `capturedRulingStructure` reads `captureState` off **ANY** faction entry (`:133-137`, `factions.map(...).filter(state !== 'none')`), not only the governing one, and counts `criminal !== 'none'` as captured (`:142`) — so **`adversarial` IS captured for the birth-time reading** | HOLDS on the cite; see O-ADD-4 for the ladder divergence |
| A-7 | INTERESTED machinery: `standingOf` `:642`, organ gate `:677`, ABSENT-BY-RULE `:683`, `interested` `:707` | `export function standingOf(institution, settlement, world = {}, kind = null)` `:642`; `const organ = STATE_ORGAN_KINDS.includes(String(kind \|\| ''))` `:677`; `:683`; `interested: corrupt \|\| corruptImpairment \|\| captured === true \|\| controlled === true \|\| capturedAtBirth === true` `:707-708` | HOLDS (but the surveyor's §3.3 READING of it is refuted below, R-3.3-2) |
| A-8 | corruption: `SECURITY_INSTITUTION_RE` `corruption.js:630`; `compromisedSecurityInstitutions` `:663`; covert/revealed split `:679-680`; the "hidden channel" comment `:670-672` | `export const SECURITY_INSTITUTION_RE = /(watch\|garrison\|constab\|guard\|magistrate\|court\|barracks)/i;` `:630` ✓; function `:663` ✓; `if (corruptionImps.some((imp) => imp?.covert !== true)) revealed.add(inst.name); else covert.add(inst.name);` `:679-680` ✓. **BUT the function's own doc block defines `covert` FIRST as "an unexposed corrupt NPC is homed there (the hidden stooge)" (`:654-655`) and the loop at `:683-688` adds an institution to `covert` when an NPC with `corrupt === true && !ousted` has a home (`npcHomeInstitution` = `factionAffiliation \|\| factionLink \|\| institutionId`, `:648-650`) that name-matches a security institution.** The impairment flag is the SECOND covert route, not the whole of it | CONDITIONAL — the covert channel has two grounds (an NPC-homed corruption and an `impairments[].covert === true` mark); the surveyor and the addendum name only the second |
| A-9 | brokerage: `BROKERAGE_PATRON_SOURCES = ['genesis','captured']` `:60`; `genesisPatronOf` `:202`; `capturedPatronOf` `:228`; binding `:272-295` | all at the lines named; the binding's `covert: house.legality === 'illegal'` is at `:295` — **visibility is keyed on the HOUSE's legality, never on the patron's source** | HOLDS, with the covert key added |
| A-10 | PERSON never a referent: `COLUMN_SOURCES.holderRole` hardcoded null, `read: false` (`institutionTable.js:129-138`); `holderTable.js:32-36` | `holderRole` row `:129-138` ✓ (`source: 'a typed NPC→institution edge'`, `read: false`, note "none exists; `npcProfile.js:341-353` infers a link by name regex and this module does not call it"). **BUT three engine facts narrow the claim:** (a) the NPC schema DECLARES `institutionId` (`settlement.schema.js:444`, `:953`) — the generator does not write it (the legacy note `:955-957` lists the produced shape without it), so the edge is a schema slot that is empty at birth, not a slot that does not exist; (b) `corruption.js:648-650` reads `factionAffiliation \|\| factionLink \|\| institutionId` and `:685-688` name-matches it to a security institution — a PERSON is the GROUND of an organ's covert standing at run time; (c) `officesOf` (`institutionTable.js:377-396`) mints OFFICE words from `npcs[].role` and `.title` and `COLUMN_SOURCES.office` declares that source `read: true` (`:124-127`) — the engine READS role words OFF PERSONS. The measured null is the holder-ROLE column (who is the officer of a holder); it is not "no NPC-to-institution edge anywhere" | CONDITIONAL — true of the holder-role column; false as "the engine never joins a person to an institution" (`corruption.js:685-688` does) and false as "a role word comes only from an institution row" (`officesOf` reads the roster) |
| A-11 (§1.1-1) | a THIRD and FOURTH purse: `monsterUpkeepMult` `:223` floor 0.7, `econHealthMult` `:289` floor 0.45; "all four published side by side on `economicGates` at `:467-471`" | `const monsterUpkeepMult = Math.min(1, 0.7 + (econOutput / 50) * 0.3);` `:223` ✓; `const econHealthMult = Math.min(1, 0.45 + (econOutput / 50) * 0.55);` `:289` ✓. **The publication is CONDITIONAL:** `economicGates: { ...(hasAnyDefense ? { military } : {}), monster, ...(hasLawInfra ? { internal } : {}), economic }` `:467-472`, comment `:462-466` "military/internal are recorded only when their gate has a paid stack to bite … a hamlet with no paid defenses reports no military gate at all" | CONDITIONAL — four multipliers exist; only `monster` and `economic` are always published; `military` needs `hasAnyDefense`, `internal` needs `hasLawInfra` |
| A-12 (§1.1-2) | `hasWatch` feeds military +7 (`:163`) and sits in `hasAnyDefense` (`:178`); internal +18 (`:235`) | `if (inst.hasWatch) military += 7;` `:163`; `hasAnyDefense = … inst.hasWatch …` `:177-178`; `if (inst.hasWatch) internal += 18;` `:235` | HOLDS |
| A-13 (§1.1-3) | "A ROLE WORD IN THIS ENGINE IS A BODY ROW, NOT A PERSON": seven catalog rows; `hasInst('reeve','steward','elder','household council','free elder')` `defenseGenerator.js:151-152` | the seven rows are at the lines named (`Household elder` `:14`, `Village headman` `:20`, `Village elder` `:26`, `Warden's Lodge` `:491`, `Lord's steward` `:1035`, `Priest (resident)` `:1194`, `Village reeve` `:1346`); `:151-152` ✓. **BUT** (a) `hasInst` is a SUBSTRING match (`:89-90` `nativeSemanticName(i).toLowerCase().includes(kw)`), so `'elder'` also reaches `Elder Grove Council` (`institutionServices.js:497`, services: Nature arbitration · Druidic consultation …) and `Informal elder consensus` (`:1448`), neither of which carries `Record of custom` and neither of which is an elders-kind holder; and (b) `officesOf` mints role words from the NPC roster (A-10c). So on this desk a role word CAN be a body row, and it can also be a druidic body or a person's title | CONDITIONAL — holds for the seven named rows on the seven named reads; false as a general law of the engine |

---

## 2. THE NOUN TABLE (§2) — LAYER ASSIGNMENTS TRIED AGAINST THE ENGINE

Verification method for every "always-safe class word": a node sweep over the 263 top-level keys of `src/data/institutionServices.js` (the shipped roster; `src/data/institutionalCatalog.js` carries no `name:` fields) for each keyword the row depends on. The sweep's material results: **`guard` 0 rows · `magistrate` 0 · `constab` 0** · `barracks` 1 (`Barracks` `:1555`) · `watch` 3 (`Professional city watch` `:1206`, `Town watch` `:1322`, **`Watchtower` `:1561`**) · `market` 10 (`Market`, `Slave market`, `Weekly market`, **`Black market`**, **`Black market bazaar`**, `Daily markets`, `Fish market`, `Market square`, `Multiple market squares`, **`Whisper market`**) · `elder` 4 (`Household elder`, `Village elder`, `Elder Grove Council`, `Informal elder consensus`) · `council` 3 (`Town council`, `Elder Grove Council`, `Mayor and council`) · `hall` 8. Custom-content institutions reach NONE of the booleans: `nativeSemanticName` returns `''` for materialized custom content (`customContentSemanticAuthority.js:40-41`), and every `has*` flag and `hasInst` run through it (`priorityHelpers.js:42`, `defenseGenerator.js:90`).

### 2.1 the order and force family

| noun | surveyor's assignment | tried against | verdict |
|---|---|---|---|
| the watch | B ✔ H ✔ P ✔; always-safe: "the guard for the body; the watch only where the roster row resolves and the read is the order record" | B rows ✓ (`priorityHelpers.js:48` `hasWatch: hasAny(names, ['town watch','city watch','professional city watch'])`; `:45` `'watch'` inside `hasMilitaryInst`; bucket `:95-97`; scores `:163`, `:235`). H ✓ (`holderTable.js:318-322`: services `Crime reporting · Crime response · Missing persons`; the shipped carriers are `Professional city watch` → `Crime reporting` `on: true, p: 0.8` `:1208`, `Missing persons` `on: false, p: 0.4` `:1209`; `Town watch` → `Crime response` `on: false, p: 0.7` `:1325` — so the WATCH HOLDER is a per-town roll even where a watch row stands). P ✓. Display spelling `safetyProfile.js:30-35` ✓ — and it is `'local watch'` for every tier below town **whether or not any watch institution exists** (the label is tier-derived, `tier === 'town' ? 'town watch' : tierAtLeast(tier,'city') ? 'city watch' : 'local watch'`). **"the guard" as the safe body word: no shipped institution row contains `guard` (sweep: 0); the garrison bucket's `'professional guard'` (`:89`) and `hasGarrison`'s `'professional guard'` (`:46`) are keywords with no member; and `'guard'` is read by the engine as a FACTION-NAME substring naming the enforcement POWER (`factionDynamics.js:232` `mil = factions.find(f => f.faction?.toLowerCase().includes('military') \|\| …includes('guard'))`)** | CONDITIONAL — the three layers hold; "the guard" is safe ONLY as a class word for the paid force where `hasGarrison` resolves, is grounded in no shipped row, and is also a power word in the capture ladder; the watch HOLDER additionally needs the service roll |
| the guard | B ✔ P ✔; "a body word, never a holder" | `'professional guard'` in bucket `:89` and `hasGarrison` `:46` ✓; `'guard'` in `hasMilitaryInst` `:45` ✓; glyph `barracks` = `/(barracks\|garrison\|guardhouse\|guard house)/i` `glyphAssign.js:98` ✓; `SECURITY_INSTITUTION_RE` ✓. **Zero shipped rows match any of them**; the only shipped ground of the word is `factionDynamics.js:232` where it names a faction | REFUTED as a BODY-grounded word (no institution row); CONDITIONAL as a class word; the P column is understated (a faction-name slot, not only an impairment) |
| the garrison | B ✔ H(muster) ✔ P ✔ | `hasGarrison` `:46`; bucket `:88-91`; `+28` `:161`, `+20` `:201`, `+15` `:234`; `garrison → muster` `holderTable.js:189`; `SECURITY_INSTITUTION_RE`; shipped rows `Garrison` `:185`, `Multiple garrisons` `:1127`, `Barracks` `:1555` | HOLDS |
| the militia / the muster | B ✔ H ✔; roster-backed by ONE institution (`Citizen militia`) | `hasMilitia` `:47`; bucket `:92-94`; `holderTable.js:280-287` note quoted ✓; `Muster training` under `Citizen militia` `:835-837` — **`on: false, p: 0.5`**, so even the one carrier instantiates the muster record by roll | CONDITIONAL — holds on the roster; the muster HOLDER resolves only where the `Muster training` row materialised |
| the mercenary company / free company | B ✔ H(muster) ✔ | `hasMercenary` `:49` = `['mercenary company','mercenary quarter','hired muscle','hireling hall','free company hall',"veteran's lodge"]`; `hasFreeCompany` `:50`; bucket `:98-100` = `['mercenary company','mercenary quarter','hired muscle']`. **`'hireling hall'` is ALSO in `hasCharterHall` (`:51`)**, and `'free company hall'` / `"veteran's lodge"` set `hasMercenary` but sit in NO bucket (the bucket has three keywords, the boolean six) | CONDITIONAL — the boolean and the bucket disagree on three members; a Hireling hall is both mercenary and charter |
| the charter hall | B ✔ H(muster) ✔ | `hasCharterHall` `:51`; bucket `:101-104`; scores `:165`, `:200`, `:237`; `holderTable.js:192` | HOLDS |
| the walls / the gates | B ✔ H(muster) ✔; `Gates (if walled)` a toll-bar row | `hasWalls` `:52`; `hasGates` `:53` = `['gates','town walls','city walls','massive walls','palisade']` — **a palisade or a wall with no gate row sets `hasGates`**; bucket `:84-87`; `walls → muster` `:188`; `Gates (if walled)` → `Toll collection` `:1551` (toll-bar) ✓ and in `hasWalls` ✓ | HOLDS, with the `hasGates` reach added |
| the court | B ✔ H ✔ P ✔; "the seat where the row is a hall; the court only where courthouse" | `hasCourtSystem` `:55` = `['courthouse','court buildings','democratic assembly','city hall','town hall']`; `+20` `:232`; court kind `holderTable.js:325-329` services `Criminal trials · Civil disputes · Notary services · Criminal proceedings · Civil litigation · Appeals`, `dutyNamed: 0`; its FIELD tokens are the seat's (`govMultiplier` `:215`, `governanceFractured` `:216`, `breakdown` `:217`, `blocs` `:218`, `stability` `:219`, `recentConflict` `:220`, `termLines` `:221`, `fraying` `:222`, `yearsRemaining` `:223` — the survey lists four of nine) | HOLDS (the field list is a subset) |
| the prison / the gaol | B ✔ | `hasPrison` `:54`; `+15` `:233`; rows `Small prison/stocks`, `Large prison`; no kind | HOLDS |
| the magistrate / the constable / the barracks | "exist ONLY as members of `SECURITY_INSTITUTION_RE`"; unreachable on this desk | magistrate, constab: 0 shipped rows ✓. **`Barracks` IS a shipped row (`institutionServices.js:1555`), a garrison-bucket keyword (`:89`), a `hasGarrison` member (`:46`) and a glyph member (`glyphAssign.js:98`)** | REFUTED for the barracks (it is a garrison body on every layer the garrison has); HOLDS for magistrate and constable |

### 2.2 the seat, the record and the purse

| noun | surveyor's assignment | tried against | verdict |
|---|---|---|---|
| the hall / the council / the seat | Town council = elders + toll-bar; Town hall = treasury + office; City administration = treasury + office; `{seat}` never baked | `Town council` `:32`: `Market charter and tolls` `:35` (toll-bar) + `Record of custom` `:37` (elders) ✓. `Town hall` `:1623`: `Tax payment` (treasury) + `Record filing` (office) ✓. `City administration` `:39`: `Public records` `:45` (office) + the treasury cite at `holderTable.js:277` names it ✓. **`City hall` `:1629`: `Public record access` (office) only — NO treasury service; the treasury cite (`:277`) does not name it.** The survey's row text ("Town hall / City hall / City administration — a treasury roster row AND an office roster row") is wrong for City hall. `{seat}` rule `RECEIPT_POOLS_DOSSIER_STATE.md:194`, `:1816` ✓ | CONDITIONAL — holds for Town council, Town hall, City administration; City hall is office-only (and a `hasCourtSystem` member `:55`) |
| the elders | B/R the same rows; H kind `elders` roster `Household elder, Village elder, Village headman, Town council` | `Record of custom` at `:17` (Household elder), `:24` (Village headman), `:30` (Village elder), `:37` (Town council) ✓ — and the Town council's own desc reads *"Keeps the charter, rolls, and civic archive"* (`:37`). The generator's `hasInst('elder')` reaches two more rows (A-13) | HOLDS on the holder roster; the B/R "same rows" claim is CONDITIONAL (A-13) |
| the reeve · the headman · the steward | role words are BODY words; `Village reeve` → treasury; `Village headman` → elders AND treasury; `Lord's appointee` → treasury | `Village reeve` `:1346` with `Tax collection` `:1349` (`on: false, p: 0.7`) ✓; `Village headman` `:20` with `Tithe and dues` + `Record of custom` `:23-24` ✓; `Lord's appointee` `:1030` with `Tax collection` `:1032` ✓; `Lord's steward` `:1035` — in NO holder cite (the survey does not claim one) | HOLDS (on the treasury rows the record row is `on: false` for the reeve — a roll) |
| the treasury | H ✔ P ✔ | `holderTable.js:273-277`; tokens `:166-169`; `STATE_ORGAN_KINDS` `:115` | HOLDS |
| the office | H ✔ P ✔; standing always OFFICE, never LICENSED (`:563-570`) | `sourceOfRow` `:528`; the OFFICE branch `:564-575` (`standing: 'OFFICE'` `:570`, `stateOrgan: true` `:573`); the field-level `standing: 'OFFICE'` at `:489`. **Its own service `Public records` registers "deeds, wills, births, and freemen" (`institutionServices.js:45`)** — a population register the survey's §8.1 does not credit to any kind | HOLDS, with the births register noted |
| the toll-bar | H ✔ | `:304-308`; tokens `:183-185`; carriers `Gates (if walled)` `:1551`, `Major Port` `:199` (`Customs brokerage`), `Town council` `:35` | HOLDS |
| the census | H ✔; carriers `Democratic assembly`, `Royal seat`; no general-desk pool reads it | `:290-294`; `Citizen registration` `:895` under `Democratic assembly` `:891` — **`on: false, p: 0.5`**; `Noble registration` `:1233` under `Royal seat` `:1231` — **`on: false, p: 0.5`**. **`Democratic assembly` is ALSO a `hasCourtSystem` member (`priorityHelpers.js:55`)** — the census holder is a court-system body for DS-GEN-17 ADMINISTERED | CONDITIONAL — the census holder is a roll even on the two carriers; the overlap with the court body is unrowed (O-ADD-9) |
| the tradition | H only; `rosterBacked: false` | `:339-346` quoted ✓ | HOLDS |
| the road holder | H ✔; carriers `Listening post`, `Waystation`; tokens `terrainType` `:233`, `monsterThreat` `:234` | `Road register` `:1653` under `Listening post` `:1652` (`on: true, p: 1.0`); `Way-bill registration` `:408` under `Waystation` `:405` (`on: false, p: 0.6`); tokens ✓ | HOLDS |

### 2.3 the civic and economic family

| noun | surveyor's assignment | tried against | verdict |
|---|---|---|---|
| the market / the stalls / the exchange | B: `MARKET_NAME` `generalStateProse.js:1109`; H: kind `market`, carrier `Market square`; `Weekly market` also a treasury row | `MARKET_NAME = /\b(market\|bazaar\|exchange\|shambles\|stalls)\b/i` `:1109` ✓; `marketPoolKey` `:1138-1150` tests it over `liveInstitutions(state)` ✓. **The word-bounded `market` matches `Black market` (`institutionServices.js` key), `Black market bazaar`, `Whisper market`, `Slave market`, `Fish market`: a town whose only market-class row is a Black market resolves `MARKET-OPEN`/`MARKET-NARROW`/`ENTREPOT`, and DS-GEN-13 says the town "keeps a market". `hasMarket` (`:56`, substring `'market'`) does the same.** `Weekly market` is a treasury row (`:524-526`, `Tax collection`) and the market kind's service under `Market square` (`:1065`) ✓ | REFUTED as "the market body" — the body class the read resolves contains the criminal houses; the row's layers hold only on the licit members |
| the church / the temple / the parish | B ✔ H ✔ R (`Priest (resident)`) | `hasChurch` `:65` eight kinds ✓; parish kind `:297-301`; `Register of the dead` `:1413` under `Parish burial grounds` `:1411` ✓; `Priest (resident)` `:1194` ✓. Add: `Church/Temple` is ONE shipped row carrying both class words; and a governing faction NAME containing `temple`/`clergy`/`order` is read as non-secular (`narrativeGenerator.js:625-627`) — the temple word is also a POWER-name substring | HOLDS, with the power-name overlap added (O-ADD-3) |
| the granary / the storehouse | B ✔; civic classes `store` vs `storehouse` | `hasGranary` `:63` substring `'granar'`; classes `wiringCensus.js:1309`, `:1322` ✓ | HOLDS |
| the hospital / the healer | B ✔ | `hasHospital` `:64` (`monastery` sets it and `hasChurch` `:65`) ✓; `+5` `:204`; class `care` `:1315` | HOLDS |
| the port / the harbour / the navy | B ✔ H(toll-bar via Major Port) | `PORT_INFRA_RE` `:32`, `hasPort` `:61`, `hasNavy` `:62`; `Major Port` → `Customs brokerage` `:199`; `Navy` is a shipped row | HOLDS |
| the guild / the trade | B ✔ | `hasGuild` `:57` substring; class `craft` `:1316` | HOLDS |
| the magic house | B ✔ R ("a practitioner") | `:68-71`; bucket `magicDef` `:105-108`; scores `:167-173` | HOLDS |
| the criminal house | B ✔ P ✔ | `hasCriminalInst` `:72` …; `computeCriminalCaptureState` `factionDynamics.js:220-232` ✓ — and it reads a FACTION NAME containing `'thiev'` as criminal (`:221`) | HOLDS, with the name-substring added |
| `{institution}` (a workshop) | B; values are category labels | `craftInstitutionFill` `:1259-1268` ✓; `RECEIPT…:172` ✓ | HOLDS |
| `{steading}` · `{ruin}` | B, no holder | `steadingPoolKey` `:1442`, `ancientRuinPoolKey` `:1417` ✓ | HOLDS |

### 2.4 the power family

| slot | surveyor's assignment | tried against | verdict |
|---|---|---|---|
| `{faction}` / `{faction2}` | proper; `:171`, `:198` | quoted ✓ | HOLDS |
| `{govFaction}` | proper; `:201`; producer `narrativeGenerator.js:632` | `:201` ✓; the note string is at `:633` (S-9) | HOLDS (line 633) |
| `{governing}` | `governingName` or the literal `'the governing faction'`; `hookEscalation.js:511`; `rulingStructure.js:787` | `stages: tmpl.stages.map(s => fillStage(s, { governing: govName \|\| 'the governing faction' }))` `:511` ✓; `governingName:` `:787` ✓ | HOLDS |
| `{controller}` | NOT a power: filled from a chain's institution or `'unattributed'` | `inferController` `supplyChainState.js:482-487` ✓ (`return 'unattributed'` `:486`); the design comment `:473-477` says "usually a faction or institution that takes a rent" — the CODE fills institution only; `hookEscalation.js:481`, `:493` ✓. **The engine's OWN stages use `{controller}` as an AGENT: "Workers accuse {controller} of hoarding." (`:394`), "{controller} loses revenue; tax base shrinks." (`:405`)** | HOLDS, and the finding is sharper: the engine itself renders the body-filled slot as a power |
| `government` / `governingName` | `:787`, `:792` | ✓ | HOLDS |
| `criminalCaptureState` | computed `factionDynamics.js:220-232`, `rulingStructure.js:755`, published `:797`; watch kind `holderTable.js:211` | ✓ all four | HOLDS |
| per-faction `captureState` | stamped on the GOVERNING entry at `equilibrium`+ (`:764-766`); read by `capturedRulingStructure` `:133-137` | `if (['equilibrium','corrupted','capture'].includes(criminalCaptureState)) { govEntry.captureState = … }` `:764-766` ✓, the comment `:762-763` "'adversarial' is not seeded". **`capturedRulingStructure` reads ANY faction's `captureState` and counts `criminal !== 'none'` (`:142`) — so at `adversarial` the birth-time capture is TRUE for the state organs while no faction carries the stamp; and `standingOf`'s `world.captureState` counts only `corrupted`/`capture` (`:659`).** Three ladders, three thresholds | CONDITIONAL — the cite holds; the three readings of "captured" disagree at the `adversarial` rung (O-ADD-4) |
| corruption impairment | `:630`, `:674-681`; read by no general-desk pool | ✓; and the NPC channel (A-8) | HOLDS, with A-8 |
| brokerage PATRON | `:60`, `:202`, `:228`, `:272-295`; read by no general-desk pool | ✓; covert = illegal house `:295` | HOLDS |

### 2.5 the person

| claim | tried against | verdict |
|---|---|---|
| `{npc}` filled by `localNpcFill` from `primaryNPCName`, this town's end only (`:1534-1541`); the one person-naming pool; card read `(not-produced)` | `localNpcFill` `:1534-1541` ✓ (`primaryNPCName`); `crossSettlementNpcPoolKey` `:1502-1507`; annex `:5560` ✓; `:173` ✓ | HOLDS |
| the engine's refusal is of an NPC→INSTITUTION EDGE and the card's of a FATE, "neither is a refusal to name" | `institutionTable.js:134-136` ✓; `:173` "never minted, no fate resolved" ✓. Per A-10 the engine's refusal is narrower still: the holder-ROLE edge is what is null; `officesOf` names offices off persons and `corruption.js` homes persons in institutions | HOLDS (and the tension is real; the chair's call) |

---

## 3. THE READS BY LAYER (§3)

| # | surveyor's claim | tried against | verdict |
|---|---|---|---|
| R-3.1-1 | `readings.inst` → DS-GEN-17's five keys over the boolean roster (`generalStateProse.js:1178-1186`) | `institutionsPoolKey` `:1178-1186` ✓: `ADMINISTERED` = `hasCourtSystem && hasPrison`; `GARRISONED` = `hasMilitaryInst \|\| hasNavy \|\| hasWatch`; `LETTERED` = `hasMagicInst`; `PROVISIONED` = `hasGranary \|\| hasHospital`. **`hasWatch` is REDUNDANT inside GARRISONED (every `hasWatch` spelling contains `watch`, which `hasMilitaryInst` matches at `:45`), and `hasMilitaryInst`'s `'watch'` substring also fires on `Watchtower` (`institutionServices.js:1561`), which is neither `hasWatch` nor a watch-bucket member nor a watch-kind holder** | HOLDS, with the Watchtower reach added (O-ADD-1) |
| R-3.1-2 | DS-GEN-13 reads a market-class row, ruin-filtered (`:1138-1152`; `institutionRoster.js:30`, `:53`) | `marketPoolKey` `:1138-1150` uses `liveInstitutions(state)` ✓; `INACTIVE_STATUS` `:30` ✓; `liveInstitutions` `:53-56` ✓. The class includes the criminal market rows (§2.3) | CONDITIONAL — the body read holds; its referent may be a Black/Whisper/Slave market |
| R-3.1-3 | DS-GEN-18 reads a processing institution by category label (`:1259-1268`, `:1332`) | ✓ | HOLDS |
| R-3.1-4 | DS-GEN-8 reads a steading / a ruin (`:1417`, `:1442`) | ✓ | HOLDS |
| R-3.2-1 | military axis components `:159-191` (walls +30 · garrison +28 · mercenary +14 · militia +10 · charter +10 · watch +7 · arcane · divine · community base · terrain · the military purse) | `:160-165` ✓, `:167-173` arcane ✓, `:175` divine ✓, `:181` terrain ✓, `:189-191` purse ✓ | HOLDS |
| R-3.2-2 | internal axis `:231-254` (court +20 · watch +18 · prison +15 · garrison +15 · militia +8 · charter +5 · … minus crime) | `:232-237` ✓; `hasLawInfra` `:244` ✓; purse `:251` ✓; crime `:254` ✓ | HOLDS |
| R-3.2-3 | monster axis `:195-228` (charter +35 · garrison +20 · walls +20 · militia +12 · hospital +5 · traditions · monster purse · plagued −15) | `:200-204` ✓, `:223` ✓, `:227` ✓ | HOLDS |
| R-3.2-4 | readiness = "the AVERAGE of the five axes + tier bonus − threat penalty" (`:507-522`) | `const dims = magicExists ? [military, monster, internal, economic, magical] : [military, monster, internal, economic]` `:503-505` — **four axes in a no-magic world**; `readiness = Math.min(100, Math.max(0, avgScore + tierBonus - threatPenalty))` `:510` ✓; bands `:515-521` (`Lightly Defended` at `>= 24` `:519` ✓) | CONDITIONAL — five axes only where `magicExists`; four otherwise |
| R-3.2-5 | safety: `head` via `SAFETY_POOL_OF` (`:240-249`, `:320`); field token `safetyProfile` → watch (`holderTable.js:212`) | `SAFETY_POOL_OF` `:240-250` ✓; `safetyPoolKey` `:320-324` ✓; `:212` ✓ | HOLDS |
| R-3.2-6 | THE AGGREGATE RULE: 29 pools read a number/word computed over bodies and license no body | the reads re-measured: DS-GEN-3's 42 rows read `axis`+`score` (20), `text(label)` via PROSPERITY (5) / READINESS (6) / FOOD (6), `head` via SAFETY (3), `readings.viable` (2) — the 29 = 20 + 6 + 3 ✓ | HOLDS |
| R-3.3-1 | the general desk has NO standing read (covert 0; no capture/impairment/patron path; ten state-organ rows) | re-measured ✓ (S-2, S-4) | HOLDS |
| R-3.3-2 | "For those ten pools, `sourceOfForTown` will return `standing: INTERESTED` instead of `LICENSED` on any town whose `criminalCaptureState` is not `none` or whose governing faction carries a `captureState` … roughly a third of towns would flip those ten rows to INTERESTED" | **(a) the three OFFICE rows can NEVER flip:** `sourceOfForTown` returns early `if (base.standing !== 'LICENSED') return { … standing: base.standing … }` (`holderTable.js:724-729`) — an OFFICE row stays OFFICE on every town. **(b) the seven treasury rows flip ONLY where the town instantiates a treasury holder:** `holdersOf` (`:590-607`) walks `instantiatedServices` for a LIVE institution carrying a treasury service; with none, `sourceOfForTown` returns `standing: 'LICENSED'` with `absent: ['holder (this town instantiates no institution that keeps this record)']` (`:746-756`) and never reaches `standingOf`. **(c) the faction check is over ANY faction entry, not the governing one** (`:133-137`). The "third" (`none 495 · adversarial 194 · equilibrium 64 · corrupted 15` `:100-102`) is the right fraction of towns where `capturedRulingStructure.captured` is true (adversarial counts, `:142`), but the flip is conditional on (b) | REFUTED as stated (ten rows; any such town); CONDITIONAL as corrected: seven treasury rows, only with a materialised treasury holder, on any faction's stamp or a non-`none` ladder |
| R-3.3-3 | the frozen covert list is `impairment.covert`, `mobilization.covert`, `blocs.covert` (`wiringCensus.js:1256-1258`) | `COVERT_SOURCES` `:1252-1259` has SIX entries: `compromisedSecurityInstitutions` `:1253`, `npc.corrupt` `:1254`, `corruptNpc` `:1255`, then the three the survey names; `isCovertPath` `:1265-1269` ✓; `row.covert` `:1681` ✓ | CONDITIONAL — the list is twice as long; the NPC channel is on it |
| R-3.3-4 | visibility table: a covert impairment → DM only (`:670-672`, `:679-680`); a revealed one → both faces (`:654-658`, `:679`); birth capture → no audience flag; brokerage → `projectPatronBindings({audience, exposed})` `:319` | impairment rows ✓ with A-8's second covert ground; birth capture: `capturedRulingStructure` carries no audience field ✓ and `sourceOfForTown` prints INTERESTED with the ground in `marks` (`:695`) and no audience ✓; `projectPatronBindings(bindings, { audience = 'dm', exposed = [] })` `:319` ✓ — a LEGAL house's patron projects to the player, an ILLEGAL house's does not unless exposed (`:295`, `:322-325`). Add: the corruption drag counts covert and revealed alike — "a bought watch shields recruits whether or not the town knows it's bought" (`:695-697`) — the effect is public even where the standing is covert | HOLDS, with the two additions |
| R-3.3-5 | the desk's DM face is an angle marker under §0e (`RECEIPT…:455-462`), not a field; nine `· dm-only` variants | `:455-462` quoted ✓; `:4931-4933`, `:5212`, `:5448`, `:5449`, `:5454`, `:5457` carry `· dm-only` ✓ (eight lines printed; the ninth is inside `:5448-5457`'s block per the survey's own list — not re-counted) | HOLDS |
| R-3.4-1 | `link.npcConnections` → the one person-naming read; `link` + `localRelationshipRole` → `patron`/`client`, withheld where absent; `overlord`/`vassal` have no pool (`:1477-1494`; `canonicalRelationship.js:54-57`, `:143`, `:304-307`) | `neighbourTiePoolKey` `:1483-1494` ✓ (`vassal` → `''`); `overlord: 'vassal'` `:54`, `:143`; `overlord_of` `:307` ✓ | HOLDS |
| R-3.4-2 | `readings.inst` (indirectly) licenses role words that are institution rows | per A-13: CONDITIONAL | CONDITIONAL |

---

## 4. THE ENGINE'S OWN OVERLAPS (§4) — O-1 … O-14

| # | verdict | re-derivation / correction |
|---|---|---|
| O-1 | HOLDS | `:90`, `:96` |
| O-2 | HOLDS, with two members missing | (a)-(g) all at the lines named. Missing: (h) `Watchtower` (`institutionServices.js:1561`) is a `watch` for `hasMilitaryInst` (`:45`) and for `SECURITY_INSTITUTION_RE` (`:630`) and has its own glyph (`glyphAssign.js:99` `/\b(watchtower\|watch tower\|signal tower)\b/i`), and is NOT `hasWatch`, NOT the watch bucket, NOT the watch kind; (i) `'local watch'` is the display label at every tier below town with no roster test (`safetyProfile.js:30-35`) |
| O-3 | HOLDS | `:35` + `:37` under `Town council` `:32` |
| O-4 | HOLDS | `:23-24` under `Village headman` `:20` |
| O-5 | CONDITIONAL | `Town hall`: `Tax payment` + `Record filing` ✓ (`:1623-1628`); `City administration`: `Public records` `:45` + treasury cite ✓; **`City hall` `:1629`: `Public record access` only — office, NOT treasury**; the `hasCourtSystem` membership of both halls ✓ (`:55`) |
| O-6 | HOLDS | `:524-526` (institution, `Tax collection`) and `:1065` (service under `Market square`) |
| O-7 | HOLDS | `:199` under `Major Port` `:194`; `:61-62` |
| O-8 | HOLDS | `:1551` under `Gates (if walled)` `:1550`; `:52` |
| O-9 | HOLDS | `:325`, `:115`, `:630`, `:1311`, `:104`, `:55` |
| O-10 | HOLDS | `:88`, `:189`, `:630`, `:1303`, `:98` |
| O-11 | HOLDS | `:92`, `:280-287`, `:1303` |
| O-12 | HOLDS | `temple: [... 'patron']` `wiringCensus.js:1312`; `:60`; `canonicalRelationship.js:302-305` for the relationship value |
| O-13 | HOLDS | `hall: ['hall','council','charter','seat','office','chamber','moot']` `:1314`; `:194`, `:1816` |
| O-14 | HOLDS, and wider | the substring reaches `Elder Grove Council` (`:497`) and `Informal elder consensus` (`:1448`) — A-13 |

---

## 5. THE FINDINGS (§5) — F-1 … F-48

Every one of the 48 quotes was printed from the annex at the line the survey cites and matched verbatim (the print is in this refuter's transcript; the lines are `:5303, :5305, :5306, :5316, :5317, :5349, :5352, :5190, :5189, :5192, :5340, :5341, :5342, :4891, :4873, :4853, :5317, :5214, :5219, :5226, :5231, :5241, :5254, :5256, :5455, :5469, :5473, :5474, :5528, :5567, :5781, :5712, :5789, :6213, :5950, :5553, :5566, :5227, :5819, :6218, :5448, :5777, :5771, :6225`, and `hookEscalation.js:396, :407, :419, :420, :421`). The verdicts below are on the FAULT each row asserts, not on the quote.

| # | verdict | note |
|---|---|---|
| F-1 | HOLDS | `holderTable.js:284-287` note quoted ✓ |
| F-2 | HOLDS | arithmetic checked: a garrisoned village (`communityMilBase` 10 `:145`, +28 `:161`) at the purse floor reads `10 + 28 × 0.6 = 26.8` before terrain and `milEffective` (`:189-191`) → WEAK (20–39, `defenseScoreBands.js:39`) |
| F-3 | HOLDS | `:5297` SLOTS `{settlement}` only; `:1816` |
| F-4 | HOLDS | `:186-191` "The community baseline … is unpaid and exempt" |
| F-5, F-6 | HOLDS | `:232-237`; `hasLawInfra` `:244`; and `dutyNamed: 0` on the watch kind (`:318-321`) |
| F-7 | HOLDS | `:503-510` |
| F-8 | HOLDS | `Lightly Defended` = 24–37 (`:519`); reachable on community base + tier bonus with `hasWalls` false |
| F-9, F-10, F-11 | HOLDS | the tension record is `{...tension, description, factions}` (`historyGenerator.js:477-485`) — no institution field; STATE-KEY `:5181` ✓ |
| F-12 | CONDITIONAL on the wiring claim | the FIELD token maps to `watch` (`:212`) ✓ and the pool's grain is `head` ✓; but the survey's §7 item 6 ("a read-grain fix … would license three of the twenty") needs MORE than the re-key: the holder is resolved only where a live roster row carries `Crime reporting` (`Professional city watch`, `on: true, p: 0.8` `:1208`) or `Crime response` (`Town watch`, `on: false, p: 0.7` `:1325`) or `Missing persons` (`on: false, p: 0.4` `:1209`) — a per-town roll; and the watch is a STATE ORGAN, so on a captured town the standing is INTERESTED, not LICENSED |
| F-13, F-14 | HOLDS | `:5341-5342` |
| F-15 … F-18 | HOLDS | `HOLDER_SOURCES` `:164-240` carries no `migration`/`population` token (re-grepped: zero) |
| F-19 … F-31 | HOLDS | the `{seat}` rule `:194`, `:1816`; each block's SLOTS line printed and matches (`:5182`, `:5442`, `:5499`, `:5543`, `:5720`, `:5685`). F-30: `{governing}` is the typed slot ✓ (`:5720`) |
| F-32 | HOLDS | `hookEscalation.js:519-523` (`powerDelta <= 8 && top.archetype !== second.archetype`) |
| F-33 | HOLDS | the read is `hasCourtSystem && hasPrison` (`:1180`) and reads no roster; note only that "officers" is NOT an unconditionally refused word in the engine — `officesOf` mints office words from `npcs[].role/.title` (`institutionTable.js:377-396`); it is refused HERE because this read does not reach them |
| F-34 | HOLDS | `:207`, `:229` |
| F-35, F-36, F-37 | HOLDS | `:5553`, `:5566`, `:5227`; `crossEngagementPoolKey` `:1516-1520` reads `row.type === 'faction_engagement'` |
| F-38 | HOLDS | `brokeragePatronage.js:60`; DS-GEN-11 reads `readings.viable` (census) |
| F-39 | HOLDS, and stronger | `hasMilitaryInst` `:45` contains `'walls'`, `'citadel'` AND `'watch'`; shipped rows `Town walls`, `City walls and gates`, `Massive walls and fortifications`, `Citadel`, **`Watchtower`** each resolve GARRISONED alone; the gloss `:6205` ("a standing paid force") and the leaf comment `:1157` ("a force the town PAYS for") are both false of the key |
| F-40 | HOLDS | the card's `NAMED BUT NEVER FILLED` (not re-run; the survey's card print is accepted as the surveyor's receipt) |
| F-41, F-42, F-43 | HOLDS | `:484-494` (`chain.status`, `chain.needKey`); `:63`; `:1183` |
| F-44 … F-48 | HOLDS, and three more | `:396`, `:407`, `:419`, `:420`, `:421` ✓. Also baked on the same page: `'Workers accuse {controller} of hoarding.'` `:394` and `'{controller} loses revenue; tax base shrinks.'` `:405` (the body-filled slot as an AGENT), `'The temple opens emergency stores.'` `:395` (a temple body on every town the clock fires for), `'A criminal faction consolidates the new routes.'` `:408` (a power with no name) |

---

## 6. THE SUMMARY, THE RAISED ITEMS, THE RECORD WORDS AND THE PRECEDENT (§§6–9)

| # | surveyor's claim | verdict | note |
|---|---|---|---|
| §6-1 | 29 (and 40 of DS-GEN-3's 42) pools read an aggregate; no noun of any layer is licensed on them | HOLDS | re-measured (R-3.2-6) |
| §6-2 | BODY reachable on 13 pools (DS-GEN-17 ×5, -13 ×4, -18 ×4); only DS-GEN-13 ruin-filters | HOLDS | `:1139` `liveInstitutions`; `institutionsPoolKey` takes the boolean bag, which `getInstitutionNames` builds from the raw roster (`priorityHelpers.js:41-43`) — no ruin filter there ✓ |
| §6-3 | HOLDER reachable on 31 pools across 7 kinds, never the order organs | HOLDS | 7 + 5 + 5 + 4 + 4 + 3 + 3 = 31 ✓ |
| §6-4 | POWER only through `{faction}`/`{faction2}`, `{govFaction}`, `{governing}` (+ mis-typed `{controller}`) | HOLDS | |
| §6-5 | ROLE and BODY are the same rows here | CONDITIONAL | A-13 |
| §6-6 | PERSON refused everywhere except `{npc}`; the engine refuses an EDGE and the card a FATE | CONDITIONAL | A-10: the engine's null is the holder-ROLE column; it does join persons to institutions in `corruption.js` and reads role words off persons in `officesOf` |
| §7-1 | the card has no referent column; a fourth value AGGREGATE may be needed | HOLDS | `sourceText` `:329-340` |
| §7-2 | `{controller}` is a power-named slot filled from a body | HOLDS | `:482-487`; and `:394`, `:405` |
| §7-3 | the DM face is an angle marker; rule 4 binds nothing on this desk as wired | HOLDS | |
| §7-4 | the `{npc}` tension | HOLDS (as a question) | |
| §7-5 | two glosses false about their own key | HOLDS | `:6205`, `:1157` vs `:45` |
| §7-6 | a `SAFETY_POOL_OF` re-key would license three of twenty watch rows | CONDITIONAL | F-12: the re-key resolves the KIND; the HOLDER is a roll and the standing may be INTERESTED |
| §7-7 | the hook stages bake three layers beside DS-HK-1 | HOLDS | and `:394`, `:395`, `:405` |
| §7-8 | four purses, all published together | CONDITIONAL | A-11 |
| §8.1-1 | no `population`/`migration`/`births`/`deaths` token in `HOLDER_SOURCES` (`:164-270`) | HOLDS on the grep (zero); the table spans `:164-240`, not `:270` | |
| §8.1-2 | the kind whose record IS the roll is `census`; carriers `Democratic assembly`, `Royal seat` | CONDITIONAL | the office's `Public records` registers "births … and freemen" (`institutionServices.js:45`), the parish's `Register of the dead` the deaths (`:1413`), and the census services are both `on: false, p: 0.5` (`:895`, `:1233`) — the population record is SPLIT across three kinds and the census carriers hold it by roll |
| §8.1-3 | DS-POP-1/-2 WIRING-UNRESOLVED `reads: []`; DS-POP-3 RESOLVED with `source.kind` `""` | HOLDS | re-measured: the five DS-POP-3 rows carry `fields {readings.populationTrend.band: "", .window: ""}`, `kind ""` |
| §8.2 F-49 … F-83 | HOLDS | every quote printed at its line ✓; F-75 (`road`, LICENSED) and F-82 (`market`, LICENSED) cite `the books` (office/treasury) — the wrong-organ case ✓ |
| §8.3 the (a)/(b) question | HOLDS as a question | the engine's own text puts "rolls" inside a BODY's service desc — `Town council :: Record of custom` *"Keeps the charter, rolls, and civic archive"* (`:37`) — which is evidence for (b) the chair did not have: the engine itself uses "rolls" metonymically inside a non-census holder |
| §9 precedent (`:257-259`) | HOLDS | quoted verbatim |
| §9.1 `dutyNamed` table | HOLDS | treasury 4 · muster 1 · census 2 · parish 3 · toll-bar 3 · market 0 · watch 0 · court 0 · elders 1 · tradition 0 · road 2 · office 3 (`:273-361`) ✓ |
| §10.2 grep hazard | HOLDS (CONFIRMED) | S-8 |

---

## 7. ADDED OVERLAPS — the ones the surveyor missed (each a wiring fact, ADDENDUM 11 rule 5)

| # | word | the rows it sits in | file:line |
|---|---|---|---|
| **O-ADD-1** | **Watchtower** | a shipped institution row; a `watch` for `hasMilitaryInst` (substring `'watch'`) → resolves DS-GEN-17 GARRISONED alone; a `watch` for `SECURITY_INSTITUTION_RE` → can carry a corruption impairment that a face would render as "the watch bought"; its own glyph `watchtower`; **NOT** `hasWatch`, **NOT** the watch bucket, **NOT** the watch kind (no `Crime *` service: `Signal fire · Approaching threat warning · Night watch`) | `institutionServices.js:1561-1565`; `priorityHelpers.js:45` vs `:48`; `corruption.js:630`; `glyphAssign.js:99`; `defenseInstitutionBuckets.js:95-97`; `holderTable.js:318-322` |
| **O-ADD-2** | **market** (the criminal members) | `MARKET_NAME` (`\bmarket\b`) and `hasMarket` (substring) both admit `Black market`, `Black market bazaar`, `Whisper market`, `Slave market`, `Fish market`; DS-GEN-13 "keeps a market" and DS-GEN-3's economic +10 (`defenseGenerator.js:263`) fire on a criminal house; `hasBlackMarket` (`:74`) and `hasCriminalInst` (`:72`) name the same rows on the criminal side — one row, two civic classes (`market` and the criminal house) | `generalStateProse.js:1109`, `:1139`; `priorityHelpers.js:56`, `:72`, `:74`; the keys in `institutionServices.js` |
| **O-ADD-3** | **guard · military · thiev · temple · clergy · order as FACTION-NAME substrings** | the capture ladder finds the enforcement power by faction name (`'military'` or `'guard'`) and the criminal one by `category === 'criminal' \|\| name includes 'thiev'`; the coherence note reads a governing faction as non-secular by `temple`/`clergy`/`order` in its NAME — body words and class words are read as POWER names by the engine, so "the guard" and "the temple" are power words on those reads | `factionDynamics.js:221`, `:232`; `narrativeGenerator.js:625-627` |
| **O-ADD-4** | **captured (three thresholds)** | `capturedRulingStructure` counts `criminalCaptureState !== 'none'` (adversarial included) on any faction's stamp; the birth stamp writes `captureState` only at `equilibrium`+ and only on the governing entry; `standingOf`'s world `captureState` counts only `corrupted`/`capture`. The word "captured" has three referents in the holder table alone | `holderTable.js:133-142`, `:659`; `rulingStructure.js:762-766` |
| **O-ADD-5** | **hireling hall · free company hall · veteran's lodge** | `Hireling hall` sets `hasMercenary` AND `hasCharterHall`; `Free company hall` sets `hasMercenary` AND `hasFreeCompany`; `Veteran's Lodge` sets `hasMercenary`; none of the three is in the mercenary bucket (`'mercenary company','mercenary quarter','hired muscle'`) — the boolean and the bucket disagree | `priorityHelpers.js:49-51`; `defenseInstitutionBuckets.js:98-100` |
| **O-ADD-6** | **gates** | `hasGates` fires on `'town walls'`, `'city walls'`, `'massive walls'`, `'palisade'` with no gate row; `Gates (if walled)` is the toll-bar carrier and a `hasWalls` member | `priorityHelpers.js:52-53`; `holderTable.js:308` |
| **O-ADD-7** | **elder** (the substring's extra reach) | `hasInst('elder')` reaches `Elder Grove Council` (a druidic body) and `Informal elder consensus`; neither carries `Record of custom` | `defenseGenerator.js:89-90`, `:151`; `institutionServices.js:497`, `:1448` |
| **O-ADD-8** | **covert** (two grounds) | an institution is covert-compromised by an `impairments[].covert === true` mark OR by an unexposed corrupt NPC homed there (`factionAffiliation \|\| factionLink \|\| institutionId`, name-matched); `COVERT_SOURCES` lists both channels; the corruption drag counts both alike | `corruption.js:648-650`, `:654-658`, `:679-688`, `:695-697`; `wiringCensus.js:1252-1259` |
| **O-ADD-9** | **Democratic assembly** | a `census` carrier (`Citizen registration`) AND a `hasCourtSystem` member → DS-GEN-17 ADMINISTERED fires on an assembly + stocks; the engine's stage `'Public assembly forces a confrontation.'` bakes the same word | `holderTable.js:294`; `priorityHelpers.js:55`; `hookEscalation.js:421` |
| **O-ADD-10** | **office / officer** (the ROLE layer off persons) | `officesOf` mints office words from `npcs[].role` and `.title` plus the governing seat's name, and `COLUMN_SOURCES.office` declares those sources read — a ROLE word in this engine has a PERSON source as well as an institution-row source | `institutionTable.js:124-127`, `:377-396` |
| **O-ADD-11** | **local watch** | the safety display label is tier-derived (`'local watch'` below town) with no roster test, so a page can say "local watch" on a town with no watch row | `safetyProfile.js:30-35` |
| **O-ADD-12** | **patron (covert by legality)** | a brokerage binding is `covert` iff `house.legality === 'illegal'`, whatever its source (`genesis`/`captured`); the player projection drops the NAME and keeps the house | `brokeragePatronage.js:295`, `:319-325` |
| **O-ADD-13** | **City hall** | office-only (`Public record access`), NOT a treasury row; a `hasCourtSystem` member; carries `Small prison/stocks` and `Holding cells` as SERVICE names — a hall row with a gaol inside it | `institutionServices.js:1629-1636`; `priorityHelpers.js:55` |
| **O-ADD-14** | **Public records (births)** | the OFFICE kind's service registers "deeds, wills, births, and freemen" — a population register in a state organ, beside the census kind's two rolled services | `institutionServices.js:45`; `holderTable.js:357-361` |
| **O-ADD-15** | **Church/Temple** | one shipped row carrying both class words; `hasChurch` matches on either | `institutionServices.js` key `Church/Temple`; `priorityHelpers.js:65` |
| **O-ADD-16** | **custom content** | a materialized custom institution is invisible to every boolean, bucket and holder (`nativeSemanticName` → `''`), so a custom "City Guard" resolves NO layer and no face may name it by class | `customContentSemanticAuthority.js:40-46`; `priorityHelpers.js:42`; `defenseGenerator.js:90` |
| **O-ADD-17** | **economicGates (conditional keys)** | `military` present only with `hasAnyDefense`, `internal` only with `hasLawInfra`; a face reading "the military purse" on a hamlet reads a key that is not there | `defenseGenerator.js:462-472` |
| **O-ADD-18** | **{controller} as an agent in the engine's own stages** | `'Workers accuse {controller} of hoarding.'`, `'{controller} loses revenue; tax base shrinks.'` — the body-filled slot rendered as a rent-taking power by the engine itself, on the page DS-HK-1 frames | `hookEscalation.js:394`, `:405`; `supplyChainState.js:473-487` |

---

## 8. MISSED NOUNS — institution-class nouns and role words of the desk the surveyor did not row

| noun | where it appears | the layer question |
|---|---|---|
| **the watchtower** | shipped row; GARRISONED's trigger; a SECURITY body | body (garrison-adjacent), never the watch kind — O-ADD-1 |
| **the barracks** | shipped row `:1555`; garrison bucket, `hasGarrison`, glyph, `SECURITY_INSTITUTION_RE` | body; the survey filed it as "SECURITY_RE only" (refuted, §2.1) |
| **the navy** | shipped row `Navy`; `hasNavy` (`:62`); GARRISONED's second disjunct | body; no holder kind; no bucket |
| **the citadel / the palisade / the earthwork** | `Citadel`, `Palisade or earthworks` rows; walls bucket; `hasMilitaryInst` (`citadel`); glyph `towered-keep` for citadel (`glyphAssign.js:97`) | body; `{defmaterial}` is the defense desk's wiring |
| **the stocks / the gaol / the holding cells** | `Small prison/stocks`, `Large prison` rows; `City hall`'s services `Small prison/stocks`, `Holding cells` (`:1634-1635`) | body (`hasPrison`); a hall row can carry the gaol as a service |
| **the assembly** | `Democratic assembly` — census carrier + court-system body; the stage at `:421` | O-ADD-9 |
| **the royal seat** | `Royal seat` `:1231` — census carrier (`Noble registration`, rolled) | holder (census) and a seat body; `{seat}` is the typed slot |
| **the mayor / the lord / the sexton / the warden** | `Mayor and council` `:1069`; `Lord's appointee` `:1030`, `Lord's steward` `:1035`; the sexton in `Register of the dead`'s desc (`:1413`); `Warden's Lodge` `:491` | role words inside body rows and service descs; none is an NPC record; `Mayor and council` is NEITHER an elders nor a toll-bar holder |
| **the occupier** | DS-GEN-7 `:5469` *"The occupier collects and does not administer"* | a POWER with no typed slot on this desk (the defense desk's O-4 `occupierStillPresent` row) |
| **the temple** (as a body on a coherence read) | DS-GEN-7 `:5473` *"the temple is what the deciding has to be arranged around"* | body word on a `note.type\|tab` read whose only typed slot is `{govFaction}`; the producer's own test is a faction-NAME substring (O-ADD-3) |
| **the official authority / the actual one** | `:5474` | two unnamed powers on a coherence-note read |
| **the gate** (as an institution) | `:5214` *"a habit of the gate"*, `:5306` *"the gate is shut at night"* | body (`hasGates`, toll-bar `Gates (if walled)`) on reads that resolve neither |
| **the levy** | `:4873` | a treasury duty word (`DUTY_SERVICE_KINDS` admits `levy`, `institutionTable.js:106`) on a migration read with no holder |
| **the fence / the outlaw shelter / the street gang** | `Local fence`, `Fence (word of mouth)`, `Outlaw shelter`, `Street gang` rows; `hasCrimOrg = hasInst('fence','smuggl','gang','outlaw')` (`defenseGenerator.js:154`) feeds the community internal base | criminal bodies with a score effect; no pool names them |
| **the whisper market / the black market** | market-class rows (O-ADD-2); `blackMarketCapture` is a watch-kind field (`holderTable.js:210`) | a body inside the market class AND the object of a watch-kind record |
| **the households / the shopkeepers / the complainant / the founders / the neighbouring settlements / the realm** | `:5305`, `:5192`, `:5189`, `:5950`, `:4931`, `:4933` | collective or person agents on reads with no person field; `{founder}` is a `phrase` (`:207`) |
| **the successor / the citizens / the workers / the bakers** | `hookEscalation.js:416`, `:420`, `:394`, `:393` | persons as actors in engine-rendered stages beside DS-HK-1 |
| **the Art** | `:5226-5227` | the magic house's practice as a body word (`hasMagicInst`), on a tension read |

---

## 9. THE REFUTER'S SUMMARY FOR THE CHAIR

1. **The survey's measurements HOLD.** Every census figure, every word count, every annex quote, every `holderTable.js` row and every `priorityHelpers.js` boolean re-derived exactly; one line-drift (`narrativeGenerator.js:633`, not `:632`).
2. **Three of its engine READINGS are refuted or narrowed:** (a) the INTERESTED flip is seven treasury rows, never the three OFFICE rows, and only with a materialised treasury holder (`holderTable.js:724-756`); (b) "the market body" includes the Black, Whisper, Slave and Fish markets (`generalStateProse.js:1109`); (c) "the guard" and "the barracks" — the guard has no shipped row and is a faction-name word in the capture ladder; the barracks is a full garrison body, not an RE-only word.
3. **The PERSON row of the law is narrower than drafted, by the engine's own code:** the holder-ROLE column is null, but `corruption.js:685-688` grounds an organ's covert standing in a homed NPC, and `institutionTable.js:377-396` reads office words off `npcs[].role/.title`. The law should say "a PERSON is never a REFERENT of a face" (product scope), not "the engine has no person-to-institution edge".
4. **The watch is THREE things on this desk's page, not two:** the body (`hasWatch`), the order organ (the kind), and the Watchtower (a `watch` for `hasMilitaryInst` and `SECURITY_INSTITUTION_RE` only). Rule 5's register row should carry all three.
5. **Layer words are read as POWER names by the engine** (`guard`, `military`, `thiev`, `temple`, `clergy`, `order` inside faction names): the law's "the power is named only through the typed slot" is right for the FACE, but the slot's VALUE can itself be a body word, so a face rendering `{faction}` may print "the Temple Guard" on a body read — rule 3's same-word-same-referent needs a clause for a proper slot whose value contains a class word.
6. **The strongest lever the survey found (§8.3, the rolls) gets one more piece of evidence for (b):** the engine's own `Town council :: Record of custom` desc says *"Keeps the charter, rolls, and civic archive"* — the engine uses "rolls" metonymically inside a non-census body. The chair's ruling still settles 26 rows at once; this refuter's default (uncertain → do not license) is (a) for any read whose holder does not resolve, with the `[ledger]` sentence re-seated on the READ'S OWN NOUN (the count, the trend, the band), never on a register.
