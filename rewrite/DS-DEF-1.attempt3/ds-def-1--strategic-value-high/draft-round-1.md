Seat: Opus 5 — Fable-unvalidated. Block `DS-DEF-1` · pool key `strategic value HIGH` · draft round 1.
The rows below are the COMPLETE replacement for the pool's three variant rows, in order, under the pool's
existing bold heading. The heading and the typed lines of the pool are untouched and are not repeated here.

**strategic value HIGH**
1. `[ledger]` The value of what {settlement} holds is high.
   - `[face]` A prize is what {settlement} comes to.
   - `[face]` Possession of {settlement} would be worth the having.
   - `[face]` The gain in taking {settlement} would be large.
2. `[street]` What the town sits on is worth taking.
   - `[face]` The town holds a prize.
   - `[face]` A thing of value is what the place keeps.
   - `[face]` The place would be worth the taking.
3. `[visitor]` A stranger comes into {settlement} and comes into a place worth taking.
   - `[face]` What a newcomer walks into at {settlement} is a place of value.
   - `[face]` A traveller at {settlement} stands inside something worth having.
   - `[face]` Arrival at {settlement} is arrival at a prize.

--- NOTES

**THE CARD THIS POOL IS WRITTEN UNDER** (printed by `node scripts/prose-licence-card.mjs DS-DEF-1 'strategic value HIGH'` in `laneRW-DEF1`)
- role `spine` · form `sentence` · move `(none declared)` · relation `(a spine takes no relation)` · attach `(empty)` · covert `no` · audience `player (no mark)`.
- reads `text(terrain)` (via `TERRAIN_PRIZE_OF` in `defenseStateProse.js`); predicate `text(terrain) === Coastal`.
- **may claim: that the reader `text(terrain)` selects the row `Coastal` of `TERRAIN_PRIZE_OF`, as a STANDING fact of the record.** Call this **[MC]**. The row's own reading is the pool key's band words `strategic value HIGH`, so the one claim triple this pool may assert is `(PRESENT, text(terrain), a site of HIGH strategic value)` — in English, *this place is worth taking*. Every one of the twelve wordings asserts that triple and nothing else.
- **may NOT: a count, a cause, a season, a future, a standpoint, a second fact.** Call this **[MN]**.
- bag `{band: RESERVED, counterpart: proper, route: proper, settlement: proper}`, FILLED at this block's call sites `{settlement}`. Call this **[BAG]**.
- angle set `ledger street visitor`. Call this **[ANGLE]**.
- source `(none)` · standing SOURCE-UNRESOLVED — **no citation is licensed** (arm A13). Call this **[SRC]**. No wording names a holder, a roll, a book or a keeper.
- REFUSED COLUMNS, always: a totality over persons; an exemption from a duty; a named character and that character's fate; a theological claim. None is approached.

**THE ONE READING THIS PACKET DECLARES BEFORE THE FACES** — `worth taking` (and `a prize`, `a gain`, `possession`, `of value`) is the band words `strategic value HIGH` rendered in English. It asserts the STANDING value of the place. **No wording asserts a taker, an attempt, a threat, a pressure, a want held by anyone, or anything at all about who might come** — those are `DS-DEF-2`'s threat rows and the readiness lens's `[counterforce]` variants (arm A1). The subjunctive `would` in three wordings is A2's edge, never a future indicative (MOVE-GRAMMAR §1.3: FORECAST does not exist).

**PER FACE — WHICH CLAUSE LICENSES WHICH CLAIM**

Variant 1 `[ledger]` — the record's flat accounting. Grammar V1 (PRESENT).

| wording | words | claims it makes | licensed by |
|---|---|---|---|
| spine row | 8 | the value of what the town holds is high | [MC], the row's band reading stated at its plainest; `{settlement}` by [BAG]'s FILLED set; `[ledger]` by [ANGLE] |
| face 1 | 7 | the place is a prize | [MC], the same triple as a predicate nominal |
| face 2 | 8 | holding the place would be worth the holding | [MC]; the subjunctive is A2's edge, not a future ([MN]) |
| face 3 | 8 | the gain in taking the place would be large | [MC], the band read as magnitude in words (R-DA-16: never a digit, never a count); A2's edge |

Variant 2 `[street]` — the town's own plain speech about itself. Grammar V1 (PRESENT). **This variant carries NO slot**, because its parent row carries none and a face whose `{slot}` set differs from its parent's is refused (ARCH §2.5).

| wording | words | claims it makes | licensed by |
|---|---|---|---|
| spine row | 8 | what the town sits on is worth taking | [MC]; `sits on` is the old row's own verb, kept; `[street]` by [ANGLE] |
| face 1 | 5 | the town holds a prize | [MC] |
| face 2 | 9 | what the place keeps is a thing of value | [MC] |
| face 3 | 7 | the place would be worth the taking | [MC]; A2's edge |

Variant 3 `[visitor]` — the place as it stands to one arriving. Grammar V1 (PRESENT).

| wording | words | claims it makes | licensed by |
|---|---|---|---|
| spine row | 12 | the place a stranger comes into is worth taking | [MC] for the value; the arriving party is the ANGLE's frame under [ANGLE], not a claim — see refusal R1 |
| face 1 | 12 | the place a newcomer walks into is a place of value | [MC]; [ANGLE] as above |
| face 2 | 9 | the place a traveller stands in is worth having | [MC]; [ANGLE] as above |
| face 3 | 8 | arrival is arrival at a prize | [MC]; [ANGLE] as above |

**WHAT THE OLD SENTENCES CLAIMED AND THIS DRAFT DROPS** (dropping is the rewrite's purpose; the shipped breach is the corpus's known state; nothing is added)
- v1 old, *"and that is the whole of the defensive problem"*: a statement of what the fact MEANS for the town's defence. DROPPED under [MN] (a second fact), MOVE-GRAMMAR §1.3 (the MEANING non-move) and arm A1 (it restates the readiness lens's territory). With it goes the colon joint and the word `defensive`, which belongs to the readiness pools.
- v1 old, *"the town must be worth more to keep than it is to seize"*: a general proposition about what any prize town must be, in a normative modality. DROPPED under R-DA-12 (the generalisation test) and [MN] (a second fact).
- v2 old, *"The town is aware of what it sits on and aware that others are aware"*: a cognitive state of the town and of unnamed others. DROPPED under MOVE-GRAMMAR §1.3 (the FEELING non-move — no field carries motive, belief or mood), [MN] (a standpoint) and the REFUSED COLUMNS (the second limb is a claim over unnamed persons). The licensed residue — *what the town sits on has value* — is what variant 2 now says.
- v2 old, *"and the awareness runs underneath a great deal of what it decides"*: a CAUSE, and an abstraction moving under another. DROPPED under [MN] (a cause), the block's own PROVENANCE FENCE (terrain and strategic value never license a causal clause) and R-DA-11 (the figure policy).
- v3 old, *"understands within a day"* and *"understands shortly afterwards"*: a duration, twice, and a stranger's cognition. DROPPED under [MN] (a count; a standpoint) — no field of this block holds a span.
- v3 old, *"why the town is so careful about who it offends"*: a standing claim about the town's conduct toward outsiders, and a second fact caused by the first. DROPPED under [MN] (a cause, a second fact) and ruling 5 (no typed field holds it; `{counterpart}` is not filled at this block's call sites).
- Added: nothing. No holder ([SRC]), no count, no cause, no season, no future, no second fact, no sibling-band contrast (the old rows carried none, and R-DA-02 licenses a contrast, it does not require one).

**FORM DECLARATIONS**
- No em dash, no exclamation mark, no question mark, no digit, no percent, no `which`-clause, no colon, no semicolon, no first or second person, no citation, in any of the twelve wordings.
- One bracketed tag on each numbered row, the variant's own — `[ledger]`, `[street]`, `[visitor]`, exactly as they stand. **No `[plain]` marker anywhere**: that marker belongs to modifier rows and the projector refuses it on a spine row.
- Face rows are written in the shipped annex form the projector reads (three-space indent, `- ` bullet, backticked `[face]` tag; `FACE_ROW_RE` at `scripts/lib/dossier-annex-grammar.mjs:107`).
- **No wording opens on `{settlement}`**: a sentence-form row opening on a `proper`-typed slot of the bag is refused (ARCH §2.5, T-F8; the seam contract). Old variant 1 opened on the slot and its opener is moved. This is a form change, not a claim change, and it takes the pool's settlement-openers from one to zero (wall 10 satisfied at zero).
- Slot sets: variants 1 and 3 carry exactly one `{settlement}` in every one of their four wordings; variant 2 carries none in any of its four, matching its parent. No wording uses `{band}`, `{route}` or `{counterpart}` — `band` is RESERVED and the other two are unfilled at this block's call sites.
- Twelve distinct two-word openers, so no two wordings of the pool share an opener under any draw: `The value` · `A prize` · `Possession of` · `The gain` · `What the` · `The town` · `A thing` · `The place` · `A stranger` · `What a` · `A traveller` · `Arrival at`.
- Zero pronoun closers (R-DA-04's measured metric). The close KINDS vary across the pool: a band condition (`high`), a valuation (`comes to`), a possession (`the having`), a magnitude (`large`), a state (`worth taking`), an act of keeping (`keeps`), an object (`a prize`, `prize`), a value (`of value`), a holding (`having`).
- Rhythms inside each variant are four, not four costumes: variant 1 runs a possessive subject, a fronted predicate nominal, a nominalised subject with the subjunctive, and a nominalised subject with the subjunctive on a magnitude; variant 2 runs a cleft subject clause, a plain short SVO, a fronted complement, and the subjunctive; variant 3 runs a doubled-verb sentence, a cleft subject clause, a locative predicate, and a repeated-noun copula.
- THE THREAD (§1.4.1): every wording is ONE sentence, so no wording can drop a thread inside itself; the one two-clause wording (variant 3's spine row) carries its verb and subject forward across the joint. The pool is a SPINE and opens its passage, so each wording is written to read as a passage's first sentence and to close on a thing a modifier seated after it by salience can pick up — the value, the prize, the taking, the having.
- No connective from `§7b THE STATE CONNECTIVES` appears inside a wording (the seam contract, T-F1); the one joint used is a plain `and`.
- SIBLING DISTANCE inside `DS-DEF-1` (arms A1 and A11). This block mounts three lenses at one header position, and on a Mountain town the `terrain FAVOURABLE to the defender` pool fires beside this one. So: **no wording here says the ground helps or hinders** (the terrain lens's fact) — the words `ground`, `site`, `terrain`, `approach`, `climb`, `slope`, `height`, `country` and `flat` appear nowhere; **no wording says what the town can field or has built** (the readiness lens's) — `defence`, `defensive`, `walls`, `watch`, `muster`, `arrangements` and `covered` appear nowhere; **no wording says anyone is or is not pressing the town** (the readiness `[counterforce]` variants'); and no wording restates the `strategic value LOW` band or contradicts it.

**MEASURES** (reported, not asserted as passing; §16 reads every soft size as a band with its distance)
- Wordings 12 · variants 3 · faces 9 · slot-bearing wordings 8 of 12 · settlement-openers 0 of 12.
- Word counts: variant 1 — 8, 7, 8, 8; variant 2 — 8, 5, 9, 7; variant 3 — 12, 12, 9, 8. Mean 8.42, range 5 to 12, within-pool sd 1.89.
- Lexical spread of the one claim across the twelve: `worth` 5 · `taking` 4 · `prize` 3 · `value` 3 · `having` 2 · `gain` 1 · `possession` 1.

**REFUSALS** (a refusal is a result)
1. **R1 · THE CARD CONTRADICTS ITSELF ON THE STANDPOINT, AND VARIANT 3 CANNOT CLEAR IT.** [ANGLE] names `visitor` (and `street`); [MN] bars *a standpoint*. Variant 3's tag is not mine to touch, and a `[visitor]` wording must position somebody arriving. This draft cures every cognitive limb — no wording says a stranger sees, notices, understands, learns or concludes anything, and the duration is gone — so what remains is a vantage and not a mental state. **If the sitting reads a bare vantage as a standpoint under [MN], variant 3 cannot be made lawful in any wording**, because the only cure is dropping its angle, which the rewrite rules forbid. Written under [ANGLE], flagged here. This is the chair row the attempt-1 judgment already opened (its §8 row 4); one ruling settles it for `terrain EXPOSED`'s `[street]` variant as well.
2. **R2 · THE CARD'S PRINTED PREDICATE IS NARROWER THAN THE CODE.** The card prints `=== Coastal`. `TERRAIN_PRIZE_OF` (`src/domain/display/stateProse/defenseStateProse.js:719-723`) maps **Coastal AND Mountain** to `strategic value HIGH`. A wording naming the sea, the harbour, the anchorage or the sea road would be licensed on a Coastal town and FALSE on a Mountain one, and a wording naming the pass or the height would be false on a Coastal one. **No wording in this packet names any terrain feature, and none may until the card is re-cut.** A card fault for the chair, not a writer's cure; it also removes the sharpest concrete nouns this pool could otherwise reach, which is why the twelve wordings run abstract.
3. **R3 · THE DISTINCT-GRAMMAR LAW CANNOT BE MET FROM THE FIELDS.** MOVE-GRAMMAR §2.1: a pool of k variants carries min(k, 8) DISTINCT level-1 grammars. Only **V1 (PRESENT)** is drawable here — the card declares no structural-consequence field (no V2), no `none-exists` field (no V3), no named-object field (no V4), no institution row (no V5), no unresolved state field (no V6), no event provenance (no V7) and no `not-held` provenance field (no V8). A member whose licensing field is null is not drawn and is not written empty (§1.1). All three variants are therefore PRESENT, told apart by angle, vocabulary and rhythm. **The law that cannot be met is unmeetable from the block's fields, not from the writing**; no wording is refused on its account.
4. **R4 · R-DA-05's WITHIN-POOL LENGTH SPREAD IS NOT MET, AND PADDING WOULD BREAK A WALL.** The direction is sd ≥ 4.0 within a pool; this pool measures 1.89. With exactly one licensed claim, every wording is one sentence, and the only ways to lengthen are a second fact ([MN]), a gloss (the MEANING non-move) or a summarising second sentence (§16(6)'s machine signature). The distance is reported and left, per §16's channel rule: a soft rule reports its distance and fails only past the depth, the budget or the ceiling.
5. **Carried for the refuters, not refused.** `worth taking` presupposes that value is value TO a would-be holder; that is what the band words mean, and the shipped rows said it the same way. If the sitting reads the phrase itself as importing an unlicensed party, the whole pool loses its only English rendering of `HIGH` and the band must be re-worded from the card, not from the pool. Named here so it is ruled and not re-found.
