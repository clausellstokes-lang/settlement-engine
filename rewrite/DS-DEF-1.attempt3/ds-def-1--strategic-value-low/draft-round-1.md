# DS-DEF-1 · `strategic value LOW` · REWRITE draft round 1
Seat: Opus 5 (Fable-unvalidated) · block `DS-DEF-1` · role `spine` · 3 variants in, 3 variants out · 12 wordings (3 numbered lines + 9 `[face]` rows).

Paste-ready: the three numbered rows below are the COMPLETE replacement for the pool's three variant rows under the bold pool line **strategic value LOW**. Same vids, same order, same angle tags; none added, removed, merged or reordered. This pool declares no typed lines (ROLE, READS, RELATION, ATTACH, FORM, MOVE) in the annex today; none is touched and none is repeated here.

1. `[counterforce]` Nothing at {settlement} would answer as plunder.
   - `[face]` Spoil is not what {settlement} holds.
   - `[face]` A seizure at {settlement} would carry off nothing of worth.
   - `[face]` What could be taken from {settlement} would not be much.
2. `[ledger]` The strategic worth of {settlement} stands low.
   - `[face]` As a prize {settlement} counts for little.
   - `[face]` Small return would come of taking {settlement}.
   - `[face]` No part of {settlement} would be worth a taking.
3. `[street]` The town keeps nothing that would be worth taking.
   - `[face]` What is in the town is not worth coming after.
   - `[face]` Nothing worth having is to be found in the town.
   - `[face]` Small gain would come of a taking here.

--- NOTES

**THE CARD, AS PRINTED** (`node scripts/prose-licence-card.mjs DS-DEF-1 'strategic value LOW'`). Block `DS-DEF-1` · role `spine` · key `strategic value LOW`.
`reads: text(terrain) (via TERRAIN_PRIZE_OF in defenseStateProse.js)` · `predicate: text(terrain) === Forest` ·
`bag: {band: RESERVED, counterpart: proper, route: proper, settlement: proper}`, FILLED at this block's call sites `{settlement}` ·
`relation: (a spine takes no relation)` · `seat/form: (not a seat-taker) / sentence` · `move: (none declared)` · `angle: counterforce ledger street` ·
`attach: (empty: a spine takes no attach set)` · `echo: spine mounts 1 (tabs: defense) · modifier mounts 0` · `covert: no` ·
`source: (none) · standing SOURCE-UNRESOLVED` — no citation is licensed; a face naming a record holder is refused by arm A13 ·
`may claim: that the reader text(terrain) selects the row Forest of TERRAIN_PRIZE_OF, as a STANDING fact of the record` ·
`may NOT: a count, a cause, a season, a future, a standpoint, a second fact` · `audience: player (no mark)` ·
`REFUSED COLUMNS, always: a totality over persons; an exemption from a duty; a named character and that character's fate; a theological claim.`

The card licenses exactly ONE standing fact, and the pool key states it in the record's own terms: **this settlement's strategic-value reading stands LOW — the site is not worth taking.** All twelve wordings assert that one fact and nothing besides. Every wording binds its worth to the PRIZE sense in its own vocabulary (*plunder*, *spoil*, *seizure*, *taken*, *a prize*, *return*, *a taking*, *coming after*, *worth having*, *gain*) rather than leaving a bare statement of worth standing, because an unbounded "worth nothing" would read as a claim about the town's wealth, which is the economy slice's field and not this card's (sibling coherence in structural fact, CLERK-LAWS C5).

**PER FACE — THE CLAUSE THAT LICENSES EACH CLAIM.** Each of the twelve wordings makes exactly one claim. The licensing clause in every case is the card's `may claim` line (the pool's predicate as a STANDING fact of the record); the slot set in every case is the card's `bag`, FILLED `{settlement}`. The grammar is V1 (PRESENT) throughout, licensed by the state key alone (MOVE-GRAMMAR §2.1).

*Variant 1 `[counterforce]` — the cap put as a refusal, in the vocabulary of what a force would carry away. Slot set `{settlement}` in all four, matching the shipped row.*

| wording | the claim | licensed by | notes on the wording |
|---|---|---|---|
| `1` Nothing at {settlement} would answer as plunder. | the strategic-value reading stands LOW | `may claim` (PRESENT, V1); slot → `bag` (FILLED); angle `[counterforce]` → `angle`; form sentence → `seat/form` | no source named → `source: (none)`, arm A13. *would* is the subjunctive edge (A2, R-DA-07), never a future indicative |
| `[face]` Spoil is not what {settlement} holds. | same single claim | `may claim` | the short line of the variant; the copula kept, the expletive struck (R-DA-07). *Spoil* is the prize sense in one word and names no particular good, so nothing is invented to make the line land (fault 24; MOVE-GRAMMAR §1.2 row 4) |
| `[face]` A seizure at {settlement} would carry off nothing of worth. | same single claim | `may claim` | subjunctive throughout → A2. The sentence is about the GAIN, never about whether an attempt would succeed: no claim is made about the walls, the watch or the ground, which are `readiness`'s and `terrain`'s fields, not this card's. The passive names no agent, so no actor enters that no field holds |
| `[face]` What could be taken from {settlement} would not be much. | same single claim | `may claim` | a magnitude stated as a word, never a digit (R-DA-16, A4); *not be much* is a rating on what could be taken, licensed by `resourceAnalysis.strategicValue` being a typed rating field (CL-7), and attached to the prize, never to the place |

*Variant 2 `[ledger]` — the flat valuation in the clerk's diction. Slot set `{settlement}` in all four, matching the shipped row.*

| wording | the claim | licensed by | notes on the wording |
|---|---|---|---|
| `2` The strategic worth of {settlement} stands low. | the strategic-value reading stands LOW | `may claim`; slot → `bag` | the band word carried in the record's own diction; the verb is the copula's plain relative (*stands*), predicative and never causal (`may NOT: a cause`) |
| `[face]` As a prize {settlement} counts for little. | same single claim | `may claim` | the fronted frame bounds the worth to the prize sense and is not a second fact |
| `[face]` Small return would come of taking {settlement}. | same single claim | `may claim` | subjunctive → A2; *return* is the ledger's word for what a thing yields, a rating on the prize and not a count (`may NOT: a count`) |
| `[face]` No part of {settlement} would be worth a taking. | same single claim | `may claim` | a distributive negation over the site, never over PERSONS — the refused totality is a totality over persons, which no wording here makes |

*Variant 3 `[street]` — the town's own idiom for the same fact, with no talk, no knowledge and no feeling asserted. Slot set EMPTY in all four, matching the shipped row.*

| wording | the claim | licensed by | notes on the wording |
|---|---|---|---|
| `3` The town keeps nothing that would be worth taking. | the strategic-value reading stands LOW | `may claim`; no slot, matching the shipped row's own slot set (T-F8 face rule: a face's `{slot}` set equals its parent's) | plain ordinary verbs; nothing is attributed to the town as belief, knowledge or temper |
| `[face]` What is in the town is not worth coming after. | same single claim | `may claim` | *coming after* is an impersonal gerund: no person, class or totality is named (REFUSED COLUMNS) |
| `[face]` Nothing worth having is to be found in the town. | same single claim | `may claim` | the passive names no finder, so no PROVENANCE move fires against `source: (none)` and no holder is cited (arm A13) |
| `[face]` Small gain would come of a taking here. | same single claim | `may claim` | the compressed line; subjunctive → A2. Density law (§21.4): the compression is the ceiling, not a candidate for being made plainer |

**HOW THE `[street]` ANGLE IS REALISED.** The palette reads `[street]` as the town's own talk about its condition. The card refuses a standpoint and the belief-frame floor is zero (R-DA-13), so the angle is realised as DICTION — the plain ordinary nouns and verbs a town would use (*keeps*, *coming after*, *worth having*, *gain*) — and never as an attribution of talk, knowledge or feeling to the town. The tag is inherited unchanged.

**WHAT THE REWRITE DROPPED, AND UNDER WHICH REFUSAL.** Dropping the unlicensed claim is the rewrite's purpose; the shipped breach is the corpus's known state. Nothing was added to any variant.
- v1 shipped *"worth an army's season"*: the DURATION is dropped (`may NOT: a season`) and the ACTOR with it (no field of this block holds an army); the licensed core — not worth taking — is kept.
- v1 shipped *"and the town's best defense is that plain fact rather than anything on its walls"*: dropped entire. It is a second fact (`may NOT: a second fact`), a superlative no field holds, a claim about the walls (which is `readiness`'s field and `DS-DEF-2`'s, not this card's), and a MEANING move, which exists nowhere in the estate (MOVE-GRAMMAR §1.3).
- v2 shipped *"{settlement} is not a prize"*: KEPT as the variant's claim, rewritten in the ledger's diction.
- v2 shipped *"What it can field matters less here than what it does not have"*: dropped — a second fact about the town's fielded force, and a comparison joining two fields this card does not join (R-DA-11).
- v2 shipped *"that anyone would come for"*: dropped — a totality over persons (REFUSED COLUMNS, always).
- v3 shipped *"The town knows it is not worth taking"*: the KNOWING is dropped and the FACT kept — a belief frame over persons (`may NOT: a standpoint`; R-DA-13's belief-frame floor of zero).
- v3 shipped *"and has made a kind of peace with the knowledge"*: dropped — a FEELING, a move that does not exist anywhere in the estate (MOVE-GRAMMAR §1.3).
- v3 shipped *", which is not the same as being comfortable with it"*: dropped twice over — a `, which` tail (wall 6; R-DA-03) and a second evaluative claim about the town's temper.

**CLAIM-EQUALITY ACROSS THE FACES (arm A6, read across the faces and not back to the old sentence).** All four wordings of variant 1 assert the same single typed claim; likewise variants 2 and 3; and all twelve assert the same one claim as each other, because the card licenses one. No face carries a claim a sibling face lacks, so the unweighted seeded roll at render cannot move what the reader is told — only how it is said.

**THE THREAD (§1.4.1).** This pool is a SPINE and stands first in every composed unit, so it hands the thread forward rather than picking one up. Each wording leaves a following modifier a noun to carry: the settlement itself in the first eight, the town in the last four, and in every case the thing it would be worth (*plunder*, *spoil*, *a prize*, *a taking*, *gain*). No wording closes on a pronoun (R-DA-04's `closers.pronounRate`). The kinds of close vary: an object close (*as plunder*, *a taking*, *coming after*), a condition close (*holds*, *stands low*, *counts for little*, *would not be much*), an absence close (*nothing of worth*), a name close (*taking {settlement}*), and a place close (*in the town*, *here*).

**SIBLING DISTANCE (arms A1 and A11).** The block's sibling spines are `readiness STRONG/ADEQUATE/WEAK/CRITICAL`, `terrain FAVOURABLE to the defender`, `terrain EXPOSED` and `strategic value HIGH`. No wording here restates or contradicts one: nothing is said about the walls, the watch, the works, the arrangements, the approach, the ground, or the town's capacity to hold. Vocabularies deliberately held out, each because a sibling pool of this block owns it: *thin*, *slender*, *slight* (`readiness WEAK`); *cover*, *spare*, *ordinary*, *sufficient* (`readiness ADEQUATE`); *the lowest mark*, *the scale*, *undefended* (`readiness CRITICAL`); *price* (the shipped `readiness STRONG` line, "not worth the price of trying"); *climb*, *slope*, *ground*, *approach*, *uphill*, *site* (`terrain FAVOURABLE`, `terrain EXPOSED`); *beneath notice* (`DS-DEF-2`'s neither-walls-nor-force row); *repay* (the `strategic value HIGH` family's "repay"). **The live composition risk, named:** `TERRAIN_PRIZE_OF` routes `Forest` to BOTH this key and `terrain FAVOURABLE to the defender`, so a forest town can carry both readings on one page — which is exactly why no wording here says the place would be easy to take, hard to take, or anything at all about the ground.

**OPENER DISPERSION (A11; R-DA-17).** The twelve first-two-word pairs are: *Nothing at · Spoil is · A seizure · What could · The strategic · As a · Small return · No part · The town · What is · Nothing worth · Small gain*. All twelve are distinct, and no two variants share their first two words. No row of this pool opens on the `{settlement}` token (see constraint 3 below), so the settlement-opener share for this pool is zero, inside R-DA-17's ceiling.

**WORD COUNTS (the `{settlement}` slot token counted as one word).**
- v1: `1` 7 · faces 6 · 10 · 10
- v2: `2` 7 · faces 7 · 7 · 9
- v3: `3` 9 · faces 10 · 10 · 8
- Spread six to ten across the pool; the short line exists (six words), and the rhythm follows the load rather than a metronome (R-DA-05).

**WALLS CHECKED ON ALL TWELVE.** No em dash · no exclamation · no question mark · no digit or percent · no `which`-clause · no "I" and no "you" · no citation and no named record holder (`source: (none)`, arm A13) · no future indicative, every edge subjunctive (A2) · no expletive opener, no "There is" and no "It is" (R-DA-07) · no figure, no sense verb on an abstraction, nothing inanimate acting with intent (R-DA-11) · no totality over persons · no named character, no fate, no deity · no second fact and no summarising second sentence (R-DA-03; MOVE-GRAMMAR §1.3's MEANING) · one bracketed angle tag on each numbered row and no `[plain]` marker anywhere · every face's slot set equal to its parent's · no participial or gerund form at word 1 or 2 of any wording, and the one at word 3 (*Nothing worth having*) is of the shape the detector was measured not to reach.

**REFUSALS.** **None.** All three variants are lawful under the card; no variant is banked, none is trimmed, and the variant count and face count stand at 3 and 12.

**FOUR CONSTRAINTS RECORDED FOR THE CHAIR (limits the card imposes; not refusals).**
1. **This pool can realise only level-1 grammar V1 (PRESENT).** V2 through V8 each need a second licensing field — a structural-consequence field, a `none-exists` field, a named object, an institution row, an unresolved state field, event provenance, a `not-held` field. This card holds one read and declares no move, so MOVE-GRAMMAR §3.2's licensing filter removes every other member, and writing one would mean writing a member the block cannot license, which §3.2 forbids outright. §2.1's "min(k, |set|) distinct grammars per pool" is therefore unreachable here. Recorded, not cured — and it is the pool family's, not this pool's: the same constraint holds for `strategic value HIGH`.
2. **No wording carries two sentences.** With `may NOT: a second fact` and no declared MOVE, the pool has one fact to state, so A1's two-sentence reach is unavailable to it by construction. The variety is vocabulary and rhythm across a six-to-ten-word spread, never sentence count.
3. **No row of this pool opens on the `{settlement}` token; the shipped variant 2 did** ("{settlement} is not a prize"). T-F8 refuses a sentence face opening on a `proper`-typed slot of the block's bag, and ARCH §2.5's seam contract states the same detection rule for any sentence-form row. This is a deliberate change to the pool's opener profile and is flagged rather than assumed; if the chair reads T-F8 as binding faces only, variant 2's numbered line may be re-fronted on the slot without touching its claim.
4. **The card's `may claim` line is machine-shaped**, naming the read and the row (`text(terrain)` selects `Forest` of `TERRAIN_PRIZE_OF`) rather than a claimable object. It is read here as the pool's own predicate — the strategic-value reading stands LOW, the site not being worth taking — which is complete for this pool, since `TERRAIN_PRIZE_OF` routes `Forest` to this key. If the chair reads the line otherwise, all twelve wordings move together, because all twelve assert that one fact.
