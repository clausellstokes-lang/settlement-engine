# DS-DEF-1 · `terrain EXPOSED` · REWRITE draft round 2
Seat: Opus 5 (Fable-unvalidated) · block DS-DEF-1 · role `spine` · 3 variants in, 3 variants out, 12 faces.
Paste-ready: the three numbered rows below replace the pool's three variant rows under the bold pool line. The typed lines (ROLE, READS, RELATION, ATTACH, FORM, MOVE) are untouched and not repeated here.
Round 2 is a TARGETED round: it moves the three failing owned measures the gate named and changes nothing else. Two faces move; ten units are byte-identical to round 1, so every measure delta is attributable to the two edits.

1. `[visitor]` Open ground reaches {settlement} from every side.
   - `[face]` The way in to {settlement} crosses ground unbroken by ridge or defile.
   - `[face]` From any quarter, the approach to {settlement} runs over open country.
   - `[face]` The country about {settlement} carries no rise and no hollow.
2. `[ledger]` The site of {settlement} holds no advantage of ground.
   - `[face]` Under terrain, {settlement} takes no strength from the land.
   - `[face]` The land at {settlement} lies open from end to end.
   - `[face]` Nothing in the site of {settlement} rises or narrows.
3. `[street]` No hill and no narrows lie anywhere about {settlement}.
   - `[face]` The town stands on open ground at {settlement}.
   - `[face]` The ground at {settlement} has neither bank nor fold.
   - `[face]` What the ground gives {settlement} is open country and no more.

--- NOTES

## A. THE GATE'S THREE FAILING MEASURES, ANSWERED ONE BY ONE

**Measure 1 — `shapes.participialOpenerRate` (over) = 28.24 band-widths on 1 of 12 faces; band DEPTH at most 1.75 band-widths on every face.**
The single offender was variant 2's first face, round 1: *Standing on open ground, {settlement} takes no strength from the land.* — the packet's own §E flagged it as the one participial opener in the twelve, and the gate measured it. **MOVED.** The face now reads *Under terrain, {settlement} takes no strength from the land.* The nonfinite opening clause is gone and is replaced by a fronted prepositional phrase naming the record's own head, which is the office's formula and not a participle. The predicate after the comma is byte-identical to round 1, so the claim, the vocabulary and the density are unchanged and the only thing that moved is the opener's shape. **Expected new value: 0 of 12 faces carry a participial opener** — no other unit in the pool opens on a nonfinite clause (checked one by one: `Open ground`, `The way`, `From any`, `The country`, `The site`, `Under terrain`, `The land`, `Nothing in`, `No hill`, `The town`, `The ground`, `What the`).

**Measure 2 — composed walk · Ambiguity · a reading that depends on the spine = 1 owned finding, value "without cover", site: joint.**
The offender was variant 1's first face, round 1: *The way in to {settlement} crosses ground without cover or defile.* Read at the joint, *cover* carries two readings — concealment of the ground, and the military sense of what a defender is covered by — and only the spine disambiguates it. That is exactly the finding's class. **MOVED.** The face now reads *The way in to {settlement} crosses ground unbroken by ridge or defile.* The bare noun `cover` is struck and replaced by `ridge`, a terrain feature with one reading; `unbroken by` states the same absence without a polysemous head. The subject, the verb and `defile` are byte-identical to round 1, so the density law is honoured: nothing lawful was made plainer, and the one word that carried the ambiguity was the one word replaced. **Expected new value: 0 owned Ambiguity findings.**

**Measure 3 — composed walk · owned unit verdicts = FAIL 0 · WITHHELD 1 · PASS 11; band FAIL 0 and WITHHELD 0 on the writers' own grain.**
The single WITHHELD unit is the same unit measure 2 names — the finding carried no locatable evidence and was read at the joint, so it counted against the pool as a withhold rather than a fail. Curing measure 2 at its word cures this verdict with it. **Expected new value: FAIL 0 · WITHHELD 0 · PASS 12.** No other unit changed, so no unit can newly fall.

**Nothing else moved, deliberately.** §21.3 gives one effort per round and §21.4 forbids trading density for plainness with no law behind the change; ten of the twelve units passed every owned measure in round 1, so re-writing them would spend the round's evidence and risk a new failure with no failing measure to answer. This round is therefore not dry — it moves all three failing owned measures — and it is attributable at the byte.

## B. The card, as this packet reads it (unchanged from round 1; restated so this file stands alone)
The card licenses ONE claim: *that the reader `text(terrain)` selects the row `Plains` of `TERRAIN_DEFENCE_OF` in `defenseStateProse.js`, as a STANDING fact of the record* (card line **may claim**, under **reads** and **predicate**). The row's own name is `terrain EXPOSED` and the map's contrast is `terrain FAVOURABLE to the defender`, so the licensed fact is that the site's ground carries no defensive advantage — nothing more and nothing less.

The card refuses a count, a cause, a season, a future, a standpoint and a second fact (**may NOT**); a totality over persons, an exemption from a duty, a named character's fate and a theological claim (**REFUSED COLUMNS**); and every citation, the source standing SOURCE-UNRESOLVED, so arm A13 refuses any face that names a record holder (**source**). The bag is FILLED at this block's call sites with `{settlement}` alone, so `{band}`, `{route}` and `{counterpart}` appear in no face. **seat/form** is `sentence`, so every face is a full sentence and none is a fragment. **relation** is empty because a spine takes no relation, and **attach** is empty for the same reason.

**One reading the packet takes deliberately, carried from round 1.** The card prints `predicate: … === Plains`, but `TERRAIN_DEFENCE_OF` routes both `Plains` and `Desert/Arid` to this pool. No face therefore claims anything true of one of those two and false of the other: no *level*, no *flat*, no *grass*, no *field*, no *dust*, and no claim that the ground stands at one height throughout. Every face states only that the ground carries no defensive feature, which is what both rows hold.

## C. Licence, face by face — which card clause licenses each claim
Every unit asserts exactly one claim, the terrain row, and nothing else; the four faces of a variant are claim-equal to each other, which is what arm A6 reads. The feature nouns (*ridge*, *defile*, *rise*, *hollow*, *hill*, *narrows*, *bank*, *fold*) are the realisation vocabulary of the row itself — the same vocabulary the shipped pool already spent on *hill* and *narrows* — and each names a feature the site does NOT hold, so none adds a particular to the record (R-DA-10's fault-24 guard).

**Variant 1 `[visitor]`** — none of the four opens on the `{settlement}` slot (ARCH §2.5, T-F8), and each carries it.

| unit | claim | licensing clause |
|---|---|---|
| row | the ground about the site carries no defensive feature | **may claim** (the row `EXPOSED` is selected), read with **reads** / **predicate**; `{settlement}` by **bag** (FILLED) |
| f1 (NEW) | the same claim, worded as the way in and as two named absent features | **may claim**, with the arrival vantage licensed by the card's **angle** line (`ledger street visitor`); `{settlement}` by **bag**; `ridge` and `defile` are absences of the selected row, licensed by the row itself, never by a geography field the block does not hold |
| f2 | the same claim, worded as the approach from any quarter | **may claim** + **angle**; `{settlement}` by **bag** |
| f3 | the same claim, worded as the surrounding country | **may claim**; `{settlement}` by **bag** |

**Variant 2 `[ledger]`** — none opens on the slot; each carries it.

| unit | claim | licensing clause |
|---|---|---|
| row | the site holds no advantage of ground | **may claim**; `{settlement}` by **bag** |
| f1 (NEW) | the same claim, entered under the record's own head | **may claim**; `{settlement}` by **bag**; the fronted `Under terrain` names the READER (`text(terrain)` via `TERRAIN_DEFENCE_OF`), which is the card's **reads** line and is therefore the office's own formula, not a second fact — the register card's "Repeat the office's formula because it is the office's; the WORD may recur, the FACT must not" |
| f2 | the same claim, worded as the openness of the land | **may claim**; `{settlement}` by **bag** |
| f3 | the same claim, compressed to the two things the site does not do | **may claim**; `{settlement}` by **bag** |

**Variant 3 `[street]`** — unchanged from round 1; none opens on the slot; each carries it.

| unit | claim | licensing clause |
|---|---|---|
| row | no hill and no narrows about the site | **may claim** (the shipped sentence's licensed half, kept); `{settlement}` by **bag** |
| f1 | the same claim, worded as where the town stands | **may claim**; `{settlement}` by **bag** |
| f2 | the same claim, worded as two further absent features | **may claim**; `{settlement}` by **bag** |
| f3 | the same claim, worded as what the ground gives | **may claim**; `{settlement}` by **bag** |

**No face claims anything the card refuses.** No count (no numeral, no quantity word, no band word — note that `counts` was considered for variant 2's new face and REFUSED, both because it reads as a count word to the C1 arm and because the sibling pool `strategic value LOW` already spends it on worth: *As a prize {settlement} counts for little*). No cause: no face says why the site is as it is. No season, no future, no subjunctive standing in for one. No standpoint: no face reports what anyone thinks or would find, only what the ground holds; the `[visitor]` angle is a vantage on the same standing fact, never a belief attributed to a traveller. No second fact. No citation and no record holder named, the source standing SOURCE-UNRESOLVED (arm A13).

## D. What each old sentence claimed, and what was dropped (carried from round 1; unchanged, because round 2 dropped nothing further and added nothing)
Dropping is the rewrite's purpose; the shipped breach is the corpus's known state. Nothing was added in either round.

**Variant 1, shipped:** *"{settlement} sits open. There is no ground here that helps it, and everything the town has must be built rather than found."*
- KEPT: the terrain row, twice over (*sits open*; *no ground here that helps it*).
- DROPPED: *everything the town has must be built rather than found* — (a) a SECOND FACT, about the town's works rather than its ground (**may NOT**: a second fact); (b) a totality over the town's holdings, which this lens cannot assert because it reads the terrain record and nothing about any arm of the defence; (c) the antithesis shape *built rather than found*, whose rejected alternative names no sibling key or band (R-DA-02).
- Also corrected in form: the expletive opener *There is* (R-DA-07) and the row opening on the `{settlement}` slot (wall 10; R-DA-17; T-F8).

**Variant 2, shipped:** *"The site gives {settlement} nothing defensively. Every advantage the town holds is one it has paid for and must keep paying for."*
- KEPT: the terrain row, reworded as the advantage of ground.
- DROPPED: the whole second sentence, carrying (a) a totality over the town's advantages (**REFUSED COLUMNS**; R-DA-15's quantifier rule — no column of this card is `closed`); (b) a COST claim, which is the CONSEQUENCE move and is double-licensed by event provenance and a household row, neither of which this card holds (MOVE-GRAMMAR §1.2 row 7); (c) *must keep paying for*, a standing obligation reaching forward, a future in all but mood (**may NOT**: a future; A2; THE PROMISE).

**Variant 3, shipped:** *"The town has no hill and no narrows and does not pretend otherwise; what {settlement} holds it holds by standing on it."*
- KEPT: *no hill and no narrows*, the licensed half, now the row.
- DROPPED: (a) *does not pretend otherwise*, a belief frame and a STANDPOINT attributed to the town, and a totality over its people (**may NOT**: a standpoint; **REFUSED COLUMNS**: a totality over persons; R-DA-13's belief-frame floor); (b) *what {settlement} holds it holds by standing on it*, a SECOND FACT about the town's holdings and a near-gnomic closer (**may NOT**: a second fact; R-DA-12).
- Also corrected in form: the pronoun closer *on it* (R-DA-04) and the three-limb chain joined by *and … and*.

## E. Sibling distance (arms A1 and A11), re-run on the two new faces
The block's sibling pools are the four `readiness` bands, `terrain FAVOURABLE to the defender`, and `strategic value HIGH` / `LOW`.
- Against **readiness**: no face names the works, the walls, the watch, the muster or the margin, and none says the town is defended or undefended. The word *defense* and its cognates appear in no face of this pool, so nothing here can be read as a claim about any arm of the five. The new variant 2 face was checked against `readiness WEAK`'s *Set down under readiness, {settlement} is entered weak* — the shared element is the office's under-the-head formula, which the register card licenses to recur; the FACTS are different fields and neither restates nor contradicts the other. The formula-echo is recorded here so the refuter reads it as a licensed recurrence and not as a template.
- Against **terrain FAVOURABLE**: that pool's wordings run *the climb counts for the town*, *carries part of the defense*, *is in the lie of the land*, *Ground of this kind is worth more*, *the height is the town's to keep*. None of those constructions is mirrored here, and all five phrases are struck from this packet by name. The two rows are mutually exclusive readings of one field, so no town can meet both and no contradiction is possible.
- Against **strategic value HIGH / LOW**: no face names *worth*, *a prize*, *plunder*, *spoil*, *a taking*, *gain* or *return*. This was the reason `counts for the town` was refused for variant 2's new face — `strategic value LOW` already carries *As a prize {settlement} counts for little*, and a spine that says *counts for* about the same town at a joint would import a worth reading into a terrain claim.
- The neighbouring packet for `readiness CRITICAL` reserved *open*, *the ground*, *the approach*, *hill* and *narrows* to this pool; this packet spends exactly that vocabulary and takes none of that packet's (*floor*, *band*, *stranger*, *undefended*).

## F. Form checks run over all twelve units
No em dash, no exclamation, no question mark, no digit or percent, no `which`-clause, no citation and no record holder named (**source**: none, standing SOURCE-UNRESOLVED — arm A13), no first or second person, no figure, no simile, no sense verb on an abstraction, no inanimate subject with an intent verb, no expletive opener, no evaluative adjective on the place, no triad, no band word (`strong`, `adequate`, `weak`, `critical`, `high`, `low`) anywhere. Spelling follows the shipped corpus (`defense` where the word occurs at all, which here is nowhere; `favourable` is the sibling key's spelling and the word is not used).

- **Openers.** All twelve first-two-word pairs are distinct: `Open ground` · `The way` · `From any` · `The country` · `The site` · `Under terrain` · `The land` · `Nothing in` · `No hill` · `The town` · `The ground` · `What the`. NO row and no face opens on the `{settlement}` slot, so wall 10 sits at zero and ARCH §2.5's T-F8 refusal cannot fire.
- **Participial openers: zero of twelve** (the round-1 offender is cured; see §A measure 1).
- **Closes** (R-DA-04, the kind varied): a condition of place (*from every side*), an absent feature (*defile*, *no rise and no hollow*, *bank nor fold*), a civic noun (*the land*, *open country*, *ground*), an object with its absence (*about {settlement}*), a pseudo-cleft on the object (*open country and no more*). No pronoun closer. One abstract-noun closer at most across the twelve, which holds the `abstractNounRate` guard at or under 0.083 on a twelve-unit grain.
- **Lengths**, in units: variant 1 — 7 · 12 · 11 · 10; variant 2 — 9 · 9 · 10 · 9; variant 3 — 9 · 8 · 9 · 11. Range 7 to 12, one unit under eight words, which meets R-DA-06's `< 8` floor of 0.030 at 1 of 12. The band is narrow and the reason is the card, not the pen: one licensed fact is one sentence's load, and rhythm follows load. The spread the pool can carry lives in construction and opening, and it is carried there — a plain predication, a prepositional frame, a fronted spatial adverbial, a fronted record-head adverbial, a negative subject, a pseudo-cleft.
- **Sentence count.** Every unit is one sentence. Two sentences would require a second licensed fact and this card declares none; see §H.

## G. THE THREAD
Each row is a spine and stands first in its passage, so each is written to hand a noun forward for whatever modifier the composer seats next: *ground* and *side* (variant 1), *land* and *ground* (variant 2), *hill*, *narrows* and *ground* (variant 3). Every face is one sentence carrying one claim, so no face changes subject mid-passage and none depends on a sentence before it. Where a face is read after a sibling modifier rather than first, its opening noun (*open ground*, *the way in*, *the country*, *the site*, *the land*, *the ground*, *the town*) is a concrete civic noun of the passage's own subject matter, so the thread is picked up rather than dropped. The new variant 2 face opens on the record's head and lands on *the land*, which is the strongest hand-forward in the variant — the noun a following modifier about the site can take up directly.

The two round-2 edits were checked against the thread specifically, because both touched the head or the tail of a face: variant 1's new face still closes on a terrain noun (*defile*), and variant 2's new face still closes on *the land*, so neither edit removed a noun the next sentence could carry.

## H. REFUSALS
- **No variant refusal.** All three variants are written lawful under the card, in round 2 as in round 1.
- **One POOL-level law this pool cannot meet, and it is not curable by wording (carried unchanged from round 1).** MOVE-GRAMMAR §2.1's level-1 rule that a pool of k variants carries min(k, 8) DISTINCT level-1 grammars, at least two in any pool of two or more. This card resolves one reading, declares no move, no relation and no attach set, and names no second field, so V2 (a structural-consequence field), V3 (`none-exists`), V4 (a named object), V5 (an institution row), V6 (an unresolved state value), V7 (event provenance) and V8 (a `not-held` field with provenance) are all unlicensed here. All three variants are V1 `PRESENT`, and the pool's spread lives in its angles, its openings and its rhythm. Recorded for the chair as a finding, not as a wording fault, and not moved by round 2 because no wording can mint a licensing field.
- **A consequence of the drops, declared so it is not re-found as a defect.** The shipped pool ran 2 sentences / 2 sentences / 1 sentence; every rewritten variant is ONE sentence. This is forced, not chosen: in all three variants the second sentence (or the second limb) was the only place the unlicensed claims sat, and once they come out the card licenses exactly one fact — so a second sentence could only restate the first, which is R-DA-03's summarising second sentence at a ceiling of 0.000 and MOVE-GRAMMAR §1.3's non-existent MEANING move. A lawful QUALIFY needs a SECOND typed field and this card declares none. The pool's sentence-count spread (§3.4) therefore cannot be held here without breaking a wall, and the spread is carried instead by opening, length (7 to 12 words) and construction. Nothing is trimmed: three variants in, three out, the same vids, the same order, the same angles, and every wording that leaves the product stays in the annex history (§22 (a) and (e) — the round-1 wordings *without cover or defile* and *Standing on open ground* are substitutions, one for one, not removals).
- **A consequence of the same drops, stated once:** with the unlicensed claims gone, the three variants carry the IDENTICAL claim set and are distinguished only by angle and wording. That is the arithmetic of a spine whose card licenses a single standing fact, not a merge.
- **One wording flagged for the refiner, lawful as written.** Variant 1's `defile` and variant 2's row phrase `no advantage of ground` are the packet's two most compressed lines; under the density law (§21.4) they are kept as written, and a refinement that makes either plainer with no law behind the change is the regression that section names. The round-1 flag on the participial opener is DISCHARGED by this round.
- **One recurrence declared, not a refusal.** Variant 2's new face fronts the record's head (`Under terrain`), and the sibling pool `readiness WEAK` fronts its own (`Set down under readiness`). The register card licenses the office's formula to recur while the FACT may not; both facts are different fields of different readers. Declared here so a refuter reads it as a licensed recurrence rather than as a template, and so that a third pool fronting a third head would be worth counting.
