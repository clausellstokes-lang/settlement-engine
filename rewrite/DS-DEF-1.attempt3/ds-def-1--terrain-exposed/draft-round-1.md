# DS-DEF-1 · `terrain EXPOSED` · REWRITE draft round 1
Seat: Opus 5 (Fable-unvalidated) · block DS-DEF-1 · role `spine` · 3 variants in, 3 variants out, 12 faces.
Paste-ready: the three numbered rows below replace the pool's three variant rows under the bold pool line. The typed lines (ROLE, READS, RELATION, ATTACH, FORM, MOVE) are untouched and not repeated here.

1. `[visitor]` Open ground reaches {settlement} from every side.
   - `[face]` The way in to {settlement} crosses ground without cover or defile.
   - `[face]` From any quarter, the approach to {settlement} runs over open country.
   - `[face]` The country about {settlement} carries no rise and no hollow.
2. `[ledger]` The site of {settlement} holds no advantage of ground.
   - `[face]` Standing on open ground, {settlement} takes no strength from the land.
   - `[face]` The land at {settlement} lies open from end to end.
   - `[face]` Nothing in the site of {settlement} rises or narrows.
3. `[street]` No hill and no narrows lie anywhere about {settlement}.
   - `[face]` The town stands on open ground at {settlement}.
   - `[face]` The ground at {settlement} has neither bank nor fold.
   - `[face]` What the ground gives {settlement} is open country and no more.

--- NOTES

## A. The card, as this packet reads it
The card licenses ONE claim: *that the reader `text(terrain)` selects the row `Plains` of `TERRAIN_DEFENCE_OF` in `defenseStateProse.js`, as a STANDING fact of the record* (card line **may claim**, under **reads** and **predicate**). The row's own name is `terrain EXPOSED`, and the map's contrast is with `terrain FAVOURABLE to the defender`, so the licensed fact is that the site's ground carries no defensive advantage — nothing more and nothing less.

The card refuses a count, a cause, a season, a future, a standpoint and a second fact (**may NOT**); a totality over persons, an exemption from a duty, a named character's fate and a theological claim (**REFUSED COLUMNS**); and every citation, the source standing SOURCE-UNRESOLVED, so arm A13 refuses any face that names a record holder (**source**). The bag is FILLED at this block's call sites with `{settlement}` alone, so `{band}`, `{route}` and `{counterpart}` appear in no face. **seat/form** is `sentence`, so every face is a full sentence and none is a fragment.

**One reading the packet takes deliberately.** The card prints `predicate: … === Plains`, but `TERRAIN_DEFENCE_OF` routes both `Plains` and `Desert/Arid` to this pool. No face therefore claims anything true of one of those two and false of the other: no *level*, no *flat*, no *grass*, no *field*, no *dust*. Every face states only that the ground carries no defensive feature, which is what both rows hold.

## B. Licence, face by face
Every face asserts exactly one claim — the terrain row — and nothing else. The clause cited is the clause that licenses that claim in that wording. The feature nouns (*cover*, *defile*, *rise*, *hollow*, *hill*, *narrows*, *bank*, *fold*) are the realisation vocabulary of the row itself, the same vocabulary the shipped pool already spends on *hill* and *narrows*; each names a feature the site does NOT hold, so none adds a particular to the record (R-DA-10's fault-24 guard: nothing is invented to make a sentence land).

**Variant 1 `[visitor]`** (all four faces carry `{settlement}`, none opens on it — ARCH §2.5 / T-F8)

| face | claim | licensing clause |
|---|---|---|
| row | the ground about the site carries no defensive feature | **may claim** (the row `EXPOSED` is selected), read with **reads** / **predicate**; `{settlement}` by **bag** (FILLED) |
| f2 | same claim, worded as the way in | **may claim**, with the arrival vantage licensed by the card's **angle** line (`ledger street visitor`); `{settlement}` by **bag** |
| f3 | same claim, worded as the approach | **may claim** + **angle**; `{settlement}` by **bag** |
| f4 | same claim, worded as the surrounding country | **may claim**; `{settlement}` by **bag** |

**Variant 2 `[ledger]`** (all four faces carry `{settlement}`, none opens on it)

| face | claim | licensing clause |
|---|---|---|
| row | the site holds no advantage of ground | **may claim**; `{settlement}` by **bag** |
| f2 | same claim, worded as what the town takes from the land | **may claim**; `{settlement}` by **bag** |
| f3 | same claim, worded as the openness of the land | **may claim**; `{settlement}` by **bag** |
| f4 | same claim, compressed to the two things the site does not do | **may claim**; `{settlement}` by **bag** |

**Variant 3 `[street]`** (all four faces carry `{settlement}` — the shipped variant carried the slot, so the slot set is held; none opens on it)

| face | claim | licensing clause |
|---|---|---|
| row | no hill and no narrows about the site | **may claim** (the shipped sentence's licensed half, kept); `{settlement}` by **bag** |
| f2 | same claim, worded as where the town stands | **may claim**; `{settlement}` by **bag** |
| f3 | same claim, worded as the two absent features | **may claim**; `{settlement}` by **bag** |
| f4 | same claim, worded as what the ground gives | **may claim**; `{settlement}` by **bag** |

The four faces of each variant are claim-equal to one another (arm A6 reads across the faces, never back to the old sentence) and differ in vocabulary and rhythm: the ground / the way in / the approach / the country in variant 1; advantage of ground / strength from the land / the land end to end / the compressed negative in variant 2; the two absences / where the town stands / two further absences / the pseudo-cleft in variant 3. No two of the twelve share their first two words.

## C. What each old sentence claimed, and what was dropped
Dropping is the rewrite's purpose; the shipped breach is the corpus's known state. Nothing was added.

**Variant 1, old:** "{settlement} sits open. There is no ground here that helps it, and everything the town has must be built rather than found."
- KEPT: the terrain row, twice over ("sits open"; "no ground here that helps it").
- DROPPED: "everything the town has must be built rather than found" — (a) a SECOND FACT, about the town's works rather than its ground (**may NOT**: a second fact); (b) a totality over the town's holdings, which the block's own fence forbids because this lens reads the terrain record and asserts nothing about any arm of the defence (`:2510-2516`); (c) the antithesis shape "built rather than found", whose rejected alternative names no sibling key or band (R-DA-02).
- Also corrected in form: the expletive opener "There is" (R-DA-07) and the row opening on the `{settlement}` slot (wall 10; R-DA-17).

**Variant 2, old:** "The site gives {settlement} nothing defensively. Every advantage the town holds is one it has paid for and must keep paying for."
- KEPT: the terrain row ("the site gives nothing defensively"), reworded as the advantage of ground.
- DROPPED: the whole second sentence, which carried (a) a totality over the town's advantages (**REFUSED COLUMNS**, and R-DA-15's quantifier rule: no column of this card is `closed`); (b) a COST claim, which is the CONSEQUENCE move and is double-licensed by event provenance and a household row, neither of which this card holds (MOVE-GRAMMAR §1.2 row 7); (c) "must keep paying for", a standing obligation reaching forward, which is a future in all but mood (**may NOT**: a future; A2; THE PROMISE).

**Variant 3, old:** "The town has no hill and no narrows and does not pretend otherwise; what {settlement} holds it holds by standing on it."
- KEPT: "no hill and no narrows", the licensed half, now the row.
- DROPPED: (a) "does not pretend otherwise", a belief frame and a STANDPOINT attributed to the town, and a totality over its people (**may NOT**: a standpoint; **REFUSED COLUMNS**: a totality over persons; R-DA-13's belief-frame floor); (b) "what {settlement} holds it holds by standing on it", a SECOND FACT about the town's holdings and a near-gnomic closer (**may NOT**: a second fact; R-DA-12).
- Also corrected in form: the pronoun closer "on it" (R-DA-04) and the three-limb chain joined by "and … and".

## D. Sibling distance (arms A1 and A11)
The block's sibling pools are the four `readiness` bands, `terrain FAVOURABLE to the defender`, and `strategic value HIGH` / `LOW`.
- Against **readiness**: no face names the works, the walls, the watch, the muster or the margin, and none says the town is defended or undefended. The word *defence* and its cognates appear in no face, so nothing here can be read as a claim about any arm of the five.
- Against **terrain FAVOURABLE**: that pool's shipped wording is "the approach is narrow", "sited where the terrain carries part of the defense", "the ground here does the arguing". No face here mirrors those constructions, and *carries part of the defense* and *does the arguing* are struck; the two rows are mutually exclusive readings of one field, so no town can meet both and no contradiction is possible.
- Against **strategic value**: no face names *worth taking*, *a prize*, *an army's season* or anything a town could be wanted for.
- The neighbouring packet for `readiness CRITICAL` reserved *open*, *the ground*, *the approach*, *hill* and *narrows* to this pool; this packet spends exactly that vocabulary and takes none of that packet's (*floor*, *band*, *stranger*, *undefended*).

## E. Form checks run over all twelve faces
No em dash, no exclamation, no question mark, no digit or percent, no `which`-clause, no citation and no record holder named (**source**: none, standing SOURCE-UNRESOLVED — arm A13), no first or second person, no figure, no simile, no sense verb on an abstraction, no inanimate subject with an intent verb, no expletive opener, no evaluative adjective on the place, no triad. Spelling follows the shipped corpus's *defense* where the word occurs at all, which here is nowhere (R-DA-22). The three rows' first two words are distinct (`Open ground` / `The site` / `No hill`); NO row and no face opens on the settlement token, so wall 10 is met at zero; the rows close on three kinds — a condition of place (*from every side*), an absence (*no advantage of ground*), an object with its absence (*no hill and no narrows … about {settlement}*) — under R-DA-04. Face lengths run 7 to 11 words (7 · 11 · 11 · 10 | 9 · 11 · 10 · 9 | 9 · 8 · 9 · 11), with one line under eight words against R-DA-06's `< 8` floor. The band is narrow and the reason is the card, not the pen: one licensed fact is one sentence's load, and rhythm follows load. The variety the pool can carry is in construction and opening, and it is carried there — a plain predication, a *that*-free prepositional frame, a fronted adverbial, a negative subject, a participial opener, a pseudo-cleft.

## F. THE THREAD
Each row is a spine and stands first in its passage, so each is written to hand a noun forward for whatever modifier the composer seats next: *ground* and *side* (variant 1), *site* and *ground* (variant 2), *hill*, *narrows* and *ground* (variant 3). Every face is one sentence carrying one claim, so no face changes subject mid-passage and none depends on a sentence before it. Where a face is read after a sibling modifier rather than first, its opening noun (*open ground*, *the way in*, *the site*, *the land*, *the ground*) is a concrete civic noun of the same passage's subject matter, so the thread is picked up rather than dropped.

## G. REFUSALS
- **No variant refusal.** All three variants are written lawful under the card.
- **One POOL-level law this pool cannot meet, and it is not curable by wording:** MOVE-GRAMMAR §2.1's level-1 rule that a pool of k variants carries min(k, 8) DISTINCT level-1 grammars, at least two in any pool of two or more. This card resolves one reading, declares no move, no relation and no attach set, and names no second field, so V2 (a structural-consequence field), V3 (`none-exists`), V4 (a named object), V5 (an institution row), V6 (an unresolved state value), V7 (event provenance) and V8 (a `not-held` field with provenance) are all unlicensed here. All three variants are V1 `PRESENT`, and the pool's spread lives in its angles, its openings and its rhythm. Recorded for the chair as a finding, not as a wording fault.
- **A consequence of the drops, declared so it is not re-found as a defect.** The shipped pool ran 2 sentences / 2 sentences / 1 sentence; every rewritten variant is ONE sentence. This is forced, not chosen: in all three variants the second sentence (or the second limb) was the only place the unlicensed claims sat, and once they come out the card licenses exactly one fact — so a second sentence could only restate the first, which is R-DA-03's summarising second sentence at a ceiling of 0.000 and MOVE-GRAMMAR §1.3's non-existent MEANING move. A lawful QUALIFY needs a SECOND typed field and this card declares none. The pool's sentence-count spread (§3.4's "where they differed in sentence count they keep differing") therefore cannot be held here without breaking a wall, and the spread is carried instead by opening, length (7 to 11 words) and construction. Nothing is trimmed: three variants in, three out, the same vids, the same order, the same angles, and every wording that leaves the product stays in the annex history.
- **A consequence of the same drops, stated once:** with the unlicensed claims gone, the three variants carry the IDENTICAL claim set and are distinguished only by angle and wording. That is the arithmetic of a spine whose card licenses a single standing fact, not a merge.
- **Two wordings flagged for the refiner, both lawful as written.** (i) `[visitor]` f2's *defile* and `[ledger]` f4's *rises or narrows* are the packet's two most compressed lines; under the density law they are kept as written, and a refinement that makes either plainer with no law behind the change is the regression §21.4 names. (ii) `[ledger]` f2 opens on a participle (*Standing on open ground*); it is the only participial opener in the twelve, against R-DA-18's floor of 0.020 measured register-wide, and a second one anywhere in this block would be worth counting.
