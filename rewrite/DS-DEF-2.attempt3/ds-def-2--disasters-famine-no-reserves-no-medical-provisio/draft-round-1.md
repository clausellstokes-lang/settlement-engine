# DS-DEF-2 · `Disasters & Famine: NO reserves, NO medical provision` — REWRITE, draft round 1

Seat: Opus 5 (Fable-unvalidated). Role: spine. Three existing variants, rewritten in place under their own vids, their own angle tags, in their own order. No variant added, removed or merged. Four faces per variant (the numbered line plus three `[face]` sub-rows). Paste the block below under the pool's bold heading, replacing the three numbered rows and nothing else. The pool's typed lines are untouched and are not repeated here.

---

1. `[ledger]` Food is not held in store at {settlement}, and no house stands for the sick.
   - `[face]` Neither a granary nor an infirmary stands at {settlement}.
   - `[face]` Provision at {settlement} runs to neither grain kept back nor care for the ill.
   - `[face]` No grain is kept at {settlement}, and no sick-house.
2. `[street]` The town keeps no store against hunger and no place for its sick.
   - `[face]` Nothing is laid up against hunger in the town, and nothing stands ready for the sick.
   - `[face]` A grain loft and a sick-house are both absent from the town.
   - `[face]` What the town holds against hunger and what it holds for the sick come to the same nothing.
3. `[visitor]` A stranger asking after the granary or the sick-house at {settlement} finds neither.
   - `[face]` A visitor crosses {settlement} without passing a granary or a house for the sick.
   - `[face]` No granary and no sick-house stand at {settlement} for a newcomer to find.
   - `[face]` Whoever comes to {settlement} for stored grain or for the care of the sick comes for what the town has not got.

---

--- NOTES

**REFUSALS: none.** All three variants are written lawful. No variant is banked.

**The claim set, once, for all twelve faces.** The card's `may claim` line licenses exactly one thing: that the reader `disasterRowSituation(granary, hospital, church)` selects the row `no reserves, no medical provision`, as a STANDING fact of the record. The row's two limbs — no food reserve, no medical provision — are the two halves of that one licensed value, not two facts; the card's `may NOT: a second fact` bars a fact from OUTSIDE the row, and every face carries both limbs and nothing else. Arm A6 therefore reads claim-equal across all four faces of each variant and across the three variants.

**Slot sets (T-F8, the projector).** Variant 1: `{settlement}` in all four faces. Variant 2: no slot in any face. Variant 3: `{settlement}` in all four faces. Each variant's four faces carry the identical slot set, as the parent's. No face OPENS on `{settlement}` (a `proper`-typed slot of the block's bag), so no sentence face is refused under T-F8, and R-DA-17's settlement-opener rule is satisfied at zero.

**Per-face licensing.**

*Variant 1 `[ledger]` — the compiled accounting.*
- Numbered line — "Food is not held in store at {settlement}": limb one of the row (`may claim`, the row value's first half); the reads' `granary` input is the object stated absent. "no house stands for the sick": limb two (`may claim`, second half); the reads' `hospital`/`church` inputs are the provision stated absent. `{settlement}`: the bag, FILLED at this block's call sites. Angle `ledger`: the card's `angle` line.
- Face 2 — "Neither a granary nor an infirmary": both limbs, same clause, named by the reads' own two objects. Nothing else asserted.
- Face 3 — "grain kept back" / "care for the ill": both limbs; "Provision … runs to" is a measurement in words (R-DA-11), not a figure, and adds no claim.
- Face 4 — "No grain is kept" / "no sick-house": both limbs; the elliptical second clause is one sentence, form `sentence` as the card requires.

*Variant 2 `[street]` — the town's plain daily terms; no slot.*
- Numbered line — "no store against hunger": limb one. "no place for its sick": limb two. "against hunger" names what a food reserve is FOR, which is the reads' own object read at its function; it asserts no season, no event and no future, and is not a second fact.
- Face 2 — "Nothing is laid up against hunger" / "nothing stands ready for the sick": both limbs, passive-flat.
- Face 3 — "A grain loft and a sick-house are both absent": both limbs, object-first, the flat absence verb (R-DA-02, the LACK stated flat with no completing "but").
- Face 4 — "come to the same nothing": both limbs held in one measurement; it states the two absences' equality, which is the row's own arithmetic, not a verdict, a moral or a gloss (the MEANING move does not exist — MOVE-GRAMMAR §1.3).

*Variant 3 `[visitor]` — what a newcomer meets; the frame is the card's licensed `visitor` angle.*
- Numbered line — "asking after the granary or the sick-house … finds neither": both limbs. The generic visitor's own act is the angle's frame, licensed by the card's `angle` line; no act of the TOWN is asserted anywhere in this variant (that is what the shipped sentence asserted and what the rewrite drops).
- Face 2 — "without passing a granary or a house for the sick": both limbs as a spatial negative; no geography claim beyond the settlement itself.
- Face 3 — "No granary and no sick-house stand at {settlement}": both limbs, object-first, the visitor kept as the tail frame.
- Face 4 — "stored grain or … the care of the sick … what the town has not got": both limbs; the repetition of "comes" is rhythm, and the WORD may recur where the FACT does not (the register card).

**What the shipped sentences claimed and this rewrite DROPS (dropping is the rewrite's purpose; the shipped breach is the corpus's known state).**

| vid | dropped from the shipped sentence | the card clause that refuses it |
|---|---|---|
| 1 | "a failed harvest is immediate hardship here and a plague runs until it burns out" | `may NOT: a cause`, `a second fact`; and it is a CONSEQUENCE with no event-provenance field on this reading (R-DST-B / MOVE-GRAMMAR §1.2 row 7) |
| 1 | "against a bad year" | `may NOT: a season` |
| 1 | "has nobody to treat the sick" | REFUSED COLUMNS, always: a totality over persons — restated as the absence of the provision, which is what the reads hold |
| 2 | "which is to endure it and count afterwards" | the which-clause is barred outright (R-DA-03, wall 6); the act asserted is `a second fact`; "count afterwards" is `a count` |
| 2 | "a bad season" | `may NOT: a season` |
| 2 | the standpoint carried by "is the same as … which is to endure" | `may NOT: a standpoint` |
| 3 | "is directed to neither" | `may NOT: a second fact` — an act of the town's people that no field holds |
| 3 | "because there is neither" | `may NOT: a cause`; and the clause restates its own subject |

**Nothing added.** No face names a count, a cause, a season, a future, a standpoint, a second fact, or a second civic object of the class `store` (each face names at most one store object). No face carries a totality over persons, an exemption, a named character, or a theological claim. No citation is written anywhere: the card's `source` is `(none) · standing SOURCE-UNRESOLVED`, so arm A13 would refuse a named holder, and none is named.

**Surface walls.** No em dash, no exclamation, no question mark, no digit, no percent, no `which`-clause, no bare future indicative, no existential opener, no pronoun closer, in any of the twelve faces.

**Spread (R-DA-05, A11).** The three numbered lines open "Food is", "The town", "A stranger". Across all twelve faces the first two words are distinct within each variant: variant 1 — "Food is" / "Neither a" / "Provision at" / "No grain"; variant 2 — "The town" / "Nothing is" / "A grain" / "What the"; variant 3 — "A stranger" / "A visitor" / "No granary" / "Whoever comes". Lengths run from nine to twenty-two words, so whichever face the roll takes, the pool does not read to a metronome.

**THE THREAD.** Every face is a SPINE and stands first in its composed passage, so it must hand a noun forward rather than pick one up. Each face closes on or carries a concrete civic noun a modifier can take — the granary, the grain, the store, the sick-house, the infirmary, the sick, the town — and no face ends on a pronoun or an abstraction a following modifier could not grasp. Variant 2's fourth face ends on "nothing"; the nouns it hands forward are "the town", "hunger" and "the sick", all in the spine's own clause, which the thread law allows (a noun carried forward from the spine, not necessarily its last word).

**Siblings (arms A1 and A11).** The pool's four sibling rows in this reading — `granary AND hospital`, `granary AND parish care only`, `granary, NO medical provision`, `NO reserves, hospital present` — are neither restated nor contradicted: no face here names a granary, a hospital or parish care as PRESENT, and none names the other rows' bands. The nouns this pool spends are grain loft, grain kept back, store, granary, sick-house, infirmary, house for the sick and care for the ill. Two of them, "granary" and "infirmary", also stand in the siblings' shipped text (`granary AND hospital`; the `granary AND parish care only` visitor row's "modest infirmary"), which is lawful and unavoidable — they are the reads' own objects and this pool states them absent where the sibling states them present, so the rows differ in FACT and cannot be read as a restatement. Every other noun above is this pool's alone.

**Measured (this draft, counted on the rendered row with `{settlement}` as one word).** Twelve faces over three variants. Variant 1: fifteen, nine, fourteen, nine. Variant 2: thirteen, sixteen, twelve, eighteen. Variant 3: thirteen, fourteen, thirteen, twenty-two. Surface-wall scan over all twelve: zero em dashes, zero exclamations, zero question marks, zero digits, zero percents, zero `which`-clauses, zero bare futures, zero existential openers, zero quantifiers over persons, zero causal connectives.
