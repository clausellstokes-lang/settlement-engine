1. `[ledger]` Medical provision at {settlement} runs to a hospital, and no reserve of food is kept.
   - `[face]` Nothing is laid by to eat at {settlement}, and the sick have a hospital.
   - `[face]` A hospital serves {settlement}. No food is set by.
   - `[face]` The hospital at {settlement} is kept up; nothing is stored to eat.
2. `[street]` The town keeps a house for the sick and keeps nothing put by.
   - `[face]` The sick here get a roof and a bed, and no food is laid up.
   - `[face]` This town has a hospital and nothing in reserve.
   - `[face]` No food is kept here at all. The sick have beds.
3. `[unfolding]` Beds at {settlement} are in use, and nothing is being laid in.
   - `[face]` As things are kept at {settlement}, the sick have a hospital and nothing is held back to eat.
   - `[face]` Reserve at {settlement} stands at nothing, and the sick go to a hospital.
   - `[face]` Care of the sick at {settlement} goes on in a hospital, while nothing goes into reserve.

--- NOTES

**REFUSALS:** none. All three variants are written lawful; no variant is banked.

**THE CARD, as this packet reads it.** The one licensed claim is the row itself: `disasterRowSituation(granary, hospital, church)` selects `no reserves, hospital` of `DISASTER_ROW_POOL` in `defenseStateProse.js`, as a STANDING fact of the record. That row value has two limbs — a hospital present, no food reserve held — and both limbs are the one licensed claim, not two facts. Every one of the twelve wordings states both limbs and nothing else. Slot set: variants 1 and 3 carry `{settlement}` (the bag's only filled slot at this block's call sites), variant 2 carries none, each face matching its parent's slot set (T-F8); no wording opens on the `proper`-typed slot, so every row parses as sentence-form (§2.5 seam contract) and the settlement token opens none of the pool (R-DA-17).

**WHAT WAS DROPPED, and under which clause.**
- Variant 1, old tail *"a crop failure here becomes hardship the same season it happens"* — DROPPED under `may NOT: a cause · a season · a future`. It is a consequence forecast on an event the card holds no provenance for.
- Variant 1, old *"can treat and contain an outbreak"* — the containment capability DROPPED under `may NOT: a second fact`; the hospital is kept as the standing civic object the row names.
- Variant 2, old *"which is an unusual way round"* — DROPPED twice over: `may NOT: a standpoint`, and the which-clause is a wall (R-DA-03, wall 6).
- Variant 2, old *"and does not comfort anyone"* — DROPPED under `REFUSED COLUMNS: a totality over persons`, and as a feeling (MOVE-GRAMMAR §1.3, FEELING does not exist).
- Variant 2, old *"better prepared for the sickness than for the hunger"* — the comparative frame DROPPED as an assessment no rating field holds; both limbs it rested on are kept flat.
- Variant 3, old *"the sickness it has seen"* and *"the hunger it has not"* — DROPPED under `may NOT: a cause`, R-DST-B (a standing configuration field licenses a STRUCTURAL clause and never a HISTORICAL one) and MOVE-GRAMMAR §1.2 row 2 (no event-provenance field here).
- Variant 3, old *"nothing in hand is correcting the imbalance"* — DROPPED as a trend claim toward a future state (`may NOT: a future`; NL-6 / R-DA-07, state never fate) and as a standpoint.
- Nothing was ADDED anywhere. No citation appears in any face: `source: (none) · standing SOURCE-UNRESOLVED`, so arm A13 would refuse one.

**PER FACE — which card clause licenses each claim.**

*Variant 1 `[ledger]`*
| wording | claims | licensing clause |
|---|---|---|
| parent | a hospital present; no food reserve | `may claim`: the row `no reserves, hospital`, both limbs, as a STANDING fact |
| face 1 | no food reserve; a hospital present | same row; the lack stated flat as its own half with no completing "but" (R-DA-02's LACK limb) |
| face 2 | a hospital present; no food reserve | same row; the second sentence is the one turn outward and sits last (THE THREAD, §1.4.1) |
| face 3 | a hospital present; no food reserve | same row; `stored` is a verb of the same absent reserve, never a second civic object of the class `store` |

*Variant 2 `[street]`*
| wording | claims | licensing clause |
|---|---|---|
| parent | a hospital present (as the house for the sick); no food reserve | same row; the hospital named by its civic function, the object the row holds (R-DA-10) |
| face 1 | a hospital present; no food reserve | same row; `a roof and a bed` are the same object in the street's vocabulary, two items and never three (R-DA-10) |
| face 2 | a hospital present; no food reserve | same row; the short line exists (R-DA-06's `< 8` floor; NL-2) |
| face 3 | no food reserve; a hospital present | same row; the subject shift is last, the passage's one turn outward (THE THREAD) |

*Variant 3 `[unfolding]`*
| wording | claims | licensing clause |
|---|---|---|
| parent | a hospital in use; nothing going into reserve | same row, in the continuing present; no tick, no trend, no term is asserted |
| face 1 | a hospital present; no food reserve | same row; the fronted `As things are kept` frames the standing state, not a history |
| face 2 | no food reserve; a hospital present | same row; the lack flat, the object last |
| face 3 | a hospital present; no food reserve | same row; `while` joins the two limbs of one row value and asserts no cause (MOVE-GRAMMAR wall 1 not engaged: there is no CAUSE move here) |

**THE WALLS, checked across all twelve.** No em dash; no exclamation; no digit; no `which`; no question; no first or second person; no figure, sense verb on an abstraction or inanimate intent; no future indicative and no `would`; no verdict, moral, uplift or summarising second sentence; no office, count or exemption (the institution table licenses none here); no named character; no theology; no citation.

**SPREAD, reported (information, not a claim of passing).** Word counts 9 to 18. First two words distinct across all twelve: *Medical provision · Nothing is · A hospital · The hospital · The town · The sick · This town · No food · Beds at · As things · Reserve at · Care of* (A11 on the three variant rows, and held across the faces). Joints varied: `and` eight, a full stop two, a semicolon one, `while` one. Sentence counts one or two, inside A1. Close kinds: nine on the absence, three on the object — the row's own shape is a lack, so the absence close is load-driven; reported here rather than forced, since R-DA-04 asks the KIND to vary and the pool cannot close on a store it does not hold.

**SIBLING DISTANCE (arms A1 / A11).** The four sibling rows of `Disasters & Famine` were read first. No wording restates one: the `granary AND hospital` row's *somewhere to put the sick* is not reused (the parent of variant 2 was rewritten off it); the `NO reserves, NO medical provision` row's *holds no food against a bad year* is not reused; no wording contradicts a sibling, because each states only its own row's two limbs.
