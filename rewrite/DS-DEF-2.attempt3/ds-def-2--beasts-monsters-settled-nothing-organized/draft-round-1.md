1. `[ledger]` The country around {settlement} holds few creatures; the town keeps no wall and musters neither garrison nor militia.
   - `[face]` Beasts are few in the country about {settlement}. The town fields no garrison and no militia, and stands unwalled.
   - `[face]` The beast account at {settlement} comes to little on both sides: few creatures in the country, and in the town neither a wall nor a garrison.
   - `[face]` Facing a country thin of creatures, {settlement} holds no wall and keeps no force of its own under arms.
2. `[street]` Out beyond {settlement} the creatures are few, and the town has no wall and no garrison or militia.
   - `[face]` Quiet country lies about {settlement}, and the town neither walls itself nor musters.
   - `[face]` Creatures are scarce about {settlement}. The town stands without wall or militia.
   - `[face]` Little presses on {settlement} out of the country, and with no wall and no garrison or militia the town stands open.
3. `[visitor]` The ground about {settlement} carries few creatures, and the town on it shows no wall and no force under arms.
   - `[face]` Few creatures walk the open country at {settlement}, and the town in it stands unwalled, with no garrison and no militia.
   - `[face]` In a country that keeps few creatures, {settlement} stands unwalled, and no garrison or militia is quartered in the town.
   - `[face]` What comes out of the country at {settlement} is little, and the town keeps neither wall nor muster.

--- NOTES

**The card, as this packet reads it.** The licence card for `Beasts & Monsters: settled, nothing organized` licenses ONE typed triple and no more: `may claim` — that `beastsRowSituation(family, perimeter, force)` selects the row `settled country, neither` of `BEASTS_ROW_POOL`, as a STANDING fact of the record. Read at the call site (`defenseStateProse.js:655`), that row's three arguments resolve as `family = settled`, `perimeter = standingDefenseForces(settlement).walls.present === false`, `force = garrison.present || militia.present === false`. So the triple has three realised halves and the packet names them:

- **[claim-a]** the `settled` family word: the country around this settlement carries little of the beast kind, as a STANDING fact.
- **[claim-b1]** `walls.present === false`: no wall stands at this settlement (the walls bucket is empty, so the town is unwalled).
- **[claim-b2]** `garrison.present === false` and `militia.present === false`: the town fields neither garrison nor militia.

**[claim-b1] and [claim-b2] together are the row's `neither`, and they are ONE typed LACK, not two absence moves.** MOVE-GRAMMAR §1.1 defines the variant's typed claim as the set of (move, field, value) triples it asserts; the field here returns one composite value, so every variant and every face asserts the same single triple and the twelve rows are claim-equal by construction (arm A6 read across the faces; arm C-pair against each row's own BEFORE; arm C-sibling across the three variants). A face that named only the wall, or only the force, would under-realise the value that discriminates this pool from `settled country, perimeter`, which is U9's protected clause — so all twelve name both halves.

**[bag]** `bag: {band: RESERVED, route: proper, settlement: proper}` · `FILLED at this block's call sites: {settlement}`. Every row carries `{settlement}` and nothing else, so the face slot set equals the parent's in all twelve (annex grammar §2.5, the face sub-row row; T-F8). No row opens on `{settlement}`, which T-F8 refuses for a sentence face and R-DA-17 refuses as the register's default opener.

**[spine]** `relation: (a spine takes no relation)` · `attach: (empty)` · `seat/form: (not a seat-taker) / sentence` · `move: (none declared)`. Every row is a full sentence; none is a fragment; none opens on a comma or on a clause-list word; none carries a connective owed to the connectives leaf.

**Per face, the licence.**

*Variant 1 `[ledger]`* — the office's flat account, the two columns set down. Rhythms: a semicolon pair, two sentences, a colon apposition, a fronted participial.
- Parent: "The country around {settlement} holds few creatures" = [claim-a]; "keeps no wall" = [claim-b1]; "musters neither garrison nor militia" = [claim-b2]. Slot by [bag].
- Face 2: "Beasts are few in the country about {settlement}" = [claim-a]; "stands unwalled" = [claim-b1]; "fields no garrison and no militia" = [claim-b2].
- Face 3: "few creatures in the country" = [claim-a]; "neither a wall" = [claim-b1]; "nor a garrison" plus "comes to little on both sides" = [claim-b2]. The colon carries an apposition of the two columns, never a cause.
- Face 4: "a country thin of creatures" = [claim-a]; "holds no wall" = [claim-b1]; "keeps no force of its own under arms" = [claim-b2]. "Of its own" is the reading's own boundary: the bucket read here is the town's garrison and militia, so the phrase claims nothing about a hired company, which this reading does not read.

*Variant 2 `[street]`* — the town's plain terms, the pool's short lines.
- Parent: "the creatures are few" = [claim-a]; "no wall" = [claim-b1]; "no garrison or militia" = [claim-b2].
- Face 2: "Quiet country lies about {settlement}" = [claim-a]; "neither walls itself" = [claim-b1]; "nor musters" = [claim-b2].
- Face 3: "Creatures are scarce about {settlement}" = [claim-a]; "without wall" = [claim-b1]; "or militia" = [claim-b2]. Twelve words in two sentences, the pool's short line (R-DA-06's `< 8` limb on its first sentence).
- Face 4: "Little presses on {settlement} out of the country" = [claim-a]; "with no wall" = [claim-b1]; "and no garrison or militia" = [claim-b2]; "the town stands open" closes on the condition the two make.

*Variant 3 `[visitor]`* — the angle tag stands exactly as the row carries it; the stranger does not, see the drops.
- Parent: "The ground about {settlement} carries few creatures" = [claim-a]; "shows no wall" = [claim-b1]; "and no force under arms" = [claim-b2].
- Face 2: "Few creatures walk the open country at {settlement}" = [claim-a]; "stands unwalled" = [claim-b1]; "with no garrison and no militia" = [claim-b2].
- Face 3: "a country that keeps few creatures" = [claim-a]; "{settlement} stands unwalled" = [claim-b1]; "no garrison or militia is quartered in the town" = [claim-b2].
- Face 4: "What comes out of the country at {settlement} is little" = [claim-a]; "neither wall" = [claim-b1]; "nor muster" = [claim-b2].

**Claims DROPPED, with the card clause that refuses each (the rewrite's purpose; the shipped breach is the corpus's known state).**
- Variant 1, old: "the arrangement is a reasonable one rather than a gap" — a VERDICT on the town's arrangement. `may NOT: a standpoint`; MOVE-GRAMMAR §1.3's VERDICT non-move; R-DA-12's generalisation test. The "rather than a gap" half is also a CONTRAST whose rejected alternative names no sibling pool key and no sibling band, which R-DA-02 refuses outright. DROPPED in all four faces.
- Variant 1, old: "no organized defense" as the whole force-and-wall claim — an UMBRELLA over every defence bucket. `standingDefenseForces` partitions six buckets (`walls`, `garrison`, `militia`, `watch`, `mercenary`, `charter`) and this reading is handed only `walls` and `garrison || militia`; a town on this row may still hold a town watch, a mercenary company or a charter hall. So "no organized defense" asserts three absences no field of this reading holds. `may NOT: a second fact`; R-DA-08's LACK is licensed by the `none-exists` field and by nothing wider. DROPPED and replaced by the two halves the reading actually reads. This is the packet's sharpest correction and the reason no row anywhere says "watch", "nothing organized" or "no defense".
- Variant 2, old: "has never needed" — a NEED, which is a cause (the quiet country as the reason for the empty arrangement), and a claim over the town's whole past. `may NOT: a cause`; R-DST-B (MOVE-GRAMMAR §1.1): a standing configuration field licenses a STRUCTURAL clause and never a HISTORICAL one, and no event-provenance field is read here. DROPPED.
- Variant 2, old: "to think about what is outside it, and does not" — a belief frame and a mental state held over the town's persons. `may NOT: a standpoint`; the REFUSED COLUMN "a totality over persons"; MOVE-GRAMMAR §1.3's FEELING non-move; the register card's "no persona, no assigned reaction". DROPPED.
- Variant 3, old: "A stranger walks out of {settlement}" — a per-entry VANTAGE with an assigned experience. MOVE-GRAMMAR §1.3's "a per-entry FRAME / VANTAGE" non-move; the register card's office-holds-the-pen line (R-DA-01, declared once at the front matter, never per entry); `may NOT: a standpoint`. DROPPED; the `[visitor]` tag is kept exactly as the row carries it, because the tag is not the writer's to move and no second tag may be added.
- Variant 3, old: "in any direction at any hour" — a totality over the ground and the day, held by no field. `may NOT: a count` (a totality is a count in words) and `a second fact`. DROPPED.
- Variant 3, old: "meets nothing that would justify a watch" — two refusals. The JUSTIFICATION joins the country's quiet to the empty arrangement as a reason, which is `may NOT: a cause`; and the "watch" is the `watch` bucket, which this reading does not read at all, so its absence is unlicensed. DROPPED.
- Nothing is ADDED anywhere: no count, no cause, no season, no future, no standpoint, no second fact, no belief frame, no totality over persons, no named character, no deity, no exemption.

**Judgment calls, recorded for the veto.**
1. **No citation in any of the twelve.** `source: muster · standing LICENSED` permits one and §24 caps the budget at one per unit for one of S3's three reasons. Refused here for three reasons: a citation is TWO licensed claims, so a face carrying one would not be claim-equal to its three siblings (arm A6); citing in every face is the citation habit §24 makes a refuter's finding, against exemplar registers that cite at zero per 786 sentences; and on THIS row the muster holds no record to cite, because the town musters nothing. A line such as "the muster roll names nobody" would be an R-DA-08 class (b) GAP, licensed only by a typed `not-held` record field, and no such field exists here. The word "muster" appears twice in the packet as the common noun for raising a force, never as an attribution.
2. **The absent things are named by the buckets the reading reads.** `wall` and `unwalled` for `walls`; `garrison`, `militia`, `force of its own`, `musters` for `garrison || militia`. Terms the reading does not read (`watch`, `mercenary`, `charter hall`) appear nowhere, and no umbrella ("defense", "nothing organized") stands in for them. One term per thing inside a face (R-DA-22); the term varies across faces because the owner's four-faces law requires a different vocabulary or rhythm per face.
3. **The LACK never opens a variant.** MOVE-GRAMMAR wall 3 bars the ABSENCE move from the opening slot, so all twelve rows put [claim-a] first and the `neither` second. [claim-a] itself is a PRESENT — the `settled` family word rendered as a scarcity measured in words (R-DA-11: a comparison is a measurement in words) — and is not an absence move, which is why "Little presses on {settlement}" and "What comes out of the country is little" open lawfully.
4. **Two level-1 grammars, which is the admissible set.** With `attach` empty, no event provenance, no named object, no institution row and no `not-held` field, the drawable members of MOVE-GRAMMAR §2.1 are V1 (PRESENT) and V3 (PRESENT → LACK). §2.1 asks for min(k, |set|) distinct grammars, so two over three variants is the ceiling the licensing filter leaves, and the spread is carried instead by rhythm: one semicolon pair, one colon apposition, two two-sentence rows, two fronted phrases, one cleft.
5. **No sibling-band contrast word.** R-DA-02 would license a contrast naming `plagued` or `frontier`, and it is refused: naming a sibling band asserts what such a country holds, which this card does not license, and the three sibling rows of `BEASTS_ROW_POOL` are mutually exclusive branches a reader never meets together. Drafted and cut. The sibling pool `settled country, perimeter` is neither restated nor contradicted: its claim is a perimeter standing in a quiet country, this one's is no perimeter in the same, and no row of this packet borrows its "over-provided" vocabulary or its "Against a country slight in creatures" opening (arms A1 and A11).
6. **The thread.** Every row names both durable nouns of the passage, the country and the town, so a modifier placed after this spine can pick up either; no row closes on a pronoun (R-DA-04's `closers.pronounRate`), and the close kinds vary across the twelve between an absence, a condition ("stands unwalled", "stands open") and a civic object.
7. **Measured position, reported not claimed.** Twelve rows, 18 · 19 · 26 · 19 · 18 · 13 · 12 · 21 · 20 · 21 · 20 · 18 words; mean 18.75, within-pool sd 3.49 against R-DA-05's floor of 4.0 — one soft rule outside its band by roughly a sixth of a band-width, inside §16.2's ENTRY budget and depth. One semicolon and one colon in twelve (R-DA-06's rationing); zero digits, zero em dashes, zero exclamations, zero questions, zero `which` tails, zero expletive openers, zero second person; all twelve first-two-word openers distinct (A11); the settlement token opens none.

**REFUSALS: none.** All three variants are written lawful; none is banked. No variant is added, removed, merged or reordered; each keeps its number and its one bracketed angle tag, and the pool's typed lines (ROLE, READS, RELATION, ATTACH, FORM, MOVE) are untouched and unrepeated.
