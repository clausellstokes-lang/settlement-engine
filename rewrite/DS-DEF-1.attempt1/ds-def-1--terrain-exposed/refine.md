# DS-DEF-1 · pool `terrain EXPOSED` · REWRITE, REFINEMENT round (phase 2)

Seat: Opus 5 (Fable-unvalidated) — the REFINER, a different author than the drafter, writing for the Fable chair.
Refines `draft-round-1.md` one for one: 3 variants, 12 wordings, same vids, same order, same tags, same claim sets. No variant or face added, dropped or merged (§22). The pool's typed lines are untouched and are not repeated here.

## THE LICENCE CARD (re-printed from the dock this session, `node scripts/prose-licence-card.mjs DS-DEF-1 'terrain EXPOSED'`)

```
LICENCE (block DS-DEF-1 · role spine · key `terrain EXPOSED`)
  reads:      text(terrain) (via TERRAIN_DEFENCE_OF in defenseStateProse.js)
              (absent ⇒ no candidate; a modifier is silent, never "false")
  predicate:  text(terrain) (via TERRAIN_DEFENCE_OF in defenseStateProse.js) === Plains
  bag:        {band: RESERVED, counterpart: proper, route: proper, settlement: proper}
              FILLED at this block's call sites: {settlement}
  relation:   (a spine takes no relation)
  seat/form:  (not a seat-taker) / sentence      move: (none declared)     angle: ledger street visitor
  attach:     (empty: a spine takes no attach set)
  echo:       spine mounts 1 (tabs: defense) · modifier mounts 0 (none)
  covert:     no
  source:     (none) · standing SOURCE-UNRESOLVED
              NO citation is licensed: a face naming a record holder here is refused by arm A13
  may claim:  that `js)` (=== Plains) holds, as a STANDING fact of the record
  may NOT:    a count, a cause, a season, a future, a standpoint, a second fact
  audience:   player (no mark)
  REFUSED COLUMNS, always: a totality over persons; an exemption from a duty; a named character and
  that character's fate; a theological claim about a deity
```

Card clauses cited below as **C-reads**, **C-pred**, **C-bag**, **C-form**, **C-source**, **C-may**, **C-maynot**, **C-refused**. The claim inventory is the draft's, unchanged: **c1** the exposure fact · **c2** built, not found · **c3** the cost, standing and continuing (REFUSED, banked) · **c4** the town's own account (REFUSED, banked) · **c5** holding by presence.

---

## THE ROWS (paste under the pool's bold heading `**terrain EXPOSED**`; the heading itself is not part of the paste)

1. `[visitor]` `[plain]` Open ground reaches {settlement} from every quarter. The town built every defence standing here.
   - `[face]` The land offers {settlement} no cover of any kind. What cover the town keeps was made and not found.
   - `[face]` Flat country runs out from {settlement}. Its defence begins where the town does.
   - `[face]` Nothing in the site favours {settlement}. Its defence is the town's own doing, first to last.
2. `[ledger]` `[plain]` The country around {settlement} is no part of its defence. Every advantage the town holds is one it bought, and one it goes on buying.
   - `[face]` Set on ground that gives a defender nothing, {settlement} pays for its defence in the raising and in the keeping.
   - `[face]` The lie of the land does nothing for {settlement}. The town took its defence on as a charge and has not put it down.
   - `[face]` The plain lies level on all sides of {settlement}. Its defence was got by paying and is held by paying still.
3. `[street]` `[plain]` No hill stands over {settlement} and no narrows close upon it. The town says as much itself, and holds what it holds by standing on it.
   - `[face]` The town has no high ground and no close country, and does not talk as though it had. Nothing but the town itself holds {settlement}.
   - `[face]` Neither hill nor defile breaks the ground about {settlement}. Its hold is its own standing there, and the town owns to it.
   - `[face]` Nothing rises anywhere near {settlement}, and nothing narrows. The town keeps its place by standing in it, and puts the matter no higher.

--- NOTES

### Variant 1 · `[visitor]` (c1 + c2 in all four; claim set unchanged from the draft and from the BEFORE)

- **`[plain]`** — *"Open ground runs up to {settlement} on every side. Whatever defence stands here, the town built."* → the fronted-object close is **struck** and the line put in plain subject-first order. Target: the pool carried two fronted-object constructions (this one and variant 3's `holds what it holds`, which is the BEFORE's own spine and cannot move); two instances in twelve is the beginning of a habit, and the brief bars an inversion habit repeated across the pool. Index 0 is the canonical line a falsy seed draws (MOVE-GRAMMAR §3.2), so the flattest of the four belongs here. `reaches … from every quarter` replaces `runs up to` so the verb is not shared with variant 2's face 3.
- **`[face]` 1** — *"Level country runs up to the edge of {settlement}…"* → the terrain word moved from **level** to **cover** (`The land offers {settlement} no cover of any kind`), and `of any kind` moved out of the second sentence so the thread carries the bare noun forward (`no cover` → `What cover the town keeps`, §1.4.1). Target: `level` stood in four of the draft's twelve wordings — a lexical tic — and the edge-of-town noun asserted a boundary the card does not read. The antithesis `made and not found` is kept as the pool's one instance, licensed because the rejected alternative names the sibling band `terrain FAVOURABLE to the defender` (R-DA-02).
- **`[face]` 2** — *"Open ground surrounds {settlement} and gives the town nothing to work with. Its defence begins where the town does."* → first sentence cut to five words (`Flat country runs out from {settlement}`), the compressed close kept exactly. Target: §21.4, the density law — the close is the sharpest line in the variant and was not to be traded for plainness; the load belonged in front of it, not around it. This is now the pool's short line (13 words against a mean of 20.7), which R-DA-05 asks for by name.
- **`[face]` 3** — *"…The town put its defence there itself."* → the loose deictic `there` is **struck** for `Its defence is the town's own doing, first to last`. Target: the read-aloud ear and a varied close kind (R-DA-04) — the draft closed three of variant 1's four wordings on *the town*; this one now closes on extent. `favours` is kept deliberately: it is the field's own band word, the negation of the sibling key, and the sharpest licensed statement of c1 available (**C-may** with **C-pred**).

### Variant 2 · `[ledger]` (c1 + c3 in all four; c3 stays REFUSED and banked — refusal R-1 of the draft stands unchanged)

- **`[plain]`** — *"…one it bought, and one it is still buying."* → close changed to `and one it goes on buying`. Target: two of the draft's four ledger closes ended on *still*; a closer habit inside one variant is the tic the brief names. The claim is untouched (a continuing present, never a future — A2).
- **`[face]` 1** — *"Set on ground that yields a defender nothing, {settlement} carries the price of its own defence, in the putting up of it and in the keeping of it."* → the baggy double tail (`in the putting up of it and in the keeping of it`, 28 words) compressed to `pays for its defence in the raising and in the keeping` (20). Target: the ceiling, not the floor — the ledger parallel lands harder without the two `of it`s, and the single periodic sentence is now the one rhythm in the pool that no sibling face repeats. The fronted participle is kept as the pool's **only** fronted opener (the draft carried three: `Set on`, `Defensively the`, `Level on every side`).
- **`[face]` 2** — *"Defensively the site does nothing for {settlement}. What the town has for its defence it has by expense, and the expense does not stop."* → wholly re-vocabularied: `The lie of the land does nothing for {settlement}. The town took its defence on as a charge and has not put it down.` Target: three faults at once — the clinical fronted adverb `Defensively`, a second left-dislocation, and `the site` shared with variant 1 face 3. `a charge … not put down` is the ledger's own idiom for a standing liability and is the antique air living in the noun, never in the syntax (register card).
- **`[face]` 3** — *"The plain about {settlement} runs level in every direction…"* → `The plain lies level on all sides of {settlement}`. Target: `about {settlement}` stood in three of the draft's twelve wordings; it is now at one, in variant 3. The deliberate repetition of *paying* is kept — the WORD may recur where the FACT does not (register card), and it is this wording's thread noun.

### Variant 3 · `[street]` (c1 + c4 + c5 in all four; c4 stays REFUSED and banked — refusal R-2 of the draft stands unchanged)

- **`[plain]`** — *"…and no narrows close the road in."* → `and no narrows close upon it`. Target: hollow specificity (fault 24) — the draft's *the road in* asserted a route, and `route: proper` is unfilled at this block's call sites (**C-bag**), so no road is licensed. The BEFORE's own spine `holds what it holds by standing on it` is kept verbatim, which is where the pool's one free-relative construction now lives.
- **`[face]` 1** — kept as written. Target: §21.2 — a face already at the ceiling keeps its wording; `does not talk as though it had` is the least unlawful rendering of c4 (an ACT, never an interior — R-DA-14 / NL-5) and `Nothing but the town itself holds {settlement}` is the pool's one prohibition-kind close and its one name-given close together.
- **`[face]` 2** — *"Neither a hill nor a defile lies about {settlement}, and the town makes no claim to either. Its hold on the place is its own standing there."* → the articles dropped (`Neither hill nor defile`), the verb changed to `breaks the ground` (the Plains value in its own vocabulary, and no longer sharing `lies` with variant 2 face 3), and the claim order **reversed** so c4 sits last as the passage's one turn outward (§1.4.1's second arm). c4 is re-worded to `the town owns to it`. Target: sibling distance — `makes no claim to either` and the draft's `makes no larger claim` both read against `readiness ADEQUATE` variant 3's shipped *does not claim more than that*, and readiness mounts on this same block.
- **`[face]` 3** — *"Level on every side, {settlement} holds what it holds by standing on it, and the town has never said otherwise."* → rebuilt as two sentences: `Nothing rises anywhere near {settlement}, and nothing narrows. The town keeps its place by standing in it, and puts the matter no higher.` Target: three at once — `has never said otherwise` reached into history with no event-provenance field (R-DST-B / **C-maynot**); the draft repeated variant 3 plain's c5 clause verbatim, which is a sibling paraphrase and the one thing a face may never be; and `Level on every side` was the third fronted opener. c1 is now stated in **verbs** rather than a noun list, which is the widest vocabulary distance from this variant's three siblings that the voice allows.

### Laws re-checked on the refined twelve, with what was found

- **Walls.** No em dash · no exclamation · no question · no digit · no `which` (variant 2 face 1's relative is `that`) · no second person · no citation and no record holder named (**C-source**, arm A13) · no future indicative · no expletive opener (`There is` / `It is` at zero) · no figure and no inanimate intent (`favours`, `gives a defender nothing`, `breaks the ground`, `is no part of its defence` are the field's own measurement vocabulary, the sibling key being `terrain FAVOURABLE to the defender`, and the shipped sibling pool already carries *favours* and *hard going for an attacker*).
- **Slots.** `{settlement}` once in every wording, in no wording's opening position (T-F8); the slot set is identical across each variant's four wordings and is exactly **C-bag**'s filled bag. No `{band}`, `{route}` or `{counterpart}` is written.
- **Claim equality (arm A6 / C-pair).** Every wording of a variant carries that variant's claim set and no more; the sets are the BEFORE's. Nothing added, nothing spent. The two refused claims (c3, c4) are carried, not trimmed (§22 (e): refinement is a one-for-one substitution).
- **A11 / spread.** No two of the twelve share their first two words: `Open ground` · `The land` · `Flat country` · `Nothing in` · `The country` · `Set on` · `The lie` · `The plain` · `No hill` · `The town` · `Neither hill` · `Nothing rises`. Lengths 14/19/13/16 · 25/20/24/21 · 26/25/22/23, counting `{settlement}` as one word; mean 20.67, population sd 4.21, sample sd 4.40. **Declared:** the draft measured 4.82 / 5.04 and the refinement lowered it to 4.21 / 4.40. R-DA-05's within-pool floor of 4.0 is still met, so no failing state is added (§21.2); the fall is reported because the length followed the ear and the density law, not the figure (§21.1: position inside a band is information, never a target).
- **Tics, counted before and after.** `level` 4 → 1 · `about {settlement}` 3 → 1 · fronted openers 3 → 1 · fronted-object constructions 2 → 1 (variant 3 plain, the BEFORE's own) · closes on *still* 2 → 1 · variant-1 closes on *the town* 3 → 2 · tricolons 0 → 0 · antithesis shapes 1 → 1.
- **Close kinds varied** (R-DA-04): a place condition (*standing here*) · an absence-shaped condition (*made and not found*) · an object-anchored condition (*where the town does*) · an extent (*first to last*) · a continuing state (*goes on buying*) · an abstract noun (*the keeping*, one instance, reported) · an act (*has not put it down*) · a continuing state (*paying still*) · a standing condition (*by standing on it*) · a name given (*{settlement}*) · an act (*owns to it*) · a negation (*no higher*). Pronoun closers: zero.
- **THE THREAD** (§1.4.1). Every two-sentence wording carries a noun or its deictic forward: ground → *standing here* · cover → *what cover* · {settlement} → *its defence* · the site → *its defence* · its defence → *every advantage the town holds* · {settlement} → *the town … its defence* · {settlement} → *its defence* · hill and narrows → *the town says as much* · the town → *the town itself* · {settlement} → *its hold* · {settlement} → *the town keeps its place*. Variant 2 face 1 is a single sentence and takes no thread. In variant 3 faces 2 and 3 the change of subject to the town's own account is placed LAST, as the passage's one turn outward. Each wording is a spine and hands a civic noun forward for a modifier to pick up: the ground, the land, the country, the site, the plain, the defence, the charge, the town, the hold.
- **Siblings.** Nothing restates or contradicts `terrain FAVOURABLE to the defender` (its *account*, *reckoning*, *counted into*, *contends with*, *the way in*, *rough country* are all avoided; *favours* is kept as the shared band word this pool negates), the four `readiness` pools (no band word, no thinness, no adequacy claimed; the draft's collision with ADEQUATE's *does not claim more than that* is cured) or `strategic value HIGH`/`LOW`. No wording asserts a wall, ditch, road, watch or garrison.
- **Density** (§21.4). Three compressed lines were kept exactly as the drafter set them because they land when read: *Its defence begins where the town does* · *made and not found* · *Nothing but the town itself holds {settlement}*. No line was made plainer with no law behind the change.
- **Refusals.** R-1 (c3, the standing and continuing cost, all four wordings of variant 2) and R-2 (c4, the town's own account, all four wordings of variant 3) stand exactly as the draft banked them; the refinement changed their wording, never their standing. The draft's third row — c2 declared and not refused — also stands: the faces keep the BEFORE's universal quantification, so the claim stays vacuous where the town has nothing.
