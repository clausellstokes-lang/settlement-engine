# DS-DEF-1 · pool `readiness STRONG` · REFINEMENT round (Phase 2, arm A)
Seat: Opus 5 — Fable-unvalidated, REFINER. A different author from the drafter, on purpose (Part B §21 phase 2; §23 arm A).
Input: `draft-round-3.md`, lawful at the draft commit. Four existing variants, four wordings each (the variant's own plain line plus three `[face]` sub-rows), refined ONE FOR ONE: none added, none removed, none merged, none reordered, no claim added and none dropped. The typed lines of the pool and the bracketed angle tags are untouched and are not repeated.

Targets, in the order they were applied (§21.1 the ceiling not the middle; §21.2 every set; §21.3 one effort; §21.4 the density law): the sharpest licensed fact · the strongest rhythm inside the voice · the widest sibling distance · zero tics · the clerk's read-aloud ear. Nothing here is made plainer for plainness; every change carries a law, named per face under NOTES.

**readiness `STRONG`**

1. `[visitor]` A traveller coming in by road meets a defended place, and the approaches to {settlement} are watched. The town's works are kept up and nothing in the arrangement is patched together.
   - `[face]` The approaches are watched and the works are in repair. Nothing at {settlement} is hurried work, and the town reads as defended from the road.
   - `[face]` No part of the defence at {settlement} is makeshift, and the approaches are under watch. A stranger coming in by road reads a held town and works kept whole.
   - `[face]` The road gives a stranger the look of a defended place at {settlement}, and none of it is hasty work. The works are sound, and watch is kept on the approaches.
2. `[ledger]` What {settlement} can field and what it has built are both in good order. A threat made good against the town would take real effort, and the town knows as much.
   - `[face]` The force {settlement} fields is in trim, and so is the fabric it has raised. An attempt on the town would be dear work, and nobody in it thinks otherwise.
   - `[face]` In what {settlement} can field and in what it has built, the place is well found. An assault on it would be no light undertaking, and the town understands the cost.
   - `[face]` The fielded strength and the built defence at {settlement} stand equally sound. Any move against the town would be hard bought, and the town carries that in its reckoning.
3. `[street]` The arrangements are in place and nothing else is doing the work. Attack is not much on the town's mind at {settlement}, and complacency is not the reason.
   - `[face]` Nobody at {settlement} spends much of the day expecting an attack, and complacency has no part in it. The arrangements stand, and the town relies on nothing else.
   - `[face]` The thought of attack comes up seldom at {settlement}, and not for want of care. What the town has arranged holds, and nothing else is set in its place.
   - `[face]` Little of the day at {settlement} goes on the prospect of attack, and that is not ease of mind. The town leans on no substitute, and the arrangements are in order.
4. `[counterforce]` At present nothing presses {settlement}, and the quiet is not luck. A town set this well is not worth the price of an attempt.
   - `[face]` Chance has no share in the quiet at {settlement}, and nothing is pressing the town at present. A town in this order does not repay the cost of a try.
   - `[face]` Quiet holds at {settlement}. None of it is luck, and an attempt on a town in this condition costs more than it wins.
   - `[face]` Nothing stands against {settlement}. A town arranged to this standard is not worth the outlay, and the quiet owes nothing to accident.

--- NOTES

**Variant 1 `[visitor]`, wording 0 (the plain line).** `The works are kept up` became `The town's works are kept up`, and its `, and` joint became a bare `and`. Two laws: THE THREAD (MOVE-GRAMMAR §1.4.1) — the second sentence took a new subject and handed nothing back, and now carries the spine's own noun forward; and fault 5 / R-DA-05 — the wording used the identical `, and` joint twice, a metronome inside one unit, so the second joint is now a different one. No claim moves: the works are the town's by the parent's own reading.

**Variant 1, face 1.** `has been thrown up in a hurry` became `is hurried work`. The law is R-DST-B under MOVE-GRAMMAR §1.1 and R-DA-19: a STANDING configuration field licenses a structural clause and never a historical one, and a present-perfect passive about an act of building is a historical clause under a band word that holds no provenance. The claim (nothing is makeshift) is unchanged, the sentence is four words shorter, and the second sentence now carries `work` forward from `the works are in repair` before making its one turn outward, placed last (§1.4.1).

**Variant 1, face 2.** `works that are kept` became `works kept whole`. R-DA-04 and the density law (§21.4): the relative clause was a limp filler at the close, where the register wants a standing fact landing on a civic thing; `kept whole` is the same claim in one fewer word with a harder landing, and it gives this face its own vocabulary for the upkeep claim against `kept up`, `in repair` and `sound` in its three siblings, so no two of the four are paraphrases.

**Variant 1, face 3.** `The road offers a stranger` became `The road gives a stranger`, and the second sentence was turned to read `The works are sound, and watch is kept on the approaches`. Two laws: R-DA-11 (nothing inanimate acts with intent) — a road that OFFERS is an intent verb on an inanimate subject, where a road that GIVES is not; and THE THREAD plus fault 5 — the sentence now opens on `The works`, carrying `work` forward from `hasty work`, and its verbatim `The approaches are watched` (which stood in three of the four wordings) is replaced by `watch is kept on the approaches`, so the pool no longer repeats one formula three times.

**Variant 2 `[ledger]`, wording 0 (the plain line).** `A threat against the town would take real effort` became `A threat made good against the town would take real effort`. The sharper licensed fact (§21.1): it is not the threat that takes effort but its execution, and `made good` states that in two words without touching the subjunctive edge, so A2, R-DA-07 and STATE-NEVER-FATE hold exactly as at round 3 and the participial opener that round 3 cured is not re-derived (draft R8). Density is added, not spent (§21.4).

**Variant 2, face 1.** `is in good order` became `is in trim`. Arm A6 sibling distance: the face carried its parent's own phrase, so two of the four wordings said the upkeep claim in the same words, which the four-faces law forbids; `in trim` is a different vocabulary for the identical claim, and the four realisations of the upkeep claim now read `in good order`, `in trim`, `well found` and `equally sound`, no one of them a paraphrase of another.

**Variant 2, face 2.** `In what it can field and in what it has built, {settlement} is sound` became `In what {settlement} can field and in what it has built, the place is well found`. Two laws: the clerk's ear — the fronted phrase put `it` ahead of the noun it stands for, and a record does not make its reader wait for the antecedent; and arm A6 sibling distance — `sound` also carried face 3's claim, and `well found` gives this face its own vocabulary. The slot moves inside the fronted phrase, so the wording still does not OPEN on a `proper`-typed slot (ARCH §2.5, T-F8).

**Variant 2, face 3.** `would cost real effort` became `would be hard bought`. Arm A6 again: `real effort` was the plain line's own phrase, so the family carried one wording twice; `hard bought` is the ledger's idiom for the same subjunctive cost, keeps the edge subjunctive, and closes the sentence harder before the reckoning clause. The density law is the warrant for the compression rather than a longer paraphrase.

**Variant 3 `[street]`, wording 0 (the plain line).** The two sentences are exchanged, so the wording now opens on the arrangements and closes on the town's mind, and the first joint became a bare `and`. THE THREAD is the law: the parent's second sentence changed subject in the middle of the passage and handed nothing back, and the exchanged order makes the change of subject the passage's one turn outward, placed last (§1.4.1). Word order is spendable under B-CLAIM (MOVE-GRAMMAR §3.4); the claim set, its order in the licence map and the angle are untouched, and R-DA-17 gains a second opening move in this pool.

**Variant 3, face 1.** `the town relies on nothing else instead` became `the town relies on nothing else`. `instead` was carrying nothing: the claim is the absence of a substitute, and the adverb only restates the noun it follows. R-DA-04's close (an ABSENCE is one of the five licensed close kinds) lands cleanly on `nothing else` once the filler is gone, and the sentence drops to ten words against its eighteen-word neighbour.

**Variant 3, face 2.** `What the town has arranged is standing` became `What the town has arranged holds`. The read-aloud ear and R-DA-18 (plain is not flat): the nominalisation was already heavy, and a progressive copula on top of it made the sentence sag; `holds` is one hard verb for the same standing fact and it is this face's own word against `are in place`, `stand` and `are in order` in the three siblings.

**Variant 3, face 3.** `its arrangements are in order` became `the arrangements are in order`. R-DA-22 (one term for one thing) and the clerk's ear: the sentence already opened on `The town`, so the possessive stacked a second reference to the same subject inside twelve words where the definite article carries it. Nothing else in this face is touched; it was the family's strongest rhythm at the draft (the claims arrive in the order absence-then-condition, which none of its siblings uses) and the refinement protects that rather than levelling it.

**Variant 4 `[counterforce]`, wording 0 (the plain line).** Kept as drafted, one word tested and refused: `presses` was measured against `bears on` and `stands against`, and the plain line keeps `presses` because face 3 now owns `stands against` and the plain line is the family's shortest close (`the price of an attempt`) — moving it would have narrowed sibling distance for no law. Recorded so the test is not re-run (§21.3: no second pass; a refusal is banked with its reason).

**Variant 4, face 1.** `A place set this well is not worth the cost of a try` became `A town in this order does not repay the cost of a try`. Arm A6: `set this well` was the plain line's own phrase and `not worth the ... of a ...` its own frame, so this face was a paraphrase of its sibling rather than a face; the new wording changes both the noun phrase and the verb frame (`does not repay` against `is not worth`) while asserting the identical cost claim, and it drops `place`, which face 2 of variant 2 now uses.

**Variant 4, face 2.** `costs more than it returns` became `costs more than it wins`. The sibling fence (arm A1 across this block's pools): `returns` reads on the town's VALUE, which is `strategic value HIGH` / `LOW`'s own fact and never this pool's, and the draft's own note fenced this variant to the cost of an attempt and away from the worth of the town; `wins` states the same cost claim without touching the neighbouring pool's field. The four-word opening sentence is left exactly as drafted — it is the pool's short line and carries the register's `< 8 words` floor (R-DA-05).

**Variant 4, face 3.** `Nothing stands against {settlement} at present.` became `Nothing stands against {settlement}.` R-DA-05's rhythm floor and fault 5: `at present` stood in three of this family's four wordings, which is a tic across a pool that a refuter names, and the present tense already carries the time claim, so nothing is dropped from the claim set. The cut takes the sentence from six words to four against an eighteen-word neighbour, which is the widest gap any wording in this pool carries and the largest single rhythm change in the round.

## Declared consequences of the refinement (not refusals)

- **Every refusal of `draft-round-3.md` §B stands, unchanged and uncured.** R1 (the card licenses no claim at all: `reads` empty, `predicate` none recovered, `may claim: nothing`, so the whole pool is banked as a refusal set with its faces, never trimmed), R2 (the card's `angle` line against its `may NOT: a standpoint` line), R3 (a denied cause is still a cause), R4 and R4b (more than one fact per unit; the knowledge claim as a FEELING non-move), R5 and R5b (the block's PROVENANCE FENCE; the threat-environment claim), R6 (the generalisation over towns of this band). None is a writer's to cure and no refinement above touches one. The claims are the shipped parent's throughout.
- **No citation.** The card's `source: (none) · standing SOURCE-UNRESOLVED` and its `NO citation is licensed` line are obeyed at zero across all sixteen wordings; no roll, book, keeper, office or authority is named, so MOVE-GRAMMAR §4.4.3's PROVENANCE move is not made and Part B §24's ceiling of one per unit is met at zero, which that section records as the exemplars' own rate.
- **Walls held in every wording, re-checked after every edit:** no em dash, no exclamation, no question, no digit or percent, no `which`-clause, no colon, no semicolon, no first or second person, no future indicative, no figure or simile, no inanimate intent, no named character, no theological claim, no count, no season, no totality over persons, no exemption; at most two sentences; `{settlement}` present in every wording and no other slot in any of them; no wording opens on the slot.
- **Participial openers: still zero.** All thirty-two sentence openers are a determiner, a noun, a pronoun, a quantifier, a preposition or a wh-word. The four openers this round created or moved are `The town's`, `The works`, `The arrangements` and `Nothing stands`; none is an `-ing` or `-ed` form, so round 3's cured band depth is not re-opened.
- **A11 spread: sixteen distinct first-two-word pairs, re-derived after the round-3 exchange in variant 3.** *A traveller · The approaches · No part · The road · What {settlement} · The force · In what · The fielded · The arrangements · Nobody at · The thought · Little of · At present · Chance has · Quiet holds · Nothing stands.* Variant 3's plain line changed its pair from `Attack is` to `The arrangements`; the pair is not used by any other wording.
- **Rhythm, measured on this packet against the draft, and stated at its true size.** Sentence-length standard deviation over the thirty-two sentences 3.63 → 3.67; the mean gap between a wording's two sentences 5.19 → 5.12; two sentences under eight words in both (`Quiet holds at {settlement}.` at four, and `Nothing stands against {settlement}.` now at four where it was six). **The spread is therefore materially UNCHANGED by this round, not improved:** the round's gains are in the joints, the thread, the sibling distance and the sharper fact, and no rhythm claim beyond that is made. No wording is a tricolon: every sentence still carries at most two coordinated segments, so the 3+-segment count for this packet stays at zero.
- **Joints.** The `, and` joint falls from twenty-three to twenty-one and the bare `and` between clauses rises from five to seven, so no wording now carries the same joint twice (variant 1's plain line and variant 3's plain line are the two that changed; variant 1 face 1 already had one). An inverted `nor` clause was drafted and REFUSED: the register card bars costume in the syntax, and an antique inversion is exactly that, so the joint stayed plain.
- **Close kinds vary across the pool** (R-DA-04): a quality of the arrangement · a condition read from the road · an object kept whole · the approaches under watch · a knowledge of the cost · a denial · the cost · a reckoning · a denied reason · an absence of substitute · a thing set in another's place · a condition of the arrangements · an object priced · an outlay · a denial of accident. No wording closes on a pronoun of the `it` family.
- **The thread.** Every wording's second sentence now either carries a noun forward from the first (the town, the works, the work, the quiet, the place) or makes its change of subject the passage's one turn outward and places it last (variant 1 face 1 and face 2, variant 3's plain line, and variant 4's three generalising closes). Every wording still ends on a civic noun or a present condition of one, so a modifier seated after this spine has a noun to pick up: *arrangement · road · works · approaches · effort · cost · reckoning · work · substitute · order · attempt · try · accident.*
- **Density (§21.4).** No edit in this round trades density for plainness. Three edits ADD compression (`is hurried work`, `works kept whole`, `would be hard bought`), four remove a word that carried nothing (`instead`, `its`, `at present`, `that are`), and two exchange a word for a sharper one of the same length class (`gives`, `wins`). The one addition of length, `made good`, buys a sharper fact.
- **Reversion condition.** Per §21.2 this set was unlawful at the draft on R1 and stays unlawful on R1 alone, which no wording can cure; the failure count is unchanged and no new failing state is introduced, so the refinement is kept under the same clause. If the gate reports any new failing measure, the reverting unit is the single face named in its row, never the pool.
