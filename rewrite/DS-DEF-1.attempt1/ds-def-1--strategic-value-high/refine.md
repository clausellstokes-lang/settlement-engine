Seat: Opus 5 — Fable-unvalidated. Block `DS-DEF-1`, pool key `strategic value HIGH`, REWRITE **arm A refinement** (Opus draft → Opus refiner, a different author than the drafter; §23).
Refines `draft-round-3.md` one wording for one wording: three variants, same vids, same order, same bracketed angle tags, none added, none removed, none merged (§22). Each variant is a numbered `[plain]` wording plus three `- [face]` sub-rows — four wordings per semantic variant, each a different vocabulary or rhythm inside the voice, none a paraphrase of its sibling; an unweighted seeded roll picks one at render, so every wording stands alone. The typed lines of the pool (ROLE, READS, RELATION, ATTACH, FORM, MOVE, the angle tag) are untouched and unrepeated.
One effort, no second pass (§21.3). Where sharper and licensed conflicted, licensed won (§21.3). Claims are identical to round 3's, wording for wording; none added, none dropped (A6, §22 (e)).

**THE ROUND'S ONE STRUCTURAL LIFT — THE THREE `[plain]` ROWS WERE STILL THE PRODUCT'S OWN SENTENCES.** Round 3 rewrote nine faces and left all three numbered rows byte-identical to the shipped text at `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`, on the reasoning that the baseline should stay recognisable. Under the draw that ships, the numbered row is not a baseline: it is one of four wordings an unweighted roll picks, so one read in four was getting un-rewritten prose while the packet reported a rewritten pool. All three are rewritten here, and with them go the pool's two worst survivals — variant 2's `runs underneath`, an abstraction moving through space (R-DA-11's figure floor, the plainest law this pool was breaking), and variant 1's `defensive problem`, the analyst's abstraction that face 2 was echoing word for word.

**What else this round moves, all of it inside the gate's cleared measures.** Zero pronoun closers, down from two (`it` at variant 1 face 1 — round 3 named it a Phase 2 target and this is Phase 2 — and `another's` at variant 1 face 3): R-DA-04's `closers.pronounRate`. Zero participial or gerund openers, held. Zero colons, zero semicolons, zero `which`, zero digits, zero em dashes, zero settlement-openers, held. Twelve distinct first-two-word openers, held. The three template repetitions round 3 left standing are gone: `the same stranger` three times in variant 3 (now once), `is enough for a stranger to see why` twice inside one wording, and `a great deal of what the town decides` shared between variant 2's plain row and its third face. Word-count spread widens from 24–35 to 20–32 with the sd held (≈ 3.4 to ≈ 3.5 on the hand count below), the licensed vocabulary being the ceiling on how far a two-sentence unit carrying three claims can stretch either way.

---

## THE ROWS (replacing rows 1–3 under the pool's existing bold line)

**strategic value HIGH**

1. `[ledger]` What {settlement} holds is worth taking, and the town's defence turns on that one fact. The town must be worth more to keep than it is to seize.
   - `[face]` A prize stands at {settlement}, and the whole defence of the place is that reckoning. It must come out for keeping over taking.
   - `[face]` Something at {settlement} is worth the taking, and the town's defence weighs nothing else. Its worth to the town must stand above its worth as a spoil.
   - `[face]` Another would want what {settlement} has, and the town's defence answers to that alone. In its own hands the town must count for more than it would count for in a taker's.
2. `[street]` The town is aware of what it sits on, and aware that others are aware. Very little it does stands clear of that awareness.
   - `[face]` The value of the place is common knowledge here, and the town knows it is common knowledge abroad. Little the town settles stands apart from that value.
   - `[face]` Nothing about what the place is worth is lost on the town, and nothing about the notice that worth draws from outside. Neither of those is absent from the town's dealings.
   - `[face]` The measure of the place is taken here, and the town knows it is taken elsewhere as well. By that measure a great part of the town's business is settled.
3. `[visitor]` A stranger takes a day to see why anybody would want {settlement}, and not much longer to see why the town is careful whom it offends.
   - `[face]` A day at {settlement} is enough to show a stranger why the place is wanted. Why the town keeps its quarrels few takes the same stranger a little longer.
   - `[face]` Inside a day a stranger sees what draws others to {settlement}. The town's care over whom it offends follows soon after.
   - `[face]` Why anybody would want {settlement} is plain to a stranger by the end of a day. Not long after, so is the care the town takes over giving offence.

---

--- NOTES

### One line per face: what changed, and the law or target behind it

**1 `[ledger]` `[plain]`** — the shipped row rewritten at last: `and that is the whole of the town's defensive problem` → `and the town's defence turns on that one fact`, striking the abstraction `defensive problem` and the empty frame `that is the whole of`. R-DA-10 (the working sentence over the abstract noun) and R-DA-04's `abstractNounRate` guard; it also frees `weighs nothing else` for face b, which round 3 had echoing this row's exact phrase.

**1 `[ledger]` `[face]` a** — the close moved off the pronoun: `It must come out for the town and against whoever would take it` → `It must come out for keeping over taking`, and the muddy antecedent chain repaired by naming `the place` in sentence one's own clause. R-DA-04's pronoun-closer ceiling (the pool now carries zero, down from two) and §1.4.1's thread (`prize` → `the place` / `that reckoning` → `It`); the density law keeps the compressed `come out for keeping over taking`, which is the ledger's own idiom and is not to be loosened.

**1 `[ledger]` `[face]` b** — `and there the town's whole defensive problem lies` → `and the town's defence weighs nothing else`: removes the pool's only inversion tic and the verbatim echo of the numbered row (the paraphrase bar; A11 sibling distance), and puts a measurement verb where an abstraction was lying in a fronted `there` — R-DA-11 licenses a comparison as a measurement in words, which is the one figure-shaped move this card can carry.

**1 `[ledger]` `[face]` c** — two cures in one wording: `has no other question in it` → `answers to that alone` (face b already owned the `nothing else` frame, and the plain row owns `no other`), and the close `than in another's` → `than it would count for in a taker's`, with `In its own hands` fronted. Removes the pool's second pronoun closer and the doubled `another … another's` (R-DA-22, R-DA-04); the doubled `count for` is this face's deliberate balance and its own rhythm, shared with no sibling.

**2 `[street]` `[plain]`** — the figure struck: `That awareness runs underneath a great deal of what it decides` → `Very little it does stands clear of that awareness`. R-DA-11's floor — an abstraction moving under the town's decisions is exactly the class that rule forbids, and it was still shipping in the row round 3 declined to touch; `aware … aware … that awareness` now carries the thread on a real noun (§1.4.1), and the sentence is nine words shorter with the same claim.

**2 `[street]` `[face]` a** — `the town holds it known abroad as well` → `the town knows it is common knowledge abroad`: the contorted `holds it known` replaced by the register's plainest verb, and `common knowledge here … common knowledge abroad` set as one term for one thing across the two limbs (R-DA-22 — the variation is meant to live in the syntax, not in a thesaurus). The compression `Little the town settles stands apart from that value` is round 3's and is kept unloosened under §21.4.

**2 `[street]` `[face]` b** — rebuilt as the pool's long line (31 words, against a 20-word floor at variant 3): `nor is the attention it draws from outside` → `and nothing about the notice that worth draws from outside`, the anaphora carried in full, and `Much of what the town chooses is chosen with both in view` → `Neither of those is absent from the town's dealings`. Cures a sight-figure on two abstractions (R-DA-11) and the `chooses/chosen` polyptoton, gives R-DA-05 its spread back at the top of the range, and lands the one civic-object close variant 2 can license.

**2 `[street]` `[face]` c** — the closing sentence rebuilt from `A great deal of what the town decides rests on that knowledge`, which was the numbered row's own phrase reworded, to `By that measure a great part of the town's business is settled`: the paraphrase bar and A11 sibling distance, with `measure` carried forward from sentence one so the fronted phrase hands a noun back (§1.4.1) instead of pointing at an unnamed `that knowledge`.

**3 `[visitor]` `[plain]`** — the shipped row tightened: `understands within a day` → `takes a day to see`, `shortly afterwards` → `not much longer to see`, `so careful about who it offends` → `careful whom it offends`. R-DA-18 (the plain register; the intensifier `so` is the loosest word in the pool) and R-DA-22 on the relative form; the single-sentence shape is kept deliberately, because it is the only one-sentence wording in twelve and R-DA-05's spread is measured on shape as well as length.

**3 `[visitor]` `[face]` a** — the two-beat template broken: round 3 ran `is enough for a stranger to see why …` and then `is enough for the same stranger to see why …`, one frame twice inside one wording. Now `A day … is enough to show a stranger why the place is wanted` against `Why the town keeps its quarrels few takes the same stranger a little longer` — two different constructions, and `the same stranger` kept here and struck from the other two wordings so the phrase is the pool's once rather than three times (§21.1's zero tics; R-DA-05's `runsOfThree`).

**3 `[visitor]` `[face]` b** — `Soon after, the same stranger sees why the town is sparing with its offence` → `The town's care over whom it offends follows soon after`, which drops the second `sees` of a `sees … sees` pair and the pool's third `the same stranger`, and makes this the shortest wording in twelve at twenty words. R-DA-05 (the short line exists, and the pool needed one at the floor); the subject stays the town's own conduct, so the second sentence is the passage's one turn outward and sits last (§1.4.1).

**3 `[visitor]` `[face]` c** — the parallel `Why X is plain to a stranger … Why Y is plain to the same stranger …` replaced by an ellipsis: `Not long after, so is the care the town takes over giving offence`. The template was the most mechanical thing in the packet; the ellipsis carries `plain` forward without restating it, which is both the thread (§1.4.1) and the density law's own case — the line lands shorter and is not made plainer.

### What did NOT move, and why that is a decision

- **The seven banked refusals (R1 through R7) stand exactly as round 3 banked them, none cured and none added.** Every one is a card matter or a claim matter above a writer's pen: the card licenses one standing fact and each variant carries three (R1); `angle:` and `may NOT:` contradict each other on the standpoint (R2); the printed predicate is narrower than `TERRAIN_PRIZE_OF`'s two keys, so no wording here names a terrain feature and none may (R3); the causal claim is the parents' (R4); variant 3's duration is the parents' and is carried identically across its four wordings so they stay claim-equal (R5); variant 2's whole content is the FEELING non-move (R6); variant 1's closing proposition engages R-DA-12's generalisation test, whose walker baseline has not run (R7). **This packet does not assert the pool is lawful.**
- **No citation, no record holder, no source.** The card prints `source: (none) · standing SOURCE-UNRESOLVED`, so arm A13 has nothing to fire on and §24's ceiling of one citation per unit is met at zero. No wording names a roll, a book, a keeper or an authority.
- **The block's composition fence, held.** No wording claims the ground helps or hinders (the terrain lens), what the town can field or has built (the readiness lens), or that anyone is pressing the town (the `counterforce` variants). `ground`, `country`, `site`, `siting`, `hill`, `narrows`, `walls`, `perimeter`, `muster` and `watch` appear in no wording.
- **Variant 2 stays slotless** — its parent carries no `{settlement}`, so none of its four wordings does (ARCH §2.5's slot-set rule). Variants 1 and 3 carry `{settlement}` in all four.
- **Walls held in every wording:** no em dash, no exclamation, no question, no digit or percent, no `which`-clause, no colon, no semicolon, no first or second person, no future indicative, no citation, no simile, no named character, no theological claim, no exemption, no totality over persons (`every`, `everyone`, `all`, `nobody`, `no one` appear nowhere — `every part of` was drafted and struck for this reason), at most two sentences, and the parent's slot set exactly.

### The measured shape of the twelve wordings

| vid | wording | words | sentences | opener | close |
|---|---|---|---|---|---|
| 1 | `[plain]` | 28 | 2 | `What {settlement}` | seize |
| 1 | face a | 23 | 2 | `A prize` | taking |
| 1 | face b | 27 | 2 | `Something at` | spoil |
| 1 | face c | 32 | 2 | `Another would` | a taker's |
| 2 | `[plain]` | 24 | 2 | `The town` | awareness |
| 2 | face a | 27 | 2 | `The value` | value |
| 2 | face b | 31 | 2 | `Nothing about` | dealings |
| 2 | face c | 30 | 2 | `The measure` | settled |
| 3 | `[plain]` | 26 | 1 | `A stranger` | offends |
| 3 | face a | 29 | 2 | `A day` | longer |
| 3 | face b | 21 | 2 | `Inside a` | after |
| 3 | face c | 29 | 2 | `Why anybody` | offence |

Counted by splitting on whitespace, a `{slot}` marker counting as one word. Twelve distinct first-two-word openers; zero participial or gerund openers; zero settlement-openers; zero pronoun closers. Close kinds across the pool: two nominal objects, one person-role, one object, two abstractions (variant 2 can license no civic object and is held to two), two conditions, two acts, two times. Range 21 to 32 against round 3's 24 to 35; the pool's sd is held, marginally up, and the position inside the band is reported as information only (§21.1).
