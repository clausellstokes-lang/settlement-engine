# REFUTE — DS-DEF-1 · `readiness CRITICAL` (Fable refuter, 2026-09-09)

**What stands.** `kept.md` holds the three OLD shipped rows, single-tagged, with no `[face]` sub-rows. CONFIRMED against the dock: the annex at the refine tip (8236588ed, `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2565-2568`) carries those three rows byte-identical to the pre-rewrite annex (`8236588ed~2`, `:2553-2556`); draft round 1 (b47d05cfd) refused the pool on the second bracketed `[plain]` tag, rounds 2 and 3 applied no packet for it, and car 8b's refinement was refused on the same tag. No face of this pool has ever passed the gate, and the annex carries no refusal row for it. So the only faces to read are face 0 of each variant; faces 1 to 3 do not exist.

**The card.** Printed by `node scripts/prose-licence-card.mjs DS-DEF-1 'readiness CRITICAL'`: `may claim: nothing: the census recovered no reading, so no claim is licensed`. Under the rule that binds me (every claim licensed by the card and by nothing else) every standing face fails on that line alone. The card's ground is contradicted by the code I read: `posturePoolKey` (`src/domain/display/stateProse/defenseStateProse.js:676-680`) interpolates `readiness ${scoreBand(score)}` and is bound at `:752`; `scoreBand` (`defenseScoreBands.js:38-39`) returns CRITICAL below twenty. That is the chair's to rule; I report it and do not lift the finding.

**A state the generator reaches (CONFIRMED by the formula, not executed on a fixture).** `computeDefenseReadiness` (`src/generators/defenseGenerator.js:488-522`) takes the rounded MEAN of the five arms plus a tier bonus (0 to 12) minus a threat penalty. Military 65 with the other four arms at 0 gives 13, CRITICAL at every tier. So a CRITICAL header can stand on the same Defense tab as walls (`DS-DEF-11 WALLED-*`) and a STRONG military badge (`DS-DEF-10`). The block's own PROVENANCE FENCE says the readiness word asserts nothing about any one arm.

## Verdicts

| variant | face | verdict | law | quote | finding |
|---|---|---|---|---|---|
| 1 `[ledger]` | 0 | FAIL | the licence card (`may claim: nothing`); the PROVENANCE FENCE + CLERK-LAWS §1.3 LACK row; C7 | "There is no arrangement here that would slow a serious attempt" | An unlicensed LACK: "no arrangement ... would slow" asserts an absence across every arm, which the band (a mean) does not license and which contradicts a same-tab walls sentence and a STRONG military badge in the reachable state above. Reported, not failed: the second sentence opens on the expletive (R-DA-07 band) and "effectively" spends the one hedge R-DA-13 allows. |
| 1 | 1–3 | FAIL | four faces per semantic variant (owner rule; ARCH §2.5 `[face]` rows); never-trim (b) | (no face in the annex) | No face exists to read; none passed the gate in any round; no refusal row stands. |
| 2 `[street]` | 0 | FAIL | the licence card; FEELING is a non-move (MOVE-GRAMMAR §1.3; R-DA-13 belief-frame floor); R-DA-11; C3/R-DA-07 | "The town knows perfectly well what it could not survive" | A belief frame with no field behind it, with an intensifier ("perfectly well") asserting a degree of knowledge no field holds; "the knowledge shapes" gives an abstraction intent (R-DA-11); "what it will and will not provoke" forecasts conduct on a standing fact (defensible as habitual will under MOVE-GRAMMAR §3.4, so the weakest of the four). |
| 2 | 1–3 | FAIL | four faces; never-trim (b) | (no face in the annex) | As variant 1. |
| 3 `[visitor]` | 0 | FAIL | REFUSED COLUMNS, always: a totality over persons (the card); CLERK-LAWS §1.3 quantifier row; R-DA-15 row 6; the PROVENANCE FENCE | "without being stopped, challenged or counted by anybody at all" | "by anybody at all" quantifies over persons, open on every settlement; "counted" is an institution negative with no `whatItDoesNotDo` row; "stopped, challenged" implies no watch and no gate, a per-arm claim the fence refuses and DS-DEF-3 owns. Reported, not failed: the one triad in the pool (the register card's "three of anything by habit"). |
| 3 | 1–3 | FAIL | four faces; never-trim (b) | (no face in the annex) | As variant 1. |

## Read-aloud (information only)
- V1: the four-word opener then a twenty-six-word second sentence lands well aloud; the ear hears "walk in" as the close.
- V2: "knows perfectly well" reads as speech, not record; the ear takes "will and will not provoke" as a spoken emphasis.
- V3: the triad reads cleanly; "by anybody at all" is heard as emphasis, which is exactly the tell.
