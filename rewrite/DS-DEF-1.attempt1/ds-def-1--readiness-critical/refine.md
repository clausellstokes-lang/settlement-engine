1. `[ledger]` `[plain]` {settlement} is undefended. What would delay an attacker here is the walk to the centre and nothing besides.
   - `[face]` Whatever came against {settlement} would meet nothing but the distance to be crossed.
   - `[face]` Undefended is the word the record has for {settlement}. A force in earnest would be held by the time it takes to come in and by no arrangement the town keeps.
   - `[face]` Between the edge of {settlement} and its middle, the only cost to an enemy is the walking. The town has no answer to make.
2. `[street]` `[plain]` The town provokes nothing that could end it, and leaves a great deal alone.
   - `[face]` What would finish the town is the bound on what it takes up. The rest it does not touch.
   - `[face]` A quarrel is not started here, and one already going stops well short of what the town could not survive.
   - `[face]` Nothing the town deals in comes near what it could not survive, and it keeps clear of what could.
3. `[visitor]` `[plain]` A stranger comes as far as the centre of {settlement} and is not stopped, challenged or counted.
   - `[face]` A stranger walks into the centre of {settlement} and is asked nothing.
   - `[face]` Arrival at {settlement} goes unmarked. A stranger is standing in the centre of the town before a question is put.
   - `[face]` Nothing at {settlement} comes between a stranger and its centre, and nothing is asked.

--- NOTES

**Variant 1 `[ledger]`**
- face 0 — cut the hedge "effectively" from the band statement and re-cut the measure to close on an absence ("and nothing besides") instead of a possessive tail. R-DA-13's vague-hedge floor is EXECUTABLE and stands: the band is CRITICAL, not "effectively" anything, and a hedge with no distance field behind it is the register's own tell. The plain line is canonical-at-zero, so it takes the flattest form in the set.
- face 1 — replaced the two-sentence "amounts to nothing an attempt in force would have to work at / reaching the centre is the whole of the delay" with one thirteen-word line carrying both halves. Two targets: R-DA-05's short line, which this variant lacked entirely at 20 · 30 · 28 · 24 words, and §21.4 the density law — the compression is the ceiling, not a plainer restatement. "Whatever came against" also retires "an attempt", which collided head-on with the CO-FIRING sibling `strategic value LOW` ("Nothing at {settlement} would repay an attempt on the town"); posture and prize compose on one page, so that was a live sibling-distance fault, not a hypothetical one.
- face 2 — kept the draft's best device, the zeugma "held by the time it takes to come in and by no arrangement the town keeps", and rebuilt its first sentence as the record naming its own word ("the word the record has for"). AMENDMENT S3 and R-DA-08's in-the-record-never-in-the-world idiom license the office speaking of its own compilation; "the plain word for" read as self-regard rather than record. Also re-cut the measure phrase so "walk" is not the noun in three faces running.
- face 3 — reordered to measure-first with the band as the last standing fact, and dropped the fronted "For its defence" and the repeated "such an attempt" (the draft used "such an attempt" in two of four faces, which is the tic the refinement round exists to remove). Close kind is an absence on the civic subject, R-DA-04.

**Variant 2 `[street]`**
- face 0 — replaced the fronted abstract nominal "What the town could not survive shapes what it provokes" with an active clause on the town. "Shapes" gave an abstraction a shaping intent, which R-DA-11 refuses (nothing inanimate acts with intent); the claim survives whole in "provokes nothing that could end it, and leaves a great deal alone". Slot set stays empty, matching the parent, per ARCH §2.5's face refusal.
- face 1 — kept the measuring frame, cut the doubled "takes on", and gave the second half its own five-word sentence ("The rest it does not touch") so this face carries the variant's short-line rhythm. R-DA-05 rhythm-follows-load; R-DA-04 prohibition close, distinct in kind from the other three.
- face 2 — the draft's second sentence, "Both restraints are cut to what the town could not stand", is a summarising second sentence naming what the first sentence did. That is R-DA-03's 2nd-sentence summary at target 0.000 and best-ai's own signature under §16 (6). Rebuilt as one sentence whose second half adds the EDGE rather than glossing the first half.
- face 3 — replaced "is not a thing the town tests" (limp, and a near-repeat of face 1's frame) with "comes near", and retired the second "lets alone" so the pool does not close on one idiom twice. "Keeps clear of what could" is the elliptical close the density law protects; it is not made plainer.

**Variant 3 `[visitor]`**
- face 0 — kept the shipped triad (the pool's one triad, R-DA-10's ration) but moved it to the reaction point and made the stranger the subject of an active verb, so the line ends on the three negatives rather than opening on a bare arrival verb. The claim set is unchanged, including "counted", which the draft banked as a residual risk and which A6 forbids me to drop.
- face 1 — replaced the draft's "Unasked … and unasked" anaphora. The doubled adverb is a flourish in the SYNTAX, and the register card is explicit that the air lives in the noun and never in the syntax; a rhetorical figure is also the machine kind of variance under §16 (6). The plain twelve-word line carries the same fact.
- face 2 — cut "before the business of asking begins", which implies that asking does eventually begin and so asserts a challenging institution the card licenses nothing about (the annex's PROVENANCE FENCE, and the corrected card's `may NOT` on a second fact). "Before a question is put" is agentless and asserts only the arrival's depth. The thread holds: Arrival, then the stranger, then the town.
- face 3 — retired "Passage into {settlement} is not stopped" (two passives, no subject) for "Nothing at {settlement} comes between a stranger and its centre". Deliberately avoided the verb "stands", which the co-firing sibling `strategic value LOW` owns ("No prize stands at {settlement}"). Every quantification in this variant is agentless: no face contains "nobody", "no one", "anyone" or "all", because the card refuses a totality over persons always and CLERK-LAWS §1.3 licenses a quantifier only by `closed` on the quantified column, closed nowhere here.

**Walls re-checked on all twelve faces.** No em dash, no exclamation, no question, no digit or count word, no `which`-clause, no colon or semicolon, no citation (SOURCE-UNRESOLVED licenses none, and a face naming a holder is refused by arm A13), no future indicative (every edge subjunctive), no figure, no sense verb on an abstraction, no inanimate intent, no second-sentence summary, no verdict or moral close, no reader address, no per-arm claim about walls, garrison, militia, muster or watch. Slot sets are identical to each parent: {settlement} on variants 1 and 3, none on variant 2. Openers are distinct within every variant, and only variant 1's plain line opens on the settlement token (R-DA-17 wall 10). Word counts run 18 · 13 · 31 · 24 / 13 · 19 · 21 · 19 / 17 · 12 · 20 · 14, mean 18.4, sd 5.1, against R-DA-05's within-pool floor of 4.0.

**REFUSALS CARRIED FORWARD (the draft's, re-verified in the dock, unchanged by this round).**
1. **WHOLE POOL — the printed card says `may claim: nothing`.** Under the card as printed no lawful face of this pool exists, and these rows are written against the corrected licence the draft states. The correction is CONFIRMED by reading, not inferred: `src/domain/display/stateProse/defenseStateProse.js:676-680` is the template the card says does not exist (`posturePoolKey` returns the interpolated key, guarded by `CORPUS['DS-DEF-1'].pools[key]`), `:752` binds it on the `posture` lens, and `src/domain/display/defenseScoreBands.js:38-39` returns `CRITICAL` below twenty. The recovery gap is mechanical: the terrain lens recovers because `TERRAIN_DEFENCE_OF` is a module-level key table, and the four `readiness` pools do not because their key is interpolated from a band function's return. It hits all four readiness pools identically, it is the chair's to rule, and no refinement round can move it.
2. **Variant 2 — the belief frame "The town knows perfectly well" is carried in no face**, on the draft's two independent grounds (FEELING is a non-move, MOVE-GRAMMAR §1.3; and the co-firing sibling `strategic value LOW` already carries the knowing frame on its own `[street]` row). A refusal only if the chair rules the knowing to be a claim of the shipped row rather than the angle's realisation of it; it is not a face-level fix, since no face carries it.
3. **Variant 3 — "by anybody at all" is carried in no face**, a totality over persons the card refuses in the line that holds always. Its removal is required, not permitted, under R-DA-15 row 6.
4. Nothing is trimmed: three variants under their own numbers with their tags untouched, four wordings each, one-for-one against the draft.
