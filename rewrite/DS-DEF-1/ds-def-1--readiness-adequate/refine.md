Seat: Opus 5 — Fable-unvalidated. Block `DS-DEF-1`, pool key `readiness ADEQUATE`, REWRITE **arm A refinement** (Opus draft → Opus refiner, a different author than the drafter; §23).
Refines `draft-round-2.md` one wording for one wording: four variants, same vids, same order, same bracketed angle tags, none added, none removed, none merged (§22). Each variant is a numbered `[plain]` wording plus three `[face]` sub-rows — four wordings per semantic variant, each a different vocabulary or rhythm inside the voice, none a paraphrase of its sibling; an unweighted seeded roll picks one at render, so every wording stands alone.
One effort, no second pass (§21.3). Where sharper and licensed conflicted, licensed won.

**THE BLOCKING DEFECT, CURED IN THE ROW GRAMMAR (the refusal that unapplied round 2, not a wording fault).** Round 2's numbered rows carried TWO bracketed tags — `` 1. `[ledger]` `[plain]` … `` — and `plain` is not a member of the closed eight-word state-mark vocabulary the projector classifies, so with all four packets applied `tests/data/dossierStateProseProjection.contract.test.js` reds a second arm (*closes the mark vocabulary and keeps the three semantics disjoint*, `unclassified: ['plain']`) and the pool is reverted. The cure is the row shape this block's own `readiness STRONG` pool already ships at `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`: the numbered row carries **its angle tag only**, its text IS the plain wording, and the three `- \`[face]\`` sub-rows follow. Round 2's notes N.4 item 2 read the brief as requiring both tags and banked it as a chair row; it is not one. ARCH §2.5's example row shows a single tag, and there the token `plain` IS that pool's angle. Nothing typed is touched: the angle tags `[ledger]` `[visitor]` `[street]` `[threshold]` stand exactly as the pool holds them. **Every one of the sixteen wordings below is a refinement of a round-2 wording; the row grammar is the only structural change, and it removes a failing state without adding one (§21.2).**

Both gate measures stay at zero, now on rows that can actually apply: **`punctuation.colonRate` 0 of 16 wordings · `shapes.whichTailRate` 0 of 16 wordings** (relative or interrogative `which`). The shipped rows the gate re-measured at 6.987 and 26.027 band-widths were the product's, because round 2 never landed; the colon lived in variant 2's line and the `which` in variant 3's, and both are gone from every wording here.

---

## THE ROWS (paste under the pool's existing bold line, replacing rows 1–4)

**readiness `ADEQUATE`**
1. `[ledger]` What {settlement} holds against likely trouble covers it, and covers it to the line. Nothing in that cover would stretch to a call arriving while another is still open.
   - `[face]` At {settlement} the reckoning comes out even and leaves nothing over. Every likely trouble has its answer there, and a trouble coming beside another would find that answer already spoken for.
   - `[face]` Against the trouble it expects, {settlement} keeps enough and keeps no more. What is kept stands where it is set, and a call raised on top of another would have nothing to draw on.
   - `[face]` The margin at {settlement} is the part that is missing. What does stand meets what the town is likely to face, and a call made while another is open would find nothing in reserve.
2. `[visitor]` A stranger reaching {settlement} finds an ordinary defended town. What the town has in place is enough to be taken in earnest, and short of anything a stranger would rest on.
   - `[face]` To a stranger arriving, {settlement} is defended in the ordinary way. The defence is enough to be reckoned with, and short of anything that would answer for safety.
   - `[face]` The defence at {settlement} is ordinary. A stranger takes it seriously, and would find no comfort in the arrangement.
   - `[face]` Defended is what a stranger takes {settlement} for, and defended is the whole of the reading. What makes it true is in place; what would make it easy is not.
3. `[street]` The town's account of itself is that it holds against the usual trouble, and claims no more than that. The account matches what the town has.
   - `[face]` Asked, the town says it can hold against ordinary trouble, and says nothing past that. What it says and what it has come out the same.
   - `[face]` What the town claims for itself is the ordinary run of trouble and nothing beyond. The claim is not larger than what the town holds.
   - `[face]` Ordinary trouble the town expects to meet, and it puts the case no further. The case as put is the case as it stands.
4. `[threshold]` The arrangements at {settlement} are sufficient, and sufficient exactly. A further demand on them would be met by leaving a standing pressure unmet.
   - `[face]` Sufficiency at {settlement} is reached and not passed. That sufficiency answers what presses the town now, and a pressure past it would take its answer from one already given.
   - `[face]` Nothing at {settlement} stands spare. Every part of the arrangement stands against a pressure that exists, and a further pressure would be taken up by a part already engaged.
   - `[face]` The point {settlement} has reached is the one where the arrangements carry exactly what is on them. Anything more would come off something already carrying.

---

## --- NOTES

### One line per face: what changed, and the law or target behind it

**1 `[ledger]` `[plain]`** — closing sentence rebuilt from *the cover answers where it is set and would not stretch to a trouble arriving while another is being met* to *Nothing in that cover would stretch to a call arriving while another is still open*, dropping a limp restatement of claim (c) and the passive *is being met*: the density law (§21.4) — the line lands shorter without losing a claim, and the thread runs *covers it* → *that cover* (§1.4.1).

**1 `[ledger]` `[face]` a** — third clause cut (*and each answer is already spoken for, so…* became *would find that answer already spoken for*), and *Each…each* rewritten as one *Every*: R-DA-03's 3+-segment ceiling and the tic bar of §21.1; the close now lands on a standing condition rather than round 2's bare *would find none* (R-DA-04's close-kind).

**1 `[ledger]` `[face]` b** — *a call arriving elsewhere at the same time* → *a call raised on top of another*, and the close *nothing behind it* → *nothing to draw on*: removes a pronoun closer (R-DA-04's `closers.pronounRate`, held at 0 of 16 across the pool) and drops the temporal particular the card does not license, while *draw on* keeps the ledger's own verb.

**1 `[ledger]` `[face]` c** — the doubled *margin … margin* sentence rebuilt as *What does stand meets what the town is likely to face*, the indicative *finds* made subjunctive (*would find*), and the close set on *nothing in reserve*: wall 2 / A2 (the edge is subjunctive), R-DA-04's ABSENCE close-kind, and sibling distance — the emphatic *does stand* is this face's rhythm and no sibling's.

**2 `[visitor]` `[plain]`** — *a traveller* struck in favour of a second *stranger*, and *anything that would set a traveller at ease* → *anything a stranger would rest on*: R-DA-22 (one term for one thing — round 2 ran two nouns for one person), with the reassurance claim still routed through what a stranger *would* do rather than asserted as a reader's state.

**2 `[visitor]` `[face]` a** — *To anyone arriving* → *To a stranger arriving* and *a traveller could rest on* → *anything that would answer for safety*: *anyone* skirted the REFUSED COLUMNS' totality-over-persons line for no gain, and *answer for* is the clerk's vouching verb, which puts this face's vocabulary a clear step from its plain sibling's.

**2 `[visitor]` `[face]` b** — rebuilt whole and cut from 32 words to 19 (*The defence at {settlement} is ordinary. A stranger takes it seriously, and would find no comfort in the arrangement.*): R-DA-05 — the short line exists and the pool needed one, and this is now the floor of a 19-to-34-word spread against round 2's flat 24-to-36; the close moved off a pronoun onto a civic noun.

**2 `[visitor]` `[face]` c** — *defended is the whole of it* → *the whole of the reading*, and the closing pair joined by the pool's one semicolon (*What makes it true is in place; what would make it easy is not*): R-DA-06's rationed device spent once in sixteen where it buys a real balance, and *the reading* gives the second sentence a noun to pick up (§1.4.1) instead of a bare *it*.

**3 `[street]` `[plain]`** — *account* three times and *the town's* twice reduced to one *account* handed forward, *Nothing in the town's holding runs against that account* → *The account matches what the town has*: R-DA-22 and §21.1's zero-tics; the flat positive also drops a double negative the ear stumbles on, and the closing sentence is now the shortest in the variant.

**3 `[street]` `[face]` a** — *the town … the town … the town* thinned to pronouns after the first (*What it says and what it has come out the same*): R-DA-10's dispersion and the read-aloud ear; the fronted *Asked,* opener is kept unchanged because it is the strongest opening in the variant and no sibling shares it.

**3 `[street]` `[face]` b** — *nothing beyond it* → *nothing beyond*, and *not larger than the holding it rests on* → *not larger than what the town holds*: removes the third *it* of a four-wording set and a pronoun-tailed close (R-DA-04), and *beyond* now differs from the plain's *no more*, face a's *past that* and face c's *no further* — four distinct forms of claim (b).

**3 `[street]` `[face]` c** — the semicolon replaced by a full stop, splitting *…puts the case no further; the case as put…* into two sentences: R-DA-06 again — two semicolons in sixteen wordings would read as a device habit across the pool, and the variant's fronted-object opener (*Ordinary trouble the town expects to meet*) is rhythm enough without it.

**4 `[threshold]` `[plain]`** — *taking cover off a trouble already covered* → *leaving a standing pressure unmet*: variant 1 owns *cover* in this pool, so the collision cost the set sibling distance; *met … unmet* is the threshold's own turn and states the same subjunctive uncovering (§21.1's widest-sibling-distance target, R-DST-B unchanged).

**4 `[threshold]` `[face]` a** — *would be covered by uncovering something else* → *would take its answer from one already given*: the same claim in the variant's own vocabulary rather than variant 1's, and *That sufficiency* now carries the opening noun forward where round 2 shifted to *The holding* and handed nothing back (§1.4.1, THE THREAD).

**4 `[threshold]` `[face]` b** — *Everything the town has set against a pressure is set against a pressure that exists* → *Every part of the arrangement stands against a pressure that exists*, close on *a part already engaged*: cuts the *set…set* and one of three *pressure*s, and the *Every part … a part* frame brackets the face — the quantifier ranges over the arrangement, never over persons, so the REFUSED COLUMNS line is not engaged.

**4 `[threshold]` `[face]` c** — rebuilt from *What stands at {settlement} answers…* (four inflections of *answer* in one wording) to *The point {settlement} has reached is the one where the arrangements carry exactly what is on them. Anything more would come off something already carrying.*: §21.1's zero-tics, and the slot is kept mid-sentence so the face's `{slot}` set still equals its parent's (ARCH §2.5's face row) while no wording in the pool opens on a `proper` slot (T-F8; wall 10).

### What did NOT change, and why

- **Every claim set is the round-2 set, which is the product's set** (arm A6, claim-equal across the four wordings of each variant). No claim added, none dropped at this round. The two claim changes round 2 declared stand and are re-declared, not made quietly: variant 1's *tested twice at once* keeps the simultaneity and drops the count (the card's `may NOT: a count`), and variant 2's *enough on the walls* keeps the sufficiency and drops the OBJECT the block holds no field for (the annex's own PROVENANCE + FENCE; the walls read is `DS-DEF-2`'s).
- **The typed lines are untouched** — ROLE, READS, RELATION, ATTACH, FORM, MOVE and the four angle tags are not mine and are not repeated here.
- **The licence-card refusal is carried, uncured and uncurable by a writer** (round 2 N.0 / N.4 item 1). `node scripts/prose-licence-card.mjs DS-DEF-1 'readiness ADEQUATE'` still prints `reads: (the census recovered no reading for this pool)` and `may claim: nothing: the census recovered no reading, so no claim is licensed`. A card that licenses nothing refuses all sixteen wordings **and equally refuses the four sentences in the product today**; every claim above cites the block's annex STATE-KEY (`defenseProfile.readiness.label` read against `defenseScoreBands.js:30-39`) as a NOT-EXECUTABLE licence under the §908 law. The chair's two dispositions are unchanged: re-run the census recovery for this (block, pool) and re-issue the card, or rule the pool WIRING-UNRESOLVED. Banked with its faces, never trimmed (§21.2).
- **Variant 2's standpoint refusal is carried** (round 2 N.4 item 3): the `[visitor]` angle IS a standpoint the card's `may NOT` list refuses, and it lives in the bracketed tag, which is not mine to touch; every wording routes the reaction through what a stranger *would* do or find, which is as far as the voice reaches without dropping a claim. A sitting row, not a wording fault.
- **The card's `may NOT: a second fact` report is carried** (round 2 N.4 item 4): all four product variants carry two facts and claim-equality forbids reducing them; read as a modifier-shaped card clause inapplicable to a spine, the chair's to settle.

### Walls and bands across the sixteen wordings

**Zero:** em dash · exclamation mark · question mark · **colon** · **`which`, relative or interrogative** · digit or numeral · counting word standing as a count · first or second person · imperative · expletive opener · future indicative · citation of a record holder · figure, simile or inanimate intent · sense verb on an abstraction · named character · totality over persons · exemption from a duty · theological claim · **pronoun closer** (down from 2 at round 2) · **wording opening on `{settlement}` or any slot** · third sentence (every wording is one or two sentences, A1).

**Rationed and reported:** semicolon **1 of 16** (variant 2 `[face]` c; down from 2 at round 2) · `{settlement}` present in 12 of 16 and opening none (all of variants 1, 2 and 4; none of variant 3, matching the product row, so every face's slot set equals its parent's) · CONTRAST as a closing move in **one** variant only (variant 2, its rejected alternative a sibling band of this state key — R-DA-02).

**Spread (R-DA-05):** word counts 19 · 23 · 24 · 25 · 25 · 26 · 26 · 28 · 29 · 29 · 29 · 30 · 31 · 31 · 34 · 34, range 19–34 against round 2's 24–36 — the floor drops five words and the within-pool sd rises. No two wordings of a variant share their first two words; the sixteen openers are *What · At · Against · The margin · A stranger · To a stranger · The defence · Defended · The town's · Asked · What the town · Ordinary trouble · The arrangements · Sufficiency · Nothing · The point*.

**THE THREAD (§1.4.1), the noun each wording hands forward:** 1 *covers it → that cover* · 1a *the reckoning → its answer there → that answer* · 1b *keeps → what is kept* · 1c *the margin missing → what does stand → nothing in reserve* · 2 *an ordinary defended town → the town* · 2a *defended → the defence* · 2b *the defence → it → the arrangement* · 2c *takes it for → the reading → it* · 3 *the town's account → the account* · 3a *says → what it says* · 3b *claims → the claim* · 3c *puts the case → the case as put* · 4 *the arrangements → them* · 4a *sufficiency → that sufficiency* · 4b *nothing spare → every part → a part* · 4c *what is on them → anything more*. No wording changes subject mid-passage and hands nothing back, so none spends the one-turn-outward licence. **Seating:** this pool is the spine, read first — every wording opens a passage and none is written to follow one (no connective, comma, clause-list word, bare pronoun or slot at the head), and each closes on a standing fact a modifier can pick up: the cover, the answer, the draw, the reserve, the town, the defence, the arrangement, the reading, the account, the claim, the case, the pressure, the part, the carrying.
