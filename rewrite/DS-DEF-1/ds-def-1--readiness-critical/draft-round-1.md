# DS-DEF-1 · `readiness CRITICAL` · REWRITE draft round 1
Seat: Opus 5 (Fable-unvalidated) · block DS-DEF-1 · role `spine` · 3 variants in, 3 variants out, 12 faces.
Paste-ready: the three numbered rows below replace the pool's three variant rows under the bold pool line. The typed lines (ROLE, READS, RELATION, ATTACH, FORM, MOVE) are untouched and not repeated here.

1. `[ledger]` Readiness at {settlement} stands at its floor.
   - `[face]` Graded on readiness, {settlement} falls in the bottom band.
   - `[face]` What {settlement} holds in the way of readiness comes to almost nothing.
   - `[face]` In effect, {settlement} is an undefended town.
2. `[street]` The town is as good as undefended.
   - `[face]` What the town keeps against a threat is nothing to speak of.
   - `[face]` Little the town has to hand amounts to a defense.
   - `[face]` Undefended is the state of the town.
3. `[visitor]` A stranger comes into {settlement} and finds next to nothing kept for its defense.
   - `[face]` To a stranger arriving, {settlement} is a town with barely any defense.
   - `[face]` What a stranger meets at {settlement} is a town that holds almost nothing to its defense.
   - `[face]` Arriving at {settlement}, a stranger meets an undefended town.

--- NOTES

## A. The card, as this packet reads it
The card licenses ONE claim: *that the reader `scoreBand(readinessScore)` selects the row `CRITICAL` of `READINESS_ROW_POOL` in `defenseStateProse.js`, as a STANDING fact of the record* (card line **may claim**, under **reads** and **predicate**). It refuses a count, a cause, a season, a future, a standpoint and a second fact (**may NOT**); a totality over persons, an exemption, a named character's fate and a theological claim (**REFUSED COLUMNS**); and every citation, the source standing SOURCE-UNRESOLVED (**source**). The bag is FILLED at this block's call sites with `{settlement}` alone, so `{band}`, `{route}` and `{counterpart}` appear in no face.

## B. Licence, face by face
Every face asserts exactly one claim — the band — and nothing else. The clause cited is the clause that licenses that claim in that wording.

**Variant 1 `[ledger]`** (all four faces carry `{settlement}`, none opens on it — ARCH §2.5 seam contract / T-F8)
| face | claim | licensing clause |
|---|---|---|
| row | readiness for this town stands at the lowest band | **may claim** (the row `CRITICAL` is selected), read with **predicate** (`=== CRITICAL`); `{settlement}` by **bag** (FILLED) |
| f2 | same claim, worded as the grading | **may claim** + **reads** (`scoreBand(readinessScore)` is what grades); `{settlement}` by **bag** |
| f3 | same claim, worded as the holding | **may claim**; `{settlement}` by **bag** |
| f4 | same claim, worded flat | **may claim**; `{settlement}` by **bag** |

**Variant 2 `[street]`** (no slot in any of the four — slot sets are equal across the variant's faces, as the annex requires)
| face | claim | licensing clause |
|---|---|---|
| row | the town's readiness stands at the lowest band | **may claim**; the town referred to without a slot, so no bag entry is spent |
| f2 | same claim | **may claim**; "a threat" is the band's own generic subject, not a particular threat (block fence at `:2501`) |
| f3 | same claim | **may claim** |
| f4 | same claim | **may claim** |

**Variant 3 `[visitor]`** (all four faces carry `{settlement}`, none opens on it)
| face | claim | licensing clause |
|---|---|---|
| row | the town's readiness stands at the lowest band, framed at arrival | **may claim**, with the arrival framing licensed by the card's **angle** line (`ledger street visitor`); `{settlement}` by **bag** |
| f2 | same claim | **may claim** + **angle**; `{settlement}` by **bag** |
| f3 | same claim | **may claim** + **angle**; `{settlement}` by **bag** |
| f4 | same claim | **may claim** + **angle**; `{settlement}` by **bag** |

The four faces of each variant are claim-equal to one another (arm A6) and differ in vocabulary and rhythm: floor / grading / holding / plain state in variant 1; degree idiom / keeping / to hand / inverted state in variant 2; entry / dative frame / encounter / participial in variant 3.

## C. What each old sentence claimed, and what was dropped
Dropping is the rewrite's purpose; the shipped breach is the corpus's known state. Nothing was added.

**Variant 1, old:** "{settlement} is effectively undefended. There is no arrangement here that would slow a serious attempt on the town by more than the time it takes to walk in."
- KEPT: the band ("effectively undefended").
- DROPPED: the whole second sentence, which carried (a) a SECOND FACT about the town's arrangements (**may NOT**: a second fact); (b) a subjunctive projection of what an attempt would meet, which is a forecast in all but mood (**may NOT**: a future); (c) a totality over the arrangements, which the header's own fence forbids because the readiness word asserts nothing about any one of the five arms (block fence, `:2510-2516`).
- Also corrected in form: the expletive opener "There is" (R-DA-07) and the row opening on the `{settlement}` slot (ARCH §2.5 / T-F8).

**Variant 2, old:** "The town knows perfectly well what it could not survive, and the knowledge shapes what it will and will not provoke."
- KEPT: the band, which the old sentence carried only obliquely ("what it could not survive"); it is now stated.
- DROPPED: (a) the belief frame "the town knows", a STANDPOINT and a totality over persons (**may NOT**: a standpoint; **REFUSED COLUMNS**: a totality over persons); (b) the second fact and its cause, "the knowledge shapes…" (**may NOT**: a second fact, a cause); (c) "what it will and will not provoke", a bare future indicative and a FATE breach (A2; THE PROMISE; **may NOT**: a future).

**Variant 3, old:** "A stranger reaches the centre of {settlement} without being stopped, challenged or counted by anybody at all."
- KEPT: the band, framed at arrival.
- DROPPED: (a) "by anybody at all", a totality over persons (**REFUSED COLUMNS**); (b) "counted", an INSTITUTION assertion about who counts, licensed only by a row of the institution table, which this card does not carry (MOVE-GRAMMAR §1.2 row 5; CLERK-LAWS §1.3); (c) "stopped, challenged", the watch's conduct, which the block's own fence routes to the public-order shape at DS-DEF-3 (`:2515-2516`); (d) the habitual triad (R-DA-10).

## D. Sibling distance (arms A1 and A11)
The block's sibling pools are `terrain FAVOURABLE` / `terrain EXPOSED` and `strategic value HIGH` / `LOW`. No face uses the terrain pools' vocabulary — *open*, *sits open*, *the ground*, *the approach*, *sited*, *hill*, *narrows* — and none uses the strategic-value pools' — *worth taking*, *a prize*, *worth an army's season*. The first shipped `terrain EXPOSED` variant is "{settlement} sits open", so *open* was struck from an earlier draft of variant 2's row. No face restates or contradicts a sibling readiness band; the three surviving faces name no arm of the five.

## E. Form checks run over all twelve faces
No em dash, no exclamation, no question mark, no digit or percent, no `which`-clause, no citation and no record holder named (**source**: none, standing SOURCE-UNRESOLVED — arm A13 refuses a holder here), no first or second person, no figure, no sense verb on an abstraction, no expletive opener, no evaluative adjective on the place. Spelling follows the shipped corpus's *defense* (R-DA-22, one name form). The three rows' first two words are distinct (`Readiness at` / `The town` / `A stranger`), no row opens on the settlement token, and the rows' closes are three kinds — a condition (the floor), a condition (undefended), an absence (nothing kept for its defense) — under R-DA-04.

## F. THE THREAD
Each row is a spine and stands first in its passage, so it is written to hand a noun forward for whatever modifier the composer seats next: *readiness* and *the floor* (variant 1), *the town* (variant 2), *the town* and *a stranger* (variant 3). No face depends on a preceding sentence, and none changes subject mid-passage, because each is one sentence carrying one claim.

## G. REFUSALS
- **No variant refusal.** All three variants are written lawful under the card.
- **One POOL-level law this pool cannot meet, and it is not curable by wording:** MOVE-GRAMMAR §2.1's level-1 rule that a pool of k variants carries min(k, 8) DISTINCT level-1 grammars, at least two in any pool of two or more. This card resolves one reading and declares no second field, no relation, no attach set and no move, so V2 (needs a structural-consequence field), V3 (needs `none-exists`), V4 (needs a named object), V5 (needs an institution row), V6 (needs an unresolved state value), V7 (needs event provenance) and V8 (needs a `not-held` field with provenance) are all unlicensed here. All three variants are therefore V1 `PRESENT`, and the pool's spread lives entirely in its angles and its wording. Recorded for the chair as a finding, not as a wording fault.
- **A consequence of the drops, stated so it is not re-found:** once the unlicensed claims come out, the three variants carry the IDENTICAL claim set — the one licensed claim — and are distinguished only by angle and by wording. That is the arithmetic of a spine whose card licenses a single band fact, not a merge; the three slots, their vids and their order are untouched, and nothing is trimmed.
