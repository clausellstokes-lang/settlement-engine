# DS-DEF-1 · `readiness WEAK` · REWRITE draft round 1
Seat: Opus 5 (Fable-unvalidated) · block DS-DEF-1 · role `spine` · 3 variants in, 3 variants out, 12 wordings (3 numbered lines + 9 `[face]` rows).
Paste-ready: the three numbered rows below replace the pool's three variant rows under the bold pool line **`readiness `WEAK``**. The pool declares no typed lines (ROLE, READS, RELATION, ATTACH, FORM, MOVE); none is touched and none is repeated here.

1. `[ledger]` The defense of {settlement} is thin.
   - `[face]` What {settlement} has standing to its defense is an arrangement that falls short of adequate.
   - `[face]` Set down under readiness, {settlement} is entered weak.
   - `[face]` Against pressure {settlement} has slender provision.
2. `[visitor]` As a defended place, {settlement} stands thin.
   - `[face]` Taken whole, {settlement} amounts to a thinly defended town.
   - `[face]` Before any threat is named, what {settlement} has to its defense is slight.
   - `[face]` Whatever else {settlement} is, its defense is weak.
3. `[unfolding]` From one day to the next, the defense at {settlement} is thin.
   - `[face]` Slight is what stays at {settlement} by way of defense.
   - `[face]` At {settlement} the defense is weak now and weak in the ordinary run of things.
   - `[face]` A thin defense is what {settlement} carries as its ordinary condition.

--- NOTES

## A. The card, as this packet reads it

`node scripts/prose-licence-card.mjs DS-DEF-1 'readiness WEAK'` returns: role **spine**; **reads** `scoreBand(readinessScore)` via `READINESS_ROW_POOL` in `defenseStateProse.js`; **predicate** `=== WEAK`; **bag** `{band: RESERVED, counterpart: proper, route: proper, settlement: proper}`, **FILLED at this block's call sites: `{settlement}`**; **relation** none and **attach** empty (a spine is the seat and carries neither); **move** none declared; **angle** `ledger unfolding visitor`; **covert** no; **source** `(none) · standing SOURCE-UNRESOLVED` — no citation is licensed and arm A13 refuses a named holder; **may claim:** that the reader `scoreBand(readinessScore)` selects the row `WEAK` of `READINESS_ROW_POOL`, as a STANDING fact of the record; **may NOT:** a count, a cause, a season, a future, a standpoint, a second fact; **REFUSED COLUMNS, always:** a totality over persons, an exemption from a duty, a named character and that character's fate, a theological claim.

The clause labels used in the licence table below:

- **C1** = the card's **may claim** line — the band selected for this town is `WEAK`, as a standing fact.
- **C2** = the card's **bag** line — `{settlement}`, the one slot FILLED at this block's call sites.
- **C3** = the card's **angle** line — `ledger unfolding visitor`, the three tags this pool already carries.
- **C4** = the card's **reads** + **predicate** lines — what the reading IS (`scoreBand(readinessScore)`, one grade over the whole town), which is what licenses a wording to say *readiness* and to say it of the town entire.
- **C5** = R-DA-02's sibling-band licence — a contrast is kept only where the rejected alternative names a sibling pool key or a sibling band. `ADEQUATE` is the sibling band of this same `scoreBand` reading, so *falls short of adequate* is the band's own ordinal content and not a second fact. Used ONCE in the pool (variant 1, face 1), never fronted, and the closing move of one wording only.

**The one licensed claim, stated once.** This card resolves a single reading into a single band word, and declares no second field, no relation, no attach set and no move. There is therefore exactly ONE claim available: *the town's defensive readiness stands at `WEAK`*. Unlike this block's `ADEQUATE` cell, which the block's own COMPOSITION FENCE at `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2517-2521` glosses as "a covered-with-no-margin *town*" (a fact and its bound), the `WEAK` cell carries no such second-half gloss in the block text. This packet therefore does **not** import a no-depth or a partial-coverage half, and every one of the twelve wordings asserts C1 and nothing else. That is the conservative reading and it is deliberate: where one effort must choose between a sharper wording and a licensed one, the licensed one wins (Part B §21.3).

## B. Licence, wording by wording

Every wording asserts exactly one claim. The face rows are claim-equal to one another and to their numbered line (arm A6 reads across the faces, not back to the old sentence).

**Variant 1 `[ledger]`** — the office's own accounting. All four wordings carry `{settlement}`; none opens on it.

| wording | claim asserted | licensing clause |
|---|---|---|
| line | readiness for this town is thin | C1; C4 (one grade over the town entire); slot by C2; tag by C3 |
| face 1 | the same claim, put as an ordinal against the sibling band | C1; C5 (the rejected alternative is the sibling band `ADEQUATE`); C2; C3 |
| face 2 | the same claim, put as the grade the record enters | C1 + C4 (`scoreBand` is what grades); C2; C3 |
| face 3 | the same claim, put as what the town has against pressure | C1; C2; C3. "Against pressure" is the band's own generic subject — no particular threat is named, which is the block's declared scope ("before any particular threat is named", `:2501`) |

**Variant 2 `[visitor]`** — the town taken whole, as a defended place. All four carry `{settlement}`; none opens on it.

| wording | claim asserted | licensing clause |
|---|---|---|
| line | the same claim, put as what the town amounts to as a defended place | C1; C4 (the reading is of the town entire, never of an arm); C2; C3 |
| face 1 | the same claim, put as the whole rather than the parts | C1; C4; C2; C3 |
| face 2 | the same claim, put before any threat is named | C1; C4; C2; C3 |
| face 3 | the same claim, put concessively | C1; C2; C3 |

**Variant 3 `[unfolding]`** — the state as it continues, in the durative present. All four carry `{settlement}`; none opens on it.

| wording | claim asserted | licensing clause |
|---|---|---|
| line | the same claim, put as the state from day to day | C1 (a STANDING fact, which is what a durative present asserts); C2; C3 |
| face 1 | the same claim, put as what stays | C1; C2; C3 |
| face 2 | the same claim, put as the present and the ordinary run | C1; C2; C3 |
| face 3 | the same claim, put as the town's ordinary condition | C1; C2; C3 |

No wording asserts a count, a cause, a season, a future, a standpoint or a second fact; none names a holder, a source, an office, a person, a date or an arm of the five; none carries a totality over persons, an exemption, a named character or a theological claim. **Nothing is added.**

## C. What each old sentence claimed, and what was dropped

Dropping the unlicensed claims is the rewrite's purpose; the shipped breach is the corpus's known state. No dropped claim is replaced by another.

**Variant 1, shipped:** "The defensive arrangements at {settlement} are thin. Something is in place against most pressures and nothing is in place in depth."
- KEPT: the band — *the arrangements are thin* (C1).
- DROPPED, "Something is in place against most pressures": a SECOND FACT (card **may NOT**) about how cover is distributed, carrying a quantification over pressures ("most") that is a COUNT (card **may NOT**), and a per-pressure reading that the block's own **PROVENANCE + FENCE** (`:2510-2516`) bars in terms — the readiness word is a summary of the five arms and asserts nothing about any one of them.
- DROPPED, "nothing is in place in depth": a second fact about the STRUCTURE of the arrangements (depth is layering, a property of the arms), unlicensed by a card that reads one score and returns one word; also a totality over the arrangements.

**Variant 2, shipped:** "What a stranger notices at {settlement} is how much of the perimeter has nobody on it, and how normal the town seems to find that."
- KEPT: the band, framed as what the town amounts to as a defended place — the block's own header gloss (`:2501`).
- DROPPED, "What a stranger notices": a STANDPOINT (card **may NOT**), an asserted person no field holds (R-DA-14: the person of record is an office, a holding, at most one recorded act), and a per-entry vantage that R-DA-01 refuses (the office's origin is declared once on the front matter, never per entry).
- DROPPED, "how much of the perimeter has nobody on it": a COUNT (card **may NOT**) over a named arm — the perimeter and who stands on it is the watch, which the block's fence routes to `guardEffectivenessDesc` and the public-order shape at `DS-DEF-3` (`:2515-2516`). No wording in this packet names walls, watch, garrison, militia, muster, gate or perimeter.
- DROPPED, "how normal the town seems to find that": a BELIEF FRAME (R-DA-13's belief-frame floor is EXECUTABLE and stands at zero) and a TOTALITY OVER PERSONS (**REFUSED COLUMNS**); also an assigned reaction, which the register card bars outright.

**Variant 3, shipped:** "{settlement} is getting by on not being tested. The margin has been narrowing rather than widening, and nothing in hand is closing it."
- KEPT: the band — the defense is thin (C1).
- DROPPED, "is getting by on not being tested": a CAUSE (card **may NOT**) — why the town is still standing — resting on an event claim (that no test has occurred) that only an event-provenance field licenses; MOVE-GRAMMAR §1.2 row 2 puts HISTORY out of R1 STATE entirely, and this card declares no provenance.
- DROPPED, "The margin has been narrowing rather than widening": a TRAJECTORY, which is history in the perfect progressive and is unlicensed twice over — no event-provenance field (§1.2 row 2) and no prior-value field; and a contrast whose rejected alternative ("widening") names no sibling pool key and no sibling band, which R-DA-02 refuses.
- DROPPED, "nothing in hand is closing it": a SECOND FACT about what the town holds, a totality over that holding, and a forecast in all but mood (card **may NOT**: a second fact, a future).
- Also corrected in FORM: the shipped row opened on the `{settlement}` slot, which ARCH §2.5's seam contract refuses for a sentence row (T-F8) and MOVE-GRAMMAR §1.4 wall 10 rations to at most one variant per pool. This packet opens no wording on the slot at all.

## D. The walls, checked over all twelve wordings

Zero em dashes · zero exclamation marks · zero digits and zero percents · zero `which` clauses (variant 1 face 1 uses `that`, which is not the barred form) · zero questions · zero first or second person · zero citations and no record holder named (**source**: none, standing SOURCE-UNRESOLVED; MOVE-GRAMMAR §4.4.3, and the office does not cite its own books) · zero bare future indicatives and no fate (THE PROMISE; A2) · zero figures, sense verbs on abstractions and inanimate intents (R-DA-11) · zero expletive openers (R-DA-07) · zero evaluative adjectives on the place (R-DA-10) · zero invented particulars.

**Spelling.** `defense`, which is the shipped corpus's form and the one this block already uses — measured on the annex: `defense` 125, `defence` 6 (R-DA-22, one term for one thing, one name form). **Flagged for the chair:** the sibling packet at `rewrite/DS-DEF-1/ds-def-1--readiness-adequate/draft-round-1.md` writes `defence` throughout, which contradicts this count and its own R-DA-22 citation. One of the two packets must move before the block lands; this one follows the corpus.

## E. Form

Every numbered line carries exactly ONE bracketed angle tag, its own, unchanged: 1 `[ledger]` · 2 `[visitor]` · 3 `[unfolding]`. No `[plain]` marker appears anywhere — it belongs to modifier rows and the projector refuses it on a spine. Three variants, in their shipped order, under their shipped numbers; none added, removed, merged or reordered (§22, never trim). Face rows are written to `FACE_ROW_RE` = ``/^\s*-\s+`\[face\]`\s+(.*)$/`` (`scripts/lib/dossier-annex-grammar.mjs:107`).

**Slots.** The parent's slot set is `{settlement}` on all three variants, and all twelve wordings carry `{settlement}` exactly once — a face whose slot set differs from its parent's is refused by ARCH §2.5, and none differs here. `{band}` is RESERVED and unused; `{counterpart}` and `{route}` are not filled at this block's call sites and appear nowhere. No wording opens on the slot (variant 1 face 1 and variant 3 face 2 carry it as their second token, which is the treatment the sibling `ADEQUATE` packet also applies).

**Openers.** The twelve first-two-word pairs are all distinct: *The defense · What {settlement} · Set down · Against pressure · As a · Taken whole · Before any · Whatever else · From one · Slight is · At {settlement} · A thin*. The three numbered lines share no opener (A11), and the settlement-token opener rate for this pool is zero (wall 10; R-DA-17).

**Closes.** thin · adequate · weak · provision · thin · town · slight · weak · thin · defense · things · condition. No wording closes on a pronoun (R-DA-04's `pronounRate` ceiling). Two of the five close KINDS are used — a condition (eight) and an object (four); see the refusal at §H for why the other three are unavailable on this card.

**Word counts** (a `{slot}` counts as one word): variant 1 — line 6 · faces 15, 8, 6. Variant 2 — line 7 · faces 9, 13, 8. Variant 3 — line 12 · faces 10, 15, 11. Pool mean 10.0, sd 3.08, range 6 to 15. The short line exists (three wordings at six or seven words) and two run to fifteen.

## F. THE THREAD (owner, 2026-09-08 ~21:4x; MOVE-GRAMMAR §1.4.1)

Every wording in this pool is ONE sentence, so no wording turns its subject mid-passage and none hands a reader a disconnect. This pool is a SPINE and is composed first, so each wording is written to stand from a cold start and to hand a noun forward for whatever the composer seats next: *the defense* (nine wordings), *readiness* (two), *provision*, *the town*, *the arrangement*, *the condition*. The card records `modifier mounts 0 (none)` for this block today, so no modifier follows in the shipped composition; the nouns are handed forward for the wave that adds them.

## G. Sibling distance (arms A1 and A11 — neither restate nor contradict)

The block holds two further SPINES that CAN be seated on the same page as this one — `terrain FAVOURABLE`/`EXPOSED` and `strategic value HIGH`/`LOW` — and three sibling band rows of this same lens that can never be seated beside it (one `scoreBand` fires per town).

- **Against the terrain spines:** no wording names ground, site, siting, approach, hill, narrows, flat ground, open country or anything the town holds by where it is. The word *open* is absent from the pool, the shipped `terrain EXPOSED` line being "{settlement} sits open".
- **Against the strategic-value spines:** no wording names a prize, worth taking, what the town sits on, or an army's season; and none says or implies that the town holds anything worth coming for, which would restate `strategic value HIGH`.
- **Against the band siblings:** *falls short of adequate* (variant 1, face 1) names the sibling band as the rejected alternative. That is the case R-DA-02 licenses, and it is not a contradiction of the `ADEQUATE` pool, because the two pools are alternatives on one reading and never render together. Nothing here implies `STRONG` (no "in good order", no "would take real effort to threaten") or `CRITICAL` (no "undefended", no "floor", no "bottom band", no "nothing to speak of" — the words the `CRITICAL` packet uses). `WEAK` is thin provision, not the absence of provision, and no wording overshoots into absence.

## H. Refusals

**Variant refusals: none.** All three variants are written lawful under the card; three of three, twelve of twelve wordings. Nothing is banked, and the banked count for this pool stands at zero.

**Two laws this pool cannot meet, neither curable by wording. Recorded as findings so they are not re-found:**

1. **MOVE-GRAMMAR §2.1's level-1 grammar distinctness** — "a pool of k variants carries min(k, 8) DISTINCT level-1 grammars, at least two in every pool of two or more". This card declares one reading, no second field, no relation, no attach set and no move, so V2 (needs a structural-consequence field), V3 (`none-exists`), V4 (a named object), V5 (an institution row), V6 (an unresolved state value), V7 (event provenance) and V8 (a `not-held` field with provenance) are all unlicensed on this block. All three variants are therefore V1 `PRESENT`, and the pool's spread lives entirely in its angles, its rhythms and its lexis. This is the same finding the `readiness CRITICAL` packet records for the same reason; it is a property of a single-band spine, not a wording fault.
2. **R-DA-04's close-kind variety** — the closed set is {condition · prohibition · absence · object · a name not given}. An ABSENCE close needs a `none-exists` or `not-held` field (MOVE-GRAMMAR §1.2 row 11) and this card holds neither; a PROHIBITION close needs an institution row with a duty; a name-not-given close needs a person or holder, and the source stands SOURCE-UNRESOLVED. Only *condition* and *object* are licensed here, and the pool uses both.

**One band reported, not met:** R-DA-05's within-pool word-count sd target of 4.0 is reported at 3.08 over the twelve wordings. The distance is bounded by the card itself — with one licensed claim and no second fact available, length can only be bought with syntax, and buying it past this point would be padding, which the density law (§21.4) treats as the regression rather than the cure.

## I. The two judgment calls, recorded vetoably

1. **`[visitor]` is realised as a WORDING REGISTER — the town taken whole, as a defended place — and NOT as an asserted observer.** Every observer form (*a stranger notices*, *a stranger sees*, *to anyone arriving*) asserts a person the fields do not hold and plants a per-entry vantage; the card bars a standpoint in terms, the register card bars persona and assigned reaction, and R-DA-01 declares the office's origin once and never per entry. The angle survives as the tag on the row and as the outward, whole-town framing of the same fact — the stage licenses the claim, never the shape. **This diverges from the sibling `readiness CRITICAL` packet in this same block, which realises its `[visitor]` variant with an asserted stranger** ("A stranger comes into {settlement} and finds..."). The two readings cannot both be right, and the block cannot ship both; the chair should rule once for DS-DEF-1. If the ruling is that `[visitor]` REQUIRES an asserted observer, variant 2 of this pool is unlawful under this card and becomes a refusal row with its four wordings banked (never trimmed, §22(b)) — the packet flags it rather than deciding it.
2. **`[unfolding]` is realised as the DURATIVE PRESENT — the state as it continues — and not as a trajectory.** The shipped variant's unfolding was a narrowing margin, which is history without an event-provenance field and is dropped under §C. The lawful residue of the angle is the state read across time rather than at an instant, which is what a STANDING fact is; no wording asserts a change, a direction of change, or a rate. If the chair rules that `[unfolding]` REQUIRES an asserted change over time, then variant 3 is unlawful under any card that declares no provenance, and the finding belongs to the census (this pool has no `source` and no event field) rather than to the wording.
