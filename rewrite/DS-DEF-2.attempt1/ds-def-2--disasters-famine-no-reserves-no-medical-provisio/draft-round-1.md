1. `[ledger]` `[plain]` No food is set aside at {settlement} against a bad year, and no one here treats the sick. The year that fails is hardship at once, and a plague runs until it burns out.
   - `[face]` No reserve of food stands at {settlement}, and no hand here tends the sick. Hunger arrives with the failed harvest rather than after it; the sickness stops only when it has run its length.
   - `[face]` Against a bad year {settlement} keeps no store, and against a sickness nobody at all. The bad year is hardship from its beginning, and the sickness is not ended here, only waited out.
   - `[face]` Neither food nor care is held at {settlement} against a bad year or a sickness. The year whose harvest fails turns to hardship without an interval, and the sickness ends of itself or not at all.
2. `[street]` `[plain]` The town's answer to a bad season is the answer it gives a sickness. Both are endured, and the counting comes after.
   - `[face]` A season gone bad and a sickness come to one answer here. The town endures them, and counts afterwards.
   - `[face]` Hunger and sickness are met here in one way. The town bears them while they last, and takes its count when they have gone by.
   - `[face]` Between a bad season and a sickness the town makes no difference of answer, and the answer is to bear the thing and count it when it is done.
3. `[visitor]` `[plain]` A stranger asking after the granary or the sick-house at {settlement} is sent to neither. Neither of them stands here.
   - `[face]` A newcomer at {settlement} asking after the store of food or the house of the sick is given no direction, because the town keeps neither.
   - `[face]` Whoever comes to {settlement} looking for a grain store or a place for the sick is directed to neither, and no such building stands in the town.
   - `[face]` Strangers at {settlement} ask where grain is kept and where the sick are taken, and get no answer to either. The town holds neither place.

--- NOTES

**Packet.** Block DS-DEF-2 · pool key `Disasters & Famine: NO reserves, NO medical provision` · draft round 1 · Opus WRITER seat (Fable-unvalidated). Three existing variants rewritten in place under their own numbers and their own bracketed tags; none added, removed, merged or reordered; four wordings per variant (the `[plain]` parent plus three `[face]` sub-rows), twelve wordings in the pool. The typed lines of the pool (ROLE, READS, RELATION, ATTACH, FORM, MOVE) are untouched and not repeated here.

**The card, as printed.** `role: spine` · `reads: disasterRowSituation(granary, hospital, church)` (via `DISASTER_ROW_POOL` in `defenseStateProse.js`; absent ⇒ no candidate) · `predicate: === no reserves, no medical provision` · `bag: {band: RESERVED, route: proper, settlement: proper}`, FILLED at this block's call sites `{settlement}` · `relation: (a spine takes no relation)` · `seat/form: (not a seat-taker) / sentence` · `move: (none declared)` · `angle: ledger street visitor` · `attach: (empty: a spine takes no attach set)` · `echo: spine mounts 1 (tabs: defense); modifier mounts 0` · `covert: no` · `source: (none) · standing SOURCE-UNRESOLVED — NO citation is licensed; a face naming a record holder is refused by arm A13` · `may claim: that the predicate (=== no reserves, no medical provision) holds, as a STANDING fact of the record` · `may NOT: a count, a cause, a season, a future, a standpoint, a second fact, another civic object of the class store` · `audience: player (no mark)` · `REFUSED COLUMNS: a totality over persons; an exemption from a duty; a named character and that character's fate; a theological claim about a deity`.

**The claim set carried, one id per claim (the BEFORE's, unchanged).**
- K1 — no food is held at the settlement against a bad year.
- K2 — there is nobody at the settlement to treat the sick.
- K3 — a failed harvest is hardship with no interval between the failure and the hardship.
- K4 — a plague runs on until it stops of its own accord.
- K5 — the town's answer to a bad season and its answer to a sickness are one answer.
- K6 — that answer is to endure the thing.
- K7 — the counting comes after the enduring.
- K8 — a stranger asks after a store of food or a house for the sick.
- K9 — the stranger is sent to neither.
- K10 — the reason is that the town holds neither.

**Variant 1 `[ledger]` — the four wordings and the clause licensing each claim.**

| wording | claims made | licence |
|---|---|---|
| `[plain]` | K1 | card `may claim:` (the predicate as a STANDING fact of the record) on `reads: disasterRowSituation(granary, hospital, church)` — the `no reserves` term of the predicate licenses K1 as R-DA-02's flat LACK |
| `[plain]` | K2 | card `may claim:` — the `no medical provision` term of the predicate licenses K2 as a second flat LACK; the read's `church` term is FALSE under this predicate, so no wording names a church, a parish or clergy anywhere in the pool |
| `[plain]` | K3 | NOT licensed by any card clause: the predicate holds that nothing is stored, never what a failed harvest does to a town that stores nothing — convicted by `may NOT: a second fact` and, on its temporal half, by `may NOT: a cause` (see REFUSALS R1, R4) |
| `[plain]` | K4 | NOT licensed by any card clause: the predicate holds that no one treats the sick, never the course a disease takes — convicted by `may NOT: a second fact` (see R1, R4) |
| `[plain]` slot | `{settlement}` | card `bag: {settlement: proper}`, FILLED at this block's call sites; used once, never as the opener (ARCH §2.5 T-F8) |
| `[plain]` angle | ledger | card `angle: ledger street visitor` |
| `[face] 1` | K1, K2, K3, K4 | as the plain, clause for clause; K3 written as a denial of sequence (`rather than after it`) instead of the plain's `at once`; the semicolon is the chosen joint (R-DA-06), spent once in the pool and nowhere else |
| `[face] 2` | K1, K2, K3, K4 | as the plain; K1 and K2 carried by a gapped coordinate (`and against a sickness nobody at all`), which is word order and punctuation, the two things a grammar may spend (MOVE-GRAMMAR §3.4) |
| `[face] 3` | K1, K2, K3, K4 | as the plain; K1 and K2 compressed into one correlative subject (`Neither food nor care`), which is the density law's compression and not a claim change — the two LACKs are still two claims |

**Variant 2 `[street]` — the four wordings and the clause licensing each claim.**

| wording | claims made | licence |
|---|---|---|
| `[plain]` | K5 | NOT licensed — an identity between two responses; the predicate holds two absences and says nothing about what the town does with them; convicted by `may NOT: a second fact`, and the bad season is the unit of the claim, which sits against `may NOT: a season` (see R5) |
| `[plain]` | K6 | NOT licensed as an act of the town; the nearest licensed thing is the predicate itself, since a town with neither reserve nor medical provision has nothing else the record holds. Written as a passive standing fact in three wordings so no wording assigns the town a motive, a resolve or a mood (the FEELING non-move, MOVE-GRAMMAR §1.3) |
| `[plain]` | K7 | NOT licensed — an ordering of two acts that no field holds, and the counting act sits at the nearest edge of `may NOT: a count` (no number is written, and none is implied) (see R5) |
| `[plain]` slot | none | the parent variant carries no slot; every face of it carries none (ARCH §2.5: a face whose `{slot}` set differs from the parent's is refused) |
| `[plain]` angle | street | card `angle: ledger street visitor` |
| `[face] 1` | K5, K6, K7 | as the plain; K5 carried by convergence (`come to one answer`) rather than by the plain's equation |
| `[face] 2` | K5, K6, K7 | as the plain; K5 carried by manner (`met here in one way`); K7 carried as a completed span (`when they have gone by`), which is a sequence and not a duration |
| `[face] 3` | K5, K6, K7 | as the plain; K5 carried as a denied difference; the whole wording is one sentence, which is the pool's longest and its one single-sentence `[street]` rhythm |

**Variant 3 `[visitor]` — the four wordings and the clause licensing each claim.**

| wording | claims made | licence |
|---|---|---|
| `[plain]` | K8 | NOT licensed — what a stranger asks after is a standpoint, and the card's `may NOT: a standpoint` bars it while the card's own `angle: … visitor` names it (the card contradicts itself; see R2) |
| `[plain]` | K9 | NOT licensed — a direction given by somebody; no field holds a person doing the directing, and the act is the parent's (see R2) |
| `[plain]` | K10 | the LACK half is licensed by card `may claim:` — both terms of the predicate are false, so `the town holds neither` is the predicate restated as R-DA-02's flat lack; the CAUSAL half (the direction is refused BECAUSE of the lack) is convicted by `may NOT: a cause` (see R6) |
| `[plain]` slot | `{settlement}` | card `bag:` as above; used once, never as the opener |
| `[plain]` angle | visitor | card `angle: ledger street visitor` |
| `[face] 1` | K8, K9, K10 | as the plain; the parent's causal joint (`because`) is kept in exactly this one wording so the construction survives in the annex; one sentence |
| `[face] 2` | K8, K9, K10 | as the plain; K10 carried as an adjacent standing fact (`and no such building stands in the town`), which states the lack without asserting the causal link |
| `[face] 3` | K8, K9, K10 | as the plain; the stranger is plural, which removes the pronoun a singular stranger would need — the R6 gendered-line breach (CLERK-LAWS §2.2 arm C3(a)) has no purchase on any wording in this pool |

**REFUSALS — six. Every wording is written and none is trimmed (§22).**

1. **R1 · POOL-WIDE: `may NOT: a second fact`.** The card licenses exactly one claim, that the predicate holds. The predicate is itself a conjunction of two absences, so K1 and K2 alone are already two facts on the card's plain words, and every variant carries one to two more beyond them. Nothing can be dropped: arm A6 requires the four faces to be claim-equal to the variant as it stood, and §22 (a)/(e) forbids removing what a sentence stood for. The same card/register conflict is on record from the `Disasters & Famine: granary AND parish care only` and `Invasion & War: walls AND professional garrison` packets: the register card (A1, amendment S2) permits two sentences and a second fact in its own sentence; the licence card forbids a second fact outright. On the card's plain words no wording in this pool is lawful; on the register's, all twelve are. The chair's ruling is owed and this packet decides nothing.

2. **R2 · The card contradicts itself on the standpoint, and variant 3 is the one it convicts.** `angle:` names `ledger street visitor`; `may NOT:` bars `a standpoint`. Variant 3 is a visitor standpoint by its tag, which is not mine to touch, and its only claims (K8, K9, K10) are the stranger's asking, the stranger's being sent on, and the lack behind both. If `may NOT` governs, variant 3 cannot be made lawful in any wording and the pool collapses to two variants, which is a trim by another route, so the ruling matters. Written under the `angle` clause. K9 additionally asserts an unnamed person giving a direction; no field holds one, and no wording names an office, so the act is left agentless in three of the four wordings (`is sent to`, `is directed to`, `get no answer`) and takes a passive in the fourth (`is given no direction`).

3. **R3 · Wall 3 is UNSATISFIABLE for this pool, and this is the pool's structural refusal.** `ABSENCE never opens and never sits beside another ABSENCE` (MOVE-GRAMMAR §1.4 wall 3; R-DA-08; CL-12). This pool's predicate IS two absences, so (a) every wording that carries K1 and K2 places one ABSENCE beside another, and claim-equality requires every wording of variant 1 to carry both; (b) the pool's first licensed fact is a lack, so a wording that does not open on an absence must open on the thing the town does not have, which is what all four of variant 1's wordings do (`No food`, `No reserve`, `Against a bad year`, `Neither food nor care`) and what none of variant 2's do. No rewording cures either limb; a pool whose state key is a double negative cannot obey a wall written for a record that mostly holds facts. Declared here rather than silently broken; the disposition is the chair's, and the honest options are a scoped exemption for double-negative state keys or a walker channel that reports rather than fails on them.

4. **R4 · Variant 1's K3 and K4 are consequences with no consequence field.** The card refuses `a cause`, and K3 asserts the absence of an interval between a failed harvest and hardship, while K4 asserts the course a disease runs where nothing stops it. Both are the parent's own claims, both are structural rather than historical (R-DST-B holds: no wording says a harvest failed, a plague came, or a town suffered — every wording is a standing conditional of the arrangement), and both are kept in all four wordings. **Held, not refused:** no wording takes the future indicative. K3 and K4 are written as present generals or as edges (`is hardship at once`, `arrives with`, `stops only when`, `ends of itself or not at all`), so STATE never FATE holds and the card's `may NOT: a future` is not breached.

5. **R5 · Variant 2 touches `a season` and the near edge of `a count`.** K5's unit is a bad season, and the card refuses a season; the parent's own noun is `season` and it is kept in three of the four wordings, with the fourth carrying it as `hunger` so the annex holds both. K7's counting is an act with no number written and no number implied, which is the nearest a wording in this pool comes to `may NOT: a count`; it is the parent's claim and is kept. **Held, not refused:** what the counting counts is left unstated in every wording, exactly as the parent left it, so nothing is invented and no totality over persons is asserted (the card's REFUSED COLUMNS are clear).

6. **R6 · A stated cause in variant 3, kept once.** The parent's `because there is neither` asserts the lack as the reason for the refused direction, and the card refuses a cause. The claim is kept in all four wordings; the causal JOINT is written in one (`because the town keeps neither`) so the parent's construction stays in the annex, and the other three set the lack beside the refusal without asserting the link (`Neither of them stands here`, `and no such building stands in the town`, `The town holds neither place`). That is the same claim with the causal assertion unstated, not a claim removed. If the chair rules the joint out, three wordings already stand without it and the fourth reverts to the same shape.

**Laws tested and held across all twelve wordings.**
- No em dash, no exclamation mark, no digit, no percent, no question mark, no `which` anywhere in the pool. The parent variant 2 carried `which is to endure it and count afterwards`; the which-tail is gone from every wording and K6 and K7 are carried by a coordinate clause or their own sentence instead (wall 6; R-DA-03; A9). **This is the one hard-wall cure the rewrite performs.**
- No first or second person, no persona, no address to the reader, no imperative. Variant 2's answer is never written as an instruction (`endure it, and count`), which would have read as second person; it is written as a passive or as a third-person act of the town in every wording.
- No future indicative anywhere; no modal `will`; every edge is a present general or a subjunctive.
- No citation of any kind. `source: (none) · SOURCE-UNRESOLVED` licenses none, so no wording names a keeper, a roll, a register, an elder or an authority; arm A13 has nothing to fire on. Nobody is named as giving the stranger a direction, which also keeps R2's agentless reading.
- No named character and no fate of one; no theological claim. The read's `church` term is FALSE under this predicate, so no wording mentions a church, a parish, clergy, prayer or a deity — the DEITY DOCTRINE is held by omission, and the adjacent `granary AND parish care only` pool's prayer language appears nowhere here.
- No totality over persons and no exemption from a duty (the card's REFUSED COLUMNS). `no one here treats the sick` and `no hand here tends the sick` are the predicate's own lack of provision, not a claim about every person in the town.
- No figure and no simile; no sense verb on an abstraction; no inanimate thing acting with intent. `a plague runs until it burns out` is the parent's own idiom and is the pool's one dead-metaphor risk; it is kept in exactly one wording and the other three render K4 without it (`stops only when it has run its length`, `is not ended here, only waited out`, `ends of itself or not at all`). Declared, inherited, not added.
- `may NOT: another civic object of the class store` — at most one store-class noun per wording: `store` (v1 face 2), `reserve of food` (v1 face 1), `granary` (v3 plain), `store of food` (v3 face 1), `grain store` (v3 face 2); the remaining seven wordings name none.
- One term for one thing WITHIN each wording (R-DA-22): no wording says both `store` and `granary`, or both `sickness` and `plague`. `the sick` beside `the sickness` is the deliberate thread echo §1.4.1 licenses (a noun carried forward), not a second term for one thing — the first is the people, the second is the illness.
- The four vocabularies per variant, none a paraphrase of its sibling. Variant 1: set aside/treats/plague · reserve/tends/sickness · keeps no store/nobody at all/waited out · neither food nor care/turns to hardship/ends of itself. Variant 2: answer given/endured/counting · come to one answer/endures/counts · met in one way/bears/takes its count · no difference of answer/bear/count. Variant 3: granary/sick-house/sent to neither · store of food/house of the sick/no direction · grain store/place for the sick/no such building · where grain is kept/where the sick are taken/no answer to either.
- Slot discipline: every wording of variants 1 and 3 carries `{settlement}` exactly once; every wording of variant 2 carries none; each face matches its parent's set exactly.
- T-F8: no wording opens on the proper-typed slot, and the `[plain]` parents are held to the same rule as the faces, because the draw is unweighted over all four and each must stand alone.
- A1 and R-DA-03: every wording is one or two sentences; no third sentence, no qualification carried as a tail. Four wordings are single sentences (v2 face 3, v3 faces 1 and 2, and no other), which is the pool's rhythm spread rather than a shape rule.
- THE THREAD (§1.4.1): in every two-sentence wording the second sentence carries a noun forward — `year` and `sick`/`sickness` (v1 plain, faces 1, 2, 3), `a bad season`/`a sickness` as `Both` and `them` (v2 plain, faces 1, 2), `neither` (v3 plain), `either`/`neither place` (v3 face 3). No wording changes subject mid-passage and hands nothing back. As this pool is the SPINE, every wording closes on a civic noun or a present condition of one that a modifier can pick up after it: `burns out` · `run its length` · `waited out` · `not at all` · `comes after` · `counts afterwards` · `gone by` · `when it is done` · `stands here` · `keeps neither` · `stands in the town` · `neither place`.
- A11 spread: the twelve wordings carry twelve distinct first-two-word pairs — `No food` · `No reserve` · `Against a` · `Neither food` · `The town's` · `A season` · `Hunger and` · `Between a` · `A stranger` · `A newcomer` · `Whoever comes` · `Strangers at`.
- R-DA-05 rhythm: word counts with the slot as one word run 34 / 34 / 33 / 36 for variant 1, 22 / 19 / 25 / 29 for variant 2, 20 / 25 / 27 / 25 for variant 3. The shortest wording is nineteen words and the longest thirty-six; the shortest sentence in the pool is five words (`Neither of them stands here`).
- R-DA-06: the semicolon is spent once in the pool (v1 face 1) and the colon nowhere; every other joint is a comma with a coordinator or a full stop.
- R-DA-04 close kinds vary inside each variant: variant 1 closes on a course, a measure, a condition, an absolute; variant 2 on a sequence, a sequence, a completed span, a completed act; variant 3 on a condition, an absence, a condition, an absence. Variant 2's four closes are the narrowest set in the pool, because K7 fixes the counting as the last claim in a claim-equal family; declared, not hidden.
- Sibling distance (arms A1, A11): no wording restates or contradicts `granary AND hospital`, `granary AND parish care only`, `granary, NO medical provision` or `NO reserves, hospital present`. The siblings' own phrases are avoided throughout — `laid by`, `kept back`, `a place for grain`, `a place for the ill`, `somewhere to put the sick`, `outlast a hungry year`, `a sick one`, `spreads until it stops of its own accord`, `hardship the same season it happens`, `catastrophe`, and every word of the parish family. `against a bad year` is this pool's own parent's phrase and is kept.

**Declared consequences of the rewrite (not refusals).**
- **The which-tail is gone.** Variant 2's parent carried the pool's only relative tail; no wording in this packet contains `which`. This is a wall cure, and it is the only claim-preserving change the walls forced.
- **Zero settlement-openers, down from one.** The BEFORE's variant 1 opened `{settlement} holds no food…`; ARCH §2.5's T-F8 refuses a sentence face opening on a `proper`-typed slot, so no wording in this packet opens on the slot. Wall 10 (R-DA-17) is satisfied at zero rather than at one, and the settlement token's register-wide share falls by one occurrence from this pool. Deliberate, declared, not a trim.
- **The semicolon-as-payoff is gone from variant 1's parent shape.** The BEFORE joined the two lacks to the two consequences with a semicolon that made the second half the first half's payoff; all four wordings now carry the consequences in their own sentence, and the one surviving semicolon (v1 face 1) joins two facts of equal weight rather than a claim to its gloss.
- **`burns out` survives once.** The parent's idiom is kept in exactly one wording so the annex does not lose it; the other three carry K4 in different words. This is a spread choice, not a trim.
- **The pool is banked as unlawful on the card's plain words** (R1, R2, R3) and lawful on the register's. All twelve wordings are written to the ceiling within that failure and no new failing state is added (§21.2, §21.3). The banked count for this pool is three variants and twelve wordings; nothing here should be read as evidence that the pool is lawful.
