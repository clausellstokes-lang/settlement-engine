1. `[ledger]` The country about {settlement} is thick with creatures, and the town holds its wall with people of its own.
   - `[face]` On the town's own entry {settlement} stands in plagued country, behind a wall its people hold.
   - `[face]` Plagued is the country {settlement} sits in, and its wall does not stand without people.
   - `[face]` Creatures are thick about {settlement}, and the people of the town keep hold of its wall.
2. `[street]` A wall about {settlement} is up, and the town's own people hold it where the creatures are thick.
   - `[face]` The wall about {settlement} is up and held, in country thick with creatures.
   - `[face]` Creatures run thick where {settlement} sits, and the wall the town keeps is held by its people.
   - `[face]` At {settlement} the wall is up with people on it, and the country about the town is plagued.
3. `[unfolding]` The town at {settlement} goes on keeping people on its wall, in country that goes on thick with creatures.
   - `[face]` Held by the town's own people, the wall at {settlement} stands where the creatures run thick.
   - `[face]` In plagued country {settlement} keeps its people on the wall.
   - `[face]` Where the country about {settlement} runs with creatures, the town's people go on keeping the wall up.

--- NOTES

**Pool.** DS-DEF-2 · `Beasts & Monsters: plagued, perimeter AND organized force` · draft round 1 · seat Opus 5 (Fable-unvalidated), for the Fable chair.
Card read by `node scripts/prose-licence-card.mjs DS-DEF-2 'Beasts & Monsters: plagued, perimeter AND organized force'` in `$SC/laneRW-DEF2`. The block's annex section re-read whole at `laneRW-DEF2/docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2568-2726` (arms A1 and A11) — the pool's own rows at `:2596-2598`, the six sibling `Beasts & Monsters` spines, and the nineteen spines of the block's other four rows. The selecting branch read at `src/domain/display/stateProse/defenseStateProse.js:336-388` and its caller at `:560-591`.
**3 variants · 12 wordings** (3 parent lines + 9 `[face]` sub-rows) · every wording ONE sentence · ROLE `spine` · FORM `sentence` · MOVE (none declared) · audience player, no mark.
**Slot set: `{settlement}`, exactly once, in every one of the twelve lines** — never sentence-initial (T-F8), never sentence-final (the `…ment` abstract-closer trap, `proseFingerprint.js:147`).

**Words per wording** (whitespace tokens, `{settlement}` counting one):

| variant | parent | `[face]` 1 | `[face]` 2 | `[face]` 3 |
|---|---|---|---|---|
| 1 · `[ledger]` | **19** | **16** | **15** | **16** |
| 2 · `[street]` | **18** | **13** | **17** | **18** |
| 3 · `[unfolding]` | **19** | **16** | **10** | **17** |

mean **16.17** · min **10** · max **19** · sd **2.48** · none under 8, none over 30.

---

## 1. THE ROW SHAPE, AND THE ONE JUDGMENT IT TOOK

The brief asks for "a `[plain]` line and three `[face]` sub-rows". ARCH §2.5's `[plain]` is an ANGLE, not a second marker: item 16 of the architecture's own amendment list reads "`plain` joins the angle palette **for `role: modifier` rows only**", and the example row in §2.5 is a modifier whose angle is therefore `plain`. This pool is a `spine`, and its three variants carry the angles the corpus authored — `ledger`, `street`, `unfolding` — which the brief also orders me to keep exactly as they stand. A row written `` 1. `[ledger]` `[plain]` … `` mints a two-token declaration the generator's typed-declaration branch throws on (`generate-dossier-state-prose.mjs:326-348`).

**So the parent line of each variant carries the variant's own angle tag and IS the `[plain]` line in the brief's sense.** The three sub-rows are `[face]` rows exactly as §2.5 writes them. If the chair wants the literal two-tag row it is a one-line change to each parent and none of the twelve wordings moves.

---

## 2. WHAT THE CARD LICENSES, AND THE THREE COMPONENTS EVERY WORDING CARRIES

The card prints one claim and nothing else:

> `may claim:` that `js)` (=== plagued country, perimeter and force) holds, **as a STANDING fact of the record**

The leaf `` `js)` `` is the card's own reading of the census's synthetic field label `beastsRowSituation(family, perimeter, force) (via BEASTS_ROW_POOL in defenseStateProse.js)`; `claimTokensOf` (`entryWalker.js:818-833`) splits that label on `.` and takes the last segment, so the field's vocabulary is the two dead tokens `js)` and `js)s`. **This is a card defect, reported not worked around** — §5 carries it, with what it costs arm Q.

The claim resolves through the branch to exactly three components, and every one of the twelve wordings states all three (arm A6, claim-equality across a face family):

| component | the read that licenses it | what it does NOT license |
|---|---|---|
| the country about the town is plagued | `settlement.config.monsterThreat` → `measuredMonsterFamily` → `plagued` (`defenseStateProse.js:217-222, 269-273`) | how many creatures, what kind, what they have done, when |
| a perimeter stands | `forces.walls.present` — a non-empty `standingDefenseForces.walls` bucket (`:560`) | what the wall is made of, how good it is, where it runs, a gate or any other work |
| an organized force stands | `garrison \|\| militia` (`:591`) | WHICH of the two, how many, what they do, whether they are paid |

The three are the pool key's own conjunction, not three facts: the key exists because the branch returns `'plagued country, perimeter and force'` only when all three hold, and a wording naming two of them states a different pool's claim. That is why no wording drops one to buy room.

---

## 3. LICENCE, WORDING BY WORDING

Every claim in every line, against the clause of the card that licenses it.

| wording(s) | the claim it makes | the card clause that licenses it |
|---|---|---|
| all 12 | the country about this town is plagued / thick with creatures / runs with creatures | `reads` + `predicate` (`=== plagued country, perimeter and force`); **`may claim: … as a STANDING fact of the record`**; MOVE `PRESENT` (MOVE-GRAMMAR §1.2 row 1 — a standing configuration field licenses a structural clause). `plagued` is the field's own value word and this pool's predicate is that single value, not a union, so naming it is exact |
| all 12 | a wall stands about the town | the `perimeter` limb of the predicate. Named as a bare civic object, never described: no material, no extent, no quality |
| all 12 | people of the town hold it | the `force` limb. Named as `people` and nothing narrower — the branch reads `garrison \|\| militia`, so `garrison`, `militia` and `watch` are each false on part of the firing population (R10) |
| all 12 | `{settlement}` | `bag: {band: RESERVED, route: proper, settlement: proper}`, **FILLED at this block's call sites: `{settlement}`** (`:583`, the composer's `slots`). Never sentence-initial (T-F8) |
| 1-parent, 1-f2, 1-f3, 2-parent, 3-f1 `of its own` / `the town's own` / `people of the town` | the force is the town's, not a hired or foreign one | ⚠ **the narrowest claim in the packet, and it is the branch's.** `garrison \|\| militia` are the town's own institution buckets and `mercenary` is a SEPARATE bucket this branch does not read (`defenseStateProse.js:560-562`), so "the town's own" states which buckets fired and asserts nothing about the one that did not. Written as a possessive, never as a contrast with a hired force (which would be R-DA-02's bare antithesis) |
| 1-f1 `On the town's own entry` | the fact is one the record carries, entered | the `of the record` half of `may claim`. The record is oblique and is never the grammatical subject; it says nothing, holds nothing and names no keeper, so it is neither a `RECORD_CITATION` (`entryLexicons.js:335`) nor a PROVENANCE move (`moveGrammar.js:225`) — see R9. One line of twelve, by the spread rule (Part B §16(4)) |
| 1-f1 `behind a wall`, 3-f3 / 1-parent `about {settlement}` | the wall stands between the town and the country | the `perimeter` limb's own meaning — a perimeter is the object's definition, not an added geometry. No member of `PROVENANCE_LEXICONS.spatial` appears (`arranged around`, `ringed by`, `built along`, `strung along`, `on the far bank` and the rest are all absent) |
| 1-f2 `does not stand without people` | the force limb, stated by negation | the same `force` limb. It matches no member of `CONTRAST_SHAPES` (`entryLexicons.js:304`), no `QUANTIFIER` (`without exception` is the member; bare `without` is not), and no `AMBIGUOUS_EXEMPTION_LEMMA` (`free of` is the member) |
| 3-parent, 3-f3 `goes on` / `go on keeping`; 3-f2 `keeps`; 2-parent, 2-f1 `is up` | the state stands and continues to stand | the **STANDING** clause of `may claim`. The `[unfolding]` angle is realised as durative present ONLY: no future indicative, no change asserted, no cause, no span. MOVE-GRAMMAR §1.2 row 2 needs an event-provenance field for HISTORY and this block holds none, so the eventless continuing reading is the only licensed one |

**The shapes held at zero, each checked against its detector.** No `{settlement}`-final closer. No pronoun closer (`it · them · there · this · that · one`; three natural drafts died on `…people on it.`). No `-ly` adverb. No `-ing` opener (`Held` is a past participle and outside `/ing$/`, the precedent the DEF-11 packet set with `Entered`). No `There is` / `It is` opener and no expletive anywhere. No doubled-adjective shape `\w+(ed|ing|ous|ful|less|ive|al|ant|ent|y) and \w+` — it killed `the town stays walled and its people stay on the wall`, which is why 3-f3 reads as it does. No comma triad, no three-clause chain. No semicolon, colon, em dash, question, exclamation, parenthesis, digit or percent. No `which`. No `will` or `shall`. No `was · were · had · used to · once · formerly · no longer`. No `QUANTIFIERS` member — which is why 1-f2 negates with `not` and never with `no`. No `CARDINAL_WORDS` member and no `both`. No `AUTHORED_MAGNITUDES` member governing `people` (a `COUNT_NOUNS` member, inert while no band word governs it). No `DUTY_PREDICATES` or `DUTY_STEM_NOUNS` member — `muster`, `count` and `register` are all absent, so the pool takes not even a C2 NOTE. No `EXEMPTION_LEMMAS`, no `RELATION_LEMMAS`, no `OFFICE_NOUN_CANDIDATES`, no `SUPPLY_CLAIM_LEXICONS` member (`pressed` above all — the word that cost the DEF-11 pool eight WITHHELD rows for quoting its own key). No `SPECIFICATIONAL_COPULA`: no `is what · is who · is the only · is the one`, and no line of the form `The x … is the y` (1-parent, 2-f1 and 3-parent all open `The` and none carries `is the`). No `BARE_RELATIVE`. No `RECORD_CITATION`. No move detector fires except `PRESENT`: `the walls` (plural) would have fired OBJECT and does not appear (R11), `watch` and `garrison` would have fired INSTITUTION beside a verb, `is kept here` would have fired TRADITION, `cost` and `falls on` CONSEQUENCE, `since the` and `were built` HISTORY, `stands open` OPEN.

**Close kinds across the pool** (`CLOSE_KINDS`, `entryLexicons.js:307-322`), counted rather than characterised: `own · hold · people · wall` | `thick · creatures · people · plagued` | `creatures · thick · wall · up`. Two land on the civic noun the field names (`wall`), five on the condition word (`thick` twice, `creatures` twice, `plagued`), two on the people, three on a verb or a possessive (`hold`, `up`, `own`). **None on a pronoun, none on an abstraction, none on the slot**, and no variant closes twice on one word.

**Sibling distance.** The twelve openers are pairwise distinct on their first two words across the whole pool: `The country` · `On the` · `Plagued is` · `Creatures are` | `A wall` · `The wall` · `Creatures run` · `At {settlement}` | `The town` · `Held by` · `In plagued` · `Where the`. A11's cross-variant rule holds on the three parent lines (`The country` / `A wall` / `The town`). The four rhythms inside each variant are a plain compound, a fronted prepositional frame, an inversion, and a fronted subject or subordinate clause — not four wordings of one shape.

---

## 4. WHAT THIS REWRITE CURES, MEASURED

**(a) The pool stops being an arm-Q debtor, and this is the finding that transfers.** The DEF-11 taste packet established that all 204 listed findings across seven pools and three blocks were raised on shipped SPINE text and none on a writer's modifier face; two of its rows name DS-DEF-2 spines by line. `armQualify` (`entryWalker.js:866-918`) splits a composed unit on sentence boundaries AND on `;`, and withholds every segment after the first that carries no `{slot}`, no band reading and no word claiming a read field. Today's variant 3 of this pool is `…is being spent doing it; the posture is survivable, and survivable is the most that can be said of it here.` — a semicolon and then a trailing coordinate that names nothing typed. **Every composed unit carrying that variant inherits the WITHHELD, whether or not a modifier mounts; and where one does, the finding is raised on a unit the modifier's author cannot reach and, on the taste harness's own attribution, against the modifier's pool.** All twelve rewritten wordings are one sentence with no semicolon (**measured: 12 of 12 split to exactly one segment on arm Q's own `/(?<=[.?!])\s+(?=[A-Z"'(])/` and on `;`**), so the count of Q segments this pool can raise is zero, at every face, for every modifier that ever attaches. The two colons in today's variants 1 and 2 go with them (arm Q does not split on a colon, but the register bars a qualification as a tail — R-DA-03).

**(b) Five unlicensed claims leave the product.** §6 lists them one by one with the law each fails. The pool's slot count, order, keys and angle tags are untouched (§22: the counts only ever rise; today's text stays in the annex history).

**(c) The pool's own template goes.** Today all three variants open on a different noun but land the same way — a statement, then a verdict about it (`answered it properly`, `nobody treats any of it as unusual`, `survivable is the most that can be said of it here`). That trailing evaluative beat is one of the four machine signatures Part B §16(6) names, in three of three variants: the spread measure, not the individual line, is what convicts it.

---

## 5. THE CARD DEFECT, REPORTED (it is not this pool's alone)

`claimTokensOf` reads a field's vocabulary from the LAST dot-separated segment of the field path. This pool's read is the census's synthetic rung-3 label, `beastsRowSituation(family, perimeter, force) (via BEASTS_ROW_POOL in defenseStateProse.js)`, whose last dot-segment is `js)`. Two consequences, both measurable before the wave dispatches further writers:

- **The card's `may claim` line prints `` that `js)` … holds ``**, which is what a writer is handed as the pool's licence. It is legible only because the card also prints the predicate in words beside it.
- **Arm Q's second licence is dead on every pool of this shape.** A segment naming `perimeter`, `force` or `family` — the branch's own argument names — claims nothing, because those words are not in the leaf. What survives is the holder-kind vocabulary: `fieldSynonymsFor` (`fieldSynonyms.js`) attaches this row's `source.kind: muster` nouns (`roll`, `rolls`, `muster`) to the field, so a second segment saying `roll` would be licensed while one saying `perimeter` would not. **The fix is one line where the census builds the label, or a `FIELD_SYNONYM_ROWS` entry citing the branch** — and a synonym row is the cheaper of the two, since the label is the census's own honest naming of a rung-3 read (`defenseStateProse.js:310-313` names the label shape in terms).

This packet needs neither, because no wording of mine has a second segment. **It is reported because the next writer on a rung-3 pool will meet it, and because a card a writer cannot read is the failure mode this whole apparatus exists to refuse.** Vetoable; the census is not my fence.

**The level-1 grammar count, stated rather than assumed.** MOVE-GRAMMAR §3.2 asks a pool of k variants to carry `min(k, |set|)` distinct level-1 grammars, where the set is filtered by which licensing fields the block holds. Filtered for this pool: V2 needs a structural-consequence field (none), V3 a `none-exists` field (all three components are PRESENT), V5 an institution row (the card licenses none), V6 an unresolved state field (none), V7 event provenance (none), V8 a `not-held` field (none). **V1 (`PRESENT`) is the only member whose fields are non-null**, and V4 (`OBJECT → PRESENT`) is reachable only by wording — `moveGrammar.js`'s OBJECT detector fires on the literal `the walls` and on nothing else this pool can lawfully say. So `min(3, 1) = 1` and the rule is satisfied at one; the pool's variation is carried by rhythm and vocabulary, which is what the owner's four-faces law asks for anyway. **The sitting row:** whether a spine whose predicate is a single standing conjunction can ever show a second level-1 grammar, and whether the plural `the walls` — which is the field's own shape, a non-empty bucket — should be the block's name form so the OBJECT member opens. I decline to take that unilaterally (R11).

---

## 6. REFUSALS (a refusal is a result)

Every claim today's three variants carry that is not in the card, with the law it fails. None is carried forward; each is one variant's own text and stays in the annex history under §22.

- **R1 — `and the town has answered it properly` (variant 1), REFUSED.** VERDICT is one of the moves that do not exist anywhere in the estate (MOVE-GRAMMAR §1.3); a rating word is licensed only by a typed rating field and the card holds none. `properly` is also an evaluative adverb on the town's own arrangement (R-DA-10, R-DA-12).
- **R2 — `and both are in use constantly` (variant 1), REFUSED.** Three faults in five words: `both` is a count over a closed pair (the card's `may NOT: a count`); `in use` asserts a usage the field does not hold — `walls.present` and `garrison || militia` are presence flags and say nothing about employment; `constantly` is a frequency claim and an adverb the register holds at a floor.
- **R3 — `is not an emergency arrangement, it is the week's work` (variant 2), REFUSED.** A CONTRAST is licensed only where the rejected alternative names a sibling pool key or a sibling band (R-DA-02; MOVE-GRAMMAR §1.4 wall 5). "An emergency arrangement" names neither — the six sibling `Beasts & Monsters` keys are `perimeter but NO force`, `NO perimeter and NO force`, the two `frontier` keys and the two `settled` keys, and none of them is that. `the week's work` is a span, which the card refuses beside the season.
- **R4 — `the rotations run, the gates close on time` (variant 2), REFUSED.** A gate is `another civic object of the class 'wall'`, refused on the card's own line; a rotation and a closing time are duties and schedules no institution row carries (CLERK-LAWS §1.3; the `whatItDoes` column is `closed: false` and the walker's C2 arm judges a duty against it). The three-item chain is also R-DA-10's triad by habit.
- **R5 — `and nobody treats any of it as unusual` (variant 2), REFUSED.** FEELING is a non-move (MOVE-GRAMMAR §1.3): no field carries mood, belief or how a thing is taken. `any` is a `QUANTIFIER` over an open column, which the card's REFUSED COLUMNS line closes permanently.
- **R6 — `What {settlement} has built` (variant 3), REFUSED.** An act of construction is HISTORY, licensed by an event-provenance field alone (MOVE-GRAMMAR §1.2 row 2; R-DST-B). This block holds none — `walls.present` is a standing bucket, and R-DA-19's own words are that the causal clauses here are capability clauses and never historical ones unless the history surface supplies the ancestry, which the block's PROVENANCE fence at `:2579-2592` states in terms.
- **R7 — `and is being spent doing it` (variant 3), REFUSED.** CONSEQUENCE is double-licensed by event provenance × a household, trade or office row (MOVE-GRAMMAR §1.2 row 7; R-DA-19; H-3). Neither half exists here, so the clause states a cost against no field. ⚠ **And no instrument catches it:** `moveGrammar.js`'s CONSEQUENCE detector is `cost · costs · paid for · pays for · falls on · fell on · the bill · charged to · comes out of · is borne by · at the expense of · at a loss · the loss is`, and `is being spent` matches none of them. The refusal is the licence's, not a detector's — **and the gap is a finding for the walker lane**, because a cost clause phrased with `spent`, `spends`, `eats`, `takes out of` or `comes off` walks past the arm on any pool in the wave.
- **R8 — `the posture is survivable, and survivable is the most that can be said of it here` (variant 3), REFUSED.** A summarising second beat closing on a maxim: R-DA-12's gnomic closer, R-DA-03's second-sentence summary (figure → 0.000), and the machine signature Part B §16(6) names. It is also the exact segment shape arm Q withholds (§4(a)). `the most that can be said` is a claim about the record's own limits with no `not-held` field behind it, which is R-DA-08's honest-absence licence used without the licence.
- **R9 — THE SOURCE CITATION, REFUSED; the provenance count for this pool is 0, deliberately.** The card prints `source: muster · standing LICENSED`, and a citation is therefore *permitted where the budget allows*. It is not taken, because Part B §24 caps the budget at one per unit AND restricts it to one of register-card S3's three reasons — two accounts that disagree, a count from an interested party, a record whose keeper is a power. None holds: there is no second account of this branch, the claim is not a count, and the muster is not a power over whether a wall stands. §24's own measurement is that the exemplar registers with raw text cite at 0 per 786 sentences, so a citation habit is a refuter's finding. MOVE-GRAMMAR §4.4.3 adds that a citation on a fact whose holder is the office itself is a finding in its own right. **Chair-vetoable:** if the sitting wants the move exercised on this pool, the lawful form is `The muster roll at {settlement} carries the wall and the people to hold it, in plagued country.` — and it should be one face of twelve, never four. No line matches the PROVENANCE detector's holder kinds; 1-f1's `the town's own entry` is a common noun naming no keeper, which is precisely the narrowing SITTING §Q.2 made and `moveGrammar.js:205-225` records.
- **R10 — `garrison`, `militia`, `watch`, `soldiers`, `the guard`, REFUSED AS TERMS FOR THE FORCE.** The branch reads `garrison || militia` (`:591`), so either institution word is FALSE on the half of the firing population that has the other. `watch` is a third bucket this branch never consults, and it fires `moveGrammar.js`'s INSTITUTION detector beside any of eleven verbs. `people` is the one term true on the whole population, and it is the corpus's own word in this pool.
- **R11 — `the line`, `the perimeter`, `the works`, and the plural `the walls`, REFUSED AS TERMS FOR THE WALL.** R-DA-22 is one term for one thing and one name form; today's block uses `wall`, `line`, `perimeter` and `works` for one object across its twenty-six pools, and a pool that adds a fourth spelling makes the block's problem worse while the other twenty-five are being rewritten in parallel. `wall` is chosen because it is this pool's own first word for the thing and the one `CLOSE_KINDS.civicNoun` member available. The plural `the walls` is the single wording that would fire the OBJECT move detector and buy the pool a second level-1 grammar (§5) — **it is a block-wide name-form ruling and therefore the chair's, not one writer's**, and I flag it rather than take it.
- **R12 — QUANTIFIERS, REFUSED.** `every`, `all`, `no`, `none`, `any`, `whole` are licensed only by a `closed` column and the card's REFUSED COLUMNS line shuts that door permanently. Two drafts died here: `no part of the wall stands empty` and `every stretch has its people`.
- **R13 — EVALUATIVE CONDITION WORDS, REFUSED.** `bad country`, `hard country`, `dangerous ground`, `a serious perimeter` are evaluative adjectives on a place, which R-DA-10 holds at zero in the record registers. The field's own value word `plagued` and the corpus's own `thick with creatures` do the same work and are licensed.
- **R14 — THE PURPOSE CLAUSE, REFUSED.** `a wall against them`, `the wall faces the country`, `built for what comes out of the country` all state what the wall is FOR. Purpose is a cause wearing a different coat, and the card's `may NOT: a cause` bars it; the conjunction of the three components is the whole of what the branch knows.
- **R15 — THE LENGTH TARGET, REFUSED, and the figure it fails is named rather than hidden.** R-DA-05's PROBE figure for R1 is a within-pool word-count sd of `2.9 → ≥ 4.0`. This pool reads **2.48**, under the floor, and I did not pad to reach it. The reason is structural and worth the sitting's attention: that figure was measured over VARIANTS whose claims differ, so their length spread tracks a load that genuinely varies. **A face family is claim-equal by law (arm A6) — all twelve wordings here carry the identical three-component claim — so the load is constant and there is nothing lawful for the length to vary on except padding.** §21.4 names padding as the regression: compression that rewards the reader is the ceiling, and a one-claim sentence stretched to nineteen words to buy a band position trades density for plainness. The spread is widened where it could be honestly (a 10-word line beside a 19-word one, and one line noticeably shorter than its siblings inside every variant); the position is reported as information only (§21.1). **The figure needs a grain before it gates a face family, exactly as §16.2 gave the three numbers one.**

---

## 7. WHAT THE SITTING SHOULD TAKE FROM THIS PACKET

1. **A rung-3 read makes the licence card and arm Q's second licence unreadable** (§5). The card hands the writer `` that `js)` … holds ``, and no segment naming `perimeter`, `force` or `family` can claim the field. One `FIELD_SYNONYM_ROWS` entry citing the branch fixes both. Do it before the other twenty-five DS-DEF-2 pools are dispatched — the four rung-3 rows of this block all share the shape.
2. **The spine rewrite is where the taste sample's findings actually get paid down** (§4(a)). Twelve one-sentence wordings with no semicolon means this spine contributes zero arm-Q segments to every composed unit that ever mounts a modifier on it. If the wave wants the sample's 204-finding debt gone, the instruction to spine writers is one line: **one sentence per face, and no semicolon**, unless the second segment names a second typed field in words.
3. **`min(k, |set|)` distinct level-1 grammars is satisfied at ONE on a conjunction spine, and that is arithmetic, not a shortfall** (§5). Twenty-two of DS-DEF-2's twenty-six pools are conjunction branches of the same shape. Either the rule is read as this packet reads it, or the block will report twenty-two grammar failures that no wording can cure.
4. **A within-pool length-spread figure measured on variants cannot gate a claim-equal face family** (R15). It will read as a failure on every well-written face set in the wave, and the only way to pass it is to pad.
5. **The block needs one name form for the perimeter and one for the force before its pools land** (R10, R11). Twenty-six pools are being written in parallel against `wall`/`line`/`perimeter`/`works` and `garrison`/`militia`/`watch`/`soldiers`; R-DA-22 is a register wall, and no writer can hold it alone.
