1. `[ledger]` The country about {settlement} is thick with creatures, and the town's people hold the wall.
   - `[face]` On the town's own entry {settlement} stands in plagued country, behind a wall its people keep.
   - `[face]` Plagued is the country {settlement} sits in, and the wall does not stand without people.
   - `[face]` Creatures are thick about {settlement}, and its wall is held by people of the town.
2. `[street]` A wall about {settlement} stands with the town's own people on it, and the country beyond runs with creatures.
   - `[face]` The wall about {settlement} is up and held by the town's people, in plagued country.
   - `[face]` Creatures run thick where {settlement} sits, and the wall is kept by its people.
   - `[face]` Outside {settlement} the country is plagued, and the town has people on the wall.
3. `[unfolding]` The people of {settlement} go on keeping the wall, in country that stays thick with creatures.
   - `[face]` Held by the town's own people, the wall at {settlement} still stands where the creatures run thick.
   - `[face]` In plagued country {settlement} keeps its people on the wall.
   - `[face]` Where the country about {settlement} runs with creatures, the wall stays in the keeping of the town's people.

--- NOTES

**Pool.** DS-DEF-2 · `Beasts & Monsters: plagued, perimeter AND organized force` · REFINEMENT round (Part B §21 phase 2), seat Opus 5 (Fable-unvalidated), a different author from the drafter, for the Fable chair.
Card re-read first by `node scripts/prose-licence-card.mjs DS-DEF-2 'Beasts & Monsters: plagued, perimeter AND organized force'` in `$SC/laneRW-DEF2`. Draft read whole; the pool's rows re-read in place at `laneRW-DEF2/docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2594-2606` with the block head and its six sibling `Beasts & Monsters` pools. The detector lexicons read at source rather than trusted from the draft: `src/domain/prose/entryLexicons.js` (QUANTIFIERS `:93`, DUTY_PREDICATES `:110`, EXEMPTION_LEMMAS `:143`, AMBIGUOUS_EXEMPTION_LEMMAS `:161`, CONTRAST_SHAPES `:304`, CLOSE_KINDS `:312`, SPECIFICATIONAL_COPULA `:332`, RECORD_CITATION `:335`) and `src/domain/prose/moveGrammar.js` CLAUSE_DETECTORS + `clauseUnits`.

**One for one: 3 variants · 12 wordings · every one refined, none added, none dropped, none merged, no vid moved, no angle tag touched** (§22 (a)/(e)). Every wording still one sentence, one `{settlement}`, never sentence-initial, never sentence-final. Claim set unchanged: all twelve carry the three components of the key's conjunction — the country is plagued, a wall stands, the town's people hold it (arm A6).

**Per face — what changed, and the law or target behind it.**

1. `[ledger]` parent — dropped the slack tail `holds its wall with people of its own` for `the town's people hold the wall`: the line closes on the civic noun the field names instead of on a possessive, so the modifier that mounts after it has a noun to pick up (THE THREAD, MOVE-GRAMMAR §1.4.1; R-DA-04's civic close). Nineteen words to fifteen with no claim lost — compression that rewards the reader, not plainness (§21.4).
2. `[ledger]` f1 — `behind a wall its people hold` → `behind a wall its people keep`. The verb now differs from the parent's `hold`, so the four faces of the variant carry four verbs (`hold` · `keep` · `stand` · `held by`) rather than two: sibling distance inside the family (owner's four-faces law; §21.1's widest-distance target). The record-frame `On the town's own entry` is kept deliberately — it is the pool's one oblique touch of the record, names no keeper, and is neither a RECORD_CITATION (`entryLexicons.js:335` needs a book plus a reporting verb) nor a PROVENANCE move (`moveGrammar.js` needs a holder kind).
3. `[ledger]` f2 — `its wall` → `the wall`. The draft's possessive sat one noun after `the country`, so the only reading that parses is the right one but the ear stumbles; the definite article carries the same reference with no stumble. Same length, and the inversion with its negation — the pool's strongest single rhythm — is untouched.
4. `[ledger]` f3 — `the people of the town keep hold of its wall` → `its wall is held by people of the town`. `keep hold of` was the limpest predicate in the packet; the passive puts the wall in subject position, which no other face of this variant does, and moves the close off the civic noun onto `town`, so the ledger variant now closes four different ways (`wall` · `keep` · `people` · `town`). Same claim, one word shorter.
5. `[street]` parent — rebuilt. The draft subordinated the country to a locative tail (`where the creatures are thick`) and closed on the pronoun-adjacent `hold it`; now the wall and its people stand in the main clause and the country is a coordinate that closes on `creatures`. All three components are asserted at equal grammatical weight, which is what a conjunction key's spine owes (the card's `predicate`, and the draft's own §2 reasoning that no component may be demoted to buy room).
6. `[street]` f1 — `in country thick with creatures` → `held by the town's people, in plagued country`. The draft's tightest line was also its one agentless line: `held` asserted the force limb without naming whose. The agent is now named and the country claim takes the field's own value word, at fifteen words against thirteen. Still the shortest line of its variant.
7. `[street]` f2 — `the wall the town keeps is held by its people` → `the wall is kept by its people`. A stacked relative plus a passive was the packet's clumsiest clause; one passive says the same thing in three fewer words. Density law again: nothing is explained, a joint is removed.
8. `[street]` f3 — rebuilt from `At {settlement} the wall is up with people on it` to `Outside {settlement} the country is plagued, and the town has people on the wall`. The draft repeated its own parent's `with people on it` inside one variant — the tic a refuter names first (Part B §16(4): the same shape everywhere is the template). The new opener keeps the pool's twelve opener pairs pairwise distinct while freeing `At` for the block's other pools.
9. `[unfolding]` parent — two faults cured. `The town at {settlement}` was a false apposition (the slot is the town, so the line located the town at itself); and `goes on … goes on` doubled the angle's own formula inside one line, which is CL-9's loop rather than the licensed formula. Now `go on keeping` once, with `stays` carrying the second durative, and the subject is the people.
10. `[unfolding]` f1 — the smallest change in the packet, and deliberately so: `stands` → `still stands`. The line was already at the ceiling (the participial opener, the concrete close), and §21.2 pushes a face toward the ceiling, it does not move a face that is there. `still` puts the angle's durative in the face rather than leaving the parent to carry it twice.
11. `[unfolding]` f2 — unchanged wording, and this is a declared refusal rather than an oversight: ten words, four distinct components of rhythm, the field's own value word first, the civic noun last. Every rewrite I tried either padded it toward a length figure (the regression §21.4 names) or spent a quantifier or a temporal totality (`never without people`) that the card's REFUSED COLUMNS line shuts. It is the pool's shortest line and stays so: the minimum is held at ten.
12. `[unfolding]` f3 — `the town's people go on keeping the wall up` → `the wall stays in the keeping of the town's people`. Third use of `go on` in one variant was the angle turning into a rota; `in the keeping of` puts the antique air in the noun where the register card puts it, and closes on the people rather than on the adverbial `up`.

**Measures, as information (§21.1 — position in a band is reported, never targeted).**
Word counts (whitespace tokens, `{settlement}` counting one, both sets measured on one tokenizer): refined 15 · 16 · 15 · 15 | 19 · 15 · 14 · 14 | 16 · 17 · 10 · 18 — mean 15.33 · min 10 · max 19 · sd 2.27; draft mean 16.17 · min 10 · max 19 · sd 2.59. **The mean and the sd both FELL, and that is declared rather than dressed:** four wordings were compressed (the parent, both `[street]` faces after the first, the `[unfolding]` close) and nothing was lengthened to hold a figure. R-DA-05's `≥ 4.0` within-pool sd was already under the floor at the draft and is further under it now; the draft's R15 refusal is the reason and it stands unchanged — a claim-equal face family has no lawful load for its length to vary on, and padding to restore the figure is exactly the regression §21.4 names. Min and max are held, so the pool's range is unchanged.
Openers, first two words, all twelve pairwise distinct: `The country` · `On the` · `Plagued is` · `Creatures are` | `A wall` · `The wall` · `Creatures run` · `Outside {settlement}` | `The people` · `Held by` · `In plagued` · `Where the`. The three parent lines are distinct on A11's own rule.
Closes: `wall` · `keep` · `people` · `town` | `creatures` · `country` · `people` · `wall` | `creatures` · `thick` · `wall` · `people` — wall ×3, people ×3, creatures ×2, country ×2, keep ×1, town ×1, thick ×1. None on a pronoun, none on an abstraction, none on the slot, no variant closing twice on one word.
The force is named four different ways inside every variant (`the town's people` · `its people` · bare `people` · `people of the town` · `the town's own people` · `the people of {settlement}`), and the country four different ways (`thick with creatures` · `plagued country` · `creatures are/run thick` · `runs with creatures` · `stays thick with creatures`).
Arm Q: twelve of twelve split to exactly one segment on `/(?<=[.?!])\s+(?=[A-Z"'(])/` and on `;` — the spine contributes zero withheld segments to any composed unit that mounts on it, which is the draft's finding preserved.
Detectors re-checked line by line at source: no quantifier member (`every · all · each · only · none · any · no · whole · entire`), no duty predicate or stem noun, no exemption lemma (`without` is not a member; `free of` is), no CONTRAST_SHAPES match, no SPECIFICATIONAL_COPULA match, no RECORD_CITATION, no future indicative, no digit, no em dash, no exclamation, no question, no semicolon, no colon, no `which`, no `-ly` adverb, no `-ing` opener, no expletive, no pronoun close, no `\w+(ed|ing|ous|ful|less|ive|al|ant|ent|y) and \w+` shape. `the wall` stays singular in all twelve, so `moveGrammar.js`'s OBJECT detector does not fire and every clause classifies PRESENT, as V1 requires. One term for the wall (`wall`) and one for the force (`people`) throughout — the draft's R10/R11 refusals are kept, and the block-wide name-form ruling they flag is still the chair's.

**Provenance count for this pool: 0, unchanged.** Part B §24 caps the budget at one citation per unit and restricts it to S3's three reasons; none holds here (no second account, no count, the muster is no power over whether a wall stands), and §24's own measurement makes a citation habit the refuter's finding. `the town's own entry` names no keeper and is not the move.

**Carried to the chair, unchanged from the draft and not re-argued here:** the rung-3 licence-card defect (`may claim: that js) … holds`), the `min(k, |set|)` grammar count satisfied at one on a conjunction spine, the within-pool length-spread figure that cannot gate a claim-equal face family, the block's missing name-form ruling for the perimeter and the force, and the CONSEQUENCE detector's blindness to `spent · spends · eats · takes out of · comes off`.
