# DS-DEF-2 · pool `Disasters & Famine: NO reserves, hospital present` · REWRITE draft, round 2
Seat: Opus 5 — Fable-unvalidated. Block DS-DEF-2, role spine, form sentence, angles `ledger street unfolding`.
Written under the card printed by `node scripts/prose-licence-card.mjs DS-DEF-2 'Disasters & Famine: NO reserves, hospital present'` in laneRW-DEF2. Three variants in, three variants out: same vids, same order, same bracketed tags, same slot sets, none added, none removed, none merged. The typed lines of the pool are untouched and are not repeated here.

**ROUND 2 ANSWERS THE GATE, MEASURE BY MEASURE** (each cure named at NOTES §E).

## THE ROWS (paste under the pool's bold heading; the typed lines above it are untouched)

1. `[ledger]` The hospital at {settlement} can take an outbreak in and hold it there, and the town keeps nothing back against a failed harvest. The failure and the hardship fall in the same season.
   - `[face]` A fever at {settlement} can be treated and kept from spreading. Against a poor harvest the town has laid by nothing, and the failed harvest is the harvest that brings the want.
   - `[face]` The sick at {settlement} are taken in and kept apart. What the town has laid up against a failed harvest is nothing, and hardship is not deferred past the season of the failure.
   - `[face]` An outbreak at {settlement} can be treated and confined. Behind a failed harvest the town keeps nothing at all, and the hardship arrives with the harvest.
2. `[street]` The town is set better against sickness than against hunger, and takes no comfort in the ordering. Towns are ordinarily set the other way about.
   - `[face]` Sickness is better met here than hunger. The town takes no ease in that, and the order is uncommon among towns.
   - `[face]` A plague would find more provision here than a poor harvest would, and the town draws no comfort from it. Such an arrangement is rare.
   - `[face]` This town is readier against a sickness than against hunger, and no ease comes to it from the difference. That ordering is seldom found in a town.
3. `[unfolding]` The provision at {settlement} answers the sickness the town has met and stops short of the hunger it has not. The gap between them is not being closed.
   - `[face]` A hospital at {settlement} stands against the sickness the town has met, and nothing stands against the hunger it has not. Nothing in hand is making up the difference.
   - `[face]` Care for the sick at {settlement} matches a trouble the town has known, and no store matches the trouble it has not. The arrangement is not being altered.
   - `[face]` Against sickness {settlement} has a hospital, and against hunger it has nothing. The town has met the sickness and not the hunger, and the difference is left where it stands.

--- NOTES

## A. The row grammar, corrected (the gate's refusal)

The numbered row carries the variant's ANGLE tag and then the wording itself. No `[plain]` token is written anywhere. Round 1 wrote `` 1. `[ledger]` `[plain]` … `` and the gate refused it: a second bracketed tag on the numbered row folds into `marks`, and the contract test reds with `unclassified: [ 'plain' ]`. ARCH §2.5 routes only `` `[grammar: Vn]` `` to `marks`; on a spine variant the angle tag is the row's one bracketed token and the parent wording is the row itself. The block's already-rewritten siblings are written the same way. The three `` - `[face]` `` sub-rows are unchanged in shape.

## B. What the card licenses, and what every wording claims

The card's one substantive clause is `may claim: that js) (=== no reserves, hospital) holds, as a STANDING fact of the record`. That predicate has two readable halves — the HOSPITAL half (a hospital is present and can treat and hold sickness) and the RESERVES half (no food is kept against a bad harvest). Everything beyond those two halves in this pool is INHERITED from the shipped variant and is refused at §D, never dropped: arm A6 requires the four wordings of a variant to be claim-equal to the variant as it stood, and §22 (a)/(e) forbids removing what a sentence stood for.

Clause by clause:

- **`bag: {band: RESERVED, route: proper, settlement: proper}`, FILLED `{settlement}`.** `{settlement}` appears exactly once in all four wordings of variants 1 and 3 and in none of variant 2, matching each parent's slot set (ARCH §2.5's face refusal). No band word (`STRONG`, `ADEQUATE`, `WEAK`, `CRITICAL`) appears; `{route}` is unfilled and unused.
- **`seat/form: (not a seat-taker) / sentence`.** All twelve wordings are full sentences; no fragment is written, so §2.5's fragment-opening refusals cannot fire. None opens on the `proper`-typed slot (T-F8).
- **`source: (none) · standing SOURCE-UNRESOLVED` / `NO citation is licensed`.** Obeyed. No wording names a roll, a book, a register, a keeper or an authority, so arm A13 has nothing to fire on and the provenance move (MOVE-GRAMMAR §4.4.3) is absent by licence rather than by budget. §24's ceiling of one citation per unit is not approached.
- **`covert: no`.** Obeyed; nothing is marked or written dm-only.
- **`relation` / `attach` (a spine takes neither).** Nothing declared; the connective phrases inside a wording are the wording's own, not a leaf's.
- **`REFUSED COLUMNS, always`.** No totality over persons (see cure C3), no exemption, no named character, no theological claim. The prayer/parish vocabulary of the sibling pools is absent here, so the deity doctrine is not engaged at all.

Claim by claim. The four wordings of a variant are claim-equal, so one map serves all four (arm A6).

**Variant 1 `[ledger]`** — K1 the hospital can treat sickness and hold it where it starts; K2 the town keeps no food against a bad harvest; K3 a crop failure becomes hardship in the season of the failure.
- **K1 — LICENSED**, `may claim`, the HOSPITAL half. Written as a present capability of a named civic object in all four (`take an outbreak in and hold it there` · `treated and kept from spreading` · `taken in and kept apart` · `treated and confined`), never as an act performed, so no event provenance is implied and R-DA-19's structural half is respected.
- **K2 — LICENSED**, `may claim`, the RESERVES half. Written as a flat LACK (R-DA-02's LACK limb) in all four, with no completing "but", and never as the opening move of the unit (MOVE-GRAMMAR wall 3).
- **K3 — REFUSED**, three ways by the card's own `may NOT` line: **a season**, **a cause**, **a second fact**. Inherited from the shipped variant; kept in all four; refusal R1.

**Variant 2 `[street]`** — K4 the town is better set against sickness than against hunger; K5 that ordering is unusual among towns; K6 the town takes no comfort in it.
- **K4 — LICENSED**, `may claim`. A comparison of the predicate's own two halves is a measurement in words, which R-DA-11 permits, and the rejected alternative is named by this block's four sibling `Disasters & Famine` pool keys, which is what R-DA-02 requires of a CONTRAST. Written as a comparative in all four rather than as a bare "X, not Y" antithesis, so R-DA-02's shape ceiling is not spent four times, and never fronted as the subject (wall 5).
- **K5 — REFUSED.** A claim about settlements at large; no field of this block holds a distribution over towns, and R-DA-12's generalisation test is engaged. Inherited; kept; refusal R2.
- **K6 — REFUSED.** A verdict or feeling about the arrangement; MOVE-GRAMMAR §1.3 holds no FEELING move and no VERDICT move anywhere in the estate. Inherited; kept; refusal R3. Its person-totality was struck as a form cure (C3).

**Variant 3 `[unfolding]`** — K7 the town is arranged against the sickness it has met; K8 it is not arranged against the hunger it has not met; K9 nothing in hand is correcting the imbalance.
- **K7, K8 — the STRUCTURAL halves are LICENSED** (`may claim`, the two halves of the predicate); **the HISTORICAL half of each is REFUSED** — that the town has met the one trouble and not the other is an event claim with no event-provenance field, barred by `may NOT: a cause` and at the wall by R-DA-19 and MOVE-GRAMMAR §1.2 (a HISTORY move may not exist in R1 STATE). Inherited; kept in all four in the perfect aspect (`has met` · `has met` · `has known` · `has met`) rather than as a bare past event, so the breach is not enlarged; refusal R4.
- **K9 — REFUSED** by `may NOT: a second fact`. Inherited; kept; refusal R5. Rendered as a standing condition of the arrangement in three wordings (`is not being closed` · `is not being altered` · `is left where it stands`) and in the parent's own absence shape in exactly one (`Nothing in hand is making up the difference`), so the pool holds one wording in the before's shape for the refuters to compare.

## C. Form cures carried from round 1 (a claim moved in none of them)

- **C1 · The which-tail is struck.** The shipped variant 2 reads `…, which is an unusual way round and does not comfort anyone`. `, which` is a wall (R-DA-03; ARCH §2.5's connectives row bars it outright; MOVE-GRAMMAR wall 6). The qualification takes its own sentence or its own comma-and clause in all four wordings. Claim set unchanged. **This is the cure the gate's one failing measure asked for; see §E.**
- **C2 · The three-claim tail is split.** The same parent packed two further claims into one tail. R-DA-03 bars the third segment and the tail form; every wording in this packet carries at most two sentences and no appositive tail, and the 3+-segment share for the pool is zero.
- **C3 · The totality over persons is struck.** `does not comfort anyone` quantifies over persons, which the card's `REFUSED COLUMNS, always` bars unconditionally and which R-DA-15 licenses only on a `closed` column. The quantifier goes, the claim stays (`takes no comfort in the ordering` · `takes no ease in that` · `draws no comfort from it` · `no ease comes to it`). Removing a barred quantifier is a form cure, not a claim removal.
- **C4 · Zero settlement-openers, down from two.** T-F8 refuses a sentence face opening on a `proper`-typed slot, and wall 10 caps settlement-openers at one per pool. The shipped variants 1 and 3 both opened on `{settlement}`. No wording in this packet opens on the slot; the token's register-wide share falls by two occurrences from this pool. Declared, not a trim.
- **C5 · Variant 2 stays slotless** in all four wordings, matching its parent exactly.

## D. Refusals — the laws this pool cannot meet inside its card

- **R1 · Variant 1's third claim is refused three ways.** `a crop failure here becomes hardship the same season it happens` asserts a season, a cause and a second fact, all three on the card's `may NOT` line. Dropping it would break claim-equality and red the C-pair arm (R-DA-20 as amended: the AFTER carries the BEFORE's typed claim set), so it is written in every wording and refused here. **Not curable by wording.** HELD from round 1.
- **R2 · Variant 2's second claim generalises over settlements.** No field of DS-DEF-2 holds a distribution over towns. Inherited, kept, **not curable by wording.** HELD from round 1.
- **R3 · Variant 2's third claim is a feeling or a verdict.** FEELING and VERDICT are non-moves estate-wide (MOVE-GRAMMAR §1.3), and the card licenses no such claim. Inherited, kept, **not curable by wording.** HELD from round 1.
- **R4 · Variant 3's history has no provenance field.** That the town has met sickness and not hunger is the sharpest wall breach in the pool: R-DA-19 and MOVE-GRAMMAR §1.2 bar the HISTORY move from R1 STATE outright, not merely off this card. Inherited, kept, **not curable without dropping a claim.** HELD from round 1.
- **R5 · Variant 3's fourth claim is a second fact, and in one wording a second adjacent absence.** `nothing in hand is correcting the imbalance` is barred by `may NOT: a second fact`, and because it follows K8, which is itself an absence, the parent's reading also breaches wall 3 (an ABSENCE never sits beside another ABSENCE). Three wordings cure the WALL by stating the claim as a standing condition; the first `[face]` keeps the parent's absence shape deliberately. The CLAIM stays refused in all four. HELD from round 1.
- **R6 · The card contradicts itself on the standpoint.** `angle:` names `ledger street unfolding`; `may NOT:` bars "a standpoint". The `[street]` and `[unfolding]` tags are standpoints and are not a writer's to touch, so the rows are written under the `angle` clause. A chair ruling is owed on which line governs; the same conflict is raised in the block's other packets, so it is a card-grammar question and not a pool question. HELD from round 1.
- **R7 · Every variant carries more than one fact.** The card's `may NOT` bars "a second fact"; variants 1 and 2 carry three claims and variant 3 carries three with a historical half on two of them. A1 permits one or two sentences and the register card's amendment S2 permits a computed consequence to ride as a clause, so this is a card-versus-register conflict rather than a writer's fault. On the card's plain words **no wording in this pool is lawful**, and the pool is banked accordingly (§21.2: banked with its faces, never trimmed). The rows are written out in full rather than left empty because each is lawful against the block's own typed STATE-KEY, which is what Part B §9 ruling 5 requires. HELD from round 1.

**Net: three variants written, twelve wordings written, zero variants withheld, seven refusal rows, all seven inherited and none newly added by this round.** Nothing in this packet should be read as evidence that the pool passes on the card's plain words.

## E. THE GATE'S FAILURES, EACH ANSWERED

1. **REFUSAL · a second bracketed tag on the numbered row (`` `[ledger]` `[plain]` ``), folding into `marks`, contract test red with `unclassified: [ 'plain' ]` — CURED.** The `[plain]` token is struck from all three numbered rows; each row's only bracketed token is its angle tag. See §A. This is what blocked round 1 from landing, which is why the gate measured the pool's PREVIOUS rows and not round 1's text.
2. **`shapes.whichTailRate` (over) = 26.027 band-widths on 1 of 3 faces — CURED, to zero.** The measured text was the shipped rows, whose variant 2 carries `…, which is an unusual way round and does not comfort anyone` — one `, which` tail in a three-wording pool, which is the 26.027 band-widths. **No wording in this packet contains the word `which` in any position**, so the pool's rate is 0.0000, inside the exemplar band 0.0000–0.0370 at martin-chronicle's own floor, and the DEPTH is zero rather than 1.75 band-widths or more. The cure is C1 above and it is a form cure: the qualification takes its own sentence or its own comma-and clause, and variant 2's claim set is unchanged.
3. **No joint metric is allowed to take the depth the which-tail vacated.** The pool contains **zero colons and zero semicolons** across all twelve wordings (round 1 carried one of each), so `punctuation.colonRate` and `semicolonRate` are both zero and neither can carry a depth in the way `colonRate` did on the block's `Beasts & Monsters` sibling. Zero em dashes, zero digits, zero percents, zero exclamations, zero question marks.
4. **Nothing else the gate owns is moved the wrong way.** `closers.pronounRate` is **zero** (no wording closes on a pronoun); the 3+-segment share is **zero**; the settlement-opener count is **zero** (T-F8 and wall 10); the sentence-count ceiling of two holds in all twelve; the within-pool length spread is widened, not narrowed (§F).

**This round moves a failing owned measure** (`shapes.whichTailRate`, 26.027 band-widths to 0.000) **and clears the refusal that blocked the round-1 text from being measured at all. It is not a dry round.**

## F. Declared craft properties (for the refuters and the gate)

- **Openers.** Twelve distinct first-two-word pairs: `The hospital` · `A fever` · `The sick` · `An outbreak` · `The town` · `Sickness is` · `A plague` · `This town` · `The provision` · `A hospital` · `Care for` · `Against sickness`. No two wordings of any variant share an opener, and no two variants do. No wording opens on `{settlement}`, on an expletive (`There is` / `It is`, R-DA-07), or on a comma.
- **Closes (R-DA-04).** Variant 1 closes on `season` · `want` · `failure` · `harvest`; variant 2 on `about` · `towns` · `rare` · `town`; variant 3 on `closed` · `difference` · `altered` · `stands`. **Zero pronoun closers.** The KIND varies across the pool — an object (`harvest`), a condition (`season`, `rare`), a prohibition or standing negative (`closed`, `altered`, `not being closed`), a civic noun (`town`, `towns`) — rather than inside a wording, because claim order is forced (see below).
- **The thread (§1.4.1).** Every two-sentence wording carries a noun forward from the sentence before it — `harvest` → `the failure` · `poor harvest` → `the failed harvest` · `failed harvest` → `the season of the failure` · `the sickness`/`the hunger` → `the difference`, `the gap` — or makes its change of subject the passage's ONE TURN OUTWARD and places it LAST. Variant 2's outward turn (what is usual in towns generally) is the last move in all four wordings for exactly this reason and for no other. Every wording ends on a civic noun or a present condition of one, so a modifier seated after this spine by salience has something to pick up: `the harvest`, `the season`, `the town`, `the difference`, `the arrangement`.
- **Claim order is forced, and that is why the rhythms cluster.** Wall 3 bars an ABSENCE from opening, so the HOSPITAL half precedes the RESERVES half in every wording of variants 1 and 3; wall 1 puts STATE before CAUSE, so variant 1's consequence is last in every wording; wall 5 forbids fronting the contrast, so variant 2's comparison is never the subject. Variation is therefore spent on vocabulary, sentence count, sentence length and joint rather than on order. Reported as information, not offered as an excuse.
- **Rhythm and spread, measured.** Wordings run from twenty-one to thirty-three words, mean 28.1. Sentence lengths run from five words to twenty-three, sd 6.00 over the pool's twenty-four sentences; three sentences are under eight words (`Such an arrangement is rare` at five, `The arrangement is not being altered` at six, `Sickness is better met here than hunger` at seven), which is 0.125 of the pool's sentences against R-DA-06's `< 8 words ≥ 0.030` floor. Three wordings exceed thirty words, 0.250 of the pool, inside R-DA-06's `> 30 words ≤ 0.340` band. Joints used: the comma-and and the plain period, and nothing else — no colon, no semicolon, no dash.
- **Sentence count is uniform at two, and this is FORCED, not a flattening.** Every wording carries three inherited claims (§B), and R-DA-03 bars a third segment, so three claims cannot sit in one sentence and two sentences is the only lawful shape. A11's uniform-count concern is therefore engaged by the CLAIM LOAD the card refuses (R1, R2, R3, R5), not by the wording: the same inheritance that makes the pool unlawful also removes the pool's sentence-count spread. Reported as a finding for the chair, because it is the one craft property this pool cannot vary while claim-equality binds. Length spread inside the two sentences is where the variation is spent instead, and it is wide — the shortest sentence is five words and the longest twenty-three.
- **Vocabulary spread inside the voice (the four-faces law).** The HOSPITAL half is written with five different noun-and-verb pairings across the pool (`hospital` / `take in and hold` · `fever` / `treated and kept from spreading` · `the sick` / `taken in and kept apart` · `outbreak` / `treated and confined` · `care for the sick` / `matches`), and the RESERVES half with five (`keeps nothing back` · `has laid by nothing` · `what the town has laid up … is nothing` · `keeps nothing at all` · `no store matches`). One term for one thing (R-DA-22) is held at the level of the CIVIC OBJECT — the hospital is never renamed as an infirmary, a sick-house or a lazaret, and no second object of the `store` class is ever named — while the TROUBLE the object answers takes the register's ordinary range (`sickness`, `fever`, `outbreak`, `plague`), which is the shipped parent's own practice (`an outbreak` and `the sickness` in one pool).
- **Sibling distance (arms A1 and A11).** No phrase is carried from the four sibling `Disasters & Famine` pools. This pool does not reuse `somewhere to put the sick`, `holds food against a bad year`, `can feed itself`, `clergy who tend the sick`, `knows which of the two it fears`, `endure it and count afterwards`, `until it burns out`, or `a stranger`. Nor does it reuse the shipped parent's own `treat and contain`, `an unusual way round`, `nothing in hand is correcting the imbalance` or `arranged against the sickness it has seen`. No wording contradicts a sibling pool: the church arm belongs to `granary AND parish care only` and is named nowhere here, and the hospital is asserted present, as this key requires.
- **The density law (§21.4).** No wording was made plainer than round 1 without a law behind the change. The three changes that shorten a wording are C1 (the which-tail), C2 (the third segment) and C3 (the person-totality), each a named wall. `The gap between them is not being closed` replaces round 1's `the difference stands where it was set` because the shorter line lands when read aloud and drops a past-setting implication that would have widened R4's breach, not because it is plainer.
- **Walls held in every wording:** no em dash, no exclamation, no question, no digit, no percent, no `which`, no colon, no semicolon, no first or second person, no future indicative, no expletive opener, no citation or record holder, no figure or simile, no named character, no theological claim, no office or institution the table does not hold, no band word, no count, no exemption, no totality over persons, at most two sentences, `{settlement}` and no other slot, and no wording opening on the slot.
