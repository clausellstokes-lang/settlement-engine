# DS-DEF-2 · pool `Disasters & Famine: granary AND parish care only` — REWRITE draft, round 2

Seat: Opus 5 — Fable-unvalidated. Block DS-DEF-2, role **spine** (the card takes no relation, no attach, no move), form **sentence**, angles `ledger street visitor`.
Three existing variants, rewritten in place under their own numbers and their own bracketed tags; four wordings each (the variant's own line plus three `[face]` sub-rows); none added, none removed, none merged, none reordered. The pool's typed lines (ROLE / READS / SLOTS / SECTION-TARGET, carried at the block head) are not repeated here and are not touched.

**Round 2 answers the gate on both rows, each measured under NOTES §0.** The second bracket `` `[plain]` `` is GONE from every numbered row — the plain wording IS the numbered row's text, which is ARCH §2.5's worked example and the shape of this block's already-faced rows, so nothing folds into `marks` and the contract test has no `unclassified: [ 'plain' ]` to report. And all twelve wordings carry **zero colons**, so `punctuation.colonRate`, which sat 6.987 band-widths over on 1 of 3, reads 0 once these rows apply.

Paste the twelve rows below under the pool's bold heading, in place of its three numbered rows.

---

1. `[ledger]` Grain is laid by at {settlement} against hunger, and the clergy tend the sick. What the clergy can do against disease is better than nothing and well short of a hospital.
   - `[face]` The stores at {settlement} are held against want, and the sick are left to the parish. Parish care falls well short of a hospital and is still more than nothing at all.
   - `[face]` What is kept back at {settlement} stands against hunger, and the church nurses the ill. A hospital would do more, and nothing at all would do less.
   - `[face]` Hunger at {settlement} would be met out of the store, and sickness by the church. The church is more than nothing against a disease and less than a hospital.
2. `[street]` A bad year is something the town can eat its way through. A plague it meets with prayer, and after the prayer with nursing.
   - `[face]` Want the town can sit through on what is stored. Sickness it answers with prayer, and with nursing behind the prayer.
   - `[face]` Food enough to carry the town through a year that fails is in hand, and a plague is met by the parish at prayer and the parish at the bedside, in that order.
   - `[face]` Stored food carries the town through a year gone wrong. The town's answer to a plague is prayer, and nursing when the praying is done.
3. `[visitor]` A traveller stopping at {settlement} finds the store full and the infirmary small, and can tell from the pair which of the two the town has thought about.
   - `[face]` What a stranger sees at {settlement} is a full store and a small room for the sick. Where the town's thinking went is plain in the pair.
   - `[face]` The granary at {settlement} is in good supply and the sick-room barely furnished. A stranger reads the preference off the pair.
   - `[face]` A stranger walks past a full store at {settlement} and into a small room where the sick are nursed, and does not have to ask which of them the town has favoured.

---

--- NOTES

## 0. THE GATE'S FEEDBACK, MEASURE BY MEASURE (round 1 → round 2)

| the gate's row | round 1 | what round 2 did | round 2 |
|---|---|---|---|
| **REFUSAL — a second bracketed tag on the numbered row** (`` 1. `[ledger]` `[plain]` … ``; folds into `marks`; contract test reds with `unclassified: [ 'plain' ]`) | present on all three numbered rows | the numbered row now carries the ANGLE tag alone and its text IS the plain wording, exactly as ARCH §2.5's worked row (`` 1. `[plain]` the muster behind it… ``) carries one tag and as this pool's shipped rows already carry `` `[ledger]` `` / `` `[street]` `` / `` `[visitor]` `` alone. No row carries two brackets. | **CURED — 0 of 3 rows carry a second bracket** |
| **band depth · `punctuation.colonRate` (over) = 6.987 band-widths on 1 of 3** (ENTRY depth ceiling 1.75 band-widths, Part B §16.2) | 2 colons, both in variant 3 (faces 1 and 2), each the nominal head of a compressed unit | both joints removed: face 1 is recast as a cleft with a short second sentence, face 2 as two flat sentences. Nothing else in the pool ever held a colon. | **CURED — 0 colons in 12 of 12 wordings** |
| semicolon (not flagged; held) | 0 | untouched | 0 |

**Neither cure was bought with a claim.** The two rewritten faces carry K8, K9 and K10 exactly as they did in round 1 and as the variant carried them before the rewrite (arm A6). **No failing state is added** (§21.2): the rhythm spread is held (§4 below), the `which` count FALLS from three wordings to two, and the two rewritten faces gain a compressed close rather than a plainer one (§21.4, the density law).

**Variants 1 and 2 are byte-unchanged from round 1**, deliberately. They carried no colon and no second-bracket text of their own, they cleared every other law, and §21.3 rules that where one effort must choose, the licensed wording wins over the churned one. Round 2's effort is spent where the gate pointed.

## 1. THE CARD, AS PRINTED

`role: spine` · `reads: disasterRowSituation(granary, hospital, church)` (via `DISASTER_ROW_POOL` in `defenseStateProse.js`) · `predicate: === granary, parish care` · `bag: {band: RESERVED, route: proper, settlement: proper}`, FILLED at this block's call sites `{settlement}` · `relation: (a spine takes no relation)` · `seat/form: (not a seat-taker) / sentence` · `move: (none declared)` · `angle: ledger street visitor` · `attach: (empty)` · `echo: spine mounts 1 (tabs: defense)` · `covert: no` · `source: (none) · standing SOURCE-UNRESOLVED — NO citation is licensed; a face naming a record holder is refused by arm A13` · `may claim: that the predicate (=== granary, parish care) holds, as a STANDING fact of the record` · `may NOT: a count, a cause, a season, a future, a standpoint, a second fact, another civic object of the class temple` · `audience: player (no mark)` · `REFUSED COLUMNS: a totality over persons; an exemption from a duty; a named character and that character's fate; a theological claim about a deity`.

## 2. THE CLAIM SET CARRIED, ONE ID PER CLAIM (the BEFORE's, unchanged through both rounds)

- **K1** — grain is stored at the settlement.
- **K2** — the clergy of the parish tend the sick there.
- **K3** — the store stands as a reserve against hunger.
- **K4** — what stands against disease is better than nothing and well short of a hospital (a comparison in words, both halves).
- **K5** — the town can eat through a bad year.
- **K6** — what the town does about a plague is pray and nurse.
- **K7** — prayer comes before the nursing.
- **K8** — the store is full.
- **K9** — the provision for the sick is a small room.
- **K10** — a stranger can tell which of the two the town has given its thought to.

## 3. WHICH CARD CLAUSE LICENSES EACH CLAIM, FACE BY FACE

### Variant 1 `[ledger]` — the four wordings

| wording | claims made | licence |
|---|---|---|
| numbered row (the plain) | K1, K2 | card `may claim:` (the predicate as a STANDING fact of the record) on `reads: disasterRowSituation(granary, hospital, church)` — the `granary` term licenses K1, the `parish care` term licenses K2 |
| numbered row | K3 | NOT licensed by any card clause: the predicate holds that a granary stands, never what it is held against — convicted by `may NOT: a second fact` (REFUSAL R1) |
| numbered row | K4 | the LACK half (`well short of a hospital`) is licensed by the read's own `hospital` term being FALSE in the predicate, written as R-DA-02's flat lack and R-DA-11's comparison in words; the `better than nothing` half names the sibling key `Disasters & Famine: granary, NO medical provision` and is licensed at the register grain by R-DA-02 (the rejected alternative names a sibling pool key), by NO card clause (R1) |
| numbered row, slot | `{settlement}` | card `bag: {settlement: proper}`, FILLED at this block's call sites |
| numbered row, angle | ledger | card `angle: ledger street visitor` |
| `[face]` 1 | K1, K2, K3, K4 | as the numbered row, clause for clause; the second sentence carries `the parish` forward as `Parish care` (THE THREAD) |
| `[face]` 2 | K1, K2, K3, K4 | as the numbered row; K4 written as a two-sided subjunctive measurement (`would do more` / `would do less`), so no future indicative and no evaluative verdict |
| `[face]` 3 | K1, K2, K3, K4 | as the numbered row; the hunger clause subjunctive (`would be met`), the disease clause present, so the edge and the standing fact are told apart (A2, R-DA-07) |

### Variant 2 `[street]` — the four wordings

| wording | claims made | licence |
|---|---|---|
| numbered row (the plain) | K5 | NOT licensed — the predicate holds that a granary stands, not what the town can eat through; convicted by `may NOT: a second fact`, and the bounded bad year sits against `may NOT: a season` (REFUSAL R2) |
| numbered row | K6 | the `parish care` half of the predicate licenses that the church's people nurse the sick; PRAYER is licensed as a TRADITION move (MOVE-GRAMMAR §1.2 row 9, a practice the culture holds), and no wording says a god is addressed, answers or exists — the DEITY DOCTRINE is held, faith as culture and never theology |
| numbered row | K7 | NOT licensed — a precedence between two acts that no field holds; convicted by `may NOT: a second fact` (R2) |
| numbered row, slot | none | the variant carries no slot; every face of it carries none (ARCH §2.5: a face whose `{slot}` set differs from the parent's is refused) |
| numbered row, angle | street | card `angle: ledger street visitor` |
| `[face]` 1 | K5, K6, K7 | as the numbered row; K7 carried by placement (`with nursing behind the prayer`) rather than by the parent's ordinal phrase |
| `[face]` 2 | K5, K6, K7 | as the numbered row; K7 carried by the parent's own ordinal phrase, kept once in the pool so the wording is not lost from the annex |
| `[face]` 3 | K5, K6, K7 | as the numbered row; K7 carried as a completed-act clause (`when the praying is done`), a sequence and not a duration |

### Variant 3 `[visitor]` — the four wordings (faces 1 and 2 rewritten this round)

| wording | claims made | licence |
|---|---|---|
| numbered row (the plain) | K8 | NOT licensed — a fill state of the store; the card refuses a count and the read holds no quantity (REFUSAL R3 (b)) |
| numbered row | K9 | NOT licensed — a civic object of the medical class asserted while the read's `hospital` term is FALSE in the predicate (R3 (c)) |
| numbered row | K10 | NOT licensed — convicted twice: `may NOT: a standpoint` (what a stranger can tell) and the FEELING non-move of MOVE-GRAMMAR §1.3 (what the town has thought about is a collective mental state) (R3 (a)) |
| numbered row, slot | `{settlement}` | card `bag:` as above |
| numbered row, angle | visitor | card `angle: ledger street visitor` |
| `[face]` 1 **(rewritten)** | K8, K9, K10 | as the numbered row, claim for claim. The colon is gone: the wording is now a cleft (`What a stranger sees … is a full store and a small room for the sick`) with a compressed second sentence (`Where the town's thinking went is plain in the pair`). `the pair` carries the two objects forward, so THE THREAD holds without repeating either noun. No new claim: `the town's thinking` is K10's own mental state, not an added one, and `plain` is a state of the evidence rather than a verdict on the town |
| `[face]` 2 **(rewritten)** | K8, K9, K10 | as the numbered row, claim for claim. The colon is gone: two flat sentences, the second the shortest wording in the pool. `A stranger reads the preference off the pair` is K10 compressed — the same standpoint claim in eight words instead of fourteen, which is the density law's compression and never a trim (§21.4, §22 NOT-forbidden clause: shortening a sentence inside its band is editing) |
| `[face]` 3 | K8, K9, K10 | as the numbered row; the stranger's movement is the `visitor` angle's manner, never a second claim |

## 4. LAWS TESTED AND HELD ACROSS ALL TWELVE WORDINGS (round 2 figures)

- **Punctuation walls.** Zero em dashes, zero exclamation marks, zero question marks, zero digits, zero percents, zero `, which` tails, **zero colons** (round 1: two), zero semicolons. The two surviving `which` occurrences (numbered row 3, face 3 of variant 3) are interrogative pronouns heading a subject or object clause (`which of the two…`, `which of them…`), the variant's own inherited construction, and never a relative tail — MOVE-GRAMMAR order wall 6 is about the tail. Round 1 carried three; round 2 carries two.
- **Person and address.** No first or second person, no persona, no address to the reader, no question to anybody. The compiler shows only through what the record holds.
- **STATE never FATE.** No bare future indicative anywhere. The four modal edges (`would be met`, `would do more`, `would do less`, and the numbered row 2's hypothetical bad year) are subjunctive (A2, R-DA-07).
- **No citation of any kind.** `source: (none) · SOURCE-UNRESOLVED` licenses none, so no wording names a keeper, a roll, a register or an authority; arm A13 has nothing to fire on. The parish is named as the SUBJECT of the fact, never as the holder of a record about it. The provenance ceiling of Part B §24 is spent at zero, which the exemplar registers' own rate (0 citations per 786 sentences) makes the norm.
- **No figure.** No simile, no metaphor, no inanimate thing acting with intent. Round 1's `what {settlement} shows a stranger` (a town showing) is gone from face 2 this round; the town no longer performs any act in variant 3, and the two rewritten faces put the perceiving on the stranger and the plainness on the evidence. `the town's thinking` and `the preference` are possessives of the K10 mental state already convicted at R3 (a), not new actors.
- **The REFUSED COLUMNS, all four clear.** No totality over persons, no exemption from a duty, no named character or that character's fate, no theological claim about a deity.
- **One term for one thing WITHIN each wording (R-DA-22).** A wording that says `store` never also says `granary`; a wording that says `the church` never also says `the parish` or `the clergy`. The term varies ACROSS the four faces, which is the owner's four-vocabularies rule and not a registry breach. Variant 1: grain/clergy · stores/parish · what is kept back/church · store/church. Variant 2: bad year/prayer · want/sickness · food enough/parish · stored food/praying. Variant 3: store/infirmary · store/room for the sick · granary/sick-room · store/room where the sick are nursed — variant 3's faces 1 and 3 are told apart by RHYTHM (a cleft with a short close; a walk-through in one long sentence), which the four-faces rule admits as the alternative to a fourth vocabulary.
- **One religious object per wording, never two** (`may NOT: another civic object of the class temple`). Variant 2 face 2 names `the parish` twice, which is one object under two of its functions and is R-DA-22's single term held.
- **Slot discipline.** Every wording of variants 1 and 3 carries `{settlement}` exactly once; every wording of variant 2 carries none. Each face matches its parent's set exactly (ARCH §2.5).
- **T-F8.** No wording opens on the proper-typed slot, the numbered rows held to the same rule as the faces, because the draw is unweighted over all four and each must stand alone. Wall 10 (R-DA-17, at most one settlement-opener per pool) is satisfied at zero.
- **A1 and R-DA-03.** Every wording is one or two sentences; no third sentence, no qualification carried as a tail, no summarising second sentence (the second sentence of every two-sentence wording carries a FACT, never a gloss of the first).
- **No fragments.** The card's `form: sentence` means every wording must pass the sentence regex (arm A9). All twelve are full sentences; the two rewrites deliberately avoided the nominal-fragment shape that a colon removal invites.
- **THE THREAD (§1.4.1).** Every two-sentence wording carries a noun forward: `the clergy` · `the parish`/`Parish care` · `the church` · `the town`/`it` · `prayer`/`the praying` · `a full store` and `a small room`/`the pair` · `the granary` and `the sick-room`/`the pair`. Variant 1 face 2's second sentence turns outward to the hospital and to nothing at all, and sits last, which is the one lawful turn. As this pool is the SPINE, every wording closes on a civic noun or a present condition of one that a modifier can pick up: `a hospital` · `nothing at all` · `less` · `a hospital` · `nursing` · `the prayer` · `order` · `done` · `thought about` · `the pair` · `the pair` · `favoured`.
- **A11 spread.** No two of the twelve wordings share their first two words: `Grain is` · `The stores` · `What is` · `Hunger at` · `A bad` · `Want the` · `Food enough` · `Stored food` · `A traveller` · `What a` · `The granary` · `A stranger`. (`The stores` and `The granary` diverge at word two; so do `What is` and `What a`.)
- **R-DA-05 rhythm, held against round 1.** Word counts with the slot as one word: variant 1 **31 / 32 / 27 / 29**; variant 2 **24 / 21 / 33 / 25**; variant 3 **28 / 27 / 21 / 32** (round 1 was 28 / 25 / 21 / 32). The whole-pool sd is **3.97** against round 1's 4.03 — held to within a sixteenth of a word, which is why face 2 was written short (21 words, the pool's shortest wording, tied with variant 2 face 1) rather than at the length the cleft first produced. The shortest sentence in the pool is eight words (variant 3 face 2's second); the longest wording is thirty-three.
- **R-DA-04 close kinds.** Varied inside each variant: variant 1 closes on an object, an absence, a condition, an object; variant 2 on a practice, an object, a condition, a condition; variant 3 on a condition, a condition, a condition, an act. Two of variant 1's four close on `a hospital`, the price of keeping K4's comparison in every claim-equal face; declared, not hidden.
- **R-DA-06.** The semicolon is spent nowhere and the colon is spent nowhere. The joints in use are the comma-and coordination and the full stop.
- **Sibling distance (arms A1, A11).** No wording restates or contradicts `granary AND hospital`, `granary, NO medical provision`, `NO reserves, hospital present` or `NO reserves, NO medical provision`. Those siblings' own phrases (`a failed harvest`, `a place for grain`, `a place for the ill`, `the sick-house`, `outlast a hungry year`, `somewhere to put the sick`, `until it burns out`) appear in none of the twelve. Where a comparison to a sibling's state is inherited (K4's `better than nothing`), it names the state and never the sibling's words.

## 5. REFUSALS — three, one per variant; every wording is written and none is trimmed (§22)

**R1. Variant 1 `[ledger]` cannot meet the card's `may NOT: a second fact`.** The card licenses exactly one claim, that the predicate holds. K3 and K4 are two further standing facts, and K4's second half compares this town's provision to a sibling key's. Neither can be dropped: arm A6 requires the four faces claim-equal to the variant as it stood, and §22 (a)/(e) forbids removing what the sentence stood for. Every other law tested in §4 holds in all four wordings. The disposition is the chair's — carry K3 and K4 as inherited on the spine, or migrate them to modifier pools by a chair act, which a writer may not perform.

**R2. Variant 2 `[street]` cannot meet `may NOT: a second fact`, and touches `a season`.** K5 asserts a capability over a bounded bad year; the read holds that a granary stands and nothing about what it can carry the town through, and a bounded bad year is the nearest thing in this pool to the season the card refuses. K7 asserts a precedence between prayer and nursing that no field holds. Both are the variant's own claims and are kept in all four wordings. **Held, not refused:** the prayer itself, licensed as a practice of the parish under the DEITY DOCTRINE (faith as culture); no wording claims a deity acts, answers or exists, and no wording assigns a feeling to the townspeople.

**R3. Variant 3 `[visitor]` cannot be made lawful by rewording at all, and is the sharpest refusal in the pool.** Its only claims are K8, K9 and K10, and each is convicted:
- **(a)** K10 is refused twice over — the card's `may NOT: a standpoint`, and MOVE-GRAMMAR §1.3's FEELING non-move, since what a town has *thought about* (round 2: `the town's thinking`, `the preference`) is a collective mental state no field carries. R-DA-13's belief-frame floor is zero.
- **(b)** K8's `full` / `in good supply` and K9's `small` / `barely furnished` are fill and size claims. The read carries a presence, never a quantity or a scale, so both sit against `may NOT: a count` at its nearest edge.
- **(c)** K9 asserts a room or house for the sick as a civic object while the predicate holds the `hospital` term FALSE. This is a same-entry contradiction candidate of the C3-lexical class (CLERK-LAWS §2.2, the class whose shipped anchor is `RECEIPT_POOLS_DOSSIER_STATE.md:2247` under §2.4.1's guard roster), and it is the one finding in this packet a walker can be expected to fire on: the sibling key `Disasters & Famine: granary, NO medical provision` says a town with the granary and no medical provision has *nothing at all* against disease, so an infirmary here reads as an institution the block's own branch structure does not hold. The parish's nursing is licensed; a furnished room for it is not.

All four wordings of variant 3 are written to the ceiling within that failure and **no new failing state is added** (§21.2). Round 2's two rewrites remove a punctuation exceedance and add nothing; the failure count is unchanged at three (a), (b), (c). A ruling is owed: either the `parish care` branch is typed to say what fabric the care uses, or the variant is banked as an unlawful set with its faces (§21.2's banked count), or the claim is re-seated by an owner-gated claim change.

**The card/register conflict, restated because it governs all three refusals.** No variant of this pool carries only one claim, and the card licenses only one. The register card (A1, amendment S2) permits two sentences and a second fact in its own sentence; the licence card's `may NOT: a second fact` forbids it outright. On the card's plain words no wording in this pool is lawful; on the register's, all twelve are. The same conflict is on record from the `Invasion & War: walls AND professional garrison` packet. The chair's ruling is owed and this packet decides nothing.

## 6. DECLARED CONSEQUENCES OF THE REWRITE (not refusals)

- **The existential opener is gone** (carried from round 1). The BEFORE's variant 1 opened `There is food stored at {settlement}`, which R-DA-07 caps at 0.020 per variant and which this pool ran at 0.333. No wording opens on `There is` or `It is`, and none contains an expletive subject anywhere. The pool's contribution to `thereIsOpenerRate` falls from one to zero.
- **The colon-as-payoff joint is gone from the whole pool** (round 2's cure, extending round 1's). The BEFORE joined variant 1's two facts with a colon that made the second half a gloss of the first; round 1 cured that and spent two colons of its own in variant 3; round 2 spends none. `punctuation.colonRate` for this pool is 0.
- **Zero settlement-openers, unchanged.** The BEFORE opened no variant on the slot and neither does this packet, so wall 10 is satisfied at zero.
- **`in that order` survives once.** The variant's ordinal phrase is kept in exactly one face so the annex does not lose the wording; the other three carry K7 by placement. A spread choice, not a trim.
- **The `which` count falls again.** BEFORE three of three variants used a `which`-headed clause; round 1 used three of twelve wordings; round 2 uses two of twelve. No occurrence is a relative tail in any round.
