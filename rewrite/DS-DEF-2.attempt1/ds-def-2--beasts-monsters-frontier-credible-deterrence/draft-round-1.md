REWRITE · block DS-DEF-2 · pool `Beasts & Monsters`: `frontier`, credible deterrence · draft round 1
Seat: Opus 5 (Fable-unvalidated), writer, for the Fable chair. Three variants in, three variants out; vids, order and angle tags unmoved; nothing added, nothing merged, nothing removed.

The rows below are the complete replacement for the pool's variant rows, ready to paste under the pool's bold heading.

1. `[ledger]` `[plain]` {settlement} sits on an active frontier with a line around it and a force standing behind the line. Most of what comes out of that country will not press a defended perimeter, and most of what comes at this town does not.
   - `[face]` The frontier at {settlement} is live, and the town holds a perimeter against it with a force posted behind the perimeter. Most of what comes out of the country will not try a defended line, and most of what arrives here does not try this one.
   - `[face]` Frontier country runs to the edge of {settlement}, and a wall stands at that edge with a force behind it. Most of what comes out of the country will not press a defended perimeter, and this perimeter is little pressed.
   - `[face]` Around {settlement} the country is frontier, the perimeter stands, and a force holds it. Most of what comes out of that country will not come against a defended line, and most of what comes at this one does not.

2. `[street]` `[plain]` The town takes the frontier seriously, and has taken it seriously long enough that the arrangements are ordinary rather than urgent.
   - `[face]` Nothing about the frontier is new to this town. The arrangements against it have been ordinary long enough to pass without remark.
   - `[face]` Frontier work is old at this town, and the arrangements it requires sit inside the ordinary order of the place rather than apart from it.
   - `[face]` Being on a frontier is not a new condition at this town, and the answer to it is old enough to be ordinary and settled enough not to be urgent.

3. `[counterforce]` `[plain]` Very little reaches {settlement} out of the wild country, and the reason is that the line and the force behind it can be seen from a long way off.
   - `[face]` Little comes at {settlement} out of the wild country, and what accounts for it is a perimeter that can be made out at a distance and the people kept on it.
   - `[face]` Out of the wild country little comes at {settlement}. The reason is a wall and a force that can be seen from far out.
   - `[face]` What comes out of the wild country seldom reaches {settlement}, and the reason is that a perimeter and the force on it can be told from a distance.

--- NOTES

**The licence card this pool is written against** (printed at the head of the work, `node scripts/prose-licence-card.mjs DS-DEF-2 'Beasts & Monsters: frontier, credible deterrence'`), with the clause names used below:

- **C-READ** — `reads: beastsRowSituation(family, perimeter, force)`; `predicate: === frontier country, perimeter and force`.
- **C-CLAIM** — `may claim: that the predicate holds, as a STANDING fact of the record`.
- **C-BAG** — `bag: {band: RESERVED, route: proper, settlement: proper}`, FILLED at this block's call sites: `{settlement}`.
- **C-KEY** — the pool key's own discriminating term, `credible deterrence`, which the card prints as the key and which U9 forbids a rewrite to drop.
- **C-SOURCE** — `source: muster · standing LICENSED`; a citation of the holder is licensed where the provenance budget allows.
- **C-NOT** — `may NOT: a count, a cause, a season, a future, a standpoint, a second fact`.
- **C-REFUSED** — refused columns always: a totality over persons; an exemption from a duty; a named character and that character's fate; a theological claim.

**Slots.** Every face carries exactly the parent variant's `{slot}` set and nothing else (ARCH §2.5: a face whose `{slot}` set differs from the parent's is refused). Variants 1 and 3 carry `{settlement}` once; variant 2 carries no slot, as it does today. `{band}` is RESERVED on the card and `{route}` is unfilled at this block's call sites, so neither is written. C-BAG.

**Provenance.** Zero citations across the twelve wordings. C-SOURCE licenses one, but none of S3's three reasons is present here (no two accounts disagree, no count is taken from an interested party, no cited keeper is a power), and §24's ceiling plus the exemplars' measured zero per seven hundred eighty-six sentences make silence the norm.

**C-REFUSED, cleared.** No wording claims a totality over persons, an exemption, a named person, or a deity. The draft's one totality-over-persons risk (`nobody remarks on them`) was removed at 2's first face in favour of `pass without remark`.

---

**Variant 1 `[ledger]` — the claim set, and the clause under each claim.** The four wordings are claim-equal (arm A6).

| # | claim | licensed by |
|---|---|---|
| c1 | the country around the town is frontier country | C-READ (`family`), C-CLAIM |
| c2 | the town holds a perimeter | C-READ (`perimeter`), C-CLAIM |
| c3 | a force stands behind the perimeter | C-READ (`force`), C-CLAIM |
| c4 | most of what comes out of such country will not press a defended perimeter | **no clause** — see REFUSAL R1 |
| c5 | most of what comes at this town does not press it | C-KEY (`credible deterrence`) — see REFUSAL R5 |

- `[plain]` c1 `an active frontier` · c2 `a line around it` · c3 `a force standing behind the line` · c4 sentence two, first half · c5 sentence two, second half. No claim outside the five.
- `[face] 1` (`The frontier at {settlement} is live…`) same five; `live` restates c1's `active` and adds nothing.
- `[face] 2` (`Frontier country runs to the edge…`) same five; `a wall` is c2 read through `defenseProfileHasWalls`, which the block's own fence names as the licensing predicate for the perimeter; `this perimeter is little pressed` is c5 in the passive.
- `[face] 3` (`Around {settlement} the country is frontier…`) same five; the three-clause opening states c1, c2 and c3 in one sentence and asserts no fourth thing.

**Variant 2 `[street]` — the claim set.**

| # | claim | licensed by |
|---|---|---|
| c1 | the town keeps arrangements against the frontier, seriously | C-READ (`perimeter`, `force`) and C-CLAIM, obliquely: the variant names the arrangements without itemising them, as it does today |
| c2 | it has done so for a long time | **no clause** — see REFUSAL R3 |
| c3 | the arrangements are ordinary and not urgent | C-KEY, as the settled face of credible deterrence — see REFUSAL R5 |

- `[plain]` c1 `takes the frontier seriously` · c2 `long enough` · c3 `ordinary rather than urgent`.
- `[face] 1` c1 `The arrangements against it` · c2 `Nothing about the frontier is new` and `long enough` · c3 `ordinary … to pass without remark`.
- `[face] 2` c1 `the arrangements it requires` · c2 `old at this town` · c3 `sit inside the ordinary order of the place rather than apart from it`.
- `[face] 3` c1 `the answer to it` · c2 `not a new condition`, `old enough` · c3 `ordinary`, `settled enough not to be urgent`.
- The BEFORE's `anxious` is not carried as a word in any face. It was a feeling on an inanimate subject (R-DA-11's transferred epithet; the FEELING non-move, MOVE-GRAMMAR §1.3), and c3's claim is the arrangements' ordinariness, which every face states. This is a wording change inside c3, not a claim spent.

**Variant 3 `[counterforce]` — the claim set.**

| # | claim | licensed by |
|---|---|---|
| c1 | the country is wild frontier country | C-READ (`family`), C-CLAIM |
| c2 | a perimeter stands and a force is on it | C-READ (`perimeter`, `force`), C-CLAIM |
| c3 | very little reaches the town out of that country | C-KEY (`credible deterrence`) — see REFUSAL R5 |
| c4 | the reason for c3 is that the arrangements can be seen at a distance | **no clause** — see REFUSAL R4 |

- `[plain]` c1 `the wild country` · c2 `the line and the force behind it` · c3 `Very little reaches` · c4 `the reason is that … can be seen from a long way off`.
- `[face] 1` c1 `the wild country` · c2 `a perimeter … and the people kept on it` · c3 `Little comes` · c4 `what accounts for it is … can be made out at a distance`.
- `[face] 2` c1 `Out of the wild country` · c2 `a wall and a force` · c3 `little comes` · c4 `The reason is … can be seen from far out`.
- `[face] 3` c1 `out of the wild country` · c2 `a perimeter and the force on it` · c3 `seldom reaches` · c4 `the reason is that … can be told from a distance`.

---

**REFUSALS — laws these variants cannot meet. Every one is INHERITED from the wording that ships today; none is introduced by this draft. Under §22 (a) and (e) the claim cannot be dropped to make the row lawful, so the row is written with the claim intact and the debt is declared here (CLERK-LAWS §2.6: a rewrite may not ADD a fail; a pre-existing fail is reported as debt).**

- **R1 — variant 1, all four wordings. C-NOT (`a second fact`) and the generalisation test (R-DA-12).** `most of what comes out of that country will not press a defended perimeter` is a general proposition about defended perimeters anywhere, not a standing fact of this town's record. No field on the card holds it. It is the first half of the BEFORE's second sentence and cannot be removed. **The variant is written; it is not lawful on c4.**
- **R2 — variant 1, all four wordings. C-NOT (`a count`), soft.** `most` is a proportion word over a set the card does not hold. It is not a totality and not over persons, so C-REFUSED is not engaged and this is a band finding rather than a wall. Carried unchanged from the BEFORE.
- **R3 — variant 2, all four wordings. C-NOT (`a season`) and R-DST-B.** The duration claim (`long enough`, `old at this town`, `not a new condition`) is HISTORICAL. R-DST-B licenses a structural clause from a standing configuration field and never a historical one, and this branch carries no event-provenance field. **The variant is written; it is not lawful on c2.** This is the pool's sharpest single finding: the whole of variant 2's distinguishing content is its duration.
- **R4 — variant 3, all four wordings. C-NOT (`a cause`), and no field for the sight line.** `the reason is that the arrangements can be seen from a long way off` asserts both a cause and a visibility fact. The card holds neither a cause field nor any field about sight lines or distance, so the visibility half is also hollow specificity (fault 24). **The variant is written; it is not lawful on c4.** The `[counterforce]` angle is defined by exactly this move, so the finding is against the angle as authored, not against a wording choice.
- **R5 — variants 1, 2 and 3. C-CLAIM does not reach the deterrence claim; C-KEY does, and the card does not say so.** The card's `may claim` line licenses only the predicate (`frontier country, perimeter and force`). The deterrence outcome (`most of what comes here does not`; `the arrangements are ordinary`; `very little reaches`) rests on the pool key's own term, `credible deterrence`, which U9 forbids a rewrite to drop. Written on C-KEY throughout. **A chair ruling is owed on whether a spine's key term licenses a claim its `may claim` line does not print.** If the ruling is no, all three variants lose their discriminating claim and the pool becomes three sayings of the predicate.
- **R6 — variant 3, order. MOVE-GRAMMAR §1.4 wall 1.** The `[counterforce]` angle states the outcome first and the licensed standing state second, so the state does not precede the thing it explains. Pre-existing and angle-defined; carried, not cured, because curing it would reorder the angle out of existence.
- **R7 — variant 1, declared not a breach.** The habitual `will not` is kept in all four wordings because §3.4 bars spending a MODALITY and names the habitual `will` as kept. It is not a bare future indicative and does not engage R-DA-07's FATE arm or the `FUTURE INDICATIVE ADDED` check.

**One grammar question for the chair, not a refusal.** ARCH §2.5's worked row puts `` `[plain]` `` in the tag position because that pool's variant carries no angle mark. This pool's variants carry `[ledger]`, `[street]` and `[counterforce]`, which are not mine to touch, so the rows above carry both markers (`` 1. `[ledger]` `[plain]` … ``). If the projector expects the angle tag alone on the numbered row, as the shipped corpus has it, the `` `[plain]` `` marker drops and the numbered line is unchanged in shape; the three `[face]` sub-rows are unaffected either way.

**Sibling distance, measured.** Against the other twenty-five pools of DS-DEF-2, the twelve wordings share exactly two four-word runs, both `and the reason is`, at variant 3's plain line and its third face. That phrase is the `[counterforce]` angle's own formula and recurs in the angle's sibling variants elsewhere in the block (`and the reason is money`; `and the reason is in the two buildings`). The register card licenses the office's formula to recur while the fact may not, so this is carried rather than cured; variant 3's other two faces use `what accounts for it is` and a bare second sentence, so three constructions serve four wordings. Every other four-word run in the draft is new to the block. Overlap with the pool's own BEFORE lines is deliberate where the BEFORE was already lawful.

**Walls cleared across all twelve wordings.** No em dash, no exclamation, no question, no digit, no percent, no `which`-clause, no first or second person, no named person, no simile or sense verb on an abstraction, no inanimate intent, no bare future indicative, no third sentence in any wording (R-DA-03), no ABSENCE move opening a wording, no citation. Each wording opens on a different pair of first words from all eleven others (A11 generalised across the faces). Variant 1 is the pool's single settlement-token opener (R-DA-17); variants 2 and 3 open on the frontier, the country, or what comes out of it. In every two-sentence wording the second sentence carries a noun forward from the first (THE THREAD, MOVE-GRAMMAR §1.4.1): variant 1 carries `country` and `perimeter`, variant 2's first face carries `frontier` as `it`, variant 3's second face carries the outcome into `The reason`.
