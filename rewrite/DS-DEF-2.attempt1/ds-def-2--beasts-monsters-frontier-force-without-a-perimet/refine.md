# REFINEMENT PACKET — block `DS-DEF-2` · pool `Beasts & Monsters: frontier, force without a perimeter`
Refinement round · arm A · refiner seat: Opus 5 (Fable-unvalidated), a different author than the drafter · 2026-09-09
Refines `draft-round-1.md` one wording for one wording: three variants, twelve wordings, no variant or face added, dropped or merged, no typed line touched, every claim carried and none added.
Licence card re-read before writing (`node scripts/prose-licence-card.mjs DS-DEF-2 'Beasts & Monsters: frontier, force without a perimeter'`, printed at N0 below, unedited).

---

## THE ROWS (paste under the pool heading)

1. `[ledger]` {settlement} meets a frontier country with armed people. The town has no perimeter.
   - `[face]` The country outside {settlement} is frontier, and the answer set against it is a force kept standing. No wall rings the town.
   - `[face]` At {settlement} the country is frontier country, and no wall stands against it. What the town keeps instead is written on the muster roll.
   - `[face]` Soldiers are kept at {settlement} against a frontier country. They stand behind no line.
2. `[visitor]` A stranger coming into {settlement} out of the country passes no wall before meeting its armed people.
   - `[face]` An arrival leaving the country enters {settlement} across no line, and is among its soldiers at once.
   - `[face]` A newcomer from the country comes up against the muster of {settlement} and no wall.
   - `[face]` Strangers come in with the country behind them and no wall before them, and the force of {settlement} stands inside the town.
3. `[street]` Armed people stand against the country here, and with them stands no perimeter.
   - `[face]` What comes in from the country is met here by the muster, not turned at a line.
   - `[face]` Soldiers answer the country here. No wall does.
   - `[face]` This town goes armed against the country, and no works stand about the place.

---

## --- NOTES

### One line per wording — what changed, and the law or target behind it

**1 `[ledger]` `[plain]`** — `keeps armed people against` became `meets a frontier country with armed people`: the shipped sibling `Invasion & War: force with NO walls` opens `{settlement} keeps a professional force and no perimeter`, so the draft's verb and object put the pool's canonical line inside one clause of a same-block sibling; **target: sibling distance (A1/A11, the brief's widest-distance target)**, and `meets` states the discriminating fact itself — a force without a perimeter meets what comes instead of keeping it out (§21.1, the sharpest licensed fact). The LACK keeps its own flat sentence (R-DA-02) and the settlement-token opener stays the pool's one (R-DA-17, one variant per pool).

**1 face a** — `the answer to it is a standing force` became `the answer set against it is a force kept standing`: the agentless clerk's passive drops the second `the town` from the unit and the participial `kept standing` replaces the flat attributive, **R-DA-18 (plain is not flat; the register's participial floor 0.0117 ≤ 0.020 is not troubled by one instance)**; the close `No wall rings the town` is kept as the pool's only wall-as-subject construction and remains the passage's one turn outward, placed last (§1.4.1 THE THREAD).

**1 face b** — `the country beyond` became `the country is frontier country` (the draft's `beyond` had no stated object after the wall was removed from the first clause), and `The strength it keeps instead stands on the muster roll` became `What the town keeps instead is written on the muster roll`: strength standing on a roll is a sense-figure on an abstraction, **R-DA-11**, while a fact written on a roll is what a roll literally holds; `sets no wall against it` became `no wall stands against it` because the plagued sibling already carries `{settlement} sets no line about it`. This is the pool's one citation of the card's licensed holder (`source: muster · standing LICENSED`), on S3 reason (iii), inside §24's ceiling of one per unit; the office does not cite its own books (MOVE-GRAMMAR §4.4.3) and the muster is not the compiling office.

**1 face c** — `Armed people are kept` became `Soldiers are kept`: the draft repeated the `[plain]` line's own noun phrase inside the same variant, and the owner's four-faces law asks each face for a different vocabulary or rhythm; **R-DA-10 (vary the noun across the pool)**. The five-word close `They stand behind no line` is kept unchanged — it is the variant's rhythm (a long flat first sentence against a short second) and its pronoun thread carries the force forward (§1.4.1).

**2 `[visitor]` `[plain]`** — `finds soldiers there and no wall around the town` became `passes no wall before meeting its armed people`: the shipped `Invasion & War: force with NO walls` visitor row is `A stranger sees soldiers at {settlement} and no line for them to stand behind`, the closest row in the block, so the draft's sees-soldiers-and-no-wall shape had to go; **target: sibling distance**, and the arrival's passing of nothing is the perimeter's absence rendered as the reader meets it. The close moves from a place to the force itself, widening the pool's close-kind spread (**R-DA-04**).

**2 face a** — `Whoever comes in off the country finds {settlement} armed and unwalled, its soldiers on open ground` became `An arrival leaving the country enters {settlement} across no line, and is among its soldiers at once`: `Whoever comes … out of the country` is verbatim the opening of the `Invasion & War: militia only` visitor face, and `unwalled` is the plagued-nothing sibling's own word (`The town is unwalled and unarmed in plagued country`) — two collisions in one wording; **target: sibling distance (A1/A11)**. `among its soldiers at once` puts the force inside the town, which is the licensed fact the draft's `open ground` only gestured at.

**2 face b** — `A traveller out of the country meets the muster of {settlement} and passes no wall coming in` became `A newcomer from the country comes up against the muster of {settlement} and no wall`: `A traveller` opens two sibling faces in this block, and the coordination `comes up against the muster … and no wall` carries both licensed terms on one verb, which is compression that rewards the reader rather than plainness (**§21.4, the density law**); the wording also loses the draft's fourth repetition of the `out of the country` formula (**R-DA-10, rationed pet words ≤ 0.200**).

**2 face c** — `Strangers walk into {settlement} out of the country and pass no wall, and the force is met inside` became `Strangers come in with the country behind them and no wall before them, and the force of {settlement} stands inside the town`: `A stranger walks out of {settlement}` is the settled-nothing sibling's own line, so the verb moved; the paired `behind them … before them` gives this face the rhythm the other three do not have, and the force is named as standing inside rather than merely met, which is the state the card holds (**C-pred**, not a standpoint).

**3 `[street]` `[plain]`** — `The town keeps armed people against the country and keeps no line` became `Armed people stand against the country here, and with them stands no perimeter`: the doubled `keeps` was the pool's clearest tic, and the line now opens on the force rather than on the estate's default subject; **R-DA-05 (the pool is the unit of spread) and R-DA-22 (the noun may bind, the mood may not)**. The inverted second clause is the pool's one inversion, spent so the wording lands on the civic noun instead of on a pronoun (**R-DA-04's `closers.pronounRate`** — the refiner's own first attempt, `stands with them`, closed on `them` and was refused). The slotless surface and the single sentence are preserved.

**3 face a** — `is met by the muster here, not kept out at a line` became `is met here by the muster, not turned at a line`: `here` moves to the beat where the street angle sits, and `turned` is one verb against the draft's two-word `kept out`, which keeps the contrast tight. This stays the pool's **only** wording whose closing move is a contrast, so wall 5 of MOVE-GRAMMAR §1.4 holds (`never the closing move of more than one variant per pool`); the rejected alternative is the sibling key `frontier, credible deterrence` (**R-DA-02**), and it is not fronted.

**3 face b** — `Soldiers answer the country here and no wall does` became two sentences, `Soldiers answer the country here. No wall does.`: the full stop makes the gapped verb land as a stop rather than a tail, and the eight-word wording is now the pool's short line against a twenty-four-word longest, which is the spread the register asks for (**R-DA-05: within-pool sd, and the short line exists**). The elided verb is itself the thread, and the absence sits second, never opening (**R-DA-08 / wall 3**).

**3 face c** — `Against the country this town sets a force and no perimeter` became `This town goes armed against the country, and no works stand about the place`: `a force and no perimeter` is four words of the `Invasion & War: force with NO walls` ledger row, and the draft's fronted `Against the country` would have made a second fronted-place opener in one pool; **target: sibling distance and zero tics**. `goes armed` is the street's own idiom for a fact the town treats as ordinary, and `works` gives the pool a fourth wall-class noun (**R-DA-10**).

### N0. The licence card this packet was refined against (printed, unedited)

```
LICENCE (block DS-DEF-2 · role spine · key `Beasts & Monsters: frontier, force without a perimeter`)
  reads:      beastsRowSituation(family, perimeter, force) (via BEASTS_ROW_POOL in defenseStateProse.js)
              (absent ⇒ no candidate; a modifier is silent, never "false")
  predicate:  beastsRowSituation(family, perimeter, force) === frontier country, force without a perimeter
  bag:        {band: RESERVED, route: proper, settlement: proper}
              FILLED at this block's call sites: {settlement}
  relation:   (a spine takes no relation)
  seat/form:  (not a seat-taker) / sentence      move: (none declared)     angle: ledger street visitor
  attach:     (empty: a spine takes no attach set)
  echo:       spine mounts 1 (tabs: defense) · modifier mounts 0 (none)
  covert:     no
  source:     muster · standing LICENSED
              a citation of this holder is licensed where the provenance budget allows
  may claim:  that the predicate holds, as a STANDING fact of the record
  may NOT:    a count, a cause, a season, a future, a standpoint, a second fact,
              another civic object of the class `wall`
  audience:   player (no mark)
  REFUSED COLUMNS, always: a totality over persons; an exemption from a duty; a named character
  and that character's fate; a theological claim about a deity
```

### N1. What the refinement did NOT touch

- **No claim moved.** Every wording carries exactly the claims its draft wording carried: v1's four carry frontier × force × no-perimeter × `{settlement}`; v2's four carry the country × force × no-wall × `{settlement}` and deliberately do not assert the level word `frontier`, as the shipped row and the draft did not; v3's four carry the country × force × no-perimeter and no slot. Face b of v1 carries the one provenance citation the draft placed there, unchanged in claim. Arm A6 (the four faces claim-equal) and C-pair (the AFTER carries the BEFORE's typed claim set) hold by construction.
- **No typed line, tag, vid or order.** The pool heading, `RECEIPT` / `STATE-KEY` / `SLOTS` / `SECTION-TARGET` / `PROVENANCE + FENCE` / `PDF PARITY`, the three angle tags and the row numbers are untouched and are not repeated in the paste block.
- **No trim.** Twelve wordings in, twelve out; three variants in, three out. Where a wording is shorter it is shorter inside its band, which §22 names as editing and not trimming.
- **The two draft REFUSALS stand and are re-declared:** every variant is level-1 grammar **V1 (PRESENT)** because the card declares `move: (none declared)` and this block holds no event-provenance, `none-exists`, `not-held`, structural-consequence, institution or named-object field, so MOVE-GRAMMAR §2.1's min(k, 8) distinct grammars is unsatisfiable here (§3.2 forbids writing a member whose licensing fields are null); and no CONSEQUENCE face is possible, so v3 keeps its PRESENT shape. Both are licence limits, offered again to the chair as the same wiring finding: a `perimeter` absence typed as its own `none-exists` field would make V3 drawable here and in the six sibling beasts cells at once.

### N2. The refiner's own receipts (claims to be checked, not passes)

- **Openers.** All twelve wordings differ in their first two words (A11): `{settlement} meets` · `The country` · `At {settlement}` · `Soldiers are` · `A stranger` · `An arrival` · `A newcomer` · `Strangers come` · `Armed people` · `What comes` · `Soldiers answer` · `This town`. Exactly one wording opens on the settlement token (v1 `[plain]`), one in twelve, under R-DA-17's 0.167 register ceiling and its one-variant-per-pool rule; v1 face b opens on `At`, not on the token.
- **The wall class.** Each wording names exactly one civic object of the class `wall` — perimeter · wall · wall · line · wall · line · wall · wall · perimeter · line · wall · works — so the card's last refusal is not engaged, and the pool now carries four distinct nouns of the class against the draft's three.
- **Hard rules.** No em dash, no exclamation, no question mark, no digit, no percent, no `which` in the twelve wordings; no connective carries a digit or a percent.
- **State never fate.** Every wording is present tense; no future indicative and no subjunctive edge anywhere.
- **Closers.** No wording closes on a pronoun (R-DA-04's `pronounRate`). Kinds across the twelve: absence (perimeter, line, wall, perimeter, line, wall, works), object (the muster roll, its armed people), place (the town, the town), condition (at once). The absence-weighted profile is declared rather than papered over: the perimeter's absence is the term that discriminates this pool from its sibling key `frontier, credible deterrence`, so landing there is landing on the discriminating civic noun; four wordings close otherwise, which is the variety R-DA-04 asks for.
- **Sentence counts.** v1 four wordings at two sentences (13 · 22 · 24 · 14 words); v2 four at one (17 · 17 · 15 · 22); v3 three at one and face b at two (13 · 17 · 8 · 14). Counted by script over the paste block, a `{slot}` token counting as one word; the same script confirms zero em dashes, exclamations, question marks, digits, percents and `which`, zero quantifiers of the `every / only / all / none / nobody / never / any / whole` family, exactly one civic object of the class `wall` per wording, and twelve distinct first-two-word openers. The eight-word v3 face b against the twenty-four-word v1 face b widens the pool's spread rather than flattening it (R-DA-05). No rule binds a face to its parent's sentence count; ARCH §2.5's face refusals are the slot set, the pinned count, the bound rows and the two opening forms, and none is engaged.
- **Sibling distance (the round's largest single gain).** Five verbatim or near-verbatim collisions with rows of this same block are cured: `A stranger sees soldiers … and no line for them to stand behind` (`Invasion & War: force with NO walls`), `Whoever comes to {settlement} out of the country meets` and `A traveller at {settlement}` (`Invasion & War: militia only`), `The town is unwalled and unarmed in plagued country` (`plagued, NO perimeter and NO force`), `keeps a professional force and no perimeter` and `The town's defense is people rather than works` (`Invasion & War: force with NO walls`), and `{settlement} sets no line about it` (`plagued, NO perimeter and NO force`). No wording of this pool now names walls held, garrison, militia, the charter, creatures or beasts, which are the neighbouring cells' fields.
- **Tics.** The draft's `out of the country` formula ran four times in twelve wordings; it now runs once, with `from the country`, `leaving the country`, `in from the country` and `the country behind them` carrying the rest — the dispersion R-DA-10's rationed-word ceiling asks for. The doubled `keeps` of v3 `[plain]` is gone. One fronted-place opener remains in the pool (none where the draft had two), and no inversion or ledger-closer habit repeats across the twelve.
- **The thread (§1.4.1).** Each two-sentence wording either carries a noun forward (v1 `[plain]` town ← `{settlement}`; v1 face b the town → what the town keeps; v1 face c soldiers → they) or makes its change of subject the one turn outward, placed last (v1 face a, the wall after the country and the force; v3 face b, whose elided verb is itself the thread). The eight single-sentence wordings do not engage the rule.
- **Figure policy.** No simile, no metaphor, no sense verb on an abstraction, no inanimate subject acting with intent; the country is never a subject that does anything, and `rings`, `stands`, `comes up against`, `goes armed` and `written on` are literal working verbs of the register. The draft's one figure risk (strength standing on a roll) is removed.
- **Quantifiers and gender.** No `every`, `only`, `all`, `none`, `nobody`, `never`, `any` — no column of this card is `closed`, so no totality is written (R-DA-15 (i)). No gendered noun or pronoun: the engine holds no gender for this force (CLERK-LAWS C3 arm a).
- **Provenance budget.** One citation in twelve wordings, on the card's own licensed holder. §24 sets the ceiling at one per unit and the exemplar registers with raw text cite at zero per 786 sentences, so this is offered as the rare case and stays the first thing a refuter should test.
- **Reading position.** This cell takes k = 0 facts today (ARCH §6.4), so every wording stands as the whole passage; each close lands on a civic noun a later modifier could pick up should the fact budget open — perimeter, wall, line, works, the muster roll, the armed people, the town.
