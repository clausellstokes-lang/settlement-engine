# DS-DEF-5 · `walls ABSENT` · REWRITE draft round 2

Seat: Opus 5 (Fable-unvalidated) · block `DS-DEF-5` · role `spine` · 3 variants in, 3 variants out, 12 wordings (the numbered row plus three faces on every variant). Round 1's packet was REFUSED at the projector and never landed; the pool still carries its three shipped rows. This round cures the refusal and answers the owned failing measure.

Paste-ready: the three numbered rows below are the COMPLETE replacement for the pool's three variant rows under the bold pool line **walls ABSENT**. Same vids, same order, same angle tags (`[ledger]`, `[visitor]`, `[street]`), none added, none removed, none merged. The pool declares no typed lines (ROLE, READS, RELATION, ATTACH, FORM, MOVE) in the annex today; none is touched and none is repeated here.

1. `[ledger]` {settlement} keeps no wall.
   - `[face]` No wall stands at {settlement}.
   - `[face]` What {settlement} goes without is a wall.
   - `[face]` Nothing walls the town at {settlement}.
2. `[visitor]` A traveller comes the whole way into {settlement} without meeting a wall.
   - `[face]` The way into {settlement} passes no wall.
   - `[face]` An approach to {settlement} from any quarter of the compass meets no wall.
   - `[face]` From outside, {settlement} is a town no wall encloses.
3. `[street]` The town has an edge and not a wall.
   - `[face]` Open on every side, the town is unwalled.
   - `[face]` Round the town no wall runs.
   - `[face]` Where the town ends, no wall stands.

--- NOTES

## A. THE GATE'S FEEDBACK, ANSWERED MEASURE BY MEASURE

**A.1 — REFUSED at the projector (ARCH 2.5 / T-F8, wall 10 widened across the join): "a sentence face may not open on the `proper`-typed slot {settlement}". The line: variant #1 face 2, `{settlement} is entered as an unwalled town.` — CURED, and cured twice over.**

- That face is gone. Its replacement is `What {settlement} goes without is a wall.`, which opens on `What`. **No face row in this packet opens on `{settlement}` or on any other `proper`-typed slot**; the twelve openers are, in order: `{settlement}` · `No` · `What` · `Nothing` · `A` · `The` · `An` · `From` · `The` · `Open` · `Round` · `Where`. Exactly ONE wording in the pool opens on the token, and it is the numbered VARIANT row of v1, not a face — the row class T-F8 does not reach, and the class the landed corpus shows opening on the token in this very block (`garrison PRESENT` v1 `[ledger]` `{settlement} keeps a garrison of its own…`; `walls PRESENT` v2 `[ledger]` `{settlement} is walled, and…`). Wall 10 of MOVE-GRAMMAR §1.4 is therefore held at its own limit — at most one variant per pool opens on the settlement token, and no two adjacent.
- The second cure goes past the refusal to the pressure behind it. **Variant 3 is now slot-free entirely** — row and all three faces carry no `{slot}`, so the face set equals the parent set (empty) and the ARCH 2.5 face refusal is not approached. The precedent is landed and is in this block: `walls PRESENT` v3 ships as `The town has a wall for its edge.` with three slot-free faces. This drops the pool's `{settlement}` count from twelve wordings to eight and takes four wordings out of the register's `openers.topOpeners` pressure (R-DA-17's 0.209 → ≤ 0.167 ceiling, Q15's 0.150 target) without spending a claim, a length or a variant.

**A.2 — `shapes.whichTailRate` (over) = 12.514 band-widths on 1 of 3 faces, against a DEPTH of at most 1.75 band-widths on every face — ANSWERED, and the reason it did not move last round is named.**

- The gate's own line records that this figure is the SHIPPED row's, not the draft's: the pool kept `1. [ledger] {settlement} is unfortified. There is no perimeter, which means the town cannot control entry and cannot make a chokepoint of anything.` because the draft was refused at the projector. The `, which means` tail in that row is the whole of the 12.514 band-widths.
- **Round 1's wordings already carried zero `which`; round 2's carry zero `which`.** So the measure was never the draft's to move by rewording — **the move on this measure IS the refusal cure of §A.1**, which is what puts a which-free set through the projector. On landing, this pool's `shapes.whichTailRate` is 0.000 on all twelve wordings (0 of 12 contain the string `which` in any position, tail or otherwise), which is inside the band (R-DA-03's per-variant size ≤ 0.010, the band's floor being martin-chronicle's 0.0000) at depth 0.
- This is not a dry round under §21 (Part B): the failing owned measure moves from 12.514 band-widths to 0 by the cure, and eight of the twelve wordings changed besides.

**A.3 — What else round 2 moved toward the ceiling (§21.1 / §21.2 / §21.3), unasked.** Round 1 carried a tic the gate had no arm for and a refuter would have named: v2's row and v2's face 2 both doubled a verb (`comes … coming`, `arrives … arrives`), which is the signature rhythm of the landed `garrison PRESENT` v3 (`A stranger who comes to {settlement} comes to a town that keeps soldiers.`) — a spine that renders in the SAME defense passage as this one. Two spines of one passage sharing a device is an A1/A11 finding. Both doublings are gone. Round 1's `From the road` (v2 face 3) presupposed a road, which no field on this card holds; it is now `From outside`, a vantage the angle itself licenses. Round 1's v3 face 3 was a cleft (`A wall is what the town at {settlement} lacks.`) and v1 had none; the cleft moved to v1 face 2, where it is the ledger's construction, and v3 face 3 became `Where the town ends, no wall stands.`, a fronted temporal-locative that carries the town's own idiom and closes on a state verb rather than on a pronoun (R-DA-04's `closers.pronounRate` band: 0 of 12 close on a pronoun).

## B. The card, printed, and the clause labels used below

`node scripts/prose-licence-card.mjs DS-DEF-5 'walls ABSENT'` returns, in the dock at `laneRW-DEF5`:

```
LICENCE (block DS-DEF-5 · role spine · key `walls ABSENT`)
  reads:      forces.walls.present   (measured)
              (absent ⇒ no candidate; a modifier is silent, never "false")
  predicate:  (none recovered: the pool has no key-function branch)
  bag:        {band: RESERVED, counterpart: proper, faction: proper, settlement: proper}
              FILLED at this block's call sites: {settlement}
  relation:   (a spine takes no relation)
  seat/form:  (not a seat-taker) / sentence      move: (none declared)     angle: ledger street visitor
  attach:     (empty: a spine takes no attach set)
  echo:       spine mounts 2 (tabs: defense) · modifier mounts 0 (none)
  covert:     no
  source:     muster · standing LICENSED
  may claim:  that `present` holds, as a STANDING fact of the record
  may NOT:    a count, a cause, a season, a future, a standpoint, a second fact
  audience:   player (no mark)
  REFUSED COLUMNS, always: a totality over persons; an exemption from a duty (whoIsExempt is null
  everywhere); a named character and that character's fate (product scope); a theological claim
  about a deity (the deity doctrine)
```

- **[MC]** — the card's `may claim` line, read with `reads` and with the POOL KEY. **The card's polarity is coarse and this packet says so up front:** the same card prints for `walls PRESENT` and for `walls ABSENT`, both reading `may claim: that present holds`, because `predicate` recovered nothing (the block has no key-function branch for the walls pools). The discriminator is the pool key itself, exactly as the two sibling pools of one field divide it, together with the block's own STATE-KEY (`RECEIPT_POOLS_DOSSIER_STATE.md:2866-2869`, the group `Fortifications` (walls)) and its FENCE (`:2872-2879`, *walls ride `defenseProfileHasWalls`, never a presence check*). So [MC] for THIS pool is: **the reading of the fortifications group for this town is the ABSENT value — the town has no wall — as a STANDING fact of the record.** One claim; it has no second half.
- **[BAG]** — `{settlement}`, `proper`, FILLED; `{band}` RESERVED, `{counterpart}` and `{faction}` unfilled at this block's call sites, so no wording names them. v1 and v2 carry exactly one `{settlement}` in the row and one in each of their three faces (face set = parent set). v3 carries none in the row and none in any face (face set = parent set, both empty).
- **[MN]** — `may NOT`: a count, a cause, a season, a future, a standpoint, a second fact.
- **[SRC]** — source `muster`, standing LICENSED, *a citation of this holder is licensed where the provenance budget allows*. **No wording spends it.** §24's ceiling licenses one citation per unit and only for one of S3's three reasons (two accounts that disagree; a count from an interested party; a record whose keeper is a power); this card holds no second account, the packet writes no count, and no typed fact here makes the muster a power. The exemplar registers with raw text cite at 0 per 786 sentences, so the budget is spent nowhere and arm A13 has nothing to catch. Recorded because the licence exists and was declined, not because it was overlooked.
- **[RC]** — REFUSED COLUMNS, always. None is approached: **zero personal pronouns anywhere in the packet** (the shipped `[visitor]` row's *him* went with the clause that carried it), no person is counted, excepted or named, no deity is named.
- **[FENCE]** — the block's PROVENANCE + FENCE (`:2872-2879`) and arms A1/A11 against the sibling spines this pool sits beside on the `defense` tab. `walls ABSENT` can render in one passage with `garrison PRESENT`, `militia PRESENT (no garrison)`, `watch PRESENT`, `mercenary / contracted forces PRESENT`, `charter hall PRESENT`, `arcane defense PRESENT/ABSENT` and with `NO organized force at all`. **So no wording here says that anybody is or is not there, that anything is or is not stopped, watched, counted or kept, that entry can or cannot be controlled, or what the town can or cannot do.** It names no terrain, no threat, no upkeep and no readiness band (`DS-DEF-1`, `DS-DEF-2`, `DS-DEF-6`), and no rationale for the state of the walls (`DS-DEF-11`, `defense.wallRationale`). This remains the largest change from the shipped rows.

## C. The claim set, and what is dropped from each shipped variant

All twelve wordings carry [MC] and nothing else, so the four wordings of each variant are claim-equal to EACH OTHER (arm A6 read across the faces, never back to the shipped sentence). What the shipped rows carried and the card does not license is dropped, which is the rewrite's purpose; nothing is added.

- **v1 `[ledger]`** — shipped: *"{settlement} is unfortified. There is no perimeter, which means the town cannot control entry and cannot make a chokepoint of anything."* KEPT: the town is unfortified ([MC]). DROPPED: *"There is no perimeter"* as a second sentence (the summarising restatement R-DA-03 and R-DA-12 drive to 0.000, and an existential opener against R-DA-07's `There/It is` band); *", which means…"* (a `which` tail — wall 6 of MOVE-GRAMMAR §1.4, R-DA-03, and the gate's own 12.514-band-width finding — and the MEANING move, which exists nowhere in the estate, §1.3); *"the town cannot control entry"* (a CONSEQUENCE the card refuses under [MN], and a claim that reads across the watch and garrison spines — [FENCE]); *"cannot make a chokepoint of anything"* (a second consequence, and a totality). "Perimeter" is not carried into any wording: R-DA-22 asks one term for one thing, and the term is *wall*.
- **v2 `[visitor]`** — shipped: *"A stranger arrives at {settlement} from whichever direction suits him and is not obliged to pass anybody on the way in."* KEPT: the approach meets no wall ([MC], in the angle's vantage); the compass sense of *from whichever direction* survives lawfully in face 2's *from any quarter of the compass*, which is a totality over the compass and never over persons ([RC] held). DROPPED: *"is not obliged to pass anybody"* (a fact about persons on the way in that the card does not hold, a totality over persons under [RC], and a flat contradiction of the `garrison PRESENT` and `watch PRESENT` spines this can be composed beside — [FENCE]); *"him"* (a pronoun no `gender` field underwrites — NL-4's 234-line finding).
- **v3 `[street]`** — shipped: *"The town has an edge rather than a boundary at {settlement}, and the edge is wherever the last building happens to be."* KEPT: the town's edge is not a made wall ([MC]), as the sibling-licensed contrast. DROPPED: *"the edge is wherever the last building happens to be"* (a second fact, about the built fabric, on a field this card does not carry — [MN]); *"boundary"* becomes *"wall"* because the licensed fact is the fortification and a boundary reads also as a jurisdiction, a different field (a precision cut, never a plainness cut — §21.4). The `{settlement}` slot leaves this variant with the clause that carried it, which is lawful because the row and its faces move together and the parent's slot set is what a face must match.

## D. Per face — which card clause licenses each claim it makes

**Variant 1 `[ledger]` — the office's flat entry; the pool's short lines and its one settlement-token opener.**
- **Row, `{settlement} keeps no wall.`** — [MC] entire, in the block's own verb (its sibling spines read *keeps a garrison*, *keeps a watch*, *keeps monster work under charter*), so the pool's negative reads in the same idiom as the block's positives. Four words: the short line the register card licenses, and the line a falsy seed draws (canonical-at-zero, MOVE-GRAMMAR §3.2). *Keeps* is the office's plain word for what a town has in its keeping and carries no choosing — the WHY of a wall is `DS-DEF-11`'s and is untouched. No second claim: no cause ([MN]), no count ([MN]), no person ([RC]).
- **Face 1, `No wall stands at {settlement}.`** — [MC], the same fact with the wall as subject. Not an existential: the verb is a real one and the opener is a negative determiner, so R-DA-07's `There is` band is not entered. Closes on the slot, a proper noun, not a pronoun (R-DA-04).
- **Face 2, `What {settlement} goes without is a wall.`** — [MC] as a cleft, the ledger's construction. *Goes without* is plain idiom for the licensed lack and is the density §21.4 protects; it is a WORLD lack (the town has none), never a GAP in the record — no wording anywhere in this packet says the record is empty, silent or missing a return, because the GAP class needs a `not-held` field with provenance and this card holds none (R-DA-08; MOVE-GRAMMAR §1.2 row 11 class (b)). Closes on the civic object.
- **Face 3, `Nothing walls the town at {settlement}.`** — [MC] with the fact carried on a denominal verb, the packet's one such construction. *Nothing* is a totality over things, never over persons ([RC] held). No intent is given to an inanimate thing: nothing is the subject and nothing acts (R-DA-11).

**Variant 2 `[visitor]` — the vantage of the approach, with no observer's verdict in it.** The angle tag is a property of the row and licenses the VANTAGE (the way in, the approach, from outside) exactly as this block's other `[visitor]` rows do; it licenses no second claim. No face says what anybody sees, concludes, is obliged to do or is free to do, so [MN]'s standpoint is not entered and [RC]'s totality over persons is not approached.
- **Row, `A traveller comes the whole way into {settlement} without meeting a wall.`** — [MC] and only [MC]: what is asserted of the traveller is that the thing not met is a wall. *The whole way in* is the line's density and is the same fact stated to its end, not a second one. Twelve words, the pool's long line. The landed `NO organized force at all` v3 (`A stranger enters {settlement} unmet by any watch…`) is the block's own precedent for a traveller entering in the visitor angle.
- **Face 1, `The way into {settlement} passes no wall.`** — [MC] with the approach as subject. *Passes* is a path verb on a path; no intent is given to an inanimate thing (R-DA-11). *The way in* names no road, so no GEOGRAPHY move is made and no geography field is needed (MOVE-GRAMMAR §1.2 row 8).
- **Face 2, `An approach to {settlement} from any quarter of the compass meets no wall.`** — [MC]. *From any quarter of the compass* is the shipped row's *from whichever direction* kept inside the licence: a totality over the compass, licensed because the claim is about the wall's absence on every side of the town and not about who may come ([RC] held; the shipped row's *is not obliged to pass anybody* is what was dropped). Thirteen words, the packet's longest.
- **Face 3, `From outside, {settlement} is a town no wall encloses.`** — [MC]. *A town no wall encloses* is a contact relative and is restrictive, not a `which` tail (wall 6 held; the gate's measure at 0). *From outside* is the vantage with no perceiver named and no road presupposed.

**Variant 3 `[street]` — the town's plain idiom, slot-free, with no mind in it.** Nothing is asserted about what anyone in the town believes, notices, fears or has decided ([MN], [RC]). The variant carries no `{slot}`, on the landed pattern of this block's `walls PRESENT` v3.
- **Row, `The town has an edge and not a wall.`** — [MC], carried by the ONE contrast the pool spends. R-DA-02 licenses a contrast whose rejected alternative names a sibling pool key, and the rejected alternative here IS the sibling key `walls PRESENT`. It is not fronted (the subject is the town), it takes no completing *but*, and it is the closing move of this variant only — v1 and v2 carry no contrast at all, so the pool sits well inside R-DA-02's per-variant band. *An edge* asserts nothing the town could be without and adds no field; it is the near side of the licensed contrast, and §G puts it to the refuter for the second round running.
- **Face 1, `Open on every side, the town is unwalled.`** — [MC]. The absolute phrase is the face's rhythm; *open* is the state of the perimeter and not an invitation, and *every side* is a totality over the compass, never over persons ([RC] held).
- **Face 2, `Round the town no wall runs.`** — [MC] with the locative fronted. Ordinary English fronting, not costume: the antique air, if any, lives in the noun (R-DA-18), and no wording in this packet inverts subject and verb for period colour. Six words, the pool's shortest face.
- **Face 3, `Where the town ends, no wall stands.`** — [MC] under a fronted adverbial clause. It asserts nothing about WHERE the town ends — that is exactly the shipped second fact this rewrite drops — only that no wall marks the ending. Closes on a state verb, which is one of the four close kinds this card can fund (§F item 1).

## E. The walls, checked on the twelve wordings

Zero em dashes · zero exclamation marks · zero digits and zero percents · zero `which` in any position · zero questions · zero first or second person and zero personal pronouns of any person · zero citations and zero named holders ([SRC]) · zero named characters, totalities over persons, exemptions or theological claims ([RC]) · zero future indicatives and zero modal futures, every wording in the present ([MN]; A2; R-DA-07) · zero existential openers (`There is` / `It is`) · zero figures, similes, sense verbs on abstractions and inanimate intents (R-DA-11) · zero second sentences, zero three-segment sentences and zero which-tails (R-DA-03) · zero semicolons and zero colons · zero causes, consequences, counts, seasons and standpoints ([MN]) · zero facts of any sibling spine ([FENCE]) · zero pronoun closers (R-DA-04) · one bracketed angle tag per row and no `[plain]` marker anywhere (it is the modifier rows' marker and the projector refuses it on a spine) · **no face opens on a `proper`-typed slot** (T-F8, the round-1 refusal) · the settlement token opens exactly one wording, and that wording is a variant row (wall 10) · face slot sets equal their parents' throughout ({settlement}×1 for v1 and v2, empty for v3) · no two of the twelve wordings share their first two words.

Word counts, `{settlement}` counted as one word — variant rows in bold, then their three faces in order:

| variant | row | face 1 | face 2 | face 3 |
|---|---|---|---|---|
| 1 `[ledger]` | **4** | 5 | 7 | 6 |
| 2 `[visitor]` | **12** | 7 | 13 | 9 |
| 3 `[street]` | **9** | 8 | 6 | 7 |

Spread over the three VARIANT ROWS (R-DA-05's unit, which is the variant): mean 8.33, sample sd **4.04**, against the floor of 4.0 — met, and met by construction rather than by padding (the pool runs from a four-word ledger entry to a twelve-word approach). Over all TWELVE wordings: mean 7.75, sample sd 2.67 (round 1: 2.54). **The unit is flagged, not assumed:** if the gate measures spread per wording rather than per variant, 2.67 is below the 4.0 floor and this packet reports it as an exceedance whose cure would need a second licensed fact the card does not hold (§F item 2). Short lines (under eight words) are 6 of 12, well above R-DA-06's floor of 0.030; lines over thirty words are 0 of 12, well under its ceiling of 0.340.

The twelve closing words in order: wall · {settlement} · wall · {settlement} · wall · wall · wall · encloses · wall · unwalled · runs · stands. By kind: eight close on a named civic thing (the wall, or the town by name), one on a condition word, three on a verb of state. No pronoun closer; no abstract-noun closer (R-DA-04's `abstractNounRate` guard of 0.060 is not approached).

## F. Bands exceeded, reported (§16's channel: a soft rule reports its distance and fails only past the depth, the budget or the ceiling)

1. **R-DA-04, the KIND of close varies less than the closed set allows.** The set is {condition · prohibition · absence · object · a name not given}. This card licenses no duty or exemption (so no prohibition), no withheld name, and one civic object only — the wall that is not there. The pool therefore closes on an object, a condition or the absence itself, and the closing NOUN carries what variation is left. Reported, not cured: curing it would take a field the card does not hold.
2. **Sentence count goes uniform at one, and the pool's spread in sentence count is spent.** MOVE-GRAMMAR §3.4 says a rewrite may not spend a pool's spread, and the shipped pool had one two-sentence variant (v1) against two one-sentence variants. The second sentence of shipped v1 was the summarising restatement plus the `which`-tail consequence; both are WALLS, not bands, and a second sentence built from [MC] alone would be exactly the summarising second sentence R-DA-03 and R-DA-12 drive to 0.000. No second fact is available: [MN] refuses one outright, and S2's clause seat needs a CONSEQUENCE the engine computed of this sentence's own fact, which this card does not carry. The spread is rebuilt in WORD COUNT (row sd 4.04 against the shipped rows' 0.58 at 21, 21 and 22 words) and in construction, and the loss is declared here rather than paid for with an unlicensed clause. This is a words cut, never a variant cut: all three vids, numbers, angle tags and slot sets survive, and the wording count rises from three to twelve (§22 (a), (b), (e)).
3. **The one claim recurs in all twelve wordings.** With a single-clause claim there is nothing else to say, so VOCABULARY and RHYTHM carry the variation: the office's account in v1, the approach in v2, the town's own idiom in v3, and four distinct constructions of the fact inside every variant — a plain predicate, a negative-subject predicate, a cleft and a denominal verb; a long adverbial approach, a path subject, a compass totality and a contact relative; a licensed contrast, an absolute phrase, a fronted locative and a fronted adverbial clause. No face is a paraphrase of a sibling. Nine of the twelve close on the word *wall* or on the town's name; that is the recurring word, and the register card's rule is that the WORD may recur while the FACT must not.
4. **The ABSENCE move opens every wording.** Wall 3 of MOVE-GRAMMAR §1.4 says an ABSENCE never opens and never sits beside another ABSENCE. That wall governs the position of an absence move inside a composed order; it cannot bind a SPINE whose state key's own value IS the absence, because the composer puts the spine first and the pool has no other content. Declared here, with the reason, rather than left for the walker to find. No two absences sit adjacent inside any one wording.
5. **`wordsPerSentence` sits below the record registers' band.** The pool's mean of 7.75 words is short against exemplar registers whose sentences run longer, and the depth is unmeasurable from here. The cause is the card, not the writing: one claim, no cause, no count, no second fact. Reported so the gate reads it as a consequence of the licence rather than as thin prose, and so a refuter can put the card's poverty rather than the wording to the chair.

## G. THREAD and composition (owner, 2026-09-08 ~21:4x; MOVE-GRAMMAR §1.4.1)

This pool is a SPINE, so it is the passage's first sentence and hands the thread forward rather than picking one up; it must read well with nothing before it and must leave a noun for whatever modifier the composer seats next by salience. Every one of the twelve wordings puts *wall* or *the town* in its last four words, so the modifier that follows has the spine's own noun to carry forward. Three of the twelve end on a state verb (`encloses`, `runs`, `stands`) whose subject is *wall* in the same clause, so the noun is still live at the join. No wording ends on a pronoun, an abstraction or a slot the modifier cannot pick up, and no wording turns outward (there is nothing to turn outward to — the card holds one fact), so the passage's one turn outward remains available to the last modifier the composer seats.

## H. Refusals

**None.** All three variants are written lawful under the card; no variant is banked as a refusal row, and none is trimmed. The round-1 refusal was the projector's, on one face, and it is cured in §A.1.

## I. The two calls a refuter should test first

1. **Whether `an edge` in v3's row is a second fact.** The row reads *The town has an edge and not a wall.* It is written as ONE claim: the rejected alternative is the sibling pool key `walls PRESENT`, the single contrast R-DA-02 licenses, and *an edge* is the near side of that contrast rather than a field of its own — a settled place ends somewhere whether or not the ending is built, so the word adds no particular the card must hold. If the chair rules that *an edge* asserts a fact about the town's outer limit, v3's row loses the contrast and goes to the plain form (*The town is unwalled at its edge.*), the pool loses its only contrast, and the row's word count falls from nine to about six, which pulls the row sd of §E from 4.04 below R-DA-05's floor and turns §F item 2 into a harder report.
2. **The sibling-polarity note in [MC].** The printed card cannot tell `walls PRESENT` from `walls ABSENT`, and this packet is written on the pool key and the block's STATE-KEY group. If a car ever teaches the card the branch, this pool's card should print `may claim: that present does NOT hold`, and nothing in these twelve wordings changes.
