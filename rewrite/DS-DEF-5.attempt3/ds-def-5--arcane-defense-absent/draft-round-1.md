1. `[ledger]` {settlement} keeps no arcane provision in its defenses, neither warding nor detection.
   - `[face]` {settlement}'s defense answers what arms can meet, and beyond that reach the town keeps nothing.
   - `[face]` Conventional arms are the whole of {settlement}'s defense.
   - `[face]` No ward stands anywhere in {settlement}.
2. `[street]` The town has no answer to anything it cannot see.
   - `[face]` The defense here is made of what can be seen and handled.
   - `[face]` Whatever the town does to look after itself is done by ordinary means, and warding is no part of the day.
   - `[face]` Nothing here is warded.
3. `[counterforce]` Where warding would stand in {settlement}'s defense, nothing stands.
   - `[face]` Of everything {settlement} keeps for its defense, nothing answers what cannot be seen.
   - `[face]` Detection is a thing {settlement}'s defense could carry and does not.
   - `[face]` Against what cannot be seen, {settlement} sets nothing.

--- NOTES

**The card, as this packet reads it.** LICENCE (block DS-DEF-5 · role spine · key `arcane defense ABSENT`): reads `magicWorks`; bag `{band, counterpart, faction, settlement}`, FILLED at this block's call sites `{settlement}`; seat/form not-a-seat-taker / sentence; move none declared; angle `counterforce ledger street`; source none, standing SOURCE-UNRESOLVED, NO citation licensed; may claim that the read holds as a STANDING fact of the record; may NOT a count, a cause, a season, a future, a standpoint, a second fact; refused columns a totality over persons, an exemption, a named character's fate, a theological claim.

**The one licensed claim.** The pool's key is the ABSENT branch of the `magicDef` institution group, so the single claim every face carries is: *the arcane arm of this town's defense is not held* — the ABSENCE move, class (a) LACK, a `none-exists` world field (MOVE-GRAMMAR §1.2 row 11), licensed by the card's `may claim` line and by nothing else. All twelve faces are claim-equal to one another (arm A6 across the faces) and to the licensed residue of the three shipped sentences.

**Claims dropped, per variant (the rewrite's purpose; the shipped breach is the corpus's known state).**
- Variant 1 `[ledger]` — dropped *"What arrives unseen here goes undetected and therefore unanswered."* Three unlicensed claims in one sentence: a SECOND FACT (card `may NOT`), a CAUSE carried on `therefore` (card `may NOT`; MOVE-GRAMMAR §1.3 the MEANING non-move — the summarising second sentence is a best-ai signature, Part B §16 (6)), and an assertion that things do arrive unseen at this town, which no field of the block holds (R-DA-20's field licence; the card's read is the institution group alone).
- Variant 2 `[street]` — dropped *"and is unprepared rather than protected in that respect."* A STANDPOINT (card `may NOT`) and a VERDICT (MOVE-GRAMMAR §1.3), plus a CONTRAST whose rejected alternative ("protected") names no sibling pool key and no sibling band (R-DA-02; wall 5 of MOVE-GRAMMAR §1.4); the `rather than` construction is also the register's own ration (R-DA-02's figure, 0.113 → the band's edge).
- Variant 3 `[counterforce]` — dropped *"Nothing unseen has come at {settlement},"* (a HISTORY claim, a closed negative span, with no event-provenance field — MOVE-GRAMMAR §1.2 row 2 and R-DA-19: no HISTORY move exists in R1 STATE) and *"the record of safety and the capacity to detect are two different things"* (a gnomic closer, the generalisation test — R-DA-12; a life-general claim no field holds). The surviving licensed core is the lack itself, kept in the counterforce angle as the alternative state the town could be in (R-DA-02 obeys (5): the alternative is a state, not a decision — no cause is written, because the card refuses one).

**Per-face licence.**
- 1 `[ledger]` numbered — one claim: the group is not held; card `may claim` (the STANDING fact) with the group named by its own two members, `warding` and `detection`, which is the read's own content and not a second field. Slot `{settlement}` from the card's FILLED bag. No source named (card: no citation licensed).
- 1 face a — same claim, stated as the defense's composition and its scope; `beyond that reach the town keeps nothing` is the same lack, not a capability field. Card `may claim`.
- 1 face b — same claim, stated positively as the conventional whole. Card `may claim`.
- 1 face c — same claim, on the object noun. Card `may claim`; the totality is over the arcane group (empty by the read), never over persons (REFUSED COLUMNS honoured).
- 2 `[street]` numbered — the lawful half of the shipped line, kept as first written (§21.4, the density law: a lawful compressed line is not made plainer). `no answer to anything it cannot see` names the group by its own content, which the sibling pool `arcane defense PRESENT` defines as detection, wards, and an answer to what conventional arrangements cannot see coming. Card `may claim`. Slot set empty, as the shipped variant's is.
- 2 face a — same claim as the defense's material. Card `may claim`.
- 2 face b — same claim; `the day` is the block's own street noun. Card `may claim`. One sentence, one fact: no qualification rides as a tail (R-DA-03).
- 2 face c — same claim, the short line (R-DA-05: rhythm follows load; the short line exists).
- 3 `[counterforce]` numbered — same claim, in the subjunctive against the state the town could be in (A2, the edge is subjunctive; R-DA-02's sibling-licensed alternative, the sibling key being `arcane defense PRESENT`). No cause, no decision, no cost: the card refuses all three.
- 3 face a — same claim, scoped over what the town keeps. Card `may claim`.
- 3 face b — same claim as the unheld alternative. Card `may claim`; `could carry` is the alternative state, not a capability field and not a forecast (no bare future indicative anywhere — R-DA-07, A2).
- 3 face c — same claim, fronted, the short counterforce line. Card `may claim`.

**Walls checked across all twelve faces.** No em dash · no exclamation · no question · no digit or percent · no `which`-clause · no citation and no holder named (source unresolved) · no first or second person · no figure, no sense verb on an abstraction, no intent for an inanimate thing · no future indicative · no tricolon · no `rather than` · no existential opener · no gnomic closer · no totality over persons · no named character, no deity claim. Every face is one sentence carrying one fact. Slot sets are constant inside each variant (variant 1 `{settlement}`, variant 2 empty, variant 3 `{settlement}`) and drawn only from the card's bag.

**Dispersion.** No two of the twelve faces share their first two words. The settlement token opens exactly one face of one variant (R-DA-17's per-pool opener rule; wall 10 of MOVE-GRAMMAR §1.4). Face lengths run 4 to 21 words, so the pool's within-pool spread is carried by the faces and not by a metronome (R-DA-05).

**The thread (owner, 2026-09-08 ~21:4x).** This pool is a SPINE, so it is read first and hands the thread on. The nouns each face leaves available to a following modifier: variant 1 — the defense, the arms, the ward, the detection; variant 2 — the town, the defense, the warding, the day; variant 3 — the warding, the defense, the detection. Every face names at least one of `defense`, `arms`, `warding`, `ward` or `detection`, so a modifier can carry a spine noun forward from whichever face the roll returns.

**Three judgment calls, recorded for the refuter and the sitting (not refusals).**
- (i) *ABSENCE as the opening move.* MOVE-GRAMMAR §1.4 wall 3 says ABSENCE never opens; its citation is R-DA-08, which governs class (b) GAP (a fact of the record). This pool's state key IS the class (a) LACK, a fact of the world, and the spine has no other move to open on. Read as lawful; if the sitting rules the wall covers class (a) as well, every ABSENT pool of this block falls with it, not this pool alone.
- (ii) *Naming the group by its content.* Four faces state the lack as `no answer to what cannot be seen` / `nothing answers what cannot be seen`. This is treated as the read's own content, because the twin pool `arcane defense PRESENT` defines the group as detection, wards, and an answer to what conventional arrangements cannot see coming — so the phrasing denies the group, not some narrower scope, and the faces stay claim-equal. No face asserts that anything unseen exists or has come; that claim was dropped from variant 3.
- (iii) *A shape echo with a sibling pool.* Face 3b (`could carry and does not`) shares its shape with the shipped `militia PRESENT (no garrison)` counterforce row (`could keep soldiers and does not`) in this same block, and the composer may mount both on the defense tab. It is used in exactly one face of twelve and carries no cause, where the militia row carries a decision. Flagged for the refinement round as the pool's likeliest template finding.

**Refusals: none.** All three variants are written lawful under the card; no variant is banked.
