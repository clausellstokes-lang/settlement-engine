# DS-DEF-5 · `walls PRESENT` · REWRITE draft round 1

Seat: Opus 5 (Fable-unvalidated) · block `DS-DEF-5` · role `spine` · 3 variants in, 3 variants out, 12 wordings (the numbered row plus three faces on every variant).

Paste-ready: the three numbered rows below are the COMPLETE replacement for the pool's three variant rows under the bold pool line **walls PRESENT**. Same vids, same order, same angle tags, none added, none removed, none merged. The pool declares no typed lines (ROLE, READS, RELATION, ATTACH, FORM, MOVE) in the annex today; none is touched and none is repeated here.

1. `[visitor]` Seen from outside, {settlement} ends at a wall.
   - `[face]` What stands at the edge of {settlement} is a wall, and the town lies within.
   - `[face]` The wall of {settlement} is the town's outer bound.
   - `[face]` Beyond the wall lies {settlement}.
2. `[ledger]` {settlement} is walled, and a wall is what the town holds under the head of defense.
   - `[face]` On this account {settlement} stands walled, and stands so at present.
   - `[face]` Fortification at {settlement} is a wall, and that wall is standing.
   - `[face]` What {settlement} carries here is a wall.
3. `[street]` The town has a wall for its edge.
   - `[face]` Round the town goes a wall.
   - `[face]` Inside the wall is where the town is, and the wall is up.
   - `[face]` Walled is the state of the town.

--- NOTES

## A. The card, printed, and the clause labels used below

`node scripts/prose-licence-card.mjs DS-DEF-5 'walls PRESENT'` returns: role **spine**; **reads** `forces.walls.present` (measured; absent ⇒ no candidate, and a modifier is silent, never "false"); **predicate** none recovered (the pool has no key-function branch); **bag** `{band: RESERVED, counterpart: proper, faction: proper, settlement: proper}`, FILLED at this block's call sites `{settlement}`; **relation** none and **attach** empty (a spine is the seat and takes neither); **seat/form** not a seat-taker / `sentence`; **move** none declared; **angle** `ledger street visitor`; **echo** spine mounts 2 (tab `defense`), modifier mounts 0, with the card's own caveat that the echo table is keyed on the producer-token root `forces` and is coarser than this pool's read; **covert** no; **source** `muster`, standing LICENSED; **audience** player, no mark.

- **[MC]** — the card's `may claim` line: *that `present` holds, as a STANDING fact of the record.* One claim, no halves. Read with `reads` (`forces.walls.present`, a boolean) this is the whole of what the pool may assert: the settlement's wall is there, now, on the record. The block's own STATE-KEY confirms the grain — `Fortifications` (walls) is one of six presence-and-content groups, and this pool is the presence half of the first (`RECEIPT_POOLS_DOSSIER_STATE.md:2864-2868`).
- **[MN]** — `may NOT`: a count, a cause, a season, a future, a standpoint, a second fact.
- **[BAG]** — `{settlement}`, `proper`, FILLED; `{faction}` and `{counterpart}` are not filled at this block's call sites and `{band}` is RESERVED, so no wording names them. Variants 1 and 2 carry exactly one `{settlement}` in the row and in each of their three faces; variant 3 carries none in the row and none in any face, exactly as the shipped variant 3 carried none. Every face's `{slot}` set therefore equals its parent's (§2.5's face refusal held), and no face row opens on a `proper`-typed slot (T-F8) — only the variant-2 numbered row does, which is the one settlement-token opener wall 10 of MOVE-GRAMMAR §1.4 allows this pool.
- **[SRC]** — source `muster`, standing LICENSED: a citation of this holder is licensed only *where the provenance budget allows*. **No wording in this packet cites anything.** Part B §24 sets the budget at one citation per unit, on a licensed holder, and only for one of S3's three reasons — two accounts that disagree, a count from an interested party, a record whose keeper is a power. This pool holds one boolean, no second account and no count, so no reason applies; the exemplar registers with raw text cite at 0 per 786 sentences, and a citation habit is a refuter's finding. Arm A13 has nothing to catch.
- **[RC]** — REFUSED COLUMNS, always: a totality over persons; an exemption from a duty; a named character and that character's fate; a theological claim. None is approached: the subject of every wording is the town or the wall, never its people counted, excepted or named.
- **[FENCE]** — the block's own PROVENANCE + FENCE (`:2870-2881`): the surface today narrates only ABSENCE and this shape authors presence at the same density; a group's presence asserts capability, never history; *walls ride `defenseProfileHasWalls`, never a presence check*. The history half of the fence is obeyed absolutely — no wording carries an event, a raising, a building or a repair. **The capability half is read narrowly, and the reading is declared in §G below**, because the card is the authority the brief names and its `may NOT` line refuses a second fact.

## B. The claim set, and what was dropped from each shipped variant

All twelve wordings carry [MC] and nothing else, so the four faces of each variant are claim-equal to each other (arm A6 read across the faces, not back to the shipped sentence). What the shipped rows carried and the card does not license is dropped, which is the rewrite's purpose and the shipped breach is the corpus's known state; nothing is added.

- **v1 `[visitor]`** — shipped: *"There is a line around {settlement} and it is kept: the gates shut, the works are repaired, and a stranger enters where the town intends him to."* Kept: a perimeter stands around the settlement ([MC]). Dropped: *"it is kept"* and *"the works are repaired"* (upkeep and repair — a second fact under [MN]; the read is a boolean of presence and holds no maintenance value, and repair is a HISTORY move with no event-provenance field, MOVE-GRAMMAR §1.2 row 2); *"the gates shut"* (a gate is a particular the read does not hold, and its shutting is an operational second fact — R-DA-10's every-particular-licensed and fault 24's hollow specificity); *"a stranger enters where the town intends him to"* (a person the world does not hold, a control-of-entry capability, and *intends* is an intent attributed to an inanimate collective — [MN]'s standpoint, R-DA-11's inanimate-intent floor, and a restatement of the sibling pools `garrison PRESENT` and `watch PRESENT`, which own the stranger-at-the-gate line in this block: arms A1 and A11). The three-segment tail also broke R-DA-03.
- **v2 `[ledger]`** — shipped: *"{settlement} controls its own entry points. That is what a perimeter buys: not invulnerability, but the choice of where anything happens."* Kept: the wall is there ([MC]). Dropped: *"controls its own entry points"* (a capability claim distinct from presence — the V2 grammar `PRESENT → CONSEQUENCE(structural)` needs a structural-consequence field beside the state key, MOVE-GRAMMAR §2.1, and this card declares no field beyond the boolean and no move at all); the entire second sentence — *"That is what a perimeter buys"* is the MEANING move, which does not exist anywhere in the estate (MOVE-GRAMMAR §1.3; R-DA-03's 2nd-sentence summary → 0.000; R-DA-12's generalisation test), and *"not invulnerability, but the choice of where anything happens"* is a fronted antithesis whose rejected alternative names no sibling pool key and no sibling band (R-DA-02; wall 5 of §1.4).
- **v3 `[street]`** — shipped: *"The town has a wall and arranges its day around the gates without noticing that it does."* Kept: the town has a wall ([MC]), and the shipped clause is kept nearly verbatim because it was already the licensed half. Dropped: *"arranges its day around the gates"* (a practice of the town's people and a gate particular — a second fact under [MN], unlicensed under R-DA-10); *"without noticing that it does"* (a belief frame over the town's people — [MN]'s standpoint, [RC]'s totality over persons, and R-DA-13's belief floor, which is EXECUTABLE today and stands).

## C. Per face — which card clause licenses which claim

Every one of the twelve wordings asserts exactly ONE claim and it is the same claim in all twelve: [MC], on the read `forces.walls.present`, with the slot fill licensed by [BAG]. The variation is carried entirely by vocabulary and rhythm, which is what §3.4 of MOVE-GRAMMAR lets a rewrite spend.

**Variant 1 `[visitor]` — the wall as the town's outer limit, taken from the far side.** The angle licenses a VANTAGE and never a second claim. **No face names a stranger, a traveller or an arrival**, deliberately: the shipped visitor rows of `garrison PRESENT`, `watch PRESENT`, `NO organized force`, `charter hall PRESENT` and `arcane defense PRESENT` all open on a stranger, so a person here would both spend an unlicensed claim ([MN]) and collapse the sibling distance arms A1 and A11 protect. The vantage survives as position — outside, edge, beyond — which is analytic to a perimeter and asserts nothing the boolean does not.
- Row: *"ends at a wall"* is [MC]; where the town ends is where the wall is, which is the enclosure the read reports. *"Seen from outside"* is a vantage with no experiencer, so no standpoint is asserted. `{settlement}` by [BAG].
- Face 1: *"What stands at the edge of {settlement} is a wall"* is [MC] in a pseudo-cleft; *"the town lies within"* restates the same enclosure from the inside and adds nothing. The thread carries `wall` forward to `town` (§1.4.1).
- Face 2: *"is the town's outer bound"* is [MC]; a bound is what a wall present is, not a capability it confers.
- Face 3: [MC] in five words, the short line the register card licenses, closing on the town itself.

**Variant 2 `[ledger]` — the wall as a holding of the record.**
- Row: *"is walled"* is [MC]; *"a wall is what the town holds under the head of defense"* is the same fact restated in the office's own accounting idiom, `holds` being the holding of R-DA-14's vocabulary and `the head of defense` the record's own section heading, which is the block's `SECTION-TARGET`. Naming its own heading is the office showing through what it holds (R-DA-01) and is **not** a provenance move: no holder is cited, so [SRC]'s ceiling and §24's "the office does not cite its own books" are both clear.
- Face 1: *"stands walled, and stands so at present"* is [MC] with the STANDING modality the card's own words carry; `at present` is the standing fact's tense, never a season ([MN]).
- Face 2: *"is a wall, and that wall is standing"* is [MC]; `Fortification` is the group name the block's STATE-KEY gives this row and names no construction the read does not hold.
- Face 3: [MC] in seven words; `carries` is the ledger's verb for what an entry holds.

**Variant 3 `[street]` — the wall in the town's plain idiom, with no mind in it.** The angle survives as vocabulary; nothing is asserted about what anyone in the town believes, notices, does or fears ([MN], [RC]). No slot is written in any of the four, matching the shipped row.
- Row: *"has a wall for its edge"* is [MC]; the shipped row's own licensed clause, closed on a civic thing.
- Face 1: [MC] inverted, six words.
- Face 2: *"Inside the wall is where the town is"* is [MC]; *"the wall is up"* is the plain idiom for the same standing fact.
- Face 3: [MC] as a fronted predicate; `the state of the town` names the record's own reading of it and adds no field.

## D. The walls, checked on the twelve wordings

Zero em dashes · zero exclamation marks · zero digits and zero percents anywhere, including the connectives · zero `which` clauses · zero questions · zero first or second person · zero citations and zero named holders ([SRC]) · zero named characters, totalities over persons, exemptions or theological claims ([RC]) · zero future indicatives and zero bare futures; no edge is written, so no subjunctive is owed (A2, R-DA-07) · zero existential openers, the shipped *"There is a line around…"* being the one this pool carried · zero figures, similes and inanimate intents (R-DA-11) · zero second sentences, zero three-segment sentences and zero which-tails (R-DA-03) · zero semicolons and zero colons · every connective is `and` · zero gates, works, repairs, strangers, entries, soldiers, watchmen or wards named ([FENCE]; arms A1 and A11 against the block's nine sibling pools) · one bracketed angle tag per row, no `[plain]` marker anywhere · one slot only, `{settlement}`, on variants 1 and 2 and none on variant 3, face sets equal to parent sets · no face row opens on a proper-typed slot (T-F8) · the settlement token opens exactly one variant of the pool, variant 2, and its neighbours do not (wall 10) · no two of the twelve wordings share their first two words (A11): `Seen from` · `What stands` · `The wall` · `Beyond the` · `{settlement} is` · `On this` · `Fortification at` · `What {settlement}` · `The town` · `Round the` · `Inside the` · `Walled is`.

Closing nouns, four distinct per variant, and no pronoun closer anywhere (R-DA-04's `closers.pronounRate` at 0.000 against a ceiling of 0.055): v1 `wall` · `within` · `bound` · `{settlement}`; v2 `defense` · `present` · `standing` · `wall`; v3 `edge` · `wall` · `up` · `town`.

Word counts, `{settlement}` counted as one word — variant rows first, then their three faces in order:
- v1 `[visitor]`: **8** · 15 · 9 · 5
- v2 `[ledger]`: **16** · 11 · 11 · 7
- v3 `[street]`: **8** · 6 · 13 · 7

Within-pool spread over the three variant rows: mean 10.67, sample sd **4.62**, against R-DA-05's floor of 4.0 — met. Over all twelve wordings: mean 9.67, sample sd 3.55; four of the twelve sit under eight words, which is the short line R-DA-06's `< 8` floor asks for and the register card licenses outright.

## E. Bands exceeded, reported (§16's channel: a soft rule reports its distance; it fails only past the depth, the budget or the ceiling)

1. **R-DA-04, the KIND of close does not vary.** All twelve wordings close on a CONDITION or an OBJECT of a civic thing. The close-kind set is {condition · prohibition · absence · object · a name not given}; this card licenses no `none-exists` field (no absence — that is the sibling pool `walls ABSENT`), no duty or exemption ([RC], and `whoIsExempt` is null everywhere), and no withheld name. Two of the five kinds are reachable and both are used; the CLOSING NOUN varies across all twelve as listed in §D.
2. **One claim, twelve wordings.** With a single boolean read there is nothing else to say, so vocabulary and rhythm carry the whole variation — the outside vantage in v1, the record's accounting idiom in v2, the town's plain idiom in v3, with sentence lengths from five to sixteen words. No face is a paraphrase of a sibling, but the distance between faces is narrower here than on a banded pool, and the distance is reported so the refuter reads it as a measured consequence of the card and not as a tic.
3. **Sentence count goes uniform at one, and the pool gets shorter.** All three shipped variants ran to two sentences or to three segments; every second sentence and every third segment carried an unlicensed claim, and a second sentence built from [MC] alone would be exactly the summarising second sentence R-DA-03 and R-DA-12 drive to 0.000. This is a words cut, never a variant cut: each of the three keeps its vid, its number, its angle and its slot set, and the face count rises from three wordings to twelve (§22 (a) and (e); the counts only ever rise).
4. **The three angles converge in claim and are told apart only by register.** `[visitor]`, `[ledger]` and `[street]` differ lawfully in standpoint and grammar and cohere in structural fact, which is what chair C-3's C-sibling arm asks; but a reader who meets two of them across two towns meets the same fact in the same shape. That is the pool's nature, not a defect of the drafting, and it is why the four faces matter more here than on a pool with bands.

## F. Refusals

**None.** All three variants were made lawful under the card; no variant is banked as a refusal row.

## G. The three calls a refuter or the chair should test first

1. **The fence's "presence asserts capability" against the card's "may NOT: a second fact."** The block's own FENCE says a group's presence asserts capability, never history. Read widely, that would license the shipped v2's *"controls its own entry points"* as the structural entailment of a perimeter — MOVE-GRAMMAR's V2, `PRESENT → CONSEQUENCE(structural)`. This packet reads it narrowly and drops the control claim, on three grounds: the card is the authority the brief names and its `may NOT` line refuses a second fact; V2 needs a structural-consequence field beside the state key and this card declares none; and the card's `move` line is `(none declared)`. **If the chair rules the wide reading**, every wording here may take a second clause naming what a perimeter makes possible, the pool's shipped v2 recovers most of its first sentence, and this draft is the conservative one. The ruling should be taken across the block's nine presence pools at once, not on this pool alone, because `garrison`, `watch`, `mercenary`, `charter` and `arcane defense` all sit under the same fence sentence.
2. **The cross-block echo on the wall.** This pool's spine asserts the wall is present; `DS-DEF-11`'s four WALLED-\* spines assert the same fact with its rationale, and both blocks mount on the `defense` tab. The card's own echo line counts spine mounts 2 for this pool and warns that the echo table is keyed on the producer-token root `forces`, which is coarser than this pool's read, so a mount counted there may be reading a sibling field. A composed page that draws both blocks says the wall twice. That is a composer echo-bound question (ARCH §4.6), not a wording one, and no wording here can cure it; it is named so the chair sees it before the freeze. The vocabularies are kept apart on purpose: `DS-DEF-11`'s rewritten rows turn on `{defwork}` and on `entered` / `carried` / `stands`, so this packet uses `wall` as a bare common noun (this block has no `{defwork}` slot and its SLOTS line offers none) and reserves `holds` and `carries here` for its ledger face.
3. **`Fortification` as the row's own noun.** The word is the block's STATE-KEY group name for this row (`Fortifications` (walls)) and is used once, in v2 face 2, as a heading noun rather than as a construction. It names no material, no kind and no count. If the chair reads a group label as chrome that the prose may not borrow, that one face loses its opener and takes `Under this head` instead, at no cost to the claim.
