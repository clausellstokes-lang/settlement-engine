# THE CLAIM-FREEZE AUDIT OF THE ECONOMY LEAF — 319 re-cut units read against 7992713d0 (a chair reader + six Opus slice readers; 2026-09-14)
COUNT: read 319 · moved 3 · undeclared 42b 0 · clean 316. Mechanical controls all green (slot multiset, hedges, modals, causal words, negations, simile markers identical both sides; only 26 of 319 units carry any content-word delta). The `That is X` sort: 23 relation-kept, 3 verdict-only that carry content the shipped tail asserted — none touched.

## THE CHAIR'S RULINGS
R-ECO-1. Finding 1 APPLIED (a definite future made a gnomic present); finding 2 APPLIED (a generic sent to `is going to` against the leaf's own six-to-one precedent — the plain present).
R-ECO-2. Finding 3 CLEARED: `X is the measure of Y` and `X measures Y` are idiomatic equivalents here; the exclusivity is not a claim the shipped line pressed. The lane's recast stands (it was made to hold the X arm).
R-ECO-3. 42b does NOT reach idiom (`will do` → `are enough` stands); R-C stopped at metaphor and stops there.
R-ECO-4. THE GRANARY SYNONYM ROW IS SIGNED: `fieldSynonyms.js` gains `{ field: 'granary', nouns: ['grain store', 'store', 'stores', 'grain'] }` — the lane measured it strictly a shrink on the A0b arm (under-claims 1120→1109, over 54→54, implicit negations 9→9). With it, ruling 42c is applied to the THIRTEEN `granary` spines (11 in DS-ECO-2, 2 in DS-STR-1 FAMINE) — `a grain store` / `the store` per the LEVEL1 rule (check `orderIdOf` per spine; `the grain store` adds OBJECT; where `answers`/`holds` makes it an INSTITUTION move, keep the move by keeping the verb's object a body: `the grain store answers` is still the institution). Any spine where no 42c wording holds its order STAYS `granary` and is named.

## THE CUT (the `+` is the exact text at bc1773983)
1. DS-STR-2 `COUNTERFORCE: weak or absent` v1 [street]: `It ends when it ends, and not because of anything {settlement} does.` → `It is going to end when it ends, and not because of anything {settlement} does.`
2. DS-ECO-10 `SCARCITY: shortage` v3 [visitor]: `A stranger wanting {good} in {settlement} is going to find it and pay for the finding.` → `A stranger wanting {good} in {settlement} finds it and pays for the finding.`
3. THE SYNONYM ROW + the thirteen granary spines under R-ECO-4 (the A0b ratchet must not rise; the census must stay byte-identical; every spine's `orderIdOf` before = after).
