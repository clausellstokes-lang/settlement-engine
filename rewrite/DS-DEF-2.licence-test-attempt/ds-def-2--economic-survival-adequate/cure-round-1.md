# CURE ROUND 1 — DS-DEF-2 · `Economic Survival: ADEQUATE`

Seat: an **Opus** CURER seat (a different author from the drafter and the refiner), 2026-09-11, for the
Fable chair. The COMPLETE replacement rows of the pool: every variant in order under its own vid with its
ONE angle tag, every face. FOUR faces are changed — the four targets the chair's judgment named
(v1 f2, v2 f1, v2 f2, v3 f2). Every other face is BYTE-IDENTICAL to the annex at HEAD
(`471ce894a83658b0e0fe480819a9f1dcd22605e3`), including v3 f3, which the judge ruled a REVERT and which
already stands reverted in the annex.

STATUS: COMPLETE — 4 targets, 4 cured, 0 refused.

1. `[ledger]` The capacity {settlement} holds against a crisis measures adequate. That capacity is carried with little margin.
   - `[face]` Against a crisis {settlement} is covered. The cover is thin at its edge.
   - `[face]` What {settlement} can meet in a crisis stands adequate. That standing sits on a narrow margin.
   - `[face]` Adequate is the word entered for {settlement} against a crisis. Under that word the margin is not wide.
2. `[unfolding]` The town's capacity to meet a crisis is real, and the margin left standing on it is little.
   - `[face]` Set against a crisis the town's standing is adequate, and the little margin is the part left standing open.
   - `[face]` This town is covered against an emergency on a little margin, and that margin stands open.
   - `[face]` What covers the town against a crisis holds, and the little margin on that cover is the matter left standing.
3. `[threshold]` Cover against a crisis is adequate at {settlement}, and the margin under it would be little.
   - `[face]` A crisis would be met at {settlement}, and met on a little margin.
   - `[face]` The covering at {settlement} stands, and its edge would be close in a crisis.
   - `[face]` In a crisis {settlement} is covered, and the margin of the cover would be little.

--- NOTES

One line per target: the cure made, and the law behind each claim the cured face carries.

- **v1 f2 · CURED** (was `Economic survival at {settlement} stands adequate. That standing sits on a narrow margin.`).
  The verdict's ground was W14 THE LABEL BAR with skeleton §0.2 and §8's label wall ("never 'survival' as an
  outcome"), the card's may-NOT (a future) and arm A6. The cure takes the refiner's own recorded fallback, which
  the refuter and the judge both named in terms, and which cures the label and the near-twin with face 0 in one
  move: the label phrase is gone and the arm is written at its ENGINE meaning as a free relative —
  `What {settlement} can meet in a crisis` — the spelling skeleton §0.7 fence 1 licenses for the axis
  ("the town's capacity to meet a crisis"; `meet` is fence 1's recommended verb, the one with no condition-word
  neighbour), at the read's own AGGREGATE layer with no body and no component (W20; census `objectClasses []`).
  Claim by claim: the COVER half is `stands adequate` — the band's own word as a standing fact of the record
  (D-22, the band word is exactly the ladder's reading; R-DA-07, the present stands, the copular posture verb
  kept); the MARGIN half is the untouched second sentence `That standing sits on a narrow margin.`, the ladder's
  authored "with little margin" in the writer's own words at the same claim (skeleton §0.7 fence 3), `sits on`
  being the skeleton's own register for a band's position and a posture verb, not a sense verb or an intent
  (R-DA-11 untouched); `{settlement}` is the parent's slot set and does not open the face (ARCH §2.5 T-F8;
  MOVE-GRAMMAR §1.4 wall 10); TWO sentences, the parent's pin (A11), the second carrying `stands` → `That
  standing` forward (R-i, the thread at k = 0; §1.4.1) and stating the other half rather than summarising the
  first (W6; R-DA-03; R-DA-12). Construction: a FREE-RELATIVE subject with the capability verb, which is a fourth
  subject AND a fourth construction against face 0's complex-NP subject with `measures`, face 1's fronted-pressure
  copula and face 3's inversion (A5; ADDENDUM 7 rule 4; W4, the variant's V1 grammar unchanged). No claim added:
  the face carries exactly {the cover against the pressure class · the margin little · `{settlement}`}, the same
  set as its three siblings (A6). Hard walls re-scanned: no body, component, stock, pay or coin word, duration,
  season, count, particular crisis, trend, forecast, cause, comparison, person, belief, totality, figure,
  citation or record noun (skeleton §8; W16; W20; W22; W24; A13).

- **v2 f1 · CURED** (was `… and the little margin is what that standing leaves open.`).
  The verdict's ground was R-DA-11 THE FIGURE POLICY ("nothing inanimate acts with intent"; the register card's
  "an intent for an inanimate thing") with move 10's declarative form — an abstraction made the AGENT of a
  transitive act, the identical shape the refiner struck from variant 1 and left standing here. The cure is the
  one the refuter and the judge named: the OPEN is recast AGENTLESS as `the part left standing open`, so no
  abstraction acts on anything, while the fronted pressure adjunct, the standing as subject and the information
  order R-iv wants are all kept unmoved (W4 THE CONSTRUCTION CONTRACT). Claim by claim: the COVER half is
  `the town's standing is adequate` — the band word on the town's own reading, the town being the licensed
  possessor (the score is `defenseProfile.scores.economic`), at AGGREGATE with no body (W20); the MARGIN half is
  `the little margin`, the ladder's authored phrase verbatim (D-22; skeleton §0.7 fence 3); the OPEN move lands
  LAST, after the presence, which is what R-iv and W8 require of the `[unfolding]` angle, in move 10's own
  declarative vocabulary (a civic matter left standing open and named — MOVE-GRAMMAR §1.2 row 10, "declarative
  always"), and it settles nothing past the margin (skeleton §0.7 fence 4). No `{settlement}` anywhere, the
  parent's empty slot set (ARCH §2.5); ONE sentence with one `, and` joint, the parent's pin against vid 1's two
  (A11; R-DA-06's ration). Thread: `standing` → `that standing` is gone with the transitive clause, and the
  thread now runs on the deliberate `standing` → `left standing` noun echo §1.4.1 licenses. No claim added or
  lost — the face is claim-equal to its three siblings (A6). No movement, season, length, history, trend or
  forecast word (the fence R-DST-W4-b and annex V1-e already ruled on); no body, component, pay or coin word,
  person, belief or totality (skeleton §8).

- **v2 f2 · CURED** (was `This town holds through an emergency on a little margin, and that margin stands open.`).
  The verdict's ground was the card's may-NOT (a future) with skeleton §8's "NO FORECAST, NO OUTCOME PAST THE
  EDGE", W14's label wall (survival never an outcome), R-DA-12 (the generalisation / habitual) and A6: the
  capability frame had been replaced by an OUTCOME in the present indicative, habitual as written. The cure is
  the one named: the copular COVER frame is restored — `is covered against an emergency` — which is the ladder's
  authored first half in the record's own flat copula (R-DA-07 keeps the copula; D-22, the band word is exactly
  the ladder's reading), so nothing is said about what an emergency would do or how it would end, and the
  habitual reading is gone with the eventive verb. Everything else in the face is kept: `This town` seats the
  band on THIS town rather than on a kind of town, so the maxim frame W5 and R-DA-12 refuse does not arise;
  `an emergency` is the engine's own second word for the pressure CLASS (`threatAssessment.js:167`; skeleton
  §0.2's lawful nouns; §0.7 fence 2) and carries no particular crisis and no length (W17; R-DA-16; the card's
  "a season"); `on a little margin` is the ladder's authored second half (D-22; fence 3); and the face keeps its
  own OPEN idiom `that margin stands open`, distinct from its three siblings' (`left standing` · `left standing
  open` · `the matter left standing`), landing LAST as R-iv and W8 require. No `{settlement}` (the parent's empty
  slot set); ONE sentence, one `, and` joint (A11). Thread: `margin` → `that margin` (R-i; §1.4.1). Construction
  unchanged and still distinct from the siblings — the TOWN as subject with the margin as a PP adjunct, against
  face 0's capacity-NP subject, face 1's fronted adjunct and face 3's free relative (A5; W4). No claim added: the
  face now carries exactly {the cover against the pressure class · the margin little · the margin open}, claim-equal
  across the variant (A6).

- **v3 f2 · CURED** (was `The covering at {settlement} stands, and in a crisis its edge would be close.`).
  The verdict's ground was W1 POSSESSOR BINDING with the card's may-NOT (the edge LOCATED — skeleton §(5) on
  vid 3, "never located"): the possessive followed the noun `a crisis` immediately and bound to it, so the
  available reading was THE CRISIS'S edge being close. The cure is the one named, and it adds no word and moves
  no claim: the adjunct comes off the front of the second clause, so `its` now sits against the clause whose
  subject is `The covering` and binds to it (W1's own cure — move the possessive next to what it binds). Claim
  by claim: the COVER half is `The covering at {settlement} stands` — the ladder's first half as a standing fact,
  on the arm's own noun at AGGREGATE, naming no body and no component (W20; D-22); the MARGIN half is
  `its edge would be close`, the ladder's "with little margin" stated as the `[threshold]` angle's own business —
  the edge SUBJUNCTIVE (MOVE-GRAMMAR §1.4 wall 2; R-DA-07 "the edge is subjunctive"), stated as the floor's
  NEARNESS in words, which is exactly what skeleton §0.7 fence 4 and §(5) license, and neither located, counted,
  crossed nor followed to an outcome (the angle's own bar; state never fate); `in a crisis` is the pressure class
  as a plain closing adjunct (fence 2), naming no particular crisis and no length. `{settlement}` is the parent's
  slot set and does not open the face (T-F8; wall 10), and the face does not share vid 1's first two words (A11).
  ONE sentence, one `, and` joint (A11; R-DA-06). Construction still distinct from the siblings — a cover-noun
  subject with the EDGE as the second clause's subject, against face 0's margin-subject second clause, face 1's
  coordinate ellipsis and face 3's fronted-pressure passive (A5; W4) — and the close moves off the adjective onto
  the pressure noun, which widens the close-kind spread of the variant (R-DA-04's vary-the-kind limb). No claim
  added and none lost; claim-equal across the variant (A6).

### THE PINS THIS CURE ROUND DID NOT MOVE (checked face by face against the annex at HEAD)

- **Eight of twelve faces are byte-identical to the annex at HEAD** (v1 f0/f1/f3 · v2 f0/f3 · v3 f0/f1/f3);
  only the four named targets differ.
- **Vids, order, tags:** 1 `[ledger]` · 2 `[unfolding]` · 3 `[threshold]`, one bracketed tag each, no `[plain]`
  anywhere, no variant added, removed, merged or reordered (§22 "never trim"; the projector's tag contract).
- **Slot sets, exactly the parents':** vid 1 `{settlement}` ×4 · vid 2 **no slot** ×4 · vid 3 `{settlement}` ×4
  (ARCH §2.5). `{band}` and `{route}` are named by nobody.
- **Sentence counts, exactly the parents' (A11):** vid 1 TWO ×4 · vid 2 ONE ×4 · vid 3 ONE ×4.
- **Openers:** the settlement token opens NO face (T-F8; wall 10); the three numbered lines' first two words
  remain `The capacity` · `The town's` · `Cover against` (A11).
- **The three distinct level-1 grammars are untouched:** vid 1 V1 PRESENT with the qualification in its own
  sentence · vid 2 V6 PRESENT → OPEN · vid 3 PRESENT with a subjunctive EDGE tail (W4).
- **Both halves of the ladder's reading in all twelve faces** (skeleton §8's one fence this pool's siblings do
  not carry): the COVER *and* the LITTLE MARGIN.
- **Hard walls re-scanned over the twelve faces:** no em dash · no exclamation · no question · no digit · no
  percent · no which-clause · no "I"/"you" · no body · no component · no stock word · no pay or coin word · no
  duration, season or count · no particular crisis · no trend, onset or movement · no forecast or outcome past
  the edge · no cause · no comparison to a norm or another rung · no person, belief or totality · no figure ·
  no citation and no record noun.
