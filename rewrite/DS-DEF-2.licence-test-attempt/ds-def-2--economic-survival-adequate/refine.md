# REFINE — DS-DEF-2 · `Economic Survival: ADEQUATE`

Seat: an **Opus** REFINER seat (Opus 5, Fable-unvalidated), 2026-09-11, for the Fable chair — a
DIFFERENT author than the drafter, on purpose (Part B §21 phase 2; §21.1 the ceiling not the middle;
§21.2 every set is refined; §21.3 one effort; §21.4 the density law).

Refined one for one from `draft-round-1.md` against the printed licence card, `skeleton.md`, the
shipped rows (skeleton §0.5), the register card with S2 and S3, Part B §1 and §16–§23,
MOVE-GRAMMAR §1–§3 / §1.4.1 / §4.4.1–§4.4.3, CLERK-LAWS §2.4.1 / §2.6.1, ARCH v2 §2.5 and §8.3,
and brief ADDENDA 12 and 13 A + B (W1–W27).

**Nothing added, nothing dropped, nothing merged.** Three variants, four faces each — 12 wordings,
the same vids, the same order, the same angle tags, the same slot sets, the same sentence counts.

---

## THE ROWS, ready to paste under the pool's bold heading

1. `[ledger]` The capacity {settlement} holds against a crisis measures adequate. That capacity is carried with little margin.
   - `[face]` Against a crisis {settlement} is covered. The cover is thin at its edge.
   - `[face]` Economic survival at {settlement} stands adequate. That standing sits on a narrow margin.
   - `[face]` Adequate is the word entered for {settlement} against a crisis. Under that word the margin is not wide.
2. `[unfolding]` The town's capacity to meet a crisis is real, and the margin left standing on it is little.
   - `[face]` Set against a crisis the town's standing is adequate, and the little margin is what that standing leaves open.
   - `[face]` This town holds through an emergency on a little margin, and that margin stands open.
   - `[face]` What covers the town against a crisis holds, and the little margin on that cover is the matter left standing.
3. `[threshold]` Cover against a crisis is adequate at {settlement}, and the margin under it would be little.
   - `[face]` A crisis would be met at {settlement}, and met on a little margin.
   - `[face]` The covering at {settlement} stands, and in a crisis its edge would be close.
   - `[face]` The town of {settlement} is covered against a crisis, and the cover would hold by a narrow margin.

---

## THE PINS THIS REFINEMENT DID NOT MOVE (checked mechanically, face by face)

- **Vids, order, tags:** 1 `[ledger]` · 2 `[unfolding]` · 3 `[threshold]`, one bracketed tag each, no
  `[plain]` anywhere, no variant added, removed, merged or reordered (§22 "never trim").
- **Slot sets, exactly the parents':** vid 1 `{settlement}` ×4 · vid 2 **no slot** ×4 · vid 3
  `{settlement}` ×4 (ARCH §2.5: a face whose `{slot}` set differs from its parent's is refused).
  `{band}` and `{route}` are named by nobody.
- **Sentence counts, exactly the parents' (A11):** vid 1 TWO ×4 · vid 2 ONE ×4 · vid 3 ONE ×4.
- **Joints:** one `, and` per single-sentence face; no semicolon anywhere; the shipped vid 3's two
  joints in one sentence stay cured (R-DA-06's ration).
- **Openers:** the settlement token opens NO face (T-F8; wall 10). The three numbered lines' first two
  words are `The capacity` · `The town's` · `Cover against` — three distinct pairs (A11).
- **Three distinct level-1 grammars kept** (the cure of the census's `grammars 1`): vid 1 **V1 PRESENT
  with the qualification in its own sentence**; vid 2 **V6 PRESENT → OPEN** (R-iv / W8); vid 3
  **PRESENT + EDGE, the edge subjunctive** (wall 2). No variant moved onto a sibling's grammar (W4).
- **Both halves of the reading in all twelve faces** (skeleton §8's one fence this pool's siblings do
  not carry): the COVER *and* the LITTLE MARGIN. Verified face by face below.
- **Claim-equal across the four faces of each variant (arm A6):** each face carries exactly
  {the cover against the pressure class · the margin little · `{settlement}` where the parent has it}.
- **Word range 13–20**, the short line at 13 (two of them), the long at 20 (R-DA-05: the pool is the
  unit of spread; the spread is carried by construction, not by a metronome).
- **Hard walls, re-scanned mechanically over the twelve faces:** no em dash · no exclamation · no
  question · no digit · no percent · no which-clause · no "I"/"you" · no body · no component · no
  stock word · no pay or coin word · no duration, season or count · no particular crisis · no trend,
  onset or movement · no forecast or outcome past the edge · no cause · no comparison to a norm or
  another rung · no person, belief or totality · no figure · no citation and no record noun.

---

## --- NOTES

One line per face: what changed, and the law or refinement target behind it
(§21.1 the ceiling · §21.2 lawful is not done · §21.4 never plainer with no law behind the change).

### VARIANT 1 · `[ledger]` — the office enters and measures, and cites nothing (W8)

- **numbered line** — second sentence moved from the flat `The margin on that capacity is little.` to
  `That capacity is carried with little margin.`: the ladder's own authored phrase ("Covered, but with
  little margin") is now carried verbatim in the close, and the predicate is the office's own formula
  (*carried standing*, R-vi / W7's lawful ledger surface), so the ledger's second sentence is an entry
  rather than a restatement. Density law §21.4 (the lawful turn is a floor; the ladder's clause was
  written to be kept, not paraphrased) + W6 (the second sentence states the other half, never the
  first again). Thread kept: capacity → capacity (R-i at k = 0). First sentence untouched — it is the
  pool's sharpest compression already (the arm's own noun, the ledger's own verb, the band's own word).
- **face 2** — `The covering is thin at its edge.` → `The cover is thin at its edge.`: this is now the
  skeleton's licensed exemplar clause (§0.7 fence 3) word for word, the `-ing` nominal is gone, and
  `the covering` is freed for vid 3 face 3 so the pool's nouns disperse (R-DA-10: vary the noun across
  the pool). The fronted `Against a crisis` is KEPT deliberately as the pool's ONE fronted-pressure
  opener — the other three in the draft were removed instead (the zero-tics target), so the habit is
  gone and the best instance stands. Shortest face in the pool at 13 words (R-DA-05's short line).
- **face 3** — rebuilt from `Through a crisis the town of {settlement} holds. The margin the town holds
  by is narrow.` to `Economic survival at {settlement} stands adequate. That standing sits on a narrow
  margin.`: three faults cured at once — the second fronted-pressure opener (tic), the `holds … holds
  by` verb doubling inside one face (R-DA-22's one term for one thing read as rhythm; the draft's own
  worst repetition), and a subject that duplicated the numbered line's arm-noun. The new subject is the
  ARM AT ITS LABEL, licensed by name at skeleton §0.7 fence 1 and rendered at its ENGINE meaning (W14),
  which gives the variant a fourth genuinely different subject rather than a fourth synonym (arm A5;
  ADDENDUM 7 rule 4; W4 reads on the variant's grammar, which is unchanged). `sits on` is the
  skeleton's own register for a band's position ("the reading sits near the bottom of its band") and is
  a posture verb, not a sense verb or an intent (R-DA-11 untouched). Thread: stands → standing.
- **face 4** — `The margin that word leaves is not a wide one.` → `Under that word the margin is not
  wide.`: two law-backed changes. (a) `that word leaves` gave an inanimate thing an act — the closest
  thing in the draft to R-DA-11's "an intent for an inanimate thing" — and is gone. (b) the close moved
  off the pro-form `one` onto the adjective, so the face no longer closes on a pronoun (R-DA-04's
  pronoun-closer ceiling). The one negated surface per variant is kept, and is still not the opener
  (W10). `Adequate is the word entered for {settlement} against a crisis.` is kept as drafted: *entered*
  is named in W7 as the ledger's lawful surface, it names no holder and no record, so A13 and W24 stand
  untouched, and it is the only face in the pool that shows the ledger as a ledger.

### VARIANT 2 · `[unfolding]` — the OPEN move after the presence, the margin named last and left (R-iv, W8)

- **numbered line** — `The town's capacity against a crisis is real` → `The town's capacity to meet a
  crisis is real`: the shipped sentence's own infinitival frame (`The town's capacity to …`) is
  restored with the unlicensed verb replaced, so more of the shipped lawful SHAPE survives than the
  draft kept — the density floor read against the shipped line (§21.4; skeleton §2(6), which names
  `The town's capacity` the pool's one verbatim-lawful noun phrase). `meet` is the spelling skeleton
  fence 1 recommends as the one with no condition-word neighbour. `is real` is kept verbatim: the
  shipped copula is lawful under the card, and this index-0 line is where a lawful shipped clause
  belongs. OPEN idiom unchanged (`left standing`).
- **face 2** — `… and what that standing leaves open is a little margin.` → `… and the little margin is
  what that standing leaves open.`: the OPEN now LANDS LAST, which is what R-iv actually requires (the
  unfolding realises OPEN *after* the presence), and the ladder's phrase `little margin` sits intact in
  the subject. Same length, same claim, same construction contract (the pressure fronted as an adjunct,
  the standing as subject — W4); only the information order inside the OPEN clause moved. The
  standing → standing echo is kept as the deliberate thread §1.4.1 licenses.
- **face 3** — rebuilt from `Here a crisis is met on a little margin, and there the margin is left.` to
  `This town holds through an emergency on a little margin, and that margin stands open.`: the draft's
  `Here … and there …` was a deictic balance that pointed at nothing the record holds and left `the
  margin is left` saying almost nothing; it was the weakest line in the set. The rebuild (a) seats the
  town so the sentence is this town's band and not a maxim (W5), (b) takes `an emergency` — the
  engine's own second word for the pressure class (`threatAssessment.js:167`; skeleton §0.2's lawful
  nouns) — which widens the sibling distance from vid 3 face 2's `A crisis would be met at
  {settlement}` without spending a claim, and (c) gives the OPEN its own idiom, `stands open`, distinct
  from the other three faces' (`left standing` · `leaves open` · `the matter left standing`). Still the
  variant's short line.
- **face 4** — `… the little margin on it is where the matter stands.` → `… the little margin on that
  cover is the matter left standing.`: `on it` became a noun carried forward (R-i; R-DA-04), and the
  locative `where the matter stands` became move 10's own vocabulary — *a civic matter left standing
  open and named* (MOVE-GRAMMAR §1.2 row 10, declarative always) — which states the OPEN instead of
  gesturing at it. Construction unchanged (the free relative as subject).

### VARIANT 3 · `[threshold]` — the state, then the edge, subjunctive wherever it reaches (wall 2)

- **numbered line** — rebuilt from `The standing {settlement} carries against a crisis is adequate, and
  the margin the town would hold by is little.` to `Cover against a crisis is adequate at {settlement},
  and the margin under it would be little.`: the draft's frame was `The <noun> {settlement} <verb>s
  against a crisis …`, which mirrored vid 1's numbered line almost beat for beat — a pool-level tic
  the refinement target names explicitly, and the only one A11's first-two-words rule does not catch.
  The bare-noun subject `Cover` breaks the mirror, moves the slot off the front entirely, and `under
  it` picks up the skeleton's own licensed spelling of the margin's nearness ("the cover has its floor
  close under it", §0.7 fence 4). The edge stays subjunctive (`would be`, wall 2) and is neither
  located, counted, nor crossed.
- **face 2** — kept as drafted, `A crisis would be met at {settlement}, and met on a little margin.`:
  under §21.4 this face is already at the ceiling — 13 words, subjunctive throughout, coordinate
  ellipsis carrying the second half on one repeated participle, both halves of the reading, the close
  on the ladder's own noun. The refinement that would have "improved" it would have made it plainer
  with no law behind the change, which is the regression the density law names. The sibling distance it
  needed was bought by rewriting vid 2 face 3 instead.
- **face 3** — kept as drafted, `The covering at {settlement} stands, and in a crisis its edge would be
  close.`: it is the pool's only face carrying the EDGE as an explicit noun, which is the `[threshold]`
  angle's own business, the mood is already subjunctive (wall 2), `its` binds correctly to the covering
  (W1), and the edge is stated as near without being placed or counted. `The covering` is the noun
  freed by vid 1 face 2's change, so the pool now spends four distinct arm-nouns (capacity · cover ·
  standing · covering) rather than three.
- **face 4** — rebuilt from `In a crisis {settlement} is covered, and the margin of the cover would be
  little.` to `The town of {settlement} is covered against a crisis, and the cover would hold by a
  narrow margin.`: (a) the third fronted-pressure opener in the pool is gone (the zero-tics target) and
  the naming form `the town of {settlement}` (W5) takes its place; (b) `the margin … would be little`
  repeated the numbered line's own close word for word — the close now lands on the OBJECT (`a narrow
  margin`) rather than on the same condition, varying the close KIND across the variant (R-DA-04);
  (c) `hold by a narrow margin` is a measurement in words, which the register permits where a figure
  would not (R-DA-11), and keeps the reach subjunctive.

### THE JUDGMENT CALLS THIS SEAT MADE, RECORDED VETOABLY (each is the chair's to strike)

1. **`Economic survival` as a face's subject (vid 1 face 3).** Licensed by name at skeleton §0.7 fence 1
   ("economic survival (the label, as a name)") and rendered at its ENGINE meaning per W14. Recorded
   because W14 also bars "survival" read as an OUTCOME: this face uses the label as the ROW'S NAME and
   predicates only the band of it. If the chair reads the label as importable only inside a longer
   frame, the face reverts to `What {settlement} can meet in a crisis stands adequate.` with the same
   second sentence.
2. **`the word entered for {settlement}` (vid 1 face 4), carried over from the draft unchanged.**
   *Entered* is named in W7 / R-vi as the office's own lawful surface; the face names no holder and no
   record noun, so A13 and W24 are untouched. Re-recorded rather than re-decided, because a refuter may
   read "the word entered" as implying a register.
3. **The qualification riding as a coordinate clause in vids 2 and 3.** R-DA-03 wants the qualification
   in its own sentence; A11 pins vids 2 and 3 to ONE sentence each. This seat reads the two halves as
   ONE read — the ladder's single authored reading — and therefore as one claim carrying its own
   qualification, not a second fact (the card's may-NOT is untouched; S2's consequence seat is not
   used and is not available on a spine with no relation row). Carried over from the draft's call 3.
4. **Three of the draft's four fronted-pressure openers were removed and one kept.** A fronted adjunct
   is lawful; four of them across twelve faces is the fronted-place habit the refinement target names.
   The chair should know this changed three faces' openings by tic-count rather than by any wall.
5. **Vid 3's numbered line was rebuilt on a different frame from the draft's.** The draft's frame
   mirrored vid 1's numbered line; A11's first-two-words rule does not catch a mirrored frame with
   different head nouns, so the cure is a refiner's judgment, recorded here for the gate to check
   rather than taken as discretion.
6. **Two faces were kept verbatim from the draft (vid 3 faces 2 and 3).** §21.2 says a lawful face just
   above the floor is pushed toward the ceiling; this seat judged both already at it, and §21.4 forbids
   trading their density for plainness. Recorded so that "unchanged" is read as a decision with a
   reason and not as a face the refinement skipped.
7. **No new failing state was introduced and none was added to (§21.2's keep-or-revert test).** The set
   was lawful at the draft and is lawful here; the changes are tic removal, close-kind variation,
   pronoun-closer removal, one inanimate-act removal, one OPEN move re-landed to R-iv's order, one
   shipped frame restored under the density floor, and one noun freed so the pool spends four arm-nouns
   instead of three. Nothing is banked (§21.2's banked count is unchanged by this round).
