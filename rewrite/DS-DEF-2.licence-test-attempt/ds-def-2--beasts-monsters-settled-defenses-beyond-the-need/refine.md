# REFINEMENT ROUND (arm: Opus refiner); DS-DEF-2 · pool `Beasts & Monsters: settled, defenses beyond the need`

Refiner: Opus 5 (Fable-unvalidated), a DIFFERENT author than the drafter, under the Fable chair. REWRITE car 8b, block DS-DEF-2, phase 2 (Part B §21 phase 2; §21.1 the ceiling not the middle; §21.2 every set is refined; §21.3 one effort, the licensed wording wins; §21.4 the density law).
Written under the checkpoint law: sections land as they are finished.

PROGRESS: sources read whole (REGISTER-CARD with S2/S3; Part B §1 R-DA-00 to R-DA-24, §16–16.2, §18, §20, §21–21.4, §22, §23, §24; MOVE-GRAMMAR §1–3, §4.4.1–4.4.3, §1.4.1; CLERK-LAWS §2.4.1, §2.6.1; ARCH v2 §2.5, §8.3; ADDENDUM 13 A and B as the brief carries them; the licence card re-printed in laneRW-DEF2; the skeleton whole; the draft whole; the annex block `### DS-DEF-2` for the siblings). Faces refined: 12 of 12, of which four were re-cut at a second sitting after the window cut the first (the last section says which and why). VERIFIED and COMPLETE.

The three rows below are the COMPLETE REPLACEMENT for the pool's variant rows. Vids, order, angle tags and the slot set `{settlement}` are unchanged; no variant or face is added, removed or merged. The typed lines of the pool are untouched and are not repeated here.

---

1. `[counterforce]` There is very little of beast or monster in the country around {settlement}, and the works stand.
   - `[face]` The country around {settlement} is a quiet one in the way of beasts, and what the town has built stands.
   - `[face]` Works stand at {settlement}, and the country around the town is little troubled by creatures.
   - `[face]` Monsters come to little in the country around {settlement}; the town's works stand.

2. `[ledger]` {settlement} is entered as provided against beasts, and the country around the town as quiet.
   - `[face]` The works at {settlement} are carried as standing, and the creatures of the country around the town come to little.
   - `[face]` Little of beast or monster troubles the country around {settlement}, and what the town has built there is set down as standing.
   - `[face]` Provision against beasts is entered at {settlement}, and the country around the town is a quiet one.

3. `[visitor]` A stranger notices the perimeter at {settlement} first, and then how quiet the country around the town is of beasts.
   - `[face]` What a stranger sees at {settlement} is the works standing, and few creatures in the country around the town.
   - `[face]` To a stranger the country around {settlement} is quiet in the way of creatures, and the works stand at the town.
   - `[face]` A stranger comes on works standing at {settlement}. Monsters are little seen in the country around the town.

--- NOTES

## Per face: what changed, and the law or target behind it

**V1 line** (`[counterforce]`, was *There is very little in the country around {settlement}, and the works stand.*) — inserted `of beast or monster` into the shipped clause: the shipped sentence named its referent only inside the clause the rewrite drops (*it is not the creatures*), so the draft's line left R1 as a bare "very little" that a reader may take for a peace and the war row on the same page contradicts (C7; entailment item 4's band, never a peace). The shipped words stand whole inside the longer clause, so the density floor (Part B §21.4; skeleton 1.6) is kept and the dropped clause's weight is repaid in licensed specificity (the row's own noun), not in nothing. Target: the sharper licensed fact.

**V1 F1** (was the same) — kept verbatim. It is the pool's best single line: `a quiet one in the way of beasts` states the band AND the referent in one compression, `what the town has built stands` is D-6's always-safe periphrasis, and the fall on *stands* lands. §21.4 forbids trading that density for plainness with no law behind the change; the refinement round keeps a face at the ceiling rather than moving it (§21.2). The pool's two uses of `a quiet one` and its two of `in the way of` are rationed to this face and to V2 F3 / V3 F2 (R-DA-10's pet-word ceiling).

**V1 F2** (was *Works stand at {settlement}, and the country around the town carries little in the way of creatures.*) — swapped the tail to the passive `is little troubled by creatures`. The draft spent `in the way of` three times across the pool (V1 F1, V1 F2, V3 F3), a pet-word rate R-DA-10 rations at 0.200; and the draft's four V1 faces all ran the same clause-comma-and-clause active shape. The passive predicate gives the variant a fourth CONSTRUCTION, not a fourth vocabulary (ADDENDUM 7 rule 4; the brief's four-faces-differ-in-construction rule). Claim unchanged: the same low band over the same country (W2).

**V1 F3** (was *Creatures come to little in the country around {settlement}; the town's works stand.*) — referent noun moved to `Monsters`, so the four faces of V1 spell the row four genuinely different ways (beast or monster · beasts · creatures · monsters) with no claim moved: `Beasts & Monsters` is the STATE-KEY row's own name and the `settled` family is the low tier of both. The change is driven by SIBLING DISTANCE, the refinement round's named target: the annex's `plagued, perimeter but NO force` F3 at `:2613` reads *Beasts are abroad in the country around {settlement}. The town's works stand …*, so a face opening `Beasts … in the country around {settlement}` and closing `the town's works stand` was a twin shell of the adjacent branch differing only in the polarity word. The opener now parts them; the shared tail is the pool's irreducible licensed core (both possessive spellings of the works, `the town's works` and `the works of the town`, are already spent by that same sibling pool at `:2612`, `:2613` and `:2621`, so there is no closing cure, and the fact is stated not hidden). The semicolon and the thirteen-word tightness are kept as the pool's one rationed joint (R-DA-06) and its shortest single-sentence line.

**V2 line** (`[ledger]`, was the same) — kept verbatim. `entered as provided against beasts` is the shipped compression the skeleton names this variant's density floor (2.6), and the ellipsis of *is entered* in the second half is the strongest clerk's move in the pool: the office's formula governs two facts and is spoken once. It is also the pool's one settlement-token opener (order constraint 10) and its one apposition joint. Carried risk, stated not hidden: a refuter may read *provided against* as a purpose, which the card refuses on V1; the skeleton ruled it a standing configuration under the row's own key, and if the chair rules otherwise the cure is `entered as walled and the country around the town as quiet` in the line and F3 alike.

**V2 F1** (was *The works at {settlement} are carried as standing, and the country around the town is entered as little troubled by creatures.*) — dropped the second office formula from the tail and re-subjected it as `the creatures of the country around the town come to little`. The draft put a passive formula on BOTH halves of this face, which is the doubled beat W6 names and which left the four V2 faces indistinguishable in rhythm; one formula per face lets the ledger's surface recur as a WORD without the FACT recurring (register card: the word may recur, the fact must not). The variant now spends `carried as standing`, `set down as standing` and `entered` once each, twice for `entered` in two different constructions.

**V2 F2** (was *Little troubles the country around {settlement}, and what the town has built is carried as standing.*) — three changes. `there` carries the country forward into the second clause, which is THE THREAD's carry (§1.4.1; R-i) rather than a bare subject change that hands nothing back; the formula moved to `set down as standing`, ending the `carried as standing` collision with F1; and the subject is now `Little of beast or monster`, because the bare `Little` named no referent. That was the same peace-reading fault the round cured on V1's line and V3's line and it survived one pass here: DS-DEF-2 carries the war row and three more pressures on the same page (C7), so an unqualified "little troubles the country" claims a quiet the card does not license and a sibling row may contradict. The band is now stated of the row's own referent and of the COUNTRY (W2), and the variant keeps its one negated-magnitude opener (W10, one per row) as its fourth construction.

**V2 F3** (was *Provision against beasts is entered at {settlement}, and the country around the town is set down as a quiet one.*) — cut `set down as` from the tail, leaving the plain copula `is a quiet one`. The draft had every V2 face carrying a formula in both halves; stripping this one gives the variant a face whose second read is stated flat, which is the shape the ledger's own second fact takes when the office has already entered the first. The nominalised subject `Provision against beasts` is kept as the variant's fourth construction and as the shipped compression re-spent (§21.4).

**V3 line** (`[visitor]`, was *A stranger notices the perimeter at {settlement} first, and then how little the country around the town holds.*) — tail re-cut to `how quiet the country around the town is of beasts`. The draft's tail named no referent (the same peace-reading fault as V1's line) and leaned on `holds`, which the skeleton bars near the works for importing resistance. The degree frame `how quiet … is of beasts` states the band as a degree, so it cannot be read as a zero (entailment item 4), and it lands the sentence on the row's own noun instead of on a verb. The shipped clause `A stranger notices the perimeter at {settlement}` stands verbatim as the variant's density floor (3.6), with its one carried caveat: `the perimeter` sits on the referent table's HOLDS and on D-6's geometric warning at once; all three faces spend the safe generic, and the one-word cure is `the works`.

**V3 F1** (was *The works at {settlement} are what a stranger sees first, and little shows in the country around the town.*) — replaced with the pseudo-cleft carrying both reads in one complement, `What a stranger sees at {settlement} is the works standing, and few creatures in the country around the town`. The draft's F1 and F3 were both clefts on what the stranger sees, the pool's one true paraphrase pair; this face takes the better of the two constructions and the freed slot goes to F3's two-sentence shape. The verbless second complement is a real parallel, and the referent is named where the draft left `little shows` bare. The second complement reads `few creatures`, not `little of beast or monster`: with V2 F2 now carrying that pairing, a third use would put the phrase at 0.250 of the pool against R-DA-10's 0.200 pet-word ceiling, and `few` is the band's own vague magnitude and not a count (the card refuses a count, never a quantifier).

**V3 F2** (was *To a stranger the country around {settlement} is a quiet one, and the works stand at the town.*) — `a quiet one` → `quiet in the way of creatures`. The draft spent `a quiet one` three times across the pool; two is the ration, and this face is the one that also owed its referent. The `To a stranger` adjunct with the country as subject is kept as the variant's one R1-first construction.

**V3 F3** (was *What a stranger sees at {settlement} is the works standing, and the country around the town carrying little in the way of beasts.*) — rewritten as two short sentences, `A stranger comes on works standing at {settlement}. Monsters are little seen in the country around the town.` This is the pool's only two-sentence face and its only pair of sentences under eleven words, which the register card asks for ("run long or short as the load demands; the short line exists") and R-DA-06's short-sentence floor wants (0.017 → ≥ 0.030). THE THREAD at k = 0 is satisfied by carrying `{settlement}` forward as `the town`, with the shift of subject the passage's one turn outward and placed last (R-i; §1.4.1). The cleft it replaced now lives once, at F1. The second sentence read `Few creatures show` after the first pass: SIBLING DISTANCE again — the adjacent `settled, nothing organized` pool's own third face at `:2691` opens *Few creatures meet a stranger in the country about {settlement}.*, the same two opening words in the same variant slot of the same angle family, on the branch that differs from this one only by the works. `little seen` is the skeleton's own prescription for this variant (3.4: R1 in the visitor's stance is what is SEEN of the country on the way in) and is R-DA-14's "seen, not meant" said of the country rather than of a person. `scarce` was considered for this slot and REFUSED: it is a canonical engine token on the supply desk (`impaired` → `scarce`, annex `:1286`, `:1288`), and R-DA-22 keeps one term for one thing.

## What binds across the twelve, re-checked after the refinement

- **The claim set, unchanged from the draft and identical across all twelve** (A6 reads across faces): (R1) the country around the town is the LOW beast tier, stated over the COUNTRY (W2), never as a zero and never as a peace; (R2) works stand at the town, by the class word. Joined as ONE keyed condition by co-ordination, apposition or a rationed semicolon; never a causal joint, never a purpose frame, never a verdict on the surplus (ADDENDUM 8 (1) and (2)). Nothing added, nothing dropped.
- **The licensed silence held.** The branch does not consult `force` (`defenseStateProse.js:423-425`): no face names a force, muster, garrison, militia, watch, soldier or person, in either direction (W20, W12, W13, W22).
- **Provenance: zero citations, zero record nouns.** The card licenses `muster · standing`, but none of S3's three reasons obtains and the exemplar rate is zero (Part B §24; W24). `entered` · `carried as standing` · `set down as standing` are the office's own formula (R-vi / W7), never a source.
- **The walls, re-walked one by one.** No em dash; no exclamation; no digit or percent; no `which`-clause; no question; no second person; no material or material source (W11); no size, condition, extent, geometry, orientation or count; no manning; no season, year or span; no cause, purpose or reason clause; no verdict word (beyond · over · more than needed · wasted · comfortably); no forecast; no gate word (upkeep · keeping · wages · pay · purse · spending); no badge word; no summarising second sentence. Eleven rows are one sentence, one row is two (A1).
- **The tics the refinement removed.** `in the way of` 3 → 2; `a quiet one` 3 → 2; `carried as standing` twice inside V2 → once; the clause-comma-and-clause active shape on all four V1 faces → three shapes plus a semicolon; the two near-identical V3 clefts → one cleft plus one two-sentence face; the doubled office formula on every V2 face → one formula per face; `beasts`/`Beasts` counted as two spellings inside V1 → four genuinely different referent words. No fronted-place, no inversion and no ledger-closer habit is repeated anywhere in the pool.
- **The rations, counted on the final twelve.** Every distinctive phrase sits at 2 of 12 (0.167) or below, inside R-DA-10's 0.200 pet-word ceiling: `come to little` 2 (V1 F3, V2 F1) · `in the way of` 2 (V1 F1, V3 F2) · `a quiet one` 2 (V1 F1, V2 F3) · `beast or monster` 2 (V1 line, V2 F2) · `what the town has built` 2 (V1 F1, V2 F2) · `monsters` 2 (V1 F3, V3 F3) · `few` 1 · the office's three formula surfaces 1 each. The safe words the skeleton names for the two reads (`quiet` 5, `little` 6, `works` 7) are not rationed phrases; they are the licensed vocabulary of a pool that must state both reads in every one of twelve faces, and the register card licenses the recurring WORD where the FACT does not recur.
- **The walls and the slot set, checked mechanically on the final rows** (a read-only scan over the packet, no file written): twelve rows parse to the projector's `FACE_ROW_RE` shape, three numbered variants with one bracketed angle tag each and three `[face]` sub-rows each; zero em dashes, zero exclamation marks, zero question marks, zero digits, zero `which`, zero second person; every one of the twelve carries `{settlement}` and no other slot, so no face's slot set differs from its parent's.
- **The openers.** The three numbered lines open `There is`, `{settlement} is`, `A stranger` (A11). Exactly one settlement-token opener, on variant 2 (order constraint 10); exactly one existential opener, on variant 1 (R-DA-07's band); no face sub-row opens on the proper slot (T-F8); every face carries `{settlement}`, so no face's slot set differs from its parent's (ARCH §2.5).
- **The one shared opener, declared.** V3's numbered line and V3 F3 both begin `A stranger`; every other pair of the twelve differs in its first two words. A11's first-two-words rule binds the VARIANTS (the three numbered lines: `There is` · `{settlement} is` · `A stranger`, all distinct), and the two V3 rows are not paraphrases under the brief's test: one is a twenty-word co-ordination whose second limb is a degree clause, the other is two sentences of eight and ten words. The visitor's eye is the angle's own subject and cannot be withheld from most of its faces without losing the stance.
- **The siblings, MEASURED and not merely declared.** The first pass declared the sibling check and missed two of them, so the final twelve were scanned against every other variant and face row of the annex by trigram overlap. No face uses the `frontier, credible deterrence` sibling's *reaches … out of the wild country*, nor the `settled, nothing organized` sibling's *the country about*, *runs low in beasts*, *meets a stranger*, *passes no works*, *Coming on*, *Few creatures meet* or *Against that country*; every face states the works, so no face can be read as the no-works sibling. Two twins the scan found are cured above (V1 F3's opener against `:2613`; V3 F3's second sentence against `:2691`). What the scan leaves standing, stated not hidden: V1 F3 still shares `in the country around {settlement}` and `the town's works stand` with `:2613`, and V2 F1 shares the office's formula head with `:2597`, `:2599`, `:2694` and `:2808`. Both residues are the licensed core of the block — the two reads have one safe vocabulary, and the ledger's formula is licensed to recur as a WORD — and every alternative spelling of the works close is already spent by that same sibling pool at `:2612`, `:2613` and `:2621`. The FACTS differ in every case, which is what C5 and the sibling arm ask.
- **THE THREAD.** This pool is a spine and sits first on the defense tab, so every row hands forward a noun the modifiers can pick up (the works · the country · the beasts) and closes on a standing fact, never on a hook, a purpose, a question or the stranger. V2 F2 carries the country forward with `there`; V3 F3's second sentence carries `{settlement}` forward as `the town` and makes its change of subject the passage's one turn outward, placed last (R-i; §1.4.1).
- **The reads' order and the landing noun, across the pool.** Five of the twelve state the country first and seven the works first; each variant carries at least one face in the minority order (V1 F2, V2 F2, V3 F2), and inside every variant the four subjects differ (V1: the expletive · the country · the works · the monsters; V2: the settlement token · the works · the magnitude · the provision; V3: the stranger · the cleft · the country under a `To a stranger` adjunct · the stranger then the monsters). Four faces close on the works standing and eight on the country or its creatures. This is the brief's construction test (the subject, the order of the reads, the landing noun) met per variant, not vocabulary alone.

## REFUSALS

**None.** Twelve faces refined one for one; no face or variant added, dropped or merged; no claim added; the vids, the order, the angle tags and the slot set are the draft's. Two items are CARRIED rather than refused and are named above with their one-word cures: `the perimeter` in V3's numbered line (referent table HOLDS beside entailment D-6), and `provided against beasts` in V2's line and F3 (the shipped compression the skeleton ruled a standing configuration, which a refuter may read as a purpose). One word was considered and REFUSED at V3 F3: `scarce`, a canonical engine token on the supply desk (R-DA-22, one term for one thing).

## THE SECOND SITTING OF THIS PACKET (the checkpoint law's continuation)

The five-hour window ended inside the first refinement pass, which left a complete packet on disk. This sitting did not restart it: the sources, the licence card, the skeleton, the draft and the annex were re-read, the packet was read as the earlier attempt left it, and the twelve rows were VERIFIED rather than trusted — mechanically for the walls, the slot set and the projector's row shape, and by a trigram scan against every other row of the annex for sibling distance. Ten of the twelve rows and all of the reasoning survived unchanged. Four rows moved, each for a named law or target and none for taste: V1 F3 and V3 F3 for SIBLING DISTANCE against `:2613` and `:2691` (the first pass declared a sibling check it had not measured), V2 F2 for the C7 referent cure it had applied everywhere else and missed here, and V3 F1 to hold `beast or monster` inside R-DA-10's ration once V2 F2 spent it. No claim moved in any of the four; each face is claim-equal to its siblings under A6 as before.

PROGRESS FINAL: 12 of 12 faces refined; four rows re-cut at the second sitting and their notes rewritten; the pool re-verified whole; packet complete.
