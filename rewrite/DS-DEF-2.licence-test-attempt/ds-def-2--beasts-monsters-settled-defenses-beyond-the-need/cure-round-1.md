1. `[counterforce]` There is very little of beast or monster in the country around {settlement}, and the works stand.
   - `[face]` The country around {settlement} is a quiet one in the way of beasts, and what the town has built stands.
   - `[face]` Works stand at {settlement}, and the country around the town is little troubled by creatures.
   - `[face]` Monsters come to little in the country around {settlement}; the town's works stand.
2. `[ledger]` {settlement} is entered as provided against beasts, and the country around the town as quiet of them.
   - `[face]` The works at {settlement} are carried as standing, and the creatures of the country around the town come to little.
   - `[face]` Little of beast or monster troubles the country around {settlement}, and what the town has built there is set down as standing.
   - `[face]` Beasts amount to little in the country around {settlement}, and provision against them is entered at the town.
3. `[visitor]` A stranger notices the works at {settlement} first, and then how quiet the country around the town is of beasts.
   - `[face]` What a stranger sees at {settlement} is the works standing, and few creatures in the country around the town.
   - `[face]` To a stranger the country around {settlement} is quiet in the way of creatures, and the works stand at the town.
   - `[face]` A stranger comes on works standing at {settlement}. Monsters are little seen in the country around the town.
--- NOTES
PROGRESS FINAL: 3 of 3 targets cured; 0 refused. Every other face byte-identical to the annex at HEAD `471ce894a83658b0e0fe480819a9f1dcd22605e3` (verified by diff against the annex rows). Three variants, their own numbers, their own single angle tags, four faces each; no variant added, removed or merged; nothing trimmed. COMPLETE.

- **variant 2 · face 0 · CURED.** `and the country around the town as quiet` -> `and the country around the town as quiet of them`. The refuter's own cure, taken verbatim. `them` binds to `beasts`, the clause's only plural and the only plural in the line, so the predicate reaching the country through the ellipsis of `is entered` is now restricted to the row's own referent. LAW behind each claim: R1 `family === 'settled'` is the LOW beast-and-raider tier stated over the COUNTRY (ENTAILMENT item 4 as ratified; W2; C7's same-page war row is no longer contradicted because the quiet is bounded to beasts) - the unrestricted `quiet` was the card's may-NOT (a second fact) and is DROPPED, not replaced. R2 `perimeter === true` is carried by `provided against beasts`, the variant's density floor (skeleton 2.6; Part B 21.4) and it survives the cure untouched, as do the ellipsis of `is entered` (the ledger's office formula, R-vi / W7, no record noun and no citation, 0b.2) and the pool's ONE settlement-token opener (order constraint 10). No claim is added: the cure only narrows a predicate. The pairing `beast or monster` was NOT used, per the refuter's express instruction: it already stands twice in the pool (v1 f0, v2 f2) and a third use puts it at 0.250 against R-DA-10's 0.200 ration.

- **variant 2 · face 3 · CURED.** `Provision against beasts is entered at {settlement}, and the country around the town is a quiet one.` -> `Beasts amount to little in the country around {settlement}, and provision against them is entered at the town.` The refuter's cure SHAPE taken whole, with one licensed substitution in the first clause, stated below. Both halves of the finding are met. (a) The A5 / four-faces ground against face 0: the read ORDER reverses (R1 then R2, where face 0 runs R2 then R1), the SUBJECT moves (`Beasts` for `{settlement}`), the LANDING NOUN moves (`the town` for `quiet of them`), and the second clause changes construction from an ellipsis of the office formula to a finite nominal-subject clause; the verb `is entered` recurs because it is the ledger's own formula, which the register card licenses to recur as a WORD while the FACT does not (R-vi / W7). (b) The unreferented `is a quiet one` is gone: the tier is now predicated of `Beasts` directly and country-scoped (`in the country around {settlement}`), so W2 and ENTAILMENT item 4 are both met and C7's same-page rows are not contradicted. The variant's DENSITY FLOOR, the nominal compression `provision against` the row's own referent (skeleton 2.6; Part B 21.4), is kept whole as `provision against them`, with `them` bound to `Beasts`, the clause's only plural (W1 is not engaged: no possessive follows an object). No claim is added; `amount to little` is the LOW band's vague magnitude, not a count and not a verdict, exactly as the refuter passed `come to little` at v1 f3 and v2 f1 and `few` at v3 f1.
  - **The one deviation from the cure as quoted, with its law.** The refuter's model wording was `Beasts come to little in the country around {settlement}, and provision against them is entered at the town.` `come to little` already stands TWICE in this pool (v1 f3 `Monsters come to little`; v2 f1 `the creatures of the country around the town come to little`), so a third use is 3 of 12 faces = 0.250 against R-DA-10's 0.200 ration - the identical arithmetic by which the same refuter, one target earlier in this same packet, forbade curing v2 face 0 with `beast or monster`. Under Part B 21.3 (where one effort must choose between a sharper wording and a licensed one, the licensed one wins) the phrase is replaced by `amount to little`, which no face of the pool spends, is the same idiom class rather than a plainer paraphrase (so 21.4's density floor is not traded down), and is a magnitude in words, never a figure (R-DA-11) and never a rating (R-DA-12). Every other element of the named cure - the reversed order, the subject, the landing noun, the referent named by `them`, the kept compression - is taken as written.
  - **Distance from face 2, re-tested after the reversal.** Face 2 also runs R1 then R2. Subject moves (`Little of beast or monster` / `Beasts`), first predicate moves (an active `troubles` / a copular magnitude), second-clause subject moves (the free relative `what the town has built` / the nominal `provision against them`), landing noun moves (`standing` / `the town`). The variant's four faces now split two-to-two on read order instead of three-to-one, which widens the variant's spread (R-DA-05).

- **variant 3 · face 0 · CURED.** `A stranger notices the perimeter at {settlement} first` -> `A stranger notices the works at {settlement} first`. The refuter's own cure, taken verbatim: one word, and nothing else in the line moves. LAW behind each claim: R2 `perimeter === true` reaches the `walls` bucket, whose members include `Citadel` (inner) and a lone `Gates (if walled)` row (a point), of neither of which a perimeter is true - so `perimeter` was the branch argument's NAME rendered as English geometry (W14, the label bar; ENTAILMENT item 1, a recorded noun licenses what the word MEANS; D-6 and A-7), and `the works` is the always-safe generic both tables name (W15, the alias bar). The word is on this block's own label-trap list. R1 `family === 'settled'` stands untouched in the second limb, country-scoped and bounded to the row's referent (`how quiet the country around the town is of beasts`), so W2 and ENTAILMENT item 4 hold. No claim is added and none is dropped but the false geometry. The variant's DENSITY FLOOR - the seeing and the seen in one clause (skeleton 3.6) - is kept whole, as is the `notices X first, and then Y` construction, which no sibling face spends (W4, the construction contract). W27 holds: the stranger sees and does nothing else. The face still closes on a standing fact a modifier can pick up (the country), and all four faces of the variant now carry the same safe generic, which is where the three PASSED siblings already stood.

## What this file holds, stated for the gate

- The COMPLETE replacement rows of the pool: variants 1, 2 and 3 in order, under their own numbers, each with its own single bracketed angle tag (`[counterforce]`, `[ledger]`, `[visitor]`) and exactly three `[face]` sub-rows.
- Exactly THREE faces differ from the annex at HEAD, and they are exactly the three targets: v2 f0, v2 f3, v3 f0. The other nine faces are byte-identical.
- Mechanical sweep re-run over the three cured faces: no em dash, no exclamation mark, no digit, no percent, no `which`-clause, no question, no second person, no citation, no record noun, no gate word, no force, watch, garrison, muster, militia or person word, no material, no verdict word, no forecast. Each cured face carries `{settlement}` exactly once and no other slot, and states BOTH licensed reads. No sub-row opens on a proper slot (T-F8). The pool keeps its ONE settlement-token opener (v2 f0) and its one existential opener (v1 f0); no two variants share their first two words (A11).

## Re-verification receipt (a second curer seat, 2026-09-12, after a window cut-off)

This packet was found already written and COMPLETE at the start of a fresh curer turn, and was continued under the checkpoint law rather than restarted. What was re-executed, not assumed:

- The twelve rows were diffed line-for-line against the annex at HEAD `471ce894a83658b0e0fe480819a9f1dcd22605e3` (`RECEIPT_POOLS_DOSSIER_STATE.md` lines 2666 to 2677). Exactly three lines differ and they are exactly the three targets: variant 2 spine, variant 2 face 3, variant 3 spine. The other nine faces are byte-identical.
- The refuter's three FAIL entries and the judge's three `CURE` rulings for this pool were re-read. All three targets are RULE 4 (the draft face carries the same breach), so no draft carve-out was available to any of them and each cure had to be authored fresh; each cure standing in this file is the cure the refuter and the judge both named.
- The one deviation is at variant 2 face 3, `come to little` replaced by `amount to little`, and the ration was recounted on the rows as they now stand: `come to little` stands twice in twelve faces and `beast or monster` stands twice in twelve faces, both at 0.167 and both under R-DA-10's 0.200 ceiling; a third `come to little` would have put it at 0.250, which is the identical arithmetic by which the refuter expressly forbade curing variant 2 face 0 with `beast or monster`. The cure's named SHAPE (the reversed read order, the moved subject, the moved landing noun, the referent named on the compression) is taken whole.
- Mechanical sweep re-run over the sentence bodies of all twelve rows with the numbering stripped: no em dash, no exclamation mark, no digit, no percent, no question mark, no `which`-clause. Row shape re-counted: three numbered spine rows, each with ONE bracketed angle tag of `[counterforce]` / `[ledger]` / `[visitor]`, and nine `[face]` sub-rows, three to a variant. No variant added, removed or merged; nothing trimmed.

Result unchanged: 3 of 3 targets cured, 0 refused.
