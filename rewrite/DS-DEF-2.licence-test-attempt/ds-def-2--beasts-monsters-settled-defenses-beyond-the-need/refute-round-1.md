# REFUTE ROUND 1 — DS-DEF-2 · pool `Beasts & Monsters: settled, defenses beyond the need`

Refuter seat: **opus** (the verifier), under the Fable chair. REWRITE car 8b, block DS-DEF-2, refute round 1.
Read against `kept.md` (the rows standing after the refinement), `draft-round-1.md`, `refine.md`, `skeleton.md`, the licence card printed in laneRW-DEF2, the SHIPPED rows (annex `:2621`–`:2623`, quoted in the skeleton), the REGISTER CARD, RULES-V2-PART-B §§1/16–16.2/18/20–23, MOVE-GRAMMAR §§1–3 + 4.4.1–4.4.3, CLERK-LAWS §§2.4.1/2.6.1, ARCH-COMPOSED-PROSE-v2 §§2.5/8.3, ADDENDUM 12 (W1–W10, R-i…R-viii′), ADDENDUM 13 A (W11–W19) and B (W20–W27), and the two law tables' defense sections (ENTAILMENT D-4/D-6/A-7; REFERENT walls row, D-F3, D-F24).
Written under the CHECKPOINT LAW: verdicts land as they are reached; the PROGRESS line says exactly what is on disk.

Face numbering: **face 0** = the numbered tagged line; **faces 1–3** = the `[face]` sub-rows in order. 3 variants x 4 faces = 12 faces.

PROGRESS FINAL: all three variants refuted; 12 of 12 faces on disk; summary and read-aloud notes written. COMPLETE.

## THE CARD, RESTATED AS THE TEST (what every face is judged against)

ONE read, `beastsRowSituation(family, perimeter, force) === 'settled country, perimeter'`; two licensed facts:
- **R1** `family === 'settled'` — the country's beast-and-raider tier is the LOW tier, stated over the COUNTRY (W2), as a band and never a zero or a peace (entailment item 4).
- **R2** `perimeter === true` — a wall-class row stands, by a class word only; no material (W11), no recorded name (the bag has no `{defwork}`), no size, condition, extent, geometry, orientation, count or manning (entailment item 1; D-6).
- `force` is NOT consulted on this branch; the silence on any force, watch, garrison, muster, militia, soldier or person on the works is the licensed state (W20, W12, W13, W22).
- may NOT: a count, a cause, a season, a future, a standpoint, a second fact. Bag: `{settlement}` only. Provenance ceiling: ZERO citations, ZERO record nouns (skeleton 0b.2; W24).

MECHANICAL SWEEP, run over `kept.md` first: no em dash, no exclamation mark, no digit, no percent, no `which`-clause, no question, no second person, no citation, no record noun, no gate word, no force or person word, no material, no verdict word, no forecast. All twelve faces carry `{settlement}` exactly once and no other slot. All twelve state BOTH R1 and R2. No face sub-row opens on the proper slot (T-F8). The three numbered lines open "There is" / "{settlement} is" / "A stranger" (A11 holds); one existential opener and one settlement opener in the pool; one semicolon (V1 F3). **No mechanical finding in the pool.**

## VERDICTS (variant · face · verdict · law · quote · finding · cure)

### VARIANT 1 · `[counterforce]`

**V1 · face 0 · PASS** — law: Part B §21.4 (the density floor) + entailment item 4 + W2. Quote: *There is very little of beast or monster in the country* (10 w). Finding: none. The shipped opener stands inside a longer clause, so the floor is kept and not smoothed; the inserted referent closes the peace-reading the bare "very little" left open (C7); R1 country-scoped, R2 by the always-safe class word, one comma-and joint, no cause and no purpose frame; the restraint is stated, so it is not an inventory line; it hands `the works` and `the country` forward and closes on a standing fact.

**V1 · face 1 · PASS** — law: the four-faces rule (construction) + D-6 + §21.4. Quote: *a quiet one in the way of beasts* (7 w). Finding: none. The country as subject is a construction its siblings do not spend; `what the town has built` is D-6's always-safe periphrasis; the band carries its referent; nothing is added. This is the pool's densest lawful line and §21.4 protects it from being made plainer.

**V1 · face 2 · PASS** — law: the four-faces rule (the order of the reads) + W2 + W10. Quote: *the country around the town is little troubled by creatures* (9 w). Finding: none. The one V1 face that runs R2 before R1, with a passive predicate its siblings do not use; the band is stated of the country and of the row's own referent; one low-magnitude surface only. (Noted, not a finding: the bare plural `Works stand at {settlement}` is the one place in the pool where the ear wants an article; no law reaches it, and an article would not change a claim.)

**V1 · face 3 · PASS** — law: A5 / the four-faces rule + R-DA-06 (the rationed joint) + W1. Quote: *Monsters come to little in the country around {settlement}* (9 w). Finding: none that carries. The pair with face 0 was tested hardest: the two share the read order and a works landing, but the first clause is genuinely recast (an existential quantifier becomes a subject-and-predicate), the joint changes from a comma-and to the unit's one rationed semicolon, and the possessor is seated on the town (W1's own cure form), so the pair clears A5's "past a synonym swap" bar. The band is present habitual, with no perfect aspect and no forecast (C3).

### VARIANT 2 · `[ledger]`

**V2 · face 0 · FAIL** — law: the card's `may NOT` (a second fact) + ENTAILMENT item 4 (the `settled` band is the LOW beast-and-raider tier, never a peace) + C7 (the same-page war row) + W2. Quote: *and the country around the town as quiet* (7 w). Finding: the second clause is an ellipsis of `is entered`, not of `against beasts`; the restriction to the row's referent sits on `provided`, so the predicate that reaches the country is an unrestricted `quiet`. That is the peace-reading the refinement cured three times over (V1's line, V3's line, V2 face 2) and this face escaped by being kept verbatim; DS-DEF-2 carries the war row and three more pressures on the same page, so an unqualified quiet of the country is a claim the card does not license and a sibling row on the same page may contradict. Cure: name the referent on the elided predicate and nothing else, e.g. *and the country around the town as quiet of them* (`them` binds to `beasts`, the clause's only plural); the ellipsis, the settlement opener and the density floor `provided against beasts` all survive untouched. Do NOT cure with `of beast or monster`: V1 face 0 and V2 face 2 already spend that pairing twice, and a third use puts it at 0.250 against R-DA-10's 0.200 pet-word ceiling.

**V2 · face 1 · PASS** — law: W7 / R-vi (the office's formula as a surface, never an agent-source) + W24 + W6 + W2. Quote: *The works at {settlement} are carried as standing* (8 w). Finding: none. One formula per face, so the ledger's word recurs across the family while the fact does not; no record noun and no citation anywhere in the face; the band is stated of the creatures of the country, which is country-scoped; the works as subject is a construction its siblings do not spend; the shift of subject is the unit's one turn outward and is placed last (THE THREAD).

**V2 · face 2 · WITHHELD** — law: the card's `may NOT` (a claim the read does not hold) + ENTAILMENT item 1 / D-6 (no geometry, no placement on the wall class). Quote: *what the town has built there is set down as standing* (11 w). Finding, unproven: `there` has two available bindings, and on the writer's own stated reading (refine.md: "`there` carries the country forward into the second clause") it binds to *the country around {settlement}*, which places the town's built works in the country AROUND the town; the read holds only that a wall-class row stands on the town's roll. The natural binding for a reader is `{settlement}` itself, so the breach is available but not established, and unclarity alone is not a finding. Cure if the chair takes it: delete `there`; THE THREAD's carry is already made by `the town`, which picks up `{settlement}`, so the deletion costs the face nothing and adds no claim.

**V2 · face 3 · FAIL** — law: A5 / the four-faces rule (never a paraphrase of a sibling; the faces differ in CONSTRUCTION, not vocabulary alone) + ENTAILMENT item 4 + C7. Quote: *Provision against beasts is entered at {settlement}* (7 w). Finding: two, and the cure must take both. (a) Against face 0 this is one clause in nominal dress, not a second construction: the same read order (R2 then R1), the same verb `is entered`, the same lexis `provided/provision against beasts`, and the same landing (`as quiet` / `is a quiet one`); the subject is the only axis of the three that moves, and the words themselves do not move at all, so the pair does not clear A5's "past a synonym swap" bar. (b) `is a quiet one` names no referent, and here it is a full clause with its own copula rather than an ellipsis, so the unrestricted-quiet fault of face 0 bites harder. Cure: reverse the read order and re-land the face so it is not face 0 recast, naming the referent in the same stroke, e.g. *Beasts come to little in the country around {settlement}, and provision against them is entered at the town.* — the nominal compression (the variant's density floor, skeleton 2.6) is kept, the order, the subject and the landing noun all move, the referent is named, and no claim is added.

CARRIED, NOT A FINDING (stated so the chair can rule and the curer does not re-open it): `provided against beasts` in V2 faces 0 and 3 reads, on a strict test, as the purpose frame the card refuses on V1 (*whatever the walls here are for*) — the works stated as provision AGAINST a thing is a relation between the two reads. I do not fail it. The marker ruled it a licensed idiom and this variant's density floor (skeleton 2.6), the block's PROVENANCE fence admits CAPABILITY clauses and refuses only HISTORICAL ones, and the reader itself is `beastsRowSituation`: the branch's own semantics join `perimeter` to the beasts read, so the compression renders the row rather than adding to it. What made the shipped V1 clause unlawful was the negation and the second pressure it implied, neither of which is present here.

### VARIANT 3 · `[visitor]`

**V3 · face 0 · FAIL** — law: W14 (the label bar: an engine label at its ENGINE meaning, never its dictionary sense) + W15 (the alias bar: a generic English word only where the row it names resolves; otherwise "the works" / "what the town has built") + ENTAILMENT D-6 and A-7 (`"perimeter"` is FALSE of `Citadel`, which is inner, and of `Gates`, which is a point; the class has no always-safe geometric generic) + entailment item 1 (a recorded noun licenses what the word MEANS). Quote: *A stranger notices the perimeter at {settlement} first* (8 w). Finding: `perimeter` is the branch argument's NAME rendered as English geometry. The walls bucket it stands for admits a `Citadel` and a lone `Gates (if walled)` row, of neither of which a perimeter is true, so the face asserts a continuity the record does not hold and is false on two of the seven rows that select this branch. The referent table's HOLDS for `the perimeter` is a LAYER ruling (a body word on a body read) and does not answer the entailment question; D-6 and A-7 do, and both name `the works` as the safe generic. The block's own label-trap list carries this exact word. Cure: one word, `the perimeter` → `the works`: *A stranger notices the works at {settlement} first, and then how quiet the country around the town is of beasts.* The density floor (skeleton 3.6: the seeing and the seen in one clause) is kept whole, the three sibling faces already spend the same safe generic, and no claim moves. The draft and the refinement both named this cure and carried the word rather than taking it; the refuter takes it.

**V3 · face 1 · PASS** — law: the four-faces rule (construction) + the card's `may NOT` (a count) + W2 + W27. Quote: *What a stranger sees at {settlement} is the works standing* (10 w). Finding: none. The pseudo-cleft carries both reads in one complement and is a construction no sibling spends; `few` is the band's own vague magnitude and not a count (no number is stated, and the LOW tier is precisely "some, and not many"); the stranger sees and does nothing else; the face closes on the country, a standing fact a modifier can pick up.

**V3 · face 2 · PASS** — law: W2 + W27 + R-DA-10 (the pet-word ration). Quote: *To a stranger the country around {settlement} is quiet* (9 w). Finding: none that carries. Tested for a dropped angle: the `To a stranger` adjunct is the only mark of the seeing and the second clause is stated flat, but the adjunct scopes the unit and one read is explicitly a thing seen, so the skeleton's dropped-angle test (3.7: "a face in which nothing is SEEN") is not met. The only V3 face that runs R1 before R2. The referent is named on the band.

**V3 · face 3 · PASS** — law: A5 / the four-faces rule + R-i / THE THREAD at k = 0 + W27 + the register card's short-line floor. Quote: *Monsters are little seen in the country around the town* (9 w). Finding: none that carries. The pair with face 0 was tested: the two share the subject and the read order, but the landing noun moves, the sentence count moves from one to two, and the verbs and the second subject are wholly different, so it is a fourth construction and not a fourth vocabulary. R-i holds: the second sentence picks `the town` up from `{settlement}` and its shift of subject is the unit's one turn outward, placed last. `comes on` is an encountering and not an act of will, so W27's bar on a stranger who acts or decides is not reached; `little seen` is the skeleton's own prescription for the visitor's R1 (3.4) and is a band, not a visibility claim the record lacks.

## SUMMARY

| | face 0 | face 1 | face 2 | face 3 |
|---|---|---|---|---|
| **V1 `[counterforce]`** | PASS | PASS | PASS | PASS |
| **V2 `[ledger]`** | **FAIL** | PASS | WITHHELD | **FAIL** |
| **V3 `[visitor]`** | **FAIL** | PASS | PASS | PASS |

Three FAILs of twelve; one WITHHELD. The cure round's targets are V2 face 0, V2 face 3 and V3 face 0; each cure above is a word or a clause and none adds a claim. Variant 1 stands whole. No variant is an inventory line, no licensed read is missing from any face, no licensed claim of a shipped sentence is dropped, no unlicensed shipped claim is re-imported, and the three density floors (V1's co-ordinated shape and its shipped opener; V2's `provided against beasts`; V3's seeing-and-seen in one clause) are all kept and survive their cures.

## READ-ALOUD NOTES (information only, one per variant)

**V2** — the ellipsis of `is entered` across the line's two halves is the pool's most clerkly turn in the mouth and survives the referent cure whole; faces 1 and 2 both run their second clause long, and the ear loses the subject of `come to little` once on a cold read. Face 3 is the flattest aloud, which is the audible face of the paraphrase finding above.

**V3** — the two short sentences of face 3 give the pool its only breath break and are the best-spoken lines in it. The numbered line is the hardest clause in the pool to say: `how quiet the country around the town is of beasts` strings three light syllables before `of beasts`, and the cure to `the works` does not change that, since the difficulty sits in the tail.

**V1** — the family has two real tempos aloud: the loose existential of face 0 against the thirteen-word snap of face 3, which is the pool's shortest single-sentence line and its best-spoken one. Face 2 is the only stumble: `Works stand at {settlement}` wants a definite article in the mouth, and the passive tail runs one beat longer than the ear expects after so short an opening.
