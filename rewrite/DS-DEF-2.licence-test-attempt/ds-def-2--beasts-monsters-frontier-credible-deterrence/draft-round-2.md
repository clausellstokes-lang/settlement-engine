Seat: Opus 5 — Fable-unvalidated. Block `DS-DEF-2` · pool `Beasts & Monsters: frontier, credible deterrence` · REWRITE draft round 2.
Written to the marker's `skeleton.md` in this directory, the printed licence card (re-printed read-only this round, byte-identical), the register card (S2, S3), RULES-V2 Part B §1 / §16–§16.2 / §18 / §20–§23, MOVE-GRAMMAR §1–§3 and §4.4.1–§4.4.3, CLERK-LAWS §2.4.1 / §2.6.1, ARCH-COMPOSED-PROSE-v2 §2.5 and §8.3, `rulings-DEF2.txt`, and brief ADDENDUM 13 parts A and B (bars W1–W27).
ROUND 2 ANSWERS THE GATE'S ONE NAMED MEASURE: `shapes.participialOpenerRate (over) = 13.62 band-widths on 1 of 12 faces`. Exactly one sentence in the set opened on a participle — face 1-a's second sentence, `Standing with the works is the town's force.` It is re-cut in place. Nothing else in the set moves, because nothing else failed and §21.2 keeps a lawful face only while it stays lawful.
Three variants in, three variants out: the same vids, the same order, the same angle tags, none added, none removed, none merged. One bracketed tag per row; no `[plain]` anywhere.
Paste the block below under the pool's bold heading, in place of the pool's three numbered rows.

**`Beasts & Monsters`: `frontier`, credible deterrence**
1. `[ledger]` Around {settlement} lies frontier country, and the town's works stand in it. A force stands behind the works.
   - `[face]` The works at {settlement} stand on an active frontier. With the works stands the town's force.
   - `[face]` At {settlement} the works are carried as standing, and a force with them. The country the works stand in is frontier country.
   - `[face]` A force stands at {settlement}, and the country it stands in is frontier country. What the town has built stands with the force.
2. `[street]` Frontier country lies outside the town, and what stands inside it are the works and a force.
   - `[face]` The works are up and a force stands with them in frontier country.
   - `[face]` In frontier country the town has a force, and that force stands behind the works.
   - `[face]` The town's force stands behind what the town has built, and the country outside is frontier country.
3. `[counterforce]` The works at {settlement} stand against frontier country, and behind the works the town's force.
   - `[face]` At {settlement} the works are not without a force, and the country outside them is frontier country.
   - `[face]` Outside {settlement} the country is frontier country, and inside the town a force stands with the works.
   - `[face]` In the frontier country around {settlement} the town's force stands with what the town has built.
--- NOTES

## 0. THE GATE'S FEEDBACK, ANSWERED MEASURE BY MEASURE (round 1 → round 2)

| the gate's row (round 1) | what it names | what round 2 did | round 2's figure |
|---|---|---|---|
| band depth · `shapes.participialOpenerRate` (over) = 13.62 band-widths on 1 of 12 faces; DEPTH at most 1.75 band-widths on every face | face 1-a's SECOND sentence opened on a present participle: *Standing with the works is the town's force.* The band for this shape is at or near zero on the exemplars (fault 2, the participial glossary tail, `best-ai.md`: MODERATE, archival YES), so a single instance in a two-sentence face blew the depth by an order of magnitude | the sentence is re-cut in place as a fronted prepositional phrase with the verb before the subject: **With the works stands the town's force.** The participle is gone; the face's DECLARED construction — the inverted second sentence, which is what tells 1-a apart from its three siblings (W4, the construction contract) — is kept exactly, because moving 1-a onto a sibling's plain subject-verb-object shape would itself be the regression W4 names | participial openers in the set: **0 of 16 sentences, 0 of 12 faces**. Depth on this measure: 0 band-widths |
| (no other failing measure was named) | — | nothing else moved. §21.2: a lawful set keeps its refinement only if it stays lawful, and every other measured band was held at round 1 | unchanged; §E re-states them |

**This round is NOT dry.** It moves the one failing owned measure to zero and adds no new failing state. The two collateral effects on reported (non-failing) figures are both in the favourable direction and are declared here rather than claimed as cures: the within-pool sentence-length sd rises from 3.70 to **3.79** (sample) and 3.58 to **3.67** (population) against R-DA-05's direction of sd ≥ 4.0, because the re-cut sentence is one word shorter than the sentence it replaces; and the set's mean face length falls from 17.25 to **17.17** words. Neither figure was padded toward: the re-cut was chosen for the participle and for the construction contract, and the sd moved as a consequence (§21.4 — a figure is never the reason to pad or split a lawful face).

**What was considered and refused as the cure**, recorded so the chair can veto the reading:
- *"The town's force stands with them."* — plain subject-verb-object. REFUSED twice: it closes on a pronoun (R-DA-04's pronoun-closer ceiling is 0.055; the set is at 0.000 and this would put it at 0.063 on 16 sentences), and it abandons 1-a's declared inverted construction for the parent's shape (W4).
- *"Behind the works stands the town's force."* — an inversion, and the licensed backing idiom. REFUSED as a near-paraphrase of its own sibling: the parent's second sentence is *A force stands behind the works*, the same five content words re-ordered, and a face may never be a paraphrase of its sibling. `with` keeps the key's own conjunction (the works AND a force) without re-using the parent's words.
- *"There is a force with the works."* — REFUSED: the expletive is struck (R-DA-07, CC-8).
- Splitting or lengthening any other face to move the sd figure — REFUSED as the regression §21.4 names.

## A. THE CARD, AND THE THREE CLAUSES EVERY CLAIM IS KEYED TO

Printed read-only in `laneRW-DEF2` this round: `node scripts/prose-licence-card.mjs DS-DEF-2 'Beasts & Monsters: frontier, credible deterrence'`. The card is byte-identical to the one round 1 was written against.

- **MAY CLAIM** (verbatim): *that the reader `beastsRowSituation(family, perimeter, force)` selects the row `frontier country, perimeter and force` of `BEASTS_ROW_POOL` in `defenseStateProse.js`, as a STANDING fact of the record.* The selected row has three limbs, and this packet keys every claim in every face to one of them:
  - **C1** — the `family` limb: the country is **frontier country**. Layer NONE (a country-tier token; it names no body). Stated over the COUNTRY, never as a totality over the town (W2; C7: the war, crime, economy and disaster rows sit on the same tab).
  - **C2** — the `perimeter` limb: **the works stand** (at least one `walls`-bucket member). Layer BODY. Spoken only by the always-safe class words "the works" / "what the town has built" (W15; entailment D-6: the class has no safe geometric generic).
  - **C3** — the `force` limb: **a force stands** (`garrison || militia`). Layer BODY. Spoken only by the class words "a force" / "the town's force" (ADDENDUM 13 A.5; W12/W13/W15 bar "the garrison", "the militia", "the watch", "the guard", "soldiers", "townspeople" on a key that reads garrison OR militia and never the watch).
- **THE KEYED CONDITION (R-v, `rulings-DEF2.txt`).** The reader returns this row only when all three limbs hold, so C1 + C2 + C3 are ONE state, not three facts. A face stating all three states the state the key names and spends no "second fact"; the card's may-NOT "a second fact" refuses a fact OUTSIDE the three (a pay gate, a stock, a gate, a watch, a charter, a count, a badge).
- **MAY NOT** (verbatim): a count, a cause, a season, a future, a standpoint, a second fact. **REFUSED COLUMNS:** a totality over persons; an exemption from a duty; a named character and that character's fate; a theological claim.
- **BAG:** `{band: RESERVED, route: proper, settlement: proper}`, FILLED at this block's call sites `{settlement}`. Every face carries exactly its parent's slot set: vid 1 `{settlement}`, vid 2 `{}` (no slot in any face, as the shipped row), vid 3 `{settlement}`. `{band}` and `{route}` are named by no face; `{defwork}` is not in this block's bag and is named by no face.
- **FORM:** sentence. **ROLE:** spine (no relation, no attach, no consequence clause: S2's clause seat has no RELATION row to sit on here).
- **SOURCE:** `muster · standing LICENSED`, spent at ZERO — see refusal 5.

## B. PER FACE — WHICH CARD CLAUSE LICENSES EACH CLAIM

Every face of every variant states all three limbs. No face makes a claim outside C1, C2, C3 and the bag. Order is written as the sequence of limbs; the close names the landing noun and its KIND (R-DA-04). **The one row that changed this round is marked ROUND 2.**

### Variant 1 · `[ledger]` (vid 1 · slot set `{settlement}` · two sentences · grammar V1 PRESENT)

The ledger is a STANDPOINT and licenses no record noun and no citation (W24); its lawful surface is the office's own formula (R-vi/W7: *entered*, *carried standing*), which one face uses and three do not.

- **1 (parent)** — "Around {settlement} lies frontier country, and the town's works stand in it. A force stands behind the works."
  - `frontier country` = **C1**, stated as the country's own condition. `the town's works` = **C2**; the possessive is seated on the TOWN (W1), never on the works. `A force` = **C3**. `{settlement}` = **CARD-BAG**, and it does not open the face (ARCH §2.5 T-F8).
  - `behind the works` is the key's own conjunction read together — the licensed idiom of backing, not a spatial fact and not a manning (the skeleton's claim row: it becomes unlicensed the moment it is sharpened to "on", "in" or "manning"). Order C1 → C2 → C3. Thread (R-i): sentence two carries the noun *the works* forward. Close: *the works*, an OBJECT.
- **1-a (ROUND 2)** — "The works at {settlement} stand on an active frontier. With the works stands the town's force."
  - `The works` = **C2**. `an active frontier` = **C1** at the label's own engine rendering (W14; the skeleton's fence 1 — the engine prints exactly this phrase for this value). This is the pool's one turn kept from the shipped line (§D below) and it is untouched by this round's re-cut. `the town's force` = **C3**, possessor seated on the town (W1).
  - `With the works` is the key's own conjunction — the works AND a force, read together — stated as the anchor of the second sentence, not as a spatial fact and not as a manning. The noun echo is the THREAD and is lawful as such (MOVE-GRAMMAR §1.4.1: a deliberate noun echo for the thread is lawful; A11's echo bound counts facts, not nouns). It is not a doubled beat (W6): the second sentence asserts C3, a limb the first sentence does not carry; the repeated noun is the hinge, not a restatement of the read.
  - Order C2 → C1 → C3. Thread: *the works* carried forward. Close: *the town's force*, an OBJECT. **Construction:** the inverted second sentence — a fronted prepositional phrase with the verb before its subject — which is the declared contract of this face and what tells it apart from the parent (subject-verb-object on an indefinite subject), from 1-b (a copular sentence on a relative-clause subject) and from 1-c (subject-verb-object on a free-relative subject). Round 1 realised the same inversion with a participle; round 2 realises it with the preposition, which is the whole of the change.
- **1-b** — "At {settlement} the works are carried as standing, and a force with them. The country the works stand in is frontier country."
  - `the works` = **C2**; `carried as standing` = **CARD-MAYCLAIM** (the standing fact of the record) in the office's own formula (R-vi) — it cites nothing and names no record noun. `a force with them` = **C3** (the second conjunct elliptical; "them" binds to the works, W1). `frontier country` = **C1**.
  - Order C2 → C3 → C1. Thread: *the works* carried forward. Close: *frontier country*, a CONDITION.
- **1-c** — "A force stands at {settlement}, and the country it stands in is frontier country. What the town has built stands with the force."
  - `A force` = **C3**. `frontier country` = **C1**. `What the town has built` = **C2** in the second always-safe spelling of the wall class (W15).
  - Order C3 → C1 → C2. Thread: *the force* carried forward. Close: *the force*, an OBJECT.

### Variant 2 · `[street]` (vid 2 · slot set `{}` · one sentence · grammar V1 PRESENT)

The street states the standing plainly, because no field holds what the town says or thinks about it (R-iii; R-DA-13's belief-frame floor; W23's fused agent). Its difference from the ledger is the plain idiom and the absence of the office's formula, and no face carries a slot. Unchanged this round.

- **2 (parent)** — "Frontier country lies outside the town, and what stands inside it are the works and a force."
  - `Frontier country` = **C1**, the country's condition. `the works` = **C2**. `a force` = **C3**. `inside it` binds to *the town* (W1). No slot, as the parent's set requires.
  - Order C1 → C2 → C3. Close: *a force*, an OBJECT.
- **2-a** — "The works are up and a force stands with them in frontier country."
  - `The works are up` = **C2** in the street's plain idiom (presence, never a height, a material, a gate or a manning). `a force` = **C3**. `frontier country` = **C1**.
  - Order C2 → C3 → C1. Close: *frontier country*, a CONDITION. The pool's shortest face.
- **2-b** — "In frontier country the town has a force, and that force stands behind the works."
  - `frontier country` = **C1**. `the town has a force` = **C3** (possession as a standing fact; the town is not an agent that decides, W23). `the works` = **C2**.
  - Order C1 → C3 → C2. Close: *the works*, an OBJECT. `that force` carries the noun across the joint.
- **2-c** — "The town's force stands behind what the town has built, and the country outside is frontier country."
  - `The town's force` = **C3**. `what the town has built` = **C2**. `frontier country` = **C1**.
  - Order C3 → C2 → C1. Close: *frontier country*, a CONDITION.

### Variant 3 · `[counterforce]` (vid 3 · slot set `{settlement}` · one sentence · grammar V1 PRESENT)

The counterforce sets the PRESSURE beside the THING STANDING AGAINST IT and settles nothing (the skeleton's fence 3, from the block's own subtitle: *the five pressures, and what the town actually has standing against each*). It states no deterrence, no cause, no forecast, no frequency of arrivals, no mechanism and no badge word; the reader draws what the key is named for. Unchanged this round.

- **3 (parent)** — "The works at {settlement} stand against frontier country, and behind the works the town's force."
  - `The works` = **C2**. `frontier country` = **C1** (the pressure side). `the town's force` = **C3**. *Stand against* is the block's own frame for what a town has standing against a pressure (the header line: *the five pressures, and what the town actually has standing against each*) — an orientation, never an effect, a purpose or a deterrence, and never an intent predicated of an inanimate thing beyond the block's own words (R-DA-11).
  - Order C2 → C1 → C3. Close: *the town's force*, an OBJECT. The second conjunct is elliptical, the face's construction.
- **3-a** — "At {settlement} the works are not without a force, and the country outside them is frontier country."
  - `the works` = **C2**; `not without a force` = **C3** as the pool's ONE negated surface on a present read (W10: never the opener, at most one). `frontier country` = **C1**; `outside them` binds to the works (W1).
  - Order C2 → C3 → C1. Close: *frontier country*, a CONDITION.
- **3-b** — "Outside {settlement} the country is frontier country, and inside the town a force stands with the works."
  - `frontier country` = **C1**. `a force` = **C3**. `the works` = **C2**. The outside/inside pair is the counterforce's two sides stated as position, never as a line, a circuit or an enclosure (entailment D-6).
  - Order C1 → C3 → C2. Close: *the works*, an OBJECT.
- **3-c** — "In the frontier country around {settlement} the town's force stands with what the town has built."
  - `the frontier country around {settlement}` = **C1**. `the town's force` = **C3**. `what the town has built` = **C2**.
  - Order C1 → C3 → C2. Close: *what the town has built*, an OBJECT (a name not given in its own noun — the wall class spoken by its safe generic). The pool's one face that states the counterforce as containment rather than opposition; recorded as a reading judgment at refusal 3.

## C. WHAT THE SHIPPED SENTENCES CLAIMED AND THIS DRAFT DROPS

Dropping is the rewrite's purpose; the shipped breach is the corpus's known state. Nothing here is added back under another wording, and no claim is added. This section is unchanged from round 1 — the round-2 re-cut changed a construction, never a claim, and face 1-a's claim set (C1, C2, C3, `{settlement}`) is identical before and after, which is arm A6's test across the four faces.

**Vid 1, shipped:** *"{settlement} sits on an active frontier with a line and a force behind it. Most of what comes out of the country will not press a defended perimeter, and most of what comes here does not."*
- `a line` — DROPPED. It asserts a CONTINUOUS enclosure the `walls` bucket does not entail (entailment D-6: continuity NOT ENTAILED; false of `Citadel`, which is inner, and of `Gates (if walled)`, which is a point); W15's alias bar. Replaced by the always-safe class words, never by nothing.
- `Most of what comes out of the country` and `most of what comes here` — DROPPED twice over: a COUNT (a frequency over arrivals), and an OBSERVABLE no field holds (there is no arrival, incursion or raid record on this key or on the desk; the stress roll is a generation-time probability, never a record of what came). Card may-NOT "a count"; MOVE-GRAMMAR §1.2 row 1's may-NOT.
- `will not press` — DROPPED: a bare future indicative, a FATE breach (A2, R-DA-07, THE PROMISE; FORECAST is a non-move, MOVE-GRAMMAR §1.3). Card may-NOT "a future".
- `a defended perimeter` — DROPPED: "defended" asserts MANNING, not entailed by any walls row (D-3; "the men on the wall" struck at ADDENDUM 13 A.5); "perimeter" is D-6's refused geometric generic.
- the whole second sentence's implication that the works and the force are the reason nothing presses — DROPPED: a CAUSE (card may-NOT), and the key's own name *credible deterrence* is not a licence (ADDENDUM 8 ruling 2 as the skeleton generalises it). The sentence is the engine's chrome (`threatAssessment.js:74`) copied into the record; product code is not a licence for the same words in a face.
- Also dropped as a pool-level cure: the shipped opener on `{settlement}` (a sentence face may not open on a `proper`-typed slot of the bag, ARCH §2.5 T-F8; MOVE-GRAMMAR §1.4 wall 10). Moving the opener is the cure, not a regression.

**Vid 2, shipped:** *"The town takes the frontier seriously and has taken it seriously long enough that the arrangements are ordinary rather than anxious."*
- `takes the frontier seriously` — DROPPED: a BELIEF FRAME and a disposition no field carries (R-DA-13's floor; R-DA-14 seen-not-meant; card may-NOT "a standpoint"), and a FUSED COLLECTIVE AGENT (W23 — the town as a regarding, judging subject; the same fault as the desk's `:2900` "the town has decided", D-F16).
- `has taken it seriously long enough` — DROPPED: a HISTORY and a DURATION (R-DST-B: a standing configuration field licenses a structural clause and never a historical one; no event-provenance field on this key; card may-NOT "a season").
- `the arrangements are ordinary` — DROPPED: a quality of the bodies no field holds, an assigned reaction.
- `ordinary rather than anxious` — DROPPED: a contrast whose rejected alternative names no sibling pool key and no sibling band (R-DA-02), and a FEELING word on an inanimate thing (R-DA-11).
- `long enough that` — DROPPED: a CAUSE (card may-NOT; no RELATION row, so S2's clause seat is closed on this spine).
- `the arrangements` as the one civic noun — REPLACED, not kept: the shipped row folded the two BODY reads into one authored generic and stated ONE of the three limbs. Every face now names the works and the force apart. This raises the variant's licensed density above the shipped line's, which is the density floor working in the rewrite's favour here (§21.4).

**Vid 3, shipped:** *"Very little reaches {settlement} out of the wild country, and the reason is that the arrangements are visible from a long way off."*
- `Very little reaches {settlement}` — DROPPED three times over: a COUNT, an OBSERVABLE the fields do not hold, and a TOTALITY over the town's safety on a tab that also carries the war, crime, economy and disaster rows (C7, W2 — the threat is country-scoped).
- `the wild country` — DROPPED: a LABEL read at its dictionary sense (W14). `frontier` is the MIDDLE exposure tier and the engine's own words for it are "frontier" and "active frontier"; "wild" over-reads it toward `plagued`. The tier is now stated by its own name in every face.
- `the reason is that` — DROPPED: an explicit CAUSE (card may-NOT), and the register's refusal to explain what a fact means or to sum up the sentence before (R-DA-03, R-DA-12). The same six-word cause frame is a template tic elsewhere in this block (`Economic Survival: STRONG` v3, `Disasters & Famine: granary AND hospital` v3); no face here reuses it.
- `visible from a long way off` — DROPPED: an OBSERVABLE and a spatial fact no field holds (visibility, distance, sightline; a HEIGHT of the works is not entailed, D-3 — a thorp palisade fills the same bucket as city walls), and a MECHANISM the engine does not model (the monster arm carries `hasWalls +30` and a force term, never a sight-deterrence).
- KEPT from the row: only the scope noun *the country* (now named as frontier country) and the two-sided counterforce SHAPE, which is the shipped line's one lawful property.

**Kept across all three:** exactly C1, C2 and C3, plus `{settlement}` where the parent carried it. Nothing else. No claim is added anywhere.

## D. THE DENSITY FLOOR (§21.4), READ AGAINST THE SHIPPED SENTENCES

- **The one shipped clause that is lawful under the card stands verbatim as a face.** `on an active frontier` is carried into face 1-a exactly as it was written, and round 2 did not touch it: the re-cut is confined to that face's SECOND sentence, so the pool's one kept turn survives the round intact. It is the shipped line's sharpest lawful turn — the tier stated in four words at the label's own engine rendering — and the owner's exemplar clauses were written to be kept, not smoothed. It is kept as a NON-OPENING clause because the shipped opener on `{settlement}` is itself a breach (§C).
- **`and a force behind it` survives as a rhythm, not as its words.** The shipped conjunction is lawful once the pronoun is bound to a licensed noun (W1): "a force behind them", "behind the works the town's force", "with the works stands the town's force". The shipped `a line`, which the conjunction hung on, cannot stand (D-6), so the turn is carried on "the works".
- **The round-2 re-cut is not a plainer line.** It trades a participial fronting for a prepositional fronting at the same length class, keeps the inversion, keeps the landing noun and keeps every claim; the change is a shape the exemplars do not carry being replaced by one they do. No lawful compression was traded for plainness, which is the regression §21.4 names.
- **Where the shipped line was richer, the richness was unlicensed invention**, copied in vid 1's case from the engine's own chrome. A faithful rewrite is plainer in exactly those places by design, and the skeleton records this as the honest limit of this pool. The density the rewrite can pay back is: all three limbs stated in every face where the shipped rows stated three, one and none; the tier named by its own word in every face where two rows never named it; the works and the force named apart in every face where two rows folded them into "the arrangements"; and four distinct constructions per variant where there was one.
- **No face is made plainer than the card allows with no law behind the change.** Every simplification in §C is named to the law that forces it.

## E. REFUSALS (a refusal is a result)

1. **No variant is refused.** All three are written lawful under the card, the walls, the bars W1–W27 and the bands.
2. **POOL-LEVEL REFUSAL — the distinct-grammar rule cannot be met.** MOVE-GRAMMAR §2.1 asks a pool of k variants to carry min(k, 8) DISTINCT level-1 grammars. This card licenses ONE reading and no second typed field: no `none-exists` field (no V3), no named-object FIELD in the bag (no V4 — `{defwork}` is not in this block's bag, and a class word does not make an OBJECT move), no institution-table row (no V5), no unresolved value (no V6), no structural-consequence field (no V2), no event provenance (no V7), no `not-held` field (no V8). Under §3.2's licensing filter the drawable set is `{V1}`, so all three variants are V1 (PRESENT) and the pool shows one grammar. The census reads `grammars: 2` for this pool; this packet cannot produce the second from the fields the card prints, and records the disagreement for the chair rather than inventing a field. The variation is carried instead by the angle, the limb order, the subject, the landing noun, the sentence count and the rhythm. **Not curable at the writer's desk. Carried unchanged from round 1.**
3. **PARTIAL REFUSAL — the `[counterforce]` stance has no register-card line.** The skeleton's fence 3 defines it from the two shipped uses and the register card, and this packet writes to that definition: the pressure set beside the thing standing against it, as two standing facts, settling neither. Face 3-c states the relation as CONTAINMENT (the force and the works standing IN the frontier country) rather than as opposition; this packet judges that lawful and within the stance, and records it as the set's one reading judgment, vetoable. If the chair defines `[counterforce]` as requiring an event negation (the thing that did NOT happen), variant 3 is unsatisfiable under this card — an event negation is an observable no field holds — and becomes a sitting row; the packet's reading is that the standing opposition is the angle's lawful form on a state pool. R-DA-19's live corpus defect (`economicBase: extraction → [counterforce]`) awaits the same ruling. **Carried unchanged from round 1.**
4. **REPORTED, not cured — the within-pool sentence-length spread.** Sixteen sentences across twelve faces run 6 to 17 words, mean 12.88, sd 3.79 (sample) / 3.67 (population), against R-DA-05's within-pool direction of sd ≥ 4.0. The distance is 0.21 of the direction's own unit (round 1: 0.30), inside §16.1's DEPTH of 0.5 band-widths and inside the BUDGET of one third of the measurable soft rules (this is the only soft rule this set exceeds). Padding a lawful face or splitting a lawful sentence to move the figure is the regression §21.4 names, so the figure is reported and left; the improvement this round is a consequence of the participial cure, not an attempt at the figure. Every other measured band the set holds: zero participial openers (round 1: one — the gate's row, now cured), zero digits, zero em dashes, zero `which`, zero questions, zero exclamations, zero expletive openers, zero future indicatives, zero pronoun closers (R-DA-04's ceiling is 0.055; this set is 0.000), zero semicolons, zero colons, zero citations, zero belief frames, zero triads, one negated surface in twelve faces (W10's ceiling is one per variant), zero contrast shapes (refusal 6).
5. **DELIBERATE ZERO — the provenance move.** The card marks `source: muster · standing LICENSED`, and no face cites it. Three grounds. (a) Part B §24's ceiling: one citation per unit, on a licensed holder, and only for one of S3's three reasons (two accounts that disagree; a count from an interested party; a record whose keeper is a power) — none of the three obtains on a three-limb presence read, and the exemplar registers with raw text cite at 0 per 786 sentences, so a citation here is the habit a refuter names. (b) The census row resolves `holder: null` for this kind on this pool (`holderReason: town-resolved`), so there is no holder to name; OW-23's rule is the class word or nothing. (c) W24 and R-vi: "the roll shows" / "the books" is a citation the budget does not license, and a garrison-only town has men under arms and no roll at all (entailment D-8), so no face writes "the muster roll", "the roll" or any record noun. The ledger's `carried as standing` in face 1-b is the OFFICE'S OWN FORMULA, not a citation and not a record noun.
6. **DELIBERATE ZERO — the CONTRAST move.** R-DA-02 licenses a contrast only where the rejected alternative names a sibling pool key or a sibling band, never fronted, never the closing move of more than one variant per pool. The sibling keys `plagued` and `settled` and the sibling row `frontier, force without a perimeter` are all nameable in principle, but naming a sibling tier inside a `frontier` face risks the label traps W14 guards (`settled` is the LOW tier, never "no live threat"; `plagued` is monster activity) for no licensed gain, so the set spends no contrast at all. Face 3-a's `not without a force` is a NEGATED SURFACE on a present read (W10), not a contrast: it rejects no alternative and names no sibling.
7. **DELIBERATE ZERO — the capability clause (the skeleton's fence 2).** The block's PROVENANCE line says the causal clauses here are *capability* clauses (walls without people cannot be held). This packet writes the bare conjunction the key itself makes (the works, and a force with them or behind them) and refuses every capability PREDICATE — "a force to hold it", "held", "manned", "defended", "enough to", "could be held" — because each adds a manning, an observable or a forecast no field holds, against the card's may-NOT. If the chair rules the capability joint lawful for this block, this set gains a lawful option it did not take; nothing in it has to move.

## F. FORM, THREAD, SIBLINGS AND THE BARS — the checks this packet ran on itself

**Form (ARCH §2.5, the projector).** Twelve rows: three numbered variant rows and nine `   - `[face]`` sub-rows, three per variant, no more and no fewer. Each variant row carries exactly ONE bracketed angle tag, the tag the shipped row carries (`[ledger]`, `[street]`, `[counterforce]`), and no `[plain]` marker appears anywhere (it belongs to modifier rows; the projector refuses it on a spine). The typed lines (ROLE, READS, RELATION, ATTACH, FORM, MOVE) are untouched and unrepeated. No vid moves; the count stays three; the order stays 1, 2, 3. Every row is sentence-form (a finite verb in the main clause; the two elliptical second conjuncts, 1-b and 3, hang on a finite main clause; face 1-a's second sentence is a finite inversion, `stands` before its subject, not a fragment). No row — parent or face — opens on a `proper`-typed slot (T-F8): `{settlement}` never stands first. Each face's `{slot}` set is exactly its parent's.

**The participial check, run on every sentence this round.** Sixteen sentences; the opening token of each is a determiner, a preposition, a bare noun, a free relative or a locative adverb, and none is a participle or a participial phrase: *Around · A · The · With · At · The · A · What · Frontier · The · In · The · The · At · Outside · In*. The set's `participialOpenerRate` is 0 of 16 sentences and 0 of 12 faces. No `-ing` form stands at the head of any sentence or of any clause after a joint; the two `-ing` forms that remain in the set are `standing` inside face 1-b's `carried as standing`, which is the office's formula in a noun position, and nothing else.

**The thread (MOVE-GRAMMAR §1.4.1; R-i at k = 0).** This pool is the SPINE and opens the passage, so every face hands a noun forward that a modifier can pick up — *the works*, *the force*, *frontier country* — and none closes on a pronoun or an abstraction. The census reads `k 2`: two modifier positions may follow, ordered by salience, so each face is written to read as well before an unknown modifier as it does alone. Inside vid 1's own two sentences the second sentence carries a noun forward from the first in all four faces (parent *the works*; 1-a *the works*, now carried as the fronted anchor `With the works`; 1-b *the works*; 1-c *the force*), which is R-i regardless of k. The round-2 re-cut strengthens the thread rather than weakening it: the carried noun now stands FIRST in the second sentence instead of second.

**A11 and the four-faces rule.** No two VARIANTS share their first two words: *Around {settlement}* · *Frontier country* · *The works*. The sentence counts that differed keep differing: vid 1 two sentences in every face, vids 2 and 3 one. The four faces of each variant differ in CONSTRUCTION — the subject, the order of the limbs, the landing noun — and not in vocabulary alone (ADDENDUM 7 rule 4; W4): vid 1 runs C1→C2→C3, C2→C1→C3, C2→C3→C1, C3→C1→C2 on four different subjects, and its four second sentences now run subject-verb-object on an indefinite subject (parent), a fronted-prepositional inversion (1-a), a copular sentence on a relative-clause subject (1-b) and a free-relative subject (1-c) — four shapes, no two alike; vid 2 runs C1→C2→C3, C2→C3→C1, C1→C3→C2, C3→C2→C1; vid 3 runs C2→C1→C3, C2→C3→C1, C1→C3→C2, C1→C3→C2 — faces 3-b and 3-c share a limb order and are told apart by their subject (*a force* against *the town's force*), their clause count (two against one) and their landing noun (*the works* against *what the town has built*). That is the one place in the set where the order alone does not separate two faces, and it is recorded rather than cured by a wording no field licenses.

**Openers across variants, recorded.** Only ONE variant of this pool is drawn per town, and only one face of it, so no two rows of this set ever meet a reader together. Three faces nevertheless open on the same four words — 1-a and 3 (parent) on *The works at {settlement}*, 1-b and 3-a on *At {settlement} the works* — which the licensed vocabulary makes hard to avoid when the works are the only wall-class noun the card allows and the slot may not stand first. A11's variant-level rule is met (the three parent rows open *Around {settlement}* · *Frontier country* · *The works*); the face-level overlap is recorded here rather than cured by a noun no field licenses.

**No inventory line (ADDENDUM 7 rule 3).** Every variant states all three of the card's licensed reads AND the angle's stance: the ledger enters the fact as the record holds it and uses the office's formula once; the street states the standing plainly in the town's idiom with no slot, no formula and no attitude; the counterforce sets the country against what stands in it. No row says only that the key fact holds.

**W1–W19 (the entailment bars), as this set obeys them.** W1: every possessive is seated on the TOWN (*the town's works*, *the town's force*), and every "them" / "it" binds to the noun intended (the works, the town); face 1-a's re-cut sentence carries no pronoun at all. W2: the threat is stated over the COUNTRY in all twelve faces, never as a totality over the town, because the war, crime, economy and disaster rows sit on the same tab. W3: no tier band word is used, so no band marker is owed. W4: the construction declared per face is kept — and the round-2 re-cut keeps 1-a's declared inversion rather than moving it onto a sibling's shape, which is the bar's own test. W5: the naming form is never asked to carry a band. W6: no clause restates the read it follows; `With the works` is the thread's anchor on a sentence that asserts C3. W7: no roll as an agent-source. W8: the ledger enters and cites nothing. W9: no entailment is treated as a must. W10: one negated surface in the pool, never an opener. W11 (THE MATERIAL BAR): no material and no material source anywhere — no stone, no timber, no earth. W12 (THE GARRISON BAR): the word "garrison" appears nowhere. W13 (THE WATCH BAR): "the watch" appears nowhere; this read never consults the watch bucket. W14 (THE LABEL BAR): `frontier` is rendered at its ENGINE meaning (the middle exposure tier, printed by the engine as "an active frontier"), never as "wild", "embattled", "dangerous" or "quiet"; no other label is touched. W15 (THE ALIAS BAR): the wall class is spoken only as "the works" / "what the town has built"; the force class only as "a force" / "the town's force"; no "the militia", "the guard", "soldiers", "townspeople", "the men". W16: no pay, wage, upkeep or keeping word (the pay gate is DS-DEF-11's cell); "has" and "stands" carry presence, not funding. W17, W18, W19: no crisis, no stress record, no faith read on this key.

**W20–W27 (the referent bars), as this set obeys them.** W20 (THE LAYER BAR): the country is spoken at NONE (a tier token; no body word rides it), the works and the force at BODY by their class words, and no badge, score or label word appears. W21: no power, seat, hall or council. W22 (THE PERSON BAR): no person anywhere — no "somebody", no "whoever", no "the people on it", no role. W23 (THE FUSED-AGENT BAR): the town possesses and stands; it never decides, takes, believes, minds or knows. W24 (THE RECORD-WORD BAR): no record noun at all (the holder resolves null), and the `[ledger]` tag is treated as a standpoint. W25: no external body. W26: no visibility read; the audience is the player face with no mark. W27 (THE ANGLE-TAG BAR): the stances carry no bodies of their own — no "the elders", no stranger, no holder's record.

**Siblings (arms A1 and A11, the block's other twenty-two pools read whole).** No face restates a sibling's opening: the `plagued, perimeter AND organized force` row opens "The country around {settlement} is thick with creatures", which is why vid 1 opens *Around {settlement} lies frontier country* and not "The country around". The sibling pool `plagued, NO perimeter and NO force` carries a participial face opening (`Coming to {settlement} out of monster country, a stranger finds…`) under a `[visitor]` tag; this pool's round-2 cure means no face of this pool carries that shape, and the sibling's row is that pool's to answer, not this packet's. No face reuses the block's `and the reason is` cause frame (`Economic Survival: STRONG`, `Disasters & Famine: granary AND hospital`), its `A stranger …` visitor opening, or the `walls with citizen militia` row's "at {settlement} with townspeople behind them" shape. No face contradicts a sibling in structural fact: the frontier tier is named only as this row's own value, and the sibling rows `frontier, force without a perimeter`, `settled, defenses beyond the need` and `plagued, …` are neither named nor implied. The four known shipped breaches this block carries (`settled` v1 "substantial works"; `settled` v3 "how relaxed the people on it are"; `walls with NO force` v1 "takes this town with ladders"; `granary AND parish care only` v1 "clergy who tend the sick") are other pools' rows and are not this packet's to cure, and nothing here reproduces their shape.

**Measurements (the set, for the gate's report; `{settlement}` counted as one word).**

| face | words | sentences (words each) | limb order | close (kind) |
|---|---|---|---|---|
| 1 parent | 18 | 2 (12, 6) | C1 C2 C3 | the works (object) |
| 1-a | 16 | 2 (9, 7) | C2 C1 C3 | the town's force (object) |
| 1-b | 22 | 2 (13, 9) | C2 C3 C1 | frontier country (condition) |
| 1-c | 23 | 2 (14, 9) | C3 C1 C2 | the force (object) |
| 2 parent | 17 | 1 (17) | C1 C2 C3 | a force (object) |
| 2-a | 13 | 1 (13) | C2 C3 C1 | frontier country (condition) |
| 2-b | 15 | 1 (15) | C1 C3 C2 | the works (object) |
| 2-c | 17 | 1 (17) | C3 C2 C1 | frontier country (condition) |
| 3 parent | 15 | 1 (15) | C2 C1 C3 | the town's force (object) |
| 3-a | 17 | 1 (17) | C2 C3 C1 | frontier country (condition) |
| 3-b | 17 | 1 (17) | C1 C3 C2 | the works (object) |
| 3-c | 16 | 1 (16) | C1 C3 C2 | what the town has built (object) |

Faces 12 · mean 17.17 words · range 13 to 23. Sentences 16 · mean 12.88 · sd 3.79 (sample), 3.67 (population) · range 6 to 17. Slots: `{settlement}` in eight faces (vids 1 and 3), none in four (vid 2). Participial openers 0 · citations 0 · digits 0 · em dashes 0 · `which` 0 · questions 0 · exclamations 0 · semicolons 0 · colons 0 · expletive openers 0 · future indicatives 0 · pronoun closers 0 · negated surfaces 1 · contrast shapes 0 · triads 0.

**The one byte that changed from round 1**, for the chair's diff: face 1-a's second sentence, `Standing with the works is the town's force.` → `With the works stands the town's force.` Every other row of the block is byte-identical to round 1 as it stands in the dock's annex at `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md` line 2637 and following.
