# SKELETON — DS-DEF-2 · `Beasts & Monsters: frontier, credible deterrence` (the marker's packet; Fable, 2026-09-11)

Seat: Fable 5.1 — marker for the REWRITE, working for the Fable chair. This file is the only thing this seat wrote. Everything below is judged against the printed licence card, the census row it was printed from, the reader function the card names, the annex block's header lines, the register card, brief ADDENDUM 13 parts A and B (the entailment law, the referent law, the writer bars W11–W27) and `rewrite/rulings-DEF2.txt`; where a shipped clause reads well but no field licenses it, it is UNLICENSED here and the law is named.

Status: COMPLETE — sections 0, variants 1 to 3, 8 and 9 all written (the checkpoint law was followed section by section; nothing is pending).

## 0. THE CARD, THE KEY, THE THREE INPUTS AND THEIR LAYERS (what the faces are wired to)

### 0.1 The licence card as printed (`node scripts/prose-licence-card.mjs DS-DEF-2 'Beasts & Monsters: frontier, credible deterrence'` in laneRW-DEF2, read-only)

```
LICENCE (block DS-DEF-2 · role spine · key `Beasts & Monsters: frontier, credible deterrence`)
  reads:      beastsRowSituation(family, perimeter, force) (via BEASTS_ROW_POOL in defenseStateProse.js)
              (absent ⇒ no candidate; a modifier is silent, never "false")
  predicate:  beastsRowSituation(family, perimeter, force) (via BEASTS_ROW_POOL in defenseStateProse.js) === frontier country, perimeter and force
  bag:        {band: RESERVED, route: proper, settlement: proper}
              FILLED at this block's call sites: {settlement}
  relation:   (a spine takes no relation)
  seat/form:  (not a seat-taker) / sentence      move: (none declared)     angle: counterforce ledger street
  attach:     (empty: a spine takes no attach set)
  echo:       spine mounts 1 (tabs: defense) · modifier mounts 0 (none)
              every pool that selects a row of `BEASTS_ROW_POOL` shares ONE echo key: a mount counted there may be a sibling ROW of the same table
  covert:     no
  source:     muster · standing LICENSED
              a citation of this holder is licensed where the provenance budget allows
  may claim:  that the reader `beastsRowSituation(family, perimeter, force)` selects the row `frontier country, perimeter and force` of `BEASTS_ROW_POOL` in `defenseStateProse.js`, as a STANDING fact of the record
  may NOT:    a count, a cause, a season, a future, a standpoint, a second fact
  audience:   player (no mark)
  REFUSED COLUMNS, always: a totality over persons; an exemption from a duty (whoIsExempt is null everywhere); a named character and that character's fate (product scope); a theological claim about a deity (the deity doctrine)
```

The census row behind the card (`docs/content/wiring-census.json`, block DS-DEF-2, this pool): `status RESOLVED` · `rung table` · `keyFunction BEASTS_ROW_POOL` · `readsGrain branch` · `reads` = the one synthetic label above (`readsCount 1` in the leaf) · `narrowed false` · `objectClasses []` (this card, unlike the plagued sibling's, does NOT refuse "another civic object of the class wall") · `sites ["defense.threatAssessment"]` · `k 2` · `rateBp 1549` · `source {kind: muster, fields: {family: "", perimeter: "", force: "muster"}, holder: null, holderReason: "town-resolved: the register is not a town, so the holder is named by holdersOf(kind, settlement)", standing: LICENSED, twoSource: false}` · `variants 3` · `grammars 2` · `slotsFilled ["settlement"]`.

### 0.2 The one read, unpacked to its three inputs (`defenseStateProse.js:429-440`, `:447-451`, `:623-626`, `:655`) — read for the VALUES at this key, not as a licence of its own

The reader returns this pool's situation only when ALL THREE hold, so (chair, rulings-DEF2 R-v, from ADDENDUM 8 ruling 1) the three are ONE KEYED CONDITION: a face that states all three states the state the key names and adds no "second fact"; the card's "a second fact" refuses facts OUTSIDE the three (a pay gate, a stock, a gate, a watch, a charter, a count).

| input | how the caller derives it | value at this key | REFERENT LAYER of the read (ADDENDUM 13 B A.1) | the lawful nouns | the unlawful nouns |
|---|---|---|---|---|---|
| `family` | `measuredMonsterFamily(settlement.config.monsterThreat)` — the RAW value must be present; `normalizeMonsterThreat` maps `medium` → `frontier`; `frontier` is the producer's own middle tier (`MONSTER_THREAT_TIERS` = heartland · frontier · plagued, calmest → most dangerous) | `frontier` | **NONE** — a country-tier token, a condition of the COUNTRY (a terrain-class token names no body) | "frontier country", "the frontier", "the country" (W2: stated over the country, never as a totality over the town); the tier's engine rendering is "an active frontier" (`SummaryTab.jsx:37` prints exactly "It sits on an active frontier." for this value; `threatAssessment.js:74` "Active frontier") — see fence 1 | "wild", "dangerous", "hostile" (a dictionary sense of the label, W14); "embattled" (the `plagued` word); "quiet", "settled" (the low tier); any monster KIND (the field holds a tier); an incursion, a raid, a season (an event no field records); "the frontier" as a border between realms (the tier is exposure, not a boundary) |
| `perimeter` | `standingDefenseForces(settlement).walls.present` — the LIVE roster, ruin-filtered; the `walls` bucket = `wall · citadel · palisade · earthwork · inner citadel · massive walls` (`defenseInstitutionBuckets.js:84-87`) | `true` | **BODY** — the walls bucket (an institution row the read reaches by the bucket) | "the works", "what the town has built" (the always-safe generics, entailment D-6, rulings-DEF2); "the wall" only as the class word where the writer accepts that a Citadel town reads an inner fortress | "a line", "a perimeter", "a circuit", "enclosed" AS GEOMETRY (continuity is NOT ENTAILED; false of `Citadel` (inner) and `Gates (if walled)` (a point) — D-6, W15); a material (W11); height, condition, age, a gate, a walkway (D-3, D-1); "defended", "manned", "held", "people on it" (manning is NOT ENTAILED; "the men on the wall" struck at ADDENDUM 13 A.5); `{defwork}` (NOT in this block's bag) |
| `force` | `forces.garrison.present \|\| forces.militia.present` — the `garrison` bucket (`garrison · barracks · professional guard · professional city watch · multiple garrison`) OR the `militia` bucket (`citizen militia · militia`); EXCLUDES the `watch`, `mercenary`, `charter` and `magicDef` buckets (referent table 2.1, row `force`) | `true` | **BODY** — the garrison-or-militia buckets; HOLDER kind **muster** (the card's `source`) for the force fact, resolving `holder: null` on this row (a garrison-only town keeps no roll) | "a force", "the town's force", "the muster" as the CLASS word (ADDENDUM 13 A.5: always-safe for the paid military; a class word asserts no roll) | "the garrison" (only where a `Garrison` / `Multiple garrisons` row resolves, W12 — false on a militia town); "the militia" (only where `forces.militia.present` — false on a garrison town); "the watch" (the read never consults it — the sibling's D-F3 finding; W13); "the guard" (struck, A.5); "soldiers" on a militia town, "townspeople" on a garrison town; "the muster roll"/"the roll" as a RECORD (only with a resolved `Citizen militia` + `Muster training`, OV-5 — never here); a count, arms, pay, wages, upkeep (the pay gate is NOT a read of this card; it is DS-DEF-11's cell); a headcount or the force's strength (DS-DEF-5's cell) |

**What the KEY'S NAME licenses: nothing.** "credible deterrence" is the pool key's tail, the corpus's own gloss; ADDENDUM 8 ruling 2 ("the block's title 'why the wall' is its name, not a licence") applies one for one — no face says the works deter, that creatures are kept off, or that the posture is adequate, because no field holds a deterrence, an arrival, or a rating of this arm's sufficiency (the arm's badge is a separate AGGREGATE read this pool does not carry). The engine's own chrome for this branch (`threatAssessment.js:74-77`, "Walls and garrison provide credible deterrence: most creature threats will not press a defended perimeter … Adequate for the threat level.") is PRODUCT CODE outside the REWRITE's corpus (ADDENDUM 13 B D, OW-20) and is not a licence for the same words in a face; it is where the shipped v1 got its second sentence.

### 0.3 The block's header lines (annex `RECEIPT_POOLS_DOSSIER_STATE.md`, `### DS-DEF-2`, lines 2568-2596 of the laneRW-DEF2 copy)

- **STATE-KEY:** five fixed rows (`Beasts & Monsters` · `Invasion & War` · `Internal Security` · `Economic Survival` · `Disasters & Famine`), each with a `scoreBand` badge, read against `config.monsterThreat` (`plagued` / `frontier` / `settled`), the institution presence flags, and `compound.inst`.
- **SLOTS:** `{settlement}` `{band}` `{route}` — the card: only `{settlement}` is FILLED at this block's call sites; `{band}` RESERVED; `{route}` proper but unfilled here. A face names neither `{band}` nor `{route}`.
- **SECTION-TARGET:** `defense`.
- **PROVENANCE + FENCE:** `buildThreatAssessment` is dossier-native and this shape EXTENDS it into a pool (each branch held ONE string; every settlement in a branch said the same words); two defects must not be reintroduced (the `plagued`+nothing lowercase lead; the walls read rides `defenseProfileHasWalls`, never a presence check on `institutions.walls`); **institution presence is a STANDING fact with no recorded history; the causal clauses here are CAPABILITY clauses (walls without people cannot be held), never HISTORICAL ones** unless the history surface supplies the ancestry — see fence 2 on what a capability clause may say under the card's "may NOT a cause".
- **RECEIPT:** `threatAssessment.js:28-195`; rendered `DefenseTab.jsx:150-183`; the walls predicate `causalState.js:300-315`. **PDF PARITY:** parity.
- **Same page (C7, W2):** DS-DEF-2 mounts the Invasion & War, Internal Security, Economic Survival and Disasters rows on the SAME tab; a Beasts face that totalises the town's safety ("nothing reaches", "very little reaches {settlement}") contradicts a war or crime row beside it. The threat is stated over the COUNTRY.

### 0.4 The register card's six one-line registers (read whole; the dossier binds here)

The dossier: the record itself; the clerk's third person; the six shapes of its closed set; the town's name is not the default opener. The NPC ladder, the Herald, the chronicle, the DM page, chrome and the docent do not reach this pool. Amendment S2 (a computed CONSEQUENCE of the sentence's own fact may ride as one clause, the joint's own comma and word from the connectives list, never "which", two sentences at most — and here no relation row exists, so NO consequence clause is available on this spine) and S3 (every fact has a holder; the office cites one only for an S3 reason; never "according to" by habit) bind every face. The three questions before any sentence ships: which field licenses it; which claims does it carry and are they the same claims as before; what does it leave standing open.

### 0.5 The pool's shipped rows, verbatim (the annex, bold line `**\`Beasts & Monsters\`: \`frontier\`, credible deterrence**`; leaf vids 1-3)

1. `[ledger]` {settlement} sits on an active frontier with a line and a force behind it. Most of what comes out of the country will not press a defended perimeter, and most of what comes here does not.
2. `[street]` The town takes the frontier seriously and has taken it seriously long enough that the arrangements are ordinary rather than anxious.
3. `[counterforce]` Very little reaches {settlement} out of the wild country, and the reason is that the arrangements are visible from a long way off.

Variant count: THREE shipped variants (ledger · street · counterforce), matching the card's `angle: counterforce ledger street`. Slot sets per parent (a face whose `{slot}` set differs from its parent's is refused, ARCH §2.5): vid 1 `{settlement}` · vid 2 `{}` (NO slot — every face of variant 2 carries no `{settlement}`) · vid 3 `{settlement}`. Sentence counts: vid 1 two sentences; vids 2 and 3 one each (A11: where they differed in sentence count they keep differing).

### 0.6 Three fences this seat records for the writer and the chair, not decided here

1. **"active" as the tier's word.** The card licenses the tier `frontier` as the country's condition; the engine's own rendering of exactly this value on two product surfaces is "an active frontier" (`SummaryTab.jsx:37`; `threatAssessment.js:74`), and the tier's engine meaning is the MIDDLE exposure band (stress roll ×1.4 on siege and monster, ×1.5 on wartime; monster arm +4; readiness penalty 7). This seat reads "active" as the LABEL'S ENGINE RENDERING (W14: the label at its engine meaning), lawful as the tier's name and never sharpened into an event, a season, a count of incursions or a monster kind. If the chair rules that only the bare tier word is licensed, "active" drops from the turns worth keeping below and nothing else moves.
2. **The PROVENANCE capability clause against the card's "may NOT a cause".** The block's header says the causal clauses here ARE capability clauses; the card prints "a cause" and "a second fact" under may-NOT. This seat judges by the card as ADDENDUM 8 ruling 2 did: a capability clause that names ONLY the key's two bodies in the key's own conjunction ("the works, and a force behind them") is the keyed state and lawful; a capability clause that PREDICATES holding, manning or deterrence ("held", "defended", "a force to hold it", "will not press") adds an observable, a manning, or a forecast no field holds and is UNLICENSED. If the chair rules the capability joint lawful for this block, the rows marked "a capability the fields do not hold" below flip; the cause rows ("the reason is") and the forecast rows ("will not") do not.
3. **The counterforce stance has no line of its own on the register card** (W27 lists it as a STANCE; R-DA-19 records `economicBase: extraction → [counterforce]` as a live corpus defect awaiting a chair ruling). This seat defines it from the two shipped uses on this block and the register card's "carry two accounts and settle neither": the counterforce sets the PRESSURE (the country) beside the THING STANDING AGAINST IT (the works and the force) as two standing facts and lets the reader draw the deterrence; it may NOT state the deterrence as a cause ("the reason is"), a forecast ("will not press"), a frequency of arrivals ("very little reaches"), or a mechanism ("visible from a long way off"). If the chair defines the stance otherwise, section (5) of variant 3 is re-cut and nothing else moves.

---

## VARIANT 1 · `[ledger]` (vid 1; slot set `{settlement}`; two sentences)

**(2) Shipped, verbatim:**
> {settlement} sits on an active frontier with a line and a force behind it. Most of what comes out of the country will not press a defended perimeter, and most of what comes here does not.

**(3) Every claim, one per line (licence · law · referent layer beside the read's layer):**
- {settlement} sits in frontier country ("sits on an active frontier") — **LICENSED** the `family` input at `frontier` (the keyed condition, read over the country). Layer **NONE** (a country-tier condition) = the read's layer. "active": LICENSED as the tier's engine rendering under fence 1; never an event.
- {settlement} opens the sentence — **a pool-level fence, not a claim**: a sentence face may not open on a `proper`-typed slot of the block's bag (ARCH §2.5, T-F8); this opener cannot be carried into any face.
- a line stands ("a line") — **LICENSED in substance** the `perimeter` input (a walls-bucket member stands; layer **BODY** = the read's layer), **UNLICENSED in its noun**: "a line" asserts a CONTINUOUS enclosure the bucket does not entail (entailment D-6: continuity NOT ENTAILED; false of `Citadel` and of `Gates (if walled)`; the chair's rulings-DEF2 line names the safe generic "the works"). W15 alias bar. The lawful spelling: "the works" / "what the town has built".
- a force stands ("a force") — **LICENSED** the `force` input (garrison or militia present). Layer **BODY** = the read's layer; "a force" is the always-safe class word (ADDENDUM 13 A.5).
- the force is BEHIND the line ("behind it") — **LICENSED as the key's own conjunction** (the works AND a force, read together), an idiom of backing and not a spatial or manning fact; **it becomes UNLICENSED the moment it is sharpened** into "on the wall", "standing in it", "manning it" (manning NOT ENTAILED; "the men on the wall" struck).
- most of what comes out of the country WILL NOT press a defended perimeter — **UNLICENSED: a forecast** (a bare future indicative; the register never takes the future; MOVE-GRAMMAR §1.3 FORECAST does not exist; THE PROMISE); **a count** ("most", a frequency over incursions no field records); **a maxim** (a life-general claim about creatures and perimeters, R-DA-12's generalisation test); and **a manning** ("defended" asserts people on the works, NOT ENTAILED). Layer **NONE** (an unrecorded event class) against a BODY read — a wrong-layer predicate. This sentence is the engine's chrome (`threatAssessment.js:74`) copied into the record; the chrome is not a licence.
- most of what comes HERE does not press the perimeter — **UNLICENSED: an observable the fields do not hold** (no incursion or arrival field exists; the stress roll is a probability at generation, never a record of what came), **a count** ("most"), and **a totality over the town's safety on a page that carries the war and crime rows** (C7, W2: the threat is country-scoped). Layer **NONE**.
- the arrangement deters ("credible deterrence", implied by the whole second sentence) — **UNLICENSED: a cause** (the works and the force as the reason nothing presses; the card's may-NOT; the key's name is not a licence, §0.2).

**(4) The reads the rewrite must state (all of them) plus the licensed shipped claims:**
- `family === 'frontier'` — the country around {settlement} is frontier country, a standing condition of the country (the road's register in the holder table's terms: "the look of the country, which anyone travelling it can see"); stated over the country, never as the town's safety, never as an event.
- `perimeter === true` — the works stand at {settlement} (the walls-bucket class word); never "a line", never a material, a height, a gate or a manning.
- `force === true` — a force stands at {settlement} (the town's force; the muster as the class word); never "the garrison" or "the militia" as the specific, never the watch, never a count, never pay.
- the shipped licensed claims carried forward: {settlement} sits in frontier country; a force stands with the works (the conjunction, "with … and a force behind"). Nothing of the second sentence survives.

**(5) The angle's stance:** the ledger is the clerk's own way of stating the fact (W24: a STANDPOINT that licenses NO record noun and no citation), so this face enters the three reads as the compiled record holds them — the country's tier, the works, the force — in the office's formula ("entered as standing", "carried standing") if it wants one, with the wall-class and force-class nouns at their read's layer; it may cite a holder at most once and only for an S3 reason (two accounts disagreeing, a count from an interested party, a keeper who is a power), none of which the three reads present, and the muster holder resolves `null` on this row (OW-23: a kind with no holder is cited by the class word or not at all), so the recommended citation count is ZERO and "the roll shows" / "the books" is a finding (R-vi, W7); it may NOT invent a forecast, a frequency, a manning, a deterrence, a badge word, a pay fact, a count, a season, or a "because".

**(6) Turns worth keeping (verbatim, lawful as they stand):**
- `sits on an active frontier` — lawful under fence 1 as a NON-OPENING clause (the settlement token may not open a sentence face); the density floor of this variant's first read in four words. If fence 1 rules the bare tier only, the turn becomes "sits in frontier country" and is no longer verbatim.
- `and a force behind it` — lawful as the key's conjunction once "it" binds to the works (W1: seat the referent; "behind them" where "the works" is plural); the rhythm "X, with the works and a force behind them" is the shape to carry even though the shipped words "a line" cannot.
- Not kept: "a line" (D-6, W15); the whole second sentence (a forecast, two counts, a maxim, a manning, an observable, a cause).

**(7) What would make the rewrite a regression here:**
- inventory: vid 1 loses its slot set `{settlement}`, its `[ledger]` tag, its two-sentence shape against its one-sentence siblings (A11), or drops below four faces; a face that paraphrases a sibling face rather than differing in construction;
- a lost licensed read: a face without the frontier country, without the works, or without the force — the shipped line states all three, so the floor is three reads in every face;
- a lost lawful turn: "sits on an active frontier" gone from every face (under fence 1), or the conjunction "with the works and a force behind them" traded for two facts in two disconnected sentences with no noun carried forward (R-i, the thread at k = 0 inside the variant's own two sentences);
- a dropped angle: a face that reads as the town's talk or a stranger's eye under a ledger tag; a face that cites a holder;
- an added unlicensed claim: "will", "most", "defended", "a line", "perimeter", a monster kind, "wild", a badge word ("adequate"), a pay or upkeep word, "because" or "the reason", a season, an em dash, a digit, a which-clause, an exclamation, a totality over the town's safety.

---

## VARIANT 2 · `[street]` (vid 2; slot set `{}` — NO `{settlement}` in any face; one sentence)

**(2) Shipped, verbatim:**
> The town takes the frontier seriously and has taken it seriously long enough that the arrangements are ordinary rather than anxious.

**(3) Every claim, one per line (licence · law · referent layer beside the read's layer):**
- the country is frontier country ("the frontier") — **LICENSED in substance** the `family` input; layer **NONE** = the read's layer. **Under-stated in form**: the frontier appears only as the object of the town's regard, not as the country's condition; the rewrite states it outright.
- the town TAKES the frontier SERIOUSLY — **UNLICENSED: a belief frame / a disposition** (no field carries an attitude; R-DA-13's belief-frame floor; R-DA-14 seen-not-meant) and **a fused collective agent** (W23: "the town has decided" class — the town as a regarding, judging agent; W22: no person-plural as decider). Layer: the town as an agent is **NONE** (a collective person) against BODY reads — a wrong-layer subject.
- the town HAS taken it seriously LONG ENOUGH — **UNLICENSED: a history and a duration** (R-DST-B: a standing configuration field licenses a structural clause and never a historical one; no event-provenance field on this key; the card's "a season"), compounded by the same belief frame.
- the arrangements exist ("the arrangements") — **LICENSED in substance** as a class noun over the key's two BODY reads (the works and the force together); layer **BODY** = the read's layer. See open row 4 on whether this authored generic is accepted as the class word; the always-safe spellings are "the works" and "the town's force" named apart.
- the arrangements are ORDINARY — **UNLICENSED: a condition of the bodies no field holds** (routine, settledness — a standpoint; the register's "an assigned reaction"); a quality word on the read's nouns.
- ordinary RATHER THAN anxious — **UNLICENSED: a contrast whose rejected alternative names no sibling key or band** (R-DA-02: a bare "X, not Y"; "anxious" is no pool key of this table) and **a feeling word** on an inanimate thing (R-DA-11: no intent or feeling for an inanimate thing; "anxious arrangements" is a figure).
- the ordinariness FOLLOWS FROM the long seriousness ("long enough that") — **UNLICENSED: a cause** (the card's may-NOT; no relation row; S2's clause seat is closed on this spine).
- the works and the force — **NOT STATED** as the two bodies; folded into "the arrangements". The rewrite states each.

**(4) The reads the rewrite must state (all of them) plus the licensed shipped claims:**
- `family === 'frontier'` — frontier country outside the town, said in the town's own plain words as a condition of the country (never "we", never "you", never what the town thinks of it).
- `perimeter === true` — the works stand: the plain noun ("the works", "what the town has built"); never "the wall" sharpened into a material or a gate, never "the line".
- `force === true` — a force stands: "the town's force", "the muster" as the class word; never "the garrison"/"the militia" as the specific, never the watch, never the pay.
- the shipped licensed claims carried forward: the frontier (as the country's condition); the arrangements (only as the two bodies named). Nothing else in this row survives as licensed.
- **No `{settlement}` in any face** (the parent's slot set is empty).

**(5) The angle's stance:** the street is the town's own talk about its condition, so this face may state the frontier country, the works and the force as the town's standing condition in the town's idiom — the short clause, the thing named rather than judged, the plain noun where the ledger would use the formula — and may leave one matter standing open (stated, never asked); the PROVENANCE line licenses only structural reads, so the street may NOT say what the town thinks, takes, minds, knows or is used to, may NOT quantify or personify the town ("the town takes", "nobody", "everyone"), may NOT report a habit or a duration ("long enough", "always has"), may NOT grade the arrangements (ordinary, anxious, settled, easy), and may NOT explain one read by another.

**(6) Turns worth keeping (verbatim, lawful as they stand):**
- none survives whole. The lawful residue is the two nouns `the frontier` and `the arrangements` and the street's plainness. The density floor for this variant is therefore the three reads at the street's stance in one sentence, with a sharper licensed fact in place of the belief frame (the works and the force named apart, the frontier country stated as the country's own condition) — richer in truth than the shipped line, which states one read.

**(7) What would make the rewrite a regression here:**
- inventory: vid 2 gains a `{settlement}` slot (its parent has none), loses its `[street]` tag, moves from one sentence to two while its siblings keep their counts (A11's differing-count rule), or drops below four faces; a face that paraphrases a sibling;
- a lost licensed read: a face without the frontier country, the works, or the force — the shipped line states one read only, so the rewrite's floor is HIGHER than the shipped line, and returning to one read is the regression;
- a lost lawful turn: none to lose; but a face that trades the street's idiom for the ledger's formula or a stranger's eye is a dropped angle;
- an added unlicensed claim: a belief or attitude verb (takes seriously, minds, knows, is used to), a duration ("long enough", "for years", "always"), a quality on the arrangements (ordinary, routine, easy, anxious), a contrast naming no sibling, a cause, a totality, "the garrison"/"the militia"/"the watch" as the specific, an em dash, a digit, "which", an exclamation.

---

## VARIANT 3 · `[counterforce]` (vid 3; slot set `{settlement}`; one sentence)

**(2) Shipped, verbatim:**
> Very little reaches {settlement} out of the wild country, and the reason is that the arrangements are visible from a long way off.

**(3) Every claim, one per line (licence · law · referent layer beside the read's layer):**
- very little reaches {settlement} — **UNLICENSED: a count** ("very little", a frequency of incursions), **an observable the fields do not hold** (no arrival, incursion or raid field exists on this key or anywhere on the desk; the stress roll is a generation-time probability, never a record of what came), and **a totality over the town's safety on a page that carries the war and crime rows** (C7, W2). Layer **NONE** (an unrecorded event class) against BODY reads. Also a negated-surface opener on a claim that is not a PRESENT read at all (R-ii licenses one negated surface per variant only on a PRESENT read).
- something comes OUT OF THE COUNTRY — **LICENSED in the noun only**: "the country" is the frontier read's scope (W2); the claim that things come out of it is the same unrecorded observable. Layer **NONE** = the read's layer for the country.
- the country is WILD — **UNLICENSED: a label rendered at its dictionary sense** (W14): `frontier` is the MIDDLE exposure tier and the engine's own words for it are "frontier" and "active frontier"; "wild" over-reads the tier toward `plagued`. The tier itself (the frontier) is **NOT STATED** by its name in this row.
- the arrangements exist ("the arrangements") — **LICENSED in substance** as the class noun over the works and the force (layer **BODY** = the read's layer); open row 4 as for variant 2.
- the arrangements are VISIBLE FROM A LONG WAY OFF — **UNLICENSED: an observable and a spatial fact the fields do not hold** (visibility, distance, sightlines: no field; a HEIGHT of the works is NOT ENTAILED, D-3; a palisade at a thorp fills the same bucket as city walls), and **a mechanism the engine does not model** (the monster term is `hasWalls +30` and a force term, never a sight-deterrence).
- the visibility is THE REASON little reaches the town — **UNLICENSED: a cause** (the card's may-NOT; the explaining sentence — the register never explains what a fact means or sums up the sentence before; R-DA-03/R-DA-12) and, with the first clause, **the deterrence claim itself** (the key's name is not a licence, §0.2).
- the works and the force — **NOT STATED** as the two bodies; the frontier — **NOT STATED** as the tier. All three reads must be stated.

**(4) The reads the rewrite must state (all of them) plus the licensed shipped claims:**
- `family === 'frontier'` — the country around {settlement} is frontier country: the PRESSURE side of the counterforce, stated as the country's standing condition (never "wild", never an incursion, never a frequency).
- `perimeter === true` — the works stand: the first half of what stands against the country.
- `force === true` — a force stands: the second half; the class word only.
- the shipped licensed claims carried forward: "the country" (as the read's scope); "the arrangements" (only as the two bodies). Nothing else survives; this variant's density floor is the three reads at the counterforce stance, which is a HIGHER floor than the shipped line (which states none of the three by name).

**(5) The angle's stance (fence 3):** the counterforce sets the pressure beside the thing standing against it and settles nothing — the frontier country on one side, the works and the force on the other, as two standing facts in one sentence or two, so that the reader draws the deterrence the key is named for; the PROVENANCE line licenses structural reads only, so it may NOT state the deterrence as a cause ("the reason is", "which is why"), a forecast ("will not press"), a frequency of arrivals ("very little reaches", "nothing has come"), a mechanism (visibility, distance, a sight line), a totality over the town's safety, a badge word ("adequate", "credible"), or a manning ("defended", "held"); it may leave the matter standing open (the country presses; the works and the force stand; what comes of it the record does not say) as a stated fact, never a question.

**(6) Turns worth keeping (verbatim, lawful as they stand):**
- none survives whole. The lawful residue is `out of the … country` with "wild" struck (the country as the read's scope) and `the arrangements` as the class noun. The counterforce's two-sided shape (pressure, then what stands against it) is the shipped line's one lawful property and the shape to carry.
- Not kept: "Very little reaches {settlement}" (a count, an observable, a totality); "wild" (W14); "the reason is that" (a cause); "visible from a long way off" (an observable, a height, a mechanism).

**(7) What would make the rewrite a regression here:**
- inventory: vid 3 loses its slot set `{settlement}`, its `[counterforce]` tag, its one-sentence count against vid 1's two (A11), or drops below four faces; a face that paraphrases a sibling;
- a lost licensed read: a face without the frontier country, the works, or the force — every face states three reads where the shipped line named none;
- a lost lawful turn: the two-sided counterforce shape gone from every face (a face that states the three reads as a flat inventory with no pressure set against a standing thing is an inventory line, ADDENDUM 7 rule 3);
- a dropped angle: a face that reads as the ledger's entry or the street's talk under a counterforce tag; a face that closes on a cause or a verdict;
- an added unlicensed claim: "reaches", "comes", "arrives" as a record of incursions; "very little", "nothing", "most"; "wild", "dangerous"; "the reason", "because", "which is why"; "visible", "seen from", a distance; "deters", "deterrence", "credible", "adequate"; a manning word; a monster kind; an em dash, a digit, "which", an exclamation.

---

## 8. POOL-LEVEL FENCES THE FACES MUST OBEY TOGETHER (the marker's inventory of the walls)

- **The settlement token never opens a sentence face** (ARCH §2.5, T-F8: a sentence face opening on a `proper`-typed slot of the block's bag is refused; MOVE-GRAMMAR §1.4 wall 10). Today vid 1 opens on `{settlement}` — a shipped breach; moving the opener is a cure, not a regression. Lawful openers: the country ("Frontier country lies around {settlement}…"), the works, the force, the office's formula, a plain civic noun. Vid 2 carries no slot at all.
- **The slot sets are the parents' exactly:** vid 1 `{settlement}` · vid 2 `{}` · vid 3 `{settlement}`. `{band}` (RESERVED) and `{route}` (unfilled here) are named by no face. `{defwork}` is NOT in this block's bag and is named by no face; the works are the class word.
- **A11's spread:** no two variants of one pool share their first two words; vid 1 keeps two sentences and vids 2 and 3 one each; the four faces of a variant differ in CONSTRUCTION, not vocabulary alone (ADDENDUM 7 rule 4; W4 the construction contract).
- **Level-1 grammars this block can license:** V1 PRESENT (the keyed condition, its three reads in any order — country first, works first, force first) is the only member whose licensing field this card prints; V4 OBJECT → PRESENT needs a named-object FIELD (the bag carries no `{defwork}`, so a class word does not make V4); V2 needs a structural-consequence field (none); V3/V8 need a `none-exists` or `not-held` field (none: every read is present); V5 needs an institution table row (none on the card); V6 an unresolved value (none). A pool of three carries min(3, |set|) distinct grammars; the census reads `grammars: 2`, so the writer varies the ORDER of the three reads and the sentence count, and declares the grammar per variant.
- **The thread (MOVE-GRAMMAR §1.4.1; R-i at k = 0):** this pool is the SPINE, so its face opens the passage and must hand a noun forward that a modifier can carry — the works, the force, the country — and should not close on a pronoun (R-DA-04's pronoun-closer ceiling); inside vid 1's own two sentences the second carries a noun forward from the first regardless of k. The census reads `k 2`: two modifier positions may follow this spine, ordered by salience, so every face is written to read well before an unknown modifier as well as alone.
- **The close:** a standing fact of varied KIND across the faces (a condition, a prohibition, an absence, an object, a name not given); never a verdict ("adequate"), a maxim, a hook, a cause.
- **The referent walls (W20–W27) on this pool:** the country at NONE (a tier word, never a body, never "the frontier" as a border); the works at BODY (the walls bucket) by the class word, never a geometry, material, height, gate or manning; the force at BODY by the class word "a force" / "the town's force" / "the muster", never "the garrison", "the militia", "the watch", "the guard", "soldiers", "townspeople" as the specific on a key that reads garrison OR militia; no person as agent (W22); no fused agent ("the town takes", "the town has decided", W23); no record word (W24: the muster holder resolves null here; the ledger is a standpoint that licenses no citation); the stance's nouns never a holder's record (W27).
- **The label walls (W14) on this pool:** `frontier` at its engine meaning (the middle tier; "an active frontier" under fence 1), never "wild", "embattled", "quiet"; `plagued` and `settled` are the SIBLING keys and may not be named (R-DA-02 contrast only where a sibling key is named as the rejected alternative — and even then never fronted; the safest form names neither).
- **Hard walls, every face:** no em dash, no exclamation, no digit, no which-clause, no question, no "I"/"you", no figure, no sense verb on an abstraction, no intent or feeling for an inanimate thing, no forecast (the edge, if any, subjunctive), no totality over persons or over the town's safety, no belief frame, no monster kind, no count, no season or duration, no cause, no badge word, no pay or upkeep word (DS-DEF-11's cell), no headcount or strength of the force (DS-DEF-5's cell), no charter, watch, mercenary or arcane body (the read excludes them).
- **Provenance ceiling:** at most one citation per unit on a licensed holder and only for an S3 reason (Part B §24); the muster kind resolves `holder: null` on this row and a class word asserts no roll (OW-23), so the recommended citation count for this pool is ZERO; "the muster" is the force's class word, never a record.
- **The echo:** every pool that selects a row of `BEASTS_ROW_POOL` shares ONE echo key, so a mount counted against this pool may be a sibling row; the faces neither rely on nor fear it.
- **The density floor (ADDENDUM 6, §21.4):** the shipped v1 states all three reads in its first sentence; every face of every variant states all three (the floor rises for v2 and v3, which shipped with one and none); a face stating only the key fact with no angle and no second read is an inventory line and is refused (ADDENDUM 7 rule 3). The honest limit (ADDENDUM 7): this pool's shipped richness (the forecast, the frequency, the visibility, the seriousness) was unlicensed invention copied from the engine's chrome, so a faithful rewrite is plainer in those places by design; the richness comes from the three reads placed at each stance and from the counterforce's two-sided shape.

## 9. THE MARKER'S OPEN ROWS FOR THE CHAIR (recorded, not decided)

1. **Fence 1 — "active".** Whether the tier's engine rendering "an active frontier" (`SummaryTab.jsx:37`, `threatAssessment.js:74`) is a lawful spelling of `frontier` under W14, or only the bare tier word. Variant 1's one kept turn depends on it; nothing else does.
2. **Fence 2 — the PROVENANCE capability clause.** Whether "the causal clauses here are capability clauses" in the block's header licenses a face to say the works CAN BE HELD by the force (a capability predicate) against the card's "may NOT a cause / a second fact". This seat licensed the bare conjunction and refused the predicate; the rows marked "a capability the fields do not hold" flip if the chair rules otherwise. Sibling pools of this table ("perimeter but NO force to hold it") carry the same question with the opposite sign.
3. **Fence 3 — the counterforce stance.** No register-card line defines `[counterforce]`; §0.6 gives this seat's definition from the shipped uses and the register card. R-DA-19's live corpus defect (`economicBase: extraction → [counterforce]`) is the same stance awaiting a ruling; a ruling there binds here.
4. **"the arrangements"** as an authored class noun spanning the two BODY reads (the works and the force). This seat licensed it in substance (both rows resolve) while recommending the two bodies be named apart at least once per face; the chair may refuse the generic as a summarising beat (arm Q: a spine surface naming neither a slot nor a band word — this pool's faces name `{settlement}` on vids 1 and 3 and nothing on vid 2, so vid 2's faces must carry their licence in the nouns of the reads, not in "the arrangements").
5. **The key's name is not a licence** ("credible deterrence"; ADDENDUM 8 ruling 2 generalised). Recorded so the drafter does not read the pool key as permission to say the works deter.
6. **The census reads line prints ONE synthetic read at branch grain** (`beastsRowSituation(...) (via BEASTS_ROW_POOL …)`), while the reader's three inputs are `config.monsterThreat`, `forces.walls.present` and `forces.garrison.present || forces.militia.present`. This seat stated all three as the keyed condition under R-v (rulings-DEF2). The card also prints `source: muster` for the force input alone (`fields: {family: "", perimeter: "", force: "muster"}`) — the perimeter's holder (the muster ROLL per the referent table, walls row) is not printed on this card; a citation of the works is therefore not licensed here at all.
7. **The frontier default.** `normalizeMonsterThreat(undefined) === 'frontier'`; the desk refuses the default by requiring the raw value (`measuredMonsterFamily`), so at this key the tier is measured. Recorded because a fixture or import with no raw value renders silence, not this pool; a face never says the tier was measured (no such field).
8. **The live roster against the snapshot.** `perimeter` and `force` read the ruin-filtered LIVE roster (`standingDefenseForces`), while the tab's own `buildThreatAssessment` reads the generation snapshot (`threatAssessment.js:40-43`; entailment C-10): a flattened citadel is `walls ABSENT` to this pool and `hasWalls` to the chrome beside it. A wiring fact for the register car, not a writer's; the faces follow the card's read.
