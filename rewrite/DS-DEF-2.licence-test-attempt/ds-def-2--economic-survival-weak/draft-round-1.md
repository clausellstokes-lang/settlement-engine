# DRAFT ROUND 1 — DS-DEF-2 · `Economic Survival`: `WEAK` (Opus writer seat, 2026-09-11)

Seat: Opus 5 — Fable-unvalidated, WRITER for the REWRITE, working for the Fable chair. Written to the printed licence card, the marker's `skeleton.md`, the register card (with S2 and S3), Part B §1 and §16–§16.2, §18, §20, §21–§23, MOVE-GRAMMAR §1–§3 and §4.4.1–§4.4.3 (with §1.4.1 THE THREAD), CLERK-LAWS §2.4.1 and §2.6.1, ARCH v2 §2.5 and §8.3, and brief ADDENDUM 12's W1–W10 and ADDENDUM 13's W11–W27.

THREE variants in, THREE out — same vids, same order, same one bracketed angle tag each, none added, none removed, none merged. Four faces per variant (the numbered line plus three `- [face]` sub-rows) — TWELVE faces. Zero refusals. The typed lines of the pool (ROLE, READS, RELATION, ATTACH, FORM, MOVE) are untouched and are not repeated here.

---

## THE ROWS, READY TO PASTE UNDER THE POOL'S HEADING

1. `[ledger]` Against a crisis, {settlement} is entered thinly covered, and a real one would strain the cover.
   - `[face]` What the town holds against a crisis at {settlement} is entered thin. A real crisis would strain what the town holds.
   - `[face]` The thin cover entered for {settlement} limits what the town can do in an emergency, and a real crisis would strain that cover.
   - `[face]` The town is entered weak at {settlement} on what it could do about a crisis, and a real one would strain that capacity.
2. `[street]` Thin cover is what {settlement} has to meet a crisis with, and a real crisis would strain that cover.
   - `[face]` What the town can do in an emergency is thin at {settlement}, and a real crisis would strain what the town can do.
   - `[face]` The town's capacity to hold through a crisis is thin at {settlement}, and a real crisis would strain that capacity.
   - `[face]` Against a crisis the town has thin cover at {settlement}. A real one would strain that cover.
3. `[unfolding]` The cover {settlement} keeps against a crisis stands thin, and a real crisis would strain it where it stands.
   - `[face]` What the town could do about a crisis stands thin at {settlement}, and a real crisis would strain that capacity as it stands.
   - `[face]` A real crisis would strain what {settlement} keeps against one, and what the town keeps now stands thin.
   - `[face]` The town stands thinly covered at {settlement} against a crisis. A real crisis would strain that cover as things stand.

--- NOTES

## N.0 THE CARD CLAUSES CITED BELOW BY SHORT NAME

- **[R]** `reads: scoreBand(economicScore) (via ECONOMIC_ROW_POOL in defenseStateProse.js)` with `predicate: === WEAK` — the pool's one read.
- **[C]** `may claim: that the reader scoreBand(economicScore) selects the row WEAK of ECONOMIC_ROW_POOL, as a STANDING fact of the record`.
- **[L]** the band's authored READING, `bandLadders.js:206` (`Weak`): "At or above 20. Thinly covered; a real threat would strain it" — admitted as the content of [C] by ENTAILMENT D-22 (HOLDS): the band word entails **exactly the ladder's reading**. [L] has two halves: **[L1]** thin cover (the condition) and **[L2]** the subjunctive edge (a real one would strain it).
- **[B]** `bag: {band: RESERVED, route: proper, settlement: proper}` · `FILLED at this block's call sites: {settlement}` — the one slot every face carries, exactly once.
- **[W14]** the label at its ENGINE meaning: `Economic Survival` is the economic-RESILIENCE axis (the generator's own noun for it is "crisis logistics", `defenseGenerator.js:283`), so the arm is spoken as the town's capacity against the pressure CLASS and never as coin, revenue or prosperity (L-53; G-19; `rulings-DEF2.txt`).
- **[A]** `audience: player (no mark)` — no face carries `dm-only`; nothing covert is claimed (W26 not engaged).
- **[S]** `source: (none) · standing SOURCE-UNRESOLVED` — **zero citations licensed**: no face names a record holder, uses a record noun, or cites anything (A13; W24). Compliance, not a claim.

Every face makes **exactly three claims** — the arm ([R] + [W14]), the band's condition ([C] + [L1]), and the band's subjunctive edge ([C] + [L2]) — plus the slot ([B]). The `[unfolding]` faces state the same three and realise them in the OPEN grammar (see N.4). **The four faces of each variant are claim-equal to each other** (arm A6 reads across the faces). No face makes a fourth claim.

## N.1 THE PRESSURE WORD AND THE EDGE VERB, FIXED FOR THE WHOLE POOL

- The pressure is the **CLASS** only: `a crisis` and `an emergency`. Licensed by [W14] (the axis's own vocabulary) and by the skeleton's §0.7 fence 2. **"threat" is not used in any face**, deliberately: on this tab it is the `Beasts & Monsters` row's word (`config.monsterThreat`) and the tab's own title, so the ladder's literal "a real threat" would drift across the referent law's rule 3 (same word, same referent). The ladder's `real` is kept on the class word instead: **"a real crisis"** carries [L2] at the ladder's own claim.
- The edge verb is the ladder's own — **`strain`**, subjunctive, never hardened. No face uses `will strain`, `exhaust`, `overwhelm`, `break`, `collapse`, `run out`, `would fail`, `would not hold`, `for long`, `sustained`, or `when one comes`. [L2] · R-DA-07 · MOVE-GRAMMAR §1.4 wall 2 · state never fate.
- The band is spelled four ways across the pool, all at one claim: `thinly covered` (the ladder's own), `thin`, `thin cover`, `weak` (the row key's own word). "shortfall" is used **nowhere**: §0.7 fence 3 — DS-DEF-11's pay-gate pools own that noun on this tab under R-viii′, and a bare one would hand the reader the purse.

## N.2 VARIANT 1 · `[ledger]` — the construction contract (W4) and the licences

**Grammar: V1 PRESENT.** **Stance:** the compiled entry — the office's own formula (`entered`, R-vi/W7) as the surface, the AGGREGATE stated as the record holds it. **The ledger cites nothing and names no record** (W24: the `[ledger]` tag is a STANDPOINT, not a holder; [S] resolves no holder, so the licensed citation count is ZERO). **The contract:** all four faces carry `entered` as the ledger's surface; each face differs in SUBJECT, in the ORDER of the three components, in the band's spelling and in the landing noun.

| face | subject | order | band spelled | close (kind) | claims and their licences |
|---|---|---|---|---|---|
| 1 (the numbered line) | `{settlement}` | arm → band → edge | `thinly covered` | the cover (object) | `Against a crisis` = the arm's pressure class [R]+[W14]; `{settlement}` [B]; `is entered thinly covered` = [C]+[L1], the office's formula R-vi/W7; `a real one would strain the cover` = [C]+[L2] |
| 2 | what the town holds | arm+band, then the edge in its own sentence | `thin` | what the town holds (condition) | `What the town holds against a crisis` [R]+[W14]; `at {settlement}` [B]; `is entered thin` [C]+[L1]; `A real crisis would strain what the town holds` [C]+[L2]. R-i THE THREAD at k = 0: the second sentence carries `what the town holds` forward from the first |
| 3 | the thin cover | band → arm → edge | `thin cover` | that cover (object) | `The thin cover` [C]+[L1]; `entered for {settlement}` R-vi/W7 + [B]; **`limits what the town can do in an emergency`** — the shipped v1's one lawful turn, KEPT VERBATIM under the density law §21.4 [R]+[W14]; `a real crisis would strain that cover` [C]+[L2] |
| 4 | the town | band → arm → edge | `weak` | that capacity (object) | `The town is entered weak` = the row key's own band word [C], the office's formula R-vi/W7; `at {settlement}` [B]; `on what it could do about a crisis` — the shipped v3's lawful noun phrase, kept [R]+[W14]; the pronoun `it` binds to `the town`, the clause's own subject (W1); `a real one would strain that capacity` [C]+[L2] |

**Dropped from the shipped v1, each with the law:** `Chronic` (a duration, a history and a trend — the card's may-NOT "a season"; D-22 "a trend"; G-19 "a history"); `shortfall` bare (§0.7 fence 3; W20/W24 on a SOURCE-UNRESOLVED read); `before the emergency starts` (the MEANING non-move, and a definite future by presupposition); `the pay is irregular` (a pay-gate word on a read that is not the gate — W16, referent rule 5; a SECOND FACT the card refuses; and the engine's chrome, `defenseDisplay.js:214-221`, which bands at 25 while this pool's WEAK spans 20 to 39 — OW-20); `irregular pay shows up as morale` (a cause, a FEELING, a totality over persons by the back door, and a sense verb on an abstraction — R-DA-11); `exactly when it matters` (a maxim and an evaluative close — R-DA-12; §16 item 6). **Nothing was added.**

## N.3 VARIANT 2 · `[street]` — the construction contract (W4) and the licences

**Grammar: V1 PRESENT.** **Stance:** the same one read in the town's own plain idiom — the plain verb (`has`, `keeps`, `could do`, `capacity to hold through`) where the ledger takes the office's formula; **the town is the thing that HAS a capacity and is thin in it.** **The contract:** no face of this variant uses `entered`, `set down` or `carried` (those are vid 1's surface and a standpoint switch under this tag); no face reports what anyone knows, says, remembers or is owed. **The shipped row's one salvageable SHAPE is carried as a frame, not a quote:** the subjunctive capability frame `would have to hold … through`, **re-seated on the town** and given the pressure class as its object (face 3).

| face | subject | order | band spelled | close (kind) | claims and their licences |
|---|---|---|---|---|---|
| 1 (the numbered line) | thin cover | band → arm → edge | `thin cover` | that cover (object) | `Thin cover` [C]+[L1]; `is what {settlement} has to meet a crisis with` = the arm in the town's plainest idiom [R]+[W14]+[B]; `a real crisis would strain that cover` [C]+[L2] |
| 2 | what the town can do | arm → band → edge | `thin` | what the town can do (condition) | `What the town can do in an emergency` — the shipped v1's lawful arm phrase, kept [R]+[W14]; `is thin at {settlement}` [C]+[L1]+[B]; `a real crisis would strain what the town can do` [C]+[L2] |
| 3 | the town's capacity | arm → band → edge | `thin` | that capacity (object) | `The town's capacity to hold through a crisis` = the shipped v2's capability frame RE-SEATED on the town, the skeleton's §0.2 lawful spelling [R]+[W14]; the town is the possessor, so W1 is satisfied by construction; `is thin at {settlement}` [C]+[L1]+[B]; `a real crisis would strain that capacity` [C]+[L2] |
| 4 | the town | arm+band, then the edge in its own sentence | `thin cover` | that cover (object) | `Against a crisis the town has thin cover at {settlement}` [R]+[W14]+[C]+[L1]+[B]; `A real one would strain that cover` [C]+[L2]. The short pair is R-DA-06's short-relief limb; R-i: `cover` is carried forward into the second sentence |

**Dropped from the shipped v2, each with the law:** `The people who would have to hold` (W22 THE PERSON BAR; W20 a force BODY by implication on an AGGREGATE read — D-F25 convicts the sibling STRONG v1 for the same thing; and the band is reachable with `hasAnyDefense` FALSE, so on a real share of WEAK towns the clause names a body the world does not hold — W15, and a same-page contradiction with a `neither walls nor force` Invasion row, C7); `through something` (fault 29's hedged non-statement — the pressure CLASS replaces it); `are already owed` (a debt and a pay word on a read that is not the gate — W16, rule 5; refused even on the gate read by D-31; `already` a history; W23 a fused agent, since no field computes who owes whom); `and they have not forgotten it` (a BELIEF and MEMORY frame — R-DA-13, R-DA-14; a totality over persons, a REFUSED COLUMN; a standpoint; a near-kicker close). **THE SHIPPED ROW STATED THE CARD'S READ NOWHERE**, so every face here is more informative than the line it replaces; nothing was added.

## N.4 VARIANT 3 · `[unfolding]` — the construction contract (W4) and the licences

**Grammar: V6 PRESENT → OPEN**, licensed for this variant by the skeleton's §0.7 fence 5 on R-iv (the `[unfolding]` realises OPEN after the presence) and MOVE-GRAMMAR §1.2 row 10 (a state whose value is UNMET — the ladder's own word for this cover is "thinly covered", which is the cover unmet against the pressure it is measured by). **Stance:** the condition as it stands NOW, not settled and not tested — **stated, never asked** (zero questions, B0.8). **The contract:** the OPEN is realised **last, after the presence**, by a standing marker (`where it stands` · `as it stands` · `now stands` · `as things stand`), and by nothing else; no face of this variant uses the office's formula (vid 1's surface) or the town's `has` idiom (vid 2's).

| face | subject | order | band spelled | OPEN marker (last) | claims and their licences |
|---|---|---|---|---|---|
| 1 (the numbered line) | the cover | arm → band → edge → OPEN | `thin` | `where it stands` | `The cover {settlement} keeps against a crisis` [R]+[W14]+[B]; `stands thin` [C]+[L1]; `a real crisis would strain it` [C]+[L2]; `where it stands` = the OPEN realisation of the same [C], MOVE-GRAMMAR §1.2 row 10 |
| 2 | what the town could do | arm → band → edge → OPEN | `thin` | `as it stands` | **`What the town could do about a crisis`** — the shipped v3's one lawful turn, KEPT VERBATIM under the density law §21.4 [R]+[W14]; `stands thin at {settlement}` [C]+[L1]+[B]; `a real crisis would strain that capacity` [C]+[L2]; `as it stands` the OPEN |
| 3 | a real crisis | edge → arm → band+OPEN | `thin` | `now stands thin` | `A real crisis would strain what {settlement} keeps against one` [C]+[L2]+[R]+[W14]+[B]; `what the town keeps now stands thin` [C]+[L1] with the OPEN carried by `now stands`. `now` is the present marker of R-DA-07, never a duration: no face carries `already`, `still`, `yet`, `so far` or a season |
| 4 | the town | arm+band, then the edge in its own sentence, OPEN last | `thinly covered` | `as things stand` | `The town stands thinly covered at {settlement} against a crisis` [C]+[L1]+[R]+[W14]+[B]; `A real crisis would strain that cover` [C]+[L2]; `as things stand` the OPEN. R-i: `cover` is carried forward from `covered` |

**Dropped from the shipped v3, each with the law:** `The shortfall` bare (§0.7 fence 3 — the purse's word on this tab under R-viii′; W20/W24); `is chronic` (a duration, a history, a trend — D-22, G-19, R-DA-16); `rather than sudden` (a CONTRAST whose rejected alternative names no sibling BAND — the sibling bands are STRONG, ADEQUATE and CRITICAL, and "sudden" is none of them: MOVE-GRAMMAR §1.4 wall 5, R-DA-02); `each season of it` (the card's may-NOT "a season", and `each` makes it a RATE — the card's "a count"); `removes a little more of` (a TREND, refused by D-22 and G-19 — the band says WHERE the number is, never which way it moves; and an inanimate abstraction given an agent's verb, R-DA-11); `when one comes` (a FORECAST by the temporal `when` — R-DA-07, the card's "a future", state never fate). **Nothing was added, and the erosion story is not restored under the tag: R-iv rules the stance is OPEN, never PROCESS** — no face carries `little by little`, `goes on`, `keeps`, `grows`, `worsens`, `erodes` or `removes`.

## N.5 THE POOL-LEVEL WALLS, CHECKED FACE BY FACE (the writer's own receipt)

- **Slots.** Every one of the twelve faces carries `{settlement}` exactly once and no other slot; `{band}` (RESERVED) and `{route}` (a COMPONENT of the score, refused twice over) are named nowhere. ARCH §2.5: each face's `{slot}` set equals its parent's.
- **T-F8 / wall 10.** No SENTENCE of any face opens on the settlement token; the token's nearest approach to an opener is the fifth word. The pool spends its settlement-opener budget at zero.
- **A11 across the variants.** First two words of the three numbered lines: `Against a` · `Thin cover` · `The cover` — all distinct, and the gap the shipped pool left ("Chronic shortfall" beside "The shortfall", one word apart) is widened. Within each variant the four faces open on four distinct first-two-word pairs.
- **No body, no component** (W20; D-22; the census's `objectClasses []`): no granary, stores, grain, food or months; no market, route, port or road; no hospital or medicine; no garrison, muster, militia, watch, soldiers or force; no treasury, purse, chest, books, revenue, tax, coin or money; no hall, seat or council. The only nouns the faces spend are the band's own, the arm's, the pressure class and the town.
- **No pay word, no debt** (W16; rule 5; D-31): none of `pay`, `paid`, `wages`, `owed`, `arrears`, `upkeep`, `the keeping`, `purse`, `roll`.
- **No prosperity and no poverty** (L-53; G-19; `rulings-DEF2.txt`): the arm is food-storage-driven resilience, so no `poor`, `broke`, `cannot afford`, `reserves` or `wealth`, and no component behind the score either.
- **No duration, history, trend or count**; **no cause**; **no comparison and no contrast without a sibling band**; **no person, belief, fused agent or totality**; **no maxim, summarising clause or kicker**; **no citation and no record noun** ([S]; A13; W24; R-vi). Checked by scan over all twelve faces.
- **Hard walls, all twelve:** no em dash, no exclamation, no question, no digit and no percent (the twenty and the forty stay thresholds on the card and never reach prose), no which-clause, no first or second person, no figure, no sense verb on an abstraction, no intent for an inanimate thing, no future indicative.
- **W10.** Zero negated surfaces in the pool (the ceiling is one per variant), so no face can be read as an absence opener.
- **W1 possessor binding.** The only possessives are `the town's capacity` (V2 face 3) and the pronoun `it` in V1 face 4, which binds to `the town`, its own clause's subject. No possessive follows a clause whose subject is the `{defwork}` or any object.
- **W2, W11–W13, W15, W17–W19, W21–W23, W25–W27.** Not reachable by construction: the faces name no body, no material, no garrison, no watch, no external force, no stress record, no creed, no power, no person and no holder. W20 is the live bar on this AGGREGATE read and is obeyed. W3 is not engaged (this pool reads no tier).
- **THE THREAD (§1.4.1; R-i).** This pool is the SPINE and `k 2` says two modifier positions may follow. Every face hands forward a noun an unknown modifier can pick up — `the cover`, `that capacity`, `what the town holds`, `what the town can do` — and every face's landing clause is a standing condition of a civic thing, so each reads as well immediately after nothing as it does immediately before a sibling modifier. The four two-sentence faces (V1 f2, V2 f4, V3 f4) each carry a noun forward into their second sentence, which is the edge, never a summary.
- **The close.** Ten faces land on a condition or on the object the fact names; none closes on a bare pronoun (R-DA-04's pronoun-closer band), none on a maxim, a cause, an outcome or a verdict beyond the band's reading.
- **Rhythm (R-DA-05, R-DA-06).** Face lengths run 15 to 23 words (mean ≈ 20), with three two-sentence faces carrying a short second clause; no semicolon and no colon is spent in the pool; every joint is a comma and `and`.

## N.6 THE DENSITY FLOOR, STATED HONESTLY (§21.4; ADDENDUM 6 and 7)

Two shipped clauses are lawful under the card and are **kept verbatim**, one per face, as the owner's exemplar clauses are meant to be kept: **`limits what the town can do in an emergency`** (V1 face 3) and **`what the town could do about a crisis`** (V3 face 2). Everything else the shipped pool spent its richness on — a chronic history, irregular pay, morale, a maxim, people already owed and not forgetting, a season-by-season erosion, a crisis that will come — was unlicensed invention, most of it bred from the engine's chrome (`threatAssessment.js:159-176`; `defenseDisplay.js:214-221`), so **the rewrite is plainer in exactly those places by design and by law**. The compensating richness is the arm named at its own layer, the thinness stated as a condition of that arm, the pressure set against it as a class, the ladder's subjunctive edge carried in every face, and — on the `[unfolding]` — the matter left standing open. The shipped v2 stated the card's read nowhere at all, so its four replacements are strictly more informative than the line they replace.

## N.7 REFUSALS

**NONE.** All three variants were made lawful under the card, and all twelve faces stand. No variant is banked as a refusal row; the variant count (three) and the face count (four per variant) both rise or hold, never fall (§22 NEVER TRIM).

## N.8 ROWS THE WRITER CARRIES UP TO THE CHAIR (recorded, not decided)

1. **The ladder's edge is in every face.** The skeleton's open row 1 asks the chair whether the card's may-NOT "a future" bars even the ladder's own subjunctive. This draft takes the marker's licence and carries `a real crisis would strain …` in all twelve faces. **If the chair rules the edge out, all twelve faces lose their second half** and the pool falls back to the condition alone; that is a single, mechanical cut, and the draft is written so the cut leaves a lawful sentence in every case.
2. **`threat` is not spelled anywhere**, though the ladder's own reading uses it, because on this tab it is the `Beasts & Monsters` row's word (the skeleton's open row 2). The class word `a real crisis` carries the claim instead. If the chair prefers the ladder's literal frame, the substitution is one word per face.
3. **The OPEN move on a resolved band** (the skeleton's open row 3) is realised in vid 3 by a standing marker only, never by a claim that no crisis has come — deliberately: the WEAK band is reachable on a town carrying a siege or famine penalty, so a face asserting that the crisis has not arrived would contradict the stressors record on a real share of towns. This is the writer's reading and is recorded for the chair beside open row 3.
4. **Sentence-count spread** (the skeleton's open row 6): three of the twelve faces take two sentences, the other nine one. A1 allows one or two and the census reads `grammars 1`, so no pin is breached; recorded so that a refuter does not read a two-sentence face as a breach of a pin that does not exist.
