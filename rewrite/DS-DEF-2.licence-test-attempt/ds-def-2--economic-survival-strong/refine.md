# REFINE — DS-DEF-2 · `Economic Survival: STRONG` (Opus REFINER, seat Opus 5 Fable-unvalidated; 2026-09-11)

Phase 2 refinement (Part B §21 phase 2, §21.1 the ceiling, §21.2 every set, §21.3 one effort, §21.4 the density law) of `draft-round-2.md`, by a different author than the drafter. Written to the printed licence card, `skeleton.md`, the REGISTER CARD (S2, S3), Part B §1 · §16–§16.2 · §18 · §20 · §21–§24, MOVE-GRAMMAR §1–§3 · §1.4.1 · §4.4.1–§4.4.3, CLERK-LAWS §2.4.1 · §2.6.1, ARCH v2 §2.5 · §8.3, ADDENDUM 12's W1–W10 and ADDENDUM 13 A+B's W11–W27, and the two ratified tables' rows for this desk (L-52, L-53, D-22, G-19, D-F25, D-F15, A-25).

STATUS: COMPLETE — three variants, twelve faces, one for one. TEN faces moved; TWO stand byte-identical (V2 face 1, V3 face 1), each with its reason below. No variant added, none removed, none merged; every angle tag, slot set, sentence count and claim set is the draft's exactly.

---

## 1. THE REFINED VARIANT ROWS (paste under the pool's heading; the typed lines are untouched and not repeated)

1. `[ledger]` Entered at {settlement}, holding through a crisis is strong.
   - `[face]` What {settlement} would bear in a crisis is entered as well covered.
   - `[face]` Economic survival at {settlement} is set down as a pressure the town meets well.
   - `[face]` That {settlement} can absorb a crisis well is carried as standing.
2. `[street]` The town could go through a crisis well.
   - `[face]` A crisis is a pressure the town has well covered.
   - `[face]` Strong is how the town carries a crisis.
   - `[face]` Where a crisis would have to be borne, the town is strong.
3. `[counterforce]` What stands at {settlement} against a crisis is the town's carrying. That carrying is strong.
   - `[face]` A crisis is the pressure {settlement} is measured against. The town is well covered for that pressure.
   - `[face]` The town of {settlement} can hold through a crisis. Against that pressure the holding is strong.
   - `[face]` Well covered at {settlement} is the pressure of a crisis. What stands under that pressure is the town's carrying.

--- NOTES

### 2. Per face — what changed, and the law or refinement target behind it

**VARIANT 1 · `[ledger]` · vid 1 · slot set `{settlement}` · one sentence per face · declared construction kept: the office ENTERS the fact, the subject varying across the four (gerund · free relative · label · that-clause), W4.**

1. **Parent row.** `Through a crisis, holding is entered strong at {settlement}.` → **`Entered at {settlement}, holding through a crisis is strong.`** The draft's cure for the participial opener (`shapes.participialOpenerRate`, `proseFingerprint.js:141`, first word only) had left the gerund stripped of its complement, so the face turned on a bare `holding` — which the draft's own fence 7 names as the one word on this desk that can take a holdfast reading (a BODY on an AGGREGATE read, W15/W20). Fronting the office's own formula instead restores `holding through a crisis` whole, so the verbal reading is forced by the words themselves rather than by a displaced adverbial, and the line lands on the band word. Opener is `Entered` (no `-ing`; the measure stays 0.0000), the slot sits third and opens nothing (T-F8, wall 10), the gerund subject is the draft's declared construction (W4), and the claim set is byte-for-byte the same one read. Target: §21.1's read-aloud ear and the sharper licensed fact; law: W15/W20 and W4.
2. `What {settlement} can carry through a crisis is well covered.` → **`What {settlement} would bear in a crisis is entered as well covered.`** Two faults cured. First, a category slip: the ladder's authored reading (`bandLadders.js:203`) predicates *well covered* of the **pressure**, and the draft predicated it of the **load the town can carry** — so the face said something the card does not quite license in the shape it licensed it. The free relative now names what would have to be borne (the pressure), which is exactly what the reading covers. Second, `carry` was the pool's dominant verb (eight occurrences across seven of twelve faces); `bear` is fresh in this pool and in all three sibling band pools. The subjunctive `would` frames a capacity, never an edge and never a forecast (R-DA-07, state never fate). The free-relative subject and the office's formula are kept (W4, R-vi/W7). Target: the sharpest licensed fact; law: D-22 (exactly the ladder's reading) and A5 sibling distance.
3. `Economic survival at {settlement} is set down as a pressure the town carries well.` → **`Economic survival at {settlement} is set down as a pressure the town meets well.`** One word. The label face is the pool's only place the arm's own name appears, and W14 requires it to be unpacked at its ENGINE meaning inside the same clause — `a pressure the town … well` is that unpacking and it stays verbatim. `meets` replaces `carries` to take the third of the pool's four distinct capability verbs (hold · bear · meet · absorb in this variant), which is the A5 target the draft's own §5 reported against itself. Nothing else in the face moves, because every alternative lift available here — an appositive restating the band, a colon, a second clause — is W6's doubled beat or the card's refused second fact. Target: zero tics; law: W14 kept, W6 and the card's `may NOT: a second fact` as the fence on going further.
4. `That {settlement} absorbs a crisis well is carried as standing.` → **`That {settlement} can absorb a crisis well is carried as standing.`** The modal is restored. The shipped line's one lawful turn is `can absorb a … crisis` (skeleton §V1(6)), and §21.4 makes that turn a FLOOR the rewrite may not say more plainly with no law behind the change; the draft's bare present indicative `absorbs` also reads as a habitual — what the town *does* to crises, which edges toward a frequency the record does not hold — where `can absorb` is the standing capacity the card actually licenses. The that-clause subject and the office's `carried as standing` are unchanged. Target: the density floor; law: §21.4 and R-DA-07 (a capacity, never a fate).

**VARIANT 2 · `[street]` · vid 2 · slot set `{}` — no `{settlement}` in any face (ARCH §2.5) · one sentence per face · declared construction kept: the town's plain idiom, the subject varying (town · pressure · band fronted on a manner clause · fronted relative), W4.**

1. **Parent row — UNCHANGED: `The town could go through a crisis well.`** This is the shipped clause `The town could go through` standing verbatim as one of the four, which §21.4 expressly permits and the skeleton names as this variant's density frame; the only unlicensed words the shipped line carried after it (`a bad season`, `with its arrangements intact`, the persons who `know it`) were already dropped by the draft. Every lift tried against it either added a gradation the card refuses (`with room to spare` — D-22 forbids a fifth gradation; `margin` is the ADEQUATE pool's own noun), doubled the beat (`and go through it well`), or hedged the band downward (`well enough`). Kept deliberately, not by omission.
2. `A crisis is a pressure the town is well covered for.` → **`A crisis is a pressure the town has well covered.`** The draft's face closed on a stranded preposition, which is the weakest landing in the pool against R-DA-04 (land on the civic thing, vary the kind of close). `has well covered` is the street's own resultative idiom for the same standing state, keeps the ladder's word `covered` as the band's carrier, adds no claim and no tense (it is a standing state, not a perfect of some act), and closes on the band. The pressure-as-subject construction is kept (W4). Target: the read-aloud ear and the close; law: R-DA-04.
3. **UNCHANGED: `Strong is how the town carries a crisis.`** The band word fronted on a manner clause is this face's declared construction (W4) and the pool's one OBJECT close (`a crisis`), which R-DA-04's close-kind spread needs; it also carries the pool's one remaining `carries`, which is now the only occurrence in this variant. Every substitute verb tried was either unlawful in sense (`takes a crisis` reads as a reaction, the FEELING non-move), a collision with a sibling band pool's own wording (`meets`), or a counterforce verb under a street tag (`stands`). Kept deliberately.
4. `Where a crisis would have to be carried, the town is strong.` → **`Where a crisis would have to be borne, the town is strong.`** One word, for the pool-wide verb spread: this removes the fourth of the draft's eight `carry` forms and leaves the variant with exactly one. `borne` is plain modern English, not costume, and the antique air the register card permits lives in the noun and never in the syntax. The fronted relative that SCOPES the band to this arm alone — the construction D-22's refused column and W2 both require — is untouched, as is the subjunctive capacity frame. Target: zero tics; law: W4 kept, R-DA-18 (plain is not flat, the lift never in the syntax).

**VARIANT 3 · `[counterforce]` · vid 3 · slot set `{settlement}` · TWO sentences per face (A11's spread against vids 1 and 2) · declared construction kept: the pressure and the covering capacity as two standing facts of ONE read, the second sentence a second CONSTRUCTION carrying a noun forward (R-i / §1.4.1), never a gloss and never a maxim.**

1. **Parent row — UNCHANGED: `What stands at {settlement} against a crisis is the town's carrying. That carrying is strong.`** The cleft names the counterforce the card actually licenses (the band itself, skeleton §V3(5)), the possessive is seated on the town (W1), the second sentence carries `carrying` forward — the deliberate noun echo §1.4.1 expressly makes lawful at k = 0 — and lands the band in the pool's one short relief sentence (R-DA-06's `< 8` floor). Every lift tried at the second sentence either imported a ledger formula under a counterforce tag (`is entered strong` — the skeleton's own regression list), collided with the CRITICAL pool's verb (`reads strong`), or closed on a pronoun (`and it is strong`). Kept deliberately.
2. `A crisis is the pressure {settlement} is measured against. The town carries that pressure well.` → **`A crisis is the pressure {settlement} is measured against. The town is well covered for that pressure.`** The second sentence is rebuilt on the ladder's authored reading instead of a fifth `carries`, so the two sentences now differ in construction as well as in subject (the record's operation, then the band as a state), which is what this variant's declared grammar asks of the pair. `is measured against` remains a VERB and not a record noun, so no holder is cited on a SOURCE-UNRESOLVED card (A13, W24). The thread is unchanged: `pressure` handed forward and taken up as the sentence's own close, an object close where the draft closed on an adverb. Target: sibling distance and the close; law: D-22, W24, R-i.
3. `The town of {settlement} can hold through a crisis. Against that pressure the holding is strong.` → **UNCHANGED.** W5's naming form carries the subject without the slot opening the sentence; `can hold through a crisis` is the capability verb on the pressure class; `the holding` is anaphoric to `can hold` in the sentence before, so the holdfast reading fence 7 guards against is closed inside the face itself; both `pressure` and `holding` are handed forward. Attempts to vary its close either broke the ladder's predication (`the holding is well covered` — the pressure is what is covered, not the holding), imported the ledger's formula under this tag, or closed on `one`, a pronoun R-DA-04 refuses. Kept deliberately; the face is at its construction's ceiling.
4. `Well covered at {settlement} is the pressure of a crisis. The town's carrying is what stands under that pressure.` → **`Well covered at {settlement} is the pressure of a crisis. What stands under that pressure is the town's carrying.`** The second sentence is inverted so that the face ends on the civic thing the fact names — the town's carrying — instead of on a demonstrative phrase repeating the noun it had just carried forward. This is R-DA-04 applied at the grain it is written (land on the civic noun; vary the kind of close): the variant's four closes are now `strong` · `pressure` · `strong` · `carrying`, where the draft's were `strong` · `well` · `strong` · `pressure`. The fronted ladder reading in sentence one — this face's declared construction (W4) — is untouched, the thread still runs `pressure` → `pressure`, and no word is added or dropped from the claim. Target: the close and the read-aloud ear; law: R-DA-04, W4 kept.

### 3. What did NOT change, and why the walls still hold

- **Inventory (NEVER TRIM, §22):** three variants, three angle tags (`[ledger]` · `[street]` · `[counterforce]`, one bracketed tag each, no `[plain]` on a spine row), twelve faces, the same vids in the same order. One-for-one substitution throughout (§22 e).
- **Slot sets, the parents' exactly** (ARCH §2.5): vid 1 `{settlement}` ×4 · vid 2 `{}` ×4 · vid 3 `{settlement}` ×4. `{band}` (RESERVED) and `{route}` (a component of the score, refused twice over) are named by no face.
- **Sentence counts pinned** (A11): one · one · two, unchanged face for face.
- **Claim equality** (arm A6, read across the faces): every one of the twelve asserts exactly `(PRESENT, scoreBand(scores.economic), STRONG)` and nothing else. No claim is added anywhere; no licensed claim the draft carried is dropped.
- **No BODY, no COMPONENT** (W20, D-22, D-F25): no granary, stores, grain, food, months; no market, route, port, road, sea; no hospital, healer, medicine; no garrison, muster, soldiers, militia, watch, force; no treasury, purse, chest, books, revenue, tax, coin, money; no hall, seat, council. W11, W12, W13 and W15 stay unreachable by construction, and the parent-row cure was chosen partly to keep them so.
- **No PAY WORD** (W16, referent rule 5): no pay, paid, wages, upkeep, keeping, purse. Nothing of DS-DEF-11's cell or DS-DEF-6's chrome is spoken.
- **No particular crisis, no duration, no event, no live pressure** (the card's `a season` and `a future`; W17; W18; R-DA-16): `a crisis` and `the pressure` only, always indefinite or anaphoric; no `sustained`, `prolonged`, `while they last`, `so far`, `yet`; no perfect of an event.
- **No cause, no comparison, no self-sufficiency** (the card; D-22; G-19): no `because`, `out of`, `the reason`; no `more than`; no `its own`.
- **No person, no belief, no fused agent** (W22, W23, R-DA-13/14): the town HAS, CAN, BEARS and MEETS; it never knows, decides, believes or is told. No ROLE word, because this read records none.
- **No maxim, no summarising second sentence** (R-DA-12; R-DA-03; §16's machine signatures): each of vid 3's second sentences is a second CONSTRUCTION of the one read that hands a noun forward, never a gloss on the sentence before (W6).
- **ZERO citations, zero record nouns** (A13; W24; §24): `entered`, `set down`, `carried as standing` and `is measured against` are the office's own formula and verbs; no `books`, `rolls`, `record`, `accounts`, `reckoning`, `the entry` appears. The `[ledger]` tag is a standpoint and licenses no record noun (W24).
- **Hard walls:** no em dash, no exclamation, no digit, no which-clause, no question, no colon, no `I`/`you`, no figure, no sense verb on an abstraction, no inanimate intent, no forecast, no totality over persons or over the town (W2).
- **THE THREAD** (§1.4.1; the census reads `k 2`): this pool is the SPINE and opens the passage. Every face hands forward a noun a modifier can pick up — `crisis`, `pressure`, `carrying`, `holding`, `standing` — and no face closes on a pronoun. Inside vid 3 the second sentence carries a noun forward in all four faces (R-i at k = 0).

### 4. The measures this round moves, reported as distances (§16.2's ENTRY grain; bands, not walls)

- **`shapes.participialOpenerRate` 0.0000 on all twelve** (first words: Entered · What · Economic · That | The · A · Strong · Where | What · A · The · Well; second sentences: That · The · Against · What). The draft's cured measure is held, and the cure's own cost — a gerund stripped of its complement — is now paid off rather than carried.
- **Capability-verb spread, the round's largest gain:** the `carry` family falls from **eight occurrences over seven faces to five over four** (V1 loses both, V2 loses one, V3 keeps two of which one is §1.4.1's licensed thread echo). The pool's verbs are now hold · bear · meet · absorb · go through · cover · carry, and V1 alone carries four distinct ones.
- **Close-kind spread** (R-DA-04): the twelve closes are `strong` · `covered` · `well` · `standing` | `well` · `covered` · `crisis` · `strong` | `strong` · `pressure` · `strong` · `carrying` — four distinct inside every variant except vid 3, which holds two `strong` closes in two different constructions (reported, not hidden: the card licenses exactly two band spellings, so a four-way close spread inside a two-sentence variant cannot be bought without an unlicensed word). The draft carried an adverb close and a stranded preposition; both are gone. `closers.pronounRate` 0.0000 · `closers.abstractNounRate` 0.0000 (`standing`, `carrying`, `pressure` are not members of the `-ness/-tion/-ity/-ment/-ance/-ence/-ship/-hood/-dom` set).
- **Word counts, in pool order:** 9 · 12 · 14 · 11 | 8 · 10 · 8 · 12 | 15 · 17 · 16 · 19. Mean 12.6; within-pool sd ≈ **3.7**, up from the draft's 3.49 and still below R-DA-05's within-pool floor of 4.0 — reported as a distance, not hidden, and still a consequence of the card (a one-read AGGREGATE pool with no body, no component, no citation and no second fact has a narrow honest length range; padding a face to buy the measure would buy it with an unlicensed word).
- **Other rates unchanged at zero:** `thereIsOpenerRate` · `whichTailRate` · `emDashRate` · `questionRate` · `exclamationRate` · `parenthesisRate` · `dialogueShare` · `antithesisRate` · `triadRate` · `doubledAdjectiveRate` · `adverbsPerSentence` (no `-ly` form; `well` and `strong` are not members).
- **Settlement-token openers 0 of 12** (T-F8, wall 10): the parent row's slot moves from last to third and still opens nothing. **Citations 0 of 12** (§24's ceiling on a SOURCE-UNRESOLVED pool is not approached).
- **Two fronted-complement faces remain** (`Strong is how …` and `Well covered at {settlement} is …`), one per variant, each its face's declared construction under W4. Reported as a shape proximity, not cured: moving either onto another construction is the regression W4 names, and only one face of this pool ever renders for a town.
- **Level-1 grammar:** V1 PRESENT throughout — the only member whose licensing field this card prints. The variation carried is the sentence count (one · one · two) and the ORDER inside the read (pressure before capacity, or capacity before pressure), as the draft declared per variant. The pool stands one short of §2.1's `min(k, 8)` distinct grammars at k = 3; a card consequence, reported in all three rounds, not a writing choice.
- **The density floor, honoured on all three shipped lawful turns** (§21.4): `can absorb a … crisis` is now carried with its modal intact at vid 1 face 4 (the round restored it); `The town could go through` stands verbatim as vid 2's parent row; `a crisis` runs through every face of vid 3. **No face in this round was made plainer with no law behind the change** — each of the ten moves names a law (W15/W20, D-22, W14, §21.4, R-DA-04, R-DA-07, R-DA-18, A5, R-i) or a §21.1 target, and the two unmoved faces are recorded as kept deliberately with the alternatives that were tried and refused.

### 5. REFUSALS

**None.** All twelve faces are lawful at four faces a variant; no face was banked and no refusal row is owed.

### 6. Recorded vetoably for the chair (nothing new is decided here)

1. **The parent row's fronted past participle.** `Entered at {settlement}, holding through a crisis is strong.` opens on a non-`-ing` participle, which no measure counts and which is the office's own formula, not costume; if the chair reads a fronted participle as syntax-borne lift (R-DA-18's fence), the face falls back to the draft's `Through a crisis, holding is entered strong at {settlement}.` and nothing else in the pool moves.
2. **`meets` at vid 1 face 3** shares a root with the ADEQUATE pool's `A crisis would be met at {settlement}` — a one-word cross-band proximity, on a sibling pool only one of which renders for any town. Recorded beside the two proximities the draft already reported (`Through a crisis,` and the fronted band word), all three inherited or unchanged in kind.
3. **W4 at the face grain.** Every face keeps the subject construction the draft declared for it; no face was moved onto a sibling's. The draft's open row 8 (whether W4 binds at the variant or the face grain) is therefore not reached by this round either way.
4. The marker's open rows 1–4 and 9 (the arm's plain-word spellings; `a crisis` as the pressure class; the capability verb; the pay words refused; `{route}` unnamed) are relied on exactly as the draft relied on them, and are unchanged by this round.
