# DRAFT ROUND 1 — DS-DEF-2 · `Disasters & Famine: NO reserves, hospital present`

Seat: Opus WRITER (Opus 5, Fable-unvalidated), for the Fable chair. This file is the only thing this
seat wrote, anywhere. `laneRW-DEF2` was READ only; the only thing executed there was the read-only
licence-card script the brief names. Nothing in any dock was edited, committed or tested.

Written against: the printed licence card; the marker's `skeleton.md` (read whole); the annex rows at
`RECEIPT_POOLS_DOSSIER_STATE.md:2715-2718` and the block header at `:2568-2593`; REGISTER-CARD.md with
amendments S2 and S3; RULES-V2-PART-B §1 (R-DA-02/03/04/05/07/08/10/11/12/17/18/20/21/22), §16–§16.2,
§18, §20, §21–§21.4, §22, §23, §24; MOVE-GRAMMAR §1–§3, §1.4.1 (the thread), §4.4.1–§4.4.3;
CLERK-LAWS §2.4.1 and §2.6.1; ARCH v2 §2.5 (the annex grammar) and §8.3 (the licence card); brief
ADDENDUM 13 parts A and B (W11–W27); `rewrite/rulings-DEF2.txt` (R-i · R-ii · R-iv · R-vi · R-viii′);
the two ratified tables' rows that name this pool's reads (D-12, D-13, L-20, A-10, A-15, S-8;
REFERENT-TABLE rows 62, 63, 64 and the AGGREGATE row).

**THREE variants rewritten in place, in order, each under its own vid and its own ONE angle tag.
TWELVE faces (the numbered line plus three `[face]` sub-rows per variant). ZERO refusals.**

---

## THE ROWS (the complete replacement for the pool's variant rows; paste under the pool's bold heading)

1. `[ledger]` Those tending the sick are entered at {settlement}, and against hunger no granary is entered.
   - `[face]` Carried at {settlement} are those tending the sick. The town carries no granary against hunger.
   - `[face]` Set down at {settlement} are those tending the sick, and against hunger no granary is set down.
   - `[face]` The town of {settlement} has those tending the sick entered, and against hunger no granary is entered.
2. `[street]` The town has those tending the sick. Against hunger the town keeps no granary.
   - `[face]` Those tending the sick are in the town, and against hunger it has no granary.
   - `[face]` What the town has are those tending the sick, and against hunger no granary.
   - `[face]` In the town are those tending the sick, and against hunger no granary at all.
3. `[unfolding]` Those tending the sick stand at {settlement}, and against hunger no granary stands.
   - `[face]` Standing at {settlement} are those tending the sick. Against hunger the town stands with no granary.
   - `[face]` What stands at {settlement} are those tending the sick: against hunger, no granary stands.
   - `[face]` The town of {settlement} has those tending the sick standing and no granary standing against hunger.

--- NOTES

### 0. THE FOUR CLAIMS EVERY ONE OF THE TWELVE FACES CARRIES, AND THE CLAUSE THAT LICENSES EACH

The pool's claim set is IDENTICAL in all twelve faces (arm A6 reads across the faces): the key is the
CONJUNCTION `hasGranary === false && hasHospital === true`, so a face naming one half prints a sibling
cell's fact (skeleton §261), and every face therefore states both halves in the fixed order.

| the claim, as every face makes it | the surface used | the card clause / law that licenses it |
|---|---|---|
| C1 — a CARE-class row stands on this town's record | **those tending the sick** (this spelling and no other, in all twelve faces) | the card's `reads` / `predicate`: `disasterRowSituation(granary, hospital, church) === no reserves, hospital` is reached only on `hospital === true`; `may claim:` the row is selected **as a STANDING fact of the record**. The spelling is fixed by the alias bar (A-15; W15; `rulings-DEF2`), which is the only always-safe form of `hasHospital` — true of a hospital, a monastery and a healer alike. Layer BODY (REFERENT-TABLE row 63) |
| C2 — NO GRANARY stands on this town's record | **no granary** (the body word negated at the roster grain, never a stock word) | the same `reads` / `predicate`: the row is reached only on `granary === false`; the LACK move (MOVE-GRAMMAR §1.2 row 11 class (a), licensed by a `none-exists` field, its own examples being body words negated) and R-DA-08's declared gap, written **in the record, never in the world**. Layer: the negated BODY — the marker's fence 1 ruling (skeleton §98; open row 1) |
| C3 — the conjunction itself, presence FIRST and absence SECOND | the fixed order of every face | the card's `predicate` is the conjunction; the order is a WALL, not a band — an ABSENCE never opens (MOVE-GRAMMAR §1.4 wall 3; R-DA-08; the register card's absence clause). The one structural thing the shipped rows get right, and it is kept |
| C4 — hunger is the pressure a granary answers to | **against hunger** (bare, generic, in all twelve faces) | the ROW'S LABEL at its ENGINE meaning (W14) — "Disasters & Famine" is the fifth readiness row's label (`threatAssessment.js:190`), and the block's own subtitle is *the five pressures, and what the town actually has standing against each* (annex `:2569`) — together with D-12's ENTAILED purpose of the recorded noun (`granary`: "grain is stored; **it buffers a harvest**"). Named as a CLASS, never as an event, a season, a harvest, a year or an outcome (W17, W18, R-DA-16; the card's may-NOT "a season") |
| the `{settlement}` token, in variants 1 and 3 only, never as the opener | `at {settlement}` · `The town of {settlement}` | the card's `bag: {settlement: proper}`, **FILLED at this block's call sites**; the parent slot sets are kept exactly (ARCH §2.5: a face whose `{slot}` set differs from its parent's is REFUSED) — vid 1 `{settlement}`, **vid 2 `{}`**, vid 3 `{settlement}`. `{band}` is RESERVED and `{route}` unfilled: no face names either and no face speaks a band word |

**SICKNESS IS NOT NAMED AS A SEPARATE CLAUSE IN ANY FACE, AND THAT IS DELIBERATE (W6).** The draft's
first pass carried `against sickness` beside `those tending the sick` in every face; that is the
DOUBLED BEAT — a clause restating the read it follows — because the body's own safe spelling already
names its pressure. The pressure class is therefore spoken once per face, on the half where it is not
a restatement. The row's label carries the other half beside every face.

**CITATION COUNT: ZERO, in all twelve faces.** The card prints `source: (none) · standing
SOURCE-UNRESOLVED` and "NO citation is licensed: a face naming a record holder here is refused by arm
A13". No face carries a record NOUN either (W24: on a read that resolves no holder, no record word at
all) — `entered`, `carried`, `set down`, `stands`, `has`, `keeps` are VERBS; the office's own formula
is the `[ledger]`'s lawful surface (R-vi, W7) and the tag is a STANDPOINT that licenses no noun. The
earlier draft's `on the record` was cut for this reason alone.

### 1. VARIANT 1 · `[ledger]` — the construction contract, and each face's licence

**The angle's stance realised (skeleton §139; R-vi; W7; W24).** The clerk ENTERS the two conditions as
the compiled record holds them, in the office's own formula (`entered` · `carried` · `set down`), both
as STANDING facts with no history behind them (the block's PROVENANCE line), citing nothing and
measuring nothing. **The variant's declared construction (W4): the RECORD'S ACT** — the entry formula
in every face, which is what tells vid 1 apart from vid 2's plain having and vid 3's present standing.
**Level-1 grammar: V3 (PRESENT → LACK)**, the state key plus the `none-exists` half.

| face | words | construction (the four faces differ in SUBJECT, JOINT, VOICE and LANDING, not in vocabulary alone) | the licence, beyond C1–C4 above |
|---|---|---|---|
| 1 (the numbered line) | 15 | care-body SUBJECT · passive · `and` JOINT · lands on the absence | `at {settlement}` = the bag's filled proper slot, riding INSIDE the sentence (T-F8; wall 10: the shipped opener is CURED here, not kept) |
| face 2 | 15 | fronted participial INVERSION · **two sentences** · the town as the second subject · lands on the pressure | the second sentence carries **the town** forward from `{settlement}` — R-i (the thread at k = 0) and MOVE-GRAMMAR §1.4.1; the deliberate noun echo is lawful there. `carries` is the office's formula recurring, which the register card licenses in terms (the WORD may recur; the FACT must not) |
| face 3 | 17 | fronted participial INVERSION on a different formula verb · one sentence · gapped second clause · lands on `set down` | as face 1; the formula verb varies the rhythm, never the claim |
| face 4 | 17 | **the naming form** as SUBJECT · active · lands on `entered` | `The town of {settlement}` is the naming form W5 names verbatim; it states no band and needs no generic article to carry one |

### 2. VARIANT 2 · `[street]` — the construction contract, and each face's licence

**The angle's stance realised (skeleton §187; §192).** The town's own plain talk about its condition:
the everyday verb where the ledger would use the office's formula, the town as the subject that HAS the
one and HAS NOT the other, and the matter left standing open by silence. **NO field here holds anything
the town knows, says, believes, feels, values, minds or is comforted by**, so no face reports any of it
(W22, W23, R-DA-13/14, the FEELING non-move). **The variant's declared construction (W4): THE TOWN'S
HAVING** (`has` · `keeps`), never the entry formula and never the present standing.

| face | words | construction | the licence, beyond C1–C4 above |
|---|---|---|---|
| 1 (the numbered line) | 14 | the town as SUBJECT · **two sentences** · the second opens on the pressure · lands on the granary | **`The town` is the pool's one genuinely reusable shipped asset** (skeleton §190) — the only clause-initial material in the three shipped rows that survives, and it is kept verbatim as the street's lawful subject and opener. The second sentence carries **the town** forward by noun echo (R-i; §1.4.1) |
| face 2 | 15 | care-body SUBJECT · locative predicate · `and` JOINT · the pronoun binds to the town | `it` binds to `the town`, the sentence's own subject-referent — W1 is satisfied because the town is seated as the possessor and no object intervenes |
| face 3 | 14 | **pseudo-cleft** SUBJECT · gapped second clause · lands on the granary | the cleft is word order, which the register card says may be spent freely; it adds no claim and no emphasis the fields do not hold |
| face 4 | 15 | locative INVERSION · `and` JOINT · lands on `at all` | as face 2 |

**NO `{settlement}` IN ANY FACE OF THIS VARIANT.** The parent's slot set is `{}` (skeleton §94; ARCH
§2.5), so the town is "the town" and never its name, in all four faces.

### 3. VARIANT 3 · `[unfolding]` — the construction contract, and each face's licence

**The angle's stance realised (skeleton §239; R-iv).** The pair set out AS IT NOW STANDS — a care row
standing, no granary standing — with **one matter left standing open in the present by what the face
declines to say**: the stock the town actually holds, which no field on this card reads and which no
face guesses at. Under skeleton §234 the OPEN QUESTION move is **not licensed as a sentence** here (no
typed field on this card carries an unresolved, contested, pending or unmet value), so the standing-open
matter is realised exactly as the register card permits — *stated, never asked* — by the face stopping
where the card stops. The shipped row's trajectory (a past that explains, a future nobody is arresting)
is HISTORY plus FORECAST and is dropped whole. **The variant's declared construction (W4): THE PRESENT
STANDING** (`stand` · `stands` · `standing`), which is the paired frame the shipped row reached for,
kept as a SHAPE with both nouns supplied and both relative clauses struck (skeleton §242).

| face | words | construction | the licence, beyond C1–C4 above |
|---|---|---|---|
| 1 (the numbered line) | 13 | care-body SUBJECT · `and` JOINT · verb echo · lands on `stands` | the shortest face in the pool; the short line exists (R-DA-05; NL-2's rhythm-follows-load) |
| face 2 | 16 | participial INVERSION · **two sentences** · the town as the second subject · lands on the granary | the second sentence carries **the town** forward from `{settlement}` (R-i; §1.4.1) |
| face 3 | 14 | **pseudo-cleft** SUBJECT · COLON joint · lands on `stands` | the colon is the chosen joint (R-DA-06: the joint is chosen, one per variant); punctuation is spendable, the claim is not |
| face 4 | 16 | the naming form as SUBJECT · active with a participial complement · gapped · lands on the pressure | `The town of {settlement}` per W5, as in vid 1 face 4 — the two sit in different variants and the draw shows one variant per seed |

### 4. WHAT THE SHIPPED ROWS CLAIMED THAT IS DROPPED, WITH THE LAW (never ADDED to; never kept as a floor)

Dropping these is the rewrite's purpose, and the shipped breach is the corpus's known state.

| shipped clause (vid) | dropped because |
|---|---|
| `can treat and contain an outbreak` (v1) | D-13's entailment is CONDITIONAL on a `hospital`-NAMED row, and at this key that condition **cannot resolve on a native roster** (`Town granary`/`City granaries` are `required: true`, so `hasGranary === false` is village-and-below, where the only `hasHospital` row is `Healer (divine, 1st level)`): ADDENDUM 13 A item 6 fails a conditional used without its condition resolved. D-13's NOT-ENTAILED column carries "that it can contain the outbreak" almost verbatim. The impersonal "can" also reaches a PERSON as the load-bearing referent (W22) |
| `keeps no food against a bad harvest` (v1) | **the pool's ratified shipped breach, and factually wrong besides.** The read is the BUILDING flag (stem `granar`); "food" is the `store` class — the STOCK — and A-10 splits the two at 8a-8 ("say the building or what is in it, never both in one word"). L-20 and the engine: a granary-less settlement holds 1.0 months to `foodGenerator.js:161-165` and 2.0 to `foodStockpile.js:190-193`, and `isolationSupport.js:47` counts a root cellar as a reserve. "a bad harvest" is a season and an event (the card's may-NOT "a season"; W17) |
| `a crop failure here becomes hardship the same season it happens` (v1) | a second FACT carried as a joint (R-DA-03; wall 6; S2's clause seat is unavailable — a spine takes no relation); an EVENT and a CAUSE (the card's may-NOT); a FORECAST in a general-truth dress (the FORECAST non-move; STATE never FATE); a season and a rate |
| `is better prepared for the sickness than for the hunger` (v2) | a GRADE the card does not carry — the row's own badge is `scores.disaster`, a SEPARATE read that prints beside the face and may say the opposite (W20: an AGGREGATE names no body; skeleton §0.3) — and a CONTRAST whose rejected alternative is the other half of the SAME key, fronted as the subject, which wall 5 bars in terms |
| `, which is an unusual way round` (v2) | a hard wall: no which-clause (R-DA-03; wall 6). "Unusual" is also a claim about OTHER TOWNS that no field on this card holds, and a MEANING move |
| `and does not comfort anyone` (v2) | **a REFUSED COLUMN** — a totality over persons, refused *always* by the card's last line — plus a FEELING (the FEELING non-move; R-DA-14; NL-5), a person as a load-bearing referent (W22) and a kicker |
| `arranged against the sickness it has seen and not against the hunger it has not` (v3) | HISTORY on a standing-configuration read: R-DST-B/A6 licenses a STRUCTURAL clause and never a historical one, and the block's own PROVENANCE line refuses it by name ("institution presence is a STANDING fact with no recorded history … never historical ones (walls built after a siege)"). The negative half also writes an absence in the WORLD rather than in the record (R-DA-08), and the frame asserts a CAUSE for both halves |
| `and nothing in hand is correcting the imbalance` (v3) | a totality over a set the record never enumerates; a FORECAST in continuous dress (STATE never FATE); and "the imbalance" is the same GRADE as v2's in another word |
| the `{settlement}` OPENER (v1 and v3) | T-F8 (ARCH §2.5's face row) and wall 10, which allows ONE settlement-opener per pool and is breached TWICE today. Cured structurally in all twelve faces: the token rides inside the sentence |
| the definite-particular pressures (`the sickness`, `the hunger`, v2 and v3) | the definite article slides a CLASS into an EVENT (W17, W18; skeleton fence 3). The bare class is the cure and is what every face carries |

### 5. THE DENSITY FLOOR, READ AGAINST THE SHIPPED SENTENCE AS WELL AS THE DRAFT (Part B §21.4)

**Not one clause of the three shipped sentences is lawful whole**; the single surviving piece of shipped
material in the pool is `The town` (v2's subject), and it is kept verbatim as vid 2's opener, exactly as
the density law requires of a lawful shipped turn. The one shipped STRUCTURE that is lawful — the
presence first and the absence second, in one unit — is kept in all twelve faces, and vid 3's paired
frame is kept as a shape.

Everything else that made the shipped rows look rich is six inventions in v1, three in v2 and four in
v3, two of which the engine actively contradicts. The rewrite is therefore **plainer in exactly those
places, by law, and more knowledgeable in none** — and it is CORRECT where two of the three shipped
rows are factually wrong (skeleton §286, open row 11). Part B §21.4's regression is *a lawful line made
plainer with no law behind the change*; every change here names its law in §4 above. The whole lawful
purse for this pool — the care body in its one safe spelling, the storehouse body negated at the roster
grain, the pressure class bare, the angle's own surface, the fixed order, the varied landing noun, and
the matter left standing open by silence — **is spent in full in every one of the twelve faces**.

### 6. THE POOL-LEVEL WALLS, CHECKED FACE BY FACE

- **Both halves in every face** — no face prints a sibling cell's fact by naming one half (skeleton §261). ✓
- **Presence first, absence second, in all twelve** (wall 3; R-DA-08). ✓
- **One negated surface per face**, and it is the licensed LACK, never a negated PRESENT read (R-ii/W10). ✓
- **No stock word anywhere**: no food, reserves, stores, stock, larder, harvest, put by, held back, buffer, months, enough, empty, bare. ✓
- **No capability on the care body**: no treat, contain, containment, quarantine, recovery, copes, equipped, prepared, answer. ✓
- **No person, no totality over persons**: the plural class phrase is what keeps the `Healer` row's single person off the page (W22; the card's REFUSED COLUMNS). ✓
- **No faith, deity, parish, clergy or spell** — the `church` argument is not consulted on this branch, and `Monastery or friary` would set both flags from one row (W19; rule 3). ✓
- **No event, season, duration, history, forecast, trajectory, cause, counterfactual or meaning gloss.** ✓
- **No grade, no band word, no comparison of the key's own two halves**, and no reading of the `scores.disaster` badge printed beside the face (W20). ✓
- **No second civic object**: one care body and one storehouse absence; none of the other four readiness rows on the same tab is named (C7; W2; A1/A11), and no sibling pool's material is imported. ✓
- **No citation, no record noun** (A13; W24; SOURCE-UNRESOLVED). ✓
- **Hard walls**: no em dash, no exclamation, no digit, no percent, no which-clause, no question, no "I" or "you", no figure, no sense verb on an abstraction, no intent for the town or a building, no expletive opener (R-DA-07), no participial gloss tail, no maxim, no kicker, no hook, no tricolon, no settlement-token opener. ✓
- **Slot sets exactly the parents'**: vid 1 `{settlement}` · vid 2 `{}` · vid 3 `{settlement}` (ARCH §2.5). ✓
- **Openers**: distinct within every variant; the fixed body phrase `Those tending the sick` opens one face in each of the three variants (three of twelve) — unavoidable while the presence must lead and its spelling is fixed, and a reader meets one variant per seed. The settlement token opens no face; `At {settlement}` was tried and cut.
- **Sentence counts**: one variant of the three runs TWO sentences at its numbered line (vid 2), and one face of each of vids 1 and 3 runs two — so the pool is no longer uniform in segment count (R-DA-05; A11). Every second sentence carries a noun forward from its first (R-i; §1.4.1).
- **Word counts** 13–17, sd ≈ 1.3 across the twelve; the shortest face is vid 3's numbered line at 13.
- **THE THREAD to the two unknown modifiers (`k 2`).** Every face hands forward **the town** (as `{settlement}`, `The town of {settlement}` or `the town`) and **the pressure class**. Neither a `store`-class nor a `care`-class modifier can attach (T-F12 refuses a modifier whose key names a class this key names, and this key names both — ARCH §8.3's worked card holds the `stores: short` modifier off the two NO-reserves cells), so no face leans on a stock or care continuation.

### 7. REFUSALS

**NONE.** All three variants are written, all twelve faces are lawful under the card, the two ratified
tables and the writer bars W11–W27 as this seat reads them. No variant was banked, none was trimmed,
none was merged, no vid moved, and no face was withheld.

### 8. THE JUDGMENT CALLS THIS SEAT MADE, RECORDED VETOABLY (each is a marker OPEN ROW the chair has yet to rule)

1. **The absence's noun (marker open row 1).** Every face names the granary in the negative. This
   follows the marker's fence 1 — the NONE layer governs what a noun may REFER TO on a stock, status or
   condition read, and an absence read whose negated predicate IS the body flag is the one place where
   naming the body IS the read (MOVE-GRAMMAR §1.2 row 11 class (a)'s own examples are body words
   negated). **If the chair rules ADDENDUM 13 B A.1's NONE layer strictly instead, all twelve faces
   lose their sharpest licensed word** and the absence must be carried with no noun; the pool would then
   be re-cut whole, not patched.
2. **The pressure class (marker open row 4).** `against hunger` rides in every face as the row LABEL's
   own material at its engine meaning plus D-12's entailed purpose of the recorded noun. If the chair
   reads even the bare class as importing a live pressure, the tail drops from all twelve and the floor
   falls to the two bodies alone — which would cut vids 2 and 3 hardest.
3. **Sickness spoken once, not twice.** The pressure is named on the hunger half only, because
   `against sickness` beside `those tending the sick` is the summarising beat W6 bars. A refuter
   reading A6 across the faces will find the claim set identical in all twelve; the asymmetry is a
   surface fact about the body's safe spelling, not a claim difference.
4. **The care body's noun (marker open row 3).** `those tending the sick` in all twelve, refusing every
   building noun and every capability verb — the alias bar's always-safe spelling, and stronger here
   than at the sibling pool because the hospital-named condition cannot resolve at this key at all. The
   plural class phrase asserts a kind, not a count, and is what keeps W22 satisfied over a row that is,
   on the generated path, one person.
5. **`[unfolding]`'s OPEN is carried by SILENCE, not by a clause** (skeleton §234; R-iv). No typed field
   on this card holds an unresolved value, so the OPEN QUESTION move is not licensed as a sentence; the
   matter left standing open is the stock the town actually holds, which the card never reads and no
   face guesses at. If the chair wants the open matter SPOKEN, a field must license it and this seat
   does not invent one.
6. **The spread debt (marker open row 6) is only part-repaid.** The census reads `grammars 2` over three
   variants against MOVE-GRAMMAR §2.1's min(k, 8) = 3. All three rewritten variants realise
   PRESENT → LACK (V3) at level 1, because the card holds exactly one state key and one `none-exists`
   half and no other member's licensing fields are non-null here (§3.1: a member with a null field is
   not drawn and is not written empty). What the rewrite does repay is the SEGMENT-COUNT uniformity and
   the repeated-opener fault; the level-1 order spread is a wiring limit of this card, not a writer's
   choice, and is recorded for the sitting rather than manufactured.
