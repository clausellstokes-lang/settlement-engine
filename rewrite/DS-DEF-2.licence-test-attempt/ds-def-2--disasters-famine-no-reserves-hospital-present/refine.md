# REFINE — DS-DEF-2 · `Disasters & Famine: NO reserves, hospital present`

Seat: Opus REFINER (Opus 5, Fable-unvalidated) — a DIFFERENT author than the drafter, for the Fable
chair. Phase 2, the one refinement round (Part B §21 second bullet; §21.1 the ceiling not the middle;
§21.2 every set is refined; §21.3 one effort; §21.4 the density law).

This file is the only thing this seat wrote, anywhere. `laneRW-DEF2` was READ only; the only thing
executed there was the read-only licence-card script the brief names. Nothing in any dock was edited,
committed or tested.

**Read whole before a word was written:** the printed licence card (re-run); `skeleton.md` §0–§9 (the
marker's fences, the three variants' stances, the turns worth keeping, the regression lists, §8's
pool-level walls); `draft-round-1.md` and `draft-round-2.md` whole (the lawful set this round
refines, and the two gate measures round 2 answered); the pool's rows as they now stand at
`laneRW-DEF2/docs/content/RECEIPT_POOLS_DOSSIER_STATE.md` under `### DS-DEF-2`, with the sibling
pools above and below; `REGISTER-CARD.md` whole with S2 and S3; `RULES-V2-PART-B.md` §1 (the R-DA
rules), §8 (the absence rule), §9 (the clerk rule), §16, §16.1, §16.2, §18, §20, §21, §21.1–§21.4,
§22, §23, §24; `MOVE-GRAMMAR.md` §1–§3 (the eleven moves, the eight non-moves, the ten walls, the
level-1 grammars), §1.4.1, §4.4.1–§4.4.3; `CLERK-LAWS.md` §2.4.1 and §2.6.1; `ARCH-COMPOSED-PROSE-v2`
§2.5 (the annex grammar and its refusals) and §8.3 (the licence card); brief ADDENDUM 13 A and B with
the writer bars W11–W27; the ten writer rules W1–W10; R-i · R-ii · R-iv · R-vi · R-viii′.

**TWELVE faces refined one for one. NO variant added, dropped, merged or moved. NO face added or
dropped. ELEVEN faces changed; ONE face held byte-unchanged with its reason recorded (§8 call 5).
ZERO claims added, ZERO dropped. ZERO refusals.**

---

## THE ROWS (the complete replacement for the pool's variant rows; paste under the pool's bold heading)

1. `[ledger]` Those tending the sick are entered at {settlement}, and against hunger no granary.
   - `[face]` Set down at {settlement} are those tending the sick. The town carries no granary against hunger.
   - `[face]` The town of {settlement} has those tending the sick entered, and against hunger sets down no granary.
   - `[face]` What is entered at {settlement} are those tending the sick, and no granary against hunger is carried.
2. `[street]` The town has those tending the sick. It keeps no granary against hunger.
   - `[face]` Those tending the sick are in the town, and against hunger the town is without a granary.
   - `[face]` What the town has are those tending the sick, and against hunger no granary at all.
   - `[face]` In the town are those tending the sick, and against hunger no granary is kept.
3. `[unfolding]` Those tending the sick stand at {settlement}, and against hunger no granary stands.
   - `[face]` Here at {settlement} stand those tending the sick. Against hunger no granary stands in the town.
   - `[face]` What stands at {settlement} are those tending the sick, and what does not is a granary against hunger.
   - `[face]` The town of {settlement} has those tending the sick standing, and against hunger no granary.

--- NOTES

### 1. PER FACE — WHAT CHANGED, AND THE LAW OR TARGET BEHIND IT

**VARIANT 1 · `[ledger]` · construction contract kept (W4): THE RECORD'S ACT** — `entered` · `set
down` · `carried`, agentless or on the town, never on a holder. Slot set `{settlement}` in all four,
never the first token.

| # | before (draft round 2) | after | what changed, and the law or target |
|---|---|---|---|
| 1 (the numbered line) | Those tending the sick are entered at {settlement}, and against hunger no granary is entered. | Those tending the sick are entered at {settlement}, and against hunger no granary. | **The second `is entered` is STRUCK and the clause gapped.** Target: W6, the doubled beat — the draft repeated the office's formula inside one sentence, and the ELLIPSIS is the sharper licensed surface because the clerk does not re-run the formula for a thing that is not there; the gap ENACTS the absence instead of narrating it (§21.4: compression that rewards the reader is the ceiling). The line also becomes the variant's shortest at thirteen words (NL-2, R-DA-05: the short line exists) and now lands on `granary`, the civic noun the field names (R-DA-04). Claim set untouched; the gapped clause recovers `is entered`, so the absence stays a fact of the record. |
| 2 | Carried at {settlement} are those tending the sick. The town carries no granary against hunger. | Set down at {settlement} are those tending the sick. The town carries no granary against hunger. | **The fronted participle moves from `Carried` to `Set down`.** Target: the lexical echo `Carried … carries` inside sixteen words. The register card licenses the office's formula RECURRING ("the WORD may recur; the FACT must not") and R-vi/W7 make it the `[ledger]`'s lawful surface — but recurring across the pool is the licence, not twice in one breath, which is the summarising beat's rhythm. The variant now spends three distinct formula verbs across its four faces. The INVERSION, the two sentences, the thread (`the town` carried forward from `{settlement}`, R-i and §1.4.1), the landing on `hunger` and the word count are all kept. |
| 3 | Set down at {settlement} are those tending the sick, and against hunger no granary is set down. | The town of {settlement} has those tending the sick entered, and against hunger sets down no granary. | **The construction is re-cut from a second fronted-participial inversion to the naming form with an active second predicate.** Three targets. (a) W4 and the brief's four-faces rule: the draft ran the SAME construction class at faces 2 and 3, which is the one collision inside this variant. (b) W6: `Set down … is set down` was the flattest of the draft's five within-face formula repetitions. (c) R-DA-04: the face lands on `granary` rather than on the formula verb. The naming form `The town of {settlement}` is spelled exactly as W5 gives it, and the elided subject of `sets down` is that same naming form, so no new referent enters. |
| 4 | The town of {settlement} has those tending the sick entered, and against hunger no granary is entered. | What is entered at {settlement} are those tending the sick, and no granary against hunger is carried. | **The construction is re-cut to a pseudo-cleft with a full passive second clause.** The naming form moved to face 3, so face 4 takes the variant's fourth construction: the four faces now differ in SUBJECT (care body · fronted participle · naming form · cleft), in the GRAMMAR OF THE ABSENCE (gapped · separate active clause · active predicate · full passive), and in LANDING — which is what W4 and the brief's THE FOUR FACES DIFFER IN CONSTRUCTION require, and what the draft's faces 2 and 3 breached. `has … entered, and … is entered` also goes (W6). The cleft is word order, which the register card says may be spent freely; it adds no claim and no emphasis the fields do not hold. The mid-sentence `against hunger` breaks the fronted-PP habit. |

**VARIANT 2 · `[street]` · construction contract kept (W4): THE TOWN'S HAVING** — `has` · `keeps` ·
`is without`, never the entry formula and never the present standing. Slot set `{}`: no `{settlement}`
in any of the four faces.

| # | before | after | what changed, and the law or target |
|---|---|---|---|
| 1 (the numbered line) | The town has those tending the sick. Against hunger the town keeps no granary. | The town has those tending the sick. It keeps no granary against hunger. | **The second sentence's subject becomes the pronoun and its prepositional phrase moves to the end.** Target: the street's ear. Both sentences opened on `The town`, which is the flattest shape the variant can take and reads as an inventory rather than as the town's plain talk (the skeleton's named regression for this angle). Ordinary speech pronominalises; the face is now thirteen words against fourteen and lands on `hunger`, giving the variant four distinct landings. `It` is singular and the only singular antecedent in the unit is `the town`, so W1 binds it correctly and no object intervenes. Recorded as a judgment call (§8 call 1: a pronoun carries the referent forward where R-i asks for a noun). |
| 2 | Those tending the sick are in the town, and against hunger it has no granary. | Those tending the sick are in the town, and against hunger the town is without a granary. | **`has no granary` becomes `is without a granary`, and the pronoun becomes the noun.** Two targets. (a) Sibling distance (A5): the draft's four faces all carried the town's `has`/`keeps`, so the four differed in vocabulary far less than in construction; `is without` is the third everyday verb the street's idiom holds and is already the block's own surface at the rewritten sibling pool (`things the town is without`). (b) The pronoun moves to face 1, where it does more work, so the variant spends it once. The face now lands on `granary`. |
| 3 | What the town has are those tending the sick, and against hunger no granary. | What the town has are those tending the sick, and against hunger no granary at all. | **`at all` is added to the gapped tail.** Target: §21.1's ceiling and the variant's landing spread. `At all` is a negative-polarity intensifier, not a claim: it asserts the absoluteness of an absence the flag already holds absolutely (`hasGranary === false`), adds no count, no degree and no grade, and is the street's own emphasis rather than the clerk's. It also gives the face a close of a different KIND from its three siblings, which is what R-DA-04 asks for. The pseudo-cleft — the street's most idiomatic emphatic and the only fifth construction this slot-free variant offers — is kept. |
| 4 | In the town are those tending the sick, and against hunger no granary at all. | In the town are those tending the sick, and against hunger no granary is kept. | **The gapped tail becomes a full passive clause, and `at all` moves to face 3.** Target: W4 and the four-constructions rule — the draft ran the same gapped tail at faces 3 and 4, so the two differed only in their opening. The locative inversion is kept; the absence now has its own verb and the face lands on `kept`. The variant's four absence grammars are now a fronted clause, a full active clause, a gap with an intensifier, and a full passive. |

**VARIANT 3 · `[unfolding]` · construction contract kept (W4): THE PRESENT STANDING** — `stand` ·
`stands` · `standing`. Slot set `{settlement}` in all four, never the first token. The angle's OPEN is
carried by silence, as the drafter ruled and the skeleton requires (§234: no typed field on this card
holds an unresolved value, so the OPEN QUESTION move is not licensed as a sentence).

| # | before | after | what changed, and the law or target |
|---|---|---|---|
| 1 (the numbered line) | Those tending the sick stand at {settlement}, and against hunger no granary stands. | *(HELD byte-unchanged)* | **The one face this round holds, deliberately and recorded.** The paired contract verb across both halves IS this variant's stance — the pair set out as it now stands — and it is the only face whose landing (`stands`) keeps the variant's four landings distinct (`stands` · `town` · `hunger` · `granary`). Every substitution tried cost either the pair or the spread: `and against hunger no granary does` trades the body verb for a pro-verb; `and no granary stands against hunger` duplicates face 3's landing; `stands at all` closes on an adverbial face 3 of variant 2 already owns. §21.3 — where one effort must choose between a sharper wording and a licensed one, the licensed one wins — and §21.2 keeps a refinement only if the set stays lawful. Recorded vetoably at §8 call 5. |
| 2 | Here at {settlement} stand those tending the sick. Against hunger the town stands with no granary. | Here at {settlement} stand those tending the sick. Against hunger no granary stands in the town. | **`the town stands` is STRUCK; the granary becomes the second sentence's subject.** Target: a latent unlicensed reading. `The town stands with no granary` puts the contract verb on the TOWN, and on a Disasters and Famine row `the town stands` reads as the town ENDURING — an outcome claim the card refuses in terms (`may NOT: … a future`; the skeleton's "no outcome, no hardship"), and a grade the badge two inches away may contradict. Moving `stands` onto the granary keeps the variant's contract verb, keeps the deictic inversion, keeps the two sentences, keeps the thread (`the town` carried forward from `{settlement}`, R-i), keeps the eight-and-eight shape, and keeps the second sentence opening on the pressure rather than on the negated body (wall 3's spirit: the absence never opens). The face now lands on `town`. |
| 3 | What stands at {settlement} are those tending the sick, and what does not stand is a granary against hunger. | What stands at {settlement} are those tending the sick, and what does not is a granary against hunger. | **The second limb's `stand` is gapped away.** Target: §21.4's density law read as a gain, not a trim — the verb is recovered by ellipsis from the first limb, so the paired cleft (the skeleton's one "kept as a shape" for this variant: something standing against the one pressure and nothing standing against the other, in one unit, presence first) now closes in eighteen words instead of nineteen and the third `stand` of the sentence goes. The claim set, the paired construction, the single negated surface (W10), the one-sentence form and the landing on `hunger` are all unchanged. Cutting words inside the band is editing, not trimming (§22's own carve-out). |
| 4 | The town of {settlement} has those tending the sick standing and no granary standing against hunger. | The town of {settlement} has those tending the sick standing, and against hunger no granary. | **`standing` is not said twice; the second limb is gapped and the pressure fronted.** Two targets. (a) W6 and the read-aloud ear: `standing … standing` was the pool's heaviest repetition and made the face a list of two items rather than a stated pair. (b) The variant's absence grammars are now four distinct ones — a full clause, a separate sentence, a gapped cleft, and a gap — which is the skeleton's own named axis for telling four faces apart. The face lands on `granary`, the civic noun, and loses a word without losing a claim. |

### 2. THE CLAIM SET — UNCHANGED IN ALL TWELVE, AND IDENTICAL ACROSS EACH VARIANT'S FOUR FACES (arm A6)

Every one of the twelve refined faces carries exactly the four claims the draft's twelve carried, and
no other:

| claim | the surface, after refinement | licence |
|---|---|---|
| C1 — a CARE-class row stands on this town's record | **those tending the sick**, this spelling and no other, in all twelve | the card's `reads`/`predicate` reached only on `hospital === true`; the alias bar's always-safe form (A-15, W15); layer BODY |
| C2 — NO GRANARY stands on this town's record | **no granary** / **a granary** under a single negation, the body word at the roster grain, never a stock word | the same `reads`/`predicate`, reached only on `granary === false`; the LACK move (MOVE-GRAMMAR §1.2 row 11 class (a), a `none-exists` field, its own examples being body words negated); the marker's fence 1 |
| C3 — the conjunction, presence FIRST and absence SECOND | the fixed order of all twelve | the card's `predicate` is the conjunction; the order is wall 3, not a band |
| C4 — hunger is the pressure a granary answers to | **against hunger**, bare and generic, in all twelve | the row label at its ENGINE meaning (W14) with D-12's entailed purpose of the recorded noun; a CLASS, never an event, a season or an outcome |

`{settlement}` rides inside the sentence in all four faces of vids 1 and 3 and in none of vid 2's,
exactly the parents' slot sets (ARCH §2.5). `{band}` is RESERVED and `{route}` unfilled; no face names
either and no face speaks a band word. **Sickness is still spoken once per face and only through the
care body's own safe spelling** (W6: `against sickness` beside `those tending the sick` is the doubled
beat), as the drafter ruled — so the asymmetry is a surface fact, not a claim difference, and A6 read
across each variant's four faces finds the same set.

**Nothing was added.** No face gained a stock word, a capability, a person, a totality, a count, a
cause, a season, an event, a history, a forecast, a grade, a band word, a second civic object, a
holder, a record noun or a citation. **Nothing was dropped.** Every face still states both halves of
the conjunction, which on this pool is a wall: a face naming one half prints a SIBLING CELL'S fact.

### 3. THE DENSITY FLOOR, READ THREE WAYS (Part B §21.4)

- **Against the SHIPPED sentences.** Unchanged from the draft's finding, and re-verified against the
  three rows the skeleton records verbatim at §0.6: not one clause of the three is lawful whole, the
  single surviving shipped word in the pool is `The town` at vid 2, and the one lawful shipped
  STRUCTURE — the pair in one unit, presence first — is carried by all twelve faces and carried most
  completely by vid 3's paired cleft. **No lawful shipped turn is made plainer by this round.** The
  material that is gone (a capability the roster cannot supply, an empty store the engine refutes, two
  events, a past, a future, a rarity, a grade and a totality over persons) was dropped by the draft
  under the laws its §4 names; this round re-admits none of it.
- **Against the DRAFT.** No face is plainer than the face it replaces. Nine faces gained a sharper
  construction or a sharper landing at the same or lower word count; two (vid 3 faces 3 and 4) gained
  compression by ellipsis, which §21.4 names as part of the ceiling and §22 names as editing rather
  than trimming; one face was held. The pool's word range moves from 13–19 to 12–18 with the claim
  count constant, which is density rising, not falling.
- **Against the CEILING.** The lawful purse for this pool is fixed and small — the care body in its
  one safe spelling, the storehouse body negated at the roster grain, the pressure class bare, the
  angle's own surface, the fixed order, the varied landing, and the matter left standing open by
  silence. What this round buys with it is what the purse actually allows: four genuinely different
  CONSTRUCTIONS per variant rather than three plus a near-twin, four distinct landings per variant
  rather than two or three, three distinct formula verbs inside the `[ledger]`, three distinct
  everyday verbs inside the `[street]`, and four distinct absence grammars inside the `[unfolding]`.

### 4. THE TICS THE ROUND REMOVED, COUNTED

| tic in the draft | count before | count after | how |
|---|---|---|---|
| the office's formula repeated in the SAME form INSIDE one face (`entered … is entered`, `Carried … carries`, `set down … is set down`, `standing … standing`) | 5 of 12 faces | **0** | gapping, a second formula verb, or an active predicate |
| two faces of one variant sharing a construction class | vid 1 faces 2 and 3 (both fronted-participial inversions); vid 2 faces 3 and 4 (both gapped tails) | **0** | vid 1 face 4 re-cut to a cleft; vid 2 face 4 re-cut to a full passive |
| the identical tail `against hunger no granary stands` on two faces of vid 3 | 2 | **1** | vid 3 face 4 gapped |
| a face landing on the same noun as a sibling inside its variant | vid 1 one, vid 2 two, vid 3 one | **vid 1 one, vid 2 zero, vid 3 zero** | the prepositional phrase moved, or the predicate made active |
| `The town` opening both sentences of one face | 1 | **0** | the pronoun at vid 2 face 1 |
| a fronted `against hunger` in the absence half | 9 of 12 | **8 of 12** | four faces now carry it mid-sentence or at the end; the phrase itself is the licensed claim C4 and cannot be spent down further without dropping it from a face, which A6 forbids |
| the pseudo-cleft across the pool | 2 of 12 | **3 of 12** | one added at vid 1 face 4; each is filled by its own variant's contract verb (`is entered` · `has` · `stands`), a reader meets one variant per seed, and it is the only fifth construction the slot-free `[street]` offers. Recorded as a judgment call (§8 call 3) |

### 5. THE WALLS, RE-CHECKED FACE BY FACE AFTER TWELVE SUBSTITUTIONS

- **Both halves in every face**; no face prints a sibling cell's fact. ✓
- **Presence first, absence second, in all twelve** (wall 3; R-DA-08); no sentence of any two-sentence
  face opens on the negated body. ✓
- **Exactly one negated surface per face**, always the licensed LACK, never a negated PRESENT read
  (R-ii/W10) — including vid 3 face 3, whose `does not` is its only negation and whose `a granary` is
  positive. ✓
- **No stock word anywhere**: no food, grain, reserves, stores, stock, larder, harvest, put by, held
  back, buffer, months, enough, empty, bare. The pool's ratified shipped breach stays dropped. ✓
- **No capability on the care body**: no treat, contain, containment, quarantine, recovery, copes,
  equipped, prepared, answer. ✓
- **No person and no totality over persons** (W22; the card's REFUSED COLUMNS); the plural class
  phrase is what keeps the `Healer` row's single person off the page. ✓
- **No faith, deity, parish, clergy or spell**; the `church` argument is not consulted on this branch
  (W19). ✓
- **No event, season, duration, history, forecast, trajectory, cause, counterfactual or meaning
  gloss.** Every verb in all twelve faces is present tense; there is no perfect, no past and no
  future anywhere. ✓
- **No grade, no band word, no comparison of the key's own two halves**, and no reading of the
  `scores.disaster` badge printed beside the face (W20). The one latent grade the draft carried —
  `the town stands` at vid 3 face 2, readable as endurance on a disaster row — is cured this round. ✓
- **No second civic object**: one care body and one storehouse absence; none of the other four
  readiness rows is named (C7; W2; A1/A11); no sibling pool's material is imported. ✓
- **No citation and no record noun** (A13; W24; the card's `source: (none) · SOURCE-UNRESOLVED`).
  `entered`, `set down`, `carried`, `carries`, `sets down`, `has`, `keeps`, `is without`, `is kept`,
  `stand`, `stands`, `standing` are VERBS; the office's own formula is the `[ledger]`'s lawful surface
  (R-vi, W7) and the tag is a STANDPOINT licensing no noun. No face says `the record`, `the books`,
  `the rolls`, `the register`, `according to` or `the town's own count`. ✓
- **Hard walls, all twelve**: no em dash, no exclamation, no digit, no percent, no which-clause, no
  question, no `I` or `you`, no figure, no sense verb on an abstraction, no intent for the town or a
  building, no expletive opener (R-DA-07 — no `there is`, no `there stands`), **no colon and no
  semicolon anywhere in the pool** (the joint stays a comma-and or a sentence break, which is what
  round 2 measured the bands to allow), no participial gloss tail, no maxim, no kicker, no hook, no
  tricolon, **no `-ing` first word on any face**, no settlement-token opener. ✓
- **First tokens**, by variant: `Those · Set · The · What` / `The · Those · What · In` / `Those ·
  Here · What · The`. Distinct within every variant; none is the slot (T-F8); the pool sits at **zero**
  settlement-openers against wall 10's allowance of one. ✓
- **Slot sets exactly the parents'**: vid 1 `{settlement}` ×4 · vid 2 `{}` ×4 · vid 3 `{settlement}`
  ×4 (ARCH §2.5). ✓
- **Sentence counts**: three of twelve faces run two sentences (vid 1 face 2, vid 2 face 1, vid 3
  face 2), one in each variant, so the pool is not uniform in segment count (R-DA-05; A11); no face
  runs three (R-DA-03). Every second sentence connects to its first — `the town` carried forward as a
  noun at vid 1 face 2 and vid 3 face 2, and as the pronoun `It` at vid 2 face 1 (R-i; §1.4.1). ✓
- **Word counts**, `{settlement}` counted as one word: vid 1 **13 · 16 · 17 · 17**; vid 2 **13 · 17 ·
  16 · 15**; vid 3 **13 · 16 · 18 · 15**. Range 13–18 against the draft's 13–19; none under 8, none
  over 30. ✓
- **THE THREAD to the two unknown modifiers (k = 2).** Every face hands forward **the town** (as
  `{settlement}`, `The town of {settlement}`, `the town` or `It`) and **the pressure class**, and every
  face reads as well after a sibling modifier as it does after nothing, because none of the twelve
  leans on a continuation. Neither a `store`-class nor a `care`-class modifier can attach (T-F12; ARCH
  §8.3's worked card holds the `stores: short` modifier off the two NO-reserves cells), so no face
  leans on a stock or care continuation. ✓

### 6. THE FINGERPRINT, READ ON THE TWO MEASURES ROUND 2 CURED AND ON THE ONES THIS ROUND COULD HAVE BROKEN

Computed by hand from `src/domain/prose/proseFingerprint.js:97-150` as it stands in the dock (read
only; nothing executed there but the licence-card script), on the face text with `{settlement}`
counted as one word:

| metric | reading across the twelve refined faces |
|---|---|
| `shapes.participialOpenerRate` (`/ing$/` on a sentence's first word, minus `NOT_PARTICIPLES`) | **0** on every face and every sentence. `Set`, `Carried`, `Here`, `What`, `In`, `Those`, `The`, `It`, `Against` are none of them `-ing` forms. Round 2's single failing face is not re-broken. |
| `punctuation.colonRate` · the semicolon | **0** across the pool; no face carries either mark. Round 2's second failing face is not re-broken. |
| `shapes.antithesisRate` | **0**. No `not X but`, no `rather than`, no `, not <lower-case>`, no `less … than`. Vid 3 face 3's paired cleft states two parallel facts of ONE key and ranks nothing, so wall 5's contrast is not engaged (the rejected alternative a contrast needs does not exist here). |
| `closers.abstractNounRate` | **0**. The twelve closers are `granary · hunger · granary · carried · hunger · granary · all · kept · stands · town · hunger · granary` — no `-ness`, `-tion`, `-ity` or `-ment` among them. |
| the existential and pronoun closers (`check-pair.mjs:125-129`) | **0**. No face opens on `there` and none closes on a pronoun. |
| `shapes.under8` | 1.0 on vid 2 face 1 (seven words and six), which is the same reading the draft's vid 2 numbered line carried and the gate PASSED; 0 on the other eleven. |
| `shapes.triadRate` · `doubledAdjectiveRate` | **0**. No face carries three of anything or an adjacent adjective pair. |

**No refined face reads worse on any metric than the draft face it replaces**, and two faces (vid 3
faces 3 and 4) read strictly cleaner by losing a repeated word without losing a claim. §21.2's
condition for keeping a refinement — the set stays lawful, with no new failing state — is met.

### 7. WHAT THIS ROUND DID NOT DO, AND WHY

- **No variant was re-tagged, re-numbered, moved, merged or added**, and the typed lines of the pool
  (ROLE, READS, RELATION, ATTACH, FORM, MOVE) were not touched and are not repeated here.
- **The claim set was not touched in either direction.** The refinement's whole budget was spent on
  construction, verb, order and landing — which is exactly what B-CLAIM allows a rewrite to spend and
  what §21.1 asks a refinement round to spend it on.
- **The spread debt (census `grammars 2` over three variants) is not repaid by this round either.**
  All three variants still realise PRESENT → LACK (V3) at level 1, because the card holds exactly one
  state key and one `none-exists` half and no other level-1 member's licensing fields are non-null
  here (MOVE-GRAMMAR §3.1: a member with a null field is not drawn and is not written empty). The
  drafter recorded this as a wiring limit of the card rather than a writer's choice; this seat read the
  level-1 table again and agrees. It stays a sitting row (marker open row 6), not a manufactured
  order.
- **The `[unfolding]`'s OPEN is still carried by silence, not by a clause.** No typed field on this
  card holds an unresolved, contested, pending or unmet value, so the OPEN QUESTION move is not
  licensed as a sentence (skeleton §234; R-iv); the matter left standing open is the stock the town
  actually holds, which the card never reads and which no face guesses at. Inventing a field to speak
  it would be the one thing the register card forbids absolutely.

### 8. THE JUDGMENT CALLS THIS SEAT MADE, RECORDED VETOABLY

The drafter's nine calls stand unamended and are not restated (`draft-round-1.md` §8 items 1–6;
`draft-round-2.md` §8 items 7–9), except that item 7's `Here` survives this round unchanged and item
9's complaint — face 4 sharing face 3's landing noun at vid 3 — is now **cured**, so the chair need
not rule on it. This round adds five.

1. **A PRONOUN CARRIES THE THREAD AT VID 2 FACE 1.** R-i says a spine's own second sentence carries a
   NOUN forward from its first; `It keeps no granary against hunger` carries the referent by pronoun.
   This seat judges the thread's purpose met — the two sentences connect, unambiguously and in the
   street's own idiom, and §1.4.1's concern is connection rather than a particular part of speech —
   and takes the ear's gain (the draft opened both sentences on `The town`) over the letter. **If the
   chair reads R-i literally, the cure costs one word and no construction:** `Against hunger the town
   keeps no granary`, which is the draft's own second sentence.
2. **`AT ALL` AT VID 2 FACE 3.** A negative-polarity intensifier on an absence the flag holds
   absolutely. This seat judges it claim-free — it adds no count, no degree, no grade and no
   comparison — and street-native, and notes the drafter already shipped it at this variant's face 4,
   so the round moves it rather than introduces it. If the chair reads an intensifier as emphasis the
   fields do not hold, the face reverts to the bare gap and loses only its distinct close.
3. **A THIRD PSEUDO-CLEFT ENTERS THE POOL, AT VID 1 FACE 4.** The alternative was to leave two faces
   of vid 1 sharing the fronted-participial inversion, which W4 and the brief's four-constructions rule
   refuse more sharply than a shape recurring once per variant. Each cleft is filled by its own
   variant's contract verb and a reader meets one variant per seed. **If the chair reads three of
   twelve as a pool habit**, vid 1 face 4 reverts to the naming form and vid 1 carries the construction
   collision the draft had — a sitting choice between two faults, recorded rather than hidden.
4. **`THE TOWN CARRIES` AND `SETS DOWN` KEEP THE TOWN AS THE FORMULA'S SUBJECT** at vid 1 faces 2 and
   3. The office's formula is the `[ledger]`'s lawful surface (R-vi, W7) and `carries` is a verb, not a
   record noun (W24), and the town is not a holder kind the holder table names, so A13 is not engaged.
   This seat kept the drafter's reading. If the chair reads the town-as-enactor of the record's act as
   a holder claim on a SOURCE-UNRESOLVED read, both faces take the agentless passive (`no granary is
   carried` / `no granary is set down`) at the cost of two landings.
5. **ONE FACE IS HELD BYTE-UNCHANGED: VID 3'S NUMBERED LINE.** §21.2 requires every set to be refined
   and this set is — eleven of twelve faces changed — but §21.2 also keeps a refinement only where the
   set stays lawful and no new failing state is added, and §21.3 rules that a licensed wording beats a
   sharper one where an effort must choose. Every substitution this seat tried on that face cost either
   the variant's paired contract verb (its whole stance) or the variant's four-distinct-landing spread.
   The face is the pool's shortest at thirteen words and is judged at its ceiling. Recorded here rather
   than disguised with a cosmetic edit, because a churned passing face is the regression §21.4 names.

### 9. REFUSALS

**NONE.** All three variants are refined, all twelve faces are lawful under the card, the two ratified
tables and the writer bars W11–W27 as this seat reads them. No variant was banked, none was trimmed,
none was merged, no vid moved, no face was withheld, and no face was added or dropped. The round is
not dry: it removes five within-face formula repetitions, two within-variant construction collisions,
one duplicated tail, three of the four landing duplications, one latent endurance reading at vid 3 face 2, and one
double-`The town` sentence pair, and it adds no failing measure on the fingerprint's twenty-one
formulas.
