# REFUTE ROUND 2 — DS-DEF-2 · `Disasters & Famine: NO reserves, hospital present`

Seat: REFUTER (opus), for the Fable chair. This file is the only thing this seat wrote, anywhere.
`laneRW-DEF2` was READ only; the only thing executed there was the read-only licence-card script the
brief names. Nothing in any dock was edited, committed or tested.

Judged against: the printed licence card (re-run); `skeleton.md` whole (§0–§9); `draft-round-2.md`
and `refine.md` whole (the named targets and the five recorded judgment calls); `kept.md` (the rows
standing after refinement, the text judged); `REGISTER-CARD.md` with S2 and S3; `RULES-V2-PART-B.md`
§1 (R-DA-01…R-DA-24), §16–§16.2, §18, §20, §21–§21.4, §22, §23; `MOVE-GRAMMAR.md` §1–§3, §1.4.1,
§4.4.1–§4.4.3; `CLERK-LAWS.md` §2.4.1, §2.6.1; `ARCH-COMPOSED-PROSE-v2` §2.5, §8.3; brief ADDENDUM 13
A and B (W11–W27); the ten writer rules W1–W10; `rewrite/rulings-DEF2.txt` (R-i · R-ii · R-iii · R-iv
· R-vi · R-viii′); `rewrite/entailment/ENTAILMENT-TABLE.draft.md` (D-12, D-13, L-14, L-20, A-10, A-15)
and `REFERENT-TABLE.draft.md`'s defense rows.

TWELVE faces judged. **FAIL 5 · WITHHELD 1 · PASS 6.**

The card as printed for this pool, in the two lines that decide most of what follows:
`source: (none) · standing SOURCE-UNRESOLVED — NO citation is licensed`;
`may NOT: a count, a cause, a season, a future, a standpoint, a second fact, another civic object of
the class store`.

---

## THE VERDICTS — one line per face (variant · face · verdict · law · quote · finding · cure)

**1 · 0 · PASS** — no breach found. Both licensed reads stated, presence first (wall 3); the office's
formula agentless (R-vi/W7); no record noun, no citation (W24, A13); no stock word (L-20, A-10); the
care body in its one ratified spelling (A-15, W15); slot set `{settlement}`, never the opener (T-F8).

**1 · 1 · WITHHELD** — law: R-vi / W7 with W24 (unproven, not cleared) — quote: *"The town carries no
granary against hunger."* — finding: the town is made the SUBJECT of the ledger's own formula verb.
`carries` has two readings here — plain possession (lawful) and "carried on the record" (the office's
formula given an agent on a card that resolves NO holder, `holderReason`: "no mapping row resolves any
field this pool reads"). No law this seat can name settles which reading governs, and the refiner
recorded the question itself at §8 call 4. **If the chair reads the town-as-enactor of the record's act
as a keeper claim, this is face 2's breach and takes face 2's cure.** — cure (only if ruled): recast
the second sentence agentless, *"Against hunger no granary is carried."*; no claim moves.

**1 · 2 · FAIL** — law: R-vi / W7 (the office's formula as an AGENT-SOURCE), with W24 and the card's
`SOURCE-UNRESOLVED` — quote: *"and against hunger sets down no granary"* — finding: the elided subject
of `sets down` is `The town of {settlement}`, so the town is made the ENACTOR of the record's act.
R-vi names this shape verbatim as the unlicensed one ("the roll shows, the roll sets down is a citation
the budget does not license"); the lawful surface is the formula AGENTLESS (`entered`, `on the roll`,
`carried standing`). `sets down`, unlike `carries`, has no possession reading — it can only be the act
of recording, so the face asserts a record-keeper on a pool whose card prints no holder at all. —
cure: *"and against hunger no granary is set down"* (the refiner's own recorded alternative, §8 call
4). The claim set, the construction contract (W4: THE RECORD'S ACT) and the landing all survive.

**1 · 3 · PASS** — no breach found. Agentless passive throughout; the pseudo-cleft is word order, which
the register card says may be spent freely; distinct subject, absence grammar and landing from its
three siblings (W4).

**2 · 0 · FAIL** — law: R-i (the chair's ruling binding this block), with MOVE-GRAMMAR §1.4.1 — quote:
*"It keeps no granary against hunger."* — finding: the spine's own second sentence carries a PRONOUN
forward from its first, not a noun. R-i is stated for exactly this case — *a spine's own second
sentence carries a noun forward from its first regardless of k* — and the refiner recorded the
departure as a judgment call (§8 call 1) rather than as a licence. The thread's purpose being met is
not the test the ruling sets. — cure: *"Against hunger the town keeps no granary."* — the draft's own
second sentence; one word, no construction change, no claim moved.

**2 · 1 · PASS** — no breach found. Both reads; `is without` is the street's third everyday verb and
keeps W4's contract (THE TOWN'S HAVING); no slot, as the parent's empty slot set requires (ARCH §2.5).

**2 · 2 · FAIL** — law: R-DA-08 and the register card's absence clause (*in the record, never in the
world; **flat***), with the pool's no-grade fence (skeleton §0.7 fence 4) — quote: *"and against hunger
no granary at all"* — finding: the intensifier makes the declared lack EMPHATIC where the law requires
it flat. `hasGranary` is a boolean the card states without degree, so `at all` adds nothing the record
holds and adds the one thing the record may not carry on this row — a weighing of the absence, on a
row whose own badge (`scores.disaster`) is a separate read printed two inches away. The refiner
recorded the intensifier as a judgment call (§8 call 2). — cure: strike `at all`; the face reverts to
the bare gap, *"and against hunger no granary"*, and loses only its distinct close.

**2 · 3 · PASS** — no breach found. Locative inversion, full passive absence, distinct landing; no
expletive (R-DA-07).

**3 · 0 · FAIL** — law: R-iv (the `[unfolding]` realises OPEN after the presence, never the ledger's
measure), with R-DA-05 (the POOL is the unit of spread) — quote: *"Those tending the sick stand at
{settlement}"* — finding: this is variant 1's numbered line with `are entered` replaced by `stand` —
the same subject, the same `at {settlement}`, the same `and` joint, the same gapped `against hunger no
granary` tail — and nothing in it stands open. The angle is carried on the tag alone. The refiner's
defence (the OPEN is realised "by silence", §7) makes R-iv satisfiable by any text whatever, and the
register card's own permission is *one matter left standing open — **stated**, never asked*. The
skeleton names this failure mode for this variant in terms: *an `[unfolding]` face that states the pair
flatly with nothing standing open (the ledger's job)*. — cure: re-cut onto a construction variant 1
does not take and let the LACK close the face as the matter still standing in the present (the paired
frame the skeleton keeps for this variant, §(6)); add no claim, keep the contract verb.

**3 · 1 · PASS** — no breach found. The deictic inversion is a surface the `[ledger]` does not take;
`Here` asserts no place fact, no second referent and no address to a reader (R-DA-01), and the block's
own rewritten sibling pool already carries it on an `[unfolding]` row; the second sentence opens on the
pressure, not on the negated body (wall 3), and carries `the town` forward as a noun (R-i).

**3 · 2 · PASS** — no breach found. The paired cleft is the one shipped construction the skeleton keeps
for this variant (§(6): *something standing against the one pressure and nothing standing against the
other, in one unit, presence first*), so wall 5's fronted contrast is not engaged — the two limbs state
a PRESENT and a LACK of one key and rank nothing. The gapped `does not` recovers `stands` from the
first limb; strained to the ear, which is not a finding by itself.

**3 · 3 · FAIL** — law: R-iv, with R-DA-05 — quote: *"The town of {settlement} has those tending the
sick standing"* — finding: the face repeats variant 1 face 2's opening seven words and its whole frame
(the naming form as subject, the participial complement, the fronted `against hunger`, the gapped
second limb) with the contract verb swapped, so in the simulated reading sequence the `[unfolding]` and
the `[ledger]` are interchangeable sentences; nothing here stands open. Two of this variant's four
faces are thus the `[ledger]`'s own faces in a different verb. — cure: re-cut the frame off variant 1's
naming-form construction and carry the present standing on the absence half, so the pair reads as still
standing rather than as an entry made; add no claim.

---

## THE POOL-LEVEL ROWS THE CHAIR SHOULD SEE (reported, not charged to a face)

- **THE `[unfolding]`'s OPEN IS UNREALISED ACROSS THE VARIANT.** The two FAILs above are the two faces
  where it is provable by a twin in variant 1; faces 1 and 2 carry constructions the `[ledger]` does not
  take and are passed on that ground alone, not because the angle does its work. **The variant-level
  question for the chair is whether "OPEN by silence" satisfies R-iv at all.** If it does not, all four
  faces fall and the cure is a variant-wide re-cut. The marker recorded the same tension (§(4), §(5))
  and the refiner leaned on it (§7).
- **THE FRONTED `against hunger`.** Eight of the twelve faces front the pressure phrase in the absence
  half. This is a spread reading, not a wall: §16's channel rule reports a soft rule's distance and
  fails only past the depth, the budget or the ceiling, and the refiner records that the phrase is the
  licensed claim C4 and cannot be spent down without dropping it from a face, which A6 forbids. Reported
  for the sitting's spread row, charged to no face.
- **THE CLOSE-KIND SPREAD.** Ten of twelve faces close on the ABSENCE kind, which is forced by wall 3's
  fixed order (the absence never opens) on a key that is a presence conjoined with a lack. R-DA-04 asks
  the KIND to vary across the pool; on this pool it structurally cannot. Reported as a card fact, not a
  writer's fault.
- **NOT FINDINGS, checked and cleared:** no stock word anywhere (L-20, A-10 — the pool's ratified
  shipped breach stays dropped); no capability on the care body (D-13's conditional, unreachable at this
  key); no person, no totality over persons (W22; the card's REFUSED COLUMNS); no building noun for the
  care row (A-15, W15 — `those tending the sick` in all twelve); no faith, deity, parish or spell (W19);
  no event, season, duration, history, forecast, trajectory, cause or meaning gloss; no grade, band word
  or badge reading (W20); no second civic object (T-F12); no citation and no record NOUN (A13, W24); no
  em dash, no exclamation, no digit, no percent, no which-clause, no question, no expletive opener; slot
  sets exactly the parents' (`{settlement}` ×4 · `{}` ×4 · `{settlement}` ×4, ARCH §2.5); zero
  settlement-token openers against wall 10's allowance of one; one negated surface per face (R-ii/W10);
  every face states BOTH halves of the conjunction, so no face prints a sibling cell's fact.
- **THE DENSITY LAW, read both ways (§21.4).** Against the SHIPPED rows: no lawful shipped turn is made
  plainer — the shipped pool holds no clause lawful whole, and the one surviving shipped asset (`The
  town`, vid 2's subject) is kept. Against the DRAFT: no face is plainer than the face it replaces
  except where a law is named for the change; the two ellipses (vid 3 faces 2 and 3) are editing inside
  the band, which §22 carves out. **No face is charged with a density regression.**

---

## READ-ALOUD NOTES (information only, one per variant)

1. `[ledger]` — the three formula verbs carry the variant cleanly and the short numbered line lands.
   The one place the ear catches is *sets down no granary*: the town stops being the thing described and
   becomes the thing doing the writing, mid-sentence, and the reader hears a clerk who is not there.
2. `[street]` — the plainest of the three and the closest to a person saying it; *is without a granary*
   is the best-sounding clause in the pool. *no granary at all* is the one place the voice leans on the
   fact instead of stating it, and *It keeps* after *The town has* sounds like one sentence cut in half
   rather than two facts set down.
3. `[unfolding]` — read aloud beside variant 1 this is the same passage with one verb changed. The
   paired cleft is the only face that sounds like its own angle; the numbered line and the last face
   sound like the ledger's, and nothing anywhere in the variant sounds like a matter still standing.
