Seat: Opus 5 — Fable-unvalidated. Block DS-DEF-2 · pool key `Economic Survival: CRITICAL` · draft round 1 (Phase 1).

The rows below are the COMPLETE REPLACEMENT for the pool's three variant rows, ready to paste under the pool's bold heading `**\`Economic Survival\`: \`CRITICAL\`**` in `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`. The pool's typed lines and the bold heading are untouched and are not repeated here. Three variants in, three variants out; same order, same vids, same bracketed tags; none added, none removed, none merged. No `[plain]` marker anywhere (it belongs to modifier rows; the projector refuses it on a spine). Backticks around the tags follow ARCH §2.5's example row and the shipped annex.

---

1. `[ledger]` Against a crisis, {settlement} stands effectively uncovered. The town's capacity is entered critical.
   - `[face]` Economic survival at {settlement} is entered critical. The town holds no cover against a crisis.
   - `[face]` Critical is the standing entered at {settlement} for economic survival. The town cannot hold through a crisis.
   - `[face]` Measured against a crisis, the cover the town of {settlement} keeps would not hold. The town's economic survival is entered critical.

2. `[unfolding]` The cover the town keeps against a crisis reads critical, and the matter stands open.
   - `[face]` For a crisis the town has no cover, and that want goes on standing.
   - `[face]` Held against a crisis, the town's cover reads critical, and the town now stands uncovered.
   - `[face]` What the town could set against a crisis reads critical, and stands unmet.

3. `[street]` Whatever the town of {settlement} has against a crisis, it would not hold.
   - `[face]` The town of {settlement} could not hold through a crisis.
   - `[face]` A crisis would be more than the town of {settlement} could hold.
   - `[face]` Set against a crisis, the town of {settlement} has no cover.

---

--- NOTES

## 1. The card as printed, and the ONE claim every face carries

`node scripts/prose-licence-card.mjs DS-DEF-2 'Economic Survival: CRITICAL'` (read-only, in `laneRW-DEF2`): role `spine` · **reads** `scoreBand(economicScore)` (via `ECONOMIC_ROW_POOL` in `defenseStateProse.js`; absent ⇒ no candidate) · **predicate** `scoreBand(economicScore) === CRITICAL` · **bag** `{band: RESERVED, route: proper, settlement: proper}`, FILLED at this block's call sites `{settlement}` · relation none (a spine takes no relation) · not a seat-taker / form `sentence` · move none declared · **angle** `ledger street unfolding` · attach empty · echo `spine mounts 1 (tabs: defense)` · covert `no` · **source** `(none) · standing SOURCE-UNRESOLVED` — no citation licensed, a face naming a record holder refused by arm A13 · **may claim:** that the reader selects the row `CRITICAL`, as a STANDING fact of the record · **may NOT:** a count, a cause, a season, a future, a standpoint, a second fact · audience `player` (no mark) · REFUSED COLUMNS always: a totality over persons, an exemption from a duty, a named character and that character's fate, a theological claim.

**C1 — the one claim, carried by all twelve wordings and by nothing else.** *The town's cover against a crisis — the economic-survival arm — stands at the band CRITICAL, as a standing fact of the record.* Stated WHOLE in every face, in its three parts:

| part of C1 | what licenses it |
|---|---|
| the ARM named (`economic survival` as the label; `the town's capacity`; `the cover the town keeps`) | the card's `reads` / `predicate` lines (the arm IS the reader) + W14, which requires the label at its ENGINE meaning (the economic-resilience axis), never the dictionary sense; skeleton §0.7 fence 1 licenses these three spellings and refuses any that names a component or a body |
| the BAND stated (`critical`; `effectively uncovered`; `no cover`; `cannot / could not / would not hold`) | the card's `may claim` line (the row `CRITICAL` as a STANDING fact) + `bag: {band: RESERVED}` (a face writes the band word itself, never the slot) + the ladder's authored reading of this rung, `bandLadders.js:206`, "effectively uncovered against this pressure", which skeleton §0.7 fence 3 rules is the band's reading in other words |
| the PRESSURE CLASS set against it (`a crisis`; once `the pressure`) | the ladder's own noun for what the arm is measured against (skeleton §0.7 fence 2, open row 4, which licenses both the engine's `crisis` and the ladder's `pressure` as the CLASS and refuses every particular and every duration on either) |

Claim-equality (arm A6) holds *across the four faces of each variant* and across the three variants (arm C): one claim, three parts, twelve constructions. Nothing else is asserted anywhere.

## 2. Per face, the licence clause behind every claim it makes

| row | wording | words | claims made | licensed by |
|---|---|---|---|---|
| 1 (numbered) | Against a crisis, {settlement} stands effectively uncovered. The town's capacity is entered critical. | 13 | C1 only: pressure (`a crisis`) + band as the ladder's reading (`effectively uncovered`, with the ladder's own hedge carried) + arm (`the town's capacity`) + band word (`critical`) | card `reads`+`predicate`+`may claim`; `bandLadders.js:206` for `effectively uncovered`; skeleton §(6) keeps `the town's capacity` verbatim as the shipped row's one lawful noun (the density floor); `entered` is the office's own formula, R-vi / W7 |
| 1 face a | Economic survival at {settlement} is entered critical. The town holds no cover against a crisis. | 15 | C1 only: arm by its label + band word + entry formula; then arm as `cover`, band as the negated capability, pressure | as above; W14 for the label at engine meaning; §0.7 fence 3 for `no cover` |
| 1 face b | Critical is the standing entered at {settlement} for economic survival. The town cannot hold through a crisis. | 17 | C1 only: band word fronted + standing + entry formula + arm by its label; then the negated capability against the pressure | as above; `standing` is a condition noun, not a record noun (W24 bars record nouns here, and none is used) |
| 1 face c | Measured against a crisis, the cover the town of {settlement} keeps would not hold. The town's economic survival is entered critical. | 21 | C1 only: pressure fronted + arm as `cover` + the negated capability in the subjunctive; then the arm by its label + band word + entry formula | as above; the subjunctive edge is the lawful modality, R-DA-07; W5's naming form `the town of {settlement}` |
| 2 (numbered) | The cover the town keeps against a crisis reads critical, and the matter stands open. | 15 | C1 only, in the PRESENT → OPEN order: arm + pressure + band word; then the same matter stated as standing open | card as above; R-iv and W8 require the `[unfolding]` variant to realise OPEN after the presence; skeleton open row 3 rules CRITICAL — the ladder's "effectively uncovered" — IS an unmet standing condition and therefore a lawful OPEN realisation when stated as the record's own condition; MOVE-GRAMMAR §1.2 row 10 (declarative always, the matter named, never an interrogative, never pre-deciding an outcome) |
| 2 face a | For a crisis the town has no cover, and that want goes on standing. | 14 | C1 only: pressure fronted + arm + band as the negated capability; then the same want stated as standing | as above; the durative present is the lawful unfolding surface (R-DA-07: the present stands), never a process, a season or a trend |
| 2 face b | Held against a crisis, the town's cover reads critical, and the town now stands uncovered. | 15 | C1 only: pressure fronted + arm + band word; then the standing stated in the ladder's own word | as above; W1 satisfied — the town is seated as the possessor, no possessive pronoun anywhere |
| 2 face c | What the town could set against a crisis reads critical, and stands unmet. | 13 | C1 only: arm as a free relative + pressure + band word; then the same matter stated unmet | as above; `unmet` is MOVE-GRAMMAR row 10's own word for a standing-open value |
| 3 (numbered) | Whatever the town of {settlement} has against a crisis, it would not hold. | 13 | C1 only: arm (whatever the town has) + pressure + band as the negated capability in the subjunctive | card as above; §0.7 fence 3; R-DA-07's subjunctive edge; the `[street]` stance is the town's plain talk about its own standing (skeleton VARIANT 3 (5)) |
| 3 face a | The town of {settlement} could not hold through a crisis. | 10 | C1 only: the town + the negated capability + pressure | as above; `could not` is kept verbatim from the shipped row as its one lawful turn (the density floor, §(6)) |
| 3 face b | A crisis would be more than the town of {settlement} could hold. | 12 | C1 only: pressure as subject + the capacity below it, stated as a comparison in words | as above; R-DA-11: "a comparison is a measurement in words" — no figure, no quantity, no gradation named |
| 3 face c | Set against a crisis, the town of {settlement} has no cover. | 11 | C1 only: pressure fronted + the town + arm + band as the negated capability | as above; §0.7 fence 3 |

Word counts: 13, 15, 17, 21 · 15, 14, 15, 13 · 13, 10, 12, 11. Mean 14.08; within-pool sd 2.91.

## 3. What the shipped rows claimed that NO card clause licenses, and is therefore dropped

Dropping is the rewrite's purpose (Part B §16's first WALL; the shipped breach is the corpus's known state). No slot, vid, tag or sentence count moves, so §22's ratchets are untouched one for one.

- **v1 `{settlement} cannot fund a response to anything.`** — `fund` dropped three ways: a pay-and-purse word on a read that is not the pay gate (W16 and the referent law's rule 5 file the pay synonyms under `defenseProfile.economicGates.military`, `fieldSynonyms.js:49-51`, which this card does not read); "what the score is made of" (D-22), the primary driver being food storage months and not prosperity (L-53, `rulings-DEF2`); and FALSE on the engine's own model — a CRITICAL thorp with a sound `econOutput` has coin and no stores, and the health gate's floor is 0.45 and never zero (entailment law item 3). `a response` dropped as an ACT no field records (R-DST-B), and it is the engine chrome's own word, which is product code outside the corpus. `to anything` dropped as a totality (W2; D-22's refused column: a badge on one arm says nothing of another, and the same tab may read `walls and professional garrison`).
- **v1 `Any sustained pressure exhausts the town's capacity almost immediately and then continues.`** — `pressure` and `the town's capacity` KEPT (both appear above, the second verbatim). `Any` dropped as a totality over pressures; `sustained` and `almost immediately` as a duration and a rate (the card's "a season"; R-DA-16); `exhausts` as a narrated consequence and a trajectory (G-19; state never fate); `and then continues` as a future (the card's may-NOT verbatim; R-DA-07). The sentence's SHAPE — a summarising second sentence — is itself refused (R-DA-03; §16 item 6's machine signatures), so vid 1's second sentence is now a second CONSTRUCTION of the one read carrying a noun forward, never a gloss.
- **v2 `The town is not spending its way out of trouble because there is nothing to spend, and each thing that goes wrong makes the next thing cheaper to happen.`** — `The town` KEPT as the subject and possessor. `is not spending its way out` dropped as a coin word, a FIGURE and a progressive act no field records; `of trouble` as a live pressure the card does not read (W18; A-25 — the crisis words name two record classes and the selector binds neither); `because` as a cause (the card, verbatim); `there is nothing to spend` as an empty purse that is false on the engine's model, a totality, and an expletive opener inside the clause; the closing clause as a MAXIM, a FIGURE, a trajectory and a relation no field computes (R-DA-12; R-DA-11; W23).
- **v3 `The town could not pay for a bad month at {settlement}, and the people who would have to be paid know it.`** — `The town` and `could not` KEPT. `pay` dropped on the same three grounds as `fund`; `for a bad month` as a duration and a particular crisis (W17); the placement of `at {settlement}` cured, since it attached to the month and not the town (every face now seats the slot as `the town of {settlement}`); `the people who would have to be paid` dropped as PERSONS as a load-bearing referent (W22; PERSON is never a referent), a pay word, and a force BODY by implication on an AGGREGATE read (W20; D-F25 holds on the sibling STRONG's "the garrison"); `know it` as a belief and knowledge frame (R-DA-13, R-DA-14; the FEELING non-move).
- **Provenance of the defect, recorded.** Every clause that carried the shipped rows' richness came from the engine chrome printed beside them (`threatAssessment.js:173` supplies "cannot fund", "Any sustained", "capacity to respond" almost word for word; DS-DEF-6's Economic Backing cell prints "Cannot sustain forces" at a DIFFERENT threshold, twenty-five against `scoreBand`'s twenty). That chrome is product code outside the REWRITE's corpus and licenses nothing here, so the rewrite reads plainer than the string beside it BY DESIGN (the honest limit; the owner's ADDENDUM 9 principle governs the cure — the most memorable sentence that can be written without increasing the claim set).

## 4. The angles, each in its own stance

- **`[ledger]` (vid 1) is a STANDPOINT, not a register** (W27) and licenses NO record noun and NO citation (W24; the card is SOURCE-UNRESOLVED, so the provenance ceiling is ZERO and arm A13 refuses any naming face). Its lawful surface is the office's own formula — `entered`, `the standing entered`, `carried` — used as a passive with no agent; there is no `the books show`, no `the roll`, no holder, no record noun of any kind in any of the four. Both sentences of every face are the one read in two constructions: one sentence carries the pressure and the standing, the other the arm's label, the band word and the entry — so neither summarises the other (the skeleton's own requirement at VARIANT 1 (4) and §8: "a second licensed CONSTRUCTION of the same read … never a gloss on it").
- **`[unfolding]` (vid 2) realises PRESENT → OPEN** (R-iv; W8; MOVE-GRAMMAR V6), the open matter stated as the record's own standing condition and never as an expectation, a forecast, a process or a question. It is the pool's only variant in a second level-1 grammar, which is what keeps the pool off MOVE-GRAMMAR §2.1's floor of two grammars in a pool of two or more. No face narrates a season, a compounding or a course of events.
- **`[street]` (vid 3) is the town's plain talk about its own condition** — the short clause, the common noun, the town as the subject that simply HAS its standing, and nothing about what anyone knows, believes, expects, fears or is owed. No person, no crowd, no knower, no pay word, no named month or season, no verdict, no maxim.

## 5. Walls checked line by line on the exact strings above

No em dash · no exclamation · no question · no digit, numeral or percent · no `which` in any form · no `I`, no `you`, no imperative to the reader · no citation, no record holder, no record noun (`books`, `roll`, `rolls`, `accounts`, `register`, `count` appear nowhere) · no figure, simile, sense verb on an abstraction or inanimate intent (a crisis never finds, asks, comes for or overwhelms anything; in 3 face b it is only measured against) · no future indicative and no bare `will`; the only modality is the subjunctive edge (`would`, `could`) and the plain modal negation (`cannot`) · no `There is` / `It is` expletive · no `rather than`, no `X, not Y` antithesis · no cause and no causal connective anywhere (`and` is the only joint) · no season, month, duration, rate or date · no count and no quantifier over persons · no person, role, office or body of any layer — no granary, stores, grain, food, market, port, route, hospital, treasury, purse, garrison, muster, soldiers, militia, watch, force, hall or seat (W20, W12, W13, W15, W21, W25 are unreachable by construction, and the census row carries `objectClasses []`) · no pay or coin word — `fund`, `pay`, `paid`, `wages`, `upkeep`, `the keeping`, `purse`, `spend`, `money`, `coin`, `revenue` appear nowhere (W16; rule 5; L-53; skeleton open row 2) · no totality — `anything`, `any pressure`, `nothing`, `everything`, `no one`, `defenceless`, `exposed` appear nowhere (W2) · no assertion that a pressure is LIVE and none that a pressure is ABSENT (W18; skeleton open row 6: the arm measures resilience against a pressure class, not the presence of one) · no comparison to another arm, another band or another town · no gradation beyond the band (`thin`, `short`, `shallow`, `lower`, `worse` appear nowhere; 3 face b's `more than` is a comparison between the pressure and the capacity, which is the band's own reading stated as a measurement in words, not a fifth rung) · no maxim, gloss, uplift or evaluative close · no tricolon and no run of three · no absence sentence, so wall 3 is untouched.

**Negated surfaces (R-ii / W10: at most one per variant, never as the opener).** Exactly one per wording, or zero: 1 `uncovered` · 1a `no cover` · 1b `cannot` · 1c `would not` ‖ 2 none · 2a `no cover` · 2b `uncovered` · 2c `unmet` ‖ 3 `would not` · 3a `could not` · 3b none · 3c `no cover`. No wording carries both `uncovered` and `cannot` (skeleton open row 5's rule), and no wording opens on the negation.

**The settlement token** (T-F8; MOVE-GRAMMAR §1.4 wall 10). No wording opens on `{settlement}`, so the shipped vid 1 breach is cured; the pool contributes zero to the register's settlement-opener share. The slot set is the parents' exactly: vid 1 `{settlement}` in all four faces, vid 2 `{}` in all four, vid 3 `{settlement}` in all four (ARCH §2.5 refuses a face whose slot set differs from its parent's). `{band}` and `{route}` are named by nobody; `{route}` would also be a component of the score and is refused twice over.

**The openers spread** (A11, the shipped breach cured). No two of the twelve share their first two words: *Against a · Economic survival · Critical is · Measured against · The cover · For a · Held against · What the · Whatever the · The town · A crisis · Set against.* The shipped pool had vids 2 and 3 both opening `The town`; the three numbered rows now open `Against a`, `The cover`, `Whatever the`.

**Sentence counts** (A11: where they differed they keep differing). Vid 1 TWO sentences in every face; vids 2 and 3 ONE each. The pool's shipped spread is held exactly.

**The close** (R-DA-04). Closing words: `critical` · `a crisis` · `a crisis` · `critical` ‖ `open` · `standing` · `uncovered` · `unmet` ‖ `hold` · `a crisis` · `hold` · `cover`. Every one asserts a present condition of a civic thing or names the thing itself; zero pronoun closers; no verdict, no maxim, no hook, no outcome. Close KINDS used are the condition and the object — the absence and the name-not-given are unavailable on this card (no absence field, no names), which is reported, not padded.

**THE THREAD** (§1.4.1; R-i at k = 0). This pool is the SPINE and opens the passage, with `k 2` on the census, so up to two modifiers may follow and the writer does not choose their place. Every wording ends on a civic noun or on the condition of one — the capacity, a crisis, the cover, the pressure, the matter, the town — so a modifier always has a noun to carry. Inside vid 1's two sentences the second carries a noun forward from the first in every face (`{settlement}` → `The town` / `The town's`; in face c the town is named once as `the town of {settlement}` and picked up as `The town's`). Inside the one-sentence variants no clause changes subject without handing something back: in 2 face b the shift from `the town's cover` to `the town` is to the possessor already named; in 2 and 2c the second clause keeps the first's subject or elides it. Every wording also reads whole standing alone, and none opens on a connective, a pronoun or a demonstrative, so each reads as well after an unknown sibling modifier as after nothing.

**One term for one thing** (R-DA-22). The pressure is `a crisis` in eleven wordings and `the pressure` in none of the final set. The arm takes three spellings across the pool — `economic survival` (the label), `the town's capacity`, `the cover` — which is the skeleton's own instruction that the four faces differ in the landing noun (§8), and within any single wording only one arm noun is used except in vid 1, where the two sentences are by design the label construction and the plain construction of the same read. The band is `critical` where a record verb makes the sense unambiguous (`entered critical`, `reads critical`, `Critical is the standing entered`) and otherwise the ladder's reading; **no wording places `critical` where it could be read as the dictionary "crucial"** (never `critical for`, never `critical to`), which is W14's label bar taken at the sentence grain. The town is `the town`, and where the slot is carried it is `the town of {settlement}` (W5's naming form), which also cures the shipped vid 3 misparse.

## 6. Refusals

**No variant is refused.** All three are written, all four faces each, and this seat holds every one lawful on every wall and every named bar.

Two things are recorded rather than cured, both at the POOL grain and neither a wording fault:

1. **MOVE-GRAMMAR §2.1's distinctness law is met only in part.** "A pool of k variants carries min(k, 8) DISTINCT level-1 grammars, at least two in every pool of two or more." This pool is k = 3 and carries **two** — **V1 PRESENT** (vids 1 and 3) and **V6 PRESENT → OPEN** (vid 2). The floor of two is met; the ceiling of three is not reachable, because every remaining member is filtered out by a licensing field the block does not hold (§3.1: a member whose field is null is not drawn and is never written empty): V2 needs a structural-consequence field (a spine carries no relation row, so S2's clause seat is closed), V3 and V8 a `none-exists` or `not-held` field with provenance (none; and a LACK here would assert an absence the card does not read), V4 a named-object field (the bag carries no object slot and a body is refused), V5 an institution row (SOURCE-UNRESOLVED), V7 is R2 only. The cure is a wiring row, not a rewrite.
2. **Three of the skeleton's open rows are relied on, and the packet moves if the chair rules otherwise.** (a) **Open row 1** — the arm's noun: this draft uses `economic survival`, `the town's capacity` and `the cover` as spellings of the ARM. If the chair rules the arm may be spoken only by its label word, the six wordings that carry `capacity` or `cover` alone need their arm noun replaced by the label, and nothing else moves. (b) **Open row 3** — the `[unfolding]` OPEN realisation on a band read: vid 2's four faces realise OPEN as the band read as an unmet standing condition, which the skeleton rules lawful. If the chair reads OPEN as needing its own field, vid 2 falls back to V1 PRESENT in the unfolding's plainness, the pool falls to ONE grammar, and R-iv's angle test is then unmeetable — the two rulings would need reconciling at the fold. (c) **Open row 5** — the capability verb: `cannot hold through`, `could not hold`, `would not hold` and `stands uncovered` are read here as the ladder's "effectively uncovered" in other words, with no consequence narrated after them and no duration or rate on them. If the chair reads a capability verb as a forecast, all nine wordings carrying one fall back to the ladder's copula and the shipped rows' two kept turns (`could not`, `the town's capacity`) are not kept.

**Reported, not cured (a soft band, with its reason).** Within-pool word-count sd is **2.91** against R-DA-05's within-pool floor of 4.0 — the packet's one soft-band exceedance. Its cause is stated rather than padded: the card licenses ONE claim with three parts and no second field, so a longer wording can only be reached by padding, and the density law (Part B §21.4) makes padding the regression, not the cure. The range is 10 to 21 words and the register's short line is deliberately the `[street]` variant's.

**Grammar assignment, recorded (the `grammar:` tag is not this packet's to add).** Vid 1 **V1**, vid 2 **V6**, vid 3 **V1** — all AUTHORED tags with `{slot}` markers intact, not §4.4.2's silent-fallback V1.

## 7. Neither restating nor contradicting the neighbours (arms A1 and A11)

The three sibling rungs of this row — `STRONG`, `ADEQUATE`, `WEAK` — are what this pool is told apart from, and no phrasing of any of them is reused: `absorb`, `sustained`, `revenue`, `reserves`, `emergency measures`, `a bad season`, `a bad month`, `chronic`, `shortfall`, `irregular` and every pay word appear nowhere here. Nothing here claims a gradation against a sibling band; the badge carries the rung and the prose states the rung's own reading. Across the block, the other four rows on the same tab (Beasts, Invasion, Internal Security, Disasters) name walls, force, court, gaol, granary and hospital; **no wording here names any body at all**, so no drift with them is possible by construction and the same-page hazard C7 cannot fire — a town reading CRITICAL here may read `walls and professional garrison` on the Invasion row and hold months of grain on the Disasters row without contradicting one word above. The block's authored modifiers attach elsewhere; this pool's `attach` is empty and nothing here is written to accommodate a modifier, though every wording hands a noun forward if one ever sits behind it.
