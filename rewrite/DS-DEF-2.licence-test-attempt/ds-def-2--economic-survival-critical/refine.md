Seat: Opus 5 — Fable-unvalidated. Block DS-DEF-2 · pool key `Economic Survival: CRITICAL` · REFINEMENT round (Phase 2, Part B §21), a different author than the drafter.

Refined one for one from `draft-round-1.md`: three variants in, three variants out; same vids, same order, same bracketed tags, same slot sets, same sentence counts; three faces under each; none added, none removed, none merged; no `[plain]` anywhere. The pool's typed lines and its bold heading are untouched and are not repeated. Backticked tags follow ARCH §2.5's example row and the shipped annex.

---

1. `[ledger]` Economic survival is entered critical at {settlement}. The capacity so entered would not hold through a crisis.
   - `[face]` The town's capacity against a crisis is entered critical at {settlement}. Under that pressure the town stands effectively uncovered.
   - `[face]` Critical is the standing entered at {settlement} on economic survival. The cover the town would have through a crisis is effectively none.
   - `[face]` The cover {settlement} has against a crisis is entered critical. Through that pressure the town's capacity could not hold.

2. `[unfolding]` The capacity the town has against a crisis reads critical, and the want of cover stands open.
   - `[face]` Against a crisis the town's cover reads critical, and the town now stands uncovered.
   - `[face]` What the town could set against a crisis would not hold, and the matter is open now.
   - `[face]` The town's cover through a crisis stands at critical, and the want it leaves is open.

3. `[street]` Whatever the town of {settlement} has against a crisis, that cover would not meet the pressure.
   - `[face]` The town of {settlement} could not hold through a crisis.
   - `[face]` A crisis would be more than the town of {settlement} could hold.
   - `[face]` Set against a crisis, the town of {settlement} has effectively no cover.

---

--- NOTES

## The four named refinement targets (Part B §21.1: the ceiling, not the middle), fixed before a word moved

1. **The fronted-pressure opener was the pool's tic.** Five of the draft's twelve wordings opened on a pressure adverbial (`Against a crisis,` · `Measured against a crisis,` · `For a crisis` · `Held against a crisis,` · `Set against a crisis,`), a 0.42 share of one opening move — fault 9 by the back door at the face grain, and the one habit the refinement brief names first. It is now **2 of 12 (0.167)**, in two different variants and never on a numbered row.
2. **Two verbs had become the pool's formula.** `reads critical` carried three of variant 2's four wordings and `entered critical` closed two of variant 1's four. The office's formula may recur (the register card: the WORD may recur, the FACT must not), but a formula that owns three quarters of a variant is R-DA-05's uniform pool at the face grain. Now: `reads critical` 2 of 4 in variant 2 beside `stands at critical` and the ladder's reading; no two wordings of variant 1 close alike.
3. **The draft collided with its own sibling rungs.** The draft's numbered row opened `Against a crisis, {settlement} stands effectively uncovered`, against the shipped WEAK canonical `Against a crisis, {settlement} is entered thinly covered` — the same opening clause with the band word swapped, which is the template A1/A11 exist to catch; and `the cover the town keeps against a crisis` (draft vid 2) is WEAK vid 3's `The cover {settlement} keeps against a crisis` almost to the word. Both are gone. The ARM's nouns (`the town's capacity`, `the cover`, `a crisis`) stay shared across the four rungs on purpose — R-DA-22 is one term for one thing, so sharing the arm's spelling is the law, not a collision; what may not be shared is the construction.
4. **The density floor was under-collected.** The skeleton records four turns of the shipped rows that are lawful as they stand — `the town's capacity`, `pressure`, `The town`, `could not`. The draft carried three and dropped `pressure` from all twelve wordings by its own admission. All four now appear verbatim: `the town's capacity` (1a, 1c), `pressure` (1a, 1c, 3), `The town` (2, 2a, 3a, 3b, 3c), `could not` (1c, 3a).

## Per wording: what changed and the law or target behind it

| row | refined wording | what moved, and why |
|---|---|---|
| **1** (`[ledger]`, canonical) | Economic survival is entered critical at {settlement}. The capacity so entered would not hold through a crisis. | The fronted `Against a crisis,` opener is struck (target 1) and with it the near-verbatim clash with the shipped WEAK canonical (target 3). The arm's LABEL now carries the subject, which no sibling rung's canonical uses. Sentence two is re-cut so it is a second CONSTRUCTION and not the draft's second statement of the band: `so entered` picks the office's own formula up as the thread (R-i at k = 0; R-vi / W7), and the sentence lands the ladder's reading and the pressure class, neither of which sentence one holds — so nothing is summarised (R-DA-03; §16 item 6). Close moved from the band word to `a crisis`, an object, so the variant's four closes are four kinds (R-DA-04). |
| **1a** | The town's capacity against a crisis is entered critical at {settlement}. Under that pressure the town stands effectively uncovered. | The draft's face a opened on the label, which is now the numbered row's; this face takes the ARM NOUN as subject instead, so the four faces differ in subject and not in vocabulary (W4; ADDENDUM 7 rule 4). It carries **both** shipped lawful turns the draft let go — `the town's capacity` verbatim and `pressure` as the class word (the density floor, §21.4; the skeleton's VARIANT 1 (6)). `Under that pressure` threads back to `a crisis` and `the town` to `{settlement}`. The ladder's own hedge `effectively` is kept, its one negated surface (R-ii / W10), never the opener. |
| **1b** | Critical is the standing entered at {settlement} on economic survival. The cover the town would have through a crisis is effectively none. | The band-word inversion is kept — it is the pool's one inversion and the face's declared construction (W4) — but `for economic survival` becomes **`on economic survival`**: `critical … for` is the exact adjacency W14's label bar reads as the dictionary "crucial", and the draft's own wall check claimed the pool held none. The draft's flat second sentence (`The town cannot hold through a crisis`) was the polarity flip of the shipped STRONG sibling's face and gave the variant a second bare modal; it is replaced by the subjunctive `would have … effectively none`, which is the lawful edge (R-DA-07) and gives this variant its ABSENCE close, a kind no other wording in the pool takes (R-DA-04's closed set). |
| **1c** | The cover {settlement} has against a crisis is entered critical. Through that pressure the town's capacity could not hold. | The draft's `Measured against a crisis, the cover the town of {settlement} keeps would not hold` carried the second fronted opener AND the WEAK sibling's `the cover … keeps` (targets 1 and 3); both are struck. The slot is seated at word three so it binds to the cover the town has and never opens the sentence (T-F8; the shipped vid 3 misparse stays cured). This face carries the shipped `could not` verbatim (the skeleton's VARIANT 3 (6) turn, lawful here because its verb is now a capability verb and not `pay`), plus `pressure` and `the town's capacity`. |
| **2** (`[unfolding]`, canonical) | The capacity the town has against a crisis reads critical, and the want of cover stands open. | The draft closed on `and the matter stands open`, and MOVE-GRAMMAR §1.2 row 10 requires the open matter to be **named**: `the matter` names nothing. The matter is now named as `the want of cover` — the ladder's reading carried into the OPEN clause as a noun, so PRESENT → OPEN (V6) realises on two different surfaces instead of stating the band twice (W6's doubled beat). `the cover the town keeps` is gone (target 3); `the capacity the town has` is the arm at its layer. One negated surface (`want`), not the opener (W10). |
| **2a** | Against a crisis the town's cover reads critical, and the town now stands uncovered. | Kept as the pool's **one lawful fronted opener** — a register that never fronts is as uniform as one that always does, and this is the variant the skeleton's R-iv reading makes the natural home for it. The draft's version of this OPEN clause (`and the town now stands uncovered`) is the skeleton's own sanctioned realisation (VARIANT 2 (5)) and is kept verbatim; the PRESENT clause is re-cut so the subject shifts from the cover to the town across the joint, which is what keeps `now stands uncovered` an OPEN move and not a gloss (W6). |
| **2b** | What the town could set against a crisis would not hold, and the matter is open now. | The draft's `What the town could set against a crisis reads critical, and stands unmet` put `unmet` on the COVER, and a cover is not met — a pressure is. The referent slip is cured by moving the band onto the ladder's negated capability (`would not hold`) and letting the OPEN clause take its own subject. `is open now` marks the standing-open present without a duration word (`still`, `yet`, `so far` are all refused as durations under the card's "a season"). This is the pool's one wording with no band WORD; A6 reads claim-equality across the faces, and the ladder's reading is the band. |
| **2c** | The town's cover through a crisis stands at critical, and the want it leaves is open. | The draft's `Held against a crisis, the town's cover reads critical, and the town now stands uncovered` was the third fronted opener and the second wording to spend `reads critical`. `stands at critical` is a third band surface inside the variant, so no formula owns it; `the want it leaves` threads off the standing itself and names the open matter (row 10's "named"), and `through a crisis` varies the preposition the pool sets the pressure with. |
| **3** (`[street]`, canonical) | Whatever the town of {settlement} has against a crisis, that cover would not meet the pressure. | The draft closed `…, it would not hold` — a bare pronoun subject on the clause that carries the whole read. `that cover` names the thing the free relative introduced (the thread; R-DA-22's one term for one thing), and `would not meet the pressure` lands on the shipped `pressure` and gives the street variant an OBJECT close distinct from its siblings'. The `[street]` stance is held: the town's plain talk about its own condition, no band label (the street states the standing plainly, never the office's word), no person, no knower. |
| **3a** | The town of {settlement} could not hold through a crisis. | **Kept verbatim, deliberately.** It is the pool's short line — R-DA-05's `< 8 words ≥ 0.030` floor and NL-2's "the short line exists" both want one, and this pool's shortest is the street's by design — and it carries two of the shipped row's four lawful turns (`The town`, `could not`) in the plainest construction the card allows. §21.4 governs: a refinement that made a lawful line longer or plainer with no law behind the change is the regression, and every addition tested here (`and has no cover`, `cover and all`, `for a week`) bought a second negated surface, a totality or a duration. The change here is the pool around it. |
| **3b** | A crisis would be more than the town of {settlement} could hold. | **Kept verbatim.** The pressure as grammatical subject is the pool's one such construction, it is the only wording with no negated surface at all (W10 is a ceiling, never a floor), and `more than … could hold` is R-DA-11's comparison-as-a-measurement-in-words with no figure, no gradation and no fifth rung. Tested replacements (`would come to more than`, `would run past`) each imported a crisis that arrives or moves — a forecast and a figure (R-DA-07; the FORECAST non-move). |
| **3c** | Set against a crisis, the town of {settlement} has effectively no cover. | The one substantive change is `effectively`, the ladder's own hedge at `bandLadders.js:206` carried onto the flattest wording in the pool: the draft's bare `has no cover` states the band a notch harder than the record does, and the hedge is the licensed word, not an added softening (W9 — entailments are may, never must). This keeps the pool's second and last fronted opener where the street's idiom makes it plainest. |

## The inventory, unmoved (Part B §22, never trim — the refinement is one-for-one substitution)

Three variants · three faces each · twelve wordings, exactly as the draft. Vids, order and bracketed tags unchanged (`[ledger]` · `[unfolding]` · `[street]`; one tag each; no second tag; no `[plain]`). Slot sets the parents' exactly — vid 1 `{settlement}` in all four, vid 2 **none** in all four, vid 3 `{settlement}` in all four (ARCH §2.5). Sentence counts the parents' — vid 1 TWO in all four, vids 2 and 3 ONE each (A11's spread). Authored grammars unchanged: vid 1 **V1**, vid 2 **V6**, vid 3 **V1** (W4's construction contract; the refinement moves no grammar). `{band}` and `{route}` named by nobody. No variant added, removed or merged; no face added or dropped; nothing trimmed.

## The measured state of the twelve, after the refinement

**Word counts** (a `{slot}` counts one): 17 · 19 · 22 · 19 ‖ 17 · 14 · 17 · 16 ‖ 16 · 10 · 12 · 12. Mean **15.92**, range **10 to 22**, within-pool sd **3.42** (the draft's was 2.91 over a 10-to-21 range).

**Openers, first two words.** *Economic survival · The town's · Critical is · The cover ‖ The capacity · Against a · What the · The town's ‖ Whatever the · The town · A crisis · Set against.* Eleven distinct of twelve; the one repeated pair (1a and 2c) sits in different variants and diverges at word three. A11's law binds the NUMBERED rows, and those three — *Economic survival · The capacity · Whatever the* — are distinct. Zero settlement-token openers (T-F8; MOVE-GRAMMAR §1.4 wall 10), so the shipped vid 1 breach stays cured and the pool contributes nothing to the register's settlement-opener share.

**The tics, counted.** Fronted-pressure opener **2 of 12** (0.167; the draft's 5). Inversion **1 of 12** (1b; the draft's 2). Participial opener **1 of 12** (3c). `reads critical` 2 of 12, `entered critical` 3 of 12 (the office's formula, which the register card licenses to recur), `stands at critical` 1, the ladder's negated reading 6 — no single band surface owns a variant. No wording of variant 1 closes as another does.

**Negated surfaces** (R-ii / W10: at most one per wording, never the opener, never an absence opener). 1 `would not hold` · 1a `uncovered` · 1b `none` · 1c `could not` ‖ 2 `want` · 2a `uncovered` · 2b `would not hold` · 2c `want` ‖ 3 `would not` · 3a `could not` · 3b **none at all** · 3c `no cover`. Exactly one or zero per wording; no wording opens on one; no wording carries both `uncovered` and a modal negation (the skeleton's open row 5).

**Closes** (R-DA-04, the kind varied). `a crisis` · `uncovered` · `none` · `hold` ‖ `open` · `uncovered` · `now` · `open` ‖ `the pressure` · `a crisis` · `hold` · `cover`. By kind: OBJECT 4 · CONDITION 7 · ABSENCE 1 — three of the closed set's five. PROHIBITION and A-NAME-NOT-GIVEN are unavailable on this card (no prohibition field, no names), which is reported, not padded. Zero pronoun closers. No verdict, no maxim, no hook, no outcome, no cause.

**The thread** (§1.4.1; R-i at k = 0). The pool is the SPINE and opens the passage at `k 2`, so up to two modifiers may follow and this seat does not choose their place. Every wording ends on a civic noun or on the condition of one — the capacity, the cover, the pressure, a crisis, the town, the want — so a modifier always has a noun to carry. Inside vid 1's two sentences the second carries a noun forward in every face (`so entered` ← `entered critical`; `that pressure` ← `a crisis`; `the town` ← `{settlement}`). Inside the one-sentence variants no clause changes subject without handing something back: 2a's shift from the cover to the town is to the possessor already named; 2 and 2c hand the standing forward as `the want`; 3's `that cover` names what the free relative introduced. Every wording reads whole standing alone, and none opens on a connective, a pronoun or a demonstrative, so each reads as well after an unknown sibling modifier as after nothing.

**The claim, unchanged from the draft and from the card.** One claim in three parts, in every one of the twelve and nothing else: the economic-survival arm, at the band CRITICAL (by the band's own word or by the ladder's authored reading), measured against the pressure CLASS — a STANDING fact of the record. Claim-equality holds across the four faces of each variant (arm A6 reads across the faces) and across the three variants (arm C). No claim is added and none is dropped relative to the draft; the shipped rows' unlicensed clauses stay dropped exactly as §3 of the draft packet sets out, and that section stands unamended.

**Walls, checked on the exact strings above.** No em dash · no exclamation · no question · no digit, numeral or percent · no `which` in any form · no `I`, no `you` · no citation, no record holder, no record noun (`books`, `roll`, `rolls`, `register`, `accounts`, `record`, `count` appear nowhere; `entered`, `so entered`, `the standing entered` are the office's own formula, R-vi / W7, and no face says the books show anything) · no pay or coin word · no body of any layer and no component of the score · no figure, no sense verb on an abstraction, no intent for an inanimate thing (no crisis finds, asks, comes, arrives, runs or overwhelms anything) · no future indicative and no bare `will`; the only modality is the subjunctive edge (`would`, `could`) · no expletive opener · no antithesis and no `rather than` · no cause and no causal connective (`and` is the only joint) · no season, month, duration or rate (`now` is a present marker; `still`, `yet`, `so far` appear nowhere) · no count and no quantifier over persons · no totality · no assertion that a pressure is LIVE and none that a pressure is ABSENT · no comparison to another arm, another band or another town · no gradation beyond the band · no maxim, gloss, uplift or evaluative close · no tricolon and no run of three · no absence sentence, so R-DA-08's walls are untouched. W11, W12, W13, W15, W21 and W25 are unreachable by construction: no face names a body of any layer.

## Refusals

**None.** All three variants are written, three faces each, and this seat holds every one of the twelve lawful on every wall and every named bar of ADDENDUM 13 A and B.

## Carried, not cured (each a pool-grain row, none a wording fault)

1. **MOVE-GRAMMAR §2.1's distinctness is met only in part, unchanged by the refinement.** k = 3 carrying **two** distinct level-1 grammars — V1 PRESENT (vids 1 and 3) and V6 PRESENT → OPEN (vid 2). The floor of two is met; the ceiling of three is unreachable because every remaining member is filtered out by a licensing field this block does not hold (V2 a structural-consequence field, V3 and V8 a `none-exists` or `not-held` field, V4 a named-object field, V5 an institution row, V7 event provenance). The cure is a wiring row, not a wording.
2. **Three of the skeleton's open rows are still relied on, and the packet moves if the chair rules otherwise.** Open row 1 (the arm's plain-word spellings `the town's capacity` and `the cover` beside the label `economic survival`); open row 3 (the `[unfolding]` OPEN realised on a band read, here as the want standing open and the town standing uncovered now); open row 5 (the capability verb read as the ladder's reading and not as a forecast). The fallbacks the draft names are unchanged.
3. **Within-pool word-count sd 3.42 against R-DA-05's floor of 4.0** — improved from 2.91 but still the packet's one soft-band exceedance, reported rather than padded away. Its cause is the card: ONE claim with three parts and no second field, so a longer wording can only be bought with words that carry nothing, and §21.4 makes that the regression rather than the cure. The range was widened at both ends (10 to 22) by lengthening the ledger's subjunctive face and leaving the street's short line short.
4. **One shared opener pair** (1a and 2c both open `The town's`), recorded so the chair sees it counted. A11 binds the numbered rows, which are distinct; every construction this seat tested to break the pair either spent a third fronted-pressure opener, took a sibling rung's opening clause, or put a holder's word (`the word on …`, W24) on a SOURCE-UNRESOLVED read.

## What this refinement did NOT touch

The draft packet's §3 (what the shipped rows claimed that no card clause licenses, and is therefore dropped), §4 (the angles' stances), §6's grammar assignment and §7 (the neighbours) stand as written; nothing in this round re-opens a dropped claim or restores a shipped clause. The pool's typed lines (ROLE, READS, RELATION, ATTACH, FORM, MOVE), its bold heading and the `grammar:` tags are not this packet's to write and are untouched.
