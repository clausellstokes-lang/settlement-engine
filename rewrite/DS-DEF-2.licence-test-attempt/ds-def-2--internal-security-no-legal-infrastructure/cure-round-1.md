Seat: Opus 5 — Fable-unvalidated (CURER; a different author from the drafter and the refiner). Block `DS-DEF-2` · pool `Internal Security: no legal infrastructure` · CURE ROUND 1, for the Fable chair.
Written to the printed licence card (re-printed read-only in `laneRW-DEF2` this round: `reads: court (not-produced) · prison (not-produced)`; `source: (none) · standing SOURCE-UNRESOLVED`; `may NOT: a count, a cause, a season, a future, a standpoint, a second fact`), the marker's `skeleton.md` in this directory (§0.4, §0.5, §0.7 (a)–(d), §0.8, §1), the register card (S2, S3), RULES-V2 Part B §1 / §16–§16.2 / §18 / §20–§23 (§21.4 the density law), MOVE-GRAMMAR §1–§3 / §1.4.1 / §4.4.1–§4.4.3, CLERK-LAWS §2.4 / §2.6 (C2, C5), ARCH-COMPOSED-PROSE-v2 §2.5 (the annex grammar and its refusals; FACE_ROW_RE, T-F8) and §8.3 (the licence card), brief ADDENDUM 13 parts A and B (bars W1–W27), the rulings R-i…R-viii′, the refuter packet `refute-round-1.md` and the judge's rulings `JUDGMENT.md` (this pool's table and §4 notes N-1 and the two cross-variant rows).
THE BASELINE IS THE ANNEX AT HEAD `471ce894a83658b0e0fe480819a9f1dcd22605e3` (`docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`, the bold pool line at `:2819` and the rows at `:2820-2832`), which already carries the judge's three REVERTS in this pool (v2 f2, v3 f2, v3 f3). Two faces are changed — the two the judge named as CURE targets, both in variant 1. Every other face is byte-identical to HEAD.
TWO TARGETS IN, TWO CURED, NONE REFUSED. Same vids, same order, same one bracketed angle tag per row (`[ledger]` · `[street]` · `[visitor]`), no `[plain]`, none added, none removed, none merged; the slot sets are the parents' exactly (vid 1 `{settlement}` on every face · vid 2 `{}` on every face · vid 3 `{settlement}` on every face); every face is one sentence, as at HEAD.
Paste the block below under the pool's bold heading, in place of the pool's three numbered rows and their face sub-rows.

**`Internal Security`: no legal infrastructure**
1. `[ledger]` Neither a court to try a charge nor a place of confinement to hold under one is entered as standing at {settlement}.
   - `[face]` Against a wrong done inside the town, {settlement} keeps no court to try it, and no cell.
   - `[face]` The place, {settlement}, is entered with no gaol to hold under an offence and no court to try the offence.
   - `[face]` A court for a charge and a cell for the one it names are what {settlement} does without.
2. `[street]` A wrong done here comes before no court, and no cell is kept in the town.
   - `[face]` Here the town keeps no court against a quarrel, and no gaol.
   - `[face]` No cell here holds the one a charge names, and no court the charge.
   - `[face]` Neither the court a charge would come before nor the cell that would hold under it stands in this town.
3. `[visitor]` A stranger who comes to {settlement} with a complaint finds no cell for the one complained of, and no court to hear the complaint.
   - `[face]` The court a stranger would look for at {settlement} does not stand, nor does the cell.
   - `[face]` What a stranger notices first at {settlement} is that no cell holds the one a charge names and no court hears the charge.
   - `[face]` A stranger's complaint at {settlement} finds no court, and the one it names no cell.
--- NOTES

**THE DIFF AGAINST HEAD, measured.** `diff` of the thirteen lines above (the bold pool line and the twelve rows) against `RECEIPT_POOLS_DOSSIER_STATE.md:2819-2831` at `471ce894a` reports exactly two changed lines and no others: lines 3 and 4 of the block, the two judge-named CURE targets (variant 1, faces 1 and 2). The bold pool line, the three numbered rows and the other seven `[face]` sub-rows are byte-identical, character for character, including the leading three spaces, the backticked `[face]` marker and the trailing stops. The judge's three REVERTS in this pool (v2 f2, v3 f2, v3 f3) already stand at HEAD and are carried through untouched.

**TARGET 1 — variant 1, face 1 (the first `[face]` sub-row of the `[ledger]` row).** Was: *What stands at {settlement} against a wrong done inside the town is no court to try it, and no cell.* Now: **Against a wrong done inside the town, {settlement} keeps no court to try it, and no cell.** The cure is the refuter's named cure, with one hazard of its literal spelling fixed (below). The finding cured: skeleton §0.7 (c) TOTALITY over the read, the card's `may NOT: a second fact`, and CLERK-LAWS C2 read as the same-page class (a claim the engine's own record contradicts) — the specificational pseudo-cleft *what stands at X … is Y* is EXHAUSTIVE, so the face equated everything standing at {settlement} against an internal wrong with the two absences and thereby asserted that nothing else stands against one, while every tier this pool fires on (thorp, hamlet, village; skeleton §0.4, CONFIRMED against `institutionalCatalog.js`) carries a governing row whose `Dispute mediation` / `Community mediation` service the engine RECORDS and this card does not read, so the totality is undeniable and false at once. The judge ruled it NOT revertible, the draft of record carrying the same exhaustive cleft frame verbatim. The cure keeps the row's matter as a FRONTED ADJUNCT, which is the frame at its licensed extent (§0.7 (c): the matter a court would hear and a cell would hold is the standpoint of the reading, never a totality, never an event, never a practice, never a cause), and the cleft is gone with it — which also parts this face from its sibling face 3, the pool's other (inverted) cleft, an A5 pair the refuter's read-aloud note named. THE ONE DEPARTURE FROM THE REFUTER'S LITERAL TEXT: a comma after *town*. Without it the string reads *inside the town {settlement} keeps* — the appositive naming form, which is precisely the band-marker breach W3/W5 convicts at target 2 below, reintroduced by punctuation alone; the comma blocks that parse and changes no word and no claim. The law behind each claim in the cured face: `Against a wrong done inside the town` = the row's frame at layer NONE, a generic hypothetical of the STATE-KEY's own matter class (`Internal Security`), in the habitual present and never an event that happened (R-DST-B; the shipped v3's "a stranger wronged … discovers" is the form that is refused), with *the town* the record's generic subject carrying no tier value (§0.7 (d); W3 and W5 reach only "a town", "the village", "the town of X"); `{settlement}` = the card's bag, the one filled slot, carried exactly once and not sentence-initial (ARCH §2.5 T-F8; MOVE-GRAMMAR §1.4 wall 10); `keeps no court to try it` = R1, the `law` class's court member at its FALSE value by the class word (skeleton §0.8: never "the courts", "a courthouse", "a bench", "a magistrate"), on a ratified possessor verb — the town keeps, it does not settle, judge, hear or decide (W22, W23; the shipped v2's practice claim stays dropped) — with *it* bound to the frame's wrong and no person given an act or a fate; `and no cell` = R2, the prison member at FALSE by its class word, gapped onto the same verb so that the two reads stand as ONE keyed condition under one negating surface (§0.7 (a); R-v), not as two ABSENCE moves side by side (MOVE-GRAMMAR §1.4 wall 3), and the face does not open on an absence (R-ii/W10). No record noun and no citation anywhere in it (W24, A13; the card's SOURCE-UNRESOLVED), no force, watch, guard, reeve, headman, elders or custom asserted or denied (§0.4, §0.8; W13, W15, W20), no cause joint, no rate, no verdict, no tier, no badge word, no totality over persons, no digit, no em dash, no which-clause. The construction contract (W4) holds in the only sense it can here, the cleft being the breach itself: the face keeps its fronted matter, its read order (court then cell) and its gapped close, and takes an active possessor verb where it had a copula — which gives variant 1 four distinct constructions (f0 a correlative on the passive office formula; f1 a fronted adjunct with an active possessor verb; f2 an appositive on the passive formula with the reads reversed; f3 an inverted positive frame with no negation at all) and four distinct landing nouns (*{settlement}* · *no cell* · *the offence* · *does without*), which is the four-faces rule at the grain A5 measures (the subject, the order of the reads, the landing noun). A6 holds: the face's claim set (R1, R2, the frame's matter, `{settlement}`) is identical to its three siblings'. The thread (R-i, MOVE-GRAMMAR §1.4.1) is not engaged — the face is one sentence, as at HEAD — and every noun in it is at its read's layer (W20: court and prison at BODY negated; the town, the wrong and the slot at NONE; no HOLDER, ORGAN, POWER, ROLE, AGGREGATE or EXTERNAL BODY is reached). Cross-variant, the cured face stands near v2 f1 (*Here the town keeps no court against a quarrel, and no gaol*) in verb and gapping; that is R-DA-05 spread and not a wall — the judge ruled twice in this block that no ratified law reaches cross-variant construction echo, and only one variant of a pool renders for a town — and it was preferred to the within-variant A5 risk that every alternative verb ran into (*does without* is face 3's, *is entered* is faces 0 and 2's, and *holds a court* is the English idiom for an ACT, the same costume-in-the-syntax fault R-DA-18 convicts at the reverted v3 f2's "stands for").

**TARGET 2 — variant 1, face 2 (the second `[face]` sub-row of the `[ledger]` row).** Was: *The town of {settlement} is entered with no gaol to hold under an offence and no court to try the offence.* Now: **The place, {settlement}, is entered with no gaol to hold under an offence and no court to try the offence.** The cure is the refuter's named cure taken as written. The finding cured: W3 (a tier band carries its marker; the bare value contradicts the head record on the other tiers) and W5 (the naming-form article) read against CLERK-LAWS C2 and skeleton §0.4 — *the town of X* is the naming form, and it PREDICATES the band: it says {settlement} is a town. This pool is, in engine terms, the below-town value of its row (a town's required `Town hall` always sets `hasCourtSystem`, so a town's Internal Security rung is one of the two `court` pools; `priorityHelpers.js:46-55` against `institutionalCatalog.js`, CONFIRMED, and entailments D-15 and L-16 say the same), so the page's own head record reads a tier below town on every firing and the face contradicted it. The judge ruled it NOT revertible under J-2 (c) and N-1: the draft face is a sibling paraphrase of the KEPT face 0 (the same correlative, predicate and order) and repeats face 3's noun pair, so a one-for-one revert would install an A5 breach in place of a W3 one. The cure drops the band word and changes nothing else: `The place, {settlement},` = the appositive naming form on a NONE-layer generic noun, which §0.7 (d) ratifies as one of the two zero-risk spellings for this pool (*the place* and the bare slot), the slot still carried exactly once and still not sentence-initial (T-F8); `is entered with` = the office's own formula, the `[ledger]` stance's one lawful surface (R-vi/W7), which cites nothing, names no holder and uses no record noun on a SOURCE-UNRESOLVED pool (W24, A13 — *the roll shows*, *the books*, *the record sets down* are all refused here, and the stance licenses none of them); `no gaol to hold under an offence` = R2 by a lawful class word for the absent thing (skeleton §0.8: a cell, a gaol, a lock-up, a place of confinement; never *the stocks*, never a gaoler or a prisoner, no person as determinant); `and no court to try the offence` = R1 by its class word, sharing the one preposition so the two reads stand as one keyed condition (§0.7 (a)) with the offence carried across both limbs as the frame's matter at layer NONE. The construction contract is kept exactly (W4): the passive office formula, the REVERSED read order (prison then court, the variant's only reversal, which the refuter's read-aloud note asked to be kept through the cure) and the landing noun *the offence* are all untouched, so the face stays distinct from f0 (a correlative whose subject is the two bodies and whose landing noun is the slot), from f1 (an active possessor verb behind a fronted adjunct, landing on *no cell*) and from f3 (an inverted positive frame landing on *does without*). A6 holds: the claim set is unchanged by the cure and identical to the siblings'. Nothing licensed was lost and nothing was added: the density floor (§21.4) is met by keeping the whole of the shipped-and-refined clause that was lawful and dropping only the two words that carried the unlicensed band, and what replaces them is the record's own neutral naming noun rather than a smoothing (the tier is a wiring fact of §0.4, never a licence: the card prints no tier read, so the band could not be stated correctly either).

**REFUSED TARGETS: none.** Both were curable within the card, the `[ledger]` stance and the bars; neither had to be left as it stands. No other face was touched, so the judge's KEEPs and REVERTS stand exactly as HEAD carries them, and the pool still reads three variants with four faces each (12 faces; NEVER TRIM: none added, none removed, none merged).
