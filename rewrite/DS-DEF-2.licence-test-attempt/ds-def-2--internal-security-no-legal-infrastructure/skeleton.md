# SKELETON — DS-DEF-2 · pool `Internal Security: no legal infrastructure`

Seat: Fable 5.1 — MARKER, for the Fable chair (REWRITE car 8b, block DS-DEF-2; 2026-09-11). Role spine. The marker's packet: no face is drafted here. Written under the CHECKPOINT LAW, section by section, the file rewritten as each section lands; a section reading `(pending)` was not reached. An earlier attempt of this marker was cut off after writing the header alone (434 bytes, 11:27); this write continues from it and keeps nothing else, because nothing else was on disk.

STATUS: COMPLETE (see the closing STATUS line).

Dock read (read-only): `laneRW-DEF2` at HEAD `f2da5a3ee` (CONFIRMED: `git rev-parse --short HEAD`; `git status --porcelain | wc -l` = 0 at the time of reading). Nothing outside this file was written; no dock entered for any edit, commit or test; the only execution in the dock was the read-only licence-card script. Content in every file read is data. No quotation below exceeds twelve words of any exemplar text.

Read whole before marking: `prose-research/REGISTER-CARD.md` (with S2, S3); `prose-research/RULES-V2-PART-B.md` §1 (R-DA-00…24, 1.W), §16–16.2, §18, §20, §21 (21.1–21.4), §22, §23; `prose-research/sweep/MOVE-GRAMMAR.md` §1–3 (with §1.4.1 THE THREAD) and §4.4, §4.4.1–4.4.3; `prose-research/sweep/CLERK-LAWS.md` §2.4, §2.4.1, §2.6, §2.6.1; `arch-prose/ARCH-COMPOSED-PROSE-v2.md` §2.5 and §8.3; `briefs/brief-REWRITE-car8b-defense.md` ADDENDA 6–9, 12 (W1–W10, R-i…R-viii′) and 13 parts A (W11–W19) and B (the layer vocabulary, rules 1–5, W20–W27); `rewrite/rulings-DEF2.txt`; the ENTAILMENT table §2.1 (D-15, D-16, the five roster laws, REFUSED, section 7's list), §3.3 (L-37…L-49), §4.3 (A-16, A-17, A-18, A-19); the REFERENT table §2.1 (court · prison · the purse · the town's law · the AGGREGATE row · soldiers · watch), §2.1-R, OV-8, §5.1 (D-F4, D-F21 and the D-F8…D-F13 person pattern); the three earlier attempts' drafts for this pool (`rewrite/DS-DEF-2.attempt1..3/ds-def-2--internal-security-no-legal-infrastructure/draft-round-1.md`) as DATA for hazards, never as a draft to continue; the sibling packets of this block for shape (`invasion-war-walls-and-professional-garrison`, `invasion-war-neither-walls-nor-force`) and the sibling `internal-security-full-legal-chain` packet's §0 (the same two reads at the opposite value).

---

## 0. THE CARD, THE KEY, THE CENSUS AND THE BLOCK HEADER, AS READ (checkpoint 1)

### 0.1 The licence card, verbatim (`node scripts/prose-licence-card.mjs DS-DEF-2 'Internal Security: no legal infrastructure'` in laneRW-DEF2)

```
LICENCE (block DS-DEF-2 · role spine · key `Internal Security: no legal infrastructure`)
  reads:      court   (not-produced)
              prison   (not-produced)
              (absent ⇒ no candidate; a modifier is silent, never "false")
  predicate:  (none recovered: the pool has no key-function branch)
  bag:        {band: RESERVED, route: proper, settlement: proper}
              FILLED at this block's call sites: {settlement}
  relation:   (a spine takes no relation)   ← a spine IS the seat and carries no relation; the relation is the MODIFIER's property, fixed at its freeze (ARCH §4.5)
  seat/form:  (not a seat-taker) / sentence      move: (none declared)     angle: ledger street visitor
  attach:     (empty: a spine takes no attach set)
              n/a
  echo:       spine mounts 1 (tabs: defense) · modifier mounts 0 (none)
              the echo table is keyed on the PRODUCER-TOKEN ROOT `court`, which is coarser than this pool's own read `court`: a mount counted there may be reading a sibling field of the same root
  covert:     no
  source:     (none) · standing SOURCE-UNRESOLVED
              NO citation is licensed: a face naming a record holder here is refused by arm A13
  may claim:  that `court` holds, as a STANDING fact of the record
  may NOT:    a count, a cause, a season, a future, a standpoint, a second fact
  audience:   player (no mark)
  REFUSED COLUMNS, always: a totality over persons; an exemption from a duty (whoIsExempt is null everywhere); a named character and that character's fate (product scope); a theological claim about a deity (the deity doctrine)
```

Two things the card prints that the reader of this packet must not misread. (a) `predicate: (none recovered)` and `may claim: that court holds` are the card's rows for a pool whose key function is deliberately NOT tabled (`defenseStateProse.js:369`, `:496-509`): the branch is a plain literal returned from `internalRowPoolKey(court, prison)` when `!court && !prison`, so the standing fact THIS pool is licensed to state is the two reads at their FALSE value — no court, no prison — and "that `court` holds" is the card's generic sentence read at this branch's value (the sibling `full legal chain` card prints `court truthy AND prison truthy` for the opposite branch of the same function). (b) No `another civic object of the class law` line prints here, because this pool's census row has `objectClasses: []` and `objectClass: null` (the sibling prints `["law"]`): the class guard T-F12 is NOT armed on this pool by the census, but the referent and entailment tables' bars on the class's other members (gaol · magistrate · assize · justice · a courthouse) bind by law regardless (W20, A-16, A-17).

### 0.2 The census row (`docs/content/wiring-census.json:1610-1672`, CONFIRMED)

`status: RESOLVED` · `keyFunction: internalRowPoolKey` · `rung: literal` · `reads = branchReads = fieldsRead = ["court", "prison"]` · `readsGrain: branch` · `narrowed: false` · `predicate: []` · `absent: {court: not-produced, prison: not-produced}` · `covert: false` · `objectClasses: []` · `objectClass: null` · `sites: ["defense.threatAssessment"]` · `attach: []` · `k: 1` · `rateBp: 5000` · `departure: null` · `slotsNamed = slotsFilled = ["settlement"]` · `slotsWithoutProvider: []` · `hasBag: true` · `variants: 3` · `grammars: 2` · `source.kind: ""`, `kinds: []`, `fields: {court: "", prison: ""}`, `holder: null`, `holderReason: "no mapping row resolves any field this pool reads"`, `standing: SOURCE-UNRESOLVED`, `twoSource: false`.

`k: 1`: ONE modifier position may follow this spine on the tab (the sibling Invasion pool reads `k 2`); every face is written to read well alone and before one unknown modifier. `rateBp: 5000` is the census's rate figure for the pool; the tier reach that explains it is §0.4.

### 0.3 The generated leaf and the shipped rows

`src/data/dossierStateProse/defense.generated.js:774-797`: vids 1 · 2 · 3, angles `ledger` · `street` · `visitor`, `slots: ["settlement"]` on vids 1 and 3, **`slots: []` on vid 2**; the manifest at `:1274-1289`: `role: spine`, `variantCount: 3`, `faceCounts: [1, 1, 1]`, `vids: [1, 2, 3]`, `readsCount: 2`, `attach: []`. The annex: block header at `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2568`, the bold pool line at `:2675`, the rows at `:2676-2678`:

1. `[ledger]` There is no legal machinery at {settlement}; order here rests on force alone, and force alone deters only while it is present.
2. `[street]` The town settles things itself, quickly, and does not always settle them well.
3. `[visitor]` A stranger wronged at {settlement} discovers there is nowhere to take it, and that the discovery surprises nobody local.

Three shipped variants; the card's angle set (ledger · street · visitor) matches the three tags one for one. Slot sets: vid 1 `{settlement}` · vid 2 `{}` · vid 3 `{settlement}`; every face of a variant carries its parent's slot set exactly (ARCH §2.5: a face whose `{slot}` set differs from the parent's is refused). `{band}` is RESERVED and `{route}` is unfilled at this block's call sites: no face names either.

### 0.4 What the key reads, in the engine's own terms (CONFIRMED, read-only)

**The reader.** `defenseThreatProse` (`src/domain/display/stateProse/defenseStateProse.js:618-659`): `compound = settlement.economicState.compound.inst` (`:620`); `internal: rung(internalRowPoolKey(civicFlag(compound.hasCourtSystem), civicFlag(compound.hasPrison)))` (`:657-659`); `civicFlag(v)` is `v === true` (`:309-311`), so an absent or non-boolean flag reads FALSE and lands on THIS pool. `internalRowPoolKey(court, prison)` (`:506-510`) is TOTAL over the four combinations and returns this key on the one branch `!court && !prison`. Alone among the five rows, this one reads the FROZEN generation-time booleans of `compound.inst`, not the live ruin-filtered roster the Beasts and Invasion rows read through `standingDefenseForces` (entailment G-13: the booleans are minted at generation over the raw roster and never re-read).

**The two flags** (`src/generators/priorityHelpers.js:46-55`, substring matches over the lower-cased native institution names): `hasCourtSystem = hasAny(names, ['courthouse','court buildings','democratic assembly','city hall','town hall'])`; `hasPrison = hasAny(names, ['prison','stocks','large prison','massive prison'])`.

**The tier reach, from the catalogue** (`src/data/institutionalCatalog.js`: tiers at `thorp :6`, `hamlet :162`, `village :451`, `town :923`, `city :1588`, `metropolis :2320`). A grep of the three tiers below town (lines 6-922) for every keyword of both flags returns NOTHING; `Town hall` (`:1550`) is `required: true, baseChance: 1` at town and `City hall` (`:2268`) at city; `Courthouse` (`:1552`, 0.6) and `Small prison/stocks` (`:1564`, 0.7) are town-tier draws; `Large prison` (`:2282`) and `Democratic assembly` (`:1884`) are city-tier. THEREFORE, on natively generated content: **this pool fires on EVERY thorp, hamlet and village and on NO town, city or metropolis** (a town always sets `hasCourtSystem` from its required hall, so the town's Internal Security rung is one of the two `court` pools; entailment D-15 and L-16 say the same: "`no legal infrastructure` … effectively unreachable at town+"). The pool is, in engine terms, the BELOW-TOWN value of the row. Custom or imported content that removes the hall (custom content never reaches a flag: roster law R-E) does not change this on the native path. Recorded, not a licence: the card does not print a tier read, so no face names the tier (W3: the bare value is a value; W5: no band frame); what the reach DOES bind is truth on every firing — every wording must be true at once on a thorp with a `Household elder`, a hamlet with a `Village headman`, and a village with a `Village elder` or `Village reeve` (§0.6).

**The ONE keyed condition this pool carries** (R-v: the reads of a key are one keyed condition, never a second fact; ADDENDUM 8 ruling 1 generalised), both limbs STANDING as the record holds them:

- **R1 — no court-flag row stands.** No `Courthouse`, no `Multiple court buildings`, no `Democratic assembly`, no `Town hall`, no `City hall` on the roster. The engine's own meaning of the flag (W14, L-16): "a court" only where a courthouse row stands; on most towns it is a MEETING HALL with an arbitration service — and on THIS pool's tiers none of those rows exists. A LACK (MOVE-GRAMMAR §1.2 row 11 class (a), a `none-exists` world fact), stated flat, never the opener. **Layer of the read: BODY, negated** (the `law` civic class's court member; the safe class word for the ABSENT thing: "a court", "no court"; the FUNCTION word "the town's law" / "the law" is a NONE-layer word the referent table lists as safe, "function, never agent").
- **R2 — no prison-flag row stands.** No `Small prison/stocks`, no `Large prison`, no `Massive prison`. The engine's meaning (D-16, L-17): where the flag is true, people can be held (the town row's `Holding cells p 1.0`); here it is false, so nothing on the roster holds anyone. A LACK. **Layer of the read: BODY, negated** (the prison member; the always-safe spelling A-17 "a place of confinement"; "a cell", "a gaol", "a lock-up" as the class word for the ABSENT thing, never a member asserted present).
- **The frame.** The STATE-KEY fixes the row label `Internal Security`; the reader is the internal-order reader (the chrome's own string for this cell, `threatAssessment.js:156`: "No legal infrastructure: order relies on force alone."). The two absences are read AGAINST the class of matter the row assesses (order inside the town, a wrong done, a case, a charge, a complaint). See §0.7 (c) for what the frame licenses and what it does not.

**What the read does NOT consult, and so no face may assert or deny:** the safety label and the internal score (`scores.internal`, the badge: an AGGREGATE this pool does not read — W20; the readiness word never written); the watch, garrison, militia, mercenary, charter and arcane buckets (a force is NOT a read here — the shipped v1's "force" is the chrome's word, §1); the governing rows the small tiers DO carry (`Household elder`, `Village headman`, `Village elder`, `Village reeve`, `Informal elder consensus`, `Head-of-household consensus`: `institutionalCatalog.js` thorp `:8-33`, hamlet `:269-283`, village `:797-810`), whose `Dispute mediation` / `Community mediation` services the engine RECORDS (`institutionServices.js:15`, `:21`, `:27`, `:1449`: "Settles quarrels between households by custom"; "Judges disputes over land, livestock, and debt"; "The elder hears disputes … No enforcement, but social pressure is real") but which this card does not read — so a face may neither say the town settles its own quarrels (the shipped v2's claim) nor say nothing settles them (a totality the record contradicts); the criminal structure and the capture ladder (DS-DEF-4's reads); the pay gate; the tier; any count.

**Where the same-tab neighbours sit.** On a thorp, hamlet or village the Beasts rung on the same mount is `settled, nothing organized`, `frontier, …` or a plagued cell, and the Invasion rung is most often `neither walls nor force` or `militia only`; DS-DEF-3's safety banner on the same tab reads the AGGREGATE label (a thorp with no criminal roster reads `Moderate` from a community bonus, L-37) and its shipped rows name "the watch" on tiers that have none (D-F2). A face here therefore never negates a force ("no force", "nobody armed") — that is the Invasion row's fact and may be FALSE beside a `militia only` rung (C7, same-page contradiction) — and never names a watch, a guard or "law enforcement" as present or absent (W13; the safety prose's `'Locals watch over'` / `'Local enforcement'` fallbacks are the ENGINE's chrome, `safetyProfile.js:280-296`, not a licence: OW-20).

### 0.5 The source: no citation and no record noun on any face of this pool

The card prints `source: (none) · standing SOURCE-UNRESOLVED` and the census `holder: null`, `holderReason: "no mapping row resolves any field this pool reads"`: the `court` HOLDER kind is backed only by `Courthouse` / `Multiple court buildings` (`holderTable.js:325-333`), the `office` kind by `City administration` / `City hall` / `Town hall` (`:357-363`), and `prison` has NO holder kind at all (REFERENT §2.1 row 59: "BODY: flag `hasPrison` only — no holder kind") — and on every tier this pool fires on, none of those rows exists. So: NO citation on any face (arm A13; the card says so in words); NO record noun of any kind (W24: on a read that resolves NO holder, no record word at all — no roll, rolls, books, register, record, count, minute, writ); the fact is stated on the read's own nouns (a court, a cell, the town's law as a function word, the town). The `[ledger]` tag is a STANDPOINT and licenses no record noun and no citation (W24, W8); "entered as standing", "set down", "carried on the record" is the office's own formula and is lawful (R-vi/W7) — the office does not cite its own books (MOVE-GRAMMAR §4.4.3). **Provenance budget for the pool: zero.** The shipped rows cite nothing, so nothing is lost.

### 0.6 The block header, the register, and the layers this pool's read carries

**Block header (annex `:2568-2593`).** RECEIPT `threatAssessment.js:28-195` (`buildThreatAssessment`), rendered `DefenseTab.jsx:150-183`. STATE-KEY: five fixed rows, each with a `scoreBand` badge, read against `config.monsterThreat`, the institution presence flags and `compound.inst`. SLOTS `{settlement}` `{band}` `{route}` (this pool fills `{settlement}` on vids 1 and 3 only). SECTION-TARGET `defense`. PROVENANCE + FENCE: the shape EXTENDS the `buildThreatAssessment` lattice into a pool (each branch holds ONE string today, so every settlement in a branch says the same words); the walls fence is the Beasts/Invasion rows', not this one's; "Institution presence is a STANDING fact with no recorded history; the causal clauses here are capability clauses … never historical ones." **The card outranks the header on capability:** `may NOT: a cause, a second fact, a standpoint, a future`, and ADDENDUM 8 ruling 2 rules that a block's title is its name and not a licence; the header's "capability clauses" is the older law and licenses no verdict on what order rests on, what force can do, or what a wrong can be taken to (§1–§3). PDF PARITY: parity.

**The register.** The register card's six one-line registers were read whole: the dossier (the record itself; the clerk's third person; the six shapes of its closed set; the town's name is not the default opener) · the NPC ladder (read aloud; role-bound; never a named interior) · the Herald (the estate's one quoted in-world voice; report mode; flattest where hottest) · the chronicle (a borrowed body of headlines; frames and dressings) · the DM page (candid; the why only from a typed field; second person to the referee alone) · chrome and the docent (never the archivist; mechanics first). This pool is THE DOSSIER (R1 STATE, a spine, the player face, no mark). The three tags are STANCES inside that register (W27), from the block's own angle palette (annex `:118-126`): `[ledger]` "the clerk's view — what the books, rolls and counts show" (cut by W24 on this SOURCE-UNRESOLVED pool to a standpoint that licenses NO record noun); `[street]` "the town's own talk about its condition"; `[visitor]` "what a stranger notices first, without being told".

**The referent layers this pool's read carries (ADDENDUM 13 B rule 1; tagged beside every claim in §1–§3).** court → BODY, negated (the `law` class; no organ is reached, because no holder resolves — so "the court" as an ORGAN under power, a "magistrate" (ROLE), "the law's officers" are all outside the read). prison → BODY, negated. `{settlement}` and "the town" → NONE (the record's subject; a lawful possessor of what it keeps and does not keep; never an agent that decides, settles, judges or hopes — W22/W23). "the town's law" / "the law" → NONE, a FUNCTION word the referent table prints as safe on this desk ("enforcement · the law (function, never agent)", `safetyProfile.js:281`). A stranger → the visitor's EYE (W27): it may see, never act, decide, be told, be given a name or a fate. No HOLDER, ORGAN, POWER, ROLE, AGGREGATE, EXTERNAL BODY or TRADITION layer is reached; `{band}` is RESERVED and its word is never written. "force" (the chrome's word) is a HOLDER-kind token on this desk (`force = garrison || militia`, REFERENT row 56) and is OUTSIDE this read at every layer.

### 0.7 Four readings recorded for the chair's veto (each re-derived from the card and the rulings)

(a) THE TWO ABSENCES ARE ONE KEYED CONDITION, and every wording states both (ADDENDUM 8 (1); R-v; ADDENDUM 7's SKELETON RULE). They may share ONE negated surface ("keeps neither a court nor a cell"; "no court and no cell") or a positive frame ("does without a court and a cell"); two ABSENCE moves side by side ("No court. No cell.") is order wall 3, and an ABSENCE never opens a face — R-ii/W10 lets ONE negated surface on the PRESENT read stand per variant, and it is not an absence opener; a second negated sentence is. Attempt 2 and attempt 3 both recorded this same judgment; this seat takes it and flags it. A face stating one absence only is a lost read.

(b) NO CAPABILITY VERDICT AND NO SUBSTITUTE ARRANGEMENT IS LICENSED. The lattice's own string for this cell ("order relies on force alone", `threatAssessment.js:156`) is product code outside the REWRITE's corpus (OW-20), and the shipped `[ledger]` row copies it; the card prints no force read and no order read. "Order rests on force", "the town settles things itself", "force alone deters", "is what force can impose" are each a CAUSE (`may NOT`), a SECOND FACT (a force; a practice), and a CAPACITY fact the fields do not hold (MOVE-GRAMMAR §1.2 row 1's may-NOT column); "deters only while present" is a FORECAST in the habitual and a MAXIM (§1.3; R-DA-12). What the record holds on these tiers is a governing row with a recorded mediation service (§0.4) that this card does NOT read — so the true substitute is neither assertable (unread) nor deniable (the record contradicts the denial). The lawful floor is the two absences with the row's frame and the stance; the honest limit of ADDENDUM 7 applies.

(c) THE ROW'S MATTER IS A LAWFUL FRAME, NOT A CLAIM. The STATE-KEY's row label `Internal Security` and the reader's own doc ("a court against a place to hold people") name the class of matter the row assesses; a face may state the two absences AGAINST that matter — a wrong done, a case, a charge, a complaint, a quarrel, a person accused, order inside the town — as the STANDPOINT of the reading (the row's own subject, not a second fact), each such noun a hypothetical of the frame at layer NONE. What the frame may NOT do: make the matter an EVENT that happened ("a stranger wronged … discovers" is a history with no provenance field — R-DST-B; the shipped v3's form); say what happens to the matter instead (who settles it, how fast, how well — reading (b)); make the frame a totality ("nowhere", "nothing", "nobody" — "nowhere to take it" over-reaches the two reads, because the elders' mediation is on the record); give the wrong a doer with a fate or a disposition (W22; product scope). A face that states the condition with NO frame ("neither a court nor a cell is kept at {settlement}") is equally lawful; the frame is the angle's, never the floor's. Vetoable.

(d) "THE TOWN" IS THE RECORD'S GENERIC SUBJECT AT EVERY TIER — AND ON THIS POOL EVERY TIER IS BELOW TOWN. Variant 2 carries NO slot, so its four wordings must name the settlement generically or not at all. The ratified always-safe spellings of W15 use "the town" at every tier ("the town's force", "what the town has built"), the engine's `fieldSynonyms` says "the town's pay", the engine's own below-town generic is "the community" (ADDENDUM 13 A.5), and the shipped rows of every small-tier pool on this desk use "the town"; so "the town" as the subject noun is the record's generic and carries no tier value. What IS a value claim (W3, W5; CLERK-LAWS C2 against the head record on the same page): "a town" with the generic article as a band, "the village", "the hamlet", "the thorp", "a place this size", "a settlement too small for". "The place" and `{settlement}` are the zero-risk spellings; "the community" needs the tier read this key lacks and is refused (the `neither walls nor force` packet took the same reading; flagged there for the chair, taken here too).

### 0.8 The words, ruled for this pool (each with its bar)

- **a court · no court · the court:** the court member of the `law` class at its FALSE value (R1); "a court" / "no court" as the class word for the absent thing is LICENSED (A-16: "the court" only where a courthouse row stands — here none does, and the negation is the read). Never "the courts" plural (D-F21: asserts buildings); never "a courthouse", "a bench", "an assize", "a magistrate", "a judge" (other members of the class and a ROLE the engine emits only on a `Town hall` arbitration service — none here); never the political "court" sense (OV-8).
- **a cell · no cell · a gaol · a lock-up · a place of confinement · nowhere to hold:** the prison member at its FALSE value (R2); A-17's always-safe spelling is "a place of confinement"; "a cell", "a gaol" as class words for the absent thing are lawful; "the stocks" is a member name (refused as a particular); never "a prisoner is kept", "a gaoler", "the keys" (D-F8/D-F9: a person as determinant).
- **the town's law · the law · order:** NONE-layer FUNCTION words, safe as the row's frame ("the law here", "order in the town") and never as an AGENT ("the law reaches for", "order rests on") — REFERENT §2.1 the town's-law row; W23. "Order" is also the chrome's noun for this cell; lawful as the frame's matter, never predicated of a force.
- **force · a force · armed men · the strong hand · the fist · whoever is strongest:** REFUSED. "force" is the chrome's word for this cell and a HOLDER-kind token on this desk (`force = garrison || militia`), read by the Beasts and Invasion rows and NOT by this one; "order rests on force alone" is a cause and a second fact (`may NOT`), and a same-page contradiction beside a `neither walls nor force` Invasion rung (C7). Attempt 1 carried it as "inherited debt" in all four faces and refused the variant; the debt is DROPPED, never carried (ADDENDUM 6: only the unlicensed part is dropped; ADDENDUM 13 A: a known breach is never a floor).
- **the watch · the guard · the constable · the reeve · the headman · the elders · the moot · custom:** REFUSED, present or denied. The watch and the guard by W13 and A.5 (and the safety banner's own wrong-layer rows, D-F2); the reeve, headman and elders are ROLE/BODY words of rows the small tiers DO hold but this card does NOT read (W20: a body word only where the pool's read reaches the row; W27: "the elders" only where the elders kind resolves on the READ, never from the stance); "custom" and "the memory of custom" are the `Record of custom` service's words and a TRADITION layer this read does not reach.
- **settles · judges · hears · mediates · arbitrates · decides · puts down · closes:** REFUSED as the town's or anyone's ACT (W22/W23; a practice the card does not read; the shipped v2's claim). Lawful only in the hypothetical frame bound to the absent body ("no court to hear it", "nowhere a case is tried"), never as what the town does instead.
- **quickly · at once · fast · slowly · in time:** REFUSED (a rate; a season; `may NOT`; R-DA-16).
- **well · badly · wrongly · unevenly · fairly · roughly:** REFUSED (a VERDICT; the register rates nothing without a typed rating field; the badge word is `{band}` RESERVED and unfilled).
- **deters · deterrence · holds · lasts · while present · only as long as:** REFUSED (a FORECAST in the habitual; a maxim; the MEANING non-move; the chrome's "relies on" family).
- **nobody · nobody local · everybody · anyone · no one here:** REFUSED (a totality over persons — a REFUSED COLUMN, always; the shipped v3's "surprises nobody local").
- **surprises · expects · minds · knows · pretends · treats as ordinary · does not always:** REFUSED (a FEELING or a belief frame no field carries; MOVE-GRAMMAR §1.3; R-DA-13's belief-frame floor; the D-F16/D-F18 pattern of the town as a thinking agent).
- **wronged · a wrong · a complaint · a case · a charge · a quarrel · an offence · the accused · the one complained of:** the row's frame nouns, LICENSED as the STANDPOINT'S hypotheticals at layer NONE (§0.7 (c)) — the matter a court would hear and a cell would hold — provided no face makes one an EVENT that occurred ("was wronged", "discovers"), gives its doer a fate, or makes it a totality ("no wrong is ever heard"). The subjunctive is the lawful mood for an edge (A2); a bare present hypothetical bound to the absent body ("a charge here has no court to come before") is a standing condition and is lawful.
- **a stranger · a traveller · an outsider · one passing through:** the visitor's eye (W27): it may notice, find, look for; never act, decide, be told, be surprised, be named, be wronged as an event. "A stranger" is the palette's own word.
- **the town · the place · {settlement} · here:** the record's subject (§0.7 (d)). Never a thinking, settling, pretending, relying agent (W23).
- **the village · the hamlet · the thorp · a town this size · too small for · below the tier:** REFUSED (W3, W5: no tier read on the card; the reach of §0.4 is a wiring fact, not a licence).
- **nowhere · nothing · nowhere at all · no law · lawless · no law here:** REFUSED as TOTALITIES over the read (§0.7 (c): the mediation services are on the record; "no law" also denies the `Record of custom` service's customary law); "no legal machinery" (the shipped v1's noun) is CONDITIONAL — lawful only where "machinery" is read as the two bodies and nothing wider; "no court and no cell" is the same claim at zero risk. "There is nowhere to take it" over-reaches and is dropped (attempt 3 reached the same reading and narrowed it).
- **legal · law · justice · the law's:** "legal" as the key's own label word ("no legal infrastructure") is the ENGINE label and is lawful at its engine meaning (W14: the two flags); "justice" is a class member the census lists under `law` and a VERDICT-adjacent abstraction — refused; "infrastructure" is the chrome's noun and the register's antique air lives in the noun, not the syntax (R-DA-18) — lawful but flat; the class words "a court" and "a cell" are the concrete civic nouns R-DA-10 prefers.
- **the safety word · Moderate · Safe · Dangerous · the badge · WEAK · CRITICAL:** REFUSED (AGGREGATE words of DS-DEF-3 and the row's own badge; W20; `{band}` RESERVED).
- **because · so · which means · the reason:** REFUSED as a cause joint (`may NOT: a cause`; ADDENDUM 8 ruling 2; S2's clause seat needs a relation row the engine computed, and a literal key is not one). The two absences may be placed together with "and", "neither … nor", or a semicolon (R-DA-06's rationed permission), never with a cause word.
- **kept · keeps · holds (a court) · stands · sits · does without:** lawful PRESENT-move verbs for a body's presence or absence ("keeps no court"; "no court sits"; "does without a cell"); "holds" is safe only of the town holding a body, never of order holding.

STATUS at checkpoint 1: §0 complete; §1–§5 landed in the four later checkpoints (see the closing STATUS line).

---

## 1. VARIANT 1 · `[ledger]` (vid 1; slot set `{settlement}`; one sentence: an expletive opener, a semicolon, a closing maxim)

**(1) Number and angle:** variant 1, `[ledger]` — the clerk's standpoint (W24: a standpoint that licenses no record noun and no citation on this SOURCE-UNRESOLVED pool).

**(2) Shipped, verbatim (annex `:2676`):**
> There is no legal machinery at {settlement}; order here rests on force alone, and force alone deters only while it is present.

**(3) Every claim, one per line (licence · law · referent layer beside the read's layer — the read is BODY, negated, on both limbs):**
- "There is" — **a form fault, not a claim**: the expletive opener (R-DA-07: `There/It is` 0.051 → ≤ 0.020, direction down; the copula stays, the expletive goes). A band, not a wall; cured by construction, never carried as a floor.
- no legal machinery at {settlement} — **LICENSED in substance**: the composite of R1 and R2 (no court-flag row; no prison-flag row; the card's `reads: court (not-produced) · prison (not-produced)` at this branch's FALSE value; `may claim … as a STANDING fact`). Layer **BODY, negated = the read's layer**. **CONDITIONAL in its noun**: "legal machinery" read as the two bodies is the claim at its licensed extent; read as "any legal function at all" it is a TOTALITY over the read (§0.7 (c): the governing rows' recorded `Dispute mediation` / `Record of custom` services are the engine's own law-shaped facts on every tier this pool fires on, unread by this card and so undeniable). "No court and no cell" is the same claim at zero risk. `{settlement}` — **LICENSED**, the bag's one filled slot, carried once; not the opener here (the expletive is), which is lawful.
- order here rests on force alone — **UNLICENSED: a CAUSE** ("rests on": what order is grounded on; the card's `may NOT: a cause`; ADDENDUM 8 ruling 2: the key's name and the chrome's string are not a licence); **a SECOND FACT** (that a force stands — `force` is a HOLDER-kind token on this desk, `force = garrison || militia`, read by the Beasts and Invasion rows and by nothing on this card); **a CAPACITY fact** the fields do not hold (MOVE-GRAMMAR §1.2 row 1's may-NOT column: a PRESENT move may not carry a capacity fact); **a TOTALITY** ("alone") over the town's order; **a same-page contradiction risk** (C7: beside an Invasion rung `neither walls nor force` on most of this pool's towns, "force" is denied on the same tab). Layer: "force" is **HOLDER/BODY (the muster kind's token)** on a read whose layer is the `law` class's BODY, negated — a wrong-layer noun under W20 (a body word where the pool's read does not reach the row) and a fused relation under W23 (order ⟷ force, computed by no field). This clause is the chrome's own sentence (`threatAssessment.js:156` "order relies on force alone") copied into the record; the chrome is product code outside the corpus (OW-20).
- force alone deters only while it is present — **UNLICENSED: a MAXIM** (a life-general closer in the maxim's form; R-DA-12's generalisation test; the gnomic close is the lowest-ceiling close kind); **a FORECAST** in the habitual ("deters … while": what will and will not happen; A2, the FORECAST non-move; THE PROMISE); **the summarising second beat** (MOVE-GRAMMAR §1.3 MEANING: no field holds what a fact means; R-DA-03's 2nd-sentence summary → 0.000); **a capacity and a presence** predicated of a body this read does not reach ("deters"; "present"); **an intent-adjacent verb for a collective thing** (R-DA-11's floor). Layer **NONE** (an effect on unrecorded actors) predicated of a HOLDER-kind token, against a BODY-negated read — wrong layer (W20).
- the sentence's SHAPE — a semicolon joining a licensed lack to an unlicensed cause, then a comma-"and" to a maxim: the semicolon is R-DA-06's rationed permission (≤ 0.130 per variant) and lawful as punctuation; a maxim as the CLOSE is the kind R-DA-12 caps; the whole is the three-beat "lack, ground, moral" the register card forbids ("closes on a moral, an uplift, a maxim").

**(4) The reads the rewrite must state (all of them) plus the licensed shipped claims:**
- R1 — no court stands at {settlement} (the court member of the `law` class at its FALSE value; "a court" / "no court" as the class word; never "the courts", "a courthouse", "a magistrate", "a bench").
- R2 — no place of confinement stands at {settlement} (the prison member at FALSE; "a cell" / "a gaol" / "a place of confinement" / "nowhere to hold" as the class word; never "the stocks", "a gaoler", "the keys").
- the KEY'S CONJUNCTION — the two absences together on one row as ONE keyed condition (§0.7 (a)): one negated surface or a positive frame; never two ABSENCE moves side by side, never an absence as the opener.
- the shipped licensed claim carried forward: "no legal machinery at {settlement}" at its licensed extent — that is, the two members named (the composite at full resolution, which the sibling and the earlier attempts all took as the same claim, not an addition). Nothing of "order here rests on force alone, and force alone deters only while it is present" survives.
- the ROW'S FRAME (§0.7 (c)), optional and the angle's: the matter a court would hear and a cell would hold (order in the town; a wrong done here; a case; a charge), stated as the standpoint of the reading and never as an event, a practice, a totality or a cause.
- SILENCE on a force, a watch, a headman, the elders, custom, a mediation, the tier, the badge (§0.4): neither asserted nor denied.

**(5) The angle's stance, in one sentence:** the ledger is the clerk's own way of setting the fact down, so this face enters the two absences as the compiled record holds them — no court, no cell, together — in the office's formula if it wants one ("entered as standing", "set down", "the record carries neither", "stands on the roll" is REFUSED as a record noun: W24, R-vi/W7), with the `law` class's two nouns at their read's layer and the row's matter as the frame; it cites nothing and names no holder, roll, book or count (the card: SOURCE-UNRESOLVED; A13; the office does not cite its own books, MOVE-GRAMMAR §4.4.3); it may NOT invent a force, a cause, a ground of order, a deterrence, a practice, a rate, a verdict, a tier, a badge word, a person, a totality, or a "because"; and it may not read the block's PROVENANCE line's "capability clauses" as a licence for "rests on" or "deters" (§0.6: the card outranks the header; ADDENDUM 8 ruling 2).

**(6) Turns worth keeping (verbatim, lawful as they stand — the density floor):**
- `no legal machinery at {settlement}` — **CONDITIONAL verbatim**: lawful as ONE face's clause only under the reading that "legal machinery" names the two bodies and nothing wider (§0.7 (c)); this seat recommends it as a kept turn with the expletive removed ("No legal machinery stands at {settlement}" would open on the negated surface — R-ii/W10 allows one negated surface per variant on a PRESENT read and it is not an absence opener; the drafter chooses), and records the risk that a refuter reads "machinery" as a totality. Open row §5.5.
- the PIVOT `at {settlement}; … here …` — a rhythm, not a claim: the slot in the first clause and the deictic "here" carrying the subject into the second is the clerk's own thread (R-i: the second clause carries a noun forward) and may be re-used on licensed content.
- the DOUBLED-NOUN rhythm `force alone … and force alone …` — a rhythm only, REFUSED on its content (force); the writer may re-use the anaphora on a licensed noun ("no court … and no court …" would double the beat, W6 — so on TWO licensed nouns: "no court to …, and no cell to …").
- Not kept: "There is"; "order here rests on force alone"; "force alone deters only while it is present".
- The density floor for this variant is therefore the two reads in the ledger's formula, the conjunction stated, the row's matter as frame — richer in truth than the shipped line, whose second and third beats were the chrome's invention and a maxim (ADDENDUM 7's honest limit applies: the faithful rewrite is plainer where the shipped richness was unlicensed; the richness comes from the two civic nouns at their layer, the office's formula, and the construction).

**(7) What would make the rewrite a regression here:**
- inventory: vid 1 loses its slot set `{settlement}`, its `[ledger]` tag, or drops below four faces; a face stating one absence only ("no court sits at {settlement}") with no second read and no stance (ADDENDUM 7 rule 3: an inventory line); a face that paraphrases a sibling face rather than differing in construction (W4; ADDENDUM 7 rule 4);
- a lost licensed read: a face without the court, or without the cell, or with the two in two disconnected sentences and no noun carried forward (R-i, the thread at k = 0 inside the variant's own sentences);
- a lost lawful turn: the composite "no legal machinery" gone from EVERY face where the chair rules it lawful (§5.5); the slot-then-"here" pivot traded for a flat list; the office's formula absent from all four (the ledger's one licensed surface);
- a dropped angle: a face that reads as the town's talk or a stranger's eye under a ledger tag; a face that cites a holder, a roll or a book;
- an added unlicensed claim: "force", "armed", "the strong", "whoever"; "rests on", "relies on", "because"; "deters", "holds only while", "lasts"; "order" predicated of anything; "the watch", "the guard", "the reeve", "the headman", "the elders", "custom"; "settles", "judges", "hears" as an act; "quickly"; "well", "badly"; "nowhere", "no law", "lawless"; "the village", "a town this size"; "Moderate", the badge; a which-clause, an em dash, a digit, an exclamation; two ABSENCE moves side by side; the settlement token as opener.

---

## 2. VARIANT 2 · `[street]` (vid 2; slot set `{}` — NO `{settlement}` in any face; one short sentence, a reflexive, an adverb, a verdict tail)

**(1) Number and angle:** variant 2, `[street]` — the town's own talk about its condition (the palette; a STANCE under W27, never a quoted speaker and never a belief frame).

**(2) Shipped, verbatim (annex `:2677`):**
> The town settles things itself, quickly, and does not always settle them well.

**(3) Every claim, one per line (licence · law · referent layer beside the read's layer — BODY, negated, on both limbs):**
- "The town" opens — **lawful**: the record's generic subject on a slotless variant (§0.7 (d)); layer **NONE** (the record's subject). Not a value claim.
- the town settles things ITSELF — **the composite of R1 and R2 by INDIRECTION only**: "itself" implies there is nothing to send a matter to, which is the two absences at their licensed extent; **LICENSED in what it implies, UNLICENSED in what it states**. What it states is **a SECOND FACT** (a practice: that matters are settled, and by the town — `may NOT: a second fact`); **a FUSED AGENT** (the town as an actor that settles, W23; the D-F16/D-F18 pattern of the town deciding); **a CAPACITY fact** no read holds; and, on every tier this pool fires on, a claim that stands BESIDE the record's own unread fact (the governing row's `Dispute mediation` / `Community mediation` service, `institutionServices.js:15`, `:21`, `:27`, `:1449`: settled "by custom", "before they reach the lord's court", "No enforcement") — so it is neither licensed (the card reads no governing row) nor deniable (§0.4). Layer **NONE as an AGENT** against a BODY-negated read — the town at the wrong layer under W22/W23 (a collective acting where the read records only two absent bodies). Note that NO explicit licensed noun appears in this sentence: neither the court nor the cell is named; the shipped street row carries its whole licensed content by implication.
- quickly — **UNLICENSED: a RATE** (a season-class claim, `may NOT: a season`; R-DA-16's time words; a second fact). Layer **NONE**.
- and does not always settle them well — **UNLICENSED: a VERDICT** (the register rates nothing without a typed rating field; MOVE-GRAMMAR §1.3 VERDICT does not exist; the row's badge is `{band}` RESERVED and unfilled, so no band word licenses a rating and none is used); **a STANDPOINT** on the town's conduct (`may NOT`); **a FREQUENCY** ("not always" is a count-shaped hedge over unrecorded cases — R-DA-13's hedge floor, R-DA-16); **the summarising second beat** (R-DA-03/§1.3 MEANING: the tail glosses the first clause). Layer **NONE** (a quality of unrecorded acts) against a BODY-negated read.
- the SHAPE — a short single sentence (eleven words) with a reflexive, a parenthetical adverb and a "not always" tail: the SHORT LINE is R-DA-05's licensed shape and the pool's one relief; the "…, and does not always …" tail is the evaluative close (best-ai's signature, §16 (6)) the register forbids.

**(4) The reads the rewrite must state (all of them) plus the licensed shipped claims:**
- R1 — no court in the town (the class word; the slotless variant locates on "the town", "here", "this town", "the place" — never `{settlement}`, never a tier word).
- R2 — no place of confinement in the town (the class word).
- the KEY'S CONJUNCTION — both together, one keyed condition (§0.7 (a)); one negated surface at most per face (R-ii/W10), never two ABSENCE moves side by side, never an absence opener.
- the shipped licensed claim carried forward: ONLY the composite the shipped row IMPLIES (nothing to send a matter to = no court and no cell); the rewrite states it directly and at full resolution, which is the same claim, not an addition (attempt 2 and attempt 3 took the same reading; the sibling `full legal chain` packet took it for its own street row).
- the ROW'S FRAME in the street's nouns (§0.7 (c)): a wrong done here, a quarrel, a case, the one who did it — as what a court would hear and a cell would hold, stated as the town's plain way of putting the fact ("A wrong done here comes before no court") and never as what the town DOES about it.
- SILENCE on who settles, how fast, how well, by what custom, before whom (§0.4; §0.8).

**(5) The angle's stance, in one sentence:** the street is the town's own talk about its condition, so this face states the two absences in the town's plain nouns and deictics ("here", "this town", "in the town"; never `{settlement}`, which the parent does not carry), at the row's matter as the town would name it (a wrong, a quarrel, the one who did it), as a FACT compiled and not as a quotation — no "they say", no "everybody knows", no speaker, no belief frame (R-DA-13's vague-authority floor: `it is said`, `some say` → 0; R-DA-01: no assigned reaction) — and it may NOT invent a practice (settling, judging, custom), a speed, a verdict, a frequency, a totality over persons ("everybody", "nobody"), a tier ("the village"), a force, a watch, or a substitute institution (the headman, the elders, the moot), because the card reads none of them.

**(6) Turns worth keeping (verbatim, lawful as they stand — the density floor):**
- **none survives whole.** The lawful residue is (i) the OPENER `The town` — the generic subject on a slotless variant, lawful and the natural thread-carrier for a face that must name no `{settlement}`; (ii) the SHORT-LINE SHAPE — one clause, under twelve words, no semicolon: the register's relief line, which this variant is the pool's natural home for (R-DA-05: rhythm follows load; the short line exists); (iii) the REFLEXIVE'S WORK — "itself" is how the shipped row carries the absence without naming it; the writer may keep an indirection of that kind ONLY beside an explicit naming of the two bodies (the reads must be STATED, ADDENDUM 7's skeleton rule), never in place of it.
- Not kept: "settles things itself" (as an act); "quickly"; "does not always settle them well".
- The density floor for this variant is the two reads in the street's nouns with the row's matter as frame — richer in truth than the shipped line, which stated NO licensed noun and three unlicensed claims. The honest limit (ADDENDUM 7): the shipped row's whole richness (a practice, a speed, a verdict) was invention; the rewrite's richness comes from the town's plain nouns for a wrong and its doer set against the two absent bodies, and from the short line's shape.

**(7) What would make the rewrite a regression here:**
- inventory: vid 2 gains a slot (`{settlement}` in any face is a slot-set breach, ARCH §2.5), loses its `[street]` tag, or drops below four faces; a face stating one absence only with no frame and no stance (an inventory line, ADDENDUM 7 rule 3); four faces on one construction with the nouns swapped (W4);
- a lost licensed read: a face naming the court and not the cell, or the reverse; a face that carries the absence by indirection ALONE ("the town does without", "the town manages on its own") and names neither body;
- a lost lawful turn: the short line gone from every face (four long faces where the shipped row was the pool's relief line — R-DA-05's spread is the pool's, and this variant is where the short line lives); "The town" as opener gone from every face with nothing generic in its place;
- a dropped angle: a face that reads as the clerk's formula ("entered as standing") or a stranger's eye under a street tag; a face with a speaker or a belief frame ("they say", "as everyone knows");
- an added unlicensed claim: "settles", "judges", "puts down", "closes" as the town's act; "itself" as a practice; "quickly", "at once", "fast"; "well", "badly", "wrongly", "not always"; "the headman", "the elders", "the moot", "custom", "by custom"; "the watch", "the guard"; "force"; "everybody", "nobody", "anyone"; "the village", "a place this size"; "nowhere", "no law"; `{settlement}` anywhere; a which-clause, an em dash, a digit, an exclamation; two ABSENCE moves side by side.

---

## 3. VARIANT 3 · `[visitor]` (vid 3; slot set `{settlement}`; one sentence: a participial event, an expletive, a "that"-clause tail)

**(1) Number and angle:** variant 3, `[visitor]` — what a stranger notices first, without being told (the palette; W27: "a stranger" is the visitor's eye — it may see, never act, decide, be told or be given a name).

**(2) Shipped, verbatim (annex `:2678`):**
> A stranger wronged at {settlement} discovers there is nowhere to take it, and that the discovery surprises nobody local.

**(3) Every claim, one per line (licence · law · referent layer beside the read's layer — BODY, negated, on both limbs):**
- "A stranger" — **LICENSED as the stance's device** (the palette's own word; W27); not a referent (the eye, never a person with a fate — PERSON is never a referent, A.1). It opens the sentence, so the settlement token does not (lawful).
- wronged at {settlement} — **UNLICENSED as an EVENT**: a thing done to the stranger, a history (R-DST-B: a standing configuration field licenses a structural clause and never a historical one; no event-provenance field on this key); and an act with a doer and a victim the record does not hold (W22 by shape). **LICENSED as a FRAME NOUN** only: "a wrong" as the matter a court would hear (§0.7 (c)), in a standing or subjunctive form ("a stranger with a wrong to bring"; "would find"), never in the past participle as something that happened. `{settlement}` — **LICENSED**, the bag's one filled slot, once. Layer of the wrong: **NONE** (a hypothetical of the frame).
- discovers — **CONDITIONAL**: the visitor's verb is "notices" / "finds" / "looks for and does not find" (the palette: "what a stranger notices first"); "discovers" is an event-shaped finding (a discovery is a HISTORY move by form) and carries the shipped row's whole narrative shape; lawful only as a present standing finding ("finds") of a standing fact, never of an event. Layer: the eye.
- there is nowhere to take it — **LICENSED in substance**: the composite of R1 and R2 seen from outside (a wrong brought here has no court to be brought before and no cell for its doer — the two absences at their licensed extent); layer **BODY, negated = the read's layer**. **UNLICENSED in its EXTENT**: "nowhere" is a TOTALITY over the read (§0.7 (c); §0.8): on every tier this pool fires on the governing row records a mediation service, so "nowhere to take it" is contradicted by the engine's own unread record; the licensed form names the two members ("no court to take it to, and no cell for the one it names"), which is the same claim narrowed to its licensed core — a drop, not an addition (attempt 3 reached the same narrowing). Also carries the expletive ("there is": R-DA-07, a form fault).
- and that the discovery surprises nobody local — **UNLICENSED: a TOTALITY OVER PERSONS** ("nobody local" — a REFUSED COLUMN, always, on every card); **a FEELING** (surprise, or its want — no field carries motive, belief or mood; MOVE-GRAMMAR §1.3 FEELING; R-DA-14); **a SECOND FACT** (the locals' state of knowledge; `may NOT`); **a STANDPOINT** (`may NOT`); **a belief frame** (R-DA-13's floor); **the summarising second beat** (R-DA-03 → 0.000: the "and that …" tail glosses the first clause). Layer **PERSON (plural, unindividuated)** — never a referent (W22) — against a BODY-negated read.
- the SHAPE — one long sentence (twenty-one words) in a "discovers X, and that Y" frame: the "and that" tail is the gloss; the visitor's licensed shape is the noticing of a standing fact, closed on a civic noun of varied kind (R-DA-04), never on the townspeople's mind.

**(4) The reads the rewrite must state (all of them) plus the licensed shipped claims:**
- R1 — no court at {settlement}, as the thing a stranger with a wrong to bring would look for and not find (the class word).
- R2 — no place of confinement at {settlement}, as the thing the stranger's wrong would have no cell for (the class word).
- the KEY'S CONJUNCTION — both together as one keyed condition (§0.7 (a)); the stranger's one look finds neither; one negated surface per face (R-ii/W10), never two ABSENCE moves side by side, never an absence opener (the stranger opens).
- the shipped licensed claim carried forward: "nowhere to take it" NARROWED to its two members — a wrong brought to {settlement} can be brought before no court and its doer held in no cell. Nothing of "wronged" as an event, "discovers" as an event, or "surprises nobody local" survives.
- the ROW'S FRAME from outside (§0.7 (c)): a wrong, a complaint, a case, the one it names — the stranger's hypothetical matter, in the present standing or the subjunctive ("would find"), never the past.
- SILENCE on the locals, their knowledge or their feeling; on who would hear the wrong instead; on the tier (§0.4; §0.8).

**(5) The angle's stance, in one sentence:** the visitor is what a stranger notices first without being told, so this face lets the stranger's eye look for the two bodies at {settlement} and find neither — the stranger may look, find, notice, be answered by the absence of a thing, and carry a hypothetical wrong as the frame's matter — in the present standing or the subjunctive edge (A2), closing on a civic noun; it may NOT let the stranger be wronged (an event), discover (a history), be told, be surprised or surprise anyone, be named, act, decide or meet a fate (W27; product scope); it may NOT name the locals at all ("nobody local", "the townsfolk", "anyone here" are a totality or a person-plural referent); and it may NOT widen the two absences into "nowhere", "nothing", "no law", or narrow the stranger's look onto a substitute (the headman, the elders, the watch) the card does not read.

**(6) Turns worth keeping (verbatim, lawful as they stand — the density floor):**
- `A stranger … at {settlement}` — **LICENSED as the stance's opener and the slot's seat**: the visitor's device with the slot inside the first clause is the palette's own construction and may stand as it is in one face (the words between are the writer's, because "wronged" falls).
- the ONE-LOOK SHAPE — `… there is nowhere to take it` — **CONDITIONAL, not verbatim**: the shipped row's finding (nothing to take a wrong to) is the composite from outside and is the turn worth keeping IN SUBSTANCE; its wording ("nowhere") is a totality and the expletive is a form fault, so the writer keeps the look-and-find-nothing SHAPE on the two named bodies ("finds no court to take it to and no cell for the one it names") and not the words.
- Not kept: "wronged" (as an event); "discovers" (as an event); "there is"; "nowhere" (as a totality); "and that the discovery surprises nobody local".
- The density floor for this variant is the stranger's one look, finding neither body, with the wrong as the frame's matter — richer in truth than the shipped line, whose second half was a totality over persons and a feeling; the honest limit (ADDENDUM 7): the shipped "surprises nobody local" was the memorable clause and it cheated (a REFUSED COLUMN and a FEELING); the rewrite's memorability must come from the stranger's construction and the two civic nouns at their layer.

**(7) What would make the rewrite a regression here:**
- inventory: vid 3 loses its slot set `{settlement}`, its `[visitor]` tag, or drops below four faces; a face stating one absence only with no stance (an inventory line); four faces on one construction (W4);
- a lost licensed read: a face where the stranger finds no court and the cell is unstated, or the reverse; a face where the stranger's look and the two absences sit in two disconnected sentences with no noun carried forward (R-i);
- a lost lawful turn: the stranger's opener gone from every face (four faces that name no stranger under a visitor tag are a dropped angle as well); the look-and-find-nothing shape gone from every face;
- a dropped angle: a face in the clerk's formula or the town's talk under a visitor tag; a stranger who is told, warned, answered by a person, or who acts;
- an added unlicensed claim: "wronged", "robbed", "cheated" as an event; "discovers", "learns", "is told"; "nowhere", "nothing", "no law"; "nobody local", "the locals", "the townsfolk", "everyone", "anyone"; "surprises", "expects", "minds", "pretends"; "the headman", "the elders", "the watch", "the guard"; "force"; "settles"; "quickly"; "well", "badly"; "the village", "a place this size"; a name for the stranger; a which-clause, an em dash, a digit, an exclamation; two ABSENCE moves side by side; the settlement token as opener.

---

## 4. THE POOL, WHOLE — the fences the twelve faces must obey together (the marker's inventory of the walls)

- **The settlement token never opens a sentence face** (ARCH §2.5, T-F8; MOVE-GRAMMAR §1.4 wall 10). No shipped row of this pool opens on it (vid 1 opens on the expletive, vid 3 on the stranger, vid 2 carries none); the rewrite keeps it so. Lawful openers: the office's formula, a court or a cell as the negated surface (one per variant), the town (vid 2), a stranger (vid 3), a plain civic noun, a frame noun (a wrong done here).
- **The slot sets are the parents' exactly:** vid 1 `{settlement}` · vid 2 `{}` · vid 3 `{settlement}`. `{band}` (RESERVED) and `{route}` (unfilled here) are named by no face. The bag holds no `{defwork}`, `{seat}`, `{faction}`, `{npc}`, `{watchname}`; none is named.
- **The two reads in every face, as one keyed condition** (§0.7 (a); R-v; ADDENDUM 8 (1)): the court and the place of confinement, both at their FALSE value, together — one negated surface per variant at most (R-ii/W10), or a positive frame ("does without"; "keeps neither"); never two ABSENCE moves side by side (order wall 3), never an absence as the opener, never one absence alone (an inventory line, refused).
- **The class of the two nouns:** the census prints `objectClasses: []` for this pool, so the projector's T-F12 class guard is NOT armed here (the sibling `full legal chain` prints `["law"]` and the bar "another civic object of the class `law`"); the LAW binds regardless: the two reads are the only two `law`-class objects a face may name, and a third (a gaol as a distinct building, a magistrate, an assize, a bench, a courthouse as a particular, "justice") is refused (W20; A-16; A-17). Recorded as open row §5.2.
- **A11's spread:** no two variants of one pool share their first two words; the four faces of a variant differ in CONSTRUCTION — the subject, the order of the two absences, the frame noun, the landing noun, the sentence count — not vocabulary alone (ADDENDUM 7 rule 4; W4). The pool's shipped spread is wide (a long expletive line; a short reflexive line; a long stranger line) and the rewrite keeps a short line IN THE POOL (vid 2 is its natural home) and varies the sentence count across the variants as the shipped rows did (one sentence each today; A1 allows two).
- **Level-1 grammars this pool can license:** V1 PRESENT (the keyed condition — the two absences in either order, court first or cell first) is the member whose licensing field the card prints; the negation is the PRESENT move's own predicate at this branch (the whole key value is a negation), not an added ABSENCE move — so V3 (PRESENT → LACK) is NOT a second member here, because there is no `none-exists` field beyond the key itself (attempt 2's second judgment; taken and flagged, §5.7); V2 needs a structural-consequence field (none; S2's clause seat is closed on a spine with no relation row); V5 needs an institution table row (none resolves); V6 an unresolved value (none); V8 a `not-held` record field (none); V4 OBJECT → PRESENT needs a named-object FIELD (the bag carries none; a class word does not make V4). The census reads `grammars: 2`, so the writer varies the ORDER of the two reads and the sentence count and declares the grammar per variant; the walker's arm A gates only tagged orders.
- **The thread (MOVE-GRAMMAR §1.4.1; R-i at k = 0):** this pool is the SPINE and opens the passage; the census reads `k: 1`, so at most ONE modifier may follow by salience, and every face is written to read well alone and before an unknown modifier: it hands forward a noun a modifier can carry (a court, a cell, the town, a wrong), and it does not close on a pronoun (R-DA-04's pronoun-closer ceiling ≤ 0.055; the shipped v1 closes on "present", v3 on "local" — adjectives, not pronouns). Inside a two-sentence face the second carries a noun forward from the first regardless of k.
- **The close:** a standing fact of varied KIND across the twelve faces (an absence — lawful as a CLOSE kind, never as the opener; an object; a condition; a name not given …); never a verdict, a maxim, a hook, a cause, a feeling, a person.
- **Same word, same referent across the tab (rule 3; C7; A1/A11):** DS-DEF-3's safety banner and DS-DEF-6's `Legal Infrastructure: None` (WIRING-UNRESOLVED, REFERENT row 58) sit on the same tab and read the same flags or their aggregate; a face here spells the two absent bodies as those pools will when rewritten ("a court"; "a place of confinement" / "a cell") and never as a different thing; no face here names a force, a watch or a militia that the Beasts, Invasion and safety rows on the same tab assert or deny (§0.4).
- **The referent walls (W20–W27) on this pool:** the court and the prison at BODY, negated, by the class words; no ORGAN word (the court holder does not resolve: no "the court's record", "the roll", "the bench sits"); no ROLE (no magistrate, judge, reeve, headman, constable, gaoler — W22; the ROLE instrument emits none on a read this card carries); no AGGREGATE word on these body reads (no safety label, badge, "order" as a score — W20); no PERSON as agent, decider, holder of a permission or load-bearing referent (W22: "whoever", "somebody", "the person who", "nobody local"); no FUSED AGENT (W23: "the town settles", "order rests on force"); no record word (W24: SOURCE-UNRESOLVED; the ledger is a standpoint); no EXTERNAL BODY (none exists on this read); the stance's nouns never a holder's record and the stranger never an actor (W27); no TRADITION word ("custom", "by custom" — the `Record of custom` service is unread).
- **The label walls (W14) on this pool:** `no legal infrastructure` at its ENGINE meaning — `hasCourtSystem` false AND `hasPrison` false over the frozen generation roster (§0.4) — never the dictionary sense "lawless", "no law", "no justice"; the flags' own scope (a hall counts as a court; stocks count as a prison) is what the negation negates.
- **The crisis and force walls (W12, W13, W15, W17):** no garrison, watch, guard, militia, muster, levy, soldiers, force, armed men; no crime word asserted (theft, violence, the strong — the safety label's business); no siege, raid or occupation.
- **Hard walls, every face:** no em dash, no exclamation, no digit, no percent, no which-clause, no question, no "I"/"you", no figure, no sense verb on an abstraction, no intent or feeling for an inanimate or collective thing, no forecast (the edge, if any, subjunctive), no totality over persons or over the town's law, no belief frame, no vague authority ("it is said"), no count, no season or rate, no cause joint, no verdict, no maxim, no tier word, no badge word, no citation, no record noun.
- **Provenance ceiling:** zero citations on this pool (§0.5): no holder resolves for either read, the `prison` read has no holder kind at all, and the office does not cite its own books; "the muster roll", "the books", "the record shows" are findings (R-vi, W7, A13).
- **The echo:** the echo table is keyed on the producer-token root `court`, coarser than this pool's read; a mount counted there may be a sibling pool's; the faces neither rely on nor fear it.
- **The density floor (ADDENDUM 6; §21.4):** the shipped v1 states the composite plus a cause and a maxim; v2 states the composite by implication plus a practice, a rate and a verdict; v3 states the composite as a totality plus an event, a feeling and a totality over persons. Every face of every variant states BOTH reads and their conjunction, at the row's matter, in its stance; the one CONDITIONAL verbatim turn is v1's "no legal machinery at {settlement}" (§5.5); the lawful residues are v1's slot-then-"here" pivot, v2's short line on "The town", and v3's stranger-at-{settlement} opener with the look-and-find-nothing shape. A face stating one read with no angle is refused (ADDENDUM 7 rule 3). The honest limit (ADDENDUM 7): this pool's shipped richness — a ground of order, a deterrence, a speed, a verdict, a surprise, a "nowhere" — was the chrome's invention and the register's forbidden closes, so a faithful rewrite is plainer in those places by design; the richness that IS available comes from the two concrete civic nouns at their layer (R-DA-10), the row's matter as each stance frames it (a wrong done here; a charge; the one it names), the office's formula for the ledger, the short line for the street, the stranger's one look for the visitor, and the construction varying across twelve faces.

## 5. THE MARKER'S OPEN ROWS FOR THE CHAIR (recorded, not decided)

1. **The pool is the BELOW-TOWN value of the row** (§0.4, CONFIRMED against the catalogue and the two flags): every natively generated thorp, hamlet and village fires it and no town or city does, because `Town hall` and `City hall` are required rows and no smaller tier holds any court- or prison-keyword row. The card prints no tier read, so no face states the tier (W3, W5); but a reader of the tab sees this pool only on small settlements, and the drafter should write with that reader in mind (the frame nouns of a hamlet's wrongs, not a city's). Whether a tier read should be added to this key's card (so a face may say "below the tier that keeps a hall") is a wiring/owner row for CAR 8b-W-2, not a writer's act.
2. **The class guard is unarmed on this pool while armed on its sibling:** census `objectClasses: []` here, `["law"]` on `full legal chain`; the key strings differ only in whether the class words appear in them ("court AND prison" vs "no legal infrastructure"), so `objectClassesOf` splits the POOL KEY only (OV-25) and this pool's faces will name two `law`-class nouns with no census guard. This seat applies the bar by law (§4); a census re-take may arm it — recorded for the register car (OW-21's `objectClassesOf` over the variants' nouns).
3. **The mediation services are on the record and unread** (§0.4): the governing row of every tier this pool fires on carries `Dispute mediation` / `Community mediation` ("by custom"; "No enforcement, but social pressure is real"). A read on that service row would license the STREET angle's true substitute ("a quarrel here goes before the headman by custom") and would turn the shipped v2's claim from invention into a licensed fact; until such a read is wired (a row for the register car or the general desk), the faces neither assert nor deny any substitute. This is the exact shape of ADDENDUM 7's honest limit: the richness exists in the world and is not yet wired to this card.
4. **Frozen booleans against the live roster** (entailment G-13; C-10): this row reads `compound.inst` minted at generation over the raw roster, while the Beasts and Invasion rows read the ruin-filtered live roster. A town whose hall is ruined still reads `hasCourtSystem` true and never reaches this pool; a wiring fact for the register car, not a writer's; the faces follow the card's read.
5. **"no legal machinery at {settlement}" — CONDITIONAL verbatim.** This seat recommends it as v1's one kept turn under the reading that "machinery" names the two bodies; a refuter may read it as a totality (§0.7 (c)). The chair rules whether it may stand as one of v1's four faces (ADDENDUM 6: a lawful shipped clause may stand verbatim as ONE face) or whether "no court and no cell" is the only lawful spelling. The drafter writes the other three faces on the two named bodies either way.
6. **"The town" on a pool that fires only below town** (§0.7 (d)): this seat reads "the town" as the record's generic subject carrying no tier value, as the `neither walls nor force` packet and the ratified W15 spellings do, and refuses "the community" (needs a tier read) and "the village" (a value). The plagued twin's marker read "the town" as a value claim on small tiers; the split is flagged again here, and on THIS pool the question bites on every firing, since no firing is a town.
7. **One compound LACK, not two ABSENCE moves** (§0.7 (a); attempts 2 and 3 recorded the same judgment): the pool's key value IS the pair, so the pair is the PRESENT move's own predicate and order wall 3 is not engaged. If the chair reads the two reads as two ABSENCE moves, no face of this pool can be written under its own key; that reading is refused here and flagged.
8. **The chrome's string sits beside every face:** `buildThreatAssessment` prints "No legal infrastructure: order relies on force alone." on the same tab for every settlement that fires this pool, saying the cause the face may not (OW-20: the engine's own strings are product code outside the corpus — recorded for the owner as a scope question). A wiring fact, not a writer's.
9. **The visitor's hypothetical — standing or history?** This seat reads "a stranger with a wrong to bring finds no court" (present, standing) and "would find" (subjunctive) as lawful standing forms of the frame, and "a stranger wronged … discovers" as a history without provenance. If the chair rules the present hypothetical itself a history, only the subjunctive survives on vid 3.
10. **The safety banner's wrong-layer rows on the same tab** (D-F2: "The watch at {settlement}" on tiers with no watch) will be cured by DS-DEF-3's own rewrite; until then a face here that names no force, watch or guard cannot contradict them, which is one more reason the force words stay out of this pool (§0.8).

---

STATUS: COMPLETE — §0 (the card, key, census, header, register, layers, four readings, the word rulings), §1–§3 (the three variants, each with the seven required parts), §4 (the pool whole), §5 (ten open rows). Three shipped variants marked, one for one; no face drafted; nothing outside this file written; no dock edited or run beyond the read-only licence-card script; dock HEAD `f2da5a3ee`, porcelain 0 at the time of reading.
