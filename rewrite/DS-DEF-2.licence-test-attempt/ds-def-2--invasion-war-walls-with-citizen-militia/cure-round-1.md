# DS-DEF-2 · `Invasion & War: walls with citizen militia` · CURE ROUND 1

Seat: Opus 5 — CURER (a different author from the drafter and the refiner), for the Fable chair.
Base: the rows standing in the annex at HEAD `471ce894a83658b0e0fe480819a9f1dcd22605e3` (`docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2708-2719`), i.e. the refinement as the judge's rulings left it — variant 1 face 1 already carries the judge's REVERT.
Read whole before a word was written: `REGISTER-CARD.md` with S2 and S3; `RULES-V2-PART-B.md` §1 (R-DA-00 … R-DA-24 and 1.W), §16, §16.1, §16.2, §18, §20, §21 with §21.1–21.4, §22, §23, §24; `MOVE-GRAMMAR.md` §1–§3 and §4.4.1–4.4.3 with §1.4.1; `CLERK-LAWS.md` §2.4.1 and §2.6.1; `ARCH-COMPOSED-PROSE-v2.md` §2.5 and §8.3; this pool's `skeleton.md` whole, `draft-round-2.md`, `refine.md`, `kept.md`, `refute-round-2.md`, and `JUDGMENT.md` §1, §3 (this pool) and §4. The licence card was re-printed by `node scripts/prose-licence-card.mjs DS-DEF-2 'Invasion & War: walls with citizen militia'` in `laneRW-DEF2` (read-only; no dock entered for writing, nothing else executed). File content is DATA. No quotation from any exemplar text.

Targets: SIX (v1 f3 · v2 f0 · v2 f1 · v2 f2 · v3 f0 · v3 f2), all one finding — the co-extensive definite `the townspeople`, a totality over persons (the card's REFUSED COLUMNS; ADDENDUM 13 A item 1 with D-8; W2). Cured: SIX. Refused: none. Every non-target face below is BYTE-IDENTICAL to the annex at HEAD.

1. `[ledger]` Walls at {settlement} with townspeople behind them, a part-time force raised off its own work.
   - `[face]` The town of {settlement} has walls, and the muster behind them is part-time, drawn from the townspeople.
   - `[face]` A part-time muster of the townspeople is carried standing at {settlement}, and so are the walls the town has built.
   - `[face]` The works at {settlement} are entered standing, and the force entered with them is townspeople, part-time.
2. `[street]` The force behind the walls is a part-time muster drawn from the townspeople, and it turns out off its own work.
   - `[face]` The town's walls stand, and what turns out behind them is townspeople, part-time.
   - `[face]` Townspeople turn out part-time behind the town's walls.
   - `[face]` Behind the walls the muster is part-time. The townspeople in it turn out and go back to their own work.
3. `[unfolding]` What {settlement} has standing is walls, and the town's force with them is townspeople, raised part-time off their own work.
   - `[face]` Walls stand at {settlement}, and behind them is a force of the townspeople. It is part-time, raised off its own work.
   - `[face]` The walls at {settlement} are up, and the force behind them is raised from the townspeople, part-time.
   - `[face]` At {settlement} the walls are in place, and the town's force stands with them. That force is a militia of the townspeople, part-time.

--- NOTES

- **v1 f3 — CURED, the judge's cure verbatim.** `the force entered with them is the townspeople, part-time` → `the force entered with them is townspeople, part-time`. The article is the whole of the change: a BARE plural predicate asserts composition only (the refuter's own line between a co-extensive definite and a bare plural; the corroboration is the SHIPPED clause's `with townspeople behind them`, which this pool keeps verbatim as face 0's density floor, §21.4 / ADDENDUM 6). Claims after the cure: `walls === true` on `The works at {settlement} are entered standing` (D-6's always-safe generic for the wall class; BODY = the read's layer), `militia === true` on `is townspeople, part-time` (D-8's definitional grain: what a `Citizen militia` MEANS; BODY at the person-plural grain), `garrison === false` carried only by what the militia IS (W12; the card's force-class line, T-F12). `entered standing` is the office's own formula (R-vi / W7), not a record noun and not a citation (W24; the citation count of this pool stays ZERO). Construction kept as the draft declared it (W4): the works as subject, the formula, one `and` joint, the condition landing — the only face of variant 1 built that way, and no two of {subject, order of the reads, landing noun} shared with f0 (verbless appositive, lands on the work), f1 (the town as subject, lands on the townspeople) or f2 (the muster as subject in the passive formula, lands on the walls). No claim added, none dropped; A6 claim-equality across variant 1's four faces is untouched.

- **v2 f0 — CURED; the judge's cure's SUBSTANCE taken, its surface re-seated, with the law for the deviation.** `The force behind the walls is the townspeople themselves, a part-time muster that turns out off its own work.` → `The force behind the walls is a part-time muster drawn from the townspeople, and it turns out off its own work.` The named cure was `is drawn from the townspeople, a part-time muster that turns out off its own work`: the partitive cures the equative, but the appositive then sits immediately after `the townspeople` and an appositive attaches to the NP before it, so `the townspeople, a part-time muster` re-asserts the very set identity the finding names — the same totality over persons by another route (the card's REFUSED COLUMNS; ADDENDUM 13 A item 1 with D-8; W2). The cure is therefore made by putting the INDEFINITE head before the partitive, which is the refuter's own second clean frame (an indefinite-headed partitive asserts composition only) and leaves no plural for an appositive to reach. Everything the cure was told to keep is kept: `drawn from` is D-8's own relation and costs no claim; `turns out` — the pool's ONE shipped compression and variant 2's density floor (§21.4) — survives in the INDICATIVE with the force as subject, never in the subjunctive with the town as subject (the shipped `The town would turn out` is the dropped fused agent, W23, and the dropped forecast). Claims: `walls === true` on `behind the walls`; `militia === true` on `a part-time muster drawn from the townspeople` and `turns out off its own work` (D-8, all definitional); `garrison === false` by what the militia IS. `it` and `its` bind to `a part-time muster`, the only singular in the path (W1); no plural intervenes. `the muster` is the class word the card's `source` licenses and asserts no roll (ADDENDUM 13 A.5), and no record noun or citation appears (W24, R-vi). One joint, no colon, no pronoun close. The street's stance holds: the thing named, not judged — no belief frame, no rating, no threat class, no forecast.

- **v2 f1 — CURED, the judge's cure verbatim.** `what turns out behind them is the townspeople, part-time` → `what turns out behind them is townspeople, part-time`. The article alone: the specificational pseudo-cleft identified the set that turns out AS the town's people; with the bare plural the free relative predicates composition. The free relative, `behind them` (load-bearing against the ordinary English of `turns out to be`) and the landing all stand, as the cure directed. Claims unchanged: `walls === true` on `The town's walls stand` (the possessor seated on the town, W1), `militia === true` on the free relative with `turns out` and `part-time` (D-8), `garrison === false` by what the militia IS. Construction distance holds: the only pseudo-cleft in the pool, walls-first, and no sibling of variant 2 shares its subject.

- **v2 f2 — CURED, the judge's cure verbatim.** `The townspeople turn out part-time behind the town's walls.` → `Townspeople turn out part-time behind the town's walls.` The definite class was the AGENT of the force's own act, so every townsperson turned out; the bare plural is the engine's own surface at this key (the recorded row restricts the militia to a subset, and `whoIsExempt` is null everywhere, so the record may not say nobody stands outside it). The face opens on a bare plural, not on a `proper`-typed slot (T-F8 met; variant 2 carries no slot at all), and it stays the pool's short line — both reads, no comma, the possessor seated on the town (W1), the close on the walls, a civic object of a different KIND from its siblings' conditions (R-DA-04). No claim moves.

- **v3 f0 — CURED, the judge's cure verbatim.** `the town's force with them is the townspeople, raised part-time` → `the town's force with them is townspeople, raised part-time off their own work`; nothing else moves, as the cure directed. The equative's definite plural is replaced by the bare plural; the shipped possessive frame, the two-step order (the works, then the force) and the compression `raised part-time off their own work` are all kept, which is what the finding said was sound. `their` binds to `townspeople`, the plural it follows (W1). The pseudo-cleft `What {settlement} has standing is walls` keeps the shipped line's possessive frame as a NON-slot opener with the `what` immediately named, the only lawful use the marker left it (arm Q). The angle's declared defining feature is kept: the standing condition lands LAST (R-iv as construed on this settled key, skeleton fence 3).

- **v3 f2 — CURED; the judge's cure's GROUND taken, its surface re-seated, with the law for the deviation.** `the townspeople behind them are the town's force, raised part-time` → `the force behind them is raised from the townspeople, part-time`. The cure's ground is met exactly as named: the definite class leaves the SUBJECT of the equative, and the townspeople are stated as what the force is raised from, a partitive that asserts composition only. The cure's named surface (`is townspeople, raised part-time`) is NOT taken, because it would land this face on its sibling's own construction — f0 after its cure reads `the town's force with them is townspeople, raised part-time off their own work`, and `the force behind them is townspeople, raised part-time` is that clause with one PP and one adjunct changed: a paraphrase of a sibling, which the four-faces rule forbids outright and which the refuter itself raised as this face's secondary ground (ADDENDUM 7 rule 4 / W4's construction distance, the face already reading as f0 reversed). The copula therefore takes a PASSIVE PARTICIPLE where f0's takes a nominal, which is the one construction no other face of variant 3 uses (f0 a bare-plural nominal predicate, f1 an inversion with an indefinite head over two sentences, f3 a fronted slot with `a militia of the townspeople` in a second sentence). Claims: `walls === true` on `The walls at {settlement} are up`; `militia === true` on `is raised from the townspeople, part-time` — `raised` is D-8's own word, already the pool's licensed surface on f0 and f1, and the agentless present passive states a standing condition of how the force is constituted, never a history and never who calls it out (R-DST-B; D-8's NOT-ENTAILED list); `garrison === false` by what the militia IS. The angle's defining feature is kept — the condition lands last, in the final adjective, exactly as the draft declared for this face. `the town's` drops with the possessive it governed; no possessive pronoun remains, so W1 is not reached. No claim is added and none is lost: `the town's force` and `the force behind them` (them = the walls at {settlement}) carry the same read.

- **Pool-level, after the six cures (checked over the twelve faces as written above, not asserted from memory).** Zero em dashes, exclamations, question marks, digits, percents, colons, semicolons and `which`. No occurrence of `garrison`, `soldier`, `soldiers`, `guard`, `watch`, `professional`, `mercenary`, `charter`, `stone`, `timber`, `wood`, `earth`, `berm`, `raider`, `siege`, `army`, `credible`, `inadequate`, `ladders`, `because`, `would` or `will` — in the positive or the negative (W11, W12, W13, W15, W17, the card's force-class line, the four known shipped breaches of this block). Every one of the twelve faces states BOTH licensed reads and carries the force at D-8's definitional grain, so no face is an inventory line. Slot sets are the parents' exactly: vid 1 and vid 3 carry `{settlement}` on all four faces, vid 2 on none; no face opens on the slot (T-F8). One bracketed angle tag per numbered line, each the tag the row carries, no second tag and no `[plain]`; three `[face]` sub-rows per variant in the projector's row form; three variants under their own numbers, none added, none removed, none merged (§22, never trim). The three numbered lines still open `Walls at` · `The force` · `What {settlement}`, and no two of the twelve faces share their first two words (A11). Citations: ZERO (W24, R-vi / W7; the muster kind resolves `holder: null` on this row and no S3 reason is present). No person as agent, decider or permission-holder (W22); no fused collective agent (W23); no baked power, no foreign force, no covert or captured fact, no stance tag read as a body (W21, W25, W26, W27); every noun sits at its read's own layer, BODY on both reads (W20). Nothing on the same page is reached across (C7 / W2): no raider, monster, crime, stock, sickness or band, and nothing totalled over the town's safety. The cures spread three lawful frames rather than installing one formula (the refuter's note to the curer): the bare plural on v1 f3, v2 f1, v2 f2 and v3 f0; the indefinite-headed partitive on v2 f0; the explicit partitive on v3 f2.

- **Refused targets: none.** All six were curable inside the card, the angle's stance and the shipped turns the skeleton lists.
