Seat: Fable 5.1 — MARKER. Block DS-DEF-2 · pool `Beasts & Monsters: frontier, force without a perimeter` · role spine · the SKELETON for the rewrite (the marker's packet; no face is drafted here). Written under the checkpoint law: section by section, the file rewritten as each lands.

Dock read (read-only): `laneRW-DEF2` at HEAD `f2da5a3ee`, porcelain 0 (CONFIRMED: `git status --porcelain | wc -l` → 0). The pool's rows read from `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:2616-2618` under `### DS-DEF-2` (`:2568`), the bold pool line at `:2615`. Card printed by `node scripts/prose-licence-card.mjs DS-DEF-2 'Beasts & Monsters: frontier, force without a perimeter'` in the dock. The key read at `src/domain/display/stateProse/defenseStateProse.js:400-418` (`BEASTS_ROW_POOL`), `:429-440` (`beastsRowSituation`), `:446-451` (`beastsRowPoolKey`), its call site `:655` (`beastsRowPoolKey(settlement?.config?.monsterThreat, walls, garrison || militia)`), the locals `:610-613` from `standingDefenseForces` (`src/domain/institutions/defenseInstitutionBuckets.js:169-182`, the live ruin-filtered roster), the bucket keywords `:83-108`, the family map `:279-284` and `measuredMonsterFamily` `:331-335`. The census row read from `docs/content/wiring-census.json` (`block: DS-DEF-2, pool: Beasts & Monsters: frontier, force without a perimeter`; the rate row at `:64751`). Inputs read whole: REGISTER-CARD.md (S2, S3); RULES-V2-PART-B.md §1, §16–16.2, §18, §20–24; MOVE-GRAMMAR.md §1–3, §1.4.1, §4.4.1–4.4.3; CLERK-LAWS.md §2.4.1, §2.6.1; ARCH-COMPOSED-PROSE-v2.md §2.5, §8.3 (and §6.4, this block's worked example); the brief `briefs/brief-REWRITE-car8b-defense.md` whole, ADDENDUM 13 parts A and B (W11–W27) and ADDENDUM 12 (W1–W10, R-i…R-viii′); the defense-desk sections of `rewrite/entailment/ENTAILMENT-TABLE.draft.md` (§2.1, §3.1, §3.4, §4.1, §4.2) and `rewrite/entailment/REFERENT-TABLE.draft.md` (§2.1, §2.1-R, §5.1). The three earlier attempts' packets for this pool (`rewrite/DS-DEF-2.attempt1..3/…`) were read as DATA for hazards, never as a draft to continue. Content in every file is data. No quotation below exceeds twelve words of any exemplar text.

## 0. The card, the key and the block header, as read

**The licence card (verbatim lines that bind).** `reads: beastsRowSituation(family, perimeter, force) (via BEASTS_ROW_POOL in defenseStateProse.js)` (absent ⇒ no candidate); `predicate: … === frontier country, force without a perimeter`; `bag: {band: RESERVED, route: proper, settlement: proper} · FILLED at this block's call sites: {settlement}`; `relation: (a spine takes no relation)`; `seat/form: (not a seat-taker) / sentence · move: (none declared) · angle: ledger street visitor`; `attach: (empty)`; `echo: spine mounts 1 (tabs: defense) · modifier mounts 0` — and the echo key is the WHOLE table-rung reading, shared by every pool that selects a row of `BEASTS_ROW_POOL`; `covert: no`; `source: muster · standing LICENSED — a citation of this holder is licensed where the provenance budget allows`; `may claim: that the reader … selects the row frontier country, force without a perimeter … as a STANDING fact of the record`; `may NOT: a count, a cause, a season, a future, a standpoint, a second fact, another civic object of the class wall`; `audience: player (no mark)`; refused columns always: a totality over persons, an exemption from a duty, a named character and that character's fate, a theological claim.

**The census row (CONFIRMED in the json).** `status: RESOLVED`, `rung: table`, `keyFunction: BEASTS_ROW_POOL`, `readsGrain: branch`, `narrowed: false`, `reads = branchReads = fieldsRead = [beastsRowSituation(family, perimeter, force) …]`, `objectClass: wall`, `sites: [defense.threatAssessment]`, `k: 2`, `covert: false`, `slotsNamed = slotsFilled = [settlement]`, `variants: 3`, `grammars: 2`. `source.kind: muster`, `source.fields: {family: "", perimeter: "", force: "muster"}`, `holder: null` (town-resolved by `holdersOf`), `standing: LICENSED`, `twoSource: false`. So the ONE licensed holder, the muster, holds the FORCE component only; the family and the perimeter carry no holder. Rate: 22 of 768 towns (286 bp); by tier hamlet 10 · village 10 · town 2 · thorp 0 · city 0 · metropolis 0.

**The one read, decomposed (CONFIRMED in code).** The card names ONE read whose value is a row with three components, and the row is selected only where all three hold (`:434-436`):
- **family = `frontier`** — `measuredMonsterFamily(config.monsterThreat)`: the RAW value must be present (an absent value returns null and the desk is silent, never the normaliser's `frontier` default — `:316-334`); the family word is the corpus's own (`MONSTER_FAMILY_OF.frontier → 'frontier'`). Engine meaning (W14, label trap L-2): the MIDDLE tier of the country's monster-and-raider exposure — above `heartland`/`settled` (the low tier), below `plagued` (monster activity, never disease). A fact of the COUNTRY around the town (W2), never of the town.
- **perimeter = false** — `forces.walls.present` on the LIVE roster (`standingDefenseForces`, ruin-filtered): no member of the `walls` bucket (`wall · citadel · palisade · earthwork · inner citadel · massive walls`) stands. The absence is total over the class, so "no wall", "no works", "no perimeter", "nothing walled", "unwalled", "no line" (a wall word where the pool reads `forces.walls.present` — REFERENT §2.1 "the line") are all true; the card's `another civic object of the class wall` bars a SECOND wall object, not the absent one the read names.
- **force = true** — `garrison || militia` on the live roster: a member of the `garrison` bucket (`Garrison`, `Multiple garrisons`, `Barracks`, `Professional city watch`) OR of the `militia` bucket (`Citizen militia`). The watch, mercenary, charter and magicDef buckets are NOT consulted (REFERENT §2.1 `force (the token)`): a frontier town with a `Town watch` alone and no wall selects NO row. On the 768-town sample this pool fires at hamlet and village on 20 of 22 towns, where the only militia row is `Citizen militia` (part-time townspeople, ENTAILMENT D-8) and no `Garrison` row exists (city-required, D-7); at town (2 of 22) the member is a `Barracks` (a housing row) or an imported militia. So the force is a CLASS, never a named member: "armed people", "people under arms", "the town's force", "the muster" (the class word, ADDENDUM 13 A.5) are licensed; "the garrison" (W12), "the militia" (only where `forces.militia.present` is the read — it is not), "the watch" (W13; the read excludes the bucket), "the guard" (struck, A.5), "the levy" (A-4: three things) are not.

**The block header lines.** STATE-KEY: five fixed rows, each read against `config.monsterThreat` (`plagued` / `frontier` / `settled`), the institution presence flags and `compound.inst`. SLOTS `{settlement}` `{band}` `{route}` — this pool names and fills `{settlement}` only; `{band}` is RESERVED, `{route}` unfilled. SECTION-TARGET `defense`. PROVENANCE + FENCE: the shape EXTENDS the `buildThreatAssessment` lattice into a pool; the walls read must ride the predicate, never a presence check on `institutions.walls`; institution presence is a STANDING fact with no recorded history; "the causal clauses here are capability clauses … never historical ones". The card outranks the header on cause: `may NOT: a cause`, and ADDENDUM 8 (2) rules that the card binds on cause; the header's "capability clauses" is the older law and licenses nothing a face writes. PDF PARITY: parity.

**The register card's six one-line registers, and which one this is.** This is the DOSSIER: the record itself; the clerk's third person; the town's name is not the default opener. Not the NPC ladder, not the Herald, not the chronicle, not the DM page, not chrome. The block's own §0b palette binds the three tags: `[ledger]` "the clerk's view — what the books, rolls and counts show"; `[street]` "the town's own talk about its condition"; `[visitor]` "what a stranger notices first, without being told". W24 and W27 cut those stances to size: the ledger tag licenses NO record noun and no citation; "a stranger" may see, never act, decide, be told or be given a name.

**The referent layers this pool's read carries (ADDENDUM 13 B, rule 1; tagged beside every claim below).** family → **NONE** (a config label of the country; a terrain-class token; no institution noun of any layer). perimeter → **BODY** (the walls bucket, read FALSE; the noun is the class's own word inside its negation). force → **BODY** (the garrison-or-militia bucket, read TRUE; the person-plural class word, unindividuated). The town, `{settlement}` → **NONE** (the record's subject through a `proper` slot; never an agent that decides or acts, W22/W23). A stranger → a STANCE, not a referent (W27). No HOLDER noun is licensed on the surface (the muster is a citable holder for the force fact only, and none of S3's reasons obtains here); no ORGAN, POWER, ROLE, AGGREGATE, EXTERNAL BODY or TRADITION layer is reached by this read.

**Two readings recorded for the chair's veto, both re-derived from the card and the rulings.**
(a) THE THREE COMPONENTS ARE ONE KEYED CONDITION, and every variant states all three. ADDENDUM 8 ruling (1) and ADDENDUM 12 R-v: the reads of a key are one keyed condition, never "a second fact"; the SKELETON RULE (ADDENDUM 7) requires all of the card's reads stated in every face. The shipped `[visitor]` and `[street]` rows never name the country; the three earlier attempts for this pool read "never add a claim" as barring the frontier from those variants. This packet reads the ruling the other way: the country is the key's DISCRIMINATOR against `plagued, NO perimeter and NO force` (the same stranger, the same armed-and-unwalled town, in a plagued country) and against `settled, nothing organized`; a face without it strikes the clause carrying the key's discriminating claim (MOVE-GRAMMAR §3.4, U9) and reads as a sibling row's. Stating a read of the key's own row is not an added claim under A6.
(b) THE LATTICE'S OWN STRING DOES NOT LICENSE A POSTURE WORD. The engine's `buildThreatAssessment` branch for this cell (`src/domain/display/threatAssessment.js:96-99`) says "Defense is reactive" and that attackers choose the point of engagement; the shipped `[ledger]` row copies it. That string is product code outside the REWRITE's corpus (REFERENT §D, OW-20: recorded for the owner as a scope question), the card does not print it, and `may NOT: a standpoint, a second fact` bars it; "the shape EXTENDS the lattice" in the PROVENANCE line names the row structure, not a licence for the lattice's adjectives. Read here as UNLICENSED; named so the chair can rule otherwise.

---

## VARIANT 1

### (1) Number and angle
`1.` `[ledger]` — index 0, canonical-at-zero (the line a falsy seed draws). Slot set `{settlement}`.

### (2) The shipped sentence, verbatim
{settlement} keeps armed people on an open frontier, which means the defense is reactive: whatever comes chooses where the fighting happens, and the town arrives afterwards.

### (3) Every claim it makes, one per line (licence · referent layer beside the read's layer)
- `{settlement}` "keeps armed people" — the town holds an organized force, standing — **LICENSED** the `force` component (`garrison || militia` present on the live roster), stated at the CLASS grain: "armed people" names neither bucket member and no count. Layer **BODY** (the garrison-or-militia bucket, a person-plural class word) beside a BODY read — match. "keeps" is the standing present; it asserts no pay, no upkeep, no roll.
- "on an open frontier" — the country around the town is frontier — **LICENSED** the `family` component (`measuredMonsterFamily(config.monsterThreat) === 'frontier'`; W14: the middle exposure tier of the country, measured, never the normaliser's default; W2: scoped to the country). Layer **NONE** (a config label of the country) beside a NONE read — match.
- "open" — no perimeter — **LICENSED** as the `perimeter` component (the walls bucket empty) ONLY where the adjective is read of the town's want of enclosure. HAZARD recorded, not a fault: the adjective sits on "frontier" and reads as the country's openness (a terrain sense the fields do not hold) as readily as the town's unwalled state; the refuters' ground "a claim opened by an ambiguity" reaches it. A face states the absence on the town or its works in words ("no wall", "nothing walled", "no works"). Layer **BODY** (the walls bucket, read false) beside a BODY read — match when so read.
- "which means the defense is reactive" — **UNLICENSED**: a `which`-clause (a hard wall of the brief; R-DA-03 the qualification as a tail); a MEANING move (MOVE-GRAMMAR §1.3: no field holds what a fact means); a posture characterisation of the arrangement that no field reads (row 1 PRESENT may NOT carry a capacity fact the field does not hold; `may NOT: a standpoint`). The lattice's own string says the same word (§0 reading (b)); it licenses nothing here. Layer: "the defense" is a function word (NONE); the fault is the move, not the noun.
- "whatever comes chooses where the fighting happens" — **UNLICENSED**: a FORECAST (a future event on a standing configuration field; MOVE-GRAMMAR §1.3; A2 state never fate); an INTENT given to an unnamed thing ("chooses" — R-DA-11 the figure policy); a totality ("whatever comes"); an event the record holds no provenance for (a fight); a second fact (`may NOT: a future, a second fact`). Layer **EXTERNAL BODY** unnamed and unpossessed — no field reaches it (W25).
- "and the town arrives afterwards" — **UNLICENSED**: a FORECAST continued; the town as an ACTING agent in an event no field records (W23 the fused-agent bar; W22 no decider/actor; `may NOT: a future`). Layer **NONE** (the town) made an agent — refused.
- Form findings, not claims: a colon carrying a gloss after a `which`-tail; a three-beat cascade (the meaning, the arrival, the response) that is the summarising beat (W6); the settlement token as the opener's head at index 0 (R-DA-17: the town's name is not the default opener; T-F8 refuses a FACE row opening on a `proper` slot).

### (4) The reads the rewrite must state
Every face of this variant states ALL THREE, and nothing else:
- **R1** the country around `{settlement}` is frontier — stated over the COUNTRY (frontier country; a frontier; the country outside), never as a totality over the town and never as "nothing threatens" (W2).
- **R2** an organized force stands at `{settlement}` — at the class grain (armed people · people under arms · the town's force · the muster as the class word), never a member (garrison · militia · watch · guard · soldiers, see V2), never a count, never its pay (the gate is not a read of this pool).
- **R3** no wall stands — the walls bucket empty, inside its own negation (no wall · no works · no perimeter · nothing walled · unwalled · no line), never a second civic object of the class, never the material or shape of the wall that is not there (W11).
The LICENSED claims of the shipped sentence are exactly R2 ("keeps armed people"), R1 ("frontier") and R3 ("open", read of the town); all three are carried; nothing else is.

### (5) The angle's stance, in one sentence
`[ledger]` is "the clerk's view — what the books, rolls and counts show" (§0b), and here it may set the three components down as the record's standing entry — the country entered as frontier, a force carried standing, no wall entered against it — in the office's own formula (entered, carried, stands on the record: R-vi's lawful SURFACE) and cite nothing (W24: the ledger tag is a standpoint licensing no record noun and no citation; the muster is the card's holder for the force fact alone, and none of S3's three reasons — two accounts, an interested count, a keeper who is a power — obtains, so "the muster roll shows" or "the roll" is a citation the budget does not license and a record word this read does not resolve, R-vi/W7/OV-5); it may NOT invent the posture ("reactive"), a fight, an arrival, a choice, a count of the armed, the pay or the upkeep, which member stands (garrison or militia), a charter hall or specialists (the Beasts key reads no `charter`, REFERENT §2.1 "specialists"), a season, or the material of an absent wall.

### (6) The turns worth keeping (the density floor)
- "{settlement} keeps armed people on an open frontier" — seven words carrying all three reads (R2 in the verb and object, R1 and R3 in the adjunct) with no joint. This is the pool's density floor: a face may carry it as it stands (inside a sentence, since a face row may not OPEN on `{settlement}` — T-F8 — and index 0 should move off the town token, R-DA-17) or in its own vocabulary at the same compression, sharpening "open" into the town's unwalled state where the ear needs it.
- "keeps armed people" — the ledger's standing verb with the force at the class grain: a lawful phrase, kept as this row's own vocabulary so the three variants do not converge on one noun (R-DA-10; A11).
- The ORDER town → force → country is lawful; the colon-and-gloss that follows is not, and the sentence stops where the lawful part stops.

### (7) What would make the rewrite a regression here
- INVENTORY: variant 1 stays at index 0 with the `[ledger]` tag and grows to four wording faces (the numbered line plus three `[face]` sub-rows), every face naming `{settlement}` exactly as the parent does (ARCH §2.5: a face whose slot set differs from its parent's is refused); one fewer face, a merged or reordered variant, or a second bracketed tag on the numbered line (ADDENDUM 3 (1)) is a trim or a malformed row.
- A LOST LICENSED READ: a face without R1 reads as `plagued, NO perimeter and NO force`'s or `settled, nothing organized`'s arrangement; a face without R3 reads as `frontier, credible deterrence`'s (perimeter AND force); a face without R2 says nothing the pool is for.
- A LOST LAWFUL TURN: giving up the seven-word compression of three reads for three flat clauses with no law behind the change (§21.4 the density law; ADDENDUM 6 the density floor against the SHIPPED sentence).
- A DROPPED ANGLE: a ledger face that reads as the visitor's (what is met coming in) or the street's (the town's talk) under the `[ledger]` tag; or a ledger face that cites — "the roll", "the books", "the muster roll shows" (W24; R-vi; §24's ceiling is one citation per unit on a licensed holder for an S3 reason, and the reason is absent).
- AN ADDED CLAIM (the walker's C-pair arm; the refuters' grounds): any of the four unlicensed claims above returning in new words — "reactive", "meets what comes", "chooses", "the fight", "arrives", "answers after", a member name, "soldiers" (V2's finding), the gate, a count ("a handful", "a few"), a season, a cause ("because the country is frontier" — ADDENDUM 8 (2): no engine rule joins the country to the arrangement; the reader draws the why from the three facts placed together).
- FORM: a face opening on `{settlement}` (T-F8); a `which`; a colon carrying a gloss; an em dash; a digit; a question; an existential opener; a future indicative; a maxim frame ("a town on a frontier …", W5); a doubled beat (W6).
- THE THREAD (MOVE-GRAMMAR §1.4.1; R-i at k = 0): as the spine this line is read first and a modifier the composer seats next reaches back for its noun; each face closes on a civic noun of the key — the force, the wall's absence, the country — never on a pronoun, "afterwards", or an abstraction. No modifier pool attaches to this cell at this dock (`attach: empty`; ARCH §6.4 reads the three-field Beasts cells at k = 0 for modifiers), so the thread is the face's own second sentence where it has one.
- SIBLING DISTANCE on the SAME PAGE (A1/A11; the same-page C7 the DEF-11 refuters named): `Invasion & War: force with NO walls` fires on the same tab whenever the force is a garrison-bucket member, and its `[ledger]` row opens "{settlement} keeps a professional force and no perimeter" — a face here may not share that opener's first two words or its "keeps … and no perimeter" predicate shape; `frontier, credible deterrence` owns "sits on an active frontier with a line and a force"; `plagued, NO perimeter and NO force` owns "no line, no force". Nothing here may contradict `Invasion & War: militia only` or `force with NO walls`, which read the same roster from the other row.
- WORD BARS on this read: "the watch" never (the force disjunct excludes the bucket, W13); "the garrison" never (W12; the member is unread); "the guard" struck (A.5); "the levy" never (A-4); "the muster roll" / "the roll" as a record never (W24; OV-5); "specialists" never (the key reads no charter); "stone", "timber", "palisade" never (W11, and there is no wall to have a material).
- FOUR FACES, NOT FOUR PARAPHRASES: the DEF-11 refuters failed a face for carrying its sibling's frame to the word class; four ledger faces differ in CONSTRUCTION (the entry, the standing line, the country fronted, the force fronted), never by a synonym in one frame (W4 the construction contract; the owner's four-faces rule).
- EAR HAZARDS recorded from the three earlier attempts (data, not findings): "against a frontier country" made the country the force's opponent (a use frame near the posture claim); "stands on the muster roll" and "written on the muster roll" cited a record this read does not resolve (W24, R-vi); "keeps … and no perimeter" borrowed the Invasion sibling's frame; a fronted "Where the country is frontier, …" read as a conditional licence (a cause by another door).

---

## VARIANT 2

### (1) Number and angle
`2.` `[visitor]` — index 1. Slot set `{settlement}`.

### (2) The shipped sentence, verbatim
A stranger finds soldiers at {settlement} and no wall for them to stand on, and can see how that would go against anything that arrived in more than one place.

### (3) Every claim it makes, one per line (licence · referent layer beside the read's layer)
- "A stranger finds … at {settlement}" — the visitor's eye: what is met on arrival — **LICENSED as the STANCE** (§0b: what a stranger notices first, without being told; W27: the stranger may see, never act, decide, be told or be given a name). Not a referent: "a stranger" is no PERSON layer and takes no act beyond seeing.
- "finds soldiers" — an organized force stands at `{settlement}` — the FACT is **LICENSED** (the `force` component, garrison-or-militia present); the WORD is **UNLICENSED** under W15 (the alias bar) as the ratified alias table reads it (ENTAILMENT §4.1 A-6: "soldiers" said over a bucket holding a housing row and a police force; safe spellings "the town's force", "the armed"): on this key the force resolves to `Citizen militia` on 20 of 22 firings, whose row ENTAILS part-time townspeople (D-8), while "soldiers" carries the professional standing of the `Garrison` row (D-7, city-required; 0 firings at city in the sample) — a member's standing asserted off a class read, a body word at the wrong member. TENSION recorded for the chair: ADDENDUM 13 B rule 5 lists "the soldiers" beside "the town's force" as a person-plural class word safe on a PAY-GATE read, and REFERENT §2.1 "soldiers (missed noun)" calls it the one always-safe body spelling; this read is not the pay gate, the alias table is the refuters' instrument for this desk, and the marker follows the stricter row and names the tension. Layer **BODY** beside a BODY read — the layer matches; the member does not. Safe class spellings: "armed people" (the shipped V1's own word), "people under arms", "the town's force", "the muster" (class word).
- "and no wall" — no perimeter — **LICENSED** the `perimeter` component (the walls bucket empty on the live roster; "wall" inside its negation is the read itself, not a second civic object of the class). Layer **BODY** (walls, read false) beside a BODY read — match.
- "for them to stand on" — the absent wall's USE, manned by the force — **UNLICENSED**: a standpoint (a purpose the record does not hold; `may NOT: a standpoint`); manning is NOT ENTAILED of any wall (ENTAILMENT D-1/D-3) and "the men on the wall" was struck as asserting manning plus a wall (ADDENDUM 13 A.5) — the counterfactual form changes nothing, since the claim is what the force would do with a wall; and the clause is the `Invasion & War: force with NO walls` `[visitor]` row's own ("no line for them to stand behind"), which fires on the SAME PAGE when the force is a garrison — a same-page paraphrase (A11; the C7 class). Layer: BODY nouns in a hypothetical relation no field computes (W23).
- "and can see how that would go" — **UNLICENSED**: an assigned reaction and an inference put on the stranger (R-DA-01: no assigned reaction; W27: the stranger sees, never judges); a subjunctive EDGE on no band field (the THRESHOLD move needs a band's own edge — this key is a row, not a band; A2); a forecast in the conditional (`may NOT: a future`).
- "against anything that arrived in more than one place" — **UNLICENSED**: a manufactured particular (an attack at several points at once — hollow specificity, fault 24; no field holds an attack's shape); a COUNT in words ("more than one place" — `may NOT: a count`); a hypothetical event with no provenance; a totality ("anything"). Layer **EXTERNAL BODY** unnamed and unpossessed — no field reaches it (W25).
- SILENCE on the country: the shipped row never states R1. Recorded as a DEFECT of the shipped sentence (the key's discriminator against the plagued and the settled stranger is missing), not as a claim; §0 reading (a) requires the rewrite to state it.
- Form findings: the "and … and" cascade with the inference as its third beat (the summarising beat, W6); the close on "place" (a hollow noun handing nothing forward); a perfect "arrived" inside the hypothetical.

### (4) The reads the rewrite must state
Every face of this variant states ALL THREE (R1, R2, R3 as in variant 1 §(4)), in the visitor's stance, and nothing else. The LICENSED claims of the shipped sentence are R2 (a force met, at the class grain — the word replaced) and R3 (no wall); R1 is absent from the shipped line and required by ADDENDUM 8 (1) / R-v.

### (5) The angle's stance, in one sentence
`[visitor]` is "what a stranger notices first, without being told" (§0b), and here it may state what is met coming in — the town unenclosed (no wall passed, no works about the place, open ground on every side) and armed people met in it — with the country the stranger comes across stated as frontier (the record's fact carried in the approach: out of frontier country, across a frontier, from the country outside, which is frontier), and it may NOT let the stranger be told anything, judge, foresee a fight, count, see a wall's use, name a member (soldiers · garrison · militia · watch), act, decide, or take a name; the visitor carries no record noun, no citation and no gate (W8, W24, W27).

### (6) The turns worth keeping (the density floor)
- "A stranger finds [the force] at {settlement} and no wall" — the visitor's frame carrying R2 and R3 on one verb in eight words (with the class-grain noun in the force's place). This is the variant's density floor: a face may keep the frame and the compression, never the member word and never the "for them" tail.
- "A stranger finds" — the visitor's lawful opener (finds · meets · comes upon · passes), kept as this row's own construction so the ledger's "keeps" and the street's idiom stay apart (A11; R-DA-10). Note for the drafter: "A stranger" opens the visitor row of most pools in this block (twelve rows at this dock); one face may keep it and the others should vary the eye (a newcomer, an arrival, whoever comes in) without making the stranger act (W27).

### (7) What would make the rewrite a regression here
- INVENTORY: variant 2 stays at index 1 with the `[visitor]` tag and grows to four wording faces, every face naming `{settlement}` once as the parent does; a fall, a merge, a reorder or a second tag is a trim or a malformed row.
- A LOST LICENSED READ: a face without R1 (the earlier attempts' reading) is the plagued-neither row's stranger or the settled-neither's by another door; a face without R3 is `frontier, credible deterrence`'s stranger; a face without R2 is the plagued-neither's.
- A LOST LAWFUL TURN: giving up the one-verb compression of force and absence for two flat clauses with no law behind the change (§21.4; ADDENDUM 6); dropping the observation frame altogether so the face reads as the ledger's entry under the `[visitor]` tag — attempt 3's "The outward standing of {settlement} is …" is the hazard: the stance must stay legible as the arrival's eye (what is met, seen, passed), with or without the word "stranger".
- A DROPPED ANGLE: a visitor face that enters the record's formula ("entered", "carried standing"), cites, or gives the town's talk ("here", "this town says"); a visitor who is TOLD (W27) or who HEARS (the street's ear).
- AN ADDED CLAIM: "soldiers" (W15/A-6, above), "the garrison", "the militia", "the watch", "the guard", a use of the wall ("to stand on", "to hold", "to man"), "would go", "would hold", any fight or arrival, a count ("more than one", "a few", "a handful"), a season, the gate or pay, "open ground" only where it means the town's want of works and not a terrain fact (the country's ground is unread — L-3: terrain is a route default on most towns).
- SAME-PAGE PARAPHRASE (A11; C7): `Invasion & War: force with NO walls` `[visitor]` — "sees soldiers at {settlement} and no line for them to stand behind, and can see how that decides where any fight would happen" — shares the shipped V2's verb, object, tail and inference and fires on the same tab whenever the force is a garrison-bucket member; the rewritten faces must not share its first two words, its "soldiers … and no line" object pair, or its "can see how" tail. Near neighbours to stay clear of: `militia only` `[visitor]` "A stranger at {settlement} meets armed townspeople …" (the same roster read from the other row — "meets armed townspeople" is that row's; a face here that says "armed townspeople" both borrows it and asserts the militia member); `settled, nothing organized` `[visitor]` "A stranger walks out of {settlement}"; `plagued, NO perimeter and NO force` `[visitor]` "A stranger arriving at {settlement} understands the danger".
- FORM: no face row opens on `{settlement}` (T-F8); no `which`; no em dash; no digit; no question; no perfect aspect on the arrival ("has come", "arrived") that makes the visit a history; no subjunctive edge.
- THE THREAD: a visitor face closes on the force, the open place, or the wall's absence — a civic noun the composer's next modifier can carry — never on "them", "place" as a hollow noun, or the stranger.
- W2: the country is frontier; never "nothing threatens" and never a totality over the town.
- FOUR FACES, NOT FOUR PARAPHRASES: four visitor faces differ in construction (the approach fronted; the force met first; the absence met first; the two-sentence form with the country carried by the thread), never by a synonym for "stranger" in one frame (W4).
- EAR HAZARDS from the earlier attempts (data): "passes no wall before meeting its armed people" made the wall an event in sequence (fine) but "its" after `{settlement}` bound the force to the town — lawful here (the town's force) and worth keeping bound explicitly (W1); "is among its soldiers at once" carried the member word; "comes up against the muster of {settlement}" read the class word as an encounter with a body the roster may not hold as a muster (REFERENT A-11: on a Barracks town it names a roll the town does not keep) — "the town's force" or "armed people" is the safer class spelling on this read.

---

## VARIANT 3

### (1) Number and angle
`3.` `[street]` — index 2. Slot set EMPTY: the shipped row names no `{settlement}`, so every face of this variant names none (ARCH §2.5: a face whose `{slot}` set differs from its parent's is refused). The town is "the town", "this town", "here".

### (2) The shipped sentence, verbatim
The town can answer trouble and cannot prevent it, and the difference costs it something every season.

### (3) Every claim it makes, one per line (licence · referent layer beside the read's layer)
- "The town can answer trouble" — **UNLICENSED** as written: a CAPACITY fact (row 1 PRESENT may NOT carry a capacity fact the field does not hold; `may NOT: a standpoint`); "trouble" is a generic threat noun tied to no read — the licensed threat word is the country's frontier standing (W2), and this sentence never names the country. The LICENSED residue beneath it: R2, an organized force stands (the town has armed people), which a face states as the standing state, never as what the force CAN do. Layer **NONE** (the town) given an act on an external thing — W23's shape; the residue's layer is BODY (the force), matching the read.
- "and cannot prevent it" — **UNLICENSED**: a capacity claim on the negative side and a forecast of outcomes (what will not be stopped; `may NOT: a future`). The LICENSED residue: R3, no wall stands — stated as the absence, never as an inability.
- "and the difference costs it something every season" — **UNLICENSED**: a CONSEQUENCE / BILL with no event provenance and no household or office row (MOVE-GRAMMAR §1.2 row 7 is double-licensed; neither licence exists here); a SEASON and a recurrence (`may NOT: a season`); "something" a hollow particular (fault 24); the town as a payer — a purse fact this pool does not read (the upkeep gate is not on this card, and even where it is, R-viii′ bars a per-thing bill). Layer **NONE** (the town) as an economic agent; refused (W23).
- SILENCE on the country: R1 is absent from the shipped row; recorded as a defect, required by §0 reading (a).
- Form findings: the antithesis "can … and cannot" is a CONTRAST shape. Under R-DA-02 / wall 5 a CONTRAST is lawful only where the rejected alternative is a sibling key — and it is: the town without a wall against `frontier, credible deterrence` (perimeter AND force). So a force-yes / wall-no contrast is a lawful MOVE for this pool, never fronted as the subject and never the closing move of more than one variant in the pool; the shipped line dresses the lawful contrast as two capacities, which is the unlicensed part. The "and … and" cascade closes on a cost (the summarising bill, W6).

### (4) The reads the rewrite must state
Every face of this variant states ALL THREE (R1, R2, R3 as in variant 1 §(4)), in the town's own idiom, with NO slot. The shipped sentence states no licensed claim in licensed form; read beneath its capacity frame, its residue is R2 and R3; R1 is absent and required.

### (5) The angle's stance, in one sentence
`[street]` is "the town's own talk about its condition" (§0b), and here it may say, in the town's plain working idiom and with no slot, that armed people stand for the place and nothing walled does, and that the country outside is frontier country (the town's own word for what lies beyond it, scoped to the country, W2); it may NOT give the town a mind (knows · believes · treats · fears · has never needed to think — the FEELING non-move, R-DA-14; the plagued-neither `[street]` row's "understood by everyone" is the shape to avoid), a decision or a plan (W23 fused agent; W22), a capacity, a cost, a season, a forecast, a maxim about people or walls, or a citation, and it never names `{settlement}`.

### (6) The turns worth keeping (the density floor)
- No clause stands verbatim under the card: every clause is a capacity or a bill.
- The lawful RESIDUE as SHAPE: the sibling-licensed CONTRAST (armed, and not walled) delivered as the pool's SHORT LINE — the street is where the register's short line lives (R-DA-05: "the short line exists"; the pool's shipped rhythm is 16 · 28 · 17 words, and a face here of six to ten words is the spread the register asks for). A face may keep the contrast at that compression, once in the pool as a close (wall 5), and must not keep the words.
- The street's plain subject ("the town", "here") is lawful and is this row's construction against the ledger's entry and the visitor's approach (A11; W4).

### (7) What would make the rewrite a regression here
- INVENTORY: variant 3 stays at index 2 with the `[street]` tag, slotless, and grows to four wording faces with NO `{settlement}` in any of them; a slot written in is a projector refusal (ARCH §2.5), and a fall, a merge, a reorder or a second tag is a trim or a malformed row.
- A LOST LICENSED READ: a face without R1 (the earlier attempts dropped it here too) is `plagued, NO perimeter and NO force`'s street line or `settled, nothing organized`'s; without R3 it is `frontier, credible deterrence`'s "arrangements are ordinary"; without R2 it is the plagued-neither's "does not defend itself".
- A LOST LAWFUL TURN: giving up the short line and the sibling-licensed contrast for a flat two-clause restatement with no law behind it (§21.4; ADDENDUM 6); making the street's face longer than the ledger's.
- A DROPPED ANGLE: a street face in the ledger's formula ("entered", "on the record", "carried standing") or the visitor's eye ("a stranger", "coming in"); a street face that gives the town a mind or a plan.
- AN ADDED CLAIM: "can", "cannot", "answer", "prevent", "costs", "every season", "trouble" untied from the country, "knows", "believes", "has decided", "arms itself when needed" (the DS-DEF-5 fused-agent finding D-F16), "people can be gone around" (the Invasion sibling's maxim), a member name, "soldiers" (V2 §(3)), a count, the gate or pay, a cause ("because", "so", "which is why").
- W2: the country is frontier; never "nothing comes here", never a totality over the town.
- SAME-PAGE PARAPHRASE (A11; C7): `Invasion & War: force with NO walls` `[street]` — "The town's defense is people rather than works, and people can be gone around" — fires on the same tab whenever the force is a garrison-bucket member; a face here may not take "people rather than works" or its maxim tail. Near neighbours: `militia only` `[street]` "The town knows its own country …"; `neither walls nor force` `[street]` "The town's plan for an army …"; `plagued, NO perimeter and NO force` `[street]` "The town does not defend itself. What it does is watch, and move …"; `settled, nothing organized` `[street]` "has never needed to think about what is outside it". Nothing here may contradict the Invasion row that reads the same roster.
- FORM: no `which`; no colon carrying a gloss; no em dash; no digit; no question; no existential opener; at most one negated-surface opener in the variant (W10); the contrast never fronted as the subject and never the closing move of more than one variant in the pool (wall 5).
- THE THREAD: a street face closes on the force, the wall's absence, or the country — never on "it", "something", or the difference.
- FOUR FACES, NOT FOUR PARAPHRASES: four street faces differ in construction (the short line; the force fronted; the country fronted; a two-sentence form carrying the thread on the force), never by a synonym in one frame (W4).
- EAR HAZARDS from the earlier attempts (data): "Soldiers answer the country here. No wall does." carried the member word and made the force act on the country (a capacity by another door); "goes armed against the country" made the country the opponent; "The town, unwalled, keeps armed people" is the lawful short-line SHAPE at six words (kept as a model of length, not of words); "Defense at the town rests on no standing work, and on the people who carry arms" fronted the absence and closed on the force — a lawful order, its "who" a relative on persons that a refuter may read as W22's grain (persons as the load-bearing referent) — the class noun without the relative is the safer form.

---

## Closing inventory (the marker's count)

- Shipped variants: **3** (`[ledger]` at index 0 with `{settlement}`; `[visitor]` at index 1 with `{settlement}`; `[street]` at index 2, slotless). None is trimmed, merged or reordered; the rewrite delivers 3 variants × 4 faces = 12 wordings, the counts only rising; every face keeps its parent's slot set exactly.
- The ONE read, three components, stated in EVERY face: R1 the country frontier (over the country, W2) · R2 an organized force at the class grain (armed people · people under arms · the town's force · the muster as class word) · R3 no wall (the walls bucket empty, inside its negation). No predicate beyond the row; no relation; no attach; no S2 clause seat (no engine-computed consequence of this row is read); no covert twin; `{band}` and `{route}` never written.
- Licensed holder: the muster, for the FORCE fact only; no S3 reason obtains; the lawful citation count on every unit of this pool is ZERO; the `[ledger]` tag licenses no record noun (W24).
- Claims of the shipped rows that survive the card: variant 1 — three of six (R2, R1, R3 with the "open" hazard); variant 2 — two of six (R2 with its word replaced, R3), R1 missing; variant 3 — none in licensed form, R2 and R3 as residue, R1 missing. Everything else was bought with an effect: a meaning, two forecasts, an inference, a count, a use, two capacities, a bill and a season.
- Word bars binding every face (the desk's tables): "the watch" (W13; the read excludes the bucket) · "the garrison" (W12) · "the guard" (struck) · "the militia" as the member · "soldiers" (W15/A-6, with the rule-5 tension named) · "the levy" · "the muster roll" / "the roll" as a record (W24; OV-5) · "specialists" / "the charter hall" (unread) · any wall material or shape (W11) · "the line" only inside its negation as a wall word · "open ground" only of the town's want of works.
- Same-page siblings to keep distance from (A11; C7): every row of `Invasion & War: force with NO walls` (fires on the same tab when the force is a garrison-bucket member) and `militia only` (when it is the militia); the three other Beasts rows a reader meets across towns.
- Owed to the chair, named so a drafter does not decide them silently: (i) §0 reading (a) — the visitor and street variants STATE the frontier under ADDENDUM 8 (1) / R-v, against the three earlier attempts' "never add" reading; (ii) §0 reading (b) — the lattice's own "Defense is reactive" (`threatAssessment.js:96-99`) licenses no posture word on this card (OW-20); (iii) V2 §(3) — "soldiers" refused under alias A-6 on a militia-resolving read while rule 5 lists it as a safe class word on the pay gate; (iv) V1 §(3) — whether "on an open frontier" alone carries R3, or R3 must be stated on the town's works in words (this packet: state it in words); (v) V3 §(3) — the force-yes / wall-no CONTRAST read as sibling-licensed by `frontier, credible deterrence` (wall 5), at most once as a close in the pool.
