# DS-DEF-2 · `Invasion & War`: walls AND professional garrison — REFUTE (round 2)

Seat: Opus 5 — REFUTER (the verifier) for the Fable chair. Judged against: the printed licence card (read-only, laneRW-DEF2), `skeleton.md` (the licensed reads, the stances, the turns worth keeping, §8's pool-level fences), `draft-round-2.md` (the draft the refinement started from), `refine.md` (the named targets and the laws claimed for each change), the register card with S2/S3, Part B §1 · §16–§16.2 · §18 · §20 · §21 · §22 · §23, MOVE-GRAMMAR §1–§3 and §4.4.1–§4.4.3, CLERK-LAWS §2.4.1 · §2.6.1, ARCH v2 §2.5 · §8.3, and ADDENDA 12 and 13 (W1–W27).

The rows judged are `kept.md` (the rows standing after the refinement). Face 0 = the numbered tagged line; faces 1 to 3 = the three `[face]` sub-rows in order.

STATUS: COMPLETE — twelve faces judged (written face by face under the checkpoint law): 9 PASS, 2 FAIL, 1 WITHHELD.

## 0. WHAT WAS CHECKED, AND AGAINST WHAT

- The card's two licensed reads (`walls === true`, `garrison === true`) and their conjunction, in every one of the twelve faces; the card's may-NOTs (a count, a cause, a season, a future, a standpoint, a second fact, another civic object of the class `force`); the refused columns.
- The pool-level fences of `skeleton.md` §8: the slot sets per parent (`{settlement}` · `{settlement}` · `{}`), no face opening on the slot (T-F8), ONE force object and one spelling per face, silence on a militia, no band or badge word on a body read, no material (W11), no pay or gate word (W16), no siege / raid / assault / attacker / stores (W17), no record noun and no citation (W24), no person as agent (W22), no fused agent (W23), the stranger never acting or deciding (W27), no em dash, digit, which-clause, exclamation.
- The draft (`draft-round-2.md`) beside the refinement (`refine.md`), for §21.4's regression test in BOTH directions (against the draft and against the shipped rows), and for W4 (the declared construction per face kept).
- The block's own landed corpus in the dock's annex (`docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`, the `Beasts & Monsters` and `Invasion & War` neighbours at :2680-2712, read-only) for the referent law's rule 3 across the tab — the spellings `the works` · `the muster` · `the town's force` · `the soldiers` are the block's own and no face drifts from them into `the garrison`, `the militia`, `the watch` or `the guard`.

**Nine of twelve faces PASS. Two FAIL. One is WITHHELD.** No face carries a slot the bag lacks, a digit, an em dash, a which-clause, a citation, a second force object, a militia, a badge word, a material, a crisis word, a pay word, a person as agent or a baked `the garrison`. No face adds a claim the card does not license, and no face drops a licensed claim the shipped sentence carried (arm A6 reads equal across all twelve: the works stand · the soldiers stand · the two on one row).

## 1. THE VERDICTS, ONE LINE PER FACE

Face 0 is the numbered tagged line; faces 1 to 3 are the `[face]` sub-rows in order.

| variant | face | verdict | law | quote (≤ 12 words) | finding | cure |
|---|---|---|---|---|---|---|
| 1 `[ledger]` | 0 | PASS | — | `The works at {settlement} stand entered, and so do the soldiers.` | Both reads in the office's own formula (R-vi / W7: `entered` is the ledger's lawful surface, not a citation); the conjunction on the `and so do` ellipsis; closes on a body; slot present and not the opener (T-F8); no record noun (W24). | — |
| 1 `[ledger]` | 1 | PASS | — | `Soldiers are carried standing at {settlement}, and what the town has built…` | The repeated formula is the register card's own licence ("Repeat the office's formula because it is the office's. The WORD may recur; the FACT must not") and the two clauses carry two different reads, so W6's doubled beat is not engaged; `what the town has built` is the always-safe wall spelling (entailment D-6). | — |
| 1 `[ledger]` | 2 | PASS | — | `Two things are entered together at {settlement}, the works and the town's muster.` | `Two things` is a count IN WORDS of the key's own two reads, not a count over persons or over a civic object (R-DA-16; the skeleton's own ruling at VARIANT 2 (3)); `together` carries the key's conjunction; `the town's muster` is the class word with the possessor seated (ADDENDUM 13 A.5; OV-34). | — |
| 1 `[ledger]` | 3 | PASS | — | `Both the works and the town's force are set down at {settlement}…` | `set down` is the office's formula, not a record noun (W24); `Both … and …` carries the conjunction; closes on a condition (R-DA-04's varied kind); differs from face 2 in subject and landing noun, not in vocabulary alone (W4). | — |
| 2 `[visitor]` | 0 | PASS | — | `A stranger at {settlement} sees the works and the soldiers together.` | The skeleton's own density floor held whole: the visitor's frame, the two bodies as a pair, and the shipped `together` — the one lawful shipped word that carries the key's conjunction. The eye only sees (W27). | — |
| 2 `[visitor]` | 1 | PASS | — | `What meets a stranger at {settlement} is the works, and the soldiers.` | `meets` is presentational and is the block's own verb for this eye (the landed sibling row `No works and no muster meet a stranger at {settlement}`), so R-DA-11's inanimate-intent floor is not engaged; the cleft is a construction difference from face 0, not a synonym swap (A5/W4). Closest pair in the pool; distance holds. | — |
| 2 `[visitor]` | 2 | **FAIL** | OV-34 (referent table §3) with ADDENDUM 13 A.5 as re-cut; W15 the alias bar; alias A-4 | `At {settlement} a stranger sees both the muster and the works.` | `the muster` stands here as a BODY word on a PRESENT body read with NO possessor seated. OV-34 ratifies the class word only "as a BODY word on a body read WITH THE POSSESSOR SEATED"; every other lawful use in this pool and in the block's landed neighbours seats it (`the town's muster`; `the town keeps no muster`; `The town holds no works and no muster`). Unseated, the word is the alias A-4 trap — four engine senses ride it (the posture ramp, `hasMilitia`, the compelled banner, the cap on arms) — and a stranger who SEES a muster reads the act, not the body. The draft's own wording here (`the town's force`) was seated; the refinement unseated it while shortening. | Seat the possessor and change nothing else: `At {settlement} a stranger sees both the town's muster and the works.` (No claim added; the fronted place, the `both … and …` frame and the force-first order are kept, so W4's construction contract holds.) |
| 2 `[visitor]` | 3 | WITHHELD | (skeleton VARIANT 2 (4) and (7): the conjunction carrier; §21.4 read against the draft) | `The stranger finds the town's force.` | The key's conjunction is carried by nothing but the repeated verb across a sentence boundary: the draft stated it (`The town's muster stands with the works`) and the refinement removed the carrier for a named law (the manning reading of `stands with`) without re-carrying it, leaving the two reads as two separate findings. Against the finding: the visitor's single arrival and the repeated eye do place them in one seeing, and the skeleton's own stance line reads the eye as the unifier. UNPROVEN, not cleared — it is the thinnest conjunction of the twelve. | If the chair rules the carrier owed: `The stranger finds the town's force as well.` — an additive, no preposition, no manning, no claim added. |
| 3 `[street]` | 0 | PASS | — | `The town has the works, and the muster besides.` | The possessor is seated by the have-frame (`The town has … the muster`), which is the block's own seating shape in its landed neighbours; `besides` carries the conjunction with no preposition; the shipped row's one lawful residue (`The town` as the street's subject) is kept as the subject it was. | — |
| 3 `[street]` | 1 | **FAIL** | W20 (the layer bar: a posture is not the body read) with the entailment law item 1 and ADDENDUM 13 A.5's struck manning spelling (D-3, D-7: manning NOT ENTAILED) | `The works are up, and the soldiers are standing.` | `are standing`, predicated of PERSONS and set in parallel with a wall that is `up`, states a POSTURE — the soldiers on their feet, posted, standing by. Manning and readiness are both outside this card (readiness is the arm's AGGREGATE badge the pool does not read; "the men on the wall" is struck at A.5). Every lawful use of `standing` in this pool attaches to the RECORD (`carried standing`, `as standing`) or to the WORKS (`finds the works standing`), where it means present; on persons the word cannot mean that, and the face is the pool's one place where the word changes referent (R-DA-22, one term for one thing). | State the presence with no posture, keeping the two short copular clauses and the works-first order: `The works are up, and the soldiers are in the town.` (The roster read IS the town's, so the placement is licensed and no claim is added; the refiner's own candidate `the soldiers are the town's` stays refused — D-7, whose pay and whose command are not entailed.) |
| 3 `[street]` | 2 | PASS | — | `The town keeps its works. In the town stand the soldiers.` | `keeps` is the street's possession verb the skeleton itself licenses ("{settlement} keeps its {defwork}" was kept as a turn at the taste), never the gate's noun `the keeping` (W16); `its` follows its own subject (W1); the thread carries `the town` forward at k = 0 (R-i); the locative inversion is the face's DECLARED construction and W4 requires it kept — and locative inversion is plain modern English, not the antique syntax R-DA-18 bars. | — |
| 3 `[street]` | 3 | PASS | — | `What the town holds is the works, and the town's force.` | `holds` is possession, and the block's own landed rows use it that way (`The town holds no works and no muster`), so the siege reading the shipped row carried (`could be held`) is not re-opened; the cleft differs from face 0 in verb, conjunction device and landing noun; the possessor is seated (W1, A.5). | — |

## 2. THE READ-ALOUD NOTE, ONE PER VARIANT (information only, no verdict rides on it)

- **Variant 1 `[ledger]`.** The four land as one office speaking: `stand entered` is the sharpest thing in the pool and the ellipsis after it is genuinely the clerk's economy. Face 1 is the one that asks something of the ear — `carried standing … is carried standing` is seventeen words with the formula twice, and it reads as deliberate only if the reader has already heard the formula once; at index 0 of a reading it will be heard as a stumble. Face 2's count frame reads lightest of the four.
- **Variant 2 `[visitor]`.** Face 0 is the best line in the pool read aloud — eleven words, no preposition, the conjunction landing last on `together`. Face 1's comma before `and the soldiers` gives the soldiers a beat and works. Face 3's two sentences read flat in the mouth: the second is five words with the subject and the verb both repeated from the first, and after `finds the works standing` the ear expects the second finding to be bound to the first, not restarted.
- **Variant 3 `[street]`.** Face 0 is the pool's shortest and its most idiomatic — `and the muster besides` is the one place the record sounds like a town talking. Face 2's inversion is the most marked syntax in the twelve; it survives on W4 but a reader hears the register lift for one clause. Face 1 reads plainest of all and is the face whose second clause the ear completes as `standing [guard]`, which is the finding above.

STATUS: COMPLETE — twelve faces judged, three variants noted, nothing pending.
