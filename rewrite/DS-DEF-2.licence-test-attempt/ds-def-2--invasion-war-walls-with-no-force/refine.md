# REFINE (arm A) — DS-DEF-2 · pool `Invasion & War: walls with NO force`

Seat: Opus 5 (Fable-unvalidated), REFINER — a different author than the drafter, per §23.
Refined from `draft-round-2.md` (which is also the pool's CURRENT annex text, `RECEIPT_POOLS_DOSSIER_STATE.md:2721-2733`), against the licence card printed in `laneRW-DEF2`, `skeleton.md`, the REGISTER CARD (S2, S3), Part B §1, §16–§16.2, §18, §20, §21–§23, MOVE-GRAMMAR §1–§3 and §4.4.1–§4.4.3, CLERK-LAWS §2.4.1 / §2.6.1, ARCH §2.5 and §8.3, and ADDENDUM 12's W1–W10 with ADDENDUM 13's W11–W27.
Three variants in, three out, under their own vids, their own angle tags, their own order; none added, removed or merged. Twelve faces in, twelve out, one for one. The typed lines (ROLE · READS · RELATION · ATTACH · FORM · MOVE) are untouched and are not repeated.

STATUS: COMPLETE — three variants, twelve faces, zero refusals, zero new failing measures.

---

## THE REFINED ROWS (paste under the pool's heading, replacing the three variant rows)

1. `[ledger]` The works at {settlement} are entered as standing, and to hold them the town has neither garrison nor militia.
   - `[face]` No force stands at {settlement}, and what stands is the works.
   - `[face]` Set down at {settlement} are the works standing, and no garrison or militia in the town.
   - `[face]` At {settlement} the town carries no force of its own, and its works stand entered.
2. `[visitor]` A stranger at {settlement} sees the works standing and no force at them.
   - `[face]` To a stranger at {settlement} the works stand, and with them stands neither garrison nor militia.
   - `[face]` A stranger finds no force at {settlement}, and the works standing.
   - `[face]` A stranger comes to the town of {settlement} and sees the works. No garrison and no militia stand in the town.
3. `[street]` The town has the works and neither a garrison nor a militia to hold them.
   - `[face]` What the town has built stands, and the town keeps no garrison and no militia.
   - `[face]` The town is without a garrison and without a militia. The town's works stand.
   - `[face]` The works stand, and the town has no garrison and no militia.

--- NOTES

### One line per face — what changed, and the law or target behind it

**v1 `[ledger]` f1 (the numbered line) — KEPT VERBATIM.** The pool's canonical ledger entry already sits at the ceiling: the office's own formula (`entered as standing`, R-vi/W7, no record noun and no citation, W24), all three legs stated, and the licensed form of the shipped compression `has walls and nobody to put on them` at the same weight with the two rows NAMED (skeleton §1.6); every rewording I tested traded that density for cosmetic distance, which §21.4 names as the regression.

**v1 f2 — KEPT VERBATIM.** `No force stands at {settlement}, and what stands is the works` is the sharpest line in the pool — eleven words, the turn on `stands`, the key's own compression, landing on the works for the composer's pickup; it is also the ONE pseudo-cleft I allowed to survive the tic cure below, and making it plainer or longer would be a change with no law behind it (§21.4, the density law).

**v1 f3 — CHANGED, absence limb re-spelt.** `and no force in the town` → `and no garrison or militia in the town`: the named rows are strictly more licensed specificity than the compression (§21.4's "what replaces it must be as sharp as the card allows"; C-READ(b) + C-READ(c) apart), and it un-duplicates `no force` with f2 so the variant now spends four distinct absence surfaces across its four faces.

**v1 f4 — REBUILT.** `What the town has built at {settlement} stands entered. To hold the works the town has no force.` → `At {settlement} the town carries no force of its own, and its works stand entered.`: three targets at once — it removes the fourth fronted free-relative of the pool (the tic cure, §21.1 "zero tics"), it gives the variant its one reversed read order (absence → works) that no other ledger face carries (the refiner's "the order of the reads" distance), and it moves `{settlement}` off the pool's uniform mid-sentence `at` placement (the one fronted placement in the pool, lawful because the opener is `At` and not the proper token, T-F8 and wall 10 both untouched); `no force of its own` is the skeleton's own licensed spelling (§4.1) and `its` binds to `the town`, the preceding clause's subject (W1).

**v2 `[visitor]` f1 (the numbered line) — KEPT VERBATIM.** The skeleton names `A stranger at {settlement} sees` as the lawful turn that must survive in this variant (§2.6) and `sees the works and no force at them` as the licensed form of the shipped compression at the same weight; it is the density floor and the refiner does not smooth it.

**v2 f2 — REBUILT.** `What a stranger finds at {settlement} is the works, and neither garrison nor militia.` → `To a stranger at {settlement} the works stand, and with them stands neither garrison nor militia.`: the cleft goes (tic cure), the stranger is re-seated as the EXPERIENCER rather than the grammatical agent — which is the safest reading of W27 (the eye may see, never act) — the subject becomes the works where every other face of the variant makes it the stranger (construction distance, not vocabulary distance), and the comitative inversion gives the variant its one strong close on a named row.

**v2 f3 — CHANGED, tightened.** `A stranger finds no force at {settlement}, and sees the works.` → `A stranger finds no force at {settlement}, and the works standing.`: the second perception verb echoed f1's `sees` inside one variant (best-ai fault 10, sameness, the same fault round 2 cured once in this variant), and the gapped coordination lets one verb govern both legs while `standing` restores C-STAND, so the face loses a word of scaffolding and gains a licensed read-marker at the same eleven words.

**v2 f4 — CHANGED, twice.** `A stranger comes to {settlement} and sees the works. No garrison and no militia stand at the works.` → `A stranger comes to the town of {settlement} and sees the works. No garrison and no militia stand in the town.`: the draft repeated `the works` verbatim across the sentence seam, which reads as the doubled beat (W6) rather than as a thread, so the carried-forward noun moves to `the town`, taken from the naming form `the town of {settlement}` (W5) — which is also the pool's second cure for the uniform `at {settlement}` placement — and R-i is satisfied by a NOUN carried forward, never by a pronoun.

**v3 `[street]` f1 (the numbered line) — KEPT VERBATIM.** It carries the shipped opener `The town has`, the sibling-licensed contrast shape (wall 5 / R-DA-02), the two rows named, and the A1 distance from the `Beasts & Monsters` partner row on the same defense tab that round 2 bought; it is the street's density floor (skeleton §3.6) and any smoothing is the §21.4 regression.

**v3 f2 — CHANGED, absence limb re-voiced.** `and no force is kept in the town` → `and the town keeps no garrison and no militia`: the passive with no agent is the flattest surface in the pool, the active seats the town as the possessor the street's stance needs, and the named rows raise specificity (§21.4); the repeated `the town` is forced and not a tic — a pronoun there would bind to the works, the subject of the preceding clause (W1), so the noun is repeated deliberately.

**v3 f3 — REBUILT.** `The town is without a garrison and without a militia, and what it has is the works.` → `The town is without a garrison and without a militia. The town's works stand.`: the closing cleft was the pool's fifth free-relative fronting and goes with the rest (tic cure), and the replacement buys the pool a rhythm it did not own — the long negative limb answered by a four-word positive, the register card's "The short line exists; rhythm follows what is being said" — with `The town's works` carrying the noun `town` forward across the seam (R-i) and the possessive seated on the town, never on the works (W1).

**v3 f4 — REBUILT.** `The town has the works. The town keeps no garrison and no militia.` → `The works stand, and the town has no garrison and no militia.`: the draft's two sentences opened on the same two words and handed nothing across the seam, which is the inventory line the gate and the refuters both refuse (skeleton §4.5); the replacement makes the works the subject — the one face of the variant that does — states all three legs in one clause pair, and becomes the pool's short face, which is what widens the within-pool word-count spread R-DA-05 asks for.

### Reported, not gating (the chair's table)

- **The tic this round cured** (both counts executed by script over the two files' face rows, not asserted). Fronted free-relative or pseudo-cleft constructions: **5 of 12 faces in the draft → 2 of 12** (v1 f2's turn on `stands`, kept as the pool's ceiling line; v3 f2's `What the town has built`, kept because it is W15's always-safe spelling of the wall class). `{settlement}` placement: the draft put **8 of 8** slot-bearing faces on a bare mid-sentence prepositional phrase (seven `at {settlement}`, one `to {settlement}`), none fronted and none in the naming form; the refined set reads **6 of 8** mid-sentence `at`, one fronted `At {settlement}` (v1 f4) and one naming form `the town of {settlement}` (v2 f4).
- **The walls, re-scanned by script over the twelve refined faces.** 0 colons, 0 semicolons, 0 em dashes, 0 exclamation marks, 0 question marks, 0 digits, 0 percents, 0 `which`-clauses, 0 expletive openers, 0 second person, 0 citations, 0 record nouns (`roll` · `books` · `record`), 0 material words (`stone` · `timber`), 0 person words (`nobody` · `anyone` · `no one` · `people` · `men` · `unmanned`), 0 `wall` · `perimeter` · `line` · `watch` · `gate` tokens, 0 definite body nouns for an unresolved row (`the garrison` · `the militia`). The gate's one round-1 failing measure (`punctuation.colonRate`) stays at 0 and is not re-opened.
- **W10, one negated-surface opener per variant:** v1 f2 (`No force stands`), v2 f4's second sentence (`No garrison and no militia stand`), v3 none — `The town is without` opens on a positive verb and is not a negated surface. Unchanged in count from the draft.
- **The bars W1–W27:** unchanged in disposition from the draft's N.4 audit, re-run on the refined text. The three touched by this round are named in the face notes above: W1 (v1 f4's `its`, v3 f2's repeated noun, v3 f3's `The town's`), W5 (v2 f4's naming form), W6 (v2 f4's seam). W11–W19 and W20–W27 are not engaged by any edit: no material, no garrison asserted present, no watch in either polarity, no engine label, no gate word, no crisis, no stressor, no creed, no body word on an aggregate, no baked power, no person, no fused agent, no external body, no visibility mark.
- **Claim equality.** Every face of every variant states all three legs (the works standing · no garrison · no militia, with `no force` as the key's own licensed compression of the last two); no claim was added and none dropped; A6 holds across each variant's four faces, and the slot sets are equal inside each variant (v1 four of four `{settlement}`; v2 four of four; v3 zero of four, the shipped v3's own set).
- **Measures.** Twelve faces; mean 14.83 words (draft 14.75); range **11 to 21** (draft 11 to 19); sd **2.88** (draft 2.59), moved toward R-DA-05's ≥ 4.0 direction by the rebuilt short and long faces, not by padding. Two-sentence faces 2 of 12 (draft 3). Pronoun closers 2 of 12, unchanged — both are the marker-named density floors (v2 f1 `at them`, v3 f1 `to hold them`) and neither was added this round.
- **Sibling distance.** The three variants' first two words stay distinct (`The works` · `A stranger` · `The town`), A11 at the grain it is measured on. The two numbered lines of v1 and v3 still share `to hold them`: that is the phrase the skeleton names as the licensed form of the shipped compression for BOTH variants (§1.6 and §3.6), so it is reported and deliberately not cured — trading it for distance is the §21.4 regression. `militia` is the final word of 4 of 12 faces (draft 3); the licensed absence vocabulary is small, only one face of one variant ever renders, and the CLOSE KINDS still vary (a named absence · a compressed absence · the works standing · the town), which is what R-DA-04 measures.
- **Carried from the draft, unchanged and still the chair's.** N.7's four items stand as written: the slot returned on variant 3; the page-level A1 / C7 finding against the DS-DEF-2 row SET (this pool's claim set is a subset of the `Beasts & Monsters: plagued, perimeter but NO force to hold it` pool's on a plagued walled force-less town, and both render on one defense tab — a composition finding, not a wording one); `no garrison` / `no militia` as the class words of an ABSENCE under W12; and zero citations on this pool under skeleton §4.4.

Seat: Opus 5 (Fable-unvalidated) — refiner packet, arm A; three variants, twelve faces, four kept verbatim on the density law and eight refined; zero refusals; the packet is whole.
