# SKELETON — DS-DEF-2 · `Disasters & Famine: granary AND hospital` (the marker's packet; Fable, 2026-09-11)

Seat: Fable 5.1 — marker for the REWRITE, working for the Fable chair. This file is the only thing this seat wrote. Everything below is judged against the printed licence card, the census row it was printed from, the reader function the card names and the producer flags behind it, the annex block's header lines, the register card (with S2 and S3), Part B §1 and §16–§24, MOVE-GRAMMAR §1–§3 and §4.4.1–§4.4.3 (with §1.4.1 the thread), CLERK-LAWS §2.4.1 and §2.6.1, ARCH v2 §2.5 and §8.3, brief ADDENDUM 13 parts A and B (the entailment law, the referent law, the writer bars W11–W27), the two ratified tables' rows that name this pool, and `rewrite/rulings-DEF2.txt`; where a shipped clause reads well but no field licenses it, it is UNLICENSED here and the law is named.

Status: COMPLETE — section 0 (the card, the reads, the producer, the header lines, the register card, the shipped rows, the tables' findings, the seat's fences), the three shipped variants each with items (1) to (7), section 8 (the pool-level fences, whole) and section 9 (ten open rows for the chair). Variant count: THREE (`[ledger]` · `[street]` · `[counterforce]`), twelve faces owed at four per variant.

## 0. THE CARD, THE KEY, THE TWO READS AND THEIR LAYER (what the faces are wired to)

### 0.1 The licence card as printed (`node scripts/prose-licence-card.mjs DS-DEF-2 'Disasters & Famine: granary AND hospital'` in laneRW-DEF2, read-only)

```
LICENCE (block DS-DEF-2 · role spine · key `Disasters & Famine: granary AND hospital`)
  reads:      disasterRowSituation(granary, hospital, church) (via DISASTER_ROW_POOL in defenseStateProse.js)
              (absent ⇒ no candidate; a modifier is silent, never "false")
  predicate:  disasterRowSituation(granary, hospital, church) (via DISASTER_ROW_POOL in defenseStateProse.js) === granary, hospital
  bag:        {band: RESERVED, route: proper, settlement: proper}
              FILLED at this block's call sites: {settlement}
  relation:   (a spine takes no relation)   ← a spine IS the seat and carries no relation; the relation is the MODIFIER's property, fixed at its freeze (ARCH §4.5)
  seat/form:  (not a seat-taker) / sentence      move: (none declared)     angle: counterforce ledger street
  attach:     (empty: a spine takes no attach set)
              n/a
  echo:       spine mounts 1 (tabs: defense) · modifier mounts 0 (none)
              the echo table is keyed on this pool's WHOLE table-rung reading (truncated at the file's first dot) and NOT on a producer-token root, so every pool that selects a row of `DISASTER_ROW_POOL` shares ONE echo key: a mount counted there may be a sibling ROW of the same table
  covert:     no
  source:     (none) · standing SOURCE-UNRESOLVED
              NO citation is licensed: a face naming a record holder here is refused by arm A13
  may claim:  that the reader `disasterRowSituation(granary, hospital, church)` selects the row `granary, hospital` of `DISASTER_ROW_POOL` in `defenseStateProse.js`, as a STANDING fact of the record
  may NOT:    a count, a cause, a season, a future, a standpoint, a second fact, another civic object of the class `care`
  audience:   player (no mark)
  REFUSED COLUMNS, always: a totality over persons; an exemption from a duty (whoIsExempt is null everywhere); a named character and that character's fate (product scope); a theological claim about a deity (the deity doctrine)
```

The census row behind the card (`docs/content/wiring-census.json`, `rows[]`, block DS-DEF-2, pool `Disasters & Famine: granary AND hospital`): `status RESOLVED` · `rung table` · `keyFunction DISASTER_ROW_POOL` · `readsGrain branch` · `reads` = the one synthetic label above (`fieldsRead` and `branchReads` the same) · `narrowed false` · **`objectClasses ["care", "storehouse"]`, `objectClass "care"`** (this pool SPENDS two civic classes: the storehouse class — granary · silo · storehouse · warehouse — and the care class — hospital · infirmary · healer · medical · physician · ward; the projector's class guard T-F12 keeps any modifier of either class off this spine, and the card's own may-NOT adds "another civic object of the class `care`": ONE care object, never two) · `sites ["defense.threatAssessment"]` · `k 2` · `rateBp 3542` (the key fires on about thirty-five towns in a hundred of the RATE sample) · **`source {kind: "", kinds: [], fields: {granary: "", hospital: "", church: ""}, holder: null, holderReason: "no mapping row resolves any field this pool reads", standing: SOURCE-UNRESOLVED, twoSource: false}`** · `variants 3` · **`grammars 1`** (every shipped variant is ONE sentence — the pool is uniform in sentence count, the fault MOVE-GRAMMAR §0 measures; see §8) · `slotsFilled ["settlement"]` · `slotsNamed ["settlement"]` · `slotsWithoutProvider []` · `covert false` · `absent {}` · `attach []`. No `dm-only` mark on any of the three rows.

### 0.2 The read, unpacked to its producer — read for what the key MEANS, never as a licence of its own

The caller (`defenseStateProse.js:661-663`) reads `disasterRowPoolKey(civicFlag(compound.hasGranary), civicFlag(compound.hasHospital), civicFlag(compound.hasChurch))`; `civicFlag(v)` is `v === true` (`:309-311`); `disasterRowSituation` (`:580-586`) returns `'granary, hospital'` when `granary` is true AND `hospital` is true — the `church` argument is NOT CONSULTED on this branch (it is read only where `hospital` is false). So the key is TWO KEYED CONDITIONS of two producer booleans and nothing else:

| input | how the producer derives it (`src/generators/priorityHelpers.js:63-64`) | what the flag records | REFERENT LAYER (ADDENDUM 13 B A.1) | the lawful nouns | the unlawful nouns |
|---|---|---|---|---|---|
| `compound.inst.hasGranary === true` | `hasAny(names, ['granar'])` — a substring over the LIVE roster's institution names; matching rows `Town granary` (`institutionalCatalog.js:925`, `required: true` at town), `City granaries` (`:1590`, required), `State granary complex` (`:2505`) | that a GRANARY BUILDING is on the roster; and, as a TIER PROXY (label trap L-20, `rulings-DEF2`): TRUE on every town-and-up, FALSE on every village-and-below whatever it stores (the thorp `Communal root cellar` is a reserve to `isolationSupport.js:47` and not to this flag) | **BODY** — the referent table §2.1 row "granary · the stores": `BODY: flag hasGranary (substring granar)`, filed under `DS-DEF-2 Disasters ×5`; a `granary` holder row was drafted and WITHDRAWN (HOLDER: none; W24: no record word) | "the granary" (the BUILDING — the `storehouse` class word); "a place for grain"; entailed by D-12 (HOLDS): that grain is stored, that the store buffers a harvest, communal ownership at town — each a MAY, never a floor (W9) | the STOCK and its level ("full", "well stocked", "thin", "holds food against …" as a quantity, "the stores" as a reading — the `store` class is a DIFFERENT civic object, split at 8a-8, A-10: "say the building or what is in it, never both in one word"; the stock is the `stores: short` MODIFIER's own fact, which ATTACHES to this very spine — ARCH §8.3's example card); how many months (`foodSecurity.storageMonths`, a separate fact); who holds the key; whether it has been drawn on; a count of buildings; "a bad year" / "a season" (the card's "a season"; R-DA-16); ENGINE CONTRADICTS (E-12): "a granary means the town eats through a bad year" — the economic gate `econHealthMult` to ×0.45 and the disaster gate floor 0.55 can make it false |
| `compound.inst.hasHospital === true` | `hasAny(names, ['hospital', 'monastery', 'healer', 'friary'])`; matching rows `Small hospital` (`:1275`, town, bc 0.3), `Major hospital` (`:1814`), `Hospital network` (metropolis), `Monastery or friary` (`:1267`, bc 0.4, "MAY operate hospital/school" — and the same row sets `hasChurch`, `hasMonastery` and the generator's `hasDivine`), `Healer (divine, 1st level)` (`:845`, a village row, a PERSON; the licence held at `low` for the deity doctrine) | that SOME care-class row is on the roster — a hospital, a monastery that MAY run one, or one first-level divine healer; the flag cannot tell which (label trap L-14) | **BODY** — the referent table §2.1 row "hospital · infirmary": `BODY: flag hasHospital — catches monastery, healer, friary (so "hospital present" can be a friary)`, filed under `DS-DEF-2 Disasters ×5`; HOLDER: none (SOURCE-UNRESOLVED) | the always-safe spelling of the alias bar (A-15; W15; `rulings-DEF2`): **"those tending the sick"** — true of a hospital, a monastery and a healer alike; "a house that takes in the sick" ONLY on a hospital-named row, which this card cannot resolve; the referent table's own safe column reads "the infirmary · somewhere for the sick" (row 25) — a disagreement between the two tables recorded in §9 | "the hospital" from the boolean (A-15: never); "a place for the ill" / "somewhere to put the sick" as a BUILDING (D-13: CONDITIONAL on a hospital-named row; a healer is a person and a monastery MAY; a CONDITIONAL used where its condition does not resolve on the card FAILS — ADDENDUM 13 A item 6); beds, physicians, a count, "that it can contain the outbreak it faces", funding (D-13 NOT ENTAILED); "the two buildings" / "both buildings" (the referent refuter's finding on `:2703`: a BODY COUNT asserted on flag reads that a friary or a healer satisfies — HOLDS); "the plague" (S-8: only "illness", "sickness", "disease" are safe; the two records contradict on the word) |
| `compound.inst.hasChurch` | consulted by the reader only where `hospital` is false | NOTHING on this key: the church argument is not read on the `granary, hospital` branch | — | — | any clergy, parish or church word: a face here says nothing of the parish (a second civic object; and a `Monastery or friary` sets both flags, so "clergy" beside "a hospital" would name ONE row twice — rule 3, W20) |

**What the KEY'S NAME licenses: only its engine meaning (W14).** "Disasters & Famine" is the fifth readiness row's LABEL (`threatAssessment.js:190`); on this pool the key reads the two building flags and NOTHING of the badge: the row's BADGE on the same page is `scores.disaster` = `foodSecurity.resilienceScore × disasterGate` (`defenseGenerator.js:608-618`; `DefenseTab.jsx:185`), a DIFFERENT read this card does not carry — so a town with a granary building and a hospital row can print this face beside a WEAK or CRITICAL badge. Therefore NO face may say the pressure is covered, absorbed, held, or "without catastrophe": the badge beside it may say otherwise, and the card says nothing of the badge. The engine's own chrome for this branch (`threatAssessment.js:181-187`: "Granary provides food buffer. The community can absorb a bad harvest without immediate hardship. Hospital infrastructure enables disease containment and systematic quarantine.") is PRODUCT CODE outside the REWRITE's corpus (ADDENDUM 13 B D, OW-20) and is not a licence for the same claims in a face; it is where the shipped v1 got its capability clause. The DS-DEF-6 Medical Readiness and Logistics & Supply cells (`defenseDisplay.js:235-243`: "Hospital present · Casualty treatment, outbreak containment, recovery capacity." / "Granary present") band the SAME two flags as chrome beside the face; the desk's own docblock (`defenseStateProse.js:1476-1479`) records that this pool "speaks BOTH the reserve and the medical halves" and that DS-DEF-6's medical lens "is the second half of that sentence on its own".

**The pressure the row is keyed against, as a class.** The row's label names two pressures — hunger and sickness — as the arm's engine meaning; a face may name them as the CLASS the two bodies stand against ("against hunger", "against sickness"), in the counterforce's stance especially, but never as an EVENT that is on, was, or will be (W17: a famine never entails a harvest failure or a season; W18: a stress is licensed by its own record, and none is on this card; S-8: "the plague" is refused). "A failed harvest" is licensed ONLY as the granary's entailed purpose ("it buffers a harvest", D-12 HOLDS) — never as an event; "an outbreak" is NOT licensed on the flag read (D-13's "outbreak containment" is CONDITIONAL on a hospital-named row, unresolved here). Recorded as open row 3 in §9.

### 0.3 The block's header lines (annex `RECEIPT_POOLS_DOSSIER_STATE.md`, `### DS-DEF-2`, lines 2568-2596 of the laneRW-DEF2 copy; this pool's rows at 2700-2703)

- **STATE-KEY:** five fixed rows (`Beasts & Monsters` · `Invasion & War` · `Internal Security` · `Economic Survival` · `Disasters & Famine`), each with a `scoreBand` badge, read against `config.monsterThreat`, the institution presence flags, and `compound.inst`. This pool is the `Disasters & Famine` row's spine; its key is TWO of the `compound.inst` presence flags (the badge is a separate read no face may speak, §0.2).
- **SLOTS:** `{settlement}` `{band}` `{route}` — the card: only `{settlement}` is FILLED at this block's call sites; `{band}` RESERVED; `{route}` proper but unfilled here. A face names neither `{band}` nor `{route}`.
- **SECTION-TARGET:** `defense`.
- **PROVENANCE + FENCE:** `buildThreatAssessment` is dossier-native and this shape EXTENDS it into a pool (each branch held ONE string); two defects must not be reintroduced (neither touches this row); **"institution presence is a STANDING fact with no recorded history; the causal clauses here are CAPABILITY clauses (walls without people cannot be held) and never HISTORICAL ones unless the history surface supplies the ancestry."** On this row two BODIES are present, so the header's capability licence is REACHABLE in form — but it is BOUNDED by the ratified entailment law (ADDENDUM 13 A item 3: the engine's own model bounds every entailment): E-12 records that the engine's gates can make "the town eats through a bad year" FALSE, and D-13 records "that it can contain the outbreak it faces" as NOT ENTAILED; so a capability clause joining the two bodies to an OUTCOME ("can take a failed harvest or an outbreak without either becoming a catastrophe") is refused on this row, and the only capability statement a face may carry is the entailed PURPOSE of each body (the granary buffers a harvest; those tending the sick tend the sick) as a MAY.
- **RECEIPT:** `threatAssessment.js:28-195` (the disaster branch `:181-192`); rendered `DefenseTab.jsx:150-183`. **PDF PARITY:** parity.
- **Same page (C7; W2; rule 3 across the tab; A1/A11):** DS-DEF-2 mounts the other four rows on the SAME tab — the Invasion and Beasts rows name the works and the force, the Internal Security row the court and the gaol, the Economic row its band; a face here names none of those. The `stores: short` MODIFIER (`economicState.foodSecurity.label ∈ {Deficit, Deficit × Active Famine}`, relation `addition`, ARCH §8.3) ATTACHES to this spine by derivation ("the cells whose branch tests neither the stock nor its object class") — so a composed unit may read this face and then "the stores are short": a face that asserts the STOCK ("holds food", "a full store", "can feed itself") collides with its own modifier on the same unit (C5/C7 by construction). A face states the BUILDING and hands "the granary" forward; the stock is the modifier's. DS-ECO-2 bands the stock on the economy tab (the `store` class, layer NONE). DS-DEF-6's Medical Readiness and Logistics cells are chrome beside the face (§0.2). The row's own BADGE beside the face is a different read (§0.2): no face grades the pressure.

### 0.4 The register card's six one-line registers (read whole; the dossier binds here)

The dossier: the record itself; the clerk's third person; the six shapes of its closed set; the town's name is not the default opener. The NPC ladder, the Herald, the chronicle, the DM page, chrome and the docent do not reach this pool. Amendment S2 (a computed CONSEQUENCE of the sentence's own fact may ride as one clause — here NO relation row joins the two presence flags to any computed consequence on this spine; the `stores: short` modifier is a separate pool with its own seat; so NO consequence clause is available to a face) and S3 (every fact has a holder; the office cites one only for an S3 reason; here the holder is UNRESOLVED, so no citation at all — A13, W24) bind every face. The three questions before any sentence ships: which field licenses it (two: the granary flag and the care flag); which claims does it carry and are they the same claims as before (two claims: a granary stands, and those tending the sick are present — in the angle's stance); what does it leave standing open (the stock the granary holds and which house the care row is are both unread here — the one lawful open matter is stated as an absence in the RECORD only where a typed field holds it, and none does, so the open matter is left by SILENCE, never by "the record does not say").

### 0.5 The pool's shipped rows, verbatim (the annex, bold line `**\`Disasters & Famine\`: granary AND hospital**`; leaf vids 1-3)

1. `[ledger]` {settlement} holds food against a bad year and has somewhere to put the sick; between them the town can take a failed harvest or an outbreak without either becoming a catastrophe.
2. `[street]` The town has a place for grain and a place for the ill, and knows exactly what having both is worth.
3. `[counterforce]` Neither a failed harvest nor an outbreak turns into a catastrophe at {settlement}, and the reason is in the two buildings rather than in the luck.

Variant count: THREE shipped variants (ledger · street · counterforce), matching the card's `angle: counterforce ledger street`. Slot sets per parent (a face whose `{slot}` set differs from its parent's is refused, ARCH §2.5): vid 1 `{settlement}` · vid 2 `{}` (NO slot — every face of variant 2 carries no `{settlement}`) · vid 3 `{settlement}`. Sentence counts: vid 1 ONE (a semicolon joint); vid 2 ONE (an "and" joint); vid 3 ONE (an "and" joint) — the census reads `grammars 1`: the pool is UNIFORM in sentence count today, which is the spread fault (arm E; R-DA-05: uniform pools 0.576 → ≤ 0.400), not a pin; A1 allows one or two sentences per variant, so a writer MAY split one variant into two sentences (a spread gain), never a third (R-DA-03). Openers: vid 1 opens on `{settlement}` (a T-F8 breach; a cure, not a floor), vid 2 on "The town", vid 3 on "Neither".

### 0.6 Findings of the two ratified tables and the packets that touch this pool's rows

- **REFERENT refuter §I (`defense.referent.refute.md:248`), carried into the table's §3 overlaps:** "the two buildings / both buildings" at `:2703` (this pool's v3) and `:2994` — "BODY count asserted on flag reads that a town hall / a friary satisfy" (`priorityHelpers.js:55, :64`). A breach the rewrite DROPS, never keeps as a floor.
- **REFERENT-TABLE §2.1 rows 24-26:** granary BODY (the building; the stores the stock — never both in one word); hospital BODY (may be a friary; safe: "the infirmary · somewhere for the sick"); church BODY/parish ORGAN (the `church` arg — not read on this key).
- **ENTAILMENT-TABLE D-12 (granary):** ENTAILS grain is stored; it buffers a harvest; communal at town (HOLDS). NOT ENTAILED: how full; how many months; who holds the key; drawn on; a count of buildings. ENGINE CONTRADICTS: "a granary means the town eats through a bad year"; `hasGranary` is a TIER PROXY.
- **ENTAILMENT-TABLE D-13 (hospital):** ENTAILS "somewhere to put the sick; casualty treatment and outbreak containment" — **CONDITIONAL: a `hospital`-named row on the roster**; a monastery "MAY", a healer is a person. NOT ENTAILED: beds; physicians; a count; that it can contain the outbreak; funding. ENGINE CONTRADICTS: "Hospital present means a hospital".
- **ENTAILMENT-TABLE L-14, L-20 (label traps); A-10, A-15 (alias traps):** as folded into §0.2's table.
- **ENTAILMENT survey E-13 (`defense.survey.md:309`):** names this pool's v1 "has somewhere to put the sick" (annex `:2701`) as the exposure of the healer-and-monastery members. (The survey's example — a village whose only medical row is the healer — cannot draw THIS key, because `hasGranary` is false below town; the exposure on this key is the `Monastery or friary` row at town, bc 0.4, and any town roster carrying the healer row. The finding stands; its example is corrected here.)
- **ENTAILMENT refuter §7 (`defense.refute.md:330-340`):** names the SIBLING pool `granary AND parish care only` v1 "clergy who tend the sick" (`:2706`) as more knowledgeable than the simulation; no row of §7 names this pool's lines — this pool is judged from the card, the producer and the laws.
- **`rulings-DEF2.txt`:** "hasGranary is a tier proxy (every town-plus, no village-and-below) and says nothing of the stock; hasHospital fires on a first-level divine healer (a PERSON) and on a monastery that MAY run one — 'a house that takes in the sick' only on a hospital-named row, else 'those tending the sick'"; "the readiness band word is exactly the ladder's reading and a STRONG badge on one arm says nothing of another".

### 0.7 Fences this seat records for the writer and the chair, not decided here

1. **The care body's NOUN.** The card reads a flag that three kinds of row satisfy; the chair's ruling licenses "a house that takes in the sick" only on a hospital-named row, which this card cannot resolve, and "those tending the sick" otherwise. This seat therefore licenses **"those tending the sick"** (the alias bar's always-safe spelling, true of every member) on every face, and REFUSES "a place for the ill", "somewhere to put the sick", "the sick-house", "the infirmary", "the hospital" as a building claim the flag does not hold. The referent table's row 25 lists "somewhere for the sick" as layer-safe; if the chair reads that column as a licence over the entailment table's CONDITIONAL, the shipped "somewhere to put the sick" (v1) and "a place for the ill" (v2) become lawful turns and §6 of variants 1 and 2 gains one each. Recorded as open row 1 in §9.
2. **"holds food" / "a place for grain".** D-12 entails that grain is stored (HOLDS), so "a place for grain" and "holds grain" are licensed as MAY; but the `stores: short` modifier attaches to this spine, so a face that asserts the stock as a QUANTITY or a SUFFICIENCY ("holds food against a bad year", "a full store", "can feed itself") collides with its own unit. This seat licenses the BUILDING word and the bare entailment ("a place for grain"; "keeps a granary") and refuses every sufficiency, level or duration on it. Recorded as open row 2.
3. **The pressure class words.** "Hunger" and "sickness" as the CLASS the two bodies stand against (the row's label at its engine meaning, W14); never "a failed harvest" as an event, never "an outbreak" (CONDITIONAL, unresolved), never "the plague" (S-8), never "catastrophe" (no field). Recorded as open row 3.
4. **The header's capability clause.** Reachable in form on this row (two bodies present), bounded by E-12 and D-13 to the entailed PURPOSE of each body; an OUTCOME after it is a forecast. Recorded as open row 4.

---

## VARIANT 1 · `[ledger]` (vid 1; slot set `{settlement}`; ONE sentence, a semicolon joint, an "and" inside the first half)

**(2) Shipped, verbatim:**
> {settlement} holds food against a bad year and has somewhere to put the sick; between them the town can take a failed harvest or an outbreak without either becoming a catastrophe.

**(3) Every claim, one per line (licence · law broken · REFERENT LAYER beside the READ's layer, which is BODY twice — the granary flag and the care flag, §0.2):**

- `{settlement}` OPENS the sentence — **a pool-level fence, not a claim**: a sentence face may not open on a `proper`-typed slot of the block's bag (ARCH §2.5, T-F8; MOVE-GRAMMAR §1.4 wall 10). A shipped breach; moving the opener off the token is a CURE, not a regression, and no face may carry this opener.
- HOLDS FOOD — **UNLICENSED: an observable the fields do not hold (a stock), and the wrong object entirely.** The read is the flag `hasGranary`, which records that a GRANARY BUILDING is on the roster and says nothing of the stock (W14 the label bar: `hasGranary` is a tier proxy "whatever it stores"; D-12 NOT ENTAILED: how full, how many months, whether drawn on; A-10: say the building or what is in it, never both in one word). "Holds food" states the CONTENTS and never names the body, so the licensed read is not merely damaged, it is ABSENT from this clause. Compounded by the composition: the `stores: short` modifier (`foodSecurity.label ∈ {Deficit, Deficit × Active Famine}`) ATTACHES to this very spine (ARCH §8.3's own example card), so a unit may read "{settlement} holds food …" and then "the stores are short" (C5/C7 by construction). Layer **NONE** (a stock, A.1's own example) against the read's **BODY** — a wrong-layer noun under W20.
- AGAINST A BAD YEAR — **UNLICENSED three times: a season** (the card's may-NOT verbatim; R-DA-16), **a sufficiency the engine contradicts** (E-12 ENGINE CONTRADICTS names almost this sentence: "a granary means the town eats through a bad year" is made FALSE by `econHealthMult` to ×0.45 and the disaster-gate floor 0.55), and **an EVENT in the pressure's place** (W17: a famine never entails a harvest failure or a season). The licensed neighbour is the pressure as a CLASS ("against hunger"), never as a year, a harvest or a season. Layer **NONE** against BODY.
- HAS SOMEWHERE TO PUT THE SICK — **UNLICENSED under this seat's reading: a CONDITIONAL entailment used where its condition does not resolve on the card** (ADDENDUM 13 A item 6, the ratified law; D-13 entails "somewhere to put the sick" only ON A `hospital`-NAMED ROW, and this card resolves a FLAG that `Monastery or friary` — which only MAY run one — and `Healer (divine, 1st level)` — a PERSON — each satisfy; L-14: the flag cannot tell which). Named as the block's exposure by the entailment survey E-13 (`defense.survey.md:309`) at this exact annex line. The alias bar's always-safe spelling is "those tending the sick" (W15; `rulings-DEF2`: "'a house that takes in the sick' only on a hospital-named row, else 'those tending the sick'"). Layer **BODY (asserted as a building)** against a **BODY** read three kinds of row satisfy — the noun's LAYER is right and its ROW is not, which is the alias half of W15 rather than W20. **Open row 1** in §9: the referent table's row 25 lists "somewhere for the sick" in its layer-safe column, and if the chair reads that column as a licence over the entailment table's CONDITIONAL, this clause becomes a lawful turn and §6 below gains one.
- the AND joining the two halves of the first clause — **LAWFUL, and it is the read itself.** The key is the CONJUNCTION of two presence flags (`disasterRowSituation` returns `granary, hospital` only where both are true), so naming both bodies is not "a second fact" in the card's sense: it is the one read stated whole. The card's may-NOT "a second fact" bars a fact BEYOND the two presences, and its "another civic object of the class `care`" bars a SECOND care object beside the first.
- THE SEMICOLON and everything after it — **UNLICENSED as a whole: a capability clause joined to an OUTCOME.** The block's PROVENANCE line does licence capability clauses on presence rows ("walls without people cannot be held"), and two bodies stand here, so the licence is REACHABLE in form; it is BOUNDED by the ratified entailment law's item 3 (the engine's own model bounds every entailment) to the entailed PURPOSE of each body. An outcome after it is refused by E-12 and by D-13's NOT ENTAILED row "that it can contain the outbreak it faces". Layer **NONE** against BODY.
- BETWEEN THEM — **LAWFUL as a joining turn** (it carries no claim of its own; it points at the two bodies TOGETHER, which is exactly the key's conjunction, and hands both forward). Kept in §6.
- THE TOWN CAN TAKE — **UNLICENSED in this use: a capability asserted over the outcome rather than the body's purpose** (see the semicolon row). The verb itself is not the fault; its object is.
- A FAILED HARVEST OR AN OUTBREAK — **UNLICENSED: two EVENTS** (W17: a famine never entails a harvest failure or a season; and an outbreak is NOT licensed on this flag read at all, D-13's outbreak-containment entailment being CONDITIONAL on a hospital-named row and unresolved here; W18: a stress is licensed by its own record and NO stress record is on this card). Layer **NONE** against BODY.
- WITHOUT EITHER BECOMING A CATASTROPHE — **UNLICENSED: a forecast, and a totality over outcomes** (the card's may-NOT "a future"; the register card: nothing takes the future, an edge is subjunctive and no edge field is here; state never fate). "Catastrophe" is held by no field. And the same page contradicts it: the row's BADGE (`scores.disaster` = `foodSecurity.resilienceScore × disasterGate`) is a DIFFERENT read this card does not carry (§0.2), so this face can print beside a WEAK or CRITICAL badge asserting the opposite. Layer **NONE** against BODY.
- PROVENANCE of the whole second half — it is the engine's own chrome rewritten (`threatAssessment.js:181-187`: "Granary provides food buffer. The community can absorb a bad harvest without immediate hardship. Hospital infrastructure enables disease containment …"). Product code is outside the REWRITE's corpus (OW-20) and is NEVER a licence for the same claims in a face.
- the sentence's SHAPE — one sentence, a semicolon joint, an internal "and": punctuation the writer may spend freely (the register card). What this one joins is unlicensed from the semicolon onward, and its first half names the stock instead of the body.
- **CITATION COUNT: zero, and lawful** — no holder is named. The card is SOURCE-UNRESOLVED and A13 refuses any naming face here (W24: on a read that resolves no holder, NO record word at all).

**(4) THE READS THE REWRITE MUST STATE (every read the card names, plus every LICENSED claim above):**
- **Read A — `hasGranary === true`:** a GRANARY stands on this town's record (the `storehouse`-class body, the building at its own name). May be stated at its entailed purpose as a MAY, never as a floor: that it is a place for grain, that grain is stored, that it buffers a harvest, that it is communal at town (D-12 HOLDS). Layer BODY.
- **Read B — `hasHospital === true`:** a CARE-class row is present on this town's record — stated in the always-safe spelling **"those tending the sick"** (true of a hospital, a monastery and a healer alike), never as a named building. Layer BODY.
- **The conjunction itself:** BOTH stand, together, as ONE standing fact of the record — the row `granary, hospital` of `DISASTER_ROW_POOL`. A face that states one body and not the other has dropped half the read.
- **The pressure the row is keyed against, as a CLASS** (optional, licensed by W14 at the label's engine meaning): hunger and sickness as what the two bodies stand against — never an event, never a season, never an outcome.
- the shipped licensed material carried forward: the joining turn "between them"; the two-body frame. **Nothing of "holds food", "against a bad year", "the town can take a failed harvest or an outbreak", or "without either becoming a catastrophe" survives**; "has somewhere to put the sick" survives only if the chair rules open row 1 for the referent table's column.
- `{settlement}` in every face (the parent's slot set), **never as the opener**.
- SILENCE on: the stock and its level, months, the key, whether it has been drawn on, a count of buildings, beds, physicians, funding, which house the care row is, the parish or any clergy (the `church` argument is NOT READ on this branch), the badge, the other four readiness rows, any event, any season, any outcome.

**(5) THE ANGLE'S STANCE — `[ledger]` on this pool (one sentence):** the ledger is the clerk's own way of setting the fact down (W24: a STANDPOINT that licenses NO record noun and NO citation; R-vi: the office's own formula — "entered", "carried on the record", "set down as standing" — is the ledger's lawful surface, while "the books show" is a citation the budget does not license), so this face enters the two presences as the compiled record holds them: a granary standing and those tending the sick, both carried as standing facts with no history behind them (the PROVENANCE line: institution presence is a STANDING fact with no recorded history), landing on one of the two bodies as the civic thing the fact names; it may NOT invent a stock, a level, a count, a date, a cause, a season, an event, an outcome, a badge reading, a holder or a record word, and it may not explain what having both MEANS.

**(6) THE TURNS WORTH KEEPING (lawful under the card, verbatim; the density floor):**
- `between them` — the joining turn that points at the two bodies together and carries no claim; the pool's one genuinely reusable shipped asset, lawful before any licensed continuation.
- **Not kept:** "holds food"; "against a bad year"; "the town can take a failed harvest or an outbreak"; "without either becoming a catastrophe"; the `{settlement}` opener.
- **Conditionally kept (open row 1):** `has somewhere to put the sick` — lawful verbatim ONLY if the chair rules the referent table's layer-safe column over the entailment table's CONDITIONAL; under this seat's reading it is refused and "those tending the sick" is the spelling.
- The density floor for this variant is therefore TWO licensed bodies stated in the office's formula with a lawful joint between them — the honest limit (ADDENDUM 7): the shipped line's whole second half was the chrome's invention and its first half named the stock the composition's own modifier owns, so a faithful rewrite is plainer exactly there, and its richness comes from the two bodies named at their own layer, the ledger's formula, the pressure class where the face wants it, and the varied landing noun.

**(7) WHAT WOULD MAKE THE REWRITE A REGRESSION HERE:**
- **an inventory line:** a face that lists the two bodies and stops ("{settlement} keeps a granary and has those tending the sick") with no stance, no formula, no landing — ADDENDUM 7 rule 3; the read being a conjunction is not a licence to write a list;
- **a lost licensed read:** a face naming ONE body only (half the key), or naming the granary by the stock again, or replacing the care body with a parish, clergy or a church word (the `church` argument is not read here, and a `Monastery or friary` row sets BOTH flags, so clergy beside the care body would name ONE row twice — rule 3, W20);
- **a lost lawful turn:** "between them" dropped from every face with nothing of its work replacing it (the two bodies never stated as standing TOGETHER, which is the key);
- **a dropped angle:** a face that reads as the town's talk, a stranger's eye or a counterforce under a `[ledger]` tag; a face that cites a holder or reaches for a record word (A13, W24); a face built on "the books", "the rolls" or "according to";
- **an added unlicensed claim:** any of "food", "grain stores", "the stores", "full", "well stocked", "months", "enough"; "the hospital", "a place for the ill", "beds", "physicians", "the infirmary"; "two buildings", "both buildings", any count; "a bad year", "a season", "a failed harvest", "an outbreak", "the plague", "a catastrophe"; "can take", "will hold", "without", "safe", "covered", "absorbs"; "because", "the reason", "thanks to"; the badge word; a which-clause, an em dash, a digit, an exclamation; the settlement token as opener.

---

## VARIANT 2 · `[street]` (vid 2; slot set `{}` — NO `{settlement}` in this variant or any of its faces; ONE sentence, an internal "and" and a comma-joined tail)

**(2) Shipped, verbatim:**
> The town has a place for grain and a place for the ill, and knows exactly what having both is worth.

**(3) Every claim, one per line (licence · law broken · REFERENT LAYER beside the READ's layer, which is BODY twice):**

- THE TOWN as subject ("The town has …") — **lawful as a frame**: the town is the possessor of the two roster rows, so "the town has" states the presences in the street's naming form (W5; the sibling packets' ruling on "The town" as the street's lawful subject). It is also the lawful opener this variant already has (no slot to breach T-F8 with — the parent carries no `{settlement}`). What the town may not do is know, believe, decide, value, judge or feel.
- HAS A PLACE FOR GRAIN — **LICENSED**: the granary flag at the body's entailed purpose (D-12 ENTAILS that grain is stored — HOLDS; §0.7 fence 2 licenses the BUILDING word and the bare entailment). It names the BODY and not the stock, so it does not collide with the `stores: short` modifier that attaches to this spine, and it does not assert a level, a quantity or a duration. Layer **BODY** = the read's layer. **The pool's one fully lawful shipped clause.**
- AND A PLACE FOR THE ILL — **UNLICENSED under this seat's reading: a BUILDING claim on a flag that a person and a "may" satisfy** (D-13 CONDITIONAL on a `hospital`-named row, unresolved on this card; `hasHospital` fires on `Monastery or friary` — bc 0.4, which only MAY operate a hospital — and on `Healer (divine, 1st level)`, a PERSON; L-14: the flag cannot tell which; W15 the alias bar: the always-safe spelling is "those tending the sick"). Note the asymmetry the shipped line builds on and this seat refuses: "a place for grain" is lawful because D-12 HOLDS, while the parallel "a place for the ill" is not, because D-13 is CONDITIONAL — the parallelism is the trap. Layer **BODY (asserted as a building)** against a **BODY** read three kinds of row satisfy. **Open row 1** in §9 (the referent table's row 25 column would make it lawful).
- the AND joining the two places — **LAWFUL, and it is the read itself** (the key is the conjunction; see variant 1's row). The parallel construction "a place for X and a place for Y" is the street's plain form of the conjunction and is worth keeping as a SHAPE once its second half carries a lawful noun.
- the COMMA-JOINED TAIL ", and knows …" — **UNLICENSED as a joint: a second fact carried as a tail.** The register card is explicit that a qualification is a sentence and never a tail, and a second fact takes its own sentence (R-DA-03); amendment S2's clause seat is NOT available here, because S2 licenses only a CONSEQUENCE THE ENGINE COMPUTED of the sentence's own fact, and this spine carries NO relation row joining the two presence flags to any computed consequence (§0.1: "a spine takes no relation"; §0.4).
- KNOWS — **UNLICENSED: a belief and knowledge frame assigned to the town** (R-DA-13: never on a belief; R-DA-14: seen, not meant, no interior; the FEELING non-move, MOVE-GRAMMAR §1.3; W23 THE FUSED-AGENT BAR: "the town has decided" is its named shape). Layer **NONE** against BODY.
- EXACTLY — **UNLICENSED: a precision claim on an unheld quantity** (it modifies a valuation no field computes; the card's may-NOT "a count" by the back door). Layer NONE.
- WHAT HAVING BOTH IS WORTH — **UNLICENSED: a VALUATION no field holds** (no field computes what the two presences are worth; the register card's bar on explaining what a fact MEANS, and on closing on a moral — the kicker, an effect bought with a fact); and **a standpoint** (the card's may-NOT verbatim). Layer **NONE** against BODY. "Having both" also inherits the building claim of the clause before it.
- BOTH — **borderline, recorded:** "both" refers back to the two named places rather than asserting a body count in the open, so it is NOT the `:2703` breach (that is v3's "the two buildings"); but where its antecedent is a building noun the flag does not license, it carries that fault forward. On a face whose two nouns are lawful ("a place for grain" and "those tending the sick"), a bare "both" is lawful.
- **NO SLOT** — the parent's slot set is empty; a face of this variant carrying `{settlement}` is refused outright (ARCH §2.5: a face whose slot set differs from its parent's is refused). The town is named "the town", never by its name.
- **CITATION COUNT: zero, and lawful** (A13, W24; SOURCE-UNRESOLVED).

**(4) THE READS THE REWRITE MUST STATE (every read the card names, plus every LICENSED claim above):**
- **Read A — `hasGranary === true`:** a granary stands, in the street's plain idiom — "a place for grain" is the licensed spelling and is already here. Layer BODY.
- **Read B — `hasHospital === true`:** a care-class row is present — "those tending the sick" in the street's idiom; never a building noun. Layer BODY.
- **The conjunction:** both, together, as the one standing fact; the street's parallel form ("a place for grain and those tending the sick") states it whole.
- **The pressure class** (optional, W14): hunger and sickness as what the two stand against, in plain words — never a year, a harvest, an outbreak, a plague.
- the shipped licensed material carried forward: **"The town has a place for grain"** verbatim; the parallel "a place for X and …" shape; "both" once its antecedents are lawful. **Nothing of "a place for the ill" (pending open row 1), "knows", "exactly", or "what having both is worth" survives.**
- **No `{settlement}` in any face.** ONE sentence per face unless the sitting moves this variant's grammar (§8's spread row).
- SILENCE on: the stock, a level, months, a count, the badge, the parish, any event, any outcome, anything the town knows, thinks, expects or has been told.

**(5) THE ANGLE'S STANCE — `[street]` on this pool (one sentence):** the street is the town's own talk about its condition, so this face states the two presences in the town's plain idiom — the short clause, the ordinary noun where the ledger would use the office's formula, the town as the subject that HAS the two things — and it may leave one matter standing open (stated, never asked); but NO field here holds anything the town does, says, believes or values about either body, so the street may NOT report what the town knows, is glad of, counts on, has been told or thinks it is worth (W22, W23, R-DA-13, R-DA-14), may NOT personify or total the town ("everybody", "nobody", "no one here"), may NOT name a stock, a count, an event, a season or an outcome, and may NOT cite a record (the holder is unresolved).

**(6) THE TURNS WORTH KEEPING (lawful under the card, verbatim; the density floor):**
- `The town has a place for grain` — lawful whole, subject and body and entailed purpose together; the pool's strongest shipped asset and the street's natural opener.
- `a place for grain` — the licensed body spelling on its own, usable in any face of any variant.
- the parallel SHAPE `a place for … and …` — lawful as a shape once its second half carries "those tending the sick" or another licensed spelling.
- **Not kept:** "a place for the ill" (pending open row 1); ", and knows exactly what having both is worth" entire.
- The density floor for this variant is therefore the two bodies in the street's parallel idiom, one of them already written and lawful — a higher floor than variants 1 and 3 give.

**(7) WHAT WOULD MAKE THE REWRITE A REGRESSION HERE:**
- **an inventory line:** a face that names the two places and stops with no street idiom and no landing (ADDENDUM 7 rule 3);
- **a lost licensed read:** dropping "a place for grain" (or its equal) from every face, when it is the one shipped clause the card licenses whole; naming one body only; slipping the granary back into the stock ("the town has food", "the town can eat");
- **a lost lawful turn:** the parallel two-place shape abandoned in all four faces, so the conjunction is never heard as a pair;
- **a dropped angle:** a face in the ledger's formula or the counterforce's stance under a `[street]` tag; a face that recovers the town's interior by a softer route ("the town is glad of", "nobody here worries", "they are used to having both") — these are the SAME breach as "knows", not a cure for it;
- **a slot breach:** any face carrying `{settlement}`, which this parent does not have;
- **an added unlicensed claim:** "the hospital", "the infirmary", "a place for the ill", "somewhere to put the sick" (pending open row 1), "beds", "a healer" as a named person; "food", "the stores", "full", "enough", "months"; "two", "both buildings"; "a bad year", "a harvest", "an outbreak", "the plague"; "worth", "lucky", "better off", "knows", "thinks", "expects"; a which-clause, an em dash, a digit, an exclamation.

---

## VARIANT 3 · `[counterforce]` (vid 3; slot set `{settlement}`, carried inside the sentence and not as the opener; ONE sentence, an "and" joint whose second clause explains the first)

**(2) Shipped, verbatim:**
> Neither a failed harvest nor an outbreak turns into a catastrophe at {settlement}, and the reason is in the two buildings rather than in the luck.

**(3) Every claim, one per line (licence · law broken · REFERENT LAYER beside the READ's layer, which is BODY twice):**

- NEITHER … NOR as the OPENER — **UNLICENSED as a shape here: a negated surface opening a PRESENT read** (R-ii: a negated surface on a present read is not an absence opener, at most one per variant, W10). The card's read is two things PRESENT; this face opens on two things NOT happening, which is not the read at all. It is also an absence the RECORD does not hold: the register card licenses an absence only where the record holds a TYPED gap, and no typed field here holds a non-event.
- A FAILED HARVEST — **UNLICENSED: an EVENT** (W17: a famine never entails a harvest failure or a season; W18: a stress is licensed by its own record and no stress record is on this card; the card's may-NOT "a season" and "a second fact"). D-12 licenses "it buffers a harvest" as the granary's entailed PURPOSE; it does not licence a harvest that failed. Layer **NONE** against BODY.
- NOR AN OUTBREAK — **UNLICENSED: an EVENT, and one this flag cannot reach at all** (D-13's "outbreak containment" is CONDITIONAL on a hospital-named row and unresolved here; W17: a disease outbreak is "illness" or "sickness" and never "the plague", and the two records contradict on the word; S-8). Layer **NONE** against BODY.
- TURNS INTO A CATASTROPHE / …DOES NOT — **UNLICENSED: a forecast stated as a standing general truth, and a totality over outcomes** (the card's may-NOT "a future"; the register card: nothing takes the future, an edge is subjunctive, and no edge field is here; state never fate). "Catastrophe" is held by no field. E-12 ENGINE CONTRADICTS the claim's granary half directly, D-13's NOT ENTAILED row ("that it can contain the outbreak it faces") contradicts its care half, and the row's own BADGE beside the face on the same page (`scores.disaster`, a read this card does not carry — §0.2) may print WEAK or CRITICAL under exactly this sentence. Layer **NONE** against BODY.
- AT `{settlement}` — **lawful placement, and worth keeping as a placement**: the slot rides INSIDE the sentence rather than opening it, which is what T-F8 requires and what vid 1 breaches. The slot set matches the parent. No claim.
- AND THE REASON IS — **UNLICENSED: a cause** (the card's may-NOT "a cause", verbatim; MOVE-GRAMMAR §1.3's MEANING non-move: the record never explains what a fact means; the register card's bar on the kicker, an effect bought with a fact). Layer **NONE** against BODY.
- IN THE TWO BUILDINGS — **UNLICENSED twice, and this is the block's NAMED SHIPPED BREACH: a COUNT** (the card's may-NOT "a count") **and a BODY COUNT asserted on flag reads** — the referent refuter's finding §I at this exact annex line `:2703` (carried into the ratified table's §3 overlaps): "BODY count asserted on flag reads that a town hall / a friary satisfy" (`priorityHelpers.js:55, :64`). `hasHospital` is TRUE on a `Monastery or friary` and on a `Healer (divine, 1st level)`, a PERSON, so "the two buildings" can be false on the very towns that draw this row. Layer **BODY (counted)** against BODY — the wrong cardinality on a right-layer noun, ratified as a breach the rewrite DROPS and never keeps as a floor.
- RATHER THAN IN THE LUCK — **UNLICENSED: a counterfactual cause and a STANDPOINT** (the card's may-NOT "a standpoint"; the register card's bar on closing on a moral or a maxim — this is the kicker in its purest form, the sentence buying its effect by denying an alternative explanation no field holds). "Luck" is held by no field and is not a thing the record can rule out. Layer **NONE** against BODY.
- the AND JOINT — **UNLICENSED as a joint**: the second clause EXPLAINS the first (the MEANING non-move; R-DA-03: a second fact takes its own sentence; the register card: a qualification is a sentence, never a tail). S2's clause seat is not available (no relation row on a spine; §0.4).
- **THE READ ITSELF IS NEVER STATED.** Neither body is named: the granary is never named, and the care row is named only as "buildings" inside an unlicensed count. This variant states ZERO of the card's two reads in licensed form — the strictest regression risk in the pool is therefore that a rewrite copies its stance and keeps its silence about the bodies.
- **CITATION COUNT: zero, and lawful** (A13, W24; SOURCE-UNRESOLVED). "The luck" is not a holder and does not breach A13; it breaches the standpoint bar.

**(4) THE READS THE REWRITE MUST STATE (every read the card names, plus every LICENSED claim above):**
- **Read A — `hasGranary === true`:** a granary stands, named as the body, in the counterforce's stance — what the town has standing against hunger. Layer BODY.
- **Read B — `hasHospital === true`:** a care-class row is present — "those tending the sick", what the town has standing against sickness. Layer BODY.
- **The conjunction:** both, together; the counterforce's stance is precisely the stance in which the pair reads as one answer, so a face here that names one body has lost more than half the read.
- **The pressure class, named as a class** (W14, and the stance's own material): hunger and sickness as the two pressures the row is keyed against — the ONE place in this pool where naming the pressure is most natural, and still never as an event, a season, an outcome or a grade.
- the shipped licensed material carried forward: the SLOT PLACEMENT (`at {settlement}` inside the sentence) and the counterforce STANCE. **No clause of this variant survives verbatim.**
- `{settlement}` in every face, never as the opener.
- SILENCE on: any count ("two", "both buildings", "a pair"), any cause ("the reason", "because", "thanks to"), luck, fortune, chance; any event; any outcome or grade; the badge; the stock; the parish.

**(5) THE ANGLE'S STANCE — `[counterforce]` on this pool (one sentence):** the counterforce is a STANCE within the dossier (W27, never a body and never a voice), and on this pool it is the record stated with the PRESSURE named as the class the row is keyed against and the two bodies named as what the town has standing against it — hunger and the granary, sickness and those tending the sick, set against each other as the record holds them; it may NOT say the pressure is met, held, covered, absorbed or survived (E-12, D-13, and the badge on the same page), may NOT name the pressure as an event that is on, was or will be, may NOT name a cause or rule one out ("the reason", "not luck"), may NOT count the bodies, and may NOT grade the arm.

**(6) THE TURNS WORTH KEEPING (lawful under the card, verbatim; the density floor):**
- `at {settlement}` — the slot carried inside the sentence rather than opening it; a lawful placement to keep, and the model for curing vid 1's opener.
- **Not kept — nothing else.** "Neither a failed harvest nor an outbreak", "turns into a catastrophe", "and the reason is", "in the two buildings", "rather than in the luck" are each refused, the count being the ratified breach.
- The density floor for this variant is therefore the STANCE alone — the pressure class named and the two bodies set against it — with no shipped clause beneath it. The honest limit (ADDENDUM 7) is at its sharpest here: this variant's whole apparent richness (the two named disasters, the catastrophe averted, the reason located, luck ruled out) is invention, and a faithful rewrite is plainer in every one of those places while being the only one of the three that finally NAMES both bodies.

**(7) WHAT WOULD MAKE THE REWRITE A REGRESSION HERE:**
- **the breach re-kept:** any face saying "the two buildings", "both buildings", "the pair of them", "two things", or any other count of the bodies — the ratified `:2703` breach, dropped and never a floor;
- **an inventory line:** a face naming the two bodies with no stance at all, which is the ledger's face under a counterforce tag;
- **a lost licensed read:** a face that keeps this variant's habit of never naming the bodies (gesturing at "what the town has" or "what it has built" while naming neither), or names one only;
- **a lost lawful turn:** the slot moved back to the opener, breaching T-F8 the way vid 1 does;
- **a dropped angle:** a counterforce face that states the presences flatly with no pressure named (the ledger's job) or that reports the town's talk (the street's);
- **an added unlicensed claim:** "the reason", "because", "thanks to", "on account of"; "luck", "chance", "fortune", "not by accident"; "catastrophe", "disaster" as an event, "a failed harvest", "a bad year", "an outbreak", "the plague", "famine" as an event; "holds", "meets", "covers", "absorbs", "survives", "sees it through", "is ready for"; "never", "always", "nothing here ever"; "two", "both", any count; the badge word; a which-clause, an em dash, a digit, an exclamation; the settlement token as opener.

---

## 8. POOL-LEVEL FENCES THE FACES MUST OBEY TOGETHER (the marker's inventory of the walls)

- **THE READ IS A CONJUNCTION, AND EVERY FACE STATES BOTH HALVES.** `disasterRowSituation` returns `granary, hospital` only where BOTH flags are true; a face naming one body is a half-read, not a terse face. This is the pool's first wall and the one the shipped rows breach most (vid 3 names neither body in licensed form).
- **THE TWO BODY SPELLINGS, fixed for the whole pool:** the granary as the BUILDING or at its bare entailed purpose — "the granary", "a place for grain", "keeps a granary", "grain is stored" (D-12 HOLDS) — and the care row as **"those tending the sick"** (W15's always-safe spelling, true of a hospital, a monastery and a healer alike). Under this seat's reading NO face writes "the hospital", "the infirmary", "a place for the ill", "somewhere to put the sick", "a sick-house", "a healer" or "a physician" (D-13 CONDITIONAL, unresolved on the card; L-14; the chair may move this at open row 1).
- **THE STOCK IS NEVER SPOKEN (A-10; D-12's NOT ENTAILED; the composition).** No "food", "the stores", "full", "well stocked", "enough", "months", "holds", "can eat", "can feed itself", "reserves". The stock is a DIFFERENT civic object with its own pool (DS-ECO-2, layer NONE), and the `stores: short` MODIFIER attaches to this very spine (ARCH §8.3's example card) — a face asserting the stock collides with its own composed unit. Say the building or what is in it, never both in one word; here, always the building.
- **NO COUNT OF BODIES (the card's may-NOT; the ratified `:2703` breach):** never "two", "both buildings", "the pair", "a count of". "Both" is lawful only as a bare back-reference to two lawful nouns already named.
- **NO EVENT, NO SEASON, NO DURATION (W17, W18, the card's "a season", R-DA-16):** never a failed harvest, a bad year, a bad season, a harvest, a famine as an event, an outbreak, a sickness that is happening, the plague, a winter, "when it comes", "so far", "yet", "this year". The pressures are a CLASS — hunger, sickness — and nothing more.
- **NO OUTCOME, NO GRADE, NO FORECAST (E-12, D-13, the card's "a future", the badge):** never "takes it", "comes through", "holds", "covers", "absorbs", "without catastrophe", "is ready", "safe", "provided for", "well placed", "strong", "adequate", "weak". The row's own BADGE is a separate read (`scores.disaster`) that can say the opposite on the same page directly beside the face.
- **NO CAUSE AND NO COUNTERFACTUAL (the card's may-NOT "a cause"; MOVE-GRAMMAR §1.3):** never "because", "the reason", "thanks to", "rather than luck", "not by accident", "which is why".
- **NO STANDPOINT, NO VALUATION, NO BELIEF (the card's may-NOT "a standpoint"; R-DA-13; R-DA-14; W22; W23):** never "worth", "knows", "thinks", "expects", "is glad of", "counts on", "has not forgotten", "the people who"; no person as a referent at all, which also keeps the `Healer` row's PERSON off the page.
- **NO SECOND CIVIC OBJECT (the card's may-NOT verbatim: "another civic object of the class `care`"; the census `objectClasses ["care","storehouse"]`; the projector's class guard T-F12):** ONE care object and ONE storehouse object, and nothing else civic — no parish, no church, no clergy (the `church` argument is NOT READ on this branch, and a `Monastery or friary` row sets both flags, so clergy beside the care body names ONE row twice — rule 3, W20), no hall, no market, no walls, no muster, no watch, no court, no gaol. The other four readiness rows sit on the SAME tab and a face here names none of them (C7, W2, A1/A11).
- **NO CITATION AND NO RECORD WORD (A13; W24; SOURCE-UNRESOLVED):** the recommended citation count for this pool is **ZERO**; the `[ledger]` angle is a STANDPOINT licensing no record noun, so "the books", "the rolls", "the register", "according to", "the town's own count" are each a finding. The office's own formula ("entered", "carried on the record", "set down as standing") is the ledger's lawful surface (R-vi).
- **THE SLOT SETS ARE THE PARENTS' EXACTLY:** vid 1 `{settlement}` · vid 2 `{}` · vid 3 `{settlement}` (ARCH §2.5: a face whose slot set differs from its parent's is refused). `{band}` is RESERVED and `{route}` unfilled here: no face names either, and no face speaks the badge as a word.
- **THE SETTLEMENT TOKEN NEVER OPENS A SENTENCE FACE** (ARCH §2.5, T-F8; MOVE-GRAMMAR §1.4 wall 10). Vid 1 opens on it today — a shipped breach and a cure. Vid 3's `at {settlement}` inside the sentence is the model.
- **THE SPREAD (census `grammars 1`; arm E; R-DA-05):** all three shipped variants are ONE sentence, which is the uniformity fault the measure names, not a pin. A1 allows one or two sentences per variant, never three (R-DA-03). **This seat's recommendation, vetoable (open row 6):** the `[ledger]` variant takes TWO sentences — the two presences set down, then the matter standing open — and `[street]` and `[counterforce]` stay at one; where a variant runs two, its second sentence carries a noun forward from its first regardless of k (R-i) and is never a summary, a maxim or a second fact.
- **THE THREAD (MOVE-GRAMMAR §1.4.1; the owner, 2026-09-08):** this pool is the SPINE and opens the passage; the census reads `k 2`, so up to two modifiers follow, ordered by salience, and the writer does not choose the order. Every face must hand a NOUN forward that a modifier can carry — the granary, those tending the sick, the pressure class — and must read well both alone and immediately before an unknown sibling. The attaching `stores: short` modifier is the known neighbour: a face that hands "the granary" forward reads cleanly before "the stores are short", and a face that hands the STOCK forward does not.
- **THE FOUR FACES DIFFER IN CONSTRUCTION, NEVER IN VOCABULARY ALONE** (the owner's four-faces rule; ADDENDUM 7 rule 4; W4): the subject (the town · the granary · those tending the sick · the pressure · the office's formula), the ORDER of the two bodies (grain first or care first — the pool's freest axis, and the only one that is a genuine rhythm change rather than a synonym), the joint (a plain "and", a parallel pair, a colon, a second sentence), and the landing noun. Each face states the conjunction WHOLE; an unweighted seeded roll picks it, so none may lean on a sibling.
- **NEVER TRIM:** four faces per variant, twelve for the pool; a variant short of four is refused, and a face that is a paraphrase of its sibling is refused with it.
- **Hard walls, every face:** no em dash, no exclamation, no digit, no which-clause, no question, no "I" or "you", no figure, no sense verb on an abstraction, no intent or feeling for the town or for a building, no forecast, no totality over persons, no maxim, no hook, no opener on the settlement token, no three-of-anything by habit.
- **The label walls (W14):** "Disasters & Famine" is the ROW'S LABEL and this pool reads only the two building flags beneath it — never the badge, never a live famine, never "disaster" as an event. `hasGranary` is a TIER PROXY (every town-plus, no village-and-below) saying nothing of the stock; `hasHospital` fires on a friary and on one first-level divine healer.
- **The bars that are reachable here and must be found by a drafter reaching to "enrich":** W15 (the alias bar, on the care body — the pool's live one), W20 (a body word at the wrong layer, and the stock as a body's content), W22 (the healer as a person), W24 (a record word where no holder resolves), W17 and W18 (the events), W14 (the labels). W11, W12, W13 and W16 are unreachable by a lawful face here because no wall, garrison, watch or gate read is on this card — listed so a drafter finds the bar rather than the gap.
- **The echo:** every pool selecting a row of `DISASTER_ROW_POOL` shares ONE echo key (the card's own note), so a mount counted against this pool may be a sibling ROW of the same table; the faces neither rely on nor fear it.
- **The density floor (ADDENDUM 6, §21.4) for the pool:** the lawful material is TWO bodies, their entailed purposes as MAYs, the pressure class, the office's formula, the order of the pair, and the matter left standing open. That is the whole purse, and it is spent in full in every face — a face stating one body, or both bodies with no stance, is an inventory line and is refused (ADDENDUM 7 rule 3). The honest limit: of the three shipped sentences, ONE clause is lawful whole ("The town has a place for grain"), one turn is lawful ("between them"), one placement is lawful (`at {settlement}` inside the sentence), and everything else is the chrome's capability claim, an event, a count, a cause, a valuation or an outcome. The rewrite is plainer in exactly those places by design and is more knowledgeable in none.

## 9. THE MARKER'S OPEN ROWS FOR THE CHAIR (recorded, not decided; the drafter writes under this seat's reading until ruled)

1. **THE CARE BODY'S NOUN (§0.7 fence 1) — the pool's one live disagreement between the two ratified tables.** The ENTAILMENT table's D-13 makes "somewhere to put the sick" CONDITIONAL on a `hospital`-named row, which this card cannot resolve; the REFERENT table's §2.1 row 25 lists "the infirmary · somewhere for the sick" in its layer-safe column. This seat rules the CONDITIONAL binding (ADDENDUM 13 A item 6 is explicit that a CONDITIONAL used without its condition resolved FAILS) and licenses **"those tending the sick"** only. **If the chair rules the referent table's column a licence:** vid 1's "has somewhere to put the sick" and vid 2's "a place for the ill" become lawful turns, §6 of variants 1 and 2 each gains one, and the faces may carry a building noun for the care row. Nothing else in this packet moves.
2. **THE GRANARY'S PURPOSE CLAUSE (§0.7 fence 2).** D-12 entails that grain is stored and that the store buffers a harvest (HOLDS), so "a place for grain" is licensed and kept. This seat refuses every sufficiency, level and duration on it, and also refuses "it buffers a harvest" in any wording that names the harvest as an EVENT. If the chair reads the buffer entailment as speakable ("kept against hunger"), the pressure-class clause gains a second lawful spelling; the stock stays refused either way, because the attaching modifier owns it.
3. **THE PRESSURE CLASS WORDS (§0.7 fence 3).** This seat licenses "hunger" and "sickness" as the CLASS the two bodies stand against, at the row label's engine meaning (W14), and refuses "famine", "a failed harvest", "an outbreak", "the plague", "disaster" and "catastrophe". If the chair reads even the class words as importing a live event, the counterforce variant loses its stance material entirely and falls back to the pair stated with no pressure named — recorded because that would gut variant 3, whose density floor is the stance alone.
4. **THE HEADER'S CAPABILITY LICENCE (§0.7 fence 4).** The block's PROVENANCE line licenses capability clauses on presence rows, and two bodies stand here, so it is reachable in form; this seat bounds it by ADDENDUM 13 A item 3 to the entailed PURPOSE of each body and refuses every OUTCOME after it (E-12 contradicts the granary half; D-13's NOT ENTAILED row contradicts the care half; the badge on the same page may say the opposite). If the chair reads the header's licence as reaching an outcome, the shipped capability clauses on vids 1 and 3 come back as turns — this seat recommends against it on the badge alone.
5. **THE BADGE BESIDE THE FACE.** `scores.disaster` (`foodSecurity.resilienceScore × disasterGate`) prints as the row's band directly beside every face of this pool and is a read this card does not carry, so a lawful face can sit under a CRITICAL badge. Recorded as a COMPOSITION fact for the chair and a wiring question for the register car: whether a presence spine should ever be lit without its own row's band on the card. Not a writer's task, and no face grades the pressure.
6. **THE SPREAD PIN (§8's spread row).** The census reads `grammars 1` — the pool is uniform in sentence count, the fault arm E measures. This seat recommends the `[ledger]` variant run TWO sentences and the other two stay at ONE; the pin is the sitting's to set, not the marker's, and A1 permits either.
7. **THE SAME-PAGE CHROME SAYS WHAT THE FACE MAY NOT.** Every town drawing this row prints `buildThreatAssessment`'s own string beside the face ("Granary provides food buffer. The community can absorb a bad harvest without immediate hardship. Hospital infrastructure enables disease containment and systematic quarantine.") and DS-DEF-6's Medical Readiness and Logistics cells band the same two flags again. The engine's strings are product code outside the REWRITE's corpus (OW-20, recorded for the owner as a scope question, not acted); the face follows the card and will therefore read plainer than the chrome two inches away. Recorded so the plainness is not mistaken for a regression at review.
8. **THE DESK'S OWN DOCBLOCK** (`defenseStateProse.js:1476-1479`) records that this pool "speaks BOTH the reserve and the medical halves" and that DS-DEF-6's medical lens "is the second half of that sentence on its own". Recorded so no drafter imports DS-DEF-6's cell material here and no reviewer reads the overlap as this pool's fault.
9. **THE HOLDER IS UNRESOLVED AND LIKELY TO STAY SO** (`holderReason`: "no mapping row resolves any field this pool reads"). A granary holder row was drafted and WITHDRAWN (referent table §2.1). Citation count ZERO stands for this pool until a register car mints one (OW-23's shape); recorded so a later card printing a holder is read as a change, not as a licence this packet missed.
10. **E-13's EXAMPLE CORRECTED, ITS FINDING KEPT.** The entailment survey names this pool's v1 as the healer-and-monastery exposure but illustrates it with a village; `hasGranary` is false below town, so the exposure on THIS key is the `Monastery or friary` row at town (bc 0.4) and any town roster carrying the `Healer (divine, 1st level)` row. The finding stands and is the ground of open row 1; the correction is recorded so the chair is not reading a refuted example as a refuted finding.
