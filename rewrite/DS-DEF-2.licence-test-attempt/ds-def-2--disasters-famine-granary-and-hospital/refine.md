# REFINE — DS-DEF-2 · `Disasters & Famine: granary AND hospital`

Seat: Opus 5 (Fable-unvalidated) — REFINER for the REWRITE, a different author than the drafter, working for the Fable chair.
Status: COMPLETE — twelve faces, one for one against `draft-round-2.md`; three variants, their vids, their order and their ONE angle tag unchanged; no variant or face added, removed, merged or trimmed. EIGHT faces moved, FOUR held at their ceiling with the reason named (§H). Every face carries the identical claim set {granary BODY · the granary's entailed purpose · care BODY · hunger as class · sickness as class · `{settlement}` where the parent's slot set holds it}; no claim added, none dropped. Zero colons, zero semicolons, zero citations, zero record words, zero counts, zero events, zero outcomes across all twelve.

1. `[ledger]` The granary at {settlement} is entered as standing, a place for grain against hunger. Those tending the sick are entered beside it, against sickness.
   - `[face]` Those tending the sick are set down at {settlement} as standing, against sickness. The granary where grain is stored is set down with them, against hunger.
   - `[face]` A granary stands at {settlement} and grain is stored in it against hunger. Those tending the sick are entered with it, against sickness.
   - `[face]` Entered as standing at {settlement} are those tending the sick against sickness and a granary where grain is stored. It stands against hunger.
2. `[street]` The town has a place for grain against hunger, and those tending the sick against sickness.
   - `[face]` Grain has its place in the town against hunger, and those who tend the sick are there against sickness.
   - `[face]` What the town has against hunger is a place for grain, and against sickness those tending the sick.
   - `[face]` A place for grain and those tending the sick, the town has these against hunger and sickness.
3. `[counterforce]` Against hunger at {settlement} stands a granary where grain is stored, and against sickness those tending the sick.
   - `[face]` Those who tend the sick stand against sickness at {settlement}, and the granary where grain is stored against hunger.
   - `[face]` What stands against hunger at {settlement} is a granary where grain is stored, and those tending the sick stand against sickness.
   - `[face]` Hunger at {settlement} has a place for grain standing against it, and sickness those tending the sick.

--- NOTES

Face numbering below: **1.1** is variant 1's NUMBERED line, **1.2 / 1.3 / 1.4** its three `[face]` sub-rows in order; the same for variants 2 and 3.

**1.1 `[ledger]` HELD, unchanged.** The variant's canonical line (index 0, the line a falsy seed draws) is already at its ceiling: the body named at its own layer, the office's formula (`entered as standing`, R-vi / W7 — a STANDPOINT, no record noun, no holder, W24), the entailed purpose as an appositive (D-12 HOLDS), the pressure class at the label's engine meaning (W14), the slot inside the sentence (T-F8), the thread carried by `it` (R-i). Tested and refused: `beside it` → `with it`, which trades a live turn for a flatter one with no law behind the change (§21.4, the density law). Its adjacency word is read as the record's order and not as a spatial fact; recorded at §J row 1.

**1.2 MOVED — the mirror of 1.1 broken on two axes, and the relative clause unstranded.** Was *"…The granary is set down beside them, a place for grain against hunger."* The purpose spelling moves from `a place for grain` (1.1's) to `where grain is stored`, and the relative is seated on its own head (`The granary where grain is stored is set down with them`) instead of trailing behind an adjacency phrase; the adjacency word moves from `beside` to `with` so the face no longer reads as 1.1 run backwards. Target: sibling distance at the ceiling (§21.1; the owner's four-faces rule — the faces differ in construction, never as paraphrases of each other). Claim set, vid, angle tag, slot set, sentence count and read order (care → grain) all unchanged (W4, the construction contract).

**1.3 MOVED — the heavy noun repetition cut to the thread's pronoun.** Was *"Those tending the sick are entered with the granary, against sickness."* Now `entered with it`. The deliberate noun echo was licensed for the thread (MOVE-GRAMMAR §1.4.1) but the pronoun carries the same noun forward at half the weight and the face's first sentence already says `stored in it`, so the two `it`s bind to one antecedent and the line reads as one breath. Target: the read-aloud ear (§21.1); law: R-i holds, since a pronoun carrying the subject forward IS the thread.

**1.4 MOVED — the summarising second sentence replaced by a licensed short beat; the pool gains its one short line.** Was *"…and a granary where grain is stored. They stand against sickness and hunger."* The care body now takes its pressure inside the inversion (`those tending the sick against sickness`) and the second sentence carries the granary's own pressure alone: **"It stands against hunger."** Three laws behind it: (a) R-DA-03 / §16 (6) — the 2nd-sentence summary is the machine's signature and its lawful rate is 0.000; the old second sentence restated what the first had already presented, the new one carries a beat the first does not; (b) the paired `against sickness and hunger` on a subject that was the TWO BODIES implied the granary stands against sickness, a pairing the card does not license — the pressures are now distributed to their own bodies; (c) R-DA-06's short-relief floor (`< 8 words` 0.017 → ≥ 0.030) had no instance anywhere in this pool, and the register card's "the short line exists" is a ceiling property, not a licence to run long everywhere. The face keeps ZERO commas, its fronted formula with inversion, its care → grain order and its `{settlement}` inside the sentence.

**2.1 `[street]` HELD, unchanged.** `The town has a place for grain` is the pool's one fully lawful SHIPPED clause and stands verbatim as the density floor requires (§21.4: an exemplar clause lawful under the card may stand as one of the four faces and is never smoothed); the rest of the line is the street's plain parallel with the pressures distributed, the town as possessor only (W1, W23 — it does not know, value or decide), no slot (the parent's set is `{}`), 16 words. Nothing in it is above the floor to push.

**2.2 MOVED — the pressures distributed, the padding struck.** Was *"Grain has its place in the town, and those who tend the sick are there as well, against hunger and against sickness."* Now the pressure rides with its own body in each half (`in the town against hunger` / `are there against sickness`), the doubled preposition `against … and against …` is gone and `as well` — a padding phrase carrying no claim — is struck. Three targets: the same cross-pairing fault cured in 1.4 (a tail carrying both pressures reads them onto both bodies); R-DA-05's rhythm (22 → 19 words, and the face no longer ends on a four-beat tail); the street's ear. `its` binds to `Grain`, its own clause subject (W1).

**2.3 HELD, unchanged.** The variant's pseudo-cleft, and the pool's one street cleft — the construction is exactly what the street angle can spend (a cleft is spoken English, not costume — R-DA-18) and the gapped second clause (`and against sickness those tending the sick`) already carries the second read without a second cleft head. Tested and refused: fronting the pressure (`Against hunger the town has …`), which would share its first two words with face 3.1 and cost the variant its fourth construction (A11's shared-opener arm; W4).

**2.4 MOVED — the doubled preposition struck from the tail.** Was *"…the town has these against hunger and against sickness."* Now `against hunger and sickness`. The pairing is lawful here and nowhere else in the pool because the SUBJECT is the town, which stands against both pressures, where a body subject would not (the same distinction that moved 1.4 and 2.2). Target: the close (R-DA-04 — the face lands on `sickness` with one beat instead of three), 18 → 17 words, one comma. The left dislocation, the fronted pair and the resumptive `these` are kept exactly (W4); `these` stays the bare back-reference and `both` is refused pool-wide, though the marker licenses it, because the `:2703` count breach is this block's ratified finding and no face will stand near it.

**3.1 `[counterforce]` HELD, unchanged.** The variant's canonical line states the stance whole: the pressure fronted as the class the row is keyed against (W14), the inversion, the gapped second half, both bodies named in licensed form — which is the largest single gain the rewrite made over the shipped row, where neither body was named at all. At its ceiling; moving it would only trade one lawful turn for another.

**3.2 MOVED — the pressures taken out of the front of the second clause, and the landing changed.** Was *"Those who tend the sick stand at {settlement} against sickness, and against hunger the granary where grain is stored."* Now both pressures sit clause-final (`stand against sickness at {settlement}` / `the granary where grain is stored against hunger`). Two targets: the fronted-pressure habit was running twice inside one variant and three times in the variant's four faces — the refiner's brief names a repeated fronting as a tic, and the pool's own fence bars a habit repeated across it; and the landing moves from the granary (an object close) to `hunger` (a condition close), so variant 3's four faces now close on four different nouns of two different KINDS (R-DA-04: vary the kind of close). Subject, read order (care → grain) and slot placement unchanged (W4).

**3.3 MOVED — the doubled cleft head struck and its number-agreement wobble cured.** Was *"What stands against hunger at {settlement} is a granary where grain is stored, and what stands against sickness are those tending the sick."* The second cleft head becomes a plain clause (`and those tending the sick stand against sickness`). Three faults cured at once: the doubled `what stands against …` was the pool's one loop shape (R-DA-22 / fault 17 — the WORD may recur, the SHAPE may not, and this shape recurred inside a single 23-word face); `what … are those tending the sick` reads as a number error to an ear that expects `what … is`, and a line that trips is a line that does not land (§21.4's read-aloud ear); and the face was the pool's longest at 23 words with no load to justify it (R-DA-05: rhythm follows load). 21 words now, landing on `sickness`. The single cleft is kept because it is the variant's fourth construction.

**3.4 MOVED — the stranded relative cured by the compact purpose spelling.** Was *"Hunger at {settlement} has a granary standing against it, where grain is stored, and sickness has those tending the sick."* The relative `, where grain is stored` sat after `against it`, three phrases from its head, which is the participial glossary tail in its relative form (fault 2; R-DA-03: the qualification is not a tail) and forced a second `has` to restart the parallel. `a place for grain` carries body and entailed purpose in one phrase (D-12), so the relative is gone, the second `has` is gapped and the parallel closes clean: 20 → 17 words. The pressure-as-subject construction, the read order and the landing on the care body are kept exactly (W4).

## H. THE FOUR HELD FACES, AND WHY HOLDING IS THE REFINEMENT

§21.2 pushes every lawful face toward the ceiling and §21.4 refuses any change that makes a lawful line plainer with no law behind it. Faces 1.1, 2.1, 2.3 and 3.1 were each tested against a named alternative (recorded above, one per face) and each alternative was refused on a law or on sibling distance, not on taste. Three of the four are the variants' NUMBERED lines — the canonical-at-zero rows a falsy seed draws (MOVE-GRAMMAR §3.2), so churn there is the most expensive churn in the pool; 2.1 additionally carries the block's one lawful shipped clause verbatim, which the density floor requires to stand. Eight of twelve faces moved, every move on a named law.

## J. ROWS THIS SEAT RECORDS FOR THE CHAIR (recorded, not decided)

1. **`beside it` / `with them` / `with it` (faces 1.1, 1.2, 1.3) are read as the RECORD's order, not as a spatial fact.** MOVE-GRAMMAR §1.2's PRESENT move may not carry "a spatial fact the field does not hold", and the card holds no geography. This seat's reading: under `entered` / `set down`, the adjacency word locates one entry beside another on the compiled record, which is the ledger's own surface and asserts nothing about where either body stands in the town. If a refuter reads it as a spatial claim, the lawful replacement in all three faces is to carry the thread by repeating the body noun (1.3's shipped device), at the cost of the pool's lightest lines. The temporal alternatives (`after it`, `entered next`) were refused outright: they would assert an order of founding, and institution presence is a STANDING fact with no recorded history (the block's PROVENANCE line).
2. **Face 3.4's pressure-as-subject construction remains the pool's one face exposed to a figure reading** (R-DA-11: nothing inanimate acts with intent). The drafter recorded it; this seat keeps it and keeps the reading — `has … standing against it` is relational and hunger does nothing in the sentence — and notes that the refinement made the exposure smaller, not larger, by removing the intervening relative clause that made the predicate harder to parse. If the chair rules it a figure, the variant loses its fourth construction and falls back to a second body-subject face differing from 3.2 in subject, order and landing only.
3. **The pool now carries one sentence under eight words** (face 1.4's `It stands against hunger.`), where the draft carried none. This is a deliberate move toward R-DA-06's short-relief floor and the register card's "the short line exists"; it is also the only sentence-length change anywhere in the packet, and it changes no variant's sentence COUNT (variant 1 stays at two sentences per face, variants 2 and 3 at one), so the census's `grammars` reading and the sitting's spread pin are untouched.
4. **`both` stays refused pool-wide** though the marker licenses it as a bare back-reference on lawful antecedents (skeleton §8; variant 2's claim row). Face 2.4 keeps `these`. The reason is the block's ratified `:2703` breach (a BODY COUNT on flag reads a friary and a healer satisfy): a count word standing four lines from the breach the rewrite exists to drop is a cost this seat will not spend for one word of idiom. Recorded so the chair reads the judgment rather than re-deriving it.
5. **The pool's twelve two-word openers remain twelve distinct openers** (`The granary` · `Those tending` · `A granary` · `Entered as` · `The town` · `Grain has` · `What the` · `A place` · `Against hunger` · `Those who` · `What stands` · `Hunger at`), which constrained three of the eight moves: any care-first face in variant 2 must open on `Those tending` or `Those who`, both already spent, so variant 2 stays grain-first in all four faces by arithmetic and not by habit (A11; R-DA-05).
