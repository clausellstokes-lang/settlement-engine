# REFUTE ROUND 2 — DS-DEF-2 · `Disasters & Famine: granary AND hospital`

Seat: Opus 5 — REFUTER (the verifier) for the REWRITE, working for the Fable chair.
Judged: the twelve faces standing in `kept.md` after the refinement, against the printed licence card
(`node scripts/prose-licence-card.mjs DS-DEF-2 'Disasters & Famine: granary AND hospital'`, read-only in
laneRW-DEF2), `skeleton.md` (the marker's licensed skeleton, §0–§9 whole), `draft-round-2.md` (the lawful
draft the refinement moved from, for the density-law regression arm), the register card with S2 and S3,
Part B §1, §16–§16.2, §18, §20, §21–§21.4, §22, §23, MOVE-GRAMMAR §1–§3, §1.4.1, §4.4.1–§4.4.3,
CLERK-LAWS §2.4.1 and §2.6.1, ARCH v2 §2.5 and §8.3, and brief ADDENDUM 13 A + B (bars W11–W27) with the
two ratified tables' rows for this desk (D-12, D-13, L-14, L-20, A-10, A-15, A-41).

Status: COMPLETE — twelve face verdicts (the numbered tagged line is face 0, the three `[face]` sub-rows
faces 1 to 3), then three read-aloud notes and the pool-level rows this seat records for the chair.

Tally: **FAIL 4 · WITHHELD 1 · PASS 7.**

## VERDICTS (variant · face · verdict · law · quote · finding · cure)

**1 · 0 · PASS** — the office's formula (R-vi / W7), no record noun and no citation (W24, A13, SOURCE-UNRESOLVED);
both bodies at their own layer (W20), the granary at D-12's entailed purpose, the care body in W15's
always-safe spelling; the slot inside the sentence (T-F8); the thread carried by `it` (R-i). The two
exposures were tested and both hold: the appositive `a place for grain against hunger` is the entailment,
not a gloss of the read; `entered beside it` is adjacency ON THE COMPILED RECORD under `entered`, not a
spatial fact the card lacks (MOVE-GRAMMAR §1.2 PRESENT). Cure: none.

**1 · 1 · PASS** — the two reads in the reverse order, the formula in its second spelling, the purpose seated
on its own head as a restrictive relative; claim-equal to its siblings (A6); `them` carries the care body
forward (R-i). `set down` is the office's formula in the ledger's standpoint and names no keeper (W24).
Cure: none.

**1 · 2 · FAIL** — law: **A5 / the four-faces rule (never a paraphrase of a sibling; the faces differ in
construction, not in vocabulary), and §21.4's regression arm read against the DRAFT.** Quote:
"Those tending the sick are entered with it, against sickness." Finding: this is face 0's second sentence
with ONE word changed (`beside` → `with`) — nine of its ten words are face 0's, in face 0's order, with
face 0's comma tail. Half of a two-sentence face is therefore a synonym swap of the canonical line an
unweighted seeded roll draws instead of it, which is the sibling paraphrase the owner's rule refuses. It is
also a refinement REGRESSION on a named target: the draft read "are entered with the granary, against
sickness", a deliberate noun echo licensed for the thread (§1.4.1), and the refinement traded that distance
for a pronoun. Cure: restore the draft's clause — "Those tending the sick are entered with the granary,
against sickness." (no claim added, the construction contract kept, the thread carried by the noun).

**1 · 3 · PASS** — the formula fronted with its own inversion, care → grain, the pool's one short sentence
closing the face. The second sentence is a new licensed beat (the granary's pressure), not the summarising
second sentence (§16 (6), R-DA-03); `It` binds to the singular `a granary` and carries it forward (R-i). The
coordination `against sickness and a granary` invites a momentary misparse, but the misparse opens no claim,
and unclarity alone is not a finding. Cure: none.

**2 · 0 · PASS** — the pool's one fully lawful SHIPPED clause stands verbatim, which the density floor
requires (§21.4); the town as possessor only (W1, W23 — it does not know, value or decide); no slot, as the
parent's set is `{}` (ARCH §2.5); the gapped second conjunct states the second read whole. Cure: none.

**2 · 1 · PASS** — the entailed purpose as the subject is a genuine construction change; `its` binds to
`Grain`, its own clause subject (W1); `are there` states presence and gives the care class no act (W22).
Tested and held: `Grain has its place in the town` is D-12's ENTAILS row (grain is stored), not the stock —
it asserts no level, no months and no sufficiency, so it does not collide with the `stores: short` modifier
that attaches to this spine (A-10, ARCH §8.3). Cure: none.

**2 · 2 · PASS** — the pseudo-cleft is spoken English, not costume (R-DA-18); the gapped second half carries
the second read without a second cleft head; landing on the care body varies the kind of close. Cure: none.

**2 · 3 · FAIL** — law: **the licence card (`may NOT`: a second fact) with D-12 and D-13's own scopes; the
same cross-pairing the refinement itself ruled unlicensed at faces 1·3 and 2·1.** Quote: "the town has these
against hunger and sickness." Finding: the two pressures are predicated on `these` — the resumed PAIR — so
the sentence reads each body against each pressure: the granary against sickness and those tending the sick
against hunger. Neither is licensed; D-12 entails the granary at grain and a harvest buffer only, D-13's care
entailment is CONDITIONAL and unresolved on this card, and the record holds no relation joining either body
to the other's pressure. The refinement struck exactly this shape at 1·3 ("the paired `against sickness and
hunger` … implied the granary stands against sickness, a pairing the card does not license") and at 2·1, and
left it standing here; the defence that the TOWN is the subject does not reach it, because the `against`
phrase modifies `has these` and `these` are the two bodies. Cure: distribute the pressures inside the
fronted pair and let the resumptive close — "A place for grain against hunger and those tending the sick
against sickness, the town has these." (the left dislocation, the resumptive and the claim set all kept).

**3 · 0 · WITHHELD** — law argued: **W17 / W18 and the card's `may NOT` (no stress record is on this card; a
famine is never an event here).** Quote: "Against hunger at {settlement} stands a granary where grain is
stored". Finding, unproven: `hunger at {settlement}` is available as a constituent, and on that reading the
canonical line of the counterforce variant locates a live pressure IN the town — the one claim this pool's
whole rewrite exists to keep out, and the reading face 3·3 makes explicit. The dominant parse is two fronted
sentence adjuncts (`Against hunger` + `at {settlement}` on `stands`), so this seat does not carry it to a
breach; it is recorded because the same NP is a FAIL one face away and because this is the line a falsy seed
draws. Cure, if the chair rules the reading: carry the slot on the care body instead of beside the pressure
— "Against hunger stands a granary where grain is stored, and those tending the sick at {settlement} against
sickness."

**3 · 1 · FAIL** — law: **R-DA-03 (the qualification is a sentence, never a tail — the relative split from
its clause) with the skeleton's §4 read law, and §21.4's regression arm against the DRAFT.** Quote: "and the
granary where grain is stored against hunger." Finding: the second conjunct gaps `stand`, and the relative
clause now sits between the head and the gapped predicate, so the nearest verb to `against hunger` is
`is stored`: the line is first read as "grain is stored against hunger" and the granary is left with no
predicate at all, which means the conjunct never states the read it exists for (the granary STANDING against
hunger — the counterforce's own material). The draft had no such strand ("and against hunger the granary
where grain is stored"); the refinement moved the pressure clause-final to vary the close and broke the
clause doing it. Cure: take the compact spelling for the second conjunct, which keeps the clause-final
pressure and the face's close — "and a place for grain against hunger."

**3 · 2 · PASS** — one cleft, not two (the loop shape the draft carried is gone), the second conjunct a plain
clause, the slot inside the sentence, both bodies named in licensed form. The residual `stands against …
stand against` is a coordinated predicate, not a recurring shape inside one face. Cure: none.

**3 · 3 · FAIL** — law: **W17 / W18 and the card's `may NOT` (a live pressure at this town is held by no
field), with W20 / W23 (a NONE-layer condition made the possessor of a BODY) and R-DA-11's inanimate limb in
the second place.** Quote: "Hunger at {settlement} has a place for grain standing against it". Finding: here
`Hunger at {settlement}` is the subject NP itself, so the face asserts hunger AT this town — a stress the
card does not read (no stress record is on this card at all; W18) and an event the entailment law refuses
(W17: a famine entails no harvest failure and no season). The pressure class is licensed as the class the
row is keyed against, never as a condition located in the town. The face compounds it by double-gapping the
second conjunct ("and sickness those tending the sick"), which leaves that half with no predicate to carry
`standing against it` — the draft's own `and sickness has those tending the sick` did carry one, so the
refinement removed the repair. Cure: strike the locative from the pressure and restore the draft's second
`has` — "Hunger has a place for grain standing against it at {settlement}, and sickness has those tending
the sick." (the pressure-as-subject construction, the read order, the landing and the claim set all kept).

## READ-ALOUD NOTES (information only, one per variant)

- **Variant 1 `[ledger]`.** The two-sentence faces read evenly and the office's formula carries them; face 3's
  "It stands against hunger." is the pool's one short line and lands harder than any of the long closes. What
  the ear hears across the variant is one closing music: all four faces end on a bare pressure adjunct, three
  of them on a comma tail of the same shape.
- **Variant 2 `[street]`.** The plainest and best-sounding of the three; the shipped clause at the head of
  face 0 is the pool's best opening aloud. Face 3 asks the reader to hold two nouns before any verb arrives,
  which slows the line without hurting it.
- **Variant 3 `[counterforce]`.** The pool's heaviest syntax, and mostly earned — the gapped second conjuncts
  land cleanly in faces 0 and 2. Face 3 is the exception: the double gap stops the reader, and face 1's
  relative clause makes the ear reach for a verb that is not there.

## ROWS THIS SEAT RECORDS FOR THE CHAIR (recorded, not ruled; no verdict rests on them)

1. **The care body paired with its pressure is near-tautological in eleven of twelve faces** ("those tending
   the sick … against sickness"). This seat tested it as a breach on three grounds and refused all three:
   W14 does not bite, because the row's medical half is the ENGINE's own situation vocabulary, not a
   dictionary reading of the label — the sibling keys `DISASTER_ROW_POOL` names are `granary, no medical
   provision` and `granary, parish care`, so the flag's meaning on this branch is medical provision, and the
   desk's docblock records that this pool "speaks BOTH the reserve and the medical halves"; D-13's CONDITIONAL
   is not engaged, because the phrase adds no building and no capability; and W6 does not reach a
   prepositional adjunct, whose bar is written for a clause that restates the READ. The tautology that
   remains is the cost of W15's forced alias, not a writer's padding. Recorded because the marker left it
   open (skeleton §9 row 3) and because a chair ruling against it moves all twelve faces at once and cannot
   be taken face by face without breaking A6.
2. **The close-kind habit.** Eight of the twelve faces close on a pressure adjunct (`against hunger` /
   `against sickness`) and four on the care body; variant 1 is 4 of 4 on the pressure. R-DA-04's
   "vary the kind of close" is measured at the register grain by a walker with a per-kind ceiling, so this
   seat does not convert it into a face verdict — but the cure round should not add a ninth.
3. **Two pseudo-clefts on one pivot.** Faces 2·2 and 3·2 both cleft on `against hunger` and land on a body.
   They sit in different variants with different slot sets, so no sibling-distance arm reaches them; recorded
   so the cure round does not make a third.
4. **`beside it` / `with them` / `with it` are read as the RECORD's order** (the refinement's §J row 1 put
   this to the refuter). Ruled here as lawful: under `entered` / `set down` the adjacency word locates one
   entry beside another on the compiled record and asserts nothing about where either body stands in the
   town. The finding at face 1·2 is about sibling distance, not about the word.
5. **No face cites, counts, grades, forecasts, or names a second civic object.** Citation count zero across
   all twelve (A13, W24, SOURCE-UNRESOLVED); no `two`/`both buildings` anywhere (the ratified `:2703` breach
   stays dropped); no stock word, no event, no season, no outcome, no cause, no standpoint, no badge word; no
   digit, em dash, exclamation, question or which-clause; every face carries its parent's slot set exactly
   and none opens on the settlement token.
