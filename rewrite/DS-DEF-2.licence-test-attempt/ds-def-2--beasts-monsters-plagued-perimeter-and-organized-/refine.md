# REFINE (phase 2, one round) — DS-DEF-2 · pool `Beasts & Monsters: plagued, perimeter AND organized force`

Status: COMPLETE (checkpoint law: written section by section).
Seat: Opus 5 — Fable-unvalidated, a DIFFERENT author than the drafter. Refined ONE FOR ONE from
`draft-round-1.md` against `skeleton.md`, the licence card printed by
`node scripts/prose-licence-card.mjs DS-DEF-2 'Beasts & Monsters: plagued, perimeter AND organized force'`,
and the voice documents named in the brief (REGISTER-CARD S2/S3; Part B §1, §16–16.2, §18, §20, §21–§23,
§21.1–21.4, §24; MOVE-GRAMMAR §1–§3, §1.4.1, §4.4.1–4.4.3; CLERK-LAWS §2.4.1, §2.6.1; ARCH §2.5, §8.3;
ADDENDUM 12's W1–W10 and ADDENDUM 13's two law tables, bars W11–W27).

Three variants, their own numbers, their own angle tags, four wordings each: none added, none removed, none
merged, no tag changed, no typed line touched. Slot set on every wording: exactly `{settlement}`, once.
Claim set unchanged from the draft — the same three reads on all twelve, nothing added, nothing dropped.

---

## THE ROWS — the complete replacement for the pool's variant rows

1. `[ledger]` The country around {settlement} carries creatures. Entered to the town's name are a wall and a muster.
   - `[face]` In a country where creatures are abroad, the works and the muster of {settlement} are carried standing.
   - `[face]` Creatures range the country around {settlement}, and the town's wall and its muster both stand.
   - `[face]` A wall and a muster are entered as standing at {settlement}, and creature country lies around the town.
2. `[street]` Defense at {settlement} is a wall and a muster, and creatures press the country outside.
   - `[face]` The town has a wall at {settlement} and a muster of its own, and the creatures are out in the country.
   - `[face]` Creatures are abroad in the country around {settlement}, and the town's own wall and muster stand.
   - `[face]` The creatures are in the country around {settlement}, and what the town has is a wall and a force.
3. `[unfolding]` What {settlement} has built is in place and the muster with it, and the creature pressure out of the country stands unmet.
   - `[face]` The wall at {settlement} stands and so does the muster, and what lies open is the pressure from the country's creatures.
   - `[face]` At {settlement} the muster stands and the wall as well, and the pressure of creatures in the country stands open.
   - `[face]` Both the works and the town's force at {settlement} are in place, and the pressure out of the country, where the creatures are, is unsettled.

---

## --- NOTES

### One line per face — what changed, and the law or target behind it

**V1 numbered** (`The country around {settlement} carries creatures. Entered to the town's name are a wall and a muster.`)
— was one `X, and Y` sentence with a stacked double joint (`creatures, and the works and the muster are…`); split into
two sentences and inverted the second onto the office's own entry formula, which gives the pool its only inversion and
its only two-sentence wording: target §21.1 (the strongest rhythm inside the voice) and R-DA-05 (rhythm follows load;
the pool is the unit of spread), with the shipped country-scoped frame and `creatures` kept verbatim as the density
floor (§21.4); the thread at k = 0 (R-i) is carried by `the town's` taking `{settlement}` forward; `Entered to…` is the
office's formula (W7, R-vi) and not a record noun or a citation (W24, the budget is zero).

**V1 face 2** (`In a country where creatures are abroad, the works and the muster of {settlement} are carried standing.`)
— the draft's tail `and the muster with them` was the second of two ellipsis tails in the pool, so the pair was made one
compound subject and the flat closing `in a country where the creatures are` was fronted as the frame: removes a
cross-variant tic (the tail now occurs once, on V3's numbered line), lands the wording on the standing rather than on a
copula (R-DA-04, land on the civic noun / the standing, vary the kind of close), and states R1 country-scoped (W2, C7).

**V1 face 3** (`Creatures range the country around {settlement}, and the town's wall and its muster both stand.`)
— the draft's flat `Creatures are in the country…` plus the inverted entry formula was re-cut onto an active predicate
for the tier word and a plain paired standing, with the possessor seated on the town on both bodies (W1): keeps this
face free of the ledger's formula so the variant's four faces differ in CONSTRUCTION and not only in vocabulary
(the brief's four-faces rule; §21.1 sibling distance), and `range` reads `plagued` at its engine meaning, monster
ACTIVITY in the country (L-1, W14), with no magnitude (`thick`, `constant` stay out).

**V1 face 4** (`A wall and a muster are entered as standing at {settlement}, and creature country lies around the town.`)
— the draft's `In the country around {settlement} there are creatures…` spent the pool's one expletive on its weakest
clause; replaced with the bodies first under the entry formula and `creature country` as the tier's compressed surface:
R-DA-07 (the expletive goes; the pool now spends none), §21.4 (a compression that rewards the reader is the ceiling),
and the wording lands on `the town`, a standing civic noun, so it reads well before any sibling modifier.

**V2 numbered** (`Defense at {settlement} is a wall and a muster, and creatures press the country outside.`)
— UNCHANGED, deliberately: it is the pool's tightest wording, it carries the shipped line's one lawful turn (the frame
`Defense at {settlement}`, the only licensed thing the shipped `[street]` row held) and its engine noun `press`
(`threatAssessment.js:52`'s pressure), and §21.4 forbids making a lawful compressed line plainer with no law behind the
change; the declared reading risk is re-declared below, not cured by dilution.

**V2 face 2** (`The town has a wall at {settlement} and a muster of its own, and the creatures are out in the country.`)
— `a muster to its name` became `a muster of its own`: `to the town's name` is the LEDGER's entry idiom and was bleeding
into the street row (a dropped angle by the skeleton's §2 (7) list), while `of its own` is the street's plain possessive
with the possessor seated on the town (W1); the rest stands as the pool's clearest reading of the town's own talk.

**V2 face 3** (`Creatures are abroad in the country around {settlement}, and the town's own wall and muster stand.`)
— was `A wall and a muster stand at {settlement}, and the country around the town carries creatures`, whose predicate
repeated V1's numbered line and whose shape repeated V2 face 4's order; re-cut to the threat first and the two bodies
as one possessive compound subject, which gives the pool its shortest fully-loaded wording (sixteen words) and a fourth
distinct construction in the variant: R-DA-05's within-pool spread and the brief's rule that the faces differ in the
subject, the order of the reads and the landing noun.

**V2 face 4** (`The creatures are in the country around {settlement}, and what the town has is a wall and a force.`)
— UNCHANGED: the cleft is the variant's only cleft, `a force` is the ratified class word (ADDENDUM 13 A.5, W15) and the
wording lands on it; nothing here is plainer or sharper for being moved, so §21.2's "keeps its refinement only if it
stays lawful" is satisfied by leaving it.

**V3 numbered** (`What {settlement} has built is in place and the muster with it, and the creature pressure out of the country stands unmet.`)
— UNCHANGED: `What {settlement} has built` is the ratified always-safe wall spelling carried verbatim as the density
floor (§21.4; A-7, W15), the ellipsis tail is now the pool's only one, and `stands unmet` is the skeleton's own lawful
realisation of R-iv's OPEN move, placed last as V6 requires (W4, the construction contract).

**V3 face 2** (`The wall at {settlement} stands and so does the muster, and what lies open is the pressure from the country's creatures.`)
— `what stands open` became `what lies open` because the draft ran `stands … so does … stands open` inside one wording:
R-DA-22 (one term for one thing) is untouched, the change is rhythm only, and it frees `stands open` for face 3 so the
variant carries four distinct OPEN operators (unmet · lies open · stands open · unsettled) with no forecast, no trend
and no duration (R-DST-W4-b).

**V3 face 3** (`At {settlement} the muster stands and the wall as well, and the pressure of creatures in the country stands open.`)
— `is in place` became `stands` and `is unresolved` became `stands open`: three of the four faces had read `in place`
and `unresolved` is the chrome register's abstraction, so the wording now turns on the standing verb twice on purpose —
what stands, and what stands open — which is the office's licensed repetition (the register card: the WORD may recur,
the FACT must not) and lands the wording on the openness itself.

**V3 face 4** (`Both the works and the town's force at {settlement} are in place, and the pressure out of the country, where the creatures are, is unsettled.`)
— UNCHANGED: it is the pool's longest wording and its only embedded clause, it fixes the threat's class as monsters
(W14) and its scope as the country (W2) inside that clause, and cutting it would flatten the pool's length spread
(R-DA-05) with no law behind the change.

### What did NOT move, and why that is the refinement's result

- **The claim set.** All twelve wordings state exactly the card's three reads and nothing else: R1 `family === 'plagued'`
  (monster activity in the COUNTRY, never disease, never a totality over the town — L-1, W14, W2, C7), R2
  `forces.walls.present` (a wall-class body stands, at the bucket's class words only — no gate, no geometry, no
  material under W11), R3 `forces.garrison.present || forces.militia.present` (a force stands, at the always-safe class
  words `the muster` / `the town's force` / `a force` — never garrison W12, never militia, never the watch W13, never
  the guard, never specialists or a charter hall the key does not read). Arm A6 reads across the four faces of each
  variant and finds them claim-equal.
- **No citation anywhere.** `source: muster · standing LICENSED` is a permission the provenance budget does not fund
  here (Part B §24: one account, no count, a holder that is neither a state organ nor a power), so no wording names a
  roll, a book or a record (W24); the `[ledger]` rows carry the office's FORMULA instead.
- **Refused throughout, as in the draft:** a count or magnitude, a cause, a season or time word, a future, a standpoint
  or band word (`{band}` is RESERVED), a second fact (a rota, a use, a manning, a condition of the works), a relation
  between any two of the three reads, a history, a totality over persons, a belief frame, a digit, an em dash, an
  exclamation, a which-clause, a semicolon, a colon.
- **Form.** No wording opens on `{settlement}` (T-F8). All twelve first-two-word openers are distinct after slot
  normalisation: `The country` · `In a` · `Creatures range` · `A wall` · `Defense at` · `The town` · `Creatures are` ·
  `The creatures` · `What {settlement}` · `The wall` · `At {settlement}` · `Both the`. The three numbered lines are
  distinct at two words. The pool now spends ZERO expletive openers and zero expletives (down from one), ONE inversion,
  ONE ellipsis tail, ONE two-sentence wording and ONE fronted place-frame at a sentence opener — no shape is a habit.
  Lengths run fifteen to twenty-five words with `{settlement}` counted as one word (the draft ran fifteen to twenty-five
  on a flatter distribution: every draft wording was a single `X, and Y` sentence, which was the refinement's largest
  target). Every wording ends on a standing civic noun or on the standing itself, so each reads well immediately after
  the spine and before any sibling modifier.

### The two readings the refuters should look at hardest (declared, carried forward from the draft, not cured)

- **The `Defense at {settlement}` frame** (V2 numbered). It is the shipped line's only lawful turn and the skeleton
  licenses it as a frame the three reads then fill, which they do in the same sentence; a refuter who reads
  `Defense … is a wall and a muster` as a PURPOSE claim (the works exist for defense — a cause the card refuses) rather
  than as the frame naming what stands would fail it. The refinement left it standing under §21.4 rather than diluting
  a lawful compression; the variant's other three faces carry no frame, so it survives that reading with them.
- **The OPEN operators** (V3: `stands unmet`, `lies open`, `stands open`, `is unsettled`). They realise R-iv's OPEN move
  on R1 and are read as saying the country's condition is live and unsettled on the record — never that the town has
  failed to answer it (a relation between the reads), never that anything will come of it (a forecast), never for how
  long (R-DST-W4-b).

Status: COMPLETE (2026-09-11). Twelve refined wordings on disk, one for one with the draft; nothing outside this file
was written; nothing but the read-only licence-card script was executed.
