# CONVERT ET-2 — section 2 of `rewrite/entailment/ENTAILMENT-TABLE.draft.md` re-cut under brief ADDENDUM 14

Seat: Opus, 2026-09-12. Read whole first: ADDENDUM 14, then 12, 13 part A, 13 part B (`briefs/brief-REWRITE-car8b-defense.md`). Dock read-only: `laneRW-DEF2` (no git write, no test, no build). Slice: **section 2, all six desk tables AND their REFUSED sub-tables** — 235 items (D-1..D-32, D-R1..D-R5; G-1..G-23, G-R1..G-R15; E-1..E-20, E-R1..E-R17; P-1..P-20, P-R1..P-R7; S-1..S-24, S-R1..S-R21; WF-1..WF-36, WF-R1..WF-R15). The row is the unit, not the word.

## THE TEST APPLIED

A face is LAWFUL unless it CONTRADICTS the record. "The card does not license it" is no longer a finding. Silence is permission. Four floors survive: (1) no self-contradiction against the WHOLE town's record or another surface on the same page; (2) no invented HISTORY, DATE or NUMBER (THE PROMISE); (3) product scope + the deity doctrine — **an UNNAMED person may now act, hold a key, be resented**; (4) the engine's own positive model.

**THREE STANDING RULES I applied to every row, stated once here instead of 235 times:**

- **R-α THE NUMBER FLOOR IS GLOBAL.** Every `NOT ENTAILED` list in section 2 contains a count, a date, an age in years, a price, a volume or a death toll. Those survive on floor 2 on EVERY row and are not re-argued per row; they are named in `keptAs` only where they are the row's principal survivor. Everything else on those lists — height, condition, manner, mood, busy-ness, who cares, what it smells like, why — is STRUCK.
- **R-β A DISJUNCT IS NOT A CONTRADICTION.** Where the record itself states a disjunction (`Palisade or earthworks`; "navigable river OR a river crossing"; "wolves, raiders, or worse"), naming ONE disjunct is permitted: the record offers it, it does not deny it. This strikes D-2, E-R14 and half of S-10/S-R9 — bars that cost the prose its concrete nouns for nothing.
- **R-γ A TREND OR A PAST STATE IS HISTORY.** "worse than last season", "it has grown", "larger each time" assert a past STATE the engine does not hold and will later compute differently. These stay, on floor 2, not on floor 1.

**ONE GAP RECORDED FOR THE CHAIR.** The verdict vocabulary has tags for floors 1, 3 and 4 (CONTRADICTION · SCOPE · MODEL) and none for **floor 2, THE PROMISE**. Roughly forty rows' only survivor is floor 2. Where floor 2 is the whole of a row I have tagged it **MODEL** and said so in the ground; where it is half a row it sits in `keptAs`. The chair should mint a **PROMISE** verdict before this table becomes the refuters' instrument, or floor 2 will be read as struck.

## 2.1 DEFENSE

| id | gist | verdict | the field / the ground | what a writer may now do |
|---|---|---|---|---|
| D-1 | palisade: timber only as a bare word; no rot, no "could not afford stone" | SPLIT | KEPT `defenseGenerator.js:186-187` (no decay path, no removal for want of money). STRUCK: the material bar — ADDENDUM 14 rules a Palisade IS timber | Call it timber, stakes, a wooden ring, plainly; say the wood was got cheap. Never rot it, never let it fail for want of upkeep |
| D-2 | `Palisade or earthworks`: never resolve the disjunction | STRUCK | The record OFFERS both (`institutionDescVariants.js:445-448`); naming one is not a denial (R-β). The supply-chain stone argument is dead under ADDENDUM 14 | Say "the earth bank" or "the stake line" and build a scene on it |
| D-3 | the stone wall rows: stone/gates entailed; no "stone endures", no decay | SPLIT | KEPT `institutionalCatalog.js:1332`, `:1910` (the row fixes stone/masonry) + `defenseGenerator.js:186-187`. STRUCK: height, thickness, a moat, towers, who paid, manning | Give Town walls a height, a walkway, a look; never call them timber, never crumble them |
| D-4 | citadel: inner, a last refuge; not a perimeter | SPLIT | KEPT the row's own class word + `defenseStateProse.js:1251-1253` (the walls lens prints "a line around" over an inner fortress). STRUCK: stone, a keep, a well, age, a command | Call it stone, give it a keep, a well, a stair; never make it the town's enclosing line |
| D-5 | gate: a controlled entry with gatekeepers | SPLIT | KEPT the watch alias (`priorityHelpers.js:48`; the roster panel). STRUCK: closing at night, a portcullis, "a gate implies a wall" | Close it at night, hang a portcullis; do not call the gatekeepers "the watch" unless a watch row stands |
| D-6 | "perimeter / line / circuit" is false of Citadel and Gates | SPLIT | KEPT `defenseInstitutionBuckets.js:169-182` + the roster panel: a citadel is inner, a gate is a point. STRUCK: continuity, material, height, manning of "the works" | Say "the works" freely and describe them; never draw a line around a town whose only walls row is a citadel or a gate |
| D-7 | garrison: professionals in noble/royal pay; a Barracks is not one | SPLIT | KEPT `institutionLadders.js:23` (Barracks → Garrison is an UPGRADE) + `defenseGenerator.js:387-390` (occupation strips the town's own). STRUCK: a commander, loyalty, tenure, local men | Give the garrison a commander, a grievance, a habit; never mint one out of a Barracks row or call the occupier's men the town's |
| D-8 | militia/muster: part-time townspeople | SPLIT | KEPT `institutionalCatalog.js:1340`, `:1348` (militia and watch share one `exclusiveGroup` at town) + `holderTable.js:280-288`. STRUCK: arms, competence, who calls them out | Arm them badly, drill them, name the field they leave; never print a militia AND a watch at town, never call a garrison town's force "the muster" |
| D-9 | watch: night patrol; not one thing, not the safety label | SPLIT | KEPT the roster/safety panel: below town no watch row resolves (`priorityHelpers.js:48`; `safetyProfile.js:30-35`). STRUCK: honesty, effectiveness, armed, hours, gate duty, crime reports | Make the watch idle, corrupt, thorough — where a watch row stands. Never mint one on a hamlet |
| D-10 | mercenaries: paid, contracted, companies of hundreds | SPLIT | KEPT floor 2 (the row's own magnitude is the only recorded one). STRUCK: a term, loyalty, who pays, foreignness, whether the contract holds | Say who they might sell to next, where they drink; never invent a company's strength in numbers |
| D-11 | charter hall: a licence, monster response | SPLIT | KEPT the roster: no garrison row exists at hamlet/village (`institutionalCatalog.js:325`, `:837`). STRUCK: the grantor, fees, busy-ness, tenure, specialists, and the charter word on `Multiple adventurers' guilds` | Name a fee, a grantor, a crowded board; never hand the work to "the garrison" in a town that has none |
| D-12 | granary: grain stored, a harvest buffered | SPLIT | KEPT the roster panel (no granary/storehouse row ⇒ no building) and the DS-ECO-2 storage tile on the same page (a fullness claim against it) | Describe the doors, the dust, the keeper — where a row stands; never contradict the tile's band |
| D-13 | hospital: somewhere to put the sick | SPLIT | KEPT `priorityHelpers.js:64`: the flag fires on `Healer (divine, 1st level)` and `Monastery or friary`; the roster denies the building. STRUCK: beds, physicians, funding | Write the healer's hands, the monastery's cots; never name a hospital building the roster does not carry |
| D-14 | church/parish/clergy: consecrated ground | SPLIT | KEPT `institutionalCatalog.js:50`, `:300`: `Access to parish church` puts the church 2-5 km away in ANOTHER settlement; `Wayside shrine` has no clergy | Write clergy tending the sick where a parish row stands; never seat a priest in a town whose row is "access to" one |
| D-15 | court: a process that runs | SPLIT | KEPT `priorityHelpers.js:55`: `hasCourtSystem` fires on `Town hall`/`City hall`/an assembly; the roster denies a courthouse. STRUCK: judges, a code, sentencing, impartiality | Put a slow magistrate in the town hall, a dispute, a grudge; never build a courthouse the roster lacks |
| D-16 | prison: people can be held; no capacity, conditions, gaoler | STRUCK | The bar rested on "stocks are not a prison", which general refute R-L7 corrects: `Small prison/stocks` DOES hold cells (`institutionalCatalog.js:1567`) | Write the gaoler, the damp, someone held since the spring (no count, no date) |
| D-17 | port/docks: water access; not the sea | SPLIT | KEPT the route/terrain fields printed on the overview: `Docks` admits a `river` route (`priorityHelpers.js:32`) | Write the quay, the smell, the boats; never sail a stranger in from the ocean on a river town |
| D-18 | navy/fleet: no member, nothing to entail | STRUCK | A dead pool, not a bar: zero of 316 catalogue rows match (`defenseStateProse.js:1608-1612`). There is nothing on the page to contradict | Nothing changes; the sea claim is policed at D-17 |
| D-19 | arcane defence: practitioners only; no wards | SPLIT | KEPT the roster (`Druid Circle`, `Warden's Lodge`, parish/monastery rows set `hasDruid`/`hasDivine` and are NOT in `magicDef`) and `magicWorksAt.js:49-53` | Write wards, sigils, a night-light, a warded gate — freely. Never say nothing arcane stands where a druid or divine row prints; never write magic in a world where magic does not work |
| D-20 | terrain: exactly the `strategicValue` sentence | SPLIT | KEPT the terrain field (a height on Plains, water on a dry terrain). STRUCK: weather, a named feature, why the site was chosen, the ground deciding the wall's material | Name the rock, the wind, the reason they settled the bend; never give Plains a cliff |
| D-21 | the terrain multiplier: the same works are worth more on a mountain | MODEL | `defenseGenerator.js:129-135`: riverside and coastal carry a small POSITIVE multiplier | Never say the river or the coast does nothing for the town's defence |
| D-22 | the readiness band word: exactly the ladder's reading | SPLIT | KEPT the badge printed beside the prose and the other arms' badges; plus floor 2 for a trend (R-γ). STRUCK: a cause, a mood, what the score is made of | Say what the band FEELS like and why the town thinks so; never outrun the badge or borrow another arm's |
| D-23 | the overall readiness label: a composite with a tier bonus | SPLIT | KEPT `defenseGenerator.js:491` (+12 for being cheap to defend) and the roster (no wall, no garrison from a label) | Write the confidence of a well-rated thorp; never turn it into absolute strength or into walls |
| D-24 | the live band: a 0-100 causal position; `collapsed` ≠ nobody left | CONTRADICTION | `causalState.js:423-427`; the population figure prints on the same page | — |
| D-25 | the safety label: a RATIO, floored two ways | SPLIT | KEPT `bandLadders.js:189` ("Fortress towns and occupations sit here") + the criminal-structure panel. STRUCK: how the streets feel, whether people walk at night | Write ease or unease freely; never read contentment or an absence of crime off the word where the occupation or criminal panel says otherwise |
| D-26 | the fifteen posture words: the stress type's own consequences | CONTRADICTION | `defenseDisplay.js:25-41` over `STRESS_TYPE_MAP`; the type's recorded consequences print on the same tab (e.g. `'QUARANTINE ACTIVE'` is a display posture, not a declaration) | — |
| D-27 | criminal structure words; `null` ≠ no organized crime | SPLIT | KEPT the roster (`Rookery`, `Assassins' guild`, `Street gang` all fall to `null`). STRUCK: a hierarchy, who gives permission, territory, an UNNAMED boss | Give the underworld a shape, a rule, an unnamed man everyone avoids; never say nothing organized operates where such a row prints |
| D-28 | the capture rungs: exactly the rung | SPLIT | KEPT the Power tab's printed rung (both directions) + floor 2 for a price or a date. STRUCK: an unnamed person bought, that the town suspects | Write the bought reeve (unnamed), the favour, the silence; never move the rung |
| D-29 | magic dependency: the binding term is the STRESS | SPLIT | KEPT `magicWorksAt.js:49-53` (world law on the same page). STRUCK: reading the crisis flag as a standing dependency | Say the town leans on its wards for its living; never write magic into a world where magic does not work |
| D-30 | blockade: bypass is teleport or airship | SPLIT | KEPT `foodStockpile.js:313-316` (the only channels) + the relationship graph (naming an allied neighbour as blockader). STRUCK: duration, a cause, the fleet's mood | Write the empty quay and a hostile sail; never smuggle grain over a pass the model does not run |
| D-31 | the economic-upkeep gate: ONE purse, the wall stands through it | MODEL | R-viii′, ADDENDUM 12/13A; `defenseGenerator.js:182-192`, `:244-252` | Never "the wall keeps itself and the wages do not", never a thinning muster, never desertion, never arrears |
| D-32 | stress/crisis: the type's own `viabilityNote` consequences | SPLIT | KEPT the type's consequences printed on the tab. STRUCK: duration, a cause, an ending | Write how it is borne; never deny what the type says it does |
| D-R1 | a charter for `Multiple adventurers' guilds` | STRUCK | "Competing organizations" (`institutionalCatalog.js:2175`) is silence about a charter, not a denial | Say the guilds hold licences, or fight over one |
| D-R2 | `organized` ⇒ one hierarchy, permission by somebody | CONTRADICTION | `defenseDisplay.js:187` classifies `Multiple criminal factions` ("Competing gangs. Turf disputes", `:2006`) as `organized`: the row's own desc denies a single hierarchy | — |
| D-R3 | `semi-organized` ⇒ routes and territories | STRUCK | The label fires on a `Gambling den`; nothing on the page denies quiet arrangements | Write the den's routes and understandings as a hook |
| D-R4 | the gate read as a payroll / a per-body wage split | MODEL | `defenseGenerator.js:189`: `milUpkeepMult` is a function of `econOutput`, applied to a palisade exactly as to a garrison; R-viii′ keeps ONE purse | Never make the shortfall a wage fact about one body, never split the purse |
| D-R5 | the `[RECORDED]` tag on "timber" for Palisade | STRUCK | A tagging dispute; ADDENDUM 14 licenses the material word outright | Say timber |

## 2.2 GENERAL

| id | gist | verdict | the field / the ground | what a writer may now do |
|---|---|---|---|---|
| G-1 | `{settlement}`: the page's own town, nothing more | SPLIT | KEPT the panels that print size, tier, walls and prosperity. STRUCK: everything the name may colour | Let the name carry a mood, a history of usage; never read a wall or a fortune out of it |
| G-2 | `{counterpart}`: a directed relation exists | SPLIT | KEPT the neighbour panel (its kind, its size, the tie's charge). STRUCK: distance, the road, what passes along it | Write the road between them, the traffic, the feeling; never re-label the counterpart or its standing |
| G-3 | `{faction}`: a NAMED political actor is a party | SPLIT | KEPT the Power tab's governing name and power figures. STRUCK: manner, grudges, reputation | Give the faction a temper and a reputation; never make it govern where the seat says otherwise |
| G-4 | `{govFaction}` / `{governing}` / `{controller}` / `{npc}` | SPLIT | KEPT floor 3 for a NAMED `{npc}`'s act, decision, disposition or fate (W22 as ADDENDUM 14 leaves it) + floor 2 for tenure. STRUCK: how the seat was held, hostility | An UNNAMED person may now act, hold a key, be slow with the books, be resented. A NAMED one may be named and nothing more |
| G-5 | `{issue}` / `{stakes}`: a matter in dispute, something forward | SPLIT | KEPT floor 2 (an age, a date for the quarrel). STRUCK: taking a side, what it will cost, how it ends | Take the reader inside the dispute; never date it |
| G-6 | `{steading}` / `{ruin}` / `{event}` | SPLIT | KEPT floor 2 (a head count; who fell the ruin). STRUCK: reading an absent ruins flag as an absence in the world; that the event is still felt | Say the ruin is avoided, the event still named; never count the steading or narrate the fall |
| G-7 | `{founder}` / `{challenge}`: a lowercase phrase, a first trial | SPLIT | KEPT floors 2 and 3: a founder's NAME and a founding act on a named person. STRUCK: that the trial was overcome, the founding's manner | Write the kind of people who came; never name them |
| G-8 | the time bands: an elapsed span as a band | MODEL | Floor 2 primarily (THE PROMISE) with `heraldCausalGrammar.js:333-340` as the band model; the engine holds no construction date (annex `:127`) | Never a year, never a date, never "three winters ago" |
| G-9 | `{institution}` / `{resource}` / `{good}`: category labels | SPLIT | KEPT the exploitation bucket printed on the Economy tab ("worked here" against `unexploited`) + floor 2 (price, volume). STRUCK: naming a house, calling a seam rich or poor | Name a workshop as a hook; never work a seam the tab says nobody works |
| G-10 | terrain family word; terrain is derived from the route | SPLIT | KEPT the terrain and route fields (a harbour on a road town; a defensible height on Plains). STRUCK: a compass direction, a named river, soil, climate, a season | Put a river, a wood, a weather in the scene — inside the terrain's own kind |
| G-11 | the approach word; `isolated` is not "no route at all" | SPLIT | KEPT `foodGenerator.js:307-309` (the minor-route trickle, floor 4) AND the cross-desk clash: DS-GEN-5 folds `mountain_road` onto "ordinary" while DS-GEN-13/DS-POP-3 call it NARROW (`generalStateProse.js:820-842` vs `:1102`). STRUCK: road condition, a named road, who maintains it | Describe the road; never say nothing reaches the town, never contradict the sibling line on the same page |
| G-12 | market-class work: the NAME test credits four non-markets | SPLIT | KEPT the roster + criminal panel: `Whisper market`, `Slave market`, `Black market` (`priorityHelpers.js:76-77`). STRUCK: a market day, a square, stalls, a crier, a charter | Write market day in full; never write an open lawful market where the only market-named row is a black or whisper one |
| G-13 | the DS-GEN-17 roster words; the booleans are FROZEN | SPLIT | KEPT the sibling filtered roster (DS-GEN-13, `institutionRoster.js:29-53`) and the antecedents (`hasMilitaryInst` includes `walls`; `ADMINISTERED` fires on a town hall + stocks). STRUCK: whether justice is done, staffing, honesty | Write the clerks and the cells; never call a walls-only town garrisoned by professionals, never speak of a house the filtered roster has removed |
| G-14 | `{calamity}` / the event word | SPLIT | KEPT floor 2 (a death toll, a year, a season, a count) and a named doer. STRUCK: which quarter, what it was like, what people did | Put the fire in a quarter and write it; never count the dead or date it |
| G-15 | `anchored`: a narrative-fit stamp on a 0.6 coin | SPLIT | KEPT the sibling line: DS-GEN-16 reads `lastingEffects.length > 0` (`historyGenerator.js:646-648`) | Say the event is still spoken of; never say it still marks the town where the sibling line says nothing remains |
| G-16 | recency · `historicalCharacter` · founding · ancient ruin | SPLIT | KEPT floor 2 (a year, a generation count, an age cut from the wrong ladder — TIME_BANDS 10/25/60 vs recency 10/30/80/200) + the population figure ("older therefore larger") | Write living memory and the town's sense of its own age; never date it or size it by age |
| G-17 | fabric scar kinds; scars decay on a kind half-life | SPLIT | KEPT `urbanFabricKernel.js:644-647`: the ONLY decay clock over built fabric, keyed on the calamity kind, never on a material (floor 4). STRUCK: where the scar sits, its shape, a district, a direction | Place the burn lots where you like and walk the reader through them; never let anything else in the town rot |
| G-18 | population words; the band's English is not a count | SPLIT | KEPT the population figure printed beside the prose + the trend verb's direction + floor 2. STRUCK: who they are, why they came | Write the people; never number them or turn the band word into a number |
| G-19 | standing · verdicts · scores (eleven ladders) | SPLIT | KEPT every OTHER band printed on the same page (a famine face over a `Surplus` tile; a watch off a score; `viable` never re-run). STRUCK: a cause, a mood, a trajectory as a hook, self-sufficiency as colour — and the ten banned temporal tokens, a pure construction bar | Write what a band feels like from inside; never let one band deny another on the same screen |
| G-20 | relationship type words; STATELESS | SPLIT | KEPT floor 2 (an origin, a date, a "became") + the role the panel prints (the wrong end of patron/client) + the War tab (a treaty it does not carry) | Write what passes between two towns; never say when it started or which way it once ran |
| G-21 | tensions · conflicts · warnings | SPLIT | KEPT floor 2: for eight of ten tension types nothing HAPPENED — no event id, no `yearsAgo` — and a debt has no sum | Write the tension as it is lived; never give it an event or a figure |
| G-22 | steadings and lifecycle; the organic fold hides two provenances | SPLIT | KEPT the typed provenance (`forced` / `resource_strike` / `resettlement`, `settlementLifecycleKernel.js:1207`) + floor 3 (a named person's fate) + floor 2 (a head count, a distance) | Write the satellite's life; never call a decreed settlement organic |
| G-23 | hook category / clock id; the stages are a TEMPLATE | SPLIT | KEPT the clock's printed stage + floor 2 (a stage stated as having happened is an event) | Write the pressure the clock names; never narrate a stage as past |
| G-R1 | `{reason}` = a typed reason | STRUCK | A wiring fact: the slot never fills (`generalStateProse.js:1790-1795`); no writer was ever barred from anything | — |
| G-R2 | `{calamity}` = a typed BLOW | CONTRADICTION | The fill accepts "The Pilgrimage", "The Return", "the great harvest compact" (`:509-517`; `historyGenerator.js:661-676`); the history panel prints the event's own kind | — |
| G-R3 | `road` on DS-GEN-6 = a road | CONTRADICTION | `ORIGIN_POOL_OF_ROUTE[...] \|\| 'road'` (`generalStateProse.js:887-892`): `mountain_pass`, `desert_road` and an ABSENT route all print "is on a road"; the overview prints the real route | — |
| G-R4 | market-class = goods bought and sold (class-wide) | CONTRADICTION | Four members are not lawful markets (`institutionalCatalog.js:2086`, `:1759`, `:1036`; the criminal roster) | — |
| G-R5 | `ADMINISTERED` = "keeps a court" | CONTRADICTION | A `Town hall` + stocks town resolves it; the roster denies a courthouse | — |
| G-R6 | `GARRISONED` = professionals, paid "in wages, not only in stone" | CONTRADICTION | `hasMilitaryInst` includes `'walls'` (`priorityHelpers.js:45`), so a walls-only town resolves it; and the wage half is R-viii′ (one purse) | — |
| G-R7 | `PROVISIONED` care arm = a house of care | CONTRADICTION | `hasHospital` matches `Healer (divine, 1st level)` — a PERSON — and "MAY operate hospital" (`:64`); the roster denies the house | — |
| G-R8 | "the siege" = a besieging force | CONTRADICTION | 'The Siege' names `external_threat`, a DIVIDED RESPONSE (`historyData.js:1560`, `:1182-1184`); the history panel prints the template | — |
| G-R9 | mountain / hills = an approach that climbs | CONTRADICTION | The approach is `tradeRouteAccess`; `hills` + route `river` has a river approach (`terrainHelpers.js:24`) | — |
| G-R10 | `anchored: true` = still bears on the present | CONTRADICTION | A 0.6 coin (`historyGenerator.js:600-604`); DS-GEN-16 reads `lastingEffects` on the same page | — |
| G-R11 | `stable and prosperous` = a prosperity reading | CONTRADICTION | The ELSE-ARM of a timeline COUNT (`historyGenerator.js:848`): a `Struggling` town prints it beside its own rung | — |
| G-R12 | the tension type list is closed at 10 | STRUCK | A producer/display gap (`generalStateProse.js:415-418`), a wiring finding, never a writer's bar | — |
| G-R13 | conflict `intensity` = closeness to violence | STRUCK | It is a COUNT of rivalries (`conflicts.js:238`) — but nothing on the page denies that a high-intensity quarrel could turn. A forward claim is now a hook | Write the quarrel as one that could turn |
| G-R14 | clock `bread_riot` = the food chain is strained, and only that | CONTRADICTION | `if (chain.status === 'stable') continue;` — it fires on `substituted` (magically sustained) and `captured` too (`hookEscalation.js:463-466`); the Economy tab prints the real status | — |
| G-R15 | clock `smuggling_rise` = the trade chain is strained | CONTRADICTION | Same shape, any non-stable `trade_entrepot` chain (`:470-481`) | — |

## 2.3 ECONOMY

| id | gist | verdict | the field / the ground | what a writer may now do |
|---|---|---|---|---|
| E-1 | `{access}`: road · river · port · crossroads · pass; `isolated` has no fill | SPLIT | KEPT the route and terrain fields (naming a way on `isolated`; the SEA on a riverside port, `generationContext.js:195-197`; high ground on a plains "pass") + `tradeRouteSemantics.js:34-47` (the closure is an ANNUALISED rate, never a live winter, floor 4). STRUCK: width, surfacing, upkeep, patrols, a toll, a bridge, a ferry, age, who built it | Build the road, the ferry, the toll-bar, the ruts; never name an approach the route field denies and never close the pass this winter |
| E-2 | `{complexity}`: eleven authored phrases over a label | SPLIT | KEPT the roster/criminal panel (`hasMarketInst` is satisfied by a `Black market` or a `Thieves' guild`, `economicState.js:882`) and the prosperity rung printed beside it. STRUCK: guilds, a square, headcount, tenure | Write the trades, their spread, their crowding; never read a lawful market or a fortune off the label |
| E-3 | `{season}`: a position in the year, one of four | SPLIT | KEPT the `seasonalEvent` field and the stress banner (a hard winter, a failed harvest). STRUCK: weather, temperature, a sowing, a festival, the light | Write the season fully; never make it a hardship the record does not carry |
| E-4 | `{good}` (export column, shared bag) | SPLIT | KEPT the export list printed ("its largest export"; "made here" on a `(transit)`-marked good) + floor 2 (price, volume). STRUCK: a buyer, a route, the wagons | Follow the good to its buyer as a hook; never claim rank or local making against the list |
| E-5 | `{good}` (the exploitation lens's own bag) | SPLIT | KEPT the Economy tab's export list (the two lists are separate records, `resourceGenerator.js:295-317` vs `economicState.js:741-751`) + floor 2. STRUCK: the article's qualities, its reputation | Describe the finished article; never say the town sells it unless the export list does |
| E-6 | `{resource}`: the material nature of 23 raw inputs | SPLIT | KEPT the roster (a process "here" with no house) + the exploitation bucket (`unexploited` = never worked, not stopped) + `resourceSemantics.js` recovery modes (renewable never exhausts, floor 4). STRUCK: quality, depth, tenure, owner, how hard the work is | Say what the seam is like and what the work costs a body; never work it out for good, never stop it, never smelt without a smelter |
| E-7 | `{institution}` (the processing house) | SPLIT | KEPT `economicState.js:692-720` with the whole-estate rule: NO condition, age or wear is modelled anywhere this desk reads — the economy analogue of "walls never decay"; only the calamity ruin filter removes a house (floor 4). STRUCK: size, staffing, busy-ness, the owner, the noise | Fill the mill with noise and a temper; never let it fall derelict, fail or go quiet |
| E-8 | the granary tile: a fraction of what THIS town can hold | SPLIT | KEPT the roster: capacity falls through to 1.5/2.0 months with NO granary row (`foodStockpile.js:190-193`), so the doors may not exist; + floor 2 (months to last) | Write the store where a row stands; never mint the building out of the tile |
| E-9 | the prosperity rung: an ordered position plus a reading | SPLIT | KEPT floor 2 (coin, countable reserves, a past prosperity). STRUCK: a cause, a comparison, the feel of money | Write how wealth or its absence shows; never count it or give it a history |
| E-10 | the food-security rung: a RATIO reading | SPLIT | KEPT the storage tile on the same page (empty stores off `Deficit` where the tile says "well stocked") + floor 2 (deaths). STRUCK: a cause, a season, hunger's texture | Write hunger; never contradict the tile beside it |
| E-11 | the export posture word: six statuses | SPLIT | KEPT the export list ("one good it makes" where the single line is a service or a `(transit)` good, `exportPosture.js:33-36`) + floor 2 (a route once cut is an event). STRUCK: value, buyers, reliability | Write the trade's rhythm; never miscount the list or invent a cut route |
| E-12 | the live trade-flow band | SPLIT | KEPT the price ledger on the same page (market scarcity is a DIFFERENT ledger, `marketPrices.js`). STRUCK: a reason, a caravan, a season, the wharf's mood | Write the caravan and the reason as a hook; never move prices the price ledger holds steady |
| E-13 | the canonical supply-chain status (seven) | SPLIT | KEPT the Power tab's capture rung (a faction taking rents off `captured`, which has NO producer, `supplyChainState.js:443`). STRUCK: every gloss word, a cause, "everything at once" on `collapsing` | Gloss a `strained` chain any way that reads; never hand a chain to a faction the capture rung says has nothing |
| E-14 | the entrepôt: goods that leave were not made here | SPLIT | KEPT the roster (a warehouse or yard as a BUILDING where `hasWarehouse` is false) + floor 2 (a measured volume, a toll). STRUCK: a clerk, a merchant class, the carts | Fill the crossroads with traffic; never build the warehouse district |
| E-15 | the shadow-economy capture tier (three bands) | SPLIT | KEPT the income mix printed (the minor tier 3–10 carries NO criminal income line, `economicState.js:319`) + the faction list (a NAMED faction). STRUCK: organisation, who profits (unnamed), violence as colour | Write the skim and the man who takes it (unnamed); never point at an income line that is not there |
| E-16 | the service catalog and its absences | SPLIT | KEPT floor 2: "the healer left" is an EVENT the record does not hold. STRUCK: the reason a category is thin, what the town does without it | Write the lack and how it is felt; never narrate the departure |
| E-17 | the tier word: a population band and an expectation list | CONTRADICTION | `servicesDisplay.js:15-22` is an EXPECTATION, not a roster: walls, a market, a garrison, a charter or a mayor read off the tier contradict the roster panel | — |
| E-18 | the terrain word (economy): seven producible | SPLIT | KEPT `nearbyResources` (salt, fish and the rest are present only where listed and not depleted, `resourceGenerator.js:59-68`). STRUCK: climate, a named river, a view, the town's shape, a fertility comparison between two terrains | Give the ground its weather and its look; never mint a resource the list omits |
| E-19 | the market as an object (holder kind `market`) | SPLIT | KEPT the roster (a market SQUARE as a building where no such row stands; only `Market square` carries both services, `institutionServices.js:1064-1066`). STRUCK: a day, stalls, a crier, a charter, the record it keeps | Write market day and its crier; never raise the square out of nothing |
| E-20 | dear · steady · cheap: a movement against the town's own usual | SPLIT | KEPT the chain status printed (a shortage as the cause where the chain reads stable) + floor 2 (a named coin, a price). STRUCK: a reason as a hook, a season, demand, a merchant's part | Give the price a story; never copy the crier's receipt clause and never coin a currency |
| E-R1 | `specialist trade` = one speciality | STRUCK | SPECIALIZED is four or five income lines (`prosperity.js:304-306`); nothing on the page denies the town having a signature trade | Name the town's signature trade |
| E-R2 | `mix of field and market` = farming and a market | STRUCK | Farming is never tested, but every thorp/hamlet/village carries `Agricultural Rents` on the income list (`economicState.js:55-60`), so fields are consistent with the page | Write the fields |
| E-R3 | `surplus farm trade` / `farm surplus` = food left over | CONTRADICTION | The label reads tier, market flag and export count, never `foodSecurity` (`prosperity.js:308-314`): a village labelled `Deficit` prints it, and the food tile denies the surplus | — |
| E-R4 | `subsistence living` = nothing left over | CONTRADICTION | SUBSISTENCE is thorp/hamlet && exportCount < 3 (`:315`); a hamlet labelled `Surplus` prints it beside its own tile | — |
| E-R5 | `{good}` (shared bag) = a made or handled THING | CONTRADICTION | The bag holds `'Maritime services (cargo, pilotage)'`, mercenary services and the slave-trade sentences (`economicState.js:624-663`, `:793-795`); the export list prints the label | — |
| E-R6 | the lens `{good}` leaves the town | CONTRADICTION | A separate list (`resourceGenerator.js:295-317`); the Economy tab prints the real exports | — |
| E-R7 | `Secure` = the town covers itself | CONTRADICTION | Residual AFTER imports and magic (`foodGenerator.js:318-330`); the import line prints on the same tab | — |
| E-R8 | `Import-Dependent` = a real share is bought in | CONTRADICTION | Reachable on an isolated thorp with `importCoverageRate` 0 (`:293-296`); the route field denies the buying | — |
| E-R9 | `Active Famine` = nothing is bringing enough | CONTRADICTION | The label is the famine STRESS alone; imports still cover (`:318`) and print | — |
| E-R10 | chain `stable` = all inputs present | CONTRADICTION | Unknown canonicalises to `stable`; `entrepot` is stable with NO local input (`supplyChainState.js:177`, `:199-202`); the chain's own input list prints | — |
| E-R11 | chain `blocked` = a hard upstream failure | CONTRADICTION | The only writer is the isolated-subsistence route shutdown (`customTradeEndpointIntegration.js:113-118`); the route prints `isolated` | — |
| E-R12 | "mill grinds" on the exploitation lens | CONTRADICTION | A.1: `Mill` fills over a town with Taverns and an Ale house and no mill (`resourceGenerator.js:140`, `:166-181`); the roster denies it | — |
| E-R13 | Coastal = a harbour edge | CONTRADICTION | `deep_harbour` may be absent or depleted and `hasPort` is separate; an `isolated` route on coastal terrain is constructible — the route field denies the harbour | — |
| E-R14 | Riverside = a crossing | STRUCK | `geographyData.js:130` is a DISJUNCTION ("along a navigable river OR at a river crossing"); naming a disjunct is permitted (R-β) | Put a ford or a bridge in the scene |
| E-R15 | Mountain = a short growing year | STRUCK | No season-length field exists; a low yield is consistent with a short year, and nothing denies it | Say the growing year is short |
| E-R16 | `steady` (market tag) is a measurement | STRUCK | An absent ledger entry reads `adequate` (`marketPrices.js:318`) — an epistemic bar, not a truth one | Call the price steady |
| E-R17 | the crier's reason ("for it comes scarce this season") | STRUCK | A.5: `causalReceipt` INVENTS the seasonal cause (`marketPrices.js:213-218`) — the defect is the shipped surface's, not the writer's; a cause is now a hook | Give the price your own cause; never lift the receipt clause verbatim (a craft rule, not a finding) |

## 2.4 POWER

| id | gist | verdict | the field / the ground | what a writer may now do |
|---|---|---|---|---|
| P-1 | `{seat}` / the governing body's name (22 labels) | SPLIT | KEPT the seat label AND the DS-POW-5 lens word on the same page, which can say the opposite (`Military Council` → `autocrat`; `Royal Authority` → `council`, `cohesionWeave.js:171-177`) + floor 2 (how many sit). STRUCK: a building, a chamber, how often it meets, whether it is loved | Write the seat's manner, its room, its habits; never call it a council where the label makes it one decider, or the reverse |
| P-2 | "the hall": no hall-name producer exists | SPLIT | KEPT the roster: on a thorp/hamlet the governing institution is `Head-of-household consensus` / `Informal elder consensus` (`institutionServices.js:1448-1453`) — a consensus, not a place. STRUCK: the door, the chamber, the stranger walking in, where a `Town hall`/`City hall` row stands | Walk the reader into the hall at town and up; never put one on a thorp |
| P-3 | "the watch" (power desk) | SPLIT | KEPT the roster/safety panel: five corpus places assert a watch off pools reading legitimacy, breakdown, capture or stability. STRUCK: pay, competence, honesty, whom it answers to, the rota | Write the rota and its failings where a watch row stands; never mint one from a legitimacy score |
| P-4 | "the rolls" / "the clerks" / "the record" | SPLIT | KEPT the roster: the `office` holder kind is instantiated only by `City administration`, `City hall`, `Town hall` (`holderTable.js:357-366`). STRUCK: clerks as people, a ledger book, literacy, a chase for arrears | Fill the office with slow clerks and older ledgers; never write rolls in a town with no office row |
| P-5 | "the court" (political sense) vs the judicial holder | SPLIT | KEPT the roster: a courtroom, a bench or adjudication for the POLITICAL court where no `Courthouse` row stands. STRUCK: an audience, a favourite, the manners of the seat | Write the seat's courtiers and their manoeuvres; never try a case there |
| P-6 | "a bloc" / "the combination" | SPLIT | KEPT the faction panel: a bloc that GOVERNS needs the governing faction inside AND ≥ 0.4 share (`settlementPolitics.js:549-592`), and `coalitionConsolidation01` returns 0 for a DORMANT layer too (`:581`, `:604-607`). STRUCK: a name, a leader, a meeting, a count beyond two as colour | Name the bloc, give it a meeting place; never let it govern against the derivation or read "fully divided" off a dormant zero |
| P-7 | the glue words (compromise · threat · doctrine · patronage · concession) | SPLIT | KEPT the roster and the War tab: the `threat` detail says "at the walls" while `settlementUnderThreat` reads NO wall row (`:438-440`); + floor 3 (nothing predicated of a deity). STRUCK: kinship, affection, money, a debt, a rite, orthodoxy, who holds the other end | Bind a bloc with money, blood or a grudge; never seat it at walls the roster lacks or in a god's will |
| P-8 | the end words (seats · doctrine · patron · commerce · survival) | SPLIT | KEPT floor 3 (a creed's content, a deity) + floor 2 (a sum). STRUCK: ambition, a named target seat, a route, a temple as a place | Give the bloc a want and a target; never give it a doctrine's content |
| P-9 | "the ladder" / "a rung" / "standing" | SPLIT | KEPT `npcLadderState.js:108-110`: depth is a TIER CAP, so "the ladder is short because the faction is" is false (floor 4); + floor 2 (a rise, a fall, a death, an age, a tenure). STRUCK: a title, an office's manner, who is climbing | Write the climbing and the crowding at the top; never explain the ladder's length by the faction's size or date a fall |
| P-10 | "a challenger" / "the seat could fall" | SPLIT | KEPT the capture rung on the same tab: `coupContenders` EXCLUDES criminal factions (`rulingPowerCoup.js:102-104`), so "nobody is positioned to take the seat" is false where the rung reads `corrupted`/`capture`. STRUCK: an intention, a plan, a claim, a grievance | Write the challenger's intent and plan as hooks; never say nobody is positioned where the underworld already holds the seat |
| P-11 | the legitimacy band words | SPLIT | KEPT floor 2 (a past legitimacy, a measured trend — R-γ). STRUCK: a cause, whether anyone was asked, a person's opinion, the mood of consent, the annex's "second fracture fact" fence | Write consent and its absence; never chart it over time |
| P-12 | "the dominant contributor" (four signed numbers) | SPLIT | KEPT the stress banner: the SAFETY word is fed by `famine` → −20, `quarantined` → −8, `controlled` → −5 (`factionDynamics.js:33-40`), so crime read off it contradicts the banner. STRUCK: that the town cares most, a cause, a direction | Say what weighs on the seat; never name crime as the weight where the banner says famine |
| P-13 | "the hold" (`govMultiplier`, five bands into three pools) | CONTRADICTION | `powerStateProse.js:519-524` vs `EngineSections.jsx:208-214`: 1.15 ("lends them weight") draws "backing hardens" and 0.80 ("doubt loosens") draws "rejection is breaking" — the prose can print the opposite of the band beside it | — |
| P-14 | the capture rungs (power desk) | SPLIT | KEPT the faction panel: the DS-POW-6 `{faction}` fill is `governingName` while the rollup may sit on `Military/Guard` or `Merchant Guilds` (`powerStateProse.js:962`; `factionCapture.js:136-152`) + floor 2 (a sum, a date). STRUCK: a method, an unnamed person's part, the manner | Write the arrangement and the unnamed hand in it; never name the wrong body as the captured one |
| P-15 | the criminal operation roles (seven, substring) | SPLIT | KEPT the route/port field (a smuggling landing where no water access resolves, `institutionDescVariants.js:81`). STRUCK: who runs it (unnamed), age, profit, whether the watch knows | Write the landing, the fence, the unnamed boss; never land a boat inland |
| P-16 | the ruling-power words (the DS-POW-5 lens) | CONTRADICTION | `structuralLensOf` reads the MAX-POWER faction, never `isGoverning` (`cohesionWeave.js:279-285`); under `under_siege` the `Military/Guard` row is doubled, so a besieged council town reads `autocrat` against its own seat label on the same page | — |
| P-17 | the economic-base words | STRUCK | All five pools are DARK by declaration and every world reads `mixed` (`cohesionWeave.js:286`; `powerStateProse.js:668-679`): there is no face to police | — |
| P-18 | the stability words (26 labels by FIRST WORD) | SPLIT | KEPT the label printed: the FLOOR pool absorbs `Fractured — no stable governing authority` and its prose asserts "{faction} holds the hall" (`governanceNarrative.js:295`, `:313`). STRUCK: duration, a cause, a rival, a trend | Write instability as it is lived; never seat a settled holder under a "Fractured" label |
| P-19 | "the militia" / "the garrison" / "the guard" / "the community" | CONTRADICTION | `governanceNarrative.js:499-506`: `The guard` is the NON-SMALL-TIER FALLBACK when no force institution matches, and on that same town `safetyProfile.js:300`, `:309` prints "no meaningful guard presence" / "effectively no law enforcement"; and `'watch'` matches the roster row `Watchtower`. ADDENDUM 13A already struck "the guard" from the safe list | — |
| P-20 | "previous government" / "the cause" | SPLIT | KEPT floor 2 (a past transfer, its cause, its date, its battle where `previousGovernments` is empty on 48 of 48 generated towns) + `rulingPower.js:400-404` (a demotion needs Register VII, floor 4). STRUCK: a sense that other arrangements came before | Let the town feel older than its seat; never narrate the transfer |
| P-R1 | a ruling bloc = "a working majority" | CONTRADICTION | `RULING_CONSOLIDATION_FLOOR: 0.4` (`settlementPolitics.js:149`): a 40 % plurality, and the faction shares print beside it | — |
| P-R2 | glue `threat` = an EXTERNAL danger is live | CONTRADICTION | `settlementUnderThreat` is also true for the town's OWN `war_preparation`/`mobilized`/`deployed` posture (`:516-518`); the War tab prints the town's own army | — |
| P-R3 | end `commerce` = merchants | CONTRADICTION | `return base \|\| 'commerce'` (`:495`) is the FAIL-SOFT for `Town Mayor`, `Elder Consensus`, `Elected Reeve`; the faction list carries no merchant | — |
| P-R4 | end `survival` = an external threat, and only that | CONTRADICTION | `GOAL_END_HINT.break_vassalage = 'survival'` with no threat at all (`:470`); the War tab denies the enemy | — |
| P-R5 | the ladder array is sorted top-first by standing | CONTRADICTION | It is a SEAT order (`npcLadderKernel.js:677-683`); `standing[0] − standing[1]` can be NEGATIVE while the desk prints "{npc} leads by very little" against the standing figures | — |
| P-R6 | `recentConflict present` = the quarrel it came through | CONTRADICTION | Every birth producer string is an UNRESOLVED present-tense dispute ("Neither side will back down", `governanceNarrative.js:372-487`) and prints on the page | — |
| P-R7 | the nine corpus nouns (walls · gates · watch rota · granary door · streets after dark · the season · memory · a meeting's minute · a person who speaks for {faction}) | SPLIT | KEPT the roster (walls, gates, a watch, a granary door read by NO power pool) and the faction panel (a person speaking for the wrong faction). STRUCK: the streets after dark, the seasons as texture, the town's memory, a meeting as a scene, spending goodwill | Hold a meeting, walk the streets after dark, let the town remember being hungry — as prose; never furnish the scene with a roster row the town lacks |

## 2.5 STRESSORS

| id | gist | verdict | the field / the ground | what a writer may now do |
|---|---|---|---|---|
| S-1 | siege: a besieger exists; supply cut or contested | SPLIT | KEPT the roster (walls are a PROBABILITY MODIFIER only, `stressGenerator.js:107`; `Gates (if walled)` is a town row at bc 0.5) and the port lifeline (`computeActiveChains.js:756-761`) which denies "nothing moves". STRUCK: the besieger's manner, the mood, stores as texture, the unworked fields | Write the waiting, the shuttered stalls, the men on whatever they have; never put people on walls the roster lacks or stop a port town's water |
| S-2 | famine: food scarcity is public | SPLIT | KEPT the record's own summary (a BLOCKADE famine: "This famine is not a failure of harvest", `stressorDynamics.js:469`) and the roster (a granary building). STRUCK: the hoarders, the price talk, the thinness as texture | Write the hoarding (the record mints 'Grain Holders'), the queues, the bargaining; never blame a harvest the record says was not the cause |
| S-3 | occupied: another authority's writ runs here | SPLIT | KEPT floor 4 (the model REMOVES the town's own force: military ×0.40, −35, factions ×0.3) and the roster (`Garrison` is a CITY row; the walls bar of S-1). STRUCK: the occupier's manner, the resentment, an unnamed collaborator, the hall's continuing business | Write the resented sergeant (unnamed), the taxed stalls, the accommodating alderman; never leave the town's own force standing, never quarter the occupier inside walls it does not have |
| S-4 | politically fractured: no stable authority, decisions paralysed | SPLIT | KEPT the roster (a chamber, clerks, an office where no admin row stands, `stressorDynamics.js:49`) and the faction panel (who holds which district). STRUCK: two sets of written instructions, the paralysis's texture, who is avoided | Write the paralysis in full; never furnish it with an office the roster lacks |
| S-5 | indebted: revenue extracted, investment stopped | SPLIT | KEPT the minted faction list (an OUTSIDE creditor where the record mints resident noble houses, `stressFactions.js:76-92`) + floor 2 (a sum, a share). STRUCK: the collector's manner, what was pledged, the town's feeling | Write the collector at the door; never place the creditor on the wrong side of the wall from the faction row |
| S-6 | recently betrayed: trust low, systems at partial capacity | SPLIT | KEPT the record's own summary ("the wound hasn't closed", `stressNarrative.js:93`; "The betrayer may still be here", `stressTypes.js:70`). STRUCK: what was sold, longer agreements as a hook, the wariness | Write the suspicion and the new carefulness; never close the wound or send the betrayer away |
| S-7 | infiltrated: an outside interest has penetrated | SPLIT | KEPT the record's own lines ("The settlement does not know", `stressNarrative.js:96`; "Its presence is not known", `stressFactions.js:107`). STRUCK: who; what has been discussed; the unease | Write the reader's knowledge against the town's ignorance; never let the town suspect |
| S-8 | disease outbreak: an illness spreading | SPLIT | KEPT floor 2 (a count of the sick), the record's own hedges ("not yet a plague", `stressNarrative.js:99`; "The origin is disputed") and the resisted/informal quarantine (`safetyProfile.js:134-143`). STRUCK: marked doors, the smell, the healers' tiredness | Write the sickness in full and the quarantine as disobeyed; never settle the origin, never count the sick, and see NEW FINDING 1 on the plague word |
| S-9 | succession void: the seat is empty | SPLIT | KEPT `SummaryTab.jsx:32`, which prints "Power rests with <that faction>" beside the void (the faction still EXISTS carrying `'vacant'`) + floor 3 (a named heir) + floor 2 (a death on the world/play path). STRUCK: unsigned instructions, deferred decisions, the claimants' manoeuvres | Write the claimants and the drift; never say there is no authority while the Summary names one |
| S-10 | beast and raider threat | SPLIT | KEPT the roster: "the watch" as an INSTITUTION below town, where the engine's own generic is 'local watch' (`safetyProfile.js:30-35`; `priorityHelpers.js:48`). STRUCK: naming the threat monsters, wolves or raiders — the record's own disjunction (R-β); the roads, the fields, the fear | Name the wolves or the raiders and write them; never mint a watch on a thorp |
| S-11 | insurgency: a movement contests legitimacy | SPLIT | KEPT the record's own summary ("not street fighting but the systematic withdrawal of cooperation", `stressNarrative.js:115`; neither minted opposition is armed). STRUCK: a badge, the withdrawal's texture, who refuses what | Write the refusal, the unpaid tax, the shut door; never arm it on the generation record |
| S-12 | religious crisis: the settlement in question, tithing split | SPLIT | KEPT floor 3 (any theological content) AND the intersection rule: the summary branch is `settlementName.length % 3` and the faction branch `governingFactionName.length % 3`, INDEPENDENTLY, so a branch-specific face can contradict its own record. STRUCK: the parallel services' feel, the calendars, the quarrel | Write the two congregations and their bitterness; never say "contested from within" where the record's other half names an outside authority, and never predicate anything of a deity |
| S-13 | slave revolt | SPLIT | KEPT floor 4 on the WORLD record ("the sim has no slavery substrate to make the claim honestly", `stressorsCore.js:175-180`) and the roster (a `Slave market` row). STRUCK: overseers, the containment, the fear — on the generation record, which asserts the revolt itself | Write the revolt and its containment where the generation record carries it; never build the economy on bound labour |
| S-14 | wartime: inside a war, military spending dominates | SPLIT | KEPT this record's own summary (45 % say the town is positioned to PROFIT, `stressNarrative.js:40-42`) and the roster (`Garrison` is a city row). STRUCK: levies, requisitions, the absent men, the crown as the record's own word | Write the war's weight or its windfall — whichever THIS record's summary says; never reinforce a garrison that is not on the roster |
| S-15 | mass migration: a movement the infrastructure was not built for | SPLIT | KEPT the intersection rule: the summary always says arrivals while the faction block mints a 'Departure Committee' at `economyOutput < 50` (`stressFactions.js:230-241`) + floor 2 (a count). STRUCK: camps, crowding, the newcomers' ways | Write the pressure; state a DIRECTION only where the summary and the factions agree |
| S-16 | the lifecycle stage word (seven) | SPLIT | KEPT F4: stage, age and severity are ORIGIN-relative (`stressors.js:770-776`), `peakSeverity` may be past (`stressorsCore.js:364-367`), escalation keeps raising severity (`stressors.js:740-759`). STRUCK: the town's sense of it, a plan as a hook, the waiting | Write how the town carries it; never say "new here", "the worst has passed" or "it has stopped compounding" |
| S-17 | the origin variant word (seventeen) | SPLIT | KEPT four denials: `unattributed` prints beside a NAMED attacker (F5, `stressors.js:616-623`); `resistance` is a birth stamp, not a present occupation; `palace_coup` is the FALLBACK (F6); `servile_uprising` has no substrate. Plus the roster for walls, halls, warehouses, pulpits, workrooms. STRUCK: the variant's colour, an unnamed hand, the plot's manner | Write the conspiracy and its unnamed people; never call the attacker unnamed where a name is stamped, never read a birth stamp as today |
| S-18 | the condition severity band (four) | SPLIT | KEPT the archetype printed: for `reconstruction`, `boom`, `flourishing`, `war_spoils` a high severity is the magnitude of a LIFT (`archetypeCatalog.js:28-31`); and nothing sorts `conditions[0]` (`activeConditions.js:680-684`). STRUCK: capacity, a limit, a bend, the strain's texture | Write the condition at full strength; never read ruin off a boom's `critical` or call the first condition the defining one |
| S-19 | the condition direction word (worsening · stable · easing) | SPLIT | KEPT floors 1 and 2: every generated condition sits at `elapsedTicks: 0` (`stressorsStateProse.js:329-331`), `easing` is FORCED within 2 ticks of expiry (`activeConditions.js:859`), and `reconstruction` defaults `easing` while `boom` defaults `stable`. STRUCK: what the direction feels like now, what the town expects | Write the dread or the relief; never narrate a past course or read recovery off the expiry flag |
| S-20 | the provenance split (traced / untraced) | SPLIT | KEPT floors 1 and 2: every birth condition is stamped `'GENERATION'` and routed to TRACED (`conditionPromotion.js:203-208`), and a condition can NEVER be old (caps 5–18 ticks). STRUCK: the not-knowing as a mood, the rumours | Write the rumours and the arguments about the cause; never say "before anyone was writing things down" |
| S-21 | the wind-down (duration) | SPLIT | KEPT the engine's own 2-tick window against the desk's 25 % cut, which disagrees IN BOTH DIRECTIONS (`activeConditions.js:859`; `stressorsStateProse.js:259-278`). STRUCK: that nothing was fixed, the relief, the exhaustion | Write the ending as unearned; never say "nearly over" outside the engine's window |
| S-22 | the condition archetype (three written of 46) | SPLIT | KEPT the roster (a school, a workshop, a warehouse as an INSTITUTION) and the purse model (a wage). STRUCK: scaffolding, ruins, the rebuilding's noise, the boom's feel | Write the rebuilding and its noise; never institutionalise it into a house the roster lacks or pay it in wages |
| S-23 | ARITY ("several banners standing at once") | MODEL | `stressGenerator.js:339-346` (independent rolls) and `stressorDynamics.js:458-460` ("Everything not listed keeps today's behavior (no interaction)") outside the listed synergies | Never say the crises compound or that each makes the others harder to end |
| S-24 | `{settlement}`: the town's own name | STRUCK | The row bars nothing; the fill is the name | — |
| S-R1 | a siege ⇒ an army at the walls; "nothing moves through the gates" | CONTRADICTION | Walls are a probability modifier (`stressGenerator.js:107`, `:123`); the thorp `Palisade` is bc 0.3; the port lifeline (`computeActiveChains.js:756-761`); the roster prints what stands | — |
| S-R2 | famine = harvest failure; rationing "by rule rather than by price" | CONTRADICTION | The blockade famine (`stressorDynamics.js:469`); 'Grain Holders' "hold more real power than any formal authority" and "The wealthy are hoarding" (`stressFactions.js:137-142`) deny a rule-based ration | — |
| S-R3 | "The garrison is quartered inside the walls" (occupation) | CONTRADICTION | The town's own force is stripped (`priorityHelpers.js:324`; `defenseGenerator.js:387-389`); `Garrison` is city-tier; the walls bar of S-R1 | — |
| S-R4 | "the clerks have quietly decided which to obey" | SPLIT | KEPT the roster: clerks as an OFFICE where no admin row stands. STRUCK: the standpoint over persons — floor 3 is relaxed, an UNNAMED person may now act | Write the clerk who quietly ignores one of the two instructions, where an office row stands |
| S-R5 | the creditor's kind; "beyond the walls" | SPLIT | KEPT the roster (walls). STRUCK: settling the creditor's kind — four are listed and none settled, which is silence, not denial | Name the creditor's kind as a hook, consistent with the minted faction |
| S-R6 | "The betrayal is over"; "agreements written longer" | SPLIT | KEPT the record's own summary (the wound is open). STRUCK: the contract claim — no contract model is an ABSENCE, not a denial | Write longer, warier agreements; never close the betrayal |
| S-R7 | "the town knows it without knowing who" | CONTRADICTION | "The settlement does not know" (`stressNarrative.js:96`; `stressFactions.js:107`) | — |
| S-R8 | the plague noun; "The sick outnumber the hands"; the origin | SPLIT | KEPT the record's hedge, the disputed origin, and floor 2 (a count over persons). STRUCK: marked doors and the two lists as scene-setting | Mark the doors, keep the lists; never count, never settle the origin |
| S-R9 | "monsters"; "defences adequate for normal times"; "on the rolls as abandoned" | SPLIT | KEPT the readiness badge (the summary asserts the defence profile UNCONDITIONALLY, `stressNarrative.js:105`, and may contradict the badge) and the record (`abandoned_roads` is a world RESIDUAL only). STRUCK: "monsters" — the record's own disjunction (R-β) | Say monsters; never say the defences are adequate against the badge, never put ground "on the rolls" |
| S-R10 | armed / street fighting on DS-STR-1; "The watch's patrols" | CONTRADICTION | "not street fighting but the systematic withdrawal of cooperation" (`stressNarrative.js:115`); `inst.hasWatch` is town-and-up while the type fires lower | — |
| S-R11 | "contested from within"; altars, stonework, clerks | SPLIT | KEPT the branch contradiction (branch 2 is an OUTSIDE authority requiring conversion) and the roster (a temple-class building) + floor 3. STRUCK: the stonework and the altar as scene where a religious row stands | Write the altar and the split congregation; never call it internal where the record's other half says otherwise |
| S-R12 | slave revolt ⇒ town tier; "built on its reliability"; "two populations" | SPLIT | KEPT floor 4 (no slavery substrate) and floor 2 (a demographic split is a count over persons the engine does not model). STRUCK: the tier objection, a wiring fact (`requiresTier` is checked only in Mode 3) | Write the revolt; never build the town's economy on it or split its population in two |
| S-R13 | wartime = hardship / losing; "The garrison is reinforced" | CONTRADICTION | The 45 % profit coin and the independent faction coin (`stressNarrative.js:42`; `stressFactions.js:250-251`); a garrison asserted at any tier against `institutionalCatalog.js:1925` | — |
| S-R14 | "camps outside the wall"; "came from somewhere worse"; "housing has not grown" | SPLIT | KEPT the roster (the wall) and floor 2 ("housing has not grown" is a past state, R-γ). STRUCK: the camps and the newcomers' origin — no camp model and no origin field is SILENCE | Put the camps somewhere the roster allows and give the newcomers a story |
| S-R15 | peaking = "as bad as it has been"; "stopped compounding"; easing = planning | CONTRADICTION | `peakSeverity` tracks the worst and a 0.95→0.75 decline is still `peaking`; escalation keeps raising severity (`stressors.js:740-759`) | — |
| S-R16 | `unattributed`; `resistance` "IS occupied"; `servile_uprising`; `palace_coup` | CONTRADICTION | F5 (`stressors.js:616-623`), a birth stamp never re-interpreted (`stressorDynamics.js:888-890`), no slavery substrate, F6 (the fallback) | — |
| S-R17 | `conditions[0]` is "one of the defining facts"; `critical` on a lift | CONTRADICTION | No sort (`activeConditions.js:680-684`); lifts RAISE the systems they declare (`archetypeCatalog.js:28-31`) | — |
| S-R18 | "each season costs a little more"; "It is lifting" for `reconstruction`; boom as stasis | SPLIT | KEPT `elapsedTicks: 0` at birth, the 2-tick expiry override and the polarity defaults. STRUCK: "spending on repair rather than on holding" — no spending model is an absence | Write where the money goes; never narrate a course the record has not run |
| S-R19 | traced ⇒ "knows exactly what did this"; untraced ⇒ "before anyone was writing" | CONTRADICTION | The `'GENERATION'` sentinel routes every birth condition to TRACED (`conditionPromotion.js:203-208`); a condition can never be old | — |
| S-R20 | scaffolding, ruins, warehouses, wages, workshops, schools | SPLIT | KEPT the roster aliases (a warehouse, a workshop, a school as institutions) and the wage model. STRUCK: scaffolding and ruins as atmosphere | Raise the scaffolding; never raise a school |
| S-R21 | "several" is always two; the crises interact | SPLIT | KEPT floor 4 (world pairs default to NO interaction, `stressorDynamics.js:458-460`) and the banner list's own count. STRUCK: "always two" — a survey error, the cap is Mode 3 only | Say "several"; never make them compound |

## 2.6 WARFAITH

| id | gist | verdict | the field / the ground | what a writer may now do |
|---|---|---|---|---|
| WF-1 | siege: a live `war_front` INTO this town | SPLIT | KEPT the deployment ledger ("an army still on the road is NOT besieging", `warDeployment.js:111-112`; a front is minted from a BARE hostile relationship, `warStatus.js:47-52`), the roster (a wall state) and floor 2 (a duration, a count). STRUCK: the waiting, the mood, the closed shutters | Write the town under threat in full; never put an army at walls the roster lacks or the ledger has not landed |
| WF-2 | campaign: an army committed against a named target | SPLIT | KEPT the food tile (a food or harvest effect it denies) + floor 2 (how many are gone, how long). STRUCK: whether winning, the thinned streets, the waiting for news | Write the absence of the men; never move the granary or count them |
| WF-3 | occupation: a foreign power holds the town by arms | SPLIT | KEPT `occupation.js:873-893` (`occupierStillPresent` is a SEPARATE, falsifiable read — a liberated or relieved occupation loses it) and floor 4 (`vassalized` is NOT permanent; the ladder slides back and `liberated` is the exit). STRUCK: the hall's continuing business, the resentment, an unnamed collaborator | Write the occupier's hand on the town; never assume soldiers in the street or a permanent vassalage |
| WF-4 | pays / paying for itself | SPLIT | KEPT floor 4: the engine never compares yield to cost, and at `extractive` its own scales are benefit 0.45 against burden 0.55 (`occupation.js:135-156`); plus `burdened: !pays \|\| resistance >= 0.35` (`occupationStatus.js:102`). STRUCK: the weight of holding it, the occupier's impatience | Write the holding as a grind or a prize; never make it an accounting that turns a profit |
| WF-5 | resistance bands (four rungs) | SPLIT | KEPT the band printed: `largely quiescent` covers [0, 0.2) and is not "no resistance" + floor 2 (a count of incidents). STRUCK: organisation, leadership, a target, the resentment's shape | Give the resistance a shape and a leader (unnamed); never zero it out |
| WF-6 | muster / mobilization postures (seven) | SPLIT | KEPT the posture word printed: `ticksToDeploy === 0` sits at BOTH ends of the ramp (`warFaithStateProse.js:49-55`) and the figure is "presentation-only" + floor 2 (a headcount, a date). STRUCK: the muster's noise, whose call it is, the levy's texture | Write the muster; never call a demobilizing or exhausted town ready to march |
| WF-7 | war-weariness / the scar (four rungs) | SPLIT | KEPT floor 4 (the scar DECAYS at 0.03 per tick with the army home, `warHomeCosts.js:69`, so it is not permanent) and the deployment record (the scar is written onto a LEVIED VASSAL that fielded nothing, `:432`). STRUCK: the weariness's texture, what the town will not do again, full strength as a boast | Write the tiredness; never call it permanent or earned in this town's own fighting |
| WF-8 | treaty / the document | SPLIT | KEPT `treatyOrientation.js:171-176`: `victorId`/`loserId` carry a WR-10 SALE exactly as a war settlement, so a BUYER is told it is the victor (the declared defect at `warFaithStateProse.js:420-426`); and the printed counterpart is the martial one. STRUCK: why it was signed, whether it will hold, the signing as a scene | Write the signing and the resentment in it; never read a war, a victory or a defeat off the document's sides |
| WF-9 | term / clause (26 types, 13 families) | SPLIT | KEPT the obligor axis (`termObligationOf`, 0.1: the directional cells are authored from the PAYER's chair and the desk does not hold the axis) and the catalog's own executor (`seam` rows are not enforced; only 7 of 26 are `stream: true`). STRUCK: the clause's burden, its indignity, the clerks reading it | Write what the clause costs the town's pride; never state who owes whom against the axis or enforce a `seam` row |
| WF-10 | tribute | SPLIT | KEPT floor 4 (`executor: 'transfer'` moves conserved granary months; a `seam` or `grant` row moves nothing, `peaceTerms.js:108-111`) + floor 2 (an amount). STRUCK: the wagons, the clerks, the grudge, the humiliation | Load the wagons and write the grudge; never move real goods on a row the catalog does not execute |
| WF-11 | compliance: honored · strained · defaulted | SPLIT | KEPT floor 4 + floor 1: the state is the OBSERVED one under fog (`peaceTerms.js:705-707`) and the truth is kept off the read-model on purpose, so "on time and in full" can be denied by the DM surface; and anything unrecognised normalises to `honored` (`treatyDocument.js:160`). STRUCK: a reason, an intent, a next step | Write the suspicion that a clause is being shaved; never assert it is kept in truth |
| WF-12 | blockade (the impairment class) | MODEL | `blockadeTransport.js:19-28`: the engine impairs rows matching `/airship/i` ONLY, and "impaired, never inoperable" | Never give the blockade a road, a harbour or a water effect, and never stop supply outright |
| WF-13 | the aftermath archetypes (eight) | SPLIT | KEPT floor 2 (a measured gain, a before/after comparison). STRUCK: spoils as enrichment — `war_spoils` not listing `economic_capacity` is an ABSENCE, and ADDENDUM 14 rules an absence is not a contradiction source | Write the town flush after the fighting; never measure the gain or date the recovery |
| WF-14 | feasibility (six verdicts) | SPLIT | KEPT the verdict printed + floor 2 (a coalition's membership, a date). STRUCK: intent, a plan, the counterpart's scheming — `require_betrayal` is a fact about THIS town's fracture, but attributing an enemy's intent is now a hook | Write what the enemy might be working on; never re-rank the verdict |
| WF-15 | aggression posture (belligerent … pacific) | STRUCK | The block is DARK and has no exported canonical producer; the one exported reader disagrees on two of five words (`livingWorldSignals.js:152-162` vs `liveWorld.js:89-95`). A wiring finding, not a writer's bar | — |
| WF-16 | the compelled banner vs the cap on arms (four "muster" senses on one page) | CONTRADICTION | `peaceTermsCatalog.js:170` vs `:171`; `mobilization.js:302-306` — a `demilitarization` cap is a real ramp ceiling and a `compelled_alliance` banner is not; both may print on one page | — |
| WF-17 | creed / patron / faith (`{creed}`) | SCOPE | The deity doctrine, constitutional: the fill is the DEITY's NAME and nothing may be predicated of the deity (`warFaithStateProse.js:56-64`) | Name the creed and write its people; never its god |
| WF-18 | temple / shrine / church | SPLIT | KEPT the roster: `hasChurch` matches `priest` and `shrine` substrings, so a `Priest (resident)` with no building sets it (`priorityHelpers.js:65`) + floor 2 (a congregation count). STRUCK: wealth, an endowment, an age, the congregation's manner | Write the rite and the room it happens in; never raise a temple the roster lacks |
| WF-19 | rank: major / minor / cult | SPLIT | KEPT floors 1 and 2: `rankAxis` is the AUTHORED embed rank, defaulted to `'minor'` (`deityCommitEmbed.js:135`), while the pantheon ledger's `tier` is a separate hysteretic field that may say otherwise — so "kept across enough of the realm" is a seat count off a token that counts nothing. STRUCK: the creed's weight in this town as colour | Say how the creed sits HERE; never give it a reach across the realm |
| WF-20 | standing: cult / established / ascendant | SPLIT | KEPT floor 2 and the printed share ("a few households" over a `cult` at share 14 of 55 faithful). STRUCK: the following's character, where it meets | Write where the cult meets and who joins; never count them |
| WF-21 | legitimacy: secure / established / tenuous / contested | CONTRADICTION | `discredited` is a FALL token meaning the seat was LOST (`patronFall.js:76`, `:113-115`); a `contested` legitimacy with the patron still seated is not a fall, and the fall record on the same panel denies it | — |
| WF-22 | contested (the field) | SPLIT | KEPT the legitimacy band printed: nothing couples the two except `patronSecurity ×0.65` (`religionState.js:629`). STRUCK: a schism, a quarrel, a niche collision as hooks | Write the rivalry between two creeds; never infer the seat's legitimacy from it |
| WF-23 | the STANDINGS keys (dominant · pressed · plural) | CONTRADICTION | `!model.contested` is a SIX-POINT MARGIN, not a majority (`religionState.js:626`): a patron at 34 against a rival at 27 is "dominant", and the shares print beside the prose | — |
| WF-24 | cults[] (the smaller observances) | CONTRADICTION | The static embed list is never reconciled against suppression or eviction (`faithPanelModel.js:192`; `religionState.js:270-278`) and `{rival_creed}` may be `contested`, so the cults face denies the rivalry the standings lens asserts an inch away on the same page | — |
| WF-25 | devotion / piety band (five) | SPLIT | KEPT the patron printed (`secular` is the FLOOR band, not "keeps no faith"; DS-FTH-2 is the pool for a town with no patron) + floor 2 (an attendance count). STRUCK: a cause, sincerity, the shape of observance | Write thin or fervent observance; never unseat the patron with the band word |
| WF-26 | the arc: rising / falling / steady | SPLIT | KEPT floors 1 and 2: the trend is PROSPECTIVE — "where the lagged reading is HEADING" (`faithPanelModel.js:79`) — so a series of past readings is invented history, and a town whose piety just FELL can read `rising`. STRUCK: a rate as feel, an endpoint, a cause | Write the direction as a direction; never as a history |
| WF-27 | the unaffiliated sink | MODEL | The sink's own kinetics are `secularPull` and `crisisDisorder` (`religiousContest.js:932`; `religionState.js:386-395`), not the piety target gap | Never grow or shrink the sink off the devotion trend |
| WF-28 | the fall: displaced · discredited · imposed · suppressed | SPLIT | KEPT the roster (eviction sets `suppressed: true, share: 0` and NO building record changes, so "its shrines are closed" is denied) and the patron record (`abandoned` is deliberately absent: a town can secularize to 45 with its patron seated). STRUCK: the manner of the fall, the resentment, an unnamed hand | Write the creed pushed out of favour; never close its shrines or abandon a seated patron |
| WF-29 | niche / schism (`<temper>:<alignment>`) | MODEL | `deityAxes.js:148-153`: the temper is DERIVED from the alignment axes, not independent; and cross-niche competition IS modelled, including capacity-full cross-niche eviction (`religionState.js:68-71`, `:283-285`) | Never say two creeds in different niches compete for nobody, and never treat temper and alignment as two independent choices |
| WF-30 | divine mandate (three phrases) | SPLIT | KEPT floor 3 (any theology) and the phrase printed (the reader is GOVERNMENT-SCOPED and returns `null` for a merchant council, `religionState.js:789`). STRUCK: the ruler's actual authority, the clergy's opinion, the endorsement's texture | Write the clergy's hold on the seat; never predicate anything of a god, never read an absence off a scoped `null` |
| WF-31 | tenure (the seat's length of hold) | MODEL | Floor 2 primarily: the field is NEVER projected (`religionState.js:620`), so every tenure figure is invented; the `requires tenure` variants are undrawable | Never state how long a patron has held the seat |
| WF-32 | covert congregation · temple wealth · pilgrim season | STRUCK | No writer exists for any of the three (`warFaithStateProse.js:96-99`); the record is SILENT, which is now permission. The finding survives as a WIRING one (pools authored against absent fields), not a writer's bar | Write a rich temple or a pilgrim season as flavour; see NEW FINDING 16 |
| WF-33 | `live: false` (the faith-dark lens) | CONTRADICTION | `live` means "a `faithProfile` has been projected" (`faithPanelModel.js:194`), not a new creed: a standalone town with a long-standing patron and no campaign draws "has only just taken root", against the town's own age record on the same page | — |
| WF-34 | the dormant note / the faith teaser | SPLIT | KEPT the cult snapshots printed ("keeps no named faith" where `cultDeitySnapshots` stand and `selectPatron` would seat one) + floor 2 (a duration). STRUCK: the quiet of it, what people do instead | Write a town at peace and unchurched; never deny named faiths the page carries |
| WF-35 | `{counterpart}` (the martial counterpart) | CONTRADICTION | `warFaithStateProse.js:795`, `:930-933`: the fill is the occupier or the lone besieger, NEVER the treaty party, so a treaty with C on a town occupied by B prints B — a fluent, filled, false sentence | — |
| WF-36 | stretchedThin / strengthened (the occupier's holdings) | SPLIT | KEPT the inverted slot roles against the reader that shares their name (`warFaithStateProse.js:282-294`) + floor 2 (a garrison's size or its reinforcement history). STRUCK: the strain of holding many places as colour | Write an overstretched occupier; never size its garrisons or name the wrong holding |
| WF-R1 | siege ⇒ an army physically present | CONTRADICTION | `liveSieges` counts deployments whose target has no front yet and the display never consults the arrival gate (`warStatus.js:103-116`; `warDeployment.js:497-503`) | — |
| WF-R2 | occupation ⇒ a garrison present; "The soldiers are here" | CONTRADICTION | `occupierStillPresent` is a separate read a liberated/relieved occupation loses (`occupation.js:873-893`) | — |
| WF-R3 | pays ⇒ the yield exceeds the garrison's cost | MODEL | Benefit 0.45 against burden 0.55 at `extractive`, capped per occupier (`occupation.js:135-156`, `:794-829`) | — |
| WF-R4 | "the muster has begun" for `alert`; "could march this season" for `deployed`; weariness "from its fighting" | CONTRADICTION | `alert` is "on alert" with nothing raised (`mobilizationStatus.js:29`); `deployed` pins the posture (`mobilization.js:311-320`); the scar is written onto a levied vassal that fielded nothing (`warHomeCosts.js:432`) | — |
| WF-R5 | compliance = the ledger classified the clause as TRUTH | CONTRADICTION | `term.complianceState = comp.observedState` under fog; the truth is deliberately withheld from the read-model (`treatyDocument.js:218-239`) | — |
| WF-R6 | `At war` (the residue key) = a declared state of war | CONTRADICTION | The key fires on a decaying scar, an `alert` posture, an army in transit and a holding of others (`WarTab.jsx:485-492`); the War tab prints no war | — |
| WF-R7 | rank `major`/`minor`/`cult` = a realm seat count | CONTRADICTION | `rankAxis` is the authored embed rank; the pantheon `tier` is never written back (`pantheon.js:383-388`) | — |
| WF-R8 | legitimacy `contested` ⇒ "The faith is discredited" | CONTRADICTION | `discredited` is a fall cause (`patronFall.js:113-115`); the band records no fall | — |
| WF-R9 | STANDINGS dominant ⇒ "holds most of the town's adherence" | CONTRADICTION | A six-point margin (`religionState.js:626`); a seven-point lead closes in two ticks (`SHARE_STEP_MAX: 6`) | — |
| WF-R10 | `CULTS: cults[] present` as authored | CONTRADICTION | The static list is never reconciled; variant 2 denies the rivalry the standings lens asserts (`faithPanelModel.js:192`, `:249`) | — |
| WF-R11 | the arc as a series; the sink growing off piety | CONTRADICTION | Prospective trend (`faithPanelModel.js:79`); the sink's kinetics are separate (`religiousContest.js:932`) — a series is also floor 2 | — |
| WF-R12 | niche uncontested ⇒ "nobody competes for the same believer" | MODEL | Cross-niche contest and capacity-full cross-niche eviction are modelled (`religionState.js:68-71`, `:283-285`) | — |
| WF-R13 | `live: false` ⇒ "newly founded" | CONTRADICTION | As WF-33 (`faithPanelModel.js:194`; `FaithTab.jsx:190`) | — |
| WF-R14 | `{counterpart}` as the treaty party | CONTRADICTION | The fill is the martial counterpart (`warFaithStateProse.js:795`) | — |
| WF-R15 | the corpus nouns (the hall · the market · the wagons · the clerks · the walls · the observers · the court · the pledge · a size history) | SPLIT | KEPT the roster (the walls, and the hall where a conquest replaced it, `warStatus.js:306-309`), the executor model (goods moving on a `seam` row) and floor 2 (a size history). STRUCK: the wagons, the clerks, the observers, the court as scene-setting | Fill the scene with clerks, wagons and watchers; never furnish it with a roster row the town lacks |

## COUNT

| verdict | rows |
|---|---|
| CONTRADICTION | 64 |
| MODEL | 11 |
| SCOPE | 1 |
| STRUCK | 19 |
| SPLIT | 140 |
| **total** | **235** |

Every SPLIT strikes a permission or tone bar and keeps a named field. The programme's own shape shows in the tally: **216 of 235 rows carry a real denial and 159 of the same 235 also carried something the writer never needed to be forbidden** — 140 rows carried both at once. That is the arithmetic ADDENDUM 14 describes: the research was right about what is false and wrong about what that licensed.

## NEW FINDINGS — real contradictions no bar ever stated

1. **The engine contradicts ITSELF about the plague on one settlement.** `stressNarrative.js:99` writes "It is not yet a plague" while the promoted condition on the same settlement is archetype `plague`, label 'Plague', "A virulent illness spreads" (`activeConditions.js:59-61`). Whatever word the writer picks, one surface denies it. This is a wiring ruling, not a refuter's finding.
2. **Mass migration: the record states both directions at once.** The summary always says arrivals (`stressNarrative.js:158-164`) while the faction block mints a 'Departure Committee' and "Managing the emigration crisis" when `economyOutput < 50` (`stressFactions.js:230-241`). A poor town's page says it is filling and emptying.
3. **Wartime: two independent profit coins.** The 45 % summary coin (`stressNarrative.js:40-42`) and `ye = militaryEffective >= 55 && economyOutput >= 45` (`stressFactions.js:250-251`) can disagree on ONE record.
4. **Religious crisis: two independent mod-3 selectors.** The summary branch is `settlementName.length % 3` (`stressNarrative.js:121`) and the faction branch `governingFactionName.length % 3` (`stressFactions.js:301`) — the page can describe an internal schism and an externally imposed conversion simultaneously.
5. **One town is "ordinary" and "narrow" on one page.** DS-GEN-5 folds `mountain_road` onto the ordinary scene (`generalStateProse.js:820-842`) while DS-GEN-13 / DS-POP-3 call it NARROW (`:1102`).
6. **The hold prints the opposite of its band.** Five `govMultiplier` bands into three pools: 1.15 ("lends them weight") draws "backing hardens" and 0.80 ("doubt loosens") draws "rejection is breaking" (`powerStateProse.js:519-524` vs `EngineSections.jsx:208-214`).
7. **`{counterpart}` names the wrong party on a treaty line.** The fill is the martial counterpart, so a treaty with C on a town occupied by B prints B (`warFaithStateProse.js:795`, `:930-933`).
8. **A buyer is told it is a victor.** `victorId` is `receiverId` on a WR-10 SALE exactly as on a war settlement (`treatyOrientation.js:171-176`); the desk declares the defect at `warFaithStateProse.js:420-426` and still prints it.
9. **The DS-WAR-1 `occupierHoldings.*` pools have inverted slot roles** against the reader that shares their name (`warFaithStateProse.js:282-294`) — the desk's own worked example of a fluent, filled, FALSE sentence.
10. **The shipped crier line invents a cause to the reader.** `causalReceipt` mints "for it comes scarce this season" whenever road drift does not explain the band (`marketPrices.js:213-218`). That is a floor-2 breach in the PRODUCT, not in the rewrite, and no bar ever named it as such.
11. **"The seat is empty" prints beside "Power rests with …".** `succession_void` sets the modifier `'vacant'` while the governing faction still exists and `SummaryTab.jsx:32` names it.
12. **`deriveDefenseGroupLabel` mints "The watch" on a `Watchtower`** — a masonry tower — through `includesInstitution` (`governanceNarrative.js:499-506`; `institutionServices.js:1561`), so the page can name a body no roster row carries.
13. **DS-GEN-17's booleans are FROZEN at generation while DS-GEN-13 filters the live roster** (`economicState.js:51`; `institutionRoster.js:29-53`): after a played calamity the two lines on one page disagree about what still stands.
14. **A town can be shown besieged by an army the deployment model says is on the road.** `liveSieges` counts deployments whose target has no front yet and the display never consults the arrival gate (`warStatus.js:103-116`; `warDeployment.js:497-503`).
15. **A town with no granary row still prints a granary reading** — capacity falls through to 1.5 / 2.0 months (`foodStockpile.js:190-193`) — so the storage tile and the roster panel can disagree about whether the building exists.
16. **Three DS-FTH pools are authored against fields with NO producer** (covert congregation, temple wealth, pilgrim season; `warFaithStateProse.js:96-99`), and DS-FTH-3's three `TEMPLE WEALTH:` pools are UNMARKED in the census while DS-FTH-4's `ENDOWED` is marked.
17. **The re-classification vocabulary has no tag for floor 2.** THE PROMISE is one of the four surviving floors and roughly forty rows' only survivor; with tags for floors 1, 3 and 4 alone, a refuter reading this table will read floor 2 as struck. The chair should mint a **PROMISE** verdict.
