Seat: Opus 5 (REFINER, DS-DEF-11 · pool `WALLED-QUIET` · arm A, the Opus refiner on the Opus draft; a different author than the drafter. One-for-one substitution over `draft-round-1.md`: three variants, twelve wordings, none added, none dropped, none merged, none reordered. The typed lines of the pool are untouched and not repeated here.)

1. `[visitor]` {settlement} keeps a {defwork} and lets nothing of its keeping go; the country carries no live threat.
   - `[face]` The country about {settlement} is quiet. The town's {defwork} stands, and its keeping is not let fall.
   - `[face]` Nothing live threatens {settlement}, and the {defwork} the town keeps up stands.
   - `[face]` A {defwork} has standing at {settlement}, kept up by the town, and nothing live is abroad in the country.
2. `[elder]` The {defwork} stands, its keeping paid in full, and the country holds nothing live against {settlement}.
   - `[face]` The upkeep of the {defwork} at {settlement} is met, and it stands. The country is settled.
   - `[face]` Nothing in the country is live against {settlement}, and the {defwork} stands with no part of its keeping unpaid.
   - `[face]` No live threat presses {settlement}. The {defwork} it keeps stands, and nothing of the keeping is left aside.
3. `[ledger]` The town asks little of its {defwork}, and its upkeep is paid whole; the work stands entered on the roll.
   - `[face]` On the roll the {defwork} is carried standing, its upkeep met in full, and nothing live lies against the town.
   - `[face]` Little is asked of the {defwork}. The roll shows the work standing, and its keeping met.
   - `[face]` The roll sets down the {defwork} standing and its upkeep paid, and the country is quiet.

--- NOTES

**Claim set: UNCHANGED.** All twelve carry the same three licensed claims as the draft, and only those — C1 the {defwork} stands (`forces.walls.present`), C2 the country carries no live threat (`settlement.config.monsterThreat` → settled), C3 the upkeep is met (`economicGates.military` not below one) — stated as one composite standing condition with no causal joint. Nothing added, nothing dropped, no variant merged, the face count held at four per variant.

**Per wording: what changed, and the law behind it.**

- **v1 row `[visitor]`** — the draft's `and keeps it up` became `and lets nothing of its keeping go`. The draft repeated *keeps* inside one clause (`keeps a {defwork} and keeps it up`), which reads as a stammer, and stated C3 in the flattest form the fence allows. The new clause is active, the town is the agent, and the gate is still stated only as the wall being kept, never as a sum (skeleton §1.5). The shipped lawful turn `{settlement} keeps a {defwork}` is kept verbatim (§1.6, the density floor). The C2 tail moved from `no live threat stands in the country` to `the country carries no live threat` to kill a cross-pool signature: `WALLED-THREATENED`'s own row opens *Danger stands in the country* (A11 sibling distance; §0.7's cross-pool-signature finding).
- **v1 f1** — was `Nothing live threatens {settlement}, and the {defwork} the town keeps up is standing.` Rebuilt as two sentences with the country as the opening subject and the wall second, so the variant no longer opens three of its four wordings on a negation. `is standing` (a limp copular participle) is gone; the wall now `stands`, and C3 lands as `its keeping is not let fall`. THE THREAD: sentence two picks {settlement} up as `The town's`, so the second sentence hands a noun back rather than changing subject empty-handed (MOVE-GRAMMAR §1.4.1).
- **v1 f2** — inherits the draft f1's opener but loses its tail: `is standing` → `stands`. Twelve words, the pool's short line (the register card: the short line exists; rhythm follows load). Ends on the bare stative, which varies the KIND of close against the noun-landings elsewhere (R-DA-04; the CLOSE move's own guard against "always end on an object").
- **v1 f3** — was `In a country that carries no live threat, {settlement} has a {defwork} standing, and nothing of its keeping is let go.` Two faults cured: the fronted circumstance opener (the pool carried three of them, the brief's named tic) and the *that*-relative. The rebuilt line opens on the wall in the card's own licensed form `has standing`, carries C3 as an agent-bearing passive, and closes on the country. `A {defwork} stands at {settlement}` was deliberately NOT used here: both sibling pools already carry that exact frame (`WALLED-THREATENED` v2 row, `WALLED-STRAINED` v1 f1), so the verb was changed rather than the frame reused.
- **v2 row `[elder]`** — HELD verbatim from the draft, deliberately. It opens on the shipped stative `The {defwork} stands` (§2.6's turn to keep), sets the gate as an absolute in the middle, and lands on the town; it is already the pool's strongest line. §21.4: a refinement that makes a lawful, compressed line plainer with no law behind the change is the regression, so the round's one substitution here was refused. The only pool-level consequence recorded: `in full` is spent here and once more (v3 f1), nowhere else.
- **v2 f1** — was `Nothing in the country is live against {settlement}, and nothing is short in the upkeep of the {defwork} it keeps standing.` Two cures. First, `nothing is short` is a polarity flip of `WALLED-STRAINED`'s own signature (*the pay of its muster is short*, *entered short of the roll*) and had to leave the pool. Second, the wording was the draft's second `Nothing`-opener. Rebuilt with the UPKEEP as the grammatical subject, a construction no other wording in the pool uses, and a short second sentence carrying the tier word `settled` — the elder's plainness bought with aspect and order, never with a span, a date or a *still* (§2.5).
- **v2 f2** — was `With no live threat in the country, the {defwork} at {settlement} has standing, and no part of its keeping goes unpaid.` The fronted PP opener is cut (the tic), `has standing` moves out (it is now v1 f3's, once in the pool), and the gate rides as a `with`-absolute so the wording is one sentence against its siblings' two. C2 keeps the copular `is live against`, distinct from the row's `holds nothing live against`.
- **v2 f3** — was `No threat in the country is live, and the {defwork} of {settlement} is standing. The keeping of it is met.` `is standing` and `The keeping of it is met` were the draft's flattest pair; `met` was also carried by two other wordings. Rebuilt: `presses` for C2 (a measurement in words, negated, no resistance frame imported onto the wall — §0.7), the wall as `The {defwork} it keeps`, and C3 as `nothing of the keeping is left aside`, a surface used nowhere else in the block.
- **v3 row `[ledger]`** — the shipped MOVE `asks little of its {defwork}` is kept (§3.6, the density floor). `keeps its upkeep whole` became `its upkeep is paid whole`: *keeps* is the visitor's verb and was doing duty in four v1 wordings, and the ledger's own direction is the gate at identity stated as paid, never as the shipped `pays little`. The roll close `the work stands entered on the roll` is held.
- **v3 f1** — was `On the roll the {defwork} is entered standing, with its upkeep met in full, and the country carries nothing live against the town.` `is entered standing` is `WALLED-STRAINED`'s exact phrase (v1 f3, *A {defwork} is entered standing at {settlement}*); changed to `is carried standing`, which is the office formula §3.5 lists and the sibling does not hold. The `with`-phrase became a bare absolute (tighter), and C2 took a fresh verb, `lies against`. This is the pool's ONE fronted opener; the other two were removed above.
- **v3 f2** — was `Little is asked of the {defwork}. Nothing of its keeping goes unmet, and the roll shows the work standing.` `unmet` is `WALLED-STRAINED`'s word twice over (*the upkeep of its muster is unmet*, *stands unmet*), so it left the pool. The rebuilt second sentence puts the roll first and hangs the gate on an ellipsis of `shows`, which shortens the line to sixteen words and lands on the keeping. THE THREAD: sentence two carries {defwork} forward as `the work` and `its keeping`.
- **v3 f3** — was `The roll holds the {defwork} standing and its upkeep met at full rate, and the country is settled.` `at full rate` sits one word from `WALLED-THREATENED`'s `at full charge`; it is gone. `holds` moved off (it is spent once in the pool, at v2's row, and in the country's mouth, never the wall's — §0.7 keeps `holds` off a wall to avoid importing resistance on a pool whose key is no live threat). `The roll sets down` is the fourth and last unused office formula, `settled` moved to v2 f1, and this wording closes on the quiet.

**Pool-wide, what the round moved (the refiner's named targets, Part B §21.1 to §21.4).**

- **Tics cut.** Fronted circumstance openers fell from three (v1 f1, v2 f2, v3 f1) to ONE (v3 f1, and that one is the office's own `On the roll`, not a place-fronting). Negation openers fell from four to three, and the three now negate by three different grammars (a verb, a copula, a transitive). The draft's face sets for v1 and v2 were shape-parallel position for position (both f1 on `Nothing`, both f2 on a fronted PP, both f3 a two-clause sentence plus a short second sentence); that parallelism is gone. The limp `is standing` (three wordings) is gone entirely.
- **Cross-pool signatures removed, five of them**, every one measured against the block's own siblings in the annex: `stands in the country` (THREATENED's row), `A {defwork} stands at {settlement}` (THREATENED v2 row and STRAINED v1 f1), `is entered standing` (STRAINED v1 f3), `at full rate` beside THREATENED's `at full charge`, and the two STRAINED polarity flips `nothing is short` and `goes unmet`. A11 sibling distance and §0.7's cross-pool-signature finding.
- **Distinct surfaces, all twelve.** Twelve distinct statements of C1, twelve of C2, twelve of C3; twelve distinct first-two-word openers; no face a paraphrase of a sibling face. The four faces of each variant differ in SUBJECT (the town, the wall, the country, the upkeep, the roll, a negated threat), in the ORDER of the three reads, and in the landing noun.
- **Order spread.** v1: C1-C3-C2 · C2-C1-C3 · C2-C3-C1 · C1-C3-C2. v2: C1-C3-C2 · C3-C1-C2 · C2-C1-C3 · C2-C1-C3. v3: C2-C3-C1 · C1-C3-C2 · C2-C1-C3 · C1-C3-C2. Where two wordings of one variant share an order they differ in subject, in sentence count and in landing.
- **Rhythm.** Word counts (a `{slot}` counts one): v1 17 · 17 · 12 · 19; v2 16 · 16 · 19 · 18; v3 20 · 20 · 16 · 16. Five of the twelve run to two sentences (A1's ceiling), seven to one; the draft ran three. v1 f2 at twelve words is the pool's short line.
- **Rationed devices, counted.** Two semicolons (v1 row, v3 row), unchanged from the draft, each joining co-ordinate standing conditions and never a maxim. Three absolutes (v2 row, v3 f1, v1 f3's agent phrase). No colon, no cleft, no inversion, no participial opener, no `which`, no em dash, no exclamation, no digit, no perfect tense, no durative reading forward, no `still` / `yet` / `no longer` / `again`, no condition word on the fabric beyond standing.
- **Provenance: still zero spent.** No wording attributes a fact to a holder. The roll is named exactly once in each of the four `[ledger]` wordings and nowhere else, as the ledger register's own record surface (§3.5's formula: `entered`, `carried`, `set down`, `on the roll`, `the roll shows` — the round spends all four, one per wording, which is the widest spread the formula allows); the office's own books are never named. §0.6's tension between pricing the roll at zero and §3.7's requirement that a ledger face carry the roll is unchanged by this round and remains the chair's to close.
- **The angle fences held.** The visitor never reads the purse: its four C3 surfaces are `lets nothing of its keeping go`, `its keeping is not let fall`, `the town keeps up`, `kept up by the town` — the wall being kept, never a sum. The elder buys its length only in the stative's aspect (`stands`, `it stands`, `has standing` is spent on the visitor this round so the elder carries `stands` three ways) and asserts no span, date, reign or forecast. The ledger carries the roll in every wording and states the gate in the roll's direction, never the shipped `pays little`.
- **Two rows carried to the chair, unchanged by this round** (from the skeleton §4): the card's `{defwork}` NAMED BUT NEVER FILLED line against the desk's `defworkFill`, and the card's `predicate: none recovered` against the key function's three-way branch. Both wiring rows. The card's `may claim` line names `present` alone while the chair's brief orders every read stated; this packet follows the brief, as the draft did.
