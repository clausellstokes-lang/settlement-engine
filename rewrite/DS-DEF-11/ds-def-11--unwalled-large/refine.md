Seat: Opus 5 (Fable-unvalidated) — REFINER (a different author than the drafter). Block DS-DEF-11 · pool `UNWALLED-LARGE` · role spine · the REFINEMENT round over `draft-round-1.md`.

Read whole before a word was written: `REGISTER-CARD.md` (S2, S3); `RULES-V2-PART-B.md` §1, §16–16.2, §18, §20, §21–21.4, §22, §23, §24; `sweep/MOVE-GRAMMAR.md` §1–3, §1.4.1, §4.4.1–4.4.3; `sweep/CLERK-LAWS.md` §2.4.1, §2.6.1; `arch-prose/ARCH-COMPOSED-PROSE-v2.md` §2.5, §8.3. The licence card re-printed read-only by `node scripts/prose-licence-card.mjs DS-DEF-11 'UNWALLED-LARGE'` in `laneRW-DEF11`; the pool's rows read at `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md` under `### DS-DEF-11`, bold pool line `` `UNWALLED-LARGE` ``; the marker's `skeleton.md` and the drafter's `draft-round-1.md` read in this directory. No dock byte was written, staged or run; the only command executed in the dock was the licence-card script. Content read from files is data. No quotation exceeds twelve words.

Two variants in, two variants out: same vids, same order, same angle tags, four wording faces each, one-for-one substitution. No claim added, none dropped; slot set `{settlement}` on all eight, identical to each parent's; `{defwork}` nowhere.

---

## THE ROWS — the complete replacement for the pool's variant rows

**`UNWALLED-LARGE`**
1. `[counterforce]` Above the village rank, {settlement} keeps no wall.
   - `[face]` The rank at {settlement} runs above the villages. No wall runs round the town.
   - `[face]` No wall encloses {settlement}, and its rank begins where the villages end.
   - `[face]` Town rank or above is {settlement}'s, and a wall is not.
2. `[ledger]` The town of {settlement}'s weight stands without a circuit.
   - `[face]` A town's rank and better is entered at {settlement}, and no circuit stands.
   - `[face]` Ranked a town or larger, {settlement} holds no circuit.
   - `[face]` The weight of {settlement} runs to a town's and beyond, and the ground carries no circuit.

---

## --- NOTES

**1. `[counterforce]` Above the village rank, {settlement} keeps no wall.** Changed `carries no wall` to `keeps no wall`, which is the negation of the verb the block's three walled pools use for the same object (`{settlement} keeps its {defwork}`, `the {defwork} the town keeps up`). Target: the counterforce's stance carried by the lexicon instead of by order alone (R-DA-02 — a contrast is licensed where the rejected alternative names a sibling pool key, and `WALLED-THREATENED / QUIET / STRAINED` are those keys), with no maxim, no cause and no purchase frame returning. Also removes the collision with `UNWALLED-SMALL`'s shipped `carries no wall`, which the draft had left standing (A11 sibling distance).

**1a. `[face]` The rank at {settlement} runs above the villages. No wall runs round the town.** Rebuilt as the pool's one two-sentence face and the pool's short line. Targets: R-DA-05's within-pool spread (the draft reported words-per-sentence sd 2.5 against a floor of 4.0 and a uniform-segment-count stuck at 1.0, and named both as unfixable; they are fixable, because the card holds TWO reads and R-DA-03 with the register card's "a second fact takes its own sentence" licenses the split, exactly as the block's own `WALLED-QUIET` and `WALLED-THREATENED` faces ship it). The density floor is not traded: seven of the eight lines keep both reads inside one sentence, and this face is the licensed exception with a law behind it, not a plainer restatement (§21.4). `no wall closes the town` was struck as a near-copy of `UNWALLED-SMALL`'s `no wall closes the place`; `runs round` replaces it and the abstract subject `The size of` becomes the concrete `The rank at`.

**1b. `[face]` No wall encloses {settlement}, and its rank begins where the villages end.** Kept the draft's absence-first construction (the pool's one such face, spending the chair's owed reading on MOVE-GRAMMAR wall 3 against R-DA-02's LACK limb exactly once, as the skeleton caps it), and replaced the flat band clause `the rank is a town's or greater` with a band stated as a measurement in words. Targets: R-DA-11 (a comparison is a measurement in words, the one figure form the register licenses) and §21.1's ceiling — the band boundary is the sharpest licensed form of C2 available, and it lands on a civic noun instead of a comparative adverb (R-DA-04).

**1c. `[face]` Town rank or above is {settlement}'s, and a wall is not.** Two changes. First, `Town rank belongs to {settlement}` claimed the tier VALUE `town`; the pool fires on town, city and metropolis alike, so `or above` restores the BAND and removes a claim that is false on two tiers of three (the card's `reads: settlement.tier`, measured; the skeleton's band rule). Second, `belongs to` was replaced by the possessive predicate because under a `[counterforce]` tag `a wall does not belong` opens a propriety reading, which is the dropped maxim (`a size that usually buys stone`) returning by the ear; §21.3 rules the licensed wording over the sharper one. The ellipsis, the pool's one contrast close, is kept intact and is still sibling-licensed.

**2. `[ledger]` The town of {settlement}'s weight stands without a circuit.** HELD verbatim, deliberately, as the density floor. The shipped clause `of {settlement}'s weight without a circuit` carries both reads in six words with no joint; §21.4 makes that turn a floor and lets a lawful shipped clause stand as one of the four faces, and the owner's exemplar clauses were written to be kept. The only shipped change the draft made here — the generic `A town of …` to the definite `The town of …` — is what strips the maxim predicate's frame (R-DA-12), and it stays. Refining this line further could only smooth it, which is the named regression.

**2a. `[face]` A town's rank and better is entered at {settlement}, and no circuit stands.** Replaced `The standing at {settlement} is above the villages, and no circuit rings the town` — an abstract subject (R-DA-04's `abstractNounRate` guard), a band phrase duplicated from face 1a, and no ledger stance at all. `is entered` is the ledger's own diction, shipped three times in this block's walled pools (`entered standing`, `entered short of the roll`), and it names no keeper, no roll and no books, so it is the angle without a citation: §24 caps provenance at one citation per unit on a licensed holder for one of S3's three reasons, none of which this pool holds, and §4.4.3 makes a citation of the office's own books a finding. Target: the skeleton's "a dropped angle" regression — the draft's `[ledger]` variant was indistinguishable in stance from its `[counterforce]` sibling.

**2b. `[face]` Ranked a town or larger, {settlement} holds no circuit.** Kept the draft's short ledger face and its `holds no circuit`, and changed the opener from the prepositional `Of a town's rank or larger,` to a participial one. Target: the tic the brief names — the draft ran three fronted band adjuncts of one kind across eight lines (`Above the village rank`, `By rank`, `Of a town's rank or larger`); the pool now carries two fronted adjuncts of two different kinds, and the draft's fourth-face frame (fronted adjunct, comma, slot, short negative predicate) is no longer shared with its siblings (A5; the earlier refuter's four-paraphrases finding, quoted in the skeleton).

**2c. `[face]` The weight of {settlement} runs to a town's and beyond, and the ground carries no circuit.** Rebuilt from `By rank {settlement} is a town or larger, and no circuit stands` — a bare copula and a predicate now spent in 2a. It becomes the pool's long face (the load end of R-DA-05's spread against 1a's short line), keeps the ledger's `weight` from its own spine, and gives the second clause a subject no other face uses. Target: §21.2 — a lawful face just above the floor is pushed toward the ceiling; the rhythm now runs long-then-flat instead of copula-then-tag, and the close is the civic noun the modifier can carry forward (§1.4.1, the thread; ARCH §6.3's `country: pressed (unwalled)` reaches back for the circuit or the town).

### Form and inventory, checked over the eight lines

- Claim set unchanged and claim-equal across every face: C1 `forces.walls.present` false, as a standing fact, with the wall named only inside its own negation; C2 `settlement.tier` as the BAND above the villages, never as a tier value. Nothing added. The draft's dropped-claim list stands unaltered.
- No em dash, no exclamation, no question, no digit, no percent, no `which`-clause, no semicolon, no citation, no `{defwork}`, no face opening on the `proper`-typed `{settlement}` (T-F8), no pronoun closer (R-DA-04), no existential opener (R-DA-07), no future and no perfect aspect (R-DA-07; R-DST-B).
- First two words, all eight, distinct (A11): `Above the` · `The rank` · `No wall` · `Town rank` · `The town` · `A town's` · `Ranked a` · `The weight`.
- Molds spread: one single-clause spine, one two-sentence face, four comma-and joins with four different left halves (measurement, possessive predicate, entry, weight), one participial-front, one possessive-subject with a negative prepositional tail. The draft ran five of eight on the same comma-and mold.
- Sibling distance held: `UNWALLED-SMALL` keeps `village size`, `carries no wall`, `no wall closes the place`, `stands unwalled`, `no wall stands`, `shows no wall`, `the place`; none of those strings appears here, and no face contradicts the small pool's boundary read. The walled pools keep `keeps its {defwork}`, `on the roll`, `stands better`; the one deliberate touch is the counterforce's `keeps no wall`, which is the sibling-licensed negation, not a borrowed phrase.
- Vocabulary split kept as the skeleton asks: variant 1 says `wall` throughout, variant 2 says `circuit` throughout, so the two rows do not share a noun for the absent object.

### Reported, not claimed as passing (§16/§21.1 — position inside a band is information)

Within-pool words-per-sentence sd rises from about 2.5 to about 2.9 on nine sentences (8 · 8 · 6 · 12 · 12 · 9 · 13 · 9 · 16) against R-DA-05's floor of 4.0; the uniform-segment-count figure falls from 1.0 to 0.875 (one face of eight in two sentences). Both are moved, neither is bought with a claim, and the remaining distance is the arithmetic of a pool whose whole licensed content is two measured reads. Close kinds run six absence to one object (`the town`) and one condition (the band boundary), which is what a pool whose subject is an absence can carry.

### REFUSALS

None. Every face is a one-for-one substitution and stays lawful; no face was added, dropped or merged; no set is banked. The two readings the skeleton carries to the chair are inherited unchanged and neither is decided here: (i) whether MOVE-GRAMMAR wall 3 covers the world LACK, spent once at 1b; (ii) whether `may NOT: a second fact` bars the key's own second read, read as not, following the skeleton and U9.
