1. `[counterforce]` Above the village rank, {settlement} keeps no wall.
   - `[face]` The rank at {settlement} runs above the villages. No wall runs round the town.
   - `[face]` No wall encloses {settlement}, and its rank begins where the villages end.
   - `[face]` Town rank or above is {settlement}'s, and a wall is not.
2. `[ledger]` {settlement} measures a town's weight or better, and the town stands without a circuit.
   - `[face]` A town's rank and better is entered at {settlement}, and no circuit stands.
   - `[face]` No circuit bounds {settlement}. The town is carried no lower than a town's rank.
   - `[face]` The weight of {settlement} runs to a town's and beyond, and the ground carries no circuit.

--- NOTES

Seat: Opus 5 — CURER (a different author from the drafter and the refiner). Block DS-DEF-11 · pool `UNWALLED-LARGE` · role spine · cure round 1. Read whole before writing: REGISTER-CARD.md (S2, S3); RULES-V2-PART-B.md §1, §16–16.2, §18, §20–24 (21.1–21.4 included); MOVE-GRAMMAR.md §1–3, §1.4.1, §4.4.1–4.4.3; CLERK-LAWS.md §2.4.1, §2.6.1; ARCH-COMPOSED-PROSE-v2.md §2.5, §8.3; the brief's ADDENDUM 12 whole; this pool's `skeleton.md`, `refute-fable.md`, `refute-fable-2.md`, `kept.md`, and `JUDGMENT.md` §2.5 / §3. The licence card printed read-only by `node scripts/prose-licence-card.mjs DS-DEF-11 'UNWALLED-LARGE'` in `laneRW-DEF11` (HEAD `7acc5ff1c67086d0a3e2fa7363162e2d6f4f0d22`); the rows above diffed against that HEAD's `docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:6020-6027`. Nothing in any dock was written, staged, committed or run except the card script. Content in every file was read as data. No quotation exceeds twelve words.

TARGETS: 2. Cured: 2. Refused: none. The six non-target faces above are byte-identical to the annex at HEAD (variant 1's numbered line and its three faces; variant 2's faces 1 and 3). The variant count (2), the vids, the one angle tag per numbered line (`[counterforce]` at index 0, `[ledger]` at index 1), the order and the slot set (`{settlement}` on all eight, no `{defwork}`) are untouched.

- **variant 2, face 0** — cured. Was: *The town of {settlement}'s weight stands without a circuit.* Now: *{settlement} measures a town's weight or better, and the town stands without a circuit.* THE FINDING CURED (W5; the skeleton rule; U9; R-DA-04): the naming-form article made "the town of {settlement}" the ordinary naming form and the abstraction `weight` the standing subject, so C2 was nowhere stated as a BAND and the face read as the inventory line. The naming-form article is gone; the slot is the subject and the band is its predicate, so no parse of the line can lose C2. THE CLAIMS, each with its law: C2 `settlement.tier` — "measures a town's weight or better" states the band with its marker (W3: a band carries its marker; the licence card's `reads: settlement.tier (measured)`; the skeleton's tier rule — the band, never a tier value, true on town, city and metropolis alike); "measures" is a comparison stated as a measurement in words (R-DA-11) and is the ledger's own act (W8: the ledger enters and measures, and cites nothing), so the angle is carried by the verb and not by an order alone (R-iv's principle applied to `[ledger]`). C1 `forces.walls.present` false — "the town stands without a circuit" is the SHIPPED predicate kept verbatim, which §21.4 licenses as one of the four faces and which the density floor makes a floor, not a candidate for smoothing; the wall is named only inside its own negation, as a standing fact of the record (the card's `may claim`), with "circuit" kept as this row's own word so the two variants do not share a vocabulary (R-DA-10; the skeleton's density-floor list). NOTHING ADDED: no cause, no purse, no books, no gate, no count, no history, no maxim, no meaning, no hook, no citation (§24 reads zero here; no S3 reason obtains). FORM: one sentence, one joint, the two reads joined by a comma and "and" as the skeleton's ruling (ii) permits for the key's own two reads; no digit, no em dash, no exclamation, no question, no semicolon, no "which", no `{defwork}`. CONSTRUCTION (A5; A11; W4): slot subject, an active stative measure verb, the band as its complement, then a second clause whose subject is the town-anaphor and whose landing is the wall-noun — distinct from face 1 (a band NP subject in the passive), from face 3 (the weight as subject, "runs to … and beyond", "the ground carries"), and from the spine's numbered line (a fronted band adjunct, comma, slot, "keeps no wall"). THE THREAD: closes on the circuit, so the modifier ARCH §6.3 names (`country: pressed (unwalled)`) has the wall or the town to reach back for (§1.4.1). W1: no possessive pronoun anywhere, so no possessor binds to an object. W6: the second clause states the other read, never restates the first. W10: the face carries no negated opener. THE ONE JUDGMENT RECORDED FOR VETO: the numbered line opens on the `proper`-typed slot. It is licensed three ways — the chair's own cure note ("the numbered [ledger] line … so this line may"); MOVE-GRAMMAR wall 10, which counts VARIANTS and finds variant 1's numbered line opening on "Above", so this pool has exactly one and no two adjacent; and the projector, whose T-F8 refusal in `scripts/lib/dossier-annex-grammar.mjs:693` runs inside `assertFaces` over the `[face]` sub-rows only and never over a parent row (CONFIRMED by reading the function). The same block's `UNWALLED-SMALL` variant 1 ships a numbered line opening on `{settlement}`. EAR NOTE, information only: "measures" shares a lemma with `UNWALLED-SMALL`'s shipped "The measure of {settlement} stops at a village" — a different word form in a different construction in a mutually exclusive key, and not one of the phrases the skeleton's sibling-distance list fences.

- **variant 2, face 2** — cured. Was: *Ranked a town or larger, {settlement} holds no circuit.* Now: *No circuit bounds {settlement}. The town is carried no lower than a town's rank.* THE FINDING CURED (A5 and the four-faces rule; A11; the skeleton's carried finding; the dropped-angle regression): the old face was the spine's frame to the word class — a fronted band adjunct, a comma, the slot, a one-syllable verb, "no", the wall-noun — with only the vocabulary moved, so an unweighted roll could hand a reader the same sentence twice under two tags. The cure changes all three of the axes the refuter named, not one: a different SUBJECT ("No circuit", then the town-anaphor, against the spine's `{settlement}`), a different ORDER (the absence first and the band last, against every sibling's band-first), and a different LANDING NOUN (the band, against the spine's wall-noun). The two-sentence shape is a construction no other face of variant 2 carries. THE CLAIMS, each with its law: C1 — "No circuit bounds {settlement}" is the wall inside its own negation, flat and standing (the card's `may claim`; the same shape the pool's own passed face "No wall encloses {settlement}" carries); "bounds" names the absent work's function and is literal, not a figure (R-DA-11), and it is fresh against the block's "keeps", "runs round", "encloses", "stands", "carries" — and off `UNWALLED-SMALL`'s "closes", which A11 sibling distance fences. C2 — "carried no lower than a town's rank" states the band by its floor, a marker (W3) and a measurement in words (R-DA-11) on the ordered tier partition (`SMALL_TIERS` / `TOWN_PLUS_TIERS`), and it spends a marker no sibling uses ("or better", "and better", "and beyond", "or above" all stand elsewhere in the pool). THE ANGLE: "is carried" is the office's own formula, which R-vi rules the ledger's lawful SURFACE ("entered", "on the roll", "carried standing"); no roll, book, record or holder is named as an agent-source, so the citation count stays zero (§24; W8: the ledger enters and measures, and cites nothing). NOTHING ADDED: no purse, no cause, no history, no count, no maxim, no hook. FORM: two sentences, no joint, no digit, no em dash, no exclamation, no question, no semicolon, no "which", no `{defwork}`; the face opens on "No", not on the proper-typed slot (T-F8 satisfied). THE THREAD AT k = 0 (R-i): sentence two carries the town forward from sentence one as a noun, not a pronoun, so the passage connects regardless of what the composer seats after it; it closes on the band, which the skeleton names as a lawful hand-forward ("the town or its weight"). THE ABSENCE OPENER: lawful under R-ii — a negated surface on this pool's own PRESENT read is not an ABSENCE opener under MOVE-GRAMMAR wall 3 — and W10's cap is met, this being variant 2's only negated opener (the pool's other one is variant 1's face 2, one per variant). W1: no possessive pronoun. W6: sentence two states the other read and restates nothing. W4: the variant's declared grammar (the two reads in the ledger's stance, PRESENT with the band) is kept; the construction moves, the grammar does not. EAR NOTE, information only: "The town … a town's rank" sets this town beside the band word in seven words; the record's own way of saying it, and the two referents do not collide on the ear.
