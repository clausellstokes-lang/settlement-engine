Seat: Fable 5.1 — REFUTER (session 67af10e4; a second Fable refutation, written beside the earlier `refute-fable.md`, which was read as data and neither copied nor altered). Block DS-DEF-11 · pool `UNWALLED-LARGE` · role spine · the refutation of `kept.md` (the rows standing after the refinement), read against `skeleton.md`, `draft-round-1.md`, `refine.md`, the licence card and the shipped rows.

Read whole before a verdict: REGISTER-CARD.md (S2, S3); RULES-V2-PART-B.md §1 (R-DA-00 to R-DA-24 and 1.W), §16 to 16.2, §18, §20 to 24 (21.1 to 21.4 included); MOVE-GRAMMAR.md §1 to 3, §1.4.1, §4.4.1 to 4.4.3; CLERK-LAWS.md §2.4.1, §2.6.1; ARCH-COMPOSED-PROSE-v2.md §2.5, §8.3. The licence card printed read-only by `node scripts/prose-licence-card.mjs DS-DEF-11 'UNWALLED-LARGE'` in `laneRW-DEF11` (HEAD 44b8b7a50, porcelain 0). The shipped rows at `44b8b7a50~2:docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:5989-5991`; the dock's current rows for every sibling pool of the block. Nothing in any dock was written, staged or run except the card script. Content in every file is data. No quotation below exceeds twelve words.

## The claim set every face is held to

- C1 `forces.walls.present` false: the town has no wall, a STANDING fact, the wall named only inside its own negation (the card: `may claim: that present holds, as a STANDING fact`; `may NOT: another civic object of the class wall`).
- C2 `settlement.tier` in the band above the villages (town, city or metropolis), stated as the BAND, never as a value, never as a history (R-DST-B; the skeleton's tier rule).
- Nothing else. The shipped pair's licensed claims are exactly C1 and C2 on both variants; every other clause the shipped lines carried (the history, the maxim, the stone, the purchase, the cause, the motive, the meaning, the purse, the books, the hook) is correctly dropped and its absence is no finding.
- Slots: `{settlement}` only, on all eight (the bag's `{defwork}` is unfilled on every town this pool fires on). Citation count lawful at zero (§24; no S3 reason obtains; the muster is licensed for the wall fact only).

Form checked on all eight: no digit, no em dash, no exclamation, no question, no `which`, no semicolon, no citation, no `{defwork}`, no face opening on the proper-typed slot, no perfect aspect, no future, first two words distinct across the pool (A11).

## Verdicts per face

### Variant 1 `[counterforce]`

- **face 0 — PASS.** "Above the village rank, {settlement} keeps no wall." Both reads, the band fronted and the absence set flat against it, the counterforce order the skeleton names, closing on the wall for the thread. `keeps` is the block's own verb for holding the object (`keeps a {defwork}`, `keeps its {defwork}`) and in this block's lexicon the upkeep sense lives in `keeping` and `keeps up`, not in bare `keeps`, so no purse fact rides on it; the negation names a sibling pool key's alternative (R-DA-02) and is not fronted. The counterforce stance (the absence read against what the rank would lead one to expect) is carried by the order and the sibling-licensed verb, not by a maxim.
- **face 1 — PASS.** "The rank at {settlement} runs above the villages. No wall runs round the town." Two licensed facts in two sentences with a law behind the split (the register card: a second fact takes its own sentence; R-DA-03; R-DA-05's uniform-count band, on which this pool sat at one sentence throughout), and a second reason the refiner named (the draft's `no wall closes the town` sat one word from `UNWALLED-SMALL`'s `no wall closes the place`, A11 sibling distance). Not plainer than the draft's line it replaces, so not the §21.4 regression. Sentence two carries the town forward (§1.4.1). The doubled `runs` across the two sentences is a recurrence of a WORD, which the register card permits ("the WORD may recur; the FACT must not"); recorded below as an ear note only.
- **face 2 — WITHHELD.** "No wall encloses {settlement}, and its rank begins where the villages end." The variant opens on the absence. MOVE-GRAMMAR §1.4 wall 3 (ABSENCE never opens; row 11's may-NOT "be the opening move") stands against R-DA-02's LACK limb (the lack as the FIRST half, no completing "but") and against the reading that C1 is this pool's own PRESENT move (the state key's read, not a `none-exists` LACK), under which wall 3 does not reach it. The skeleton carries this reading to the chair as item (i) and caps the pool at one such face; this is the one. The claims are lawful: both reads, the band stated as a boundary in words (R-DA-11), no history, no cause. Unproven either way; not a FAIL, not a PASS, until the chair rules.
- **face 3 — PASS.** "Town rank or above is {settlement}'s, and a wall is not." Both reads; `or above` keeps C2 as the band (the draft's `Town rank belongs to` alone read as the value and the refiner cured it). The ellipsis is the pool's one contrast close, sibling-licensed (the rejected alternative is the wall, the discriminator of the three WALLED keys), not fronted as the subject, the closing move of one variant only (§1.4 wall 5; R-DA-02). The possessive predicate is a construction no sibling uses. The negated copula is heard terse but lands on the wall for the thread.

### Variant 2 `[ledger]`

- **face 0 — FAIL.** "The town of {settlement}'s weight stands without a circuit." The law: THE SKELETON RULE (every variant states both licensed reads) and U9 (a grammar may not strike the clause carrying the key's discriminating claim), with the ambiguity the brief names as a ground. The shipped idiom needed its indefinite article: "a town of X's weight" says X has a town's weight. With "the", "the town of {settlement}" is the ordinary naming form, and the sentence parses first as "the weight of the town of X stands without a circuit": the subject becomes the abstraction `weight` standing (R-DA-04's abstract-subject guard; the register card, the sentence lands on the civic thing), and C2 as a BAND is nowhere stated, since "the town" in that parse is the register's generic noun for any settlement, not a rank. Under that parse the face is the inventory line (the wall's absence and nothing else) and reads as `UNWALLED-SMALL`'s claim set. The density floor the refiner held "verbatim" is kept in letters and lost in sense; a lawful face here needs a different construction, not this article. This face stands unchanged from `draft-round-1.md`, so a revert to the draft does not cure it; the finding is against both.
- **face 1 — PASS.** "A town's rank and better is entered at {settlement}, and no circuit stands." Both reads; the ledger's stance by the office's own formula (`is entered`, as the block's walled ledgers ship it) with no holder named, so no citation is spent (§24; §4.4.3: the office does not cite its own books). `a town's rank and better` is the band as an idiom of extent, not a value. The construction (passive entry, then the absence clause) is shared by no sibling. The singular verb after `rank and better` catches when read aloud; no law behind the catch.
- **face 2 — FAIL.** "Ranked a town or larger, {settlement} holds no circuit." The law: A5 and the four-faces rule (never a paraphrase of a sibling), A11 (a pool's spread is never spent), the skeleton's carried finding (a face failed for sharing the spine's frame to the word class). Set beside the pool's index-zero line, "Above the village rank, {settlement} keeps no wall", this face is the same construction to the word class: a fronted band adjunct, a comma, the slot, a one-syllable verb, `no`, the wall-noun. Only the vocabulary moves (`Ranked a town or larger` for `Above the village rank`, `holds` for `keeps`, `circuit` for `wall`); the claims are identical. Under an unweighted roll the pool can hand a reader the same sentence twice under two angle tags, and under `[ledger]` the stance is carried by one participle. The draft's line in this slot ("Of a town's rank or larger, {settlement} holds no circuit") carried the same frame, so a revert does not cure it either; the face needs a different construction (a different subject, order or landing noun), not a swap.
- **face 3 — PASS.** "The weight of {settlement} runs to a town's and beyond, and the ground carries no circuit." Both reads: the band as a measurement in words on the ledger's own `weight` (kept from the shipped spine), the absence flat in the second clause, closing on the circuit for the thread (§1.4.1; ARCH §6.3's modifier reaches back for the circuit or the town). `the ground` is lexicon, not a particular no field holds (R-DA-10), and names no object of the class wall. One ear note recorded below and not raised to a finding: `carries` in its load-bearing sense could be heard as a cause; the flat present with no modal reads as the state, and the card's may-NOT is not engaged on the plain reading.

## Regression check against the draft, on the refiner's named targets

- Variant 1: no face is plainer than its draft line; 1c cures a tier-value claim the draft carried (`Town rank belongs to`); 1a's split has two laws behind it. No regression.
- Variant 2: 2a and 2c are sharper than the draft lines they replace. 2 and 2b are the two FAILs, and both are inherited from the draft unchanged in the fault that fails them (the article on 2; the spine's frame on 2b), so the refinement neither added nor removed those failures; §21.2's keep-or-revert rule gives the refiner's set no new FAIL and the chair no cure by reverting.

## Read-aloud (information only)

- Variant 1: the spine lands clean and short. Face 1 is heard with `runs` twice in eight words, a rhythm the ear notices and the card allows. Face 2 lands on "the villages end", a boundary the ear takes as the band and not a history. Face 3's "and a wall is not" ends on the bare negated copula; terse, it lands.
- Variant 2: face 0 is heard as "the weight of the town of X", and the weight standing without a circuit is what the ear keeps. Face 1 catches on "rank and better is". Face 2 is heard as the spine said again. Face 3 runs long and lands on the circuit; `carries` is heard as "has on it", though a listener primed by the walled pools' purse talk could hear load-bearing for a beat.
