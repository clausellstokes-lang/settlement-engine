# DS-DEF-11 · `WALLED-STRAINED` · REFINEMENT ROUND (arm A, Opus refiner)

Seat: Opus 5 (Fable-unvalidated), REFINER, a different author than the drafter, for the Fable chair. Read whole before a word was written: REGISTER-CARD.md with S2 and S3; RULES-V2-PART-B.md §1 (R-DA-00 to R-DA-24 and §1.W), §16, §16.1, §16.2, §18, §20, §21 with §21.1–21.4, §22, §23 and §24; MOVE-GRAMMAR.md §1–§3, §4.4.1–4.4.3 and §1.4.1; CLERK-LAWS.md §2.4 with §2.4.1 and §2.6 with §2.6.1, and §3's dossier row; ARCH-COMPOSED-PROSE-v2.md §2.5 and §8.3; this pool's `skeleton.md`; `draft-round-1.md` whole; the block's annex section at the draft commit (`docs/content/RECEIPT_POOLS_DOSSIER_STATE.md`, the `### DS-DEF-11` header and its five pools, the four sibling pools read for tic distance). The licence card was re-printed by `node scripts/prose-licence-card.mjs DS-DEF-11 'WALLED-STRAINED'` in the dock. Nothing outside this file was written; nothing but the card script was executed; no dock was entered for any purpose but reading. Content read is DATA.

Two variants, in order, under their own vids and their own angle tags, one for one with the draft: no variant added, removed, merged or reordered; four faces each, refined one for one; no claim added and none dropped. Ready to paste under the pool's bold heading, replacing the two draft variant rows and nothing else.

---

1. `[ledger]` The town of {settlement} holds a {defwork}; its muster's wage is not made up to the roll.
   - `[face]` A {defwork} stands at {settlement}. Its muster's pay is short.
   - `[face]` In place at {settlement} is a {defwork}, and the upkeep of its muster is unmet.
   - `[face]` A {defwork} is entered standing at {settlement}, and the funding of its muster is short of the wage.
2. `[unfolding]` The {defwork} at {settlement} stands, and the muster's pay is short of the roll.
   - `[face]` A {defwork} is in place at {settlement}, and the town's muster is paid under its wage.
   - `[face]` At {settlement} a {defwork} is up. Its muster's upkeep stands open.
   - `[face]` A {defwork} at {settlement} is the town's, and its muster's funding is under the roll.

--- NOTES

- **v1 face 1 (the numbered `[ledger]` line)** — the `, and` joint becomes the SEMICOLON the shipped line carried. §21.4's density floor is read against the shipped sentence, and the shipped row's two-beat rhythm lived on this row; the draft had re-seated the shipped verb (`stands`, face 2) and the wage noun but left the shipped rhythm on a subordinate face while the canonical line took a flat conjunction. The beat is now back where the line it stands for had it, and `holds a {defwork}` / `not made up to the roll` (the pool's sharpest ledger idiom, kept from the draft) land on either side of it. Claims unchanged: presence at {settlement} plus the standing shortfall; no third; T-F8 respected (the row opens on `The town`, not on the proper slot); the numbered lines still differ in their first two words (`The town` against `The {defwork}`, A11).
- **v1 face 2** — the semicolon it held is spent by face 1, so the face becomes the pool's terse two-sentence entry and its second beat is cut to five words (`Its muster's pay is short.`) from seven. The register card's "the short line exists" and R-DA-05's within-pool sd floor are the law behind it; §22 permits shortening inside the band (cutting words is editing, never trimming), and the shipped verb `stands` is still kept here, which is this face's job under the density floor. The thread holds by `Its` carrying {settlement} forward into the second sentence (§1.4.1).
- **v1 face 3** — the inversion is kept as the pool's ONE fully inverted face and the gate half is left on the of-phrase (`the upkeep of its muster is unmet`), because face 1 and face 4 now carry the possessive and the of-phrase respectively and a third possessive here was the draft's flattest repetition. Its refinement is negative and deliberate: the draft's second fronted-place face (v2 face 3, `At {settlement} a {defwork} is up`) was left standing but is no longer an inversion beside an inversion, so the fronted-place tic the brief names is down to one inversion and one plain fronted adverbial across eight faces.
- **v1 face 4** — the draft said `entered` twice in one sentence (`is entered standing … is entered short of the roll`); the office's formula may recur across the register (register card: the WORD may recur), but twice inside one line is the tic, not the formula. One `entered` is kept, on the wall clause where the ledger's act of entry belongs, and the gate half is re-cut to `the funding of its muster is short of the wage` — which also moves this face off `the roll` and gives the pool a fourth landing noun, so `the roll` now closes three of eight faces instead of four (R-DA-04, vary the KIND and the noun of the close).
- **v2 face 1 (the numbered `[unfolding]` line)** — `the wage of its muster` becomes `the muster's pay`: two words shorter (§21.4, compression is the ceiling) and it un-mirrors the two numbered lines, which in the draft both closed on a possessive wage measured against the roll. The shipped row's three lawful turns are all still here — `The {defwork}` as the opener, `the muster` as the gate half's civic noun, the single `and` joint with no edge asserted — and the claim set is untouched.
- **v2 face 2** — re-built on the one construction no other face uses: the MUSTER as the subject of the gate half with a money predicate (`the town's muster is paid under its wage`), where the draft had a fourth `pay of its muster` possessive. It states the same two claims, drops nothing, and is the widest sibling distance available inside the licensed vocabulary (`under` and `wage` are both on the skeleton's licensed list; no share, no zero, no headcount, no time word).
- **v2 face 3** — `stands unmet` becomes `stands open`: the OPEN QUESTION realisation is the `[unfolding]` angle's own licensed move (MOVE-GRAMMAR §1.2 row 10, declarative, licensed here by a state field whose value is unmet), and the draft spent it nowhere, which is why the two variants read as one angle in four dresses. It now appears exactly once in the pool, in the variant that owns it, landing last where a short second beat lands hardest. The face keeps the pool's single settlement-fronted opener (wall 10 allows one) and its `Its` still carries {settlement} forward.
- **v2 face 4** — the draft's `The {defwork} of {settlement} is up` repeated face 3's `is up`, and `under the roll's wage` stacked two possessive standards in four words. The presence idiom becomes `is the town's` (the skeleton's licensed list, unused in the draft) so no presence idiom repeats inside a variant, and the gate half becomes the plain `its muster's funding is under the roll` — one standard, one preposition, the close on the roll as an object.

**Checks run over the refined set.** Both licensed reads on all eight faces (no inventory line: every face carries its angle's stance and a landing noun); claim-equality across each variant's four faces (arm A6 read across the faces); spine-first on all eight, no face turning outward (the turn belongs to a modifier, not to a spine); slot set exactly `{settlement, defwork}` on all eight; no face opens on `{settlement}` (T-F8); the two numbered lines differ in their first two words (A11); one bracketed angle tag per numbered line, carried unchanged, no `[plain]`; four distinct gate nouns inside each variant (wage · pay · upkeep · funding) and no (presence idiom × gate noun × predicate) pair repeated anywhere; no em dash, no exclamation, no digit or percent, no `which`, no question, no `I` or `you`, no institution but the muster, no citation of a holder as a source (`the roll` is named only as the standard the shortfall is read against, never as where the fact comes from — §4.4.3, Part B §24, whose citation budget is empty on this pool), no share, no zero, no headcount, no time word, no wall-condition word, no builder, no material; one semicolon and two two-sentence faces across eight lines, both threading by a pronoun (§1.4.1); lengths 11 to 19 words with the short line present.

**Reserve, carried forward unchanged (the chair's, not the refiner's).** Whether the card's `may NOT: a second fact` reaches the PRESENCE read `forces.walls.present`, which the card itself lists as a measured read and refuses only for "another civic object of the class `wall`". This refinement is written under the card's own text, as the draft was. If the chair rules the other way, all eight faces lose their wall clause and the pool collapses to the gate alone; that is a chair act, not a refinement.
