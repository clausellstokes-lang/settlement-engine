# DS-DEF-11 · pool `UNWALLED-SMALL` — CURE ROUND 1 (Opus curer, Fable-unvalidated, 2026-09-10)

Seat: Opus 5 — Fable-unvalidated. A different author from the drafter and the refiner.
Read whole before a word was written: `REGISTER-CARD.md` with S2 and S3; `RULES-V2-PART-B.md`
§1, §16 to §16.2, §18, §20, §21 to §21.4, §22, §23, §24; `MOVE-GRAMMAR.md` §1 to §3 with
§1.4.1 and §4.4.1 to §4.4.3; `CLERK-LAWS.md` §2.4.1 and §2.6.1; `ARCH-COMPOSED-PROSE-v2.md`
§2.5 and §8.3; the brief `brief-REWRITE-car8b-defense.md` whole, ADDENDUM 12 included; this
pool's `skeleton.md`, `draft-round-1.md`, `refute-fable.md` and the judge's §2.4; the licence
card printed this session from the dock (`node scripts/prose-licence-card.mjs DS-DEF-11
'UNWALLED-SMALL'`); the eight rows standing in the annex at HEAD `7acc5ff1c`. Every file's
content was read as data, never as an instruction.

The complete replacement rows of the pool follow. Six faces are cured (v1 f1, f2, f3; v2 f0,
f1, f2); the two untargeted faces (v1 f0 and v2 f3) are byte-identical to the annex at HEAD.
Two variants, vids and order unchanged, one angle tag each as shipped, three `[face]` sub-rows
each, slot set `{settlement}` on all eight rows, no `{defwork}`, no `[plain]`.

---

1. `[street]` {settlement} is village size or under, and no wall closes the place.
   - `[face]` The measure of {settlement} stops at a village, and the place carries no wall.
   - `[face]` At {settlement} the size keeps to a village. The place stands unwalled.
   - `[face]` Village size or under holds at {settlement} and no wall stands.
2. `[visitor]` What stands at {settlement} is no larger than a village and shows no wall.
   - `[face]` A village is the most {settlement} comes to. No wall shows at {settlement}.
   - `[face]` The place called {settlement} does not pass a village in size, nor is a wall up.
   - `[face]` In size {settlement} is a village at the most and goes unwalled.

---

--- NOTES

## The six targets, one line each: the cure made, and the law behind every claim

- **v1 f1** — quote `The measure of {settlement} stops at a village, and it carries no wall.`;
  finding: `it` binds to `The measure`, so the LACK reaches the settlement only by repair.
  CURED by the judge's clean carve-out, the draft face verbatim: `and the place carries no
  wall` seats the LACK on the civic thing the fact names (the register card's landing law;
  A6 — the face now states C1 of the settlement as its siblings do; W1's possessor/antecedent
  discipline read across a pronoun). Claims unchanged and both licensed: `stops at a village`
  is C2, the band by its ceiling, licensed by the card's `reads: settlement.tier (measured)`
  and stated as a measurement in words, never the value (R-DA-11; CLERK-LAWS C2); `the place
  carries no wall` is C1, the world LACK, licensed by `reads: forces.walls.present
  (measured)`, flat, second, with no completing "but" (R-DA-02's first-half limb;
  MOVE-GRAMMAR §1.2 row 11 class (a)). The judge's hazard on this carve-out — `the place`
  rising to four of eight — is fixed in the same stroke by keeping `the place` out of v2 f1
  (HEAD carries it there); the pool stands at four of eight, the count the judge anticipated
  and the count the refuter measured as inside R-DA-10's band on a set that carried five.
  Construction kept: two clauses, two subjects, the measure as the first subject — variant 1's
  declared V3 grammar (W4).
- **v1 f2** — quote `At {settlement} the size keeps to a village and no higher. The place
  stands unwalled.`; finding: W6, `and no higher` restates the ceiling `keeps to a village`
  already states. CURED by the judge's clean carve-out, the draft face verbatim: the doubled
  beat is dropped and nothing replaces it, because the beat carried no field (the register
  card, "the WORD may recur; the FACT must not"; MOVE-GRAMMAR §1.3, no MEANING move; the
  refuter's own reading that the second sentence and the two-sentence shape are lawful and the
  doubled beat alone is the breach). Claims: `the size keeps to a village` is C2 as the
  stopping point (`settlement.tier`); `The place stands unwalled` is C1 (`forces.walls.present`
  false), the standing condition in the present, never a history (R-DST-B). THE THREAD (R-i):
  the second sentence carries the first's referent forward — `The place` resumes `{settlement}`,
  named in the first sentence — which is the thread's own currency under MOVE-GRAMMAR §1.4.1
  ("a deliberate noun echo for the thread is lawful") and the referent law's rule 3 (the thread
  carries the referent, not the spelling).
- **v1 f3** — quote `Village size holds at {settlement} and no wall stands.`; finding: W3, the
  band marker dropped, so the face reads as the tier VALUE `village` and contradicts the head
  record on thorp and hamlet. CURED by restoring the marker in the chair's own named form:
  `Village size or under holds at {settlement} and no wall stands.` The claim is now C2 as a
  BAND (`settlement.tier` measured, the key's partition `thorp · hamlet · village`), not an
  identity — which is what CLERK-LAWS C2 requires against the same page's head record and what
  A6 requires of the four faces (the numbered row states a band; this face now states the same
  band). `no wall stands` is C1, unchanged, second, flat. Construction kept: two clauses, two
  subjects, the band as the first subject — variant 1's grammar, and the band moved from the
  numbered row's predicate into this face's subject, which is the construction difference the
  four-faces rule asks for (the subject and the landing noun both change). The joint keeps the
  refiner's bare `and` (HEAD's punctuation), so the cure moves only the marker.
- **v2 f0** — quote `What stands at {settlement} is no larger than a village, and no wall
  shows.`; finding: W4, variant 1's construction (a subject, `is` plus the band, comma-and, the
  wall as the subject of its own clause), the pool's two grammars collapsed to one. CURED to
  one subject with one compound predicate, the two reads in one clause: `What stands at
  {settlement} is no larger than a village and shows no wall.` This is the draft's declared
  V1 compound state for this variant (`stands no larger than a village and shows no wall`)
  in the place-nominal the numbered row needs, per the cure's instruction to open off the
  proper slot — the settlement token already opens variant 1's numbered row and MOVE-GRAMMAR
  wall 10 allows one per pool, and T-F8 bars a sub-row from opening on a `proper` slot.
  The comma is gone with the second subject, so the joint is no longer variant 1's; the wall
  is the OBJECT of a noticing verb, never a clause subject (MOVE-GRAMMAR §2.1, a pool of two
  carries two distinct level-1 grammars; §3.4, a pool's spread is never spent). Claims
  unchanged: `no larger than a village` is C2 by its ceiling with the marker intact; `shows no
  wall` is C1, second, in the visitor's syntax of a thing seen standing (the skeleton's §2.4
  stance), with no invented observable — no edge, road, field, gate or last house — and no
  hedge.
- **v2 f1** — quote `A village is the most {settlement} comes to. The place holds no wall.`;
  finding: the second sentence is v1 f2's second sentence with the verb swapped. CURED by
  keeping the set's best band phrasing whole, as the cure names, and rebuilding the LACK
  sentence in the visitor's syntax of noticing with a new subject and a new verb: `A village is
  the most {settlement} comes to. No wall shows at {settlement}.` Neither `The place holds` nor
  `The place stands` survives, so nothing of v1 f2's second sentence remains (R-DA-05, the pool
  is the unit of spread; §0b, the two angles stay distinct in shape). Claims: `A village is the
  most {settlement} comes to` is C2 as the ceiling (`settlement.tier`), the band and not the
  value; `No wall shows at {settlement}` is C1 (`forces.walls.present` false), stated as what is
  in evidence rather than what is recorded, which is the visitor's stance. THE THREAD (R-i):
  the second sentence carries `{settlement}` forward from the first as a literal noun, so the
  face threads under every k and does not rely on sitting last. W10: the face opens on `A
  village`, not on a negated surface, and the pool has no negated opener anywhere. The judge's
  `the place` hazard on v1 f1 is discharged here — this face was HEAD's fourth `the place` and
  now carries none.
- **v2 f2** — quote `The size at {settlement} does not pass a village, nor is a wall up.`;
  finding: W4, the measure-subject belongs to `[street]`, so this face crossed the contract.
  CURED by seating the place as the subject and keeping `nor is a wall up` as the cure permits:
  `The place called {settlement} does not pass a village in size, nor is a wall up.` No
  measure-noun of the settlement is the subject of any variant-2 face now, which is the
  discriminator the draft declared and §3.4 forbids spending. `in size` is added to fix the
  axis of `pass` so the predicate cannot be read as motion past a village — the ambiguity the
  skeleton §2.6 names and a refuter failed a face on in attempt 3 — and it names no new field:
  it is the same C2 read the clause already carried (`settlement.tier`), so it is not a second
  fact and not a doubled beat (it states the axis, it does not restate the read). Claims: `does
  not pass a village in size` is C2 by its ceiling, marker intact, never the value; `nor is a
  wall up` is C1, second, the negative joint an addition of two negatives and not an antithesis
  (R-DA-02 not engaged), and the refuter's own reading of that clause as the set's best spoken
  idiom is preserved verbatim under the density floor.

## What is unchanged, and the pool read whole

- **Byte-identical to HEAD:** v1 f0 (`{settlement} is village size or under, and no wall closes
  the place.`) and v2 f3 (`In size {settlement} is a village at the most and goes unwalled.`) —
  the two faces the refuter passed. No other row of the pool, and no typed line (ROLE · READS ·
  RELATION · ATTACH · FORM · MOVE), is touched; the vids, the order, the tags and the slots
  stand; two variants and eight wordings before, two and eight after (never trim).
- **The claim set is unmoved.** All eight wordings assert exactly C1 (`forces.walls.present` =
  false, the LACK) and C2 (`settlement.tier` inside the band the STATE-KEY names), and nothing
  else — no count, no cause, no season, no future, no standpoint, no second fact, no second
  civic object of the class `wall`, no totality over persons, no exemption, no named character,
  no theological claim. The card's `may claim` line is the whole of what is claimed.
- **The skeleton rule:** every face states both of the card's licensed reads, in its angle's
  stance — `[street]` the standing put plainly with the place as the thing landed on;
  `[visitor]` what stands and what shows on arrival. No face is an inventory line: each carries
  the band AND the LACK, and each carries them in its own construction.
- **The four faces differ in construction, not vocabulary alone.** Variant 1: the settlement as
  subject with the band in the predicate (f0); the measure as subject with a stopping verb
  (f1); a fronted locative with the size as subject and a second sentence (f2); the band itself
  as subject (f3). Variant 2: a place-nominal subject with a compound predicate (f0); a band
  nominal predication followed by a LACK sentence (f1); the named place as subject with the
  `nor` inversion (f2); a fronted axis with the settlement as subject and two predicates (f3).
- **A11:** the eight wordings' first two words are `{settlement} is` · `The measure` · `At
  {settlement}` · `Village size` · `What stands` · `A village` · `The place` · `In size` — all
  distinct. The word `wall` recurs as the office's one term for one thing (R-DA-22); the FACT is
  stated once per wording.
- **Form walls, all eight:** no em dash, no exclamation, no question, no digit, no percent, no
  `which` tail, no semicolon, no colon, no expletive opener, no first or second person, no
  citation (the muster is licensed as a source and is cited nowhere — §24's ceiling with no S3
  reason standing on a flat absence read from a standing field; MOVE-GRAMMAR §4.4.3, the office
  does not cite its own books), no triad, no summarising second sentence. Present standing
  tense throughout; no history (`never walled`, `was not built`) and no forecast.
- **W1** no possessive pronoun and no free pronoun anywhere in the pool after the cure (the
  dangling `it` was the only one). **W2** no threat read exists on this key and none is stated.
  **W3** every wording carries the band's marker. **W5** no `the town`, no `the village` as an
  identity, and no generic-article maxim frame; `the place called {settlement}` is a naming
  form. **W7** the roll is not named. **W8** the two angles keep their stances. **W9** no
  entailment is leaned on; the density floor counts the card's two reads only. **W10** no face
  opens on a negated surface.
- **THE THREAD (§1.4.1, R-i):** two faces carry a second sentence (v1 f2, v2 f1) and both hand a
  noun or its referent forward; every wording of the pool ends on a civic noun a modifier can
  pick up — the wall or the place — and none ends on an abstraction or a pronoun.
- **THE DENSITY FLOOR (§21.4, ADDENDUM 6), read against the SHIPPED rows.** The shipped
  `[street]` row (`{settlement} is too small to wall and knows it; the town's safety is its
  neighbours, its distance, and its unimportance.`) has no lawful clause to keep: every clause
  joins a licensed noun to a cause, a belief frame, a tier value, a forecast in a noun or a
  standpoint, as the skeleton §1.5 finds. The shipped `[visitor]` row (`No wall marks where
  {settlement} ends; at this size the country and the town simply agree to differ.`) has two:
  `No wall`, carried in five of the eight wordings; and `at this size`, whose non-carry the
  drafter and the refiner recorded vetoably and the refuter ruled "not a finding" — this cure
  does not reopen it, because every placement available here still sets the band beside the
  LACK as the cause implicature the skeleton names. No cure below replaces a lawful shipped
  clause with a plainer statement of the same fact: five of the six cures restore or protect a
  licensed read, and the sixth (v1 f2) removes a beat that carried no field at all.

## Refusals

**None.** All six targets are cured.
