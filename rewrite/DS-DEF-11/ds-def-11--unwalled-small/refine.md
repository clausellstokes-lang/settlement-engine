# DS-DEF-11 · pool `UNWALLED-SMALL` — REFINEMENT ROUND (Opus refiner, Fable-unvalidated, 2026-09-09)

Arm A of the taste (Opus drafts, Opus refines). One-for-one over `draft-round-1.md`: two variants, eight
wordings, same vids, same order, same angle tags, same slot set `{settlement}` on every row, no `{defwork}`,
no `[plain]`. Nothing added, nothing dropped, no claim added, no claim removed. The claim set of every
wording is exactly {C1 the wall LACK, C2 the tier band} as the licence card licenses them.

---

1. `[street]` {settlement} is village size or under, and no wall closes the place.
   - `[face]` The measure of {settlement} stops at a village, and it carries no wall.
   - `[face]` At {settlement} the size keeps to a village and no higher. The place stands unwalled.
   - `[face]` Village size holds at {settlement} and no wall stands.
2. `[visitor]` What stands at {settlement} is no larger than a village, and no wall shows.
   - `[face]` A village is the most {settlement} comes to. The place holds no wall.
   - `[face]` The size at {settlement} does not pass a village, nor is a wall up.
   - `[face]` In size {settlement} is a village at the most and goes unwalled.

---

--- NOTES

**1 · `[street]` numbered row — HELD, verbatim.** The draft's canonical line is already at the ceiling this
pool allows: the band as a bare ceiling (`village size or under`), the LACK in the record's own formula
(`no wall closes the place`) with the wall as the agent of the closing, the pool's one settlement-token
opener (wall 10), a close of kind OBJECT that hands `the place` forward to a modifier (§1.4.1, the thread).
Refining it would have been change for its own sake, which §21.4 names the regression; index 0 is the line
a falsy seed draws and the one the canonical projection keeps live, so it is where holding costs least.

**2 · `[street]` face 1 — the landing noun moved off `the place`.** Was `and the place carries no wall`;
now `and it carries no wall`, so the sentence lands on `wall` and the pool's rate of `the place` falls from
seven of eight (the draft) to three of eight. Target: R-DA-10's rationed-pet-word ceiling and R-DA-22 read
across the pool, not inside one line; the pronoun is a mid-sentence subject and never a closer, so
R-DA-04's `closers.pronounRate` is untouched.

**3 · `[street]` face 2 — the band given a second beat and the sentence lengthened.** Was `the size keeps
to a village. The place stands unwalled`; now `keeps to a village and no higher`. `and no higher` restates
the SAME read as its ceiling, so no claim is added (the triple is unchanged); it buys the pool its longest
wording and a falling cadence before the flat second sentence. Target: R-DA-05's within-pool spread and
§21.1's ceiling, taken without padding, since the added words are the band and not a second fact.

**4 · `[street]` face 3 — the comma struck from the joint.** Was `at {settlement}, and no wall stands`; now
`at {settlement} and no wall stands`. Nine words, the pool's short line, and the only bare-`and` joint in
variant 1. Target: R-DA-06 (the joint is chosen, never habitual) and the draft's real tic, which was eight
wordings joined the same way; the pool now carries four joint kinds.

**5 · `[visitor]` numbered row — `The whole of` struck; the LACK re-verbed.** Was `The whole of {settlement}
stands no larger than a village and shows no wall`; now `What stands at {settlement} is no larger than a
village, and no wall shows`. `The whole of` was flab that leaned on an extent the fields do not hold (the
card's may-NOT, a second fact); `what stands` is the visitor's own subject and asserts nothing past the two
reads. The intransitive `no wall shows` puts the wall in the subject seat, which no other wording of the
pool does. Target: §21.4's density law read the other way, a sharper licensed fact in the same breath.

**6 · `[visitor]` face 1 — rebuilt as two sentences on a ceiling nominal.** Was `In {settlement} the place
is of village size and stands without a wall`, whose opener read as `in the place, the place`; now `A
village is the most {settlement} comes to. The place holds no wall.` The band is a ceiling nominal and
never an identity (`the most ... comes to`, not `is a village`), which is the CLERK-LAWS C2 hazard the
skeleton names; `holds no wall` is the block's own shipped verb turned negative (`{settlement} holds a
{defwork}` in `WALLED-STRAINED`), so the pool speaks the block's vocabulary.

**7 · `[visitor]` face 2 — the joint changed to `nor`.** Was `does not pass a village, and the place is
unwalled`; now `does not pass a village, nor is a wall up`. `nor` asserts the addition of two negatives,
never a rejected alternative, so R-DA-02's antithesis shape is not engaged; it is the pool's fourth joint
kind and the record's own cadence. `is a wall up` is the negative of `a {defwork} is up`, again the block's
vocabulary. VETOABLE: this is the one device in the set a shape-regex refuter could mis-key as a contrast;
the fallback, if the chair strikes it, is the period form `does not pass a village. The place stands
without a wall.` with no other row disturbed.

**8 · `[visitor]` face 3 — the naming frame struck.** Was `The place called {settlement} is a village at
most and goes unwalled`; now `In size {settlement} is a village at the most and goes unwalled`. `The place
called` was three words of frame carrying no fact and the pool's fourth `the place`; `In size` fronts the
AXIS, never the tier value, so it is not the fronted nominal absolute that fired rule 4 at attempt 3, and
the face does not open on a `proper`-typed slot (T-F8). `at the most` over `at most` for the ear.

**Set level, reported not asserted, all vetoable.**
- **`at this size` is still not carried, and the refiner re-derived the decision rather than inheriting
  it.** The skeleton rules the phrase lawful only mid-sentence or trailing. Every trailing placement puts
  the band after the absence and so makes the LACK the variant's opening move, which is the chair's open
  ruling (i); every fronted placement is the cause implicature of ruling (ii); the parenthetical placement
  (`{settlement}, at this size, carries no wall`) reads as the same implicature with commas. The band is
  carried instead by eight distinct measurements in words, each a ceiling or a stopping point, so nothing
  licensed is lost and the density floor is met by sharpness rather than by the shipped words.
- **What the refinement actually moved:** joint kinds one to four (comma-and, bare and, period, nor);
  `the place` seven of eight to three of eight; two-sentence wordings one to two; the wall in the subject
  seat zero to two; eight distinct band phrasings and eight distinct LACK phrasings held, with no two of
  the eight sharing their first two words.
- **What it did not move:** the word-count spread. Lengths are twelve, thirteen, fifteen, nine, fourteen,
  thirteen, fourteen, twelve, an sd near one and seven tenths against R-DA-05's within-pool floor of four.
  The pool carries exactly two flat standing reads and the only ways to lengthen a wording further are a
  second fact, a gloss or a triad, each refused by the card. Reported as a limit of the load, not padded.
- **Zero citations**, as the skeleton recommends: the card licenses the muster as a source, but none of
  S3's three reasons holds for a flat absence read from a standing field, and §24 makes a muster citation
  here a citation habit.
- **Refusals: none.** Every face is lawful under the card as this refiner reads it; if the chair strikes
  the `nor` joint, face 7 reverts to the lawful draft shape and the set stays lawful.
