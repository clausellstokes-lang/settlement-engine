# DS-DEF-11 · pool `UNWALLED-SMALL` — DRAFT ROUND 1 (Opus writer, Fable-unvalidated, 2026-09-09)

Ready to paste under the pool's bold heading, replacing the two numbered rows and nothing else.
The pool's typed lines are untouched and are not repeated here. Two variants, eight wordings
(two numbered rows plus three `[face]` sub-rows each). Slot set `{settlement}` on every row; no
`{defwork}` anywhere; no `[plain]` marker; one bracketed angle tag per numbered row, each the tag
the shipped row carries.

---

1. `[street]` {settlement} is village size or under, and no wall closes the place.
   - `[face]` The measure of {settlement} stops at a village, and the place carries no wall.
   - `[face]` At {settlement} the size keeps to a village. The place stands unwalled.
   - `[face]` Village size holds at {settlement}, and no wall stands.
2. `[visitor]` The whole of {settlement} stands no larger than a village and shows no wall.
   - `[face]` In {settlement} the place is of village size and stands without a wall.
   - `[face]` The size at {settlement} does not pass a village, and the place is unwalled.
   - `[face]` The place called {settlement} is a village at most and goes unwalled.

---

--- NOTES

## 0. The two licensed claims, named once (every citation below is to the printed licence card)

- **C1 — the LACK.** No wall-class work stands at the settlement. Licensed by the card's
  `reads: forces.walls.present (measured)` under `may claim: that present holds, as a STANDING
  fact of the record` — on this key the read is false, so the standing fact is the world LACK
  (MOVE-GRAMMAR §1.2 row 11 class (a); R-DA-02's first-half limb, stated flat, no completing
  "but", no second civic object of the class).
- **C2 — the band.** The settlement's size lies inside the band the STATE-KEY names, village and
  below. Licensed by the card's `reads: settlement.tier (measured)`, stated as the band or its
  ceiling and never as the value (a measurement in words, R-DA-11; the identity claim would
  contradict the head record on two of the three tiers the key fires on, CLERK-LAWS C2).
- **The slot.** `{settlement}` alone, from the card's `bag: {defwork: bare-common, settlement:
  proper}` with `FILLED at this block's call sites: {settlement}`. No wording carries `{defwork}`,
  because this pool exists only where the wall-class row is empty (annex SLOTS line; ARCH §2.5
  refuses a face whose slot set differs from its parent's).
- **Nothing else is claimed anywhere below.** The card's `may NOT` list (a count, a cause, a
  season, a future, a standpoint, a second fact, another civic object of the class `wall`) and its
  REFUSED COLUMNS (a totality over persons, an exemption, a named character, a theological claim)
  are unbreached in all eight wordings.
- **Provenance: zero citations in the pool**, as the skeleton recommends. The card's
  `source: muster · standing LICENSED` makes a citation licensable, but no S3 reason holds for a
  flat absence read from a standing field, and §24's ceiling plus MOVE-GRAMMAR §4.4.3 (the office
  does not cite its own books) make a muster citation here a citation habit.

## 1. Variant 1 · `[street]` · grammar V3 (PRESENT band → LACK)

The angle's stance: the standing stated plainly, as a resident would state it, with the record's
own formula allowed and no field holding what the town says, knows, feels or credits its condition
to. Every wording of this variant takes the MEASURE as its subject and the record's flat verbs
(`is`, `stops`, `keeps`, `holds`), which is what tells it apart from variant 2's construction.

| wording | words | claims, each with its licensing clause |
|---|---|---|
| numbered row | 12 | "is village size or under" = C2 (card `reads: settlement.tier`, as the band's ceiling) · "no wall closes the place" = C1 (card `reads: forces.walls.present`, the LACK) · `{settlement}` = the card's bag |
| face 1 | 14 | "stops at a village" = C2 · "the place carries no wall" = C1 · slot from the bag |
| face 2 | 12 | "the size keeps to a village" = C2 · "The place stands unwalled" = C1 · slot from the bag |
| face 3 | 9 | "Village size holds at {settlement}" = C2 · "no wall stands" = C1 · slot from the bag |

- The LACK is second in all four (never the opening move, never sentence-initial), which forecloses
  the chair's open ruling (i) on whether MOVE-GRAMMAR wall 3 reaches the world LACK at no cost.
- The numbered row is the pool's ONE settlement-token opener (wall 10); no sub-row opens on the
  slot (T-F8); no two of the pool's eight wordings share their first two words (A11).
- `no wall closes the place` is the record's formula the skeleton names as lawful for this angle;
  it is written here in the pool's own words, not carried over from the shipped row, because no
  clause of the shipped `[street]` sentence survives whole (every one of them joins a licensed
  noun to an unlicensed claim).

**Dropped from the shipped `[street]` row, and why (dropping these is the rewrite's purpose):**
"too small to wall" (a cause, and a capacity fact the purse side does not read on this key);
"and knows it" (a belief frame, a standpoint, and a totality over persons); "the town's" (a tier
value the band excludes); "safety" (a second fact and a forecast in a noun); "its neighbours"
(a geography move with no neighbour field); "its distance" (a second fact); "its unimportance"
(a standpoint and a verdict); the habitual triad; the semicolon joining a second fact into one
sentence outside S2's consequence exception. The weight each carried is replaced by licensed
specificity: the band stated as a ceiling rather than as the bare word "small", and the LACK
stated in the record's own formula rather than implied by "too small to wall".

## 2. Variant 2 · `[visitor]` · grammar V1 (the compound state, the two reads in one predicate)

The angle's stance: the two things a stranger meets, the size and the missing wall, in the shape of
a thing standing rather than a thing recorded — no invented observable (no edge, road, field, gate
or last house), no hedge ("seems", "to a stranger"), no figure, no land or town that agrees or
intends. Every wording takes the PLACE as its subject and verbs of standing and showing, which is
the construction difference from variant 1.

| wording | words | claims, each with its licensing clause |
|---|---|---|
| numbered row | 14 | "stands no larger than a village" = C2 (card `reads: settlement.tier`, the ceiling form) · "shows no wall" = C1 (card `reads: forces.walls.present`, the LACK; `no wall` kept as the shipped row's lawful two-word noun phrase) · `{settlement}` = the bag |
| face 1 | 13 | "is of village size" = C2 · "stands without a wall" = C1 · slot from the bag |
| face 2 | 14 | "does not pass a village" = C2 · "the place is unwalled" = C1 · slot from the bag |
| face 3 | 12 | "is a village at most" = C2 · "goes unwalled" = C1 · slot from the bag |

- The LACK is second in all four; the numbered row does not open on the slot (variant 1's does, and
  wall 10 allows one per pool) and does not open on the LACK, so the shipped row's opening ABSENCE
  is cured without a ruling.
- No verb of motion or extent is used with the place as object ("runs to", "reaches to", "stretches
  to"), which is the ambiguity attempt 3's refuter failed a face on.

**Dropped from the shipped `[visitor]` row, and why:** "marks where {settlement} ends" (a spatial
fact the fields do not hold, and a second fact); "at this size ... agree to differ" as a join (a
cause by implicature); "the country and the town simply agree to differ" (a figure with inanimate
intent, a totality over persons as one will, a tier value the band excludes, a standpoint in
"simply", and a maxim-shaped closer); the semicolon join. The weight is replaced by the band stated
as a ceiling and the LACK stated as a thing standing.

## 3. The density floor, accounted for line by line

- **`at this size` (the shipped row's one lawful verbatim turn) is NOT carried into any face, by
  decision, and the decision is recorded here so the chair can veto it.** The skeleton rules it
  lawful only mid-sentence or trailing, never comma-fronted; every trailing placement available on
  this pool sets it beside the LACK ("stands unwalled at this size"), which puts the band after the
  absence and reads as the cause implicature the same section names, and which also lifts the LACK
  into the opening assertion while the chair's ruling (i) stands open. The band is carried instead
  by eight sharper measurements in words — the ceiling ("no larger than a village", "a village at
  most", "village size or under"), the stopping point ("stops at a village", "keeps to a village",
  "does not pass a village") — which §1.5 names as the ceiling the shipped "small" was reaching
  for. Nothing licensed is lost: every wording states the band.
- **`No wall` is carried**, lowercase and mid-sentence, in the `[visitor]` numbered row ("shows no
  wall") and in three other wordings.
- No shipped clause is replaced by a plainer statement of the same fact: the only clauses replaced
  are the unlicensed ones, and each is replaced by a licensed measurement, never by nothing and
  never by a flatter word for what the old row already said lawfully.

## 4. Measured properties of the set (reported, not asserted as gate passes)

- Variants 2 · wordings 8 · faces per variant 4 (one numbered row plus three `[face]` sub-rows).
- Word counts: 12 · 14 · 12 · 9 (variant 1) and 14 · 13 · 14 · 12 (variant 2); range 9 to 15,
  which is a narrower within-pool spread than R-DA-05's sd floor asks. **This is reported as a
  limit of the pool's load, not cured by padding:** the pool carries exactly two flat standing
  reads, and the only ways to lengthen a wording here are a second fact, a gloss or a triad, each
  of which the card refuses. The short line (nine words) and the two-sentence wording carry what
  variance the load allows.
- Close kinds across the eight: object ("the place") ×1, absence ("no wall" as the landing) ×4,
  condition ("unwalled") ×3. The pool does not always end on the wall, and no kind is uniform.
- Eight distinct band phrasings and eight distinct LACK phrasings; no two wordings share their
  first two words; the word "wall" recurs as the office's own term for one thing (R-DA-22), while
  the FACT is stated once per wording.
- Form walls held across all eight: no em dash, no exclamation, no question, no digit, no percent,
  no `which` tail, no expletive opener, no first or second person, no citation, no triad, no
  summarising second sentence, no semicolon, no colon.
- Siblings (arms A1 and A11): nothing here restates or contradicts `WALLED-THREATENED`,
  `WALLED-QUIET`, `WALLED-STRAINED` or `UNWALLED-LARGE`. No wording says a size buys or fails to
  buy anything (that expectation is `UNWALLED-LARGE`'s `[counterforce]` arm), and the words
  "stone", "circuit", "openness", "muster", "watch", "threat" and "upkeep" appear nowhere.
- The thread (§1.4.1): this pool is the spine of its composed unit, so every wording ends on a
  civic noun a modifier can pick up — the place or the wall — and none ends on an abstraction.

## 5. Refusals

**None.** Both variants are written and both are lawful under the card as this writer reads it.
Two items are recorded above as decisions rather than refusals, and both are vetoable: the
non-carry of `at this size` (§3) and the narrow word-count spread (§4).
