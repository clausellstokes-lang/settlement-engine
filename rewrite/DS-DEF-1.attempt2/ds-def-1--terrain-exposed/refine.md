Seat: Opus 5 — Fable-unvalidated (the REFINER; a different author than the drafter, per §23 arm A). Block `DS-DEF-1` · pool key `terrain EXPOSED` · refinement round over `draft-round-1.md` (the committed lawful draft; no `draft-round-2.md` exists in the packet).
Three variants in, three variants out; same vids, same order, same angle tags; three face sub-rows each. One-for-one substitution, nothing added, nothing removed (§22 e).
The rows below are the complete replacement for the pool's variant rows, ready to paste under the pool's heading.

1. `[visitor]` The approaches to {settlement} lie open, and the ground they cross carries no part of the defense.
   - `[face]` Open country comes up to {settlement} on every side. A defense takes nothing from such ground.
   - `[face]` No ground about {settlement} serves a defense. The country is level to the edge of the town.
   - `[face]` An unbroken horizon rings {settlement}. The ground beneath the town holds nothing a defense can use.
2. `[ledger]` The ground at {settlement} counts for nothing in the defense of the town.
   - `[face]` The defense of {settlement} owes nothing to its ground.
   - `[face]` Nothing in the site of {settlement} tells in the town's defense.
   - `[face]` In any account of the defense at {settlement}, the ground does not appear.
3. `[street]` No hill stands over {settlement}, and no narrows pinch the approach. The ground gives the town no cover.
   - `[face]` Nothing commands the way into {settlement}. The ground takes no part in the defense.
   - `[face]` The land at {settlement} shows no rise and no narrows, and the defense takes nothing from the ground.
   - `[face]` The town has no height to hold, and the site {settlement} stands on does not enter the defense.

--- NOTES

**The card, re-read before a word was written** (`node scripts/prose-licence-card.mjs DS-DEF-1 'terrain EXPOSED'`, run in `laneRW-DEF1`; printed verbatim as the draft recorded it). The two licensed claims are unchanged from the draft and are the whole claim set of this pool: **K1** the site's ground is open and level (`predicate: text(terrain) === Plains`), **K2** that ground bears no part of the defense (`reads: TERRAIN_DEFENCE_OF`). `may NOT: a count, a cause, a season, a future, a standpoint, a second fact`. `source: (none) · standing SOURCE-UNRESOLVED` — **no citation is licensed, so no face names a holder** (arm A13; §24's ceiling never engaged). Bag `{settlement}` only.

**What this round did NOT touch.** No claim moved: variants 1 and 3 carry K1 and K2, variant 2 carries K2 alone (its shipped `[ledger]` sentence named no landform, and adding K1 there would be adding a claim). The four faces of each variant remain claim-equal to each other (arm A6). The dropped claims of the shipped rows stay dropped and none returned: the town's works and their standing obligation (v1), the paid-for-advantage BILL and its continuing payment (v2), the town's standpoint and the life-general closer (v3).

**The three faults the round was aimed at**, all pool-level, all measured on the draft:
- **The comma-and tic.** 6 of 12 draft wordings joined K1 to K2 with a comma and `and` (1p, 1f1, 1f3, 3f1, 3f2, 3f3). Now **3 of 12** (1p, 3f2, 3f3); five faces are the clerk's two strokes and four are single uncompounded sentences. The one further `, and` in the pool (3p) joins two lacks inside K1, never K1 to K2. R-DA-05 (rhythm follows load; the pool is the unit of spread) and §21.1 (the ceiling, not the middle).
- **The ledger-closer habit.** 5 of 12 draft wordings closed on `the defense`. Now **4 of 12**, and the close KINDS are spread across R-DA-04's closed set: a civic object (town, ground, site), an absence (`no cover`, `does not appear`), a condition (`nothing a defense can use`).
- **The doubled-lack formula in variant 3.** All four draft faces ran `no X and no Y`, three of them naming `narrows`. Now two faces carry the doubled lack and two carry a single one; `narrows` falls from 3 to 2; the commanding-height lack is written with four different nouns and verbs across the four faces (hill · commands · rise · height), which is R-DA-10's vary-the-noun-across-the-pool.

**Face by face — what changed and the law or target behind it.**

*Variant 1 `[visitor]` — the ground as it stands to anyone coming at the town.*
1. **parent** — `the ground carries no part of the defense` → `the ground they cross carries no part of the defense`. The relative pulls the second clause back onto the first clause's own subject, so the sentence is one connected thing rather than two facts bolted together with `and`: THE THREAD (§1.4.1) applied inside the unit, and the density law (§21.4) — three added words that tie, not that pad. Claim set unmoved; no `which` (wall 6); `they` is mid-sentence, never a closer (R-DA-04's pronoun ceiling untouched).
2. **face 1** — the comma-and split into two strokes (`… on every side. A defense takes nothing from such ground.`). Same words, different rhythm; it now differs from its parent in shape as well as vocabulary, which is what §21.1 asks of the sibling distance. `such ground` keeps the generalising reading fenced to this site (R-DA-12's generalisation test).
3. **face 2** — held verbatim. It is the pool's one K2-then-K1 face, its two strokes are already the shortest way the record can say both halves, and it closes on the civic noun `the town`. §21.4: a lawful compressed line made plainer with no law behind the change is the regression, so the effort went to its eleven siblings.
4. **face 3** — `The horizon runs unbroken around {settlement}, and the ground beneath the town does nothing in its defense.` → `An unbroken horizon rings {settlement}. The ground beneath the town holds nothing a defense can use.` Three gains: the pool's only indefinite-article opener (A11 spread), a five-word first stroke against an eleven-word second (R-DA-05's within-pool sd, which the round otherwise narrows), and a close of the CONDITION kind rather than a fourth `defense` (R-DA-04's vary-the-kind). `rings` is a verb of position, not of intent (R-DA-11); `can` is a present capability, never a future (A2).

*Variant 2 `[ledger]` — the site's standing in the town's defensive account; no landform named.*
5. **parent** — held verbatim. The plainest ledger statement of K2 in the pool, closing on `the town` rather than the abstraction; nothing in the register's ledger idiom sits above `counts for nothing`.
6. **face 1** — held verbatim. Nine words, the pool's short line and its one inverted subject (`The defense … owes nothing to its ground`). R-DA-05's short line exists; §22 confirms shortening inside the band is editing, and there is nothing here to shorten further without losing a claim.
7. **face 2** — held verbatim. `tells in` is the one rationed archaism in the pool and it is doing work no plainer verb does; it is also the pool's only negative-subject sentence.
8. **face 3** — `In any account of its defense, {settlement} takes nothing from its site.` → `In any account of the defense at {settlement}, the ground does not appear.` The slot moves inside the fronted phrase so the main clause is a bare five-word statement instead of a third `takes nothing` (the pool had four `nothing`-verb ledger lines and now has three, each with a different verb: counts · owes · tells). The close changes kind, to a not-appearing absence. Two constructions were tried and refused: `what defends {settlement}` presupposes a non-empty set of defenders (a second fact the card does not license), and `reckoning` was dropped because it is the `terrain FAVOURABLE` sibling's own frame word and a mirror of a sibling row reads as an inversion rather than a face.

*Variant 3 `[street]` — the lack of defensive landform, plainly.*
9. **parent** — `no narrows guard the approach` → `no narrows pinch the approach`, and a comma before the joining `and`. `guard` gives a landform intent, which R-DA-11 refuses outright; `pinch` is the literal geometry. The comma is for the ear. Close held on `no cover`, the pool's absence-kind close.
10. **face 1** — `Nothing overlooks {settlement} and nothing pinches the approach, and the ground takes no part in the defense.` → `Nothing commands the way into {settlement}. The ground takes no part in the defense.` The doubled lack goes, one `nothing` goes, the comma-and goes, and `commands` is the precise word for the ground a defender would want and does not have — the sharpest licensed fact available to this face (§21.1's aim). Fourteen words against the draft's eighteen: compression that rewards, not a trim (§22, no sentence and no face removed).
11. **face 2** — `The country about {settlement}` → `The land at {settlement}`. The draft carried `about {settlement}` twice and `around {settlement}` once; the pool now carries `about {settlement}` once. `land` is a head noun used nowhere else in the pool, which is R-DA-10's dispersion floor read across the pool rather than within a line. The comma-and is kept here deliberately: it is one of the three the round allows, and this face is the pool's plainest statement of the doubled lack.
12. **face 3** — `The town has no hill to hold and no narrows to close, and the ground at {settlement} does not enter the defense.` → `The town has no height to hold, and the site {settlement} stands on does not enter the defense.` The third `narrows` and the third `hill/no-X and no-Y` pair both go; `height` is the fourth distinct noun for the same lack; `the site {settlement} stands on` is the street's own way of naming the ground and keeps the required single `{settlement}` in the face.

**Walls re-checked on every one of the twelve.** No em dash · no exclamation · no question · no digit and no percent · no `which`-clause · no semicolon and no colon · no first or second person · no future indicative · no existential opener (R-DA-07) · no citation and no holder named (`source: (none)`) · no count word and no band word (`{band}` is RESERVED; neither the field's value word nor a readiness word appears) · **exactly one `{settlement}` in every one of the twelve**, so every face's slot set is identical to its parent's (ARCH §2.5's face-row refusal — two draft-derived candidates that had lost the slot were caught and repaired) · **no wording opens on `{settlement}`** (T-F8) · no totality over persons · no exemption · no named character · no deity · no absence written where the world holds a fact.

**A11, checked on the twelve.** All twelve first-word pairs are distinct: The approaches · Open country · No ground · An unbroken · The ground · The defense · Nothing in · In any · No hill · Nothing commands · The land · The town. Two `Nothing` openers, two `No X` openers, one fronted prepositional, one indefinite article, five definite articles each on a different head noun.

**Shape and length.** Sentence counts run 1 · 2 · 2 · 2 / 1 · 1 · 1 · 1 / 2 · 2 · 1 · 1 — never three (A1). Word counts run 17 · 16 · 17 · 16 / 13 · 9 · 11 · 13 / 18 · 14 · 18 · 18 (counted with the slot as one word); the pool keeps its nine-word short line and its eighteen-word long ones. The parents' own spread moves 15 · 13 · 18 → 17 · 13 · 18 (the relative added to 1p), so the parent row's own range widens rather than narrows and no spread measure regresses.

**THE THREAD (owner, 2026-09-08 ~21:4x).** This pool is the SPINE: it leads the passage and hands a noun forward rather than picking one up. Every one of the twelve closes on a noun a modifier can carry — the defense, the ground, the town, cover, the site — and none closes on a pronoun. Where a face runs to two sentences, the second picks up the first's own noun or its subject (`open country` → `such ground`; `No ground … serves a defense` → `The country is level`; `An unbroken horizon` → `The ground beneath the town`; `No hill … no narrows` → `The ground`; `Nothing commands the way into {settlement}` → `The ground`), so no face contains an internal disconnect for a refuter to name.

**Siblings, neither restated nor contradicted.** The `readiness` bands own the town's arrangements, works and watch: every face here confines its nil-worth claim to what the GROUND does, so a `STRONG` town on open ground stays a real and sayable state. The `strategic value` pools own what the town is worth to a taker: no face says the site is worth nothing simply, always worth nothing IN A DEFENSE. `terrain FAVOURABLE` is the same field's other value and can never render beside this one; this round deliberately moved 2f3 OFF that sibling's `reckoning … stands to the … account` frame so that no face reads as its mechanical inversion.

**Refusals: none.** All three variants stay lawful; no face is banked, and the banked count for this pool stays at zero. Four faces are held verbatim (1f2, 2p, 2f1, 2f2) with the reason recorded at each: they sit at the ceiling the card and the register allow, and §21.4 makes a lawful compressed line made plainer with no law behind the change the regression, not the refinement. Eight faces changed.

**One judgment recorded for the chair, vetoable, carried from the draft and re-examined.** R-DA-02 would license a contrast here, because the rejected alternative is named by a sibling band (`terrain FAVOURABLE`). No face uses one. The antithesis shape is the register's measured tell with a down-direction ceiling, and the contrast would have had to assert what DOES defend the town, which this card does not license. The licence is left unspent deliberately, in this round as in the draft.
