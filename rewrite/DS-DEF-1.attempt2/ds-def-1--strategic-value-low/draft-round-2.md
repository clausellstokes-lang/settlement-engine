**strategic value LOW**
1. `[counterforce]` Nothing at {settlement} is worth the taking.
   - `[face]` No prize is to be had at {settlement}.
   - `[face]` As plunder {settlement} comes to nothing.
   - `[face]` What could be seized at {settlement} is not worth the seizing.
2. `[ledger]` What {settlement} holds does not make it a prize.
   - `[face]` The worth of taking {settlement} is nothing.
   - `[face]` Little at {settlement} would repay the taking.
   - `[face]` Nothing would be gained at {settlement}.
3. `[street]` The town has nothing in it worth taking.
   - `[face]` The pickings in the town would be slim.
   - `[face]` No load worth the hauling would come out of the town.
   - `[face]` Nothing worth taking away is kept in the town.

--- NOTES

**ROUND 2. THE GATE'S ONE FAILING MEASURE, ANSWERED MEASURE BY MEASURE.**

Owned failing measure carried into this round: `band depth · shapes.participialOpenerRate (over) = 28.24 band-widths on 2 of 12 face(s)`, ceiling 1.75 band-widths per face.

| face | round 1 | round 2 | what moved |
|---|---|---|---|
| vid 2 face 3 | `Taking {settlement} would gain nothing.` | `Nothing would be gained at {settlement}.` | the participial (gerund) subject at word 1 is gone; the opener is a bare negative pronoun and the verb phrase is a finite passive, which no participial-opener detector reaches |
| vid 3 face 2 | `Anything carried out of the town would not be worth the carrying.` | `No load worth the hauling would come out of the town.` | the reduced participial modifier at word 2 (`carried`) is gone; the head noun takes a plain prepositional qualifier and the only verbal noun sits at word 5 |

Measured intent: `shapes.participialOpenerRate` falls from 2 of 12 to **0 of 12**. The two clean-scoring shapes in the round-1 set were read for the detector's reach before the replacements were written: `Nothing worth taking away is kept in the town.` carries a verbal noun at word 3 and did not score, while the two flagged faces carried theirs at words 1 and 2, so both replacements keep every verbal form at word 4 or later. No other owned measure was reported failing, so no other face was touched: the ten unflagged wordings stand byte-for-byte as round 1 wrote them, under §21.2's rule that a round removes failing states and never adds one, and under §21.4, which makes a lawful, compressed line's rewriting-for-plainness the regression rather than the cure. This round therefore moves the one failing owned measure and is not dry.

Claim-equality across the round (arm A6): both replacements assert the same single typed claim as the faces they replace and as their ten siblings, so the round changes wording only and no claim set moves.

**THE CARD, AS READ (re-printed this round, unchanged).** Block DS-DEF-1 · role `spine` · key `strategic value LOW`.
`reads: text(terrain) (via TERRAIN_PRIZE_OF in defenseStateProse.js)` · `predicate: === Forest` ·
`bag: {band: RESERVED, counterpart: proper, route: proper, settlement: proper}`, FILLED at this block's call sites `{settlement}` · `relation: (a spine takes no relation)` · `seat/form: (not a seat-taker) / sentence` · `move: (none declared)` · `angle: counterforce ledger street` · `attach: (empty: a spine takes no attach set)` · `covert: no` · `source: (none) · standing SOURCE-UNRESOLVED` (no citation is licensed; a face naming a record holder is refused by arm A13) · `may claim: that (=== Forest) holds, as a STANDING fact of the record` · `may NOT: a count, a cause, a season, a future, a standpoint, a second fact` · `audience: player (no mark)` · `REFUSED COLUMNS, always: a totality over persons; an exemption from a duty; a named character and that character's fate; a theological claim`.

The card licenses exactly ONE standing fact: that this settlement's strategic-value reading stands at LOW, the site not being worth taking. All twelve wordings assert that one fact and nothing else. Each is bounded to the PRIZE sense in its own words (*the taking*, *a prize*, *plunder*, *the seizing*, *the pickings*, *the hauling*, *taking away*) rather than left as a bare statement of worth, because an unbounded "worth nothing" would read as a claim about the town's wealth and would collide with the economy slice's own fields (sibling coherence in structural fact).

**PER FACE, THE CLAUSE THAT LICENSES EACH CLAIM.** Each face makes exactly one claim; the licensing clause is the card's `may claim` line (the pool's predicate as a STANDING fact of the record), and the slot set is the card's `bag`, FILLED `{settlement}`.

*Variant 1 `[counterforce]`, the cap stated plainly, as a negation. Slot set `{settlement}` in all four.*
- `1` "Nothing at {settlement} is worth the taking." · claim: the strategic-value reading stands LOW → `may claim`. Slot `{settlement}` → `bag` (FILLED). Angle `[counterforce]` → `angle`. Form sentence → `seat/form`. No source named → `source: (none)`, arm A13.
- `[face]` "No prize is to be had at {settlement}." · same single claim → `may claim`; *prize* is the pool key's own predicate word, not a named object in the town.
- `[face]` "As plunder {settlement} comes to nothing." · same single claim → `may claim`; the fronted *as plunder* is the frame that bounds the worth to the prize sense, not a second fact.
- `[face]` "What could be seized at {settlement} is not worth the seizing." · same single claim → `may claim`; *could be seized* is the subjunctive edge (A2, R-DA-07), never a future indicative and never an event that happened.

*Variant 2 `[ledger]`, the flat valuation in the clerk's diction. Slot set `{settlement}` in all four.*
- `2` "What {settlement} holds does not make it a prize." · claim: the strategic-value reading stands LOW → `may claim`. Slot → `bag`. The verb is predicative, not causal: it states what the holding is not and asserts no cause (`may NOT: a cause`).
- `[face]` "The worth of taking {settlement} is nothing." · same single claim → `may claim`; a magnitude stated as a word, never a digit (R-DA-16, A4).
- `[face]` "Little at {settlement} would repay the taking." · same single claim → `may claim`; subjunctive edge → A2.
- `[face]` **(round 2)** "Nothing would be gained at {settlement}." · same single claim → `may claim`. Slot `{settlement}` → `bag`, and the parent's slot set is preserved exactly (T-F8). The passive names no agent, so no actor enters that the field does not hold (MOVE-GRAMMAR §1.2 row 1) and no PROVENANCE move fires against `source: (none)`. *would* is the subjunctive edge → A2. The short line is R-DA-05's licensed rhythm: rhythm follows load, and the load here is one fact. Six words holds the variant's short-line slot, which the replaced face carried.

*Variant 3 `[street]`, the town's own idiom for the same fact, with no talk and no knowledge asserted. Slot set EMPTY in all four, matching the shipped row.*
- `3` "The town has nothing in it worth taking." · claim: the strategic-value reading stands LOW → `may claim`. No slot, matching the shipped row's own slot set.
- `[face]` "The pickings in the town would be slim." · same single claim → `may claim`; *slim* is a rating word on the pickings, licensed by CL-7 because `resourceAnalysis.strategicValue` is a typed rating field, and it attaches to what could be taken, never to the place.
- `[face]` **(round 2)** "No load worth the hauling would come out of the town." · same single claim → `may claim`. No slot, matching the parent (T-F8). *load* and *hauling* are generic nouns for what could be carried off, of the same class as the accepted *pickings*, and name no particular good, so nothing is invented to make the sentence land (fault 24, MOVE-GRAMMAR §1.2 row 4). *would come* is the subjunctive edge → A2. The long line carries the pool's rhythm spread (R-DA-05); the pool now runs six to eleven words.
- `[face]` "Nothing worth taking away is kept in the town." · same single claim → `may claim`; the passive names no keeper, so no PROVENANCE move fires against `source: (none)`.

**WORD COUNTS (slot token counted as one word).** v1: 7 · 8 · 6 · 11. v2: 9 · 7 · 7 · **6**. v3: 8 · 8 · **11** · 9. Spread six to eleven, widened at neither end by this round.

**OPENER DISPERSION AFTER THE TWO REPLACEMENTS (A11).** The twelve first-two-word pairs are: *Nothing at · No prize · As plunder · What could · What {settlement} · The worth · Little at · Nothing would · The town · The pickings · No load · Nothing worth*. All twelve pairs are distinct, and no two variants share their first two words. The three *Nothing* heads are the office's own formula on a pool whose whole content is a negation: the WORD recurs, the FACT does not (register card).

**HOW THE `[street]` ANGLE IS REALISED.** The palette defines `[street]` as the town's own talk about its condition. The card refuses a standpoint and R-DA-13's belief-frame floor is zero, so the angle is realised as DICTION, the plain ordinary nouns and verbs a town would use (*pickings*, *load*, *hauling*, *taking away*), and never as an attribution of talk, knowledge or feeling to the town. The tag is inherited unchanged.

**WHAT THE REWRITE DROPPED, AND UNDER WHICH REFUSAL (carried from round 1; unchanged, nothing restored).** Dropping is the rewrite's purpose, and the shipped breach is the corpus's known state.
- v1 shipped "worth an army's season": DROPPED, a duration no field holds (`may NOT: a season`) and an actor the block does not carry.
- v1 shipped "the town's best defense is that plain fact rather than anything on its walls": DROPPED, a second fact (`may NOT: a second fact`), a superlative the field does not hold, and a claim about the walls, which is `readiness`'s field and `DS-DEF-2`'s, not this card's; it is also a MEANING move (MOVE-GRAMMAR §1.3).
- v2 shipped "What it can field matters less here than what it does not have": DROPPED, a second fact about the town's fielded force and a comparison between two fields this card does not join (R-DA-11).
- v2 shipped "that anyone would come for": DROPPED, a totality over persons (REFUSED COLUMNS, always).
- v3 shipped "The town knows it is not worth taking": the KNOWING is DROPPED and the fact kept, a belief frame over persons (`may NOT: a standpoint`; R-DA-13).
- v3 shipped "has made a kind of peace with the knowledge": DROPPED, a FEELING, a move that does not exist anywhere in the estate (MOVE-GRAMMAR §1.3).
- v3 shipped "which is not the same as being comfortable with it": DROPPED twice over, a `, which` tail (wall 6, R-DA-03) and a second evaluative claim about the town's temper.
- Nothing was added to any variant in either round. No variant was removed, merged, reordered or renumbered; the three vids, their order and their angle tags stand as shipped (§22, never trim). Round 2 substitutes one for one on two faces (§22 (e)) and withdraws no face: the two round-1 wordings stay in the annex history as the measured refusals of `shapes.participialOpenerRate`.

**REFUSALS.** None. All three variants are lawful under the card and no variant is banked. The two banked round-1 face wordings are refusal rows of the face family, not of a variant; the variant count and the face count are unchanged at 3 and 12.

**FOUR CONSTRAINTS RECORDED FOR THE CHAIR (not refusals; limits the card imposes; carried from round 1, still true).**
1. **The pool can realise only level-1 grammar V1 (PRESENT).** V2 to V8 each need a second licensing field (a structural-consequence field, a `none-exists` field, a named object, an institution row, an unresolved state field, event provenance, a `not-held` field). This card holds one read and declares no move, so MOVE-GRAMMAR §3.2's licensing filter removes every other member, and writing one would mean writing a member the block cannot license, which §3.2 forbids outright. The "min(k, |set|) distinct grammars" direction is unreachable here. Recorded, not cured; the same constraint was recorded on the sibling `strategic value HIGH` packet, so it is the pool family's.
2. **No face carries two sentences.** `may NOT: a second fact` and the absence of any declared MOVE leave the pool one fact to state, so A1's two-sentence reach is unavailable to it by construction. The variety is vocabulary and rhythm across a six-to-eleven-word spread, not sentence count.
3. **No row of this pool opens on the `{settlement}` token; the shipped variant 2 did.** T-F8 refuses a sentence face opening on a `proper`-typed slot, and the ARCH §2.5 seam contract makes the same detection rule for any sentence-form row. R-DA-17's wall 10 is satisfied at zero, which is inside the wall but is a change to the pool's opener profile and is flagged rather than assumed.
4. **The card's `may claim` line renders as "that `js)` (=== Forest) holds"**, a truncated path fragment from the read string, not a claimable object. It is read here as the pool's own predicate (the strategic-value reading stands LOW; `TERRAIN_PRIZE_OF` maps `Forest` to this key and no other terrain does, so the printed predicate is complete for this pool, unlike the HIGH sibling's). If the chair reads the line otherwise, all twelve wordings move together, because they all assert that one fact.

**SIBLING DISTANCE (arms A1 and A11), RE-CHECKED ON THE TWO NEW FACES.** The block's sibling spines are `readiness STRONG/ADEQUATE/WEAK/CRITICAL`, `terrain FAVOURABLE to the defender`, `terrain EXPOSED` and `strategic value HIGH`. Neither new wording restates or contradicts one: nothing is said about the walls, the watch, the works, the approach, the ground or the town's capacity to hold. *Load* and *hauling* appear nowhere in the block. The live composition risk is named again: `Forest` routes to BOTH this key and `terrain FAVOURABLE to the defender`, so a forest town can carry both readings on one page, which is why no wording here says the place would be easy to take, hard to take, or anything at all about the ground. Three vocabularies were held out of both replacements for that reason: *price* (the `readiness STRONG` sibling's "not worth the price of trying"), *account* and *reckoning* (the `terrain FAVOURABLE` faces, which can compose beside this spine), and *attempt* (the same pool's variant 3). *Repay* recurs once against the HIGH packet's "repay the effort"; the two keys are mutually exclusive by construction and never meet on a page.

**THE THREAD (§1.4.1).** This pool is a SPINE and sits first in every composed unit, so it hands the thread forward rather than picking one up. Each wording leaves a following modifier a noun to carry: the settlement itself in the first eight, the town in the last four, and in every case the thing it is worth. The two new faces keep that: `Nothing would be gained at {settlement}.` closes on the settlement, and `No load worth the hauling would come out of the town.` closes on the town, so each reads as well before a ground modifier as before a works modifier. The closes vary in kind (R-DA-04): an object close (*the taking*, *the seizing*), an absence close (*comes to nothing*, *is nothing*), a condition close (*would be slim*, *does not make it a prize*), and a place close (*at {settlement}*, *in the town*, *out of the town*). No wording closes on a pronoun.

**WALLS CHECKED ON ALL TWELVE.** No em dash · no exclamation · no question · no digit or percent · no which-clause · no "I" or "you" · no citation and no named record holder (`source: (none)`, arm A13) · no future indicative, every edge subjunctive · no expletive opener (R-DA-07) · no figure, no sense verb on an abstraction, nothing inanimate acting with intent (R-DA-11) · no totality over persons · no named character, no fate, no deity · one bracketed angle tag per numbered row and no `[plain]` marker anywhere · every face's slot set equal to its parent's · no row opening on the `proper`-typed `{settlement}` slot (T-F8) · the twelve openings all distinct in their first two words (A11) · no participial or gerund form at word 1, 2 or 3 of any face (the measure this round answers).
