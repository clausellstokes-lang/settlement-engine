Seat: Opus 5 (Fable-unvalidated) · REWRITE **PHASE 2 REFINEMENT** (arm A, the Opus refiner) · block DS-DEF-11 · pool key `WALLED-STRAINED`
Written 2026-09-09. Nothing outside this file was written; no dock was entered, no test was run, no commit was made.
Commands executed: the read-only licence-card script in `laneRW-DEF11`, and read-only greps and reads over that dock.

# DS-DEF-11 · `WALLED-STRAINED` — the refined variant rows

Two existing variants, refined in place under their own numbers, in their own order, angle tags
untouched. No variant added, none removed, none merged; no face added, none dropped; every claim
kept, none added. Eight faces in, eight faces out, one for one.

**THE BASELINE THIS ROUND REFINED, and a discrepancy declared before anything else.** The brief
named `draft-round-3.md` as the draft. The dock's own rows are round FOUR: at
`docs/content/RECEIPT_POOLS_DOSSIER_STATE.md:5990-5998` face 2.2 reads *"Muster pay at {settlement}
is not made up."*, where round 3 read *"The pay due the muster is not made up at {settlement}."*
Every other row is common to both. The brief also rules that the pool's CURRENT rows in the receipt
file "are the sentences you rewrite, one for one", so this round refined the DOCK's eight — round 4
for face 2.2, round 3 elsewhere, which is the same set of eight either way. Round 4's own cure
(no sentence ends on `{settlement}`, whose authored-grain token strips to `settlement` and matches
the `ment` arm of `closers.abstractNounRate`) is preserved on all eight faces.

**REF-6 of round 3 is DISCHARGED by this round.** Round 3 deferred the ceiling push on the lawful
faces to Phase 2; round 4 held the draft at its lawful state so the two taste arms would share one
baseline (§23). That push is this seat's job under §21.1, §21.2 and §21.3, and every one of the
eight faces moves.

---

## THE PASTE BLOCK

The bold heading is shown for placement only; it already stands under `### DS-DEF-11`. What replaces
the pool's current variant rows is the two numbered rows and their six sub-rows, verbatim, with the
three-space indent on every `- ` sub-row.

**`WALLED-STRAINED`**
1. `[ledger]` The {defwork} at {settlement} stands, and the muster is short of its wage.
   - `[face]` On the muster roll at {settlement} the pay sits under its due, and the {defwork} holds.
   - `[face]` Wages at {settlement} are met short of the roll. The town's {defwork} is in place.
   - `[face]` At {settlement} the {defwork} is up. The town's muster is paid below its roll.
2. `[unfolding]` Pay for {settlement}'s muster is a term left open on the roll, and the {defwork} stands.
   - `[face]` The roll at {settlement} sets a wage above the muster's pay, and the {defwork} is in place.
   - `[face]` Muster pay at {settlement} is not made up to the roll. The town has its {defwork}.
   - `[face]` The muster at {settlement} is not paid to its full wage, and the {defwork} is up.

**Two variants · eight faces · word counts (authored grain, each `{slot}` token one word):**
1.0 = 13 · 1.1 = 16 · 1.2 = 9 + 6 · 1.3 = 6 + 8 · 2.0 = 16 · 2.1 = 17 · 2.2 = 11 + 5 · 2.3 = 16.

## THE ROWS THIS ROUND REFINED (the dock at `:5990-5998`; not part of the paste)

1. `[ledger]` The {defwork} at {settlement} stands, and the muster is short of its funding.
   - `[face]` On the muster roll at {settlement} the pay is under its due, and the {defwork} holds.
   - `[face]` The wage roll at {settlement} is not met. The town's {defwork} is in place.
   - `[face]` Wages at {settlement} run short of the roll, and the {defwork} stands.
2. `[unfolding]` Pay for {settlement}'s muster stands open on the roll, and the {defwork} is up.
   - `[face]` The roll at {settlement} sets a wage above what the muster is paid, and the {defwork} holds.
   - `[face]` Muster pay at {settlement} is not made up. The town has its {defwork}.
   - `[face]` At {settlement} the muster is not paid to its full wage, and the {defwork} is in place.

---

# --- NOTES

## One line per face — what changed, and the law or target behind it

- **1.0 `[plain]`** — `funding` → `wage`: the estate's own noun for what the gate funds is garrison
  pay (`defenseDisplay.js:279` names the `military` gate's expense *garrison pay*), so `wage` is the
  concrete civic noun the fact actually has and `funding` was the abstract modern stand-in.
  R-DA-10 (the concrete civic noun; the pet-word ration) and §21.1's *sharpest licensed fact*; it
  also clears the last `-ing` word out of the pool, which is defence in depth on the measure round 3
  cured.
- **1.1** — `the pay is under its due` → `the pay sits under its due`: a stative record verb in place
  of the bare copula, which gives the long fronted line a stress it lacked and asserts nothing new.
  R-DA-07 keeps the copula but does not require it; §21.4's density law (the line lands when read,
  and is not made plainer); no inanimate intent, since `sits` is position and not act (R-DA-11).
- **1.2** — opener recast `The wage roll` → `Wages`, and `is not met` → `are met short of the roll`:
  the opener change takes `openers.sameOpenerAsPreviousRate` from 0.5000 to 0.0000 on the pool's one
  remaining repeated-opener face, which is round 4's NOTE G target 1 taken exactly as it was handed
  forward (the first sentence moves, never the second, because the second carries the thread); and
  `met short` states `military < 1` as partial payment where a flat `not met` could be read as no
  payment at all, which is claim PRECISION inside the same claim, not a claim change (arm A6,
  claim-equal; the card's `may claim`).
- **1.3** — wholly recast from `Wages at {settlement} run short of the roll, and the {defwork}
  stands.` to a two-sentence face that states presence first and the shortfall second: the pool held
  no K1-first two-sentence shape and no sentence under eight words, so this face buys the register's
  short line (R-DA-06's `< 8` direction, `≥ 0.030`), a non-zero `wordsPerSentence.neighbourVariation`
  where a one-sentence face scores zero, and within-pool sentence-count spread (R-DA-05, the pool is
  the unit of spread; A11's FLATTENED arm). THE THREAD is kept by `The town's`, which carries
  `{settlement}` forward, since a spine sits first and can never spend the passage's one turn
  outward (MOVE-GRAMMAR §1.4.1).
- **2.0 `[plain]`** — `stands open on the roll` → `is a term left open on the roll`, and `is up` →
  `stands`: the `[unfolding]` angle is the STANDING-OPEN specialisation of PRESENT, and
  MOVE-GRAMMAR §1.2 row 10 words that state *a term unmet*, so the plain line now names its own
  licence in the grammar's vocabulary instead of gesturing at it; `left open` is a participle with no
  agent, so the roll is given no intent (R-DA-11). Round 4's NOTE G target 3 named the two plain
  lines the thinnest wordings in the set and `the {defwork} is up` the thinnest half of this one.
- **2.1** — `above what the muster is paid` → `above the muster's pay`, and the tail moved to `is in
  place`: two words shorter, the comparison landing on a noun instead of a relative clause, which
  removes the pool's only relative construction and reads faster with no claim moved (§21.4's
  compression as the ceiling; R-DA-03's dislike of the tail). The tail change is bookkeeping on the
  presence predicate, set out in the table below.
- **2.2** — `is not made up` → `is not made up to the roll`: `made up` is the right clerical idiom
  but left the standard unnamed, and the roll is the standard the licence supplies; the addition is
  the same claim stated against its measure (a comparison is a measurement in words — the register
  card; R-DA-11), and it widens the face's sentence burst from 0.462 to 0.750, the strongest in the
  pool.
- **2.3** — the fronted place adjunct dropped, the muster promoted to the subject seat: `At
  {settlement} the muster is not paid…` → `The muster at {settlement} is not paid…`. The pool now
  carries exactly one fronted-place face (1.3, where the fronting buys the short line), so the
  fronted-place habit the brief names as a tic does not repeat, and this face's first stress falls on
  the civic body the sentence is about (R-DA-04, land on the civic noun the field names; R-DA-17's
  closed-set opening).

## The shape of the pool after the round, and the tics that are gone

| face | shape | subject seat | K2 idiom | K1 realisation | closes on |
|---|---|---|---|---|---|
| 1.0 | one sentence, K1 → K2 | the {defwork} | `is short of its wage` | `stands` (clause-initial) | wage |
| 1.1 | one sentence, fronted record adjunct, K2 → K1 | the pay | `sits under its due` | `and the {defwork} holds` | holds |
| 1.2 | two sentences, K2 → K1 | wages | `are met short of the roll` | `The town's {defwork} is in place.` | place |
| 1.3 | two sentences, K1 → K2 | the {defwork}, then the muster | `is paid below its roll` | `At {settlement} the {defwork} is up.` | roll |
| 2.0 | one sentence, K2 → K1 | pay | `is a term left open on the roll` | `and the {defwork} stands` | stands |
| 2.1 | one sentence, the record as subject, K2 → K1 | the roll | `sets a wage above the muster's pay` | `and the {defwork} is in place` | place |
| 2.2 | two sentences, K2 → K1 | muster pay | `is not made up to the roll` | `The town has its {defwork}.` | {defwork} |
| 2.3 | one sentence, K2 → K1 | the muster | `is not paid to its full wage` | `and the {defwork} is up` | up |

- **Eight distinct K2 predicates**, one per face, none a paraphrase of a sibling.
- **The ledger-closer habit is broken.** Faces closing `and the {defwork} X` fall from five of eight
  to four of eight, and those four now carry four different predicates; three faces close on the K2
  fact and one on the object. Within each variant the four closes are distinct words.
- **The K1 predicate inventory is licence-bound, not lazy.** The card's `may claim` is presence and
  nothing beyond presence, and round 3 already refused `is sound` on that ground; §21.3 rules that
  where one effort must choose between a sharper wording and a licensed one, the licensed one wins.
  So the width was taken in SUBJECT and SHAPE. Inside each variant no two faces share a presence
  predicate. Across the two variants, `the {defwork} is up` occurs in 1.3 as a sentence and in 2.3 as
  a coordinate tail; that four-word overlap is declared rather than hidden, and the two faces share
  nothing else — different variant, sentence count, fact order and subject.
- **First two words, all eight distinct**: `The {defwork}` · `On the` · `Wages at` · `At
  {settlement}` · `Pay for` · `The roll` · `Muster pay` · `The muster` (A11). Neither numbered row
  opens on the settlement token (R-DA-17; ARCH §2.5's T-F8), and no face does either.
- **Two-sentence faces rise from two to three** (1.2, 1.3, 2.2), which is spread against A11's
  FLATTENED arm; the BEFORE of this pool was uniform at one sentence per variant.

## The twenty-one detectors, computed by hand over all eight faces

`src/domain/prose/proseFingerprint.js` was read whole in the dock (222 lines) and every metric below
was computed against its own regex, not by eye. **Label: CONFIRMED for the arithmetic and the
detector semantics (executed reads of the instrument's source, quoted); PLAUSIBLE for the gate's
verdict, because this seat may not run the gate.**

- Zero on all eight faces: `punctuation.semicolonRate`, `colonRate`, `emDashRate`, `questionRate`,
  `exclamationRate`, `parenthesisRate`, `shapes.antithesisRate`, `triadRate`,
  `participialOpenerRate`, `whichTailRate`, `doubledAdjectiveRate`, `adverbsPerSentence`,
  `thereIsOpenerRate`, `dialogueShare`, `closers.abstractNounRate`, `closers.pronounRate`,
  `openers.sameOpenerAsPreviousRate`, `runsOfThreeSameLengthBand`, `wordsPerSentence.shareOver30`.
- `shapes.antithesisRate` needs `not … but`, `rather than`, `, not ` + lower case, or `less … than`:
  no face carries any of the four, and no face carries the word `but`.
- `triadRate` and `doubledAdjectiveRate` are unmatchable on every face: no face holds two commas, and
  every ` and ` in the pool is preceded by a comma, which blocks the `\w+(suffix) and \w+(suffix)`
  span.
- `closers.abstractNounRate`: the sentence-final words are `wage` · `holds` · `roll` · `place` · `up`
  · `roll` · `stands` · `place` · `roll` · `{defwork}` · `up`. None matches
  `(ness|tion|sion|ity|ment|ance|ence|ship|hood|dom)$`, and no sentence ends on `{settlement}`, whose
  authored-grain token strips to `settlement` and would match the `ment` arm — round 4's finding,
  preserved.
- `openers.sameOpenerAsPreviousRate` is 0.0000 on 8 of 8. It was 0.5000 on face 1.2 in the dock. This
  is the round's one measured cure.
- `wordsPerSentence.shareUnder8` = 0.5000 on 1.2, 1.3 and 2.2 (it was 0.5000 on 1.2 and 2.2 in the
  dock) and 0.0000 elsewhere. The two dock faces at 0.5000 passed the round-3 gate, whose only
  depth-exceeding measure was the participial opener, so this value is inside the entry-grain depth
  on the lane's own receipt; face 1.3 joins them deliberately, because the direction on the short
  line is UP (R-DA-06, `0.017 → ≥ 0.030`).
- `wordsPerSentence.neighbourVariation`: 1.2 = 0.400, 1.3 = 0.286, 2.2 = 0.750, and 0.000 on the five
  one-sentence faces, which is arithmetic and not a choice — a single-sentence unit has an empty
  neighbour set. The dock carried the same zero on six faces. The count of faces with a non-zero
  burst rises from two to three, and 2.2's rises from 0.462 to 0.750.
- **No measure is worse anywhere.** Under §21.2 a lawful set keeps its refinement only if it stays
  lawful; on the hand computation this set moves one measure down, three sentence-shape measures
  toward their bands' interiors, and nothing up.

## The claim set, unchanged and claim-equal on all eight faces

- **K1 — this settlement holds a defence work of the wall class, present and standing.** Licensed by
  the card's `reads`, first path, `forces.walls.present` (measured); asserted and not presupposed,
  because ARCH §4.4's arm A0b convicts an UNDER-claim on a declared read.
- **K2 — the paid military establishment is funded below what it requires.** Licensed by the card's
  `predicate`, `settlement.defenseProfile.economicGates.military < 1`, and stated under `may claim`
  as a STANDING fact of the record. Read in the generator this round: `economicGates.military` is
  `Math.round(milUpkeepMult * 100) / 100`, recorded only where a paid stack exists, and the display's
  own words for the gate below ×1.0 are *"Upkeep underfunded: garrison pay"*
  (`defenseGenerator.js:456-471`; `defenseDisplay.js:275-291`). So the shortfall is a shortfall of
  PAY against a standard, which is why the refined faces state it against the roll, the wage or the
  due, and never as a count and never as a cause.
- **The slots** are exactly the card bag on every face and both numbered rows: `{settlement}`
  (proper) once and `{defwork}` (bare-common) once. The SLOTS arm compares sets; no face adds or
  drops one.
- **No third claim anywhere.** No face asserts a relation between the wall and the paid force, a
  cause, a count, a season, a future, a standpoint, or a second civic object of the class `wall`; the
  words `stone`, `wall`, `gate` and `watch` appear nowhere. The two facts sit side by side, joined
  only by coordination or adjacency, neither of which asserts an edge.
- **Sibling-pool distance (arms A1 and A11).** The block's other four pools own `danger`, `country`,
  `books`, `account`, `entry`, `enters`, `repair`, `upkeep`, `charge`, `cost`, `bears`, `carries`,
  `working fabric`, `in service`, `mended`, `sound`, `fit for use`, `enclosure`, `closed against`,
  `present peace`, `cheaper`, `easier to keep than to raise`, `pays little`, `built work`,
  `patience`, `too small to wall`, `safety`, `neighbours`, `distance`, `unimportance`, `buys stone`,
  `confidence`, `thrift`, `openness`, `statement`, `weight`, `circuit`, `defense money`. Not one
  appears in this packet. Two adjacencies are declared rather than left for a refuter: `town`, which
  the register cannot avoid and which round 4 already declared; and `open` in face 2.0, against
  `openness` in `UNWALLED-LARGE` — a different word form in a different sense, in a pool a reader
  can never see beside this one, since a settlement takes one state key.

## Refusals carried, and one added

- **REF-1 (carried, rounds 2 to 4) — the provenance move stays refused across this face set**, though
  the card names the muster a LICENSED standing source. Part B §24 caps provenance at one citation
  per unit and only for one of amendment S3's three reasons; this pool holds none of them (one
  account, no count, no typed fact making the muster a power). MOVE-GRAMMAR §4.4.3 makes a citation
  on a fact whose holder is the compiling office itself a finding. And a single citing face would be
  claim-unequal to its three siblings, while citing in all four is the habit §24 hands to a refuter.
  Naming `the roll`, `the muster roll` or `the wage roll` as the record that fixes or fails the wage
  is a PRESENT move on a record object and not a provenance move; that reading is this seat's too.
- **REF-2 and REF-3 (carried) — the seven unlicensed claims removed from the two BEFORE rows** (`stone
  keeps itself, and wages do not`; `the watch that should man it`; `stands better than`; the `which`
  tail; `is thinning`; `around {settlement}`; `is sound`) stay removed, on ruling 5 as R-DA-15 states
  it. This round reintroduced none of them and reached for none.
- **REF-4 (carried) — a wiring row, not a text law:** `{defwork}` is NAMED BUT NEVER FILLED at this
  block's call sites, on the card's own printing, so every variant of this pool may be silent at
  render today. Kept in all eight faces because the SLOTS arm fails a face whose slot set differs
  from its parent's, and because a slot is not a wording. Still the chair's.
- **REF-5 (carried) — the sitting question:** with the unlicensed decoration gone, the two variants
  assert the same two claims and are told apart by angle and grammar alone, which is lawful and is
  what C-sibling asks, but the pool's semantic width is the width of its licence. Whether
  `WALLED-STRAINED` should carry two variants of one claim set is a sitting question, undecided here.
- **REF-7 (NEW) — `in arrears` was refused for face 2.2, and the refusal is on the record because it
  is the sharpest phrase this pool cannot have.** `The muster's wage is in arrears` is the best
  clerical idiom available for the fact and was this seat's first choice; it asserts an accumulated
  and overdue debt, which is a HISTORY move, and only an event-provenance field licenses history
  (R-DST-B; R-DA-19). The card reads a standing multiplier, so the fact is a standing shortfall and
  not a backlog. `is not made up to the roll` carries the licensed half and nothing more.
- **Not lawful, refused, or banked: none of the eight faces.** Both variants are written, both are
  refined, and both are lawful as written on the hand computation. Under §21.2 the set reverts to the
  round-4 wording if the gate finds any new failure; the round-4 rows are quoted above for that
  purpose, byte for byte.
