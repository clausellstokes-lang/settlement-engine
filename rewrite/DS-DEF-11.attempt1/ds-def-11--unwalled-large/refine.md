Seat: Opus 5 — REFINER (a different author than the drafter, by the owner's arm A of §23), Fable-unvalidated. Block DS-DEF-11 · pool `UNWALLED-LARGE` (role SPINE) · phase 2, the refinement round over `draft-round-1.md`.
**The draft this refines is `draft-round-1.md`. There is no `draft-round-3.md` in the packet directory; the round-1 draft is the only draft committed and is the lawful set this round pushes toward the ceiling.** Two variants in, two variants out, under their own numbers, their own order and their own bracketed angle tags; four wordings each, one for one with the draft. The block's typed lines (RECEIPT / STATE-KEY / SLOTS / SECTION-TARGET / PROVENANCE) and the pool's heading are not repeated and are not this packet's to touch.

1. `[counterforce]` `[plain]` The town of {settlement} is unwalled.
   - `[face]` At town size, {settlement} has no stone to its name.
   - `[face]` The weight of a town sits at {settlement}, and the town carries that weight inside no circuit.
   - `[face]` Though {settlement} holds the rank of a town, the place is not enclosed.
2. `[ledger]` `[plain]` A town, {settlement} stands open.
   - `[face]` The entry at {settlement} is a town's weight and no enclosure.
   - `[face]` Where {settlement} is set down as a town, it is set down with no walls about the place.
   - `[face]` The size at {settlement} is a town's, and the town keeps no wall.

--- NOTES

**1.plain — was `The town of {settlement} has no wall.`** *Have* is the weakest verb the register owns, and a bare possession is not the standing fact the card licenses: the card's `may claim` is that `present` holds **as a STANDING fact of the record**, so the line is rewritten to the condition itself. Six words, the shortest line in the pool, which is the register card's own permission (*the short line exists*) and is what buys the pool its length spread. *Wall* is released to 2.c, where a verb can work it. **Target: the sharpest licensed fact + the low end of R-DA-05's spread.**

**1.a — was `At town size, {settlement} keeps no stone.`** *Keeps* moved off this line so the pool does not carry one verb twice, and the close moved from the bare object to *to its name*, which varies the close KIND (R-DA-04's closed set: a name, against the pool's four object closes) and gives the line the idiom a clerk actually writes. The fronted adjunct is kept here deliberately: it is now one of only two in the pool, not one of five. **Target: close-kind variety + the tic count.**

**1.b — was `The weight of a town belongs to {settlement}, and the town bears that weight inside no circuit.`** *Belongs to* is limp and *bears* imports burden where the card holds only a tier. Rewritten to *sits at* / *carries*, which are placement and measurement verbs and assert no purpose (R-DA-11: no inanimate intent). The *weight → that weight* echo is kept and is now the pool's declared thread device: §1.4.1 states in terms that a deliberate noun echo for the thread is lawful and that A11's echo bound counts facts, not nouns. Seventeen words, the pool's second-longest. **Target: the verb, and the high end of the spread.**

**1.c — was `For all that {settlement} holds the rank of a town, the place is not enclosed.`** *For all that* is antique SYNTAX, which R-DA-18 refuses in terms (the antique air lives in the noun, never the syntax); *Though* is the same concessive with none of the costume, and two words shorter. The concessive itself is licensed, not smuggled: R-DA-02 keeps a contrast where the rejected alternative names a sibling pool key, and `WALLED-THREATENED` / `WALLED-QUIET` / `WALLED-STRAINED` are exactly that alternative at this tier — which is also the `[counterforce]` arm the block's PROVENANCE line charters. No face says walls are usual; the contrast is carried by the sibling key, never by a frequency word. **Target: R-DA-18, and the counterforce stated as law rather than as tone.**

**2.plain — was `By weight a town, {settlement} stands without walls.`** *Without* is a weak preposition and the line was the pool's fourth fronted adjunct in a row of them. Cut to five words on a bare appositive: the hardest, flattest entry in the pool, and the `[ledger]` variant's proper canonical line. *Open* is the shipped row's own word (`the openness`), so the strip of that row's unlicensed tail does not cost the pool its vocabulary. **Target: the ceiling for a ledger entry, and the pool's lower length bound.**

**2.a — was `Among the towns, {settlement} is one without an enclosure.`** *Is one without* is a periphrasis around a fact the ledger states flat, and *Among the towns* was fronted adjunct number three. Rewritten as an entry with two complements and no verb between them: the tier and the lack read as one line of a book, which is what `[ledger]` means and what V1 (one flat condition, against variant 1's PRESENT → LACK) asks of this variant. Density law: this is the pool's most compressed line and it is not made plainer. **Target: the angle, carried by the syntax rather than by an adjunct.**

**2.b — was `A town on the record, {settlement} carries no wall to its name.`** Two changes with law behind them. *On the record* gestures at the keeper of the record, and MOVE-GRAMMAR §4.4.3 makes a citation on a fact whose holder is the office itself a finding; *set down* is the office's own compiling act and names no keeper, so the provenance refusal stays clean. Second, the shape: this is now the pool's only subordinate-clause opener and its longest line at eighteen words, and the *set down … set down* parallel is the read-aloud ear of a clerk copying a line twice. *To its name* moved to 1.a. **Target: the provenance ceiling (§24), sibling distance, the high end of the spread.**

**2.c — was `The size at {settlement} is a town's, and the town stands unwalled as a matter of course.`** *As a matter of course* asserts that the unwalled state is the routine one — a frequency claim about the world that no field on this card holds, and a gnomic beat under R-DA-12's generalisation test. It is struck, and the clause is re-landed on the civic object with *keeps*, the block's own upkeep verb used at its other value (the walled pools *keep* their `{defwork}`; this pool keeps none). The two can never both be drawn, so the shared verb restates no sibling. **Target: an unlicensed claim removed; R-DA-04's civic noun.**

**WHAT THE ROUND DID NOT DO.** No variant added, removed, merged or renumbered; no face added or dropped; both angle tags untouched; the claim set is byte-identical in content to the draft's — every one of the eight faces asserts exactly `(PRESENT, settlement.tier, town-and-above)` and `(walls, forces.walls.present, false)`, and nothing else (arm A6, claim-equal). Slot set `{settlement}`, once per face, and no face opens on it. No citation, no `{defwork}`, no count, no cause, no season, no future, no standpoint, no second civic object of the class `wall`, no totality, no exemption, no named character, no theological claim. The draft's three declared refusals stand unchanged and are carried forward: the provenance move refused on all four grounds (§24's ceiling; S3's three reasons absent; claim-equality; §4.4.3's self-citation), `{defwork}` refused as unfilled at this block's call sites, and the two shipped rows' unlicensed clauses (`whether that is confidence or thrift, the openness is itself a statement`; `is spending its defense money on something else, and the books say what`) struck under ruling 5 with their text kept in the annex history — nothing trimmed.

**MEASURED, NOT ESTIMATED (the eight face lines only).** Em dash 0 · exclamation 0 · question mark 0 · digit 0 · percent 0 · semicolon 0 · colon 0 · `which` 0 · first person 0 · second person 0 · `will` / `shall` / `would` 0 · pronoun closers 0 · settlement-openers 0 (R-DA-17's per-pool allowance unspent) · a second wall-class object 0 · sibling purse vocabulary 0 · modifier threat vocabulary 0. One terminal stop per face. First two words, all eight distinct: *The town* · *At town* · *The weight* · *Though {settlement}* · *A town* · *The entry* · *Where {settlement}* · *The size*. Fronted adjunct or appositive openers 2 of 8, down from 5 of 8 — the draft's one real tic. Coordinate two-clause shape 2 of 8, unchanged. Close kinds: condition 3 · object 4 · a name 1, against the draft's object 5.

**BAND POSITION, REPORTED AS INFORMATION ONLY (§21.1).** Words per face 6 · 10 · 17 · 13 | 5 · 11 · 18 · 13; n = 8, mean 11.625, population sd 4.357, sample sd 4.658, against R-DA-05's within-pool floor of 4.0 — met on either estimator and improved on both against the draft (4.062 / 4.342). One sentence per face, which is the card's `form: sentence`. Uniform-grammar share 0.50, unchanged and uncurable here: a two-variant pool shows at most two grammars, so the share of the most-used is 0.5 by arithmetic. Reported, not curable by a refiner; the cure is a chair act minting a key, never an append and never a trim.

**TWO READINGS THIS ROUND MADE, BOTH VETOABLE AT THE SITTING.**
1. **`The entry at {settlement}` and `is set down` are the office's own voice, not a provenance move.** §4.4.3 bars a citation that NAMES a holder; neither line names one, and the office compiling its own record is the register card's first sentence. Had either named the muster, the treasury or the roll, it would be a citation and would be refused on all four of the draft's grounds.
2. **`Where` in 2.b is registral, not spatial** — the sense of *in the case where the record sets it down so*. `set down` in the same clause fixes the reading, and the sibling pool `UNWALLED-SMALL` owns the spatial sense (`No wall marks where {settlement} ends`), which this pool does not touch.
