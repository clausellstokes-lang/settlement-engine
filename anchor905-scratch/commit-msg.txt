§905 follow-on car: the mobileSingleChrome retirement is anchored in flags.test.js (ratified vetoable at §904) — and the negative-assertion row for the file banks 1 → 0 by deletion

THE MECHANISM, MEASURED — AND R4's READING OF IT IS REFUTED.
The §904 receipt's row R4 declined to write this anchor because it judged that the
new negative would move the walker's baseline 1 → 2, making the car "a fifth file and
a ratchet act". That is true only of an UN-ANCHORED site. negativeAssertionAnchor's
`scanUnanchoredNegatives` (:115-138) counts a `not.(toContain|toMatch|toHaveProperty)(`
site only when the line fails all three exemptions — a helper call on the line (:129),
`// anchored:` on the line (:130), or `// anchored:` on the ONE line above (:131) — and
records a file only `if (hits.length)` (:135), so a file at zero is ABSENT from the scan
rather than present with 0. An ANCHORED new negative is never counted. R4 was right that
a bare new negative would ratchet, and wrong that this car had to write one.

Anchoring BOTH sites therefore takes the row DOWN, not up: 1 → 0. The walker spells zero
as DELETION, on three of its own citations — :193-194 "LOWER this file's number — delete
the row at 0", the inventory-honesty arm's own failure text "(delete it at 0)", and the
recorded precedent at :200 (tests/ui/homeLanding.test.jsx 1 → 0, row deleted). A fourth
reason is structural: `renderLiteral` (:140-146) emits a row only for files in `found`, so
a UPDATE_EPISTEMIC_ALLOWLIST regeneration can never print a `: 0` row, and keeping one
would put the hand-maintained literal permanently out of step with its own regenerator.
General roster: 502 → 501 rows, 1526 → 1525 sites.

THE ANCHOR FORM: the helper, not a comment marker.
`expectAbsentWithAnchor` asserts a live sibling IS present before asserting the retired
key is not, which converts the negative from a promise into a proof — the brief's stated
preference, and it fits. One wrinkle, measured: `assertContainable` (helper :50-60) admits
only string/Array/Set/Map, so FLAGS itself — a plain object — would be REFUSED; the
subject must be `Object.keys(FLAGS)`. That is not a workaround but the exact surface:
src/lib/flags.js:95-101 builds FLAGS as `Object.fromEntries(Object.keys(FLAG_DEFAULTS)…)`,
and both getAllFlags and the dev panel read the key set, so "exposed by the registry" IS
"present in Object.keys(FLAGS)". The idiom is already estate-established in this very
directory (tests/lib/accountImport.test.js:281). Anchors are the nearest surviving
relatives, so the drift that would remove a family reds on the anchor: `loadingJourneyFilm`
for the retired media-set companion, `workshopNav` for the retired mobile-chrome nav flag.

THE NEGATIVE CONTROL, IN THREE STATES rather than the two asked for. The edit was made,
parked in scratch and the dock restored to pristine (cp from a pre-edit backup, verified
with cmp; porcelain back to 0) so the pre-edit reading was honest.
  STATE 1 pristine tree, row at 1 ........ EXIT 0, 9 passed — the frozen 1 was a LIVE
      reading on arrival, not an already-stale row.
  STATE 2 anchored tree, row still at 1 ... EXIT 1, 2 failed | 7 passed. The scanner
      reports "tests/lib/flags.test.js: 0 un-anchored site(s) found, ceiling 1 — a site
      was anchored; LOWER the row to 0 (delete it at 0) to bank the win", and the vacuity
      arm reads "expected 1530 to be greater than or equal to 1531". This is what proves
      the helper form is actually RECOGNISED by the scanner rather than merely believed
      to be, and the two numbers confirm the frozen total predicted by hand before any
      instrument ran. State 2 needed no deliberate un-anchoring: it IS the intermediate
      state, so the control is the real thing rather than a mock-up of one.
  STATE 3 anchored tree, row deleted ...... EXIT 0, 9 passed.

REGISTERS — predicted in writing before any instrument ran, then measured.
  flags.test.js ......... 10 → 11 tests passed. PREDICTED, CONFIRMED.
  OSR plain read ........ 1972 exact, no drift (zero src/ bytes). PREDICTED, CONFIRMED.
  Negative-assertion ..... row deleted; 1531 → 1530 frozen. PREDICTED, CONFIRMED by the
      instrument's own arithmetic in state 2.
  Lighting census ....... ⚠ titles 23653 → 23654, a PREDICTED red this car owns. The arm
      asserts exact equality against the WORKING TREE and is sequenced, so files (2543),
      parked (373) and credited (2170) each passed before it — measured proof they did
      not move — and it stops at titles: "expected 23654 to be 23653" (ACTUAL first).
      suiteTitles holds at 6333 (describe( count unchanged at 3). NOT cured and NOT
      banked: the baseline's own _doc forbids hand-editing the five figures, and the only
      legal cure is the LIGHTING_CENSUS_REFREEZE door, which is the chair's.
  Test ratchet .......... ⚠ NO DOOR OWED — a refinement of the brief's premise. totalTests
      31970 → 31971 is right arithmetic but not a gate movement: check-test-ratchet.mjs
      :1330-1331 and :1350-1351 compare against Math.floor(total × SCOPE_FLOOR_RATIO) with
      the ratio 0.9 (:124), i.e. COLLAPSE FLOORS, and 31971 sits far above floor(31970×0.9)
      = 28773. tests/lint/testRatchet.test.js is a static shape pin over the baseline JSON
      and reads no live count. The frozen figure is one stale-low inside a 10% band.
  Writer-reach, prose-numerics ... unchanged (zero src/ bytes).
  Golden-freeze ......... checked, not assumed: the register names this walker only inside
      excludedEnvSpellings (UPDATE_EPISTEMIC_ALLOWLIST is excluded as an allowlist ratchet,
      not a world fingerprint). No env spelling added or removed, so no pin covers the edit.

THE OWED tests/lint/ DIRECTORY RUN (a tests/lint/ file and a tests/ file were edited):
EXIT 1 — Test Files 1 failed | 139 passed (140), Tests 1 failed | 2193 passed (2194). The
single failing arm is the predicted lighting census above; the other 139 files are green,
including every scanner-family member. No src/ bytes moved, so no typecheck run is owed.

Seat: Opus 5 — Fable-unvalidated
Lane: ANCHOR-905
