
## Walked by the Fable chair, 09-05 evening (read-only re-derivations; fold into FABLE_VALIDATION_QUEUE.md at the review)
- LH9 row 1 (HIGH) — CONFIRMED by the chair's own read of `goldenFreeze.walker.test.js` (arm 'every golden fixture file in tests/fixtures is claimed by a register row'): the orphan scan filters `/golden/i.test(rel) && rel.endsWith('.json')` under `tests/fixtures/`, so a fixture named without "golden" is never an orphan and its register row is voluntary. The rename to `preset-lighting-witness-golden.json` is the right cure for this surface; the roster-by-content car stays owed (plan row added).
- LH8 (the GENERATION_LAWS denominator's premise) — CONFIRMED by grep at 9f7378a55: `newSettlementLivingContentLaw` has no caller in `src/` outside its own definition (`livingContentLaw.js:154`), its header, and LH8's register row; the "second law with no caller" claim holds.
- LH9 row 4 (a non-`*.test.js` helper under `tests/simulation/` is free of every census) — PLAUSIBLE by the chair's read (vitest collects only `*.test.*`, the lighting census counts collected files, and the three walkers that walk `tests/` without that filter — `contractTestAntiVacuity`, `goldenFreeze`, `sovereigntyLightingContract` — key on `.contract.test`, `/golden/i` and lighting titles respectively); CONFIRMED by the lane's own farmed probe (files 2523 → 2524, the one new `.test.js`). Not re-executed by the chair.
- LH8 (the stripper false green: "181 / 2,174 files, 521,714 chars, settlementSlice 32,723") — RE-DERIVED by the chair with an independent probe (`prose-research/strip-order-rederive*.mjs`). With a LINE-BOUNDED single-quote regex the wrong order hides 10,091 chars in 7 files (traditionsKernel.js 9,692); with the walker's own NEWLINE-PERMISSIVE regex: files scanned 2174; files where the wrong order changes the stripped text: 181; chars hidden (right − wrong, summed where positive): 439446
worst five: src/store/settlementSlice.js 30359 | src/domain/worldPulse/informationStatecraft.js 21131 | src/domain/causalState.js 18635 | src/domain/worldPulse/armyTransitKernel.js 16104 | src/domain/events/mutateEntities.js 16000. ⭐ The hazard has TWO parts — the pass ORDER and a quote regex that may cross newlines; STRIPPER-UNIFY must cure both (a quoted string never spans a newline in this estate's source; the template literal does).
- Taste-sample refuter finding §4.6 ("#32 is a live corpus defect: a war doctrine authored under `economicBase: extraction`, RECEIPT_POOLS_DOSSIER_STATE.md:2247") — REFUTED by the chair from the annex itself: the variant belongs to DS-POW-5 (`POWER: ruling structure … the ruling-power lens`), whose header reads *"what kind of thing decides here, what it is afraid of, and how it fights"* and whose STATE-KEY says `economicBase` is "five, closed … each with an authored FEAR and WAR STYLE" (receipt `cohesionWeave.js` `ECONOMIC_BASE_LENS`). Every economicBase pool's `[counterforce]` variant is the war style by design (agrarian fights seasonally; trade_hub fights route wars; craft guards its supply; mixed fights in a balanced way). Not a defect; no ruling needed. The refuter's U11 point on "two or three places" stands as an observation only: §0d's QUANTITY bands govern people, not a small-number idiom, and prose-numerics permits the words.
- VIS-RESOLVER row 10 ("all eighteen general-desk mounts are OPEN") — PARTIALLY RE-DERIVED by the chair: the five host lines the arm graded [open] (OverviewTab:329, :401; PlotHooksTab:47; RelationshipsTab:163, :204) read at 7ef501df9 — hosts not plainly open: 0 of 5 (a host with `defaultOpen={false}` or a data-dependent expression would count). The thirteen host-less mounts are open by construction (no Section between mount and tab). CONFIRMED for the five hosts by read; the arm's own run (29/29) is the executed receipt.
- §900 lighting refreeze (register car 1, 0de65c4ce): probe == register at 7ef501df9 — files 2537 / parked 373 / credited 2164 / titles 23555 / suites 6302 (frozen was 2523/371/2152/23204/6217). CONFIRMED by the runner's own E4 line.
