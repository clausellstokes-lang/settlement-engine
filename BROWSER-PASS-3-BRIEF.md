# Browser pass 3 — the second wave on the final consist (Opus verification lane, read-only)

Preview: `.claude/launch.json` entry `consist-preview` (port 5219, strictPort) on the consist worktree; preview_start {name:"consist-preview"}. Desktop (1440) AND phone (375×812 via resize_window preset mobile; reload after switching) AND tablet (768×1024). Prefer read_page/find/javascript_tool probes; zoomed screenshots for visual items. Do not sign in, create accounts or purchase. Forge two settlements (a village and a town, one Mesoamerican-inspired). Walk, with receipts, exactly these — each was ordered tonight and built:
1. §934.20 THE FOOD BAR: Economics tab — the bar's runs (production, imports, a magic segment when the record credits one) plus its tail sum to 100% and equal the sentence's figures; find a world with a magic provision if you can (the sentence names "by druidic/divine/arcane provision" with a figure) else report the zero-magic case draws no magic segment. PDF: the same sentence in both economics chapters.
2. §934.20 CHARTS: the faction share bars print one number per faction; the Overview's systems-health row and Defense score rows: the drawn width equals the printed figure (probe the style width vs the text).
3. §934.21 COLUMNS: Power tab → a contender's "Institutions behind this power" runs in 3 columns at 1440, 2 at 768, 1 at 375; the basis caption once per group; Substrate foundations / Magic facets / Faith channels likewise where present.
4. §934.23 NO CLAMP: Economics revenue rows read whole as gazetteer entries (bar full width; one running line with the bold title; no ellipsis anywhere on the dossier tabs: probe `getComputedStyle(el).textOverflow` = 'ellipsis' count = 0 across the tab panels).
5. §934.22 LEAKS CURED: no "Parish church/churches/parish" on any tab or in the PDF (incl. Spatial Layout's Religious Quarter bullet and description, Economic Flows "Via", Medical Readiness); Economics imports print catalogue names, never `snake_case` keys; a VILLAGE is called a village in its prose and in the save nudge; status words in sentence case ("Well-defended", not "Well-Defended"); the Tonight card's badge reads "Hook" via text-transform.
6. §934.22 /create: no raw `pricing.tiers.*` text anywhere at 1440 and 375; the Founder card reads "A founding place, held until launch" / "Opens with the launch"; no horizontal scroll at 375 on /, /create, /library, /gallery, /compendium, a dossier (documentElement.scrollWidth <= clientWidth on each).
7. §934.24 REFUSALS SPEAK: exhaust the anonymous daily cap (3 generations) → the fourth attempt shows the refusal notice naming the cap; "Fork this sample" (Mossgate, a Town) STILL lands a settlement under the exhausted cap (forks are exempt); the tier upsell for a City fork still speaks.
8. §934.24/§934.22 PHONE FLOORS: on /create and the header at 375 every chrome label ≥ 12 px except the painted Sign In slip (registered exception, 10 px) whose tap target is ≥ 44×44 (probe getBoundingClientRect of the control); on every other route, prose ≥ 14 / chrome ≥ 12 (sample 30 leaves per route).
9. §934.26 THE PHONE FRONT: at 375 the header shows the arrow with the plaque+seal and Sign In only (no nav labels); the bottom bar reads Create, Library | Compendium, Gallery, About with a divider after Library; no Realm item; /realm at 375 shows the honest notice ("opens on a tablet or larger screen") not a blank; at 768 the Realm is reachable and the map shell renders; at 1440 the arrow shows all six labels in order Create, Library, Realm, Compendium, Gallery, About.
10. §934.27 THE PHONE LANDING: at 375 the landing shows the hero only — the four texts present, nothing below the fold (probe the DOM for the below-fold root: absent; network: no second painting fetched); at 768/1440 the full landing.
11. §934.28 + §934.35 STAFF VIEW VIA THE PREVIEW PERSONA: the preview runs with VITE_PREVIEW_ROLE=admin (the consist's .env.local) — the account menu shows the "PREVIEW PERSONA · admin" marker at its top and the "Developer" item; /admin renders the admin panel shell (not the refusal); a City/metropolis tier is not refused by the client gate (do NOT generate more than the two settlements; do not sign in). Confirm also that NO request carries a persona header (read_network_requests: no VITE_PREVIEW_ROLE / persona in any request).
12. CONSOLE: 0 errors/0 warnings on /, /create, /realm, /gallery, /compendium, /library, a dossier at 1440 and 375 (hook console.error/warn per route); specifically no React shorthand/longhand style warnings and no "Buffer is not defined" when a PDF is generated (generate one PDF through the app's own path).
13. §934.24 ADDENDUM: the create page's tier teaser shows NO "Founder" card at 1440 or 375 (find "Founder" → 0 matches on /create); the other tier cards present.
14. §934.26 ADDENDUM: on the LIBRARY at 375, scroll the main region to its end — the realm gate card's "Sign in", "See Cartographer" and the instant-generate control are all reachable above the bottom bar (probe getBoundingClientRect().bottom ≤ innerHeight − bar height for each); repeat on /create, a dossier, /compendium, /gallery: the last interactive element of each route is reachable.
15. §934.29 ONBOARDING: trigger any onboarding hint (a first dossier's save nudge, the create page's first-visit hint if one shows); navigate to another route → the hint is gone; return → it is back; close it → gone; reload → still gone.
16. §934.30 (if lane 33 is composed — check `git log --oneline -3` for "Cnocby"): the landing at 1440 shows Cnocby's facts (name, population) matching src/components/home/landingFixture.js; the advance-time card lists EVENTS with week labels (no "increased/decreased"); the realm image is the captured PNG (network: /landing-maps/realm-cnocby.png 200); /create's Founding Worlds and the library shelf list Cnocby and not Thornwell.
17. §934.26 ADDENDUM: exactly ONE link to the About route per width — at 1440 on the arrow (none in the footer); at 375 in the bottom bar (none in the footer, none in the header). Probe document.querySelectorAll('a[href*="about"]') per width and name each match's container.
18. §934.31 DIALOGS: open Feedback & Support (footer or account menu) → a labelled close control (aria-label Close) is present, Escape closes it, focus returns to the opener; repeat for any other dialog you meet (the realm advance confirm, the institution dialog on a dossier).
19. §934.24 ADDENDUM: on /create the sign-in sentence reads "…unlock thorpe, city, and metropolis, to customize, and save up to 3 drafts…" (find the text; at 1440 and 375).
20. §934.26 ADDENDUM: the realm gate card (library at 375; /realm at 768/1440) lists "Advance the realm month by month and watch the chronicle fill" and then exactly "Access wars, religion, trade, the world!" — neither old bullet (war layer / living pantheon) anywhere.
21. §934.32 (if lane 33 is composed): every settlement card without an owner image shows its tier bucket's stock picture (thorpe for thorp/hamlet, village for village/town, city for city/metropolis — the landing set's file loads 200) — the gallery cards, the create page's Founding Worlds, the library shelf; a shared gallery page's og:image meta points at the tier image when no owner image exists.
22. §934.34: anonymously, the create page's tier picker shows thorp LOCKED (like city and metropolis); attempting a thorp forge anonymously shows the tier refusal notice naming sign-in; the sentence reads "…unlock thorpe, city, and metropolis, to customize, and save up to 3 drafts…" — and anonymously the wizard's pre-generation options are VISIBLE BUT LOCKED (disabled, with the sign-in notice on interaction) and the forge runs with everything on random.
Report ≤ 900 words: PASS / FAIL / NOT OBSERVABLE per item with the probe or the screenshot path; console counts per route per width; anything else a reader would meet, with a route and a DOM anchor.
