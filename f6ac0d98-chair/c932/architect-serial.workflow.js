export const meta = {
  name: 'architect-the-simulation-932-serial',
  description: 'Architect the whole simulation build into implementation-ready lane briefs — FABLE ARCHITECTS, ONE FAMILY AT A TIME (the owner, 2026-09-15): the protocols first, then each family in boarding order with an Opus skeptic and the architect\'s own correction before the next, then the fold',
  phases: [
    { title: 'Protocols', detail: 'the cross-cutting architect (Fable) first' },
    { title: 'Families', detail: 'one family at a time, sequentially: the Fable architect writes the briefs, the Opus skeptic checks, the architect corrects' },
    { title: 'Fold', detail: 'the master ARCH-SIM document and the brief index (Fable)' },
  ],
}
const L = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a'
const S = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932'
const PRICING = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/recon-32-34/PRICING-32-33.md'
const PROG = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/recon-32-34/PROGRAMME-32-34.md'
const RECHECK = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/recon-32-34/RECHECK-32-34.md'
const OUT = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932'
const KIT = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit'
const RECON = '/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/recon-32-34'
// 19:1x EDT 2026-09-15 — the trade HISTORY CENSUS landed (RECON/HISTORY-TRADE-2026-09-15.md) and the same read runs over the
// other families as history-census-families.workflow.js (HISTORY-<CODE>-2026-09-15.md as each lands). The protocols call below is
// byte-identical to the first run so the resume replays it from cache; only the family prompts carry this block.
const HISTORY = (code) => `THE HISTORY CENSUS. The owner asked whether the past logs show these waves built in more depth under other programmes' names; the answer for TRADE is at ${RECON}/HISTORY-TRADE-2026-09-15.md and it is REQUIRED READING FOR EVERY FAMILY (its §A.1, §A.3, §A.4, the grep-trap paragraph after §B, and §C) because its findings bind any family that touches trade, goods, treaties, belief, sentiment or coin and because they set the standard every brief meets. If ${RECON}/HISTORY-${code}-2026-09-15.md EXISTS (check with ls), read it WHOLE FIRST: where it corrects PRICING §B's rows for this family, THE HISTORY FOLD WINS — its §A.4 corrected estimate, its part-built statuses, its mandatory car 0s, its RECORD≠CODE rows, its grep traps. If it does not exist yet, write 'history pass not yet run for ${code}' in every brief's STATE OF THE RECORD and proceed from the three recon documents. THE TRADE FINDINGS THAT BIND: (a) TR-5's charter is FROZEN by ODQ §96.2 ('destructive as written; no TR train compiles against it') with no re-charter recorded — the TR-5 lane's CAR 0 is the charter re-cut, and any lane whose charter the ledger has frozen carries the same car 0; (b) TR-3's belief ground truth is GENERATION-FROZEN (foodRatio is written only by src/generators/foodGenerator.js:337/:424; beliefAxisSubjects.js memoises generator lists and imports no commodity state), so the JOIN CAR to live M6a commodityStocks/commodityBand is TR-3's precondition, and until it lands lighting pactFormationEnabled buys a ONE-SHOT pact — every brief that lights a belief-fed lane declares this, and every family asks the same question of its own ground truths; (c) famine profiteering ALREADY SHIPS LIT AND UNGATED as sentiment in src/domain/factionRelationshipUpdate.js (merchant wealth +4 / trust −6; criminal +5/+4 on black-market grain, on the production path via timeProgression.js:30 and eventPipeline.js:44) — TR-6 and any wave that moves merchant or criminal sentiment on famine RECONCILES with it (own it, gate it, or fold it) or double-counts at two grains; (d) TR-1 mounted lights 6 of 16 reasons and cannot light more until TR-5/TR-6/TR-9 land the five seam producers (buildCommercialEvidence at commercialReasons.js:333-335 is fed seams = {} because nothing calls advanceCommercialReasons :457-458) — the wiring car DECLARES 6/16; (e) the volume's 'no money' premise (treatyTransfer.js's header) is FALSE since W-COIN's treasury.js — TR-2's house books, TR-4's grain credit and TR-6's corner positions are re-derived against the conserved coin before drafting; (f) GR-2 mounted the WHOLE peacetime pact lifecycle end to end (scoreTradeDemand pactTriggers.js:197 → draftPactSheet pactFormation.js:318-352, which writes NO good key though the catalog documents one at peaceTermsCatalog.js:123/:138 — a near-free car — → advancePeacetimePacts as a stage at settlementLifecycleKernel.js:564 → peaceTerms.js:715-730 → treatyOrientation.js's negotiated arm → treatyTransfer.js:100/:129), flag dark; GR-3a minted exclusivity/market_access/toll_exemption producer-less by design (peaceTermsCatalog.js:266-287; the tripwire tests/domain/peaceTermsGrantTerms.test.js:272); (g) the TR count is 0 built / 4 part / 5 unbuilt (+2b reserved) and TR prices 35–49 cars (centre 42), superseding PRICING §B's 32–45 — lighting is NOT priced per wave, it lives in the single declared window (PRICING §D); (h) three RECORD≠CODE rows outside TR to design around: ODQ §763.2 promised treasuryEnabled lit in three presets once W-COIN-2's chip landed (the chip landed at dossierViewModel.js:409; the flag is dark in every preset); §733.2 said the treasury cures the inert razing-spoils subsystem (warDeployment.js:788 still reads economicState.wealthIndex, which has zero writers); §49's LG blocker on POP-1 is DEAD (demographicsMigration.js:107-109 MIGRATION_REFUSALS landed without POP-1); (i) the twelve grep traps after the trade fold's §B are adopted verbatim (INSTR-1≠TR-1 · cornering is TR-1's reason name · the T8 trades train is a generator taxonomy car · Surveyor is the paid SKU · W-COIN is state money · houseId in brokerageServices.js is INFO's news brokerage · corner/venture in src are geometry and institution copy). THE FIRST-PAINT MARGIN IS CONTESTED: lane A0 measured 5,828 B on the plain base build (1,042,172 vs 1,048,000) where PRICING quotes 795 B (1,047,205) — treat the margin as UNMEASURED until the chair re-measures; every car that adds an eager byte names its byte rule and quotes a real build regardless. LANE STATE: A0 (headroom) has car 4 built and unproven (institutionDescVariants.js behind a dynamic import awaited at the tail of steps/index.js; worker −80,981 B); lane B struck SEAM-4 (already built as tests/lib/roadNetworkCanonReceipt.test.js, 83704ad3f) and found that lighting characterDriftEnabled would be a FALSE CLAIM (nothing calls the funnel) so it lands DARK with the lighting priced to the producer car; lane D found DESIGN_FP_ARCH_LG.md has NEVER existed on a build branch (#33 cannot author a band into an absent volume) and that ten of the twelve band prefixes have no reconciler (frozen shrink-only).`

const LAWS = `THE CROSS-CUTTING LAWS EVERY BRIEF OBEYS (the owner's words today, and the chair's rulings under the 14:4x grant): (1) EVERYTHING BUILT LANDS LIT (owner 16:5x): the feature's key is set to the lit default in the lit presets IN THE SAME CAR that builds it; the car is a DECLARED SHIFT — it names its movers, re-records the goldens it moves (the espionage fence's driven constant by its header's ritual, the prose-manifest cells by \`prose-manifest-cells.mjs --record\`, the drift manifest by its recorder), and re-charters any dormancy fence over the family it lights; the generator golden master stays 525/525 red until the owner's GENESIS signature — never re-record it; the claim is the classified base-vs-tip diff with zero UNDECLARED lines. This rule holds until the terminal soak, then everything reverts to built-dark. (2) THE FLAG-MINT CAR IS THE LAST CAR OF EACH LANE, rebased at landing, its manifest count taken from HEAD, never from a brief (PRICING §A.4.4). (3) THE BUDGETS THAT BIND (PRICING §A.3): the eager first-paint closure (\`CLOSURE_BUDGET_BYTES\` 1,048,000 vs 1,047,205 measured — 795 B margin until lane A0 buys more), the lazy engine chunk (< 676,000 vs 675,323), the generation worker (1,404,493, only generator-side code), and the SHARED-FILE LINE CEILINGS at zero slack: \`peaceTerms.js\` 797/800, \`roadsKernel.js\` 838/838, \`pulseKernel.js\` 1581/1581, \`applyWorldPulse.js\` 907 — every car that touches one of these says what it does about the ceiling (an extraction car FIRST, never a raise); the dist-gated size suites skip silently without a built dist, so every byte claim quotes a real build. (4) THE ESTATE'S CAR SHAPE: one gated commit an Opus implementer lands in one sitting; per car: the files, the pins with an executed negative control each, the census bills (lighting refreeze on a clean tree as its own commit; the mutation manifest; the wiring census stamp; prose-numerics), the gate (focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators · \`npm run build && npm run verify:dist\` under the mutex for any src/ byte · check:quick), the Seat/Lane trailer, porcelain 0; PACKET_STANDARD caps per car (≤1 behaviour family, ≤1 new persisted record family, ≤1 user-facing surface, ≤2 leaves ≤250 eff, ≤12 handwritten files); a lane is ≤ 8 cars. (5) THE LANDING RECORD IS THE CALIBRATION (PRICING §A.1): waves ship as 3–6 cars, a flag mint is a car, an L6 news kind gets its own walker file. (6) NOTHING PUSHES, DEPLOYS, APPLIES A MIGRATION, DELETES DATA OR SPENDS; a persisted-shape widening is TAKEN (every world is test data; a veto is a code revert) and recorded; the owner's five Scribe acts, the legal sitting and the golden/tuning signatures stay the owner's. (7) THE RULINGS in PRICING §A.4 (TR-4's CR-2 consolidation first; TR-4 before WY-3; TR-5 before WF-6 on peaceTerms; the flag-mint car last) and the RECHECK's twenty grep traps (§5) are adopted verbatim.`

const COMMON = `READ-ONLY except under ${OUT}: never run vitest, npm or a mutating git command; never write outside ${OUT}. THE RECORD is the ledger worktree ${L} (the family volumes docs/DESIGN_FP_ARCH_<CODE>.md; docs/DESIGN_FP_ARCHITECTURE.md §5 the compiled programme; the ODQ — locate sections with grep -n '^## §N' and read with sed -n). THE CODE is ${S} (claude/composite-r4 at a5876c0ea plus LT37's eight cars — read code there). THE THREE RECON DOCUMENTS are your starting map and you must not re-litigate them: ${PROG} (§B per-row status with evidence), ${RECHECK} (the corrected rows and the grep traps §5), ${PRICING} (§B the per-wave prices and dependencies, §C the boarding order, §A.4 the rulings). ${LAWS}`

const BRIEF_SHAPE = `EACH LANE BRIEF is a self-contained implementer document in the estate's shape, written with the Write tool to ${OUT}/briefs/brief-<lane-number>-<family>-<waves>.md: # THE LANE (its number in PRICING §C, the waves, the cars, the prerequisites with anchors) · ## THE STATE OF THE RECORD (the volume sections and ODQ anchors that charter every wave) · ## THE STATE OF THE CODE (path:line at ${S} for every module the lane touches or reuses; what exists; the shared-file ceilings it will meet) · ## ALREADY DONE (with evidence, from the recon documents) · ## THE CARS (numbered; each with: the change in one paragraph an implementer can start from; the files; the pins and their negative controls; the census bills; the size/byte rule for that car; the gate; the LIT act — which key, which presets — and the declared-shift receipt; the car's own exit criterion) · ## THE FLAG-MINT CAR (last; the manifest count taken from HEAD) · ## OWNER-GATED KEPT OUT (only the standing carve-outs; persisted shapes are TAKEN and recorded) · ## RISKS (the grep traps that apply, the collision surfaces, the load hazards) · ## GATES (the per-car gate list verbatim). Every figure labelled CONFIRMED (a recon skeptic confirmed it) or PLAUSIBLE. Layman's terms in the one-paragraph car descriptions; exact identifiers everywhere else.`

const FAMILIES = [
  { code: 'TR', lanes: 'PRICING §C lanes 1 (TR-4), 2 (TR-5), 7 (TR-1 mount → TR-3), 8 (TR-6), and TR-2/TR-2b/TR-7/TR-8/TR-9 by family after' },
  { code: 'WF', lanes: 'PRICING §C lanes 5 (WF-6), 16 (WF-0 → WF-3), 17 (WF-2), 18 (WF-4), 19 (WF-5a/b), 20 (WF-7), 21 (WF-8), 22 (WF-9)' },
  { code: 'POP', lanes: 'PRICING §C lanes 3 (POP-1), 23 (POP-3 → POP-4), 24 (POP-5a), 25 (POP-5b), 26 (POP-7 → POP-2), 27 (POP-6)' },
  { code: 'WY', lanes: 'PRICING §C lanes 4 (WY-0 → WY-1), 28 (WY-2), 29 (WY-3 with the roadsKernel extraction car FIRST), 30 (WY-4), 31 (WY-5), 32 (WY-6), 33 (WY-8a — unblocks WC-10..16), 34 (WY-7), 35 (WY-8b/9/10/11)' },
  { code: 'IN', lanes: 'PRICING §C lanes 6 (IN-4), 10 (IN-5), 36 (IN-1c-b), 37 (IN-2), 38 (IN-3), 39 (IN-6)' },
  { code: 'ES', lanes: 'PRICING §C lanes 9 (ES-6b), 11 (ES-7 a+b), 12 (ES-7 c+d) — the espionage dormancy fence is RE-CHARTERED by the lane that lights the layer' },
  { code: 'WC', lanes: 'PRICING §C: WC-1..WC-9 by family (≤ 8 cars a lane; WC-1 and WC-6 break several PACKET_STANDARD caps — split them), then WC-10..16 after WY-8a' },
  { code: 'HB', lanes: 'PRICING §C: HB-3..HB-9 by family (the two persisted habit fields exist dark; Q1 is three stamped fields)' },
  { code: 'EP', lanes: 'PRICING §C: EP-4 onward incl. §7a row 4 (FORCE_RESETTLE — a same-seed shift, declared) and the one unflagged live product break (EP-4 s1, H1)' },
  { code: 'INT', lanes: 'PRICING §C lanes 40 (INT-1), 41 (INT-2), 42 (INT-3a — the EAGER stressorsCore.js row measured against a built dist), 43 (INT-4 ∥ INT-5), 44 (INT-6), and INT-7/INT-8 after' },
  { code: 'GR-CW', lanes: 'PRICING §C lanes 13 (SC-6 causedBy walker → CW-1), 14 (CW-2x), 15 (CW-3 the terminus), and GR-4/GR-5/GR-6/GR-7 by family (GR-4/GR-6 are mount-blocked by pulseKernel.js 1581/1581 — an extraction car first)' },
  { code: 'LG', lanes: 'PRICING §B "LG — #33 first train": LG-0 (re-priced against the built circle network) and LG-1 (the allied predicate + the toll signed at §931.6b item 5); then LG-2 (the commitment ledger, signed item 7) and LG-3/4 as the next lanes; the Q10 band-walker prefix is lane D (in flight)' },
]

// The skeptic's verdict schema (was referenced but never defined — the first two runs died before the skeptic step).
const VERDICT = { type: 'object', properties: { briefs: { type: 'array', items: { type: 'object', properties: { path: { type: 'string' }, lane: { type: 'string' }, startable: { type: 'string', enum: ['YES', 'NO'] }, gaps: { type: 'array', items: { type: 'object', properties: { car: { type: 'string' }, missing: { type: 'string' } }, required: ['car', 'missing'] } }, notes: { type: 'string' } }, required: ['path', 'lane', 'startable', 'gaps'] } }, summary: { type: 'string' } }, required: ['briefs', 'summary'] }

// From POP on (the owner, 23:1x: it is progressing slow), agents read the 20 KB ARCHITECTS-CARD.md instead of the 77 KB protocols and open the
// protocols only at the section a car needs. WF keeps the full read so its cached and in-flight prompts stay valid.
const READS = (code) => `Read ${OUT}/ARCHITECTS-CARD.md WHOLE (the chair's distillation of the protocols and the addendum; open ${OUT}/ARCH-PROTOCOLS.md only at the section a car needs, and ${OUT}/ARCH-PROTOCOLS-ADDENDUM-1.md only for the exact wording of a ruling you cite) and`
// THE CHECKPOINT RULE (the owner, 23:4x: progress is saved per agent per lane, regularly, for when a death happens or the usage window
// is spent). Gated so agents already finished or in flight keep their cached prompts: the WF architect (in flight) is exempt.
const CHECKPOINT = (dir, label) => `CHECKPOINT RULE: (1) at your FIRST step create ${dir}/progress-${label}.txt (mkdir -p ${dir}) and APPEND one line after every meaningful step (a file read and its finding; a claim confirmed; a brief section written) in the form 'HH:MM · <step> · <what is now known> · next: <the next step>'; (2) write each output file EARLY with its skeleton and RE-WRITE it after every section you finish (the Write tool overwrites; a half-written file with finished sections is worth more than a perfect file that never lands); (3) if ${dir}/progress-${label}.txt ALREADY EXISTS when you start, you are RESUMING after a death or a spent window: read it and every partial output it names FIRST, keep everything finished, and continue from its last 'next:' line, never from scratch. Return only when your output is complete; your last progress line says 'DONE'.`
const PLAN_SCHEMA = { type: 'object', properties: { planFile: { type: 'string' }, lanes: { type: 'array', items: { type: 'object', properties: { laneNo: { type: 'string' }, waves: { type: 'string' }, briefFile: { type: 'string' }, seed: { type: 'string' } }, required: ['laneNo', 'waves', 'briefFile', 'seed'] } }, summary: { type: 'string' } }, required: ['planFile', 'lanes', 'summary'] }

phase('Protocols')
const protocols = await agent(`${COMMON}\n\nYOU ARE THE CROSS-CUTTING ARCHITECT (the Fable seat). Write ${OUT}/ARCH-PROTOCOLS.md with the Write tool: (1) THE LIT-LANDING PROTOCOL — exactly how a car sets its key lit (read simulationRules.js:644-654 WAVES, src/lib/flagRegistry.js, the presets, subsystemRowsVirtual.js's certification row law, contributionLedgerShape.test.js's literals, the engine-gated rule-keys walker, the edge-shared MODIFY rows), which suites red when a key lights and what each needs; (2) THE DECLARED-SHIFT RECEIPT — the exact commands and files for re-recording the espionage fence constant (read tests/property/espionageDormancyFence.test.js's golden header for the last two windows' ritual), the prose-manifest cells, the drift manifest; the base-vs-tip classified diff (the chair's ${KIT}/c931-f6ac0d98/declared/__chair_dump.test.js and classify.py — extend classify.py's pattern table per family); the GOLDEN_SHIFT_LEDGER.md entry shape; (3) THE FENCE PROTOCOL — every dormancy/reachability fence in tests/ (grep for 'FENCE', 'dormancy', 'unreachable', 'terminates') with the family it guards and how its lane re-charters it; (4) THE BUDGET PROTOCOL — the four budgets with their current measured values, the dist-gated skip hazard, the per-car measurement commands, and the shared-file ceilings table (peaceTerms.js · roadsKernel.js · pulseKernel.js · applyWorldPulse.js · every file within 20 lines of a size baseline — read tests/lint/sizeBaseline.test.js and scripts/.size-baseline.json) with the extraction-first rule; (5) THE FLAG-MINT CAR — its exact file list and the count-from-HEAD rule; (6) THE COMPOSE PROTOCOL — the consist laneCONSIST-932 at a5876c0ea, compose.sh + the Seat/Lane message filter, the three-way byte check, the register pass with every register re-stamped at the consist tip BEFORE the totals car, the ratchet under the quiet law, totals, capsule, seal, gate, chair-verify, CAS (read ${KIT}/c931-f6ac0d98/*.sh and the ledger's §931.3 and §931.5); (7) THE SERIALIZATION TABLE — which lanes may run together and which must serialize on a shared file. Return a 15-line summary.`, { label: 'arch:protocols', phase: 'Protocols', effort: 'high' })

phase('Families')
const summaries = []
const refuted = []
for (const f of FAMILIES) {
  log(`Architecting family ${f.code} (Fable, one at a time)`)
  // Resume-awareness (the owner, 22:4x: one recon and one architecture at a time so a death loses little): from the SECOND family on,
  // the architect keeps any brief already written for its own lanes (a death mid-family) and writes only the missing ones. The first
  // family's prompt is unchanged so its cached result still replays.
  const prior = summaries.length ? `THE BRIEFS ALREADY WRITTEN (read them under ${OUT}/briefs/ before you write — shared files, flag-mint counts and the serialization table must agree across families): ${summaries.map(x => x.slice(0, 1500)).join(' || ')} \n\nRESUME RULE: if a brief for one of YOUR OWN lanes already exists under ${OUT}/briefs/ (ls it), it was written by you before a session death — READ IT, KEEP IT (fix only what the addendum ${OUT}/ARCH-PROTOCOLS-ADDENDUM-1.md now requires of it) and write ONLY the lanes still missing; never rewrite a finished brief from scratch. Also read ${OUT}/ARCH-PROTOCOLS-ADDENDUM-1.md WHOLE: its rulings R1–R5 (the virtuality law amended for the SIM_LIT cohort; the lighting-order law and the door train D0 that lands first; the mint/lighting split; the 7a/7b split) bind every family — cite it by path at every LIT car instead of re-deriving the law.` : 'You are the first family; ARCH-PROTOCOLS.md is written — read it first.'
  if (f.code === 'TR') {
    const a = await agent(`${COMMON}\n\n${BRIEF_SHAPE}\n\n${HISTORY(f.code)}\n\nYOU ARE THE ARCHITECT FOR FAMILY ${f.code} (the Fable seat). Read ${OUT}/ARCH-PROTOCOLS.md first. ${prior}\nYour lanes: ${f.lanes}. Read the family's volume, PRICING §B's rows for it and §C's rows, the RECHECK's corrections, and the code at ${S} for every module you name. Write ONE BRIEF PER LANE (≤ 8 cars each; split a lane that would exceed 8). Then return a summary as your final text: for each brief its path, its cars and their car-count range, its prerequisites, its shared-file ceilings met, and the three things an implementer is most likely to get wrong.`, { label: `arch:${f.code}`, phase: 'Families', effort: 'high' })
    if (!a) { log(`architect ${f.code} returned nothing`); continue }
    const v = await agent(`${COMMON}\n\nYOU ARE THE SKEPTIC FOR FAMILY ${f.code}'s briefs (Opus verifies). The architect's summary:\n${String(a).slice(0, 20000)}\n\nOpen every brief under ${OUT}/briefs/ for this family. For each, answer as an implementer: could I start car 1 in the next ten minutes without asking a question? Check: every car names its files and the change; every pin has a negative control; every car that touches src/ names its byte rule and the build measurement; every car that touches a shared file at its line ceiling names the extraction car first; the LIT act names the key and the presets and the declared-shift receipt; the dependencies match PRICING §C and the volume; nothing re-finds a RECHECK grep trap; the flag-mint car is last; nothing is owner-gated except the standing carve-outs; AND the brief agrees with the history census (${HISTORY(f.code)} — a brief that builds what the history fold shows already built, ignores a mandatory car 0, lights a belief-fed lane without declaring the one-shot outcome, or moves famine sentiment without reconciling the live producer is NOT startable). DO NOT edit the briefs — report the gaps precisely (path, car, what is missing) so the architect can correct them. Mark each brief startable YES/NO.`, { label: `refute:${f.code}`, phase: 'Families', schema: VERDICT, model: 'opus', effort: 'high' })
    refuted.push(v)
    const gaps = v ? v.briefs.filter(b => b.startable === 'NO') : []
    let final = a
    if (gaps.length) {
      log(`${f.code}: ${gaps.length} brief(s) not startable — the architect corrects`)
      final = await agent(`${COMMON}\n\n${BRIEF_SHAPE}\n\n${HISTORY(f.code)}\n\nYOU ARE THE ARCHITECT FOR FAMILY ${f.code} (the Fable seat), CORRECTING your briefs on the skeptic's findings. The skeptic marked these NOT STARTABLE with these gaps (JSON):\n${JSON.stringify(gaps).slice(0, 20000)}\nOpen each named brief under ${OUT}/briefs/, fix every gap in place with the Write tool (read the code at ${S} where the gap is a missing path:line), and return the corrected summary for the family (paths, cars, prerequisites, ceilings, the three likeliest mistakes).`, { label: `arch:${f.code}:fix`, phase: 'Families', effort: 'high' })
    }
    summaries.push(`${f.code}: ${String(final || a).slice(0, 2000)}`)
  } else {
    // THE SEQUENTIAL FAMILY (the owner, 23:2x: "just do it sequentially in the architect lane"): ONE Fable architect writes the family's
    // briefs, the Opus skeptic checks them, the architect corrects. The card replaces the 77 KB protocols read; a plan file left by an
    // earlier run (plans/plan-<CODE>.md) is followed if it exists; briefs already on disk are kept (the RESUME RULE in `prior`).
    const a = await agent(`${COMMON}\n\n${BRIEF_SHAPE}\n\n${HISTORY(f.code)}\n\nYOU ARE THE ARCHITECT FOR FAMILY ${f.code} (the Fable seat). ${f.code === 'WF' ? '' : CHECKPOINT(`${OUT}/progress`, `arch-${f.code}`) + ' '}${READS(f.code)} then, if ${OUT}/plans/plan-${f.code}.md EXISTS (ls it), read it WHOLE and follow its lane table, shared-file table, car 0s and serialization rows (it was written for this family by a planner before the owner asked for the sequential shape; where the code contradicts it, follow the code and say so). ${prior}\nYour lanes: ${f.lanes}. Read the family's volume, PRICING §B's rows for it and §C's rows, the RECHECK's corrections, the history fold if it exists, and the code at ${S} for every module you name. Write ONE BRIEF PER LANE (at most 8 cars each; split a lane that would exceed 8 into sittings a/b, once). Then return a summary as your final text: for each brief its path, its cars and their car-count range, its prerequisites in both directions, its shared-file ceilings met, and the three things an implementer is most likely to get wrong.`, { label: `arch:${f.code}`, phase: 'Families', effort: 'high' })
    if (!a) { log(`architect ${f.code} returned nothing`); continue }
    const v = await agent(`${COMMON}\n\nYOU ARE THE SKEPTIC FOR FAMILY ${f.code}'s briefs (Opus verifies). ${CHECKPOINT(`${OUT}/progress`, `refute-${f.code}`)} Write your verdict-in-progress to ${OUT}/progress/verdict-${f.code}.md brief by brief as you finish each. Read ${OUT}/ARCHITECTS-CARD.md first (the protocols distilled; open ARCH-PROTOCOLS.md only where a brief's claim needs the full section). The architect's summary:\n${String(a).slice(0, 20000)}\n\nOpen every brief under ${OUT}/briefs/ for this family. For each, answer as an implementer: could I start car 1 in the next ten minutes without asking a question? Check: every car names its files and the change; every pin has a negative control; every car that touches src/ names its byte rule and the build measurement; every car that touches a shared file at its line ceiling names the extraction car first; the LIT act names the key, the presets and the declared-shift receipt AND obeys ${OUT}/ARCH-PROTOCOLS-ADDENDUM-1.md (R1 the SIM_LIT cohort; R2 the lighting order and the door D0; R3 AS AMENDED: the dark-mint car carries the row, the three test edits and both literals off HEAD at its base, and the last car re-reads them; R5 the split); the dependencies match PRICING §C and the volume in BOTH directions; nothing re-finds a RECHECK grep trap or contradicts the history fold; nothing is owner-gated except the standing carve-outs; the briefs AGREE WITH EACH OTHER on shared files and flag counts (name every disagreement as a gap on both briefs). DO NOT edit the briefs; report the gaps precisely (path, car, what is missing) so the architect can correct them. Mark each brief startable YES/NO.`, { label: `refute:${f.code}`, phase: 'Families', schema: VERDICT, model: 'opus', effort: 'high' })
    refuted.push(v)
    const gaps = v ? v.briefs.filter(b => b.startable === 'NO') : []
    let final = a
    if (gaps.length) {
      log(`${f.code}: ${gaps.length} brief(s) not startable; the architect corrects`)
      final = await agent(`${COMMON}\n\n${BRIEF_SHAPE}\n\n${HISTORY(f.code)}\n\nYOU ARE THE ARCHITECT FOR FAMILY ${f.code} (the Fable seat), CORRECTING your briefs on the skeptic's findings. ${CHECKPOINT(`${OUT}/progress`, `fix-${f.code}`)} Read ${OUT}/ARCHITECTS-CARD.md first. The skeptic marked these NOT STARTABLE with these gaps (JSON):\n${JSON.stringify(gaps).slice(0, 20000)}\nOpen each named brief under ${OUT}/briefs/, fix every gap in place with the Write tool (read the code at ${S} where the gap is a missing path:line; where a gap says two briefs disagree, make them agree), mark each header CORRECTED with the date, and return the corrected summary for the family (paths, cars, prerequisites both ways, ceilings, the three likeliest mistakes).`, { label: `arch:${f.code}:fix`, phase: 'Families', effort: 'high' })
    }
    summaries.push(`${f.code}: ${String(final || a).slice(0, 2500)}`)
  }
}
const good = summaries

phase('Fold')
const fold = await agent(`${COMMON}\n\nYou are the chair's folder. ${CHECKPOINT(`${OUT}/progress`, 'fold')} The Fable architects wrote the lane briefs under ${OUT}/briefs/ ONE FAMILY AT A TIME (with ${OUT}/ARCH-PROTOCOLS.md first); an Opus skeptic checked each family and the architect corrected the gaps. The family summaries:\n${good.join('\n\n')}\n\nThe skeptics' verdicts (JSON):\n${refuted.filter(Boolean).map(r => JSON.stringify(r).slice(0, 8000)).join('\n\n')}\n\nTHE HISTORY CENSUS FOLDS under ${RECON}/HISTORY-*-2026-09-15.md (ls them; the trade one is required reading, §A.1/§A.4/§C) correct the recon rows per family — the master document carries their corrected counts and estimates, never PRICING's stale rows. Write ${OUT}/ARCH-SIM-2026-09-15.md with the Write tool — THE MASTER ARCHITECTURE OF THE SIMULATION BUILD, one coherent document: (A) in plain words for the owner, what will be built and in what order, with the honest total (PRICING §A.1: #32 400–610 cars, #33's first train 11–17, then LG-2 onward) and the critical path; (B) THE BOARDING ORDER as a numbered table of lanes (from PRICING §C, adjusted by the skeptics), each with its brief path, cars, prerequisites, shared-file ceilings and the four-lane cadence (which four board together at each step, and why); (C) THE CROSS-CUTTING PROTOCOLS summarised with a pointer to ARCH-PROTOCOLS.md; (D) THE DOORS THAT MUST OPEN FIRST (the first-paint margin, the engine chunk, the three shared-file ceilings, the Q10 prefix, W-OPS wiring car A for ES-7) and which lane opens each; (E) THE RECEIPTS every landing produces (the declared-shift receipt, the goldens, the fence re-charters, the registers) and the §932→§9xx landing cadence; (F) WHAT STAYS THE OWNER'S; (G) THE BRIEF INDEX (every brief path with startable YES/NO and the skeptic's gaps that remain). Then return a 15-line summary: the lane count, the startable count, the doors, and the first four lanes to dispatch.`, { label: 'fold', phase: 'Fold', effort: 'high' })
return { summary: fold, families: good.length, skeptics: refuted.filter(Boolean).length, protocols: String(protocols).slice(0, 2000) }