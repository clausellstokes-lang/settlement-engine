You are the implementer for §932 LANE B — ITEM #34's FREE REMAINDER (the political, economic and heraldic programmes' unbuilt units that need no owner answer). Read /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/briefs/PLAN-932-2026-09-15.md (lane B) and then /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/briefs/PROGRAMME-32-34-2026-09-15.md (§B.3 the per-row table for #34, §C.3 lane 2, §E items 3–7) and OWNER-QUESTIONS-32-34-2026-09-15.md items 4, 13, 15, 16 with the RULINGS under the sheet in docs/OWNER_SITTING_2026-09-15.md at HEAD of the ledger branch (read it with `git -C /Users/cstokes/Desktop/settlement-engine show HEAD:docs/OWNER_SITTING_2026-09-15.md`). Your dock is /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/lane-932-lt34, cut at a5876c0ea (THE NEW BUILD SLOT; porcelain 0; node_modules symlinked).

Cars, in order, each DARK behind its existing key or display-only, NON-OUTPUT-MOVING under §764.3 (any car that would move same-seed output is HELD with its diff kept as a file under docs/implementation/ and reported): 1–2 the two W-OPS wiring cars (the feed envelope + desk registration; the errand mint site — read `src/domain/certification/subsystemRowsOps.js` :193 and :239 first; the content stays unsigned behind the doors exactly as the leaves did); 3 ENC-5's grudge fold lifted at `envoyChanceMeetingLedger.js:70`, `npcLadderKernel.js:409`, `envoyChanceMeetingStage.js:717` under `chanceEncountersEnabled`; 4 the drift-door car — `characterDriftEnabled` given its manifest home (the chair's ruling under TE-VIRT-1; read `envoyChanceMeetingStage.js:61` and the L-HOMES-7 pricing in the ledger); 5 W-COIN-4 (the display row + realm aggregation; the treasury is BUILT and dark — read `src/domain/worldPulse/treasury.js`'s header and `dossierViewModel.js:382`); 6–9 the WEAVE stragglers SEAM-4 · NAME-2 (behind `markovNamingEnabled`) · POLIS-5 · ST-1 — VAR-1b and VAR-2 are NOT yours (VAR-1b is a declared shift → lane A); 10 SEAT-8 behind `irregularForceEnabled` with the `civilContests` ledger shape (GRANTED by the chair under the owner's grant; `subsystemRowsSeat.js:162`, `treasury.js:490/:1036`, `treasuryConservation.walker.test.js:197` — the conservation walker must stay green with the new shape parameterised). ⚠ Every car that touches src/ runs the build and quotes the worker-bundle line: the worker is at its ceiling until lane A0 lands — a car that adds worker bytes WAITS for A0 (check `refs/preserve/lane-932-headroom-*`), does not squeeze.

⭐⭐⭐ THE OWNER (2026-09-15 16:5x EDT): "from this moment forward everything that we built is built lit on." EVERY CAR YOU LAND BUILDS LIT: set the feature's key to the lit default in the lit presets (`simulationRules` WAVES / the flag registry; read `simulationRules.js:644-654` and `src/lib/flagRegistry.js` for the shape) IN THE SAME CAR that builds it; the car is therefore a DECLARED SHIFT — name every mover in the body, re-record the goldens it moves (the espionage fence's driven constant per its header's ritual, the prose-manifest cells by `prose-manifest-cells.mjs --record`, the drift manifest by its recorder) in the same car, and if your feature has a dormancy fence, RE-CHARTER it in the car that lights it (the fence's own header says how). The generator golden master stays 525/525 red — never re-record it; the claim is the classified base-vs-tip diff (zero UNDECLARED). `FLAGS.scribe` is the one exception (the owner's).

Rules that bind absolutely: never `git stash`, `git checkout -- <file>`, `git reset --hard`, never push, never deploy, never `supabase db push`, never edit package.json or add a dependency, never raise any size or build ceiling (`tests/lint/sizeBaseline.test.js`, the `tests/build/` ceilings incl. `WORKER_BUNDLE_CEILING_BYTES`, the first-paint and CSS budgets are exact and frozen — lower them when a car shrinks, never raise), never touch /Users/cstokes/Desktop/settlement-engine or any other dock; commit by explicit pathspec after `git diff --cached --stat`; porcelain 0 after every car; pay every census bill a changed or new test file owes (the lighting census re-freeze on a CLEAN tree as its own commit; the mutation-coverage manifest; the wiring census stamp; the prose-numerics baseline). EVERY commit message carries, above the Co-Authored-By line, exactly `Seat: Opus 5 — Fable-unvalidated` and `Lane: <your lane>`. THE GATE per car: the focused vitest files · `tests/lint/` whole · `tests/copy/` whole · `tests/property/` whole · `tests/edgeFunctions/` whole · `tests/security/` whole · `tests/generators/` for any generator touched · `npm run build && npm run verify:dist` under `sh scripts/gate-mutex.sh --run --` for ANY car that touches src/ (quote the worker-bundle line and the first-paint byte table verbatim) · `npm run check:quick` — ⛔ `scripts/gate-mutex.sh` GIVES UP AFTER 40 POLLS AND EXITS 0 when another lane holds the mutex; a gate line with no printed test count DID NOT RUN — re-run until it prints a count. ⛔ HOLD RULE: before EVERY vitest / npm run / build, if the file /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/HOLD-VITEST exists, sleep 60 and re-check — run nothing while it exists. Quote `uptime` with every gate line; a timeout at load > 8 is measured alone before it is called a defect. Every pin gets an executed negative control. No em dash (U+2014) and no exclamation point in any new user-facing string. Do not seat model agents; no subagents; no Agent tool. If a gate stays red after three distinct hypothesis-driven attempts, stop and report the exact red. When you finish, write your final report ALSO to /private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/report-<lane>.md and append one line per car to progress-<lane>.txt beside it.
