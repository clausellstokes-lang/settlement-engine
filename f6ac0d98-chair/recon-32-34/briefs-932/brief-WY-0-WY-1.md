# BRIEF — LANE 4 · WY-0 THE LAWFUL CANVAS → WY-1 THE SCALE CHARTER (no flags; WY-1 data-gated by `spatialDigest.kmScale`)

Implementer brief, §932, lane 4 of the simulation build. Self-contained. Cut the dock from the build slot `claude/composite-r4` @ `a5876c0ea`. Every figure is **CONFIRMED** (the WY skeptic re-opened the file at `a5876c0ea`) or **PLAUSIBLE** (a reader's claim untested). Line numbers rot; re-measure by symbol at charter. WY-0 2–3 cars + WY-1 3–4 cars = 5–7 (CONFIRMED ranges). Nothing here pushes, deploys, applies a migration, deletes data, or spends. **WY-0 car 2 is one of only two WY rows that MOVE OUTPUT AT LANDING** — the ledger row says so out loud.

## THE STATE OF THE RECORD (anchors)

- Governing volume: `docs/DESIGN_FP_ARCH_WY.md` (ledger `a803dee7a`, 1,721 lines) — §5 LANE S calls WY-0 "buildable NOW; the program's first commit in either lane"; `SOL_QUEUE.md` §2 Lane B orders it as the early opener; §5b item 3 places WY-1 at the PHASE 1 tail after SP-E (LANDED — `tests/helpers/kindPoolWalker.js`, `components/map/heraldFeed.js`), so WY-1 is unblocked today; §5b item 7's WY prefix admission is PAID (`tests/domain/couplingRegistry.test.js:108` and the `toEqual` at `:587`, CONFIRMED); §5b item 9 requires TWO pointer sentences in `DESIGN_WAR_RULINGS_ARCHITECTURE.md` in WY-1's fold commit so the two binding volumes never disagree.
- Chair rulings this lane carries, all vetoable: **Q3** — the `WarFaithMapOverlay` deployment arrows are DELETED under the ambiguity law (§7 Q3), with the owner's veto window OPEN UNTIL WY-0 CAR 2 LANDS — car 2's ledger row names it owner-visible; **§1a** — WY-1's flag home is decoupled from `portOpportunityEnabled` (data-gated by the datum, no flag); the two moved test files are a declared one-time shift against directive 2c.
- Owner: build signed under the 14:4x grant and §931. `MODE_SPEEDS` (km/week) and the default `kmScale` are §8 TUNING rows — raw-authored until the owner's soak signature; never enter `proposedSoakBands` before ratification. The F9 doc contradiction is live (slot `DESIGN_FP_ARCHITECTURE.md:669` "UNSIGNED" vs ledger `:669` "SIGNED"; the slot self-contradicts at `:678/:1891`) — not this lane's field, but WY-1's fold commit may catch the slot copy up in passing (docs only).

## THE STATE OF THE CODE (read at `a5876c0ea`)

WY-0:
- `DEFAULT_LAYERS` at `src/store/mapSlice.js:88` carries **22 keys — 17 boolean rows and 5 config keys** (`relationshipFilter`, `chainFilter`, `regionalChannelFilter`, `regionalImpactStatusFilter`, `regionalMinSeverity`) (CONFIRMED; the reader's 23 and r1's "thirteen" are both wrong). The walker's anti-vacuity floor is 17, MEASURED AT BUILD.
- `travelersFilter` is UNDECLARED (CONFIRMED): written on demand by `setLayerFilter` at `mapSlice.js:382-384` (NOT `:328-330`), which — unlike `toggleLayer` at `:377-380` — has NO declared-key guard; read at `LayersPanel.jsx:95-103` and `TravelersLayer.jsx:40`. `mapSlice.js:109`'s own comment prices a key at ~20 B eager.
- Deletion targets (CONFIRMED): `chevronPoints` imported at `TravelersLayer.jsx:25`, rendered at `:148`; both `angleDeg+180` flips at `:87` and `:126`; the marker-end arrow machinery at `WarFaithMapOverlay.jsx:152/:196` on a default-ON public-tier surface. After deletion `chevronPoints` (`travelersGeometry.js:84`) has ZERO consumers tree-wide — a dead-export disposition is owed.
- Shipped test files that move (CONFIRMED): `tests/components/travelersLayer.test.jsx`, `tests/domain/roadsTravelersGeometry.test.js`.
- Sizes (CONFIRMED, effective): `LayersPanel.jsx` 364, `MapOverlay.jsx` 284, `MapLegend.jsx` 180, `WarFaithMapOverlay.jsx` 158, `TravelersLayer.jsx` 112, `WorldMapStage.jsx` 390 — all under the 600 component ceiling, none in `scripts/.size-baseline.json`.
- ⛔ THE EAGER FIRST-PAINT CLOSURE IS THIS LANE'S ONLY BYTE CONSTRAINT: `store/index.js:49` statically imports `mapSlice.js`; `CLOSURE_BUDGET_BYTES = 1_048_000` (`tests/build/vendorPdfLazy.test.js:565`) against a last-MEASURED 1,047,205 (`:539`) — **795 B**. The suite is `it.skipIf(!requireDistRead)` at `:957` and SKIPS WITHOUT DIST. Every byte claim quotes a measured build.

WY-1:
- ABSENT (CONFIRMED): `src/domain/spatial/modeSpeeds.js`; `kmScale` zero hits in src and tests.
- `hopWeeks` at `src/domain/spatial/distanceRead.js:639` (468 eff — no leaf split owed); `buildSpatialDigest` at `spatialDigest.js:580` (415 eff); `campaignSpatialCanonize.js` 98 eff (already performs the prior-worldState read for the biome flag — the carry-forward rides it); `MAX_MARCH_WEEKS: 52` at `armyTransit.js:59` with the below-`SIEGE_MAX_AGE` invariant in its docstring at `:662-664`; the `gradeMultiplier` socket injected at `namedPersonTransit.js:44` (seam 6 — ONE position model, no second speed floor, or law M's walker reds).
- ⚠ THE SPECTRUM-DERIVATION CENSUS SURFACE IS **37 FILES** (CONFIRMED by grep): MapOverlay, TravelRingsLayer, TravelersLayer, edgeAnnotations, subsystemRowsCompact, settlementPestilence, seaRoads, roads/state, roads/verification, armyTransit, distanceRead, migration, navalLayer, pestilence, rumorNetwork, spatialArrival, supplyShipments, applyWorldPulse, armyTransitKernel, beliefMap, calamityKernel, candidateEvents, distancePricedNews, migrationKernel, npcCirculationTransit, pactProposals, pestilenceKernel, pulseKernel, realmVerbExecution, roadsKernel, routeNetworkCharter, routeNetworkConsumersTransit, routeNetworkFlowsObjective, seatIntervention, sovereigntyReach, spatialUsage, mapSlice. A shrink-only census over it is a car, not a bullet.
- A store-reachable static edge into `spatial/distanceRead.js` was severed once before (the vendorPdfLazy WAVE-D seam repair); a new one re-drags the whole spatial graph into the 795 B margin.
- Worker: an independent BFS from `src/workers/generation.worker.js` reaches 219 modules with ZERO `domain/worldPulse/` and ZERO `domain/spatial/` (CONFIRMED); `advanceInterval.worker.js` (545 modules) carries no byte ceiling. WY-1 adds ~3–5 KB to that graph (PLAUSIBLE); WY-0 adds ~20 B EAGER and nets NEGATIVE lazy-side.

## ALREADY DONE (do not re-land)

- The WY prefix admission (both sites). SP-E (the kind-pool walker helper, the herald feed). `hasMartialRecord` exported at `martialReadiness.js:256` (the duplicate local at `opportunism.js:122` is WY-7's, not this lane's). The both-direction walker idiom (`premiumGateSingleSource` is the named model). The `MAX_MARCH_WEEKS` clamp and its invariant docstring.
- NOT done and NOT this lane's: `WarFaithTab.jsx` no longer exists (WarTab / FaithTab / WarFaithDesk) — WY-7/8b navigate by symbol.

## THE CARS

Every car: one gated commit; anchored negatives; executed mutants in `scripts/mutation-coverage-manifest.json`; full gate on the slot with a printed test count; a MEASURED dist build for any byte claim; worker-bytes rule stated.

**WY-0 car 1 — the layer-manifest walker + `travelersFilter` declared + the merge pin. Moves NO output.** NEW `tests/ui/mapLayerManifest.walker.test.js`: `DEFAULT_LAYERS` keys ↔ `LayersPanel` rows ↔ the overlay mounts ↔ `MapLegend` entries, BOTH directions; the structural config-key exemption class (a non-boolean default value implies config — the five `*Filter`/`regionalMinSeverity` keys exempt themselves, never a hand list); the anti-vacuity floor = the boolean row count measured at build (17 today, asserted ≥ a floor that only rises); the census domain = declared keys ∪ source-scanned `setLayerFilter`-written keys. C-WYF-4's cure: declare `travelersFilter: null` in `DEFAULT_LAYERS` (or give `setLayerFilter` `toggleLayer`'s guard — pick one, record why). The persisted-layers MERGE pin traces the spread order so a new default key reaches old campaigns with its default (the ghost-write class: a key that survives creation and vanishes on load). Negative controls: an undeclared key planted in a panel row → the walker reds both ways; a boolean row removed → the floor reds. Byte rule: ~+20 B eager, MEASURED against a built dist and quoted with the closure figure; if the measured margin is under 100 B after this car, STOP and report — the ceiling act is the chair's.

**WY-0 car 2 — the deletions. MOVES OUTPUT AT LANDING (no flag; declared).** `TravelersLayer` chevrons/flips/tooltips (C1) and the `WarFaithMapOverlay` deployment arrows (Q3), both halves in one commit but each revertible on its own hunk; the two shipped test files MOVED (not deleted) with their retained assertions re-homed; `chevronPoints`'s dead-export disposition (delete with an anchored absence pin, or keep with a recorded reason — never leave it silently orphaned). Gate: anchored negatives that no chevron/flip/tooltip/arrow path renders (`// anchored:` on each), the moved tests green, the layer walker from car 1 green (the deleted mounts leave no orphan rows), sr-only/legend fallout measured. The ledger row NAMES: Q3 as owner-visible with the veto window closing at this landing; the two moved files as the declared one-time shift against 2c; the canvas change as output-moving. Byte rule: nets NEGATIVE lazy-side (measured); zero eager delta expected — quote it.

**WY-1 car 1 — the mechanism.** NEW `src/domain/spatial/modeSpeeds.js` (~60 eff): the `MODE_SPEEDS` km/week table (raw-authored, frozen, a derivation comment per row — §43 rationale-or-STOP; ratified only at the soak). `distanceRead.js`'s `hopWeeks`/calibration gains the `kmScale` arm plus the spectrum surface; `buildSpatialDigest` stamps the datum; `campaignSpatialCanonize` supplies it — creation-supplied on FIRST canonize, PRIOR-DIGEST CARRY-FORWARD on every receipted re-canonize, riding the same prior-worldState read the body already performs for the biome flag. Seam 6: the `gradeMultiplier` socket at `namedPersonTransit.js:44` is the ONE position model — no second speed floor anywhere (law M's walker). The absent-datum arm IS the legacy arm and is the world's identity forever — not a migration. Gate: the LEGACY-ABSENT BYTE-IDENTITY GOLDEN (every existing world lacks `kmScale`; same-seed serialized state byte-identical before/after across the dormancy corpus and the ONE_REGEN preset). Byte rule: ZERO eager delta (no store-reachable static edge into `distanceRead.js` — assert by a measured closure and by the vendorPdfLazy seam pin); ~3–5 KB into the advance graph (no ceiling; state the measured figure).

**WY-1 car 2 — the instrument family (where the money is).** THE RE-CANONIZE ROUND-TRIP (THE PROMISE class, the volume's r2 HIGH pin): canonize scaled → founding edit → receipted re-canonize → `kmScale` identical, week spectra identical, `spatialCanonVersion` bumped. The ABSOLUTE-ARM fixture: two scales, one geometry → different week spectra, law M's floor holding at both. The `MAX_MARCH_WEEKS` clamp receipt; the invariant pin that the march cap stays below siege max age in BOTH worlds. THREE MUTANTS in the manifest: kmScale-ignored; carry-forward-severed (the GHOST-WRITE mutant — this user's most-bitten class: the datum survives the first canonize and vanishes on re-canonize); floor-severed. Negative controls seeded positive first (a scaled world proves the arm is live before the absent arm proves it dark). Byte rule: test-side; zero.

**WY-1 car 3 — the 37-file spectrum-derivation census + the war-volume fold (+ the disclosure screen if it fits).** Shrink-only lint census over the named mover-estate consumers of `hopWeeks`/the spectrum, navigating by symbol (37 files today — the number is measured at build and only shrinks), asserting every consumer derives its weeks from the ONE spectrum and none re-spells a speed; §1a.3's PER-MAP derivation obligation; §5b item 9's two pointer sentences landed in `DESIGN_WAR_RULINGS_ARCHITECTURE.md` (SLOT copy) in the SAME commit, each doc-reading pin asserting its target EXACTLY ONCE (the first-match hole is live — LT36). The creation-flow disclosure screen (Lane B surface) rides here only if the census lands clean; otherwise it is recorded deferred to a WY-1 car 4 (PLAUSIBLE) — never dropped. Byte rule: the screen is a component (lazy creation flow) — measure; the census is test-side.

**Repair allowance**: every landed flag-less instrument wave in this estate (CW-0w: four slices, four cars, no repairs) suggests WY-0/WY-1 may land clean; budget the seventh car anyway.

## OWNER-GATED, KEPT OUT

- The `MODE_SPEEDS` table values and the default `kmScale` — owner-signed at the soak (THE PROMISE); raw-authored here with derivations, never in `proposedSoakBands`.
- The §2a persisted-field rows (F1–F9) — WY-3 onward's; this lane mints NO field (WY-1's datum is a digest slot on a canon, creation-supplied, not a §2a row — recorded as such in the ledger row).
- Q3's arrow deletion is a CHAIR ruling with the owner's veto window open until car 2 lands — the row names it; the implementer does not close the window early by landing car 2 before car 1 is collected.
- Any raise of `CLOSURE_BUDGET_BYTES` — a family-level ceiling act, the chair's, with a measured build.

## RISKS (grep traps and hazards)

- `DEFAULT_LAYERS` has 22 keys / 17 boolean — the reader's 23 and the record's "thirteen" are both wrong; the floor is measured, never inherited.
- `setLayerFilter` is at `mapSlice.js:382-384`, not `:328-330`; `flows.js` is under `src/domain/worldPulse/`, not `spatial/`; `supplyWebWarfare.js` is 542 eff, not 538. Navigate by symbol.
- `WarFaithTab.jsx` does not exist (WarTab / FaithTab / WarFaithDesk) — any surface anchor from the volume that names it is rotted.
- The byte pins SKIP SILENTLY without dist (`vendorPdfLazy.test.js:957 it.skipIf`, `generationWorkerLazy.test.js:267 describe.runIf`) — a green suite line is not a measurement. The margin is 795 B, not the reader's 1,222.
- A static import of `distanceRead.js` from anything store-reachable re-drags the spatial graph into first paint — the WAVE-D seam pin exists; run it.
- `kmScale` zero hits today; after car 1, a grep for it must find EXACTLY the digest stamp, the canonize supply, the `hopWeeks` arm and the tests — a fifth site is a second speed floor.
- `tithe`, `cornering`, `endingsMix`, `POP-1` (dossier ids) are other families' grep traps; `EP-5` in lint tests is a 2026-07-27 programme.
- CHECK-GIT-FIRST on `mapSlice.js` (WY-9 and the layer surface waves), `distanceRead.js` (LG-0's predicate lane when it boards), and the CQ5 shared files (this lane mints NO flag — it does not collide with lanes 1–3 there).
- `gate-mutex.sh` exits 0 after 40 polls (false green); re-freeze `deno.lock` before `--frozen`; `git grep` without a rev reads the index; `check:quick` is invalid in the main checkout; ledger edits start from `git show HEAD:<path>`; the generator golden is 525/525 red — code-to-code claims only.
