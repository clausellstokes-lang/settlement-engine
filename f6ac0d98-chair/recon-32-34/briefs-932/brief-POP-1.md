# BRIEF — LANE 3 · POP-1 THE BELIEVED ROAD (`believedMigrationEnabled`)

Implementer brief, §932, lane 3 of the simulation build. Self-contained. Cut the dock from the build slot `claude/composite-r4` @ `a5876c0ea`. Every figure is **CONFIRMED** (the POP skeptic re-opened the file at `a5876c0ea`) or **PLAUSIBLE** (a reader's claim untested). Line numbers rot; re-measure by symbol at charter. 5–7 cars (CONFIRMED range). Nothing here pushes, deploys, applies a migration, deletes data, spends, or lights a flag on a preset.

## THE STATE OF THE RECORD (anchors)

- Governing volume: `docs/DESIGN_FP_ARCH_POP.md` wave POP-1 (RF-1 the columnOf whitelist, RF-2 the fail-closed classifier, Q1 extend-the-closed-set with `'returning'`, Q3 the homeward column wears the `migration_flight` face for v1); the compiled `docs/DESIGN_FP_ARCHITECTURE.md:2043-2044` slots WY-4 IMMEDIATELY AFTER POP-1 because the shared `columnOf` amendments compose BY ORDER, POP-1 first; POP-5a's `events[]` carry-through composes onto the same surface.
- Coupling obligations the volume omits (CONFIRMED in `DESIGN_FP_ARCHITECTURE.md`): row 27 (SC-1) — a same-commit registry row for every new cross-layer import pair; row 31 (SC-6) — every state-writing FP wave populates `causedBy` at the writer with CW-1 depth fixtures; row 34 (SC-9) is POP-7's, not this wave's.
- Owner: signed under the 14:4x grant and §931. Lighting is the second declared lighting (owner). Chair ruling Q1 (extend the closed set rather than a marker field) and Q3 are recorded in the ledger row as vetoable. J-POP-14 (arrivals feeding `mass_migration`) is NO in this programme (item 18b) — RF-4 fences the temptation.
- The declared-pending door is STRUCK (`subsystemRowsVirtual.js:595`; the walker's `PENDING_MANIFEST_KEYS` empty at `:288`): the flag owes a full AUTHORED certification row. The BACKLOG is full at 17/17 shrink-only.

## THE STATE OF THE CODE (read at `a5876c0ea`)

- ABSENT (CONFIRMED): all six POP flags (zero hits in src and tests); `believedMigrationRead.js`, `arrivalClearing.js`, `couplingRegistryPopulations.js`; the coupling registry head at `src/domain/certification/couplingRegistry.js:40-50` composes Trade, Grammar, Info, Espionage, Encounters, War — no POP leaf. The POP prefix IS inside the closed twelve-prefix alternation (`DESIGN_FP_ARCHITECTURE.md:2645`), so the four-edit-site admission ceremony is spared.
- ⛔ **THE COLUMN-CLASS SURFACE IS THREE STRUCTURES DEEP, NOT TWO** (CONFIRMED — WC-0B landed under the volume): `src/domain/spatial/migration.js:474 DEMOGRAPHIC_COLUMN_CLASSES` (`['refugee','voluntary']`), `:504-506 MILITARY_COLUMN_CLASSES` (three members), `:509 COLUMN_CLASSES` (the totality union), `:521 isMilitaryColumn`, the release fork widened at `:641` to `if (isDemographicColumn(col) || isMilitaryColumn(col)) { next[key] = col; continue; }`. A WAR-family pin file, `tests/domain/militaryColumnRelease.test.js`, asserts `DEMOGRAPHIC_COLUMN_CLASSES` by EXACT EQUALITY at `:64`, `COLUMN_CLASSES` by exact membership at `:65`, the totality identity at `:73-74`, disjointness at `:76`, and SOURCE-SCANS the release fork's exact line at `:106-107` to mutate it. Adding `'returning'` reds all of it. This is the same trap shape as `contributionLedgerShape:49`, one surface over.
- The class is PART OF THE COLUMN KEY (CONFIRMED): `enqueueColumns` at `:591` appends `:${planClass}` whenever the class is recognised, with the authored reason that an M4 and a P2 column on the same origin/dest/tick would otherwise collide. The return column's mint must carry the suffix or collide with the outbound column it answers.
- `columnOf` (`:543-558`) is a WHITELIST that rebuilds every record as `{originId,destId,arrivals,departTick,arrivalTick,...travelClass?}` and runs at THREE sites; `travelClass`'s conditional spread at `:558` is the precedent to copy verbatim (RF-1). `isDemographicColumn` (`:483-485`) fails closed toward M4 (RF-2); its three consumers inside `src/domain/worldPulse/demographicsMigration.js` (`:458` the stage-0 read, `:505`, `:636`) all begin seeing returning columns the moment the set widens — three deliberate rulings with pins.
- Reception machinery LANDED (CONFIRMED): `MIGRATION_REFUSALS` at `demographicsMigration.js:107-109`, the refusal mint at `:231-237`, `destinationMenuFor` still the ONE menu builder at `:276` with viability at `:293` and the spare/inbound read at `:302`; the unconditional landing at `:464-478` is exactly where the clearing inserts.
- THE BIG REUSE (CONFIRMED): the 300-tick conservation identity at `tests/domain/demographicsMigration.test.js:209/:211` and its leak-detecting negative control at `:234` — POP-1 EXTENDS a working instrument.
- `migrationRumors.js`'s carrier filters only origin≠dest and arrivals>0 (no direction/class filter) — homeward carriage is automatic once a return column exists (S29).
- Lit preconditions satisfied (CONFIRMED): `demographicsEnabled` virtual at `simulationRules.js:1027`; `believedConditionsEnabled` (SP-B) and `migrationRumorsEnabled` both in the 35-member manifest.
- Sizes (CONFIRMED, effective): `demographicsMigration.js` 380/800, `spatial/migration.js` 247, `demographicsLadder.js` 101, `demographicsObservation.js` 96, `simulationRules.js` 352, `subsystemRowsVirtual.js` 284 — no POP file is in `scripts/.size-baseline.json`; no decomposition car is owed.
- Flag bill: `contributionLedgerShape.test.js:119-120` `toHaveLength(35)` ×2, title at `:49` "at 28" (grep trap); the neighbouring `conquestDoctrineEnabled` row in `subsystemRowsVirtual.js:113-164` is the ~60-line shape to copy.
- Worker: `git grep -ln 'demographicsMigration|commonsVoiceKernel|demographicsLadder|demographicsHerald' -- src/generators src/store` is EMPTY; `generation.worker.js` imports only `generationRequest.js` and the living-content seam (CONFIRMED). POP bytes ride `advanceInterval.worker.js` (no byte ceiling) — ~22–28 KB raw source, ~5–7 KB minified (PLAUSIBLE). They CANNOT live in a lazy chunk (runtime `=== true` inside the pulse tick) and need not.

## ALREADY DONE (do not re-land)

- The reception machinery, the refusal vocabulary, the single menu builder, the unconditional landing site.
- The 300-tick conservation identity and its negative control (EXTEND, never rewrite).
- The column-class union and the three-way release fork (WC-0B, `4c87bae45`) — POP-1 widens the DEMOGRAPHIC set inside the union; it does not re-derive the union.
- SP-B's believed-conditions family (`beliefAxisSubjects.js`, `SUBJECT_AXIS_FIELDS = ['conditionsBands','devotionBand','scarcityBands']`) — the belief arm swaps ONE resolver against it.
- The POP prefix's admission to the coupling-id alternation.

## THE CARS

Every car: one gated commit; four-fence dormancy set captured at the pull seam BEFORE wiring; anchored negatives; executed mutants in `scripts/mutation-coverage-manifest.json`; full gate on the slot with a printed test count; worker-bytes rule stated.

**Car 1 — the class extension + both `columnOf` carry-throughs + the war-family pins re-cut. No flag.** `DEMOGRAPHIC_COLUMN_CLASSES` gains `'returning'`; `COLUMN_CLASSES` re-derives; `columnOf` carries `believedBand` and the returning class through all three sites by the `:558` conditional-spread idiom. Gate: `tests/domain/militaryColumnRelease.test.js` is AMENDED in the same commit — `:64` exact equality to the three-member set, `:65`/`:73-74`/`:76` re-asserted, the `:106-107` source scan re-cut against the widened predicate (a war-chair queue row per touch: this is a WAR-family file). RF-1 pinned by a round-trip THROUGH ENQUEUE (`enqueueColumns` whole-ledger rebuild + `releaseArrivals` + the P2 stage-0 read), never save/load; the key-suffix pin: a returning column and its outbound twin on the same origin/dest/tick do NOT collide. RF-2: the wrong-lane negative with an EXECUTED mutant that reverts the class-set extension and watches M4 steal the column. Negative controls: a two-member set (the mutant) proves the whitelist deletes the fields; a mis-classed return column lands via `releaseArrivals` and is narrated as flight (the failure mode, seeded, then cleared). Dormancy: the seeded world is byte-identical before and after (no flag yet; the fields are conditional and absent). Worker bytes: 0 to the generation worker (assert with a measured build; `generationWorkerLazy.test.js:267` skips without dist).

**Car 2 — `believedMigrationRead.js` (≤250 eff) + the seam swap + the flag mint + the four fences.** The belief arm swaps ONE resolver in `destinationMenuFor` (reading SP-B's `conditionsBands` via `beliefMap`'s record, never ground truth — the K3 import pin, token scan and guard-the-guard with a positive control) and leaves the over-send guard truth-side. The flag bill in this commit: manifest key (count from HEAD — lanes 1 and 2 mint concurrently), AUTHORED certification row (~60 lines on the `conquestDoctrineEnabled` shape; TAIL-append), `VIRTUAL_RULES` twin at `tests/domain/subsystemRowsVirtual.test.js:255`, the `LANE_LEAVES` entry, both `contributionLedgerShape` literals, `engineGatedRuleKeys.walker`, seven edge-shared MODIFY rows + `npm run build:edge-shared`, the acceptance literal `believedMigrationEnabled: true`, one by-name `=== true` read. Gate: four fences (ledger keys · rng draw count · news kinds · display) byte-identical with the key absent and `false`; the dormancy golden captured at the pull seam BEFORE wiring; the lit-mutant control. THE OMNISCIENCE COLLAPSE pin: `infoMode: 'omniscient'` lit ≡ dark byte-for-byte. Worker bytes: 0.

**Car 3 — `arrivalClearing.js` (≤300 eff) + the return column + the conservation RE-DERIVATION.** The clearing inserts at the unconditional landing (`:464-478`): refused/unattractive arrivals become a `returning` column minted with the key suffix through the ONE writer, `causedBy` populated at the writer (SC-6) with a CW-1 depth fixture. Gate: the 300-tick identity at `demographicsMigration.test.js:209` EXTENDED over both new arms (outbound believed + returning), re-executed, its `:234` negative control still leak-detecting (plant a half-column leak → reds). NEVER-OUTRUNS-THE-ROAD: a returning column's arrivalTick obeys `populationLegCost`, byte-identical leg cost. Negative: with zero refusals no returning column exists (byte-absent). Worker bytes: 0.

**Car 4 — disappointment + letters home + the gold-rush pin.** The disappointment read (arrived-and-found-less) and the letters-home carriage through `migrationRumors.js`'s existing carrier (no filter edit — S29). THE GOLD RUSH PIN: a believed-good destination over-attracts until the returning columns' letters correct the belief — measured as a DISTRIBUTION over seeds, not single-seed (the wave-E vacuity lesson). Q3 recorded: the homeward column wears `migration_flight` for v1. Negative: omniscient mode → no gold rush (belief = truth). Worker bytes: 0.

**Car 5 — `couplingRegistryPopulations.js` + the composition edit + the three CW-0 rows + the SP-2 tripwire + SC-1 rows.** The new desk leaf (copy the six-leaf shape; the head's composition at `couplingRegistry.js:40-50` gains one import), its rows for the belief read (INFO→POP), the rumour carriage (POP→INFO) and the arrival clearing (POP→POP), SC-1 rows for every new cross-layer import pair, and `beliefSubjectsGrewSincePop1()` — a source-scan tripwire that reds when `SUBJECT_AXIS_FIELDS` grows without POP-1 ruling on the new family. Gate: `couplingDesk.walker` and `couplingInclusion.walker` (`:785-810` ARGUED_UNLAYERED shape if any leaf needs it) green with the new leaf; the registry row count re-derived, never restated. Worker bytes: 0 (certification side).

**Car 6 — the mutant battery + the full gate (if cars 1–5 run long).** Three manifest-entered mutants beyond car 1's: belief-reads-truth (the K3 plant), clearing-bypassed (arrivals land unconditionally again → conservation reds), key-suffix-dropped (collision → the through-enqueue pin reds). The full gate, quoted with test counts.

## OWNER-GATED, KEPT OUT

- Lighting `believedMigrationEnabled` — the second declared lighting.
- J-POP-14 / CR-FP-6 — arrivals feeding `mass_migration` stressors: NO in this programme (item 18b); the reads at `capacityModel:633` and `stressorGates:476` keep their non-arrival writers (RF-4).
- WY-4's §2a fields (F4 `faithCargo`, F5 `causeReceipt`) — signed, but WY-4's to land, after this lane, composing BY ORDER onto the whitelist this lane amends.
- Any edit to the WAR-family pin file beyond the exact re-cut car 1 needs — recorded as a war-chair queue row.

## RISKS (grep traps and hazards)

- Every `POP-1` hit in `src/` is a `DS-POP-1` DOSSIER id — the wave is UNBUILT regardless of the count (PROGRAMME §A grep trap, CONFIRMED by the WY skeptic).
- `contributionLedgerShape.test.js:49` says "at 28"; the literals are 35. `tithe`, `cornering`, `endingsMix` are other families' landed tokens.
- `settlementRumors.js` lives at `src/domain/display/`, not `src/display/`; `flows.js` at `src/domain/worldPulse/`.
- `tests/domain/militaryColumnRelease.test.js` is NOT named in the POP volume and reds on car 1 — find it before the first edit (`grep -rn DEMOGRAPHIC_COLUMN_CLASSES tests/`).
- The key suffix at `enqueueColumns:591` — a return column minted without it collides with its outbound twin and the collision is silent (a `next[key]` overwrite).
- The isDemographicColumn consumers at `:458/:505/:636` turn on together; rule each, pin each.
- CHECK-GIT-FIRST on `simulationRules.js`, `subsystemRowsVirtual.js`, `contributionLedgerShape.test.js` — lanes 1 and 2 mint flags in the same window; the flag-mint car (car 2) rebases at landing with the count from HEAD.
- `gate-mutex.sh` exits 0 after 40 polls (false green); re-freeze `deno.lock` before `--frozen`; `git grep` without a rev reads the index; ledger edits start from `git show HEAD:<path>`; the generator golden is 525/525 red — code-to-code claims; the dist-gated byte suites skip silently.
