# BRIEF — LANE 1 · TR-4 THE GRAIN ROAD (`foodCaravansEnabled`; two slices; the CR-2 pin first)

Implementer brief, §932, lane 1 of the simulation build. Self-contained. Cut the dock from the build slot `claude/composite-r4` @ `a5876c0ea` (never from the ledger branch — its `src/` is a fossil). Every figure is **CONFIRMED** (the TR skeptic re-opened the file at `a5876c0ea`) or **PLAUSIBLE** (a reader's claim untested). Line numbers rot; re-measure by symbol at charter. 5–7 cars (CONFIRMED range). Nothing here pushes, deploys, applies a migration, deletes data, spends, or lights a flag on a preset.

## THE STATE OF THE RECORD (anchors)

- Governing volume: `docs/DESIGN_FP_ARCH_TR.md` (ledger `a803dee7a`), wave TR-4, two slices; the compiled `docs/DESIGN_FP_ARCHITECTURE.md` §5 #26; `DESIGN_FP_TRADE.md` §3 (the T7 walker, calm-equivalence, the bypass arms).
- Owner: the 14:4x grant (09-14 ~22:5x, "do everything you can without me") and the 09-15 sitting (§931) rule this wave's build signed. The second declared lighting (§764.3 amended at §931 item 1) is the owner's — this wave lands DARK.
- **Two record-vs-record contradictions were RULED by the chair's folder (PRICING-32-33.md §A.4, vetoable):**
  1. **CR-2 wins over §5 #26.** `docs/FABLE_VALIDATION_QUEUE.md:303-309` (CR-2) and `docs/SOL_QUEUE.md:342-345` row 14 order the two `storageMonths` folds CONSOLIDATED into the one applicator FIRST, behind a same-seed byte-identical pin, with a STOP-and-report clause if consolidation shifts behaviour. `DESIGN_FP_ARCHITECTURE.md` §5 #26 carries Q9's REPORTED-NOT-DEFECT ("the queue row filed; the sixth-writer scan fences it"). Consolidation is the ruling: it is the more conservative act, its pin catches a live two-writer defect the fence would only contain, and it banks a LOWER `applyWorldPulse.js` baseline. Car 0 amends §5 #26 to point at CR-2 (never deletes it).
  2. **TR-4 lands BEFORE WY-3.** §5 slots WY-3 immediately before TR-4 "so grain inherits the caravan body"; WAYFARE is 12/12 UNBUILT (`caravanBodiesEnabled` zero hits). The commodityStocks/supplyShipments serialize→regen→undo→import round-trip pin is TR-4's to LAND (car 1); WY-3 later VERIFIES and extends. Grain arrivals light without a mover body; that consequence is DECLARED in the certification row and the ledger row, not inherited silently.
- The TR volume's Q4 ("each wave lands PENDING, TR-9 converts all eight") is DEAD in code: `VIRTUAL_PENDING_RULE_KEYS = Object.freeze([])` at `src/domain/certification/subsystemRowsVirtual.js:595` (CONFIRMED). Every TR wave owes a full AUTHORED certification row in the flag's commit; the stale comment at `simulationRules.js:256-258` is amended in passing.

## THE STATE OF THE CODE (read at `a5876c0ea`)

- ABSENT (CONFIRMED): `src/domain/worldPulse/grainFlow.js`, `src/domain/worldPulse/grainArrivalCredit.js`, `foodCaravansEnabled` (zero hits in src and tests).
- The `storageMonths` write set — FIVE modules, SIX expressions (CONFIRMED, re-measured; the volume's `:322` and `:92-110` are rotted): `src/domain/foodStockpile.js:406` and `:479`; `src/domain/worldPulse/generosityUpdates.js:65`; `src/domain/worldPulse/applyWorldPulse.js:106` (the SECOND fold — CR-2's subject); `src/domain/magic/magicBufferApply.js:357` (PLAUSIBLE path; re-locate by symbol); `src/domain/events/mutateWorld.js:1153` (NOT `worldPulse/` — the reader's path was wrong).
- The one applicator: `generosityUpdates.applyFoodDeltasToUpdates` (90 eff), named as such by `src/domain/worldPulse/treatyTransfer.js:11-32`'s own header (CONFIRMED).
- Substrate: `src/domain/spatial/commodityFlow.js` 448 eff (CONFIRMED path — `spatial/`, not `worldPulse/`), five-term integer conservation `assertGoodsConservation` at `:260`, `MAX_SHIP` at `:117`, `MIN_SHIP: 2` at `:118`; `foodStockpile.js` 253 eff with the six-term effective-deficit sum at `:347` and the generation stash at `:288` (GENERATION IS SACRED — never rewritten); `supplyShipments.js` 210 eff at `src/domain/spatial/` (K-ranked producers, starvation latch); `treatyTransfer.js` 49 eff; `supplyKernel.js` 489/800 eff — the mount seam, own-flag-before-host-gate; bypass rates at `src/data/foodImportRates.js:18-20` (teleport 0.3, airshipBesieged 0.15, underways trickle); grain ALREADY canonical at `src/domain/region/goodsCatalog.js:30-36` with criticality 0.95 — no catalog edit.
- Lit preconditions exist: `commodityFlowEnabled` (`simulationRules.js:769/:938`, true in full_simulation); `demographicsEnabled` (`:1027`, virtual); `routeLifecycleEnabled` measured FALSE → the collapse arm yields an honest permanent zero, not a bug.
- Zero-headroom heads (CONFIRMED, `scripts/.size-baseline.json`): `pulseKernel.js` 1581/1581 (R-BLD-10, banked permanently), `applyWorldPulse.js` 907/907. Both take ZERO added lines. CR-2's consolidation must be a net SHRINK of `applyWorldPulse.js` and the baseline number is LOWERED in the same commit.
- The flag bill's fourth surface: `tests/domain/contributionLedgerShape.test.js:119-120` asserts `toHaveLength(35)` twice; its title at `:49` still says "at 28" (grep trap). The manifest `ENGINE_GATED_VIRTUAL_RULE_KEYS` (`simulationRules.js`) is 35 at the slot — take the count from HEAD at landing (lanes 2 and 3 mint flags concurrently).

## ALREADY DONE (do not re-land)

- TR-1 whole (`d7ea69a4b` + `93c118b6d`): the commercial-reason family, `commercialReceiptPools.js` (386/800), the TR coupling desk leaf. Its writer is UNMOUNTED (`advanceCommercialReasons` zero callers) — that is lane 7's car, not this lane's.
- TR-9c's `tradeConvergenceContract.js` (398 eff) with TR-4's spec rows at `:136-143`.
- Grain's goods-catalog row and the food ledger's bypass rates.
- The single applicator and its header ruling in `treatyTransfer.js`.

## THE CARS

Every car: one gated commit; four-fence dormancy set captured BEFORE wiring; anchored negatives (`// anchored:` on every absence assertion); executed mutants registered in `scripts/mutation-coverage-manifest.json`; the full gate run on the slot (`gate-mutex.sh` gives up after 40 polls and EXITS 0 — a gate line with no printed test count DID NOT RUN; re-freeze `deno.lock` before any `--frozen` run); worker-bytes rule stated per car.

**Car 0 — CR-2: the two `storageMonths` folds consolidated into the one applicator, behind a byte-identical pin.** Gate: the same-seed serialized worldState is byte-identical in BOTH flag states (the flag does not exist yet — assert absent-key identity) across the dormancy corpus and the ONE_REGEN preset before and after; `applyWorldPulse.js` measured by the enforcer's own Linter, baseline LOWERED to the new figure in `scripts/.size-baseline.json` with a rationale key. STOP clause (verbatim from the queue rows): if consolidation shifts a single byte, the lane STOPS and reports to the chair — a live two-writer defect is adjudicated, never normalized. Pins: the five-site census asserts the write set is now FOUR modules / FIVE expressions (shrink-only), with the applicator the only in-cycle fold. Negative control: re-insert the second fold as a plant → the census reds and the byte-identity pin reds (executed, then reverted with `cp` backup + md5-exact restore, never a checkout-family command). Census bills: the storageMonths census file is NEW here (there is none at the slot — PLAUSIBLE, verify by grep in `tests/lint` before writing). Docs: §5 #26 amended to cite CR-2 on the SLOT copy of `DESIGN_FP_ARCHITECTURE.md` (the ledger copy is the chair's). Worker bytes: 0 — none of the five modules is in the generation worker's graph (CONFIRMED by BFS); state the measured build anyway.

**Car 1 — the commodityStocks/supplyShipments round-trip pin.** Gate: serialize → regenerate → undo (through the pulse ring) → import, THROUGH at least one in-transit tick so the arrive-loop rebuild is inside the loop, on a fixture with ≥2 live shipments including one multi-tick; every field of the shipment record survives (CONFIRMED absent at the slot — "do not inherit" is right). Pins: the record shape enumerated as a closed key set; the arrive-loop reconstruction at `commodityFlow.js:558` (the in-file comment "a multi-tick smuggle run loses smuggle:true on reconstruction" is the exact loss class) covered by a field-dropping plant that reds. Negative control: drop `smuggle` in the rebuild → the pin reds. Bill: a one-line delta row in the WY volume's ledger copy is the chair's; this car records in its own header "LANDED at TR-4; WY-3 verifies and extends, never re-lands". Worker bytes: 0.

**Car 2 — `grainFlow.js` + `grainArrivalCredit.js` + the equal-and-opposite offset + the flag mint + fences.** `grainFlow.js` (≤250 eff): grain joins `commodityFlow`'s five-term balance — `assertGoodsConservation` covers it for free (J-TR-8); dispatch through the existing K-ranked producer pick. `grainArrivalCredit.js` (≤150 eff): the ONE authored `GRAIN_UNITS_PER_MONTH` constant lives HERE (⛔ never in `foodImportRates.js`, `goodsCatalog.js` or `tradeGoodsData.js` — all three are IN the generation worker's 153-module graph and the ceiling has zero slack); physical arrivals credit `storageMonths` through `applyFoodDeltasToUpdates` and NOWHERE else; the netted rate share is offset equal-and-opposite at the same applicator so grain-months conserve at the one door. Mount inside `supplyKernel.js` (489/800), own-flag read `simulationRules.foodCaravansEnabled === true` BY NAME before the host gate; `pulseKernel.js` and `applyWorldPulse.js` take ZERO edits. The flag bill, all in this commit: manifest key (count from HEAD), AUTHORED certification row in `subsystemRowsCompact.js` or the TR leaf (TR-1's at `subsystemRowsCompact.js:265` is the ~30-line shape; TAIL-append only — the ordered-equality pin shifts later rows on a mid-insert), `VIRTUAL_RULES` twin at `tests/domain/subsystemRowsVirtual.test.js:255`, the `LANE_LEAVES` entry, both `contributionLedgerShape.test.js` literals, `engineGatedRuleKeys.walker` exact membership, the seven edge-shared MODIFY rows + `npm run build:edge-shared`, the acceptance-file literal `foodCaravansEnabled: true` drive (mechanismLitCoverage credits only a literal), `src/lib/spatialUsage.js` classification if any new spatialLedgers key is written (TRACKED with prose or EXEMPT with prose + its row in `tests/lib/spatialUsage.test.js`). Gate: the four-fence dormancy set (ledger keys · rng draw count · news kinds · display surface) byte-identical with the key absent AND with `false`, plus the lit-mutant control (lit → the fence reds). The certification row DECLARES: grain lights without a mover body until WY-3; the lit path is a bounded declared shift (calm-equivalence, car 4). Worker bytes: 0 — assert with a measured build that the generation worker's byte count is unchanged (the suite at `tests/build/generationWorkerLazy.test.js:267` is `describe.runIf(DIST_EXISTS)` and SKIPS without dist — quote a real build).

**Car 3 — the T7 walker + the storageMonths census + the sixth-writer plant.** T7: exactly one of {netted rate share, physical arrivals} live per settlement per tick, in BOTH flag states, forever — shaped as a ROUTED CENSUS over the food ledger's `importDependency` consumers, shrink-only, NEVER a count-of-N row (§5's correction). The storageMonths census from car 0 gains the grain applicator as an admitted writer and an EXECUTED sixth-writer plant (plant a sixth expression → reds; restore md5-exact). Negative controls: both flag states seeded with a live importer FIRST so the exactly-one assertion is non-vacuous; a settlement with zero imports proves the walker still sees it. Worker bytes: 0.

**Car 4 — calm-equivalence + the three bypass arms + T-6.** THE CALM-EQUIVALENCE PIN: on a calm corpus, lit physical arrivals land within ONE authored band of the old netted rate — the disclosed, bounded lit-path shift, its band a raw constant in a frozen `GRAIN_TUNING` table with an executed derivation (§43 rationale-or-STOP; ratified only at the owner's soak). THE BYPASS-PRESERVATION PIN, all three arms EACH executed: teleport 0.3, airshipBesieged 0.15, underways trickle — each arm proven to route physical grain through its bypass at the measured rate, read from `foodImportRates.js` (read, never edited). Grain-months conservation at the one applicator with a leak mutant. THE BELIEF-CANNOT-STARVE NEGATIVE (a believed shortage moves no physical grain). T-6 the siege TELLABLE week-by-week (roads closed → arrivals stop → the stockpile falls on the food ledger's own clock → the bells) plus its open-route negative. Three mutants in the manifest: rate-share-not-offset, bypass-arm-severed, applicator-bypassed. Worker bytes: 0.

**Car 5 — slice 2: the treaty fork + the robbery claim.** `grain_provision` (TR-5's term) has a physical arm here only if TR-5 has landed; otherwise the arm is DECLARED degraded (the named conserved-rate read) with a pre-pin toward TR-5's executor that reds when the executor lands without wiring it. The robbery claim on a seized grain caravan rides the existing claim family — no new ledger. Worker bytes: 0.

**Repair allowance**: the estate's own record (TR-1, GR-2, SP-B, GR-3a each took a repair commit) makes a sixth/seventh car the realistic ceiling. Budget it; do not report it as a surprise.

## OWNER-GATED, KEPT OUT

- Lighting `foodCaravansEnabled` on any preset — the second declared lighting (§764.3, §931 item 1), the owner's window.
- The `GRAIN_TUNING` band values — landed raw with derivations, ratified at the soak under THE PROMISE.
- WY-3's own §2a field rows (F1–F3) — signed at item 9(d) but WY-3's to land; this wave touches no caravan-body field.
- Any edit to `pulseKernel.js` or `applyWorldPulse.js` beyond CR-2's net shrink; any raise of a size ceiling.

## RISKS (grep traps and hazards)

- `cornering` hits are TR-1's landed taxonomy, not TR-6; `tithe` (38 files) is the old economy layer; `endingsMix` is trade/war. A grep for "28" in `contributionLedgerShape.test.js` edits nothing and reds at the gate — the literals are 35.
- `mutateWorld.js` is under `src/domain/events/`, not `worldPulse/` — the storageMonths census must navigate by symbol.
- The volume's `applyWorldPulse.js:322` and `:92-110` are rotted addresses; the second fold is at `:106` today and may move again.
- `foodImportRates.js`, `goodsCatalog.js`, `tradeGoodsData.js` are in the generation worker's graph: the natural-looking home for the grain constant pays worker bytes against a ceiling last re-minted for 245 B with a chair ruling.
- `git grep` without a rev reads the INDEX; `check:quick` is invalid in the main checkout (fossil src); every ledger edit starts `git show HEAD:<path> > <path>`.
- The generator golden is 525/525 RED at the slot (bisected to the em-dash wave) — golden claims are CODE-TO-CODE, never "N of 525".
- `gate-mutex.sh` exits 0 after 40 polls — a false green; the dist-gated byte suites skip silently without a build.
- CHECK-GIT-FIRST on `simulationRules.js`, `subsystemRowsVirtual.js`, `contributionLedgerShape.test.js` — lanes 2 (TR-5) and 3 (POP-1) mint flags in the same window; the flag-mint car is rebased at landing with the count taken from HEAD.
