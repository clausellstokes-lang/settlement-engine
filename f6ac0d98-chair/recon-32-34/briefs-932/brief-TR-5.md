# BRIEF — LANE 2 · TR-5 THE PACT LANE (`tradePactsEnabled`)

Implementer brief, §932, lane 2 of the simulation build. Self-contained. Cut the dock from the build slot `claude/composite-r4` @ `a5876c0ea`. Every figure is **CONFIRMED** (the TR skeptic re-opened the file at `a5876c0ea`) or **PLAUSIBLE** (a reader's claim untested). Line numbers rot; re-measure by symbol at charter. 4–6 cars (CONFIRMED range). Nothing here pushes, deploys, applies a migration, deletes data, spends, or lights a flag on a preset.

## THE STATE OF THE RECORD (anchors)

- Governing volume: `docs/DESIGN_FP_ARCH_TR.md` wave TR-5; `DESIGN_FP_TRADE.md:857-859` names FIVE commercial terms — `market_access`, `toll_exemption`, `exclusivity`, `grain_provision`, `route_wardenship`; the compiled `docs/DESIGN_FP_ARCHITECTURE.md` §5 SUPERSEDES the TR volume's C-TLF-2 discharge block (J-FP-1): `catalogGrewSinceWr10`'s discharge was EXECUTED at GR-3 — TR-5 VERIFIES it and does NOT re-run it. What still binds: the CR-WR10-B twin-degradation-sentence law (byte-equal across both volumes, never a second sentence) and the amendment of the "HALF DISCHARGED… TR-5 STILL OWED" blockquote to "DISCHARGED" — never deleted — across its eleven consumer files plus `DESIGN_FP_ARCHITECTURE.md:2349`, on TR-5's OWN commit.
- Owner: signed under the 14:4x grant and §931. Lighting is the second declared lighting (owner). No push/deploy/migration/deletion/spend.
- **Chair rulings recorded here (vetoable):**
  1. **`route_wardenship` is a LIVE COLLIDING TOKEN.** TR-1 spent it as a commercial-REASON kind: `commercialReasonTaxonomy.js:66/:85` (`route_predation ↔ route_wardenship`), `commercialReceiptPools.js:205/:410`, `settlementRumors.js:263`, `heraldRouting.js:241`, pinned by `tests/lint/commercialKindPools.walker.test.js:72` (CONFIRMED). Minting a `TERM_CATALOG` type on the same bare string is the "one catalog quietly becomes two" failure the estate records against a treaty family at `subsystemRowsBelief.js:247`. **Ruling: the treaty TERM is minted under the distinct spelling `route_warden` (family `commercial`); the reason kind `route_wardenship` stays as landed; the two are cross-referenced in both docblocks and a pin asserts neither string appears in the other's registry.** The implementer may propose a better spelling in the ledger row; it may not reuse the bare string.
  2. **TR-5 boards before WF-6** on the `peaceTerms.js` 797/800 collision (PRICING §A.4.3). This lane holds the T9 serialization: no other lane touches `peaceTerms*.js` while lane 2 is in flight, and WF-6 boards when this lane's catalog car has landed.
- The declared-pending door is STRUCK (`subsystemRowsVirtual.js:595`): the flag owes a full AUTHORED certification row in its own commit.

## THE STATE OF THE CODE (read at `a5876c0ea`)

- ABSENT (CONFIRMED): `commercialTermExecutors.js`, `tradeDemandTrigger.js`, `tradePactsEnabled` (zero hits); `grain_provision` zero hits tree-wide; `route_warden` (the new spelling) zero hits.
- GR-3 MINTED THREE OF THE FIVE (CONFIRMED): `src/domain/worldPulse/peaceTermsCatalog.js:285-287` carries `exclusivity` / `market_access` / `toll_exemption` at `family:'commercial'`, `executor:'seam'`, display strings at `:425-427`. The catalog is 115/800 eff with 26 frozen rows / 13 families; `TERM_FAMILIES` is DERIVED at `:328`. TR-5 mints TWO rows (`grain_provision`, `route_warden`), not one.
- **`peaceTerms.js` measures EXACTLY 797/800 effective** (CONFIRMED by two independent counts). Any head seam of ≥4 effective lines is leaf-first or the wave STOPs with an extraction recipe (the `peaceTermsSale.js`/`pactAmendment.js` pattern: a writer-family leaf reached by an import edge the head already has). Re-measure with the enforcer's own Linter at the publishing commit; never inherit 797.
- THE TWO DESIGNED RED-FLIPS assert by SET EQUALITY, not merely producer-lessness (CONFIRMED): `tests/domain/peaceTermsGrantTerms.test.js:272-275` runs `expect([...commercial].sort()).toEqual(['exclusivity','market_access','toll_exemption'])`; `tests/domain/sovereigntyBundleWr10.test.js:480-486` does the same and its arm is anchored on `'tribute'` via `expectAbsentWithAnchor`. Two new rows red BOTH set assertions AND the "stay producer-less until TR-5 lands" clauses. A TR-5 that reds them without re-pointing them (with the anchor kept honest) has not landed.
- The dead consumer (CONFIRMED): `realmPressure01` is produced at `demographicsObservation.js:137` and consumed only by the war path (`demographicsWar.js:172/:295-312`, `warReasons.js:801-853`); `tradeDemandTrigger` is the trade-agreement consumer that has never existed.
- Substrate: `negotiationPictures.js` 303 eff — THE one terms evaluator, CONSUMED (a second evaluator anywhere is a design defect, seam 3); `strategicPosture.js` 136 eff, `courtPostureOf` has a real caller at `pactFormation.js:110` (SP-C's "zero callers" is stale); `treatyClock.js:6-7` `INTERVAL_WEEKS` identity (52-week years; the 12-tick trap is cured law); `treatyOrientation.js` and the treaties artifact carry the pacts — ZERO new top-level worldState keys; `pactFormation.js` 426 eff, `pactProposals.js` 161 eff (GR-2, landed); `treatyEnforcement.js` 113 eff with the generic `grantTermFor`/`grantedRightFor`/`grantedRightStateFor` trio.
- The flag bill's fourth surface: `contributionLedgerShape.test.js:119-120` `toHaveLength(35)` ×2; title at `:49` says "at 28" (grep trap). Take the count from HEAD — lanes 1 and 3 mint flags concurrently.
- Worker: none of `peaceTerms.js`, `peaceTermsCatalog.js`, `negotiationPictures.js`, `strategicPosture.js`, `treatyTransfer.js`, `demographicsObservation.js` is in the generation worker's graph (CONFIRMED by BFS). 0 KB.

## ALREADY DONE (do not re-land)

- GR-2 peacetime formation (`pactFormation.js`, `pactProposals.js`, `a18fdcfae` + `91075d457`); GR-3a/3b the term catalog rows and readers (`tests/domain/peaceTermsGrantTerms.test.js:38` says "GR-3b HAS LANDED"); `catalogGrewSinceWr10()` FIRED and pinned naming the grown set `['commercial','faith','population']`.
- SP-3 via GR-2/GR-3 (`pactFormationEnabled` manifest member 15); SP-4 the posture read (for SETTLEMENT actors only).
- TR-1 (`d7ea69a4b` + `93c118b6d`) including `commercialReceiptPools.js` (386/800, ~19 eff per kind — TR-5's pact kinds spend from the 414-eff headroom; if the pool nears 800, SPLIT into a second family leaf and amend the walker to see both, never squeeze).
- TR-4 is lane 1 and may or may not have landed when this lane's car (b) runs: `grain_provision`'s PHYSICAL arm is declared degraded (the named conserved-rate read) when `foodCaravansEnabled` is absent — never blocked.

## THE CARS

Every car: one gated commit; four-fence dormancy set before wiring; anchored negatives; executed mutants in `scripts/mutation-coverage-manifest.json`; full gate on the slot with a printed test count; worker-bytes rule stated.

**Car (a) — the two catalog rows + the five executors as lazy `peaceTerms`-family siblings + the TERM_CATALOG membership pin + the two red-flip re-pointings.** Rows land in `peaceTermsCatalog.js` (115/800) and NEVER in `peaceTerms.js`. `commercialTermExecutors.js` (≤250 eff) as a writer-family leaf: `market_access`, `toll_exemption`, `exclusivity` executors flip GR-3's `executor:'seam'` rows to real producers; `grain_provision` streams on `spec.stream` physics (the `restitution` precedent, executor `transfer`) with its physical arm gated on `foodCaravansEnabled` and declared degraded otherwise; `route_warden` prices a patrol obligation against leg loss rates (read from the existing route-loss substrate; no new number without a derivation). Gate: `TERM_CATALOG` membership pin asserts the commercial family is now exactly the five (sorted); `TERM_FAMILIES` unchanged (derived, `commercial` already present — the tripwire must NOT fire); `peaceTerms.js` net-zero effective lines measured by the Linter. The two red-flips re-pointed: `peaceTermsGrantTerms.test.js:272-275` and `sovereigntyBundleWr10.test.js:480-486` now assert the five-member set and PRODUCED buckets, with the `'tribute'` anchor kept and the reachability obligation moved into this wave's own pins. Negative controls: a sixth commercial row planted → the membership pin reds; the collision pin — `route_wardenship` planted into `TERM_CATALOG` → reds; `route_warden` planted into the reason taxonomy → reds. Census bills: `commercialReceiptPools.js` rows for any pact-formed/pact-broken kinds this car voices, WHAT_PHRASES + heraldRouting + the commercial kind-pool walker re-derived. Worker bytes: 0 (measured build quoted).

**Car (b) — `tradeDemandTrigger.js` + the `realmPressure01` landing + the flag mint + fences.** `tradeDemandTrigger.js` (≤200 eff): the trade-agreement consumer of `realmPressure01` — amendment-shaped formation (ONE instrument per pair; a proposal AMENDS, minting only when absent) through `pactProposals`' existing closed triggers (add the fifth trigger only if GR-5 has not — CHECK-GIT-FIRST; GR-5's renewal trigger is the same slot). Every term names duration in 52-week years with the `INTERVAL_WEEKS` identity pin (`treatyClock.js:6-7`; the pin names 52, never 12). The flag bill in this commit: manifest key (count from HEAD), AUTHORED certification row (TAIL-append), `VIRTUAL_RULES` twin, `LANE_LEAVES`, both `contributionLedgerShape` literals, `engineGatedRuleKeys.walker`, seven edge-shared MODIFY rows + `npm run build:edge-shared`, the acceptance literal `tradePactsEnabled: true`, one by-name `=== true` read in the same commit. Gate: the four-fence dormancy set byte-identical with the key absent and `false`; the lit-mutant control. Negative: `realmPressure01` severed → the trigger never fires (seeded positive control first: a high-pressure pair forms a pact). Worker bytes: 0.

**Car (c) — the T-5 end-to-end tellable + its dark twin + the K3 absurdity twin + the lean-year fork.** T-5 is FIVE subsystems deep with a receipt at every link: pact forms → delivery lands (stream physics) → `realmPressure01` falls (`demographicsObservation.js:137`) → the war-motive weight falls (`warReasons.js:801-853`, READ ONLY — no war-path edit; a war-chair queue row per touch). The SAME fixture dark is byte-identical. K3: a `grain_provision` signed for grain the seller no longer has → the mismatch becomes the next grievance through the existing breach grading (`treatyBreach`), never a silent zero. The lean-year suspension with a buyer-belief fork (the buyer's believed stores band, SP-B's `storesBand`, decides whether suspension reads as breach or as hardship). Negative controls: the K3 twin with stock present → no grievance; the lean-year fork with omniscient infoMode → the belief arm collapses to truth. Mutants: delivery-not-lowering-pressure; grievance-suppressed. Worker bytes: 0.

**Car (d) — the CR-WR10-B amendment sweep.** The "HALF DISCHARGED… TR-5 STILL OWED" blockquote → "DISCHARGED" across its eleven consumer files plus `DESIGN_FP_ARCHITECTURE.md:2349` — AMENDED, never deleted; every doc-reading pin asserts its target appears EXACTLY ONCE (the first-match retargeting hole is LIVE, second instance found at LT36); the twin-degradation sentence stays byte-equal across both volumes (a pin compares the two). Docs land on the SLOT copies; the ledger copies are the chair's collection. Worker bytes: 0.

**Repair allowance**: GR-2 — the nearest analogue — cost 3,945 + 719 insertions in two commits; TR-5 is strictly larger. Budget a fifth/sixth car.

## OWNER-GATED, KEPT OUT

- Lighting `tradePactsEnabled` — the second declared lighting.
- The ransom/claim persistence ruling (item 8) — not touched here; TR-8's problem.
- Any WR-shared substrate edit beyond the catalog rows: a WR lane in flight on `peaceTermsCatalog.js` is STOP-and-report (T9).
- Any war-path behavioural edit — Q2 stands: read-only consumers behind the TR flag, a war-chair queue row per touch.
- Tuning values the executors introduce (route_warden's price, the lean-year threshold) — raw in a frozen table with derivations, ratified at the soak.

## RISKS (grep traps and hazards)

- `route_wardenship` (TR-1's reason kind) vs `route_warden` (this wave's term) — a grep for the volume's spelling finds the WRONG registry. `cornering` = TR-1; `tithe` = old economy layer; `endingsMix` = trade/war.
- `catalogGrewSinceWr10` — already FIRED; a lane that "discharges" it again re-runs an executed act and moves a pinned fired-state.
- `peaceTerms.js` 797/800: the third line of headroom is not yours; leaf-first. Re-measure at the publishing commit.
- The `'tribute'` anchor in `sovereigntyBundleWr10.test.js` — re-pointing the set assertion without keeping the anchor makes the absence vacuous.
- `commercialReceiptPools.js` fills at ~19 eff per kind; the SPLIT is a sub-car nobody budgeted — measure before authoring rows.
- The flag-mint literals (35) — CHECK-GIT-FIRST; lanes 1 and 3 mint in the same window; take the count from HEAD; the title at `:49` still says 28.
- `gate-mutex.sh` false green; `deno.lock` re-freeze; `git grep` reads the index without a rev; ledger edits start from `git show HEAD:<path>`; generator golden 525/525 red — code-to-code claims only; dist-gated byte suites skip silently.
