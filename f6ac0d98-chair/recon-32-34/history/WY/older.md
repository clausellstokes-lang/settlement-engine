# WY (WAYFARE) — THE OLDER-PROGRAMME READER'S TABLE — 2026-09-16

**Seat:** the OLDER-PROGRAMME reader for family WY (wayfare, 44–67 C). The sibling seat (`ledger.md`) reads the ODQ, the queue docs and the git log and answers "was any WY wave ever dispatched" (its answer: no — 0 built / 0 part / 12 designed-only, plus one inert prefix token). **This seat answers the owner's other half: what do the programmes that came BEFORE WY already do of each wave's DESCRIBED behaviour, today, in running code.**

**Code read at** `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, HEAD `315080928` (2026-09-15, "LT37 car 6").
**Record read at** the ledger copy `a803dee7a` (`docs/DESIGN_FP_ARCH_WY.md`, 1,721 lines).
Read-only; nothing built, staged or run. Every figure is an executed `grep`/`sed`/`find` receipt quoted with path:line ⇒ **CONFIRMED**. Derivations and arithmetic are **PLAUSIBLE** and labelled.

Status vocabulary (the TRADE pilot's): **LANDED-LIT** (runs in a shipped preset) · **LANDED-DARK** (code present, no preset lights it) · **LANDED-INERT** (code present, unreachable regardless of flag) · **LANDED-THEN-REVERTED** · **DESIGNED-ONLY** · **RECORD≠CODE** (the record says one thing, the consist another).

---

## A. THE ANSWER FIRST

### A.1 In one paragraph, for the owner

**Yes — wayfare was built in far more depth than the WY volume's twelve-unbuilt row admits, and unlike trade, much of what exists is the waves' OWN described behaviour rather than mere substrate.** No WY wave's leaf exists — all nine `NEW` modules and all three walkers are ABSENT (CONFIRMED) — so 12/12 unbuilt is right at the leaf and the sibling seat's "never dispatched" verdict stands. But four older programmes already do the work: **THE ROADS (engine lift #5** — `roadsKernel.js`, 1,226 lines, `roadsEnabled: true` in the DEFAULT preset**)** runs named-person travel with legs, arrival, whereabouts, capture and ransom; **M1/army-transit** already carries WY-3's ENTIRE mover-body model on a different record — a path frozen at dispatch, a `departTick`, a per-tick `position01`, a nearest-node co-location snap and a deterministic two-body collision detector; **the M5/M7/M8/M9c/D-0 rumour carriers** already make six classes of mover carry news on the production tick; and **`TravelersLayer.jsx`** already *draws* three families of mover bodies interpolated at read time. So WY-3 is not "build a mover body" but "give the SHIPMENT record the body the ARMY record already has"; WY-5 is not "make movers carry news" but "make a mover carry a departure-time SNAPSHOT instead of relaying per hop"; WY-9/WY-10 extend a shipped layer rather than building a canvas. Six findings move the programme rather than the count. **(1) WY-3's C4 lie fence is already violated in shipped code** — the migrant sub-layer calls a route solver at render (`TravelersLayer.jsx:103`) and back-derives its depart tick (`:106`), because migration columns carry no `path` and no `departTick`; the wave's source-scan pin as specced would RED on landed code. **(2) WY-1's `modeSpeeds.js` has a pre-built seam it does not know about** — `namedPersonTransit.js` owns law-M's floor and an injected per-leg speed multiplier with three live suppliers, and its header reserves the slot verbatim for "a later mode-speed table"; meanwhile five de-facto mode speeds already exist at three different composition positions, so the car must rule composition, not author values. **(3) WY-8a double-drives a LIT four-layer supply model on BOTH of its arms** — `supplyIntegrity` already drives `accumulatedAttrition` through the attrition mitigation term, and reinforcement already replenishes it — and the model's seed is **generation-frozen** in `foodGenerator.js`, the TR-3 freeze's second instance in a different family. **(4) WY-6's economics already ship, lit** — `seizureTake` and banditry loss run inside `commodityFlow.js` under `commodityFlowEnabled: true`, so the prize arm joins two working machines. **(5) WY-5's going-dark fiction is inverted by a live producer** — the roads' known-world law already treats twenty-six weeks of silence as **calm**, and routes envoys on it. **(6) Two of the volume's own instructions have rotted** — `WarFaithTab.jsx` no longer exists (WY-7's budget clause names a dead file) and `hasMartialRecord` is already exported from a second module, so WY-8b's "export the module-local one" would mint a third spelling.

### A.2 Wave by wave — what an older programme already does of the described behaviour

| Wave | Recon row (PRICING §B) | What an older programme already does | This seat's verdict |
|---|---|---|---|
| **WY-0** lawful canvas | UNBUILT, 2–3 C | THE ROADS §13 shipped the whole layer the wave tidies: `TravelersLayer.jsx` (154 ln) + `travelersGeometry.js`, mounted `MapOverlay.jsx:351`, store key `travelers: false` (`mapSlice.js:112`), LayersPanel toggle `:252`. BOTH deletion targets present and drawn (`chevronPoints`, the Q3 arrows). | **the wave is tidying, not building** — and its manifest walker has a live catch waiting (claim 7) |
| **WY-1** scale charter | UNBUILT, 3–4 C | `distanceRead.hopWeeks` is the calibrated normalizer (37-file consumer census CONFIRMED); **five de-facto mode speeds already exist** under other names at three composition positions; and `namedPersonTransit.js` already owns law-M's floor AND an injected multiplier seam that its header reserves for a mode-speed table. | **PART-BUILT AS A SEAM** — the table plugs into a built mechanism; `kmScale` (absolute distance) is the genuinely new half |
| **WY-2** durable truth | UNBUILT, 2–3 C | The M10/M10a weathering lane is **LANDED-LIT on the production tick** with no flag; fork law, both distorting modes and the hiFi exemption are exactly as §1b describes; `degradeTelling` is severity-BLIND; the ground truth is LIVE (no TR-3 freeze). | **UNBUILT; substrate whole and LIT** — one multiplier on a running lane, but the rung source the volume names is a feedback ratchet (claim 12) |
| **WY-3** mover bodies | UNBUILT, 5–7 C | **M1/army-transit implements the entire model on the armyTransit record** (claim 13). The nine `legArrivalTick` writes re-census EXACT. The arrive-loop loss class is documented in-file. | **UNBUILT for shipments; BUILT WHOLE for armies** — second instance of a landed pattern; the lie-fence pin reds on shipped code |
| **WY-4** population cargo | UNBUILT, 4–6 C | The drop-when-absent conditional-field idiom F4/F5 need is **already built in the exact function they must edit** (`travelClass`, WAVE P2, `migration.js:558`). The two-producer overlap the r2 census flagged is real and measurable at source. | **UNBUILT; the mechanism has an exact in-file precedent** |
| **WY-5** carried word | UNBUILT, 3–5 C | **SIX mover-borne rumour carriers already relay on the production tick** — trade, army (M5), criminal (M7), ship (M8), refugee (D-0), teleport (M9c, hi-fi) — at `rumorNetwork.js:742-750`. `pickBetterTelling` (the wave's stated mount) exists at `:445`. The going-dark quantity is live and INVERTED: silence ⇒ calm (claim 19). | **PART-BUILT** — "movers carry news" ships for six classes; the new parts are the departTick snapshot, the tap-order law, and a Herald kind that contradicts a live read |
| **WY-6** prize arm | UNBUILT, 3–5 C | **E8 and E9 run LIT**: gate seizure via `seizureTake` (`commodityFlow.js:613`) and banditry loss (`:643-645`) under `commodityFlowEnabled: true`. E1's two-body detector is built (`armyTransit.js:534-558`); the army co-location snap exists (`:391`); E15 is landed AND mounted, so "born seeing fifteen" is true. | **UNBUILT; economics and both co-location halves already LIT** — the wave joins two working machines |
| **WY-7** projection spine | UNBUILT, 5–8 C | The audience-parameterized projection pattern already exists as `townMap/audienceProjection.js` over `display/publicSafe.js`, with a `'dm'\|'player'\|'public'` vocabulary that fails closed; three `*Projection.js` domain leaves already ship; the whereabouts vocabulary has a landed home (`roads/whereaboutsDisplay.js`). | **UNBUILT; every pattern it mints already exists under another name** — and one of its file targets is dead (claim 24) |
| **WY-8a** supply train | UNBUILT, 4–6 C | **A LIT four-layer army-supply model the volume names only once** — seeded `warArmyRecord.js:127`, eroded `attrition.js:312`, **lifted by reinforcement** `reinforcement.js:213`, and **already driving `accumulatedAttrition`** via the mitigation term. Seed **generation-frozen** at `foodGenerator.js:464`. | **UNBUILT; DOUBLE-DRIVES a live model on both arms** — C-WYF-3's one-supply-truth contract is load-bearing and the volume under-states it |
| **WY-8b** the mirror | UNBUILT (in 13–20 P) | `CONDITION_BANDS` still collides by name/quantity/model (`armyStrength.js:133`) and **the rename is still free — zero external consumers, tests included**. But M2's instruction has rotted (claim 25). | **UNBUILT; C-WYF-3's free-rename premise CONFIRMED, its sibling instruction ROTTED** |
| **WY-9** clickable canvas | UNBUILT (in 13–20 P) | Q4's chosen primitive `PortablePopup.jsx` exists (SP-6's) **with a working map-popup precedent** (`CausalityPopup.jsx:120`); the covert fail-closed idiom is the house pattern across four map surfaces; `navalTransit` (the ledger armies must absorb) is written on a production path. `linesForEntity` = 0 hits. | **UNBUILT; every primitive it names already ships** |
| **WY-10** flow layers | UNBUILT (in 13–20 P) | The `publicSafe` map-only wall exists; the caravan record it must bind to (`ShipmentRecord`) carries NO `path` and NO `departTick` (`supplyShipments.js:273-278`) — the binding is blocked at source, exactly as designed. | **UNBUILT; blocked on WY-3 at source, as charted** |
| **WY-11** lifeblood envelope | UNBUILT (in 13–20 P) | **The hole is exact and documented in-file**: `if (want >= T.MIN_SHIP)` (`commodityFlow.js:762`, `MIN_SHIP: 2` at `:118`) silently drops every sub-2-unit deficit, "mirrors M4's MIN_COLUMN" (`:116`; `MIN_COLUMN: 3` at `demographicsPushPull.js:231`). The EV floor the wave would raise already exists as a bounded scalar (`APPETITE_FLOOR: 0.3`). | **UNBUILT; the hole is measured, the knob exists** |

### A.3 What the older programmes already do that the WY waves describe

| Older mover / programme | Live at | What it does that a WY wave describes | Why it is not the WY wave |
|---|---|---|---|
| **THE ROADS, engine lift #5** (`DESIGN_THE_ROADS.md`; slices R-1b/R-2/R-3/R-4) | `roadsKernel.js` 1,226 ln + 12-module `src/domain/roads/`; `roadsEnabled: true` `simulationRules.js:675` (spread `:772` realistic_regional = DEFAULT, `:824`, `:865`, `:1073`) | Named persons physically travel: legs, phases (outbound/visiting/returning), arrival, whereabouts, capture, ransom, conversion; E3–E7 encounter arms | It is the estate WY-3/5/6/9 CONSUME; no WY field, no WY flag, no caravan or population body |
| **M1 army transit** (`4f76f1ee2`, per the TRADE fold) | `armyTransit.js` 711 ln + `armyTransitKernel.js`; `spatial/embattlement.js` | **WY-3's whole model on another record**: `planMarch` freezes `{path, departTick, arrivalTick}` (`:696-710`); `stepArmyPosition` (`:366-378`); `currentRegion` Math.round snap (`:391`); `remainingRegions` (`:400`); `detectCollisions` (`:534-558`) = E1 | The record is `armyTransit`, not `supplyShipments`; WY-3's F1/F2 are the SHIPMENT instance of this exact pattern |
| **M2 caravans / M6a commodity continuity** | `supplyShipments.js` 517 ln, `supplyKernel.js` 815 ln, `commodityFlow.js` 951 ln; `commodityFlowEnabled: true` (`:769`, `:938`) | Caravans physically travel with finite origin stocks, conservation, en-route tapping (E10), **gate seizure (E8) and banditry loss (E9)** | The shipment record has no body (no `path`, no `departTick`) — WY-3's exact hole; WY-6 adds an army seizer to a working taking-machine |
| **M5 / M7 / M8 / M9c / D-0 rumour carriers** | `rumorNetwork.js:742-750`, mounted `pulseKernel.js:2021` | **Movers carry news**: army, criminal (smuggle), ship (sea lanes), refugee (migration columns), teleport (hi-fi, never weathers) — six lanes on the production tick | Relay is per-hop along mover-derived edges, not a departTick SNAPSHOT of the origin's telling set; no tap-order law; no `no_word_from` kind |
| **M10/M10a organic weathering** | `degradeTelling` `rumorNetwork.js:472-506`; site `:705-711` | Truth decays with transit; both distorting modes; hiFi exempt; keyed fork per (event, carrier, edge, hop) | **Severity-blind** — WY-2's whole content is the missing multiplier |
| **M8 sea lanes** | `seaLanes.js` 629 ln, `SEA_COST_PER_DIST = 12` (`:96`, "~10× cheaper") | A de-facto sea MODE SPEED, expressed as a cheaper edge cost | WY-1 wants a MODE table; this is a cost, at a different composition position |
| **J4 route grades / named-person transit** | `namedPersonTransit.js` 201 ln; suppliers `routeNetworkConsumersTransit.js:147`, `npcCirculationTransit.js:141`, `ransomClaim.js:249/:253` | **Law-M's floor** (`MIN_LEG_TICKS: 1`, `:64-67`) and an **injected per-leg speed multiplier** with three live suppliers | The header (`:43-46`) reserves the slot for WY-1's table explicitly; the volume never names this module |
| **J1 route network** (`ed5ac49dd`) | 17 `routeNetwork*` modules, DARK — `routeLifecycleEnabled: false` (`:976`) | Charter/promote/demote/abandon/revive; E11 interdiction | Dark; WY-11's 2j riding and WY-10's legend inherit the darkness |
| **W-NAVY naval layer** | `navalLayer.js` 620 ln; `navalTransit` ledger written `realmVerbExecution.js:527/:539` | Sea movement records with the same strength/supply facet shape | WY-9's "armies absorb navalTransit" has a real ledger to absorb |
| **THE KNOWN PICTURE (R-2a)** | `roads/knownWorld.js`; `SILENCE_CALM_WEEKS: 26` (`roads/state.js:240`); callers `roadsKernel.js:493/:922` | **The going-dark quantity, live and routing on it**: a region heard nothing about is ASSUMED SAFE | WY-5's `no_word_from` would narrate the same silence as ALARM; the two readings must be reconciled |
| **W-C1 / W-F8 war supply** | `warArmyRecord.js:127`, `attrition.js:226/:310/:312`, `reinforcement.js:213`, `armyStrength.js:133-152`, `supplyQuality.js` | **A four-layer army supply model** with erosion, reinforcement replenishment, an attrition coupling and a banded phrase surface | WY-8a's F9 `supplyCargo` would be a THIRD supply quantity; both of 8a's arms already have a live driver |
| **SP-6 PortablePopup / townMap audience wall** | `primitives/PortablePopup.jsx`, `CausalityPopup.jsx:120`, `townMap/audienceProjection.js`, `display/publicSafe.js` | Q4's popup primitive with a landed map instance; the audience-parameterized projection with a fail-closed wall | WY-7/8b/9 mint the pattern again under new names unless they reuse these |
| **M4 demographics migration / flows.js displacement** | `demographicsMigration.js` (`MIN_COLUMN: 3`, `:221/:540`), `flows.js:42/:62` | Two non-disjoint refugee producers — the overlap WY-4's r2 census predicted | WY-4's "exactly one" fence has two live producers to arbitrate, measured |

### A.4 What this changes in the pricing (folder's arithmetic — **PLAUSIBLE**; each cause is a CONFIRMED receipt)

PRICING-32-33.md §B prices WY at **44–67 C**. This seat's findings do not move a wave to BUILT; they move car CONTENT and add three cars:

| Wave | PRICING | Suggested | Why |
|---|---|---|---|
| WY-1 | 3–4 C | **3–4 C** | unchanged, but car 1 must be re-briefed: supply the table into `namedPersonTransit.js`'s existing multiplier seam (`:43-46`) and RULE the composition of the five existing speed mechanisms, rather than mint a second floor/position model |
| WY-3 | 5–7 C | **6–8 C** | **+1: the lie-fence reconciliation car.** The C4 source scan reds on `TravelersLayer.jsx:103` today; either the migrant sub-layer stops re-solving (needs a column body, i.e. WY-4 work pulled forward) or the scan lands with a declared, dated exemption |
| WY-5 | 3–5 C | **4–6 C** | **+1: the going-dark reconciliation car.** `SILENCE_CALM_WEEKS: 26` already reads silence as calm and routes on it; `no_word_from` must either compose with that read or be declared a different quantity — otherwise the map and the Herald say opposite things about the same silence |
| WY-8a | 4–6 C | **5–7 C** | **+1: the one-supply-truth car (C-WYF-3's engine half).** Both of 8a's arms already have live drivers (`attrition.js:226`→`:310`; `reinforcement.js:213`); the car must fold, gate or own them before F9 mints a third quantity — and declare that the seed is generation-frozen (`foodGenerator.js:464`) |
| WY-8b | (in 13–20 P) | **+0, re-brief** | M2's instruction is rotted: `hasMartialRecord` is already exported at `martialReadiness.js:256`; reuse THAT and delete the module-local duplicate at `opportunism.js:122` |
| WY-7 | 5–8 C | **5–8 C** | unchanged; re-brief the budget clause (WarFaithTab.jsx is gone) and reuse `townMap/audienceProjection.js`'s audience shape rather than minting a new one |
| **WY total** | **44–67** | **47–70, centre ≈ 58** | +3 cars ≈ +5 %; no wave moves to BUILT, two move to PART-BUILT (WY-1 as a seam, WY-5 via six carriers) |

The #32 count row for WY moves from **0 built / 0 part / 12 unbuilt** to **0 built / 2 part / 10 unbuilt** — a status change, not a car change, because each "part" is behaviour a neighbouring programme landed under its own name.

---

## B. THE FULL CLAIM TABLE

| # | Claim | Wave | Status | Evidence at laneCONSIST-932 (`315080928`) | Label |
|---|---|---|---|---|---|
| 1 | **Every WY leaf the volume names is ABSENT.** `modeSpeeds.js`, `severityDurability.js`, `flowRegimeContract.js`, `moverTidings.js`, `armyCaravanSeizure.js`, `armySupply.js`, `treatyProjection.js`, `personProjection.js`, `armyProjection.js`; walkers `encounterPairs.walker.test.js`, `mapLayerManifest.walker.test.js`, `wayfareMirror.walker.test.js`. | all | DESIGNED-ONLY | `find src -name <leaf>` / `find tests -name <walker>` return nothing for all twelve | CONFIRMED |
| 2 | **THE ROADS (engine lift #5) is LANDED-LIT in the DEFAULT preset.** `roadsKernel.js` = 1,226 lines; `roadsEnabled: true` at `simulationRules.js:675` (ONE_REGEN), spread at `:772` (realistic_regional — the default), `:824`, `:865`, `:1073`. A 12-module `src/domain/roads/` estate sits beside it (`embassyHazard`, `embassyLedger`, `knownWorld`, `migrationReason`, `ops`, `seaRoads`, `state`, `thirdPartyRansom`, `userRouteIdentity`, `userRoutes`, `verification`, `whereaboutsDisplay`). | WY-0/3/5/6/9 | LANDED-LIT | the lines above; `ls src/domain/roads/` | CONFIRMED |
| 3 | **`commodityFlowEnabled: true`** at `simulationRules.js:769` and `:938` — so every caravan mechanism below (E8, E9, E10, MIN_SHIP) runs in a shipped world. | WY-3/6/10/11 | LANDED-LIT | the lines above | CONFIRMED |
| 4 | **`tradeFlowsEnabled: true`** at `simulationRules.js:73` (DEFAULT); `false` in the quiet (`:744`) and void (`:836`) presets. | WY-3/10/11 | LANDED-LIT | the lines above | CONFIRMED |
| 5 | **`routeLifecycleEnabled: false`** at `simulationRules.js:976` — the 17-module J1 route estate is DARK, so WY-10's legend rows and WY-11's 2j riding inherit a dark network. | WY-3/10/11 | LANDED-DARK | the line above; `find src -name "routeNetwork*"` = 17 modules | CONFIRMED |
| 6 | **`TravelersLayer.jsx` already draws three families of mover bodies, interpolated at read time.** 154 lines; header cites DESIGN_THE_ROADS §13. (a) ARMY columns off `spatialLedgers.armyTransit` positioned by `rec.position01 ?? progress01(tick, rec.departTick, rec.arrivalTick)` (`:81-82`); (b) MIGRANT columns (`:94-111`); (c) NAMED-NPC ENVOYS off `spatialLedgers.roads.missions` with three phases (`:114-131`). Mounted `{layers.travelers && <TravelersLayer />}` at `MapOverlay.jsx:351`; store key `travelers: false` (`mapSlice.js:112`); user toggle `LayersPanel.jsx:252`. Its own header states the army + migrant sub-layers are NOT flag-gated (`:8-11`) and that the layer is DM-secret (`:14-17`, gate `:52 viewerSeesDmSecrets`). | WY-0/9/10 | LANDED, DEFAULT-OFF (store key, not a preset flag) | the lines above | CONFIRMED |
| 7 | ⭐ **`travelersFilter` is an UNDECLARED store key with two readers and an unguarded writer.** It appears in `mapSlice.js` only inside a COMMENT (`:109`); it is NOT a `DEFAULT_LAYERS` key (the key list runs `placements … travelers: false … travelRings … territory …`). Read at `TravelersLayer.jsx:40` and `LayersPanel.jsx:96-97`. `toggleLayer` guards `if (!(key in state.mapState.layers)) return;` (`mapSlice.js:378`) but `setLayerFilter` writes unguarded (`:382-384`). Readers treat `undefined` as "all three sub-layers", so it works today — and it is exactly the undeclared-key class WY-0's manifest walker is chartered to catch, live in shipped code before the walker exists. | WY-0 | LANDED-INERT until a user writes it | the lines above | CONFIRMED |
| 8 | **Both WY-0 deletion targets are present and drawn.** `chevronPoints` is defined at `travelersGeometry.js:84` and called for every marker at `TravelersLayer.jsx:148`. The Q3 deployment arrows are built at `WarFaithMapOverlay.jsx:93-94` from `activeDeployments(worldState)` ("Public-tier ledger"), with the `sf-deploy-arrow` marker def at `:152` and the render at `:188-192`. | WY-0 | LANDED-LIT (inside the lit overlay) | the lines above | CONFIRMED |
| 9 | **`hopWeeks` is the calibrated normalizer, and the 37-file consumer census holds.** `PRIMARY_HOP_WEEKS_ANCHOR = 1` (`distanceRead.js:60`), `MAX_HOP_WEEKS = 52` (`:71`), calibration derived from the digest (`:489`, `:530`, `:539`), documented `:624-634` ("even a max-winter mountain hop is SLOW, never infinite"). `grep -rln hopWeeks src/` = **37 files**. | WY-1 | LANDED-LIT | the lines above | CONFIRMED |
| 10 | ⭐ **Five de-facto mode speeds already exist under other names, at THREE different composition positions.** (a) ARMY: `ARMY_SPEED_FACTOR: 1.5` + `MAX_MARCH_WEEKS: 52` (`armyTransit.js:57/:59`) applied as `Math.ceil((base * ARMY_SPEED_FACTOR) / speedMult)` clamped to `[1, 52]` (`:299-300`), with the march-cap < siege-max-age invariant documented at `:662-664`. (b) SEA: a cheaper EDGE COST — `SEA_COST_PER_DIST = 12` "vs land's ~90..1560 per cell-step: ~10× cheaper" (`seaLanes.js:96`, applied `:472`). (c) MIGRATION/COURIER: raw `hopWeeks` (`migration.js:39-40`, "arrivalTick = now + hopWeeks"). (d) SEASONS-B M3: multiplies `hopWeeks` at READ time. (e) J4 route grade: a leg multiplier (claim 11). A MODE_SPEEDS table must therefore rule COMPOSITION across edge-cost, read-time and leg-tick positions, not merely author values. | WY-1 | LANDED-LIT (each in its own lane) | the lines above | CONFIRMED |
| 11 | ⭐⭐ **`namedPersonTransit.js` is the domain-side canonical mover physics the WY volume never names — and it already reserves WY-1's slot.** Law-M's floor is built: `NAMED_PERSON_TRANSIT_TUNING.MIN_LEG_TICKS: 1` (`:19`), applied as `Math.max(MIN_LEG_TICKS, Math.floor(nominal * multiplier))` (`:64-67`). The injected per-leg speed multiplier already has THREE live suppliers — J4 route grade (`routeNetworkConsumersTransit.js:147 gradeLegMultiplier(hop.grade)`), hidden-path slowdown (`npcCirculationTransit.js:141 HIDDEN_PATH_SLOWDOWN`) and ransom legs (`ransomClaim.js:249/:253`). The header states the reservation verbatim (`:43-46`): J4 owns the grade vocabulary today "while a later mode-speed table can supply another multiplier without creating a second floor or position model". | WY-1 | LANDED-LIT | the lines above | CONFIRMED |
| 12 | **The M10/M10a rumour weathering lane is LANDED-LIT on the production tick, with no flag.** `advanceRumorLedgers` is called from `pulseKernel.js:2021` at STEP 3.5, gated only by dormancy (no spatial digest / omniscient infoMode, documented `:1992-1995`). The weathering site (`rumorNetwork.js:705-711`) fires in `'unreliable'` AND `'full'` and never for hiFi carriers, forking `rumor-organic:${record.eventRef}:${carrier}:${edgeId}:${hop}` (`:710`) — §1b's substrate description is exact at the consist. | WY-2 | LANDED-LIT | the lines above | CONFIRMED |
| 13 | ⭐ **`degradeTelling` is severity-BLIND, and the rung source the volume names is a FEEDBACK RATCHET.** The drift constants read nothing severity-derived: `u < 0.08` perfect, `u >= 0.97` severe-garble (×0.55/×0.65 + a forced band jump), else `completeness *= 0.8 + 0.15*u'`, a 0.30 band-drift chance and a 0.12 name-swap chance (`rumorNetwork.js:477-499`). Raw `severity` is consumed exactly ONCE, at the seed — `magnitude: magnitudeBandOf(entry.severity)` (`:398`, bands at `:247-252`) — and is never stored on the record (`grep -n severity rumorNetwork.js` returns only `:156 :193 :246 :247 :398`). The arrival record keeps only `content.magnitude`, which the weathering itself mutates ±1 (`:481`, `:486`). So deriving the durability rung "by INDEX from the existing magnitude bands" (§1b.2) feeds a hop's durability from prior garbles — a downward ratchet the spec does not name. **Two DRIFT-IMMUNE carriers already ride every packet**: `significance` ('major'\|'notable', seeded `:628`) and `score` (`:629`), both copied verbatim at relay (`:734-735`). | WY-2 | live producer / better ground truth available | the lines above | CONFIRMED |
| 14 | ⭐⭐ **WY-3's mover-body model is already BUILT WHOLE on the armyTransit record.** `planMarch` freezes `{ path, departTick, arrivalTick, marchWeeks }` at dispatch (`armyTransit.js:696-710`); the record declares them (`:311-313`) and normalizes them (`:345-347`); the kernel mints them at `armyTransitKernel.js:718` and `:910-912`; `stepArmyPosition` advances `position01 = clamp01((t - depart)/span)` (`:366-378`); arrival is guaranteed because both ticks are frozen at seed (`:662-663`); `currentRegion` snaps to the nearest path node with `Math.round(position01 * (path.length-1))` (`:391`) and `remainingRegions` slices the rest (`:400`). This IS WY §2b's shipment-body lifecycle clause, landed for a different record. | WY-3 (also 6/9/10) | LANDED-LIT | the lines above | CONFIRMED |
| 15 | **The nine `legArrivalTick` WRITE sites re-census EXACT at the consist.** Eight in `roadsKernel.js` — assignments at `:487`, `:510`, `:706`, `:720`, `:731`, `:749`, object-literal mints at `:812`, `:1028` — plus `consumeMissionRecall` at `roads/state.js:189`. `seaRoads.js`'s three occurrences (`:96`, `:103`, `:107`) are READS, not a tenth write. `TravelersLayer.jsx`'s two (`:120`, `:121`) are reads. | WY-3 | census holds | the lines above | CONFIRMED |
| 16 | **The arrive-loop reconstruction loss class WY-3 must fix is documented in-file.** `commodityFlow.js:558` records the precedent verbatim: a multi-tick smuggle run "loses smuggle:true on reconstruction" and the criminal rumour carrier is affected. | WY-3 | live hazard | `commodityFlow.js:558` | CONFIRMED |
| 17 | ⭐⭐ **WY-3's C4 lie fence is ALREADY VIOLATED by shipped code — the migrant sub-layer re-solves a route at render.** `TravelersLayer.jsx:103` calls `candidateRoutes(digest, o, d)` inside the render `useMemo` and draws the column on `routes[0].path`; `:106` back-derives the depart tick as `progress01(tick, arrivalTick − hopWeeks(o,d), arrivalTick)`. It must, because a `MigrationColumn` carries NO `path` and NO `departTick` — the record is `{originId, destId, travellers, roadDeaths, arrivals, arrivalTick}` (`migration.js:326-331`). Today's migrant marker is therefore exactly the "lie" WY-3's MARKER-TRUTH SOURCE SCAN is written to forbid, and the pin as specced would RED on landed code unless the scan carries a declared exemption or the column gains a body. | WY-3 / WY-4 / WY-10 | live violation of an unbuilt wave's own fence | the lines above | CONFIRMED |
| 18 | **WY-4's conditional-field idiom has an exact precedent in the function it must edit.** `travelClass` (WAVE P2) is stamped drop-when-absent inside `columnOf`: `...(COLUMN_CLASSES.indexOf(travelClass) >= 0 ? { travelClass } : {})` (`migration.js:558`), with class predicates at `:485` (demographic) and `:523` (military), the plan-side stamp at `:582-583` and the class folded into the column key at `:591`. | WY-4 | LANDED-LIT | the lines above | CONFIRMED |
| 19 | **WY-4's two-producer overlap is real and measurable at source.** Producer A: `flows.js` `DISPLACEMENT_STRESSORS` (`:42`) gated at severity ≥ 0.6 (`:62`, attenuated per settlement `:69`), emitting refugee fractions `min(0.12, 0.04 + (sev−0.6)*0.2)` (`:78`). Producer B: M4 demographics migration, gated by `MIN_COLUMN: 3` (`demographicsPushPull.js:231`; `demographicsMigration.js:221` and `:540`, "A PLACEMENT UNDER MIN_COLUMN IS NOT A COLUMN" `:184`). Neither knows about the other; the "exactly one" fence has two live producers to arbitrate. | WY-4 | two live producers | the lines above | CONFIRMED |
| 20 | ⭐⭐ **SIX mover-borne rumour carriers already relay on the production tick — "movers carry news" is built.** `advanceRumorLedgers` calls `relayVia` six times (`rumorNetwork.js:742-750`): TRADE/'merchant' over `tradeNeighbours(graph, sid)`; ARMY/'army' (M5); CRIMINAL/'criminal' (M7 smuggle); SHIP/'ship' (M8 sea lanes, port-to-port "skips the land chain"); REFUGEE/'refugee' (D-0 migration columns, behind `migrationRumorsEnabled`); TELEPORT/'teleport' with `hiFi = true` (M9c, never weathers). Carrier declarations and their design notes are at `:72-101`; the per-carrier neighbour maps at `:330`, `:340`, `:351`; assembly in `migrationRumors.rumorCarrierParams` (`:129`) because pulseKernel is at its line ceiling. WY-5's stated mount, `pickBetterTelling`, already exists (`:445`, used `:430`). | WY-5 | LANDED-LIT | the lines above | CONFIRMED |
| 21 | ⭐⭐ **WY-5's going-dark fiction is INVERTED by a live producer that already routes on it.** `roads/knownWorld.js` (THE KNOWN PICTURE, engine lift #5 slice R-2a) implements the §5 law "SILENCE DECAYS TOWARD CALM": a danger rumour's believed weight decays linearly with age to calm after `SILENCE_CALM_WEEKS`, and "a region the observer has heard NOTHING about is ASSUMED safe (absent ⇒ level 0)" (`:17-19`); the constant is `SILENCE_CALM_WEEKS: 26` at `roads/state.js:240` ("no news from a region for this long ⇒ ASSUMED calm"), read at `knownWorld.js:65`. `knownEmbattlementView` is called on the LIT production path at `roadsKernel.js:493` and `:922` (and at `routeNetworkCharterDanger.js:166`, dark). WY-5's `no_word_from` Herald line would narrate as ALARM the same silence the engine already treats as REASSURANCE — the wave must compose with this read or declare a different quantity. | WY-5 | live producer with the opposite sign | the lines above | CONFIRMED |
| 22 | ⭐ **WY-6's economics already ship, LIT.** E8 gate seizure: `take = seizureTake(rec.carried, smuggle.conscienceOf(worst.id))` at `commodityFlow.js:613`, the conscience-scaled taking WY-6 re-uses (`smuggle.js:316`, documented `:35` — "a good/lawful seizer loots little, an evil one takes near-all"). E9 banditry: a `banditry:${key}:${rec.arrivalTick}` fork over `routeDangerLevel(route)` feeding `banditryLoss({channelStrength, dangerLevel, rng})` (`commodityFlow.js:643-645`). Both inside a LIT module (claim 3), and both already named in the conservation identity (`:45-47`, `:248`: "Σ banditry loss + Σ carried on caravans cut mid-transit"). | WY-6 | LANDED-LIT | the lines above | CONFIRMED |
| 23 | **E1's two-body encounter detector is built, pure and deterministic.** `detectCollisions(records, hostilePair)` (`armyTransit.js:534-558`) scans codepoint-sorted pairs, skips arrived columns (`:548`), intersects `remainingRegions` and emits the codepoint-first shared region. It is E14's shape one body class short (army × army rather than army × caravan). | WY-6 | LANDED | the lines above | CONFIRMED |
| 24 | **E15 is landed AND mounted, so WY-6's walker is correctly "born seeing fifteen".** `src/domain/worldPulse/espionage/espionageGauntlet.js` exists; `advanceEspionageGauntlet` is imported by `envoyPulse.js:48`; it carries a fork-registry row (`habitForkRegistry.js:147`, `HBF-05`) and is named as a home in `infiltrationDepth.js:101/:108`. | WY-6 | LANDED | the lines above | CONFIRMED |
| 25 | **§4's recorded co-location divergence is confirmed at source, with both sides named.** `armyTransit.currentRegion` snaps with `Math.round(position01 * (path.length-1))` (`:391`); `namedPersonPathPosition` snaps with `Math.floor(alongStoredPath * last)` (`namedPersonTransit.js:190`). The volume records the disagreement without naming the second module. | WY-6/9 | measured divergence | the lines above | CONFIRMED |
| 26 | ⭐ **The audience-parameterized projection pattern WY-7/8b/9 describe already exists under another name.** `src/domain/townMap/audienceProjection.js` — "the audience wall shared by the plan, handout, fog view, and the 3D scene compiler" — declares `@typedef {'dm'\|'player'\|'public'} TownMapAudience` (`:22`), routes through `toPublicSafe` (`display/publicSafe.js`, imported `:16`) and **fails closed on unknown visibility values** (`isPlayerVisibleMapFact`, `:27-33`). Three more `*Projection.js` domain leaves already ship (`npcLedgerProjection.js`, `envoyErrandProjection.js`, `dmFieldProjection.js`). | WY-7/8b/9/10 | LANDED-LIT | the lines above | CONFIRMED |
| 27 | **RECORD≠CODE: WY-7's `WarFaithSection` census still counts TWELVE files, but the MEMBERSHIP has drifted and one of the volume's named budget files no longer exists.** At the consist the twelve are `dossierLazyTabs.js`, `LivingWorldTab.jsx`, **`FaithTab.jsx`**, `faithPanelModel.js`, `livingWorldSignals.js`, `entityLinks.js`, `pdf/lib/liveWorld.js`, `pdf/sections/FaithWar.jsx` + four tests (`entityIndex`, `liveWorld`, `liveWorldBTrack.parity`, `summaryTabPhase2`). The volume's list names `WarFaithTab`; `find src -name "WarFaithTab*"` returns NOTHING — `FaithTab.jsx` carries the spelling now (`:20`). WY-7's budget clause ("OutputContainer and WarFaithTab are near their ceilings — every consumption lands in tab-body leaves") therefore names a dead file. The volume's own rule (C5: re-census by grep at build, never a fixed count) covers the count; it does not cover the budget sentence. | WY-7 | RECORD≠CODE | the lines above | CONFIRMED |
| 28 | **`KIND_PREFIX` is module-local exactly where PRICING says.** `entityLinks.js:13` declares it `const` (not exported), consumed at `:38` (`KIND_PREFIX[kind] \|\| slugifyEntity(kind)`). | WY-7 | LANDED | the lines above | CONFIRMED |
| 29 | **WY-7's D7 whereabouts unification has a landed home and a wide surface.** `roads/whereaboutsDisplay.js` is "THE DOSSIER WHEREABOUTS LINE (§12) — pure display formatter over the `npc.whereabouts` mirror: one line + one short badge label", SECRET BY CONSTRUCTION and a ZERO-IMPORT LAZY LEAF. `grep -rln whereabouts src/` = **35 files** across roads, envoy, espionage, npc, store and certification. | WY-7 | LANDED-LIT | the lines above | CONFIRMED |
| 30 | ⭐⭐ **WY-8a would DOUBLE-DRIVE a LIT four-layer supply model, on BOTH of its two arms.** (a) THE COUPLING ARM is already live: `attrition.js:226` folds `supplyIntegrity` into `mitigation` (`SUPPLY_MITIGATION * supply + …`, capped `MAX_MITIGATION`), so low supply already raises `lossFraction`, which already ratchets the army's own wear — `accumulatedAttrition: clamp01((Number(r.accumulatedAttrition) \|\| 0) + lossFraction)` (`:310`) — and the same pass erodes supply itself (`:312 supplyIntegrity: erode(r.supplyIntegrity, 0.5)`, documented `:266`). (b) THE REINFORCEMENT ARM is already live: `reinforcement.js:213 supplyIntegrity: lift(r.supplyIntegrity, flow.flowFraction * 0.8)` replenishes supply from reinforcement flow with no physical cargo. (c) A THIRD spelling already exists on the transit record: `supplyQuality` (`armyTransit.js:316/:350`), derived at `armyTransitKernel.js:674` as `clamp01(num(dep.supplyIntegrity, num(dep.deployedQuality, 1)))` and multiplied into `effectiveFieldStrength` (`armyTransit.js:418-422`). WY-8a's `supplyCargo` (F9) would be a FOURTH. ODQ §94.2i's C-WYF-3 one-supply-truth contract is therefore load-bearing, and the volume names only the band collision. | WY-8a | LANDED-LIT (the model the wave must reconcile with) | the lines above | CONFIRMED |
| 31 | ⭐ **The army-supply model's SEED is GENERATION-FROZEN — the TR-3 freeze's second instance, in another family.** `supplyIntegrity` seeds as `clamp01(norm(facets.logistics, 0.5) * qMult)` (`warArmyRecord.js:127`) and `foodReserve` from the same facet (`:132`); `facets` comes from `capacityFor(id)` (`warCapacityReads.js:192`); `facets.logistics` is computed at `militaryStrength.js:237` as `clamp0100(food.resilienceScore * 0.6 + storageContribution)` over `economicState.foodSecurity.resilienceScore` (`:238`) and `.storageMonths` (`:241`); and the ONLY writer of `foodSecurity.resilienceScore` tree-wide is **`src/generators/foodGenerator.js:464`** — the same generator file the TRADE pass found freezing `foodRatio`. Every other hit reads it (PDF view models, OverviewTab `:405`, DefenseTab `:185`, `structuralFingerprint.js:212`, `fieldManifest.js:96`). **Unlike TR-3 the derived state then EVOLVES** (eroded `attrition.js:312`, lifted `reinforcement.js:213`), so the freeze bounds the seed, not the trajectory — but nothing a settlement does in play can improve the logistics facet it was generated with. | WY-8a/8b | frozen ground truth | the lines above | CONFIRMED |
| 32 | **C-WYF-3's "the rename is measured FREE" premise HOLDS at the consist.** `CONDITION_BANDS` is declared `const` (not exported) at `armyStrength.js:133`, consumed only at `:152` inside `conditionPhrase`, and re-exported only as a member of `ARMY_STRENGTH_PHRASES` (`:237`). `grep -rn "CONDITION_BANDS" src/ tests/` returns exactly those three lines. Its quantity is `Math.min(supplyIntegrity, morale, foodReserve)` (`:148-151`) — a THIRD meaning of "condition", distinct from `REMAINING_BANDS` (`:80-86`, the attrition ladder WY-8b's M4 actually wants) and from `accumulatedAttrition`. | WY-8b | collision live; cure still free | the lines above | CONFIRMED |
| 33 | ⭐ **RECORD≠CODE: WY-8b's M2 instruction has ROTTED — following it would mint a THIRD spelling.** The volume says `hasMartialRecord` "already exists module-local at `opportunism.js:122` … EXPORT-AND-REUSE, never a second spelling". At the consist it is **already exported** from `martialReadiness.js:256` and consumed by `WarTab.jsx:48/:421`, while the module-local duplicate at `opportunism.js:122` (used `:137`) still stands. The correct act is now "reuse `martialReadiness`'s export and delete the `opportunism` local", not "export the `opportunism` one". | WY-8b | RECORD≠CODE | the lines above | CONFIRMED |
| 34 | **WY-9's chosen popup primitive exists, with a working MAP precedent.** `src/components/primitives/PortablePopup.jsx` (SP-6's, signature `{open, title, onClose, children, testId, width = 560}` at `:39`) is already used as a map popup by `CausalityPopup.jsx` (`:38` import, `:120` render) with the "no acknowledge" convention documented at `:16`. `linesForEntity` (the marker→Herald query) = **0 hits tree-wide**. | WY-9 | primitive LANDED-LIT; the wave's own read ABSENT | the lines above | CONFIRMED |
| 35 | **WY-9's covert tightening rides an established fail-closed idiom.** `viewerSeesDmSecrets` (`domain/display/viewerSecrets.js`) is consumed by four map surfaces — `TravelersLayer.jsx:30/:52`, `RoadScenePanel.jsx:14/:61`, `HeraldRemembrance.jsx:62/:212`, and `heraldWanderers.js:285` — each computing `{ isOwner: !!auth?.user, authenticated: !!auth?.user }`. | WY-9 | LANDED-LIT | the lines above | CONFIRMED |
| 36 | **WY-9's "armies absorb navalTransit" has a real ledger to absorb.** The `navalTransit` spatial ledger is written on a production path (`realmVerbExecution.js:527` read, `:539` `setSpatialLedger(state, 'navalTransit', …)`), counted by `spatialUsage.js:139` ("W-NAVY sea movement") and listed among the arrival-time record families at `:268`; `navalLayer.js:367-445` gives naval records the same strength/supplyQuality facet shape as armies. `pulseKernel.js:2389` documents the naval dormancy golden. | WY-9 | LANDED | the lines above | CONFIRMED |
| 37 | **WY-10 is blocked at source exactly as charted — the shipment record has no body.** `ShipmentRecord` is `{institutionId, settlementId, input, sourceId, arrivalTick, starving}` (`supplyShipments.js:273-278`) — no `path`, no `departTick`. The minted record at `commodityFlow.js:759-763` confirms it in code. So "recordless shipments simply do not draw" is not a design choice the wave makes, it is the state of every shipment today. | WY-10 | blocked at source | the lines above | CONFIRMED |
| 38 | ⭐ **WY-11's hole is exact, documented in-file, and mirrored in migration.** `if (want >= T.MIN_SHIP)` at `commodityFlow.js:762` gates every dispatch, with `MIN_SHIP: 2` and `MAX_SHIP: 12` at `:117-118` and the comment at `:115-116`: "a link with a deficit below MIN_SHIP does NOT dispatch (keeps the ledger sparse — no 1-unit top-up caravans; mirrors M4's MIN_COLUMN)". A persistent 1-unit deficit is therefore served NEVER, not slowly — which is precisely what WY-11's floor law (2h) and WY-3's 3b quantization cadence exist to fix. The migration twin is `MIN_COLUMN: 3` (`demographicsPushPull.js:231`; "A PLACEMENT UNDER MIN_COLUMN IS NOT A COLUMN", `demographicsMigration.js:184`). Two adjacent constants: `ORIGIN_MIN_AVAILABLE: 1` (`:112`) and `TAP_CAP: 3` (`:123`). | WY-11 / WY-3 | the hole, measured | the lines above | CONFIRMED |
| 39 | **WY-11's "raise the dispatch EV/eligibility floor" has a built, bounded knob.** `dispatchEV.js` already carries `APPETITE_FLOOR: 0.3` / `APPETITE_CEIL` bounding a dynamic greed scalar (`:105`, clamps at `:243`, `:265`, `:280`, `:285`) and a `RISKY_DANGER_FLOOR: 0.25` deadband (`:121`). The "ECONOMIC never physical" constraint maps onto an existing bounded scalar rather than a new one. | WY-11 | LANDED-LIT | the lines above | CONFIRMED |
| 40 | **The volume's own §4 substrate claim (thirteen existing resolvers E1–E13) is sound at the consist for every arm this seat opened**: E1 `detectCollisions` (claim 23), E8 `seizureTake` + E9 banditry + E10 tap (claims 22, and `TAP_CAP` claim 38), E11 `routeNetworkConsumersInterdiction.js` present (dark, claim 5), E15 mounted (claim 24). E2–E7/E12/E13 live in the roads/envoy estate (`roadsKernel.js`, `embassyHazard.js`, `seaRoads.js`, `thirdPartyRansom.js`) — all present as files; their individual resolvers were not each opened. | WY-6 | LANDED | file presence CONFIRMED; per-arm behaviour for E2–E7/E12/E13 **PLAUSIBLE** | PLAUSIBLE |

---

## C. METHOD NOTES, GREP TRAPS, AND THE SEAT'S YIELD

### C.1 Grep traps banked by this pass (all CONFIRMED at source)

1. **`travelMode`** is the world's derived spatial axis `instant/compressed/standard/slow` (already banked in RECHECK §5) — **not** WY-1's mode speeds. WY-1's real cousin is `gradeMultiplier` (claim 11), which no WY name matches.
2. **"supply" names FIVE unrelated families**: `supplyIntegrity` (deployment record, live-evolving), `supplyQuality` (transit record, derived from it), `supplyCargo` (WY-8a's unbuilt F9), `supplyKernel.js`/`supplyShipments.js` (M2's TRADE links — nothing to do with armies), and `supplyWebWarfare.js` (a pressure instrument). A grep for "supply" in the army context returns mostly M2's trade estate.
3. **"condition" names THREE quantities inside ONE file**: `REMAINING_BANDS` (`armyStrength.js:80`, attrition remainder — what WY-8b's M4 actually wants), `CONDITION_BANDS` (`:133`, `min(supply, morale, food)` — the name WY-8b must rename), and `accumulatedAttrition` (the ratchet). C-WYF-3 calls this "three layers deep"; the three layers are all in `armyStrength.js`.
4. **`progress01` is a name collision the estate documents against itself**: `travelersGeometry.js:22` warns that the export "shares a NAME with an independently-computed property in four" places. `routeNetworkConsumersTransit.js:278/:293` has its own leg-local `progress01`; `armyTransit`'s is `position01`.
5. **`hasMartialRecord` has TWO live spellings** — the exported `martialReadiness.js:256` and the module-local `opportunism.js:122`. A grep that stops at the first hit picks the wrong one (claim 33).
6. **`WarFaithTab.jsx` does not exist.** Any WY-7 instruction keyed to it (including its ceiling budget) is unanchored; `FaithTab.jsx` carries the `WarFaithSection` spelling now (claim 27).
7. **`legArrivalTick` in `seaRoads.js` (3 hits) is READ-ONLY** — it is not a tenth write site, and a naive count of occurrences (18 in `roadsKernel.js` alone) inflates the nine-write census by 2×.
8. **`path` is on the armyTransit record ONLY.** `MigrationColumn` and `ShipmentRecord` have none — a grep for `path:` across the spatial ledgers finds the army one and suggests, wrongly, that mover bodies are general.
9. **`travelers` (a `DEFAULT_LAYERS` key) ≠ `travelersFilter` (not a key at all)** — the filter exists only as a comment plus three consumers (claim 7).
10. **`roadsEnabled` is THE ROADS' own flag, not a WY flag.** Every WY flag is at 0 hits (the sibling seat's R2); `roadsEnabled` being lit is what makes half this table live.
11. **`MIN_SHIP` / `MIN_COLUMN` are the WY-11 hole, but they live in TRADE's and POP's files** — a WY-named grep finds neither.
12. **`banditry` is spelled only inside `commodityFlow.js` and `embattlement.js`** — WY-6's "prize"/"seizure" vocabulary matches neither of the two live taking mechanisms.

### C.2 Did reading the older programmes find things a name-search missed?

**Yes — eight findings, none of which a WY flag/leaf name-search could produce**, because each lives under another programme's name or in a mechanism rather than an identifier:

1. **WY-3's model is already built for armies** (claim 14) — found by asking "what already has a body?", not "does `caravanBodiesEnabled` exist?". Turns a 5–7 C build into a second instance of a landed pattern.
2. **WY-3's own lie fence is already violated** (claim 17) — found by reading the shipped layer the wave edits, not by grepping for the fence.
3. **WY-1 has a reserved seam with three live suppliers** (claim 11) — found in a module the volume never names, whose header names the future wave.
4. **WY-2's rung source is a feedback ratchet, and two drift-immune carriers already exist** (claim 13) — found by asking "what does the record actually keep?".
5. **WY-5's fiction is already built six times over, and its Herald line contradicts a live routing read** (claims 20, 21) — found by following the carrier constants and the roads' §5 law.
6. **WY-8a double-drives a lit model on both arms, over a generation-frozen seed** (claims 30, 31) — found by tracing `supplyIntegrity` end to end; the freeze is the TR-3 finding's second instance and was not in either code pass.
7. **Two volume instructions have rotted** (claims 27, 33) — found only by re-opening the files the volume names.
8. **Twelve grep traps** (C.1), of which the five-way "supply" split and the three-way "condition" split would each mislead an implementer inside a single file.

**What it did NOT find: a single WY wave built whole under another name.** The sibling seat's "designed-only, never dispatched" verdict is unshaken. The owner's suspicion is right about the *physics and the surfaces* — roads, army bodies, caravan taking, mover-borne news, the popup, the audience wall, the travelers overlay are all real and mostly lit — and right that the WY volume under-credits them; it is the WY *waves* that were never dispatched.
