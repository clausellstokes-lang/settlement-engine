# HISTORY PASS — POP (POP-1 … POP-7, +POP-5a/5b) — **THE SKEPTIC** — 2026-09-15

Every verdict below was **re-derived from scratch** at the product consist
`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`,
HEAD **`3150809288ba864527bacbca0b861bec0d139eff`** (2026-09-15 17:43:28 -0400, "LT37 car 6" —
`git log -1`, executed), against the volume
`ledger-a803dee7a/docs/DESIGN_FP_ARCH_POP.md` (605 lines, read whole) and the standard set by
`HISTORY-TRADE-2026-09-15.md` (§A.1, §A.3, §A.4, the grep-trap paragraph, §C, read whole).

**Standard.** CONFIRMED = a receipt I executed myself in this seat (a `grep`, a `sed -n` read,
a `git log`), quoted with `path:line`. PLAUSIBLE = derivation, arithmetic, or an absence
argument. Read-only: no suite run, no `npm`, no `vitest`, nothing staged, nothing mutated.
I verified **all twenty shas** the adjudicator cites by `git log -1` — every one exists with the
subject claimed (table §F).

---

## A. THE ANSWER FIRST

### A.1 In one paragraph, for the owner

**The adjudicator's headline is right and its count is too generous.** Populations *were*
built far more deeply than the POP volume admits — four older programmes (WAVE P, M11a/M11b,
SP-B, GR-2/GR-3, CAPACITY) built most of POP's machinery under their own names, and some of it
is lit in the preset a new realm is born into. But when I apply the pilot's own rule —
*substrate the wave was chartered to CONSUME is not the wave* — four of the adjudicator's five
"part-built" waves fail it, because the volume itself already **booked** the thing being
credited: §2 books SP-B's `pullBand` as POP-1's satisfied precondition, §1a S31 books M11a's
plague arc, §5 contract 2 books GR-2/GR-3's treaty terms *with their landing date*. Crediting
booked substrate is double-counting: PRICING already priced those waves with it in hand. Only
two waves have code that landed **after** the volume compiled, that the volume did **not** book,
and that genuinely reduces the wave's own work: **POP-6** (CAPACITY C1's crowding line +
NEWSTRAIN's eight phrase rows, both of which refute the volume's own S30) and **POP-7**
(CAPACITY C3's four capacity tripwire rows + the per-year series). My count is therefore
**0 built / 2 part-built / 6 unbuilt**, not 0/5/3. The money moves slightly the other way from
the adjudicator's: **33–47 C, centre ≈ 40** (POP-4 +2 and POP-6 −1 and POP-7 −1 as adjudicated,
**plus a new POP-5a +1** I found by fiction search). And the four mandatory car 0s stand —
the owner sitting on `demographicsEnabled` above all, which I confirmed twice over, once in the
code and once in the ledger's own preset enumeration.

### A.2 Wave by wave — the re-derived verdict

Legend: **ADJ** = the adjudicator's verdict. **SKEPTIC** = mine. The deciding receipt is my own
executed read at HEAD `315080928`.

| Wave | ADJ | **SKEPTIC** | Why I differ (or confirm), with the receipt |
|---|---|---|---|
| **POP-1** the believed road | PART | **UNBUILT (substrate booked)** | **DOWNGRADE.** The wave's own four behaviours are zero: `believedMigrationRead.js`, `arrivalClearing.js` = **0 files ever created on any branch** (`git log --all --diff-filter=A -- "*/<leaf>"` returns empty for all eight POP leaves); `believedMigrationEnabled` **0 src / 0 tests**; the landing is still unconditional — `demographicsMigration.js:464` `const target = writable(column.destId) ? column.destId : writable(column.originId) ? column.originId : null;`, `:468` `accounting.lost`, `:478` `accounting.returned`, `:495` "newcomers reach ${name} and are **taken in**", and a grep of the whole landing stage (`:450-500`) for `room\|capac\|bound\|viab` returns **one comment line and no read**. What the adjudicator credits — SP-B's `pullBand` and `conditionsGroundTruth` — is exactly what the volume's §2 POP-1 row already books as "SP-2 believed-conditions subject family LANDED (**SATISFIED**)", and PRICING priced POP-1 at 5–7 C *with that satisfied*. Crediting it again is double-counting. The adjudicator's own caution ("a brief change, not a car change") concedes this. |
| **POP-2** the commons arc | UNBUILT | **UNBUILT** | **CONFIRM.** `commonsArcEnabled` 0/0; `commonsAnswer.js` never created; `refused_by_the_commons` **0 src hits**; the misrule gap open verbatim at `assizeKernel.js:344-347`. The substrate is dark twice over: `commonsVoiceEnabled` has **no manifest row** (it is a BACKLOG entry at `tests/lint/engineGatedRuleKeys.walker.test.js:195`) and **0 `: true`** in src. Fiction search (second angle): nothing anywhere refuses a levy or a plan on popular consent — `warHomeCosts.js:445/:452` banks `leviedPopulationBySource` with no refusal term. |
| **POP-3** departure memory | UNBUILT — "the only genuinely greenfield POP wave" | **UNBUILT — but NOT greenfield** | **CONFIRM the status, REJECT the epithet.** `departureMemoryEnabled` 0/0; `departureMemory` **0 src**; `kinBand` 0; `rememberedAs` 0; `departureMemory.js` never created. **But an origin-keyed arrival memory already ships LIT**: traditions T-4 captures each in-transit column's `{originId, count}` (`traditionsKernel.js:629-649`) and mints a **transplanted rite** when cumulative influx from ONE origin crosses `ADOPTION_THRESHOLD = 0.12` over `INFLUX_WINDOW_YEARS = 3` (`traditions/relations.js:50-51`) — and `traditionsEnabled: true` sits in `ONE_REGEN` (`simulationRules.js:674`), spread into the **default** preset at `:772`. The volume books this as S25 and POP-3's pin list already names the double-count negative; so it is booked substrate, not a status change — but "no older cousin" is false, and the S25 reconciliation is real work. |
| **POP-4** the plague arc | PART (+2 C) | **UNBUILT (+2 C stands)** | **DOWNGRADE, cars unchanged.** Every described behaviour is absent: `calamityArcEnabled` 0/0, `calamityArc.js` never created, **`calamityArcs` 0 src hits**, the burial line still food-gated only (`demographicsHerald.js:83-93` `HUNGER_DEFICIT_FLOOR: 0.20` / `HUNGER_NET_LOSS_FLOOR: 4`, no sickness arm), and the close is silent (`pestilenceKernel.js:262-264` pushes only `receipts.push({ id: c.id, kind: 'cleared' })` while the open pushes a full `materializationNews` at `:258`/`:270-298`). The staging the adjudicator credits is **M11a's, and the volume books it at S31** ("Pestilence front is a genuine traveling arc … VERIFIED"). M11b is a **collision**, not a slice: it is a different subsystem that POP-4 must reconcile with, and the reconciliation is why this wave's history finding **adds** two cars. A wave whose history pass raises its price is not part-built. |
| **POP-5a** road drama | UNBUILT | **UNBUILT (+1 C — NEW)** | **CONFIRM the status; ADD A CAR the adjudicator and both readers missed.** `roadDramaEnabled` 0/0; `columnEvents.js` never created; RF-1 holds verbatim (`spatial/migration.js:543-560`, `columnOf` rebuilds a five-key whitelist with the conditional `travelClass` spread). **The fiction already ships, lit and silent**: M4 kills people on the road today — `roadDeaths` is a per-route integer draw on embattlement × season at `spatial/migration.js:402-415`, conserved into `departures == originDeaths + roadDeaths + arrivals` (`:351`), gated only on `migrationActive` = `spatialCanonVersion > 0` (`:175-179`), i.e. **any canonized world, behind no preset flag** — and `grep -c newsEntries` is **0** in BOTH `migrationKernel.js` and `spatial/migration.js`. POP-5a's road-death narration and its `no-new-mortality negative on P2` therefore sit beside a live, unnarrated mortality sink. This is POP's exact analogue of trade's famine-profiteering find. **POP-5a 4–5 → 5–6.** |
| **POP-5b** the permit table | PART | **UNBUILT (substrate booked)** | **DOWNGRADE.** Zero of the wave's four permit read-sites exist: `moverPermitted` (`demographicsLadder.js:209`) has **exactly one** src caller, `demographicsResponses.js:244` (P4's founding gate); `migrationRightFor` (`treatyEnforcement.js:300`) has **zero** src callers (the only other hits are a comment and a registry row at `couplingRegistryGrammar.js:148/:208`); `moverPermitsEnabled` 0/0. The treaty half the adjudicator credits is GR-2/GR-3's and the volume **books it with its date** in §5 contract 2: "GR-3 term catalog (**LANDED 2026-08-06** — contract COLLAPSED to one state)". `RECHECK-32-34.md:72` already ruled this exact shape: "a tested function with no engine caller and a registry row reading `built:false` does not make a wave". I hold that standard. (The drafting lane is real and mounted — `pactTriggers.js:256` → `pactFormation.js:262`, ladder `:180-183`, catalog `peaceTermsCatalog.js:250/:251/:254`, mount `settlementLifecycleKernel.js:91/:564` — and dark: `pactFormationEnabled` 0 `: true`.) |
| **POP-6** the hopeful half | PART (−1, "possibly −2") | **PART-BUILT (−1 only)** | **CONFIRM the status, REJECT the second −1.** Genuine post-volume relief, landed by other programmes and contradicting the volume's own S30: the crowding line (`demographicsHerald.js:166 crowdingCrossingOf`, ranks `:143-145`, minted `:436-446`; file grown 266 → **465** lines, CAPACITY C1 `ec6b0a132`) and **eight** WHAT_PHRASES rows (`settlementRumors.js:349,350,351,355,357,474,475,477`). **But the adjudicator's correction 2 over-reads.** `population_decline` and `population_growth` do have fallback pools (`rumorFallbackPhrasePools.js:624`, `:634`-family, rostered `:990-991`) — and they are **`recordMode: 'state_only'` candidates**: `populationDynamics.js:499` `...(!major && kind !== 'emigration' ? { recordMode: 'state_only' } : {})`, and the certification row states the measurement: "**MEASURED 2026-07-31: zero population_growth and zero population_decline in all seven completed release cases**" (`subsystemRowsPeople.js:280`), corroborated in the ledger ("population drift was already `state_only`", ODQ:10524-block). A pool that no public telling reaches is not voicing. **Practically five kinds are mute, not three** — the older reader's original five, reached by a different route. Keep −1; refuse −2. R-28 still open at the moved address `demographicsHerald.js:420` with the honest fork at `:422-423`. C2's reader `display/demographicReading.js` (13,551 B) has **zero src importers** — LANDED-INERT, confirmed. |
| **POP-7** convergence | PART (−1) | **PART-BUILT (−1, PLAUSIBLE)** | **CONFIRM the status; CORRECT the receipt.** The rail is real: all four capacity rows are inside the exported roster (`scripts/soak/tripwires.mjs:202 export const TRIPWIRES`, rows at `:392 capacity_plateau`, `:461 capacity_floor_thaw`, `:494 capacity_realm_load`, `:543 capacity_envelope_30y`, array closes `:623`), and `measureRealmDemography` is wired (`demographicsObservation.js:103/:196` → `scripts/audit/behavioral-observation.mjs:15/:1003`). **Two corrections.** (i) "three execute" was **false at §907 and true only after §909**: ODQ §907's M1-F1 found `capacity_plateau` and `capacity_floor_thaw` "unable to fire on any written receipt — a false green", and the file now carries the cure in its own words ("⛔⛔ MEASURED BLIND, SAID SO, AND **CURED AT §909**", `:397-401`, `:464-467`). (ii) `capacity_envelope_30y` is **armed, not refused** — it is a full row in the exported array with a gate and a `requires`; `:703` is the walker's account of the *old* C3 refusal, which §907 itself calls "a wrong refusal". (iii) And the thing that matters: **all four rows gate on `receipt?.subsystems?.rules?.demographicsEnabled === true`** — they can only ever grade a configuration no shipped preset produces. No population convergence contract exists (`ls src/domain/certification/` = `tradeConvergenceContract.js` + three war files, nothing population-side). |

**COUNT (skeptic): 0 built / 2 part-built / 6 unbuilt.** Part: **POP-6, POP-7**. Unbuilt:
POP-1, POP-2, POP-3, POP-4, POP-5a, POP-5b. Against the two code passes' "8 of 8 UNBUILT" this
is still a correction — two waves move — but it is half the adjudicator's move, and the
difference is entirely the booked-substrate rule. **CONFIRMED at every row; PLAUSIBLE at the
total.**

### A.3 The corrected car estimate

| Wave | PRICING §B | ADJ | **SKEPTIC** | Why |
|---|---|---|---|---|
| POP-1 | 5–7 | 5–7 | **5–7** | unchanged; the brief re-cut (R1 + stale S1 + the three-structure Q1) is free |
| POP-2 | 4–6 | 4–6 | **4–6** | unchanged |
| POP-3 | 3–4 | 3–4 | **3–4** | unchanged; the S25 traditions double-count is already in its pin list |
| POP-4 | 3–4 | 5–7 | **5–7** | **+2 ADOPTED**: the M11b reconciliation car 0 and the silent-close fork, both CONFIRMED |
| POP-5a | 4–5 | 4–5 | **5–6** | **+1 NEW**: own, gate or fold M4's live silent road mortality before asserting no-new-mortality |
| POP-5b | 4–5 | 4–5 | **4–5** | unchanged |
| POP-6 | 6–9 | 5–8 | **5–8** | **−1 ADOPTED** (crowding line + eight rows); the further −1 **REFUSED** (state_only, measured zero) |
| POP-7 | 3–5 | 2–4 | **2–4** | **−1 ADOPTED, PLAUSIBLE** — the rows execute only against a lit receipt no preset produces |
| **POP total** | **32–45** | 32–46 | **33–47, centre ≈ 40** | +1 car net on the adjudicator. Still not money: the yield is the briefs |

Lighting is not priced per wave — PRICING §D routes every #32 flag through the single second
declared lighting window. POP's is unusual and must be declared there: its *precondition*
`demographicsEnabled` governs a measured behaviour change (§907: realm 21844 → 12289, ×0.56 over
300 years), so POP's lighting is a declared shift with movers named, never a quiet flip.

---

## B. THE FIFTEEN CORRECTIONS I MAKE TO THE ADJUDICATOR

Each is a claim of the adjudicator's that I re-opened and found wrong, overstated, or
under-counted. Every receipt is my own.

| # | The adjudicator said | What I measured | Label |
|---|---|---|---|
| **S1** | POP-4 **PART-BUILT** — "the most built of the seven" | Every POP-4 behaviour absent; the staging credited is M11a's, **booked at the volume's own §1a S31**; M11b is a collision that *raises* the price. → **UNBUILT** | CONFIRMED (code); status call PLAUSIBLE |
| **S2** | POP-5b **PART-BUILT** | Zero of four permit reads wired; the treaty half is **booked at §5 contract 2 with its landing date**; `RECHECK:72` already ruled this shape UNBUILT. → **UNBUILT** | CONFIRMED |
| **S3** | POP-1 **PART-BUILT** | The credited axis is **booked at §2 as the wave's SATISFIED precondition** and was already priced in. → **UNBUILT (substrate booked)** | CONFIRMED |
| **S4** | POP-6 possibly **−2 C** because `population_decline`/`_growth` "are already voiced" | Both are `recordMode: 'state_only'` (`populationDynamics.js:499`) and **measured zero in all seven completed release cases** (`subsystemRowsPeople.js:280`). Five kinds are practically mute, not three. → **−1 only** | CONFIRMED |
| **S5** | C.4: "K_food is **generation-frozen** at every configuration the product ships" | **Overstated.** The LOCAL arm is frozen (CONFIRMED by the code's own words: `demographicsRates.js:532` "*resilience every tick but never touches dailyProduction or dailyNeed*"; writers only `foodGenerator.js:428` and `economy/foodBalance.js:459`). Both IMPORT multipliers are dark. But the **OBLIGATION arm is LIVE AND LIT**: `reliefCreditorCount` (`demographicsRates.js:499-511`) reads `spatialLedgers.obligations` for `kind === 'grain_relief'`, which `generosityKernel.js:775` mints every tick behind `constructiveFlowsEnabled` — and that flag is in `WAVES` (`simulationRules.js:653`), spread into the **default** preset at `:770`. **Corrected statement: K_food can only ratchet DOWN** — which is worse for the §907 plateau question, not better | CONFIRMED |
| **S6** | POP-5a: only "declare `roads/migrationReason.js` as the waiting reader" | **A mandatory reconciliation car is owed**: M4's `roadDeaths` already kill, lit and silent (§A.2). **+1 C** | CONFIRMED |
| **S7** | POP-3 is "the only genuinely **greenfield** POP wave" | A lit, origin-keyed arrival memory already ships (traditions T-4, §A.2). Status unchanged; the epithet is wrong | CONFIRMED |
| **S8** | R1's false claim lives at "`PROGRAMME-32-34.md:371` / `RECHECK §3 L371` / `HISTORY-TRADE` row 37" | It is at **four** PROGRAMME sites — `:45`, `:80`, `:273`, `:371` — and at HISTORY-TRADE `:116`, `:148`, `:179`. It is **NOT in RECHECK** (190 lines total; its POP rows at `:116-117` label POP-1..7 **PLAUSIBLE** and make no §49 claim). A re-brief must fix four lines, not one | CONFIRMED |
| **S9** | `demographicsEnabled: true` — "src 0, **tests 10**" | src 0 ✔; **tests 19 files / 48 lines**; and **3 script files** the adjudicator omitted: `scripts/soak/register.mjs:193`, `scripts/audit/realm-scale-certification.mjs:99/:109`, `scripts/review/readerCorpus.mjs:135`. The soak harness *does* light it — which is how §907's measurements exist at all | CONFIRMED |
| **S10** | `:1027 demographicsEnabled: false` (preset unnamed) | It sits in **`full_simulation`** (that preset opens at `:867`; `realistic_regional` at `:746`) — the "everything-on" ceiling preset declares it false by name. Independently corroborated in the ledger: "all seven shipped presets checked — **zero light `demographicsEnabled`** (six omit it, `full_simulation` declares it false, the default rules carry no key)" (ODQ:10524) | CONFIRMED |
| **S11** | POP-7: "three tripwire rows **execute**"; the fourth "present and **refused**" | Two of the three were a **false green** until §909 cured them (ODQ §907 M1-F1; the cure is written into the file at `:397-401`/`:464-467`); the fourth is **armed**, inside the exported roster; and all four gate on a lit receipt no preset produces | CONFIRMED |
| **S12** | `pullBand` is "reserved to **POP** by name" (`beliefAxisSubjects.js:120-123`) | The code says: "*`pullBand` is **SP-B's own populations road** and no ES product may name it (ES architecture, amendment F5)*" (`:119-120`). It is reserved **from ES, to SP-B**. POP is not named | CONFIRMED |
| **S13** | `git log --all --grep='POP-'` returns **22** commits | 22 **without** `-i`; **30 with `-i`**, which is the form the brief prescribes. Still zero POP-wave commits either way | CONFIRMED |
| **S14** | (not raised) | **New grep trap**: POP-3's `causeClass` collides with the live CAUSE-LIFECYCLE subsystem's field — **109 src hits across 12+ files** (`causeVocabulary.js`, `causeLifecycle.js`, `settlement.schema.js:1009`, `npcProfile.js:598`, `demographicsHerald.js:12`). A bare grep says POP-3 is built | CONFIRMED |
| **S15** | (not raised) | **New grep trap**: `git log --all -S<POP flag>` over the whole repo returns **41–44 commits for every one of the six POP flags** — and `… -- src` returns **0 for all six**. Every hit is a DOCS commit (the volume names all six flags). `-S` on a POP identifier must be pathspec-limited to `-- src`, or it reads the design corpus as code. Executed: `believedMigrationEnabled` 0/44 · `calamityArcEnabled` 0/41 · `roadDramaEnabled` 0/43 · `moverPermitsEnabled` 0/41 · `departureMemoryEnabled` 0/43 · `commonsArcEnabled` 0/43 | CONFIRMED |

---

## C. WHAT I RE-OPENED AND **CONFIRM** — the adjudicator was right

| Claim | My own receipt |
|---|---|
| **R1: §49's POP-1 blocker is ALIVE** (the one finding that would mis-brief a launch-path lane) | The ledger's words at ODQ:2017-2022 (inside §49, heading at :1991): "*there IS no caravan reception machinery at HEAD; migration landing is unconditional with no capacity read (demographicsMigration.js:453-482)*". At HEAD those two cited sites are pre-departure: `MIGRATION_REFUSALS` at `:107-109` under a docstring reading "*Every column **that did not form** names why*", minted inside `competeForDestinations` (`:194`) at `:231-237`. The landing (`:453-497`) reads no capacity. **LG-9..12, ES §46c and pg-3 still wait on POP-1** | CONFIRMED |
| **Car 0 #1 — THE OWNER SITTING** | `simulationRules.js:1027 demographicsEnabled: false` with the code's own instruction at `:1024-1026` ("**LIGHTING IT belongs at the owner-signed soak redo** … flip this one value there"); `demographicsRates.js:375 DEMOGRAPHIC_TUNING_SIGNATURE = Object.freeze({ signed: false, lit: null })`; `densityBands.js:60 REGISTER_VII_SIGNATURE`, derived at `densityLaw.js:99`; ODQ **§907(e)** verbatim: "*The `demographicsEnabled` class-E STOP inside L-DEFAULT: its trigger (the runaway) does not reproduce, its plateau half is unproven … **the STOP holds; nothing here lights it**.*" And FVQ:1803 confirms WR-10's lighting sequences "*after `demographicsEnabled`*" | CONFIRMED |
| **Car 0 #2 — POP-4's M11b reconciliation** | `simulationRules.js:768 disastersEnabled: true` inside `realistic_regional: preset(DEFAULT_SIMULATION_PRESET_ID, …)` (`:746`), also `:819`, `:927`; `spatial/calamity.js:134` is the `=== true` gate; `calamityKernel.js:410 popBefore`, `:434 population: afterDeaths`, `:437 reason: 'Killed in the calamity.'`, `:441 popToTier(afterDeaths - loss.exodus)`, `:459 calamityHistory`; mounted `pulseKernel.js:70/:2451`. A live shipping mortality sink | CONFIRMED |
| **Car 0 #4 — C-POPF-6 unresolved** | `C-POPF` appears at exactly **7** ODQ lines; the last is `:5350` "*C-POPF-6 is a hard STOP awaiting chair*"; nothing later resolves it | CONFIRMED (grep); "unresolved" PLAUSIBLE (absence argument) |
| **R2 — the volume's S1 is stale** | `demographicsKernel.js:303 const pool = Math.max(0, before - named);` but `:313 const after = Math.max(Math.min(named, before), before + births - deaths);` with `// CS-B2 (cs-4): floor WITHOUT the mint.` at `:305` (`ac2d32ac8`, 2026-08-15, eleven days after the volume) | CONFIRMED |
| **R3 — PRICING's POP-1 "blocked on: nothing" is false** | `demographicsEnabled` 0 `: true`; `migrationRumorsEnabled` is a manifest entry at `simulationRules.js:309` and 0 `: true` anywhere. Two of three lit-preconditions unmet | CONFIRMED |
| **R4 — ONE_REGEN carries `urbanFabricEnabled: true`** | `simulationRules.js:666-675` `ONE_REGEN` with `urbanFabricEnabled: true` at `:670`, spread into the default at `:772` (and `:1078`) | CONFIRMED |
| **R5 — the "no treasury" premise is false** | `src/domain/worldPulse/treasury.js` = **73,777 B**; `treasuryEnabled` 0 `: true` in src (dark, but present) | CONFIRMED |
| **R6 — S30's fallback premise is pool-gated** | `settlementRumors.js:587-594` `const legacyPool = FALLBACK_PHRASE_POOLS[key]; if (legacyPool) { … } return UNREGISTERED_SUBJECT;` (value at `:517`) | CONFIRMED |
| **R8 — R-28 open at a moved address** | `demographicsHerald.js:420` | CONFIRMED |
| **C.5 — two owner-ordered programmes never built** | `plagueTouched` 0/0 · `afflictionBand` 0/0 · `contagionEnabled` 0/0 · `stressorPin` 0 · `pinnedStressor` 0 · `dmPinned` 0 | CONFIRMED |
| **realmPressure01 is dark, not frozen** | producer `demographicsObservation.js:137`; consumers only `warReasons.js:803-804` (gated `demographicsLit`) and `demographicsWar.js:172/:295` | CONFIRMED |
| **RF-1 holds verbatim** | `spatial/migration.js:543-560` | CONFIRMED |
| **POP-5a's waiting reader is real and imported** | `src/domain/roads/migrationReason.js` (3,573 B) ← `src/domain/briefs/roadScene.js:40` | CONFIRMED |
| **C2's reader is LANDED-INERT** | `src/domain/display/demographicReading.js` (13,551 B); `grep -rln` returns the file itself only | CONFIRMED |
| **All twenty cited shas exist as described** | §F below | CONFIRMED |

---

## D. TWO FINDINGS OF MY OWN, FROM SEARCHING BY FICTION

The brief orders every UNBUILT wave searched two further ways, **by the wave's fiction rather
than its identifiers**. That produced the +1 car above and one more declaration duty.

**D.1 — POP-5a's fiction ships lit and silent (the +1 car).** Asked "does anyone already die on
the road in this world?", not "does `columnEvents.js` exist?". Answer: yes, every tick, in any
canonized world. `spatial/migration.js:402-415` draws `roadDeaths` per route from embattlement ×
season and folds it into the conservation identity at `:351`; the gate is `migrationActive`
(`:175-179`) = `spatialCanonVersion > 0`, a **user-invoked state**, not a preset flag
(`campaignSpatialCanonize.js:171` is the only writer; the macro is
`canonizeCampaignWorldSpatial`, reached from `LivingWorldGates.jsx:116` via
`campaignWorldPulseSlice.js:377/:398`). And `grep -c newsEntries` is **0** in both
`migrationKernel.js` and `spatial/migration.js`. So the world already kills travellers and
never says so. POP-5a must own, gate or fold that sink before it narrates road death or asserts
`no-new-mortality` on P2 — exactly the shape of trade's famine-profiteering reconciliation, and
for the same reason: otherwise the wave double-counts its own fiction at two grains.

**D.2 — POP-1's reception fiction is half-built on the CHOICE side, lit.** Asked "is anyone
already turned away, or welcomed, anywhere?". `generosityKernel.js:569/:966-1004/:1121-1133`
writes a drop-when-empty **`refugePostures`** sub-ledger (E1c), and
`migrationKernel.buildDestinationCandidate` **reads it as the `refugePosture01` axis on
`destinationScore`** (`generosityKernel.js:51-53`; `spatialUsage.js:341`). It runs behind
`constructiveFlowsEnabled`, which is **lit in the default preset** (`simulationRules.js:653` in
`WAVES` → `:770`). So a host's willingness to take refugees already biases *where* columns go —
and still nothing happens *at the wall*. This sharpens R1 rather than softening it (the estate's
reception machinery is all on the departure/choice side), and it hands POP-1 a live producer it
must declare rather than duplicate. No car: a declaration in the arrival-clearing charter.

---

## E. GREP TRAPS — CONFIRMED AT SOURCE BY THIS SEAT

1. ⛔ **`POP-[0-9]` matches `DS-POP-1/2/3`.** `git log --all -i --grep='POP-'` = **30** commits
   (22 without `-i`), **not one a POP wave**: `bc1773983`/`94a6f33f4` are September's prose-rewrite
   dossier sections, `acdc2bcd5` retires a DS-POP-2 item, `9165f910b` is the POP-S stamping round;
   the rest are `pop-up`, `population`, `highlights`. Executed.
2. ⛔ **`PULL_BANDS` has three incompatible definitions.** `beliefAxisSubjects.js:107` =
   `['shunned','overlooked','sought','coveted']` (the one `pactFormation.js:262` imports);
   `faithWitnessSource.js:104` and `npc/livedExperienceCatalog.js:134` are both
   `['faint','firm','heavy']`.
3. ⛔ **`calamity` is M11b's LIT natural disaster**, not POP-4's `calamityArcs` (**0 src hits**).
   The collision sits directly over a live mortality sink.
4. ⛔ **`causeClass` is the live cause-lifecycle field — 109 src hits, 12+ files** (NEW, §B S14).
5. ⛔ **`git log -S<POP flag>` unrestricted reads the DOCS corpus** — 44 commits for
   `believedMigrationEnabled`, all documentation (NEW, §B S15). Always `-- src`.
6. **`demographicReading.js`** (singular, `display/`, inert) vs **`demographicReadings`**
   (plural, `demographicsPushPull.js`, live). One letter.
7. **`population_decline` in `settlementRumors.js:353` is a COMMENT**; the real pool is in a
   different file (`rumorFallbackPhrasePools.js:624`). Per-file grep says absent, tree-wide says
   present — and **both mislead**, because the kind is `state_only` and measured zero.
8. **`commonsVoiceEnabled` is not in the manifest** — it is a walker BACKLOG entry
   (`engineGatedRuleKeys.walker.test.js:195`) while the kernel reads it at
   `commonsVoiceKernel.js:50`.
9. **`migrationRumorsEnabled` is manifested (`simulationRules.js:309`) and lit nowhere** —
   manifest membership is not lighting, and PRICING read it as satisfied.
10. **A preset flag is not the only kind of lighting.** M11a pestilence, M4 migration and the
    whole column lane are gated on `spatialCanonVersion > 0` — a **user action**
    (`canonizeCampaignWorldSpatial`), invisible to every `: true` census. A `grep ': true'`
    lighting sweep will call all three dark. They are not.
11. Inherited and re-confirmed: `mass_migration` is a generation stressor; `C-POPF-n` are compile
    cures while `POP-F`/`POP-S` are stamping rounds; `P1..P5` is the July/August wave-P programme;
    `tests/domain/demographicsWorldsHand.test.js:524`'s "the permit table is TOTAL" is P4's ladder,
    not POP-5b.

---

## F. SHA VERIFICATION — all twenty, `git log -1` executed at the consist

| Sha | Date | Subject (short) | Verified |
|---|---|---|---|
| `d620a05d4` | 2026-08-01 | P1: the trillions die — demography becomes a difference of rates | ✔ |
| `439030622` | 2026-08-01 | P1a: the floor thaws | ✔ |
| `ef54119e9` | 2026-08-01 | P2: the realm circulates | ✔ |
| `7ce0148bd` | 2026-08-01 | P3: a settlement stops reacting and starts deciding | ✔ |
| `99e2d54f2` | 2026-08-01 | P4 lands + two missing war causes | ✔ |
| `47b4ed9dc` | 2026-08-01 | Demographics P5: reconcile promotion and capacity truth | ✔ |
| `82ad676b2` | 2026-07-13 | Phase 5.5, M11a — PESTILENCE | ✔ |
| `62c81a0ce` | 2026-07-13 | Phase 5.5, M11b — CALAMITY | ✔ |
| `45eb5387e` | 2026-07-15 | W-UPSWING stage 0 — THE CALAMITY BUCKET | ✔ |
| `ec6b0a132` | 2026-09-03 | CAPACITY C1 | ✔ |
| `6bddd6183` | 2026-09-03 | CAPACITY C2 | ✔ |
| `d02c5acde` | 2026-09-03 | CAPACITY C3 | ✔ |
| `4c87bae45` | 2026-08-15 | WC-0B I2: the column-class union | ✔ |
| `ac2d32ac8` | 2026-08-15 | CS-B2: the named floor stops minting people | ✔ |
| `432ff6441` | 2026-09-06 | LGT-C2-DEFAULT hunk 1: twenty-one VIRTUAL keys into the default | ✔ |
| `898dcb5b8` | 2026-09-07 | §907: HORIZON-B6 | ✔ |
| `bc1773983` | 2026-09-14 | REWRITE CLARITY economy B12 (DS-POP-2/3) | ✔ |
| `94a6f33f4` | 2026-09-14 | REWRITE CLARITY economy B11 (DS-POP-1) | ✔ |
| `acdc2bcd5` | 2026-08-22 | §401: DS-POP-2 item retired | ✔ |
| `9165f910b` | 2026-08-14 | docs: POP-S collected | ✔ |

Plus, new here: **none of POP's eight new leaves has ever existed on any branch** —
`git log --all --diff-filter=A -- "*/<leaf>"` returns empty for `arrivalClearing.js`,
`believedMigrationRead.js`, `commonsAnswer.js`, `departureMemory.js`, `calamityArc.js`,
`columnEvents.js`, `populationEditor.js`, `populationLines.js`. **There is no
LANDED-THEN-REVERTED anywhere in POP.**

---

## G. WHAT I COULD NOT SETTLE

1. **Whether the owner ever signed the demographic tuning constants.** Three unsigned
   signatures read; no ODQ § records a signature; FVQ:147's 2026-08-01 LIGHTING BATCH ruling
   sequences the flip "**BEFORE the soak redo**" and it never happened. **PLAUSIBLE: unsigned.**
2. **Whether C-POPF-6 was resolved off-ledger.** **PLAUSIBLE: unresolved.**
3. **Whether a pool-only kind satisfies the three registration walkers** — the adjudicator's own
   E.4. I went one step further and found it moot for the two kinds at issue: they are
   `state_only` and measured zero, so the question does not decide POP-6's price. It still
   decides how a *new* POP-6 kind must land. **Open.**
4. ~~The `-S` census over `src` alone.~~ **CLOSED, executed.** `git log --all --no-renames
   -S<flag> -- src` returns **0 commits for all six POP flags**, against 41–44 commits each
   tree-wide (all documentation). Combined with §F's `--diff-filter=A` leaf search, no POP flag
   or leaf has ever been added to `src` on any branch in the estate's history. Nothing open.
5. Read-only. **CONFIRMED** here means the code, commit or ledger text was read at the cited
   address at HEAD `315080928`; it never means a suite was observed green.
