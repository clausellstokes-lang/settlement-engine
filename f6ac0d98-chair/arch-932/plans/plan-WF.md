# PLAN — FAMILY WF (FAITH), the §932+ lane plan (the Fable seat, family planner, 2026-09-15 ~23:1x EDT)

Read-only at the consist `laneCONSIST-932` = `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932` (HEAD `315080928`, LT37 car 6, porcelain 0 — CONFIRMED by `git log -1` / `git status --porcelain`), against the ledger worktree `…/f6ac0d98…/scratchpad/ledger-a803dee7a` (`docs/DESIGN_FP_ARCH_WF.md` 846 lines, `DESIGN_FP_ARCHITECTURE.md` §5 rows #32–#41 and L2 at `:92`). Governing documents, in precedence: `ARCH-PROTOCOLS-ADDENDUM-1.md` (R1–R5, the door train D0) > `ARCH-PROTOCOLS.md` (§1–§7) > `recon-32-34/HISTORY-WF-2026-09-15.md` (the history fold WINS over PRICING §B wherever they differ — its §A.4 estimate, its five mandatory car 0s, its ten RECORD≠CODE rows, its grep traps) > `HISTORY-TRADE-2026-09-15.md` §A.1/§A.3/§A.4/the grep-trap paragraph/§C (binding on every family that touches treaties, belief or coin) > `PRICING-32-33.md` §A.3/§A.4/§B/§C/§D > `RECHECK-32-34.md` §1/§5 > `PROGRAMME-32-34.md` §B. Every figure below is **CONFIRMED** (I opened the file at the cited line, or ran `eff-any.mjs` / a `node -e` import at the consist) or **PLAUSIBLE** (reasoning). Nothing was executed against vitest, npm or git state.

**STATE OF THE RECORD (one paragraph, for every WF brief to copy):** the history pass HAS run for WF (`HISTORY-WF-2026-09-15.md`, four seats + the folder's own re-execution). It corrects the recon in three places every brief obeys: (1) **WF-1 is LANDED-DARK-WHOLE**, not "BUILT — code wins": six `feat(WF-1x)` cars are ancestors, `faithUnseatingEnabled: true` appears in NO preset (`grep -c` = 0 at `simulationRules.js`; re-run by me: the register carries it at `:295`, the presets do not), and it has NO PRICING §B row — the built-lit directive makes its lighting an unpriced 1–2 C car this plan seats in lane 16a; (2) **WF-6's landed half is LANDED-INERT**: `pactFormation.js:212-219 selfBandsOf` passes `row.religionState` (a key nothing writes; the pulse projects to `config.faithProfile`, `religionState.js:614-671`) into `devotionGroundTruth` → `null` → `rungOf = -1` → `NO_CROSSING` at `pactTriggers.js:230-232`, so no faith term has ever been drafted (re-read by me at both files — CONFIRMED), and two of the five catalog rows are unproducible by arithmetic (`shared_rite` min 0 / `missionary_access` min 0.7 against the attainable score set {1/3, 2/3, 1} and `CROSSING_FLOOR01 = 0.5`; `CLASS_TERM` at `peaceTermsCatalog.js:385-394` carries zero faith rows, so the catalog's own `:238-239` "the row a war's end can EXTRACT" is RECORD≠CODE); (3) **five mandatory car 0s** (WF-0 the DATA car re-ranked first; WF-1's lighting; WF-3's five-producer reconciliation; WF-6's crossing repair + rung ruling; WF-8's Q1 chair ruling with an overlap clause) and the corrected estimate **51–79 C, centre ≈ 65** (PRICING said 47–74). This plan adds ONE family-wide finding the fold did not price (§3 C0-F below): the faith SPREAD conjunct `faithSpreadEnabled` is a `RULE_COMPARISON_KEYS` member (`DEFAULT_SIMULATION_RULES:101`; `RULE_COMPARISON_KEYS = […, ...BOOLEAN_KEYS]` at `:1162-1166`) and is resolved FALSE in `realistic_regional` and `living_realm` (CONFIRMED by `node -e` over every preset: lit only in `dramatic_campaign :811` and `full_simulation :875`), so the D0-car-B move (light the conjunct in the two remaining presets) is BLOCKED for faith by the O-12 record at `simulationRules.js:797-809` (a comparison key lit on a legacy id re-labels every installed campaign to `custom` with no receipt). The three SPREAD-lane WF keys therefore light in EXACTLY TWO presets through a named conjunction cohort, with the reason recorded in each key's comment (ARCH §1.3's "not in a subset … without a recorded reason"), and the LOCAL-lane keys light in all four.

---

## 1. THE LANE TABLE

Boarding law for WF (ARCH §7.1 row "religiousContest.js · religionState.js · deityStance.js · faithNews.js — ONE WF lane at a time"): **one WF lane in flight at any moment**, in the order below. PRICING §C seats WF-6 at slot 5 "when TR-5's catalog car has landed"; this plan makes ONE forced adjustment, named: **lane 16a (the family's DOOR sitting: the faith-spread cohort, the deity-bearing fixture, WF-1's lighting) boards BEFORE lane 5**, because WF-6's LIT car needs the two-preset cohort and its anti-vacuity twin needs the deity-bearing fixture. WF-6 still waits for TR-5 car 1 on the consist. Splits are made HERE, once (a lane > 8 cars becomes sittings a/b), never again downstream.

| §C lane | Sitting | Waves | Cars (history-corrected) | Brief file (under `…/arch-932/briefs/`) | NEEDS landed/lit first | UNBLOCKS | LIT keys (this sitting) · preset roster · lighting-order conjuncts (R2) |
|---|---|---|---|---|---|---|---|
| **16** | **16a** | WF-0 + WF-1 (lighting) — THE DOOR SITTING | 4 (+1 repair) = 5 | `brief-16a-WF-WF-0-WF-1.md` | **D0 cars A+B on the consist** (the `SIM_LIT` cohort and the `:367` re-cut exist); nothing else | EVERY later WF lane (the cohort, the fixture, the first lit WF key); the two blocked certification rows (`subsystemRowsBaseline.js:60`, `subsystemRowsGrowth.js:91`) become observable; WF-8A (`faith_last_altar_dark`) becomes mintable by inheritance | `faithUnseatingEnabled` — LOCAL, single conjunct (the data gate `isSubsystemActive(snapshot,'religion')` at `religiousContest.js:547` does the second gate's work — the register comment `:286-291`) — **all four lit presets** via `SIM_LIT`. Car 0 mints the EMPTY `SIM_LIT_FAITH_SPREAD` cohort (two presets) and amends the D0 walker's arm (b) — no key in it yet |
| **5** | 5 | WF-6 faith terms | 6 (+1 repair) = 7, 7a/7b split if the LIT car > 12 files | `brief-5-WF-WF-6.md` | **lane 16a** (cohort + fixture); **TR-5 car 1** (the catalog car) on the consist — PRICING §A.4.3; T9: no second lane in flight on `peaceTerms*.js`; for the LIT car: **TR-5 car 7a** has lit `pactFormationEnabled` + `beliefAxesEnabled` (R2 — `faithTermsEnabled`'s door reads the mounted lifecycle; `believedDevotionEnabled` is a conjunction with `beliefAxesEnabled`, `beliefAxes.js:93`) | WF-5b (the `toleranceGuaranteeFor` surfacing arm), WF-2 (`pilgrimageRightFor`), GR-5 renewal (boards after WF-6 on `peaceTerms*.js`), POP-5b (`migrationRightFor` — same file order) | `faithTermsEnabled` (NEW mint) — SPREAD → **two presets** via `SIM_LIT_FAITH_SPREAD`; `believedDevotionEnabled` (registered `:251`, row at `subsystemRowsBelief.js:285`; lit nowhere) — **four presets** via `SIM_LIT`; conjuncts: `pactFormationEnabled`, `beliefAxesEnabled` (TR-5 7a), `isFaithSpreadEnabled(rules)` |
| **16** | **16b** | WF-3 stance consequences | 5 (+1 repair) = 6 | `brief-16b-WF-WF-3.md` | lane 16a (cohort); lane 5 landed (WF-3's durability seam touches the pact readers TR-5/WF-6 edit — `treatyEnforcement.js`/`pactAmendment.js`, never `peaceTerms.js`); GR-2 landed (mounted, CONFIRMED) | WF-5b (the foothold seed), WF-8 (voices) | `faithStanceConsequencesEnabled` (NEW) — SPREAD → **two presets**; conjunct `isFaithSpreadEnabled(rules)` |
| **17** | **17a** | WF-2a the season | 3 (+1) = 4 | `brief-17a-WF-WF-2a.md` | lane 16a (the observance-bearing fixture — WF-0's case carries a qualifying grand observance, `pilgrimage.js:31-38`'s bar); lane 5 (believed-devotion lit — else the `observanceLabel` dark arm is the live leg, declared) | 17b; WF-8 (Great Pilgrimage arc keyed to the season); WF-6's `pilgrimage_right` refusal arm flips to a live grant | `pilgrimageEnabled` (NEW) — SPREAD → **two presets**; the door splits `pilgrimSeasonActive(rules)` (spread ∧ key) from `pilgrimMoversActive(rules)` (∧ `errandSpineEnabled === true`) so 17a lights without SP-D |
| **17** | **17b** | WF-2b legates + pilgrims (the movers) | 4 (+1) = 5 | `brief-17b-WF-WF-2b.md` | 17a; **lane B's W-OPS wiring cars, then IN-4 (lane 6)** on `envoyErrandVocabulary.js` (ARCH §7.1: B → IN-4 → WF-2 → TR-8); WR-7b's `foreignGuestHold.js` (landed, 424 eff); for the movers' LIT arm: `errandSpineEnabled` lit by whichever errand consumer lands first (lane B car B / IN-4 / TR-8 / this lane) | TR-8 (boards after on the vocabulary file); ES-6b/ES-7 | no new key; the movers arm of `pilgrimageEnabled`'s door — conjunct `errandSpineEnabled` CHECK-GIT-FIRST: lit already, or lit HERE with `errandSpineDormancyFence.test.js` re-chartered (both polarities — the espionage gate's `!== true` precondition at `espionageGate.js:80`) |
| **18** | 18 | WF-4 omen reads | 5 (+1) = 6 | `brief-18-WF-WF-4.md` | lane 16a (fixture); D0; the `omenReadings` sub-ledger is a persisted-shape widening TAKEN under law 6 and recorded on #20's OSR list | WF-8 (resolution beats); IN (the false-omen lure consumes) | `omenReadsEnabled` (NEW) — LOCAL → **four presets**; single conjunct (data gate) |
| **19** | **19a** | WF-5a the split + `creedRef` | 4 (+1) = 5 | `brief-19a-WF-WF-5a.md` | lane 16a (WF-1 LIT: the landed prune fork `religionState.js:409-440`, `KEEP = 3 :434`); GR-3 landed | 19b; WF-8 (Schism/Persecution arcs if Q1 keeps them) | `faithSchismEnabled` (NEW) — LOCAL → **four presets** |
| **19** | **19b** | WF-5b the covert congregation | 4 (+1) = 5 | `brief-19b-WF-WF-5b.md` | 19a; lane 5 (the `toleranceGuaranteeFor` surfacing arm — `treatyEnforcement.js:293`; declared fallback if WF-6 is not landed); lane 16b (the foothold seed, degraded-dark otherwise) | WF-8; IN (informant exposure arm) | no new key; the covert seam arm of `faithSchismEnabled`'s door; the `deities[ref].covert` shape TAKEN and recorded for #20 |
| **20** | 20 | WF-7 the tithe | 6 (+1) = 7 | `brief-20-WF-WF-7.md` | lane 16a (WF-1 LIT — coffers-at-the-fall reads the ring); `warDeployment.js` 684/800 (STOP is green, 116 slack); NOT lane B (this plan corrects ARCH §7.1's "lane B → WF-7 → WC-1 on `treasury.js`": WF-7 reads and writes NO treasury field — `templeWealth` is a BAND, J-WF-3; the row is struck for WF-7, see §4) | WF-8 (tithe/remission voices); WF-9's sack lure | `titheEnabled` (NEW) — LOCAL → **four presets**; `templeWealth` TAKEN and recorded for #20; `ENDOWED` becomes drawable (prose cells move — `--record` with cause) |
| **21** | **21a** | WF-8 narration — the settlement voice | 4 (+1) = 5 | `brief-21a-WF-WF-8a.md` | **CHAIR ACT FIRST: Q1 RULED** (four or five realm arcs, WITH the overlap clause for `gods_abandonment` — a live faith arc, `realmEvents.js:8`, routed `heraldRouting.js:147` — and the UNGATED live `pantheon_ascendancy :338` / `pantheon_twilight :361`); every earlier WF lane landed (the fire sites) | 21b | none (the flag mints in 21b — "lights LAST in WF"); 21a pays the two unpaid obligations |
| **21** | **21b** | WF-8 — the realm arcs + the flag | 4 (+1) = 5 | `brief-21b-WF-WF-8b.md` | 21a | WF-9 | `faithNarrationEnabled` (NEW) — LOCAL → **four presets**; LAST WF key; ruling recorded: WF-8A's `faith_last_altar_dark` STAYS on `faithUnseatingEnabled` (re-gating a landed lit beat under a new key would be a second shift) |
| **22** | 22 | WF-9 convergence (no flag) | 5 (+1) = 6 | `brief-22-WF-WF-9.md` | every WF lane landed; the WF-0 case | the family's DONE-WHEN; CW-3's non-degenerate sky (PRICING lane 15) | none — the WF CONTRACT EVALUATOR (`faithConvergenceContract.js`) lands here; until then the family's lighting law is executed by the fence arm lane 16a car 0 writes (R2: "each family's own, where one exists; ARCH §3's fences otherwise") |

Totals: 12 sittings · **57 cars + 12 repair allowances = 69 max** (inside the fold's 51–79; the plan's centre ≈ 63 without repairs). Flag mints: 7 new register entries (`faithTerms`, `faithStanceConsequences`, `pilgrimage`, `omenReads`, `faithSchism`, `tithe`, `faithNarration`) + 2 already-registered keys lit (`faithUnseating`, `believedDevotion`) = 9 WF lighting acts. The register is 35 at the consist (`ENGINE_GATED_VIRTUAL_RULE_KEYS.length` = 35 = `VIRTUAL_SUBSYSTEM_ROWS.length`, `node -e` CONFIRMED; the two literals `contributionLedgerShape.test.js:118-119` read 35); every WF mint car counts from HEAD at its landing, never from this table (PRICING §A.4.4).

**The lighting order inside WF (R2, binding on the boarding order):** `faithUnseatingEnabled` (16a, no conjunct) → [the empty `SIM_LIT_FAITH_SPREAD` cohort, 16a car 0] → `faithTermsEnabled` + `believedDevotionEnabled` (5; AFTER TR-5 7a lights `pactFormationEnabled` + `beliefAxesEnabled`) → `faithStanceConsequencesEnabled` (16b) → `pilgrimageEnabled` (17a; 17b's movers arm after `errandSpineEnabled`) → `omenReadsEnabled` (18) → `faithSchismEnabled` (19a) → `titheEnabled` (20) → `faithNarrationEnabled` (21b, LAST). Every LIT car is built AT LANDING on the consist, rebased, where TR-5 7a's keys and the door are visible (R2 iii).

**What every WF LIT car declares (the family's DECLARED-SHIFT sentence, PLAUSIBLE as wording, CONFIRMED as mechanism):** *"<key> lit on <roster> at landing <sha>; `rulesSha256` and `litFlagCount` moved on those presets (and on `__birth_default__` where `realistic_regional` is in the roster); `worldStateSha256` / `wizardNewsSha256` / `settlementUpdatesSha256` UNMOVED on every preset because the witness's born worlds are DEITY-FREE and the outer gate `isSubsystemActive(snapshot,'religion')` returns before any fork — a moved pulse hash on ANY preset is a STOP (the key leaked past the data gate); the generation dump 525/525 unchanged; no installed campaign is re-labelled (the key is VIRTUAL); no tuning value moved."* The DATA ceiling is therefore stated once per car, not discovered: the lit layer is observable only in a deity-bearing world — WF-0's soak case (16a car 1) — and every WF fence's anti-vacuity / lit twin drives THAT fixture.

---

## 2. THE SHARED FILES (measured at the consist tip `315080928` with `arch-932/eff-any.mjs` — eslint's own `max-lines` counter, skipBlankLines + skipComments; ceiling = `scripts/.size-baseline.json` entry where present, else the 800 layer ceiling for `src/`/`scripts/`/`tests/` `.js`, 600 for components `.jsx`)

| File | eff / ceiling | Slack | WF lanes that touch it (and the foreign lanes) | Rule · extraction owner |
|---|---|---|---|---|
| `src/domain/worldPulse/simulationRules.js` | 352 / 800 (comments dominate a 1,444-line file) | 448 | EVERY WF mint/LIT car (register tail + `SIM_LIT` line); **16a car 0 mints `SIM_LIT_FAITH_SPREAD`**; every #32 lane; lane B | parallel-safe ONLY because the LIT car is last and rebased (§5.2); the chair resolves by measurement. Eager first-paint file — every car quotes the closure table from a real build. Each WF key ≈ 30–60 B eager (PLAUSIBLE); nine WF acts ≈ 0.3–0.5 KB against an UNMEASURED margin (795 B recorded vs 5,828 B lane A0 measured) — lane A0b (closure headroom) is owed before roughly the twentieth lit key estate-wide |
| `src/domain/certification/subsystemRowsVirtual.js` | 284 / 800 | 516 | the FIRST WF mint car to land spreads `...FAITH_SUBSYSTEM_ROWS` at the TAIL (expected lane 5; CHECK-GIT-FIRST — 16b mints it if it lands first); 16a car 3 amends WF-1's INLINE row `:460-…` (`other` text: "no preset declares this key" becomes false) | WF-1's row STAYS at `:460` (moving it shifts indices; `VIRTUAL_RULES` is ordered equality, `subsystemRowsVirtual.test.js:646`); new WF rows go in the NEW tail leaf |
| `src/domain/certification/subsystemRowsFaith.js` (NEW) | 0 → ≤ 250 | — | lanes 5, 16b, 17a, 18, 19a, 20, 21b append one 25–40-line row each (7 rows ≈ 200–280 eff — the LAST appender extracts a second leaf if the ceiling nears) | append-only; each row obeys THE EVIDENCE LAW (`eventType` ↔ a `candidateType:` literal; `stateKeys` a v5-census container the subsystem owns — `religionStates[cid].*` is NOT one, WF-1's row says so) |
| `tests/domain/subsystemRowsVirtual.test.js` 437 · `tests/domain/contributionLedgerShape.test.js` 152 · `tests/soak-harness/coveringArrayCoverage.test.js` 146 · the two edge bundles + metas | under ceiling | — | every WF mint car; every #32 mint car | counts from HEAD (§5.2); `LANE_LEAVES` scoped to WF's OWN leaves; the bundles rebuilt by the clone → build → no-path-leak → relink recipe |
| `src/domain/worldPulse/peaceTerms.js` | **797 / 800** (unbaselined; 3 lines from the layer ceiling) | 3 | TR-5 (lane 2) → **WF-6 (lane 5): net-zero seam lines ONLY** (executors in `faithTermExecutors.js`) → POP-5b → GR-5. **WF-3 (16b) touches it NOT AT ALL** — its durability read lands in `treatyEnforcement.js` (113) or `pactAmendment.js` (140), never the head; if the only lawful site is the head the car STOPs and proposes the extraction | SERIAL, T9 STOP; any WF growth here is an EXTRACTION CAR FIRST (a whole stage body to a leaf, byte-neutral, the baseline entry created LOWER), never a squeeze |
| `src/domain/worldPulse/peaceTermsCatalog.js` 115 · `treatyEnforcement.js` 113 · `pactFormation.js` 426 · `pactTriggers.js` 133 · `pactAmendment.js` 140 | under ceiling | ample | TR-5 car 1 (`pactFormation.js` 2 lines, catalog +14), car 3 (`pactFormation.js` +1, `pactTriggers.js` +3) → **WF-6 car 0 (the `:217` repair in `pactFormation.js`; the RECORD≠CODE sentence at `peaceTermsCatalog.js:238-239`; the attainable-rung walker)** → WF-3 (durability read, `treatyEnforcement.js`/`pactAmendment.js`) → POP-5b (`migrationRightFor` :300) → GR-4/GR-5 | SERIAL in that order (these are GRAMMAR's files — the `:217` repair is a VETOABLE seam: if the chair re-homes it to GR, WF-6 drops to 5–8 C and GR gains the car; the work exists either way) |
| `src/domain/worldPulse/religiousContest.js` 493 · `religionState.js` 373 · `deityStance.js` 60 · `deityStanceLane.js` 235 · `faithNews.js` 73 · `src/domain/realm/heraldRouting.js` 273 · `realmEvents.js` 309 · `src/domain/display/settlementRumors.js` 555 | all under 800 | ≥ 245 each | the WF chain: 16a (`deityBearers` export at `religiousContest.js:265`; the fence) → 5 (the `missionary_access` CHANNEL read in the foothold lane) → 16b (`deityStanceLane.js` calls the leaf) → 17 → 18 (`heraldRouting` rows) → 19 (`religionState.js` in-place: `covert` through `suppressDeity :274-280`; `religionLegitimacy.js`) → 20 (`religionState.js` in-place: `templeWealth` through the one writer) → 21 (`faithNews.js` registry; `settlementRumors.js` ~20 kinds — extraction fallback `faithPhrasePool.js` budgeted; `realmEvents.js` → `realmFaithArcs.js` leaf) → 22 | **ONE WF lane in flight at a time** (ARCH §7.1's row, amended in §4 to the sitting order above). `settlementRumors.js` at 555 + ~20 kinds stays under 800 (PLAUSIBLE) — 21a measures at its cut and extracts the faith pool to a leaf if slack < delta |
| `src/domain/worldPulse/pulseKernel.js` | **1581 / 1581** (R-BLD-10, banked) | 0 | **NO WF LANE ADDS A LINE.** Every WF wave mounts through an EXISTING kernel call: `advanceReligionStates` (`:1261`; WF-4's fold, WF-5, WF-7's quarterly fold ride the religion fold), `traditionsKernel` (WF-2a's season composes inside `traditionsKernel.js:724-727`, 484 eff), `settlementLifecycleKernel.js:564 advancePeacetimePacts` (WF-6), `deityStanceLane` (WF-3), `evaluateMoralInstitutionFounding` (`:114` import; WF-5a forks it). The existing WF-1 read at `:1637` stays | a new kernel call site is a STOP + an extraction recipe (a stage body out to its kernel), never a raise; the volume's V39/R4 "no edits to pulseKernel/applyWorldPulse/momentum" binds |
| `src/domain/worldPulse/warTermination.js` | **818 / 818** (baselined) | 0 | WF-1 reads it (landed, `:538`); no WF lane adds a line; 16a verifies the two-flag fixture still holds | extraction first if any WF car ever needs a line (none planned) |
| `src/domain/worldPulse/applyWorldPulse.js` | **907 / 907** (baselined) | 0 | TR-4 car 0 LOWERS it; **no WF lane edits it** — WF-0's fixture READS the `primaryDeitySnapshot` writer's path (`reEmbedPrimaryDeity`, `:242`), never edits it | any WF need here waits for TR-4 car 0 and inherits the lower number |
| `src/domain/worldPulse/warDeployment.js` | 684 / 800 | 116 | **WF-7 (lane 20)**: the temple-loot weight as a READ the sack arithmetic consumes (`:803-812`; the conserved arithmetic lives in `warArmyRecord.js` per `:209`); GR-6; WC lanes | SERIAL WF-7 → GR-6/WC (whichever boards later CHECK-GIT-FIRST); PACKET_STANDARD ≤ 15 delta; STOP if slack < delta at the cut |
| `src/domain/worldPulse/envoyErrandVocabulary.js` 367 · `errandMint.js` 87 · `foreignGuestHold.js` 424 | under ceiling | ample | lane B (W-OPS wiring A/B) → IN-4 → **WF-2b (17b: the two `built: true` flips at `:322-335`, `tests/lint/errandConsumerRegistry.walker.test.js` 270 eff reds both directions)** → TR-8 (`factorErrand.js` row `:315-321`) → ES; `foreignGuestHold.js` read-only + close-through-the-writer by BOTH WF-2b and TR-8 | SERIAL by that order; WF-2b ↔ TR-8 on `foreignGuestHold.js` CHECK-GIT-FIRST (the one that boards second re-reads) |
| `src/domain/traditions/pilgrimage.js` 41 · `src/domain/worldPulse/traditionsKernel.js` 484 | under ceiling | ample | WF-2a only (the ~5-line subsumption fork at the scalar source; the kernel call at `:726` unchanged) | the `traditions` dormancy golden and the traditions layer's LIT behaviour (`traditionsEnabled: true` in all four lit presets, `:674`) — the lit-vs-dark observance delta is the DISCLOSURE pin, declared as a shift only when `pilgrimageEnabled` lights |
| `src/domain/worldPulse/moralInstitutionPressure.js` 321 · `foundingCatalog.js` 57 · `src/domain/institutions/institutionRoster.js` 10 · `institutionTolerance.js` 123 · `religionLegitimacy.js` 227 | under ceiling | ample | WF-5a (19a: the founding-lane FORK, `FOUNDING_STEP 0.015 / FOUNDING_FLOOR 0.6 / FOUNDING_EMIT_P 0.2` at `:51-63`; the creed-aware backing fork at `religionLegitimacy.js:181-184/:199/:495`) → WF-7 (20: splendor INSIDE `W_INSTITUTION 0.12 :92`) | SERIAL 19a → 20 on `religionLegitimacy.js` (the chain order already) |
| `src/domain/spatial/generosityEV.js` 403 | under ceiling | ample | WF-7 (`templeMediated` at `:243` — the wealth-band scaling); TR-4 touches `generosityKernel.js` (800/800) and `generosityUpdates`, NOT this file | CHECK-GIT-FIRST against TR-4/TR-6 at the cut |
| `src/domain/worldPulse/beliefAxisSubjects.js` 179 · `beliefAxes.js` · `tests/property/believedWorldAxesDormancyFence.test.js` (×3 keys) | under ceiling | — | TR-5 7a (lights `beliefAxesEnabled` + `believedScarcityEnabled`; re-charters the axes + scarcity arms) → **WF-6 LIT (lights `believedDevotionEnabled`; re-charters the devotion arm)** → POP-1 (conditions) | SERIAL by landing order on the fence file; each re-reads the roster off `SIMULATION_RULE_PRESETS` (§3.2 arm 1), never a literal |
| `scripts/audit/whole-world-soak.mjs` | **780 / 800** | 20 | **WF-0 (16a car 1): the deity-bearing case lands as a SIDECAR module (`scripts/audit/soakCases/faithBearing.mjs`, PLAUSIBLE path) with ≤ 6 lines in the grid**; TR-9 car 3 (the soak joins, brief-56c) also edits the grid; WF-9 grows the grid in the sidecar | SERIAL WF-0 ↔ TR-9 ↔ WF-9, one at a time; the sidecar is the extraction — whichever boards first creates `soakCases/`; if the file is at slack < delta at a cut, that car extracts a case-table body first |
| `tests/lint/tradeConvergenceContract.walker.test.js` 636 | under 800 | 164 | D0 car A re-cuts `:367` → **16a car 0 amends arm (b)** (a key may sit in a NAMED conjunction cohort whose roster equals the presets where its conjunct resolves true) → nothing else | SERIAL after D0; one amendment, once |
| `tests/property/faithUnseatingDormancyFence.test.js` 121 | under 800 | — | 16a car 3 (the lit-roster arm + the lit twin over the deity-bearing fixture); every later WF lane only VERIFIES it green | the LGT-P13 lit-shape precedent (site census `DECLARED_GATE_SITES :58-63` of FOUR modules — re-measured: the key is read by name in `pulseKernel.js`, `religionState.js`, `religiousContest.js`, `warTermination.js`, and ALSO `faithPanelModel.js` (display) — the census counts `src/domain/worldPulse` sites; 16a asserts the roster it finds) |
| `docs/content/RECEIPT_POOLS_FAITH.md` · `RECEIPT_POOLS_DOSSIER_STATE.md` (DS-FTH) · `tests/fixtures/dossier-prose-manifest-golden.json` · `docs/content/wiring-census.json` | prose corpus | — | 16a (the DS-FTH doc pin — WF-1's, never landed with WF-1a; VERIFY-AT-BUILD whether `3ac279db4`/`5d18b4a0c` carried it) → 20 (`ENDOWED` drawable: prose cells MOVE; `--record` with the cause; wiring census stamp) → 21a (the annex `faith.fall.abandoned` pool at `:174` removed; `faithKindPools.walker.test.js:80` reads the annex) | never beside lane E (the Scribe) or lane A (the declared window) — both touch the prose desks (ARCH §7.1's last row) |
| `docs/GOLDEN_SHIFT_LEDGER.md` · `docs/A_PLUS_ROADMAP.md` · `docs/implementation/INDEX.md` · `docs/implementation/packets/fp/WF-*.md` (seven exist: WF-1A…1F, WF-8A) | docs | — | every WF lane (the dock copies; a packet per wave where the family runs packets — WF DOES: seven packets exist, so every WF lane writes one) | append-only; the chair merges by hand |

Files WF touches that NO other lane does (measured, for the briefs' headers): `patronFall.js` 28 · `pantheon.js` 179 · `sacredClaim.js` 85 · `faithChannelBindings.js` 18 · `faithField.js` 109 · `deityAxes.js` 28 · `disposition.js` 237 · `dispositionProfile.js` 122 · `piety.js` 216 · `latentPantheon.js` 20 · `faithTuningSurface.js` 56 · `deityFlaws.js` 91 · `faithWitnessSource.js` 75 (zero importers — INERT) · `src/components/settlement/faithPanelModel.js` 140 · `src/domain/display/faithDeepening.js` 101 · `deityNames.js` 27 · `auspice.js` 32 (V-16, the DM's forecast omen — READ for WF-4's naming reconciliation, never edited) · `subsystemActivation.js` 17 · `subsystemRowsBaseline.js` 252 · `subsystemRowsGrowth.js` 241 · `institutionStatusLifecycle.js` 265 (W-K; `magicEconomyEnabled` lit nowhere — HISTORY says "zero importers"; measured: TWO in-family src importers, `magicSubstitutionReagents.js` and `institutionStatusModel.js`, plus the `subsystemRowsWaves.js` module list — still flag-dark, still UNAVAILABLE to WF-5a/WF-6, the correction recorded here). Worker bundle: **0 B** — no WF file is on the generation worker graph (PRICING §A.3's nine BFS runs: zero `worldPulse/**`, `spatial/**`, realm, display, certification); every WF car still runs `npm run build && npm run verify:dist` and quotes the worker `stat`, the engine line and the closure table (the dist-gated skip hazard, ARCH §4.1).

---

## 3. THE FAMILY-WIDE CAR 0s (from the history fold, plus this plan's one finding) — and who owns each

| # | Car 0 | Why it is mandatory (CONFIRMED receipt) | Owner (lane · car) |
|---|---|---|---|
| C0-A | **THE DATA CAR** — one authored deity-bearing soak case (spatial canon + two opposed-quadrant deities + a qualifying grand observance `scaleBand ≥ 3`, act ∈ {procession, offering, fair}, at the patron host + ≥ 1 mapped reachable neighbour — `pilgrimage.js:31-38`'s own bar) + the v5 `deityBearers` receipt field | deity presence is the OUTER gate of every faith mechanism (`religiousContest.js:544-547`); the release soak generates with `customContent: {}` (`whole-world-soak.mjs:248/:713`, then `delete config.primaryDeitySnapshot :276-277`); the premade pool is DELETED (`5f8dc7830`, walker-guarded `noPremadeDeityPool.walker.test.js`); two certification families wait on this one case (`subsystemRowsBaseline.js:60`, `subsystemRowsGrowth.js:91` — both texts read by me). Until it lands every WF "lit" acceptance is vacuous in a shipped world | **16a · car 1** (sidecar module; `whole-world-soak.mjs` at 780/800) |
| C0-B | **WF-1's LIGHTING** (the unpriced row) | six `feat(WF-1x)` cars are ancestors and `faithUnseatingEnabled: true` is in no preset; the row exists (`subsystemRowsVirtual.js:460`), the fence exists in the lit shape (LGT-P13), the register entry exists (`:295`) — the lighting is the `SIM_LIT` line + the fence's roster arm + the witness + the ledger entry + the row text | **16a · car 3** |
| C0-C | **WF-3's RECONCILIATION over FIVE live deity→world producers** (own / gate / declare each): (1) `disposition.js:195 W_DEITY = 0.35` → `:208 deityTemperDrive` × `:281`, `:469 W_LAW_DEITY 0.3`; (2) `dispositionProfile.js:135 deityPressureOf` under §851's one-arm-per-deity (`authoredAspectsOf :124`); (3) W-FAITH's channel lift `faithChannelBindings.js:81-88` → `causalState.js` UNFLAGGED (six call sites, `CAUSAL_SWING 20`); (4) `moralInstitutionPressure.js` mounted `pulseKernel.js:114` (abolish/found under patron conviction); (5) `applyDivineMandate` (`religionState.js:755-762` ← `pulseKernel.js:1807`) → `publicLegitimacy` — inheriting `faithField.js:30-38`'s no-double-count law (the temper is NOT fed into `war_readiness`) | without it WF-3 prices the same quantity at a second grain — the TRADE famine-block shape five times over (HISTORY-TRADE (c)) | **16b · car 0** (docs + a K3-inward walker: `stanceConsequences.js` imports none of the five modules and writes none of their variables; + the two stale in-code headers cured: `faithField.js:12-22` "THE FIELD IS DARK" and `deityAxes.js:35-42` "the engine still derives" — RECORD≠CODE R3/R4) |
| C0-D | **WF-6's CROSSING REPAIR + RUNG RULING** — repair `pactFormation.js:217` (`row.religionState` → the live spelling: the self side reads `worldState.religionStates[fromId]`, the belief side's own pattern at `beliefMap.js:503`; VERIFY the `crossingsFor`/`selfBandsOf` call shape carries the world state — it does: `crossingsFor({ worldState, … })` `:245`); THEN rule the two unproducible rows (`shared_rite` min 0 and `missionary_access` min 0.7 vs the attainable set {1/3, 2/3, 1} under `CROSSING_FLOOR01 = 0.5` and the single-rung selector `pactFormation.test.js:435`) and the missing war door (zero faith rows in `CLASS_TERM :385-394`); THEN rule `temple_restitution`'s semantics (a term that has NEVER fired) | a chair-signed GRAMMAR crossing is inert on a key-spelling defect no suite can see (every ladder test injects `score01`; `grep -c religionState tests/domain/pactFormation.test.js` = 0 — re-run by me). The executor car does NOT narrow this | **5 · car 0** — VETOABLE SEAM: `pactFormation.js` is GRAMMAR's; if the chair re-homes the repair to GR, WF-6 returns to 5–8 C. The rung re-band is a TUNING act (`PACT_DRAFT_LENS` bounds are "AUTHORED LITERALS … UNSOAKED … the owner signs them at the soak redo", `pactFormation.js:165-171`) — so car 0 lands the ATTAINABLE-RUNG WALKER in its FIRED state (asserts by name which rungs are unattainable at the current bounds) and the catalog sentence cure; the re-band waits for the owner's tuning signature; the war door (a faith `CLASS_TERM` row) is a design row for the brief |
| C0-E | **WF-8's Q1 RULING** (four arcs or five) WITH the overlap clause: `gods_abandonment` IS a faith arc (`realmEvents.js:8-10`, types incl. `religious_conversion_fracture`; `heraldRouting.js:147 compound_gods_abandonment: 'faith'`) whose prose covers Persecution and a false-prophet Awakening; `pantheon_ascendancy :338` / `pantheon_twilight :361` are LIT and UNGATED (mint on ordinary tier changes); only `pantheon_extinction :315` is WF-1C's; a realm Schism arc does not exist | left open at ODQ §78.3 ("Q1 … stay open for the WF family sitting", L3014) and never ruled; WF-8 is unpriceable without it | **the CHAIR, before lane 21 boards** (a docs act; the two unpaid obligations — the registry shrink-back review `faithNews.js:99-107` and the annex `faith.fall.abandoned` `RECEIPT_POOLS_FAITH.md:174` — are 21a car 1's) |
| **C0-F** | **THE FAITH DOOR (this plan's finding, not in the fold)** — `faithSpreadEnabled` is a `DEFAULT_SIMULATION_RULES` boolean (`:101`) and therefore a `RULE_COMPARISON_KEYS` member (`:1162-1166` spreads `...BOOLEAN_KEYS`); it resolves FALSE in `realistic_regional` and `living_realm` (CONFIRMED by `normalizeSimulationRules` over every preset at the consist) and TRUE only in `dramatic_campaign :811` and `full_simulation :875` (lockstep with `religionDynamicsEnabled :817/:876`). The three SPREAD-lane WF keys AND with `isFaithSpreadEnabled(rules)` (`religiousContest.js:536-542`'s idiom; the volume §2 "a SPREAD WF flag lit while spread is dark is an invalid config WF-9's certification walker reds"). The D0-car-B move (light the conjunct in the two remaining presets) is BLOCKED for faith: the O-12 record at `simulationRules.js:797-809` says in terms that lighting a comparison key on a LEGACY id makes `rulesMatchPreset` miss, `presetIdForRules` fall to `custom`, and "every installed Dramatic Campaign silently re-label itself at its next ensureWorldState with no receipt" — THE PROMISE by construction; the lawful home is "a LIT SUCCESSOR id (row O-1's birth form)", an owner-gated public-surface act | ADDENDUM R1(b) as written admits a lit key "ONLY through `SIM_LIT`" (spread in exactly four presets), so a two-preset roster has NO lawful home today; ARCH §1.1's `neutralNeighborsEnabled` template says "do not light half the presets", §1.3 says a subset is lawful WITH a recorded reason. RULING (vetoable, under the 14:4x grant): (i) a second frozen cohort **`SIM_LIT_FAITH_SPREAD`**, spread after `...SIM_LIT` in `dramatic_campaign` and `full_simulation` ONLY, with the O-12 reason in its header and in each key's register comment; (ii) the D0 walker's arm (b) amended once to admit "through `SIM_LIT`, or through a NAMED conjunction cohort whose preset roster EQUALS the presets where its conjunct resolves `true`" — the roster computed from `SIMULATION_RULE_PRESETS` in-test, never a literal; (iii) a family fence arm (`faithSpreadCohortFence.test.js`, NEW): every key in the cohort is a SPREAD-lane key by a frozen in-file list, and the cohort's presets == `{p : isFaithSpreadEnabled(normalize(p.rules))}`; (iv) the alternative — light the SPREAD keys NOWHERE (built-dark for three of nine WF acts) — is recorded as the fallback if the chair reads §1.1 strictly; (v) the "lit successor id" act is named to the chair as the only way to four presets, owner-gated, NOT priced here | **16a · car 0** — EMPTY at this car (deep-equal every preset's resolved rules base-vs-tip — the arm that proves the car moves nothing); the chair may fold it into D0 as car C if D0 has not composed; either way it is priced ONCE, here |
| C0-G | The ~0-cost DECLARATION lines the fold owes each wave, folded into that wave's car 1: WF-4 — the V-16 AUSPICE (`auspice.js`, `AuspicePanel.jsx` ← `DmScreen.jsx:107`, DM-only, "the omen, not the promise") is the product's live "omen"; WF-4's reading is the PULPIT's speech, the `divination` desk (`heraldRouting.js:64`) is the FORECAST desk — the "open expectation → divination" routing (volume V20) is VERIFIED-AT-BUILD against that desk's live semantics; WF-5 — fork `moralInstitutionPressure`'s founding lane (never a second path); declare W-K's status lifecycle UNAVAILABLE (flag-dark); re-derive the never-prune-the-cellar pin against the LANDED prune (`pruneSuppressed`, `KEEP = 3`, `narrativePrune` fork `:428-440`) not the volume's V4; the settlement schism ALREADY SHIPS (`resolvePatronContest :532` under `faithSpreadEnabled`) and WF-1 classifies its outcome `discredited` (`patronFall.js:113`) — WF-5a mints NO new fall cause (§215.2(1)'s FOUR-CAUSE STOP; `PATRON_FALL_CAUSES :76` is four tokens); WF-7 — `W_INSTITUTION 0.12` already prices temple richness as a saturated count × scale (`religionLegitimacy.js:181-184/:199`); the two self-declaring comments (`warFaithStateProse.js:96` "HAS NO WRITER ANYWHERE IN THE ENGINE", `dossierMounts.js:504-505`) become RECORD≠CODE the day the writer lands and are cured IN the writer car; the W-COIN treasury is STATE money — `templeWealth` stays a BAND (J-WF-3) and the tithe draws no `treasury` field, so no conservation crossing and no double-count (declared, pinned by an import-source scan) | each is a line in the brief and a sentence in the car body | lanes 18, 19a, 20 — car 1 of each |

RECORD≠CODE rows this plan carries forward for the chair's ledger act (not lane work): the recon's own WF-1 "BUILT — code wins" (PROGRAMME §B) → LANDED-DARK-WHOLE; §925.5(1) / `DEFERRAL_SWEEP_LAUNCH_PATH.md` row 1 / the atlas row still calling the deity pool an open constitutional violation — the pool is GONE since `5f8dc7830` (07-28); the FVQ J-GR-2-3 tombstone and L4096 "blocked on GR-3" — both dead, but the producer that replaced them cannot fire (C0-D); `DESIGN_FP_ARCH_WF.md:406-412` still says "NEW leaf `patronFall.js` … budget ≤ 200 eff" (landed, 28 eff), and the volume's §2 "nothing in this program lights a flag" is superseded by the owner's 16:5x order; the volume's WF-0 sentence "SET_PRIMARY_DEITY/IMPOSE_CULT (mutateEntities.js:820/:864)" — at the consist the verbs are composed in `src/components/settlement/eventComposer/buildEvent.js:178/:187` and the production writer of `config.primaryDeitySnapshot` is `applyWorldPulse.js:242` (VERIFY-AT-BUILD which path a test fixture uses; the doctrine pin says deities round-trip the custom-content path).

**Grep traps** every WF brief adopts VERBATIM by reference: the HISTORY-WF paragraph after its §B (twenty-odd traps — `religionState` singular is a DEAD POINTER; a populated `PACT_DRAFT_LENS` rung is not a reachable rung; `feat(WF` is the only clean landing door, `docs/implementation/packets/fp/` the second; `W-F<n>` ≠ `W-FAITH F<n>c` ≠ `WF-<n>`; `schism` has three non-faith senses; `omen` matches `moment`; `tithe` is the granary reserve + `tithe_rights`; `pilgrim` is TRADITIONS + the term + the reserved row; `faithNarrationEnabled` = 0 hits ⇒ WF-8 scores UNBUILT and is wrong; `courtRiskAppetiteOf` not `riskToleranceOf`; `divination` is the forecast desk; `covert` is espionage vocabulary; `restitution` mostly matches WR's band; `faithProfile` is the pulse-minted projection; line numbers rot inside a day — navigate by symbol), RECHECK §5 (`omen` in the UI = THE AUSPICE; `Herald*` is the news desk), and HISTORY-TRADE's twelve (`INSTR-1 ≠ TR-1`; W-COIN is state money; `toll_exemption`'s one consumer reads treaty terms). One more, banked by this plan: **`INTERVAL_WEEKS` has THREE homes** — the canonical `src/domain/worldPulse/intervalWeeks.js:7` and two LOCAL copies (`foodStockpile.js:66`, `populationDynamics.js:19`); WF-5's clocks and WF-7's 13-week quarter import the canonical leaf, never a fourth copy (the volume's V34 — CONFIRMED that the copies exist).

---

## 4. THE SERIALIZATION ROWS WF ADDS TO ARCH-PROTOCOLS §7.1 (and the two it amends)

| Shared surface | Lanes that touch it | Rule |
|---|---|---|
| `simulationRules.js` — the **`SIM_LIT_FAITH_SPREAD`** cohort (two presets) + `tests/lint/tradeConvergenceContract.walker.test.js:367` arm (b) | D0 car A → **16a car 0** (mints the empty cohort; amends arm (b) once) → lanes 5, 16b, 17a append one line each | SERIAL after D0; the amendment lands once; every appender re-reads the cohort's preset roster off `SIMULATION_RULE_PRESETS` |
| `src/domain/certification/subsystemRowsFaith.js` (NEW tail leaf) + the tail spread in `subsystemRowsVirtual.js` | the FIRST WF mint car to land mints the leaf (expected lane 5; CHECK-GIT-FIRST — 16b if it lands first); lanes 16b, 17a, 18, 19a, 20, 21b append | append-only at the tail; WF-1's inline row at `subsystemRowsVirtual.js:460` NEVER moves (16a car 3 amends its text only) |
| `pactFormation.js` · `pactTriggers.js` · `peaceTermsCatalog.js` · `treatyEnforcement.js` · `pactAmendment.js` (GRAMMAR's) | TR-5 cars 1, 3 → **WF-6 car 0** (the `:217` repair; the `:238-239` sentence; the attainable-rung walker) → WF-6 cars 1–3 → **WF-3** (16b; the durability read in `treatyEnforcement.js` or `pactAmendment.js`, NEVER `peaceTerms.js`) → POP-5b → GR-4 / GR-5 | SERIAL in that order; the `peaceTerms.js` 797/800 row (ARCH §7.1) is AMENDED to read "TR-5 → WF-6 (net-zero seam only) → POP-5b → GR-5; WF-3 does not touch it" |
| `tests/property/believedWorldAxesDormancyFence.test.js` (×3 keys) · `beliefAxes.js:93` | TR-5 7a (`beliefAxesEnabled` + `believedScarcityEnabled`) → **WF-6 LIT (`believedDevotionEnabled`)** → POP-1 (`believedConditionsEnabled`) | SERIAL by landing order; each re-charters ITS arm and re-reads the roster |
| `envoyErrandVocabulary.js` `:322-335` (the two WF-2b rows) · `foreignGuestHold.js` | lane B → IN-4 → **WF-2b (17b)** → TR-8 → ES-6b/ES-7 (the ARCH row, unchanged); `foreignGuestHold.js`: WF-2b ↔ TR-8 | SERIAL; `errandConsumerRegistry.walker.test.js` reds a module without its `built: true` flip and vice versa — each row flips in the SAME commit as its module |
| `scripts/audit/whole-world-soak.mjs` **780/800** + `scripts/audit/soakCases/` (NEW sidecar) | **WF-0 (16a car 1)** ↔ TR-9 car 3 ↔ WF-9 (22) | SERIAL, one at a time; the first to board creates the sidecar; ≤ 6 grid lines per car; slack < delta ⇒ extract a case-table body first |
| the WF chain (`religiousContest.js` · `religionState.js` · `deityStance.js`/`deityStanceLane.js` · `faithNews.js` · `heraldRouting.js` · `realmEvents.js` · `settlementRumors.js` · `religionLegitimacy.js`) | **16a → 5 → 16b → 17a → 17b → 18 → 19a → 19b → 20 → 21a → 21b → 22** | ONE WF lane in flight (the ARCH §7.1 row "WF-0 → WF-3 → WF-2 → …" is AMENDED to this sitting order; WF-6 boards after 16a — forced by the door and the fixture) |
| `warDeployment.js` 684/800 | **WF-7 (20)** → GR-6 / WC lanes | SERIAL; ≤ 15 delta; CHECK-GIT-FIRST at the cut |
| `treasury.js` · `treasuryConservation.walker.test.js` · `warCosts.js` | lane B → WC-1 | **WF-7 REMOVED from this row** (ARCH §7.1 "lane B → WF-7 → WC-1" is corrected): WF-7 reads and writes no treasury field — `templeWealth` is a band; an import-source pin in `tithe.js` asserts it |
| `heraldRouting.js` (realm) | lane B → IN-5 (the section list) · **WF-4 rows (18), WF-8 rows (21a/21b)** kinds on the existing `faith` / `divination` desks, each kind family with its OWN walker file (L6 five joins) | the section list is SERIAL (B → IN-5); WF kinds may land beside other families' kinds; the `faith_` PREFIX is the trap — a desk-bearing registered kind takes an EXACT row (`heraldRouting.js:154-158`'s refusal) |
| the prose corpus (`RECEIPT_POOLS_FAITH.md` · DS-FTH in `RECEIPT_POOLS_DOSSIER_STATE.md` · the prose-manifest golden · `wiring-census.json`) | **16a (the DS-FTH doc pin) → 20 (`ENDOWED` drawable) → 21a (the annex pool removed)** | never beside lane E or lane A; a moved cell is re-recorded by `prose-manifest-cells.mjs --record` with the cause in the shift ledger, the moved rows listed BY POOL KEY |
| `intervalWeeks.js:7` (canonical) | WF-5b (19b: 520w/156w), WF-7 (20: 13 weeks) — imports only | never a fourth local copy (the two locals at `foodStockpile.js:66`, `populationDynamics.js:19` are the counter-examples) |

Rows this plan CONFIRMS unchanged for WF: `pulseKernel.js` 1581/1581 (no WF line — §2); `applyWorldPulse.js` (TR-4 car 0 first; WF none); `warTermination.js` 818/818 (WF none); `WorldMap.jsx` / `mapSlice.js` (WF none); the generator graph (WF 0 B); `couplingRegistry*.js` (WF appends rows in its own family leaf `couplingRegistryGrammar.js:69/:148-172` forward references exist — VERIFY the FAITH leaf's name at the cut; append-only).

---

## 5. THE BRIEF SEEDS — ten lines an architect starts from, per lane (the cars in order · the leaves · the pins' shape · the census bills · the byte rule · the LIT act)

Common to every seed (stated once): the car shape is ARCH §4.5 + PACKET_STANDARD (≤ 1 behaviour family · ≤ 1 new persisted record family · ≤ 1 user-facing surface · ≤ 2 leaves ≤ 250 eff · ≤ 12 handwritten files); the gate per car is focused · `tests/lint` · `tests/copy` · `tests/property` · `tests/edgeFunctions` · `tests/security` · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte · `check:quick`; every WF fixture is DEITY-BEARING (WF-0's case or a `config.primaryDeitySnapshot` authored through the embed path — VERIFY-AT-BUILD); every new negative assertion `// anchored:`; every dial lands INSIDE a frozen `<NAME>_TUNING` table with an executed derivation or the compile STOPs at promotion (THE WF TUNING-HOME LAW, volume §2); every LIT car cites `ARCH-PROTOCOLS-ADDENDUM-1.md` by path in its OWNER-GATED/RISKS row and is built AT LANDING, rebased; the lighting census refreeze is its OWN commit on a clean tree; a new invariant test file gets a `kind:'mutation'` manifest entry with a planted `check_caught*`; the Seat/Lane trailer `Seat: Opus 5 — Fable-unvalidated` · `Lane: <n>`; porcelain 0.

**16a — WF-0 + WF-1 lighting (THE DOOR SITTING; 4 cars + repair)**
1. Car 0 THE FAITH DOOR: `SIM_LIT_FAITH_SPREAD = Object.freeze({})` spread after `...SIM_LIT` in `dramatic_campaign` and `full_simulation` only, header quoting O-12 (`:797-809`) and the resolved roster; `tradeConvergenceContract.walker.test.js:367` arm (b) amended to admit a named conjunction cohort whose roster == `{p : isFaithSpreadEnabled(normalize(p.rules))}`; NEW `tests/property/faithSpreadCohortFence.test.js` (the roster arm; the SPREAD-key allowlist frozen in-file: `faithTermsEnabled`, `faithStanceConsequencesEnabled`, `pilgrimageEnabled`).
2. Car 0 pins: deep-equal every preset's resolved rules base-vs-tip (the empty cohort moves nothing); negative controls: a SPREAD key planted in `SIM_LIT` reds the cohort fence; a key planted in the faith cohort without a register entry reds arm (c); `faithSpreadEnabled: true` planted in `living_realm` reds the roster arm (the O-12 tripwire). Byte rule: `simulationRules.js` is eager — quote the closure table; bundles rebuilt (four files).
3. Car 1 THE DATA CAR: `scripts/audit/soakCases/faithBearing.mjs` (NEW sidecar; ≤ 6 lines in the 780/800 grid) — spatial canon + two opposed-quadrant deities + a `scaleBand ≥ 3` procession/offering/fair at the patron host + a mapped reachable neighbour; `deityBearers` (private at `religiousContest.js:265`, used `:591`) exported or re-derived into the v5 receipt as `subsystems.deityBearers`; pins: bearer count correct on the bearing case AND zero on the deity-free corpus (both arms); doctrine pin (the deity round-trips the custom-content/embed path); the WF-2 precondition pin (the observance qualifies by `drawsPilgrims`); mutant: bearer predicate counts cult embeds only → the pin reds.
4. Car 2 the two certification rows: `subsystemRowsBaseline.js:60` `FAITH_SPREAD_OTHER` and `subsystemRowsGrowth.js:91` gain the bearer-count invariant sentence and name the case id; the certification-surface render pin from a fixture (the dossier landing); Req 13/14 recorded ("engine-only, instrument").
5. Car 3 WF-1 LIT: `faithUnseatingEnabled: true, // WF-1 · lane 16a car 3` in `SIM_LIT` (four presets); the register comment `:283-295` amended to say LIT (four presets, the car); `faithUnseatingDormancyFence.test.js` gains the lit-roster arm (§3.2 arm 1, roster off `SIMULATION_RULE_PRESETS`) and the LIT TWIN over the deity-bearing fixture (fence 3's count MOVES under `full_simulation`; the projection carries `patronFall` on a real fall); WF-1's row text at `subsystemRowsVirtual.js:460` amended ("no preset declares this key" → the four presets; UNOBSERVED → observable via the WF-0 case); WF-8A rides by inheritance (`religiousContest.js:819 if (unseating)`) — `faith_last_altar_dark` is now mintable, `faithKindPools.walker` unchanged.
6. Car 3 receipts: the witness re-recorded by hand (`measureWitness()`; expected: `rulesSha256` + `litFlagCount` move on the four lit rows and `__birth_default__`; every pulse hash UNMOVED — a moved `worldStateSha256` is a STOP); the generation dump 525/525 unchanged (the zero-movement receipt); `espionageDormancyFence` 21/21 (engine-side key; no header block); the GOLDEN_SHIFT_LEDGER draft with the DECLARED-SHIFT sentence of §1; the two literals UNCHANGED (35 — the key is already registered) and stated as such.
7. Car 3 also: the DS-FTH doc pin (volume §5-7; exactly-once; `patronFalls[]` spelling) if no WF-1 car carried it — VERIFY-AT-BUILD by grep of `tests/` for `DS-FTH-3`.
8. Census bills: refreeze own commit (new files: the cohort fence, the sidecar's test); mutation manifest for the cohort fence; no prose byte unless the doc pin lands (then wiring census `--check` only).
9. Byte rule: closure ±(30–60 B × 1 key + the empty cohort's string) — quote the real table; worker unchanged (`stat` quoted); engine line quoted.
10. Exit: D0 walker green with the amended arm; every preset's resolution byte-identical at car 0; the bearing case grades the two rows observable; `faithUnseatingEnabled` in `SIM_LIT`; the fence 100 % green with the lit twin moving; zero UNDECLARED lines. Handoff to lane 5: the cohort exists (empty), the fixture exists, WF-1 is lit.

**5 — WF-6 faith terms (`faithTermsEnabled` + `believedDevotionEnabled`; 6 cars + repair; 7a/7b if the LIT car > 12 files)**
1. Car 0 THE CROSSING REPAIR + RULING: `pactFormation.js:217` reads `worldState.religionStates[fromId]` (the belief side's spelling at `beliefMap.js:503`) — VETOABLE SEAM (GRAMMAR's file; chair may re-home); NEW `tests/lint/faithCommunionAttainability.walker.test.js` computing the attainable score set from `DEVOTION_BANDS` × `COMMUNION_RUNG_FLOOR` × `CROSSING_FLOOR01` against `PACT_DRAFT_LENS.faith_communion` and asserting BY NAME the unattainable rungs (`shared_rite`, `missionary_access`) in its FIRED state — the re-band is the owner's tuning signature; `peaceTermsCatalog.js:238-239`'s "a war's end can EXTRACT" cured to the truth (no faith `CLASS_TERM` row); the ruling on the war door (a faith asset class or none) and on `temple_restitution`'s semantics (the `restitution` stream precedent verbatim, `peaceTerms.js:710-735`) written as design rows. Pins: a fixture with a real `religionStates` entry crosses (positive control FIRST); negative control: the repair reverted → `NO_CROSSING` every call.
2. Car 1 `faithTermExecutors.js` (NEW ≤ 300): consumers for the four idle grant readers (`treatyEnforcement.js:270-293`, zero callers — CONFIRMED): `toleranceGuaranteeFor` → a suppression of the guaranteed creed becomes a BREACH (leaf-first; the compliance seam reached through `treatyEnforcement`/the breach family, NEVER a `peaceTerms.js` head line); `missionaryAccessFor` → moves the CHANNEL (admits the grantee's creed to the grantor's targeted-foothold candidates in `religiousContest`'s spread lane) never share; `sharedRiteFor` → `peaceReasons`' `common_rite` scorer; `pilgrimageRightFor` → the WF-2 arm, declared dark until 17a (refusal receipted).
3. Car 2 the two arms: negotiated (the repaired crossing → `draftPactSheet` → the stream loop for `temple_restitution`) and dictated (the war door per car 0's ruling); the bundle-composition fixture (Q3 COMPOSE: a faith term composes in the sovereignty bundle through derived `TERM_FAMILIES`, no family literal outside the catalog — the source-scan pin both directions); one herald sentence for the faith-consideration sale on the existing faith desk.
4. Car 3 the tellable end to end on WF-0's fixture + its dark twin + the K3 twin (self-read never through the belief map — `selfBandsOf`'s own header) + refusal receipts (`pilgrimage_right` with `pilgrimageEnabled` dark) + the GRAMMAR→FAITH coupling rows.
5. Car 4 (7a) THE LIT CAR: register `faithTermsEnabled` (tail; comment names the two-preset roster and O-12) + door `faithTermsActive(rules) = rules.faithTermsEnabled === true && isFaithSpreadEnabled(rules)` in `faithTermExecutors.js`; `SIM_LIT_FAITH_SPREAD` gains `faithTermsEnabled: true`; `SIM_LIT` gains `believedDevotionEnabled: true` (four presets; conjunct `beliefAxesEnabled` — lit by TR-5 7a, CHECK-GIT-FIRST); NEW `subsystemRowsFaith.js` tail leaf (if not yet minted) with the `faithTermsEnabled` row; `subsystemRowsBelief.js:285`'s devotion row text amended LIT; the three `subsystemRowsVirtual.test.js` edits; both literals read off HEAD (+1: 35 → 36 if first, else N+1); covering-array union re-measured; `believedWorldAxesDormancyFence` devotion arm re-chartered; NEW `faithTermsDormancyFence.test.js` (LIT shape, no 64-hex literal); witness by hand; shift-ledger draft.
6. Car 4b (7b) if > 12 files: the census bills and the literals.
7. Pins' shape throughout: executor honesty (`missionary_access` writes no `share`); breach reachability under `tolerance_guarantee`; Law One walker over executor writes; `pactFormationDormancyFence` fence 1 dark identity holds (TR-5 7a re-chartered it — verify only).
8. Census bills: refreeze own commit; manifest ×(new files); no prose byte except the herald sentence (wiring census stamp).
9. Byte rule: closure +2 keys (one in each cohort) — quote; worker 0; engine quoted.
10. Exit: a faith pact drafts and streams on the bearing fixture; the attainability walker green in its fired state; both keys lit per roster; `evaluateTradeFlagLighting` green (the TR keys this lane depends on are lit); zero UNDECLARED.

**16b — WF-3 stance consequences (`faithStanceConsequencesEnabled`; 5 cars + repair)**
1. Car 0 THE RECONCILIATION (docs + walker): the five producers of §3 C0-C each ruled own/gate/declare; NEW `tests/lint/stanceConsequencesNoDoubleCount.walker.test.js` — `stanceConsequences.js` imports none of `disposition.js` / `dispositionProfile.js` / `faithChannelBindings.js` / `moralInstitutionPressure.js` / `religionState.js`'s mandate and writes none of `causalState.SYSTEM_VARIABLES` (K3 pointed inward; positive control that the scan sees a planted import); the stale headers cured (`faithField.js:12-22`, `deityAxes.js:35-42`) — two comment edits, zero behaviour.
2. Car 1 `stanceConsequences.js` (NEW ≤ 200): `aggression` → a THRESHOLD COLOUR on `deityStanceLane`'s escalation (never a selector — E3); `treatyDurability` → a decay-resistance multiplier the pact compliance read consumes in `treatyEnforcement.js` (113) or `pactAmendment.js` (140) — NEVER `peaceTerms.js`; `deityStance.js:160-174`'s deferral comment amended to "consumed by WF-3 (lane 16b)" (the GOLDEN_SHIFT_LEDGER `[worldpulse-religion-trade-3]` deferral closed — named in the shift ledger).
3. Car 2 the G1c fence FIRST: dark ⇒ the two consumed fields and every receipt byte-identical (captured before the seam); REVERSAL both directions on one fixture; COLOUR-NEVER-DROWN cap; the departed-minister lifecycle; guard-absoluteness regression; mutants (cap removal; fraying-scale zero).
4. Car 3 the posture read: `courtRiskAppetiteOf` (`strategicPosture.js:303`; the volume's `riskToleranceOf` collision is CURED — pre-pin the import SOURCE, not the collision) as a declared arm; the WF-5b foothold seed published (degraded-dark until 19b).
5. Car 4 THE LIT CAR: register + door `stanceConsequencesActive(rules)` (∧ spread); `SIM_LIT_FAITH_SPREAD` gains the key (two presets; comment cites O-12); the `subsystemRowsFaith.js` row; the three test edits; literals from HEAD; census; NEW `faithStanceDormancyFence.test.js` (LIT shape; lit twin over the bearing fixture); witness; shift ledger.
6. Pins' shape: the G1c golden's shift is a LIGHTING-DAY disclosure (J-WF-8) — declared in the ledger entry with the fields that moved.
7. Census bills: refreeze own commit; manifest ×2 (the walker, the fence); no prose byte.
8. Byte rule: closure +1 key; worker 0; engine quoted.
9. Serialization: boards after lane 5 (the pact readers); never in flight beside TR-5/WF-6/POP-5b/GR-5 on the pact files.
10. Exit: the two fields consumed; the no-double-count walker green; the key lit in two presets with the reason in its comment; zero UNDECLARED.

**17a — WF-2a the season (`pilgrimageEnabled`; 3 cars + repair)**
1. Car 1 `pilgrimSeason.js` (NEW ≤ 250; `PILGRIM_SEASON_TUNING` frozen): the co-location gate (patron creed at host ∧ believed/observed devotion at the neighbour), the season composition over the LIVE draw math (`pilgrimage.js:38-59`), the injectable belief leg (`beliefLegsFor`-style: `believedDevotionEnabled` lit ⇒ the SP-B devotion band; else the `observanceLabel` substrate — declared).
2. Car 2 the subsumption fork at `traditions/pilgrimage.js`'s scalar source (~5 lines of 41) + the door `pilgrimSeasonActive(rules)` (∧ spread) / `pilgrimMoversActive(rules)` (∧ `errandSpineEnabled === true`) in the leaf; pins: roads-fill-at-the-feast on WF-0's fixture; the THREE-WAY split negative (no-observance / aspatial / unmapped-or-no-neighbour, each its own seeded arm); the K3-shape import pin on the leaf; the DISCLOSURE pin (lit vs dark observance-success delta on one seed — the number WF-9 envelopes); dark ⇒ `pilgrimage.js`'s scalar byte-identical (the `traditions` dormancy golden is the own-footprint fence).
3. Car 3 THE LIT CAR: register + row + `SIM_LIT_FAITH_SPREAD` line + `faithPilgrimageDormancyFence.test.js` (LIT shape) + witness + shift ledger (the traditions layer is lit in all four presets — the season's delta is a DECLARED SHIFT for the two faith-lit presets, deity-bearing worlds only).
4. Mutants: season-band saturation (all bands → feast) reds the distribution pin; co-location gate dropped reds the three-way negative.
5. Census bills: refreeze; manifest; no prose byte.
6. Byte rule: closure +1; worker 0; `traditionsKernel.js` untouched (484).
7. Serialization: 17a alone on `pilgrimage.js`; boards after lane 5.
8. GRAMMAR seam: `pilgrimage_right` (WF-6's arm) flips from refusal to a live grant — the lane re-runs WF-6's refusal pin as the positive control.
9. Req 13/14 recorded (season derived; no verb).
10. Exit: the season runs on the bearing fixture; disclosure delta quoted; key lit in two presets.

**17b — WF-2b legates + pilgrims (the movers; 4 cars + repair)**
1. Car 1 `legateErrand.js` (NEW; `purposeClass 'religious'`) + the `built: true` flip at `envoyErrandVocabulary.js:322-327` IN THE SAME COMMIT (`errandConsumerRegistry.walker` reds both directions); mint through SP-D's writer (`errandMint.js`, `worldState.envoyErrands`); `ENVOY_PURPOSES` untouched (the class layer is the join).
2. Car 2 `pilgrimErrand.js` (NEW; `purposeClass 'personal'` — NEVER `religious`) + its flip `:328-333`; per-head pilgrims are NOT modelled (a mover is a party).
3. Car 3 interception via `foreignGuestHold.js` (WR-7b, read-only + close-through-the-writer; CHECK-GIT-FIRST vs TR-8); `envoyDiplomacyEnabled` is FALSE in EVERY preset incl. `full_simulation` (CONFIRMED by resolution) — the "degraded" harassment/turn-back arm is the LIVE arm everywhere, declared; the never-resolve walker (no named fate); war-on-the-road counterforce pin.
4. Car 4 the movers' LIT arm: `errandSpineEnabled` CHECK-GIT-FIRST — lit by lane B / IN-4 / TR-8 already, or lit HERE in `SIM_LIT` with `errandSpineDormancyFence.test.js` re-chartered (both polarities; the espionage gate's `!== true` precondition at `espionageGate.js:80-81` pinned); the `pilgrimageEnabled` fence's movers arm; witness; shift ledger. No new key.
5. Pins' shape: the registry row is the seam (volume §5-3 — the red-until-built probe is STRUCK); JSON-alias trap (`shepherdRef`/clergy are `npcs[]`).
6. Census bills: refreeze; manifest; no prose byte.
7. Byte rule: closure ±0 unless `errandSpineEnabled` lights here (+1); worker 0.
8. Serialization: after IN-4 on `envoyErrandVocabulary.js`; before TR-8.
9. Two ERRAND_CONSUMERS rows flip 6 → 8 built; TR-8's brief (56b) expects to find them flipped — state the count.
10. Exit: two movers mint and travel on the bearing fixture; both rows `built: true`; the movers arm lit.

**18 — WF-4 omen reads (`omenReadsEnabled`; 5 cars + repair)**
1. Car 1 `omenReading.js` (NEW ≤ 300; `OMEN_READING_TUNING`): the ONE writer of `spatialLedgers.omenReadings` (conditional materialization; `spatialLedgers` is already in `CONDITIONAL_LEDGER_KEYS`, `worldState.js:452-455`; the sub-key drop-when-empty pinned at the byte level through `ensureWorldState`); five kinds, each with its opposable arm ON THE SAME ROW; the lens reads patron temper × quadrant through the SAME embed read the ten W-FAITH consumers use (the authored temper is heard — `deityTemperConsumerCensus.walker.test.js`); caps/cooldown. Declaration line: the V-16 Auspice is the DM's forecast omen; WF-4's reading is the pulpit's; the `divination` desk is the forecast desk — VERIFY the "open expectation → divination" routing against that desk's live semantics.
2. Car 2 the fold: expectation window open/close moving EXISTING state through EXISTING writers (religionState/piety/religionLegitimacy UNTOUCHED); the failed-prophecy arm seeded once per kind; the credibility echo as a pure read off the ring (no new stock); the razed-mid-expectation close path (lifecycle fixture).
3. Car 3 the Herald rows (`heraldRouting.js` exact rows — the `faith_` prefix is the trap; own walker file with the L6 five joins) + the coverage manifest bill for `spatialLedgers`.
4. Car 4 pins: two-towns-one-famine TELLABLE on the bearing fixture; secular-town negative non-vacuous; NO-PHYSICS byte-diff over physical ledgers with the import pin excluding every physical-state writer (K3 inward); damped-lens echo; mutants (window never closes; lens damp zero).
5. Car 5 THE LIT CAR: register + door (LOCAL; single conjunct) + row + `SIM_LIT` line (four presets) + `omenReadsDormancyFence.test.js` (LIT shape; dark ⇒ no ledger key materializes) + witness + shift ledger + the #20 OSR row recorded (the `omenReadings` reader in the panel is the trailing mint's).
6. Persisted shape: TAKEN under law 6 (every world is test data; a veto is a code revert); the lifecycle clause (CREATE/READ/PERSIST/REGENERATE/UNDO/MIGRATE/VEIL) re-derived against `ensureWorldState`'s loop in car 1's body.
7. Census bills: refreeze; manifest; wiring census if a phrase lands; prose-numerics (bands, never floats).
8. Byte rule: closure +1 key; worker 0; engine quoted.
9. Serialization: alone in the WF chain; kinds beside other families' kinds are safe.
10. Exit: a reading opens and resolves (fulfilled and failed arms) on the bearing fixture; dark ⇒ zero ledger bytes; key lit in four.

**19a — WF-5a the split + `creedRef` (`faithSchismEnabled`; 4 cars + repair)**
1. Car 1 the charter clauses (§3 C0-G) as the car body + the split-condition gate leaf (or in-place in `religionState.js`, small) + `creedRef` on `settlement.institutions[]` rows minted ONLY by the FORKED founding call (`evaluateMoralInstitutionFounding` → `FOUNDING_INSTITUTIONS`, the reduced-ceremony call carries `creedRef`; never a second path); NO new fall cause (`PATRON_FALL_CAUSES` stays four; the contest outcome is `discredited`).
2. Car 2 the creed-aware backing fork in `religionLegitimacy.js` (`INSTITUTION_SCALE`/`STANDING_BACKING :181-184`, `:199`, `:495` — the `W_INSTITUTION` slot; `creedRef` rows back their creed only; absent ⇒ today's creed-agnostic semantics byte-identical); the ANTI-ENTRENCHMENT pin (the winner's backing does not rise vs the pre-split baseline).
3. Car 3 pins/mutants: backing-fork mutant (creedRef rows back the seat) reds anti-entrenchment; the lifecycle re-derivation (institutions ride the roster's regen path; a single-settlement regen preserves them; dangling `creedRef` heals to ABSENT through the one writer with a receipt); the never-prune-the-cellar pin re-derived against the LANDED prune (`pruneSuppressed`, `KEEP = 3`, `narrativePrune`).
4. Car 4 THE LIT CAR: register + door (LOCAL) + row + `SIM_LIT` (four) + `faithSchismDormancyFence.test.js` + witness + shift ledger + the #20 row for `creedRef`.
5. Substrate declared: the settlement schism ships (`resolvePatronContest :532`, `religiousContest.js:893`); W-K's status lifecycle UNAVAILABLE (flag-dark; two in-family importers); `clandestineFacet` stays criminal-only.
6. Census bills: refreeze; manifest; prose-numerics if a band word lands.
7. Byte rule: closure +1; worker 0.
8. Serialization: 19a → 20 on `religionLegitimacy.js`; boards after 16b (the foothold seed).
9. Req 14 recorded: the split rides the roster's EXISTING founding/abolition verb lane.
10. Exit: a split founds a creed-bearing institution on the bearing fixture; anti-entrenchment holds; key lit in four.

**19b — WF-5b the covert congregation (4 cars + repair; no new key)**
1. Car 1 `covertCongregation.js` (NEW ≤ 300; `COVERT_CONGREGATION_TUNING`: growth half-life 520w, assimilation 156w — DERIVED from `intervalWeeks.js:7`, never the `foodStockpile.js:66`/`populationDynamics.js:19` locals; each value carries its executed derivation or STOPs); `deities[ref].covert { share(band), sinceTick, shepherdRef? }` written ONLY through `suppressDeity` (`religionState.js:274-280`, the one writer) at the suppression/imposed-flip fraction; never-prune-the-cellar (covert > 0 unprunable).
2. Car 2 surfacing/exposure/assimilation evaluators; the `tolerance_guarantee` surfacing arm through `toleranceGuaranteeFor` (WF-6 landed — else the declared fallback); backfire wins on a weak seat; the WF-3 foothold seed consumed where lit (degraded-dark otherwise).
3. Car 3 THE PREMIUM-ISOLATION pin (covert receipts absent from every non-`includeCovert` projection — free, shared, lapsed, premium-player renders all audited) + the finite-semantics render pin (closed covert-size vocabulary: a handful / a score / a congregation in all but name) + exposure-at-zero-covert unreachable (seeded lower bound) + assimilation empties a cellar in-horizon + the never-resolve walker (no npc-death write in the faith arm).
4. Car 4 the covert arm's LIT act: no new key — the `faithSchismEnabled` fence gains the covert arms (dark ⇒ no `covert` key ever materializes, byte-level); witness re-recorded ONLY if a field moved (deity-free worlds: none expected); the shift-ledger entry names the second persisted shape (`covert`) for #20.
5. Mutants: covert-share leak (not banded) reds the render pin; prune-cure mutant reds the cellar pin.
6. Census bills: refreeze; manifest; prose-numerics (the covert-size words).
7. Byte rule: closure ±0; worker 0.
8. Serialization: after 19a and lane 5; alone in the chain.
9. VEIL: every new projection rides `includeCovert`/`includeGroundTruth` (double-gated); actor-id keys spread only when non-empty.
10. Exit: a cellar grows, surfaces or is exposed on the bearing fixture; no covert byte on any public render.

**20 — WF-7 the tithe (`titheEnabled`; 6 cars + repair)**
1. Car 1 `tithe.js` (NEW ≤ 250; `TITHE_TUNING`; quarterly = 13 weeks off `intervalWeeks.js:7` — the pin names 13, the 12-tick trap is cured law) + `religionStates[cid].templeWealth { band, movedTick }` through the ONE writer (`religionState.js` in-place, small); the declarations (§3 C0-G: `W_INSTITUTION` already prices richness as a count; `templeWealth` is a BAND not coin — no `treasury` read, asserted by an import-source scan; the DS-FTH-3/4 spelling law §333.2 — `tenure` too); the two stale comments cured in this car (`warFaithStateProse.js:96`, `dossierMounts.js:504-505`).
2. Car 2 the splendor term INSIDE `W_INSTITUTION`'s 0.12 slot (banded within it, never additive — ~4 lines in `religionLegitimacy.js`) + `generosityEV.js:243 templeMediated` wealth-band scaling + the remission lever (the seat's ACT through posture, both arms priced) + the SPLENDOR CAP pin (gold gilds, never buys).
3. Car 3 the sack coupling: the temple-loot weight as a READ the conserved sack arithmetic consumes (`warDeployment.js:803-812` / `warArmyRecord.js`; 684/800, ≤ 15 delta; STOP if slack < delta) with conservation pinned; coffers-at-the-fall disposition (inherit named / dispersal conserved) hooked at `recordPatronFall`; `movableWealth :788` (the writer-less `economicState.wealthIndex`, RECORD≠CODE §733.2) is READ, not cured — declared, not this wave's.
4. Car 4 pins/mutants: secular/temple-less negative non-vacuous; the resentment arm WINS measurably (→ commons voice read-only); dispersal-fraction 1.0 reds conservation; quarterly-cadence drift (12) reds the clock pin; fall → coffer both arms; dark ⇒ no `templeWealth` key ever materializes (byte-level).
5. Car 5 the prose side: `ENDOWED` becomes drawable — `prose-manifest-cells.mjs --out` base/tip, `--diff`, the moved rows BY POOL KEY, then `--record` with the cause; `node scripts/wiring-census.mjs`; prose-numerics (bands and words).
6. Car 6 THE LIT CAR: register + door (LOCAL) + row + `SIM_LIT` (four) + `titheDormancyFence.test.js` + witness + shift ledger + the #20 row for `templeWealth`.
7. Census bills: refreeze; manifest; wiring census stamp; prose cells; prose-numerics.
8. Byte rule: closure +1; worker 0; engine quoted; `warDeployment.js` delta stated.
9. Serialization: after 19a on `religionLegitimacy.js`; WF-7 → GR-6/WC on `warDeployment.js`; never beside lane E or A (the prose cells).
10. Exit: a temple's coffers fill quarterly, gild legitimacy under the cap, fall to the sack conserved; the dossier draws `ENDOWED`; key lit in four.

**21a — WF-8 the settlement voice (4 cars + repair; no key yet) — BOARDS ONLY AFTER THE CHAIR RULES Q1**
1. Car 1 the two unpaid obligations: the registry shrink-back review written into `faithNews.js:99-107` (FAITH leaves `kindPoolFloors.walker`'s one-row exception list as the family reaches ≥ 5 rows in this sitting); the annex `### faith.fall.abandoned (WF-1)` at `RECEIPT_POOLS_FAITH.md:174` REMOVED (ODQ §839 FLAG 2; §400 — a pool no state can draw); `faithKindPools.walker.test.js` gains the arm; prose cells `--record` if a cell moves (cause: the annex shrink-back's second half).
2. Car 2 kinds batch 1 (the WF-1/5/7 fire sites: the four fall causes, the split, the tithe/remission) in `FAITH_KIND_REGISTRY` (`faithNews.js` 73) + `WHAT_PHRASES` in `settlementRumors.js` (555/800 — measure at the cut; extract `faithPhrasePool.js` if slack < the batch) + exact `heraldRouting.js` rows + ONE walker file per kind family (L6: id + address chain + typed action + settlements by name + reason).
3. Car 3 kinds batch 2 (the WF-2/3/4/6 fire sites) + the honesty fix (the Ascendancy clause keyed to WF-2's season).
4. Car 4 the GOVERNOR pin (the flood fixture holds under the pacing caps — SP-6's classes referenced, never minted; the caps program-local and registered) + the PREMIUM pin on RENDERED projections + the TOTALITY walker (every WF kind in `WHAT_PHRASES` + `heraldRouting`; flag-dark ⇒ zero new entries byte-identical — the kinds mint behind their producers' keys, all lit by now) + the ≥ 3× census re-baselined against the LIVE phrased-token count AT THIS COMMIT (R6 — never inherit `:158`'s 18-row baseline).
5. Desk census: `heraldRouting.js:140-159` is 18 exact faith rows + 3 tier keys; `sovereignty_sale_judged` counted ONCE.
6. Census bills: refreeze (many new walker files — predict `totalFiles`); manifest per invariant file; wiring census stamp; prose cells if moved.
7. Byte rule: closure ±0 (display/pulse files); worker 0.
8. Serialization: after every WF lane 5–20; never beside E or A.
9. WF-8A stays on `faithUnseatingEnabled` (ruling recorded).
10. Exit: the faith desk speaks every landed WF transition; the family's kind pool ≥ 5 rows; the annex clean.

**21b — WF-8 the realm arcs + `faithNarrationEnabled` (4 cars + repair; LAST WF key)**
1. Car 1 `realmFaithArcs.js` (NEW ≤ 250; consumed at the compound-signature surface — VERIFY-AT-BUILD its extension shape; `realmEvents.js` is 309 eff, so the leaf is the volume's budgeted fallback taken as the default for parallel safety): the arcs Q1 keeps, with the overlap clause (Persecution/Awakening vs `gods_abandonment`'s existing coverage; Great Pilgrimage keyed to WF-2's season; Reformation as a pure derived read over `patronFalls` rings — window 260w / ≥ 3 settlements / one cause token, banded; Schism only if ruled).
2. Car 2 seeded-cluster reachability pins per arc + a WF-9 occurrence envelope each (the numbers WF-9 consumes).
3. Car 3 the realm tier's live beats declared (`pantheon_ascendancy`/`pantheon_twilight` UNGATED — not re-gated; `pantheon_extinction` WF-1C's) and the desk census closed.
4. Car 4 THE LIT CAR: register `faithNarrationEnabled` (LOCAL; the voice-fork at the first silent-transition receipt site — the fork is the door) + row + `SIM_LIT` (four) + `faithNarrationDormancyFence.test.js` (J-WF-9: dark worlds stay silent, no retroactive narration) + witness + shift ledger; the family's NINTH and last lighting act.
5. Census bills: refreeze; manifest; wiring census stamp.
6. Byte rule: closure +1; worker 0.
7. Mutants: governor-cap removal reds the flood pin; a kind dropped from `WHAT_PHRASES` reds the totality walker (executed once as the control).
8. Serialization: alone; after 21a.
9. Req 13: observer-axis judgments (purge, plunder, sale) name the OBSERVER's axis at the mouth.
10. Exit: the arcs reach on a seeded cluster of the bearing fixture; the flag lit in four; WF's flag count at the consist = the register length read off HEAD (7 WF mints + the neighbours').

**22 — WF-9 convergence instrumentation (no flag; 5 cars + repair)**
1. Car 1 `src/domain/certification/faithConvergenceContract.js` (NEW; templates `warConvergenceContract.js` 515 eff / `tradeConvergenceContract.js`): the WF CONTRACT EVALUATOR — `evaluateFaithFlagLighting(presets)` (a SPREAD key lit where `isFaithSpreadEnabled` is false ⇒ `spread_precondition_dark`; a LOCAL key lit in fewer than four ⇒ `roster_short` unless its comment records the reason; the movers arm without `errandSpineEnabled` ⇒ `errand_precondition_dark`); the arm lane 16a car 0's cohort fence carried is FOLDED here (the fence keeps a pointer, never a second copy).
2. Car 2 the eight-token endings walker (`faith_converted` qualified spelling); instrument-arc vocabularies OUTSIDE the mix with `roads_closed` + `plundered` floors; the tenure tail.
3. Car 3 the per-mechanism envelopes (the subsumption-seam delta from 17a; per-kind reading ratios from 18; arc occurrence from 21b; the fall histogram from 16a) + `FAITH_CONVERGENCE_TUNING` frozen with derivations + a mutant negative control per envelope (L7); the NINE-row counterforce audit + the sack lure.
4. Car 4 the WF-0 case grown to a small grid in the sidecar (`whole-world-soak.mjs` 780/800 — grid lines ≤ 6); the v5 `faithConvergence` receipt slot honest-empty from day one; certification rows grown from WF-0's shells and rendered on the surface from a fixture (the dossier landing).
5. Car 5 the lighting law exercised under the built-lit regime (every WF key's roster == expected; every fence green; the evaluator green on all seven presets) + the family's DONE-WHEN recorded: "the envelopes hold on the owner-ordered soak, and not before".
6. Census bills: refreeze; manifest; the coverage manifest for the receipt slot.
7. Byte rule: closure ±0 (certification is lazy — VERIFY by build); worker 0.
8. Serialization: after every WF lane; WF-0 ↔ TR-9 ↔ WF-9 on the soak grid.
9. No flag, no LIT act; the invalid-config walker is the family's contract from here (R2's "each family's own").
10. Exit: `faithConvergenceContract.js` reports zero violations at the consist; the terminal soak grades the WF rows.

---

## 6. OPEN CHAIR ACTS (vetoable rulings this plan makes, and the ones it hands up)

1. **RULED (vetoable):** the two-preset roster for SPREAD-lane WF keys through `SIM_LIT_FAITH_SPREAD` + the one-arm amendment to the D0 walker (§3 C0-F). **Handed up:** whether D0 gains a car C (the chair's train) or lane 16a car 0 carries it (priced there either way); whether the owner wants the LIT SUCCESSOR id (O-1's birth form) that would let faith light in four — owner-gated, unpriced.
2. **RULED (vetoable):** WF-6 boards after 16a (forced adjustment to PRICING §C slot 5) and its car 0 repairs GRAMMAR's `pactFormation.js:217`. **Handed up:** re-home the repair to GR (WF-6 → 5–8 C).
3. **RULED (vetoable):** the `PACT_DRAFT_LENS.faith_communion` re-band is a TUNING act — the attainability walker lands FIRED; the bounds wait for the owner's soak signature; the war door (a faith `CLASS_TERM` asset class) is a design row in brief 5.
4. **RULED (vetoable):** WF-8A's `faith_last_altar_dark` stays gated on `faithUnseatingEnabled` when `faithNarrationEnabled` mints.
5. **RULED (vetoable):** ARCH §7.1's `treasury.js` row loses WF-7; the WF chain row is amended to the sitting order of §1.
6. **HANDED UP (a chair act before lane 21):** Q1 — four arcs or five, WITH the overlap clause (C0-E).
7. **HANDED UP (ledger acts at the landing):** the RECORD≠CODE rows of §3's closing paragraph; L2's amendment already ruled by ADDENDUM R1 gains the conjunction-cohort clause if ruling 1 stands.

*Every line number above is a point-in-time reading at `315080928` and rots; a brief re-measures by symbol at charter (HISTORY-WF §C.2 item 8). The measurement script is `arch-932/eff-any.mjs <consist> <paths…>`.*
