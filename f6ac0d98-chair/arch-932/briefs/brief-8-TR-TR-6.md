# THE LANE — PRICING §C lane 8 · TR-6 THE CORNER + THE FAMINE SPECULATOR (`corneringEnabled`; ⚠ boards AFTER TR-2, not before it)

Architect: the Fable seat (family TR), 2026-09-15, read-only at the consist `laneCONSIST-932` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, tip `315080928`) against the ledger worktree `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a`. CONFIRMED / PLAUSIBLE as marked; line numbers rot — re-measure by SYMBOL. Protocols: `…/arch-932/ARCH-PROTOCOLS.md` (PROTO §n).

**Cars: 8 in this sitting (0, 1, 2, 3, 4, 5, 7a, 7b) + car 6 DETACHED into sitting 8b; range 8–9 with the repair allowance** (PRICING §B 5–7; HISTORY §A.4 **6–8** "+1: reconcile with the live famine block"; the LIT act lights a FOREIGN backlog key and counted 14 handwritten files, so ADDENDUM R5 makes the 7a/7b split THE PLAN: 7a the commons voice's full mint + lighting, 7b the corner's lighting). The ≤ 8 rule then detaches **car 6 (the Herald kinds) into a second sitting, 8b** — nothing depends on it, it lights nothing, it boards after 7b. **⚠ CORRECTED 2026-09-15 at the skeptic's five findings under `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`:** TR-2's leaves are named as brief 55 spells them (`merchantHouseLedger.js` · `merchantHouseActs.js` · `merchantHouseFactor.js` · `merchantHouseNews.js` — the existing TRADE stem `merchant` at `tests/lint/couplingInclusion.walker.test.js:180`, no pattern edit); the virtuality law is cited (R1); 7a/7b is the plan (R5); the BACKLOG's two assertions are named (`:1027` the exact sorted-set equality the row deletion moves; `:1032` the `≤ 17` burn-down ceiling lowered to 16); the mint/lighting split is read under R3.

**⛔ THE BOARDING-ORDER CORRECTION (vetoable, for the chair; CONFIRMED by the addendum's R2(ii) "`merchantHousesEnabled` (lane 55) before `corneringEnabled` (lane 8), as brief 8 already orders"):** PRICING §C lane 8 reads "TR-6 … Prerequisites: ⏳ lane 7" and its §B row "Depends on: TR-3 (unbuilt)". The CODE and the volume both say more: `tradeConvergenceContract.js:337-352` `corneringEnabled` `requiresTradeFlags: ['merchantHousesEnabled', 'believedMarketsEnabled', 'foodCaravansEnabled']`, `requiresForeignFlags: ['commonsVoiceEnabled']` (the walker parses `DESIGN_FP_TRADE.md:274` `TR-6 ⇒ TR-2 + TR-3 + TR-4 + \`commonsVoiceEnabled\` (` out of the document). The corner is a HOUSE act (`corner_attempt` is a member of TR-2's closed verb set in `merchantHouseActs.js`; positions are house-book interests in `spatialLedgers.houses`) — there is no corner without a house. **Ruling: lane 8 boards AFTER lane 55 (TR-2), lane 1 (TR-4) and lane 7 (TR-3) are on the consist, and after lane 2 (TR-5) for the monopoly arm's `market_access` counterforce.** The chair's boarding table moves TR-2 ahead of TR-6 (TR-2's own blocker, SP-4a, is TAKEN as a declared change by ADDENDUM R4 — lane 55 car 0). Recorded here so the dispatcher never boards a corner lane with no houses.

**THE LIGHTING PRECONDITIONS, BOTH DIRECTIONS (ADDENDUM R2):** what 7b NEEDS lit before it declares `corneringEnabled`: door D0 (car A — the cohort; car B is not this key's direct precondition, but `foodCaravansEnabled`'s, so it is inherited); `merchantHousesEnabled` (lane 55 car 5), `believedMarketsEnabled` (lane 7 car 5), `foodCaravansEnabled` (lane 1 car 6) — the three `requiresTradeFlags`; `commonsVoiceEnabled` — the one `requiresForeignFlags`, lit by THIS lane's 7a (or already by POP-7/POP-2 lane 26: CHECK-GIT-FIRST). `evaluateTradeFlagLighting` (`:497-522`) reds `trade_precondition_dark`/`foreign_precondition_dark` for any lit preset where one is dark, and TR-6's row deliberately has NO degraded arm (`:344-346`) — so a missing precondition at 7b's rebase is a BOARDING STOP, never a declaration. What 7b UNBLOCKS: nothing in the TR table depends on `corneringEnabled`; the endings `monopoly` and `cornered` become mintable (`TRADE_ENDING_MINT_ROWS`: `monopoly` `:431-436`, `cornered` `:449-454` — CONFIRMED) — lane 56c's envelopes read them. 7a/7b are built AT LANDING on the consist (R2(iii)).

**Prerequisites (then):** TR-2 (`spatialLedgers.houses`; `merchantHouseActs.js`'s closed verb set with `corner_attempt` RESERVED `built: false`; `merchantHouseLedger.js`'s formation rule — the ENTRY counterforce's builder; `merchantHouseFactor.js`'s cast factor — the one who flees at the riot), TR-3 (`commercialBeliefScarcity.js` the belief read; the no-merge scan's whitelist naming the corner composer BY NAME), TR-4 (`grainArrivalCredit.js`'s `GRAIN_TUNING.GRAIN_UNITS_PER_MONTH` — the ONE applicator's conversion), TR-5 car 2 (`marketAccessFor`), TR-1 mounted with its `commercialSeamsFor` registry (lane 7 car 1). `commonsVoiceEnabled`: BUILT/DARK (`commonsVoiceKernel.js`), in the walker's BACKLOG (17) — 7a lights it as a conjunct (below) unless POP-7/POP-2 (lane 26) cleared it first (CHECK-GIT-FIRST).

**Shared files this lane declares, slack at the cut:** `src/domain/spatial/commodityFlow.js` 448/800 (READ; the writer-family census over it — car 0; the warehouse-seizure writer JOINS the family — car 2; SERIAL after TR-4 → WY-3) · `src/domain/worldPulse/supplyKernel.js` 489/800 (the seizure/forced-sale write-back seam, ≤8 lines, car 2) · `src/domain/worldPulse/merchantHouseActs.js` (TR-2's, lane 55 car 2; the `corner_attempt` row flips `built: true`, car 3) · `src/domain/worldPulse/commercialReasons.js` 289/800 (the seams registry rows, car 3) · `src/domain/worldPulse/commercialReceiptPools.js` 386/800 (car 6: ~5 kinds × ~19 eff → ~480; SPLIT if a later lane nears 800) · `src/domain/worldPulse/commonsVoiceKernel.js` (READ ONLY — its header law: no new writer, no new effect vocabulary) · `src/domain/spatial/moralDrift.js` + `src/domain/worldPulse/moralInstitutionPressure.js` (READ) · `tests/lint/engineGatedRuleKeys.walker.test.js` (the BACKLOG row deletion — the `:1026-1029` exact equality — and the `:1032` ceiling 17 → 16, car 7a) · the flag-mint surfaces + `SIM_LIT` + `subsystemRowsTrade.js` · `tests/lint/couplingInclusion.walker.test.js` (add `corner[A-Z]|warehouse[A-Z]|monopoly[A-Z]` to the TRADE pattern at car 1 — or name every leaf `commercial*`; this brief names them `commercialCorner*.js` etc. so NO pattern edit is needed) · `tests/lint/scarcityNoMerge.walker.test.js` (lane 7's — the whitelist already names the corner composer; car 1 fills the name).

---

## THE STATE OF THE RECORD

- **The history fold** (`…/recon-32-34/HISTORY-TRADE-2026-09-15.md`, the TR fold; `HISTORY-TR-2026-09-15.md` does not exist under that name): TR-6 **UNBUILT (+1 reconciliation car)** — "No corner engine under any spelling: all five leaves absent"; `cornering` is TR-1's severance NAME (`commercialReasonTaxonomy.js:50`, mirror `provision` `:81`) scored against a seam whose producer is TR-6 itself (`commercialReasons.js:349`); the nearest cousin to `warehouseSeizure` is M7's conserved `seizureTake` (`smuggle.js:316-320` — a CARAVAN's carried load at a gate, `commodityFlow.js:613`; no stationary-stock arm exists). **THE SKEPTIC'S CORRECTION C — famine profiteering ALREADY SHIPS, LIT AND UNGATED, as sentiment:** `factionRelationshipUpdate.js`'s `famine` table `:221-237` moves merchant `wealth +4` ("Scarce grain commands crisis prices") / `publicTrust −6` ("Hoarding suspicion hardens against anyone holding stores"), criminal `power +5` / `wealth +4` ("Desperation sets the price of a smuggled sack") — and `food_anchor_lost` `:118-134` the same shape; `recalculateFactionRelationships` is called from `timeProgression.js:388` and `eventPipeline.js:269` on CANON EVENTS (a DM-applied famine/anchor-loss event), no flag. "TR-6 stays UNBUILT, but must reconcile with a live producer of its own fiction or double-count at two grains."
- **The volume:** `docs/DESIGN_FP_ARCH_TR.md` §4 TR-6 (five leaves; the pins; "moral pricing READS spatial/moralDrift.js + moralInstitutionPressure.js (S32 — path resolved, nothing rebuilt)"; C-TLF-3: "the commodityFlow writer-family census this line assumed DOES NOT EXIST — TR-6 AUTHORS the shrink-only source-scan census, the T4/storageMonths shape with an executed third-writer plant"); §5b seam 10 (toward POPULATIONS: "TR-6 consumes commonsVoiceKernel's rung reads under its header cohesion law (no new effect vocabulary, no new writer — the seizure writer is TR's own leaf)"). The design law `docs/DESIGN_FP_TRADE.md` §5 TR-6 (`:936-1019`): the corner gate (INDEPENDENT truth census — "the self-referential-pin hazard is the reason this sentence exists"), the moral pricing (observer-alignment scaling, the WR-8 world-judges idiom; the conscience arm), THE FIVE COUNTERFORCES each able to win (summoned caravans · the smugglers' road · the commons riot → THE WAREHOUSE SEIZURE (NEW conserved machinery) · the seat's FORCED SALE or EMERGENCY PURCHASE · the house's own carry), THE MONOPOLY ARM (share past MONOPOLY_DWELL seasons without the forced band; counterforces ENTRY via TR-2's formation rule, the smugglers' road, `market_access`), "the two never merge (Seam Three — the corner composer is Seam Three's whitelisted thin composer #1)"; §7 tuning (corner share band, corner term cap, carry strain, profiteering scale, observer-alignment consequences, forced-sale band, riot seizure share, monopoly share band, MONOPOLY_DWELL); §8's sentences ("House Verren holds the grain, and the bread knows it" · "They sold dear to the starving, and the town remembers" · "The corner broke: the smugglers' road fed Threeways when the warehouse would not"). Compiled programme §5 #28 (line 1690). The convergence contract's endings `monopoly` and `cornered` (`TRADE_ENDING_KEYS`, TR-9c) — MINTED HERE (`cornered` RAREST, `monopoly` RARE).
- **The frozen pending door** (`subsystemRowsVirtual.js:595`); the BACKLOG (`engineGatedRuleKeys.walker.test.js:193-210`, 17 exact, `commonsVoiceEnabled` row 2); the owner's laws (built lit; tuning raw + ratified at the soak; nothing pushed/deployed/applied/deleted/spent; the persisted shape TAKEN and recorded — this lane widens NONE: positions are house-book interests over real M6a stocks, TR-2's key).

## THE STATE OF THE CODE (CONFIRMED at the consist)

| Module | eff/ceiling | What is there |
|---|---|---|
| `src/domain/spatial/commodityFlow.js` | 448/800 | `spatialLedgers.commodityStocks` (`{sid: {gid: units}}`), `setStock` (module-private `:230-236`), `assertGoodsConservation` `:260-270` (five terms, integers), `commodityBand` `:187-193`, the M7 seizure branch `:605-625` (`seizureTake(rec.carried, smuggle.conscienceOf(worst.id))` `:613`; the take lands in the seizer's stock) |
| `src/domain/spatial/smuggle.js` | 119/800 | `seizureTake(carried, conscience)` `:316-320` (conscience-gated, W-C2); `smuggleDispatchWarrant` `:330-…` (the EV spill — counterforce 2's existing machinery) |
| `src/domain/worldPulse/supplyKernel.js` | 489/800 | the ONE write-back of stocks after `advanceCommodityFlow` (`:486-500` "Stocks live in the commodityStocks ledger") — the family's stock-write seam; `buildSmuggleContext` |
| `src/domain/worldPulse/commercialReasons.js` | 289/800 | `scoreRegisteredSeam` `:244-250`; `cornering: scoreRegisteredSeam(evidence.seam_cornering)` `:349`, `famine_profiteering: …seam_famine_profiteering` `:350`; `amendmentBSuppressions` `:263-274` (a PRESENT-and-zero stock read strikes `famine_profiteering` out, naming `spatialLedgers.commodityStocks`); `commercialSeamsFor(worldState)` — the registry lane 7 car 1 creates (CHECK-GIT-FIRST) |
| `src/domain/worldPulse/commonsVoiceKernel.js` | — | `commonsVoiceActive` `:48-51` (`commonsVoiceEnabled === true`, THE ONE door); `commonsGrievance01` `:102`; `RUNG_KIND = ['', 'commons_petition', 'commons_gathering', 'commons_riot']` `:184`; `advanceCommonsVoice` `:234` — mounted inside `assizeKernel.js` (the volume S27); its header law `:26-30`: CONSUMER + REFRAMER, no new writer |
| `src/domain/spatial/moralDrift.js` | — | `moralDriftTerm(worldState, id)` `:280`, `advanceMoralDrift({instigations, worldState, tick})` `:181` (its `instigations` input is the ONE road a gouge moves drift through — consumed, never a second writer), `moralReckoningNewsEntries` `:301` |
| `src/domain/worldPulse/moralInstitutionPressure.js` | — | `moralViabilityPressure` `:119`, `foundingFitFromTolerance` `:207` — the conscience arm reads these (the `institutionTolerance` idiom pointed inward) |
| `src/domain/factionRelationshipUpdate.js` | 645/800 | the EVENT-grain sentiment table: `famine` `:221-237`, `food_anchor_lost` `:118-134`; called on canon events only (`timeProgression.js:388`, `eventPipeline.js:269`) — NOT the pulse |
| `src/domain/worldPulse/generosityKernel.js` | 800/800 (AT THE WALL) | the PURCHASE lane (`:65-70`, `purchaseNews`, the prosperity BAND-STEP payment) — counterforce 4's EMERGENCY PURCHASE reuses its receipts by CALL, never by a line here |
| `src/domain/worldPulse/grainArrivalCredit.js` (TR-4 car 2) | ≤150 | `GRAIN_TUNING.GRAIN_UNITS_PER_MONTH` — the ONE conversion the seizure inherits; `applyFoodDeltasToUpdates` the ONE applicator |
| `src/domain/worldPulse/merchantHouseActs.js` / `merchantHouseLedger.js` / `merchantHouseFactor.js` / `merchantHouseNews.js` (TR-2, lane 55 cars 1–3 — the names brief 55 spells so the leaves match the existing TRADE stem `merchant` at `tests/lint/couplingInclusion.walker.test.js:180` without a pattern edit; ABSENT at the consist) | — | `merchantHouseActs.js`: the closed verb set `HOUSE_VERBS` with `corner_attempt` and `venture_stake` RESERVED as `built: false` rows, flipped by TR-6/TR-7 in THEIR commits (the `ERRAND_CONSUMERS` idiom); `merchantHouseLedger.js`: the ONE writer of `spatialLedgers.houses`, books `{holdings, credit, interests[]}`, the formation rule (the ENTRY counterforce), the ruin latch — corner positions and the monopoly dwell are `interests` rows written THROUGH it; `merchantHouseFactor.js`: `castHouseFactor` (the factor who flees at the riot); `merchantHouseNews.js`: TR-2's kinds (this lane's five corner kinds go in `commercialCornerNews.js`, sitting 8b) |
| `src/domain/worldPulse/commercialBeliefScarcity.js` (TR-3) | — | `believedDearness01Of` — the corner MOTIVE's belief read |
| `tests/lint/scarcityNoMerge.walker.test.js` (TR-3) | — | the whitelist census naming the corner composer BY NAME ahead of its existence |
| `src/domain/certification/tradeConvergenceContract.js` | 398 eff | TR-6's row `:337-352` ("deliberately NO degraded four-of-five arm"); `TRADE_ENDING_KEYS` incl. `monopoly`, `cornered`; their mint rows `TRADE_ENDING_MINT_ROWS` `:431-436` (`monopoly`, `requiresFlags: ['corneringEnabled']`) and `:449-454` (`cornered`) |
| `tests/lint/engineGatedRuleKeys.walker.test.js` | — | `BACKLOG_RULE_KEYS` `:193-210` (17 rows; `commonsVoiceEnabled` row 2 "THE COMMONS' VOICE — a pulse-tick kernel behind its own virtual flag (worldPulse/commonsVoiceKernel.js)"). **TWO ASSERTIONS GOVERN IT, AND THEY ARE DIFFERENT (CONFIRMED `:1020-1032`):** (1) `:1026-1029` `expect(Object.keys(BACKLOG_RULE_KEYS).sort()).toEqual(measuredGap)` — an EXACT sorted-set equality against the MEASURED gated-but-uncensused set (`measuredGap = readKeys.filter(key => !censusSet.has(key) && !(key in EXEMPT_RULE_KEYS))`, `:1023-1025`); the moment 7a adds `commonsVoiceEnabled` to the register it enters the census and LEAVES `measuredGap`, so the row MUST be deleted in the same commit or this equality reds ("bank wins by deleting rows, never by widening the list"); (2) `:1032` `expect(Object.keys(BACKLOG_RULE_KEYS).length).toBeLessThanOrEqual(17)` — a `≤` CEILING, "the burn-down marker, never the guard. Lower it whenever a key earns its manifest entry and its certification row; never raise it" (`:1030-1031`) — 7a lowers the literal 17 → 16 in the same commit (the assertion would stay green at 17 with 16 rows; lowering is the instruction the file gives itself, not a red it cures). PROTO §1.4 row 4 |
| `tests/property/commonsVoiceDormancyGolden.test.js` (+ fixture) | — | drives LITERAL rules — a preset declaration moves NOTHING here (PROTO §3.1B) |
| ABSENT | — | `cornerGate/cornerComposer/warehouseSeizure/monopolyDwell/cornerNews` under any spelling; `corneringEnabled` (zero hits); any commodityFlow writer-family census; any `commonsVoiceEnabled` certification row |

**Budgets (PROTO §4.1):** worker EXACT — nothing here is in the worker graph (`factionArchetypes.js`, a TR-2 read, is; READ ONLY); `engine` ±0 (advance-worker leaves); first-paint: ONE `SIM_LIT` line at 7a and ONE at 7b, each quoted from its own build (the margin is CONTESTED — A0 measured 5,828 B where PRICING quotes 795 B); `generosityKernel.js` 800/800 — ZERO lines there (call its exports).

## ALREADY DONE (do not re-land)

M7's caravan seizure (`seizureTake`, the pipeline) — the COUSIN, not the warehouse arm · the commons ladder (V-22/V-23, BUILT/DARK) — consumed, never rewritten · the moral substrate (S32 resolved) · TR-1's `cornering`/`famine_profiteering` scorers and Herald kinds (`commercial_cornering`, `commercial_famine_profiteering`, `commercial_provision`, `commercial_famine_relief` — the REASON kinds exist; this lane feeds their seams and mints its OWN five corner kinds) · the amendment-B suppression (a physically-empty warehouse strikes profiteering out — this lane's truth census makes that read REACHABLE for most pairs) · the event-grain famine sentiment (LIT, ungated — reconciled at car 0, not re-landed).

---

## THE CARS

Every car: one gated commit; anchored negatives; executed mutants manifest-rowed; NUL-scanned; porcelain 0; `Seat: Opus 5 — Fable-unvalidated` + `Lane: TR-6`; PACKET caps.

### Car 0 — the commodityFlow WRITER-FAMILY CENSUS authored from nothing (C-TLF-3) + the famine-block RECONCILIATION ruled and pinned

**In plain words:** before a new piece of machinery may move warehouse stock, a permanent test lists exactly which modules are allowed to write the stock ledgers today and proves a stranger cannot join unnoticed; and the lane writes down, once, how the corner's profiteering relates to the older "merchants get richer in a famine" sentiment table — so the two never count the same crime twice.

**The change.** `tests/lint/commodityFlowWriters.walker.test.js` (NEW): a source scan over `src/` for writes to `spatialLedgers.commodityStocks` / `supplyShipments` (`setSpatialLedger(… 'commodityStocks'` and the private `setStock`), asserting the EXACT measured set (expected: `spatial/commodityFlow.js` (the engine) and `worldPulse/supplyKernel.js` (the write-back) — MEASURE; the T4/storageMonths census shape from lane 1 car 0), SHRINK-ONLY, with an EXECUTED third-writer plant (a `setSpatialLedger(ws, 'commodityStocks', …)` planted in a throwaway leaf → reds; restored md5-exact). **THE RECONCILIATION RULING (chair, vetoable):** the event-table block (`factionRelationshipUpdate.js:221-237`) is the EVENT-GRAIN sentiment on a DM-applied canon event (a faction's `wealth`/`publicTrust` WORDS); TR-6's corner is the PULSE-GRAIN mechanism (house books, stock positions, the TR-1 ledger's `famine_profiteering` seam). **OWN AND CROSS-REFERENCE — neither gate nor fold:** gating a live ungated canon-event table would be a declared behaviour shift on the DM's own verbs (owner-facing, not this lane's to take); folding the event table into the corner would make a DM famine event depend on a virtual engine key. The pin that keeps them apart: a source scan asserting no module under the TR-6 leaf set imports `factionRelationshipUpdate.js` or writes `resources.wealth`/`publicTrust` (anchored on `factionRelationshipUpdate.js:230` doing exactly that), and the TR-6 certification row (car 7) records both grains by file:line. `docs/DESIGN_FP_ARCH_TR.md` §4 TR-6 (dock copy) gains the ruling as a dated bracket.

**Files:** the census test, `tests/lint/cornerNoEventSentiment.walker.test.js` (NEW — may be one file with two describes), the doc. **Census bills:** manifest ×2; refreeze own commit. **Byte rule:** no src byte — state so. **Gate:** tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · check:quick. **LIT act:** none. **Exit:** the census names the measured set and reds its plant; the ruling is written.

### Car 1 — `commercialCornerGate.js` (the INDEPENDENT truth census) + `commercialCornerComposer.js` (Seam Three whitelist #1)

**In plain words:** two pure pieces — one that counts, from the town's real stocks and the roads' real inbound capacity, how much of a good a house actually holds versus everything reachable (never trusting the house's own books for the denominator), and one that combines "the house BELIEVES a famine is coming" with "the house REALLY holds the gate" into two separate answers without ever blending the two.

**The change.** `commercialCornerGate.js` (≤200 eff): `reachableSupplyOf(worldState, digest, settlementId, goodId)` = local `commodityStocks` units + inbound in-transit `supplyShipments` carried toward the settlement + the ranked producers' available stock reachable this season (`rankSupplySources`/`deriveConsumingLinks` shapes from `supplyShipments.js`/`supplyKernel.js`, READ) — the DENOMINATOR, derived from the ledgers, NEVER from `houses[].books.interests`; `heldShare01(house, reachable)`; `cornerGateOpen({heldShare01, truthBand})` — true iff share ≥ `CORNER_TUNING.CORNER_SHARE_BAND` AND the TRUTH band (`commodityBand`) is ≥ dear; the corner TERM: `cornerTermFor(truthBand, gateOpen)` — forces the local band one rung up while the gate is open (the read `marketPrices.commodityBandForGood` and `commodityFlow.commodityBand` consumers see it through ONE hook — `commodityBandWithCorner(worldState, sid, gid)` exported here, consumed by the display in car 3). `commercialCornerComposer.js` (≤150 eff; Seam Three whitelist #1): `cornerDecision({believedDearness01, believedTrajectory, gate, appetite, credibility})` → `{motive01, gateOpen, receipt}` — TWO OUTPUTS, no combining expression; imports `commercialBeliefScarcity.js` (belief) and `commercialCornerGate.js` (truth) — the ONLY module besides TR-3's dispatch composer permitted to import both sides (the whitelist census at `tests/lint/scarcityNoMerge.walker.test.js` already names it — this car fills the name; a third co-importer reds). `CORNER_TUNING` RAW with derivations (share band, term cap, carry strain, profiteering scale, observer-alignment consequence table, forced-sale band, riot seizure share, monopoly share band, `MONOPOLY_DWELL` in 13-week seasons via `INTERVAL_WEEKS`).

**Pins / negatives:** NO PHANTOM CORNER — a house whose books CLAIM 90 % while the ledgers show 10 % reachable does NOT open the gate (the self-referential-denominator MUTANT: swap the census for the house's own map → the pin reds); corner-at-glut impossible (truth `surplus` ⇒ gate false whatever the share — FALSE-branch reachability); the no-merge scan green with the composer whitelisted; guard-the-guard: a combining expression planted in the composer → the scan reds. **Files:** two leaves + two tests + the no-merge walker's whitelist name. **Census bills:** manifest ×2; refreeze own commit. **Byte rule:** build quoted; worker unchanged; engine ±0. **Gate:** full list. **LIT act:** none (zero callers — the dark-instrument shape). **Exit:** both leaves pinned; the whitelist filled.

### Car 2 — `commercialWarehouseSeizure.js` (NEW conserved machinery, the family's third writer) + `commercialMonopolyDwell.js`

**In plain words:** a way for stock to be TAKEN out of a house's warehouse and into the town's granary or a rival's stock — riot or edict — that keeps the goods ledger balanced to the unit and converts to granary months only through the one existing door; and a quiet detector for a supplier that has held a market alone for too many seasons.

**The change.** `commercialWarehouseSeizure.js` (≤200 eff): `seizeFromWarehouse({stocks, houseInterest, share, toSettlementId})` — moves integer units from the house-interest position (a stock the house holds at its settlement in `commodityStocks` under the house's interest row) to the seizing party's stock in the SAME `commodityStocks` map, returning `{stocks, moved, receipt}`; joins the five-term balance (`assertGoodsConservation` — a seizure is a transfer, `before + produced === after + consumed + lost` holds with `moved` inside `after`); units → granary MONTHS ONLY when the destination is the town's granary, via a delta handed to `applyFoodDeltasToUpdates` using TR-4's `GRAIN_TUNING.GRAIN_UNITS_PER_MONTH` (imported; never a second constant); serves BOTH the riot counterforce (car 4) and the seat's forced sale (car 4). **THE FAMILY CENSUS ADMITS IT AT N+1 IN THIS COMMIT** (car 0's walker: the set grows by exactly this module and its third-writer plant re-runs). The write-back seam in `supplyKernel.js` (≤8 lines, under `corneringActive` — read from `commercialCornerGate.js`'s door, THE ONE by-name read `rules.corneringEnabled === true`): the seizure's returned stocks replace the tick's `commodityStocks` before the existing write-back. `commercialMonopolyDwell.js` (≤120 eff): `monopolyDwellOf(prior, {heldShare01, gateOpen, tick})` — a per-(house, settlement, good) dwell counter in the house's interest row (TR-2's `houses` shape; conditional, drop-when-absent) that advances while share ≥ `MONOPOLY_SHARE_BAND` and the corner gate is NOT open, resets otherwise; `monopolyCrossed` when dwell ≥ `MONOPOLY_DWELL` seasons (INTERVAL_WEEKS-derived).

**Pins / negatives:** conservation before/after a seizure (integers; the moved quantity appears exactly once); no months minted without units (months × constant ≤ units); the dwell resets when the corner gate opens (a corner is not a monopoly); dark ⇒ the seam is never entered (fixture identity). Mutants: a seizure that credits months without debiting units → conservation reds; the dwell not reset → the reset pin reds; the census plant → reds. **Files:** two leaves, `supplyKernel.js` (+≤8), `tests/lint/commodityFlowWriters.walker.test.js` (N+1), two tests. **Census bills:** manifest ×3; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list (the M6a integration fixtures unmoved). **LIT act:** none. **THE MINT/LIGHTING SPLIT, READ UNDER ADDENDUM R3 (recorded, vetoable):** `corneringEnabled === true` is read by name in `commercialCornerGate.js` from THIS car (the seam in `supplyKernel.js` calls it), and `engineGatedRuleKeys.walker.test.js`'s `scanGateReads` (`:538-541`) reads every `src/` file regardless of callers, so `auditEngineGatedKeys` (`:496-497`) would red `unaccountedReads` unless the register entry lands in the same commit — R3 lets that DARK MINT land early. The estate's one-commit law drags the rest with it: a register entry without its row reds `manifestWithoutRow` (`:512-520`; `VIRTUAL_PENDING_RULE_KEYS` is `[]`, pinned at `subsystemRowsVirtual.test.js:647`), a row without its `VIRTUAL_RULES` position reds `:646`, both moved lengths red `contributionLedgerShape.test.js:118-119`, `coveringArrayCoverage.test.js`'s `union` counts register members, and any `simulationRules.js` byte stales the two edge bundles. So THIS car pays in ONE commit: the register entry (tail, DARK — the key in NO preset), the gate read, the AUTHORED row in `subsystemRowsTrade.js` (CHECK-GIT-FIRST for the leaf; the row must not spell any FOREIGN key in a string field — `commonsVoiceEnabled` is named only in a comment until 7a mints it), the three `subsystemRowsVirtual.test.js` edits, BOTH `contributionLedgerShape` literals READ OFF HEAD at this car's base, the covering-array `union`, the four edge-shared files. 7b (LAST, rebased) pays the LIGHTING and RE-READS the literals and census figures off the composed HEAD (7a will have moved them by one for the commons key). This is R3 applied as far as the walker allows; the departure from R3's literal list is forced by `manifestWithoutRow` and recorded here for the chair. **Exit:** the seizure conserves; the census admits it; the mint registered dark (`ENGINE_GATED_DORMANT_RULE_KEYS` CONTAINS `corneringEnabled` at this car — measured).

### Car 3 — the corner ACT wired: `corner_attempt` as a house verb, the forced band on the truth read, the moral pricing, the TR-1 seams FED

**In plain words:** a merchant house can now try to corner a good — its sponsored caravans buy into its warehouse at markets it believes soft, the town's price band is forced up while it holds the gate, every sale to the hungry mints a profiteering receipt that the town's grievance ledger, the moral-drift layer and each neighbour's credibility read all see — scaled by how each observer's own alignment judges it, never gating whether it may happen.

**The change.** `merchantHouseActs.js` (TR-2's; +≤10): the `corner_attempt` verb becomes live — chosen threshold-style from `cornerDecision`'s motive (belief) when the truth gate could open; executed by depositing dispatch warrants into the EXISTING machinery (M6a purchases into the house-interest stock — `commodityFlow`'s dispatch, the position credited on arrival). The forced band: `marketPrices.commodityBandForGood` and the M6a `needPremium` read consult `commodityBandWithCorner` (car 1) under the flag — the corner term is a TRUTH-side derivation (the gate is truth), so the display follows for free and the summoned-caravans counterforce (car 4) is the same read. THE MORAL PRICING (Req 13 ENGAGED IN FULL, T6/J-TR-7: NEVER A GATE): when the forced band coincides with need (`storageMonths` low, famine season, siege), every sale mints a profiteering receipt; `commercialSeamsFor(worldState)` (lane 7 car 1's registry in `commercialReasons.js`) gains the TR-6 producers — `seam_cornering` and `seam_famine_profiteering` `{severance01, partnership01}` per directed pair (the house's settlement → every buyer) — **6/16 → 8/16 (or 7 → 9 if TR-5 car 4 landed) DECLARED** in the TR-1 row text and the `commercialReasons.test.js` arm; moral drift moves through `advanceMoralDrift`'s `instigations` input (the ONE road — read the shape at `moralDrift.js:181`); SP-5 credibility falls per observer SCALED BY THE OBSERVER'S ALIGNMENT AXIS (the WR-8 world-judges idiom — cite `conquestDoctrineStage.js`/`vengeanceLicense.js` for the shape; the scale table in `CORNER_TUNING`); the house's own settlement suffers the conscience arm (`moralViabilityPressure` pointed inward at the house's institutions). ALIGNMENT NEVER GATES: a good house corners and pays conscience + credibility + commons; an evil one pays less morally and equally physically — BOTH ARMS pinned.

**Pins / negatives:** amendment-B from this side (a corner over a physically-EMPTY warehouse mints no profiteering — the suppression's read is now reachable); the two alignment arms; the seams fed exactly when the gate is open; the dark twin byte-identical (TR-2's house fixtures unmoved). Mutants: alignment turned into a gate (an evil-only branch) → the both-arms pin reds; the seam fed on a closed gate → reds. **Files:** `merchantHouseActs.js`, `commercialReasons.js` (the registry rows), `commercialCornerGate.js` (the read hook), `marketPrices.js` (+2), `dispatchEV.js` (+1 — the need read through the hook; SERIAL after lane 7 car 3), `tests/domain/commercialCorner.test.js`, `tests/domain/commercialReasons.test.js` (the 8/16 arm), `couplingRegistryTrade.js` (TRADE→INFO credibility, TRADE→INTERIOR moral drift rows — the same-commit law). **Census bills:** manifest ×2; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list. **LIT act:** none. **Exit:** the act runs lit; the seams fed; both alignment arms; dark identical.

### Car 4 — THE FIVE COUNTERFORCES, each WINNING on its own fixture, + the monopoly arm + the endings

**In plain words:** five ways a corner breaks, each proved to actually break one on a purpose-built world — rival caravans drawn by the very dearness the corner advertises, smugglers under the gate, a riot that seizes the warehouse, a seat that forces a sale or buys its way out, and the house's own carrying cost — plus the quieter case of a lone supplier that has held a market too long.

**The change (tests + the counterforce wiring in the leaves, ≤40 src lines total):** (1) SUMMONED CARAVANS — the forced band IS a believed-dear broadcast (TR-3's letters-home write propagates it); other dispatchers read opportunity through `commercialDispatchDestination` and arrivals break the band: fixture → the gate closes within N ticks. (2) THE SMUGGLERS' ROAD — the deep-shortage link spills to M7's `smuggleDispatchWarrant` (existing); the hidden path feeds the town; the gate closes. (3) THE COMMONS — `famine_profiteering` at magnitude raises `commonsGrievance01`; the ladder climbs petition → gathering → riot (`RUNG_KIND[3]`, READ); TR-6's house-side machinery READS the riot rung against the house's position and executes the WAREHOUSE SEIZURE (car 2) into the granary (months through the one applicator); the factor flees to the roster (never engine-killed). (4) THE SEAT — two priced arms chosen by posture + books: the FORCED SALE (stock → granary at the forced-sale band through the SAME seizure writer; the house mints a grievance against the seat — `contract_default`-class through TR-5's seam if present, else a receipt) or the EMERGENCY PURCHASE (the generosity lane's PURCHASE receipts at the forced band, by CALL into `generosityKernel.js`'s exports — ZERO lines in that 800/800 file). (5) THE HOUSE'S OWN CARRY — warehouse ceilings cap the position (`originStockCap`); credit-band strain per season held; an exit is only ever a SALE, and a sale into its own broken band is the RUIN arm (TR-2's ruin latch). THE MONOPOLY ARM: `monopolyCrossed` ⇒ `dependency_fear` accrues in dependants' ledgers off the same channel read (the seam registry gains `seam_dependency`? — NO: `dependency_fear` is a LIVE scorer (`scoreDependency`), so the monopoly feeds its EVIDENCE (`salience01`/`trust01`) not a seam — design note); sale receipts name the sole supplier; counterforces ENTRY (the dear-band evidence summons a rival house through TR-2's formation rule — pinned by driving the formation predicate), the smugglers' road, `market_access` (TR-5's `marketAccessFor` opens what the monopolist holds shut). ENDINGS: `monopoly` mints on the dwell crossing, `cornered` on a corner held a full season — receipts in the convergence contract's vocabulary (TR-9 polices envelopes; this car mints).

**Pins:** EACH counterforce wins on its own fixture (five fixtures); NO PHANTOM MONOPOLY (independent census); monopoly reachable-but-rare (one fixture reaches it; the calm corpus over 52 ticks reaches none); `cornered` reachable-but-rarest; the appetite reversal after ruin (SP-4a reversal); the seizure and forced-sale conservation pins re-run on the fixtures. Mutants: counterforce 3 severed (the riot never seizes) → its fixture reds; the entry builder severed → the monopoly fixture's rival never forms. **Files:** `tests/domain/commercialCornerCounterforces.test.js` (NEW, may split ×2), the leaves' wiring lines, `tests/domain/commercialMonopoly.test.js`. **Census bills:** manifest ×2; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list. **LIT act:** none. **Exit:** five wins; two endings reachable; no phantom.

### Car 5 — T-2 / T-3 tellables + the dossier line (ONE user-facing surface)

**In plain words:** the two stories pinned end to end — "House Verren holds the grain, and the bread knows it" and "They sold dear to the starving, and the town remembers" — and the town page's market line gains the corner's word ("held by House Verren") beside TR-3's belief line.

**The change:** `tests/domain/commercialCornerTellables.test.js` (T-2: corner → forced band → profiteering receipts → commons rung → seizure, receipts at each link; T-3: the gouge remembered — the TR-1 ledger's `famine_profiteering` row persists past the corner's end and decays on healed state); `marketPrices.js` (+≤4): the corner term's word on the market line (bands and words; the house's NAME is a settlement-page fact, allowed); `EconomicsTab.jsx` **596/600** — ONE line or extract first (lane 7 car 4 may already have extracted `EconomicsMarketBeliefs.jsx` — CHECK-GIT-FIRST and add the corner word THERE). Prose-numerics: no digits. **Files:** the tellables test, `marketPrices.js`, the tab or its leaf, a component test. **Census bills:** wiring census `--check`; prose-numerics; manifest; refreeze own commit. **Byte rule:** build quoted (lazy display; closure ±0 asserted). **Gate:** full list + `tests/components`. **LIT act:** none. **Exit:** both tellables; the line renders lit, nothing dark.

### Car 6 — `commercialCornerNews.js`: five Herald kinds, the L6 five joins, their OWN walker — DETACHED INTO SITTING 8b (the ≤ 8 rule, once 7a/7b became two cars under ADDENDUM R5; boards after 7b on the consist; no flag act; nothing in cars 0–5/7a/7b depends on it)

**In plain words:** the Herald learns the corner's five sentences — the hold, the gouge, the break, the seizure, the monopoly — each with its phrase pool, desk, address chain and a permanent walker of its own; the pacing governor meters them because a corner arc is exactly the kind of thing that floods a season.

**The change.** Kinds `commercial_corner_held` ('notable'), `commercial_corner_gouge` ('major'), `commercial_corner_broken` ('notable'), `commercial_warehouse_seized` ('major'), `commercial_monopoly_held` ('routine') — registered in a `commercialCornerNews.js` leaf (≤150 eff, the `commercialReasonsNews.js` registry idiom); pool rows in `commercialReceiptPools.js` (386 → ~480) mirroring `docs/content/RECEIPT_POOLS_TRADE.md` (the ONE reader `anchoredOnce`); `WHAT_PHRASES` rows; `heraldRouting.js` `trade` desk rows; the address chain from the corner receipts under `corneringActive` only; `tests/lint/commercialCornerKindPools.walker.test.js` (NEW; frequency-scaled floors); `heraldContaminationFence` green; every kind pacing-registered (`narrativeTempo.js`'s machinery — SPINE §1.12); the metronome law: the corner emitter names its cooldown (`metronomeCooldownLint`). **Files:** ≈8. **Census bills:** wiring census stamp; prose-numerics; manifest; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list + the kind walkers, `heraldContaminationFence`, `proseWiringCensus.walker`, `metronomeCooldownLint`. **LIT act:** none. **Exit:** five kinds × five joins; the annex the one reader.

### Cars 7a and 7b — THE LIT CARS (ADDENDUM R5: the split IS the plan — the first cut counted ≈14 handwritten files against PACKET_STANDARD's ≤12): 7a THE COMMONS LIT (`commonsVoiceEnabled` leaves the BACKLOG: full mint + lighting, its own bill); 7b THE CORNER LIT (`corneringEnabled` lit in the four lit presets; the dark mint was car 2) — both rebased at landing, 7a before 7b, both built on the consist after door D0 and lanes 55, 7, 1

**In plain words:** two switches, two commits. First the long-dark commons voice (petitions, gatherings, riots) is switched on for worlds born into the four living presets, with its own certification row authored and its backlog row retired — it is the riot counterforce the record refuses a corner without. Then the corner is switched on for the same four presets, lawfully, because by then every switch it depends on (merchant houses, believed markets, the grain road, the commons voice, and the caravan layer under them) is already on.

**⛔ THE LAWS BOTH CARS OBEY BY CITATION (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`):** R1 — the virtuality law is amended; `SIM_LIT` is the one lawful home of a lit FP key; the `tradeConvergenceContract.walker.test.js:367` arm is D0 car A's re-cut (this lane never edits it; 7b runs it and quotes it green with `corneringEnabled` in the cohort — `commonsVoiceEnabled` is NOT a TRADE key and escapes that arm, but L2-as-amended binds it the same way). R2 — 7b lands after door D0, after `merchantHousesEnabled` (lane 55 car 5), `believedMarketsEnabled` (lane 7 car 5), `foodCaravansEnabled` (lane 1 car 6) and after 7a; `evaluateTradeFlagLighting(SIMULATION_RULE_PRESETS[id].rules).ok === true` for all seven presets at 7b's tip (the four `requiresTradeFlags`/`requiresForeignFlags` of `corneringEnabled`, `:337-352`, all lit in all four), the negative control `{ corneringEnabled: true }` → exactly four violations; a dark precondition at the rebase is a BOARDING STOP, never a declaration (TR-6's row has no degraded arm, `:344-346`). R3 — the corner's dark mint was car 2 (register entry, gate read, row, the three test edits, both literals off HEAD at car 2's base); 7b is its LIGHTING and re-reads the counts; the commons voice has NO early mint (its gate read `commonsVoiceEnabled === true` at `commonsVoiceKernel.js:48-51` pre-exists as a BACKLOG read), so 7a is a FULL mint + lighting in one commit — the only TR LIT car that is.

**7a — THE COMMONS VOICE'S LIGHTING (the conjunct rule, PROTO §1.2 step 3):** `SIM_LIT` gains `commonsVoiceEnabled: true, // V-22/V-23's key — lit by TR-6 car 7a as the riot counterforce's conjunct (the first lane on it)` — CHECK-GIT-FIRST: if POP-7/POP-2 (lane 26) cleared and lit it, 7a COLLAPSES to nothing and the lane reports so. `commonsVoiceEnabled` is a BACKLOG key (a virtual key with a live gate and no manifest entry) — so this car pays its FULL MINT + LIGHTING: the register entry (tail, with the comment naming V-22/V-23, `commonsVoiceKernel.commonsVoiceActive` the ONE door, and this car as the lighting lane), its AUTHORED certification row in a NEW `subsystemRowsPeople.js`? — NO: that leaf exists (`src/domain/certification/subsystemRowsPeople.js`) for another lane; the commons voice's row goes at the TAIL of `VIRTUAL_SUBSYSTEM_ROWS` in a new `subsystemRowsCommons.js` leaf (evidence law: `eventTypes` = the `candidateType:` literals the kernel mints — locate them (`commonsVoiceKernel.js:184-197` builds `wizard_news` ids `commons_petition/gathering/riot` — read whether they are `candidateType` literals or news ids; declare honestly), `stateKeys: ['spatialLedgers.commonsVoice']` (the v5 census container `spatialUsage.js:349` EXEMPT row names it)), the BACKLOG row deleted — TWO ASSERTIONS, EACH NAMED (the skeptic's correction of the first cut's "the exact ceiling 17 → 16"): `tests/lint/engineGatedRuleKeys.walker.test.js:1027` is the EXACT sorted-set equality `expect(Object.keys(BACKLOG_RULE_KEYS).sort()).toEqual(measuredGap)` — `measuredGap` is the measured gated-but-uncensused set, so it SHRINKS by `commonsVoiceEnabled` the moment the register entry lands, and the row at `:195` must be deleted IN THE SAME COMMIT or the equality reds from the other side (`staleBacklog`/`backlogInCensus`, `:522-524`); `:1032` is `expect(Object.keys(BACKLOG_RULE_KEYS).length).toBeLessThanOrEqual(17)` — a `≤` burn-down CEILING, not an exact count, lowered to 16 in the same commit by the file's own law ("Lower it whenever a key earns its manifest entry … never raise it", `:1030-1031`); CHECK-GIT-FIRST: IN-4's `J-INA-4` cure (PRICING §C lane 6 car 1) also deletes a row and lowers this ceiling — take the ceiling from HEAD (16 or 15), never from this brief — the three module-scope edits in `subsystemRowsVirtual.test.js`, both `contributionLedgerShape` literals (+1 this car for the commons key — READ OFF HEAD; cornering's +1 was paid at car 2 and is RE-READ at 7b), the census figures (`union` +1 for the commons key), `tests/property/commonsVoiceDormancyGolden.test.js` UNCHANGED (literal rules), a NEW LIT-shape fence `tests/property/commonsVoiceDormancyFence.test.js` (four fences + lit mutant over `advanceCommonsVoice` through `assizeKernel.js`; site census: `commonsVoiceKernel.js` the one door), and the chair informs the POP family (POP-2's `commonsArcEnabled` is NOT required — the riot rung is the kernel's own, `DESIGN_FP_TRADE.md:277-279`). 7a's witness re-recorded by hand: `rulesSha256`/`litFlagCount` on the four lit rows + `__birth_default__` (five rows); `worldStateSha256` EXPECTED to move on ALL FOUR lit rows from the commons key alone (petitions read legitimacy/corruption/unrest — a born world with an unrest reading moves) — the movers named in 7a's GOLDEN_SHIFT_LEDGER draft with its DECLARED-SHIFT sentence ("`commonsVoiceEnabled` lit on the four presets at landing `<sha>` by TR-6 7a as the riot counterforce's conjunct; the POP family informed; the BACKLOG 17 → 16; …"); a moved DARK row a STOP; census literals out of band; the edge bundles rebuilt; the espionage fence 21/21 and prose cells zero quoted. **7a Files (≈10 handwritten):** `simulationRules.js` (the register entry + the `SIM_LIT` line), `subsystemRowsCommons.js` (NEW) + `subsystemRowsVirtual.js` (the tail spread), `tests/domain/subsystemRowsVirtual.test.js`, `tests/domain/contributionLedgerShape.test.js`, `tests/soak-harness/coveringArrayCoverage.test.js`, `tests/lint/engineGatedRuleKeys.walker.test.js` (the row + the ceiling), `tests/property/commonsVoiceDormancyFence.test.js` (NEW, LIT shape), the witness golden, `docs/GOLDEN_SHIFT_LEDGER.md` (dock); regenerated: the four edge-shared files. **7a Exit:** `ENGINE_GATED_DORMANT_RULE_KEYS` lacks `commonsVoiceEnabled` (measured); `:1027` green without the row and `:1032` at the HEAD-measured ceiling; the fence LIT-shape green; the witness diff quoted; the POP notice in the report.

**7b — THE CORNER'S LIGHTING (the dark mint was car 2; this is PROTO §1.2 steps 2, 3, 10–12 for `corneringEnabled`):** `SIM_LIT` gains `corneringEnabled: true, // TR-6 car 7b`; the register entry's comment (from car 2) gains the LIT paragraph naming the four presets and this car; the four preconditions ASSERTED lit in every lit preset at 7b's tip — `merchantHousesEnabled`, `believedMarketsEnabled`, `foodCaravansEnabled` (the `requiresTradeFlags`, `:340-343`) and `commonsVoiceEnabled` (7a) — a dark one is a BOARDING STOP; `evaluateTradeFlagLighting(SIMULATION_RULE_PRESETS[id].rules).ok === true` for all seven presets, quoted, with the executed negative control `{ corneringEnabled: true }` → exactly four violations in declared order. `tests/property/corneringDormancyFence.test.js` (NEW at car 2, dark shape) → LIT shape (PROTO §3.2): "VIRTUAL, REGISTERED, LIT in exactly the four lit presets THROUGH `SIM_LIT`, absent from the three dark — the roster spelled from `SIMULATION_RULE_PRESETS`"; fence 1's dark world `quiet_local.rules`; the site census (`commercialCornerGate.js` the ONE `=== true`); fence 3's lit twin MOVES the composer's call count under EVERY lit preset (all four carry every conjunct after D0 + lanes 55/7/1 + 7a); the literal-rules conjunction arm (a rules object `{ corneringEnabled: true }` alone ⇒ zero composer calls — the corner cannot run without houses) survives as a fixture arm, never a preset arm; the truthy probe; no 64-hex literal. 7b's witness re-recorded by hand against 7a's golden (base = the consist tip after 7a): `rulesSha256`/`litFlagCount` on five rows; the corner's own `worldStateSha256` movement needs houses + belief + a spatial canon (PLAUSIBLE zero in the 52-tick birth worlds — say so honestly); 7b's GOLDEN_SHIFT_LEDGER draft with its DECLARED-SHIFT sentence ("`corneringEnabled` lit on the four presets at landing `<sha>`; the 8/16 (or 9/16) seam count; the `monopoly`/`cornered` endings mintable; …"); the count-from-HEAD figures RE-READ (R3): both `contributionLedgerShape` literals and the census figures off the composed HEAD (car 2 paid them at its own base; 7a moved them by one); TR-6's row `other` amended for LIT; the edge bundles rebuilt; the espionage fence 21/21 and prose cells zero quoted. **7b Files (≈8 handwritten):** `simulationRules.js` (the `SIM_LIT` line + comment), `tests/property/corneringDormancyFence.test.js`, the witness golden, `coveringArrayCoverage.test.js` (if moved), `contributionLedgerShape.test.js` (if HEAD moved), `subsystemRowsTrade.js` (text), `docs/GOLDEN_SHIFT_LEDGER.md` (dock); regenerated: the four edge-shared files. **7b Exit:** `ENGINE_GATED_DORMANT_RULE_KEYS` lacks `corneringEnabled` (measured); the fence LIT-shape green; the `:367` arm green with the key in the cohort; the lighting law `ok: true` ×7 quoted; the witness diff quoted; the shift draft.

**Files:** per car, as listed above (7a ≈10, 7b ≈8 — the first cut's single ≈14 is what ADDENDUM R5 split; the bundles are regenerated, the refreeze is its own commit after EACH car). **Census bills (each car):** refreeze own commit; the manifest for its fence file; its witness re-record; its shift draft. **Byte rule (each car):** build + verify:dist; the ONE `SIM_LIT` line in bytes; worker unchanged; engine ±0. **Gate (each car):** the full list + the witness, the stability test, the two freshness tests, the engine-gated walker (at 7a: `:1027` green without the row, `:1032` at the HEAD-measured ceiling), the totality walker, the espionage fence, the soak suites alone at load < 8; at 7b additionally `tests/lint/tradeConvergenceContract.walker.test.js` (the re-cut `:367` arm with `corneringEnabled` in the cohort) and the lighting law `ok: true` ×7 quoted.

---

## THE FLAG-MINT CAR (last; the count from HEAD) — READ UNDER ADDENDUM R3

Law 2 ("the flag-mint car is the LAST car of each lane, rebased at landing, its manifest count taken from HEAD") means the LIGHTING car — 7b — under ADDENDUM R3 (`…/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`). TWO mints in this lane, and they differ in shape: (1) `corneringEnabled`'s DARK MINT lands at car 2 because the gate read is the seizure seam's door (`engineGatedRuleKeys.walker.test.js:538-541` scans `src/` regardless of callers; `:496-497` reds `unaccountedReads`), and the estate's one-commit law (`subsystemRowsVirtual.js:73-81`; `:512-520` `manifestWithoutRow`; `subsystemRowsVirtual.test.js:646-647`; `contributionLedgerShape.test.js:118-119`) drags the row, the three test edits and both literals INTO car 2 with the register entry — recorded in car 2 as the one forced departure from R3's literal list; 7b is its LIGHTING and re-reads the counts. (2) `commonsVoiceEnabled` has NO early mint to split: its gate read pre-exists as a BACKLOG read (`commonsVoiceKernel.js:48-51`), so 7a is a FULL mint + lighting in ONE commit (register entry + row in the new `subsystemRowsCommons.js` tail leaf + the three test edits + both literals + the BACKLOG row deletion and ceiling + the `SIM_LIT` line + the fence + the witness + the shift draft) — the only TR LIT car of that shape. So: **at car 2**, immediately before editing: `node -e "import('./src/domain/worldPulse/simulationRules.js').then(m => console.log(m.ENGINE_GATED_VIRTUAL_RULE_KEYS.length))"` and `VIRTUAL_SUBSYSTEM_ROWS.length`; both literals = that + 1 — by this lane's boarding lanes 1/2/7/55 have moved it far past 35: NEVER inherited. **At 7a** (REBASED; composed after door D0 and lanes 55/7/1 in the chair's order) the same two commands are re-run and the literals go +1 again for the commons key, the BACKLOG ceiling is read off HEAD (16 — or 15 if IN-4's `J-INA-4` cure composed first) and lowered by one. **At 7b** (LAST) the counts are re-read and re-verified against the composed tree (no new register member; the census `governed` may move for the preset declaration). Conflicts on the register tail, the `SIM_LIT` cohort, the two literals, the census figures, the BACKLOG list and `subsystemRowsVirtual.js`'s tail spread (every lane minting a NEW family leaf appends there — order = composition order) are EXPECTED and resolved by MEASUREMENT (PROTO §5.2), never OURS/THEIRS. The lane's report states the numbers it wrote at car 2, at 7a, at 7b, and the numbers it expects at the consist.

## OWNER-GATED, KEPT OUT

- Tuning (`CORNER_TUNING` entire; MONOPOLY_DWELL; the observer-alignment consequence table) — raw + derivation, ratified at the soak.
- Gating or folding the event-grain famine sentiment table — a DM-verb behaviour shift; owned and cross-referenced instead (car 0's ruling, vetoable).
- `generosityKernel.js` 800/800 — ZERO lines; `pulseKernel.js` ZERO; ceiling raises; push/deploy/migration/deletion/spend; the golden master.
- Persisted shapes widened: NONE (positions and dwell ride TR-2's `houses` interest rows; stock rides `commodityStocks`) — recorded.

## RISKS

**Grep traps (verbatim):** `cornering` is TR-1's reason NAME (`commercialReasonTaxonomy.js:50`) — a grep for it finds TR-1's landed taxonomy, not TR-6 machinery; `corner` (78 files) is cartography/ornament GEOMETRY; `seizure`/`confiscation` are M7's caravan branches (`smuggle.js:250-260`); `monopoly` appears in institution copy; `commons` in the UI is the landing page's gallery section; `commonsVoice ≠ commonsArc` (POP-2's); `riot` in `RUNG_KIND` is the kernel's rung word, not a stressor; W-COIN is state money; `INSTR-1≠TR-1`.

**Collision surfaces:** `commodityFlow.js`/`supplyKernel.js` (SERIAL after TR-4, WY-3; beside WY-6 only if the chair boards it — both write the stock seam: CHECK-GIT-FIRST); `merchantHouseActs.js` (TR-2's — lane 55 must be ON THE CONSIST); `commercialReasons.js`'s seams registry (TR-5 car 4 adds `contract_default` — the same object literal: a textual conflict resolved by union); `marketPrices.js`/`EconomicsTab.jsx` (lane 7 car 4; lane B's W-COIN-4); the BACKLOG list (POP-7/POP-2, IN-4's `J-INA-4` row — every backlog deletion edits the same frozen object: expected conflicts, resolved by measurement); the flag-mint surfaces; `subsystemRowsVirtual.js`'s tail spread (every lane minting a NEW family leaf appends there — order = composition order).

**The addendum, by path (cite, never re-derive):** `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` — the virtuality law is amended by R1 (a `SIM_LIT` declaration is lawful; the `:367` arm is D0 car A's to re-cut — a lane that edits it collides with the door); this lane's LIT cars land after door D0 and after `merchantHousesEnabled` (lane 55 car 5), `believedMarketsEnabled` (lane 7 car 5) and `foodCaravansEnabled` (lane 1 car 6) — R2(ii) names "`merchantHousesEnabled` (lane 55) before `corneringEnabled` (lane 8)" — and 7b after 7a; R2(iii): both are built at landing on the consist, never in the dock (a dock-green 7b against dark preconditions is a FALSE GREEN); R3: the corner's dark mint (car 2) carries the register entry, the gate read, the row, the three test edits and both literals because `manifestWithoutRow` forbids a register entry without its row — 7b re-reads the counts; 7a is a full mint + lighting (a BACKLOG key has no early mint to split); R4: SP-4a (lane 55 car 0) is TAKEN — the house-actor kind this lane's corner rides on exists by the chair's declared change, vetoable by code revert; R5: 7a/7b is the plan, and the ≤ 8 rule then detaches car 6 into sitting 8b. ⚠ The addendum's `src/config/simulationRules.js` is `src/domain/worldPulse/simulationRules.js` at the consist (no `src/config/` copy exists — `ls` CONFIRMED).

**Load hazards:** `gate-mutex.sh` exits 0 after 40 polls; dist-gated suites skip without a build; `deno.lock` re-freeze; `git grep` reads the INDEX; the soak suites under load — alone at < 8; a moved DRIVEN GOLDEN (`commonsVoiceDormancyGolden` drives literal rules and must NOT move at 7a or 7b — a move means a kernel change under an absent key) is a STOP.

**Design hazards:** the denominator — never the house's own interests (the self-referential mutant is mandatory); alignment NEVER gates (both arms pinned); the seizure is a TRANSFER inside the five-term balance and converts to months only at the one applicator with TR-4's constant; the commons kernel gains NO writer and NO effect word (its header law); the corner emitter must name a cooldown; `generosityKernel.js` is at the wall — call, never add a line; the two keys light in two cars so each witness diff is attributed by construction (7b's base is 7a's golden); `EconomicsTab.jsx` 596/600; the TR-6 row in `subsystemRowsTrade.js` must not spell a FOREIGN key in a string field before that key is registered (the exact-namer-set shape of `pactFormationDormancyFence.test.js:198-214` — a future commons fence may count namers the same way).

## GATES (per car, verbatim)

focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators (none — state so) · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte (quote the worker `stat`, the `engine-*.js` line, the first-paint table) · `npm run check:quick` — every line with a PRINTED test count; the lighting refreeze on a clean tree as its OWN commit after any car adding a test file/title; the mutation manifest per new invariant file; the wiring census stamp at car 5 and at car 6 (sitting 8b); prose-numerics at car 5 and car 6 (8b); the edge bundles at cars 2, 7a and 7b; `metronomeCooldownLint` at car 6 (8b); the Seat/Lane trailer; porcelain 0. At car 2 (the dark mint) additionally the engine-gated walker (directions 1–3), `subsystemRowsVirtual.test.js`, `contributionLedgerShape.test.js`, `coveringArrayCoverage.test.js`, the two freshness tests and the totality walker. At 7a and 7b (built on the consist after door D0 — ADDENDUM R2(iii); never gated in the dock) additionally: the witness, the stability test, the two freshness tests, the engine-gated walker (at 7a the `:1026-1029` BACKLOG equality green with the row gone and the `:1032` ceiling at the HEAD-measured value), the totality walker, the espionage fence 21/21, the soak suites alone at load < 8; at 7b additionally `tests/lint/tradeConvergenceContract.walker.test.js` (the D0-re-cut `:367` arm green with `corneringEnabled` in `SIM_LIT`) and `evaluateTradeFlagLighting` `ok: true` on all seven presets QUOTED; at car 5 additionally `tests/components`.
