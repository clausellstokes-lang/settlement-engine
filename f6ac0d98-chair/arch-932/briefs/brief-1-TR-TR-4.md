# THE LANE — PRICING §C lane 1 · TR-4 THE GRAIN ROAD (`foodCaravansEnabled`; two slices; the CR-2 pin first)

Architect: the Fable seat (family TR), 2026-09-15, read-only at the consist `laneCONSIST-932` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, tip `315080928` = `a5876c0ea` + LT37's eight cars) against the ledger worktree `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a`. Every figure is **CONFIRMED** (I opened the file at the cited line, or a recon skeptic confirmed it) or **PLAUSIBLE** (reasoning only). Line numbers rot; re-measure by SYMBOL at charter. The cross-cutting protocols this brief obeys are `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS.md` (§1 the lit act · §2 the declared-shift receipt · §3 fences · §4 budgets · §5 the flag-mint car · §6 compose · §7 serialization) — cited below as PROTO §n.

**Cars: 7 (6 + the flag-mint/LIT car), range 6–8 with the repair allowance** (PRICING §B 5–7 C; HISTORY §A.4 5–7 unchanged; +1 here because the LIT act is a car of its own under the built-lit regime and the volume's slice 2 stays a car). Waves: TR-4 slice 1 (cars 2–4), slice 2 (car 5), the pre-ruled CR-2 act (car 0), the round-trip pin the record mis-assigned to WY-3 (car 1), the mint (car 3, the gate-read car) + the LIT (car 6). **⚠ CORRECTED 2026-09-15 at the skeptic's findings, SECOND PASS under the chair's `ARCH-PROTOCOLS-ADDENDUM-1.md` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`):** the first correction pass's two-preset `SIM_LIT_COMMODITY` roster is WITHDRAWN — the chair TOOK `commodityFlowEnabled`'s lighting on `dramatic_campaign` and `living_realm` as door D0 car B (ADDENDUM §3), so `foodCaravansEnabled` lights in ALL FOUR lit presets through the one `SIM_LIT` cohort and the config is VALID under `evaluateTradeFlagLighting` in every preset (car 6 item 2); the `:367` walker arm and L2 are D0 car A's and the chair's to re-cut/amend (R1), never this lane's (the first pass's "car 6 re-charters the VIRTUAL arm" is struck; PROTO §1.2 step 13 is superseded by the addendum on that point); the mint/lighting split is read under R3 with the walker's own one-commit law (car 3; THE FLAG-MINT CAR); the dependency-clause aside has a disposition (car 6 item 1); the `generosityUpdates.js` importer set is the real five (the table). The lane's lighting preconditions are stated in both directions below (R2).

**Prerequisites (all in place for cars 0–5; car 6 waits on the door):** ruling PRICING §A.4.1 (CR-2 consolidation FIRST, the queue rows win over §5 #26) and §A.4.2 (TR-4 lands BEFORE WY-3; the round-trip pin is TR-4's to LAND, WY-3 verifies) — both adopted verbatim here; the build slot at `a5876c0ea` (cut the dock with `sh $SC/mkdock.sh <name> a5876c0ea`, symlinked `node_modules`); no other lane in flight on `applyWorldPulse.js` (car 0 is the ONE lane that touches it, PROTO §7.1) or on `src/domain/spatial/commodityFlow.js` / `supplyKernel.js` (serial TR-4 → WY-3 → {TR-6, WY-6}, PROTO §7.1). Lane 2 (TR-5) may run beside this lane: the two share only the flag-mint surfaces (last car) and `treatyTransfer.js` is NOT on TR-5's file list (declared below so the chair can board both).

**THE LIGHTING PRECONDITIONS, BOTH DIRECTIONS (ADDENDUM R2 — `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`):** what this lane NEEDS lit before car 6 declares `foodCaravansEnabled`: the door train D0 on the consist — car A (the `:367` virtuality arm re-cut, the EMPTY `SIM_LIT` cohort spread in the four lit presets) and car B (`commodityFlowEnabled` lit in `dramatic_campaign` and `living_realm`, the two presets where it was dark — CONFIRMED dark at the consist: the only `commodityFlowEnabled: true` declarations are `simulationRules.js:769` and `:938`); `foodCaravansEnabled`'s row (`tradeConvergenceContract.js:330-336`) has NO `requiresTradeFlags`, so no TR lane precedes it. What car 6 UNBLOCKS: `corneringEnabled` (lane 8 — `foodCaravansEnabled` is one of its three `requiresTradeFlags`, `:337-352`) and the `grain_provision` physical arm of `tradePactsEnabled` (a declared degraded arm, `TRADE_DEGRADED_ARMS[0]` `:393-399` — CONFIRMED; never a precondition). Car 6 is BUILT AT LANDING on the consist, rebased, where D0 is visible (R2(iii)) — never in the dock against the slot's dark presets: `evaluateTradeFlagLighting(SIMULATION_RULE_PRESETS[id].rules)` must return `ok: true` for all seven presets at car 6's tip, and it cannot in a dock cut at `a5876c0ea` (two `foreign_precondition_dark` rows, `dramatic_campaign` and `living_realm`).

**Shared files this lane declares (PROTO §7.3 rule 1), with slack at the cut:** `src/domain/worldPulse/applyWorldPulse.js` 907/907 (baselined, ZERO slack — car 0 LOWERS it) · `src/domain/worldPulse/generosityUpdates.js` 90/800 · `src/domain/worldPulse/supplyKernel.js` 489/800 · `src/domain/spatial/commodityFlow.js` 448/800 · `src/domain/worldPulse/treatyTransfer.js` 49/800 · `src/domain/worldPulse/foodStockpile.js` 253/800 (READ ONLY in this lane — see car 2) · the four flag-mint surfaces (PROTO §5.1) · `tests/lint/couplingInclusion.walker.test.js` (the TRADE pattern row, car 2) · `tests/domain/pactKernelMount.test.js` untouched (that is lane 7's).

---

## THE STATE OF THE RECORD

- **The history fold.** `HISTORY-TR-2026-09-15.md` does not exist under that name; the trade family's history pass is `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/recon-32-34/HISTORY-TRADE-2026-09-15.md` (folded 19:08) and this brief adopts it as the fold that wins over PRICING §B for TR. Its TR-4 row: **UNBUILT, body lit, hole exact** — "M2/M6a deliberately exclude food from the shipment lane (`supplyKernel.js:18-19`); `commodityFlow.js` has zero `'food'|grain` hits; the caravan body it would ride is fully built and LIT (`supplyShipments.js`, `supplyKernel.js`, `commodityFlow.js:260 assertGoodsConservation`)"; grain moves today only as a treaty obligation (`treatyTransfer.js:100 computeTreatyGrainDraw`, `:129 applyTreatyFoodDeltas`, called from `peaceTerms.js:722` — I re-read the call at `:723-730`, CONFIRMED), so slice 2 re-prices as a FORK on an existing applicator. HISTORY finding (e): the volume's "no money" premise (`treatyTransfer.js:11-32`) is FALSE since W-COIN's `treasury.js` — TR-4's grain credit is re-derived below against the conserved coin: **grain stays denominated in storage-months; the treasury is STATE coin and never a grain denomination; nothing in this lane touches `treasury.js`** (ruled, vetoable).
- **The volume:** `docs/DESIGN_FP_ARCH_TR.md` §1b S14–S16 (the food lane's ground truth), §3's "Single-writer enforcement" clause (TR-4 lands the no-SIXTH-storageMonths-writer scan), §4 TR-4 (two slices; the pins list; "pulseKernel/applyWorldPulse: ZERO edits (S44)"), §5a seam 5 (the T7 granary fence), §6 Q2 (the second fold — SUPERSEDED by PRICING §A.4.1). The design law `docs/DESIGN_FP_TRADE.md` §3 SEAM RULING TWO (the granary seam, the corrected arithmetic: NO per-tick import term; the netted stash `baseDeficitPct` at `foodStockpile.js:288`; the three BUILT bypass counterforces PRESERVED; "the T7 walker asserts exactly ONE of {netted rate share, physical arrivals} live per settlement per tick, in BOTH flag states"), §5 TR-4 (the tellables T-6/T-9), §7 (GRAIN_UNITS_PER_MONTH is OWNER-SIGNED at the soak — it "recalibrates food"). The compiled programme `docs/DESIGN_FP_ARCHITECTURE.md` §5 #26 (line 1630) and the WY-3 block above it (line 1616: "the round-trip pin LANDS HERE (TR-4 verifies…)") — the WY-3 sentence is what §A.4.2 inverts; car 1 amends the SLOT copy's #26 to say "LANDED at TR-4; WY-3 verifies".
- **The queue rows that RULE car 0:** `docs/FABLE_VALIDATION_QUEUE.md:303-308` CR-2 ("Sol consolidates the two folds into the one applicator FIRST, behind a no-behavior-change pin (same-seed byte-identical pre/post) … if consolidation shifts behavior, STOP and report to the chair — a live two-writer defect is adjudicated, never silently normalized") and `docs/SOL_QUEUE.md:342-346` row 14 (same text; "the applyWorldPulse.js:322 second storageMonths fold" — the ADDRESS is rotted, the fold is at `:88-110` today).
- **The frozen pending door:** `VIRTUAL_PENDING_RULE_KEYS = Object.freeze([])` at `src/domain/certification/subsystemRowsVirtual.js:595` — the volume's Q4 ("each TR wave lands PENDING") is DEAD; the mint car AUTHORS its row.
- **The flag-dependency ruling and the walker that enforces it:** `DESIGN_FP_TRADE.md` §3 line 268 `TR-4 ⇒ \`commodityFlowEnabled\` + \`demographicsEnabled\` (grain reads harvest/K_food/storageMonths — wave-P machinery)`; `tests/lint/tradeConvergenceContract.walker.test.js:148-175` parses that clause head EXACTLY ONCE out of the document and requires `TRADE_FLAG_LIGHTING_ROWS` (`src/domain/certification/tradeConvergenceContract.js:330-336`, `requiresForeignFlags: ['commodityFlowEnabled', 'demographicsEnabled']`) to match it. **Car 6 amends BOTH in one commit** (the narrowing ruling, below).
- **The virtuality law and the lighting-order law — both AMENDED/BOUND by the chair's addendum (`…/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`, CONFIRMED at source by the skeptic):** (F1) the walker's arm at `:367` ("every TRADE flag is VIRTUAL — absent from the defaults and from every preset spread"; `PRESET_RULE_KEYS` `:141-144` is built from `Object.keys(preset.rules)`, so a `...SIM_LIT` spread IS a preset key) and L2 (`docs/DESIGN_FP_ARCHITECTURE.md:92`) would red and contradict ANY lit FP key — **R1 amends L2 and D0 car A re-cuts the `:367` arm** (absent from the defaults · present in a preset spread ONLY through `SIM_LIT` · every `SIM_LIT` key has a register entry and a row); this lane inherits the re-cut and never edits `:367` itself. (F2) `evaluateTradeFlagLighting` `:493-525` reds a lit row whose `requiresForeignFlags` member is dark in the same rules object (`foreign_precondition_dark`) — **R2 binds the boarding order: D0 car B lights `commodityFlowEnabled` in `dramatic_campaign` and `living_realm` BEFORE car 6 declares `foodCaravansEnabled`**, so the four-preset declaration is a VALID config by the estate's own executed law, and the "dormant by conjunction" phrasing this brief carried (which has no arm in the evaluator — its only kinds are `trade_precondition_dark`/`foreign_precondition_dark`; `DORMANT_BY_CONFIG` is `TRADE_FLAG_CERTIFICATION_VERDICTS`' word for an OBSERVATION row, `:149-154`) is STRUCK. (R3) the mint/lighting split is read below at car 3 and THE FLAG-MINT CAR.
- **The owner's laws that bind:** everything built lands LIT (16:5x) until the terminal soak; the tuning values land raw with an executed derivation and are ratified only at the soak under THE PROMISE (GRAIN_UNITS_PER_MONTH, the calm-equivalence tolerance, the surplus-export bands, the robbery-claim decay); nothing pushes, deploys, applies a migration, deletes data or spends.

## THE STATE OF THE CODE (read at the consist; CONFIRMED unless marked)

| Module | eff/ceiling (eslint `max-lines`, measured) | What is there |
|---|---|---|
| `src/domain/worldPulse/applyWorldPulse.js` | **907/907** (baselined `scripts/.size-baseline.json`) | `applyFoodStockpileOutcomeToSettlement` at `:88-110`: the SECOND `storageMonths` fold ("target loses, victor gains"), clamps `[0, storageCapacityMonths]`, rounds to the tenth-month — the SAME arithmetic as the applicator's, spelled twice. Imports `storageCapacityMonths` from `./foodStockpile.js` (line 14 of the import block) and NOT `generosityUpdates.js`. The head keeps this function by pin (R-BLD-4 writer-family reading; see the baseline's `_decomposition_wave_war_2026_08_03` key: "applyOutcomeToSettlement + applyFoodStockpileOutcomeToSettlement, which call [reEmbedPrimaryDeity]") — so car 0 consolidates the BODY, not the call site |
| `src/domain/worldPulse/generosityUpdates.js` | 90/800 | `applyFoodDeltasToUpdates(updates, updateIndex, foodDeltas)` at `:46-70` — THE ONE DELTA APPLICATOR (`treatyTransfer.js:26-32` names it so). **FIVE importers, CONFIRMED by `grep -rn "from './generosityUpdates.js'" src`:** `generosityKernel.js:129` (`applyFoodDeltasToUpdates`, `applyLegitimacyDeltasToUpdates`, `applyProsperityDeltasToUpdates`), `treatyTransfer.js:49` (`applyFoodDeltasToUpdates`), `realmVerbExecution.js:61`, `treasury.js:87`, `sovereigntyTransfer.js:80` (each `applyLegitimacyDeltasToUpdates` only) — the `applyFoodDeltasToUpdates` EXPORT alone has the two importers the first draft named; `pulseKernel.js:1823` and `magicBufferApply.js:18` name the module in COMMENTS only. Its own imports: `./foodStockpile.js` (`storageCapacityMonths`, `:17`) and `../../data/constants.js` (`:18`) |
| `src/domain/worldPulse/foodStockpile.js` | 253/800 | the generation stash `baseDeficitPct` at `:288` (GENERATION IS SACRED — never rewritten); `_bypassShare` `:314-316` (teleport 0.3 / airshipBesieged 0.15 from `FOOD_IMPORT_RATES`), `_underwaysShare` `:320` (`UNDERWAYS_TUNING.FOOD_TRICKLE`), `_channelShare` `:321`, `blockadePct = max(0, importDependency − _channelShare)·100` `:322-324`; the six-term `effectiveDeficit` at `:346-349`; writes `storageMonths` at `:406` and `:479`; `computeSackFoodTransfer` `:514` (the sink-only primitive) |
| `src/domain/worldPulse/supplyKernel.js` | 489/800 | header `:18-19` "FOOD is deliberately EXCLUDED (category 'food')"; `advanceCommodityContinuity` `:411-500` — builds `links` via `deriveConsumingLinks` (`:155`), `producers` via `buildProducers` (`:371`), calls `advanceCommodityFlow` (`:479-482`), asserts conservation (`:486-488`), writes stocks back to `spatialLedgers.commodityStocks`; `commodityFlowActive` asserted by `advanceSettlementSupply` upstream (`:459-462` comment). THE MOUNT SEAM for car 3 |
| `src/domain/spatial/commodityFlow.js` | 448/800 | `commodityFlowActive` `:169-174` (marker + `commodityFlowEnabled === true`); `assertGoodsConservation` `:260-270` (before + produced == after + consumed + lost, integers); `advanceCommodityFlow` `:441-…`, the arrive loop `:545-…` reconstructs each shipment record by hand (`:548-562`: the M7 comment "a multi-tick smuggle run loses smuggle:true on reconstruction" — the exact field-loss class car 1 pins); ledgers `commodityStocks` + `supplyShipments` (`:451-456`), `merchantAppetite` + `dispatchWillingness` (`:460-463`); `MAX_SHIP: 12`, `MIN_SHIP: 2` `:117-118` |
| `src/domain/spatial/supplyShipments.js` | 210/800 | M2 K-ranked producers, starvation latch, `routeIntercepted` `:209` |
| `src/domain/worldPulse/treatyTransfer.js` | 49/800 | `computeTreatyGrainDraw` `:100-121` (reserve floor 1.5 months, CAPTURE 0.6 — `TREATY_TRANSFER_TUNING`), `applyTreatyFoodDeltas` `:129-138`, `freshestSettlement` `:148-156`; consumed by `peaceTerms.js:723-730` (the stream loop) and `warCoalitionSettlement.js` (HISTORY) |
| `src/data/foodImportRates.js` | 23 lines | `FOOD_IMPORT_RATES` teleport 0.3 · airship 0.3 · airshipBesieged 0.15 · minorRoutes 0.08 · minorRoutesVillage 0.05 — ⛔ IN THE GENERATION WORKER GRAPH (`recon-32-34/worker-graph.txt:6`); READ, never edited |
| `src/domain/region/goodsCatalog.js` | 641 lines | `grain` canonical at `:30-38`, `category: 'food'`, `criticality: 0.95`, twelve aliases — ⛔ worker graph (`worker-graph.txt:74`); no edit |
| `src/generators/economy/economicState.js` | 791/800 | ⛔ worker graph; no edit |
| the other `storageMonths` writers (the census set, six expressions in five modules) | — | `foodStockpile.js:406`, `:479`; `generosityUpdates.js:65`; `applyWorldPulse.js:106`; `magicBufferApply.js:357` (`src/domain/worldPulse/`, not `magic/`); `mutateWorld.js:1153` (`src/domain/events/`) — re-measured by `grep -rn "storageMonths" src --include='*.js' \| grep -E "storageMonths: (next|leg|round2)"` |
| `src/domain/worldPulse/simulationRules.js` | 352 eff | `commodityFlowEnabled: true` at `:769` (`realistic_regional`, THE DEFAULT) and `:938` (`full_simulation`) ONLY; `dramatic_campaign` `:781-783` "commodityFlowEnabled stays OPT-IN (not lit here) per owner design"; `living_realm` `:848-866` does not declare it; `demographicsEnabled: false` at `:1027` in `full_simulation` with the comment "LIGHTING IT belongs at the owner-signed soak redo" (`:1013-1026`) and ABSENT from every other preset — dark everywhere; the register `ENGINE_GATED_VIRTUAL_RULE_KEYS` `:230-…` (35 members, all DORMANT: `ENGINE_GATED_DORMANT_RULE_KEYS` `:1141` = the register); `WAVES` `:644-654`, `ONE_REGEN` `:664-674`, spread at `:771-772`, `:822-824`, `:864-865`, `:1072-1073` |
| `src/domain/certification/tradeConvergenceContract.js` | 398 eff | `TRADE_FLAG_LIGHTING_ROWS` `:305-363`; TR-4's row `:330-336` |
| the tests that already exist and bind | — | `tests/domain/commodityFlow.test.js`, `commodityFlow.kernel.integration.test.js`, `supplyShipments*.test.js`, `foodStockpile.test.js`, `generosityKernel.zeroGrainGift.test.js`, `peaceTerms.test.js` (streams); `tests/lint/couplingInclusion.walker.test.js:180-184` the TRADE `LAYER_PATTERNS` (`routeNetwork|tradeRoute|tradeWar|commodity|merchant|foodStockpile|foodLedger`, `commercial[A-Z]`, and the spatial set) — a leaf named `grain*.js` under `worldPulse/` matches NO pattern and reds the walker as unlayered; `tests/domain/metronomeCooldownLint.test.js` (a NEW worldPulse module with `candidateType:` must name a cooldown) |
| ABSENT | — | `grainFlow.js`, `grainArrivalCredit.js`, `foodCaravansEnabled` (zero hits in src and tests), any storageMonths writer census, any commodityStocks/supplyShipments round-trip pin |

**Budgets this lane meets (PROTO §4.1):** the generation worker 1,404,493 B EXACTLY at ceiling — none of this lane's files is in the worker graph (the three tripwires above are read-only), so the worker `stat` must not move by ONE byte; the lazy `engine` chunk (< 676,000, ~677 B slack) — worldPulse leaves ride the advance worker, not the `engine` chunk, so the delta is expected 0 and is QUOTED from the build; the eager first-paint closure — only the mint car's `SIM_LIT` line touches the eager store slice (≈30–60 B, PLAUSIBLE), quoted from the build; the margin is CONTESTED (A0 measured 5,828 B where PRICING quotes 795 B) so every src car quotes the figure regardless.

## ALREADY DONE (do not re-land)

- The whole caravan body, LIT: M2 (`67a946616`) + M6a (`a664d15bc`) + M6b/M6c/M6d/M7 — HISTORY §A.3 rows 1–7; `commodityFlowEnabled` lit in the default preset at ODQ §903 (HISTORY row 31).
- Treaty grain as a conserved stream through the ONE applicator (`treatyTransfer.js`, `peaceTerms.js:715-730`) — slice 2 forks this, never re-lands it.
- Grain's catalog row and the food ledger's three bypass counterforces (S14).
- TR-1 whole (`d7ea69a4b` + `93c118b6d`) and TR-9c's contract (`c7933e845`) with TR-4's row at `tradeConvergenceContract.js:330-336`.

---

## THE CARS

Every car: one gated commit an Opus implementer lands in one sitting; `// anchored:` on every absence assertion; every load-bearing conjunction gets an EXECUTED mutant with a `scripts/mutation-coverage-manifest.json` entry (`kind:'mutation'` + the `check_caught*` plant in `scripts/mutation-sweep.sh`); every authored file byte-scanned for NUL (the Write-tool class); porcelain 0; the Seat/Lane trailer (`Seat: Opus 5 — Fable-unvalidated`, `Lane: TR-4`). PACKET_STANDARD caps bind per car (≤1 behaviour family · ≤1 new persisted record family · ≤1 user-facing surface · ≤2 leaves ≤250 eff · ≤12 handwritten files · delta in a shared/hot file ≤15).

### Car 0 — CR-2: the second `storageMonths` fold consolidated into the one applicator, behind a byte-identical pin, LOWERING `applyWorldPulse.js`

**In plain words:** two places in the engine add or subtract granary months with the same clamp-and-round arithmetic spelled twice; this car makes them one function, proves the world comes out byte-for-byte the same, and banks the smaller file size so no later wave can grow it back.

**The change.** Extract the arithmetic `nextMonths = round(max(0, min(cap, storageMonths + delta)) · 10) / 10` into ONE exported primitive `applyStorageMonthsDelta(settlement, deltaMonths)` in `generosityUpdates.js` (returns the settlement by reference when nothing moves — the applicator's own idiom); `applyFoodDeltasToUpdates` calls it; `applyWorldPulse.js`'s `applyFoodStockpileOutcomeToSettlement` keeps its NAME and its outcome-summing head (`:93-97`) and delegates the write to the primitive — the head stays where R-BLD-4 pinned it, only the duplicated body leaves. `applyWorldPulse.js` gains one import symbol on an existing line if `generosityUpdates.js` is not yet imported there (it is not: add ONE import line) and loses ~9 body lines: NET SHRINK, measured with the enforcer's own Linter (`node $MY/arch-932/eff-any.mjs "$DOCK" src/domain/worldPulse/applyWorldPulse.js`), and the baseline entry `"src/domain/worldPulse/applyWorldPulse.js": 907` is LOWERED to the new figure in the same commit with a dated `_cr2_…` rationale key (the `_substrate_coupling…` key at the baseline is the form). ⛔ Import-cycle check before the edit, stated over the REAL importer set: `applyWorldPulse.js` becomes the SIXTH importer of `generosityUpdates.js` (after `generosityKernel.js:129`, `treatyTransfer.js:49`, `realmVerbExecution.js:61`, `treasury.js:87`, `sovereigntyTransfer.js:80`); a cycle needs a path FROM `generosityUpdates.js` back to `applyWorldPulse.js`, and `generosityUpdates.js` imports only `./foodStockpile.js` (`:17`) and `../../data/constants.js` (`:18`) — so the check is: `foodStockpile.js`'s and `constants.js`'s transitive import closures must not contain `applyWorldPulse.js` (PLAUSIBLE they do not — `applyWorldPulse.js` already imports `foodStockpile.js`, so a back-edge would be a cycle TODAY; assert it anyway with `npx madge --circular src/domain/worldPulse/applyWorldPulse.js` or the estate's `tests/lint` cycle walker, and quote the output). The five existing importers are unaffected by the edit (none imports the new primitive; `applyLegitimacyDeltasToUpdates` is untouched).

**Files:** `src/domain/worldPulse/generosityUpdates.js` (+~12), `src/domain/worldPulse/applyWorldPulse.js` (−~8 net), `scripts/.size-baseline.json` (the number + the key), `tests/lint/storageMonthsWriters.walker.test.js` (NEW — the census), `tests/domain/generosityUpdates.primitive.test.js` (NEW — the equivalence pin), `docs/DESIGN_FP_ARCHITECTURE.md` (dock copy: §5 #26's last sentence amended to cite CR-2 — "handled per CR-2 (`FABLE_VALIDATION_QUEUE.md` CR-2 / `SOL_QUEUE.md` row 14): the two folds CONSOLIDATED at TR-4 car 0 behind a byte-identical pin; Q9's REPORTED-NOT-DEFECT is superseded" — amended, never deleted; the ledger copy is the chair's).

**Pins and negative controls.**
1. THE EQUIVALENCE PIN: over 500 seeded `(storageMonths, cap, delta)` triples spanning negatives, zeros, sub-tenth deltas and over-cap sums, the OLD inline expression (kept verbatim in the test as a fixture function, never in src) and the primitive agree exactly, INCLUDING the "no change ⇒ same reference" identity. Negative control: mutate the primitive's rounding (`* 10) / 10` → `* 100) / 100`) → the pin reds on the sub-tenth cases (executed, then restored by `cp` backup + `cmp`, never a checkout-family command).
2. THE SAME-SEED IDENTITY: the preset-lighting witness `tests/simulation/presetLightingWitness.test.js` is GREEN with `tests/fixtures/preset-lighting-witness-golden.json` UNTOUCHED (all eight rows' `worldStateSha256` / `settlementUpdatesSha256` byte-identical — the executed pulse-side identity receipt); plus `tests/domain/conquestExecutionWr8.test.js` and `tests/domain/warDeployment.test.js` (the sack path that feeds `foodStockpileDeltas`) green unchanged; plus every `tests/property/*DormancyGolden.test.js` green with no fixture touched. **THE STOP CLAUSE (verbatim from the queue rows): if consolidation shifts a single byte on any of these, the lane STOPS and reports to the chair — a live two-writer defect is adjudicated, never normalized.**
3. THE CENSUS (`storageMonthsWriters.walker.test.js`): a source scan over `src/` for `storageMonths:` write expressions (the blanker from `tests/lint/engineGatedRuleKeys.walker.test.js`'s `codeOnly` so a comment cannot count), asserting the EXACT measured set after the move — expected FOUR modules / FIVE expressions: `foodStockpile.js` ×2, `generosityUpdates.js` ×1 (the primitive), `magicBufferApply.js` ×1, `mutateWorld.js` ×1 — SHRINK-ONLY, with an EXECUTED sixth-writer plant (a `storageMonths: x` write planted in a throwaway module under `src/domain/worldPulse/` → the census reds; restored md5-exact). Anchored: the census's own positive control is that it finds `foodStockpile.js:406`.

**Census bills:** mutation manifest entries for both new test files (the planted rounding mutant; the sixth-writer plant); the lighting-census refreeze as its OWN commit on a clean tree after this car (two new test files → `files`/`titles` move); no prose byte, no wiring census; no simulationRules byte, no edge bundle.

**Byte rule:** `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist`; quote `stat -f %z dist/assets/generation.worker-*.js` (= 1,404,493, unchanged), the `engine-*.js` line, the first-paint table. Expected: worker unchanged; engine unchanged; closure ±0 (both files are lazy).

**Gate:** focused (`tests/domain/generosityUpdates.primitive.test.js`, `tests/lint/storageMonthsWriters.walker.test.js`, `tests/lint/sizeBaseline.test.js`, `tests/simulation/presetLightingWitness.test.js`, `tests/domain/conquestExecutionWr8.test.js`, `tests/domain/warDeployment.test.js`) · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · `npm run build && npm run verify:dist` (mutex) · check:quick.

**LIT act:** none (no flag yet). **Exit criterion:** the baseline number is lower than 907, the witness golden is untouched and green, the census reds a planted sixth writer, and the STOP clause was NOT triggered (or the lane stopped and the chair holds the report).

### Car 1 — the commodityStocks / supplyShipments serialize → regenerate → undo → import round-trip pin (LANDED here; WY-3 verifies)

**In plain words:** the caravan ledgers are saved, reloaded, undone and imported by machinery nobody has ever proved keeps every field of an in-flight caravan; this car proves it before grain rides those caravans.

**The change.** Test-only (no src byte). A fixture world with ≥2 live shipments, one multi-tick and one a SMUGGLE run (`smuggle: true`, `ranDanger` stamped) plus a `merchantAppetite`/`dispatchWillingness` row, driven THROUGH at least one in-transit tick of `advanceCommodityContinuity` so the arrive-loop rebuild at `commodityFlow.js:545-562` is inside the loop; then (a) `JSON.parse(JSON.stringify(worldState))` byte-identical; (b) the store's undo path (the `tests/store/proposalUndoRing.test.js` idiom — the pulse ring restores the prior worldState's `spatialLedgers`) restores every ledger key; (c) the import normalizer (`ensureWorldState` / the campaign import path — locate by symbol `normalizeWorldState` at charter) preserves the closed key set of a `CommodityShipment` (`commodityFlow.js:298-334` typedef: institutionId · settlementId · input · sourceId · arrivalTick · carried · starving · ranDanger? · smuggle? · smuggleBranch? · smuggleGateId? · seizedTake? · smuggleDispatched?) — enumerated as a CLOSED set the test reads off the typedef's own text so a field added to the shape without a pin reds; (d) REGENERATE under THE PROMISE: a settlement regen (`regenSection` path, `src/store/settlementGenerateAction.js`) leaves `spatialLedgers.commodityStocks` and `supplyShipments` untouched (world ledgers are not per-settlement generation output).

**Pins and negative controls:** the field-drop plant — delete the `if (raw.smuggle) rec.smuggle = true;` line at `commodityFlow.js:562` in a throwaway copy → arm (a)-after-tick reds (the exact loss class the in-file comment names); restore md5-exact. Anchored: the shape enumeration's positive control asserts `carried` survives.

**Files:** `tests/domain/commodityFlow.roundTrip.test.js` (NEW), `docs/DESIGN_FP_ARCHITECTURE.md` (dock copy: #26 and the WY-3 block amended — "LANDED at TR-4 car 1 (§932); WY-3 verifies and extends it to the caravan-body fields, never re-lands it" — the §A.4.2 inversion recorded in the compiled programme; both sentences amended, neither deleted).

**Census bills:** mutation manifest (the field-drop plant); lighting refreeze own commit. **Byte rule:** none (no src byte; state so). **Gate:** focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · check:quick. **LIT act:** none. **Exit:** the four arms green; the plant reds; the compiled programme's two sentences read the inverted order.

### Car 2 — the two dark leaves: `grainFlow.js` + `grainArrivalCredit.js`, zero callers

**In plain words:** the grain road's two pure pieces — how much grain a town can spare, and how arriving grain becomes granary months — land as functions nothing calls yet, fully tested, so the mount and the pins in the next cars attach to something proven.

**The change.** `src/domain/worldPulse/grainFlow.js` (≤250 eff): `foodCaravansActive(rules)` — THE ONE by-name strict read `rules.foodCaravansEnabled === true` (the door lives here; nothing else in the tree may spell the key); `grainSurplusUnitsOf(settlement, ledger)` — the surplus derivation from the SAME `foodLedger`/`foodStockpile` truth (one source, two denominations: `surplusPct` and `storageMonths` above the reserve → units via `GRAIN_TUNING.GRAIN_UNITS_PER_MONTH`); `grainProducersFrom(snapshot, localSettlements)` and `grainLinksFrom(...)` — producers/links in `commodityFlow`'s `CommodityProducer`/`CommodityLink` shapes for the good id `grain` ONLY (the catalog's canonical id; every alias folds through `normalizeGood`), each producer's `rate` from the surplus, `cap` from `originStockCap`; the seller-posture export appetite as ONE line (Req 13: an insular seat holds grain at fair — priced by the existing `riskToleranceFromAlignment`/posture read, receipted, never forbidden). `src/domain/worldPulse/grainArrivalCredit.js` (≤150 eff): `GRAIN_TUNING = Object.freeze({ GRAIN_UNITS_PER_MONTH, CALM_EQUIVALENCE_BAND, SURPLUS_EXPORT_FLOOR_MONTHS, ROBBERY_CLAIM_DECAY_TICKS })` — every value RAW with an executed derivation in the docblock (units per month derived from `COMMODITY_TUNING.STOCKPILE_TARGET` × a population-per-month ratio measured over the 525 golden configs — quote the measurement; ratified only at the soak); `grainArrivalMonthDeltas(arrivals)` → `Map<settlementId, +months>` (units ÷ constant, floored to the tenth so credit never exceeds units); `grainOffsetMonthDeltas(settlement, arrivalsThisTick)` — THE EQUAL-AND-OPPOSITE OFFSET: the lit-only netting of the stash's import share, computed FROM `fs.stockpile.baseDeficitPct` and `ledger.importDependency` (READ, never written), returned as a NEGATIVE delta so both terms pass through the ONE applicator in car 3. ⛔ `foodStockpile.js` takes ZERO edits (the volume's "the offset term joins the :347 sum" is superseded by the ONE-DOOR law: a second by-name read of the flag inside `foodStockpile.js` would be a second door; the offset is a delta at the applicator — ruled, vetoable).

**The layer-pattern bill (the trap):** `tests/lint/couplingInclusion.walker.test.js:180-184` — add `grain[A-Z]` to the TRADE `LAYER_PATTERNS` in THIS commit with a comment on the CR-FP-11 arm-A precedent (`:181-183`), or both leaves red the walker as unlayered. A pattern names a family, not a file.

**Pins:** unit tests per export (surplus is zero at or under the reserve floor; a settlement with no food ledger yields no producer; units→months floors; the offset is ≤ 0 and zero when nothing arrived); `tests/domain/metronomeCooldownLint.test.js` is untouched because neither leaf builds a `candidateType` (assert it in the leaf's header). Negative control: the derivation pin — change `GRAIN_UNITS_PER_MONTH` by one → the units→months unit test reds.

**Files:** the two leaves, `tests/domain/grainFlow.test.js`, `tests/domain/grainArrivalCredit.test.js`, `tests/lint/couplingInclusion.walker.test.js` (one pattern line). **Census bills:** mutation manifest (the derivation mutant); refreeze own commit; no prose. **Byte rule:** build + verify:dist quoted; worker unchanged (the constant lives HERE, ⛔ never in `foodImportRates.js` / `goodsCatalog.js` / `tradeGoodsData.js`); engine unchanged (zero importers); closure ±0. **Gate:** the full list. **LIT act:** none (WR-10 dark-instrument shape — `espionageDormancyFence.test.js:16-19` precedent). **Exit:** both leaves green and imported by nothing in `src/` (assert with an importer census in the test).

### Car 3 — the mount inside `supplyKernel.js` + the T7 walker + the sixth-writer plant

**In plain words:** grain joins the caravan traffic when the flag is on — the food exclusion is lifted for grain only, arrivals become granary months through the one door, and a permanent test proves that each town is fed by EITHER the old netted rate OR real wagons, never both.

**The change.** In `advanceCommodityContinuity` (`supplyKernel.js:411`): after `links`/`producers` are built (`:436`), `if (foodCaravansActive(worldState.simulationRules)) { links.push(...grainLinksFrom(...)); producers.push(...grainProducersFrom(...)); }` — own-flag-before-host-gate (the host is already inside `commodityFlowActive`); after `out.arrivals` is known, `const grainDeltas = foodCaravansActive(...) ? mergeDeltas(grainArrivalMonthDeltas(out.arrivals), grainOffsetMonthDeltas(...)) : null;` folded onto `settlementUpdates` through `applyFoodDeltasToUpdates` (imported from `generosityUpdates.js`) — the ONE applicator; the `changed` vote carries it. `deriveConsumingLinks`' food exclusion (`:18-19`, `:155`) is NOT edited: grain links are ADDED by the leaf, so the exclusion stays true for every other food good and the dark path is byte-identical by construction. ≤12 lines in `supplyKernel.js` (489 → ≤501; delta ≤15). `pulseKernel.js` and `applyWorldPulse.js`: ZERO edits.

**The T7 walker** (`tests/lint/grainRoadRegime.walker.test.js`, NEW): EXACTLY ONE of {the netted rate share, physical arrivals} is live per settlement per tick, in BOTH flag states, forever — shaped as a ROUTED CENSUS over the grep-derived `importDependency` consumer set of the food ledger (measured at HEAD: `src/domain/foodLedger.js`, `worldPulse/demographicsRates.js` (`foodCapacityOf` `:548`), `worldPulse/foodStockpile.js`, `worldPulse/magicSubstitution.js` (`substitutedImportDependency` `:484`), `worldPulse/tierResourceDynamics.js` (the same-name LOCAL, excluded by symbol-home), `generators/foodGenerator.js`), SHRINK-ONLY, NEVER a count-of-N row (the compiled programme's correction at line 1620-1624) — plus the executed arm: drive a fixture with a live importer in both flag states and assert dark ⇒ `blockadePct` arithmetic unchanged and no grain deltas; lit ⇒ grain deltas present and the offset present with them (the exactly-one property is that the OFFSET accompanies every arrival credit — a credit without its offset is the double-count). The storageMonths census from car 0 gains the grain applicator route as an admitted CALL SITE (not a new writer: the primitive is the writer) and re-runs its sixth-writer plant.

**Pins / negatives:** seeded positive control FIRST (a live importer exists on the fixture, so exactly-one is non-vacuous); a settlement with zero imports proves the walker still sees it; the both-arms-live plant (credit arrivals AND skip the offset) → the walker reds; the dark run is BYTE-IDENTICAL to the pre-car world on the fixture (object identity of `worldState` when nothing else moves — the `pactKernelMount` idiom).

**Files:** `supplyKernel.js`, `tests/lint/grainRoadRegime.walker.test.js`, `tests/lint/storageMonthsWriters.walker.test.js` (the call-site row), `tests/domain/supplyKernel.grain.test.js` (NEW). **Census bills:** mutation manifest (both-arms plant); refreeze own commit. **Byte rule:** build + verify:dist quoted; worker unchanged; engine unchanged; closure ±0 (supplyKernel is lazy). **Gate:** the full list (tests/property includes every DormancyGolden — they drive literal rules with the flag absent, so none may move; a moved fixture is a STOP). **LIT act:** none yet — the flag is read by name in `grainFlow.js` (the gate read lands in car 2's leaf, but the leaf has zero callers until this car; the walker's `scanGateReads` reads `src/` regardless of callers, `engineGatedRuleKeys.walker.test.js:538-541`) and is NOT declared until car 6. **THE MINT/LIGHTING SPLIT, RULED UNDER ADDENDUM R3 AND THE WALKER'S OWN ONE-COMMIT LAW (vetoable):** R3 lets the DARK MINT (the register entry + the gate read) land early and puts the LIGHTING last. Applied to this lane: **car 3 is the dark-mint car** — and the estate's guards make the dark mint LARGER than R3's two items, because a register entry without its row reds `manifestWithoutRow` (direction 3, `engineGatedRuleKeys.walker.test.js:512-520`; the pending door `VIRTUAL_PENDING_RULE_KEYS` is `[]` and pinned `toEqual([])` at `subsystemRowsVirtual.test.js:647`), a row without its `VIRTUAL_RULES` position reds the ordered equality (`:646`), and both moved lengths red `contributionLedgerShape.test.js:118-119` (`toHaveLength(35)` ×2); `coveringArrayCoverage.test.js:185-222`'s `union` counts register members; and ANY `simulationRules.js` byte stales the two edge bundles (PROTO §1.2 step 9). So car 3 pays, in ONE commit: the register entry (tail, DARK — the key in NO preset), the gate read (already in `grainFlow.js`), the AUTHORED row in `subsystemRowsTrade.js` (a NEW tail leaf spread at the TAIL of `VIRTUAL_SUBSYSTEM_ROWS` — CHECK-GIT-FIRST: lane 2 may have created it; then append), the three `subsystemRowsVirtual.test.js` edits, BOTH `contributionLedgerShape` literals READ OFF HEAD at car 3's base, the covering-array `union` re-measured out of band, the four edge-shared files rebuilt. Car 6 (LAST, REBASED at landing) pays the LIGHTING (the `SIM_LIT` line, the LIT-shape fence, the witness, the shift draft, the LIT comment, the row's `other` text) and **RE-READS both literals and the census figures off HEAD** — they move at car 6 only if another lane's dark mint composed between (the compose re-measures regardless, PROTO §5.2). This is R3 read as far as the walker allows; the one departure from R3's literal list (row + three edits + literals at the mint car rather than the last car) is FORCED by `manifestWithoutRow` and is recorded here for the chair. **Exit:** lit fixture moves grain; dark fixture byte-identical; the T7 walker reds its plant; the register carries `foodCaravansEnabled` with an AUTHORED row in a NEW tail leaf `src/domain/certification/subsystemRowsTrade.js` spread at the TAIL of `VIRTUAL_SUBSYSTEM_ROWS` (CHECK-GIT-FIRST: lane 2 may have created the file — then append).

### Car 4 — calm-equivalence + the three bypass arms + conservation + belief-cannot-starve + the T-6 tellable

**In plain words:** the proofs that lighting grain does not change a peaceful world by more than the disclosed band, that a besieged town with a teleport circle or an airship dock or tunnels still gets its trickle as real wagons, that grain is neither created nor destroyed, that rumours cannot starve anyone, and that a siege tells its story week by week.

**The change (tests + the tuning table's derivation text; ≤3 src lines if a bypass arm needs a route hint):** (1) THE CALM-EQUIVALENCE PIN — on a calm corpus (no blockade, no famine; ≥12 settlements from the 525 golden configs driven 52 ticks), lit `storageMonths` trajectories land within `GRAIN_TUNING.CALM_EQUIVALENCE_BAND` of the dark trajectories, per settlement per tick — the disclosed, BOUNDED lit-path shift; the band is a raw constant with its derivation. (2) THE BYPASS-PRESERVATION PIN, three arms EACH executed: a besieged settlement with `resolveBlockadeBypassChannel === 'teleport'` receives physical grain at the 0.3 share, `'airship'` at 0.15, an underways town at `UNDERWAYS_TUNING.FOOD_TRICKLE` — each arm asserting the physical arrivals' month-credit equals (within the tenth) `_bypassShare × importDependency × months` while the dark arithmetic's `blockadePct` term is offset — the rates READ from `foodImportRates.js`, never re-typed. (3) Goods conservation with grain rows: `assertGoodsConservation` holds every tick of a 20-tick grain run (J-TR-8, "for free" — but asserted). (4) Grain-months conservation at the applicator: Σ credited months × constant ≤ Σ arrived units (floors). (5) THE BELIEF-CANNOT-STARVE NEGATIVE: a believed shortage (a belief record with `scarcityBands.food: 'scant'`, SP-B) moves no physical grain and no `storageMonths` under either flag state. (6) T-6 TELLABLE: a siege fixture — roads closed → arrivals stop → the granary falls on the food ledger's own clock (`monthsForInterval`) to the physical trickle floor → the bells (the existing famine/blockade receipts) — pinned week by week, plus its open-route negative (same fixture, siege lifted at tick 5 → arrivals resume within `arrivalTick` weeks). Three mutants, all manifest-rowed: rate-share-not-offset (the offset deleted → calm-equivalence reds, the double-count made visible), bypass-arm-severed (teleport arm → 0 → arm 1 reds), applicator-bypassed (a direct `storageMonths` write → the car-0 census reds).

**Files:** `tests/property/grainRoadCalmEquivalence.test.js`, `tests/domain/grainRoadBypass.test.js`, `tests/domain/grainRoadTellables.test.js` (NEW ×3); `grainArrivalCredit.js` (the band's derivation text only). **Census bills:** mutation manifest ×3; refreeze own commit; prose-numerics untouched (no reader prose — the receipts are the existing famine/blockade kinds). **Byte rule:** build quoted if any src byte moved; else state "no src byte". **Gate:** the full list. **LIT act:** none. **Exit:** all six pins green in both flag states where applicable; the three mutants red.

### Car 5 — slice 2: treaty streams ride caravans when lit; the robbery claim

**In plain words:** when the grain road is on, a treaty's grain tribute travels as real wagons that can be robbed on the road instead of appearing instantly in the victor's granary; when it is off, nothing changes.

**The change.** In `treatyTransfer.js` (49 → ≤120 eff; a leaf of the peaceTerms family — `peaceTerms.js` at 797/800 takes ZERO edits and is NOT on this lane's file list; the fork is INSIDE `applyTreatyFoodDeltas` / a new `dispatchTreatyGrain` the head already reaches through `computeTreatyGrainDraw`'s return): lit ⇒ the installment's `lostMonths` leaves the payer immediately (the debit is real) and the `gainedMonths` becomes a grain SHIPMENT record in `supplyShipments` keyed to the treaty (`sourceId` payer, `settlementId` payee, `input: 'grain'`, `carried` units, `arrivalTick` from the digest's hop weeks — the M6a record shape from car 1's closed set, no new field), credited at arrival by car 3's route; dark ⇒ the existing instantaneous applicator path, byte-identical. The ROBBERY CLAIM: a treaty caravan cut or seized mid-route (`shipmentCut` / the M7 seizure branch) mints a claim on the I2 reparations-claim shape (`ransomClaim.js:14` names the shape: "the I2 reparations-claim shape" — locate the claim family's writer by symbol at charter) with the creditor's-picture fork (J-TR-6: the victor's BELIEVED delivery vs the true loss — the K3 fog gap), decaying by `GRAIN_TUNING.ROBBERY_CLAIM_DECAY_TICKS`; no new ledger key — the claim rides the existing family (≤1 persisted family per car: ZERO new). `grain_provision` (TR-5's term) is NOT this car's: when TR-5 lands its row it streams through the same fork automatically (the fork keys on `spec.stream`, not on the term type) — declared, with a pre-pin in this car that any `family: 'commercial'` stream term reaches `computeTreatyGrainDraw` (reds the day TR-5 lands a stream row that bypasses it).

**Pins / negatives:** dark byte-identity on the `peaceTerms.test.js` stream fixtures (no fixture moves); lit: the payee's credit arrives `arrivalTick − tick` ticks later and equals the dark path's `gainedMonths` within the tenth when the road is open (T-9 both belief arms: omniscient vs fogged observer); the robbery claim minted exactly once per cut caravan (the metronome law — the claim writer names its cooldown, or `metronomeCooldownLint` reds); negative: a caravan that arrives mints no claim. Mutant: the fork inverted (lit path taken dark) → the dark-identity pin reds.

**Files:** `treatyTransfer.js`, `tests/domain/treatyTransfer.caravan.test.js` (NEW), the claim family's tests extended. **Serialization:** `treatyTransfer.js` is declared in this lane's header; TR-5 (lane 2) does not touch it; WF-6/POP-5b/GR-5 do not. **Census bills:** mutation manifest; refreeze own commit. **Byte rule:** build quoted; worker unchanged; engine unchanged; closure ±0. **Gate:** full list. **LIT act:** none. **Exit:** both arms pinned; the stream fixtures unmoved.

### Car 6 — THE LIT CAR (the LAST car, rebased at landing, built on the consist after door D0): `foodCaravansEnabled` lit in the FOUR lit presets through `SIM_LIT`, the dependency clause NARROWED, the declared shift receipted

**In plain words:** the grain road is switched on for every world born into the four living presets — lawfully, because the chair's door train has already switched the caravan layer on in the two presets that lacked it — its permanent dormancy fence is rewritten to describe a lit key, the preset fingerprint golden is re-recorded by hand with the movers named, and the design volume's dependency sentence is corrected so the walker that reads it agrees with what the code actually needs. The two documents that say "no flag is ever in a preset" are the chair's and the door lane's to amend, not this car's.

**⛔ THE LAWS THIS CAR OBEYS BY CITATION, NOT RE-DERIVATION:** the virtuality law is amended by ARCH-PROTOCOLS-ADDENDUM-1 R1 (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`) — `SIM_LIT` is the ONE lawful home of a lit FP key, and the `tests/lint/tradeConvergenceContract.walker.test.js:367` arm is re-cut by door D0 car A (this lane never edits that arm; it runs it and quotes it green with its key in the cohort); **this lane's LIT car lands after door D0 (cars A and B) and after no TR key** (`foodCaravansEnabled` has no `requiresTradeFlags`); it is built AT LANDING on the consist (R2(iii)).

**The change (PROTO §1.2 as read under ADDENDUM R3 — all in ONE commit; the dark mint landed in car 3, so this car carries the LIGHTING — steps 2, 3, 10–12 — plus the dependency amendment and the re-read of the count-from-HEAD figures):**
1. **THE CONJUNCTION RULING (vetoable, veto window open until this car lands).** The volume's lit-preconditions are `commodityFlowEnabled` + `demographicsEnabled`. `demographicsEnabled` is `false` in `full_simulation` (`simulationRules.js:1027`) and ABSENT elsewhere, with its flip reserved by its own comment to "the owner-signed soak redo" — a key this lane may not light, and a key the grain road's MECHANISM does not read: `grainFlow.js` and `grainArrivalCredit.js` import nothing from `demographics*.js` (K_food is a CONSUMER of granary months, not a precondition of moving grain). Keeping the conjunct would red `foreign_precondition_dark` in ALL FOUR lit presets under the executable law (`tradeConvergenceContract.js:511-522`) — an INVALID CONFIG everywhere. **Ruling: the precondition is NARROWED to `commodityFlowEnabled` alone**, in one commit across the three surfaces the walker couples: `docs/DESIGN_FP_TRADE.md` §3 line 268 clause head rewritten to `TR-4 ⇒ \`commodityFlowEnabled\` (` with a dated bracketed correction in the volume's own idiom ("[CORRECTED 2026-09-15 (§932 TR-4 car 6) — `demographicsEnabled` was a consumer mis-filed as a precondition: grainFlow/grainArrivalCredit import no demographics module; the import pin in tests/domain/grainFlow.test.js holds it]"), `tests/lint/tradeConvergenceContract.walker.test.js:170` `VOLUME_DEPENDENCY_CLAUSES['TR-4']` to the new head, `tradeConvergenceContract.js:334` `requiresForeignFlags: ['commodityFlowEnabled']`, and `docs/DESIGN_FP_ARCH_TR.md` §2's flag table row. **THE ASIDE'S DISPOSITION (the skeptic's gap):** the parenthetical that follows the head at `DESIGN_FP_TRADE.md:268-269` — `(grain reads harvest/K_food/storageMonths — wave-P machinery)` — is the JUSTIFICATION of the struck conjunct and would survive as an orphan; it is REWRITTEN in the same edit to `(grain rides M6a's caravan ledgers — \`commodityStocks\`/\`supplyShipments\`; \`demographicsEnabled\` was a CONSUMER mis-filed as a precondition: K_food READS granary months, it does not gate their movement — corrected 2026-09-15, §932 TR-4 car 6)`, and the sentence that follows the aside — `— the collapse-ending arm additionally requires \`routeLifecycleEnabled\` (the J decay ladder provides the mechanics; declared FALSE today …) … never a tuning failure;` (`:269-273`) — is UNTOUCHED byte for byte (its precondition is the collapse ENDING's, not the flag's, and `TRADE_ENDING_MINT_ROWS`' `collapse` row at `tradeConvergenceContract.js:437-442` still names `['foodCaravansEnabled', 'routeLifecycleEnabled']` with `permanentZeroWhileDark: 'routeLifecycleEnabled'` — CONFIRMED). The walker's parser stops at the first `(` (`:166-169`), so both edits are invisible to it — the car asserts the head parses to exactly `['commodityFlowEnabled']` and that the `collapse` row is unchanged, so neither edit is silent. An import pin (`grainFlow.js` and `grainArrivalCredit.js` import nothing matching `/demographics/`) makes the ruling executable. Docs land on the DOCK copies; the ledger copies are the chair's collection.
2. **THE SECOND CONJUNCT IS LIT IN ALL FOUR PRESETS BY DOOR D0 CAR B — THE TWO-PRESET ROSTER AND THE OWNER QUESTION ARE WITHDRAWN.** The lighting-order law is EXECUTABLE: `evaluateTradeFlagLighting` (`tradeConvergenceContract.js:493-525`) pushes `foreign_precondition_dark` for every lit row whose `requiresForeignFlags` member is dark IN THE SAME RULES OBJECT (`:511-522`); the header at `:476` names such a config INVALID; TR-6's row (`:344-346`) records that a degraded arm is DELIBERATELY absent; and `DORMANT_BY_CONFIG` is `TRADE_FLAG_CERTIFICATION_VERDICTS`' word for an observation row (`:149-154`), not an evaluator output. At the consist `commodityFlowEnabled: true` is declared ONLY at `simulationRules.js:769` (`realistic_regional`) and `:938` (`full_simulation`); `dramatic_campaign` held it OPT-IN "per owner design" (`:781-783`) and `living_realm` (`:848-866`) never declared it — so a four-preset `foodCaravansEnabled` in the dock TODAY is two INVALID CONFIGS. **The chair TOOK the cure as door D0 car B (ADDENDUM §3): `commodityFlowEnabled` lit in the two remaining presets as a DECLARED SHIFT (the M6a movers named, the goldens re-recorded by their own rituals, pin 1 = no `foreign_precondition_dark` for it in any lit preset), landing FIRST at §932.** So this lane declares `foodCaravansEnabled` in ALL FOUR lit presets through the one `SIM_LIT` cohort — a VALID config by the executed law — and does NOT light `commodityFlowEnabled` itself, does NOT mint a second cohort, and does NOT re-ask the owner question this brief's first two cuts posed (struck; the chair's veto of car B is a code revert that would re-open it, and would then narrow this key to the two lit presets by the same law under the `neutralNeighborsEnabled` recorded-reason form, PROTO §1.3 — recorded so the fallback is known, not planned). At car 6's rebase the implementer ASSERTS car B is on the consist — `grep -c "commodityFlowEnabled: true" src/domain/worldPulse/simulationRules.js` returns 4 — before declaring anything; 2 means the lane has boarded before the door: STOP. The cascade this brief's second cut declared for the family (`venturesEnabled`, `corneringEnabled`, `factorErrandsEnabled` following a two-preset roster) is likewise WITHDRAWN: all four ride `SIM_LIT` in four presets (briefs 8 and 56a say so; 56b's train prompt carries the addendum).
3. The `SIM_LIT` cohort EXISTS (D0 car A created it EMPTY, spread after `...ONE_REGEN` in exactly the four lit presets — never re-mint it; CHECK-GIT-FIRST for lines other lanes appended): append `foodCaravansEnabled: true, // TR-4 car 6 (lane 1)`; the register entry's comment (from car 3) gains the LIT paragraph naming the four presets and this car. ⚠ The addendum §3 spells the file `src/config/simulationRules.js`; at the consist the file is `src/domain/worldPulse/simulationRules.js` (CONFIRMED by `ls` — no `src/config/` copy exists) — the implementer edits the file that exists. The stability test's roster pins (`simulationRulesPreset.stability.test.js:379-420`) are UNTOUCHED — they govern the nine `ENGINE_WAVE_FLAGS`, not TRADE keys; assert green.
4. The fence `tests/property/foodCaravansDormancyFence.test.js` — NEW in car 3 in the DARK shape (the four fences + lit mutant + the door pin: `foodCaravansActive` the ONE `=== true` site, `commodityFlowActive` the host door, both polarities pinned like `errandSpineDormancyFence`) — RE-CHARTERED here into the LIT shape (PROTO §3.2): the preset arm reads "VIRTUAL (absent from `DEFAULT_SIMULATION_RULES`), REGISTERED, LIT in exactly the four lit presets THROUGH `SIM_LIT` and absent from the three dark ones — the roster spelled from `SIMULATION_RULE_PRESETS` itself, cross-checked against `WAVE_LIT_PRESET_IDS`"; fence 1's dark world is `SIMULATION_RULE_PRESETS.quiet_local.rules`; fence 3's lit twin requires the call count to MOVE under EACH of the four lit presets' rules (D0 car B made `commodityFlowActive` reachable in all four — a ZERO under `dramatic_campaign`/`living_realm` is now a DEFECT, not a proof); the host-door proof survives as a LITERAL-RULES arm (rules `{ foodCaravansEnabled: true }` with `commodityFlowEnabled` ABSENT ⇒ zero grain calls — the flag cannot leak past `commodityFlowActive`), never a preset arm; the truthy-non-true probe; no 64-hex literal (`faithUnseatingDormancyFence.test.js:33-37`); and the lighting-law arm: `evaluateTradeFlagLighting(SIMULATION_RULE_PRESETS[id].rules).ok === true` for all seven presets, with the executed negative control of a throwaway rules object `{ foodCaravansEnabled: true }` (no `commodityFlowEnabled`) yielding exactly one `foreign_precondition_dark` violation.
5. The witness re-recorded BY HAND (PROTO §1.4 row 1): run `measureWitness()`, paste the rows, refresh `distinctWorldStateHashes`/`birthSuccessorPresetIdWhenRecorded`/`presetRosterWhenRecorded`; QUOTE the row diff per preset in the car body: expected `rulesSha256` + `litFlagCount` move on the four lit rows and `__birth_default__`; `worldStateSha256` MAY move on any of those five rows — ONLY if the born worlds carry a spatial canon with a food importer (PLAUSIBLE that they do not — `commodityFlowActive` needs `supplyActive`; if no world hash moves, say so: "the layer is lit and reachable in all four, and the 52-tick birth worlds carry no spatial canon to move it" — the honest receipt, not a defect); a moved DARK-preset row is a STOP. ⚠ D0 car B re-recorded the witness for the two newly-lit `commodityFlowEnabled` presets before this car: this car's diff is taken against THAT golden (base = the consist tip after D0), never against the slot's. The GOLDEN_SHIFT_LEDGER draft section (PROTO §2.5) in the dock's `docs/GOLDEN_SHIFT_LEDGER.md` with the DECLARED-SHIFT sentence: "`foodCaravansEnabled` lit on realistic_regional · dramatic_campaign · living_realm · full_simulation through `SIM_LIT` at landing `<sha>` (after D0 car B lit `commodityFlowEnabled` in all four); `rulesSha256`/`litFlagCount` moved on five rows; `worldStateSha256` moved on <n> rows; the generation arm 525 of 525 bit-identical; the espionage fence 21/21; the first-paint closure +<n> B; no installed campaign is re-labelled; no tuning value moved."
6. The count-from-HEAD figures RE-READ at this car (ADDENDUM R3: the LAST car's counts are taken from HEAD): `node -e` on `ENGINE_GATED_VIRTUAL_RULE_KEYS.length` and `VIRTUAL_SUBSYSTEM_ROWS.length` at the rebased tip → `contributionLedgerShape.test.js:118-119` re-read (they MOVE here only if another lane's dark mint composed between car 3 and this car — then the note line "N → N+1 at …" is re-derived on the composed tree); `coveringArrayCoverage.test.js:185-222` re-measured out of band by calling `flagDomainCensus` (the flag is already in `union` from car 3; a preset declaration may move `governed` — MEASURE, never infer); `subsystemRowsVirtual.test.js`'s three edits from car 3 re-verified in position on the composed tree.
7. **THE LIGHTING-ORDER LAW EXERCISED AT THIS CAR'S TIP:** `evaluateTradeFlagLighting(SIMULATION_RULE_PRESETS[id].rules)` → `{ ok: true, violations: [] }` for ALL SEVEN presets, quoted in the car body (the four lit: `foodCaravansEnabled` lit with `commodityFlowEnabled` lit beside it; the three dark: nothing lit) — the per-lane form of the family-terminus assertion lane 56c car 5 makes over all eight keys.
8. The edge bundles (PROTO §1.2 step 9): the `SIM_LIT` byte in `simulationRules.js` stales both freshness tests → rebuild the four files by the clone → build → `grep -rl 'Users/cstokes' supabase/functions/_shared/` EMPTY → relink recipe.
9. The certification row (car 3's, in `subsystemRowsTrade.js`) amended: `soakEvidence` stays `'unobserved'` (no soak has graded it) but the `other` text records LIT in all four through `SIM_LIT` (after D0 car B); `stateKeys` declares `spatialLedgers.commodityStocks`? — NO: that container is M6a's and would grade the row ALIVE off the caravan layer's ordinary traffic (the TR-1 row's own warning at `subsystemRowsCompact.js:283-292`); the honest channel is the grain rows INSIDE it, which the v5 census cannot address — so `stateKeys: []` with the reason, `eventTypes: []` (no candidateType), and the invariants name the T7 walker, the calm-equivalence pin and the bypass pins by file.
10. The espionage fence run at the tip and 21/21 QUOTED (TR-4 is engine-side: zero generator bytes ⇒ the constant cannot move; the header block is the landing's declared-window lane's to write, not this lane's); `node scripts/prose-manifest-cells.mjs --out` base and tip → zero rows moved, quoted.

**Files (≈12 handwritten — AT the cap, not over it):** `simulationRules.js` (the `SIM_LIT` line + the LIT comment), `tests/property/foodCaravansDormancyFence.test.js`, `tests/fixtures/preset-lighting-witness-golden.json`, `tests/soak-harness/coveringArrayCoverage.test.js` (only if a figure moved), `tests/domain/contributionLedgerShape.test.js` (only if HEAD moved), `src/domain/certification/subsystemRowsTrade.js` (text), `src/domain/certification/tradeConvergenceContract.js`, `tests/lint/tradeConvergenceContract.walker.test.js` (`VOLUME_DEPENDENCY_CLAUSES['TR-4']` ONLY — the `:367` arm is D0's), `tests/domain/grainFlow.test.js` (the import pin), `docs/DESIGN_FP_TRADE.md`, `docs/DESIGN_FP_ARCH_TR.md`, `docs/GOLDEN_SHIFT_LEDGER.md` (dock copies); regenerated: the four edge-shared files. If both "only if" files move at the rebase the count is 12 exactly; a thirteenth handwritten file splits the docs (`DESIGN_FP_TRADE.md` + `DESIGN_FP_ARCH_TR.md` + the walker clause) into a docs-only car 6a landed immediately before 6b (ADDENDUM R5's shape). **Census bills:** the refreeze (own commit after — the fence's titles moved), the mutation manifest (the fence file's entry already from car 3; a `kind:'rationale'` if no new plant), the witness, the shift ledger draft. **Byte rule:** build + verify:dist quoted; the closure delta of the `SIM_LIT` line named in bytes; worker unchanged; engine unchanged. **Gate:** the full list, and additionally `tests/simulation/presetLightingWitness.test.js`, `tests/domain/simulationRulesPreset.stability.test.js`, `tests/edgeFunctions/aiCharterBundle.freshness.test.js`, `aiOutputSchemaBundle.freshness.test.js`, `tests/lint/engineGatedRuleKeys.walker.test.js`, `tests/lint/subsystemCertificationTotality.walker.test.js`, `tests/lint/tradeConvergenceContract.walker.test.js` (the re-cut `:367` arm green with `foodCaravansEnabled` in `SIM_LIT`); the preset-driven soak suites measured ALONE at load < 8 before any red is called a defect (PROTO §1.4 row 8).

**Exit criterion:** `ENGINE_GATED_DORMANT_RULE_KEYS` no longer contains `foodCaravansEnabled` (measured by `node -e` import); the fence 21+/21+ in the LIT shape with the lit twin MOVING under all four lit presets and the literal-rules conjunction arm green; `evaluateTradeFlagLighting` `ok: true` on all seven presets, quoted, with the planted conjunct-absent object refused; the witness green on the re-recorded golden with the row diff quoted; the walker's three surfaces agree on the narrowed clause; the shift-ledger draft written; no red anywhere the car did not cause (a moved DormancyGolden fixture is a STOP).

### Repair allowance (the eighth car)
TR-1, GR-2, SP-B and GR-3a each took a repair commit; the CR-2 STOP clause may itself spend a car. Budget it; report it, never as a surprise.

---

## THE FLAG-MINT CAR (last; the count from HEAD) — READ UNDER ADDENDUM R3

Law 2 ("the flag-mint car is the LAST car of each lane, rebased at landing, its manifest count taken from HEAD") means the LIGHTING car — car 6 — under ADDENDUM R3 (`…/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`). The DARK MINT lands at car 3 because the gate read is the mount (`engineGatedRuleKeys.walker.test.js:538-541` scans `src/` regardless of callers and `:496-497` reds `unaccountedReads`), and the estate's own one-commit law (`subsystemRowsVirtual.js:73-81`; `engineGatedRuleKeys.walker.test.js:512-520` `manifestWithoutRow`; `subsystemRowsVirtual.test.js:646-647`; `contributionLedgerShape.test.js:118-119`) drags the row, the three test edits and both literals INTO car 3 with the register entry — recorded in car 3 as the one forced departure from R3's literal list (PROTO §1.2's closing sentence carries the same reading). So: **at car 3**, immediately before editing: `node -e "import('./src/domain/worldPulse/simulationRules.js').then(m => console.log(m.ENGINE_GATED_VIRTUAL_RULE_KEYS.length))"` and the same for `VIRTUAL_SUBSYSTEM_ROWS.length` from `./src/domain/certification/subsystemRowsVirtual.js`; both `contributionLedgerShape.test.js` literals = that + 1 (35 → 36 if this is the first TR dark mint on the consist; higher if lanes 2/7 minted first — NEVER inherited from this brief). **At car 6** (REBASED at landing, built on the consist after D0; the compose cherry-picks it last) the same two commands are RE-RUN and the literals, the census figures and the `subsystemRowsTrade.js` tail are re-verified against the composed tree; a textual conflict on the register tail, the `SIM_LIT` cohort (D0's empty cohort plus every lane's appended line), the two literals, the census figures and `subsystemRowsTrade.js` is EXPECTED and the chair resolves it by MEASUREMENT on the composed tree, never OURS/THEIRS. The lane's report states the numbers it wrote at car 3, the numbers it re-read at car 6, and the numbers it expects at the consist.

## OWNER-GATED, KEPT OUT

- The tuning values (`GRAIN_TUNING` entire; the calm-equivalence band; the surplus-export bands; the robbery-claim decay) — landed RAW with executed derivations; ratified only at the soak under THE PROMISE (`DESIGN_FP_TRADE.md` §7 names GRAIN_UNITS_PER_MONTH owner-signed).
- Lighting `commodityFlowEnabled` on `dramatic_campaign`/`living_realm` — NOT this lane's act and no longer an open question: the chair TOOK it as a declared shift in door D0 car B (ARCH-PROTOCOLS-ADDENDUM-1 §3, vetoable by code revert). This lane asserts car B is on the consist at its rebase and declares nothing before it is; the two-preset roster the second cut derived is WITHDRAWN (car 6 item 2).
- The virtuality law's amendment (L2 and its four restatements — amended ON THE LEDGER by the chair at the landing, ADDENDUM R1) and the `:367` walker re-cut (D0 car A) — the chair's and the door lane's; this lane cites them by path and never edits either surface.
- Lighting `demographicsEnabled` — wave P's own comment reserves it to the owner-signed soak redo; this lane narrows its own dependency instead.
- Any raise of a size ceiling; any edit to `pulseKernel.js`; any push/deploy/migration/deletion/spend; the generator golden master (525/525 red) is NEVER re-recorded.
- Persisted shapes: this lane widens NONE (grain rides `commodityStocks`/`supplyShipments`; the claim rides the existing I2 family) — recorded so nobody re-finds a widening.

## RISKS

**Grep traps (RECHECK §5 + HISTORY, adopted verbatim):** `cornering` is TR-1's reason NAME, not TR-6; `grain` in ODQ §209/§229 is drawing granularity; `tithe` (38 files) is the old economy layer; `toll` everywhere is entrepôt/gate/smuggle, never a circle fee; `Surveyor` is the paid SKU; W-COIN is STATE money; `INSTR-1` matches `TR-1` in the ledger (85 % of naive hits); `mutateWorld.js` is under `src/domain/events/` and `magicBufferApply.js` under `src/domain/worldPulse/` — the volume's `magic/` path is wrong; the queue rows' `applyWorldPulse.js:322` is a rotted address (the fold is at `:88-110`); `contributionLedgerShape.test.js`'s title still says "at 28" while the literals are 35 — edit the literals, never the title's number.

**The addendum, by path (cite, never re-derive):** `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` — the virtuality law is amended by R1 (a `SIM_LIT` declaration is lawful; the `:367` arm is D0 car A's to re-cut — a lane that edits it collides with the door); this lane's LIT car lands after door D0 (car A the cohort, car B `commodityFlowEnabled` in all four lit presets) and after no TR key; R2(iii): the LIT car is built at landing on the consist, never in the dock (in the dock `evaluateTradeFlagLighting` reds two presets and the walker's `:367` arm is still the old one — a dock-green LIT car is a FALSE GREEN); R3: the dark mint (car 3) carries the register entry, the gate read, the row, the three test edits and both literals because `manifestWithoutRow` forbids a register entry without its row — the last car re-reads the counts. ⚠ The addendum's `src/config/simulationRules.js` is `src/domain/worldPulse/simulationRules.js` at the consist (no `src/config/` copy exists — `ls` CONFIRMED).

**Collision surfaces:** `applyWorldPulse.js` (ONE lane — this one, car 0); `commodityFlow.js`/`supplyKernel.js` (SERIAL TR-4 → WY-3 → {TR-6, WY-6}); the four flag-mint surfaces + `subsystemRowsTrade.js` + `SIM_LIT` (lanes 2 and 3 mint in the same window — CHECK-GIT-FIRST at every rebase; take the count from HEAD); `tests/lint/couplingInclusion.walker.test.js` LAYER_PATTERNS (any lane adding a TR leaf edits the same line — a textual conflict the compose resolves by union); `docs/DESIGN_FP_TRADE.md` §3 line 268 (the walker asserts the clause head EXACTLY ONCE — a second lane re-wording TR-5's clause on the same line block conflicts textually; the parser stops at the first parenthetical).

**Load hazards:** `gate-mutex.sh` gives up after 40 polls and EXITS 0 — a gate line with no printed test count DID NOT RUN; the dist-gated size suites (`vendorPdfLazy.test.js:957`, `generationWorkerLazy.test.js:267`) SKIP silently without a built dist — every byte claim quotes a real build; re-freeze `deno.lock` before any `--frozen` run; `git grep` without a rev reads the INDEX; the preset-driven soak suites time out under load — measure alone at load < 8 before naming a defect; a moved DRIVEN GOLDEN is a STOP by its own law (the fixture goldens drive literal rules and must not move under this lane; a move means car 3/4 changed behaviour under an absent key — find the mover first).

**Design hazards specific to this lane:** the ONE-DOOR law — a second `foodCaravansEnabled === true` anywhere (in `foodStockpile.js` most temptingly) reds the fence's site census and the walker; the offset is a DELTA at the applicator, never a term in `foodStockpile.js`'s sum; `deriveConsumingLinks` is never edited (grain links are ADDED by the leaf); `GRAIN_UNITS_PER_MONTH` lives in `grainArrivalCredit.js` and NOWHERE in the worker graph; after door D0 car B there are NO dormant-by-conjunction lit presets — the host-door proof lives in the literal-rules arm (`{ foodCaravansEnabled: true }` with `commodityFlowEnabled` absent ⇒ zero grain calls), and a grain call under that object is the flag leaking past `commodityFlowActive`; the three DARK presets must show ZERO call-count and ZERO witness movement — a movement there is a spread in the wrong preset; the likeliest way to ship two INVALID CONFIGS is to build car 6 in the dock (cut at `a5876c0ea`, where `commodityFlowEnabled` is dark on two lit presets and the `:367` arm is un-re-cut) and call its green a green — R2(iii) forbids it, the fence's lighting-law arm (item 4) and item 7 catch it, and lane 56c car 5 catches it a third time at the family terminus; `grainFlow.js` and the `subsystemRowsTrade.js` row must not spell any FOREIGN key's name in code (the estate counts namers with comments stripped — the `pactFormationDormancyFence.test.js:198-214` five-member shape; the grain road names only `foodCaravansEnabled` and reads `commodityFlowActive` by IMPORT, never `commodityFlowEnabled` by name).

## GATES (per car, verbatim)

focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators (none in this lane — state so) · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte (quote the worker `stat`, the `engine-*.js` line, the first-paint table) · `npm run check:quick` — every line with a PRINTED test count; the lighting refreeze on a clean tree as its OWN commit after any car that added a test file or title; the mutation manifest entry per new invariant file; the wiring census `--check` (expected untouched); prose-numerics untouched; the Seat/Lane trailer; porcelain 0. At car 3 (the dark mint) additionally the engine-gated walker (directions 1–3), `tests/domain/subsystemRowsVirtual.test.js`, `tests/domain/contributionLedgerShape.test.js`, `tests/soak-harness/coveringArrayCoverage.test.js`, the two freshness tests and the totality walker. At car 6 (built on the consist after door D0 — ADDENDUM R2(iii); never gated in the dock) additionally the witness, the stability test, the two freshness tests, the engine-gated walker, the totality walker, `tests/lint/tradeConvergenceContract.walker.test.js` (the D0-re-cut `:367` arm green with `foodCaravansEnabled` in `SIM_LIT`), `evaluateTradeFlagLighting` `ok: true` on all seven presets QUOTED, and the espionage fence 21/21 quoted.
