# THE LANE — PRICING §C lane 56a · TR-7 VENTURES (`venturesEnabled`) — the first sitting of lane 56 (TR-7 → TR-8 → TR-9, split by the ≤ 8 rule)

Architect: the Fable seat (family TR), 2026-09-15, read-only at the consist `laneCONSIST-932` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, tip `315080928`) against the ledger worktree `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a`. CONFIRMED / PLAUSIBLE as marked; re-measure by SYMBOL. Protocols: `…/arch-932/ARCH-PROTOCOLS.md` (PROTO §n).

**Cars: 5 (1–5), range 4–5** (PRICING §B 3–4; HISTORY §A.4 3–4 unchanged; +1 for the LIT car). PRICING §C lane 56 "TR-7 → TR-8 → TR-9, 11–15 C (two sittings)" exceeds 8 — SPLIT into 56a (this), 56b (TR-8), 56c (TR-9). **THE LANE-56 OVERRUN, STATED HERE BECAUSE 56a BOARDS FIRST (the skeptic's minor gap):** 56a (5) + 56b (6) + 56c (5) = 16 cars at centre against lane 56's 11–15 — ONE over the top of the range; the excess is the two LIT cars the built-lit regime adds to the flagged waves (this brief's car 5, 56b's car 6); the family summary's "+5 overall" already counts them. **CORRECTED 2026-09-15 22:3x at the skeptic's three gaps, under the chair's `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` (read it before car 5):** the first cut's "dormant by conjunction" on `dramatic_campaign`/`living_realm` is STRUCK — it named two INVALID CONFIGS under `evaluateTradeFlagLighting` (`tradeConvergenceContract.js:511-522`, `foreign_precondition_dark` for `venturesEnabled`'s `requiresForeignFlags: ['commodityFlowEnabled']` `:359`) and the phrase has no arm in the evaluator; the chair TOOK `commodityFlowEnabled`'s lighting on those two presets as door D0 car B (ADDENDUM §3), so `venturesEnabled` lights in ALL FOUR lit presets through `SIM_LIT` as a VALID config and car 5 is BUILT AT LANDING on the consist after D0 (R2(iii)); the `:367` VIRTUAL arm is D0 car A's (R1) — car 5 asserts it green, never edits it; the mint/lighting split (car 1 dark mint, car 5 lighting) is recorded under R3 with its count-from-HEAD consequence.

**Prerequisites:** TR-2 landed (lane 55 — `merchantHouseLedger.js`, the `venture_stake` verb reserved in `merchantHouseActs.js`), TR-3 (lane 7 — the venture is CHOSEN on believed bands: `commercialBeliefScarcity.js`), `routeLifecycleEnabled` is DECLARED FALSE (J1 dark) ⇒ the `new_route` venture kind SUPPRESSES to the named degraded set `{far_market, joint_venture}` (`DESIGN_FP_TRADE.md:280-283`; `TRADE_DEGRADED_ARMS[1]` at `tradeConvergenceContract.js:400-405` — a declaration, not a gate: CONFIRMED). SERIAL after TR-4 → WY-3 on `commodityFlow.js` (read; the legs bind to dispatch through `supplyKernel.js`'s seam).

**THE LIGHTING PRECONDITIONS, BOTH DIRECTIONS (ADDENDUM R2):** what car 5 NEEDS lit before it declares `venturesEnabled`: door D0 — car A (the `:367` arm re-cut; the EMPTY `SIM_LIT` cohort) AND car B (`commodityFlowEnabled` lit in `dramatic_campaign` and `living_realm`; at the consist it is declared only at `simulationRules.js:769` and `:938` — CONFIRMED), because `venturesEnabled`'s row carries `requiresForeignFlags: ['commodityFlowEnabled']` (`:353-361`) and `evaluateTradeFlagLighting` (`:511-522`) reds `foreign_precondition_dark` in any lit preset where it is dark — two INVALID CONFIGS in the dock today; plus `merchantHousesEnabled` (lane 55 car 5) and `believedMarketsEnabled` (lane 7 car 5), the two `requiresTradeFlags`. What car 5 UNBLOCKS: `factorErrandsEnabled` (lane 56b car 6 — `requiresTradeFlags: ['tradePactsEnabled', 'venturesEnabled']`, `:362-367`) and the `fortune`/`ruin` endings' principal mint (their `TRADE_ENDING_MINT_ROWS` rows require only `merchantHousesEnabled`, `:419-430` — CONFIRMED; the venture is the mint, the house key the gate). Car 5 is built AT LANDING on the consist (R2(iii)) and asserts `grep -c "commodityFlowEnabled: true" src/domain/worldPulse/simulationRules.js` = 4 before declaring — 2 means the lane boarded before the door: STOP.

**Shared files this lane declares, slack at the cut:** `src/domain/worldPulse/merchantHouseActs.js` (TR-2's; the `venture_stake` row flips `built: true`) · `src/domain/worldPulse/supplyKernel.js` 489/800 (+≤8 for the leg binding — after lanes 1, 7, 8 on this file) · `src/lib/spatialUsage.js` (the `ventures` key classification) · `src/domain/worldPulse/commercialReceiptPools.js` (~3 kinds) · `src/domain/worldPulse/demographicsPlans.js` **375/800 — ZERO EDITS** (its `:18-22` fence: "This is not a general planning system and must not become one"; S31/J-TR-12) · the flag-mint surfaces + `SIM_LIT` + `subsystemRowsTrade.js` · `tests/lint/couplingInclusion.walker.test.js` (no edit: leaves named `merchantVenture*.js` match `merchant`).

## THE STATE OF THE RECORD

- **The history fold** (`…/recon-32-34/HISTORY-TRADE-2026-09-15.md`; `HISTORY-TR-…` does not exist under that name): TR-7 **UNBUILT** — "Nothing under any name: `ventureLedger/ventureLegs/ventureNews` absent, `spatialLedgers.ventures` = 0; 'joint venture' appears only in institution description copy; `venture` in src is overwhelmingly institution text. `venturesEnabled` exists only as a spec row in `tradeConvergenceContract.js`." Grep trap: `venture` is institution copy.
- **The volume:** `docs/DESIGN_FP_ARCH_TR.md` §3 (`spatialLedgers.ventures`: `[ { id, houseId, kind, stakeBand, legs/shipmentRefs, departedTick, expectedTick, state } ]`; lifecycle: "regenerate: in-flight ventures PRESERVED (event-accrued); shipmentRefs resolve at load or the venture closes `failed` with a receipt (no dangling refs — the lifecycle-bug class)"), §4 TR-7 (three leaves; the pins; "seatBooks colour is ABSENT-not-zero until INT-1 (seam 8)"; "demographicsPlans.js ZERO edits (the fence)"). Design law `docs/DESIGN_FP_TRADE.md` §5 TR-7 (`:1094-1160`): the closed kinds `{far_market, new_route, joint_venture}`; ONE active venture per house; band-crossing trigger (not drift); startup cost; real duration (the road prices it — no authored duration); named failures (interception, refusal, wrong-market arrival, route loss); cooldown; T-10 the Bardi arc; the INT-1 colour term ("when `seatBooksEnabled` AND `venturesEnabled` are BOTH lit, venture appetite is COLOURED by `booksOf(settlementId)` … until INT-1 lands the books term is ABSENT, not zero"); endings `fortune`/`ruin` (the venture is their principal mint). Compiled programme §5 #29 (line 1702). The dependency clause `DESIGN_FP_TRADE.md:279` `TR-7 ⇒ TR-2 + TR-3 (the venture is CHOSEN on believed bands)` → `tradeConvergenceContract.js:353-361` (`requiresForeignFlags: ['commodityFlowEnabled']`). PRICING §B's note: "regen-PRESERVES exception (J-TR-3); `warSeatBooks.js` EXISTS — scope the books pin by module, not by the `seatBooksEnabled` grep" (CONFIRMED: `warSeatBooks.js` 492 eff is WR-5's ruler/realm objective read, `seatBooksEnabled` has zero src hits).
- **The owner's laws:** built lit; tuning raw (trigger thresholds, stake bands, cooldown, split bands) ratified at the soak; the persisted shape `spatialLedgers.ventures` TAKEN and recorded (the #20 batch); nothing pushed/deployed/applied/deleted/spent.

## THE STATE OF THE CODE (CONFIRMED)

| Module | eff/ceiling | What is there |
|---|---|---|
| `src/domain/worldPulse/demographicsPlans.js` | 375/800 | the plan GRAMMAR (band-crossing draw, ONE plan per settlement, proposed→underway→completed/failed/abandoned, startup cost, duration, named failures, cooldown, receipt) — the SHAPE TEMPLATE, never shared code; its fence `:18-22` |
| `src/domain/spatial/commodityFlow.js` / `supplyShipments.js` | 448 / 210 | the dispatch (`advanceCommodityFlow` `:441`), the shipment record shape (`:298-334`), `shipmentCut`, the M7 seizure — the venture's LEGS are these records; `spatial/seaLanes.js` (M8) for a sea leg |
| `src/domain/worldPulse/supplyKernel.js` | 489/800 | the dispatch seam (`buildDispatchEV`, the `advanceCommodityFlow` call `:479-482`) — a venture's sponsored dispatch enters here under `venturesActive` |
| `src/domain/worldPulse/merchantHouseLedger.js` / `merchantHouseActs.js` (TR-2) | — | books `{holdings, credit, interests[]}`, appetite, credibility; the `venture_stake` verb row `built: false` |
| `src/domain/worldPulse/commercialBeliefScarcity.js` (TR-3) | — | `believedDearness01Of` — the far-market choice |
| `src/domain/worldPulse/warSeatBooks.js` | 492/800 | WR-5's seat read — NOT INT-1's `seatBooksEnabled` (zero hits); the seam-8 pin scopes by MODULE |
| `src/domain/spatial/distanceRead.js` | — | `hopWeeks`/the digest's leg pricing — the road prices the duration (WY-1's `kmScale` may re-price it later: declared) |
| `src/domain/certification/tradeConvergenceContract.js` | 398 | TR-7's row `:353-361`; `TRADE_ENDING_KEYS` `fortune`/`ruin` |
| ABSENT | — | every venture leaf; `venturesEnabled` (zero hits); `spatialLedgers.ventures` |

**Budgets:** worker EXACT (nothing here is in the graph); `engine` ±0; first-paint: one `SIM_LIT` line, quoted.

## ALREADY DONE (do not re-land)

M6a/M8 dispatch and the shipment record (the legs' physics); the plan grammar (as SHAPE); TR-2's books and verb set; TR-3's belief read; the interception/seizure/wrong-market physics (M1/M7/TR-3).

---

## THE CARS

Every car: one gated commit; anchored negatives; executed mutants manifest-rowed; NUL-scanned; porcelain 0; `Seat: Opus 5 — Fable-unvalidated` + `Lane: TR-7`; PACKET caps.

### Car 1 — `merchantVentureLedger.js`: THE ONE WRITER — the plan grammar at house grain, the closed kinds, the state machine, the normalizer; `spatialLedgers.ventures` TAKEN

**In plain words:** a house can put a stake on a voyage — a caravan to a far market it believes is paying dear, or a shared venture with another house — recorded as one entry per house that moves through proposed, underway, completed or failed, costs the books at departure, and is repaired on load so no entry ever points at a caravan that no longer exists.

**The change.** `merchantVentureLedger.js` (≤250 eff): `venturesActive(rules)` — THE ONE by-name read `rules.venturesEnabled === true`; `VENTURE_KINDS = ['far_market', 'joint_venture', 'new_route']` with `new_route` SUPPRESSED while `routeLifecycleEnabled !== true` (the declared degraded set — a pin drives both states); `VENTURE_TUNING` RAW (trigger crossing band, stake bands by holdings, cooldown seasons INTERVAL_WEEKS-derived, split bands); the state machine `proposed → underway → completed | failed | abandoned`; ONE active venture per house (the cap); the trigger is a band CROSSING of believed dearness (TR-3) not drift (the plan lane's anti-weather law re-pinned); startup cost = the books band steps DOWN at departure through TR-2's writer (ONE writer per ledger: the venture ledger asks `merchantHouseLedger.js` to debit — never writes `houses` itself); `shipmentRefs` name the M6a records; `normalizeMerchantVentures(raw, worldState)` — a ref that does not resolve at load closes the venture `failed` with a receipt (no dangling refs); `advanceMerchantVentures({…})` the writer (dark ⇒ reference; lit-empty ⇒ drop). `spatialUsage.js`: `ventures` classified TRACKED with its paragraph (+ the test row); the single-writer census walker + third-writer plant; the lane's report NAMES `spatialLedgers.ventures` for the #20 batch.

**Pins / negatives:** the cap; crossing-not-drift (a slow drift across the band never triggers; a crossing does); `new_route` suppressed dark / available under a lit `routeLifecycleEnabled` fixture (literal rules); JSON round-trip; regen PRESERVES in-flight ventures (pinned on the regen path; recorded as the second J-TR-3 instance); undo/import; the dangling-ref close (delete a shipment record → the venture closes `failed` with a receipt on load); dark identity. Mutants: the cap removed; the normalizer's ref check removed; the third-writer plant. **Files:** the leaf, `spatialUsage.js`, its test, the census walker, `tests/domain/merchantVentureLedger.test.js`. **Census bills:** manifest ×3; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list + `spatialLedgerCoverage.walker`, `observedShapeReaders.walker` (no new reader minted — reported). **LIT act:** none. **THE MINT/LIGHTING SPLIT, READ UNDER ADDENDUM R3 (recorded, vetoable):** the by-name read `venturesEnabled === true` lands in THIS car's leaf, and `engineGatedRuleKeys.walker.test.js`'s `scanGateReads` (`:538-541`) reads every `src/` file regardless of callers — so `auditEngineGatedKeys` (`:496-497`) would red `unaccountedReads` unless the register entry lands in the same commit: R3 lets that DARK MINT land early. And the estate's own one-commit law drags the rest of the mint with it: a register entry without its row reds `manifestWithoutRow` (`:512-520`; `VIRTUAL_PENDING_RULE_KEYS` is `[]`, pinned `toEqual([])` at `subsystemRowsVirtual.test.js:647`), a row without its `VIRTUAL_RULES` position reds the ordered equality (`:646`), both moved lengths red `contributionLedgerShape.test.js:118-119`, `coveringArrayCoverage.test.js`'s `union` counts register members, and any `simulationRules.js` byte stales the two edge bundles. So THIS car pays, in ONE commit: the register entry (tail, DARK — the key in NO preset), the gate read, the AUTHORED row in `subsystemRowsTrade.js` (`stateKeys: ['spatialLedgers.ventures']` — CHECK-GIT-FIRST for the leaf), the three `subsystemRowsVirtual.test.js` edits, BOTH `contributionLedgerShape` literals READ OFF HEAD at this car's base, the covering-array `union`, the four edge-shared files. Car 5 (LAST, rebased at landing) pays the LIGHTING and RE-READS the literals and census figures off the composed HEAD. This is R3 applied as far as the walker allows; the one departure from R3's literal list (row + three edits + literals travelling with the dark mint) is forced by `manifestWithoutRow` and recorded here for the chair. **Exit:** every lifecycle path pinned; the shape recorded; the mint dark (`ENGINE_GATED_DORMANT_RULE_KEYS` CONTAINS `venturesEnabled` at this car — measured).

### Car 2 — `merchantVentureLegs.js`: the M6a/M8 dispatch binding — the road prices the duration; stake genuinely at risk; appetite learns both ways

**In plain words:** a venture's goods really travel — as caravans on roads or ships on sea lanes — and can really be robbed, intercepted, seized or arrive to find the market glutted; a failed venture really lowers the house's books; and a house grows bolder after fortune and more cautious after ruin, never only one way.

**The change.** `merchantVentureLegs.js` (≤200 eff): `dispatchVentureLegs(venture, ctx)` — the venture's stake becomes sponsored M6a dispatch records (through `supplyKernel.js`'s seam under `venturesActive`, ≤8 lines there; sea legs via M8 where the digest says water) with `shipmentRefs` stamped; duration = the road's hop weeks (`distanceRead`), NO authored duration; the named failures resolve off the EXISTING physics: `shipmentCut`/interception → `failed:intercepted`, the M7 seizure → `failed:seized`, arrival into a `surplus` truth band → `completed:wrong_market` (fortune denied — the T-1 idiom at house grain), a route lost mid-leg → `failed:route_lost`; on `completed` the books band steps UP (through TR-2's writer), on `failed` the stake is gone (the step-down at departure is not refunded — stake at risk); `joint_venture` splits stake and take by band between two houses; the unpaid share on default feeds `contract_default` at house grain (TR-5's seam registry `commercialSeamsFor` gains a house-grain row — CHECK-GIT-FIRST); APPETITE REVERSAL (SP-4a): the outcome moves the house's `appetite` in BOTH directions (bounded; no courage ratchet); `venture_stake` in `merchantHouseActs.js` flips `built: true` in THIS commit (the registry law: the row and the writer are one commit).

**Pins / negatives:** stake genuinely at risk (a failed venture's books band falls; a completed one rises); each named failure reachable on its own fixture; joint split honoured and the default arm mints the house-grain casus; appetite reversal executed BOTH ways (ruin → cautious; fortune → bold; bounded); the seatBooks colour term ABSENT-not-zero (seam 8: the ledger carries no `seatBooksColour` key; the twin reds when `seatBooksEnabled` appears without the colour — scoped by MODULE, `warSeatBooks.js` excluded by name); dark identity. Mutants: the refund of the stake on failure (phantom stake) → reds; the reversal made one-way → reds. **Files:** the leaf, `supplyKernel.js` (+≤8), `merchantHouseActs.js` (the row), `commercialReasons.js` or `commercialTermExecutors.js` (the seam row), `tests/domain/merchantVentureLegs.test.js`. **Census bills:** manifest ×2; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list (the M6a integration fixtures unmoved — the binding is inside the lit path). **LIT act:** none. **Exit:** legs move; failures reachable; reversal both ways.

### Car 3 — `merchantVentureNews.js` + the five joins + own walker + T-10 THE BARDI ARC + DM-KILL closes

**In plain words:** the Herald learns "The Verren venture returned with fortune" and "The sea took the Verren fortune"; one fixture rides a house from top-band fortune through a failed venture into ruin with a receipt at every step; and a DM who kills the supercargo mid-voyage sees the errand close `lost` and the venture `failed` through their own writers, never an orphan.

**The change.** Kinds `commercial_venture_departed` ('routine'), `commercial_venture_returned` ('notable'), `commercial_venture_lost` ('notable') — registry leaf, pool rows in `commercialReceiptPools.js` mirroring `docs/content/RECEIPT_POOLS_TRADE.md`, `WHAT_PHRASES`, `heraldRouting.js` `trade` desk, the address chain, `tests/lint/commercialVentureKindPools.walker.test.js` (NEW), `heraldContaminationFence`, pacing-registered, the metronome latch; the endings `fortune`/`ruin` receipts in the convergence vocabulary (TR-9 polices). T-10 (`tests/domain/merchantVentureTellables.test.js`): fortune → failed venture → ruin on ONE fixture, receipts each step (TR-2's ruin latch closes the house). DM-KILL: the supercargo (a cast factor, TR-2; with TR-8 lit an SP-D errand) killed mid-voyage → the venture closes `failed` through THIS writer (and, when TR-8 exists, the errand `lost` through ITS writer — a pre-pin toward TR-8). The dossier house entry (TR-2 car 4's line) gains the live venture ("The Sunless Coast venture — underway, due by autumn" — bands and words; ONE user-facing surface). **Files:** ≈8. **Census bills:** wiring census stamp; prose-numerics; manifest; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list + the kind walkers, `heraldContaminationFence`, `proseWiringCensus.walker`, `metronomeCooldownLint`, `tests/components`. **LIT act:** none. **Exit:** three kinds × five joins; T-10; DM-KILL closes.

### Car 4 — the census and coupling bills gathered: the CW-0 rows, the endings' receipt fields, the `new_route` pre-pin toward J1

**In plain words:** the bookkeeping the other programmes require — every cross-layer read this wave makes gets its registry row, the fortune and ruin endings get the receipt fields the auditor will sample, and the road-charter venture kind is pinned to wake the day the route network lights.

**The change.** `couplingRegistryTrade.js`: rows for TRADE→WAR (the venture reads charter-danger belief — the frightened endpoint governs), TRADE→INFO (a planted far-market lure baits a venture — the LURE), TRADE→INTERIOR (the seatBooks colour, ABSENT arm); the `fortune`/`ruin` receipt fields (`kind`, `houseId`, `ventureId`, `outcome`) declared for TR-9's SC-9 sampling; the `new_route` pre-pin: a fixture with `routeLifecycleEnabled: true` (literal) and a J1 charter docket row makes `new_route` available and binds the house sponsor to the docket's expedition-worth read (`routeNetworkCharter.js` — READ; the docket's shape by symbol at charter) — the day J1 lights on a preset, the kind wakes with no edit. **Files:** the registry leaf, `merchantVentureLedger.js` (+≤10), tests. **Census bills:** manifest; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list + `couplingInclusion.walker`. **LIT act:** none. **Exit:** rows present; the pre-pin drives both states.

### Car 5 — THE LIT CAR (the LAST car, rebased at landing, built on the consist after door D0 and after lanes 55 and 7): `venturesEnabled` lit in the FOUR lit presets through `SIM_LIT` — a VALID config by the executable law, because door D0 car B lit `commodityFlowEnabled` where it was dark

**In plain words:** a house's ventures are switched on for every world born into the four living presets — lawfully, because the chair's door train has already switched the caravan layer on in the two presets that lacked it, and the merchant houses and believed markets a venture needs were lit by their own lanes first; the permanent dormancy fence is rewritten to describe a lit key, the preset fingerprint golden is re-recorded by hand with the movers named, and the shift is written down.

**⛔ THE LAWS THIS CAR OBEYS BY CITATION (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`):** R1 — the virtuality law is amended; `SIM_LIT` is the one lawful home of a lit FP key; the `tests/lint/tradeConvergenceContract.walker.test.js:367` arm is re-cut by door D0 car A and this lane never edits it (it runs it and quotes it green with `venturesEnabled` in the cohort). R2 — this car lands after door D0 (car A the cohort, car B `commodityFlowEnabled` in `dramatic_campaign` and `living_realm`) and after `merchantHousesEnabled` (lane 55 car 5) and `believedMarketsEnabled` (lane 7 car 5); the first cut's "DORMANT BY CONJUNCTION in `dramatic_campaign`/`living_realm`" is STRUCK — `evaluateTradeFlagLighting` (`tradeConvergenceContract.js:511-522`) has no such arm, only `foreign_precondition_dark`, and would have named both presets INVALID CONFIGS (the skeptic's finding, CONFIRMED against `:353-361` `requiresForeignFlags: ['commodityFlowEnabled']`); the owner question lane 1's first cut posed is SUPERSEDED by the chair's D0 car B (a declared shift under the 14:4x grant, vetoable by code revert). R3 — the dark mint was car 1; this car is the LIGHTING and re-reads the counts off HEAD.

**The change (PROTO §1.2 steps 2, 3, 10–12; the dark mint was car 1):** `SIM_LIT` gains `venturesEnabled: true, // TR-7 car 5` (the cohort exists from D0 car A — append, never mint; CHECK-GIT-FIRST for the line's neighbours); the register entry's comment gains the LIT paragraph naming the four presets and this car; the conjuncts `merchantHousesEnabled` + `believedMarketsEnabled` are ASSERTED lit in every lit preset at this car's tip (a dark one is a BOARDING STOP, never a declaration) and `commodityFlowEnabled` asserted `true` in all four (`grep -c "commodityFlowEnabled: true" src/domain/worldPulse/simulationRules.js` = 4 — 2 means the lane boarded before the door: STOP); `new_route` suppressed everywhere (J1 dark — `TRADE_DEGRADED_ARMS[1]`, `:400-405`, a declaration not a gate, so it is NOT a lighting violation). `tests/property/venturesDormancyFence.test.js` (NEW at car 1, dark) → LIT shape (PROTO §3.2): the preset arm reads "VIRTUAL (absent from `DEFAULT_SIMULATION_RULES`), REGISTERED, LIT in exactly the four lit presets and absent from the three dark ones — the roster spelled from `SIMULATION_RULE_PRESETS`"; fence 1's dark world `quiet_local.rules`; the site census (`merchantVentureLedger.js` the ONE `=== true`); fence 3's lit twin MOVES the writer's call count under EVERY lit preset (all four now carry the conjunct — the first cut's "under `dramatic_campaign` lit it stays ZERO" is gone with the door); the literal-rules CONJUNCTION pin stays as a fixture arm: a rules object `{ venturesEnabled: true }` WITHOUT `commodityFlowEnabled` leaves the writer's count at zero (the leg binding sits inside `commodityFlowActive`) — the dormancy-by-conjunction fact survives as a literal-rules test, never as a preset declaration; the truthy-non-true probe; no 64-hex literal; and the lighting-law arm: `evaluateTradeFlagLighting(SIMULATION_RULE_PRESETS[id].rules).ok === true` for all seven presets at this car's tip, with the executed negative control `{ venturesEnabled: true, merchantHousesEnabled: true, believedMarketsEnabled: true }` (no `commodityFlowEnabled`) yielding exactly one `foreign_precondition_dark`. The witness re-recorded by hand with the diff quoted: `rulesSha256`/`litFlagCount` on the four lit rows + `__birth_default__` (five rows), a moved DARK row a STOP; `worldStateSha256` moves only if a born world forms a venture in 52 ticks (needs a house AND a believed-dear far market AND a spatial canon — PLAUSIBLE zero; say so honestly). The shift-ledger draft (PROTO §2.5) with the DECLARED-SHIFT sentence ("`venturesEnabled` lit on the four presets at landing `<sha>`; `spatialLedgers.ventures` TAKEN (the #20 batch); `new_route` suppressed while J1 is dark; …"); the census literals re-measured out of band; BOTH `contributionLedgerShape` literals RE-READ off the composed HEAD (R3 — car 1 paid them at its own base); the edge bundles rebuilt; TR-7's row `other` amended for LIT; the espionage fence 21/21 and prose cells zero quoted. **Files (≈9 handwritten):** `simulationRules.js` (the `SIM_LIT` line + comment), the fence, the witness golden, `coveringArrayCoverage.test.js` (if moved), `contributionLedgerShape.test.js` (if HEAD moved), `subsystemRowsTrade.js` (text), `docs/GOLDEN_SHIFT_LEDGER.md` (dock); regenerated: the four edge-shared files. **Gate:** full list + the witness, the stability test, the two freshness tests, the engine-gated walker, the totality walker, the espionage fence, the soak suites alone at load < 8. **Exit:** the key absent from `ENGINE_GATED_DORMANT_RULE_KEYS`; the fence LIT-shape green with the conjunction twin; the witness diff; the shift draft.

---

## THE FLAG-MINT CAR (last; the count from HEAD)
THE SPLIT, READ UNDER ADDENDUM R3 (recorded, vetoable; it is the reading of the chair's law 2 "the flag-mint car is the LAST car" that the estate's own walker permits): the DARK MINT at car 1 (the register entry + the gate read — R3's two items — PLUS the row, the three `subsystemRowsVirtual.test.js` edits and both `contributionLedgerShape` literals off HEAD at car 1's base, because `manifestWithoutRow` (`engineGatedRuleKeys.walker.test.js:512-520`) forbids a register entry without its row — car 1's own paragraph records why); the LIGHTING at car 5 (LAST, REBASED at landing, built on the consist after door D0 and lanes 55/7) which RE-READS both literals and the census figures off the composed HEAD and quotes them — the lane's report states car 1's numbers (at its base), car 5's numbers (at the rebase) and the number it expects at the consist. Conflicts on the register tail, `SIM_LIT`, the two literals, the census figures and `subsystemRowsTrade.js` are EXPECTED and resolved by MEASUREMENT (PROTO §5.2), never OURS/THEIRS.

## OWNER-GATED, KEPT OUT
Tuning (`VENTURE_TUNING`) raw, ratified at the soak · lighting `commodityFlowEnabled` on `dramatic_campaign`/`living_realm` is NOT this lane's — the chair TOOK it as door D0 car B (ADDENDUM §3, a declared shift under the 14:4x grant; the owner's veto is a code revert of car B, which would re-open the question and by the same executable law narrow `venturesEnabled` to the two lit presets under the `neutralNeighborsEnabled` recorded-reason form, PROTO §1.3 — recorded so the fallback is known, not planned) · lighting `routeLifecycleEnabled` on any preset (J1's own declared false) · **the addendum, by path (cite, never re-derive):** `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` — R1 the virtuality law amended (the `:367` arm is D0 car A's; this lane never edits it), R2 this lane's car 5 lands after door D0 and after `merchantHousesEnabled` (lane 55) and `believedMarketsEnabled` (lane 7), built at landing on the consist, R3 the split (car 1 dark mint, car 5 lighting); ⚠ the addendum's `src/config/simulationRules.js` is `src/domain/worldPulse/simulationRules.js` at the consist · `demographicsPlans.js` ZERO edits (its fence) · push/deploy/migration/deletion/spend · the golden master · the persisted shape `spatialLedgers.ventures` TAKEN and recorded for #20 (no migration written or applied).

## RISKS
**Grep traps:** `venture` is institution COPY in src ("joint venture" in descriptions); `warSeatBooks.js` is WR-5's read, NOT INT-1's `seatBooksEnabled` (zero hits) — scope the seam-8 pin by module; `spatialLedgers.commitments` is MOMENTUM's (RECHECK §5); `INSTR-1≠TR-1`. **Collision surfaces:** `supplyKernel.js` (after lanes 1, 7, 8 and WY-3; beside WY-6 only with the chair's word); `merchantHouseActs.js` (TR-2's — must be on the consist); `spatialUsage.js` key lists; the seams registry (TR-5/TR-6 rows — union); `commercialReceiptPools.js` (the SPLIT rule); the flag-mint surfaces; `subsystemRowsTrade.js`. **Load hazards:** `gate-mutex.sh` false green; dist-gated suites skip without a build; `deno.lock`; `git grep` reads the INDEX; soak suites alone at < 8; a moved DRIVEN GOLDEN is a STOP. **Design hazards:** the venture ledger never writes `houses` (one writer per ledger — it ASKS TR-2's writer); the dangling-ref close on load is the lifecycle bug class that bites regen/undo/import — pin every path; the stake is never refunded on failure; appetite moves both ways; `new_route` is suppressed by a LITERAL-rules read of `routeLifecycleEnabled` (declared FALSE in presets — a declared false is a boolean the dormant-derivation counts: it is NOT a virtual key; do not add it to `SIM_LIT`).

## GATES (per car, verbatim)
focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators (none — state so) · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte (quote the worker `stat`, the `engine-*.js` line, the first-paint table) · `npm run check:quick` — every line with a PRINTED test count; the lighting refreeze on a clean tree as its OWN commit after any car adding a test file/title; the mutation manifest per new invariant file; the wiring census stamp at car 3; prose-numerics at car 3; the edge bundles at cars 1 and 5; the Seat/Lane trailer; porcelain 0. At car 5 additionally: the witness, the stability test, the two freshness tests, the engine-gated walker, the totality walker, the espionage fence 21/21, the soak suites alone at load < 8; at car 3 additionally `tests/components`.
