# THE LANE — PRICING §C lane 17, sitting 17a · WF-2a THE PILGRIM SEASON (`pilgrimageEnabled`; the season over the live TRADITIONS draw; the family's first kinds car and the §350 R-1 shrink-back)

Architect: the Fable seat (family WF), 2026-09-16, read-only at the consist `laneCONSIST-932` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, tip `315080928` — LT37 car 6, porcelain 0, D0 NOT yet composed) against the ledger worktree `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a`. CONFIRMED = opened at the cited line by me or by a recon skeptic; PLAUSIBLE = reasoning. Line numbers rot inside a day — re-measure by SYMBOL. Protocols `…/f6ac0d98…/scratchpad/arch-932/ARCH-PROTOCOLS.md` (PROTO §n) via `…/arch-932/ARCHITECTS-CARD.md`; `…/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` (R1–R7, door train D0) WINS wherever they differ. Family plan `…/arch-932/plans/plan-WF.md` §1 row 17a, §2, §4, §5 "17a"; where the code contradicts it this brief says so (three places, named below: the kernel call at `traditionsKernel.js:726` DOES take lines; the kinds car is this lane's, not WF-8's; the §350 R-1 shrink-back is paid HERE).

**Cars: 4 (1, 2, 3, 4) + 1 repair allowance = 4–5.** PRICING §B WF-2 7–10 C for both slices; HISTORY-WF §A.4 7–10 unchanged ("the belief leg is de-risked — no join car; the draw is live under TRADITIONS"); the plan seated 17a at 3 + 1. This brief adds ONE car the plan did not see: THE KINDS CAR (car 3) — the FAITH annex `docs/content/RECEIPT_POOLS_FAITH.md` §C census gives WF-2 FOURTEEN phrased kinds, six of them the season's (`:250-309`), and the volume's WF-8 row says in terms "each earlier wave registered its kinds at mint, WF-8 gathers and adds only its own" (`DESIGN_FP_ARCH_WF.md:672-708`, Collisions) — so the plan's "21a car 2 · kinds batch 1 (the WF-1/5/7 fire sites)" is a misread of the annex: §K's fifteen WF-8 kinds are the SILENT-MACHINERY voices (Phase 4's transitions), and every WF wave voices its own beats in its own lane. CONFIRMED by the annex §C table (`RECEIPT_POOLS_FAITH.md:131-145`).

**Prerequisites (anchors):** lane 16a on the consist (the `SIM_LIT_FAITH_SPREAD` cohort exists EMPTY or carrying lane 5/16b's keys; the deity-bearing fixture `faith_bearing_v1` at `scripts/audit/soakCases/faithBearing.mjs` — its patron host carries a `scaleBand ≥ 3` procession/offering/fair and a mapped reachable neighbour, `pilgrimage.js:59-63 drawsPilgrims`'s own bar — `brief-16a-WF-WF-0-WF-1.md` car 1 pin 4); lane 5 LANDED (WF-6 — `faithTermExecutors.js`'s `pilgrimRoadFor` refusal arm exists to be flipped, `brief-5-WF-WF-6.md` car 2; lane 5 lit `believedDevotionEnabled` in four presets, so the season's belief leg has a live SP-B band — else the `observanceLabel` dark arm is the live leg, DECLARED); lane 16b LANDED (the WF chain is ONE lane in flight — plan §4). WC-e `027e4e4ce` (the pilgrimage draw), SP-B `4c0f2f38b` (`DEVOTION_BANDS`, `devotionGroundTruth` LIVE) are ancestors (CONFIRMED, HISTORY §B rows 6, 12).

**THE LIGHTING PRECONDITIONS, BOTH DIRECTIONS (ADDENDUM R2):** NEEDS lit first — `pilgrimageEnabled` is SPREAD-lane: its door ANDs `isFaithSpreadEnabled(rules)` (a DEFAULT boolean resolved true only in `dramatic_campaign` `:811` and `full_simulation` `:875` — CONFIRMED), so the roster is the TWO-preset cohort `SIM_LIT_FAITH_SPREAD` (lane 16a car 0's O-12 ruling, the reason recorded in the key's register comment); the season's belief leg prefers `believedDevotionEnabled` lit (lane 5 car 5) but degrades to the `observanceLabel` substrate with a receipt, so it is a MECHANISM preference, not a lighting-table row; `traditionsEnabled` is lit in all four lit presets (`simulationRules.js:674` — CONFIRMED) so the draw the season subsumes is live wherever the key lights. WHAT IT UNBLOCKS: 17b (the movers dispatch from the season's feast/flood bands); WF-6's `pilgrimage_right` flips from a refusal to a live grant (this lane re-runs lane 5's refusal pin as its POSITIVE control — car 2); WF-8's Great Pilgrimage arc keys to the season (lane 21b); WF-9's subsumption-seam envelope (lane 22 consumes car 2's disclosure delta).

**Shared files this lane declares (PROTO §7.3 rule 1), eff/ceiling at the cut (measured with `arch-932/eff-any.mjs "$CONSIST" <paths>`, eslint's own counter):** `src/domain/traditions/pilgrimage.js` 41/800 (the ~5-line subsumption fork — the ONLY edit to a TRADITIONS leaf) · `src/domain/worldPulse/traditionsKernel.js` 484/800 (⚠ CONTRA THE PLAN's "the kernel call at `:726` unchanged": the season needs `rules` + `religionStates` + the belief legs the kernel does not pass today, so the kernel gains ≤ 8 lines — the once-per-tick season resolution under the door beside `activeSpatialDigest(worldState)` at `:657`, the scalar handed into `pilgrimageDraw` at `:726`, and the season RECEIPT minted beside the tradition beat; 484 → ≤ 492, far under 800) · `src/domain/worldPulse/faithTermExecutors.js` (lane 5's leaf — `pilgrimRoadFor` flips to a live grant under `pilgrimSeasonActive`, ≤ 6 lines) · `src/domain/worldPulse/faithNews.js` 73/800 (the FAITH_KIND_REGISTRY gains six rows — see THE REGISTRY GROWTH RULE below) · `src/domain/display/settlementRumors.js` 555/800 (six `WHAT_PHRASES` rows + one `TRANSITION_KINDS`? — no: season beats are phrased) · `src/domain/realm/heraldRouting.js` 273/800 (six EXACT_SECTION rows on the `faith` desk) · `tests/helpers/kindRegistryRoster.js` (the freezes) · `tests/lint/kindPoolFloors.walker.test.js` (`REGISTERED_KIND_COUNT` +7; FAITH struck from the < 5 exception list) · `tests/domain/impactKindWalkers.test.js` (EXPECTED_VOICE rows) · `tests/lint/couplingInclusion.walker.test.js` (NO edit — the leaf is named `faithPilgrimSeason.js` to match the FAITH regex `:187`; see NAMING below) · `src/domain/worldPulse/simulationRules.js` 352 (register tail + the `SIM_LIT_FAITH_SPREAD` line) · `src/domain/certification/subsystemRowsFaith.js` (created by lane 5 — APPEND one row; CHECK-GIT-FIRST) · `src/domain/certification/couplingRegistryFaith.js` (lane 5's — APPEND) · the flag-mint test surfaces · the witness golden · `docs/GOLDEN_SHIFT_LEDGER.md` (dock draft). NOT touched: `pulseKernel.js` 1581/1581 (the season composes INSIDE `traditionsKernel.js`, mounted at `pulseKernel.js:2619` already — the plan's rule holds), `routeNetworkFlows.js` (`ROUTE_FLOW_SOURCES` frozen — the `pilgrim` source row is the volume's declared deferral, V33), `beliefMap.js` 781/800, `religiousContest.js`, `religionState.js`, `envoyErrandVocabulary.js` (17b's). Worker bundle: 0 B (no WF or traditions file is on the generation graph — PRICING §A.3; quote the `stat`).

**NAMING (a law every WF brief obeys — CONFIRMED at `tests/lint/couplingInclusion.walker.test.js:186-207`):** the FAITH layer regex is `/^src\/domain\/worldPulse\/(?:faith|sacred|religion|pantheon|conversion|piety|deity|temple)/` plus the exact-path row for `patronFall.js`; a new leaf named `pilgrimSeason.js` (the volume's spelling) matches NOTHING and lands as a NEW UNLAYERED module against a coupling baseline with zero headroom (the WF-1a finding, verbatim in the walker's comment `:188-191`). RULING: the leaf is `src/domain/worldPulse/faithPilgrimSeason.js`; the tuning table keeps the volume's name `PILGRIM_SEASON_TUNING`. The volume's spelling is a RECORD≠CODE row the chair carries (R6).

---

## THE STATE OF THE RECORD

- **The history pass HAS run for WF** (`…/recon-32-34/HISTORY-WF-2026-09-15.md`, four seats + the folder's re-execution) and WINS over PRICING §B. Its WF-2 row (§A.2, CONFIRMED): UNBUILT; none of the wave's leaves exist (`find src -name '*ilgrim*'` = one file, `traditions/pilgrimage.js`); the substrate is stronger than TRADE's on three counts — (1) SP-1's spine is built with both WF-2b rows reserved in code (17b's); (2) the pilgrimage DRAW is LIT under TRADITIONS' name — `027e4e4ce` WC-e (07-19), `traditions/pilgrimage.js` mounted at `traditionsKernel.js:79/:726`, `traditionsEnabled: true` at `simulationRules.js:674` — and DEITY-BLIND (zero `deity|patron|faith|religio` tokens in the file — re-run by me: CONFIRMED), so WF-2a is a co-location gate + a season over a live draw, not a new draw; (3) ⭐ the believed-devotion ground truth is LIVE (`devotionGroundTruth` `beliefAxisSubjects.js:356-373` reads `religionStates[cid]` rewritten every tick) — THE TR-3-SHAPED JOIN CAR IS NOT OWED. The fold's grep traps adopted VERBATIM by reference (`pilgrim` is TRADITIONS + the peace term + the reserved errand row; `pilgrimSeason.js` does not exist; `W-F<n>` ≠ `W-FAITH F<n>c` ≠ `WF-<n>`; line numbers rot — navigate by symbol).
- **The volume** `docs/DESIGN_FP_ARCH_WF.md` §4 WF-2 (`:442-495`): "pilgrimage.js 41 eff (the subsumption seam — the scalar source switch is a ~5-line fork); NEW leaf `pilgrimSeason.js` (season composition over the existing draw math; budget ≤ 250 eff)"; WF-2a deltas — "the co-location gate is this wave's own composition (V24 verified the tree has NO faith-site linkage); believed-devotion input … the pull's belief leg reads the beliefMap observanceLabel substrate (V19) with the composition seam injectable (`beliefLegsFor`-style, the WR-10/CR-WR10-H precedent) so SP-2 supplies legs without touching the leaf"; pins — "roads-fill-at-the-feast on WF-0's authored preconditions; the THREE-WAY split negative — no-observance / aspatial / unmapped-or-no-neighbour each its own seeded arm; K3-shape import pin on pilgrimSeason.js; dormancy — dark ⇒ pilgrimage.js scalar byte-identical; ADD the subsumption-seam DISCLOSURE pin — lit vs dark observance-success delta measured on one seed (J-WF-13's addendum, quantified for WF-9)"; mutants — "season-band saturation (all bands → feast) reds the distribution pin; co-location gate mutant (patron requirement dropped) reds the three-way negative"; Req 13 "season pull is belief-side; alignment-empty with reason"; Req 14 "season is derived (no state ⇒ no verb — recorded)"; Collisions — "traditions lane (J-WF-13 disclosed shift at lighting); GRAMMAR (pilgrimage_right forms only with WF-2 lit)". §2 the flag row: `pilgrimageEnabled` | SPREAD + `errandSpineEnabled === true` | WF-2a | "pilgrimSeason.js's subsumption switch" — ⚠ THE PLAN SPLITS THE DOOR (§1 row 17a): `pilgrimSeasonActive(rules)` = spread ∧ key (17a's) and `pilgrimMoversActive(rules)` = the same ∧ `errandSpineEnabled === true` (17b's), so the season lights without SP-D — this brief adopts the split; the volume's single conjunct is a RECORD≠CODE row for R6. §3: "the pilgrim season (pure evaluator, never stored)". §5 seam 2 (TRADE): "tithe.js / pilgrimSeason.js import nothing from TRADE's internals (K3-shape, both directions with a guard-the-guard positive control)" — this lane's import pin. THE WF TUNING-HOME LAW (§2, chair-signed): the new leaf mints exactly ONE `PILGRIM_SEASON_TUNING` frozen table; `pilgrimage.js:32-37`'s six bare dials are "the measured counter-example, not the template" — this lane does NOT promote them (they are TRADITIONS' — soak-certified per the file's own header) and does NOT add a seventh bare dial.
- **The compiled programme** `docs/DESIGN_FP_ARCHITECTURE.md` §5 #34 WF-2 (`:1942-1953`): "2a THE SEASON: co-location gate (genuinely new); the pull's belief leg reads SP-B's devotion family through an injectable composition seam (landed by order — the observanceLabel substrate is the declared dark arm); the three-way split negative; the subsumption-seam disclosure pin"; the flag table row 24 (`:485`).
- **The annex** `docs/content/RECEIPT_POOLS_FAITH.md` §E WF-2 (`:248-427`): the six season kinds this lane voices — `faith.season.trickle` (`:250`, routine) · `faith.season.steady` (`:264`, routine) · `faith.season.feast` (`:278`, notable) · `faith.season.flood` (`:289`, major) · `faith.season.feast_kept` (`:298`, arc transition, notable) · `faith.season.roads_closed` (`:309`, arc transition — the counterforce, notable) — all Herald `faith`; plus `faith.dossier.season_line` (`:412`, FaithSection dossier line, `section: null`). The annex's own preface: "kind ids below are DESCRIPTIVE placeholders for the census — the canonical spelling of every id is minted by the wave that mints the kind, and registered in WHAT_PHRASES + heraldRouting there" (`faithReceiptPools.js:24-30` quotes it). CANONICAL SPELLINGS RULED HERE: `faith_season_trickle`, `faith_season_steady`, `faith_season_feast`, `faith_season_flood`, `faith_season_feast_kept`, `faith_season_roads_closed`, and the dossier line `faith_season_line` (`section: null`).
- **The ODQ anchors** (locate with `grep -n '^## §N'` on the ledger's `docs/OWNER_DECISION_QUEUE.md`): §78 (`:2987`) THE WF FABLE ROUND — F-1 discharged WF-2b's SP-1 precondition (17b's); §78.2 F-5 THE WF TUNING-HOME LAW; §308 (`:12908`) THE WF FAMILY LAW (trains cap at EIGHT); §350 (`:14676`) R-1 "the one-row FAITH kind registry lands honestly, with the shrink-back review OBLIGATED on the next WF-8 member" — ⚠ THIS LANE IS THE FIRST WF MEMBER TO TAKE FAITH PAST FIVE ROWS (1 → 8 with car 3), so the shrink-back is PAID HERE (the plan seated it at 21a; the code decides — `tests/lint/kindPoolFloors.walker.test.js:236-247` says "strike FAITH from this list in that same commit" of "the member that takes this family to five rows or more"); §333.2 Q4 (the DS-FTH corpus binds as reader-side spelling law — the dossier line's `season` spelling is bound by DS-FTH-3 `:4670`'s "× the pilgrim season band").
- **The owner's laws:** everything built lands LIT (16:5x) — the season is a DECLARED SHIFT for the traditions layer in the two faith-lit presets on deity-bearing worlds (J-WF-13); the revert after the terminal soak empties the cohorts; nothing pushes/deploys/applies/deletes/spends; tuning raw + derivation, ratified at the soak; the golden master never re-recorded.

## THE STATE OF THE CODE (CONFIRMED at the consist unless marked)

| Module | eff/ceiling | What is there |
|---|---|---|
| `src/domain/traditions/pilgrimage.js` | 41/800 | header law `:1-22` (a READ that modulates the HOST's outcome; NEVER writes visitor settlements; aspatial ⇒ 0; pure); six bare dials `:32-37` (`PILGRIM_ACTS {procession, offering, fair}`, `GRAND_SCALE_MIN 3`, `SPECTACLE_SCALE 5`, `CANDIDATE_POOL 8`, `POP_ANCHOR 6000`, `DRAW_HALF 2`, `PILGRIM_MAX 0.1`); `drawsPilgrims(rec)` `:59-63`; `pilgrimageDraw({ hostId, hostRec, settlements, digest })` `:82-107` — the SCALAR SOURCE is the last line `return PILGRIM_MAX * (raw / (raw + DRAW_HALF))` `:106`; zero faith tokens (deity-blind) |
| `src/domain/worldPulse/traditionsKernel.js` | 484/800 | `import { pilgrimageDraw }` `:79`; the digest resolved ONCE per tick `const spatialDigest = activeSpatialDigest(worldState)` `:657`; the observance pass — `const pilgrimBonus = pilgrimageDraw({ hostId: sid, hostRec: rec, settlements: items, digest: spatialDigest })` `:726`, `score = clampNum(successScore(...) + pilgrimBonus, SCORE_MIN, SCORE_MAX)` `:727`, the keyed draw `outcomeForDraw(score, r)` `:733`; effects via bounded accumulators (`prosperityDeltas`, `fairTradePulse` `:740`, `LEGITIMACY_HIT` `:745`); the tradition-outcome chronicle beat builder `:469-490` (the SITE the season receipt lands beside); mounted at `pulseKernel.js:2619` (a name swap, not a WF line) |
| `src/domain/worldPulse/beliefAxes.js` · `beliefAxisSubjects.js` | 112 · 179 | `observanceLabel` — "CULTURAL: believed dominant rite `${motif}:${patronOrNull}`" `beliefAxes.js:11`, ground truth `observanceFromTraditions(s.traditions)` `:170`, folded at `:239-241`; `DEVOTION_BANDS` `beliefAxisSubjects.js:115` (`secular, lukewarm, observant, faithful, devout`); `devotionGroundTruth(religionState)` `:356-373` — LIVE; the belief record's `devotionBand` exists only where `believedDevotionEnabled` is lit AND `infoMode !== 'omniscient'` (`beliefMap.js:204-208 beliefsActive` — `dramatic_campaign` is omniscient, so in that preset the belief leg is ALWAYS the substrate arm; DECLARED) |
| `src/domain/worldPulse/religiousContest.js` | 493/800 | `isSubsystemActive(snapshot,'religion')` outer gate `:547`; `const spread = isFaithSpreadEnabled(rules)` `:553`; `patronSnapshot(state)` — the host's PATRON creed for the co-location gate is read off `worldState.religionStates[cid].patronRef` (the LIVE spelling — never `settlement.religionState`, the dead pointer) |
| `src/domain/worldPulse/faithTermExecutors.js` | lane 5's (≤ 300) | `pilgrimRoadFor(worldState, grantorId, granteeId, tick)` → `{ granted, refusal: 'pilgrimage_season_dark' }` while the season is dark (`brief-5` car 2) — THIS lane flips the arm |
| `src/domain/worldPulse/faithNews.js` | 73/800 | `faithKindRow(kind, significance, audience, desk, requiredSlots)` `:118`; `FAITH_KIND_REGISTRY` ONE row `faith_last_altar_dark` `:118-124`; `FAITH_KINDS` `:127`; `FAITH_NEWS_TUNING` `:140`; `faithLine(kind, seedKey, slots)` the keyed `hash01` picker; `faithReceipt(...)` `:209-240` — `candidateType: kind` (a CONSTANT, not a literal — see THE EVIDENCE LAW below); the header's registry shrink-back obligation `:99-107` |
| `src/domain/worldPulse/faithReceiptPools.js` | 9/800 | `FAITH_RECEIPTS` — annex-VERBATIM (§D WF-1 block), "a wave-scoped corpus gets a wave-scoped file" `:9-12` — THIS lane creates the sibling `faithSeasonReceiptPools.js` (§E's six season pools) |
| `src/domain/display/settlementRumors.js` | 555/800 | `WHAT_PHRASES` `:123` (the faith block `:316-329`); `WHAT_STRIP_PREFIX` includes `faith_` `:500`; `whatPhrase` `:566` refuses unregistered tokens; `TRANSITION_KINDS` `:493` |
| `src/domain/realm/heraldRouting.js` | 273/800 | `EXACT_SECTION` faith rows `:140-159` (18 exact + 3 tier keys; `faith_last_altar_dark` `:158` with the `faith_`-prefix refusal recorded `:154-157`); `PREFIX_RULES` `['faith_', 'faith']` `:388` — a phrased, desk-bearing kind takes an EXACT row (the registered-minus-routed honesty check, `kindPoolFloors.walker:163-169`); an exact row WITHOUT a registry pool grows `LEGACY_UNVOICED_TOKENS` (shrink-only, 274 — `:406-407`) ⇒ every WF kind that takes an exact row MUST be a registry row with its pool |
| `tests/helpers/kindRegistryRoster.js` · `tests/lint/kindPoolFloors.walker.test.js` | — | `KIND_REGISTRIES` (FAITH at `:64`); `KIND_REGISTRATION_FREEZES.routedTokens` (378 — `kindPoolFloors:197`) · `REGISTERED_KIND_COUNT = 115` (`:208`); the < 5 exception list with FAITH and its shrink-back obligation `:236-247`; the registered-minus-routed divergence asserted at 8 |
| `tests/lint/faithKindPools.walker.test.js` | — | the ONE-kind census template (`EXPECTED` `:70-72`, `SECTION '## §D WF-1'`/`UNTIL '## §E WF-2'` `:76-77`, the sentinel slot probe, `receiptAnnexPool` through `tests/helpers/receiptAnnex.js` — ⚠ NO `FAITH_ANNEX_URL` export exists there yet (`:46-69` has WAR/GRAMMAR/TRADE/INFORMATION); the WF-8A walker built its own `ANNEX_URL` `:78` — this lane's walker does the same or adds the export ONCE) |
| `tests/domain/impactKindWalkers.test.js` · `tests/lint/newsSubjectVocabulary.walker.test.js` | — | every minted `impactKind`/`candidateType` LITERAL must have a `WHAT_PHRASES` row and an `EXPECTED_VOICE` classification; a mint through a CONSTANT (`candidateType: kind`) is CONVICTED unless a manifest row names the registry that governs the site — the WF-8A packet paid this for `faithReceipt`; this lane's builders spell their kinds as LITERALS (the evidence law prefers it) |
| `tests/property/traditionsDormancyGolden.test.js` | — | drives LITERAL rules `{ warLayerEnabled: false, traditionsEnabled: true }` `:77` — `pilgrimageEnabled` ABSENT ⇒ the season is dark ⇒ the traditions golden MUST NOT MOVE at any car (a move = a byte-identity break in the fork — STOP) |
| `src/domain/worldPulse/simulationRules.js` | 352 | `pilgrimageEnabled` ZERO hits (NEW mint); `SIM_LIT_FAITH_SPREAD` exists (16a car 0), carrying `faithTermsEnabled` (lane 5) and `faithStanceConsequencesEnabled` (16b) — CHECK-GIT-FIRST |
| ABSENT | — | `faithPilgrimSeason.js`, `faithSeasonReceiptPools.js`, `faithSeasonKindPools.walker.test.js`, `faithPilgrimageDormancyFence.test.js`, `pilgrimageEnabled` |

**Budgets (PROTO §4.1):** worker 1,404,493 EXACT untouched (quote the `stat`); `engine` chunk ±0 expected (traditions/worldPulse leaves ride the advance worker; `settlementRumors.js` reaches first paint through React imports — PRICING §A.3 names it — so SIX phrase strings ≈ 200–300 B EAGER (PLAUSIBLE) against the UNMEASURED margin: quote the first-paint table at car 3 and STOP if the closure exceeds `CLOSURE_BUDGET_BYTES`; the chair's A0b headroom act is the cure, never a raise); the register string + cohort line ≈ 50–100 B eager (PLAUSIBLE).

## ALREADY DONE (do not re-land)

The pilgrimage DRAW whole (`pilgrimage.js`, soak-certified dials, mounted, LIT under `traditionsEnabled`) · the spatial digest resolution once per tick (`traditionsKernel.js:657`) · the observance success/outcome machinery and its bounded effects · SP-B's `DEVOTION_BANDS` + live `devotionGroundTruth` + the belief record's `devotionBand` (lit by lane 5) · the `observanceLabel` cultural axis (D-1) · the FAITH kind registry family + its walker shape (WF-8A) · the `pilgrimage_right` catalog row (GR-3a) and its refusal arm (lane 5 car 2) · the six season pools AUTHORED in the annex (2026-08-02, verifier-passed) — this lane TRANSCRIBES, never authors.

---

## THE CARS

Every car: one gated commit; anchored negatives (`// anchored:`); executed mutants manifest-rowed; NUL-scanned files; porcelain 0; `Seat: Opus 5 — Fable-unvalidated` + `Lane: 17a`; PACKET_STANDARD caps; the packet `docs/implementation/packets/fp/WF-2A.md` in the dock (WF runs packets — the WF-8A packet `docs/implementation/packets/fp/WF-8A.md` is the shape: Status alone on its line, verified base, preamble by SHA-256, design authority, train/member, census authority).

### Car 1 — `faithPilgrimSeason.js`: the pure season evaluator (co-location gate · season bands over the live draw · the injectable belief leg · `PILGRIM_SEASON_TUNING`) with its door, and THE DARK MINT of `pilgrimageEnabled`

**In plain words:** today a grand festival draws a fixed trickle of visitors from its neighbours no matter whose god is honoured; this car builds the module that decides, for a festival in a town that keeps a patron faith, whether the neighbours who share (or are believed to share) that faith turn the trickle into a season — a steady flow, a feast, a flood — as a pure calculation with no memory, behind a new switch that is registered but off, with the certification row the estate requires in the same commit. Nothing is wired yet.

**The change.** NEW `src/domain/worldPulse/faithPilgrimSeason.js` (≤ 250 eff; the name matches the FAITH layer regex — NAMING above): `export function pilgrimSeasonActive(rules)` — THE ONE by-name read `rules?.pilgrimageEnabled === true && isFaithSpreadEnabled(rules)` (`religiousContest.js:536-553`'s idiom; the conjunct is the reader, never a second flag); `PILGRIM_SEASON_TUNING = Object.freeze({ BAND_FLOORS: [<trickle>, <steady>, <feast>, <flood>], SEASON_CAP: <raw>, FAITH_WEIGHT: <raw>, ROADS_CLOSED_WAR_FLOOR: <raw> })` — every value RAW with an EXECUTED DERIVATION in the comment (THE WF TUNING-HOME LAW): `SEASON_CAP` derived as a multiple of `PILGRIM_MAX 0.1` (imported by name from `pilgrimage.js` — the season SUBSUMES the scalar, so the cap is stated relative to it and can never silently exceed `SCORE_MAX` clamping: e.g. `3 × PILGRIM_MAX` so a flood lifts a score by at most 0.3 — the derivation says why); the four band floors derived from the draw's own soft-saturation curve (`raw / (raw + DRAW_HALF)` at raw = 0.5, 1, 2, 4 neighbours-of-anchor); `pilgrimSeasonFor({ hostId, hostRec, settlements, digest, religionStates, beliefLegsFor })` → `{ band: 'trickle'|'steady'|'feast'|'flood'|null, scalar: number, reasons: string[] }` — (a) THE CO-LOCATION GATE: the host carries a patron creed (`religionStates[hostId]?.patronRef` — the LIVE spelling; a host with no patron ⇒ `band: null, scalar: PILGRIM_MAX-identical` — the gate is the volume's V24 "genuinely new" composition); (b) for each reachable neighbour in the draw's own bounded pool (REUSE `pilgrimage.js`'s walk — export a `reachableNeighbours(...)` helper from it in car 2 OR recompute with the same three `distanceRead.js` reads `pathCost`/`isMapped`/`distanceWeight`; RULING: export the helper in car 2 — one walk, two readers), the BELIEF LEG `beliefLegsFor(observerId, subjectId)` returns the neighbour's devotion toward the host's creed: the SP-B `devotionBand` off the observer's belief record where `believedDevotionEnabled` is lit and the map exists (`beliefMap.js:225-231 beliefRecord`; `beliefsActive` false in omniscient presets ⇒ the fallback), else the `observanceLabel` substrate (does the neighbour's believed dominant rite name the host's patron? `${motif}:${patron}` — `beliefAxes.js:11`) with the receipt word `substrate`; (c) the season = the draw's `raw` re-weighted by `FAITH_WEIGHT × legWeight(band)`, banded by `BAND_FLOORS`, the scalar = `min(SEASON_CAP, PILGRIM_MAX × bandMultiplier)`; (d) PURE — no store, no `Date`, no `Math.random`, no rules read except the door; (e) THE ROADS-CLOSED COUNTERFORCE (the annex's `faith.season.roads_closed`): a neighbour whose road to the host crosses an active war edge or a `tradeWar` hostile pair contributes ZERO (the pure read of the relationship graph / `warPosture` the kernel hands over — VERIFY-AT-BUILD which snapshot field carries "at war" at this seam; the TRADE seam law says the leaf imports nothing from TRADE's internals, so read the RELATIONSHIP, never `tradeWar.js`). THE DARK MINT in the same commit (ADDENDUM R3 as amended — `tests/lint/engineGatedRuleKeys.walker.test.js:538-541 scanGateReads` reads every `src/` file; `unaccountedReads` `:501-502` reds a by-name read without a register entry; `manifestWithoutRow` `:518` reds the entry without its row; `tests/domain/subsystemRowsVirtual.test.js:646-647` pin the ordered `VIRTUAL_RULES` and `VIRTUAL_PENDING_RULE_KEYS = []`; `tests/domain/contributionLedgerShape.test.js:118-119` pin both lengths): `simulationRules.js` register TAIL gains `'pilgrimageEnabled'` (comment: WF-2a · SPREAD lane · the two-preset roster + O-12 by symbol ("THE LIT HOME IS NOT THIS ENTRY") · DARK at this car · the door split: `pilgrimSeasonActive` here, `pilgrimMoversActive` (∧ `errandSpineEnabled`) in 17b); `src/domain/certification/subsystemRowsFaith.js` (lane 5 created it and the tail spread in `subsystemRowsVirtual.js`; CHECK-GIT-FIRST — if neither lane 5 nor 16b composed first, THIS lane mints the leaf + the tail spread `...FAITH_SUBSYSTEM_ROWS` after `...ENCOUNTERS_SUBSYSTEM_ROWS` `:584`) gains the `pilgrimageEnabled` row (25–35 eff; THE EVIDENCE LAW: `eventTypes` = the six season kinds AS LITERALS — this leaf's receipt builders (car 3) spell them `candidateType: 'faith_season_feast'` etc., so the lane leaf's `candidateType:` literals trace (the `LANE_LEAVES` MINT_RE scan at `subsystemRowsVirtual.test.js:825-828` reads the leaf); at THIS car the builders do not yet exist, so the row lands with `eventTypes: []` DELIBERATELY EMPTY AND MEASURED and car 3 fills it — say so in the row's `other`; `stateKeys: []` — the season is a pure evaluator (§3); `moverFamilies: []`; `other` names the door split, the two-preset roster, the traditions layer it subsumes, and the DATA ceiling (deity-bearing worlds only)); the three `subsystemRowsVirtual.test.js` edits (the key const; the `VIRTUAL_RULES` member IN POSITION at the tail — after lane 5's and 16b's; `LANE_LEAVES` entry `pilgrimageEnabled: ['src/domain/worldPulse/faithPilgrimSeason.js']`); BOTH `contributionLedgerShape.test.js:118-119` literals READ OFF HEAD at this car's base (+1 — 37 → 38 if lanes 5 and 16b composed first and no TR mint; NEVER inherited); the covering-array `union` re-measured (`flagDomainCensus` out of band; `governed`/`ungoverned` UNMOVED — a virtual key is never normalizer-governed); the four edge-shared files regenerated (any `simulationRules.js` byte stales `supabase/functions/_shared/aiCharterBundle*` + `aiOutputSchemaBundle*`: `cp -Rc` immer+seedrandom, `node scripts/build-edge-shared.mjs`, `grep -rl 'Users/cstokes' supabase/functions/_shared/` EMPTY, relink before any vite build). NEW `tests/property/faithPilgrimageDormancyFence.test.js` in the DARK shape (fence 1: `traditionsKernel`'s observance pass on the bearing fixture under `traditionsEnabled: true, faithSpreadEnabled: true` with `pilgrimageEnabled` ABSENT ⇒ every observance outcome and `prosperityDeltas` byte-identical to the pre-car engine — computed in-file, no 64-hex literal (ADDENDUM R7: the register is FROZEN; a NEW fence computes both sides); fence 2 absent ≡ explicit false; fence 3 the leaf's `pilgrimSeasonFor` call count ZERO dark (a spy at the leaf boundary — the SP-B fence's `believedWorldAxesDormancyFence.test.js:294` shape) and MOVING lit — the lit twin is car 2's once the mount exists, so at car 1 fence 3 asserts ZERO both ways with the lit arm marked "mounted at car 2"; fence 4 gate polarity — exactly ONE `=== true` site (`faithPilgrimSeason.js`), the truthy-non-true probes `1`/`'true'`/`{}` dark). NEW `tests/domain/faithPilgrimSeason.test.js`: the evaluator over a hand fixture — no-patron host ⇒ `band: null` and `scalar === PILGRIM_MAX`-equivalent (the SUBSUMPTION identity: the season with no faith signal returns EXACTLY what the draw returns — pin by calling `pilgrimageDraw` on the same fixture and asserting equality to 1e-12); the four bands reached by four seeded neighbour sets; the SP-B leg vs the substrate leg on the same fixture (both arms driven; the substrate arm's receipt word); the roads-closed neighbour contributes zero; purity (two calls, same bytes).

**Negative controls (executed, restored md5-exact):** the door removed → fence 4 reds (the site census 1 → 0) and the engine-gated walker's `unaccountedReads` … no — REMOVING the read makes the entry an orphan: `manifestWithoutRow`'s sibling arm (a manifested key with ZERO gate reads — CR-WR10-C item 4's pending list) reds; state which arm caught it; the co-location gate dropped (any host banded) → the no-patron identity pin reds; `FAITH_WEIGHT` set to 0 → the four-band pin collapses to `trickle` and reds.

**Files (≈11 handwritten):** `faithPilgrimSeason.js` (NEW), `simulationRules.js`, `subsystemRowsFaith.js` (append; or mint + `subsystemRowsVirtual.js` if first), `tests/domain/subsystemRowsVirtual.test.js`, `tests/domain/contributionLedgerShape.test.js`, `tests/soak-harness/coveringArrayCoverage.test.js`, `tests/property/faithPilgrimageDormancyFence.test.js` (NEW), `tests/domain/faithPilgrimSeason.test.js` (NEW), `scripts/mutation-coverage-manifest.json`, `couplingRegistryFaith.js` (the FAITH→TRADITIONS READ row `read: 'src/domain/traditions/pilgrimage.js#pilgrimageDraw'`, `flags: ['pilgrimageEnabled']`, `owningWave: 'WF-2a'` — lands with the read it describes; `tests/lint/couplingInclusion.walker.test.js` needs NO pattern edit for `faithPilgrimSeason.js`; a `traditions/` → FAITH pair may be a NEW cross-layer pair absent from `tests/lint/.coupling-inclusion-baseline.json` — the registry row is what admits it, walker rule 1), the packet; regenerated: the four edge files. **Census bills:** the lighting refreeze on a clean tree as its OWN commit (two new test files); manifest ×2 (the fence with a planted regression — the door removed — `kind:'mutation'`; the evaluator test `kind:'mutation'` with the gate-dropped plant); the edge bundles. **Byte rule:** `simulationRules.js` EAGER (+≈30 B PLAUSIBLE) — quote the first-paint table from a real `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist`; worker `stat` unchanged; engine line ±0. **Gate:** the full list (GATES below) + the engine-gated walker (directions 1–3; `ENGINE_GATED_DORMANT_RULE_KEYS` CONTAINS `pilgrimageEnabled` — measured), `subsystemRowsVirtual.test.js`, `contributionLedgerShape.test.js`, `coveringArrayCoverage.test.js`, the two edge freshness tests, `subsystemCertificationTotality.walker`, `traditionsDormancyGolden` (UNMOVED — asserted), `couplingInclusion.walker`. **LIT act:** none (DARK mint). **Exit:** the evaluator pure and pinned with the subsumption identity; the mint registered dark with its row; both literals read off HEAD and stated; the traditions golden unmoved.

### Car 2 — the subsumption fork at the scalar source and the once-per-tick season in `traditionsKernel.js`; the `pilgrimage_right` refusal flips to a live grant; the fence's lit twin; the THREE-WAY negative, roads-fill-at-the-feast and the DISCLOSURE pin

**In plain words:** the festival machinery now asks the new module for its season before scoring a grand festival — but only when the switch is on and only where the town keeps a faith; where the switch is off, the old fixed trickle is returned byte for byte. The treaty engine's "your pilgrims may travel our roads" promise, honestly refused until now, becomes a real grant. And the lane measures, on one seeded world, exactly how much the season changes festival outcomes — the number the convergence lane will later hold the family to.

**The change.** `src/domain/traditions/pilgrimage.js` (~5 lines of 41): `pilgrimageDraw({ hostId, hostRec, settlements, digest, seasonScalar = null })` — the scalar source `:106` becomes `const cap = Number.isFinite(seasonScalar) ? seasonScalar : PILGRIM_MAX; return cap * (raw / (raw + DRAW_HALF));` — ABSENT ⇒ byte-identical (the SUBSUMPTION: the season REPLACES the fixed cap, never adds beside it — J-WF-13); plus `export function reachableNeighbours({ hostId, settlements, digest })` extracted from the body (the walk `:92-97`, called by both the draw and the season — one walk). `src/domain/worldPulse/traditionsKernel.js` (≤ 8 lines): beside `:657` — `const season = pilgrimSeasonActive(worldState?.simulationRules) ? { religionStates: asObject(worldState?.religionStates), beliefLegsFor: beliefLegsOf(worldState) } : null;` (resolved ONCE per tick; `beliefLegsOf` = the injectable seam — the WR-10/CR-WR10-H `beliefLegsFor` precedent, `sovereigntyMarketStage.js:407` — built in the WF leaf, reading `beliefRecord` where `beliefsActive` else the substrate); at `:726` — `const seasonRead = season ? pilgrimSeasonFor({ hostId: sid, hostRec: rec, settlements: items, digest: spatialDigest, ...season }) : null; const pilgrimBonus = pilgrimageDraw({ hostId: sid, hostRec: rec, settlements: items, digest: spatialDigest, seasonScalar: seasonRead?.scalar ?? null });` — dark: `season === null` ⇒ `seasonScalar: null` ⇒ the draw byte-identical; the season RECEIPT is car 3's (the builder does not exist yet — this car carries the `seasonRead` into the tradition beat's local scope and mints nothing). `src/domain/worldPulse/faithTermExecutors.js` (lane 5's leaf, ≤ 6 lines): `pilgrimRoadFor` returns `{ granted: true }` when `pilgrimSeasonActive(rules)` holds and the term stands (`grantedRightFor` compliance door), else the existing refusal `pilgrimage_season_dark` — lane 5 car 2's refusal pin is RE-RUN as this car's POSITIVE CONTROL (the same fixture, key lit ⇒ granted) — and the grant's consumer is the season's roads-closed read (a granted `pilgrimage_right` OVERRIDES the roads-closed zero for that pair — the term's whole fiction; ≤ 3 lines in the leaf). `tests/property/faithPilgrimageDormancyFence.test.js` gains the LIT TWIN (fence 3: `pilgrimSeasonFor` call count MOVES under a LITERAL bag `{ traditionsEnabled, faithSpreadEnabled, pilgrimageEnabled: true, believedDevotionEnabled, beliefAxesEnabled }` on the bearing fixture over a full year — the festival window must open: drive `advanceTraditions` through the real kernel across the host observance's `window`).

**Pins (`tests/domain/faithPilgrimSeason.mount.test.js`, NEW):** ROADS-FILL-AT-THE-FEAST — on `faith_bearing_v1` (the patron host's grand observance + a mapped kindred neighbour) under the lit literal bag the host's observance outcome distribution over 16 seeds shifts toward TRIUMPH/GOOD vs the dark run (quote both histograms) and `seasonRead.band ∈ {feast, flood}` on ≥ 1 seed; THE THREE-WAY SPLIT NEGATIVE, each its own seeded arm: (i) NO OBSERVANCE (the host's `scaleBand` lowered to 2 — `drawsPilgrims` false ⇒ `band: null`, the draw 0, outcomes byte-identical lit vs dark), (ii) ASPATIAL (`spatialDigest` null — the digest deleted from the fixture ⇒ 0, byte-identical), (iii) UNMAPPED-OR-NO-NEIGHBOUR (the neighbour unmapped in the digest ⇒ 0, byte-identical); THE DISCLOSURE PIN — on ONE seed, `observanceSuccessDelta = mean(score_lit) − mean(score_dark)` over the host's observances is computed and asserted within `[0, SEASON_CAP]`, and the NUMBER is written into the test's own receipt (`tests/fixtures/faith-season-disclosure.json` — an INSTRUMENT, not a golden: the value is re-derived every run and the file records it for lane 22; it is NOT on the freeze register — say so in the file's `_doc`); the K3-SHAPE IMPORT PIN — `faithPilgrimSeason.js` imports nothing from `src/domain/spatial/(tradeFlow|commodityFlow|supplyShipments|entrepots|dispatchEV|smuggle|seaLanes)` nor `worldPulse/(tradeWar|routeNetwork|merchant|commercial)` (the TRADE seam law §5-2), with the guard-the-guard positive control (a planted import in a temp copy is SEEN); the `pilgrimage_right` GRANT arm (lane 5's refusal pin inverted) and the override (a roads-closed pair with a signed `pilgrimage_right` contributes non-zero). **Mutants:** season-band saturation (every band → `flood`) reds the distribution pin (the histogram collapses); the co-location gate dropped reds arm (i)'s byte-identity? no — reds the no-patron identity (car 1) AND makes a patron-less host banded here — assert both; the fork's `seasonScalar` ignored (the cap hard-wired to `PILGRIM_MAX`) reds the lit twin (fence 3 moves but the outcome does not — the anti-vacuity of the fork itself).

**Files (≈7):** `pilgrimage.js`, `traditionsKernel.js`, `faithTermExecutors.js`, the leaf (`beliefLegsOf` + the override ≤ 30 lines), the fence (lit twin), the mount test (NEW), the disclosure receipt (NEW fixture), `couplingRegistryFaith.js` (the FAITH→GRAMMAR `pilgrimRoadFor` row; the TRADITIONS→FAITH MOUNT row `read: 'src/domain/worldPulse/traditionsKernel.js#advanceTraditions'` — the kernel now imports a FAITH leaf: a NEW cross-layer pair the registry row admits). **Census bills:** refreeze own commit; manifest ×2 (the mount test; the fence's lit twin — a planted `seasonScalar` ignore). **Byte rule:** build quoted; worker 0; engine ±0; `traditionsKernel.js` eff stated (484 → ≤ 492). **Gate:** full list + `traditionsDormancyGolden` (UNMOVED — the golden drives `traditionsEnabled` only; a move is a STOP), `worldpulseDeityGolden` (drives `faithSpreadEnabled` LITERAL rules without `pilgrimageEnabled` — UNMOVED), `pactFormationDormancyFence` (lane 5's leaf edited — fence 1 dark identity holds), `namedPersonTransitTotality.walker` (the leaf prices NO leg — it reads `pathCost`/`distanceWeight` only, which are NOT discovery tokens; assert the walker green unchanged). **LIT act:** none. **Exit:** the fork byte-identical dark (fence 1) and moving lit (fence 3); the three-way negative green; the disclosure number recorded; the grant flips; both goldens unmoved.

### Car 3 — THE KINDS CAR: six season kinds + the dossier season line join the FAITH registry with their annex pools; the receipts mint beside the tradition beat; the §350 R-1 shrink-back PAID (FAITH leaves the < 5 exception list)

**In plain words:** the season now speaks. Six kinds of news — a trickle of pilgrims, a steady flow, a feast, a flood, a feast that was kept, and roads closed by war — get their authored sentences (written in August and verified then; this car copies them, it does not write them), their place on the Herald's faith desk, their phrase in the town rumour vocabulary, and their own test walker; and the town dossier gains one season line. Because this takes the faith news family from one kind to eight, the estate's rule that a family under five kinds is a visible exception is retired for faith in the same commit, as the rule itself demanded.

**The change.** NEW `src/domain/worldPulse/faithSeasonReceiptPools.js` (≤ 120 eff; ANNEX-VERBATIM transcription of `RECEIPT_POOLS_FAITH.md` §E's six season pools `:250-319` + the dossier line `:412-427` — every line byte-identical to its authored variant with `{slot}` → interpolation, the `faithReceiptPools.js:1-40` header law copied: "a corpus defect is a chair annex act, never an edit to this file"). `src/domain/worldPulse/faithNews.js` (73 → ≤ 130): `FAITH_KIND_REGISTRY` gains SIX rows through `faithKindRow(kind, significance, audience, desk, requiredSlots)` — `faith_season_trickle` (routine, public, faith), `faith_season_steady` (routine), `faith_season_feast` (notable), `faith_season_flood` (major), `faith_season_feast_kept` (notable), `faith_season_roads_closed` (notable) — significance/desk/slots READ OFF THE ANNEX HEADINGS (`### faith.season.<x> (WF-2a) — Herald faith — significance: <s>` + its `SLOTS:` line), never predicted; plus ONE `section: null` DOSSIER row `faith_season_line` (the IN-1c-a `mirror_standing_line` road: registered, NOT routed — raises the registered-minus-routed divergence by exactly one, 8 → 9, asserted at its new value); `FAITH_RECEIPTS` becomes the union of the two pool files (or `faithLine` takes the pool by kind from a `POOLS_BY_KIND` map — RULING: the map, so each wave's pool file stays wave-scoped). THE REGISTRY GROWTH RULE (binding on every later WF kinds car): `faithNews.js` is a LEAF under PACKET_STANDARD's ≤ 250 eff; the first WF kinds car whose rows would carry it past 250 FIRST extracts the row table to `src/domain/worldPulse/faithKindRows.js` (a pure data leaf; byte-neutral; its own commit) — PREDICTED at lane 19b (rows 1 + 6 + 7 + 11 + 2 = 27 ≈ 235 eff before 19b's eight); this car stays under (≈ 130). THE RECEIPT BUILDERS in `faithPilgrimSeason.js` (+≤ 40): `seasonReceipt({ kind, cid, settlementName, creedName, neighbourName, tick, slots })` — ONE builder per kind spelling `candidateType: 'faith_season_feast'` etc. AS A STRING LITERAL (THE EVIDENCE LAW — the `LANE_LEAVES` MINT_RE scan and `impactKindWalkers`/`newsSubjectVocabulary` both read literals; a `candidateType: kind` constant would owe a `newsSubjectVocabulary` manifest row instead — the WF-8A road, refused here because the row's `eventTypes` must trace); `curationClass: 'transition'` for `feast_kept`/`roads_closed` (arc transitions — `pulseHelpers.js`'s vocabulary, so `isDriftOnlyOutcome` is false and the beat reaches the feed), the season bands `curationClass` per the metronome's own classes (VERIFY-AT-BUILD `pulseHelpers.js`); `ruleFamily: 'religion'`, `ruleId: 'religious_pilgrim_season'`; the `{npc}`/`{route}` slots supplied only where the seam can (the WF-8A eligibility discipline: a variant whose named evidence is absent is skipped — `requiredSlots` parallel to the pool; ⚠ the annex's season pools name `{route}` and `{counterpart}` — the seam supplies `counterpart` (the top neighbour by draw) and `route` ONLY where the digest names a route ref (VERIFY-AT-BUILD `distanceRead.js`'s route-ref read); the walker pins the eligible set BOTH ways). THE MINT SITE: `traditionsKernel.js` (+≤ 4 lines) beside the tradition-outcome chronicle beat (`:469-490`'s consumer inside the observance pass): `if (seasonRead?.band) outcomes.push(seasonReceipt({...}))` — one receipt per observance per year at the band's significance; the arc transitions (`feast_kept` when the SAME host's band is `feast`+ in two consecutive years — read off the tradition record's `lastHeldYear`/the prior season word carried on the pulse history, NEVER a new state key; `roads_closed` when a kindred neighbour's contribution dropped to zero this year on the war read) minted at most once per host per year. `src/domain/display/settlementRumors.js` `WHAT_PHRASES` gains six rows in the faith block (`faith_season_trickle: 'a trickle of pilgrims on the road'` … — the phrase is the annex's exemplar noun phrase; digits never — prose-numerics); `src/domain/realm/heraldRouting.js` `EXACT_SECTION` gains SIX exact rows on `'faith'` beside `faith_last_altar_dark :158` with the refusal comment (the `faith_` prefix would route them free; the exact row is what keeps the registered-minus-routed census honest for a desk-bearing registered kind — `kindPoolFloors.walker:163-169`); `faith_season_line` takes NO routing row (dossier line, `section: null`). `tests/helpers/kindRegistryRoster.js` `KIND_REGISTRATION_FREEZES.routedTokens` 378 → 384 (MEASURED from `heraldRouting.js`, never predicted — the capsule reads it; re-measure at the compose); `tests/lint/kindPoolFloors.walker.test.js` `REGISTERED_KIND_COUNT` 115 → 122 (the ordered `+1 at WF-2a: …` comment rows in the file's own idiom, one per kind, naming ROAD B for the six desk-bearing rows and the IN-1c-a road for the dossier line); the registered-minus-routed divergence 8 → 9 (asserted at 9 with the reason); ⭐ FAITH STRUCK from the `rows.length < 5` exception list (`:236-247` — "strike FAITH from this list in that same commit"): the §350 R-1 SHRINK-BACK, PAID; the review sentence written into `faithNews.js:99-107`'s header ("REVIEWED at §932 lane 17a car 3: the family reaches eight rows; the exception is struck") — the plan's 21a car 1 obligation (a) is DISCHARGED here; lane 21a VERIFIES. NEW `tests/lint/faithSeasonKindPools.walker.test.js` — the L6 five-joins walker in the `faithKindPools.walker.test.js` shape (ONE literal `describe`, straight-line literal `test` calls — the lighting-census discipline `informationKindPools.walker:14-19`): the exact seven-kind census (`EXPECTED` rows read off the annex), the pool re-derived from the document through `receiptAnnexPool` with `SECTION '## §E WF-2'`/`UNTIL '## §F WF-3'` and pinned IDENTICAL to the pool module, the sentinel slot probe (which `{slot}` markers survive per variant), the eligible set both arms (variants naming a slot the seam cannot supply are skipped — asserted per kind), the desk (`SECTION_OF(kind) === 'faith'` for six; `null` desk for the dossier line), `WHAT_PHRASES` presence, the ADDRESS CHAIN on a driven receipt (id + address chain + typed action + settlements by name + reason — L6's five joins: `id`, `candidateType`, `targetSaveId`/`affectedSettlementIds`, `headline`/`summary`, `reasons[0]`). `tests/domain/impactKindWalkers.test.js` `EXPECTED_VOICE` gains seven classifications (the season bands → a crier voice category the file's vocabulary offers for faith, or `null` — decide per kind and say why; the dossier line `null`). `tests/lint/heraldRouting.walker.test.js` green unchanged (every new literal explicitly routed); `newsSubjectVocabulary.walker` green (literals). The DS-FTH-3 spelling: the dossier line's composer reads "the pilgrim season band" (`RECEIPT_POOLS_DOSSIER_STATE.md:4670`) — VERIFY-AT-BUILD whether a DS-FTH composer exists for the season word (`warFaithStateProse.js`); if NONE, the dossier line is REGISTERED (the row) and its composer is DECLARED owed to the FaithSection (the IN-1c-a "no honest producer" posture — recorded, not hidden).

**Pins:** the walker's arms; `whatPhrase('faith_season_feast')` returns the row (not `UNREGISTERED_SUBJECT`); every receipt on the bearing fixture's lit year passes `heraldSectionOfRecord` → `'faith'`; the eligibility both ways. **Mutants:** a season kind dropped from `WHAT_PHRASES` → `impactKindWalkers` reds (the control, executed once); a pool line edited by one byte → the walker's annex-identity arm reds; the exact row removed for one kind → `kindPoolFloors`' divergence arm reds.

**Files (≈12 handwritten — AT the cap):** `faithSeasonReceiptPools.js` (NEW), `faithNews.js`, `faithPilgrimSeason.js` (builders), `traditionsKernel.js` (≤ 4), `settlementRumors.js`, `heraldRouting.js`, `kindRegistryRoster.js`, `kindPoolFloors.walker.test.js`, `faithSeasonKindPools.walker.test.js` (NEW), `impactKindWalkers.test.js`, `subsystemRowsFaith.js` (the row's `eventTypes` filled with the six literals), `scripts/mutation-coverage-manifest.json`. ⚠ If the count exceeds 12 at the cut, the R5 split: 3a = registry + pools + builders + mint + walker; 3b = the routing/phrase/freeze bills. **Census bills:** refreeze own commit (a new walker file; the WF-8A walker's titles unchanged); manifest (the walker `kind:'mutation'` with the planted pool byte); NO wiring census stamp (the pools are `src/` data, annex-verbatim — `docs/content/wiring-census.json` is the DOSSIER desk's; the dossier LINE lands as a registry row, its composer VERIFY-AT-BUILD — if a DS-FTH composer byte moves, `node scripts/wiring-census.mjs` re-stamps and `prose-manifest-cells.mjs --out/--diff/--record` with the cause); prose-numerics (words, never digits — `tests/lint/.prose-numerics-baseline.json` shrink-only). **Byte rule:** `settlementRumors.js` reaches FIRST PAINT (PRICING §A.3) — six phrase strings ≈ 200–300 B EAGER (PLAUSIBLE): quote the closure table; if `CLOSURE_BUDGET_BYTES` is exceeded, STOP and report (the chair's A0b headroom act is owed before ~the twentieth key — this may be the car that discovers it); worker 0; engine ±0. **Gate:** full list + `faithKindPools.walker` (WF-8A's — unchanged), `phrasedKindPools.walker`, `wizardNewsAuthoring.walker` (the traditions kernel is a governed authoring site — its census row moves by the new mint; re-measure its `byPath` figure), `kindPoolFloors.walker`, `heraldRouting.walker`, `heraldContaminationFence` (the builders read TRUTH — religionStates and the digest; the belief LEG is read by the SEASON not the RECEIPT — the receipt composer must not import `beliefMap.js`: keep `beliefLegsOf` in a sibling leaf `faithPilgrimSeasonLegs.js` if the fence's reachability arm reds — VERIFY-AT-BUILD; a composer that reaches a belief record is exactly what the fence forbids), `newsSubjectVocabulary.walker`, `impactKindWalkers`. **LIT act:** none. **Exit:** seven rows registered, six routed, pools annex-identical, the walker green with its five joins driven, FAITH off the exception list, the divergence at 9, `REGISTERED_KIND_COUNT` and `routedTokens` re-measured and stated.

### Car 4 — THE LIT CAR: `pilgrimageEnabled` → `SIM_LIT_FAITH_SPREAD` (two presets); the fence to the LIT shape; the witness; the shift ledger — BUILT AT LANDING ON THE CONSIST, rebased, after D0, lane 16a, lane 5 and lane 16b

**In plain words:** the pilgrim-season switch is turned on in the two living presets where faith already spreads between towns. The commit proves a world without gods is byte-for-byte unchanged, proves on the god-bearing world that festivals there now draw a season, writes down that this deliberately changes festival outcomes in those two presets (a change to a layer another programme built), and names the number by which it changes them.

**⛔ THE LAWS THIS CAR OBEYS BY CITATION (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`):** R1 — the two-preset cohort is lane 16a car 0's recorded extension of `SIM_LIT`'s law (the O-12 reason); the `:367` walker is D0's — run, quoted, never edited. R2 — composes after D0, 16a, 5, 16b; `traditionsEnabled` is lit in every lit preset (a mechanism fact, stated); a dock-green LIT car is a FALSE GREEN. R3 — the dark mint was car 1; this car LIGHTS and RE-READS every count off the composed HEAD. R6 — the report lists the volume rows below. R7 — the witness re-record goes through the signed door: the chair cuts `docs/shift-records/<§932-WF-2a>.json` (surfaces: `tests/fixtures/preset-lighting-witness-golden.json` `re-record`, predictedRows = the two lit rows); the implementer runs the capture arm under `GOLDEN_SHIFT_SIGNED=<path>` (the door THROWS by design; the plain re-run is the receipt) and commits fixture + register row + record with `Owner-Signed: §<row>`.

**The change:** (1) `simulationRules.js`: `SIM_LIT_FAITH_SPREAD` gains `pilgrimageEnabled: true, // WF-2a · lane 17a car 4 · SPREAD lane — two presets, O-12; the movers arm (17b) stays dark by its errandSpine conjunct` (CHECK-GIT-FIRST: lanes 5 and 16b's lines precede; write only what is absent); the register comment gains the LIT paragraph. (2) THE MOVERS, DECLARED BY NAME: the season scalar in `dramatic_campaign` and `full_simulation` on deity-bearing worlds whose patron host holds a grand observance with kindred/believed-kindred reachable neighbours; the belief leg is SP-B's `devotionBand` in `full_simulation` (the one lit preset with a belief map and spread) and the `observanceLabel` substrate in `dramatic_campaign` (omniscient — no belief map); the `pilgrimage_right` grant flips wherever a WF-6 pact carries it (`full_simulation`); the six season kinds reach the faith desk. (3) `tests/property/faithPilgrimageDormancyFence.test.js` → LIT shape: the roster arm ("VIRTUAL — absent from `DEFAULT_SIMULATION_RULES`; REGISTERED; `true` in exactly the presets where `SIM_LIT_FAITH_SPREAD` is spread == the presets where `isFaithSpreadEnabled(normalize(p.rules))` is true — computed from `SIMULATION_RULE_PRESETS`, never a literal; absent from the other five"); fence 1's dark world → `quiet_local.rules`; fence 3's lit twin under `full_simulation.rules` on the bearing fixture MOVES; `faithSpreadCohortFence` (lane 16a's — `pilgrimageEnabled` is on its `SPREAD_LANE_KEYS` allowlist; arm 3 must still hold). (4) `subsystemRowsFaith.js` the row's `other` amended LIT (two presets; the DATA ceiling sentence). (5) THE WITNESS by hand through the signed door (`measureWitness()`): `rulesSha256` + `litFlagCount` move on `dramatic_campaign` and `full_simulation` ONLY; every pulse hash UNMOVED on every row (the born worlds are deity-free — the co-location gate finds no patron ⇒ the draw returns its landed scalar — byte-identical); a moved pulse hash on ANY row is a STOP (the key leaked past the co-location gate — find the mover); a moved DARK row is a STOP. (6) THE TRADITIONS GOLDEN UNMOVED (it drives no `pilgrimageEnabled`) — asserted; ⚠ if any `*DormancyGolden` moves, it is a kernel change under a neighbour's key: STOP, the chair's bisect (ARCHITECTS-CARD §2 last row). (7) 525/525 unchanged (the generation classified diff); the espionage fence 21/21; `prose-manifest-cells.mjs --out` base/tip zero rows (no dossier prose byte in this car). (8) Covering-array figures re-measured; both `contributionLedgerShape` literals re-read off HEAD (car 1 paid at its base; a lane composed between moves them — re-derive); the edge bundles rebuilt. (9) THE SHIFT-LEDGER DRAFT (`docs/implementation/GOLDEN_SHIFT_LEDGER-WF-2a.draft.md`, PROTO §2.5 shape): *"`pilgrimageEnabled` lit on `dramatic_campaign`, `full_simulation` at landing <sha> (the SPREAD-lane roster; O-12 forbids the other two); `rulesSha256`/`litFlagCount` moved on those two witness rows; every pulse hash UNMOVED (deity-free born worlds — the co-location gate returns the landed scalar); THE TRADITIONS LAYER'S OBSERVANCE OUTCOMES MOVE in those two presets on deity-bearing worlds only (J-WF-13's disclosed shift: the season SUBSUMES `PILGRIM_MAX` — the measured one-seed delta is <n> from `tests/fixtures/faith-season-disclosure.json`); the movers arm stays DARK by its `errandSpineEnabled` conjunct; the `pilgrimage_right` grant flips where a WF-6 pact carries it; the generation dump 525/525; espionage 21/21; closure +<n> B; no installed campaign re-labelled (VIRTUAL); no tuning value moved (`PILGRIM_SEASON_TUNING` RAW with derivations, ratified at the soak)."*

**Files (≈8):** `simulationRules.js`, the fence, the witness golden (+ `tests/fixtures/.golden-freeze-register.json` row + the chair's record), `subsystemRowsFaith.js` (text), `coveringArrayCoverage.test.js` (if moved), `contributionLedgerShape.test.js` (if HEAD moved), the shift draft, the packet; regenerated: four edge files. **Census bills:** refreeze; manifest `kind:'rationale'` for the re-charter; bundles. **Byte rule:** closure +1 line quoted; worker unchanged; engine ±0. **Gate:** full list + the witness, `tests/domain/simulationRulesPreset.stability.test.js`, the two freshness tests, the engine-gated walker (`ENGINE_GATED_DORMANT_RULE_KEYS` LACKS the key), the totality walker, `tradeConvergenceContract.walker` (D0's arm + 16a's amendment), `faithSpreadCohortFence`, `goldenFreeze.walker` (R7 — the witness row bound), the espionage fence 21/21, `traditionsDormancyGolden` + `worldpulseDeityGolden` UNMOVED, the soak suites alone at load < 8. **LIT act:** the key. **Exit:** lit per roster; fence LIT-shape green with the twin moving; the witness diff quoted row by row; the shift draft with the disclosure number; zero UNDECLARED lines; no red this lane did not cause.

---

## THE FLAG-MINT CAR (last; the count from HEAD) — READ UNDER ADDENDUM R3

Law 2's LAST car is car 4 (the LIGHTING). The DARK MINT is car 1 (the leaf's door is the by-name read; `scanGateReads` reads every `src/` file; `manifestWithoutRow` forbids the entry without the row; the pinned `VIRTUAL_RULES`/`VIRTUAL_PENDING_RULE_KEYS`/both lengths drag the three test edits and both literals into car 1) — the forced departure from R3-as-first-written, recorded as every WF and TR brief records it. **At car 1**, before editing: `node -e "import('./src/domain/worldPulse/simulationRules.js').then(m => console.log(m.ENGINE_GATED_VIRTUAL_RULE_KEYS.length))"` and `VIRTUAL_SUBSYSTEM_ROWS.length` (35 at the consist; +1 lane 5; +1 lane 16b; + any TR mint) — the literals = that + 1, NEVER inherited from this brief. **At car 4** (rebased at landing) the same two commands re-run; the register tail, the two cohorts, `subsystemRowsFaith.js`'s tail, `couplingRegistryFaith.js`, the literals, the covering-array census, `REGISTERED_KIND_COUNT`/`routedTokens` (car 3's — a sibling family's kinds car may have moved them) are CHECK-GIT-FIRST and resolved by MEASUREMENT (PROTO §5.2), never OURS/THEIRS. The report states the numbers written at car 1 and car 3, re-read at car 4, and expected at the consist.

## THE VOLUME ROWS THIS LANE CHANGES (ADDENDUM R6 — the chair writes the dated LANDING BLOCK into `docs/DESIGN_FP_ARCH_WF.md` on the ledger from this list)

`§4 WF-2` slice 2a → "LANDED-LIT (§932 lane 17a; two-preset roster)" with the cars by sha; the leaf is `faithPilgrimSeason.js` (not `pilgrimSeason.js` — the `couplingInclusion` FAITH regex); the kernel call at `traditionsKernel.js:726` DID take lines (the once-per-tick season and the scalar hand-off — the plan's "unchanged" corrected); THE KINDS ARE THE WAVE'S (six season kinds + the dossier line registered HERE, per the volume's own WF-8 collision clause); `§2` the flag row → the DOOR SPLIT (`pilgrimSeasonActive` = SPREAD ∧ key; `pilgrimMoversActive` adds `errandSpineEnabled` — 17b's) and → LANDED-LIT (two presets); `§1b V19/V24` → the co-location gate composed; the belief leg's two arms named per preset; `§5 seam 2` (TRADE) → the K3 import pin landed; the §350 R-1 shrink-back → PAID at 17a car 3 (FAITH off the < 5 list at eight rows); the disclosure number (J-WF-13) recorded for WF-9; the history fold's WF-2 row folded.

## OWNER-GATED, KEPT OUT

- Tuning: `PILGRIM_SEASON_TUNING`'s floors, cap and weight — RAW with executed derivations, ratified at the soak; `pilgrimage.js`'s six bare dials are TRADITIONS' soak-certified values — UNTOUCHED (the tuning-home law's counter-example is not this lane's to cure).
- `routeNetworkFlows.js`'s `pilgrim` source row (`ROUTE_FLOW_SOURCES` frozen, V33) — the volume's declared deferral, re-affirmed.
- Any `pulseKernel.js`/`applyWorldPulse.js`/`beliefMap.js` byte; a ceiling raise; push/deploy/migration/deletion/spend; the golden master; the LIT SUCCESSOR preset id (the road to four presets).
- Persisted shapes widened: NONE (the season is a pure evaluator — §3; the disclosure receipt is an INSTRUMENT file, not world state, not on the freeze register) — recorded.
- The `errandSpineEnabled` lighting — 17b's CHECK-GIT-FIRST; NOT this sitting's.

## RISKS

**Grep traps (HISTORY-WF's paragraph after §B + RECHECK §5 + HISTORY-TRADE's twelve, VERBATIM by reference; the ones that bite here):** `pilgrim` is TRADITIONS (`pilgrimage.js`) + the GR-3a term `pilgrimage_right` + the reserved errand row — three senses in one grep; `pilgrimSeason.js` (the volume's spelling) does not exist and MUST NOT (the layer regex); `religionState` singular is a DEAD POINTER — the host's patron is `worldState.religionStates[cid].patronRef`; `faithProfile` on `settlement.config` is the pulse-minted PROJECTION (readable but a tick stale) — the season reads `religionStates`, the truth; `season` in `pactFormation.js`/`sovereigntyMarketStage.js` is the CALENDAR season word (`calendar.season`), not the pilgrim season; `observance` in the belief axes is `observanceLabel` (a rite label), not an attendance; `W-F<n>` ≠ `WF-<n>`.

**Collision surfaces:** the WF chain (`traditionsKernel.js` is TRADITIONS' — no other WF lane touches it; `faithTermExecutors.js` is lane 5's — ONE WF lane in flight); `faithNews.js` + `heraldRouting.js` + `settlementRumors.js` + `kindRegistryRoster.js` + `kindPoolFloors.walker` (EVERY family's kinds car moves the freezes — `REGISTERED_KIND_COUNT`, `routedTokens`, the divergence — resolved by MEASUREMENT at the compose, never by arithmetic on two briefs); `simulationRules.js` and the flag-mint surfaces; `subsystemRowsFaith.js`/`couplingRegistryFaith.js` (append-only); `tests/lint/.coupling-inclusion-baseline.json` (a new cross-layer pair admitted ONLY by a registry row — rule 1).

**The addendum, by path (cite, never re-derive):** `…/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` — R1 + lane 16a car 0's two-preset cohort (the fallback if the chair reads PROTO §1.1 strictly: this key lands DARK with its lighting priced to the successor-id act); R2 (after D0, 16a, 5, 16b); R3 (dark mint at car 1); R5 (car 3 sits AT 12 — the 3a/3b split is the plan if it exceeds); R6 (the volume rows above); R7 (the witness through the signed door).

**Load hazards:** `gate-mutex.sh` exits 0 after 40 polls — a gate line with no PRINTED test count DID NOT RUN; dist-gated size suites skip without `dist/`; `deno.lock` re-freeze before `--frozen`; `git grep` without a rev reads the INDEX; 120 faith suites in one vitest = exit 137 — run the faith property suites in two halves; a moved DRIVEN GOLDEN (`traditionsDormancyGolden`, `worldpulseDeityGolden`) is a STOP and the chair's bisect.

**Design hazards:** THE SUBSUMPTION IDENTITY — the season REPLACES `PILGRIM_MAX`, never adds beside it (a `+` instead of the cap swap double-counts the draw and moves the traditions golden the day the key lights); the co-location gate reads the host's PATRON (a settlement with cults only ⇒ no season — the doctrine's "absence of a faith is not a shared faith"); THE HERALD TRUTH LAW — the receipt composer must not reach a belief record (keep the belief LEG in the evaluator, the RECEIPT off truth-side fields; split the leaf if the contamination fence's reachability arm reds); the kinds are LITERALS in the lane leaf (the evidence law + two walkers); an exact `EXACT_SECTION` row for an UNREGISTERED kind grows `LEGACY_UNVOICED_TOKENS` (shrink-only) — every routed WF kind is registered with its pool; the dossier line is `section: null` (routing it would claim a desk it never reaches); `settlementRumors.js` is EAGER — six strings may be the byte that finds the closure ceiling (STOP, never squeeze).

## GATES (per car, verbatim)

focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators (NONE — state so) · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte (quote the worker `stat`, the `engine-*.js` line, the first-paint table) · `npm run check:quick` — every line with a PRINTED test count; the lighting refreeze on a clean tree as its OWN commit after any car adding a test file/title (cars 1, 2, 3, 4); the mutation manifest per new invariant file; no wiring census stamp unless a DS-FTH composer byte moves (car 3, VERIFY-AT-BUILD — then `node scripts/wiring-census.mjs` + `prose-manifest-cells.mjs --out/--diff/--record` with the cause); prose-numerics at car 3; the edge bundles at cars 1 and 4; the Seat/Lane trailer `Seat: Opus 5 — Fable-unvalidated` · `Lane: 17a`; porcelain 0. At car 1 (the dark mint) additionally the engine-gated walker (directions 1–3), `subsystemRowsVirtual.test.js`, `contributionLedgerShape.test.js`, `coveringArrayCoverage.test.js`, the two freshness tests, the totality walker, `traditionsDormancyGolden` UNMOVED. At car 2 additionally `pactFormationDormancyFence`, `namedPersonTransitTotality.walker`, `worldpulseDeityGolden` UNMOVED. At car 3 additionally `faithKindPools.walker`, `faithSeasonKindPools.walker`, `phrasedKindPools.walker`, `wizardNewsAuthoring.walker`, `kindPoolFloors.walker`, `heraldRouting.walker`, `heraldContaminationFence`, `newsSubjectVocabulary.walker`, `impactKindWalkers`. At car 4 (built on the consist after D0, 16a, 5, 16b — ADDENDUM R2(iii); never gated in the dock) additionally: the witness through the signed door, the stability test, the two freshness tests, the engine-gated walker, the totality walker, `tradeConvergenceContract.walker`, `faithSpreadCohortFence`, `goldenFreeze.walker`, the espionage fence 21/21, `prose-manifest-cells.mjs --out` base/tip, the generation classified diff 525/525 unchanged, both dormancy goldens UNMOVED, the soak suites alone at load < 8.
