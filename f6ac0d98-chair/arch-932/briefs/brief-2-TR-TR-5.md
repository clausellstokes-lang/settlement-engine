# THE LANE — PRICING §C lane 2 · TR-5 THE PACT LANE (`tradePactsEnabled`; car 0 the §96.2 charter re-cut; the lane that lights the belief spine)

Architect: the Fable seat (family TR), 2026-09-15, read-only at the consist `laneCONSIST-932` (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/4e3d2f70-f45f-4e14-b571-514c339cfa17/scratchpad/kit/laneCONSIST-932`, tip `315080928`) against the ledger worktree `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/ledger-a803dee7a`. CONFIRMED = opened at the cited line or skeptic-confirmed; PLAUSIBLE = reasoning. Line numbers rot; re-measure by SYMBOL. Protocols: `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS.md` (PROTO §n).

**Cars: 8 in this sitting (0, 1, 2, 3, 4, 6, 7a, 7b) + car 5 DETACHED into sitting 2b; range 8–9 with the repair allowance** (PRICING §B 4–6; HISTORY §A.4 5–7 "+1: the §96.2 charter re-cut is a mandatory car 0"; +1 because the LIT act lights FOUR keys and re-charters two foreign fences; +1 because that LIT act counted 13 handwritten files and ADDENDUM R5 makes the 7a/7b split THE PLAN, not a contingency). The ≤ 8 rule then detaches **car 5 (the Herald kinds) into a second sitting, 2b** — it is the one car nothing else in the lane depends on, it lights nothing, and it boards after 7b on the consist. **⚠ CORRECTED 2026-09-15 at the skeptic's five findings under `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`:** the LIT act boards after `casusCommerciiEnabled` is lit (R2); the virtuality law is cited, not re-derived (R1); 7a/7b (R5); the five-member namer set of `pactFormationDormancyFence.test.js:198-214` is stated exactly and the row/coupling-row consequence drawn; the mint/lighting split is read under R3 with the walker's one-commit law.

**Prerequisites (all in place for cars 0–6; 7a/7b wait on the door and on lane 7 car 1):** PRICING §A.4.3 (TR-5 boards FIRST on `peaceTerms*.js`; WF-6 boards when THIS lane's catalog car has landed on the consist; the T9 STOP on any second lane in flight on `peaceTerms*.js`) and §A.4.4 (the flag-mint car last — read under ADDENDUM R3, below) — adopted verbatim; the chair's spelling ruling on `route_wardenship` (recorded below); GR-2 (`a18fdcfae`), GR-3a (`0be4800db`), GR-3b, SP-B (`4c0f2f38b`), TR-1 (`d7ea69a4b`) landed; TR-4 is NOT a prerequisite (`grain_provision`'s physical arm rides TR-4 car 5's fork automatically when TR-4 lands; dark-TR-4 = the conserved rate-stream arithmetic `treatyTransfer.js` already runs — the ONE named degraded read, `DESIGN_FP_TRADE.md` §3 line 273; the contract records it as `TRADE_DEGRADED_ARMS[0]`, `tradeConvergenceContract.js:393-399` — CONFIRMED).

**THE LIGHTING PRECONDITIONS, BOTH DIRECTIONS (ADDENDUM R2):** what this lane NEEDS lit before 7a/7b declare anything: (i) door D0 on the consist (car A: the `:367` arm re-cut and the EMPTY `SIM_LIT` cohort in the four lit presets; car B is not a precondition of THIS key — `tradePactsEnabled`'s row has no `commodityFlowEnabled` in `requiresForeignFlags`, `tradeConvergenceContract.js:337-346`); (ii) **`casusCommerciiEnabled` lit — lane 7 car 1 (TR-1's mount and LIT car) ON THE CONSIST** — because `tradePactsEnabled` has `requiresTradeFlags: ['casusCommerciiEnabled']` (`:337-341`) and `evaluateTradeFlagLighting` (`:497-510`) pushes `trade_precondition_dark` for every lit preset where it is dark. The first cut boarded lane 2 beside lane 1 and ahead of lane 7 (PRICING §C's first four), which would have lit `tradePactsEnabled` out of order in all four presets: **RULING (R2(ii), adopted): lane 2's cars 0–6 may board and build beside lane 1, but 7a/7b are built AT LANDING (R2(iii)) and compose AFTER lane 7 car 1** — this is the cleanest of the skeptic's two options (lighting `casusCommerciiEnabled` here would be a lit-but-unmounted key, the false-lit shape lane B refused). (iii) The three spine keys `pactFormationEnabled`, `beliefAxesEnabled`, `believedScarcityEnabled` are FOREIGN preconditions/conjuncts this lane lights ITSELF in 7a (PROTO §1.2 step 3), unless lane 3 (POP-1) or lane 7 car 5 composed first and lit a shared one — CHECK-GIT-FIRST. What 7b UNBLOCKS: `factorErrandsEnabled` (lane 56b — `requiresTradeFlags: ['tradePactsEnabled', 'venturesEnabled']`, `:362-367`) and, through 7a, every belief-fed TR key (`believedMarketsEnabled`'s `requiresForeignFlags: ['believedScarcityEnabled']`, `:322-328` — lane 7 car 5 inherits the lit spine if 7a composed first). At 7b's tip `evaluateTradeFlagLighting(SIMULATION_RULE_PRESETS[id].rules).ok === true` for all seven presets, quoted.

**Shared files this lane declares (PROTO §7.3 rule 1), slack at the cut:** `src/domain/worldPulse/peaceTermsCatalog.js` 115/800 · `src/domain/worldPulse/peaceTerms.js` **797/800 — ZERO EDITS in this lane** (every seam is leaf-first; measured with the enforcer's Linter at the publishing commit) · `src/domain/worldPulse/pactFormation.js` 426/800 (GRAMMAR's; two seam lines) · `src/domain/worldPulse/pactTriggers.js` 133/800 (read, not edited) · `src/domain/worldPulse/treatyEnforcement.js` 113/800 (read; the generic `grantedRightFor` is consumed, not edited) · `src/domain/worldPulse/commercialReasons.js` 289/800 (the evidence lift, car 2) · `src/domain/worldPulse/commercialReceiptPools.js` 386/800 (car 5) · `src/domain/worldPulse/demographicsRates.js` (one gated read, car 2) · `tests/domain/peaceTermsGrantTerms.test.js` + `tests/domain/sovereigntyBundleWr10.test.js` (the two designed red-flips) · the flag-mint surfaces + `SIM_LIT` + `subsystemRowsTrade.js` · `tests/property/pactFormationDormancyFence.test.js` and `tests/property/believedWorldAxesDormancyFence.test.js` (re-chartered at 7a — GRAMMAR's and SPINE's fences, edited by the lane that lights their keys, PROTO §3.3; the GR-2 fence's five-member namer set is NOT widened) · `tests/lint/couplingInclusion.walker.test.js` (no pattern edit needed: every new leaf is named `commercial*`/`tradeDemand*` — `commercial[A-Z]` matches; `tradeDemand` does NOT match the TRADE pattern (`tradeRoute|tradeWar` only) — name the trigger leaf `commercialDemandTrigger.js` and the pattern is untouched). NOT touched: `treatyTransfer.js` (TR-4 car 5's), `sovereigntyBundle.js` (derives `TERM_FAMILIES` at call time — `commercial` already exists, so `catalogGrewSinceWr10()` does not move), `tradeWar.js` (T9: the exclusivity incumbent is a declared-degraded pre-pin, below).

---

## THE STATE OF THE RECORD

- **The history fold.** `HISTORY-TR-2026-09-15.md` does not exist under that name; the trade family's history pass is `…/recon-32-34/HISTORY-TRADE-2026-09-15.md` and it WINS over PRICING §B for TR. Its TR-5 row: **PART-BUILT — "the most built of the nine, and the recon row under-states it"**: GR-3a minted three of the five catalog rows (`exclusivity`, `market_access`, `toll_exemption` at `peaceTermsCatalog.js:285-287`, `family:'commercial'`, `executor:'seam'`); GR-2 built the demand crossing (`scoreTradeDemand` `pactTriggers.js:197-221`) AND mounted the whole peacetime lifecycle end to end (`pactFormation.js:260` → `draftPactSheet` `:318-352` → `advancePeacetimePacts` `:617` as a stage at `settlementLifecycleKernel.js:564` → `peaceTerms.js:715-730` → `treatyOrientation.js`'s negotiated arm `:256-275` → `treatyTransfer.js:100/:129`) — two courts at peace CAN sign and deliver a conserved grain pact today, flag dark. The NEAR-FREE CAR: the crossing returns the good category as its subject (`pactTriggers.js:218-221`, the fourth argument to `crossing(...)`), and `draftPactSheet` writes NO `good` key though the catalog documents one (`peaceTermsCatalog.js:123` AppraisedAsset, `:138` TermRecord). THE ONE-SHOT FINDING (b): the crossing compares two GENERATION-FROZEN constants (`scarcityGroundTruth` reads generator lists memoised per settlement, `beliefAxisSubjects.js:229-291`; `foodRatio` written only at `foodGenerator.js:337/:424`), so lighting `pactFormationEnabled` buys a pact at the first eligible tick that never re-prices — until lane 7's JOIN CAR wires live M6a stocks into the ground truth. **This brief declares it in car 0, car 7 and the shift ledger.** THE LEDGER'S ONLY TR STOP: ODQ §96.2 (`OWNER_DECISION_QUEUE.md:3650-3660`) — "TR-5's CHARTER IS FROZEN — destructive as written (it orders an already-fired tripwire re-trip, retirement of pins that no longer exist, and a row DELETE that would ERASE the GR-3 discharge record): no TR train compiles against it; the TR re-charter joins the family's first sitting with the round's cure text as its draft" — and NO re-charter sitting is recorded after. Car 0 is that sitting's record.
- **The cure text IS ALREADY IN THE VOLUME.** `docs/DESIGN_FP_ARCH_TR.md` §4 TR-5's block "THE WR-10 DISCHARGE — RE-CHARTERED ON MEASURED STATE (C-TLF-2…)" points (1)–(5) is the round's cure text merged into the volume; what is missing is the SITTING that adopts it and the heading's stale "⚠ RE-DERIVE the blockers at compile". The compiled programme `docs/DESIGN_FP_ARCHITECTURE.md` §5 #27 (line 1674) still says "peaceTermsCatalog gains NOTHING new here if GR-3 minted the five commercial rows as seams" — FALSE against the code (GR-3 minted THREE; TR-5 mints TWO). The design law `docs/DESIGN_FP_TRADE.md` §5 TR-5 (line 844; the five terms at `:850-862`; the pins) and §3 SEAM RULING ONE (the pact seam: "extends its CATALOG and replaces its TRANSPORT, never its MATH"; AMENDMENT-SHAPED formation R1 — one instrument per pair). The flag-dependency clause the walker parses: `DESIGN_FP_TRADE.md` line 271 `TR-5 ⇒ SP-3 landed + TR-1 (` → `tradeConvergenceContract.js:337-346` `requiresTradeFlags: ['casusCommerciiEnabled']`, `requiresForeignFlags: ['pactFormationEnabled']`, `requiresLandedWork: ['GR-3 commercial TERM_CATALOG rows']`.
- **The record's spelling hole, and the ruling.** `DESIGN_FP_TRADE.md:857-862` names `route_wardenship` as the fifth term; TR-1 SPENT that bare string as a PARTNERSHIP reason kind (`commercialReasonTaxonomy.js:66` and the mirror `:85`; the Herald kind `commercial_route_wardenship` at `settlementRumors.js:263`, `heraldRouting.js:241`, pool rows `commercialReceiptPools.js:205/:410`, pinned by `tests/lint/commercialKindPools.walker.test.js:72`). Two registries sharing one bare string is the "one catalog quietly becomes two" class (`subsystemRowsBelief.js:247`). **RULING (chair, vetoable, recorded in car 0):** the TERM is minted as `route_warden` (family `commercial`); the reason kind `route_wardenship` stays as landed; both docblocks cross-reference; a pin asserts neither string appears in the other's registry. `grain_provision` has zero hits tree-wide (free).
- **The frozen pending door** (`subsystemRowsVirtual.js:595`): the mint AUTHORS its row. **The FLAG-MINT BILL's founding precedent is TR-1** (ODQ §441.4).
- **The owner's laws:** everything built lands LIT; tuning values raw + derivation, ratified at the soak (the crossing thresholds, term budgets/durations, the exclusivity strain, the wardenship curve, the suspension floor — `DESIGN_FP_TRADE.md` §7); nothing pushes/deploys/applies/deletes/spends; a persisted-shape widening is TAKEN and recorded (this lane widens NONE: pacts ride the treaties artifact; the `good` key on a term is a documented TermRecord field already).

## THE STATE OF THE CODE (CONFIRMED at the consist)

| Module | eff/ceiling | What is there |
|---|---|---|
| `src/domain/worldPulse/peaceTermsCatalog.js` | 115/800 | `TERM_CATALOG` `:159-…` (26 rows, 13 families); the three commercial seams `:285-287` under the header `:266-284` ("GR-3 mints the ROWS; TR-5 lands the executors … TR-5 POINTS HERE; it must not re-mint under the other spelling"); `TermExecutor` union `'transfer'|'overlay'|'readiness_cap'|'war_block'|'occupation_hold'|'grant'|'seam'` `:104`; `TERM_TYPES` `:325` (sorted keys), `TERM_FAMILIES` `:328` (DERIVED); `TERM_EXECUTORS` `:348`; `CLASS_TERM` `:385-395`; `termLabel` `:398-431` (GR-3's crier labels — `exclusivity` "pledge of trade primacy", `market_access` "grant of market access", `toll_exemption` "exemption from tolls"); `humanTermGood` `:433` |
| `src/domain/worldPulse/peaceTerms.js` | **797/800** | the head; the stream loop `:715-730` (`spec.stream` ⇒ `computeTreatyGrainDraw`); `materializeCarriedTermSheet` `:211` (`clause.seam === true ⇒ term.seam = true` `:242`); mint-time executions `:584-586`; mounted from `dispositionChannels.js:63` |
| `src/domain/worldPulse/pactFormation.js` | 426/800 | `PACT_DRAFT_LENS` `:174-192` (`trade_demand: [{min:0, terms:['resource_share']}]`; `renewal: []` the GR-5 tombstone; "THE SIX BOUNDS ARE AUTHORED LITERALS AND THEY ARE UNSOAKED" `:166-171`); `selfBandsOf` `:211-218` (truth: `scarcityGroundTruth(row)`), `believedBandsOf` `:222-232` (only through `beliefRecord`); `crossingsFor` `:251-267` (the four scorers; `scoreTradeDemand({ladder: SCARCITY_BANDS, proposerBands: self.scarcity, counterpartyBands: believed.scarcity})` at `:260`); `rungTermsFor`; `draftPactSheet` `:318-352` (writes type/family/magnitude/mintedTick/expiresTick/weightSpent/complianceState/trueState/burden01/beneficiary/receipt; `stream ⇒ deliveredToVictor/extractedFromLoser = 0`; `seam ⇒ term.seam = true`; NO `good`); `advancePeacetimePacts` `:617-799` (gate `pactFormationActive(worldState)` `:625`; the proposal loop `:755-790`) |
| `src/domain/worldPulse/pactTriggers.js` | 133/800 | `PACT_TRIGGERS` `:60-62` (five, codepoint-frozen), `PACT_TRIGGERS_PRODUCED` `:68-70`; `PACT_TRIGGER_TUNING.DEMAND_RUNG_GAP` `:73-…`; `scoreTradeDemand` `:197-221` returns `crossing('trade_demand', gap/span, receipt, best.category)` |
| `src/domain/worldPulse/pactProposals.js` | 161/800 | `pactFormationActive` `:132-134` — THE ONE `pactFormationEnabled === true` in the tree. **THE FENCE'S NAMER CENSUS, STATED EXACTLY (CONFIRMED at `tests/property/pactFormationDormancyFence.test.js:198-214`):** fence 4 scans every `src/**/*.js(x)` with comments STRIPPED (`codeOf`, `:191-193`) and asserts the set of files whose CODE contains the token `pactFormationEnabled` is EXACTLY FIVE — `src/domain/certification/couplingRegistryGrammar.js`, `src/domain/certification/subsystemRowsCompact.js`, `src/domain/certification/tradeConvergenceContract.js`, `src/domain/worldPulse/pactProposals.js`, `src/domain/worldPulse/simulationRules.js` — four naming it as DATA (string members of frozen tables) and exactly one gating (`pactProposals.js`, `=== true`; `:236-239` the by-name count is 1). **THE CONSEQUENCE FOR THIS LANE:** any NEW src file whose code spells the token is a SIXTH namer and reds the census — so (a) `commercialDemandTrigger.js` never spells it (it reads `tradePactsEnabled` and imports `pactFormationActive` if it needs the host door); (b) TR-5's AUTHORED row in `subsystemRowsTrade.js` must not carry it in ANY string field (`preconditions`, `invariants`, `other`, `notes` — the natural, honest place to write "requires pactFormationEnabled" is exactly the sixth namer); a JSDoc/`//` comment MAY name it (stripped before the scan) — the row points at GR-2's row by lane name in prose, or names the key in a comment; (c) the TRADE→GRAMMAR coupling row in `couplingRegistryTrade.js` (car 1) lists `flags: ['tradePactsEnabled']` ONLY — a `flags` entry naming `pactFormationEnabled` (the natural mirror of `couplingRegistryGrammar.js`'s own rows) is the sixth namer; the GRAMMAR key is named in the row's comment. The alternative — admitting a sixth file to the exact list — edits GR-2's fence, which 7a already re-charters (its LIT arm), so if the row's evidence law truly needs the string, 7a admits `subsystemRowsTrade.js` to the list in the same commit and says so; the default is NOT to name it. `openProposalBetween` `:278` (one instrument per pair — the R1 amendment shape exists); `pactTransportFor` `:250` (TR-8's switch, not this lane's) |
| `src/domain/worldPulse/treatyEnforcement.js` | 113/800 | `grantTermFor` (beneficiary-aware) + `grantedRightFor` `:245-249` (the compliance door) + `grantedRightStateFor` `:258` — GENERIC over any type; the named readers `:263-300` (five faith + migration + labor) are pointers to it — **TR-5's three executors are this shape** |
| `src/domain/worldPulse/treatyOrientation.js` | 117/800 | `termObligationOf` `:256-275` — the NEGOTIATED arm resolves obligor/obligee from `beneficiary` |
| `src/domain/worldPulse/commercialReasons.js` | 289/800 | `commercialEvidenceFor` `:283-335` — `marketClosed: NO_TRADE_RELATIONSHIPS.has(...)` `:305`, `toll01` from the entrepot record `:310-312`, `dependence01`, the seam keys `:333-335`; `scoreMarketAccess` `:203`, `scoreToll` `:183`, `scoreDependency` `:222` |
| `src/domain/worldPulse/demographicsObservation.js` | 96/800 | `realmPressure01` `:137-139` = `clamp(population / capacity, 0, PRESSURE_MAX)`; consumers ONLY `warReasons.js:801-805` + `demographicsWar.js:172/:295-312`; `measureRealmDemography` is gated by `demographicsActive(ws)` at `warReasons.js:802` |
| `src/domain/worldPulse/demographicsRates.js` | — | `:268-280` the admission ("No standing food-export treaty quantity exists in the tree yet" per the volume's S46); `demographicsActive` `:102-105`; `foodCapacityOf` `:548` |
| `src/domain/worldPulse/tradeSalience.js` | 252/800 | `pairTradeSalience` `:321`, `commodityTradeSalience` `:255` — the "salient good" read |
| `src/domain/worldPulse/strategicPosture.js` | 136/800 | `courtPostureOf` — a real caller at `pactFormation.js:110` (SP-C's "zero callers" is stale) |
| `src/domain/worldPulse/negotiationPictures.js` | 303/800 | THE one terms evaluator — CONSUMED, never duplicated (seam 3) |
| `src/domain/worldPulse/treatyClock.js` | — | `:6-7` `LEGACY_TREATY_TICKS_PER_YEAR = 12`, `CURRENT_TREATY_TICKS_PER_YEAR = INTERVAL_WEEKS.one_year` — the identity pin names 52, never 12 |
| `src/domain/worldPulse/simulationRules.js` | 352 eff | the register (35, all dormant); `pactFormationEnabled` `:316-324` (GR-2's entry: "THE GATE SITS AT THE LEDGER'S ONE WRITER"), `beliefAxesEnabled` `:242`, `believedScarcityEnabled` `:252` (SP-B: "a CONJUNCTION with beliefAxesEnabled"); `infoMode` DEFAULT `'omniscient'` `:56`; `living_realm` `infoMode: 'perfect_delayed'` `:857`, `full_simulation` `'full'` `:930`; `realistic_regional` and `dramatic_campaign` inherit omniscient; `warLayerEnabled: true` only at `:809` (dramatic) and `:873` (full) |
| `src/domain/worldPulse/beliefMap.js` | 781/800 | `beliefsActive` `:204-208` — `infoModeOf(rules) !== 'omniscient'` AND the spatial marker; `beliefRecord` `:225-231` reads `spatialLedgers.beliefMaps` — so in an omniscient preset `believedBandsOf` returns `{}` and `scoreTradeDemand` returns NO_CROSSING by construction |
| the two designed red-flips | — | `tests/domain/peaceTermsGrantTerms.test.js:272-282` (`expect([...commercial].sort()).toEqual(['exclusivity','market_access','toll_exemption'])`, every executor `'seam'`, none produced, none in `PRODUCER_OWED`) and `tests/domain/sovereigntyBundleWr10.test.js:478-487` (the same set equality; `expectAbsentWithAnchor(Object.values(CLASS_TERM), 'exclusivity', 'tribute', …)`) — both red BY DESIGN when two rows land; `peaceTermsGrantTerms.test.js:284-…` "every one of the twelve new rows has an authored crier's label" — a TABLE the two new rows join |
| the fences | — | `tests/property/pactFormationDormancyFence.test.js` (GR-2's four fences; fence 4 `:179-241` EXACTLY ONE module names the key; the DORMANT arm `:239`; the three guard doors `:262-…`); `tests/property/believedWorldAxesDormancyFence.test.js` (SP-B ×3 keys; the spy on `beliefAxisSubjects.js`); `tests/domain/pactKernelMount.test.js` (stage order `['market','pacts','demo']` `:167/:174/:181`; lane 7 extends it, this lane does not) |
| ABSENT | — | `commercialTermExecutors.js`, any trade-demand trigger leaf, `tradePactsEnabled` (zero hits), `grain_provision` (zero), `route_warden` (zero) |

**Budgets (PROTO §4.1):** worker 1,404,493 EXACT — none of this lane's files is in the worker graph (worker `stat` must not move); `engine` chunk ±0 expected (worldPulse leaves ride the advance worker) — QUOTED; first-paint: the mint car's FOUR `SIM_LIT` lines (≈120–240 B, PLAUSIBLE) — QUOTED, the margin being contested.

## ALREADY DONE (do not re-land)

GR-2 the whole peacetime lifecycle (mounted, dark) · GR-3a the three commercial rows (seams) + GR-3b the faith/population rows with their `executor:'grant'` readers (the SHAPE this lane copies) · `catalogGrewSinceWr10()` FIRED and pinned (`sovereigntyBundle.js:56-80`) — **VERIFY, never re-run** (J-FP-1) · SP-B's scarcity family + `SCARCITY_BANDS` · SP-C's posture read with its live caller · TR-1 whole · the `treatyClock` 52-week identity · `termObligationOf`'s negotiated arm · `openProposalBetween` (R1's one-instrument-per-pair already exists — car 3 PINS it, never re-implements it).

---

## THE CARS

Every car: one gated commit; anchored negatives; executed mutants manifest-rowed; NUL-scanned files; porcelain 0; `Seat: Opus 5 — Fable-unvalidated` + `Lane: TR-5`; PACKET_STANDARD caps.

### Car 0 — the §96.2 charter RE-CUT recorded (docs-only)

**In plain words:** the ledger froze this wave's old instructions because following them would have deleted a landed record; the corrected instructions are already written into the design volume but no sitting ever adopted them — this car is that adoption, on the dock's copies, so the wave compiles against a charter the ledger recognises.

**The change (dock copies; the ledger copies are the chair's at collection, and the chair writes the ODQ entry "§932.x — TR-5 RE-CHARTERED; §96.2 DISCHARGED"):** (1) `docs/DESIGN_FP_ARCH_TR.md` §4 TR-5 heading: "⚠ RE-DERIVE the blockers at compile…" → "RE-CHARTERED 2026-09-15 (§932 lane 2 car 0; ODQ §96.2 discharged): the C-TLF-2 block below IS the charter"; the block's (1)–(5) kept VERBATIM; two amendments appended as (6)–(8): (6) the spelling ruling (`route_warden`, the collision recorded with the five addresses above); (7) the ONE-SHOT declaration ("until TR-3's join car (lane 7 car 2) wires live M6a stocks into `scarcityGroundTruth`, the trade-demand crossing compares generation-frozen bands: a lit lane forms a pact at the first eligible tick and re-prices only when the pact expires; this is declared, not a defect"); (8) the LIGHTING SCOPE ("this lane lights `tradePactsEnabled` AND its conjuncts `pactFormationEnabled`, `beliefAxesEnabled`, `believedScarcityEnabled` in the four lit presets; belief-fed crossings run only where `infoMode !== 'omniscient'` — living_realm, full_simulation; `shared_threat` runs in all four"). (2) `docs/DESIGN_FP_ARCHITECTURE.md` §5 #27: "peaceTermsCatalog gains NOTHING new here if GR-3 minted the five" → "GR-3 minted THREE (`exclusivity`, `market_access`, `toll_exemption`); TR-5 mints TWO (`grain_provision`, `route_warden`) — measured at `peaceTermsCatalog.js:285-287`, 2026-09-15" (amended, never deleted). (3) `docs/DESIGN_FP_TRADE.md:857-862`: `route_wardenship` (the term) → `route_warden` with a dated bracketed correction — ⚠ the walker `tradeConvergenceContract.walker.test.js:148-175` parses `DESIGN_FP_TRADE.md` §3's clause heads EXACTLY ONCE; §5's term list is not a clause head, but run the walker and `tests/lint/commercialKindPools.walker.test.js` (it reads the TRADE annex) to prove nothing moved.

**Files:** the three docs (dock copies). **Pins:** none new; the doc walkers stay green. **Census bills:** none. **Byte rule:** no src byte — state so. **Gate:** tests/lint (the doc walkers) · tests/copy · check:quick. **LIT act:** none. **Exit:** the walkers green; the three amendments present exactly once each (grep counts quoted).

### Car 1 — the two catalog rows, their crier labels, the `good` thread, the draft-lens rungs, the membership pin, the two red-flips re-pointed

**In plain words:** the treaty catalog learns two new trade clauses — a standing grain supply and a road-patrol promise — every drafted trade pact now names WHICH good it is about, and the two tests that were written to go red the day this happened are re-pointed to the five-clause truth.

**The change.** `peaceTermsCatalog.js` (+~14 eff): `grain_provision: Object.freeze({ family: 'commercial', weight: 0.8, baseYears: 3, maxYears: 8, baseMag: 0.2, stream: true, executor: 'transfer' })` and `route_warden: Object.freeze({ family: 'commercial', weight: 0.5, baseYears: 4, maxYears: 10, baseMag: 1.0, stream: false, executor: 'grant' })` — ⭐ the four numeric fields TRANSCRIBED from `DESIGN_FP_TRADE.md` §5/§7's bands where named, otherwise RAW with a one-line derivation in the row comment (the WC-0D idiom at `:288-…`: "TRANSCRIBED, NOT AUTHORED (ODQ §59.3); the chair signs at collection"); `termLabel` gains `case 'grain_provision': return 'grain compact'; case 'route_warden': return 'warden of the road';` (house voice, the GR-3 comment's law). `pactFormation.js` (+2 seam lines, GRAMMAR's file — a CW-0 coupling row in `couplingRegistryTrade.js` lands in the SAME commit, the same-commit law of `couplingRegistryTrade.js:9-13`; ⛔ the row's `flags` column lists `['tradePactsEnabled']` ONLY — the natural mirror of `couplingRegistryGrammar.js`'s rows would add `pactFormationEnabled`, and that string in CODE makes `couplingRegistryTrade.js` the SIXTH namer that `tests/property/pactFormationDormancyFence.test.js:198-214` forbids (an exact five-file set, comments stripped); the GRAMMAR key is named in the row's COMMENT): `draftPactSheet` writes `good` when `spec.stream` and the crossing carried a subject (`best.subject` from `crossingsFor` — pass it through `draftPactSheet`'s input as `subject`; conditional and drop-when-absent on the `assetId` precedent, so every treaty ever minted stays byte-identical); `PACT_DRAFT_LENS.trade_demand` gains rungs `{min: 0.5, terms: ['market_access']}`, `{min: 0.75, terms: ['market_access', 'toll_exemption']}` and — for a `food` subject — `grain_provision` REPLACES `resource_share` at rung 0 (the replacement is spelled as a pure `termsForSubject(trigger, score01, subject)` in a NEW TR leaf `commercialPactLens.js` (≤80 eff) that `rungTermsFor` consults for the `trade_demand` trigger only, so GRAMMAR's table stays the authored literals it says it is). ⚠ THE SIX BOUNDS LAW (`pactFormation.js:166-171`): the new rung minimums are authored literals, UNSOAKED, raw in `COMMERCIAL_PACT_TUNING` with derivations.

**The pins.** THE TERM_CATALOG MEMBERSHIP PIN (`tests/domain/commercialTermCatalog.test.js`, NEW): `TERM_TYPES.filter(family === 'commercial').sort()` equals exactly `['exclusivity','grain_provision','market_access','route_warden','toll_exemption']`; each appears EXACTLY ONCE; `TERM_FAMILIES` unchanged (`commercial` already present — `catalogGrewSinceWr10()` must return the SAME value as before: assert it, the tripwire must NOT re-fire); THE COLLISION PIN: `'route_wardenship' in TERM_CATALOG` is false AND `'route_warden'` appears in neither `SEVERANCE_REASON_TYPES` nor `PARTNERSHIP_REASON_TYPES` (anchored on `route_predation`/`route_wardenship` being present there). THE TWO RED-FLIPS RE-POINTED: `peaceTermsGrantTerms.test.js:272-282` → the five-member set; the three GR-3 rows STILL `executor:'seam'` and producer-less at THIS car (the flip is car 2's — the test's "producer-less until TR-5 lands its executors" clause moves to car 2 with its wording updated to "until lane 2 car 2"); the two new rows asserted `'transfer'`/`'grant'` and REACHABLE through the lens (produced); `sovereigntyBundleWr10.test.js:478-487` → the five-member set, the `'tribute'` anchor KEPT (an anchor dropped makes the absence vacuous). The label table at `peaceTermsGrantTerms.test.js:284-…` gains the two rows. THE GOOD-THREAD PIN: a `trade_demand` crossing with subject `food` drafts a `grain_provision` carrying `good: 'food'`; a `shared_threat` sheet carries no `good` key (drop-when-absent).

**Negative controls (executed, restored md5-exact):** a sixth commercial row planted → the membership pin reds; `route_wardenship` planted into `TERM_CATALOG` → the collision pin reds; `good` written unconditionally → the drop-when-absent pin reds on the security sheet.

**Files:** `peaceTermsCatalog.js`, `pactFormation.js` (2 lines), `src/domain/worldPulse/commercialPactLens.js` (NEW), `src/domain/certification/couplingRegistryTrade.js` (the TRADE→GRAMMAR row), `tests/domain/commercialTermCatalog.test.js` (NEW), the two red-flip tests, `tests/domain/pactFormation.test.js` (the good-thread arm). **Census bills:** mutation manifest ×2; refreeze own commit. **Byte rule:** build + verify:dist quoted; worker unchanged; engine ±0; closure ±0. **Gate:** the full list (tests/property includes `pactFormationDormancyFence` — its fence 1 dark identity must hold: the new rows are drafted only under a lit flag). **LIT act:** none. **Exit:** the five rows; the two red-flips green in their re-pointed form; the tripwire value unchanged; WF-6 (lane 5) may now board on `peaceTerms*.js` once this car is ON THE CONSIST (report it to the chair by name — PRICING §A.4.3).

### Car 2 — `commercialTermExecutors.js`: the three seams become producers; the readers bite

**In plain words:** the three trade-rights clauses stop being empty registrations — a court that holds a market-access grant no longer counts as shut out, a toll exemption zeroes the toll grievance, a primacy pledge quiets dependency fear, and the standing grain supply becomes a quantity another engine can read.

**The change.** `commercialTermExecutors.js` (NEW, ≤200 eff; `commercial[A-Z]` matches the TRADE pattern): `marketAccessFor(w, grantorId, granteeId, tick)`, `tollExemptionFor(w, gateId, travellerId, tick)`, `exclusivityFor(w, grantorId, granteeId, tick)`, `routeWardenFor(w, wardenId, wardId, tick)` — each a pointer to `grantedRightFor` (the GR-3b shape, `treatyEnforcement.js:263-300`), plus `standingFoodExportFor(w, fromId, toId, tick)` → `{ monthsPerYear, good, complianceState } | null` read off a live `grain_provision` term (magnitude × `TICKS_PER_YEAR`-denominated — the `demographicsRates.js:268-280` admission PAID as a READER). Flip the three GR-3 rows to `executor: 'grant'` (the `TERM_EXECUTORS` roster already carries `'grant'`; `peaceTermsGrantTerms.test.js`'s seam clause from car 1 retires HERE and the reachability obligation moves into this file's pins). CONSUMERS (the bite): `commercialReasons.js` `commercialEvidenceFor` (+4 lines): `marketClosed` becomes false when `marketAccessFor(w, toId, fromId, tick)` holds (the `market_opened` mirror then scores — TR-1's own bijection does the rest); `toll01` becomes 0 when `tollExemptionFor(w, toId, fromId, tick)`; `exclusivityHeld` enters `scoreDependency`'s evidence as comfort (the pledge of primacy is the counterparty's promise not to cut the tie — PLAUSIBLE design; the volume's tradeWar incumbent is DECLARED DEGRADED here as a pre-pin: `tradeWar.js` contains no `exclusivityFor` read and the pin asserts that absence anchored on its existing `makeCommercialPressureRead` import, red the day a lane adds it without a coupling row); `demographicsRates.js` `foodCapacityOf` (+3 gated lines under `demographicsActive`, dark everywhere today ⇒ byte-identical): a standing export ADDS to the importer's food capacity by its months-per-year share — with a CW-0 coupling row TRADE→POP in the same commit. The `route_warden` executor is a READER only at this car (patrol obligation priced against leg loss — its consumer is WY-3's mover bodies, declared to WY as a pre-pin).

**Pins:** each reader true/false on a drafted-and-signed fixture pair (through `signPactProposal` `pactFormation.js:444`, never a hand-built ledger); the compliance door (a `defaulted` clause grants nothing — `grantedRightFor`'s own law, asserted for each); the evidence lift: a `market_partner`-closed pair with a live grant scores `market_opened`, not `market_exclusion`; the toll lift: a gouging entrepôt with an exemption scores zero toll; the dark twin: no grant ⇒ every evidence field byte-identical to the pre-car engine (the `commercialReasons.test.js` fixtures unchanged). Mutants: compliance door severed (a defaulted grant still lifts) → reds; the evidence lift inverted → reds.

**Files:** the new leaf, `peaceTermsCatalog.js` (three `executor` words), `commercialReasons.js` (+4), `demographicsRates.js` (+3), `couplingRegistryTrade.js` (two rows), `tests/domain/commercialTermExecutors.test.js` (NEW), `peaceTermsGrantTerms.test.js` (the clause retired), `tests/domain/commercialReasons.test.js` (the lift arms). **Census bills:** mutation manifest ×2; refreeze own commit. **Byte rule:** build quoted; worker unchanged; engine ±0. **Gate:** full list. **LIT act:** none. **Exit:** five readers pinned; `producedTypes()` includes the five; the pre-pin toward tradeWar green; no dark fixture moved.

### Car 3 — `commercialDemandTrigger.js`: the hunger arm lands (the DEAD CONSUMER), amendment-shaped formation pinned

**In plain words:** "buy the grain before you bleed for it" gets its code path — a realm under population pressure asks its neighbours for trade before it asks its army — and a pact is always an amendment to the pair's one standing instrument, never a second piece of paper.

**The change.** `commercialDemandTrigger.js` (NEW, ≤150 eff): `tradeDemandFor({worldState, snapshot, fromId, toId, believedGap, tick})` composes the crossing's modifier: `realmPressure01`'s hunger arm (the dead consumer — read through `measureRealmDemography(demographyMembersFromSnapshot(snapshot), ws).realmPressure01` under `demographicsActive(ws)`, exactly as `warReasons.js:801-805` reads it; dark demographics ⇒ 0 ⇒ the crossing is GR-2's verbatim) × the salient good (`pairTradeSalience`'s dominant tie, `tradeSalience.js:321`) × posture (`courtPostureOf` — an insular seat's refused good pact is priced in a missed-relief receipt, never forbidden: Req 13, one line). `pactFormation.js` `crossingsFor` (+1 seam line at `:260`): `scoreTradeDemand({ …, demand: tradePactsActive(rules) ? tradeDemandFor(...) : null })` — and `scoreTradeDemand` (`pactTriggers.js`, GRAMMAR's leaf, +3 lines) folds `demand.hunger01` into `score01` and `demand.receipt` into the receipt when present, byte-identical when null. ⚠ `tradePactsActive(rules)` is THE ONE by-name read of `tradePactsEnabled` and it lives in `commercialDemandTrigger.js`; ⛔ THE NAMER SET, STATED PRECISELY (the skeptic corrected the first cut's "exactly ONE module names it"): `tests/property/pactFormationDormancyFence.test.js:198-211` asserts an EXACT FIVE-MEMBER set of `src/` files whose CODE (comments stripped by `codeOf`, `:189-191`) contains the token `pactFormationEnabled` — `certification/couplingRegistryGrammar.js`, `certification/subsystemRowsCompact.js`, `certification/tradeConvergenceContract.js`, `worldPulse/pactProposals.js`, `worldPulse/simulationRules.js` — with exactly ONE of them gating (`pactProposals.js`, `:212`). A SIXTH namer reds the GRAMMAR fence. THE PRACTICAL CONSEQUENCES FOR THIS LANE, each a natural mistake: (1) `commercialDemandTrigger.js`, `commercialPactLens.js`, `commercialTermExecutors.js` never spell the token in code (a JSDoc comment may — `codeOf` strips it); (2) the TR-5 certification row in `subsystemRowsTrade.js` must NOT carry `pactFormationEnabled` in ANY string field (`preconditions`, `invariants`, `other` — the place an honest row would name its host; write "GR-2's pact-formation key" in prose or put the key in a comment); (3) the CW-0 coupling rows this lane adds to `couplingRegistryTrade.js` list `flags: ['tradePactsEnabled']` ONLY (the TR1 row's shape at `:63`, `flags: ['warLayerEnabled', 'casusCommerciiEnabled']`) — the GRAMMAR key goes in the row's comment, never its `flags` column; (4) 7a (the spine-lighting car) writes the `SIM_LIT` line inside `simulationRules.js`, which is ALREADY a namer, so the set does not grow. The fence's list is NOT widened by this lane; a `grep -rlw pactFormationEnabled src | wc -l` = 5 is asserted at every car's exit. AMENDMENT-SHAPED FORMATION: pinned (not re-implemented) — `openProposalBetween` refuses a second open proposal for a pair; a signed pair's next crossing AMENDS via `pactAmendment.js`'s road when GR-5's window exists, else waits (the tombstone `renewal: []` is GR-5's; CHECK-GIT-FIRST whether GR-5 has landed the renewal trigger; TR-5 adds NO fifth trigger). Every term names duration in 52-week years with the `INTERVAL_WEEKS` identity pin (`treatyClock.js:6-7`; the pin names 52 and asserts `expiresTick − mintedTick === baseYears × TICKS_PER_YEAR`).

**Pins / negatives:** below-threshold hunger mints NO proposal on both arms (a positive control first: a high-pressure pair with a believed gap crosses); the seeded-positive → severed `realmPressure01` → no crossing where the belief gap alone is under `DEMAND_RUNG_GAP`; one-instrument-per-pair: two crossings in two ticks yield ONE open proposal; the 52 identity. Mutants: the hunger term dropped → the pressure arm reds; the amendment guard removed → the one-instrument pin reds.

**Files:** the new leaf, `pactFormation.js` (+1), `pactTriggers.js` (+3), `couplingRegistryTrade.js` (the TRADE→POP realm-pressure row), `tests/domain/commercialDemandTrigger.test.js` (NEW), `tests/domain/pactTriggers.test.js` (the demand arm). **Census bills:** manifest ×2; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list — `pactFormationDormancyFence` fence 3's spy sits on `pactTriggers.js`: the dark count stays ZERO (the seam is inside the lit path only). **LIT act:** none. **THE MINT/LIGHTING SPLIT, READ UNDER ADDENDUM R3 (recorded, vetoable):** `tradePactsEnabled === true` is read by name in `commercialDemandTrigger.js` from THIS car, and `engineGatedRuleKeys.walker.test.js`'s `scanGateReads` (`:538-541`) reads every `src/` file regardless of callers, so `auditEngineGatedKeys` (`:496-497`) would red `unaccountedReads` unless the register entry lands in the same commit — R3 lets that DARK MINT land early. The estate's one-commit law drags the rest with it: a register entry without its row reds `manifestWithoutRow` (`:512-520`; `VIRTUAL_PENDING_RULE_KEYS` is `[]`, pinned `toEqual([])` at `subsystemRowsVirtual.test.js:647`), a row without its `VIRTUAL_RULES` position reds `:646`, both moved lengths red `contributionLedgerShape.test.js:118-119`, `coveringArrayCoverage.test.js`'s `union` counts register members, and any `simulationRules.js` byte stales the two edge bundles. So THIS car pays in ONE commit: the register entry (tail, DARK — the key in NO preset), the gate read, the AUTHORED row in `subsystemRowsTrade.js` (CHECK-GIT-FIRST for lane 1's file; ⛔ the row's string fields never spell `pactFormationEnabled` — the sixth-namer trap, THE STATE OF THE CODE), the three `subsystemRowsVirtual.test.js` edits, BOTH `contributionLedgerShape` literals READ OFF HEAD at this car's base, the covering-array `union`, the four edge-shared files. 7b (LAST, rebased) pays the LIGHTING and RE-READS the literals and census figures off the composed HEAD (7a will have moved `union` by three for the spine keys; the literals move only for NEW register members — the three spine keys are already registered). This is R3 applied as far as the walker allows; the departure from R3's literal list is forced by `manifestWithoutRow` and recorded here for the chair. **Exit:** the hunger arm crosses; dark identical; the mint registered dark (`ENGINE_GATED_DORMANT_RULE_KEYS` CONTAINS `tradePactsEnabled` at this car — measured).

### Car 4 — the T-5 tellable end to end, its dark twin, the K3 absurdity twin, the lean-year fork

**In plain words:** one story pinned five systems deep — a pact forms, grain is delivered, the realm's pressure falls, the war-motive weight falls — with the same world proved unchanged when the flag is off; a pact signed for grain the seller never had becomes a grievance rather than a silent nothing; and a bad harvest suspends the delivery in a way the buyer reads as hardship or as betrayal depending on what it believes.

**The change (tests + ≤2 leaf lines):** T-5 (`tests/domain/tradePactTellables.test.js`, NEW): a two-court fixture under `full_simulation` rules with `pactFormationEnabled`, `beliefAxesEnabled`, `believedScarcityEnabled`, `tradePactsEnabled`, `demographicsEnabled` lit LITERALLY (the mechanismLitCoverage credit rule: a literal `<flag>: true` in a test) → the pact forms (receipt) → the stream installment lands (`deliveredToVictor` > 0, `storageMonths` moves through the ONE applicator) → `realmPressure01` falls on the re-measure → the war-motive weight falls (`warReasons.js:801-853` READ ONLY — a war-chair queue row per touch; no war-path edit) — receipts at every link; THE SAME FIXTURE DARK (`tradePactsEnabled` absent) byte-identical. K3: a `grain_provision` signed for grain the seller no longer has (drain the seller's `storageMonths` to the reserve floor after signing) ⇒ the installment defaults ⇒ `complianceState` slips ⇒ the breach grading (`treatyBreach.js`) mints the next grievance (`contract_default` — and HERE TR-1's `seam_contract_default` gains its PRODUCER: the lifecycle mount's `seams` object (lane 7 car 1) receives `{contract_default: {severance01, partnership01}}` from a `commercialSeamsFor(worldState)` registry this car creates in `commercialTermExecutors.js` — CHECK-GIT-FIRST: lane 7 may have created the registry; declare 6/16 → 7/16). The LEAN-YEAR SUSPENSION with the buyer-belief fork: a seller under famine suspends (`computeTreatyGrainDraw` returns null at the floor — existing); the BUYER's believed `storesBand` of the seller (SP-B `conditionsBands.storesBand` — dark unless `believedConditionsEnabled` is lit ⇒ the fork collapses to truth: declared) decides whether the suspension receipt reads hardship or breach. Negative controls: K3 twin with stock present → no grievance; the lean-year fork with omniscient infoMode → the belief arm collapses to truth. Mutants: delivery-not-lowering-pressure (the applicator bypassed) → T-5 link 3 reds; grievance suppressed → K3 reds.

**Files:** the tellables test, `commercialTermExecutors.js` (+the seams registry, ≤20), `tests/domain/commercialReasons.test.js` (the 7/16 declaration). **Census bills:** manifest ×2; refreeze own commit. **Byte rule:** build quoted. **Gate:** full list. **LIT act:** none. **Exit:** five links receipted lit; dark twin identical; K3 mints; the fork's two arms.

### Car 5 — the two TR-5 Herald kinds with the L6 five joins and their OWN walker file — DETACHED INTO SITTING 2b (the ≤ 8 rule, once car 7 became 7a/7b under ADDENDUM R5; boards after 7b on the consist; no flag act; nothing in cars 0–4/6/7a/7b depends on it)

**In plain words:** the Herald learns to say "They bought the grain before they bled for it" and "A grain compact: Aldenmoor's wagons for Thornwall's peace of mind" — each sentence with its full address chain, its phrase pool in the trade annex, its desk, and a permanent walker of its own.

**The change.** Kinds `commercial_pact_bought_peace` (T-5's headline, `significance: 'notable'`) and `commercial_grain_compact` (the signing, `'routine'`), registered in `commercialReasonsNews.js`'s `COMMERCIAL_KIND_REGISTRY` idiom (or a new `commercialPactNews.js` ≤120 eff if the registry's "twenty kinds" pins are exact — CHECK `tests/lint/commercialKindPools.walker.test.js:21-30`'s counts and extend them); pool rows in `commercialReceiptPools.js` (386 → ~424; ~19 eff per kind) mirroring `docs/content/RECEIPT_POOLS_TRADE.md` (the TRADE annex, `TRADE_ANNEX_URL` in `tests/helpers/receiptAnnex.js`) — the annex is the ONE reader (`anchoredOnce`); `WHAT_PHRASES` rows (`settlementRumors.js:123`); `heraldRouting.js` rows on the `trade` desk (`:226-244`'s block); the address chain (id + settlementIds + typed action + recorded reasons + audience) minted from `advancePeacetimePacts`' receipts (`pact_proposed`/signing) under `tradePactsActive` only; `tests/lint/commercialPactKindPools.walker.test.js` (NEW — the `envoyNews.js`/`commercialKindPools` template: pool verbatim, registry row + requiredSlots, WHAT_PHRASES, section authority, frequency-scaled floors); `tests/lint/heraldContaminationFence.test.js` (SP-6's truth law) passes for both. Prose-numerics: bands and words, no digits.

**Files:** ≈7. **Census bills:** the wiring census `node scripts/wiring-census.mjs` (a prose byte — `--check` reds otherwise); prose-numerics; manifest (the walker file); refreeze own commit. **Byte rule:** build quoted (the pools are lazy). **Gate:** full list + `tests/lint/commercialKindPools.walker.test.js`, `heraldContaminationFence`, `proseWiringCensus.walker`. **LIT act:** none. **Exit:** both kinds pass the five joins; the annex is the one reader.

### Car 6 — the CR-WR10-B amendment sweep (docs-only)

**In plain words:** three documents still say the trade-rights half of a war ruling is "HALF DISCHARGED… TR-5 STILL OWED"; now that the executors exist, each is amended to say DISCHARGED — amended, never deleted, and each amendment is asserted to appear exactly once.

**The change.** `grep -rn 'HALF DISCHARGED' docs` at the ledger measures THREE files (the volume said eleven consumer files — the ADDRESS COUNT rotted; measure at charter): `docs/DESIGN_FP_ARCH_TR.md`, `docs/DESIGN_WAR_RULINGS_ARCHITECTURE.md`, `docs/DW0-CHARTER-COMPILED.md`; plus `docs/DESIGN_FP_ARCHITECTURE.md`'s CR-WR10-B row (locate by `grep -n 'CR-WR10-B'`, the "retire-and-delete" instruction — amend to "amended, never deleted"). Each blockquote → "DISCHARGED at §932 lane 2 car 2 (executors landed; `producedTypes()` includes the five)"; the twin-degradation sentence stays BYTE-EQUAL across both volumes (PIN 6 of `sovereigntyBundleWr10.test.js` compares them — run it); every doc-reading pin asserts EXACTLY ONCE (the first-match retargeting trap, LT36). Dock copies only.

**Files:** the four docs. **Gate:** tests/lint · tests/copy · `tests/domain/sovereigntyBundleWr10.test.js` (PIN 6) · check:quick. **Byte rule:** no src byte. **LIT act:** none. **Exit:** grep counts quoted; PIN 6 green.

### Cars 7a and 7b — THE LIT CARS (ADDENDUM R5: the split IS the plan — the first cut's single car counted ≈13 handwritten files against PACKET_STANDARD's ≤12): 7a THE SPINE LIT (`pactFormationEnabled` + `beliefAxesEnabled` + `believedScarcityEnabled` in the four lit presets; the two foreign fences re-chartered; the GR/SP row texts); 7b THE PACTS LIT (`tradePactsEnabled` in the four lit presets; its own fence; the counts re-read) — both rebased at landing, 7a before 7b, 7b ALSO after lane 7 car 1

**In plain words:** two switches in two commits. First the peacetime pact engine and the believed-scarcity axis — one GRAMMAR's key, two SPINE's — are switched on for every world born into the four living presets, their permanent fences rewritten and the shift written down with its movers named; then the trade-pact lane itself is switched on, lawfully, because by then the trade-grievance ledger it builds on (lane 7's mount) and the spine under it are already on.

**⛔ THE LAWS BOTH CARS OBEY BY CITATION (`/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`):** R1 — the virtuality law is amended; `SIM_LIT` is the one lawful home of a lit FP key; the `tradeConvergenceContract.walker.test.js:367` arm is D0 car A's re-cut (this lane never edits it; 7b runs it and quotes it green with `tradePactsEnabled` in the cohort — the three spine keys are not TRADE keys and escape that arm, but L2-as-amended binds them the same way). R2 — THE OUT-OF-ORDER HOLE THE SKEPTIC FOUND: `tradePactsEnabled`'s row carries `requiresTradeFlags: ['casusCommerciiEnabled']` (`tradeConvergenceContract.js:337-341`) and `evaluateTradeFlagLighting` (`:497-510`) pushes `trade_precondition_dark` in any lit preset where `casusCommerciiEnabled` is dark; the first cut lit four keys and `casusCommerciiEnabled` was not among them, and the adopted boarding order (lane 1 ∥ lane 2 → lane 7) put lane 7 car 1 — TR-1's mount + LIT, the ONLY lane that may light that key (lighting it here would be a lit-but-unmounted key, the false-lit-claim shape lane B refused) — AFTER this lane. **Ruling: 7b composes AFTER lane 7 car 1 is on the consist** (cars 0–6 and 7a board in the first four as PRICING §C places them; the hold costs one compose slot, not a boarding slot, because lane 7 car 1 is a one-car act); 7b asserts `casusCommerciiEnabled` lit in all four presets at its tip and `evaluateTradeFlagLighting(SIMULATION_RULE_PRESETS[id].rules).ok === true` for all seven, negative control `{ tradePactsEnabled: true, pactFormationEnabled: true }` → exactly one `trade_precondition_dark`. 7a has NO TRADE precondition (the three spine keys are foreign) and composes with the lane after door D0. Both are built AT LANDING on the consist (R2(iii)). R3 — the dark mint of `tradePactsEnabled` was car 3 (register entry, gate read, row, the three test edits, both literals off HEAD at car 3's base); 7b is its LIGHTING and re-reads the counts; the three spine keys are ALREADY registered (GR-2's `:316-324`, SP-B's `:242`/`:252`) so 7a is LIGHTING-only for them — no literal moves at 7a.

**The change (PROTO §1.2 — the mint half of `tradePactsEnabled` landed in car 3; items 1–9 below carry a [7a]/[7b]/[both] tag naming the car that pays them):**
1. **[7a the three spine lines · 7b the `tradePactsEnabled` line] THE CONJUNCTION RULING (PROTO §1.2 step 3, vetoable).** `tradePactsEnabled` lit alone would light NOTHING: every commercial clause is drafted by `advancePeacetimePacts` behind `pactFormationActive`, and the `trade_demand` crossing reads `believedBandsOf` → `scarcityBands`, which exist only under `believedScarcityEnabled ∧ beliefAxesEnabled`. A lit key whose every consumer is dormant is the FALSE LIT CLAIM lane B refused. So 7a appends three lines to the `SIM_LIT` cohort (which EXISTS from door D0 car A — never re-mint it; CHECK-GIT-FIRST for lines other lanes appended): `pactFormationEnabled: true, // GR-2's key — lit by TR-5 car 7a (the first lane on its spine)`, `beliefAxesEnabled: true, // SP-B's host — lit by TR-5 car 7a`, `believedScarcityEnabled: true, // SP-B — lit by TR-5 car 7a`; and 7b appends `tradePactsEnabled: true, // TR-5 car 7b`. ⚠ `simulationRules.js` is already one of the FIVE `pactFormationEnabled` namers, so 7a's line does not widen the GRAMMAR fence's set (`pactFormationDormancyFence.test.js:198-211`). **CHECK-GIT-FIRST at the rebase:** lane 3 (POP-1, the believed road) may have lit `beliefAxesEnabled` (+`believedConditionsEnabled`) in the same landing; whichever LIT car composes FIRST writes the shared line and re-charters `believedWorldAxesDormancyFence`'s HOST arms; the second finds it present, writes only its own key's line, and re-charters only its own key's arms. The lane's report states which case it hit.
2. **[7a (a) (b) (d) · 7b (c)] WHAT THIS LIGHTS, DECLARED BY NAME (the movers):** (a) GR-2's whole peacetime pact lane in all four presets — `shared_threat` crossings (WR-6's alliance web, no belief needed) can now form non-aggression / mutual-defence pacts in `realistic_regional` and `dramatic_campaign` too; `faith_communion` and `migration_pressure` stay dormant (their axes `believedDevotionEnabled`/`believedConditionsEnabled` are dark unless a foreign lane lights them); (b) the belief axes' D-1 fields (`populationTrendBand`, `observanceLabel`, `beliefMap.js:345-346`) and the scarcity family on every belief record — ONLY where belief maps exist: `living_realm` (`perfect_delayed`) and `full_simulation` (`full`); in `realistic_regional` and `dramatic_campaign` (`infoMode` omniscient by preset) `beliefsActive` is false and BOTH keys are inert by construction (the witness must show ZERO `worldStateSha256` movement from THESE two keys there — but the pact lane's `shared_threat` WILL move those rows: attribute the movement per key with a two-key settling experiment: lit `pactFormationEnabled` alone vs all four); (c) the trade-demand crossing and the commercial clauses in the two fogged presets — ONE-SHOT until lane 7's join car (declared); (d) ES-0's three-door conjunction gains one lit door (`beliefAxesEnabled`; `espionageGate.js:80-81` still needs `errandSpineEnabled` and `espionageEnabled` — nothing espionage moves; assert with `tests/property/espionageDormancyFence.test.js` 21/21 quoted).
3. **[7a the three foreign comments · 7b `tradePactsEnabled`'s]** The register comments for the three foreign keys gain a LIT paragraph naming 7a and the four presets (PROTO §1.2 step 12); `tradePactsEnabled`'s entry likewise at 7b.
4. **[7a the two foreign fences · 7b `tradePactsDormancyFence`] The fences re-chartered (PROTO §3.2/§3.3):** `tests/property/pactFormationDormancyFence.test.js:239` DORMANT arm → the LIT form (VIRTUAL + REGISTERED + LIT in exactly the four THROUGH `SIM_LIT`, absent from the three — roster spelled from `SIMULATION_RULE_PRESETS`); fence 1's dark world → `quiet_local.rules`; fence 3's lit twin under `full_simulation` requires the scorer count to MOVE; the three guard doors unchanged; ⚠ fence 4's EXACT FIVE-MEMBER namer set (`:198-211`) is UNCHANGED — `simulationRules.js` is already a member, and nothing this lane authors may join it (THE STATE OF THE CODE row; car 3's list of the four natural mistakes). `tests/property/believedWorldAxesDormancyFence.test.js`: the `believedScarcityEnabled` arms (and the host `beliefAxesEnabled` arms if this lane is first) → LIT form; the `believedConditionsEnabled`/`believedDevotionEnabled` arms UNCHANGED (still dark) — the three-family independence goldens must still hold. At 7b: `tests/property/tradePactsDormancyFence.test.js` (NEW at car 3 in the dark shape) → LIT shape; its site census: `commercialDemandTrigger.js` the ONE `tradePactsEnabled === true`; its lit twin MOVES the trigger's call count under `living_realm`/`full_simulation` (the fogged presets, where `beliefsActive` is true) and stays ZERO under `realistic_regional`/`dramatic_campaign` (omniscient — `beliefMap.js:204-208`; the fog gate, a mechanism fact, NOT a lighting-table precondition, so it is no violation); the lighting-law arm (`ok: true` ×7; the negative control in the LAWS paragraph).
5. **[both — each car re-records for its own keys]** The witness re-recorded by hand (PROTO §1.4 row 1): 7a QUOTES the per-preset field diff against the post-D0 golden (base = the consist tip after door D0, never the slot's): `rulesSha256` + `litFlagCount` on the five rows; `worldStateSha256`/`settlementUpdatesSha256`/`wizardNewsSha256` on all four lit rows IF the 52-tick birth worlds have ≥2 courts with a shared threat (PLAUSIBLE; if none forms in 52 ticks say so honestly), ATTRIBUTED per key by the settling experiment (`pactFormationEnabled` alone vs all three); 7b re-records against 7a's golden: `rulesSha256`/`litFlagCount` on five rows, `worldStateSha256` only in the two fogged presets and only if a trade-demand crossing fires in 52 ticks (PLAUSIBLE not — the ONE-SHOT fact, item 2(c)); a moved DARK preset row is a STOP at either car.
6. **[both — one draft section per car]** The GOLDEN_SHIFT_LEDGER draft (PROTO §2.5): 7a's DECLARED-SHIFT sentence names the three spine keys, the four presets, the fog-gate fact and the GR/SP families informed; 7b's names `tradePactsEnabled`, the ONE-SHOT until lane 7's join car, the `casusCommerciiEnabled` precondition met at lane 7 car 1; each ends "the generation arm 525 of 525 bit-identical, the espionage fence 21/21, the first-paint closure +<n> B; no installed campaign re-labelled; no tuning value moved".
7. **[7a the foreign rows + census · 7b TR-5's row + the literals]** The census literals re-measured out of band (`coveringArrayCoverage.test.js:185-222` — 7a's three preset declarations and 7b's one may move `governed`/`ungoverned`; MEASURE by calling `flagDomainCensus` at each car); `contributionLedgerShape.test.js:118-119` re-read off HEAD at 7b (R3: the mint was car 3 at its own base; 7a moves NO literal — its three keys are already registered; another lane's dark mint composed between moves them — re-derive the "N → N+1" note on the composed tree); the edge bundles rebuilt at each car (four files; the clone → build → path-leak grep EMPTY → relink recipe); the certification rows: at 7a GR-2's row (`subsystemRowsCompact.js`, `pactFormationEnabled`) and SP-B's scarcity row (`subsystemRowsBelief.js`) gain ONE sentence each recording "LIT at §932 by TR-5 car 7a (the first lane on the spine)" — an edit inside a BODY leaf that changes no row's INDEX (text only) — and the GR/SP families are informed by the chair at collection; at 7b TR-5's row (car 3) `other` amended for LIT.
8. **[both]** `node scripts/prose-manifest-cells.mjs --out` base and tip → zero rows moved, quoted; the espionage fence 21/21 quoted.
9. **[7b] THE VIRTUALITY ARM, ASSERTED NOT EDITED:** `tests/lint/tradeConvergenceContract.walker.test.js:367` in D0 car A's re-cut form (ADDENDUM R1: absent from the defaults; present in a preset spread ONLY through `SIM_LIT`; every `SIM_LIT` key registered with a row) runs GREEN with `tradePactsEnabled` in the cohort — quoted; if the arm still reads "absent from every preset spread" at 7b's tip, the lane boarded before the door: STOP.

**7a Files (≈9 handwritten):** `simulationRules.js` (three `SIM_LIT` lines + three register comments), `tests/property/pactFormationDormancyFence.test.js`, `tests/property/believedWorldAxesDormancyFence.test.js`, the witness golden, `coveringArrayCoverage.test.js` (if moved), `subsystemRowsCompact.js` + `subsystemRowsBelief.js` (text), `docs/GOLDEN_SHIFT_LEDGER.md` (dock); regenerated: the four edge-shared files. **7b Files (≈8 handwritten):** `simulationRules.js` (one `SIM_LIT` line + one register comment), `tests/property/tradePactsDormancyFence.test.js`, the witness golden, `coveringArrayCoverage.test.js` (if moved), `contributionLedgerShape.test.js` (only if HEAD moved), `subsystemRowsTrade.js` (text), `docs/GOLDEN_SHIFT_LEDGER.md` (dock); regenerated: the four edge-shared files. `tests/lint/engineGatedRuleKeys.walker.test.js` untouched by either (no BACKLOG member here). **Census bills (each car):** the refreeze (own commit after each); the manifest (the fence entries — `tradePactsDormancyFence` from car 3; a `kind:'rationale'` for each re-charter if no new plant); its witness; its shift draft. **Byte rule (each car):** build + verify:dist; the closure delta of its `SIM_LIT` lines named in bytes (three at 7a, one at 7b); worker unchanged; engine ±0. **Gate (each car):** the full list + the witness, the stability test (its identity assertions must stay green — virtual keys only), the two freshness tests, the engine-gated walker (directions 1–3), the totality walker, the espionage fence, the preset-driven soak suites measured ALONE at load < 8; at 7b additionally `tests/lint/tradeConvergenceContract.walker.test.js` (the re-cut `:367` arm) and the lighting law `ok: true` ×7 quoted.

**Exit criteria:** 7a — `ENGINE_GATED_DORMANT_RULE_KEYS` lacks the three spine keys (measured); the two foreign fences green in the LIT shape with the five-member namer set unchanged; the witness re-recorded with the diff quoted and the per-key attribution stated; the shift draft written; the GR/SP notices in the report. 7b — `ENGINE_GATED_DORMANT_RULE_KEYS` lacks `tradePactsEnabled` (measured); `casusCommerciiEnabled` lit in all four at the tip (lane 7 car 1 on the consist); `evaluateTradeFlagLighting` `ok: true` ×7 quoted and the negative control red; the fence green in the LIT shape with the fog-gate twin; the `:367` arm green with the key in the cohort; the witness diff quoted; the shift draft; no red either car did not cause.

---

## THE FLAG-MINT CAR (last; the count from HEAD) — READ UNDER ADDENDUM R3

Law 2 ("the flag-mint car is the LAST car of each lane, rebased at landing, its manifest count taken from HEAD") means the LIGHTING car — 7b — under ADDENDUM R3 (`…/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md`). The DARK MINT of `tradePactsEnabled` lands at car 3 because the gate read is the hunger arm's seam (`engineGatedRuleKeys.walker.test.js:538-541` scans `src/` regardless of callers; `:496-497` reds `unaccountedReads`), and the estate's one-commit law (`subsystemRowsVirtual.js:73-81`; `:512-520` `manifestWithoutRow`; `subsystemRowsVirtual.test.js:646-647`; `contributionLedgerShape.test.js:118-119`) drags the row, the three test edits and both literals INTO car 3 with the register entry — recorded in car 3 as the one forced departure from R3's literal list. The three spine keys 7a lights are ALREADY registered (GR-2's `pactFormationEnabled` `simulationRules.js:316-324`; SP-B's `beliefAxesEnabled` `:242`, `believedScarcityEnabled` `:252`) and already rowed — 7a moves NO literal, only the census `governed`/`ungoverned` figures (preset declarations), which it re-measures out of band. So: **at car 3**, immediately before editing: `node -e "import('./src/domain/worldPulse/simulationRules.js').then(m => console.log(m.ENGINE_GATED_VIRTUAL_RULE_KEYS.length))"` and `VIRTUAL_SUBSYSTEM_ROWS.length`; both `contributionLedgerShape.test.js` literals = that + 1 — 35 → 36 if this is the first TR dark mint on the consist, higher if lanes 1/7 minted first; NEVER inherited from this brief. **At 7a and 7b** (REBASED at landing, built on the consist after door D0 and — for 7b — after lane 7 car 1; composed in the chair's order, 7a then 7b) the same two commands are RE-RUN and the literals, the census figures and the `subsystemRowsTrade.js` tail are re-verified against the composed tree; conflicts on the register tail, the `SIM_LIT` cohort (D0's empty cohort plus every lane's appended lines — lane 3 POP-1 and lane 7 car 5 may have written the shared spine lines first: CHECK-GIT-FIRST, write only what is absent), the two literals, the census figures and `subsystemRowsTrade.js` are EXPECTED and resolved by MEASUREMENT (PROTO §5.2), never OURS/THEIRS. The lane's report states the numbers it wrote at car 3, the numbers it re-read at 7a/7b, and the numbers it expects at the consist.

## OWNER-GATED, KEPT OUT

- Tuning: the two rows' four numeric fields, the lens rungs, the demand thresholds, the exclusivity strain, the wardenship curve, the suspension floor — RAW with derivations, ratified at the soak under THE PROMISE.
- The ransom/claim persistence ruling (OWNER-QUESTIONS item 8) — TR-8's, untouched here.
- Any WAR-path behavioural edit (Q2 stands: `warReasons.js` is READ ONLY; a war-chair queue row per touch); any `peaceTerms.js` byte; any raise of a ceiling; push/deploy/migration/deletion/spend; the golden master.
- Persisted shapes widened: NONE (the `good` key is a documented `TermRecord` field; pacts ride the treaties artifact) — recorded.

## RISKS

**Grep traps (RECHECK §5 + HISTORY, verbatim):** `route_wardenship` (TR-1's reason kind) vs `route_warden` (this term) — a grep for the volume's spelling finds the WRONG registry; `INSTR-1≠TR-1`; `cornering` is TR-1's; W-COIN is state money; `Surveyor` is the paid SKU; `market` in ODQ §87/§576/§629/§632 is the map programme; `toll` is entrepôt/gate/smuggle; `catalogGrewSinceWr10` — ALREADY FIRED, verify, never re-run; `contributionLedgerShape.test.js`'s title says "at 28", the literals say 35; the volume's "eleven consumer files" for CR-WR10-B measures THREE at the ledger — count, never inherit; `DESIGN_FP_ARCHITECTURE.md:2349` is a rotted address — locate `CR-WR10-B` by grep.

**Collision surfaces:** `peaceTerms*.js` (this lane FIRST; WF-6 boards after car 1 is on the consist; the T9 STOP on any second lane in flight); `pactFormation.js`/`pactTriggers.js` (GRAMMAR's — GR-4/GR-5 lanes are later; CHECK-GIT-FIRST for GR-5's renewal trigger); the belief spine keys (POP-1, lane 3 — the first-composed mint car wins the shared `SIM_LIT` line); the flag-mint surfaces (lanes 1 and 3); `commercialReasons.js`/`commercialReceiptPools.js` (lane 7's mount car touches `commercialReasons.js`'s CALLER, not the file — but lane 8 (TR-6) later feeds the same `seams` registry: CHECK-GIT-FIRST); `subsystemRowsTrade.js` (created by whichever TR mint composes first).

**The addendum, by path (cite, never re-derive):** `/private/tmp/claude-502/-Users-cstokes-Desktop-settlement-engine/f6ac0d98-bb14-48ae-88d1-c6887f1e2704/scratchpad/arch-932/ARCH-PROTOCOLS-ADDENDUM-1.md` — the virtuality law is amended by R1 (a `SIM_LIT` declaration is lawful; the `:367` arm is D0 car A's to re-cut — a lane that edits it collides with the door); this lane's LIT cars land after door D0 (7a) and, for 7b, after `casusCommerciiEnabled` is lit by lane 7 car 1 (R2(ii) names it: "`casusCommerciiEnabled` (lane 7) is lit before `tradePactsEnabled` (lane 2)"); R2(iii): both LIT cars are built at landing on the consist, never in the dock (in the dock the walker's `:367` arm is still the old one and `casusCommerciiEnabled` is dark — a dock-green 7b is a FALSE GREEN); R3: the dark mint (car 3) carries the register entry, the gate read, the row, the three test edits and both literals because `manifestWithoutRow` forbids a register entry without its row — 7b re-reads the counts; R5: 7a/7b is the plan, and the ≤ 8 rule then detaches car 5 into sitting 2b. ⚠ The addendum's `src/config/simulationRules.js` is `src/domain/worldPulse/simulationRules.js` at the consist (no `src/config/` copy exists — `ls` CONFIRMED).

**Load hazards:** `gate-mutex.sh` exits 0 after 40 polls — a gate line with no printed test count DID NOT RUN; dist-gated size suites skip without a build; `deno.lock` re-freeze before `--frozen`; `git grep` reads the INDEX without a rev; the soak suites under load — measure alone; a moved DRIVEN GOLDEN is a STOP (every `*DormancyGolden` drives literal rules — 7a's and 7b's preset declarations move none; a move means a kernel change under an absent key).

**Design hazards:** THE SIXTH-NAMER TRAP — `tests/property/pactFormationDormancyFence.test.js:198-214` asserts an EXACT FIVE-FILE set of src files whose CODE (comments stripped) spells `pactFormationEnabled` (`couplingRegistryGrammar.js`, `subsystemRowsCompact.js`, `tradeConvergenceContract.js`, `pactProposals.js`, `simulationRules.js`), so THREE natural, honest edits red it: the token in `commercialDemandTrigger.js` (import `pactFormationActive` instead), the token in any STRING field of TR-5's row in `subsystemRowsTrade.js` ("requires pactFormationEnabled" in `preconditions`/`other` — say it in a comment or by lane name), and the token in the `flags` column of the TRADE→GRAMMAR coupling row in `couplingRegistryTrade.js` (`flags: ['tradePactsEnabled']` only); the `'tribute'` anchor must survive the WR-10 pin's re-pointing; `TERM_FAMILIES` must NOT grow (`commercial` exists) or `catalogGrewSinceWr10()` re-arms a spent tripwire; `peaceTerms.js` takes ZERO bytes — the stream loop already executes any `stream: true` row; the `good` key is drop-when-absent (a security sheet with a `good` key moves every treaty golden); the executors are READERS (`grant`) not a second evaluator (seam 3: `negotiationPictures.js` is the one); the receipt pool split (386 → ~424 at car 5 — if a later TR lane pushes toward 800, SPLIT into a second leaf and teach the walker both, never squeeze).

## GATES (per car, verbatim)

focused · tests/lint · tests/copy · tests/property · tests/edgeFunctions · tests/security · tests/generators for generators (none — state so) · `sh scripts/gate-mutex.sh --run -- npm run build && npm run verify:dist` for any `src/` byte (quote the worker `stat`, the `engine-*.js` line, the first-paint table) · `npm run check:quick` — every line with a PRINTED test count; the lighting refreeze on a clean tree as its OWN commit after any car adding a test file/title; the mutation manifest per new invariant file; the wiring census stamp at car 5 (sitting 2b); prose-numerics at car 5 (2b); the edge bundles at cars 3, 7a and 7b; the Seat/Lane trailer; porcelain 0. At car 3 (the dark mint) additionally the engine-gated walker (directions 1–3), `subsystemRowsVirtual.test.js`, `contributionLedgerShape.test.js`, `coveringArrayCoverage.test.js`, the two freshness tests and the totality walker. At 7a and 7b (built on the consist after door D0 — ADDENDUM R2(iii); 7b after lane 7 car 1; never gated in the dock) additionally: the witness, the stability test, the two freshness tests, the engine-gated walker, the totality walker, the espionage fence 21/21, the soak suites alone at load < 8; at 7b additionally `tests/lint/tradeConvergenceContract.walker.test.js` (the D0-re-cut `:367` arm green with `tradePactsEnabled` in `SIM_LIT`) and `evaluateTradeFlagLighting` `ok: true` on all seven presets QUOTED with the `casusCommerciiEnabled`-dark negative control red.
